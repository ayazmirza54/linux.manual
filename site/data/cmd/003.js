window.LCL_BUCKET(3,{"2fa":{"n":"2fa","t":"command-line two-factor authentication code generator","s":[["TAGLINE","command-line two-factor authentication code generator"],["TLDR","**Add a new TOTP secret** for an account\n\n```2fa -add [account_name]```\n\n**Generate a TOTP code** for a specific account\n\n```2fa [account_name]```\n\n**List all configured accounts**\n\n```2fa -list```\n\n**Add a counter-based (HOTP) account**\n\n```2fa -add -hotp [account_name]```\n\n**Add an account that produces 8-digit codes**\n\n```2fa -add -8 [account_name]```\n\n**Generate codes for all TOTP accounts**\n\n```2fa```"],["SYNOPSIS","**2fa** [**-add** [**-7**|**-8**] [**-hotp**] _name_]\n\n**2fa** [**-list**]\n\n**2fa** [**-clip**] _name_"],["PARAMETERS","**-add**\n> Add a new account with the given name. Prompts for the base32-encoded TOTP/HOTP key.\n\n**-list**\n> List all configured account names.\n\n**-7**\n> When adding a key, generate 7-digit codes instead of the default 6.\n\n**-8**\n> When adding a key, generate 8-digit codes instead of the default 6.\n\n**-hotp**\n> When adding a key, treat it as a counter-based HOTP key rather than time-based TOTP.\n\n**-clip**\n> Copy the generated code to the system clipboard instead of printing it."],["DESCRIPTION","**2fa** is a command-line two-factor authentication agent that generates one-time passwords compatible with services like Google Authenticator. It supports both time-based (TOTP) and counter-based (HOTP) keys, producing 6-digit codes by default (or 7 or 8 with **-7**/**-8**). TOTP codes refresh every 30 seconds.\n\nThe tool reads keys in base32 format (the same format shown when setting up 2FA on websites). When run without arguments, it displays codes for all configured TOTP accounts. Specify an account name to get only that code.\n\nSecrets are stored in **~/.2fa** by default. The file should be protected with appropriate permissions as it contains sensitive authentication material."],["CAVEATS","Secrets are stored unencrypted in a plain text file. Ensure proper file permissions (**chmod 600**) on the storage file. System clock must be accurate for TOTP codes to work correctly; time drift will cause authentication failures."],["HISTORY","The **2fa** command was created by Russ Cox and released as an open-source Go utility. It provides a minimal Unix-philosophy approach to TOTP generation, designed for users who prefer command-line tools over mobile authenticator apps."],["INSTALL","```aur: yay -S 2fa```"],["SEE ALSO","[oathtool](/man/oathtool)(1), [pass-otp](/man/pass-otp)(1), [gpg](/man/gpg)(1)"],["RESOURCES","```[Source code](https://github.com/rsc/2fa)```"]],"e":["oathtool","pass-otp","gpg"],"x":"2fa -add [account_name]"},"alr-version":{"n":"alr-version","t":"Display Alire version and build information.","s":[["TAGLINE","Display Alire version and build information."],["TLDR","**Display alr version** and build information\n\n```alr version```\n\n**Display version** with diagnostic information\n\n```alr --version```"],["SYNOPSIS","**alr** **version**"],["DESCRIPTION","**alr version** displays the version number and build information for the Alire command-line tool. This is useful for troubleshooting, reporting issues, and verifying installation.\n\nAlire is a source package manager for Ada/SPARK projects, providing dependency management and build process automation."],["INSTALL","```apt: sudo apt install alire```\n\n```aur: yay -S alire```\n\n```nix: nix profile install nixpkgs#alire```"],["SEE ALSO","[alr](/man/alr)(1), [alr-help](/man/alr-help)(1)"],["RESOURCES","```[Source code](https://github.com/alire-project/alire)```\n\n```[Homepage](https://alire.ada.dev/)```\n\n```[Documentation](https://alire.ada.dev/docs/)```"]],"e":["alr","alr-help"],"x":"alr version"},"aptitude-create-state-bundle":{"n":"aptitude-create-state-bundle","t":"Bundle aptitude state for bug reports","s":[["TAGLINE","Bundle aptitude state for bug reports"],["TLDR","**Create** state bundle for bug reports\n\n```aptitude-create-state-bundle [bundle.tar.bz2]```\n\nCreate with **verbose** output\n\n```aptitude-create-state-bundle --verbose [bundle.tar.bz2]```"],["SYNOPSIS","**aptitude-create-state-bundle** [_options_] _output-file_"],["DESCRIPTION","**aptitude-create-state-bundle** creates a compressed archive containing aptitude's state information. This bundle is useful for submitting bug reports, as it captures the exact state needed to reproduce issues.\n\nThe bundle includes package lists, aptitude settings, and resolver state."],["PARAMETERS","**--force-bzip2**\n> Force bzip2 compression regardless of file extension.\n\n**--force-gzip**\n> Force gzip compression even if bzip2 is available.\n\n**--print-inputs**\n> Display the list of files and directories that would be included without creating the bundle.\n\n**-v**, **--verbose**\n> Verbose output"],["CAVEATS","Bundle may contain system-specific information. Used primarily for debugging aptitude issues. Large bundle sizes possible on systems with many packages."],["HISTORY","**aptitude-create-state-bundle** was added to help developers reproduce and debug aptitude dependency resolution issues."],["INSTALL","```apt: sudo apt install aptitude```"],["SEE ALSO","[aptitude](/man/aptitude)(8), [aptitude-run-state-bundle](/man/aptitude-run-state-bundle)(1)"]],"e":["aptitude","aptitude-run-state-bundle"],"x":"aptitude-create-state-bundle [bundle.tar.bz2]"},"archivemount":{"n":"archivemount","t":"Mount archive files as virtual filesystems via FUSE","s":[["TAGLINE","Mount archive files as virtual filesystems via FUSE"],["TLDR","**Mount** an archive to a specific mountpoint\n\n```archivemount [path/to/archive] [path/to/mount_point]```\n\nMount an archive as **read-only**\n\n```archivemount -o readonly [path/to/archive] [path/to/mount_point]```\n\n**Unmount** the archive filesystem\n\n```fusermount -u [path/to/mount_point]```"],["SYNOPSIS","**archivemount** [_options_] _archive_ _mountpoint_"],["DESCRIPTION","**archivemount** is a FUSE-based filesystem that mounts archives as virtual directories. It supports a wide variety of archive formats including tar, zip, cpio, ISO 9660, and compressed variants (gzip, bzip2, xz, lzma).\n\nOnce mounted, the archive contents can be accessed like a normal directory. This is useful for browsing or extracting specific files from large archives without fully extracting them."],["PARAMETERS","**-o readonly**\n> Mount the archive as read-only\n\n**-o nosave**\n> Do not save changes to the archive when unmounting\n\n**-o nobackup**\n> Do not create a backup before modifying the archive\n\n**-f**\n> Run in foreground (do not daemonize)"],["CAVEATS","Write support is experimental and may not work reliably with all archive formats. Modifications are buffered in memory and only written when unmounting, which can consume significant RAM for large changes. The archive format must be supported by libarchive."],["HISTORY","**archivemount** was created as part of the FUSE ecosystem to provide transparent access to archive contents. It leverages **libarchive** for format support and has been available since the mid-2000s."],["INSTALL","```apt: sudo apt install archivemount```\n\n```dnf: sudo dnf install archivemount```\n\n```aur: yay -S archivemount```\n\n```apk: sudo apk add archivemount```\n\n```brew: brew install archivemount```\n\n```nix: nix profile install nixpkgs#archivemount```"],["SEE ALSO","[fusermount](/man/fusermount)(1), [tar](/man/tar)(1), [mount](/man/mount)(8)"],["RESOURCES","```[Source code](https://github.com/cybernoid/archivemount)```"]],"e":["fusermount","tar","mount"],"x":"archivemount [path/to/archive] [path/to/mount_point]"},"auditctl":{"n":"auditctl","t":"Control and manage Linux audit system rules.","s":[["TAGLINE","Control and manage Linux audit system rules."],["TLDR","Show **status**\n\n```sudo auditctl -s```\n\n**List** all rules\n\n```sudo auditctl -l```\n\n**Delete** all rules\n\n```sudo auditctl -D```\n\n**Enable/disable** auditing\n\n```sudo auditctl -e 1```\n\n**Watch** file for changes\n\n```sudo auditctl -a always,exit -F arch=b64 -F path=/path/to/file -F perm=wa```\n\nWatch **directory** recursively\n\n```sudo auditctl -a always,exit -F arch=b64 -F dir=/path/to/dir/ -F perm=wa```\n\nWatch file with a filter **key** for easy searching\n\n```sudo auditctl -w /etc/passwd -p wa -k passwd_changes```\n\n**Delete** a specific rule\n\n```sudo auditctl -W /etc/passwd```"],["SYNOPSIS","**auditctl** [_OPTIONS_]"],["DESCRIPTION","**auditctl** controls the behavior and manages rules of the Linux Auditing System. It can enable or disable auditing, add or remove audit rules, and display the current audit status and configuration.\n\nRules can monitor specific files and directories for access, track system calls by process attributes, and filter events by user, group, or architecture. The tool communicates directly with the kernel audit subsystem to apply rules immediately, though these runtime rules are lost on reboot unless persisted to the audit rules file."],["PARAMETERS","**-s**\n> Display the audit system status\n\n**-l**\n> List all currently loaded audit rules\n\n**-D**\n> Delete all audit rules\n\n**-b** _backlog_\n> Set max number of outstanding audit buffers (kernel default: 64)\n\n**-e** _0|1|2_\n> Disable (0), enable (1), or lock (2) audit configuration. Locked config cannot be changed without reboot.\n\n**-f** _0|1|2_\n> Set failure mode: 0=silent, 1=printk, 2=panic\n\n**-r** _rate_\n> Set message rate limit in messages/sec (0=none)\n\n**-a** _list,action_\n> Append a rule to the end of a list (e.g., always,exit)\n\n**-d** _list,action_\n> Delete a matching rule from the specified list\n\n**-w** _path_\n> Place a watch on a file or directory for changes\n\n**-W** _path_\n> Remove a watch from a file or directory\n\n**-F** _field=value_\n> Add a field comparison (path, perm, arch, uid, pid, etc.)\n\n**-S** _syscall_\n> Specify a syscall name or number to audit (use with -a)\n\n**-k** _key_\n> Set a filter key on an audit rule for easier log searching\n\n**-p** _permissions_\n> Permissions filter (r=read, w=write, x=execute, a=attribute change)\n\n**-R** _file_\n> Read and execute auditctl commands from a file"],["CONFIGURATION","**/etc/audit/audit.rules**\n> Persistent audit rules loaded at boot by auditd. Rules added with auditctl are lost on reboot unless saved here."],["CAVEATS","Requires root privileges. Rules added with auditctl are not persistent across reboots; use /etc/audit/audit.rules for persistence. Excessive auditing can impact system performance."],["HISTORY","**auditctl** is part of the **audit** package, providing the Linux Audit Framework for security monitoring and compliance."],["INSTALL","```apt: sudo apt install auditd```\n\n```dnf: sudo dnf install audit```\n\n```pacman: sudo pacman -S audit```\n\n```apk: sudo apk add audit```\n\n```zypper: sudo zypper install audit```\n\n```nix: nix profile install nixpkgs#audit```"],["SEE ALSO","[ausearch](/man/ausearch)(8), [aureport](/man/aureport)(8), [auditd](/man/auditd)(8)"]],"e":["ausearch","aureport","auditd"],"x":"sudo auditctl -s"},"aws-ecs":{"n":"aws-ecs","t":"Manage Amazon Elastic Container Service resources","s":[["TAGLINE","Manage Amazon Elastic Container Service resources"],["TLDR","**List all ECS clusters**\n\n```aws ecs list-clusters```\n\n**List services** in a cluster\n\n```aws ecs list-services --cluster [cluster_name]```\n\n**Describe a service** with details\n\n```aws ecs describe-services --cluster [cluster_name] --services [service_name]```\n\n**List running tasks** in a cluster\n\n```aws ecs list-tasks --cluster [cluster_name]```\n\n**Run a new task** using a task definition\n\n```aws ecs run-task --cluster [cluster_name] --task-definition [task_def:revision]```\n\n**Update a service** to force new deployment\n\n```aws ecs update-service --cluster [cluster_name] --service [service_name] --force-new-deployment```\n\n**Execute a command** inside a running container\n\n```aws ecs execute-command --cluster [cluster_name] --task [task_id] --container [container_name] --interactive --command \"/bin/sh\"```\n\n**Stop a running task**\n\n```aws ecs stop-task --cluster [cluster_name] --task [task_arn]```"],["SYNOPSIS","**aws ecs** _subcommand_ [_options_]"],["DESCRIPTION","**aws ecs** is a subcommand of the AWS CLI that manages Amazon Elastic Container Service, a fully managed container orchestration service. ECS runs Docker containers on a cluster of EC2 instances or serverless with Fargate.\n\nThe core concepts are: **clusters** (logical grouping of resources), **task definitions** (blueprints for containers), **tasks** (running instances of task definitions), and **services** (maintain desired count of tasks). ECS integrates with load balancers, auto scaling, and IAM for production workloads.\n\nECS Exec enables interactive shell access to running containers for debugging, similar to docker exec but through the SSM agent."],["PARAMETERS","**list-clusters**\n> List all ECS clusters in the region.\n\n**describe-clusters**\n> Get detailed information about clusters.\n\n**create-cluster**\n> Create a new ECS cluster.\n\n**delete-cluster**\n> Remove an empty cluster.\n\n**list-services**\n> List services running in a cluster.\n\n**describe-services**\n> Get service details, deployments, and events.\n\n**create-service**\n> Create a new service.\n\n**update-service**\n> Modify service configuration or trigger deployment.\n\n**delete-service**\n> Remove a service from the cluster.\n\n**list-tasks**\n> List tasks in a cluster or service.\n\n**describe-tasks**\n> Get task details and container status.\n\n**run-task**\n> Start a standalone task.\n\n**stop-task**\n> Terminate a running task.\n\n**execute-command**\n> Run interactive command in a container.\n\n**register-task-definition**\n> Create a new task definition revision.\n\n**deregister-task-definition**\n> Deregister a task definition revision.\n\n**list-task-definitions**\n> List registered task definition families or revisions.\n\n**describe-task-definition**\n> Get full details of a task definition.\n\n**tag-resource**\n> Add tags to an ECS resource.\n\n**wait**\n> Wait until a condition is met (e.g., services-stable, tasks-stopped).\n\n**--cluster** _name_\n> Target cluster name or ARN.\n\n**--service** _name_\n> Target service name or ARN.\n\n**--task-definition** _name:revision_\n> Task definition family and revision.\n\n**--desired-count** _number_\n> Number of task instances to run.\n\n**--force-new-deployment**\n> Force service to pull new image and redeploy."],["CAVEATS","Deleting a cluster requires stopping all tasks and deleting all services first. Task definitions cannot be deleted, only deregistered. ECS Exec requires the SSM agent and proper IAM permissions on the task role. Fargate tasks must specify CPU and memory at the task level."],["HISTORY","Amazon ECS was announced at **AWS re:Invent 2014** and launched in **April 2015** as AWS's first container orchestration service. **Fargate** serverless compute was added in **November 2017**. ECS Exec for interactive debugging was introduced in **March 2021**. The service continues to evolve alongside AWS's container strategy including integration with EKS."],["INSTALL","```dnf: sudo dnf install aws```\n\n```pacman: sudo pacman -S aws-cli```\n\n```apk: sudo apk add aws-cli```\n\n```zypper: sudo zypper install aws-cli```"],["SEE ALSO","[aws](/man/aws)(1), [aws-ecr](/man/aws-ecr)(1), [aws-eks](/man/aws-eks)(1), [docker](/man/docker)(1), [kubectl](/man/kubectl)(1)"]],"e":["aws","aws-ecr","aws-eks","docker","kubectl"],"x":"aws ecs list-clusters"},"aws-help":{"n":"aws-help","t":"Display AWS CLI command documentation","s":[["TAGLINE","Display AWS CLI command documentation"],["TLDR","**Get** general help\n\n```aws help```\n\n**Service** help\n\n```aws s3 help```\n\n**Command** help\n\n```aws s3 cp help```\n\n**Subcommand** help\n\n```aws ec2 describe-instances help```\n\n**List** available help topics\n\n```aws help topics```"],["SYNOPSIS","**aws** [_service_] [_command_] **help**"],["DESCRIPTION","**aws help** displays comprehensive documentation for AWS CLI commands. It shows syntax, parameters, examples, and related commands using a pager interface.\n\nHelp is available at every level: general CLI help, service-specific help, and individual command help."],["FEATURES","Documentation includes:\n- Command syntax and structure\n- Parameter descriptions and types\n- Usage examples\n- Available subcommands\n- Output format details\n- Related commands"],["NAVIGATION","Within help pager:\n- **Space** - Page down\n- **b** - Page up\n- **/** - Search\n- **q** - Quit"],["WORKFLOW","```bash"],["General help","aws help"],["S3 service help","aws s3 help"],["Specific command help","aws s3 cp help"],["EC2 describe-instances help","aws ec2 describe-instances help\n```"],["CAVEATS","Opens in pager by default (use --no-cli-pager to disable). Requires internet connection for complete documentation in some cases. Help text can be very long for complex commands."],["HISTORY","The **help** command has been part of the AWS CLI since its initial release in **2013**, providing inline documentation for all services and commands."],["INSTALL","```dnf: sudo dnf install aws```\n\n```pacman: sudo pacman -S aws-cli```\n\n```apk: sudo apk add aws-cli```\n\n```zypper: sudo zypper install aws-cli```"],["SEE ALSO","[aws](/man/aws)(1), [man](/man/man)(1), [less](/man/less)(1)"],["RESOURCES","```[Source code](https://github.com/aws/aws-cli)```\n\n```[Homepage](https://aws.amazon.com/cli/)```\n\n```[Documentation](https://docs.aws.amazon.com/cli/latest/reference/)```"]],"e":["aws","man","less"],"x":"aws help"},"axel":{"n":"axel","t":"Lightweight multi-connection download accelerator","s":[["TAGLINE","Lightweight multi-connection download accelerator"],["TLDR","**Download** a file with multiple connections\n\n```axel [https://example.com/file.zip]```\n\nDownload with a **specific number of connections**\n\n```axel -n [16] [https://example.com/file.zip]```\n\n**Save** with a different filename\n\n```axel -o [output.zip] [https://example.com/file.zip]```\n\n**Limit speed** to a given number of bytes per second\n\n```axel -s [1024000] [https://example.com/file.zip]```\n\nSet a custom **user agent**\n\n```axel -U \"[Mozilla/5.0]\" [https://example.com/file.zip]```\n\n**Skip** the download if the file already exists\n\n```axel -c [https://example.com/file.zip]```"],["SYNOPSIS","**axel** [_options_] _url_"],["DESCRIPTION","**axel** is a lightweight download accelerator that opens multiple HTTP/FTP connections to the same file. By downloading different parts of the file simultaneously, it can significantly speed up downloads compared to single-connection tools.\n\nThe tool is particularly effective for large files on servers that don't rate-limit individual connections."],["PARAMETERS","**--num-connections=**_num_, **-n** _num_\n> Number of connections to open.\n\n**--output=**_file_, **-o** _file_\n> Local filename to save to.\n\n**--max-speed=**_bytes_, **-s** _bytes_\n> Try to keep the average speed around this many bytes per second.\n\n**--alternate**, **-a**\n> Show an alternate progress bar with per-thread status.\n\n**--quiet**, **-q**\n> No output to stdout.\n\n**--verbose**, **-v**\n> Show more status messages. Repeat for more detail.\n\n**--header=**_header_, **-H** _header_\n> Add an HTTP header in the form 'Header: Value'.\n\n**--user-agent=**_agent_, **-U** _agent_\n> Set the HTTP user agent.\n\n**--no-proxy**, **-N**\n> Do not use any proxy server.\n\n**--no-clobber**, **-c**\n> Skip the download if a file with the same name already exists.\n\n**--insecure**, **-k**\n> Do not verify the SSL certificate.\n\n**--timeout=**_secs_, **-T** _secs_\n> Set the I/O and connection timeout.\n\n**--max-redirect=**_num_\n> Maximum redirects to follow (default: 20)."],["FEATURES","- Multiple simultaneous connections\n- Resume interrupted downloads\n- Speed limiting\n- FTP and HTTP support\n- Progress indicators\n- Proxy support"],["CONFIGURATION","**/etc/axelrc**\n> System-wide configuration file.\n\n**~/.axelrc**\n> Per-user configuration file for default settings like number of connections and speed limits."],["CAVEATS","Some servers block multiple connections. May be considered aggressive by some sites. Not all servers support range requests. Can waste bandwidth if connection limit is too high. Less feature-rich than aria2."],["HISTORY","**axel** was created by Wilmer van der Gaast around **2001** as a lightweight alternative to download managers, focusing on speed through parallel connections."],["INSTALL","```apt: sudo apt install axel```\n\n```dnf: sudo dnf install axel```\n\n```pacman: sudo pacman -S axel```\n\n```apk: sudo apk add axel```\n\n```zypper: sudo zypper install axel```\n\n```brew: brew install axel```\n\n```nix: nix profile install nixpkgs#axel```"],["SEE ALSO","[wget](/man/wget)(1), [curl](/man/curl)(1), [aria2](/man/aria2)(1)"],["RESOURCES","```[Source code](https://github.com/axel-download-accelerator/axel)```"]],"e":["wget","curl","aria2"],"x":"axel [https://example.com/file.zip]"},"az-image":{"n":"az-image","t":"Manage custom virtual machine images in Azure","s":[["TAGLINE","Manage custom virtual machine images in Azure"],["TLDR","**Create a managed image** from a VM\n\n```az image create --name [MyImage] --resource-group [MyResourceGroup] --source [MyVM]```\n\n**Create an image** from a managed disk\n\n```az image create --name [MyImage] --resource-group [MyResourceGroup] --os-disk [disk-id] --os-type [Linux]```\n\n**List all images** in a resource group\n\n```az image list --resource-group [MyResourceGroup]```\n\n**Show details** of an image\n\n```az image show --name [MyImage] --resource-group [MyResourceGroup]```\n\n**Update image tags**\n\n```az image update --name [MyImage] --resource-group [MyResourceGroup] --tags [env=prod]```\n\n**Delete an image**\n\n```az image delete --name [MyImage] --resource-group [MyResourceGroup]```"],["SYNOPSIS","**az** **image** _subcommand_ [_options_]"],["SUBCOMMANDS","**copy**\n> Copy a managed image to other regions (requires extension).\n\n**create**\n> Create a custom virtual machine image from managed disks or snapshots.\n\n**delete**\n> Delete an image.\n\n**list**\n> List images in a resource group or subscription.\n\n**show**\n> Get information about an image.\n\n**update**\n> Update custom VM images.\n\n**wait**\n> Place the CLI in a waiting state until a condition is met."],["PARAMETERS","**--name** **-n**\n> The name of the image.\n\n**--resource-group** **-g**\n> Name of resource group.\n\n**--source**\n> Source VM, managed disk, or snapshot to create the image from.\n\n**--os-disk**\n> Source managed disk for OS disk.\n\n**--os-type**\n> Operating system type: Linux or Windows.\n\n**--location** **-l**\n> Location for the image.\n\n**--hyper-v-generation**\n> Hypervisor generation of the VM: V1 or V2.\n\n**--tags**\n> Space-separated tags: key[=value].\n\n**--data-disk-sources**\n> Managed disks or snapshots for data disks."],["DESCRIPTION","**az image** manages custom virtual machine images in Azure. Custom images can be created from generalized VMs, managed disks, or snapshots. These images can then be used to create new VMs with your custom configuration.\n\nCustom images are different from marketplace images (managed via **az vm image**) as they contain your specific software and configurations."],["CAVEATS","Requires Azure CLI to be installed and authenticated with **az login**. The source VM must be generalized (sysprep on Windows, waagent -deprovision on Linux) before creating an image. Images can only create VMs in the same region."],["INSTALL","```dnf: sudo dnf install azure-cli```\n\n```pacman: sudo pacman -S azure-cli```\n\n```zypper: sudo zypper install azure-cli```\n\n```brew: brew install azure-cli```\n\n```nix: nix profile install nixpkgs#azure-cli```"],["SEE ALSO","[az](/man/az)(1), [az-disk](/man/az-disk)(1), [az-vm](/man/az-vm)(1)"]],"e":["az","az-disk","az-vm"],"x":"az image create --name [MyImage] --resource-group [MyResourceGroup] --source [MyVM]"},"brace":{"n":"brace","t":"Shell syntax for grouping and expansion","s":[["TAGLINE","Shell syntax for grouping and expansion"],["TLDR","**Command grouping (current shell)**\n\n```{ [command1]; [command2]; }```\n\n**Brace expansion**\n\n```echo {a,b,c}```\n\n**Sequence expansion**\n\n```echo {1..10}```\n\n**Sequence with a step/increment**\n\n```echo {0..20..5}```\n\n**Parameter expansion**\n\n```echo ${var}```\n\n**Create multiple files**\n\n```touch file{1,2,3}.txt```"],["SYNOPSIS","**{** _commands_**;** **}**\n\n**{**_a_**,**_b_**,**_c_**}**\n\n**{**_start_**..**_end_**}**\n\n**${**_parameter_**}**"],["DESCRIPTION","**{ }** (braces/curly brackets) serve multiple purposes in shell:\n\n**Command grouping**: **{ cmd1; cmd2; }** groups commands to run in the current shell (unlike **(...)** which uses a subshell). Requires spaces after **{** and **;** before **}**.\n\n**Brace expansion**: **{a,b,c}** expands to **a b c**. Useful for generating lists:\n- **file{1,2,3}** → **file1 file2 file3**\n- **{a..z}** → alphabet\n- **{1..10}** → numbers 1-10\n- **{01..10}** → zero-padded: 01 02 ... 10\n- **{0..20..5}** → step/increment (bash 4.0+): 0 5 10 15 20\n\n**Parameter expansion**: **${var}** accesses variables, required for:\n- Array access: **${array[0]}**\n- Modifiers: **${var:-default}**\n- Disambiguation: **${var}text**"],["EXAMPLES","```bash"],["Group with redirect","{ echo \"line1\"; echo \"line2\"; } > file.txt"],["Brace expansion for backups","cp config.{txt,bak}"],["Generate sequences","mkdir dir{01..12}\necho {A..Z}"],["Nested expansion","echo {a,b}{1,2}  # a1 a2 b1 b2\n```"],["CAVEATS","Command grouping requires space after **{** and semicolon before **}**: **{ cmd; }** not **{cmd}**.\n\nBrace expansion is not glob expansion - it happens before other expansions and doesn't check if files exist.\n\n**{}** in find commands is the replacement string, not brace expansion."],["SEE ALSO","[bash](/man/bash)(1), [sh](/man/sh)(1), [zsh](/man/zsh)(1)"]],"e":["bash","sh","zsh"],"x":"{ [command1]; [command2]; }"},"browser-sync":{"n":"browser-sync","t":"Live-reloading development server with browser synchronization","s":[["TAGLINE","Live-reloading development server with browser synchronization"],["TLDR","**Start server in current directory**\n\n```browser-sync start --server```\n\n**Start server from specific directory**\n\n```browser-sync start --server [app]```\n\n**Start server watching all files**\n\n```browser-sync start --server --files \"**/*\"```\n\n**Watch specific file types**\n\n```browser-sync start --server --files \"*.html, css/*.css\"```\n\n**Proxy existing server**\n\n```browser-sync start --proxy \"[localhost:8080]\"```\n\n**Create configuration file**\n\n```browser-sync init```\n\n**Send reload event**\n\n```browser-sync reload```\n\n**Start with custom port**\n\n```browser-sync start --server --port [3000]```"],["SYNOPSIS","**browser-sync** _command_ [_options_]"],["DESCRIPTION","**browser-sync** is a development tool that keeps multiple browsers and devices synchronized while building websites. It watches files for changes and either injects CSS/images or triggers full page reloads.\n\nThe tool works by injecting an asynchronous script tag after the body tag during initial requests, enabling real-time synchronization across all connected browsers."],["COMMANDS","**start**\n> Start Browsersync server\n\n**init**\n> Create a configuration file\n\n**reload**\n> Send a reload event over HTTP protocol"],["PARAMETERS","**--server** _directory_\n> Serve files from specified directory\n\n**--proxy** _url_\n> Proxy an existing server\n\n**--files** _pattern_\n> File patterns to watch for changes\n\n**--port** _number_\n> Port to use (default: 3000)\n\n**--https**\n> Enable HTTPS for localhost development\n\n**--no-open**\n> Don't open browser on start\n\n**--browser** _name_\n> Browser to open (chrome, firefox, etc.)\n\n**--config** _file_\n> Path to configuration file\n\n**--reload-delay** _ms_\n> Milliseconds to wait before reloading"],["CONFIGURATION","**bs-config.js**\n> Configuration file generated by `browser-sync init`. Defines server settings, file watching patterns, proxy targets, and middleware options."],["CAVEATS","Requires the body tag to be present in HTML files. HTTPS is automatically inferred when proxying HTTPS targets. CSS and image changes are injected without full reload; other changes trigger full reload."],["INSTALL","```nix: nix profile install nixpkgs#browser-sync```"],["SEE ALSO","[live-server](/man/live-server)(1), [webpack](/man/webpack)(1), [gulp](/man/gulp)(1)"],["RESOURCES","```[Source code](https://github.com/BrowserSync/browser-sync)```\n\n```[Homepage](https://browsersync.io)```"]],"e":["live-server","webpack","gulp"],"x":"browser-sync start --server"},"bun-patch":{"n":"bun-patch","t":"Patch installed packages locally","s":[["TAGLINE","Patch installed packages locally"],["TLDR","**Prepare a package for patching**\n\n```bun patch [package-name]```\n\n**Prepare a specific version** for patching\n\n```bun patch [package-name]@[1.2.3]```\n\n**Commit patches** after making modifications\n\n```bun patch --commit [package-name]```\n\n**Commit with a custom patches directory**\n\n```bun patch --commit [package-name] --patches-dir=[mypatches]```\n\n**Patch using the path** to the package in node_modules\n\n```bun patch --commit node_modules/[package-name]```"],["SYNOPSIS","**bun** **patch** [_options_] _package-name_"],["PARAMETERS","**--commit** _path-or-pkg_\n> Generate a .patch file from modifications. Accepts package name, name@version, or path to patched package.\n\n**--patches-dir** _dir_\n> Directory to store patch files (only with --commit). Default: patches.\n\n**-p, --production**\n> Don't install devDependencies\n\n**--frozen-lockfile**\n> Disallow changes to the lockfile\n\n**--dry-run**\n> Don't install anything, only show what would be done\n\n**--ignore-scripts**\n> Skip lifecycle scripts in the project's package.json\n\n**-f, --force**\n> Always request the latest versions from the registry and reinstall all dependencies\n\n**--verbose**\n> Enable verbose logging output"],["DESCRIPTION","**bun patch** allows you to persistently patch node_modules in a maintainable, git-friendly way. It extracts a package for editing, and after making changes, **--commit** creates a .patch file that is applied on future installs.\n\nPatch files are stored in a patches directory and tracked via \"patchedDependencies\" in package.json. They can be committed to your repository and reused across multiple installs, projects, and machines.\n\nYou can supply a package name, a precise version (if multiple versions are installed), or the path to the package in node_modules."],["CAVEATS","Patches are stored in a patches directory and must be committed to version control. Patches may break when the package is updated."],["SEE ALSO","[bun](/man/bun)(1), [bun-install](/man/bun-install)(1), [bun-add](/man/bun-add)(1), [bun-update](/man/bun-update)(1)"]],"e":["bun","bun-install","bun-add","bun-update"],"x":"bun patch [package-name]"},"cargo-doc":{"n":"cargo-doc","t":"Generate documentation for a Rust package","s":[["TAGLINE","Generate documentation for a Rust package"],["TLDR","**Build documentation**\n\n```cargo doc```\n\n**Build and open in browser**\n\n```cargo doc --open```\n\n**Build without dependencies**\n\n```cargo doc --no-deps```\n\n**Include private items**\n\n```cargo doc --document-private-items```\n\n**Build for all workspace members**\n\n```cargo doc --workspace```\n\n**Build with release profile**\n\n```cargo doc --release```\n\n**Build with all features**\n\n```cargo doc --all-features```"],["SYNOPSIS","**cargo doc** [_options_]"],["DESCRIPTION","**cargo doc** builds documentation for the local package and all dependencies using rustdoc. Output placed in target/doc/ in HTML format."],["PARAMETERS","**--open**\n> Open documentation in browser after building\n\n**--no-deps**\n> Don't build documentation for dependencies\n\n**--document-private-items**\n> Include non-public items (default for binaries)\n\n**-r**, **--release**\n> Build with release profile\n\n**--workspace**\n> Document all workspace members\n\n**--exclude** _spec_\n> Exclude packages (with --workspace)\n\n**-p**, **--package** _spec_\n> Document specific packages\n\n**--lib**\n> Document library only\n\n**--bins**\n> Document all binaries\n\n**--target** _triple_\n> Document for target architecture\n\n**-j**, **--jobs** _n_\n> Parallel jobs (default: CPU count)\n\n**--features** _features_\n> Enable specified features\n\n**--all-features**\n> Enable all features\n\n**--no-default-features**\n> Disable default features"],["ENVIRONMENT","**BROWSER**\n> Browser to use with --open"],["OUTPUT","Documentation generated in target/doc/<crate_name>/index.html"],["CAVEATS","Skips binaries with same name as library. Skips binaries with missing required features."],["INSTALL","```apt: sudo apt install cargo```\n\n```dnf: sudo dnf install cargo```\n\n```apk: sudo apk add cargo```\n\n```zypper: sudo zypper install cargo```\n\n```nix: nix profile install nixpkgs#cargo```"],["SEE ALSO","[cargo](/man/cargo)(1), [rustdoc](/man/rustdoc)(1)"],["RESOURCES","```[Source code](https://github.com/rust-lang/cargo)```\n\n```[Documentation](https://doc.rust-lang.org/cargo/commands/cargo-doc.html)```"]],"e":["cargo","rustdoc"],"x":"cargo doc"},"cargo-login":{"n":"cargo-login","t":"Save a registry API token for authentication","s":[["TAGLINE","Save a registry API token for authentication"],["TLDR","**Login to crates.io** (token read from stdin)\n\n```cargo login```\n\n**Login with a token provided as argument**\n\n```cargo login [token]```\n\n**Login to a specific registry**\n\n```cargo login --registry [my-registry]```"],["SYNOPSIS","**cargo login** [_options_] [_token_]"],["DESCRIPTION","**cargo login** runs a credential provider to save a token for registry authentication. Required for publishing crates with `cargo publish`. The token is read from stdin. Tokens are stored in `$CARGO_HOME/credentials.toml` by default.\n\nFor crates.io, obtain your token from https://crates.io/me"],["PARAMETERS","**--registry** _name_\n> Registry to authenticate with (default: crates-io)\n\n**-v**, **--verbose**\n> Verbose output. May be specified twice for extra detail.\n\n**-q**, **--quiet**\n> Suppress output.\n\n**--color** _when_\n> Control colored output (auto, always, never).\n\n**--config** _KEY=VALUE_\n> Override a Cargo configuration value."],["TOKEN STORAGE","**Default location**\n> ~/.cargo/credentials.toml\n\n**Format**\n```toml\n[registry]\ntoken = \"your-api-token\"\n```"],["CREDENTIAL PROVIDERS","Configure in ~/.cargo/config.toml:\n\n```toml\n[registry]\nglobal-credential-providers = [\n    \"cargo:token\",\n    \"cargo:libsecret\",\n    \"cargo:macos-keychain\",\n    \"cargo:wincred\"\n]\n```"],["ENVIRONMENT","**CARGO_REGISTRIES_<NAME>_TOKEN**\n> Token via environment variable\n\n**CARGO_REGISTRY_TOKEN**\n> Default registry token"],["CAVEATS","Keep tokens secret. The cargo:token provider stores tokens unencrypted. For enhanced security, use system keychain providers."],["INSTALL","```apt: sudo apt install cargo```\n\n```dnf: sudo dnf install cargo```\n\n```apk: sudo apk add cargo```\n\n```zypper: sudo zypper install cargo```\n\n```nix: nix profile install nixpkgs#cargo```"],["SEE ALSO","[cargo](/man/cargo)(1), [cargo-publish](/man/cargo-publish)(1), [cargo-logout](/man/cargo-logout)(1)"]],"e":["cargo","cargo-publish","cargo-logout"],"x":"cargo login"},"cargo-publish":{"n":"cargo-publish","t":"Upload a package to a registry","s":[["TAGLINE","Upload a package to a registry"],["TLDR","**Publish to crates.io**\n\n```cargo publish```\n\n**Dry run without uploading**\n\n```cargo publish --dry-run```\n\n**Publish without verification**\n\n```cargo publish --no-verify```\n\n**Allow uncommitted changes**\n\n```cargo publish --allow-dirty```\n\n**Publish to alternate registry**\n\n```cargo publish --registry [my-registry]```\n\n**Publish specific package**\n\n```cargo publish -p [crate_name]```"],["SYNOPSIS","**cargo publish** [_options_]"],["DESCRIPTION","**cargo publish** uploads a package to crates.io or another registry. It creates a `.crate` archive, verifies that the archive builds successfully, and then uploads it to the registry. Authentication is required and is typically set up via `cargo login`.\n\nPublishes are permanent and cannot be deleted or overwritten. Once a version is published, that exact version number can never be reused. The `cargo yank` command can mark a version as deprecated to prevent new projects from depending on it, but it remains available for existing users. Before publishing, ensure the version number in Cargo.toml has been incremented according to semver conventions."],["PARAMETERS","**--dry-run**\n> Perform checks without uploading\n\n**--no-verify**\n> Skip build verification\n\n**--allow-dirty**\n> Allow uncommitted VCS changes\n\n**--registry** _name_\n> Target registry\n\n**--index** _url_\n> Registry index URL\n\n**--token** _token_\n> API token\n\n**-p**, **--package** _spec_\n> Package to publish\n\n**--target** _triple_\n> Build target for verification\n\n**-j**, **--jobs** _n_\n> Parallel jobs\n\n**--features** _features_\n> Enable features for verification\n\n**--all-features**\n> Enable all features"],["CONFIGURATION","**~/.cargo/credentials.toml**\n> Stores API tokens for crates.io and other registries. Created by `cargo login`."],["PREPARATION","1. Create account on crates.io\n2. Verify email address\n3. Generate API token\n4. Run cargo login"],["RELEASING NEW VERSIONS","1. Update version in Cargo.toml\n2. Optionally run cargo package\n3. Run cargo publish"],["CAVEATS","Publishes are permanent. Use cargo yank to mark versions as deprecated but not delete them. Follow semver for version changes."],["INSTALL","```apt: sudo apt install cargo```\n\n```dnf: sudo dnf install cargo```\n\n```apk: sudo apk add cargo```\n\n```zypper: sudo zypper install cargo```\n\n```nix: nix profile install nixpkgs#cargo```"],["SEE ALSO","[cargo](/man/cargo)(1), [cargo-login](/man/cargo-login)(1), [cargo-package](/man/cargo-package)(1), [cargo-yank](/man/cargo-yank)(1)"]],"e":["cargo","cargo-login","cargo-package","cargo-yank"],"x":"cargo publish"},"cargo-selector":{"n":"cargo-selector","t":"Interactive Cargo target selector","s":[["TAGLINE","Interactive Cargo target selector"],["TLDR","**Interactively select** and run a binary target\n\n```cargo selector run```\n\n**Interactively select** and run a test\n\n```cargo selector test```\n\n**Interactively select** and run a benchmark\n\n```cargo selector bench```\n\n**Interactively select** and run an example\n\n```cargo selector example```"],["SYNOPSIS","**cargo selector** _command_ [_options_]"],["DESCRIPTION","**cargo-selector** is a Cargo subcommand that provides an interactive fuzzy-search interface for selecting and running targets in a Rust project. Instead of typing exact target names, it presents a searchable list of available binaries, tests, examples, or benchmarks and runs the selected one.\n\nThis is particularly useful in workspaces or projects with many targets, where remembering exact names is impractical. The selector uses a fuzzy finder interface for quick filtering."],["CAVEATS","Requires a valid Cargo project with defined targets. Additional arguments to the underlying cargo command may need to be passed after the target selection."],["INSTALL","```apt: sudo apt install cargo```\n\n```dnf: sudo dnf install cargo```\n\n```apk: sudo apk add cargo```\n\n```zypper: sudo zypper install cargo```\n\n```nix: nix profile install nixpkgs#cargo```"],["SEE ALSO","[cargo](/man/cargo)(1), [fzf](/man/fzf)(1)"]],"e":["cargo","fzf"],"x":"cargo selector run"},"colorls":{"n":"colorls","t":"enhanced ls with colors and icons","s":[["TAGLINE","enhanced ls with colors and icons"],["TLDR","**List files** with colors and icons\n\n```colorls```\n\n**List all files** including hidden\n\n```colorls -a```\n\n**List in long format** with details\n\n```colorls -l```\n\n**List with directory tree view**\n\n```colorls --tree```\n\n**Sort by modification time**\n\n```colorls -t```\n\n**List only directories**\n\n```colorls -d```\n\n**List only files**\n\n```colorls -f```\n\n**Show file sizes** in human-readable format\n\n```colorls -lh```"],["SYNOPSIS","**colorls** [_-alrtdfsgh_] [_--tree_] [_--report_] [_--gs_] [_--sd_] [_path ..._]"],["PARAMETERS","**-a**, **--all**\n> Show hidden files (starting with .).\n\n**-l**, **--long**\n> Display long format with permissions, owner, size, and date.\n\n**-r**, **--reverse**\n> Reverse sort order.\n\n**-t**, **--sort-time**\n> Sort by modification time (newest first).\n\n**-d**, **--dirs**\n> Show only directories.\n\n**-f**, **--files**\n> Show only files.\n\n**-s**, **--sort-size**\n> Sort by file size.\n\n**-1**\n> One entry per line.\n\n**--tree** [_depth_]\n> Show directory tree (optional depth limit).\n\n**--report**, **--r**\n> Show brief report of files and directories.\n\n**--gs**, **--git-status**\n> Show git status for each file.\n\n**--sd**, **--sort-dirs**\n> Sort directories first.\n\n**--sf**, **--sort-files**\n> Sort files first.\n\n**-h**, **--human-readable**\n> Show file sizes in human-readable format (K, M, G).\n\n**--light**\n> Use light color scheme.\n\n**--dark**\n> Use dark color scheme (default).\n\n**--help**\n> Display help information."],["CONFIGURATION","**~/.config/colorls/dark_colors.yaml**\n> Color scheme for dark terminal backgrounds.\n\n**~/.config/colorls/light_colors.yaml**\n> Color scheme for light terminal backgrounds.\n\n**~/.config/colorls/file_aliases.yaml**\n> Custom file type associations and icon mappings."],["DESCRIPTION","**colorls** is a Ruby gem that provides an enhanced, colorful alternative to the standard ls command. It displays file listings with icons (using Nerd Fonts), colors based on file type, and additional features like git status integration and tree views.\n\nEach file type is represented by a distinct icon and color: directories, executables, images, documents, archives, and more. This visual differentiation makes it easier to scan directory contents quickly. The icons require a Nerd Font (patched font with icons) to display correctly.\n\nGit integration shows the status of files in repositories: modified, staged, untracked, etc. The tree view provides a hierarchical display of directory structure similar to the **tree** command but with colorls styling.\n\nConfiguration can be customized via **~/.config/colorls/dark_colors.yaml** and **~/.config/colorls/light_colors.yaml** for colors, and **~/.config/colorls/file_aliases.yaml** for custom file type associations."],["CAVEATS","Requires Ruby and the colorls gem (gem install colorls). Icons require a Nerd Font to be installed and configured in the terminal. Performance may be slower than standard ls for directories with many files. Some terminal emulators may not render colors or icons correctly."],["HISTORY","**colorls** was created by Athitya Kumar and released as a Ruby gem. It gained popularity in the terminal customization community for its visual appeal and practical features. The project was inspired by the desire for a more visually informative directory listing tool. Similar projects include **exa** (now **eza**) and **lsd** which offer comparable features in different languages."],["INSTALL","```nix: nix profile install nixpkgs#colorls```"],["SEE ALSO","[ls](/man/ls)(1), [exa](/man/exa)(1), [eza](/man/eza)(1), [lsd](/man/lsd)(1), [tree](/man/tree)(1)"],["RESOURCES","```[Source code](https://github.com/athityakumar/colorls)```"]],"e":["ls","exa","eza","lsd","tree"],"x":"colorls"},"filefrag":{"n":"filefrag","t":"report on file fragmentation","s":[["TAGLINE","report on file fragmentation"],["TLDR","Display **fragmentation report** for files\n\n```filefrag [path/to/file1] [path/to/file2]```\n\nReport using **1024 byte** blocksize\n\n```filefrag -k [path/to/file]```\n\nReport using **specific** blocksize\n\n```filefrag -b[1024|1K|1M|1G] [path/to/file]```\n\n**Sync** file before requesting mapping\n\n```filefrag -s [path/to/file1] [path/to/file2]```\n\nDisplay **verbose** report with extent details\n\n```filefrag -v [path/to/file1] [path/to/file2]```\n\nDisplay mapping of **extended attributes**\n\n```filefrag -x [path/to/file1] [path/to/file2]```\n\nDisplay extent block numbers in **hexadecimal**\n\n```filefrag -X [path/to/file]```\n\nPrint **version** number\n\n```filefrag -V```"],["SYNOPSIS","**filefrag** [**-b** _blocksize_] [**-BeEkPsvVxX**] [_files..._]"],["DESCRIPTION","**filefrag** reports on how badly fragmented a particular file might be. It shows the number of extents (contiguous blocks) for each file, which indicates fragmentation level. A file with 1 extent is not fragmented; more extents indicate higher fragmentation.\n\nUseful for identifying files that may benefit from defragmentation."],["PARAMETERS","**-b** _blocksize_\n> Use blocksize in bytes, or with [KMG] suffix, up to 1GB for output instead of the filesystem blocksize. If blocksize is unspecified it defaults to 1024 bytes. Must be added without any space after -b.\n\n**-B**\n> Force use of the older FIBMAP ioctl instead of the FIEMAP ioctl for testing purposes.\n\n**-e**\n> Print output in extent format, even for block-mapped files.\n\n**-E**\n> Display the contents of ext4's extent status cache. Only supported on ext4 filesystems.\n\n**-k**\n> Use 1024-byte blocksize for output (identical to '-b1024').\n\n**-P**\n> Pre-load the ext4 extent status cache for the file. Only supported on ext4 filesystems.\n\n**-s**\n> Sync the file before requesting the mapping.\n\n**-v**\n> Be verbose when checking for file fragmentation.\n\n**-V**\n> Print version number of program and library. If given twice, also print the FIEMAP flags understood by the current version.\n\n**-x**\n> Display mapping of extended attributes.\n\n**-X**\n> Display extent block numbers in hexadecimal format."],["CAVEATS","Part of e2fsprogs. Works best on ext2/ext3/ext4 but supports other filesystems via FIEMAP ioctl. The -E and -P options are ext4-specific and not supported on all kernels."],["INSTALL","```apt: sudo apt install e2fsprogs```\n\n```dnf: sudo dnf install e2fsprogs```\n\n```pacman: sudo pacman -S e2fsprogs```\n\n```apk: sudo apk add e2fsprogs-extra```\n\n```zypper: sudo zypper install e2fsprogs```\n\n```brew: brew install e2fsprogs```\n\n```nix: nix profile install nixpkgs#e2fsprogs```"],["SEE ALSO","[e4defrag](/man/e4defrag)(8), [debugfs](/man/debugfs)(8), [fsck](/man/fsck)(8)"]],"e":["e4defrag","debugfs","fsck"],"x":"filefrag [path/to/file1] [path/to/file2]"},"flac":{"n":"flac","t":"free lossless audio codec encoder and decoder","s":[["TAGLINE","free lossless audio codec encoder and decoder"],["TLDR","**Encode WAV to FLAC**\n\n```flac [audio.wav]```\n\n**Decode FLAC to WAV**\n\n```flac -d [audio.flac]```\n\n**Maximum compression**\n\n```flac -8 [audio.wav]```\n\n**Fast compression**\n\n```flac --fast [audio.wav]```\n\n**Test file integrity**\n\n```flac -t [audio.flac]```\n\n**Add metadata tags** during encoding\n\n```flac -T \"ARTIST=[Name]\" -T \"TITLE=[Song]\" [audio.wav]```\n\n**Output to specific file**\n\n```flac -o [output.flac] [input.wav]```"],["SYNOPSIS","**flac** [_options_] _files_..."],["DESCRIPTION","**flac** encodes and decodes audio in the Free Lossless Audio Codec format. FLAC provides lossless compression, meaning the audio is identical to the original after decoding.\n\nThe tool handles encoding, decoding, testing, and metadata operations. FLAC files are typically 50-60% of the original size while maintaining perfect audio fidelity."],["PARAMETERS","**-d**, **--decode**\n> Decode FLAC to WAV.\n\n**-t**, **--test**\n> Test file integrity.\n\n**-a**, **--analyze**\n> Analyze file.\n\n**-0** to **-8**\n> Compression level (0=fast, 8=best).\n\n**--fast**\n> Same as -0.\n\n**--best**\n> Same as -8.\n\n**-o** _file_\n> Output file name.\n\n**-c**, **--stdout**\n> Write output to stdout.\n\n**-f**, **--force**\n> Force overwriting of output files.\n\n**-T**, **--tag=** _FIELD=VALUE_\n> Add a Vorbis comment tag (may be repeated).\n\n**--tag-from-file=** _FIELD=FILENAME_\n> Read the tag value from a file.\n\n**--delete-input-file**\n> Delete input after successful encoding/decoding.\n\n**-s**, **--silent**\n> Suppress runtime statistics.\n\n**-V**, **--verify**\n> Verify by decoding in parallel during encoding.\n\n**-w**, **--warnings-as-errors**\n> Treat all warnings as errors."],["CAVEATS","Higher compression levels slower but not much smaller. Decoding speed consistent regardless of level. Some players don't support all FLAC features."],["HISTORY","**FLAC** was created by **Josh Coalson** starting in **2000**. It became the leading open source lossless audio format, gaining support in most media players and devices. The project became part of the Xiph.Org Foundation alongside Ogg Vorbis."],["INSTALL","```apt: sudo apt install flac```\n\n```dnf: sudo dnf install flac```\n\n```pacman: sudo pacman -S flac```\n\n```apk: sudo apk add flac```\n\n```zypper: sudo zypper install flac```\n\n```brew: brew install flac```\n\n```nix: nix profile install nixpkgs#flac```"],["SEE ALSO","[sox](/man/sox)(1), [lame](/man/lame)(1), [ffmpeg](/man/ffmpeg)(1), [metaflac](/man/metaflac)(1)"]],"e":["sox","lame","ffmpeg","metaflac"],"x":"flac [audio.wav]"},"flash":{"n":"flash","t":"terminal-based spaced repetition flashcards","s":[["TAGLINE","terminal-based spaced repetition flashcards"],["TLDR","Launch the **deck picker** UI\n\n```flash```\n\nShow flash **system information**\n\n```flash -i```\n\nUse a custom **preview** program instead of bat\n\n```flash -p [cat]```\n\nPrint **version**\n\n```flash -v```\n\nShow **help**\n\n```flash -h```"],["SYNOPSIS","**flash** [_-i_] [_-p previewer_] [_-h_] [_-v_]"],["PARAMETERS","**-i**\n> Display information about the flashcard system and deck location\n\n**-p** _previewer_\n> Change the card previewer (default: bat, fallback: cat)\n\n**-h**\n> Display help message\n\n**-v**\n> Display version information"],["DESCRIPTION","**Flash** (fla.sh) is a terminal-based flashcard application implementing spaced repetition and active recall learning principles. It uses plain text CSV files as flashcard decks and presents cards in randomized study sessions.\n\nCards are stored in colon-separated CSV format: **category:question:answer:score**. The score field manages card difficulty—lower scores cause cards to appear more frequently, while cards rated \"Easy\" receive higher scores and appear less often.\n\nThe tool uses **fzf** for fuzzy selection of decks and optionally **bat** for syntax-highlighted card display. Decks are stored in **~/.local/share/flash/** and support nested directory organization."],["CAVEATS","Requires **fzf** as a mandatory dependency for deck selection. On macOS, GNU coreutils must be installed via Homebrew for the **shuf** command used in randomization. The tool creates its data directory on first run if it does not exist."],["HISTORY","Flash was created by **Bryan Jenks** (tallguyjenks) as a lightweight terminal alternative to graphical flashcard applications. Written entirely in **Bash**, it draws inspiration from spaced repetition systems like Anki while maintaining Unix philosophy simplicity."],["INSTALL","```apt: sudo apt install flash```\n\n```brew: brew install flash```"],["SEE ALSO","[fzf](/man/fzf)(1), [bat](/man/bat)(1), [cat](/man/cat)(1)"],["RESOURCES","```[Source code](https://github.com/tallguyjenks/fla.sh)```"]],"e":["fzf","bat","cat"],"x":"flash"},"forth":{"n":"forth","t":"stack-based interactive programming language","s":[["TAGLINE","stack-based interactive programming language"],["TLDR","**Start Forth interpreter (Gforth)**\n\n```gforth```\n\n**Run Forth script**\n\n```gforth [script.fs]```\n\n**Evaluate expression**\n\n```gforth -e \"[1 2 + .] [bye]\"```\n\n**Include file then go interactive**\n\n```gforth [script.fs] -```"],["SYNOPSIS","**gforth** [_options_] [_file_...]"],["PARAMETERS","**-e** _code_\n> Evaluate Forth code.\n\n**-m** _size_\n> Dictionary size.\n\n**-d** _size_\n> Data stack size.\n\n**-r** _size_\n> Return stack size.\n\n_file_\n> Forth source file to load.\n\n**-**\n> Read from stdin."],["DESCRIPTION","**Forth** is a stack-based programming language known for its simplicity and extensibility. Gforth is the GNU implementation of Forth, conforming to the ANS Forth standard.\n\nForth uses reverse Polish notation (RPN) with an explicit stack. Words (functions) are defined with `:` and `;`. The language is highly interactive and extensible, commonly used in embedded systems."],["BASIC OPERATIONS","```forth\n\\ Push numbers, add, print\n2 3 + .   \\ prints 5\n\n\\ Define a word\n: square ( n -- n^2 ) dup * ;\n5 square .   \\ prints 25\n\n\\ Stack manipulation\n1 2 3 swap   \\ 1 3 2\ndup          \\ duplicates top\ndrop         \\ removes top\n```"],["CAVEATS","Stack-based paradigm differs from most languages. Manual memory management. Limited standard library compared to modern languages. Debugging can be challenging."],["HISTORY","Forth was invented by **Charles H. Moore** in the late **1960s** for controlling radio telescopes. It became popular in embedded systems due to its small footprint and efficiency. Gforth was started in **1992** by Anton Ertl and Bernd Paysan."],["SEE ALSO","[dc](/man/dc)(1)"]],"e":["dc"],"x":"gforth"},"gixy-next":{"n":"gixy-next","t":"Maintained fork of gixy for nginx security analysis","s":[["TAGLINE","Maintained fork of gixy for nginx security analysis"],["TLDR","**Analyze nginx config**\n\n```gixy-next [/etc/nginx/nginx.conf]```\n\n**Run only specific checks**\n\n```gixy-next --tests [ssrf,host_spoofing] [config.conf]```\n\n**Skip specific checks**\n\n```gixy-next --skips [http_splitting] [config.conf]```\n\n**Output as JSON**\n\n```gixy-next -f json [config.conf]```\n\n**Show only medium severity and above**\n\n```gixy-next -ll [config.conf]```"],["SYNOPSIS","**gixy-next** [_options_] _config_"],["PARAMETERS","_CONFIG_\n> Nginx configuration file.\n\n**--tests** _RULES_\n> Only run specific checks (comma-separated).\n\n**--skips** _RULES_\n> Skip specific checks (comma-separated).\n\n**-f**, **--format** _FORMAT_\n> Output format (text, json).\n\n**-l**\n> Filter by severity level. Use -l for LOW+, -ll for MEDIUM+, -lll for HIGH only.\n\n**--help**\n> Display help information."],["DESCRIPTION","**gixy-next** is a maintained fork of gixy for nginx security analysis. It continues development of the original tool with updates, bug fixes, and support for newer nginx features.\n\nThe tool analyzes nginx configurations for security issues, detecting misconfigurations that could lead to vulnerabilities such as SSRF and host spoofing."],["CAVEATS","Fork of original gixy with additional checks and performance features. Defaults to analyzing /etc/nginx/nginx.conf if no path given. Can also read config from stdin."],["HISTORY","gixy-next continues development of **gixy** after the original project became less actively maintained."],["INSTALL","```brew: brew install gixy```\n\n```nix: nix profile install nixpkgs#gixy```"],["SEE ALSO","[gixy](/man/gixy)(1), [nginx](/man/nginx)(1)"]],"e":["gixy","nginx"],"x":"gixy-next [/etc/nginx/nginx.conf]"},"gphotos-uploader-cli":{"n":"gphotos-uploader-cli","t":"Mass upload media folders to Google Photos","s":[["TAGLINE","Mass upload media folders to Google Photos"],["TLDR","**Initialize** default configuration\n\n```gphotos-uploader-cli init```\n\n**Overwrite** existing configuration\n\n```gphotos-uploader-cli init --force```\n\n**Authenticate** with Google Photos (browser flow)\n\n```gphotos-uploader-cli auth```\n\n**Upload** configured folders\n\n```gphotos-uploader-cli push```\n\n**Dry-run** upload (no files sent)\n\n```gphotos-uploader-cli push --dry-run```\n\n**List albums**\n\n```gphotos-uploader-cli list albums```\n\n**Show version**\n\n```gphotos-uploader-cli version```"],["SYNOPSIS","**gphotos-uploader-cli** [*global-options*] *command* [*args*]"],["DESCRIPTION","**gphotos-uploader-cli** is a Google Photos command-line client for Linux (and other platforms where Go binaries run). The official Google backup tools target macOS and Windows; this project fills the gap for bulk folder upload, album organization, and listing media already in the library.\n\nWorkflow: **init** creates config under **~/.gphotos-uploader-cli** (override with **--config**), you set Google API app credentials and upload jobs in **config.hjson**, **auth** completes OAuth in a browser, then **push** scans configured source folders and uploads new objects. A file tracker avoids re-uploading known media."],["PARAMETERS","**init**\n\n> Create default configuration. **--force** overwrites an existing config.\n\n**auth**\n\n> Run the OAuth authentication flow against Google Photos.\n\n**push**\n\n> Upload local folders defined in the config jobs. **--dry-run** reports work without uploading.\n\n**list albums** / **list** media-related subcommands\n\n> List albums or media items already in Google Photos.\n\n**version**\n\n> Print the CLI version.\n\n**reset**\n\n> Reset local tracking state (see **gphotos-uploader-cli reset --help**).\n\nGlobal flags include **--config** (config directory), **--debug**, and **--silent** (mutually exclusive with debug)."],["CONFIGURATION","Default directory: **~/.gphotos-uploader-cli**, file **config.hjson**. You must supply Google Photos API app credentials (**APIAppCredentials**) and define **jobs** (source folders, include/exclude patterns, album policy). See the project documentation for the full schema."],["CAVEATS","Requires Google API credentials and interactive browser OAuth (headless setups need SSH port forwarding to the local callback port). The project has sought new maintainers; check GitHub for current status. Google Photos API quotas and policy limits apply. Not a full replacement for Google Takeout or the official backup clients."],["INSTALL","```brew: brew install gphotos-uploader-cli```"],["SEE ALSO","[rclone](/man/rclone)(1), [gdrive](/man/gdrive)(1), [curl](/man/curl)(1)"],["RESOURCES","```[Source code](https://github.com/gphotosuploader/gphotos-uploader-cli)```\n\n```[Documentation](https://gphotosuploader.github.io/gphotos-uploader-cli)```"]],"e":["rclone","gdrive","curl"],"x":"gphotos-uploader-cli init"},"grub-script-check":{"n":"grub-script-check","t":"validate GRUB configuration script syntax","s":[["TAGLINE","validate GRUB configuration script syntax"],["TLDR","Check a GRUB script file for **syntax errors**\n\n```grub-script-check [path/to/grub.cfg]```\n\nDisplay each line of input after reading (**verbose**)\n\n```grub-script-check -v [path/to/grub.cfg]```\n\nCheck script from **stdin**\n\n```cat [path/to/script] | grub-script-check```\n\nDisplay **help**\n\n```grub-script-check --help```"],["SYNOPSIS","**grub-script-check** [_options_] [_file_]"],["PARAMETERS","**-v**, **--verbose**\n> Display each line of input after reading it\n\n**--help**\n> Display help message\n\n**--version**\n> Display version information"],["DESCRIPTION","**grub-script-check** validates GRUB configuration scripts for syntax errors. It parses the GRUB scripting language used in configuration files like grub.cfg and reports any syntax problems.\n\nIf no file path is provided, the tool reads from standard input. This is useful for validating generated configurations or testing script fragments before deployment."],["CAVEATS","Only checks syntax, not semantic correctness. Valid syntax doesn't guarantee the configuration will boot properly. Cannot verify that referenced files or modules exist."],["HISTORY","grub-script-check is part of GRUB 2, providing validation for GRUB's configuration scripting language. The scripting capability was introduced in GRUB 2 to replace the simpler configuration format of GRUB Legacy."],["INSTALL","```apt: sudo apt install grub-common```\n\n```pacman: sudo pacman -S grub```\n\n```apk: sudo apk add grub```"],["SEE ALSO","[grub-mkconfig](/man/grub-mkconfig)(8), [grub-install](/man/grub-install)(8), [update-grub](/man/update-grub)(8)"],["RESOURCES","```[Homepage](https://www.gnu.org/software/grub/)```\n\n```[Source code](https://git.savannah.gnu.org/cgit/grub.git)```\n\n```[Documentation](https://www.gnu.org/software/grub/manual/grub/grub.html)```"]],"e":["grub-mkconfig","grub-install","update-grub"],"x":"grub-script-check [path/to/grub.cfg]"},"hdparm":{"n":"hdparm","t":"Get and set SATA/IDE hard drive parameters","s":[["TAGLINE","Get and set SATA/IDE hard drive parameters"],["TLDR","**Get device identification info**\n\n```sudo hdparm -I [/dev/sda]```\n\n**Test buffered and cached read speed**\n\n```sudo hdparm -tT [/dev/sda]```\n\n**Get or set write-caching**\n\n```sudo hdparm -W [1] [/dev/sda]```\n\n**Show current power mode status**\n\n```sudo hdparm -C [/dev/sda]```\n\n**Set Advanced Power Management level** (1-127 allow spindown, 128-254 keep spinning)\n\n```sudo hdparm -B [127] [/dev/sda]```\n\n**Set standby timeout** (value in 5-second increments, 0 to disable)\n\n```sudo hdparm -S [60] [/dev/sda]```\n\n**Force drive into standby mode**\n\n```sudo hdparm -y [/dev/sda]```"],["SYNOPSIS","**hdparm** [_OPTIONS_] _device_"],["DESCRIPTION","**hdparm** gets and sets SATA/IDE hard drive parameters. It can display device information, configure power management settings, test performance, and enable or disable drive features."],["PARAMETERS","**-I**\n> Display detailed device identification\n\n**-i**\n> Display kernel's cached identification info\n\n**-B** [_value_]\n> Get or set Advanced Power Management (1-254)\n\n**-C**\n> Display current power mode status\n\n**-S** _value_\n> Set standby timeout (0=off, 1-240=5s increments, 241-251=30m increments)\n\n**-y**\n> Force drive into standby mode\n\n**-Y**\n> Force drive into sleep mode (may require hard reset)\n\n**-t**\n> Test buffered read speed\n\n**-T**\n> Test cache read speed\n\n**-g**\n> Display drive geometry (cylinders, heads, sectors, size)\n\n**-M** [_value_]\n> Get or set Automatic Acoustic Management (0=off, 128=quiet, 254=fast)\n\n**-W** [_value_]\n> Get or set write-caching (0=off, 1=on)\n\n**-F**\n> Flush drive write cache\n\n**--security-erase** _password_\n> Secure erase the drive (WARNING: destroys all data)"],["CAVEATS","Some features require kernel and hardware support. APM values 1-127 allow spindown while 128-254 keep the drive spinning. Sleep mode (-Y) may require power cycling to recover. When no options are given, -acdgkmur is assumed."],["HISTORY","**hdparm** has been the standard Linux tool for drive parameter control since the early days of IDE drives."],["INSTALL","```apt: sudo apt install hdparm```\n\n```dnf: sudo dnf install hdparm```\n\n```pacman: sudo pacman -S hdparm```\n\n```apk: sudo apk add hdparm```\n\n```zypper: sudo zypper install hdparm```\n\n```nix: nix profile install nixpkgs#hdparm```"],["SEE ALSO","[smartctl](/man/smartctl)(8), [blkid](/man/blkid)(8), [lsblk](/man/lsblk)(8)"]],"e":["smartctl","blkid","lsblk"],"x":"sudo hdparm -I [/dev/sda]"},"iam-policy-autopilot":{"n":"iam-policy-autopilot","t":"Generate baseline AWS IAM policies from application source","s":[["TAGLINE","Generate baseline AWS IAM policies from application source"],["TLDR","**Generate policies** from source (pretty JSON)\n\n```iam-policy-autopilot generate-policies [./src/app.py] --pretty```\n\n**Limit analysis** with service hints\n\n```iam-policy-autopilot generate-policies [./src/app.py] --service-hints s3 iam organizations --pretty```\n\n**Include region and account** in resource ARNs\n\n```iam-policy-autopilot generate-policies [./src] --region [us-east-1] --account [123456789012] --pretty```\n\n**Suggest fixes** for an AccessDenied error\n\n```iam-policy-autopilot fix-access-denied \"[error message text]\"```\n\n**Start MCP server** for AI coding assistants\n\n```iam-policy-autopilot mcp-server```\n\n**MCP over HTTP**\n\n```iam-policy-autopilot mcp-server --transport http```"],["SYNOPSIS","**iam-policy-autopilot** *command* [*options*]"],["DESCRIPTION","**IAM Policy Autopilot** is an open-source CLI and MCP server from AWS Labs that analyzes application code locally and produces baseline identity-based IAM policies for the AWS SDK calls it finds. Supported languages and SDKs include Python (Boto3/Botocore), Go (AWS SDK v2), Java (SDK v2), and JavaScript/TypeScript (AWS SDK v3).\n\nIt also helps interpret **AccessDenied** errors and can run as an MCP server so AI coding assistants can request policy generation. Install via **uvx iam-policy-autopilot**, **pip install iam-policy-autopilot**, or the project install script to **/usr/local/bin/iam-policy-autopilot**.\n\nGenerated policies are starting points: review and tighten them before production. The tool does not produce resource-based policies (bucket policies, KMS key policies), SCPs, RCPs, or permission boundaries."],["PARAMETERS","**generate-policies** *source_files*...\n\n> Analyze sources and emit IAM policy documents.\n>\n> **--region** *REGION* – region for resource ARNs\n>\n> **--account** *ACCOUNT* – AWS account id for ARNs\n>\n> **--service-hints** *SERVICES* – limit analysis to named services (recommended)\n>\n> **--upload-policies** *PREFIX* – upload generated policies to IAM with a name prefix\n>\n> **--pretty** – pretty-print JSON\n\n**fix-access-denied** *message*\n\n> Analyze an AccessDenied error string and suggest policy changes. **--yes** applies changes without confirmation.\n\n**mcp-server**\n\n> Start the MCP server. **--transport** **stdio** (default) or **http**.\n\n**-h**, **--help** / **-V**, **--version**\n\n> Help and version."],["CAVEATS","Static analysis may include extra actions when method names match multiple services; use **--service-hints**. Runtime resource names are not always known, so resource ARNs may be broad. AI assistants may further edit policies when generating IaC; always review before deploy. Some commands need AWS credentials configured (AWS CLI profile/region). Corporate proxies may need network allowlisting for AWS endpoints."],["SEE ALSO","[aws](/man/aws)(1), [iam-policy-json-to-terraform](/man/iam-policy-json-to-terraform)(1), [policy_sentry](/man/policy_sentry)(1)"],["RESOURCES","```[Source code](https://github.com/awslabs/iam-policy-autopilot)```\n\n```[Documentation](https://github.com/awslabs/iam-policy-autopilot#readme)```"]],"e":["aws","iam-policy-json-to-terraform","policy_sentry"],"x":"iam-policy-autopilot generate-policies [./src/app.py] --pretty"},"importctl":{"n":"importctl","t":"manages disk images for systemd containers and system extensions","s":[["TAGLINE","manages disk images for systemd containers and system extensions"],["TLDR","**Download** a tarball image from URL\n\n```sudo importctl pull-tar [URL] [name]```\n\n**Pull** a raw or qcow2 disk image\n\n```sudo importctl pull-raw [https://example.com/source.ext] [name] --class=[machine|portable|sysext|confext]```\n\n**Import** a raw disk image\n\n```importctl import-raw [path/to/file.ext] [name] --class=[machine|portable|sysext|confext]```\n\n**List** current transfers in progress\n\n```importctl list-transfers```\n\n**Cancel** a running transfer\n\n```importctl cancel-transfer [transfer-id]```\n\n**Export** a container image as tarball\n\n```importctl export-tar --class=[machine|portable|sysext|confext] [name] [path/to/file.ext]```"],["SYNOPSIS","**importctl** _command_ [_options_]"],["PARAMETERS","**pull-tar** _URL_ _NAME_\n> Download and import tarball from URL\n\n**pull-raw** _URL_ _NAME_\n> Download and import raw/qcow2 disk image\n\n**import-tar** _FILE_ _NAME_\n> Import local tarball into image directory\n\n**import-raw** _FILE_ _NAME_\n> Import local raw disk image\n\n**export-tar** _NAME_ _FILE_\n> Export container as tarball\n\n**export-raw** _NAME_ _FILE_\n> Export container as raw disk image\n\n**list-transfers**\n> Show currently running transfers\n\n**cancel-transfer** _ID_\n> Cancel a running import, export, or download\n\n**--class** _TYPE_\n> Image class: machine, portable, sysext, confext\n\n**--verify** _MODE_\n> Verification mode for downloads: no, checksum, signature (default: signature)\n\n**--format** _FORMAT_\n> Image format for raw operations: raw, qcow2 (default: raw)"],["DESCRIPTION","**importctl** manages disk images for systemd containers and system extensions. It can download, import, and export various image formats including tarballs, raw disk images, and qcow2 virtual machine images.\n\nThe tool integrates with systemd-machined and systemd-importd services to handle image management for containers, portable services, and system/configuration extensions."],["CAVEATS","Requires systemd and appropriate services running. Some operations require root privileges. Network operations depend on systemd-importd service."],["HISTORY","importctl is part of systemd, introduced to provide a unified interface for managing container and extension images. It replaces older, more fragmented approaches to image management in the systemd ecosystem."],["INSTALL","```apt: sudo apt install systemd-container```\n\n```dnf: sudo dnf install systemd-container```\n\n```zypper: sudo zypper install systemd-container```"],["SEE ALSO","[machinectl](/man/machinectl)(1), [systemd-nspawn](/man/systemd-nspawn)(1), [systemctl](/man/systemctl)(1)"]],"e":["machinectl","systemd-nspawn","systemctl"],"x":"sudo importctl pull-tar [URL] [name]"},"iwconfig":{"n":"iwconfig","t":"Configure and display wireless network interface parameters","s":[["TAGLINE","Configure and display wireless network interface parameters"],["TLDR","**Show all wireless interfaces and their settings**\n\n```iwconfig```\n\n**Show a specific wireless interface**\n\n```iwconfig [wlan0]```\n\n**Set the network name (ESSID)**\n\n```iwconfig [wlan0] essid [network_name]```\n\n**Set the operating mode**\n\n```iwconfig [wlan0] mode [Managed]```\n\n**Set the channel**\n\n```iwconfig [wlan0] channel [6]```\n\n**Set the transmit power in dBm**\n\n```iwconfig [wlan0] txpower [20]```\n\n**Set the bit rate**\n\n```iwconfig [wlan0] rate [54M]```\n\n**Enable monitor mode on an interface**\n\n```iwconfig [wlan0] mode Monitor```"],["SYNOPSIS","**iwconfig** [_interface_] [_parameter_ _value_...]"],["PARAMETERS","**essid** _name_\n> Set the network name (SSID). Use **essid off** or **essid any** to disable ESSID checking.\n\n**mode** _mode_\n> Set operating mode: Ad-Hoc, Managed, Master, Repeater, Secondary, Monitor, or Auto.\n\n**freq** _frequency_\n> Set operating frequency (e.g. 2.46G for 2.46 GHz). Append k, M, or G suffix.\n\n**channel** _channel_\n> Set operating channel number. Use **iwlist** to see available channels.\n\n**ap** _address_\n> Force association with a specific access point by MAC address. Use **ap off** to re-enable auto selection.\n\n**rate** _rate_\n> Set the bit rate (e.g. 11M, 54M). Use **rate auto** for automatic selection.\n\n**txpower** _power_\n> Set transmit power in dBm, or use mW suffix for milliwatts. Use **txpower off** to disable the radio.\n\n**sens** _threshold_\n> Set sensitivity threshold for roaming decisions.\n\n**retry** _limit_\n> Set maximum MAC retransmission retry limit.\n\n**rts** _threshold_\n> Set the RTS/CTS handshake threshold in bytes. Use **rts off** to disable.\n\n**frag** _threshold_\n> Set maximum fragment size in bytes. Use **frag off** to disable.\n\n**power** _mode_\n> Set power management mode. Use **power off** to disable power management.\n\n**key** _key_\n> Set WEP encryption key (deprecated and insecure)."],["DESCRIPTION","**iwconfig** is similar to **ifconfig**, but is dedicated to wireless network interfaces. It is used to set parameters specific to wireless operation such as ESSID, frequency, mode, encryption key, and power management. It can also display those parameters along with wireless statistics from /proc/net/wireless.\n\nWhen called without arguments, iwconfig displays the wireless parameters for all interfaces. When called with an interface name only, it displays the settings for that interface."],["CAVEATS","**iwconfig** is deprecated in favor of **iw**. WEP encryption configured via **key** is insecure and should not be used. For WPA/WPA2/WPA3, use **wpa_supplicant** instead. Only the superuser can change wireless parameters."],["HISTORY","**iwconfig** was part of the **wireless-tools** package created by Jean Tourrilhes at Hewlett-Packard. It has been largely superseded by **iw** and **wpa_supplicant** for modern wireless configuration."],["INSTALL","```apt: sudo apt install wireless-tools```\n\n```apk: sudo apk add wireless-tools```\n\n```zypper: sudo zypper install wireless-tools```"],["SEE ALSO","[iw](/man/iw)(8), [iwlist](/man/iwlist)(8), [ifconfig](/man/ifconfig)(8), [wpa_supplicant](/man/wpa_supplicant)(8)"]],"e":["iw","iwlist","ifconfig","wpa_supplicant"],"x":"iwconfig"},"jfrog":{"n":"jfrog","t":"Official CLI for the JFrog Platform","s":[["TAGLINE","Official CLI for the JFrog Platform"],["TLDR","**Configure** a server connection interactively\n\n```jfrog config add [server-id]```\n\n**Upload** artifacts to Artifactory\n\n```jfrog rt upload [file_pattern] [repo/path/]```\n\n**Download** artifacts from Artifactory\n\n```jfrog rt download [repo/path/pattern]```\n\n**Search** for artifacts in Artifactory\n\n```jfrog rt search [repo/path/pattern]```\n\n**Publish** build info to Artifactory\n\n```jfrog rt build-publish [build-name] [build-number]```\n\n**Scan** the current project for security vulnerabilities\n\n```jfrog audit```\n\n**Push** a Docker image to an Artifactory Docker registry\n\n```jfrog docker push [image:tag] [repo]```\n\n**Delete** artifacts matching a pattern\n\n```jfrog rt delete [repo/path/pattern]```"],["SYNOPSIS","**jfrog** _command_ [_subcommand_] [_options_] [_arguments_]"],["PARAMETERS","**config**\n> Manage server configurations (add, edit, remove, show, export, import).\n\n**rt**\n> Artifactory commands (upload, download, search, delete, copy, move, build-publish, etc.).\n\n**xr**\n> Xray commands for security scanning and vulnerability management.\n\n**audit**\n> Scan project dependencies for security vulnerabilities and license compliance.\n\n**docker**\n> Docker registry integration (push and pull via Artifactory).\n\n**npm**, **pip**, **go**, **maven**, **gradle**, **nuget**\n> Package manager integrations with build-info collection.\n\n**--url** _URL_\n> JFrog Platform URL.\n\n**--access-token** _TOKEN_\n> Access token for authentication.\n\n**--user** _USER_\n> Username for authentication.\n\n**--password** _PASSWORD_\n> Password for authentication.\n\n**--server-id** _ID_\n> Use a specific configured server.\n\n**--help**\n> Display help information for a command."],["DESCRIPTION","**jfrog** is the official CLI for the JFrog Platform. It provides unified command-line access to Artifactory, Xray, Distribution, Pipelines, and other JFrog services.\n\nThe tool manages binary artifacts across all major package types including Maven, npm, PyPI, Docker, Go, NuGet, and generic files. It integrates with CI/CD pipelines for build-info collection, promotion, and security scanning. The CLI supports file specs (JSON-based query syntax) for advanced artifact operations.\n\nThe **jfrog** executable has been renamed to **jf** in recent versions, though the **jfrog** name continues to work as an alias."],["CAVEATS","Requires a JFrog server (self-hosted or SaaS). Enterprise features such as Xray scanning require appropriate licensing. Server configuration is stored locally per user and must be set up before use with **jfrog config add**."],["HISTORY","JFrog CLI was created by **JFrog** to provide unified command-line access to their artifact management platform. The CLI executable was renamed from **jfrog** to **jf** starting in 2022, though both names remain functional."],["SEE ALSO","[jf](/man/jf)(1), [docker](/man/docker)(1), [mvn](/man/mvn)(1), [npm](/man/npm)(1), [pip](/man/pip)(1), [go](/man/go)(1), [gradle](/man/gradle)(1)"]],"e":["jf","docker","mvn","npm","pip","go","gradle"],"x":"jfrog config add [server-id]"},"jj-git-init":{"n":"jj-git-init","t":"initializes a Jujutsu repository with Git backend","s":[["TAGLINE","initializes a Jujutsu repository with Git backend"],["TLDR","**Create a new colocated jj/Git repo (default)**\n\n```jj git init```\n\n**Create a new jj repo at a specific path**\n\n```jj git init [path]```\n\n**Initialize jj using an existing Git repo as the backing store**\n\n```jj git init --git-repo=[path/to/git-repo]```"],["SYNOPSIS","**jj** **git** **init** [_options_] [_path_]"],["PARAMETERS","**--colocate**\n> Create a colocated jj/Git repo. This is the default unless `git.colocate` config is set to false.\n\n**--git-repo** _path_\n> Path to an existing Git repository to use as the backing store. Mutually exclusive with --colocate."],["DESCRIPTION","**jj git init** initializes a Jujutsu repository with a Git backend. By default it creates a colocated repo where both `.jj` and `.git` directories exist, allowing both jj and git commands to work on the same repo. IDE Git integration works as-is in colocated mode."],["INSTALL","```pacman: sudo pacman -S jujutsu```\n\n```apk: sudo apk add jujutsu```\n\n```zypper: sudo zypper install jujutsu```\n\n```brew: brew install jujutsu```\n\n```nix: nix profile install nixpkgs#jujutsu```"],["SEE ALSO","[jj](/man/jj)(1), [jj-git-clone](/man/jj-git-clone)(1), [jj-git-fetch](/man/jj-git-fetch)(1), [git](/man/git)(1)"]],"e":["jj","jj-git-clone","jj-git-fetch","git"],"x":"jj git init"},"kr":{"n":"kr","t":"krypton CLI for SSH key management","s":[["TAGLINE","krypton CLI for SSH key management"],["TLDR","**Pair with phone**\n\n```kr pair```\n\n**Show paired devices**\n\n```kr devices```\n\n**Generate SSH key**\n\n```kr generate --email [email@example.com]```\n\n**Add public key to server**\n\n```kr add [user@host]```\n\n**Remove pairing**\n\n```kr unpair```"],["SYNOPSIS","**kr** _command_ [_options_]"],["PARAMETERS","**pair**\n> Pair with Krypton phone app.\n\n**devices**\n> List paired devices.\n\n**generate**\n> Generate SSH key on phone.\n\n**add** _HOST_\n> Add key to host.\n\n**unpair**\n> Remove device pairing.\n\n**--help**\n> Display help information."],["DESCRIPTION","**kr** is the command-line interface for Krypton, a system that stores SSH and GPG private keys on a paired smartphone rather than on the computer. By keeping private keys on a separate device, Krypton provides a form of two-factor authentication for SSH connections: each login attempt triggers a push notification on the phone that must be approved before the connection proceeds.\n\nThe CLI handles pairing with the Krypton mobile app, generating keys on the phone, and copying the corresponding public key to remote servers. Once configured, kr integrates transparently with the local SSH agent so that standard ssh commands work without modification. The Krypton project has been deprecated, but the CLI and its architecture influenced later approaches to hardware-backed SSH key management."],["CAVEATS","Requires Krypton app. Phone needed for auth. Project deprecated."],["HISTORY","kr was the CLI for **Krypton** (now deprecated), which stored SSH and GPG keys on smartphones for 2FA."],["INSTALL","```aur: yay -S kr```"],["SEE ALSO","[ssh](/man/ssh)(1), [ssh-keygen](/man/ssh-keygen)(1), [gpg](/man/gpg)(1)"]],"e":["ssh","ssh-keygen","gpg"],"x":"kr pair"},"larasail":{"n":"larasail","t":"server provisioning tool for deploying Laravel applications on Digital Ocean","s":[["TAGLINE","server provisioning tool for deploying Laravel applications on Digital Ocean"],["TLDR","**Set up** server with default PHP version\n\n```larasail setup```\n\nSet up server with **specific PHP** version\n\n```larasail setup [php71]```\n\n**Add** a new Laravel site\n\n```larasail host [domain] [path/to/site_directory]```\n\nRetrieve Larasail **user password**\n\n```larasail pass```\n\nRetrieve **MySQL password**\n\n```larasail mysqlpass```"],["SYNOPSIS","**larasail** _command_ [_arguments_]"],["PARAMETERS","**setup** [_php_version_]\n> Set up server with Laravel dependencies\n\n**host** _domain_ _directory_\n> Add a new Laravel site\n\n**pass**\n> Display the Larasail user password\n\n**mysqlpass**\n> Display the MySQL password"],["DESCRIPTION","**larasail** is a server provisioning tool for deploying Laravel applications on Digital Ocean servers. It automates the installation of PHP, Nginx, MySQL, Composer, and other Laravel dependencies.\n\nThe tool simplifies the process of setting up a production Laravel environment, handling web server configuration, SSL certificates, and database setup."],["CAVEATS","Designed specifically for Digital Ocean droplets running Ubuntu. Requires root access on the server. SSL certificate configuration may require additional steps."],["HISTORY","Larasail was created by DevDojo to simplify Laravel deployment, providing a lightweight alternative to more complex server management tools."],["SEE ALSO","[composer](/man/composer)(1), [php](/man/php)(1), [nginx](/man/nginx)(8)"]],"e":["composer","php","nginx"],"x":"larasail setup"},"lvextend":{"n":"lvextend","t":"increases the size of a logical volume in LVM","s":[["TAGLINE","increases the size of a logical volume in LVM"],["TLDR","Extend to **absolute** size\n\n```sudo lvextend -L 120G logical_volume```\n\nExtend by **relative** size\n\n```sudo lvextend -L +40G -r logical_volume```\n\nUse **all free** space\n\n```sudo lvextend -l +100%FREE logical_volume```\n\nExtend and **resize filesystem**\n\n```sudo lvextend -l +100%FREE -r logical_volume```"],["SYNOPSIS","**lvextend** [_OPTIONS_] _LogicalVolume_"],["DESCRIPTION","**lvextend** increases the size of a logical volume in LVM. It can extend by an absolute size, relative size, or percentage of available space. The underlying filesystem can be resized automatically."],["PARAMETERS","**-L, --size** _size_\n> Specify new absolute size or relative increase (+size)\n\n**-l, --extents** _extents_\n> Specify size in extents or percentage (%VG, %FREE, %PVS)\n\n**-r, --resizefs**\n> Automatically resize the underlying filesystem\n\n**-n, --nofsck**\n> Skip filesystem check before resize\n\n**--use-policies**\n> Use thin pool autoextend policies"],["CAVEATS","Ensure sufficient free space in the volume group. The filesystem resize with -r only works for ext2/3/4 and XFS. For other filesystems, manual resize is required after extending. XFS can only grow, not shrink."],["HISTORY","**lvextend** is part of **LVM2**, the Linux Logical Volume Manager, providing dynamic storage management."],["INSTALL","```apt: sudo apt install lvm2```\n\n```dnf: sudo dnf install lvm2```\n\n```pacman: sudo pacman -S lvm2```\n\n```apk: sudo apk add lvm2```\n\n```zypper: sudo zypper install lvm2```\n\n```nix: nix profile install nixpkgs#lvm2```"],["SEE ALSO","[lvm](/man/lvm)(8), [lvresize](/man/lvresize)(8), [lvreduce](/man/lvreduce)(8), [lvcreate](/man/lvcreate)(8)"]],"e":["lvm","lvresize","lvreduce","lvcreate"],"x":"sudo lvextend -L 120G logical_volume"},"mcs":{"n":"mcs","t":"mono C# compiler","s":[["TAGLINE","mono C# compiler"],["TLDR","**Compile C# file**\n\n```mcs [program.cs]```\n\n**Output executable**\n\n```mcs -out:[program.exe] [program.cs]```\n\n**Build library**\n\n```mcs -target:library [library.cs]```\n\n**Reference assembly**\n\n```mcs -r:[System.dll] [program.cs]```\n\n**Debug build**\n\n```mcs -debug [program.cs]```\n\n**Compile multiple files**\n\n```mcs [file1.cs] [file2.cs] -out:[program.exe]```"],["SYNOPSIS","**mcs** [_options_] _files_"],["PARAMETERS","_FILES_\n> C# source files to compile.\n\n**-out:**_FILE_\n> Output file name.\n\n**-target:**_TYPE_\n> Output type: **exe** (console app, default), **library** (DLL), **module**, or **winexe** (GUI app).\n\n**-r:**_ASSEMBLY_\n> Reference an external assembly.\n\n**-debug**\n> Emit debugging information.\n\n**-optimize**\n> Enable compiler optimizations.\n\n**-define:**_SYMBOL_\n> Define a preprocessor symbol.\n\n**-pkg:**_PACKAGE_\n> Reference packages registered with pkg-config.\n\n**-recurse:**_PATTERN_\n> Compile all files matching the pattern recursively.\n\n**-warn:**_LEVEL_\n> Set warning level (0-4, default 4).\n\n**-warnaserror**\n> Treat warnings as errors.\n\n**-nowarn:**_NUMBERS_\n> Suppress specified warning numbers.\n\n**-unsafe**\n> Enable compilation of unsafe code.\n\n**-checked**\n> Enable overflow checking.\n\n**-main:**_CLASS_\n> Specify which class contains the Main entry point.\n\n**-lib:**_PATHLIST_\n> Comma-separated list of directories to search for assemblies.\n\n**-sdk:**_VERSION_\n> Specify Base Class Library version (2 or 4, default 4).\n\n**-noconfig**\n> Disable loading the default compiler configuration.\n\n**-langversion:**_VERSION_\n> Specify C# language version (e.g., default, latest, ISO-1, ISO-2, 3-7).\n\n**--help**\n> Display help information."],["DESCRIPTION","**mcs** is the Mono C# compiler. It compiles C# source code into Common Intermediate Language (CIL) assemblies that run on the Mono or .NET runtime. It supports C# language versions up to C# 7.\n\nOptions use a colon separator (e.g., `-out:file.exe`, `-target:library`) following Microsoft csc conventions, though the alternate syntax with a space is also accepted in many cases."],["CAVEATS","Part of the Mono framework. The Mono project has been largely superseded by .NET (Core/.NET 5+) and the Roslyn-based **csc** compiler. New projects should prefer **dotnet build** or **csc**. Language support stops at C# 7; newer C# features require Roslyn."],["HISTORY","**mcs** is the **Mono** project's C# compiler, originally written by **Miguel de Icaza** and the Mono team. It was one of the first open-source implementations of the C# compiler, enabling .NET development on Linux and macOS before Microsoft open-sourced .NET."],["INSTALL","```pacman: sudo pacman -S mono```\n\n```apk: sudo apk add mono```\n\n```brew: brew install mono```\n\n```nix: nix profile install nixpkgs#mono```"],["SEE ALSO","[mono](/man/mono)(1), [csc](/man/csc)(1), [dotnet](/man/dotnet)(1)"]],"e":["mono","csc","dotnet"],"x":"mcs [program.cs]"},"mount":{"n":"mount","t":"attaches a filesystem found on a device to the directory tree","s":[["TAGLINE","attaches a filesystem found on a device to the directory tree"],["TLDR","Show all **mounted filesystems**\n\n```mount```\n\n**Mount** a device to a directory\n\n```mount [path/to/device] [path/to/target_directory]```\n\nMount a device for a **specific user**\n\n```mount -o uid=[user_id],gid=[group_id] [device] [directory]```\n\nMount a **CD-ROM** as read-only\n\n```mount -t iso9660 -o ro /dev/cdrom /cdrom```\n\nMount all filesystems defined in **/etc/fstab**\n\n```mount -a```\n\n**Bind mount** a directory to another location\n\n```mount --bind [old_directory] [new_directory]```\n\nCreate directory if needed and **mount**\n\n```mount -m [device] [directory]```"],["SYNOPSIS","**mount** [_-t type_] [_-o options_] [_device_] [_directory_]"],["DESCRIPTION","**mount** attaches a filesystem found on a device to the directory tree. Without arguments, it displays currently mounted filesystems. The filesystem type can be auto-detected in most cases."],["PARAMETERS","**-t, --types type**\n> Specify filesystem type (ext4, ntfs, iso9660, vfat, etc.)\n\n**-o, --options options**\n> Mount options (ro, rw, noexec, nosuid, uid, gid, etc.)\n\n**-a, --all**\n> Mount all filesystems in /etc/fstab\n\n**-B, --bind**\n> Bind mount (mount directory to another location)\n\n**-m, --mkdir**\n> Create mount point directory if it doesn't exist\n\n**-r, --read-only**\n> Mount read-only (equivalent to -o ro)\n\n**-w, --rw**\n> Mount read-write (default)\n\n**-v, --verbose**\n> Verbose mode\n\n**-n**\n> Mount without writing to /etc/mtab\n\n**-L label**\n> Mount partition by label\n\n**-U uuid**\n> Mount partition by UUID"],["COMMON OPTIONS","**ro**: read-only\n**rw**: read-write\n**noexec**: don't allow execution\n**nosuid**: ignore setuid bits\n**noatime**: don't update access times\n**uid/gid**: set owner user/group"],["CAVEATS","Most mount operations require root privileges. Use /etc/fstab for persistent mounts across reboots."],["HISTORY","Part of **util-linux** package. The mount command has been part of Unix since early versions, dating back to **1971**."],["INSTALL","```apt: sudo apt install mount```\n\n```apk: sudo apk add mount```\n\n```nix: nix profile install nixpkgs#mount```"],["SEE ALSO","[umount](/man/umount)(8), [fstab](/man/fstab)(5), [findmnt](/man/findmnt)(8), [lsblk](/man/lsblk)(8)"]],"e":["umount","fstab","findmnt","lsblk"],"x":"mount"},"mtr":{"n":"mtr","t":"Network diagnostic tool combining traceroute and ping","s":[["TAGLINE","Network diagnostic tool combining traceroute and ping"],["TLDR","**Run traceroute to a host**\n\n```mtr [hostname]```\n\n**Use TCP instead of ICMP**\n\n```mtr --tcp [hostname]```\n\n**Use UDP instead of ICMP**\n\n```mtr --udp [hostname]```\n\n**Specify port for TCP/UDP**\n\n```mtr --tcp --port [443] [hostname]```\n\n**Generate a report and exit**\n\n```mtr --report [hostname]```\n\n**Generate report with N cycles**\n\n```mtr --report --report-cycles [100] [hostname]```\n\n**Show numeric IPs only without DNS resolution**\n\n```mtr --no-dns [hostname]```\n\n**Output as JSON**\n\n```mtr --json [hostname]```"],["SYNOPSIS","**mtr** [_--report_] [_--tcp_|_--udp_] [_--port num_] [_--no-dns_] [_options_] _hostname_"],["PARAMETERS","**-r**, **--report**\n> Generate report mode output and exit.\n\n**-c** _NUM_, **--report-cycles** _NUM_\n> Number of pings per hop (default: 10).\n\n**-s** _SIZE_, **--psize** _SIZE_\n> Packet size in bytes.\n\n**-T**, **--tcp**\n> Use TCP SYN packets.\n\n**-U**, **--udp**\n> Use UDP datagrams.\n\n**-P** _PORT_, **--port** _PORT_\n> Target port for TCP/UDP.\n\n**-n**, **--no-dns**\n> Do not resolve hostnames.\n\n**-b**, **--show-ips**\n> Show both hostname and IP.\n\n**-w**, **--report-wide**\n> Wide report format (no truncation).\n\n**-j**, **--json**\n> Output in JSON format.\n\n**-x**, **--xml**\n> Output in XML format.\n\n**-C**, **--csv**\n> Output in CSV format.\n\n**-i** _SECONDS_, **--interval** _SECONDS_\n> Interval between pings.\n\n**-m** _NUM_, **--max-ttl** _NUM_\n> Maximum TTL / hops.\n\n**-f** _NUM_, **--first-ttl** _NUM_\n> Starting TTL.\n\n**-4**\n> Use IPv4 only.\n\n**-6**\n> Use IPv6 only.\n\n**-a** _ADDRESS_, **--address** _ADDRESS_\n> Bind to source address.\n\n**-e**, **--mpls**\n> Display MPLS information from ICMP extensions.\n\n**-o** _FIELDS_, **--order** _FIELDS_\n> Specify fields and order for report output."],["DESCRIPTION","**mtr** (My TraceRoute) combines the functionality of traceroute and ping into a single network diagnostic tool. It sends packets with purposely low TTLs, continuously probing each hop along the route to a destination, and displays real-time statistics about packet loss and latency.\n\nThe interactive display updates continuously, showing each router hop with loss percentage, sent/received counts, and latency statistics (best, average, worst, standard deviation). This makes it easy to identify where network problems occur.\n\nReport mode (**-r**) runs a fixed number of cycles (default 10) and outputs summary statistics, useful for logging and scripting. Wide report mode (**-w**) prevents hostname truncation. Output formats include plain text, JSON, XML, and CSV for integration with monitoring systems.\n\nTCP and UDP modes help diagnose issues when ICMP is filtered. Using TCP port 443 or 80 often reaches destinations that block ping. The tool can also detect asymmetric routing and path changes.\n\nMTR is invaluable for diagnosing intermittent network issues -- you can watch it over time to catch sporadic packet loss that a single traceroute might miss."],["CAVEATS","Requires root or capabilities for raw socket access (ICMP/UDP) unless using TCP mode. Some routers don't respond to TTL-exceeded messages, appearing as \"???\". Rate limiting on routers can cause false packet loss readings. ICMP deprioritization may show higher latency than actual data traffic."],["HISTORY","**mtr** was written by **Matt Kimball** in **1997** as an improvement over traditional traceroute. The name originally stood for \"Matt's TraceRoute.\" Roger Wolff took over maintenance, adding many features. The tool has become a standard network diagnostic utility, included in most Linux distributions and available for other Unix-like systems and Windows."],["INSTALL","```apt: sudo apt install mtr```\n\n```dnf: sudo dnf install mtr```\n\n```pacman: sudo pacman -S mtr```\n\n```apk: sudo apk add mtr```\n\n```zypper: sudo zypper install mtr```\n\n```brew: brew install mtr```\n\n```nix: nix profile install nixpkgs#mtr```"],["SEE ALSO","[traceroute](/man/traceroute)(1), [ping](/man/ping)(1), [tracepath](/man/tracepath)(1), [nmap](/man/nmap)(1), [ss](/man/ss)(1), [netstat](/man/netstat)(1)"]],"e":["traceroute","ping","tracepath","nmap","ss","netstat"],"x":"mtr [hostname]"},"nerdlog":{"n":"nerdlog","t":"Fast remote-first multi-host TUI log viewer with timeline histogram","s":[["TAGLINE","Fast remote-first multi-host TUI log viewer with timeline histogram"],["TLDR","**View logs from a remote host**\n\n```nerdlog [user]@[host]```\n\n**View logs from multiple hosts**\n\n```nerdlog [user]@[host1] [user]@[host2]```"],["SYNOPSIS","**nerdlog** [_options_] [_hosts_...]"],["DESCRIPTION","**nerdlog** is a fast, remote-first, multi-host TUI log viewer with an interactive timeline histogram, inspired by Graylog and Kibana but without the bloat. It queries logs from multiple remote machines simultaneously over SSH, filters them by time range and patterns, and draws a visual timeline.\n\nThe tool reads system logs from /var/log/messages, /var/log/syslog, or journalctl, and uses standard GNU tools (bash, awk) on the remote side for filtering and histogram generation. No central server or agent installation is needed."],["CAVEATS","Requires SSH access to target hosts. Remote hosts must have bash and awk available."],["HISTORY","**nerdlog** was created by **dimonomid** and is written in **Go** using the tview library. It supports Linux, FreeBSD, macOS, and Windows (client only)."],["INSTALL","```apk: sudo apk add nerdlog```\n\n```brew: brew install nerdlog```\n\n```nix: nix profile install nixpkgs#nerdlog```"],["SEE ALSO","[journalctl](/man/journalctl)(1), [lnav](/man/lnav)(1), [lazyjournal](/man/lazyjournal)(1)"]],"e":["journalctl","lnav","lazyjournal"],"x":"nerdlog [user]@[host]"},"nginx":{"n":"nginx","t":"web server and reverse proxy","s":[["TAGLINE","web server and reverse proxy"],["TLDR","**Start nginx**\n\n```nginx```\n\n**Test configuration**\n\n```nginx -t```\n\n**Reload configuration**\n\n```nginx -s reload```\n\n**Stop nginx**\n\n```nginx -s stop```\n\n**Graceful shutdown**\n\n```nginx -s quit```\n\n**Show version**\n\n```nginx -v```\n\n**Test config and dump it** to stdout\n\n```nginx -T```\n\n**Use specific config**\n\n```nginx -c [/etc/nginx/nginx.conf]```\n\n**Set global directives** at startup\n\n```nginx -g \"[daemon off;]\"```"],["SYNOPSIS","**nginx** [_-?hqtTvV_] [_-s signal_] [_-p prefix_] [_-e filename_] [_-c filename_] [_-g directives_]"],["PARAMETERS","**-?**, **-h**\n> Print help for command-line parameters.\n\n**-t**\n> Test the configuration file: nginx checks the syntax and then tries to open files referenced in the configuration.\n\n**-T**\n> Same as **-t**, but additionally dump the configuration files to standard output (1.9.2+).\n\n**-q**\n> Suppress non-error messages during configuration testing.\n\n**-s** _SIGNAL_\n> Send a signal to the master process. The signal may be one of: **stop** (fast shutdown), **quit** (graceful shutdown), **reload** (reload configuration), or **reopen** (reopen log files).\n\n**-p** _PREFIX_\n> Set the path prefix, that is, a directory that will keep server files. Default is _/usr/local/nginx_.\n\n**-e** _FILE_\n> Use an alternative error log file. The special value **stderr** logs to standard error (1.19.5+).\n\n**-c** _FILE_\n> Use an alternative configuration file instead of the default _prefix/conf/nginx.conf_.\n\n**-g** _DIRECTIVES_\n> Set global configuration directives, for example `nginx -g \"pid /var/run/nginx.pid;\"`.\n\n**-v**\n> Print the nginx version.\n\n**-V**\n> Print the nginx version, compiler version, and configure parameters."],["DESCRIPTION","**nginx** (\"engine x\") is a high-performance HTTP and reverse proxy server, as well as a mail (IMAP/POP3/SMTP) proxy server. It is designed to handle a large number of concurrent connections with a low, predictable memory footprint using an event-driven, asynchronous architecture.\n\nRunning **nginx** with no arguments starts the server (in the foreground or as a daemon, depending on the configuration). Once running, the master process is controlled at runtime by sending signals with **-s** rather than by restarting. Changes to the configuration are applied with `nginx -s reload`, which starts new worker processes and gracefully shuts down the old ones.\n\nBeyond serving static files, nginx is widely used as a reverse proxy, load balancer, TLS terminator, and HTTP cache in front of application servers."],["CAVEATS","Behavior is driven entirely by the configuration file; always validate changes with **-t** before reloading. Paths printed in help and defaults are relative to the compile-time prefix and often differ on distribution packages (commonly _/etc/nginx/nginx.conf_)."],["HISTORY","nginx was created by **Igor Sysoev** and first publicly released in **2004** to solve the C10k problem of handling many simultaneous connections. It is now developed by **F5, Inc.** and powers a large share of the busiest sites on the web."],["INSTALL","```apt: sudo apt install nginx```\n\n```dnf: sudo dnf install nginx```\n\n```pacman: sudo pacman -S nginx```\n\n```apk: sudo apk add nginx```\n\n```zypper: sudo zypper install nginx```\n\n```brew: brew install nginx```\n\n```nix: nix profile install nixpkgs#nginx```"],["SEE ALSO","[apache2](/man/apache2)(8), [caddy](/man/caddy)(1), [haproxy](/man/haproxy)(1)"]],"e":["apache2","caddy","haproxy"],"x":"nginx"},"npm-version":{"n":"npm-version","t":"bumps package version","s":[["TAGLINE","bumps package version"],["TLDR","**Bump patch version**\n\n```npm version patch```\n\n**Bump minor version**\n\n```npm version minor```\n\n**Bump major version**\n\n```npm version major```\n\n**Set specific version**\n\n```npm version [1.2.3]```\n\n**Prerelease version**\n\n```npm version prerelease```\n\n**No git tag**\n\n```npm version patch --no-git-tag-version```"],["SYNOPSIS","**npm** **version** [_version_] [_options_]"],["PARAMETERS","_VERSION_\n> Version or increment type.\n\n**patch**\n> Increment patch (0.0.x).\n\n**minor**\n> Increment minor (0.x.0).\n\n**major**\n> Increment major (x.0.0).\n\n**--no-git-tag-version**\n> Don't create git tag.\n\n**--help**\n> Display help information."],["DESCRIPTION","**npm version** bumps package version. Updates package.json and creates git tag.\n\nThe command manages version numbers. Follows semver conventions."],["CAVEATS","Creates git commit/tag by default. Working directory must be clean."],["HISTORY","npm version provides **semver version management** with git integration."],["INSTALL","```pacman: sudo pacman -S npm```\n\n```apk: sudo apk add npm```\n\n```brew: brew install npm```"],["SEE ALSO","[npm](/man/npm)(1), [npm-publish](/man/npm-publish)(1), [semver](/man/semver)(1)"]],"e":["npm","npm-publish","semver"],"x":"npm version patch"},"nxc-smb":{"n":"nxc-smb","t":"performs SMB protocol operations for security testing","s":[["TAGLINE","performs SMB protocol operations for security testing"],["TLDR","**Spray credentials** against an entire subnet\n\n```nxc smb [192.168.1.0/24] -u [user] -p [password]```\n\n**Enumerate shares and access levels**\n\n```nxc smb [target] -u [user] -p [password] --shares```\n\n**Enumerate domain users** via SAMR\n\n```nxc smb [target] -u [user] -p [password] --users```\n\n**Authenticate against the local SAM** (not the domain)\n\n```nxc smb [target] -u [user] -p [password] --local-auth```\n\n**Execute a cmd.exe command**\n\n```nxc smb [target] -u [admin] -p [password] -x [whoami]```\n\n**Execute a PowerShell command**\n\n```nxc smb [target] -u [admin] -p [password] -X \"[Get-Process]\"```\n\n**Pass the hash** (NTLM)\n\n```nxc smb [target] -u [user] -H [NTLM_hash]```\n\n**Dump the SAM hashes**\n\n```nxc smb [target] -u [admin] -p [password] --sam```\n\n**Run a NetExec module**\n\n```nxc smb [target] -u [admin] -p [password] -M [spider_plus]```"],["SYNOPSIS","**nxc smb** _target_ [_options_]"],["PARAMETERS","**-u** _USER_\n> Username or username file (prefix with **@** to read from file).\n\n**-p** _PASS_\n> Password or password file.\n\n**-H** _HASH_\n> NTLM hash (LM:NT or just NT).\n\n**-d**, **--domain** _DOMAIN_\n> Authenticate against the given domain.\n\n**--local-auth**\n> Authenticate locally (use the machine's SAM, skip domain lookup).\n\n**-x** _CMD_\n> Execute _CMD_ via **cmd.exe**.\n\n**-X** _CMD_\n> Execute _CMD_ via **PowerShell**.\n\n**--exec-method** _METHOD_\n> Remote-execution technique: **wmiexec**, **atexec**, or **smbexec**.\n\n**--shares**\n> Enumerate SMB shares and the current user's permissions on each.\n\n**--users**\n> Enumerate domain users via SAMR.\n\n**--groups**\n> Enumerate local or domain groups.\n\n**--sessions**\n> List active SMB sessions on the target.\n\n**--loggedon-users**\n> List users currently logged on to the target.\n\n**--pass-pol**\n> Retrieve the effective password policy.\n\n**--sam**\n> Dump the local SAM hash database (requires admin).\n\n**--lsa**\n> Dump cached LSA secrets.\n\n**-M**, **--module** _NAME_\n> Run a NetExec module (see **nxc smb -L** for the full list)."],["DESCRIPTION","**nxc smb** performs SMB protocol operations for security testing. Part of **NetExec** (the community-maintained successor to **CrackMapExec**). It enumerates shares, users, sessions, groups, and password policies; validates credentials across entire subnets; and can execute commands remotely through WMI, scheduled tasks, or named pipes."],["CAVEATS","For authorized security assessments only. Requires proper authorization."],["SEE ALSO","[nxc](/man/nxc)(1), [smbclient](/man/smbclient)(1)"]],"e":["nxc","smbclient"],"x":"nxc smb [192.168.1.0/24] -u [user] -p [password]"},"pacconf":{"n":"pacconf","t":"queries and displays pacman's configuration as it would be parsed by pacman","s":[["TAGLINE","queries and displays pacman's configuration as it would be parsed by pacman"],["TLDR","Show **full parsed** pacman configuration\n\n```pacconf```\n\nList **configured repositories**\n\n```pacconf --repo-list```\n\nAlways show **directive names** even for single directives\n\n```pacconf --verbose [directive]```\n\nDisplay only **first value** of multi-value options\n\n```pacconf --single```\n\nDisplay **help**\n\n```pacconf --help```"],["SYNOPSIS","**pacconf** [_options_] [_directive_...]"],["PARAMETERS","**--config** _file_\n> Use alternate configuration file\n\n**--root** _path_\n> Use alternate installation root\n\n**--repo-list**\n> List configured repositories\n\n**--repo** _name_\n> Query options for specific repository\n\n**--verbose**\n> Always show directive names in output\n\n**--single**\n> Display only the first value of multi-value directives\n\n**--null**\n> Use null delimiter for output\n\n**--raw**\n> Display unprocessed directive values\n\n**--help**\n> Display help information\n\n**--version**\n> Display version information"],["DESCRIPTION","**pacconf** queries and displays pacman's configuration as it would be parsed by pacman itself. It can show the complete configuration or specific directive values, making it useful for scripts that need to read pacman settings.\n\nThe tool handles configuration include directives and processes values the same way pacman does, ensuring accurate representation of the active configuration. It can query global options or repository-specific settings."],["CAVEATS","Output represents the parsed configuration, which may differ from the raw config file due to includes and defaults. Repository-specific queries require the **--repo** option."],["HISTORY","Part of **pacutils**, a collection of helper utilities for pacman by Andrew Gregory. Provides programmatic access to pacman configuration, useful for system administration scripts and tools that interact with pacman."],["SEE ALSO","[pacman](/man/pacman)(8), [pacman.conf](/man/pacman.conf)(5), [paccheck](/man/paccheck)(8)"]],"e":["pacman","pacman.conf","paccheck"],"x":"pacconf"},"pam_filter":{"n":"pam_filter","t":"PAM filter module for terminal I/O","s":[["TAGLINE","PAM filter module for terminal I/O"],["TLDR","**Run a filter** on authentication I/O\n\n```auth required pam_filter.so run1 /path/to/filter```\n\n**Run a filter** on session open with debug output\n\n```session required pam_filter.so debug run1 /path/to/filter```\n\n**Run a filter** with a new pseudo-terminal allocated\n\n```session required pam_filter.so new_term run1 /path/to/filter```"],["SYNOPSIS","**pam_filter.so** [debug] [new_term] [non_term] run1|run2 _filter_ [_..._]"],["PARAMETERS","**debug**\n> Print debug information to syslog.\n\n**new_term**\n> Set PAM_TTY to the filtered pseudo-terminal. The default action is to set PAM_TTY to indicate the terminal the user is using to connect.\n\n**non_term**\n> Do not try to set the PAM_TTY item.\n\n**run1** _FILTER_\n> Run the filter at the first stage. For auth: pam_authenticate. For session: pam_open_session. For password: PAM_PRELIM_CHECK phase.\n\n**run2** _FILTER_\n> Run the filter at the second stage. For auth: pam_setcred. For session: pam_close_session. For password: PAM_UPDATE_AUTHTOK phase."],["DESCRIPTION","**pam_filter** is a PAM module intended to be a platform for providing access to all of the input/output that passes between the user and the application. It is only suitable for tty-based and stdin/stdout applications.\n\nThe filter is always called with the filter as argv[0], followed by the connection type and the PAM service type. All module types (auth, account, password, and session) are provided."],["CAVEATS","This is an experimental module. The filter must be an executable binary. Only suitable for tty-based and stdin/stdout applications. There are no known filters provided with the standard PAM distribution."],["SEE ALSO","[pam](/man/pam)(8), [pam_tty_audit](/man/pam_tty_audit)(8)"]],"e":["pam","pam_tty_audit"],"x":"auth required pam_filter.so run1 /path/to/filter"},"pbmtoppa":{"n":"pbmtoppa","t":"converts PBM bitmaps to HP PPA format for certain HP DeskJet printers","s":[["TAGLINE","converts PBM bitmaps to HP PPA format for certain HP DeskJet printers"],["TLDR","**Convert PBM to HP PPA format**\n\n```pbmtoppa [input.pbm] > [output.ppa]```\n\n**Specify gamma correction**\n\n```pbmtoppa -g [1.0] [input.pbm] > [output.ppa]```"],["SYNOPSIS","**pbmtoppa** [_options_] [_file_]"],["PARAMETERS","**-g** _value_\n> Gamma correction value.\n\n**-b** _n_\n> Blackness value."],["DESCRIPTION","**pbmtoppa** converts PBM bitmaps to HP PPA (Printing Performance Architecture) format for certain HP DeskJet printers. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pbmtolj](/man/pbmtolj)(1), [pbmtoepson](/man/pbmtoepson)(1)"]],"e":["pbmtolj","pbmtoepson"],"x":"pbmtoppa [input.pbm] > [output.ppa]"},"pct-set":{"n":"pct-set","t":"modifies the configuration of a Proxmox LXC container","s":[["TAGLINE","modifies the configuration of a Proxmox LXC container"],["TLDR","Enable **autostart** on boot\n\n```pct set 100 --onboot```\n\nSet **static IP**\n\n```pct set 100 --net0 name=eth0,bridge=vmbr0,ip=10.0.0.100/24,gw=10.0.0.1```\n\nSet **memory** and CPU limit\n\n```pct set 100 --memory 8192 --cpulimit 4```\n\n**Mount** host directory\n\n```pct set 100 --mp0 /host/path,mp=/guest/mount```\n\nSet **tags**\n\n```pct set 100 --tags tag1,tag2```\n\n**Delete** options\n\n```pct set 100 --delete net0,mp0```"],["SYNOPSIS","**pct set** _vmid_ [_OPTIONS_]"],["DESCRIPTION","**pct set** modifies the configuration of a Proxmox LXC container. Changes may require a container restart to take effect. This command can configure networking, resources, mount points, and other container properties."],["PARAMETERS","**vmid**\n> The numeric ID of the container\n\n**--onboot** _boolean_\n> Start container on host boot\n\n**--memory** _megabytes_\n> Memory limit in MB\n\n**--cpulimit** _number_\n> CPU time limit (e.g., 4 for 4 cores worth)\n\n**--net**_N_ _config_\n> Network interface configuration\n\n**--mp**_N_ _config_\n> Mount point configuration for bind mounts\n\n**--tags** _list_\n> Comma-separated list of tags\n\n**--delete** _list_\n> Remove specified options"],["CAVEATS","Some changes require stopping the container first. Network changes may disconnect running sessions. Mount points must exist on the host system."],["HISTORY","**pct set** is part of the **Proxmox VE** virtualization platform for managing LXC containers."],["SEE ALSO","[pct](/man/pct)(1), [pct-config](/man/pct-config)(1)"]],"e":["pct","pct-config"],"x":"pct set 100 --onboot"},"pct":{"n":"pct","t":"LXC container management tool for Proxmox VE","s":[["TAGLINE","LXC container management tool for Proxmox VE"],["TLDR","**List** containers\n\n```pct list```\n\n**Start/Stop** container\n\n```pct start 100```\n\n**Enter** container shell\n\n```pct enter 100```\n\n**Create** container\n\n```pct create 100 local:vztmpl/distro.tar.zst --rootfs local-lvm:4```\n\n**Resize** disk\n\n```pct resize 100 rootfs 20G```\n\n**Show** configuration\n\n```pct config 100```\n\n**Snapshot** container\n\n```pct snapshot 100 my-snapshot --description \"description\"```\n\n**Destroy** container\n\n```pct destroy 100 --purge```"],["SYNOPSIS","**pct** _command_ [_OPTIONS_] [_vmid_]"],["DESCRIPTION","**pct** is the LXC container management tool for Proxmox VE. It provides a complete command-line interface for creating, configuring, starting, stopping, and managing Linux containers."],["PARAMETERS","**list**\n> List all containers\n\n**create** _vmid_ _template_\n> Create a new container from template\n\n**config** _vmid_\n> Display container configuration\n\n**set** _vmid_\n> Modify container configuration\n\n**start** _vmid_\n> Start a container\n\n**stop** _vmid_\n> Stop a container\n\n**enter** _vmid_\n> Access container shell\n\n**exec** _vmid_ _command_\n> Execute command in container\n\n**snapshot** _vmid_ _name_\n> Create a snapshot\n\n**destroy** _vmid_\n> Remove a container\n\n**resize** _vmid_ _disk_ _size_\n> Resize container disk\n\n**--purge**\n> Remove all related resources when destroying"],["CAVEATS","Container IDs must be unique across the Proxmox cluster. Some operations require the container to be stopped. Nesting must be enabled for containers running Docker or LXC inside."],["HISTORY","**pct** is part of the **Proxmox VE** virtualization platform, providing lightweight containerization using LXC technology."],["SEE ALSO","[pct-create](/man/pct-create)(1), [pct-exec](/man/pct-exec)(1), [qm](/man/qm)(1)"]],"e":["pct-create","pct-exec","qm"],"x":"pct list"},"php-yii":{"n":"php-yii","t":"Yii framework console command runner","s":[["TAGLINE","Yii framework console command runner"],["TLDR","**List available commands**\n\n```php yii help```\n\n**Run database migrations**\n\n```php yii migrate```\n\n**Create a new migration**\n\n```php yii migrate/create [create_users_table]```\n\n**Rollback last migration**\n\n```php yii migrate/down```\n\n**Generate model from table**\n\n```php yii gii/model --tableName=[table] --modelClass=[ModelName]```\n\n**Generate CRUD**\n\n```php yii gii/crud --modelClass=[app\\\\models\\\\Post]```\n\n**Clear all caches**\n\n```php yii cache/flush-all```\n\n**Get help for a specific command**\n\n```php yii help [migrate]```"],["SYNOPSIS","**php** **yii** [_command_] [_--option=value_...]"],["SUBCOMMANDS","**help** [_command_]\n> Show available commands or help for a specific command.\n\n**migrate**\n> Run pending database migrations.\n\n**migrate/create** _name_\n> Create a new migration file.\n\n**migrate/down** [_count_]\n> Rollback migrations. Default: 1.\n\n**migrate/redo** [_count_]\n> Redo migrations (down then up).\n\n**gii/model**\n> Generate model class from database table.\n\n**gii/crud**\n> Generate CRUD controller and views.\n\n**gii/controller**\n> Generate controller class.\n\n**cache/flush-all**\n> Clear all application caches.\n\n**cache/flush** _component_\n> Flush a specific cache component.\n\n**asset/compress** _configFile_ _bundleFile_\n> Combine and compress JavaScript and CSS files.\n\n**serve**\n> Run PHP built-in web server."],["PARAMETERS","**--tableName** _name_\n> Database table name (gii/model).\n\n**--modelClass** _class_\n> Model class name (gii/model, gii/crud).\n\n**--interactive** _0|1_\n> Run in non-interactive mode. Default: 1.\n\n**--color** _0|1_\n> Enable/disable ANSI color output."],["DESCRIPTION","**yii** is the console entry point for Yii 2.0 PHP framework applications. It provides access to built-in commands for database migrations, code generation, caching, asset management, and application serving.\n\nConsole commands use the route format **controller/action** (e.g., migrate/create). Options are passed as **--option=value**. Custom console commands can be created by extending yii\\console\\Controller.\n\nThe **gii** commands provide code generation capabilities for models, controllers, CRUD interfaces, forms, modules, and extensions. The **migrate** commands manage database schema changes through versioned migration files."],["CAVEATS","Must be run from the project root directory where the yii bootstrap file exists. Requires a properly configured Yii 2.0 application. Some commands like gii require the yii2-gii extension. Database commands require configured database connection."],["HISTORY","The Yii console was introduced with **Yii 2.0**, released in **2014** by **Qiang Xue**. The console runner replaced the yiic tool from Yii 1.x with a more flexible command architecture."],["INSTALL","```dnf: sudo dnf install php```\n\n```pacman: sudo pacman -S php```\n\n```apk: sudo apk add php85```\n\n```brew: brew install php```\n\n```nix: nix profile install nixpkgs#php85```"],["SEE ALSO","[php](/man/php)(1), [composer](/man/composer)(1), [artisan](/man/artisan)(1), [symfony](/man/symfony)(1)"]],"e":["php","composer","artisan","symfony"],"x":"php yii help"},"pio-settings":{"n":"pio-settings","t":"manages PlatformIO configuration settings","s":[["TAGLINE","manages PlatformIO configuration settings"],["TLDR","**Show all settings**\n\n```pio settings get```\n\n**Get specific setting**\n\n```pio settings get [projects_dir]```\n\n**Set setting value**\n\n```pio settings set [projects_dir] [~/pio-projects]```\n\n**Reset to defaults**\n\n```pio settings reset```"],["SYNOPSIS","**pio settings** _command_ [_options_]"],["PARAMETERS","**get** [_name_]\n> Get setting value(s).\n\n**set** _name_ _value_\n> Set setting value.\n\n**reset**\n> Reset to defaults."],["DESCRIPTION","**pio settings** manages PlatformIO configuration settings. Controls default directories, auto-updates, telemetry, and other global preferences."],["INSTALL","```aur: yay -S pio```"],["SEE ALSO","[pio-system](/man/pio-system)(1), [pio-account](/man/pio-account)(1)"]],"e":["pio-system","pio-account"],"x":"pio settings get"},"pkgz":{"n":"pkgz","t":"Cross-distribution package manager wrapper","s":[["TAGLINE","Cross-distribution package manager wrapper"],["TLDR","**Search packages**\n\n```pkgz search [query]```\n\n**Install package**\n\n```pkgz install [package]```\n\n**List installed**\n\n```pkgz list```\n\n**Update packages**\n\n```pkgz update```"],["SYNOPSIS","**pkgz** [_command_] [_options_]"],["PARAMETERS","**search** _QUERY_\n> Search packages.\n\n**install** _PACKAGE_\n> Install package.\n\n**list**\n> List installed.\n\n**update**\n> Update packages.\n\n**--help**\n> Display help."],["DESCRIPTION","**pkgz** is a package manager wrapper that provides a unified command interface across Linux distributions. It translates common operations like install, search, and update into the appropriate commands for the underlying package manager (apt, dnf, pacman, zypper, etc.).\n\nThis allows users and scripts to use a single set of commands regardless of which distribution is running, simplifying cross-distribution workflows and documentation."],["CAVEATS","Wrapper tool. Underlying package manager required."],["HISTORY","pkgz provides **simplified package management** across distributions."],["SEE ALSO","[apt](/man/apt)(1), [yum](/man/yum)(1), [pacman](/man/pacman)(1)"]],"e":["apt","yum","pacman"],"x":"pkgz search [query]"},"pnmtopnm":{"n":"pnmtopnm","t":"Copy and normalize PNM image format","s":[["TAGLINE","Copy and normalize PNM image format"],["TLDR","**Copy a PNM image normalizing to raw format**\n\n```pnmtopnm [input.pnm] > [output.pnm]```\n\n**Convert a PNM image to plain (ASCII) format**\n\n```pnmtopnm -plain [input.pnm] > [output.pnm]```"],["SYNOPSIS","**pnmtopnm** [**-plain**] [_pnmfile_]"],["DESCRIPTION","**pnmtopnm** copies a PNM image to standard output. The output has the same PNM format (PBM, PGM, or PPM) and maxval as the input. Though similar to `cat`, it allows conversion between the plain (ASCII) and raw (binary) subformats of PNM. Use **-plain** to produce ASCII output; omit it to produce raw (binary) output.\n\nSince Netpbm 10.27, **pnmtopnm** is simply an alternate name for **pamtopnm**. Part of the Netpbm toolkit."],["HISTORY","**pnmtopnm** was introduced in Netpbm 10.23 (July 2004), obsoleting **pnmtoplainpnm**. In Netpbm 10.27 (March 2005) it became an alias for **pamtopnm**."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pamtopnm](/man/pamtopnm)(1), [pnmtoplainpnm](/man/pnmtoplainpnm)(1), [netpbm](/man/netpbm)(1)"]],"e":["pamtopnm","pnmtoplainpnm","netpbm"],"x":"pnmtopnm [input.pnm] > [output.pnm]"},"poetry-source":{"n":"poetry-source","t":"Manage package repository sources","s":[["TAGLINE","Manage package repository sources"],["TLDR","**Add package source**\n\n```poetry source add [name] [url]```\n\n**Remove package source**\n\n```poetry source remove [name]```\n\n**Show configured sources**\n\n```poetry source show```\n\n**Add private repository**\n\n```poetry source add --priority=supplemental [name] [url]```"],["SYNOPSIS","**poetry source** _command_ [_options_]"],["PARAMETERS","**add** _NAME_ _URL_\n> Add package source.\n\n**remove** _NAME_\n> Remove source.\n\n**show**\n> List sources.\n\n**--priority** _LEVEL_\n> Source priority (primary, supplemental, explicit)."],["DESCRIPTION","**poetry source** manages package repository sources in **pyproject.toml**. It allows adding private or alternative package indexes alongside or instead of the default PyPI.\n\nThe **--priority** option controls how sources are used: **primary** sources are checked first, **supplemental** sources are checked only for packages not found in primary sources, and **explicit** sources are only used when a package specifically references them. Use **show** to list configured sources."],["CAVEATS","Modifies pyproject.toml. Priority affects resolution order."],["HISTORY","poetry source provides **repository management** for package sources."],["INSTALL","```dnf: sudo dnf install poetry```\n\n```apk: sudo apk add poetry```\n\n```brew: brew install poetry```\n\n```nix: nix profile install nixpkgs#poetry```"],["SEE ALSO","[poetry](/man/poetry)(1), [poetry-config](/man/poetry-config)(1)"]],"e":["poetry","poetry-config"],"x":"poetry source add [name] [url]"},"ppmdist":{"n":"ppmdist","t":"Map PPM colors to evenly distributed grayscale levels","s":[["TAGLINE","Map PPM colors to evenly distributed grayscale levels"],["TLDR","Sort by **intensity** and map to grayscale\n\n```ppmdist -intensity [input.ppm] > [output.pgm]```\n\nSort by **frequency** and map to grayscale\n\n```ppmdist -frequency [input.ppm] > [output.pgm]```"],["SYNOPSIS","**ppmdist** [**-intensity** | **-frequency**] [_ppmfile_]"],["PARAMETERS","**-intensity**\n> Sort input colors by grayscale intensity before mapping to evenly distributed gray levels.\n\n**-frequency**\n> Sort input colors by number of occurrences before mapping to evenly distributed gray levels."],["DESCRIPTION","**ppmdist** reads a PPM image and produces a PGM output with evenly distributed gray levels. An input of n colors produces n gray levels, maximizing contrast in the output. The mapping order is controlled by the **-intensity** or **-frequency** options. Most useful for images with a small number of colors. Part of the Netpbm toolkit."],["CAVEATS","Only helpful for images with a very small number of colors. Reads from stdin if no file is specified."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[ppmcolormask](/man/ppmcolormask)(1)"]],"e":["ppmcolormask"],"x":"ppmdist -intensity [input.ppm] > [output.pgm]"},"ppmntsc":{"n":"ppmntsc","t":"Adjust colors to NTSC broadcast safe range","s":[["TAGLINE","Adjust colors to NTSC broadcast safe range"],["TLDR","**Convert to NTSC-safe colors**\n\n```ppmntsc [input.ppm] > [output.ppm]```\n\n**Use PAL limits** instead of NTSC\n\n```ppmntsc --pal [input.ppm] > [output.ppm]```\n\n**Show count of illegal pixels**\n\n```ppmntsc --verbose [input.ppm] > [output.ppm]```\n\n**Output only corrected pixels** (compliant pixels become black)\n\n```ppmntsc --correctedonly [input.ppm] > [output.ppm]```"],["SYNOPSIS","**ppmntsc** [**--pal**] [**--legalonly**] [**--illegalonly**] [**--correctedonly**] [**--verbose**] [**--debug**] [_infile_]"],["PARAMETERS","_infile_\n> Input PPM file. Reads from standard input if omitted.\n\n**--pal**\n> Use PAL broadcast color limits instead of the default NTSC.\n\n**--verbose**\n> Display a count of illegal pixels found.\n\n**--debug**\n> Display detailed output listing each illegal color and its corrected version.\n\n**--legalonly**\n> Output only compliant pixels; replace non-compliant ones with black.\n\n**--illegalonly**\n> Output only non-compliant pixels uncorrected; replace compliant ones with black.\n\n**--correctedonly**\n> Output only corrected versions of non-compliant pixels; replace compliant ones with black."],["DESCRIPTION","**ppmntsc** adjusts the colors in a PPM image to fall within the legal range for NTSC television broadcast. It tests each pixel and reduces saturation for those outside the legal range, while leaving compliant pixels unchanged. Colors that exceed the NTSC signal limits are clipped to the nearest legal value, preventing artifacts like blooming and color bleeding on broadcast displays.\n\nThe **--pal** option switches to PAL broadcast limits instead. The **--verbose** mode reports how many pixels were illegal, helping assess whether the source image is significantly out of gamut. The filtering options (**--legalonly**, **--illegalonly**, **--correctedonly**) allow isolating specific pixels for analysis. Part of the Netpbm toolkit."],["CAVEATS","For video production. Part of the Netpbm suite. The program processes multi-image PPM streams."],["HISTORY","ppmntsc was created by Wes Barris at the Minnesota Supercomputer Center as part of the **Netpbm** toolkit for NTSC color compliance."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[ppm](/man/ppm)(5), [ppmdim](/man/ppmdim)(1), [pambrighten](/man/pambrighten)(1), [pamdepth](/man/pamdepth)(1)"]],"e":["ppm","ppmdim","pambrighten","pamdepth"],"x":"ppmntsc [input.ppm] > [output.ppm]"},"qm-cleanup":{"n":"qm-cleanup","t":"Clean up Proxmox VM resources after shutdown","s":[["TAGLINE","Clean up Proxmox VM resources after shutdown"],["TLDR","**Clean up** VM resources\n\n```qm cleanup vmid clean-shutdown guest-requested```"],["SYNOPSIS","**qm cleanup** _vmid_ _clean-shutdown_ _guest-requested_"],["DESCRIPTION","**qm cleanup** cleans up resources associated with a QEMU/KVM virtual machine in Proxmox VE. It handles cleanup of tap devices, VGPUs, and other resources after a VM shuts down, crashes, or is stopped."],["PARAMETERS","**vmid**\n> The numeric ID of the virtual machine\n\n**clean-shutdown**\n> Whether this was a clean shutdown (0 or 1)\n\n**guest-requested**\n> Whether the guest initiated the shutdown (0 or 1)"],["CAVEATS","This command is typically called automatically by Proxmox VE after VM shutdown events. Manual invocation is rarely needed. It is part of the VM lifecycle management system."],["HISTORY","**qm cleanup** is part of the **Proxmox VE** virtualization platform, managing QEMU/KVM virtual machines and their associated resources."],["INSTALL","```dnf: sudo dnf install qm```"],["SEE ALSO","[qm](/man/qm)(1), [qm-start](/man/qm-start)(1), [qm-stop](/man/qm-stop)(1)"]],"e":["qm","qm-start","qm-stop"],"x":"qm cleanup vmid clean-shutdown guest-requested"},"qrcp":{"n":"qrcp","t":"Transfer files between devices via QR codes","s":[["TAGLINE","Transfer files between devices via QR codes"],["TLDR","**Send** files or directories\n\n```qrcp send [path/to/files...]```\n\n**Receive** files\n\n```qrcp receive```\n\nSend with **compression**\n\n```qrcp send --zip [path/to/directory]```\n\nUse specific **port**\n\n```qrcp send -p [8080]```\n\nUse specific **network interface**\n\n```qrcp send -i [eth0]```\n\n**Keep server** alive\n\n```qrcp send --keep-alive```\n\nReceive files to specific **directory**\n\n```qrcp receive --output [path/to/directory]```\n\nOpen QR code in **browser**\n\n```qrcp send --browser [path/to/file]```"],["SYNOPSIS","**qrcp** [**send**|**receive**|**config**] [**-p** _port_] [**-i** _interface_] [**--zip**] [**--keep-alive**] [_files_...]"],["PARAMETERS","**-p**, **--port** _port_\n> Use specific port number\n\n**-i**, **--interface** _iface_\n> Use specific network interface\n\n**-z**, **--zip**\n> Compress files before transfer\n\n**-k**, **--keep-alive**\n> Keep server running after transfer\n\n**-q**, **--quiet**\n> Only print errors\n\n**--browser**\n> Open QR code in default browser window\n\n**--output** _dir_\n> Directory to receive files into\n\n**--secure**\n> Use HTTPS connection\n\n**--path** _path_\n> URL path to use (default: random string)\n\n**-c**, **--config** _file_\n> Path to config file\n\n**-d**, **--fqdn** _domain_\n> Fully-qualified domain name for resulting URLs"],["DESCRIPTION","**qrcp** transfers files between devices using QR codes. It starts a temporary web server and displays a QR code that other devices can scan to download or upload files.\n\nThe tool is useful for quick file transfers when setting up network shares is impractical. It works across different operating systems as long as the devices are on the same network."],["CAVEATS","Devices must be on the same network. Transfer speed depends on network conditions. Large files may take time over WiFi. Firewall may need port access."],["HISTORY","**qrcp** was created by **Claudio d'Angelis** to simplify file transfers between devices. It eliminates the need for cables, email attachments, or cloud services for quick local transfers."],["INSTALL","```brew: brew install qrcp```\n\n```nix: nix profile install nixpkgs#qrcp```"],["SEE ALSO","[wormhole](/man/wormhole)(1), [croc](/man/croc)(1), [rsync](/man/rsync)(1)"]],"e":["wormhole","croc","rsync"],"x":"qrcp send [path/to/files...]"},"rabbitmq-plugins":{"n":"rabbitmq-plugins","t":"Manage RabbitMQ broker plugins","s":[["TAGLINE","Manage RabbitMQ broker plugins"],["TLDR","**List plugins**\n\n```rabbitmq-plugins list```\n\n**Enable plugin**\n\n```rabbitmq-plugins enable [rabbitmq_management]```\n\n**Disable plugin**\n\n```rabbitmq-plugins disable [rabbitmq_management]```\n\n**Enable multiple plugins**\n\n```rabbitmq-plugins enable [plugin1] [plugin2]```\n\n**Show enabled plugins**\n\n```rabbitmq-plugins list --enabled```"],["SYNOPSIS","**rabbitmq-plugins** [_options_] _command_ [_command_options_]"],["PARAMETERS","**-n** _node_\n> Target node.\n\n**--offline**\n> Modify without running node.\n\n**--online**\n> Require running node."],["COMMANDS","**list**\n> List all plugins.\n\n**enable** _plugins_\n> Enable plugins.\n\n**disable** _plugins_\n> Disable plugins.\n\n**set** _plugins_\n> Set exact plugin list."],["DESCRIPTION","**rabbitmq-plugins** manages RabbitMQ plugins. Plugins extend RabbitMQ functionality with features like management UI, MQTT support, and LDAP authentication."],["EXAMPLES","```bash"],["List all plugins","rabbitmq-plugins list"],["Enable management UI","rabbitmq-plugins enable rabbitmq_management"],["Enable MQTT","rabbitmq-plugins enable rabbitmq_mqtt"],["Disable plugin","rabbitmq-plugins disable rabbitmq_stomp"],["Offline enable","rabbitmq-plugins enable --offline rabbitmq_management\n```"],["COMMON PLUGINS","```\nrabbitmq_management      - Web UI and API\nrabbitmq_mqtt            - MQTT protocol\nrabbitmq_stomp           - STOMP protocol\nrabbitmq_shovel          - Message transfer\nrabbitmq_federation      - Federation\nrabbitmq_prometheus      - Metrics export\n```"],["CAVEATS","Some plugins require node restart. Dependencies enabled automatically. Management UI on port 15672."],["HISTORY","RabbitMQ plugins system was developed by **Pivotal** (now VMware) as part of RabbitMQ's extensible architecture."],["INSTALL","```dnf: sudo dnf install rabbitmq-server```\n\n```apk: sudo apk add rabbitmq-server```\n\n```zypper: sudo zypper install rabbitmq-server```\n\n```nix: nix profile install nixpkgs#rabbitmq-server```"],["SEE ALSO","[rabbitmqctl](/man/rabbitmqctl)(8), [rabbitmq-diagnostics](/man/rabbitmq-diagnostics)(8), [rabbitmq-server](/man/rabbitmq-server)(8)"]],"e":["rabbitmqctl","rabbitmq-diagnostics","rabbitmq-server"],"x":"rabbitmq-plugins list"},"sc_analysis_dump":{"n":"sc_analysis_dump","t":"Dump scamper warts analysis data","s":[["TAGLINE","Dump scamper warts analysis data"],["TLDR","**Dump scamper warts analysis data**\n\n```sc_analysis_dump [input.warts]```\n\n**Output specific format**\n\n```sc_analysis_dump -t [traceroute] [input.warts]```"],["SYNOPSIS","**sc_analysis_dump** [_options_] _file_"],["PARAMETERS","**-t** _type_\n> Analysis type (traceroute, ping, etc.).\n\n**-c**\n> CSV output format."],["DESCRIPTION","**sc_analysis_dump** extracts and displays analysis data from scamper warts files. Part of the scamper network measurement toolkit."],["INSTALL","```apt: sudo apt install scamper```\n\n```zypper: sudo zypper install scamper```\n\n```brew: brew install scamper```"],["SEE ALSO","[sc_wartsdump](/man/sc_wartsdump)(1), [scamper](/man/scamper)(1)"]],"e":["sc_wartsdump","scamper"],"x":"sc_analysis_dump [input.warts]"},"sensible-browser":{"n":"sensible-browser","t":"Open user's preferred web browser","s":[["TAGLINE","Open user's preferred web browser"],["TLDR","Open a **new window** of the default browser\n\n```sensible-browser```\n\nOpen a **URL** in the default browser\n\n```sensible-browser https://example.com```"],["SYNOPSIS","**sensible-browser** [_URL_]"],["DESCRIPTION","**sensible-browser** opens the user's preferred web browser. It checks environment variables and system defaults to determine which browser to launch.\n\nThe browser selection order checks BROWSER environment variable, then system alternatives."],["CAVEATS","Behavior depends on environment configuration. May not work in non-graphical environments without proper setup."],["HISTORY","Part of **sensible-utils** package, providing Debian policy-compliant default application launchers."],["SEE ALSO","[xdg-open](/man/xdg-open)(1), [update-alternatives](/man/update-alternatives)(8)"]],"e":["xdg-open","update-alternatives"],"x":"sensible-browser"},"so":{"n":"so","t":"Terminal interface for Stack Overflow","s":[["TAGLINE","Terminal interface for Stack Overflow"],["TLDR","**Search with default settings**\n\n```so [how do i reverse a list in python]```\n\n**Search a specific StackExchange site**\n\n```so --site [tex] [how to put tilde over character]```\n\n**Search multiple sites using Google**\n\n```so -e google -s [askubuntu] -s [unix] [how do i install linux]```"],["SYNOPSIS","**so** [_options_] _query_"],["PARAMETERS","**--site**, **-s** _SITE_\n> StackExchange site to search. Can be specified multiple times.\n\n**-e** _ENGINE_\n> Search backend: stackexchange, google, or duckduckgo."],["DESCRIPTION","**so** is a TUI that lets you search and browse answers across the entire StackExchange network directly from your terminal. It supports multiple search backends (StackExchange API, Google, DuckDuckGo), simultaneous multi-site queries, configurable themes, and system clipboard integration."],["HISTORY","**so** was created by **Sam Tay** and is written in **Rust**."],["INSTALL","```brew: brew install so```\n\n```nix: nix profile install nixpkgs#so```"]],"e":[],"x":"so [how do i reverse a list in python]"},"sockstat":{"n":"sockstat","t":"List open Internet and Unix domain sockets","s":[["TAGLINE","List open Internet and Unix domain sockets"],["TLDR","Show **all sockets** (IPv4 and IPv6, listening and connected)\n\n```sockstat```\n\nShow **IPv4 listening** sockets on specific ports\n\n```sockstat -4 -l -p [port1,port2]```\n\nShow **connected** and **Unix** sockets\n\n```sockstat -cu```\n\nFilter by **process** ID or name\n\n```sockstat -P [pid|process]```\n\nFilter by **user**\n\n```sockstat -U [uid|user]```\n\nFilter by **group**\n\n```sockstat -G [gid|group]```\n\nFilter by **protocol**\n\n```sockstat -R [tcp|udp|raw|unix]```"],["SYNOPSIS","**sockstat** [**-46clouh**] [**-p** _ports_] [**-P** _pid|process_] [**-U** _uid|user_] [**-G** _gid|group_] [**-R** _protocol_]"],["PARAMETERS","**-4**\n> Show only IPv4 sockets\n\n**-6**\n> Show only IPv6 sockets\n\n**-u**\n> Include Unix domain sockets\n\n**-c**\n> Show only connected sockets\n\n**-l**\n> Show only listening sockets\n\n**-o**\n> Apply filters with OR logic instead of AND\n\n**-p** _ports_\n> Filter by ports (comma-separated or ranges like 80-443)\n\n**-P** _pid|process_\n> Filter by process ID or process name\n\n**-U** _uid|user_\n> Filter by user ID or username\n\n**-G** _gid|group_\n> Filter by group ID or group name\n\n**-R** _protocol_\n> Filter by protocol (tcp, udp, raw, unix)\n\n**-h**\n> Display help"],["DESCRIPTION","**sockstat** lists open Internet and Unix domain sockets on the system. It displays information about which processes have which sockets open, useful for network troubleshooting and security auditing.\n\nOutput includes USER, COMMAND, PID, FD (file descriptor), PROTO (protocol), LOCAL ADDRESS, and FOREIGN ADDRESS for Internet sockets. Multiple filters can be combined to narrow results."],["CAVEATS","Requires appropriate permissions to see all sockets; root can see everything. On Linux, similar functionality is provided by **ss** or **netstat**. The sockstat command originated on BSD systems and may not be available on all Linux distributions."],["HISTORY","**sockstat** originated on **FreeBSD** and other BSD systems as a user-friendly alternative to parsing /proc or using netstat. Linux versions provide similar functionality. The tool has been part of BSD since the late **1990s** and was ported to Linux as part of various utility packages."],["INSTALL","```apt: sudo apt install sockstat```\n\n```nix: nix profile install nixpkgs#sockstat```"],["SEE ALSO","[ss](/man/ss)(8), [netstat](/man/netstat)(8), [lsof](/man/lsof)(8), [fuser](/man/fuser)(1)"]],"e":["ss","netstat","lsof","fuser"],"x":"sockstat"},"sops":{"n":"sops","t":"Encrypt secrets in structured config files","s":[["TAGLINE","Encrypt secrets in structured config files"],["TLDR","**Encrypt file**\n\n```sops -e [secrets.yaml] > [secrets.enc.yaml]```\n\n**Decrypt file**\n\n```sops -d [secrets.enc.yaml]```\n\n**Edit encrypted file**\n\n```sops [secrets.enc.yaml]```\n\n**Encrypt with specific key**\n\n```sops -e --age [age-public-key] [file.yaml]```\n\n**Rotate keys**\n\n```sops -r [secrets.enc.yaml]```\n\n**Add KMS key**\n\n```sops --add-kms [arn:aws:kms:...] [file.yaml]```"],["SYNOPSIS","**sops** [_options_] _file_"],["DESCRIPTION","**sops** (Secrets OPerationS) encrypts files while keeping their format intact. It encrypts values but leaves keys readable, making diffs and reviews practical.\n\nThe tool supports multiple key sources including AWS KMS, GCP KMS, Azure Key Vault, age, and PGP."],["PARAMETERS","**-e**, **--encrypt**\n> Encrypt file.\n\n**-d**, **--decrypt**\n> Decrypt file.\n\n**-r**, **--rotate**\n> Rotate data key.\n\n**-i**, **--in-place**\n> Modify file in place.\n\n**--age** _key_\n> Age public key.\n\n**--pgp** _key_\n> PGP fingerprint.\n\n**--kms** _arn_\n> AWS KMS key ARN.\n\n**--gcp-kms** _resource_\n> GCP KMS key.\n\n**--azure-kv** _url_\n> Azure Key Vault key.\n\n**--input-type** _type_\n> Input format.\n\n**--output-type** _type_\n> Output format."],["CONFIGURATION","**.sops.yaml**\n> Project-level configuration defining default encryption keys and rules for matching files to specific key sets.\n\n**SOPS_AGE_KEY_FILE**\n> Path to the age private key file for decryption (default: ~/.config/sops/age/keys.txt).\n\n**SOPS_AGE_KEY**\n> Age private key provided directly as an environment variable."],["CAVEATS","Requires key access for decryption. Multiple keys recommended for redundancy. Key rotation should be periodic. Some formats have limitations."],["HISTORY","**sops** was created by **Mozilla** for managing secrets in configuration files. It emerged from needs at Mozilla and has been adopted widely for secrets management in GitOps workflows."],["INSTALL","```pacman: sudo pacman -S sops```\n\n```apk: sudo apk add sops```\n\n```zypper: sudo zypper install sops```\n\n```brew: brew install sops```\n\n```nix: nix profile install nixpkgs#sops```"],["SEE ALSO","[age](/man/age)(1), [gpg](/man/gpg)(1), [vault](/man/vault)(1)"]],"e":["age","gpg","vault"],"x":"sops -e [secrets.yaml] > [secrets.enc.yaml]"},"subl":{"n":"subl","t":"Sublime Text command-line launcher","s":[["TAGLINE","Sublime Text command-line launcher"],["TLDR","**Open a file** in Sublime Text\n\n```subl [path/to/file]```\n\n**Open a file at a specific line** and column\n\n```subl [path/to/file]:[line]:[column]```\n\n**Open a directory** as a project\n\n```subl [path/to/directory]```\n\n**Open files in a new window**\n\n```subl -n [file1] [file2]```\n\n**Add folders to the current window** instead of opening a new one\n\n```subl -a [path/to/directory]```\n\n**Wait for files to close** before returning (useful for git)\n\n```subl -w [path/to/file]```\n\n**Open from stdin**\n\n```echo \"[text]\" | subl -```"],["SYNOPSIS","**subl** [_options_] [_files_] [_directories_]"],["PARAMETERS","**-n**, **--new-window**\n> Open a new window instead of reusing an existing instance.\n\n**-a**, **--add**\n> Add folders to the current window instead of opening a new window.\n\n**-w**, **--wait**\n> Wait for the files to be closed before returning. Useful for version control editors.\n\n**-b**, **--background**\n> Open files without activating the application window.\n\n**--project** _project_\n> Load a specific .sublime-project or .sublime-workspace file.\n\n**--command** _command_\n> Execute a Sublime Text internal command (e.g., **save_all**, **build**).\n\n**--safe-mode**\n> Launch in a sandboxed clean environment without plugins.\n\n**--launch-or-new-window**\n> Only open a new window if the application is already running.\n\n**-v**, **--version**\n> Print version information and exit.\n\n**-h**, **--help**\n> Display help message and exit."],["DESCRIPTION","**subl** is the command-line interface for Sublime Text, a popular cross-platform text editor. It allows opening files and directories from the terminal and integrating with Unix tools like git and subversion.\n\nFiles can include a **:line** or **:line:column** suffix to open at a specific location (1-based). The tool can read from stdin using **-**. When used with **-w**, it blocks until files are closed, making it suitable as an external editor for version control systems (e.g., `EDITOR='subl -w' git commit`)."],["CAVEATS","The **subl** command requires Sublime Text to be installed. On manual installations, you may need to create a symlink: **ln -s /opt/sublime_text/sublime_text /usr/local/bin/subl**. Package manager installations typically create this symlink automatically in /usr/bin/."],["HISTORY","Sublime Text was created by **Jon Skinner** and first released in **January 2008**. The command-line helper **subl** has been part of Sublime Text since version 2, enabling terminal-based workflows. Sublime Text 3 was released in **2013**, and Sublime Text 4 followed in **2021** with GPU rendering and improved performance."],["SEE ALSO","[code](/man/code)(1), [vim](/man/vim)(1), [nano](/man/nano)(1), [emacs](/man/emacs)(1), [atom](/man/atom)(1)"]],"e":["code","vim","nano","emacs","atom"],"x":"subl [path/to/file]"},"tlmgr-search":{"n":"tlmgr-search","t":"Search TeX Live packages","s":[["TAGLINE","Search TeX Live packages"],["TLDR","**Search packages**\n\n```tlmgr search [keyword]```\n\n**Search in descriptions**\n\n```tlmgr search --global [keyword]```\n\n**Search files**\n\n```tlmgr search --file [filename]```"],["SYNOPSIS","**tlmgr search** [_options_] _pattern_"],["PARAMETERS","**--global**\n> Search descriptions and names.\n\n**--file**\n> Search for files.\n\n**--word**\n> Match whole words only.\n\n**--all**\n> Search all packages, not just installed."],["DESCRIPTION","**tlmgr search** finds TeX Live packages by name, description, or contained files. Part of TeX Live Manager for discovering LaTeX packages."],["SEE ALSO","[tlmgr-info](/man/tlmgr-info)(1), [tlmgr-install](/man/tlmgr-install)(1)"]],"e":["tlmgr-info","tlmgr-install"],"x":"tlmgr search [keyword]"},"trashy":{"n":"trashy","t":"Rust-based command-line trash utility","s":[["TAGLINE","Rust-based command-line trash utility"],["TLDR","Move a specific **file** to the trash\n\n```trash [path/to/file]```\n\nMove **multiple files** to the trash\n\n```trash [path/to/file1] [path/to/file2]```\n\n**List** items in the trash\n\n```trash list```\n\n**Restore** a specific file from the trash\n\n```trash restore [file]```\n\n**Remove** a specific file from the trash\n\n```trash empty [file]```\n\nRestore **all files** from the trash\n\n```trash restore --all```\n\nRemove **all files** from the trash\n\n```trash empty --all```"],["SYNOPSIS","**trash** [_command_] [_options_] [_files_...]"],["PARAMETERS","**put** _files_\n> Move files to trash (default if no command given)\n\n**list**\n> List items in the trash\n\n**restore** _files_\n> Restore files from trash to original location\n\n**empty** _files_\n> Permanently delete files from trash\n\n**--all**\n> Apply to all items (with restore or empty)\n\n**-f**, **--force**\n> Skip confirmation prompts\n\n**-r**, **--recursive**\n> Include directories recursively\n\n**-v**, **--verbose**\n> Show verbose output\n\n**-h**, **--help**\n> Display help message"],["DESCRIPTION","**trashy** is a Rust-based command-line trash utility that provides a safe alternative to rm by moving files to the XDG Trash specification-compliant trash can. It offers a modern, fast implementation with intuitive subcommands.\n\nThe main command **trash** moves files to trash by default. Files can be listed, restored to their original location, or permanently deleted using the respective subcommands. It integrates with desktop trash locations."],["CAVEATS","The **trash** command name may conflict with trash-cli if both are installed. Uses the same trash directory as desktop file managers. Disk space is not freed until files are removed from trash with **trash empty**."],["HISTORY","Written in **Rust** by oberblastmeister as a modern alternative to trash-cli. Designed for speed and ease of use with a simplified command interface compared to trash-cli's separate commands."],["INSTALL","```nix: nix profile install nixpkgs#trashy```"],["SEE ALSO","[trash](/man/trash)(1), [rm](/man/rm)(1), [gio-trash](/man/gio-trash)(1)"]],"e":["trash","rm","gio-trash"],"x":"trash [path/to/file]"},"until":{"n":"until","t":"Shell loop executing until condition becomes true","s":[["TAGLINE","Shell loop executing until condition becomes true"],["TLDR","**Loop until condition**\n\n```until [condition]; do [commands]; done```\n\n**Wait for file**\n\n```until [ -f [file] ]; do sleep 1; done```\n\n**Wait for process**\n\n```until pgrep [process]; do sleep 1; done```\n\n**Counter loop**\n\n```until [ $i -gt 10 ]; do echo $i; i=$((i+1)); done```"],["SYNOPSIS","**until** _condition_; **do** _commands_; **done**"],["DESCRIPTION","**until** is a shell loop construct that repeatedly executes a block of commands as long as a condition evaluates to false (non-zero exit status). It is the logical opposite of **while**, which loops while a condition is true. The condition is tested before each iteration, so if it is already true when the loop starts, the body never executes.\n\nThe most common use of until is polling for a condition to become true, such as waiting for a network host to come online, a file to appear, or a process to start. Combined with **sleep**, it provides a simple way to implement retry logic in shell scripts.\n\nThe exit status of an until loop is the exit status of the last command executed in the body. If the body never executes because the condition is already true, the exit status is zero."],["CAVEATS","Shell builtin. Opposite of while. Condition evaluated each iteration."],["EXAMPLES","```\nuntil ping -c1 host &>/dev/null; do\n  echo \"Waiting...\"\n  sleep 5\ndone\necho \"Host is up\"\n```"],["HISTORY","**until** is a standard shell control structure available in POSIX shells, opposite of the while loop."],["SEE ALSO","[while](/man/while)(1), [for](/man/for)(1), [if](/man/if)(1), [sleep](/man/sleep)(1), [test](/man/test)(1)"]],"e":["while","for","if","sleep","test"],"x":"until [condition]; do [commands]; done"},"valgrind":{"n":"valgrind","t":"Memory debugging and profiling framework","s":[["TAGLINE","Memory debugging and profiling framework"],["TLDR","**Check for memory leaks**\n\n```valgrind --leak-check=full [./program]```\n\n**Run with detailed leak info**\n\n```valgrind --leak-check=full --show-leak-kinds=all [./program]```\n\n**Track memory origins**\n\n```valgrind --track-origins=yes [./program]```\n\n**Use cachegrind** for cache profiling\n\n```valgrind --tool=cachegrind [./program]```\n\n**Use callgrind** for call profiling\n\n```valgrind --tool=callgrind [./program]```\n\n**Use helgrind** for thread error detection\n\n```valgrind --tool=helgrind [./program]```\n\n**Generate suppressions** for known issues\n\n```valgrind --gen-suppressions=all [./program]```"],["SYNOPSIS","**valgrind** [**--tool=**_toolname_] [_options_] _program_ [_args_]"],["PARAMETERS","**--tool=**_name_\n> Select tool (memcheck, cachegrind, callgrind, helgrind, drd, massif)\n\n**--leak-check=**_level_\n> Check for memory leaks (no, summary, full)\n\n**--show-leak-kinds=**_kinds_\n> Which leaks to show (definite, indirect, possible, reachable, all)\n\n**--track-origins=**_yes|no_\n> Track origins of uninitialized values\n\n**--log-file=**_file_\n> Write output to file\n\n**--xml=**_yes_\n> Output in XML format\n\n**--gen-suppressions=**_level_\n> Generate suppression entries (no, yes, all)\n\n**--suppressions=**_file_\n> Use suppressions from file\n\n**-v**, **--verbose**\n> More verbose output\n\n**-q**, **--quiet**\n> Less verbose output\n\n**--num-callers=**_N_\n> Maximum stack depth for error reports (default: 12)\n\n**--vgdb=**_yes|no|full_\n> Enable gdb server for debugging under Valgrind"],["TOOLS","**memcheck**: Memory error detector (default)\n**cachegrind**: Cache and branch profiler\n**callgrind**: Call-graph profiler\n**helgrind**: Thread error detector\n**drd**: Thread error detector (different algorithm)\n**massif**: Heap profiler"],["DESCRIPTION","**valgrind** is an instrumentation framework for dynamic analysis tools. The default tool, memcheck, detects memory management problems: leaks, use of uninitialized memory, buffer overflows, and invalid frees.\n\nPrograms run significantly slower under Valgrind (10-50x) as every memory access is instrumented. This is normal and expected.\n\nOutput indicates error type, location (with line numbers if compiled with **-g**), and call stack. \"Definitely lost\" memory is a real leak; \"still reachable\" may be acceptable cleanup deferred to exit.\n\nOther tools profile performance (cachegrind, callgrind), detect threading issues (helgrind, drd), or analyze heap usage (massif)."],["CAVEATS","Compile programs with **-g** for line numbers and **-O0** or **-O1** for accurate debugging (high optimization confuses source mapping).\n\nFalse positives occur, especially with system libraries. Use suppression files to ignore known issues.\n\nValgrind doesn't work well with JIT compilers or custom memory allocators without additional configuration."],["HISTORY","**Valgrind** was created by **Julian Seward** and first released in **2002**. The name comes from the entrance to Valhalla in Norse mythology. It has become the standard memory debugging tool for C/C++ on Linux."],["INSTALL","```apt: sudo apt install valgrind```\n\n```dnf: sudo dnf install valgrind```\n\n```pacman: sudo pacman -S valgrind```\n\n```apk: sudo apk add valgrind```\n\n```zypper: sudo zypper install valgrind```\n\n```brew: brew install valgrind```\n\n```nix: nix profile install nixpkgs#valgrind```"],["SEE ALSO","[gdb](/man/gdb)(1), [strace](/man/strace)(1), [ltrace](/man/ltrace)(1), [asan](/man/asan)(7)"]],"e":["gdb","strace","ltrace","asan"],"x":"valgrind --leak-check=full [./program]"},"vigr":{"n":"vigr","t":"Edit the group or shadow-group file with locking","s":[["TAGLINE","Edit the group or shadow-group file with locking"],["TLDR","**Edit the /etc/group file**\n\n```sudo vigr```\n\n**Edit the /etc/gshadow (shadow group) file**\n\n```sudo vigr -s```\n\n**Edit group files in a chroot environment**\n\n```sudo vigr -R [/path/to/chroot]```"],["SYNOPSIS","**vigr** [_options_]"],["DESCRIPTION","**vigr** edits the `/etc/group` file (or `/etc/gshadow` with **-s**) while setting appropriate locks to prevent file corruption from concurrent access. It first locks the file, creates a temporary copy, opens it in the user's preferred editor (checking `$VISUAL`, then `$EDITOR`, falling back to `vi`), performs syntax checks after editing, and then atomically replaces the original file."],["PARAMETERS","**-g**, **--group**\n> Edit the group database (default).\n\n**-s**, **--shadow**\n> Edit the gshadow (shadow group) database.\n\n**-p**, **--passwd**\n> Edit the passwd database (equivalent to running vipw).\n\n**-q**, **--quiet**\n> Quiet mode.\n\n**-R**, **--root** _CHROOT_DIR_\n> Apply changes in a chroot environment.\n\n**-h**, **--help**\n> Display help message."],["CAVEATS","The **vigr** command should always be used instead of directly editing `/etc/group` to prevent file corruption from concurrent access. Changes take effect immediately. Requires root privileges. If you edit `/etc/group`, you should also check `/etc/gshadow` for consistency by running `vigr -s`."],["HISTORY","**vigr** is part of the **shadow-utils** package, providing safe editing of group files similar to how vipw edits password files."],["INSTALL","```apt: sudo apt install passwd```\n\n```pacman: sudo pacman -S shadow```\n\n```apk: sudo apk add shadow```\n\n```zypper: sudo zypper install shadow```\n\n```nix: nix profile install nixpkgs#shadow```"],["SEE ALSO","[vipw](/man/vipw)(8), [group](/man/group)(5), [gshadow](/man/gshadow)(5), [groupadd](/man/groupadd)(8), [groupmod](/man/groupmod)(8), [groupdel](/man/groupdel)(8)"]],"e":["vipw","group","gshadow","groupadd","groupmod","groupdel"],"x":"sudo vigr"},"vitree":{"n":"vitree","t":"Vim file browser that runs in a separate terminal","s":[["TAGLINE","Vim file browser that runs in a separate terminal"],["TLDR","**Start vitree** in the current directory (requires a Vim server running)\n\n```vitree```\n\n**Start Vim with a server name** so vitree can communicate with it\n\n```vim --servername vim```\n\n**Open vitree in a specific directory**\n\n```vitree [path/to/directory]```\n\n**Cycle filter modes** (default → changed only → show all) from within vitree\n\n```f```\n\n**Show the keybinding help** inside vitree\n\n```?```"],["SYNOPSIS","**vitree** [_path_]"],["DESCRIPTION","**vitree** is a terminal-based file browser designed to be used alongside **Vim**. Instead of running the file tree inside the editor (and forcing you to learn another set of bindings for resizing, splitting, and focus), it runs in a separate terminal window managed by your usual window manager or terminal multiplexer.\n\nIt connects to a running Vim instance through Vim's **--servername** mechanism, so selecting a file in vitree opens it in the connected Vim. The interface is an asynchronous TUI with automatic tree refresh, Vim-like navigation keybindings, mouse support, and ANSI color output.\n\nBy default, files matched by **.gitignore** are hidden and Git status markers are shown for tracked files. Pressing **f** toggles between filter modes (default → changed only → show all), and **?** displays the full keybinding reference."],["PARAMETERS","**_path_**\n> Directory to open. Defaults to the current working directory."],["CONFIGURATION","vitree expects a Vim instance started with a server name so it can send file-open commands to it:\n\n```vim --servername vim```\n\nOnce Vim is running, launching **vitree** in another terminal connects automatically. Git integration uses the repository found at or above the opened directory; **.gitignore** rules are honored for filtering."],["CAVEATS","Requires Vim built with **+clientserver** support and an active Vim server (start Vim with **--servername**). Without a running server, file selection in vitree has no editor to send to. The project is third-party software (not part of a standard Linux distribution) and is distributed via Homebrew, Go install, or prebuilt binaries from the GitHub releases page."],["HISTORY","**vitree** was written by **hoffa** and published on GitHub in **2024** under the MIT license. It is written in **Go** and intended as a lightweight alternative to in-editor file trees such as NERDTree or nvim-tree."],["SEE ALSO","[vim](/man/vim)(1), [nnn](/man/nnn)(1), [ranger](/man/ranger)(1), [lf](/man/lf)(1), [broot](/man/broot)(1)"]],"e":["vim","nnn","ranger","lf","broot"],"x":"vitree"},"websocat":{"n":"websocat","t":"WebSocket client and server for the terminal","s":[["TAGLINE","WebSocket client and server for the terminal"],["TLDR","**Connect to a WebSocket server**\n\n```websocat ws://[example.com]/socket```\n\n**Connect with secure WebSocket**\n\n```websocat wss://[example.com]/socket```\n\n**Create a WebSocket server**\n\n```websocat -s [8080]```\n\n**Pipe stdin to WebSocket**\n\n```echo \"hello\" | websocat ws://[example.com]/socket```\n\n**Connect with custom headers**\n\n```websocat -H \"Authorization: Bearer [token]\" wss://[example.com]/socket```\n\n**Binary mode** for raw data\n\n```websocat -b ws://[example.com]/socket```\n\n**One message per line** mode\n\n```websocat -n ws://[example.com]/socket```\n\n**Forward TCP to WebSocket**\n\n```websocat --binary tcp-l:127.0.0.1:[8080] ws://[example.com]/socket```"],["SYNOPSIS","**websocat** [_options_] _address1_ [_address2_]"],["PARAMETERS","**-s**, **--server**\n> Simple WebSocket server mode on specified port.\n\n**-b**, **--binary**\n> Send and receive binary frames instead of text.\n\n**-n**, **--no-close**\n> Don't send close frame, disconnect abruptly.\n\n**-1**, **--one-message**\n> Send one message and exit.\n\n**-u**, **--unidirectional**\n> Only copy from address1 to address2.\n\n**-U**, **--unidirectional-reverse**\n> Only copy from address2 to address1.\n\n**-H** _header_\n> Add custom HTTP header.\n\n**-E**, **--exit-on-eof**\n> Exit when stdin reaches EOF.\n\n**-t**, **--text**\n> Force text frame mode.\n\n**--ping-interval** _secs_\n> Send WebSocket pings at interval.\n\n**--ping-timeout** _secs_\n> Close connection if pong not received.\n\n**-k**, **--insecure**\n> Accept invalid TLS certificates.\n\n**--origin** _url_\n> Set Origin header.\n\n**-q**, **--quiet**\n> Suppress informational messages.\n\n**-v**, **--verbose**\n> Increase verbosity level."],["ADDRESS TYPES","**ws://** / **wss://**\n> WebSocket client connection.\n\n**tcp:** / **tcp-l:**\n> TCP client/listener.\n\n**unix:** / **unix-l:**\n> Unix socket client/listener.\n\n**exec:**\n> Execute program and connect to its stdio.\n\n**sh-c:**\n> Execute shell command.\n\n**stdio:**\n> Standard input/output.\n\n**mirror:**\n> Echo back received messages.\n\n**autoreconnect:**\n> Wrapper for automatic reconnection."],["DESCRIPTION","**websocat** is a command-line WebSocket client and server, similar to netcat but for WebSocket connections. It enables bidirectional communication between various endpoints including WebSocket, TCP, Unix sockets, and stdio.\n\nBasic usage connects stdin/stdout to a WebSocket URL. Input lines become WebSocket messages; received messages print to stdout. Use **-b** for binary data or **-n** for line-based text.\n\nThe server mode (**-s**) creates a simple WebSocket server useful for testing. For production, combine address specifiers like **tcp-l:** with **ws://** for protocol conversion.\n\nAddress chaining enables complex setups like WebSocket tunnels, TCP-over-WebSocket, and command execution. The **autoreconnect:** wrapper handles connection drops automatically.\n\nwebsocat supports both secure (wss://) and insecure (ws://) connections, custom headers for authentication, and ping/pong for connection keepalive."],["CAVEATS","Large messages may require increased buffer sizes. Some features require specific address type combinations. The --insecure flag should not be used in production. Complex address specifiers have steep learning curve."],["HISTORY","websocat was created by **Vitaly Shukela** (vi) and first released around **2017**. Written in Rust, it was designed as a Swiss Army knife for WebSocket operations. The tool has become popular for debugging WebSocket services, creating tunnels, and testing real-time applications. It continues active development with features for various networking scenarios."],["INSTALL","```pacman: sudo pacman -S websocat```\n\n```apk: sudo apk add websocat```\n\n```brew: brew install websocat```\n\n```nix: nix profile install nixpkgs#websocat```"],["SEE ALSO","[nc](/man/nc)(1), [curl](/man/curl)(1), [socat](/man/socat)(1)"]],"e":["nc","curl","socat"],"x":"websocat ws://[example.com]/socket"},"yuy2topam":{"n":"yuy2topam","t":"Convert YUY2 packed video to PAM","s":[["TAGLINE","Convert YUY2 packed video to PAM"],["TLDR","**Convert YUY2 video to PAM**\n\n```yuy2topam [width] [height] [input.yuy2] > [output.pam]```"],["SYNOPSIS","**yuy2topam** _width_ _height_ [_file_]"],["PARAMETERS","_width_\n> Image width.\n\n_height_\n> Image height."],["DESCRIPTION","**yuy2topam** converts YUY2 (YUYV) packed YUV format to PAM. YUY2 is a common video format used by webcams and video capture. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[yuvtoppm](/man/yuvtoppm)(1)"]],"e":["yuvtoppm"],"x":"yuy2topam [width] [height] [input.yuy2] > [output.pam]"}});
