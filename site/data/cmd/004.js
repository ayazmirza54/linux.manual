window.LCL_BUCKET(4,{"acm":{"n":"acm","t":"networked aerial combat flight simulator","s":[["TAGLINE","networked aerial combat flight simulator"],["TLDR","**Start** the aerial combat game\n\n```acm```\n\nConnect to a **server** for multiplayer\n\n```acm -server [hostname]```\n\nHost a **multiplayer game** as server\n\n```acm -host```\n\nSet **callsign** for multiplayer\n\n```acm -name [callsign]```"],["SYNOPSIS","**acm** [_-server host_] [_-host_] [_-name callsign_] [_options_]"],["DESCRIPTION","**acm** (Aerial Combat Multiplayer) is a flight combat simulator that allows networked dogfighting between players. The game simulates aerial combat with various aircraft types, featuring realistic flight physics and weapons systems.\n\nThe game can be played in single-player mode against AI opponents or in multiplayer mode across a network. Players control jet aircraft, engaging in air-to-air combat with guns and missiles."],["PARAMETERS","**-server** _host_\n> Connect to specified multiplayer server\n\n**-host**\n> Run as a multiplayer server\n\n**-name** _callsign_\n> Set your pilot callsign\n\n**-plane** _type_\n> Select aircraft type\n\n**-team** _n_\n> Join specified team number\n\n**-geometry** _WxH_\n> Set window geometry"],["CAVEATS","Requires X Window System for display. Network play requires proper firewall configuration for UDP traffic. Performance depends on graphics hardware and network latency for multiplayer games."],["HISTORY","**acm** was developed in the early **1990s** as one of the first networked multiplayer combat flight simulators for Unix/X11 systems. It demonstrated the potential of networked gaming on Unix workstations during an era when such games were primarily found on dedicated gaming platforms."],["SEE ALSO","[xpilot](/man/xpilot)(1)"]],"e":["xpilot"],"x":"acm"},"apache2":{"n":"apache2","t":"Apache HTTP Server daemon","s":[["TAGLINE","Apache HTTP Server daemon"],["TLDR","**Start** Apache server\n\n```sudo apache2```\n\nStart in **foreground** for debugging\n\n```sudo apache2 -X```\n\nTest **configuration syntax**\n\n```apache2 -t```\n\nShow **loaded modules**\n\n```apache2 -M```\n\nShow **version** and build options\n\n```apache2 -V```\n\nShow **parsed virtual host** settings\n\n```apache2 -S```\n\nStart with **specific config**\n\n```sudo apache2 -f [/path/to/httpd.conf]```"],["SYNOPSIS","**apache2** [_-d serverroot_] [_-f config_] [_-D parameter_] [_options_]"],["DESCRIPTION","**apache2** is the Apache HTTP Server daemon, one of the most widely used web servers. It handles HTTP requests, serves static content, runs CGI scripts, and integrates with application servers through various modules.\n\nOn Debian/Ubuntu systems, apache2 is typically managed through systemctl or apache2ctl rather than direct invocation."],["PARAMETERS","**-t**\n> Test configuration file syntax\n\n**-X**\n> Run in single-process debugging mode\n\n**-M**\n> List loaded modules (compiled-in and shared)\n\n**-V**\n> Show version and compile settings\n\n**-f** _file_\n> Specify configuration file\n\n**-d** _directory_\n> Server root directory\n\n**-D** _name_\n> Define parameter for <IfDefine>\n\n**-e** _level_\n> Startup error log level.\n\n**-k** _command_\n> Signal command: start, stop, restart, graceful, graceful-stop.\n\n**-S**\n> Show parsed virtual host settings.\n\n**-l**\n> List compiled-in modules.\n\n**-L**\n> List available configuration directives.\n\n**-C** _directive_\n> Process directive before reading config files.\n\n**-c** _directive_\n> Process directive after reading config files.\n\n**-h**\n> Display short summary of command-line options."],["CONFIGURATION","**/etc/apache2/apache2.conf**\n> Main configuration file on Debian/Ubuntu systems.\n\n**/etc/apache2/sites-available/**\n> Virtual host configuration files, enabled via a2ensite.\n\n**/etc/apache2/mods-available/**\n> Module configuration files, enabled via a2enmod.\n\n**/etc/apache2/envvars**\n> Environment variables for the Apache process (user, group, pid file).\n\n**/etc/apache2/ports.conf**\n> Listen directives specifying which ports Apache binds to."],["CAVEATS","Direct invocation is rarely needed; use apache2ctl or systemctl. Configuration testing (-t) should be run before restarts. Running as root is required for ports below 1024."],["HISTORY","Apache HTTP Server originated from NCSA HTTPd patches in **1995**, with \"Apache\" reportedly meaning \"A Patchy Server.\" It has dominated the web server market for decades."],["INSTALL","```apt: sudo apt install apache2-bin```\n\n```zypper: sudo zypper install apache2```\n\n```brew: brew install apache2```"],["SEE ALSO","[apache2ctl](/man/apache2ctl)(8), [apachectl](/man/apachectl)(8), [nginx](/man/nginx)(8)"]],"e":["apache2ctl","apachectl","nginx"],"x":"sudo apache2"},"apkeep":{"n":"apkeep","t":"Download APKs from app stores","s":[["TAGLINE","Download APKs from app stores"],["TLDR","**Download** an APK from the default source (APKPure)\n\n```apkeep -a [com.example.app] .```\n\nDownload a **specific version**\n\n```apkeep -a [com.example.app]@[version] .```\n\nDownload from **Google Play** using an account email and AAS token\n\n```apkeep -a [com.example.app] -d google-play -e [user@gmail.com] -t [aas_token] .```\n\nDownload from **F-Droid**\n\n```apkeep -a [com.example.app] -d f-droid .```\n\nList available **versions**\n\n```apkeep -a [com.example.app] -l```\n\nDownload many apps listed in a **CSV file**\n\n```apkeep -c [apps.csv] .```"],["SYNOPSIS","**apkeep** <**-a** _app_id[@version]_ | **-c** _csv_> [_options_] _OUTPATH_"],["DESCRIPTION","**apkeep** downloads APK files from several Android app sources. It can fetch current or specific historical versions of applications, useful for security research, app archival, or accessing apps without a device.\n\nBy default it downloads from APKPure, which requires no credentials. Downloading from Google Play requires an account email and an AAS token. Apps can be specified individually with **-a** or in bulk from a CSV file with **-c**."],["PARAMETERS","**-a, --app** _app_id[@version]_\n> App ID, optionally with @version (e.g. com.instagram.android).\n\n**-c, --csv** _file_\n> CSV file listing app IDs to download in bulk.\n\n**-f, --field** _n_\n> CSV field (column) containing app IDs. Default is **1**.\n\n**-v, --version-field** _n_\n> CSV field containing versions.\n\n**-d, --download-source** _source_\n> Where to download from: **apk-pure** (default), **google-play**, **f-droid**, **huawei-app-gallery**.\n\n**-l, --list-versions**\n> List the available versions of an app instead of downloading.\n\n**-e, --email** _email_\n> Google account email address (required for google-play).\n\n**-t, --aas-token** _token_\n> Google AAS token (required for google-play).\n\n**-o, --options** _opts_\n> Comma-separated extra options for the download source (e.g. arch=arm64-v8a).\n\n**-r, --parallel** _n_\n> Number of parallel APK fetches to run at a time. Default is **4**.\n\n**-s, --sleep-duration** _ms_\n> Sleep duration in milliseconds before each download request. Default is **0**."],["CAVEATS","Google Play downloads require a valid account email and AAS token; see the project documentation for how to obtain one. Servers may rate-limit or block excessive downloads, so use **-s** to throttle. Downloaded APKs remain subject to their original license."],["HISTORY","**apkeep** was developed by the EFF Threat Lab and first released in **2021**, written in Rust, providing command-line access to multiple Android app sources for archival and security research."],["INSTALL","```aur: yay -S apkeep```\n\n```brew: brew install apkeep```\n\n```nix: nix profile install nixpkgs#apkeep```"],["SEE ALSO","[adb](/man/adb)(1), [apktool](/man/apktool)(1), [aapt](/man/aapt)(1)"],["RESOURCES","```[Source code](https://github.com/EFForg/apkeep)```\n\n```[Homepage](https://www.eff.org/pages/apkeep)```"]],"e":["adb","apktool","aapt"],"x":"apkeep -a [com.example.app] ."},"apptainer-run":{"n":"apptainer-run","t":"Execute the default runscript of a container.","s":[["TAGLINE","Execute the default runscript of a container."],["TLDR","**Run the default action** of a container\n\n```apptainer run [container.sif]```\n\n**Run a container** with arguments passed to the runscript\n\n```apptainer run [container.sif] [arg1] [arg2]```\n\n**Run from Docker Hub** with bound directories\n\n```apptainer run --bind [/data]:[/mnt/data] docker://[python:3.11] [script.py]```\n\n**Run with NVIDIA GPU support**\n\n```apptainer run --nv [container.sif]```\n\n**Run with isolated environment**\n\n```apptainer run --containall [container.sif]```\n\n**Run with writable overlay**\n\n```apptainer run --overlay [overlay.img] [container.sif]```\n\n**Run as fakeroot** for root-like operations\n\n```apptainer run --fakeroot [container.sif]```\n\n**Run with custom environment variables**\n\n```apptainer run --env [KEY=value] [container.sif]```"],["SYNOPSIS","**apptainer run** [_options_] _container_ [_args_]"],["PARAMETERS","**--bind**, **-B** _src[:dest[:opts]]_\n> Bind mount a host path into the container.\n\n**--overlay**, **-o** _image_\n> Use an overlay image for a writable layer.\n\n**--writable**, **-w**\n> Make the container filesystem read-write (default is read-only).\n\n**--writable-tmpfs**\n> Add a writable tmpfs overlay; changes are discarded on exit.\n\n**--nv**\n> Enable NVIDIA GPU support.\n\n**--rocm**\n> Enable AMD ROCm GPU support.\n\n**--contain**, **-c**\n> Use minimal /dev and empty home/tmp directories.\n\n**--containall**, **-C**\n> Full containment of filesystems, PID, IPC, and environment.\n\n**--cleanenv**, **-e**\n> Clean environment before running.\n\n**--env** _VAR=value_\n> Set an environment variable.\n\n**--home**, **-H** _path_\n> Specify a custom home directory.\n\n**--pwd** _path_\n> Set working directory inside the container.\n\n**--fakeroot**, **-f**\n> Run with apparent root privileges without host root.\n\n**--net**, **-n**\n> Run in a new network namespace.\n\n**--app** _name_\n> Run a specific SCIF app within the container.\n\n**--no-home**\n> Do not bind the home directory."],["DESCRIPTION","**apptainer run** executes the default runscript of an Apptainer container. The runscript is defined in the container's **%runscript** section during build and typically contains the container's primary function or entrypoint.\n\nThis differs from **apptainer exec** which runs an arbitrary command. When you run a container, Apptainer invokes the runscript as if it were a regular executable, passing any additional command-line arguments to it.\n\nContainers can be specified as local SIF files, Docker references (docker://), library references (library://), or OCI archives. If the container has no runscript defined, the command will execute a shell.\n\nThe run command supports SCIF (Scientific Filesystem) apps via the **--app** flag, allowing containers to bundle multiple applications with separate entry points. Standard bind mounts, environment variables, and GPU support work identically to other Apptainer commands."],["CONFIGURATION","**/etc/apptainer/apptainer.conf**\n> Main configuration file controlling default bind paths, security options, GPU support, and namespace settings."],["CAVEATS","The container must have a runscript defined for predictable behavior. Arguments after the container path are passed to the runscript, not Apptainer. Use **--** to separate Apptainer options from runscript arguments if needed. GPU support requires appropriate drivers and container configuration."],["HISTORY","The run command has been central to Singularity/Apptainer since the project began at Lawrence Berkeley National Laboratory in **2015**. It was designed to make containers behave like executables, simplifying integration into HPC workflows. Apptainer inherited this functionality when the project transitioned from Singularity under the Linux Foundation in **2021**."],["INSTALL","```dnf: sudo dnf install apptainer```\n\n```pacman: sudo pacman -S apptainer```\n\n```apk: sudo apk add apptainer```\n\n```zypper: sudo zypper install apptainer```\n\n```brew: brew install apptainer```\n\n```nix: nix profile install nixpkgs#apptainer```"],["SEE ALSO","[apptainer](/man/apptainer)(1), [apptainer-exec](/man/apptainer-exec)(1), [apptainer-shell](/man/apptainer-shell)(1), [apptainer-run-help](/man/apptainer-run-help)(1), [apptainer-build](/man/apptainer-build)(1)"],["RESOURCES","```[Source code](https://github.com/apptainer/apptainer)```\n\n```[Homepage](https://apptainer.org)```\n\n```[Documentation](https://apptainer.org/docs/)```"]],"e":["apptainer","apptainer-exec","apptainer-shell","apptainer-run-help","apptainer-build"],"x":"apptainer run [container.sif]"},"apt-config":{"n":"apt-config","t":"Query and display APT configuration settings.","s":[["TAGLINE","Query and display APT configuration settings."],["TLDR","**Show** APT configuration\n\n```apt-config dump```\n\nShow **specific option**\n\n```apt-config dump | grep [Dir::Cache]```\n\nShow configuration in **shell format**\n\n```apt-config shell VAR [Dir::Cache]```"],["SYNOPSIS","**apt-config** [_-o option_] [_-c config_] _command_"],["DESCRIPTION","**apt-config** displays APT's configuration values. It shows the effective configuration from all sources including /etc/apt/apt.conf, /etc/apt/apt.conf.d/, and command-line options.\n\nThis is useful for debugging APT behavior, understanding which settings are active, and scripting with APT configuration values."],["PARAMETERS","**dump**\n> Show entire configuration\n\n**shell** _var_ _option_\n> Output shell-compatible variable assignment\n\n**-o** _option_\n> Set configuration option\n\n**-c** _file_\n> Read additional configuration file"],["CONFIGURATION","**/etc/apt/apt.conf**\n> Main APT configuration file.\n\n**/etc/apt/apt.conf.d/**\n> Directory for drop-in configuration fragments, processed in alphanumeric order."],["CAVEATS","Configuration is hierarchical; lower-level options override higher ones. Command-line options override file settings. Some options require specific APT versions."],["HISTORY","**apt-config** has been part of APT since early versions, providing programmatic access to APT's configuration system."],["INSTALL","```apt: sudo apt install apt```\n\n```dnf: sudo dnf install apt```\n\n```pacman: sudo pacman -S apt```\n\n```apk: sudo apk add apt```\n\n```brew: brew install apt```\n\n```nix: nix profile install nixpkgs#apt```"],["SEE ALSO","[apt](/man/apt)(8), [apt-get](/man/apt-get)(8)"]],"e":["apt","apt-get"],"x":"apt-config dump"},"avro-tools":{"n":"avro-tools","t":"Utilities for working with Apache Avro data files","s":[["TAGLINE","Utilities for working with Apache Avro data files"],["TLDR","**Convert JSON to Avro**\n\n```avro-tools fromjson --schema-file [schema.avsc] [input.json] > [output.avro]```\n\n**Convert Avro to JSON**\n\n```avro-tools tojson [input.avro] > [output.json]```\n\n**Display Avro file schema**\n\n```avro-tools getschema [file.avro]```\n\n**Display file metadata**\n\n```avro-tools getmeta [file.avro]```\n\n**Concatenate Avro files**\n\n```avro-tools concat [file1.avro] [file2.avro] [output.avro]```\n\n**Compile Avro schema to Java**\n\n```avro-tools compile schema [schema.avsc] [output_directory]```\n\n**Generate random data from schema**\n\n```avro-tools random --schema-file [schema.avsc] --count [100] [output.avro]```"],["SYNOPSIS","**avro-tools** _command_ [_options_] [_arguments_]"],["PARAMETERS","**fromjson**\n> Convert JSON to Avro format.\n\n**tojson**\n> Convert Avro to JSON format.\n\n**getschema**\n> Extract and print the schema from an Avro file.\n\n**getmeta**\n> Print metadata from an Avro file.\n\n**concat**\n> Concatenate multiple Avro files with compatible schemas.\n\n**compile**\n> Compile schema to Java source files.\n\n**idl**\n> Compile Avro IDL to schema.\n\n**random**\n> Generate random data based on a schema.\n\n**recodec**\n> Change the codec of an Avro file.\n\n**--schema-file** _file_\n> Path to Avro schema file (.avsc).\n\n**--count** _n_\n> Number of records to generate (for random).\n\n**--codec** _name_\n> Compression codec (null, deflate, snappy, bzip2, zstd, xz)."],["DESCRIPTION","**avro-tools** is a collection of utilities for working with Apache Avro data files. Avro is a data serialization system that provides rich data structures, a compact binary format, and schema evolution capabilities.\n\nThe tools enable conversion between Avro's binary format and human-readable JSON, schema extraction and validation, file manipulation, and code generation. Avro files are self-describing, containing their schema alongside the data, which avro-tools can extract and display.\n\nCommon workflows include inspecting Avro files produced by data pipelines, converting data between formats for debugging, generating test data, and compiling schemas to language-specific classes for application development."],["CAVEATS","JSON to Avro conversion requires a schema file; the JSON structure must match the schema. Large files may require increased JVM heap space (**-Xmx** option). Schema compatibility is enforced when concatenating files. The tool is distributed as a Java JAR file and requires a JRE."],["HISTORY","Apache Avro was created by **Doug Cutting** (also creator of Hadoop and Lucene) and first released in **2009**. It was developed to address limitations of existing serialization frameworks when working with big data systems like Hadoop. Avro became a top-level Apache project in **2010** and is widely used in data engineering, particularly with Kafka, Spark, and Hadoop ecosystems."],["INSTALL","```aur: yay -S avro-tools```\n\n```brew: brew install avro-tools```\n\n```nix: nix profile install nixpkgs#avro-tools```"],["SEE ALSO","[parquet-tools](/man/parquet-tools)(1)"]],"e":["parquet-tools"],"x":"avro-tools fromjson --schema-file [schema.avsc] [input.json] > [output.avro]"},"aws-backup":{"n":"aws-backup","t":"Centrally manage backups across AWS services","s":[["TAGLINE","Centrally manage backups across AWS services"],["TLDR","**Create** a backup vault\n\n```aws backup create-backup-vault --backup-vault-name [vault_name]```\n\n**Create** a backup plan\n\n```aws backup create-backup-plan --backup-plan [file://plan.json]```\n\n**Start** an on-demand backup job\n\n```aws backup start-backup-job --backup-vault-name [vault] --resource-arn [arn] --iam-role-arn [role_arn]```\n\n**List** all backup vaults\n\n```aws backup list-backup-vaults```\n\n**List** recovery points in a vault\n\n```aws backup list-recovery-points-by-backup-vault --backup-vault-name [vault]```\n\n**Start** a restore job from a recovery point\n\n```aws backup start-restore-job --recovery-point-arn [arn] --iam-role-arn [role_arn] --metadata file://[metadata.json]```\n\n**Describe** the status of a backup job\n\n```aws backup describe-backup-job --backup-job-id [job_id]```"],["SYNOPSIS","**aws backup** _command_ [_options_]"],["DESCRIPTION","**AWS Backup** is a unified, fully managed backup service designed to protect AWS resources and their associated data across multiple AWS services. It centralizes and automates backup operations, eliminating the need to create custom scripts and manual processes for each AWS service.\n\nThe service simplifies backup management by providing:\n\n> **Centralized Backup Management** - Create backup plans with retention policies and schedules that apply across AWS services including Amazon EBS, Amazon RDS, Amazon DynamoDB, Amazon EFS, Amazon FSx, AWS Storage Gateway, Amazon EC2, and more\n\n> **Policy-Based Backup Plans** - Define backup policies that specify frequency, retention, and lifecycle rules, automatically applying them to resources via tags or resource IDs\n\n> **Cross-Region and Cross-Account Backup** - Copy backups to different AWS regions or accounts for disaster recovery and compliance requirements\n\n> **Backup Vaults** - Organize and secure recovery points in encrypted vaults with access policies and resource-based permissions\n\n> **Compliance and Audit** - Monitor backup activity through AWS Backup Audit Manager frameworks, generate compliance reports, and track backup coverage across your organization\n\nThe service integrates with **AWS Organizations** for centralized backup policies, **AWS CloudTrail** for audit logging, and **Amazon EventBridge** for event-driven workflows. It supports **point-in-time recovery** for continuous backup services and **incremental backups** to minimize storage costs."],["AVAILABLE COMMANDS","**create-backup-vault**\n> Create a vault to store recovery points\n\n**delete-backup-vault**\n> Delete an empty backup vault\n\n**list-backup-vaults**\n> List all backup vaults in the account\n\n**create-backup-plan**\n> Create a backup plan with rules and schedules\n\n**update-backup-plan**\n> Modify an existing backup plan\n\n**delete-backup-plan**\n> Remove a backup plan\n\n**list-backup-plans**\n> List all backup plans\n\n**create-backup-selection**\n> Define which resources a backup plan applies to\n\n**start-backup-job**\n> Initiate an on-demand backup for a resource\n\n**stop-backup-job**\n> Cancel a running backup job\n\n**list-backup-jobs**\n> View backup job history and status\n\n**describe-backup-job**\n> Get the status and details of a backup job\n\n**list-protected-resources**\n> List resources that are backed up by AWS Backup\n\n**start-restore-job**\n> Restore a resource from a recovery point\n\n**list-restore-jobs**\n> View restore job history and status\n\n**list-recovery-points-by-backup-vault**\n> List all recovery points in a vault\n\n**delete-recovery-point**\n> Delete a specific recovery point\n\n**start-copy-job**\n> Copy a recovery point to another vault or region\n\n**create-framework**\n> Create compliance framework for backup auditing\n\n**create-report-plan**\n> Generate reports on backup activity and compliance\n\n**create-legal-hold**\n> Apply legal hold to prevent deletion of recovery points\n\n**put-backup-vault-access-policy**\n> Configure vault access permissions\n\n**tag-resource**\n> Add tags to backup resources for organization"],["CAVEATS","IAM roles with appropriate permissions are required for backup and restore operations. Some services require specific backup configurations or have limitations on restore options. Cross-region copy incurs data transfer costs. Deleting a vault requires it to be empty of all recovery points."],["HISTORY","**AWS Backup** was announced at **AWS re:Invent 2018** and launched in **January 2019** as a centralized backup service. Over time, it expanded support to include more AWS services and added features like cross-account backup, audit frameworks, and legal hold capabilities."],["INSTALL","```dnf: sudo dnf install aws```\n\n```pacman: sudo pacman -S aws-cli```\n\n```apk: sudo apk add aws-cli```\n\n```zypper: sudo zypper install aws-cli```"],["SEE ALSO","[aws-s3](/man/aws-s3)(1), [aws-rds](/man/aws-rds)(1), [aws-dynamodb](/man/aws-dynamodb)(1), [aws-efs](/man/aws-efs)(1), [aws](/man/aws)(1)"],["RESOURCES","```[Source code](https://github.com/aws/aws-cli)```\n\n```[Documentation](https://docs.aws.amazon.com/cli/latest/reference/backup/)```"]],"e":["aws-s3","aws-rds","aws-dynamodb","aws-efs","aws"],"x":"aws backup create-backup-vault --backup-vault-name [vault_name]"},"aws-cli":{"n":"aws-cli","t":"Unified command-line interface for Amazon Web Services.","s":[["TAGLINE","Unified command-line interface for Amazon Web Services."],["TLDR","**Configure AWS credentials**\n\n```aws configure```\n\n**List S3 buckets**\n\n```aws s3 ls```\n\n**Copy file to S3**\n\n```aws s3 cp [path/to/file] s3://[bucket]/[key]```\n\n**Sync directory to S3**\n\n```aws s3 sync [path/to/directory] s3://[bucket]/[prefix]```\n\n**List EC2 instances**\n\n```aws ec2 describe-instances```\n\n**Get caller identity (verify credentials)**\n\n```aws sts get-caller-identity```\n\n**Invoke a Lambda function**\n\n```aws lambda invoke --function-name [function_name] [output.json]```\n\n**Use a specific profile**\n\n```aws --profile [profile_name] s3 ls```"],["SYNOPSIS","**aws** [_options_] _service_ _command_ [_parameters_]"],["PARAMETERS","**configure**\n> Interactive setup for credentials and default region.\n\n**--profile** _name_\n> Use named profile from credentials file.\n\n**--region** _region_\n> Override the default region.\n\n**--output** _format_\n> Output format: json, yaml, text, table.\n\n**--query** _expression_\n> JMESPath query to filter output.\n\n**--no-cli-pager**\n> Disable paging of output.\n\n**--no-paginate**\n> Disable automatic pagination of multi-page results.\n\n**--endpoint-url** _url_\n> Override the default service endpoint URL.\n\n**--cli-binary-format** _format_\n> Format for binary input/output (base64 or raw-in-base64-out).\n\n**--debug**\n> Enable debug logging.\n\n**--dry-run**\n> Preview operation without executing (supported commands).\n\n**--cli-auto-prompt**\n> Enable interactive auto-prompt mode."],["COMMON SERVICES","**s3**\n> Simple Storage Service (object storage).\n\n**ec2**\n> Elastic Compute Cloud (virtual servers).\n\n**iam**\n> Identity and Access Management.\n\n**lambda**\n> Serverless compute functions.\n\n**sts**\n> Security Token Service.\n\n**cloudformation**\n> Infrastructure as code stacks.\n\n**rds**\n> Relational Database Service.\n\n**dynamodb**\n> NoSQL database service."],["DESCRIPTION","**aws** is the official command-line interface for Amazon Web Services, providing unified access to all AWS services. It enables managing cloud infrastructure, deploying applications, and automating operations from the terminal.\n\nThe CLI uses credentials stored in **~/.aws/credentials** and configuration in **~/.aws/config**. Multiple profiles support working with different AWS accounts or roles. Commands follow the pattern **aws service operation --parameters**.\n\nOutput can be formatted as JSON, YAML, text, or tables, and filtered using JMESPath queries with **--query**. The CLI supports shell completion and an interactive auto-prompt mode for discovering commands and parameters."],["CONFIGURATION","**~/.aws/credentials**\n> Stores access key ID and secret access key, organized by named profiles.\n\n**~/.aws/config**\n> Stores default region, output format, SSO settings, and role assumptions per profile."],["CAVEATS","Credentials stored in plain text; use IAM roles on EC2 or credential helpers for better security. API rate limits apply to most AWS operations. Some commands incur AWS charges. Region must be specified or defaulted for most services. MFA-protected operations require session tokens."],["HISTORY","The AWS CLI was first released in **2012** to replace the older service-specific command-line tools. Version 2, released in **2020**, added features like auto-prompt, SSO integration, and native installers. The CLI is maintained by AWS and updated frequently to support new services and features, making it the primary tool for AWS automation and administration."],["INSTALL","```dnf: sudo dnf install aws```\n\n```pacman: sudo pacman -S aws-cli```\n\n```apk: sudo apk add aws-cli```\n\n```zypper: sudo zypper install aws-cli```"],["SEE ALSO","[aws-vault](/man/aws-vault)(1), [terraform](/man/terraform)(1), [eksctl](/man/eksctl)(1), [sam](/man/sam)(1)"],["RESOURCES","```[Source code](https://github.com/aws/aws-cli)```\n\n```[Homepage](https://aws.amazon.com/cli/)```\n\n```[Documentation](https://docs.aws.amazon.com/cli/latest/reference/)```"]],"e":["aws-vault","terraform","eksctl","sam"],"x":"aws configure"},"aws-s3-sync":{"n":"aws-s3-sync","t":"Synchronize directories and S3 prefixes","s":[["TAGLINE","Synchronize directories and S3 prefixes"],["TLDR","**Sync a local directory** to an S3 bucket\n\n```aws s3 sync [./local-dir] s3://[bucket-name]/```\n\n**Download S3 bucket** to local directory\n\n```aws s3 sync s3://[bucket-name]/ [./local-dir]```\n\n**Sync between two S3 buckets**\n\n```aws s3 sync s3://[source-bucket] s3://[dest-bucket]```\n\n**Sync and delete** files not in source\n\n```aws s3 sync [./local-dir] s3://[bucket-name]/ --delete```\n\n**Sync excluding** certain file types\n\n```aws s3 sync [./local-dir] s3://[bucket-name]/ --exclude \"*.log\"```\n\n**Preview sync operation** without executing\n\n```aws s3 sync [./local-dir] s3://[bucket-name]/ --dryrun```\n\n**Sync only new files** without overwriting\n\n```aws s3 sync [./local-dir] s3://[bucket-name]/ --no-overwrite```"],["SYNOPSIS","**aws s3 sync** _source_ _destination_ [_options_]"],["DESCRIPTION","**aws s3 sync** synchronizes directories and S3 prefixes by recursively copying new and updated files from the source to the destination. It only transfers files that are new or have changed, making it efficient for incremental backups and deployments.\n\nBy default, sync compares file size and last modified time to determine which files need updating. It creates folders in the destination only if they contain files."],["PARAMETERS","**--delete**\n> Delete destination files that don't exist in the source\n\n**--dryrun**\n> Display operations that would be performed without executing them\n\n**--quiet**\n> Suppress all output except errors\n\n**--no-overwrite**\n> Only transfer files not present at the destination\n\n**--size-only**\n> Use file size alone to determine if sync is needed (ignore timestamps)\n\n**--exact-timestamps**\n> Match timestamps exactly when syncing from S3 to local\n\n**--include** _pattern_\n> Include files matching the pattern\n\n**--exclude** _pattern_\n> Exclude files matching the pattern from the sync\n\n**--acl** _value_\n> Set access control list: private, public-read, etc.\n\n**--storage-class** _value_\n> Specify storage class: STANDARD, STANDARD_IA, GLACIER, etc.\n\n**--source-region** _value_\n> Source bucket region (required for cross-region S3 to S3 sync)"],["CAVEATS","The **--delete** option removes files from the destination that don't exist in the source; use with caution. Pattern matching with **--include** and **--exclude** is order-dependent. When syncing to local, directories are not created unless they contain files. Sync does not preserve symbolic links."],["INSTALL","```dnf: sudo dnf install aws```\n\n```pacman: sudo pacman -S aws-cli```\n\n```apk: sudo apk add aws-cli```\n\n```zypper: sudo zypper install aws-cli```"],["SEE ALSO","[aws-s3](/man/aws-s3)(1), [aws-s3-cp](/man/aws-s3-cp)(1), [rsync](/man/rsync)(1)"],["RESOURCES","```[Source code](https://github.com/aws/aws-cli)```\n\n```[Homepage](https://aws.amazon.com/cli/)```\n\n```[Documentation](https://docs.aws.amazon.com/cli/latest/reference/s3/sync.html)```"]],"e":["aws-s3","aws-s3-cp","rsync"],"x":"aws s3 sync [./local-dir] s3://[bucket-name]/"},"badblocks":{"n":"badblocks","t":"Scan devices for bad disk blocks","s":[["TAGLINE","Scan devices for bad disk blocks"],["TLDR","**Check** device for bad blocks (read-only)\n\n```badblocks -v [/dev/sdb]```\n\n**Destructive** write test\n\n```badblocks -wsv [/dev/sdb]```\n\n**Non-destructive** read-write test\n\n```badblocks -nsv [/dev/sdb]```\n\nSave **bad block list**\n\n```badblocks -o [badblocks.txt] [/dev/sdb]```"],["SYNOPSIS","**badblocks** [_options_] _device_ [_last-block_] [_first-block_]"],["DESCRIPTION","**badblocks** searches a device for bad blocks by performing read, write, or non-destructive read-write tests. It's used to verify disk integrity and create lists of bad blocks for filesystem tools.\n\nThe tool is particularly useful for testing new drives or diagnosing failing storage devices."],["PARAMETERS","**-v**\n> Verbose mode\n\n**-w**\n> Destructive write test (erases data)\n\n**-n**\n> Non-destructive read-write test\n\n**-s**\n> Show progress\n\n**-o** _file_\n> Output bad blocks to file\n\n**-b** _size_\n> Block size in bytes (default: 1024)\n\n**-c** _blocks_\n> Number of blocks to test at once (default: 64)\n\n**-p** _passes_\n> Number of test passes (default: 0, meaning single pass)\n\n**-t** _pattern_\n> Test pattern (use `random` for random data, or a numeric value 0-255)\n\n**-i** _file_\n> Read existing bad blocks list from file (skip retesting known blocks)\n\n**-f**\n> Force testing on a mounted device (dangerous, normally prevented)"],["TEST MODES","**Read-only** (default)\n> Safe, detects existing bad blocks\n\n**Write** (-w)\n> Destructive, thorough testing, erases all data\n\n**Non-destructive** (-n)\n> Safe, reads then writes back original data"],["WORKFLOW","```bash"],["Check for bad blocks (safe)","sudo badblocks -sv /dev/sdb"],["Create bad block list for e2fsck","sudo badblocks -o badblocks.txt /dev/sdb\nsudo e2fsck -l badblocks.txt /dev/sdb"],["Thorough destructive test (WARNING: erases data)","sudo badblocks -wsv /dev/sdb\n```"],["CAVEATS","Write modes DESTROY all data on the device. Requires root privileges. Very slow on large drives (hours or days). Modern drives remap bad sectors automatically. Non-destructive mode still carries small risk of data loss. SMR drives may show poor performance during testing."],["HISTORY","**badblocks** has been part of e2fsprogs since the early **1990s**, created as a companion tool for ext2 filesystem management."],["INSTALL","```apt: sudo apt install e2fsprogs```\n\n```dnf: sudo dnf install e2fsprogs```\n\n```pacman: sudo pacman -S e2fsprogs```\n\n```apk: sudo apk add e2fsprogs-extra```\n\n```zypper: sudo zypper install e2fsprogs```\n\n```brew: brew install e2fsprogs```\n\n```nix: nix profile install nixpkgs#e2fsprogs```"],["SEE ALSO","[e2fsck](/man/e2fsck)(8), [fsck](/man/fsck)(8), [smartctl](/man/smartctl)(8), [dd](/man/dd)(1), [hdparm](/man/hdparm)(8)"]],"e":["e2fsck","fsck","smartctl","dd","hdparm"],"x":"badblocks -v [/dev/sdb]"},"burn":{"n":"burn","t":"Console front-end for burning Data, Audio, ISO and copy CDs","s":[["TAGLINE","Console front-end for burning Data, Audio, ISO and copy CDs"],["TLDR","**Burn a data CD** from a directory tree\n\n```burn -D -p [path/to/dir]```\n\n**Burn an ISO image** to disc\n\n```burn -I -n [image.iso]```\n\n**Create an audio CD** from MP3, Ogg, or WAV files\n\n```burn -A -a [track1.mp3] -a [track2.wav]```\n\n**Copy a CD** on the fly\n\n```burn -C```\n\n**Simulate** a burn without actually writing\n\n```burn -D -s -p [path/to/dir]```\n\n**Burn a multisession data CD**\n\n```burn -D -m -p [path/to/dir]```"],["SYNOPSIS","**burn** _MAIN_MODE_ [_options_]... [_file/s_]..."],["PARAMETERS","**-D**, **--data-cd**\n> Create a Data-CD from files and directories.\n\n**-I**, **--iso-cd**\n> Burn an existing ISO image to disc.\n\n**-C**, **--copy-cd**\n> Copy a CD, optionally on the fly.\n\n**-A**, **--audio-cd**\n> Create an audio CD from MP3, Ogg, or WAV input files.\n\n**-s**, **--simulate**\n> Perform a burn simulation (no laser write); useful for testing media and pipeline.\n\n**-p**, **--path** _path_\n> Add the contents of _path_ to the CD root without wrapping the directory.\n\n**-r**, **--preserve-path** _path_\n> Add files while preserving the original directory structure.\n\n**-x**, **--exclude-path** _pattern_\n> Exclude files or directories matching _pattern_.\n\n**-c**, **--change-path** _from_=_to_\n> Rename a path on the CD during burning.\n\n**-l**, **--follow-symlink**\n> Follow symbolic-link directories instead of recording the link.\n\n**-m**, **--multisession**\n> Mark the session as appendable so further data can be added later.\n\n**-n**, **--name** _file_\n> Name of the ISO image to burn (with **-I**).\n\n**-a**, **--audio-file** _file_\n> Audio track to add (with **-A**); repeat for additional tracks.\n\n**--help**\n> Print built-in help and exit."],["DESCRIPTION","**burn** is a console-driven wrapper around the standard CD-recording tool-chain (**cdrecord/wodim**, **mkisofs/genisoimage**, decoders for MP3 and Ogg) that exposes the common burning workflows through a single command. One of four mutually exclusive main modes selects whether to write data, an ISO image, a copy of another disc, or an audio CD assembled from compressed and uncompressed sources.\n\nThe program decodes audio files transparently before writing, generates the necessary ISO9660 filesystem for data discs, and forwards the resulting track to the underlying burner. Burn parameters and recorder device are read from the user and system configuration; command-line options override them per invocation."],["CONFIGURATION","**/etc/burn.conf**\n> System-wide configuration: device path, speed, default file system options.\n\n**~/.burn/burn.conf**\n> Per-user override of the system configuration."],["CAVEATS","Requires a working **cdrecord** or **wodim** plus **mkisofs**/**genisoimage** in **PATH**. Audio mode depends on external decoders (**lame**, **oggdec**) being installed for non-WAV input. Optical media is largely obsolete; consider USB images and **dd**, **xorriso**, or **growisofs** for modern workflows."],["HISTORY","**burn** was written by **Ben Finney** and **Gaetano Paolone** as a friendly Bash wrapper over the cdrtools suite, packaged in **Debian** since the early **2000s**. It is distributed under the **GNU GPL v2** or later."],["SEE ALSO","[wodim](/man/wodim)(1), [cdrecord](/man/cdrecord)(1), [growisofs](/man/growisofs)(1), [genisoimage](/man/genisoimage)(1)"]],"e":["wodim","cdrecord","growisofs","genisoimage"],"x":"burn -D -p [path/to/dir]"},"cabextract":{"n":"cabextract","t":"Extract Microsoft cabinet archives","s":[["TAGLINE","Extract Microsoft cabinet archives"],["TLDR","**Extract cabinet file**\n\n```cabextract [file.cab]```\n\n**List contents without extracting**\n\n```cabextract -l [file.cab]```\n\n**Extract to specific directory**\n\n```cabextract -d [output_dir] [file.cab]```\n\n**Test archive integrity**\n\n```cabextract -t [file.cab]```\n\n**Extract with filter pattern**\n\n```cabextract -F \"*.dll\" [file.cab]```\n\n**Extract with lowercase filenames**\n\n```cabextract -L [file.cab]```\n\n**Extract from embedded cabinet in exe**\n\n```cabextract [setup.exe]```"],["SYNOPSIS","**cabextract** [_options_] _cabinet_file_..."],["DESCRIPTION","**cabextract** extracts files from Microsoft cabinet (.cab) archives. It can also extract cabinets embedded in other files like executables. Supports all Microsoft cabinet formats and Windows CE installation files."],["PARAMETERS","**-l**, **--list**\n> List contents without extracting\n\n**-t**, **--test**\n> Test integrity, show MD5 checksums\n\n**-d** _directory_, **--directory**=_directory_\n> Extract to specified directory\n\n**-F** _pattern_, **--filter**=_pattern_\n> Extract only files matching shell pattern\n\n**-f**, **--fix**\n> Attempt to salvage corrupted archives\n\n**-L**, **--lowercase**\n> Convert filenames to lowercase\n\n**-p**, **--pipe**\n> Pipe extracted files to stdout\n\n**-s**, **--single**\n> Don't follow multi-part cabinet links\n\n**-q**, **--quiet**\n> Suppress normal output\n\n**-v**, **--version**\n> Show version"],["MULTI-PART CABINETS","For multi-part cabinet archives, only specify the first file. cabextract automatically locates and processes remaining parts."],["CAVEATS","Can only extract archives, not create them. Use **gcab** to create cabinet files. Preserves internal directory structure, permissions, and dates."],["INSTALL","```apt: sudo apt install cabextract```\n\n```dnf: sudo dnf install cabextract```\n\n```pacman: sudo pacman -S cabextract```\n\n```apk: sudo apk add cabextract```\n\n```zypper: sudo zypper install cabextract```\n\n```brew: brew install cabextract```\n\n```nix: nix profile install nixpkgs#cabextract```"],["SEE ALSO","[unzip](/man/unzip)(1), [7z](/man/7z)(1)"]],"e":["unzip","7z"],"x":"cabextract [file.cab]"},"chkrootkit":{"n":"chkrootkit","t":"local rootkit detection scanner","s":[["TAGLINE","local rootkit detection scanner"],["TLDR","**Scan system for rootkits**\n\n```sudo chkrootkit```\n\n**Quiet mode (show infections only)**\n\n```sudo chkrootkit -q```\n\n**Expert mode with more details**\n\n```sudo chkrootkit -x```\n\n**Use alternate root directory**\n\n```sudo chkrootkit -r [/mnt/system]```\n\n**Test specific check**\n\n```sudo chkrootkit [chkwtmp]```\n\n**List available tests**\n\n```chkrootkit -l```"],["SYNOPSIS","**chkrootkit** [_options_] [_test_...]"],["DESCRIPTION","**chkrootkit** locally checks for signs of rootkits on a system. It examines system binaries for known modifications, checks for deleted log entries, detects loadable kernel module (LKM) trojans, and identifies promiscuous network interfaces.\n\nThe tool works by comparing system binaries against known signatures of rootkit modifications and by running a series of tests that look for common rootkit behaviors. It can detect over 70 known rootkits and worms. For best results, it should be run from trusted binaries on a clean system or live CD, since a compromised system's tools may hide infections."],["PARAMETERS","**-q**\n> Quiet mode, show infections only\n\n**-x**\n> Expert mode: outputs raw strings from analyzed binary files for manual inspection\n\n**-e**\n> Exclude known false positive files/directories\n\n**-r** _dir_\n> Use alternate root directory\n\n**-p** _dir1:dir2_\n> Custom path for binaries\n\n**-l**\n> List available tests\n\n**-n**\n> Skip NFS mounted directories"],["TESTS","**chkwtmp**\n> Check wtmp deletions\n\n**chklastlog**\n> Check lastlog deletions\n\n**ifpromisc**\n> Check for promiscuous interfaces\n\n**chkproc**\n> Check for LKM trojans\n\n**strings**\n> Quick strings check"],["CAVEATS","Not foolproof - advanced rootkits can hide. Use with rkhunter for comprehensive scanning. Set up cron jobs for regular scans. If infection found, isolate system and investigate."],["INSTALL","```apt: sudo apt install chkrootkit```\n\n```dnf: sudo dnf install chkrootkit```\n\n```brew: brew install chkrootkit```"],["SEE ALSO","[rkhunter](/man/rkhunter)(1), [lynis](/man/lynis)(1), [clamav](/man/clamav)(1)"]],"e":["rkhunter","lynis","clamav"],"x":"sudo chkrootkit"},"chpasswd":{"n":"chpasswd","t":"batch update user passwords","s":[["TAGLINE","batch update user passwords"],["TLDR","Change the password for a **specific user**\n\n```printf \"[username]:[new_password]\" | sudo chpasswd```\n\nChange passwords for **multiple users**\n\n```printf \"[username_1]:[password_1]\\n[username_2]:[password_2]\" | sudo chpasswd```\n\nChange password using an **encrypted form**\n\n```printf \"[username]:[encrypted_password]\" | sudo chpasswd -e```\n\nChange password with **specific encryption method**\n\n```printf \"[username]:[new_password]\" | sudo chpasswd -c [SHA512]```"],["SYNOPSIS","**chpasswd** [_options_]"],["DESCRIPTION","**chpasswd** reads username:password pairs from standard input and updates user passwords in batch. This is useful for system administration tasks requiring password changes for multiple users at once, such as during initial account provisioning or automated password rotation.\n\nInput format is one user per line as **username:password** with no spaces around the colon. Passwords are encrypted using the system default method (typically SHA-512) unless a different method is specified. Pre-encrypted passwords can also be supplied with the **-e** flag."],["PARAMETERS","**-e, --encrypted**\n> Passwords are supplied in encrypted form\n\n**-c, --crypt-method** _method_\n> Specify encryption method (NONE, DES, MD5, SHA256, SHA512)\n\n**-m, --md5**\n> Use MD5 encryption\n\n**-s, --sha-rounds** _rounds_\n> Number of rounds for SHA encryption"],["CONFIGURATION","**/etc/login.defs**\n> Controls default encryption method (ENCRYPT_METHOD), SHA rounds (SHA_CRYPT_MIN_ROUNDS, SHA_CRYPT_MAX_ROUNDS), and other password policy settings used by chpasswd."],["CAVEATS","Requires root privileges. Passwords passed via command line may be visible in process listings. Consider using encrypted passwords or secure input methods in scripts. DES encryption is considered weak."],["HISTORY","**chpasswd** is part of the **shadow-utils** package for batch password management."],["INSTALL","```apt: sudo apt install passwd```\n\n```pacman: sudo pacman -S shadow```\n\n```apk: sudo apk add shadow```\n\n```zypper: sudo zypper install shadow```\n\n```nix: nix profile install nixpkgs#shadow```"],["SEE ALSO","[passwd](/man/passwd)(1), [usermod](/man/usermod)(8), [useradd](/man/useradd)(8)"]],"e":["passwd","usermod","useradd"],"x":"printf \"[username]:[new_password]\" | sudo chpasswd"},"cryptcat":{"n":"cryptcat","t":"netcat with twofish encryption","s":[["TAGLINE","netcat with twofish encryption"],["TLDR","**Listen** on a port with encryption\n\n```cryptcat -k password -l -p 1234```\n\n**Connect** to a port with encryption\n\n```cryptcat -k password host 1234```\n\nConnect with **timeout**\n\n```cryptcat -k password -w 10 host 1234```\n\n**Port scan** a host\n\n```cryptcat -v -z host 1-1024```\n\n**Proxy** data between local and remote\n\n```cryptcat -k password -l -p 8080 | cryptcat -k password host 80```\n\n**Transfer** a file\n\n```cryptcat -k password -l -p 1234 < file.txt```"],["SYNOPSIS","**cryptcat** [_options_] [_hostname_] [_port_]"],["DESCRIPTION","**cryptcat** is netcat with twofish encryption capabilities. It provides the same functionality as netcat but with encrypted data transmission for secure communication over untrusted networks."],["PARAMETERS","**-k PASSWORD**\n> Specify encryption password\n\n**-l**\n> Listen mode (act as server)\n\n**-p PORT**\n> Specify local port number\n\n**-w SECONDS**\n> Timeout for connects and final net reads\n\n**-z**\n> Zero-I/O mode (scanning)\n\n**-v**\n> Verbose mode\n\n**-n**\n> Numeric-only IP addresses, no DNS\n\n**-u**\n> UDP mode instead of TCP"],["CAVEATS","Both ends must use the same password for encryption. If no password is specified with **-k**, cryptcat uses the hardcoded default password \"metallica\", making the connection effectively unencrypted. This tool is intended for authorized security testing and legitimate encrypted communication."],["HISTORY","**cryptcat** was developed as a secure version of netcat, adding twofish encryption to prevent eavesdropping on network communications."],["SEE ALSO","[nc](/man/nc)(1), [netcat](/man/netcat)(1), [ncat](/man/ncat)(1), [socat](/man/socat)(1)"]],"e":["nc","netcat","ncat","socat"],"x":"cryptcat -k password -l -p 1234"},"dunst":{"n":"dunst","t":"lightweight notification daemon for desktops","s":[["TAGLINE","lightweight notification daemon for desktops"],["TLDR","**Start dunst** notification daemon\n\n```dunst```\n\n**Start with a custom config file**\n\n```dunst -config [path/to/dunstrc]```\n\n**Print notifications** to stdout as well as displaying them\n\n```dunst -print```\n\n**Test dunst** with a sample notification\n\n```notify-send \"Title\" \"Message body\"```\n\n**Show the most recent** notification again\n\n```dunstctl history-pop```\n\n**Toggle Do Not Disturb**\n\n```dunstctl set-paused toggle```\n\n**Start with verbose output**\n\n```dunst -verbosity debug```\n\n**Print version information**\n\n```dunst -v```"],["SYNOPSIS","**dunst** [_-config file_] [_-print_] [_-verbosity level_] [_-startup_notification_]"],["PARAMETERS","**-config** _file_\n> Use the specified configuration file.\n\n**-print**\n> Print notifications to stdout instead of displaying them.\n\n**-verbosity** _level_\n> Set verbosity level: crit, warn, mesg, info, debug.\n\n**-startup_notification**\n> Display a notification on startup.\n\n**-v**, **-version**\n> Print version information.\n\n**-h**, **-help**\n> Display help information."],["CONFIGURATION","**~/.config/dunst/dunstrc**\n> Main configuration file controlling appearance, behavior, urgency styling, and notification rules."],["DESCRIPTION","**dunst** is a lightweight notification daemon for X11 and Wayland that displays desktop notifications following the freedesktop.org notification specification. It's highly customizable through its configuration file and popular in tiling window manager setups.\n\nNotifications appear as styled popups that can be configured for position, size, colors, fonts, icons, and behavior. Dunst supports notification actions, urgency levels (low, normal, critical), and can execute scripts when notifications arrive based on rules.\n\nConfiguration is done through **~/.config/dunst/dunstrc**. The config file defines global settings, urgency-specific styling, and rules for matching notifications by app name, summary, body, or other criteria. Rules can modify notification appearance or trigger actions.\n\nKey features include notification history (accessed via **dunstctl history-pop**), Do Not Disturb mode, keyboard navigation for interacting with notifications, and support for notification actions. Critical notifications can be configured to persist until acknowledged.\n\nDunst integrates with **dunstctl** for runtime control and **dunstify** for sending notifications with advanced features like notification replacement and progress bars."],["CAVEATS","Notification daemons are **D-Bus activated**, and only one can own the `org.freedesktop.Notifications` name at a time. If a desktop environment already ships one (GNOME and KDE both do), dunst will refuse to start, or will start and never receive anything, depending on who claimed the name first. This is why dunst is mostly seen alongside tiling window managers, which ship no daemon of their own.\n\nThe corollary is that dunst does not need to be launched at all in the ordinary case: D-Bus starts it on the first notification. Adding it to an autostart script *and* relying on activation is a common way to end up with two instances.\n\nNote that the flags take a **single dash**, even the long ones: `-config`, `-verbosity`, not `--config`.\n\nConfiguration is reloaded on SIGUSR2 in recent versions, but many settings are still read at startup, and an invalid `dunstrc` prevents dunst from starting at all, which on a bare window manager means notifications silently stop working with no visible error. Check with `dunst -verbosity debug` from a terminal when that happens."],["HISTORY","**dunst** was created by **Sascha Kruse** around **2012**, for people running a tiling window manager who found that the notification daemons on offer were either tied to a full desktop environment or drew popups they could not restyle. Its configuration file, which lets any notification be matched on app name, summary, body, or urgency and then restyled or acted upon, is what set it apart and is still the reason people choose it. Wayland support arrived later, and **mako** now fills much the same role for those who want a Wayland-native daemon."],["INSTALL","```apt: sudo apt install dunst```\n\n```dnf: sudo dnf install dunst```\n\n```pacman: sudo pacman -S dunst```\n\n```apk: sudo apk add dunst```\n\n```zypper: sudo zypper install dunst```\n\n```nix: nix profile install nixpkgs#dunst```"],["SEE ALSO","[dunstctl](/man/dunstctl)(1), [dunstify](/man/dunstify)(1), [notify-send](/man/notify-send)(1), [mako](/man/mako)(1)"],["RESOURCES","```[Source code](https://github.com/dunst-project/dunst)```\n\n```[Homepage](https://dunst-project.org)```"]],"e":["dunstctl","dunstify","notify-send","mako"],"x":"dunst"},"envycontrol":{"n":"envycontrol","t":"GPU switching utility for Nvidia Optimus laptops","s":[["TAGLINE","GPU switching utility for Nvidia Optimus laptops"],["TLDR","**Switch** between GPU modes\n\n```sudo envycontrol [-s|--switch] [nvidia|integrated|hybrid]```\n\nSpecify **display manager** manually\n\n```envycontrol --dm [gdm|gdm3|sddm|lightdm]```\n\n**Check** current GPU mode\n\n```sudo envycontrol [-q|--query]```\n\n**Reset** settings\n\n```sudo envycontrol --reset```\n\nDisplay **help**\n\n```envycontrol [-h|--help]```\n\nDisplay **version**\n\n```envycontrol [-v|--version]```"],["SYNOPSIS","**envycontrol** [_options_]"],["DESCRIPTION","**envycontrol** is a GPU switching utility specifically designed for Nvidia Optimus laptops running Linux. It provides a streamlined interface for switching between integrated Intel/AMD graphics, hybrid mode (both GPUs active), and dedicated Nvidia GPU mode without requiring manual configuration file editing.\n\nThe tool simplifies power management and performance tuning on dual-GPU systems by handling the complex configuration changes needed for GPU mode switching, including display manager setup and driver configuration. Changes require a system reboot to take effect as the underlying graphics stack must be reinitialized."],["PARAMETERS","**-s**, **--switch** _mode_\n> Switch to nvidia, integrated, or hybrid\n\n**--dm** _manager_\n> Specify display manager\n\n**-q**, **--query**\n> Show current GPU mode\n\n**--reset**\n> Reset to default settings\n\n**-h**, **--help**\n> Display help message\n\n**-v**, **--version**\n> Display version"],["CAVEATS","Requires Nvidia Optimus laptop. Changes require reboot to take effect. Display manager must be correctly specified. May conflict with other GPU switching solutions."],["INSTALL","```aur: yay -S envycontrol```"],["SEE ALSO","[nvidia-smi](/man/nvidia-smi)(1)"]],"e":["nvidia-smi"],"x":"sudo envycontrol [-s|--switch] [nvidia|integrated|hybrid]"},"etcd":{"n":"etcd","t":"distributed key-value store with Raft consensus","s":[["TAGLINE","distributed key-value store with Raft consensus"],["TLDR","**Start etcd server**\n\n```etcd```\n\n**Start with custom data directory**\n\n```etcd --data-dir [/var/lib/etcd]```\n\n**Start with specific listen address**\n\n```etcd --listen-client-urls [http://localhost:2379]```\n\n**Start with advertised URLs**\n\n```etcd --advertise-client-urls [http://localhost:2379]```\n\n**Start a cluster member**\n\n```etcd --name [node1] --initial-cluster [node1=http://host1:2380,node2=http://host2:2380]```\n\n**Enable TLS**\n\n```etcd --cert-file [cert.pem] --key-file [key.pem]```"],["SYNOPSIS","**etcd** [_options_]"],["PARAMETERS","**--name** _name_\n> Human-readable node name.\n\n**--data-dir** _path_\n> Data directory path.\n\n**--listen-client-urls** _urls_\n> Client listen URLs.\n\n**--advertise-client-urls** _urls_\n> Advertised client URLs.\n\n**--listen-peer-urls** _urls_\n> Peer listen URLs.\n\n**--initial-cluster** _config_\n> Initial cluster configuration.\n\n**--initial-cluster-state** _state_\n> Initial cluster state (new or existing).\n\n**--cert-file** _file_\n> TLS certificate file.\n\n**--key-file** _file_\n> TLS key file.\n\n**--initial-cluster-token** _token_\n> Unique cluster token to prevent cross-cluster interaction.\n\n**--initial-advertise-peer-urls** _urls_\n> Peer URLs to advertise to the rest of the cluster.\n\n**--snapshot-count** _count_\n> Number of committed transactions to trigger a snapshot to disk (default 100000).\n\n**--quota-backend-bytes** _bytes_\n> Raise alarms when backend size exceeds the given quota (0 defaults to low space quota).\n\n**--max-request-bytes** _bytes_\n> Maximum client request size in bytes the server will accept (default 1572864)."],["DESCRIPTION","**etcd** is a distributed key-value store that provides reliable, consistent data storage for distributed systems. It implements the Raft consensus algorithm to ensure strong consistency across multiple nodes, making it suitable for critical configuration data and service coordination.\n\nThe server forms the backbone of Kubernetes cluster state management and is used extensively in cloud-native architectures for configuration management, service discovery, and distributed locking. Its simple HTTP/gRPC API and watch functionality enable applications to respond to configuration changes in real-time.\n\netcd prioritizes consistency and availability, making it ideal for storing cluster membership, feature flags, and other distributed system metadata."],["CONFIGURATION","**/etc/etcd/etcd.conf.yml**\n> Main configuration file for etcd server settings, cluster topology, and security options. Overrides all command-line flags and environment variables when specified.\n\n**ETCD_***\n> Every flag has a corresponding environment variable prefixed with ETCD_ in all caps and snake case (e.g., --data-dir becomes ETCD_DATA_DIR). Command-line flags take precedence over environment variables."],["CAVEATS","Requires careful cluster planning for production use. Network partitions affect availability. Disk performance impacts latency. Regular backups essential. Raft consensus requires quorum majority."],["HISTORY","etcd was created by **CoreOS** in **2013** and became a Cloud Native Computing Foundation (CNCF) project. It has become the de facto standard for Kubernetes configuration storage and is widely deployed in production cloud environments."],["INSTALL","```apt: sudo apt install etcd-server```\n\n```pacman: sudo pacman -S etcd```\n\n```apk: sudo apk add etcd```\n\n```zypper: sudo zypper install etcd```\n\n```brew: brew install etcd```\n\n```nix: nix profile install nixpkgs#etcd```"],["SEE ALSO","[etcdctl](/man/etcdctl)(1), [kubectl](/man/kubectl)(1), [consul](/man/consul)(1)"]],"e":["etcdctl","kubectl","consul"],"x":"etcd"},"find":{"n":"find","t":"search directory trees for files","s":[["TAGLINE","search directory trees for files"],["TLDR","**Find files by** name\n\n```find [path] -name \"[*.txt]\"```\n\n**Find directories**\n\n```find [path] -type d -name \"[dirname]\"```\n\n**Find and delete**\n\n```find [path] -name \"[*.tmp]\" -delete```\n\n**Find by modification** time\n\n```find [path] -mtime -[7]```\n\n**Find and execute** command\n\n```find [path] -name \"[*.log]\" -exec rm {} \\;```\n\n**Print a custom** output format\n\n```find [path] -printf \"[%p %s bytes\\n]\"```"],["SYNOPSIS","**find** [_path_...] [_expression_]"],["PARAMETERS","_PATH_\n> Starting directories (default: current).\n\n**-name** _PATTERN_\n> Match filename pattern (glob).\n\n**-iname** _PATTERN_\n> Case-insensitive name match.\n\n**-type** _TYPE_\n> File type: f (file), d (directory), l (link).\n\n**-mtime** _N_\n> Modified N days ago.\n\n**-size** _N_\n> File size (c bytes, k KB, M MB).\n\n**-exec** _CMD_ **{}** **\\;**\n> Execute command on results.\n\n**-delete**\n> Delete matched files.\n\n**-maxdepth** _N_\n> Maximum directory depth.\n\n**-printf** _FORMAT_\n> Print a custom format for each match (e.g. %p path, %f filename, %s size, %t time). No newline unless \\n is included.\n\n**--help**\n> Display help information."],["DESCRIPTION","**find** searches directory trees for files matching criteria. It's one of Unix's most powerful utilities, combining searching with file operations through -exec.\n\nThe tool evaluates expressions left to right, short-circuiting with -a (and) and -o (or). Actions like -print, -delete, and -exec operate on matches.\n\nfind's flexibility handles complex queries combining name patterns, timestamps, sizes, permissions, and ownership."],["CAVEATS","Glob patterns need quoting. -delete acts immediately without confirmation. Complex expressions need careful ordering."],["HISTORY","find appeared in **Unix Version 5** (1974). It remains the standard file-finding utility, though modern alternatives like fd offer simpler syntax for common cases."],["INSTALL","```apt: sudo apt install findutils```\n\n```dnf: sudo dnf install findutils```\n\n```pacman: sudo pacman -S findutils```\n\n```apk: sudo apk add findutils```\n\n```zypper: sudo zypper install findutils```\n\n```brew: brew install findutils```\n\n```nix: nix profile install nixpkgs#findutils```"],["SEE ALSO","[fd](/man/fd)(1), [locate](/man/locate)(1), [xargs](/man/xargs)(1)"]],"e":["fd","locate","xargs"],"x":"find [path] -name \"[*.txt]\""},"fwupdtool":{"n":"fwupdtool","t":"low-level firmware manipulation tool","s":[["TAGLINE","low-level firmware manipulation tool"],["TLDR","Display all **devices detected** by fwupd\n\n```fwupdtool get-devices```\n\n**Install** firmware from a file\n\n```fwupdtool install [path/to/firmware]```\n\nDisplay **help**\n\n```fwupdtool -h```"],["SYNOPSIS","**fwupdtool** [_command_] [_options_]"],["PARAMETERS","**get-devices**\n> List all devices detected by fwupd\n\n**install** _FILE_\n> Install firmware from a local cabinet (.cab) file\n\n**get-details** _FILE_\n> Show details about a firmware file\n\n**verify** _DEVICE_\n> Verify firmware on a device\n\n**firmware-parse** _FILE_\n> Parse and display firmware file structure\n\n**firmware-convert** _FILE_\n> Convert firmware to different format\n\n**-h**, **--help**\n> Display help message\n\n**-v**, **--verbose**\n> Enable verbose output"],["DESCRIPTION","**fwupdtool** is a low-level firmware manipulation tool that works alongside fwupd. Unlike **fwupdmgr** which handles automated updates from LVFS, fwupdtool allows manual firmware operations including installing local firmware files, parsing firmware archives, and device verification.\n\nThis tool is primarily used for debugging, development, and situations where firmware must be installed manually from a vendor-provided file rather than through the LVFS repository."],["CAVEATS","Manual firmware installation bypasses LVFS signature verification. Only install firmware from trusted sources. Incorrect firmware can brick devices. Most users should prefer **fwupdmgr** for routine updates."],["HISTORY","fwupdtool is part of the fwupd project, created by Richard Hughes in **2015**. It serves as the development and debugging companion to the main fwupdmgr tool."],["INSTALL","```apt: sudo apt install fwupd```\n\n```dnf: sudo dnf install fwupd```\n\n```pacman: sudo pacman -S fwupd```\n\n```apk: sudo apk add fwupd```\n\n```zypper: sudo zypper install fwupd```\n\n```brew: brew install fwupd```\n\n```nix: nix profile install nixpkgs#fwupd```"],["SEE ALSO","[fwupdmgr](/man/fwupdmgr)(1), [efibootmgr](/man/efibootmgr)(8)"],["RESOURCES","```[Source code](https://github.com/fwupd/fwupd)```\n\n```[Homepage](https://fwupd.org/)```"]],"e":["fwupdmgr","efibootmgr"],"x":"fwupdtool get-devices"},"git-locked":{"n":"git-locked","t":"List files locked with git lock","s":[["TAGLINE","List files locked with git lock"],["TLDR","**Show locked files**\n\n```git locked```"],["SYNOPSIS","**git locked**"],["DESCRIPTION","**git locked**, a git-extras command, lists files in the current repository that have been marked with the skip-worktree bit via `git lock`. Internally it runs `git ls-files -v` and filters for entries flagged **S**, Git's indicator for a skip-worktree file.\n\nThis gives quick visibility into which locally-tracked files have local-only edits protected from being picked up by `git status`/`git add`, such as machine-specific config files."],["CAVEATS","Part of git-extras package, not core Git. Only shows the local, per-clone skip-worktree state, not any lock shared with teammates or a remote."],["HISTORY","git locked is part of **git-extras**, complementing git lock and git unlock."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-lock](/man/git-lock)(1), [git-unlock](/man/git-unlock)(1), [git-ls-files](/man/git-ls-files)(1)"],["RESOURCES","```[Source code](https://github.com/tj/git-extras)```\n\n```[Documentation](https://github.com/tj/git-extras/blob/master/Commands.md)```"]],"e":["git-lock","git-unlock","git-ls-files"],"x":"git locked"},"helix":{"n":"helix","t":"post-modern modal text editor inspired by Kakoune and Vim","s":[["TAGLINE","post-modern modal text editor inspired by Kakoune and Vim"],["TLDR","**Open file**\n\n```hx [file]```\n\n**Open at specific line**\n\n```hx [file]:[line]```\n\n**Open multiple files**\n\n```hx [file1] [file2]```\n\n**Check health**\n\n```hx --health```\n\n**Open tutor**\n\n```hx --tutor```\n\n**Open a file at a specific working directory**\n\n```hx -w [path/to/project] [file]```"],["SYNOPSIS","**hx** [_options_] [_files_]..."],["PARAMETERS","_FILES_\n> Files to edit, optionally suffixed with **:LINE** or **:LINE:COLUMN**.\n\n**+**_N_\n> Open the first given file at line _N_ (or the last line if _N_ is omitted).\n\n**--health** [_CATEGORY_]\n> Check for potential setup problems; _CATEGORY_ can be a language name, or one of clipboard, languages, all-languages, all.\n\n**--tutor**\n> Open the interactive tutorial.\n\n**-g**, **--grammar** {**fetch**|**build**}\n> Fetch or build the tree-sitter grammars listed in languages.toml.\n\n**-c**, **--config** _FILE_\n> Specify a configuration file to use.\n\n**--log** _FILE_\n> Specify a file to use for logging.\n\n**-w**, **--working-dir** _PATH_\n> Specify an initial working directory.\n\n**--vsplit**\n> Split all given files vertically into separate windows.\n\n**--hsplit**\n> Split all given files horizontally into separate windows.\n\n**--strict**\n> Bail with an error for commands that can fail.\n\n**-v**\n> Increase logging verbosity (repeatable, up to 3 times).\n\n**-h**, **--help**\n> Display help information.\n\n**-V**, **--version**\n> Print version information."],["DESCRIPTION","**Helix** is a post-modern modal text editor inspired by Kakoune and Vim. It features multiple selections, built-in LSP support, and tree-sitter integration.\n\nThe editor provides syntax highlighting, auto-completion, and diagnostics out of the box. It uses selection-action model instead of verb-object."],["CAVEATS","Different keybindings than Vim. Learning curve for selection model. Rust-based, requires compilation."],["HISTORY","**Helix** was started by **Blaž Hrastnik** in 2020 and first announced publicly in 2021. Unlike Vim/Neovim, it ships with sane defaults, tree-sitter-based syntax analysis, and LSP support built in rather than added via plugins, and it adopts Kakoune's selection-first (\"selection, then action\") editing model."],["INSTALL","```dnf: sudo dnf install helix```\n\n```pacman: sudo pacman -S helix```\n\n```zypper: sudo zypper install helix```\n\n```brew: brew install helix```\n\n```nix: nix profile install nixpkgs#helix```"],["SEE ALSO","[hx](/man/hx)(1), [vim](/man/vim)(1), [nvim](/man/nvim)(1), [kakoune](/man/kakoune)(1)"],["RESOURCES","```[Source code](https://github.com/helix-editor/helix)```\n\n```[Homepage](https://helix-editor.com)```\n\n```[Documentation](https://docs.helix-editor.com)```"]],"e":["hx","vim","nvim","kakoune"],"x":"hx [file]"},"hexyl":{"n":"hexyl","t":"modern hex viewer for the terminal","s":[["TAGLINE","modern hex viewer for the terminal"],["TLDR","**View file in hex**\n\n```hexyl [file]```\n\n**Show specific length**\n\n```hexyl -n [100] [file]```\n\n**Skip bytes**\n\n```hexyl --skip [50] [file]```\n\n**Show in plain style**\n\n```hexyl --plain [file]```\n\n**Read from stdin**\n\n```cat [file] | hexyl```\n\n**No character panel**\n\n```hexyl --no-characters [file]```"],["SYNOPSIS","**hexyl** [_options_] [_file_]"],["PARAMETERS","_FILE_\n> File to display (use - for stdin).\n\n**-n**, **--length** _N_\n> Number of bytes to read (supports k, M, G suffixes and hex).\n\n**-c**, **--bytes** _N_\n> Alias for --length.\n\n**-s**, **--skip** _N_\n> Skip first N bytes (negative values seek from end).\n\n**-o**, **--display-offset** _N_\n> Add N bytes to the displayed file offset.\n\n**--block-size** _SIZE_\n> Size of the block unit (default: 512).\n\n**-v**, **--no-squeezing**\n> Display all data; don't collapse identical lines with an asterisk.\n\n**--color** _WHEN_\n> Color output: always (default), auto, never.\n\n**-p**, **--plain**\n> Plain output (shorthand for --no-characters --no-position --border=none --color=never).\n\n**--no-characters**\n> Hide the character panel.\n\n**--no-position**\n> Hide the position offset column.\n\n**--border** _STYLE_\n> Border style: unicode (default), ascii, none.\n\n**--panels** _N_\n> Number of hex data panels (or 'auto').\n\n**--group-size** _N_\n> Bytes per group: 1 (default), 2, 4, or 8.\n\n**--endianness** _ENDIAN_\n> Byte ordering within groups: little or big.\n\n**-h**, **--help**\n> Display help information.\n\n**-V**, **--version**\n> Display version information."],["DESCRIPTION","**hexyl** is a modern hex viewer for the terminal. It displays file contents with colored output distinguishing different byte types.\n\nThe tool uses colors to highlight ASCII, non-ASCII, null bytes, and whitespace. It provides a clean, readable hex dump with offset and character views."],["CAVEATS","Requires color terminal. Large files may be slow. Written in Rust."],["HISTORY","hexyl was created as a modern, colorful alternative to traditional hex dump tools like xxd and hexdump."],["INSTALL","```apt: sudo apt install hexyl```\n\n```dnf: sudo dnf install hexyl```\n\n```pacman: sudo pacman -S hexyl```\n\n```apk: sudo apk add hexyl```\n\n```zypper: sudo zypper install hexyl```\n\n```brew: brew install hexyl```\n\n```nix: nix profile install nixpkgs#hexyl```"],["SEE ALSO","[xxd](/man/xxd)(1), [hexdump](/man/hexdump)(1), [hd](/man/hd)(1)"]],"e":["xxd","hexdump","hd"],"x":"hexyl [file]"},"impacket-dumpntlminfo":{"n":"impacket-dumpntlminfo","t":"Dump remote host NTLM authentication information","s":[["TAGLINE","Dump remote host NTLM authentication information"],["TLDR","**Dump NTLM info** from a target host via SMB\n\n```impacket-dumpntlminfo [192.168.1.100]```\n\n**Dump NTLM info** using a specific port\n\n```impacket-dumpntlminfo -port [139] [192.168.1.100]```\n\n**Dump NTLM info** using RPC protocol instead of SMB\n\n```impacket-dumpntlminfo -protocol RPC [192.168.1.100]```\n\n**Dump NTLM info** with a specific target IP\n\n```impacket-dumpntlminfo -target-ip [192.168.1.100] [hostname]```\n\n**Enable debug output** for troubleshooting\n\n```impacket-dumpntlminfo -debug [192.168.1.100]```"],["SYNOPSIS","**impacket-dumpntlminfo** [_-debug_] [_-ts_] [_-target-ip IP_] [_-port PORT_] [_-protocol {SMB,RPC}_] _target_"],["PARAMETERS","**-debug**\n> Turn DEBUG output on.\n\n**-ts**\n> Add timestamp to every logging output.\n\n**-target-ip** _IP_\n> IP address of the target machine. Useful when target is a NetBIOS name that cannot be resolved.\n\n**-port** _PORT_\n> Destination port to connect to the SMB/RPC server. Default is 445.\n\n**-protocol** {**SMB**, **RPC**}\n> Protocol to use. Default is SMB. Port 135 normally uses RPC."],["DESCRIPTION","**impacket-dumpntlminfo** performs NTLM authentication against a remote host and extracts information from the NTLM challenge response, **without requiring any credentials**. By initiating an SMB or RPC connection, the tool triggers an NTLM authentication handshake and parses the server's response to reveal details such as the hostname, domain name, DNS information, OS version, and timestamp.\n\nThis is useful for reconnaissance during penetration testing, as it provides network and host information without authentication."],["CAVEATS","Only works against hosts with SMB or RPC services exposed. Firewalls or security policies may block unauthenticated NTLM negotiation. The amount of information returned depends on the target's SMB/RPC configuration and Windows version."],["HISTORY","Part of the **Impacket** library, originally developed by **SecureAuth** (now **Fortra**). Impacket is a collection of Python classes for working with network protocols, widely used in penetration testing and security research. The tool leverages the NTLM authentication handshake to extract server metadata without credentials."],["INSTALL","```pacman: sudo pacman -S impacket```"],["SEE ALSO","[impacket-psexec](/man/impacket-psexec)(1), [impacket-smbclient](/man/impacket-smbclient)(1), [impacket-rpcdump](/man/impacket-rpcdump)(1)"]],"e":["impacket-psexec","impacket-smbclient","impacket-rpcdump"],"x":"impacket-dumpntlminfo [192.168.1.100]"},"jj-status":{"n":"jj-status","t":"shows the status of the working copy in Jujutsu","s":[["TAGLINE","shows the status of the working copy in Jujutsu"],["TLDR","**Show working copy status**\n\n```jj status```\n\n**Short format**\n\n```jj st```"],["SYNOPSIS","**jj** **status** | **st**"],["DESCRIPTION","**jj status** shows the status of the working copy in Jujutsu. Displays the current change, parent commit, and any modified files. In jj, the working copy is always treated as a commit being worked on."],["INSTALL","```pacman: sudo pacman -S jujutsu```\n\n```apk: sudo apk add jujutsu```\n\n```zypper: sudo zypper install jujutsu```\n\n```brew: brew install jujutsu```\n\n```nix: nix profile install nixpkgs#jujutsu```"],["SEE ALSO","[jj](/man/jj)(1), [jj-diff](/man/jj-diff)(1)"]],"e":["jj","jj-diff"],"x":"jj status"},"kdenlive":{"n":"kdenlive","t":"non-linear video editor for KDE","s":[["TAGLINE","non-linear video editor for KDE"],["TLDR","**Start Kdenlive**\n\n```kdenlive```\n\n**Open project**\n\n```kdenlive [project.kdenlive]```\n\n**Render from command line**\n\n```kdenlive_render [project.kdenlive]```"],["SYNOPSIS","**kdenlive** [_options_] [_file_]"],["PARAMETERS","_file_\n> Project file to open.\n\n**--config** _file_\n> Use alternative config.\n\n**--mlt-log**\n> MLT debug output.\n\n**--help**\n> Show help."],["DESCRIPTION","**Kdenlive** is a non-linear video editor for KDE. It provides multi-track editing, effects, transitions, and supports a wide range of video formats through FFmpeg and MLT.\n\nKdenlive offers professional features like keyframing, color correction, audio mixing, and titling. It's suitable for both amateur and professional video production."],["FEATURES","- Multi-track timeline editing\n- Video and audio effects\n- Transitions and compositing\n- Keyframe animation\n- Proxy editing for performance\n- Color correction tools\n- Audio mixing\n- Titler"],["KEYBOARD SHORTCUTS","```\nSpace       Play/Pause\nJ/K/L       Shuttle control\nI/O         Set in/out points\nV           Selection tool\nX           Razor tool\nS           Split clip\n```"],["CAVEATS","Requires KDE libraries. Resource-intensive for 4K editing. Proxy clips recommended for performance. Stability varies by version."],["HISTORY","Kdenlive was started by **Jason Wood** in **2002** and has been continuously developed by the KDE community. It became one of the most capable open-source video editors on Linux."],["INSTALL","```apt: sudo apt install kdenlive```\n\n```dnf: sudo dnf install kdenlive```\n\n```pacman: sudo pacman -S kdenlive```\n\n```apk: sudo apk add kdenlive```\n\n```zypper: sudo zypper install kdenlive```"],["SEE ALSO","[ffmpeg](/man/ffmpeg)(1), [melt](/man/melt)(1), [openshot](/man/openshot)(1)"]],"e":["ffmpeg","melt","openshot"],"x":"kdenlive"},"kid3":{"n":"kid3","t":"audio tag editor supporting MP3, Ogg Vorbis, FLAC, MPC, MP4/AAC, and other","s":[["TAGLINE","audio tag editor supporting MP3, Ogg Vorbis, FLAC, MPC, MP4/AAC, and other"],["TLDR","**Start Kid3**\n\n```kid3```\n\n**Open directory**\n\n```kid3 [/path/to/music]```\n\n**Command-line tagging**\n\n```kid3-cli -c \"[get title]\" [file.mp3]```\n\n**Set tag value**\n\n```kid3-cli -c \"[set title 'My Song']\" [file.mp3]```\n\n**Batch convert**\n\n```kid3-cli -c \"[fromtag '%{artist} - %{title}' 2]\" [*.mp3]```"],["SYNOPSIS","**kid3** [_options_] [_directory_]\n\n**kid3-cli** [_options_] [_files_...]"],["PARAMETERS","_directory_\n> Music directory to open.\n\n**-c** _command_\n> Execute command (kid3-cli)."],["DESCRIPTION","**Kid3** is an audio tag editor supporting MP3, Ogg Vorbis, FLAC, MPC, MP4/AAC, and other formats. It can edit ID3v1, ID3v2, APE, Vorbis, and other tag formats.\n\nThe application provides batch editing, filename-to-tag and tag-to-filename conversion, and import from online databases like MusicBrainz and Discogs."],["CLI COMMANDS","```bash"],["Get tag","kid3-cli -c \"get artist\" song.mp3"],["Set multiple tags","kid3-cli -c \"set artist 'Band'\" -c \"set album 'Album'\" song.mp3"],["Rename from tags","kid3-cli -c \"fromtag '%{track} - %{title}' 2\" *.mp3"],["Export to CSV","kid3-cli -c \"export export.csv 'CSV'\" .\n```"],["CAVEATS","Different tag versions may conflict. Batch operations should be previewed. Some formats have tag limitations."],["HISTORY","Kid3 was created by **Urs Fleisch** as a simple tag editor for KDE. It evolved to support many formats and became available on multiple platforms."],["INSTALL","```apt: sudo apt install kid3```\n\n```dnf: sudo dnf install kid3```\n\n```pacman: sudo pacman -S kid3```\n\n```zypper: sudo zypper install kid3```\n\n```nix: nix profile install nixpkgs#kid3```"],["SEE ALSO","[id3v2](/man/id3v2)(1), [easytag](/man/easytag)(1)"]],"e":["id3v2","easytag"],"x":"kid3"},"krunvm":{"n":"krunvm","t":"manages lightweight microVMs","s":[["TAGLINE","manages lightweight microVMs"],["TLDR","**Create microVM**\n\n```krunvm create [image] --name [myvm]```\n\n**Start microVM**\n\n```krunvm start [myvm]```\n\n**List microVMs**\n\n```krunvm list```\n\n**Delete microVM**\n\n```krunvm delete [myvm]```\n\n**Create with port mapping**\n\n```krunvm create [image] --name [vm] -p [8080:80]```\n\n**Set CPU and memory**\n\n```krunvm create [image] --cpus [2] --mem [1024]```"],["SYNOPSIS","**krunvm** _command_ [_options_]"],["PARAMETERS","**create** _IMAGE_\n> Create microVM from image.\n\n**start** _NAME_\n> Start microVM.\n\n**delete** _NAME_\n> Delete microVM.\n\n**list**\n> List microVMs.\n\n**--name** _NAME_\n> VM name.\n\n**--cpus** _N_\n> Number of CPUs.\n\n**--mem** _MB_\n> Memory in MB.\n\n**-p** _MAPPING_\n> Port mapping."],["DESCRIPTION","**krunvm** is a tool for creating and managing lightweight microVMs built from OCI container images. It uses libkrun, a library that leverages KVM virtualization to run each workload in its own minimal virtual machine, providing stronger isolation than containers while maintaining a container-like user experience with fast startup times and low memory overhead.\n\nMicroVMs are created from standard container images pulled from registries, so existing container workflows and images can be reused directly. Each VM can be configured with a specific number of CPUs, memory allocation, and port mappings, similar to container runtime options. The result is a workflow that feels like managing containers but with the security boundaries of hardware virtualization, making krunvm well suited for multi-tenant environments or workloads that require stronger isolation guarantees."],["CAVEATS","Linux with KVM required. Container images as base. Lightweight isolation."],["HISTORY","krunvm was created by **Red Hat** as a simple tool for managing microVMs using libkrun virtualization."],["INSTALL","```dnf: sudo dnf install krunvm```\n\n```pacman: sudo pacman -S krunvm```\n\n```zypper: sudo zypper install krunvm```\n\n```nix: nix profile install nixpkgs#krunvm```"],["SEE ALSO","[podman](/man/podman)(1), [docker](/man/docker)(1)"]],"e":["podman","docker"],"x":"krunvm create [image] --name [myvm]"},"kube-fzf":{"n":"kube-fzf","t":"provides fuzzy-finding commands for Kubernetes","s":[["TAGLINE","provides fuzzy-finding commands for Kubernetes"],["TLDR","**Interactive pod selection**\n\n```findpod```\n\n**Tail pod logs**\n\n```tailpod```\n\n**Exec into pod**\n\n```execpod```\n\n**Describe pod**\n\n```describepod```\n\n**Port forward**\n\n```pfpod```"],["SYNOPSIS","Commands: **findpod**, **tailpod**, **execpod**, **describepod**, **pfpod**"],["PARAMETERS","**findpod**\n> Fuzzy find and select pod.\n\n**tailpod**\n> Tail logs with fuzzy selection.\n\n**execpod**\n> Exec into pod interactively.\n\n**describepod**\n> Describe selected pod.\n\n**pfpod**\n> Port forward to selected pod."],["DESCRIPTION","**kube-fzf** is a set of shell functions that wrap common kubectl operations with fzf fuzzy-finding for interactive Kubernetes resource selection. Instead of typing exact pod names or copying them from `kubectl get pods` output, kube-fzf presents a searchable, filterable list of resources and executes the chosen kubectl command on the selected item.\n\nThe package provides several purpose-built commands: `findpod` for locating and selecting pods, `tailpod` for streaming logs, `execpod` for opening a shell session inside a container, `describepod` for viewing pod details, and `pfpod` for setting up port forwarding. Each command queries the current Kubernetes context and namespace, presents matching pods through fzf's interactive interface, and passes the selection directly to the appropriate kubectl subcommand."],["CAVEATS","Requires fzf installed. Shell functions. kubectl must work."],["HISTORY","kube-fzf combines **fzf** fuzzy finder with kubectl for interactive Kubernetes resource selection."],["INSTALL","```aur: yay -S kube-fzf```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [fzf](/man/fzf)(1), [k9s](/man/k9s)(1)"]],"e":["kubectl","fzf","k9s"],"x":"findpod"},"kustomize":{"n":"kustomize","t":"customizes Kubernetes YAML configurations without templates","s":[["TAGLINE","customizes Kubernetes YAML configurations without templates"],["TLDR","**Build kustomization** from current directory\n\n```kustomize build .```\n\n**Build from specific directory**\n\n```kustomize build [path/to/overlay]```\n\n**Build from git repository**\n\n```kustomize build [https://github.com/org/repo//path]```\n\n**Create new kustomization** in current directory\n\n```kustomize create```\n\n**Create with autodetected resources**\n\n```kustomize create --autodetect```\n\n**Edit to add a resource**\n\n```kustomize edit add resource [deployment.yaml]```\n\n**Edit to set namespace**\n\n```kustomize edit set namespace [my-namespace]```\n\n**Edit to set image tag**\n\n```kustomize edit set image [nginx:1.25]```"],["SYNOPSIS","**kustomize** _command_ [_options_]"],["COMMANDS","**build** [_path_]\n> Build kustomization target from directory or URL to stdout.\n\n**create**\n> Create a new kustomization.yaml in current directory.\n\n**edit**\n> Edit an existing kustomization.yaml file.\n\n**cfg**\n> Commands for reading and writing configuration.\n\n**fn**\n> Commands for running functions against configuration.\n\n**completion** _shell_\n> Generate shell completion script.\n\n**version**\n> Print kustomize version.\n\n**help**\n> Help about any command."],["PARAMETERS","**--enable-helm**\n> Enable Helm chart inflator generator.\n\n**--enable-alpha-plugins**\n> Enable alpha kustomize plugins.\n\n**--as-current-user**\n> Use uid and gid of command executor.\n\n**--load-restrictor** _value_\n> Control file loading: LoadRestrictionsRootOnly or LoadRestrictionsNone.\n\n**-o**, **--output** _path_\n> Write output to file instead of stdout.\n\n**--stack-trace**\n> Print stack trace on error.\n\n**-h**, **--help**\n> Show help information."],["EDIT SUBCOMMANDS","**edit add resource** _file_\n> Add a resource to kustomization.\n\n**edit add patch** _file_\n> Add a patch file.\n\n**edit set namespace** _name_\n> Set the namespace for all resources.\n\n**edit set image** _name:tag_\n> Set or override an image tag.\n\n**edit set replicas** _name=count_\n> Set replica count for a deployment.\n\n**edit add label** _key:value_\n> Add a label to all resources.\n\n**edit add annotation** _key:value_\n> Add an annotation to all resources."],["DESCRIPTION","**kustomize** customizes Kubernetes YAML configurations without templates. It uses a **kustomization.yaml** file to declaratively specify base resources and overlay modifications like patches, labels, namespaces, and image tags.\n\nThe tool follows a base/overlay pattern: bases contain common resources, overlays customize them for specific environments (dev, staging, prod). This approach avoids duplication while maintaining environment-specific differences.\n\nKustomize is built into kubectl (since v1.14) as **kubectl kustomize** and **kubectl apply -k**. The standalone CLI provides the latest features and is recommended for advanced use cases.\n\nKey concepts include **resources** (YAML files to include), **patches** (strategic merge or JSON patches), **configMapGenerator** and **secretGenerator** (generate ConfigMaps/Secrets from files), and **vars** (variable substitution)."],["CAVEATS","The kubectl-embedded version may lag behind the standalone CLI. Some features require **--enable-helm** or **--enable-alpha-plugins** flags. Variable substitution has limitations compared to templating tools like Helm."],["HISTORY","Kustomize was developed by the **Kubernetes SIG CLI** team starting around **2017** as a template-free approach to Kubernetes configuration management. It was integrated into kubectl in version **1.14 (2019)**. The project emphasizes declarative configuration and follows Kubernetes' philosophy of desired state management."],["INSTALL","```apt: sudo apt install kustomize```\n\n```dnf: sudo dnf install kustomize```\n\n```pacman: sudo pacman -S kustomize```\n\n```apk: sudo apk add kustomize```\n\n```zypper: sudo zypper install kustomize```\n\n```brew: brew install kustomize```\n\n```nix: nix profile install nixpkgs#kustomize```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [helm](/man/helm)(1), [k9s](/man/k9s)(1)"]],"e":["kubectl","helm","k9s"],"x":"kustomize build ."},"lint":{"n":"lint","t":"classic static analysis tool for C programs","s":[["TAGLINE","classic static analysis tool for C programs"],["TLDR","**Check C program**\n\n```lint [program.c]```\n\n**Check with library**\n\n```lint -l[library] [program.c]```\n\n**Suppress specific warnings**\n\n```lint -e[code] [program.c]```\n\n**Portable mode**\n\n```lint -p [program.c]```"],["SYNOPSIS","**lint** [_options_] _files_..."],["PARAMETERS","**-a**\n> Report assignments in conditionals.\n\n**-b**\n> Report break statements.\n\n**-c**\n> Produce .ln files.\n\n**-e** _code_\n> Suppress error code.\n\n**-l** _lib_\n> Include lint library.\n\n**-p**\n> Portable C checking.\n\n**-v**\n> Verbose output."],["DESCRIPTION","**lint** is a classic static analysis tool for C programs. It checks source code for potential bugs, stylistic errors, and suspicious constructs that are syntactically valid but likely incorrect.\n\nlint performs deeper analysis than the compiler, catching type mismatches, unused variables, and problematic patterns."],["COMMON CHECKS","```\n- Type consistency\n- Unused variables\n- Unreachable code\n- Format string issues\n- Missing return values\n```"],["CAVEATS","Original lint largely replaced by compiler warnings and modern tools like clang-tidy, cppcheck. Traditional lint may not be available on all systems."],["HISTORY","lint was written by **Stephen C. Johnson** at Bell Labs in **1978**. The name comes from the lint (fluff) it picks from programs. It pioneered static analysis tools."],["SEE ALSO","[cppcheck](/man/cppcheck)(1), [clang-tidy](/man/clang-tidy)(1), [gcc](/man/gcc)(1)"]],"e":["cppcheck","clang-tidy","gcc"],"x":"lint [program.c]"},"lsyncd":{"n":"lsyncd","t":"watches directories for changes and synchronizes them in real-time","s":[["TAGLINE","watches directories for changes and synchronizes them in real-time"],["TLDR","**Sync directories locally**\n\n```lsyncd -rsync [/source/] [/destination/]```\n\n**Sync to remote server**\n\n```lsyncd -rsyncssh [/local/] [user@host] [/remote/]```\n\n**Use configuration file**\n\n```lsyncd [config.lua]```\n\n**Run in foreground with logging**\n\n```lsyncd -log all [config.lua]```\n\n**Dry run** (show what would sync)\n\n```lsyncd -nodaemon -log all [config.lua]```\n\n**Specify log file**\n\n```lsyncd -logfile [/var/log/lsyncd.log] [config.lua]```"],["SYNOPSIS","**lsyncd** [_-rsync source dest_] [_-rsyncssh source host dest_] [_-log level_] [_options_] [_config.lua_]"],["PARAMETERS","**-rsync** _SOURCE_ _DEST_\n> Simple rsync sync mode.\n\n**-rsyncssh** _SOURCE_ _HOST_ _DEST_\n> Rsync over SSH to remote host.\n\n**-nodaemon**\n> Run in foreground, don't daemonize.\n\n**-log** _LEVEL_\n> Set log level (all, scarce, normal, error).\n\n**-logfile** _FILE_\n> Write logs to file.\n\n**-pidfile** _FILE_\n> Write PID file.\n\n**-delay** _SECONDS_\n> Delay before syncing (aggregation window).\n\n**-insist**\n> Keep trying on startup errors.\n\n**-version**\n> Show version.\n\n**-help**\n> Show help."],["DESCRIPTION","**lsyncd** (Live Syncing Daemon) watches directories for changes and synchronizes them in real-time. It uses inotify on Linux to detect file changes efficiently.\n\nWhen changes occur, lsyncd waits briefly (delay) to aggregate multiple changes, then triggers rsync to synchronize. This approach handles burst writes efficiently while maintaining near-real-time sync.\n\nConfiguration files use Lua scripting for flexibility. They define sync pairs, exclude patterns, rsync options, and can implement custom sync logic.\n\nThe tool works with both local directories and remote servers via SSH. Multiple sync pairs can run simultaneously from a single configuration.\n\nCommon uses include backup systems, cluster file synchronization, and development environments where files need to stay synchronized across machines."],["CAVEATS","Initial sync can take time for large directories. Inotify watch limits may need increasing for large trees. High-frequency changes can cause delays. Symlinks and special files need careful handling."],["HISTORY","**lsyncd** was created by **Axel Kittenberger** around **2008**. It combined inotify's efficient change notification with rsync's reliable file synchronization. The project introduced Lua configuration in version 2, enabling sophisticated sync scenarios."],["INSTALL","```apt: sudo apt install lsyncd```\n\n```dnf: sudo dnf install lsyncd```\n\n```apk: sudo apk add lsyncd```\n\n```zypper: sudo zypper install lsyncd```\n\n```brew: brew install lsyncd```\n\n```nix: nix profile install nixpkgs#lsyncd```"],["SEE ALSO","[rsync](/man/rsync)(1), [inotifywait](/man/inotifywait)(1), [unison](/man/unison)(1), [syncthing](/man/syncthing)(1)"]],"e":["rsync","inotifywait","unison","syncthing"],"x":"lsyncd -rsync [/source/] [/destination/]"},"memray":{"n":"memray","t":"Python memory profiler that tracks allocations","s":[["TAGLINE","Python memory profiler that tracks allocations"],["TLDR","**Profile memory usage**\n\n```memray run [script.py]```\n\n**Profile with output file**\n\n```memray run -o [output.bin] [script.py]```\n\n**Generate flame graph**\n\n```memray flamegraph [output.bin]```\n\n**Generate summary report**\n\n```memray summary [output.bin]```\n\n**Live memory view**\n\n```memray run --live [script.py]```\n\n**Profile with native tracking**\n\n```memray run --native [script.py]```\n\n**Generate tree view**\n\n```memray tree [output.bin]```\n\n**Attach to running process**\n\n```memray attach [pid]```"],["SYNOPSIS","**memray** [_run_] [_flamegraph_] [_summary_] [_tree_] [_attach_] [_options_] [_args_]"],["PARAMETERS","**run** _SCRIPT_\n> Profile script execution.\n\n**flamegraph** _FILE_\n> Generate flame graph HTML.\n\n**summary** _FILE_\n> Show allocation summary.\n\n**tree** _FILE_\n> Show tree view of allocations.\n\n**stats** _FILE_\n> Show statistics.\n\n**table** _FILE_\n> Show table of allocations.\n\n**attach** _PID_\n> Attach to running process.\n\n**-o** _FILE_, **--output** _FILE_\n> Output file for profile data.\n\n**--live**\n> Show live memory view.\n\n**--native**\n> Track native (C/C++) allocations.\n\n**--follow-fork**\n> Follow child processes.\n\n**-f**, **--force**\n> Overwrite existing output."],["DESCRIPTION","**memray** profiles Python memory allocations, tracking where memory is allocated and by which code. It helps identify memory leaks, high-water marks, and allocation patterns.\n\nThe tool tracks every allocation and deallocation, recording stack traces. Post-run analysis generates visualizations showing memory usage over time and by location.\n\nFlame graphs show allocation call stacks, with width representing allocation size. They quickly identify which functions allocate most memory.\n\nLive mode provides real-time visualization during execution. It's useful for understanding memory patterns in long-running applications.\n\nNative tracking includes C extension allocations, not just Python objects. This reveals memory used by numpy, pandas, and other libraries with native components.\n\nProcess attachment profiles already-running applications, useful for production debugging without restart."],["CAVEATS","Tracking overhead slows execution. Large profiles consume disk space. Native tracking requires debug symbols for readable output. Some allocations may be missed in highly optimized code."],["HISTORY","**memray** was developed by **Bloomberg** and released as open source in **2022**. It was designed for debugging memory issues in large-scale Python applications used in financial services."],["INSTALL","```apt: sudo apt install python3-memray```\n\n```dnf: sudo dnf install python3-memray```\n\n```pacman: sudo pacman -S memray```\n\n```brew: brew install memray```\n\n```nix: nix profile install nixpkgs#memray```"],["SEE ALSO","[py-spy](/man/py-spy)(1), [valgrind](/man/valgrind)(1)"]],"e":["py-spy","valgrind"],"x":"memray run [script.py]"},"mkvmerge":{"n":"mkvmerge","t":"creates Matroska files","s":[["TAGLINE","creates Matroska files"],["TLDR","**Merge files**\n\n```mkvmerge -o [output.mkv] [input1.mp4] [input2.mp4]```\n\n**Add subtitles**\n\n```mkvmerge -o [output.mkv] [video.mkv] [subtitles.srt]```\n\n**Extract tracks**\n\n```mkvmerge -o [output.mkv] --audio-tracks [1] [input.mkv]```\n\n**Set default track flag**\n\n```mkvmerge -o [output.mkv] --default-track-flag [0:1] [input.mkv]```\n\n**Split by size**\n\n```mkvmerge -o [output.mkv] --split [700M] [input.mkv]```\n\n**Set track language**\n\n```mkvmerge -o [output.mkv] --language [0:eng] [input.mkv]```\n\n**Add title**\n\n```mkvmerge -o [output.mkv] --title \"[Movie Title]\" [input.mkv]```\n\n**Identify tracks** in a file\n\n```mkvmerge -i [input.mkv]```"],["SYNOPSIS","**mkvmerge** [_options_] -o _output_ _inputs_"],["PARAMETERS","**-o** _FILE_\n> Output file.\n\n_INPUTS_\n> Input files.\n\n**-i** _FILE_\n> Identify tracks, codecs, and properties of input file.\n\n**--audio-tracks** _IDS_\n> Select audio tracks to include.\n\n**--video-tracks** _IDS_\n> Select video tracks to include.\n\n**--subtitle-tracks** _IDS_\n> Select subtitle tracks to include.\n\n**--no-audio**\n> Exclude all audio tracks.\n\n**--no-subtitles**\n> Exclude all subtitle tracks.\n\n**--language** _TID:LANG_\n> Set language for a track (ISO 639-2 code).\n\n**--default-track-flag** _TID:BOOL_\n> Set or clear the default track flag.\n\n**--split** _SIZE_|_duration:TIME_|_chapters:all_\n> Split output by size, duration, or chapters.\n\n**--title** _TEXT_\n> Set segment title.\n\n**--append-to** _SPEC_\n> Append tracks from one file after another.\n\n**-l, --list-types**\n> List supported source file formats."],["DESCRIPTION","**mkvmerge** creates Matroska (MKV/MKA/MKS) container files by merging video, audio, and subtitle streams from various source formats. It does not transcode; it only remuxes streams into the Matroska container.\n\nThe tool is part of MKVToolNix. Track IDs can be found with **mkvmerge -i** or **mkvinfo**. Option order matters: global options first, then per-file options before the input file they apply to."],["CAVEATS","Does not transcode (remux only). Part of MKVToolNix. Option order matters: per-file options must precede the input file they apply to. Track IDs are zero-indexed."],["HISTORY","mkvmerge is part of **MKVToolNix**, providing Matroska container manipulation tools."],["INSTALL","```apt: sudo apt install mkvtoolnix```\n\n```dnf: sudo dnf install mkvtoolnix```\n\n```apk: sudo apk add mkvtoolnix```\n\n```zypper: sudo zypper install mkvtoolnix```\n\n```brew: brew install mkvtoolnix```\n\n```nix: nix profile install nixpkgs#mkvtoolnix```"],["SEE ALSO","[ffmpeg](/man/ffmpeg)(1), [mkvpropedit](/man/mkvpropedit)(1)"]],"e":["ffmpeg","mkvpropedit"],"x":"mkvmerge -o [output.mkv] [input1.mp4] [input2.mp4]"},"mraptor":{"n":"mraptor","t":"detects malicious VBA macros in Microsoft Office documents","s":[["TAGLINE","detects malicious VBA macros in Microsoft Office documents"],["TLDR","**Scan document for macros**\n\n```mraptor [document.doc]```\n\n**Scan multiple files**\n\n```mraptor [*.doc]```\n\n**Scan directory recursively**\n\n```mraptor -r [directory/]```\n\n**Output as JSON**\n\n```mraptor -j [document.xlsm]```\n\n**Match specific patterns**\n\n```mraptor -m [pattern] [document.doc]```"],["SYNOPSIS","**mraptor** [_options_] _files_..."],["PARAMETERS","**-r**, **--recursive**\n> Scan directories recursively.\n\n**-j**, **--json**\n> JSON output.\n\n**-m** _pattern_\n> Match specific pattern.\n\n**-l** _level_\n> Log level.\n\n**-z** _password_\n> ZIP password."],["DESCRIPTION","**mraptor** (Macro Raptor) detects malicious VBA macros in Microsoft Office documents. It analyzes macro code to identify suspicious patterns typically found in malware.\n\nPart of the oletools suite, mraptor provides rapid triage of potentially dangerous documents."],["DETECTION FLAGS","```\nA - AutoExec (auto-run)\nW - Write file\nX - Execute\nS - Suspicious keywords\n```"],["RISK LEVELS","```\nSAFE      - No macros or no suspicious code\nLOW       - Macros but no execution\nMEDIUM    - Some suspicious patterns\nHIGH/VERY HIGH - Strong malware indicators\n```"],["CAVEATS","Heuristic-based; may have false positives. Cannot detect all malware. Part of oletools package."],["HISTORY","mraptor was developed by **Philippe Lagadec** as part of **oletools**, a collection of Python tools for analyzing Microsoft Office documents."],["INSTALL","```apk: sudo apk add py3-oletools```"],["SEE ALSO","[olevba](/man/olevba)(1), [oleid](/man/oleid)(1), [oletools](/man/oletools)(1), [yara](/man/yara)(1)"]],"e":["olevba","oleid","oletools","yara"],"x":"mraptor [document.doc]"},"mysqlbinlog":{"n":"mysqlbinlog","t":"displays MySQL binary log contents in readable format","s":[["TAGLINE","displays MySQL binary log contents in readable format"],["TLDR","**Read binary log**\n\n```mysqlbinlog [mysql-bin.000001]```\n\n**Read from specific position**\n\n```mysqlbinlog --start-position=[4] [mysql-bin.000001]```\n\n**Read events in time range**\n\n```mysqlbinlog --start-datetime=\"[2024-01-01 00:00:00]\" --stop-datetime=\"[2024-01-02 00:00:00]\" [mysql-bin.000001]```\n\n**Output specific database only**\n\n```mysqlbinlog --database=[mydb] [mysql-bin.000001]```"],["SYNOPSIS","**mysqlbinlog** [_options_] _log_file_..."],["PARAMETERS","**--start-position** _pos_\n> Start reading the binary log at the given byte position.\n\n**--stop-position** _pos_\n> Stop reading the binary log at the given byte position.\n\n**--start-datetime** _datetime_\n> Only show events with a timestamp equal to or later than the given datetime.\n\n**--stop-datetime** _datetime_\n> Stop reading at the first event with a timestamp equal to or later than the given datetime.\n\n**-d** _db_, **--database** _db_\n> Show only events belonging to the named database.\n\n**-r** _file_, **--result-file** _file_\n> Direct output to the specified file instead of stdout.\n\n**--base64-output** _mode_\n> Control base64 encoding of row-based events (AUTO, NEVER, DECODE-ROWS).\n\n**-v**, **--verbose**\n> Reconstruct row events as pseudo-SQL; repeat (**-vv**) to include column metadata comments.\n\n**-R**, **--read-from-remote-server**\n> Read binary log from a remote MySQL server rather than a local file.\n\n**-h** _host_, **--host** _host_\n> Connect to the given host (with **-R**).\n\n**-u** _user_, **--user** _user_\n> MySQL user name (with **-R**).\n\n**-p**, **--password**[=_pw_]\n> MySQL password (with **-R**).\n\n**--to-last-log**\n> With **-R**, continue reading through the last binary log on the server.\n\n**--disable-log-bin**\n> Write SET sql_log_bin=0 to the output so replayed statements are not re-logged."],["DESCRIPTION","**mysqlbinlog** reads MySQL binary (and relay) log files and displays their contents as SQL statements. It is commonly used for point-in-time recovery (by piping its output into **mysql**), replication debugging, and auditing changes.\n\nStatement-based events appear directly as SQL; row-based events are emitted as base64-encoded BINLOG statements, optionally decoded to pseudo-SQL via **--verbose**."],["CAVEATS","Output from row-based events is not directly executable SQL unless decoded appropriately. When used for recovery, pipe the output to **mysql** rather than saving and re-reading as separate statements, to preserve session state (e.g., temporary tables)."],["INSTALL","```dnf: sudo dnf install mariadb```\n\n```pacman: sudo pacman -S mariadb```\n\n```apk: sudo apk add mariadb```\n\n```zypper: sudo zypper install mariadb```\n\n```brew: brew install mariadb```\n\n```nix: nix profile install nixpkgs#mariadb```"],["SEE ALSO","[mysql](/man/mysql)(1), [mysqldump](/man/mysqldump)(1)"]],"e":["mysql","mysqldump"],"x":"mysqlbinlog [mysql-bin.000001]"},"ng-analytics":{"n":"ng-analytics","t":"manages Angular CLI usage analytics","s":[["TAGLINE","manages Angular CLI usage analytics"],["TLDR","**Enable analytics**\n\n```ng analytics enable```\n\n**Disable analytics**\n\n```ng analytics disable```\n\n**Check analytics status**\n\n```ng analytics info```\n\n**Prompt for analytics**\n\n```ng analytics prompt```"],["SYNOPSIS","**ng analytics** _command_ [_options_]"],["PARAMETERS","**enable**\n> Enable usage analytics.\n\n**disable**\n> Disable usage analytics.\n\n**info**\n> Show current analytics setting.\n\n**prompt**\n> Prompt to set analytics preference.\n\n**--global**\n> Apply globally."],["DESCRIPTION","**ng analytics** manages Angular CLI usage analytics. Control whether anonymous usage data is shared with the Angular team to improve the CLI."],["SEE ALSO","[ng](/man/ng)(1)"]],"e":["ng"],"x":"ng analytics enable"},"nixos-rebuild":{"n":"nixos-rebuild","t":"reconfigures a NixOS system by building a new configuration","s":[["TAGLINE","reconfigures a NixOS system by building a new configuration"],["TLDR","**Build and switch** to new configuration (make boot default)\n\n```sudo nixos-rebuild switch```\n\nBuild and switch with **custom profile name**\n\n```sudo nixos-rebuild switch -p [name]```\n\nBuild, switch, and **install updates**\n\n```sudo nixos-rebuild switch --upgrade```\n\n**Rollback** to previous generation\n\n```sudo nixos-rebuild switch --rollback```\n\nBuild and make **boot default** without activating\n\n```sudo nixos-rebuild boot```\n\nBuild and **test** without making boot entry\n\n```sudo nixos-rebuild test```\n\nBuild configuration and run in **virtual machine**\n\n```sudo nixos-rebuild build-vm```\n\n**List** available generations\n\n```nixos-rebuild list-generations```"],["SYNOPSIS","**nixos-rebuild** [**switch**|**boot**|**test**|**build**|**build-vm**] [**--upgrade**] [**--rollback**] [**-p** _name_]"],["COMMANDS","**switch**\n> Build, activate, and make boot default\n\n**boot**\n> Build and make boot default without activating\n\n**test**\n> Build and activate without making boot entry\n\n**build**\n> Build configuration without activating\n\n**build-vm**\n> Build and open configuration in virtual machine\n\n**dry-build**\n> Show what would be built\n\n**dry-activate**\n> Show what would change on activation\n\n**list-generations**\n> List available system generations"],["PARAMETERS","**--upgrade**\n> Update nixpkgs channel before building\n\n**--rollback**\n> Roll back to previous generation\n\n**-p, --profile-name _name_**\n> Name for the boot entry\n\n**--flake _uri_**\n> Build from flake reference\n\n**--no-build-nix**\n> Don't rebuild Nix during switch\n\n**--fast**\n> Skip rebuilding Nix and documentation\n\n**--show-trace**\n> Show detailed error traces"],["DESCRIPTION","**nixos-rebuild** reconfigures a NixOS system by building a new configuration from /etc/nixos/configuration.nix and optionally activating it. NixOS uses a declarative configuration model where the entire system is described in Nix expressions.\n\nEach build creates a new generation that can be selected at boot time. This allows safe rollback to previous working configurations if problems occur."],["CAVEATS","Requires root privileges for most operations. Configuration errors prevent build completion. Large changes may require significant download and build time. The switch command restarts services that changed."],["HISTORY","**nixos-rebuild** is part of **NixOS**, the Linux distribution based on the Nix package manager. NixOS was created by Eelco Dolstra and first released in 2003. It pioneered declarative system configuration and atomic upgrades with rollback capability."],["SEE ALSO","[nix](/man/nix)(1), [nix-env](/man/nix-env)(1), [nix-channel](/man/nix-channel)(1)"]],"e":["nix","nix-env","nix-channel"],"x":"sudo nixos-rebuild switch"},"nomino":{"n":"nomino","t":"Batch rename utility for developers","s":[["TAGLINE","Batch rename utility for developers"],["TLDR","**Rename files using a regex pattern**\n\n```nomino -r \"[regex]\" \"[output_pattern]\"```\n\n**Preview changes without renaming (test mode)**\n\n```nomino -t -r \"[regex]\" \"[output_pattern]\"```\n\n**Sort files and rename with ascending numbers**\n\n```nomino -s asc \"[output_pattern]\"```\n\n**Generate a JSON map of renames**\n\n```nomino -g [map.json] -r \"[regex]\" \"[output_pattern]\"```\n\n**Rename using a JSON map file**\n\n```nomino -m [map.json]```\n\n**Rename in a specific directory**\n\n```nomino -d [/path/to/dir] -r \"[regex]\" \"[output_pattern]\"```"],["SYNOPSIS","**nomino** [_options_] [[_source_] _output_]..."],["PARAMETERS","**-r**, **--regex** _PATTERN_\n> Regex pattern to match filenames.\n\n**-s**, **--sort** _ORDER_\n> Sort files naturally by name (asc or desc) and rename using enumerator.\n\n**-m**, **--map** _PATH_\n> Use a JSON map file for renaming files.\n\n**-g**, **--generate** _PATH_\n> Store a JSON map file after renaming.\n\n**-d**, **--dir** _PATH_\n> Set the working directory.\n\n**-E**, **--no-extension**\n> Do not preserve file extensions in sort and regex modes.\n\n**-k**, **--mkdir**\n> Recursively create parent directories of output if missing.\n\n**-p**, **--print**\n> Print the map table to stdout.\n\n**-q**, **--quiet**\n> Do not print the map table to stdout.\n\n**-t**, **--test**\n> Run in test mode without renaming actual files.\n\n**-w**, **--overwrite**\n> Overwrite output files. Otherwise a '_' is prepended to the filename.\n\n**--depth** _DEPTH_\n> Override inferred subdirectory depth in regex mode.\n\n**--max-depth** _DEPTH_\n> Set maximum subdirectory depth in regex mode."],["DESCRIPTION","**nomino** is a batch rename utility for developers. It supports regex-based pattern matching, natural sorting, JSON map generation, and directory creation for documenting and executing rename operations.\n\nFile extensions are preserved by default in sort and regex modes unless **-E** is used."],["CAVEATS","Always preview changes with **-t** (test mode) before executing. File extensions are preserved by default in sort and regex modes. Without **-w**, existing output files are not overwritten; a '_' is prepended instead."],["HISTORY","**nomino** was created by **yaa110** and is written in **Rust**."],["INSTALL","```zypper: sudo zypper install nomino```\n\n```brew: brew install nomino```\n\n```nix: nix profile install nixpkgs#nomino```"],["SEE ALSO","[rename](/man/rename)(1), [mv](/man/mv)(1), [mmv](/man/mmv)(1)"]],"e":["rename","mv","mmv"],"x":"nomino -r \"[regex]\" \"[output_pattern]\""},"p10k":{"n":"p10k","t":"powerlevel10k configuration tool","s":[["TAGLINE","powerlevel10k configuration tool"],["TLDR","**Run configuration wizard**\n\n```p10k configure```\n\n**Reload configuration without restarting shell**\n\n```p10k reload```\n\n**Display help information**\n\n```p10k help```"],["SYNOPSIS","**p10k** [_command_]"],["PARAMETERS","**configure**\n> Run interactive setup wizard to customize prompt appearance.\n\n**reload**\n> Reload Powerlevel10k configuration without restarting the shell.\n\n**help**\n> Display help information."],["DESCRIPTION","**p10k** is the configuration tool for Powerlevel10k, a fast and flexible Zsh prompt theme.\n\nThe **configure** command launches an interactive wizard to customize prompt style, icons, and segments. The **reload** command applies changes made to **~/.p10k.zsh** without restarting the shell. Powerlevel10k supports hot reload of POWERLEVEL9K options after initialization."],["CAVEATS","Requires Powerlevel10k. Zsh only. Nerd Fonts recommended."],["HISTORY","p10k is part of **Powerlevel10k**, a fast Zsh prompt theme."],["SEE ALSO","[zsh](/man/zsh)(1), [oh-my-zsh](/man/oh-my-zsh)(1), [oh-my-posh](/man/oh-my-posh)(1), [starship](/man/starship)(1)"]],"e":["zsh","oh-my-zsh","oh-my-posh","starship"],"x":"p10k configure"},"pdm":{"n":"pdm","t":"python package manager following PEP standards","s":[["TAGLINE","python package manager following PEP standards"],["TLDR","**Initialize a new project**\n\n```pdm init```\n\n**Add a dependency**\n\n```pdm add [requests]```\n\n**Add a development dependency**\n\n```pdm add -d [pytest]```\n\n**Install all dependencies**\n\n```pdm install```\n\n**Update dependencies**\n\n```pdm update```\n\n**Run a command in environment**\n\n```pdm run [python] [script.py]```\n\n**List installed packages**\n\n```pdm list```\n\n**Build package**\n\n```pdm build```"],["SYNOPSIS","**pdm** _command_ [_options_] [_args_...]"],["COMMANDS","**init**\n> Create new project with pyproject.toml.\n\n**add** _packages_\n> Add dependencies.\n\n**remove** _packages_\n> Remove dependencies.\n\n**install**\n> Install dependencies from lock file.\n\n**update** [_packages_]\n> Update dependencies.\n\n**lock**\n> Generate or update lock file.\n\n**sync**\n> Sync packages with lock file.\n\n**run** _command_\n> Run command in project environment.\n\n**list**\n> List installed packages.\n\n**build**\n> Build distribution packages.\n\n**publish**\n> Publish to PyPI.\n\n**use** _python_\n> Switch the project's Python interpreter.\n\n**venv** create|list|remove|activate\n> Manage virtual environments for the project.\n\n**info**\n> Show project, environment, and Python interpreter information.\n\n**cache** clear|list|info\n> Inspect or clear the package cache.\n\n**config** _key_ [_value_]\n> Get or set a PDM configuration option."],["PARAMETERS","**-d**, **--dev**\n> Development dependency.\n\n**-G**, **--group** _name_\n> Dependency group.\n\n**-L**, **--lockfile** _file_\n> Custom lock file path.\n\n**--no-sync**\n> Don't sync after adding.\n\n**--no-lock**\n> Skip lock file update.\n\n**--prod**, **--production**\n> Exclude dev dependencies.\n\n**-p**, **--project** _path_\n> Project directory.\n\n**-v**, **--verbose**\n> Increase verbosity.\n\n**-q**, **--quiet**\n> Suppress output."],["DESCRIPTION","**pdm** is a Python package manager following PEP standards. It manages dependencies, virtual environments, and builds using pyproject.toml.\n\n**pdm init** creates a new project with pyproject.toml. It detects or creates a Python interpreter and optionally initializes a virtual environment.\n\nDependencies are declared in pyproject.toml and locked in pdm.lock. **add** modifies both; **install** reads the lock file. Dependency groups separate dev, test, and optional dependencies.\n\nPDM historically supported PEP 582 (__pypackages__) as an alternative to virtual environments, but PEP 582 was rejected in 2023; modern PDM defaults to virtualenvs. Local-package mode can still be enabled with **pdm config python.use_venv false** for users who prefer it.\n\n**pdm run** executes commands with the project environment active. Scripts defined in pyproject.toml under [tool.pdm.scripts] provide shortcuts.\n\nBuild and publish follow PEP 517/518. **pdm build** creates wheels and sdists; **pdm publish** uploads to PyPI."],["CAVEATS","PEP 582 support varies by Python version and tools. Some older packages may not work with new standards. Lock file format is PDM-specific. Requires Python 3.8+."],["HISTORY","PDM was created by **Frost Ming** and first released in **2019**. It pioneered PEP 582 support and modern Python packaging standards. The project emphasizes standards compliance (PEP 517, 518, 621) over custom formats. PDM gained popularity as an alternative to pip, Poetry, and pipenv with its focus on PEP standards and performance."],["INSTALL","```brew: brew install pdm```\n\n```nix: nix profile install nixpkgs#pdm```"],["SEE ALSO","[pip](/man/pip)(1), [poetry](/man/poetry)(1), [pipenv](/man/pipenv)(1), [uv](/man/uv)(1)"]],"e":["pip","poetry","pipenv","uv"],"x":"pdm init"},"phpdoc":{"n":"phpdoc","t":"generates API documentation from PHP source code","s":[["TAGLINE","generates API documentation from PHP source code"],["TLDR","**Generate documentation**\n\n```phpdoc -d [src/] -t [docs/]```\n\n**Specify config file**\n\n```phpdoc -c [phpdoc.xml]```\n\n**Generate with specific template**\n\n```phpdoc -d [src/] -t [docs/] --template=\"clean\"```\n\n**Parse single file**\n\n```phpdoc -f [file.php] -t [docs/]```"],["SYNOPSIS","**phpdoc** [_options_]"],["PARAMETERS","**-d**, **--directory** _path_\n> Source directory.\n\n**-f**, **--filename** _file_\n> Single file to parse.\n\n**-t**, **--target** _path_\n> Output directory.\n\n**-c**, **--config** _file_\n> Configuration file.\n\n**--template** _name_\n> Template to use.\n\n**--ignore** _pattern_\n> Patterns to ignore (supports globs, e.g., `\"**/*test.php\"`).\n\n**--visibility** _level_\n> Filter by visibility: public, protected, private, api, or internal.\n\n**--title** _text_\n> Set browser and template title.\n\n**--encoding** _encoding_\n> Set file encoding (default: UTF-8)."],["DESCRIPTION","**phpDocumentor** (phpdoc) generates API documentation from PHP source code. It parses DocBlock comments to create navigable HTML documentation. The default command is `phpdoc run`, which can also be invoked simply as `phpdoc`. It supports configuration files (phpdoc.xml or phpdoc.dist.xml) for persistent settings and multiple output templates."],["DOCBLOCK FORMAT","```php\n/**\n * Short description.\n *\n * Long description.\n *\n * @param string $name The name\n * @return bool Success status\n * @throws Exception On failure\n */\n```"],["CONFIGURATION (phpdoc.xml)","```xml\n<?xml version=\"1.0\"?>\n<phpdocumentor>\n    <paths>\n        <output>docs</output>\n    </paths>\n    <source>\n        <path>src</path>\n    </source>\n</phpdocumentor>\n```"],["CAVEATS","Requires proper DocBlocks. Large projects take time to process. Template quality varies."],["HISTORY","phpDocumentor was created by **Joshua Eichorn** and is now maintained by **Mike van Riel** and the community."],["SEE ALSO","[doxygen](/man/doxygen)(1), [php](/man/php)(1), [composer](/man/composer)(1), [phpunit](/man/phpunit)(1)"]],"e":["doxygen","php","composer","phpunit"],"x":"phpdoc -d [src/] -t [docs/]"},"pixi-global":{"n":"pixi-global","t":"Manage globally installed Pixi packages","s":[["TAGLINE","Manage globally installed Pixi packages"],["TLDR","**Install package globally**\n\n```pixi global install [package]```\n\n**List globally installed packages**\n\n```pixi global list```\n\n**Remove global package**\n\n```pixi global remove [package]```\n\n**Update all global environments**\n\n```pixi global update```\n\n**Update a specific** global environment\n\n```pixi global update [package]```\n\n**Sync** manifest with installed environments\n\n```pixi global sync```\n\n**Edit** the global manifest file\n\n```pixi global edit```"],["SYNOPSIS","**pixi** **global** _command_ [_options_]"],["PARAMETERS","**install** _package_\n> Install a package in a globally accessible location and expose its executables.\n\n**uninstall** _environment_\n> Uninstall a global environment.\n\n**list**\n> List global environments with their dependencies and exposed commands.\n\n**add** _package_\n> Add dependencies to a global environment.\n\n**remove** _package_\n> Remove dependencies from a global environment.\n\n**update** [_environment..._]\n> Update global environments. Without arguments, updates all environments.\n\n**sync**\n> Synchronize the global manifest with installed environments.\n\n**edit**\n> Open the global manifest file in your editor.\n\n**expose**\n> Manage exposure of binaries in global environments.\n\n**shortcut**\n> Manage shortcuts on your machine for global environments.\n\n**tree**\n> Show a dependency tree for a specific global environment."],["DESCRIPTION","**pixi global** manages globally installed packages. Installs command-line tools accessible from anywhere, similar to pipx for Python. Packages are isolated in their own environments."],["INSTALL","```pacman: sudo pacman -S pixi```\n\n```brew: brew install pixi```\n\n```nix: nix profile install nixpkgs#pixi```"],["SEE ALSO","[pixi](/man/pixi)(1), [pipx](/man/pipx)(1), [conda](/man/conda)(1)"]],"e":["pixi","pipx","conda"],"x":"pixi global install [package]"},"pl2pm":{"n":"pl2pm","t":"Convert Perl4 libraries to Perl5 modules","s":[["TAGLINE","Convert Perl4 libraries to Perl5 modules"],["TLDR","**Convert Perl4 library to module**\n\n```pl2pm [library.pl] > [Module.pm]```\n\n**Convert multiple files**\n\n```pl2pm [lib1.pl] [lib2.pl]```"],["SYNOPSIS","**pl2pm** [_file_...]"],["PARAMETERS","_FILE_\n> Perl4 library file(s).\n\nOutput goes to stdout."],["DESCRIPTION","**pl2pm** converts Perl 4 library files (.pl) to Perl 5 module format (.pm). It performs basic syntactic transformations such as adding package declarations, converting require statements, and updating subroutine definitions to follow Perl 5 module conventions.\n\nPart of the standard Perl distribution, the conversion is approximate and the output typically requires manual review and adjustment to produce fully functional Perl 5 modules."],["CAVEATS","Part of Perl. Basic conversion. Manual review needed."],["HISTORY","pl2pm was created for **migrating Perl4 code** to Perl5 modules."],["INSTALL","```apt: sudo apt install perl```\n\n```dnf: sudo dnf install perl```\n\n```pacman: sudo pacman -S perl```\n\n```apk: sudo apk add perl-utils```\n\n```zypper: sudo zypper install perl```\n\n```brew: brew install perl```\n\n```nix: nix profile install nixpkgs#perl```"],["SEE ALSO","[perl](/man/perl)(1), [h2xs](/man/h2xs)(1)"]],"e":["perl","h2xs"],"x":"pl2pm [library.pl] > [Module.pm]"},"pnmcrop":{"n":"pnmcrop","t":"Automatically crop borders from PNM images","s":[["TAGLINE","Automatically crop borders from PNM images"],["TLDR","**Crop borders from image**\n\n```pnmcrop [input.pnm] > [output.pnm]```\n\n**Crop only white borders**\n\n```pnmcrop -white [input.pnm] > [output.pnm]```\n\n**Crop only black borders**\n\n```pnmcrop -black [input.pnm] > [output.pnm]```\n\n**Crop specific sides**\n\n```pnmcrop -left -right [input.pnm] > [output.pnm]```"],["SYNOPSIS","**pnmcrop** [_options_] [_file_]"],["PARAMETERS","_FILE_\n> Input PNM file.\n\n**-white**\n> Crop white borders.\n\n**-black**\n> Crop black borders.\n\n**-left**\n> Crop left side only.\n\n**-right**\n> Crop right side only.\n\n**-top**\n> Crop top only.\n\n**-bottom**\n> Crop bottom only.\n\n**-verbose**\n> Show cropping information."],["DESCRIPTION","**pnmcrop** automatically detects and removes uniform-color borders from PNM images. It identifies rows and columns at the edges that consist entirely of the border color and trims them.\n\nBy default it detects the border color automatically, but **-white** and **-black** force specific border colors. Individual sides can be cropped selectively with **-left**, **-right**, **-top**, and **-bottom** flags. Part of the Netpbm toolkit."],["CAVEATS","Border color must be uniform. Part of Netpbm suite."],["HISTORY","pnmcrop was created as part of **Netpbm** for automatic image border removal."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pnmcut](/man/pnmcut)(1), [pnmpad](/man/pnmpad)(1), [pamcut](/man/pamcut)(1)"]],"e":["pnmcut","pnmpad","pamcut"],"x":"pnmcrop [input.pnm] > [output.pnm]"},"pod2text":{"n":"pod2text","t":"Convert Perl POD documentation to plain text","s":[["TAGLINE","Convert Perl POD documentation to plain text"],["TLDR","**Convert POD to plain text**\n\n```pod2text [file.pod]```\n\n**Output to file**\n\n```pod2text [file.pod] > [output.txt]```\n\n**Specify width**\n\n```pod2text -w [80] [file.pod]```\n\n**Loose formatting**\n\n```pod2text -l [file.pod]```"],["SYNOPSIS","**pod2text** [_options_] [_file_]"],["PARAMETERS","_FILE_\n> Input POD file.\n\n**-w** _WIDTH_\n> Output width in columns.\n\n**-l**, **--loose**\n> Loose paragraph formatting.\n\n**-q** _QUOTES_\n> Quote characters.\n\n**-c**, **--color**\n> Use ANSI colors."],["DESCRIPTION","**pod2text** converts Perl POD (Plain Old Documentation) into formatted plain text suitable for terminal display. It renders headings, lists, code blocks, and other POD formatting into readable ASCII output.\n\nThe **-w** option sets the output width in columns, **-l** enables loose paragraph formatting with extra spacing, and **-c** enables ANSI color output for enhanced terminal readability. Part of the standard Perl distribution."],["CAVEATS","Perl-specific format. Part of standard Perl."],["HISTORY","pod2text was created for **terminal viewing** of Perl documentation."],["INSTALL","```apt: sudo apt install perl```\n\n```dnf: sudo dnf install perl```\n\n```pacman: sudo pacman -S perl```\n\n```apk: sudo apk add perl```\n\n```zypper: sudo zypper install perl```\n\n```brew: brew install perl```\n\n```nix: nix profile install nixpkgs#perl```"],["SEE ALSO","[pod2man](/man/pod2man)(1), [pod2html](/man/pod2html)(1), [perldoc](/man/perldoc)(1)"]],"e":["pod2man","pod2html","perldoc"],"x":"pod2text [file.pod]"},"ppmdim":{"n":"ppmdim","t":"Dim PPM image by brightness factor","s":[["TAGLINE","Dim PPM image by brightness factor"],["TLDR","**Dim image**\n\n```ppmdim [factor] [input.ppm] > [output.ppm]```\n\n**Dim by 50%**\n\n```ppmdim 0.5 [input.ppm] > [output.ppm]```\n\n**Dim by 25%**\n\n```ppmdim 0.25 [input.ppm] > [output.ppm]```"],["SYNOPSIS","**ppmdim** _factor_ [_file_]"],["PARAMETERS","_FACTOR_\n> Dimming factor (0.0-1.0).\n\n_FILE_\n> Input PPM file."],["DESCRIPTION","**ppmdim** reduces the brightness of a PPM image by multiplying each pixel value by the specified factor. A factor of 1.0 leaves the image unchanged, while 0.0 produces a completely black image. Reads from stdin if no file is specified.\n\nThis is the complement of **ppmflash**, which brightens toward white. Both tools are useful for creating fade-to-black or fade-to-white animation effects when applied with varying factors across a sequence of frames. Part of the Netpbm toolkit.\n\nThis program is largely superseded by **pamfunc** (with the -multiplier option), which is more general. ppmdim remains available for backward compatibility and may be faster due to its use of integer arithmetic."],["CAVEATS","Factor 1.0 is no change, 0.0 is black. Only works with PPM format images. Part of Netpbm suite."],["HISTORY","ppmdim was created as part of **Netpbm** for image dimming."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[ppmflash](/man/ppmflash)(1), [pamfunc](/man/pamfunc)(1), [ppmbrighten](/man/ppmbrighten)(1), [ppm](/man/ppm)(5)"]],"e":["ppmflash","pamfunc","ppmbrighten","ppm"],"x":"ppmdim [factor] [input.ppm] > [output.ppm]"},"ppmtorgb3":{"n":"ppmtorgb3","t":"Split PPM into separate RGB channel files","s":[["TAGLINE","Split PPM into separate RGB channel files"],["TLDR","**Split** a PPM image into three PGM component files\n\n```ppmtorgb3 [input.ppm]```\n\n**Read from stdin** (produces noname.red/.grn/.blu)\n\n```cat [input.ppm] | ppmtorgb3```"],["SYNOPSIS","**ppmtorgb3** [_ppmfile_]"],["DESCRIPTION","**ppmtorgb3** reads a PPM (Portable Pixmap) image and writes three PGM (Portable Graymap) images as output: one each for the red, green, and blue channels.\n\nOutput filenames are derived from the input filename by stripping any extension and appending **.red**, **.grn**, and **.blu**. For example, separating **lenna.ppm** produces **lenna.red**, **lenna.grn**, and **lenna.blu**. When input comes from stdin, the names default to **noname.red**, **noname.grn**, and **noname.blu**.\n\nThere are no options specific to **ppmtorgb3**, but it accepts the common options shared by all programs based on libnetpbm."],["HISTORY","Written by **Jef Poskanzer** in 1991 as part of **Netpbm** (originally **Pbmplus**), a classic toolkit of image-processing utilities that uses simple PBM/PGM/PPM formats as intermediate pipelines."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[rgb3toppm](/man/rgb3toppm)(1), [ppmtopgm](/man/ppmtopgm)(1), [pgmtoppm](/man/pgmtoppm)(1), [ppmtoyuvsplit](/man/ppmtoyuvsplit)(1), [ppm](/man/ppm)(5), [pgm](/man/pgm)(5)"]],"e":["rgb3toppm","ppmtopgm","pgmtoppm","ppmtoyuvsplit","ppm","pgm"],"x":"ppmtorgb3 [input.ppm]"},"pspg":{"n":"pspg","t":"Unix pager designed for tables and PostgreSQL output","s":[["TAGLINE","Unix pager designed for tables and PostgreSQL output"],["TLDR","**Use as the pager for psql**\n\n```export PAGER=pspg```\n\n**View a CSV file**\n\n```pspg --csv < [file.csv]```\n\n**View a TSV file**\n\n```pspg --tsv < [file.tsv]```\n\n**Open a file directly**\n\n```pspg -f [file.txt]```\n\n**Set color style**\n\n```pspg -s [5] < [file.csv]```\n\n**Freeze first N columns while scrolling**\n\n```pspg --csv -c [2] < [file.csv]```\n\n**Exit if content fits on one screen**\n\n```pspg -F < [file.txt]```\n\n**Use with psql in streaming mode**\n\n```psql -c \"SELECT * FROM table\" | pspg --stream```"],["SYNOPSIS","**pspg** [_options_] [**-f** _file_]"],["PARAMETERS","**-f**, **--file** _FILE_\n> Open specified file.\n\n**-F**, **--quit-if-one-screen**\n> Exit if content fits on one screen.\n\n**--csv**\n> Read input as CSV format.\n\n**--csv-separator** _CHAR_\n> Specify CSV field separator character.\n\n**--csv-header** _on|off_\n> Control header line usage for CSV input.\n\n**--tsv**\n> Read input as TSV format.\n\n**--stream**\n> Read input continuously in streaming mode.\n\n**--querystream**\n> Read a stream of queries from pipe or file.\n\n**-c**, **--freezecols** _N_\n> Freeze first N columns (0-9).\n\n**-s**, **--style** _N_\n> Set color style (0-22).\n\n**-b**, **--blackwhite**\n> Use black and white color style.\n\n**-a**, **--ascii**\n> Force ASCII characters for borders.\n\n**-i**, **--ignore-case**\n> Ignore case in searches without uppercase characters.\n\n**-I**, **--IGNORE-CASE**\n> Ignore case in all searches.\n\n**-g**, **--hlite-search**\n> Don't highlight search results.\n\n**-G**, **--HILITE-SEARCH**\n> Don't highlight search result lines.\n\n**--line-numbers**\n> Show line number column.\n\n**--no-mouse**\n> Disable custom mouse handling.\n\n**--no-bars**\n> Hide bottom and top bars.\n\n**--no-cursor**\n> Hide row cursor.\n\n**--no-scrollbar**\n> Hide scrollbar.\n\n**--no-sound**\n> Disable beep on scroll limit.\n\n**--vertical-cursor**\n> Display vertical column cursor.\n\n**--bold-labels**\n> Make row and column labels bold.\n\n**--force-uniborder**\n> Replace ASCII borders with Unicode borders.\n\n**--only-for-tables**\n> Use standard pager when content is not a table.\n\n**--on-sigint-exit**\n> Exit on Ctrl+C or Escape.\n\n**-X**, **--reprint-on-exit**\n> Preserve content in terminal after exit.\n\n**--ni**\n> Non-interactive mode (CSV and query only).\n\n**-q**, **--query** _QUERY_\n> Execute specified query.\n\n**-w**, **--watch** _N_\n> Repeat query or file read every N seconds.\n\n**-d**, **--dbname** _NAME_\n> Specify database name for direct queries.\n\n**-h**, **--host** _HOST_\n> Database server host.\n\n**-p**, **--port** _PORT_\n> Database server port.\n\n**-U**, **--username** _USER_\n> Database user name.\n\n**-V**, **--version**\n> Show version information.\n\n**--help**\n> Show help message."],["DESCRIPTION","**pspg** is a Unix pager with rich functionality designed for working with tables. Originally created for PostgreSQL's psql, it also supports MySQL and works as a general CSV/TSV viewer. It features frozen column headers, searching, row/column/block selection, clipboard export, and mouse support.\n\nKey features include freezable columns with **-c**, multiple color styles selectable with **-s**, vertical and horizontal scrolling, and the ability to directly query databases with **-q**. In streaming mode (**--stream**), it can display continuously updating output such as psql's **\\watch** results."],["HISTORY","**pspg** was created by **Pavel Stehule** (okbob) and is written in **C**."],["INSTALL","```apt: sudo apt install pspg```\n\n```dnf: sudo dnf install pspg```\n\n```apk: sudo apk add pspg```\n\n```zypper: sudo zypper install pspg```\n\n```brew: brew install pspg```\n\n```nix: nix profile install nixpkgs#pspg```"],["SEE ALSO","[less](/man/less)(1), [psql](/man/psql)(1), [mysql](/man/mysql)(1), [ov](/man/ov)(1)"]],"e":["less","psql","mysql","ov"],"x":"export PAGER=pspg"},"pulumi-about":{"n":"pulumi-about","t":"Display Pulumi environment diagnostics","s":[["TAGLINE","Display Pulumi environment diagnostics"],["TLDR","**Show Pulumi version and environment info**\n\n```pulumi about```\n\n**Show output as JSON**\n\n```pulumi about --json```\n\n**Include transitive plugin dependencies**\n\n```pulumi about --transitive```\n\n**Show info for a specific stack**\n\n```pulumi about --stack [org/project/dev]```"],["SYNOPSIS","**pulumi** **about** [_options_]"],["PARAMETERS","**-j**, **--json**\n> Emit output as JSON, suitable for scripting.\n\n**-t**, **--transitive**\n> Include transitive language plugin dependencies in the output.\n\n**-s**, **--stack** _name_\n> Run against a specific stack (defaults to the currently selected stack)."],["DESCRIPTION","**pulumi about** displays diagnostic information about the local Pulumi environment. Output covers the Pulumi CLI version, the host OS and architecture, the active backend (Pulumi Cloud, S3, Azure Blob, etc.), the current user, and the language runtime plus any installed resource plugins.\n\nWhen run inside a Pulumi project, it also reports the project name, runtime, and the currently selected stack. The information is primarily intended for support tickets and CI debugging — Pulumi staff frequently ask for the output when triaging bug reports."],["CAVEATS","The output includes paths and stack identifiers; redact sensitive values before sharing publicly. Some fields are only populated inside a Pulumi project directory."],["INSTALL","```pacman: sudo pacman -S pulumi```\n\n```apk: sudo apk add pulumi```\n\n```brew: brew install pulumi```\n\n```nix: nix profile install nixpkgs#pulumi```"],["SEE ALSO","[pulumi](/man/pulumi)(1), [pulumi-version](/man/pulumi-version)(1), [pulumi-stack](/man/pulumi-stack)(1)"]],"e":["pulumi","pulumi-version","pulumi-stack"],"x":"pulumi about"},"pwdump":{"n":"pwdump","t":"Extract Windows password hashes for auditing","s":[["TAGLINE","Extract Windows password hashes for auditing"],["TLDR","**Dump Windows password hashes**\n\n```pwdump [system] [sam]```\n\n**Dump from local SAM**\n\n```pwdump localhost```\n\n**Output to file**\n\n```pwdump [target] > [hashes.txt]```\n\n**With specific user**\n\n```pwdump [target] -u [administrator] -p [password]```"],["SYNOPSIS","**pwdump** [_options_] _target_"],["DESCRIPTION","**pwdump** extracts Windows password hashes from the SAM database. These hashes can then be cracked offline. Used for security auditing and penetration testing.\n\nSeveral versions exist (pwdump7, fgdump, etc.) with varying capabilities."],["EXAMPLES","```bash"],["Dump local hashes","pwdump localhost"],["Remote system (admin required)","pwdump \\\\\\\\192.168.1.100"],["Save for cracking","pwdump localhost > hashes.txt"],["Then crack with John","john --format=NT hashes.txt\n```"],["OUTPUT FORMAT","```\nUsername:RID:LM_Hash:NT_Hash:::\n```"],["HASH TYPES","```\nLM Hash   - Legacy, weak (pre-Vista)\nNT Hash   - NTLM hash (current)\n```"],["CAVEATS","Requires administrator privileges. Windows-specific. Use only on systems you own or have authorization to test. May be detected by antivirus."],["HISTORY","pwdump was developed for Windows password auditing. Multiple versions (pwdump2-7, fgdump) have been created by various security researchers."],["SEE ALSO","[john](/man/john)(1), [hashcat](/man/hashcat)(1)"]],"e":["john","hashcat"],"x":"pwdump [system] [sam]"},"quickget":{"n":"quickget","t":"Download OS images for Quickemu VMs","s":[["TAGLINE","Download OS images for Quickemu VMs"],["TLDR","**List** all supported operating systems\n\n```quickget --list```\n\nList in **CSV/JSON** format\n\n```quickget --list-csv```\n\n**Download** and create VM config\n\n```quickget [os] [release] [edition]```\n\nDownload **Windows 11**\n\n```quickget windows 11```\n\nDownload **macOS**\n\n```quickget macos [sonoma]```\n\nShow **ISO URL**\n\n```quickget --url [os] [release]```\n\n**Check** if ISO available\n\n```quickget --check [os] [release]```\n\n**Download only** without config\n\n```quickget --download [os] [release]```"],["SYNOPSIS","**quickget** [**--list**|**--url**|**--check**|**--download**] [_os_] [_release_] [_edition_]"],["PARAMETERS","**--list, --list-csv, --list-json**\n> List supported operating systems\n\n**--url _os_ _release_**\n> Show ISO download URL\n\n**--check _os_ _release_**\n> Check if ISO is available\n\n**--download _os_ _release_**\n> Download without creating config\n\n**--create-config _os_ _iso_**\n> Create config from existing ISO\n\n**os**\n> Operating system name\n\n**release**\n> Version/release number\n\n**edition**\n> Edition variant (optional)"],["DESCRIPTION","**quickget** downloads operating system images and creates Quickemu configuration files. It supports hundreds of operating systems including Linux distributions, Windows, macOS, and BSD variants.\n\nThe tool automatically selects appropriate download mirrors and creates optimized VM configurations based on the OS requirements."],["CAVEATS","Large downloads may take significant time. Some OS editions require specific release versions. macOS downloads are recovery images. Windows needs virtio drivers."],["HISTORY","**quickget** is the companion tool to **quickemu**, developed by **Martin Wimpress**. It simplifies the process of obtaining and preparing VM images for various operating systems."],["INSTALL","```apt: sudo apt install quickemu```\n\n```dnf: sudo dnf install quickemu```\n\n```nix: nix profile install nixpkgs#quickemu```"],["SEE ALSO","[quickemu](/man/quickemu)(1), [wget](/man/wget)(1), [aria2c](/man/aria2c)(1)"]],"e":["quickemu","wget","aria2c"],"x":"quickget --list"},"rpi-eeprom-update":{"n":"rpi-eeprom-update","t":"Update Raspberry Pi bootloader EEPROM firmware","s":[["TAGLINE","Update Raspberry Pi bootloader EEPROM firmware"],["TLDR","Print **current EEPROM** version and update status\n\n```sudo rpi-eeprom-update```\n\n**Apply** update if newer firmware is available\n\n```sudo rpi-eeprom-update -a```\n\n**Cancel** a pending update\n\n```sudo rpi-eeprom-update -r```\n\nUpdate using a specific **firmware directory**\n\n```sudo rpi-eeprom-update -d [/lib/firmware/raspberrypi/bootloader/stable]```\n\nDisplay **help**\n\n```rpi-eeprom-update -h```"],["SYNOPSIS","**rpi-eeprom-update** [_options_]"],["PARAMETERS","**-a**\n> Apply update if newer version is available. The update is installed at next reboot.\n\n**-d** _directory_\n> Use the specified firmware image directory instead of the default.\n\n**-j** _file_\n> Write output in JSON format to file (use /dev/stdout for stdout).\n\n**-l**\n> Display the update status without checking for newer firmware.\n\n**-m**\n> Display information about the recovery.bin bootloader update file.\n\n**-r**\n> Cancel a pending update and revert to the current EEPROM contents.\n\n**-h**\n> Display help."],["DESCRIPTION","**rpi-eeprom-update** manages the bootloader EEPROM firmware on Raspberry Pi 4, 400, Compute Module 4, and Pi 5 models. The EEPROM contains the boot code that initializes the system before the operating system loads, including USB boot, network boot, and NVMe boot support.\n\nFirmware images are provided in **/lib/firmware/raspberrypi/bootloader/** under release channels: **critical** (rarely updated, highest stability), **stable** (default, recommended), **beta** (new features, less tested), and **latest** (most recent).\n\nUpdates improve boot reliability, add features like new boot modes, and fix bugs in the low-level boot firmware."],["CAVEATS","EEPROM updates are applied at next reboot. A power failure during the update process may require SD card recovery using recovery.bin. Only use official update sources. The update channel can be changed by editing **/etc/default/rpi-eeprom-update**."],["HISTORY","Introduced for **Raspberry Pi 4** (2019) which uses EEPROM-based boot instead of the SD card boot used in earlier models. Extended to support the Raspberry Pi 5 and Compute Module variants."],["SEE ALSO","[raspi-config](/man/raspi-config)(1), [vcgencmd](/man/vcgencmd)(1), [rpi-otp-private-key](/man/rpi-otp-private-key)(1)"]],"e":["raspi-config","vcgencmd","rpi-otp-private-key"],"x":"sudo rpi-eeprom-update"},"sa":{"n":"sa","t":"Summarize system process accounting data","s":[["TAGLINE","Summarize system process accounting data"],["TLDR","Display **executable invocations** per user\n\n```sudo sa```\n\nDisplay invocations with **usernames**\n\n```sudo sa --print-users```\n\nList **resources used** per user\n\n```sudo sa --user-summary```"],["SYNOPSIS","**sa** [_options_] [_file_]"],["PARAMETERS","**--print-users**\n> Show responsible usernames\n\n**--user-summary**\n> Display per-user resource summary"],["DESCRIPTION","**sa** summarizes accounting information about commands executed by users. It reports CPU time spent, I/O operations, and other resource usage from process accounting data.\n\nPart of the acct package, it requires process accounting to be enabled for data collection."],["CAVEATS","Requires process accounting to be enabled (accton). Historical data depends on accounting file rotation."],["HISTORY","Part of **acct** (process accounting) package, providing system-wide command usage statistics."],["INSTALL","```apt: sudo apt install acct```\n\n```apk: sudo apk add acct```\n\n```zypper: sudo zypper install acct```\n\n```nix: nix profile install nixpkgs#acct```"],["SEE ALSO","[lastcomm](/man/lastcomm)(1), [accton](/man/accton)(8), [ac](/man/ac)(1)"]],"e":["lastcomm","accton","ac"],"x":"sudo sa"},"skillfile":{"n":"skillfile","t":"CLI for the Skillscript agent workflow runtime","s":[["TAGLINE","CLI for the Skillscript agent workflow runtime"],["TLDR","**Install** runtime, **init** project, open **dashboard**\n\n```npm install -g skillscript-runtime\nskillfile init\nskillfile dashboard```\n\n**Lint**, **compile**, and **execute** a skill\n\n```skillfile lint [skill]\nskillfile compile [skill]\nskillfile execute [skill]```\n\n**Approve** a draft skill (secured mode)\n\n```skillfile approve [hello]```\n\nShow **health**, **diagram**, or **replay** a trace\n\n```skillfile health\nskillfile diagram [skill]\nskillfile replay [trace_id]```"],["SYNOPSIS","**skillfile** _command_ [_options_] [_args_]"],["DESCRIPTION","**skillfile** is the command-line interface for **Skillscript**, a constrained declarative language and runtime for agent-authored, human-approved automation. Skills look like Make-inspired workflows with variables, connectors, and allowlists—without unrestricted shell or arbitrary package installs.\n\nThe runtime (**skillscript-runtime** on npm) executes skills via configured connectors, filesystem and shell allowlists, and optional secured mode with operator signatures. Agents typically author skills over MCP (**http://localhost:7878/rpc** when the dashboard is up); operators review, approve, and run them with **skillfile**."],["PARAMETERS","**init**\n> Initialize a local Skillscript project\n\n**dashboard** [**--host**] [**--port**]\n> Start the operator UI and MCP endpoint (default port **7878**)\n\n**lint** / **compile** / **execute** _skill_\n> Validate, compile, or run a skill\n\n**approve** _skill_\n> Approve a draft skill for execution\n\n**diagram** / **fires** / **replay** / **health**\n> Visualize, inspect triggers, replay traces, or check runtime health"],["CONFIGURATION","Environment controls include **SKILLSCRIPT_SHELL_ALLOWLIST**, **SKILLSCRIPT_FS_ALLOWLIST** (default deny), **SKILLSCRIPT_SECURED_MODE**, and **SKILLSCRIPT_SECRET_*** for runtime-held secrets. Project config examples ship as **skillscript.config.json.example** and **connectors.json.example**."],["CAVEATS","Pre-1.0; language and connector contracts are still stabilizing. Shell and filesystem access are deny-by-default until allowlisted. Bind the dashboard to **0.0.0.0** only with appropriate network protection."],["SEE ALSO","[make](/man/make)(1)"],["RESOURCES","```[Source code](https://github.com/sshwarts/skillscript)```\n\n```[Homepage](https://skillscript.ai)```\n\n```[Documentation](https://docs.skillscript.ai/docs)```"]],"e":["make"],"x":"npm install -g skillscript-runtime\nskillfile init\nskillfile dashboard"},"tailscale-ssh":{"n":"tailscale-ssh","t":"SSH over the Tailscale network with identity-based authentication","s":[["TAGLINE","SSH over the Tailscale network with identity-based authentication"],["TLDR","**SSH to a Tailscale device**\n\n```tailscale ssh [user]@[device]```\n\n**SSH to a device using its Tailscale hostname**\n\n```tailscale ssh [user]@[hostname]```\n\n**SSH and run a remote command**\n\n```tailscale ssh [user]@[device] [command]```\n\n**SSH with verbose output**\n\n```tailscale ssh -v [user]@[device]```"],["SYNOPSIS","**tailscale** **ssh** [_ssh_options_] [_user_@]_target_ [_command_]"],["PARAMETERS","_target_\n> Tailscale device name, hostname, or IP address to connect to.\n\n_user_\n> Remote username. If omitted, the current local username is used.\n\n_command_\n> Optional command to execute on the remote host.\n\nStandard SSH flags (e.g., **-v**, **-p**, **-L**) are passed through to the underlying SSH connection."],["DESCRIPTION","**tailscale ssh** connects to another machine on your Tailscale network using SSH, with authentication handled by Tailscale's identity system instead of traditional SSH keys or passwords.\n\nWhen Tailscale SSH is enabled on the target machine, connections are authenticated using the Tailscale identity of the connecting user. Access is controlled through Tailscale SSH ACLs in the admin console, allowing fine-grained rules about which users can access which machines and as which system users.\n\nThe connection is encrypted end-to-end over the WireGuard tunnel that Tailscale establishes between nodes. This means SSH access works without exposing port 22 to the public internet and without managing SSH key distribution."],["CAVEATS","Tailscale SSH must be enabled on the target device (via **tailscale up --ssh** or the admin console). Both machines must be on the same Tailnet or share access via Tailscale sharing. SSH ACLs must be configured in the Tailscale admin console to permit the connection. The target machine must have a Tailscale SSH server running (built into the Tailscale client)."],["INSTALL","```dnf: sudo dnf install tailscale```\n\n```pacman: sudo pacman -S tailscale```\n\n```apk: sudo apk add tailscale```\n\n```zypper: sudo zypper install tailscale```\n\n```brew: brew install tailscale```\n\n```nix: nix profile install nixpkgs#tailscale```"],["SEE ALSO","[tailscale](/man/tailscale)(1), [tailscale-up](/man/tailscale-up)(1), [ssh](/man/ssh)(1)"]],"e":["tailscale","tailscale-up","ssh"],"x":"tailscale ssh [user]@[device]"},"tv":{"n":"tv","t":"Terminal viewer for tabular data","s":[["TAGLINE","Terminal viewer for tabular data"],["TLDR","**View a CSV file**\n\n```tv [data.csv]```\n\n**View a TSV file**\n\n```tv -t [data.tsv]```\n\n**View with a custom delimiter**\n\n```tv -d \";\" [data.txt]```\n\n**Show only the first N rows**\n\n```tv -n [10] [data.csv]```\n\n**View without a header row**\n\n```tv --no-header [data.csv]```\n\n**Use a specific color palette**\n\n```tv -c [1] [data.csv]```\n\n**Pipe data from another command**\n\n```cat [data.csv] | tv```"],["SYNOPSIS","**tv** [_-t_] [_-d delim_] [_--no-header_] [_options_] _file_"],["PARAMETERS","**-t**\n> Tab-separated.\n\n**-d** _DELIM_\n> Custom delimiter.\n\n**--no-header**\n> No header row.\n\n**-n** _NUM_\n> Number of rows to output. Default: 25.\n\n**-c** _N_\n> Color palette: 1 (nord), 2 (one_dark), 3 (gruvbox), 4 (dracula), 5 (uncolor).\n\n**-u** _WIDTH_\n> Upper (maximum) column width. Default: 20.\n\n**-l** _WIDTH_\n> Lower (minimum) column width. Must be 2 or larger. Default: 2.\n\n**-h**, **--help**\n> Show help."],["DESCRIPTION","**tv** (tidy-viewer) is a terminal viewer for tabular data that displays CSV, TSV, and other delimited files as formatted, column-aligned tables. It reads from files or stdin and renders data with proper alignment for easy reading in the terminal.\n\nCustom delimiters can be specified for non-standard formats, and the **--no-header** flag handles files without a header row. The tool supports limiting output to a specified number of rows (default 25) for previewing large datasets. Missing values (NA, NULL, empty) are detected and highlighted.\n\nOutput is optimized for terminal display, with configurable column widths and 5 built-in color palettes. A dotfile (**~/.tv.toml**) can be used for persistent configuration."],["CAVEATS","Multiple tools share the name \"tv\". This refers to the Rust-based tidy-viewer/tv tool. Large files may be truncated to fit terminal dimensions."],["HISTORY","**tv** (tidy-viewer) is a Rust-based CLI tool for rendering tabular data in the terminal."],["INSTALL","```apt: sudo apt install treeviewx```\n\n```nix: nix profile install nixpkgs#tv```"],["SEE ALSO","[column](/man/column)(1), [csvlook](/man/csvlook)(1), [miller](/man/miller)(1)"]],"e":["column","csvlook","miller"],"x":"tv [data.csv]"},"unzipsfx":{"n":"unzipsfx","t":"Create self-extracting ZIP executables","s":[["TAGLINE","Create self-extracting ZIP executables"],["TLDR","Create **self-extracting** binary\n\n```cat $(which unzipsfx) [path/to/archive.zip] > [filename] && chmod 755 [filename]```\n\n**Extract** self-extracting binary\n\n```./[path/to/binary]```\n\n**Test** for errors\n\n```./[path/to/binary] -t```\n\n**Print** file content without extracting\n\n```./[path/to/binary] -c [path/to/file]```\n\nShow **comments**\n\n```./[path/to/binary] -z```"],["SYNOPSIS","**unzipsfx** is typically concatenated with a ZIP archive"],["PARAMETERS","**-t**\n> Test archive for errors\n\n**-c** _FILE_\n> Print file content without extraction\n\n**-z**\n> Print archive comments"],["DESCRIPTION","**unzipsfx** creates self-extracting compressed binary files by prepending self-extracting stubs to ZIP archives. The resulting executable can extract itself without requiring unzip to be installed.\n\nThe stub is typically concatenated with a ZIP archive using cat, then made executable."],["CAVEATS","Self-extracting binaries are platform-specific. The stub adds size overhead to the archive. Some systems may block execution of self-extracting archives."],["INSTALL","```apt: sudo apt install unzip```\n\n```dnf: sudo dnf install unzip```\n\n```pacman: sudo pacman -S unzip```\n\n```apk: sudo apk add unzip```\n\n```zypper: sudo zypper install unzip```\n\n```brew: brew install unzip```\n\n```nix: nix profile install nixpkgs#unzip```"],["SEE ALSO","[zip](/man/zip)(1), [unzip](/man/unzip)(1)"]],"e":["zip","unzip"],"x":"cat $(which unzipsfx) [path/to/archive.zip] > [filename] && chmod 755 [filename]"},"update-initramfs":{"n":"update-initramfs","t":"Manage initial RAM filesystem images","s":[["TAGLINE","Manage initial RAM filesystem images"],["TLDR","**Create** a new initramfs for a specific kernel version\n\n```sudo update-initramfs -c -k [kernel_version]```\n\n**Create** initramfs for all installed kernel versions\n\n```sudo update-initramfs -c -k all```\n\n**Update** an existing initramfs\n\n```sudo update-initramfs -u```\n\n**Remove** an existing initramfs\n\n```sudo update-initramfs -d -k [kernel_version]```"],["SYNOPSIS","**update-initramfs** [_options_]"],["PARAMETERS","**-c**\n> Create a new initramfs\n\n**-u**\n> Update an existing initramfs\n\n**-d**\n> Delete an existing initramfs\n\n**-k** _version_\n> Specify kernel version (default: latest). Use \"all\" for all versions.\n\n**-t**\n> Take over a custom initramfs with a newer one.\n\n**-v**\n> Verbose mode\n\n**-b** _directory_\n> Set alternate boot directory\n\n**-h, --help**\n> Print help and exit.\n\n**--version**\n> Print version of the underlying initrd generation tool."],["DESCRIPTION","**update-initramfs** manages the initial RAM filesystem (initramfs) images used during Linux boot. The initramfs contains essential drivers and scripts needed to mount the root filesystem.\n\nThis tool is typically used after installing new kernel modules, changing boot configuration, or when the initramfs becomes corrupted. It uses **initramfs-tools** on Debian-based systems."],["CAVEATS","Requires root privileges. Be careful when using \"all\" with the delete option. A corrupted or missing initramfs may prevent the system from booting. Debian/Ubuntu specific; other distributions use **mkinitcpio** or **dracut**."],["SEE ALSO","[mkinitramfs](/man/mkinitramfs)(8), [initramfs-tools](/man/initramfs-tools)(7), [dracut](/man/dracut)(8), [mkinitcpio](/man/mkinitcpio)(8)"]],"e":["mkinitramfs","initramfs-tools","dracut","mkinitcpio"],"x":"sudo update-initramfs -c -k [kernel_version]"},"v":{"n":"v","t":"V programming language compiler","s":[["TAGLINE","V programming language compiler"],["TLDR","**Compile file**\n\n```v [file.v]```\n\n**Run file**\n\n```v run [file.v]```\n\n**Build optimized**\n\n```v -prod [file.v]```\n\n**Format code**\n\n```v fmt [file.v]```\n\n**Start REPL**\n\n```v repl```\n\n**Test module**\n\n```v test [module]```"],["SYNOPSIS","**v** [_run_] [_-prod_] [_options_] _file_"],["PARAMETERS","**run**\n> Compile the source file(s) and immediately execute the resulting binary.\n\n**fmt**\n> Format source files using the canonical V style.\n\n**test**\n> Compile and run **_test.v** files in the given directory or module.\n\n**repl**\n> Start the interactive Read-Eval-Print Loop.\n\n**build-module**, **build**, **init**, **new**, **install**, **remove**, **search**\n> Subcommands for module scaffolding and dependency management.\n\n**-o** _FILE_\n> Output binary filename.\n\n**-prod**\n> Enable production optimizations (slower compile, faster binary).\n\n**-cc** _COMPILER_\n> Back-end C compiler to use (e.g. **clang**, **gcc**, **tcc**).\n\n**-g**\n> Include debug information in the output.\n\n**-cflags** _FLAGS_, **-ldflags** _FLAGS_\n> Append extra flags for the C compiler or linker.\n\n**-shared**\n> Build a shared library instead of an executable.\n\n**-autofree**\n> Enable the experimental auto-free memory management mode.\n\n**-gc** _MODE_\n> Choose a garbage-collector mode (e.g. **boehm**, **none**).\n\n**-stats**\n> Print detailed statistics (especially useful with **v test**).\n\n**-show-timings**\n> Report per-phase compilation times.\n\n**-skip-unused**\n> Skip code generation for unused functions (faster builds)."],["DESCRIPTION","**v** is the compiler for the V programming language, a statically typed systems language designed for simplicity and fast compilation. It compiles V source code to native machine code via C, achieving near-instant build times even for large projects.\n\nThe compiler includes a built-in code formatter, test runner, REPL, and package manager. V emphasizes memory safety through features like no null pointers, immutable variables by default, and optional garbage collection. It provides seamless C interoperability, allowing direct calls to C libraries without bindings.\n\nProduction builds with the **-prod** flag enable additional optimizations. The compiler can also generate C source code for portability to platforms without a V compiler available."],["CAVEATS","Young language. Ecosystem growing. V-specific."],["HISTORY","**V** was created by **Alexander Medvednikov** as a simple, fast, and safe systems programming language."],["INSTALL","```brew: brew install v```"],["SEE ALSO","[gcc](/man/gcc)(1), [go](/man/go)(1), [zig](/man/zig)(1)"]],"e":["gcc","go","zig"],"x":"v [file.v]"},"vmtouch":{"n":"vmtouch","t":"Control file system page cache","s":[["TAGLINE","Control file system page cache"],["TLDR","**Show file cache status**\n\n```vmtouch [file]```\n\n**Load file into cache**\n\n```vmtouch -t [file]```\n\n**Evict from cache**\n\n```vmtouch -e [file]```\n\n**Lock in memory**\n\n```vmtouch -l [file]```\n\n**Verbose output**\n\n```vmtouch -v [file]```\n\n**Check directory**\n\n```vmtouch [directory/]```"],["SYNOPSIS","**vmtouch** [_-t_] [_-e_] [_-l_] [_-v_] _files_"],["PARAMETERS","**-t**\n> Touch (load to cache).\n\n**-e**\n> Evict from cache.\n\n**-l**\n> Lock in memory.\n\n**-v**\n> Verbose.\n\n**-q**\n> Quiet mode.\n\n**-m** _SIZE_\n> Max memory."],["DESCRIPTION","**vmtouch** is a tool for inspecting and manipulating the file system page cache on Linux. It can report which pages of a file or directory are currently resident in memory, providing visibility into what the operating system has cached for fast access.\n\nBeyond inspection, vmtouch can actively manage the cache by touching files to load them into memory, evicting files to free cached pages, or locking files into RAM to prevent the kernel from swapping them out. This makes it useful for warming caches before high-traffic events, ensuring critical data stays in memory, or clearing cached data for benchmarking purposes.\n\nThe tool works on individual files or entire directory trees and displays per-file cache residency statistics in verbose mode. Locking pages into memory requires root privileges and is subject to system memory limits."],["CAVEATS","Root for lock. Linux-specific features. Page cache only."],["HISTORY","**vmtouch** was created by **Doug Hoyte** for controlling the file system cache from the command line."],["INSTALL","```apt: sudo apt install vmtouch```\n\n```dnf: sudo dnf install vmtouch```\n\n```zypper: sudo zypper install vmtouch```\n\n```brew: brew install vmtouch```\n\n```nix: nix profile install nixpkgs#vmtouch```"],["SEE ALSO","[free](/man/free)(1), [sync](/man/sync)(1), [dd](/man/dd)(1)"]],"e":["free","sync","dd"],"x":"vmtouch [file]"},"vsftpd":{"n":"vsftpd","t":"Lightweight secure FTP server daemon","s":[["TAGLINE","Lightweight secure FTP server daemon"],["TLDR","**Start vsftpd** in standalone mode\n\n```vsftpd```\n\n**Start with a specific configuration file**\n\n```vsftpd [/etc/vsftpd.conf]```\n\n**Start the vsftpd service** via systemd\n\n```sudo systemctl start vsftpd```\n\n**Enable vsftpd on boot**\n\n```sudo systemctl enable vsftpd```\n\n**Print version** information and exit\n\n```vsftpd -v```"],["SYNOPSIS","**vsftpd** [_config_file_]"],["PARAMETERS","**-v**\n> Print version information and exit.\n\n**-ooption=value**\n> Set a single configuration option as per the config file format."],["DESCRIPTION","**vsftpd** (Very Secure FTP Daemon) is a lightweight, secure FTP server for Unix-like systems. It serves as the default FTP server on many Linux distributions including Ubuntu, Fedora, and RHEL.\n\nThe daemon can run standalone (recommended) by setting **listen=YES** in /etc/vsftpd.conf, or be launched via inetd/xinetd. It listens on TCP port 21 by default for control connections.\n\nvsftpd supports anonymous FTP, local user authentication, virtual users via PAM, SSL/TLS encryption (FTPS), and passive mode for NAT environments. Access control is managed through /etc/vsftpd/ftpusers and /etc/vsftpd/user_list."],["CONFIGURATION","Key settings in **/etc/vsftpd.conf**:\n\n**listen=YES**\n> Run in standalone mode\n\n**anonymous_enable=NO**\n> Disable anonymous access\n\n**local_enable=YES**\n> Allow local user logins\n\n**write_enable=YES**\n> Allow upload operations\n\n**chroot_local_user=YES**\n> Confine users to home directories\n\n**ssl_enable=YES**\n> Enable SSL/TLS encryption\n\n**pasv_enable=YES**\n> Enable passive mode\n\n**pasv_min_port/pasv_max_port**\n> Passive mode port range"],["CAVEATS","FTP transmits credentials in cleartext; enable SSL/TLS for security. Passive mode requires firewall rules for the data port range. Consider SFTP (SSH-based) as a more secure alternative to FTP."],["HISTORY","vsftpd was written by **Chris Evans** with a focus on security, becoming the most widely deployed FTP server on Linux. Its design prioritizes security through privilege separation and minimal attack surface."],["INSTALL","```apt: sudo apt install vsftpd```\n\n```dnf: sudo dnf install vsftpd```\n\n```pacman: sudo pacman -S vsftpd```\n\n```apk: sudo apk add vsftpd```\n\n```zypper: sudo zypper install vsftpd```\n\n```brew: brew install vsftpd```\n\n```nix: nix profile install nixpkgs#vsftpd```"],["SEE ALSO","[sftp](/man/sftp)(1), [ftp](/man/ftp)(1)"]],"e":["sftp","ftp"],"x":"vsftpd"},"whatwaf":{"n":"whatwaf","t":"Web Application Firewall detection and bypass","s":[["TAGLINE","Web Application Firewall detection and bypass"],["TLDR","**Detect WAF**\n\n```whatwaf -u [https://example.com]```\n\n**From file**\n\n```whatwaf -l [urls.txt]```\n\n**Use Tor**\n\n```whatwaf -u [url] --tor```\n\n**Specific payloads**\n\n```whatwaf -u [url] --payload \"[<script>]\"```\n\n**JSON output**\n\n```whatwaf -u [url] --json```"],["SYNOPSIS","**whatwaf** [_-u url_] [_-l file_] [_--tor_] [_options_]"],["PARAMETERS","**-u** _URL_\n> Target URL.\n\n**-l** _FILE_\n> URL list file.\n\n**--tor**\n> Use Tor network.\n\n**--payload** _PAY_\n> Custom payload.\n\n**--json**\n> JSON output.\n\n**--tamper** _SCRIPT_\n> Tamper script."],["DESCRIPTION","**whatwaf** is a security tool that detects Web Application Firewalls (WAFs) protecting websites and suggests potential bypass techniques. It sends various payloads to the target and analyzes responses to fingerprint the specific WAF product in use.\n\nBeyond detection, whatwaf provides tamper scripts and evasion suggestions tailored to the identified WAF, helping penetration testers understand what protections they need to work around during authorized assessments. Custom payloads can be specified for targeted testing.\n\nThe tool supports batch scanning from URL files, Tor network routing for anonymous testing, and JSON output for integration with other security tools. It is intended exclusively for authorized security testing and research."],["CAVEATS","Authorized testing only. May trigger alerts. Python required."],["HISTORY","**WhatWaf** was created for identifying Web Application Firewalls and suggesting bypass techniques."],["SEE ALSO","[wafw00f](/man/wafw00f)(1), [nmap](/man/nmap)(1), [sqlmap](/man/sqlmap)(1)"]],"e":["wafw00f","nmap","sqlmap"],"x":"whatwaf -u [https://example.com]"},"xdelta3":{"n":"xdelta3","t":"VCDIFF binary delta encoding","s":[["TAGLINE","VCDIFF binary delta encoding"],["TLDR","**Create delta**\n\n```xdelta3 -e -s [old_file] [new_file] [patch.xd3]```\n\n**Apply patch**\n\n```xdelta3 -d -s [old_file] [patch.xd3] [new_file]```\n\n**High compression**\n\n```xdelta3 -9 -e -s [old_file] [new_file] [patch.xd3]```\n\n**Encode to stdout (for piping)**\n\n```xdelta3 -e -c -s [old_file] [new_file] > [patch.xd3]```\n\n**Show delta information**\n\n```xdelta3 printdelta [patch.xd3]```\n\n**Decode without source**\n\n```xdelta3 -d [patch.xd3] [output_file]```"],["SYNOPSIS","**xdelta3** [_command_] [_options_] [_input_ [_output_]]"],["COMMANDS","**encode**\n> Compress the input (default). Also set by **-e**.\n\n**decode**\n> Decompress the input. Also set by **-d**.\n\n**printdelta**\n> Print information about the entire delta.\n\n**printhdr**\n> Print information about the first window.\n\n**printhdrs**\n> Print information about all windows.\n\n**config**\n> Print xdelta3 configuration.\n\n**recode**\n> Re-encode with new application/secondary settings."],["PARAMETERS","**-e**\n> Encode (compress/create delta).\n\n**-d**\n> Decode (decompress/apply delta).\n\n**-s** _FILE_\n> Source file to copy from.\n\n**-0** to **-9**\n> Compression level.\n\n**-f**\n> Force overwrite of existing files.\n\n**-c**\n> Output to stdout.\n\n**-v**\n> Verbose output (may be specified up to 2 times).\n\n**-q**\n> Quiet, suppress output messages.\n\n**-B** _bytes_\n> Source window size.\n\n**-W** _bytes_\n> Input window size.\n\n**-S** [**djw**|**fgk**]\n> Enable/disable secondary compression.\n\n**-n**\n> Disable checksum.\n\n**-V**\n> Show version."],["DESCRIPTION","**xdelta3** creates and applies binary delta patches using the VCDIFF format defined in RFC 3284. Encoding compares a source file with a target file and produces a compact delta that captures only the differences, while decoding reconstructs the target by applying the delta to the original source.\n\nCompression levels from 0 to 9 trade processing speed for smaller delta sizes. A streaming mode processes data in chunks, enabling memory-efficient handling of very large files. Self-contained patches can be created without requiring the original source file for decoding, embedding the necessary data within the delta itself."],["CAVEATS","The source file must be available for decoding unless the delta was created without a source. Memory options (**-B**, **-W**) may need tuning for very large files. The command-line syntax is similar to gzip."],["HISTORY","**xdelta3** is the third version of xdelta, implementing RFC 3284 VCDIFF. It provides efficient binary delta compression."],["INSTALL","```apt: sudo apt install xdelta3```\n\n```pacman: sudo pacman -S xdelta3```\n\n```apk: sudo apk add xdelta3```\n\n```zypper: sudo zypper install xdelta3```"],["SEE ALSO","[xdelta](/man/xdelta)(1), [bsdiff](/man/bsdiff)(1), [diff](/man/diff)(1), [patch](/man/patch)(1), [gzip](/man/gzip)(1)"]],"e":["xdelta","bsdiff","diff","patch","gzip"],"x":"xdelta3 -e -s [old_file] [new_file] [patch.xd3]"},"xplr":{"n":"xplr","t":"Hackable terminal file explorer","s":[["TAGLINE","Hackable terminal file explorer"],["TLDR","**Start xplr**\n\n```xplr```\n\n**Start in directory**\n\n```xplr [/path/to/dir]```\n\n**Pipe selection to command**\n\n```xplr | xargs [command]```\n\n**Start with config**\n\n```xplr --config [config.lua]```\n\n**Read paths from stdin**\n\n```find . -name \"*.txt\" | xplr```\n\n**Print and quit** (on selection)\n\n```xplr --print-pwd-as-result```"],["SYNOPSIS","**xplr** [_--config file_] [_--print-pwd-as-result_] [_options_] [_paths_]"],["PARAMETERS","**--config** _FILE_\n> Configuration file.\n\n**--print-pwd-as-result**\n> Print current directory on exit instead of focused path.\n\n**-C** _NAME=VALUE_\n> Override config value.\n\n**--read-only**\n> Read-only mode (disables all write operations).\n\n**--vroot** _PATH_\n> Set virtual root to restrict navigation boundary.\n\n**-m** _MSG_\n> Send a message to xplr for processing.\n\n**-M** _MSG_\n> Send a message to xplr without quoting the value.\n\n**--version**, **-V**\n> Show version.\n\n**--help**, **-h**\n> Show help."],["KEY BINDINGS","**j/k** or **down/up**\n> Navigate.\n\n**Enter**\n> Open/select.\n\n**q**\n> Quit.\n\n**/**\n> Search.\n\n**space**\n> Toggle selection.\n\n**h**\n> Go to parent.\n\n**g**\n> Go to path.\n\n**.**\n> Toggle hidden.\n\n**ctrl-c**\n> Cancel."],["DESCRIPTION","**xplr** is a hackable, minimal terminal file explorer. Written in Rust, it's fast and highly configurable.\n\nLua configuration enables complete customization. Modes, keybindings, and UI can be redefined entirely.\n\nSelection handling pipes to stdout. This enables integration with other commands and scripts.\n\nModes define behavior sets. Normal mode, selection mode, and custom modes provide context-aware keybindings.\n\nPlugins extend functionality. Community plugins add features like preview, bulk rename, and integration.\n\nThe focus is on composition with Unix tools rather than built-in features for everything."],["CAVEATS","Steep learning curve for configuration. Fewer built-in features than ranger. Lua knowledge helpful for customization."],["HISTORY","**xplr** was created by **Arijit Basu** around **2021** as an extensible file explorer. It emphasizes hackability and composition over built-in features."],["INSTALL","```pacman: sudo pacman -S xplr```\n\n```apk: sudo apk add xplr```\n\n```zypper: sudo zypper install xplr```\n\n```brew: brew install xplr```\n\n```nix: nix profile install nixpkgs#xplr```"],["SEE ALSO","[ranger](/man/ranger)(1), [vifm](/man/vifm)(1), [nnn](/man/nnn)(1), [lf](/man/lf)(1)"]],"e":["ranger","vifm","nnn","lf"],"x":"xplr"},"zypper":{"n":"zypper","t":"SUSE and openSUSE package manager","s":[["TAGLINE","SUSE and openSUSE package manager"],["TLDR","**Synchronize** list of packages and versions available\n\n```sudo zypper refresh```\n\n**Install** a new package\n\n```sudo zypper install [package]```\n\n**Remove** a package\n\n```sudo zypper remove [package]```\n\n**Upgrade** installed packages to newest available versions\n\n```sudo zypper update```\n\n**Search** for a package via keyword\n\n```zypper search [keyword]```\n\n**Show** information about configured repositories\n\n```zypper repos --sort-by-priority```"],["SYNOPSIS","**zypper** [_global-options_] _command_ [_command-options_] [_arguments_]"],["PARAMETERS","**ref, refresh**\n> Refresh all repositories\n\n**in, install _package_**\n> Install packages\n\n**rm, remove _package_**\n> Remove packages\n\n**up, update**\n> Update installed packages\n\n**se, search _pattern_**\n> Search for packages\n\n**lr, repos**\n> List all configured repositories\n\n**dup, dist-upgrade**\n> Perform distribution upgrade\n\n**--no-confirm**\n> Don't ask for confirmation\n\n**--sort-by-priority**\n> Sort repository list by priority"],["DESCRIPTION","**zypper** is the command-line package management utility for SUSE and openSUSE Linux. It provides complete package management including installation, removal, updates, and repository management.\n\nZypper supports dependency resolution, patch management, and can handle distribution upgrades. It uses the libzypp library and is the CLI frontend for YaST software management."],["CAVEATS","Requires root privileges for package operations. Repository refresh (**zypper ref**) should be run before updates. Distribution upgrades (**zypper dup**) should be done carefully."],["INSTALL","```apt: sudo apt install zypper```\n\n```dnf: sudo dnf install zypper```\n\n```pacman: sudo pacman -S zypper```\n\n```zypper: sudo zypper install zypper```"],["SEE ALSO","[rpm](/man/rpm)(8), [yast2](/man/yast2)(8)"]],"e":["rpm","yast2"],"x":"sudo zypper refresh"}});
