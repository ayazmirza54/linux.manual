window.LCL_BUCKET(5,{"aa-enforce":{"n":"aa-enforce","t":"Set AppArmor profiles to enforce mode","s":[["TAGLINE","Set AppArmor profiles to enforce mode"],["TLDR","**Enable** a profile in enforce mode\n\n```sudo aa-enforce [path/to/profile]```\n\nEnable **multiple profiles** in enforce mode\n\n```sudo aa-enforce [path/to/profile1] [path/to/profile2]```\n\nEnable profiles from a **specific directory**\n\n```sudo aa-enforce -d [path/to/profiles]```\n\nSet enforce mode **without reloading** the policy\n\n```sudo aa-enforce --no-reload [path/to/profile]```"],["SYNOPSIS","**aa-enforce** _executable_ [_executable_ ...] [_-d /path/to/profiles_] [_--no-reload_]"],["DESCRIPTION","**aa-enforce** sets one or more AppArmor security profiles to enforce mode, which is the default and most secure mode. In enforce mode, security policy is strictly applied and any access violations are blocked and logged.\n\nThis command reverses the effects of **aa-complain** (which sets complain mode) and **aa-disable** (which unloads profiles)."],["PARAMETERS","**-d, --dir /path/to/profiles**\n> Specifies the directory containing AppArmor profiles; defaults to /etc/apparmor.d\n\n**--no-reload**\n> Do not reload the profile after modifying it"],["CAVEATS","Enabling enforce mode on a profile that hasn't been properly tested may cause applications to malfunction if the profile is too restrictive."],["HISTORY","Part of the **AppArmor** utilities package for managing application security profiles on Linux systems."],["INSTALL","```apk: sudo apk add apparmor-utils```\n\n```zypper: sudo zypper install apparmor-utils```\n\n```nix: nix profile install nixpkgs#apparmor-utils```"],["SEE ALSO","[aa-complain](/man/aa-complain)(8), [aa-disable](/man/aa-disable)(8), [aa-logprof](/man/aa-logprof)(8), [aa-status](/man/aa-status)(8), [apparmor](/man/apparmor)(7)"],["RESOURCES","```[Source code](https://gitlab.com/apparmor/apparmor)```\n\n```[Homepage](https://apparmor.net/)```\n\n```[Documentation](https://gitlab.com/apparmor/apparmor/-/wikis/home)```"]],"e":["aa-complain","aa-disable","aa-logprof","aa-status","apparmor"],"x":"sudo aa-enforce [path/to/profile]"},"asar":{"n":"asar","t":"Pack and extract Electron application archives.","s":[["TAGLINE","Pack and extract Electron application archives."],["TLDR","**Create** asar archive\n\n```asar pack [dir/] [output.asar]```\n\n**Extract** asar archive\n\n```asar extract [archive.asar] [output_dir/]```\n\n**List** contents\n\n```asar list [archive.asar]```\n\nExtract **single file**\n\n```asar extract-file [archive.asar] [path/in/archive] [output_file]```\n\nPack while keeping files **unpacked**\n\n```asar pack [dir/] [output.asar] --unpack [*.node]```"],["SYNOPSIS","**asar** _pack_|_extract_|_list_|_extract-file_ [_options_] _arguments_"],["DESCRIPTION","**asar** creates and extracts Electron archive files. ASAR (Atom Shell Archive) is a tar-like format used by Electron applications to package source files into a single file while maintaining random access capability.\n\nThe format allows Electron to read files directly from the archive without extraction, improving load times. The tool is distributed on npm as **@electron/asar** (formerly the standalone **asar** package)."],["PARAMETERS","**pack** _dir_ _output_ (alias **p**)\n> Create archive from directory\n\n**extract** _archive_ _dest_ (alias **e**)\n> Extract archive to directory\n\n**list** _archive_ (alias **l**)\n> List archive contents\n\n**extract-file** _archive_ _file_ _dest_ (alias **ef**)\n> Extract single file\n\n**--unpack** _glob_\n> Keep files matching the glob unpacked (stored in a sibling app.asar.unpacked directory)\n\n**--unpack-dir** _glob_\n> Keep directories matching the glob unpacked\n\n**--exclude-hidden**\n> Exclude hidden (dotfile) files from the archive\n\n**--ordering** _file_\n> Pack files in the order listed in the given file"],["CAVEATS","ASAR archives are not encrypted; contents can be easily extracted. Some Electron apps may integrity-check their archives. Not suitable for sensitive data."],["HISTORY","**ASAR** was developed for the Atom text editor (later Electron framework) as a way to package Node.js applications efficiently."],["INSTALL","```pacman: sudo pacman -S asar```\n\n```zypper: sudo zypper install asar```\n\n```nix: nix profile install nixpkgs#asar```"],["SEE ALSO","[tar](/man/tar)(1), [npm](/man/npm)(1), [electron](/man/electron)(1)"],["RESOURCES","```[Source code](https://github.com/electron/asar)```\n\n```[Homepage](https://www.electronjs.org)```"]],"e":["tar","npm","electron"],"x":"asar pack [dir/] [output.asar]"},"auto-round":{"n":"auto-round","t":"Low-bit quantization toolkit for LLMs and VLMs","s":[["TAGLINE","Low-bit quantization toolkit for LLMs and VLMs"],["TLDR","**Quantize a model** with the default recipe\n\n```auto-round --model [Qwen/Qwen3-0.6B] --scheme \"[W4A16]\" --format \"[auto_round]\"```\n\n**Use the best recipe** (slower, higher accuracy)\n\n```auto-round-best --model [model_id] --scheme \"[W4A16]\"```\n\n**Use the light recipe** (faster)\n\n```auto-round-light --model [model_id] --scheme \"[W4A16]\"```\n\n**Quantize to 4-bit** with multiple export formats\n\n```auto-round --model [model_id] --bits 4 --group_size 128 --format \"[auto_round,auto_awq,auto_gptq]\" --output_dir [path/to/output]```\n\n**Calibration-free** RTN mode\n\n```auto-round --model [model_id] --bits 4 --iters 0```\n\n**Multi-GPU** quantization\n\n```auto-round --model [model_id] --device_map \"[0,1,2,3]\"```\n\n**Evaluate** an already-quantized model\n\n```auto-round --model [path/to/quantized] --eval --tasks [mmlu,lambada_openai]```"],["SYNOPSIS","**auto-round** **--model** _MODEL_ [_options_]\n\n**auto-round-best** **--model** _MODEL_ [_options_]\n\n**auto-round-light** **--model** _MODEL_ [_options_]"],["DESCRIPTION","**auto-round** is a weight-only post-training quantization (PTQ) toolkit for **LLMs** and **VLMs**, developed by **Intel**. It uses signed gradient descent to jointly optimize weight rounding and clipping ranges, achieving high accuracy at ultra-low bit widths (down to 2 bits) with minimal calibration time.\n\nThe toolkit supports CPU, Intel GPU (XPU), HPU, and CUDA back-ends and exports to several popular quantization formats including **auto_round**, **auto_awq**, **auto_gptq**, and **gguf**, so models can be served via Transformers, vLLM, SGLang, or llm-compressor without re-quantization.\n\nThree recipes are provided: **auto-round** (default balance), **auto-round-best** (slowest, highest accuracy, 4–5× slower), and **auto-round-light** (fastest, 2–3× speedup)."],["PARAMETERS","**--model** _MODEL_\n> Model identifier or local path (e.g. _Qwen/Qwen3-0.6B_).\n\n**--scheme** _SCHEME_\n> Quantization scheme such as _W4A16_, _W2A16_, _W8A16_.\n\n**--bits** _N_\n> Weight bit width: 2, 3, 4, or 8.\n\n**--group_size** _N_\n> Quantization group size (e.g. 32, 64, 128).\n\n**--format** _FORMAT_\n> Export format(s), comma-separated: _auto_round_, _auto_gptq_, _auto_awq_, _gguf:q4_k_m_, etc.\n\n**--output_dir** _PATH_\n> Directory where the quantized model is written.\n\n**--dataset** _SPEC_\n> Calibration data (local path or HuggingFace dataset). Supports _name:num=N_, _:concat=True_, _:apply_chat_template_, and comma-separated lists.\n\n**--iters** _N_\n> Tuning iterations (_0_ for RTN, default _200_, up to _1000_ for best accuracy).\n\n**--bs** _N_\n> Batch size (default 8).\n\n**--seqlen** _N_\n> Calibration sequence length (default 2048).\n\n**--nsamples** _N_\n> Number of calibration samples (default 128, up to 512 for best).\n\n**--lr** _RATE_\n> Learning rate.\n\n**--device_map** _SPEC_\n> GPU assignment, e.g. _auto_ or _0,1,2,3_.\n\n**--low_gpu_mem_usage**\n> Reduce VRAM at the cost of more time.\n\n**--enable_torch_compile**\n> Use **torch.compile** (requires PyTorch 2.6+).\n\n**--quant_lm_head**\n> Also quantize the language-model head (auto_round format only).\n\n**--adam**\n> Use the **AdamW** optimizer instead of signed gradient descent.\n\n**--eval**\n> Evaluate the model after quantization.\n\n**--eval_backend** _BACKEND_\n> Evaluation engine, _vllm_ or default Hugging Face.\n\n**--tasks** _LIST_\n> Comma-separated lm-eval-harness tasks (e.g. _mmlu,lambada_openai_)."],["DESCRIPTION OF FORMATS","**auto_round**\n> Native AutoRound format, supports lm-head quantization.\n\n**auto_gptq**\n> GPTQ-compatible format.\n\n**auto_awq**\n> AWQ-compatible format.\n\n**gguf:q4_k_m**, **gguf:q2_k_s**\n> GGUF formats for llama.cpp / Ollama-style runtimes."],["CAVEATS","Calibration is sensitive to dataset quality and length; using domain-mismatched calibration data can degrade accuracy. Lower bit widths (2-3 bits) may need the **best** recipe to recover accuracy. Some export formats restrict feature combinations (e.g. **--quant_lm_head** only works with the auto_round format)."],["HISTORY","**AutoRound** was introduced by **Intel** as part of its LLM quantization stack. It distinguishes itself from older PTQ methods such as **GPTQ** and **AWQ** by jointly optimizing rounding and clipping with signed gradient descent, narrowing the accuracy gap to QAT at low bit widths while remaining a calibration-only method."],["SEE ALSO","[python](/man/python)(1), [llama.cpp](/man/llama.cpp)(1)"]],"e":["python","llama.cpp"],"x":"auto-round --model [Qwen/Qwen3-0.6B] --scheme \"[W4A16]\" --format \"[auto_round]\""},"aws-ec2":{"n":"aws-ec2","t":"Manage virtual servers and networking on Amazon EC2.","s":[["TAGLINE","Manage virtual servers and networking on Amazon EC2."],["TLDR","**Launch a new EC2 instance**\n\n```aws ec2 run-instances --image-id [ami-0c55b159cbfafe1f0] --instance-type [t2.micro] --key-name [my-key]```\n\n**List all instances** with their state\n\n```aws ec2 describe-instances --query \"Reservations[*].Instances[*].[InstanceId,State.Name,InstanceType]\" --output table```\n\n**Stop a running instance**\n\n```aws ec2 stop-instances --instance-ids [i-1234567890abcdef0]```\n\n**Start a stopped instance**\n\n```aws ec2 start-instances --instance-ids [i-1234567890abcdef0]```\n\n**Terminate an instance** permanently\n\n```aws ec2 terminate-instances --instance-ids [i-1234567890abcdef0]```\n\n**Create a security group** and add an SSH rule\n\n```aws ec2 create-security-group --group-name [my-sg] --description \"[My security group]\" && aws ec2 authorize-security-group-ingress --group-name [my-sg] --protocol tcp --port 22 --cidr 0.0.0.0/0```\n\n**Create a key pair** and save it\n\n```aws ec2 create-key-pair --key-name [my-key] --query \"KeyMaterial\" --output text > [my-key.pem]```\n\n**List security groups** with their rules\n\n```aws ec2 describe-security-groups --query \"SecurityGroups[*].[GroupId,GroupName]\" --output table```"],["SYNOPSIS","**aws ec2** _command_ [_options_]"],["DESCRIPTION","**aws ec2** is the AWS CLI interface for Amazon Elastic Compute Cloud (EC2), providing programmatic access to scalable compute capacity. EC2 enables launching virtual servers, configuring networking and security, and managing storage.\n\nEC2 commands cover instance lifecycle management, Amazon Machine Images (AMIs), Elastic Block Store (EBS) volumes, VPC networking, security groups, and key pairs. The service supports on-demand, reserved, and spot pricing models."],["COMMANDS","**run-instances**\n> Launch new EC2 instances\n\n**describe-instances**\n> List and describe instances\n\n**start-instances**\n> Start stopped instances\n\n**stop-instances**\n> Stop running instances\n\n**terminate-instances**\n> Permanently delete instances\n\n**reboot-instances**\n> Reboot running instances\n\n**create-image**\n> Create an AMI from an instance\n\n**create-security-group**\n> Create a security group\n\n**describe-security-groups**\n> List and describe security groups\n\n**authorize-security-group-ingress**\n> Add inbound rules to security group\n\n**create-key-pair**\n> Generate a new key pair\n\n**create-vpc**\n> Create a Virtual Private Cloud\n\n**create-volume**\n> Create an EBS volume\n\n**attach-volume**\n> Attach volume to an instance"],["CAVEATS","Instances continue incurring charges until terminated (not just stopped) for EBS-backed instances. Security group rules are stateful but NACLs are not. Instance metadata service (IMDS) v1 is insecure; use IMDSv2. Spot instances can be terminated with 2 minutes notice."],["HISTORY","Amazon EC2 launched in **August 2006** as one of AWS's first services, revolutionizing cloud computing. VPC became mandatory in **2013**. The service has continuously expanded instance types, from the original m1.small to hundreds of specialized types for compute, memory, GPU, and storage optimization."],["INSTALL","```dnf: sudo dnf install aws```\n\n```pacman: sudo pacman -S aws-cli```\n\n```apk: sudo apk add aws-cli```\n\n```zypper: sudo zypper install aws-cli```"],["SEE ALSO","[aws](/man/aws)(1), [aws-autoscaling](/man/aws-autoscaling)(1), [aws-elb](/man/aws-elb)(1), [aws-vpc](/man/aws-vpc)(1)"],["RESOURCES","```[Source code](https://github.com/aws/aws-cli)```\n\n```[Homepage](https://aws.amazon.com/cli/)```\n\n```[Documentation](https://docs.aws.amazon.com/cli/latest/reference/ec2/index.html)```"]],"e":["aws","aws-autoscaling","aws-elb","aws-vpc"],"x":"aws ec2 run-instances --image-id [ami-0c55b159cbfafe1f0] --instance-type [t2.micro] --key-name [my-key]"},"biber":{"n":"biber","t":"Modern bibliography processor for BibLaTeX","s":[["TAGLINE","Modern bibliography processor for BibLaTeX"],["TLDR","**Process a BibLaTeX bibliography**\n\n```biber [document]```\n\n**Process with a specific output directory**\n\n```biber -O [output_dir] [document]```\n\n**Validate data** against the data model\n\n```biber --validate-datamodel [document]```\n\n**Process with quiet output**\n\n```biber -q [document]```\n\n**Enable tracing** for debugging\n\n```biber --trace [document]```\n\n**Specify output format**\n\n```biber --output-format [bbl] [document]```\n\n**Convert a .bib database to BibLaTeXML**\n\n```biber --tool --output-format=biblatexml [references.bib]```"],["SYNOPSIS","**biber** [_options_] _file_[.bcf]"],["DESCRIPTION","**biber** is a modern bibliography processor designed as a replacement for BibTeX, serving as the backend for the biblatex package in LaTeX documents. It provides advanced sorting, filtering, and Unicode support.\n\nBiber reads a **.bcf** (biblatex control file) generated by LaTeX and produces a **.bbl** file containing the formatted bibliography. It supports multiple bibliography databases and offers extensive configuration options."],["PARAMETERS","**--output-directory** _dir_\n> Directory where output files will be written\n\n**-O, --output-file** _file_\n> Output to specific file instead of basename.bbl\n\n**--output-format** _format_\n> Output format: bbl (default), dot, bibtex, biblatexml, bblxml\n\n**-e, --input-encoding** _encoding_\n> Character encoding for input files (default: UTF-8)\n\n**-E, --output-encoding** _encoding_\n> Character encoding for output files (default: UTF-8)\n\n**--tool**\n> Run in tool mode to convert/process a single data source (.bib) without a .bcf control file\n\n**-V, --validate-datamodel**\n> Validate data against the data model\n\n**--validate-control**\n> Schema validate the .bcf control file\n\n**-q, --quiet**\n> Log only errors; use twice to suppress all output\n\n**-T, --trace**\n> Enable low-level tracing for debugging (also turns on --debug)\n\n**--logfile** _file_\n> Write log messages to specified file\n\n**--nolog**\n> Do not write any logfile\n\n**-v, --version**\n> Display version number"],["CAVEATS","Biber requires a **.bcf** file generated by running LaTeX with biblatex first. The typical workflow is: pdflatex -> biber -> pdflatex -> pdflatex. Biber is not a drop-in replacement for BibTeX; it requires the biblatex package."],["HISTORY","Biber was first written by **Francois Charette**; **Philip Kime** joined the development in **2009** and made it into the full-featured backend it is today. Written in Perl, it addressed limitations in BibTeX, particularly around Unicode support and advanced sorting. It became the recommended backend for biblatex and is now the standard for modern LaTeX bibliography processing."],["INSTALL","```dnf: sudo dnf install biber```\n\n```pacman: sudo pacman -S biber```\n\n```apk: sudo apk add biber```\n\n```brew: brew install biber```\n\n```nix: nix profile install nixpkgs#biber```"],["SEE ALSO","[bibtex](/man/bibtex)(1), [pdflatex](/man/pdflatex)(1), [latex](/man/latex)(1)"],["RESOURCES","```[Source code](https://github.com/plk/biber)```\n\n```[Homepage](https://sourceforge.net/projects/biblatex-biber/)```"]],"e":["bibtex","pdflatex","latex"],"x":"biber [document]"},"bunny":{"n":"bunny","t":"Official command-line interface for bunny.net","s":[["TAGLINE","Official command-line interface for bunny.net"],["TLDR","**Authenticate** the CLI with your bunny.net account\n\n```bunny login```\n\n**Deploy** the current project as a Magic Containers app\n\n```bunny apps deploy```\n\n**List** databases on your account\n\n```bunny db list```\n\n**Install** the CLI via the shell installer\n\n```curl -fsSL https://cli.bunny.net/install.sh | sh```\n\n**Install** the CLI globally via npm\n\n```npm install -g @bunny.net/cli```\n\n**Show** help and list available subcommands\n\n```bunny --help```"],["SYNOPSIS","**bunny** _command_ [_subcommand_] [_options_]"],["PARAMETERS","**login**\n> Authenticate the local CLI with a bunny.net account; tokens are stored locally for subsequent commands.\n\n**apps deploy**\n> Build and deploy the current project to **Magic Containers**. Automatically provisions infrastructure, scales globally, and routes traffic.\n\n**db list**\n> List databases (including bunny.net's edge database product) attached to the active account.\n\n**--help**, **-h**\n> Print help for the CLI or a specific subcommand.\n\n**--version**\n> Print version information."],["DESCRIPTION","**bunny** is the official command-line client for **bunny.net**, providing terminal access to the platform's developer toolkit: **Magic Containers**, edge databases, storage buckets, edge scripts, DNS zones, and CDN pull zones. It is implemented in **TypeScript** and distributed both as a standalone binary and as the npm package **@bunny.net/cli**.\n\nThe CLI's primary workflows include logging in, deploying applications to Magic Containers (which auto-provisions globally distributed runtimes), and managing supporting resources such as databases. It is part of bunny.net's broader move toward a unified developer experience, alongside the **@bunny.net/api** type-safe SDK and one-click templates."],["CAVEATS","The CLI is comparatively new and still evolving - subcommands and flags may change between releases. **bunny** is the binary name shipped by **BunnyWay/cli**; several unrelated community projects also call themselves \"bunny CLI\" (for example **own3d/bunny-cli**, **straticus1/bunny-cli**, and **simplesurance/bunny-cli**), each with its own command surface. Confirm which package is installed before troubleshooting."],["CONFIGURATION","After **bunny login**, credentials are persisted to the user's home directory (typically under **$XDG_CONFIG_HOME/bunny** or **~/.config/bunny**). API tokens used for non-interactive automation can be supplied via environment variables defined per subcommand."],["HISTORY","**bunny** is published by **bunny.net** in the **BunnyWay/cli** monorepo on GitHub. It was introduced in **2024** to expose the company's developer platform - in particular the **Magic Containers** product - through a single, scriptable CLI."],["SEE ALSO","[curl](/man/curl)(1), [docker](/man/docker)(1), [flyctl](/man/flyctl)(1), [wrangler](/man/wrangler)(1)"]],"e":["curl","docker","flyctl","wrangler"],"x":"bunny login"},"caja":{"n":"caja","t":"File manager for the MATE desktop","s":[["TAGLINE","File manager for the MATE desktop"],["TLDR","Open the **home directory**\n\n```caja```\n\nOpen **specific directories** in separate windows\n\n```caja [path/to/directory1] [path/to/directory2] ...```\n\nOpen directories in **tabs**\n\n```caja -t [path/to/directory1] [path/to/directory2] ...```\n\nOpen with a **specific window size**\n\n```caja -g [600]x[400] [path/to/directory]```\n\n**Close** all windows\n\n```caja -q```"],["SYNOPSIS","**caja** [_options_] [_directories_...]"],["DESCRIPTION","**caja** is the default file manager for the MATE desktop environment. It provides a graphical interface for browsing files and directories, managing file operations, and accessing network locations.\n\nCaja is a fork of GNOME's Nautilus file manager, maintaining a traditional desktop file management experience."],["PARAMETERS","**-t, --tabs**\n> Open directories in tabs instead of windows\n\n**-g, --geometry** _WxH_\n> Set window size in pixels\n\n**-q, --quit**\n> Close all Caja windows\n\n**-n, --no-default-window**\n> Only create windows for explicitly specified paths\n\n**--browser**\n> Open browser window"],["INSTALL","```apt: sudo apt install caja```\n\n```dnf: sudo dnf install caja```\n\n```pacman: sudo pacman -S caja```\n\n```apk: sudo apk add caja```\n\n```zypper: sudo zypper install caja```\n\n```nix: nix profile install nixpkgs#caja```"],["SEE ALSO","[nautilus](/man/nautilus)(1), [dolphin](/man/dolphin)(1), [thunar](/man/thunar)(1), [ranger](/man/ranger)(1)"],["RESOURCES","```[Source code](https://github.com/mate-desktop/caja)```\n\n```[Homepage](https://mate-desktop.org/)```"]],"e":["nautilus","dolphin","thunar","ranger"],"x":"caja"},"chdig":{"n":"chdig","t":"TUI for ClickHouse monitoring and debugging","s":[["TAGLINE","TUI for ClickHouse monitoring and debugging"],["TLDR","**Connect** to a local ClickHouse instance and show running queries\n\n```chdig```\n\n**Connect** to a remote ClickHouse host\n\n```chdig -u 'user:password@clickhouse-host.com/?secure=true'```\n\n**Connect** using a named connection from a config file\n\n```chdig --config [/path/to/config.yaml] --connection [prod]```\n\n**Monitor** a ClickHouse cluster across all shards\n\n```chdig -u '[host]' --cluster [my_cluster]```\n\n**Show** slow queries over 1 second from query log\n\n```chdig slowqueries```\n\n**Show** server logs\n\n```chdig serverlogs```\n\n**View** MergeTree merges\n\n```chdig merges```"],["SYNOPSIS","**chdig** [_OPTIONS_] [_SUBCOMMAND_]"],["PARAMETERS","**-u**, **--url** _URL_\n> Connection URL. Also reads **CHDIG_URL** environment variable.\n\n**--host** _HOST_\n> Override host (clickhouse-client compatible).\n\n**--port** _PORT_\n> Override port.\n\n**--user** _USER_\n> Override user. Also reads **CLICKHOUSE_USER**.\n\n**--password** _PASS_\n> Override password. Also reads **CLICKHOUSE_PASSWORD**.\n\n**--secure**\n> Use TLS/SSL.\n\n**--config** _FILE_\n> ClickHouse-like config file (YAML).\n\n**-C**, **--connection** _NAME_\n> Named connection profile from config file.\n\n**-c**, **--cluster** _NAME_\n> Cluster name; enables distributed queries across shards.\n\n**--history**\n> Aggregate historical system log data using merge().\n\n**-d**, **--delay-interval** _MS_\n> Refresh interval in milliseconds (default: 30000).\n\n**-g**, **--group-by**\n> Group distributed queries (default in cluster mode).\n\n**-b**, **--start** _TIME_\n> Begin of time interval (default: 1 hour ago).\n\n**-e**, **--end** _TIME_\n> End of time interval.\n\n**--completion** _SHELL_\n> Generate shell completion script."],["VIEWS","**queries** (default), **lastqueries**, **slowqueries**, **merges**, **mutations**, **replicationqueue**, **replicatedfetches**, **replicas**, **tables**, **tableparts**, **errors**, **backups**, **dictionaries**, **serverlogs**, **loggers**, **partlog**, **asynchronousinserts**, **s3queue**, **azurequeue**, **client**"],["DESCRIPTION","**chdig** provides an interactive terminal dashboard for ClickHouse, similar to how **top** or **htop** work for Linux system processes. It consolidates ClickHouse's extensive system introspection tables into a navigable TUI with flamegraph visualization directly in the terminal (CPU, real-time, memory), EXPLAIN integration for analyzing query plans and pipelines, cluster mode for aggregating queries across all shards, and historical mode for examining rotated system log tables."],["KEY BINDINGS","```\nF1     Help               F2     Switch views\nF8     Show actions        /     Filter\nj/k    Navigate            r     Refresh\np      Pause/resume        C     CPU flamegraph\nR      Real-time flamegraph M     Memory flamegraph\nS      Display query       y     Copy query\nK      Kill query          l     Show query logs\nq/Esc  Back/quit           Q     Force quit\n```"],["CAVEATS","The project is self-described as **pre-alpha**; keyboard shortcuts, views, and features may change without notice. Requires ClickHouse 21.2 or newer. Mouse mode is enabled by default, which interferes with standard terminal text selection; use Alt/Shift/Ctrl modifiers to select text."],["HISTORY","**chdig** was created by **Azat Khuzhin**, a Principal Software Engineer at ClickHouse. Written in Rust under the MIT license. The latest version is v26.2.3. There is an ongoing effort to bundle chdig as part of ClickHouse itself."],["INSTALL","```brew: brew install chdig```"],["SEE ALSO","[htop](/man/htop)(1), [top](/man/top)(1)"]],"e":["htop","top"],"x":"chdig"},"chirpw":{"n":"chirpw","t":"amateur radio programming tool","s":[["TAGLINE","amateur radio programming tool"],["TLDR","**Launch CHIRP radio programming software**\n\n```chirpw```\n\n**Launch with a specific radio driver**\n\n```chirpw --onlydriver [driver_name]```\n\n**Start with download action**\n\n```chirpw --action download```\n\n**Restore previous session tabs**\n\n```chirpw --restore```\n\n**Run with verbose output**\n\n```chirpw -v```\n\n**Display version information**\n\n```chirpw --version```"],["SYNOPSIS","**chirpw** [**--module** _module_] [**--onlydriver** _driver_] [**--action** _action_] [**--restore**] [**--page** _page_] [**--force-language** _code_] [**-q**|**-v**] [**--log** _file_] [**--log-level** _level_]"],["DESCRIPTION","**chirpw** is the graphical interface for CHIRP, a free and open-source tool for programming amateur (ham) radio transceivers. It allows users to manage radio memory channels, configure settings, and transfer data between computer and radio via serial or USB connections.\n\nCHIRP supports hundreds of radio models from manufacturers including Baofeng, Yaesu, Kenwood, Icom, and others. The software can import/export various file formats and query online repeater databases."],["PARAMETERS","**--module** _module_\n> Load specified module on startup.\n\n**--onlydriver** _driver_\n> Include only the specified radio driver.\n\n**--action** _action_\n> Start UI action immediately: upload, download, query_rr, query_mg, query_rb, query_dm, new.\n\n**--restore**\n> Restore previous session tabs.\n\n**--profile**\n> Enable profiling.\n\n**--inspect**\n> Show wxPython inspector.\n\n**--page** _PAGE_\n> Select this page of the default editor at start.\n\n**--force-language** _CODE_\n> Force locale to this ISO language code.\n\n**-q**\n> Quiet mode (less output).\n\n**-v**\n> Verbose mode (more output).\n\n**--log** _file_\n> Write log to specified file.\n\n**--log-level** _level_\n> Set logging verbosity level (critical, error, warn, info, debug). Default: debug.\n\n**--version**\n> Print version and exit."],["CAVEATS","Serial port access requires appropriate permissions. Add your user to the serial port group:\n```\nsudo usermod -a -G $(stat -c %G /dev/ttyUSB0) $USER\n```\nLog out and back in after group changes.\n\nThe radio port is typically **/dev/ttyUSB0** on Linux systems."],["HISTORY","**CHIRP** was created by Dan Smith (KK7DS) and has been actively developed since 2008. It began as a tool for Icom IC-91/92 radios and expanded to support hundreds of radio models across multiple manufacturers."]],"e":[],"x":"chirpw"},"cockpit-ws":{"n":"cockpit-ws","t":"Cockpit web service for server administration","s":[["TAGLINE","Cockpit web service for server administration"],["TLDR","Start with **SSH authentication** enabled\n\n```cockpit-ws --local-ssh```\n\nStart HTTP server on a **specific port**\n\n```cockpit-ws --port [port]```\n\n**Bind** to a specific IP address\n\n```cockpit-ws --address [ip_address]```\n\nStart **without TLS**\n\n```cockpit-ws --no-tls```"],["SYNOPSIS","**cockpit-ws** [_options_]"],["DESCRIPTION","**cockpit-ws** is the Cockpit web service that communicates between the browser application and various configuration tools like cockpit-bridge. It serves the web interface and handles authentication.\n\nBy default, it binds to 0.0.0.0 and requires TLS."],["PARAMETERS","**--local-ssh**\n> Enable SSH authentication at 127.0.0.1:22\n\n**--port** _port_\n> Listen on specified port\n\n**--address** _address_\n> Bind to specific IP address (default: 0.0.0.0)\n\n**--no-tls**\n> Run without TLS encryption\n\n**--help**\n> Display help information"],["CAVEATS","Part of the Cockpit suite. Running without TLS is insecure and should only be used behind a reverse proxy. Typically managed by systemd."],["HISTORY","**Cockpit** was developed by Red Hat as a web-based server administration interface. It provides a modern alternative to traditional command-line administration."],["INSTALL","```dnf: sudo dnf install cockpit-ws```\n\n```zypper: sudo zypper install cockpit-ws```"],["SEE ALSO","[cockpit-bridge](/man/cockpit-bridge)(1), [cockpit-tls](/man/cockpit-tls)(8)"],["RESOURCES","```[Source code](https://github.com/cockpit-project/cockpit)```\n\n```[Documentation](https://cockpit-project.org/guide/latest/)```"]],"e":["cockpit-bridge","cockpit-tls"],"x":"cockpit-ws --local-ssh"},"crane-index-filter":{"n":"crane-index-filter","t":"Filter platforms from a container image manifest index","s":[["TAGLINE","Filter platforms from a container image manifest index"],["TLDR","**Filter an index to keep only linux/amd64**\n\n```crane index filter [image] --platform linux/amd64```\n\n**Filter to multiple platforms and tag the result**\n\n```crane index filter [image] --platform linux/amd64 --platform linux/arm64 -t [registry/image:tag]```\n\n**Filter with verbose output**\n\n```crane index filter [image] --platform [linux/amd64] -v```\n\n**Filter allowing insecure registries**\n\n```crane index filter [image] --platform [linux/arm64] --insecure -t [registry/image:filtered]```"],["SYNOPSIS","**crane** **index** **filter** [_options_] _image_"],["PARAMETERS","**--platform** _platform_\n> Platform(s) to keep in the form os/arch[/variant][:osversion]. Can be specified multiple times.\n\n**-t**, **--tag** _tag_\n> Tag to apply to the resulting filtered image.\n\n**--insecure**\n> Allow image references to be fetched without TLS.\n\n**--allow-nondistributable-artifacts**\n> Allow pushing non-distributable (foreign) layers.\n\n**-v**, **--verbose**\n> Enable debug logs.\n\n**-h**, **--help**\n> Show help for the filter command."],["DESCRIPTION","**crane index filter** modifies a remote multi-platform image index by removing platforms that do not match the specified **--platform** flags. The result is a new index containing only the selected architectures and OS combinations.\n\nThis is useful for reducing the size of a multi-architecture image index, creating platform-specific distributions, or removing unwanted platforms before pushing to a registry.\n\nThe **--platform** flag uses the format **os/arch[/variant][:osversion]**, such as `linux/amd64`, `linux/arm64`, or `linux/arm/v7`."],["SEE ALSO","[crane](/man/crane)(1), [crane-index](/man/crane-index)(1), [crane-index-append](/man/crane-index-append)(1), [crane-manifest](/man/crane-manifest)(1), [crane-push](/man/crane-push)(1)"]],"e":["crane","crane-index","crane-index-append","crane-manifest","crane-push"],"x":"crane index filter [image] --platform linux/amd64"},"deluged":{"n":"deluged","t":"daemon component for Deluge BitTorrent client","s":[["TAGLINE","daemon component for Deluge BitTorrent client"],["TLDR","**Start Deluge daemon**\n\n```deluged```\n\n**Start daemon on specific** port\n\n```deluged -p [58846]```\n\n**Start in foreground**\n\n```deluged -d```\n\n**Use specific config** directory\n\n```deluged -c [~/.config/deluge]```\n\n**Start with logging** to file\n\n```deluged -l [/var/log/deluged.log]```\n\n**Enable debug logging**\n\n```deluged -L debug```"],["SYNOPSIS","**deluged** [_options_]"],["PARAMETERS","**-p**, **--port** _PORT_\n> Daemon listen port (default: 58846).\n\n**-i**, **--interface** _IP_\n> Interface to listen on.\n\n**-d**, **--do-not-daemonize**\n> Run in foreground.\n\n**-c**, **--config** _DIR_\n> Configuration directory.\n\n**-l**, **--logfile** _FILE_\n> Log to specified file.\n\n**-L**, **--loglevel** _LEVEL_\n> Log level: none, error, warning, info, debug.\n\n**-P**, **--pidfile** _FILE_\n> PID file location.\n\n**--help**\n> Display help information."],["DESCRIPTION","**deluged** is the daemon component of the Deluge BitTorrent client. It handles all BitTorrent protocol operations and runs in the background, allowing clients to connect for management.\n\nThe daemon architecture allows Deluge to run on headless servers with clients connecting remotely. Multiple clients (GTK, web, console) can connect to the same daemon simultaneously. Downloads continue even when clients disconnect.\n\ndeluged listens on a configurable port for client connections and handles authentication, ensuring secure access to the torrent management functions."],["CONFIGURATION","**~/.config/deluge/**\n> Configuration directory containing core.conf, auth file, and state files."],["CAVEATS","Requires proper port configuration for remote access. Firewall rules may need adjustment. Authentication must be configured for secure remote access. Resource usage depends on active torrent count."],["HISTORY","deluged is the core component of the Deluge BitTorrent client, developed since **2006**. The daemon/client separation was a key design decision enabling flexible deployment scenarios from desktop use to dedicated seedboxes."],["INSTALL","```dnf: sudo dnf install deluge```\n\n```pacman: sudo pacman -S deluge```\n\n```apk: sudo apk add deluge```\n\n```zypper: sudo zypper install deluge```\n\n```nix: nix profile install nixpkgs#deluge```"],["SEE ALSO","[deluge](/man/deluge)(1), [deluge-console](/man/deluge-console)(1)"]],"e":["deluge","deluge-console"],"x":"deluged"},"doctl-account":{"n":"doctl-account","t":"display DigitalOcean account information","s":[["TAGLINE","display DigitalOcean account information"],["TLDR","**Show account information**\n\n```doctl account get```\n\n**Show rate limit status**\n\n```doctl account ratelimit```"],["SYNOPSIS","**doctl** **account** _command_"],["SUBCOMMANDS","**get**\n> Display account information.\n\n**ratelimit**\n> Show API rate limit status."],["DESCRIPTION","**doctl account** displays information about your DigitalOcean account including email, UUID, status, and API rate limits.\n\nThe command provides quick access to account details without navigating the web console. This is useful for verifying which account you're currently authenticated with, especially when managing multiple contexts. The ratelimit subcommand helps monitor API usage to avoid exceeding rate limits during automated operations or intensive scripting."],["INSTALL","```dnf: sudo dnf install doctl```\n\n```pacman: sudo pacman -S doctl```\n\n```apk: sudo apk add doctl```\n\n```brew: brew install doctl```\n\n```nix: nix profile install nixpkgs#doctl```"],["SEE ALSO","[doctl](/man/doctl)(1), [doctl-auth](/man/doctl-auth)(1)"],["RESOURCES","```[Source code](https://github.com/digitalocean/doctl)```\n\n```[Documentation](https://docs.digitalocean.com/reference/doctl/)```"]],"e":["doctl","doctl-auth"],"x":"doctl account get"},"doppler-projects":{"n":"doppler-projects","t":"manage Doppler projects and environments","s":[["TAGLINE","manage Doppler projects and environments"],["TLDR","**List projects** (the bare command lists them; there is no `list` subcommand)\n\n```doppler projects```\n\n**Create a project** with a description\n\n```doppler projects create [name] --description \"[description]\"```\n\n**Get project info**\n\n```doppler projects get [project_id]```\n\n**Rename a project** or change its description\n\n```doppler projects update [project_id] --name [new_name] --description \"[description]\"```\n\n**Delete a project** without a confirmation prompt\n\n```doppler projects delete [project_id] --yes```\n\n**Page through** a long project list\n\n```doppler projects --number [50] --page [2]```"],["SYNOPSIS","**doppler projects** [_subcommand_] [_project_id_] [_flags_]"],["SUBCOMMANDS","(none)\n> Listing is the default action: running **doppler projects** with no subcommand prints the projects you can see.\n\n**get** [_project_id_]\n> Show the details of one project.\n\n**create** [_name_]\n> Create a project. **--name** and **--description** may be given as flags instead of positionally.\n\n**update** [_project_id_]\n> Change a project's **--name** or **--description**.\n\n**delete** [_project_id_]\n> Delete a project and everything in it."],["PARAMETERS","**-p**, **--project** _NAME_\n> Project to act on, overriding the one configured for this directory.\n\n**--name** _NAME_ / **--description** _TEXT_\n> Metadata to set on **create** and **update**.\n\n**-n**, **--number** _N_\n> Maximum number of projects to display. Defaults to 100.\n\n**--page** _N_\n> Page of results to display. Defaults to 1.\n\n**-y**, **--yes**\n> Proceed without an interactive confirmation prompt.\n\n**--json**\n> Print the result as JSON."],["DESCRIPTION","**doppler projects** manages Doppler projects, the top-level container in the Doppler model. A project holds *environments* (development, staging, production), each of which holds one or more *configs*, and it is the configs that actually store secrets.\n\nProjects normally map one-to-one onto an application or service. Because a project is the unit that access control and audit logging hang off, splitting services into separate projects is what lets you grant a team access to one service's secrets without exposing another's.\n\nMost day-to-day work happens in `doppler secrets` and `doppler run`; the project subcommands are mainly used at setup time, when onboarding a new service, or from scripts that provision environments automatically."],["CAVEATS","**delete** removes the project along with every config and secret inside it, and the CLI will only prompt once. Passing **--yes** removes even that prompt, which makes it easy to destroy a production project from a script. Project names are used as identifiers throughout the CLI and API, so renaming one with **update --name** can break service tokens, CI configuration, and `doppler setup` state that referred to the old name."],["INSTALL","```brew: brew install doppler```\n\n```nix: nix profile install nixpkgs#doppler```"],["SEE ALSO","[doppler](/man/doppler)(1), [doppler-secrets](/man/doppler-secrets)(1)"],["RESOURCES","```[Source code](https://github.com/DopplerHQ/cli)```\n\n```[Homepage](https://www.doppler.com)```\n\n```[Documentation](https://docs.doppler.com/docs/cli)```"]],"e":["doppler","doppler-secrets"],"x":"doppler projects"},"fkill":{"n":"fkill","t":"interactive cross-platform process killer","s":[["TAGLINE","interactive cross-platform process killer"],["TLDR","**Interactive process killer**\n\n```fkill```\n\n**Kill by name**\n\n```fkill [process_name]```\n\n**Kill by port**\n\n```fkill :[port]```\n\n**Kill by PID**\n\n```fkill [pid]```\n\n**Force kill**\n\n```fkill -f [process_name]```\n\n**Kill silently** (always exit 0)\n\n```fkill -s [process_name]```\n\n**Kill multiple targets** (name, PID, and port)\n\n```fkill [process_name] [pid] :[port]```\n\n**Force kill after timeout** if SIGTERM fails\n\n```fkill -t [5] [process_name]```"],["SYNOPSIS","**fkill** [_options_] [_target_...]"],["PARAMETERS","**-f**, **--force**\n> Force kill (SIGKILL instead of SIGTERM).\n\n**-s**, **--silent**\n> Silently kill and always exit with code 0.\n\n**-v**, **--verbose**\n> Show process arguments alongside names in the interactive list.\n\n**-t** _N_, **--force-timeout** _N_\n> Force kill processes that didn't exit after _N_ seconds.\n\n**--smart-case**\n> Case-insensitive matching unless the pattern contains uppercase letters.\n\n**--case-sensitive**\n> Force case-sensitive matching of process names."],["DESCRIPTION","**fkill** is a cross-platform process killer with an interactive interface. It provides fuzzy search for selecting processes to terminate, supporting multiple selection methods.\n\nThe tool can kill processes by name, PID, or port number (prefix the port with **:**, e.g. `:8080`). Multiple targets can be passed at once. In interactive mode, it displays a searchable list of running processes with CPU and memory usage, allowing users to select targets with arrow keys and fuzzy matching.\n\nWritten in Node.js and distributed via npm (`npm install --global fkill-cli`), fkill works consistently across Linux, macOS, and Windows."],["CAVEATS","Requires Node.js. On Unix, killing system processes usually requires **sudo**. Matching is fuzzy, so double-check the selected process before confirming — especially in interactive mode where the top match is chosen by default."],["INSTALL","```pacman: sudo pacman -S fkill```"],["SEE ALSO","[kill](/man/kill)(1), [pkill](/man/pkill)(1)"]],"e":["kill","pkill"],"x":"fkill"},"fstrim":{"n":"fstrim","t":"SSD TRIM command issuer","s":[["TAGLINE","SSD TRIM command issuer"],["TLDR","Trim **all** mounted partitions\n\n```sudo fstrim -a```\n\nTrim **specific** partition\n\n```sudo fstrim /```\n\nTrim with **verbose** output\n\n```sudo fstrim -v /```"],["SYNOPSIS","**fstrim** [_OPTIONS_] _mountpoint_"],["DESCRIPTION","**fstrim** discards unused blocks on a mounted filesystem, sending TRIM commands to the underlying storage device. This is primarily useful for SSDs and flash storage to maintain performance and extend device lifespan."],["PARAMETERS","**-a, --all**\n> Trim all mounted filesystems that support discard\n\n**-v, --verbose**\n> Display number of bytes trimmed\n\n**-n, --dry-run**\n> Print what would be done without actually trimming\n\n**-o, --offset** _offset_\n> Byte offset in filesystem to start trimming\n\n**-l, --length** _length_\n> Number of bytes to trim after offset\n\n**-m, --minimum** _size_\n> Minimum contiguous free range to trim"],["CAVEATS","Only works on filesystems whose underlying storage supports discard (most SSDs, NVMe, thin-provisioned devices). Running too frequently provides no benefit; a weekly schedule via the **fstrim.timer** systemd unit is typical. Continuous **discard** mount option is an alternative but generally less efficient than periodic **fstrim**."],["HISTORY","**fstrim** is part of the **util-linux** package, providing TRIM support for SSD optimization on Linux systems."],["INSTALL","```apt: sudo apt install util-linux```\n\n```dnf: sudo dnf install util-linux```\n\n```pacman: sudo pacman -S util-linux```\n\n```apk: sudo apk add fstrim```\n\n```zypper: sudo zypper install util-linux```\n\n```brew: brew install util-linux```\n\n```nix: nix profile install nixpkgs#util-linux```"],["SEE ALSO","[blkdiscard](/man/blkdiscard)(8), [mount](/man/mount)(8)"]],"e":["blkdiscard","mount"],"x":"sudo fstrim -a"},"git-check-ref-format":{"n":"git-check-ref-format","t":"Validate and normalize Git reference names","s":[["TAGLINE","Validate and normalize Git reference names"],["TLDR","**Check if ref name is valid**\n\n```git check-ref-format [refs/heads/branch]```\n\n**Check branch name**\n\n```git check-ref-format --branch [name]```\n\n**Normalize refname**\n\n```git check-ref-format --normalize [refs/heads/branch]```"],["SYNOPSIS","**git** **check-ref-format** [_options_] _refname_"],["PARAMETERS","**--branch**\n> Check as branch name.\n\n**--normalize**\n> Normalize refname.\n\n**--allow-onelevel**\n> Allow single-level refs (refnames without multiple `/`-separated components).\n\n**--no-allow-onelevel**\n> Require hierarchical refs (default).\n\n**--refspec-pattern**\n> Interpret refname as a refspec pattern, allowing a single `*` wildcard."],["DESCRIPTION","**git check-ref-format** validates and normalizes Git reference names according to strict naming rules. This ensures branch and tag names are portable across filesystems and don't conflict with Git internals.\n\nReference names must not contain spaces, tildes, carets, colons, question marks, asterisks, or brackets. They cannot begin or end with slashes, contain consecutive slashes, or end with .lock. Components between slashes cannot begin with dots. These rules prevent filesystem conflicts and shell interpretation issues.\n\nThe command is used programmatically by scripts creating branches or tags to validate user input before attempting operations. Normalization removes redundant slashes and ensures consistent formatting. The --branch option interprets the name as a branch shorthand, expanding it to refs/heads/ format."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-branch](/man/git-branch)(1), [git-tag](/man/git-tag)(1)"]],"e":["git-branch","git-tag"],"x":"git check-ref-format [refs/heads/branch]"},"git-crypt":{"n":"git-crypt","t":"Transparent encryption of files in Git repositories","s":[["TAGLINE","Transparent encryption of files in Git repositories"],["TLDR","**Initialize git-crypt in repo**\n\n```git-crypt init```\n\n**Add GPG user**\n\n```git-crypt add-gpg-user [user@example.com]```\n\n**Unlock repository**\n\n```git-crypt unlock```\n\n**Lock repository**\n\n```git-crypt lock```\n\n**Export symmetric key**\n\n```git-crypt export-key [/path/to/key]```\n\n**Unlock with key file**\n\n```git-crypt unlock [/path/to/key]```"],["SYNOPSIS","**git-crypt** _command_ [_options_]"],["PARAMETERS","**init** [**-k** _keyname_]\n> Generate a key and prepare the repository to use git-crypt.\n\n**add-gpg-user** [**-n**] [**--trusted**] _gpg-user-id_...\n> Add GPG user(s) who can unlock the repository. **-n**, **--no-commit** skips auto-committing the change; **--trusted** skips the GPG web-of-trust check.\n\n**unlock** [_keyfile_]\n> Decrypt the repository. Uses GPG-encrypted keys by default, or a symmetric key file if given.\n\n**lock** [**-a**] [**-k** _keyname_] [**-f**]\n> Re-encrypt files and remove the cached key. **-a**, **--all** locks every key instead of just the default; **-f**, **--force** locks even if the working directory is unclean.\n\n**export-key** [**-k** _keyname_] _file_\n> Export the symmetric key to _file_.\n\n**status** [**-e**] [**-u**] [**-f**]\n> Show encryption status of files. **-e** shows only encrypted files, **-u** only unencrypted files, **-f**, **--fix** encrypts files that should be encrypted but weren't."],["DESCRIPTION","**git-crypt** enables transparent encryption of files in a Git repository. Configured files are encrypted when committed and decrypted when checked out, allowing sensitive data to be stored securely alongside regular code.\n\nEncryption is configured via .gitattributes patterns. Users with the symmetric key or authorized GPG keys can unlock the repository to view and edit protected files."],["CONFIGURATION","**.gitattributes**\n> Defines which files are encrypted using filter and diff attributes.\n\n```\nsecrets/** filter=git-crypt diff=git-crypt\n*.key filter=git-crypt diff=git-crypt\n.env filter=git-crypt diff=git-crypt\n```"],["CAVEATS","Encrypted files appear as binary. File names are not encrypted. Requires GPG for multi-user access. History contains encrypted versions. Not for large files (use git-lfs)."],["HISTORY","git-crypt was created by **Andrew Ayer** to solve the problem of storing secrets in git repositories. It provides simpler setup than alternative approaches like git-secret."],["INSTALL","```apt: sudo apt install git-crypt```\n\n```dnf: sudo dnf install git-crypt```\n\n```pacman: sudo pacman -S git-crypt```\n\n```apk: sudo apk add git-crypt```\n\n```zypper: sudo zypper install git-crypt```\n\n```brew: brew install git-crypt```\n\n```nix: nix profile install nixpkgs#git-crypt```"],["SEE ALSO","[git](/man/git)(1), [gpg](/man/gpg)(1), [git-secret](/man/git-secret)(1)"],["RESOURCES","```[Source code](https://github.com/AGWA/git-crypt)```"]],"e":["git","gpg","git-secret"],"x":"git-crypt init"},"gomplate":{"n":"gomplate","t":"Flexible command-line template renderer","s":[["TAGLINE","Flexible command-line template renderer"],["TLDR","**Render** a template file\n\n```gomplate -f [template.tmpl]```\n\n**Render** with an environment datasource\n\n```gomplate -d [config=env:] -f [template.tmpl]```\n\n**Render** from stdin\n\n```cat [template.tmpl] | gomplate```\n\n**Output** to a file\n\n```gomplate -f [in.tmpl] -o [out.txt]```"],["SYNOPSIS","**gomplate** [*options*]"],["DESCRIPTION","**gomplate** is a template CLI built on Go templates. It can pull **datasources** from files, HTTP, environment variables, AWS/GCP metadata, Consul, Vault, and more, then render text configs, scripts, or manifests. Popular for generating Kubernetes YAML, dotenv files, and bootstrapping scripts without a full programming run."],["PARAMETERS","**-f**, **--file** *path*\n\n> Template file (repeatable / dir modes available).\n\n**-o**, **--out** *path*\n\n> Output file (default stdout).\n\n**-d**, **--datasource** *alias=url*\n\n> Define a named datasource (e.g. **config=file:///…**, **env:/**).\n\n**-c**, **--context** *alias=url*\n\n> Attach datasource into the template context.\n\n**--input-dir** / **--output-dir**\n\n> Batch-render directory trees.\n\nExtensive datasource URL schemes are documented at https://docs.gomplate.ca/"],["CAVEATS","Template injection risks apply if templates or datasources are untrusted. Network datasources need connectivity and credentials. Syntax is Go text/template plus gomplate functions—not Jinja/Mustache."],["INSTALL","```apk: sudo apk add gomplate```\n\n```brew: brew install gomplate```\n\n```nix: nix profile install nixpkgs#gomplate```"],["SEE ALSO","[envsubst](/man/envsubst)(1), [jinja2](/man/jinja2)(1), [ytt](/man/ytt)(1), [helm](/man/helm)(1)"],["RESOURCES","```[Source code](https://github.com/hairyhenderson/gomplate)```\n\n```[Documentation](https://docs.gomplate.ca/)```"]],"e":["envsubst","jinja2","ytt","helm"],"x":"gomplate -f [template.tmpl]"},"gova":{"n":"gova","t":"Build and hot-reload native desktop apps written with the Gova Go GUI framework","s":[["TAGLINE","Build and hot-reload native desktop apps written with the Gova Go GUI framework"],["TLDR","**Install** the CLI\n\n```go install github.com/nv404/gova/cmd/gova@latest```\n\n**Start the dev loop** with file-watching and hot reload\n\n```gova dev```\n\n**Build and run** once without watching for changes\n\n```gova run```\n\n**Compile a release binary** for the current platform\n\n```gova build```\n\n**Build a stripped, smaller release binary**\n\n```gova build -ldflags \"-s -w\"```\n\n**Build with a custom output name**\n\n```gova build -o [myapp]```\n\n**Run dev loop against a specific entry package**\n\n```gova dev [./cmd/myapp]```"],["SYNOPSIS","**gova** _command_ [_flags_] [_package_]"],["PARAMETERS","**dev**\n> Watch the working directory, rebuild on file save, and relaunch the window. Ignores **.git**, **node_modules**, **vendor**, and **_test.go** files.\n\n**run**\n> Build and launch the application once without starting a file watcher. Useful for CI pipelines or one-shot verifications.\n\n**build**\n> Compile a single static binary for the host platform. Accepts Go build flags such as **-ldflags** and **-o**.\n\n**-ldflags** _flags_\n> Pass linker flags through to the underlying **go build** call (e.g. **\"-s -w\"** to strip symbols).\n\n**-o** _name_\n> Set the output binary name produced by **gova build**."],["DESCRIPTION","**Gova** is a declarative GUI framework for Go that emits a single static binary for macOS, Windows, and Linux. The **gova** CLI wraps **go build** and **go run** with file-watching, cgo setup, and platform-specific native toolkit plumbing so developers can iterate quickly on a Gova application without hand-writing build tooling.\n\nComponents are written in pure Go with a reactive state model and typed props. Typed platform dialogs (file pickers, alerts, notifications) are exposed through the framework and linked against native system libraries via cgo.\n\nDuring **gova dev**, saving any watched **.go** file triggers an incremental rebuild and relaunches the window, preserving development flow. **gova build** produces a release-ready binary that can be distributed without a runtime dependency on Go."],["CAVEATS","The project is **pre-1.0** — the CLI surface and framework API are still evolving. Requires **Go 1.26+** and a working **C toolchain** on every target platform because of cgo. The name collides with unrelated Go packages (**golang-collections/gova**), with **govc** (VMware vSphere CLI), and with **govm** (Go version manager) — none of which are related."],["HISTORY","Gova was created by **NV404** and published at **github.com/NV404/gova** with documentation at **gova.dev**. It grew out of the wider movement to build native desktop UIs in Go (alongside Fyne and Wails) while keeping the developer experience closer to modern reactive frameworks."],["SEE ALSO","[go](/man/go)(1)"]],"e":["go"],"x":"go install github.com/nv404/gova/cmd/gova@latest"},"halo":{"n":"halo","t":"RLM-based analyzer for AI agent execution traces","s":[["TAGLINE","RLM-based analyzer for AI agent execution traces"],["TLDR","**Analyze a trace file** with a diagnostic prompt\n\n```halo [traces.jsonl] -p \"[Diagnose errors and suggest fixes]\"```\n\n**Use a specific model** for the analysis run\n\n```halo [traces.jsonl] -p \"[prompt]\" --model [gpt-5.4-mini]```\n\n**Point at an OpenAI-compatible provider**\n\n```halo [traces.jsonl] -p \"[prompt]\" --base-url [https://openrouter.ai/api/v1]```\n\n**Emit telemetry** for the HALO run itself\n\n```halo [traces.jsonl] -p \"[prompt]\" --telemetry```"],["SYNOPSIS","**halo** _TRACE_PATH_ **--prompt** _TEXT_ [_options_]\n\n**halo** _TRACE_PATH_ **-p** _TEXT_ [_options_]"],["DESCRIPTION","**halo** is the command-line front end for the HALO engine, a recursively structured trace analyzer built around **RLM** (Recursive Language Models). It ingests OpenTelemetry-compatible JSONL execution traces from agent harnesses, decomposes failures across many runs, and produces a report of systemic issues rather than overfitting to a single bad trace.\n\nThe CLI sends a user prompt plus the trace file to a root agent that can spawn subagents, summarize long contexts, and synthesize findings. Output is intended to feed back into coding agents such as **claude** or **cursor** so harness prompts, tools, and guardrails can be improved iteratively. A separate desktop app and hosted service exist, but **halo** is the local, scriptable entry point installed from the **halo-engine** Python package."],["PARAMETERS","**--prompt**, **-p** _TEXT_\n> Required user prompt describing what to look for in the traces.\n\n**--model**, **-m** _MODEL_\n> Model used for root and subagent calls (default `gpt-5.4-mini`).\n\n**--synthesis-model** _MODEL_\n> Cheaper model for trace summarization (defaults to **--model**).\n\n**--compaction-model** _MODEL_\n> Cheaper model for context compaction (defaults to **--model**).\n\n**--max-depth** _N_\n> Maximum subagent recursion depth (default 2).\n\n**--max-turns** _N_\n> Maximum turns per agent (default 20).\n\n**--max-parallel** _N_\n> Maximum concurrent subagents (default 10).\n\n**--base-url** _URL_\n> OpenAI-compatible API base URL.\n\n**--api-key** _KEY_\n> Provider API key (falls back to **OPENAI_API_KEY**).\n\n**--header**, **-H** _NAME:VALUE_\n> Extra provider header; repeatable.\n\n**--temperature** _N_\n> Sampling temperature forwarded to the model.\n\n**--max-output-tokens** _N_\n> Maximum output tokens forwarded to the model.\n\n**--telemetry**\n> Emit OpenInference telemetry for HALO's own LLM and tool activity."],["CONFIGURATION","**OPENAI_API_KEY**\n> Default credentials for the configured provider.\n\n**OPENAI_BASE_URL**\n> Default API base URL when **--base-url** is not set.\n\n**CATALYST_OTLP_TOKEN**\n> When **--telemetry** is enabled, upload spans to inference.net Catalyst over OTLP."],["CAVEATS","**halo** requires a valid API key and provider access for the chosen model. Trace files must be JSONL in an OpenTelemetry-compatible format produced by an integrated harness. Analysis cost scales with trace size, recursion depth, and model choice; using smaller models for synthesis and compaction is recommended on large runs."],["HISTORY","HALO was developed by **Context Labs** (inference.net) in 2026 as a methodology and engine for improving agent harnesses from production traces. The **halo** CLI ships with the open-source **halo-engine** package on PyPI."],["INSTALL","```nix: nix profile install nixpkgs#halo```"],["SEE ALSO","[claude](/man/claude)(1), [codex](/man/codex)(1), [jq](/man/jq)(1)"],["RESOURCES","```[Source code](https://github.com/context-labs/HALO)```\n\n```[Homepage](https://inference.net/products/halo)```\n\n```[Documentation](https://docs.inference.net)```"]],"e":["claude","codex","jq"],"x":"halo [traces.jsonl] -p \"[Diagnose errors and suggest fixes]\""},"hexowl":{"n":"hexowl","t":"Lightweight programmer's calculator with variables and functions","s":[["TAGLINE","Lightweight programmer's calculator with variables and functions"],["TLDR","**Start the interactive calculator**\n\n```hexowl```\n\n**Perform a hex calculation**\n\n```hexowl -e \"0xFF + 0x10\"```\n\n**Mix number bases** in expressions\n\n```hexowl -e \"255 + 0b1010 + 0xF\"```\n\n**Define and use variables**\n\n```hexowl -e \"x = 42; x * 2\"```"],["SYNOPSIS","**hexowl** [**-e** _expression_] [_options_]"],["DESCRIPTION","**hexowl** is a lightweight and flexible programmer's calculator with support for user-defined **variables** and **functions**. Its main purpose is to perform operations on numbers regardless of their base — a single expression can contain **decimal**, **hexadecimal**, and **binary** numbers mixed together.\n\nUsers can declare variables for reuse and define custom functions using the **->** operator. The calculator supports standard arithmetic operations, bitwise operations, and base conversions, making it useful for low-level programming, embedded development, and reverse engineering tasks."],["CAVEATS","Has no external dependencies. Function syntax uses the **->** operator which differs from most calculator conventions. Results are displayed in decimal by default."],["HISTORY","**hexowl** was created by **DECE2183** and is written in **Go**. A web implementation is also available at hexowl.ru for browser-based usage."],["INSTALL","```aur: yay -S hexowl```"],["SEE ALSO","[bc](/man/bc)(1), [dc](/man/dc)(1), [kalker](/man/kalker)(1)"]],"e":["bc","dc","kalker"],"x":"hexowl"},"ibmcloud-cr":{"n":"ibmcloud-cr","t":"manages IBM Cloud Container Registry","s":[["TAGLINE","manages IBM Cloud Container Registry"],["TLDR","**Login to registry**\n\n```ibmcloud cr login```\n\n**List images**\n\n```ibmcloud cr image-list```\n\n**Create namespace**\n\n```ibmcloud cr namespace-add [namespace]```\n\n**Build image**\n\n```ibmcloud cr build -t [registry/namespace/image:tag] [.]```\n\n**Remove image**\n\n```ibmcloud cr image-rm [image]```\n\n**List namespaces**\n\n```ibmcloud cr namespace-list```"],["SYNOPSIS","**ibmcloud cr** _command_ [_options_]"],["PARAMETERS","_COMMAND_\n> Container Registry command.\n\n**login**\n> Log the local Docker daemon in to the IBM Cloud registry.\n\n**image-list**\n> List images in your account/namespaces.\n\n**image-rm** _IMAGE_\n> Remove one or more images from the registry.\n\n**image-tag** _SRC_ _DST_\n> Add a new tag to an existing image.\n\n**namespace-add** _NAME_\n> Create a namespace.\n\n**namespace-list**\n> List namespaces in the targeted account.\n\n**namespace-rm** _NAME_\n> Delete a namespace (must be empty).\n\n**build** _CONTEXT_\n> Build a container image with IBM Cloud's remote builder and push the result.\n\n**va** _IMAGE_\n> Show Vulnerability Advisor results for an image.\n\n**quota**\n> Display storage and pull-traffic quota for the account.\n\n**region-set** _REGION_\n> Switch the registry region (e.g., _us-south_, _eu-de_, _jp-tok_).\n\n**--help**\n> Display help information."],["DESCRIPTION","**ibmcloud cr** manages IBM Cloud Container Registry. It provides private Docker image storage and vulnerability scanning.\n\nThe tool handles namespaces, images, and registry authentication. It integrates with IBM Cloud Kubernetes Service."],["CAVEATS","Registry plugin required. Namespace quotas apply. Vulnerability scanning available."],["HISTORY","ibmcloud cr is the Container Registry plugin for **IBM Cloud** CLI."],["SEE ALSO","[ibmcloud](/man/ibmcloud)(1), [docker](/man/docker)(1), [ibmcloud-ks](/man/ibmcloud-ks)(1)"]],"e":["ibmcloud","docker","ibmcloud-ks"],"x":"ibmcloud cr login"},"l2ping":{"n":"l2ping","t":"sends L2CAP echo requests to a Bluetooth device, similar to ping for IP networks","s":[["TAGLINE","sends L2CAP echo requests to a Bluetooth device, similar to ping for IP networks"],["TLDR","**Ping a Bluetooth device**\n\n```l2ping [AA:BB:CC:DD:EE:FF]```\n\n**Send specific number of packets**\n\n```l2ping -c [5] [AA:BB:CC:DD:EE:FF]```\n\n**Set packet size**\n\n```l2ping -s [1024] [AA:BB:CC:DD:EE:FF]```\n\n**Use specific Bluetooth adapter**\n\n```l2ping -i [hci0] [AA:BB:CC:DD:EE:FF]```\n\n**Set timeout**\n\n```l2ping -t [10] [AA:BB:CC:DD:EE:FF]```"],["SYNOPSIS","**l2ping** [_options_] _bd_addr_"],["PARAMETERS","**-i** _hciX_\n> Use specific Bluetooth adapter.\n\n**-c** _count_\n> Number of packets to send.\n\n**-s** _size_\n> Size of data packets in bytes.\n\n**-t** _timeout_\n> Response timeout in seconds.\n\n**-d** _delay_\n> Delay between pings in seconds.\n\n**-f**\n> Flood ping mode (no delay between packets).\n\n**-r**\n> Reverse ping mode (send echo response instead of echo request).\n\n**-v**\n> Verify that response payload is identical to request payload."],["DESCRIPTION","**l2ping** sends L2CAP echo requests to a Bluetooth device, similar to ping for IP networks. Measures round-trip time, detects packet loss, and tests Bluetooth connectivity. Part of the BlueZ Bluetooth stack."],["CAVEATS","Requires root privileges. Not all Bluetooth devices respond to L2CAP pings. The Bluetooth adapter must be up and enabled."],["INSTALL","```apt: sudo apt install bluez```\n\n```dnf: sudo dnf install bluez```\n\n```pacman: sudo pacman -S bluez```\n\n```apk: sudo apk add bluez```\n\n```zypper: sudo zypper install bluez```\n\n```brew: brew install bluez```\n\n```nix: nix profile install nixpkgs#bluez```"],["SEE ALSO","[hcitool](/man/hcitool)(1), [bluetoothctl](/man/bluetoothctl)(1)"]],"e":["hcitool","bluetoothctl"],"x":"l2ping [AA:BB:CC:DD:EE:FF]"},"libreoffice":{"n":"libreoffice","t":"free, open-source office productivity suite including word processor","s":[["TAGLINE","free, open-source office productivity suite including word processor"],["TLDR","**Open a document**\n\n```libreoffice [path/to/document]```\n\n**Convert document to PDF**\n\n```libreoffice --headless --convert-to pdf [document.docx]```\n\n**Convert to another format**\n\n```libreoffice --headless --convert-to [xlsx] [spreadsheet.ods]```\n\n**Convert multiple files**\n\n```libreoffice --headless --convert-to pdf [*.docx]```\n\n**Specify output directory**\n\n```libreoffice --headless --convert-to pdf --outdir [/output] [document.docx]```\n\n**Start Writer** (word processor)\n\n```libreoffice --writer```\n\n**Start Calc** (spreadsheet)\n\n```libreoffice --calc```\n\n**Start Impress** (presentations)\n\n```libreoffice --impress```"],["SYNOPSIS","**libreoffice** [_--headless_] [_--convert-to format_] [_--outdir path_] [_--writer|--calc|--impress|--draw_] [_options_] [_files_]"],["PARAMETERS","**--headless**\n> Run without GUI (for conversion/scripting).\n\n**--convert-to** _format[:filter]_\n> Convert to specified format and exit.\n\n**--outdir** _path_\n> Output directory for converted files.\n\n**--writer**\n> Open LibreOffice Writer.\n\n**--calc**\n> Open LibreOffice Calc.\n\n**--impress**\n> Open LibreOffice Impress.\n\n**--draw**\n> Open LibreOffice Draw.\n\n**--math**\n> Open LibreOffice Math.\n\n**--base**\n> Open LibreOffice Base.\n\n**--print-to-file** _file_\n> Print to file instead of printer.\n\n**--infilter** _filter_\n> Force input filter.\n\n**-n**, **--new**\n> Open new empty document.\n\n**-o**, **--view**\n> Open in read-only mode.\n\n**--safe-mode**\n> Start in safe mode (disabled extensions).\n\n**--nofirststartwizard**\n> Skip first-start wizard.\n\n**--quickstart**\n> Enable quickstarter.\n\n**--terminate_after_init**\n> Exit after loading (for testing).\n\n**--norestore**\n> Disable automatic crash recovery.\n\n**--nologo**\n> Suppress the splash screen on startup.\n\n**--nolockcheck**\n> Disable checks for remote running instances.\n\n**--invisible**\n> Start without any UI (no window, no tray icon).\n\n**--cat** _file_\n> Dump document text content to standard output, then exit.\n\n**-p** _file..._\n> Print the given files to the default printer and exit.\n\n**--pt** _printer_ _file..._\n> Print to the named printer and exit.\n\n**--version**\n> Print version information and exit.\n\n**--help**\n> Display help."],["CONVERSION FORMATS","**pdf**\n> PDF document.\n\n**html**\n> HTML web page.\n\n**docx**\n> Microsoft Word 2007+.\n\n**doc**\n> Microsoft Word 97-2003.\n\n**xlsx**\n> Microsoft Excel 2007+.\n\n**xls**\n> Microsoft Excel 97-2003.\n\n**pptx**\n> Microsoft PowerPoint 2007+.\n\n**odt**\n> OpenDocument Text.\n\n**ods**\n> OpenDocument Spreadsheet.\n\n**csv**\n> Comma-separated values.\n\n**txt**\n> Plain text."],["DESCRIPTION","**LibreOffice** is a free, open-source office productivity suite including word processor (Writer), spreadsheet (Calc), presentation software (Impress), drawing application (Draw), formula editor (Math), and database manager (Base).\n\nThe command line enables powerful automation. The **--headless** mode runs without GUI, essential for server-side document conversion. Combined with **--convert-to**, it can batch-convert documents between formats - commonly used for generating PDFs from various office documents.\n\nConversion supports filters for fine-tuned output. Format:filter syntax (e.g., csv:Text - txt - csv (StarCalc)) allows specifying exact export options. Available filters depend on installed components.\n\nFor scripting, LibreOffice supports macros in Basic, Python, JavaScript, and BeanShell. The UNO API provides programmatic document manipulation. Combined with headless mode, this enables document generation pipelines.\n\nThe quickstarter feature keeps LibreOffice partially loaded for faster document opening, useful on systems where it's frequently used."],["CAVEATS","Headless conversion requires the full LibreOffice installation. Some complex documents may not convert perfectly. Conversion spawns a separate process - watch memory usage with many files. Filter syntax can be complex. Some features require Java runtime."],["HISTORY","**LibreOffice** was created in **2010** as a fork of OpenOffice.org after Oracle acquired Sun Microsystems. The Document Foundation was established to guide development. It has become the leading open-source office suite, included by default in most Linux distributions and available for Windows and macOS."],["INSTALL","```dnf: sudo dnf install libreoffice```\n\n```apk: sudo apk add libreoffice-common```\n\n```zypper: sudo zypper install libreoffice```\n\n```nix: nix profile install nixpkgs#libreoffice```"],["SEE ALSO","[pandoc](/man/pandoc)(1), [wkhtmltopdf](/man/wkhtmltopdf)(1), [unoconv](/man/unoconv)(1)"]],"e":["pandoc","wkhtmltopdf","unoconv"],"x":"libreoffice [path/to/document]"},"lynis":{"n":"lynis","t":"security auditing tool for Unix-based systems","s":[["TAGLINE","security auditing tool for Unix-based systems"],["TLDR","Check for **updates**\n\n```sudo lynis update info```\n\nRun **full system audit**\n\n```sudo lynis audit system```\n\nRun audit **quietly** (non-interactive)\n\n```sudo lynis audit system --quick```\n\nAudit a **Dockerfile**\n\n```lynis audit dockerfile [path/to/Dockerfile]```\n\nRun only tests from a **specific category**\n\n```sudo lynis audit system --tests-from-category [networking]```"],["SYNOPSIS","**lynis** [_mode_] [_options_]"],["DESCRIPTION","**lynis** is a security auditing tool for Unix-based systems. It performs an extensive health scan covering system hardening, vulnerability scanning, and compliance testing."],["PARAMETERS","**audit system**\n> Perform full system security audit.\n\n**audit dockerfile** _FILE_\n> Audit a Dockerfile for security issues.\n\n**update info**\n> Check for lynis updates.\n\n**show profiles**\n> List available audit profiles.\n\n**show settings**\n> Display current settings.\n\n**show categories**\n> List available test categories.\n\n**show commands**\n> Show available lynis commands.\n\n**-c**, **--cronjob**\n> Run as cronjob (non-interactive, with auto-rotation of log files)\n\n**-Q**, **--quick**\n> Quick mode, do not wait for user input.\n\n**-q**, **--quiet**\n> Suppress output to screen. Implies --quick.\n\n**--no-colors**\n> Disable colored output.\n\n**--pentest**\n> Non-privileged scan, show points of interest for penetration testing.\n\n**--forensics**\n> Perform forensics on a running or mounted system.\n\n**--tests** _TEST-IDs_\n> Only run specific test(s).\n\n**--tests-from-category** _CATEGORY_\n> Only run tests belonging to the specified category.\n\n**--profile** _FILE_\n> Use alternative audit profile.\n\n**--logfile** _FILE_\n> Define alternative log file location.\n\n**--report-file** _FILE_\n> Define alternative report file location."],["CAVEATS","Requires root privileges for complete audit. Reports are stored in /var/log/lynis-report.dat. Some tests may be distribution-specific."],["HISTORY","Developed by CISOfy as an open-source security auditing tool, evolved from rkhunter concepts with broader security coverage."],["INSTALL","```dnf: sudo dnf install lynis```\n\n```pacman: sudo pacman -S lynis```\n\n```zypper: sudo zypper install lynis```\n\n```brew: brew install lynis```\n\n```nix: nix profile install nixpkgs#lynis```"],["SEE ALSO","[rkhunter](/man/rkhunter)(8), [chkrootkit](/man/chkrootkit)(8)"]],"e":["rkhunter","chkrootkit"],"x":"sudo lynis update info"},"lzma":{"n":"lzma","t":"compresses files using the Lempel-Ziv-Markov chain Algorithm","s":[["TAGLINE","compresses files using the Lempel-Ziv-Markov chain Algorithm"],["TLDR","**Compress file**\n\n```lzma [file]```\n\n**Decompress file**\n\n```lzma -d [file.lzma]```\n\n**Keep original file**\n\n```lzma -k [file]```\n\n**Compress to stdout**\n\n```lzma -c [file] > [file.lzma]```\n\n**Set compression level**\n\n```lzma -9 [file]```\n\n**List** information about a compressed file\n\n```lzma -l [file.lzma]```\n\n**Test archive integrity**\n\n```lzma -t [file.lzma]```"],["SYNOPSIS","**lzma** [_options_] [_files_...]"],["PARAMETERS","**-d**, **--decompress**\n> Decompress.\n\n**-z**, **--compress**\n> Compress (default).\n\n**-k**, **--keep**\n> Keep original files.\n\n**-c**, **--stdout**\n> Write to stdout.\n\n**-1** to **-9**\n> Compression level.\n\n**-e**, **--extreme**\n> Maximum compression.\n\n**-t**, **--test**\n> Test integrity.\n\n**-f**, **--force**\n> Force overwrite.\n\n**-v**, **--verbose**\n> Verbose mode; show compression ratio and other details.\n\n**-q**, **--quiet**\n> Suppress warnings; specify twice to suppress errors too.\n\n**-l**, **--list**\n> List information about compressed files.\n\n**-T**, **--threads** _NUM_\n> Set number of worker threads (0 for auto-detect based on CPU cores)."],["DESCRIPTION","**lzma** compresses files using the Lempel-Ziv-Markov chain Algorithm. It provides high compression ratios at the cost of slower compression speed.\n\nThe lzma command is typically a symlink to xz, which supports the legacy .lzma format alongside the newer .xz format."],["COMPARISON","```\nTool     Compression  Speed\ngzip     Medium       Fast\nbzip2    Good         Medium\nlzma/xz  Best         Slow\n```"],["CAVEATS","Slower compression than gzip. High memory usage at extreme levels. Legacy format; prefer xz for new files."],["HISTORY","LZMA was developed by **Igor Pavlov** for the 7-Zip archiver in **1998**. The algorithm was later standardized and the xz format created as a modern container."],["INSTALL","```dnf: sudo dnf install xz```\n\n```pacman: sudo pacman -S xz```\n\n```apk: sudo apk add xz```\n\n```zypper: sudo zypper install xz```\n\n```brew: brew install xz```\n\n```nix: nix profile install nixpkgs#xz```"],["SEE ALSO","[xz](/man/xz)(1), [unlzma](/man/unlzma)(1), [gzip](/man/gzip)(1), [bzip2](/man/bzip2)(1), [zstd](/man/zstd)(1), [7z](/man/7z)(1)"]],"e":["xz","unlzma","gzip","bzip2","zstd","7z"],"x":"lzma [file]"},"manhole":{"n":"manhole","t":"provides remote debugging access to Python processes","s":[["TAGLINE","provides remote debugging access to Python processes"],["TLDR","**Connect to process**\n\n```manhole [pid]```\n\n**Connect to Unix socket**\n\n```manhole [/var/run/manhole.sock]```\n\n**Connect with timeout**\n\n```manhole -t [30] [pid]```\n\n**Verbose connection**\n\n```manhole -v [pid]```"],["SYNOPSIS","**manhole** [_options_] _target_"],["PARAMETERS","_TARGET_\n> Process ID or socket path.\n\n**-t** _SECONDS_\n> Connection timeout.\n\n**-v**\n> Verbose output.\n\n**--help**\n> Display help information."],["DESCRIPTION","**manhole** provides remote debugging access to Python processes. It opens an interactive Python shell.\n\nThe tool connects to processes running the manhole library. Useful for debugging live applications."],["CAVEATS","Target must use manhole library. Security considerations for production. Python-specific."],["HISTORY","manhole was created to provide runtime debugging access to **Python** applications without restart."],["SEE ALSO","[gdb](/man/gdb)(1), [strace](/man/strace)(1), [python](/man/python)(1)"]],"e":["gdb","strace","python"],"x":"manhole [pid]"},"matchpathcon":{"n":"matchpathcon","t":"looks up the default SELinux security context for a given file path","s":[["TAGLINE","looks up the default SELinux security context for a given file path"],["TLDR","Lookup the **persistent security context** setting of an absolute path\n\n```matchpathcon [/path/to/file]```\n\nRestrict lookup to settings on a **specific file type**\n\n```matchpathcon -m [file|dir|pipe|chr_file|blk_file|lnk_file|sock_file] [/path/to/file]```\n\n**Verify** that the persistent and current security context agree\n\n```matchpathcon -V [/path/to/file]```"],["SYNOPSIS","**matchpathcon** [_options_] _path_..."],["PARAMETERS","**-m _type_**\n> Restrict lookup to a specific file type: file, dir, pipe, chr_file, blk_file, lnk_file, or sock_file\n\n**-V**\n> Verify that the current context matches the expected persistent context\n\n**-n**\n> Do not display the path in output\n\n**-N**\n> Do not look up the context in the policy\n\n**-P _policy_path_**\n> Use an alternate policy root path"],["DESCRIPTION","**matchpathcon** looks up the default SELinux security context for a given file path based on the file context configuration. It shows what context a file **should** have according to the SELinux policy, regardless of its current context.\n\nThis is useful for diagnosing SELinux issues—comparing the expected context (from matchpathcon) with the actual context (from **ls -Z**) reveals whether a file has been mislabeled.\n\nThe **-V** (verify) option directly compares the expected and actual contexts, reporting mismatches that may cause SELinux denials."],["CAVEATS","Only works on systems with SELinux enabled. The lookup is based on file path patterns, not actual file attributes. A path may match multiple patterns; the most specific pattern wins. This command shows policy defaults, not runtime overrides."],["INSTALL","```apt: sudo apt install selinux-utils```\n\n```dnf: sudo dnf install libselinux-utils```\n\n```apk: sudo apk add libselinux-utils```"],["SEE ALSO","[semanage-fcontext](/man/semanage-fcontext)(8), [restorecon](/man/restorecon)(8), [chcon](/man/chcon)(1), [secon](/man/secon)(1), [ls](/man/ls)(1)"]],"e":["semanage-fcontext","restorecon","chcon","secon","ls"],"x":"matchpathcon [/path/to/file]"},"matrirc":{"n":"matrirc","t":"IRC gateway to Matrix with end-to-end encryption","s":[["TAGLINE","IRC gateway to Matrix with end-to-end encryption"],["TLDR","**Run the server** for the first time with registration enabled\n\n```matrirc --allow-register```\n\n**Run the server** in normal mode (after registration is complete)\n\n```matrirc```\n\n**Configure** a directory to store encrypted media files\n\n```matrirc --media-dir [path/to/media]```\n\n**Set the URL prefix** used to serve uploaded media\n\n```matrirc --media-url [https://media.example.com/]```\n\n**Connect from an IRC client** by pointing it at the matrirc port (default 6667) and using your registered Matrix password."],["SYNOPSIS","**matrirc** [_--allow-register_] [_--media-dir path_] [_--media-url url_]"],["PARAMETERS","**--allow-register**\n> Allow new IRC clients to bind a Matrix account on first connection. Once a nick/password is bound, restart **without** this flag for normal operation.\n\n**--media-dir** _path_\n> Local directory where encrypted Matrix media is decrypted and stored when downloaded for IRC clients.\n\n**--media-url** _url_\n> URL prefix substituted into messages so IRC clients receive a clickable link to media saved in **--media-dir**."],["DESCRIPTION","**matrirc** is a small, single-binary IRC gateway that lets a standard IRC client connect to a Matrix homeserver. Unlike a server-side application service such as **matrix-appservice-irc**, matrirc runs locally in front of the user's IRC client and translates between the two protocols. It supports Matrix end-to-end encryption (Olm/Megolm) and cross-signing device verification, so encrypted rooms remain readable.\n\nOn first launch with **--allow-register**, the server prompts for Matrix login credentials when an IRC client connects with a chosen nick and password. After successful login the nick/password pair is bound to the Matrix session, and subsequent reconnections restore the session without going through registration again. Encrypted media attachments can be decrypted and exposed to the IRC client through a local directory served behind a configurable URL prefix.\n\nThe project is written in **Rust** and aimed at users who prefer the IRC client experience but participate in Matrix rooms."],["CAVEATS","Several Matrix features are not yet implemented: topic and icon change notifications, joining rooms from the IRC side, @mentions, and voice/video calls. Media cleanup is manual - downloaded files in **--media-dir** must be pruned by the user. Restarting with **--allow-register** is sometimes required to add a second account; once binding is done, omit the flag."],["CONFIGURATION","State (sessions, encryption keys, room cache) is persisted under matrirc's data directory. The exact path depends on the build but typically lives under **$XDG_DATA_HOME/matrirc** or the directory matrirc is started in. Connect using any IRC client by pointing it at the matrirc-bound port (default IRC port 6667) with the password you registered."],["HISTORY","**matrirc** was created by **Dominique Martinet** (**martinetd**) and is hosted at **github.com/martinetd/matrirc**. It was developed to give long-time IRC users a lightweight, locally-run bridge into Matrix without operating a homeserver-level appservice."],["INSTALL","```aur: yay -S matrirc```"],["SEE ALSO","[weechat](/man/weechat)(1), [irssi](/man/irssi)(1)"]],"e":["weechat","irssi"],"x":"matrirc --allow-register"},"menyoki":{"n":"menyoki","t":"Screenshot, screencast, and image operations on the command line","s":[["TAGLINE","Screenshot, screencast, and image operations on the command line"],["TLDR","**Capture a screenshot** of the focused window and save as PNG\n\n```menyoki capture png save```\n\n**Record a screencast** of a window as a GIF\n\n```menyoki record gif save```\n\n**Split a GIF** into individual frames\n\n```menyoki split [animation.gif]```\n\n**Edit an image** by converting to grayscale\n\n```menyoki edit --grayscale [path/to/image] png save```\n\n**Analyze an image** and print its details\n\n```menyoki analyze [path/to/image]```\n\n**View an image** in the terminal\n\n```menyoki view [path/to/image]```"],["SYNOPSIS","**menyoki** [_flags_] [_options_] _subcommand_"],["PARAMETERS","**-v**, **--verbose**\n> Increase logging verbosity.\n\n**-q**, **--quiet**\n> Do not show output.\n\n**-c**, **--config** _FILE_\n> Set the configuration file.\n\n**--color** _HEX_\n> Set the main color (default: 3AA431).\n\n**capture**\n> Capture a screenshot. Accepts format subcommand (png, jpg, webp, bmp, ico, tiff, tga, pnm, ff, exr).\n\n**record**\n> Record an animation. Accepts format subcommand (gif, apng).\n\n**split**\n> Split an animation into frames.\n\n**make**\n> Make an animation from frames.\n\n**edit**\n> Edit an image. Supports --grayscale, --invert, --convert, --crop, --resize, --rotate, --flip, --blur, --hue, --contrast, --brightness.\n\n**analyze**\n> Analyze an image and print its properties.\n\n**view**\n> View an image in the terminal."],["DESCRIPTION","**menyoki** is a screenshot and screencast utility that can also perform various image operations such as making/splitting GIFs and modifying/analyzing/viewing image files. Originally designed for recording terminal windows, it can be adapted for other purposes.\n\nSubcommands include **capture** for screenshots, **record** for screencasts, **split** and **make** for GIF operations, **edit** for image manipulation, **analyze** for image analysis, and **view** for terminal image display. Output formats include PNG, JPG, WebP, BMP, ICO, TIFF, PNM, and GIF/APNG.\n\nActions are composed as a chain of subcommands: the action subcommand is mandatory, while format and **save** subcommands are optional. During capture or record, key bindings allow selecting the target area or window."],["CAVEATS","Screen capture requires X11 on Linux. Key bindings are only active during capture or record operations."],["HISTORY","**menyoki** was created by **Orhun Parmaksız** and is written in **Rust**."],["INSTALL","```pacman: sudo pacman -S menyoki```\n\n```nix: nix profile install nixpkgs#menyoki```"],["SEE ALSO","[scrot](/man/scrot)(1), [import](/man/import)(1), [ffmpeg](/man/ffmpeg)(1)"]],"e":["scrot","import","ffmpeg"],"x":"menyoki capture png save"},"minecraft":{"n":"minecraft","t":"launches the Minecraft game","s":[["TAGLINE","launches the Minecraft game"],["TLDR","**Launch Minecraft**\n\n```minecraft-launcher```\n\n**Run server**\n\n```java -Xmx[2G] -jar [server.jar] nogui```\n\n**Specify version**\n\n```minecraft-launcher --version [1.20]```\n\n**Specify game directory**\n\n```minecraft-launcher --gameDir [/path/to/minecraft]```\n\n**Demo mode**\n\n```minecraft-launcher --demo```"],["SYNOPSIS","**minecraft-launcher** [_options_]"],["PARAMETERS","**--version** _VER_\n> Game version.\n\n**--gameDir** _DIR_\n> Game directory.\n\n**--demo**\n> Demo mode.\n\n**--server** _HOST_\n> Auto-connect to server.\n\n**--help**\n> Display help information."],["DESCRIPTION","**minecraft** launches the Minecraft game. The launcher manages versions and profiles.\n\nThe tool downloads game files and manages Java runtime. Supports mods and custom configurations."],["CAVEATS","Requires purchase. Java needed for server. Large download."],["HISTORY","Minecraft was created by **Markus \"Notch\" Persson** in 2009, now developed by Mojang Studios (Microsoft)."],["SEE ALSO","[java](/man/java)(1), [mcli](/man/mcli)(1)"]],"e":["java","mcli"],"x":"minecraft-launcher"},"miniflare":{"n":"miniflare","t":"simulator for Cloudflare Workers","s":[["TAGLINE","simulator for Cloudflare Workers"],["TLDR","**Start local worker**\n\n```miniflare [worker.js]```\n\n**Specify port**\n\n```miniflare [worker.js] --port [8787]```\n\n**With KV namespace**\n\n```miniflare [worker.js] --kv [NAMESPACE]```\n\n**With D1 database**\n\n```miniflare [worker.js] --d1 [DB]```\n\n**With environment variables**\n\n```miniflare [worker.js] --env [.env]```\n\n**Watch for changes**\n\n```miniflare [worker.js] --watch```"],["SYNOPSIS","**miniflare** [_options_] [_script_]"],["PARAMETERS","**--port** _port_\n> HTTP port.\n\n**--host** _host_\n> Bind address.\n\n**--kv** _namespace_\n> KV namespace.\n\n**--d1** _database_\n> D1 database.\n\n**--r2** _bucket_\n> R2 bucket.\n\n**--env** _file_\n> Environment file.\n\n**--watch**\n> Watch for changes.\n\n**--wrangler-config** _file_\n> Wrangler config."],["DESCRIPTION","**Miniflare** is a simulator for Cloudflare Workers. It runs Workers locally with full support for KV, Durable Objects, R2, D1, and other Cloudflare services.\n\nMiniflare enables local development and testing without deploying to Cloudflare."],["FEATURES","```\n- KV storage\n- Durable Objects\n- R2 buckets\n- D1 databases\n- Cache API\n- Scheduled events\n```"],["CAVEATS","Behavior may differ from production. Storage is local/temporary. Some features require configuration. Now integrated into Wrangler."],["HISTORY","Miniflare was created by **Brendan Coll** in **2021** to enable local Cloudflare Workers development. It was later officially adopted by Cloudflare."],["SEE ALSO","[wrangler](/man/wrangler)(1), [cloudflared](/man/cloudflared)(1)"]],"e":["wrangler","cloudflared"],"x":"miniflare [worker.js]"},"mmove":{"n":"mmove","t":"move or rename an MS-DOS file or subdirectory","s":[["TAGLINE","move or rename an MS-DOS file or subdirectory"],["TLDR","**Move/rename a file on an MS-DOS disk**\n\n```mmove a:oldname a:newname```\n\n**Move a file to a directory on the disk**\n\n```mmove a:[file.txt] a:[dir]/```\n\n**Move multiple files matching a wildcard**\n\n```mmove a:*.[txt] a:[backup]/```\n\n**Move a subdirectory**\n\n```mmove a:[olddir] a:[newdir]```\n\n**Move with verbose output**\n\n```mmove -v a:[file] a:[newfile]```"],["SYNOPSIS","**mmove** [**-v**] [**-D** _clash_option_] _source_ [_sources_...] _target_"],["PARAMETERS","_SOURCE_\n> Source file(s) or subdirectories on an MS-DOS filesystem. Drive letter prefix (e.g., a:) specifies the device.\n\n_TARGET_\n> Target filename or directory.\n\n**-v**\n> Verbose mode. Print the name of each file as it is moved.\n\n**-D** _clash_option_\n> Specify what to do on filename conflicts. Lowercase is for the primary (long) name, uppercase for the secondary (short) name: o/O (overwrite), r/R (rename), s/S (skip), a/A (autorename)."],["DESCRIPTION","**mmove** moves or renames files and subdirectories on MS-DOS (FAT) filesystems without needing to mount them first. Unlike the MS-DOS MOVE command, mmove can also move subdirectories. It is part of the **mtools** package, which provides a set of utilities to access FAT filesystems from Unix.\n\nSource and target must be on the same MS-DOS filesystem. Cross-device moves are not supported -- use **mcopy** and **mdel** instead."],["CAVEATS","Only works on FAT filesystems. Cannot move files between different drives. Drive mappings are configured in **/etc/mtools.conf** or **~/.mtoolsrc**."],["INSTALL","```apt: sudo apt install mtools```\n\n```dnf: sudo dnf install mtools```\n\n```pacman: sudo pacman -S mtools```\n\n```apk: sudo apk add mtools```\n\n```zypper: sudo zypper install mtools```\n\n```brew: brew install mtools```\n\n```nix: nix profile install nixpkgs#mtools```"],["SEE ALSO","[mcopy](/man/mcopy)(1), [mdel](/man/mdel)(1), [mren](/man/mren)(1), [mmd](/man/mmd)(1)"]],"e":["mcopy","mdel","mren","mmd"],"x":"mmove a:oldname a:newname"},"mr":{"n":"mr","t":"myrepos — run version-control commands across many repositories at once","s":[["TAGLINE","myrepos — run version-control commands across many repositories at once"],["TLDR","**Update every registered repo**\n\n```mr update```\n\n**Check the status of every repo**\n\n```mr status```\n\n**Commit changes** across all repos with the same message\n\n```mr commit -m \"[message]\"```\n\n**Register the current repo** in `~/.mrconfig`\n\n```mr register```\n\n**Run an arbitrary command** inside every repo\n\n```mr run [git fetch]```\n\n**Run only on a repo in the current directory subtree**\n\n```mr -d [.] status```\n\n**List configured repositories**\n\n```mr list```\n\n**Bootstrap a new checkout from a shared `.mrconfig`**\n\n```mr checkout```\n\n**Use a non-default config file**\n\n```mr -c [/path/to/mrconfig] update```\n\n**Run sequentially** (disable default parallel jobs)\n\n```mr -s update```"],["SYNOPSIS","**mr** [_options_] _command_ [_command args_]"],["PARAMETERS","**-c** _CONFIG_\n> Use _CONFIG_ instead of `$HOME/.mrconfig`.\n\n**-d** _DIR_\n> Operate only on the repository at _DIR_ (or the closest enclosing one).\n\n**-t** _SECONDS_\n> Timeout each sub-command after _SECONDS_.\n\n**-j** _N_\n> Run _N_ jobs in parallel (default 1, `-j0` = unlimited).\n\n**-s**\n> Stop on first error (also serialize when using `-j`).\n\n**-i**\n> Interactive: prompt before each action.\n\n**-n**\n> Limit recursion: only act on repos whose path is under the current directory.\n\n**-p** _DIR_\n> Chdir to _DIR_ before running.\n\n**-f**\n> Force action even on repos that are marked `skip`.\n\n**-v**\n> Verbose.\n\n**-q**\n> Quiet: suppress standard output from mr and its children.\n\n**-m**\n> Commit message (for `commit`).\n\n**--version**\n> Show version.\n\n**--help**\n> Show help."],["COMMON COMMANDS","**checkout** — clone every repo listed in the config.\n**update** — `git pull --rebase` / `svn update` / `hg pull -u` / etc., per-repo.\n**status** — show working-tree status for each repo.\n**commit** — commit (use `-m` to set a message).\n**push** / **pull** / **fetch** — VCS-specific wrappers.\n**diff** / **log** — per-repo diff or log.\n**run** _CMD_ — run _CMD_ in each repo directory.\n**register** [_DIR_] — add a repo (default: current) to the config.\n**list** — show repos that would be acted on.\n**config** [_REPO_] [_KEY_ [_VALUE_]] — read/write config keys.\n**bootstrap** _URL_ — fetch an `.mrconfig` from a URL and run `checkout`.\n**online** / **offline** — mark repos as reachable/unreachable for later `--online-only` runs.\n**help** — show help for a specific command."],["CONFIGURATION","**~/.mrconfig** (primary) and any `.mrconfig` in the current tree.\n\nSections are per-repo; the `[DEFAULT]` section holds defaults. Commands are defined as shell blocks:\n\n```\n[src/vim]\ncheckout = git clone git@github.com:you/vim .\nupdate = git pull --rebase\nskip = [ \"$1\" = \"fetch\" ] && ! mr_any_args\n```\n\nSupported backends out of the box: git, subversion, mercurial, bazaar, cvs, darcs, fossil, veracity, git-svn, unison. Custom backends can be added by writing matching actions."],["DESCRIPTION","**mr** (myrepos) orchestrates version-control operations across a heterogeneous collection of repositories. It reads per-repo action definitions from one or more `.mrconfig` files and runs them, so a single `mr update` can pull dozens of git repos, an svn checkout, and a mercurial clone in one go. Parallel execution (`-j`), timeouts (`-t`), and per-repo `skip` rules make it practical on large fleets."],["CAVEATS","Requires Perl. Parallel mode interleaves output; use `-j1` (the default) when you need clean logs. `mr commit` only commits already-staged (or tracked, depending on VCS) changes — it does not add new files."],["HISTORY","**mr** / **myrepos** was created by **Joey Hess** and is now maintained by **Richard Hartmann** (RichiH). It has been packaged in Debian since the late 2000s and is the spiritual ancestor of tools like `vcsh` and `gita`."],["INSTALL","```dnf: sudo dnf install myrepos```\n\n```pacman: sudo pacman -S myrepos```\n\n```apk: sudo apk add myrepos```\n\n```zypper: sudo zypper install mr```\n\n```brew: brew install myrepos```\n\n```nix: nix profile install nixpkgs#mr```"],["SEE ALSO","[git](/man/git)(1), [vcsh](/man/vcsh)(1), [gita](/man/gita)(1)"]],"e":["git","vcsh","gita"],"x":"mr update"},"msgunfmt":{"n":"msgunfmt","t":"converts compiled gettext MO files back to human-readable PO format","s":[["TAGLINE","converts compiled gettext MO files back to human-readable PO format"],["TLDR","**Convert MO to PO**\n\n```msgunfmt [messages.mo] -o [messages.po]```\n\n**Convert Java properties**\n\n```msgunfmt --java -d [classes/] -l [locale] -o [output.po]```\n\n**Specify output encoding**\n\n```msgunfmt --output-encoding=[UTF-8] [messages.mo] -o [messages.po]```"],["SYNOPSIS","**msgunfmt** [_options_] _inputfile_"],["PARAMETERS","**-o** _file_\n> Output file.\n\n**--java**\n> Java ResourceBundle mode.\n\n**-d** _dir_\n> Class directory for Java.\n\n**-l** _locale_\n> Locale for Java mode.\n\n**--output-encoding** _enc_\n> Output character encoding.\n\n**--no-wrap**\n> Do not break long message lines.\n\n**--sort-output**\n> Generate sorted output.\n\n**-v, --verbose**\n> Increase verbosity level."],["DESCRIPTION","**msgunfmt** converts compiled gettext MO (machine object) files back to human-readable PO (portable object) format. Also extracts translations from Java ResourceBundle classes. Part of GNU gettext."],["INSTALL","```apt: sudo apt install gettext```\n\n```dnf: sudo dnf install gettext```\n\n```pacman: sudo pacman -S gettext```\n\n```apk: sudo apk add gettext```\n\n```brew: brew install gettext```\n\n```nix: nix profile install nixpkgs#gettext```"],["SEE ALSO","[msgfmt](/man/msgfmt)(1), [msgcat](/man/msgcat)(1), [msgattrib](/man/msgattrib)(1)"]],"e":["msgfmt","msgcat","msgattrib"],"x":"msgunfmt [messages.mo] -o [messages.po]"},"needle":{"n":"needle","t":"Needleman-Wunsch global pairwise sequence alignment (EMBOSS)","s":[["TAGLINE","Needleman-Wunsch global pairwise sequence alignment (EMBOSS)"],["TLDR","**Globally align** two sequences from FASTA files\n\n```needle -asequence [seq1.fasta] -bsequence [seq2.fasta] -gapopen [10] -gapextend [0.5] -outfile [out.needle]```\n\n**Align by database accession** (e.g. UniProt)\n\n```needle -asequence sp:[hba_human] -bsequence sp:[hbb_human] -gapopen [10] -gapextend [0.5] -outfile [result.needle]```\n\n**Use a specific scoring matrix**\n\n```needle -asequence [a.fa] -bsequence [b.fa] -datafile [EBLOSUM62] -gapopen [10] -gapextend [0.5] -outfile [out.needle]```\n\n**Choose an alternative output format**\n\n```needle -asequence [a.fa] -bsequence [b.fa] -gapopen [10] -gapextend [0.5] -aformat3 [markx10] -outfile [out.txt]```\n\n**Run non-interactively** (no prompts)\n\n```needle -auto -asequence [a.fa] -bsequence [b.fa] -gapopen [10] -gapextend [0.5] -outfile [a_vs_b.needle]```"],["SYNOPSIS","**needle** **-asequence** _seqfile_ **-bsequence** _seqfile_ **-gapopen** _f_ **-gapextend** _f_ **-outfile** _file_ [_options_]"],["PARAMETERS","**-asequence** _file_\n> First input sequence (single sequence, any EMBOSS-supported format).\n\n**-bsequence** _file_\n> Second input sequence (one or many sequences to align against the first).\n\n**-gapopen** _float_\n> Penalty for opening a gap (typical: 10.0 for proteins, 10.0 for DNA).\n\n**-gapextend** _float_\n> Penalty for extending an existing gap (typical: 0.5).\n\n**-datafile** _matrix_\n> Scoring matrix name (e.g. _EBLOSUM62_, _EDNAFULL_).\n\n**-endweight**\n> Apply end-gap penalties (default: false; end gaps are free).\n\n**-outfile** _file_\n> Path to the alignment report.\n\n**-aformat3** _format_\n> Output alignment format (_pair_, _markx0_..._markx10_, _msf_, _fasta_, ...).\n\n**-brief**\n> Print a brief alignment summary instead of the full pairwise view.\n\n**-auto**\n> Skip all interactive prompts (suitable for scripts)."],["DESCRIPTION","**needle** computes the optimal **global** pairwise alignment of two sequences using the **Needleman-Wunsch** dynamic programming algorithm. It ships as part of **EMBOSS** (European Molecular Biology Open Software Suite) and is intended for nucleotide or protein sequences of comparable length where the entire sequences should be aligned end-to-end.\n\nGap-open and gap-extend penalties are mandatory parameters that shape the alignment, and a scoring matrix (BLOSUM, PAM, EDNAFULL, ...) determines how matches and mismatches are weighted. The output is a formatted alignment that reports score, length, percentage identity, similarity, and gap statistics; many alternative formats are available via **-aformat3**.\n\nFor local alignment of subsequences use **water**; for very long sequences where memory is a concern use **stretcher**, which implements a linear-space variant of the algorithm."],["CAVEATS","Time and memory complexity are O(m·n) in the lengths of the two sequences, so **needle** is not appropriate for very long sequences — use **stretcher** instead. Option syntax is EMBOSS-specific (long names introduced by a single dash) and is not interchangeable with GNU-style flags. End gaps are free by default; enable **-endweight** if you want them penalized."],["HISTORY","**needle** was written by **Alan Bleasby** as part of **EMBOSS**, a project started in **1996** at the **Sanger Centre / MRC** to provide an open, integrated suite of bioinformatics tools. The Needleman-Wunsch algorithm itself was published in **1970** by **Saul B. Needleman** and **Christian D. Wunsch**."],["INSTALL","```apt: sudo apt install emboss```\n\n```brew: brew install needle```\n\n```nix: nix profile install nixpkgs#emboss```"],["SEE ALSO","[blastp](/man/blastp)(1)"]],"e":["blastp"],"x":"needle -asequence [seq1.fasta] -bsequence [seq2.fasta] -gapopen [10] -gapextend [0.5] -outfile [out.needle]"},"nmap":{"n":"nmap","t":"Network exploration and security auditing tool","s":[["TAGLINE","Network exploration and security auditing tool"],["TLDR","**Scan a single host**\n\n```nmap [192.168.1.1]```\n\n**Scan a network range**\n\n```nmap [192.168.1.0/24]```\n\n**Scan specific ports**\n\n```nmap -p [22,80,443] [host]```\n\n**Service and version detection**\n\n```nmap -sV [host]```\n\n**OS detection** (requires root)\n\n```sudo nmap -O [host]```\n\n**Aggressive scan** (OS, version, scripts, traceroute)\n\n```sudo nmap -A [host]```\n\n**Stealth SYN scan** (requires root)\n\n```sudo nmap -sS [host]```\n\n**UDP scan** (requires root)\n\n```sudo nmap -sU [host]```\n\n**Scan all 65535 ports**\n\n```nmap -p- [host]```\n\n**Save output in all formats**\n\n```nmap -oA [output_basename] [host]```\n\n**Run a specific NSE script**\n\n```nmap --script [vuln] [host]```"],["SYNOPSIS","**nmap** [_scan type_] [_options_] _target specification_"],["PARAMETERS","_TARGETS_\n> Hosts/networks to scan. Accepts hostnames, IPs, CIDR ranges, or ranges like 192.168.1.1-254.\n\n**-p** _PORTS_\n> Ports to scan. Ranges (1-1024), lists (22,80,443), or -p- for all 65535 ports.\n\n**-sS**\n> TCP SYN scan (stealth/half-open). Default scan type when run as root.\n\n**-sT**\n> TCP connect scan. Default when run without root.\n\n**-sU**\n> UDP scan. Slower than TCP scans.\n\n**-sn**\n> Ping scan (host discovery only, no port scan).\n\n**-sV**\n> Probe open ports for service version information.\n\n**-O**\n> Enable OS detection (requires root).\n\n**-A**\n> Aggressive scan: enables -O, -sV, --script=default, and --traceroute.\n\n**-T**_0-5_\n> Timing template from paranoid (0) to insane (5). Default is -T3.\n\n**--script** _scripts_\n> Run specified NSE (Nmap Scripting Engine) scripts.\n\n**-oN** _FILE_\n> Normal text output to file.\n\n**-oX** _FILE_\n> XML output to file.\n\n**-oG** _FILE_\n> Grepable output to file.\n\n**-oA** _BASENAME_\n> Output in all formats (normal, XML, grepable).\n\n**-v**\n> Increase verbosity level (use -vv for more).\n\n**--top-ports** _N_\n> Scan only the N most common ports.\n\n**-Pn**\n> Skip host discovery (treat all hosts as online).\n\n**-iL** _FILE_\n> Read targets from a file.\n\n**--open**\n> Only show open ports in results."],["DESCRIPTION","**nmap** (Network Mapper) is a free and open-source tool for network discovery and security auditing. It uses raw IP packets to determine available hosts, their services, operating systems, firewall rules, and other characteristics.\n\nNmap includes the Nmap Scripting Engine (NSE) with hundreds of scripts for vulnerability detection, service enumeration, and brute-force testing. Output can be saved in multiple formats for integration with other tools.\n\nCommon scan types include TCP SYN scans (-sS) for stealth, TCP connect scans (-sT) for unprivileged users, UDP scans (-sU), and ping scans (-sn) for host discovery."],["CAVEATS","Unauthorized scanning may be illegal. Many scan types (SYN, OS detection) require root/sudo privileges. Aggressive scanning can trigger IDS/IPS alerts or cause network disruption. UDP scans are significantly slower than TCP scans."],["HISTORY","Nmap was created by **Gordon Lyon (Fyodor)** and first released in **1997**. It has since become the de facto standard for network scanning and is used by security professionals worldwide."],["INSTALL","```apt: sudo apt install nmap```\n\n```dnf: sudo dnf install nmap```\n\n```pacman: sudo pacman -S nmap```\n\n```apk: sudo apk add nmap```\n\n```brew: brew install nmap```\n\n```nix: nix profile install nixpkgs#nmap```"],["SEE ALSO","[masscan](/man/masscan)(1), [netcat](/man/netcat)(1), [zenmap](/man/zenmap)(1), [ss](/man/ss)(8), [tcpdump](/man/tcpdump)(1), [wireshark](/man/wireshark)(1)"]],"e":["masscan","netcat","zenmap","ss","tcpdump","wireshark"],"x":"nmap [192.168.1.1]"},"onionsearch":{"n":"onionsearch","t":"searches Tor hidden service search engines for content","s":[["TAGLINE","searches Tor hidden service search engines for content"],["TLDR","**Search onion sites**\n\n```onionsearch \"[query]\"```\n\n**Search specific engine**\n\n```onionsearch --engines [ahmia] \"[query]\"```\n\n**Use Tor SOCKS proxy**\n\n```onionsearch --proxy [socks5h://127.0.0.1:9050] \"[query]\"```\n\n**Set timeout**\n\n```onionsearch --timeout [30] \"[query]\"```\n\n**Limit results**\n\n```onionsearch --limit [50] \"[query]\"```\n\n**Output to file**\n\n```onionsearch -o [results.txt] \"[query]\"```"],["SYNOPSIS","**onionsearch** [_--engines engine_] [_--proxy url_] [_--limit n_] [_options_] _query_"],["PARAMETERS","**--engines** _NAME_\n> Search engine to use (e.g., ahmia, torch, darksearchio).\n\n**--proxy** _URL_\n> SOCKS proxy URL for Tor (e.g., socks5h://127.0.0.1:9050).\n\n**--timeout** _SECONDS_\n> Request timeout.\n\n**--limit** _N_\n> Maximum results.\n\n**-o**, **--output** _FILE_\n> Output file.\n\n**--len**\n> Show result count only.\n\n**--list**\n> List available engines.\n\n**-v**, **--verbose**\n> Verbose output."],["DESCRIPTION","**onionsearch** searches Tor hidden service (.onion) search engines for content. It aggregates results from multiple dark web search engines.\n\nMultiple search engines are supported including Ahmia, Torch, and others. Results are collected and deduplicated.\n\nProxy configuration connects through Tor for anonymous searching. Without Tor, many engines won't be accessible.\n\nResults include onion URLs and page titles. Output formats support further analysis."],["CAVEATS","Requires Tor running for full functionality. Search engines may be unreliable. Results may include illegal content. Use responsibly for security research only."],["HISTORY","**onionsearch** was created for security researchers needing to search dark web content. It provides OSINT capabilities for threat intelligence gathering."],["SEE ALSO","[tor](/man/tor)(1), [torsocks](/man/torsocks)(1), [proxychains](/man/proxychains)(1)"]],"e":["tor","torsocks","proxychains"],"x":"onionsearch \"[query]\""},"pacman-s":{"n":"pacman-s","t":"short form of **pacman --sync**, which synchronizes packages","s":[["TAGLINE","short form of **pacman --sync**, which synchronizes packages"],["TLDR","This command is an alias of **pacman --sync**\n\nView documentation for the original command\n\n```tldr pacman sync```"],["SYNOPSIS","**pacman -S** [_options_] [_package(s)_]"],["PARAMETERS","**-y**, **--refresh**\n> Download a fresh copy of the master package databases from the server.\n\n**-u**, **--sysupgrade**\n> Upgrade all out-of-date packages.\n\n**-s**, **--search** _regexp_\n> Search package names and descriptions in sync databases.\n\n**-i**, **--info**\n> Display detailed information for packages in the sync database.\n\n**-c**, **--clean**\n> Remove old packages from the cache directory.\n\n**-l**, **--list**\n> List all packages in the specified repositories.\n\n**-g**, **--groups**\n> Display members of a package group.\n\n**-q**, **--quiet**\n> Show less information for query and search results.\n\n**--needed**\n> Do not reinstall packages that are already up-to-date.\n\n**--asdeps**\n> Install packages as non-explicitly installed (as dependencies).\n\n**--overwrite** _glob_\n> Overwrite conflicting files matching the given glob pattern.\n\n**--downloadonly**, **-w**\n> Download packages without installing them.\n\n**--ignore** _package_\n> Skip upgrades for the specified package."],["DESCRIPTION","**pacman -S** is the short form of **pacman --sync**, which synchronizes packages. It handles installing packages from repositories, updating the package database, and performing system upgrades."],["INSTALL","```apt: sudo apt install pacman-package-manager```\n\n```dnf: sudo dnf install pacman```\n\n```pacman: sudo pacman -S pacman```\n\n```apk: sudo apk add pacman```\n\n```nix: nix profile install nixpkgs#pacman```"],["SEE ALSO","[pacman](/man/pacman)(8), [pacman-sync](/man/pacman-sync)(8)"]],"e":["pacman","pacman-sync"],"x":"tldr pacman sync"},"pacman-upgrade":{"n":"pacman-upgrade","t":"installs packages from local archive files rather than from remote repositories","s":[["TAGLINE","installs packages from local archive files rather than from remote repositories"],["TLDR","**Install** from local files\n\n```sudo pacman -U path/to/package.pkg.tar.zst```\n\nInstall **without** prompting\n\n```sudo pacman -U --noconfirm path/to/package.pkg.tar.zst```\n\n**Overwrite** conflicting files\n\n```sudo pacman -U --overwrite path/to/file path/to/package.pkg.tar.zst```\n\n**Skip** dependency checks\n\n```sudo pacman -Ud path/to/package.pkg.tar.zst```\n\n**Preview** upgrade\n\n```pacman -Up path/to/package.pkg.tar.zst```"],["SYNOPSIS","**pacman -U** [_OPTIONS_] _PACKAGE_..."],["DESCRIPTION","**pacman -U** (upgrade) installs packages from local archive files rather than from remote repositories. This is useful for installing locally built packages, AUR packages, or downgrading to older package versions."],["PARAMETERS","**-U, --upgrade**\n> Upgrade or install from package files\n\n**-d, --nodeps**\n> Skip dependency version checks\n\n**-p, --print**\n> Print what would be installed without installing\n\n**--overwrite** _glob_\n> Overwrite conflicting files matching the glob pattern\n\n**--noconfirm**\n> Skip confirmation prompts\n\n**--asdeps**\n> Mark packages as non-explicitly installed (dependencies)\n\n**--asexplicit**\n> Mark packages as explicitly installed"],["CAVEATS","Package files must be compatible with the system architecture. Dependencies are not automatically resolved from repositories when using **-U**. Using **-d** to skip dependency checks can break the system."],["HISTORY","**pacman** is the package manager for **Arch Linux** and its derivatives."],["INSTALL","```apt: sudo apt install pacman-package-manager```\n\n```dnf: sudo dnf install pacman```\n\n```pacman: sudo pacman -S pacman```\n\n```apk: sudo apk add pacman```\n\n```nix: nix profile install nixpkgs#pacman```"],["SEE ALSO","[pacman](/man/pacman)(8), [pacman-sync](/man/pacman-sync)(8), [makepkg](/man/makepkg)(8)"]],"e":["pacman","pacman-sync","makepkg"],"x":"sudo pacman -U path/to/package.pkg.tar.zst"},"pamtoqoi":{"n":"pamtoqoi","t":"converts PAM/PNM images to QOI format","s":[["TAGLINE","converts PAM/PNM images to QOI format"],["TLDR","**Convert PAM to QOI format**\n\n```pamtoqoi [input.pam] > [output.qoi]```"],["SYNOPSIS","**pamtoqoi** [_file_]"],["DESCRIPTION","**pamtoqoi** converts PAM/PNM images to QOI (Quite OK Image) format. QOI is a fast, lossless image format designed for simplicity and speed. Part of Netpbm toolkit."],["PARAMETERS","**-quiet**\n> Suppress informational messages.\n\n**-version**\n> Display program version and exit."],["CAVEATS","Input must be a PAM (Portable Arbitrary Map) or PNM image. QOI supports RGB and RGBA only; grayscale inputs may be rejected or converted."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[qoitopam](/man/qoitopam)(1), [pnmtopng](/man/pnmtopng)(1), [pamtopnm](/man/pamtopnm)(1), [pam](/man/pam)(5), [pnm](/man/pnm)(5)"]],"e":["qoitopam","pnmtopng","pamtopnm","pam","pnm"],"x":"pamtoqoi [input.pam] > [output.qoi]"},"pbmnoise":{"n":"pbmnoise","t":"creates random noise images","s":[["TAGLINE","creates random noise images"],["TLDR","**Create random noise bitmap**\n\n```pbmnoise [100] [100] > [noise.pbm]```\n\n**Set density**\n\n```pbmnoise -ratio=[0.5] [100] [100] > [noise.pbm]```\n\n**Set random seed**\n\n```pbmnoise -randomseed=[12345] [100] [100] > [noise.pbm]```"],["SYNOPSIS","**pbmnoise** [_options_] _width_ _height_"],["PARAMETERS","_WIDTH_\n> Image width.\n\n_HEIGHT_\n> Image height.\n\n**-ratio** _RATIO_\n> Black pixel ratio (0-1).\n\n**-randomseed** _SEED_\n> Random seed."],["DESCRIPTION","**pbmnoise** creates random noise images. Generates random black and white pixels.\n\nThe tool produces noise patterns. Part of Netpbm."],["CAVEATS","Part of Netpbm. Random output. Reproducible with seed."],["HISTORY","pbmnoise is part of **Netpbm** for generating random bitmap noise."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pbm](/man/pbm)(5), [pgmnoise](/man/pgmnoise)(1), [netpbm](/man/netpbm)(1)"]],"e":["pbm","pgmnoise","netpbm"],"x":"pbmnoise [100] [100] > [noise.pbm]"},"pet":{"n":"pet","t":"Simple command-line snippet manager","s":[["TAGLINE","Simple command-line snippet manager"],["TLDR","**Add a new snippet**\n\n```pet new```\n\n**Add a new snippet with tags**\n\n```pet new -t```\n\n**Search and run a snippet**\n\n```pet exec```\n\n**Run a snippet filtered by tag**\n\n```pet exec -t [tag_name]```\n\n**Copy a snippet to the clipboard**\n\n```pet clip```\n\n**List all snippets**\n\n```pet list```\n\n**Search snippets interactively**\n\n```pet search```\n\n**Edit the snippet file**\n\n```pet edit```"],["SYNOPSIS","**pet** _command_ [_options_]"],["PARAMETERS","**new**\n> Create a new command snippet. Use **-t** to add tags.\n\n**exec**\n> Search for a snippet and execute it. Use **-t** to filter by tag.\n\n**clip**\n> Copy the selected snippet to the clipboard.\n\n**list**\n> Show all stored snippets.\n\n**search**\n> Search snippets interactively.\n\n**edit**\n> Open the snippet file in your editor.\n\n**configure**\n> Edit the configuration file.\n\n**sync**\n> Sync snippets via Gist, GitLab Snippets, or GitHub Enterprise.\n\n**version**\n> Print the version number.\n\n**--config** _string_\n> Specify config file (default $HOME/.config/pet/config.toml).\n\n**--debug**\n> Enable debug mode."],["DESCRIPTION","**pet** is a simple command-line snippet manager. It stores frequently used commands with descriptions and optional parameter placeholders, then lets you search and execute them interactively using fuzzy finders like fzf or peco. Snippets can be synced across machines via GitHub Gist, GitLab Snippets, or GitHub Enterprise."],["CAVEATS","Requires **fzf** or **peco** for interactive selection."],["HISTORY","**pet** was created by **Teppei Fukuda** (knqyf263) and is written in **Go**."],["INSTALL","```brew: brew install pet```\n\n```nix: nix profile install nixpkgs#pet```"],["SEE ALSO","[nap](/man/nap)(1), [tldr](/man/tldr)(1), [fzf](/man/fzf)(1), [peco](/man/peco)(1)"]],"e":["nap","tldr","fzf","peco"],"x":"pet new"},"pinky":{"n":"pinky","t":"displays information about logged-in users","s":[["TAGLINE","displays information about logged-in users"],["TLDR","**Show logged-in users**\n\n```pinky```\n\n**Show specific user**\n\n```pinky [username]```\n\n**Long format**\n\n```pinky -l [username]```\n\n**Short format**\n\n```pinky -s```\n\n**Omit full names**\n\n```pinky -f```\n\n**Omit remote host**\n\n```pinky -h```"],["SYNOPSIS","**pinky** [_-l_] [_-s_] [_-f_] [_-h_] [_options_] [_users_]"],["PARAMETERS","**-l**\n> Long output format.\n\n**-s**\n> Short output format.\n\n**-f**\n> Omit full name.\n\n**-h**\n> Omit remote host.\n\n**-w**\n> Omit home directory.\n\n**-b**\n> Omit home in long format.\n\n**-p**\n> Omit project in long format.\n\n**--help**\n> Show help."],["DESCRIPTION","**pinky** displays information about logged-in users. It's a lightweight alternative to finger.\n\nDefault output shows login name, full name, terminal, idle time, login time, and host.\n\nLong format adds home directory, shell, and plan/project files. Similar to finger's detailed view.\n\nShort format shows minimal information. Useful for quick user listing.\n\nThe command reads system utmp and user information. No network queries are made."],["CAVEATS","Less feature-complete than finger. No remote host queries. Plan files may not exist."],["HISTORY","**pinky** is part of GNU coreutils, created as a lighter-weight local alternative to the **finger** command. It doesn't support network finger protocol."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[finger](/man/finger)(1), [who](/man/who)(1), [w](/man/w)(1), [users](/man/users)(1)"]],"e":["finger","who","w","users"],"x":"pinky"},"playerctl":{"n":"playerctl","t":"Control MPRIS media players from command line","s":[["TAGLINE","Control MPRIS media players from command line"],["TLDR","**Toggle** play/pause\n\n```playerctl play-pause```\n\nSkip to **next** track\n\n```playerctl next```\n\nGo to **previous** track\n\n```playerctl previous```\n\n**List** all players\n\n```playerctl -l```\n\nControl **specific** player\n\n```playerctl -p [player_name] [command]```\n\nControl **all** players\n\n```playerctl -a [command]```\n\nDisplay **metadata**\n\n```playerctl metadata -f \"Now playing: {{artist}} - {{title}}\"```"],["SYNOPSIS","**playerctl** [**-p** _player_] [**-a**] [**-l**] _command_"],["COMMANDS","**play-pause**\n> Toggle playback\n\n**play**\n> Start playback\n\n**pause**\n> Pause playback\n\n**stop**\n> Stop playback\n\n**next**\n> Next track\n\n**previous**\n> Previous track\n\n**position**\n> Get or set position\n\n**volume**\n> Get or set volume\n\n**status**\n> Get playback status\n\n**metadata**\n> Get track metadata"],["PARAMETERS","**-p, --player _player_**\n> Target specific player\n\n**-a, --all-players**\n> Control all players\n\n**-l, --list-all**\n> List available players\n\n**-f, --format _format_**\n> Output format template"],["DESCRIPTION","**playerctl** controls media players that implement the MPRIS D-Bus specification. It provides a unified command-line interface for controlling Spotify, VLC, Firefox, and many other media applications.\n\nThe tool is commonly used for keyboard shortcuts and scripts to control media playback without focusing the player window."],["CAVEATS","Requires MPRIS-compatible player. Some players have limited MPRIS support. Player must be running to be controlled. Metadata availability varies by player."],["HISTORY","**playerctl** was created by **Tony Crisci** to provide a simple, reliable way to control media players from scripts and keybindings. It's widely adopted in tiling window manager setups."],["INSTALL","```apt: sudo apt install playerctl```\n\n```dnf: sudo dnf install playerctl```\n\n```pacman: sudo pacman -S playerctl```\n\n```apk: sudo apk add playerctl```\n\n```zypper: sudo zypper install playerctl```\n\n```nix: nix profile install nixpkgs#playerctl```"],["SEE ALSO","[pactl](/man/pactl)(1), [mpc](/man/mpc)(1)"]],"e":["pactl","mpc"],"x":"playerctl play-pause"},"rdiff":{"n":"rdiff","t":"Compute and apply rsync-style file deltas","s":[["TAGLINE","Compute and apply rsync-style file deltas"],["TLDR","**Create signature file**\n\n```rdiff signature [original] [signature.sig]```\n\n**Create delta from signature**\n\n```rdiff delta [signature.sig] [new_file] [delta.delta]```\n\n**Apply delta to recreate**\n\n```rdiff patch [original] [delta.delta] [output]```"],["SYNOPSIS","**rdiff** _command_ [_options_] _files_"],["COMMANDS","**signature** _basis_ [_sig_]\n> Create signature of basis file.\n\n**delta** _sig_ _new_ [_delta_]\n> Create delta from signature and new file.\n\n**patch** _basis_ _delta_ [_output_]\n> Apply delta to basis file."],["PARAMETERS","**-b** _blocksize_\n> Block size (bytes) for the signature. Smaller = more accurate delta but larger signature.\n\n**-s**, **--statistics**\n> Print signature / delta timing and size statistics.\n\n**-S** _sum_\n> Length (bytes) of the strong checksum in the signature (default 8).\n\n**-H** _hash_\n> Hash algorithm: `md4` (legacy) or `blake2` (default for librsync ≥ 2).\n\n**-R**, **--rollsum**, **-I**, **--rabinkarp**\n> Select the rolling checksum variant (rabinkarp is the default).\n\n**-f**, **--force**\n> Overwrite the output file if it already exists.\n\n**-z**, **--compress** _FORMAT_\n> Compress deltas with `gzip` or `bzip2` when writing.\n\n**-v**, **--verbose**\n> Increase verbosity. Repeat for more detail.\n\n**-V**, **--version**\n> Print librsync version and exit."],["DESCRIPTION","**rdiff** computes and applies binary deltas using the rsync algorithm. It enables efficient file synchronization by transferring only the differences between files."],["EXAMPLES","```bash"],["Create signature","rdiff signature original.tar sig.sig"],["Create delta (only differences)","rdiff delta sig.sig modified.tar changes.delta"],["Apply delta to recreate file","rdiff patch original.tar changes.delta reconstructed.tar"],["With statistics","rdiff -s signature largefile sig\n```"],["WORKFLOW","```\n1. Create signature of original (small)\n2. Send signature to remote\n3. Remote creates delta against new file\n4. Send delta back (efficient)\n5. Apply delta to reconstruct\n```"],["CAVEATS","Part of librsync. Basis file needed for patch. Signature must match original file version."],["HISTORY","rdiff uses the **rsync** algorithm by **Andrew Tridgell**, implemented in **librsync** by **Martin Pool**."],["INSTALL","```apt: sudo apt install rdiff```\n\n```dnf: sudo dnf install librsync```\n\n```pacman: sudo pacman -S librsync```\n\n```apk: sudo apk add librsync```\n\n```zypper: sudo zypper install rdiff```\n\n```brew: brew install librsync```\n\n```nix: nix profile install nixpkgs#librsync```"],["SEE ALSO","[rsync](/man/rsync)(1), [diff](/man/diff)(1), [patch](/man/patch)(1), [xdelta](/man/xdelta)(1)"]],"e":["rsync","diff","patch","xdelta"],"x":"rdiff signature [original] [signature.sig]"},"resend":{"n":"resend","t":"Email API CLI for sending emails and managing webhooks","s":[["TAGLINE","Email API CLI for sending emails and managing webhooks"],["TLDR","**Authenticate** with your Resend account\n\n```resend login```\n\n**Send an email** with subject and body\n\n```resend emails send --from [sender@example.com] --to [recipient@example.com] --subject \"[Hello]\" --text \"[Message body]\"```\n\n**Check API key** and verify domain readiness\n\n```resend doctor```\n\n**Start a local webhook** listener\n\n```resend webhooks listen```\n\n**Create a webhook** for production events\n\n```resend webhooks create```\n\n**Send an email** with HTML body\n\n```resend emails send --from [sender@example.com] --to [recipient@example.com] --subject \"[Hello]\" --html \"[<h1>Hello</h1>]\"```"],["SYNOPSIS","**resend** _command_ [_options_]"],["PARAMETERS","**login**\n> Authenticate with your Resend account via the browser.\n\n**doctor**\n> Verify API key validity, list verified domains, and check readiness.\n\n**emails send**\n> Send an email with specified sender, recipient, subject, and body.\n\n**webhooks listen**\n> Start a local server on port 4318 to capture and display incoming webhook events.\n\n**webhooks create**\n> Register a new webhook endpoint for production environments.\n\n**--from** _address_\n> Sender email address.\n\n**--to** _address_\n> Recipient email address.\n\n**--subject** _text_\n> Email subject line.\n\n**--text** _body_\n> Plain text email body.\n\n**--html** _body_\n> HTML email body.\n\n**--forward-to** _url_\n> Forward webhook payloads to a local server, preserving signature headers.\n\n**--version**\n> Show version information.\n\n**--help**\n> Show available commands and options."],["DESCRIPTION","**resend** is the official CLI for the Resend email API. It enables sending transactional emails, managing webhooks, and diagnosing account configuration directly from the terminal. The CLI is designed for developers, CI/CD pipelines, and AI agent workflows.\n\nIn non-interactive mode, output is formatted as JSON with consistent exit codes, making it suitable for scripting and automation. Errors include structured **message** and **code** fields for programmatic handling.\n\nThe webhook listener (**resend webhooks listen**) creates a local development server that captures incoming events and streams them to the terminal, useful for testing email event handlers without deploying to production."],["CAVEATS","Requires a Resend account and API key for authentication. Sending emails requires a verified domain. The webhook listener needs Tailscale for exposing local ports to the internet during development. The CLI outputs JSON in non-interactive mode but human-readable text in interactive mode."],["HISTORY","**Resend** was founded by **Zeno Rocha** as a modern email API for developers, offering an alternative to legacy transactional email services. The CLI was built to complement the REST API and SDKs, providing terminal-based access to all core email operations. It is written in **TypeScript** and distributed via npm and Homebrew."],["SEE ALSO","[mail](/man/mail)(1), [sendmail](/man/sendmail)(1), [curl](/man/curl)(1)"]],"e":["mail","sendmail","curl"],"x":"resend login"},"rollup":{"n":"rollup","t":"Bundle JavaScript modules for distribution","s":[["TAGLINE","Bundle JavaScript modules for distribution"],["TLDR","**Bundle JavaScript**\n\n```rollup [main.js] --file [bundle.js]```\n\n**Use configuration file**\n\n```rollup -c```\n\n**Output format**\n\n```rollup [main.js] -o [bundle.js] -f [es]```\n\n**Watch for changes**\n\n```rollup -c --watch```\n\n**Generate sourcemap**\n\n```rollup [main.js] -o [bundle.js] -m```\n\n**Bundle with a specific plugin**\n\n```rollup [main.js] -o [bundle.js] -p [node-resolve]```"],["SYNOPSIS","**rollup** [_options_] _input_"],["DESCRIPTION","**rollup** is a JavaScript module bundler focused on ES modules. It compiles small pieces of code into larger bundles, with excellent tree-shaking to eliminate unused code.\n\nThe tool produces efficient bundles for libraries and applications, supporting multiple output formats."],["PARAMETERS","**-c** _file_, **--config** _file_\n> Configuration file.\n\n**-o** _file_, **--file** _file_\n> Output file.\n\n**-f** _fmt_, **--format** _fmt_\n> Output format (es, cjs, umd, iife, amd, system).\n\n**-n** _name_, **--name** _name_\n> UMD/IIFE global name.\n\n**-m**, **--sourcemap**\n> Generate sourcemap.\n\n**-w**, **--watch**\n> Watch mode.\n\n**-p** _plugin_, **--plugin** _plugin_\n> Use plugin.\n\n**-d** _dir_, **--dir** _dir_\n> Output directory (for code splitting).\n\n**-e** _modules_, **--external** _modules_\n> Comma-separated list of external module IDs.\n\n**-g** _pairs_, **--globals** _pairs_\n> Comma-separated global:module pairs for UMD/IIFE.\n\n**--no-treeshake**\n> Disable tree shaking."],["CONFIGURATION","**rollup.config.js** / **rollup.config.mjs**\n> Project-level configuration file defining input entry points, output formats, plugins, and external dependencies. Loaded automatically with **-c** or by specifying a path."],["CAVEATS","Configuration needed for complex projects. Plugin ecosystem essential. CommonJS needs plugin. Build time can be slow for large projects."],["HISTORY","**Rollup** was created by **Rich Harris** in **2015**. It pioneered ES module bundling with tree-shaking, influencing later bundlers. Harris also created Svelte."],["INSTALL","```zypper: sudo zypper install rollup```\n\n```brew: brew install rollup```"],["SEE ALSO","[webpack](/man/webpack)(1), [esbuild](/man/esbuild)(1), [vite](/man/vite)(1), [parcel](/man/parcel)(1)"]],"e":["webpack","esbuild","vite","parcel"],"x":"rollup [main.js] --file [bundle.js]"},"rustc":{"n":"rustc","t":"Rust programming language compiler","s":[["TAGLINE","Rust programming language compiler"],["TLDR","**Compile a Rust file**\n\n```rustc [main.rs]```\n\n**Compile with output name**\n\n```rustc [main.rs] -o [program]```\n\n**Compile with optimizations**\n\n```rustc -O [main.rs]```\n\n**Compile in release mode**\n\n```rustc -C opt-level=3 [main.rs]```\n\n**Compile with debugging info**\n\n```rustc -g [main.rs]```\n\n**Show warnings**\n\n```rustc -W warnings [main.rs]```\n\n**Emit assembly**\n\n```rustc --emit=asm [main.rs]```\n\n**Emit LLVM IR**\n\n```rustc --emit=llvm-ir [main.rs]```\n\n**Check without compiling**\n\n```rustc --emit=metadata [main.rs]```\n\n**Show version**\n\n```rustc --version```"],["SYNOPSIS","**rustc** [_options_] _input_"],["DESCRIPTION","**rustc** is the compiler for the Rust programming language. It compiles Rust source code (.rs files) into executables or libraries. The compiler performs type checking, borrow checking, and optimization.\n\nWhile rustc can be used directly, most Rust development uses Cargo which invokes rustc with appropriate settings. Direct rustc usage is common for simple programs, learning, or advanced build customization.\n\nrustc uses LLVM for code generation, providing excellent optimization and support for many target platforms."],["PARAMETERS","**-o** _file_\n> Output filename.\n\n**-O**\n> Optimize (equivalent to -C opt-level=3).\n\n**-g**\n> Include debug information.\n\n**-C** _option_\n> Codegen options.\n\n**-W** _lint_\n> Set lint warning level.\n\n**-A** _lint_\n> Allow lint.\n\n**-D** _lint_\n> Deny lint (make it error).\n\n**--emit=** _type_\n> Output type (asm, llvm-ir, llvm-bc, obj, link, metadata, dep-info, mir).\n\n**--crate-type=** _type_\n> Crate type (bin, lib, rlib, dylib, cdylib, staticlib, proc-macro).\n\n**--edition=** _year_\n> Rust edition (2015, 2018, 2021, 2024).\n\n**--test**\n> Build a test harness.\n\n**--print** _info_\n> Print compiler information (e.g., target-list, cfg, sysroot).\n\n**-F** _lint_\n> Forbid lint (cannot be overridden).\n\n**--target=** _triple_\n> Target platform.\n\n**--explain** _code_\n> Explain an error code.\n\n**-L** _path_\n> Add library search path.\n\n**--extern** _name=path_\n> Specify external crate location."],["CAVEATS","Direct rustc usage requires manual dependency management. Most projects should use Cargo instead. Edition differences may cause compatibility issues. Cross-compilation requires target toolchain installation."],["HISTORY","**rustc** was developed as part of the **Rust programming language** project, started by **Graydon Hoare** at Mozilla in **2006**. Rust reached version 1.0 in **May 2015**. The compiler was originally written in OCaml but was rewritten in Rust (self-hosted) by version 1.0. Development continues under the Rust Foundation, established in **2021**."],["INSTALL","```apt: sudo apt install rustc```\n\n```dnf: sudo dnf install rust```\n\n```pacman: sudo pacman -S rust```\n\n```apk: sudo apk add rust```\n\n```zypper: sudo zypper install rust```\n\n```brew: brew install rust```\n\n```nix: nix profile install nixpkgs#rustc```"],["SEE ALSO","[cargo](/man/cargo)(1), [rustup](/man/rustup)(1), [rustfmt](/man/rustfmt)(1), [clippy](/man/clippy)(1)"]],"e":["cargo","rustup","rustfmt","clippy"],"x":"rustc [main.rs]"},"sbatch":{"n":"sbatch","t":"Submit batch jobs to the Slurm scheduler","s":[["TAGLINE","Submit batch jobs to the Slurm scheduler"],["TLDR","**Submit** batch job\n\n```sbatch [job.sh]```\n\nSubmit with **custom name**\n\n```sbatch --job-name=[myjob] [job.sh]```\n\nSet **time limit**\n\n```sbatch --time=[00:30:00] [job.sh]```\n\nRequest **multiple nodes**\n\n```sbatch --nodes=[3] [job.sh]```"],["SYNOPSIS","**sbatch** [**--job-name** _name_] [**--time** _limit_] [**--nodes** _n_] [**--cpus-per-task** _n_] [_script_]"],["PARAMETERS","**--job-name _name_**\n> Job name\n\n**--time _time_**\n> Time limit (HH:MM:SS)\n\n**--nodes _n_**\n> Number of nodes\n\n**--ntasks _n_**\n> Number of tasks\n\n**--cpus-per-task _n_**\n> CPUs per task\n\n**--mem _size_**\n> Memory per node\n\n**--partition _name_**\n> Partition/queue to use\n\n**--output _file_**\n> Output file (%j for job ID)\n\n**--error _file_**\n> Error file\n\n**--mail-type _type_**\n> Email notifications (BEGIN, END, FAIL)"],["DESCRIPTION","**sbatch** submits batch job scripts to the SLURM workload manager. Jobs are queued and executed when requested resources become available on the cluster.\n\nJob scripts contain SLURM directives (lines starting with #SBATCH) specifying resource requirements, followed by commands to execute. SLURM handles job scheduling and resource allocation."],["CAVEATS","Resource requests affect queue wait time. Exceeding time limit kills job. Environment may differ from submission shell. Check partition limits with sinfo."],["HISTORY","**sbatch** is part of **SLURM** (Simple Linux Utility for Resource Management), developed at Lawrence Livermore National Laboratory. SLURM is now the most widely used HPC job scheduler."],["INSTALL","```apt: sudo apt install slurm-client```"],["SEE ALSO","[squeue](/man/squeue)(1), [scancel](/man/scancel)(1), [sinfo](/man/sinfo)(1), [srun](/man/srun)(1)"]],"e":["squeue","scancel","sinfo","srun"],"x":"sbatch [job.sh]"},"setsebool":{"n":"setsebool","t":"Set SELinux boolean values at runtime","s":[["TAGLINE","Set SELinux boolean values at runtime"],["TLDR","**Set** a boolean temporarily\n\n```sudo setsebool [boolean_name] on```\n\n**Unset** a boolean temporarily\n\n```sudo setsebool [boolean_name] off```\n\n**Set** a boolean persistently across reboots\n\n```sudo setsebool -P [boolean_name] on```\n\n**Set** multiple booleans persistently\n\n```sudo setsebool -P [bool1]=[on] [bool2]=[off]```"],["SYNOPSIS","**setsebool** [_-PNV_] _boolean_ _value_ | _bool1=val1 bool2=val2..._"],["PARAMETERS","**-P**\n> Make changes persistent across reboots\n\n**-N**\n> Don't reload policy after change\n\n**-V**\n> Verbose output"],["DESCRIPTION","**setsebool** sets the current state of a particular SELinux boolean or a list of booleans to a given value. Booleans allow runtime customization of SELinux policy without requiring policy modification or recompilation.\n\nValues can be specified as 1, true, or on for enabled, or 0, false, or off for disabled. Multiple booleans can be set at once using the **bool=value** syntax."],["CAVEATS","Requires root privileges. Without -P flag, changes are lost on reboot. Use **getsebool** to view current boolean values. SELinux must be enabled for this command to work."],["INSTALL","```apt: sudo apt install policycoreutils```\n\n```dnf: sudo dnf install policycoreutils```\n\n```zypper: sudo zypper install policycoreutils```\n\n```nix: nix profile install nixpkgs#policycoreutils```"],["SEE ALSO","[getsebool](/man/getsebool)(8), [semanage-boolean](/man/semanage-boolean)(8), [setenforce](/man/setenforce)(8)"]],"e":["getsebool","semanage-boolean","setenforce"],"x":"sudo setsebool [boolean_name] on"},"sqlfluff":{"n":"sqlfluff","t":"Modular SQL linter and auto-formatter with dialect support","s":[["TAGLINE","Modular SQL linter and auto-formatter with dialect support"],["TLDR","**Lint** a SQL file with a specific dialect\n\n```sqlfluff lint --dialect [postgres] [path/to/query.sql]```\n\n**Auto-fix** lint violations in place\n\n```sqlfluff fix --dialect [bigquery] [path/to/queries/]```\n\n**Format** SQL via stdin\n\n```cat [query.sql] | sqlfluff format --dialect [snowflake] -```\n\n**Show parsed token tree** for a query\n\n```sqlfluff parse --dialect [mysql] [query.sql]```\n\n**Lint only specific rules**\n\n```sqlfluff lint --rules [LT01,LT02,CP01] [path/to/file.sql]```\n\n**Exclude noisy rules** from a run\n\n```sqlfluff lint --exclude-rules [L034,L036] [path/to/file.sql]```\n\n**Render** a templated query (expand Jinja/dbt) without linting\n\n```sqlfluff render --dialect [ansi] [query.sql]```"],["SYNOPSIS","**sqlfluff** _command_ [_options_] _PATH_...\n\nCommands: **lint**, **fix**, **format**, **parse**, **render**, **rules**, **dialects**, **fluff** (legacy alias)"],["PARAMETERS","**--dialect** _NAME_\n> SQL dialect to parse against (e.g. **ansi**, **postgres**, **mysql**, **bigquery**, **snowflake**, **redshift**, **tsql**, **sqlite**, **duckdb**, **databricks**). Run **sqlfluff dialects** for the full list.\n\n**--templater** _NAME_\n> Templating engine to use before parsing: **raw**, **jinja**, **python**, **placeholder**, or **dbt**.\n\n**--rules** _LIST_\n> Comma-separated list of rules (or rule groups) to enable.\n\n**--exclude-rules** _LIST_\n> Comma-separated list of rules to skip.\n\n**-f**, **--force**\n> For **fix**: apply changes without asking for confirmation.\n\n**--FIX-EVEN-UNPARSABLE**\n> Allow **fix** to operate on files with templating or parse errors (dangerous).\n\n**--processes** _N_\n> Number of parallel workers; **0** means all CPUs, negative means cpus + N.\n\n**-v**, **-vv**, **-vvv**\n> Increase verbosity; stackable.\n\n**--nocolor**\n> Disable ANSI color in output.\n\n**--format** _FMT_\n> Output format: **human**, **json**, **yaml**, **github-annotation**, **github-annotation-native**.\n\n**--config** _FILE_\n> Path to an explicit **.sqlfluff** configuration file.\n\n**--encoding** _ENC_\n> File encoding to use when reading sources (default **autodetect**).\n\n**--disable-noqa**\n> Ignore **-- noqa:** comments embedded in SQL."],["DESCRIPTION","**sqlfluff** is a dialect-aware linter and auto-formatter for SQL. It parses queries into an abstract syntax tree, applies a configurable set of rules, and reports or rewrites violations. Unlike text-based linters, the AST approach lets it enforce structural rules such as comma placement, keyword casing, ambiguous joins, and column-reference qualification.\n\nThe tool supports a wide range of dialects (PostgreSQL, MySQL, BigQuery, Snowflake, Redshift, T-SQL, SQLite, DuckDB, Databricks, ANSI, and more) and integrates with templating engines so it can lint queries written in **Jinja**, **dbt**, or custom placeholder syntaxes without first rendering them.\n\nOperation is split into commands: **lint** reports violations, **fix** rewrites the source in place, **format** applies layout-only fixes, **parse** dumps the token tree, and **render** expands templating without linting. Configuration is layered: built-in defaults are overridden by **.sqlfluff** files discovered up the directory tree, **pyproject.toml** sections, then command-line flags."],["CONFIGURATION","**.sqlfluff**\n> Per-project INI-style configuration; the search walks upward from the linted file. Example:\n\n```\n[sqlfluff]\ndialect = postgres\ntemplater = jinja\nexclude_rules = L034,L036\n\n[sqlfluff:rules:capitalisation.keywords]\ncapitalisation_policy = upper\n\n[sqlfluff:indentation]\ntab_space_size = 2\nindent_unit = space\n```\n\n**pyproject.toml**\n> Settings live under **[tool.sqlfluff.*]** sections that mirror the INI layout.\n\n**~/.sqlfluff**\n> User-level fallback applied to projects without their own config.\n\n**-- noqa:** comments\n> Inline directives to disable rules on a line or block, e.g. **SELECT * -- noqa: L044**."],["RULE GROUPS","Rules are namespaced by area: **LT** layout, **CP** capitalisation, **CV** convention, **RF** references, **ST** structure, **AM** ambiguity, **AL** aliasing, **JJ** Jinja. Use **sqlfluff rules** to print the complete catalog with codes, names, and descriptions."],["CAVEATS","Templated SQL must parse after rendering; unresolved variables raise template errors that suppress most lint output. **fix** rewrites files even when the underlying SQL has subtle semantic implications (e.g. column-order changes from re-aliasing); review diffs before committing. Dialect parsers vary in coverage; very recent vendor syntax may not yet be recognized. Older numeric rule codes (**L001**, **L002**, ...) were renamed to mnemonic codes (**LT01**, **CP01**, ...) in **2.0**; legacy codes are aliased but deprecated."],["HISTORY","**SQLFluff** was started by **Alan Cruickshank** in **2019** as an open-source dialect-aware alternative to text-based SQL linters. Version **1.0** shipped in **2022**, and **2.0** in **2023** brought a redesigned rule taxonomy with mnemonic codes and a templating-aware fix engine. The project is maintained on **github.com/sqlfluff/sqlfluff** under the MIT license."],["INSTALL","```pacman: sudo pacman -S sqlfluff```\n\n```brew: brew install sqlfluff```\n\n```nix: nix profile install nixpkgs#sqlfluff```"],["SEE ALSO","[dbt](/man/dbt)(1), [psql](/man/psql)(1)"]],"e":["dbt","psql"],"x":"sqlfluff lint --dialect [postgres] [path/to/query.sql]"},"ssh-keygen":{"n":"ssh-keygen","t":"authentication key generation, management and conversion for OpenSSH","s":[["TAGLINE","authentication key generation, management and conversion for OpenSSH"],["TLDR","**Generate an SSH key pair** (Ed25519, recommended)\n\n```ssh-keygen -t ed25519```\n\n**Generate an RSA key** with 4096 bits\n\n```ssh-keygen -t rsa -b 4096```\n\n**Generate a key with custom filename** and comment\n\n```ssh-keygen -t ed25519 -f [~/.ssh/mykey] -C \"[user@host]\"```\n\n**Change the passphrase** of an existing key\n\n```ssh-keygen -p -f [~/.ssh/id_ed25519]```\n\n**View the fingerprint** of a key\n\n```ssh-keygen -l -f [~/.ssh/id_ed25519.pub]```\n\n**Extract public key** from private key\n\n```ssh-keygen -y -f [~/.ssh/id_ed25519] > [~/.ssh/id_ed25519.pub]```\n\n**Generate host keys** (all standard types)\n\n```sudo ssh-keygen -A```\n\n**Convert key to different format**\n\n```ssh-keygen -e -f [~/.ssh/id_rsa.pub] -m [RFC4716]```"],["SYNOPSIS","**ssh-keygen** [_options_]\n\n**ssh-keygen** -t _type_ [-b _bits_] [-f _file_] [-C _comment_]"],["PARAMETERS","**-t** _TYPE_\n> Key type: ed25519 (recommended), rsa, ecdsa, dsa\n\n**-b** _BITS_\n> Key size in bits (RSA: 2048-4096, ECDSA: 256/384/521)\n\n**-f** _FILE_\n> Output filename for the key\n\n**-C** _COMMENT_\n> Comment to embed in key (typically user@host)\n\n**-N** _PASSPHRASE_\n> New passphrase for the key\n\n**-P** _PASSPHRASE_\n> Old passphrase when changing\n\n**-p**\n> Change passphrase of existing key\n\n**-l**\n> Show fingerprint of key file\n\n**-E** _HASH_\n> Hash algorithm for fingerprint (md5, sha256)\n\n**-y**\n> Output public key from private key\n\n**-e**\n> Export key to other formats\n\n**-i**\n> Import key from other formats\n\n**-m** _FORMAT_\n> Key format (RFC4716, PKCS8, PEM)\n\n**-A**\n> Generate all missing host key types\n\n**-R** _HOST_\n> Remove host from known_hosts\n\n**-F** _HOST_\n> Search for host in known_hosts\n\n**-q**\n> Quiet mode"],["DESCRIPTION","**ssh-keygen** generates, manages, and converts authentication keys for SSH. It creates public/private key pairs used for passwordless authentication and host verification.\n\nThe private key stays on your machine (protected by passphrase), while the public key is copied to servers you want to access. Modern best practice recommends **Ed25519** keys for their security and performance, though **RSA 4096** remains widely compatible.\n\nKeys are stored in **~/.ssh/** by default: private key as **id_ed25519** (or id_rsa, etc.) and public key with **.pub** extension. The public key is copied to **~/.ssh/authorized_keys** on remote servers for authentication."],["CAVEATS","Private keys must be kept secure—anyone with access can authenticate as you. Use a strong passphrase to protect private keys. DSA keys are deprecated and disabled in newer OpenSSH versions. Default RSA key size (3072) is secure but 4096 provides additional margin. Key permissions must be restrictive (600 for private, 644 for public)."],["HISTORY","ssh-keygen is part of **OpenSSH**, which originated from the OpenBSD project in **1999** as a free implementation of SSH. The original SSH protocol was developed by **Tatu Ylönen** in **1995**. OpenSSH added support for newer key types over time: ECDSA in 2011 and Ed25519 in 2014. Ed25519 became the default key type in OpenSSH 8.0 (2019) due to its security and efficiency."],["INSTALL","```apt: sudo apt install openssh-client```\n\n```apk: sudo apk add openssh-keygen```"],["SEE ALSO","[ssh](/man/ssh)(1), [ssh-copy-id](/man/ssh-copy-id)(1), [ssh-agent](/man/ssh-agent)(1), [sshd](/man/sshd)(8)"]],"e":["ssh","ssh-copy-id","ssh-agent","sshd"],"x":"ssh-keygen -t ed25519"},"supervisorctl":{"n":"supervisorctl","t":"Control Supervisor managed processes","s":[["TAGLINE","Control Supervisor managed processes"],["TLDR","**Show status**\n\n```supervisorctl status```\n\n**Start process**\n\n```supervisorctl start [process_name]```\n\n**Stop process**\n\n```supervisorctl stop [process_name]```\n\n**Restart process**\n\n```supervisorctl restart [process_name]```\n\n**Reload configuration**\n\n```supervisorctl reread && supervisorctl update```\n\n**Start all processes**\n\n```supervisorctl start all```\n\n**Tail process log**\n\n```supervisorctl tail -f [process_name]```\n\n**Interactive shell**\n\n```supervisorctl```"],["SYNOPSIS","**supervisorctl** [_options_] _command_ [_args_]"],["PARAMETERS","**status**\n> Show process status.\n\n**start** _NAME_\n> Start process.\n\n**stop** _NAME_\n> Stop process.\n\n**restart** _NAME_\n> Restart process.\n\n**reread**\n> Reload config files.\n\n**update**\n> Apply config changes.\n\n**tail** _NAME_\n> Show process output. Use **-f** to follow, **stderr** suffix for stderr stream.\n\n**pid** _NAME_\n> Display PID of the process.\n\n**signal** _SIG NAME_\n> Send a signal (e.g. HUP, TERM) to the process.\n\n**clear** _NAME_\n> Clear the process log files.\n\n**-c** _FILE_\n> Path to the supervisord configuration file.\n\n**-s** _URL_\n> Supervisor server URL (e.g. unix:///path or http://host:port).\n\n**-u** _USER_ / **-p** _PASSWORD_\n> Username and password for server authentication."],["DESCRIPTION","**supervisorctl** is the command-line client for controlling processes managed by the supervisord daemon. It provides commands to start, stop, restart, and monitor long-running processes, as well as view their status and log output.\n\nThe status command displays all managed processes with their state (RUNNING, STOPPED, FATAL), PID, and uptime. Individual processes or process groups can be controlled independently. The reread command detects configuration file changes, and update applies those changes without restarting unaffected processes.\n\nThe tool can operate in single-command mode or as an interactive shell with tab completion. The tail command streams process stdout/stderr output in real time, useful for debugging application issues. Configuration changes typically follow a reread-then-update workflow to safely apply modifications."],["CAVEATS","Requires supervisord running. Socket permissions may be needed. Config changes need update."],["HISTORY","**Supervisor** was created by **Chris McDonough** for process management. It's widely used for running application servers and background workers."],["INSTALL","```dnf: sudo dnf install supervisor```\n\n```pacman: sudo pacman -S supervisor```\n\n```apk: sudo apk add supervisor```\n\n```zypper: sudo zypper install supervisor```\n\n```brew: brew install supervisor```"],["SEE ALSO","[supervisord](/man/supervisord)(8), [systemctl](/man/systemctl)(1)"]],"e":["supervisord","systemctl"],"x":"supervisorctl status"},"systemctl-cancel":{"n":"systemctl-cancel","t":"Cancel pending systemd jobs","s":[["TAGLINE","Cancel pending systemd jobs"],["TLDR","**Cancel** job by ID\n\n```systemctl cancel [job_id]```\n\nCancel **multiple** jobs\n\n```systemctl cancel [job_id1 job_id2 ...]```\n\nCancel **all** pending jobs\n\n```systemctl cancel```\n\nCancel **user** job\n\n```systemctl cancel [job_id] --user```"],["SYNOPSIS","**systemctl cancel** [_JOB_...]"],["PARAMETERS","**--user**\n> Cancel jobs in the user service manager\n\n**--system**\n> Cancel jobs in the system manager (default)\n\n**-M**, **--machine** _container_\n> Execute the operation on a local container\n\n**-H**, **--host** _user@host_\n> Execute the operation on a remote host over SSH\n\n**--no-pager**\n> Do not pipe output into a pager"],["DESCRIPTION","**systemctl cancel** cancels one or more pending jobs in the systemd service manager. Jobs represent pending operations like starting, stopping, or restarting units. When called without arguments, all pending jobs are cancelled.\n\nJob IDs can be found using `systemctl list-jobs`. Cancelling a job aborts the pending operation without affecting the current state of the unit."],["CAVEATS","Cannot cancel already completed jobs. Some jobs may have dependencies that restart them. Cancelling critical boot jobs may leave the system in an inconsistent state."],["HISTORY","The **cancel** subcommand is part of **systemctl** in the **systemd** project. It provides control over the job queue that manages unit state transitions."],["SEE ALSO","[systemctl](/man/systemctl)(1), [systemctl-list-jobs](/man/systemctl-list-jobs)(1)"]],"e":["systemctl","systemctl-list-jobs"],"x":"systemctl cancel [job_id]"},"trex":{"n":"trex","t":"Terminal app for regex visualization and testing","s":[["TAGLINE","Terminal app for regex visualization and testing"],["TLDR","**Launch the interactive regex playground**\n\n```trex```\n\n**Load a file for regex testing**\n\n```trex -f [file]```"],["SYNOPSIS","**trex** [_options_]"],["DESCRIPTION","**trex** is a terminal-based tool for writing, visualizing, and testing regular expressions. It displays a live regex playground where you input patterns and test strings side by side, with dynamic highlighting of matches and capture groups. It also includes a regex help menu and file loading support."],["HISTORY","**trex** was created by **Samyak Bardiya** (samyakbardiya) and is written in **Go**."],["INSTALL","```nix: nix profile install nixpkgs#trex```"],["SEE ALSO","[grex](/man/grex)(1), [rexi](/man/rexi)(1), [ripgrep](/man/ripgrep)(1)"]],"e":["grex","rexi","ripgrep"],"x":"trex"},"tzselect":{"n":"tzselect","t":"Interactive timezone name selector","s":[["TAGLINE","Interactive timezone name selector"],["TLDR","**Select** timezone interactively\n\n```tzselect```\n\nFind timezone by **coordinates**\n\n```tzselect -c [coordinates]```"],["SYNOPSIS","**tzselect** [_OPTIONS_]"],["PARAMETERS","**-c** _COORDINATES_\n> Ask for nearest timezone to ISO 6709 coordinates\n\n**-n** _LIMIT_\n> Display at most this many choices"],["DESCRIPTION","**tzselect** provides an interactive menu for selecting a timezone. It walks through geographic regions to help identify the correct timezone. The selected timezone name is printed to stdout.\n\nNote: This program does not actually set the system timezone. It only helps identify the correct timezone name that can then be used with other tools to configure the system."],["CAVEATS","Does not modify system configuration. Output must be used with other tools to set timezone. Interactive mode requires terminal input."],["INSTALL","```apt: sudo apt install libc-bin```\n\n```zypper: sudo zypper install tzselect```"],["SEE ALSO","[timedatectl](/man/timedatectl)(1), [date](/man/date)(1)"]],"e":["timedatectl","date"],"x":"tzselect"},"udiskie-dmenu":{"n":"udiskie-dmenu","t":"Dmenu-based device mount/unmount interface","s":[["TAGLINE","Dmenu-based device mount/unmount interface"],["TLDR","**Launch** mount/unmount menu\n\n```udiskie-dmenu```\n\nUse **rofi** instead of dmenu\n\n```udiskie-dmenu --rofi```\n\n**Mount** devices only\n\n```udiskie-dmenu mount```\n\n**Unmount** devices only\n\n```udiskie-dmenu unmount```\n\nDisplay **help**\n\n```udiskie-dmenu --help```"],["SYNOPSIS","**udiskie-dmenu** [_OPTIONS_]"],["PARAMETERS","**mount**\n> Show only mountable devices.\n\n**unmount**\n> Show only unmountable (mounted) devices.\n\n**--rofi**\n> Use rofi instead of dmenu.\n\n**--help**\n> Display help information."],["DESCRIPTION","**udiskie-dmenu** provides a dmenu or rofi-based frontend for udiskie to mount and unmount devices interactively. It presents a menu of available devices for quick selection.\n\nThis tool integrates with tiling window managers that use dmenu or rofi as their primary launcher interface."],["CAVEATS","Requires udiskie and either dmenu or rofi installed. Available devices depend on connected hardware and udiskie configuration."],["INSTALL","```dnf: sudo dnf install udiskie```\n\n```pacman: sudo pacman -S udiskie```\n\n```apk: sudo apk add udiskie```\n\n```zypper: sudo zypper install udiskie```\n\n```nix: nix profile install nixpkgs#udiskie```"],["SEE ALSO","[udiskie](/man/udiskie)(1), [udisksctl](/man/udisksctl)(1), [dmenu](/man/dmenu)(1), [rofi](/man/rofi)(1)"]],"e":["udiskie","udisksctl","dmenu","rofi"],"x":"udiskie-dmenu"},"wiggle":{"n":"wiggle","t":"Apply rejected patches with conflict resolution","s":[["TAGLINE","Apply rejected patches with conflict resolution"],["TLDR","**Apply a rejected patch** in-place\n\n```wiggle --replace [original] [original.rej]```\n\n**Show a word-wise diff** between two files\n\n```wiggle --diff [file1] [file2]```\n\n**Perform a three-way merge**\n\n```wiggle --merge [original] [changed] [modified]```\n\n**Interactively browse** a merge or patch\n\n```wiggle --browse [original] [original.rej]```\n\n**Extract one branch** of a patch or merge\n\n```wiggle --extract --patch [file.patch] > [patched]```"],["SYNOPSIS","**wiggle** [_mode_] [_options_] _files_"],["PARAMETERS","**-m**, **--merge**\n> Three-way merge mode (default).\n\n**-d**, **--diff**\n> Word-wise diff mode.\n\n**-x**, **--extract**\n> Extract one file/branch from patch or merge.\n\n**-B**, **--browse**\n> Interactive ncurses browser for merges, diffs, or patches.\n\n**-r**, **--replace**\n> Overwrite the original file with the merge result (keeps `.porig` backup).\n\n**-o** _file_, **--output** _file_\n> Write output to given file instead of stdout.\n\n**-p**, **--patch**\n> Treat one of the arguments as a patch file.\n\n**-w**, **--words**\n> Work at word granularity (default for text).\n\n**-l**, **--lines**\n> Work at line granularity.\n\n**-b**, **--ignore-blanks**\n> Ignore whitespace-only changes when merging.\n\n**-i**, **--no-ignore**\n> Do not ignore already-applied patches (include them in output).\n\n**-W**, **--show-wiggles**\n> Mark hunks that were wiggled into place.\n\n**-q**, **--quiet**\n> Suppress informational messages.\n\n**-v**, **--verbose**\n> Increase verbosity.\n\n**-h**, **--help**\n> Display help and exit.\n\n**-V**, **--version**\n> Display version and exit."],["DESCRIPTION","**wiggle** applies patches that have been rejected by the standard patch utility due to minor conflicts between the patch context and the current file. Where patch requires exact context matching, wiggle performs word-level analysis to find where changes should be applied even when surrounding lines have shifted or been modified.\n\nThe tool supports three-way merging of files and can work with both unified diff files and .rej reject files produced by failed patch operations. When conflicts cannot be resolved automatically, an interactive browse mode lets users review and resolve remaining conflicts manually.\n\nThis makes wiggle particularly valuable for maintaining long-lived patch series against evolving codebases, where patches frequently fail to apply cleanly due to unrelated changes in nearby code."],["CAVEATS","For patch failures. May not always work. Line changes needed."],["HISTORY","**wiggle** was created by **Neil Brown** to help apply patches that were rejected due to minor conflicts."],["INSTALL","```apt: sudo apt install wiggle```\n\n```apk: sudo apk add wiggle```\n\n```zypper: sudo zypper install wiggle```\n\n```brew: brew install wiggle```\n\n```nix: nix profile install nixpkgs#wiggle```"],["SEE ALSO","[patch](/man/patch)(1), [diff](/man/diff)(1), [merge](/man/merge)(1)"]],"e":["patch","diff","merge"],"x":"wiggle --replace [original] [original.rej]"},"wine-reg":{"n":"wine-reg","t":"Read and modify the Wine registry from the command line","s":[["TAGLINE","Read and modify the Wine registry from the command line"],["TLDR","**Add** a value to a registry key\n\n```wine reg add [HKCU\\Software\\MyApp] /v [Setting] /t [REG_SZ] /d [value] /f```\n\n**Query** a key and its values\n\n```wine reg query [HKCU\\Software\\MyApp]```\n\n**Delete** a value from a key\n\n```wine reg delete [HKCU\\Software\\MyApp] /v [Setting] /f```\n\n**Copy** a key (with subkeys) to another location\n\n```wine reg copy [HKCU\\Software\\MyApp] [HKCU\\Software\\MyAppBackup] /s /f```\n\n**Import** registry entries from a .reg file\n\n```wine reg import [file.reg]```\n\n**Export** a key to a .reg file\n\n```wine reg export [HKCU\\Software\\MyApp] [file.reg]```"],["SYNOPSIS","**wine reg** _operation_ _key_ [_options_]\n\noperation := **add** | **delete** | **query** | **copy** | **import** | **export**"],["DESCRIPTION","**wine reg** is Wine's reimplementation of the Windows **reg.exe** console tool. It lets you inspect and edit the registry of a Wine prefix without launching the graphical **regedit**, which is handy for scripting installers, applying tweaks in a Wineprefix, and automating application configuration.\n\nRegistry keys are addressed by their root and path, for example **HKEY_CURRENT_USER\\Software\\MyApp**. The standard root abbreviations are accepted: **HKLM** (HKEY_LOCAL_MACHINE), **HKCU** (HKEY_CURRENT_USER), **HKCR** (HKEY_CLASSES_ROOT), **HKU** (HKEY_USERS), and **HKCC** (HKEY_CURRENT_CONFIG).\n\nThe tool operates on the registry of the prefix named by the **WINEPREFIX** environment variable (defaulting to **~/.wine**). Each operation is a separate subcommand documented in its own page."],["OPERATIONS","**add**\n> Create a key, or add or overwrite a value within it.\n\n**delete**\n> Remove a key, a single value, or all values under a key.\n\n**query**\n> Display the values stored under a key, optionally recursing into subkeys.\n\n**copy**\n> Copy a key's values (and optionally its subkeys) to another key.\n\n**import**\n> Merge the contents of a .reg file into the registry.\n\n**export**\n> Write a key and its contents to a .reg file."],["CAVEATS","Backslashes in key paths may need quoting or escaping depending on your shell. Editing the registry of the wrong prefix is a common mistake; set **WINEPREFIX** explicitly when you maintain more than one. As with the native Windows tool, careless changes can break applications, so back up keys with **export** before modifying them."],["INSTALL","```dnf: sudo dnf install wine```\n\n```pacman: sudo pacman -S wine```\n\n```apk: sudo apk add wine```\n\n```zypper: sudo zypper install wine```\n\n```nix: nix profile install nixpkgs#wine```"],["SEE ALSO","[wine](/man/wine)(1), [reg](/man/reg)(1), [wine-reg-add](/man/wine-reg-add)(1), [wine-reg-delete](/man/wine-reg-delete)(1), [wine-reg-query](/man/wine-reg-query)(1), [wine-reg-copy](/man/wine-reg-copy)(1)"],["RESOURCES","```[Source code](https://gitlab.winehq.org/wine/wine)```\n\n```[Homepage](https://www.winehq.org)```\n\n```[Documentation](https://gitlab.winehq.org/wine/wine/-/wikis/home)```"]],"e":["wine","reg","wine-reg-add","wine-reg-delete","wine-reg-query","wine-reg-copy"],"x":"wine reg add [HKCU\\Software\\MyApp] /v [Setting] /t [REG_SZ] /d [value] /f"},"zapier-scaffold":{"n":"zapier-scaffold","t":"Generate Zapier integration boilerplate code","s":[["TAGLINE","Generate Zapier integration boilerplate code"],["TLDR","**Scaffold trigger**\n\n```zapier scaffold trigger [name]```\n\n**Scaffold create action**\n\n```zapier scaffold create [name]```\n\n**Scaffold search action**\n\n```zapier scaffold search [name]```"],["SYNOPSIS","**zapier** **scaffold** _type_ _name_ [_options_]"],["PARAMETERS","**trigger** _name_\n> Create trigger scaffold.\n\n**create** _name_\n> Create action scaffold.\n\n**search** _name_\n> Create search scaffold.\n\n**resource** _name_\n> Create resource scaffold."],["DESCRIPTION","**zapier scaffold** generates boilerplate code for integration components. Creates triggers, actions, and searches with starter code. Speeds up integration development."],["SEE ALSO","[zapier](/man/zapier)(1), [zapier-init](/man/zapier-init)(1)"]],"e":["zapier","zapier-init"],"x":"zapier scaffold trigger [name]"},"zeitfetch":{"n":"zeitfetch","t":"Instantaneous system information snapshots","s":[["TAGLINE","Instantaneous system information snapshots"],["TLDR","**Display system information**\n\n```zeitfetch```\n\n**Display without ASCII art logo**\n\n```zeitfetch --no-logo```\n\n**Display in minimal vertical layout**\n\n```zeitfetch --minimal```"],["SYNOPSIS","**zeitfetch** [_options_]"],["DESCRIPTION","**zeitfetch** is a neofetch-like system information tool that aims to be near-instantaneous with minimal dependencies. It displays OS, kernel, uptime, shell, and hardware info with ASCII art logos. Fully cross-platform on Windows, macOS, and Linux."],["HISTORY","**zeitfetch** was created by **Henrique** (nidnogg) and is written in **Rust**."],["INSTALL","```dnf: sudo dnf install zeitfetch```"],["SEE ALSO","[neofetch](/man/neofetch)(1), [fastfetch](/man/fastfetch)(1), [onefetch](/man/onefetch)(1)"]],"e":["neofetch","fastfetch","onefetch"],"x":"zeitfetch"}});
