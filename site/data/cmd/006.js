window.LCL_BUCKET(6,{"a2disconf":{"n":"a2disconf","t":"disable an Apache2 configuration file","s":[["TAGLINE","disable an Apache2 configuration file"],["TLDR","**Disable** a configuration file\n\n```sudo a2disconf [configuration_file]```\n\nDon't show **informative messages**\n\n```sudo a2disconf -q [configuration_file]```\n\nDisable a configuration and **purge** all traces from the internal state database\n\n```sudo a2disconf -p [configuration_file]```"],["SYNOPSIS","**a2disconf** [_-q_|_--quiet_] [_-m_|_--maintmode_] [_-p_|_--purge_] [_configuration_]"],["DESCRIPTION","**a2disconf** is a Debian-specific utility that disables Apache2 configuration files by removing symbolic links from **/etc/apache2/conf-enabled**. It works alongside **a2enconf**, which enables configurations.\n\nThe tool silently succeeds if the specified configuration is already disabled, making it safe for use in scripts. After disabling a configuration, Apache must be restarted or reloaded for changes to take effect."],["PARAMETERS","**-q, --quiet**\n> Don't show informative messages\n\n**-m, --maintmode**\n> Enables maintainer mode; the program invocation is effectuated automatically by a maintainer script\n\n**-p, --purge**\n> When disabling a configuration, purge all traces from the internal state database"],["CAVEATS","This utility is **Debian-specific** and not available on other Linux distributions. Configuration changes require an Apache restart or reload to take effect."],["HISTORY","Created as part of the Apache2 package for **Debian GNU/Linux**. The manual page was authored by Arno Toell and dates to **February 2012**."],["INSTALL","```apt: sudo apt install apache2```\n\n```zypper: sudo zypper install apache2```\n\n```brew: brew install apache2```"],["SEE ALSO","[a2enconf](/man/a2enconf)(8), [a2enmod](/man/a2enmod)(8), [a2dismod](/man/a2dismod)(8), [a2ensite](/man/a2ensite)(8), [a2dissite](/man/a2dissite)(8), [apache2ctl](/man/apache2ctl)(8)"],["RESOURCES","```[Homepage](https://httpd.apache.org/)```\n\n```[Documentation](https://manpages.debian.org/a2disconf)```"]],"e":["a2enconf","a2enmod","a2dismod","a2ensite","a2dissite","apache2ctl"],"x":"sudo a2disconf [configuration_file]"},"adguardian-term":{"n":"adguardian-term","t":"Terminal dashboard for AdGuard Home","s":[["TAGLINE","Terminal dashboard for AdGuard Home"],["TLDR","**Launch the dashboard** with connection details\n\n```ADGUARD_IP=\"192.168.1.1\" ADGUARD_PORT=\"3000\" ADGUARD_USERNAME=\"admin\" ADGUARD_PASSWORD=\"pass\" adguardian```\n\n**Launch with command-line flags**\n\n```adguardian --adguard-ip [192.168.1.1] --adguard-port [3000] --adguard-username [admin] --adguard-password [pass]```\n\n**Run via Docker**\n\n```docker run -e ADGUARD_IP=192.168.1.1 -e ADGUARD_PORT=3000 -e ADGUARD_USERNAME=admin -e ADGUARD_PASSWORD=pass -it lissy93/adguardian```"],["SYNOPSIS","**adguardian** [_options_]"],["PARAMETERS","**--adguard-ip** _address_\n> IP address of the AdGuard Home instance\n\n**--adguard-port** _port_\n> Port number of the AdGuard Home instance\n\n**--adguard-username** _user_\n> Authentication username\n\n**--adguard-password** _pass_\n> Authentication password"],["DESCRIPTION","**adguardian-term** is a terminal-based real-time traffic monitoring and statistics dashboard for AdGuard Home instances. It displays DNS query activity, block/allow statistics, active filter lists, and top queried domains in an interactive TUI. Written in Rust using ratatui for the interface, it communicates with the AdGuard Home API to provide a lightweight alternative to the web UI.\n\nThe dashboard refreshes automatically and shows queries allowed, filtered, or blocked, along with historical trends and domain analytics."],["CONFIGURATION","Configuration can be set via environment variables or command-line flags. Supported environment variables: **ADGUARD_IP**, **ADGUARD_PORT**, **ADGUARD_USERNAME**, **ADGUARD_PASSWORD**, **ADGUARD_PROTOCOL** (default: http), and **ADGUARD_UPDATE_INTERVAL** (default: 2 seconds)."],["CAVEATS","Requires a running AdGuard Home instance with API access. Credentials must be provided via environment variables or flags. The tool only communicates with the configured AdGuard Home instance and makes no other external requests."],["HISTORY","**adguardian-term** was created by **Alicia Sykes** (Lissy93) and is open-source under the MIT license. It was originally written in Go and later rewritten in Rust. The project was inspired by **PADD**, a similar dashboard for Pi-hole."],["SEE ALSO","[pihole](/man/pihole)(1), [htop](/man/htop)(1)"]],"e":["pihole","htop"],"x":"ADGUARD_IP=\"192.168.1.1\" ADGUARD_PORT=\"3000\" ADGUARD_USERNAME=\"admin\" ADGUARD_PASSWORD=\"pass\" adguardian"},"auto-cpufreq":{"n":"auto-cpufreq","t":"Automatic CPU speed and power optimizer","s":[["TAGLINE","Automatic CPU speed and power optimizer"],["TLDR","**Monitor** CPU frequency and power\n\n```sudo auto-cpufreq --monitor```\n\nRun in **live** mode (temporary optimization)\n\n```sudo auto-cpufreq --live```\n\nShow **statistics**\n\n```sudo auto-cpufreq --stats```\n\n**Install** as system service\n\n```sudo auto-cpufreq --install```\n\n**Remove** system service\n\n```sudo auto-cpufreq --remove```\n\n**Force** a specific governor\n\n```sudo auto-cpufreq --force=powersave```\n\n**Reset** a forced governor back to automatic behavior\n\n```sudo auto-cpufreq --force=reset```\n\n**Update** auto-cpufreq\n\n```sudo auto-cpufreq --update```"],["SYNOPSIS","**auto-cpufreq** [_options_]"],["DESCRIPTION","**auto-cpufreq** is an automatic CPU speed and power optimizer for Linux. It dynamically adjusts CPU frequency scaling and turbo boost based on system load and power source (AC/battery) to optimize performance and battery life."],["PARAMETERS","**--monitor**\n> Monitor CPU frequency, governor, and power information\n\n**--live**\n> Run in live mode with temporary optimization\n\n**--stats**\n> Display statistics from daemon mode\n\n**--install**\n> Install auto-cpufreq as a system service\n\n**--remove**\n> Remove auto-cpufreq system service\n\n**--update**\n> Update auto-cpufreq to latest version\n\n**--force=GOVERNOR**\n> Force a specific governor (powersave, performance) or use reset to return to automatic mode\n\n**--turbo=MODE**\n> Force turbo boost behavior (always, never, auto)\n\n**--config=PATH**\n> Use a custom configuration file\n\n**--debug**\n> Show debug information\n\n**--version**\n> Show the installed auto-cpufreq version"],["CONFIGURATION","**/etc/auto-cpufreq.conf**\n> System-wide configuration file. Defines governor and turbo boost preferences per power source (charger/battery).\n\n**$XDG_CONFIG_HOME/auto-cpufreq/auto-cpufreq.conf**\n> Per-user configuration file, checked before the system-wide path. A file given with --config takes precedence over both."],["CAVEATS","Using --install creates a systemd service for automatic operation. The --live and --monitor modes are for testing and do not persist after reboot. For laptops, auto-cpufreq automatically switches between performance and powersave based on power source."],["HISTORY","**auto-cpufreq** was created to provide automatic CPU frequency optimization without manual tuning, particularly useful for laptops to extend battery life while maintaining performance when needed."],["INSTALL","```aur: yay -S auto-cpufreq```\n\n```nix: nix profile install nixpkgs#auto-cpufreq```"],["SEE ALSO","[cpupower](/man/cpupower)(1), [cpufreq-info](/man/cpufreq-info)(1), [tlp](/man/tlp)(8)"],["RESOURCES","```[Source code](https://github.com/AdnanHodzic/auto-cpufreq)```"]],"e":["cpupower","cpufreq-info","tlp"],"x":"sudo auto-cpufreq --monitor"},"aws-iam":{"n":"aws-iam","t":"Manage identity, access, and permissions for AWS resources.","s":[["TAGLINE","Manage identity, access, and permissions for AWS resources."],["TLDR","**Create a new IAM user**\n\n```aws iam create-user --user-name [my-user]```\n\n**Create access keys** for a user\n\n```aws iam create-access-key --user-name [my-user]```\n\n**Attach a policy** to a user\n\n```aws iam attach-user-policy --user-name [my-user] --policy-arn [arn:aws:iam::aws:policy/ReadOnlyAccess]```\n\n**Create a role** with a trust policy\n\n```aws iam create-role --role-name [my-role] --assume-role-policy-document file://[trust-policy.json]```\n\n**List all users** in the account\n\n```aws iam list-users```\n\n**Create a group** and add a user\n\n```aws iam create-group --group-name [developers] && aws iam add-user-to-group --user-name [my-user] --group-name [developers]```\n\n**Generate a credential report**\n\n```aws iam generate-credential-report && aws iam get-credential-report --output text --query Content | base64 -d```"],["SYNOPSIS","**aws iam** _command_ [_options_]"],["DESCRIPTION","**aws iam** is the AWS CLI interface for AWS Identity and Access Management (IAM), the service for controlling access to AWS resources. IAM enables creating users, groups, roles, and policies to manage authentication and authorization.\n\nIAM is global (not region-specific) and provides fine-grained access control through JSON policies. It supports identity federation with SAML 2.0 and OIDC providers, and multi-factor authentication for enhanced security."],["COMMANDS","**create-user**\n> Create a new IAM user\n\n**delete-user**\n> Remove a user\n\n**create-access-key**\n> Generate access key credentials\n\n**create-role**\n> Create a role for service or cross-account access\n\n**attach-user-policy**\n> Attach a managed policy to a user\n\n**attach-role-policy**\n> Attach a managed policy to a role\n\n**create-policy**\n> Create a custom managed policy\n\n**create-group**\n> Create a user group\n\n**add-user-to-group**\n> Add user to a group\n\n**list-users**\n> List all IAM users\n\n**get-user**\n> Get user details\n\n**simulate-principal-policy**\n> Test policy permissions"],["CAVEATS","Policy changes can take several seconds to propagate. The root account should never be used for daily operations. Access keys should be rotated regularly. IAM has a limit of 5000 users per account. Inline policies are harder to audit than managed policies."],["HISTORY","AWS IAM launched in **May 2010** as the access control system for AWS. Roles were introduced in **2012** for cross-account and service access. Policy conditions expanded significantly over the years, and IAM Identity Center (formerly SSO) was added in **2017** for centralized access management."],["INSTALL","```dnf: sudo dnf install aws```\n\n```pacman: sudo pacman -S aws-cli```\n\n```apk: sudo apk add aws-cli```\n\n```zypper: sudo zypper install aws-cli```"],["SEE ALSO","[aws](/man/aws)(1), [aws-sts](/man/aws-sts)(1), [aws-organizations](/man/aws-organizations)(1)"],["RESOURCES","```[Source code](https://github.com/aws/aws-cli)```\n\n```[Homepage](https://aws.amazon.com/cli/)```\n\n```[Documentation](https://docs.aws.amazon.com/cli/latest/reference/iam/index.html)```"]],"e":["aws","aws-sts","aws-organizations"],"x":"aws iam create-user --user-name [my-user]"},"baobab":{"n":"baobab","t":"Graphical disk usage analyzer","s":[["TAGLINE","Graphical disk usage analyzer"],["TLDR","**Analyze** disk usage\n\n```baobab```\n\nAnalyze **specific directory**\n\n```baobab [/path/to/directory]```"],["SYNOPSIS","**baobab** [_directory_]"],["DESCRIPTION","**baobab** (also known as GNOME Disk Usage Analyzer) is a graphical tool for visualizing disk space usage. It displays directory sizes in tree view and provides an interactive treemap or ring chart visualization.\n\nThe application makes it easy to identify large files and directories consuming disk space."],["FEATURES","- Directory tree view\n- Treemap visualization\n- Ring chart (sunburst) view\n- File and folder sizes\n- Interactive navigation\n- Remote filesystem support\n- Scan history"],["INTERFACE","Provides multiple visualization modes:\n- **List view** - Traditional directory tree\n- **Treemap** - Rectangular area chart\n- **Rings chart** - Concentric circles showing hierarchy"],["CAVEATS","Scanning large filesystems can be slow. Remote filesystems require network access. Some directories require elevated permissions. May not reflect actual disk usage (sparse files, compression)."],["HISTORY","**Baobab** was created by Fabio Marzocca around **2005** and became GNOME's standard disk usage analyzer, named after the baobab tree."],["INSTALL","```apt: sudo apt install baobab```\n\n```dnf: sudo dnf install baobab```\n\n```pacman: sudo pacman -S baobab```\n\n```apk: sudo apk add baobab```\n\n```zypper: sudo zypper install baobab```\n\n```brew: brew install baobab```\n\n```nix: nix profile install nixpkgs#baobab```"],["SEE ALSO","[du](/man/du)(1), [ncdu](/man/ncdu)(1)"]],"e":["du","ncdu"],"x":"baobab"},"base32":{"n":"base32","t":"Encode and decode base32 data.","s":[["TAGLINE","Encode and decode base32 data."],["TLDR","**Encode** to base32\n\n```base32 [file]```\n\n**Decode** base32\n\n```base32 -d [file.b32]```\n\nEncode **string**\n\n```echo [text] | base32```\n\nDecode **string**\n\n```echo [ENCODED] | base32 -d```"],["SYNOPSIS","**base32** [_options_] [_file_]"],["DESCRIPTION","**base32** encodes and decodes data using base32 encoding. Base32 represents binary data using 32 ASCII characters (A-Z and 2-7), making it suitable for case-insensitive systems and human-readable tokens.\n\nThe tool is part of GNU coreutils and useful for encoding data in URLs, filenames, or other contexts where base64's case sensitivity or special characters are problematic."],["PARAMETERS","**-d**, **--decode**\n> Decode base32 data\n\n**-i**, **--ignore-garbage**\n> Ignore non-alphabet characters when decoding\n\n**-w**, **--wrap=**_cols_\n> Wrap lines at specified width (default: 76, 0 = no wrap)"],["FEATURES","- Case-insensitive encoding\n- No special characters (URL-safe)\n- Standard RFC 4648 implementation\n- Padding with = characters"],["WORKFLOW","```bash"],["Encode file","base32 file.bin > file.b32"],["Decode file","base32 -d file.b32 > file.bin"],["Encode string","echo \"Hello World\" | base32"],["Decode string","echo \"JBSWY3DPEBLW64TMMQ======\" | base32 -d\n```"],["CAVEATS","20% larger than base64 encoding. Less common than base64. Padding characters may need escaping in some contexts. Case-insensitive but output is uppercase."],["HISTORY","**base32** was added to GNU coreutils in version **8.25** (2016), implementing the base32 encoding specified in RFC 4648."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[base64](/man/base64)(1), [basenc](/man/basenc)(1), [b2sum](/man/b2sum)(1), [uuencode](/man/uuencode)(1)"],["RESOURCES","```[Source code](https://github.com/coreutils/coreutils)```\n\n```[Documentation](https://www.gnu.org/software/coreutils/manual/html_node/base32-invocation.html)```"]],"e":["base64","basenc","b2sum","uuencode"],"x":"base32 [file]"},"bfg":{"n":"bfg","t":"Remove unwanted data from Git repository history","s":[["TAGLINE","Remove unwanted data from Git repository history"],["TLDR","**Remove** file from history\n\n```bfg --delete-files [secrets.txt] [repo.git]```\n\n**Remove** large files\n\n```bfg --strip-blobs-bigger-than [100M] [repo.git]```\n\n**Replace** text in history\n\n```bfg --replace-text [passwords.txt] [repo.git]```\n\n**Delete** folders\n\n```bfg --delete-folders [.svn] [repo.git]```"],["SYNOPSIS","**bfg** [_options_] _repository_"],["DESCRIPTION","**bfg** (BFG Repo-Cleaner) is a faster, simpler alternative to git-filter-branch for removing unwanted data from Git repositories. It's designed for cleaning up accidentally committed passwords, large files, or other sensitive data from repository history.\n\nThe tool is significantly faster than git-filter-branch and easier to use for common cleaning tasks."],["PARAMETERS","**--delete-files** _pattern_ (**-D**)\n> Delete files with the specified names (e.g. '*.class', '*.{txt,log}')\n\n**--delete-folders** _name_\n> Delete folders with the specified names (e.g. '.svn', '*-tmp')\n\n**--strip-blobs-bigger-than** _size_ (**-b**)\n> Strip blobs bigger than the given size (e.g. 128K, 1M, 100M)\n\n**--strip-biggest-blobs** _num_ (**-B**)\n> Strip the top NUM biggest blobs\n\n**--strip-blobs-with-ids** _file_ (**-bi**)\n> Strip blobs with the Git object ids listed in the file\n\n**--replace-text** _file_ (**-rt**)\n> Filter file content, replacing matched text (patterns one per line)\n\n**--convert-to-git-lfs** _pattern_\n> Extract files with the specified names (e.g. '*.zip') into Git LFS\n\n**--no-blob-protection**\n> Allow the BFG to modify even your latest commit\n\n**--private**\n> Treat this rewrite as removing private data (suppresses the public-data hint)"],["WORKFLOW","```bash"],["Clone a mirror","git clone --mirror https://github.com/user/repo.git"],["Run BFG","bfg --delete-files passwords.txt repo.git"],["Clean up and push","cd repo.git\ngit reflog expire --expire=now --all\ngit gc --prune=now --aggressive\ngit push --force\n```"],["REPLACE TEXT FORMAT","File with patterns (passwords.txt):\n```\nPASSWORD1==>***REMOVED***\nsecret_key==>***REMOVED***\n```"],["FEATURES","- 10-1000x faster than git-filter-branch\n- Simple pattern-based deletion\n- Protects current commit by default\n- Batch text replacement\n- Large file cleanup\n- Folder deletion"],["CAVEATS","Rewrites history (force push required). Protects HEAD by default (use --no-blob-protection to override). Collaborators must rebase/re-clone. BFG operates on filenames and sizes, not paths: it cannot delete a file only in one directory. For path-specific or otherwise complex rewrites, use git-filter-repo. Requires a Java 11+ runtime (v1.14.0 was the last release to support Java 8)."],["HISTORY","**BFG Repo-Cleaner** was created by Roberto Tyley in **2012** to provide a faster, user-friendly alternative to git-filter-branch for repository cleaning."],["INSTALL","```aur: yay -S bfg```\n\n```brew: brew install bfg```"],["SEE ALSO","[git-filter-branch](/man/git-filter-branch)(1), [git-filter-repo](/man/git-filter-repo)(1), [git](/man/git)(1)"],["RESOURCES","```[Source code](https://github.com/rtyley/bfg-repo-cleaner)```\n\n```[Homepage](https://rtyley.github.io/bfg-repo-cleaner/)```"]],"e":["git-filter-branch","git-filter-repo","git"],"x":"bfg --delete-files [secrets.txt] [repo.git]"},"bgpd":{"n":"bgpd","t":"Border Gateway Protocol routing daemon","s":[["TAGLINE","Border Gateway Protocol routing daemon"],["TLDR","**Start bgpd** with default configuration\n\n```bgpd```\n\n**Start with a specific configuration file**\n\n```bgpd -f [/etc/bgpd.conf]```\n\n**Run in foreground** with debug output\n\n```bgpd -d```\n\n**Check configuration syntax** without starting\n\n```bgpd -n```\n\n**Start with verbose logging**\n\n```bgpd -v```"],["SYNOPSIS","**bgpd** [**-dnv**] [**-D** _macro=value_] [**-f** _file_]"],["DESCRIPTION","**bgpd** is the Border Gateway Protocol daemon from the OpenBGPD project. It manages BGP sessions with peer routers and maintains the Routing Information Base (RIB) for exchanging routing information between autonomous systems.\n\nBGP is the protocol that makes the Internet work by enabling routing between different networks (autonomous systems). bgpd implements BGP-4 and supports features like route filtering, communities, local preference, MED, and AS path manipulation.\n\nThe daemon reads its configuration from /etc/bgpd.conf, which defines neighbors, filters, and routing policies. It runs as an unprivileged user after initialization and communicates with bgpctl for runtime management."],["PARAMETERS","**-d**\n> Run in foreground, do not daemonize.\n\n**-D** _macro=value_\n> Define a macro to be used in config.\n\n**-f** _file_\n> Use specified configuration file.\n\n**-n**\n> Check configuration syntax only.\n\n**-v**\n> Produce verbose output.\n\n**-V**\n> Show version information."],["CAVEATS","Running a BGP daemon on the public Internet requires coordination with upstream providers and valid AS number assignment. Misconfiguration can cause routing issues affecting network connectivity. bgpd requires root privileges to start but drops privileges after initialization. Configuration changes typically require a restart or reload signal."],["HISTORY","**OpenBGPD** was developed starting in **2003** by Henning Brauer and Claudio Jeker for **OpenBSD** as part of the OpenBSD networking stack. It was created to provide a secure, clean BGP implementation after vulnerabilities and complexity issues with other routing daemons. The portable version for Linux and FreeBSD has been maintained since **2016**. OpenBGPD is used by ISPs and network operators valuing its simplicity and security."],["INSTALL","```apt: sudo apt install openbgpd```\n\n```dnf: sudo dnf install openbgpd```\n\n```apk: sudo apk add quagga```\n\n```nix: nix profile install nixpkgs#openbgpd```"],["SEE ALSO","[bgpctl](/man/bgpctl)(8), [ospfd](/man/ospfd)(8), [bird](/man/bird)(8)"]],"e":["bgpctl","ospfd","bird"],"x":"bgpd"},"borg":{"n":"borg","t":"Deduplicating backup with compression and encryption","s":[["TAGLINE","Deduplicating backup with compression and encryption"],["TLDR","**Initialize** repository\n\n```borg init --encryption=[repokey] [/path/to/repo]```\n\n**Create** backup\n\n```borg create [/path/to/repo::backup-{now}] [/path/to/data]```\n\n**List** archives\n\n```borg list [/path/to/repo]```\n\n**Extract** backup\n\n```borg extract [/path/to/repo::backup-name]```\n\n**Check** repository\n\n```borg check [/path/to/repo]```\n\n**Prune** old backups\n\n```borg prune --keep-daily [7] --keep-weekly [4] [/path/to/repo]```\n\n**Compact** the repository to actually free pruned space\n\n```borg compact [/path/to/repo]```\n\n**Mount** an archive to browse it as a filesystem\n\n```borg mount [/path/to/repo::backup-name] [/mnt/point]```"],["SYNOPSIS","**borg** _command_ [_options_]"],["DESCRIPTION","**borg** (BorgBackup) is a deduplicating backup program with compression and authenticated encryption. It efficiently stores multiple backups by sharing common data blocks, making it ideal for frequent backups of large datasets.\n\nThe tool provides security, efficiency, and reliability for local and remote backups."],["PARAMETERS","**init**\n> Initialize repository\n\n**create**\n> Create new archive\n\n**extract**\n> Extract archive contents\n\n**list**\n> List archives or contents\n\n**diff**\n> Compare archives\n\n**delete**\n> Delete archive\n\n**prune**\n> Delete old archives by retention policy\n\n**compact**\n> Free repository space freed by prune or delete (run after pruning)\n\n**info**\n> Show archive/repository information\n\n**mount**\n> Mount archive as filesystem\n\n**check**\n> Verify repository consistency"],["ENCRYPTION MODES","**repokey**\n> Key stored in repository\n\n**keyfile**\n> Key stored locally\n\n**none**\n> No encryption\n\n**authenticated**\n> Authenticated only (no encryption)"],["WORKFLOW","```bash"],["Initialize repository","borg init --encryption=repokey /backup/repo"],["Create first backup","borg create /backup/repo::monday /home/user"],["Create incremental backup (automatic deduplication)","borg create /backup/repo::tuesday /home/user"],["List backups","borg list /backup/repo"],["Restore","borg extract /backup/repo::monday"],["Mount for browsing","borg mount /backup/repo::monday /mnt/backup"],["Prune old backups","borg prune --keep-daily=7 --keep-weekly=4 /backup/repo"],["Reclaim the space freed by prune","borg compact /backup/repo\n```"],["FEATURES","**Deduplication**\n> Chunk-level deduplication shares common data across archives\n\n**Compression**\n> Supports lz4, zstd, zlib, and lzma\n\n**Encryption**\n> Authenticated AES encryption with repokey or keyfile modes\n\n**Remote backups**\n> Push backups to a remote repository over SSH\n\n**Other**\n> Sparse file support, archive mounting via FUSE, data verification, and efficient storage"],["CAVEATS","Repository corruption can lose all backups (keep offsite copies). Encryption key loss means permanent data loss. Large repos need significant RAM for cache. SSH connection required for remote repos. Check operations can be slow."],["HISTORY","**BorgBackup** was created by Thomas Waldmann in **2015** as a fork of Attic backup, focusing on performance, security, and reliability."],["INSTALL","```apt: sudo apt install borgbackup```\n\n```dnf: sudo dnf install borgbackup```\n\n```pacman: sudo pacman -S borg```\n\n```apk: sudo apk add borgbackup```\n\n```zypper: sudo zypper install borgbackup```\n\n```brew: brew install borgbackup```\n\n```nix: nix profile install nixpkgs#borgbackup```"],["SEE ALSO","[restic](/man/restic)(1), [duplicity](/man/duplicity)(1), [rsync](/man/rsync)(1)"],["RESOURCES","```[Source code](https://github.com/borgbackup/borg)```\n\n```[Homepage](https://www.borgbackup.org)```\n\n```[Documentation](https://borgbackup.readthedocs.io)```"]],"e":["restic","duplicity","rsync"],"x":"borg init --encryption=[repokey] [/path/to/repo]"},"carton":{"n":"carton","t":"Perl module dependency manager","s":[["TAGLINE","Perl module dependency manager"],["TLDR","**Install dependencies from cpanfile**\n\n```carton install```\n\n**Update all dependencies**\n\n```carton update```\n\n**Run a command with local dependencies**\n\n```carton exec [perl script.pl]```\n\n**Run a Plack application with local dependencies**\n\n```carton exec plackup [app.psgi]```\n\n**Show installed modules**\n\n```carton list```\n\n**Show dependency tree**\n\n```carton tree```\n\n**Check if all dependencies are satisfied**\n\n```carton check```\n\n**Install exact versions from cpanfile.snapshot (deployment mode)**\n\n```carton install --deployment```\n\n**Install from cached archives (offline)**\n\n```carton install --cached```\n\n**Bundle dependencies into vendor/cache directory**\n\n```carton bundle```"],["SYNOPSIS","**carton** _command_ [_options_]"],["PARAMETERS","**install**\n> Install dependencies specified in cpanfile.\n\n**update** [_modules_]\n> Update dependencies to latest versions.\n\n**exec** _command_\n> Execute command with local lib paths.\n\n**list**\n> List installed modules.\n\n**bundle**\n> Bundle dependencies into vendor/cache.\n\n**check**\n> Verify dependencies are satisfied.\n\n**tree**\n> Display dependency tree.\n\n**--deployment**\n> Install exact versions from cpanfile.snapshot.\n\n**--cached**\n> Install from cached archives (offline).\n\n**--path** _directory_\n> Specify local lib directory (default: local/)."],["DESCRIPTION","**Carton** is a Perl module dependency manager, similar to Bundler for Ruby or npm for Node.js. It manages project-specific dependencies defined in a **cpanfile**, installing them into a local directory isolated from the system Perl.\n\nDependencies are specified in a cpanfile using a simple DSL. Running **carton install** creates a cpanfile.snapshot that locks exact versions, ensuring reproducible installations across environments.\n\nThe **carton exec** command runs programs with the correct library paths set, avoiding conflicts with system Perl modules. This enables multiple projects with different dependency versions to coexist on the same system."],["CONFIGURATION","**cpanfile**\n> Declares project dependencies using a Perl DSL syntax.\n\n**cpanfile.snapshot**\n> Lockfile with exact resolved dependency versions for reproducible installations."],["CAVEATS","Carton requires a cpanfile; it cannot automatically detect dependencies. Large dependency trees may take significant time to resolve and install. XS modules require compilation tools. The snapshot format is not compatible between major Carton versions."],["HISTORY","Carton was created by **Tatsuhiko Miyagawa** (also creator of Plack/PSGI and cpanm) and released in **2011**. It was inspired by Bundler for Ruby and addressed the long-standing problem of managing Perl dependencies per-project rather than system-wide. The tool became an essential part of modern Perl development workflows."],["INSTALL","```apk: sudo apk add perl-carton```\n\n```brew: brew install carton```"],["SEE ALSO","[cpanm](/man/cpanm)(1), [cpan](/man/cpan)(1)"]],"e":["cpanm","cpan"],"x":"carton install"},"clear":{"n":"clear","t":"clear the terminal screen","s":[["TAGLINE","clear the terminal screen"],["TLDR","**Clear the terminal screen**\n\n```clear```\n\n**Clear and reset scrollback buffer**\n\n```clear -x```\n\n**Clear for a specific terminal type**\n\n```clear -T [xterm-256color]```\n\n**Alternative using ANSI escape codes**\n\n```printf '\\033[2J\\033[H'```"],["SYNOPSIS","**clear** [_-Ttype_] [_-V_] [_-x_]"],["PARAMETERS","**-T** _type_\n> Specify the terminal type to use instead of $TERM\n\n**-V**\n> Print version information and exit\n\n**-x**\n> Don't attempt to clear the terminal's scrollback buffer"],["DESCRIPTION","**clear** clears the terminal screen if this is possible, including its scrollback buffer (if the extended E3 capability is defined). It looks in the environment for the terminal type given by the environment variable TERM and then queries the terminfo database to determine how to clear the screen.\n\nThe command outputs escape sequences that instruct the terminal emulator to clear the visible screen area and, by default, the scrollback buffer as well. This is equivalent to pressing Ctrl+L in many shells, though Ctrl+L typically only clears the visible portion without affecting scrollback.\n\nThe clear command is useful for privacy (removing sensitive output from view), reducing visual clutter during long terminal sessions, and resetting the terminal to a known state."],["CAVEATS","Not all terminals support clearing the scrollback buffer; in such cases only the visible screen is cleared. The behavior depends on the terminal emulator's implementation of the clear sequence. In some configurations, Ctrl+L provides similar functionality but may behave differently regarding scrollback. The cleared content is not securely erased from memory; for sensitive data, consider closing the terminal entirely."],["HISTORY","The **clear** command has been part of Unix systems since the early days, originating from the **termcap** library system for terminal-independent screen handling. The modern implementation is part of the **ncurses** package, which provides terminal handling capabilities for Unix-like systems. The command has remained essentially unchanged in functionality over the decades, though the underlying terminal handling mechanisms have evolved from termcap to terminfo."],["INSTALL","```apt: sudo apt install ncurses-bin```\n\n```dnf: sudo dnf install ncurses```\n\n```pacman: sudo pacman -S ncurses```\n\n```apk: sudo apk add ncurses```\n\n```brew: brew install ncurses```\n\n```nix: nix profile install nixpkgs#ncurses```"],["SEE ALSO","[reset](/man/reset)(1), [tput](/man/tput)(1), [stty](/man/stty)(1), [tty](/man/tty)(1)"],["RESOURCES","```[Homepage](https://invisible-island.net/ncurses/)```\n\n```[Documentation](https://invisible-island.net/ncurses/man/clear.1.html)```"]],"e":["reset","tput","stty","tty"],"x":"clear"},"clj":{"n":"clj","t":"Clojure CLI with readline support","s":[["TAGLINE","Clojure CLI with readline support"],["TLDR","**Start REPL**\n\n```clj```\n\n**Run script file**\n\n```clj -M [script.clj]```\n\n**Execute function**\n\n```clj -X [namespace/function]```\n\n**Add dependency alias**\n\n```clj -A:[alias]```\n\n**List dependencies**\n\n```clj -X:deps list```\n\n**Show dependency tree**\n\n```clj -X:deps tree```\n\n**Run with extra JVM options**\n\n```clj -J-Xmx[2g] -M [script.clj]```"],["SYNOPSIS","**clj** [_clj-opts_] [_-M_|_-X_|_-T_] [_aliases_] [_args_]"],["DESCRIPTION","**clj** is a wrapper around the **clojure** command that adds readline support via **rlwrap**, providing line editing, history, and completion in the REPL. It is the recommended way to start interactive Clojure development sessions.\n\nThe tool manages dependencies through **deps.edn** configuration files and supports multiple execution modes for running programs, executing functions, and invoking tools. Aliases defined in deps.edn allow switching between different dependency configurations for development, testing, and production.\n\nFor non-interactive use cases such as shell scripts and CI pipelines, use the **clojure** command directly instead, as rlwrap can interfere with piped input and output."],["EXECUTION MODES","**-M**\n> Use clojure.main, call -main function\n\n**-X**\n> Execute function with keyword args map\n\n**-T**\n> Like -X but classpath set to . only"],["PARAMETERS","**-A**:_aliases_\n> Use aliases for deps/paths\n\n**-J** _opt_\n> Pass option to JVM\n\n**-Sdeps** _edn_\n> Override deps.edn\n\n**-Spath**\n> Print classpath\n\n**-Scp** _cp_\n> Use given classpath\n\n**-Sforce**\n> Force classpath recomputation\n\n**-Srepro**\n> Ignore user deps.edn\n\n**-Stree**\n> Print dependency tree\n\n**-Sdescribe**\n> Print environment info as EDN\n\n**-P**\n> Prepare deps only, don't exec\n\n**-h**, **--help**\n> Show help"],["BUILT-IN TOOLS","```\nclj -X:deps list       # List all deps\nclj -X:deps tree       # Dependency tree\nclj -X:deps mvn-pom    # Generate pom.xml\n```"],["CONFIGURATION","**deps.edn**\n> Project-level dependency declarations, aliases, and path configurations.\n\n**~/.clojure/deps.edn**\n> User-level default dependencies and aliases applied to all projects."],["CAVEATS","Requires deps.edn for project configuration. Use `clojure` instead of `clj` in scripts (no rlwrap). JVM options via -J or JAVA_OPTS env var."],["INSTALL","```dnf: sudo dnf install clojure```\n\n```pacman: sudo pacman -S clojure```\n\n```apk: sudo apk add clojure```\n\n```zypper: sudo zypper install clojure```\n\n```brew: brew install clojure```\n\n```nix: nix profile install nixpkgs#clojure```"],["SEE ALSO","[lein](/man/lein)(1), [java](/man/java)(1), [clojure](/man/clojure)(1)"],["RESOURCES","```[Source code](https://github.com/clojure/brew-install)```\n\n```[Documentation](https://clojure.org/reference/deps_and_cli)```"]],"e":["lein","java","clojure"],"x":"clj"},"cpio":{"n":"cpio","t":"Archive utility with stdin file lists","s":[["TAGLINE","Archive utility with stdin file lists"],["TLDR","**Create** archive\n\n```find . -depth | cpio -o > [archive.cpio]```\n\n**Extract** archive\n\n```cpio -i < [archive.cpio]```\n\n**List** archive contents\n\n```cpio -t < [archive.cpio]```\n\n**Copy** files\n\n```find . | cpio -pd [/destination]```"],["SYNOPSIS","**cpio** [_options_]"],["DESCRIPTION","**cpio** (copy in/out) is an archiving utility that reads file lists from stdin and creates or extracts archives. It's commonly used in initramfs images, RPM packages, and for copying directory trees.\n\nThe tool operates in three modes: copy-out (create archives), copy-in (extract archives), and pass-through (copy files without archiving). Unlike tar, which accepts file arguments directly, cpio reads filenames from standard input, typically piped from find. This design provides greater flexibility for file selection using find's powerful filtering capabilities.\n\ncpio is the preferred format for Linux initial ramdisk images because of its simplicity and kernel support. RPM packages use cpio internally for storing files. The format supports various archive types and can preserve file metadata including permissions, ownership, and timestamps."],["PARAMETERS","**-o**, **--create**\n> Create archive (copy-out mode)\n\n**-i**, **--extract**\n> Extract archive (copy-in mode)\n\n**-p**, **--pass-through**\n> Copy files (pass-through mode)\n\n**-t**, **--list**\n> List archive contents\n\n**-v**, **--verbose**\n> Verbose mode\n\n**-d**, **--make-directories**\n> Create directories as needed\n\n**-u**, **--unconditional**\n> Overwrite files unconditionally\n\n**-m**, **--preserve-modification-time**\n> Preserve modification times"],["MODES","**Copy-out (-o):**\n> Create archive from file list\n\n**Copy-in (-i):**\n> Extract from archive\n\n**Pass-through (-p):**\n> Copy files without archiving"],["WORKFLOW","```bash"],["Create archive","find . -name \"*.txt\" | cpio -o > textfiles.cpio"],["Create with verbose output","find . | cpio -ov > backup.cpio"],["Extract archive","cpio -i < archive.cpio"],["Extract with verbose","cpio -idv < archive.cpio"],["List contents","cpio -t < archive.cpio"],["Copy directory tree (like cp -r but preserves more)","find /source | cpio -pdm /destination"],["Extract specific files","cpio -i \"*.conf\" < archive.cpio\n```"],["COMPRESSION","```bash"],["Create compressed archive","find . | cpio -o | gzip > archive.cpio.gz"],["Extract compressed archive","gunzip < archive.cpio.gz | cpio -i"],["With bzip2","find . | cpio -o | bzip2 > archive.cpio.bz2\nzcat archive.cpio.bz2 | cpio -i\n```"],["COMMON USES","**Initramfs extraction:**\n```bash\nmkdir initramfs\ncd initramfs\ngunzip -c /boot/initramfs.img | cpio -i\n```\n\n**RPM content extraction:**\n```bash\nrpm2cpio package.rpm | cpio -idmv\n```"],["CAVEATS","Less intuitive than tar. Requires file list from stdin. No built-in compression. Pathname length limits. Different behavior between GNU and POSIX versions. Not as widely used as tar."],["HISTORY","**cpio** was included in Unix PWB/UNIX in **1977** and became part of POSIX, predating tar and offering more flexible file selection."],["INSTALL","```apt: sudo apt install cpio```\n\n```dnf: sudo dnf install cpio```\n\n```pacman: sudo pacman -S cpio```\n\n```apk: sudo apk add cpio```\n\n```zypper: sudo zypper install cpio```\n\n```brew: brew install cpio```\n\n```nix: nix profile install nixpkgs#cpio```"],["SEE ALSO","[tar](/man/tar)(1), [find](/man/find)(1), [pax](/man/pax)(1)"],["RESOURCES","```[Source code](https://git.savannah.gnu.org/cgit/cpio.git)```\n\n```[Homepage](https://www.gnu.org/software/cpio/)```\n\n```[Documentation](https://www.gnu.org/software/cpio/manual/)```"]],"e":["tar","find","pax"],"x":"find . -depth | cpio -o > [archive.cpio]"},"debhelper":{"n":"debhelper","t":"collection of programs automating Debian package building","s":[["TAGLINE","collection of programs automating Debian package building"],["TLDR","**Clean build artifacts**\n\n```dh_clean```\n\n**Install files to package directory**\n\n```dh_install```\n\n**Generate package dependencies**\n\n```dh_shlibdeps```\n\n**Compress documentation**\n\n```dh_compress```\n\n**Generate md5sums**\n\n```dh_md5sums```\n\n**Build package using dh sequencer**\n\n```dh binary```\n\n**Execute full build sequence**\n\n```debian/rules binary```"],["SYNOPSIS","**dh_**_command_ [_options_] [**--** _args_]\n\n**dh** _target_ [_options_]"],["COMMON COMMANDS","**dh_auto_configure**\n> Run upstream configure script.\n\n**dh_auto_build**\n> Build upstream source.\n\n**dh_auto_install**\n> Install into debian/tmp.\n\n**dh_auto_test**\n> Run upstream test suite.\n\n**dh_auto_clean**\n> Clean upstream build.\n\n**dh_install**\n> Install files into package directories.\n\n**dh_installdocs**\n> Install documentation.\n\n**dh_installman**\n> Install man pages.\n\n**dh_compress**\n> Compress files in packages.\n\n**dh_fixperms**\n> Fix file permissions.\n\n**dh_shlibdeps**\n> Calculate shared library dependencies.\n\n**dh_gencontrol**\n> Generate control file.\n\n**dh_builddeb**\n> Build the .deb file."],["DESCRIPTION","**debhelper** is a collection of programs (dh_*) that automate common tasks when building Debian packages. Instead of writing shell commands in debian/rules, maintainers call debhelper commands that handle standard operations correctly.\n\nThe **dh** command is a sequencer that runs appropriate debhelper commands in the correct order. A minimal debian/rules can be just three lines using dh, with overrides for customization.\n\nDebhelper reads configuration from debian/ directory files: install, docs, manpages, etc. This declarative approach simplifies packaging while maintaining flexibility for complex packages."],["MINIMAL RULES FILE","```makefile\n#!/usr/bin/make -f\n%:\n\tdh $@\n```"],["CAVEATS","Debhelper is Debian-specific; other distributions have different packaging systems. Compatibility levels (debhelper-compat) affect behavior; always specify the level. Some dh commands have many options; consult man pages for details. Override targets in debian/rules customize dh behavior."],["HISTORY","Debhelper was created by **Joey Hess** in **1997** to simplify Debian packaging. Before debhelper, each package reimplemented common tasks in shell scripts, leading to inconsistency and bugs. The dh sequencer was added in debhelper 7 (2008), dramatically simplifying debian/rules files. Debhelper has become the standard way to build Debian packages."],["INSTALL","```dnf: sudo dnf install debhelper```\n\n```zypper: sudo zypper install debhelper```"],["SEE ALSO","[dh](/man/dh)(1), [dpkg-buildpackage](/man/dpkg-buildpackage)(1), [debuild](/man/debuild)(1)"]],"e":["dh","dpkg-buildpackage","debuild"],"x":"dh_clean"},"elasticsearch-users":{"n":"elasticsearch-users","t":"Manage file-based Elasticsearch users","s":[["TAGLINE","Manage file-based Elasticsearch users"],["TLDR","**List users**\n\n```elasticsearch-users list```\n\n**Add a user**\n\n```elasticsearch-users useradd [username] -p [password] -r [roles]```\n\n**Delete a user**\n\n```elasticsearch-users userdel [username]```\n\n**Change password**\n\n```elasticsearch-users passwd [username]```\n\n**Add role to user**\n\n```elasticsearch-users roles [username] -a [role]```\n\n**Remove role from user**\n\n```elasticsearch-users roles [username] -r [role]```"],["SYNOPSIS","**elasticsearch-users** _command_ [_options_]"],["SUBCOMMANDS","**useradd**\n> Add a user.\n\n**userdel**\n> Delete a user.\n\n**passwd**\n> Change password.\n\n**roles**\n> Manage user roles.\n\n**list**\n> List users."],["PARAMETERS","**-p** _pass_\n> User password (must be at least 6 characters). If omitted, prompts interactively.\n\n**-r** _roles_\n> Comma-separated list of roles. With **useradd**, assigns roles. With **roles**, removes roles.\n\n**-a** _roles_\n> Add comma-separated roles (used with **roles** subcommand)."],["DESCRIPTION","**elasticsearch-users** manages file-based users for Elasticsearch's native realm. These users are stored locally in the node's configuration directory and don't require an external authentication system. The tool is part of the Elasticsearch security features.\n\nWhen listing users, only those registered on the local node are shown. In a multi-node cluster, file-based users must be managed on each node separately."],["CAVEATS","Users are stored in **config/users** and roles in **config/users_roles**. Changes apply only to the local node; synchronize these files across cluster nodes manually. Requires the X-Pack security features to be enabled."],["HISTORY","**elasticsearch-users** ships with Elasticsearch's security features (originally part of the commercial **X-Pack** plugin, made free under the Basic license in Elasticsearch **6.8/7.1**). It provides lightweight authentication without requiring LDAP, Active Directory, or other external realms."],["INSTALL","```nix: nix profile install nixpkgs#elasticsearch```"],["SEE ALSO","[elasticsearch](/man/elasticsearch)(1), [elasticsearch-reset-password](/man/elasticsearch-reset-password)(1)"]],"e":["elasticsearch","elasticsearch-reset-password"],"x":"elasticsearch-users list"},"filebrowser":{"n":"filebrowser","t":"web-based file management interface","s":[["TAGLINE","web-based file management interface"],["TLDR","**Start web file browser**\n\n```filebrowser```\n\n**Serve specific directory**\n\n```filebrowser -r [/path/to/directory]```\n\n**Specify address and port**\n\n```filebrowser -a [0.0.0.0] -p [8080]```\n\n**Use config file**\n\n```filebrowser -c [config.json]```\n\n**Create admin user**\n\n```filebrowser users add [admin] [password] --perm.admin```\n\n**Configure database**\n\n```filebrowser -d [database.db]```"],["SYNOPSIS","**filebrowser** [_options_]"],["PARAMETERS","**-r**, **--root** _path_\n> Root directory to serve.\n\n**-a**, **--address** _addr_\n> Listen address.\n\n**-p**, **--port** _port_\n> Listen port.\n\n**-d**, **--database** _file_\n> Database file path.\n\n**-c**, **--config** _file_\n> Config file path.\n\n**-b**, **--baseURL** _path_\n> Base URL for the application.\n\n**-t**, **--cert** _file_\n> TLS certificate.\n\n**-k**, **--key** _file_\n> TLS key.\n\n**--noauth**\n> Disable authentication.\n\n**--log** _output_\n> Log output (default \"stdout\")."],["CONFIGURATION","**config.json**\n> Configuration file for server settings, authentication, and permissions.\n\n**database.db**\n> SQLite database storing users, permissions, and settings."],["DESCRIPTION","**filebrowser** provides a self-hosted web-based file management interface accessible through any browser. It creates a complete file server with upload, download, preview, search, and sharing capabilities, along with a built-in code editor for text files.\n\nThe application supports user authentication with granular permissions, allowing different access levels for different directories. Multiple users can be configured with individual permissions controlling read, write, execute, and sharing capabilities.\n\nfilebrowser is ideal for personal cloud storage, team file sharing, or providing controlled access to server directories without SSH. It includes features like drag-and-drop uploads, multi-file operations, and custom command execution hooks."],["INSTALL","```brew: brew install filebrowser```\n\n```nix: nix profile install nixpkgs#filebrowser```"],["SEE ALSO","[nginx](/man/nginx)(8)"]],"e":["nginx"],"x":"filebrowser"},"gcloud-kms-decrypt":{"n":"gcloud-kms-decrypt","t":"decrypt data using Cloud KMS keys","s":[["TAGLINE","decrypt data using Cloud KMS keys"],["TLDR","**Decrypt file**\n\n```gcloud kms decrypt --ciphertext-file=[encrypted.enc] --plaintext-file=[decrypted.txt] --key=[key] --keyring=[keyring] --location=[global]```\n\n**Decrypt from stdin**\n\n```cat [encrypted.enc] | gcloud kms decrypt --ciphertext-file=- --plaintext-file=[output.txt] --key=[key] --keyring=[keyring] --location=[global]```\n\n**Decrypt to stdout**\n\n```gcloud kms decrypt --ciphertext-file=[encrypted.enc] --plaintext-file=- --key=[key] --keyring=[keyring] --location=[global]```"],["SYNOPSIS","**gcloud kms decrypt** [_options_]"],["PARAMETERS","**--ciphertext-file** _FILE_\n> Encrypted input file.\n\n**--plaintext-file** _FILE_\n> Decrypted output file.\n\n**--key** _KEY_\n> Crypto key name.\n\n**--keyring** _KEYRING_\n> Key ring name.\n\n**--location** _LOCATION_\n> Key location.\n\n**--help**\n> Display help information."],["DESCRIPTION","**gcloud kms decrypt** uses Cloud Key Management Service to decrypt data that was previously encrypted with a Cloud KMS key. KMS provides centralized cryptographic key management, separating key storage and access control from the applications that use them.\n\nThe decryption operation requires specifying the exact key, keyring, and location used during encryption. Access to decrypt is controlled by IAM permissions on the key, allowing fine-grained control over who can decrypt sensitive data. This enables secure secrets management where encrypted data can be stored in version control or configuration files while keys remain secured in KMS.\n\nThe command supports reading from files or stdin and writing to files or stdout, enabling integration into pipelines and scripts. Cloud KMS is commonly used for envelope encryption, where data encryption keys are themselves encrypted by KMS keys, providing an additional security layer. This is the standard pattern for encrypting application secrets, database credentials, and other sensitive configuration data in Google Cloud environments."],["CAVEATS","Requires KMS permissions. Key must match encryption key. Location must be correct."],["HISTORY","gcloud kms decrypt is part of the **Google Cloud SDK** for Cloud KMS, Google's managed service for cryptographic key management."],["SEE ALSO","[gcloud](/man/gcloud)(1)"]],"e":["gcloud"],"x":"gcloud kms decrypt --ciphertext-file=[encrypted.enc] --plaintext-file=[decrypted.txt] --key=[key] --keyring=[keyring] --location=[global]"},"gcrane-help":{"n":"gcrane-help","t":"Display help information for gcrane commands","s":[["TAGLINE","Display help information for gcrane commands"],["TLDR","**Show general help** for gcrane\n\n```gcrane help```\n\n**Show help for a specific** subcommand\n\n```gcrane help [copy]```\n\n**Show help for the gc** subcommand\n\n```gcrane help [gc]```\n\n**Show help using the --help** flag\n\n```gcrane --help```"],["SYNOPSIS","**gcrane** **help** [_command_]"],["DESCRIPTION","**gcrane help** displays help information for gcrane and its subcommands. Without arguments, it shows an overview of all available commands with brief descriptions. When given a specific command name, it displays detailed usage information including all flags and parameters.\n\nThe help system documents gcrane's GCR-specific extensions beyond standard crane functionality, including recursive copy operations and garbage collection commands."],["INSTALL","```zypper: sudo zypper install gcrane```\n\n```nix: nix profile install nixpkgs#gcrane```"],["SEE ALSO","[gcrane](/man/gcrane)(1), [gcrane-copy](/man/gcrane-copy)(1), [gcrane-gc](/man/gcrane-gc)(1), [crane](/man/crane)(1)"]],"e":["gcrane","gcrane-copy","gcrane-gc","crane"],"x":"gcrane help"},"gdebi":{"n":"gdebi","t":"local .deb package installer with dependency resolution","s":[["TAGLINE","local .deb package installer with dependency resolution"],["TLDR","**Install** local .deb packages resolving dependencies\n\n```gdebi [path/to/package.deb]```\n\nDo not show **progress** information\n\n```gdebi --quiet [path/to/package.deb]```\n\nSet an **APT configuration option**\n\n```gdebi --option [APT_OPTS] [path/to/package.deb]```\n\nUse **alternative root** directory\n\n```gdebi --root [path/to/root_directory] [path/to/package.deb]```\n\nDisplay **version**\n\n```gdebi --version```"],["SYNOPSIS","**gdebi** [_options_] _package.deb_"],["PARAMETERS","**-q**, **--quiet**\n> Suppress progress information\n\n**-o**, **--option** _APT_OPTS_\n> Set an APT configuration option\n\n**--root** _DIR_\n> Use alternative root directory\n\n**-n**, **--non-interactive**\n> Run non-interactively (assume yes to all prompts)\n\n**--apt-line**\n> Simulate only; print an apt-get install compatible line to stderr\n\n**--version**\n> Display version information"],["DESCRIPTION","**gdebi** is a tool for installing local .deb packages on Debian-based systems. Unlike **dpkg -i**, gdebi automatically resolves and installs package dependencies from configured APT repositories before installing the local package.\n\nThis makes it ideal for installing downloaded .deb files that have dependencies not already satisfied on the system. gdebi will fetch required packages from the repository and install them in the correct order.\n\nA graphical version (**gdebi-gtk**) is also available for desktop use."],["CAVEATS","Requires configured APT repositories to resolve dependencies. Cannot resolve dependencies that aren't available in configured repositories. For packages with complex dependencies, prefer adding a proper repository rather than installing standalone .deb files."],["HISTORY","gdebi was developed for Ubuntu and Debian to solve the common problem of installing downloaded .deb packages that dpkg alone cannot handle due to unmet dependencies. It became a standard tool in Ubuntu's desktop installations."],["SEE ALSO","[dpkg](/man/dpkg)(1), [apt](/man/apt)(8), [apt-get](/man/apt-get)(8)"]],"e":["dpkg","apt","apt-get"],"x":"gdebi [path/to/package.deb]"},"giskismet":{"n":"giskismet","t":"wireless recon visualization tool for Kismet data","s":[["TAGLINE","wireless recon visualization tool for Kismet data"],["TLDR","**Import Kismet NETXML data**\n\n```giskismet -x [kismet-output.netxml]```\n\n**Query for specific access points by ESSID**\n\n```giskismet -q \"select * from wireless where ESSID='[MyNetwork]'\"```\n\n**Export results to KML file**\n\n```giskismet -x [data.netxml] -o [output.kml]```\n\n**Filter by encryption type**\n\n```giskismet -x [data.netxml] --encryption [WPA]```\n\n**Filter by BSSID**\n\n```giskismet -x [data.netxml] --bssid [AA:BB:CC:DD:EE:FF]```"],["SYNOPSIS","**giskismet** [_options_]"],["PARAMETERS","**-h**, **--help**\n> Display help.\n\n**-v**, **--version**\n> Display version.\n\n**-x** _FILE_, **--xml** _FILE_\n> Parse Kismet NETXML file.\n\n**--csv** _FILE_\n> Parse Kismet CSV file.\n\n**--bssid** _FILTER_\n> Filter by BSSID (file or comma-separated list).\n\n**--essid** _FILTER_\n> Filter by ESSID (file or comma-separated list).\n\n**--encryption** _FILTER_\n> Filter by encryption type (file or comma-separated list).\n\n**--channel** _FILTER_\n> Filter by channel (file or comma-separated list).\n\n**-a**, **--ap**\n> Insert only access points.\n\n**-q** _SQL_, **--query** _SQL_\n> Execute SQL query against the database.\n\n**-o** _FILE_, **--output** _FILE_\n> Specify output filename for KML export.\n\n**-n** _NAME_, **--name** _NAME_\n> Set KML layer name.\n\n**-s**, **--silent**\n> Suppress output when adding access points.\n\n**-d** _LEVEL_, **--debug** _LEVEL_\n> Debug information level."],["DESCRIPTION","**giskismet** is a Perl-based tool that processes wireless recon data gathered by **Kismet** and stores it in a SQLite database. Users can run SQL queries against the data and generate **Google Earth KML** files for mapping access point locations.\n\nThe tool supports filtering by BSSID, ESSID, encryption type, and channel, making it useful for wireless security assessments and site surveys."],["CAVEATS","Requires Kismet NETXML output files as input. GPS data must be present in the Kismet capture for meaningful KML output. The tool is deprecated in favor of newer Kismet built-in visualization features."],["SEE ALSO","[kismet](/man/kismet)(1), [aircrack-ng](/man/aircrack-ng)(1)"]],"e":["kismet","aircrack-ng"],"x":"giskismet -x [kismet-output.netxml]"},"git-am":{"n":"git-am","t":"Apply patches from email messages in mbox format","s":[["TAGLINE","Apply patches from email messages in mbox format"],["TLDR","**Apply patches from mailbox**\n\n```git am [patches.mbox]```\n\n**Apply patch files**\n\n```git am [*.patch]```\n\n**Apply with 3-way merge**\n\n```git am --3way [patch]```\n\n**Apply and add Signed-off-by line**\n\n```git am --signoff [patch]```\n\n**Continue after resolving conflicts**\n\n```git am --continue```\n\n**Skip current patch**\n\n```git am --skip```\n\n**Show the patch where am stopped**\n\n```git am --show-current-patch```\n\n**Abort patch application**\n\n```git am --abort```"],["SYNOPSIS","**git am** [_options_] [_mbox_|_maildir_]"],["PARAMETERS","_MBOX_\n> Mailbox file with patches.\n\n**--3way**, **-3**\n> Fall back to 3-way merge if the patch does not apply cleanly.\n\n**--signoff**, **-s**\n> Add a Signed-off-by trailer to the commit message.\n\n**--keep**, **-k**\n> Pass -k to git mailinfo; preserve the subject prefix.\n\n**--quiet**, **-q**\n> Only print error messages.\n\n**--whitespace** _action_\n> Handle whitespace errors: nowarn, warn, fix, error, error-all.\n\n**--reject**\n> Leave rejected hunks in .rej files instead of failing.\n\n**--patch-format** _format_\n> Specify the patch format: mbox, mboxrd, stgit, stgit-series, hg.\n\n**--skip**\n> Skip the current patch.\n\n**--continue**, **--resolved**, **-r**\n> Continue after manually resolving a conflict.\n\n**--abort**\n> Abort and restore the original branch state.\n\n**--quit**\n> Abort but leave HEAD and index unchanged.\n\n**--show-current-patch** [**=diff**|**=raw**]\n> Show the patch where am stopped; default is raw.\n\n**--gpg-sign** [_keyid_], **-S** [_keyid_]\n> GPG-sign the resulting commits.\n\n**-i**, **--interactive**\n> Run interactively.\n\n**--help**\n> Display help information."],["DESCRIPTION","**git am** (apply mailbox) applies patches from email messages. It reads patches in mbox format, commonly used for email-based patch submission workflows in open source projects.\n\nThe command extracts patch content and commit message from email format, creating commits with the original author information preserved. It handles patch series and manages conflicts through --skip, --abort, and --continue operations.\n\nThis workflow remains fundamental to the Linux kernel development process and other projects that rely on email-based code review and patch submission."],["CAVEATS","Patch format must be correct mbox/maildir format. Conflicts require manual resolution followed by `--continue`. Original author information and dates are preserved from the email headers."],["HISTORY","git am was designed for the **Linux kernel** development workflow where patches are submitted and reviewed via email on mailing lists."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-apply](/man/git-apply)(1), [git-format-patch](/man/git-format-patch)(1), [git-send-email](/man/git-send-email)(1)"]],"e":["git-apply","git-format-patch","git-send-email"],"x":"git am [patches.mbox]"},"git-cvsexportcommit":{"n":"git-cvsexportcommit","t":"Export a single Git commit to a CVS checkout","s":[["TAGLINE","Export a single Git commit to a CVS checkout"],["TLDR","**Export a commit and automatically commit to CVS**\n\n```git cvsexportcommit -v -c [commit]```\n\n**Export a commit to a specific CVS working directory**\n\n```git cvsexportcommit -v -w [path/to/cvs_workdir] [commit]```\n\n**Export a commit with author information preserved**\n\n```git cvsexportcommit -v -c -a [commit]```\n\n**Force export even if CVS files are not up to date**\n\n```git cvsexportcommit -f -c [commit]```\n\n**Update CVS files before exporting and committing**\n\n```git cvsexportcommit -u -v -c [commit]```"],["SYNOPSIS","**git** **cvsexportcommit** [-h] [-u] [-v] [-c] [-P] [-p] [-a] [-d _cvsroot_] [-w _cvs-workdir_] [-W] [-f] [-m _msgprefix_] [-k] [_parent-commit_] _commit-id_"],["PARAMETERS","**-c**\n> Commit automatically if the patch applied cleanly. Will not commit if any hunks fail to apply or there were other problems.\n\n**-p**\n> Be pedantic (paranoid) when applying patches. Invokes patch with --fuzz=0.\n\n**-a**\n> Add authorship information. Adds Author line, and Committer (if different from Author) to the commit message.\n\n**-d** _cvsroot_\n> Set an alternative CVSROOT to use. Corresponds to the CVS -d parameter.\n\n**-f**\n> Force the merge even if the files are not up to date.\n\n**-P**\n> Force the parent commit, even if it is not a direct parent.\n\n**-m** _msgprefix_\n> Prepend the commit message with the provided prefix. Useful for patch series.\n\n**-u**\n> Update affected files from CVS repository before attempting export.\n\n**-k**\n> Reverse CVS keyword expansion (e.g., $Revision: 1.2.3.4$ becomes $Revision$) in the working CVS checkout before applying the patch.\n\n**-w** _cvs-workdir_\n> Specify the location of the CVS checkout to use for the export. The default is the value of cvsexportcommit.cvsdir.\n\n**-W**\n> Indicate that the current working directory is both a Git checkout and a CVS checkout. Git will reset the working directory to the parent commit before proceeding.\n\n**-v**\n> Verbose output."],["DESCRIPTION","**git cvsexportcommit** exports a single Git commit to a CVS checkout, enabling migration of changes from Git to legacy CVS repositories. It applies a specific Git commit's changes to a CVS working directory, optionally committing them automatically.\n\nThe command handles file additions, modifications, and deletions, translating Git's commit semantics into equivalent CVS operations. It is particularly useful in hybrid environments where teams are transitioning from CVS to Git but must maintain synchronization with CVS-dependent systems."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git](/man/git)(1), [cvs](/man/cvs)(1), [git-format-patch](/man/git-format-patch)(1)"]],"e":["git","cvs","git-format-patch"],"x":"git cvsexportcommit -v -c [commit]"},"git-paste":{"n":"git-paste","t":"Send commit patches to a pastebin service","s":[["TAGLINE","Send commit patches to a pastebin service"],["TLDR","**Paste commits** not yet in the upstream branch\n\n```git paste```\n\n**Paste a specific revision range**\n\n```git paste [HEAD~3..HEAD]```\n\n**Paste a single commit**\n\n```git paste -1 [commit]```"],["SYNOPSIS","**git** **paste** [_git-format-patch-args_]"],["PARAMETERS","_ARGS_\n> Any arguments accepted by `git format-patch` (revision range, `-1`, `-n`, etc). Defaults to `@{u}` (commits ahead of the upstream branch) when omitted."],["DESCRIPTION","**git paste** runs `git format-patch --stdout` over the given commits and pipes the resulting diff to **pastebinit**, printing the pastebin URL. Part of git-extras, it is a quick way to share a patch or a small commit series without creating a file first.\n\nBecause it delegates to `pastebinit`, the destination pastebin service (and any API key) is whatever `pastebinit` is configured to use, not necessarily GitHub Gist."],["CAVEATS","Requires **pastebinit** to be installed separately; the command aborts with an error if it is missing. Pasted content is sent to a third-party service, so avoid pasting patches containing secrets."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-format-patch](/man/git-format-patch)(1), [git-extras](/man/git-extras)(1)"],["RESOURCES","```[Source code](https://github.com/tj/git-extras)```\n\n```[Documentation](https://github.com/tj/git-extras/blob/master/Commands.md#git-paste)```"]],"e":["git-format-patch","git-extras"],"x":"git paste"},"git-rename-branch":{"n":"git-rename-branch","t":"Rename a Git branch locally and on the remote","s":[["TAGLINE","Rename a Git branch locally and on the remote"],["TLDR","**Rename current branch**\n\n```git rename-branch [new_name]```\n\n**Rename specific branch**\n\n```git rename-branch [old_name] [new_name]```"],["SYNOPSIS","**git** **rename-branch** [_old_name_] _new_name_"],["DESCRIPTION","**git rename-branch** renames a branch both locally and on the remote. Part of git-extras, it handles the local rename, pushes the new branch name to the remote, deletes the old remote branch, and updates the upstream tracking reference in a single command.\n\nWithout this tool, renaming a remote branch requires multiple steps: `git branch -m`, `git push origin new_name`, `git push origin --delete old_name`, and `git branch -u origin/new_name`."],["PARAMETERS","**old_name**\n> The current name of the branch to rename. If omitted, the current branch is used.\n\n**new_name**\n> The new name for the branch."],["CAVEATS","Requires git-extras to be installed. Other collaborators must update their local references after the rename. Pull requests or merge requests targeting the old branch name may need manual updates."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-branch](/man/git-branch)(1), [git-extras](/man/git-extras)(1)"]],"e":["git-branch","git-extras"],"x":"git rename-branch [new_name]"},"glxdemo":{"n":"glxdemo","t":"simple OpenGL rendering test","s":[["TAGLINE","simple OpenGL rendering test"],["TLDR","**Run OpenGL demo**\n\n```glxdemo```"],["SYNOPSIS","**glxdemo**"],["DESCRIPTION","**glxdemo** is a simple OpenGL demonstration program that shows how to use GLX functions to create an OpenGL program running on top of an X server. It opens a window and renders a basic 3D scene to verify that OpenGL rendering is working correctly on the system.\n\nThe program is most useful when studied in source code form. If it runs successfully, OpenGL acceleration is properly configured."],["CAVEATS","Requires X11 and OpenGL. Part of mesa-utils (or mesa-demos). Basic functionality test only; accepts no command-line options."],["HISTORY","glxdemo is part of **Mesa demos**, providing simple programs for testing OpenGL rendering on Linux/Unix systems."],["INSTALL","```apt: sudo apt install mesa-utils```\n\n```dnf: sudo dnf install mesa-demos```\n\n```pacman: sudo pacman -S mesa-utils```\n\n```apk: sudo apk add mesa-demos```\n\n```nix: nix profile install nixpkgs#mesa-demos```"],["SEE ALSO","[glxinfo](/man/glxinfo)(1), [glxgears](/man/glxgears)(1)"]],"e":["glxinfo","glxgears"],"x":"glxdemo"},"groff":{"n":"groff","t":"GNU document formatting system","s":[["TAGLINE","GNU document formatting system"],["TLDR","**Format document to PDF**\n\n```groff -Tpdf [file.ms] > output.pdf```\n\n**Format with man macros**\n\n```groff -man [file.1] > output.ps```\n\n**Format with ms macros to PDF**\n\n```groff -ms -Tpdf [file.ms] > output.pdf```\n\n**Preview in terminal**\n\n```groff -Tutf8 [file.roff] | less```\n\n**Process with table and equation preprocessors**\n\n```groff -t -e -ms [file.ms] > output.ps```\n\n**Format a man page to plain text**\n\n```groff -Tutf8 -man [file.1]```\n\n**Display the pipeline without executing it**\n\n```groff -V -Tpdf [file.ms]```"],["SYNOPSIS","**groff** [_options_] [_files_]"],["PARAMETERS","_FILES_\n> Input files to process.\n\n**-T** _DEVICE_\n> Output device (pdf, ps, html, utf8, ascii, latin1, dvi).\n\n**-m** _MACRO_\n> Use macro package (man, ms, me, mm).\n\n**-t**\n> Run tbl preprocessor for tables.\n\n**-e**\n> Run eqn preprocessor for equations.\n\n**-p**\n> Run pic preprocessor for diagrams.\n\n**-R**\n> Run refer preprocessor for bibliographic references.\n\n**-s**\n> Run soelim preprocessor for file inclusion.\n\n**-G**\n> Run grap preprocessor for graphs.\n\n**-S**\n> Safer mode. Disables .open, .opena, .pso, .sy, and .pi requests.\n\n**-U**\n> Unsafe mode. Reverts to the unsafe requests disabled by -S.\n\n**-V**\n> Print the pipeline that would be run to standard error, without executing it.\n\n**-Z**\n> Disable postprocessing. Output raw troff intermediate output.\n\n**-a**\n> Generate a plain text approximation of the typeset output.\n\n**-b**\n> Print a backtrace on each error or warning.\n\n**-man**\n> Shorthand for -m man. Use man macros.\n\n**-ms**\n> Shorthand for -m ms. Use ms macros.\n\n**--help**\n> Display help information."],["DESCRIPTION","**groff** is the GNU implementation of the roff document formatting system. It is a front-end that orchestrates the execution of preprocessors, the troff formatter, and an output driver in a pipeline. It processes text files with embedded formatting commands to produce typeset output in various formats including PDF, PostScript, HTML, and terminal display.\n\nThe system includes preprocessors for tables (tbl), equations (eqn), and diagrams (pic), and supports multiple macro packages for different document types."],["CONFIGURATION","**/usr/share/groff/current/tmac/**\n> System-wide macro files and configuration for groff output devices."],["CAVEATS","Steep learning curve. Macro packages have different conventions. The -man and -ms shorthands are groff extensions not present in traditional troff. The default output device is ps (PostScript) unless overridden with -T."],["HISTORY","groff was created by **James Clark** as a free implementation of AT&T troff. It is now maintained as part of the **GNU** project."],["INSTALL","```apt: sudo apt install groff-base```\n\n```dnf: sudo dnf install groff-base```\n\n```pacman: sudo pacman -S groff```\n\n```apk: sudo apk add groff```\n\n```zypper: sudo zypper install groff```\n\n```brew: brew install groff```\n\n```nix: nix profile install nixpkgs#groff```"],["SEE ALSO","[troff](/man/troff)(1), [nroff](/man/nroff)(1), [man](/man/man)(1), [tbl](/man/tbl)(1), [eqn](/man/eqn)(1), [pic](/man/pic)(1), [refer](/man/refer)(1), [soelim](/man/soelim)(1)"]],"e":["troff","nroff","man","tbl","eqn","pic","refer","soelim"],"x":"groff -Tpdf [file.ms] > output.pdf"},"hello":{"n":"hello","t":"friendly greeting program","s":[["TAGLINE","friendly greeting program"],["TLDR","**Print hello world**\n\n```hello```\n\n**Traditional greeting**\n\n```hello -t```\n\n**Specify greeting**\n\n```hello -g \"Custom greeting\"```\n\n**Show version**\n\n```hello -v```"],["SYNOPSIS","**hello** [_options_]"],["PARAMETERS","**-g**, **--greeting** _TEXT_\n> Custom greeting text.\n\n**-t**, **--traditional**\n> Use the traditional \"hello, world\" greeting.\n\n**-h**, **--help**\n> Display help information and exit.\n\n**-v**, **--version**\n> Show version and licensing information and exit."],["DESCRIPTION","**GNU Hello** is a friendly greeting program. It serves as an example of GNU coding standards and a template for new GNU packages.\n\nThe program demonstrates proper use of gettext, autoconf, and other GNU infrastructure. It's useful for testing and learning."],["CAVEATS","Example program. Not for production use. Demonstrates GNU standards."],["HISTORY","GNU Hello was created as part of the **GNU Project** to serve as an example program demonstrating GNU packaging standards."],["INSTALL","```apt: sudo apt install hello```\n\n```dnf: sudo dnf install hello```\n\n```apk: sudo apk add mbedtls2-utils```\n\n```zypper: sudo zypper install hello```\n\n```brew: brew install hello```\n\n```nix: nix profile install nixpkgs#hello```"],["SEE ALSO","[echo](/man/echo)(1), [printf](/man/printf)(1), [cowsay](/man/cowsay)(6)"],["RESOURCES","```[Source code](https://git.savannah.gnu.org/cgit/hello.git)```\n\n```[Homepage](https://www.gnu.org/software/hello/)```\n\n```[Documentation](https://www.gnu.org/software/hello/manual/)```"]],"e":["echo","printf","cowsay"],"x":"hello"},"hexabyte":{"n":"hexabyte","t":"Modern modular TUI hex editor","s":[["TAGLINE","Modern modular TUI hex editor"],["TLDR","**Open a file** in the hex editor\n\n```hexabyte [path/to/file]```\n\n**Open a single file** in split screen view\n\n```hexabyte -s [path/to/file]```\n\n**Open two files** side-by-side for diff\n\n```hexabyte [path/to/file1] [path/to/file2]```\n\n**Use a custom config file**\n\n```hexabyte -c [path/to/config.toml] [path/to/file]```"],["SYNOPSIS","**hexabyte** [_options_] _file_ [_file2_]"],["PARAMETERS","**-s**, **--split**\n> Display a single file in two split-screen editors.\n\n**-c**, **--config** _FILE_\n> Use a custom config file (default: `~/.config/hexabyte/config.toml`).\n\n**-h**, **--help**\n> Show help message and exit."],["DESCRIPTION","**hexabyte** is a modern, modular, and robust TUI hex editor designed for modifying file contents at the byte level. It supports viewing and editing in hexadecimal, UTF-8 text, and binary with multiple display modes: **single** file editing, **split** view for side-by-side panes on one file, and **diff** mode when two files are opened together.\n\nThe editor features a plugin architecture for extensibility and provides standard hex editing operations including inserting, deleting, and replacing bytes, as well as selecting and copy/pasting byte ranges."],["CAVEATS","Requires Python 3.10+. Very large files may impact performance. The diff mode highlights byte-level differences, which may produce noisy output for structurally different files."],["HISTORY","**hexabyte** was created by **thetacom** and is written in **Python**. It was designed to provide a modern, extensible alternative to traditional terminal hex editors."],["INSTALL","```aur: yay -S hexabyte```"],["SEE ALSO","[hexyl](/man/hexyl)(1), [xxd](/man/xxd)(1), [heh](/man/heh)(1)"]],"e":["hexyl","xxd","heh"],"x":"hexabyte [path/to/file]"},"in-toto-sign":{"n":"in-toto-sign","t":"adds cryptographic signatures to in-toto metadata files","s":[["TAGLINE","adds cryptographic signatures to in-toto metadata files"],["TLDR","**Sign a link file**\n\n```in-toto-sign -k [key.pem] -f [link_file]```\n\n**Sign layout file**\n\n```in-toto-sign -k [key] -f [root.layout]```\n\n**Append** signature to existing signatures\n\n```in-toto-sign -k [key] -f [file] -a```\n\n**Output** signed metadata to a specific file\n\n```in-toto-sign -k [key] -f [input] -o [output]```\n\n**Verify** signatures on a metadata file\n\n```in-toto-sign --verify -k [key.pub] -f [file]```\n\n**Sign** with a GPG key\n\n```in-toto-sign -g [gpg_keyid] -f [file]```"],["SYNOPSIS","**in-toto-sign** [_options_]"],["PARAMETERS","**-k** _KEY_\n> Private signing key file.\n\n**-f** _FILE_\n> File to sign (link or layout).\n\n**-a**, **--append**\n> Append signature instead of replacing.\n\n**-o** _OUTPUT_\n> Output file path.\n\n**-g** [_KEYID ..._]\n> GPG keyids used to sign or verify\n\n**--gpg-home** _PATH_\n> GPG home directory path\n\n**--verify**\n> Verify signatures instead of signing\n\n**-v**\n> Verbose output\n\n**-q**\n> Quiet output\n\n**--version**\n> Show version information\n\n**--help**\n> Display help information"],["DESCRIPTION","**in-toto-sign** adds cryptographic signatures to in-toto metadata files. It signs both link files from build steps and layout files that define supply chain policies.\n\nMultiple signatures can be added for multi-party verification. The tool supports GPG and PEM key formats."],["CAVEATS","Part of in-toto framework. Requires compatible key format. Signatures must match verification keys."],["HISTORY","in-toto-sign is part of **in-toto**, developed at **NYU** Secure Systems Lab for supply chain integrity verification."],["SEE ALSO","[in-toto-run](/man/in-toto-run)(1), [in-toto-record](/man/in-toto-record)(1)"]],"e":["in-toto-run","in-toto-record"],"x":"in-toto-sign -k [key.pem] -f [link_file]"},"jj-absorb":{"n":"jj-absorb","t":"Move changes into the appropriate ancestor commits automatically","s":[["TAGLINE","Move changes into the appropriate ancestor commits automatically"],["TLDR","**Absorb working copy changes into ancestor commits**\n\n```jj absorb```\n\n**Absorb only specific file paths**\n\n```jj absorb [file1] [file2]```\n\n**Absorb from a specific source revision**\n\n```jj absorb --from [revision]```\n\n**Absorb into a specific set of destination revisions**\n\n```jj absorb --into [revset]```"],["SYNOPSIS","**jj** **absorb** [_options_] [_filesets_...]"],["PARAMETERS","**-f**, **--from** _REVSET_\n> Source revision to absorb from (default: @).\n\n**-t**, **--into** _REVSETS_\n> Destination revisions to absorb into (default: mutable()). Only ancestors of the source are considered.\n\n**-R**, **--repository** _PATH_\n> Path to repository to operate on.\n\n**--ignore-working-copy**\n> Don't snapshot or update the working copy.\n\n**--ignore-immutable**\n> Allow rewriting immutable commits."],["DESCRIPTION","**jj absorb** splits changes in the source revision and moves each change to the closest mutable ancestor where the corresponding lines were last modified. This is useful for fixing up a series of commits without manual rebasing.\n\nIf the destination revision cannot be determined unambiguously for a change, that change is left in the source revision. The source revision is abandoned if all changes are absorbed and it has no description.\n\nThe modifications made by absorb can be reviewed with `jj op show -p`."],["INSTALL","```pacman: sudo pacman -S jujutsu```\n\n```apk: sudo apk add jujutsu```\n\n```zypper: sudo zypper install jujutsu```\n\n```brew: brew install jujutsu```\n\n```nix: nix profile install nixpkgs#jujutsu```"],["SEE ALSO","[jj](/man/jj)(1), [jj-squash](/man/jj-squash)(1), [git-absorb](/man/git-absorb)(1)"]],"e":["jj","jj-squash","git-absorb"],"x":"jj absorb"},"k8sec":{"n":"k8sec","t":"manages Kubernetes secrets from the command line","s":[["TAGLINE","manages Kubernetes secrets from the command line"],["TLDR","**List secrets**\n\n```k8sec list```\n\n**Get secret value**\n\n```k8sec get [secret-name]```\n\n**Set secret**\n\n```k8sec set [secret-name] [key=value]```\n\n**Delete secret key**\n\n```k8sec unset [secret-name] [key]```\n\n**List in namespace**\n\n```k8sec list -n [namespace]```\n\n**Dump secret as env**\n\n```k8sec dump [secret-name]```"],["SYNOPSIS","**k8sec** _command_ [_options_]"],["PARAMETERS","**list**\n> List secrets.\n\n**get** _NAME_\n> Get secret values.\n\n**set** _NAME_ _KEY=VALUE_\n> Set secret key.\n\n**unset** _NAME_ _KEY_\n> Remove secret key.\n\n**dump** _NAME_\n> Export as env vars.\n\n**-n** _NAMESPACE_\n> Kubernetes namespace.\n\n**--help**\n> Display help information."],["DESCRIPTION","**k8sec** manages Kubernetes secrets from the command line. It simplifies viewing and editing secret values.\n\nThe tool decodes base64 automatically. It provides an easier interface than kubectl for secret management."],["CAVEATS","Requires kubeconfig. Secrets stored base64. Consider RBAC permissions."],["HISTORY","k8sec was created to simplify Kubernetes secret management with a more intuitive command-line interface."],["SEE ALSO","[kubectl](/man/kubectl)(1), [kubeseal](/man/kubeseal)(1), [vault](/man/vault)(1)"]],"e":["kubectl","kubeseal","vault"],"x":"k8sec list"},"kure":{"n":"kure","t":"CLI password manager with sessions","s":[["TAGLINE","CLI password manager with sessions"],["TLDR","**Add a new entry** to the database\n\n```kure add```\n\n**List all entries**\n\n```kure ls```\n\n**Copy a password** to the clipboard\n\n```kure copy [entry_name]```\n\n**Generate a random password**\n\n```kure gen```\n\n**Start a session** to run multiple commands without re-entering the master password\n\n```kure session```\n\n**Export the database**\n\n```kure export```"],["SYNOPSIS","**kure** _command_ [_options_]"],["DESCRIPTION","**kure** is a CLI password manager that emphasizes security and privacy by reducing the attack surface to its minimum. It works completely offline with no third-party connections. Each record is encrypted using **AES-GCM 256-bit** with a unique password derived using **Argon2id**.\n\nThe master password is never stored on disk — it is encrypted and temporarily held in a protected memory buffer that is destroyed immediately after use. Sessions allow running multiple commands by entering the master password only once, with configurable timeouts and custom scripts."],["CAVEATS","Completely offline — no built-in sync across devices. The database file must be manually transferred between systems. Losing the master password means permanent loss of stored data."],["HISTORY","**kure** was created by **GGP1** and is written in **Go**. It supports Linux, macOS, BSD, Windows, and mobile platforms."],["INSTALL","```aur: yay -S kure```"],["SEE ALSO","[pass](/man/pass)(1), [gopass](/man/gopass)(1)"]],"e":["pass","gopass"],"x":"kure add"},"lazygit":{"n":"lazygit","t":"terminal user interface for git commands that simplifies common git workflows","s":[["TAGLINE","terminal user interface for git commands that simplifies common git workflows"],["TLDR","**Open lazygit** in current repository\n\n```lazygit```\n\n**Open in specific repository**\n\n```lazygit -p [/path/to/repo]```\n\n**Open with specific work tree**\n\n```lazygit -w [/path/to/worktree]```\n\n**Open log file** after session\n\n```lazygit -d```\n\n**Print default config**\n\n```lazygit -c```\n\n**Check version**\n\n```lazygit -v```"],["SYNOPSIS","**lazygit** [_options_]"],["DESCRIPTION","**lazygit** is a terminal user interface (TUI) for Git that replaces sequences of typed commands with visual, keyboard-driven panels. Its split-pane layout presents Status, Files, Branches, Commits, and Stash side by side, letting you stage individual files or hunks with a single keypress, review diffs inline, and commit without leaving the interface.\n\nBeyond everyday staging and committing, lazygit makes Git's more advanced operations approachable. Interactive rebasing is handled through a visual commit list where entries can be reordered, squashed, or edited in place. Cherry-picking is a matter of tagging commits and pasting them onto another branch. Merge conflict resolution displays conflicting sections with one-key choices for each side. Branch management, remote operations (push, pull, fetch), and stash manipulation are all accessible from their respective panels with contextual menus that surface the available actions. A customizable configuration file allows user-defined keybindings and custom command shortcuts."],["PARAMETERS","**-p** _path_, **--path** _path_\n> Repository path to open.\n\n**-f** _path_, **--filter** _path_\n> Filter files by path in status panel.\n\n**-g** _path_, **--git-dir** _path_\n> Custom git directory.\n\n**-w** _path_, **--work-tree** _path_\n> Custom work tree.\n\n**-c**, **--config**\n> Print default config.\n\n**-d**, **--debug**\n> Run with debug logging.\n\n**-v**, **--version**\n> Print version."],["KEYBOARD SHORTCUTS","**Space**: Stage/unstage file or hunk\n**c**: Commit staged changes\n**P**: Push to remote\n**p**: Pull from remote\n**b**: Create branch\n**m**: Merge branch\n**r**: Rebase\n**s**: Stash changes\n**Tab**: Switch panels\n**?**: Show all keybindings\n**q**: Quit"],["CAVEATS","Requires git to be installed. Some operations modify git history and should be used carefully. Configuration file location varies by OS (~/.config/lazygit/config.yml on Linux)."],["HISTORY","**lazygit** was created by **Jesse Duffield** in **2018** to provide an easier way to interact with git from the terminal. Written in Go, it gained popularity quickly among developers who preferred terminal workflows but found raw git commands cumbersome. The project continues active development with features like worktree support and custom commands."],["INSTALL","```apt: sudo apt install lazygit```\n\n```pacman: sudo pacman -S lazygit```\n\n```apk: sudo apk add lazygit```\n\n```zypper: sudo zypper install lazygit```\n\n```brew: brew install lazygit```\n\n```nix: nix profile install nixpkgs#lazygit```"],["SEE ALSO","[git](/man/git)(1), [tig](/man/tig)(1), [gitk](/man/gitk)(1), [gitui](/man/gitui)(1)"]],"e":["git","tig","gitk","gitui"],"x":"lazygit"},"lich":{"n":"lich","t":"Worktree-scoped development stack orchestrator","s":[["TAGLINE","Worktree-scoped development stack orchestrator"],["TLDR","**Bring up the stack** described by lich.yaml in the current worktree\n\n```lich up```\n\n**Stop the stack** in the current worktree\n\n```lich down```\n\n**Tail logs** from the running stack\n\n```lich logs```\n\n**List all running stacks** across worktrees\n\n```lich stacks```\n\n**Tear down every stack** on the machine\n\n```lich nuke```"],["SYNOPSIS","**lich** _command_ [_options_]"],["DESCRIPTION","**lich** reads a **lich.yaml** file that describes a development stack (Docker containers, host processes, environment variables and lifecycle) and brings it up with **per-worktree isolation**. Several copies of the same stack can run at once on a single machine without port collisions or resource conflicts, which makes it useful when working on multiple Git branches or worktrees in parallel.\n\nServices are containers that lich manages through Docker, while **owned** entries are host processes (for example a dev server) that lich starts and supervises. Ports can be published into environment variables so processes discover each other, and values support template interpolation such as **${services.postgres.host_port}**."],["COMMANDS","**up**\n> Bring the development stack up in the current worktree.\n\n**down**\n> Stop the development stack in the current worktree.\n\n**logs**\n> Tail logs from the running stack.\n\n**stacks**\n> List all running stacks across worktrees with status, uptime, service count and URLs.\n\n**nuke**\n> Tear down all stacks on the machine."],["CONFIGURATION","A **lich.yaml** file in the project root describes the stack. Top-level keys:\n\n**version**\n> Configuration schema version (currently 1). Required.\n\n**services**\n> Docker containers to manage. Each entry sets an image, port mappings (optionally exposed through published_env) and environment variables.\n\n**owned**\n> Host processes to manage. Each entry sets a working directory (cwd), a command (cmd), a port and environment variables, with template interpolation of service values.\n\nThe **LICH_TELEMETRY** environment variable can be set to 0 to disable anonymous usage telemetry."],["HISTORY","**lich** is an open-source CLI written in **TypeScript** and built on the **Bun** runtime, created by **RPate97**. It is distributed under the **MIT** license, with documentation at lich.sh."],["SEE ALSO","[docker](/man/docker)(1), [docker-compose](/man/docker-compose)(1), [foreman](/man/foreman)(1), [git](/man/git)(1)"]],"e":["docker","docker-compose","foreman","git"],"x":"lich up"},"lpinfo":{"n":"lpinfo","t":"lists available printers and printer drivers known to CUPS","s":[["TAGLINE","lists available printers and printer drivers known to CUPS"],["TLDR","**List available printers**\n\n```lpinfo -v```\n\n**List available printer drivers**\n\n```lpinfo -m```\n\n**List drivers matching keyword**\n\n```lpinfo -m | grep -i [keyword]```\n\n**Show long printer names**\n\n```lpinfo -l -v```\n\n**List drivers for specific make**\n\n```lpinfo --make-and-model \"[HP LaserJet]\" -m```\n\n**Exclude specific schemes**\n\n```lpinfo -v --exclude-schemes [dnssd]```\n\n**Include only specific schemes**\n\n```lpinfo -v --include-schemes [usb,parallel]```"],["SYNOPSIS","**lpinfo** [_-l_] [_-m_] [_-v_] [_--make-and-model pattern_] [_options_]"],["PARAMETERS","**-l**\n> Show long listing with device details.\n\n**-m**\n> List available printer drivers (PPD files).\n\n**-v**\n> List available printer devices.\n\n**--device-id** _ID_\n> Show only devices matching IEEE-1284 device ID.\n\n**--make-and-model** _PATTERN_\n> Filter by make and model string.\n\n**--exclude-schemes** _SCHEMES_\n> Exclude device URIs matching schemes.\n\n**--include-schemes** _SCHEMES_\n> Include only device URIs matching schemes.\n\n**-h** _SERVER_\n> Connect to CUPS server.\n\n**--timeout** _SECONDS_\n> Timeout for device discovery."],["DESCRIPTION","**lpinfo** lists available printers and printer drivers known to CUPS. It queries the CUPS server for devices and PPD (PostScript Printer Description) files.\n\nDevice listing (-v) shows all detected printer connections including USB, network, and parallel ports. Each entry includes a device URI that can be used when adding printers with lpadmin.\n\nDriver listing (-m) shows PPD files that describe printer capabilities. PPDs define paper sizes, resolutions, color options, and other features. Driverless printing uses generic IPP Everywhere or AirPrint drivers.\n\nThe make-and-model filter helps find drivers for specific printers. Combined with grep, you can search for compatible drivers for your hardware.\n\nNetwork printer discovery may include DNS-SD (Bonjour/Avahi) discovered printers. Use scheme filters to focus on specific connection types."],["CAVEATS","Requires CUPS to be running. Network discovery can be slow. Some printers need proprietary drivers not included with CUPS. Driverless printing requires modern printers with IPP Everywhere or AirPrint support."],["HISTORY","**lpinfo** is part of the **CUPS** (Common UNIX Printing System) suite, originally developed by **Michael Sweet** at Easy Software Products. CUPS was released in **1999** and acquired by **Apple** in **2007**. lpinfo provides the discovery mechanism used by print configuration tools."],["INSTALL","```apt: sudo apt install cups-client```\n\n```dnf: sudo dnf install cups-client```\n\n```apk: sudo apk add cups-client```\n\n```zypper: sudo zypper install cups-client```"],["SEE ALSO","[lpadmin](/man/lpadmin)(1), [lpstat](/man/lpstat)(1), [lp](/man/lp)(1), [lpr](/man/lpr)(1), [cupsd](/man/cupsd)(8)"]],"e":["lpadmin","lpstat","lp","lpr","cupsd"],"x":"lpinfo -v"},"lzcmp":{"n":"lzcmp","t":"compare compressed files byte by byte","s":[["TAGLINE","compare compressed files byte by byte"],["TLDR","**Compare two compressed files**\n\n```lzcmp [file1.xz] [file2.xz]```\n\n**Compare with verbose output**\n\n```lzcmp -l [file1.xz] [file2.xz]```\n\n**Compare silently**\n\n```lzcmp -s [file1.gz] [file2.gz]```\n\n**Compare compressed file with its uncompressed original**\n\n```lzcmp [file.xz]```"],["SYNOPSIS","**lzcmp** [_cmp_options_] _file1_ [_file2_]"],["PARAMETERS","**-l**\n> Print byte number and differing byte values.\n\n**-s**\n> Silent mode, output nothing, return exit status only.\n\nAll options are passed directly to **cmp**(1)."],["DESCRIPTION","**lzcmp** invokes cmp(1) on compressed files. It supports files compressed with xz, lzma, gzip, bzip2, lzop, zstd, and lz4. If only one file is specified, it is compared against a file with the compression suffix stripped. If two files are specified, both are decompressed as needed and fed to cmp. The exit status from cmp is preserved: 0 if identical, 1 if different, 2 on error.\n\nThe name lzcmp is provided for backward compatibility with LZMA Utils. It is deprecated in favor of **xzcmp** and will be removed in a future version of XZ Utils."],["INSTALL","```dnf: sudo dnf install xz```\n\n```pacman: sudo pacman -S xz```\n\n```apk: sudo apk add xz```\n\n```zypper: sudo zypper install xz```\n\n```brew: brew install xz```\n\n```nix: nix profile install nixpkgs#xz```"],["SEE ALSO","[cmp](/man/cmp)(1), [lzdiff](/man/lzdiff)(1), [xzcmp](/man/xzcmp)(1), [xzdiff](/man/xzdiff)(1), [zcmp](/man/zcmp)(1), [xz](/man/xz)(1)"]],"e":["cmp","lzdiff","xzcmp","xzdiff","zcmp","xz"],"x":"lzcmp [file1.xz] [file2.xz]"},"makoctl-dismiss":{"n":"makoctl-dismiss","t":"removes notifications from the mako notification daemon","s":[["TAGLINE","removes notifications from the mako notification daemon"],["TLDR","Dismiss **most recent** notification\n\n```makoctl dismiss```\n\nDismiss by **ID**\n\n```makoctl dismiss -n [notification_id]```\n\nDismiss **all** notifications\n\n```makoctl dismiss --all```\n\nDismiss **first group**\n\n```makoctl dismiss --group```\n\nDismiss **without history**\n\n```makoctl dismiss --no-history```"],["SYNOPSIS","**makoctl dismiss** [_options_]"],["DESCRIPTION","**makoctl dismiss** removes notifications from the mako notification daemon. It can dismiss individual, grouped, or all notifications, optionally without adding them to history."],["PARAMETERS","**-n ID**\n> Dismiss specific notification by ID\n\n**-a, --all**\n> Dismiss all notifications\n\n**-g, --group**\n> Dismiss all notifications in first group\n\n**--no-history**\n> Don't add dismissed notifications to history"],["INSTALL","```apt: sudo apt install mako-notifier```\n\n```dnf: sudo dnf install mako```\n\n```pacman: sudo pacman -S mako```\n\n```apk: sudo apk add mako```\n\n```zypper: sudo zypper install mako```\n\n```brew: brew install mako```\n\n```nix: nix profile install nixpkgs#mako```"],["SEE ALSO","[makoctl](/man/makoctl)(1), [mako](/man/mako)(1)"]],"e":["makoctl","mako"],"x":"makoctl dismiss"},"mkfs.xfs":{"n":"mkfs.xfs","t":"creates an XFS filesystem on a partition or device","s":[["TAGLINE","creates an XFS filesystem on a partition or device"],["TLDR","Create **XFS filesystem** on partition\n\n```sudo mkfs.xfs /dev/sdXY```\n\nCreate with **volume label**\n\n```sudo mkfs.xfs -L mylabel /dev/sdXY```\n\n**Force** creation (overwrite existing)\n\n```sudo mkfs.xfs -f /dev/sdXY```\n\nSet **block size**\n\n```sudo mkfs.xfs -b size=4096 /dev/sdXY```\n\nCreate with specific **inode size**\n\n```sudo mkfs.xfs -i size=512 /dev/sdXY```"],["SYNOPSIS","**mkfs.xfs** [_options_] _device_"],["DESCRIPTION","**mkfs.xfs** creates an XFS filesystem on a partition or device. XFS is a high-performance journaling filesystem designed for scalability and parallel I/O operations."],["PARAMETERS","**-L LABEL**\n> Set the filesystem label (max 12 characters)\n\n**-f**\n> Force overwrite of existing filesystem\n\n**-b size=N**\n> Set block size in bytes (512-65536, default usually 4096)\n\n**-i size=N**\n> Set inode size in bytes\n\n**-d options**\n> Data section options (agcount, file, name, size, etc.)\n\n**-l options**\n> Log section options (internal, size, version, etc.)\n\n**-n options**\n> Naming options (size, version, etc.)\n\n**-r options**\n> Real-time section options\n\n**-q**\n> Quiet mode; suppress output\n\n**-N**\n> Dry run; print parameters without creating filesystem"],["CAVEATS","Creating a filesystem destroys all existing data on the device. The device should not be mounted. XFS cannot be shrunk, only grown."],["HISTORY","**mkfs.xfs** is part of **xfsprogs**, the XFS filesystem utilities. XFS was originally developed by Silicon Graphics for IRIX and was ported to Linux in 2001."],["INSTALL","```apt: sudo apt install xfsprogs```\n\n```dnf: sudo dnf install xfsprogs```\n\n```pacman: sudo pacman -S xfsprogs```\n\n```apk: sudo apk add xfsprogs```\n\n```zypper: sudo zypper install xfsprogs```\n\n```nix: nix profile install nixpkgs#xfsprogs```"],["SEE ALSO","[xfs](/man/xfs)(5), [xfs_admin](/man/xfs_admin)(8), [xfs_repair](/man/xfs_repair)(8), [mkfs](/man/mkfs)(8)"]],"e":["xfs","xfs_admin","xfs_repair","mkfs"],"x":"sudo mkfs.xfs /dev/sdXY"},"mkswap":{"n":"mkswap","t":"sets up a Linux swap area on a device or in a file","s":[["TAGLINE","sets up a Linux swap area on a device or in a file"],["TLDR","Set up a **swap area**\n\n```sudo mkswap /dev/sdXY```\n\nCheck for **bad blocks** before creating swap\n\n```sudo mkswap -c /dev/sdXY```\n\nSet a **label** for the swap partition\n\n```sudo mkswap -L swap_label /dev/sdXY```\n\nSet a specific **UUID**\n\n```sudo mkswap -U random /dev/sdXY```\n\nCreate a **swap file** of specific size\n\n```sudo mkswap -s 4G -F /path/to/swapfile```"],["SYNOPSIS","**mkswap** [_options_] _device_ [_size_]"],["DESCRIPTION","**mkswap** sets up a Linux swap area on a device or in a file. The device can be a disk partition or a regular file. After creation, use swapon to enable the swap area."],["PARAMETERS","**-c, --check**\n> Check the device for bad blocks before creating swap area\n\n**-f, --force**\n> Force creation even if the device has a detected filesystem\n\n**-L, --label LABEL**\n> Specify a label for the swap area (for use with swapon -L)\n\n**-p, --pagesize SIZE**\n> Specify page size in bytes\n\n**-U, --uuid UUID**\n> Specify UUID (clear, random, time, or specific UUID value)\n\n**-v, --swapversion VERSION**\n> Specify swap area version (only version 1 supported)\n\n**-e, --endianness ENDIAN**\n> Specify endianness (native or little)\n\n**-o, --offset OFFSET**\n> Offset for swap header in device\n\n**-s, --size SIZE**\n> Size of swap file (with -F)\n\n**-F, --file**\n> Create swap file instead of setting up existing file\n\n**--verbose**\n> Print verbose messages"],["CAVEATS","The device or file must not be in use when running mkswap. For swap files on btrfs, special handling is required. Always run swapon after mkswap to activate the swap area."],["HISTORY","**mkswap** is part of **util-linux**. Swap space provides virtual memory when physical RAM is exhausted."],["INSTALL","```apt: sudo apt install util-linux```\n\n```dnf: sudo dnf install util-linux```\n\n```pacman: sudo pacman -S util-linux```\n\n```apk: sudo apk add util-linux-misc```\n\n```zypper: sudo zypper install util-linux```\n\n```brew: brew install util-linux```\n\n```nix: nix profile install nixpkgs#util-linux```"],["SEE ALSO","[swapon](/man/swapon)(8), [swapoff](/man/swapoff)(8), [fdisk](/man/fdisk)(8)"]],"e":["swapon","swapoff","fdisk"],"x":"sudo mkswap /dev/sdXY"},"mlbt":{"n":"mlbt","t":"TUI for viewing MLB baseball stats and live games","s":[["TAGLINE","TUI for viewing MLB baseball stats and live games"],["TLDR","**Launch the MLB TUI**\n\n```mlbt```"],["SYNOPSIS","**mlbt** [_options_]"],["DESCRIPTION","**mlbt** is a terminal user interface for the MLB stats API that lets you follow baseball games in your terminal. It provides live scores, standings, and game stats through an interactive TUI built with the ratatui framework.\n\nYou can check scores, view standings, browse stats, and watch live Gameday updates. Configuration options include setting a favorite team, timezone, and log level."],["CAVEATS","Requires an internet connection to fetch data from the MLB stats API."],["HISTORY","**mlbt** was created by **Andrew Schneider** and is written in **Rust**."]],"e":[],"x":"mlbt"},"newrole":{"n":"newrole","t":"starts a new shell with a different SELinux security context","s":[["TAGLINE","starts a new shell with a different SELinux security context"],["TLDR","Start a new shell with a specific **SELinux role**\n\n```newrole -r [role_name]```\n\nStart a new shell with a specific **SELinux type**\n\n```newrole -t [type_name]```\n\nStart a new shell with a specific **SELinux level**\n\n```newrole -l [s0-s0:c0.c1023]```\n\nStart a new shell with **both role and type**\n\n```newrole -r [role_name] -t [type_name]```"],["SYNOPSIS","**newrole** [_options_]"],["PARAMETERS","**-r, --role _role_**\n> Specify the new SELinux role\n\n**-t, --type _type_**\n> Specify the new SELinux type (domain)\n\n**-l, --level _level_**\n> Specify the new SELinux sensitivity level\n\n**-p, --preserve-environment**\n> Preserve environment variables when transitioning\n\n**-V, --version**\n> Display version information"],["DESCRIPTION","**newrole** starts a new shell with a different SELinux security context. It allows users to transition between roles they're authorized to use, enabling role-based access control (RBAC) within SELinux.\n\nSELinux contexts have the format **user:role:type:level**. The newrole command changes the role and/or type components, which affects what actions the user can perform. Common transitions include switching from a restricted user role to an administrative role.\n\nThe user must be mapped to the target role in SELinux policy (see **semanage user**). Authentication may be required depending on policy configuration."],["CAVEATS","Only works on SELinux-enabled systems. The user must be authorized for the target role in SELinux policy. Failed transitions may result in access denials. Use **id -Z** to verify the current context before and after transition."],["INSTALL","```apt: sudo apt install newrole```"],["SEE ALSO","[runcon](/man/runcon)(1), [semanage-user](/man/semanage-user)(8), [id](/man/id)(1), [seinfo](/man/seinfo)(1)"]],"e":["runcon","semanage-user","id","seinfo"],"x":"newrole -r [role_name]"},"npm-ls":{"n":"npm-ls","t":"lists installed packages","s":[["TAGLINE","lists installed packages"],["TLDR","**List installed packages**\n\n```npm ls```\n\n**List all depths**\n\n```npm ls --all```\n\n**List specific package**\n\n```npm ls [package-name]```\n\n**List global packages**\n\n```npm ls -g```\n\n**JSON output**\n\n```npm ls --json```\n\n**Production only**\n\n```npm ls --production```\n\n**Show depth**\n\n```npm ls --depth=[2]```"],["SYNOPSIS","**npm** **ls** [_package_] [_options_]"],["PARAMETERS","_PACKAGE_\n> Package to find.\n\n**--all**\n> Show all dependencies.\n\n**-g**\n> Global packages.\n\n**--json**\n> JSON output.\n\n**--depth** _N_\n> Tree depth.\n\n**--help**\n> Display help information."],["DESCRIPTION","**npm ls** lists installed packages. Shows dependency tree.\n\nThe command displays package hierarchy. Identifies dependency versions."],["CAVEATS","Alias: npm list. Tree can be large. Use depth to limit."],["HISTORY","npm ls provides **dependency visualization** for understanding package relationships."],["INSTALL","```pacman: sudo pacman -S npm```\n\n```apk: sudo apk add npm```\n\n```brew: brew install npm```"],["SEE ALSO","[npm](/man/npm)(1), [npm-explain](/man/npm-explain)(1), [npm-outdated](/man/npm-outdated)(1)"]],"e":["npm","npm-explain","npm-outdated"],"x":"npm ls"},"offpunk":{"n":"offpunk","t":"Offline-first command-line browser for Gemini, Gopher, and Web","s":[["TAGLINE","Offline-first command-line browser for Gemini, Gopher, and Web"],["TLDR","**Launch the browser** interactively\n\n```offpunk```\n\n**Open a specific URL**\n\n```offpunk [gemini://example.com]```\n\n**Synchronize all bookmarked content** for offline reading\n\n```offpunk --sync```\n\n**Synchronize with a specific depth** of linked pages\n\n```offpunk --sync [2]```\n\n**Access the built-in tutorial**\n\n```offpunk --tutorial```"],["SYNOPSIS","**offpunk** [_options_] [_url_]"],["PARAMETERS","**--sync** [_depth_]\n> Synchronize bookmarked content for offline use. Optional depth controls how many levels of links to follow (default 0).\n\n**--tutorial**\n> Open the built-in tutorial page.\n\n**--assume-yes**\n> Automatically answer yes to prompts (useful for cron-based sync).\n\n**--disable-http**\n> Disable fetching of HTTP/HTTPS content.\n\n**--version**\n> Show version information."],["DESCRIPTION","**offpunk** is a command-line and offline-first browser and feed reader supporting Gemini, Gopher, Spartan, and Web protocols. It allows you to synchronize content once and then browse and organize it while disconnected.\n\nInside the interactive shell, enter URLs or link numbers to navigate. Use `ls` to list bookmarks, `add` to bookmark pages, and `help` for the full command list. Content is rendered in the terminal using ANSI formatting.\n\nOffline content is cached in **~/.cache/offpunk/** as plain text files. The tool includes subcomponents: **netcache** for network caching, **ansicat** for terminal rendering, and **opnk** for file handling."],["CAVEATS","Web rendering is text-based and may not display complex pages accurately. Requires **python-readability** and **python-html2text** for proper web content rendering. Images require **chafa** or **timg** for terminal display."],["HISTORY","**offpunk** was created by **Ploum** (Lionel Dricot) as a fork of the **AV-98** Gemini browser, written in **Python**. It emphasizes offline-first browsing and digital minimalism."],["INSTALL","```apk: sudo apk add offpunk```\n\n```nix: nix profile install nixpkgs#offpunk```"],["SEE ALSO","[lynx](/man/lynx)(1), [w3m](/man/w3m)(1), [bombadillo](/man/bombadillo)(1)"]],"e":["lynx","w3m","bombadillo"],"x":"offpunk"},"openocd":{"n":"openocd","t":"provides debugging, in-system programming, and boundary-scan testing for embedded systems","s":[["TAGLINE","provides debugging, in-system programming, and boundary-scan testing for embedded systems"],["TLDR","**Start with interface and target config files**\n\n```openocd -f [interface/stlink.cfg] -f [target/stm32f4x.cfg]```\n\n**Program flash with verify and reset**\n\n```openocd -f [interface.cfg] -f [target.cfg] -c \"program [firmware.elf] verify reset exit\"```\n\n**Program a hex file at a specific address**\n\n```openocd -f [interface.cfg] -f [target.cfg] -c \"program [firmware.hex] 0x08000000 verify reset exit\"```\n\n**Start with a board config (includes interface and target)**\n\n```openocd -f [board/stm32f4discovery.cfg]```\n\n**Start and connect GDB server on a custom port**\n\n```openocd -f [config.cfg] -c \"gdb_port [4444]\"```\n\n**Dump flash memory to a binary file**\n\n```openocd -f [config.cfg] -c \"init\" -c \"dump_image [output.bin] 0x08000000 0x10000\" -c \"exit\"```\n\n**Erase entire flash memory**\n\n```openocd -f [interface.cfg] -f [target.cfg] -c \"init\" -c \"reset halt\" -c \"flash erase_sector 0 0 last\" -c \"exit\"```\n\n**Enable verbose debug output**\n\n```openocd -d3 -f [interface.cfg] -f [target.cfg]```"],["SYNOPSIS","**openocd** [_-f config_] [_-c command_] [_-d level_] [_options_]"],["PARAMETERS","**-f** _FILE_\n> Configuration file.\n\n**-c** _CMD_\n> Run command.\n\n**-s** _DIR_\n> Search directory.\n\n**-d** _LEVEL_\n> Debug level (0-4, default 2). Higher values produce more output.\n\n**-l** _FILE_\n> Log output to file instead of stderr.\n\n**-p**, **--pipe**\n> Use pipes for GDB communication instead of TCP.\n\n**--version**\n> Show version.\n\n**--help**\n> Show help."],["DESCRIPTION","**openocd** (Open On-Chip Debugger) provides debugging, in-system programming, and boundary-scan testing for embedded systems. It interfaces with JTAG, SWD, and other debug adapters to access microcontrollers and FPGAs.\n\nConfiguration files define the debug adapter (interface) and target device. Many boards and adapters have predefined configurations in the installation.\n\nProgramming mode flashes firmware to microcontrollers. Verify confirms the write succeeded. Reset starts the new firmware.\n\nWhen running as a server, OpenOCD provides GDB server (port 3333) and telnet interface (port 4444). GDB connects for source-level debugging.\n\nThe telnet interface allows direct commands: halt, resume, step, memory read/write, register access, and breakpoint management.\n\nMultiple targets can be debugged simultaneously. JTAG chains with several devices are supported."],["CAVEATS","Requires appropriate adapter hardware. Target configuration must match device. Some features are adapter-specific. Debug permissions may need udev rules."],["HISTORY","**OpenOCD** was started by **Dominic Rath** as a university project in **2005**. It became the standard open-source on-chip debugger, supporting hundreds of target devices and debug adapters. Development continues under community governance."],["INSTALL","```apt: sudo apt install openocd```\n\n```dnf: sudo dnf install openocd```\n\n```pacman: sudo pacman -S openocd```\n\n```apk: sudo apk add openocd```\n\n```zypper: sudo zypper install openocd```\n\n```brew: brew install openocd```\n\n```nix: nix profile install nixpkgs#openocd```"],["SEE ALSO","[gdb](/man/gdb)(1), [arm-none-eabi-gdb](/man/arm-none-eabi-gdb)(1), [stlink](/man/stlink)(1), [st-flash](/man/st-flash)(1), [avrdude](/man/avrdude)(1), [flashrom](/man/flashrom)(1)"]],"e":["gdb","arm-none-eabi-gdb","stlink","st-flash","avrdude","flashrom"],"x":"openocd -f [interface/stlink.cfg] -f [target/stm32f4x.cfg]"},"osv-scanner":{"n":"osv-scanner","t":"Scan dependencies for known vulnerabilities using the OSV database","s":[["TAGLINE","Scan dependencies for known vulnerabilities using the OSV database"],["TLDR","**Scan a directory recursively**\n\n```osv-scanner scan -r [directory]```\n\n**Scan a specific lockfile**\n\n```osv-scanner scan --lockfile [package-lock.json]```\n\n**Scan an SBOM file**\n\n```osv-scanner scan --sbom [sbom.json]```\n\n**Output results as JSON**\n\n```osv-scanner scan -r [directory] --format json```\n\n**Scan a Docker image**\n\n```osv-scanner scan --docker [image:tag]```\n\n**Scan and generate a guided remediation report**\n\n```osv-scanner fix -r [directory]```"],["SYNOPSIS","**osv-scanner** _command_ [_options_] [_targets_]"],["PARAMETERS","**scan**\n> Scan dependencies for vulnerabilities.\n\n**fix**\n> Generate guided remediation suggestions.\n\n**-r**, **--recursive** _DIR_\n> Scan directory recursively for lockfiles and manifests.\n\n**--lockfile** _FILE_\n> Scan a specific lockfile (auto-detects ecosystem).\n\n**--sbom** _FILE_\n> Scan an SBOM file (supports SPDX and CycloneDX).\n\n**--docker** _IMAGE_\n> Scan a Docker image for vulnerabilities.\n\n**--format** _FORMAT_\n> Output format: table (default), json, markdown, sarif.\n\n**--config** _FILE_\n> Path to osv-scanner.toml configuration file.\n\n**--call-analysis**\n> Enable call graph analysis to filter unreachable vulnerabilities (Go, Rust).\n\n**--no-ignore**\n> Do not respect ignore entries in the config file.\n\n**--verbosity** _LEVEL_\n> Set verbosity level: error, warn, info, verbose.\n\n**--help**\n> Display help information."],["DESCRIPTION","**osv-scanner** scans project dependencies for known vulnerabilities by querying the **OSV.dev** database, which aggregates vulnerability data from multiple sources including GitHub Advisory Database, PyPI, RubyGems, and crates.io.\n\nThe tool automatically detects and parses lockfiles from most major package ecosystems including npm, pip, Go modules, Cargo, Maven, NuGet, and more. It can also scan SBOM files in SPDX or CycloneDX formats and Docker container images.\n\nThe **fix** subcommand provides guided remediation by suggesting version upgrades that resolve detected vulnerabilities while minimizing breaking changes.\n\nCall graph analysis (supported for Go and Rust) can reduce false positives by determining whether vulnerable code paths are actually reachable from the project."],["CAVEATS","Requires network access to query the OSV.dev database. Vulnerability coverage depends on data submitted to OSV by various ecosystems. Call graph analysis is only available for Go and Rust projects."],["HISTORY","osv-scanner was released by **Google** in **December 2022** as a frontend for the OSV.dev vulnerability database. It was designed to provide a free, open-source alternative for dependency scanning. The **fix** subcommand for guided remediation was added in 2024."],["INSTALL","```pacman: sudo pacman -S osv-scanner```\n\n```apk: sudo apk add osv-scanner```\n\n```zypper: sudo zypper install osv-scanner```\n\n```brew: brew install osv-scanner```\n\n```nix: nix profile install nixpkgs#osv-scanner```"],["SEE ALSO","[npm-audit](/man/npm-audit)(1), [trivy](/man/trivy)(1), [grype](/man/grype)(1), [snyk](/man/snyk)(1)"]],"e":["npm-audit","trivy","grype","snyk"],"x":"osv-scanner scan -r [directory]"},"pam_shells":{"n":"pam_shells","t":"PAM module to validate user login shells","s":[["TAGLINE","PAM module to validate user login shells"],["TLDR","**Require valid shell for authentication**\n\n```auth required pam_shells.so```\n\n**Require valid shell for account access**\n\n```account required pam_shells.so```"],["SYNOPSIS","**pam_shells.so**"],["PARAMETERS","This module does not accept any parameters."],["DESCRIPTION","**pam_shells** is a PAM module that only allows access to the system if the user's shell is listed in **/etc/shells**. It also checks that /etc/shells is a plain file and not world-writable.\n\nThe module provides both **auth** and **account** module types. It returns PAM_AUTH_ERR if the shell is not listed, PAM_SUCCESS if the shell is valid, and PAM_SERVICE_ERR if the username cannot be determined."],["CAVEATS","Requires **/etc/shells** to exist and contain valid shell paths (one absolute path per line). Users with shells not listed in /etc/shells will be denied access. Blank lines and lines starting with # in /etc/shells are ignored."],["HISTORY","**pam_shells** is part of the Linux-PAM (Pluggable Authentication Modules) framework, providing a simple mechanism to restrict access based on whether a user has a legitimate login shell."],["SEE ALSO","[pam](/man/pam)(8), [pam_unix](/man/pam_unix)(8), [pam_deny](/man/pam_deny)(8), [chsh](/man/chsh)(1), [getent](/man/getent)(1), [login](/man/login)(1)"]],"e":["pam","pam_unix","pam_deny","chsh","getent","login"],"x":"auth required pam_shells.so"},"paperconfig":{"n":"paperconfig","t":"sets system paper size","s":[["TAGLINE","sets system paper size"],["TLDR","**Set default paper size**\n\n```paperconfig -p [a4]```\n\n**Set to letter size**\n\n```paperconfig -p letter```\n\n**Reconfigure interactively** (Debian/Ubuntu)\n\n```dpkg-reconfigure libpaper1```"],["SYNOPSIS","**paperconfig** [_options_]"],["PARAMETERS","**-p** _PAPER_\n> Set paper size.\n\n**-h**, **--help**\n> Display help."],["DESCRIPTION","**paperconfig** sets the system-wide default paper size used by the libpaper library (e.g., a4, letter, legal). Applications that query libpaper (such as many printing and TeX tools) use this value as their default.\n\nThe tool rewrites **/etc/papersize** to hold the chosen paper name. Available paper names can be listed with **paperconf -a**."],["CAVEATS","Root required. Part of libpaper. Affects printing defaults."],["HISTORY","paperconfig is part of **libpaper** for setting system paper size."],["INSTALL","```apt: sudo apt install libpaper-utils```"],["SEE ALSO","[paperconf](/man/paperconf)(1), [lp](/man/lp)(1)"]],"e":["paperconf","lp"],"x":"paperconfig -p [a4]"},"pct-status":{"n":"pct-status","t":"Show the running state of a Proxmox LXC container","s":[["TAGLINE","Show the running state of a Proxmox LXC container"],["TLDR","**Check** container status (prints e.g. `status: running`)\n\n```pct status [100]```\n\n**Show verbose** runtime and configuration details\n\n```pct status [100] --verbose```"],["SYNOPSIS","**pct status** [_OPTIONS_] _vmid_"],["DESCRIPTION","**pct status** displays the current status of a Proxmox LXC container, showing whether it is running, stopped, or in another state."],["PARAMETERS","**vmid**\n> The numeric ID of the container\n\n**--verbose**\n> Display detailed status information including resource usage"],["CAVEATS","Status is reported from the Proxmox perspective. A container showing as running may have internal issues not visible through this command."],["HISTORY","**pct status** is part of the **Proxmox VE** virtualization platform for managing LXC containers."],["SEE ALSO","[pct](/man/pct)(1), [pct-list](/man/pct-list)(1)"]],"e":["pct","pct-list"],"x":"pct status [100]"},"peerflix":{"n":"peerflix","t":"streams torrents directly","s":[["TAGLINE","streams torrents directly"],["TLDR","**Stream a torrent to the default player**\n\n```peerflix \"[magnet_link]\"```\n\n**Stream to VLC**\n\n```peerflix \"[torrent_file]\" --vlc```\n\n**Stream to mpv**\n\n```peerflix \"[magnet_link]\" --mpv```\n\n**Select a specific file by index**\n\n```peerflix \"[magnet_link]\" -i [1]```\n\n**List files in the torrent**\n\n```peerflix \"[magnet_link]\" -l```\n\n**Stream with subtitles**\n\n```peerflix \"[magnet_link]\" --vlc -t [subtitles.srt]```"],["SYNOPSIS","**peerflix** [_options_] _torrent_"],["PARAMETERS","_TORRENT_\n> Torrent file or magnet link.\n\n**--vlc**\n> Open stream in VLC.\n\n**--mplayer**\n> Open stream in MPlayer.\n\n**--mpv**\n> Open stream in mpv.\n\n**-i** _INDEX_, **--index** _INDEX_\n> File index to stream.\n\n**-l**, **--list**\n> List available files in the torrent.\n\n**-t** _FILE_, **--subtitles** _FILE_\n> Load subtitles file.\n\n**-a**\n> Play all files in the torrent.\n\n**-c** _NUM_, **--connections** _NUM_\n> Maximum connected peers (default: 100).\n\n**-p** _PORT_, **--port** _PORT_\n> HTTP streaming port (default: 8888).\n\n**--remove**\n> Delete files after streaming.\n\n**--help**\n> Display help information."],["DESCRIPTION","**peerflix** is a streaming torrent client for Node.js that enables instant playback without waiting for the full download. It prioritizes downloading the initial parts of the file first to allow immediate streaming.\n\nThe tool starts a local HTTP server and can pipe the stream to media players like VLC, mpv, or MPlayer. It supports multi-file torrents with file selection and subtitle loading."],["CAVEATS","Requires Node.js. Install via `npm install -g peerflix`. A media player (VLC, mpv, or MPlayer) is needed for playback."],["HISTORY","peerflix was created for **streaming torrent content** without full download."],["INSTALL","```aur: yay -S peerflix```"],["SEE ALSO","[webtorrent](/man/webtorrent)(1), [vlc](/man/vlc)(1), [mpv](/man/mpv)(1), [mplayer](/man/mplayer)(1)"]],"e":["webtorrent","vlc","mpv","mplayer"],"x":"peerflix \"[magnet_link]\""},"phpdismod":{"n":"phpdismod","t":"disables PHP extensions on Debian-based systems by removing the symbolic link","s":[["TAGLINE","disables PHP extensions on Debian-based systems by removing the symbolic link"],["TLDR","**Disable** extension for all PHP versions and SAPIs\n\n```sudo phpdismod [extension]```\n\nDisable extension for **specific version** and SAPI\n\n```sudo phpdismod -v [7.4] -s [cli] [extension]```"],["SYNOPSIS","**phpdismod** [**-v** _version_] [**-s** _sapi_] _module_"],["PARAMETERS","**-v** _version_\n> PHP version (e.g., 7.4, 8.0, 8.1). Use ALL for all versions.\n\n**-s** _sapi_\n> SAPI type (cli, fpm, apache2, cgi). Use ALL for all SAPIs.\n\n**-q**, **--quiet**\n> Suppress output messages.\n\n_module_\n> One or more extension names to disable."],["DESCRIPTION","**phpdismod** disables PHP extensions on Debian-based systems by removing the symbolic link from the conf.d directory. Without version or SAPI specified, it disables the extension for all installed PHP versions and all SAPIs.\n\nThe command works by manipulating symlinks in /etc/php/VERSION/SAPI/conf.d/ that point to module configuration files in mods-available."],["CAVEATS","Debian/Ubuntu specific tool. Requires root privileges. Changes take effect after restarting PHP-FPM or Apache. Some extensions have dependencies that may cause issues when disabled."],["HISTORY","**phpdismod** is part of the **php-defaults** package maintained by the Debian PHP team. It provides a standardized way to manage PHP extensions, similar to how a2enmod/a2dismod manage Apache modules."],["SEE ALSO","[phpenmod](/man/phpenmod)(8), [phpquery](/man/phpquery)(8), [php](/man/php)(1)"]],"e":["phpenmod","phpquery","php"],"x":"sudo phpdismod [extension]"},"pi3topbm":{"n":"pi3topbm","t":"converts Atari Degas PI3 high-resolution monochrome images to PBM","s":[["TAGLINE","converts Atari Degas PI3 high-resolution monochrome images to PBM"],["TLDR","**Convert Atari Degas PI3 to PBM**\n\n```pi3topbm [input.pi3] > [output.pbm]```"],["SYNOPSIS","**pi3topbm** [_file_]"],["DESCRIPTION","**pi3topbm** converts Atari Degas PI3 high-resolution monochrome images to PBM. PI3 was used by Degas paint program on Atari ST. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pbmtopi3](/man/pbmtopi3)(1), [pi1toppm](/man/pi1toppm)(1)"]],"e":["pbmtopi3","pi1toppm"],"x":"pi3topbm [input.pi3] > [output.pbm]"},"pueue-pause":{"n":"pueue-pause","t":"Pause pueue task queue processing","s":[["TAGLINE","Pause pueue task queue processing"],["TLDR","**Pause all tasks**\n\n```pueue pause```\n\n**Pause specific task**\n\n```pueue pause [task_id]```\n\n**Pause group**\n\n```pueue pause --group [group]```\n\n**Wait for current to finish**\n\n```pueue pause --wait```"],["SYNOPSIS","**pueue pause** [_options_] [_task_ids_]"],["PARAMETERS","_TASK_IDS_\n> Tasks to pause.\n\n**--group** _NAME_\n> Pause specific group.\n\n**--wait**\n> Wait for running tasks.\n\n**--all**\n> Pause all groups."],["DESCRIPTION","**pueue pause** pauses the task queue, preventing new tasks from starting. Currently running tasks continue to completion unless individually paused. Use **--wait** to let running tasks finish before pausing takes effect, or **--group** to pause only a specific task group."],["CAVEATS","Running tasks not affected. Part of pueue."],["HISTORY","pueue pause provides **queue pausing** functionality."],["INSTALL","```pacman: sudo pacman -S pueue```\n\n```apk: sudo apk add pueue```\n\n```zypper: sudo zypper install pueue```\n\n```brew: brew install pueue```\n\n```nix: nix profile install nixpkgs#pueue```"],["SEE ALSO","[pueue](/man/pueue)(1), [pueue-start](/man/pueue-start)(1)"]],"e":["pueue","pueue-start"],"x":"pueue pause"},"pvesh":{"n":"pvesh","t":"Command-line Proxmox REST API access","s":[["TAGLINE","Command-line Proxmox REST API access"],["TLDR","List available **nodes**\n\n```pvesh get /nodes```\n\nDisplay detailed information about **containers or VMs**\n\n```pvesh get /nodes/[node_name]/[lxc|qemu]```\n\n**Discover** API paths\n\n```pvesh ls /```\n\nDisplay API path **usage instructions**\n\n```pvesh usage /pools```\n\n**Add** a new thinpool storage\n\n```pvesh create /storage --storage [storage_id] --vgname [volume_group] --type lvmthin --thinpool [thinpool_name] --content [content_types]```"],["SYNOPSIS","**pvesh** _command_ _path_ [_options_]"],["PARAMETERS","**get**, **g**\n> Retrieve data from API path\n\n**ls**, **l**\n> List available subpaths\n\n**usage**, **u**\n> Show usage information for API path\n\n**create**\n> Create new resource at API path\n\n**set**\n> Modify existing resource\n\n**delete**\n> Remove resource at API path\n\n**--output-format** _format_\n> Output format (text, json, json-pretty)"],["DESCRIPTION","**pvesh** provides direct command-line access to the Proxmox VE REST API. It allows querying and modifying all cluster resources including nodes, VMs, containers, storage, and users. This is useful for scripting and automation tasks.\n\nThe tool mirrors the web interface's capabilities, enabling administrators to perform any operation available through the GUI from the command line or scripts."],["CAVEATS","Requires root privileges or appropriate Proxmox permissions. API paths match the web interface structure. Complex operations may require multiple API calls."],["HISTORY","Part of **Proxmox VE**, providing CLI access to the same API used by the web interface. Essential for automation, scripting, and headless administration of Proxmox clusters."],["SEE ALSO","[qm](/man/qm)(1), [pct](/man/pct)(1), [pvesm](/man/pvesm)(1), [pveum](/man/pveum)(1)"]],"e":["qm","pct","pvesm","pveum"],"x":"pvesh get /nodes"},"pw-top":{"n":"pw-top","t":"Real-time PipeWire performance statistics","s":[["TAGLINE","Real-time PipeWire performance statistics"],["TLDR","Display **interactive** view\n\n```pw-top```\n\nMonitor **remote** instance\n\n```pw-top -r [remote_name]```\n\nRun in **batch** mode\n\n```pw-top -b```\n\nBatch mode with **iteration** limit\n\n```pw-top -b -n [5]```"],["SYNOPSIS","**pw-top** [**-r** _remote_] [**-b**] [**-n** _iterations_] [**-V**]"],["PARAMETERS","**-r, --remote=_name_**\n> The name of the remote instance to monitor. If left unspecified, connects to the default PipeWire instance\n\n**-b, --batch-mode**\n> Print periodically instead of interactive\n\n**-n, --iterations=_num_**\n> Exit after specified number of batch iterations. Only used in batch mode\n\n**-V, --version**\n> Display version information\n\n**-h, --help**\n> Display help information"],["DESCRIPTION","**pw-top** provides a dynamic real-time view of PipeWire node and device statistics. It shows a hierarchical view of Driver nodes and follower nodes, displaying CPU usage, buffer statistics, latency, xruns, and other performance metrics in a top-like interface.\n\nThe tool is essential for diagnosing audio performance issues, identifying xruns, and monitoring PipeWire's behavior under load. Press **q** to quit and **c** to clear error counters."],["CAVEATS","Requires running PipeWire daemon. Statistics refresh rate depends on system load. Some metrics may not be available for all nodes."],["HISTORY","**pw-top** is part of **PipeWire**, providing performance monitoring similar to top but specifically for the audio subsystem. It helps diagnose latency and performance issues."],["INSTALL","```apt: sudo apt install pipewire-bin```\n\n```apk: sudo apk add pipewire-tools```\n\n```zypper: sudo zypper install pipewire-tools```"],["SEE ALSO","[pw-cli](/man/pw-cli)(1), [pw-dump](/man/pw-dump)(1), [pw-profiler](/man/pw-profiler)(1), [pipewire](/man/pipewire)(1)"]],"e":["pw-cli","pw-dump","pw-profiler","pipewire"],"x":"pw-top"},"raspi-gpio":{"n":"raspi-gpio","t":"Query and set Raspberry Pi GPIO pin states","s":[["TAGLINE","Query and set Raspberry Pi GPIO pin states"],["TLDR","**Show all GPIO pin states**\n\n```raspi-gpio get```\n\n**Get the state of a specific pin**\n\n```raspi-gpio get [17]```\n\n**Configure a pin as output**\n\n```sudo raspi-gpio set [17] op```\n\n**Drive a pin high**\n\n```sudo raspi-gpio set [17] dh```\n\n**Drive a pin low**\n\n```sudo raspi-gpio set [17] dl```\n\n**Set as input with pull-up**\n\n```sudo raspi-gpio set [18] ip pu```\n\n**List alternate functions** for a pin\n\n```raspi-gpio funcs [17]```\n\n**Dump raw register values**\n\n```sudo raspi-gpio raw```"],["SYNOPSIS","**raspi-gpio** _command_ [_pin_] [_options_]"],["COMMANDS","**get** [_pin_|_pin1,pin2,..._]\n> Show pin states. Without a pin number, shows all GPIOs (0-53).\n\n**set** _pin_ _options..._\n> Configure pin direction, drive level, pull, or alternate function.\n\n**funcs** [_pin_]\n> Print the BCM-defined alternate functions for the given pin (or all pins).\n\n**raw**\n> Print the raw GPIO register values."],["OPTIONS FOR SET","**ip** / **op**\n> Set as input / output.\n\n**a0** ... **a5**\n> Select alternate function 0-5.\n\n**dl** / **dh**\n> Drive low / drive high (only meaningful when **op** is set).\n\n**pu** / **pd** / **pn**\n> Pull up / pull down / no pull."],["DESCRIPTION","**raspi-gpio** is a low-level tool for inspecting and manipulating GPIO pins on Raspberry Pi boards. It writes directly to the BCM GPIO controller registers via /dev/gpiomem (or /dev/mem when run as root), bypassing higher-level libraries like libgpiod.\n\nThe tool is mainly intended for debugging hardware setups, verifying that DT overlays applied the expected pin functions, and one-off scripting. For production use, the kernel **gpiod** interface (via **libgpiod**'s **gpioget**/**gpioset**) is recommended because it integrates with the kernel's GPIO subsystem and respects kernel-claimed lines."],["CAVEATS","Raspberry Pi specific. **set** and **raw** require root because they write to the hardware. Writing the wrong direction or drive level on a pin connected to external hardware can damage the SoC. The numbering used is BCM (Broadcom) — not the physical board pin numbers."],["HISTORY","**raspi-gpio** is included with **Raspberry Pi OS** as a small utility maintained by the Raspberry Pi Foundation. It complements higher-level libraries like **wiringPi** (deprecated) and the kernel **gpiod** interface."],["SEE ALSO","[gpio](/man/gpio)(1), [vcgencmd](/man/vcgencmd)(1)"]],"e":["gpio","vcgencmd"],"x":"raspi-gpio get"},"semanage-permissive":{"n":"semanage-permissive","t":"Manage per-domain SELinux permissive mode","s":[["TAGLINE","Manage per-domain SELinux permissive mode"],["TLDR","**List** all process types in permissive mode\n\n```sudo semanage permissive -l```\n\n**Set** permissive mode for a domain\n\n```sudo semanage permissive -a [httpd_t]```\n\n**Unset** permissive mode for a domain\n\n```sudo semanage permissive -d [httpd_t]```"],["SYNOPSIS","**semanage permissive** [**-l**|**-a**|**-d**] [_domain_]"],["PARAMETERS","**-l, --list**\n> List all domains in permissive mode\n\n**-a, --add**\n> Add a domain to permissive mode\n\n**-d, --delete**\n> Remove a domain from permissive mode"],["DESCRIPTION","**semanage permissive** manages per-domain permissive mode in SELinux. When a domain is set to permissive, SELinux logs policy violations but does not enforce them for processes in that domain.\n\nThis provides more granular control than global permissive mode (**setenforce 0**), allowing specific services to be unconfined while the rest of the system remains in enforcing mode."],["CAVEATS","Permissive domains are effectively unconfined and should only be used for troubleshooting. For production systems, configure proper SELinux policy rules instead of leaving domains in permissive mode. Requires root privileges."],["SEE ALSO","[semanage](/man/semanage)(8), [setenforce](/man/setenforce)(8), [getenforce](/man/getenforce)(8), [audit2allow](/man/audit2allow)(1)"]],"e":["semanage","setenforce","getenforce","audit2allow"],"x":"sudo semanage permissive -l"},"shelly":{"n":"shelly","t":"Unified Arch Linux package manager CLI","s":[["TAGLINE","Unified Arch Linux package manager CLI"],["TLDR","**Upgrade all packages** from every source (repo, AUR, Flatpak, AppImage)\n\n```shelly upgrade-all```\n\n**Install** a package from the repos\n\n```shelly install [package]```\n\n**Remove** an installed package and unused dependents\n\n```shelly remove --cascade [package]```\n\n**Search** available packages in the standard repositories\n\n```shelly query --available [search_term]```\n\n**Search** the AUR\n\n```shelly aur search [search_term]```\n\n**Remove orphaned packages** (installed as dependencies but no longer required)\n\n```shelly purify --orphans```\n\n**Install without confirmation prompts**\n\n```shelly install --no-confirm [package]```"],["SYNOPSIS","**shelly** [_options_] \\<command\\> [_args_]"],["DESCRIPTION","**shelly** is the command-line interface for Shelly (Shelly-ALPM), a modern unified package manager for Arch Linux. It installs, updates, searches, and removes packages across standard ALPM repositories, the AUR, Flatpak, and AppImages from one CLI.\n\nDirect subcommands cover the common lifecycle: **install**, **remove**, **upgrade**, **upgrade-all**, **query**, **update**, **downgrade**, **purify**, **sync**, **news**, and cache/keyring utilities. Grouped subcommands handle specialized sources: **shelly aur**, **shelly flatpak**, and **shelly appimage**. Configuration is managed with **shelly config**.\n\nGlobal options such as **--no-confirm** (alias **-n**), **--json** (**-j**), **--verbose** (**-v**), and **--ui-mode** (**-U**) apply across subcommands. Optional shortcodes of the form **-\\<Type\\>\\<Action\\>…** map type letters (for example **S** standard, **A** AUR, **F** Flatpak) to verbs for compact invocations.\n\nShelly also ships a separate GUI; the CLI binary is the scriptable entry point documented here. On CachyOS it may be installable as the **shelly** package from the distribution repositories."],["PARAMETERS","**install** [_packages…_]\n> Install packages from repositories, local files, or URLs.\n\n**remove** [_packages…_]\n> Remove packages. **--cascade** (**-c**) removes unneeded reverse dependencies; **--ripple** (**-i**) removes packages that depend on the target.\n\n**upgrade**\n> Perform a full system upgrade of standard packages.\n\n**upgrade-all**\n> Upgrade packages from every source (repo, AUR, Flatpak, AppImage). Use **--no-aur**, **--no-flatpak**, **--no-appimage**, or **--no-repo** to skip a source.\n\n**query** [_package_]\n> Query repositories and packages (installed and available by default). **--available** (**-a**), **--installed** (**-i**), **--detail**/**--info** (**-d**).\n\n**aur search** \\<query…\\>\n> Search the Arch User Repository.\n\n**aur install** [_packages…_]\n> Build and install AUR packages.\n\n**flatpak** … / **appimage** …\n> Manage Flatpak apps and AppImages respectively.\n\n**purify**\n> Remove corrupted packages; **--orphans** (**-o**) also removes orphaned dependencies.\n\n**config get|set|list|reset|parallel**\n> Read or change Shelly configuration values.\n\n**--no-confirm**, **-n**\n> Disable confirmation prompts.\n\n**--json**, **-j**\n> Emit JSON output for scripting.\n\n**--version**\n> Show version information."],["CAVEATS","Arch Linux (and derivatives with ALPM) only; not a general multi-distro package manager. AUR builds execute PKGBUILD scripts and need a working build toolchain. Privileged operations typically require root or a configured privilege helper. The GUI and CLI share branding with other unrelated \"Shelly\" projects (smart-home devices, SSH clients); this page documents the Arch package manager CLI from Seafoam Labs."],["INSTALL","```aur: yay -S shelly```"],["SEE ALSO","[pacman](/man/pacman)(8), [yay](/man/yay)(8), [paru](/man/paru)(8), [flatpak](/man/flatpak)(1)"],["RESOURCES","```[Source code](https://github.com/Seafoam-Labs/Shelly-ALPM)```\n\n```[Documentation](https://www.seafoam-labs.org/shelly-alpm/docs/cli-reference/)```"]],"e":["pacman","yay","paru","flatpak"],"x":"shelly upgrade-all"},"supercollider":{"n":"supercollider","t":"Audio synthesis programming language interpreter","s":[["TAGLINE","Audio synthesis programming language interpreter"],["TLDR","**Start the SuperCollider interpreter**\n\n```sclang```\n\n**Run a SuperCollider script**\n\n```sclang [script.scd]```\n\n**Start in daemon mode** (no input)\n\n```sclang -D```\n\n**Run Main.run on startup**\n\n```sclang -r [script.scd]```\n\n**Set initial heap size**\n\n```sclang -m [8m]```\n\n**Set UDP listening port**\n\n```sclang -u [57120]```\n\n**Set runtime directory**\n\n```sclang -d [/path/to/runtime]```"],["SYNOPSIS","**sclang** [_options_] [_file_]"],["PARAMETERS","**-d** _path_\n> Set runtime directory.\n\n**-D**\n> Enter daemon mode (no user input).\n\n**-g** _size_\n> Set heap growth size (default 256k). Suffix with k or m.\n\n**-h**\n> Display help information.\n\n**-l** _path_\n> Set library configuration file.\n\n**-m** _size_\n> Set initial heap size (default 2m). Suffix with k or m.\n\n**-r**\n> Call Main.run on startup.\n\n**-s**\n> Call Main.stop on shutdown.\n\n**-u** _port_\n> Set UDP listening port (default 57120)."],["DESCRIPTION","**sclang** is the SuperCollider programming language interpreter. SuperCollider is a platform for audio synthesis and algorithmic composition, used by musicians, artists, and researchers for real-time audio processing and sound design.\n\nThe interpreter acts as a client for **scsynth**, the SuperCollider audio synthesis server. It interprets SuperCollider code and sends OSC (Open Sound Control) messages to the synthesis server to control audio generation.\n\nSuperCollider scripts (.scd files) can be executed directly from the command line for headless audio processing or batch operations. The daemon mode allows running SuperCollider without a terminal for server deployments.\n\nOn Linux, sclang can run without a display server by setting the environment variable QT_QPA_PLATFORM=offscreen."],["CAVEATS","Requires scsynth audio server for audio synthesis. The SuperCollider IDE provides a more integrated development experience. Script files must be structured as single executable blocks when run from command line. Memory settings may need tuning for complex compositions."],["HISTORY","**SuperCollider** was developed by James McCartney starting in 1996. It was initially proprietary software for Macintosh but was released as free software under the GPL in 2002. The language has become a standard tool in the computer music community, used for live coding performances and academic research."],["INSTALL","```pacman: sudo pacman -S supercollider```\n\n```zypper: sudo zypper install supercollider```\n\n```nix: nix profile install nixpkgs#supercollider```"],["SEE ALSO","[csound](/man/csound)(1)"]],"e":["csound"],"x":"sclang"},"systemctl-cat":{"n":"systemctl-cat","t":"Display systemd unit file contents","s":[["TAGLINE","Display systemd unit file contents"],["TLDR","Show **unit file** contents\n\n```systemctl cat [unit]```\n\nShow **multiple** unit files\n\n```systemctl cat [unit1 unit2 ...]```\n\nShow **template** unit file\n\n```systemctl cat [template@]```\n\nShow **user** unit file\n\n```systemctl cat [unit] --user```"],["SYNOPSIS","**systemctl cat** _PATTERN_..."],["PARAMETERS","**--user**\n> Query the calling user's **systemd --user** manager instead of the system manager.\n\n**--system**\n> Query the system manager (default).\n\n**--no-pager**\n> Do not pipe output through a pager.\n\n**--root=**_PATH_\n> Read unit files from an alternate filesystem root (useful for inspecting a chroot or mounted image).\n\n**--image=**_PATH_\n> Read unit files from a disk image file at _PATH_.\n\n**-M**, **--machine=**_NAME_\n> Operate on the systemd manager inside the named container or VM.\n\n**-l**, **--full**\n> Do not ellipsize displayed paths."],["DESCRIPTION","**systemctl cat** displays the full contents of unit files as systemd sees them, including the main unit file and any drop-in override files. It shows the absolute path of each file before its contents.\n\nThis is useful for debugging unit configuration, especially when drop-in files in `.d/` directories modify the base unit."],["CAVEATS","Shows the raw file contents, not the effective configuration after parsing. Use `systemctl show` to see the actual runtime values. Template units should be specified with the trailing @ symbol."],["HISTORY","The **cat** subcommand is part of **systemctl** in the **systemd** project. It provides a convenient way to inspect unit configuration without manually locating files across multiple directories."],["INSTALL","```apt: sudo apt install systemd```\n\n```dnf: sudo dnf install systemd```\n\n```pacman: sudo pacman -S systemd```\n\n```zypper: sudo zypper install systemd```\n\n```brew: brew install systemd```\n\n```nix: nix profile install nixpkgs#systemd```"],["SEE ALSO","[systemctl](/man/systemctl)(1), [systemctl-edit](/man/systemctl-edit)(1), [systemctl-show](/man/systemctl-show)(1)"]],"e":["systemctl","systemctl-edit","systemctl-show"],"x":"systemctl cat [unit]"},"systemctl-halt":{"n":"systemctl-halt","t":"Shut down and halt the system","s":[["TAGLINE","Shut down and halt the system"],["TLDR","**Halt** the system\n\n```systemctl halt```\n\n**Force** halt immediately\n\n```systemctl halt -f```\n\nHalt without **wall** message\n\n```systemctl halt -f --no-wall```\n\n**Emergency** halt (dangerous)\n\n```systemctl halt -ff```\n\n**Schedule** halt at time\n\n```systemctl halt --when 23:00```\n\nSchedule halt in **duration**\n\n```systemctl halt --when +2h```\n\n**Cancel** scheduled halt\n\n```systemctl halt --when cancel```"],["SYNOPSIS","**systemctl halt** [_OPTIONS_]"],["PARAMETERS","**-f, --force**\n> Skip graceful service shutdown\n\n**--force --force** (-ff)\n> Immediate halt without cleanup (dangerous)\n\n**--no-wall**\n> Don't send wall message to users\n\n**--when** _TIME_\n> Schedule halt at specific time or offset"],["DESCRIPTION","**systemctl halt** shuts down and halts the system, stopping the OS kernel but leaving hardware powered on. This differs from poweroff, which completely powers down the machine, and reboot, which restarts the system.\n\nWith one `--force`, services are not gracefully stopped. With two `--force` flags, filesystems are not unmounted and may cause data loss."],["CAVEATS","Halt leaves the system powered but unresponsive; manual power-off is required. Double-force mode risks data corruption. Scheduled operations can be cancelled with `--when cancel`."],["HISTORY","The **halt** subcommand provides a systemd-native way to halt the system, integrating with the scheduled shutdown mechanism and wall notification system."],["SEE ALSO","[systemctl-poweroff](/man/systemctl-poweroff)(1), [systemctl-reboot](/man/systemctl-reboot)(1), [halt](/man/halt)(8)"]],"e":["systemctl-poweroff","systemctl-reboot","halt"],"x":"systemctl halt"},"systemctl-hibernate":{"n":"systemctl-hibernate","t":"Hibernate system to disk","s":[["TAGLINE","Hibernate system to disk"],["TLDR","**Hibernate** the system immediately\n\n```systemctl hibernate```\n\n**Force** hibernation, ignoring inhibitors\n\n```systemctl hibernate --force```\n\nHibernate without sending a **wall message** to logged-in users\n\n```systemctl hibernate --no-wall```"],["SYNOPSIS","**systemctl hibernate** [_OPTIONS_]"],["DESCRIPTION","**systemctl hibernate** hibernates the system by saving the current state to disk (swap) and powering off. When the system is powered on again, it resumes from where it left off."],["PARAMETERS","**-f, --force**\n> Force hibernation even if inhibitors are present (programs blocking sleep). When specified twice, the operation is executed without contacting the login manager.\n\n**--no-wall**\n> Do not send wall message to logged-in users before hibernating\n\n**-i, --ignore-inhibitors**\n> Ignore inhibitor locks when performing the hibernate operation"],["CAVEATS","Requires sufficient swap space to hold the contents of RAM. The system must support hibernation at the hardware level. Some systems may need additional kernel parameters for hibernation to work properly."],["HISTORY","**systemctl hibernate** is part of **systemd**, the system and service manager for Linux, providing modern power management capabilities."],["SEE ALSO","[systemctl](/man/systemctl)(1), [systemctl-suspend](/man/systemctl-suspend)(1), [systemctl-hybrid-sleep](/man/systemctl-hybrid-sleep)(1), [systemctl-suspend-then-hibernate](/man/systemctl-suspend-then-hibernate)(1), [systemctl-poweroff](/man/systemctl-poweroff)(1)"]],"e":["systemctl","systemctl-suspend","systemctl-hybrid-sleep","systemctl-suspend-then-hibernate","systemctl-poweroff"],"x":"systemctl hibernate"},"systemctl-kill":{"n":"systemctl-kill","t":"Send signals to unit processes","s":[["TAGLINE","Send signals to unit processes"],["TLDR","Send **SIGTERM** to unit\n\n```systemctl kill [unit]```\n\nSend **specific signal**\n\n```systemctl kill -s [SIGNAL] [unit]```\n\nSend signal to **main process** only\n\n```systemctl kill --kill-whom=main -s SIGHUP [unit]```\n\nKill processes of a **user** service\n\n```systemctl --user kill [unit]```"],["SYNOPSIS","**systemctl kill** [_OPTIONS_] _PATTERN_..."],["PARAMETERS","**-s, --signal=** _SIGNAL_\n> Signal to send (name or number, default: SIGTERM)\n\n**--kill-whom=** _WHO_\n> Which processes to kill: main, control, or all (default: all). Older systemd versions use `--kill-who`.\n\n**--user**\n> Kill user service processes"],["DESCRIPTION","**systemctl kill** sends a signal to one or more processes of a unit. By default, it sends SIGTERM to all processes in the unit's cgroup. This provides more control than `systemctl stop`, which follows the unit's configured stop behavior.\n\nThe `--kill-whom` option controls which processes receive the signal: main (the main process), control (control processes), or all (every process in the cgroup)."],["CAVEATS","Bypasses the unit's configured ExecStop commands. Killing main process may leave child processes orphaned depending on unit configuration. SIGKILL cannot be caught or ignored by processes."],["HISTORY","The **kill** subcommand provides direct signal delivery to unit processes, useful when the normal stop procedure is insufficient or when specific signals are needed (like SIGHUP for config reload)."],["INSTALL","```apt: sudo apt install systemd```\n\n```dnf: sudo dnf install systemd```\n\n```pacman: sudo pacman -S systemd```\n\n```zypper: sudo zypper install systemd```\n\n```brew: brew install systemd```\n\n```nix: nix profile install nixpkgs#systemd```"],["SEE ALSO","[systemctl](/man/systemctl)(1), [systemctl-stop](/man/systemctl-stop)(1), [systemctl-restart](/man/systemctl-restart)(1), [kill](/man/kill)(1)"]],"e":["systemctl","systemctl-stop","systemctl-restart","kill"],"x":"systemctl kill [unit]"},"tkst":{"n":"tkst","t":"Track and budget Claude Code token spending from the command line","s":[["TAGLINE","Track and budget Claude Code token spending from the command line"],["TLDR","Create a **task with a budget cap**\n\n```tkst -t \"[Issue-243]\" -b [5]```\n\nLaunch **Claude Code with cost tracking** enabled\n\n```tkst claude```\n\n**List all tasks** and their current spending\n\n```tkst -l```\n\n**Update** a task's budget or name\n\n```tkst -u \"[Issue-243]\" -b [10]```\n\n**Delete** a task\n\n```tkst -d \"[Issue-243]\"```\n\n**Reallocate spend** from one task to another\n\n```tkst -ra \"[old-task]\" \"[new-task]\"```\n\nToggle tracking **on or off**\n\n```tkst --enable``` or ```tkst --disable```"],["SYNOPSIS","**tkst** [_options_] [**claude** [_claude-args_]]"],["DESCRIPTION","**tkst** (Tokenyst) is a small CLI that wraps the **claude** command and keeps a running ledger of token usage, computed dollar cost, and budget consumption per task. After each prompt during a session it prints a per-turn delta and the cumulative spend against the active budget, so you can stop before exceeding a cap. Tasks are user-defined buckets — typically one per ticket, feature, or experiment — that hold a budget, a list of recorded sessions, and an aggregated total.\n\nAll state is stored locally under **~/.tokenyst** with no cloud sync. Pricing for the supported Claude model variants is bundled with the binary and applied to the token counts reported by the **claude** CLI."],["PARAMETERS","**claude** [_args_]\n> Launch the Claude CLI under tracking. Arguments after **claude** are forwarded verbatim.\n\n**-t** _name_, **--task** _name_\n> Create a new task with the given name.\n\n**-b** _amount_, **--budget** _amount_\n> Set the budget cap (USD) for the task, used with **-t** or **-u**.\n\n**-l**, **--list**\n> List every task with its budget, total spend, and recent sessions.\n\n**-u** _name_, **--update** _name_\n> Update an existing task; combine with **-b** to change the budget or **--rename** to change the name.\n\n**-d** _name_, **--delete** _name_\n> Delete the named task.\n\n**-a**, **--allocations**\n> Show per-session and per-task spend allocations.\n\n**-ra** _from_ _to_, **--reallocate** _from_ _to_\n> Move recorded spend from one task to another (useful after misattributed sessions).\n\n**--enable**\n> Re-enable tracking after it was disabled.\n\n**--disable**\n> Suspend tracking; the **claude** wrapper still passes calls through."],["CONFIGURATION","**~/.tokenyst/**\n> Local data directory holding the tasks, session logs, and computed totals.\n\n**~/.tokenyst/config.json**\n> User-tunable settings such as default model pricing and currency."],["CAVEATS","Cost figures depend on the **bundled price list**; if Anthropic changes pricing the values are estimates until the binary is updated. Token counts are read from the **claude** CLI output, so changes to that interface can disrupt tracking. State lives locally and is **not synchronised** between machines — pair with version control or a synced home directory if you want shared budgets."],["HISTORY","**Tokenyst** was published by **jher7** in **2026** in response to the proliferation of multi-agent Claude Code workflows, where per-prompt spend was previously invisible until end-of-month billing. It is distributed as an **npm**/**pnpm** package installed globally and invoked as **tkst**."],["SEE ALSO","[claude](/man/claude)(1), [jq](/man/jq)(1), [npm](/man/npm)(1)"]],"e":["claude","jq","npm"],"x":"tkst -t \"[Issue-243]\" -b [5]"},"vgremove":{"n":"vgremove","t":"Remove LVM volume groups","s":[["TAGLINE","Remove LVM volume groups"],["TLDR","**Remove** a volume group with confirmation\n\n```sudo vgremove [volume_group]```\n\n**Forcefully remove** a volume group without confirmation\n\n```sudo vgremove -f [volume_group]```\n\n**Remove** with debug logging\n\n```sudo vgremove -d -d [volume_group]```\n\n**Remove** a volume group matching a select criteria\n\n```sudo vgremove --select '[vg_name=~old_*]'```"],["SYNOPSIS","**vgremove** [_options_] _volume_group_..."],["PARAMETERS","**-f, --force**\n> Force removal without confirmation\n\n**-d, --debug**\n> Enable debug output (repeat for more detail, up to 6 times)\n\n**-S, --select** _string_\n> Select objects for processing based on specified criteria\n\n**-t, --test**\n> Test mode, don't actually make changes\n\n**-v, --verbose**\n> Verbose output\n\n**-y, --yes**\n> Answer yes to all prompts"],["DESCRIPTION","**vgremove** removes one or more LVM volume groups. Before a volume group can be removed, all logical volumes within it must first be removed using **lvremove**.\n\nThe command updates the LVM metadata to mark the volume group as removed. The underlying physical volumes remain but are no longer associated with any volume group."],["CAVEATS","Requires root privileges. All logical volumes must be removed first. Cannot remove an active volume group with mounted filesystems. Data on the volume group will be inaccessible after removal. Part of the LVM2 package."],["INSTALL","```apt: sudo apt install lvm2```\n\n```dnf: sudo dnf install lvm2```\n\n```pacman: sudo pacman -S lvm2```\n\n```apk: sudo apk add lvm2```\n\n```zypper: sudo zypper install lvm2```\n\n```nix: nix profile install nixpkgs#lvm2```"],["SEE ALSO","[lvm](/man/lvm)(8), [vgcreate](/man/vgcreate)(8), [lvremove](/man/lvremove)(8), [pvremove](/man/pvremove)(8)"]],"e":["lvm","vgcreate","lvremove","pvremove"],"x":"sudo vgremove [volume_group]"},"xrdb":{"n":"xrdb","t":"Manage X server resource database","s":[["TAGLINE","Manage X server resource database"],["TLDR","**Start** xrdb in interactive mode\n\n```xrdb```\n\n**Load** values from a resource file\n\n```xrdb -load [~/.Xresources]```\n\n**Query** the resource database and print current values\n\n```xrdb -query```"],["SYNOPSIS","**xrdb** [_options_] [_filename_]"],["PARAMETERS","**-load**\n> Load resources from file, replacing existing values\n\n**-merge**\n> Merge resources from file with existing values\n\n**-query**\n> Display current resources\n\n**-remove**\n> Remove all resources\n\n**-symbols**\n> Print preprocessor symbols\n\n**-cpp _path_**\n> Specify preprocessor to use\n\n**-nocpp**\n> Don't preprocess input\n\n**-display _display_**\n> Specify X display"],["DESCRIPTION","**xrdb** (X Resource DataBase) manages the X server resource database. X resources configure the appearance and behavior of X applications, including colors, fonts, and other settings.\n\nResources are typically loaded from **~/.Xresources** or **~/.Xdefaults** at X session startup. The **-merge** option adds new resources without removing existing ones."],["CAVEATS","Changes only affect newly started applications. Use **-merge** to preserve existing settings when adding new ones. The file is processed by cpp by default, allowing use of #include and #define. Part of the X.Org utilities."],["HISTORY","**xrdb** has been part of the X Window System since the early releases in the mid-**1980s**, providing a way to configure application appearance and behavior before modern desktop environments existed."],["INSTALL","```apt: sudo apt install x11-xserver-utils```\n\n```dnf: sudo dnf install xrdb```\n\n```apk: sudo apk add xrdb```\n\n```zypper: sudo zypper install xrdb```\n\n```brew: brew install xrdb```\n\n```nix: nix profile install nixpkgs#xrdb```"],["SEE ALSO","[xset](/man/xset)(1)"]],"e":["xset"],"x":"xrdb"},"xvfb-run":{"n":"xvfb-run","t":"Run commands in virtual X server","s":[["TAGLINE","Run commands in virtual X server"],["TLDR","**Run a command in a virtual X server**\n\n```xvfb-run [command]```\n\n**Auto-select a free server number**\n\n```xvfb-run -a [command]```\n\n**Pass server arguments for screen resolution and depth**\n\n```xvfb-run -s \"-screen 0 1024x768x24\" [command]```\n\n**Log Xvfb errors to a file**\n\n```xvfb-run -e [/tmp/xvfb.log] [command]```\n\n**Use a specific server number**\n\n```xvfb-run -n [44] [command]```"],["SYNOPSIS","**xvfb-run** [_OPTIONS_] _COMMAND_"],["PARAMETERS","**-a, --auto-servernum**\n> Try to get a free server number, starting at 99 or the argument to --server-num\n\n**-s, --server-args** _ARGS_\n> Arguments passed to Xvfb server (default: \"-screen 0 640x480x8\")\n\n**-n, --server-num** _NUM_\n> Use specified server number (default: 99)\n\n**-e, --error-file** _FILE_\n> Store output from xauth and Xvfb in file (default: /dev/null)\n\n**-f, --auth-file** _FILE_\n> Use specified X authority file\n\n**-l, --listen-tcp**\n> Enable TCP port listening in the X server (disabled by default for security)\n\n**-p, --xauth-protocol** _PROTO_\n> X authority protocol to use (default: MIT-MAGIC-COOKIE-1)\n\n**-w, --wait** _DELAY_\n> Wait delay seconds after launching Xvfb before starting the command (default: 3)\n\n**-h, --help**\n> Display usage information and exit"],["DESCRIPTION","**xvfb-run** runs a command in a virtual X server environment using Xvfb (X Virtual Frame Buffer). This allows running graphical applications on systems without a display.\n\nThe tool is useful for automated testing, CI/CD pipelines, and headless server environments."],["CAVEATS","Xvfb must be installed. Some applications may behave differently in virtual X. Performance may vary for GPU-intensive applications."],["INSTALL","```apt: sudo apt install xvfb```\n\n```apk: sudo apk add xvfb-run```\n\n```zypper: sudo zypper install xvfb-run```\n\n```nix: nix profile install nixpkgs#xvfb```"],["SEE ALSO","[xinit](/man/xinit)(1), [xauth](/man/xauth)(1)"]],"e":["xinit","xauth"],"x":"xvfb-run [command]"},"yapf":{"n":"yapf","t":"Google Python code formatter","s":[["TAGLINE","Google Python code formatter"],["TLDR","**Format a Python file** and print to stdout\n\n```yapf [file.py]```\n\n**Format a file in place**\n\n```yapf -i [file.py]```\n\n**Show diff** of formatting changes\n\n```yapf -d [file.py]```\n\n**Format using Google style**\n\n```yapf --style=google [file.py]```\n\n**Format all Python files** recursively\n\n```yapf -r -i [directory/]```\n\n**Format in parallel** for faster processing\n\n```yapf -p -r -i [directory/]```\n\n**Show available style options**\n\n```yapf --style-help```\n\n**Use custom style configuration**\n\n```yapf --style='{based_on_style: pep8, indent_width: 4}' [file.py]```"],["SYNOPSIS","**yapf** [_options_] [_files_...]"],["PARAMETERS","**-i**, **--in-place**\n> Modify files in place\n\n**-d**, **--diff**\n> Print diff of changes without modifying files\n\n**-r**, **--recursive**\n> Recursively format files in directories\n\n**-p**, **--parallel**\n> Run formatting in parallel for multiple files\n\n**-s**, **--style** _style_\n> Style: pep8, google, chromium, facebook, or path to config file\n\n**--style-help**\n> Show documentation for all style options\n\n**-l**, **--lines** _range_\n> Format only specified lines (e.g., 1-10,15-20)\n\n**-e**, **--exclude** _pattern_\n> Exclude files matching pattern\n\n**--version**\n> Show version and exit"],["DESCRIPTION","**yapf** (Yet Another Python Formatter) is a Python code formatter developed by Google. Unlike linters that only warn about style violations, yapf reformats code to match the configured style, producing consistent output regardless of the original formatting.\n\nThe algorithm analyzes code structure and calculates optimal formatting based on the configured style. Built-in styles include **pep8** (default), **google**, **chromium**, and **facebook**. Custom styles can be defined by inheriting from a base style and overriding specific options.\n\nConfiguration files (**.style.yapf**, **setup.cfg**, or **pyproject.toml**) in the source directory or parent directories are automatically detected. The config file uses INI format with a **[style]** section.\n\nyapf can format specific line ranges with **--lines**, useful for formatting only changed code in version control workflows."],["CAVEATS","yapf may produce different output than other formatters like black. The **-i** option modifies files directly; use **-d** first to preview changes. Some complex expressions may be formatted in unexpected ways. Comments can affect formatting decisions."],["HISTORY","**yapf** was developed at **Google** and first released in **2015** as an open-source project. It was inspired by **clang-format** (for C/C++), applying similar algorithmic formatting to Python. The name follows the Unix tradition of \"Yet Another\" tools, acknowledging the existence of other Python formatters."],["INSTALL","```pacman: sudo pacman -S yapf```\n\n```apk: sudo apk add py3-yapf```\n\n```brew: brew install yapf```\n\n```nix: nix profile install nixpkgs#yapf```"],["SEE ALSO","[black](/man/black)(1), [autopep8](/man/autopep8)(1), [isort](/man/isort)(1), [flake8](/man/flake8)(1)"]],"e":["black","autopep8","isort","flake8"],"x":"yapf [file.py]"},"yj":{"n":"yj","t":"Convert between YAML, JSON, TOML, and HCL","s":[["TAGLINE","Convert between YAML, JSON, TOML, and HCL"],["TLDR","Convert a **YAML file to JSON**\n\n```cat [file.yaml] | yj```\n\nConvert a **YAML file to TOML**\n\n```cat [file.yaml] | yj -yt```\n\nConvert a **JSON file to YAML**\n\n```cat [file.json] | yj -jy```\n\nConvert a **TOML file to JSON** with **indented output**\n\n```cat [file.toml] | yj -ti```\n\nConvert an **HCL file to JSON**\n\n```cat [file.hcl] | yj -c```\n\nConvert a **JSON file to TOML** with **indented output**\n\n```cat [file.json] | yj -jti```"],["SYNOPSIS","**yj** [**-**][_ytjcrneikhv_]"],["PARAMETERS","**-yj**, **-y**\n> Convert YAML to JSON (default)\n\n**-yy**\n> Convert YAML to YAML\n\n**-yt**\n> Convert YAML to TOML\n\n**-yc**\n> Convert YAML to HCL\n\n**-jy**, **-r**\n> Convert JSON to YAML\n\n**-jj**\n> Convert JSON to JSON\n\n**-jt**\n> Convert JSON to TOML\n\n**-jc**\n> Convert JSON to HCL\n\n**-tj**, **-t**\n> Convert TOML to JSON\n\n**-ty**\n> Convert TOML to YAML\n\n**-tt**\n> Convert TOML to TOML\n\n**-tc**\n> Convert TOML to HCL\n\n**-cj**, **-c**\n> Convert HCL to JSON\n\n**-cy**\n> Convert HCL to YAML\n\n**-ct**\n> Convert HCL to TOML\n\n**-cc**\n> Convert HCL to HCL\n\n**-n**\n> Do not convert inf, -inf, and NaN to/from strings (YAML or TOML only)\n\n**-e**\n> Escape HTML characters in output (JSON only)\n\n**-i**\n> Indent output (JSON or TOML only)\n\n**-k**\n> Attempt to parse keys as objects or numeric types (YAML output only)\n\n**-h**\n> Show help\n\n**-v**\n> Show version"],["DESCRIPTION","**yj** is a lightweight command-line tool for converting between **YAML**, **JSON**, **TOML**, and **HCL** serialization formats. It reads from **stdin** and writes to **stdout**, making it easy to compose with other Unix tools via piping.\n\nA key feature of yj is that it **preserves map key order** during conversion, which many similar tools do not. The default behavior converts YAML to JSON. Conversion direction is specified by combining format letters: the first letter is the input format and the second is the output format (e.g. **-yt** for YAML to TOML).\n\nSupported format versions are YAML v1.2, TOML v1.0.0, JSON RFC 7159, and HCL v1."],["CAVEATS","Only **HCL v1** is supported. Modern Terraform files using HCL2 syntax may not parse correctly. The tool reads exclusively from stdin and writes to stdout; there are no flags for specifying input or output files directly. JSON and TOML output is compact by default and requires **-i** for human-readable indented output."],["HISTORY","**yj** was created by **Stephen Levine** and first released in **March 2017**. It is written in **Go** and is available on GitHub at sclevine/yj. The tool is installable via Homebrew and as pre-built binaries for Linux, macOS, and Windows. It is licensed under the Apache License 2.0."],["INSTALL","```apk: sudo apk add yj```\n\n```brew: brew install yj```\n\n```nix: nix profile install nixpkgs#yj```"],["SEE ALSO","[jq](/man/jq)(1), [yq](/man/yq)(1), [python](/man/python)(1)"]],"e":["jq","yq","python"],"x":"cat [file.yaml] | yj"},"zathura":{"n":"zathura","t":"Vim-like modal document viewer","s":[["TAGLINE","Vim-like modal document viewer"],["TLDR","**Open** a document\n\n```zathura [path/to/file]```\n\nOpen a document at a specific **page**\n\n```zathura -P [page_number] [path/to/file]```\n\nOpen and **fork** into the background\n\n```zathura --fork [path/to/file]```\n\nOpen with a **password-protected** document\n\n```zathura --password [password] [path/to/file]```"],["SYNOPSIS","**zathura** [_OPTIONS_] [_FILE_...]"],["PARAMETERS","**-P**, **--page** _NUM_\n> Open document at given page number (starting from 1, negative from end).\n\n**--fork**\n> Fork into the background.\n\n**--password** _PASSWORD_\n> Provide the document password.\n\n**-e** _XID_, **--reparent** _XID_\n> Reparent to window specified by XID.\n\n**h/j/k/l or Arrow keys**\n> Navigate left/up/down/right.\n\n**J/K or PgDn/PgUp**\n> Go to next/previous page.\n\n**r**\n> Rotate document.\n\n**Ctrl+r**\n> Invert colors (recolor mode).\n\n**/**\n> Search for text.\n\n**n/N**\n> Search next/previous result.\n\n**d**\n> Toggle dual page view.\n\n**F5**\n> Switch to presentation mode.\n\n**F11**\n> Toggle fullscreen.\n\n**:bmark** _NAME_\n> Create bookmark.\n\n**:bdelete** _NAME_\n> Delete bookmark.\n\n**:blist**\n> List bookmarks.\n\n**+, -**\n> Zoom in/out.\n\n**a**\n> Fit to width.\n\n**s**\n> Fit to page."],["DESCRIPTION","**zathura** is a Vim-like modal document viewer. It supports PDF, PostScript, DjVu, and other formats through backends.\n\nThe viewer features keyboard-driven navigation, bookmarks, and an integrated command line."],["CAVEATS","Requires backend plugins (poppler or mupdf for PDF, etc.). Configuration via ~/.config/zathura/zathurarc. Vim-style keybindings may have a learning curve."],["INSTALL","```apt: sudo apt install zathura```\n\n```dnf: sudo dnf install zathura```\n\n```pacman: sudo pacman -S zathura```\n\n```apk: sudo apk add zathura```\n\n```zypper: sudo zypper install zathura```\n\n```nix: nix profile install nixpkgs#zathura```"],["SEE ALSO","[evince](/man/evince)(1), [okular](/man/okular)(1), [mupdf](/man/mupdf)(1)"]],"e":["evince","okular","mupdf"],"x":"zathura [path/to/file]"}});
