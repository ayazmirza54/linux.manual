window.LCL_BUCKET(7,{"apptainer-shell":{"n":"apptainer-shell","t":"Open an interactive shell inside a container","s":[["TAGLINE","Open an interactive shell inside a container"],["TLDR","**Start an interactive shell** inside a container\n\n```apptainer shell [container.sif]```\n\n**Open shell with bound directory**\n\n```apptainer shell --bind [/host/path]:[/container/path] [container.sif]```\n\n**Shell into a Docker Hub image**\n\n```apptainer shell docker://[ubuntu:latest]```\n\n**Start shell with NVIDIA GPU support**\n\n```apptainer shell --nv [container.sif]```\n\n**Open shell with clean environment**\n\n```apptainer shell --cleanenv [container.sif]```\n\n**Shell with full containment** (isolated from host)\n\n```apptainer shell --containall [container.sif]```\n\n**Start shell with writable overlay**\n\n```apptainer shell --overlay [overlay.img] [container.sif]```\n\n**Shell with fakeroot privileges**\n\n```apptainer shell --fakeroot [container.sif]```"],["SYNOPSIS","**apptainer shell** [_options_] _container_"],["PARAMETERS","**--bind**, **-B** _src[:dest[:opts]]_\n> Bind mount a host path into the container.\n\n**--overlay**, **-o** _image_\n> Use an overlay image for a writable layer.\n\n**--writable**, **-w**\n> Make the container filesystem read-write (default is read-only).\n\n**--writable-tmpfs**\n> Add a writable tmpfs overlay; changes are discarded on exit.\n\n**--nv**\n> Enable NVIDIA GPU support.\n\n**--rocm**\n> Enable AMD ROCm GPU support.\n\n**--contain**, **-c**\n> Use minimal /dev and empty other directories.\n\n**--containall**, **-C**\n> Full containment of filesystems, PID, IPC, and environment.\n\n**--cleanenv**, **-e**\n> Clean environment before starting shell.\n\n**--env** _VAR=value_\n> Set an environment variable in the container.\n\n**--home**, **-H** _path_\n> Set a custom home directory.\n\n**--pwd** _path_\n> Initial working directory in the container.\n\n**--fakeroot**, **-f**\n> Run with apparent root privileges without host root.\n\n**--net**, **-n**\n> Run in a new network namespace.\n\n**--shell**, **-s** _path_\n> Specify the shell to use (default: /bin/sh).\n\n**--no-home**\n> Do not bind the home directory."],["DESCRIPTION","**apptainer shell** starts an interactive shell session inside an Apptainer container. This provides a command-line environment within the container for exploration, debugging, and interactive work.\n\nBy default, the shell inherits your user identity, home directory, and current working directory. The host environment is largely preserved unless containment options are used. This makes the shell feel familiar while providing access to the container's filesystem and installed software.\n\nThe container can be specified as a local SIF file, a Docker reference (docker://), a library reference (library://), or an OCI archive. The default shell is **/bin/sh** but can be overridden with the **--shell** option if the container has other shells available.\n\nFor development work, combine **--overlay** or **--writable-tmpfs** to allow temporary modifications within the container. Use **--fakeroot** when operations require apparent root privileges without actual host root access."],["CAVEATS","Shell availability depends on what's installed in the container. Some minimal containers may only have /bin/sh. Environment leakage from host to container can cause unexpected behavior; use **--cleanenv** for reproducibility. Changes made during shell session are lost unless using writable overlays or bind mounts."],["HISTORY","The shell command has been a core Singularity/Apptainer feature since the project's creation at Lawrence Berkeley National Laboratory in **2015**. It was designed to give users familiar interactive access to containers, supporting the HPC use case where users need to test and debug within container environments. Apptainer maintained full compatibility with this command when forking from Singularity in **2021**."],["INSTALL","```dnf: sudo dnf install apptainer```\n\n```pacman: sudo pacman -S apptainer```\n\n```apk: sudo apk add apptainer```\n\n```zypper: sudo zypper install apptainer```\n\n```brew: brew install apptainer```\n\n```nix: nix profile install nixpkgs#apptainer```"],["SEE ALSO","[apptainer](/man/apptainer)(1), [apptainer-exec](/man/apptainer-exec)(1), [apptainer-run](/man/apptainer-run)(1), [apptainer-build](/man/apptainer-build)(1)"],["RESOURCES","```[Source code](https://github.com/apptainer/apptainer)```\n\n```[Homepage](https://apptainer.org)```\n\n```[Documentation](https://apptainer.org/docs/)```"]],"e":["apptainer","apptainer-exec","apptainer-run","apptainer-build"],"x":"apptainer shell [container.sif]"},"autoreconf":{"n":"autoreconf","t":"Regenerate autotools build system files","s":[["TAGLINE","Regenerate autotools build system files"],["TLDR","**Regenerate** all autotools files\n\n```autoreconf```\n\nRegenerate with **install** of missing files\n\n```autoreconf --install```\n\n**Force** regeneration\n\n```autoreconf --force --install```\n\nRegenerate **verbosely**\n\n```autoreconf --install --verbose```"],["SYNOPSIS","**autoreconf** [_-f_] [_-i_] [_-v_] [_options_]"],["DESCRIPTION","**autoreconf** runs autoconf, autoheader, automake, and related tools in the correct order. It's the recommended way to regenerate configure scripts and related files in autotools projects.\n\nThe tool automatically determines which programs to run based on project configuration."],["PARAMETERS","**-i**, **--install**\n> Add missing auxiliary files\n\n**-f**, **--force**\n> Force regeneration even if files are current\n\n**-v**, **--verbose**\n> Verbose output\n\n**-s**, **--symlink**\n> When used with --install, install symbolic links instead of copying auxiliary files\n\n**-I** _dir_, **--include=**_dir_\n> Append dir to the include path searched by aclocal, autoconf, and autoheader\n\n**-m**, **--make**\n> When applicable, run ./config.status --recheck and ./config.status, then make to update everything\n\n**-W** _category_, **--warnings=**_category_\n> Report the warnings in category (all, none, error, or a specific category)\n\n**-d**, **--debug**\n> Don't remove the temporary files\n\n**--no-recursive**\n> Don't rebuild files in subdirectories (configured packages)"],["WORKFLOW","Typical usage when building from git:\n```bash\nautoreconf --install\n./configure\nmake\n```"],["CAVEATS","Requires autoconf, automake, and related tools installed. Can be slow on large projects. Modern alternatives (CMake, Meson) are often easier."],["HISTORY","**autoreconf** was added to autoconf to simplify the complex process of regenerating autotools files in the correct order."],["INSTALL","```dnf: sudo dnf install autoconf```\n\n```pacman: sudo pacman -S autoconf```\n\n```apk: sudo apk add autoconf```\n\n```zypper: sudo zypper install autoconf```\n\n```brew: brew install autoconf```\n\n```nix: nix profile install nixpkgs#autoconf```"],["SEE ALSO","[autoconf](/man/autoconf)(1), [automake](/man/automake)(1), [libtoolize](/man/libtoolize)(1)"],["RESOURCES","```[Homepage](https://www.gnu.org/software/autoconf/)```\n\n```[Documentation](https://www.gnu.org/software/autoconf/manual/)```"]],"e":["autoconf","automake","libtoolize"],"x":"autoreconf"},"bison":{"n":"bison","t":"Generate parsers from grammar descriptions","s":[["TAGLINE","Generate parsers from grammar descriptions"],["TLDR","**Generate** parser\n\n```bison [grammar.y]```\n\nGenerate with **verbose output**\n\n```bison -v [grammar.y]```\n\n**Specify** output file\n\n```bison -o [parser.c] [grammar.y]```\n\nGenerate **header** file\n\n```bison -d [grammar.y]```"],["SYNOPSIS","**bison** [_options_] _file_"],["DESCRIPTION","**bison** is a general-purpose parser generator that converts grammar descriptions (in .y files) into C, C++, or Java parsers. It's compatible with yacc and used for building compilers, interpreters, and other language processors.\n\nThe tool is a GNU replacement for yacc with additional features and better error handling."],["PARAMETERS","**-o**, **--output=**_file_\n> Output file name\n\n**-d**, **--defines**\n> Generate a header file with token definitions (also spelled **--header**)\n\n**-H**, **--header**[**=**_file_]\n> Generate the token-definitions header, optionally at a given path\n\n**-v**, **--verbose**\n> Create .output file with parser states\n\n**-t**, **--debug**\n> Enable debug output in parser\n\n**-g**, **--graph**\n> Generate VCG graph of parser\n\n**-r**, **--report=**_things_\n> Generate report (state, itemset, lookahead)\n\n**-W**, **--warnings**\n> Enable warnings\n\n**-l**, **--no-lines**\n> Don't generate #line directives\n\n**-L**, **--language=**_lang_\n> Target output language: c, c++, or java\n\n**-y**, **--yacc**\n> Emulate POSIX yacc (default output names y.tab.c, y.tab.h)"],["GRAMMAR FILE FORMAT","```yacc\n%{\n#include <stdio.h>\n%}\n\n%token NUMBER\n%%\n\nexpr: NUMBER '+' NUMBER { $$ = $1 + $3; }\n    ;\n\n%%\n```"],["FEATURES","- LALR(1) parser generation\n- GLR parsing support\n- C++, Java output\n- Location tracking\n- Error recovery\n- Precedence declarations\n- Semantic actions"],["WORKFLOW","```bash"],["Generate parser","bison -d calculator.y"],["Compile with flex lexer","flex lexer.l\ngcc lex.yy.c calculator.tab.c -o calculator"],["View parser details","bison -v grammar.y\ncat grammar.output\n```"],["CAVEATS","Learning curve for grammar syntax. Shift/reduce and reduce/reduce conflicts possible. Generated code can be large. Error messages sometimes cryptic. Usually paired with flex for lexical analysis."],["HISTORY","**Bison** was written by Robert Corbett in **1985** as a free replacement for Unix yacc. Richard Stallman made it yacc-compatible, and it became part of the GNU Project. It is now maintained by Akim Demaille and others."],["INSTALL","```apt: sudo apt install bison```\n\n```dnf: sudo dnf install bison```\n\n```pacman: sudo pacman -S bison```\n\n```apk: sudo apk add bison```\n\n```zypper: sudo zypper install bison```\n\n```brew: brew install bison```\n\n```nix: nix profile install nixpkgs#bison```"],["SEE ALSO","[flex](/man/flex)(1), [yacc](/man/yacc)(1), [antlr](/man/antlr)(1)"],["RESOURCES","```[Homepage](https://www.gnu.org/software/bison/)```\n\n```[Documentation](https://www.gnu.org/software/bison/manual/)```"]],"e":["flex","yacc","antlr"],"x":"bison [grammar.y]"},"bttf":{"n":"bttf","t":"Date/time arithmetic, parsing, and formatting CLI","s":[["TAGLINE","Date/time arithmetic, parsing, and formatting CLI"],["TLDR","**Show** current time helpers / default output\n\n```bttf```\n\n**Get help** for subcommands\n\n```bttf --help```\n\n**Subcommand help**\n\n```bttf [command] --help```"],["SYNOPSIS","**bttf** [*command*] [*options*]"],["DESCRIPTION","**bttf** (formerly **biff**) is BurntSushi’s command-line toolkit for datetime parsing, formatting, arithmetic, and timezone-aware calculations. Designed as a script-friendly companion when **date** alone is awkward. See the upstream user guide for locale configuration tips."],["PARAMETERS","Commands cover parse/format/add/duration-style operations—the surface is intentionally broad. Use **bttf --help** and per-subcommand help as the source of truth for your version."],["CAVEATS","Locale support may need explicit configuration for some environments. Previously named **biff**; update scripts after the rename."],["INSTALL","```brew: brew install bttf```\n\n```nix: nix profile install nixpkgs#bttf```"],["SEE ALSO","[date](/man/date)(1), [timedatectl](/man/timedatectl)(1)"],["RESOURCES","```[Source code](https://github.com/BurntSushi/bttf)```"]],"e":["date","timedatectl"],"x":"bttf"},"bzip3":{"n":"bzip3","t":"High-performance file compressor with improved ratios over bzip2","s":[["TAGLINE","High-performance file compressor with improved ratios over bzip2"],["TLDR","**Compress** file\n\n```bzip3 [file.txt]```\n\n**Decompress** file\n\n```bzip3 -d [file.txt.bz3]```\n\n**Keep** original file\n\n```bzip3 -k [file.txt]```\n\n**Set** block size\n\n```bzip3 -b [256] [file.txt]```"],["SYNOPSIS","**bzip3** [_options_] [_file_...]"],["DESCRIPTION","**bzip3** is a compression tool offering better compression ratios than bzip2, gzip, and zstd while maintaining reasonable speed. It uses a more modern algorithm than bzip2 with improved performance.\n\nThe tool is backward incompatible with bzip2 but provides significantly better compression for most data types."],["PARAMETERS","**-d**, **--decompress**\n> Decompress file\n\n**-k**, **--keep**\n> Keep original files\n\n**-f**, **--force**\n> Overwrite existing files\n\n**-c**, **--stdout**\n> Write to standard output\n\n**-b**, **--block** _size_\n> Block size in MiB (1-511, default 16)\n\n**-j**, **--jobs** _n_\n> Number of threads\n\n**-v**, **--verbose**\n> Verbose mode"],["FEATURES","- Better compression than bzip2\n- Multi-threaded compression\n- Configurable block sizes\n- Fast decompression\n- Low memory usage\n- Modern algorithm"],["WORKFLOW","```bash"],["Compress file","bzip3 file.txt"],["Decompress","bzip3 -d file.txt.bz3"],["Compress keeping original","bzip3 -k file.txt"],["Multi-threaded compression","bzip3 -j 4 largefile.bin"],["Custom block size","bzip3 -b 128 file.txt\n```"],["COMPARISON","Typical compression ratios:\n- **bzip3** - Best\n- **xz/lzma** - Excellent but slower\n- **bzip2** - Good\n- **gzip** - Moderate but fast\n- **lz4** - Fast but lower compression"],["CAVEATS","Not compatible with bzip2 (.bz2 files). Less widely supported than gzip/bzip2. Relatively new (may have bugs). Not installed by default on most systems. Some features require recent versions."],["HISTORY","**bzip3** was created by Kamila Szewczyk in **2022** as a modern successor to bzip2, using improved algorithms for better compression."],["INSTALL","```apt: sudo apt install bzip3```\n\n```dnf: sudo dnf install bzip3```\n\n```pacman: sudo pacman -S bzip3```\n\n```apk: sudo apk add bzip3```\n\n```zypper: sudo zypper install bzip3```\n\n```brew: brew install bzip3```\n\n```nix: nix profile install nixpkgs#bzip3```"],["SEE ALSO","[bzip2](/man/bzip2)(1), [xz](/man/xz)(1), [zstd](/man/zstd)(1)"],["RESOURCES","```[Source code](https://github.com/kspalaiologos/bzip3)```"]],"e":["bzip2","xz","zstd"],"x":"bzip3 [file.txt]"},"cargo-fix":{"n":"cargo-fix","t":"Automatically fix compiler warnings in Rust code","s":[["TAGLINE","Automatically fix compiler warnings in Rust code"],["TLDR","**Fix all warnings**\n\n```cargo fix```\n\n**Fix all targets**\n\n```cargo fix --all-targets```\n\n**Fix with all features enabled**\n\n```cargo fix --all-features```\n\n**Migrate to new edition**\n\n```cargo fix --edition```\n\n**Fix even with compiler errors**\n\n```cargo fix --broken-code```\n\n**Allow dirty working directory**\n\n```cargo fix --allow-dirty```\n\n**Allow uncommitted changes**\n\n```cargo fix --allow-staged```\n\n**Fix a specific package in a workspace**\n\n```cargo fix -p [package_name]```"],["SYNOPSIS","**cargo fix** [_options_]"],["DESCRIPTION","**cargo fix** automatically applies rustc's suggested fixes to source code. Runs **cargo check** internally and applies machine-applicable suggestions from diagnostics."],["PARAMETERS","**--edition**\n> Apply fixes for edition migration.\n\n**--edition-idioms**\n> Apply edition-specific idiom changes for the current edition.\n\n**--broken-code**\n> Fix code even with existing compiler errors. Leaves broken code for manual inspection.\n\n**--allow-dirty**\n> Allow fixes on a dirty working directory (including staged changes).\n\n**--allow-staged**\n> Allow fixes with staged changes.\n\n**--allow-no-vcs**\n> Allow fixes even if no version control system is detected.\n\n**--all-targets**\n> Fix all targets (lib, bins, tests, benches, examples).\n\n**--all-features**\n> Activate all available features.\n\n**--no-default-features**\n> Do not activate the default feature.\n\n**-F**, **--features** _features_\n> Space or comma-separated list of features to activate.\n\n**--lib**\n> Fix library only.\n\n**--bins**\n> Fix all binaries.\n\n**--tests**\n> Fix all test targets.\n\n**--benches**\n> Fix all benchmark targets.\n\n**--examples**\n> Fix all example targets.\n\n**-p**, **--package** _spec_\n> Fix specific packages.\n\n**--workspace**\n> Fix all workspace members.\n\n**--exclude** _spec_\n> Exclude specified packages (requires **--workspace**).\n\n**--target** _triple_\n> Fix for the specified target architecture.\n\n**-j**, **--jobs** _N_\n> Number of parallel jobs to run."],["EDITION MIGRATION","```cargo fix --edition```\n\nMigrates code from one Rust edition to the next (e.g., 2021 to 2024). After running, update the **edition** field in **Cargo.toml** manually."],["CAVEATS","Can only fix normally compiled code. Conditionally compiled code requires appropriate **--features** or **--target** flags. Built into Cargo since Rust 1.29. By default, requires a clean VCS state; use **--allow-dirty** or **--allow-staged** to override."],["INSTALL","```apt: sudo apt install cargo```\n\n```dnf: sudo dnf install cargo```\n\n```apk: sudo apk add cargo```\n\n```zypper: sudo zypper install cargo```\n\n```nix: nix profile install nixpkgs#cargo```"],["SEE ALSO","[cargo](/man/cargo)(1), [cargo-check](/man/cargo-check)(1), [cargo-clippy](/man/cargo-clippy)(1), [cargo-build](/man/cargo-build)(1)"]],"e":["cargo","cargo-check","cargo-clippy","cargo-build"],"x":"cargo fix"},"cavif":{"n":"cavif","t":"convert images to AVIF format","s":[["TAGLINE","convert images to AVIF format"],["TLDR","**Convert an image to AVIF format**\n\n```cavif [path/to/image.png]```\n\n**Convert with specific quality (1-100)**\n\n```cavif --quality [80] [path/to/image.jpg]```\n\n**Convert with faster encoding speed (1-10)**\n\n```cavif --speed [6] [path/to/image.png]```\n\n**Convert and specify output path**\n\n```cavif -o [path/to/output.avif] [path/to/image.png]```\n\n**Convert multiple images to a directory**\n\n```cavif -o [path/to/output_dir/] [image1.png] [image2.jpg]```\n\n**Overwrite existing AVIF files**\n\n```cavif --overwrite [path/to/image.png]```"],["SYNOPSIS","**cavif** [**--quality** _1-100_] [**--speed** _1-10_] [**-o** _path_] [**--overwrite**] [**--quiet**] _file..._"],["DESCRIPTION","**cavif** is a command-line tool written in Rust for converting PNG and JPEG images to AVIF (AV1 Image File Format). AVIF uses AV1 video codec compression, offering superior compression ratios compared to JPEG while maintaining image quality.\n\nBy default, output files are saved with the same name but with an **.avif** extension. Existing files are not overwritten unless **--overwrite** is specified."],["PARAMETERS","**--quality** _1-100_\n> Output quality level. Higher values mean better quality and larger files. Default: 80\n\n**--speed** _1-10_\n> Encoding speed. 1 is slowest with best compression, 10 is fastest but lower quality. Default: 4\n\n**-o** _path_\n> Output path. For multiple inputs, treated as a directory\n\n**--overwrite**\n> Replace existing .avif files instead of skipping them\n\n**--quiet**\n> Suppress output during conversion\n\n**--dirty-alpha**\n> Preserve RGB values of fully transparent pixels (increases file size)\n\n**--color=rgb**\n> Encode in RGB color space instead of YCbCr. Avoids color shifts but produces larger files\n\n**--depth=**_8_\n> Encode using 8-bit color depth instead of default 10-bit for wider compatibility"],["CAVEATS","Speeds 1-2 are extremely slow but produce ~3-5% smaller files. Speeds 7+ significantly degrade compression and are not recommended.\n\nThere is no lossless compression mode; quality 100 produces unreasonably large files without being truly lossless.\n\nRequires Rust 1.67 or later if building from source."],["HISTORY","**cavif** was created by Kornel Lesinski as a pure Rust implementation of an AVIF encoder. AVIF itself was standardized in **2019** by the Alliance for Open Media, leveraging the AV1 video codec for still image compression."],["INSTALL","```nix: nix profile install nixpkgs#cavif```"],["SEE ALSO","[cwebp](/man/cwebp)(1), [convert](/man/convert)(1)"]],"e":["cwebp","convert"],"x":"cavif [path/to/image.png]"},"clamd":{"n":"clamd","t":"multi-threaded antivirus scanning daemon","s":[["TAGLINE","multi-threaded antivirus scanning daemon"],["TLDR","**Start clamd** daemon\n\n```clamd```\n\n**Start in foreground**\n\n```clamd --foreground```\n\n**Start with specific config file**\n\n```clamd -c [/etc/clamav/clamd.conf]```\n\n**Check configuration** and exit\n\n```clamd --config-check```\n\n**Reload virus database** without restarting\n\n```clamdscan --reload```"],["SYNOPSIS","**clamd** [_options_]"],["DESCRIPTION","**clamd** is the ClamAV daemon, a multi-threaded virus scanning service. It loads the virus database into memory once and handles scan requests from clients, providing much faster scanning than the standalone clamscan tool.\n\nThe daemon listens on a Unix socket or TCP port and accepts commands to scan files, directories, or data streams. Multiple clients can connect simultaneously, making it suitable for email servers, file servers, and web applications.\n\nclamd is commonly used with mail transfer agents (Postfix, Exim, Sendmail), web proxies, and file upload handlers to provide real-time malware scanning. Clients interact with clamd using **clamdscan** or by sending commands directly to the socket."],["PARAMETERS","**-c** _file_, **--config-file=**_file_\n> Use specified configuration file (default: /etc/clamav/clamd.conf).\n\n**-F**, **--foreground**\n> Run in foreground, do not daemonize.\n\n**--debug**\n> Enable debug messages.\n\n**--config-check**\n> Read configuration, check for errors, and exit.\n\n**-h**, **--help**\n> Display help.\n\n**-V**, **--version**\n> Display version."],["CONFIGURATION","**/etc/clamav/clamd.conf**\n> Daemon configuration including socket type (LocalSocket or TCPSocket), scan limits (MaxFileSize, MaxScanSize), thread count (MaxThreads), and logging options (LogFile, LogSyslog)."],["CAVEATS","Requires regular virus database updates via **freshclam**. Memory usage depends on database size (typically 1-2 GB of RAM). Socket permissions must allow client access. The **Example** directive in clamd.conf must be removed or commented out before the daemon will start. Performance depends on available memory and CPU cores."],["HISTORY","**ClamAV** was created by **Tomasz Kojm** in **2001** as an open-source antivirus engine, originally designed for email gateway scanning on Unix systems. It was acquired by **Sourcefire** in **2007** and later became part of **Cisco** when Sourcefire was acquired in **2013**. ClamAV remains one of the most widely deployed open-source antivirus solutions, particularly in mail and web gateway environments."],["INSTALL","```apt: sudo apt install clamav-daemon```\n\n```dnf: sudo dnf install clamd```\n\n```apk: sudo apk add clamav-daemon```"],["SEE ALSO","[clamscan](/man/clamscan)(1), [clamdscan](/man/clamdscan)(1), [freshclam](/man/freshclam)(1)"]],"e":["clamscan","clamdscan","freshclam"],"x":"clamd"},"cms.1s":{"n":"cms.1s","t":"Cryptographic Message Syntax operations via OpenSSL","s":[["TAGLINE","Cryptographic Message Syntax operations via OpenSSL"],["TLDR","**Sign a file** with a certificate and output in S/MIME format\n\n```openssl cms -sign -in [file.txt] -out [signed.p7m] -signer [cert.pem] -inkey [key.pem]```\n\n**Verify a signed message**\n\n```openssl cms -verify -in [signed.p7m] -CAfile [ca-cert.pem]```\n\n**Encrypt a file** for a recipient\n\n```openssl cms -encrypt -in [file.txt] -out [encrypted.p7m] -recip [recipient.pem]```\n\n**Decrypt a CMS message**\n\n```openssl cms -decrypt -in [encrypted.p7m] -recip [cert.pem] -inkey [key.pem]```\n\n**Create a detached signature**\n\n```openssl cms -sign -in [file.txt] -out [signature.p7s] -signer [cert.pem] -inkey [key.pem] -nodetach```\n\n**Extract content from signed message**\n\n```openssl cms -verify -in [signed.p7m] -out [content.txt] -noverify```"],["SYNOPSIS","**openssl cms** [_operation_] [_options_]"],["PARAMETERS","**-sign**\n> Sign the input data with certificate and private key.\n\n**-verify**\n> Verify a signed CMS message.\n\n**-encrypt**\n> Encrypt the input data for one or more recipients.\n\n**-decrypt**\n> Decrypt a CMS encrypted message.\n\n**-in** _FILE_\n> Input file to process.\n\n**-out** _FILE_\n> Output file for the result.\n\n**-signer** _FILE_\n> Certificate file to use for signing.\n\n**-recip** _FILE_\n> Recipient certificate for encryption or decryption.\n\n**-inkey** _FILE_\n> Private key file for signing or decryption.\n\n**-CAfile** _FILE_\n> File containing trusted CA certificates for verification.\n\n**-nodetach**\n> Include the signed content within the CMS message (not detached).\n\n**-noverify**\n> Do not verify the signer's certificate.\n\n**-text**\n> Add text/plain MIME headers for S/MIME email."],["DESCRIPTION","**openssl cms** is the OpenSSL utility for handling Cryptographic Message Syntax (CMS) data. CMS is a standard for cryptographic protection of data, defined in RFC 5652, and is the successor to PKCS#7.\n\nThe command provides operations for signing, verification, encryption, and decryption of data. It supports multiple content types, allows detached signatures, and handles S/MIME email format. CMS is widely used in secure email (S/MIME), digital signatures, and encrypted data containers.\n\nKey operations include creating signed messages with optional timestamp authority integration, encrypting data for multiple recipients with different certificates, and verifying signatures against certificate chains with proper trust validation."],["CAVEATS","Certificate trust chains must be properly configured for verification. Self-signed certificates require explicit trust settings. The **-noverify** option bypasses certificate validation and should only be used for testing. Default encryption algorithms may vary by OpenSSL version."],["HISTORY","CMS was standardized as **RFC 5652** in **2009**, building upon PKCS#7 from RSA Laboratories. OpenSSL added the **cms** command as an improvement over the older **smime** command, offering better support for CMS-specific features and more modern cryptographic options."],["SEE ALSO","[x509](/man/x509)(1), [openssl](/man/openssl)(1)"]],"e":["x509","openssl"],"x":"openssl cms -sign -in [file.txt] -out [signed.p7m] -signer [cert.pem] -inkey [key.pem]"},"debezium":{"n":"debezium","t":"distributed platform for change data capture","s":[["TAGLINE","distributed platform for change data capture"],["TLDR","**Start Debezium server**\n\n```debezium-server```\n\n**Run Debezium Server with Docker**\n\n```docker run -it --name debezium -p 8080:8080 -v $PWD/conf:/debezium/conf debezium/server```\n\n**Run MySQL connector in Kafka Connect**\n\n```curl -X POST http://localhost:8083/connectors -H \"Content-Type: application/json\" -d @[mysql-connector.json]```\n\n**Check connector status**\n\n```curl http://localhost:8083/connectors/[connector-name]/status```\n\n**List running connectors**\n\n```curl http://localhost:8083/connectors```"],["SYNOPSIS","**debezium-server** (standalone)\n\nKafka Connect REST API at **http://localhost:8083/connectors**"],["CONFIGURATION","**application.properties** (Debezium Server):\n```properties\ndebezium.source.connector.class=io.debezium.connector.mysql.MySqlConnector\ndebezium.source.offset.storage.file.filename=data/offsets.dat\ndebezium.source.database.hostname=localhost\ndebezium.source.database.port=3306\ndebezium.source.database.user=debezium\ndebezium.source.database.password=dbz\ndebezium.source.database.server.id=1\ndebezium.source.database.server.name=mydb\ndebezium.sink.type=kafka\ndebezium.sink.kafka.producer.bootstrap.servers=localhost:9092\n```"],["CONNECTORS","**MySQL**: Reads MySQL/MariaDB binlog\n**PostgreSQL**: Uses logical replication\n**MongoDB**: Reads oplog\n**SQL Server**: Uses CDC tables\n**Oracle**: Uses LogMiner or XStream\n**Cassandra**: Reads commit log"],["DESCRIPTION","**Debezium** is an open-source distributed platform for change data capture (CDC). It monitors databases and produces events for every row-level change, enabling real-time data streaming and synchronization.\n\nDebezium connectors read database transaction logs (MySQL binlog, PostgreSQL WAL, etc.) and convert changes to events. These events can be sent to Apache Kafka, Amazon Kinesis, Google Pub/Sub, or other sinks for processing by downstream applications.\n\nThe platform runs either as Kafka Connect connectors (distributed, scalable) or as Debezium Server (standalone, simpler deployment). It captures inserts, updates, and deletes with before/after values and metadata."],["CAVEATS","Requires database configuration changes to enable log reading. Initial snapshot of large databases can take significant time. Kafka Connect mode requires Kafka infrastructure. Schema changes require careful handling. Database privileges needed for log access vary by platform."],["HISTORY","Debezium was created at **Red Hat** by **Randall Hauch** and first released in **2016**. It was developed to provide a reliable, open-source CDC solution for microservices architectures. The Apache-licensed project attracted a large and diverse community of contributors. In December **2024**, Debezium joined the **Commonhaus Foundation** to establish vendor-neutral governance while maintaining its established community and processes."],["SEE ALSO","[kafka](/man/kafka)(1), [docker](/man/docker)(1), [docker-compose](/man/docker-compose)(1)"]],"e":["kafka","docker","docker-compose"],"x":"debezium-server"},"dnf-builddep":{"n":"dnf-builddep","t":"install build dependencies for RPM packages","s":[["TAGLINE","install build dependencies for RPM packages"],["TLDR","Install **build dependencies** for a spec file\n\n```dnf builddep [path/to/specification.spec]```\n\nInstall dependencies from a **source RPM**\n\n```dnf builddep [path/to/package.src.rpm]```\n\nInstall dependencies, **skipping unavailable** packages\n\n```dnf builddep --skip-unavailable [path/to/specification.spec]```\n\n**Define RPM macro** during dependency resolution\n\n```dnf builddep -D '[macro expression]' [path/to/specification.spec]```\n\nInstall dependencies for a **package name** from repositories\n\n```dnf builddep [package_name]```\n\nInstall dependencies **without confirmation** prompts\n\n```dnf builddep -y [path/to/specification.spec]```"],["SYNOPSIS","**dnf builddep** [_options_] _package_"],["DESCRIPTION","**dnf builddep** installs all dependencies required to build a package from its spec file or source RPM. Part of dnf-plugins-core.\n\nEssential for building RPM packages from source."],["PARAMETERS","**--skip-unavailable**\n> Skip build dependencies not available in repositories.\n\n**-D**, **--define** _macro_\n> Define RPM macro when parsing spec files (not applied to SRPMs).\n\n**--spec**\n> Treat arguments as .spec files.\n\n**--srpm**\n> Treat arguments as source RPM files.\n\n**--nobest**\n> Do not limit packages to the best available versions.\n\n**-y**, **--assumeyes**\n> Automatically answer yes for all questions.\n\n**--help-cmd**\n> Display help for this command."],["CAVEATS","Requires dnf-plugins-core. Build dependencies may include many packages. Root privileges typically required. The --skip-broken option is not honored by builddep."],["SEE ALSO","[dnf](/man/dnf)(8), [rpmbuild](/man/rpmbuild)(8), [rpm](/man/rpm)(8), [mock](/man/mock)(1)"]],"e":["dnf","rpmbuild","rpm","mock"],"x":"dnf builddep [path/to/specification.spec]"},"docker-image-tag":{"n":"docker-image-tag","t":"Tag Docker images with repository and version identifiers","s":[["TAGLINE","Tag Docker images with repository and version identifiers"],["TLDR","**Tag a local image with a name and version**\n\n```docker image tag [source_image]:[tag] [target_image]:[version]```\n\n**Tag an image for a private registry**\n\n```docker image tag [image]:[tag] [registry.example.com:5000/image]:[tag]```\n\n**Tag an image by its ID**\n\n```docker image tag [image_id] [repository]:[tag]```\n\n**Create a latest tag from a versioned image**\n\n```docker image tag [image]:[version] [image]:latest```"],["SYNOPSIS","**docker** **image** **tag** _SOURCE_IMAGE[:TAG]_ _TARGET_IMAGE[:TAG]_"],["DESCRIPTION","**docker image tag** creates a tag that refers to an existing image. Tags are aliases for image IDs and are used to version images and push them to registries. Multiple tags can point to the same image, allowing different versioning schemes or registry locations without duplicating the image data.\n\nA tag name must be valid ASCII and may contain lowercase and uppercase letters, digits, underscores, periods, and hyphens. It may not start with a period or hyphen and is limited to 128 characters.\n\nIf no tag is specified, **latest** is used by default for both the source and target."],["INSTALL","```apt: sudo apt install docker-cli```\n\n```dnf: sudo dnf install docker-cli```\n\n```pacman: sudo pacman -S docker```\n\n```apk: sudo apk add docker-cli```\n\n```zypper: sudo zypper install docker```\n\n```brew: brew install docker```\n\n```nix: nix profile install nixpkgs#docker```"],["SEE ALSO","[docker-tag](/man/docker-tag)(1), [docker-image-ls](/man/docker-image-ls)(1), [docker-image-pull](/man/docker-image-pull)(1), [docker-image-rm](/man/docker-image-rm)(1), [docker-build](/man/docker-build)(1), [docker-pull](/man/docker-pull)(1)"]],"e":["docker-tag","docker-image-ls","docker-image-pull","docker-image-rm","docker-build","docker-pull"],"x":"docker image tag [source_image]:[tag] [target_image]:[version]"},"doxygen":{"n":"doxygen","t":"documentation generator for source code","s":[["TAGLINE","documentation generator for source code"],["TLDR","**Generate default config** file\n\n```doxygen -g [Doxyfile]```\n\n**Generate documentation**\n\n```doxygen [Doxyfile]```\n\n**Generate with updates** to config\n\n```doxygen -u [Doxyfile]```\n\n**Generate a minimal config** without the explanatory comments\n\n```doxygen -g -s [Doxyfile]```\n\n**Read the config from stdin** and generate\n\n```cat [Doxyfile] | doxygen -```\n\n**Launch the GUI** front end\n\n```doxywizard```"],["SYNOPSIS","**doxygen** [_configfile_]\n\n**doxygen** [**-g**|**-u**|**-s**|**-l**|**-w**|**-x**] [_file_]"],["PARAMETERS","_CONFIGFILE_\n> The configuration file to use. Defaults to `Doxyfile` in the current directory. A single **-** reads the configuration from standard input.\n\n**-g** [_FILE_]\n> Generate a template configuration file, fully commented. Writes `Doxyfile` if no name is given.\n\n**-u** [_FILE_]\n> Update an existing configuration file to the current Doxygen version, preserving your settings and adding any new options with their defaults.\n\n**-s**\n> Omit the explanatory comments. Combines with **-g** and **-u** to produce a short config that is far easier to keep in version control.\n\n**-x** [_FILE_]\n> Print the differences between the given configuration and the defaults. The quickest way to see what a project has actually customised.\n\n**-l** [_FILE_]\n> Generate a layout file (`DoxygenLayout.xml`) that controls the ordering and presence of sections in the output.\n\n**-w** _FORMAT_ ...\n> Generate style sheet or template files. _FORMAT_ is `html`, `latex`, or `rtf`.\n\n**-d** _MODE_\n> Enable a debug mode, such as `Preprocessor` or `FilterOutput`.\n\n**-b**\n> Run with unbuffered output, so progress appears immediately when piped.\n\n**-v**, **--version**\n> Show the version and exit.\n\n**--help**\n> Display help information."],["CONFIGURATION","**Doxyfile**\n> Main configuration file for project documentation settings, output formats, and parsing options."],["DESCRIPTION","**Doxygen** is the standard documentation generator for C++, C, Java, Python, and other languages. It extracts documentation from specially-formatted comments in source code and generates output in HTML, LaTeX, RTF, and other formats.\n\nThe tool parses source code, builds a cross-referenced documentation structure, and renders it with class diagrams, call graphs, and inheritance trees. Special comment markers (///, /**, etc.) identify documentation blocks.\n\nDoxygen understands Markdown inside comments, and it will happily document a codebase that has no documentation comments at all, producing a browsable, cross-referenced index of every class, function, and file from the declarations alone. That is often reason enough to run it on an unfamiliar project.\n\nBeyond C++ and C it handles Java, Python, C#, PHP, Objective-C, Fortran, and IDL, and it will read Javadoc- and Qt-style comment blocks as well as its own."],["CAVEATS","The generated `Doxyfile` is enormous, several hundred settings with page after page of comments. Generating it with **-g -s** and keeping only what you change makes it reviewable; **-x** shows what an inherited config actually alters.\n\nDiagrams are the usual disappointment. Class hierarchies, collaboration diagrams, call graphs, and include graphs all require **Graphviz**, so `HAVE_DOT` must be set to `YES` and `dot` must be on the PATH, otherwise Doxygen silently falls back to crude built-in images. Call graphs on a large codebase also generate an enormous number of images and can make a build take hours.\n\nDoxygen only documents what it can parse. Heavy preprocessor use, template metaprogramming, and macros that expand into declarations routinely confuse it, which is what `ENABLE_PREPROCESSING`, `MACRO_EXPANSION`, and `PREDEFINED` exist to work around. And the output is only as good as the comments: run with `WARN_IF_UNDOCUMENTED` and `WARN_AS_ERROR` in CI if the documentation is meant to stay complete."],["HISTORY","Doxygen was written by **Dimitri van Heesch** and first released in **1997**, originally as a tool for generating Qt-style documentation for his own C++ code. It filled an obvious gap, since Java had Javadoc but C++ had nothing comparable, and it became the de facto standard for C and C++ documentation almost by default. It is still maintained by van Heesch, now on GitHub, close to three decades on."],["INSTALL","```apt: sudo apt install doxygen```\n\n```dnf: sudo dnf install doxygen```\n\n```pacman: sudo pacman -S doxygen```\n\n```apk: sudo apk add doxygen```\n\n```zypper: sudo zypper install doxygen```\n\n```brew: brew install doxygen```\n\n```nix: nix profile install nixpkgs#doxygen```"],["SEE ALSO","[dot](/man/dot)(1), [cppcheck](/man/cppcheck)(1), [javadoc](/man/javadoc)(1), [sphinx-build](/man/sphinx-build)(1)"],["RESOURCES","```[Source code](https://github.com/doxygen/doxygen)```\n\n```[Homepage](https://www.doxygen.nl)```\n\n```[Documentation](https://www.doxygen.nl/manual/index.html)```"]],"e":["dot","cppcheck","javadoc","sphinx-build"],"x":"doxygen -g [Doxyfile]"},"dumpe2fs":{"n":"dumpe2fs","t":"ext2/ext3/ext4 filesystem information dumper","s":[["TAGLINE","ext2/ext3/ext4 filesystem information dumper"],["TLDR","Display **filesystem information**\n\n```sudo dumpe2fs /dev/sdXN```\n\nDisplay **bad blocks** only\n\n```sudo dumpe2fs -b /dev/sdXN```\n\n**Force** display with unrecognized features\n\n```sudo dumpe2fs -f /dev/sdXN```\n\nDisplay **superblock** info only\n\n```sudo dumpe2fs -h /dev/sdXN```\n\nDisplay block numbers in **hexadecimal**\n\n```sudo dumpe2fs -x /dev/sdXN```"],["SYNOPSIS","**dumpe2fs** [_-bfghixV_] [_-o superblock=superblock_] [_-o blocksize=blocksize_] _device_"],["DESCRIPTION","**dumpe2fs** prints the superblock and block group information for ext2, ext3, and ext4 filesystems. It displays detailed filesystem metadata including features, block counts, and allocation information."],["PARAMETERS","**-b**\n> Print only the blocks marked as bad\n\n**-f**\n> Force display even with unrecognized feature flags\n\n**-g**\n> Display block group descriptor information in colon-separated format\n\n**-h**\n> Display superblock info only, not block group details\n\n**-i**\n> Display filesystem data from image file\n\n**-o superblock=NUM**\n> Use alternative superblock at block NUM\n\n**-o blocksize=SIZE**\n> Specify block size when searching for superblock\n\n**-x**\n> Display block numbers in hexadecimal format\n\n**-V**\n> Display version information"],["CAVEATS","dumpe2fs only **reads**, so it is safe to run on a mounted filesystem, but the figures it prints will be slightly stale: the superblock on disk lags the kernel's in-memory copy, so free block and inode counts can be out of date. Unmount first if the numbers must be exact.\n\nThe output is long, and **-h** is usually what you actually want: the superblock alone gives the block size, inode count, feature flags, mount count, and the UUID, without pages of block group detail.\n\n**-b** does not list bad blocks from a fresh scan. It prints the blocks already recorded in the filesystem's bad-block inode, which is only populated if something put them there, typically `mke2fs -c` or `e2fsck -c`. An empty result therefore means \"none recorded\", not \"none exist\".\n\nA practical use is recovering from a damaged primary superblock: `dumpe2fs` prints the locations of the backup superblocks, which is what `e2fsck -b` then needs."],["HISTORY","**dumpe2fs** is part of **e2fsprogs**, the ext2/3/4 utility suite that **Theodore Ts'o** has maintained since **1993**. It is the counterpart to `tune2fs`: where tune2fs changes filesystem parameters, dumpe2fs shows them, and between them they cover almost everything you can ask or tell an ext filesystem without mounting it. The tool has outlived two filesystem revisions largely unchanged, since ext3 and ext4 both kept the ext2 on-disk layout and simply added features to the superblock's feature flags."],["INSTALL","```apt: sudo apt install e2fsprogs```\n\n```dnf: sudo dnf install e2fsprogs```\n\n```pacman: sudo pacman -S e2fsprogs```\n\n```apk: sudo apk add e2fsprogs-extra```\n\n```zypper: sudo zypper install e2fsprogs```\n\n```brew: brew install e2fsprogs```\n\n```nix: nix profile install nixpkgs#e2fsprogs```"],["SEE ALSO","[tune2fs](/man/tune2fs)(8), [e2fsck](/man/e2fsck)(8), [mke2fs](/man/mke2fs)(8), [debugfs](/man/debugfs)(8), [resize2fs](/man/resize2fs)(8), [blkid](/man/blkid)(8)"],["RESOURCES","```[Source code](https://git.kernel.org/pub/scm/fs/ext2/e2fsprogs.git)```\n\n```[Homepage](http://e2fsprogs.sourceforge.net)```"]],"e":["tune2fs","e2fsck","mke2fs","debugfs","resize2fs","blkid"],"x":"sudo dumpe2fs /dev/sdXN"},"dwdiff":{"n":"dwdiff","t":"word-by-word file comparison","s":[["TAGLINE","word-by-word file comparison"],["TLDR","**Compare two files word by word**\n\n```dwdiff [file1.txt] [file2.txt]```\n\n**Use color output**\n\n```dwdiff -c [file1.txt] [file2.txt]```\n\n**Show only the changes**, suppressing words common to both files\n\n```dwdiff -3 [file1.txt] [file2.txt]```\n\n**Show line numbers**\n\n```dwdiff -L [file1.txt] [file2.txt]```\n\n**Ignore case** differences\n\n```dwdiff -i [file1.txt] [file2.txt]```\n\n**Treat punctuation as delimiters** so words are split more finely\n\n```dwdiff -P [file1.txt] [file2.txt]```\n\n**Print statistics** about how much changed\n\n```dwdiff -s [file1.txt] [file2.txt]```\n\n**Page the diff** with less-style highlighting\n\n```dwdiff -l [file1.txt] [file2.txt] | less -R```"],["SYNOPSIS","**dwdiff** [_options_] _file1_ _file2_"],["PARAMETERS","**-c**[_spec_], **--color**[**=**_spec_]\n> Color the output. An optional _spec_ overrides the delete and insert colors.\n\n**-l**, **--less-mode**\n> Use overstriking (underline for deletions, bold for insertions), suitable for piping to less -R.\n\n**-L**[_width_], **--line-numbers**[**=**_width_]\n> Show line numbers at the start of each line.\n\n**-s**, **--statistics**\n> Print word counts and change percentages when done.\n\n**-i**, **--ignore-case**\n> Ignore differences in case when comparing words.\n\n**-I**, **--ignore-formatting**\n> Ignore formatting changes such as bold or underline markup.\n\n**-d** _chars_, **--delimiters=**_chars_\n> Characters to treat as word delimiters.\n\n**-P**, **--punctuation**\n> Use punctuation characters as delimiters.\n\n**-W** _chars_, **--white-space=**_chars_\n> Characters to treat as whitespace.\n\n**-1**, **--no-deleted**\n> Suppress words deleted from the first file.\n\n**-2**, **--no-inserted**\n> Suppress words inserted in the second file.\n\n**-3**, **--no-common**\n> Suppress words common to both files, leaving only the changes.\n\n**-C** _num_, **--context=**_num_\n> Show _num_ lines of context around each change.\n\n**-A** _algo_, **--algorithm=**_algo_\n> Comparison algorithm: best, normal or fast.\n\n**-w** _string_, **--start-delete=**_string_\n> String marking the start of deleted text (default `[-`).\n\n**-x** _string_, **--stop-delete=**_string_\n> String marking the end of deleted text (default `-]`).\n\n**-y** _string_, **--start-insert=**_string_\n> String marking the start of inserted text (default `{+`).\n\n**-z** _string_, **--stop-insert=**_string_\n> String marking the end of inserted text (default `+}`).\n\n**--diff-input**\n> Read unified diff output instead of two files.\n\n**--wdiff-output**\n> Produce wdiff-compatible output."],["DESCRIPTION","**dwdiff** compares files word-by-word rather than line-by-line, making it easier to spot small changes in prose or documentation. It highlights deleted words (from the first file) and inserted words (in the second file).\n\nThe tool is particularly useful for comparing text documents, translations, or any content where line-based diffs produce confusing output. It shows exactly which words changed between versions."],["OUTPUT FORMAT","```\n[-removed-] {+added+}\n```\n\nDeleted text is shown in brackets with minus, inserted text in braces with plus."],["CAVEATS","Best suited for prose, not code. Large files may be slow to process. Word boundaries depend on delimiter settings. May need adjustment for non-English text or special characters."],["HISTORY","dwdiff was written by **G.P. Halkes** as an improved alternative to the older wdiff utility. It provides better handling of Unicode, color output, and more flexible word delimiting options."],["INSTALL","```apt: sudo apt install dwdiff```\n\n```brew: brew install dwdiff```\n\n```nix: nix profile install nixpkgs#dwdiff```"],["SEE ALSO","[wdiff](/man/wdiff)(1), [diff](/man/diff)(1), [colordiff](/man/colordiff)(1), [vimdiff](/man/vimdiff)(1)"],["RESOURCES","```[Homepage](https://os.ghalkes.nl/dwdiff.html)```"]],"e":["wdiff","diff","colordiff","vimdiff"],"x":"dwdiff [file1.txt] [file2.txt]"},"expac":{"n":"expac","t":"pacman database query tool with custom formatting","s":[["TAGLINE","pacman database query tool with custom formatting"],["TLDR","List **dependencies** of a package\n\n```expac [-S|--sync] '%D' [package]```\n\nList **optional dependencies**\n\n```expac [-S|--sync] \"%o\" [package]```\n\nList **download size** of packages in MiB\n\n```expac [-S|--sync] [-H|--humansize] M '%k\\t%n' [package1] [package2]```\n\nList packages marked for **upgrade** with size\n\n```expac [-S|--sync] [-H|--humansize] M '%k\\t%n' $(pacman -Qqu) | sort [-sh|--sort --human-numeric-sort]```\n\nList **explicitly-installed** packages with optional dependencies\n\n```expac [-d|--delim] '\\n\\n' [-l|--listdelim] '\\n\\t' [-Q|--query] '%n\\n\\t%O' $(pacman -Qeq)```"],["SYNOPSIS","**expac** [_options_] _format_ [_targets_]"],["DESCRIPTION","**expac** extracts data from alpm databases using printf-like format strings. It provides flexible output formatting for pacman-based package queries.\n\nThe tool acts as a lightweight alternative to parsing pacman output, offering direct access to package metadata including dependencies, sizes, descriptions, and installation dates. Format specifiers like %n (name), %v (version), %D (dependencies) enable custom reports.\n\nUseful for scripting, package analysis, and generating custom package reports. Particularly powerful when combined with other tools for filtering and processing package information."],["PARAMETERS","**-S, --sync**\n> Query sync database\n\n**-Q, --query**\n> Query local database\n\n**-H, --humansize** _unit_\n> Human-readable sizes (K, M, G)\n\n**-d, --delim** _string_\n> Delimiter between packages (default: newline)\n\n**-l, --listdelim** _string_\n> Delimiter for list items (default: two spaces)\n\n**-t, --timefmt** _format_\n> Time output format string passed to strftime(3) (default: %c)\n\n**-1, --readone**\n> Stop after first match\n\n**-p, --file** _pkgfile_\n> Query a package file instead of the database"],["FORMAT SPECIFIERS","Common format specifiers: %n (name), %v (version), %d (description), %D (depends), %E (depends, no version), %O (optional deps), %o (optional deps, no descriptions), %N (required by), %P (provides), %H (conflicts), %G (groups), %m (install size), %k (download size), %r (repo), %u (URL), %a (architecture), %l (install date), %w (install reason), %F (files)."],["CAVEATS","Arch Linux and derivatives only. Format strings use % specifiers. Part of the pacman ecosystem. Use -H to format sizes in human-readable units (K, M, G, T)."],["INSTALL","```pacman: sudo pacman -S expac```"],["SEE ALSO","[pacman](/man/pacman)(8), [pactree](/man/pactree)(8)"]],"e":["pacman","pactree"],"x":"expac [-S|--sync] '%D' [package]"},"gcloud-version":{"n":"gcloud-version","t":"displays SDK version information","s":[["TAGLINE","displays SDK version information"],["TLDR","**Show gcloud version**\n\n```gcloud version```\n\n**Show versions as JSON**\n\n```gcloud version --format=json```\n\n**Brief version (top-level flag)**\n\n```gcloud --version```\n\n**Show versions as YAML**\n\n```gcloud version --format=yaml```"],["SYNOPSIS","**gcloud version** [_options_]"],["PARAMETERS","**--format** _FORMAT_\n> Output format: text, json, yaml.\n\n**--help**\n> Display help information."],["DESCRIPTION","**gcloud version** displays the version of the Google Cloud SDK and installed components. It shows version numbers for gcloud, bq, gsutil, and other SDK tools.\n\nThe command helps verify which SDK version is installed and identify if updates are available. Component versions help with troubleshooting and compatibility checks.\n\ngcloud version is useful for documenting environment details and ensuring consistency."],["CAVEATS","Version may differ from latest available. Component versions vary independently."],["HISTORY","gcloud version is part of the **Google Cloud SDK** diagnostic commands, providing version information for all installed components."],["SEE ALSO","[gcloud](/man/gcloud)(1), [gcloud-components](/man/gcloud-components)(1)"]],"e":["gcloud","gcloud-components"],"x":"gcloud version"},"gh-ruleset":{"n":"gh-ruleset","t":"Manage GitHub repository and organization rulesets","s":[["TAGLINE","Manage GitHub repository and organization rulesets"],["TLDR","**List rulesets in the current repository**\n\n```gh ruleset list```\n\n**List rulesets including those inherited from parents**\n\n```gh ruleset list --parents```\n\n**View a specific ruleset** by ID\n\n```gh ruleset view [ruleset_id]```\n\n**View a ruleset in the web browser**\n\n```gh ruleset view [ruleset_id] --web```\n\n**Check which rules apply to a given branch**\n\n```gh ruleset check [branch]```\n\n**List organization-level rulesets**\n\n```gh ruleset list --org [organization]```"],["SYNOPSIS","**gh** **ruleset** _command_ [_flags_]"],["SUBCOMMANDS","**check** [_branch_]\n> Show rules that apply to a given branch in the current repository.\n\n**list**\n> List rulesets for a repository or organization.\n\n**view** [_ruleset-id_]\n> View information about a ruleset. Without an ID, runs interactively."],["PARAMETERS","**-R**, **--repo** _\\[HOST/\\]OWNER/REPO_\n> Select another repository using the \\[HOST/\\]OWNER/REPO format.\n\n**-o**, **--org** _organization_\n> Operate on an organization's rulesets instead of a repository's.\n\n**-p**, **--parents**\n> Include inherited rulesets from parent organizations (list/view).\n\n**-L**, **--limit** _int_\n> Maximum number of rulesets to list (default 30).\n\n**-w**, **--web**\n> Open the ruleset in the browser (view).\n\n**-t**, **--default-branch**\n> Check rules for the repository's default branch (check)."],["DESCRIPTION","**gh ruleset** manages GitHub repository and organization rulesets, which define branch and tag protection rules, required status checks, required reviews, deployment protections, and merge requirements. Rulesets are a more expressive replacement for classic branch protection rules.\n\nA ruleset can target multiple branches or tags using fnmatch patterns, apply across all repositories in an organization, and define bypass actors. The **check** command evaluates which rules currently apply to a specific branch, useful for understanding why a push or merge was rejected.\n\nCreating and editing rulesets is not supported directly from the CLI; use the GitHub web UI or the REST API via **gh api**."],["CAVEATS","Requires authentication via **gh auth login**. Viewing organization rulesets requires organization permissions. Rules defined at the enterprise or organization level may not appear unless **--parents** is used."],["INSTALL","```apt: sudo apt install gh```\n\n```dnf: sudo dnf install gh```\n\n```pacman: sudo pacman -S github-cli```\n\n```apk: sudo apk add github-cli```\n\n```zypper: sudo zypper install gh```\n\n```brew: brew install gh```\n\n```nix: nix profile install nixpkgs#gh```"],["SEE ALSO","[gh](/man/gh)(1), [gh-api](/man/gh-api)(1), [gh-repo](/man/gh-repo)(1), [gh-auth](/man/gh-auth)(1)"]],"e":["gh","gh-api","gh-repo","gh-auth"],"x":"gh ruleset list"},"gh-screensaver":{"n":"gh-screensaver","t":"Display animated terminal screensavers","s":[["TAGLINE","Display animated terminal screensavers"],["TLDR","**Start a random screensaver**\n\n```gh screensaver```\n\n**Start a specific screensaver**\n\n```gh screensaver -s [fireworks|starfield|pipes|marquee|life|pollock]```\n\n**List** available screensavers\n\n```gh screensaver -l```\n\nRun marquee with a custom **message**\n\n```gh screensaver -s marquee -- --message=\"[Hello World]\"```\n\nRun starfield with custom **density** and **speed**\n\n```gh screensaver -s starfield -- --density [500] --speed [8]```"],["SYNOPSIS","**gh** **screensaver** [_options_]"],["PARAMETERS","**-s**, **--saver** _name_\n> Screensaver to run (fireworks, starfield, pipes, marquee, life, pollock).\n\n**-l**, **--list**\n> List available screensavers and exit.\n\n**-R**, **--repo** _repository_\n> Run in the context of a repository (currently unused)."],["DESCRIPTION","**gh screensaver** is a GitHub CLI extension that displays animated terminal screensavers. Available screensavers are: **fireworks**, **starfield**, **pipes**, **marquee**, **life** (Conway's Game of Life), and **pollock** (paint splotches). Without **-s**, a random screensaver is chosen.\n\nScreensaver-specific options are passed after **--**. For example, marquee supports **--message** and **--font**; starfield supports **--density** and **--speed**; fireworks, pipes, and life support **--color** (full/off); life supports **--seed** (glider, noise, R, dragon, gun, pulsar).\n\nThis extension must be installed first with **gh extension install vilmibm/gh-screensaver**. Press **q** or **Ctrl+C** to exit the screensaver."],["CAVEATS","Requires the GitHub CLI (**gh**) and must be installed as an extension. Some animations may not render correctly in terminals with limited color support."],["INSTALL","```nix: nix profile install nixpkgs#gh-screensaver```"],["SEE ALSO","[gh](/man/gh)(1), [gh-extension](/man/gh-extension)(1), [cmatrix](/man/cmatrix)(1)"]],"e":["gh","gh-extension","cmatrix"],"x":"gh screensaver"},"git-contrib":{"n":"git-contrib","t":"Show author contribution summary","s":[["TAGLINE","Show author contribution summary"],["TLDR","**Show author contributions**\n\n```git contrib [author-name]```\n\n**Show contributions for email**\n\n```git contrib \"[email@example.com]\"```"],["SYNOPSIS","**git contrib** _author_"],["PARAMETERS","_AUTHOR_\n> Author name or email to search for.\n\n**--help**\n> Display help information."],["DESCRIPTION","**git contrib** displays a summary of commits by a specific author. It shows commit count and commit details, providing a quick overview of someone's contributions to the repository.\n\nThe command searches commits by author name or email pattern using substring matching. It's useful for reviewing a contributor's work, generating contribution reports, or verifying commit attribution.\n\nThis provides a more focused view than **git log --author**, specifically designed for quickly assessing an individual's contributions to the project."],["CAVEATS","Part of git-extras package. Author matching is substring-based. Shows commits reachable from current branch."],["HISTORY","git contrib is part of **git-extras**, created by **TJ Holowaychuk** to provide quick contributor summaries."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-authors](/man/git-authors)(1), [git-shortlog](/man/git-shortlog)(1)"],["RESOURCES","```[Source code](https://github.com/tj/git-extras)```"]],"e":["git-authors","git-shortlog"],"x":"git contrib [author-name]"},"git-gc":{"n":"git-gc","t":"Cleanup unnecessary files and optimize the local repository","s":[["TAGLINE","Cleanup unnecessary files and optimize the local repository"],["TLDR","**Run garbage collection**\n\n```git gc```\n\n**Aggressive cleanup** (recomputes deltas, slow)\n\n```git gc --aggressive```\n\n**Prune all unreachable objects immediately**\n\n```git gc --prune=now```\n\n**Only run gc if housekeeping is needed**\n\n```git gc --auto```\n\n**Force gc even if another instance may be running**\n\n```git gc --force```\n\n**Run without progress output**\n\n```git gc --quiet```"],["SYNOPSIS","**git gc** [_options_]"],["PARAMETERS","**--aggressive**\n> Recompute deltas for more thorough optimization. Significantly slower.\n\n**--prune=**_date_\n> Prune loose objects older than date (default: 2 weeks ago, configurable via gc.pruneExpire).\n\n**--no-prune**\n> Do not prune any loose objects.\n\n**--auto**\n> Only run if housekeeping is needed based on gc.auto thresholds.\n\n**--cruft**\n> Pack unreachable objects into a cruft pack instead of leaving them loose (default: on).\n\n**--no-cruft**\n> Leave unreachable objects as loose objects instead of packing into cruft packs.\n\n**--quiet**\n> Suppress all progress reports.\n\n**--force**\n> Force gc to run even if another git gc instance may be running on this repository.\n\n**--keep-largest-pack**\n> Consolidate all packs except the largest non-cruft pack and .keep packs into a single pack."],["DESCRIPTION","**git gc** (garbage collection) cleans up unnecessary files and optimizes the local repository. It packs loose objects, removes unreachable objects, and compresses file revisions for better performance and reduced disk usage.\n\nGit runs gc automatically after certain operations (e.g., merge, rebase). Manual gc is useful after history rewrites, large imports, or when disk usage is high. The **--aggressive** mode recomputes all deltas from scratch for deeper optimization but takes significantly longer. The default **--prune** grace period of 2 weeks protects objects that may be referenced by concurrent operations."],["CAVEATS","Aggressive gc is very slow on large repositories. Running gc removes dangling objects after the prune grace period. Let gc complete before other repository operations. Concurrent gc instances are prevented by a lock file."],["HISTORY","git gc is a core **Git** command for repository maintenance, automating cleanup tasks that were originally separate commands like git-prune and git-repack."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-prune](/man/git-prune)(1), [git-repack](/man/git-repack)(1), [git-reflog](/man/git-reflog)(1), [git-fsck](/man/git-fsck)(1), [git-count-objects](/man/git-count-objects)(1)"]],"e":["git-prune","git-repack","git-reflog","git-fsck","git-count-objects"],"x":"git gc"},"git-reauthor":{"n":"git-reauthor","t":"Rewrite commit authorship history","s":[["TAGLINE","Rewrite commit authorship history"],["TLDR","**Change the email address** of matching commits\n\n```git reauthor -o [old@email.com] -e [new@email.com]```\n\n**Change both name and email**\n\n```git reauthor -o [old@email.com] -n \"[New Name]\" -e [new@email.com]```\n\n**Use values from `git config user.name` / `user.email`**\n\n```git reauthor --use-config -o [old@email.com]```\n\n**Rewrite all identities** (prompts interactively)\n\n```git reauthor --all```\n\n**Rewrite only the committer** (not author)\n\n```git reauthor -o [old@email.com] -e [new@email.com] -t committer```"],["SYNOPSIS","**git reauthor** [_options_]"],["PARAMETERS","**-o**, **--old-email** _EMAIL_\n> Email address to match on existing commits.\n\n**-e**, **--correct-email** _EMAIL_\n> Replacement email address.\n\n**-n**, **--correct-name** _NAME_\n> Replacement author/committer name.\n\n**-c**, **--use-config**\n> Take the replacement name/email from the current **git config** values.\n\n**-a**, **--all**\n> Rewrite all identities found in history (interactive).\n\n**-t**, **--type** _TYPE_\n> Which identity to rewrite: **author**, **committer**, or **both** (default).\n\n**--help**\n> Display help information."],["DESCRIPTION","**git reauthor** rewrites commit history to correct author (and/or committer) information. It updates names and email addresses across all local branches and tags for every commit whose identity matches the supplied old email.\n\nThe command is useful for fixing incorrect attribution, consolidating identities after email changes, or correcting authorship before publishing a repository. Because it rewrites history and changes every commit SHA in the rewritten range, it should only be used on branches that have not been shared."],["CAVEATS","Part of git-extras package. **Rewrites history.** Don't use on shared branches. All commit hashes change."],["HISTORY","git reauthor is part of **git-extras**, providing a convenient wrapper for the common task of fixing author information."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-commit](/man/git-commit)(1), [git-rebase](/man/git-rebase)(1), [git-filter-repo](/man/git-filter-repo)(1)"]],"e":["git-commit","git-rebase","git-filter-repo"],"x":"git reauthor -o [old@email.com] -e [new@email.com]"},"glib-compile-resources":{"n":"glib-compile-resources","t":"GLib resource file compiler","s":[["TAGLINE","GLib resource file compiler"],["TLDR","**Compile resources** to a binary bundle\n\n```glib-compile-resources [resources.gresource.xml]```\n\n**Compile with specific output** file\n\n```glib-compile-resources --target=[resources.gresource] [resources.gresource.xml]```\n\n**Generate C source code** instead of binary\n\n```glib-compile-resources --generate-source --target=[resources.c] [resources.gresource.xml]```\n\n**Generate C header file**\n\n```glib-compile-resources --generate-header --target=[resources.h] [resources.gresource.xml]```\n\n**Specify source directory** for resource files\n\n```glib-compile-resources --sourcedir=[path/to/resources] [resources.gresource.xml]```\n\n**Print dependencies** for build systems\n\n```glib-compile-resources --generate-dependencies [resources.gresource.xml]```\n\n**Set C identifier prefix**\n\n```glib-compile-resources --c-name=[myapp] --generate-source [resources.gresource.xml]```"],["SYNOPSIS","**glib-compile-resources** [_OPTIONS_] _FILE_"],["PARAMETERS","**--target**=_TARGET_\n> Output file path. Defaults to basename of input with appropriate extension.\n\n**--sourcedir**=_DIR_\n> Directory containing resource files referenced in the XML.\n\n**--generate-source**\n> Generate C source file instead of binary bundle.\n\n**--generate-header**\n> Generate C header file for use with generated source.\n\n**--generate-dependencies**\n> Print list of referenced files to stdout.\n\n**--c-name**=_PREFIX_\n> Prefix for C identifiers in generated code.\n\n**--manual-register**\n> Generate manual register/unregister functions instead of auto-initialization.\n\n**--internal**\n> Generate internal symbols (not exported)."],["DESCRIPTION","**glib-compile-resources** compiles application resources (images, UI files, CSS, icons, data) into binary bundles or C source for embedding in GLib/GTK applications. Resources are described in XML files (typically with .gresource.xml extension) that list files to include and optional preprocessing.\n\nThe GResource system enables applications to access embedded resources through a virtual filesystem path (e.g., /org/myapp/data.txt) without external file dependencies. This simplifies deployment and improves load times."],["CAVEATS","Resource paths in the XML must match actual file locations relative to sourcedir. The xmllint tool may be required for XML preprocessing options. Large resources increase binary size significantly."],["HISTORY","glib-compile-resources was introduced with GLib 2.32 in **March 2012** as part of the GResource API. It replaced ad-hoc methods for embedding resources in GTK applications, providing a standardized approach that integrates with the GNOME build system."],["INSTALL","```apt: sudo apt install libgio-2.0-dev-bin```\n\n```apk: sudo apk add glib-dev```"]],"e":[],"x":"glib-compile-resources [resources.gresource.xml]"},"grabc":{"n":"grabc","t":"Identify the color of a screen pixel by clicking on it","s":[["TAGLINE","Identify the color of a screen pixel by clicking on it"],["TLDR","**Pick a color** by clicking a pixel and print its hex value\n\n```grabc```\n\n**Print** the picked color **as RGB components** on stderr\n\n```grabc -rgb```\n\n**Print** the **window id** at the mouse click instead of a color\n\n```grabc -W```\n\n**Read** the color of a known pixel **without clicking**, using a window id and coordinate\n\n```grabc -w [window_id] -l +[x]+[y]```\n\n**Print all 16 bits** of color depth instead of the default high-order 8\n\n```grabc -a```"],["SYNOPSIS","**grabc** [_options_]"],["DESCRIPTION","**grabc** is a small X Window utility that reports the color of a screen pixel. When run with no arguments, the mouse pointer turns into a crosshair; clicking on any pixel prints that pixel's color as a hexadecimal string (prefixed with **#**) to standard output, while the equivalent RGB component values are written to standard error.\n\nBecause the hex value goes to stdout and the RGB value to stderr, the two can be captured independently in scripts, for example assigning the hex string to a shell variable while letting the RGB breakdown print to the terminal. This makes grabc convenient for sampling colors from any running application, wallpaper, or image to reuse in configuration files, themes, or drawing programs.\n\nGiven a window id and a pixel coordinate, grabc can also read a color non-interactively, which is useful for automation and testing."],["PARAMETERS","**-v**\n> Show version information.\n\n**-h**\n> Show usage help.\n\n**-hex**\n> Print the pixel value as hexadecimal on stdout (default behavior).\n\n**-rgb**\n> Print the pixel value as RGB components on stderr.\n\n**-W**\n> Print the window id at the point of the mouse click.\n\n**-w**  _id_\n> Window id in hex; use together with **-l** to read a pixel non-interactively.\n\n**-l**  _+x+y_\n> Pixel coordinate to sample; requires a window id supplied with **-w**.\n\n**-d**\n> Show debug messages.\n\n**-a**\n> Print all 16 bits of color; the default reports only the high-order 8 bits."],["CAVEATS","grabc requires a running X11 server and links against Xlib, so it does not work on a pure Wayland session without an X compatibility layer such as XWayland. Reported values reflect what is actually drawn on screen, including any compositor effects, color profiles, or transparency blending, which can differ from a source image's nominal color."],["HISTORY","grabc was written by **Muhammad A Muquit** and first released on **March 16, 1997**. It remains a compact, single-purpose tool distributed as C source that builds against libX11."],["INSTALL","```apt: sudo apt install grabc```"],["SEE ALSO","[xprop](/man/xprop)(1), [xwininfo](/man/xwininfo)(1), [import](/man/import)(1), [convert](/man/convert)(1)"],["RESOURCES","```[Source code](https://github.com/muquit/grabc)```\n\n```[Homepage](https://www.muquit.com/muquit/software/grabc/grabc.html)```"]],"e":["xprop","xwininfo","import","convert"],"x":"grabc"},"gst-inspect-1.0":{"n":"gst-inspect-1.0","t":"GStreamer plugin and element inspector","s":[["TAGLINE","GStreamer plugin and element inspector"],["TLDR","**List all available plugins and elements**\n\n```gst-inspect-1.0```\n\n**Inspect a specific element**\n\n```gst-inspect-1.0 [filesrc]```\n\n**Inspect a plugin by name**\n\n```gst-inspect-1.0 [coreelements]```\n\n**Print details of all elements**\n\n```gst-inspect-1.0 -a```\n\n**Print machine-parseable feature info for a plugin**\n\n```gst-inspect-1.0 --print-plugin-auto-install-info [playback]```\n\n**Show GStreamer version**\n\n```gst-inspect-1.0 --version```\n\n**Search for elements by name or description**\n\n```gst-inspect-1.0 | grep [audio]```"],["SYNOPSIS","**gst-inspect-1.0** [_options_] [_element_|_plugin_]"],["PARAMETERS","**-a**, **--print-all**\n> Print information about all available elements.\n\n**--print-plugin-auto-install-info**\n> Print a machine-parseable list of features the specified plugin or element provides. Useful for package managers and auto-installers.\n\n**-b**, **--print-blacklist**\n> Print a list of blacklisted (failed to load) plugins.\n\n**--plugin**\n> List features of a plugin. The argument is treated as a plugin name rather than an element.\n\n**--types** _types_\n> Filter elements by type when used with `--print-all` (e.g., Audio/Sink).\n\n**--exists** _element_\n> Check if the specified element exists. Returns exit code 0 if found, 1 if not.\n\n**--atleast-version** _version_\n> When used with `--exists`, also check that the element is at least the specified version.\n\n**--uri-handlers**\n> Print all available URI handlers.\n\n**--gst-plugin-path=**_PATH_\n> Add directories to the plugin search path (separated by colons).\n\n**--version**\n> Show GStreamer version information.\n\n**--help**\n> Print help and exit."],["DESCRIPTION","**gst-inspect-1.0** queries information about GStreamer plugins and elements. It displays element details including pad information, capabilities, properties with types and defaults, and available signals.\n\nWithout arguments, it lists all available plugins and their elements with summaries. With an element or plugin name, it shows detailed information about that component, including the element's factory details, pads, properties, and signals.\n\nThis is an essential tool for GStreamer pipeline development, allowing developers to discover available elements, understand their capabilities, and check property types before constructing pipelines with **gst-launch-1.0**."],["INSTALL","```apt: sudo apt install gstreamer1.0-tools```\n\n```apk: sudo apk add gstreamer-tools```"],["SEE ALSO","[gst-launch-1.0](/man/gst-launch-1.0)(1), [gst-discoverer-1.0](/man/gst-discoverer-1.0)(1), [gst-play-1.0](/man/gst-play-1.0)(1), [gst-typefind-1.0](/man/gst-typefind-1.0)(1)"]],"e":["gst-launch-1.0","gst-discoverer-1.0","gst-play-1.0","gst-typefind-1.0"],"x":"gst-inspect-1.0"},"gthumb":{"n":"gthumb","t":"GNOME image viewer and browser","s":[["TAGLINE","GNOME image viewer and browser"],["TLDR","**Open image browser**\n\n```gthumb```\n\n**Open specific folder**\n\n```gthumb [/path/to/photos]```\n\n**Open specific image**\n\n```gthumb [image.jpg]```\n\n**Start slideshow**\n\n```gthumb --slideshow [folder]```\n\n**Import from camera**\n\n```gthumb --import-photos```"],["SYNOPSIS","**gthumb** [_options_] [_files_]"],["PARAMETERS","_FILES_\n> Files or folders to open.\n\n**--slideshow**\n> Start slideshow.\n\n**--import-photos**\n> Import from device.\n\n**--fullscreen**\n> Start in fullscreen.\n\n**-n**, **--new-window**\n> Open new window.\n\n**--help**\n> Display help information."],["DESCRIPTION","**gThumb** is a GNOME image viewer and browser. It supports viewing, organizing, and editing photos with features like slideshows and batch operations.\n\nThe application handles common image formats and provides tools for resizing, cropping, and color adjustment. It integrates with GNOME desktop."],["CAVEATS","GNOME dependencies. Some features require plugins. Memory usage scales with library size."],["HISTORY","gThumb was created by **Paolo Bacchilega** as an image viewer for the **GNOME** desktop environment."],["INSTALL","```apt: sudo apt install gthumb```\n\n```dnf: sudo dnf install gthumb```\n\n```pacman: sudo pacman -S gthumb```\n\n```apk: sudo apk add gthumb```\n\n```zypper: sudo zypper install gthumb```\n\n```nix: nix profile install nixpkgs#gthumb```"],["SEE ALSO","[eog](/man/eog)(1), [feh](/man/feh)(1)"]],"e":["eog","feh"],"x":"gthumb"},"hostapd":{"n":"hostapd","t":"user-space daemon for wireless access points and authentication servers","s":[["TAGLINE","user-space daemon for wireless access points and authentication servers"],["TLDR","**Start access point**\n\n```sudo hostapd [/etc/hostapd/hostapd.conf]```\n\n**Start in foreground** with debug\n\n```sudo hostapd -d [hostapd.conf]```\n\n**Start in background**\n\n```sudo hostapd -B [hostapd.conf]```\n\n**Start with debug and timestamps**\n\n```sudo hostapd -dt [hostapd.conf]```"],["SYNOPSIS","**hostapd** [_options_] _config_file_"],["DESCRIPTION","**hostapd** is a user-space daemon for wireless access points and authentication servers. It implements IEEE 802.11 access point management, WPA/WPA2/WPA3 authentication, and IEEE 802.1X authenticator.\n\nThe tool transforms a wireless interface into an access point, handling client connections, encryption, and authentication protocols."],["PARAMETERS","**-B**\n> Run in background (daemon).\n\n**-d**\n> Debug output.\n\n**-dd**\n> More verbose debug.\n\n**-t**\n> Include timestamps in debug output.\n\n**-v**\n> Display version information and exit.\n\n**-K**\n> Include key data in debug output.\n\n**-g** _socket_\n> Global control interface path.\n\n**-P** _pidfile_\n> PID file path.\n\n**-f** _logfile_\n> Redirect output to log file.\n\n**-e** _entropy_file_\n> File for storing entropy (to speed up restart)."],["CONFIG OPTIONS","**interface**: Wireless interface (e.g. wlan0)\n**driver**: Driver interface (nl80211, hostap, etc.)\n**ssid**: Network name\n**hw_mode**: Band (a = 5GHz, b = 2.4GHz 11Mbps, g = 2.4GHz 54Mbps)\n**channel**: Channel number (0 = ACS if supported)\n**ieee80211n**: Enable 802.11n (1 = yes)\n**ieee80211ac**: Enable 802.11ac (1 = yes)\n**wpa**: WPA mode (1 = WPA, 2 = WPA2, 3 = both)\n**wpa_passphrase**: Password (8-63 characters)\n**wpa_key_mgmt**: Key management (WPA-PSK, SAE for WPA3)"],["CAVEATS","Requires AP-capable wireless card. Configuration complex. Driver support varies. May conflict with NetworkManager."],["HISTORY","**hostapd** was developed by **Jouni Malinen** as part of the Linux wireless stack. It became the standard way to create software access points on Linux, supporting evolving wireless security standards."],["INSTALL","```apt: sudo apt install hostapd```\n\n```dnf: sudo dnf install hostapd```\n\n```pacman: sudo pacman -S hostapd```\n\n```apk: sudo apk add hostapd```\n\n```zypper: sudo zypper install hostapd```\n\n```nix: nix profile install nixpkgs#hostapd```"],["SEE ALSO","[iw](/man/iw)(1), [iwconfig](/man/iwconfig)(1), [wpa_supplicant](/man/wpa_supplicant)(1), [dnsmasq](/man/dnsmasq)(1)"]],"e":["iw","iwconfig","wpa_supplicant","dnsmasq"],"x":"sudo hostapd [/etc/hostapd/hostapd.conf]"},"hub-clone":{"n":"hub-clone","t":"clones GitHub repositories using shorthand notation","s":[["TAGLINE","clones GitHub repositories using shorthand notation"],["TLDR","**Clone with shorthand**\n\n```hub clone [user/repo]```\n\n**Clone to directory**\n\n```hub clone [user/repo] [directory]```\n\n**Clone with depth**\n\n```hub clone --depth [1] [user/repo]```\n\n**Clone own repository**\n\n```hub clone [repo-name]```"],["SYNOPSIS","**hub clone** [_options_] _repository_ [_directory_]"],["PARAMETERS","_[USER/]REPOSITORY_\n> Repository to clone. _USER_ defaults to your own GitHub username when omitted.\n\n_DIRECTORY_\n> Target directory to clone into (default: the repository name).\n\n**-p**\n> (Deprecated) Clone a private repository over SSH.\n\n**--depth** _N_\n> Create a shallow clone with the given commit history depth. Passed straight through to git clone.\n\n**-b**, **--branch** _BRANCH_\n> Checkout _BRANCH_ instead of the remote's default branch. Passed straight through to git clone.\n\n**--help**\n> Display help information."],["DESCRIPTION","**hub clone** expands GitHub shorthand (`user/repo`, or just `repo` for your own repositories) into a full clone URL and then hands off to a normal **git clone**, so any git-clone option (**--depth**, **-b**, **--recurse-submodules**, etc.) works as-is. Without a `user/` prefix, hub assumes the repository belongs to the authenticated GitHub user.\n\nBy default hub clones over HTTPS; set `git config --global hub.protocol ssh` to have it generate SSH URLs instead for **clone**, **fork**, and related commands."],["CAVEATS","Hub is no longer actively maintained and has been superseded by the official GitHub CLI (**gh**, via **gh repo clone**). GitHub authentication is only needed for private repositories or to resolve `repo` (no user) shorthand against your own account; cloning a public `user/repo` works without it."],["HISTORY","hub clone is part of **hub**, GitHub's original command-line tool that predates the official **gh** CLI."],["INSTALL","```apt: sudo apt install hub```\n\n```pacman: sudo pacman -S hub```\n\n```zypper: sudo zypper install hub```\n\n```brew: brew install hub```\n\n```nix: nix profile install nixpkgs#hub```"],["SEE ALSO","[hub](/man/hub)(1), [git-clone](/man/git-clone)(1), [gh](/man/gh)(1)"],["RESOURCES","```[Source code](https://github.com/mislav/hub)```"]],"e":["hub","git-clone","gh"],"x":"hub clone [user/repo]"},"i3":{"n":"i3","t":"tiling window manager for X11","s":[["TAGLINE","tiling window manager for X11"],["TLDR","**Start i3**\n\n```i3```\n\n**Use custom config**\n\n```i3 -c [~/.config/i3/config]```\n\n**Replace running instance**\n\n```i3 --replace```\n\n**Start with log file**\n\n```i3 --shmlog-size=[10485760]```\n\n**Check version**\n\n```i3 --version```"],["SYNOPSIS","**i3** [_options_]"],["PARAMETERS","**-c** _FILE_\n> Configuration file.\n\n**-a**\n> Disable autostart.\n\n**--replace**\n> Replace running WM.\n\n**--shmlog-size** _SIZE_\n> Shared memory log size.\n\n**-V**\n> Verbose mode.\n\n**--version**\n> Show version.\n\n**--help**\n> Display help information."],["DESCRIPTION","**i3** is a tiling window manager for X11. It organizes windows in a tree structure with workspaces, containers, and layouts.\n\nThe window manager uses keyboard-driven navigation and configuration. It supports multi-monitor setups and extensive customization."],["CAVEATS","X11 only. Wayland version is Sway. Configuration learning curve."],["HISTORY","i3 was created by **Michael Stapelberg** in **2009** as an improved tiling window manager focused on simplicity and documentation."],["INSTALL","```apt: sudo apt install i3-wm```\n\n```dnf: sudo dnf install i3```\n\n```pacman: sudo pacman -S i3-wm```\n\n```apk: sudo apk add i3wm```\n\n```zypper: sudo zypper install i3```\n\n```nix: nix profile install nixpkgs#i3```"],["SEE ALSO","[i3-msg](/man/i3-msg)(1), [i3lock](/man/i3lock)(1), [sway](/man/sway)(1)"]],"e":["i3-msg","i3lock","sway"],"x":"i3"},"koji-taginfo":{"n":"koji-taginfo","t":"print tag information from a Koji build system","s":[["TAGLINE","print tag information from a Koji build system"],["TLDR","**Print information about a tag**\n\n```koji taginfo [tag]```\n\n**Print info for multiple tags**\n\n```koji taginfo [tag1] [tag2]```\n\n**Display help**\n\n```koji taginfo --help```"],["SYNOPSIS","**koji taginfo** [_options_] _tag_ [_tag_...]"],["DESCRIPTION","**koji taginfo** prints basic information about one or more tags from a Koji build system, including the tag ID, permissions, arches, locked status, inheritance chain, and associated build targets. This is useful for inspecting how tags are configured in Fedora, CentOS, and other RPM-based distribution build systems."],["PARAMETERS","**tag**\n> Tag name to query (can specify multiple).\n\n**--event=**_EVENT_\n> Query at the specified event (for historical state).\n\n**--user=**_USER_\n> Query at the event corresponding to the given user's session.\n\n**--inherit**\n> Show inherited information for the tag.\n\n**-h**, **--help**\n> Display help information."],["INSTALL","```dnf: sudo dnf install koji```\n\n```brew: brew install koji```\n\n```nix: nix profile install nixpkgs#koji```"],["SEE ALSO","[koji](/man/koji)(1), [koji-tag-build](/man/koji-tag-build)(1)"]],"e":["koji","koji-tag-build"],"x":"koji taginfo [tag]"},"kubectl-proxy":{"n":"kubectl-proxy","t":"runs a proxy to the Kubernetes API server","s":[["TAGLINE","runs a proxy to the Kubernetes API server"],["TLDR","**Start API proxy**\n\n```kubectl proxy```\n\n**Proxy on specific port**\n\n```kubectl proxy --port=[8001]```\n\n**Allow external access**\n\n```kubectl proxy --address=[0.0.0.0] --accept-hosts='.*'```\n\n**Proxy specific API paths**\n\n```kubectl proxy --api-prefix=/api/```"],["SYNOPSIS","**kubectl** **proxy** [_options_]"],["PARAMETERS","**--port** _port_\n> Port to run proxy on.\n\n**--address** _address_\n> Address to bind to.\n\n**--accept-hosts** _regex_\n> Hosts to accept requests from.\n\n**--api-prefix** _prefix_\n> API prefix for proxy."],["DESCRIPTION","**kubectl proxy** runs a proxy to the Kubernetes API server. Handles authentication and allows accessing the API and services without exposing them directly. Default port is 8001."],["INSTALL","```apt: sudo apt install kubectl```\n\n```pacman: sudo pacman -S kubectl```\n\n```apk: sudo apk add kubectl```\n\n```brew: brew install kubectl```\n\n```nix: nix profile install nixpkgs#kubectl```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [kubectl-port-forward](/man/kubectl-port-forward)(1)"]],"e":["kubectl","kubectl-port-forward"],"x":"kubectl proxy"},"latexpand":{"n":"latexpand","t":"Flatten LaTeX source by expanding \\input and \\include directives","s":[["TAGLINE","Flatten LaTeX source by expanding \\input and \\include directives"],["TLDR","**Expand includes in LaTeX file**\n\n```latexpand [input.tex] > [output.tex]```\n\n**Keep comments**\n\n```latexpand --keep-comments [input.tex]```\n\n**Expand bibliography**\n\n```latexpand --expand-bbl [input.bbl] [input.tex]```\n\n**Output to file**\n\n```latexpand -o [output.tex] [input.tex]```\n\n**Fail if an included file cannot be found**\n\n```latexpand --fatal [input.tex]```\n\n**Expand \\usepackage directives inline**\n\n```latexpand --expand-usepackage [input.tex]```\n\n**Also expand biber bibliography**\n\n```latexpand --biber [input.bcf] [input.tex]```"],["SYNOPSIS","**latexpand** [_options_] _file_"],["PARAMETERS","**-o**, **--output** _file_\n> Output to file instead of stdout.\n\n**--verbose**\n> Show what is going on.\n\n**--keep-comments**\n> Preserve comments (lines starting with % and anything below \\end{document}) in output. By default, comments are stripped.\n\n**--empty-comments**\n> Keep empty comments (trailing % at end of lines) for clarity.\n\n**--keep-includes**\n> Do not expand \\input and \\include directives.\n\n**--expand-usepackage**\n> Expand \\usepackage directives when a matching .sty file is found in $TEXINPUTS or the current directory.\n\n**--expand-bbl** _file_\n> Inline the given .bbl file in place of the bibliography.\n\n**--biber** _file_\n> Inline the biber .bcf/bibliography data (for biblatex with the biber backend).\n\n**-d**, **--define** _key=val_\n> Define a macro for replacement. May be given multiple times.\n\n**--explain**\n> Generate explanatory comments in the output showing what was expanded.\n\n**--show-graphics**\n> List included graphics on stderr.\n\n**--graphics_extensions** _LIST_\n> Colon-separated list of possible graphics file extensions.\n\n**--fatal**\n> Exit with an error if an included file cannot be located (default is to warn).\n\n**--makeatletter**\n> Insert a \\makeatletter in the preamble to help resolve some inclusion issues.\n\n**--in-encoding** _FMT_\n> Specify input file encoding using PerlIO syntax (e.g., :encoding(UTF-8)).\n\n**--out-encoding** _FMT_\n> Specify output file encoding using PerlIO syntax.\n\n**--help**\n> Display help."],["DESCRIPTION","**latexpand** expands **\\input** and **\\include** commands in LaTeX documents, producing a single flattened file. Useful for journal submissions that require single-file documents (e.g., arXiv) or for processing with tools that don't handle includes. It is a Perl script distributed via CTAN and TeX Live."],["INSTALL","```pacman: sudo pacman -S texlive-binextra```\n\n```apk: sudo apk add texlive-binextra```"],["SEE ALSO","[pdflatex](/man/pdflatex)(1), [latexmk](/man/latexmk)(1)"]],"e":["pdflatex","latexmk"],"x":"latexpand [input.tex] > [output.tex]"},"lazycut":{"n":"lazycut","t":"Terminal-based video trimming tool","s":[["TAGLINE","Terminal-based video trimming tool"],["TLDR","**Trim a video** file interactively in the terminal\n\n```lazycut [video.mp4]```\n\n**Open a specific video** for trimming\n\n```lazycut [path/to/file.mkv]```"],["SYNOPSIS","**lazycut** _video-file_"],["DESCRIPTION","**lazycut** is a terminal-based video trimming tool that provides an interactive TUI for marking in/out points and exporting trimmed video clips. It renders video frames directly in the terminal using **chafa** and processes cuts with **FFmpeg**, allowing quick and precise trimming without leaving the command line.\n\nThe interface uses vim-style keybindings for navigation and supports repeat counts for seeking (e.g., **5l** seeks forward 5 seconds). After setting in and out points, pressing Enter exports the trimmed clip."],["KEYBOARD CONTROLS","**Space**\n> Play or pause playback.\n\n**h** / **l**\n> Seek backward or forward 1 second.\n\n**H** / **L**\n> Seek backward or forward 5 seconds.\n\n**i** / **o**\n> Set the in point or out point for trimming.\n\n**Enter**\n> Export the trimmed clip.\n\n**?**\n> Display help menu.\n\n**q**\n> Quit the application."],["CAVEATS","Requires both **FFmpeg** and **chafa** to be installed and available in PATH. Terminal rendering quality depends on the terminal emulator's capabilities and font size. Not available in standard Linux distribution package managers — must be installed via Homebrew or built from source."],["HISTORY","**lazycut** was created by **Emin Ozata** and first released on **GitHub** as an open-source project under the **MIT license**. Written entirely in **Go**, it gained visibility after appearing on Hacker News. The tool provides a lightweight alternative to GUI video editors for simple trimming tasks."],["INSTALL","```brew: brew install lazycut```"],["SEE ALSO","[ffmpeg](/man/ffmpeg)(1), [chafa](/man/chafa)(1)"]],"e":["ffmpeg","chafa"],"x":"lazycut [video.mp4]"},"libuser-lid":{"n":"libuser-lid","t":"list groups a user belongs to","s":[["TAGLINE","list groups a user belongs to"],["TLDR","This command is an alias of [lid](/man/lid)."],["INSTALL","```apt: sudo apt install libuser```\n\n```dnf: sudo dnf install libuser```\n\n```zypper: sudo zypper install libuser```"],["SEE ALSO","[lid](/man/lid)(1), [id](/man/id)(1), [groups](/man/groups)(1)"]],"e":["lid","id","groups"],"x":""},"lkbib":{"n":"lkbib","t":"searches bibliographic databases","s":[["TAGLINE","searches bibliographic databases"],["TLDR","**Search bibliographic databases**\n\n```lkbib [keyword]```\n\n**Search specific database**\n\n```lkbib -p [database] [keyword]```\n\n**Search multiple keywords**\n\n```lkbib [keyword1] [keyword2]```\n\n**Case insensitive search**\n\n```lkbib -i [keyword]```"],["SYNOPSIS","**lkbib** [_options_] _keys_"],["PARAMETERS","_KEYS_\n> Search keywords.\n\n**-p** _DATABASE_\n> Bibliographic database file.\n\n**-i**\n> Case insensitive search.\n\n**-n** _NUM_\n> Maximum number of results.\n\n**--help**\n> Display help information."],["DESCRIPTION","**lkbib** searches bibliographic databases. It finds references matching keywords in refer-style databases.\n\nThe tool is part of groff and works with refer for formatting citations in documents."],["CAVEATS","Part of groff package. Uses refer database format. Academic use mainly."],["HISTORY","lkbib is part of the **groff** text formatting system, providing bibliography lookup for the refer preprocessor."],["INSTALL","```apt: sudo apt install groff```\n\n```dnf: sudo dnf install groff```\n\n```pacman: sudo pacman -S groff```\n\n```apk: sudo apk add groff```\n\n```zypper: sudo zypper install groff```\n\n```brew: brew install groff```\n\n```nix: nix profile install nixpkgs#groff```"],["SEE ALSO","[refer](/man/refer)(1), [lookbib](/man/lookbib)(1), [groff](/man/groff)(1)"]],"e":["refer","lookbib","groff"],"x":"lkbib [keyword]"},"loginctl":{"n":"loginctl","t":"controls the systemd login manager","s":[["TAGLINE","controls the systemd login manager"],["TLDR","Print all **current sessions**\n\n```loginctl```\n\nShow **all properties** of a session\n\n```loginctl show-session session_id -a```\n\nShow properties of a **specific user**\n\n```loginctl show-user username```\n\nShow a **specific property** of a user\n\n```loginctl show-user username -p Property```\n\nExecute on a **remote host**\n\n```loginctl list-users -H hostname```\n\n**Log out** a user from all sessions\n\n```loginctl terminate-user username```\n\n**Lock** all sessions\n\n```loginctl lock-sessions```"],["SYNOPSIS","**loginctl** [_OPTIONS_] [_COMMAND_]"],["DESCRIPTION","**loginctl** controls the systemd login manager (systemd-logind). It can be used to introspect and control the state of user sessions, seats, and the login manager itself."],["COMMANDS","**list-sessions**\n> List current sessions\n\n**session-status [ID]**\n> Show runtime session status\n\n**show-session [ID]**\n> Show properties of sessions\n\n**activate ID**\n> Activate a session\n\n**lock-session [ID]**\n> Lock sessions\n\n**unlock-session [ID]**\n> Unlock sessions\n\n**lock-sessions**\n> Lock all sessions\n\n**unlock-sessions**\n> Unlock all sessions\n\n**terminate-session ID**\n> Terminate a session\n\n**list-users**\n> List logged-in users\n\n**user-status [USER]**\n> Show runtime user status\n\n**show-user [USER]**\n> Show properties of users\n\n**enable-linger [USER]**\n> Enable user linger (keep services running after logout)\n\n**disable-linger [USER]**\n> Disable user linger\n\n**terminate-user USER**\n> Terminate all sessions of a user\n\n**list-seats**\n> List available seats\n\n**seat-status [SEAT]**\n> Show seat status"],["PARAMETERS","**-a, --all**\n> Show all properties\n\n**-p, --property**\n> Show specific property\n\n**-H, --host**\n> Execute on remote host\n\n**-M, --machine**\n> Execute in container\n\n**--no-pager**\n> Do not pipe output to pager\n\n**--no-legend**\n> Do not print header/footer"],["CAVEATS","Enable-linger allows user services to persist after logout. Terminating sessions may cause data loss in running applications."],["HISTORY","**loginctl** is part of **systemd**, managing multi-seat configurations, user sessions, and user lingering through systemd-logind."],["INSTALL","```apt: sudo apt install elogind```\n\n```apk: sudo apk add elogind```\n\n```nix: nix profile install nixpkgs#elogind```"],["SEE ALSO","[systemctl](/man/systemctl)(1), [lslogins](/man/lslogins)(1)"]],"e":["systemctl","lslogins"],"x":"loginctl"},"lorder":{"n":"lorder","t":"lists dependencies between object files","s":[["TAGLINE","lists dependencies between object files"],["TLDR","**Find library dependencies**\n\n```lorder [*.o]```\n\n**Create library order**\n\n```lorder [*.o] | tsort```\n\n**Process specific files**\n\n```lorder [file1.o] [file2.o] [file3.o]```\n\n**Pipe to archive creation**\n\n```ar cr libfoo.a $(lorder [*.o] | tsort)```"],["SYNOPSIS","**lorder** _files_"],["PARAMETERS","_FILES_\n> Object files to analyze."],["DESCRIPTION","**lorder** lists dependencies between object files. It outputs pairs showing which files depend on others.\n\nThe output is typically piped to tsort to determine correct ordering for static libraries."],["CAVEATS","BSD utility. May not be available on all systems. Used with tsort and ar."],["HISTORY","lorder is a traditional **BSD** utility for determining the order of object files in static libraries."],["SEE ALSO","[tsort](/man/tsort)(1), [ar](/man/ar)(1), [nm](/man/nm)(1), [ld](/man/ld)(1)"]],"e":["tsort","ar","nm","ld"],"x":"lorder [*.o]"},"lscgroup":{"n":"lscgroup","t":"lists cgroups in the system","s":[["TAGLINE","lists cgroups in the system"],["TLDR","**List all cgroups**\n\n```lscgroup```\n\n**List specific controller**\n\n```lscgroup [cpu]```\n\n**List specific cgroup**\n\n```lscgroup [cpu:/mygroup]```\n\n**Show all controllers**\n\n```lscgroup -a```\n\n**List cgroup hierarchy**\n\n```lscgroup [memory]```"],["SYNOPSIS","**lscgroup** [_options_] [_controller_]"],["PARAMETERS","_CONTROLLER_\n> Cgroup controller name.\n\n**-a**\n> Show all controllers.\n\n**-g** _GROUP_\n> Show specific group.\n\n**--help**\n> Display help information."],["DESCRIPTION","**lscgroup** lists cgroups in the system. Cgroups control resource allocation for processes.\n\nThe tool shows the cgroup hierarchy for controllers like cpu, memory, and blkio."],["CAVEATS","Cgroups v1 tool. May differ for cgroups v2. Part of libcgroup-tools."],["HISTORY","lscgroup is part of **libcgroup** tools for managing Linux control groups for resource management."],["INSTALL","```apt: sudo apt install cgroup-tools```\n\n```apk: sudo apk add cgroup-tools```"],["SEE ALSO","[cgcreate](/man/cgcreate)(1), [cgexec](/man/cgexec)(1), [cgset](/man/cgset)(1)"]],"e":["cgcreate","cgexec","cgset"],"x":"lscgroup"},"mdbook":{"n":"mdbook","t":"creates online books by writing Markdown files","s":[["TAGLINE","creates online books by writing Markdown files"],["TLDR","**Create** an mdbook project in the current directory\n\n```mdbook init```\n\nCreate an mdbook project in a **specific directory**\n\n```mdbook init [path/to/directory]```\n\n**Clean** the directory with the generated book\n\n```mdbook clean```\n\n**Serve** a book at http://localhost:3000, auto-build when file changes\n\n```mdbook serve```\n\n**Watch** Markdown files and automatically build when a file is changed\n\n```mdbook watch```"],["SYNOPSIS","**mdbook** _command_ [_options_]"],["PARAMETERS","**init**\n> Create a new mdbook project with default structure\n\n**build**\n> Build the book from Markdown sources\n\n**serve**\n> Start a local web server with live reload\n\n**watch**\n> Rebuild automatically when source files change\n\n**clean**\n> Remove the generated book output directory\n\n**test**\n> Test Rust code samples in the book"],["DESCRIPTION","**mdbook** creates online books by writing Markdown files. Developed by the Rust community, it compiles a collection of Markdown files into a static website with navigation, search, and theming support.\n\nA typical mdbook project contains a **SUMMARY.md** file that defines the book's structure and chapter organization. The tool processes these files and generates HTML, with support for code syntax highlighting, MathJax equations, and custom themes.\n\nThe **serve** command is particularly useful during writing, providing a local development server that automatically rebuilds and refreshes the browser when files change."],["CAVEATS","Requires Rust code blocks to be syntactically valid if **mdbook test** is used. Large books may have slow build times. Custom themes require understanding of Handlebars templating. Search functionality is client-side JavaScript."],["HISTORY","mdbook was created by the Rust community as the tool for writing Rust documentation and \"The Rust Programming Language\" book. It has since become popular for technical documentation beyond Rust projects."],["INSTALL","```apt: sudo apt install mdbook```\n\n```dnf: sudo dnf install mdbook```\n\n```pacman: sudo pacman -S mdbook```\n\n```apk: sudo apk add mdbook```\n\n```zypper: sudo zypper install mdbook```\n\n```brew: brew install mdbook```\n\n```nix: nix profile install nixpkgs#mdbook```"],["SEE ALSO","[pandoc](/man/pandoc)(1), [hugo](/man/hugo)(1), [mkdocs](/man/mkdocs)(1)"]],"e":["pandoc","hugo","mkdocs"],"x":"mdbook init"},"melt":{"n":"melt","t":"MLT command-line video editor","s":[["TAGLINE","MLT command-line video editor"],["TLDR","**Play video**\n\n```melt [video.mp4]```\n\n**Render to file**\n\n```melt [input.mp4] -consumer avformat:[output.mp4]```\n\n**Add filter**\n\n```melt [video.mp4] -filter [sepia]```\n\n**Mix audio**\n\n```melt [video.mp4] -mix [10] -mixer [mix:-1]```\n\n**Set output profile**\n\n```melt -profile [atsc_1080p_25] [input.mp4]```\n\n**Concatenate clips**\n\n```melt [clip1.mp4] [clip2.mp4] -consumer avformat:[output.mp4]```"],["SYNOPSIS","**melt** [_options_] _input_ [_filters_]"],["PARAMETERS","_INPUT_\n> Input media file.\n\n**-consumer** _TYPE_\n> Output consumer.\n\n**-filter** _NAME_\n> Apply filter.\n\n**-profile** _NAME_\n> Use profile.\n\n**-mix** _FRAMES_\n> Transition frames.\n\n**--help**\n> Display help information."],["DESCRIPTION","**melt** is the MLT command-line video editor. It processes video with filters and transitions.\n\nThe tool is MLT framework's utility. Used for scripted video editing and effects."],["CAVEATS","Complex syntax. MLT framework knowledge helpful. Part of MLT package."],["HISTORY","melt is the command-line tool for **MLT Multimedia Framework**, used by Kdenlive and Shotcut."],["INSTALL","```apt: sudo apt install melt```\n\n```dnf: sudo dnf install mlt```\n\n```pacman: sudo pacman -S mlt```\n\n```apk: sudo apk add mlt```\n\n```zypper: sudo zypper install melt```\n\n```brew: brew install melt```\n\n```nix: nix profile install nixpkgs#melt```"],["SEE ALSO","[ffmpeg](/man/ffmpeg)(1), [kdenlive](/man/kdenlive)(1), [shotcut](/man/shotcut)(1)"]],"e":["ffmpeg","kdenlive","shotcut"],"x":"melt [video.mp4]"},"mlflow":{"n":"mlflow","t":"CLI for MLflow, an open-source platform for machine learning lifecycle","s":[["TAGLINE","CLI for MLflow, an open-source platform for machine learning lifecycle"],["TLDR","**Start MLflow tracking server**\n\n```mlflow server --host [0.0.0.0] --port [5000]```\n\n**Start UI** for local experiments\n\n```mlflow ui```\n\n**Run an MLflow project**\n\n```mlflow run [project-uri] -P [param=value]```\n\n**Run project from Git**\n\n```mlflow run https://github.com/[user]/[repo]```\n\n**Create a new experiment**\n\n```mlflow experiments create -n [experiment-name]```\n\n**List experiments**\n\n```mlflow experiments search```\n\n**Serve a model**\n\n```mlflow models serve -m [models:/model-name/version] -p [5001]```\n\n**Build Docker image for model**\n\n```mlflow models build-docker -m [models:/model-name/version] -n [image-name]```"],["SYNOPSIS","**mlflow** _command_ [_options_]"],["COMMANDS","**server**\n> Start tracking server.\n\n**ui**\n> Start local tracking UI.\n\n**run** _uri_\n> Run MLflow project.\n\n**experiments** create|search|delete|rename\n> Manage experiments.\n\n**runs** list|describe|delete\n> Manage runs.\n\n**models** serve|build-docker|predict\n> Model deployment.\n\n**artifacts** download|list|log-artifacts\n> Manage artifacts.\n\n**recipes** run\n> Run ML recipes.\n\n**deployments** create|update|delete|list|predict\n> Manage model deployments.\n\n**doctor**\n> Diagnose MLflow installation."],["SERVER OPTIONS","**--host** _address_\n> Bind address. Default: 127.0.0.1.\n\n**--port** _port_\n> Server port. Default: 5000.\n\n**--backend-store-uri** _uri_\n> Database URI for experiment/run data.\n\n**--default-artifact-root** _path_\n> Default artifact storage location.\n\n**--workers** _count_\n> Number of gunicorn workers."],["RUN OPTIONS","**-P**, **--param** _key=value_\n> Project parameters.\n\n**-e**, **--entry-point** _name_\n> Entry point. Default: main.\n\n**--experiment-name** _name_\n> Experiment name for the run.\n\n**--env-manager** _type_\n> Environment manager: local, conda, virtualenv."],["DESCRIPTION","**mlflow** is the CLI for MLflow, an open-source platform for machine learning lifecycle management. It tracks experiments, packages code, and deploys models.\n\nThe **tracking server** stores experiment metadata, parameters, metrics, and artifacts. Use **mlflow ui** for local development or **mlflow server** for team deployment.\n\n**mlflow run** executes MLflow Projects—directories or Git repos with MLproject files defining entry points, parameters, and environments. It ensures reproducibility.\n\nModel serving with **models serve** creates REST endpoints for predictions. **models build-docker** packages models as containers. The Models component supports multiple ML frameworks.\n\nArtifacts include datasets, models, and outputs. The tracking server stores references; actual files go to configured storage (local, S3, GCS, Azure Blob)."],["CAVEATS","Tracking server default (SQLite) not suitable for production; use PostgreSQL/MySQL. Large artifacts need object storage. Some features require specific Python packages. Model serving uses Flask development server."],["HISTORY","MLflow was created at **Databricks** and open-sourced in **June 2018**. It became an **LF AI & Data Foundation** project in **2020**. The platform grew from Databricks' internal tools for managing ML workflows. Version 2.0 (2022) added MLflow Recipes and improved model registry. MLflow is widely adopted for experiment tracking and model management."],["SEE ALSO","[dvc](/man/dvc)(1)"]],"e":["dvc"],"x":"mlflow server --host [0.0.0.0] --port [5000]"},"mocp":{"n":"mocp","t":"console-based audio player with an ncurses interface","s":[["TAGLINE","console-based audio player with an ncurses interface"],["TLDR","**Launch** the MOC terminal UI\n\n```mocp```\n\nLaunch MOC in a **specific directory**\n\n```mocp [path/to/directory]```\n\n**Start server** in background without UI\n\n```mocp -S```\n\n**Add song** to play queue in background\n\n```mocp -q [path/to/audio_file]```\n\n**Add songs recursively** to play queue\n\n```mocp -a [path/to/directory]```\n\n**Clear** the play queue\n\n```mocp -c```\n\n**Play or stop** current song\n\n```mocp --play```\n```mocp --stop```\n\n**Stop the server**\n\n```mocp -x```"],["SYNOPSIS","**mocp** [**-S**] [**-x**] [**-p**] [**-s**] [**-q**] [**-a**] [**-c**] [**-f**] [**-r**] [_file_|_dir_...]"],["PARAMETERS","**-S, --server**\n> Run only the server in background and exit\n\n**-F, --foreground**\n> Run server in foreground with logging to stdout\n\n**-x, --exit**\n> Shut down the MOC server\n\n**-p, --play**\n> Start playing from first playlist item\n\n**-s, --stop**\n> Stop playback\n\n**-P, --pause**\n> Pause playback\n\n**-U, --unpause**\n> Resume from pause\n\n**-G, --toggle-pause**\n> Toggle between play and pause states\n\n**-f, --next**\n> Play next song\n\n**-r, --previous**\n> Play previous song\n\n**-q, --enqueue**\n> Add files to queue without starting interface\n\n**-a, --append**\n> Append files, directories, or playlists\n\n**-c, --clear**\n> Clear the playlist\n\n**-k _N_, --seek _N_**\n> Seek forward/backward by N seconds (use +/- prefix)\n\n**-v _N_, --volume _N_**\n> Set or adjust mixer volume\n\n**-i, --info**\n> Display currently playing file information\n\n**-T _theme_, --theme**\n> Load specified theme file\n\n**-C _file_, --config**\n> Use alternate configuration file\n\n**-m, --music-dir**\n> Start in configured MusicDir"],["DESCRIPTION","**MOC** (Music On Console) is a console-based audio player with an ncurses interface. It supports OGG, WAV, MP3, FLAC, and other audio formats through various decoder plugins.\n\nThe player uses a client-server architecture: the server handles playback while clients provide the interface. This allows music to continue playing after closing the interface. Navigate directories using arrow keys and press Enter to play files."],["CAVEATS","Command-line options affecting server behavior (like --sound-driver) are silently ignored if the server is already running. The server must be restarted to apply such changes. Configuration is stored in ~/.moc/ directory."],["HISTORY","**MOC** was originally developed by **Damian Pietras**. The project provides a lightweight, terminal-based alternative to graphical music players, following the Unix philosophy of doing one thing well."],["INSTALL","```apt: sudo apt install moc```\n\n```zypper: sudo zypper install moc```\n\n```brew: brew install moc```\n\n```nix: nix profile install nixpkgs#moc```"],["SEE ALSO","[cmus](/man/cmus)(1), [mpd](/man/mpd)(1), [ncmpcpp](/man/ncmpcpp)(1)"]],"e":["cmus","mpd","ncmpcpp"],"x":"mocp"},"mongod":{"n":"mongod","t":"MongoDB database server daemon","s":[["TAGLINE","MongoDB database server daemon"],["TLDR","**Start MongoDB server**\n\n```mongod```\n\n**Specify data directory**\n\n```mongod --dbpath [/data/db]```\n\n**Specify port**\n\n```mongod --port [27017]```\n\n**Bind to all interfaces**\n\n```mongod --bind_ip_all```\n\n**Enable authentication**\n\n```mongod --auth```\n\n**Use config file**\n\n```mongod --config [/etc/mongod.conf]```\n\n**Run as replica set**\n\n```mongod --replSet [rs0]```\n\n**Run as a daemon with logging**\n\n```mongod --fork --logpath [/var/log/mongod.log]```"],["SYNOPSIS","**mongod** [_options_]"],["PARAMETERS","**-f**, **--config** _FILE_\n> Configuration file path.\n\n**--dbpath** _PATH_\n> Database directory path (default: /data/db).\n\n**--port** _PORT_\n> Listening port number (default: 27017).\n\n**--bind_ip** _IP_\n> Comma-separated list of IP addresses to bind.\n\n**--bind_ip_all**\n> Bind to all IP addresses.\n\n**--auth**\n> Enable authentication.\n\n**--replSet** _NAME_\n> Replica set name.\n\n**--logpath** _FILE_\n> Path for the log file. Required when using --fork.\n\n**--fork**\n> Run the server as a background daemon.\n\n**--keyFile** _FILE_\n> Path to shared key file for replica set/sharded cluster authentication. Implies --auth.\n\n**--wiredTigerCacheSizeGB** _SIZE_\n> Maximum size of the WiredTiger internal cache in GB.\n\n**--help**\n> Display help information."],["DESCRIPTION","**mongod** is the MongoDB database server daemon. It handles data requests, manages data storage, and performs background management operations.\n\nThe tool is the core database process. Supports replication, sharding, and authentication. Uses the WiredTiger storage engine by default."],["CAVEATS","Requires proper storage configuration. Resource intensive. Needs careful security setup. When using --fork, --logpath must also be specified."],["HISTORY","mongod is the **MongoDB** server process, the core component of MongoDB since its initial release in 2009."],["SEE ALSO","[mongosh](/man/mongosh)(1), [mongo](/man/mongo)(1), [mongoimport](/man/mongoimport)(1), [mongodump](/man/mongodump)(1), [mongorestore](/man/mongorestore)(1)"]],"e":["mongosh","mongo","mongoimport","mongodump","mongorestore"],"x":"mongod"},"mp3info":{"n":"mp3info","t":"displays and edits MP3 file ID3 tags","s":[["TAGLINE","displays and edits MP3 file ID3 tags"],["TLDR","**Show MP3 info**\n\n```mp3info [file.mp3]```\n\n**Set title tag**\n\n```mp3info -t \"[Song Title]\" [file.mp3]```\n\n**Set artist tag**\n\n```mp3info -a \"[Artist Name]\" [file.mp3]```\n\n**Set album tag**\n\n```mp3info -l \"[Album Name]\" [file.mp3]```\n\n**Set year tag**\n\n```mp3info -y [2024] [file.mp3]```\n\n**Set track number**\n\n```mp3info -n [1] [file.mp3]```\n\n**Delete all tags**\n\n```mp3info -d [file.mp3]```"],["SYNOPSIS","**mp3info** [_options_] _files_"],["PARAMETERS","_FILES_\n> MP3 files to process.\n\n**-t** _TITLE_\n> Set title tag.\n\n**-a** _ARTIST_\n> Set artist tag.\n\n**-l** _ALBUM_\n> Set album tag.\n\n**-y** _YEAR_\n> Set year tag.\n\n**-n** _NUM_\n> Set track number.\n\n**-d**\n> Delete all ID3 tags.\n\n**--help**\n> Display help information."],["DESCRIPTION","**mp3info** displays and edits MP3 file ID3 tags. It's a lightweight tag editor.\n\nThe tool reads ID3v1 tags. Shows bitrate, duration, and other technical info."],["CAVEATS","ID3v1 focused. Limited ID3v2 support. Consider id3v2 for advanced editing."],["HISTORY","mp3info was created as a simple **ID3 tag** viewer and editor for command-line MP3 management."],["INSTALL","```apt: sudo apt install mp3info```\n\n```brew: brew install mp3info```"],["SEE ALSO","[id3v2](/man/id3v2)(1), [ffprobe](/man/ffprobe)(1)"]],"e":["id3v2","ffprobe"],"x":"mp3info [file.mp3]"},"named":{"n":"named","t":"DNS server daemon from BIND","s":[["TAGLINE","DNS server daemon from BIND"],["TLDR","**Start** DNS server with default configuration\n\n```named```\n\nUse a **custom configuration** file\n\n```named -c [path/to/named.conf]```\n\nUse **IPv4 only**\n\n```named -4```\n\nUse **IPv6 only**\n\n```named -6```\n\nListen on a **specific port** instead of 53\n\n```named -p [port]```\n\nRun in **foreground** (don't daemonize)\n\n```named -f```"],["SYNOPSIS","**named** [**-4**|**-6**] [**-c** _config-file_] [**-p** _port_] [**-f**] [**-g**] [**-u** _user_]"],["PARAMETERS","**-4**\n> Use IPv4 only, even if IPv6 is available\n\n**-6**\n> Use IPv6 only, even if IPv4 is available\n\n**-c _config-file_**\n> Use specified configuration file instead of /etc/named.conf\n\n**-p _port_**\n> Listen on specified port instead of default 53\n\n**-f**\n> Run in foreground; do not daemonize\n\n**-g**\n> Run in foreground with logging to stderr\n\n**-u _user_**\n> Run as specified user after binding to privileged ports\n\n**-t _directory_**\n> Chroot to specified directory after startup\n\n**-n _cpus_**\n> Number of CPUs to use for worker threads\n\n**-d _debug-level_**\n> Set debug level (higher = more verbose)"],["DESCRIPTION","**named** is the DNS (Domain Name System) server daemon from BIND (Berkeley Internet Name Domain). It resolves domain names to IP addresses and vice versa, serving DNS queries from clients.\n\nThe server reads zone files and configuration from /etc/named.conf by default. It can act as an authoritative server for domains, a recursive resolver, or both. The daemon typically runs as a system service managed by systemd."],["CAVEATS","Requires root privileges to bind to port 53. Should run as unprivileged user (-u) after startup for security. Misconfigured DNS servers can be exploited for amplification attacks. Zone file syntax errors prevent startup."],["HISTORY","**BIND** (Berkeley Internet Name Domain) was originally developed at UC Berkeley in the early 1980s. It is the most widely used DNS server software on the Internet. The Internet Systems Consortium (ISC) currently maintains BIND."],["INSTALL","```apt: sudo apt install bind9```\n\n```dnf: sudo dnf install bind```\n\n```pacman: sudo pacman -S bind```\n\n```apk: sudo apk add bind```\n\n```zypper: sudo zypper install bind```\n\n```brew: brew install bind```\n\n```nix: nix profile install nixpkgs#bind```"],["SEE ALSO","[rndc](/man/rndc)(8), [dig](/man/dig)(1), [nslookup](/man/nslookup)(1), [named.conf](/man/named.conf)(5)"]],"e":["rndc","dig","nslookup","named.conf"],"x":"named"},"netctl":{"n":"netctl","t":"arch Linux's network profile manager","s":[["TAGLINE","arch Linux's network profile manager"],["TLDR","**Start a profile**\n\n```sudo netctl start [profile]```\n\n**Stop a profile**\n\n```sudo netctl stop [profile]```\n\n**Enable profile at boot**\n\n```sudo netctl enable [profile]```\n\n**List all profiles**\n\n```netctl list```\n\n**Check profile status**\n\n```netctl status [profile]```\n\n**Switch profile**\n\n```sudo netctl switch-to [profile]```"],["SYNOPSIS","**netctl** _command_ [_profile_]"],["PARAMETERS","**start** _profile_\n> Start network profile.\n\n**stop** _profile_\n> Stop network profile.\n\n**enable** _profile_\n> Enable at boot.\n\n**disable** _profile_\n> Disable at boot.\n\n**list**\n> List all profiles.\n\n**status** _profile_\n> Show profile status.\n\n**switch-to** _profile_\n> Switch to profile."],["DESCRIPTION","**netctl** is Arch Linux's network profile manager. It manages network configurations through profile files, supporting wired, wireless, bridges, bonds, and VLANs.\n\nnetctl uses systemd and can work alongside NetworkManager (but typically not simultaneously)."],["PROFILE EXAMPLE","```"],["/etc/netctl/ethernet-static","Interface=eth0\nConnection=ethernet\nIP=static\nAddress=('192.168.1.10/24')\nGateway='192.168.1.1'\nDNS=('8.8.8.8')\n```"],["PROFILE LOCATION","```\n/etc/netctl/\n/etc/netctl/examples/\n```"],["CAVEATS","Arch Linux specific. One profile per interface. Don't use with NetworkManager simultaneously."],["HISTORY","netctl was developed for **Arch Linux** as a replacement for netcfg, using systemd integration for network management."],["INSTALL","```pacman: sudo pacman -S netctl```"],["SEE ALSO","[networkctl](/man/networkctl)(1), [ip](/man/ip)(8), [NetworkManager](/man/NetworkManager)(8)"]],"e":["networkctl","ip","NetworkManager"],"x":"sudo netctl start [profile]"},"ntfsclone":{"n":"ntfsclone","t":"Efficiently clone, image, restore, or rescue an NTFS volume","s":[["TAGLINE","Efficiently clone, image, restore, or rescue an NTFS volume"],["TLDR","**Clone NTFS partition to an image file**\n\n```ntfsclone --output [clone.img] [/dev/sda1]```\n\n**Save as space-efficient special image format**\n\n```ntfsclone --save-image --output [clone.img] [/dev/sda1]```\n\n**Restore from special image to a partition**\n\n```ntfsclone --restore-image --output [/dev/sda1] [clone.img]```\n\n**Clone metadata only (for debugging)**\n\n```ntfsclone --metadata --output [meta.img] [/dev/sda1]```\n\n**Clone to stdout and compress with gzip**\n\n```ntfsclone --save-image -o - [/dev/sda1] | gzip -c > [clone.img.gz]```\n\n**Rescue a failing disk (continue on read errors)**\n\n```ntfsclone --rescue --output [rescue.img] [/dev/sda1]```"],["SYNOPSIS","**ntfsclone** [_options_] _source_"],["PARAMETERS","**-o**, **--output** _FILE_\n> Output file or device. Use **-** for standard output.\n\n**-O**, **--overwrite** _FILE_\n> Overwrite an existing file or device (required for writing to partitions).\n\n**-s**, **--save-image**\n> Save to the special ntfsclone image format (only copies used clusters).\n\n**-r**, **--restore-image**\n> Restore from a special ntfsclone image.\n\n**-m**, **--metadata**\n> Clone only NTFS metadata (for debugging; result is still mountable).\n\n**--rescue**\n> Continue on disk read errors, filling bad sectors with zeros.\n\n**--ignore-fs-check**\n> Ignore the result of the filesystem consistency check.\n\n**-f**, **--force**\n> Force cloning even if the volume is marked dirty.\n\n**--help**\n> Display help information."],["DESCRIPTION","**ntfsclone** efficiently clones an NTFS filesystem to a sparse file, special image, device, or standard output. It works at the cluster level and only copies used data, making it much faster and more space-efficient than sector-level tools like **dd**.\n\nThe special image format (**--save-image**) encodes unused space with control codes rather than storing it, producing significantly smaller backup files. These images can only be restored with **ntfsclone --restore-image** and are not directly mountable.\n\nThe **--rescue** mode is designed for dying disks, reading data with minimal stress on the hardware and filling unreadable sectors with zeros."],["CAVEATS","Part of the ntfs-3g package. Special image files are not mountable and can only be restored with ntfsclone. When cloning to a partition, the destination must be at least as large as the source. The volume should be unmounted during cloning."],["HISTORY","**ntfsclone** was developed as part of **ntfs-3g** (formerly ntfsprogs) for efficient NTFS backup and restore on Linux systems."],["INSTALL","```apt: sudo apt install ntfs-3g```\n\n```dnf: sudo dnf install ntfs-3g```\n\n```pacman: sudo pacman -S ntfs-3g```\n\n```apk: sudo apk add ntfs-3g-progs```\n\n```zypper: sudo zypper install ntfs-3g```\n\n```brew: brew install ntfs-3g```"],["SEE ALSO","[ntfsresize](/man/ntfsresize)(1), [ntfs-3g](/man/ntfs-3g)(1), [ntfsfix](/man/ntfsfix)(1), [dd](/man/dd)(1), [partclone](/man/partclone)(1)"]],"e":["ntfsresize","ntfs-3g","ntfsfix","dd","partclone"],"x":"ntfsclone --output [clone.img] [/dev/sda1]"},"paperconf":{"n":"paperconf","t":"displays paper size information","s":[["TAGLINE","displays paper size information"],["TLDR","**Show default paper size**\n\n```paperconf```\n\n**Show paper width**\n\n```paperconf -w```\n\n**Show paper height**\n\n```paperconf -h```\n\n**Show specific paper**\n\n```paperconf -p [a4]```\n\n**Show in millimeters**\n\n```paperconf -m```"],["SYNOPSIS","**paperconf** [_options_] [_paper_]"],["PARAMETERS","**-p** _PAPER_\n> Select the paper name to query (a4, letter, legal, …). Without this, the system default is used.\n\n**-d**\n> Use libpaper's built-in default instead of the system setting.\n\n**-a**\n> Iterate over every known paper.\n\n**-n**\n> Print the paper name.\n\n**-N**\n> Print the paper name with the first letter capitalized.\n\n**-s**\n> Print size as width followed by height.\n\n**-w**\n> Print width.\n\n**-h**\n> Print height.\n\n**-c**\n> Express dimensions in centimetres.\n\n**-m**\n> Express dimensions in millimetres.\n\n**-i**\n> Express dimensions in inches (default is PostScript points).\n\n**-z**\n> Return an error when an unknown paper is given instead of falling back."],["DESCRIPTION","**paperconf** displays paper size information. Queries system paper settings.\n\nThe tool shows dimensions and names. Part of libpaper."],["CAVEATS","Part of libpaper. System paper configuration. Regional defaults."],["HISTORY","paperconf is part of **libpaper** for querying paper size configuration."],["INSTALL","```apt: sudo apt install libpaper-utils```\n\n```dnf: sudo dnf install libpaper```\n\n```pacman: sudo pacman -S libpaper```\n\n```apk: sudo apk add libpaper```\n\n```zypper: sudo zypper install libpaper```\n\n```brew: brew install libpaper```\n\n```nix: nix profile install nixpkgs#libpaper```"],["SEE ALSO","[paperconfig](/man/paperconfig)(8), [lp](/man/lp)(1)"]],"e":["paperconfig","lp"],"x":"paperconf"},"pbmtogo":{"n":"pbmtogo","t":"converts PBM bitmaps to GraphOn terminal graphics format","s":[["TAGLINE","converts PBM bitmaps to GraphOn terminal graphics format"],["TLDR","**Convert a PBM file to GraphOn format**\n\n```pbmtogo [input.pbm] > [output.go]```\n\n**Convert from standard input**\n\n```cat [input.pbm] | pbmtogo > [output.go]```"],["SYNOPSIS","**pbmtogo** [_file_]"],["PARAMETERS","_file_\n> Input PBM file. If not specified, reads from standard input."],["DESCRIPTION","**pbmtogo** reads a PBM (Portable Bitmap) image as input and produces GraphOn graphics format output. GraphOn was a company that made terminal emulators with graphics capabilities. This tool is part of the Netpbm toolkit for image format conversion.\n\nThe output is sent to standard output and can be redirected to a file."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pbm](/man/pbm)(1), [netpbm](/man/netpbm)(1), [pbmtoascii](/man/pbmtoascii)(1), [pbmtoplot](/man/pbmtoplot)(1)"]],"e":["pbm","netpbm","pbmtoascii","pbmtoplot"],"x":"pbmtogo [input.pbm] > [output.go]"},"pcdovtoppm":{"n":"pcdovtoppm","t":"Create an index image for a Kodak Photo CD","s":[["TAGLINE","Create an index image for a Kodak Photo CD"],["TLDR","**Generate an index image** from a Photo CD overview file\n\n```pcdovtoppm [overview.pcd] > [index.ppm]```\n\n**Set maximum width** of the result image\n\n```pcdovtoppm -m 768 [overview.pcd] > [index.ppm]```\n\n**Set thumbnail size** and images per row\n\n```pcdovtoppm -s 96 -a 4 [overview.pcd] > [index.ppm]```\n\n**Use a custom font** for annotations with white background\n\n```pcdovtoppm -f [smallfont.pbm] -w [overview.pcd] > [index.ppm]```"],["SYNOPSIS","**pcdovtoppm** [**-m** _width_] [**-s** _size_] [**-a** _across_] [**-c** _colors_] [**-f** _font_] [**-b**|**-w**] [_pcdfile_]"],["PARAMETERS","**-m** _width_\n> Maximum width of the result image (default: 1152).\n\n**-s** _size_\n> Maximum size of each thumbnail image in pixels (default: 192).\n\n**-a** _across_\n> Maximum number of images per row (default: 6).\n\n**-c** _colors_\n> Maximum number of colors in the output, or **n** to disable quantization.\n\n**-f** _font_\n> PBM font file to use for annotation (default: internal font).\n\n**-b**\n> Use black background (default).\n\n**-w**\n> Use white background."],["DESCRIPTION","**pcdovtoppm** generates an index image in PPM format from a Kodak Photo CD overview file. It arranges the thumbnail images from the Photo CD into a grid, optionally annotated with image numbers using a configurable font. This program was formerly called **pcdindex**. It is part of the **Netpbm** toolkit."],["CAVEATS","Requires a valid Photo CD overview file as input. The output is in PPM format and may need conversion for other uses. Photo CD is an obsolete format from the 1990s."],["HISTORY","**pcdovtoppm** was originally named **pcdindex** and was renamed to conform to Netpbm naming conventions, where converter programs follow the pattern _sourcetodest_."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pcdindex](/man/pcdindex)(1), [ppmtojpeg](/man/ppmtojpeg)(1)"]],"e":["pcdindex","ppmtojpeg"],"x":"pcdovtoppm [overview.pcd] > [index.ppm]"},"pkg-add":{"n":"pkg-add","t":"Install packages on OpenBSD and NetBSD","s":[["TAGLINE","Install packages on OpenBSD and NetBSD"],["TLDR","**Install package from repository**\n\n```pkg_add [package]```\n\n**Install specific package file**\n\n```pkg_add [/path/to/package.tgz]```\n\n**Install from URL**\n\n```pkg_add [https://example.com/package.tgz]```\n\n**Install with verbose output**\n\n```pkg_add -v [package]```"],["SYNOPSIS","**pkg_add** [_options_] _pkg_..."],["PARAMETERS","**-v**\n> Verbose mode.\n\n**-D**\n> Force installation.\n\n**-I**\n> Non-interactive mode.\n\n**-u**\n> Update mode.\n\n**-n**\n> Dry run.\n\n**-r**\n> Replace existing."],["DESCRIPTION","**pkg_add** installs software packages on OpenBSD and NetBSD systems. It handles dependencies, downloads packages from mirrors, and configures installed software.\n\nBSD's traditional package manager."],["EXAMPLES","```bash"],["Install package","pkg_add vim"],["Update package","pkg_add -u vim"],["Install from mirror","export PKG_PATH=https://cdn.openbsd.org/pub/OpenBSD/$(uname -r)/packages/$(uname -m)/\npkg_add firefox"],["Verbose install","pkg_add -v nginx\n```"],["ENVIRONMENT","```bash\nPKG_PATH  # Package repository URL\nPKG_CACHE # Local cache directory\n```"],["CAVEATS","OpenBSD/NetBSD specific. Set PKG_PATH for remote installs. Use pkg_info to list packages."],["HISTORY","pkg_add is part of the **OpenBSD** and **NetBSD** package tools, providing simple package management since the 1990s."],["INSTALL","```nix: nix profile install nixpkgs#pkg```"],["SEE ALSO","[pkg](/man/pkg)(1)"]],"e":["pkg"],"x":"pkg_add [package]"},"pnmtoxwd":{"n":"pnmtoxwd","t":"Convert PNM to X Window Dump format","s":[["TAGLINE","Convert PNM to X Window Dump format"],["TLDR","**Convert PNM to XWD format**\n\n```pnmtoxwd [input.pnm] > [output.xwd]```\n\n**Specify visual class**\n\n```pnmtoxwd -directcolor [input.pnm] > [output.xwd]```"],["SYNOPSIS","**pnmtoxwd** [_options_] [_file_]"],["PARAMETERS","**-pseudocolor**\n> Use PseudoColor visual.\n\n**-directcolor**\n> Use DirectColor visual."],["DESCRIPTION","**pnmtoxwd** converts PNM images to X Window Dump (XWD) format. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[xwdtopnm](/man/xwdtopnm)(1), [pnmtorast](/man/pnmtorast)(1)"]],"e":["xwdtopnm","pnmtorast"],"x":"pnmtoxwd [input.pnm] > [output.xwd]"},"rustfmt":{"n":"rustfmt","t":"Format Rust source code automatically","s":[["TAGLINE","Format Rust source code automatically"],["TLDR","**Format a Rust file**\n\n```rustfmt [main.rs]```\n\n**Format multiple files**\n\n```rustfmt [src/*.rs]```\n\n**Check formatting** without changing files\n\n```rustfmt --check [main.rs]```\n\n**Format with backup**\n\n```rustfmt --backup [main.rs]```\n\n**Format using specific edition**\n\n```rustfmt --edition [2021] [main.rs]```\n\n**Format from stdin**\n\n```cat [main.rs] | rustfmt```\n\n**Show diff** of formatting changes\n\n```rustfmt --emit diff [main.rs]```\n\n**Use config file**\n\n```rustfmt --config-path [rustfmt.toml] [main.rs]```"],["SYNOPSIS","**rustfmt** [_options_] [_files_...]"],["DESCRIPTION","**rustfmt** is the official Rust code formatter. It automatically formats Rust code according to style guidelines, ensuring consistent code style across a project.\n\nThe tool can be integrated into editors, CI pipelines, and pre-commit hooks. Configuration is stored in rustfmt.toml or .rustfmt.toml at the project root.\n\nWhen used with Cargo (`cargo fmt`), it formats all files in the project automatically. rustfmt supports all stable Rust syntax and can be configured for various style preferences."],["PARAMETERS","**--check**\n> Check if formatting is correct, don't modify.\n\n**--backup**\n> Create backup of original files.\n\n**--edition** _year_\n> Rust edition (2015, 2018, 2021, 2024).\n\n**--emit** _type_\n> Output mode (files, stdout, diff).\n\n**--config** _key=value_\n> Set configuration option.\n\n**--config-path** _file_\n> Path to configuration file.\n\n**--print-config** _type_\n> Print configuration (default, current, minimal).\n\n**--files-with-diff**\n> Print files that would change.\n\n**--verbose**\n> Print verbose output.\n\n**--quiet**\n> Print minimal output.\n\n**--color** _when_\n> Color output (auto, always, never)."],["CONFIGURATION","**rustfmt.toml** / **.rustfmt.toml**\n> Project-level configuration file at the project root defining formatting rules such as max_width, tab_spaces, edition, and use_small_heuristics.\n\n**max_width**\n> Maximum line width before wrapping (default 100).\n\n**tab_spaces**\n> Number of spaces per indentation level (default 4).\n\n**edition**\n> Rust edition to use for parsing (2015, 2018, 2021, 2024)."],["CAVEATS","Some configurations are unstable and require nightly. Cannot format macros that produce unparseable output. Comments may be repositioned. Configuration changes may cause large diffs."],["HISTORY","**rustfmt** was developed by **Nick Cameron** starting in **2015** to provide official formatting for Rust code. It reached 1.0 alongside Rust 1.24 in **2018**. The tool was influenced by gofmt and aims to end style debates by providing a single canonical format. It's now maintained by the Rust style team."],["INSTALL","```apt: sudo apt install rustup```\n\n```dnf: sudo dnf install rustup```\n\n```pacman: sudo pacman -S rustup```\n\n```apk: sudo apk add rustfmt```\n\n```zypper: sudo zypper install rustup```\n\n```brew: brew install rustup```\n\n```nix: nix profile install nixpkgs#rustup```"],["SEE ALSO","[cargo-fmt](/man/cargo-fmt)(1), [cargo](/man/cargo)(1), [rustc](/man/rustc)(1), [clippy](/man/clippy)(1)"]],"e":["cargo-fmt","cargo","rustc","clippy"],"x":"rustfmt [main.rs]"},"semgrep":{"n":"semgrep","t":"Lightweight static analysis for code security and quality","s":[["TAGLINE","Lightweight static analysis for code security and quality"],["TLDR","**Scan with auto-detected recommended rules**\n\n```semgrep scan --config auto```\n\n**Scan a specific directory**\n\n```semgrep scan --config auto [path/to/src/]```\n\n**Scan with a specific rule pack from the registry**\n\n```semgrep scan --config p/[security-audit]```\n\n**Scan with a local rules file**\n\n```semgrep scan --config [path/to/rules.yaml] [path/to/src/]```\n\n**Scan with an inline pattern**\n\n```semgrep scan -e '[pattern]' --lang [python]```\n\n**Output results as JSON**\n\n```semgrep scan --json --config auto```\n\n**Scan and auto-apply fixes**\n\n```semgrep scan --config auto --autofix```"],["SYNOPSIS","**semgrep** [_options_] [_targets_...]"],["DESCRIPTION","**semgrep** is a fast, lightweight static analysis tool for finding bugs, detecting security vulnerabilities, and enforcing code standards across 30+ programming languages. Unlike traditional grep, it understands code structure and uses pattern-matching with syntax that resembles the target language, making rules intuitive to write and read.\n\nRules can be sourced from the Semgrep registry using **--config auto** for recommended checks, from curated rule packs like **p/security-audit** for specific categories, or from local YAML files for custom project rules. Inline patterns with **-e** allow quick one-off searches without writing rule files.\n\nOutput formats include text, JSON, SARIF, GitLab SAST, JUnit XML, Emacs, and Vim. The tool integrates naturally into CI/CD pipelines for continuous code quality enforcement."],["PARAMETERS","**--config** _config_\n> Rules configuration: auto, p/ruleset-name, or path to YAML file.\n\n**-e**, **--pattern** _pattern_\n> Inline pattern for ephemeral rules (requires --lang).\n\n**--lang** _language_\n> Target language for inline pattern matching.\n\n**--json**\n> Output results in JSON format.\n\n**--sarif**\n> Output results in SARIF format (for GitHub Advanced Security, etc.).\n\n**--autofix**\n> Apply automatic fixes for findings that have suggested fixes.\n\n**--exclude** _pattern_\n> Exclude files or directories matching pattern. Can be specified multiple times.\n\n**--include** _pattern_\n> Only scan files matching pattern. Can be specified multiple times.\n\n**--exclude-rule** _id_\n> Skip rules with the given ID. Can be specified multiple times.\n\n**-o**, **--output** _file_\n> Save results to file or post to URL.\n\n**--severity** _level_\n> Filter by minimum severity (INFO, WARNING, ERROR).\n\n**--error**\n> Exit with code 1 if any findings (default: exit 0).\n\n**-j**, **--jobs** _N_\n> Number of parallel scanning jobs (default: 3).\n\n**--timeout** _seconds_\n> Maximum time per file (default: 5 seconds).\n\n**-v**, **--verbose**\n> Show more details about rules and parsing.\n\n**--debug**\n> All verbose output plus debugging information."],["CAVEATS","Rule quality varies across registries; false positives require tuning with `--exclude-rule` or nosemgrep comments. Custom rules have a learning curve. Very large codebases may be slow without `--include` filters. The `--autofix` option modifies files in place, so review changes before committing."],["HISTORY","**semgrep** was developed by **r2c** (now Semgrep Inc.) and released around **2019**. It built on academic research to create a practical, language-aware grep for code analysis."],["INSTALL","```brew: brew install semgrep```\n\n```nix: nix profile install nixpkgs#semgrep```"],["SEE ALSO","[grep](/man/grep)(1), [ast-grep](/man/ast-grep)(1), [eslint](/man/eslint)(1), [pylint](/man/pylint)(1)"]],"e":["grep","ast-grep","eslint","pylint"],"x":"semgrep scan --config auto"},"slurmstepd":{"n":"slurmstepd","t":"Slurm job step manager daemon","s":[["TAGLINE","Slurm job step manager daemon"],["TLDR","Start the daemon (typically **invoked automatically**)\n\n```slurmstepd```"],["SYNOPSIS","**slurmstepd**"],["DESCRIPTION","**slurmstepd** is the Slurm job step manager daemon. It is automatically spawned by **slurmd** when a job step launches and terminates when the job step completes. The daemon should not be manually invoked by users or administrators.\n\nThe daemon manages I/O streams (stdin, stdout, stderr) for job steps, tracks accounting information including CPU time, memory usage, and I/O statistics, and processes signals directed at job steps. Each job step within a multi-step job has its own slurmstepd process."],["SIGNALS","**SIGINT, SIGTERM, SIGQUIT**\n> Trigger graceful shutdown\n\n**SIGPROF**\n> Log connection manager status (debug level info or higher)\n\n**SIGTSTP, SIGPIPE, SIGUSR1, SIGUSR2, SIGALRM, SIGHUP**\n> Explicitly ignored"],["CAVEATS","Should never be manually started; it is spawned automatically by slurmd. Multiple slurmstepd processes may run simultaneously for multi-step jobs. Killing slurmstepd directly may leave job steps in inconsistent states; use **scancel** instead."],["HISTORY","**slurmstepd** is part of **Slurm** (Simple Linux Utility for Resource Management), developed at **Lawrence Livermore National Laboratory** starting in **2002**. The step daemon architecture allows efficient management of complex jobs with multiple steps, each with potentially different resource requirements. Slurm is maintained by **SchedMD**."],["SEE ALSO","[slurmd](/man/slurmd)(8), [srun](/man/srun)(1), [scancel](/man/scancel)(1), [sstat](/man/sstat)(1)"]],"e":["slurmd","srun","scancel","sstat"],"x":"slurmstepd"},"sprio":{"n":"sprio","t":"Slurm job scheduling priority viewer","s":[["TAGLINE","Slurm job scheduling priority viewer"],["TLDR","View priority factors for **all pending jobs**\n\n```sprio```\n\nView priority for **specific jobs**\n\n```sprio -j [job_id1,job_id2]```\n\nShow **extended** information\n\n```sprio -l```\n\nView jobs for **specific users**\n\n```sprio -u [user1,user2]```\n\nPrint **priority weights**\n\n```sprio -w```\n\nShow **normalized** priority factors\n\n```sprio -n```"],["SYNOPSIS","**sprio** [_OPTIONS_...]"],["PARAMETERS","**-j, --jobs** _job_ids_\n> Show priority for specific job IDs (comma-separated)\n\n**-u, --user** _users_\n> Filter by user names or IDs (comma-separated)\n\n**-p, --partition** _partitions_\n> Restrict to specific partitions\n\n**-l, --long**\n> Display extended output information\n\n**-n, --norm**\n> Show normalized priority factors (0.0-1.0)\n\n**-w, --weights**\n> Display configured priority weight values\n\n**-o, --format** _format_\n> Customize output format\n\n**-S, --sort** _fields_\n> Sort results by specified fields\n\n**-h, --noheader**\n> Suppress column headers\n\n**-M, --clusters** _names_\n> Target specific clusters\n\n**--federation**\n> Display jobs from all federated clusters\n\n**--local**\n> Show only local cluster jobs\n\n**-v, --verbose**\n> Verbose output\n\n**-V, --version**\n> Display version"],["DESCRIPTION","**sprio** displays the components that determine job scheduling priority when Slurm's multi-factor priority plugin is active. It shows how factors like age, fairshare, job size, partition, and QOS contribute to each job's overall priority score.\n\nBy default, it displays information for all pending jobs. The tool helps administrators and users understand why certain jobs are scheduled before others and troubleshoot scheduling issues."],["FORMAT SPECIFIERS","**%i**: Job ID\n**%Y**: Priority\n**%A**: Age factor\n**%F**: Fairshare factor\n**%J**: Job size factor\n**%P**: Partition factor\n**%Q**: QOS factor\n**%u**: User name"],["CAVEATS","Only works when the multi-factor priority plugin is configured in Slurm. Read-only utility; cannot modify priorities. Priority values are relative and depend on cluster configuration. Some factors may be zero if not configured."],["HISTORY","**sprio** is part of **Slurm** (Simple Linux Utility for Resource Management), developed at **Lawrence Livermore National Laboratory** starting in **2002**. The multi-factor priority plugin was added to provide fair and configurable job scheduling in HPC environments. Slurm is now maintained by **SchedMD**."],["INSTALL","```apt: sudo apt install slurm-client```"],["SEE ALSO","[squeue](/man/squeue)(1), [scontrol](/man/scontrol)(1), [sacct](/man/sacct)(1), [sprio](/man/sprio)(1)"]],"e":["squeue","scontrol","sacct","sprio"],"x":"sprio"},"standard":{"n":"standard","t":"Zero-configuration JavaScript style linter","s":[["TAGLINE","Zero-configuration JavaScript style linter"],["TLDR","**Check code**\n\n```standard```\n\n**Fix automatically**\n\n```standard --fix```\n\n**Check specific files**\n\n```standard [file1.js] [file2.js]```\n\n**Use parser**\n\n```standard --parser [babel-eslint]```\n\n**Verbose output**\n\n```standard --verbose```\n\n**Declare globals**\n\n```standard --global [globalName]```"],["SYNOPSIS","**standard** [_--fix_] [_--parser name_] [_options_] [_files_]"],["PARAMETERS","**--fix**\n> Auto-fix issues.\n\n**--parser** _NAME_\n> Custom parser.\n\n**--verbose**\n> Show rule names.\n\n**--global** _NAME_\n> Declare a global variable (can be repeated).\n\n**--plugin** _NAME_\n> Include an ESLint plugin.\n\n**--fix-dry-run**\n> Report what would be fixed without writing changes.\n\n**--env** _ENV_\n> Environment globals.\n\n**--stdin**\n> Lint from stdin."],["DESCRIPTION","**standard** is a zero-configuration JavaScript linter and formatter that enforces a fixed set of style rules. Built on top of ESLint, it eliminates the need for project-specific .eslintrc files by providing a single, opinionated style guide that includes rules like no semicolons, 2-space indentation, and single quotes.\n\nThe **--fix** flag automatically corrects fixable issues in place. The tool can lint files passed as arguments or recursively scan the current directory. Custom parsers like babel-eslint can be specified for projects using experimental JavaScript syntax."],["CAVEATS","Fixed style rules. No customization. Semicolon-free style."],["HISTORY","**standard** was created by **Feross Aboukhadijeh** as a zero-configuration JavaScript linter enforcing a single style."],["INSTALL","```brew: brew install standard```"],["SEE ALSO","[eslint](/man/eslint)(1), [prettier](/man/prettier)(1), [jshint](/man/jshint)(1)"]],"e":["eslint","prettier","jshint"],"x":"standard"},"stdbuf":{"n":"stdbuf","t":"Adjust stream buffering of commands","s":[["TAGLINE","Adjust stream buffering of commands"],["TLDR","**Line buffered stdout**\n\n```stdbuf -oL [command]```\n\n**Unbuffered stdout**\n\n```stdbuf -o0 [command]```\n\n**Unbuffered stderr**\n\n```stdbuf -e0 [command]```\n\n**Fully buffered stdin**\n\n```stdbuf -i[1M] [command]```\n\n**Line buffered all streams**\n\n```stdbuf -oL -eL [command]```\n\n**Combine with grep**\n\n```stdbuf -oL [command] | grep [pattern]```"],["SYNOPSIS","**stdbuf** [_-i mode_] [_-o mode_] [_-e mode_] _command_ [_args_]"],["PARAMETERS","**-i** _MODE_\n> stdin buffering.\n\n**-o** _MODE_\n> stdout buffering.\n\n**-e** _MODE_\n> stderr buffering."],["MODES","**L** - Line buffered\n**0** - Unbuffered\n**SIZE** - Buffer size (e.g., 1K, 1M)"],["DESCRIPTION","**stdbuf** runs a command with modified standard stream buffering. When programs detect their output is going to a pipe rather than a terminal, they typically switch from line-buffered to fully-buffered output, which delays output. stdbuf overrides this behavior using LD_PRELOAD to intercept buffering calls.\n\nThe three modes are: line-buffered (**L**) which flushes after each newline, unbuffered (**0**) which flushes immediately, and a specific buffer size. This is particularly useful when piping output through filters like **grep** or **awk** where real-time output is needed rather than waiting for the buffer to fill."],["CAVEATS","Doesn't work with all programs. Statically linked programs unaffected. Some use internal buffering."],["HISTORY","**stdbuf** is part of GNU coreutils. It uses LD_PRELOAD to intercept buffering calls."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[unbuffer](/man/unbuffer)(1), [script](/man/script)(1), [tee](/man/tee)(1)"]],"e":["unbuffer","script","tee"],"x":"stdbuf -oL [command]"},"tabby":{"n":"tabby","t":"Self-hosted AI coding assistant","s":[["TAGLINE","Self-hosted AI coding assistant"],["TLDR","**Start Tabby server** with GPU acceleration\n\n```tabby serve --model [StarCoder-1B] --device cuda```\n\n**Start server with chat model**\n\n```tabby serve --model [StarCoder-1B] --chat-model [Qwen2-1.5B-Instruct] --device cuda```\n\n**Run with CPU** only\n\n```tabby serve --model [StarCoder-1B] --device cpu```\n\n**Run via Docker** with GPU support\n\n```docker run -it --gpus all -p 8080:8080 -v $HOME/.tabby:/data tabbyml/tabby serve --model [StarCoder-1B] --device cuda```\n\n**Specify port** for the server\n\n```tabby serve --model [StarCoder-1B] --port [8080]```"],["SYNOPSIS","**tabby** serve [**--model** _name_] [**--chat-model** _name_] [**--device** _type_] [**--port** _port_]"],["PARAMETERS","**--model** _name_\n> Code completion model to use (e.g., StarCoder-1B, CodeLlama-7B).\n\n**--chat-model** _name_\n> Conversational AI model for chat features (e.g., Qwen2-1.5B-Instruct).\n\n**--device** _type_\n> Hardware acceleration: cuda (NVIDIA GPU), metal (Apple M1/M2), cpu.\n\n**--port** _port_\n> Port to expose the API server. Default: 8080.\n\n**--help**\n> Display help information."],["DESCRIPTION","**tabby** is a self-hosted AI coding assistant that provides code completion, inline edits, and chat capabilities. Unlike cloud-hosted alternatives, Tabby runs entirely on your own infrastructure, giving you full control over models, data, and costs.\n\nThe **serve** command starts the Tabby API server, which exposes an OpenAPI-compatible interface for IDE extensions and other clients. The server supports multiple code completion models including StarCoder, CodeLlama, and CodeGen families.\n\nTabby is optimized for consumer-grade GPUs and supports NVIDIA CUDA for Linux/Windows and Apple Metal for macOS. CPU-only mode is available for environments without GPU acceleration, though with reduced performance.\n\nData is stored in **~/.tabby** by default, including model weights, configuration, and indexed code repositories. The server provides a web UI at the configured port for administration, model management, and repository indexing."],["CAVEATS","GPU acceleration requires appropriate drivers (CUDA toolkit for NVIDIA, Metal for Apple Silicon). Model downloads can be several gigabytes depending on the selected model. Self-hosting requires adequate hardware resources; recommended minimum is 8GB VRAM for GPU mode or 16GB RAM for CPU mode."],["HISTORY","Tabby was created by TabbyML and released as open-source in **2023**, positioning itself as a self-hosted alternative to GitHub Copilot. The project gained traction among enterprises and developers seeking data privacy and cost control. Version 0.24 in **February 2025** added LDAP authentication, and version 0.30 in **July 2025** introduced GitLab merge request context indexing."],["INSTALL","```nix: nix profile install nixpkgs#tabby```"],["SEE ALSO","[ollama](/man/ollama)(1), [copilot](/man/copilot)(1), [docker](/man/docker)(1)"]],"e":["ollama","copilot","docker"],"x":"tabby serve --model [StarCoder-1B] --device cuda"},"toot":{"n":"toot","t":"Mastodon CLI and TUI client","s":[["TAGLINE","Mastodon CLI and TUI client"],["TLDR","**Launch the TUI**\n\n```toot tui```\n\n**Post a status**\n\n```toot post \"[your message]\"```\n\n**Log in to a Mastodon instance**\n\n```toot login```"],["SYNOPSIS","**toot** _command_ [_options_]"],["DESCRIPTION","**toot** is a CLI and TUI client for interacting with Mastodon and compatible Fediverse instances. It supports posting, replying, and deleting statuses, media uploads, spoiler text, searching by account or hashtag, following, muting and blocking accounts, and switching between multiple authenticated accounts."],["HISTORY","**toot** was created by **Ivan Habunek** (ihabunek) and is written in **Python**."],["INSTALL","```dnf: sudo dnf install toot```\n\n```pacman: sudo pacman -S toot```\n\n```apk: sudo apk add toot```\n\n```brew: brew install toot```\n\n```nix: nix profile install nixpkgs#toot```"],["SEE ALSO","[tuisky](/man/tuisky)(1), [servitor](/man/servitor)(1)"]],"e":["tuisky","servitor"],"x":"toot tui"},"udevmon":{"n":"udevmon","t":"Input device event monitor and interceptor","s":[["TAGLINE","Input device event monitor and interceptor"],["TLDR","Start with **config** file\n\n```udevmon -c [path/to/config.yaml]```"],["SYNOPSIS","**udevmon** [_OPTIONS_]"],["PARAMETERS","**-c** _FILE_\n> Use specified configuration file"],["DESCRIPTION","**udevmon** intercepts and monitors input devices for launching tasks. It filters or modifies input events according to configuration files, typically located in /etc/interception/udevmon.d/*.yaml.\n\nThis tool is part of the Interception Tools suite, enabling advanced keyboard and mouse event processing like key remapping at a low level."],["CAVEATS","Requires root privileges. Configuration files must be valid YAML. Part of the Interception Tools project. Running incorrectly can disrupt input devices."],["INSTALL","```apt: sudo apt install interception-tools```\n\n```pacman: sudo pacman -S interception-tools```\n\n```nix: nix profile install nixpkgs#interception-tools```"],["SEE ALSO","[uinput](/man/uinput)(1)"]],"e":["uinput"],"x":"udevmon -c [path/to/config.yaml]"},"vkcube":{"n":"vkcube","t":"Vulkan spinning cube demo application","s":[["TAGLINE","Vulkan spinning cube demo application"],["TLDR","**Run Vulkan demo**\n\n```vkcube```\n\n**With frame count**\n\n```vkcube --c [1000]```\n\n**Present mode**\n\n```vkcube --present_mode [0]```\n\n**GPU select**\n\n```vkcube --gpu [0]```"],["SYNOPSIS","**vkcube** [_--c frames_] [_--present_mode mode_] [_options_]"],["PARAMETERS","**--c** _FRAMES_\n> Frame count.\n\n**--present_mode** _MODE_\n> Presentation mode.\n\n**--gpu** _INDEX_\n> GPU selection.\n\n**--width** _W_\n> Window width.\n\n**--height** _H_\n> Window height."],["DESCRIPTION","**vkcube** is a simple demo application from the Vulkan SDK that displays a spinning 3D cube rendered using the Vulkan graphics API. It serves as a quick verification tool to confirm that Vulkan drivers and libraries are correctly installed and functioning on the system.\n\nThe application opens a window showing a textured cube rotating continuously. If the cube renders and spins smoothly, it indicates that the GPU, Vulkan driver, and display system are working together properly. Options allow selecting a specific GPU, setting the presentation mode, and limiting the number of rendered frames.\n\nAs part of the official Vulkan-Tools package maintained by the Khronos Group, vkcube is the C implementation of the demo (vkcubepp is the C++ version). It is commonly used as a first diagnostic step when troubleshooting Vulkan support on Linux systems."],["CAVEATS","Vulkan drivers needed. GPU required. Display required."],["HISTORY","**vkcube** is part of the **Vulkan SDK**, providing a simple test application for Vulkan functionality."],["INSTALL","```apt: sudo apt install vulkan-tools```\n\n```dnf: sudo dnf install vulkan-tools```\n\n```pacman: sudo pacman -S vulkan-tools```\n\n```apk: sudo apk add vulkan-tools```\n\n```zypper: sudo zypper install vulkan-tools```\n\n```brew: brew install vulkan-tools```\n\n```nix: nix profile install nixpkgs#vulkan-tools```"],["SEE ALSO","[vulkaninfo](/man/vulkaninfo)(1), [glxgears](/man/glxgears)(1), [vkcubepp](/man/vkcubepp)(1)"]],"e":["vulkaninfo","glxgears","vkcubepp"],"x":"vkcube"},"warp-diag":{"n":"warp-diag","t":"Cloudflare WARP diagnostic and troubleshooting tool","s":[["TAGLINE","Cloudflare WARP diagnostic and troubleshooting tool"],["TLDR","**Run full diagnostics** and create a zip report\n\n```warp-diag```\n\n**Run diagnostics with a timestamp** in the output filename\n\n```warp-diag --add-ts```\n\n**Run diagnostics and save** to a specific directory\n\n```warp-diag --output [path/to/directory]```\n\n**Submit feedback** to Cloudflare interactively\n\n```warp-diag feedback```"],["SYNOPSIS","**warp-diag** [_options_] [_subcommand_]"],["PARAMETERS","**--output** _directory_\n> Save diagnostic zip file to the specified directory.\n\n**--add-ts**\n> Add a timestamp to the output filename.\n\n**feedback**\n> Submit feedback to Cloudflare interactively."],["DESCRIPTION","**warp-diag** collects diagnostic information for the Cloudflare WARP client and packages it into a zip file. It gathers WARP client logs, system network configuration, DNS settings, connectivity test results, and client state to help troubleshoot connection issues.\n\nThe generated zip file can be shared with Cloudflare support for analysis."],["CAVEATS","Requires the Cloudflare WARP client (warp-svc) to be installed. Some diagnostics may require root/administrator access. The zip file may contain sensitive network configuration details."],["SEE ALSO","[warp-cli](/man/warp-cli)(1)"]],"e":["warp-cli"],"x":"warp-diag"},"zerotier-cli":{"n":"zerotier-cli","t":"Manage ZeroTier virtual network connections","s":[["TAGLINE","Manage ZeroTier virtual network connections"],["TLDR","**Show node status**\n\n```sudo zerotier-cli status```\n\n**Join a network**\n\n```sudo zerotier-cli join [network_id]```\n\n**Leave a network**\n\n```sudo zerotier-cli leave [network_id]```\n\n**List joined networks**\n\n```sudo zerotier-cli listnetworks```\n\n**List connected peers**\n\n```sudo zerotier-cli listpeers```\n\n**Show node info**\n\n```sudo zerotier-cli info```"],["SYNOPSIS","**zerotier-cli** [_options_] _command_ [_args_]"],["PARAMETERS","**-D** _path_\n> ZeroTier home directory\n\n**-p** _port_\n> Local API port\n\n**-T** _token_\n> Authentication token"],["SUBCOMMANDS","**status**\n> Show node status and address\n\n**info**\n> Display node information\n\n**join** _network_\n> Join a ZeroTier network\n\n**leave** _network_\n> Leave a ZeroTier network\n\n**listnetworks**\n> List joined networks\n\n**listpeers**\n> List connected peers\n\n**listmoons**\n> List joined moons (custom roots)\n\n**orbit** _world_id_ _seed_\n> Join a moon\n\n**deorbit** _world_id_\n> Leave a moon\n\n**set** _network_ _setting_ _value_\n> Configure network settings"],["DESCRIPTION","**zerotier-cli** is the command-line interface for managing ZeroTier One, a software-defined networking (SDN) service that creates secure virtual networks. It communicates with the local zerotier-one service.\n\nZeroTier creates encrypted peer-to-peer connections between nodes, forming virtual LANs across the internet. Each node gets a unique 10-digit address. Networks are identified by 16-digit network IDs.\n\nAfter joining a network, nodes can communicate as if on the same LAN, enabling remote access, gaming, and connecting distributed systems. Network membership is managed through ZeroTier Central or self-hosted controllers."],["CAVEATS","Most commands require root/sudo to communicate with the zerotier-one service.\n\nJoining a network requires authorization from the network controller (automatic for public networks, manual for private).\n\nFirewall rules may need adjustment to allow ZeroTier traffic (UDP port 9993 by default).\n\nNetwork performance depends on whether direct peer connections can be established or traffic must relay."],["SEE ALSO","[zerotier-one](/man/zerotier-one)(8), [zerotier-idtool](/man/zerotier-idtool)(1), [ip](/man/ip)(8), [wg](/man/wg)(8)"]],"e":["zerotier-one","zerotier-idtool","ip","wg"],"x":"sudo zerotier-cli status"}});
