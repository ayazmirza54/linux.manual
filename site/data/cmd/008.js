window.LCL_BUCKET(8,{"abrt-cli":{"n":"abrt-cli","t":"command-line interface for automatic bug reporting","s":[["TAGLINE","command-line interface for automatic bug reporting"],["TLDR","**List all detected problems**\n\n```abrt-cli list```\n\n**List only unreported problems**\n\n```abrt-cli list -n```\n\n**Show detailed info about a specific problem**\n\n```abrt-cli info [problem_dir]```\n\n**Report a problem to the configured bug tracker**\n\n```abrt-cli report [problem_dir]```\n\n**Remove a crash report**\n\n```abrt-cli remove [problem_dir]```\n\n**Show count of detected problems**\n\n```abrt-cli status```\n\n**Enable or disable auto-reporting**\n\n```abrt-cli auto-report [on|off]```"],["SYNOPSIS","**abrt-cli** _command_ [_options_] [_problem_dir_]"],["DESCRIPTION","**abrt-cli** is the command-line interface for the Automatic Bug Reporting Tool (ABRT) used on Fedora and RHEL systems. It manages detected application crashes, kernel oopses, and other software defects, allowing users to list, inspect, report, and remove problem data.\n\nCrash information includes identifiers, timestamps, package names, crash reasons, and report status/URLs when applicable."],["PARAMETERS","**list** [**-n**] [**--detailed**] [**--since** _NUM_] [**--until** _NUM_]\n> Display detected crashes. **-n** shows only unreported problems. **--detailed** shows full report. **--since**/**--until** filter by timestamp.\n\n**info** [**-d**] [**-s** _SIZE_] _PROBLEM_DIR_\n> Show details of a specific problem. **-d** for detailed output. **-s** abridges text larger than SIZE bytes.\n\n**report** [**--delete**] [**--unsafe**] _PROBLEM_DIR_\n> Submit a problem for analysis and reporting. **--delete** removes the problem dir after reporting. **--unsafe** ignores security checks.\n\n**remove** _PROBLEM_DIR_\n> Delete a problem from the system.\n\n**status** [**-b**] [**--since** _NUM_]\n> Show count of detected problems. **-b** prints only the count (bare mode).\n\n**process** [**--since** _NUM_] [**--unsafe**] _PROBLEM_DIR_\n> Analyze and report problems in batch.\n\n**auto-report** [**on**|**off**]\n> Enable or disable automatic reporting of detected problems.\n\n**-v**, **--verbose**\n> Increase verbosity.\n\n**-V**, **--version**\n> Show version information."],["CAVEATS","The reporting workflow opens a text editor. Editor selection uses environment variables in order: **$ABRT_EDITOR**, **$VISUAL**, **$EDITOR**, or defaults to **vi**. The ABRT daemon must be running for new problems to be detected."],["HISTORY","Part of **ABRT** (Automatic Bug Reporting Tool), developed by **Red Hat** for Fedora-based systems starting around **2009** as a replacement for bug-buddy."],["INSTALL","```dnf: sudo dnf install abrt-cli```"],["SEE ALSO","[abrt](/man/abrt)(1), [abrt-action-analyze-backtrace](/man/abrt-action-analyze-backtrace)(1), [abrt-action-analyze-c](/man/abrt-action-analyze-c)(1), [coredumpctl](/man/coredumpctl)(1), [journalctl](/man/journalctl)(1)"]],"e":["abrt","abrt-action-analyze-backtrace","abrt-action-analyze-c","coredumpctl","journalctl"],"x":"abrt-cli list"},"apg":{"n":"apg","t":"Generate random passwords","s":[["TAGLINE","Generate random passwords"],["TLDR","**Generate** random passwords\n\n```apg```\n\nGenerate with **specific length**\n\n```apg -m [16] -x [20]```\n\nGenerate **pronounceable** passwords\n\n```apg -a 0```\n\nGenerate **random character** passwords\n\n```apg -a 1```\n\nGenerate with **specific character set**\n\n```apg -M SNCL```\n\nGenerate without **ambiguous characters**\n\n```apg -E \"0O1lI\"```"],["SYNOPSIS","**apg** [_-a algorithm_] [_-m min_] [_-x max_] [_-M mode_] [_-E chars_] [_options_]"],["DESCRIPTION","**apg** (Automated Password Generator) creates random passwords using either a pronounceable password algorithm (based on phoneme sequences) or completely random characters.\n\nPronounceable passwords are easier to remember but potentially less secure. Random passwords offer maximum entropy but are harder to memorize."],["PARAMETERS","**-a** _algorithm_\n> Algorithm: 0=pronounceable, 1=random\n\n**-m** _length_\n> Minimum password length\n\n**-x** _length_\n> Maximum password length\n\n**-n** _count_\n> Number of passwords to generate\n\n**-M** _mode_\n> Character classes: S=special, N=numeric, C=capital, L=lowercase. Prefix with uppercase to require the class.\n\n**-E** _chars_\n> Exclude specific characters\n\n**-r** _file_\n> Check against dictionary file for password filtering\n\n**-t**\n> Print pronunciation for pronounceable passwords\n\n**-q**\n> Quiet mode (passwords only)\n\n**-s**\n> Prompt for random seed from keyboard input\n\n**-l**\n> Spell generated passwords phonetically"],["CAVEATS","Pronounceable passwords may have patterns that reduce entropy. For high-security applications, use random mode with sufficient length. Output goes to terminal; pipe to clip for clipboard."],["HISTORY","**apg** was created by Adel I. Mirzazhanov, implementing both random and pronounceable password generation algorithms for Unix systems."],["INSTALL","```apt: sudo apt install apg```\n\n```dnf: sudo dnf install apg```\n\n```aur: yay -S apg```\n\n```apk: sudo apk add apg```\n\n```zypper: sudo zypper install apg```\n\n```nix: nix profile install nixpkgs#apg```"],["SEE ALSO","[pwgen](/man/pwgen)(1), [openssl](/man/openssl)(1), [mkpasswd](/man/mkpasswd)(1)"]],"e":["pwgen","openssl","mkpasswd"],"x":"apg"},"apt-add-repository":{"n":"apt-add-repository","t":"Add package repositories to APT sources","s":[["TAGLINE","Add package repositories to APT sources"],["TLDR","This command is an alias of **add-apt-repository**."],["SYNOPSIS","**apt-add-repository** [_options_] _repository_"],["DESCRIPTION","**apt-add-repository** is an alternative name for **add-apt-repository**, used to add APT repositories to the system's sources list on Debian-based distributions."],["INSTALL","```apt: sudo apt install software-properties-common```"],["SEE ALSO","[add-apt-repository](/man/add-apt-repository)(1), [apt](/man/apt)(8), [apt-get](/man/apt-get)(8)"]],"e":["add-apt-repository","apt","apt-get"],"x":""},"aptitude":{"n":"aptitude","t":"Advanced package management with dependency resolution","s":[["TAGLINE","Advanced package management with dependency resolution"],["TLDR","**Synchronize** list of packages and versions\n\n```sudo aptitude update```\n\n**Install** a new package\n\n```sudo aptitude install [package]```\n\n**Search** for a package\n\n```aptitude search [package]```\n\nSearch for an **installed** package\n\n```aptitude search '?installed([package])'```\n\n**Remove** a package and all packages depending on it\n\n```sudo aptitude remove [package]```\n\n**Upgrade** installed packages\n\n```sudo aptitude upgrade```\n\n**Full upgrade** including removing obsolete packages\n\n```sudo aptitude full-upgrade```\n\n**Simulate** an install without making changes\n\n```sudo aptitude install -s [package]```\n\n**Show why** a package is installed\n\n```aptitude why [package]```\n\n**Hold** a package to prevent automatic upgrades\n\n```sudo aptitude hold '?installed([package])'```"],["SYNOPSIS","**aptitude** [_options_] _command_ [_arguments_]"],["DESCRIPTION","**aptitude** is a powerful package management utility for Debian and Ubuntu systems. It provides both a command-line interface and an interactive ncurses-based interface with advanced dependency resolution and search capabilities."],["PARAMETERS","**update**\n> Synchronize package index files from sources\n\n**install package**\n> Install or upgrade specified packages\n\n**remove package**\n> Remove packages and dependent packages\n\n**purge package**\n> Remove packages, configurations, and dependent packages\n\n**upgrade**\n> Upgrade installed packages to newest versions\n\n**full-upgrade**\n> Upgrade packages, removing obsolete ones as needed\n\n**search pattern**\n> Search for packages matching pattern\n\n**show package**\n> Display detailed package information\n\n**hold package**\n> Prevent package from being upgraded\n\n**unhold package**\n> Allow package to be upgraded\n\n**safe-upgrade**\n> Upgrade packages conservatively\n\n**clean**\n> Remove downloaded package files\n\n**autoclean**\n> Remove old downloaded package files\n\n**why package**\n> Explain why a package is installed or required\n\n**why-not package**\n> Explain why a package cannot be installed\n\n**download package**\n> Download .deb file without installing\n\n**reinstall package**\n> Reinstall an already-installed package\n\n**versions package**\n> List available versions of a package\n\n**forbid-version package**\n> Prevent upgrade to a specific package version\n\n**markauto package**\n> Mark package as automatically installed\n\n**unmarkauto package**\n> Mark package as manually installed\n\n**-s**, **--simulate**\n> Preview changes without applying them\n\n**-y**, **--assume-yes**\n> Skip confirmation prompts\n\n**-D**, **--show-deps**\n> Show explanations for automatic installations and removals\n\n**-V**, **--show-versions**\n> Display package version details in listings\n\n**-P**, **--prompt**\n> Always display a confirmation prompt before changes\n\n**-R**, **--without-recommends**\n> Do not treat recommended packages as dependencies\n\n**-t** _release_\n> Set the target release for package installation\n\n**-q**, **--quiet**\n> Suppress progress indicators\n\n**changelog** _package_\n> Download and display the changelog for a package"],["CONFIGURATION","**~/.aptitude/config**\n> User-specific aptitude configuration.\n\n**/etc/apt/apt.conf**\n> APT configuration shared with apt and apt-get.\n\n**/etc/apt/apt.conf.d/**\n> Directory for additional APT configuration fragments."],["CAVEATS","Aptitude uses its own search syntax with special terms like **?installed**, **?automatic**, etc. Different from apt-cache search syntax."],["HISTORY","Created as an alternative to apt-get with more advanced features. First released for Debian in **2001** by Daniel Burrows."],["INSTALL","```apt: sudo apt install aptitude```"],["SEE ALSO","[apt](/man/apt)(8), [apt-get](/man/apt-get)(8), [dpkg](/man/dpkg)(1), [nala](/man/nala)(8)"]],"e":["apt","apt-get","dpkg","nala"],"x":"sudo aptitude update"},"arp-scan":{"n":"arp-scan","t":"Discover hosts on local networks via ARP requests.","s":[["TAGLINE","Discover hosts on local networks via ARP requests."],["TLDR","**Scan** local network\n\n```sudo arp-scan -l```\n\nScan **specific subnet**\n\n```sudo arp-scan [192.168.1.0/24]```\n\nScan with **interface**\n\n```sudo arp-scan -I [eth0] -l```\n\nScan a specific **IP range**\n\n```sudo arp-scan [192.168.1.1-192.168.1.50]```\n\n**Quiet** output (responding hosts only)\n\n```sudo arp-scan -l -q```\n\nScan using a **custom source** MAC address\n\n```sudo arp-scan -l --srcaddr=[00:11:22:33:44:55]```"],["SYNOPSIS","**arp-scan** [_-l_] [_-I interface_] [_options_] [_targets_]"],["DESCRIPTION","**arp-scan** discovers hosts on a local network by sending ARP requests. It's faster than ping-based scanning because ARP works at layer 2 and hosts cannot easily hide from it.\n\nThe tool shows IP addresses, MAC addresses, and optionally vendor names from the IEEE OUI database."],["PARAMETERS","**-l**, **--localnet**\n> Scan all addresses on local network\n\n**-I** _interface_\n> Network interface to use\n\n**-q**, **--quiet**\n> Quiet mode (only show responding hosts)\n\n**-r** _n_, **--retry**=_n_\n> Number of retries per host (default 2).\n\n**-t** _ms_, **--timeout**=_ms_\n> Timeout in milliseconds per host (default 500).\n\n**-B**, **--bandwidth**=_bps_\n> Limit outbound bandwidth (e.g., 256000).\n\n**-x**, **--ignoredups**\n> Ignore duplicate ARP responses.\n\n**--srcaddr**=_mac_\n> Set the source MAC address in outgoing packets.\n\n**-D**, **--destaddr**=_mac_\n> Set the destination MAC address (default ff:ff:ff:ff:ff:ff)."],["CONFIGURATION","**/etc/arp-scan/mac-vendor.txt**\n> MAC address to vendor name mapping database used for OUI resolution."],["CAVEATS","Requires root/sudo for raw socket access. Only works on local network segment (can't scan across routers). Some hosts may rate-limit ARP responses."],["HISTORY","**arp-scan** was written by Roy Hills and has been maintained since the early **2000s** as a reliable network discovery tool."],["INSTALL","```apt: sudo apt install arp-scan```\n\n```dnf: sudo dnf install arp-scan```\n\n```pacman: sudo pacman -S arp-scan```\n\n```apk: sudo apk add arp-scan```\n\n```zypper: sudo zypper install arp-scan```\n\n```brew: brew install arp-scan```\n\n```nix: nix profile install nixpkgs#arp-scan```"],["SEE ALSO","[arp](/man/arp)(8), [arping](/man/arping)(8), [nmap](/man/nmap)(1)"]],"e":["arp","arping","nmap"],"x":"sudo arp-scan -l"},"audacious":{"n":"audacious","t":"Lightweight audio player with plugin support","s":[["TAGLINE","Lightweight audio player with plugin support"],["TLDR","**Start** Audacious\n\n```audacious```\n\nPlay **specific files**\n\n```audacious [file1.mp3] [file2.mp3]```\n\n**Enqueue** files\n\n```audacious -e [file.mp3]```\n\nStart **headless** (without GUI)\n\n```audacious -H```\n\n**Show** current song\n\n```audacious -t```"],["SYNOPSIS","**audacious** [_-e_] [_-H_] [_options_] [_files_]"],["DESCRIPTION","**audacious** is a lightweight, GTK-based audio player focusing on low resource usage and high audio quality. It supports numerous formats through plugins and features a Winamp-classic interface mode.\n\nThe player emphasizes customization through skins and plugins while maintaining simplicity."],["PARAMETERS","**-e**, **--enqueue**\n> Add files to playlist without playing\n\n**-H**, **--headless**\n> Run without GUI\n\n**-t**, **--show-main-window**\n> Show main window\n\n**-p**, **--play**\n> Start playback\n\n**-u**, **--pause**\n> Pause playback\n\n**-s**, **--stop**\n> Stop playback\n\n**-f**, **--fwd**\n> Skip forward\n\n**-r**, **--rew**\n> Skip backward\n\n**--shuffle**\n> Toggle shuffle\n\n**--repeat**\n> Toggle repeat\n\n**-m**, **--show-jump-box**\n> Show jump to file dialog"],["CONFIGURATION","**~/.config/audacious/config**\n> Main configuration file for player settings and preferences.\n\n**~/.config/audacious/plugin-registry**\n> Registry of installed plugins and their enabled state."],["CAVEATS","Plugin availability varies by distribution. Some formats require additional codecs. GTK2 version (legacy) differs from GTK3/Qt versions."],["HISTORY","**Audacious** was forked from Beep Media Player (itself a fork of XMMS) in **2005**. It continues the Winamp-like interface tradition on Linux."],["INSTALL","```apt: sudo apt install audacious```\n\n```dnf: sudo dnf install audacious```\n\n```pacman: sudo pacman -S audacious```\n\n```apk: sudo apk add audacious```\n\n```zypper: sudo zypper install audacious```\n\n```brew: brew install audacious```\n\n```nix: nix profile install nixpkgs#audacious```"],["SEE ALSO","[mpv](/man/mpv)(1), [vlc](/man/vlc)(1)"]],"e":["mpv","vlc"],"x":"audacious"},"aws-secrets":{"n":"aws-secrets","t":"Store and manage secrets securely","s":[["TAGLINE","Store and manage secrets securely"],["TLDR","**Create a new secret**\n\n```aws secretsmanager create-secret --name [secret_name] --secret-string \"[secret_value]\"```\n\n**Retrieve a secret value**\n\n```aws secretsmanager get-secret-value --secret-id [secret_name]```\n\n**List all secrets**\n\n```aws secretsmanager list-secrets```\n\n**Update a secret value**\n\n```aws secretsmanager put-secret-value --secret-id [secret_name] --secret-string \"[new_value]\"```\n\n**Delete a secret** (with recovery window)\n\n```aws secretsmanager delete-secret --secret-id [secret_name] --recovery-window-in-days [30]```\n\n**Delete immediately** without recovery\n\n```aws secretsmanager delete-secret --secret-id [secret_name] --force-delete-without-recovery```\n\n**Restore a deleted secret**\n\n```aws secretsmanager restore-secret --secret-id [secret_name]```\n\n**Rotate a secret**\n\n```aws secretsmanager rotate-secret --secret-id [secret_name]```"],["SYNOPSIS","**aws secretsmanager** _command_ [_options_]"],["PARAMETERS","**create-secret**\n> Create a new secret.\n\n**get-secret-value**\n> Retrieve the encrypted value of a secret.\n\n**list-secrets**\n> List secrets in the account.\n\n**put-secret-value**\n> Store a new value for an existing secret.\n\n**update-secret**\n> Update secret metadata or value.\n\n**delete-secret**\n> Mark a secret for deletion.\n\n**restore-secret**\n> Restore a previously deleted secret.\n\n**rotate-secret**\n> Trigger secret rotation.\n\n**describe-secret**\n> Get metadata about a secret.\n\n**batch-get-secret-value**\n> Retrieve values for multiple secrets in a single call.\n\n**tag-resource**\n> Add tags to a secret.\n\n**--secret-id** _id_\n> Secret name or ARN.\n\n**--secret-string** _value_\n> Secret value as a string.\n\n**--secret-binary** _blob_\n> Secret value as base64-encoded binary.\n\n**--kms-key-id** _key_\n> KMS key for encryption (optional).\n\n**--recovery-window-in-days** _days_\n> Waiting period before permanent deletion (7-30 days)."],["DESCRIPTION","**AWS Secrets Manager** is a service for securely storing and managing sensitive information like database credentials, API keys, and passwords. The **aws secretsmanager** commands provide CLI access to create, retrieve, rotate, and manage secrets.\n\nSecrets are encrypted at rest using AWS KMS and can be automatically rotated on a schedule using Lambda functions. Access is controlled through IAM policies, and all access is logged to CloudTrail for auditing.\n\nSecrets can store strings up to 64KB, supporting JSON for structured data like database credentials. Applications retrieve secrets at runtime, eliminating hardcoded credentials in code or configuration files."],["CAVEATS","Retrieving secrets incurs per-request charges. Deleted secrets have a recovery window by default; use **--force-delete-without-recovery** cautiously. Automatic rotation requires a configured Lambda function. Cross-region replication requires explicit configuration. Secrets Manager is separate from Systems Manager Parameter Store (another secrets option)."],["HISTORY","AWS Secrets Manager was launched in **April 2018** to address the challenge of managing application secrets securely. It was built on lessons learned from AWS Systems Manager Parameter Store, adding features like automatic rotation, cross-region replication, and native integration with RDS, Redshift, and DocumentDB for credential management."],["INSTALL","```dnf: sudo dnf install aws```\n\n```pacman: sudo pacman -S aws-cli```\n\n```apk: sudo apk add aws-cli```\n\n```zypper: sudo zypper install aws-cli```"],["SEE ALSO","[aws](/man/aws)(1), [vault](/man/vault)(1), [aws-vault](/man/aws-vault)(1)"]],"e":["aws","vault","aws-vault"],"x":"aws secretsmanager create-secret --name [secret_name] --secret-string \"[secret_value]\""},"bunx":{"n":"bunx","t":"Auto-install and run package executables","s":[["TAGLINE","Auto-install and run package executables"],["TLDR","**Run package executable**\n\n```bunx [prisma] migrate```\n\n**Run specific package version**\n\n```bunx [email protected] [app.js]```\n\n**Run when binary name differs from package**\n\n```bunx -p @angular/cli ng new [my-app]```\n\n**Force run with Bun runtime**\n\n```bunx --bun vite dev```\n\n**Run prettier on file**\n\n```bunx prettier [file.js]```\n\n**Run create-react-app**\n\n```bunx create-react-app [my-app]```"],["SYNOPSIS","**bunx** [_options_] _package_ [_arguments_]"],["DESCRIPTION","**bunx** auto-installs and runs executables from npm packages. It's Bun's equivalent of npx, roughly 100x faster for locally installed packages due to Bun's fast startup times.\n\nPackages not found in local node_modules are automatically installed into a global shared cache."],["PARAMETERS","**-p**, **--package** _name_\n> Specify the package to install when the binary name differs from the package name.\n\n**--bun**\n> Force running with Bun even if the executable carries a Node shebang.\n\n**--no-install**\n> Fail instead of fetching when the package is not already cached.\n\n**--silent**\n> Suppress installation-related output.\n\n**--verbose**\n> Print extra detail during installation."],["BEHAVIOR","By default, bunx respects **#!/usr/bin/env node** shebangs in scripts. Use **--bun** to override and run with Bun runtime."],["WINDOWS","On Windows, bunx uses a special **.bunx** file format for cross-filesystem symlinks that works with both Bun and Node.js."],["CAVEATS","Packages are cached globally after first use. Use **--bun** flag when you specifically need Bun's runtime features. Some packages may expect Node.js-specific APIs."],["SEE ALSO","[bun](/man/bun)(1), [bun-run](/man/bun-run)(1), [npx](/man/npx)(1)"]],"e":["bun","bun-run","npx"],"x":"bunx [prisma] migrate"},"capy":{"n":"capy","t":"Git-style end-to-end encrypted secrets CLI","s":[["TAGLINE","Git-style end-to-end encrypted secrets CLI"],["TLDR","**Install** via npm\n\n```npm install -g @capysc/cli```\n\n**Sync** secrets (initializes the project on first run)\n\n```capy```\n\n**Edit** secrets in the interactive TUI\n\n```capy edit```\n\n**Run a command** with decrypted secrets injected\n\n```capy run -- npm run dev```\n\n**Invite** a teammate\n\n```capy invite [teammate@example.com]```\n\n**Remove** a teammate (cryptographic revoke)\n\n```capy kick [teammate@example.com]```\n\n**Deploy** secrets to a hosting platform\n\n```capy deploy```"],["SYNOPSIS","**capy** [_subcommand_] [_options_] [_args_]"],["DESCRIPTION","**capy** is a secrets toolchain with git-style primitives — sync, branch, deploy, invite, and kick — that encrypts values on your machine before they leave. The service stores membership records and ciphertext only; decryption needs a key share that never leaves the client. Your `.env` becomes versioned state the team can commit (`keep.lock`), roll back, and offboard without rotating every application key.\n\nOn first run in a project with a plain `.env`, Capy encrypts each value in place to `capy:{resourceId}:{ciphertext}` snippets and backs up the original as `.env.pre-capy.old`. **capy run** decrypts into process memory and spawns any command that reads environment variables. **capy kick** makes the removed user's local key cryptographically inert without rotating the master key for remaining members.\n\nBranches parallel git: each Capy branch holds its own secret state and access list; git branches pin to a Capy branch via committed `keep.lock`. Offline **capy run** works from the local cache after the first authenticated sync."],["PARAMETERS","**(no subcommand)**\n> Three-way sync between local `.env`, pinned `keep.lock`, and remote. Initializes the project on first use.\n\n**run** **--** _command_...\n> Decrypt secrets in memory and run _command_ with them as environment variables.\n\n**edit**\n> Interactive table of variables: reveal, edit, drift/conflict status.\n\n**status**\n> Show drift between local, pinned, and remote.\n\n**push**\n> Push local changes without pulling.\n\n**lock**\n> Lock the local key so the passphrase is required next time.\n\n**deploy**\n> Generate a deploy token and walk through platform setup (Vercel, Cloudflare, Docker, Fly, Railway, Render, Heroku, GitHub Actions, AWS Lambda, and others).\n\n**connect** _provider_\n> Pull a credential from a linked provider into `.env`.\n\n**rotate** [_var_]\n> Rotate a managed credential.\n\n**invite** _email_\n> Invite a teammate (code travels out-of-band).\n\n**redeem** _code_\n> Redeem an invite code.\n\n**kick** _email_\n> Remove a teammate with cryptographic revocation.\n\n**users**\n> Interactive member management.\n\n**org**\n> List or switch organizations.\n\n**branch**, **checkout** _branch_\n> List/switch Capy branches; **checkout -b** creates.\n\n**grant-branch**, **revoke-branch**\n> Protected-branch access control.\n\n**recover**, **end-recover**\n> Restore access from a recovery phrase / end recovery.\n\n**decrypt**\n> Offline decrypt (owner only).\n\n**use** _profile_, **profile**\n> Switch or manage CLI profiles.\n\n**byoc** [_url_]\n> Connect to a self-hosted Capy instance when available.\n\n**info**, **logout**, **cleanup**, **transport**\n> Session info, clear session, remove hooks/local state, move account to another machine."],["CONFIGURATION","**.env**\n> Project secrets file; values become `capy:...` ciphertext snippets after init.\n\n**keep.lock**\n> Committed manifest pinning the branch and encrypted resource state (like a lockfile).\n\n**~/.capy/**\n> Local cache and keys so **capy run** works offline after the first sync.\n\n**.env.pre-capy.old**\n> Automatic backup of the original plaintext `.env` on first migration (gitignored)."],["CAVEATS","The hosted service component is not fully self-hostable for all deployments; losing the org owner seed phrase with no other device holding `key.enc` means permanent loss of access by design. First sync needs network; offline work uses the local cache only. CLI is AGPL-3.0; review license obligations for your team."],["HISTORY","**Capy** (npm package **@capysc/cli**) is an AGPL-3.0 secrets product from **Incentv Technologies** / capysc, positioned as zero-trust, encrypt-at-source secrets management for humans and agents alongside git."],["SEE ALSO","[sops](/man/sops)(1), [age](/man/age)(1), [doppler](/man/doppler)(1), [git](/man/git)(1)"],["RESOURCES","```[Source code](https://github.com/capysc/capy-cli)```\n\n```[Homepage](https://capy.sc)```\n\n```[Documentation](https://docs.capy.sc)```"]],"e":["sops","age","doppler","git"],"x":"npm install -g @capysc/cli"},"cdecl":{"n":"cdecl","t":"C and C++ type declaration translator","s":[["TAGLINE","C and C++ type declaration translator"],["TLDR","**Start interactive mode**\n\n```cdecl```\n\n**Explain a C declaration**\n\n```cdecl explain \"int *(*fp)(int)\"```\n\n**Convert English to a C declaration**\n\n```cdecl declare \"x as pointer to function returning int\"```\n\n**Explain a complex function declaration**\n\n```cdecl explain \"void (*signal(int, void (*)(int)))(int)\"```\n\n**Cast an expression**\n\n```cdecl cast \"x into pointer to function returning int\"```\n\n**Declare using English** via pipe\n\n```echo \"declare argv as array of pointer to char\" | cdecl```\n\n**Define a typedef**\n\n```cdecl declare \"str as typedef pointer to char\"```"],["SYNOPSIS","**cdecl** [_-ciV_] [_command ..._]"],["PARAMETERS","**-+**\n> Use C++ syntax instead of C (same as -c).\n\n**-c**\n> Use C++ syntax instead of C.\n\n**-i**\n> Enter interactive mode (default if no command given).\n\n**-V**\n> Display version information.\n\n**explain** _declaration_\n> Convert C/C++ declaration to English.\n\n**declare** _description_\n> Convert English description to C/C++ declaration.\n\n**cast** _expression_\n> Create a cast expression.\n\n**typedef** _declaration_\n> Create a typedef from a declaration.\n\n**set** _option_\n> Set options (e.g., cplusplus, preansi).\n\n**help**\n> Display help in interactive mode.\n\n**quit**\n> Exit interactive mode."],["DESCRIPTION","**cdecl** (C declaration) translates complex C and C++ type declarations between their cryptic syntax and plain English. It helps programmers understand and construct complicated declarations involving pointers, arrays, and functions.\n\nThe tool can explain existing declarations like function pointers, arrays of pointers, or pointers to arrays in understandable terms. Conversely, it can generate correct C syntax from English descriptions, helping avoid common declaration mistakes.\n\nIn interactive mode, cdecl provides a prompt where you can enter multiple explain or declare commands. It handles complex scenarios like pointers to functions returning pointers to arrays, signal handler declarations, and other notoriously confusing C constructs.\n\nThe C++ mode adds support for references and member pointers. The tool is particularly valuable when working with legacy code, signal handlers, callback functions, or any code with deeply nested type declarations."],["CAVEATS","Some very complex or non-standard declarations may not be parsed correctly. The tool focuses on type declarations and does not handle full C/C++ syntax. Newer C++ features (concepts, auto, etc.) may not be supported. Different versions may have slightly different English phrasing."],["HISTORY","**cdecl** was originally written by Graham Ross and later enhanced by several contributors including David Wolverton. It emerged in the 1980s when C's declaration syntax was a frequent source of confusion and bugs. The phrase \"declare x as...\" became a teaching tool for understanding C's inside-out declaration reading. The tool remains useful today for complex declarations."],["INSTALL","```apt: sudo apt install cdecl```\n\n```dnf: sudo dnf install cdecl```\n\n```zypper: sudo zypper install cdecl```\n\n```brew: brew install cdecl```\n\n```nix: nix profile install nixpkgs#cdecl```"],["SEE ALSO","[gcc](/man/gcc)(1), [clang](/man/clang)(1), [make](/man/make)(1), [ctags](/man/ctags)(1)"]],"e":["gcc","clang","make","ctags"],"x":"cdecl"},"certbot":{"n":"certbot","t":"Let's Encrypt certificate management","s":[["TAGLINE","Let's Encrypt certificate management"],["TLDR","Obtain certificate via **webroot** authorization\n\n```sudo certbot certonly --webroot -w [path/to/webroot] -d [subdomain.example.com]```\n\nObtain and install certificate for **nginx**\n\n```sudo certbot --nginx -d [subdomain.example.com]```\n\nObtain and install certificate for **apache**\n\n```sudo certbot --apache -d [subdomain.example.com]```\n\n**Renew** all certificates expiring soon\n\n```sudo certbot renew```\n\n**Dry run** to test without saving certificates\n\n```sudo certbot --webroot -w [path/to/webroot] -d [subdomain.example.com] --dry-run```\n\n**Obtain certificate** using standalone mode (no web server needed)\n\n```sudo certbot certonly --standalone -d [subdomain.example.com]```\n\n**List all managed certificates**\n\n```sudo certbot certificates```"],["SYNOPSIS","**certbot** [_options_] _command_"],["DESCRIPTION","**certbot** is the official Let's Encrypt client for automatically obtaining and renewing free TLS/SSL certificates. It can configure web servers like Apache and Nginx automatically.\n\nCertificates are valid for 90 days. The renew command should be run regularly (typically via cron) to maintain valid certificates."],["SUBCOMMANDS","**certonly**\n> Obtain certificate without installing\n\n**run**\n> Obtain and install certificate\n\n**renew**\n> Renew expiring certificates\n\n**revoke**\n> Revoke a certificate\n\n**delete**\n> Delete a certificate\n\n**certificates**\n> List installed certificates"],["PARAMETERS","**-d, --domain** _domain_\n> Domain name for certificate\n\n**-w, --webroot-path** _path_\n> Webroot path for validation\n\n**--nginx**\n> Use nginx plugin\n\n**--apache**\n> Use apache plugin\n\n**--dry-run**\n> Test without saving\n\n**--standalone**\n> Use built-in web server for validation\n\n**--test-cert**\n> Use staging server (untrusted certificates)\n\n**--non-interactive**\n> Run without user interaction\n\n**--agree-tos**\n> Agree to terms of service\n\n**-m**, **--email** _email_\n> Email for account registration\n\n**--preferred-challenges** _type_\n> Preferred challenge type: http-01, dns-01, or tls-alpn-01\n\n**--deploy-hook** _command_\n> Command to run after successful certificate deployment (e.g., reload web server)"],["CONFIGURATION","**/etc/letsencrypt/renewal/**\n> Per-certificate renewal configuration files controlling automatic renewal behavior.\n\n**/etc/letsencrypt/cli.ini**\n> Global certbot defaults for all certificate operations."],["CAVEATS","Webroot validation requires web server to be running. Rate limits apply on Let's Encrypt production servers. Restart web server after renewal for new certificates to take effect."],["HISTORY","**certbot** was developed by the Electronic Frontier Foundation (EFF) and originally known as the Let's Encrypt client. Let's Encrypt launched in **2016** to provide free, automated TLS certificates."],["INSTALL","```dnf: sudo dnf install certbot```\n\n```pacman: sudo pacman -S certbot```\n\n```apk: sudo apk add certbot```\n\n```brew: brew install certbot```\n\n```nix: nix profile install nixpkgs#certbot```"],["SEE ALSO","[openssl](/man/openssl)(1), [nginx](/man/nginx)(8), [apache2](/man/apache2)(8)"]],"e":["openssl","nginx","apache2"],"x":"sudo certbot certonly --webroot -w [path/to/webroot] -d [subdomain.example.com]"},"claude-code-merge-queue":{"n":"claude-code-merge-queue","t":"Local merge queue for parallel Claude Code agents","s":[["TAGLINE","Local merge queue for parallel Claude Code agents"],["TLDR","**Install** as a dev dependency and **initialize** the project\n\n```npm install --save-dev claude-code-merge-queue && npx claude-code-merge-queue init```\n\n**Land** the current lane onto the integration branch through the FIFO queue\n\n```claude-code-merge-queue land```\n\n**Sync** the main checkout after landings (reinstall deps if the lockfile changed)\n\n```claude-code-merge-queue sync```\n\n**Preview** a lane's live working tree on the main checkout\n\n```claude-code-merge-queue preview```\n\n**Promote** the integration branch to production (human-only)\n\n```claude-code-merge-queue promote```\n\n**Run a build** serialized across every lane machine-wide\n\n```claude-code-merge-queue build-lock -- [command]```"],["SYNOPSIS","**claude-code-merge-queue** _command_ [_options_]"],["DESCRIPTION","**claude-code-merge-queue** is a local, zero-cost merge queue for repositories where multiple Claude Code agents land work in parallel. It serializes rebase-and-push landings, runs a configurable check command before each landing, and coordinates worktree \"lanes\" so agents do not race on shared resources.\n\nUnlike GitHub's merge queue, it runs entirely on the developer's machine, does not require a pull request, and works on any plan or hosting setup. Configuration lives in **claude-code-merge-queue.config.mjs**. **init** writes that config, optional **CLAUDE.md** instructions, a WorktreeCreate hook, and package scripts.\n\nA pre-push hook can reject direct pushes to the integration branch and force agents through **land**. Emergency bypass uses **CLAUDE_CODE_MERGE_QUEUE_EMERGENCY_PUSH=1**."],["COMMANDS","**init**\n\n> Detect integration branch and check command; write config, hooks, and scripts.\n\n**land**\n\n> Rebase and push the current lane onto the integration branch through a FIFO queue.\n\n**sync**\n\n> Fast-forward the main checkout to match what landed; reinstall dependencies if needed.\n\n**promote**\n\n> Ship the integration branch to production. Intended for humans only, not agents.\n\n**preview** / **preview:restore**\n\n> Mirror a lane's working tree onto the main checkout for inspection without a full build.\n\n**port**\n\n> Print a lane's assigned dev-server port.\n\n**prune**\n\n> Remove already-landed sibling lane worktrees.\n\n**build-lock** -- _cmd_\n\n> Run _cmd_ under a machine-wide lock so builds do not overlap across lanes.\n\n**hook worktree-create**\n\n> Claude Code WorktreeCreate hook that assigns numbered lanes."],["CAVEATS","There is no human review gate — only **checkCommand** passing allows a land. Locks are local to one machine. A slow check command caps throughput because the FIFO lock is held for the whole check. Rebase conflicts abort cleanly; agents must resolve and re-run **land**."],["SEE ALSO","[git](/man/git)(1), [npm](/man/npm)(1), [claude](/man/claude)(1)"],["RESOURCES","```[Source code](https://github.com/funador/claude-code-merge-queue)```\n\n```[Homepage](https://www.npmjs.com/package/claude-code-merge-queue)```"]],"e":["git","npm","claude"],"x":"npm install --save-dev claude-code-merge-queue && npx claude-code-merge-queue init"},"cpuid":{"n":"cpuid","t":"dump detailed x86/x64 processor information","s":[["TAGLINE","dump detailed x86/x64 processor information"],["TLDR","Display information for **all CPUs**\n\n```cpuid```\n\nDisplay information for **current CPU** only\n\n```cpuid -1```\n\nDisplay **raw hex** information without decoding\n\n```cpuid -r```\n\nDisplay information using the **kernel CPUID module**\n\n```cpuid -k```\n\nRead CPUID data from a **file**\n\n```cpuid -f [cpuid_dump.txt]```"],["SYNOPSIS","**cpuid** [_options_]"],["DESCRIPTION","**cpuid** dumps detailed CPU information by executing the CPUID instruction and decoding the results. It displays processor identification, feature flags, cache information, and other CPU details.\n\nMore detailed than /proc/cpuinfo, it shows low-level processor capabilities."],["PARAMETERS","**-1, --one-cpu**\n> Display information for current CPU only\n\n**-r, --raw**\n> Show raw hex values without decoding\n\n**-f, --file** _file_\n> Read raw data from file instead of executing CPUID instruction\n\n**-k, --kernel**\n> Use the kernel CPUID module (/dev/cpu/*/cpuid); may require root\n\n**-h, --help**\n> Display help information\n\n**-v, --version**\n> Display cpuid version"],["CAVEATS","x86/x86_64 only. Some information may be virtualized when running in VMs. Output interpretation requires understanding of CPU architecture."],["INSTALL","```apt: sudo apt install cpuid```\n\n```dnf: sudo dnf install cpuid```\n\n```zypper: sudo zypper install cpuid```\n\n```brew: brew install cpuid```\n\n```nix: nix profile install nixpkgs#cpuid```"],["SEE ALSO","[lscpu](/man/lscpu)(1), [x86info](/man/x86info)(1)"]],"e":["lscpu","x86info"],"x":"cpuid"},"cubemastercli":{"n":"cubemastercli","t":"CLI for managing CubeSandbox templates and sandboxes","s":[["TAGLINE","CLI for managing CubeSandbox templates and sandboxes"],["TLDR","**Create a sandbox template** from a container image\n\n```cubemastercli tpl create-from-image --image [registry/image:tag]```\n\n**Expose ports** and set a probe port when creating a template\n\n```cubemastercli tpl create-from-image --image [image] --expose-port [49999] --probe [49999]```\n\n**Configure a writable layer size** for the template\n\n```cubemastercli tpl create-from-image --image [image] --writable-layer-size [1G]```\n\n**Watch the build progress** of a template job\n\n```cubemastercli tpl watch --job-id [job_id]```\n\n**Show help** for a subcommand\n\n```cubemastercli tpl --help```"],["SYNOPSIS","**cubemastercli** _subcommand_ _action_ [_options_]"],["DESCRIPTION","**cubemastercli** is the command-line interface for **CubeSandbox**, a high-performance, secure sandbox service built on **RustVMM** and **KVM**. It is used by operators to create, build, and monitor sandbox templates that back isolated execution environments for AI agents.\n\nTemplates are produced from OCI container images and encapsulate everything needed to spin up a microVM-based sandbox, including the root filesystem, writable layer, exposed ports, and a readiness probe. Once a template is built, CubeSandbox can launch sandboxes from it in under 60 ms with hardware-level isolation.\n\nThe tool is typically run against a CubeMaster control plane that orchestrates the underlying microVMs on RustVMM/KVM hosts."],["PARAMETERS","**tpl create-from-image**\n> Build a new sandbox template from a container image. Submits a build job to CubeMaster.\n\n**tpl watch**\n> Follow the progress of a running template build job.\n\n**--image** _registry/image:tag_\n> Container image used as the base of the template.\n\n**--writable-layer-size** _size_\n> Size of the writable overlay layer (e.g. _1G_). Sandboxes launched from the template get a fresh writable layer of this size.\n\n**--expose-port** _port_\n> Expose a TCP port from the sandbox. May be specified multiple times.\n\n**--probe** _port_\n> Port used for the readiness probe; the template is considered ready once this port accepts connections.\n\n**--job-id** _id_\n> Identifier of a build job to watch, as returned by **tpl create-from-image**.\n\n**--help**\n> Show help for the command or subcommand."],["EXAMPLES","Build a code-interpreter template from a Tencent Cloud image and watch the resulting job:\n\n```\ncubemastercli tpl create-from-image \\\n    --image ccr.ccs.tencentyun.com/ags-image/sandbox-code:latest \\\n    --writable-layer-size 1G \\\n    --expose-port 49999 \\\n    --expose-port 49983 \\\n    --probe 49999\n\ncubemastercli tpl watch --job-id <job_id>\n```"],["CAVEATS","**cubemastercli** requires a reachable CubeMaster control plane and a host with KVM enabled. It is part of the CubeSandbox project and is not shipped by Linux distributions; flags and subcommands may change between releases."],["HISTORY","**CubeSandbox** was open-sourced by **Tencent Cloud** as an instant, concurrent, secure and lightweight sandbox for AI agents, built on **RustVMM** and **KVM**. **cubemastercli** ships as the administrative client for its CubeMaster control plane."],["SEE ALSO","[docker](/man/docker)(1), [kvm](/man/kvm)(4), [qemu](/man/qemu)(1)"]],"e":["docker","kvm","qemu"],"x":"cubemastercli tpl create-from-image --image [registry/image:tag]"},"deborphan":{"n":"deborphan","t":"find orphaned packages on Debian-based systems","s":[["TAGLINE","find orphaned packages on Debian-based systems"],["TLDR","Display **orphan library** packages\n\n```deborphan```\n\nList orphans across **all** package sections\n\n```deborphan --all-packages```\n\nList orphans including **library-like names** from all heuristics\n\n```deborphan --guess-all```\n\nFind packages only **recommended or suggested** (not hard dependencies)\n\n```deborphan -n```\n\n**Remove orphans** automatically using apt\n\n```deborphan | xargs apt-get -y remove --purge```\n\nShow orphans with their **section** and **size**\n\n```deborphan -sz```"],["SYNOPSIS","**deborphan** [_options_] [_package_...]"],["DESCRIPTION","**deborphan** finds orphaned packages on Debian-based systems. Orphaned packages are typically libraries that were installed as dependencies but are no longer required by any installed package.\n\nBy default, it checks only packages in the \"libs\" and \"oldlibs\" sections. Use **-a** to check all sections."],["PARAMETERS","**-a**, **--all-packages**\n> Check all packages, not just libs/oldlibs sections\n\n**--guess-all**\n> Enable all heuristics to find packages with library-like names in other sections\n\n**-n**, **--nice-mode**\n> Show packages that are only recommended or suggested, not hard-depended upon\n\n**-H**, **--show-section**\n> Show the section of each package\n\n**-s** _section_\n> Check only the specified section (can be used multiple times)\n\n**-z**, **--show-size**\n> Show installed size of each package\n\n**-d**, **--show-deps**\n> Show packages that depend on the orphan\n\n**-P**, **--show-priority**\n> Show priority of each package\n\n**--no-show-section**\n> Don't show section (default)\n\n**-p** _file_, **--status-file** _file_\n> Use alternate status file instead of /var/lib/dpkg/status"],["CAVEATS","Only shows libraries by default; use **-a** for a complete scan. Results should be reviewed before removal as some packages may be manually installed and still wanted. For automatic cleanup of unused dependencies, consider **apt autoremove** as a simpler alternative."],["SEE ALSO","[apt](/man/apt)(8), [aptitude](/man/aptitude)(8), [dpkg](/man/dpkg)(1)"]],"e":["apt","aptitude","dpkg"],"x":"deborphan"},"debuginfod":{"n":"debuginfod","t":"HTTP server for distributing ELF debugging information","s":[["TAGLINE","HTTP server for distributing ELF debugging information"],["TLDR","**Scan directories for ELF/DWARF files**\n\n```debuginfod -F [/usr/lib/debug]```\n\n**Start on a specific port**\n\n```debuginfod -p [8002] -F [/usr/lib/debug]```\n\n**Index RPM archives in a directory**\n\n```debuginfod -R [/path/to/rpms]```\n\n**Index DEB archives in a directory**\n\n```debuginfod -U [/path/to/debs]```\n\n**Run in verbose mode with custom database path**\n\n```debuginfod -v -d [/var/cache/debuginfod.sqlite] -F [/usr/lib/debug]```\n\n**Set scan interval and concurrent threads**\n\n```debuginfod -t [600] -c [10] -F [/path/to/debug]```"],["SYNOPSIS","**debuginfod** [_options_] [_paths_...]"],["DESCRIPTION","**debuginfod** is a server for distributing ELF debugging information over HTTP. It indexes executables, shared libraries, and debug info, serving them to clients like gdb and systemtap on demand.\n\nThe service enables automatic debugging symbol resolution without manually installing debug packages. Clients query by build-id to fetch matching debug information."],["PARAMETERS","**-p** _port_\n> HTTP server port (default 8002).\n\n**-F**\n> Activate ELF/DWARF file scanning of specified paths.\n\n**-R**\n> Scan for RPM archives.\n\n**-U**\n> Scan for DEB/DDEB archives.\n\n**-Z** _ext_\n> Activate additional archive pattern scanning for the given extension.\n\n**-d** _file_\n> SQLite database file location (default ~/.debuginfod.sqlite).\n\n**-c** _num_\n> Scanner queue thread limit.\n\n**-C** _num_\n> Webapi thread pool size.\n\n**-t** _seconds_\n> Directory rescan interval (default 300).\n\n**-g** _seconds_\n> Grooming pass interval (default 86400).\n\n**-I** _regex_\n> Include files matching POSIX extended regex.\n\n**-X** _regex_\n> Exclude files matching POSIX extended regex.\n\n**-L**\n> Traverse symbolic links during scanning.\n\n**--passive**\n> Read-only mode; no scanning, only serve existing index.\n\n**--cors**\n> Add CORS response headers for third-party webapp access.\n\n**-v**\n> Increase verbosity (may be repeated)."],["CLIENT USAGE","Set DEBUGINFOD_URLS to enable automatic fetching:\n```\nexport DEBUGINFOD_URLS=\"https://debuginfod.example.com\"\n```"],["CAVEATS","Initial indexing can be slow for large repositories. The SQLite database grows with content. Network access is needed by clients. The service may expose details about installed binaries. Clients require matching build-ids to fetch debug information."],["HISTORY","**debuginfod** was developed as part of the **elfutils** project by **Red Hat** engineers around **2019**. It addresses the challenge of distributing debug symbols for Linux distributions, enabling on-demand fetching rather than installing large debug packages."],["INSTALL","```apt: sudo apt install debuginfod```\n\n```pacman: sudo pacman -S debuginfod```\n\n```apk: sudo apk add debuginfod```"],["SEE ALSO","[debuginfod-find](/man/debuginfod-find)(1), [gdb](/man/gdb)(1), [objdump](/man/objdump)(1), [readelf](/man/readelf)(1)"]],"e":["debuginfod-find","gdb","objdump","readelf"],"x":"debuginfod -F [/usr/lib/debug]"},"devcontainer":{"n":"devcontainer","t":"CLI for managing development containers","s":[["TAGLINE","CLI for managing development containers"],["TLDR","**Build a dev container**\n\n```devcontainer build --workspace-folder [.]```\n\n**Start a dev container**\n\n```devcontainer up --workspace-folder [.]```\n\n**Execute command in** container\n\n```devcontainer exec --workspace-folder [.] [command]```\n\n**Open shell in** container\n\n```devcontainer exec --workspace-folder [.] bash```\n\n**Read configuration**\n\n```devcontainer read-configuration --workspace-folder [.]```\n\n**List features**\n\n```devcontainer features list```\n\n**Generate templates**\n\n```devcontainer templates apply -t [python]```\n\n**Show resolved configuration as JSON**\n\n```devcontainer read-configuration --workspace-folder [.] --include-merged-configuration```"],["SYNOPSIS","**devcontainer** _command_ [_options_]"],["PARAMETERS","_COMMAND_\n> Operation: build, up, exec, read-configuration, features, templates.\n\n**--workspace-folder** _PATH_\n> Path to workspace with .devcontainer config.\n\n**build**\n> Build the dev container image.\n\n**up**\n> Create and start the container.\n\n**exec** _COMMAND_\n> Run command in container.\n\n**read-configuration**\n> Output resolved configuration.\n\n**features list**\n> List available dev container features.\n\n**templates apply** **-t** _template_\n> Apply a dev container template to the workspace.\n\n**run-user-commands**\n> Run user commands (postCreateCommand, etc.) in the container.\n\n**--help**\n> Display help information.\n\n**--version**\n> Display version information."],["DESCRIPTION","**devcontainer** is the CLI for managing development containers defined by the Dev Container specification. It allows building, running, and interacting with containerized development environments from the command line.\n\nDev containers provide reproducible development environments using Docker containers. Configuration in .devcontainer/devcontainer.json defines the container image, extensions, settings, and features needed for a project.\n\nThe CLI enables dev container workflows without IDE integration, useful for CI/CD pipelines, remote development, and automation of development environment setup."],["CONFIGURATION","**.devcontainer/devcontainer.json**\n> Configuration file defining container image, features, extensions, and environment settings."],["CAVEATS","Requires Docker or compatible container runtime. Configuration must follow Dev Container spec. Some features require specific base images. Resource-intensive for complex environments."],["HISTORY","The Dev Container CLI was released by **Microsoft** as part of the Dev Containers specification (**2022**). It extends the VS Code Dev Containers functionality to command-line workflows and other tools."],["INSTALL","```brew: brew install devcontainer```\n\n```nix: nix profile install nixpkgs#devcontainer```"],["SEE ALSO","[docker](/man/docker)(1), [docker-compose](/man/docker-compose)(1), [code](/man/code)(1)"]],"e":["docker","docker-compose","code"],"x":"devcontainer build --workspace-folder [.]"},"doppler-run":{"n":"doppler-run","t":"execute commands with injected secrets","s":[["TAGLINE","execute commands with injected secrets"],["TLDR","**Run command with** injected secrets\n\n```doppler run -- [node app.js]```\n\n**Run with specific** config\n\n```doppler run --config [dev] -- [command]```\n\n**Run with specific** project\n\n```doppler run --project [myapp] -- [command]```\n\n**Passthrough to shell**\n\n```doppler run -- sh -c \"[echo \\$SECRET_KEY]\"```\n\n**Run with fallback** file\n\n```doppler run --fallback [.env.backup] -- [command]```\n\n**Mount secrets as** a file\n\n```doppler run --mount [secrets.env] -- [command]```\n\n**Transform secret names** for .NET\n\n```doppler run --name-transformer dotnet-env -- [dotnet run]```"],["SYNOPSIS","**doppler run** [_options_] **--** _command_ [_args_]"],["PARAMETERS","**--** _COMMAND_\n> Command to run with injected secrets.\n\n**--config** _NAME_\n> Configuration environment to use.\n\n**--project** _NAME_\n> Project name.\n\n**--fallback** _FILE_\n> Fallback file if Doppler unavailable.\n\n**--no-fallback**\n> Disable fallback behavior.\n\n**--preserve-env**\n> Preserve existing environment variables. Accepts a comma-separated list of specific secrets (e.g. --preserve-env=\"FOO,BAR\") or \"true\" for all.\n\n**--mount** _PATH_\n> Write secrets to an ephemeral file instead of injecting into the environment. Format auto-detected from file extension.\n\n**--mount-format** _FORMAT_\n> File format for mounted secrets (e.g. json, env).\n\n**--name-transformer** _TRANSFORMER_\n> Transform secret names from UPPER_SNAKE_CASE (e.g. dotnet-env, tf-var, lower-camel, lower-snake).\n\n**--command** _CMD_\n> Command to execute (alternative to using -- separator).\n\n**--clean-exit**\n> Exit with a 0 status code when the command is interrupted (SIGINT).\n\n**--help**\n> Display help information."],["DESCRIPTION","**doppler run** executes a command with secrets from Doppler injected as environment variables. This enables applications to access secrets without code changes or local configuration files.\n\nSecrets are fetched from the Doppler API and made available only to the executed process. The double-dash separates doppler options from the command to run.\n\nFallback files provide resilience when Doppler is unreachable, while preserve-env prevents overwriting existing environment variables."],["CAVEATS","Requires authenticated doppler CLI. Network latency affects startup time. Secrets visible in process environment. Child processes inherit secrets."],["HISTORY","doppler run is the primary command for secrets injection in the **Doppler** CLI, implementing the zero-configuration secrets access that Doppler was designed to provide."],["INSTALL","```brew: brew install doppler```\n\n```nix: nix profile install nixpkgs#doppler```"],["SEE ALSO","[doppler](/man/doppler)(1), [doppler-secrets](/man/doppler-secrets)(1), [env](/man/env)(1)"]],"e":["doppler","doppler-secrets","env"],"x":"doppler run -- [node app.js]"},"doveadm":{"n":"doveadm","t":"Dovecot mail server administration tool","s":[["TAGLINE","Dovecot mail server administration tool"],["TLDR","**Show mailbox status**\n\n```doveadm mailbox status -u [user@example.com] all '*'```\n\n**List user mailboxes**\n\n```doveadm mailbox list -u [user@example.com]```\n\n**Search messages**\n\n```doveadm search -u [user@example.com] mailbox INBOX subject \"[test]\"```\n\n**Expunge deleted messages**\n\n```doveadm expunge -u [user@example.com] mailbox Trash all```\n\n**Force user reindex**\n\n```doveadm index -u [user@example.com] INBOX```\n\n**Show user quota**\n\n```doveadm quota get -u [user@example.com]```\n\n**Sync mailboxes**\n\n```doveadm sync -u [user@example.com] [remote_host]```"],["SYNOPSIS","**doveadm** [_options_] _command_ [_args_]"],["DESCRIPTION","**doveadm** is the administration tool for Dovecot IMAP/POP3 server. It provides commands for managing mailboxes, users, and server operations without directly manipulating mail storage.\n\nThe tool handles tasks like mailbox maintenance, message search and manipulation, quota management, and replication control."],["PARAMETERS","**-u** _user_\n> Target user for operation.\n\n**-A**\n> Apply to all users.\n\n**-D**\n> Enable debug output.\n\n**-v**\n> Verbose output.\n\n**-f** _format_\n> Output format (flow, pager, tab, table).\n\n**-o** _setting=value_\n> Override configuration."],["COMMANDS","**mailbox list/status/create/delete/rename**: Mailbox management\n**search**: Find messages\n**fetch**: Retrieve message data\n**expunge**: Delete messages\n**index**: Reindex mailboxes\n**quota**: Quota operations\n**sync**: Dsync replication\n**user**: User information"],["CONFIGURATION","**/etc/dovecot/dovecot.conf**\n> Main Dovecot configuration file controlling mail storage, authentication, and protocols."],["CAVEATS","Requires proper Dovecot configuration. Some commands need root or dovecot user. Operations on large mailboxes can be slow. User must exist in Dovecot's user database."],["HISTORY","**doveadm** was introduced in **Dovecot 2.0** around **2010**, replacing older administrative scripts. **Dovecot** itself was created by **Timo Sirainen** in **2002** as a secure, high-performance IMAP server. Doveadm unified various administrative tasks into a single, consistent interface."],["INSTALL","```apt: sudo apt install dovecot-core```\n\n```dnf: sudo dnf install dovecot```\n\n```pacman: sudo pacman -S dovecot```\n\n```apk: sudo apk add dovecot```\n\n```zypper: sudo zypper install dovecot```\n\n```brew: brew install dovecot```\n\n```nix: nix profile install nixpkgs#dovecot```"],["SEE ALSO","[dovecot](/man/dovecot)(1), [postfix](/man/postfix)(1), [mutt](/man/mutt)(1)"]],"e":["dovecot","postfix","mutt"],"x":"doveadm mailbox status -u [user@example.com] all '*'"},"dvc-checkout":{"n":"dvc-checkout","t":"restore DVC-tracked files to workspace","s":[["TAGLINE","restore DVC-tracked files to workspace"],["TLDR","**Checkout all tracked** files\n\n```dvc checkout```\n\n**Checkout specific file**\n\n```dvc checkout [data/file.csv.dvc]```\n\n**Checkout with summary**\n\n```dvc checkout --summary```\n\n**Force checkout** overwriting local\n\n```dvc checkout --force```"],["SYNOPSIS","**dvc checkout** [_options_] [_targets_...]"],["PARAMETERS","_TARGETS_\n> DVC files to checkout.\n\n**-f**, **--force**\n> Overwrite local changes.\n\n**--summary**\n> Show summary of changes.\n\n**-d**, **--with-deps**\n> Include dependencies.\n\n**--help**\n> Display help information."],["DESCRIPTION","**dvc checkout** synchronizes DVC-tracked files in the workspace with the versions specified in .dvc files. It's used after git checkout to restore data files matching the current Git commit.\n\nWhen switching Git branches or commits, dvc checkout updates data files to match the .dvc metadata. Files are retrieved from the DVC cache or require dvc pull if not cached locally.\n\nThis enables working with different versions of datasets across branches."],["CAVEATS","Requires data in local cache or remote. Large files may take time. Force overwrites local changes. Works per .dvc file."],["HISTORY","dvc checkout is part of **DVC**, providing Git-like checkout semantics for versioned data files in machine learning projects."],["INSTALL","```brew: brew install dvc```\n\n```nix: nix profile install nixpkgs#dvc```"],["SEE ALSO","[dvc](/man/dvc)(1), [git-checkout](/man/git-checkout)(1)"]],"e":["dvc","git-checkout"],"x":"dvc checkout"},"eselect-news":{"n":"eselect-news","t":"Gentoo news item reader and manager","s":[["TAGLINE","Gentoo news item reader and manager"],["TLDR","**List** news items\n\n```eselect news list```\n\nList only **new** items\n\n```eselect news list new```\n\n**Read** all unread news\n\n```eselect news read```\n\nRead **specific** news items\n\n```eselect news read 1 2 3```\n\nMark items as **unread**\n\n```eselect news unread 1 2```\n\n**Delete** read news items\n\n```eselect news purge```\n\n**Count** news items\n\n```eselect news count new```"],["SYNOPSIS","**eselect news** _action_ [_arguments_]"],["DESCRIPTION","**eselect news** is an eselect module for reading Gentoo news items. Portage prints a notice when news is available after repository synchronization."],["PARAMETERS","**list [all|new]**\n> List available news items (all by default)\n\n**read [NUMBERS...]**\n> Read specified news items or all unread if none specified\n\n**unread NUMBERS...**\n> Mark specified news items as unread\n\n**purge**\n> Delete all read news items\n\n**count [all|new]**\n> Show the number of news items (new by default)"],["CAVEATS","News items are repository-specific announcements from Gentoo developers about important changes. Reading news items marks them as read and they won't be announced again."],["HISTORY","**eselect news** is part of the **eselect** framework on Gentoo Linux, providing a modular system for configuration management."],["SEE ALSO","[eselect](/man/eselect)(1), [emerge](/man/emerge)(1), [emaint](/man/emaint)(1)"],["RESOURCES","```[Source code](https://gitweb.gentoo.org/proj/eselect.git/)```\n\n```[Documentation](https://wiki.gentoo.org/wiki/Eselect)```"]],"e":["eselect","emerge","emaint"],"x":"eselect news list"},"feh":{"n":"feh","t":"lightweight X11 image viewer","s":[["TAGLINE","lightweight X11 image viewer"],["TLDR","**View images**\n\n```feh [image.jpg]```\n\n**View directory as** slideshow\n\n```feh -D [5] [directory]```\n\n**Set wallpaper**\n\n```feh --bg-scale [image.jpg]```\n\n**Thumbnail mode**\n\n```feh -t [directory]```\n\n**Fullscreen mode**\n\n```feh -F [image.jpg]```"],["SYNOPSIS","**feh** [_options_] [_files_|_directories_|_URLs_]"],["PARAMETERS","_FILES_\n> Images to display.\n\n**-F**, **--fullscreen**\n> Start in fullscreen mode.\n\n**-D** _SEC_, **--slideshow-delay** _SEC_\n> Slideshow delay in seconds.\n\n**-t**, **--thumbnails**\n> Thumbnail browser mode.\n\n**--bg-scale** _IMAGE_\n> Fit image to screen (may distort).\n\n**--bg-fill** _IMAGE_\n> Fill screen preserving aspect ratio (may crop).\n\n**--bg-max** _IMAGE_\n> Scale to fit without cropping (may leave borders).\n\n**--bg-center** _IMAGE_\n> Center image without scaling.\n\n**--bg-tile** _IMAGE_\n> Tile image across screen.\n\n**-g** _WxH_, **--geometry** _WxH_\n> Limit window size.\n\n**-Z**, **--auto-zoom**\n> Zoom to fit window.\n\n**-r**, **--recursive**\n> Recurse into directories.\n\n**--help**\n> Display help information."],["DESCRIPTION","**feh** is a lightweight, fast image viewer for X11. It supports slideshows, thumbnail browsing, montages, and setting desktop wallpapers through command-line control.\n\nThe viewer handles multiple image formats and provides keyboard and mouse controls for navigation and zooming. It can read images from files, directories, stdin, or URLs.\n\nfeh is particularly popular for setting wallpapers in window managers that lack built-in wallpaper support."],["CAVEATS","X11 only (no Wayland native). Large images may load slowly. Wallpaper setting requires running at startup."],["HISTORY","feh was created by **Tom Gilbert** in 1999 as a fast, simple X11 image viewer. It remains widely used in lightweight Linux setups, especially for its wallpaper-setting capabilities."],["INSTALL","```apt: sudo apt install feh```\n\n```dnf: sudo dnf install feh```\n\n```pacman: sudo pacman -S feh```\n\n```apk: sudo apk add feh```\n\n```zypper: sudo zypper install feh```\n\n```brew: brew install feh```\n\n```nix: nix profile install nixpkgs#feh```"],["SEE ALSO","[sxiv](/man/sxiv)(1), [nsxiv](/man/nsxiv)(1), [imv](/man/imv)(1)"]],"e":["sxiv","nsxiv","imv"],"x":"feh [image.jpg]"},"fiascotopnm":{"n":"fiascotopnm","t":"wavelet compression to PNM converter","s":[["TAGLINE","wavelet compression to PNM converter"],["TLDR","**Convert FIASCO to PNM**\n\n```fiascotopnm [input.fiasco] > [output.pnm]```\n\n**Convert with specific frame**\n\n```fiascotopnm --frame [5] [input.fiasco] > [output.pnm]```"],["SYNOPSIS","**fiascotopnm** [_options_] _file_"],["PARAMETERS","**--frame** _num_\n> Frame number to extract."],["DESCRIPTION","**fiascotopnm** decodes images compressed with the FIASCO (Fractal Image And Sequence COdec) algorithm into PNM (Portable Any Map) format. FIASCO uses wavelet-based compression techniques similar to those in JPEG2000.\n\nThe tool is part of the Netpbm image processing toolkit and can extract individual frames from FIASCO-encoded image sequences. It handles both still images and video frame sequences, making the compressed data accessible for further processing with standard image tools.\n\nfiascotopnm is useful when working with legacy FIASCO-compressed images or when interfacing with systems that use FIASCO as an intermediate format."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pnmtofiasco](/man/pnmtofiasco)(1), [pnm](/man/pnm)(5)"],["RESOURCES","```[Homepage](https://netpbm.sourceforge.net/)```"]],"e":["pnmtofiasco","pnm"],"x":"fiascotopnm [input.fiasco] > [output.pnm]"},"gcloud":{"n":"gcloud","t":"Google Cloud Platform command-line interface","s":[["TAGLINE","Google Cloud Platform command-line interface"],["TLDR","**Initialize gcloud** and set up configuration\n\n```gcloud init```\n\n**Login to Google** Cloud\n\n```gcloud auth login```\n\n**Set the active project**\n\n```gcloud config set project [project-id]```\n\n**List all projects**\n\n```gcloud projects list```\n\n**Create a Compute Engine VM** instance\n\n```gcloud compute instances create [name] --zone=[zone]```\n\n**List running VM** instances\n\n```gcloud compute instances list```\n\n**Get application-default** credentials for local development\n\n```gcloud auth application-default login```\n\n**Format output as JSON** for scripting\n\n```gcloud compute instances list --format=json```"],["SYNOPSIS","**gcloud** _group_ [_command_] [_options_]"],["PARAMETERS","_GROUP_\n> Service group: compute, container, iam, sql, storage, functions, run, etc.\n\n**auth**\n> Authentication and credential commands.\n\n**config**\n> Configuration management.\n\n**compute**\n> Compute Engine commands.\n\n**container**\n> Kubernetes Engine commands.\n\n**projects**\n> Project management.\n\n**--project** _ID_\n> Override the default project for this command.\n\n**--format** _FORMAT_\n> Output format: json, yaml, csv, table, text, value, etc.\n\n**--filter** _EXPRESSION_\n> Filter results (e.g. 'name~prod').\n\n**--quiet**, **-q**\n> Disable interactive prompts and use defaults.\n\n**--verbosity** _LEVEL_\n> Logging verbosity: debug, info, warning, error, critical, none.\n\n**--account** _ACCOUNT_\n> Override the default account for this command.\n\n**--region** _REGION_\n> Override the default region for this command.\n\n**--zone** _ZONE_\n> Override the default zone for this command.\n\n**--configuration** _NAME_\n> Use a named configuration instead of the active one.\n\n**--flags-file** _FILE_\n> Read flags from a YAML or JSON file.\n\n**--flatten** _KEY_\n> Flatten nested list or map output.\n\n**--help**\n> Display help information."],["CONFIGURATION","**~/.config/gcloud/configurations/config_default**\n> Default configuration properties including project, region, zone, and account settings.\n\n**~/.config/gcloud/credentials.db**\n> Encrypted credentials database for authenticated accounts.\n\n**~/.config/gcloud/application_default_credentials.json**\n> Application default credentials for local development."],["DESCRIPTION","**gcloud** is the Google Cloud Platform CLI for managing cloud resources. It provides commands for all GCP services including Compute Engine, Kubernetes Engine, Cloud SQL, Cloud Run, Cloud Functions, and more.\n\nThe tool handles authentication, configuration, and resource management. Commands are organized by service groups with subcommands for specific operations. The **--format** and **--filter** flags are available on all commands for scripting and automation. Output formats include json, yaml, csv, table, text, value, and more.\n\nMultiple named configurations can be managed with **gcloud config configurations** for switching between projects and accounts."],["CAVEATS","Requires a GCP account and project. Actions may incur costs. Some commands need specific IAM permissions. The **gcloud** CLI is part of the Google Cloud SDK, which must be installed separately from most package managers."],["HISTORY","**gcloud** is part of the **Google Cloud SDK**, first released in **2013** as the primary CLI for Google Cloud Platform administration and automation. It has grown to cover hundreds of GCP services and is continuously updated with new features."],["SEE ALSO","[gsutil](/man/gsutil)(1), [kubectl](/man/kubectl)(1), [bq](/man/bq)(1), [aws](/man/aws)(1), [az](/man/az)(1), [terraform](/man/terraform)(1)"]],"e":["gsutil","kubectl","bq","aws","az","terraform"],"x":"gcloud init"},"git-annex":{"n":"git-annex","t":"manage large files with git, without checking their contents into git","s":[["TAGLINE","manage large files with git, without checking their contents into git"],["TLDR","**Initialize** a repository for annex\n\n```git annex init \"[laptop]\"```\n\n**Add** a large file to the annex\n\n```git annex add [largefile.zip]```\n\n**Download** file content from a remote that has it\n\n```git annex get [file]```\n\n**Drop** the local copy, keeping the file tracked\n\n```git annex drop [file]```\n\n**Show** which repositories hold a file's content\n\n```git annex whereis [file]```\n\n**Copy** content to a remote\n\n```git annex copy [file] --to [origin]```\n\n**Sync** metadata with all remotes\n\n```git annex sync```\n\n**Sync** metadata and file contents\n\n```git annex sync --content```\n\n**Check** that stored content is intact\n\n```git annex fsck```\n\n**Unlock** a file so it can be edited in place\n\n```git annex unlock [file]```"],["SYNOPSIS","**git annex** _command_ [_options_] [_files_...]"],["PARAMETERS","**init** [_description_]\n> Initialize a repository for use with annex, giving it a human-readable description.\n\n**add** _files_\n> Add files to the annex, replacing them with pointers to their content.\n\n**get** _files_\n> Download content from a remote that has it.\n\n**drop** _files_\n> Remove local content, after verifying enough other copies exist.\n\n**copy** _files_ **--to**/**--from** _remote_\n> Copy content to or from a remote, leaving the source copy in place.\n\n**move** _files_ **--to**/**--from** _remote_\n> Like **copy**, but drop the source copy afterwards.\n\n**sync** [**--content**]\n> Synchronize the git metadata with remotes. **--content** also transfers file contents.\n\n**whereis** _files_\n> List the repositories known to hold each file's content.\n\n**unlock** _files_, **lock** _files_\n> Unlock a file to modify it in place, then lock it again to store the new version.\n\n**fsck**\n> Verify that stored content matches its checksums and that enough copies exist.\n\n**numcopies** _n_\n> Set how many copies of each file must exist before **drop** will remove one.\n\n**info** [_remote_]\n> Show repository or remote statistics, including disk usage.\n\n**initremote** _name_ **type**=_type_\n> Set up a special remote such as S3, rsync or a directory.\n\n**--help**\n> Display help information."],["DESCRIPTION","**git-annex** lets git manage files whose contents are too large to live in git history. Instead of the file, git tracks a small pointer, while the actual bytes are kept in a separate object store under **.git/annex/objects**. History, branches and merges all work as usual, because from git's point of view the repository only contains small pointer files.\n\nThe idea it adds beyond \"big files elsewhere\" is that a repository does not need to hold every file's content. Each clone can carry whatever subset fits, and git-annex maintains a distributed record, in the **git-annex** branch, of which repositories hold which content. **git annex whereis** answers \"where are the actual bytes\", and **git annex get** fetches them on demand. This is what makes it workable for archives far larger than any single disk.\n\nContent is addressed by checksum, so identical files are stored once and corruption is detectable. **git annex fsck** verifies checksums and checks the **numcopies** guarantee, which is the mechanism that stops **drop** from removing the last copy of anything.\n\nBy default annexed files appear as symlinks into the object store, which keeps them read-only and prevents accidental modification. **git annex unlock** replaces the symlink with a writable copy, and **lock** stores the result as a new version. The newer **v6**/**v7** repository modes and the **adjusted unlocked branch** make unlocked files the normal state, which is friendlier for tools that cannot follow symlinks.\n\n**Special remotes** are the other half of the design: an annex remote does not have to be a git repository at all. S3, Backblaze B2, rsync targets, WebDAV, removable drives and many more can hold content, letting git-annex use them as storage while git itself never talks to them."],["CONFIGURATION","**.git/annex/objects/**\n> Local content store, holding files keyed by checksum.\n\n**.git/config**\n> Repository and remote configuration, including annex settings.\n\n**git-annex** branch\n> An orphan branch tracking location and metadata for all content. Synced between repositories; do not edit it by hand.\n\n**.gitattributes**\n> Controls which files are annexed rather than committed to git directly, via the **annex.largefiles** attribute."],["CAVEATS","The learning curve is real. The concepts (keys, backends, special remotes, numcopies, trust levels, the **git-annex** branch, locked versus unlocked files) do not map onto anything else in git, and the repository version history (**v5** through **v10**) means much of the documentation you find online describes behaviour that has since changed.\n\nLocked files are **symlinks**. Editors that replace files on save, build systems that copy trees, and filesystems without symlink support all interact badly with them. Windows support in particular has always lagged, since it lacks proper symlinks.\n\n**git annex drop** refuses to remove content when it cannot verify that **numcopies** copies remain elsewhere, and it trusts its own location log to decide. If that log is stale, because another repository was deleted without being told about it, the record can claim copies exist that do not. **git annex fsck** is what reconciles this, and it is worth running periodically rather than only after trouble.\n\nA plain **git clone** of an annex repository gets the pointers, not the content: the files look present but are broken symlinks until **git annex get** fetches them.\n\n**git annex sync** does not transfer file contents unless you pass **--content**, which surprises people who expect it to behave like **git pull** plus **git push**.\n\ngit-annex and **git-lfs** solve overlapping problems differently. LFS is simpler, has direct support from GitHub, GitLab and others, and always fetches content on checkout. git-annex is far more flexible about where content lives and what each clone carries. Migrating between the two is awkward, so the choice is worth making deliberately."],["HISTORY","**git-annex** was written by **Joey Hess** and first released in **2010**. Development of its assistant and web application was funded by a successful **Kickstarter** campaign in **2012**. It is written in **Haskell**, and remains widely used for research data management, notably as the foundation of the **DataLad** project, as well as for personal media archives."],["INSTALL","```apt: sudo apt install git-annex```\n\n```dnf: sudo dnf install git-annex```\n\n```pacman: sudo pacman -S git-annex```\n\n```apk: sudo apk add git-annex```\n\n```zypper: sudo zypper install git-annex```\n\n```brew: brew install git-annex```\n\n```nix: nix profile install nixpkgs#git-annex```"],["SEE ALSO","[git](/man/git)(1), [git-lfs](/man/git-lfs)(1), [git-clone](/man/git-clone)(1), [rsync](/man/rsync)(1)"],["RESOURCES","```[Source code](https://git.joeyh.name/index.cgi/git-annex.git/)```\n\n```[Homepage](https://git-annex.branchable.com/)```\n\n```[Documentation](https://git-annex.branchable.com/git-annex/)```"]],"e":["git","git-lfs","git-clone","rsync"],"x":"git annex init \"[laptop]\""},"git-check-mailmap":{"n":"git-check-mailmap","t":"Resolve canonical author and committer identities","s":[["TAGLINE","Resolve canonical author and committer identities"],["TLDR","**Check mailmap entry**\n\n```git check-mailmap \"[Name] <email@example.com>\"```\n\n**Map multiple entries**\n\n```git check-mailmap \"[Name1] <email1>\" \"[Name2] <email2>\"```\n\n**Read from stdin**\n\n```echo \"[Name] <email>\" | git check-mailmap --stdin```"],["SYNOPSIS","**git check-mailmap** [_options_] _contact_"],["PARAMETERS","_CONTACT_\n> Contact in \"Name <email>\" format.\n\n**--stdin**\n> Read contacts from stdin.\n\n**--mailmap-file** _FILE_\n> Also read the specified mailmap file, taking precedence over the repository's configured mailmap.\n\n**--mailmap-blob** _BLOB_\n> Like **--mailmap-file**, but read the mailmap from a blob in the repository.\n\n**--help**\n> Display help information."],["DESCRIPTION","**git check-mailmap** looks up canonical names and email addresses using the mailmap file. It translates author/committer identities to their canonical forms as configured in .mailmap.\n\nThe mailmap feature consolidates identity variations (typos, name changes, multiple emails) to canonical forms. This command shows how identities will be mapped in git log and shortlog output.\n\nThis is primarily a debugging tool for verifying that .mailmap entries are correctly resolving contributor identities before relying on them in reports or statistics."],["CONFIGURATION","**.mailmap**\n> Maps author and committer names/emails to canonical forms, consolidating identity variations in logs and statistics."],["CAVEATS","Requires .mailmap file in repository. Input must be proper \"Name <email>\" format. Used mainly for debugging."],["HISTORY","git check-mailmap was added to **Git** to support debugging mailmap configurations, complementing git shortlog -e functionality."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-shortlog](/man/git-shortlog)(1), [git-log](/man/git-log)(1)"],["RESOURCES","```[Source code](https://github.com/git/git)```\n\n```[Homepage](https://git-scm.com/)```\n\n```[Documentation](https://git-scm.com/docs/git-check-mailmap)```"]],"e":["git-shortlog","git-log"],"x":"git check-mailmap \"[Name] <email@example.com>\""},"git-lfs":{"n":"git-lfs","t":"Manage large file storage","s":[["TAGLINE","Manage large file storage"],["TLDR","**Install LFS for repo**\n\n```git lfs install```\n\n**Track file pattern**\n\n```git lfs track \"*.psd\"```\n\n**Show tracked patterns**\n\n```git lfs track```\n\n**List LFS files**\n\n```git lfs ls-files```\n\n**Fetch LFS objects**\n\n```git lfs fetch```"],["SYNOPSIS","**git lfs** _command_ [_options_]"],["PARAMETERS","**install**\n> Set up LFS for repository.\n\n**track** _PATTERN_\n> Track files matching pattern.\n\n**untrack** _PATTERN_\n> Stop tracking pattern.\n\n**ls-files**\n> List tracked LFS files.\n\n**fetch**\n> Download LFS objects.\n\n**pull**\n> Fetch and checkout LFS files.\n\n**push**\n> Push LFS objects to remote.\n\n**lock** _PATH_\n> Lock a file on the server so only you can push changes to it.\n\n**unlock** _PATH_\n> Release a server-side lock.\n\n**locks**\n> List locked files.\n\n**status**\n> Show LFS-tracked file status.\n\n**--help**\n> Display help information."],["DESCRIPTION","**git lfs** (Large File Storage) manages large files in Git repositories. Instead of storing large binary files directly in the repository, it replaces them with lightweight pointer files and stores the actual content on a separate LFS server.\n\nLFS significantly reduces repository size and clone times by downloading large files only on demand. It integrates transparently with standard Git commands, so `git add`, `git commit`, and `git push` work as expected while LFS handles the content storage efficiently behind the scenes."],["CONFIGURATION","**.gitattributes**\n> Specifies which file patterns are tracked by LFS.\n\n```\n*.psd filter=lfs diff=lfs merge=lfs -text\n*.zip filter=lfs diff=lfs merge=lfs -text\n```\n\n**.lfsconfig**\n> Repository-specific LFS configuration including endpoint URLs."],["CAVEATS","Requires LFS server support. Storage limits may apply. Some hosting has LFS quotas."],["HISTORY","Git LFS was created by **GitHub** in **2015** with Atlassian, addressing the long-standing problem of large files in git repositories."],["INSTALL","```apt: sudo apt install git-lfs```\n\n```dnf: sudo dnf install git-lfs```\n\n```pacman: sudo pacman -S git-lfs```\n\n```apk: sudo apk add git-lfs```\n\n```zypper: sudo zypper install git-lfs```\n\n```brew: brew install git-lfs```\n\n```nix: nix profile install nixpkgs#git-lfs```"],["SEE ALSO","[git](/man/git)(1), [git-annex](/man/git-annex)(1)"],["RESOURCES","```[Source code](https://github.com/git-lfs/git-lfs)```\n\n```[Homepage](https://git-lfs.com/)```\n\n```[Documentation](https://github.com/git-lfs/git-lfs/tree/main/docs)```"]],"e":["git","git-annex"],"x":"git lfs install"},"git-range-diff":{"n":"git-range-diff","t":"Compare two commit ranges","s":[["TAGLINE","Compare two commit ranges"],["TLDR","**Compare two revision ranges**\n\n```git range-diff [base1]..[rev1] [base2]..[rev2]```\n\n**Compare with upstream**\n\n```git range-diff @{u} @{1} @```\n\n**Compare rebase iterations**\n\n```git range-diff [main]..@{1} [main]..@```\n\n**Show with color**\n\n```git range-diff --color [range1] [range2]```"],["SYNOPSIS","**git** **range-diff** [_options_] _range1_ _range2_"],["PARAMETERS","**--creation-factor** _n_\n> Commit matching threshold.\n\n**--no-dual-color**\n> Disable dual color mode.\n\n**--notes** _ref_\n> Show notes."],["DESCRIPTION","**git range-diff** compares two commit ranges, showing how a series of patches changed between versions. It performs a diff-of-diffs, matching commits between the two ranges based on content similarity and revealing what changed during a rebase, amend, or other history rewrite.\n\nCommon use cases include comparing a feature branch before and after rebasing, reviewing how patches evolved during iterative development, or verifying that a rebase preserved intended changes. The output uses dual coloring to distinguish between the two levels of diff."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-diff](/man/git-diff)(1), [git-log](/man/git-log)(1), [git-rebase](/man/git-rebase)(1)"],["RESOURCES","```[Source code](https://github.com/git/git)```\n\n```[Homepage](https://git-scm.com/)```\n\n```[Documentation](https://git-scm.com/docs/git-range-diff)```"]],"e":["git-diff","git-log","git-rebase"],"x":"git range-diff [base1]..[rev1] [base2]..[rev2]"},"git-replace":{"n":"git-replace","t":"Create, list, or delete object replacement refs","s":[["TAGLINE","Create, list, or delete object replacement refs"],["TLDR","**Replace an object with another object**\n\n```git replace [object] [replacement]```\n\n**Force replace an object, overwriting an existing replacement**\n\n```git replace -f [object] [replacement]```\n\n**List all replacement refs**\n\n```git replace --list```\n\n**List replacement refs matching a pattern**\n\n```git replace --list 'refs/replace/abc*'```\n\n**Delete a replacement ref**\n\n```git replace -d [object]```\n\n**Edit an object interactively and create a replacement**\n\n```git replace --edit [object]```\n\n**Graft a commit to have different parents**\n\n```git replace --graft [commit] [parent1] [parent2]```\n\n**Convert legacy grafts file to replacement refs**\n\n```git replace --convert-graft-file```"],["SYNOPSIS","**git** **replace** [-f] _object_ _replacement_\n**git** **replace** [-f] **--edit** _object_\n**git** **replace** [-f] **--graft** _commit_ [_parent_...]\n**git** **replace** [-f] **--convert-graft-file**\n**git** **replace** **-d** _object_...\n**git** **replace** [**--format**=_format_] **-l** [_pattern_]"],["PARAMETERS","**-l** [_pattern_], **--list** [_pattern_]\n> List replacement refs. If a pattern is given via glob(7), only matching replacements are listed.\n\n**-d**, **--delete**\n> Delete existing replacement refs for the given objects.\n\n**-f**, **--force**\n> Overwrite an existing replace ref for the same object instead of failing.\n\n**--graft** _commit_ [_parent_...]\n> Create a graft commit. A new commit is created with the same content as the given commit but with the specified parents, then a replacement ref is created to replace the original commit.\n\n**--convert-graft-file**\n> Convert all entries in `$GIT_DIR/info/grafts` to replace refs and delete the grafts file. This is a one-time migration from the legacy grafts mechanism.\n\n**--edit** _object_\n> Edit an object's content interactively. The existing content is pretty-printed into a temporary file, an editor is launched, and the result is used to create a replacement object.\n\n**--raw**\n> When used with `--edit`, provide the raw object contents rather than pretty-printed ones. Useful for editing corrupted objects.\n\n**--format**=_format_\n> Format for `--list` output: `short` (replaced SHA only), `medium` (replaced -> replacement), or `long` (includes object types). Default: `short`."],["DESCRIPTION","**git replace** creates, lists, or deletes refs in the `refs/replace/` namespace that substitute one object for another. This allows changing how objects are viewed without modifying the original objects themselves.\n\nReplacements are used by default in all Git commands except reachability traversal (prune, pack transfer, fsck). To bypass replacements, use `git --no-replace-objects` or set the `GIT_NO_REPLACE_OBJECTS` environment variable.\n\nCommon use cases include fixing published history, grafting histories together, or replacing large blobs. The `--graft` option simplifies rewriting commit parentage, replacing the legacy `$GIT_DIR/info/grafts` mechanism."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-commit](/man/git-commit)(1), [git-tag](/man/git-tag)(1), [git-filter-branch](/man/git-filter-branch)(1), [git-fsck](/man/git-fsck)(1)"]],"e":["git-commit","git-tag","git-filter-branch","git-fsck"],"x":"git replace [object] [replacement]"},"git-sparse-checkout":{"n":"git-sparse-checkout","t":"Reduce working tree to a subset of tracked files","s":[["TAGLINE","Reduce working tree to a subset of tracked files"],["TLDR","**Enable sparse checkout and set directories** (cone mode)\n\n```git sparse-checkout set [dir1] [dir2]```\n\n**Add more directories to sparse checkout**\n\n```git sparse-checkout add [dir]```\n\n**List current sparse patterns**\n\n```git sparse-checkout list```\n\n**Reapply sparse patterns** after config changes\n\n```git sparse-checkout reapply```\n\n**Disable sparse checkout** and restore all files\n\n```git sparse-checkout disable```\n\n**Check which paths match sparse rules**\n\n```echo \"[path/to/file]\" | git sparse-checkout check-rules```\n\n**Preview files outside sparse definition** that would be removed\n\n```git sparse-checkout clean --dry-run```"],["SYNOPSIS","**git** **sparse-checkout** (_init_ | _list_ | _set_ | _add_ | _reapply_ | _disable_ | _check-rules_ | _clean_) [_options_]"],["SUBCOMMANDS","**init**\n> Deprecated. Use set instead to enable and configure sparse checkout.\n\n**set**\n> Define which directories/patterns to include in the working tree.\n\n**add**\n> Add additional directories/patterns to the sparse checkout.\n\n**list**\n> List the current sparse checkout patterns.\n\n**disable**\n> Disable sparse checkout and restore all files.\n\n**reapply**\n> Reapply patterns after manual config changes.\n\n**check-rules**\n> Check if paths match the current sparsity rules.\n\n**clean**\n> Remove files outside the sparse-checkout definition. Requires -f unless clean.requireForce is false."],["PARAMETERS","**--cone**\n> Use cone mode for directory-based patterns (default, faster).\n\n**--no-cone**\n> Use full pattern mode for arbitrary gitignore-style patterns.\n\n**--sparse-index**\n> Enable sparse index format for improved performance.\n\n**--no-sparse-index**\n> Disable sparse index format for compatibility with external tools.\n\n**--stdin**\n> Read patterns from stdin (one per line) instead of arguments.\n\n**-f**, **--force**\n> Allow cleaning without clean.requireForce config (for clean subcommand).\n\n**--dry-run**\n> Preview what clean would remove without deleting anything.\n\n**--rules-file** _file_\n> Match against rules in specified file instead of current rules (for check-rules).\n\n**-z**\n> Use NUL-terminated paths for stdin and output (for check-rules)."],["DESCRIPTION","**git sparse-checkout** enables partial repository checkouts, where only specified directories and files are materialized in the working tree. This significantly reduces working directory size for large monorepos.\n\nCone mode (the default) restricts patterns to directory-based inclusion, which is faster and simpler than full pattern matching. In cone mode, the set and add subcommands accept directory names rather than arbitrary gitignore patterns. Non-cone mode (--no-cone) allows arbitrary gitignore-style patterns but is deprecated due to poor performance and confusing semantics.\n\nThis command is experimental. Its behavior may change in the future."],["CONFIGURATION","**.git/info/sparse-checkout**\n> File containing the sparse-checkout patterns that control which paths are checked out."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-read-tree](/man/git-read-tree)(1), [git-checkout](/man/git-checkout)(1), [git-clone](/man/git-clone)(1), [git-worktree](/man/git-worktree)(1)"]],"e":["git-read-tree","git-checkout","git-clone","git-worktree"],"x":"git sparse-checkout set [dir1] [dir2]"},"glab-mr-merge":{"n":"glab-mr-merge","t":"Merge a GitLab merge request","s":[["TAGLINE","Merge a GitLab merge request"],["TLDR","**Merge a merge request**\n\n```glab mr merge [number]```\n\n**Merge with squash**\n\n```glab mr merge [number] --squash```\n\n**Merge without deleting branch**\n\n```glab mr merge [number] --remove-source-branch=false```\n\n**Merge when pipeline succeeds**\n\n```glab mr merge [number] --when-pipeline-succeeds```\n\n**Merge with a custom commit message**\n\n```glab mr merge [number] --message \"[commit message]\"```\n\n**Merge with rebase**\n\n```glab mr merge [number] --rebase```"],["SYNOPSIS","**glab** **mr** **merge** [_number_] [_options_]"],["PARAMETERS","**--squash**\n> Squash commits on merge.\n\n**--rebase**\n> Rebase the commits onto the base branch before merging.\n\n**--remove-source-branch**\n> Delete source branch after merge.\n\n**--when-pipeline-succeeds**\n> Set the merge request to merge when the pipeline succeeds.\n\n**--message** _text_\n> Custom merge commit message.\n\n**--sha** _sha_\n> Merge when the HEAD of the source branch matches the given SHA. Prevents merging unexpected changes.\n\n**-y**, **--yes**\n> Skip confirmation prompt."],["DESCRIPTION","**glab mr merge** merges a GitLab merge request. It supports squash merging, rebasing, automatic source branch deletion, and deferred merging that waits for the CI pipeline to pass before completing."],["INSTALL","```apt: sudo apt install glab```\n\n```dnf: sudo dnf install glab```\n\n```pacman: sudo pacman -S glab```\n\n```apk: sudo apk add glab```\n\n```zypper: sudo zypper install glab```\n\n```brew: brew install glab```\n\n```nix: nix profile install nixpkgs#glab```"],["SEE ALSO","[glab-mr](/man/glab-mr)(1), [glab-mr-create](/man/glab-mr-create)(1)"]],"e":["glab-mr","glab-mr-create"],"x":"glab mr merge [number]"},"glab-mr":{"n":"glab-mr","t":"Manage GitLab merge requests from the command line","s":[["TAGLINE","Manage GitLab merge requests from the command line"],["TLDR","**List merge requests**\n\n```glab mr list```\n\n**Create merge request**\n\n```glab mr create```\n\n**View merge request**\n\n```glab mr view [number]```\n\n**Checkout merge request**\n\n```glab mr checkout [number]```\n\n**Merge a merge request**\n\n```glab mr merge [number]```\n\n**Approve merge request**\n\n```glab mr approve [number]```"],["SYNOPSIS","**glab** **mr** _command_ [_options_]"],["SUBCOMMANDS","**list**\n> List merge requests.\n\n**create**\n> Open a new merge request.\n\n**view**\n> Display a merge request.\n\n**checkout**\n> Check out a merge request branch locally.\n\n**update**\n> Modify merge request details (title, description, labels, etc.).\n\n**merge**\n> Merge a merge request.\n\n**rebase**\n> Rebase a merge request's source branch against its target.\n\n**approve**\n> Approve a merge request.\n\n**approvers**\n> View or manage merge request approvers.\n\n**revoke**\n> Revoke approval of a merge request.\n\n**close**\n> Close a merge request.\n\n**reopen**\n> Reopen a closed merge request.\n\n**delete**\n> Delete a merge request.\n\n**diff**\n> Show changes in a merge request.\n\n**note**\n> Add a comment to a merge request.\n\n**issues**\n> View issues linked to a merge request.\n\n**subscribe** / **unsubscribe**\n> Subscribe to, or stop following, a merge request's notifications.\n\n**todo**\n> Add a merge request to your GitLab to-do list."],["DESCRIPTION","**glab mr** manages GitLab merge requests from the command line. It provides subcommands for creating, viewing, reviewing, approving, and merging MRs without leaving the terminal. Run `glab mr <subcommand> --help` for the flags of each subcommand."],["INSTALL","```apt: sudo apt install glab```\n\n```dnf: sudo dnf install glab```\n\n```pacman: sudo pacman -S glab```\n\n```apk: sudo apk add glab```\n\n```zypper: sudo zypper install glab```\n\n```brew: brew install glab```\n\n```nix: nix profile install nixpkgs#glab```"],["SEE ALSO","[glab](/man/glab)(1), [glab-mr-create](/man/glab-mr-create)(1)"],["RESOURCES","```[Documentation](https://docs.gitlab.com/cli/mr/)```\n\n```[Source code](https://gitlab.com/gitlab-org/cli)```"]],"e":["glab","glab-mr-create"],"x":"glab mr list"},"gnuchess":{"n":"gnuchess","t":"GNU Chess engine","s":[["TAGLINE","GNU Chess engine"],["TLDR","**Start an interactive chess game**\n\n```gnuchess```\n\n**Play in two-player mode** (user vs user)\n\n```gnuchess -u```\n\n**Play in easy mode** (no pondering)\n\n```gnuchess -e```\n\n**Load a game from a PGN file**\n\n```gnuchess -l [game.pgn]```\n\n**Set search depth level**\n\n```gnuchess -S [5]```\n\n**Run as UCI engine**\n\n```gnuchess --uci```\n\n**Run as XBoard engine**\n\n```gnuchess --xboard```"],["SYNOPSIS","**gnuchess** [_options_]"],["PARAMETERS","**-h**, **--help**\n> Display usage help.\n\n**-v**, **--version**\n> Display version information.\n\n**-u**\n> Two-player mode (user vs user).\n\n**-e**\n> Easy mode. The engine does not think during the opponent's turn.\n\n**-l** _PGNFILE_, **--load** _PGNFILE_\n> Load game from a PGN file.\n\n**-S** _LEVEL_, **--setlevel** _LEVEL_\n> Set search depth level (1-10).\n\n**--xboard**, **-x**\n> XBoard/WinBoard engine protocol mode.\n\n**--uci**\n> UCI engine protocol mode.\n\n**--post**, **-p**\n> Show engine thinking output.\n\n**--manual**, **-m**\n> Manual mode. The engine does not play until told to.\n\n**-M** _SIZE_, **--memory** _SIZE_\n> Set hash table memory size in MB."],["PREVIEW","```\n r n b q k b n r\n p p p p p p p p\n . . . . . . . .\n . . . . . . . .\n P P P P P P P P\n R N B Q K B N R\n```"],["DESCRIPTION","**gnuchess** is a free chess-playing program that can be used as a standalone interactive chess game or as a backend engine for graphical chess frontends like **XBoard** or **GNOME Chess**. It supports both the **XBoard** and **UCI** communication protocols.\n\nIn interactive mode, enter moves in algebraic notation (e.g. e2e4, Nf3). Type **help** for a list of commands. The engine includes an opening book and uses alpha-beta search with iterative deepening."],["CAVEATS","The default opening book may not be installed on all systems. Engine strength depends on the search depth and available memory for hash tables."],["HISTORY","GNU Chess was first written by **Stuart Cracraft** in **1984**, making it one of the earliest programs in the GNU Project. It has been maintained by numerous contributors over the decades and remains one of the most well-known free software chess engines."],["INSTALL","```dnf: sudo dnf install gnuchess```\n\n```pacman: sudo pacman -S gnuchess```\n\n```apk: sudo apk add gnuchess```\n\n```zypper: sudo zypper install gnuchess```\n\n```nix: nix profile install nixpkgs#gnuchess```"],["SEE ALSO","[nbcheckers](/man/nbcheckers)(1), [gnugo](/man/gnugo)(1)"]],"e":["nbcheckers","gnugo"],"x":"gnuchess"},"gradle":{"n":"gradle","t":"JVM build automation tool","s":[["TAGLINE","JVM build automation tool"],["TLDR","**Build project**\n\n```gradle build```\n\n**Run tests**\n\n```gradle test```\n\n**Clean and rebuild**\n\n```gradle clean build```\n\n**List available tasks**\n\n```gradle tasks```\n\n**Build with parallel execution**\n\n```gradle build --parallel```\n\n**Run with a build scan** for detailed reporting\n\n```gradle build --scan```\n\n**Use the configuration cache** for faster repeat builds\n\n```gradle build --configuration-cache```\n\n**Use a specific build file**\n\n```gradle -b [path/to/build.gradle] build```"],["SYNOPSIS","**gradle** [_options_] [_tasks_...]"],["PARAMETERS","_TASKS_\n> Tasks to execute.\n\n**--daemon**\n> Use Gradle daemon (enabled by default).\n\n**--no-daemon**\n> Do not use the Gradle daemon.\n\n**-q**, **--quiet**\n> Log errors only.\n\n**-i**, **--info**\n> Set log level to info.\n\n**-d**, **--debug**\n> Set log level to debug.\n\n**--parallel**\n> Build projects in parallel.\n\n**--configuration-cache**\n> Enable the configuration cache for faster repeat builds.\n\n**--scan**\n> Create a build scan at scans.gradle.com.\n\n**-b**, **--build-file** _file_\n> Specify the build file.\n\n**-c**, **--settings-file** _file_\n> Specify the settings file.\n\n**--refresh-dependencies**\n> Refresh the state of dependencies.\n\n**-x**, **--exclude-task** _task_\n> Exclude a task from execution.\n\n**--continuous**\n> Continuous build; re-execute on file changes.\n\n**--help**\n> Display help information."],["DESCRIPTION","**gradle** is a build automation tool for Java, Kotlin, Android, and other JVM projects. It uses Groovy or Kotlin DSL for build scripts and provides dependency management, incremental builds, and an extensive plugin ecosystem.\n\nGradle is the standard build system for Android development and supports multi-project builds. The Gradle daemon runs persistently in the background to improve build performance and is enabled by default. In most projects, the **Gradle Wrapper** (`./gradlew`) is preferred over invoking `gradle` directly to ensure consistent build tool versions."],["CONFIGURATION","**build.gradle** / **build.gradle.kts**\n> Project build script defining tasks, dependencies, and plugins.\n\n**gradle.properties**\n> Project and user-level properties for build configuration.\n\n**settings.gradle** / **settings.gradle.kts**\n> Multi-project build settings and included modules.\n\n**~/.gradle/gradle.properties**\n> User-level Gradle properties applied to all builds."],["CAVEATS","JVM required. First run downloads dependencies and the Gradle distribution. The daemon is enabled by default; use **--no-daemon** to disable. Prefer the Gradle Wrapper (**./gradlew**) to ensure reproducible builds."],["HISTORY","Gradle was created by **Hans Dockter**, first released in **2007**, becoming the official Android build system in **2013**. Kotlin DSL support was added in Gradle 5.0 (2018). The configuration cache for faster builds was stabilized in Gradle 8.1 (2023)."],["INSTALL","```pacman: sudo pacman -S gradle```\n\n```apk: sudo apk add gradle```\n\n```brew: brew install gradle```\n\n```nix: nix profile install nixpkgs#gradle```"],["SEE ALSO","[gradle-wrapper](/man/gradle-wrapper)(1), [mvn](/man/mvn)(1), [ant](/man/ant)(1)"]],"e":["gradle-wrapper","mvn","ant"],"x":"gradle build"},"groupdel":{"n":"groupdel","t":"delete a group from the system","s":[["TAGLINE","delete a group from the system"],["TLDR","Remove a group from the system by **name**\n\n```sudo groupdel [group_name]```"],["SYNOPSIS","**groupdel** [_options_] _group_"],["PARAMETERS","**-f**, **--force**\n> Force removal of the group, even if a user has it as their primary group\n\n**-h**, **--help**\n> Display help message and exit\n\n**-R**, **--root** _CHROOT_DIR_\n> Apply changes in the CHROOT_DIR directory and use its configuration files. Only absolute paths are supported\n\n**-P**, **--prefix** _PREFIX_DIR_\n> Apply changes in the PREFIX_DIR directory. Does not chroot; intended for preparing a cross-compilation target"],["DESCRIPTION","**groupdel** removes a group entry from the system. It modifies /etc/group and /etc/gshadow. The named group must exist. You may not remove the primary group of any existing user; the user must be removed first.\n\nFiles owned by the deleted group retain their numeric GID but lose the group name association. You should manually check all file systems to ensure no files remain owned by the deleted group."],["CAVEATS","Cannot delete a group if any user has it as their primary group; the user must be removed first. Logged-in users who are secondary members won't lose access until their next login. Files owned by the group will show numeric GID instead of group name after deletion. Exit values: 0 (success), 2 (invalid syntax), 6 (group doesn't exist), 8 (can't remove user's primary group), 10 (can't update group file)."],["HISTORY","groupdel is part of the shadow-utils package, providing the standard Unix group management tools. It works in conjunction with groupadd and groupmod for complete group lifecycle management."],["INSTALL","```apt: sudo apt install passwd```\n\n```pacman: sudo pacman -S shadow```\n\n```apk: sudo apk add shadow```\n\n```zypper: sudo zypper install shadow```\n\n```nix: nix profile install nixpkgs#shadow```"],["SEE ALSO","[groupadd](/man/groupadd)(8), [groupmod](/man/groupmod)(8), [gpasswd](/man/gpasswd)(1), [userdel](/man/userdel)(8), [chgrp](/man/chgrp)(1), [groups](/man/groups)(1)"]],"e":["groupadd","groupmod","gpasswd","userdel","chgrp","groups"],"x":"sudo groupdel [group_name]"},"grub-set-default":{"n":"grub-set-default","t":"set permanent default GRUB boot entry","s":[["TAGLINE","set permanent default GRUB boot entry"],["TLDR","Set the **default boot entry** permanently\n\n```sudo grub-set-default [entry_number]```\n\nSet default for an **alternative boot directory**\n\n```sudo grub-set-default --boot-directory /[path/to/boot_directory] [entry_number]```"],["SYNOPSIS","**grub-set-default** [_options_] _entry_"],["PARAMETERS","_entry_\n> Menu entry number (0-indexed), name, or identifier\n\n**--boot-directory** _DIR_\n> Use alternative boot directory instead of /boot"],["DESCRIPTION","**grub-set-default** sets the permanent default boot entry for GRUB. Unlike **grub-reboot**, this setting persists across all subsequent boots until changed.\n\nThe entry can be specified as a number (0 for first entry), the full entry name from grub.cfg, or a menu entry identifier. Submenu entries use notation like \"1>2\" for the third item in the second submenu."],["CONFIGURATION","**/etc/default/grub**\n> Must contain GRUB_DEFAULT=saved for grub-set-default to take effect."],["CAVEATS","Requires GRUB_DEFAULT=saved in /etc/default/grub and regenerating the configuration with grub-mkconfig. Without this, the setting in grubenv is ignored. Entry numbers can change when kernels are added or removed."],["HISTORY","grub-set-default is part of GRUB 2's saved default functionality. It provides a way to change the boot default without editing configuration files, storing the selection in the grubenv file."],["INSTALL","```apt: sudo apt install grub2-common```\n\n```dnf: sudo dnf install grub2-common```\n\n```pacman: sudo pacman -S grub```\n\n```apk: sudo apk add grub```\n\n```zypper: sudo zypper install grub2-common```"],["SEE ALSO","[grub-reboot](/man/grub-reboot)(8), [grub-mkconfig](/man/grub-mkconfig)(8), [grub-editenv](/man/grub-editenv)(1)"],["RESOURCES","```[Homepage](https://www.gnu.org/software/grub/)```\n\n```[Source code](https://git.savannah.gnu.org/cgit/grub.git)```\n\n```[Documentation](https://www.gnu.org/software/grub/manual/grub/grub.html)```"]],"e":["grub-reboot","grub-mkconfig","grub-editenv"],"x":"sudo grub-set-default [entry_number]"},"gst-play-1.0":{"n":"gst-play-1.0","t":"Simple command line media playback tool using GStreamer","s":[["TAGLINE","Simple command line media playback tool using GStreamer"],["TLDR","**Play a media file**\n\n```gst-play-1.0 [path/to/file.mp4]```\n\n**Play multiple files**\n\n```gst-play-1.0 [file1.mp3] [file2.mp3] [file3.mp3]```\n\n**Play all media files in a directory** recursively\n\n```gst-play-1.0 [path/to/directory]```\n\n**Play a file with a specific video sink**\n\n```gst-play-1.0 --videosink=ximagesink [path/to/file.mp4]```\n\n**Play files in shuffle order**\n\n```gst-play-1.0 --shuffle [file1.mp3] [file2.mp3] [file3.mp3]```\n\n**Play a file with gapless playback enabled**\n\n```gst-play-1.0 --gapless [file1.mp3] [file2.mp3]```\n\n**Play a stream from a URI**\n\n```gst-play-1.0 [https://example.com/stream.mp3]```\n\n**Play a file at half volume**\n\n```gst-play-1.0 --volume=0.5 [path/to/file.mp3]```"],["SYNOPSIS","**gst-play-1.0** [_options_] _FILE_|_DIRECTORY_|_URI_ [_FILE2_|_DIRECTORY2_|_URI2_ ...]"],["PARAMETERS","**--audiosink=**_SINK_\n> Use the specified audio sink instead of **autoaudiosink**.\n\n**--videosink=**_SINK_\n> Use the specified video sink instead of **autovideosink**.\n\n**--volume=**_VOLUME_\n> Set initial playback volume. **0.0** is silent, **1.0** is unchanged (default).\n\n**--shuffle**\n> Play files in random order.\n\n**--no-interactive**\n> Disable interactive keyboard control.\n\n**--gapless**\n> Enable gapless (seamless) playback between tracks.\n\n**-v**, **--verbose**\n> Output status information and property notifications.\n\n**-q**, **--quiet**\n> Suppress all output except errors.\n\n**--use-playbin3**\n> Use the playbin3 pipeline instead of the default playbin.\n\n**--wait-on-eos**\n> Keep the last video frame displayed after playback ends instead of closing.\n\n**--flags**\n> Control playback behavior via the playbin **flags** property.\n\n**--version**\n> Print version information and exit.\n\n**--help**\n> Show available options and exit."],["DESCRIPTION","**gst-play-1.0** is a command line playback testing tool that uses GStreamer's **playbin** element to play audio and video files. It accepts individual files, directories (which are traversed recursively), and URIs as input.\n\nThe tool provides interactive keyboard controls during playback for pausing, seeking, adjusting volume, changing playback speed, and switching between audio, video, and subtitle tracks. It supports gapless playback for seamless transitions between tracks and can play media from local files or network streams."],["INTERACTIVE CONTROLS","```\nSPACE     Pause / Resume\nArrow Up/Down    Volume up / down\nArrow Left/Right Seek backward / forward 10 seconds\n+/-       Increase / decrease playback rate\nd         Reverse playback direction\nt         Cycle trick modes\na         Cycle audio tracks\ns         Cycle subtitle tracks\nv         Cycle video tracks\n> or n    Next track in playlist\n< or b    Previous track in playlist\n0         Seek back to beginning\nQ or ESC  Quit\n```"],["CAVEATS","**gst-play-1.0** is intended as a testing and debugging tool rather than a full-featured media player. Playback capabilities depend entirely on which GStreamer plugins are installed on the system. Missing codecs will result in playback errors. Interactive keyboard controls require a terminal and are disabled when stdin is not a TTY."],["HISTORY","**gst-play-1.0** was introduced as part of the **GStreamer** multimedia framework's base plugins. GStreamer itself was created by **Erik Walthinsen** in **1999** and has grown into the standard multimedia framework on Linux. The play tool provides a simple way to test playback pipelines without constructing them manually via **gst-launch-1.0**. It replaced the older **gst-play-0.10** tool when GStreamer transitioned to the 1.x series in **2012**."],["INSTALL","```apt: sudo apt install gstreamer1.0-plugins-base-apps```\n\n```pacman: sudo pacman -S gst-plugins-base```\n\n```apk: sudo apk add gst-plugins-base```\n\n```brew: brew install gst-plugins-base```"],["SEE ALSO","[gst-launch-1.0](/man/gst-launch-1.0)(1), [gst-inspect-1.0](/man/gst-inspect-1.0)(1), [gst-discoverer-1.0](/man/gst-discoverer-1.0)(1), [ffplay](/man/ffplay)(1), [mpv](/man/mpv)(1)"]],"e":["gst-launch-1.0","gst-inspect-1.0","gst-discoverer-1.0","ffplay","mpv"],"x":"gst-play-1.0 [path/to/file.mp4]"},"hishtory":{"n":"hishtory","t":"Synced queryable shell history with context","s":[["TAGLINE","Synced queryable shell history with context"],["TLDR","**Install and enable** hishtory\n\n```hishtory install```\n\n**Query shell history**\n\n```hishtory query [search_term]```\n\n**Enable Control+R integration**\n\n```hishtory config-set enable-control-r true```\n\n**Search with AI assistance** (prefix with ?)\n\n```hishtory query ?[how to find large files]```\n\n**Export all history entries**\n\n```hishtory export```\n\n**Show sync and configuration status**\n\n```hishtory status```\n\n**Delete history entries matching a query**\n\n```hishtory redact [search_term]```\n\n**Temporarily disable recording**\n\n```hishtory disable```"],["SYNOPSIS","**hishtory** _command_ [_options_] [_arguments_]"],["DESCRIPTION","**hishtory** is a shell history tool that replaces the default history with a synced, queryable, and context-aware alternative. It hooks into your shell to track all commands that are run, saving them in a local **SQLite** database with additional context like the working directory, exit code, and duration.\n\nHistory is **end-to-end encrypted** with AES-GCM and can be synced across all your devices. When you press **Control+R** or run **hishtory query**, a SQL query finds matching entries in the local database. Queries can also be prefixed with **?** to use ChatGPT for natural language command search."],["PARAMETERS","**install**\n> Install and configure hishtory\n\n**query** _term_\n> Search shell history\n\n**export**\n> Export all history entries\n\n**status**\n> Show sync and configuration status\n\n**config-set** _key_ _value_\n> Set a configuration option\n\n**config-get** _key_\n> Get a configuration value\n\n**redact** _query_\n> Delete matching history entries locally and from all synced devices.\n\n**update**\n> Securely download and apply the latest hishtory update.\n\n**disable**\n> Temporarily stop recording shell history.\n\n**enable**\n> Resume recording shell history."],["CAVEATS","Requires an active shell session to record commands. Syncing across devices requires internet connectivity. The AI-powered search feature requires a ChatGPT API connection. End-to-end encryption means lost encryption keys result in unrecoverable data. Commands prefixed with a space are not recorded, following shell conventions."],["HISTORY","**hishtory** was created by **David Dworken** and is written in **Go**. It was designed to address the limitations of built-in shell history by adding encryption, cross-device sync, and contextual metadata."],["INSTALL","```brew: brew install hishtory```\n\n```nix: nix profile install nixpkgs#hishtory```"],["SEE ALSO","[history](/man/history)(1), [atuin](/man/atuin)(1), [fzf](/man/fzf)(1)"]],"e":["history","atuin","fzf"],"x":"hishtory install"},"ibmcloud-login":{"n":"ibmcloud-login","t":"authenticates to IBM Cloud","s":[["TAGLINE","authenticates to IBM Cloud"],["TLDR","**Login interactively**\n\n```ibmcloud login```\n\n**Login with SSO**\n\n```ibmcloud login --sso```\n\n**Login with API key**\n\n```ibmcloud login --apikey [API_KEY]```\n\n**Login to specific region**\n\n```ibmcloud login -r [us-south]```\n\n**Login with username and password**\n\n```ibmcloud login -u [user@example.com] -p [password]```\n\n**Login to specific API endpoint**\n\n```ibmcloud login -a [https://cloud.ibm.com]```\n\n**Login with API key from file**\n\n```ibmcloud login --apikey @[keyfile]```"],["SYNOPSIS","**ibmcloud** **login** [_options_]"],["PARAMETERS","**-u** _user_\n> Username (email).\n\n**-p** _password_\n> Password.\n\n**--sso**\n> Use single sign-on (federated ID).\n\n**--apikey** _key_\n> API key for authentication.\n\n**-a** _endpoint_\n> API endpoint URL.\n\n**-r** _region_\n> Target region.\n\n**-c** _account_\n> Target account ID.\n\n**-o** _org_\n> Target organization.\n\n**-s** _space_\n> Target space."],["DESCRIPTION","**ibmcloud login** authenticates to IBM Cloud. Supports interactive login, SSO for federated IDs, and API key authentication. After login, use **ibmcloud target** to set account, region, or resource group context. Environment variables IC_API_KEY, IC_API_ENDPOINT, and IC_REGION can be used for automation."],["SEE ALSO","[ibmcloud](/man/ibmcloud)(1), [ibmcloud-target](/man/ibmcloud-target)(1), [ibmcloud-api](/man/ibmcloud-api)(1)"]],"e":["ibmcloud","ibmcloud-target","ibmcloud-api"],"x":"ibmcloud login"},"ifup":{"n":"ifup","t":"brings up network interfaces configured in **/etc/network/interfaces**","s":[["TAGLINE","brings up network interfaces configured in **/etc/network/interfaces**"],["TLDR","**Enable** a specific interface\n\n```ifup [eth0]```\n\nEnable **all** interfaces marked as auto\n\n```ifup -a```"],["SYNOPSIS","**ifup** [_options_] _interface_"],["PARAMETERS","**-a**, **--all**\n> Bring up all interfaces marked as auto in /etc/network/interfaces\n\n**-v**, **--verbose**\n> Print verbose information\n\n**--force**\n> Force configuration even if interface seems to be up\n\n**--no-scripts**\n> Don't run any pre/post scripts"],["DESCRIPTION","**ifup** brings up network interfaces configured in **/etc/network/interfaces**. It reads the interface definition and executes the appropriate commands and scripts to configure the interface, including setting IP addresses, running DHCP clients, and executing user-defined scripts.\n\nThe command handles the complete interface initialization sequence, including any dependencies or prerequisites defined in the configuration file."],["CAVEATS","Part of the ifupdown package, primarily used on Debian-based systems. Modern systems often use NetworkManager or systemd-networkd instead. Requires the interface to be defined in /etc/network/interfaces."],["HISTORY","ifup is part of the ifupdown package, the traditional network interface configuration system on Debian and derivatives. While still widely used, it is gradually being replaced by NetworkManager for desktop systems and systemd-networkd for servers."],["INSTALL","```apt: sudo apt install ifupdown```\n\n```apk: sudo apk add ifupdown-ng```\n\n```nix: nix profile install nixpkgs#ifupdown-ng```"],["SEE ALSO","[ifdown](/man/ifdown)(8), [ip](/man/ip)(8)"]],"e":["ifdown","ip"],"x":"ifup [eth0]"},"ike-scan":{"n":"ike-scan","t":"discovers and fingerprints IPsec VPN servers","s":[["TAGLINE","discovers and fingerprints IPsec VPN servers"],["TLDR","**Scan for IKE hosts**\n\n```sudo ike-scan [192.168.1.0/24]```\n\n**Aggressive mode**\n\n```sudo ike-scan -A [target]```\n\n**Specify transform**\n\n```sudo ike-scan --trans=[5,2,1,2] [target]```\n\n**Show vendor ID**\n\n```sudo ike-scan -M [target]```\n\n**Source port**\n\n```sudo ike-scan -s [500] [target]```"],["SYNOPSIS","**ike-scan** [_options_] _targets_"],["PARAMETERS","_TARGETS_\n> IP addresses or ranges.\n\n**-A**, **--aggressive**\n> Aggressive mode.\n\n**-M**, **--multiline**\n> Multiline output.\n\n**--trans** _SET_\n> Transform set.\n\n**-s** _PORT_\n> Source port.\n\n**-d** _PORT_\n> Destination port.\n\n**--help**\n> Display help information."],["DESCRIPTION","**ike-scan** discovers and fingerprints IPsec VPN servers. It sends IKE packets and analyzes responses to identify VPN implementations.\n\nThe tool performs IKE protocol scanning for security assessment. It identifies vendor, version, and supported transforms."],["CAVEATS","Security testing tool. Authorization required. UDP port 500."],["HISTORY","ike-scan was created by **Roy Hills** for IPsec VPN discovery and fingerprinting."],["INSTALL","```apt: sudo apt install ike-scan```\n\n```brew: brew install ike-scan```\n\n```nix: nix profile install nixpkgs#ike-scan```"],["SEE ALSO","[nmap](/man/nmap)(1), [ipsec](/man/ipsec)(8)"]],"e":["nmap","ipsec"],"x":"sudo ike-scan [192.168.1.0/24]"},"jdupes":{"n":"jdupes","t":"fast duplicate file finder and remover","s":[["TAGLINE","fast duplicate file finder and remover"],["TLDR","**Find duplicate files** in a directory\n\n```jdupes [path/to/directory]```\n\n**Find duplicates recursively**\n\n```jdupes -r [path/to/directory]```\n\n**Delete duplicates interactively**\n\n```jdupes -r -d [path/to/directory]```\n\n**Delete duplicates automatically** (keep first)\n\n```jdupes -r -dN [path/to/directory]```\n\n**Show duplicate sizes**\n\n```jdupes -r -S [path/to/directory]```\n\n**Use hard links** instead of deleting\n\n```jdupes -r -L [path/to/directory]```\n\n**Summarize duplicate statistics**\n\n```jdupes -r -m [path/to/directory]```\n\n**Compare across multiple directories**\n\n```jdupes -r [dir1] [dir2] [dir3]```"],["SYNOPSIS","**jdupes** [_options_] _directory_ [_directory ..._]"],["PARAMETERS","**-r**, **--recurse**\n> Recurse into subdirectories for all given directories.\n\n**-R**, **--recurse:**\n> Recurse into subdirectories only for directories given after this flag.\n\n**-d**, **--delete**\n> Prompt for which duplicates to delete.\n\n**-N**, **--noprompt**\n> With -d, delete without prompting (keep first).\n\n**-D**, **--debug**\n> Show debug/diagnostic output.\n\n**-S**, **--size**\n> Show size of duplicate files.\n\n**-m**, **--summarize**\n> Print summary of duplicate statistics.\n\n**-L**, **--linkhard**\n> Replace duplicates with hard links.\n\n**-s**, **--symlinks**\n> Follow symbolic links.\n\n**-H**, **--hardlinks**\n> Treat hard links as duplicates.\n\n**-n**, **--noempty**\n> Exclude zero-length files.\n\n**-A**, **--nohidden**\n> Exclude hidden files.\n\n**-f**, **--omitfirst**\n> Omit the first file in each duplicate set.\n\n**-1**, **--sameline**\n> List each duplicate set on a single line.\n\n**-0**, **--printnull**\n> Use null character as line terminator.\n\n**-q**, **--quiet**\n> Hide progress indicator.\n\n**-T**, **--partial-only**\n> Match based on partial hash only, ignoring the rest.\n\n**-Q**, **--quick**\n> Skip byte-by-byte verification (use hashes only).\n\n**-X**, **--xsize** _[+-=]SIZE_\n> Exclude files by size criteria.\n\n**-o**, **--order** _by_\n> Order files by: name, time, none."],["DESCRIPTION","**jdupes** is a fast duplicate file finder and remover. It identifies files with identical content by comparing file sizes, then partial hashes, then full hashes, and optionally byte-by-byte comparison for certainty.\n\nThe tool is a fork and enhancement of the older **fdupes** program, offering better performance (often 7x faster) and additional features. It uses optimized comparison algorithms and can handle millions of files efficiently.\n\nDuplicates can be handled in several ways: interactive deletion (-d) presents choices for each set, automatic deletion (-dN) keeps the first file, and hard linking (-L) replaces duplicates with links to save space while preserving all filenames.\n\nWhen scanning multiple directories, jdupes compares files across all of them, useful for finding duplicates between a backup and primary storage. The summary mode (-m) gives a quick overview of potential space savings.\n\nThe ordering option (-o) controls which file is considered \"first\" for automatic operations, allowing preference for keeping newer or alphabetically-first files."],["CAVEATS","Hard linking only works on the same filesystem. Automatic deletion (-dN) with multiple directories may not preserve the expected file. Very large file counts may require significant memory. Quick mode (-Q) skips verification and could theoretically produce false positives."],["HISTORY","**jdupes** was created by Jody Bruchon starting around **2015** as an enhanced fork of fdupes. The primary goals were improved performance and additional features while maintaining compatibility. The name combines \"Jody's\" with \"dupes.\" It has become a popular choice for duplicate management, available in most Linux distribution repositories."],["INSTALL","```apt: sudo apt install jdupes```\n\n```dnf: sudo dnf install jdupes```\n\n```zypper: sudo zypper install jdupes```\n\n```brew: brew install jdupes```\n\n```nix: nix profile install nixpkgs#jdupes```"],["SEE ALSO","[fdupes](/man/fdupes)(1), [rdfind](/man/rdfind)(1), [duperemove](/man/duperemove)(8), [rmlint](/man/rmlint)(1)"]],"e":["fdupes","rdfind","duperemove","rmlint"],"x":"jdupes [path/to/directory]"},"kubectl-api-resources":{"n":"kubectl-api-resources","t":"lists all resource types available in the Kubernetes cluster","s":[["TAGLINE","lists all resource types available in the Kubernetes cluster"],["TLDR","**List all API resources**\n\n```kubectl api-resources```\n\n**List namespaced resources only**\n\n```kubectl api-resources --namespaced=true```\n\n**List resources with specific API group**\n\n```kubectl api-resources --api-group=[apps]```\n\n**Show resource short names**\n\n```kubectl api-resources -o wide```\n\n**List verbs for resources**\n\n```kubectl api-resources --verbs=[list,get]```"],["SYNOPSIS","**kubectl** **api-resources** [_options_]"],["PARAMETERS","**--namespaced** _bool_\n> Filter namespaced/cluster-scoped resources.\n\n**--api-group** _group_\n> Filter by API group.\n\n**--verbs** _verbs_\n> Filter by supported verbs.\n\n**-o** _format_\n> Output format (wide, name)."],["DESCRIPTION","**kubectl api-resources** lists all resource types available in the Kubernetes cluster. Shows resource names, short names, API group, and whether they are namespaced. Useful for discovering available resource types."],["INSTALL","```apt: sudo apt install kubectl```\n\n```pacman: sudo pacman -S kubectl```\n\n```apk: sudo apk add kubectl```\n\n```brew: brew install kubectl```\n\n```nix: nix profile install nixpkgs#kubectl```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [kubectl-api-versions](/man/kubectl-api-versions)(1)"]],"e":["kubectl","kubectl-api-versions"],"x":"kubectl api-resources"},"kubectl-replace":{"n":"kubectl-replace","t":"Replace a resource by filename or stdin","s":[["TAGLINE","Replace a resource by filename or stdin"],["TLDR","**Replace resource from file**\n\n```kubectl replace -f [manifest.yaml]```\n\n**Force replace (delete and recreate)**\n\n```kubectl replace --force -f [manifest.yaml]```\n\n**Replace from stdin**\n\n```cat [manifest.yaml] | kubectl replace -f -```\n\n**Dry run without applying changes**\n\n```kubectl replace -f [manifest.yaml] --dry-run=client```\n\n**Replace and process a kustomization directory**\n\n```kubectl replace -k [directory/]```\n\n**Replace resources from multiple files**\n\n```kubectl replace -f [file1.yaml] -f [file2.yaml]```"],["SYNOPSIS","**kubectl** **replace** **-f** _FILENAME_ [_options_]"],["PARAMETERS","**-f**, **--filename** _file_\n> Filename, directory, or URL to file to use to replace the resource.\n\n**-k**, **--kustomize** _dir_\n> Process a kustomization directory. Cannot be used with -f or -R.\n\n**-R**, **--recursive**\n> Process the directory used in -f recursively.\n\n**--force**\n> If true, immediately remove resources from API and bypass graceful deletion.\n\n**--cascade** _mode_\n> Cascade deletion strategy for dependents: \"background\", \"orphan\", or \"foreground\". Default: \"background\".\n\n**--grace-period** _seconds_\n> Period of time in seconds given to the resource to terminate gracefully. Ignored if negative. Default: -1.\n\n**--dry-run** _strategy_\n> Must be \"none\", \"server\", or \"client\". Default: \"none\".\n\n**--validate** _mode_\n> Validation mode: \"strict\", \"warn\", or \"ignore\". Default: \"strict\".\n\n**-o**, **--output** _format_\n> Output format: json, yaml, name, go-template, jsonpath, etc.\n\n**--save-config**\n> Save the configuration of the current object in its annotation for future use with kubectl apply.\n\n**--field-manager** _name_\n> Name of the manager used to track field ownership. Default: \"kubectl-replace\".\n\n**--subresource** _name_\n> If specified, replace will operate on the subresource of the requested object.\n\n**--timeout** _duration_\n> Length of time to wait before giving up on a delete.\n\n**--wait**\n> If true, wait for resources to be gone before returning.\n\n**--raw** _URI_\n> Raw URI to PUT to the server."],["DESCRIPTION","**kubectl replace** replaces a resource by filename or stdin. JSON and YAML formats are accepted. The resource must already exist. Unlike apply, replace requires the complete resource specification, which can be obtained with `kubectl get TYPE NAME -o yaml`. Use --force to delete and recreate the resource."],["INSTALL","```apt: sudo apt install kubectl```\n\n```pacman: sudo pacman -S kubectl```\n\n```apk: sudo apk add kubectl```\n\n```brew: brew install kubectl```\n\n```nix: nix profile install nixpkgs#kubectl```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [kubectl-apply](/man/kubectl-apply)(1), [kubectl-create](/man/kubectl-create)(1), [kubectl-delete](/man/kubectl-delete)(1), [kubectl-edit](/man/kubectl-edit)(1), [kubectl-patch](/man/kubectl-patch)(1)"]],"e":["kubectl","kubectl-apply","kubectl-create","kubectl-delete","kubectl-edit","kubectl-patch"],"x":"kubectl replace -f [manifest.yaml]"},"labgrid-mcp":{"n":"labgrid-mcp","t":"MCP server for labgrid hardware-in-the-loop device control","s":[["TAGLINE","MCP server for labgrid hardware-in-the-loop device control"],["TLDR","**Run the demo lab** (no real hardware; prints MCP client config)\n\n```labgrid-mcp demo```\n\n```uvx labgrid-mcp demo```\n\n**Start the MCP server** against a labgrid coordinator (stdio)\n\n```LG_COORDINATOR=[host:20408] labgrid-mcp```\n\n**One-shot via uvx** without a permanent install\n\n```uvx labgrid-mcp```\n\n**Demo on a custom coordinator port**\n\n```labgrid-mcp demo --port [20409]```"],["SYNOPSIS","**labgrid-mcp** [_command_] [_options_]"],["PARAMETERS","**(no subcommand)**\n> Serve the MCP protocol over **stdio**. This is the shape every MCP client spawns. Configuration comes from environment variables (see CONFIGURATION).\n\n**demo** [**--port** _n_]\n> Boot a hardware-free local lab: real labgrid coordinator and exporter plus a fake board (power switch and serial console). Prints a paste-ready MCP client snippet. Default port is the demo coordinator port; **Ctrl-C** tears everything down."],["DESCRIPTION","**labgrid-mcp** is a Model Context Protocol (MCP) server that exposes [labgrid](https://github.com/labgrid-project/labgrid) lab operations to AI agents and other MCP clients. labgrid is the open-source framework embedded teams use to share boards (\"places\") with remotely switchable power, serial consoles, USB muxes, and flashers. This package plugs a gRPC-era labgrid coordinator into MCP so agents can reserve hardware, power-cycle, open consoles, SSH, and (when explicitly allowed) flash devices.\n\nIt registers on the order of **47 tools** (list/acquire places, power and I/O, console open/read/send, SSH and tunnels, optional flash jobs, place metadata) plus browseable **labgrid://** resources. Safety gating keeps irreversible families (**flash**, **place_delete**) off unless listed in **LABGRID_MCP_ALLOW**; **LABGRID_MCP_READONLY=1** registers only read-oriented tools.\n\nIdentity matches **labgrid-client**: **LG_HOSTNAME** / **LG_USERNAME**, or the real host and user when unset. Network security is delegated to VPN or SSH tunnels, the same trust model as labgrid itself."],["CONFIGURATION","Environment variables (native labgrid **LG_*** plus **LABGRID_MCP_***):\n\n- **LG_COORDINATOR** — coordinator **host:port** (default **127.0.0.1:20408**)\n- **LG_HOSTNAME** / **LG_USERNAME** — labgrid client identity\n- **LABGRID_MCP_READONLY** — set to **1**/**true**/**yes** for read-only tool registration\n- **LABGRID_MCP_ALLOW** — comma-separated tool categories; **flash** and **place_delete** must be listed explicitly to enable\n- **LABGRID_MCP_SSH_KEYFILE** — private key path for SSH tools\n- **LABGRID_MCP_ACQUIRE_TIMEOUT** — max seconds **acquire_place** waits (default **120**)"],["CAVEATS","Needs a running **labgrid ≥ 24** coordinator (gRPC era; tested against 26.x). Python **3.12+**. Old crossbar coordinators cannot connect. Flashing and place deletion are off by default on purpose. Do not share one running server instance across users: each instance holds a single labgrid identity. SSH tools are full remote command execution on the acquired board."],["HISTORY","**labgrid-mcp** is written by **Onur Celep**, licensed **Apache-2.0**, and published on PyPI as **labgrid-mcp**. It depends on **labgrid** (LGPL-2.1-or-later) as an unmodified library dependency."],["SEE ALSO","[ssh](/man/ssh)(1), [pip](/man/pip)(1), [uv](/man/uv)(1)"],["RESOURCES","```[Source code](https://github.com/onurcelep/labgrid-mcp)```\n\n```[Homepage](https://pypi.org/project/labgrid-mcp/)```\n\n```[Documentation](https://github.com/onurcelep/labgrid-mcp/blob/main/docs/DESIGN.md)```"]],"e":["ssh","pip","uv"],"x":"labgrid-mcp demo"},"loggo":{"n":"loggo","t":"Terminal app for structured JSON log streaming","s":[["TAGLINE","Terminal app for structured JSON log streaming"],["TLDR","**Stream logs from a file**\n\n```loggo stream -f [path/to/logfile]```\n\n**Stream logs from piped input**\n\n```cat [logfile] | loggo stream```\n\n**Stream Kubernetes logs**\n\n```kubectl logs -f [pod_name] | loggo stream```"],["SYNOPSIS","**loggo** _command_ [_options_]"],["DESCRIPTION","**loggo** is a rich terminal UI for digesting JSON-based structured log streams. It can stream parsed logs from persisted files and from piped input, and provides tools for creating log templates.\n\nIt works with applications running locally, on Kubernetes clusters, GCP Stack Driver (Google Logs), and many other sources. The TUI is built with tview and tcell for interactive log browsing and filtering."],["CAVEATS","Designed for JSON-formatted logs. Non-JSON log formats may not be parsed correctly."],["HISTORY","**loggo** was created by **aurc** and is written in **Go**."],["INSTALL","```aur: yay -S loggo```"],["SEE ALSO","[jq](/man/jq)(1), [lnav](/man/lnav)(1)"]],"e":["jq","lnav"],"x":"loggo stream -f [path/to/logfile]"},"lvconvert":{"n":"lvconvert","t":"converts or modifies the type, redundancy, or state of logical volumes","s":[["TAGLINE","converts or modifies the type, redundancy, or state of logical volumes"],["TLDR","Convert to **RAID1**\n\n```sudo lvconvert --type raid1 -m 1 /dev/vg_name/lv_name```\n\n**Remove mirroring** (convert to linear)\n\n```sudo lvconvert -m 0 /dev/vg_name/lv_name```\n\n**Merge** snapshot into origin\n\n```sudo lvconvert --merge /dev/vg_name/snapshot_lv```\n\n**Repair** degraded RAID\n\n```sudo lvconvert --repair /dev/vg_name/lv_name```\n\nConvert to **thin pool**\n\n```sudo lvconvert --type thin-pool --poolmetadata /dev/vg_name/meta_lv /dev/vg_name/pool_lv```"],["SYNOPSIS","**lvconvert** [_options_] _vg_/_lv_"],["DESCRIPTION","**lvconvert** converts or modifies the type, redundancy, or state of logical volumes. It can add/remove mirrors, convert between linear and RAID, merge snapshots, and convert to thin pools."],["PARAMETERS","**--type TYPE**\n> Convert to specified type (raid1, linear, thin-pool, cache-pool)\n\n**-m, --mirrors N**\n> Number of mirrors (0 removes mirroring)\n\n**--merge**\n> Merge snapshot back into origin\n\n**--repair**\n> Repair degraded RAID volume\n\n**--poolmetadata LV**\n> Specify metadata LV for thin pool\n\n**--stripes N**\n> Number of stripes for conversion"],["CAVEATS","Requires root privileges. Data migration may take time. Snapshot merge applies on next activation."],["INSTALL","```apt: sudo apt install lvm2```\n\n```dnf: sudo dnf install lvm2```\n\n```pacman: sudo pacman -S lvm2```\n\n```apk: sudo apk add lvm2```\n\n```zypper: sudo zypper install lvm2```\n\n```nix: nix profile install nixpkgs#lvm2```"],["SEE ALSO","[lvcreate](/man/lvcreate)(8), [lvchange](/man/lvchange)(8), [lvextend](/man/lvextend)(8), [lvm](/man/lvm)(8)"]],"e":["lvcreate","lvchange","lvextend","lvm"],"x":"sudo lvconvert --type raid1 -m 1 /dev/vg_name/lv_name"},"memtier_benchmark":{"n":"memtier_benchmark","t":"load generation and benchmarking tool for Redis and Memcached","s":[["TAGLINE","load generation and benchmarking tool for Redis and Memcached"],["TLDR","**Benchmark Redis**\n\n```memtier_benchmark -s [localhost] -p [6379]```\n\n**Benchmark with clients and threads**\n\n```memtier_benchmark -s [host] -c [50] -t [4]```\n\n**Custom key pattern**\n\n```memtier_benchmark -s [host] --key-pattern=S:S```\n\n**Set data size**\n\n```memtier_benchmark -s [host] -d [128]```\n\n**Run for specific duration**\n\n```memtier_benchmark -s [host] --test-time=[60]```\n\n**Benchmark memcached**\n\n```memtier_benchmark -s [host] -p [11211] --protocol=memcache_text```"],["SYNOPSIS","**memtier_benchmark** [_options_]"],["PARAMETERS","**-s** _host_\n> Server hostname.\n\n**-p** _port_\n> Server port.\n\n**-c** _clients_\n> Clients per thread.\n\n**-t** _threads_\n> Number of threads.\n\n**-d** _size_\n> Data size in bytes.\n\n**--protocol** _proto_\n> redis, memcache_text, memcache_binary.\n\n**--ratio** _get:set_\n> GET:SET ratio.\n\n**--test-time** _secs_\n> Test duration.\n\n**--key-pattern** _pattern_\n> Key access pattern."],["DESCRIPTION","**memtier_benchmark** is a load generation and benchmarking tool for Redis and Memcached. It measures throughput and latency under various load conditions.\n\nThe tool supports multiple protocols and provides detailed statistics including percentile latencies."],["KEY PATTERNS","```\nS:S - Sequential\nR:R - Random\nG:G - Gaussian\n```"],["CAVEATS","Benchmarks only; doesn't test data integrity. Results depend on network. Use multiple instances for high-throughput testing."],["HISTORY","memtier_benchmark was developed by **Redis Labs** (now Redis Inc.) as a comprehensive benchmarking tool for in-memory data stores."],["INSTALL","```brew: brew install memtier_benchmark```"],["SEE ALSO","[redis-benchmark](/man/redis-benchmark)(1), [redis-cli](/man/redis-cli)(1), [memcached](/man/memcached)(1)"]],"e":["redis-benchmark","redis-cli","memcached"],"x":"memtier_benchmark -s [localhost] -p [6379]"},"mogrify":{"n":"mogrify","t":"modifies images in place","s":[["TAGLINE","modifies images in place"],["TLDR","**Resize images in place**\n\n```mogrify -resize [800x600] [*.jpg]```\n\n**Convert format**\n\n```mogrify -format [png] [*.jpg]```\n\n**Rotate images**\n\n```mogrify -rotate [90] [*.jpg]```\n\n**Add watermark**\n\n```mogrify -draw \"text 10,10 'Copyright'\" [*.jpg]```\n\n**Compress quality**\n\n```mogrify -quality [80] [*.jpg]```\n\n**Strip metadata**\n\n```mogrify -strip [*.jpg]```"],["SYNOPSIS","**mogrify** [_options_] _files_"],["PARAMETERS","_FILES_\n> Image files to modify.\n\n**-resize** _GEOM_\n> Resize geometry.\n\n**-format** _FMT_\n> Output format.\n\n**-rotate** _DEG_\n> Rotation degrees.\n\n**-quality** _NUM_\n> Compression quality.\n\n**-strip**\n> Remove metadata (EXIF, color profiles).\n\n**-crop** _GEOM_\n> Crop the image to the given region (e.g. `100x100+10+10`).\n\n**-thumbnail** _GEOM_\n> Faster resize that also strips metadata.\n\n**-flip** / **-flop**\n> Mirror vertically / horizontally.\n\n**-trim**\n> Trim uniform edges (e.g. whitespace borders).\n\n**-path** _DIR_\n> Write output files to DIR instead of overwriting.\n\n**-auto-orient**\n> Rotate by EXIF orientation so the image displays correctly.\n\n**--help**\n> Display help information."],["DESCRIPTION","**mogrify** modifies images in place. It's ImageMagick's batch processing tool.\n\nThe tool transforms multiple images at once. Unlike convert, it overwrites originals."],["CAVEATS","Overwrites originals. Part of ImageMagick. Backup before use."],["HISTORY","mogrify is part of **ImageMagick**, providing in-place image transformation since 1990."],["INSTALL","```pacman: sudo pacman -S imagemagick```\n\n```apk: sudo apk add imagemagick```\n\n```brew: brew install imagemagick```\n\n```nix: nix profile install nixpkgs#imagemagick```"],["SEE ALSO","[convert](/man/convert)(1), [identify](/man/identify)(1), [magick](/man/magick)(1)"]],"e":["convert","identify","magick"],"x":"mogrify -resize [800x600] [*.jpg]"},"mongoimport":{"n":"mongoimport","t":"imports data into MongoDB","s":[["TAGLINE","imports data into MongoDB"],["TLDR","**Import JSON file**\n\n```mongoimport --db [dbname] --collection [coll] --file [data.json]```\n\n**Import CSV file**\n\n```mongoimport --db [dbname] --collection [coll] --type csv --headerline --file [data.csv]```\n\n**Import with authentication**\n\n```mongoimport -u [user] -p [pass] --db [dbname] --collection [coll] --file [data.json]```\n\n**Import from URI**\n\n```mongoimport --uri \"[mongodb://host/db]\" --collection [coll] --file [data.json]```\n\n**Import with drop**\n\n```mongoimport --db [dbname] --collection [coll] --drop --file [data.json]```\n\n**Import JSON array**\n\n```mongoimport --db [dbname] --collection [coll] --jsonArray --file [array.json]```"],["SYNOPSIS","**mongoimport** [_options_] [_file_]"],["PARAMETERS","**--db** _NAME_\n> Database name.\n\n**--collection** _NAME_\n> Collection name.\n\n**--file** _FILE_\n> Input file path.\n\n**--type** _TYPE_\n> File type (json, csv, tsv).\n\n**--headerline**\n> Use first line as field names.\n\n**--drop**\n> Drop collection before import.\n\n**--help**\n> Display help information."],["DESCRIPTION","**mongoimport** imports data into MongoDB. It supports JSON, CSV, and TSV formats.\n\nThe tool bulk loads documents. Useful for data migration and seeding databases."],["CAVEATS","Not for large datasets. Use mongorestore for BSON. May be slow on big imports."],["HISTORY","mongoimport is part of **MongoDB Database Tools**, providing data import capabilities since MongoDB's early versions."],["INSTALL","```apk: sudo apk add mongodb-tools```\n\n```nix: nix profile install nixpkgs#mongodb-tools```"],["SEE ALSO","[mongoexport](/man/mongoexport)(1), [mongorestore](/man/mongorestore)(1), [mongosh](/man/mongosh)(1)"]],"e":["mongoexport","mongorestore","mongosh"],"x":"mongoimport --db [dbname] --collection [coll] --file [data.json]"},"nbsdgames":{"n":"nbsdgames","t":"Interactive menu for the New BSD Games collection","s":[["TAGLINE","Interactive menu for the New BSD Games collection"],["TLDR","**Open the game selection menu**\n\n```nbsdgames```\n\n**Launch a specific game directly**\n\n```nbmines```\n\n```nbsudoku```\n\n```nbreversi```"],["SYNOPSIS","**nbsdgames**"],["PREVIEW","```\n New BSD Games\n 1 Jewels  2 Sudoku\n 3 Mines   4 Reversi\n > Select a game:\n```"],["DESCRIPTION","**nbsdgames** launches an ncurses menu for the New BSD Games package, a collection of lightweight terminal games for Unix, DOS, and Plan 9. Running the command without arguments presents a numbered list of available games; selecting an entry starts that game.\n\nIndividual games can also be invoked directly by name. The collection includes puzzle and board games such as **nbmines**, **nbsudoku**, **nbreversi**, **nbbattleship**, **nbcheckers**, **nbfifteen**, **nbpipes**, and others. Many games accept command-line options to adjust board size, difficulty, or dimensions.\n\nRequires **ncurses** (on Debian-based systems: **libncurses5-dev** for building, **libncurses** at runtime). Games are written in portable C and packaged for most major Linux distributions, BSD systems, Alpine, and Homebrew."],["PARAMETERS","Individual games in the package accept their own options. Common patterns include **-c** and **-r** for columns and rows, **-m** for mine count, and **--help** for per-game usage. See each game's manual entry for details."],["CAVEATS","Requires a terminal with ncurses support. Some distribution packages may ship older versions with fewer games than the upstream release. On Windows, games can run with PDCurses; Plan 9 and DOS ports also exist."],["HISTORY","The New BSD Games collection was created by abakh as a modern companion to classic BSD games, adding original and improved text-mode implementations. Version 6.0.2 was released in **June 2026** with 21 games. Individual games in the package are documented separately in this library."],["INSTALL","```zypper: sudo zypper install nbsdgames```\n\n```brew: brew install nbsdgames```\n\n```nix: nix profile install nixpkgs#nbsdgames```"],["SEE ALSO","[nbmines](/man/nbmines)(1), [nbsudoku](/man/nbsudoku)(1), [nbreversi](/man/nbreversi)(1), [nbbattleship](/man/nbbattleship)(1), [bsdgames](/man/bsdgames)(6)"],["RESOURCES","```[Source code](https://github.com/abakh/nbsdgames)```"]],"e":["nbmines","nbsudoku","nbreversi","nbbattleship","bsdgames"],"x":"nbsdgames"},"neovim":{"n":"neovim","t":"Hyperextensible Vim-based text editor","s":[["TAGLINE","Hyperextensible Vim-based text editor"],["TLDR","**Open a file**\n\n```neovim [path/to/file]```\n\n**Open a file at a specific line**\n\n```neovim +[line_number] [path/to/file]```\n\n**View differences between files**\n\n```neovim -d [file1] [file2]```\n\n**Open in read-only mode**\n\n```neovim -R [path/to/file]```"],["SYNOPSIS","**neovim** [_options_] [_files_...]"],["DESCRIPTION","**neovim** (usually invoked as **nvim**) is a hyperextensible text editor based on Vim, refactored with modern architecture. It features built-in LSP client, Treesitter-based syntax highlighting, Lua-based configuration and plugin system, embedded terminal emulator, and asynchronous job control.\n\nNeovim maintains backward compatibility with most Vim configurations while providing a more maintainable and extensible codebase. It supports remote plugins via RPC, allowing integration with external tools and GUIs."],["CONFIGURATION","**~/.config/nvim/init.lua**\n> Main configuration file (Lua-based, replaces init.vim)."],["CAVEATS","Some Vim plugins may not be compatible. The recommended configuration language is Lua rather than Vimscript."],["HISTORY","**Neovim** was started in **2014** by **Thiago de Arruda** as a fork of Vim with the goal of aggressive refactoring and modernization. It is written in **C** and **Lua**."],["INSTALL","```dnf: sudo dnf install neovim```\n\n```pacman: sudo pacman -S neovim```\n\n```zypper: sudo zypper install neovim```\n\n```brew: brew install neovim```\n\n```nix: nix profile install nixpkgs#neovim```"],["SEE ALSO","[nvim](/man/nvim)(1), [vim](/man/vim)(1), [vi](/man/vi)(1)"]],"e":["nvim","vim","vi"],"x":"neovim [path/to/file]"},"nh-home":{"n":"nh-home","t":"manages home-manager configurations","s":[["TAGLINE","manages home-manager configurations"],["TLDR","**Switch home configuration**\n\n```nh home switch```\n\n**Build without switching**\n\n```nh home build```\n\n**Switch with flake**\n\n```nh home switch --flake [.#username]```"],["SYNOPSIS","**nh home** _command_ [_options_]"],["PARAMETERS","**switch**\n> Build and activate home configuration.\n\n**build**\n> Build configuration only.\n\n**--flake** _ref_\n> Flake reference.\n\n**--dry**\n> Dry run.\n\n**--ask**\n> Ask before activation."],["DESCRIPTION","**nh home** manages home-manager configurations. Friendly wrapper around home-manager with better output and flake support. Part of nh (nix helper) toolkit."],["INSTALL","```nix: nix profile install nixpkgs#nh```"],["SEE ALSO","[nh](/man/nh)(1), [home-manager](/man/home-manager)(1)"]],"e":["nh","home-manager"],"x":"nh home switch"},"omf":{"n":"omf","t":"oh My Fish, a Fish shell framework","s":[["TAGLINE","oh My Fish, a Fish shell framework"],["TLDR","**Install package**\n\n```omf install [package]```\n\n**List installed packages**\n\n```omf list```\n\n**Update Oh My Fish**\n\n```omf update```\n\n**Search packages**\n\n```omf search [query]```\n\n**Remove package**\n\n```omf remove [package]```\n\n**Change theme**\n\n```omf theme [theme_name]```"],["SYNOPSIS","**omf** [_command_] [_options_]"],["PARAMETERS","**install** _PACKAGE_\n> Install package/theme.\n\n**list**\n> List installed packages.\n\n**update**\n> Update framework.\n\n**search** _QUERY_\n> Search packages.\n\n**remove** _PACKAGE_\n> Uninstall package.\n\n**theme** _NAME_\n> Change theme.\n\n**--help**\n> Display help information."],["DESCRIPTION","**omf** is Oh My Fish, a Fish shell framework. Manages plugins and themes.\n\nThe tool extends Fish shell functionality. Community package ecosystem."],["CAVEATS","Fish shell required. Community packages. May conflict with other frameworks."],["HISTORY","Oh My Fish was created as a **package manager** for the Fish shell."],["SEE ALSO","[fish](/man/fish)(1), [fisher](/man/fisher)(1)"]],"e":["fish","fisher"],"x":"omf install [package]"},"pacini":{"n":"pacini","t":"parses and queries INI-style configuration files in the format used by pacman","s":[["TAGLINE","parses and queries INI-style configuration files in the format used by pacman"],["TLDR","Show the **full parsed** configuration file\n\n```pacini [path/to/file]```\n\nList **configured sections**\n\n```pacini [path/to/file] --section-list```\n\nAlways show **directive names** even for single directives\n\n```pacini [path/to/file] --verbose```\n\nDisplay directives in a **specific section**\n\n```pacini [path/to/file] --section [section_name]```\n\nDisplay **help**\n\n```pacini --help```"],["SYNOPSIS","**pacini** [_options_] [_file_] [_directive_...]"],["PARAMETERS","**--section-list**\n> List all sections in the configuration file\n\n**--section** _name_\n> Query directives only from the specified section\n\n**--verbose**\n> Always show directive names in output\n\n**--null**\n> Use null delimiter for output\n\n**--help**\n> Display help information\n\n**--version**\n> Display version information"],["DESCRIPTION","**pacini** parses and queries INI-style configuration files in the format used by pacman. It can display the full parsed configuration, list sections, or extract specific directive values from named sections.\n\nThe tool handles pacman's configuration format including Include directives, making it useful for scripts that need to extract values from pacman.conf or similar configuration files. Reads from stdin if no file is specified."],["CAVEATS","Specific to pacman-style INI format; may not correctly parse all INI variations. Include directives are not followed by default. Section names are case-sensitive."],["HISTORY","Part of **pacutils**, a collection of utilities for pacman by Andrew Gregory. Provides a dedicated tool for parsing pacman configuration files, complementing pacconf which specifically handles pacman.conf."],["SEE ALSO","[pacconf](/man/pacconf)(8), [pacman.conf](/man/pacman.conf)(5)"]],"e":["pacconf","pacman.conf"],"x":"pacini [path/to/file]"},"pam_userdb":{"n":"pam_userdb","t":"authenticates against Berkeley DB","s":[["TAGLINE","authenticates against Berkeley DB"],["TLDR","**Authenticate against DB**\n\n```auth required pam_userdb.so db=/path/to/userdb```\n\n**Case insensitive**\n\n```auth required pam_userdb.so db=/path/to/userdb icase```\n\n**Create user database**\n\n```db_load -T -t hash -f users.txt /path/to/userdb.db```"],["SYNOPSIS","**pam_userdb.so** [_options_]"],["PARAMETERS","**db=**_PATH_\n> Database file path (without .db extension on some systems).\n\n**icase**\n> Case insensitive username matching.\n\n**crypt=**_TYPE_\n> Password encryption type: _none_ (plaintext) or _crypt_ (crypt(3)-hashed).\n\n**try_first_pass**\n> Try the previously entered password before prompting.\n\n**use_first_pass**\n> Use only the previously entered password; do not prompt.\n\n**dump**\n> Dump database contents to log (debugging, insecure).\n\n**unknown_ok**\n> Do not fail authentication for users not in the DB (skip module).\n\n**key_only**\n> Authenticate based on username presence alone, ignoring password."],["DESCRIPTION","**pam_userdb** authenticates users against credentials stored in a Berkeley DB database, independent of the system account files. The database maps usernames (keys) to passwords (values), allowing application-specific or service-specific authentication without creating system accounts.\n\nCommonly used to provide separate credentials for services such as FTP, VPN, or web applications."],["CAVEATS","Requires Berkeley DB. Database format specific. Custom user management."],["HISTORY","pam_userdb enables **database-backed authentication** separate from system accounts."],["SEE ALSO","[pam](/man/pam)(8), [db_load](/man/db_load)(1)"]],"e":["pam","db_load"],"x":"auth required pam_userdb.so db=/path/to/userdb"},"pg_createsubscriber":{"n":"pg_createsubscriber","t":"converts a physical standby to logical replication subscriber","s":[["TAGLINE","converts a physical standby to logical replication subscriber"],["TLDR","**Convert a stopped standby into a logical subscriber**\n\n```pg_createsubscriber -d [dbname] -D [standby_data_dir] -P \"[host=primary dbname=app]\"```\n\n**Preview the actions without changing anything**\n\n```pg_createsubscriber --dry-run -d [dbname] -D [standby_data_dir] -P \"[host=primary dbname=app]\"```\n\n**Convert several databases at once**\n\n```pg_createsubscriber -d [db1] -d [db2] -D [standby_data_dir] -P \"[host=primary]\"```"],["SYNOPSIS","**pg_createsubscriber** [_options_] **-D** _datadir_ **-P** _conninfo_ **-d** _dbname_"],["PARAMETERS","**-d**, **--database** _name_\n> Database to set up logical replication for. Repeat for multiple databases.\n\n**-D**, **--pgdata** _dir_\n> Data directory of the target (standby) server. The server must be stopped.\n\n**-P**, **--publisher-server** _conninfo_\n> Connection string for the source (publisher) server.\n\n**-p**, **--subscriber-port** _port_\n> Port that the target subscriber server listens on while being configured.\n\n**-U**, **--subscriber-username** _name_\n> User name used to connect to the target subscriber.\n\n**-s**, **--socketdir** _dir_\n> Directory for the target server's postmaster sockets (default: current directory).\n\n**--publication** _name_\n> Publication name to create on the publisher. Repeat to match multiple databases.\n\n**--subscription** _name_\n> Subscription name to create on the subscriber. Repeat to match multiple databases.\n\n**--replication-slot** _name_\n> Replication slot name used by the logical replication. Repeat to match multiple databases.\n\n**-t**, **--recovery-timeout** _seconds_\n> Maximum time to wait for the standby to catch up before failing.\n\n**-n**, **--dry-run**\n> Show what would be done without making any changes.\n\n**-v**, **--verbose**\n> Enable verbose output (repeat for more detail)."],["DESCRIPTION","**pg_createsubscriber** converts a physical standby into a logical replication subscriber. It transforms an existing streaming-replication standby into a node that subscribes to publications on the original primary, avoiding a fresh initial data copy.\n\nThe target standby must be stopped before running the tool. Internally it uses **pg_ctl** to start and stop the target server while it creates the publication, subscription, and replication slot. When several `--database`, `--publication`, `--subscription`, or `--replication-slot` options are given, their counts and order must correspond. Introduced in PostgreSQL 17."],["CAVEATS","The target server must be a running-then-stopped physical standby, not in use during conversion. The number of `--publication`, `--subscription`, and `--replication-slot` values, when specified, must match the number of `--database` values. Always test with `--dry-run` first."],["INSTALL","```dnf: sudo dnf install postgresql17```\n\n```apk: sudo apk add postgresql17```\n\n```zypper: sudo zypper install postgresql17```"],["SEE ALSO","[pg_basebackup](/man/pg_basebackup)(1)"]],"e":["pg_basebackup"],"x":"pg_createsubscriber -d [dbname] -D [standby_data_dir] -P \"[host=primary dbname=app]\""},"pg_test_fsync":{"n":"pg_test_fsync","t":"benchmarks various fsync methods","s":[["TAGLINE","benchmarks various fsync methods"],["TLDR","**Test fsync methods**\n\n```pg_test_fsync```\n\n**Test with specific file**\n\n```pg_test_fsync -f [testfile]```"],["SYNOPSIS","**pg_test_fsync** [_options_]"],["PARAMETERS","**-f**, **--filename** _file_\n> Test file path.\n\n**-s**, **--secs-per-test** _n_\n> Seconds per test."],["DESCRIPTION","**pg_test_fsync** benchmarks the fsync/flush methods available on the current platform, reporting operations-per-second for each. The results guide the choice of PostgreSQL's `wal_sync_method` (e.g. `fsync`, `fdatasync`, `open_sync`, `open_datasync`). Run it on the actual filesystem hosting WAL to get meaningful numbers; cached or virtualized storage will distort results."],["CAVEATS","Requires write access to the target directory. Results are affected by caching layers (hardware, virtualization, filesystem) — benchmark on real production storage for accurate comparisons."],["INSTALL","```dnf: sudo dnf install postgresql17```\n\n```apk: sudo apk add postgresql17```\n\n```zypper: sudo zypper install postgresql17```"],["SEE ALSO","[pg_test_timing](/man/pg_test_timing)(1), [postgres](/man/postgres)(1)"]],"e":["pg_test_timing","postgres"],"x":"pg_test_fsync"},"pgrep":{"n":"pgrep","t":"searches for processes by name or attributes, returning their PIDs","s":[["TAGLINE","searches for processes by name or attributes, returning their PIDs"],["TLDR","**Find process by name**\n\n```pgrep [nginx]```\n\n**Find with full command line match**\n\n```pgrep -f \"[python script.py]\"```\n\n**List process names and PIDs**\n\n```pgrep -l [ssh]```\n\n**Find processes by user**\n\n```pgrep -u [username] [process]```\n\n**Find newest matching process**\n\n```pgrep -n [chrome]```\n\n**Find oldest matching process**\n\n```pgrep -o [firefox]```\n\n**Count matching processes**\n\n```pgrep -c [apache]```\n\n**Exact name match**\n\n```pgrep -x [bash]```"],["SYNOPSIS","**pgrep** [_-flnoxc_] [_-u user_] [_-g group_] [_-P ppid_] [_pattern_]"],["PARAMETERS","**-l**, **--list-name**\n> List PID and process name.\n\n**-a**, **--list-full**\n> List PID and full command line.\n\n**-f**, **--full**\n> Match against full command line.\n\n**-x**, **--exact**\n> Match exactly (not substring).\n\n**-n**, **--newest**\n> Select newest (most recent) match.\n\n**-o**, **--oldest**\n> Select oldest (first) match.\n\n**-c**, **--count**\n> Count matches instead of listing.\n\n**-d** _DELIM_, **--delimiter** _DELIM_\n> Output delimiter (default: newline).\n\n**-u** _USER_, **--euid** _USER_\n> Match effective user ID.\n\n**-U** _USER_, **--uid** _USER_\n> Match real user ID.\n\n**-g** _GROUP_, **--pgroup** _GROUP_\n> Match process group.\n\n**-G** _GROUP_, **--group** _GROUP_\n> Match real group ID.\n\n**-P** _PPID_, **--parent** _PPID_\n> Match parent process ID.\n\n**-t** _TERM_, **--terminal** _TERM_\n> Match controlling terminal.\n\n**-v**, **--inverse**\n> Invert match (negation).\n\n**-i**, **--ignore-case**\n> Case-insensitive matching."],["DESCRIPTION","**pgrep** searches for processes by name or attributes, returning their PIDs. It's a more focused alternative to piping ps through grep.\n\nPattern matching is against the process name by default (executable name). The -f flag extends matching to the full command line including arguments. Regular expressions are supported.\n\nSelection filters narrow results by user, group, terminal, or parent process. These can be combined for precise targeting. The -v flag inverts selection, finding processes that don't match.\n\nNewest (-n) and oldest (-o) options return single matches, useful when multiple instances exist. Count mode (-c) reports how many processes match without listing them.\n\nOutput formatting options control delimiter and detail level. These enable integration with shell scripts and other tools."],["CAVEATS","Matches are substring by default - \"sh\" matches bash, fish, zsh. Use -x for exact matching. Process names may be truncated. Zombie processes may still match. Pattern is a regular expression. pgrep cannot find itself."],["HISTORY","**pgrep** was introduced in **Solaris 7** (1998) and ported to Linux as part of **procps**. It provides a cleaner interface than the traditional `ps aux | grep pattern` approach, avoiding the common \"grep matching itself\" problem. The tool is part of the procps-ng package on most Linux systems."],["INSTALL","```apt: sudo apt install procps```\n\n```dnf: sudo dnf install procps-ng```\n\n```pacman: sudo pacman -S procps-ng```\n\n```apk: sudo apk add procps-ng```\n\n```zypper: sudo zypper install procps```\n\n```brew: brew install procps```\n\n```nix: nix profile install nixpkgs#procps```"],["SEE ALSO","[pkill](/man/pkill)(1), [ps](/man/ps)(1), [pidof](/man/pidof)(1), [kill](/man/kill)(1)"]],"e":["pkill","ps","pidof","kill"],"x":"pgrep [nginx]"},"pifs":{"n":"pifs","t":"Data-free FUSE filesystem backed by π","s":[["TAGLINE","Data-free FUSE filesystem backed by π"],["TLDR","**Mount a πfs filesystem**\n\n```pifs -o mdd=[/path/to/metadata] [mountpoint]```\n\n**Build from source**\n\n```./autogen.sh && ./configure && make && sudo make install```"],["SYNOPSIS","**pifs** **-o** _mdd=metadata-directory_ [_mountpoint_]"],["PARAMETERS","**-o mdd=**_path_\n> Directory where πfs stores metadata (filenames and byte offsets in π).\n\n_mountpoint_\n> Directory where the filesystem is mounted."],["DESCRIPTION","**pifs** (πfs) is a FUSE userspace filesystem that stores file data by locating each byte's position within the digits of π rather than on disk. Because π is conjectured to be a normal number, every finite byte sequence is theoretically present somewhere in its expansion; πfs records only the index and length needed to retrieve each byte.\n\nFiles are split into individual bytes, each looked up in π using the Bailey–Borwein–Plouffe formula. Metadata (paths and offsets) is written to the directory given by **mdd=**. The filesystem is largely a proof-of-concept: storing even small files is extremely slow because locating arbitrary digit sequences in π is computationally expensive.\n\nRequires **libfuse** and a C build toolchain. Install build dependencies on Debian-based systems with **autotools-dev**, **automake**, and **libfuse-dev**."],["CAVEATS","Lookup performance is impractical for real workloads; the project README notes that storing a 400-line text file can take several minutes. If metadata is lost, file locations cannot be recovered even though the data still exists in π. For a newer related project, see inferencefs."],["HISTORY","πfs was created by Philip L. as a humorous exploration of the idea that all possible data already exists within π, inspired by a 2001 observation that a normal π would contain every finite file. The repository has been widely shared since its initial release."],["SEE ALSO","[fusermount](/man/fusermount)(1), [mount](/man/mount)(8)"],["RESOURCES","```[Source code](https://github.com/philipl/pifs)```"]],"e":["fusermount","mount"],"x":"pifs -o mdd=[/path/to/metadata] [mountpoint]"},"ping6":{"n":"ping6","t":"sends ICMPv6 echo requests to IPv6 hosts","s":[["TAGLINE","sends ICMPv6 echo requests to IPv6 hosts"],["TLDR","**Ping IPv6 address**\n\n```ping6 [2001:db8::1]```\n\n**Ping hostname**\n\n```ping6 [ipv6.example.com]```\n\n**Limit packet count**\n\n```ping6 -c [5] [host]```\n\n**Set interval**\n\n```ping6 -i [2] [host]```\n\n**Set packet size**\n\n```ping6 -s [1000] [host]```\n\n**Flood ping**\n\n```ping6 -f [host]```\n\n**Quiet output**\n\n```ping6 -q -c [10] [host]```\n\n**Specify interface**\n\n```ping6 -I [eth0] [fe80::1]```"],["SYNOPSIS","**ping6** [_-c count_] [_-i interval_] [_-s size_] [_-I interface_] [_options_] _destination_"],["PARAMETERS","**-c** _COUNT_\n> Stop after count packets.\n\n**-i** _INTERVAL_\n> Seconds between packets.\n\n**-s** _SIZE_\n> Packet data size.\n\n**-I** _INTERFACE_\n> Source interface.\n\n**-q**\n> Quiet output.\n\n**-f**\n> Flood mode.\n\n**-t** _TTL_\n> Time to live.\n\n**-w** _DEADLINE_\n> Timeout in seconds."],["DESCRIPTION","**ping6** sends ICMPv6 echo requests to IPv6 hosts. It tests network connectivity and measures latency.\n\nLink-local addresses require interface specification. The -I option binds to specific network interface.\n\nStatistics show packet loss and round-trip times. Minimum, average, and maximum latency are reported.\n\nFlood mode tests network under load. Requires root privileges.\n\nMany systems now use ping for both IPv4 and IPv6. The separate ping6 command remains for compatibility."],["CAVEATS","Firewalls may block ICMPv6. Link-local requires interface. Flood ping requires root."],["HISTORY","**ping6** appeared with IPv6 implementations in the **1990s**. On many modern systems, the **ping** command handles both protocols, making ping6 a compatibility alias."],["INSTALL","```apt: sudo apt install iputils-ping```\n\n```apk: sudo apk add iputils-ping```"],["SEE ALSO","[ping](/man/ping)(1), [traceroute6](/man/traceroute6)(1), [ip](/man/ip)(8)"]],"e":["ping","traceroute6","ip"],"x":"ping6 [2001:db8::1]"},"poetry-update":{"n":"poetry-update","t":"Update project dependencies","s":[["TAGLINE","Update project dependencies"],["TLDR","**Update all dependencies**\n\n```poetry update```\n\n**Update specific packages**\n\n```poetry update [package1] [package2]```\n\n**Preview updates without installing**\n\n```poetry update --dry-run```\n\n**Update without dev dependencies**\n\n```poetry update --without dev```\n\n**Only update the lock file, don't install**\n\n```poetry update --lock```"],["SYNOPSIS","**poetry** **update** [_packages_...] [_options_]"],["PARAMETERS","**--dry-run**\n> Preview updates without installing.\n\n**--no-dev**\n> Skip dev dependencies (deprecated, use **--without dev**).\n\n**--with** _groups_\n> Include optional dependency groups.\n\n**--without** _groups_\n> Exclude dependency groups.\n\n**--only** _groups_\n> Only update the specified dependency groups.\n\n**--lock**\n> Only update the lock file, don't install packages.\n\n**--sync**\n> Synchronize the environment by removing packages not in the lock file.\n\n**-v**, **-vv**, **-vvv**\n> Increase verbosity."],["DESCRIPTION","**poetry update** resolves dependencies to their latest versions allowed by the constraints in pyproject.toml, updates the poetry.lock file, and installs the new versions. When specific packages are given, only those packages and their dependencies are updated. Without arguments, all dependencies are updated."],["INSTALL","```dnf: sudo dnf install poetry```\n\n```apk: sudo apk add poetry```\n\n```brew: brew install poetry```\n\n```nix: nix profile install nixpkgs#poetry```"],["SEE ALSO","[poetry](/man/poetry)(1), [poetry-install](/man/poetry-install)(1), [poetry-lock](/man/poetry-lock)(1), [poetry-add](/man/poetry-add)(1), [poetry-show](/man/poetry-show)(1)"]],"e":["poetry","poetry-install","poetry-lock","poetry-add","poetry-show"],"x":"poetry update"},"polkitd":{"n":"polkitd","t":"PolicyKit authorization daemon","s":[["TAGLINE","PolicyKit authorization daemon"],["TLDR","**Start PolicyKit daemon**\n\n```polkitd```\n\n**Run with debug output**\n\n```polkitd --debug```\n\n**Specify config directory**\n\n```polkitd --config-dir=[/etc/polkit-1]```"],["SYNOPSIS","**polkitd** [_options_]"],["PARAMETERS","**--debug**\n> Enable debug output.\n\n**--config-dir** _DIR_\n> Configuration directory.\n\n**--no-debug**\n> Disable debug output."],["DESCRIPTION","**polkitd** is the PolicyKit authorization daemon that processes authorization requests from applications. It evaluates policy rules defined in JavaScript or .pkla files to determine whether a user or process is authorized to perform a specific action.\n\nThe daemon is typically started automatically by D-Bus or systemd when an authorization check is needed. The **--debug** flag enables verbose logging for troubleshooting policy issues."],["CAVEATS","Usually started by systemd. System service."],["HISTORY","polkitd is the daemon component of **PolicyKit** authorization framework."],["SEE ALSO","[polkit](/man/polkit)(8), [pkexec](/man/pkexec)(1), [systemctl](/man/systemctl)(1)"]],"e":["polkit","pkexec","systemctl"],"x":"polkitd"},"ppmplasma":{"n":"ppmplasma","t":"Generate colorful plasma fractal images","s":[["TAGLINE","Generate colorful plasma fractal images"],["TLDR","**Generate plasma pattern**\n\n```ppmplasma [width] [height] > [plasma.ppm]```\n\n**With random seed**\n\n```ppmplasma -randomseed [42] [512] [512] > [plasma.ppm]```\n\n**As PNG**\n\n```ppmplasma [800] [600] | pnmtopng > [plasma.png]```"],["SYNOPSIS","**ppmplasma** [_options_] _width_ _height_"],["PARAMETERS","**width**\n> Image width in pixels.\n\n**height**\n> Image height in pixels.\n\n**-randomseed** _n_\n> Seed for reproducibility."],["DESCRIPTION","**ppmplasma** generates a plasma fractal image with colorful, cloud-like patterns. It uses recursive subdivision to create smooth, organic-looking textures.\n\nPart of Netpbm toolkit."],["EXAMPLES","```bash"],["Basic plasma","ppmplasma 640 480 > plasma.ppm"],["Reproducible result","ppmplasma -randomseed 12345 512 512 > plasma.ppm"],["Convert to PNG","ppmplasma 800 600 | pnmtopng > plasma.png"],["Create desktop background","ppmplasma 1920 1080 | pnmtojpeg > wallpaper.jpg\n```"],["ALGORITHM","Uses midpoint displacement algorithm:\n1. Set corner colors\n2. Recursively subdivide\n3. Average colors with random displacement"],["CAVEATS","Fixed color palette. For grayscale output, pipe through [ppmtopgm](/man/ppmtopgm). Part of the Netpbm toolkit. Different random seeds produce different patterns."],["HISTORY","ppmplasma is part of **Netpbm**, implementing the plasma cloud algorithm (midpoint displacement) popular in demoscene and computer graphics since the 1990s."],["SEE ALSO","[ppmforge](/man/ppmforge)(1), [pgmnoise](/man/pgmnoise)(1), [ppmmake](/man/ppmmake)(1), [netpbm](/man/netpbm)(1)"]],"e":["ppmforge","pgmnoise","ppmmake","netpbm"],"x":"ppmplasma [width] [height] > [plasma.ppm]"},"ppmtoilbm":{"n":"ppmtoilbm","t":"Convert PPM to Amiga IFF ILBM format","s":[["TAGLINE","Convert PPM to Amiga IFF ILBM format"],["TLDR","**Convert PPM to IFF ILBM format**\n\n```ppmtoilbm [input.ppm] > [output.ilbm]```\n\n**Specify bit planes**\n\n```ppmtoilbm -planes [8] [input.ppm] > [output.ilbm]```"],["SYNOPSIS","**ppmtoilbm** [_options_] [_file_]"],["PARAMETERS","**-planes** _n_\n> Number of bit planes.\n\n**-ham6**\n> Use HAM6 mode.\n\n**-ham8**\n> Use HAM8 mode."],["DESCRIPTION","**ppmtoilbm** converts PPM images to IFF ILBM (Interleaved Bitmap) format used by Amiga computers. Supports HAM (Hold-And-Modify) modes. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[ilbmtoppm](/man/ilbmtoppm)(1), [ppmtoneo](/man/ppmtoneo)(1)"]],"e":["ilbmtoppm","ppmtoneo"],"x":"ppmtoilbm [input.ppm] > [output.ilbm]"},"ppmtopi1":{"n":"ppmtopi1","t":"Convert PPM to Atari Degas PI1 format","s":[["TAGLINE","Convert PPM to Atari Degas PI1 format"],["TLDR","**Convert PPM to Atari Degas PI1**\n\n```ppmtopi1 [input.ppm] > [output.pi1]```"],["SYNOPSIS","**ppmtopi1** [_file_]"],["DESCRIPTION","**ppmtopi1** reads a PPM image as input and produces an Atari Degas .pi1 file as output. PI1 is the low-resolution (320x200, 16 colors) format used by Degas on Atari ST computers. There are no command-line options beyond those common to all Netpbm programs. Input is read from the specified file or from standard input if no file is given. Output is written to standard output. Part of the Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pi1toppm](/man/pi1toppm)(1), [ppmtoneo](/man/ppmtoneo)(1), [pbmtopi3](/man/pbmtopi3)(1), [ppmtopgm](/man/ppmtopgm)(1)"]],"e":["pi1toppm","ppmtoneo","pbmtopi3","ppmtopgm"],"x":"ppmtopi1 [input.ppm] > [output.pi1]"},"pyclean":{"n":"pyclean","t":"Remove Python compiled bytecode files","s":[["TAGLINE","Remove Python compiled bytecode files"],["TLDR","**Remove bytecode files**\n\n```pyclean [directory]```\n\n**Clean current directory**\n\n```pyclean .```\n\n**Clean package**\n\n```pyclean -p [package_name]```\n\n**Verbose output**\n\n```pyclean -v [directory]```"],["SYNOPSIS","**pyclean** [_options_] [_directory_]"],["PARAMETERS","_DIRECTORY_\n> Directory to clean.\n\n**-p** _PACKAGE_\n> Clean specific package.\n\n**-v**\n> Verbose output.\n\n**-q**\n> Quiet mode."],["DESCRIPTION","**pyclean** removes Python compiled bytecode files (**.pyc**) and **__pycache__** directories from a specified directory tree or Debian package. This is useful for cleaning up stale bytecode after Python version changes, reducing disk usage, or preparing clean source distributions.\n\nThe tool is part of Debian's Python packaging infrastructure and can target specific packages with the **-p** flag or recursively clean arbitrary directories. Since Python regenerates bytecode files automatically on import, removing them has no lasting effect beyond temporarily increasing the next import time."],["CAVEATS","Primarily a Debian/Ubuntu utility (part of the **python-minimal** package). Python automatically regenerates .pyc files on next import, so removal is safe but causes a brief slowdown on first run. The `-p` flag only works with Debian packages."],["HISTORY","**pyclean** is part of Debian's Python packaging infrastructure, designed to clean up bytecode files during package upgrades and removals."],["SEE ALSO","[python](/man/python)(1), [py3clean](/man/py3clean)(1)"]],"e":["python","py3clean"],"x":"pyclean [directory]"},"qdel":{"n":"qdel","t":"Delete jobs from batch queue systems","s":[["TAGLINE","Delete jobs from batch queue systems"],["TLDR","**Delete job**\n\n```qdel [job_id]```\n\n**Delete multiple jobs**\n\n```qdel [job_id1] [job_id2] [job_id3]```\n\n**Delete all user's jobs**\n\n```qdel -u [username]```\n\n**Force delete**\n\n```qdel -f [job_id]```\n\n**Delete job array**\n\n```qdel [job_id][]```"],["SYNOPSIS","**qdel** [_options_] _job_ids_..."],["PARAMETERS","**-f**, **--force**\n> Force deletion.\n\n**-u** _user_\n> Delete all jobs for user.\n\n**-W** _time_\n> Delay deletion.\n\n**-p**\n> Purge job (remove all traces)."],["DESCRIPTION","**qdel** deletes jobs from the batch job queue. It's part of PBS/Torque, SGE, or similar job scheduling systems used on HPC clusters."],["EXAMPLES","```bash"],["Delete single job","qdel 12345"],["Delete multiple jobs","qdel 12345 12346 12347"],["Delete range","qdel 12345-12350"],["Force kill","qdel -f 12345"],["Delete all my jobs","qdel -u $(whoami)"],["Delete array job","qdel 12345[]"],["Delete specific array task","qdel 12345[5]\n```"],["JOB STATES","Jobs can be deleted in these states:\n- Queued (Q)\n- Running (R)\n- Held (H)"],["CAVEATS","Syntax varies between PBS, SGE, SLURM. Running jobs may take time to terminate. Admin may restrict deletion."],["HISTORY","qdel is part of the **PBS** (Portable Batch System) originally developed at NASA Ames Research Center."],["INSTALL","```apt: sudo apt install gridengine-client```"],["SEE ALSO","[qsub](/man/qsub)(1), [qstat](/man/qstat)(1), [scancel](/man/scancel)(1)"]],"e":["qsub","qstat","scancel"],"x":"qdel [job_id]"},"rabbitmq-streams":{"n":"rabbitmq-streams","t":"Manage RabbitMQ stream queues","s":[["TAGLINE","Manage RabbitMQ stream queues"],["TLDR","**List** stream protocol **connections**\n\n```rabbitmq-streams list_stream_connections```\n\n**Display** the **status** of a stream\n\n```rabbitmq-streams stream_status --vhost [vhost] --stream [stream_name]```\n\n**Add** a stream **replica** on a given node\n\n```rabbitmq-streams add_replica --vhost [vhost] [stream_name] [node]```\n\n**Delete** a stream **replica** from a given node\n\n```rabbitmq-streams delete_replica --vhost [vhost] [stream_name] [node]```\n\n**Restart** a stream and all its **replicas**\n\n```rabbitmq-streams restart_stream --vhost [vhost] [stream_name]```\n\nRestart a stream with a **preferred leader** node\n\n```rabbitmq-streams restart_stream --vhost [vhost] --preferred-leader-node [node] [stream_name]```"],["SYNOPSIS","**rabbitmq-streams** [**-n** _node_] [**-t** _timeout_] [**-l**|**-q**] _command_ [_command options_]"],["PARAMETERS","**-n** _node_\n> Target node to execute the command on. Default is the node of the local host.\n\n**-t** _timeout_\n> Operation timeout in seconds. Default varies per command.\n\n**-l**, **--longnames**\n> Use long node names (FQDN). Must match the broker configuration.\n\n**-q**, **--quiet**\n> Suppress informational messages, output only essential data.\n\n**--formatter** _formatter_\n> Output format: csv, erlang, json, pretty_table, table."],["SUBCOMMANDS","**add_replica** _stream_ _node_\n> Add a stream replica on the given node.\n\n**delete_replica** _stream_ _node_\n> Remove a stream replica from the given node.\n\n**stream_status** **--stream** _stream_\n> Display the status of a stream, including leader and replicas.\n\n**restart_stream** _stream_\n> Restart a stream including all of its replicas. Accepts **--preferred-leader-node** to place the leader on a specific node.\n\n**list_stream_connections** [_connectioninfoitem_...]\n> Return stream protocol connection statistics.\n\n**list_stream_consumers** [**-p** _vhost_] [_consumerinfoitem_...]\n> Return consumers attached to a stream.\n\n**list_stream_publishers** [**-p** _vhost_] [_publisherinfoitem_...]\n> Return publishers attached to a stream.\n\n**list_stream_consumer_groups** [**-p** _vhost_]\n> List single active consumer groups for streams."],["DESCRIPTION","**rabbitmq-streams** is a command line tool for managing RabbitMQ streams. Streams are persistent, replicated, append-only log data structures designed for high-throughput messaging workloads. This tool manages stream replicas, monitors stream status, and lists stream connections and consumers. Part of the RabbitMQ CLI tools suite."],["INSTALL","```dnf: sudo dnf install rabbitmq-server```\n\n```apk: sudo apk add rabbitmq-server```\n\n```zypper: sudo zypper install rabbitmq-server```\n\n```nix: nix profile install nixpkgs#rabbitmq-server```"],["SEE ALSO","[rabbitmqctl](/man/rabbitmqctl)(1), [rabbitmq-queues](/man/rabbitmq-queues)(1), [rabbitmq-diagnostics](/man/rabbitmq-diagnostics)(1)"]],"e":["rabbitmqctl","rabbitmq-queues","rabbitmq-diagnostics"],"x":"rabbitmq-streams list_stream_connections"},"serpl":{"n":"serpl","t":"VS Code-style search and replace TUI for the terminal","s":[["TAGLINE","VS Code-style search and replace TUI for the terminal"],["TLDR","**Launch search and replace in the current directory**\n\n```serpl```\n\n**Search and replace in a specific project directory**\n\n```serpl --project-root [/path/to/project]```\n\n**Show version and config file location**\n\n```serpl --version```"],["SYNOPSIS","**serpl** [_--project-root PATH_]"],["PARAMETERS","**--project-root** _PATH_\n> Specify the project root directory for search operations. Defaults to the current directory.\n\n**--version**\n> Display version information and configuration file path."],["DESCRIPTION","**serpl** is a TUI that lets you search and replace text across an entire project folder, mimicking VS Code's search-and-replace experience. It uses **ripgrep** for fast searching and optionally **ast-grep** for syntax-aware matching. It supports multiple search modes (simple, match case, whole word, regex, AST Grep) and replace modes (simple, preserve case) with an interactive preview of changes before committing them. Key bindings are configurable via a **config.json** file."],["CAVEATS","Requires **ripgrep** to be installed. The optional AST Grep mode requires installing with the **ast_grep** feature flag (`cargo install serpl --features ast_grep`)."],["HISTORY","**serpl** was created by **Yassine Bridi** (yassinebridi) and is written in **Rust**."],["INSTALL","```pacman: sudo pacman -S serpl```\n\n```brew: brew install serpl```\n\n```nix: nix profile install nixpkgs#serpl```"],["SEE ALSO","[scooter](/man/scooter)(1), [sed](/man/sed)(1), [rg](/man/rg)(1), [ast-grep](/man/ast-grep)(1)"]],"e":["scooter","sed","rg","ast-grep"],"x":"serpl"},"setxkbmap":{"n":"setxkbmap","t":"Configure X11 keyboard layouts","s":[["TAGLINE","Configure X11 keyboard layouts"],["TLDR","Set the keyboard layout to **French AZERTY**\n\n```setxkbmap fr```\n\nSet **multiple layouts** with switching option\n\n```setxkbmap -layout us,de -variant ,qwerty -option 'grp:alt_caps_toggle'```\n\n**Query** current keyboard settings\n\n```setxkbmap -query```\n\nSet keyboard **model**\n\n```setxkbmap -model pc105 -layout us```\n\n**Print** keymap components without applying\n\n```setxkbmap -layout us -print```\n\nSet layout for a specific **input device**\n\n```setxkbmap -device [device_id] -layout [layout]```"],["SYNOPSIS","**setxkbmap** [_args_] [_layout_ [_variant_ [_option_...]]]"],["PARAMETERS","**-layout** _name_\n> Specifies layout name (comma-separated for multiple layouts)\n\n**-variant** _name_\n> Specifies layout variant (comma-separated, matching layout order)\n\n**-model** _name_\n> Specifies keyboard model (e.g., pc104, pc105, thinkpad)\n\n**-option** _name_\n> Specifies XKB option (can be used multiple times)\n\n**-device** _device_\n> Numeric device ID to update (default: core keyboard)\n\n**-display** _display_\n> X display to update\n\n**-config** _file_\n> XKB configuration file describing keyboard\n\n**-rules** _file_\n> Rules file for resolving layout to components\n\n**-query**\n> Display current rules, model, layout, variant, and options\n\n**-print**\n> Output component names in xkbcomp format, then exit\n\n**-I** _directory_\n> Add directory to search path for layout files\n\n**-v, -verbose** [_level_]\n> Set verbosity level (0-10, default 5)\n\n**-help**\n> Display usage information\n\n**-version**\n> Print program version"],["DESCRIPTION","**setxkbmap** configures keyboard layouts for the X Window System using the X Keyboard Extension (XKB). It builds keymaps from components stored in **/usr/share/X11/xkb** and applies them to the current X session.\n\nThe command supports multiple keyboard layouts that can be switched using configurable key combinations (options). Common switching options include **grp:alt_shift_toggle**, **grp:caps_toggle**, and **grp:win_space_toggle**."],["CONFIGURATION","**/usr/share/X11/xkb/**\n> System XKB data directory containing layout definitions, rules, symbols, and keycodes.\n\n**/etc/X11/xorg.conf.d/**\n> Drop-in configuration directory for persistent keyboard layout settings via X.Org."],["CAVEATS","Changes are session-only and do not persist across X restarts. For permanent changes, configure through desktop environment settings, **/etc/X11/xorg.conf.d/**, or systemd's **localectl**. Does not work in Wayland sessions; use compositor-specific tools instead. Layout names vary by distribution."],["HISTORY","**setxkbmap** is part of the X Keyboard Extension (XKB), which was developed in the mid-**1990s** to replace the older core X keyboard protocol. XKB was integrated into XFree86 and later X.Org, becoming the standard keyboard configuration mechanism for X11. The tool is maintained as part of the **xorg-x11-xkb-utils** package."],["INSTALL","```apt: sudo apt install x11-xkb-utils```\n\n```dnf: sudo dnf install setxkbmap```\n\n```apk: sudo apk add setxkbmap```\n\n```zypper: sudo zypper install setxkbmap```\n\n```nix: nix profile install nixpkgs#setxkbmap```"],["SEE ALSO","[xkbcomp](/man/xkbcomp)(1), [localectl](/man/localectl)(1), [loadkeys](/man/loadkeys)(1), [xmodmap](/man/xmodmap)(1)"]],"e":["xkbcomp","localectl","loadkeys","xmodmap"],"x":"setxkbmap fr"},"sha224sum":{"n":"sha224sum","t":"Compute and verify SHA-224 checksums","s":[["TAGLINE","Compute and verify SHA-224 checksums"],["TLDR","**Calculate SHA224 hash**\n\n```sha224sum [file]```\n\n**Check hashes from file**\n\n```sha224sum -c [checksums.txt]```\n\n**Calculate for multiple files**\n\n```sha224sum [file1] [file2]```\n\n**Read from stdin**\n\n```echo \"[text]\" | sha224sum```"],["SYNOPSIS","**sha224sum** [_options_] [_file_...]"],["PARAMETERS","**-c**, **--check**\n> Verify checksums from file.\n\n**-b**, **--binary**\n> Read in binary mode.\n\n**-t**, **--text**\n> Read in text mode.\n\n**--quiet**\n> Don't print OK for verified files."],["DESCRIPTION","**sha224sum** computes and verifies SHA-224 cryptographic hash values, producing a 224-bit (56-character hexadecimal) message digest. It is part of the SHA-2 family of hash functions and operates identically to **sha256sum** in usage, differing only in output length and the underlying truncation of the hash.\n\nSHA-224 is less commonly used than SHA-256 but provides adequate security for applications where a shorter hash is preferred, such as certain certificate signature schemes. With no FILE argument or when FILE is **-**, input is read from standard input. In check mode (**-c**), previously generated checksums are verified against current file contents."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[sha256sum](/man/sha256sum)(1), [sha384sum](/man/sha384sum)(1), [sha512sum](/man/sha512sum)(1)"]],"e":["sha256sum","sha384sum","sha512sum"],"x":"sha224sum [file]"},"snmptable":{"n":"snmptable","t":"Display SNMP MIB data in table format","s":[["TAGLINE","Display SNMP MIB data in table format"],["TLDR","**Display SNMP table**\n\n```snmptable -v [2c] -c [public] [host] [TABLE_OID]```\n\n**Show with column headers**\n\n```snmptable -v [2c] -c [public] -Cb [host] [TABLE_OID]```\n\n**Show interface table**\n\n```snmptable -v [2c] -c [public] [host] ifTable```"],["SYNOPSIS","**snmptable** [_options_] _agent_ _table_oid_"],["PARAMETERS","**-v** _version_\n> SNMP version (1, 2c, 3).\n\n**-c** _community_\n> Community string.\n\n**-Cb**\n> Brief column names.\n\n**-Cf** _sep_\n> Field separator.\n\n**-Cw** _width_\n> Column width."],["DESCRIPTION","**snmptable** displays SNMP table data in tabular format. Retrieves and formats MIB tables for easy reading. Part of Net-SNMP suite for network management."],["INSTALL","```apt: sudo apt install snmp```\n\n```apk: sudo apk add net-snmp-tools```"],["SEE ALSO","[snmpwalk](/man/snmpwalk)(1), [snmpget](/man/snmpget)(1)"]],"e":["snmpwalk","snmpget"],"x":"snmptable -v [2c] -c [public] [host] [TABLE_OID]"},"sphinx-build":{"n":"sphinx-build","t":"Generate documentation from reStructuredText","s":[["TAGLINE","Generate documentation from reStructuredText"],["TLDR","**Build HTML documentation**\n\n```sphinx-build -b html [source/] [build/html]```\n\n**Build PDF documentation**\n\n```sphinx-build -b latex [source/] [build/latex]```\n\n**Clean and rebuild**\n\n```sphinx-build -E -b html [source/] [build/html]```\n\n**Verbose output**\n\n```sphinx-build -v -b html [source/] [build/html]```\n\n**Build with warnings as errors**\n\n```sphinx-build -W -b html [source/] [build/html]```\n\n**Check links**\n\n```sphinx-build -b linkcheck [source/] [build/linkcheck]```\n\n**Set configuration option**\n\n```sphinx-build -D [key=value] -b html [source/] [build/]```"],["SYNOPSIS","**sphinx-build** [_-b builder_] [_options_] _sourcedir_ _outputdir_"],["PARAMETERS","**-b** _BUILDER_\n> Output format builder.\n\n**-E**\n> Rebuild all files.\n\n**-a**\n> Rebuild changed files.\n\n**-W**\n> Warnings as errors.\n\n**-v**\n> Verbose output.\n\n**-D** _KEY=VALUE_\n> Override config.\n\n**-c** _PATH_\n> Config directory.\n\n**-j** _N_\n> Parallel jobs."],["BUILDERS","**html** - HTML pages\n**latex** - LaTeX/PDF\n**epub** - EPUB ebook\n**man** - Man pages\n**linkcheck** - Check links"],["DESCRIPTION","**sphinx-build** is the build command for Sphinx, the standard documentation generator for Python projects. It reads reStructuredText or MyST Markdown source files and produces output in various formats including HTML websites, LaTeX/PDF documents, ePub ebooks, and Unix man pages.\n\nThe build process is controlled by a **conf.py** configuration file in the source directory, which defines the project name, theme, enabled extensions, and output settings. Sphinx supports automatic cross-referencing between documents, index generation, and code syntax highlighting. The **autodoc** extension can extract documentation directly from Python docstrings.\n\nMultiple builders are available, selected with the **-b** flag. Incremental builds only process changed files by default; the **-E** flag forces a complete rebuild. Parallel building with **-j** speeds up large documentation projects."],["CONFIGURATION","**conf.py**\n> Sphinx configuration file in the source directory defining project metadata, theme, extensions, template paths, and build options.\n\n**SPHINXOPTS**\n> Environment variable for passing additional options to sphinx-build when invoked through Makefiles."],["CAVEATS","Requires Python. reStructuredText learning curve. Large projects slow to build."],["HISTORY","**Sphinx** was created by **Georg Brandl** for Python documentation. Released in 2008, it became the standard for Python project documentation."],["INSTALL","```apk: sudo apk add py3-sphinx```"],["SEE ALSO","[make](/man/make)(1), [rst2html](/man/rst2html)(1), [doxygen](/man/doxygen)(1)"]],"e":["make","rst2html","doxygen"],"x":"sphinx-build -b html [source/] [build/html]"},"steamos-readonly":{"n":"steamos-readonly","t":"Toggle SteamOS filesystem write protection","s":[["TAGLINE","Toggle SteamOS filesystem write protection"],["TLDR","**Disable** read-only mode (make filesystem writable)\n\n```sudo steamos-readonly disable```\n\n**Enable** read-only mode\n\n```sudo steamos-readonly enable```"],["SYNOPSIS","**steamos-readonly** _enable_|_disable_"],["COMMANDS","**enable**\n> Set filesystem to read-only mode\n\n**disable**\n> Set filesystem to writable mode"],["DESCRIPTION","**steamos-readonly** controls the read-only status of the SteamOS root filesystem on the Steam Deck. By default, SteamOS uses an immutable root filesystem to maintain system integrity and simplify updates. This command allows temporarily disabling that protection for system modifications.\n\nDisabling read-only mode allows installing packages, modifying configuration files, and making other system-level changes that would otherwise be blocked."],["CAVEATS","Changes to the filesystem may be overwritten by system updates. Disabling read-only mode can affect system stability if misused. Should be re-enabled after making changes. Consider using Flatpak or other containerized solutions when possible. Requires root privileges."],["HISTORY","**steamos-readonly** is part of **SteamOS 3.0**, developed by **Valve** for the **Steam Deck** released in **February 2022**. The immutable filesystem design is inspired by other modern Linux distributions like Fedora Silverblue, providing reliability while allowing advanced users to opt-out when needed."],["SEE ALSO","[steamos-devmode](/man/steamos-devmode)(1), [btrfs](/man/btrfs)(8), [mount](/man/mount)(8)"]],"e":["steamos-devmode","btrfs","mount"],"x":"sudo steamos-readonly disable"},"stty":{"n":"stty","t":"Display and change terminal line settings","s":[["TAGLINE","Display and change terminal line settings"],["TLDR","**Show all settings**\n\n```stty -a```\n\n**Show settings for device**\n\n```stty -a -F [/dev/ttyUSB0]```\n\n**Set baud rate**\n\n```stty -F [/dev/ttyUSB0] [115200]```\n\n**Disable echo**\n\n```stty -echo```\n\n**Enable raw mode**\n\n```stty raw```\n\n**Reset to sane defaults**\n\n```stty sane```\n\n**Set terminal size**\n\n```stty rows [24] cols [80]```"],["SYNOPSIS","**stty** [_options_] [_settings_...]"],["DESCRIPTION","**stty** displays or changes terminal line settings. It controls various aspects of terminal I/O including baud rate, character processing, and signal handling.\n\nThe tool is essential for configuring serial ports and customizing terminal behavior."],["PARAMETERS","**-a**, **--all**\n> Print all settings.\n\n**-g**, **--save**\n> Print in stty-readable form.\n\n**-F** _device_, **--file**=_device_\n> Open and use the specified device instead of stdin.\n\n**sane**\n> Reset to sane values.\n\n**raw**\n> Raw mode (no processing).\n\n**cooked**\n> Normal mode (opposite of raw).\n\n**echo** / **-echo**\n> Enable/disable echoing of input characters.\n\n**rows** _n_\n> Set terminal rows.\n\n**cols** _n_\n> Set terminal columns.\n\n**ispeed** _n_\n> Set input baud rate.\n\n**ospeed** _n_\n> Set output baud rate.\n\n**ixon** / **-ixon**\n> Enable/disable START/STOP output control.\n\n**ixoff** / **-ixoff**\n> Enable/disable sending of START/STOP characters.\n\n**crtscts** / **-crtscts**\n> Enable/disable RTS/CTS hardware flow control.\n\n**cstopb** / **-cstopb**\n> Use two stop bits per character (one with '-')."],["CAVEATS","Settings affect current terminal. Some require appropriate privileges. Raw mode disables many features. Changes may not persist."],["HISTORY","**stty** (set tty) is a traditional Unix utility dating back to early Unix versions. It remains essential for serial communication and terminal configuration."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[tty](/man/tty)(1), [setserial](/man/setserial)(8), [getty](/man/getty)(8), [reset](/man/reset)(1)"]],"e":["tty","setserial","getty","reset"],"x":"stty -a"},"swaptop":{"n":"swaptop","t":"Real-time swap usage monitor for the terminal","s":[["TAGLINE","Real-time swap usage monitor for the terminal"],["TLDR","**Launch the swap monitor**\n\n```swaptop```\n\n**Run with elevated permissions for full visibility**\n\n```sudo -E swaptop```"],["SYNOPSIS","**swaptop**"],["DESCRIPTION","**swaptop** is a TUI tool that monitors swap memory usage on Linux and Windows. It displays per-process swap consumption, aggregated software usage views, and animated graphs. It supports multiple color themes and unit switching."],["HISTORY","**swaptop** was created by **luis-ota** and is written in **Rust**."],["INSTALL","```pacman: sudo pacman -S swaptop```"],["SEE ALSO","[btop](/man/btop)(1), [htop](/man/htop)(1), [free](/man/free)(1)"]],"e":["btop","htop","free"],"x":"swaptop"},"swiftlet":{"n":"swiftlet","t":"Stream large Qwen MoE models on Apple Silicon with low RAM","s":[["TAGLINE","Stream large Qwen MoE models on Apple Silicon with low RAM"],["TLDR","**Build** the release binaries\n\n```git clone https://github.com/leonickson1/Swiftlet.git && cd Swiftlet```\n\n```swift build -c release```\n\n**Download / repack** a 4-bit model container from Hugging Face\n\n```.build/release/swiftlet-repack --from-hf Leonickson/Qwen3.6-35B-A3B-qpack --output ~/models/qwen3.6-35b.qpack```\n\n**Chat** with multi-turn history\n\n```swiftlet chat ~/models/qwen3.6-35b.qpack \"[Who wrote One Hundred Years of Solitude?]\" \"[What language did he write it in?]\"```\n\n**One-shot** generation with stats\n\n```swiftlet generate ~/models/qwen3.6-35b.qpack --gpu --chat --prompt \"[Explain expert streaming in one paragraph.]\"```\n\nStart an **OpenAI-compatible** local server\n\n```swiftlet-server --model ~/models/qwen3.6-35b.qpack --port 8080```"],["SYNOPSIS","**swiftlet** **chat** _model.qpack_ [_prompt_...]\n\n**swiftlet** **generate** _model.qpack_ [**--gpu**] [**--chat**] [**--prompt** _text_]\n\n**swiftlet-repack** **--from-hf** _repo_ **--output** _path_\n\n**swiftlet-server** **--model** _path_ [**--port** _n_]"],["PARAMETERS","**chat** _model_ [_prompts_...]\n> Interactive or scripted multi-turn chat. Applies the model chat template, can disable reasoning blocks, and caches conversation state so follow-ups only prefill the new turn.\n\n**generate** _model_ [**--gpu**] [**--chat**] [**--prompt** _text_]\n> One-shot generation with optional GPU path and generation statistics.\n\n**swiftlet-repack** **--from-hf** _owner/repo_ **--output** _file.qpack_\n> Stream and pack expert weights into a fixed-stride **.qpack** container (resumable). Also accepts raw MLX checkpoints via **--source** / Hugging Face MLX community repos.\n\n**swiftlet-server** **--model** _file.qpack_ [**--port** _n_]\n> OpenAI chat-completions-compatible HTTP server (loopback-oriented)."],["DESCRIPTION","**swiftlet** is a **Swift + Metal** runtime for the **Qwen3-Next** and **Qwen3.5/3.6 MoE** hybrid family on **Apple Silicon**. Only the dense core stays resident in RAM; routed Mixture-of-Experts weights stream from SSD with **pread** into a bounded expert cache. That design runs models such as **Qwen3.6-35B-A3B** (~2.6 GB peak RAM, ~18 GB on disk) and **Qwen3-Next-80B-A3B** (~4.3 GB peak RAM, ~42 GB on disk) at 4-bit quantization on ordinary Macs—and the 35B class can run on recent iPhones inside companion apps.\n\nThe package ships three executables: **swiftlet** (chat / generate), **swiftlet-repack** (build **.qpack** containers), and **swiftlet-server** (OpenAI-compatible API). **SwiftletCore** is also a library for embedding in macOS/iOS apps. Kernels are runtime-compiled Metal shaders; layer math is validated against mlx-lm reference fixtures."],["CAVEATS","**Apple Silicon only** (macOS 14+ / iOS 17+). Requires free SSD space for full containers (tens of GB). Only a few billion parameters are active per token, so factual recall can feel closer to a smaller dense model than the headline parameter count. The decode loop remains a performance focus; reported speeds vary by chip. Model weights are downloaded separately and remain under their own licenses (Qwen models: Apache-2.0). Not a general Linux GPU inference stack."],["HISTORY","**Swiftlet** is an Apache-2.0 project that applies expert-streaming ideas (in the spirit of work such as TurboFieldfare) to Qwen hybrid MoE architectures with Gated DeltaNet and sparse expert routing, implemented from scratch in Swift and Metal against mlx-lm correctness references."],["SEE ALSO","[ollama](/man/ollama)(1), [mlx](/man/mlx)(1), [llama](/man/llama)(1)"],["RESOURCES","```[Source code](https://github.com/leonickson1/Swiftlet)```\n\n```[Homepage](https://github.com/leonickson1/Swiftlet)```"]],"e":["ollama","mlx","llama"],"x":"git clone https://github.com/leonickson1/Swiftlet.git && cd Swiftlet"},"systemctl-log-level":{"n":"systemctl-log-level","t":"Get or set systemd log verbosity","s":[["TAGLINE","Get or set systemd log verbosity"],["TLDR","Show **current** log level\n\n```systemctl log-level```\n\n**Set** log level\n\n```systemctl log-level [emerg|alert|crit|err|warning|notice|info|debug]```"],["SYNOPSIS","**systemctl log-level** [_LEVEL_]"],["LOG LEVELS","**emerg** - System is unusable\n\n**alert** - Immediate action required\n\n**crit** - Critical conditions\n\n**err** - Error conditions\n\n**warning** - Warning conditions\n\n**notice** - Normal but significant\n\n**info** - Informational messages\n\n**debug** - Debug-level messages"],["DESCRIPTION","**systemctl log-level** gets or sets the logging verbosity of the systemd service manager (PID 1). Without arguments, it displays the current level. With a level argument, it changes the verbosity at runtime.\n\nHigher verbosity levels (debug, info) produce more log output and can help diagnose issues. Lower levels (err, crit) reduce noise in production."],["CAVEATS","Changes are temporary and reset on reboot. For permanent changes, modify the systemd configuration. Debug level can produce significant log volume and impact performance."],["HISTORY","The **log-level** subcommand provides runtime control over systemd's verbosity, useful for temporary debugging without configuration changes or restarts."],["SEE ALSO","[systemctl-log-target](/man/systemctl-log-target)(1), [journalctl](/man/journalctl)(1), [systemctl](/man/systemctl)(1)"]],"e":["systemctl-log-target","journalctl","systemctl"],"x":"systemctl log-level"},"tcpkill":{"n":"tcpkill","t":"Terminate TCP connections with resets","s":[["TAGLINE","Terminate TCP connections with resets"],["TLDR","**Kill** connections by interface, host, and port\n\n```tcpkill -i [eth1] host [192.95.4.27] and port [2266]```"],["SYNOPSIS","**tcpkill** [_OPTIONS_] _EXPRESSION_"],["PARAMETERS","**-i** _INTERFACE_\n> Network interface to use\n\n**-1** to **-9**\n> Aggressiveness level (default: 3)"],["DESCRIPTION","**tcpkill** terminates in-progress TCP connections by injecting TCP RST (reset) packets. It monitors traffic matching the specified filter expression and sends forged reset packets to both endpoints.\n\nThe filter expression uses libpcap syntax, allowing filtering by host, port, network, or any combination."],["CAVEATS","Requires root privileges. Part of the dsniff package. Only works on unencrypted connections visible on the network. May require multiple attempts for active connections. Use responsibly - unauthorized use may be illegal."],["HISTORY","**tcpkill** is part of the **dsniff** suite created by **Dug Song** for network auditing. It's used for security testing and emergency connection termination."],["INSTALL","```apt: sudo apt install dsniff```\n\n```dnf: sudo dnf install dsniff```\n\n```pacman: sudo pacman -S dsniff```\n\n```nix: nix profile install nixpkgs#dsniff```"],["SEE ALSO","[tcpdump](/man/tcpdump)(8), [dsniff](/man/dsniff)(8)"]],"e":["tcpdump","dsniff"],"x":"tcpkill -i [eth1] host [192.95.4.27] and port [2266]"},"tlmgr-backup":{"n":"tlmgr-backup","t":"Backup TeX Live packages","s":[["TAGLINE","Backup TeX Live packages"],["TLDR","**Backup package**\n\n```tlmgr backup [package]```\n\n**Backup all packages**\n\n```tlmgr backup --all```\n\n**Backup to directory**\n\n```tlmgr backup --backupdir [/path/to/backup] [package]```\n\n**Clean old backups**\n\n```tlmgr backup --clean [days]```"],["SYNOPSIS","**tlmgr backup** [_options_] [_package_...]"],["PARAMETERS","**--all**\n> Backup all installed packages.\n\n**--backupdir** _dir_\n> Backup destination directory.\n\n**--clean** _n_\n> Remove backups older than n days.\n\n**--dry-run**\n> Show what would be done."],["DESCRIPTION","**tlmgr backup** creates backups of TeX Live packages. Useful before updates or system changes. Part of TeX Live Manager."],["SEE ALSO","[tlmgr-restore](/man/tlmgr-restore)(1), [tlmgr-update](/man/tlmgr-update)(1)"]],"e":["tlmgr-restore","tlmgr-update"],"x":"tlmgr backup [package]"},"transfersh":{"n":"transfersh","t":"Share files via command-line uploads","s":[["TAGLINE","Share files via command-line uploads"],["TLDR","**Upload file**\n\n```curl --upload-file [file.txt] https://transfer.sh/[filename]```\n\n**Upload with max downloads**\n\n```curl --upload-file [file.txt] -H \"Max-Downloads: [1]\" https://transfer.sh/```\n\n**Upload with expiry**\n\n```curl --upload-file [file.txt] -H \"Max-Days: [7]\" https://transfer.sh/```\n\n**Delete upload**\n\n```curl -X DELETE [delete-url]```\n\n**Encrypt upload**\n\n```cat [file] | gpg -c | curl --upload-file - https://transfer.sh/[file.gpg]```"],["SYNOPSIS","**curl** --upload-file _file_ [_-H headers_] https://transfer.sh/[_name_]"],["PARAMETERS","**--upload-file**\n> File to upload.\n\n**Max-Downloads:** _N_\n> Download limit.\n\n**Max-Days:** _N_\n> Days until expiry.\n\n**-X DELETE**\n> Delete file."],["DESCRIPTION","**transfer.sh** is a file sharing service designed for easy command-line use. Files are uploaded via standard HTTP PUT requests using curl, and the service returns a unique download URL that can be shared with others. No account or registration is required for basic uploads.\n\nUploads can be configured with expiration times and download limits through HTTP headers, giving control over how long files remain available and how many times they can be downloaded. Files are automatically deleted after the specified expiration period. The service also supports encryption by piping files through GPG before uploading.\n\nTransfer.sh can be self-hosted as an open-source Go application or used through the public instance at transfer.sh. It supports uploads up to several gigabytes and provides delete URLs for manual removal of uploaded files."],["CAVEATS","External service. Size limits. Public URLs."],["HISTORY","**transfer.sh** was created as a simple file sharing service using the command line via curl."],["SEE ALSO","[curl](/man/curl)(1), [wormhole](/man/wormhole)(1), [ffsend](/man/ffsend)(1)"]],"e":["curl","wormhole","ffsend"],"x":"curl --upload-file [file.txt] https://transfer.sh/[filename]"},"udiskie":{"n":"udiskie","t":"Automounter for removable media","s":[["TAGLINE","Automounter for removable media"],["TLDR","Start with **tray** icon and notifications\n\n```udiskie --tray --notify```\n\nStart with **smart tray** that auto-hides when no devices are available\n\n```udiskie --smart-tray```\n\nDisable **automounting** but keep the tray\n\n```udiskie --no-automount --tray```\n\nUse custom **config** file\n\n```udiskie --config [path/to/config.yml]```\n\nUse custom **password** prompt for LUKS devices\n\n```udiskie --password-prompt '[command]'```\n\nEnable **verbose** output\n\n```udiskie --verbose```"],["SYNOPSIS","**udiskie** [_OPTIONS_]"],["PARAMETERS","**-h**, **--help**\n> Display help message and exit\n\n**-V**, **--version**\n> Show version information and exit\n\n**-v**, **--verbose**\n> Enable verbose output\n\n**-q**, **--quiet**\n> Suppress output messages\n\n**-c** _FILE_, **--config**=_FILE_\n> Use specified configuration file\n\n**-C**, **--no-config**\n> Ignore all configuration files\n\n**-a**, **--automount**\n> Enable automatic mounting of new devices (default)\n\n**-A**, **--no-automount**\n> Disable automatic mounting of new devices\n\n**-n**, **--notify**\n> Enable pop-up notifications\n\n**-N**, **--no-notify**\n> Disable pop-up notifications\n\n**-t**, **--tray**\n> Show system tray icon\n\n**-s**, **--smart-tray**\n> Show tray icon that auto-hides when there is no action available\n\n**-T**, **--no-tray**\n> Disable tray icon (default)\n\n**-f** _PROGRAM_, **--file-manager**=_PROGRAM_\n> Set program to open mounted directories\n\n**-F**, **--no-file-manager**\n> Disable directory browsing\n\n**-p** _COMMAND_, **--password-prompt**=_COMMAND_\n> Set command for password retrieval (LUKS devices)\n\n**-P**, **--no-password-prompt**\n> Prevent unlocking of LUKS encrypted devices\n\n**--appindicator**\n> Use AppIndicator3 for the status icon\n\n**--password-cache** _MINUTES_\n> Cache passwords for the specified number of minutes\n\n**--no-password-cache**\n> Disable password caching\n\n**--event-hook** _COMMAND_\n> Execute command on device events"],["DESCRIPTION","**udiskie** is a user-level automounter for removable media using udisks2. It automatically mounts USB drives, optical discs, and other removable devices when they are inserted.\n\nThe daemon can show a system tray icon for easy device management and send desktop notifications when devices are mounted or unmounted. Configuration is read from **~/.config/udiskie/config.yml** by default."],["CAVEATS","Requires udisks2 daemon running. Desktop notifications need a notification daemon. Tray icon requires system tray support. Some encrypted devices need password configuration."],["INSTALL","```dnf: sudo dnf install udiskie```\n\n```pacman: sudo pacman -S udiskie```\n\n```apk: sudo apk add udiskie```\n\n```zypper: sudo zypper install udiskie```\n\n```nix: nix profile install nixpkgs#udiskie```"],["SEE ALSO","[udiskie-mount](/man/udiskie-mount)(1), [udiskie-umount](/man/udiskie-umount)(1), [udisksctl](/man/udisksctl)(1)"]],"e":["udiskie-mount","udiskie-umount","udisksctl"],"x":"udiskie --tray --notify"},"updo":{"n":"updo","t":"Uptime monitoring CLI with alerting support","s":[["TAGLINE","Uptime monitoring CLI with alerting support"],["TLDR","**Monitor a website**\n\n```updo monitor [https://example.com]```\n\n**Monitor with custom refresh interval and timeout**\n\n```updo monitor --refresh [10] --timeout [5] [https://example.com]```\n\n**Monitor with Slack webhook alerts**\n\n```updo monitor --webhook-url \"[https://hooks.slack.com/...]\" [https://example.com]```"],["SYNOPSIS","**updo** **monitor** [_options_] _URL_..."],["PARAMETERS","**--refresh** _seconds_\n> Refresh interval in seconds (default: 5).\n\n**--timeout** _seconds_\n> Request timeout in seconds (default: 10).\n\n**--count** _n_\n> Number of checks to perform (0 = infinite, the default).\n\n**--webhook-url** _url_\n> Webhook URL for alerts (Slack or Discord).\n\n**--assert-text** _text_\n> Expected response body text used to validate each check.\n\n**--simple**\n> Print plain text output instead of the interactive TUI."],["DESCRIPTION","**updo** is a command-line tool for monitoring website uptime and performance. It provides real-time metrics including response time, SSL certificate expiry, and uptime percentage. Supports multi-target monitoring, multi-region AWS Lambda deployment, Prometheus and Grafana integration, and webhook alerts for Slack and Discord."],["HISTORY","**updo** was created by **Owloops** and is written in **Go**."],["INSTALL","```aur: yay -S updo```"],["SEE ALSO","[ping](/man/ping)(1), [curl](/man/curl)(1), [httpstat](/man/httpstat)(1)"]],"e":["ping","curl","httpstat"],"x":"updo monitor [https://example.com]"},"urxvt":{"n":"urxvt","t":"Lightweight Unicode terminal emulator for X11","s":[["TAGLINE","Lightweight Unicode terminal emulator for X11"],["TLDR","Open new **window**\n\n```urxvt```\n\nOpen in specific **directory**\n\n```urxvt -cd [path/to/directory]```\n\nRun **command**\n\n```urxvt -e [command]```\n\nRun command and **keep** open\n\n```urxvt --hold -e [command]```\n\nRun with **shell**\n\n```urxvt -e sh -c [command]```"],["SYNOPSIS","**urxvt** [_OPTIONS_]"],["PARAMETERS","**-cd** _DIR_\n> Start in specified directory\n\n**-e** _COMMAND_\n> Execute command instead of shell\n\n**--hold**\n> Keep window open after command exits\n\n**-fn** _FONT_\n> Specify font\n\n**-fg** _COLOR_\n> Foreground color\n\n**-bg** _COLOR_\n> Background color"],["DESCRIPTION","**urxvt** (rxvt-unicode) is a highly customizable terminal emulator with Unicode support, transparency, and Perl extensions. It is known for being lightweight and fast while offering extensive customization through X resources.\n\nConfiguration is typically done through ~/.Xresources or command-line options."],["CAVEATS","Requires X11 display. Configuration through X resources may seem complex initially. Some features require Perl extensions to be enabled."],["INSTALL","```apt: sudo apt install rxvt-unicode```\n\n```dnf: sudo dnf install rxvt-unicode```\n\n```pacman: sudo pacman -S rxvt-unicode```\n\n```apk: sudo apk add rxvt-unicode```\n\n```zypper: sudo zypper install rxvt-unicode```\n\n```brew: brew install rxvt-unicode```\n\n```nix: nix profile install nixpkgs#rxvt-unicode```"],["SEE ALSO","[xterm](/man/xterm)(1), [st](/man/st)(1)"]],"e":["xterm","st"],"x":"urxvt"},"vnstati":{"n":"vnstati","t":"Generate network traffic graph images","s":[["TAGLINE","Generate network traffic graph images"],["TLDR","Output a **summary** of the last 2 months, days, and all-time\n\n```vnstati --summary --iface [network_interface] --output [path/to/output.png]```\n\nOutput the **10 most traffic-intensive days** of all time\n\n```vnstati --top 10 --iface [network_interface] --output [path/to/output.png]```\n\nOutput **monthly** traffic statistics from the last 12 months\n\n```vnstati --months --iface [network_interface] --output [path/to/output.png]```\n\nOutput **hourly** traffic statistics from the last 24 hours\n\n```vnstati --hours --iface [network_interface] --output [path/to/output.png]```"],["SYNOPSIS","**vnstati** [_options_] **-o** _file_ | **--output** _file_"],["PARAMETERS","**-i**, **--iface** _interface_\n> Select interface to use\n\n**-o**, **--output** _file_\n> Output filename (PNG format)\n\n**-s**, **--summary**\n> Output traffic summary\n\n**--top** _count_\n> Output top days by traffic\n\n**-h**, **--hours**\n> Output hourly traffic statistics\n\n**-d**, **--days**\n> Output daily traffic statistics\n\n**-m**, **--months**\n> Output monthly traffic statistics\n\n**-y**, **--years**\n> Output yearly traffic statistics\n\n**-5**, **--fiveminutes**\n> Output 5-minute resolution statistics\n\n**--style** _num_\n> Modify visual appearance (0-4)\n\n**-c**, **--cache** _time_\n> Update output only if database is newer than time\n\n**--headertext** _text_\n> Custom header text\n\n**--transparent**\n> Toggle background transparency"],["DESCRIPTION","**vnstati** generates PNG images from vnStat network traffic data, providing visual representations of bandwidth usage. It reads the same database as vnstat and outputs graphical summaries suitable for web pages or reports.\n\nThe tool supports various time ranges (hourly, daily, monthly, yearly) and can generate traffic summaries, top usage days, and 5-minute resolution graphs. Multiple style options allow customization of appearance."],["CAVEATS","Requires vnstat daemon to be running and collecting data. Output is PNG format only. The interface must have existing traffic data in the vnstat database."],["HISTORY","Part of the **vnStat** project by Teemu Toivola. vnstati was added to provide graphical output capabilities, commonly used for generating bandwidth graphs for web-based monitoring dashboards and reports."],["INSTALL","```apt: sudo apt install vnstati```\n\n```dnf: sudo dnf install vnstat```\n\n```pacman: sudo pacman -S vnstat```\n\n```apk: sudo apk add vnstat```\n\n```zypper: sudo zypper install vnstat```\n\n```brew: brew install vnstat```\n\n```nix: nix profile install nixpkgs#vnstat```"],["SEE ALSO","[vnstat](/man/vnstat)(1), [nethogs](/man/nethogs)(8), [iftop](/man/iftop)(8)"]],"e":["vnstat","nethogs","iftop"],"x":"vnstati --summary --iface [network_interface] --output [path/to/output.png]"},"waste":{"n":"waste","t":"local LLM inference that streams model experts from disk","s":[["TAGLINE","local LLM inference that streams model experts from disk"],["TLDR","**Build** the engine and CLI\n\n```make```\n\n**Run** a one-shot prompt against a converted container\n\n```waste run [~/models/k3.waste] \"[What is the capital of France?]\" -n [32]```\n\n**Interactive multi-turn chat**\n\n```waste chat [~/models/k3.waste]```\n\n**Show next-token distribution**\n\n```waste eval [~/models/k3.waste] \"[2 + 2 =]\" --top-k [5]```\n\n**Plan RAM budget** for a container\n\n```waste plan [~/models/k3.waste] --budget [46G]```\n\n**Run with image input** (multimodal models)\n\n```waste run [~/models/k3.waste] \"[What is in this picture?]\" --image [photo.png]```\n\n**Verify expert CRC** on load\n\n```waste run [~/models/k3.waste] \"[prompt]\" --verify```"],["SYNOPSIS","**waste** _command_ [_options_] [_args_...]"],["PARAMETERS","**run** _CONTAINER_ [_PROMPT_]\n> Generate a completion from a prompt (or stdin). Common flags: **-n** _count_ (token cap), **--budget** _SIZE_, **--image** _FILE_, **--verify**.\n\n**chat** _CONTAINER_\n> Multi-turn interactive session; conversation state is kept in the process.\n\n**eval** _CONTAINER_ _PROMPT_\n> Show next-token scores/distribution. Supports **--top-k**, **--json**, **--image**.\n\n**plan** _CONTAINER_\n> Report what RAM budget fits on this machine and what the engine would pick.\n\n**info** _CONTAINER_\n> Print container/model metadata.\n\n**bench** _CONTAINER_\n> Run a built-in performance check.\n\n**tokenize** _CONTAINER_\n> Tokenize or inspect prompt layout (see **--help** for current subflags).\n\n**--budget** _SIZE_\n> Hard RAM ceiling (e.g. `46G`). If omitted, the engine picks a budget under about 7/8 of physical RAM, never below the model floor.\n\n**--verify**\n> Check each expert record CRC on cache miss (slower; useful after copy/download). Also **WASTE_VERIFY=1**.\n\n**--json**\n> Machine-readable output for eval/plan/info/bench and similar commands.\n\n**--help**\n> List all commands and flags."],["DESCRIPTION","**waste** (Weight-Aware Streaming Tensor Engine) is a dependency-free C inference engine and CLI for running large mixture-of-experts language models when the full weight set does not fit in RAM. It keeps a resident \"trunk\" in memory, streams activated experts from a converted **.waste** container on NVMe, and uses remaining RAM as a bounded expert cache.\n\nThe flagship proof point is open-weight **Kimi K3** (~2.78T parameters, ~982 GiB container) running on consumer hardware at roughly half a token per second with enough RAM and internal NVMe. Smaller models in the same format (e.g. Kimi-Linear 48B) run much faster with far lower RAM floors.\n\nConvert published safetensors with the repo's Python tools once; at runtime only the **waste** binary (and libc/pthreads) is required. An optional OpenAI-compatible HTTP server lives under `serve/` and uses the same public C API via ctypes."],["CAVEATS","K3-class models need tens of GB of RAM (about **29 GB** minimum open, **~64 GB** for useful throughput) and a **~1 TB** container on **fast internal NVMe**—USB enclosures are far too slow. Build needs a C11 compiler and **make**; conversion needs Python/torch. Expert CRC checks are off by default. Chat templates are fully filled for models the converter knows (K3 today); other containers may run in raw prompt mode. Not a drop-in replacement for general multi-model runtimes like **ollama**."],["HISTORY","**WASTE** is developed by **SQLite Cloud, Inc.** (sqliteai) and released under **Apache 2.0**. It targets disk-streaming inference for frontier-scale MoE models on single machines rather than multi-GPU servers."],["SEE ALSO","[ollama](/man/ollama)(1), [llama.cpp](/man/llama.cpp)(1)"],["RESOURCES","```[Source code](https://github.com/sqliteai/waste)```\n\n```[Documentation](https://github.com/sqliteai/waste/tree/main/docs)```"]],"e":["ollama","llama.cpp"],"x":"make"},"waypipe":{"n":"waypipe","t":"Remote Wayland application forwarding over SSH","s":[["TAGLINE","Remote Wayland application forwarding over SSH"],["TLDR","Run **remote** program locally\n\n```waypipe ssh [user]@[server] [program]```\n\nOpen SSH **tunnel**\n\n```waypipe ssh [user]@[server]```\n\nSkip **Vulkan** test\n\n```waypipe --test-skip-vulkan ssh [user]@[server] [program]```\n\nDisplay **help**\n\n```waypipe -h```"],["SYNOPSIS","**waypipe** [_OPTIONS_] ssh _HOST_ [_PROGRAM_]"],["PARAMETERS","**--test-skip-vulkan**\n> Skip testing for Vulkan support\n\n**-h, --help**\n> Display help information"],["DESCRIPTION","**waypipe** runs graphical Wayland applications remotely and displays them locally, similar to X11 forwarding but for Wayland compositors. It handles the Wayland protocol over SSH.\n\nThe tool creates a transparent tunnel that makes remote applications appear as native local windows."],["CAVEATS","Requires Wayland on both local and remote systems. Performance depends on network latency. Some applications may not work correctly with remote rendering."],["INSTALL","```apt: sudo apt install waypipe```\n\n```dnf: sudo dnf install waypipe```\n\n```pacman: sudo pacman -S waypipe```\n\n```apk: sudo apk add waypipe```\n\n```zypper: sudo zypper install waypipe```\n\n```nix: nix profile install nixpkgs#waypipe```"],["SEE ALSO","[ssh](/man/ssh)(1)"]],"e":["ssh"],"x":"waypipe ssh [user]@[server] [program]"},"xalan":{"n":"xalan","t":"Apache XSLT processor for XML transformation","s":[["TAGLINE","Apache XSLT processor for XML transformation"],["TLDR","**Transform XML with XSLT**\n\n```java org.apache.xalan.xslt.Process -IN [input.xml] -XSL [style.xsl] -OUT [output.html]```\n\n**Transform to stdout**\n\n```java org.apache.xalan.xslt.Process -IN [input.xml] -XSL [style.xsl]```\n\n**Set stylesheet parameter**\n\n```java org.apache.xalan.xslt.Process -IN [input.xml] -XSL [style.xsl] -PARAM [name] [value]```\n\n**Output as XML**\n\n```java org.apache.xalan.xslt.Process -IN [input.xml] -XSL [style.xsl] -XML```\n\n**Check environment**\n\n```java org.apache.xalan.xslt.EnvironmentCheck```\n\n**Use XSLTC compiler**\n\n```java org.apache.xalan.xslt.Process -XSLTC -IN [input.xml] -XSL [style.xsl]```"],["SYNOPSIS","**java org.apache.xalan.xslt.Process** [_options_]"],["PARAMETERS","**-IN** _file_\n> Input XML document URL.\n\n**-XSL** _file_\n> XSLT stylesheet URL.\n\n**-OUT** _file_\n> Output file name.\n\n**-PARAM** _name_ _value_\n> Set stylesheet parameter.\n\n**-XML**\n> Output as XML with declaration.\n\n**-HTML**\n> Output as HTML.\n\n**-TEXT**\n> Output as plain text.\n\n**-XSLTC**\n> Use XSLTC compiled transformation.\n\n**-V**\n> Display version.\n\n**-EDUMP** _file_\n> Dump stack trace on error."],["DESCRIPTION","**Xalan** is an XSLT processor for transforming XML documents. It implements XSLT 1.0 and XPath 1.0, transforming XML into HTML, text, or other XML formats based on stylesheet rules.\n\nThe processor is written in Java and requires the JVM with Xalan JARs in the classpath. It supports the JAXP transformation API and can be used as a library or command-line tool.\n\nXSLTC mode compiles stylesheets to Java bytecode for faster repeated transformations. This is useful for server applications processing many documents with the same stylesheet."],["CAVEATS","Requires Java runtime and proper classpath setup. XSLT 1.0 only; for XSLT 2.0+ use Saxon. Large documents may require JVM memory tuning."],["HISTORY","**Xalan** was developed by the Apache Software Foundation as part of the Apache XML project. Named after a type of musical instrument, it became one of the most widely used open-source XSLT processors, particularly in Java environments."],["INSTALL","```apt: sudo apt install xalan```"],["SEE ALSO","[xsltproc](/man/xsltproc)(1), [saxon](/man/saxon)(1), [xmllint](/man/xmllint)(1)"]],"e":["xsltproc","saxon","xmllint"],"x":"java org.apache.xalan.xslt.Process -IN [input.xml] -XSL [style.xsl] -OUT [output.html]"},"xcowsay":{"n":"xcowsay","t":"Graphical desktop cow message display","s":[["TAGLINE","Graphical desktop cow message display"],["TLDR","Display cow with **message**\n\n```xcowsay \"[hello, world]\"```\n\nDisplay with **pipe** input\n\n```ls | xcowsay```\n\nDisplay at **position**\n\n```xcowsay --at [X],[Y]```\n\nChange cow **size**\n\n```xcowsay --cow-size [small|med|large]```\n\nDisplay **thought** bubble\n\n```xcowsay --think```\n\nUse custom **image**\n\n```xcowsay --image [path/to/file]```"],["SYNOPSIS","**xcowsay** [_OPTIONS_] [_MESSAGE_]"],["PARAMETERS","**--at** _X,Y_\n> Display at specified coordinates\n\n**--cow-size** _SIZE_\n> Cow size: small, med, or large\n\n**--think**\n> Use thought bubble instead of speech\n\n**--image** _FILE_\n> Use custom image instead of cow\n\n**--time** _SECONDS_\n> Display duration"],["DESCRIPTION","**xcowsay** displays a cute cow with a message on the Linux desktop. Similar to cowsay but with a graphical display. The cow disappears after a timeout or when clicked.\n\nMessages can be provided as arguments or piped from other commands."],["CAVEATS","X11 only. Display time depends on message length unless specified. Custom images must be PNG format."],["INSTALL","```dnf: sudo dnf install xcowsay```\n\n```zypper: sudo zypper install xcowsay```\n\n```nix: nix profile install nixpkgs#xcowsay```"],["SEE ALSO","[cowsay](/man/cowsay)(1), [notify-send](/man/notify-send)(1)"]],"e":["cowsay","notify-send"],"x":"xcowsay \"[hello, world]\""}});
