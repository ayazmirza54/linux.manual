window.LCL_BUCKET(12,{"apptainer-test":{"n":"apptainer-test","t":"Run validation tests defined in a container","s":[["TAGLINE","Run validation tests defined in a container"],["TLDR","**Run the test script** of a container\n\n```apptainer test [container.sif]```\n\n**Test a Docker Hub image**\n\n```apptainer test docker://[image:tag]```\n\n**Test a specific app** within the container\n\n```apptainer test --app [appname] [container.sif]```\n\n**Test with bound directory**\n\n```apptainer test --bind [/data]:[/mnt] [container.sif]```\n\n**Test with NVIDIA GPU support**\n\n```apptainer test --nv [container.sif]```"],["SYNOPSIS","**apptainer test** [_options_] _container_"],["PARAMETERS","**--app** _name_\n> Run the test section of a specific SCIF app.\n\n**--bind**, **-B** _src[:dest[:opts]]_\n> Bind mount a host path into the container.\n\n**--nv**\n> Enable NVIDIA GPU support.\n\n**--rocm**\n> Enable AMD ROCm GPU support.\n\n**--contain**, **-c**\n> Use minimal /dev and empty other directories.\n\n**--containall**, **-C**\n> Full containment mode.\n\n**--cleanenv**, **-e**\n> Clean environment before testing.\n\n**--fakeroot**\n> Run test with fake root privileges.\n\n**--cwd**, **--pwd** _path_\n> Set working directory inside container.\n\n**--writable**, **-w**\n> Make file system accessible as read-write.\n\n**--writable-tmpfs**\n> Make file system read-write with non-persistent data (tmpfs overlay).\n\n**--help**, **-h**\n> Display help for the test command."],["DESCRIPTION","**apptainer test** executes the test script defined in a container's **%test** section. This provides a standardized way for container authors to include validation checks that verify the container is functioning correctly.\n\nTest scripts typically check that required software is installed, libraries are accessible, and the container can perform its intended function. The exit code of the test script determines pass (0) or fail (non-zero) status.\n\nFor containers using the Scientific Filesystem (SCIF), the **--app** flag runs the test section of a specific application within the container. This allows multi-app containers to have independent test suites.\n\nContainer authors define tests during build time. If no %test section exists, the command completes successfully without executing anything. This command is useful in CI/CD pipelines to validate container builds before deployment."],["CAVEATS","Only runs tests if a %test section was defined during container build. Returns success even if no test exists (silent pass). Test scripts have the same environment constraints as the container runtime. Complex tests may require bind mounts or GPU flags to function properly."],["HISTORY","The test subcommand has been part of Singularity/Apptainer since early versions, providing container authors a standardized way to validate their builds. It supports the reproducibility goals of scientific computing by ensuring containers meet expected functionality. Apptainer preserved this feature when the project forked from Singularity in **2021**."],["INSTALL","```dnf: sudo dnf install apptainer```\n\n```pacman: sudo pacman -S apptainer```\n\n```apk: sudo apk add apptainer```\n\n```zypper: sudo zypper install apptainer```\n\n```brew: brew install apptainer```\n\n```nix: nix profile install nixpkgs#apptainer```"],["SEE ALSO","[apptainer](/man/apptainer)(1), [apptainer-build](/man/apptainer-build)(1), [apptainer-run](/man/apptainer-run)(1), [apptainer-exec](/man/apptainer-exec)(1), [apptainer-inspect](/man/apptainer-inspect)(1)"]],"e":["apptainer","apptainer-build","apptainer-run","apptainer-exec","apptainer-inspect"],"x":"apptainer test [container.sif]"},"arandr":{"n":"arandr","t":"Visual front end for display configuration","s":[["TAGLINE","Visual front end for display configuration"],["TLDR","**Launch the graphical display configuration** tool\n\n```arandr```\n\n**Configure a different display** while showing GUI on current display\n\n```arandr --randr-display [DISPLAY]```"],["SYNOPSIS","**arandr** [**--randr-display=**_DISPLAY_]"],["PARAMETERS","**--randr-display=**_DISPLAY_\n> Use a specific X display for xrandr operations while showing the GUI on the display from the environment\n\n**-h**, **--help**\n> Display help message\n\n**--version**\n> Display version information"],["DESCRIPTION","**ARandR** (Another RandR) is a graphical front end for the XRandR 1.2 extension. It provides a visual interface for configuring display outputs, resolutions, refresh rates, and multi-monitor layouts.\n\nThe application shows connected monitors as draggable rectangles, allowing intuitive arrangement of display positions. Changes take effect immediately, and configurations can be saved as executable shell scripts for later use or automatic execution at login.\n\nKey features include drag-and-drop monitor positioning with edge snapping, support for rotation, resolution selection, and enabling/disabling outputs. Saved scripts are plain text and can be edited to include additional commands.\n\nFor command-line control of display settings, use **xrandr** directly. ARandR generates xrandr commands internally and is useful for visualizing and experimenting with multi-monitor setups."],["CAVEATS","Requires a running X server with XRandR 1.2 or later support. Does not work with Wayland compositors; use their native display configuration tools instead. Saved configurations only persist if the script is executed at login (via ~/.xprofile, ~/.xinitrc, or session autostart)."],["HISTORY","**ARandR** was created by **Christian Aichinger** as an alternative to GNOME and KDE display configuration tools for users of lightweight window managers. The project aims to provide a simple, desktop-environment-independent way to configure displays visually while remaining portable and minimal."],["INSTALL","```dnf: sudo dnf install arandr```\n\n```pacman: sudo pacman -S arandr```\n\n```apk: sudo apk add arandr```\n\n```zypper: sudo zypper install arandr```\n\n```nix: nix profile install nixpkgs#arandr```"],["SEE ALSO","[xrandr](/man/xrandr)(1)"]],"e":["xrandr"],"x":"arandr"},"asp":{"n":"asp","t":"Retrieve Arch Linux package build files","s":[["TAGLINE","Retrieve Arch Linux package build files"],["TLDR","**Clone a package from the Arch repositories**\n\n```asp checkout [package]```\n\n**Update all checked out packages**\n\n```asp update```\n\n**Export PKGBUILD files without full checkout**\n\n```asp export [package]```\n\n**List repositories** containing a package\n\n```asp list-repos [package]```\n\n**Show** PKGBUILD content for a package\n\n```asp show [package]```\n\n**View revision history** for a package\n\n```asp log [package]```\n\n**List all locally tracked** packages\n\n```asp list-local```"],["SYNOPSIS","**asp** _command_ [_options_] [_packages_]"],["PARAMETERS","**checkout** _package_\n> Create a git repository with full source and history for the given package.\n\n**export** _package_\n> Dump build source files into a directory named after the package in the current directory.\n\n**update** [_packages_]\n> Track new packages or refresh existing ones from remote.\n\n**show** _package_ [_file_]\n> Display PKGBUILD content, or a specific file if given.\n\n**log** _package_\n> Show revision history for a package.\n\n**difflog** _package_\n> Show revision history with file diffs.\n\n**shortlog** _package_\n> Show condensed revision history.\n\n**list-repos** _package_\n> List repositories containing the given package.\n\n**list-arches** _package_\n> List architectures available for the given package.\n\n**list-all**\n> List all available packages.\n\n**list-local**\n> List all locally tracked packages.\n\n**ls-files** _package_\n> List source files for the given package.\n\n**untrack** _package_\n> Remove remote tracking branch from local repository.\n\n**disk-usage**\n> Report approximate disk usage for locally tracked packages.\n\n**gc**\n> Perform housekeeping to optimize and compact the local repo.\n\n**set-git-protocol** _protocol_\n> Set communication protocol (git, http, or https).\n\n**-a** _arch_\n> Use a non-default architecture.\n\n**-h**\n> Display help text.\n\n**-V**\n> Show version information."],["DESCRIPTION","**asp** (Arch Build Source Management Tool) manages version-controlled sources for the build scripts used to create Arch Linux packages. It provides a thin wrapper over the svntogit repositories and serves as a replacement for the older **abs** tool.\n\nUnlike downloading PKGBUILDs manually, asp uses a Git-based backend with sparse checkouts, enabling tracking changes, viewing commit history, and updating individual packages efficiently.\n\nCommon uses include: examining how official packages are built, modifying packages with custom options, backporting patches, and learning PKGBUILD best practices from official examples."],["CAVEATS","asp only provides build files from official Arch repositories, not the AUR. The checked-out files require **makepkg** to actually build packages. Requires Git to be installed for the underlying VCS operations. Package metadata is stored in **$ASPROOT** (defaults to **$XDG_CACHE_HOME/asp** or **~/.cache/asp**)."],["HISTORY","The **asp** tool was created to replace the older **abs** (Arch Build System) tool, which downloaded a complete copy of all PKGBUILDs. Introduced around **2015**, asp uses Git to provide more efficient, incremental access to individual packages. It became the recommended method for obtaining official PKGBUILDs as the Arch package repositories moved to Git-based workflows."],["INSTALL","```aur: yay -S asp```"],["SEE ALSO","[makepkg](/man/makepkg)(8), [pacman](/man/pacman)(8), [yay](/man/yay)(1), [paru](/man/paru)(1), [pkgctl](/man/pkgctl)(1), [git](/man/git)(1)"]],"e":["makepkg","pacman","yay","paru","pkgctl","git"],"x":"asp checkout [package]"},"aws-sns":{"n":"aws-sns","t":"Manage Simple Notification Service topics and subscriptions.","s":[["TAGLINE","Manage Simple Notification Service topics and subscriptions."],["TLDR","**Create a new SNS topic**\n\n```aws sns create-topic --name [topic-name]```\n\n**List all topics**\n\n```aws sns list-topics```\n\n**Publish a message** to a topic\n\n```aws sns publish --topic-arn [arn:aws:sns:region:account:topic-name] --message \"[Hello World]\"```\n\n**Subscribe an email endpoint** to a topic\n\n```aws sns subscribe --topic-arn [arn:aws:sns:region:account:topic-name] --protocol email --notification-endpoint [user@example.com]```\n\n**Subscribe a Lambda function** to a topic\n\n```aws sns subscribe --topic-arn [arn:aws:sns:region:account:topic-name] --protocol lambda --notification-endpoint [arn:aws:lambda:region:account:function:name]```\n\n**Unsubscribe from a topic**\n\n```aws sns unsubscribe --subscription-arn [arn:aws:sns:region:account:topic-name:subscription-id]```\n\n**List subscriptions** for a topic\n\n```aws sns list-subscriptions-by-topic --topic-arn [arn:aws:sns:region:account:topic-name]```\n\n**Delete a topic**\n\n```aws sns delete-topic --topic-arn [arn:aws:sns:region:account:topic-name]```"],["SYNOPSIS","**aws sns** _subcommand_ [_options_]"],["DESCRIPTION","**aws sns** is the AWS CLI interface for Amazon Simple Notification Service (SNS), a fully managed messaging service for application-to-application (A2A) and application-to-person (A2P) communication.\n\nSNS enables pub/sub messaging where publishers send messages to topics, and subscribers receive notifications via protocols including HTTP/HTTPS, email, SMS, SQS, Lambda, and mobile push notifications."],["SUBCOMMANDS","**Topic Management**\n> create-topic, delete-topic, list-topics, get-topic-attributes, set-topic-attributes\n\n**Publishing**\n> publish, publish-batch\n\n**Subscriptions**\n> subscribe, unsubscribe, list-subscriptions, list-subscriptions-by-topic, confirm-subscription, get-subscription-attributes, set-subscription-attributes\n\n**Mobile Push**\n> create-platform-application, create-platform-endpoint, delete-platform-application, delete-endpoint, list-platform-applications, list-endpoints-by-platform-application\n\n**SMS**\n> set-sms-attributes, get-sms-attributes, check-if-phone-number-is-opted-out, opt-in-phone-number, list-phone-numbers-opted-out\n\n**Permissions**\n> add-permission, remove-permission, tag-resource, untag-resource, list-tags-for-resource"],["CAVEATS","Email subscriptions require confirmation by the recipient before they receive messages. SMS messages have regional restrictions and may require spending limit increases. Message size is limited to 256KB for standard topics. FIFO topics require compatible SQS FIFO queue subscribers."],["HISTORY","Amazon SNS launched in **April 2010** as a push notification service. It has evolved from simple pub/sub messaging to support mobile push notifications, SMS, and advanced features like message filtering, dead-letter queues, and FIFO topics."],["INSTALL","```dnf: sudo dnf install aws```\n\n```pacman: sudo pacman -S aws-cli```\n\n```apk: sudo apk add aws-cli```\n\n```zypper: sudo zypper install aws-cli```"],["SEE ALSO","[aws](/man/aws)(1), [aws-sqs](/man/aws-sqs)(1), [aws-ses](/man/aws-ses)(1)"]],"e":["aws","aws-sqs","aws-ses"],"x":"aws sns create-topic --name [topic-name]"},"beeline":{"n":"beeline","t":"JDBC command-line client for Apache Hive","s":[["TAGLINE","JDBC command-line client for Apache Hive"],["TLDR","**Connect to HiveServer2**\n\n```beeline -u jdbc:hive2://[localhost]:10000/[default]```\n\n**Connect with username and password**\n\n```beeline -u jdbc:hive2://[localhost]:10000/[default] -n [username] -p [password]```\n\n**Execute a query** directly\n\n```beeline -u jdbc:hive2://[localhost]:10000 -e \"[SELECT * FROM table_name]\"```\n\n**Execute queries from a file**\n\n```beeline -u jdbc:hive2://[localhost]:10000 -f [path/to/script.sql]```\n\n**Connect with output in CSV format**\n\n```beeline -u jdbc:hive2://[localhost]:10000 --outputformat=csv2```\n\n**Reconnect** to previously used URL\n\n```beeline -r```"],["SYNOPSIS","**beeline** [_OPTIONS_] [_QUERY_]"],["PARAMETERS","**-u** _URL_\n> JDBC connection URL (e.g., jdbc:hive2://localhost:10000/default).\n\n**-n** _USERNAME_\n> Username for authentication.\n\n**-p** _PASSWORD_\n> Password for authentication.\n\n**-w** _FILE_\n> Path to file containing password.\n\n**-d** _DRIVER_\n> JDBC driver class to use.\n\n**-e** _QUERY_\n> Execute query string. Multiple statements can be separated by semicolons.\n\n**-f** _FILE_\n> Execute SQL script from file.\n\n**-i**, **--init** _FILE_\n> Run initialization file before executing commands.\n\n**-r**\n> Reconnect to previously used URL (since Hive 2.1.0).\n\n**--outputformat** _FORMAT_\n> Output format: table, vertical, csv, tsv, dsv, csv2, tsv2, json, jsonfile.\n\n**--showHeader**\n> Display column names in output (default: true).\n\n**--silent**\n> Suppress informational messages and query logs.\n\n**--verbose**\n> Show debug information and error details.\n\n**--hiveconf** _KEY_=_VALUE_\n> Set Hive configuration property.\n\n**--hivevar** _KEY_=_VALUE_\n> Define Hive session variable.\n\n**--force**\n> Continue script execution despite errors.\n\n**--incremental**\n> Display rows immediately as fetched.\n\n**--help**\n> Display usage message."],["DESCRIPTION","**Beeline** is a JDBC command-line client for HiveServer2, based on SQLLine CLI. It provides an interface to execute Hive queries against a HiveServer2 instance, supporting both embedded mode (running an embedded Hive) and remote mode (connecting over Thrift).\n\nRemote mode is recommended for production use as it is more secure and does not require direct HDFS or metastore access. Beeline connects to HiveServer2 which runs on port 10000 by default. The client supports various authentication methods including Kerberos, LDAP, and custom authentication.\n\nConnection URLs can include session variables, Hive configuration parameters, and connection properties. Beeline supports ZooKeeper-based service discovery, SSL/TLS connections, and HTTP transport mode for firewall-friendly deployments."],["CAVEATS","Beeline replaces the deprecated Hive CLI. Some older CLI-specific commands may behave differently. Connection URLs must be properly formatted with correct escaping of special characters. When using Kerberos authentication, proper keytab and principal configuration is required."],["HISTORY","Beeline was introduced as the recommended HiveServer2 client to replace the original Hive CLI. It is based on SQLLine, an open-source JDBC command-line tool. The Hive CLI was deprecated because it required direct access to Hadoop and Hive libraries, while Beeline provides a thin client model that connects through JDBC, improving security and deployment flexibility."],["SEE ALSO","[hive](/man/hive)(1)"]],"e":["hive"],"x":"beeline -u jdbc:hive2://[localhost]:10000/[default]"},"benben":{"n":"benben","t":"Fast command-line audio player and converter with VGM chip emulation","s":[["TAGLINE","Fast command-line audio player and converter with VGM chip emulation"],["TLDR","**Play a single audio file**\n\n```benben [path/to/song.mp3]```\n\n**Play all files in a directory**\n\n```benben [path/to/album/]```\n\n**Play a VGM file** with looping\n\n```benben --loop [2] [path/to/song.vgz]```\n\n**Render files to WAV** in parallel\n\n```benben --render [*.ogg] [*.flac]```\n\n**Render to a specific output directory** with normalization\n\n```benben --render --normalize --outdir [path/to/output] [*.vgm]```\n\n**Play with a specific audio driver** and sample rate\n\n```benben --driver [alsa] --sample-rate [48000] [path/to/file]```\n\n**Play a SID file** selecting specific songs\n\n```benben [path/to/file.sid@2,4,7]```\n\n**Shuffle and repeat** a directory\n\n```benben --shuffle --repeat [path/to/music/]```"],["SYNOPSIS","**benben** [_options_] [_files_|_directories_|_playlists_]"],["PARAMETERS","**-h**, **--help**\n> Display help information and exit\n\n**-v**, **--version**\n> Show brief version details and exit\n\n**--long-version**\n> Display verbose version information and exit\n\n**--scan-only**\n> Scan files only, then terminate\n\n**--recurse**\n> Search subdirectories recursively\n\n**--no-recurse**\n> Disable recursive subdirectory searching\n\n**--dump-config**\n> Generate a fresh configuration and output to stdout\n\n**--basedir** _path_\n> Load configuration and themes from an alternate directory\n\n**--remote**\n> Enable UNIX domain socket for remote control\n\n**--remote-socket** _path_\n> Specify alternate socket location for remote control\n\n**--list-themes**\n> Display all available themes\n\n**-T** _name_, **--theme** _name_\n> Use the specified theme\n\n**--ui** _type_\n> Select user interface: original or minimal\n\n**-L**, **--no-listenbrainz**\n> Disable ListenBrainz submission\n\n**-d** _driver_, **--driver** _driver_\n> Select audio driver (alsa, out123, ao, any)\n\n**--alsa-device** _name_\n> Use a specific ALSA device (default: default)\n\n**--volume** _x_\n> Set output volume from 0.0 to 3.0 (default: 1.0)\n\n**-S** _hz_, **--sample-rate** _hz_\n> Set output sample rate in Hz from 8000 to 48000 (default: 44100)\n\n**--replay-gain** _mode_\n> Set ReplayGain mode: disabled, mix, or album\n\n**--resampler** _mode_\n> Set resampler mode\n\n**-e**, **--no-eq**\n> Start with equalizer disabled\n\n**-c**, **--soft-clipping**\n> Start with soft clipping enabled\n\n**-C**, **--no-soft-clipping**\n> Start with soft clipping disabled\n\n**--soft-clipping-oversampling** _n_\n> Oversampling amount when soft clipping is enabled (default: 1)\n\n**-t**, **--stereo-enhancer**\n> Enable stereo enhancer effect\n\n**-E** _amount_, **--stereo-enhancement** _amount_\n> Set stereo enhancement amount from 0.0 to 1.5 (default: 0.5)\n\n**--dc-filter**\n> Enable DC offset filter\n\n**--no-dc-filter**\n> Disable DC offset filter\n\n**-r**, **--reverb**\n> Enable reverb effect\n\n**-R**, **--no-reverb**\n> Disable reverb effect\n\n**--reverb-type** _type_\n> Set reverb unit type: mverb or zita\n\n**--reverb-preset** _name_\n> Use an alternate reverb preset (default: gm-default)\n\n**--reverb-amount** _x_\n> Set reverb amount from 0.0 to 1.0 (default: 0.5)\n\n**-l** _n_, **--loop** _n_\n> Loop count; 0 for infinite looping (default: 1)\n\n**-p**, **--repeat**\n> Loop entire queue when finished\n\n**-P**, **--no-repeat**\n> Disable queue looping\n\n**--shuffle**\n> Randomize playback queue\n\n**--start-paused**\n> Begin playback in a paused state\n\n**--fadeout-seconds** _n_\n> Fade-out duration in seconds (default: 5)\n\n**--vgm-chip-info**\n> Display supported VGM chip emulators and exit\n\n**--vgm-strict-gd3-loading**\n> Be strict with GD3 tags during VGM loading\n\n**--vgm-ym2612-pseudo-stereo**\n> Enable pseudo-stereo for YM2612 emulator\n\n**--sid-mono**\n> Force 2SID/3SID playback as monaural\n\n**--sid-def-length** _seconds_\n> Default SID song length in seconds (default: 180)\n\n**--midi-soundfont** _path_\n> Specify SoundFont file for MIDI playback\n\n**-n**, **--render**\n> Render files to WAV or Au format instead of playing\n\n**-Q**, **--quiet**\n> Suppress non-error messages during rendering\n\n**-N**, **--normalize**\n> Normalize rendered files to 0 dBFS peak\n\n**-A**, **--au**\n> Render to Au format instead of WAV\n\n**-F**, **--float**\n> Output IEEE floating-point data when rendering\n\n**-b** _depth_, **--bit-depth** _depth_\n> Set bit depth: 8, 16, 24, 32, or 64 (default: 16 for PCM, 32 for float)\n\n**-o** _dir_, **--outdir** _dir_\n> Output directory for rendered files\n\n**--overwrite**\n> Overwrite existing rendered files\n\n**--jobs** _n_\n> Number of parallel rendering jobs (default: CPU core count)\n\n**--tcp-driver-host** _host_\n> TCP host to connect to (default: localhost)\n\n**--tcp-driver-port** _port_\n> TCP port to connect to (default: 6969)\n\n**--tcp-driver-format** _format_\n> Audio format for TCP transmission (default: float32)"],["DESCRIPTION","**benben** is a fast and efficient command-line audio player and audio converter for Linux and other Unix-like systems with an oldschool-inspired terminal interface. It is written almost entirely in Common Lisp and is designed for users who organize their music in folders and prefer terminal-based tools over graphical interfaces.\n\nbenben supports a wide range of audio formats including MP3, Ogg Vorbis, Opus, FLAC, WavPack, WAV, QOA, Sun Au, General MIDI, MUS, Commodore 64 SID, Doom WAD files, and all module/tracker formats supported by libxmp. It has extensive VGM (Video Game Music) format support with chip emulation for over 30 sound chips including YM2612, YM2151, YM2610, NES APU, HuC6280, QSound, and Sega MultiPCM, powered by the SatouSynth library.\n\nThe player provides multiple audio output backends (ALSA, libout123, libao) and supports TCP audio streaming. Built-in audio effects include soft clipping with configurable oversampling, a parametric equalizer with arbitrary band count, stereo enhancement, reverb (mverb and zita types with presets), and a DC offset filter. It supports ReplayGain/RVA for volume normalization.\n\nThe rendering mode can convert audio files to WAV or Au format in parallel using multiple CPU cores, with options for normalization, configurable bit depth, and floating-point output. XSPF and JSPF playlists are supported, along with ListenBrainz scrobbling and Matrix protocol integration for now-playing updates.\n\nThe multi-threaded UI is built on S-Lang and features customizable themes, banner animations, a VU meter, and full keyboard controls. A minimal headless UI is also available for resource-constrained systems or remote control via the companion **remote-benben** tool over UNIX domain sockets. SID files support song selection syntax using the @ notation (e.g., file.sid@1-3).\n\nbenben is command-line only with no file browser by design; users specify files, directories, or playlists as arguments."],["CONFIGURATION","Configuration is stored in **$XDG_CONFIG_HOME/benben/benben.yaml** (typically ~/.config/benben/benben.yaml) in YAML format. Command-line options override configuration file settings.\n\nKey configuration sections include audio driver and buffer settings, playback defaults (volume, sample rate, loop count, fade-out), audio effects (equalizer bands, soft clipping, stereo enhancer, reverb type and presets, DC filter), VGM emulator preferences (chip cores, GD3 language, pseudo-stereo), MIDI settings (soundfont path, reverb, chorus, voice filter type), C64/SID settings (ROM paths, chip model, songlength database), UI customization (theme, banner style and animation, display preferences), and integration tokens for ListenBrainz and Matrix.\n\nSong-specific configuration files can be placed in **$XDG_CONFIG_HOME/benben/song-configs/** as **song-config-*.yaml** files, each containing a **match** key with glob patterns to apply settings to particular files.\n\nThemes are stored in **$XDG_CONFIG_HOME/benben/themes/** as **theme-\\<name\\>.yaml** files and support 8-bit ANSI, 24-bit RGB, and hex color notation."],["CAVEATS","The configuration file format is considered unstable and may change until version 1.0. MIDI playback requires a SoundFont file to be specified. SID song length accuracy depends on having the HVSC songlength database configured; without it, songs default to 180 seconds. The sample rate is limited to a range of 8000 to 48000 Hz. benben has no built-in file browser and requires explicit file or directory arguments. For 24-bit terminal color support, the COLORTERM environment variable may need to be set to truecolor."],["HISTORY","**benben** was created by **Remilia Scarlet** as a personal project, originally written in the **Crystal** programming language. Development began around **2023**, with the v0.4.0 release announced on **March 7, 2024** at VGMRips. The VGM chip emulation was built on the author's **YunoSynth** library, an OOP rewrite of VGMPlay's sound emulator code. In **January 2025**, work began on porting benben from Crystal to **Common Lisp**, with the Lisp-based **v0.7.0** becoming the author's daily music player by **April 2025**. The VGM library was rewritten as **SatouSynth** for the Lisp version. Version **0.7.1** was released on **November 6, 2025**. The project is licensed under the **GNU Affero General Public License v3** and hosted in a Fossil repository."],["INSTALL","```aur: yay -S benben```"],["SEE ALSO","[mpv](/man/mpv)(1), [ffmpeg](/man/ffmpeg)(1), [sox](/man/sox)(1), [aplay](/man/aplay)(1), [cmus](/man/cmus)(1), [moc](/man/moc)(1)"]],"e":["mpv","ffmpeg","sox","aplay","cmus","moc"],"x":"benben [path/to/song.mp3]"},"bkp":{"n":"bkp","t":"simple utility for creating backups of files and directories","s":[["TAGLINE","simple utility for creating backups of files and directories"],["TLDR","Create a **backup** of a file\n\n```bkp [path/to/file]```\n\nCreate a **backup** of a directory\n\n```bkp [path/to/directory]```\n\nCreate a **TAR archive** backup with a message\n\n```bkp -a -m \"[initial version]\" [path/to/file]```\n\n**Restore** a file from a backup\n\n```bkp -r [path/to/file.b01]```\n\nRestore a backup and **delete** the backup file afterwards\n\n```bkp -r -d [path/to/file.b01]```\n\nView **metadata** of an archive backup\n\n```bkp -i [path/to/file.b01]```\n\nCreate a backup and **delete** the original\n\n```bkp -d [path/to/file]```\n\nCreate a backup, answering **yes** to all prompts\n\n```bkp -y [path/to/file]```"],["SYNOPSIS","**bkp** [_options_] [_paths_...]"],["PARAMETERS","**-r**, **--restore**\n> Restore resources from backup(s).\n\n**-d**, **--delete**\n> Delete the source file or directory after the operation.\n\n**-a**, **--archive**\n> Create a TAR archive instead of a simple copy.\n\n**-y**, **--yes**\n> Answer yes to all confirmation prompts.\n\n**-m** _TEXT_, **--message** _TEXT_\n> Message to be included in the archive metadata. Requires **-a**.\n\n**-M**, **--message-edit**\n> Open a text editor to compose the message. Requires **-a**.\n\n**-i**, **--info**\n> Read and display metadata from an archive backup.\n\n**--version**\n> Print version information.\n\n**--help**\n> Show usage information and exit."],["DESCRIPTION","**bkp** is a Python-based command-line utility that creates sequentially numbered backups of files and directories. Running **bkp** on a file **foo** produces **foo.b01**, and subsequent invocations produce **foo.b02**, **foo.b03**, and so on. Backup files are always created in the same directory as the source.\n\nTwo backup modes are supported. By default, **bkp** creates a simple file copy (or directory tree copy). With the **-a** flag, it produces a TAR archive that can embed metadata including the author (current user), timestamp, and an optional commit message.\n\nThe **-r** flag restores a backup to its original name by stripping the **.bNN** suffix. If the original file already exists, the user is prompted for confirmation unless **-y** is specified. The **-d** flag can be combined with either backup or restore to remove the source after the operation completes.\n\nArchive metadata can be inspected with **-i**, which displays the author, creation time, and any message stored in the archive.\n\nMultiple paths can be specified in a single invocation, and each will be processed in sequence."],["CONFIGURATION","**EDITOR**\n> Environment variable used to determine which text editor to open when the **-M** flag is given. Defaults to **vi** if not set."],["CAVEATS","The **-m** and **-M** options are only valid in combination with **-a**. Using them without **-a** produces an error.\n\nBackup numbering is determined by scanning existing files in the same directory. If backup files are manually renamed or removed, numbering may produce unexpected results.\n\nThe tool is primarily tested on Linux. Behavior on other platforms may vary.\n\nThe author disclaims responsibility for any data loss resulting from use of this utility."],["SEE ALSO","[cp](/man/cp)(1), [tar](/man/tar)(1), [rsync](/man/rsync)(1), [mv](/man/mv)(1)"]],"e":["cp","tar","rsync","mv"],"x":"bkp [path/to/file]"},"bsd-from":{"n":"bsd-from","t":"Display mail sender information from a mailbox","s":[["TAGLINE","Display mail sender information from a mailbox"],["TLDR","**Show who has sent mail to your mailbox**\n\n```bsd-from```\n\n**Show only a count of messages**\n\n```bsd-from -c```\n\n**Show mail from a specific sender**\n\n```bsd-from -s [sender]```\n\n**Examine a different mailbox file**\n\n```bsd-from -f [path/to/mailbox]```\n\n**Check another user's mailbox**\n\n```bsd-from [username]```"],["SYNOPSIS","**bsd-from** [**-c**] [**-f** _file_] [**-s** _sender_] [_user_]"],["DESCRIPTION","**bsd-from** (also known as **from**) prints the mail header lines from a mailbox, showing the names of people who have sent mail. By default, it examines the invoker's mailbox.\n\nThe command provides a quick way to see pending mail without opening a full mail client. It displays the \"From\" lines of messages, giving an overview of who has sent mail."],["PARAMETERS","**-c**\n> Print only a count of messages and exit\n\n**-f** _file_\n> Examine the specified file instead of the default mailbox. Use **-** to read from standard input\n\n**-s** _sender_\n> Only print mail from addresses containing the specified string\n\n**user**\n> Examine the specified user's mailbox instead of the invoker's (requires appropriate privileges)"],["CAVEATS","When using the **-f** option, the user argument should not be specified. Examining other users' mailboxes requires elevated privileges.\n\nThe default mailbox location is determined by the **MAIL** environment variable, or **/var/mail** if not set."],["HISTORY","The **from** command first appeared in **BSD 3.0** in 1979 as part of the Berkeley Software Distribution mail utilities."],["SEE ALSO","[biff](/man/biff)(1), [mail](/man/mail)(1)"],["RESOURCES","```[Source code](https://cgit.freebsd.org/src/tree/usr.bin/from)```"]],"e":["biff","mail"],"x":"bsd-from"},"conda-activate":{"n":"conda-activate","t":"activate a conda environment","s":[["TAGLINE","activate a conda environment"],["TLDR","**Activate an environment**\n\n```conda activate [env_name]```\n\n**Activate environment** at specific path\n\n```conda activate [/path/to/env]```\n\n**Activate the base environment**\n\n```conda activate base```\n\n**Activate** an environment and verify it is active\n\n```conda activate [env_name] && conda info --envs```"],["SYNOPSIS","**conda** **activate** [_env_name_|_path_]"],["DESCRIPTION","**conda activate** switches the current shell session to use a specified conda environment, modifying the PATH environment variable and other shell variables to prioritize that environment's binaries, libraries, and packages. This is the primary mechanism for working with isolated conda environments.\n\nWhen an environment is activated, the shell prompt is typically modified to show the active environment name in parentheses, providing a visual indicator of which environment is currently in use. The activation process prepends the environment's bin directory to PATH, sets environment-specific variables like CONDA_PREFIX and CONDA_DEFAULT_ENV, and may execute activation scripts included with certain packages.\n\nConda environments can be activated by name (if stored in the default envs directory) or by full path to the environment directory. The base environment is conda's root environment and is activated by default unless auto_activate_base is disabled in conda configuration. Shell integration must be initialized via conda init for activation to work properly in bash, zsh, fish, or PowerShell."],["CAVEATS","Requires conda init to have been run for shell integration. Some shells may need sourcing the activation script manually."],["INSTALL","```dnf: sudo dnf install conda```\n\n```nix: nix profile install nixpkgs#conda```"],["SEE ALSO","[conda](/man/conda)(1), [conda-deactivate](/man/conda-deactivate)(1), [conda-create](/man/conda-create)(1), [conda-env](/man/conda-env)(1), [conda-init](/man/conda-init)(1)"]],"e":["conda","conda-deactivate","conda-create","conda-env","conda-init"],"x":"conda activate [env_name]"},"debtap":{"n":"debtap","t":"convert Debian packages to Arch Linux packages","s":[["TAGLINE","convert Debian packages to Arch Linux packages"],["TLDR","**Update** debtap database (required first)\n\n```sudo debtap -u```\n\n**Convert** a Debian package\n\n```debtap [path/to/package.deb]```\n\nConvert with **minimal prompts**\n\n```debtap -q [path/to/package.deb]```\n\nGenerate **PKGBUILD** file only\n\n```debtap -p [path/to/package.deb]```"],["SYNOPSIS","**debtap** [_options_] _package.deb_"],["DESCRIPTION","**debtap** converts Debian packages (.deb) to Arch Linux packages. It translates package metadata, dependencies, and file paths to create compatible Arch packages.\n\nThe database must be updated before first use to fetch dependency mappings."],["PARAMETERS","**-u, --update**\n> Update debtap database\n\n**-q, --quiet**\n> Bypass questions except metadata editing\n\n**-Q, --Quiet**\n> Bypass all questions\n\n**-p, --pkgbuild**\n> Generate PKGBUILD file instead of package\n\n**-P, --pseudo**\n> Create pseudo-64-bit package"],["CAVEATS","Arch Linux specific. Converted packages may have dependency issues as package names differ between distributions. Review and test converted packages before production use. Database update requires internet access."],["INSTALL","```aur: yay -S debtap```"],["SEE ALSO","[pacman](/man/pacman)(8), [makepkg](/man/makepkg)(8), [dpkg](/man/dpkg)(1)"],["RESOURCES","```[Source code](https://github.com/helixarch/debtap)```"]],"e":["pacman","makepkg","dpkg"],"x":"sudo debtap -u"},"desktop-file-validate":{"n":"desktop-file-validate","t":"validate desktop entry files against specification","s":[["TAGLINE","validate desktop entry files against specification"],["TLDR","**Validate a desktop entry file**\n\n```desktop-file-validate [path/to/file.desktop]```\n\n**Validate multiple files**\n\n```desktop-file-validate [file1.desktop] [file2.desktop]```\n\n**Validate without deprecation warnings**\n\n```desktop-file-validate --no-warn-deprecated [path/to/file.desktop]```\n\n**Validate and warn about KDE-specific extensions**\n\n```desktop-file-validate --warn-kde [path/to/file.desktop]```\n\n**Validate all desktop files** in a directory\n\n```desktop-file-validate /usr/share/applications/*.desktop```"],["SYNOPSIS","**desktop-file-validate** [**--no-warn-deprecated**] [**--warn-kde**] _FILE_..."],["PARAMETERS","**--no-warn-deprecated**\n> Do not warn about usage of deprecated items from previous specification versions.\n\n**--warn-kde**\n> Warn about KDE-specific extensions including KDE Desktop Entry group, ServiceTypes, DocPath, Keywords, InitialPreference keys, and Service/ServiceType/FSDevice types."],["DESCRIPTION","**desktop-file-validate** validates desktop entry files (.desktop files) according to the freedesktop.org Desktop Entry Specification. Desktop entry files provide information such as application name, icon, description, and categories for application launchers and menus.\n\nThe validation checks for mandatory keys, correct value types, proper escaping, valid categories, and common errors that could cause improper display or misbehavior. The tool is commonly used in packaging workflows and build systems to ensure desktop entries meet specification requirements.\n\nThe command returns exit status 0 if all files are valid with no errors or strict warnings. A non-zero exit status (typically 1) indicates validation failures."],["CAVEATS","The tool validates against the freedesktop.org specification, which may differ from desktop environment-specific requirements. Some desktop environments accept non-standard extensions that this tool will flag as warnings. The --warn-kde option helps identify KDE-specific extensions when targeting cross-desktop compatibility."],["HISTORY","desktop-file-validate is part of the **desktop-file-utils** package maintained by freedesktop.org. The Desktop Entry Specification was created to provide a standard format for application metadata across Linux desktop environments. The specification has evolved through multiple versions, with desktop-file-validate tracking these changes and providing deprecation warnings for outdated keys."],["INSTALL","```apt: sudo apt install desktop-file-utils```\n\n```dnf: sudo dnf install desktop-file-utils```\n\n```pacman: sudo pacman -S desktop-file-utils```\n\n```apk: sudo apk add desktop-file-utils```\n\n```zypper: sudo zypper install desktop-file-utils```\n\n```brew: brew install desktop-file-utils```\n\n```nix: nix profile install nixpkgs#desktop-file-utils```"],["SEE ALSO","[update-desktop-database](/man/update-desktop-database)(1), [xdg-desktop-menu](/man/xdg-desktop-menu)(1)"]],"e":["update-desktop-database","xdg-desktop-menu"],"x":"desktop-file-validate [path/to/file.desktop]"},"docker-update":{"n":"docker-update","t":"dynamically update container resource limits","s":[["TAGLINE","dynamically update container resource limits"],["TLDR","**Update CPU limit**\n\n```docker update --cpus [2] [container]```\n\n**Update memory limit**\n\n```docker update --memory [512m] [container]```\n\n**Update restart policy**\n\n```docker update --restart [always] [container]```\n\n**Update multiple containers**\n\n```docker update --memory [1g] [container1] [container2]```\n\n**Remove memory limit**\n\n```docker update --memory [0] [container]```"],["SYNOPSIS","**docker** **update** [_options_] _container_ [_container..._]"],["PARAMETERS","**--cpus** _decimal_\n> Number of CPUs.\n\n**--memory**, **-m** _bytes_\n> Memory limit.\n\n**--memory-swap** _bytes_\n> Swap limit (-1 for unlimited).\n\n**--restart** _string_\n> Restart policy (no, on-failure, always, unless-stopped).\n\n**--cpu-shares** _int_\n> CPU shares (relative weight).\n\n**--memory-reservation** _bytes_\n> Memory soft limit.\n\n**--cpuset-cpus** _string_\n> CPUs in which to allow execution (e.g., 0-3, 0,1).\n\n**--pids-limit** _int_\n> Tune container pids limit (-1 for unlimited).\n\n**--blkio-weight** _uint16_\n> Block IO relative weight (10-1000), 0 to disable.\n\n**--cpu-period** _int_\n> Limit CPU CFS period (microseconds).\n\n**--cpu-quota** _int_\n> Limit CPU CFS quota (microseconds)."],["DESCRIPTION","**docker update** dynamically updates container resource constraints. Allows changing CPU, memory, and restart settings without recreating the container. Can update running or stopped containers. Not supported for Windows containers."],["CAVEATS","The **--kernel-memory** option has been deprecated since Docker 20.10. Memory changes on running containers may require a restart to take full effect on some systems."],["INSTALL","```apt: sudo apt install docker-cli```\n\n```dnf: sudo dnf install docker-cli```\n\n```pacman: sudo pacman -S docker```\n\n```apk: sudo apk add docker-cli```\n\n```zypper: sudo zypper install docker```\n\n```brew: brew install docker```\n\n```nix: nix profile install nixpkgs#docker```"],["SEE ALSO","[docker](/man/docker)(1), [docker-container-update](/man/docker-container-update)(1), [docker-run](/man/docker-run)(1), [docker-inspect](/man/docker-inspect)(1)"]],"e":["docker","docker-container-update","docker-run","docker-inspect"],"x":"docker update --cpus [2] [container]"},"dotnet-run":{"n":"dotnet-run","t":"build and execute .NET projects","s":[["TAGLINE","build and execute .NET projects"],["TLDR","**Run current project**\n\n```dotnet run```\n\n**Run with arguments**\n\n```dotnet run -- [arg1] [arg2]```\n\n**Run specific project**\n\n```dotnet run --project [path/to/project.csproj]```\n\n**Run in Release** configuration\n\n```dotnet run -c Release```\n\n**Run without building**\n\n```dotnet run --no-build```\n\n**Run specific framework**\n\n```dotnet run -f [net8.0]```"],["SYNOPSIS","**dotnet run** [_options_] [**--** _args_]"],["PARAMETERS","**--project** _PATH_\n> Project to run.\n\n**-c**, **--configuration** _CONFIG_\n> Build configuration.\n\n**-f**, **--framework** _FRAMEWORK_\n> Target framework.\n\n**--no-build**\n> Run without building first.\n\n**--no-restore**\n> Skip package restore.\n\n**--launch-profile** _NAME_\n> Launch profile to use from launchSettings.json.\n\n**--no-launch-profile**\n> Do not use any launch profile.\n\n**--arch** _ARCH_\n> Target architecture (e.g., x86, x64, arm64).\n\n**--** _ARGS_\n> Arguments to pass to application.\n\n**--help**\n> Display help information."],["CONFIGURATION","**Properties/launchSettings.json**\n> Configures launch profiles with environment variables and application arguments."],["DESCRIPTION","**dotnet run** builds and executes a .NET project in one step. It's the standard way to run applications during development without creating deployment artifacts.\n\nThe double-dash separates dotnet options from arguments passed to the application. Launch profiles in launchSettings.json can configure environment variables and arguments for development scenarios.\n\nFor web applications, dotnet run starts the development server with hot reload support in newer versions."],["CAVEATS","Includes build time in execution. Use --no-build for faster iteration. Launch profiles only work from project directory. Not intended for production use."],["HISTORY","dotnet run is part of the **.NET CLI** providing streamlined development iteration. It combines build and execution, improving developer experience over separate compile-then-run workflows."],["INSTALL","```dnf: sudo dnf install dotnet-host```\n\n```pacman: sudo pacman -S dotnet-host```\n\n```apk: sudo apk add dotnet-host```\n\n```brew: brew install dotnet```"],["SEE ALSO","[dotnet](/man/dotnet)(1), [dotnet-build](/man/dotnet-build)(1), [dotnet-test](/man/dotnet-test)(1)"]],"e":["dotnet","dotnet-build","dotnet-test"],"x":"dotnet run"},"ebuild":{"n":"ebuild","t":"Gentoo package build interface","s":[["TAGLINE","Gentoo package build interface"],["TLDR","Create/update package **manifest**\n\n```ebuild [path/to/file.ebuild] manifest```\n\n**Clean** temporary build directories\n\n```ebuild [path/to/file.ebuild] clean```\n\n**Fetch** sources\n\n```ebuild [path/to/file.ebuild] fetch```\n\n**Extract** sources\n\n```ebuild [path/to/file.ebuild] unpack```\n\n**Compile** sources\n\n```ebuild [path/to/file.ebuild] compile```\n\n**Install** to temporary directory\n\n```ebuild [path/to/file.ebuild] install```\n\nInstall to **live filesystem**\n\n```ebuild [path/to/file.ebuild] qmerge```\n\n**Full build** (fetch, unpack, compile, install, merge)\n\n```ebuild [path/to/file.ebuild] merge```"],["SYNOPSIS","**ebuild** _file_ _command_..."],["DESCRIPTION","**ebuild** is a low-level interface to the Gentoo Portage system. It processes ebuild files, which are Bash scripts describing how to build and install packages.\n\nThe tool provides direct access to individual build phases (fetch, unpack, compile, install, merge), allowing developers to test and debug packages step-by-step. This is essential for package maintainers creating or modifying ebuilds.\n\nWhile emerge is used for normal package management, ebuild gives fine-grained control over the build process. It's particularly useful for testing new packages before adding them to the portage tree or for manually installing custom ebuilds."],["PARAMETERS","**manifest**\n> Create/update package manifest\n\n**clean**\n> Remove temporary build files\n\n**fetch**\n> Download source files\n\n**unpack**\n> Extract sources\n\n**compile**\n> Build the package\n\n**install**\n> Install to temp directory\n\n**qmerge**\n> Merge to live filesystem\n\n**merge**\n> All steps combined\n\n**unmerge**\n> Remove from live filesystem\n\n**setup**\n> Run pre-build setup phase\n\n**prepare**\n> Run source preparation (patches, etc.)\n\n**configure**\n> Run configure phase\n\n**test**\n> Run package tests\n\n**prerm**\n> Execute pre-removal tasks\n\n**postrm**\n> Execute post-removal tasks\n\n**postinst**\n> Execute post-installation tasks\n\n**digest**\n> Create manifest (legacy alias for manifest)"],["CAVEATS","Gentoo-specific. For normal package management, use **emerge**. Ebuild files require specific directory structure in portage tree. By default, ebuild executes all phases in order up to the specified one, skipping phases already completed in a previous invocation."],["SEE ALSO","[emerge](/man/emerge)(1), [equery](/man/equery)(1)"]],"e":["emerge","equery"],"x":"ebuild [path/to/file.ebuild] manifest"},"edgepaint":{"n":"edgepaint","t":"color edges of a laid-out graph for visual distinction","s":[["TAGLINE","color edges of a laid-out graph for visual distinction"],["TLDR","**Color edges** and write the result\n\n```edgepaint [graph.dot] -o [colored.dot]```\n\n**Set the minimum** incidence angle (degrees)\n\n```edgepaint --angle=15 [graph.dot] -o [out.dot]```\n\n**Use a specific** color scheme\n\n```edgepaint --color_scheme=accent8 [graph.dot] -o [out.dot]```\n\n**Allow edges sharing an endpoint** to keep the same color\n\n```edgepaint --share_endpoint [graph.dot] -o [out.dot]```\n\n**Pipe through dot** to render a PNG\n\n```dot graph.dot | edgepaint | dot -Tpng -o out.png```"],["SYNOPSIS","**edgepaint** [_options_] [_input_] [**-o** _output_]"],["PARAMETERS","_input_\n> Input DOT file with node positions already laid out. Reads stdin if omitted.\n\n**-o** _FILE_\n> Write output to FILE instead of stdout.\n\n**--accuracy**=_e_\n> Precision for finding maximally different coloring at each node. Default 0.01.\n\n**--angle**=_a_\n> Color two edges differently if their incidence angle is less than _a_ degrees. Default 15.\n\n**--color_scheme**=_c_\n> Color palette: `rgb`, `gray`, `lab` (default), a list of hex colors, or any Brewer scheme name (e.g. `accent8`, `set19`).\n\n**--lightness**=_l1,l2_\n> Lightness range, only with `lab`. Integers 0-100. Default `0,70`.\n\n**--random_seed**=_s_\n> Seed for the randomized coloring. Negative values run multiple iterations with different seeds.\n\n**--share_endpoint**\n> Edges that share a node and are nearly anti-parallel (~180 degrees apart) are not treated as conflicting.\n\n**-v**\n> Verbose output.\n\n**-?**\n> Print usage and exit."],["DESCRIPTION","**edgepaint** assigns colors to the edges of a graph so that adjacent or near-parallel edges receive maximally distinct colors. It expects a graph that has already been laid out (for example by **dot** or **neato**), since the algorithm uses the geometric positions of edges to decide which pairs should differ.\n\nColors are written into each edge's `color` attribute and the modified DOT is emitted on stdout (or to the file given with **-o**). The default `lab` color scheme produces perceptually distinct hues; alternative Brewer schemes give a fixed small palette."],["CAVEATS","Input must already contain edge geometry from a layout engine; otherwise edgepaint cannot determine angles. Very dense graphs may exhaust the palette and still produce visually similar edges. The Brewer schemes have a fixed number of colors, so large graphs should prefer `lab`."],["HISTORY","**edgepaint** is part of **Graphviz**, originally developed at **AT&T Labs Research**. It implements ideas from Yifan Hu's work on edge coloring for graph readability."],["INSTALL","```apt: sudo apt install graphviz```\n\n```dnf: sudo dnf install graphviz```\n\n```pacman: sudo pacman -S graphviz```\n\n```apk: sudo apk add graphviz```\n\n```zypper: sudo zypper install graphviz```\n\n```brew: brew install graphviz```\n\n```nix: nix profile install nixpkgs#graphviz```"],["SEE ALSO","[dot](/man/dot)(1), [neato](/man/neato)(1), [graphviz](/man/graphviz)(7)"]],"e":["dot","neato","graphviz"],"x":"edgepaint [graph.dot] -o [colored.dot]"},"envfetch":{"n":"envfetch","t":"Cross-platform CLI for environment variables","s":[["TAGLINE","Cross-platform CLI for environment variables"],["TLDR","**Display** all environment variables\n\n```envfetch```\n\n**Get** a specific variable\n\n```envfetch [VARIABLE_NAME]```\n\n**Search** for variables matching pattern\n\n```envfetch --search [pattern]```\n\n**Output** in JSON format\n\n```envfetch --json```"],["SYNOPSIS","**envfetch** [_options_] [_variable_]"],["PARAMETERS","**--search** _PATTERN_\n> Search for variables matching pattern\n\n**--json**\n> Output in JSON format\n\n**--export**\n> Output as shell export commands\n\n**--no-color**\n> Disable colored output\n\n**-v, --version**\n> Display version and exit\n\n**-h, --help**\n> Display help and exit"],["DESCRIPTION","**envfetch** is a cross-platform tool for displaying and querying environment variables. It provides a unified interface across different operating systems and shells for inspecting environment configuration.\n\nThe tool supports searching, filtering, and various output formats, making it useful for debugging environment issues and extracting configuration values."],["CAVEATS","Cross-platform compatibility may have slight variations. Sensitive variables are displayed by default. Large environment sets may require filtering."],["HISTORY","**envfetch** was developed as a portable alternative to platform-specific environment inspection tools."],["INSTALL","```aur: yay -S envfetch```"],["SEE ALSO","[env](/man/env)(1), [printenv](/man/printenv)(1), [set](/man/set)(1)"]],"e":["env","printenv","set"],"x":"envfetch"},"exiv2":{"n":"exiv2","t":"image metadata reader and editor for Exif, IPTC, and XMP","s":[["TAGLINE","image metadata reader and editor for Exif, IPTC, and XMP"],["TLDR","**Show metadata** of image\n\n```exiv2 [image.jpg]```\n\n**Print all Exif** data\n\n```exiv2 -pa [image.jpg]```\n\n**Print IPTC data**\n\n```exiv2 -pi [image.jpg]```\n\n**Print XMP data**\n\n```exiv2 -px [image.jpg]```\n\n**Modify Exif** data\n\n```exiv2 -M \"set Exif.Photo.UserComment [text]\" [image.jpg]```\n\n**Delete all metadata**\n\n```exiv2 -da [image.jpg]```\n\n**Rename by date**\n\n```exiv2 -r \"%Y%m%d_%H%M%S\" [*.jpg]```"],["SYNOPSIS","**exiv2** [_options_] [_actions_] _files_..."],["PARAMETERS","_FILES_\n> Image files to process.\n\n**-pa**\n> Print all metadata (Exif, IPTC, and XMP tags).\n\n**-pi**\n> Print IPTC tags.\n\n**-px**\n> Print XMP tags.\n\n**-M** _CMD_\n> Modify metadata.\n\n**-da**\n> Delete all metadata.\n\n**-r** _FORMAT_\n> Rename files by timestamp.\n\n**-e** _FORMAT_\n> Extract thumbnail.\n\n**--help**\n> Display help information."],["DESCRIPTION","**exiv2** reads, writes, and manipulates image metadata including Exif, IPTC, and XMP. It supports JPEG, TIFF, PNG, and many other image formats.\n\nThe tool provides detailed access to camera settings, GPS coordinates, timestamps, and descriptive metadata. Batch operations can modify metadata across multiple files.\n\nexiv2 is useful for organizing photos, removing sensitive metadata, and forensic analysis of image files."],["CAVEATS","Metadata modifications are permanent. Some formats have limited metadata support. GPS data may reveal location. Backup before bulk operations."],["HISTORY","exiv2 is an open-source project providing a library and command-line tool for Exif and other metadata handling, used by many image applications."],["INSTALL","```apt: sudo apt install exiv2```\n\n```dnf: sudo dnf install exiv2```\n\n```pacman: sudo pacman -S exiv2```\n\n```apk: sudo apk add exiv2```\n\n```zypper: sudo zypper install exiv2```\n\n```brew: brew install exiv2```\n\n```nix: nix profile install nixpkgs#exiv2```"],["SEE ALSO","[exiftool](/man/exiftool)(1), [identify](/man/identify)(1), [jhead](/man/jhead)(1)"],["RESOURCES","```[Source code](https://github.com/Exiv2/exiv2)```\n\n```[Homepage](https://exiv2.org/)```"]],"e":["exiftool","identify","jhead"],"x":"exiv2 [image.jpg]"},"git-checkout-index":{"n":"git-checkout-index","t":"Copy files from index to working directory","s":[["TAGLINE","Copy files from index to working directory"],["TLDR","**Copy all files to working tree**\n\n```git checkout-index -a```\n\n**Copy specific file**\n\n```git checkout-index [file]```\n\n**Copy with force**\n\n```git checkout-index -f [file]```\n\n**Copy to different directory**\n\n```git checkout-index --prefix=[dir/] -a```\n\n**Copy all including unmerged**\n\n```git checkout-index -a -f```"],["SYNOPSIS","**git** **checkout-index** [_options_] [_files_...]"],["PARAMETERS","**-a**, **--all**\n> Check out all files.\n\n**-f**, **--force**\n> Force overwrite.\n\n**--prefix** _string_\n> Output prefix.\n\n**-u**, **--index**\n> Update stat information in the index file after checking out.\n\n**-n**, **--no-create**\n> Don't create new files, only refresh existing ones.\n\n**-z**\n> Separate input paths with NUL instead of newline (use with **--stdin**).\n\n**--stdin**\n> Read the list of paths to check out from standard input.\n\n**--stage**=(_1_|_2_|_3_|_all_)\n> Copy out unmerged files from the given stage instead of the default."],["DESCRIPTION","**git checkout-index** is a low-level plumbing command that copies files from Git's staging area (index) to the working directory. It provides fine-grained control over which index entries are materialized as working tree files.\n\nUnlike the higher-level git checkout, this command operates purely on the index without switching branches or updating HEAD. It's primarily used in Git's internal operations and specialized scripts that need precise control over working tree population.\n\nThe --prefix option enables extracting the index to an alternate location, useful for creating clean checkouts or exporting specific versions. This command is essential for understanding Git's three-tree architecture (working directory, index, and HEAD) and how file state moves between them."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-checkout](/man/git-checkout)(1), [git-read-tree](/man/git-read-tree)(1)"],["RESOURCES","```[Source code](https://github.com/git/git)```\n\n```[Homepage](https://git-scm.com/)```\n\n```[Documentation](https://git-scm.com/docs/git-checkout-index)```"]],"e":["git-checkout","git-read-tree"],"x":"git checkout-index -a"},"git-delete-tag":{"n":"git-delete-tag","t":"Delete both local and remote tags","s":[["TAGLINE","Delete both local and remote tags"],["TLDR","**Delete a local and remote tag**\n\n```git delete-tag [tag_name]```\n\n**Delete multiple tags at once**\n\n```git delete-tag [tag1] [tag2]```"],["SYNOPSIS","**git** **delete-tag** _tag_ [_tag_...]"],["DESCRIPTION","**git delete-tag** deletes a tag both locally and on the remote in a single operation. Part of the git-extras collection, it simplifies tag removal by combining what would normally require separate git tag -d and git push origin --delete commands.\n\nTags typically mark release points or significant commits, but occasionally need removal due to mistakes, versioning changes, or release rollbacks. This command ensures synchronization between local and remote repositories, preventing scenarios where a deleted local tag still appears when fetching from the remote."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-tag](/man/git-tag)(1), [git-extras](/man/git-extras)(1), [git-push](/man/git-push)(1), [git-delete-branch](/man/git-delete-branch)(1)"]],"e":["git-tag","git-extras","git-push","git-delete-branch"],"x":"git delete-tag [tag_name]"},"git-filter-branch":{"n":"git-filter-branch","t":"Rewrite branch history by applying filters","s":[["TAGLINE","Rewrite branch history by applying filters"],["TLDR","**Remove file from history**\n\n```git filter-branch --tree-filter 'rm -f [file]' HEAD```\n\n**Rewrite author**\n\n```git filter-branch --env-filter 'GIT_AUTHOR_EMAIL=\"[new@email]\"' HEAD```\n\n**Subdirectory filter**\n\n```git filter-branch --subdirectory-filter [dir] HEAD```\n\n**Remove empty commits**\n\n```git filter-branch --prune-empty HEAD```\n\n**Force rewrite**\n\n```git filter-branch -f --tree-filter '[command]' HEAD```"],["SYNOPSIS","**git** **filter-branch** [_options_] [_--_] [_rev-list_]"],["PARAMETERS","**--env-filter** _cmd_\n> Rewrite author/committer environment variables (name, email, date).\n\n**--tree-filter** _cmd_\n> Rewrite tree and contents; checks out each commit, so slow on large repos.\n\n**--index-filter** _cmd_\n> Rewrite the index without checking out the tree; much faster than **--tree-filter**. Commonly used with **git rm --cached**.\n\n**--parent-filter** _cmd_\n> Rewrite a commit's parent list.\n\n**--msg-filter** _cmd_\n> Rewrite commit messages.\n\n**--commit-filter** _cmd_\n> Replace the commit-creation step entirely.\n\n**--tag-name-filter** _cmd_\n> Rewrite tag names pointing at rewritten commits.\n\n**--subdirectory-filter** _dir_\n> Only rewrite history touching _dir_, and make it the new project root.\n\n**--prune-empty**\n> Remove commits that become empty after filtering.\n\n**--original** _namespace_\n> Namespace for backup refs (default **refs/original**).\n\n**-d** _dir_\n> Temporary directory to use (e.g. tmpfs, for I/O speed).\n\n**-f**, **--force**\n> Force the operation, overwriting an existing backup namespace."],["DESCRIPTION","**git filter-branch** is a powerful but deprecated tool for rewriting Git history by applying filter commands to every commit in a branch. It walks through the entire commit history, allowing modifications to trees, commit messages, author information, or other metadata.\n\nThe subdirectory-filter is particularly useful for extracting a subdirectory into a new repository with its history intact. Performance is notably poor on large repositories because it must check out every commit's tree. This limitation led to the development of git-filter-repo as the official replacement."],["CAVEATS","**Officially deprecated** in favor of git-filter-repo. Slow on large repositories. Changes history, invalidating commit hashes and requiring force pushes. Collaborators must reclone. Creates backup refs that must be manually cleaned."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-filter-repo](/man/git-filter-repo)(1), [git-rebase](/man/git-rebase)(1), [git-rm](/man/git-rm)(1)"],["RESOURCES","```[Source code](https://github.com/git/git)```\n\n```[Homepage](https://git-scm.com/)```\n\n```[Documentation](https://git-scm.com/docs/git-filter-branch)```"]],"e":["git-filter-repo","git-rebase","git-rm"],"x":"git filter-branch --tree-filter 'rm -f [file]' HEAD"},"git-lazy-mount":{"n":"git-lazy-mount","t":"Mount a Git repository without cloning it","s":[["TAGLINE","Mount a Git repository without cloning it"],["TLDR","**Mount a remote repository** onto a local directory\n\n```git lazy-mount [https://github.com/example/huge-repo] [~/huge-repo]```\n\n**Work in the mounted tree** with ordinary Git commands\n\n```cd [~/huge-repo] && git switch -c [feature]```\n\n**Build from source** with the FUSE feature enabled\n\n```cargo build --release -p glm-cli --features fuse```"],["SYNOPSIS","**git lazy-mount** _repository_url_ _mount_point_\n\n**git-lazy-mount** _repository_url_ _mount_point_"],["DESCRIPTION","**git-lazy-mount** mounts a Git repository as a local working tree without first cloning it. Instead of downloading every object up front, it exposes the repository through a **FUSE** file system and fetches file contents on demand, the moment a file is opened, read, or edited. Files that are never touched are never downloaded, which makes very large repositories usable in seconds and with a fraction of the disk space.\n\nBecause the mount looks like an ordinary checkout, normal Git commands work inside it: you can edit files, commit, create branches, and push. The project also ships a companion `sgrep` tool that queries a remote code-search index and overlays your uncommitted edits, so searching a huge repository does not require materializing it.\n\nIt installs as a Git subcommand, so `git lazy-mount` and the underlying `git-lazy-mount` binary are equivalent. The tool is built in **Rust** and targets Linux, where it relies on the kernel FUSE interface."],["CAVEATS","git-lazy-mount is **Linux only**; macOS and Windows are studied but not supported. It requires **libfuse3** and a system **Git of at least version 2.36**. Because file contents are fetched lazily over the network, accessing many files for the first time, or working offline, can be slow or fail, and the mount depends on the remote staying reachable. It is best suited to read-mostly work and targeted edits on large repositories rather than operations that scan the whole tree."],["HISTORY","git-lazy-mount was created by **Mohsen Azimi** (mohsen1) and is dual-licensed under **MIT** and **Apache 2.0**. In the author's benchmarks across twenty repositories it used about 1.3 GB versus 23 GB for full clones, roughly eighteen times less, while preserving complete history."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git](/man/git)(1), [sshfs](/man/sshfs)(1), [mount](/man/mount)(8), [fusermount](/man/fusermount)(1)"],["RESOURCES","```[Source code](https://github.com/mohsen1/git-lazy-mount)```"]],"e":["git","sshfs","mount","fusermount"],"x":"git lazy-mount [https://github.com/example/huge-repo] [~/huge-repo]"},"git-rev-parse":{"n":"git-rev-parse","t":"Pick out and massage parameters for other git commands","s":[["TAGLINE","Pick out and massage parameters for other git commands"],["TLDR","**Get commit hash**\n\n```git rev-parse HEAD```\n\n**Get short hash**\n\n```git rev-parse --short HEAD```\n\n**Get branch name**\n\n```git rev-parse --abbrev-ref HEAD```\n\n**Get repository root**\n\n```git rev-parse --show-toplevel```\n\n**Check if in git repository**\n\n```git rev-parse --is-inside-work-tree```\n\n**Get git directory**\n\n```git rev-parse --git-dir```\n\n**Verify** a ref exists (exits non-zero if invalid)\n\n```git rev-parse --verify [ref]```\n\n**Get path** from repo root to current directory\n\n```git rev-parse --show-prefix```"],["SYNOPSIS","**git** **rev-parse** [_options_] _args_..."],["PARAMETERS","**--short** [_length_]\n> Abbreviated hash.\n\n**--abbrev-ref**\n> Show short ref name.\n\n**--verify**\n> Verify object exists.\n\n**--show-toplevel**\n> Show repository root.\n\n**--git-dir**\n> Show .git directory.\n\n**--is-inside-work-tree**\n> Check if in work tree.\n\n**--show-prefix**\n> Show path of current directory relative to repository root.\n\n**--show-cdup**\n> Show path from current directory to repository root.\n\n**--is-bare-repository**\n> Print \"true\" if the repository is bare, \"false\" otherwise.\n\n**--absolute-git-dir**\n> Like --git-dir, but output is always an absolute path.\n\n**--symbolic**\n> Show symbolic name instead of SHA-1.\n\n**--symbolic-full-name**\n> Like --symbolic but show full ref name (e.g., refs/heads/master).\n\n**--quiet**\n> Only meaningful with --verify. Do not output error message on failure, just exit with non-zero status."],["DESCRIPTION","**git rev-parse** translates ref names, symbolic references, and other arguments into SHA-1 hashes. It also provides information about the repository structure, such as the root directory, git directory, and current branch.\n\nThis plumbing command is widely used in scripts for resolving refs and querying repository metadata."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-rev-list](/man/git-rev-list)(1), [git-describe](/man/git-describe)(1), [git-symbolic-ref](/man/git-symbolic-ref)(1), [git-log](/man/git-log)(1)"]],"e":["git-rev-list","git-describe","git-symbolic-ref","git-log"],"x":"git rev-parse HEAD"},"go-generate":{"n":"go-generate","t":"run code generation directives in Go source files","s":[["TAGLINE","run code generation directives in Go source files"],["TLDR","**Run generators in current package**\n\n```go generate```\n\n**Run generators in all packages**\n\n```go generate ./...```\n\n**Run specific generator by pattern**\n\n```go generate -run [pattern]```\n\n**Preview commands without executing**\n\n```go generate -n```\n\n**Run with verbose output**\n\n```go generate -v```\n\n**Skip generators matching pattern**\n\n```go generate -skip [pattern]```"],["SYNOPSIS","**go** **generate** [**-run** _regexp_] [**-skip** _regexp_] [**-n**] [**-v**] [**-x**] [_packages_]"],["PARAMETERS","**-run** _regexp_\n> Run only directives matching regexp.\n\n**-skip** _regexp_\n> Skip directives matching regexp.\n\n**-n**\n> Print commands without executing.\n\n**-v**\n> Print package names as processed.\n\n**-x**\n> Print commands as executed."],["DESCRIPTION","**go generate** scans Go source files for special directives of the form **//go:generate command** and executes the specified command. It automates code generation before compilation, commonly used for creating string methods, mock implementations, protocol buffers, and other generated code. Directives must start at the beginning of a line with no space between // and go:generate."],["CAVEATS","Environment variables $GOARCH, $GOOS, $GOFILE, $GOLINE, $GOPACKAGE, $GOROOT, $DOLLAR, and $PATH are set during execution. Generated files should include a comment matching **^// Code generated .* DO NOT EDIT\\.$** before non-comment content. **go generate** is never run automatically by **go build** or **go test**; it must be invoked explicitly."],["INSTALL","```apt: sudo apt install gccgo-go```\n\n```dnf: sudo dnf install gcc-go```\n\n```pacman: sudo pacman -S gcc-go```\n\n```apk: sudo apk add gcc-go```\n\n```zypper: sudo zypper install gcc-go```\n\n```brew: brew install go```\n\n```nix: nix profile install nixpkgs#go```"],["SEE ALSO","[go](/man/go)(1), [go-build](/man/go-build)(1)"],["RESOURCES","```[Documentation](https://pkg.go.dev/cmd/go#hdr-Generate_Go_files_by_processing_source)```\n\n```[Source code](https://github.com/golang/go)```"]],"e":["go","go-build"],"x":"go generate"},"grpc":{"n":"grpc","t":"high-performance remote procedure call framework","s":[["TAGLINE","high-performance remote procedure call framework"],["TLDR","**Generate Go code from proto**\n\n```protoc --go_out=. --go-grpc_out=. [service.proto]```\n\n**Generate Python code**\n\n```python -m grpc_tools.protoc -I. --python_out=. --grpc_python_out=. [service.proto]```\n\n**Call gRPC service**\n\n```grpcurl -plaintext [localhost:50051] [package.Service/Method]```\n\n**List services**\n\n```grpcurl -plaintext [localhost:50051] list```"],["DESCRIPTION","**gRPC** is a high-performance RPC framework using Protocol Buffers for serialization. It enables efficient communication between services with features like streaming, authentication, and load balancing.\n\ngRPC uses HTTP/2 for transport and provides code generation for multiple languages from .proto service definitions."],["PROTO FILE EXAMPLE","```protobuf\nsyntax = \"proto3\";\n\nservice Greeter {\n  rpc SayHello (HelloRequest) returns (HelloReply);\n  rpc SayHelloStream (HelloRequest) returns (stream HelloReply);\n}\n\nmessage HelloRequest {\n  string name = 1;\n}\n\nmessage HelloReply {\n  string message = 1;\n}\n```"],["TOOLS","```\nprotoc          Protocol buffer compiler\ngrpcurl         Command-line gRPC client\ngrpc_cli        Official gRPC CLI\nevans           Interactive gRPC client\n```"],["CAVEATS","Requires protocol buffer definitions. HTTP/2 needed; some proxies don't support it. Debugging harder than REST. Browser support requires gRPC-Web."],["HISTORY","gRPC was developed by **Google** and open-sourced in **2015**. It's based on Google's internal Stubby RPC framework and is now a CNCF project used extensively in cloud-native applications."],["INSTALL","```dnf: sudo dnf install grpc```\n\n```pacman: sudo pacman -S grpc```\n\n```brew: brew install grpc```\n\n```nix: nix profile install nixpkgs#grpc```"],["SEE ALSO","[protoc](/man/protoc)(1), [grpcurl](/man/grpcurl)(1), [curl](/man/curl)(1)"],["RESOURCES","```[Source code](https://github.com/grpc/grpc)```\n\n```[Homepage](https://grpc.io/)```\n\n```[Documentation](https://grpc.io/docs/)```"]],"e":["protoc","grpcurl","curl"],"x":"protoc --go_out=. --go-grpc_out=. [service.proto]"},"hakrawler":{"n":"hakrawler","t":"fast web crawler for gathering URLs and JavaScript file references","s":[["TAGLINE","fast web crawler for gathering URLs and JavaScript file references"],["TLDR","**Crawl single URL**\n\n```echo \"[https://example.com]\" | hakrawler```\n\n**Crawl with depth**\n\n```echo \"[https://example.com]\" | hakrawler -d [3]```\n\n**Include subdomains in scope**\n\n```echo \"[https://example.com]\" | hakrawler -s subs```\n\n**Use plain output**\n\n```echo \"[https://example.com]\" | hakrawler -plain```\n\n**Crawl with custom timeout and disable redirects**\n\n```echo \"[https://example.com]\" | hakrawler -t [15] -dr```"],["SYNOPSIS","**hakrawler** [_options_]"],["PARAMETERS","**-d** _depth_\n> Crawl depth (default 1).\n\n**-s** _scope_\n> Scope of the crawl: strict (same domain only), subs (include subdomains), or fuzzy (everything). Default is strict.\n\n**-plain**\n> Plain output mode, only prints URLs.\n\n**-t** _seconds_\n> Request timeout in seconds (default 10).\n\n**-w** _workers_\n> Number of concurrent workers (default 8).\n\n**-insecure**\n> Skip TLS certificate verification.\n\n**-dr**\n> Disable following redirects.\n\n**-h**\n> Show help message."],["DESCRIPTION","**hakrawler** is a fast web crawler for gathering URLs and JavaScript file references. It's designed for reconnaissance in web application security testing.\n\nThe tool reads URLs from stdin and outputs discovered links, making it easy to chain with other tools in security testing pipelines."],["USAGE EXAMPLES","```bash"],["Basic crawl","echo \"https://target.com\" | hakrawler"],["Crawl with plain output for piping","cat urls.txt | hakrawler -plain | sort -u"],["Deep crawl including subdomains","echo \"https://target.com\" | hakrawler -d 3 -s subs -plain\n```"],["CAVEATS","For authorized security testing only. May trigger security controls. High worker counts may cause rate limiting or IP bans."],["HISTORY","hakrawler was created by **Luke Stephens** (hakluke) as part of a suite of security tools. It's designed for bug bounty hunters and penetration testers doing web reconnaissance."],["INSTALL","```nix: nix profile install nixpkgs#hakrawler```"],["SEE ALSO","[waybackurls](/man/waybackurls)(1), [gau](/man/gau)(1), [curl](/man/curl)(1)"]],"e":["waybackurls","gau","curl"],"x":"echo \"[https://example.com]\" | hakrawler"},"hd-idle":{"n":"hd-idle","t":"utility that spins down external hard disks after a period of idle time","s":[["TAGLINE","utility that spins down external hard disks after a period of idle time"],["TLDR","**Start** the hd-idle service with default settings (10 minute idle time)\n\n```systemctl start hd-idle```\n\n**Spin down** a disk immediately\n\n```hd-idle -t [/dev/sdX]```\n\nSet **per-disk idle times** in seconds\n\n```hd-idle -i 0 -a /dev/sda -i [300] -a /dev/sdb -i [1200]```\n\nDisable spin-down globally, enable for **specific disks** only\n\n```hd-idle -i 0 -a [/dev/sda] -i [600]```"],["SYNOPSIS","**hd-idle** [_options_]"],["PARAMETERS","**-a** _DEVICE_\n> Apply the following idle time to the specified device\n\n**-i** _SECONDS_\n> Set idle time in seconds (0 disables spin-down)\n\n**-t** _DEVICE_\n> Spin down the specified disk immediately\n\n**-l** _LOGFILE_\n> Log spin-down events to specified file\n\n**-d**\n> Run in debug mode, do not daemonize"],["DESCRIPTION","**hd-idle** is a utility that spins down external hard disks after a period of idle time. It is particularly useful for USB or eSATA drives that don't support the hdparm -S command for automatic spin-down.\n\nThe daemon monitors disk activity and issues spin-down commands when disks have been idle for the configured time. Different idle times can be set for different disks, allowing fine-grained control over power management."],["CAVEATS","Works best with external drives that don't support native APM. Some drives may not respond to software spin-down commands. Frequent spin-down/spin-up cycles can reduce drive lifespan."],["HISTORY","hd-idle was created as a solution for external drives that lack proper power management support. It fills a gap where hdparm's spin-down feature doesn't work, common with USB enclosures that don't pass through ATA commands."],["INSTALL","```apt: sudo apt install hd-idle```\n\n```dnf: sudo dnf install hd-idle```\n\n```apk: sudo apk add hd-idle```\n\n```nix: nix profile install nixpkgs#hd-idle```"],["SEE ALSO","[hdparm](/man/hdparm)(8), [smartctl](/man/smartctl)(8)"]],"e":["hdparm","smartctl"],"x":"systemctl start hd-idle"},"hdf5_ui":{"n":"hdf5_ui","t":"Terminal UI for inspecting HDF5 files","s":[["TAGLINE","Terminal UI for inspecting HDF5 files"],["TLDR","**Open an HDF5 file** for inspection\n\n```h5tui [path/to/file.h5]```"],["SYNOPSIS","**h5tui** [_options_] _file_"],["DESCRIPTION","**hdf5_ui** (invoked as **h5tui**) is a Textual User Interface for inspecting **HDF5** files. It provides a tree-based browser for navigating groups and datasets within HDF5 files, displaying attributes and metadata in an organized layout.\n\nThe tool comes with built-in plugins including a **HexViewer** that displays hexadecimal representations of datasets, and an **ImageViewer** that visualizes two- or three-dimensional image data directly in the terminal. When a node is highlighted, its attributes are shown in the bottom-left panel."],["CAVEATS","Requires Python and the h5py library. Install via `pip install h5ui`. Very large datasets may be slow to render in the terminal. The ImageViewer plugin requires data that can be converted to a Pillow image. Custom plugins can be added via entrypoints."],["HISTORY","**hdf5_ui** was created by **Rik Huygen** (rhuygen) and is written in **Python** using the **Textual** TUI framework. It was developed to provide a convenient terminal-based alternative to graphical HDF5 inspection tools like HDFView."]],"e":[],"x":"h5tui [path/to/file.h5]"},"hexo":{"n":"hexo","t":"fast, simple static blog framework","s":[["TAGLINE","fast, simple static blog framework"],["TLDR","**Create new blog**\n\n```hexo init [blog-name]```\n\n**Create new post**\n\n```hexo new [post-title]```\n\n**Generate static files**\n\n```hexo generate```\n\n**Start local server**\n\n```hexo server```\n\n**Deploy to remote**\n\n```hexo deploy```\n\n**Clean generated files**\n\n```hexo clean```"],["SYNOPSIS","**hexo** _command_ [_options_]"],["PARAMETERS","_COMMAND_\n> Subcommand to execute.\n\n**init**\n> Initialize new blog.\n\n**new** _TITLE_\n> Create new post/page.\n\n**generate**, **g**\n> Generate static files.\n\n**publish** _TITLE_\n> Move a draft into the posts folder as a published post.\n\n**server**, **s**\n> Start local server.\n\n**deploy**, **d**\n> Deploy to remote.\n\n**list** _TYPE_\n> List site data (for example posts, pages, routes, tags).\n\n**clean**\n> Remove the cache file (`db.json`) and generated `public` directory.\n\n**-p**, **--port** _PORT_\n> Server port (default 4000).\n\n**-w**, **--watch**\n> Watch for file changes (with `generate` or `server`).\n\n**--help**\n> Display help information."],["DESCRIPTION","**Hexo** is a fast, simple static blog framework. It uses Markdown for content and supports themes, plugins, and multiple deployment targets.\n\nThe framework generates static HTML from Markdown posts. It's popular for developer blogs and documentation sites."],["CAVEATS","Node.js required. Learning curve for themes. Plugin compatibility varies."],["HISTORY","Hexo was created by **Tommy Chen** as a fast static site generator for blogs, gaining popularity in the developer community."],["INSTALL","```brew: brew install hexo```"],["SEE ALSO","[hugo](/man/hugo)(1), [jekyll](/man/jekyll)(1), [gatsby](/man/gatsby)(1)"]],"e":["hugo","jekyll","gatsby"],"x":"hexo init [blog-name]"},"home-manager":{"n":"home-manager","t":"nix-based tool for managing user environment configuration declaratively","s":[["TAGLINE","nix-based tool for managing user environment configuration declaratively"],["TLDR","**Apply home configuration** from default location\n\n```home-manager switch```\n\n**Apply a specific flake** configuration\n\n```home-manager switch --flake [.#username]```\n\n**Initialize** a new flake-based configuration\n\n```home-manager init```\n\n**Initialize and immediately activate** the configuration\n\n```home-manager init --switch```\n\n**Build without activating**\n\n```home-manager build```\n\n**List generations** (configuration history)\n\n```home-manager generations```\n\n**Rollback to previous generation**\n\n```home-manager rollback```\n\n**Remove old generations**\n\n```home-manager expire-generations \"[30 days ago]\"```\n\n**Edit configuration file**\n\n```home-manager edit```\n\n**List installed packages**\n\n```home-manager packages```"],["SYNOPSIS","**home-manager** [_options_] _command_ [_args_]"],["PARAMETERS","**switch**\n> Build and activate new configuration.\n\n**build**\n> Build configuration without activating.\n\n**init** [_--switch_] [_dir_]\n> Generate an initial Home Manager configuration. Use **--switch** to also activate it.\n\n**generations**\n> List all configuration generations.\n\n**rollback**\n> Revert to previous generation.\n\n**expire-generations** _period_\n> Remove generations older than period.\n\n**remove-generations** _IDs_\n> Remove specific generations by ID.\n\n**edit**\n> Open configuration in $EDITOR.\n\n**news**\n> Show news entries about configuration.\n\n**packages**\n> List installed packages.\n\n**uninstall**\n> Remove Home Manager from the user environment.\n\n**option** _name_\n> Show value of configuration option.\n\n**--flake** _flake-uri_\n> Use flake-based configuration.\n\n**-f**, **--file** _file_\n> Configuration file path.\n\n**-A** _attribute_\n> Attribute to build from configuration.\n\n**-b** _ext_\n> Backup file extension for existing files that would be overwritten.\n\n**-n**, **--dry-run**\n> Show what would be done without doing it.\n\n**-v**, **--verbose**\n> Increase verbosity.\n\n**--show-trace**\n> Show detailed location information for errors."],["DESCRIPTION","**home-manager** is a Nix-based tool for managing user environment configuration declaratively. It handles dotfiles, user packages, and environment settings through Nix expressions, enabling reproducible and versionable home directory setups.\n\nConfiguration is written in Nix language, typically in **~/.config/home-manager/home.nix** or as part of a Nix flake. The configuration declares packages to install, programs to configure, files to manage, and services to run.\n\nMany programs have dedicated Home Manager modules that generate configuration files automatically. For example, configuring **programs.git** generates ~/.gitconfig with proper syntax. This is more maintainable than managing dotfiles manually.\n\nGenerations provide configuration history. Each **switch** creates a new generation, and you can roll back to any previous state. This makes experimentation safe - broken configurations can be reverted instantly.\n\nHome Manager can run standalone or integrate with NixOS. In standalone mode, it manages the user environment independently. With NixOS, it can be imported as a module for tighter integration."],["CAVEATS","Requires Nix package manager. Learning curve for Nix language. Some program configurations may conflict with manually managed dotfiles. Flake-based configs need experimental features enabled. Generation management requires manual cleanup to prevent disk usage growth."],["HISTORY","**Home Manager** was created by Robert Helgesson (rycee) around **2017** to bring NixOS's declarative configuration approach to user home directories. It filled a gap for users who wanted Nix's reproducibility for their personal environment without running NixOS. The project has grown to include modules for hundreds of programs."],["INSTALL","```nix: nix profile install nixpkgs#home-manager```"],["SEE ALSO","[nix](/man/nix)(1), [nix-env](/man/nix-env)(1), [nix-build](/man/nix-build)(1), [nix-shell](/man/nix-shell)(1), [nixos-rebuild](/man/nixos-rebuild)(8), [chezmoi](/man/chezmoi)(1), [stow](/man/stow)(8)"]],"e":["nix","nix-env","nix-build","nix-shell","nixos-rebuild","chezmoi","stow"],"x":"home-manager switch"},"hwloc-ls":{"n":"hwloc-ls","t":"hardware locality topology viewer alias","s":[["TAGLINE","hardware locality topology viewer alias"],["TLDR","This command is an alias of **lstopo-no-graphics**\n\n```tldr lstopo-no-graphics```"],["INSTALL","```apt: sudo apt install hwloc```\n\n```dnf: sudo dnf install hwloc```\n\n```pacman: sudo pacman -S hwloc```\n\n```apk: sudo apk add hwloc-tools```\n\n```zypper: sudo zypper install hwloc```\n\n```brew: brew install hwloc```\n\n```nix: nix profile install nixpkgs#hwloc```"],["SEE ALSO","[lstopo-no-graphics](/man/lstopo-no-graphics)(1), [lstopo](/man/lstopo)(1)"]],"e":["lstopo-no-graphics","lstopo"],"x":"tldr lstopo-no-graphics"},"idevicebackup":{"n":"idevicebackup","t":"creates and restores backups of iOS devices","s":[["TAGLINE","creates and restores backups of iOS devices"],["TLDR","**Create backup**\n\n```idevicebackup backup [backup-dir]```\n\n**Restore from backup**\n\n```idevicebackup restore [backup-dir]```\n\n**Backup specific device**\n\n```idevicebackup -u [device-udid] backup [backup-dir]```"],["SYNOPSIS","**idevicebackup** [_options_] _command_ _directory_"],["SUBCOMMANDS","**backup**\n> Create device backup.\n\n**restore**\n> Restore from backup."],["PARAMETERS","**-u** _udid_\n> Target specific device.\n\n**-n**\n> Connect over network.\n\n**-d**\n> Enable debug output."],["DESCRIPTION","**idevicebackup** creates and restores backups of iOS devices. Part of the libimobiledevice suite. Works with older iOS versions. For iOS 4 and later, use **idevicebackup2** instead."],["CAVEATS","Deprecated for newer iOS versions. Use idevicebackup2 for iOS 4+."],["INSTALL","```apt: sudo apt install libimobiledevice-utils```\n\n```dnf: sudo dnf install libimobiledevice-utils```\n\n```apk: sudo apk add libimobiledevice-progs```"],["SEE ALSO","[idevicebackup2](/man/idevicebackup2)(1), [ideviceinfo](/man/ideviceinfo)(1)"]],"e":["idevicebackup2","ideviceinfo"],"x":"idevicebackup backup [backup-dir]"},"impacket-sniffer":{"n":"impacket-sniffer","t":"Simple packet sniffer using raw sockets from the Impacket library","s":[["TAGLINE","Simple packet sniffer using raw sockets from the Impacket library"],["TLDR","**Sniff default protocols (ICMP, TCP, UDP)**\n\n```sudo impacket-sniffer```\n\n**Sniff specific protocols**\n\n```sudo impacket-sniffer [icmp] [tcp]```\n\n**Sniff only UDP traffic**\n\n```sudo impacket-sniffer [udp]```"],["SYNOPSIS","**impacket-sniffer** [_protocol_ ...]"],["PARAMETERS","_protocol_\n> One or more protocol names to sniff (e.g., icmp, tcp, udp). Any name recognized by **socket.getprotobyname()** is valid. When no protocols are specified, defaults to icmp, tcp, and udp."],["DESCRIPTION","**impacket-sniffer** is a simple packet sniffer that uses raw sockets to listen for packets matching specified protocols. It opens a raw socket for each protocol, uses **select()** to monitor incoming packets, and decodes them using Impacket's **ImpactDecoder.IPDecoder** for display.\n\nUnlike **tcpdump**, this tool does not accept an interface argument or BPF filter expressions. It listens on all available interfaces via raw sockets and filters only by protocol type."],["CAVEATS","Requires root/administrator privileges for raw socket access. Does not support interface selection or BPF filters. For comprehensive packet analysis, dedicated tools like **Wireshark** or **tcpdump** are more appropriate."],["HISTORY","Part of the **Impacket** library, originally by SecureAuth (now maintained by Fortra). Demonstrates Impacket's capabilities for network traffic capture and protocol decoding in Python."],["SEE ALSO","[tcpdump](/man/tcpdump)(1), [wireshark](/man/wireshark)(1), [impacket-sniff](/man/impacket-sniff)(1), [tshark](/man/tshark)(1), [ngrep](/man/ngrep)(1)"]],"e":["tcpdump","wireshark","impacket-sniff","tshark","ngrep"],"x":"sudo impacket-sniffer"},"ksh":{"n":"ksh","t":"Korn shell, combining features of sh and csh","s":[["TAGLINE","Korn shell, combining features of sh and csh"],["TLDR","**Start Korn shell**\n\n```ksh```\n\n**Run a script**\n\n```ksh [script.ksh]```\n\n**Run a command string**\n\n```ksh -c \"[command]\"```\n\n**Start a login shell**\n\n```ksh -l```\n\n**Start a restricted shell**\n\n```ksh -r```\n\n**Enable POSIX compliance mode**\n\n```ksh -o posix```\n\n**Enable vi-style line editing**\n\n```ksh -o vi```"],["SYNOPSIS","**ksh** [_options_] [_script_] [_args_...]"],["PARAMETERS","_SCRIPT_\n> Script file to execute.\n\n**-c** _CMD_\n> Execute command string.\n\n**-l**\n> Login shell (reads profile files).\n\n**-r**\n> Restricted shell (limits `cd`, path changes, and redirections). Equivalent to invoking as `rksh`.\n\n**-s**\n> Read commands from standard input.\n\n**-o** _OPTION_\n> Set shell option (e.g., `vi`, `emacs`, `posix`, `noclobber`, `errexit`, `noglob`, `pipefail`).\n\n**+o** _OPTION_\n> Unset the given shell option.\n\n**-i**\n> Force interactive shell mode.\n\n**-n**\n> Read commands but do not execute them (syntax check).\n\n**-x**\n> Print commands and arguments as they are executed (trace mode).\n\n**-e**\n> Exit immediately if a command exits with non-zero status.\n\n**-v**\n> Print shell input lines as they are read.\n\n**-a**\n> Export all assigned variables automatically.\n\n**-u**\n> Treat unset variables as an error when substituting.\n\n**-R** _file_\n> Write a cross-reference listing to _file_ (ksh93 only)."],["DESCRIPTION","**ksh** is the Korn shell, combining features of the Bourne shell (sh) and C shell (csh). It provides advanced scripting capabilities with interactive command-line editing, command history, job control, and aliases.\n\nThe shell supports both vi and emacs editing modes, associative and indexed arrays, arithmetic evaluation, coprocesses, and pattern matching. ksh93 is the most widely used version; ksh2020 is a more recent release."],["CAVEATS","Multiple implementations exist (ksh88, ksh93, mksh, pdksh). Behavior may differ subtly between implementations and from bash. POSIX compatible when invoked with `-o posix`."],["HISTORY","The Korn shell was created by **David Korn** at **Bell Labs** in the early **1980s**, influencing many modern shells including bash and zsh."],["INSTALL","```dnf: sudo dnf install ksh```\n\n```pacman: sudo pacman -S ksh```\n\n```apk: sudo apk add loksh```\n\n```zypper: sudo zypper install ksh```\n\n```brew: brew install ksh```\n\n```nix: nix profile install nixpkgs#loksh```"],["SEE ALSO","[bash](/man/bash)(1), [sh](/man/sh)(1), [zsh](/man/zsh)(1), [tcsh](/man/tcsh)(1)"]],"e":["bash","sh","zsh","tcsh"],"x":"ksh"},"ktool":{"n":"ktool","t":"Mach-O and Objective-C analysis toolkit for the terminal","s":[["TAGLINE","Mach-O and Objective-C analysis toolkit for the terminal"],["TLDR","**Dump Mach-O header information**\n\n```ktool dump [path/to/binary]```\n\n**List segments and sections**\n\n```ktool list [path/to/binary]```\n\n**Show symbols** from a binary\n\n```ktool symbols [path/to/binary]```\n\n**Output analysis as JSON**\n\n```ktool json [path/to/binary]```\n\n**Show file type information**\n\n```ktool file [path/to/binary]```"],["SYNOPSIS","**ktool** _command_ [_options_] [_file_]"],["DESCRIPTION","**ktool** is a CLI and TUI toolkit for inspecting and editing Mach-O binaries and Objective-C metadata. It provides commands for MachO analysis (dump, json, list, symbols, info), MachO editing (insert, edit, lipo), and miscellaneous utilities (file, img4).\n\nThe tool works without external compiled dependencies and runs anywhere with a Python interpreter. It can analyze Apple mach-o and dyld_shared_cache files."],["CAVEATS","Designed specifically for Apple Mach-O binary format. Not applicable to ELF or PE binaries."],["HISTORY","**ktool** was created by **0cyn** and is written in **Python**. It can be installed via pip with `pip install k2l`."],["SEE ALSO","[nm](/man/nm)(1)"]],"e":["nm"],"x":"ktool dump [path/to/binary]"},"kubectl-edit":{"n":"kubectl-edit","t":"opens a resource's configuration in your default editor","s":[["TAGLINE","opens a resource's configuration in your default editor"],["TLDR","**Edit deployment in editor**\n\n```kubectl edit deployment [name]```\n\n**Edit pod**\n\n```kubectl edit pod [pod-name]```\n\n**Edit with specific editor**\n\n```EDITOR=nano kubectl edit deployment [name]```\n\n**Edit resource in YAML format**\n\n```kubectl edit deployment [name] -o yaml```"],["SYNOPSIS","**kubectl** **edit** _type_/_name_ [_options_]"],["PARAMETERS","**-o**, **--output** _format_\n> Edit format (yaml, json).\n\n**-n**, **--namespace** _name_\n> Kubernetes namespace.\n\n**--record**\n> Record command in resource annotation."],["DESCRIPTION","**kubectl edit** opens a resource's configuration in your default editor. Changes are applied when you save and exit. The resource is validated before applying. Set EDITOR or KUBE_EDITOR environment variable to change the editor."],["INSTALL","```apt: sudo apt install kubectl```\n\n```pacman: sudo pacman -S kubectl```\n\n```apk: sudo apk add kubectl```\n\n```brew: brew install kubectl```\n\n```nix: nix profile install nixpkgs#kubectl```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [kubectl-patch](/man/kubectl-patch)(1)"]],"e":["kubectl","kubectl-patch"],"x":"kubectl edit deployment [name]"},"linode-cli-nodebalancers":{"n":"linode-cli-nodebalancers","t":"manages Linode NodeBalancers","s":[["TAGLINE","manages Linode NodeBalancers"],["TLDR","**List node balancers**\n\n```linode-cli nodebalancers list```\n\n**Create node balancer**\n\n```linode-cli nodebalancers create --region [us-east] --label [my-balancer]```\n\n**View node balancer**\n\n```linode-cli nodebalancers view [nodebalancer_id]```\n\n**List configs**\n\n```linode-cli nodebalancers configs-list [nodebalancer_id]```\n\n**Delete node balancer**\n\n```linode-cli nodebalancers delete [nodebalancer_id]```\n\n**Add node**\n\n```linode-cli nodebalancers node-create [nodebalancer_id] [config_id] --address [192.168.1.1:80]```"],["SYNOPSIS","**linode-cli nodebalancers** _subcommand_ [_options_]"],["PARAMETERS","_SUBCOMMAND_\n> NodeBalancer operation.\n\n**list**\n> List all NodeBalancers.\n\n**create**\n> Create NodeBalancer.\n\n**view** _ID_\n> View NodeBalancer details.\n\n**configs-list** _ID_\n> List configurations.\n\n**delete** _ID_\n> Delete NodeBalancer.\n\n**--region** _REGION_\n> Datacenter region.\n\n**--label** _NAME_\n> NodeBalancer name.\n\n**--help**\n> Display help information."],["DESCRIPTION","**linode-cli nodebalancers** manages Linode NodeBalancers. NodeBalancers distribute traffic across instances.\n\nThe tool configures load balancing, health checks, and SSL termination for high availability."],["CAVEATS","Requires authentication. Billed hourly. Needs backend nodes configured."],["HISTORY","linode-cli nodebalancers is part of the **Linode CLI** for managing NodeBalancer load balancing services."],["SEE ALSO","[linode-cli](/man/linode-cli)(1), [linode-cli-linodes](/man/linode-cli-linodes)(1)"]],"e":["linode-cli","linode-cli-linodes"],"x":"linode-cli nodebalancers list"},"lintian":{"n":"lintian","t":"checks Debian packages for policy compliance","s":[["TAGLINE","checks Debian packages for policy compliance"],["TLDR","**Check Debian package**\n\n```lintian [package.deb]```\n\n**Check changes file**\n\n```lintian [package.changes]```\n\n**Verbose output**\n\n```lintian -v [package.deb]```\n\n**Show info tags**\n\n```lintian -I [package.deb]```\n\n**Show all tags**\n\n```lintian -iI [package.deb]```\n\n**Check specific tag**\n\n```lintian --tags [tag-name] [package.deb]```"],["SYNOPSIS","**lintian** [_options_] _packages_"],["PARAMETERS","_PACKAGES_\n> Debian packages, changes, or dsc files.\n\n**-v**\n> Verbose output.\n\n**-I**\n> Display info tags.\n\n**-i**\n> Display tag descriptions.\n\n**--tags** _TAG_\n> Check specific tags only.\n\n**--suppress-tags** _TAG_\n> Suppress specific tags.\n\n**--help**\n> Display help information."],["DESCRIPTION","**lintian** checks Debian packages for policy compliance. It identifies bugs, policy violations, and issues.\n\nThe tool is essential for Debian maintainers. It checks both binary and source packages."],["CAVEATS","Debian/Ubuntu focused. Large tag database. Some checks need installed packages."],["HISTORY","Lintian was created by **Christian Schwarz** in 1998 to help Debian maintainers produce quality packages."],["INSTALL","```aur: yay -S lintian```"],["SEE ALSO","[lintian-info](/man/lintian-info)(1), [dpkg](/man/dpkg)(1), [debuild](/man/debuild)(1)"]],"e":["lintian-info","dpkg","debuild"],"x":"lintian [package.deb]"},"lzmore":{"n":"lzmore","t":"displays contents of lzip-compressed files using more","s":[["TAGLINE","displays contents of lzip-compressed files using more"],["TLDR","**View lzip-compressed file**\n\n```lzmore [file.lz]```\n\n**View multiple files**\n\n```lzmore [file1.lz] [file2.lz]```"],["SYNOPSIS","**lzmore** [_options_] [_file_...]"],["DESCRIPTION","**lzmore** displays contents of lzip-compressed files using more. Automatically decompresses for viewing. Press space to page, q to quit. For better navigation, use lzless instead."],["INSTALL","```dnf: sudo dnf install xz```\n\n```pacman: sudo pacman -S xz```\n\n```apk: sudo apk add xz```\n\n```zypper: sudo zypper install xz```\n\n```brew: brew install xz```\n\n```nix: nix profile install nixpkgs#xz```"],["SEE ALSO","[lzip](/man/lzip)(1), [more](/man/more)(1), [lzless](/man/lzless)(1)"]],"e":["lzip","more","lzless"],"x":"lzmore [file.lz]"},"mkcert":{"n":"mkcert","t":"creates locally-trusted certificates","s":[["TAGLINE","creates locally-trusted certificates"],["TLDR","**Install local CA**\n\n```mkcert -install```\n\n**Generate certificate**\n\n```mkcert [localhost]```\n\n**Multiple domains**\n\n```mkcert [localhost] [127.0.0.1] [::1]```\n\n**Wildcard certificate**\n\n```mkcert [*.local.dev]```\n\n**Specify output names**\n\n```mkcert -key-file [key.pem] -cert-file [cert.pem] [localhost]```\n\n**Generate a PKCS#12 bundle**\n\n```mkcert -pkcs12 [example.test]```\n\n**Show CA root directory**\n\n```mkcert -CAROOT```\n\n**Uninstall CA**\n\n```mkcert -uninstall```"],["SYNOPSIS","**mkcert** [_options_] [_domains_]"],["PARAMETERS","_DOMAINS_\n> Domain names for certificate.\n\n**-install**\n> Install local CA.\n\n**-uninstall**\n> Uninstall local CA.\n\n**-key-file** _FILE_\n> Key output file.\n\n**-cert-file** _FILE_\n> Certificate output file.\n\n**-p12-file** _FILE_\n> PKCS#12 output file (used with **-pkcs12**).\n\n**-pkcs12**\n> Produce a \".p12\"/\".pfx\" file with certificate and key (for Java/legacy tools).\n\n**-client**\n> Generate a certificate for client authentication.\n\n**-ecdsa**\n> Use ECDSA keys instead of RSA.\n\n**-csr** _CSR_\n> Generate a certificate based on the supplied CSR.\n\n**-CAROOT**\n> Print the CA certificate and key storage location.\n\n**--help**\n> Display help information."],["DESCRIPTION","**mkcert** creates locally-trusted certificates. It simplifies HTTPS for development.\n\nThe tool installs a local CA and generates trusted certificates. No browser warnings."],["CAVEATS","Development only. Keep root CA secure. Don't share CA key."],["HISTORY","mkcert was created by **Filippo Valsorda** to simplify local HTTPS development with trusted certificates."],["INSTALL","```apt: sudo apt install mkcert```\n\n```dnf: sudo dnf install mkcert```\n\n```pacman: sudo pacman -S mkcert```\n\n```zypper: sudo zypper install mkcert```\n\n```brew: brew install mkcert```\n\n```nix: nix profile install nixpkgs#mkcert```"],["SEE ALSO","[openssl](/man/openssl)(1), [certbot](/man/certbot)(1)"]],"e":["openssl","certbot"],"x":"mkcert -install"},"notes":{"n":"notes","t":"simple command-line note-taking application","s":[["TAGLINE","simple command-line note-taking application"],["TLDR","**Create new note**\n\n```notes new \"[note title]\"```\n\n**List all notes**\n\n```notes ls```\n\n**Search notes**\n\n```notes search \"[query]\"```\n\n**Edit note**\n\n```notes edit \"[note title]\"```\n\n**Delete note**\n\n```notes rm \"[note title]\"```\n\n**Sync notes**\n\n```notes sync```"],["SYNOPSIS","**notes** _command_ [_options_]"],["PARAMETERS","**new** _title_\n> Create new note.\n\n**ls**\n> List notes.\n\n**search** _query_\n> Search notes.\n\n**edit** _title_\n> Edit note.\n\n**rm** _title_\n> Delete note.\n\n**sync**\n> Sync with remote.\n\n**cat** _title_\n> Display note."],["DESCRIPTION","**notes** is a simple command-line note-taking application. It stores notes as plain text files, making them easy to manage, search, and sync.\n\nNotes are typically stored in ~/.notes or a configured directory."],["EXAMPLE WORKFLOW","```bash"],["Create a note","notes new \"meeting-notes\""],["List notes","notes ls"],["Find notes","notes search \"project\""],["View note","notes cat \"meeting-notes\"\n```"],["CAVEATS","Implementation varies (several tools named \"notes\"). Plain text storage. Editor configured via $EDITOR."],["HISTORY","Various command-line note tools exist; implementations include those by **Nick Nisi** and others, following Unix philosophy of simple text file management."],["INSTALL","```nix: nix profile install nixpkgs#notes```"],["SEE ALSO","[nb](/man/nb)(1), [jrnl](/man/jrnl)(1), [vim](/man/vim)(1)"]],"e":["nb","jrnl","vim"],"x":"notes new \"[note title]\""},"nstat":{"n":"nstat","t":"Display network statistics from /proc/net/netstat and /proc/net/snmp","s":[["TAGLINE","Display network statistics from /proc/net/netstat and /proc/net/snmp"],["TLDR","View network statistics **since last time** nstat was run\n\n```nstat```\n\nView **all-time** network statistics\n\n```nstat -a```\n\nShow statistics **without updating** history\n\n```nstat -s```\n\nFilter counters by **pattern**\n\n```nstat Tcp```\n\nOutput in **JSON** format\n\n```nstat -j```"],["SYNOPSIS","**nstat** [_options_] [_PATTERN_...]"],["PARAMETERS","**-a, --ignore**\n> Show absolute values (all-time statistics) instead of incremental\n\n**-n, --nooutput**\n> Don't display header or output (just reset counters)\n\n**-r, --reset**\n> Reset history\n\n**-s, --noupdate**\n> Don't update history (show stats without resetting)\n\n**-z, --zeros**\n> Show entries with zero values\n\n**-j, --json**\n> Output in JSON format\n\n**-p, --pretty**\n> Pretty print JSON output (use with -j)\n\n**-d, --scan** _INTERVAL_\n> Run in daemon mode, collecting statistics every INTERVAL seconds\n\n**-t, --interval** _INTERVAL_\n> Time interval to average rates (default: 60 seconds)\n\n**-h, --help**\n> Display help information"],["DESCRIPTION","**nstat** displays network statistics from /proc/net/netstat and /proc/net/snmp, showing IP, ICMP, TCP, and UDP counters. By default, it shows only the changes since the last time nstat was run, making it useful for monitoring network activity over time.\n\nThe tool maintains a history file (~/.nstat.u) to track previous values. Running nstat shows the difference (increment) since the last run, then updates the history. Use **-s** to view without updating.\n\nStatistics include packet counts, errors, retransmissions, and various protocol-specific metrics useful for network troubleshooting."],["CAVEATS","Incremental mode depends on the history file; first run shows all zeros. The **-a** flag shows totals since boot, ignoring history. Part of the iproute2 package."],["INSTALL","```apt: sudo apt install iproute2```\n\n```pacman: sudo pacman -S iproute2```\n\n```apk: sudo apk add iproute2```\n\n```zypper: sudo zypper install iproute2```\n\n```brew: brew install iproute2```\n\n```nix: nix profile install nixpkgs#iproute2```"],["SEE ALSO","[ss](/man/ss)(8), [ip](/man/ip)(8), [netstat](/man/netstat)(8), [sar](/man/sar)(1)"]],"e":["ss","ip","netstat","sar"],"x":"nstat"},"papis":{"n":"papis","t":"command-line bibliography manager","s":[["TAGLINE","command-line bibliography manager"],["TLDR","**Add document from file**\n\n```papis add [document.pdf]```\n\n**Add with metadata**\n\n```papis add --set author \"[Name]\" --set title \"[Title]\" [document.pdf]```\n\n**Add a document from a DOI**\n\n```papis add --from doi [10.1007/s00214-007-0422-6]```\n\n**Search and open a document**\n\n```papis open \"[query]\"```\n\n**Export to BibTeX**\n\n```papis export --format bibtex \"[query]\"```\n\n**Edit document metadata**\n\n```papis edit \"[query]\"```\n\n**List all documents matching a query**\n\n```papis list \"[query]\"```"],["SYNOPSIS","**papis** _command_ [_options_] [_args_]"],["PARAMETERS","**add**\n> Add document.\n\n**open**\n> Open document.\n\n**edit**\n> Edit metadata.\n\n**export**\n> Export references.\n\n**list**\n> List documents.\n\n**browse**\n> Open in browser.\n\n**rm**\n> Remove document.\n\n**rename**\n> Rename document folder.\n\n**doctor**\n> Check and fix document metadata.\n\n**cite**\n> Retrieve citation information.\n\n**--set** _field_ _value_\n> Set metadata field.\n\n**-l** **--lib** _library_\n> Use specified library.\n\n**--from** _importer_\n> Import from source: doi, arxiv, bibtex, crossref, pmid, yaml, folder, pdf2doi."],["DESCRIPTION","**papis** is a command-line bibliography manager. It stores documents and metadata in a folder structure, supports multiple libraries, and integrates with editors and reference managers."],["LIBRARY STRUCTURE","```\n~/Documents/papers/\n├── author2023title/\n│   ├── info.yaml\n│   └── document.pdf\n```"],["CONFIGURATION","```yaml"],["~/.config/papis/config","[papers]\ndir = ~/Documents/papers\n\n[books]\ndir = ~/Documents/books\n```"],["CAVEATS","Python required. Multiple library support. Integrates with Vim, Emacs, and offers TUI interfaces. DOI and arXiv lookup supported."],["HISTORY","papis was created by **Alejandro Gallo** as a command-line alternative to GUI reference managers like Zotero."],["INSTALL","```brew: brew install papis```\n\n```nix: nix profile install nixpkgs#papis```"],["SEE ALSO","[zotero](/man/zotero)(1), [jabref](/man/jabref)(1), [mendeley](/man/mendeley)(1)"]],"e":["zotero","jabref","mendeley"],"x":"papis add [document.pdf]"},"pdfcpu":{"n":"pdfcpu","t":"PDF processing toolkit for the command line","s":[["TAGLINE","PDF processing toolkit for the command line"],["TLDR","**Validate** a PDF\n\n```pdfcpu validate [input.pdf]```\n\n**Merge** PDFs into one file\n\n```pdfcpu merge [out.pdf] [in1.pdf] [in2.pdf]```\n\n**Split** a PDF into single pages\n\n```pdfcpu split [input.pdf] [out_dir]```\n\n**Optimize** (compress) a PDF\n\n```pdfcpu optimize [input.pdf] [out.pdf]```\n\n**Encrypt** a PDF\n\n```pdfcpu encrypt -upw [user] -opw [owner] [input.pdf]```\n\n**Add a watermark** string\n\n```pdfcpu watermark add -mode text -- \"[text]\" [input.pdf] [out.pdf]```\n\n**Extract** pages to a new PDF\n\n```pdfcpu trim -pages [1-3] [input.pdf] [out.pdf]```"],["SYNOPSIS","**pdfcpu** *command* [*options*] [*file*...]"],["DESCRIPTION","**pdfcpu** is a PDF toolkit written in Go. It provides both a Go API and a rich CLI for validating, merging, splitting, optimizing, encrypting, stamping, watermarking, rotating, cropping, extracting images/fonts, managing bookmarks, and many other PDF operations without requiring Ghostscript or Adobe tools.\n\nEach verb (**validate**, **merge**, **split**, **optimize**, **encrypt**, **watermark**, **stamp**, **pages**, **trim**, **rotate**, **extract**, etc.) has its own flags. Run **pdfcpu** *command* **help** or see https://pdfcpu.io for the full command matrix."],["PARAMETERS","**validate** *file*\n\n> Check PDF integrity / compliance related checks.\n\n**merge** *out* *in*...\n\n> Concatenate input PDFs into *out*.\n\n**split** *file* *out_dir*\n\n> Split into page-level PDFs.\n\n**optimize** *in* [*out*]\n\n> Reduce size via resource optimization.\n\n**encrypt** / **decrypt** / **changeopw** / **changeupw**\n\n> Password and permission management (**-upw**, **-opw**).\n\n**watermark** / **stamp**\n\n> Add text or image watermarks/stamps.\n\n**trim** **-pages** *range*\n\n> Keep selected pages.\n\n**rotate**, **resize**, **nup**, **poster**, **booklet**\n\n> Page geometry and imposition helpers.\n\n**extract**\n\n> Extract images, fonts, content, pages, and more.\n\n**version**, **help**\n\n> Version and command help."],["CAVEATS","Not all PDF features (especially exotic encryption, signed forms, or proprietary constructs) are fully supported. Always keep originals before bulk transforms. Some operations rewrite structure and may affect advanced interactive features."],["INSTALL","```apk: sudo apk add pdfcpu```\n\n```brew: brew install pdfcpu```\n\n```nix: nix profile install nixpkgs#pdfcpu```"],["SEE ALSO","[qpdf](/man/qpdf)(1), [pdftk](/man/pdftk)(1), [gs](/man/gs)(1), [mutool](/man/mutool)(1)"],["RESOURCES","```[Source code](https://github.com/pdfcpu/pdfcpu)```\n\n```[Homepage](https://pdfcpu.io)```\n\n```[Documentation](https://pdfcpu.io)```"]],"e":["qpdf","pdftk","gs","mutool"],"x":"pdfcpu validate [input.pdf]"},"peeked":{"n":"peeked","t":"CLI for letting AI coding agents capture screenshots of browser tabs","s":[["TAGLINE","CLI for letting AI coding agents capture screenshots of browser tabs"],["TLDR","**Start** the WebSocket daemon that the browser extension connects to\n\n```peeked start```\n\n**List** currently tracked browser tab URLs\n\n```peeked list```\n\n**Capture** a screenshot of the page at a given URL (extension must be connected)\n\n```peeked at http://localhost:3000```\n\n**Check** server and extension connection status\n\n```peeked status```\n\n**Stop** the WebSocket server\n\n```peeked stop```"],["SYNOPSIS","**peeked** _command_ [arguments]"],["COMMANDS","**start**\n\n> Start the local WebSocket server (on port 7336) that receives screenshot requests and streams to the browser extension. Cleans previous images on start.\n\n**list**\n\n> List all tracked open tab URLs reported by the connected browser extension.\n\n**at** _url_\n\n> Request a screenshot of the specified URL from the connected browser tab. Saves the JPEG to a temporary directory (e.g. `$TMPDIR/peek_cli/images/`).\n\n**status**\n\n> Report whether the server is running and whether a browser extension is connected.\n\n**stop**\n\n> Stop the running WebSocket server process."],["DESCRIPTION","`peeked` is the command-line component of peek-cli. It lets AI coding agents (such as Claude Code, Codex, and others) obtain live screenshots of open browser tabs.\n\nThe tool consists of a local WebSocket server started by `peeked start`. A companion Chrome extension connects to the server and forwards screenshots on demand. Agents issue commands through the CLI (or via agent skills/plugins) to list tabs and request captures.\n\nScreenshots are delivered as base64 and written locally by the CLI; the agent never directly accesses the browser or injects code.\n\nInstallation:\n\n```bash\nnpm i -g peeked\n```\n\nA matching browser extension must also be installed from the Chrome Web Store."],["CAVEATS","Requires the official Peek CLI Chrome extension; without it the server has no tabs to report or capture from.\n\nThe server binds only to localhost. You must explicitly start it on every machine/session before use.\n\nScreenshots are written to a per-user temp directory and are not automatically cleaned except on `start`.\n\nError messages may refer to outdated command names in some versions (e.g. \"peeked begin\").\n\nOnly provides read-only screenshot capability; no navigation, input, or page manipulation is possible."],["SEE ALSO","[claude](/man/claude)(1), [codex](/man/codex)(1)"],["RESOURCES","```[Source code](https://github.com/puffinsoft/peek-cli)```"]],"e":["claude","codex"],"x":"peeked start"},"pg_walsummary":{"n":"pg_walsummary","t":"displays WAL summary file contents","s":[["TAGLINE","displays WAL summary file contents"],["TLDR","**Show WAL summary**\n\n```pg_walsummary [summary_file]```\n\n**Show with individual block detail**\n\n```pg_walsummary -i [summary_file]```\n\n**Validate a WAL summary file without printing contents**\n\n```pg_walsummary -q [summary_file]```"],["SYNOPSIS","**pg_walsummary** [_options_] [_file_...]"],["PARAMETERS","**-i**, **--individual**\n> Print a separate line for every modified block instead of consolidating consecutive blocks into ranges.\n\n**-q**, **--quiet**\n> Do not print any output, except for errors. Useful for validating that a file can be parsed.\n\n**-V**, **--version**\n> Display version information, then exit."],["DESCRIPTION","**pg_walsummary** prints the contents of WAL summary files. These binary files are found in the pg_wal/summaries subdirectory of the data directory and track which blocks were modified, supporting the incremental backup feature introduced in PostgreSQL 17."],["INSTALL","```dnf: sudo dnf install postgresql17```\n\n```apk: sudo apk add postgresql17```\n\n```zypper: sudo zypper install postgresql17```"],["SEE ALSO","[pg_waldump](/man/pg_waldump)(1), [pg_combinebackup](/man/pg_combinebackup)(1), [pg_basebackup](/man/pg_basebackup)(1)"]],"e":["pg_waldump","pg_combinebackup","pg_basebackup"],"x":"pg_walsummary [summary_file]"},"phpcs":{"n":"phpcs","t":"PHP CodeSniffer","s":[["TAGLINE","PHP CodeSniffer"],["TLDR","**Check file**\n\n```phpcs [file.php]```\n\n**Check directory**\n\n```phpcs [src/]```\n\n**Use specific standard**\n\n```phpcs --standard=PSR12 [file.php]```\n\n**Show summary only**\n\n```phpcs --report=summary [src/]```\n\n**Generate report**\n\n```phpcs --report-file=[report.txt] [src/]```"],["SYNOPSIS","**phpcs** [_options_] [_files_...]"],["PARAMETERS","_FILES_\n> Files or directories.\n\n**--standard** _NAME_\n> Coding standard.\n\n**--report** _TYPE_\n> Report format.\n\n**--report-file** _FILE_\n> Save report to file.\n\n**-n**\n> Ignore warnings.\n\n**--help**\n> Display help."],["DESCRIPTION","**phpcs** is PHP CodeSniffer. Detects coding standard violations.\n\nThe tool checks code style. Supports PSR, PEAR, and custom standards."],["CAVEATS","Configuration via phpcs.xml. Custom standards possible."],["HISTORY","PHP_CodeSniffer was created for **enforcing PHP coding standards**."],["SEE ALSO","[phpcbf](/man/phpcbf)(1), [php-cs-fixer](/man/php-cs-fixer)(1), [phpmd](/man/phpmd)(1)"]],"e":["phpcbf","php-cs-fixer","phpmd"],"x":"phpcs [file.php]"},"pnmtorast":{"n":"pnmtorast","t":"Convert PNM to Sun rasterfile format","s":[["TAGLINE","Convert PNM to Sun rasterfile format"],["TLDR","**Convert PNM to Sun raster**\n\n```pnmtorast [input.pnm] > [output.ras]```\n\n**Specify encoding**\n\n```pnmtorast -rle [input.pnm] > [output.ras]```"],["SYNOPSIS","**pnmtorast** [_options_] [_file_]"],["PARAMETERS","**-rle**\n> Use RLE compression.\n\n**-standard**\n> Use standard format."],["DESCRIPTION","**pnmtorast** converts PNM images to Sun Rasterfile format. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[rasttopnm](/man/rasttopnm)(1), [pnmtoxwd](/man/pnmtoxwd)(1)"]],"e":["rasttopnm","pnmtoxwd"],"x":"pnmtorast [input.pnm] > [output.ras]"},"podman-tui":{"n":"podman-tui","t":"Terminal UI for Podman containers","s":[["TAGLINE","Terminal UI for Podman containers"],["TLDR","**Start** the Podman TUI\n\n```podman-tui```\n\n**Ensure** the Podman API socket is running (user)\n\n```systemctl --user start podman.socket```\n\n**Or** start the API service without systemd\n\n```podman system service --time=0```"],["SYNOPSIS","**podman-tui**"],["DESCRIPTION","**podman-tui** is a terminal user interface for the Podman environment. It uses Podman Go bindings to talk to a local or remote Podman machine (including over SSH). You can browse and manage containers, images, pods, volumes, networks, and related resources without leaving the terminal.\n\nCompatibility: release-2.x targets Podman v6, release-1.x targets Podman v5, release-0.x targets Podman v4. Install from packages or build from source (see upstream install guide)."],["PARAMETERS","Interactive application; primary control is via key bindings rather than CLI subcommands. Common keys:\n\n**m**\n\n> Command menu\n\n**s**\n\n> Sort menu\n\n**l** / **h**\n\n> Next / previous screen\n\n**k** / **j**\n\n> Move up / down\n\n**Tab**\n\n> Switch widgets\n\n**Esc**\n\n> Close active dialog\n\n**Ctrl+c**\n\n> Exit"],["CAVEATS","Requires **podman.socket** (or **podman system service**) for the API. SSH key passphrases need **CONTAINER_PASSPHRASE**. Uses 256-color mode on Unix-like systems. Match **podman-tui** major version to your Podman major version."],["INSTALL","```dnf: sudo dnf install podman-tui```\n\n```apk: sudo apk add podman-tui```\n\n```brew: brew install podman-tui```\n\n```nix: nix profile install nixpkgs#podman-tui```"],["SEE ALSO","[podman](/man/podman)(1), [docker](/man/docker)(1), [lazydocker](/man/lazydocker)(1), [ctop](/man/ctop)(1)"],["RESOURCES","```[Source code](https://github.com/containers/podman-tui)```\n\n```[Homepage](https://podman.io)```"]],"e":["podman","docker","lazydocker","ctop"],"x":"podman-tui"},"poetry-config":{"n":"poetry-config","t":"Manage Poetry configuration settings","s":[["TAGLINE","Manage Poetry configuration settings"],["TLDR","**List configuration**\n\n```poetry config --list```\n\n**Set configuration**\n\n```poetry config [key] [value]```\n\n**Set repository URL**\n\n```poetry config repositories.[name] [https://repo.example.com]```\n\n**Set PyPI token**\n\n```poetry config pypi-token.pypi [token]```\n\n**Unset configuration**\n\n```poetry config --unset [key]```"],["SYNOPSIS","**poetry config** [_options_] [_key_] [_value_]"],["PARAMETERS","_KEY_\n> Configuration key.\n\n_VALUE_\n> Configuration value.\n\n**--list**\n> Show all configuration.\n\n**--unset**\n> Remove configuration.\n\n**--local**\n> Project-specific config."],["DESCRIPTION","**poetry config** reads and writes Poetry configuration settings including virtual environment preferences, repository URLs, and authentication tokens. Use **--list** to view all current settings.\n\nSettings are stored globally by default. The **--local** flag creates a **poetry.toml** file in the project directory for project-specific overrides. Common settings include `virtualenvs.in-project`, `repositories`, and `pypi-token`. Use **--unset** to remove a configuration key."],["CAVEATS","Tokens stored in config. Use --local for project settings."],["HISTORY","poetry config provides **configuration management** for Poetry settings."],["INSTALL","```dnf: sudo dnf install poetry```\n\n```apk: sudo apk add poetry```\n\n```brew: brew install poetry```\n\n```nix: nix profile install nixpkgs#poetry```"],["SEE ALSO","[poetry](/man/poetry)(1), [poetry-publish](/man/poetry-publish)(1)"]],"e":["poetry","poetry-publish"],"x":"poetry config --list"},"prek":{"n":"prek","t":"Fast pre-commit hook manager written in Rust","s":[["TAGLINE","Fast pre-commit hook manager written in Rust"],["TLDR","**Install** prek hooks into the current Git repository\n\n```prek install```\n\n**Run** all configured hooks against changed files\n\n```prek run```\n\n**Run** all hooks against every tracked file in the repo\n\n```prek run --all-files```\n\n**Run** a specific hook only\n\n```prek run [hook_id]```\n\n**Auto-update** hook revisions to their latest tags\n\n```prek auto-update```\n\n**List** every hook resolved from the configuration\n\n```prek list```\n\n**Convert** a legacy YAML config to native TOML\n\n```prek util yaml-to-toml```\n\n**Uninstall** previously installed Git hooks\n\n```prek uninstall```"],["SYNOPSIS","**prek** [_global-options_] _command_ [_command-options_] [_args_]"],["PARAMETERS","**install** [**--hook-type** _type_] [**--overwrite**]\n> Install Git hooks into _.git/hooks_. Repeat **--hook-type** to install pre-commit, commit-msg, pre-push, etc.\n\n**uninstall** [**--hook-type** _type_]\n> Remove previously installed hooks while leaving prek's cache intact.\n\n**run** [**--all-files**] [**--files** _path..._] [**--hook-stage** _stage_] [**--from-ref** _ref_] [**--to-ref** _ref_] [**--last-commit**] [**--show-diff-on-failure**] [**--no-fail-fast**] [_hook-id_]\n> Execute hooks. With no arguments, runs against staged files; with **--all-files**, runs against every tracked file.\n\n**auto-update** [**--check**] [**--bleeding-edge**] [**--freeze**] [**--cooldown-days** _N_] [**--repo** _url_]\n> Update **rev:** entries in the configuration to the latest released tag. **--check** validates without rewriting.\n\n**list** [**--hook-stage** _stage_]\n> Show the resolved list of hooks for the active configuration.\n\n**clean**\n> Remove cached hook environments from prek's store.\n\n**gc**\n> Garbage-collect unused repos from prek's cache.\n\n**init-templatedir** _directory_\n> Populate a Git template directory so newly cloned repos pick up hooks automatically.\n\n**try-repo** _repo_ [**--ref** _ref_]\n> Try a hook repository without modifying the project configuration.\n\n**util yaml-to-toml** [_path_]\n> Convert _.pre-commit-config.yaml_ into the native _prek.toml_ format.\n\n**util list-builtins**\n> List built-in hooks reimplemented in Rust.\n\n**util identify** _file..._\n> Print the file types prek associates with the given paths (useful for debugging _types_ filters).\n\n**self update**\n> Upgrade the prek binary itself to the latest release.\n\n**-c** _file_, **--config** _file_\n> Use _file_ as the configuration instead of auto-discovery.\n\n**-v**, **--verbose**\n> Increase log verbosity. Repeat for debug-level output.\n\n**-q**, **--quiet**\n> Suppress non-essential output.\n\n**--color** _when_\n> _auto_, _always_, or _never_.\n\n**-V**, **--version**\n> Print the version and exit."],["DESCRIPTION","**prek** is a Git hook manager designed as a faster, drop-in replacement for **pre-commit**. It reads the same _.pre-commit-config.yaml_ files and supports the same hook ecosystem, while shipping as a single static binary written in **Rust** with no Python runtime dependency.\n\nprek caches hook environments under _~/.cache/prek_ and reuses them across repositories. Several common hooks (file fixers, JSON/YAML validators, etc.) are reimplemented in Rust and run as built-ins, avoiding the cost of spawning interpreters. Toolchain installs for **Python**, **Node.js**, **Bun**, **Go**, **Rust**, and **Ruby** are shared across hooks, and Python environments are managed through **uv**.\n\nIn addition to the legacy YAML schema, prek understands a native _prek.toml_ format and provides _prek util yaml-to-toml_ to migrate existing configurations. Workspace mode lets a single config drive hooks across multiple sub-projects in a monorepo, with **--project** and **--skip-project** filters on the command line."],["CONFIGURATION","prek reads the first of these files in the repository root:\n\n```\nprek.toml\n.prek.toml\n.pre-commit-config.yaml\n```\n\nBoth YAML and TOML configurations declare a list of **repos**, each pinned to a **rev** and exposing one or more **hooks** by **id**. Per-hook keys such as **files**, **exclude**, **types**, **stages**, **language**, **args**, and **additional_dependencies** are accepted unchanged from pre-commit.\n\nUser-level overrides live at _~/.config/prek/config.toml_. The **PREK_HOME** environment variable relocates the cache directory; **PREK_NO_COLOR** and **NO_COLOR** disable ANSI output."],["CAVEATS","prek aims for behavioural parity with pre-commit but is not byte-identical: Python virtualenvs are managed by **uv**, not _virtualenv_, which can change resolved package versions. Hooks that rely on system Python being on **PATH** during the hook script may need explicit dependencies. The cache layout under _~/.cache/prek_ is independent of pre-commit's, so both tools can coexist but do not share environments."],["HISTORY","**prek** is authored by **Jian Zeng (j178)** and published as a Rust crate as well as a Python wheel on PyPI for installation via **pip**, **uv**, or **pipx**. It positions itself as a \"**pre-commit, reimagined**\", reusing the existing hook ecosystem while replacing the runtime. Early adopters include large Python projects such as **CPython**, **Apache Airflow**, and **FastAPI**."],["INSTALL","```zypper: sudo zypper install prek```\n\n```brew: brew install prek```\n\n```nix: nix profile install nixpkgs#prek```"],["SEE ALSO","[pre-commit](/man/pre-commit)(1), [husky](/man/husky)(1), [lefthook](/man/lefthook)(1), [uv](/man/uv)(1)"]],"e":["pre-commit","husky","lefthook","uv"],"x":"prek install"},"proxychains":{"n":"proxychains","t":"Route TCP connections through proxy servers","s":[["TAGLINE","Route TCP connections through proxy servers"],["TLDR","**Run a command** through the proxy\n\n```proxychains [command]```\n\n**Run curl through proxy**\n\n```proxychains curl [url]```\n\n**Use a specific config** file\n\n```proxychains -f [path/to/config.conf] [command]```\n\n**Quiet mode** (suppress proxy info output)\n\n```proxychains -q [command]```\n\n**SSH through proxy**\n\n```proxychains ssh [user]@[host]```\n\n**Chain proxychains with a browser**\n\n```proxychains firefox```"],["SYNOPSIS","**proxychains** [**-q**] [**-f** _config_file_] _program_ [_args_]"],["PARAMETERS","**-q**\n> Quiet mode. Do not display proxy connection information.\n\n**-f** _config_file_\n> Use specified configuration file instead of the default. Without this flag, proxychains searches in order: the path in the **PROXYCHAINS_CONF_FILE** environment variable, **./proxychains.conf**, **~/.proxychains/proxychains.conf**, and **/etc/proxychains.conf**."],["DESCRIPTION","**proxychains** forces TCP connections made by a given application to go through proxy servers such as SOCKS4, SOCKS5, or HTTP proxies. It hooks network-related libc functions using **LD_PRELOAD** to redirect traffic transparently without modifying the target application.\n\nUseful for anonymization, routing traffic through Tor, and bypassing network restrictions. Supported authentication types are user/pass for SOCKS4/5 and basic for HTTP.\n\nThe modern maintained version is **proxychains-ng** (proxychains4) by **rofl0r**, which is a continuation of the original project."],["CONFIGURATION","```"],["/etc/proxychains.conf","strict_chain\nproxy_dns\n\n[ProxyList]\nsocks5 127.0.0.1 9050\nhttp   192.168.1.1 8080\n```"],["CHAIN TYPES","```\nstrict_chain  - All proxies in order, fail if any is down\ndynamic_chain - Skip dead proxies, at least one must respond\nrandom_chain  - Random proxy order each connection\nround_robin   - Cycle through proxies in order\n```"],["CAVEATS","Only TCP connections are supported. UDP and ICMP traffic is not proxied. Statically linked programs bypass the LD_PRELOAD hook. DNS requests can be proxied by enabling **proxy_dns** in the configuration file."],["HISTORY","proxychains was originally written by **haad** and later forked as **proxychains-ng** (proxychains4) by **rofl0r** with improvements including better stability and additional features."],["INSTALL","```dnf: sudo dnf install proxychains-ng```\n\n```pacman: sudo pacman -S proxychains-ng```\n\n```apk: sudo apk add proxychains-ng```\n\n```zypper: sudo zypper install proxychains-ng```\n\n```brew: brew install proxychains-ng```\n\n```nix: nix profile install nixpkgs#proxychains-ng```"],["SEE ALSO","[tor](/man/tor)(1), [torsocks](/man/torsocks)(1), [socat](/man/socat)(1), [ssh](/man/ssh)(1), [curl](/man/curl)(1)"]],"e":["tor","torsocks","socat","ssh","curl"],"x":"proxychains [command]"},"pystun3":{"n":"pystun3","t":"Discover NAT type using STUN protocol","s":[["TAGLINE","Discover NAT type using STUN protocol"],["TLDR","**Check NAT type**\n\n```pystun3```\n\n**Use specific STUN server**\n\n```pystun3 -H [stun.example.com]```\n\n**Specify source port**\n\n```pystun3 -p [54320]```\n\n**Use specific interface**\n\n```pystun3 -i [192.168.1.100]```\n\n**Enable debug logging**\n\n```pystun3 -d```\n\n**Specify STUN server port**\n\n```pystun3 -H [stun.example.com] -P [3478]```"],["SYNOPSIS","**pystun3** [_-H server_] [_-p port_] [_-i ip_] [_options_]"],["PARAMETERS","**-H** _STUN_HOST_, **--host** _STUN_HOST_\n> STUN server hostname.\n\n**-P** _STUN_PORT_, **--host-port** _STUN_PORT_\n> STUN server port (default: 3478).\n\n**-p** _SOURCE_PORT_, **--port** _SOURCE_PORT_\n> Source port to listen on (default: 54320).\n\n**-i** _SOURCE_IP_, **--interface** _SOURCE_IP_\n> Network interface for client (default: 0.0.0.0).\n\n**-d**, **--debug**\n> Enable debug logging.\n\n**--version**\n> Show program version."],["DESCRIPTION","**pystun3** is a STUN (Session Traversal Utilities for NAT) client that discovers the type of NAT between a host and the internet. It communicates with a STUN server to determine the NAT classification (full cone, restricted cone, port restricted, or symmetric) and reports the external IP address and mapped port.\n\nUnderstanding NAT type is essential for configuring VoIP, video conferencing, and peer-to-peer applications that require direct connections between hosts. The tool can target specific STUN servers and bind to particular network interfaces."],["CAVEATS","Requires network access. STUN server must be reachable. NAT types vary."],["HISTORY","**pystun3** is a Python 3 port of pystun, implementing STUN (Session Traversal Utilities for NAT) client functionality."],["SEE ALSO","[stun](/man/stun)(1), [nmap](/man/nmap)(1), [netcat](/man/netcat)(1)"]],"e":["stun","nmap","netcat"],"x":"pystun3"},"rails-server":{"n":"rails-server","t":"Start the Rails development web server","s":[["TAGLINE","Start the Rails development web server"],["TLDR","**Start the Rails development server**\n\n```rails server```\n\n**Start the server on a specific port**\n\n```rails server -p [port]```\n\n**Start the server binding to all interfaces**\n\n```rails server -b 0.0.0.0```\n\n**Start the server in production environment**\n\n```rails server -e production```\n\n**Start using a specific server** (Puma, Thin, etc.)\n\n```rails server -u [puma|thin|webrick]```\n\n**Start in daemon mode** (background)\n\n```rails server -d```"],["SYNOPSIS","**rails server** [**-p** _port_] [**-b** _address_] [**-e** _environment_] [**-d**] [**-u** _server_]"],["PARAMETERS","**-p**, **--port** _PORT_\n> Run server on specified port (default: 3000)\n\n**-b**, **--binding** _IP_\n> Bind to specified IP address (default: localhost)\n\n**-e**, **--environment** _ENV_\n> Run in specified environment (development, test, production)\n\n**-d**, **--daemon**\n> Run server as a daemon (background process)\n\n**-u**, **--using** _SERVER_\n> Use specified Rack server (puma, thin, webrick, etc.)\n\n**-P**, **--pid** _FILE_\n> Specify PID file path\n\n**-C**, **--dev-caching**\n> Toggle development mode caching\n\n**--early-hints**\n> Enable HTTP/2 early hints\n\n**-h**, **--help**\n> Show help information"],["DESCRIPTION","**rails server** starts a web server for the Rails application. By default, it uses Puma and listens on http://localhost:3000 in the development environment.\n\nThe server automatically reloads code changes in development mode, allowing rapid iteration without restarts. It displays request logs in the terminal, showing HTTP methods, paths, response codes, and timing information.\n\nBinding to 0.0.0.0 makes the server accessible from other machines on the network, useful for testing on mobile devices or from other computers."],["CAVEATS","The default binding to localhost (127.0.0.1) only accepts connections from the local machine. Use **-b 0.0.0.0** to accept external connections, but be aware of security implications.\n\nDevelopment mode is not suitable for production. It lacks performance optimizations and may expose debugging information. Always use **-e production** with a proper production configuration for deployed applications.\n\nThe daemon mode (**-d**) requires manual process management. Use process managers like systemd or Passenger for production deployments."],["SEE ALSO","[rails](/man/rails)(1), [puma](/man/puma)(1), [rails-routes](/man/rails-routes)(1), [bundle](/man/bundle)(1)"]],"e":["rails","puma","rails-routes","bundle"],"x":"rails server"},"reboot":{"n":"reboot","t":"Restart the operating system","s":[["TAGLINE","Restart the operating system"],["TLDR","**Reboot** the system\n\n```reboot```\n\n**Power off** the system\n\n```reboot --poweroff```\n\n**Halt** the system without powering off\n\n```reboot --halt```\n\n**Force** immediate reboot without init\n\n```reboot --force```\n\nWrite **wtmp entry only** without rebooting\n\n```reboot --wtmp-only```\n\nReboot without **wall message**\n\n```reboot --no-wall```"],["SYNOPSIS","**reboot** [_OPTIONS_...]"],["DESCRIPTION","**reboot** restarts the machine. It communicates with the system manager (systemd) to perform the reboot. The command accepts the same options as the related **poweroff** and **halt** commands."],["PARAMETERS","**--halt**\n> Halt the machine instead of rebooting\n\n**-p, --poweroff**\n> Power off the machine instead of rebooting\n\n**--reboot**\n> Reboot the machine (default action)\n\n**-f, --force**\n> Force immediate reboot without contacting the init system\n\n**-w, --wtmp-only**\n> Only write the wtmp shutdown entry, do not actually reboot\n\n**-d, --no-wtmp**\n> Do not write a wtmp shutdown entry\n\n**-n, --no-sync**\n> Do not sync hard disks before reboot\n\n**--no-wall**\n> Do not send wall message before reboot"],["CAVEATS","Using **--force** bypasses the init system and may result in data loss if filesystems are not properly synced. The **systemctl** command provides equivalent functionality with additional features."],["HISTORY","**reboot** has been available since early Unix systems. On modern systemd-based systems, it is typically a symlink to **systemctl**."],["INSTALL","```apt: sudo apt install runit-init```"],["SEE ALSO","[poweroff](/man/poweroff)(8), [halt](/man/halt)(8), [systemctl](/man/systemctl)(1), [shutdown](/man/shutdown)(8)"]],"e":["poweroff","halt","systemctl","shutdown"],"x":"reboot"},"repart.d":{"n":"repart.d","t":"Declarative disk partition definitions","s":[["TAGLINE","Declarative disk partition definitions"],["TLDR","**Apply partition definitions**\n\n```sudo systemd-repart --dry-run```\n\n**Run repart**\n\n```sudo systemd-repart```\n\n**Show changes without applying**\n\n```systemd-repart --dry-run --no-pager```"],["SYNOPSIS","**/etc/repart.d/*.conf** configuration files"],["DESCRIPTION","**repart.d** contains configuration files for systemd-repart, which declaratively manages GPT partition tables. It can grow, create, or modify partitions on disk."],["CONFIGURATION","```ini"],["/etc/repart.d/50-root.conf","[Partition]\nType=root\nFormat=ext4\nLabel=root\nSizeMinBytes=10G\nSizeMaxBytes=50G\n```"],["PARTITION TYPES","```\nroot          - Root partition\nhome          - Home partition\nsrv           - Server data\nvar           - Variable data\ntmp           - Temporary files\nswap          - Swap space\nesp           - EFI System Partition\n```"],["EXAMPLES","```bash"],["Dry run","systemd-repart --dry-run"],["Apply changes","systemd-repart"],["Create partition config","cat > /etc/repart.d/50-home.conf << EOF\n[Partition]\nType=home\nFormat=ext4\nLabel=home\nSizeMinBytes=20G\nEOF\n```"],["OPTIONS","```ini\n[Partition]\nType=home\nFormat=ext4\nLabel=home\nSizeMinBytes=1G\nSizeMaxBytes=50G\nWeight=1000\nCopyFiles=/source:/dest\nMakeDirectories=/home\nVerity=off\nFactoryReset=no\nFlags=\n```\n\n**Type=**\n> GPT partition type (root, home, srv, var, tmp, swap, esp, etc.).\n\n**Format=**\n> Filesystem to create (ext4, btrfs, xfs, vfat, swap, etc.).\n\n**SizeMinBytes=**, **SizeMaxBytes=**\n> Minimum and maximum partition size constraints.\n\n**Weight=**\n> Relative weight for distributing remaining free space (default 1000).\n\n**CopyFiles=**\n> Copy files from the host into the partition, in source:destination format.\n\n**MakeDirectories=**\n> Create directories within the new filesystem with mode 0755.\n\n**Verity=**\n> dm-verity mode: off, data, hash, or signature.\n\n**FactoryReset=**\n> Mark the partition for removal during factory reset."],["CAVEATS","Requires systemd 245+. Requires careful planning. Always use --dry-run first. Primarily designed for image-based deployments and A/B update schemes."],["HISTORY","systemd-repart was introduced in **systemd 245** for declarative partition management in image-based Linux systems."],["SEE ALSO","[systemd-repart](/man/systemd-repart)(8), [parted](/man/parted)(8), [gdisk](/man/gdisk)(8), [fdisk](/man/fdisk)(8), [lsblk](/man/lsblk)(8)"]],"e":["systemd-repart","parted","gdisk","fdisk","lsblk"],"x":"sudo systemd-repart --dry-run"},"rustup-completions":{"n":"rustup-completions","t":"Generate shell completions for rustup","s":[["TAGLINE","Generate shell completions for rustup"],["TLDR","**Generate bash completions**\n\n```rustup completions bash```\n\n**Generate zsh completions**\n\n```rustup completions zsh```\n\n**Generate fish completions**\n\n```rustup completions fish```\n\n**Generate cargo completions**\n\n```rustup completions bash cargo```"],["SYNOPSIS","**rustup** **completions** _shell_ [_command_]"],["PARAMETERS","**bash**\n> Bash completions.\n\n**zsh**\n> Zsh completions.\n\n**fish**\n> Fish completions.\n\n**powershell**\n> PowerShell completions.\n\n**cargo**\n> Generate for cargo instead."],["DESCRIPTION","**rustup completions** generates shell completion scripts. Outputs completion code for rustup or cargo commands. Install to appropriate shell configuration."],["SEE ALSO","[rustup](/man/rustup)(1), [cargo](/man/cargo)(1)"]],"e":["rustup","cargo"],"x":"rustup completions bash"},"samply":{"n":"samply","t":"Cross-platform CPU sampling profiler with Firefox Profiler output","s":[["TAGLINE","Cross-platform CPU sampling profiler with Firefox Profiler output"],["TLDR","**Profile a command** and open the result in the browser\n\n```samply record [path/to/command] [arg1] [arg2]```\n\n**Set the sampling rate** in hertz\n\n```samply record --rate [1000] [path/to/command]```\n\n**Record without opening** the browser, saving to a file\n\n```samply record --save-only --output [profile.json] -- [path/to/command]```\n\n**Load a previously saved** profile\n\n```samply load [path/to/profile.json]```\n\n**Import** a profile from another profiler\n\n```samply import [path/to/perf.data]```\n\n**Profile a running process** by PID (macOS/Linux)\n\n```samply record --pid [12345]```\n\n**Attach to all processes** on the system (Linux/Windows)\n\n```samply record -a```"],["SYNOPSIS","**samply**  _command_  [_options_]  [**--**]  [_program_]  [_args_ ...]"],["COMMANDS","**samply record** [_options_] **--** _command_ [_args_]\n> Spawn _command_ and record a CPU profile of its execution. When the command exits, the profile opens in **profiler.firefox.com** by default.\n\n**samply load** _profile_\n> Open a previously saved profile in the Firefox Profiler.\n\n**samply import** _file_\n> Convert a profile from a foreign format (Linux **perf.data**, Xcode .trace, ETW, etc.) and open it in the Firefox Profiler.\n\n**samply setup**\n> On macOS, sign the **samply** binary so it can attach to existing processes."],["PARAMETERS","**--rate** _hz_\n> Sampling frequency in samples per second. Default: _1000_.\n\n**--duration** _seconds_\n> Stop recording after _seconds_ of wall-clock time.\n\n**--save-only**\n> Do not open the browser; just write the profile to disk.\n\n**-o**, **--output** _file_\n> Path to the output profile (default: _profile.json.gz_).\n\n**--profile-name** _name_\n> Set the profile name shown in the Firefox Profiler UI.\n\n**--port** _n_\n> Port for the local symbol-server (default: _3000_).\n\n**--no-open**\n> Save the profile and start the local symbol-server, but do not launch a browser.\n\n**--presymbolicate**\n> Resolve symbols at save time and embed them in the profile, making it portable to machines without the original binaries.\n\n**--pid** _pid_\n> Profile an already-running process instead of spawning a new one.\n\n**-a**, **--all**\n> Profile every process on the system (system-wide profiling).\n\n**--reuse-threads**\n> Keep thread IDs stable across forks; useful for long-running daemons."],["DESCRIPTION","**samply** is a sampling profiler that runs on **macOS**, **Linux**, and **Windows** and produces profiles in the Firefox Profiler JSON format. It periodically interrupts the target process, captures one stack trace per thread, and writes the aggregated samples to disk; when recording finishes, **samply** starts a tiny local web server, opens **profiler.firefox.com** in your default browser, and the profiler fetches the data and symbols from that server.\n\nOn macOS and Windows, **samply** reports both on-CPU and off-CPU samples, so blocking on disk, network, or locks is visible in the flame graph. On Linux it currently reports on-CPU samples only, using the kernel's **perf_event_open** subsystem (no kernel module or LD_PRELOAD is required).\n\nThe output is the same JSON format used by Firefox's built-in profiler, so the resulting flame graphs, marker chains, call trees, stack charts, and source views all work out of the box, including across machines: profiles can be shared by uploading them to the public Firefox Profiler instance or by exchanging the JSON file directly."],["CAVEATS","On **Linux**, **samply** needs access to performance events. If **/proc/sys/kernel/perf_event_paranoid** is greater than _1_, recording will fail with \"permission denied\". Lower it temporarily:\n\n```echo 1 | sudo tee /proc/sys/kernel/perf_event_paranoid```\n\nor persistently via _sysctl kernel.perf_event_paranoid=1_.\n\nOn **macOS**, code-signing prevents attaching to existing processes by default. Run **samply setup** once to self-sign the binary, after which **--pid** works.\n\nSymbols are resolved on demand by the local web server **samply** starts; if you close the terminal session, the profile in the browser loses access to symbols. Use **--presymbolicate** to embed them."],["HISTORY","**samply** was created in **2021** by **Markus Stange** at **Mozilla** to give external developers access to the same profile format that the **Gecko Profiler** had been producing for Firefox since **2011**. The project lives at _github.com/mstange/samply_ and is released under MIT or Apache-2.0."],["INSTALL","```pacman: sudo pacman -S samply```\n\n```zypper: sudo zypper install samply```\n\n```brew: brew install samply```\n\n```nix: nix profile install nixpkgs#samply```"],["SEE ALSO","[perf](/man/perf)(1), [strace](/man/strace)(1), [gprof](/man/gprof)(1), [valgrind](/man/valgrind)(1), [hyperfine](/man/hyperfine)(1)"]],"e":["perf","strace","gprof","valgrind","hyperfine"],"x":"samply record [path/to/command] [arg1] [arg2]"},"sc_tracediff":{"n":"sc_tracediff","t":"Compare traceroute paths from scamper data","s":[["TAGLINE","Compare traceroute paths from scamper data"],["TLDR","**Compare two traceroute paths**\n\n```sc_tracediff [trace1.warts] [trace2.warts]```"],["SYNOPSIS","**sc_tracediff** [_options_] _file1_ _file2_"],["PARAMETERS","**-a**\n> Show all differences.\n\n**-d**\n> Show detailed output."],["DESCRIPTION","**sc_tracediff** compares two scamper traceroute files and highlights path differences. Useful for detecting routing changes. Part of scamper toolkit."],["INSTALL","```apt: sudo apt install scamper```\n\n```zypper: sudo zypper install scamper```\n\n```brew: brew install scamper```"],["SEE ALSO","[scamper](/man/scamper)(1), [sc_wartsdump](/man/sc_wartsdump)(1)"]],"e":["scamper","sc_wartsdump"],"x":"sc_tracediff [trace1.warts] [trace2.warts]"},"sd-cli":{"n":"sd-cli","t":"Stable Diffusion image generation CLI","s":[["TAGLINE","Stable Diffusion image generation CLI"],["TLDR","**Generate an image from a text prompt**\n\n```sd-cli -m [model.safetensors] -p \"[a photo of a cat]\" -o [output.png]```\n\n**Generate with specific dimensions**\n\n```sd-cli -m [model.safetensors] -p \"[prompt]\" --width [512] --height [512] -o [output.png]```\n\n**Set generation parameters**\n\n```sd-cli -m [model.safetensors] -p \"[prompt]\" --steps [20] --cfg-scale [7.0] --seed [42] -o [output.png]```\n\n**Use a specific sampling method**\n\n```sd-cli -m [model.safetensors] -p \"[prompt]\" --sampling-method [euler_a] -o [output.png]```\n\n**Generate with negative prompt**\n\n```sd-cli -m [model.safetensors] -p \"[prompt]\" -n \"[blurry, low quality]\" -o [output.png]```\n\n**Image-to-image generation**\n\n```sd-cli -m [model.safetensors] --img2img [input.png] -p \"[oil painting style]\" --strength [0.75] -o [output.png]```"],["SYNOPSIS","**sd-cli** **-m** _model_ **-p** _prompt_ [_options_] **-o** _output_"],["PARAMETERS","**-m**, **--model** _FILE_\n> Path to model weights (.safetensors or .gguf).\n\n**-p**, **--prompt** _TEXT_\n> Text prompt for image generation.\n\n**-n**, **--negative-prompt** _TEXT_\n> Negative prompt to guide away from.\n\n**-o**, **--output** _FILE_\n> Output image path.\n\n**--steps** _N_\n> Number of sampling steps (default: 20).\n\n**--cfg-scale** _FLOAT_\n> Classifier-free guidance scale (default: 7.0).\n\n**--seed** _INT_\n> RNG seed (-1 for random).\n\n**--width** _PX_\n> Image width in pixels.\n\n**--height** _PX_\n> Image height in pixels.\n\n**--sampling-method** _METHOD_\n> Sampling method: euler, euler_a, heun, dpm2, dpm++2s_a, dpm++2m, dpm++2mv2, lcm.\n\n**--img2img** _FILE_\n> Input image for image-to-image generation.\n\n**--strength** _FLOAT_\n> Denoising strength for img2img (0.0-1.0).\n\n**--threads** _N_\n> Number of CPU threads.\n\n**--rng** _TYPE_\n> RNG type: std_default, cuda."],["DESCRIPTION","**sd-cli** is the command-line interface for **stable-diffusion.cpp**, a lightweight C/C++ implementation of Stable Diffusion using the ggml tensor library. It runs image generation models on CPU and GPU without requiring Python or heavy ML frameworks.\n\nThe tool supports Stable Diffusion 1.x, 2.x, SDXL, and Flux model architectures. Models in safetensors or gguf (quantized) format can be loaded directly. Quantized models significantly reduce memory usage while maintaining reasonable quality.\n\nText-to-image generates from a prompt. Image-to-image transforms an existing image guided by a prompt. The strength parameter controls how much the original image is altered.\n\nBuild from source using CMake. GPU acceleration is available through CUDA, Metal, and Vulkan backends."],["CAVEATS","Must be built from source. Large model files required (several GB). Quality depends on model choice and parameters. GPU acceleration requires backend-specific build flags. SDXL and Flux models need more memory than SD 1.x."],["HISTORY","**stable-diffusion.cpp** was created by **leejet** in **2023**, inspired by llama.cpp's approach of bringing ML models to C/C++ with minimal dependencies. It brought Stable Diffusion inference to resource-constrained environments and platforms without Python support."],["INSTALL","```aur: yay -S sd-cli```"],["SEE ALSO","[mflux](/man/mflux)(1), [comfyui](/man/comfyui)(1), [convert](/man/convert)(1)"]],"e":["mflux","comfyui","convert"],"x":"sd-cli -m [model.safetensors] -p \"[a photo of a cat]\" -o [output.png]"},"sha384sum":{"n":"sha384sum","t":"Compute and verify SHA-384 checksums","s":[["TAGLINE","Compute and verify SHA-384 checksums"],["TLDR","**Calculate the SHA-384 hash of a file**\n\n```sha384sum [file]```\n\n**Calculate SHA-384 hashes for multiple files**\n\n```sha384sum [file1] [file2]```\n\n**Verify checksums stored in a file**\n\n```sha384sum -c [checksums.txt]```\n\n**Verify checksums, only showing failures**\n\n```sha384sum -c --quiet [checksums.txt]```\n\n**Read from stdin**\n\n```echo \"[text]\" | sha384sum```\n\n**Generate a checksum and save to a file**\n\n```sha384sum [file] > [checksums.sha384]```"],["SYNOPSIS","**sha384sum** [_options_] [_file_...]"],["PARAMETERS","**-c**, **--check**\n> Verify checksums from file.\n\n**-b**, **--binary**\n> Read in binary mode.\n\n**-t**, **--text**\n> Read in text mode.\n\n**--quiet**\n> Don't print OK for verified files.\n\n**--status**\n> Don't output anything; use exit status for result (0 = success, 1 = failure).\n\n**--strict**\n> Exit non-zero for improperly formatted checksum lines.\n\n**-w**, **--warn**\n> Warn about improperly formatted checksum lines.\n\n**--tag**\n> Output BSD-style checksums."],["DESCRIPTION","**sha384sum** computes and verifies SHA-384 cryptographic hash values, producing a 384-bit (96-character hexadecimal) message digest. It is part of the SHA-2 family and is derived from SHA-512 with truncated output, offering a security level between SHA-256 and SHA-512.\n\nSHA-384 is commonly used in TLS/SSL certificates and digital signatures where stronger security than SHA-256 is desired without the full 128-character output of SHA-512. With no FILE argument or when FILE is **-**, input is read from standard input. In check mode (**-c**), previously generated checksums are verified against current file contents."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[sha256sum](/man/sha256sum)(1), [sha512sum](/man/sha512sum)(1), [sha224sum](/man/sha224sum)(1), [sha1sum](/man/sha1sum)(1), [md5sum](/man/md5sum)(1), [b2sum](/man/b2sum)(1)"]],"e":["sha256sum","sha512sum","sha224sum","sha1sum","md5sum","b2sum"],"x":"sha384sum [file]"},"sicstus":{"n":"sicstus","t":"SICStus Prolog development system","s":[["TAGLINE","SICStus Prolog development system"],["TLDR","**Start SICStus Prolog** interactive interpreter\n\n```sicstus```\n\n**Load a Prolog file** on startup\n\n```sicstus -l [path/to/file.pl]```\n\n**Restore a saved state** on startup\n\n```sicstus -r [path/to/saved.sav]```\n\n**Start in forced interactive mode** even with redirected input\n\n```sicstus -i```\n\n**Start without the banner**\n\n```sicstus --nologo```\n\n**Execute a goal** on startup\n\n```sicstus -e \"[goal]\"```\n\n**Start with additional arguments** passed to Prolog\n\n```sicstus -- [arg1] [arg2]```"],["SYNOPSIS","**sicstus** [_options_] [**--**] [_prolog-arguments_]"],["PARAMETERS","**-l** _file_\n> Load a Prolog file on startup. This is done before any initialization file is loaded. Only one **-l** argument is allowed.\n\n**-r** _file_\n> Restore a saved state on startup.\n\n**-i**\n> Force interactive mode. Prompts for user input even if standard input is redirected.\n\n**-e** _goal_\n> Execute the specified goal on startup.\n\n**--nologo**\n> Suppress the startup banner and version information.\n\n**--iso**\n> Start in ISO Prolog mode.\n\n**--sicstus**\n> Start in SICStus Prolog mode (default).\n\n**--**\n> End of flags. Remaining arguments are passed as Prolog arguments."],["DESCRIPTION","**sicstus** is the command-line interface for SICStus Prolog, a commercial Prolog development system developed by the Swedish Institute of Computer Science (SICS). It provides an interactive interpreter for running Prolog programs and supports both ISO Prolog and SICStus-specific extensions.\n\nAfter startup, the interpreter displays a prompt (**| ?-**) where you can enter queries. After each answer, pressing Return terminates the query, while **;** followed by Return requests the next solution. Execution can be interrupted with **^C**, and the interpreter is exited with **^D** or the **halt.** goal.\n\nSICStus Prolog includes features for constraint solving, interfacing with C and Java, and developing robust applications. It requires a valid license to run the development system."],["CAVEATS","SICStus Prolog is commercial software requiring a valid license. When executing saved states, all command-line arguments are treated as Prolog arguments, so flags like **-l** and **-r** are not available. The **-l** option only accepts one file; use the interpreter to load additional files after startup."],["HISTORY","**SICStus Prolog** was developed by the Intelligent Systems Laboratory at SICS (Swedish Institute of Computer Science), now part of RISE SICS. Development began in the **1980s**, making it one of the oldest continuously developed Prolog systems. SICStus became known for its robust constraint solving capabilities and efficient execution. The system continues active development with version 4.x series, maintaining compatibility while adding modern features."],["SEE ALSO","[swipl](/man/swipl)(1), [gprolog](/man/gprolog)(1)"]],"e":["swipl","gprolog"],"x":"sicstus"},"spicetify":{"n":"spicetify","t":"Customize Spotify desktop client","s":[["TAGLINE","Customize Spotify desktop client"],["TLDR","**Apply customizations**\n\n```spicetify apply```\n\n**Backup Spotify**\n\n```spicetify backup```\n\n**Restore Spotify**\n\n```spicetify restore```\n\n**Install extension**\n\n```spicetify config extensions [extension.js]```\n\n**Change theme**\n\n```spicetify config current_theme [theme_name]```\n\n**List config**\n\n```spicetify config```\n\n**Update spicetify**\n\n```spicetify upgrade```\n\n**Watch for changes**\n\n```spicetify watch```"],["SYNOPSIS","**spicetify** _command_ [_options_]"],["PARAMETERS","**apply**\n> Apply customizations.\n\n**backup**\n> Backup Spotify.\n\n**restore**\n> Restore original.\n\n**config** _KEY_ _VALUE_\n> Set configuration.\n\n**upgrade**\n> Update spicetify.\n\n**watch**\n> Live reload.\n\n**update**\n> Update Spotify backup."],["CONFIG OPTIONS","**current_theme** - Active theme\n**color_scheme** - Color variant\n**extensions** - Enabled extensions\n**custom_apps** - Custom applications\n**inject_css** - Enable CSS injection"],["DESCRIPTION","**spicetify** is a command-line tool for customizing the Spotify desktop client by injecting custom CSS themes, JavaScript extensions, and custom apps. It modifies the Spotify client files to apply visual and functional changes.\n\nBefore first use, a backup of the original Spotify installation must be created with **spicetify backup**. Themes control the visual appearance and color scheme, while extensions add functionality such as lyrics display, keyboard shortcuts, and playlist management features. A community marketplace provides a curated collection of themes and extensions.\n\nThe **watch** command enables a development mode that applies changes in real time as theme or extension files are modified. Configuration is stored in a TOML file and managed through the **spicetify config** command."],["CONFIGURATION","**~/.config/spicetify/config-xpui.ini**\n> Main configuration file storing the active theme, color scheme, enabled extensions, custom apps, and Spotify installation path.\n\n**~/.config/spicetify/Themes/**\n> Directory containing theme folders, each with a color.ini and user.css file defining the visual customization.\n\n**~/.config/spicetify/Extensions/**\n> Directory for JavaScript extension files that add functionality to the Spotify client."],["CAVEATS","May break with Spotify updates. Requires Spotify desktop. Backup before updating."],["HISTORY","**spicetify** was created for Spotify customization. It provides a framework for theming and extending the desktop client."]],"e":[],"x":"spicetify apply"},"ssh-copy-id":{"n":"ssh-copy-id","t":"","s":[["TLDR","**Copy the default public key** to a remote host\n\n```ssh-copy-id [user@host]```\n\n**Copy a specific public key**\n\n```ssh-copy-id -i [~/.ssh/id_ed25519.pub] [user@host]```\n\n**Copy to a host on a non-standard port**\n\n```ssh-copy-id -p [2222] [user@host]```\n\n**Dry run** to see which keys would be installed\n\n```ssh-copy-id -n [user@host]```\n\n**Force copy** without checking for existing keys\n\n```ssh-copy-id -f -i [~/.ssh/id_rsa.pub] [user@host]```"],["SYNOPSIS","**ssh-copy-id** [_-f_] [_-n_] [_-s_] [_-i identity_file_] [_-p port_] [_-o ssh_option_] [_user@_]_hostname_"],["PARAMETERS","**-i** _identity_file_\n> Use the specified identity file (public key)\n\n**-p** _port_\n> Connect to the specified port on the remote host\n\n**-f**\n> Force mode; don't check if keys already exist on remote\n\n**-n**\n> Dry run; print keys that would be installed without installing\n\n**-s**\n> Use sftp instead of cat for copying (useful for restricted shells)\n\n**-o** _ssh_option_\n> Pass options to the underlying ssh command"],["DESCRIPTION","**ssh-copy-id** installs SSH public keys on a remote server's authorized_keys file, enabling passwordless authentication. It connects via SSH (usually with password authentication), creates the ~/.ssh directory and authorized_keys file if needed, and appends your public key.\n\nThe script ensures correct permissions are set: ~/.ssh directory at 700 and authorized_keys at 600, which SSH requires for security. Incorrect permissions cause authentication failures.\n\nBy default, ssh-copy-id uses keys from ssh-add -L or the most recent ~/.ssh/id*.pub file. Use **-i** to specify a different key."],["CAVEATS","Password authentication must be enabled on the remote host for the initial copy. After installation, you may want to disable password authentication in sshd_config. The script is a shell wrapper around ssh, so all ssh options apply."],["HISTORY","ssh-copy-id is a convenience script included with **OpenSSH** to simplify the public key installation process. It automates what would otherwise require manually copying keys and setting permissions correctly."],["INSTALL","```apt: sudo apt install openssh-client```\n\n```apk: sudo apk add openssh-client-common```\n\n```brew: brew install ssh-copy-id```\n\n```nix: nix profile install nixpkgs#ssh-copy-id```"],["SEE ALSO","[ssh](/man/ssh)(1), [ssh-keygen](/man/ssh-keygen)(1), [ssh-add](/man/ssh-add)(1), [sshd](/man/sshd)(8)"],["RESOURCES","```[Source code](https://github.com/openssh/openssh-portable)```\n\n```[Homepage](https://www.openssh.com/)```"]],"e":["ssh","ssh-keygen","ssh-add","sshd"],"x":"ssh-copy-id [user@host]"},"swift":{"n":"swift","t":"Swift language compiler and package manager","s":[["TAGLINE","Swift language compiler and package manager"],["TLDR","**Start REPL**\n\n```swift```\n\n**Run script**\n\n```swift [script.swift]```\n\n**Build package**\n\n```swift build```\n\n**Build release**\n\n```swift build -c release```\n\n**Run package**\n\n```swift run```\n\n**Run tests**\n\n```swift test```\n\n**Create new package**\n\n```swift package init --type [executable]```\n\n**Show dependencies**\n\n```swift package show-dependencies```"],["SYNOPSIS","**swift** [_build_] [_run_] [_test_] [_package_] [_options_] [_files_]"],["PARAMETERS","**build**\n> Build the package.\n\n**run** [_TARGET_]\n> Build and run.\n\n**test**\n> Run tests.\n\n**package init**\n> Create new package.\n\n**package update**\n> Update dependencies.\n\n**package resolve**\n> Resolve dependencies.\n\n**-c** _CONFIG_\n> Build configuration (debug, release).\n\n**-Xswiftc** _FLAG_\n> Pass flag to compiler.\n\n**--version**\n> Show version.\n\n**--help**\n> Show help."],["DESCRIPTION","**swift** is the Swift programming language compiler and package manager. It builds, runs, and tests Swift applications.\n\nThe REPL provides interactive Swift execution. It's useful for experimentation and learning.\n\nSwift Package Manager handles dependencies and builds. Package.swift defines targets, dependencies, and products.\n\nDebug builds are fast to compile with debugging support. Release builds optimize for performance.\n\nTesting integrates XCTest framework. Tests run with swift test, supporting parallel execution.\n\nCross-platform support includes Linux and Windows beyond Apple platforms. Server-side Swift is a common use case."],["CAVEATS","Full functionality on macOS. Linux support is good but some Apple frameworks unavailable. Windows support is newer."],["HISTORY","**Swift** was announced by **Apple** at WWDC **2014**, developed by **Chris Lattner** and others. It was open-sourced in **2015**. Swift has evolved significantly, with Swift 6 introducing strict concurrency."],["INSTALL","```brew: brew install swift```\n\n```nix: nix profile install nixpkgs#swift```"],["SEE ALSO","[swiftc](/man/swiftc)(1), [xcodebuild](/man/xcodebuild)(1), [vapor](/man/vapor)(1)"]],"e":["swiftc","xcodebuild","vapor"],"x":"swift"},"terraform-logout":{"n":"terraform-logout","t":"Remove locally stored Terraform credentials","s":[["TAGLINE","Remove locally stored Terraform credentials"],["TLDR","**Log out** from HCP Terraform (default)\n\n```terraform logout```\n\n**Log out** from a specific **Terraform Enterprise** instance\n\n```terraform logout [hostname]```"],["SYNOPSIS","**terraform logout** [_hostname_]"],["PARAMETERS","**hostname**\n> The hostname of the Terraform service to log out from. Defaults to **app.terraform.io** (HCP Terraform). For Terraform Enterprise, specify your organization's hostname"],["DESCRIPTION","**terraform logout** removes locally stored API credentials for a specified remote service host. These credentials are typically stored after running **terraform login**. The command supports HCP Terraform, Terraform Enterprise, and any host offering Terraform services.\n\nWhen using the default credentials file, it removes the token for the specified hostname from **~/.terraform.d/credentials.tfrc.json**. If a **credentials helper** is configured, Terraform invokes the helper's **forget** command to remove credentials from the external system."],["CAVEATS","The API token is **only removed from local storage**. It is **not** revoked on the remote server. The token remains valid until you manually revoke it through the service's web UI or API. For proper security hygiene, always revoke tokens on the remote service after logging out locally."],["HISTORY","Introduced in **Terraform v0.12.21** in early **2020** alongside **terraform login** to simplify credential management for Terraform Cloud and Enterprise connections."],["INSTALL","```pacman: sudo pacman -S terraform```\n\n```nix: nix profile install nixpkgs#terraform```"],["SEE ALSO","[terraform](/man/terraform)(1), [terraform-login](/man/terraform-login)(1)"]],"e":["terraform","terraform-login"],"x":"terraform logout"},"test":{"n":"test","t":"Evaluate conditional expressions","s":[["TAGLINE","Evaluate conditional expressions"],["TLDR","**Check if a file exists**\n\n```test -e [path/to/file] && echo \"exists\"```\n\n**Check if file is a directory**\n\n```test -d [path/to/dir] && echo \"is directory\"```\n\n**Check if file is readable**\n\n```test -r [path/to/file] && echo \"readable\"```\n\n**Compare strings for equality**\n\n```test \"[string1]\" = \"[string2]\"```\n\n**Check if string is non-empty**\n\n```test -n \"[string]\"```\n\n**Compare integers**\n\n```test [5] -gt [3] && echo \"greater\"```\n\n**Combine conditions with AND**\n\n```test -f [file] -a -r [file]```\n\n**Combine conditions with OR**\n\n```test -f [file1] -o -f [file2]```"],["SYNOPSIS","**test** _EXPRESSION_\n\n**[** _EXPRESSION_ **]**"],["FILE TESTS","**-e** _FILE_\n> True if file exists.\n\n**-f** _FILE_\n> True if file exists and is a regular file.\n\n**-d** _FILE_\n> True if file exists and is a directory.\n\n**-r** _FILE_\n> True if file is readable.\n\n**-w** _FILE_\n> True if file is writable.\n\n**-x** _FILE_\n> True if file is executable.\n\n**-s** _FILE_\n> True if file exists and has size greater than zero.\n\n**-L** _FILE_\n> True if file is a symbolic link.\n\n**-b** _FILE_\n> True if file is a block special file.\n\n**-c** _FILE_\n> True if file is a character special file.\n\n**-p** _FILE_\n> True if file is a named pipe (FIFO).\n\n**-S** _FILE_\n> True if file is a socket.\n\n**-g** _FILE_\n> True if file has set-group-ID bit set.\n\n**-u** _FILE_\n> True if file has set-user-ID bit set.\n\n**-O** _FILE_\n> True if file is owned by the effective user ID.\n\n_FILE1_ **-nt** _FILE2_\n> True if FILE1 is newer (modification date) than FILE2.\n\n_FILE1_ **-ot** _FILE2_\n> True if FILE1 is older than FILE2.\n\n_FILE1_ **-ef** _FILE2_\n> True if FILE1 and FILE2 refer to the same device and inode numbers."],["STRING TESTS","**-n** _STRING_\n> True if string length is non-zero.\n\n**-z** _STRING_\n> True if string length is zero.\n\n_STRING1_ **=** _STRING2_\n> True if strings are equal.\n\n_STRING1_ **!=** _STRING2_\n> True if strings are not equal."],["INTEGER COMPARISONS","_INT1_ **-eq** _INT2_\n> Equal.\n\n_INT1_ **-ne** _INT2_\n> Not equal.\n\n_INT1_ **-lt** _INT2_\n> Less than.\n\n_INT1_ **-le** _INT2_\n> Less than or equal.\n\n_INT1_ **-gt** _INT2_\n> Greater than.\n\n_INT1_ **-ge** _INT2_\n> Greater than or equal."],["OPERATORS","**!** _EXPR_\n> True if expression is false.\n\n_EXPR1_ **-a** _EXPR2_\n> True if both expressions are true (AND).\n\n_EXPR1_ **-o** _EXPR2_\n> True if either expression is true (OR)."],["DESCRIPTION","**test** evaluates conditional expressions, returning exit status 0 (true) or 1 (false). It is commonly used in shell scripts for decision-making in if statements and loops.\n\nThe command can also be invoked as **[** with a closing **]** required as the last argument. This bracket syntax is more readable in conditionals: **if [ -f file ]; then**.\n\nModern shells also provide **[[** which offers additional features like pattern matching and safer string handling, but is not POSIX-compliant."],["CAVEATS","Variables in test expressions should be quoted to handle empty values and spaces correctly. The **[** form requires spaces around brackets. String comparison uses **=** not **==** for POSIX compliance. Integer comparison operators (-eq, -lt) are different from string operators."],["HISTORY","test is one of the original Unix utilities, dating back to Version 7 Unix in **1979**. The bracket notation **[** was added as an alias for improved readability in shell scripts. Both forms are specified by POSIX and available as shell builtins and standalone commands."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[bash](/man/bash)(1), [sh](/man/sh)(1), [expr](/man/expr)(1)"]],"e":["bash","sh","expr"],"x":"test -e [path/to/file] && echo \"exists\""},"tiptop":{"n":"tiptop","t":"Top-like display backed by hardware performance counters","s":[["TAGLINE","Top-like display backed by hardware performance counters"],["TLDR","**Launch the interactive monitor**\n\n```tiptop```\n\n**Refresh every 2 seconds**\n\n```tiptop -d [2]```\n\n**Show only the named process**\n\n```tiptop -p [firefox]```\n\n**Show only one user's tasks**\n\n```tiptop -u [$USER]```\n\n**Run in batch mode** for 5 iterations (e.g., for logging)\n\n```tiptop -b -n [5]```\n\n**Include kernel activity** in counter values (needs root / **CAP_SYS_ADMIN**)\n\n```sudo tiptop -K```"],["SYNOPSIS","**tiptop** [_options_] [_command_]"],["PARAMETERS","**-b**\n> Batch mode: print to stdout, no interactive control.\n\n**-c**\n> Display the full command line instead of the process name.\n\n**-d** _SECONDS_\n> Delay between refreshes (fractional values allowed).\n\n**-H**\n> Show one line per thread instead of per process.\n\n**-i**\n> Include idle tasks.\n\n**-K**\n> Count kernel-mode events (needs **perf_event_paranoid <= 1** or root).\n\n**-n** _COUNT_\n> Exit after _COUNT_ refreshes (useful with **-b**).\n\n**-p** _PID_or_NAME_\n> Restrict the display to a process by PID or basename.\n\n**-u** _USER_\n> Restrict the display to a user.\n\n**-w** _PID_\n> Highlight/follow a single task.\n\n**-S** _COL_\n> Sort by the named column.\n\n**--sticky**\n> Keep finished tasks in the table.\n\n**--epoch**\n> Print Unix epoch timestamps in batch output."],["DESCRIPTION","**tiptop** displays a top-like view of running tasks but, instead of CPU% and RSS, the columns come from **hardware performance counters** (instructions per cycle, cache misses, branch mispredictions, etc.) read through the Linux **perf_event** interface. This makes it suitable for spotting microarchitectural bottlenecks — bad cache behavior, branch-prediction issues, FPU pressure — that a standard **top** cannot see.\n\nThe set of counters and how they are combined into columns is configured per-architecture; users can also define custom columns in **~/.tiptoprc** to expose additional counters or derived metrics."],["CAVEATS","Requires hardware performance-counter support and an accessible **perf_event** subsystem; on hardened kernels you may need to lower **/proc/sys/kernel/perf_event_paranoid**. **-K** (count kernel events) requires elevated privileges. Some virtual machines do not expose PMU counters, in which case columns will read as zero."],["HISTORY","**tiptop** was written by **Erven Rohou** at **INRIA** to make hardware performance counters as accessible as **top**. A separate, unrelated Python tool of the same name exists; this page documents the canonical Erven Rohou implementation packaged as **tiptop** in Debian, Ubuntu, Fedora, and Arch."],["INSTALL","```apt: sudo apt install tiptop```\n\n```dnf: sudo dnf install tiptop```\n\n```zypper: sudo zypper install tiptop```\n\n```nix: nix profile install nixpkgs#tiptop```"],["SEE ALSO","[top](/man/top)(1), [htop](/man/htop)(1), [perf](/man/perf)(1), [pidstat](/man/pidstat)(1)"]],"e":["top","htop","perf","pidstat"],"x":"tiptop"},"tlmgr-arch":{"n":"tlmgr-arch","t":"Manage TeX Live platform architectures","s":[["TAGLINE","Manage TeX Live platform architectures"],["TLDR","**List supported architectures**\n\n```tlmgr arch```\n\n**List only installed**\n\n```tlmgr arch --installed```\n\n**Add architecture**\n\n```tlmgr arch add [x86_64-linux]```\n\n**Remove architecture**\n\n```tlmgr arch remove [i386-linux]```"],["SYNOPSIS","**tlmgr arch** [_action_] [_arch_...]"],["PARAMETERS","**--installed**\n> Show only installed architectures.\n\n**add** _arch_\n> Add platform binaries.\n\n**remove** _arch_\n> Remove platform binaries.\n\n**list**\n> List available architectures."],["DESCRIPTION","**tlmgr arch** manages TeX Live platform support. Install binaries for additional architectures for cross-platform TeX distribution. Part of TeX Live Manager."],["SEE ALSO","[tlmgr-platform](/man/tlmgr-platform)(1), [tlmgr](/man/tlmgr)(1)"]],"e":["tlmgr-platform","tlmgr"],"x":"tlmgr arch"},"treetime":{"n":"treetime","t":"Ancestral sequence reconstruction and molecular clocks","s":[["TAGLINE","Ancestral sequence reconstruction and molecular clocks"],["TLDR","Infer **ancestral** sequences\n\n```treetime ancestral```\n\nAnalyze **homoplasies**\n\n```treetime homoplasy```\n\nEstimate molecular **clock**\n\n```treetime clock```\n\nMap discrete **characters**\n\n```treetime mugration```"],["SYNOPSIS","**treetime** _COMMAND_ [_OPTIONS_]"],["COMMANDS","**ancestral**\n> Infer ancestral sequences maximizing likelihood\n\n**homoplasy**\n> Analyze patterns of recurrent mutations\n\n**clock**\n> Estimate molecular clock parameters and reroot tree\n\n**mugration**\n> Map discrete characters (host, country) to tree"],["DESCRIPTION","**treetime** provides routines for ancestral sequence reconstruction and inference of molecular-clock phylogenies. It is used in phylogenetic analysis to understand evolutionary relationships and timing of divergence events.\n\nThe tool can reconstruct ancestral sequences, detect homoplasies (convergent evolution), estimate mutation rates, and map geographic or host information onto phylogenetic trees."],["CAVEATS","Requires phylogenetic tree and sequence alignment inputs. Computationally intensive for large datasets. Results depend on model assumptions."],["HISTORY","**treetime** was developed for phylogenetic analysis in molecular epidemiology and evolutionary biology, providing tools for understanding pathogen evolution and spread."],["SEE ALSO","[iqtree](/man/iqtree)(1), [raxml](/man/raxml)(1)"]],"e":["iqtree","raxml"],"x":"treetime ancestral"},"trezorctl":{"n":"trezorctl","t":"Trezor hardware wallet command-line interface","s":[["TAGLINE","Trezor hardware wallet command-line interface"],["TLDR","**List connected Trezor devices**\n\n```trezorctl list```\n\n**Get device features and info**\n\n```trezorctl get-features```\n\n**Get Bitcoin address** at a specific path\n\n```trezorctl btc get-address -n \"m/84'/0'/0'/0/0\"```\n\n**Sign a message** with Bitcoin\n\n```trezorctl btc sign-message -n \"m/84'/0'/0'/0/0\" \"[message]\"```\n\n**Wipe device** (factory reset)\n\n```trezorctl device wipe```\n\n**Update firmware**\n\n```trezorctl firmware-update```\n\n**Set device label**\n\n```trezorctl device set-label \"[My Trezor]\"```\n\n**Change PIN**\n\n```trezorctl device change-pin```"],["SYNOPSIS","**trezorctl** [_options_] _command_ [_subcommand_] [_arguments_]"],["PARAMETERS","**list**\n> List all connected Trezor devices.\n\n**get-features**\n> Retrieve device information including model, firmware version, and settings.\n\n**btc get-address** **-n** _path_\n> Get a Bitcoin address at the specified BIP32 derivation path.\n\n**btc sign-message** **-n** _path_ _message_\n> Sign a message using the key at the specified path.\n\n**btc sign-tx**\n> Sign a Bitcoin transaction interactively.\n\n**device wipe**\n> Wipe the device and reset to factory settings.\n\n**device setup**\n> Initialize a new device with seed generation.\n\n**device recover**\n> Recover a device using an existing seed phrase.\n\n**device change-pin**\n> Change or set the device PIN.\n\n**device set-label** _label_\n> Set a custom label for the device.\n\n**device unlock-bootloader**\n> Unlock the bootloader for advanced operations.\n\n**firmware-update**\n> Update device firmware to the latest version.\n\n**set safety-checks** _strict_|_prompt_\n> Set safety check level. Strict enforces full validation; prompt allows user overrides.\n\n**crypto get-public-key** **-n** _path_\n> Get public key at the specified BIP32 derivation path.\n\n**ethereum get-address** **-n** _path_\n> Get Ethereum address at the specified derivation path.\n\n**-v**, **--verbose**\n> Enable verbose output.\n\n**--help**\n> Show help for a command."],["DESCRIPTION","**trezorctl** is the command-line interface for Trezor hardware wallets, providing advanced control over device configuration, cryptocurrency operations, and security settings. It is part of the trezorlib Python package and communicates with Trezor One, Trezor Model T, and Trezor Safe devices.\n\nThe tool enables signing transactions, deriving addresses, managing device settings, updating firmware, and performing recovery operations without using the graphical Trezor Suite application. It supports multiple cryptocurrencies including Bitcoin, Ethereum, and various altcoins.\n\nAll sensitive operations require physical confirmation on the device, maintaining security even when using command-line tools. The interface is designed for advanced users who need scriptable access to hardware wallet functions."],["CAVEATS","Requires Python 3.8 or higher and libusb. On Linux, udev rules must be installed for device detection (provided by the `trezor-common` package). Some operations like device wipe are irreversible; ensure seed phrase backup before proceeding. The device must be connected and unlocked for most commands. Install via `pip install trezor`."],["HISTORY","**trezorctl** was developed by **SatoshiLabs** as part of the Python tools for Trezor, the first hardware cryptocurrency wallet released in **2014**. The command-line interface evolved alongside the device firmware, adding support for new cryptocurrencies and security features. The tool is maintained in the official trezor-firmware repository."],["INSTALL","```nix: nix profile install nixpkgs#trezorctl```"],["SEE ALSO","[gpg](/man/gpg)(1), [openssl](/man/openssl)(1)"]],"e":["gpg","openssl"],"x":"trezorctl list"},"try-rs":{"n":"try-rs","t":"Workspace manager for temporary experiments with TUI","s":[["TAGLINE","Workspace manager for temporary experiments with TUI"],["TLDR","**Launch the TUI to manage experiments**\n\n```try-rs```\n\n**Create a new experiment directory**\n\n```try-rs [rust-test]```\n\n**Clone a repo into experiments**\n\n```try-rs [https://github.com/user/repo]```"],["SYNOPSIS","**try-rs** [_options_] [_name_]"],["DESCRIPTION","**try-rs** organizes temporary project experiments into date-prefixed directories instead of cluttering your Desktop or /tmp. It provides a rich TUI for creating, navigating, and cleaning up experiments, with features like fuzzy search and git integration for auto-cloning URLs."],["HISTORY","**try-rs** was created by **Tassio Virginio** (tassiovirginio) and is written in **Rust**."],["INSTALL","```brew: brew install try-rs```"],["SEE ALSO","[tmux](/man/tmux)(1), [direnv](/man/direnv)(1), [git](/man/git)(1)"]],"e":["tmux","direnv","git"],"x":"try-rs"},"ttsc":{"n":"ttsc","t":"TypeScript-go compiler with plugins, lint, and transforms","s":[["TAGLINE","TypeScript-go compiler with plugins, lint, and transforms"],["TLDR","**Build** the project (drop-in for **tsc**)\n\n```ttsc```\n\n**Type-check** without emitting files\n\n```ttsc --noEmit```\n\n**Watch** and rebuild on save\n\n```ttsc --watch```\n\n**Apply lint autofixes** and format\n\n```ttsc fix```\n\n**Format** only (no behavioral changes)\n\n```ttsc format```"],["SYNOPSIS","**ttsc** [_options_] [_project_options_]\n\n**ttsc** **fix** | **format**"],["DESCRIPTION","**ttsc** is a TypeScript toolchain built on **typescript-go**. It is a drop-in replacement for **tsc**: it reads the same **tsconfig.json**, accepts the same compiler flags, and emits the same JavaScript, then runs compiler-powered plugins during the type-check pass.\n\nPlugins can add transforms, extra checks, or type-driven code generation (for example **typia** validators). With **@ttsc/lint** installed, lint and format rules run inside the same compile so violations appear as compiler diagnostics. Subcommands **fix** and **format** apply autofixes and formatting in place.\n\nInstall as a dev dependency (**npm install -D ttsc typescript**) and invoke via **npx ttsc** or a package script. Pair with **ttsx** to execute TypeScript after a full project type-check."],["PARAMETERS","Most **tsc** flags are supported, including:\n\n**--noEmit**\n> Type-check only; do not write output files\n\n**--watch**, **-w**\n> Watch input files and rebuild on changes\n\n**--project**, **-p** _path_\n> Compile the project given by the configuration file\n\n**--pretty**\n> Stylize errors and messages using color and context (default as in **tsc**)\n\n**fix**\n> Apply lint autofixes and format edits\n\n**format**\n> Apply format edits only; never change program behavior"],["CAVEATS","Requires **typescript** alongside **ttsc**. Plugin and lint behavior depends on installed **@ttsc/**\\* packages and project config (for example **lint.config.ts**). Not every third-party **tsc** wrapper flag may map one-to-one; prefer documented **ttsc** setup guides for bundler and Metro integrations."],["HISTORY","Created by Jeongho Nam (**samchon**) as a modern successor to transformer toolchains such as **ttypescript** and **ts-patch**, targeting the **typescript-go** compiler and integrating lint, format, and MCP code-graph tooling."],["SEE ALSO","[ttsx](/man/ttsx)(1), [tsc](/man/tsc)(1), [tsx](/man/tsx)(1), [ts-node](/man/ts-node)(1)"],["RESOURCES","```[Source code](https://github.com/samchon/ttsc)```\n\n```[Homepage](https://ttsc.dev)```\n\n```[Documentation](https://ttsc.dev/docs)```"]],"e":["ttsx","tsc","tsx","ts-node"],"x":"ttsc"},"vso":{"n":"vso","t":"Vanilla OS system operator tool","s":[["TAGLINE","Vanilla OS system operator tool"],["TLDR","Check for **updates**\n\n```vso sys-upgrade check```\n\n**Upgrade** system now\n\n```vso sys-upgrade upgrade --now```\n\nInitialize **Pico** subsystem\n\n```vso pico-init```\n\n**Install** applications\n\n```vso install [package1] [package2]```\n\n**Remove** applications\n\n```vso remove [package1] [package2]```\n\nEnter subsystem **shell**\n\n```vso shell```\n\n**Run** application\n\n```vso run [package]```\n\nShow **config**\n\n```vso config show```"],["SYNOPSIS","**vso** _COMMAND_ [_OPTIONS_]"],["COMMANDS","**sys-upgrade check**\n> Check for system updates\n\n**sys-upgrade upgrade**\n> Upgrade the system\n\n**pico-init**\n> Initialize Pico subsystem\n\n**install**\n> Install applications in subsystem\n\n**remove**\n> Remove applications from subsystem\n\n**shell**\n> Enter subsystem shell\n\n**run**\n> Run application from subsystem\n\n**export**\n> Export an installed application as a desktop entry\n\n**unexport**\n> Remove an exported desktop entry\n\n**config show**\n> Display configuration\n\n**help**\n> Show help for a command"],["PARAMETERS","**--now**\n> Perform system upgrade immediately without scheduling\n\n**-h**, **--help**\n> Show help information\n\n**--version**\n> Show version information"],["DESCRIPTION","**vso** (Vanilla System Operator) is a package manager, system updater, and task automator for Vanilla OS. It manages the immutable host system and a mutable Pico subsystem for package installation.\n\nThe subsystem provides a container-like environment where traditional packages can be installed without modifying the base system."],["CAVEATS","Vanilla OS specific. Subsystem must be initialized before package operations. System upgrades may require reboot."],["SEE ALSO","[apx](/man/apx)(1), [flatpak](/man/flatpak)(1), [snap](/man/snap)(1)"]],"e":["apx","flatpak","snap"],"x":"vso sys-upgrade check"},"xar":{"n":"xar","t":"Extensible archive format tool","s":[["TAGLINE","Extensible archive format tool"],["TLDR","**Create archive**\n\n```xar -cf [archive.xar] [files]```\n\n**Extract archive**\n\n```xar -xf [archive.xar]```\n\n**List contents**\n\n```xar -tf [archive.xar]```\n\n**Verbose extraction**\n\n```xar -xvf [archive.xar]```\n\n**Extract to directory**\n\n```xar -xf [archive.xar] -C [directory]```"],["SYNOPSIS","**xar** [_-c_] [_-x_] [_-t_] [_-f archive_] [_options_] [_files_]"],["PARAMETERS","**-c**\n> Create archive.\n\n**-x**\n> Extract archive.\n\n**-t**\n> List contents.\n\n**-f** _FILE_\n> Archive file.\n\n**-v**\n> Verbose.\n\n**-C** _DIR_\n> Target directory."],["DESCRIPTION","**xar** (eXtensible ARchive) creates and extracts archives using a format developed by Apple, primarily used for macOS installer packages. The format uses an XML-based table of contents that supports rich metadata and multiple compression algorithms per file.\n\nThe archive format also includes signature support for code signing and integrity verification. While primarily associated with macOS, xar is available on Linux for working with macOS package files and other XAR archives."],["CAVEATS","macOS oriented. Less common on Linux. XML overhead."],["HISTORY","**xar** (eXtensible ARchive) was developed by **Apple** for macOS installer packages."],["INSTALL","```apt: sudo apt install xar```\n\n```dnf: sudo dnf install xar```\n\n```apk: sudo apk add xar```\n\n```zypper: sudo zypper install xar```\n\n```nix: nix profile install nixpkgs#xar```"],["SEE ALSO","[tar](/man/tar)(1), [pkgutil](/man/pkgutil)(1), [ar](/man/ar)(1)"]],"e":["tar","pkgutil","ar"],"x":"xar -cf [archive.xar] [files]"}});
