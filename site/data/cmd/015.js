window.LCL_BUCKET(15,{"aa-status":{"n":"aa-status","t":"Report AppArmor profile and confinement status","s":[["TAGLINE","Report AppArmor profile and confinement status"],["TLDR","Check **AppArmor status**\n\n```sudo aa-status```\n\nDisplay status in **JSON format**\n\n```sudo aa-status --json```\n\nDisplay status in **pretty JSON format**\n\n```sudo aa-status --pretty-json```\n\nDisplay the number of **loaded policies**\n\n```sudo aa-status --profiled```\n\nDisplay the number of **enforcing policies**\n\n```sudo aa-status --enforced```\n\nDisplay the number of **complaining policies**\n\n```sudo aa-status --complaining```\n\nDisplay the number of policies in **kill mode**\n\n```sudo aa-status --kill```"],["SYNOPSIS","**aa-status** [_option_]"],["DESCRIPTION","**aa-status** reports the current state of AppArmor confinement on the system. By default, it displays a summary of loaded profiles grouped by enforcement mode (enforce, complain, kill, unconfined), the number of confined processes, and which profiles apply to them. Individual flags can query specific counters for use in scripts. The **--json** and **--pretty-json** flags provide machine-parseable output suitable for monitoring and automation pipelines."],["PARAMETERS","**--enabled**\n> Returns error code if AppArmor is not enabled\n\n**--profiled**\n> Shows count of loaded AppArmor policies\n\n**--enforced**\n> Shows count of enforcing policies\n\n**--complaining**\n> Shows count of non-enforcing (complain mode) policies\n\n**--kill**\n> Shows count of enforcing policies that terminate tasks on violations\n\n**--prompt**\n> Shows count of enforcing policies with fallback to userspace mediation\n\n**--special-unconfined**\n> Shows count of unconfined mode policies\n\n**--process-mixed**\n> Shows count of processes confined by profile stacks with profiles in different modes\n\n**--verbose**\n> Displays comprehensive AppArmor policy data (default behavior)\n\n**--json**\n> Outputs policy data in JSON format for machine processing\n\n**--pretty-json**\n> Provides human and machine-readable JSON output\n\n**--count**\n> Shows only counts for selected information\n\n**--show** _TYPE_\n> Specify what to display: processes, profiles, or all (default: all)\n\n**--filter.mode** _REGEX_\n> Filter output by profile mode using a POSIX regular expression\n\n**--filter.profiles** _REGEX_\n> Filter output by confining profile name using a POSIX regular expression\n\n**--filter.pid** _REGEX_\n> Filter output by process PID using a POSIX regular expression\n\n**--filter.exe** _REGEX_\n> Filter output by executable name using a POSIX regular expression\n\n**--help**\n> Displays usage information"],["CAVEATS","Exit codes indicate different states: **0** = enabled with policy, **1** = not enabled, **2** = enabled but no policy, **3** = control files unavailable, **4** = insufficient privileges."],["HISTORY","Part of the **AppArmor** utilities package for managing application security profiles on Linux systems."],["INSTALL","```apt: sudo apt install apparmor```\n\n```pacman: sudo pacman -S apparmor```\n\n```apk: sudo apk add apparmor-utils```\n\n```zypper: sudo zypper install apparmor-utils```\n\n```nix: nix profile install nixpkgs#apparmor-utils```"],["SEE ALSO","[aa-enforce](/man/aa-enforce)(8), [aa-complain](/man/aa-complain)(8), [aa-disable](/man/aa-disable)(8), [apparmor](/man/apparmor)(7)"]],"e":["aa-enforce","aa-complain","aa-disable","apparmor"],"x":"sudo aa-status"},"adventure":{"n":"adventure","t":"Original Colossal Cave text adventure game","s":[["TAGLINE","Original Colossal Cave text adventure game"],["TLDR","**Start** the adventure game\n\n```adventure```\n\n**Restore** from saved game\n\n```adventure -r```"],["SYNOPSIS","**adventure** [_-r_]"],["PREVIEW","```\nYou are inside a building.\nThere are some keys on the\nground here.\n\n> get keys\nOK\n```"],["DESCRIPTION","**adventure** (also known as Colossal Cave Adventure or ADVENT) is the original text adventure game. Players explore a cave system described entirely through text, interacting with the environment using simple verb-noun commands like \"GO NORTH\", \"GET LAMP\", or \"KILL DRAGON\".\n\nThe game features treasure hunting, puzzle solving, and exploration of a fantastical underground world inhabited by dwarves, a pirate, and a dragon. Commands are typically two words (verb + noun) or single words for movement."],["PARAMETERS","**-r**\n> Restore a previously saved game"],["CAVEATS","The game uses a simple two-word parser that may not recognize complex sentences. Save your progress frequently as the game contains many ways to die or become stuck."],["HISTORY","**adventure** was created by Will Crowther in **1976** based on his experiences caving in Mammoth Cave, Kentucky. Don Woods expanded it in **1977**, adding fantasy elements. It is considered the first text adventure game and influenced the entire adventure game genre including Zork and countless others."],["INSTALL","```dnf: sudo dnf install bsd-games```\n\n```pacman: sudo pacman -S bsd-games```\n\n```apk: sudo apk add bsd-games```\n\n```zypper: sudo zypper install bsd-games```"],["SEE ALSO","[frotz](/man/frotz)(1), [nethack](/man/nethack)(6)"]],"e":["frotz","nethack"],"x":"adventure"},"alsactl":{"n":"alsactl","t":"Save and restore ALSA sound card settings.","s":[["TAGLINE","Save and restore ALSA sound card settings."],["TLDR","**Store** current sound card settings\n\n```sudo alsactl store```\n\n**Restore** saved settings\n\n```sudo alsactl restore```\n\nStore settings for **specific card**\n\n```sudo alsactl store [0]```\n\n**Initialize** sound cards to default\n\n```sudo alsactl init```\n\nRestore using a **specific state file**\n\n```sudo alsactl -f [/path/to/asound.state] restore```\n\n**Monitor** driver events\n\n```sudo alsactl monitor```\n\nRestore with **debug output**\n\n```sudo alsactl -d restore```"],["SYNOPSIS","**alsactl** [_options_] _command_ [_card_]"],["DESCRIPTION","**alsactl** is an ALSA soundcard control utility that manages sound card configuration state. It can save and restore mixer settings, volume levels, and other audio parameters.\n\nThe tool is typically called automatically during system startup and shutdown to preserve audio settings across reboots."],["PARAMETERS","**store** [_card_]\n> Save current state to file\n\n**restore** [_card_]\n> Restore state from file\n\n**init** [_card_]\n> Initialize card to default state\n\n**nrestore** [_card_]\n> Restore but don't fail on missing state file\n\n**monitor**\n> Monitor driver events\n\n**-f** _file_\n> State file (default: /var/lib/alsa/asound.state)\n\n**-d**\n> Debug mode\n\n**-F**\n> Force restore, try to restore as many controls as possible\n\n**-g**, **--ignore**\n> Ignore missing soundcards (no error message or exit code)\n\n**-P**, **--pedantic**\n> Don't restore mismatching control elements\n\n**-I**, **--no-init-fallback**\n> Don't initialize cards if restore fails\n\n**-r**, **--runstate** _file_\n> Save restore/init state to file for error reporting"],["CONFIGURATION","**/var/lib/alsa/asound.state**\n> Default state file where mixer settings and volume levels are saved and restored from. Can also be located at /etc/asound.state on some distributions."],["CAVEATS","Requires root privileges for system-wide operations. State files are card-specific; moving a card to a different slot may cause issues. Some driver updates may invalidate saved state."],["HISTORY","**alsactl** has been part of the ALSA (Advanced Linux Sound Architecture) project since ALSA replaced OSS (Open Sound System) as the standard Linux audio framework in the early **2000s**."],["INSTALL","```apt: sudo apt install alsa-utils```\n\n```dnf: sudo dnf install alsa-utils```\n\n```pacman: sudo pacman -S alsa-utils```\n\n```apk: sudo apk add alsa-utils```\n\n```zypper: sudo zypper install alsa-utils```\n\n```nix: nix profile install nixpkgs#alsa-utils```"],["SEE ALSO","[alsamixer](/man/alsamixer)(1), [amixer](/man/amixer)(1), [aplay](/man/aplay)(1)"]],"e":["alsamixer","amixer","aplay"],"x":"sudo alsactl store"},"amass-intel":{"n":"amass-intel","t":"Discover domains and infrastructure for an organization","s":[["TAGLINE","Discover domains and infrastructure for an organization"],["TLDR","**Discover** root domains for an organization\n\n```amass intel -org \"[Company Name]\"```\n\nFind domains from **ASN**\n\n```amass intel -asn [AS12345]```\n\nReverse lookup by **IP range**\n\n```amass intel -cidr [192.168.1.0/24]```\n\nFind domains from **whois** data\n\n```amass intel -whois -d [example.com]```"],["SYNOPSIS","**amass intel** [_-org name_] [_-asn number_] [_-cidr range_] [_options_]"],["DESCRIPTION","**amass intel** gathers intelligence to identify domains and infrastructure owned by an organization. It uses various techniques including ASN lookup, WHOIS analysis, certificate transparency, and reverse IP lookups.\n\nThis command is typically used at the beginning of reconnaissance to identify all root domains before detailed enumeration."],["PARAMETERS","**-org** _name_\n> Search string matched against AS description information\n\n**-asn** _number_\n> ASNs separated by commas (can be used multiple times)\n\n**-cidr** _range_\n> CIDRs separated by commas (can be used multiple times)\n\n**-addr** _ips_\n> IPs and ranges (for example 192.168.1.1-254) separated by commas\n\n**-whois**\n> Run all discovered domains through reverse WHOIS\n\n**-d** _domain_\n> Domain names separated by commas (can be used multiple times)\n\n**-active**\n> Enable active recon methods\n\n**-ip**\n> Show the IP addresses for discovered names\n\n**-o** _file_\n> Output file\n\n**-config** _file_\n> Configuration file"],["CONFIGURATION","**~/.config/amass/config.ini**\n> Amass configuration file defining data sources, API keys, and scope settings."],["CAVEATS","Organization name matching is fuzzy; review results for accuracy. WHOIS data may be privacy-protected. ASN information may not cover all organizational assets."],["HISTORY","**amass intel** was added to provide organizational-level reconnaissance capabilities, complementing the domain-focused enum command."],["INSTALL","```aur: yay -S amass```\n\n```brew: brew install amass```\n\n```nix: nix profile install nixpkgs#amass```"],["SEE ALSO","[amass](/man/amass)(1), [amass-enum](/man/amass-enum)(1), [amass-track](/man/amass-track)(1), [whois](/man/whois)(1)"],["RESOURCES","```[Source code](https://github.com/owasp-amass/amass)```\n\n```[Documentation](https://github.com/owasp-amass/amass/blob/master/doc/user_guide.md)```"]],"e":["amass","amass-enum","amass-track","whois"],"x":"amass intel -org \"[Company Name]\""},"aoc":{"n":"aoc","t":"Advent of Code puzzle helper (download, read, submit)","s":[["TAGLINE","Advent of Code puzzle helper (download, read, submit)"],["TLDR","**Download** today's puzzle input\n\n```aoc download```\n\n**Download** a specific day\n\n```aoc download --day [1]```\n\n**Submit** an answer\n\n```aoc submit [1] [answer]```\n\n**Show** the calendar / stars\n\n```aoc calendar```\n\n**Read** puzzle description\n\n```aoc read```"],["SYNOPSIS","**aoc** *command* [*options*]"],["DESCRIPTION","**aoc** (from the **aoc-cli** crate) talks to [Advent of Code](https://adventofcode.com) using your session cookie: download descriptions and inputs, submit answers, and view star progress from the terminal. Install via **cargo install aoc-cli**, Homebrew taps, or release binaries.\n\nAuthenticate by placing the session cookie in an environment variable or config file as documented upstream (**AOC_SESSION**, etc.)."],["PARAMETERS","**download** | **d**\n\n> Save puzzle statement and/or input files.\n\n**submit** | **s**\n\n> Submit part 1/2 answers.\n\n**read** | **r**\n\n> Show puzzle description.\n\n**calendar** | **c**\n\n> Show calendar and collected stars.\n\n**--day** *n* / **--year** *y*\n\n> Select puzzle day/year (defaults to current event day when in December)."],["CAVEATS","Requires a valid AoC session cookie; do not commit it. Respect the event's automation rules and rate limits. Puzzle content is copyright Eric Wastl."],["SEE ALSO","[curl](/man/curl)(1)"],["RESOURCES","```[Source code](https://github.com/scarvalhojr/aoc-cli)```"]],"e":["curl"],"x":"aoc download"},"bccmd":{"n":"bccmd","t":"Issue BlueCore commands to CSR Bluetooth chips","s":[["TAGLINE","Issue BlueCore commands to CSR Bluetooth chips"],["TLDR","**Get chip revision**\n\n```bccmd chiprev```\n\n**Get local Bluetooth clock**\n\n```bccmd clock```\n\n**Get random number from device**\n\n```bccmd rand```\n\n**Perform cold reset**\n\n```bccmd coldreset```\n\n**Perform warm reset**\n\n```bccmd warmreset```\n\n**Get PS key value**\n\n```bccmd psget [key]```\n\n**Set PS key value**\n\n```bccmd psset [key] [value]```\n\n**Use specific transport**\n\n```bccmd -t [HCI|USB|BCSP|H4] [command]```"],["SYNOPSIS","**bccmd** [_-t transport_] [_-d device_] _command_ [_args_]"],["DESCRIPTION","**bccmd** is a utility for the CSR BCCMD interface that issues BlueCore commands to Cambridge Silicon Radio (CSR) Bluetooth devices. It allows direct control of Bluetooth chipset parameters and persistent store values.\n\nThe tool is part of the BlueZ Bluetooth stack and provides low-level access to CSR-based Bluetooth controllers."],["PARAMETERS","**-t** _transport_\n> Transport type: HCI (default), USB, BCSP, H4. 3WIRE is recognized but not implemented\n\n**-d** _device_\n> Device to use (default: first HCI device or /dev/ttyS0 for serial)\n\n**-h, --help**\n> Display help information"],["COMMANDS","**builddef**\n> Get build definitions\n\n**buildname**\n> Get build name string\n\n**chiprev**\n> Get chip revision\n\n**memtypes**\n> Get memory types\n\n**clock**\n> Get local Bluetooth clock\n\n**rand**\n> Get random number\n\n**keylen** _handle_\n> Get current crypt key length\n\n**coldreset**\n> Perform cold reset\n\n**warmreset**\n> Perform warm reset\n\n**disabletx**\n> Disable transmitter\n\n**enabletx**\n> Enable transmitter\n\n**singlechan** _channel_\n> Lock radio on a specific channel\n\n**hoppingon**\n> Revert to channel hopping\n\n**radiotest** _test_\n> Run radio tests (tests 4, 6 and 7 are transmit tests)\n\n**psget** _key_\n> Get value for a PS key\n\n**psset** _key_ _value_\n> Set value for a PS key\n\n**psclr** _key_\n> Clear value for a PS key\n\n**pslist**\n> List all PS keys\n\n**psread**\n> Read all PS keys\n\n**psload** _file_\n> Load all PS keys from a PSR file\n\n**pscheck** _file_\n> Check the syntax of a PSR file"],["CAVEATS","Only works with CSR-based Bluetooth chipsets. Modifying PS keys can alter device behavior including Bluetooth address. Some commands require specific transport connections. Changing device settings may void warranty."],["SEE ALSO","[hciconfig](/man/hciconfig)(1), [hcitool](/man/hcitool)(1), [bluetoothctl](/man/bluetoothctl)(1)"],["RESOURCES","```[Source code](https://github.com/bluez/bluez)```"]],"e":["hciconfig","hcitool","bluetoothctl"],"x":"bccmd chiprev"},"brew-reinstall":{"n":"brew-reinstall","t":"Uninstall and reinstall a Homebrew package","s":[["TAGLINE","Uninstall and reinstall a Homebrew package"],["TLDR","**Reinstall a formula**\n\n```brew reinstall [formula]```\n\n**Reinstall a cask**\n\n```brew reinstall --cask [cask]```\n\n**Reinstall with debug output**\n\n```brew reinstall --debug [formula]```\n\n**Reinstall and build from source**\n\n```brew reinstall --build-from-source [formula]```\n\n**Reinstall without quarantine** (casks only)\n\n```brew reinstall --no-quarantine [cask]```\n\n**Reinstall interactively**\n\n```brew reinstall --interactive [formula]```"],["SYNOPSIS","**brew** **reinstall** [_options_] _formula_|_cask_ [...]"],["PARAMETERS","**--cask**\n> Treat all named arguments as casks.\n\n**--formula**\n> Treat all named arguments as formulae.\n\n**--build-from-source** **-s**\n> Compile formula from source even if a bottle exists.\n\n**--debug** **-d**\n> Enable debugging mode during installation.\n\n**--interactive** **-i**\n> Open an interactive shell for debugging the build.\n\n**--no-quarantine**\n> Disable macOS quarantine for casks.\n\n**--force**\n> Install without checking for previously installed versions.\n\n**--verbose** **-v**\n> Print detailed installation output."],["DESCRIPTION","**brew reinstall** uninstalls and then installs a formula or cask. This is useful for fixing broken installations, applying new build options, or refreshing a package to its current version.\n\nThe command preserves configuration files when reinstalling formulas that have linked kegs."],["CAVEATS","Reinstalling may interrupt services that depend on the formula. Running applications from reinstalled casks may need to be restarted."],["SEE ALSO","[brew](/man/brew)(1), [brew-install](/man/brew-install)(1), [brew-uninstall](/man/brew-uninstall)(1)"],["RESOURCES","```[Source code](https://github.com/Homebrew/brew)```\n\n```[Homepage](https://brew.sh)```\n\n```[Documentation](https://docs.brew.sh)```"]],"e":["brew","brew-install","brew-uninstall"],"x":"brew reinstall [formula]"},"btrfs-restore":{"n":"btrfs-restore","t":"Salvage files from a damaged btrfs filesystem","s":[["TAGLINE","Salvage files from a damaged btrfs filesystem"],["TLDR","**Restore** all files from a btrfs device\n\n```sudo btrfs restore [path/to/btrfs_device] [path/to/target_directory]```\n\n**List files** (dry run) without writing\n\n```sudo btrfs restore -D [path/to/btrfs_device] [path/to/target_directory]```\n\nRestore files matching a **regex pattern**\n\n```sudo btrfs restore --path-regex [regex] -c [path/to/btrfs_device] [path/to/target_directory]```\n\nRestore using a specific **root tree**\n\n```sudo btrfs restore -t [bytenr] [path/to/btrfs_device] [path/to/target_directory]```\n\nRestore with **metadata, xattrs, and symlinks**\n\n```sudo btrfs restore -m -x -S -o [path/to/btrfs_device] [path/to/target_directory]```"],["SYNOPSIS","**btrfs restore** [_options_] _device_ _target_"],["DESCRIPTION","**btrfs restore** attempts to salvage files from a damaged btrfs filesystem that cannot be mounted normally. It reads filesystem structures directly from the device and copies recoverable files to a target directory.\n\nThis is a data recovery tool for when other methods fail. It does not repair the filesystem; it extracts data to a healthy location."],["PARAMETERS","**-D, --dry-run**\n> List files without extracting\n\n**--path-regex** _regex_\n> Only restore files matching pattern\n\n**-c**\n> Case-insensitive regex matching\n\n**-t** _bytenr_\n> Use specific root tree (see btrfs-find-root)\n\n**-m, --metadata**\n> Restore metadata (permissions, times)\n\n**-x, --xattr**\n> Restore extended attributes\n\n**-S, --symlinks**\n> Restore symbolic links\n\n**-o, --overwrite**\n> Overwrite existing files in target\n\n**-v**\n> Verbose output"],["CAVEATS","Does not fix the filesystem; only extracts data. May not recover all files from severely damaged filesystems. Regex must match full path including parent directories. Target directory should be on a different filesystem."],["INSTALL","```apt: sudo apt install btrfs-progs```\n\n```dnf: sudo dnf install btrfs-progs```\n\n```pacman: sudo pacman -S btrfs-progs```\n\n```apk: sudo apk add btrfs-progs```\n\n```brew: brew install btrfs-progs```\n\n```nix: nix profile install nixpkgs#btrfs-progs```"],["SEE ALSO","[btrfs](/man/btrfs)(8), [btrfs-rescue](/man/btrfs-rescue)(8), [btrfs-check](/man/btrfs-check)(8)"],["RESOURCES","```[Source code](https://github.com/kdave/btrfs-progs)```\n\n```[Documentation](https://btrfs.readthedocs.io/)```"]],"e":["btrfs","btrfs-rescue","btrfs-check"],"x":"sudo btrfs restore [path/to/btrfs_device] [path/to/target_directory]"},"bundletool":{"n":"bundletool","t":"Manipulate Android App Bundles and APKs","s":[["TAGLINE","Manipulate Android App Bundles and APKs"],["TLDR","**Build APK set from app bundle**\n\n```bundletool build-apks --bundle=[app.aab] --output=[app.apks]```\n\n**Build APKs with signing**\n\n```bundletool build-apks --bundle=[app.aab] --output=[app.apks] --ks=[keystore.jks] --ks-key-alias=[alias]```\n\n**Install APKs to connected device**\n\n```bundletool install-apks --apks=[app.apks]```\n\n**Extract APKs for specific device**\n\n```bundletool extract-apks --apks=[app.apks] --output-dir=[output] --device-spec=[device.json]```\n\n**Get device specification**\n\n```bundletool get-device-spec --output=[device.json]```\n\n**Validate app bundle**\n\n```bundletool validate --bundle=[app.aab]```\n\n**Print bundle info**\n\n```bundletool dump manifest --bundle=[app.aab]```\n\n**Build universal APK** containing all configurations\n\n```bundletool build-apks --mode=universal --bundle=[app.aab] --output=[app.apks]```\n\n**Build APKs for the connected device** only\n\n```bundletool build-apks --connected-device --bundle=[app.aab] --output=[app.apks]```\n\n**Get APK size estimates**\n\n```bundletool get-size total --apks=[app.apks]```"],["SYNOPSIS","**bundletool** _command_ [_options_]"],["DESCRIPTION","**bundletool** is the command-line tool for manipulating Android App Bundles. It builds app bundles, generates APK sets for various device configurations, and installs APKs to connected devices.\n\nThe tool is used by Android Studio, the Android Gradle plugin, and Google Play to build and process Android App Bundles."],["COMMANDS","**build-bundle**\n> Build Android App Bundle from module zip files\n\n**build-apks**\n> Generate APK set from app bundle\n\n**extract-apks**\n> Extract APKs for specific device configuration\n\n**install-apks**\n> Install APKs to connected device\n\n**get-device-spec**\n> Write device specification to JSON file\n\n**validate**\n> Verify app bundle validity\n\n**dump**\n> Print bundle information in human-readable form\n\n**get-size**\n> Compute download size estimates\n\n**version**\n> Print bundletool version"],["PARAMETERS","**--bundle** _file_\n> Path to Android App Bundle (.aab)\n\n**--output** _file_\n> Output file path\n\n**--apks** _file_\n> Path to APK set archive\n\n**--device-spec** _file_\n> Device specification JSON file\n\n**--ks** _file_\n> Path to keystore for signing\n\n**--ks-pass** _pass_\n> Keystore password (pass:password or file:/path)\n\n**--ks-key-alias** _alias_\n> Key alias in keystore\n\n**--key-pass** _pass_\n> Key password (pass:password or file:/path)\n\n**--connected-device**\n> Target only the currently connected Android device's configuration.\n\n**--device-id** _serial_\n> Specify a device by ADB serial number.\n\n**--mode** _mode_\n> APK generation mode: _default_, _universal_, _system_, _persistent_, _instant_, _archive_.\n\n**--local-testing**\n> Add metadata for local feature module (dynamic delivery) testing.\n\n**--overwrite**\n> Overwrite existing output file.\n\n**--aapt2** _path_\n> Path to a custom AAPT2 binary."],["CAVEATS","Requires Java Runtime Environment. Device installation requires ADB and a connected Android device. Signed APKs require keystore credentials. App bundles must be valid according to Android App Bundle specification."],["INSTALL","```brew: brew install bundletool```\n\n```nix: nix profile install nixpkgs#bundletool```"],["SEE ALSO","[adb](/man/adb)(1), [apksigner](/man/apksigner)(1), [zipalign](/man/zipalign)(1)"]],"e":["adb","apksigner","zipalign"],"x":"bundletool build-apks --bundle=[app.aab] --output=[app.apks]"},"canfield":{"n":"canfield","t":"Text-based solitaire card game","s":[["TAGLINE","Text-based solitaire card game"],["TLDR","**Start a game of Canfield solitaire**\n\n```canfield```\n\n**View your canfield score history**\n\n```cfscores```\n\n**View another user's canfield scores**\n\n```cfscores [username]```\n\n**View all users' canfield scores**\n\n```cfscores -a```"],["SYNOPSIS","**canfield**\n\n**cfscores** [**-a**] [_user_]"],["PREVIEW","```\n [Kh] [  ] [  ] [  ]\n [Ac] [2d] [3c]\n [##] [##] [##] [##]\n```"],["DESCRIPTION","**canfield** is a text-based implementation of the Canfield solitaire card game (also known as Demon Solitaire) from the BSD games collection. Cards are displayed using ASCII characters in the terminal.\n\n**Gameplay:** Build tableau piles downward in alternating colors. Entire piles must be moved as units. Top cards are available for foundation play but cannot fill empty tableau spaces, which must be filled from the stock. Cards are dealt from hand to talon in groups of three.\n\n**Commands during play:**\n- Type **ht** to deal cards to the talon\n- Single-letter commands with numbers for moves\n\n**Scoring:** Inspection costs $13, final play costs $26. Each foundation card earns $5. Each pass through the hand after the first costs $5. Playing time costs $1 per minute."],["PARAMETERS","**cfscores** options:\n\n**-a**\n> Display canfield accounts for all users\n\n**user**\n> Display scores for specified username"],["CAVEATS","The game maintains a scoring database that tracks your gambling-style account balance. The scoring system simulates the casino version of the game where players would pay to play and win money for cards placed on foundations."],["HISTORY","Canfield solitaire is named after **Richard A. Canfield**, a 19th-century American casino owner who popularized this version of solitaire in his gambling houses. The BSD games implementation was originally written by Steve Levine with contributions from Steve Feldman, Kirk McKusick, Mikey Olson, and Eric Allman."],["SEE ALSO","[mille](/man/mille)(6)"]],"e":["mille"],"x":"canfield"},"cbindgen":{"n":"cbindgen","t":"generate C/C++ headers from Rust FFI libraries","s":[["TAGLINE","generate C/C++ headers from Rust FFI libraries"],["TLDR","**Generate C header from Rust library**\n\n```cbindgen -o [header.h]```\n\n**Generate C++ header**\n\n```cbindgen --lang c++ -o [header.hpp]```\n\n**Generate C header explicitly**\n\n```cbindgen --lang c -o [header.h]```\n\n**Use specific config file**\n\n```cbindgen --config [cbindgen.toml] -o [header.h]```\n\n**Generate from specific crate**\n\n```cbindgen --crate [crate_name] -o [header.h]```\n\n**Show help**\n\n```cbindgen --help```"],["SYNOPSIS","**cbindgen** [_options_] [_crate_directory_]"],["DESCRIPTION","**cbindgen** generates C and C++11 headers from Rust libraries that expose a public C API. It parses Rust source code and creates corresponding C declarations for FFI-exported types and functions.\n\nDeveloped by Mozilla, it ensures generated headers match Rust's type layout and ABI guarantees."],["PARAMETERS","**-o**, **--output** _file_\n> Output header file path\n\n**--lang** _language_\n> Output language: c or c++ (default: c++)\n\n**--config** _file_\n> Path to cbindgen.toml configuration\n\n**--crate** _name_\n> Crate name to generate bindings for\n\n**--profile** _name_\n> Cargo profile to use\n\n**-v**, **--verbose**\n> Enable verbose output\n\n**-q**, **--quiet**\n> Suppress output\n\n**--verify**\n> Verify existing header matches generated\n\n**--help**\n> Show help"],["CONFIGURATION","**cbindgen.toml**\n> Controls output language, header guards, include directives, formatting style, and type renaming rules."],["BUILD SCRIPT USAGE","Add to build.rs for automatic generation on build."],["CAVEATS","Only generates headers for pub extern \"C\" functions and #[repr(C)] types. Requires proper FFI annotations in Rust code. Configuration file customizes output format."],["INSTALL","```apt: sudo apt install cbindgen```\n\n```dnf: sudo dnf install cbindgen```\n\n```pacman: sudo pacman -S cbindgen```\n\n```apk: sudo apk add cbindgen```\n\n```brew: brew install cbindgen```"],["SEE ALSO","[cargo](/man/cargo)(1), [bindgen](/man/bindgen)(1), [rustc](/man/rustc)(1)"],["RESOURCES","```[Source code](https://github.com/mozilla/cbindgen)```\n\n```[Documentation](https://github.com/mozilla/cbindgen/blob/master/docs.md)```"]],"e":["cargo","bindgen","rustc"],"x":"cbindgen -o [header.h]"},"clusterdb":{"n":"clusterdb","t":"PostgreSQL table reclustering utility","s":[["TAGLINE","PostgreSQL table reclustering utility"],["TLDR","**Cluster all tables** in a database\n\n```clusterdb [database_name]```\n\n**Cluster a specific table**\n\n```clusterdb --table [table_name] [database_name]```\n\n**Cluster all databases** on the server\n\n```clusterdb --all```\n\n**Cluster with connection options**\n\n```clusterdb --host [hostname] --port [5432] --username [user] [database]```\n\n**Cluster with verbose output**\n\n```clusterdb --verbose [database_name]```\n\n**Cluster using a specific index**\n\n```clusterdb --table [table_name] --index [index_name] [database_name]```\n\n**Echo commands being executed**\n\n```clusterdb --echo [database_name]```"],["SYNOPSIS","**clusterdb** [_-a_] [_-d dbname_] [_-t table_] [_-i index_] [_-v_] [_-e_] [_connection-options_] [_dbname_]"],["PARAMETERS","**-a**, **--all**\n> Cluster all databases on the server.\n\n**-d**, **--dbname** _dbname_\n> Database to cluster.\n\n**-t**, **--table** _table_\n> Cluster only the specified table.\n\n**-i**, **--index** _index_\n> Use the specified index for clustering the table.\n\n**-v**, **--verbose**\n> Print detailed information during processing.\n\n**-e**, **--echo**\n> Echo the commands being sent to the server.\n\n**-q**, **--quiet**\n> Do not display progress messages.\n\n**-h**, **--host** _hostname_\n> Database server host (default: local socket or localhost).\n\n**-p**, **--port** _port_\n> Database server port (default: 5432).\n\n**-U**, **--username** _username_\n> Username to connect as.\n\n**-w**, **--no-password**\n> Never prompt for password.\n\n**-W**, **--password**\n> Force password prompt.\n\n**--maintenance-db** _dbname_\n> Database to connect to when using `-a`/`--all` (defaults to `postgres` or `template1`).\n\n**-V**, **--version**\n> Print version information.\n\n**-?**, **--help**\n> Show help about command-line arguments."],["DESCRIPTION","**clusterdb** is a PostgreSQL utility for reclustering tables in a database. Clustering physically reorders a table's data rows to match the order of an index, which can significantly improve performance for range queries that use that index.\n\nWhen a table is clustered on an index, data rows are physically sorted in index order on disk. This reduces disk I/O for queries that scan ranges of index values, as sequential disk access is faster than random access. However, the cluster ordering degrades over time as rows are inserted, updated, or deleted.\n\nThe command is a wrapper around the SQL **CLUSTER** command, providing a convenient way to cluster tables from the command line. Without the **-t** option, it clusters all previously-clustered tables in the database.\n\nClustering is a one-time operation that does not maintain physical order over time. Regular reclustering may be needed for tables with frequent updates. The operation requires exclusive access to the table and can be resource-intensive for large tables."],["CAVEATS","Requires exclusive lock on the table during operation, blocking all other access. Large tables may take significant time and temporary disk space. Does not maintain cluster order after subsequent modifications. Only one index can define the cluster order for a table."],["HISTORY","**clusterdb** has been part of the PostgreSQL distribution since early versions, providing command-line access to the CLUSTER functionality. The CLUSTER command itself dates back to PostgreSQL's origins as a research database at UC Berkeley. The utility follows PostgreSQL's pattern of providing command-line wrappers for common administrative tasks."],["INSTALL","```apk: sudo apk add postgresql17-client```"],["SEE ALSO","[psql](/man/psql)(1), [vacuumdb](/man/vacuumdb)(1), [reindexdb](/man/reindexdb)(1)"]],"e":["psql","vacuumdb","reindexdb"],"x":"clusterdb [database_name]"},"cppclean":{"n":"cppclean","t":"C++ unused code and header finder","s":[["TAGLINE","C++ unused code and header finder"],["TLDR","**Find problems** in a directory\n\n```cppclean [path/to/source]```\n\n**Check specific files**\n\n```cppclean [file1.cpp] [file2.cpp]```\n\n**Include additional header search paths**\n\n```cppclean --include-path [/path/to/includes] [source/]```\n\n**Run with verbose output**\n\n```cppclean --verbose [path/to/source]```\n\n**Suppress output messages**\n\n```cppclean --quiet [path/to/source]```"],["SYNOPSIS","**cppclean** [_options_] _paths..._"],["DESCRIPTION","**cppclean** finds problems in C++ source that slow development in large code bases, including various forms of unused code. It detects unnecessary #include directives in header files, unused forward declarations, functions declared but not defined, and classes with virtual methods but no virtual destructor.\n\nThe tool identifies which headers can be safely removed, reducing compilation dependencies and speeding up incremental builds. It also detects inconsistent casing in #include paths (e.g., `foo.h` vs `Foo.h`) and global/static data that may cause problems with threads."],["PARAMETERS","**--include-path** _path_\n> Additional include search path (like gcc -I). Can be specified multiple times. Not recursive.\n\n**--verbose**\n> Enable verbose output for debugging.\n\n**--quiet**\n> Suppress output messages."],["CAVEATS","May report false positives for conditionally compiled code (e.g., `#ifdef` blocks). Results should be verified before removing includes. Detection of unnecessary includes in source files (not headers) is limited."],["INSTALL","```nix: nix profile install nixpkgs#cppclean```"],["SEE ALSO","[clang-tidy](/man/clang-tidy)(1), [cppcheck](/man/cppcheck)(1), [cpplint](/man/cpplint)(1), [gcc](/man/gcc)(1), [cmake](/man/cmake)(1)"]],"e":["clang-tidy","cppcheck","cpplint","gcc","cmake"],"x":"cppclean [path/to/source]"},"csvclean":{"n":"csvclean","t":"Validate and clean CSV files","s":[["TAGLINE","Validate and clean CSV files"],["TLDR","**Check for rows with length mismatches** in a CSV file\n\n```csvclean --length-mismatch [data.csv]```\n\n**Report length mismatches** and omit error rows from output\n\n```csvclean --length-mismatch --omit-error-rows [data.csv]```\n\n**Report empty columns** as errors\n\n```csvclean --empty-columns [data.csv]```\n\n**Enable all checks** on a CSV file\n\n```csvclean -a [data.csv]```\n\n**Fix short rows by joining** them into preceding rows\n\n```csvclean --join-short-rows [data.csv]```\n\n**Fill short rows** with a specified value\n\n```csvclean --fill-short-rows --fillvalue \"N/A\" [data.csv]```\n\n**Validate with custom delimiter** and encoding\n\n```csvclean --length-mismatch -d \"[;]\" -e [latin1] [data.csv]```"],["SYNOPSIS","**csvclean** [_options_] [_file_]"],["PARAMETERS","_FILE_\n> CSV file to clean or validate. Reads from stdin if omitted.\n\n**--length-mismatch**\n> Report rows that are shorter or longer than the header row.\n\n**--empty-columns**\n> Report empty columns as errors.\n\n**-a**, **--enable-all-checks**\n> Enable all error reporting checks.\n\n**--join-short-rows**\n> Merge consecutive short rows into a single row.\n\n**--separator** _SEPARATOR_\n> String used to join short rows (default: newline).\n\n**--fill-short-rows**\n> Fill short rows with missing values.\n\n**--fillvalue** _VALUE_\n> Value used to fill short rows (default: empty string).\n\n**--omit-error-rows**\n> Exclude rows containing errors from standard output.\n\n**--label** _LABEL_\n> Add a label column to error output for automated workflows.\n\n**--header-normalize-space**\n> Strip leading/trailing whitespace and normalize whitespace in headers.\n\n**-d** _CHAR_, **--delimiter** _CHAR_\n> Field delimiter (default: comma).\n\n**-t**, **--tabs**\n> Use tabs as delimiter.\n\n**-q** _CHAR_, **--quotechar** _CHAR_\n> Quote character (default: double quote).\n\n**-p** _CHAR_, **--escapechar** _CHAR_\n> Escape character for the delimiter or quote character.\n\n**-e** _ENCODING_, **--encoding** _ENCODING_\n> Input file encoding.\n\n**-S**, **--no-header-row**\n> File has no header row.\n\n**-H**\n> Omit the header row from output.\n\n**-K** _N_, **--skip-lines** _N_\n> Skip the first N lines of the input file.\n\n**-v**\n> Verbose error output."],["DESCRIPTION","**csvclean** is part of csvkit that validates and cleans CSV files. It detects common problems like inconsistent column counts, empty columns, and encoding issues.\n\nSince csvkit 2.0, csvclean no longer reports or fixes errors by default. You must explicitly enable checks (such as **--length-mismatch** or **--empty-columns**) or fixes (such as **--join-short-rows** or **--fill-short-rows**). Output is written to standard output and errors to standard error.\n\nThe tool handles various CSV dialects and can work with files using different delimiters, quote characters, and encodings. It is essential for preprocessing messy data before analysis."],["CAVEATS","Automatic cleaning may alter data in unexpected ways; review cleaned output carefully. Since csvkit 2.0, running csvclean without any check or fix flags will produce an error. Original file is not modified."],["HISTORY","csvclean is part of **csvkit**, created by Christopher Groskopf and first released in **2011**. csvkit provides a suite of tools for working with CSV files, designed to bring the power of Unix philosophy to tabular data processing."],["SEE ALSO","[csvstat](/man/csvstat)(1), [csvcut](/man/csvcut)(1), [csvlook](/man/csvlook)(1), [csvformat](/man/csvformat)(1), [csvgrep](/man/csvgrep)(1)"]],"e":["csvstat","csvcut","csvlook","csvformat","csvgrep"],"x":"csvclean --length-mismatch [data.csv]"},"difft":{"n":"difft","t":"syntax-aware structural diff tool","s":[["TAGLINE","syntax-aware structural diff tool"],["TLDR","**Compare two files** with structural diffing\n\n```difft [old_file] [new_file]```\n\n**Compare files with side-by-side output**\n\n```difft --display side-by-side [old_file] [new_file]```\n\n**Show only changed lines** (inline display)\n\n```difft --display inline [old_file] [new_file]```\n\n**Compare with specific language** parsing\n\n```difft --language [rust] [old.rs] [new.rs]```\n\n**Compare directories**\n\n```difft [old_directory] [new_directory]```\n\n**Use with git diff**\n\n```git diff --external-diff difft```\n\n**Set as default git diff tool**\n\n```git config --global diff.external difft```\n\n**Compare and skip unchanged sections**\n\n```difft --skip-unchanged [old_file] [new_file]```"],["SYNOPSIS","**difft** [_options_] _old_path_ _new_path_"],["PARAMETERS","**--display** _mode_\n> Output format: side-by-side, side-by-side-show-both, inline (default).\n\n**--language** _lang_\n> Force a specific language parser (auto-detected by default).\n\n**--list-languages**\n> Show all supported languages.\n\n**--skip-unchanged**\n> Don't show unchanged parts of the file.\n\n**--context** _n_\n> Number of context lines (default: 3).\n\n**--width** _n_\n> Maximum display width (default: terminal width).\n\n**--color** _when_\n> Color output: always, never, auto.\n\n**--background** _type_\n> Background color: light, dark (default: dark).\n\n**--syntax-highlight** _on|off_\n> Enable or disable syntax highlighting.\n\n**--tab-width** _n_\n> Tab display width (default: 4).\n\n**--parse-error-limit** _n_\n> Maximum number of parse errors before falling back to text diff.\n\n**--byte-limit** _n_\n> Skip files larger than this size.\n\n**--graph-limit** _n_\n> Maximum graph size for structural diff algorithm.\n\n**--exit-code**\n> Set exit code to 1 if there are syntactic changes (useful in scripts)."],["DESCRIPTION","**difft** (Difftastic) is a structural diff tool that understands programming language syntax. Unlike line-based diff tools, it parses source code and compares the actual structure, showing meaningful changes while ignoring formatting differences.\n\nThe tool builds abstract syntax trees (ASTs) for both versions of a file, then computes the minimal structural difference. This means it can recognize when code is moved or refactored without treating it as completely different, and it won't flag changes that are purely whitespace or formatting.\n\nDifftastic supports over 50 programming languages with dedicated parsers built using tree-sitter. For unsupported files, it falls back to a text-based diff. Language detection is automatic based on file extension, but can be overridden.\n\nThe side-by-side display shows old and new versions in parallel columns with colors indicating additions (green), deletions (red), and modifications. The inline display shows changes in a more compact format. Git integration allows using difft as the external diff tool."],["CAVEATS","Large files or very different structures may hit performance limits. Parse errors cause fallback to text diff. Some language features may not be perfectly parsed. Extremely complex structural changes may produce hard-to-follow output."],["HISTORY","**Difftastic** was created by Wilfred Hughes starting in **2020**. It was developed to address the limitations of line-based diffs, which often produce noisy output for refactoring or reformatting changes. The tool uses tree-sitter parsers for language-aware structural comparison and has gained popularity as a git diff replacement."],["INSTALL","```dnf: sudo dnf install difftastic```\n\n```pacman: sudo pacman -S difftastic```\n\n```apk: sudo apk add difftastic```\n\n```zypper: sudo zypper install difftastic```\n\n```brew: brew install difftastic```\n\n```nix: nix profile install nixpkgs#difftastic```"],["SEE ALSO","[diff](/man/diff)(1), [git-diff](/man/git-diff)(1), [delta](/man/delta)(1), [colordiff](/man/colordiff)(1)"]],"e":["diff","git-diff","delta","colordiff"],"x":"difft [old_file] [new_file]"},"doctl-kubernetes-options":{"n":"doctl-kubernetes-options","t":"enumerate available Kubernetes configuration values","s":[["TAGLINE","enumerate available Kubernetes configuration values"],["TLDR","**List Kubernetes versions**\n\n```doctl kubernetes options versions```\n\n**List available regions**\n\n```doctl kubernetes options regions```\n\n**List node sizes**\n\n```doctl kubernetes options sizes```\n\n**List versions in a specific output format**\n\n```doctl kubernetes options versions --format [slug]```"],["SYNOPSIS","**doctl kubernetes options** _command_"],["PARAMETERS","_COMMAND_\n> Option type: versions, regions, sizes.\n\n**versions**\n> List available Kubernetes versions.\n\n**regions**\n> List regions supporting Kubernetes.\n\n**sizes**\n> List machine sizes available for node pools.\n\n**--format** _columns_\n> Columns for output in a comma-separated list.\n\n**--no-header**\n> Return raw data with no headers.\n\n**--help**\n> Display help information."],["DESCRIPTION","**doctl kubernetes options** enumerates valid values for use with DigitalOcean Kubernetes (DOKS) cluster commands. It helps users determine valid values for cluster creation and modification, such as supported Kubernetes versions, regions where DOKS is available, and machine sizes suitable for worker nodes.\n\nUsing these options ensures cluster configurations are valid and helps identify available resources in different regions."],["CAVEATS","Options may change as DigitalOcean updates offerings. Not all sizes available in all regions. Kubernetes versions have support lifecycles."],["HISTORY","doctl kubernetes options is part of the Kubernetes management commands added to **doctl** when DigitalOcean launched their managed Kubernetes service (DOKS) in **2018**."],["SEE ALSO","[doctl](/man/doctl)(1), [kubectl](/man/kubectl)(1), [doctl-kubernetes-cluster](/man/doctl-kubernetes-cluster)(1)"]],"e":["doctl","kubectl","doctl-kubernetes-cluster"],"x":"doctl kubernetes options versions"},"dpkg":{"n":"dpkg","t":"low-level Debian package manager","s":[["TAGLINE","low-level Debian package manager"],["TLDR","**Install** a package\n\n```sudo dpkg -i [path/to/file.deb]```\n\n**Remove** a package\n\n```sudo dpkg -r [package]```\n\n**List** installed packages\n\n```dpkg -l [pattern]```\n\nList a package's **contents**\n\n```dpkg -L [package]```\n\nList contents of a **local package file**\n\n```dpkg -c [path/to/file.deb]```\n\nFind which package **owns** a file\n\n```dpkg -S [path/to/file]```\n\n**Purge** a package including configuration files\n\n```sudo dpkg -P [package]```\n\nShow package **status**\n\n```dpkg -s [package]```"],["SYNOPSIS","**dpkg** [_options_] _action_ [_package_|_file_]"],["DESCRIPTION","**dpkg** is the low-level package manager for Debian-based systems. It handles the installation, removal, and management of .deb packages. Higher-level tools like **apt** and **apt-get** use dpkg internally."],["PARAMETERS","**-i, --install file.deb**\n> Install a package from a .deb file\n\n**-r, --remove package**\n> Remove a package (keep configuration files)\n\n**-P, --purge package**\n> Remove a package and its configuration files\n\n**-l, --list pattern**\n> List packages matching pattern\n\n**-L, --listfiles package**\n> List files installed by a package\n\n**-c, --contents file.deb**\n> List contents of a .deb file\n\n**-S, --search path**\n> Search for a package owning a file\n\n**-s, --status package**\n> Display package status details\n\n**--configure package**\n> Configure an unpacked package\n\n**--unpack file.deb**\n> Unpack but don't configure a package\n\n**--get-selections** [_pattern_]\n> Get list of package selections\n\n**--set-selections**\n> Set package selections from stdin\n\n**--audit**, **-C**\n> Search for partially installed packages and suggest how to fix them.\n\n**--force-**_things_\n> Override safety checks (e.g. **--force-all**, **--force-overwrite**). Use with care.\n\n**--no-act**, **--dry-run**, **--simulate**\n> Show what would be done without making changes."],["CAVEATS","dpkg does not handle dependencies automatically. Use **apt** or **apt-get** for dependency resolution. Configuration files are kept by default when removing packages."],["HISTORY","**dpkg** was originally created for the **Debian** project in **1994** by **Ian Murdock**, then rewritten in C by **Ian Jackson** who became its primary developer. It remains the foundation of package management on Debian-based systems."],["INSTALL","```apt: sudo apt install dpkg```\n\n```dnf: sudo dnf install dpkg```\n\n```pacman: sudo pacman -S dpkg```\n\n```apk: sudo apk add dpkg```\n\n```zypper: sudo zypper install dpkg```\n\n```brew: brew install dpkg```\n\n```nix: nix profile install nixpkgs#dpkg```"],["SEE ALSO","[apt](/man/apt)(8), [apt-get](/man/apt-get)(8), [dpkg-deb](/man/dpkg-deb)(1), [dpkg-query](/man/dpkg-query)(1)"]],"e":["apt","apt-get","dpkg-deb","dpkg-query"],"x":"sudo dpkg -i [path/to/file.deb]"},"drive":{"n":"drive","t":"command-line client for Google Drive","s":[["TAGLINE","command-line client for Google Drive"],["TLDR","**Initialize drive in directory**\n\n```drive init [~/gdrive]```\n\n**Pull files from Google Drive**\n\n```drive pull [path/to/file]```\n\n**Push files to Google Drive**\n\n```drive push [path/to/file]```\n\n**List files in Google Drive**\n\n```drive list```\n\n**Pull all changes**\n\n```drive pull -all```\n\n**Push with force**\n\n```drive push -force [path]```\n\n**Show differences**\n\n```drive diff [path]```"],["SYNOPSIS","**drive** _command_ [_options_] [_path_]"],["PARAMETERS","**init**\n> Initialize drive context in directory.\n\n**pull** [_path_]\n> Download files from Google Drive.\n\n**push** [_path_]\n> Upload files to Google Drive.\n\n**list** [_path_]\n> List files and directories.\n\n**diff** [_path_]\n> Show differences between local and remote.\n\n**trash** [_path_]\n> Move files to trash.\n\n**untrash** [_path_]\n> Restore files from trash.\n\n**delete** [_path_]\n> Permanently delete files.\n\n**-all**\n> Apply operation to all files.\n\n**-force**\n> Force operation without confirmation.\n\n**-no-prompt**\n> Disable confirmation prompts.\n\n**-depth** _n_\n> Limit directory traversal depth."],["DESCRIPTION","**drive** is a command-line client for Google Drive that deliberately borrows Git's model: you **pull** and **push** rather than letting a daemon sync in the background. Nothing moves until you say so, which is the whole appeal for anyone who has watched an automatic sync client propagate a mistake.\n\n`drive init` creates a context directory (`.gd/`) that marks a local folder as the root of a Drive mirror, much as `.git/` marks a repository. Subsequent commands compare local state against the remote and report what would change; `drive diff` shows the differences without transferring anything.\n\nNote that its flags are Go-style **single-dash** long options: `-force`, `-no-prompt`, `-depth`, not `--force`. This trips up people who expect GNU conventions."],["CAVEATS","The project is **effectively dormant**: the last upstream commit was in early **2024**, and Google's Drive API and OAuth requirements have continued to move. New installations often stumble on OAuth setup, which now expects you to register your own client credentials in a Google Cloud project rather than use a bundled key.\n\nFor most people **rclone** is the better tool today: it is actively maintained, handles Drive alongside dozens of other backends, supports server-side copies and encryption, and has a far more capable sync engine.\n\nThere is no conflict resolution to speak of. When a file has changed on both sides, drive asks, and `-force` simply overwrites, so a careless push can discard remote edits. Google Docs, Sheets, and Slides are not real files and cannot be pulled as-is; they must be exported to a concrete format."],["HISTORY","drive was written by **Emmanuel Odeke** in **2015**, at a time when Google shipped no official Drive client for Linux at all and the alternatives were FUSE mounts that tended to hang. Its Git-like explicit push/pull model was a direct reaction to the automatic sync clients on other platforms, and it earned a large following on that basis. It has since been overtaken by rclone, which covers the same ground and much more."],["INSTALL","```nix: nix profile install nixpkgs#drive```"],["SEE ALSO","[rclone](/man/rclone)(1), [gdrive](/man/gdrive)(1), [rsync](/man/rsync)(1), [git](/man/git)(1)"],["RESOURCES","```[Source code](https://github.com/odeke-em/drive)```"]],"e":["rclone","gdrive","rsync","git"],"x":"drive init [~/gdrive]"},"dvips":{"n":"dvips","t":"convert DVI to PostScript","s":[["TAGLINE","convert DVI to PostScript"],["TLDR","**Convert DVI to PostScript**\n\n```dvips [document.dvi] -o [document.ps]```\n\n**Convert specific pages**\n\n```dvips -p [5] -l [10] [document.dvi] -o [output.ps]```\n\n**Output to stdout**\n\n```dvips [document.dvi] -o -```\n\n**Convert for specific paper size**\n\n```dvips -t [letter] [document.dvi] -o [output.ps]```\n\n**Convert in landscape mode**\n\n```dvips -t landscape [document.dvi] -o [output.ps]```\n\n**Generate PDF via ps2pdf**\n\n```dvips [document.dvi] -o - | ps2pdf - [document.pdf]```"],["SYNOPSIS","**dvips** [_options_] _file.dvi_"],["PARAMETERS","**-o** _file_\n> Output file name (- for stdout).\n\n**-p** _num_\n> First page to output.\n\n**-l** _num_\n> Last page to output.\n\n**-t** _paper_\n> Paper type: letter, a4, landscape.\n\n**-D** _dpi_\n> Resolution in dots per inch.\n\n**-x** _mag_\n> Magnification factor.\n\n**-c** _copies_\n> Number of copies.\n\n**-e** _num_\n> Maximum drift in pixels.\n\n**-Z**\n> Compress bitmap fonts.\n\n**-Ppdf**\n> Optimize for PDF conversion."],["DESCRIPTION","**dvips** converts DVI (DeVice Independent) files produced by TeX and LaTeX into PostScript. The output can be printed directly or converted to PDF using tools like ps2pdf.\n\nThe program processes DVI instructions, embeds fonts, and generates PostScript code. It supports font substitution, paper size selection, and various optimization options for different output devices."],["CAVEATS","Requires Type 1 or bitmap fonts to be installed. Some modern TeX workflows use pdflatex or xelatex directly, bypassing DVI. PostScript output may be large; use -Z for compression. Font paths must be configured in texmf.cnf."],["HISTORY","dvips was written by **Tomas Rokicki** in the late **1980s** and has been a standard part of TeX distributions since. It remains widely used despite direct PDF generation becoming more common. The program is maintained as part of TeX Live."],["INSTALL","```apt: sudo apt install texlive-binaries```\n\n```apk: sudo apk add texlive-dvi```"],["SEE ALSO","[latex](/man/latex)(1), [ps2pdf](/man/ps2pdf)(1), [pdflatex](/man/pdflatex)(1), [tex](/man/tex)(1)"]],"e":["latex","ps2pdf","pdflatex","tex"],"x":"dvips [document.dvi] -o [document.ps]"},"ffuf":{"n":"ffuf","t":"fast web fuzzer written in Go","s":[["TAGLINE","fast web fuzzer written in Go"],["TLDR","**Directory fuzzing**\n\n```ffuf -u [https://target.com/FUZZ] -w [wordlist.txt]```\n\n**POST parameter fuzzing**\n\n```ffuf -u [https://target.com/api] -w [wordlist.txt] -X POST -d \"param=FUZZ\"```\n\n**Filter by status** code\n\n```ffuf -u [https://target.com/FUZZ] -w [wordlist.txt] -fc [404]```\n\n**Match response size**\n\n```ffuf -u [https://target.com/FUZZ] -w [wordlist.txt] -ms [1234]```\n\n**Multiple wordlists**\n\n```ffuf -u [https://target.com/FUZZ1/FUZZ2] -w [list1.txt]:FUZZ1 -w [list2.txt]:FUZZ2```\n\n**Virtual host discovery via Host header**\n\n```ffuf -u [https://target.com] -w [vhosts.txt] -H \"Host: FUZZ.target.com\"```\n\n**Recursive discovery with extensions**\n\n```ffuf -u [https://target.com/FUZZ] -w [wordlist.txt] -recursion -e [.php,.html,.txt]```"],["SYNOPSIS","**ffuf** [_options_]"],["PARAMETERS","**-u** _URL_\n> Target URL with FUZZ keyword.\n\n**-w** _WORDLIST_\n> Wordlist file.\n\n**-X** _METHOD_\n> HTTP method.\n\n**-d** _DATA_\n> POST data.\n\n**-H** _HEADER_\n> Custom header.\n\n**-fc** _CODES_\n> Filter out responses by status code.\n\n**-fs** _SIZE_\n> Filter out responses by size.\n\n**-fw** _COUNT_\n> Filter out responses by word count.\n\n**-fl** _COUNT_\n> Filter out responses by line count.\n\n**-mc** _CODES_\n> Match status codes (default 200-299,301,302,307,401,403,405,500).\n\n**-ms** _SIZE_\n> Match response size.\n\n**-e** _EXTENSIONS_\n> Comma-separated extensions to append to each word.\n\n**-recursion**\n> Scan recursively into discovered directories.\n\n**-recursion-depth** _N_\n> Maximum recursion depth.\n\n**-t** _N_\n> Number of concurrent threads (default 40).\n\n**-rate** _N_\n> Rate limit in requests per second (0 = unlimited).\n\n**-o** _FILE_\n> Write results to a file.\n\n**-of** _FORMAT_\n> Output format (json, csv, html, md, ...).\n\n**-c**\n> Colorize output.\n\n**-s**\n> Silent mode, show only results.\n\n**--help**\n> Display help information."],["DESCRIPTION","**ffuf** (Fuzz Faster U Fool) is a fast web fuzzer written in Go. It discovers hidden content, parameters, and vulnerabilities by sending requests with wordlist entries substituted at marked positions.\n\nThe tool excels at content discovery, parameter fuzzing, and virtual host enumeration. Filtering options allow matching or excluding responses by status, size, words, or lines.\n\nffuf's speed and flexibility make it popular for penetration testing and security assessments."],["CAVEATS","Only use against authorized targets. High thread counts may overwhelm servers. Results need manual verification."],["HISTORY","ffuf was created by **joohoi** as a fast, flexible web fuzzer. Written in Go for performance, it has become a standard tool in web application security testing."],["INSTALL","```apt: sudo apt install ffuf```\n\n```dnf: sudo dnf install ffuf```\n\n```apk: sudo apk add ffuf```\n\n```brew: brew install ffuf```\n\n```nix: nix profile install nixpkgs#ffuf```"],["SEE ALSO","[gobuster](/man/gobuster)(1), [dirb](/man/dirb)(1), [wfuzz](/man/wfuzz)(1)"]],"e":["gobuster","dirb","wfuzz"],"x":"ffuf -u [https://target.com/FUZZ] -w [wordlist.txt]"},"fortune":{"n":"fortune","t":"random quotation and fortune display","s":[["TAGLINE","random quotation and fortune display"],["TLDR","**Display random fortune**\n\n```fortune```\n\n**Show short fortunes**\n\n```fortune -s```\n\n**Show offensive fortunes**\n\n```fortune -o```\n\n**Use specific database**\n\n```fortune [literature]```\n\n**Show fortune source**\n\n```fortune -c```"],["SYNOPSIS","**fortune** [_options_] [_databases_...]"],["PARAMETERS","_DATABASES_\n> Fortune cookie files to use.\n\n**-s**\n> Short fortunes only (< 160 chars).\n\n**-l**\n> Long fortunes only.\n\n**-c**\n> Show the fortune file source.\n\n**-o**\n> Include offensive fortunes.\n\n**-a**\n> Include all fortune files.\n\n**-f**\n> List available fortune files.\n\n**-m** _PATTERN_\n> Match pattern in fortunes.\n\n**--help**\n> Display help information."],["DESCRIPTION","**fortune** displays a random quotation, joke, or saying from its database files. It's the classic Unix program for adding whimsy to terminals and login sessions.\n\nMultiple fortune databases cover categories like literature, riddles, definitions, and jokes. Custom database files can be created with the strfile utility.\n\nfortune is commonly added to shell startup files for daily motivation or amusement."],["CAVEATS","Offensive fortunes disabled by default. Database availability varies by installation. Some content may be dated."],["HISTORY","fortune originated in **Unix Version 7** (1979), inspired by fortune cookies. It became a standard part of Unix systems, providing entertainment and occasionally wisdom at the command line."],["INSTALL","```apk: sudo apk add fortune```\n\n```zypper: sudo zypper install fortune```\n\n```brew: brew install fortune```\n\n```nix: nix profile install nixpkgs#fortune```"],["SEE ALSO","[cowsay](/man/cowsay)(1), [figlet](/man/figlet)(1)"],["RESOURCES","```[Source code](https://github.com/shlomif/fortune-mod)```"]],"e":["cowsay","figlet"],"x":"fortune"},"git-check-attr":{"n":"git-check-attr","t":"Display gitattributes information for files","s":[["TAGLINE","Display gitattributes information for files"],["TLDR","**Check attribute for file**\n\n```git check-attr [attribute] [file]```\n\n**Check all attributes**\n\n```git check-attr -a [file]```\n\n**Check for multiple files**\n\n```git check-attr [attribute] [file1] [file2]```\n\n**Check from stdin**\n\n```echo \"[file]\" | git check-attr --stdin [attribute]```"],["SYNOPSIS","**git** **check-attr** [_options_] _attribute_... _pathname_..."],["PARAMETERS","**-a**, **--all**\n> Check all attributes.\n\n**--stdin**\n> Read pathnames from stdin.\n\n**-z**\n> NUL-terminated output."],["DESCRIPTION","**git check-attr** displays gitattributes information for specified paths, revealing how Git will handle files according to .gitattributes configuration. This debugging tool shows the effective attribute values after all gitattributes rules are applied.\n\nGitattributes control various behaviors including line ending conversion (text/eol), diff drivers, merge strategies, export handling, and language detection. The command resolves attribute values from .gitattributes files in the working tree, index, and repository.\n\nOutput shows each path with its attribute values, using \"set\", \"unset\", or \"unspecified\" status. This helps debug unexpected Git behavior related to file handling, such as incorrect line ending conversions or merge conflicts in generated files."],["CONFIGURATION","**.gitattributes**\n> File attribute patterns controlling line endings, diff behavior, merge strategies, and other file-specific Git operations."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-check-ignore](/man/git-check-ignore)(1)"]],"e":["git-check-ignore"],"x":"git check-attr [attribute] [file]"},"gnmic":{"n":"gnmic","t":"gNMI network management CLI client","s":[["TAGLINE","gNMI network management CLI client"],["TLDR","**Get configuration**\n\n```gnmic -a [router:57400] get --path [/interfaces]```\n\n**Set value**\n\n```gnmic -a [router:57400] set --update-path [/interface/name] --update-value \"[eth0]\"```\n\n**Subscribe to telemetry**\n\n```gnmic -a [router:57400] subscribe --path [/interfaces/interface/statistics]```\n\n**List capabilities**\n\n```gnmic -a [router:57400] capabilities```"],["SYNOPSIS","**gnmic** [_global-flags_] _command_ [_command-flags_]"],["PARAMETERS","**capabilities**\n> Query target gNMI capabilities.\n\n**get**\n> Get configuration/state.\n\n**set**\n> Set configuration.\n\n**subscribe**\n> Subscribe to telemetry.\n\n**path**\n> Generate or search YANG paths from loaded models.\n\n**prompt**\n> Interactive mode with path autocompletion.\n\n**diff**\n> Compare gNMI GetResponses or config states.\n\n**version**\n> Show gnmic version.\n\n**-a**, **--address** _TARGET_\n> Comma-separated address(es) of the target(s), host:port.\n\n**-u**, **--username** _USER_\n> Username for authentication.\n\n**-p**, **--password** _PASS_\n> Password for authentication.\n\n**--path** _PATH_\n> YANG/gNMI path.\n\n**-e**, **--encoding** _ENCODING_\n> Data encoding: json, json_ietf, bytes, proto, ascii, or config.\n\n**--insecure**\n> Disable TLS on the connection to the target.\n\n**--skip-verify**\n> Skip verification of the target's TLS certificate.\n\n**--tls-ca** _FILE_\n> TLS CA certificate file.\n\n**--tls-cert** _FILE_\n> TLS client certificate file.\n\n**--tls-key** _FILE_\n> TLS client key file.\n\n**--timeout** _DURATION_\n> gRPC timeout after which a request is cancelled (default 10s).\n\n**--config** _FILE_\n> Configuration file (default searches for .gnmic.yaml).\n\n**--log**\n> Enable logging to stderr.\n\n**--help**\n> Display help information."],["DESCRIPTION","**gnmic** is a gNMI (gRPC Network Management Interface) CLI client. It interacts with network devices supporting gNMI for configuration retrieval, modification, and streaming telemetry.\n\nThe tool supports get, set, subscribe, and capability operations. It is used for network automation and monitoring with modern network operating systems."],["CONFIGURATION","**~/.gnmic.yaml**\n> Global configuration file for targets, credentials, and default settings.\n\n**.gnmic.yaml**\n> Per-directory configuration file for project-specific settings."],["CAVEATS","Target must support gNMI. Authentication may be required. YANG model knowledge helpful."],["HISTORY","gnmic was created by **Nokia** to provide a flexible gNMI client for network automation and telemetry collection."],["INSTALL","```brew: brew install gnmic```\n\n```nix: nix profile install nixpkgs#gnmic```"],["SEE ALSO","[gnmic-get](/man/gnmic-get)(1), [gnmic-set](/man/gnmic-set)(1), [gnmic-subscribe](/man/gnmic-subscribe)(1)"],["RESOURCES","```[Source code](https://github.com/openconfig/gnmic)```\n\n```[Documentation](https://gnmic.openconfig.net/)```"]],"e":["gnmic-get","gnmic-set","gnmic-subscribe"],"x":"gnmic -a [router:57400] get --path [/interfaces]"},"gradle-dependencyinsight":{"n":"gradle-dependencyinsight","t":"Gradle dependency tracing tool","s":[["TAGLINE","Gradle dependency tracing tool"],["TLDR","**Trace a dependency** to see why it is included\n\n```gradle dependencyInsight --dependency [library-name]```\n\n**Trace a dependency** in a specific configuration\n\n```gradle dependencyInsight --configuration [compileClasspath] --dependency [guava]```\n\n**Show only a single path** to each dependency (useful for large graphs)\n\n```gradle dependencyInsight --dependency [library-name] --singlepath```\n\n**Trace a dependency** in a specific subproject\n\n```gradle :[subproject]:dependencyInsight --dependency [library-name]```"],["SYNOPSIS","**gradle dependencyInsight** [_options_]"],["PARAMETERS","**--dependency** _NAME_\n> Required. The dependency to trace. Matches partially against group, name, or version.\n\n**--configuration** _NAME_\n> Configuration to inspect (e.g., compileClasspath, runtimeClasspath). Defaults to compileClasspath for Java projects.\n\n**--singlepath**\n> Show only one path to each dependency instead of all paths. Useful for large dependency graphs."],["DESCRIPTION","**gradle dependencyInsight** traces why a specific dependency appears in the build. It shows the path from direct dependencies to the transitive inclusion, revealing how version conflicts are resolved.\n\nThe **--dependency** value is matched partially against the group, name, or version of dependencies, so a query like `guava` will match `com.google.guava:guava:31.1-jre`.\n\nThe task is essential for debugging dependency issues and understanding why specific versions are selected during resolution."],["CAVEATS","The **--dependency** flag is required. Without **--configuration**, the task defaults to compileClasspath in Java projects. The dependency value uses partial matching, which may return unexpected results for common names."],["INSTALL","```pacman: sudo pacman -S gradle```\n\n```apk: sudo apk add gradle```\n\n```brew: brew install gradle```\n\n```nix: nix profile install nixpkgs#gradle```"],["SEE ALSO","[gradle](/man/gradle)(1), [gradle-dependencies](/man/gradle-dependencies)(1)"]],"e":["gradle","gradle-dependencies"],"x":"gradle dependencyInsight --dependency [library-name]"},"hapless":{"n":"hapless","t":"Run and manage background processes","s":[["TAGLINE","Run and manage background processes"],["TLDR","**Run a command in the background**\n\n```hap run [command]```\n\n**List all tracked processes**\n\n```hap```\n\n**Show status of a specific process**\n\n```hap status [id]```\n\n**Show logs of a process**\n\n```hap logs [id]```\n\n**Show live log output** of a process\n\n```hap logs -f [id]```\n\n**Pause a running process**\n\n```hap pause [id]```\n\n**Resume a paused process**\n\n```hap resume [id]```"],["SYNOPSIS","**hap** _command_ [_options_] [_arguments_]"],["DESCRIPTION","**hapless** is a lightweight command-line utility for running and managing background processes. It wraps commands to track their status during and after execution, without requiring a daemon process or configuration files. Simply prefix any command with **hap run** to launch it in the background with automatic logging and status tracking.\n\nThe tool provides a convenient overview of all tracked processes with their current status, exit codes, and runtime. Logs for both stdout and stderr are automatically captured and can be reviewed at any time."],["PARAMETERS","**run** _command_\n> Run a command in the background\n\n**status** _id_\n> Show status of a specific process\n\n**logs** _id_\n> Show logs of a process (-f for follow mode)\n\n**pause** _id_\n> Pause a running process\n\n**resume** _id_\n> Resume a paused process\n\n**kill** _id_\n> Terminate a process\n\n**clean**\n> Remove completed processes from the list"],["CAVEATS","Does not persist across system reboots. Process tracking relies on PID files stored locally. Interactive commands that require terminal input are not supported in the background."],["HISTORY","**hapless** was created by **Misha Behersky** (bmwant) and is written in **Python**. It was designed to be a simpler alternative to tools like **screen**, **tmux**, or **nohup** for basic background process management, with the goal of requiring zero configuration."],["INSTALL","```brew: brew install hapless```"],["SEE ALSO","[nohup](/man/nohup)(1), [screen](/man/screen)(1), [tmux](/man/tmux)(1), [bg](/man/bg)(1)"]],"e":["nohup","screen","tmux","bg"],"x":"hap run [command]"},"hn":{"n":"hn","t":"browse Hacker News from the terminal (haxor-news)","s":[["TAGLINE","browse Hacker News from the terminal (haxor-news)"],["TLDR","**Show top posts**\n\n```hn top```\n\n**Show newest posts**\n\n```hn new [20]```\n\n**Show best posts**\n\n```hn best```\n\n**Show Ask HN posts**\n\n```hn ask```\n\n**Show Show HN posts**\n\n```hn show```\n\n**View a post's linked content or comments, by its list number**\n\n```hn view [1]```\n\n**View all comments for a post, piped through a pager**\n\n```hn view [8] -c | less -r```\n\n**Filter the monthly \"who is hiring\" post by keyword**\n\n```hn hiring \"[(?i)(Node|JavaScript).*(remote)]\"```"],["SYNOPSIS","**hn** _command_ [_params_] [_options_]\n\n**haxor-news**"],["COMMANDS","**top**, **best**, **new**, **ask**, **show**, **jobs**, **onion** [_limit_]\n> List posts from that category (default limit: 10; onion default: all).\n\n**view** _#_ [_options_]\n> View a listed post's linked web content, or its comments with **-c**.\n\n**hiring** [_regex_] [_post_id_]\n> Filter the latest (or a given) monthly \"who is hiring\" post.\n\n**freelance** [_regex_] [_post_id_]\n> Filter the latest (or a given) monthly freelancer-availability post.\n\n**user** _user_id_\n> Show a user's profile info.\n\n**help**\n> Show usage help."],["PARAMETERS","**-c**, **--comments**\n> Show all comments for the viewed post.\n\n**-cu**, **--comments_unseen**\n> Show only comments not previously seen (marked with **[!]**).\n\n**-cr**, **--comments_recent**\n> Show only comments posted in the last 60 minutes.\n\n**-cq**, **--comments_regex_query** _regex_\n> Show only comments matching a regular expression.\n\n**-ch**, **--comments_hide**\n> Hide (instead of collapse) comments that don't match a filter.\n\n**-b**, **--browser**\n> Open the linked content or comments in the default web browser instead of the terminal."],["DESCRIPTION","**hn** is the command-line client provided by **haxor-news**, a Python tool that brings Hacker News to the terminal without leaving the command line. It can list and page through Top, Best, New, Ask, Show, Jobs, and April Fools' \"Onion\" posts, view a post's linked web content (rendered as readable text) or its full comment tree, and filter large comment threads down to unseen, recent, or regex-matching comments.\n\nOutput can be piped to pagers, redirected to files, or combined with other command-line tools. Running the bare **haxor-news** command (rather than **hn**) launches an optional auto-completing interactive prompt using the same **hn** _command_ syntax."],["CAVEATS","Requires an internet connection; data comes from the Hacker News Firebase/Algolia APIs via the **haxor** Python library. The project has seen little activity since the mid-2010s, though its read-only browsing commands still work."],["HISTORY","**haxor-news** was created by **Donne Martin** and first released in **2015**, built on Kenneth Reitz's **requests**, Aaron Swartz's **html2text**, and the **click** and **prompt_toolkit** libraries."],["SEE ALSO","[rtv](/man/rtv)(1), [tuir](/man/tuir)(1), [w3m](/man/w3m)(1)"],["RESOURCES","```[Source code](https://github.com/donnemartin/haxor-news)```"]],"e":["rtv","tuir","w3m"],"x":"hn top"},"ispell-wrapper":{"n":"ispell-wrapper","t":"provides a unified interface to various spell checkers","s":[["TAGLINE","provides a unified interface to various spell checkers"],["TLDR","**Check file with default checker**\n\n```ispell-wrapper [file.txt]```\n\n**Force specific checker**\n\n```ispell-wrapper --ispell=[aspell] [file.txt]```\n\n**List misspellings**\n\n```ispell-wrapper -l < [file.txt]```\n\n**Use specific dictionary**\n\n```ispell-wrapper -d [english] [file.txt]```"],["SYNOPSIS","**ispell-wrapper** [_options_] _file_..."],["PARAMETERS","**--ispell** _CHECKER_\n> Specify spell checker (aspell, hunspell, ispell).\n\n**-d** _DICT_\n> Dictionary to use.\n\n**-a**\n> Pipe mode.\n\n**-l**\n> List mode.\n\n**--help**\n> Display help information."],["DESCRIPTION","**ispell-wrapper** provides a unified interface to various spell checkers. It wraps aspell, hunspell, or ispell with consistent options.\n\nThe wrapper chooses the best available checker automatically. It allows scripts to work regardless of which checker is installed."],["CAVEATS","Debian/Ubuntu specific. Depends on installed checkers. Not all options pass through."],["HISTORY","ispell-wrapper was created for **Debian** to provide compatibility between different spell checking programs."],["INSTALL","```apt: sudo apt install ispell```\n\n```pacman: sudo pacman -S ispell```\n\n```apk: sudo apk add aspell-compat```\n\n```zypper: sudo zypper install ispell```\n\n```brew: brew install ispell```\n\n```nix: nix profile install nixpkgs#ispell```"],["SEE ALSO","[ispell](/man/ispell)(1), [aspell](/man/aspell)(1), [hunspell](/man/hunspell)(1)"]],"e":["ispell","aspell","hunspell"],"x":"ispell-wrapper [file.txt]"},"jjj":{"n":"jjj","t":"Minimal file lister with Vim key bindings","s":[["TAGLINE","Minimal file lister with Vim key bindings"],["TLDR","**Launch jjj** in the current directory\n\n```jjj```\n\n**Launch jjj** in a specific directory\n\n```jjj [path/to/directory]```\n\n**Navigate and select a file** to pipe to another command\n\n```jjj | xargs [command]```\n\n**Change to a selected directory**\n\n```cd $(jjj .)```"],["SYNOPSIS","**jjj** [_path_]"],["DESCRIPTION","**jjj** is a minimal and fast terminal-based file navigation tool that uses **Vim** key bindings for browsing and listing files. You can quickly navigate directories using **j/k** keys and pipe the selected file to other command-line utilities for further processing.\n\nThe tool is designed for fast filesystem navigation directly from the command line with minimal overhead."],["KEY BINDINGS","**j/k** or **Arrow keys** — Move down/up\n**h** — Go to parent directory\n**l** — Enter directory\n**Enter** or **Spacebar** — Exit and return selected path\n**r** — Refresh current folder view\n**q** or **Esc** — Quit"],["CAVEATS","Minimal feature set by design. Does not support file operations like copy, move, or delete."],["HISTORY","**jjj** was created by **Simone Poggiali** (gibbok) and is written in **C** using the ncurses library."],["INSTALL","```aur: yay -S jjj```"],["SEE ALSO","[ls](/man/ls)(1), [lf](/man/lf)(1), [ranger](/man/ranger)(1), [nnn](/man/nnn)(1), [vifm](/man/vifm)(1)"]],"e":["ls","lf","ranger","nnn","vifm"],"x":"jjj"},"killport":{"n":"killport","t":"Kill processes listening on specified TCP/UDP ports","s":[["TAGLINE","Kill processes listening on specified TCP/UDP ports"],["TLDR","**Kill** whatever is using a port\n\n```killport [8080]```\n\n**Kill** multiple ports\n\n```killport [3000] [8000] [5432]```\n\n**Dry-run** (show what would be killed)\n\n```killport -n [8080]```\n\n**Also signal Docker containers** bound to the port\n\n```killport -s [8080]```"],["SYNOPSIS","**killport** [*options*] *port*..."],["DESCRIPTION","**killport** finds processes (and optionally containers) bound to given port numbers and terminates them. It is a small convenience tool for the common “port already in use” failure during local development, written in Rust and available via **cargo**, Homebrew, and release binaries."],["PARAMETERS","*port*...\n\n> One or more port numbers to free.\n\n**-n**, **--dry-run**\n\n> Print matching processes without killing (flag name may be **--dry-run** depending on version; see **killport --help**).\n\n**-s**, **--signal** *signal* / container-related flags\n\n> Control signal used and whether containers are targeted—consult **killport --help** for the installed version.\n\n**-h**, **--help** / **-V**, **--version**\n\n> Help and version."],["CAVEATS","Requires permission to signal the target processes (often root for privileged ports or other users' PIDs). Killing the wrong process can disrupt services—prefer dry-run when unsure."],["INSTALL","```zypper: sudo zypper install killport```\n\n```brew: brew install killport```\n\n```nix: nix profile install nixpkgs#killport```"],["SEE ALSO","[lsof](/man/lsof)(1), [fuser](/man/fuser)(1), [ss](/man/ss)(8), [kill](/man/kill)(1)"],["RESOURCES","```[Source code](https://github.com/jkfran/killport)```"]],"e":["lsof","fuser","ss","kill"],"x":"killport [8080]"},"kubectl-explain":{"n":"kubectl-explain","t":"shows resource and field documentation","s":[["TAGLINE","shows resource and field documentation"],["TLDR","**Explain resource**\n\n```kubectl explain [pod]```\n\n**Explain field**\n\n```kubectl explain [pod.spec]```\n\n**Explain nested field**\n\n```kubectl explain [pod.spec.containers]```\n\n**Show recursive structure**\n\n```kubectl explain [pod] --recursive```\n\n**Explain specific API version**\n\n```kubectl explain [deployment] --api-version=[apps/v1]```"],["SYNOPSIS","**kubectl explain** [_options_] _resource_"],["PARAMETERS","_RESOURCE_\n> Resource type and optional field path.\n\n**--recursive**\n> Show all fields recursively.\n\n**--api-version** _VERSION_\n> Explain specific API version.\n\n**--help**\n> Display help information."],["DESCRIPTION","**kubectl explain** shows resource and field documentation. It displays the schema for Kubernetes objects.\n\nThe command helps write manifests correctly. It shows field types, defaults, and descriptions."],["CAVEATS","Subcommand of kubectl. Documentation from API server. Varies by Kubernetes version."],["HISTORY","kubectl explain provides built-in API documentation for **Kubernetes** resource authoring."],["INSTALL","```apt: sudo apt install kubectl```\n\n```pacman: sudo pacman -S kubectl```\n\n```apk: sudo apk add kubectl```\n\n```brew: brew install kubectl```\n\n```nix: nix profile install nixpkgs#kubectl```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [kubectl-api-resources](/man/kubectl-api-resources)(1)"]],"e":["kubectl","kubectl-api-resources"],"x":"kubectl explain [pod]"},"kubectl-patch":{"n":"kubectl-patch","t":"updates fields of a resource using strategic merge patch, JSON merge","s":[["TAGLINE","updates fields of a resource using strategic merge patch, JSON merge"],["TLDR","**Patch deployment with JSON**\n\n```kubectl patch deployment [name] -p '{\"spec\":{\"replicas\":3}}'```\n\n**Patch with strategic merge**\n\n```kubectl patch deployment [name] --type=merge -p '[patch]'```\n\n**Patch with JSON patch**\n\n```kubectl patch deployment [name] --type=json -p '[{\"op\":\"replace\",\"path\":\"/spec/replicas\",\"value\":3}]'```\n\n**Patch from file**\n\n```kubectl patch deployment [name] --patch-file=[patch.yaml]```"],["SYNOPSIS","**kubectl** **patch** _type_ _name_ [_options_]"],["PARAMETERS","**-p**, **--patch** _patch_\n> Patch content as JSON or YAML.\n\n**--patch-file** _file_\n> File containing patch.\n\n**--type** _type_\n> Patch type (strategic, merge, json).\n\n**-n**, **--namespace** _name_\n> Kubernetes namespace."],["DESCRIPTION","**kubectl patch** applies incremental updates to Kubernetes resource fields without requiring you to replace the entire resource specification. This makes it well suited for scripted and automated changes where only specific fields need modification, such as adjusting replica counts, updating image tags, or adding annotations.\n\nThe command supports three patch strategies: strategic merge patch (the default), which intelligently merges lists and maps according to Kubernetes schema knowledge; JSON merge patch, which performs a straightforward recursive merge; and JSON patch (RFC 6902), which uses explicit operations like add, remove, and replace on specific JSON paths. Patch content can be provided inline via the -p flag or read from a file with --patch-file."],["INSTALL","```apt: sudo apt install kubectl```\n\n```pacman: sudo pacman -S kubectl```\n\n```apk: sudo apk add kubectl```\n\n```brew: brew install kubectl```\n\n```nix: nix profile install nixpkgs#kubectl```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [kubectl-edit](/man/kubectl-edit)(1)"]],"e":["kubectl","kubectl-edit"],"x":"kubectl patch deployment [name] -p '{\"spec\":{\"replicas\":3}}'"},"littler":{"n":"littler","t":"provides a shebang-capable scripting interface for R","s":[["TAGLINE","provides a shebang-capable scripting interface for R"],["TLDR","**Run R expression**\n\n```r -e \"print('Hello')\"```\n\n**Run R script**\n\n```r [script.R]```\n\n**Install package**\n\n```r -e \"install.packages('[package]')\"```\n\n**Pipe data to R**\n\n```echo \"[1,2,3]\" | r -e \"sum(scan())\"```\n\n**Run with arguments**\n\n```r [script.R] [arg1] [arg2]```"],["SYNOPSIS","**r** [_options_] [_script_] [_args_...]"],["PARAMETERS","**-e** _expr_\n> Evaluate expression.\n\n**-p**\n> Print result.\n\n**-l** _package_\n> Load package.\n\n**-i**\n> Interactive mode.\n\n**-n**\n> No implicit printing.\n\n**-t**\n> Use temporary directory.\n\n**-v**\n> Verbose output."],["DESCRIPTION","**littler** (r) provides a shebang-capable scripting interface for R. It allows running R code from the command line and in scripts, making R more accessible for shell scripting and automation.\n\nlittler starts faster than R --vanilla by avoiding R's full initialization."],["SHEBANG USAGE","```r\n#!/usr/bin/env r\n\nargs <- commandArgs(TRUE)\nprint(paste(\"Hello\", args[1]))\n```"],["CAVEATS","Must be installed separately from R. The command 'r' may conflict with other tools. Package loading adds startup time."],["HISTORY","littler was created by **Dirk Eddelbuettel** and **Jeff Horner** in **2006** to provide a proper scripting interface for R."],["INSTALL","```aur: yay -S littler```"]],"e":[],"x":"r -e \"print('Hello')\""},"llama-cli":{"n":"llama-cli","t":"Run local LLM inference from the command line (llama.cpp)","s":[["TAGLINE","Run local LLM inference from the command line (llama.cpp)"],["TLDR","**Load a model and start an interactive chat**\n\n```llama-cli -m [path/to/model.gguf]```\n\n**Run a single prompt and print the completion**\n\n```llama-cli -m [path/to/model.gguf] -p \"[prompt]\" -no-cnv```\n\n**Set the prompt context size**\n\n```llama-cli -m [path/to/model.gguf] -c [4096]```\n\n**Offload model layers to the GPU**\n\n```llama-cli -m [path/to/model.gguf] -ngl [all]```\n\n**Download and run a model from Hugging Face**\n\n```llama-cli -hf [user/model:quant]```\n\n**Read the first prompt from a file**\n\n```llama-cli -m [path/to/model.gguf] -f [path/to/prompt.txt]```\n\n**Provide a system prompt**\n\n```llama-cli -m [path/to/model.gguf] -sys \"[You are a helpful assistant]\"```\n\n**Control randomness with temperature**\n\n```llama-cli -m [path/to/model.gguf] --temp [0.7]```"],["SYNOPSIS","**llama-cli** **-m** _model.gguf_ [_options_]\n\n**llama-cli** **-hf** _user/model[:quant]_ [_options_]"],["DESCRIPTION","**llama-cli** is the command-line inference tool of the **llama.cpp** project. It loads a model in the **GGUF** format and generates text either interactively, as a back-and-forth chat, or in a single non-interactive completion. It runs entirely on local hardware, using the CPU, a GPU (CUDA, Metal, Vulkan, ROCm), or a mix of both.\n\nBy default llama-cli enters conversation mode when the model ships with a chat template, applying that template automatically and exposing an interactive prompt. The **-no-cnv** flag disables this and treats the input strictly as a raw completion prompt. Models can be supplied as a local file with **-m**, downloaded on demand from Hugging Face with **-hf**, or pulled from a Docker repository with **-dr**.\n\nThe tool exposes the full sampler stack (temperature, top-k, top-p, min-p, mirostat, DRY, and more), context management options such as context size and context shifting, and hardware controls for thread count and GPU layer offloading. It shares its argument parser with the other llama.cpp tools like **llama-server**, so most flags behave identically across them."],["PARAMETERS","**-m**, **--model** _FNAME_\n\n> Path to the GGUF model file to load.\n\n**-hf**, **--hf-repo** _user/model[:quant]_\n\n> Download the model from a Hugging Face repository; quant defaults to Q4_K_M.\n\n**-p**, **--prompt** _PROMPT_\n\n> Prompt to start generation with. For a system message use **-sys**.\n\n**-f**, **--file** _FNAME_\n\n> Read the prompt from a file.\n\n**-sys**, **--system-prompt** _PROMPT_\n\n> System prompt to use with the model, when the chat template supports one.\n\n**-c**, **--ctx-size** _N_\n\n> Size of the prompt context (default: 0, loaded from the model).\n\n**-n**, **--predict** _N_\n\n> Number of tokens to predict (default: -1, -1 = infinity).\n\n**-ngl**, **--n-gpu-layers** _N_\n\n> Maximum number of layers to store in VRAM. Accepts an exact number, **auto**, or **all**.\n\n**-t**, **--threads** _N_\n\n> Number of CPU threads to use during generation.\n\n**-cnv**, **--conversation** / **-no-cnv**, **--no-conversation**\n\n> Force conversation mode on or off (auto-enabled when a chat template is present).\n\n**--temp** _N_\n\n> Sampling temperature (default: 0.80).\n\n**-co**, **--color** [on|off|auto]\n\n> Colorize output to distinguish prompt and input from generated text.\n\n**-i**, **--interactive**\n\n> Run in interactive mode, returning control on a reverse prompt.\n\n**-r**, **--reverse-prompt** _PROMPT_\n\n> Halt generation at PROMPT and return control in interactive mode."],["CAVEAT","llama-cli only reads **GGUF** files; older GGML model files must be converted or re-downloaded. Performance and the achievable context size depend heavily on quantization and available VRAM: setting **-ngl** higher than your GPU memory allows causes a load failure or fallback to CPU. The single binary was historically named **main**; older guides referring to **./main** describe the same program."],["HISTORY","**llama.cpp** was created by **Georgi Gerganov** in **March 2023** as a plain C/C++ port of Meta's LLaMA model, aimed at running inference efficiently on consumer hardware without a GPU. The example program that became llama-cli was originally called **main**; it was renamed to **llama-cli** in **2024** when the project standardized its tool names. Development continues under the ggml-org organization with frequent releases."],["INSTALL","```apk: sudo apk add llama.cpp```\n\n```brew: brew install llama.cpp```"],["SEE ALSO","[llama.cpp](/man/llama.cpp)(1), [llamafile](/man/llamafile)(1), [ollama](/man/ollama)(1), [llm](/man/llm)(1)"],["RESOURCES","```[Source code](https://github.com/ggml-org/llama.cpp)```\n\n```[Documentation](https://github.com/ggml-org/llama.cpp/tree/master/tools/cli)```"]],"e":["llama.cpp","llamafile","ollama","llm"],"x":"llama-cli -m [path/to/model.gguf]"},"lvchange":{"n":"lvchange","t":"changes attributes or the activation state of logical volumes in LVM","s":[["TAGLINE","changes attributes or the activation state of logical volumes in LVM"],["TLDR","**Activate** logical volume\n\n```sudo lvchange -a y /dev/vg_name/lv_name```\n\n**Deactivate** logical volume\n\n```sudo lvchange -a n /dev/vg_name/lv_name```\n\nEnable **autoactivation**\n\n```sudo lvchange -a ay /dev/vg_name/lv_name```\n\nSet **read-only** permission\n\n```sudo lvchange -p r /dev/vg_name/lv_name```\n\n**Skip activation**\n\n```sudo lvchange -k y /dev/vg_name/lv_name```\n\n**Refresh** logical volume\n\n```sudo lvchange --refresh /dev/vg_name/lv_name```"],["SYNOPSIS","**lvchange** [_options_] _vg_/_lv_"],["DESCRIPTION","**lvchange** changes attributes or the activation state of logical volumes in LVM. It can activate/deactivate volumes, change permissions, and modify various LV properties."],["PARAMETERS","**-a, --activate y|n|ay**\n> Activate (y), deactivate (n), or autoactivate (ay)\n\n**-p, --permission r|rw**\n> Set read-only (r) or read-write (rw)\n\n**-k, --setactivationskip y|n**\n> Set activation skip flag\n\n**--refresh**\n> Refresh volume using latest metadata\n\n**-Z, --zero y|n**\n> Zero first KB of new logical volume\n\n**--addtag TAG**\n> Add tag to logical volume"],["CAVEATS","Requires root privileges. Volume must be unmounted for some operations. Part of LVM2 tools."],["INSTALL","```apt: sudo apt install lvm2```\n\n```dnf: sudo dnf install lvm2```\n\n```pacman: sudo pacman -S lvm2```\n\n```apk: sudo apk add lvm2```\n\n```zypper: sudo zypper install lvm2```\n\n```nix: nix profile install nixpkgs#lvm2```"],["SEE ALSO","[lvcreate](/man/lvcreate)(8), [lvdisplay](/man/lvdisplay)(8), [lvs](/man/lvs)(8), [lvm](/man/lvm)(8)"]],"e":["lvcreate","lvdisplay","lvs","lvm"],"x":"sudo lvchange -a y /dev/vg_name/lv_name"},"mdcat":{"n":"mdcat","t":"Cat for Markdown with terminal rendering","s":[["TAGLINE","Cat for Markdown with terminal rendering"],["TLDR","**Render a Markdown file**\n\n```mdcat [path/to/file.md]```\n\n**Render Markdown from stdin**\n\n```echo \"# Hello\" | mdcat```\n\n**Paginate output**\n\n```mdcat -p [path/to/file.md]```"],["SYNOPSIS","**mdcat** [_options_] [_files_...]"],["PARAMETERS","**-p**, **--paginate**\n> Paginate output through a pager.\n\n**--no-colour**\n> Disable syntax highlighting and colours."],["DESCRIPTION","**mdcat** renders Markdown documents directly in the terminal with syntax highlighting for code blocks (via syntect), inline images in supported terminals, clickable links, and jump marks for headings in iTerm2.\n\nIt works best with iTerm2, WezTerm, and kitty, and supports most modern terminal emulators including Windows Terminal and anything based on VTE. If no files are specified, it reads from stdin."],["CAVEATS","Inline image support depends on the terminal emulator. Some terminals may only display links to images instead."],["HISTORY","**mdcat** was created by **swsnr** and is written in **Rust**."],["INSTALL","```brew: brew install mdcat```\n\n```nix: nix profile install nixpkgs#mdcat```"],["SEE ALSO","[glow](/man/glow)(1), [bat](/man/bat)(1), [mandown](/man/mandown)(1), [cat](/man/cat)(1)"]],"e":["glow","bat","mandown","cat"],"x":"mdcat [path/to/file.md]"},"mise":{"n":"mise","t":"manages development tool versions","s":[["TAGLINE","manages development tool versions"],["TLDR","**Install tool**\n\n```mise install [node@20]```\n\n**Use tool version**\n\n```mise use [node@20]```\n\n**List installed**\n\n```mise list```\n\n**Set global version**\n\n```mise use -g [python@3.11]```\n\n**Run with tool**\n\n```mise exec [node@18] -- node [script.js]```\n\n**Activate shell**\n\n```eval \"$(mise activate bash)\"```"],["SYNOPSIS","**mise** _command_ [_options_]"],["PARAMETERS","_COMMAND_\n> Mise operation.\n\n**install** _TOOL_\n> Install tool version.\n\n**use** _TOOL_\n> Set tool version.\n\n**list**\n> List installed versions.\n\n**exec**\n> Run with specified version.\n\n**activate**\n> Shell activation.\n\n**--help**\n> Display help information."],["DESCRIPTION","**mise** manages development tool versions. It's a polyglot runtime manager (successor to rtx).\n\nThe tool handles Node.js, Python, Ruby, and more. Compatible with asdf plugins."],["CAVEATS","Shell activation needed. Successor to rtx. asdf-compatible."],["HISTORY","mise (formerly rtx) was created as a fast, **Rust-based** alternative to asdf for managing tool versions."],["INSTALL","```pacman: sudo pacman -S mise```\n\n```apk: sudo apk add mise```\n\n```brew: brew install mise```\n\n```nix: nix profile install nixpkgs#mise```"],["SEE ALSO","[asdf](/man/asdf)(1), [nvm](/man/nvm)(1), [pyenv](/man/pyenv)(1)"]],"e":["asdf","nvm","pyenv"],"x":"mise install [node@20]"},"mount.smb3":{"n":"mount.smb3","t":"mount SMB3 network shares","s":[["TAGLINE","mount SMB3 network shares"],["TLDR","This command is an alias of **mount.cifs** limited to the SMB3 filesystem.\n\n**Mount SMB3 share** with username\n\n```sudo mount.smb3 -o username=[user] //[server]/[share] [/mnt/point]```\n\nMount as **guest user**\n\n```sudo mount.smb3 -o guest //[server]/[share] [/mnt/point]```\n\n**Mount with credentials file**\n\n```sudo mount.smb3 -o credentials=[/etc/samba/creds] //[server]/[share] [/mnt/point]```\n\n**Mount with specific permissions**\n\n```sudo mount.smb3 -o username=[user],uid=[1000],gid=[1000],file_mode=[0644],dir_mode=[0755] //[server]/[share] [/mnt/point]```\n\n**Mount with encryption**\n\n```sudo mount.smb3 -o username=[user],seal //[server]/[share] [/mnt/point]```"],["SYNOPSIS","**mount.smb3** _//server/share_ _mount-point_ [**-o** _options_]"],["PARAMETERS","**username=**_arg_\n> Username for authentication. Defaults to the USER environment variable.\n\n**password=**_arg_\n> Password for authentication. Using a credentials file is more secure.\n\n**credentials=**_filename_\n> File containing username, password, and optionally domain.\n\n**domain=**_arg_\n> Workgroup or domain for authentication.\n\n**guest**\n> Connect without a password.\n\n**sec=**_mode_\n> Security mode (krb5, krb5i, ntlmv2, ntlmv2i, ntlmssp, ntlmsspi, none).\n\n**uid=**_arg_\n> Owner UID for files on the mounted share.\n\n**gid=**_arg_\n> Owner GID for files on the mounted share.\n\n**file_mode=**_mode_\n> Default file permissions (octal).\n\n**dir_mode=**_mode_\n> Default directory permissions (octal).\n\n**seal**\n> Enable SMB3 encryption for all data.\n\n**vers=**_version_\n> SMB protocol version (3.0, 3.02, 3.1.1).\n\n**cache=**_mode_\n> Cache mode (none, strict, loose).\n\n**ro**\n> Mount read-only.\n\n**rw**\n> Mount read-write.\n\n**multiuser**\n> Allow different users to access the mount with individual credentials.\n\n**nounix**\n> Disable Unix Extensions."],["DESCRIPTION","**mount.smb3** mounts SMB3 network shares to local directories. It is a symbolic link to **mount.cifs** that is limited to the SMB3 filesystem. It can also be invoked via **mount -t smb3**.\n\nFor older SMB versions, use mount.cifs with the **vers=** option instead."],["CAVEATS","Requires the cifs-utils package. Passwords on the command line are visible in process listings; use a credentials file instead. Kerberos authentication requires a valid keytab or ticket."],["INSTALL","```apt: sudo apt install cifs-utils```\n\n```dnf: sudo dnf install cifs-utils```\n\n```pacman: sudo pacman -S cifs-utils```\n\n```apk: sudo apk add cifs-utils```\n\n```zypper: sudo zypper install cifs-utils```\n\n```nix: nix profile install nixpkgs#cifs-utils```"],["SEE ALSO","[mount.cifs](/man/mount.cifs)(8), [mount](/man/mount)(8), [umount](/man/umount)(8), [smbclient](/man/smbclient)(1), [fstab](/man/fstab)(5)"]],"e":["mount.cifs","mount","umount","smbclient","fstab"],"x":"sudo mount.smb3 -o username=[user] //[server]/[share] [/mnt/point]"},"nagios":{"n":"nagios","t":"infrastructure monitoring system that tracks hosts, services, and network","s":[["TAGLINE","infrastructure monitoring system that tracks hosts, services, and network"],["TLDR","**Verify configuration**\n\n```nagios -v [/etc/nagios/nagios.cfg]```\n\n**Start Nagios daemon**\n\n```nagios -d [/etc/nagios/nagios.cfg]```\n\n**Run in foreground**\n\n```nagios [/etc/nagios/nagios.cfg]```\n\n**Show version**\n\n```nagios --version```\n\n**Test scheduling**\n\n```nagios -s [/etc/nagios/nagios.cfg]```"],["SYNOPSIS","**nagios** [_-v_] [_-d_] [_-s_] [_config_file_]"],["PARAMETERS","**-v**\n> Verify configuration file.\n\n**-d**\n> Run as daemon.\n\n**-s**\n> Show scheduling information.\n\n**-p** _FILE_\n> Specify precached objects file.\n\n**-u** _FILE_\n> Specify precached macros file.\n\n**-x** _FILE_\n> Dump objects to file.\n\n**--help**\n> Display help.\n\n**--version**\n> Show version."],["DESCRIPTION","**nagios** is an infrastructure monitoring system that tracks hosts, services, and network devices. It alerts administrators when problems occur and when they're resolved.\n\nConfiguration defines hosts, services, and check commands. Plugins execute checks and return status codes: OK, WARNING, CRITICAL, or UNKNOWN. Notifications alert contacts through email, SMS, or custom methods.\n\nThe web interface displays current status, trends, and reports. It shows which services are up, which are down, and acknowledgements for known issues.\n\nActive checks run on schedule. Passive checks receive results from external sources. Both integrate into the same status view.\n\nEscalations route alerts to different people based on problem duration. Dependencies prevent notification storms when parent systems fail.\n\nPerformance data from checks can feed graphing tools like PNP4Nagios or Grafana for historical trending."],["CAVEATS","Configuration can be complex for large environments. Web interface requires separate web server. Many plugins available but quality varies. Consider successors like Icinga or Naemon."],["HISTORY","**Nagios** was created by **Ethan Galstad** in **1999** as NetSaint, renamed to Nagios in **2002**. It became the standard open-source monitoring system, spawning forks (Icinga, Naemon) and inspiring modern alternatives."],["INSTALL","```dnf: sudo dnf install nagios```\n\n```apk: sudo apk add nagios```\n\n```zypper: sudo zypper install nagios```\n\n```brew: brew install nagios```\n\n```nix: nix profile install nixpkgs#nagios```"],["SEE ALSO","[icinga2](/man/icinga2)(1), [naemon](/man/naemon)(8), [check_mk](/man/check_mk)(1), [prometheus](/man/prometheus)(1)"]],"e":["icinga2","naemon","check_mk","prometheus"],"x":"nagios -v [/etc/nagios/nagios.cfg]"},"neqn":{"n":"neqn","t":"formats mathematical equations for nroff","s":[["TAGLINE","formats mathematical equations for nroff"],["TLDR","**Format equations for nroff**\n\n```neqn [input.eqn] | nroff```\n\n**Process with groff**\n\n```neqn [file] | groff -Tascii```\n\n**Check syntax only**\n\n```neqn -C [file]```"],["SYNOPSIS","**neqn** [_options_] [_files_]"],["PARAMETERS","_FILES_\n> Input files with equations.\n\n**-C**\n> Enable compatibility mode.\n\n**-N**\n> Don't allow newlines in delimiters.\n\n**--help**\n> Display help information."],["DESCRIPTION","**neqn** formats mathematical equations for nroff. It's the ASCII version of eqn.\n\nThe tool processes equation markup. Outputs text-based math formatting."],["CAVEATS","Part of groff. ASCII output only. Use eqn for typeset output."],["HISTORY","neqn is part of **troff/nroff** system, derived from the original Unix equation formatter."],["INSTALL","```apt: sudo apt install groff-base```\n\n```dnf: sudo dnf install groff-base```\n\n```pacman: sudo pacman -S groff```\n\n```apk: sudo apk add groff```\n\n```zypper: sudo zypper install groff```\n\n```brew: brew install groff```\n\n```nix: nix profile install nixpkgs#groff```"],["SEE ALSO","[eqn](/man/eqn)(1), [nroff](/man/nroff)(1), [groff](/man/groff)(1)"]],"e":["eqn","nroff","groff"],"x":"neqn [input.eqn] | nroff"},"nibble":{"n":"nibble","t":"Fast TUI scanner for hosts, vendors, and services on the local network","s":[["TAGLINE","Fast TUI scanner for hosts, vendors, and services on the local network"],["TLDR","**Launch the interactive TUI** and pick an interface\n\n```nibble```\n\nScan a **specific subnet** in headless mode\n\n```nibble -i [192.168.1.0/24]```\n\nScan **multiple targets** read from a file\n\n```nibble -i [targets.txt]```\n\nLimit the scan to a **custom port range**\n\n```nibble -i [192.168.1.0/24] -p [22,80,443,8000-8100]```\n\nScan **all 65535 ports** on a single host\n\n```nibble -i [192.168.1.10/32] -p -```\n\nWrite results to a **JSON file** instead of stdout\n\n```nibble -i [10.0.0.0/24] -o [results.json]```"],["SYNOPSIS","**nibble** [**-i** _targets_] [**-p** _ports_] [**-o** _file_]"],["DESCRIPTION","**nibble** is a single-binary local-network scanner with a clickable terminal user interface. Run without arguments it asks you to choose a network interface, sweeps the attached subnet for live hosts, maps each MAC address to a likely vendor from a built-in 40,000-entry database, probes well-known TCP ports, and reads service banners to identify the software behind each open port. Results appear in a mouse-friendly TUI that supports history lookup of previous scans.\n\nIn headless mode (any of **-i**, **-p**, or **-o**) **nibble** runs non-interactively and writes JSON to stdout or to **-o**, making it scriptable. It works on Linux, macOS, and Windows, including inside Docker and WSL, and does not require root or administrator privileges."],["PARAMETERS","**-i** _targets_\n> Comma-separated IPs or CIDR ranges to scan, or a path to a file containing one target per line.\n\n**-p** _ports_\n> Custom port specification. Accepts comma-separated lists and ranges (e.g. **22,80,8000-8100**) or **-** for all ports.\n\n**-o** _file_\n> Write scan results to _file_ as JSON instead of printing them."],["CAVEATS","Probing is **TCP-only**; UDP services such as DNS or SNMP are not detected. Banner grabbing may trigger intrusion-detection systems on monitored networks — only scan networks you are authorized to assess. Vendor lookup depends on the bundled OUI list and may misidentify devices that spoof their MAC address."],["HISTORY","**nibble** was released by the **backendsystems** team on **GitHub in 2025** as a small, fast alternative to **nmap** for everyday LAN reconnaissance. It is distributed through **apt**, **dnf**, **brew**, **winget**, **npm**, **pip**, and **go install**, plus pre-built binaries for the three major platforms."],["SEE ALSO","[nmap](/man/nmap)(1), [arp-scan](/man/arp-scan)(1), [masscan](/man/masscan)(1), [netdiscover](/man/netdiscover)(8)"]],"e":["nmap","arp-scan","masscan","netdiscover"],"x":"nibble"},"nova":{"n":"nova","t":"command-line client for OpenStack Compute, which manages virtual machine","s":[["TAGLINE","command-line client for OpenStack Compute, which manages virtual machine"],["TLDR","List servers in the **current project/tenant**\n\n```nova list```\n\nList servers across **all tenants** (admin)\n\n```nova list --all-tenants```\n\n**Boot** an instance with network, image, and flavor\n\n```nova boot --nic net-id=[net_id] --image [image_id] --flavor [flavor] --availability-zone nova:[host_name] [vm_name]```\n\n**Power on** a server\n\n```nova start [server]```\n\n**Power off** a server\n\n```nova stop [server]```\n\n**Attach** a network interface to a server\n\n```nova interface-attach --net-id [net_id] [server]```"],["SYNOPSIS","**nova** _command_ [_options_] [_arguments_]"],["PARAMETERS","**list**\n> List virtual machines\n\n**boot**\n> Create and boot a new VM\n\n**start**\n> Start a stopped VM\n\n**stop**\n> Stop a running VM\n\n**delete**\n> Delete a VM\n\n**show**\n> Show details of a VM\n\n**interface-attach**\n> Attach a network interface to a VM\n\n**--nic net-id=_id_**\n> Network to attach\n\n**--image _id_**\n> Image ID to boot from\n\n**--flavor _name_**\n> Instance type/size\n\n**--availability-zone _zone:host_**\n> Placement specification\n\n**--all-tenants**\n> Show VMs from all tenants (admin)"],["DESCRIPTION","**nova** is the command-line client for OpenStack Compute (Nova), which manages virtual machine instances in OpenStack clouds. It handles VM lifecycle operations: creating, starting, stopping, and deleting instances.\n\nNova integrates with other OpenStack services: Glance for images, Neutron for networking, Cinder for block storage, and Keystone for authentication. Authentication credentials are typically provided through environment variables (OS_USERNAME, OS_PASSWORD, etc.).\n\nThe client communicates with the Nova API to provision compute resources according to specified configurations (flavor, image, network)."],["CAVEATS","Requires OpenStack credentials configured (typically via sourcing an openrc file). The legacy nova client is being replaced by **openstack** unified client. Network and storage configurations must exist before VM creation. Admin privileges needed for cross-tenant operations."],["INSTALL","```zypper: sudo zypper install nova```\n\n```brew: brew install nova```\n\n```nix: nix profile install nixpkgs#nova```"],["SEE ALSO","[openstack](/man/openstack)(1), [neutron](/man/neutron)(1), [glance](/man/glance)(1)"]],"e":["openstack","neutron","glance"],"x":"nova list"},"odpscmd-func":{"n":"odpscmd-func","t":"manages user-defined functions","s":[["TAGLINE","manages user-defined functions"],["TLDR","**List functions**\n\n```odpscmd func list```\n\n**Create function**\n\n```odpscmd func create [name] --class [classname] --resource [res]```\n\n**Drop function**\n\n```odpscmd func drop [name]```\n\n**Describe function**\n\n```odpscmd func desc [name]```"],["SYNOPSIS","**odpscmd** **func** [_command_] [_options_]"],["PARAMETERS","**list**\n> List all functions.\n\n**create** _NAME_\n> Create function.\n\n**drop** _NAME_\n> Delete function.\n\n**desc** _NAME_\n> Show function details.\n\n**--class** _NAME_\n> Java class name.\n\n**--resource** _RES_\n> Resource dependency.\n\n**--help**\n> Display help information."],["DESCRIPTION","**odpscmd func** manages user-defined functions. Creates and manages UDFs.\n\nThe tool handles MaxCompute functions. Part of odpscmd suite."],["CAVEATS","Legacy tool. Requires appropriate permissions."],["HISTORY","odpscmd func provides **UDF management** for MaxCompute Console."],["SEE ALSO","[odpscmd](/man/odpscmd)(1), [odpscmd-resource](/man/odpscmd-resource)(1)"]],"e":["odpscmd","odpscmd-resource"],"x":"odpscmd func list"},"oproxy":{"n":"oproxy","t":"Local proxy for inspecting, replaying, and modifying traffic","s":[["TAGLINE","Local proxy for inspecting, replaying, and modifying traffic"],["TLDR","**Run oproxy** as a local HTTP, HTTPS, and SOCKS5 proxy\n\n```oproxy```\n\n**Route a request** through the proxy\n\n```curl -x http://127.0.0.1:8080 [http://example.com]```\n\n**Run oproxy in Docker**, exposing the proxy and UI ports\n\n```docker run --rm -p 127.0.0.1:8080:8080 -p 127.0.0.1:1080:1080 ghcr.io/sauravrao637/oproxy:latest```"],["SYNOPSIS","**oproxy** [_options_]"],["DESCRIPTION","**oproxy** is a local HTTP, HTTPS, and SOCKS5 proxy for inspecting, replaying, and modifying network traffic from browsers, command-line tools, and applications. After installing its local certificate authority it can perform man-in-the-middle interception of HTTPS, decoding requests and responses including headers, bodies, status, and timing, with awareness of JWT, GraphQL, gRPC, and WebSocket payloads.\n\nBeyond observation, oproxy can rewrite traffic on the fly: rule sets support map-remote and map-local redirection, throttling, breakpoints, mock responses, and DNS overrides, which makes it useful for debugging, testing error handling, and developing against APIs that are not yet available. A web UI, served locally, presents the captured traffic and rule configuration."],["CAVEATS","Intercepting HTTPS requires trusting oproxy's locally generated certificate authority; install it only on machines you control and remove it when finished. The proxy is meant for local development and testing, not as a production gateway."],["SEE ALSO","[mitmproxy](/man/mitmproxy)(1), [curl](/man/curl)(1)"],["RESOURCES","```[Source code](https://github.com/sauravrao637/oproxy)```"]],"e":["mitmproxy","curl"],"x":"oproxy"},"paci":{"n":"paci","t":"wrapper around pacman, the Arch Linux package manager","s":[["TAGLINE","wrapper around pacman, the Arch Linux package manager"],["TLDR","**Search for package**\n\n```paci [package_name]```\n\n**Install package**\n\n```paci -S [package]```\n\n**Remove package**\n\n```paci -R [package]```\n\n**Update all packages**\n\n```paci -Syu```\n\n**Show package info**\n\n```paci -Qi [package]```\n\n**List installed packages**\n\n```paci -Q```\n\n**Clean package cache**\n\n```paci -Sc```\n\n**Search installed packages**\n\n```paci -Qs [query]```"],["SYNOPSIS","**paci** [_-S_] [_-R_] [_-Q_] [_-Syu_] [_options_] [_package_]"],["PARAMETERS","**-S** _PACKAGE_\n> Install package.\n\n**-R** _PACKAGE_\n> Remove package.\n\n**-Q**\n> Query installed packages.\n\n**-Qi** _PACKAGE_\n> Show package information.\n\n**-Qs** _QUERY_\n> Search installed packages.\n\n**-Syu**\n> System upgrade.\n\n**-Sc**\n> Clean cache.\n\n**-Ss** _QUERY_\n> Search repositories."],["DESCRIPTION","**paci** is a wrapper around pacman, the Arch Linux package manager. It provides a simplified interface while maintaining pacman's power.\n\nThe tool uses a familiar syntax for common operations: install, remove, update, and query. Complex pacman flags are abstracted into simpler commands.\n\nSearch functionality finds packages in repositories without requiring explicit flags. Interactive selection may be available depending on configuration.\n\nIntegration with AUR helpers may be included, enabling installation of community packages alongside official repositories.\n\nThe wrapper aims to reduce the learning curve for pacman while remaining compatible with its behavior."],["CAVEATS","Arch Linux specific. Wraps pacman, so pacman must be available. May not support all pacman features. Behavior may differ between implementations."],["HISTORY","Various **paci** implementations exist as pacman wrappers. They emerged from the Arch Linux community's desire for simpler package management interfaces while maintaining access to pacman's functionality."],["SEE ALSO","[pacman](/man/pacman)(8), [yay](/man/yay)(1), [paru](/man/paru)(1), [trizen](/man/trizen)(1)"]],"e":["pacman","yay","paru","trizen"],"x":"paci [package_name]"},"pamexec":{"n":"pamexec","t":"runs a specified shell command on each image in a multi-image PAM stream","s":[["TAGLINE","runs a specified shell command on each image in a multi-image PAM stream"],["TLDR","**Apply command to each image in stream**\n\n```pamexec \"[pnmflip -lr]\" [input.pam] > [output.pam]```"],["SYNOPSIS","**pamexec** _command_ [_file_]"],["PARAMETERS","_command_\n> Shell command to execute on each image."],["DESCRIPTION","**pamexec** runs a specified shell command on each image in a multi-image PAM stream. The command receives each image on stdin and outputs to stdout. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pamsplit](/man/pamsplit)(1)"]],"e":["pamsplit"],"x":"pamexec \"[pnmflip -lr]\" [input.pam] > [output.pam]"},"pgbench":{"n":"pgbench","t":"runs TPC-B-like benchmarks on PostgreSQL databases","s":[["TAGLINE","runs TPC-B-like benchmarks on PostgreSQL databases"],["TLDR","**Initialize benchmark database**\n\n```pgbench -i [database]```\n\n**Run benchmark**\n\n```pgbench -c [10] -t [100] [database]```\n\n**Run benchmark for duration**\n\n```pgbench -c [10] -T [60] [database]```\n\n**Run with custom script**\n\n```pgbench -f [script.sql] [database]```\n\n**Run read-only benchmark**\n\n```pgbench -S -c [10] -T [30] [database]```\n\n**Run with multiple threads**\n\n```pgbench -c [20] -j [4] -T [60] [database]```\n\n**Initialize with scale factor**\n\n```pgbench -i -s [10] [database]```"],["SYNOPSIS","**pgbench** [_-i_] [_-c clients_] [_-t transactions_] [_-T seconds_] [_-f script_] [_options_] _database_"],["PARAMETERS","**-i**\n> Initialize benchmark tables.\n\n**-s** _SCALE_\n> Scale factor for initialization.\n\n**-c** _CLIENTS_\n> Number of concurrent clients.\n\n**-t** _TRANSACTIONS_\n> Transactions per client (default: 10).\n\n**-T** _SECONDS_\n> Duration in seconds.\n\n**-j** _THREADS_\n> Number of threads.\n\n**-f** _FILE_\n> Custom SQL script.\n\n**-S**\n> Select-only (read-only). Shorthand for -b select-only.\n\n**-N**\n> Skip updates to pgbench_tellers and branches. Shorthand for -b simple-update.\n\n**-b** _scriptname[@weight]_\n> Built-in script: tpcb-like, simple-update, select-only. Optional weight for mixing.\n\n**-C**\n> Establish a new connection for each transaction (measures connection overhead).\n\n**-M** _protocol_\n> Query protocol: simple, extended, or prepared (default: simple).\n\n**-R** _rate_\n> Target transaction rate in TPS. Adds sleep to maintain rate.\n\n**-L** _limit_\n> Report transactions exceeding _limit_ milliseconds as late.\n\n**-r**\n> Report latency per statement.\n\n**-P** _SECONDS_\n> Show progress every N seconds.\n\n**-l**\n> Log transactions to file.\n\n**-n**\n> Skip vacuuming before running the test.\n\n**-I** _steps_\n> Initialization steps to perform (default: dtgvp). Characters: d=drop, t=tables, g=generate, v=vacuum, p=primary keys, f=foreign keys.\n\n**-h** _HOST_\n> Database host.\n\n**-p** _PORT_\n> Database port.\n\n**-U** _USER_\n> Database user."],["DESCRIPTION","**pgbench** runs TPC-B-like benchmarks on PostgreSQL databases. It measures transaction throughput under concurrent load.\n\nInitialization (-i) creates benchmark tables. Scale factor multiplies data size: scale 10 creates 1 million rows. Larger scales test different workload characteristics.\n\nThe benchmark simulates banking transactions: updates to accounts, tellers, and branches. It measures transactions per second (TPS) under concurrent client load.\n\nClient count affects concurrency. More clients test parallel execution and locking. Thread count should match available CPU cores.\n\nCustom scripts test specific workloads. They can use variables, conditionals, and multiple statements to simulate application behavior.\n\nProgress output shows real-time TPS and latency. Final report includes average TPS, latency distribution, and any errors."],["CAVEATS","Not a full TPC-B implementation. Results depend on hardware, configuration, and workload. Initialize with appropriate scale for meaningful tests."],["HISTORY","**pgbench** has been part of **PostgreSQL** since version 7.0, originally written by **Tatsuo Ishii**. It provides a standard way to test PostgreSQL performance and compare configurations."],["INSTALL","```apk: sudo apk add postgresql17-client```"],["SEE ALSO","[psql](/man/psql)(1), [pg_dump](/man/pg_dump)(1), [postgres](/man/postgres)(1)"]],"e":["psql","pg_dump","postgres"],"x":"pgbench -i [database]"},"pgmtoppm":{"n":"pgmtoppm","t":"converts a PGM grayscale image to a PPM color image","s":[["TAGLINE","converts a PGM grayscale image to a PPM color image"],["TLDR","**Convert grayscale to color**\n\n```pgmtoppm [color] [input.pgm] > [output.ppm]```\n\n**Colorize with specific color**\n\n```pgmtoppm [red] [input.pgm] > [output.ppm]```\n\n**Map black-white to color range**\n\n```pgmtoppm [black]-[blue] [input.pgm] > [output.ppm]```\n\n**Use color map file**\n\n```pgmtoppm -map [colormap.ppm] [input.pgm] > [output.ppm]```"],["SYNOPSIS","**pgmtoppm** _colorspec_ [_pgmfile_]\n\n**pgmtoppm** **-map** _mapfile_ [_pgmfile_]"],["PARAMETERS","**colorspec**\n> Color name or black-white range (e.g., \"red\", \"black-blue\").\n\n**-map** _file_\n> Color map file."],["DESCRIPTION","**pgmtoppm** converts a PGM grayscale image to a PPM color image. It maps gray values to colors, either a single color, a color range, or via a colormap."],["Grayscale to red tint","pgmtoppm red image.pgm > red.ppm"],["Map to blue gradient","pgmtoppm black-blue image.pgm > blue.ppm"],["Heat map style","pgmtoppm blue-red image.pgm > heat.ppm"],["Using hex colors","pgmtoppm \"#000000-#ff5500\" image.pgm > orange.ppm"],["From color map","pgmtoppm -map palette.ppm image.pgm > colored.ppm\n```"],["COLOR FORMATS","```\nColor names: red, blue, green, yellow...\nHex: #rrggbb\nRGB: rgb:rr/gg/bb\nRange: color1-color2\n```"],["CAVEATS","Input must be PGM. For the reverse, use ppmtopgm. Part of Netpbm toolkit."],["HISTORY","pgmtoppm is part of **Netpbm** by **Jef Poskanzer**, providing grayscale to color conversion."],["SEE ALSO","[ppmtopgm](/man/ppmtopgm)(1), [pgmramp](/man/pgmramp)(1), [ppmchange](/man/ppmchange)(1), [netpbm](/man/netpbm)(1)"],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"]],"e":["ppmtopgm","pgmramp","ppmchange","netpbm"],"x":"pgmtoppm [color] [input.pgm] > [output.ppm]"},"phpspec":{"n":"phpspec","t":"SpecBDD framework for designing and testing PHP code","s":[["TAGLINE","SpecBDD framework for designing and testing PHP code"],["TLDR","**Run all specifications** in the project\n\n```phpspec run```\n\n**Generate a new specification** for a class\n\n```phpspec describe \"[App\\\\Calculator]\"```\n\n**Run a single spec file**\n\n```phpspec run [spec/App/CalculatorSpec.php]```\n\n**Use a specific output formatter** (pretty, progress, junit, dot, tap, html)\n\n```phpspec run --format [pretty]```\n\n**Run without prompting** to auto-generate missing classes/methods\n\n```phpspec run --no-interaction```\n\n**Stop on the first failure**\n\n```phpspec run --stop-on-failure```\n\n**Show full backtraces** for verbose debugging\n\n```phpspec run -vvv```"],["SYNOPSIS","**phpspec** _command_ [_arguments_] [_options_]"],["COMMANDS","**run** [_spec_]\n> Run all specs (or a single spec file/class) and offer to generate missing code interactively.\n\n**describe** _CLASS_\n> Create a new spec file for the given fully qualified class name.\n\n**help** [_command_]\n> Show help for a specific command.\n\n**list**\n> List all available commands."],["PARAMETERS","**-c**, **--config** _FILE_\n> Use a specific configuration file (default: phpspec.yml or phpspec.yml.dist).\n\n**-f**, **--format** _NAME_\n> Output formatter: progress (default), pretty, junit, dot, tap, html.\n\n**--stop-on-failure**\n> Halt the run as soon as a spec fails.\n\n**--fake**\n> Make generated dummy methods return realistic fake data.\n\n**-n**, **--no-interaction**\n> Do not ask any interactive questions; assume defaults.\n\n**-v** / **-vv** / **-vvv**\n> Increase verbosity. **-vvv** prints exception traces.\n\n**--ansi** / **--no-ansi**\n> Force or disable ANSI color output.\n\n**-q**, **--quiet**\n> Suppress all output.\n\n**-V**, **--version**\n> Display the phpspec version."],["DESCRIPTION","**phpspec** is a SpecBDD (Specification Behaviour-Driven Development) tool for PHP. Rather than verifying existing implementations like a traditional unit test runner, it drives the design of objects: you write a spec describing how a class should behave, run phpspec, and it offers to scaffold the missing class, methods, and return statements for you.\n\nSpecs are plain PHP classes that extend **PhpSpec\\\\ObjectBehavior**. Each `it_*` / `its_*` method is a single example. Collaborators are described as type-hinted parameters and phpspec automatically supplies prophecy-based test doubles for them.\n\nConfiguration lives in `phpspec.yml` (or `phpspec.yml.dist`) at the project root and controls suites, bootstrap files, formatters, and matchers/extensions. PhpSpec is intentionally narrower than PHPUnit — it focuses on isolated, design-first object specs and pairs naturally with a higher-level acceptance tool such as Behat."],["CONFIGURATION","**phpspec.yml**\n> Project configuration: declares suites, namespaces, source directories, formatter, and extensions."],["CAVEATS","PhpSpec is not a drop-in replacement for PHPUnit — it cannot test static methods, final classes (without extensions), or exercise framework-level integration. Generated code is intentionally minimal; you are expected to fill in the real implementation after scaffolding."],["HISTORY","PhpSpec was started in 2007 by **Marcello Duarte** and rewritten as PhpSpec 2 in 2014 with help from **Konstantin Kudryashov** and the Behat community. It is maintained on GitHub under the `phpspec` organisation and is installable via Composer."],["SEE ALSO","[phpunit](/man/phpunit)(1), [behat](/man/behat)(1), [composer](/man/composer)(1)"]],"e":["phpunit","behat","composer"],"x":"phpspec run"},"pnmfile":{"n":"pnmfile","t":"Display information about PNM image files","s":[["TAGLINE","Display information about PNM image files"],["TLDR","**Show image information**\n\n```pnmfile [image.pnm]```\n\n**Show info for multiple files**\n\n```pnmfile [file1.pnm] [file2.pnm]```\n\n**Machine-readable output**\n\n```pnmfile -machine [image.pnm]```"],["SYNOPSIS","**pnmfile** [_options_] [_files_]"],["PARAMETERS","_FILES_\n> Input PNM files.\n\n**-machine**\n> Machine-readable output format, printing type, width, height, depth, and maxval as space-separated values."],["DESCRIPTION","**pnmfile** displays information about PNM image files including the format type (PBM, PGM, or PPM), dimensions (width and height), maximum sample value, and whether the file uses ASCII (plain) or binary (raw) encoding.\n\nThe **-machine** flag outputs in a parseable format suitable for scripting. Part of the Netpbm toolkit; superseded by **pamfile**, which also handles PAM format files."],["CAVEATS","Only reads PNM format files (PBM, PGM, PPM). For PAM files, use **pamfile** instead."],["HISTORY","**pnmfile** was created as part of the **Netpbm** toolkit for image inspection. It has been largely superseded by **pamfile**."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pamfile](/man/pamfile)(1), [file](/man/file)(1), [identify](/man/identify)(1), [pnmtopng](/man/pnmtopng)(1), [pnmtojpeg](/man/pnmtojpeg)(1)"]],"e":["pamfile","file","identify","pnmtopng","pnmtojpeg"],"x":"pnmfile [image.pnm]"},"pnmquant":{"n":"pnmquant","t":"Reduce color count in PNM images","s":[["TAGLINE","Reduce color count in PNM images"],["TLDR","**Reduce colors**\n\n```pnmquant [256] [input.pnm] > [output.pnm]```\n\n**Use center-of-box color selection**\n\n```pnmquant -center [16] [input.pnm] > [output.pnm]```\n\n**Apply Floyd-Steinberg dithering**\n\n```pnmquant -floyd [256] [input.pnm] > [output.pnm]```\n\n**Quantize to a predefined colormap**\n\n```pnmquant -mapfile [mapfile.pnm] [256] [input.pnm] > [output.pnm]```"],["SYNOPSIS","**pnmquant** [_options_] _ncolors_ [_file_]"],["PARAMETERS","_ncolors_\n> Target number of colors.\n\n**-floyd**, **-fs**\n> Enable Floyd-Steinberg dithering to improve visual quality.\n\n**-nofloyd**, **-nofs**\n> Disable dithering (default).\n\n**-center**\n> Choose the center of each color box as the representative color.\n\n**-meancolor**\n> Choose the mean of all colors in each box (default).\n\n**-meanpixel**\n> Choose the mean weighted by pixel count in each box.\n\n**-spreadbrightness**\n> Spread boxes based on brightness range rather than color population.\n\n**-spreadluminosity**\n> Spread boxes based on luminosity range.\n\n**-mapfile** _file_\n> Quantize to a predefined colormap from file."],["DESCRIPTION","**pnmquant** reduces the number of colors in a PNM image to a specified count using the median-cut color quantization algorithm. This is useful for preparing images for formats with limited color palettes or for reducing file size.\n\nThe **-floyd** option enables Floyd-Steinberg dithering to simulate more colors by distributing quantization error to neighboring pixels. The **-mapfile** option allows quantizing to a pre-defined color palette from another image. Part of the Netpbm toolkit; this is a wrapper script that calls **pnmcolormap** and **pnmremap** internally."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm-extras```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[ppmquant](/man/ppmquant)(1), [pnmcolormap](/man/pnmcolormap)(1), [pnmremap](/man/pnmremap)(1), [pnmdepth](/man/pnmdepth)(1)"]],"e":["ppmquant","pnmcolormap","pnmremap","pnmdepth"],"x":"pnmquant [256] [input.pnm] > [output.pnm]"},"poetry-cache":{"n":"poetry-cache","t":"Manage Poetry's package download cache","s":[["TAGLINE","Manage Poetry's package download cache"],["TLDR","**Show cache directory path**\n\n```poetry cache list```\n\n**Clear cache for specific package**\n\n```poetry cache clear [cache_name] --all```\n\n**Clear PyPI cache**\n\n```poetry cache clear pypi --all```"],["SYNOPSIS","**poetry** **cache** _command_ [_options_]"],["PARAMETERS","**list**\n> Show cached packages per repository.\n\n**clear** _cache_\n> Clear a specific cache.\n\n**--all**\n> Clear all entries in cache."],["DESCRIPTION","**poetry cache** manages Poetry's package cache. Poetry caches downloaded packages to speed up subsequent installations. Use this to view cache contents or clear space."],["INSTALL","```dnf: sudo dnf install poetry```\n\n```apk: sudo apk add poetry```\n\n```brew: brew install poetry```\n\n```nix: nix profile install nixpkgs#poetry```"],["SEE ALSO","[poetry](/man/poetry)(1), [poetry-install](/man/poetry-install)(1)"]],"e":["poetry","poetry-install"],"x":"poetry cache list"},"ppdi":{"n":"ppdi","t":"Import PPD files into CUPS driver format","s":[["TAGLINE","Import PPD files into CUPS driver format"],["TLDR","**Import PPD files**\n\n```ppdi [file.ppd]```\n\n**Import to specific directory**\n\n```ppdi -d [output_dir] [file.ppd]```\n\n**Import multiple files**\n\n```ppdi [file1.ppd] [file2.ppd]```"],["SYNOPSIS","**ppdi** [_options_] _files_"],["PARAMETERS","_FILES_\n> Input PPD files.\n\n**-d** _DIR_\n> Output directory.\n\n**-o** _FILE_\n> Output file name."],["DESCRIPTION","**ppdi** imports existing PPD (PostScript Printer Description) files into the CUPS driver source format (.drv). This is the reverse operation of **ppdc**, allowing administrators to convert standalone PPD files into the compact driver source format for easier maintenance and distribution.\n\nThe imported driver source files can then be edited to add new printer models or modify options, and recompiled with ppdc. This workflow simplifies managing large collections of printer definitions by consolidating them into shared driver source files."],["CAVEATS","CUPS specific. Creates driver source files."],["HISTORY","ppdi is part of **CUPS** for PPD file importing."],["INSTALL","```apt: sudo apt install cups-ppdc```\n\n```dnf: sudo dnf install cups-client```\n\n```apk: sudo apk add cups-client```\n\n```zypper: sudo zypper install cups-client```"],["SEE ALSO","[ppdc](/man/ppdc)(1), [ppdhtml](/man/ppdhtml)(1), [cups](/man/cups)(1)"]],"e":["ppdc","ppdhtml","cups"],"x":"ppdi [file.ppd]"},"prowler-gcp":{"n":"prowler-gcp","t":"GCP cloud security assessment","s":[["TAGLINE","GCP cloud security assessment"],["TLDR","**Run GCP security assessment** against all accessible projects\n\n```prowler gcp```\n\n**Scan specific project(s)**\n\n```prowler gcp --project-ids [project-id-1] [project-id-2]```\n\n**Run specific checks**\n\n```prowler gcp --checks [cloudstorage_bucket_public_access]```\n\n**Run specific services** only\n\n```prowler gcp --services [iam] [compute]```\n\n**Run with a compliance framework**\n\n```prowler gcp --compliance [cis_2.0_gcp]```\n\n**Impersonate a service account**\n\n```prowler gcp --impersonate-service-account [name@project.iam.gserviceaccount.com]```\n\n**List accessible GCP projects**\n\n```prowler gcp --list-project-ids```\n\n**Skip API activation check**\n\n```prowler gcp --skip-api-check```"],["SYNOPSIS","**prowler** **gcp** [_options_]"],["PARAMETERS","**--project-ids** _ID_...\n> Scan only the given GCP project IDs (default: all accessible projects).\n\n**--excluded-project-ids** _ID_...\n> Exclude the given project IDs from scanning.\n\n**--list-project-ids**\n> List all GCP project IDs accessible with the current credentials and exit.\n\n**--credentials-file** _PATH_\n> Path to a service account application credentials JSON file.\n\n**--impersonate-service-account** _EMAIL_\n> Impersonate the given service account when making API calls.\n\n**--skip-api-check**\n> Skip API activation verification and assume all required APIs are enabled.\n\n**-c**, **--checks** _CHECK_...\n> Run only the specified checks.\n\n**-s**, **--services** _SERVICE_...\n> Run only checks for the specified GCP services (e.g. _iam_, _compute_, _cloudstorage_).\n\n**--compliance** _FRAMEWORK_\n> Run the specified compliance framework (e.g. _cis_2.0_gcp_, _mitre_attack_gcp_).\n\n**-M**, **--output-modes** _FORMAT_\n> Output format(s): _csv_, _json-ocsf_, _json-asff_, _html_.\n\n**-F**, **--output-filename** _NAME_\n> Base name for output files.\n\n**-o**, **--output-directory** _DIR_\n> Directory where output reports are written."],["DESCRIPTION","**prowler gcp** performs a security assessment of Google Cloud Platform projects. It evaluates configurations against best practices and compliance frameworks such as CIS, MITRE ATT&CK, and GDPR, and reports findings as CSV, JSON, or HTML reports.\n\nBy default, Prowler scans every project accessible to the authenticated principal. Authentication can use Application Default Credentials (ADC), a credentials file, or service account impersonation."],["CAVEATS","Requires valid GCP credentials with at least the _Viewer_ and _Security Reviewer_ roles on target projects. Some checks need additional APIs (Cloud Asset, Cloud Resource Manager, IAM) enabled. Rate limiting on large organizations may slow scans significantly."],["INSTALL","```brew: brew install prowler```\n\n```nix: nix profile install nixpkgs#prowler```"],["SEE ALSO","[prowler](/man/prowler)(1), [prowler-aws](/man/prowler-aws)(1), [prowler-azure](/man/prowler-azure)(1), [prowler-kubernetes](/man/prowler-kubernetes)(1), [gcloud](/man/gcloud)(1)"]],"e":["prowler","prowler-aws","prowler-azure","prowler-kubernetes","gcloud"],"x":"prowler gcp"},"ps":{"n":"ps","t":"Report running process status","s":[["TAGLINE","Report running process status"],["TLDR","**List all processes**\n\n```ps aux```\n\n**List processes for current user**\n\n```ps -u [username]```\n\n**Show process tree**\n\n```ps -ef --forest```\n\n**Show specific process**\n\n```ps -p [pid]```\n\n**Custom output format**\n\n```ps -eo pid,ppid,cmd,%mem,%cpu```\n\n**List threads**\n\n```ps -eLf```\n\n**Sort by memory usage** (descending)\n\n```ps aux --sort=-%mem```\n\n**Select processes by command name**\n\n```ps -C [nginx]```"],["SYNOPSIS","**ps** [_options_]"],["PARAMETERS","**a**\n> All users.\n\n**u**\n> User-oriented format.\n\n**x**\n> Include processes without tty.\n\n**-e**\n> Every process.\n\n**-f**\n> Full format.\n\n**-p** _PID_\n> Select by PID.\n\n**-u** _USER_\n> Select by user.\n\n**--forest**\n> Process tree.\n\n**-o** _FORMAT_\n> Custom output.\n\n**-L**\n> Show threads with LWP and NLWP columns.\n\n**-C** _CMD_\n> Select by command name.\n\n**--sort** _KEY_\n> Sort output (e.g., --sort=-%mem)."],["DESCRIPTION","**ps** reports a snapshot of currently running processes on the system. It displays information such as process ID, user, CPU and memory usage, command name, and runtime for each process, providing a point-in-time view of system activity.\n\nThe command accepts options in two distinct styles: BSD syntax (without dashes, e.g., **aux**) and POSIX syntax (with dashes, e.g., **-ef**). Both produce similar output but differ in column defaults and filtering behavior. The **-o** option allows fully custom output formats, selecting specific fields like pid, ppid, command, and resource usage.\n\nCommon usage patterns include **ps aux** to list all processes with detailed info, **ps -ef --forest** to show the process hierarchy as a tree, and **ps -p** to inspect a specific process by PID."],["CAVEATS","BSD and POSIX options differ. Use aux or -ef."],["HISTORY","ps is a **classic Unix** process monitoring utility."],["INSTALL","```apt: sudo apt install procps```\n\n```dnf: sudo dnf install procps-ng```\n\n```pacman: sudo pacman -S procps-ng```\n\n```apk: sudo apk add procps-ng```\n\n```zypper: sudo zypper install procps```\n\n```brew: brew install procps```\n\n```nix: nix profile install nixpkgs#procps```"],["SEE ALSO","[top](/man/top)(1), [htop](/man/htop)(1), [pgrep](/man/pgrep)(1), [kill](/man/kill)(1)"]],"e":["top","htop","pgrep","kill"],"x":"ps aux"},"pulumi-schema":{"n":"pulumi-schema","t":"Validate Pulumi package schemas","s":[["TAGLINE","Validate Pulumi package schemas"],["TLDR","**Check schema validity**\n\n```pulumi schema check [schema.json]```"],["SYNOPSIS","**pulumi** **schema** _command_ [_options_]"],["PARAMETERS","**check** _file_\n> Validate schema file."],["DESCRIPTION","**pulumi schema** manages Pulumi package schemas. Schemas define resource types and their properties for providers. Primarily used by provider authors for validation."],["INSTALL","```pacman: sudo pacman -S pulumi```\n\n```apk: sudo apk add pulumi```\n\n```brew: brew install pulumi```\n\n```nix: nix profile install nixpkgs#pulumi```"],["SEE ALSO","[pulumi](/man/pulumi)(1), [pulumi-plugin](/man/pulumi-plugin)(1)"]],"e":["pulumi","pulumi-plugin"],"x":"pulumi schema check [schema.json]"},"pveum":{"n":"pveum","t":"Proxmox user and permission management","s":[["TAGLINE","Proxmox user and permission management"],["TLDR","**List** users\n\n```pveum user list```\n\n**Add** a user\n\n```pveum user add [username]@pve```\n\nAdd a user with **email, description, and password**\n\n```pveum user add [username]@pve --email [email] --comment [description] --password [password]```\n\n**Change** user password\n\n```pveum passwd [username]@pve```\n\n**Delete** a user\n\n```pveum user delete [username]@pve```\n\n**Grant** user access to a VM with specific permissions\n\n```pveum acl modify /vms/1000 --user [username]@pve --role PVEVMUser```\n\n**Generate** an API token for a user\n\n```pveum user token add [username]@pve [tokenid]```\n\n**List** available roles\n\n```pveum role list```"],["SYNOPSIS","**pveum** _command_ [_subcommand_] [_options_]"],["PARAMETERS","**user** **list**, **u l**\n> List all users\n\n**user** **add**, **u a** _user_\n> Add new user\n\n**user** **delete**, **u d** _user_\n> Delete user\n\n**passwd**, **pa** _user_\n> Change user password\n\n**acl** **modify**, **a m** _path_\n> Modify access control list\n\n**group** **list**\n> List groups\n\n**role** **list**\n> List roles\n\n**realm** **list**\n> List authentication realms\n\n**realm** **add** _realm_\n> Add authentication realm (pam, pve, ldap, ad, openid)\n\n**user** **token** **add** _user_ _tokenid_\n> Generate API token for a user\n\n**pool** **add** _poolid_\n> Create a resource pool\n\n**pool** **list**\n> List resource pools\n\n**--role** _role_\n> Assign role (PVEAdmin, PVEVMUser, etc.)\n\n**--email** _email_\n> User email address\n\n**--comment** _text_\n> User description"],["DESCRIPTION","**pveum** manages Proxmox VE users, groups, roles, and access control lists. It configures authentication realms (PAM, PVE, LDAP, Active Directory) and permission assignments for cluster resources.\n\nThe permission system uses a path-based model where access is granted at specific resource paths (datacenter, nodes, VMs, storage) with roles defining allowed operations."],["CAVEATS","User realms (@pve, @pam, @ldap) determine authentication method. Permission changes take effect immediately. Built-in root@pam cannot be deleted. Some operations require PVEUserAdmin role."],["HISTORY","Part of **Proxmox VE**, providing fine-grained access control for multi-user virtualization environments. Supports enterprise features like LDAP/AD integration and two-factor authentication."],["SEE ALSO","[pvesh](/man/pvesh)(1), [qm](/man/qm)(1), [pct](/man/pct)(1)"]],"e":["pvesh","qm","pct"],"x":"pveum user list"},"qm-resize":{"n":"qm-resize","t":"Resize Proxmox VM disk volumes","s":[["TAGLINE","Resize Proxmox VM disk volumes"],["TLDR","This command is an alias of **qm disk resize**\n\nView documentation for the original command\n\n```tldr qm disk```"],["SYNOPSIS","**qm resize** _vmid_ _disk_ _size_"],["DESCRIPTION","**qm resize** is an alias for **qm disk resize** that changes the size of a virtual machine disk. It can extend disk capacity but cannot shrink disks."],["INSTALL","```dnf: sudo dnf install qm```"],["SEE ALSO","[qm](/man/qm)(1), [qm-disk](/man/qm-disk)(1)"]],"e":["qm","qm-disk"],"x":"tldr qm disk"},"safe":{"n":"safe","t":"Simplified CLI for HashiCorp Vault","s":[["TAGLINE","Simplified CLI for HashiCorp Vault"],["TLDR","**Write a secret**\n\n```safe set [secret/path] [key]=[value]```\n\n**Read a secret**\n\n```safe get [secret/path]```\n\n**List all secret paths as a tree**\n\n```safe tree```\n\n**Delete a secret**\n\n```safe rm [secret/path]```\n\n**Target a Vault server**\n\n```safe target [https://vault-url] [alias]```\n\n**Authenticate against the current Vault**\n\n```safe auth [ldap|token|github]```\n\n**Copy a secret to a new path**\n\n```safe cp [secret/old/path] [secret/new/path]```\n\n**Generate a random password**\n\n```safe gen [secret/path] [key]```"],["SYNOPSIS","**safe** _command_ [_options_] [_args_]"],["PARAMETERS","**set** (alias: **write**)\n> Write or update a secret at a path.\n\n**get** (alias: **read**)\n> Read and display a secret.\n\n**rm** (alias: **delete**)\n> Delete one or more secret paths.\n\n**tree**\n> Print a tree listing of all reachable keys.\n\n**paths**\n> Print a flat listing of all reachable keys.\n\n**target**\n> Set or list Vault targets.\n\n**auth**\n> Authenticate against the currently targeted Vault.\n\n**export**\n> Export secrets to a backup file.\n\n**import**\n> Import secrets from a backup file.\n\n**cp** (alias: **copy**)\n> Copy a secret from one path to another.\n\n**mv** (alias: **move**)\n> Move a secret from one path to another.\n\n**gen**\n> Generate a random secret.\n\n**ssh**\n> Generate a new SSH RSA keypair.\n\n**rsa**\n> Generate a new RSA keypair."],["DESCRIPTION","**safe** is a user-friendly command-line interface for HashiCorp Vault that simplifies common secret management operations. It wraps the Vault API with intuitive commands for reading, writing, and organizing secrets without needing to understand the full Vault CLI syntax.\n\nThe tool supports multiple authentication methods including LDAP, GitHub tokens, and direct token authentication. Once targeted and authenticated against a Vault server, secrets can be managed using simple commands like **set**, **get**, and **rm**. The **tree** command provides a hierarchical view of all secret paths for easy browsing.\n\nSecrets can be exported and imported for backup purposes or migration between Vault instances. The **target** command manages connections to multiple Vault servers, allowing quick switching between environments."],["CAVEATS","Requires Vault access. Authentication needed. Permissions vary."],["HISTORY","**safe** was created by Stark & Wayne as a user-friendly CLI for HashiCorp Vault, simplifying secret management operations."],["INSTALL","```nix: nix profile install nixpkgs#safe```"],["SEE ALSO","[vault](/man/vault)(1), [pass](/man/pass)(1), [aws](/man/aws)(1)"]],"e":["vault","pass","aws"],"x":"safe set [secret/path] [key]=[value]"},"sfdk-package":{"n":"sfdk-package","t":"Build RPM packages for Sailfish apps","s":[["TAGLINE","Build RPM packages for Sailfish apps"],["TLDR","**Create RPM package**\n\n```sfdk package```\n\n**Package with debug info**\n\n```sfdk package --debuginfo```"],["SYNOPSIS","**sfdk package** [_options_]"],["PARAMETERS","**--debuginfo**\n> Include debug information.\n\n**--no-fix-version**\n> Don't auto-fix version."],["DESCRIPTION","**sfdk package** builds RPM packages for Sailfish OS applications. Part of Sailfish SDK."],["SEE ALSO","[sfdk](/man/sfdk)(1), [sfdk-deploy](/man/sfdk-deploy)(1)"]],"e":["sfdk","sfdk-deploy"],"x":"sfdk package"},"sfdk":{"n":"sfdk","t":"Sailfish OS application development toolkit","s":[["TAGLINE","Sailfish OS application development toolkit"],["TLDR","**Build package**\n\n```sfdk build```\n\n**Deploy to device**\n\n```sfdk deploy --device```\n\n**Run on device**\n\n```sfdk device exec [command]```\n\n**Create project**\n\n```sfdk init --template [app] [project-name]```\n\n**List devices**\n\n```sfdk device list```\n\n**Set target**\n\n```sfdk config --target=[target-name]```"],["SYNOPSIS","**sfdk** _command_ [_options_] [_args_]"],["PARAMETERS","**build**\n> Build project.\n\n**deploy**\n> Deploy to device.\n\n**device**\n> Device operations.\n\n**init**\n> Create project.\n\n**config**\n> Configuration.\n\n**--device**\n> Target device.\n\n**--target** _NAME_\n> Build target."],["DESCRIPTION","**sfdk** is the command-line interface for the Sailfish OS SDK, providing tools for developing, building, and deploying Sailfish OS applications. It handles project scaffolding from templates, cross-compilation for ARM and x86 targets, and deployment to physical devices or emulators.\n\nThe toolkit integrates with Qt/QML build systems (qmake, cmake) and manages the build environment including target SDK installations and build dependencies. Applications can be tested on emulated devices without requiring physical hardware, streamlining the development cycle."],["CAVEATS","Sailfish SDK required. Platform-specific. Qt/QML knowledge needed."],["HISTORY","**sfdk** is part of the **Sailfish OS SDK** by Jolla, providing command-line tools for mobile application development."],["SEE ALSO","[qmake](/man/qmake)(1), [rpm](/man/rpm)(1)"]],"e":["qmake","rpm"],"x":"sfdk build"},"sgdisk":{"n":"sgdisk","t":"Scriptable GPT partition table manager","s":[["TAGLINE","Scriptable GPT partition table manager"],["TLDR","**Show partition table**\n\n```sgdisk -p [/dev/sda]```\n\n**Create new GPT**\n\n```sgdisk -o [/dev/sda]```\n\n**Create partition**\n\n```sgdisk -n [1:0:+500M] [/dev/sda]```\n\n**Set partition type**\n\n```sgdisk -t [1:8300] [/dev/sda]```\n\n**Delete partition**\n\n```sgdisk -d [1] [/dev/sda]```\n\n**Change partition name**\n\n```sgdisk -c [1:boot] [/dev/sda]```\n\n**Backup partition table**\n\n```sgdisk -b [backup.gpt] [/dev/sda]```\n\n**Restore partition table**\n\n```sgdisk -l [backup.gpt] [/dev/sda]```"],["SYNOPSIS","**sgdisk** [_-n part:start:end_] [_-t part:type_] [_options_] _device_"],["PARAMETERS","**-p**, **--print**\n> Print partition table.\n\n**-o**, **--clear**\n> Create new GPT.\n\n**-n**, **--new** _PART:START:END_\n> Create partition.\n\n**-d**, **--delete** _PART_\n> Delete partition.\n\n**-t**, **--typecode** _PART:TYPE_\n> Set type code.\n\n**-c**, **--change-name** _PART:NAME_\n> Set name.\n\n**-b**, **--backup** _FILE_\n> Backup table.\n\n**-l**, **--load-backup** _FILE_\n> Restore table.\n\n**-L**, **--list-types**\n> List known partition type codes.\n\n**-i**, **--info** _PART_\n> Show detailed information about a partition.\n\n**-v**, **--verify**\n> Verify disk's GPT data structures.\n\n**-Z**, **--zap-all**\n> Destroy both GPT and MBR data structures.\n\n**-g**, **--mbrtogpt**\n> Convert MBR to GPT."],["DESCRIPTION","**sgdisk** is the command-line, scriptable counterpart to the interactive **gdisk** partitioning tool, designed for manipulating GUID Partition Table (GPT) disk layouts. It performs all operations through flags and arguments rather than an interactive menu, making it ideal for use in scripts, automated provisioning, and disk imaging workflows.\n\nPartition creation uses flexible notation where sizes can be specified with **+** prefixes for relative sizing and **0** to fill remaining space. Type codes such as **8300** (Linux filesystem), **EF00** (EFI System Partition), and **8200** (Linux swap) identify the purpose of each partition. Multiple operations can be chained in a single command to build complete partition layouts atomically.\n\nThe tool also supports backing up and restoring entire partition tables, converting MBR disks to GPT, and verifying table integrity. All changes are written in a single operation when the command completes successfully."],["CAVEATS","Destructive operations are immediate and cannot be undone. Specifying the wrong device will destroy data. Only works with GPT partition tables (use fdisk for MBR). Use `-b` to back up the partition table before making changes."],["HISTORY","**sgdisk** is part of the **GPT fdisk** suite by **Rod Smith**, first released around **2010**. It was created as the scriptable counterpart to the interactive **gdisk** tool, filling the need for automated GPT partition management in scripts and deployment workflows."],["INSTALL","```apt: sudo apt install gdisk```\n\n```dnf: sudo dnf install gdisk```\n\n```apk: sudo apk add sgdisk```"],["SEE ALSO","[gdisk](/man/gdisk)(1), [fdisk](/man/fdisk)(1), [parted](/man/parted)(1)"]],"e":["gdisk","fdisk","parted"],"x":"sgdisk -p [/dev/sda]"},"systemctl-list-jobs":{"n":"systemctl-list-jobs","t":"List pending systemd jobs","s":[["TAGLINE","List pending systemd jobs"],["TLDR","List **all** pending and running jobs\n\n```systemctl list-jobs```\n\nFilter jobs matching a **pattern**\n\n```systemctl list-jobs [pattern]```\n\n**Cancel** a specific job by ID\n\n```systemctl cancel [job_id]```"],["SYNOPSIS","**systemctl list-jobs** [_PATTERN_...]"],["DESCRIPTION","**systemctl list-jobs** displays the queue of pending and running jobs in the systemd service manager. Jobs represent state change operations like starting, stopping, or reloading units.\n\nThe output shows the job ID, unit name, job type (start, stop, reload, etc.), and state (waiting, running). Jobs appear during boot or when multiple unit state changes are triggered."],["JOB TYPES","**start** - Starting a unit\n\n**stop** - Stopping a unit\n\n**reload** - Reloading configuration\n\n**restart** - Restarting a unit\n\n**try-restart** - Restarting a unit if active\n\n**reload-or-restart** - Reloading or restarting a unit\n\n**verify-active** - Checking if active\n\n**nop** - No operation (used for ordering)"],["CAVEATS","The job queue is usually empty on an idle system. Jobs appear briefly during state changes and may complete before the command output is displayed. Job IDs are used with `systemctl cancel`."],["HISTORY","The **list-jobs** subcommand provides visibility into systemd's internal job queue, useful for debugging stuck boots or understanding why services are not starting. It has been part of systemd since its initial release."],["SEE ALSO","[systemctl-cancel](/man/systemctl-cancel)(1), [systemctl](/man/systemctl)(1)"]],"e":["systemctl-cancel","systemctl"],"x":"systemctl list-jobs"},"termscp":{"n":"termscp","t":"Feature-rich terminal file transfer and explorer","s":[["TAGLINE","Feature-rich terminal file transfer and explorer"],["TLDR","**Launch the interactive file transfer UI**\n\n```termscp```\n\n**Connect to a remote host via SFTP**\n\n```termscp sftp://[user]@[192.168.1.10]:22```\n\n**Browse an S3 bucket**\n\n```termscp s3://[my-bucket]```"],["SYNOPSIS","**termscp** [_options_] [_protocol_://_user_@_host_:_port_]"],["DESCRIPTION","**termscp** is a terminal UI file transfer tool with support for SCP, SFTP, FTP/FTPS, Kubernetes, S3, SMB, and WebDAV protocols. It provides a dual-pane file explorer for local and remote filesystems, built-in bookmarks, SSH key storage, customizable themes, desktop notifications for large transfers, and an embedded terminal."],["HISTORY","**termscp** was created by **Veeso** (Christian Visintin) and is written in **Rust**."],["INSTALL","```pacman: sudo pacman -S termscp```\n\n```zypper: sudo zypper install termscp```\n\n```brew: brew install termscp```\n\n```nix: nix profile install nixpkgs#termscp```"],["SEE ALSO","[scp](/man/scp)(1), [sftp](/man/sftp)(1), [rclone](/man/rclone)(1)"]],"e":["scp","sftp","rclone"],"x":"termscp"},"uv-run":{"n":"uv-run","t":"Execute commands in project virtual environment","s":[["TAGLINE","Execute commands in project virtual environment"],["TLDR","**Run command in project**\n\n```uv run [command]```\n\n**Run Python script**\n\n```uv run python [script.py]```\n\n**Run with extra dependencies**\n\n```uv run --with [package] [command]```\n\n**Run a standalone script with inline dependencies**\n\n```uv run [script.py]```\n\n**Run without syncing the environment**\n\n```uv run --frozen [command]```\n\n**Run with a specific Python version**\n\n```uv run --python [3.12] python [script.py]```\n\n**Run an installed tool** (e.g., pytest, ruff)\n\n```uv run pytest [tests/]```"],["SYNOPSIS","**uv** **run** [_options_] _command_ [_args_...]"],["PARAMETERS","**--with** _package_\n> Include an extra dependency for this invocation only.\n\n**--frozen**\n> Run without updating the lockfile. Fail if lockfile is missing or outdated.\n\n**--no-sync**\n> Skip syncing the environment before running.\n\n**--isolated**\n> Run in an isolated temporary environment, ignoring the project.\n\n**--python** _version_\n> Use a specific Python version or interpreter.\n\n**--no-project**\n> Avoid discovering a project or workspace.\n\n**--package** _name_\n> Run the command in a specific package within a workspace.\n\n**--extra** _name_\n> Include optional dependency group.\n\n**--all-extras**\n> Include all optional dependency groups."],["DESCRIPTION","**uv run** executes commands in the project's virtual environment. It automatically creates the virtual environment and syncs dependencies if needed, making it the primary way to run scripts and tools in uv-managed projects.\n\nWhen running a `.py` file with inline script metadata (PEP 723), uv automatically resolves and installs the declared dependencies. The **--with** flag allows adding ad-hoc dependencies without modifying project configuration."],["CAVEATS","If no `pyproject.toml` is found, **uv run** operates without a project context. The **--frozen** flag requires an existing lockfile and will fail if one is not present."],["INSTALL","```dnf: sudo dnf install uv```\n\n```pacman: sudo pacman -S uv```\n\n```apk: sudo apk add uv```\n\n```brew: brew install uv```\n\n```nix: nix profile install nixpkgs#uv```"],["SEE ALSO","[uv](/man/uv)(1), [uv-sync](/man/uv-sync)(1), [uv-lock](/man/uv-lock)(1), [python](/man/python)(1), [pip](/man/pip)(1)"]],"e":["uv","uv-sync","uv-lock","python","pip"],"x":"uv run [command]"},"vegeta":{"n":"vegeta","t":"HTTP load testing tool","s":[["TAGLINE","HTTP load testing tool"],["TLDR","**Attack endpoint**\n\n```echo \"GET http://localhost:8080/\" | vegeta attack -rate=[50] | vegeta report```\n\n**Attack with duration**\n\n```echo \"GET http://localhost/\" | vegeta attack -rate=[100] -duration=[30s] | vegeta report```\n\n**Plot results**\n\n```vegeta attack ... | vegeta plot > [results.html]```\n\n**Encode to JSON**\n\n```vegeta attack ... | vegeta encode --to=json```\n\n**From targets file**\n\n```vegeta attack -targets=[targets.txt] -rate=[10]```\n\n**POST request**\n\n```echo \"POST http://localhost/api\" | vegeta attack -body=[data.json]```"],["SYNOPSIS","**vegeta** _command_ [_options_]"],["PARAMETERS","**attack**\n> Send requests.\n\n**report**\n> Generate report.\n\n**plot**\n> HTML plot.\n\n**encode**\n> Convert format.\n\n**-rate** _N_\n> Requests per second.\n\n**-duration** _D_\n> Attack duration.\n\n**-targets** _FILE_\n> Targets file.\n\n**-body** _FILE_\n> Request body.\n\n**-header** _H_\n> HTTP header."],["DESCRIPTION","**vegeta** is an HTTP load testing tool that sends requests at a constant, configurable rate. The **attack** command generates traffic and streams binary results to stdout, which can be piped to **report** for statistical analysis or **plot** for HTML visualization.\n\nThis pipeline design allows flexible composition of load test workflows. Target definitions support multiple endpoints, HTTP methods, headers, and request bodies. Reports include latency percentiles, success rates, and throughput metrics, making it straightforward to identify performance regressions and capacity limits."],["CAVEATS","High rates need tuning. System limits may apply. Target server must handle load."],["HISTORY","**Vegeta** was created by **Tomás Senart** for HTTP load testing. Named after the Dragon Ball character, it emphasizes power and speed."],["INSTALL","```pacman: sudo pacman -S vegeta```\n\n```zypper: sudo zypper install vegeta```\n\n```brew: brew install vegeta```\n\n```nix: nix profile install nixpkgs#vegeta```"],["SEE ALSO","[ab](/man/ab)(1), [wrk](/man/wrk)(1), [hey](/man/hey)(1)"]],"e":["ab","wrk","hey"],"x":"echo \"GET http://localhost:8080/\" | vegeta attack -rate=[50] | vegeta report"},"velero":{"n":"velero","t":"Kubernetes cluster backup and restore","s":[["TAGLINE","Kubernetes cluster backup and restore"],["TLDR","**Create backup**\n\n```velero backup create [backup-name]```\n\n**List backups**\n\n```velero backup get```\n\n**Restore from backup**\n\n```velero restore create --from-backup [backup-name]```\n\n**Schedule backup**\n\n```velero schedule create [daily] --schedule=\"0 1 * * *\"```\n\n**Describe backup**\n\n```velero backup describe [backup-name]```\n\n**Delete backup**\n\n```velero backup delete [backup-name]```\n\n**List restores**\n\n```velero restore get```\n\n**Install Velero**\n\n```velero install --provider [aws] --bucket [bucket-name]```"],["SYNOPSIS","**velero** _command_ [_options_]"],["PARAMETERS","**backup create** _NAME_\n> Create backup.\n\n**backup get**\n> List backups.\n\n**restore create**\n> Restore from backup.\n\n**schedule create** _NAME_\n> Create schedule.\n\n**--from-backup** _NAME_\n> Source backup.\n\n**--schedule** _CRON_\n> Cron schedule.\n\n**--include-namespaces** _NS_\n> Limit namespaces.\n\n**--provider** _PROVIDER_\n> Storage provider."],["DESCRIPTION","**velero** provides backup and restore capabilities for Kubernetes clusters. It captures cluster resource definitions and persistent volume snapshots, storing them in cloud object storage such as S3, GCS, or Azure Blob Storage.\n\nBackups can be created on demand or automated with cron-based schedules. Restores recreate resources from backups, supporting both full cluster recovery and selective restoration by namespace or resource type. The tool is commonly used for disaster recovery, cluster migration, and pre-upgrade snapshots."],["CAVEATS","Requires object storage. Volume snapshots depend on provider. Some resources excluded by default."],["HISTORY","**Velero** (formerly Heptio Ark) was created for Kubernetes backup. Now maintained by VMware as part of the Tanzu portfolio."],["INSTALL","```pacman: sudo pacman -S velero```\n\n```zypper: sudo zypper install velero```\n\n```brew: brew install velero```\n\n```nix: nix profile install nixpkgs#velero```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [restic](/man/restic)(1)"]],"e":["kubectl","restic"],"x":"velero backup create [backup-name]"},"wasm-opt":{"n":"wasm-opt","t":"Optimize WebAssembly binary modules","s":[["TAGLINE","Optimize WebAssembly binary modules"],["TLDR","**Optimize WASM**\n\n```wasm-opt -O [input.wasm] -o [output.wasm]```\n\n**Maximum optimization**\n\n```wasm-opt -O4 [input.wasm] -o [output.wasm]```\n\n**Size optimization**\n\n```wasm-opt -Os [input.wasm] -o [output.wasm]```\n\n**Print stats**\n\n```wasm-opt --print [input.wasm]```\n\n**Validate only**\n\n```wasm-opt --validate [input.wasm]```"],["SYNOPSIS","**wasm-opt** [_-O_] [_-Os_] [_-o output_] [_options_] _input_"],["PARAMETERS","**-O**\n> Execute default optimization passes.\n\n**-O0**\n> No optimization passes.\n\n**-O1**\n> Quick and useful optimizations for iteration builds.\n\n**-O2**\n> Most optimizations, generally gets most performance.\n\n**-O3**\n> Aggressive optimizations, may take significant time.\n\n**-O4**\n> Also flatten the IR; can take a lot more time and memory.\n\n**-Os**\n> Optimize for code size.\n\n**-Oz**\n> Optimize aggressively for code size.\n\n**-o** _FILE_\n> Output file.\n\n**--print**\n> Print module in text format.\n\n**--validate**\n> Validate the module without optimizing.\n\n**--converge**\n> Keep iterating optimization passes until no further improvement.\n\n**-g**, **--debuginfo**\n> Preserve debug info in output.\n\n**-ism** _FILE_\n> Read input source map.\n\n**-osm** _FILE_\n> Write output source map."],["DESCRIPTION","**wasm-opt** is a WebAssembly optimizer from the Binaryen toolkit that applies various optimization passes to reduce module size and improve execution speed. It processes .wasm binary files and outputs optimized versions suitable for production deployment.\n\nOptimization levels range from -O0 (none) through -O4 (maximum), with each level enabling progressively more aggressive transformations. -O1 provides quick wins for iteration builds, -O2 covers most optimizations, -O3 is more aggressive, and -O4 additionally flattens the IR. The -Os and -Oz flags optimize for code size, which is important for web delivery. Available passes include dead code elimination, inlining, constant folding, and many WebAssembly-specific optimizations.\n\nThe tool can also validate modules for specification conformance and print module contents in text format for inspection. It is commonly used as a post-processing step in WebAssembly build pipelines after compilation from source languages."],["CAVEATS","Part of Binaryen. WASM specific. May change semantics rarely."],["HISTORY","**wasm-opt** is part of **Binaryen**, a compiler infrastructure library for WebAssembly."],["INSTALL","```apt: sudo apt install binaryen```\n\n```dnf: sudo dnf install binaryen```\n\n```pacman: sudo pacman -S binaryen```\n\n```apk: sudo apk add binaryen```\n\n```zypper: sudo zypper install binaryen```\n\n```brew: brew install binaryen```\n\n```nix: nix profile install nixpkgs#binaryen```"],["SEE ALSO","[wasm2wat](/man/wasm2wat)(1), [wat2wasm](/man/wat2wasm)(1), [wasm-strip](/man/wasm-strip)(1)"]],"e":["wasm2wat","wat2wasm","wasm-strip"],"x":"wasm-opt -O [input.wasm] -o [output.wasm]"},"which":{"n":"which","t":"Locate executable in PATH","s":[["TAGLINE","Locate executable in PATH"],["TLDR","**Find the path of a command**\n\n```which [command]```\n\n**Find paths of multiple commands**\n\n```which [command1] [command2]```\n\n**Show all matching executables** (not just first)\n\n```which -a [command]```"],["SYNOPSIS","**which** [**-a**] _command_..."],["PARAMETERS","**-a**, **--all**\n> Print all matching executables in PATH, not just first"],["DESCRIPTION","**which** searches for executables in the directories listed in the PATH environment variable and prints the path of the first match found.\n\nThis is useful for determining which version of a command will be executed when multiple versions exist, or for confirming a command is installed and in your PATH.\n\nWith **-a**, all matches are shown, revealing if multiple versions exist in different PATH directories. The first listed is the one that would be executed."],["CAVEATS","Which only searches PATH; it doesn't show shell builtins, aliases, or functions. Use **type** in bash or **whence** in zsh for comprehensive command lookup.\n\nSome systems have different which implementations with varying options. The **-a** flag is common but not universal.\n\nIf a command isn't found, which typically exits with status 1 and produces no output (or an error message depending on implementation)."],["INSTALL","```dnf: sudo dnf install which```\n\n```pacman: sudo pacman -S which```\n\n```apk: sudo apk add which```\n\n```zypper: sudo zypper install which```\n\n```nix: nix profile install nixpkgs#which```"],["SEE ALSO","[type](/man/type)(1), [whence](/man/whence)(1), [whereis](/man/whereis)(1), [command](/man/command)(1), [hash](/man/hash)(1)"]],"e":["type","whence","whereis","command","hash"],"x":"which [command]"},"wienerdog":{"n":"wienerdog","t":"File-based memory, skills, and nightly routines for Claude Code and Codex CLI","s":[["TAGLINE","File-based memory, skills, and nightly routines for Claude Code and Codex CLI"],["TLDR","**Initialize** Wienerdog (preview first with dry-run)\n\n```npx wienerdog@latest init --dry-run```\n\n```npx wienerdog@latest init```\n\n```npx wienerdog@latest init --yes```\n\n**Check** install health and safety gates\n\n```wienerdog doctor```\n\n```wienerdog safety```\n\n**Sync** session digest from identity notes\n\n```wienerdog sync```\n\n**Run** a dream consolidation now\n\n```wienerdog dream```\n\n**Approve** proposed identity-note edits\n\n```wienerdog memory approve```\n\n**Remove** everything Wienerdog installed\n\n```wienerdog uninstall```"],["SYNOPSIS","**wienerdog** _command_ [_options_]"],["DESCRIPTION","**wienerdog** installs a plain-file “memory vault” and related skills/hooks so coding agents (**Claude Code** and **Codex CLI**) share one profile and long-term notes. It does not run a daemon or phone home: layout is markdown and config under **~/.wienerdog**, interpreted by the AI tools you already use.\n\n**init** creates the core, detects agents, and can drive an interview that fills **CLAUDE.md** / **AGENTS.md** from the same vault (Obsidian-style PARA). A scheduled **dream** job consolidates recent sessions into memory (at most one git commit per night) and can propose identity-note edits that only apply after **wienerdog memory approve**. Optional Google Workspace access and catalog routines use the OS scheduler (launchd / systemd / Task Scheduler) with verification before each run.\n\nRequires **Node.js ≥ 18**. Typical install: **npx wienerdog@latest init**, or the upstream **install.sh** bootstrap, or **npm i -g wienerdog**. Status is **0.x**; file formats may still evolve until 1.0."],["PARAMETERS","**init** [**--dry-run**] [**--yes**]\n> Create **~/.wienerdog**, detect AI tools, and set up skills/hooks. **--dry-run** prints the plan without writing.\n\n**adopt**\n> Point Wienerdog at an existing vault directory instead of creating a new one.\n\n**sync**\n> Re-render the session digest from the vault's identity notes (also the fix after manual **config.yaml** edits that must re-authorize scheduled jobs).\n\n**update**\n> Update Wienerdog to the latest published version without requiring a separate npm workflow.\n\n**dream**\n> Consolidate recent sessions into vault memory (same work as the nightly job).\n\n**schedule**\n> Add, remove, or list scheduled jobs (dream and optional routines).\n\n**run-job**\n> Run a scheduled job immediately (used by the OS scheduler).\n\n**doctor**\n> Check an existing install for problems.\n\n**alerts**\n> List job alerts; silence seen ones with typed confirmation.\n\n**uninstall**\n> Remove files Wienerdog created (reverses the install).\n\n**gws**\n> Google Workspace helpers (read Gmail/Calendar/Drive; draft mail) via a local capability broker.\n\n**grant**\n> Authorize a routine to send email (typed confirmation).\n\n**memory**\n> Approve identity-note changes so they inject into the next session (typed confirmation).\n\n**safety**\n> Show which sensitive actions are allowed or blocked.\n\n**--dry-run**\n> Global: show what would happen; make no changes (where supported).\n\n**--yes**\n> Global: skip confirmation prompts (where supported)."],["CONFIGURATION","**~/.wienerdog**\n> Core install root: vault, skills, hooks, and config. **uninstall** removes what Wienerdog wrote.\n\n**config.yaml** (under the install tree)\n> Runtime/schedule configuration. Edits outside **wienerdog sync** can fail closed for scheduled jobs until re-authorized.\n\n**CLAUDE.md** / **AGENTS.md**\n> Agent identity digests generated from the shared vault interview and memory notes."],["CAVEATS","Depends on Claude Code and/or Codex CLI being installed and restarted after setup so skills load. Secret scrubbing is best-effort. Scheduled integrity checks detect drift between syncs; they are not OS-level malware defense. Project is pre-1.0; treat the installed file layout as the public API."],["HISTORY","**Wienerdog** is an open-source (MIT) npm package that treats agent memory and routines as versioned files rather than a personal-AI daemon. Upstream: **github.com/wienerdog-ai/wienerdog**."],["SEE ALSO","[claude](/man/claude)(1), [codex](/man/codex)(1), [npx](/man/npx)(1), [npm](/man/npm)(1), [node](/man/node)(1)"],["RESOURCES","```[Source code](https://github.com/wienerdog-ai/wienerdog)```\n\n```[Homepage](https://www.npmjs.com/package/wienerdog)```"]],"e":["claude","codex","npx","npm","node"],"x":"npx wienerdog@latest init --dry-run"},"zapier-versions":{"n":"zapier-versions","t":"List Zapier integration versions","s":[["TAGLINE","List Zapier integration versions"],["TLDR","**List versions**\n\n```zapier versions```"],["SYNOPSIS","**zapier** **versions**"],["DESCRIPTION","**zapier versions** displays all versions of your Zapier integration. Shows version numbers and deployment status. Useful for tracking releases and rollbacks."],["SEE ALSO","[zapier](/man/zapier)(1), [zapier-history](/man/zapier-history)(1), [zapier-push](/man/zapier-push)(1)"]],"e":["zapier","zapier-history","zapier-push"],"x":"zapier versions"}});
