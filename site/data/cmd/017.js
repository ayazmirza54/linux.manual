window.LCL_BUCKET(17,{"aa-audit":{"n":"aa-audit","t":"Set AppArmor profiles to audit mode","s":[["TAGLINE","Set AppArmor profiles to audit mode"],["TLDR","Set a profile to **audit mode**\n\n```sudo aa-audit [profile_name]```\n\nSet **multiple profiles** to audit mode\n\n```sudo aa-audit [profile1] [profile2]```\n\nSet a profile to audit mode from a **specific directory**\n\n```sudo aa-audit -d /path/to/profiles [profile_name]```\n\nSet a profile to audit mode **without reloading** it\n\n```sudo aa-audit --no-reload [profile_name]```"],["SYNOPSIS","**aa-audit** _executable_ [_executable_ ...] [_-d /path/to/profiles_] [_--no-reload_]"],["DESCRIPTION","**aa-audit** configures AppArmor security profiles to operate in audit mode. In this mode, security policy is enforced and **all access attempts** (both successes and failures) are logged to the system log. This allows administrators to monitor application behavior while still enforcing security policies."],["PARAMETERS","**-d, --dir /path/to/profiles**\n> Specifies the directory containing AppArmor profiles; defaults to /etc/apparmor.d\n\n**--no-reload**\n> Prevents automatic profile reloading after modifications\n\n**-r, --remove**\n> Disable audit mode for the specified profile (reverses the effect of aa-audit).\n\n**-h, --help**\n> Display help information"],["CAVEATS","Audit mode generates significant log output as it records all access attempts. This can impact system performance and fill up log files quickly on busy systems."],["HISTORY","Part of the **AppArmor** application security framework, developed as an alternative to SELinux for Linux systems. AppArmor was originally developed by **Immunix** and later acquired by **Novell** in 2005."],["INSTALL","```apk: sudo apk add apparmor-utils```\n\n```zypper: sudo zypper install apparmor-utils```\n\n```nix: nix profile install nixpkgs#apparmor-utils```"],["SEE ALSO","[aa-enforce](/man/aa-enforce)(8), [aa-complain](/man/aa-complain)(8), [aa-disable](/man/aa-disable)(8), [aa-status](/man/aa-status)(8), [apparmor](/man/apparmor)(7)"]],"e":["aa-enforce","aa-complain","aa-disable","aa-status","apparmor"],"x":"sudo aa-audit [profile_name]"},"abrt-action-analyze-c":{"n":"abrt-action-analyze-c","t":"generate UUID from coredump for crash deduplication","s":[["TAGLINE","generate UUID from coredump for crash deduplication"],["TLDR","Compute and store a UUID for C/C++ crashes in the **current dump dir**\n\n```abrt-action-analyze-c```\n\nDo the same for a **specified dump directory**\n\n```abrt-action-analyze-c -d [path/to/directory]```\n\nRun with **verbose** output\n\n```abrt-action-analyze-c -v```"],["SYNOPSIS","**abrt-action-analyze-c** [_-v_] [_-d DIR_]"],["DESCRIPTION","**abrt-action-analyze-c** processes a coredump file from a problem data directory and generates a universally unique identifier (UUID). The UUID is saved as a new element in the directory structure.\n\nThis tool integrates with ABRT events, particularly for generating UUIDs when new coredumps are saved."],["PARAMETERS","**-d DIR**\n> Specifies the path to the problem directory where the coredump is located; defaults to current working directory\n\n**-v**\n> Enables verbose output; can be specified multiple times to increase verbosity levels"],["CAVEATS","Requires a coredump file to exist in the problem directory. Typically used as part of the ABRT event pipeline rather than directly by users."],["HISTORY","Part of **ABRT** (Automatic Bug Reporting Tool), developed by Red Hat for Fedora-based systems to detect, analyze, and report application crashes."],["SEE ALSO","[abrt-cli](/man/abrt-cli)(1), [abrt-action-analyze-backtrace](/man/abrt-action-analyze-backtrace)(1), [abrt-action-generate-backtrace](/man/abrt-action-generate-backtrace)(1)"],["RESOURCES","```[Source code](https://github.com/abrt/abrt)```\n\n```[Documentation](https://abrt.readthedocs.io/)```"]],"e":["abrt-cli","abrt-action-analyze-backtrace","abrt-action-generate-backtrace"],"x":"abrt-action-analyze-c"},"ascii-royale":{"n":"ascii-royale","t":"Peer-to-peer battle royale in the terminal","s":[["TAGLINE","Peer-to-peer battle royale in the terminal"],["TLDR","**Drop straight** into the public arena\n\n```ascii-royale play```\n\n**Browse open games** and pick one\n\n```ascii-royale browse```\n\n**Play offline** against bots\n\n```ascii-royale solo --bots [9]```\n\n**Host a match** for others to join\n\n```ascii-royale host```\n\n**Join a specific** match by ticket\n\n```ascii-royale join [ticket]```"],["SYNOPSIS","**ascii-royale** _command_ [_options_]"],["PREVIEW","```\n ~~~~~~~~~~~~~\n ~ . @ . o . ~\n ~ . ### . . ~\n ~ x . . . o ~\n ~~~ storm ~~~\n```"],["DESCRIPTION","**ascii-royale** is a battle royale game played entirely in the terminal. Up to 16 combatants fight on a procedurally generated ASCII island, with a shrinking storm that forces players together until one is left standing. Bots fill any empty slots, so solo play works without other people.\n\nMatches are peer-to-peer over the iroh networking library, with no central game server and no accounts: the whole game is a single binary. Players can drop into the public arena, browse and join open games, host their own match, or run a headless arena server. The same game is also playable in a browser."],["COMMANDS","**play**\n> Drop straight into the public arena.\n\n**browse**\n> List open games and select one to join.\n\n**join** _ticket_\n> Join a specific match using its ticket.\n\n**host**\n> Host a match. Accepts **--bots** _N_ and **--name** _NAME_.\n\n**solo --bots** _N_\n> Play offline against the given number of AI opponents.\n\n**serve**\n> Run a headless arena server."],["PARAMETERS","**--bots** _N_\n> Number of bot players to add.\n\n**--http-port** _PORT_\n> Serve the web interface on the given port.\n\n**--ticket-file** _PATH_\n> Write the join ticket to a file.\n\n**--stats-file** _PATH_\n> Persist leaderboard data to a file."],["CONTROLS","**w a s d** / **arrows**\n> Move and set aim direction.\n\n**f** / **space**\n> Fire weapon (auto-aims at lined-up enemies).\n\n**e** / **g**\n> Pick up items.\n\n**h** / **m**\n> Use a medkit.\n\n**t**\n> Throw a grenade.\n\n**M**\n> Mute or unmute sound.\n\n**q** / **Esc**\n> Quit."],["CAVEATS","Networking is peer-to-peer over iroh, so matches depend on direct connectivity between players. The project is built in Rust and is installed from source with a recent toolchain."],["HISTORY","**ascii-royale** is written in **Rust** and distributed under the MIT license. It models the battle-royale genre in pure text, using iroh for serverless peer-to-peer play and 8-bit synthesized sound effects."],["SEE ALSO","[ninvaders](/man/ninvaders)(1), [hunt](/man/hunt)(1), [nethack-console](/man/nethack-console)(1)"],["RESOURCES","```[Source code](https://github.com/chad/ascii-royale)```"]],"e":["ninvaders","hunt","nethack-console"],"x":"ascii-royale play"},"auracle":{"n":"auracle","t":"Lightweight tool for querying the Arch User Repository.","s":[["TAGLINE","Lightweight tool for querying the Arch User Repository."],["TLDR","**Search** for AUR packages matching a regex\n\n```auracle search '[regex]'```\n\nDisplay **information** about one or more AUR packages\n\n```auracle info [package1] [package2] ...```\n\nDisplay the **PKGBUILD** of one or more AUR packages\n\n```auracle show [package1] [package2] ...```\n\nDisplay **updates** for installed AUR packages\n\n```auracle outdated```\n\n**Clone** a package repository recursively with dependencies\n\n```auracle clone --recurse [package]```"],["SYNOPSIS","**auracle** _command_ [_options_] [_packages_]"],["DESCRIPTION","**auracle** is a lightweight command-line tool for querying the Arch User Repository (AUR). It provides fast search, package information lookup, and update checking without building or installing packages.\n\nWritten in C++, auracle focuses on AUR queries rather than full package management. It is designed to be used with other tools or scripts for a complete AUR workflow."],["SUBCOMMANDS","**search** _pattern_\n> Search the AUR for packages matching the pattern (regex)\n\n**info** _packages_\n> Display detailed information about specific packages\n\n**show** _packages_\n> Print the PKGBUILD file for packages\n\n**outdated**\n> List installed AUR packages with available updates\n\n**clone** _packages_\n> Clone package git repositories\n\n**download** _packages_\n> Download packages that are outdated, optionally with `--recurse` to also download new dependencies\n\n**buildorder** _packages_\n> Show the build order and origin of packages needed for a given set of AUR packages\n\n**rawsearch** _pattern_\n> Dump the raw JSON response from the AUR for a search request\n\n**rawinfo** _packages_\n> Dump the raw JSON response from the AUR for an info request"],["PARAMETERS","**-q**, **--quiet**\n> Limit output to package names only (used with `search` and `outdated`)\n\n**--sort** _field_\n> Sort search results by field (name, votes, popularity)\n\n**--rsort** _field_\n> Sort search results by field in reverse order\n\n**--literal**\n> Treat search terms as literal strings rather than regular expressions\n\n**--recurse**\n> Recursively follow and process dependencies (used with `clone` and `download`)\n\n**--show-file** _file_\n> Control which source file is displayed by the `show` subcommand\n\n**--search-by** _field_\n> Search by a specific field (name, name-desc, maintainer, depends, makedepends, optdepends, checkdepends)\n\n**--color** _when_\n> Control colored output: `auto`, `never`, or `always` (default: auto)"],["CAVEATS","Does not build or install packages; use with **makepkg** or an AUR helper for installation. Regex patterns should be quoted to prevent shell expansion. Only queries the AUR API; does not access local package databases."],["SEE ALSO","[yay](/man/yay)(1), [paru](/man/paru)(1), [makepkg](/man/makepkg)(8), [pacman](/man/pacman)(8)"]],"e":["yay","paru","makepkg","pacman"],"x":"auracle search '[regex]'"},"az-devops":{"n":"az-devops","t":"Manage Azure DevOps organizations and projects","s":[["TAGLINE","Manage Azure DevOps organizations and projects"],["TLDR","**Configure default organization** and project\n\n```az devops configure --defaults organization=[https://dev.azure.com/contoso] project=[ContosoWebApp]```\n\n**List team projects** in the organization\n\n```az devops project list```\n\n**Show details** of a project\n\n```az devops project show --project [MyProject]```\n\n**Create a new project**\n\n```az devops project create --name [MyProject] --org [https://dev.azure.com/contoso]```\n\n**Delete a project**\n\n```az devops project delete --id [project-id] --yes```\n\n**Open a project** in the web browser\n\n```az devops project show --project [MyProject] --open```\n\n**Login to Azure DevOps** with a personal access token\n\n```az devops login --organization [https://dev.azure.com/contoso]```\n\n**Logout from Azure DevOps**\n\n```az devops logout```\n\n**Invoke a DevOps REST API** endpoint directly\n\n```az devops invoke --area core --resource projects --api-version 6.0```"],["SYNOPSIS","**az** **devops** _subcommand_ [_options_]"],["SUBCOMMANDS","**configure**\n> Configure Azure DevOps CLI or view configuration.\n\n**project list**\n> List team projects.\n\n**project show**\n> Show project details.\n\n**project create**\n> Create a team project.\n\n**project delete**\n> Delete a team project.\n\n**admin**\n> Manage administration operations.\n\n**extension**\n> Manage extensions.\n\n**security**\n> Manage security related operations.\n\n**service-endpoint**\n> Manage service endpoints/connections.\n\n**team**\n> Manage teams.\n\n**user**\n> Manage users.\n\n**wiki**\n> Manage wikis.\n\n**login**\n> Set credentials for a particular organization.\n\n**logout**\n> Clear credentials for a particular organization.\n\n**invoke**\n> Invoke a request for any DevOps REST API."],["DESCRIPTION","**az devops** manages Azure DevOps organization level operations. It provides CLI access to manage projects, teams, users, security, and extensions within Azure DevOps Services.\n\nRelated command groups include **az pipelines** for CI/CD pipelines, **az boards** for work items, **az repos** for Git repositories, and **az artifacts** for package feeds."],["CAVEATS","Requires the Azure DevOps CLI extension to be installed with **az extension add --name azure-devops**. Only available for Azure DevOps Services (cloud), not Azure DevOps Server (on-premises). Requires Azure CLI v2.0.69 or later."],["INSTALL","```dnf: sudo dnf install azure-cli```\n\n```pacman: sudo pacman -S azure-cli```\n\n```zypper: sudo zypper install azure-cli```\n\n```brew: brew install azure-cli```\n\n```nix: nix profile install nixpkgs#azure-cli```"],["SEE ALSO","[az](/man/az)(1), [az-pipelines](/man/az-pipelines)(1), [az-repos](/man/az-repos)(1)"]],"e":["az","az-pipelines","az-repos"],"x":"az devops configure --defaults organization=[https://dev.azure.com/contoso] project=[ContosoWebApp]"},"bzip2":{"n":"bzip2","t":"Block-sorting file compressor","s":[["TAGLINE","Block-sorting file compressor"],["TLDR","**Compress** file\n\n```bzip2 [file.txt]```\n\n**Decompress** file\n\n```bzip2 -d [file.txt.bz2]```\n\n**Keep** original file\n\n```bzip2 -k [file.txt]```\n\n**Compress** to stdout\n\n```bzip2 -c [file.txt] > [file.txt.bz2]```\n\nSet **compression** level\n\n```bzip2 -9 [file.txt]```"],["SYNOPSIS","**bzip2** [_options_] [_file_...]"],["DESCRIPTION","**bzip2** compresses files using the Burrows-Wheeler block-sorting compression algorithm. It typically achieves better compression ratios than gzip but is slower. By default, it replaces the original file with a compressed .bz2 file.\n\nThe tool is commonly used for compressing tarballs and large files."],["PARAMETERS","**-z**, **--compress**\n> Force compression (default behavior)\n\n**-d**, **--decompress**\n> Decompress file\n\n**-k**, **--keep**\n> Keep original files\n\n**-f**, **--force**\n> Overwrite existing files\n\n**-c**, **--stdout**\n> Write to standard output\n\n**-t**, **--test**\n> Test compressed file integrity\n\n**-v**, **--verbose**\n> Verbose mode (repeat for more detail)\n\n**-1** to **-9**\n> Compression level (1=fast, 9=best, default: 9)\n\n**--fast**\n> Alias for -1\n\n**--best**\n> Alias for -9\n\n**-s**, **--small**\n> Use less memory for compression/decompression (at most 2500 KB)\n\n**-q**, **--quiet**\n> Suppress non-critical warnings\n\n**-L**, **--license**\n> Display license and version information"],["WORKFLOW","```bash"],["Compress file (creates file.txt.bz2, removes original)","bzip2 file.txt"],["Decompress","bzip2 -d file.txt.bz2"],["Compress keeping original","bzip2 -k file.txt"],["Compress to stdout","bzip2 -c file.txt > file.txt.bz2"],["Test integrity","bzip2 -t file.txt.bz2"],["Compress tar archive","tar -cjf archive.tar.bz2 directory/\n```"],["COMPRESSION","Block size affects memory usage and compression:\n- **-1** - 100 KB blocks (fast, lower compression)\n- **-9** - 900 KB blocks (slow, better compression, default)"],["CAVEATS","Slower than gzip. Default removes original file (use -k to keep). Single-threaded (use **pbzip2** or **lbzip2** for parallel compression). Memory usage proportional to block size. .bz2 files are typically larger than .xz but smaller than .gz."],["HISTORY","**bzip2** was created by Julian Seward in **1996** as a replacement for the earlier bzip, using improved algorithms for better compression."],["INSTALL","```apt: sudo apt install bzip2```\n\n```dnf: sudo dnf install bzip2```\n\n```pacman: sudo pacman -S bzip2```\n\n```apk: sudo apk add bzip2```\n\n```zypper: sudo zypper install bzip2```\n\n```brew: brew install bzip2```\n\n```nix: nix profile install nixpkgs#bzip2```"],["SEE ALSO","[bunzip2](/man/bunzip2)(1), [gzip](/man/gzip)(1), [xz](/man/xz)(1), [tar](/man/tar)(1)"]],"e":["bunzip2","gzip","xz","tar"],"x":"bzip2 [file.txt]"},"clamscan":{"n":"clamscan","t":"command-line virus scanner","s":[["TAGLINE","command-line virus scanner"],["TLDR","**Scan single file**\n\n```clamscan [file]```\n\n**Scan directory recursively**\n\n```clamscan -r [/path/to/dir]```\n\n**Show only infected files**\n\n```clamscan -ri [/path/to/dir]```\n\n**Move infected files**\n\n```clamscan -r --move=[/quarantine] [/path/to/scan]```\n\n**Remove infected files**\n\n```clamscan -r --remove [/path/to/scan]```\n\n**Scan and log results**\n\n```clamscan -r -l [scan.log] [/path/to/dir]```\n\n**Exclude directory**\n\n```clamscan -r --exclude-dir=[backup] [/home]```"],["SYNOPSIS","**clamscan** [_options_] [_file_|_directory_...]"],["DESCRIPTION","**clamscan** is a standalone command-line virus scanner from the ClamAV antivirus suite. It scans files and directories for viruses, trojans, malware, and other threats using the ClamAV signature database.\n\nEach invocation loads the full virus database into memory, which makes it suitable for one-off scans but slower for repeated use. For high-volume or frequent scanning, the daemon-based **clamdscan** is preferred as it avoids the database reload overhead.\n\nclamscan supports recursive directory scanning, pattern-based file inclusion and exclusion, configurable size limits, and various actions for infected files including removal, quarantine (move), and copying. Scan results can be logged to a file for audit purposes."],["PARAMETERS","**-r**, **--recursive**\n> Scan directories recursively\n\n**-i**, **--infected**\n> Only print infected files\n\n**-o**, **--suppress-ok-results**\n> Don't print clean files\n\n**--remove**[=yes/no]\n> Remove infected files (dangerous; disabled by default)\n\n**--move**=_dir_\n> Move infected files to directory\n\n**--copy**=_dir_\n> Copy infected files to directory\n\n**-l** _file_, **--log**=_file_\n> Write scan report to file\n\n**-d** _path_, **--database**=_path_\n> Load virus signatures from file or directory\n\n**--exclude**=_REGEX_\n> Skip files matching the regex pattern\n\n**--exclude-dir**=_REGEX_\n> Skip directories matching the regex\n\n**--include**=_REGEX_\n> Only scan files matching the regex\n\n**--include-dir**=_REGEX_\n> Only enter directories matching the regex\n\n**--max-filesize**=_size_\n> Skip files larger than size\n\n**--max-scansize**=_size_\n> Maximum data scanned per container\n\n**--bell**\n> Sound an audible alert on detection\n\n**--detect-pua**[=yes/no]\n> Detect potentially unwanted applications\n\n**--quiet**\n> Only print error messages\n\n**--no-summary**\n> Suppress the final scan summary\n\n**-v**, **--verbose**\n> Verbose output\n\n**--version**\n> Print version information"],["DATABASE UPDATE","Update signatures before scanning:\n\n```sudo freshclam```"],["CAVEATS","--remove is dangerous due to false positives. Use --move or --copy instead. Single-threaded by default. For multi-threaded scanning, use clamdscan with clamd daemon."],["INSTALL","```apt: sudo apt install clamav```\n\n```dnf: sudo dnf install clamav```\n\n```pacman: sudo pacman -S clamav```\n\n```apk: sudo apk add clamav-scanner```\n\n```zypper: sudo zypper install clamav```\n\n```brew: brew install clamav```\n\n```nix: nix profile install nixpkgs#clamav```"],["SEE ALSO","[freshclam](/man/freshclam)(1), [clamdscan](/man/clamdscan)(1), [clamd](/man/clamd)(8)"]],"e":["freshclam","clamdscan","clamd"],"x":"clamscan [file]"},"cloudsploit":{"n":"cloudsploit","t":"Open-source cloud security posture management scanner","s":[["TAGLINE","Open-source cloud security posture management scanner"],["TLDR","**Run a scan with default settings**\n\n```cloudsploit scan```\n\n**Scan a specific cloud provider**\n\n```cloudsploit scan --cloud [aws]```\n\n**Scan with a specific compliance framework**\n\n```cloudsploit scan --compliance [hipaa]```\n\n**Output results as JSON**\n\n```cloudsploit scan --json [output.json]```\n\n**Run a specific plugin only**\n\n```cloudsploit scan --plugin [pluginName]```\n\n**Scan with a custom config file**\n\n```cloudsploit scan --config [config.js]```\n\n**Output results as CSV**\n\n```cloudsploit scan --csv [output.csv]```"],["SYNOPSIS","**cloudsploit** _command_ [_options_]"],["PARAMETERS","**scan**\n> Run security scan against cloud infrastructure.\n\n**--cloud** _provider_\n> Target cloud provider: aws, azure, gcp, oracle.\n\n**--compliance** _framework_\n> Check against a compliance framework (hipaa, pci, cis). CIS runs both Level 1 and Level 2 controls.\n\n**--plugin** _name_\n> Run only the specified plugin.\n\n**--json** _file_\n> Output results in JSON format to the specified file.\n\n**--csv** _file_\n> Output results in CSV format to the specified file.\n\n**--junit** _file_\n> Output results in JUnit XML format.\n\n**--config** _file_\n> Use a custom configuration file for credentials and settings.\n\n**--suppress** _plugins_\n> Suppress specific plugins from results.\n\n**--remediate** _plugins_\n> Attempt automatic remediation for the specified plugins. Takes a comma-separated list of plugin names."],["DESCRIPTION","**CloudSploit** is an open-source cloud security scanner that detects security risks and misconfigurations across AWS, Azure, GCP, Oracle Cloud, and GitHub. It checks for hundreds of security issues including public S3 buckets, overly permissive IAM policies, unencrypted databases, and exposed services.\n\nThe tool operates in two phases: a collection phase that queries cloud APIs to gather infrastructure metadata, and a scanning phase that analyzes the data to identify misconfigurations and security risks. Results are categorized by severity (OK, WARN, FAIL, UNKNOWN) with descriptions and remediation guidance.\n\nCloudSploit supports compliance mapping to frameworks like CIS Benchmarks, PCI-DSS, and HIPAA, helping organizations meet regulatory requirements. It can be integrated into CI/CD pipelines for continuous security monitoring."],["CAVEATS","Requires appropriate cloud credentials with read permissions configured in config.js or environment variables. Scanning large environments may take significant time and incur API costs. Some checks require specific permissions that may not be available in restricted accounts. Automated remediation should be tested in non-production environments first."],["HISTORY","CloudSploit was originally created by **CloudSploit Inc.**, which was acquired by **Aqua Security** in **2019**. First released around **2015**, it grew to support multiple cloud providers and became one of the foundational open-source tools in the cloud security posture management (CSPM) space. Aqua continues to maintain the open-source version while offering a commercial SaaS platform."],["SEE ALSO","[prowler](/man/prowler)(1)"]],"e":["prowler"],"x":"cloudsploit scan"},"devspace":{"n":"devspace","t":"Kubernetes developer tool with hot reloading","s":[["TAGLINE","Kubernetes developer tool with hot reloading"],["TLDR","**Initialize a project**\n\n```devspace init```\n\n**Deploy to Kubernetes**\n\n```devspace deploy```\n\n**Start development mode**\n\n```devspace dev```\n\n**Open terminal in container**\n\n```devspace enter```\n\n**View logs**\n\n```devspace logs```\n\n**Run a command in container**\n\n```devspace run [command]```\n\n**Purge deployment**\n\n```devspace purge```"],["SYNOPSIS","**devspace** _command_ [_options_]"],["SUBCOMMANDS","**init**\n> Initialize DevSpace configuration.\n\n**deploy**\n> Deploy the application.\n\n**dev**\n> Start development mode with hot reloading.\n\n**enter**\n> Open terminal in container.\n\n**logs**\n> View container logs.\n\n**run**\n> Run a predefined command.\n\n**purge**\n> Delete deployed resources.\n\n**ui**\n> Open DevSpace UI in browser."],["DESCRIPTION","**devspace** is a Kubernetes developer tool for building, deploying, and developing applications. It provides hot reloading, log streaming, and terminal access during development.\n\nThe tool simplifies Kubernetes development by automating common tasks like building container images, deploying manifests, and syncing code changes. DevSpace watches for local file changes and automatically syncs them to running containers, enabling rapid iteration without rebuilding images.\n\nConfiguration is defined in devspace.yaml, specifying images, deployments, dev configurations, and custom commands. The tool supports Helm charts, kubectl manifests, and Kustomize."],["CONFIGURATION","**devspace.yaml**\n> Main configuration file defining images, deployments, dev settings, and pipelines."],["CAVEATS","Requires kubectl and access to a Kubernetes cluster. Hot reload performance depends on file sync mechanism. Initial deployment may be slow for large images."],["INSTALL","```brew: brew install devspace```\n\n```nix: nix profile install nixpkgs#devspace```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [skaffold](/man/skaffold)(1)"]],"e":["kubectl","skaffold"],"x":"devspace init"},"dnf-config-manager":{"n":"dnf-config-manager","t":"manage DNF repositories and configuration","s":[["TAGLINE","manage DNF repositories and configuration"],["TLDR","**Add** repository from URL\n\n```dnf config-manager --add-repo=[repository_url]```\n\n**Print** current configuration\n\n```dnf config-manager --dump```\n\n**Enable** a repository\n\n```dnf config-manager --set-enabled [repository_id]```\n\n**Disable** repositories\n\n```dnf config-manager --set-disabled [repository_id1] [repository_id2]```\n\n**Set** configuration option\n\n```dnf config-manager --setopt=[option]=[value]```"],["SYNOPSIS","**dnf config-manager** [_options_]"],["DESCRIPTION","**dnf config-manager** provides command-line management of DNF repositories and configuration settings. It's part of dnf-plugins-core and offers an alternative to manually editing configuration files.\n\nThe tool can add repositories from URLs (creating .repo files automatically), enable or disable existing repositories, and modify DNF configuration options on the fly. Changes to repositories are persisted in /etc/yum.repos.d/ as .repo files. This is particularly useful for scripted repository management, adding third-party repositories, or temporarily adjusting configuration without editing files manually. The --dump option is helpful for troubleshooting by showing the complete effective configuration."],["PARAMETERS","**--add-repo** _url_\n> Add repository from URL\n\n**--dump**\n> Print current configuration\n\n**--set-enabled** _repo_\n> Enable repository\n\n**--set-disabled** _repo_\n> Disable repository\n\n**--setopt** _opt=val_\n> Set configuration option\n\n**--save**\n> Persist `--setopt` changes to the appropriate configuration file (by default changes are session-only).\n\n**--dump-variables**\n> Print the effective values of the built-in DNF variables (`$releasever`, `$basearch`, …).\n\n**--help-cmd**\n> Display help"],["CONFIGURATION","**/etc/yum.repos.d/*.repo**\n> Repository configuration files. Changes made by dnf config-manager persist here.\n\n**/etc/dnf/dnf.conf**\n> Main DNF configuration file. Options can be modified using --setopt."],["CAVEATS","Requires **dnf-plugins-core** on DNF 4. On DNF 5 (Fedora 41+) the plugin has been merged into the main binary and the subcommand is invoked as **dnf5 config-manager**; most flags carry over but **--set-enabled**/**--set-disabled** become **enable**/**disable** subcommands. Repository changes require root. Changes to `.repo` files persist in `/etc/yum.repos.d/`."],["SEE ALSO","[dnf](/man/dnf)(8), [dnf5](/man/dnf5)(8), [yum-config-manager](/man/yum-config-manager)(1), [yum](/man/yum)(8)"]],"e":["dnf","dnf5","yum-config-manager","yum"],"x":"dnf config-manager --add-repo=[repository_url]"},"docker-secret":{"n":"docker-secret","t":"manage sensitive data in Docker Swarm","s":[["TAGLINE","manage sensitive data in Docker Swarm"],["TLDR","**Create secret from file**\n\n```docker secret create [name] [file]```\n\n**Create secret from stdin**\n\n```echo \"[secret_value]\" | docker secret create [name] -```\n\n**List secrets**\n\n```docker secret ls```\n\n**Inspect a secret**\n\n```docker secret inspect [name]```\n\n**Remove a secret**\n\n```docker secret rm [name]```"],["SYNOPSIS","**docker** **secret** _command_ [_options_]"],["SUBCOMMANDS","**create**\n> Create a secret from file or STDIN.\n\n**ls**\n> List secrets.\n\n**inspect**\n> Display detailed information.\n\n**rm**\n> Remove secrets."],["DESCRIPTION","**docker secret** manages Swarm secrets, which are sensitive data like passwords, certificates, or API keys. Secrets are encrypted and only available to services that need them. The secret content itself is never exposed in inspect output, only metadata. Secrets are mounted into service containers as files in /run/secrets."],["CAVEATS","Only available in Swarm mode. Secrets cannot be updated; create a new version and rotate services."],["INSTALL","```apt: sudo apt install docker-cli```\n\n```dnf: sudo dnf install docker-cli```\n\n```pacman: sudo pacman -S docker```\n\n```apk: sudo apk add docker-cli```\n\n```zypper: sudo zypper install docker```\n\n```brew: brew install docker```\n\n```nix: nix profile install nixpkgs#docker```"],["SEE ALSO","[docker-swarm](/man/docker-swarm)(1), [docker-service](/man/docker-service)(1)"],["RESOURCES","```[Source code](https://github.com/docker/cli)```\n\n```[Documentation](https://docs.docker.com/reference/cli/docker/)```"]],"e":["docker-swarm","docker-service"],"x":"docker secret create [name] [file]"},"dolt-blame":{"n":"dolt-blame","t":"show the commit, author, and timestamp that last modified each row of a Dolt table","s":[["TAGLINE","show the commit, author, and timestamp that last modified each row of a Dolt table"],["TLDR","**Blame every row** of a table\n\n```dolt blame [table_name]```\n\n**Show blame by primary-key value** (filter after the fact)\n\n```dolt blame [table_name] | grep \"[key_value]\"```\n\n**Query blame via SQL** for custom columns/filtering\n\n```dolt sql -q \"SELECT * FROM dolt_blame_[table_name]\"```\n\n**Show only rows modified by an author**\n\n```dolt sql -q \"SELECT * FROM dolt_blame_[table_name] WHERE committer = '[alice]'\"```"],["SYNOPSIS","**dolt blame** _table_"],["PARAMETERS","_TABLE_\n> The table to blame. Rows are identified by their primary key."],["DESCRIPTION","**dolt blame** prints a row-per-primary-key table showing the timestamp, author, commit message, and commit hash of the last commit that modified that row. It is the row-level equivalent of `git blame`, adapted to a database.\n\nThe CLI form shows a fixed set of columns. For more flexible queries (joins with other system tables, filtering by commit range, custom projections) use the equivalent SQL system view `dolt_blame_$tablename` — its columns include every primary key of the table plus `commit_hash`, `committer`, `email`, `date`, and `message`."],["CAVEATS","Only reports the **most recent** modification per row — no full history. Schema changes are currently counted as a change to every row, so a single `ALTER TABLE` blames every row to that commit. Large tables with deep history can take a while; prefer the SQL view with `LIMIT`/`WHERE` in those cases."],["HISTORY","**dolt blame** is part of **Dolt**, the versioned SQL database (\"Git for data\") by **DoltHub**. It brings Git-style per-row provenance to relational tables."],["INSTALL","```pacman: sudo pacman -S dolt```\n\n```brew: brew install dolt```\n\n```nix: nix profile install nixpkgs#dolt```"],["SEE ALSO","[dolt](/man/dolt)(1), [dolt-sql](/man/dolt-sql)(1), [git-blame](/man/git-blame)(1)"]],"e":["dolt","dolt-sql","git-blame"],"x":"dolt blame [table_name]"},"dolt-branch":{"n":"dolt-branch","t":"List, create, or delete database branches","s":[["TAGLINE","List, create, or delete database branches"],["TLDR","**List local branches**\n\n```dolt branch```\n\n**List all branches** including remote-tracking branches\n\n```dolt branch -a```\n\n**Create a branch** from the current HEAD\n\n```dolt branch [name]```\n\n**Create a branch** from a specific start point\n\n```dolt branch [name] [start_point]```\n\n**Delete a branch**\n\n```dolt branch -d [name]```\n\n**Force delete** an unmerged branch\n\n```dolt branch -D [name]```\n\n**Rename the current branch**\n\n```dolt branch -m [new_name]```\n\n**Copy a branch**\n\n```dolt branch -c [old_name] [new_name]```\n\n**List remote-tracking branches**\n\n```dolt branch -r```\n\n**Show the current branch** name\n\n```dolt branch --show-current```\n\n**List branches with verbose** commit information\n\n```dolt branch -v```"],["SYNOPSIS","**dolt branch** [**--list**] [**-v**] [**-a**] [**-r**]\n**dolt branch** [**-f**] _branchname_ [_start-point_]\n**dolt branch** **-m** [**-f**] [_oldbranch_] _newbranch_\n**dolt branch** **-c** [**-f**] [_oldbranch_] _newbranch_\n**dolt branch** **-d** [**-f**] [**-r**] _branchname_..."],["PARAMETERS","**-d**, **--delete**\n> Delete a branch. The branch must be fully merged unless **-f** is also given.\n\n**-D**\n> Shortcut for **--delete --force**.\n\n**-m**, **--move**\n> Rename a branch.\n\n**-c**, **--copy**\n> Copy a branch to a new name.\n\n**-f**, **--force**\n> Reset a branch to a start point even if it already exists, force rename/copy even if the target name exists, or allow deleting an unmerged branch.\n\n**-r**, **--remote**\n> List or operate on remote-tracking branches.\n\n**-a**, **--all**\n> List both local and remote-tracking branches.\n\n**-v**, **--verbose**\n> Show commit hash and subject line for each branch. Use **-vv** to also show upstream branch name.\n\n**-t**, **--track**\n> Set up upstream tracking for a remote branch.\n\n**--show-current**\n> Print the name of the current active branch.\n\n**--list**\n> Explicitly enter list mode.\n\n_start-point_\n> A commit reference where the new branch should originate. Defaults to the current HEAD."],["DESCRIPTION","**dolt branch** lists, creates, or deletes branches in a Dolt database repository. Branches work like Git branches but for versioned databases, allowing parallel development of database schemas and data.\n\nWithout arguments, the command lists all local branches with an asterisk marking the current branch. Creating a branch captures the current database state as a starting point for divergent development. Note that creating a branch does not switch to it; use **dolt checkout** to switch branches.\n\nBranches can be used for testing schema changes, experimenting with data transformations, or maintaining multiple versions of a dataset."],["INSTALL","```pacman: sudo pacman -S dolt```\n\n```brew: brew install dolt```\n\n```nix: nix profile install nixpkgs#dolt```"],["SEE ALSO","[dolt-checkout](/man/dolt-checkout)(1), [dolt-merge](/man/dolt-merge)(1), [dolt-commit](/man/dolt-commit)(1), [dolt-status](/man/dolt-status)(1), [dolt](/man/dolt)(1)"]],"e":["dolt-checkout","dolt-merge","dolt-commit","dolt-status","dolt"],"x":"dolt branch"},"electron":{"n":"electron","t":"Cross-platform desktop apps with web technologies","s":[["TAGLINE","Cross-platform desktop apps with web technologies"],["TLDR","**Run Electron app**\n\n```electron [.]```\n\n**Run specific directory**\n\n```electron [path/to/app]```\n\n**Run with remote debugging**\n\n```electron --remote-debugging-port=[9222] [.]```\n\n**Show version**\n\n```electron --version```\n\n**Run with verbose logging**\n\n```electron --enable-logging [.]```\n\n**Disable GPU acceleration**\n\n```electron --disable-gpu [.]```"],["SYNOPSIS","**electron** [_options_] [_path_]"],["PARAMETERS","_path_\n> Path to app directory or package.json.\n\n**--version**\n> Show Electron version.\n\n**--remote-debugging-port** _port_\n> Enable remote debugging.\n\n**--enable-logging**\n> Enable logging to console.\n\n**--disable-gpu**\n> Disable GPU hardware acceleration.\n\n**--no-sandbox**\n> Disable sandbox (not recommended).\n\n**--inspect** _port_\n> Enable Node.js inspector.\n\n**--inspect-brk** _port_\n> Enable inspector and break on start."],["DESCRIPTION","**Electron** is a framework for building cross-platform desktop applications using web technologies (HTML, CSS, JavaScript). It embeds Chromium for rendering and Node.js for backend functionality.\n\nApplications are packaged with their own Electron runtime, enabling deployment without system dependencies. Popular Electron apps include VS Code, Slack, Discord, and Atom."],["PROJECT STRUCTURE","```\nmy-app/\n├── package.json    # Main entry point defined\n├── main.js         # Main process\n├── preload.js      # Preload script\n├── index.html      # Renderer content\n└── renderer.js     # Renderer process\n```"],["CAVEATS","Large application size due to bundled Chromium. High memory usage. Security requires proper configuration (context isolation, preload scripts). Updates require careful handling. Multiple processes (main and renderer)."],["HISTORY","Electron was originally developed by **GitHub** as **Atom Shell** for the Atom editor in **2013**. It was renamed to Electron in **2015** and released as open source. The project revolutionized desktop app development by enabling web developers to build native applications."],["INSTALL","```pacman: sudo pacman -S electron```\n\n```nix: nix profile install nixpkgs#electron```"],["SEE ALSO","[node](/man/node)(1), [npm](/man/npm)(1), [tauri](/man/tauri)(1)"]],"e":["node","npm","tauri"],"x":"electron [.]"},"flux-bootstrap":{"n":"flux-bootstrap","t":"Bootstrap Flux on a Kubernetes cluster using GitOps","s":[["TAGLINE","Bootstrap Flux on a Kubernetes cluster using GitOps"],["TLDR","**Bootstrap Flux** using a GitHub repository (personal account)\n\n```flux bootstrap github --owner [username] --repository [repo] --path [clusters/my-cluster] --personal```\n\n**Bootstrap using GitLab**\n\n```flux bootstrap gitlab --owner [group] --repository [repo] --path [clusters/my-cluster]```\n\n**Bootstrap from a generic Git repository** over SSH\n\n```flux bootstrap git --url ssh://git@example.com/repo.git --branch main --path ./clusters/my-cluster```"],["SYNOPSIS","**flux** **bootstrap** _provider_ [_options_]"],["DESCRIPTION","**flux bootstrap** installs Flux on a Kubernetes cluster by pushing the necessary manifests to a Git repository and configuring the cluster to sync from it. This establishes the GitOps control plane (source-controller, kustomize-controller, helm-controller, notification-controller, etc.).\n\nIt supports GitHub, GitLab, Gitea, Bitbucket Server, and generic Git repositories. The command creates the `flux-system` namespace, required secrets, and the initial GitRepository + Kustomization that keeps Flux itself up to date.\n\nAfter bootstrap, the cluster is managed declaratively from Git."],["PARAMETERS","Common options across providers:\n\n**--owner** _name_\n> GitHub/GitLab owner or organization.\n\n**--repository** _name_\n> Name of the repository.\n\n**--path** _path_\n> Path within the repository where manifests live (e.g. `clusters/my-cluster`).\n\n**--branch** _branch_\n> Git branch (default `main`).\n\n**--personal**\n> Use a personal access token instead of an organization app.\n\n**--private-key-file** _file_\n> Path to SSH private key.\n\n**--components** _list_\n> Comma-separated list of components to install (default includes source, kustomize, helm, and notification controllers).\n\n**--components-extra** _list_\n> Additional components (e.g. `image-reflector-controller,image-automation-controller`).\n\n**--token-auth**\n> Use a personal access token instead of an SSH deploy key.\n\nOther provider-specific options exist (see `flux bootstrap --help`)."],["CAVEATS","Bootstrap requires a working `kubectl` context with cluster admin permissions and appropriate Git credentials. The repository must already exist (or be created by the command when using supported providers)."],["INSTALL","```apk: sudo apk add flux```\n\n```brew: brew install flux```\n\n```nix: nix profile install nixpkgs#flux```"],["SEE ALSO","[flux](/man/flux)(1), [flux-check](/man/flux-check)(1), [flux-create](/man/flux-create)(1)"],["RESOURCES","```[Source code](https://github.com/fluxcd/flux2)```\n\n```[Documentation](https://fluxcd.io/flux/cmd/flux_bootstrap/)```"]],"e":["flux","flux-check","flux-create"],"x":"flux bootstrap github --owner [username] --repository [repo] --path [clusters/my-cluster] --personal"},"foo2lava-wrapper":{"n":"foo2lava-wrapper","t":"PostScript/PDF to LAVAFLOW wrapper script","s":[["TAGLINE","PostScript/PDF to LAVAFLOW wrapper script"],["TLDR","**Print PostScript file**\n\n```foo2lava-wrapper [file.ps]```\n\n**Set paper size**\n\n```foo2lava-wrapper -p [a4] [file.ps]```\n\n**Color printing**\n\n```foo2lava-wrapper -c [file.ps]```\n\n**Print multiple copies**\n\n```foo2lava-wrapper -n [3] [file.ps]```"],["SYNOPSIS","**foo2lava-wrapper** [_options_] [_files_...]"],["PARAMETERS","_FILES_\n> PostScript or PDF files to print.\n\n**-p** _PAPER_\n> Paper size (letter, a4, legal).\n\n**-r** _DPI_\n> Resolution.\n\n**-c**\n> Color printing.\n\n**-n** _COPIES_\n> Number of copies.\n\n**-m** _MEDIA_\n> Media type.\n\n**--help**\n> Display help information."],["DESCRIPTION","**foo2lava-wrapper** converts PostScript/PDF documents to LAVAFLOW format for Konica Minolta magicolor printers. It chains Ghostscript rasterization with foo2lava encoding.\n\nThe wrapper provides a simplified interface for printing, handling format conversion automatically. It translates standard print options to the underlying tools.\n\nfoo2lava-wrapper enables direct command-line printing to supported Konica Minolta printers."],["CAVEATS","Requires Ghostscript. Limited to supported printer models. Color output quality varies."],["HISTORY","foo2lava-wrapper is part of the **foo2zjs** driver collection, providing easy document printing for Konica Minolta printers using the LAVAFLOW protocol."],["INSTALL","```apt: sudo apt install printer-driver-foo2zjs```\n\n```dnf: sudo dnf install foo2lava```"],["SEE ALSO","[foo2lava](/man/foo2lava)(1), [gs](/man/gs)(1), [lpr](/man/lpr)(1)"],["RESOURCES","```[Source code](https://github.com/OpenPrinting/foo2zjs)```"]],"e":["foo2lava","gs","lpr"],"x":"foo2lava-wrapper [file.ps]"},"fping":{"n":"fping","t":"parallel ping utility for multiple hosts","s":[["TAGLINE","parallel ping utility for multiple hosts"],["TLDR","**Ping multiple hosts**\n\n```fping [host1] [host2] [host3]```\n\n**Ping hosts from a file**\n\n```fping -f [path/to/hosts.txt]```\n\n**Ping a range of IP addresses**\n\n```fping -g [192.168.1.1] [192.168.1.254]```\n\n**Ping a CIDR range**\n\n```fping -g [192.168.1.0/24]```\n\n**Show only alive hosts**\n\n```fping -a [host1] [host2]```\n\n**Show only unreachable hosts**\n\n```fping -u [host1] [host2]```\n\n**Ping with statistics** (like ping)\n\n```fping -s [host1] [host2]```\n\n**Continuous ping** with interval\n\n```fping -l -p [1000] [host]```"],["SYNOPSIS","**fping** [_-aAcdDeglmnqQrstuvV_] [_-b size_] [_-B backoff_] [_-c count_] [_-f file_] [_-g addr/mask_] [_-i interval_] [_-p period_] [_-r retry_] [_-S source_] [_-t timeout_] [_targets_]"],["PARAMETERS","**-a**\n> Show only alive (reachable) hosts.\n\n**-u**\n> Show only unreachable hosts.\n\n**-g** _addr/mask_ or _start end_\n> Generate target list from CIDR or range.\n\n**-f** _file_\n> Read targets from file (- for stdin).\n\n**-c** _count_\n> Number of pings to send per target.\n\n**-l**\n> Loop: keep pinging indefinitely.\n\n**-p** _period_\n> Interval between pings to same target (ms).\n\n**-i** _interval_\n> Interval between pings to different targets (ms).\n\n**-t** _timeout_\n> Individual ping timeout in milliseconds.\n\n**-r** _retry_\n> Number of retries for unreachable hosts.\n\n**-s**\n> Print cumulative statistics at end.\n\n**-q**\n> Quiet: only show summary.\n\n**-e**\n> Show elapsed time for replies.\n\n**-A**\n> Show targets by IP address.\n\n**-D**\n> Show timestamps before each line.\n\n**-S** _source_\n> Set source address.\n\n**-b** _size_\n> Ping packet size in bytes."],["DESCRIPTION","**fping** is designed for scripting and monitoring, offering significant advantages over the standard ping command for checking multiple hosts. It can ping many hosts in parallel and report results in formats suitable for parsing.\n\nUnlike ping which waits for each host to respond before continuing, fping sends probes in a round-robin fashion to all targets, then waits for responses. This parallelism makes it vastly faster when checking many hosts.\n\nThe tool is commonly used in network monitoring scripts, host discovery, and availability checking. The **-g** option for generating IP ranges enables quick subnet scans. Exit codes indicate overall reachability status, useful in scripts.\n\nOutput modes range from verbose per-ping information to quiet mode showing only summaries. The alive-only (-a) and unreachable-only (-u) modes simplify parsing when you only need specific results.\n\nfping can read targets from files or stdin, enabling integration with other tools. For continuous monitoring, loop mode (-l) with custom intervals provides ongoing status updates."],["CAVEATS","Requires root/sudo for raw socket access on some systems (or setuid installation). Very fast scanning may be detected as aggressive by network security. High probe rates can stress network infrastructure. Some hosts may block ICMP."],["HISTORY","**fping** was written by Roland Schemers around **1992** at Stanford University to address limitations in the standard ping for network monitoring. It has become a standard tool for network administrators and is included in most Linux distribution repositories."],["INSTALL","```apt: sudo apt install fping```\n\n```dnf: sudo dnf install fping```\n\n```pacman: sudo pacman -S fping```\n\n```apk: sudo apk add fping```\n\n```zypper: sudo zypper install fping```\n\n```brew: brew install fping```\n\n```nix: nix profile install nixpkgs#fping```"],["SEE ALSO","[ping](/man/ping)(8), [nmap](/man/nmap)(1), [hping3](/man/hping3)(8), [mtr](/man/mtr)(8)"],["RESOURCES","```[Source code](https://github.com/schweikert/fping)```\n\n```[Homepage](https://fping.org/)```"]],"e":["ping","nmap","hping3","mtr"],"x":"fping [host1] [host2] [host3]"},"fzf":{"n":"fzf","t":"general-purpose command-line fuzzy finder","s":[["TAGLINE","general-purpose command-line fuzzy finder"],["TLDR","**Interactive file finder**\n\n```find . -type f | fzf```\n\n**Select with preview**\n\n```fzf --preview 'cat {}'```\n\n**Multiple selection**\n\n```fzf -m```\n\n**Filter command history**\n\n```history | fzf```\n\n**Fuzzy find and** edit\n\n```vim $(fzf)```\n\n**Exact match** mode\n\n```fzf -e```\n\n**Non-interactive filter** (fuzzy grep)\n\n```fzf -f \"[query]\"```\n\n**Auto-select single** match\n\n```fzf --select-1 -q \"[query]\"```"],["SYNOPSIS","**fzf** [_options_]"],["PARAMETERS","**-m**, **--multi**\n> Enable multiple selection.\n\n**--preview** _CMD_\n> Preview command.\n\n**-q** _QUERY_, **--query** _QUERY_\n> Initial search query.\n\n**--height** _HEIGHT_\n> Display height (rows or percentage).\n\n**--reverse**\n> Layout from top.\n\n**--border**\n> Draw border.\n\n**-e**, **--exact**\n> Enable exact-match mode instead of fuzzy.\n\n**-f** _QUERY_, **--filter** _QUERY_\n> Non-interactive filter mode (fuzzy grep).\n\n**-1**, **--select-1**\n> Auto-select if only one match.\n\n**-0**, **--exit-0**\n> Exit immediately if no match.\n\n**--ansi**\n> Enable ANSI color code processing.\n\n**-d** _DELIM_, **--delimiter** _DELIM_\n> Field delimiter regex for --nth.\n\n**-n** _N_, **--nth** _N_\n> Limit search scope to specified fields.\n\n**--layout** _LAYOUT_\n> Display layout: default, reverse, reverse-list.\n\n**--info** _STYLE_\n> Info display style: default, right, hidden, inline.\n\n**--header** _STR_\n> Display a sticky header string.\n\n**--bind** _KEYS_\n> Custom key bindings.\n\n**--tac**\n> Reverse input order.\n\n**--no-sort**\n> Preserve original input order.\n\n**--cycle**\n> Enable cyclic scrolling.\n\n**--print-query**\n> Print the query as the first output line.\n\n**--help**\n> Display help information."],["DESCRIPTION","**fzf** is a general-purpose fuzzy finder that filters any list interactively. It accepts input via stdin and outputs selected items, enabling powerful shell workflows.\n\nThe tool provides instant feedback while typing, fuzzy matching for typo tolerance, and preview windows for context. Shell integration adds keybindings for history, files, and directories.\n\nfzf transforms command-line workflows with interactive selection for files, processes, git branches, and more."],["SHELL INTEGRATION","**CTRL-T**: Paste selected file paths onto command line\n**CTRL-R**: Search command history\n**ALT-C**: cd into selected directory"],["ENVIRONMENT VARIABLES","**FZF_DEFAULT_COMMAND**\n> Command to run when input is a terminal (replaces default find).\n\n**FZF_DEFAULT_OPTS**\n> Default options applied to every fzf invocation."],["CAVEATS","Requires terminal with full capabilities. Shell integration requires sourcing the fzf setup script. Large inputs may be slow."],["HISTORY","fzf was created by **Junegunn Choi** in 2013. Written in Go for performance, it became essential tooling for command-line productivity, spawning integrations across shells, editors, and tools."],["INSTALL","```apt: sudo apt install fzf```\n\n```dnf: sudo dnf install fzf```\n\n```pacman: sudo pacman -S fzf```\n\n```apk: sudo apk add fzf```\n\n```zypper: sudo zypper install fzf```\n\n```brew: brew install fzf```\n\n```nix: nix profile install nixpkgs#fzf```"],["SEE ALSO","[sk](/man/sk)(1), [peco](/man/peco)(1), [fd](/man/fd)(1)"]],"e":["sk","peco","fd"],"x":"find . -type f | fzf"},"git-annotate":{"n":"git-annotate","t":"annotate file lines with commit information","s":[["TAGLINE","annotate file lines with commit information"],["TLDR","**Show** line-by-line authorship\n\n```git annotate [path/to/file]```\n\n**Annotate** the file as of a given revision\n\n```git annotate [revision] -- [path/to/file]```\n\n**Annotate** only a range of lines\n\n```git annotate -L [10],[20] [path/to/file]```\n\n**Ignore** whitespace-only changes\n\n```git annotate -w [path/to/file]```\n\n**Show** the line's original commit through moves and copies\n\n```git annotate -C -C [path/to/file]```\n\n**Show** the email address instead of the author name\n\n```git annotate -e [path/to/file]```"],["SYNOPSIS","**git annotate** [_options_] [_rev-opts_] [_rev_] [**--**] _file_"],["PARAMETERS","**-L** _start_,_end_\n> Annotate only the given line range. Also accepts **:**_funcname_ to select a function, and forms such as **-L 10,+5**.\n\n**-w**\n> Ignore whitespace when comparing versions, so re-indentation does not reassign blame.\n\n**-M**[_num_]\n> Detect lines moved or copied within the same file, and blame the original commit.\n\n**-C**[_num_]\n> Detect lines moved or copied from other files in the same commit. Repeat (**-C -C**) to widen the search to files the commit did not touch.\n\n**-e**, **--show-email**\n> Show the author's email address instead of their name.\n\n**-l**\n> Show the long, unabbreviated commit hash.\n\n**-t**\n> Show raw timestamps instead of human-readable dates.\n\n**-f**, **--show-name**\n> Show the filename of the originating commit.\n\n**-n**, **--show-number**\n> Show the line number in the originating commit.\n\n**-s**\n> Suppress the author name and timestamp.\n\n**--porcelain**\n> Machine-readable output for scripts.\n\n**--line-porcelain**\n> Like **--porcelain**, but repeat the full commit information for every line.\n\n**--reverse** _start_.._end_\n> Walk history forwards instead of backwards, showing the last revision in which each line still existed.\n\n**--ignore-rev** _rev_\n> Ignore a revision when assigning blame, e.g. a bulk reformatting commit.\n\n**--ignore-revs-file** _file_\n> Ignore all revisions listed in a file."],["DESCRIPTION","**git annotate** annotates each line of a file with information about the commit that last introduced it. Given a revision, it annotates the file as it existed at that point instead of in the working tree.\n\nIt is the same machinery as **git blame**, and the only real difference is the output format. **git annotate** exists for backward compatibility with existing scripts, and to offer a familiar command name to people arriving from CVS and Subversion, where **annotate** is the conventional spelling.\n\nThe value of either spelling is archaeology: finding the change that introduced a bug, understanding why a line looks the way it does, or working out who to ask. **-C** and **-M** matter for that, because without them a file that was renamed or a block that was moved shows the refactoring commit rather than the commit that wrote the code.\n\n**--ignore-rev** and **--ignore-revs-file** address the common frustration of a repository-wide reformat burying real history behind one mechanical commit."],["CAVEATS","Blame attributes each line to the commit that last **touched** it, which is not the same as the commit that caused the behaviour you are chasing. A whitespace fix, a rename, or a code move will claim lines it did not really author unless you pass **-w**, **-M** or **-C**.\n\nBecause **git annotate** exists only for compatibility, new work should use **git blame**, which has the better-supported output format and is what the git documentation treats as canonical.\n\nAnnotating a large file across deep history is slow, since git reconstructs and diffs many revisions. Restrict the work with **-L** when you only care about a few lines.\n\nThe default output format is not stable enough to parse; use **--porcelain** or **--line-porcelain** in scripts."],["HISTORY","**git annotate** was added early in git's life, in **2005**, as a Perl script providing a CVS-style name and output format for what **git blame** already did. It was later reimplemented in C and is now a thin front-end over the same code as **git blame**."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-blame](/man/git-blame)(1), [git-log](/man/git-log)(1), [git-diff](/man/git-diff)(1), [git-bisect](/man/git-bisect)(1)"],["RESOURCES","```[Source code](https://github.com/git/git)```\n\n```[Homepage](https://git-scm.com/)```\n\n```[Documentation](https://git-scm.com/docs/git-annotate)```"]],"e":["git-blame","git-log","git-diff","git-bisect"],"x":"git annotate [path/to/file]"},"git-force-clone":{"n":"git-force-clone","t":"force-clone a repository, replacing the target directory if it exists","s":[["TAGLINE","force-clone a repository, replacing the target directory if it exists"],["TLDR","**Force clone** into a directory, removing any existing contents first\n\n```git force-clone [url] [directory]```\n\n**Force clone at a specific branch**\n\n```git force-clone -b [branch] [url] [directory]```\n\n**Force clone and reset to the remote head** even if the dir is a checkout\n\n```git force-clone [https://github.com/owner/repo.git] [existing-dir]```"],["SYNOPSIS","**git force-clone** [**-b** _BRANCH_] _URL_ _DIRECTORY_"],["PARAMETERS","_URL_\n> Repository URL (https, ssh, git, or local path).\n\n_DIRECTORY_\n> Target directory. If it already exists and contains a clone of the same repo, its contents are reset to match the remote. If it exists but is a different repo (or random files), it is removed and re-cloned.\n\n**-b** _BRANCH_\n> Check out _BRANCH_ after cloning.\n\n**--help**\n> Show help."],["DESCRIPTION","**git force-clone** from the **git-extras** suite guarantees the target directory ends up as a fresh, clean clone of the given URL. It is the \"just give me what's on the remote\" hammer for deployment scripts:\n\n- If the directory does not exist, it clones normally.\n- If the directory is already a clone of the same URL, it fetches and hard-resets the working tree to match the remote, discarding any local changes.\n- If the directory exists but is a different repository (or contains unrelated files), it removes the directory and re-clones."],["CAVEATS","**Destructive.** Uncommitted changes in the target directory are lost without confirmation. Local branches not pushed to the remote will be removed if the directory is re-cloned. Use with care, especially in interactive contexts — prefer `git fetch` + `git reset --hard` when you only need to resync and want to keep other local state."],["HISTORY","**git force-clone** is part of **git-extras**, the long-running collection of git helpers by **TJ Holowaychuk** and contributors."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-clone](/man/git-clone)(1), [git-reset](/man/git-reset)(1), [git-fetch](/man/git-fetch)(1), [git-extras](/man/git-extras)(1)"]],"e":["git-clone","git-reset","git-fetch","git-extras"],"x":"git force-clone [url] [directory]"},"git-obliterate":{"n":"git-obliterate","t":"Remove file from entire history","s":[["TAGLINE","Remove file from entire history"],["TLDR","**Remove file from entire history**\n\n```git obliterate [filename]```\n\n**Remove multiple files**\n\n```git obliterate [file1] [file2]```\n\n**Limit the rewrite to a revision range**\n\n```git obliterate [filename] -- [since]..[until]```"],["SYNOPSIS","**git** **obliterate** _filename_... [_--_ _rev-list-args_]"],["DESCRIPTION","**git obliterate** completely removes one or more files from Git history. This git-extras command runs `git filter-branch --index-filter 'git rm -r --cached <files> --ignore-unmatch' --prune-empty --tag-name-filter cat` over `--all` refs (or over a revision range given after `--`), erasing the files from every rewritten commit while keeping tags pointed at their (now rewritten) commits.\n\nIt is useful for removing accidentally committed secrets, credentials, or large files that should never have been tracked. It does not repack the repository afterward; old objects remain until you run garbage collection."],["CAVEATS","This rewrites history and changes all commit hashes. Coordinate with collaborators before using on shared branches. Run `git reflog expire --expire=now --all && git gc --prune=now --aggressive` afterward to actually reclaim disk space and drop the old blobs."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-filter-branch](/man/git-filter-branch)(1), [git-gc](/man/git-gc)(1), [git-extras](/man/git-extras)(1)"],["RESOURCES","```[Source code](https://github.com/tj/git-extras)```\n\n```[Documentation](https://github.com/tj/git-extras/blob/master/Commands.md#git-obliterate)```"]],"e":["git-filter-branch","git-gc","git-extras"],"x":"git obliterate [filename]"},"git-quiltimport":{"n":"git-quiltimport","t":"Import quilt patches to commits","s":[["TAGLINE","Import quilt patches to commits"],["TLDR","**Import quilt patches**\n\n```git quiltimport```\n\n**Import from specific directory**\n\n```git quiltimport --patches [patches/]```\n\n**Dry run**\n\n```git quiltimport -n```"],["SYNOPSIS","**git quiltimport** [_options_]"],["PARAMETERS","**--patches** _DIR_\n> Patches directory. Defaults to patches or $QUILT_PATCHES.\n\n**--series** _FILE_\n> Series file location. Defaults to <patches>/series or $QUILT_SERIES.\n\n**-n**, **--dry-run**\n> Show what would be imported.\n\n**--author** _AUTHOR_\n> Default author.\n\n**--keep-non-patch**\n> Pass -b flag to git mailinfo to keep non-patch content in the commit message.\n\n**--help**\n> Display help information."],["DESCRIPTION","**git quiltimport** imports a quilt patchset into Git as a series of commits. Quilt is a patch management system, and this command reads the patches directory and series file, creating one commit per patch in order.\n\nThe command enables migration from quilt-based development workflows to Git, which was particularly common in Linux distribution packaging before Git became the standard."],["CAVEATS","Requires properly formatted quilt patches. Author info may need specification. One-way import."],["HISTORY","git quiltimport supports migration from **quilt**, a popular patch management tool used before git became widespread, particularly in Linux distribution development."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-am](/man/git-am)(1), [git-apply](/man/git-apply)(1), [quilt](/man/quilt)(1)"]],"e":["git-am","git-apply","quilt"],"x":"git quiltimport"},"gitwatch":{"n":"gitwatch","t":"Automatically commit changes when files are modified","s":[["TAGLINE","Automatically commit changes when files are modified"],["TLDR","**Watch directory and auto-commit**\n\n```gitwatch [path]```\n\n**Watch with custom message**\n\n```gitwatch -m \"[Auto commit]\" [path]```\n\n**Watch and push**\n\n```gitwatch -r [remote] -b [branch] [path]```\n\n**Watch specific branch**\n\n```gitwatch -b [branch] [path]```"],["SYNOPSIS","**gitwatch** [_options_] _path_"],["PARAMETERS","**-s** _seconds_\n> Delay before committing after a change is detected (default: 2).\n\n**-d** _format_\n> Timestamp format string used in the default commit message (see `man date`).\n\n**-r** _remote_\n> Remote to push to after each commit (default: no push).\n\n**-R**\n> Run `git pull --rebase` before pushing.\n\n**-b** _branch_\n> Branch to push commits to.\n\n**-g** _path_\n> Location of the `.git` directory (auto-detected by default).\n\n**-m** _message_\n> Commit message template; `%d` is replaced with the timestamp.\n\n**-c** _command_\n> Command whose output is used as the commit message instead of `-m`.\n\n**-C**\n> Pipe the list of changed files to the `-c` command via stdin.\n\n**-l** _lines_, **-L** _lines_\n> Include changed lines in the commit message, colored (`-l`) or uncolored (`-L`); `0` includes all lines.\n\n**-e** _events_\n> inotifywait events to monitor for changes.\n\n**-x** _pattern_\n> Exclude paths matching pattern from being watched.\n\n**-f**\n> Commit any pending changes immediately on startup.\n\n**-M**\n> Skip auto-committing while a git merge is in progress.\n\n**-v**\n> Verbose/debug output.\n\n**-h**\n> Display help message."],["DESCRIPTION","**gitwatch** watches a directory and automatically commits changes whenever files are modified. It uses inotifywait (on Linux, via inotify-tools) or fswatch (on macOS/BSD) to detect file changes in real time, then waits a short delay before committing so rapid successive edits are batched into a single commit.\n\nThe tool is useful for automatic version control of notes, configuration files, or any directory where you want every change tracked without manual commits, and can optionally push each commit to a remote."],["CAVEATS","Requires **inotify-tools** on Linux or **fswatch** on macOS/BSD to be installed. Only tracks changes gitwatch itself observes while running; it does not replay changes made while it was stopped."],["INSTALL","```brew: brew install gitwatch```\n\n```nix: nix profile install nixpkgs#gitwatch```"],["SEE ALSO","[inotifywait](/man/inotifywait)(1), [git](/man/git)(1)"],["RESOURCES","```[Source code](https://github.com/gitwatch/gitwatch)```"]],"e":["inotifywait","git"],"x":"gitwatch [path]"},"gml2gv":{"n":"gml2gv","t":"GML to Graphviz DOT format converter","s":[["TAGLINE","GML to Graphviz DOT format converter"],["TLDR","**Convert GML to DOT**\n\n```gml2gv [input.gml] -o [output.gv]```\n\n**Convert to stdout**\n\n```gml2gv [input.gml]```"],["SYNOPSIS","**gml2gv** [_options_] [_files_]"],["PARAMETERS","_FILES_\n> GML input files. Reads from stdin if omitted.\n\n**-o** _FILE_\n> Output file. Writes to stdout if omitted.\n\n**-g** _NAME_\n> Name for the generated graph. Subsequent graphs get an appended integer.\n\n**-v**\n> Verbose mode.\n\n**-?**\n> Display usage information and exit."],["DESCRIPTION","**gml2gv** converts graphs from GML (Graph Modelling Language) format to DOT format used by Graphviz. It enables using GML graphs with Graphviz visualization tools.\n\nGML is a portable format for graph description. The converter preserves graph structure for rendering with dot, neato, or other Graphviz layout engines."],["CAVEATS","Part of Graphviz. Some GML features may not have DOT equivalents. Check attribute preservation."],["HISTORY","gml2gv is part of **Graphviz**, providing format conversion between common graph description languages."],["INSTALL","```apt: sudo apt install graphviz```\n\n```dnf: sudo dnf install graphviz```\n\n```pacman: sudo pacman -S graphviz```\n\n```apk: sudo apk add graphviz```\n\n```zypper: sudo zypper install graphviz```\n\n```brew: brew install graphviz```\n\n```nix: nix profile install nixpkgs#graphviz```"],["SEE ALSO","[dot](/man/dot)(1), [gv2gml](/man/gv2gml)(1), [graphml2gv](/man/graphml2gv)(1)"],["RESOURCES","```[Source code](https://gitlab.com/graphviz/graphviz)```\n\n```[Homepage](https://graphviz.org/docs/cli/gml2gv/)```"]],"e":["dot","gv2gml","graphml2gv"],"x":"gml2gv [input.gml] -o [output.gv]"},"gtrash":{"n":"gtrash","t":"Featureful trash CLI manager and safe rm alternative","s":[["TAGLINE","Featureful trash CLI manager and safe rm alternative"],["TLDR","**Move files to trash** instead of deleting\n\n```gtrash put [path/to/file1] [path/to/file2]```\n\n**List files in the trash**\n\n```gtrash find```\n\n**Restore files interactively** with a TUI\n\n```gtrash restore```\n\n**Permanently delete files** matching a pattern from trash\n\n```gtrash find --rm \"[pattern]\"```\n\n**Show trash summary** with sizes\n\n```gtrash summary```\n\n**Clean up old files** from trash\n\n```gtrash prune --day [30]```"],["SYNOPSIS","**gtrash** _command_ [_options_] [_arguments_]"],["DESCRIPTION","**gtrash** is a featureful trash CLI manager that serves as a safer alternative to **rm**. Instead of permanently deleting files with the unlink syscall, it moves them to the system trash using the rename syscall, enabling easy restoration. It follows the **FreeDesktop.org Trash specification**, making it compatible with desktop trash implementations and other tools like **gio trash** and **trash-cli**.\n\nKey features include directory size caching for fast browsing, restoration of co-deleted files, an interactive TUI for selecting files to restore, and support for external drives with per-volume trash directories."],["PARAMETERS","**put**\n> Move files to trash\n\n**find**\n> List files in trash (supports --rm to permanently delete matches)\n\n**restore**\n> Interactively restore files from trash using TUI\n\n**summary**\n> Show trash size summary\n\n**prune**\n> Remove old files from trash\n\n**--day** _N_\n> Used with prune to specify age threshold in days\n\n**--rm**\n> Used with find to permanently delete matching files"],["CAVEATS","Files moved to trash still consume disk space until pruned or permanently deleted. The rename syscall used for trashing only works within the same filesystem; cross-filesystem moves require copying. Permanently deleting with **--rm** is irreversible, similar to rm."],["HISTORY","**gtrash** was created by **umlx5h** as a modern, feature-rich alternative to both **rm** and existing trash managers. It is written in **Go** and distributed as a single static binary."],["INSTALL","```brew: brew install gtrash```\n\n```nix: nix profile install nixpkgs#gtrash```"],["SEE ALSO","[rm](/man/rm)(1), [trash-cli](/man/trash-cli)(1)"]],"e":["rm","trash-cli"],"x":"gtrash put [path/to/file1] [path/to/file2]"},"gvfs-tree":{"n":"gvfs-tree","t":"displays directory contents in tree format using GVFS","s":[["TAGLINE","displays directory contents in tree format using GVFS"],["TLDR","**Show directory tree**\n\n```gvfs-tree [directory]```\n\n**Show remote tree**\n\n```gvfs-tree smb://server/share/```\n\n**Show hidden files**\n\n```gvfs-tree -h [directory]```\n\n**Follow symlinks**\n\n```gvfs-tree -l [directory]```"],["SYNOPSIS","**gvfs-tree** [_options_] _location_"],["PARAMETERS","_LOCATION_\n> Directory path or URI.\n\n**-h**, **--hidden**\n> Show hidden files.\n\n**-l**, **--follow-symlinks**\n> Follow symbolic links, mounts and shortcuts.\n\n**-?**, **--help**\n> Print a short help text and exit."],["DESCRIPTION","**gvfs-tree** displays directory contents in tree format using GVFS. It shows hierarchical structure of local and remote directories.\n\nThe tool visualizes directory trees across GVFS backends, providing a unified view of local and networked filesystems."],["CAVEATS","Part of the gvfs-bin command-line tools, deprecated in gvfs 1.31 (2016) and removed entirely in gvfs 1.38 (2018) in favor of the unified **gio** command (`gio tree`). Modern distributions no longer ship gvfs-tree. Large or deeply nested remote trees may be slow to enumerate."],["HISTORY","**gvfs-tree** was part of GVFS's original gvfs-bin utilities, introduced for GNOME alongside GVFS in **2008** (GNOME 2.22). It has since been superseded by **gio tree**."],["SEE ALSO","[gio](/man/gio)(1), [tree](/man/tree)(1), [gvfs-ls](/man/gvfs-ls)(1)"],["RESOURCES","```[Source code](https://gitlab.gnome.org/GNOME/gvfs)```\n\n```[Homepage](https://wiki.gnome.org/Projects/gvfs)```"]],"e":["gio","tree","gvfs-ls"],"x":"gvfs-tree [directory]"},"gvpack":{"n":"gvpack","t":"Merge and pack disjoint graphs","s":[["TAGLINE","Merge and pack disjoint graphs"],["TLDR","**Merge laid-out graphs**\n\n```gvpack [graph1.dot] [graph2.dot] > [merged.dot]```\n\n**Pack at graph level** (prevents interleaving)\n\n```gvpack -g [graph1.dot] [graph2.dot] > [merged.dot]```\n\n**Pack at node level** (ignoring clusters)\n\n```gvpack -n [graph1.dot] [graph2.dot] > [merged.dot]```\n\n**Combine without packing**\n\n```gvpack -u [graph1.dot] [graph2.dot] > [merged.dot]```\n\n**Arrange in array layout with margin**\n\n```gvpack -array -m [20] [graph1.dot] [graph2.dot] > [merged.dot]```\n\n**Set graph attributes on output**\n\n```gvpack -G \"label=[My Graph]\" [graph1.dot] [graph2.dot] > [merged.dot]```\n\n**Full pipeline: decompose, layout, pack, render**\n\n```ccomps -x [graph.dot] | dot | gvpack | neato -s -n2 -Tpng -o [output.png]```"],["SYNOPSIS","**gvpack** [_options_] [_files_]"],["PARAMETERS","**-g**\n> Pack at graph level. Uses more space but prevents parts of one graph from occurring between parts of another.\n\n**-n**\n> Pack at node level, ignoring clusters.\n\n**-u**\n> Combine without packing. Just merge into a single graph.\n\n**-array**[_flags_][_n_]\n> Arrange graphs in an array layout. Optional flags: c (column-major), t/b/l/r (align top/bottom/left/right), u (use sortv attribute). Optional _n_ sets columns (or rows with c).\n\n**-m** _margin_\n> Packing margin in points around each part.\n\n**-o** _file_\n> Output to file instead of stdout.\n\n**-s** _name_\n> Root graph name (default: root).\n\n**-G** _name=value_\n> Add attributes to the resulting union graph.\n\n**-v**\n> Verbose mode."],["DESCRIPTION","**gvpack** reads in a stream of graphs, combines them into a single layout, and produces a single graph serving as the union of the input graphs. Input must be in dot format with layout information (produced by a layout engine like dot or neato without the -T flag). By default, packing is at cluster level, preventing graph parts from overlapping. Output can be rendered with **neato -s -n2** and the desired output format."],["CAVEATS","All input graphs must be either all directed or all undirected. Nodes with the same names across graphs are automatically renamed to avoid conflicts."],["INSTALL","```apt: sudo apt install graphviz```\n\n```dnf: sudo dnf install graphviz```\n\n```pacman: sudo pacman -S graphviz```\n\n```apk: sudo apk add graphviz```\n\n```zypper: sudo zypper install graphviz```\n\n```brew: brew install graphviz```\n\n```nix: nix profile install nixpkgs#graphviz```"],["SEE ALSO","[dot](/man/dot)(1), [neato](/man/neato)(1), [ccomps](/man/ccomps)(1), [fdp](/man/fdp)(1), [twopi](/man/twopi)(1)"]],"e":["dot","neato","ccomps","fdp","twopi"],"x":"gvpack [graph1.dot] [graph2.dot] > [merged.dot]"},"hd":{"n":"hd","t":"displays file contents in hexadecimal format","s":[["TAGLINE","displays file contents in hexadecimal format"],["TLDR","**Display file in hex**\n\n```hd [file]```\n\n**Show first 100 bytes**\n\n```hd -n 100 [file]```\n\n**Skip first 50 bytes**\n\n```hd -s 50 [file]```\n\n**One-byte octal display**\n\n```hd -b [file]```\n\n**Canonical hex+ASCII display**\n\n```hd -C [file]```"],["SYNOPSIS","**hd** [_options_] [_file_]"],["PARAMETERS","_FILE_\n> File to display.\n\n**-n** _LENGTH_\n> Number of bytes to display.\n\n**-s** _OFFSET_\n> Skip offset bytes.\n\n**-b**\n> One-byte octal display.\n\n**-C**\n> Canonical hex+ASCII display.\n\n**-v**\n> Display all data (no suppression).\n\n**--help**\n> Display help information."],["DESCRIPTION","**hd** displays file contents in hexadecimal format. Invoking **hexdump** as **hd** is equivalent to running it with **-C**, showing offset, hex bytes, and the ASCII representation side by side.\n\nThe tool is useful for examining binary files, analyzing data formats, and debugging. It formats output in readable columns."],["CAVEATS","Non-printable bytes are shown as dots in the ASCII column. Large files produce extensive output; pipe through a pager or use **-n**/**-s** to limit the range."],["HISTORY","**hd** originated as a BSD convenience alias for **hexdump**. On current Linux systems it's provided by the **util-linux** package (which absorbed hexdump from the older bsdmainutils package around 2017), so both **hexdump** and **hd** invoke the same binary."],["INSTALL","```apt: sudo apt install bsdextrautils```"],["SEE ALSO","[hexdump](/man/hexdump)(1), [xxd](/man/xxd)(1), [od](/man/od)(1)"],["RESOURCES","```[Source code](https://github.com/util-linux/util-linux/blob/master/text-utils/hexdump.c)```\n```[Documentation](https://man7.org/linux/man-pages/man1/hexdump.1.html)```"]],"e":["hexdump","xxd","od"],"x":"hd [file]"},"hostname":{"n":"hostname","t":"displays or sets the system's host name","s":[["TAGLINE","displays or sets the system's host name"],["TLDR","**Display the current hostname**\n\n```hostname```\n\n**Display the fully qualified domain name** (FQDN)\n\n```hostname -f```\n\n**Display the short hostname** (without domain)\n\n```hostname -s```\n\n**Display all IP addresses** of the host\n\n```hostname -I```\n\n**Display the DNS domain name**\n\n```hostname -d```\n\n**Set the hostname** (temporary, until reboot)\n\n```sudo hostname [new_hostname]```\n\n**Set hostname from a file**\n\n```sudo hostname -F [/etc/hostname]```\n\n**Display the NIS/YP domain name**\n\n```hostname -y```"],["SYNOPSIS","**hostname** [_options_] [_name_]\n\n**hostname** [-f|--fqdn]\n\n**hostname** [-I|--all-ip-addresses]"],["PARAMETERS","**-a**, **--alias**\n> Display the alias name of the host\n\n**-d**, **--domain**\n> Display the DNS domain name\n\n**-f**, **--fqdn**, **--long**\n> Display the Fully Qualified Domain Name (FQDN)\n\n**-A**, **--all-fqdns**\n> Display all FQDNs of the machine\n\n**-i**, **--ip-address**\n> Display the IP address for the hostname\n\n**-I**, **--all-ip-addresses**\n> Display all network addresses of the host (excludes loopback and IPv6 link-local)\n\n**-s**, **--short**\n> Display the short hostname (up to the first dot)\n\n**-y**, **--yp**, **--nis**\n> Display or set the NIS/YP domain name\n\n**-F**, **--file** _FILE_\n> Read hostname from the specified file\n\n**-b**, **--boot**\n> Always set a hostname; if none specified, use default\n\n**-v**, **--verbose**\n> Be verbose about actions taken\n\n**-h**, **--help**\n> Display help message\n\n**-V**, **--version**\n> Display version information"],["DESCRIPTION","**hostname** displays or sets the system's host name. The host name is used by many network programs to identify the machine. The command can show various name components including the short name, FQDN, domain name, and IP addresses.\n\nWhen called without arguments, it displays the current hostname as returned by the gethostname system call. When called with an argument, it sets the hostname (requires root privileges). Related commands **domainname**, **dnsdomainname**, and **nisdomainname** are often symbolic links to hostname.\n\nThe **-I** option is particularly useful for scripts as it lists all configured IP addresses without relying on DNS resolution. The FQDN (**-f**) combines the short hostname with the DNS domain."],["CAVEATS","Setting the hostname with this command is temporary and does not persist across reboots. For permanent changes, edit **/etc/hostname** or use **hostnamectl** on systemd-based systems. The **-i** option depends on DNS resolution and may fail if the hostname cannot be resolved. Changes require root privileges."],["HISTORY","The hostname command has been part of Unix systems since the early BSD releases in the 1980s. It evolved from simple hostname display to include domain name handling as TCP/IP networking became standard. On modern Linux systems, **hostnamectl** from systemd provides additional functionality for persistent hostname configuration including static, transient, and pretty hostnames."],["INSTALL","```apt: sudo apt install hostname```\n\n```dnf: sudo dnf install hostname```\n\n```pacman: sudo pacman -S net-tools```\n\n```apk: sudo apk add net-tools```\n\n```zypper: sudo zypper install hostname```\n\n```brew: brew install net-tools```\n\n```nix: nix profile install nixpkgs#hostname```"],["SEE ALSO","[hostnamectl](/man/hostnamectl)(1), [domainname](/man/domainname)(1)"]],"e":["hostnamectl","domainname"],"x":"hostname"},"interdiff":{"n":"interdiff","t":"compares two unified diff files and generates a diff representing the changes","s":[["TAGLINE","compares two unified diff files and generates a diff representing the changes"],["TLDR","**Show differences between two patches**\n\n```interdiff [old.patch] [new.patch]```\n\n**Create a patch to convert** from one version to another\n\n```interdiff [version1.patch] [version2.patch] > [upgrade.patch]```\n\n**Strip leading path components** from patch filenames\n\n```interdiff -p 1 [old.patch] [new.patch]```\n\n**Display additional lines of context**\n\n```interdiff -U [3] [old.patch] [new.patch]```\n\n**Compare gzip/bzip2 compressed patches**\n\n```interdiff -z [old.patch.gz] [new.patch.gz]```"],["SYNOPSIS","**interdiff** [_-U N_] [_-p N_] [_-d PAT_] [_-q_] [_-i_] [_-w_] [_-b_] [_-B_] [_-z_] _patch1_ _patch2_"],["PARAMETERS","**-U** _N_, **--unified=**_N_\n> Display N lines of context (default: 3).\n\n**-p** _N_, **--strip-match=**_N_\n> Ignore N leading pathname components when matching filenames.\n\n**-d** _PAT_, **--drop-context=**_PAT_\n> Exclude context for files whose names match pattern PAT.\n\n**-i**, **--ignore-case**\n> Treat upper and lower case as identical when comparing.\n\n**-w**, **--ignore-all-space**\n> Ignore all whitespace when comparing.\n\n**-b**, **--ignore-space-change**\n> Ignore changes in the amount of whitespace.\n\n**-B**, **--ignore-blank-lines**\n> Ignore changes that consist only of blank-line additions/removals.\n\n**-z**, **--decompress**\n> Automatically decompress .gz and .bz2 patches.\n\n**-q**, **--quiet**\n> Reduce warnings in output.\n\n**--no-revert-omitted**\n> Do not revert portions of the first patch that are omitted from the second.\n\n**--help**\n> Display usage information.\n\n**--version**\n> Show version number."],["DESCRIPTION","**interdiff** compares two unified diff files (patches) and generates a diff representing the changes between them. This is useful for understanding what changed between two versions of a patch or for creating incremental patches.\n\nGiven patch1 that transforms A to B, and patch2 that transforms A to C, interdiff produces a patch that transforms B to C. This helps when reviewing patch revisions or creating upgrade paths between software versions."],["CAVEATS","Both input patches must be in unified diff format. Context diffs are not supported. The patches must apply to the same base files for meaningful results. Complex patches with significant structural changes may produce confusing output."],["HISTORY","**interdiff** is part of the **patchutils** package, written by Tim Waugh and first released around **2001**. The patchutils suite provides tools for manipulating patch files, addressing common needs in software development and distribution workflows."],["INSTALL","```apt: sudo apt install patchutils```\n\n```dnf: sudo dnf install patchutils```\n\n```pacman: sudo pacman -S patchutils```\n\n```apk: sudo apk add patchutils```\n\n```zypper: sudo zypper install patchutils```\n\n```brew: brew install patchutils```\n\n```nix: nix profile install nixpkgs#patchutils```"],["SEE ALSO","[combinediff](/man/combinediff)(1), [diff](/man/diff)(1), [patch](/man/patch)(1)"]],"e":["combinediff","diff","patch"],"x":"interdiff [old.patch] [new.patch]"},"irqbalance":{"n":"irqbalance","t":"distributes hardware interrupts across CPUs","s":[["TAGLINE","distributes hardware interrupts across CPUs"],["TLDR","**Start irqbalance**\n\n```sudo irqbalance```\n\n**Run in foreground**\n\n```sudo irqbalance --foreground```\n\n**One-shot balance**\n\n```sudo irqbalance --oneshot```\n\n**Debug mode**\n\n```sudo irqbalance --debug```\n\n**Ban specific IRQ**\n\n```sudo irqbalance --banirq=[irq_num]```"],["SYNOPSIS","**irqbalance** [_options_]"],["PARAMETERS","**--foreground**\n> Run in foreground.\n\n**--oneshot**\n> Balance once and exit.\n\n**--debug**\n> Enable debug output.\n\n**--banirq** _IRQ_\n> Exclude IRQ from balancing.\n\n**--policyscript** _SCRIPT_\n> Custom placement policy.\n\n**--pid** _FILE_\n> PID file location.\n\n**--help**\n> Display help information."],["DESCRIPTION","**irqbalance** distributes hardware interrupts across CPUs. It optimizes interrupt handling for multi-processor systems.\n\nThe daemon monitors IRQ patterns and migrates them for better load distribution. It improves performance on multi-core systems."],["CAVEATS","Runs as daemon. May conflict with manual IRQ affinity. Linux-specific."],["HISTORY","irqbalance was created to automatically balance IRQ loads across CPUs, replacing manual smp_affinity configuration."],["INSTALL","```apt: sudo apt install irqbalance```\n\n```dnf: sudo dnf install irqbalance```\n\n```pacman: sudo pacman -S irqbalance```\n\n```apk: sudo apk add irqbalance```\n\n```zypper: sudo zypper install irqbalance```\n\n```nix: nix profile install nixpkgs#irqbalance```"],["SEE ALSO","[cpupower](/man/cpupower)(1), [taskset](/man/taskset)(1)"]],"e":["cpupower","taskset"],"x":"sudo irqbalance"},"kpackagetool5":{"n":"kpackagetool5","t":"manages KDE Plasma packages including plasmoids, themes, wallpapers","s":[["TAGLINE","manages KDE Plasma packages including plasmoids, themes, wallpapers"],["TLDR","**List** all known package types\n\n```kpackagetool5 --list-types```\n\n**Install** a package from directory\n\n```kpackagetool5 -t [package_type] -i [path/to/directory]```\n\n**Update** an installed package\n\n```kpackagetool5 -t [package_type] -u [path/to/directory]```\n\n**List** installed plasmoids\n\n```kpackagetool5 -t Plasma/Applet -l```\n\nList **global** plasmoids (all users)\n\n```kpackagetool5 -t Plasma/Applet -l -g```\n\n**Remove** a plasmoid by name\n\n```kpackagetool5 -t Plasma/Applet -r \"[name]\"```"],["SYNOPSIS","**kpackagetool5** [_options_]"],["PARAMETERS","**--list-types**\n> List all available package types\n\n**-t**, **--type** _TYPE_\n> Package type (Plasma/Applet, Plasma/Theme, etc.)\n\n**-i**, **--install** _PATH_\n> Install package from directory\n\n**-u**, **--upgrade** _PATH_\n> Update existing package\n\n**-r**, **--remove** _NAME_\n> Remove installed package\n\n**-l**, **--list**\n> List installed packages\n\n**-g**, **--global**\n> Operate on global (system-wide) packages"],["DESCRIPTION","**kpackagetool5** manages KDE Plasma packages including plasmoids (widgets), themes, wallpapers, and other Plasma components. It handles installation, removal, and listing of packages.\n\nPackage types include Plasma/Applet (widgets), Plasma/Theme, Plasma/Wallpaper, Plasma/LookAndFeel, and more. User packages are stored in ~/.local/share/plasma/."],["CAVEATS","KDE 5 specific; KDE 6 uses kpackagetool6. Some packages may require Plasma restart to take effect. Global installation requires root privileges."],["HISTORY","kpackagetool5 is part of KDE Frameworks 5, providing package management for Plasma's modular component system. It enables users to extend Plasma with community-created content."],["INSTALL","```apt: sudo apt install kpackagetool5```\n\n```pacman: sudo pacman -S kpackage5```\n\n```apk: sudo apk add kpackage5```"],["SEE ALSO","[kpackagetool6](/man/kpackagetool6)(1)"]],"e":["kpackagetool6"],"x":"kpackagetool5 --list-types"},"let":{"n":"let","t":"bash built-in for arithmetic evaluation","s":[["TAGLINE","bash built-in for arithmetic evaluation"],["TLDR","**Arithmetic assignment**\n\n```let \"x = 5 + 3\"```\n\n**Increment variable**\n\n```let \"count++\"```\n\n**Multiple expressions**\n\n```let \"a = 1\" \"b = 2\" \"c = a + b\"```\n\n**Comparison (exit code)**\n\n```let \"5 > 3\"```\n\n**Modulo operation**\n\n```let \"result = 10 % 3\"```"],["SYNOPSIS","**let** _expression_..."],["PARAMETERS","_EXPRESSION_\n> Arithmetic expression(s).\n\nOperators:\n> +, -, *, /, %, ** (power)\n> ++, -- (increment/decrement)\n> ==, !=, <, >, <=, >=\n> &&, ||, !"],["DESCRIPTION","**let** is a Bash (and ksh) built-in that evaluates one or more arithmetic expressions. Each expression is evaluated using the same rules as `$(( ... ))`: integer math, C-style operators, and shell variable references without the leading `$`.\n\nThe exit status is **0** if the value of the **last** evaluated expression is non-zero, and **1** if it is zero. This makes `let` usable in `if`/`while` conditions but is the inverse of typical command exit semantics — a successful arithmetic result of 0 (e.g., `let \"x = 0\"`) reports failure."],["CAVEATS","Bash/ksh built-in; not available in POSIX `sh` or `dash`. Integer arithmetic only — use `bc` or `awk` for floating point. The `(( ... ))` arithmetic command is generally preferred in modern Bash because it does not require quoting and has cleaner exit semantics. Returns exit code 1 when the final expression evaluates to 0, which can trigger `set -e` exits unexpectedly."],["HISTORY","let is a **Bash** built-in command for arithmetic evaluation, similar to expr but more powerful."],["SEE ALSO","[bash](/man/bash)(1), [expr](/man/expr)(1), [bc](/man/bc)(1)"]],"e":["bash","expr","bc"],"x":"let \"x = 5 + 3\""},"mafft":{"n":"mafft","t":"high-performance multiple sequence alignment program","s":[["TAGLINE","high-performance multiple sequence alignment program"],["TLDR","**Align sequences (auto strategy)**\n\n```mafft --auto [input.fasta] > [aligned.fasta]```\n\n**Fast alignment**\n\n```mafft --retree 1 [input.fasta] > [aligned.fasta]```\n\n**Accurate alignment**\n\n```mafft --maxiterate 1000 --localpair [input.fasta] > [aligned.fasta]```\n\n**Use multiple threads**\n\n```mafft --thread [8] --auto [input.fasta] > [aligned.fasta]```\n\n**Add sequences to existing alignment**\n\n```mafft --add [new.fasta] [existing.fasta] > [combined.fasta]```"],["SYNOPSIS","**mafft** [_options_] _input_ > _output_"],["PARAMETERS","**--auto**\n> Auto-select strategy.\n\n**--maxiterate** _n_\n> Number of iterations.\n\n**--localpair**\n> Use L-INS-i (accurate).\n\n**--globalpair**\n> Use G-INS-i.\n\n**--thread** _n_\n> Number of threads.\n\n**--add** _file_\n> Add to alignment.\n\n**--retree** _n_\n> Number of tree-building iterations in progressive alignment. 1 is fast, 2 is default.\n\n**--reorder**\n> Reorder output sequences by similarity.\n\n**--adjustdirection**\n> Automatically detect and reverse-complement input sequences if needed.\n\n**--quiet**\n> Suppress progress messages and warnings."],["DESCRIPTION","**MAFFT** (Multiple Alignment using Fast Fourier Transform) is a high-performance multiple sequence alignment program. It offers various algorithms balancing speed and accuracy.\n\nMAFFT is widely used in bioinformatics for aligning DNA, RNA, and protein sequences."],["STRATEGIES","```\nFFT-NS-1  - Fast, suitable for large datasets (--retree 1)\nFFT-NS-2  - Default progressive method (--retree 2)\nFFT-NS-i  - Iterative refinement (--maxiterate >0)\nL-INS-i   - Most accurate, local alignment, <200 sequences (--localpair)\nG-INS-i   - Global alignment (--globalpair)\nE-INS-i   - For sequences with long unalignable regions (--genafpair)\n```"],["CAVEATS","Memory scales with sequence count. Very long sequences may need adjustments. Auto mode selects strategy based on input size."],["HISTORY","MAFFT was developed by **Kazutaka Katoh** and colleagues, first published in **2002**. It's one of the most cited alignment tools in bioinformatics."],["INSTALL","```apt: sudo apt install mafft```\n\n```brew: brew install mafft```\n\n```nix: nix profile install nixpkgs#mafft```"],["SEE ALSO","[samtools](/man/samtools)(1), [bedtools](/man/bedtools)(1)"]],"e":["samtools","bedtools"],"x":"mafft --auto [input.fasta] > [aligned.fasta]"},"mmcli":{"n":"mmcli","t":"command-line interface for ModemManager, a DBus-powered daemon that provides","s":[["TAGLINE","command-line interface for ModemManager, a DBus-powered daemon that provides"],["TLDR","**List available modems**\n\n```mmcli --list-modems```\n\n**Print information** about a modem\n\n```mmcli --modem=[modem_id]```\n\n**Enable** a modem\n\n```mmcli --modem=[modem_id] --enable```\n\n**Disable** a modem\n\n```mmcli --modem=[modem_id] --disable```\n\n**List SMS messages** on the modem\n\n```sudo mmcli --modem=[modem_id] --messaging-list-sms```\n\n**Delete SMS message**\n\n```sudo mmcli --modem=[modem_id] --messaging-delete-sms=[sms_path]```\n\n**Monitor modem status** changes\n\n```mmcli --monitor-modems```"],["SYNOPSIS","**mmcli** [**--list-modems**] [**--modem** _id_] [**--enable**|**--disable**] [**--messaging-list-sms**] [_options_]"],["PARAMETERS","**-L, --list-modems**\n> List all available modems\n\n**-m, --modem _id_**\n> Specify modem by index or DBus path\n\n**-M, --monitor-modems**\n> Monitor modem status changes\n\n**-e, --enable**\n> Enable the specified modem\n\n**-d, --disable**\n> Disable the specified modem\n\n**-r, --reset**\n> Reset the modem\n\n**--factory-reset _code_**\n> Reset modem to factory state with unlock code\n\n**--command _at_command_**\n> Send raw AT command to modem\n\n**--messaging-list-sms**\n> List SMS messages on modem\n\n**--messaging-create-sms _properties_**\n> Create new SMS message\n\n**--messaging-delete-sms _sms_**\n> Delete SMS message\n\n**-S, --scan-modems**\n> Scan for new modems\n\n**--output-json**\n> Output in JSON format\n\n**--output-keyvalue**\n> Output as key-value pairs\n\n**-v, --verbose**\n> Enable verbose output"],["DESCRIPTION","**mmcli** is a command-line interface for ModemManager, a DBus-powered daemon that provides unified control of mobile broadband modems on Linux. It supports various modem types including USB modems, embedded cellular modules, and phone tethering.\n\nThe tool can enable/disable modems, manage connections, send/receive SMS, control GPS functionality, scan for networks, and perform SIM operations. It communicates with ModemManager through DBus."],["CAVEATS","Some operations require root privileges, particularly SMS and connection management. Modem indices may change across reboots or device reconnections; use DBus paths for stable identification. Factory reset requires carrier-specific unlock codes and erases all modem configuration."],["HISTORY","**mmcli** is part of **ModemManager**, developed by the freedesktop.org community. ModemManager provides a unified high-level API for mobile broadband modems, abstracting differences between vendor implementations. It is commonly used with NetworkManager for mobile connectivity."],["INSTALL","```apt: sudo apt install modemmanager```\n\n```pacman: sudo pacman -S modemmanager```\n\n```apk: sudo apk add modemmanager```\n\n```nix: nix profile install nixpkgs#modemmanager```"],["SEE ALSO","[NetworkManager](/man/NetworkManager)(8), [nmcli](/man/nmcli)(1)"]],"e":["NetworkManager","nmcli"],"x":"mmcli --list-modems"},"oeis-tui":{"n":"oeis-tui","t":"TUI and CLI for browsing the On-Line Encyclopedia of Integer Sequences","s":[["TAGLINE","TUI and CLI for browsing the On-Line Encyclopedia of Integer Sequences"],["TLDR","**Launch the OEIS browser**\n\n```oeis-tui```\n\n**Search for a sequence by keyword**\n\n```oeis-tui search \"[keyword]\"```\n\n**Fetch a specific sequence**\n\n```oeis-tui get [A000045]```"],["SYNOPSIS","**oeis-tui** [_command_] [_options_]"],["DESCRIPTION","**oeis-tui** is a TUI and CLI for browsing the On-Line Encyclopedia of Integer Sequences (OEIS) in the terminal. It supports searching by ID, keyword, author, or partial sequences, with paginated browsing and detail tabs for overview, formulas, and more.\n\nThe CLI provides UNIX-friendly output with multiple formats, suitable for piping and integration with other tools."],["CAVEATS","Requires an internet connection to query the OEIS database."],["HISTORY","**oeis-tui** was created by **hako** and is written in **Go**."],["INSTALL","```zypper: sudo zypper install oeis-tui```"],["SEE ALSO","[bc](/man/bc)(1), [numbat](/man/numbat)(1)"]],"e":["bc","numbat"],"x":"oeis-tui"},"openssl-pkey":{"n":"openssl-pkey","t":"general-purpose key processing tool","s":[["TAGLINE","general-purpose key processing tool"],["TLDR","**View private key details**\n\n```openssl pkey -in [private.key] -text -noout```\n\n**Extract public key**\n\n```openssl pkey -in [private.key] -pubout -out [public.key]```\n\n**Convert to DER format**\n\n```openssl pkey -in [private.key] -outform DER -out [private.der]```\n\n**Encrypt private key**\n\n```openssl pkey -in [private.key] -aes256 -out [encrypted.key]```\n\n**Remove encryption**\n\n```openssl pkey -in [encrypted.key] -out [decrypted.key]```\n\n**Check key validity**\n\n```openssl pkey -in [private.key] -check```"],["SYNOPSIS","**openssl pkey** [_options_] [-in _file_] [-out _file_]"],["PARAMETERS","**-in** _file_\n> Input file.\n\n**-out** _file_\n> Output file.\n\n**-inform** _format_\n> Input format (PEM, DER).\n\n**-outform** _format_\n> Output format.\n\n**-pubout**\n> Output public key.\n\n**-text**\n> Print text form.\n\n**-noout**\n> Don't output key.\n\n**-check**\n> Check key.\n\n**-aes256**\n> Encrypt output."],["DESCRIPTION","**openssl pkey** is a general-purpose key processing tool. It can convert between formats, extract public keys, encrypt/decrypt private keys, and display key information.\n\nWorks with RSA, EC, ED25519, and other key types."],["KEY FORMATS","```\nPEM - Base64 encoded (-----BEGIN...)\nDER - Binary format\n```"],["CAVEATS","-nodes or no encryption outputs unprotected key. Always protect private keys."],["HISTORY","The pkey command provides unified key handling across different algorithms in OpenSSL."],["INSTALL","```apt: sudo apt install openssl```\n\n```dnf: sudo dnf install openssl```\n\n```pacman: sudo pacman -S openssl```\n\n```apk: sudo apk add openssl```\n\n```zypper: sudo zypper install openssl```\n\n```brew: brew install openssl```\n\n```nix: nix profile install nixpkgs#openssl```"],["SEE ALSO","[openssl-genpkey](/man/openssl-genpkey)(1), [openssl-rsa](/man/openssl-rsa)(1)"]],"e":["openssl-genpkey","openssl-rsa"],"x":"openssl pkey -in [private.key] -text -noout"},"picard":{"n":"picard","t":"musicBrainz's music tagger application","s":[["TAGLINE","musicBrainz's music tagger application"],["TLDR","**Start MusicBrainz Picard**\n\n```picard```\n\n**Open files**\n\n```picard [file1.mp3] [file2.flac]```\n\n**Open directory**\n\n```picard [/path/to/music/]```\n\n**Show version**\n\n```picard --version```\n\n**Debug mode**\n\n```picard -d```"],["SYNOPSIS","**picard** [_-d_] [_-c config_] [_-N_] [_options_] [_files_]"],["PARAMETERS","**-d**, **--debug**\n> Enable debug mode.\n\n**-c** _FILE_, **--config-file** _FILE_\n> Use specific config file.\n\n**-N**, **--no-restore**\n> Don't restore saved window state.\n\n**-M**, **--no-player**\n> Disable internal player.\n\n**-P**, **--no-plugins**\n> Disable plugins.\n\n**-s** _PLUGIN_, **--stand-alone-instance** _PLUGIN_\n> Run plugin standalone.\n\n**--version**\n> Show version.\n\n**--help**\n> Show help."],["DESCRIPTION","**picard** is MusicBrainz's music tagger application. It identifies music files using audio fingerprints and tags them with accurate metadata.\n\nThe application matches music against the MusicBrainz database, the largest open music encyclopedia. Fingerprinting identifies tracks even without existing tags.\n\nCluster mode groups files by album. Lookup queries MusicBrainz for matches. Scan uses AcoustID fingerprints for identification.\n\nCover art is downloaded automatically from various sources. Multiple artwork providers ensure good coverage.\n\nPlugins extend functionality: additional tagging features, format support, and integration with other services.\n\nThe tagger handles various formats: MP3, FLAC, OGG, M4A, WMA, and more. Original files are modified in place or renamed according to patterns."],["CAVEATS","Requires internet for lookups. Matching accuracy depends on MusicBrainz coverage. Large collections take time to process. Some obscure releases may not be found."],["HISTORY","**MusicBrainz Picard** has been developed by the MusicBrainz community since **2003**. Named after the Star Trek character, it succeeded earlier tagging tools to become the official MusicBrainz tagger."],["INSTALL","```apt: sudo apt install picard```\n\n```dnf: sudo dnf install picard```\n\n```pacman: sudo pacman -S picard```\n\n```apk: sudo apk add picard```\n\n```zypper: sudo zypper install picard```\n\n```nix: nix profile install nixpkgs#picard```"],["SEE ALSO","[beets](/man/beets)(1), [kid3](/man/kid3)(1), [easytag](/man/easytag)(1), [mid3v2](/man/mid3v2)(1)"]],"e":["beets","kid3","easytag","mid3v2"],"x":"picard"},"podman-pull":{"n":"podman-pull","t":"Pull container images from registries","s":[["TAGLINE","Pull container images from registries"],["TLDR","**Pull image from Docker Hub**\n\n```podman pull [image:tag]```\n\n**Pull from specific registry**\n\n```podman pull [registry.example.com/image:tag]```\n\n**Pull all tags**\n\n```podman pull --all-tags [image]```\n\n**Pull specific platform image**\n\n```podman pull --platform [linux/arm64] [image]```\n\n**Pull without TLS verification**\n\n```podman pull --tls-verify=false [image]```"],["SYNOPSIS","**podman** **pull** [_options_] _image_"],["PARAMETERS","**--all-tags**\n> Pull all tagged images.\n\n**--tls-verify** _bool_\n> Verify TLS certificates.\n\n**--creds** _user:pass_\n> Registry credentials.\n\n**--quiet**\n> Suppress output.\n\n**--arch** _arch_\n> Override the architecture of the image to be pulled.\n\n**--os** _os_\n> Override the OS of the image to be pulled.\n\n**--platform** _os/arch_\n> Specify platform for image selection (e.g., linux/amd64). Conflicts with --arch and --os.\n\n**--variant** _variant_\n> Override the architecture variant (e.g., arm/v7).\n\n**--retry** _count_\n> Number of times to retry in case of failure.\n\n**--decryption-key** _key[:passphrase]_\n> Key for decryption of encrypted images.\n\n**--authfile** _path_\n> Path to the authentication file."],["DESCRIPTION","**podman pull** downloads container images from registries. Supports Docker Hub, Quay.io, and private registries. Images are stored locally for creating containers.\n\nThe pull policy defaults to **always**. Other policies are: **missing** (pull only if not local), **never** (use local only), and **newer** (pull if registry image is newer)."],["INSTALL","```apt: sudo apt install podman```\n\n```dnf: sudo dnf install podman```\n\n```pacman: sudo pacman -S podman```\n\n```apk: sudo apk add podman```\n\n```zypper: sudo zypper install podman```\n\n```brew: brew install podman```\n\n```nix: nix profile install nixpkgs#podman```"],["SEE ALSO","[podman](/man/podman)(1), [podman-image](/man/podman-image)(1), [podman-images](/man/podman-images)(1), [podman-run](/man/podman-run)(1)"]],"e":["podman","podman-image","podman-images","podman-run"],"x":"podman pull [image:tag]"},"poetry-version":{"n":"poetry-version","t":"Manage project version numbers","s":[["TAGLINE","Manage project version numbers"],["TLDR","**Show current version**\n\n```poetry version```\n\n**Bump major version**\n\n```poetry version major```\n\n**Bump minor version**\n\n```poetry version minor```\n\n**Bump patch version**\n\n```poetry version patch```\n\n**Set specific version**\n\n```poetry version [1.2.3]```"],["SYNOPSIS","**poetry version** [_options_] [_version_]"],["PARAMETERS","_VERSION_\n> Version or bump rule.\n\n**major**\n> Bump major version.\n\n**minor**\n> Bump minor version.\n\n**patch**\n> Bump patch version.\n\n**--short**\n> Only output version.\n\n**--dry-run**\n> Don't modify files."],["DESCRIPTION","**poetry version** displays or updates the project version in **pyproject.toml**. Without arguments, it shows the current version. With a bump rule (**major**, **minor**, **patch**, **premajor**, **preminor**, **prepatch**, **prerelease**) or an explicit version string, it updates the version accordingly.\n\nThe **--short** flag outputs only the version number without the project name. Use **--dry-run** to preview the version change without modifying the file."],["CAVEATS","Only updates pyproject.toml. Does not create git tags."],["HISTORY","poetry version provides **version management** for Poetry projects."],["INSTALL","```dnf: sudo dnf install poetry```\n\n```apk: sudo apk add poetry```\n\n```brew: brew install poetry```\n\n```nix: nix profile install nixpkgs#poetry```"],["SEE ALSO","[poetry](/man/poetry)(1), [semver](/man/semver)(1)"]],"e":["poetry","semver"],"x":"poetry version"},"ppdc":{"n":"ppdc","t":"Compile CUPS PPD printer driver files","s":[["TAGLINE","Compile CUPS PPD printer driver files"],["TLDR","**Compile PPD files**\n\n```ppdc [driver.drv]```\n\n**Compile to specific directory**\n\n```ppdc -d [output_dir] [driver.drv]```\n\n**Verbose output**\n\n```ppdc -v [driver.drv]```"],["SYNOPSIS","**ppdc** [_options_] _source_"],["PARAMETERS","_SOURCE_\n> Driver source file.\n\n**-d** _DIR_\n> Output directory.\n\n**-v**\n> Verbose output.\n\n**-I** _DIR_\n> Include directory."],["DESCRIPTION","**ppdc** compiles CUPS printer driver source files (.drv) into PPD (PostScript Printer Description) files. PPD files describe a printer's capabilities including supported paper sizes, resolutions, color modes, and optional features, and are used by CUPS to generate appropriate print job data.\n\nThe driver source format provides a compact way to define multiple printer models in a single file with shared options and model-specific overrides. The compiled PPD files can be placed in the CUPS model directory for automatic printer detection and setup."],["CAVEATS","CUPS specific. PPD files for printer support."],["HISTORY","ppdc is part of **CUPS** for PPD file compilation."],["INSTALL","```apt: sudo apt install cups-ppdc```\n\n```dnf: sudo dnf install cups-client```\n\n```apk: sudo apk add cups-client```\n\n```zypper: sudo zypper install cups-client```"],["SEE ALSO","[ppdhtml](/man/ppdhtml)(1), [ppdi](/man/ppdi)(1), [cups](/man/cups)(1)"]],"e":["ppdhtml","ppdi","cups"],"x":"ppdc [driver.drv]"},"ppmtoapplevol":{"n":"ppmtoapplevol","t":"Convert PPM to Apple volume label format","s":[["TAGLINE","Convert PPM to Apple volume label format"],["TLDR","**Convert PPM to Apple volume label**\n\n```ppmtoapplevol [input.ppm] > [output.vol]```"],["SYNOPSIS","**ppmtoapplevol** [_file_]"],["DESCRIPTION","**ppmtoapplevol** converts PPM images to Apple volume label format used for custom disk icons on Macintosh. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[ppmtopict](/man/ppmtopict)(1), [macptopbm](/man/macptopbm)(1)"]],"e":["ppmtopict","macptopbm"],"x":"ppmtoapplevol [input.ppm] > [output.vol]"},"ppt":{"n":"ppt","t":"Display text as ASCII paper tape art","s":[["TAGLINE","Display text as ASCII paper tape art"],["TLDR","**Convert to paper tape format**\n\n```ppt [text]```\n\n**Convert file**\n\n```cat [file.txt] | ppt```\n\n**Display paper tape**\n\n```echo \"Hello\" | ppt```"],["SYNOPSIS","**ppt** [_text_]"],["PARAMETERS","_TEXT_\n> Text to convert."],["DESCRIPTION","**ppt** converts text input into an ASCII art representation of punched paper tape, the storage medium used by early computers and teleprinters. Each character is displayed as a row of holes (dots) corresponding to its binary encoding.\n\nThis novelty utility is part of the BSD games collection and serves as both entertainment and an educational tool for understanding historical data encoding formats."],["CAVEATS","Entertainment utility. Part of bsdgames."],["HISTORY","ppt was created as a **BSD games** utility for paper tape simulation."],["SEE ALSO","[banner](/man/banner)(1), [morse](/man/morse)(1)"]],"e":["banner","morse"],"x":"ppt [text]"},"protonvpn-connect":{"n":"protonvpn-connect","t":"Establish Proton VPN connections","s":[["TAGLINE","Establish Proton VPN connections"],["TLDR","**Connect to the fastest server available**\n\n```protonvpn connect --fastest```\n\n**Connect to the fastest server in a specific country**\n\n```protonvpn connect --cc [US]```\n\n**Connect to a specific server**\n\n```protonvpn connect [server_name]```\n\n**Connect to the fastest P2P server**\n\n```protonvpn connect --p2p```\n\n**Connect to the fastest Secure Core server**\n\n```protonvpn connect --sc```\n\n**Connect using TCP protocol**\n\n```protonvpn connect --fastest -p tcp```"],["SYNOPSIS","**protonvpn connect** [_options_] [_server_]"],["PARAMETERS","**--fastest**\n> Connect to the fastest available server.\n\n**--random**\n> Connect to a random server.\n\n**--cc** _CODE_\n> Connect to the fastest server in specified country.\n\n**--p2p**\n> Connect to the fastest P2P server.\n\n**--sc**\n> Connect to the fastest Secure Core server.\n\n**-p** _PROTOCOL_\n> Specify transmission protocol (udp or tcp).\n\n**-h**, **--help**\n> Display help message."],["DESCRIPTION","**protonvpn connect** establishes a connection to Proton VPN service. It can automatically select the fastest available server or connect to a specific server, country, or city.\n\nThe command is part of the Proton VPN CLI tool for Linux, providing secure VPN connectivity through the Proton network."],["CAVEATS","Requires valid Proton VPN subscription and prior authentication with `protonvpn login`. Network configuration may require root privileges."],["HISTORY","Part of **Proton VPN** CLI client for Linux, developed by Proton AG for secure internet access through their VPN infrastructure."],["SEE ALSO","[protonvpn](/man/protonvpn)(1), [openvpn](/man/openvpn)(8)"]],"e":["protonvpn","openvpn"],"x":"protonvpn connect --fastest"},"prt-get":{"n":"prt-get","t":"Package manager for CRUX Linux","s":[["TAGLINE","Package manager for CRUX Linux"],["TLDR","**Install** a package\n\n```prt-get install [package]```\n\nInstall with **dependencies**\n\n```prt-get depinst [package]```\n\n**Upgrade** a package\n\n```prt-get upgrade [package]```\n\n**Remove** a package\n\n```prt-get remove [package]```\n\n**System upgrade** from ports\n\n```prt-get sysup```\n\n**Search** ports tree\n\n```prt-get search [query]```\n\nSearch for **file** in packages\n\n```prt-get fsearch [filename]```\n\nShow packages with **available updates**\n\n```prt-get diff```\n\nShow **info** about a package\n\n```prt-get info [package]```\n\n**List dependencies** of a package\n\n```prt-get depends [package]```\n\n**List installed** packages\n\n```prt-get listinst```"],["SYNOPSIS","**prt-get** [**install**|**depinst**|**upgrade**|**remove**|**search**|**sysup**] [_options_] [_package_]"],["COMMANDS","**install**\n> Install package without dependency handling\n\n**depinst**\n> Install package with dependencies\n\n**upgrade**\n> Upgrade installed package\n\n**remove**\n> Remove installed package\n\n**sysup**\n> Upgrade all installed packages\n\n**search**\n> Search for packages by name\n\n**fsearch**\n> Search for files in packages\n\n**info**\n> Display package information\n\n**diff**\n> Show packages with available updates\n\n**listinst**\n> List installed packages\n\n**dependent**\n> List packages depending on a package\n\n**depends**\n> List dependencies of a package\n\n**deptree**\n> Show the full dependency tree of a package\n\n**cat**\n> Print the Pkgfile of a port\n\n**path**\n> Print the path to a port directory\n\n**lock**\n> Lock a package to prevent upgrades\n\n**unlock**\n> Unlock a previously locked package\n\n**listlocked**\n> List locked packages\n\n**isinst**\n> Check if a package is installed"],["PARAMETERS","**-fr**\n> Force rebuild even if up-to-date\n\n**-if**\n> Ignore footprint mismatches\n\n**-im**\n> Ignore MD5 mismatches\n\n**-v**\n> Show version in search results\n\n**-vv**\n> Show version and description in search results\n\n**-is**\n> Ignore signature mismatches\n\n**--margs**=_\"args\"_\n> Pass additional arguments to pkgmk\n\n**--aargs**=_\"args\"_\n> Pass additional arguments to pkgadd\n\n**--log**\n> Enable build logging\n\n**--all**\n> Include locked packages in output\n\n**--config**=_file_\n> Use alternative configuration file"],["DESCRIPTION","**prt-get** is the high-level package manager for CRUX Linux. It provides dependency resolution on top of the basic pkgutils, making package management more convenient while maintaining CRUX's simplicity.\n\nThe tool reads package information from the ports tree and can automatically build and install packages with their dependencies."],["CAVEATS","CRUX-specific tool. Requires ports tree to be synchronized. Builds packages from source. No pre-built binary packages."],["HISTORY","**prt-get** was created for CRUX Linux to add dependency handling to the basic pkgutils. It remains the primary way to manage packages on CRUX systems."],["SEE ALSO","[pkgmk](/man/pkgmk)(8), [pkgadd](/man/pkgadd)(8), [pkgrm](/man/pkgrm)(8), [pkginfo](/man/pkginfo)(8), [ports](/man/ports)(8)"]],"e":["pkgmk","pkgadd","pkgrm","pkginfo","ports"],"x":"prt-get install [package]"},"removepkg":{"n":"removepkg","t":"Remove Slackware packages","s":[["TAGLINE","Remove Slackware packages"],["TLDR","**Remove** a package\n\n```sudo removepkg package_name```\n\nGenerate a **simulated removal report**\n\n```removepkg -warn package_name```\n\n**Preserve** package files and remove\n\n```sudo removepkg -preserve package_name```\n\n**Copy** package without removing\n\n```removepkg -copy package_name```\n\n**Keep** temporary files for debugging\n\n```sudo removepkg -keep package_name```"],["SYNOPSIS","**removepkg** [_options_] _package_name_"],["PARAMETERS","**-warn**\n> Generate simulated removal report without removing\n\n**-preserve**\n> Reconstruct package tree in /tmp/preserved_packages before removing\n\n**-copy**\n> Copy package to /tmp/preserved_packages without removing\n\n**-keep**\n> Keep temporary files for debugging"],["DESCRIPTION","**removepkg** removes specified Slackware packages from the system. It can simulate removal, preserve package files before deletion, or copy packages without removing them.\n\nThe tool is part of Slackware's native package management system."],["CAVEATS","Does not handle dependencies automatically. Removing packages that others depend on may break the system."],["HISTORY","Part of **Slackware** package management utilities. Provides basic package removal functionality for the Slackware Linux distribution."],["SEE ALSO","[installpkg](/man/installpkg)(8), [upgradepkg](/man/upgradepkg)(8), [pkgtool](/man/pkgtool)(8)"]],"e":["installpkg","upgradepkg","pkgtool"],"x":"sudo removepkg package_name"},"slides":{"n":"slides","t":"Terminal-based markdown presentation tool","s":[["TAGLINE","Terminal-based markdown presentation tool"],["TLDR","**Start a presentation** from a markdown file\n\n```slides [presentation.md]```\n\n**Start with a specific slide**\n\n```slides [presentation.md] --slide [3]```\n\n**Serve a presentation** over SSH\n\n```slides serve [presentation.md]```\n\n**Execute code blocks** during presentation\n\n```slides [presentation.md]```\n> Press **Ctrl+E** on a slide with a code block to execute it"],["SYNOPSIS","**slides** [_options_] [_file_]\n\n**slides** **serve** [_file_]"],["PARAMETERS","**--slide** _N_\n> Start the presentation at slide number N.\n\n**--theme** _theme_\n> Use a specific theme for the presentation.\n\n**serve**\n> Host the presentation over SSH, allowing remote viewing."],["NAVIGATION","**h**, **Left**, **Page Up**, **Backspace**\n> Go to previous slide.\n\n**l**, **Right**, **Page Down**, **Space**, **Enter**\n> Go to next slide.\n\n**g**, **Home**\n> Jump to first slide.\n\n**G**, **End**\n> Jump to last slide.\n\n**1**-**9**\n> Jump to slide number N.\n\n**/**\n> Search slides (supports regex, use /i for case-insensitive).\n\n**Ctrl+E**\n> Execute code block on current slide.\n\n**q**, **Ctrl+C**\n> Quit the presentation."],["DESCRIPTION","**slides** is a terminal-based presentation tool that renders Markdown files as slideshows. Slides are separated by horizontal rules (**---**) in the Markdown file. It supports syntax highlighting for code blocks, dynamic content, and live code execution.\n\nThe tool automatically watches for changes in the source file and updates the presentation in real-time, making it ideal for iterative development of presentations. Code blocks can be executed directly during the presentation by pressing **Ctrl+E**, displaying the output inline.\n\nslides can also serve presentations over SSH using the **serve** subcommand, allowing audience members to follow along from their own terminals."],["CAVEATS","Requires a terminal that supports ANSI escape codes for proper rendering. Code execution runs commands in your local environment, so use caution with untrusted presentation files. The presentation appearance depends on terminal capabilities and font support."],["HISTORY","**slides** was created by Maas Lalani as an open-source terminal presentation tool written in Go. It was designed to provide a simple, distraction-free way to give presentations directly from the terminal using familiar Markdown syntax. The project gained popularity among developers who prefer terminal-based workflows and live coding demonstrations."],["INSTALL","```apk: sudo apk add slides```\n\n```brew: brew install slides```\n\n```nix: nix profile install nixpkgs#slides```"],["SEE ALSO","[mdp](/man/mdp)(1), [patat](/man/patat)(1), [presenterm](/man/presenterm)(1), [tpp](/man/tpp)(1)"]],"e":["mdp","patat","presenterm","tpp"],"x":"slides [presentation.md]"},"smbmap":{"n":"smbmap","t":"Enumerate SMB share permissions and access","s":[["TAGLINE","Enumerate SMB share permissions and access"],["TLDR","**List shares on host**\n\n```smbmap -H [192.168.1.100]```\n\n**Authenticate with credentials**\n\n```smbmap -H [192.168.1.100] -u [user] -p [password]```\n\n**List share contents**\n\n```smbmap -H [192.168.1.100] -u [user] -p [password] -r [share]```\n\n**Download file**\n\n```smbmap -H [192.168.1.100] -u [user] -p [password] --download '[share/file.txt]'```\n\n**Execute command**\n\n```smbmap -H [192.168.1.100] -u [admin] -p [password] -x '[whoami]'```"],["SYNOPSIS","**smbmap** [_options_]"],["PARAMETERS","**-H** _host_\n> Target host.\n\n**-u** _user_\n> Username.\n\n**-p** _pass_\n> Password.\n\n**-d** _domain_\n> Domain.\n\n**-r** _share_\n> Recursively list share.\n\n**--download** _path_\n> Download file.\n\n**--upload** _src_ _dst_\n> Upload file.\n\n**-x** _cmd_\n> Execute command.\n\n**-L**\n> List drives."],["DESCRIPTION","**smbmap** enumerates SMB share permissions and contents. Security tool for authorized penetration testing and share auditing. Shows read/write access levels and enables file operations."],["CAVEATS","For authorized security assessments only. Requires proper authorization before use."],["INSTALL","```nix: nix profile install nixpkgs#smbmap```"],["SEE ALSO","[smbclient](/man/smbclient)(1), [enum4linux](/man/enum4linux)(1)"]],"e":["smbclient","enum4linux"],"x":"smbmap -H [192.168.1.100]"},"solo":{"n":"solo","t":"Manage Solo FIDO2 security keys","s":[["TAGLINE","Manage Solo FIDO2 security keys"],["TLDR","**Connect to device**\n\n```solo ls```\n\n**Update firmware**\n\n```solo update```\n\n**Make a FIDO2 credential**\n\n```solo key make-credential```\n\n**Wink device (flash LED)**\n\n```solo key wink```\n\n**Reset device**\n\n```solo key reset```\n\n**Generate random bytes**\n\n```solo key rng```\n\n**Show version**\n\n```solo version```"],["SYNOPSIS","**solo** _command_ [_options_]"],["PARAMETERS","**ls**\n> List devices.\n\n**update**\n> Update firmware.\n\n**key**\n> Key operations.\n\n**version**\n> Show version.\n\n**wink**\n> Flash LED.\n\n**reset**\n> Reset device.\n\n**make-credential**\n> Generate a FIDO2 credential.\n\n**rng**\n> Output random bytes from the device RNG.\n\n**verify**\n> Verify the device is a genuine Solo key."],["DESCRIPTION","**solo** is the command-line management tool for Solo FIDO2 security keys, open-source hardware authenticators used for passwordless login and two-factor authentication. It handles firmware updates, credential management, and device diagnostics.\n\nThe tool can list connected Solo devices, update their firmware, generate FIDO2 credentials for specific domains, and test device functionality. The wink command flashes the device LED to help identify a specific key when multiple are connected.\n\nSolo keys are fully open-source (both hardware and firmware), allowing independent security audits. The CLI tool is Python-based and communicates with the key over USB HID."],["CAVEATS","Solo key required. USB connection needed. Python based."],["HISTORY","**solo** is the CLI tool for **Solo** FIDO2 security keys, providing open-source hardware authentication."],["INSTALL","```apk: sudo apk add py3-solo1```"],["SEE ALSO","[fido2-token](/man/fido2-token)(1), [ykman](/man/ykman)(1), [gpg](/man/gpg)(1)"]],"e":["fido2-token","ykman","gpg"],"x":"solo ls"},"spi":{"n":"spi","t":"Salix Package Installer: meta package manager wrapping slapt-get and slapt-src","s":[["TAGLINE","Salix Package Installer: meta package manager wrapping slapt-get and slapt-src"],["TLDR","**Search** both binary and SlackBuild repositories (default action)\n\n```spi [search_terms]```\n\n**Update** package and SlackBuild local caches\n\n```sudo spi -u```\n\n**Install** a package or SlackBuild\n\n```sudo spi -i [package_name]```\n\n**Simulate** an install, showing what would be done\n\n```spi -s -i [package_name]```\n\n**Upgrade** all installed packages\n\n```sudo spi -U```\n\n**Show information** about a package or SlackBuild\n\n```spi --show [package_name]```\n\n**Clean** local package and SlackBuild caches\n\n```sudo spi --clean```"],["SYNOPSIS","**spi** [_OPTIONS_] [_STRING(S)_]"],["DESCRIPTION","**spi** (Salix Package Installer) is a command-line meta package manager for **Salix Linux** that wraps **slapt-get** and **slapt-src**. It provides unified access to both binary packages and SlackBuilds through a single interface.\n\nWhen searching, **spi** queries both the **slapt-get** binary package cache and the **slapt-src** SlackBuild cache. When installing, it prefers binary packages: if none is available it falls back to building from source via **slapt-src**, and when a SlackBuild has dependencies that exist as binary packages it pulls those through **slapt-get** rather than rebuilding them.\n\nOnly basic functionality is exposed; for advanced features the underlying **slapt-get** and **slapt-src** tools should be used directly."],["PARAMETERS","**--search** _STRING(S)_\n> Search both the package and SlackBuild databases. Default action when **spi** is called with one or more strings and no other options. Regular expressions are supported.\n\n**--show** _PACKAGE(S)_\n> Show details for the named packages or SlackBuilds (runs **slapt-get --show** and **slapt-src --show**).\n\n**--clean**\n> Purge cached packages and SlackBuild build directories. Requires root.\n\n**-u**, **--update**\n> Update the package and SlackBuild local caches. Requires root.\n\n**-U**, **--upgrade**\n> Upgrade all installed packages using **slapt-get --upgrade**. Requires root.\n\n**-i**, **--install** _PACKAGE(S)_\n> Install the named packages or SlackBuilds. Binary packages are preferred; otherwise the SlackBuild is built and installed. Requires root.\n\n**-s**, **--simulate**\n> Used with **--install**: simulate the operation, listing packages and SlackBuilds (including dependencies) that would be installed.\n\n**-n**, **--no-colour**\n> Disable coloured output when searching.\n\n**-h**\n> Print a brief usage message and exit."],["CAVEATS","Requires **slapt-get** and **slapt-src** to be installed and configured. SlackBuild installations compile from source and can be considerably slower than binary installs. The tool is specific to **Salix** and other Slackware-derived distributions that ship the slapt toolchain. Caches must be updated (**spi -u**) before searching for or installing newly published packages."],["HISTORY","**spi** was created by **George Vlahavas** (gapan) for the **Salix** Linux distribution, a Slackware derivative focused on simplicity and ease of use. Salix was founded in **2009**, and **spi** has since provided a user-friendly bridge between Slackware's binary package and SlackBuild source-build worlds."],["SEE ALSO","[slapt-get](/man/slapt-get)(8), [slapt-src](/man/slapt-src)(8), [slackpkg](/man/slackpkg)(8)"]],"e":["slapt-get","slapt-src","slackpkg"],"x":"spi [search_terms]"},"split":{"n":"split","t":"Divide files into smaller pieces","s":[["TAGLINE","Divide files into smaller pieces"],["TLDR","**Split file into 1000-line pieces**\n\n```split [file]```\n\n**Split into pieces with custom prefix**\n\n```split [file] [prefix_]```\n\n**Split into specific number of lines**\n\n```split -l [500] [file]```\n\n**Split into specific size pieces**\n\n```split -b [10M] [file]```\n\n**Split into N equal pieces**\n\n```split -n [5] [file]```\n\n**Split with numeric suffixes**\n\n```split -d [file]```\n\n**Split with custom suffix length**\n\n```split -a [4] [file]```"],["SYNOPSIS","**split** [_options_] [_file_ [_prefix_]]"],["PARAMETERS","**-l** _lines_, **--lines**=_lines_\n> Put specified number of lines per output file\n\n**-b** _size_, **--bytes**=_size_\n> Put specified bytes per output file (K, M, G suffixes)\n\n**-n** _chunks_, **--number**=_chunks_\n> Generate specified number of output files\n\n**-d**, **--numeric-suffixes**\n> Use numeric suffixes instead of alphabetic\n\n**-a** _N_, **--suffix-length**=_N_\n> Generate suffixes of length N (default: 2)\n\n**-e**, **--elide-empty-files**\n> Do not generate empty output files with -n\n\n**--verbose**\n> Print message for each output file\n\n**--additional-suffix**=_suf_\n> Append additional suffix to file names\n\n**-x**, **--hex-suffixes**\n> Use hexadecimal suffixes"],["DESCRIPTION","**split** divides a file into smaller pieces. By default, it creates files with 1000 lines each, named with a prefix (default: **x**) followed by a suffix (aa, ab, ac, ...).\n\nThe command is useful for breaking large files for transmission, processing, or storage limitations. It works with both text and binary files.\n\nSize specifications accept suffixes: **K** (kilobytes), **M** (megabytes), **G** (gigabytes), and also **KB**, **MB**, **GB** for powers of 1000.\n\nSplit reads from stdin if no file is specified or if file is **-**."],["CAVEATS","The default 2-character suffix limits output to 676 files (aa-zz). Use **-a** to increase suffix length for more pieces, or **-d** for numeric suffixes.\n\nWhen splitting binary files, use **-b** (bytes) not **-l** (lines) to avoid corruption at arbitrary byte boundaries.\n\nTo reassemble, use **cat prefix\\*** > **original_file**. Ensure files are concatenated in correct alphabetical/numerical order."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[csplit](/man/csplit)(1), [cat](/man/cat)(1), [head](/man/head)(1), [tail](/man/tail)(1)"]],"e":["csplit","cat","head","tail"],"x":"split [file]"},"sqlite3":{"n":"sqlite3","t":"Interactive SQLite database shell","s":[["TAGLINE","Interactive SQLite database shell"],["TLDR","**Open or create a database**\n\n```sqlite3 [database.db]```\n\n**Execute a SQL query**\n\n```sqlite3 [database.db] \"[SELECT * FROM table]\"```\n\n**Execute SQL from a file**\n\n```sqlite3 [database.db] < [query.sql]```\n\n**Export query results to CSV**\n\n```sqlite3 -csv [database.db] \"[SELECT * FROM table]\" > [output.csv]```\n\n**Show all tables**\n\n```sqlite3 [database.db] \".tables\"```\n\n**Show table schema**\n\n```sqlite3 [database.db] \".schema [table_name]\"```\n\n**Import CSV into table**\n\n```sqlite3 [database.db] \".import [file.csv] [table_name]\"```\n\n**Open in-memory database**\n\n```sqlite3 :memory:```"],["SYNOPSIS","**sqlite3** [_options_] [_database_] [_sql_]"],["PARAMETERS","**-csv**\n> Set output mode to CSV\n\n**-json**\n> Set output mode to JSON\n\n**-column**\n> Set output mode to column format\n\n**-header**\n> Include column headers in output\n\n**-line**\n> Set output mode to one value per line\n\n**-separator** _sep_\n> Set field separator for CSV mode\n\n**-init** _file_\n> Execute SQL from file at startup\n\n**-batch**\n> Batch mode; exit on error\n\n**-readonly**\n> Open database in read-only mode\n\n**-version**\n> Show version information"],["DOT COMMANDS","**.tables**: List all tables\n**.schema** [_table_]: Show CREATE statements\n**.headers on|off**: Toggle column headers\n**.mode** _mode_: Set output mode (csv, column, json, line, etc.)\n**.import** _file table_: Import data from file\n**.output** _file_: Redirect output to file\n**.read** _file_: Execute SQL from file\n**.dump** [_table_]: Dump database as SQL\n**.exit** or **.quit**: Exit sqlite3\n**.help**: Show all dot commands"],["DESCRIPTION","**sqlite3** is the command-line interface for SQLite, a self-contained, serverless SQL database engine. SQLite databases are single files that can be easily shared and backed up.\n\nThe tool provides both interactive and batch modes. In interactive mode, enter SQL statements ending with semicolons or dot commands for administrative functions. Batch mode processes SQL from arguments or stdin.\n\nSQLite supports most SQL features including transactions, triggers, views, and complex queries. It's embedded in countless applications and is ideal for local storage, testing, and small-scale applications."],["CAVEATS","SQLite uses dynamic typing; any column can store any type regardless of declared type. This differs from strict SQL databases.\n\nConcurrent writes are limited—SQLite locks the entire database during writes. For high-concurrency applications, consider client-server databases.\n\nDot commands are sqlite3-specific, not SQL. They don't work in SQL statements or from application code."],["INSTALL","```apt: sudo apt install sqlite3```\n\n```dnf: sudo dnf install sqlite```\n\n```pacman: sudo pacman -S sqlite```\n\n```apk: sudo apk add sqlite```\n\n```zypper: sudo zypper install sqlite3```\n\n```brew: brew install sqlite3```\n\n```nix: nix profile install nixpkgs#sqlite```"],["SEE ALSO","[mysql](/man/mysql)(1), [psql](/man/psql)(1), [sqlite-utils](/man/sqlite-utils)(1)"]],"e":["mysql","psql","sqlite-utils"],"x":"sqlite3 [database.db]"},"strip-nondeterminism":{"n":"strip-nondeterminism","t":"Remove non-deterministic data for reproducible builds","s":[["TAGLINE","Remove non-deterministic data for reproducible builds"],["TLDR","**Strip file**\n\n```strip-nondeterminism [file]```\n\n**Strip with type**\n\n```strip-nondeterminism --type [zip] [file.zip]```\n\n**Set timestamp**\n\n```strip-nondeterminism --timestamp [1234567890] [file]```\n\n**Verbose output**\n\n```strip-nondeterminism -v [file]```\n\n**Dry run**\n\n```strip-nondeterminism -n [file]```"],["SYNOPSIS","**strip-nondeterminism** [_--type type_] [_--timestamp ts_] [_options_] _files_"],["PARAMETERS","**-t**, **--type** _TYPE_\n> Force the input to be treated as a particular file format (e.g., _zip_, _jar_, _png_, _gzip_, _ar_, _javadoc_) instead of letting the filename suffix decide.\n\n**-T**, **--timestamp** _TS_\n> Replace embedded timestamps with the given Unix epoch value. If unset, **strip-nondeterminism** falls back to the **SOURCE_DATE_EPOCH** environment variable.\n\n**-v**, **--verbose**\n> Print each modification.\n\n**-n**, **--dry-run**\n> Show what would change without writing to disk.\n\n**--help**\n> Show help."],["DESCRIPTION","**strip-nondeterminism** removes sources of non-determinism from files to enable reproducible builds. Build artifacts often contain embedded timestamps, user IDs, file ordering variations, and other metadata that differ between builds even when the source code is identical. This tool normalizes those elements so that building the same source always produces bit-identical output.\n\nThe tool supports multiple file formats including ZIP archives, JAR files, PNG images, gzip files, and ar archives. For each format, it identifies and strips or normalizes the specific metadata fields that introduce non-determinism, such as modification timestamps in archive entries or creation dates in PNG headers.\n\nIt is part of the Debian Reproducible Builds infrastructure and is commonly integrated into build systems as a post-processing step. A fixed timestamp can be provided to replace variable ones, typically sourced from the SOURCE_DATE_EPOCH environment variable."],["CAVEATS","Format-specific. May need type hint. Part of Debian reproducible builds."],["HISTORY","**strip-nondeterminism** was created for the **Reproducible Builds** project to make build outputs deterministic."],["INSTALL","```pacman: sudo pacman -S strip-nondeterminism```\n\n```apk: sudo apk add strip-nondeterminism```\n\n```zypper: sudo zypper install strip-nondeterminism```\n\n```brew: brew install strip-nondeterminism```\n\n```nix: nix profile install nixpkgs#strip-nondeterminism```"],["SEE ALSO","[touch](/man/touch)(1), [zip](/man/zip)(1), [ar](/man/ar)(1), [diffoscope](/man/diffoscope)(1)"]],"e":["touch","zip","ar","diffoscope"],"x":"strip-nondeterminism [file]"},"systemctl-emergency":{"n":"systemctl-emergency","t":"Enter minimal emergency recovery mode","s":[["TAGLINE","Enter minimal emergency recovery mode"],["TLDR","Enter **emergency** mode\n\n```systemctl emergency```\n\nEnter emergency mode **asynchronously**\n\n```systemctl emergency --no-block```"],["SYNOPSIS","**systemctl emergency** [_OPTIONS_]"],["PARAMETERS","**--no-block**\n> Return immediately without waiting for the operation to complete"],["DESCRIPTION","**systemctl emergency** transitions the system to emergency mode (`emergency.target`). Emergency mode mounts the root filesystem read-only and starts only essential services needed for system recovery. It provides a minimal environment for repairing a broken system.\n\nEmergency mode is more minimal than rescue mode and is typically used when rescue mode cannot start properly."],["CAVEATS","Requires root privileges. Most services will be stopped. Network will be unavailable. The root filesystem is mounted read-only by default; remount with `mount -o remount,rw /` if needed for repairs."],["HISTORY","The **emergency** subcommand provides a standardized way to enter the most minimal recovery mode, replacing the traditional init=/bin/sh boot parameter."],["SEE ALSO","[systemctl](/man/systemctl)(1), [systemctl-rescue](/man/systemctl-rescue)(1), [systemctl-default](/man/systemctl-default)(1)"]],"e":["systemctl","systemctl-rescue","systemctl-default"],"x":"systemctl emergency"},"tcpflow":{"n":"tcpflow","t":"TCP flow recorder","s":[["TAGLINE","TCP flow recorder"],["TLDR","Capture and display **traffic** on interface and port\n\n```tcpflow -c -i [eth0] port [80]```\n\nRead flows from a **pcap file**\n\n```tcpflow -r [path/to/capture.pcap] -o [output_dir]```\n\nCapture with **colored** console output (blue=client, red=server)\n\n```tcpflow -c -g -i [eth0]```\n\nLimit capture to **max bytes** per flow\n\n```tcpflow -b [10000] -i [eth0] port [443]```\n\nStrip **non-printable** characters in console output\n\n```tcpflow -c -s -i [eth0]```"],["SYNOPSIS","**tcpflow** [_OPTIONS_] [_EXPRESSION_]"],["PARAMETERS","**-c**\n> Console print with source/dest header (don't create files)\n\n**-C**\n> Console print without header (don't create files)\n\n**-i** _INTERFACE_\n> Capture on specified network interface\n\n**-r** _FILE_\n> Read from pcap file instead of live capture\n\n**-o** _DIR_\n> Output directory for flow files\n\n**-b** _MAX_BYTES_\n> Capture no more than max_bytes per flow\n\n**-e** _SCANNER_\n> Enable specific scanner\n\n**-s**\n> Strip non-printable characters (replace with '.')\n\n**-g**\n> Colorized console output (blue=client, red=server)\n\n**-D**\n> Console output in hex\n\n**-d** _LEVEL_\n> Debug level (0=silent, 1=default)\n\n**-f** _MAX_FDS_\n> Maximum number of file descriptors to use\n\n**-p**\n> Do not put interface into promiscuous mode\n\n**-q**\n> Quiet mode, suppress warnings\n\n**-P**\n> No purge of closed connections from hash table"],["DESCRIPTION","**tcpflow** captures TCP traffic and reconstructs data streams for debugging and analysis. Unlike packet sniffers that show individual packets, tcpflow reassembles TCP connections into complete data flows.\n\nEach TCP flow is saved to a separate file named by source and destination addresses/ports, making it easy to analyze individual connections."],["CAVEATS","Requires root privileges or appropriate capabilities. Uses libpcap filter expressions. Only captures TCP traffic, not UDP or other protocols. Large captures can consume significant disk space."],["HISTORY","**tcpflow** was created by **Jeremy Elson** and is designed for forensic analysis and debugging of network applications by reconstructing TCP sessions."],["INSTALL","```apt: sudo apt install tcpflow```\n\n```dnf: sudo dnf install tcpflow```\n\n```pacman: sudo pacman -S tcpflow```\n\n```apk: sudo apk add tcpflow```\n\n```zypper: sudo zypper install tcpflow```\n\n```brew: brew install tcpflow```\n\n```nix: nix profile install nixpkgs#tcpflow```"],["SEE ALSO","[tcpdump](/man/tcpdump)(8), [wireshark](/man/wireshark)(1), [tcpick](/man/tcpick)(8), [nmap](/man/nmap)(1)"]],"e":["tcpdump","wireshark","tcpick","nmap"],"x":"tcpflow -c -i [eth0] port [80]"},"tenere":{"n":"tenere","t":"TUI interface for large language models","s":[["TAGLINE","TUI interface for large language models"],["TLDR","**Launch the LLM chat TUI**\n\n```tenere```\n\n**Use a specific config file**\n\n```tenere --config [~/.config/tenere/config.toml]```"],["SYNOPSIS","**tenere** [_options_]"],["DESCRIPTION","**tenere** is a terminal user interface for interacting with LLMs like ChatGPT, llama.cpp, and Ollama. It features Vim-like modes (Normal, Visual, Insert), syntax highlighting, chat history, and file saving. Configuration is done via TOML files."],["HISTORY","**tenere** was created by **Badr** (pythops) and is written in **Rust**."],["INSTALL","```brew: brew install tenere```\n\n```nix: nix profile install nixpkgs#tenere```"],["SEE ALSO","[aichat](/man/aichat)(1), [ollama](/man/ollama)(1), [oterm](/man/oterm)(1)"]],"e":["aichat","ollama","oterm"],"x":"tenere"},"terraform-console":{"n":"terraform-console","t":"Interactive Terraform expression evaluator","s":[["TAGLINE","Interactive Terraform expression evaluator"],["TLDR","**Open** the interactive console\n\n```terraform console```\n\n**Evaluate** an expression against the **planned state**\n\n```terraform console -plan```\n\n**Evaluate** an expression **non-interactively**\n\n```echo '[expression]' | terraform console```\n\n**Open** the console with a specific **variable file**\n\n```terraform console -var-file=[path/to/vars.tfvars]```"],["SYNOPSIS","**terraform console** [_options_]"],["PARAMETERS","**-plan**\n> Generate an execution plan first and evaluate expressions against the planned state rather than the current state. Shows values Terraform expects after the plan is applied\n\n**-state=**_path_\n> Path to a local state file to evaluate expressions against (default: terraform.tfstate; legacy, local backend only)\n\n**-var** _'NAME=VALUE'_\n> Set a variable value on the command line\n\n**-var-file=**_FILE_\n> Load variable definitions from a file"],["DESCRIPTION","**terraform console** provides an interactive **REPL** (Read-Eval-Print Loop) for evaluating Terraform expressions. It allows testing expressions and interpolations, inspecting current state values, experimenting with built-in functions like **split()**, **lower()**, **length()**, and debugging configurations interactively.\n\nThe console is **read-only** and does not modify state, configuration files, or infrastructure resources. However, it locks the state file while running, preventing concurrent **plan** or **apply** operations.\n\nFields, outputs, variables, and locals from the current Terraform state can be accessed directly. Type **exit** or press **Ctrl-C** / **Ctrl-D** to quit."],["CAVEATS","The console **locks the state file** while running, so you cannot run **terraform plan** or **terraform apply** in the same workspace concurrently. You must run **terraform init** before using the console. Resources that have not been created yet show as unknown unless you use the **-plan** flag. The special **type()** function is only available within the console (since Terraform v1.1). You cannot define new variables interactively."],["HISTORY","Introduced in **Terraform v0.8** in **November 2016** by Mitchell Hashimoto, co-founder of HashiCorp. Originally described as an interactive console for experimenting with interpolations. Support for **-var** and **-var-file** was added later via a fix in **September 2019**. The **type()** function was added in **Terraform v1.1** as a console-only feature."],["INSTALL","```pacman: sudo pacman -S terraform```\n\n```nix: nix profile install nixpkgs#terraform```"],["SEE ALSO","[terraform](/man/terraform)(1), [terraform-plan](/man/terraform-plan)(1), [terraform-apply](/man/terraform-apply)(1), [terraform-output](/man/terraform-output)(1), [terraform-login](/man/terraform-login)(1)"]],"e":["terraform","terraform-plan","terraform-apply","terraform-output","terraform-login"],"x":"terraform console"},"tlmgr-info":{"n":"tlmgr-info","t":"Display TeX Live package information","s":[["TAGLINE","Display TeX Live package information"],["TLDR","**Show package information**\n\n```tlmgr info [package]```\n\n**List all packages**\n\n```tlmgr info --list```\n\n**List installed packages**\n\n```tlmgr info --only-installed```\n\n**Show package files**\n\n```tlmgr info --files [package]```"],["SYNOPSIS","**tlmgr info** [_options_] [_package_...]"],["PARAMETERS","**--list**\n> List all available packages.\n\n**--only-installed**\n> Show only installed packages.\n\n**--files**\n> Show package files.\n\n**--data** _fields_\n> Output specific fields (name, category, etc.).\n\n**--json**\n> Output in JSON format."],["DESCRIPTION","**tlmgr info** displays information about TeX Live packages. Shows descriptions, versions, sizes, and contained files. Part of TeX Live Manager."],["SEE ALSO","[tlmgr-search](/man/tlmgr-search)(1), [tlmgr](/man/tlmgr)(1)"]],"e":["tlmgr-search","tlmgr"],"x":"tlmgr info [package]"},"tput":{"n":"tput","t":"Query and set terminal capabilities","s":[["TAGLINE","Query and set terminal capabilities"],["TLDR","**Set bold text**\n\n```tput bold```\n\n**Set foreground color** (0-7: black, red, green, yellow, blue, magenta, cyan, white)\n\n```tput setaf [2]```\n\n**Set background color**\n\n```tput setab [4]```\n\n**Reset all attributes** to default\n\n```tput sgr0```\n\n**Clear the screen**\n\n```tput clear```\n\n**Get terminal width** in columns\n\n```tput cols```\n\n**Get terminal height** in lines\n\n```tput lines```\n\n**Move cursor** to row and column\n\n```tput cup [5] [10]```"],["SYNOPSIS","**tput** [**-T** _type_] _capname_ [_parameters_...]"],["PARAMETERS","**-T** _type_\n> Specify terminal type; defaults to $TERM environment variable\n\n**-S**\n> Read capabilities from stdin, allowing multiple operations\n\n**-V**\n> Print ncurses version and exit"],["COMMON CAPABILITIES","**bold**\n> Enable bold mode\n\n**dim**\n> Enable half-bright/dim mode\n\n**smul** / **rmul**\n> Start/end underline mode\n\n**rev**\n> Enable reverse video mode\n\n**blink**\n> Enable blinking text\n\n**smso** / **rmso**\n> Start/end standout mode\n\n**sgr0**\n> Reset all attributes to default\n\n**setaf** _n_\n> Set foreground color (0-7 or 0-255)\n\n**setab** _n_\n> Set background color\n\n**clear**\n> Clear screen and move cursor home\n\n**cup** _row_ _col_\n> Move cursor to position\n\n**cols**\n> Print number of columns\n\n**lines**\n> Print number of lines\n\n**sc** / **rc**\n> Save/restore cursor position\n\n**civis** / **cnorm**\n> Hide/show cursor"],["DESCRIPTION","**tput** queries the terminfo database to output terminal-dependent capabilities. It provides a portable way to control terminal features like colors, cursor positioning, and text attributes without hardcoding escape sequences.\n\nThe command uses the **$TERM** environment variable to determine the terminal type and looks up the appropriate escape sequences. This makes scripts portable across different terminal emulators.\n\nColor numbers 0-7 represent basic colors: black (0), red (1), green (2), yellow (3), blue (4), magenta (5), cyan (6), white (7). Extended color terminals support 256 colors (0-255).\n\nCommon usage in scripts combines capabilities: **$(tput bold)$(tput setaf 1)Error$(tput sgr0)** prints \"Error\" in bold red, then resets."],["CAVEATS","Capabilities vary by terminal type; not all terminals support all features. Always reset attributes with **sgr0** after use to avoid affecting subsequent output. The **-T** option should match the actual terminal for correct behavior."],["HISTORY","**tput** was introduced in **System V Unix** in the early 1980s as part of the terminfo system, which replaced the older termcap. The ncurses implementation provides the tput command on most Linux systems today. It was designed to abstract terminal-specific escape codes behind a consistent interface."],["INSTALL","```apt: sudo apt install ncurses-bin```\n\n```dnf: sudo dnf install ncurses```\n\n```pacman: sudo pacman -S ncurses```\n\n```apk: sudo apk add ncurses```\n\n```brew: brew install ncurses```\n\n```nix: nix profile install nixpkgs#ncurses```"],["SEE ALSO","[infocmp](/man/infocmp)(1), [tic](/man/tic)(1), [clear](/man/clear)(1), [reset](/man/reset)(1)"]],"e":["infocmp","tic","clear","reset"],"x":"tput bold"},"tracepath":{"n":"tracepath","t":"Trace network path with MTU discovery","s":[["TAGLINE","Trace network path with MTU discovery"],["TLDR","**Trace** path to a host with MTU discovery\n\n```tracepath -p 33434 [host]```\n\n**Trace** with a specific destination port\n\n```tracepath -p [port] [host]```\n\n**Print** both hostnames and IP addresses\n\n```tracepath -b [host]```\n\n**Specify** maximum TTL (hops)\n\n```tracepath -m [max_hops] [host]```\n\n**Specify** initial packet length\n\n```tracepath -l [packet_length] [host]```\n\n**Use** only IPv6 addresses\n\n```tracepath -6 [host]```"],["SYNOPSIS","**tracepath** [_options_] _host_"],["PARAMETERS","**-p _port_**\n> Set initial destination port\n\n**-b**\n> Print both hostnames and numerical IP addresses\n\n**-m _max_hops_**\n> Set maximum number of hops (TTL)\n\n**-l _length_**\n> Set initial packet length\n\n**-4**\n> Use IPv4 only\n\n**-6**\n> Use IPv6 only\n\n**-n**\n> Print only numeric addresses"],["DESCRIPTION","**tracepath** traces the path to a network host, discovering the MTU (Maximum Transmission Unit) along the path. Unlike traceroute, it does not require superuser privileges and can detect path MTU.\n\nThe output shows each hop with its RTT and MTU information."],["CAVEATS","May not work through firewalls that block UDP packets. Results can vary based on network conditions and firewall rules. Part of the iputils package."],["INSTALL","```apt: sudo apt install iputils-tracepath```\n\n```apk: sudo apk add iputils-tracepath```"],["SEE ALSO","[traceroute](/man/traceroute)(8), [ping](/man/ping)(8), [mtr](/man/mtr)(8)"]],"e":["traceroute","ping","mtr"],"x":"tracepath -p 33434 [host]"},"wn":{"n":"wn","t":"Command line interface to the WordNet lexical database","s":[["TAGLINE","Command line interface to the WordNet lexical database"],["TLDR","**Look up synonyms** of a noun\n\n```wn [word] -synsn```\n\n**Display the hypernym tree** (broader categories) for a noun\n\n```wn [word] -hypen```\n\n**Find antonyms** of an adjective\n\n```wn [word] -antsa```\n\n**Show an overview** of all senses of a word\n\n```wn [word] -over```\n\n**Display definitions** (glosses) for a noun\n\n```wn [word] -g -synsn```\n\n**Find hyponyms** (narrower categories) for a noun\n\n```wn [word] -hypon```\n\n**Search for compound words** containing a string\n\n```wn [word] -grepn```"],["SYNOPSIS","**wn** _searchstr_ [_-h_] [_-g_] [_-a_] [_-l_] [_-o_] [_-s_] [_-n#_] _search_option ..._"],["PARAMETERS","**-g**\n> Show textual glosses (definitions)\n\n**-s**\n> Show sense numbers in output\n\n**-o**\n> Display synset offset numbers\n\n**-a**\n> Show all synsets related to the search string\n\n**-n** _#_\n> Restrict results to a specific sense number\n\n**-h**\n> Display help text\n\n**-l**\n> Display license and copyright information"],["DESCRIPTION","**wn** provides a command-line interface to the **WordNet** lexical database, displaying synsets (sets of synonyms) and semantic relations as formatted text. For each word, different searches are available based on syntactic category (noun, verb, adjective, adverb) and relation type.\n\nSearch options end with a letter indicating the part of speech: **n** for nouns, **v** for verbs, **a** for adjectives, **r** for adverbs. Multiple searches can be combined in a single command.\n\nThe tool automatically performs morphological analysis, so inflected forms (e.g., \"running\") are resolved to their base form."],["SEARCH OPTIONS","**-syns**(n|v|a|r)\n> Display synonyms and immediate hypernyms of matching synsets\n\n**-ants**(n|v|a|r)\n> Display direct antonyms (opposites)\n\n**-hype**(n|v)\n> Recursively display the hypernym tree (broader categories)\n\n**-hypo**(n|v)\n> Display immediate hyponyms (narrower categories)\n\n**-tree**(n|v)\n> Display the subordinate hierarchy tree\n\n**-deri**(n|v)\n> Show morphologically related word forms\n\n**-over**\n> Display an overview of all senses across all parts of speech\n\n**-grep**(n|v|a|r)\n> List compound words containing the search string\n\n**-sims**v\n> Group verb senses by similarity of meaning"],["CAVEATS","WordNet's last official Princeton release was in **2011**, so coverage of newer vocabulary is limited. The database is English-only. Results are ordered by frequency of use, which may not reflect specialized domain usage."],["HISTORY","**WordNet** was created in **1985** at **Princeton University's** Cognitive Science Laboratory under the direction of psychologist **George Armitage Miller**. Later directed by **Christiane Fellbaum**, the project received the **2006 Antonio Zampolli Prize**. Initially funded by the U.S. Office of Naval Research and later by DARPA and NSF."],["INSTALL","```apt: sudo apt install wordnet```\n\n```dnf: sudo dnf install wordnet```\n\n```zypper: sudo zypper install wordnet```\n\n```brew: brew install wordnet```\n\n```nix: nix profile install nixpkgs#wordnet```"],["SEE ALSO","[dict](/man/dict)(1), [aspell](/man/aspell)(1), [grep](/man/grep)(1)"]],"e":["dict","aspell","grep"],"x":"wn [word] -synsn"},"xelatex":{"n":"xelatex","t":"Unicode-aware LaTeX to PDF compiler","s":[["TAGLINE","Unicode-aware LaTeX to PDF compiler"],["TLDR","**Compile a LaTeX document to PDF**\n\n```xelatex [document.tex]```\n\n**Compile with shell escape enabled**\n\n```xelatex -shell-escape [document.tex]```\n\n**Compile to a specified output directory**\n\n```xelatex -output-directory=[build/] [document.tex]```\n\n**Run in non-stop mode, skipping errors**\n\n```xelatex -interaction=nonstopmode [document.tex]```\n\n**Compile with a custom job name**\n\n```xelatex -jobname=[output] [document.tex]```\n\n**Compile with SyncTeX for editor integration**\n\n```xelatex -synctex=1 [document.tex]```\n\n**Generate XDV output instead of PDF**\n\n```xelatex -no-pdf [document.tex]```"],["SYNOPSIS","**xelatex** [_options_] [_file_]"],["PARAMETERS","**-output-directory** _DIR_\n> Write output files to the specified directory.\n\n**-jobname** _NAME_\n> Set the output file base name.\n\n**-interaction** _MODE_\n> Set interaction mode: batchmode, nonstopmode, scrollmode, errorstopmode.\n\n**-shell-escape**\n> Enable \\\\write18 shell command execution.\n\n**-no-shell-escape**\n> Disable shell command execution (default).\n\n**-synctex** _N_\n> Generate SyncTeX data for editor synchronization.\n\n**-halt-on-error**\n> Stop processing at the first error.\n\n**-file-line-error**\n> Show file name and line number in error messages.\n\n**-no-pdf**\n> Generate XDV (XeTeX DeVice-independent) output rather than PDF.\n\n**-output-driver** _CMD_\n> Use CMD as the XDV-to-PDF driver instead of the default xdvipdfmx.\n\n**-8bit**\n> Make all characters printable by default.\n\n**--help**\n> Display help information.\n\n**--version**\n> Display version information."],["DESCRIPTION","**xelatex** compiles LaTeX documents using the XeTeX engine, producing PDF output. It provides native Unicode (UTF-8) support and can use any system-installed OpenType or TrueType fonts directly via the **fontspec** package.\n\nUnlike pdfLaTeX, xelatex handles non-Latin scripts and complex typography without special encoding packages. Documents can include characters from any writing system directly. Internally, xelatex first produces an XDV (XeTeX DeVice-independent) file, then converts it to PDF using xdvipdfmx.\n\nMultiple compilation passes may be needed for cross-references, bibliographies, and indices. Build systems like **latexmk** automate this process."],["CAVEATS","Some packages designed for pdfLaTeX may be incompatible. Compilation is slightly slower than pdfLaTeX due to the two-stage XDV-to-PDF conversion. Microtype support is more limited than in pdfLaTeX or LuaLaTeX. Part of the TeX Live distribution."],["HISTORY","**XeTeX** was created by Jonathan Kew, initially for Mac OS X in **2004**. It was ported to Linux in **2006** and included in TeX Live **2007**. XeTeX brought modern font technology and Unicode support to TeX, making it practical for multilingual and professionally typeset documents."],["INSTALL","```dnf: sudo dnf install texlive-xetex```\n\n```pacman: sudo pacman -S texlive-xetex```\n\n```apk: sudo apk add texlive-xetex```\n\n```zypper: sudo zypper install texlive-xetex```"],["SEE ALSO","[xetex](/man/xetex)(1), [pdflatex](/man/pdflatex)(1), [lualatex](/man/lualatex)(1), [latexmk](/man/latexmk)(1), [biber](/man/biber)(1)"]],"e":["xetex","pdflatex","lualatex","latexmk","biber"],"x":"xelatex [document.tex]"},"xev":{"n":"xev","t":"X11 event monitoring tool","s":[["TAGLINE","X11 event monitoring tool"],["TLDR","**Start xev**\n\n```xev```\n\n**Show only keyboard events**\n\n```xev -event keyboard```\n\n**Show only mouse button** events\n\n```xev -event button```\n\n**Monitor specific window** by ID\n\n```xev -id [window_id]```\n\n**Monitor root window** events\n\n```xev -root```"],["SYNOPSIS","**xev** [_-event type_] [_-id window_] [_-root_] [_options_]"],["PARAMETERS","**-display** _DISPLAY_\n> X server to contact.\n\n**-event** _TYPE_\n> Event type filter (can be specified multiple times).\n\n**-id** _WINDOW_\n> Monitor events on an existing window by ID.\n\n**-root**\n> Monitor events on the root window.\n\n**-name** _NAME_\n> Name to assign to the created window.\n\n**-geometry** _GEOM_\n> Window size and position.\n\n**-bw** _PIXELS_\n> Border width for the window.\n\n**-bs** _TYPE_\n> Backing store type (NotUseful, WhenMapped, Always). Default NotUseful.\n\n**-rv**\n> Display window in reverse video."],["EVENT TYPES","**keyboard** - Key press/release events\n**mouse** - All pointer events\n**button** - Button press/release events\n**motion** - Pointer motion events\n**expose** - Expose events\n**visibility** - Visibility change events\n**structure** - Window structure events\n**substructure** - Substructure events\n**focus** - Focus change events\n**property** - Property change events\n**colormap** - Colormap events\n**owner_grab_button** - Owner grab button events\n**randr** - RandR events"],["DESCRIPTION","**xev** is an X11 event monitoring utility that displays all events received by a window. When launched, it opens a small window and prints detailed information about every X event that occurs within it, including key presses, mouse movements, button clicks, window exposure, and focus changes.\n\nThe tool is primarily used for debugging X11 input issues and discovering keycodes for keyboard configuration. Each event is printed with full details including keycodes, key symbols, coordinates, modifier states, and timestamps. This makes it invaluable for setting up key bindings in window managers or diagnosing input problems.\n\nEvents can be filtered by type using the **-event** option to show only keyboard, mouse, button, or motion events. The **-id** option monitors events on a specific existing window, while **-root** captures events on the root window for display-wide monitoring."],["CAVEATS","X11 only, not Wayland. Creates visible window. Output is verbose."],["HISTORY","**xev** is part of the standard X11 utilities. It's essential for understanding X input events and configuring key bindings."],["INSTALL","```apt: sudo apt install x11-utils```\n\n```dnf: sudo dnf install xev```\n\n```apk: sudo apk add xev```\n\n```zypper: sudo zypper install xev```\n\n```nix: nix profile install nixpkgs#xev```"],["SEE ALSO","[xdotool](/man/xdotool)(1), [xmodmap](/man/xmodmap)(1), [xinput](/man/xinput)(1)"]],"e":["xdotool","xmodmap","xinput"],"x":"xev"},"xml-transform":{"n":"xml-transform","t":"Apply XSLT stylesheets to XML","s":[["TAGLINE","Apply XSLT stylesheets to XML"],["TLDR","**Apply XSLT transformation**\n\n```xml transform [stylesheet.xsl] [input.xml]```\n\n**Transform with parameters**\n\n```xml transform --xinclude -p \"[param=value]\" [stylesheet.xsl] [input.xml]```\n\n**Output to file**\n\n```xml transform [stylesheet.xsl] [input.xml] > [output.xml]```"],["SYNOPSIS","**xml transform** [_options_] _stylesheet_ _file_"],["PARAMETERS","**-p**, **--param** _name=value_\n> Pass parameter to stylesheet.\n\n**-s**, **--stringparam** _name=value_\n> Pass string parameter.\n\n**--xinclude**\n> Process XInclude.\n\n**--html**\n> Input is HTML.\n\n**--omit-decl**\n> Omit XML declaration."],["DESCRIPTION","**xml transform** applies XSLT stylesheets to XML documents. Part of xmlstarlet toolkit. Transforms XML using XSLT 1.0 processor for document conversion and processing."],["INSTALL","```dnf: sudo dnf install xmlstarlet```\n\n```pacman: sudo pacman -S xmlstarlet```\n\n```apk: sudo apk add xmlstarlet```\n\n```zypper: sudo zypper install xmlstarlet```\n\n```brew: brew install xmlstarlet```\n\n```nix: nix profile install nixpkgs#xmlstarlet```"],["SEE ALSO","[xml-select](/man/xml-select)(1), [xsltproc](/man/xsltproc)(1)"]],"e":["xml-select","xsltproc"],"x":"xml transform [stylesheet.xsl] [input.xml]"},"xmount":{"n":"xmount","t":"Virtual disk image format conversion","s":[["TAGLINE","Virtual disk image format conversion"],["TLDR","Mount **raw to DMG**\n\n```xmount --in raw [path/to/image.dd] --out dmg [mountpoint]```\n\nMount **EWF to VHD** with cache\n\n```xmount --cache [path/to/cache.ovl] --in ewf [path/to/image.E??] --out vhd [mountpoint]```\n\nMount **partition** at offset\n\n```xmount --offset 2048 --in raw [path/to/image.dd] --out raw [mountpoint]```"],["SYNOPSIS","**xmount** [_OPTIONS_] --in _FORMAT_ _IMAGE_ --out _FORMAT_ _MOUNTPOINT_"],["PARAMETERS","**--in** _FORMAT_ _IMAGE_\n> Input format and image file (raw, ewf, aff, etc.)\n\n**--out** _FORMAT_\n> Output format (raw, dmg, vhd, vdi, vmdk)\n\n**--cache** _FILE_\n> Write cache file for modifications\n\n**--offset** _SECTORS_\n> Offset to partition in sectors\n\n**--sizelimit** _BYTES_\n> Limit virtual image size"],["DESCRIPTION","**xmount** creates virtual disk images using FUSE, converting between formats on-the-fly. It's useful for forensic analysis, allowing read-only access to disk images in different formats.\n\nWrite cache support enables modifications without altering the original image."],["CAVEATS","Requires FUSE support. Some format conversions may not support all features. Used primarily for forensic and virtualization purposes."],["INSTALL","```apt: sudo apt install xmount```\n\n```dnf: sudo dnf install xmount```\n\n```brew: brew install xmount```"],["SEE ALSO","[mount](/man/mount)(8), [losetup](/man/losetup)(8), [qemu-img](/man/qemu-img)(1)"]],"e":["mount","losetup","qemu-img"],"x":"xmount --in raw [path/to/image.dd] --out dmg [mountpoint]"}});
