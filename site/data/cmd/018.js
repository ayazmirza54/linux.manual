window.LCL_BUCKET(18,{"acl":{"n":"acl","t":"fine-grained file permission management","s":[["TAGLINE","fine-grained file permission management"],["TLDR","**View the ACL** of a file\n\n```getfacl [path/to/file]```\n\n**Set read and write permissions** for a specific user\n\n```setfacl -m u:[username]:rw [path/to/file]```\n\n**Set permissions for a group**\n\n```setfacl -m g:[groupname]:rx [path/to/directory]```\n\n**Remove ACL entry** for a user\n\n```setfacl -x u:[username] [path/to/file]```\n\n**Remove all ACL entries** from a file\n\n```setfacl -b [path/to/file]```\n\n**Set default ACL** for new files in a directory\n\n```setfacl -d -m u:[username]:rwx [path/to/directory]```\n\n**Apply ACL recursively** to a directory\n\n```setfacl -R -m u:[username]:rx [path/to/directory]```\n\n**Copy ACL from one file to another**\n\n```getfacl [source_file] | setfacl --set-file=- [target_file]```"],["SYNOPSIS","**getfacl** [_options_] _file_...\n\n**setfacl** [_options_] _acl_spec_ _file_..."],["PARAMETERS","**-m** _acl_spec_\n> Modify the ACL by adding or updating entries.\n\n**-x** _acl_spec_\n> Remove specified ACL entries.\n\n**-b**\n> Remove all extended ACL entries.\n\n**-d**\n> Apply operations to the default ACL (for directories).\n\n**-R**\n> Apply recursively to directories and their contents.\n\n**-k**\n> Remove the default ACL.\n\n**--set** _acl_spec_\n> Replace the entire ACL with the specified entries.\n\n**--set-file** _file_\n> Read ACL entries from a file (use **-** for stdin).\n\n**-n**\n> Do not recalculate the effective rights mask."],["DESCRIPTION","**Access Control Lists (ACLs)** extend the traditional Unix permission model, allowing fine-grained permissions for multiple users and groups on a single file or directory. While standard permissions support only owner, group, and others, ACLs can specify distinct access rights for any number of named users and groups.\n\nThe **getfacl** command displays ACL entries, while **setfacl** modifies them. ACL entries follow the format **type:name:permissions** where type is **u** (user), **g** (group), **o** (other), or **m** (mask), name identifies the user or group, and permissions are combinations of **r** (read), **w** (write), and **x** (execute).\n\nDefault ACLs on directories determine the initial ACL for new files created within. The ACL mask entry limits the effective permissions granted to named users and groups."],["CAVEATS","Not all filesystems support ACLs; ext4, XFS, and Btrfs do when mounted with ACL support. Some backup and archive tools may not preserve ACLs. Files with ACLs show a **+** after the permission bits in **ls -l** output. NFSv4 uses a different ACL model than POSIX ACLs."],["HISTORY","POSIX ACLs were developed in the **1990s** as part of the POSIX 1003.1e draft standard, which was ultimately withdrawn but widely implemented. Linux ACL support was added to the kernel in version **2.6** (2003). The specification aimed to provide a portable ACL interface across Unix systems, though implementation details vary between operating systems."],["INSTALL","```dnf: sudo dnf install acl```\n\n```pacman: sudo pacman -S acl```\n\n```zypper: sudo zypper install acl```\n\n```brew: brew install acl```\n\n```nix: nix profile install nixpkgs#acl```"],["SEE ALSO","[chmod](/man/chmod)(1), [chown](/man/chown)(1), [getfacl](/man/getfacl)(1), [setfacl](/man/setfacl)(1), [ls](/man/ls)(1)"],["RESOURCES","```[Source code](https://git.savannah.nongnu.org/cgit/acl.git)```\n\n```[Homepage](https://savannah.nongnu.org/projects/acl/)```"]],"e":["chmod","chown","getfacl","setfacl","ls"],"x":"getfacl [path/to/file]"},"airmon-ng":{"n":"airmon-ng","t":"Enable and manage wireless monitor mode","s":[["TAGLINE","Enable and manage wireless monitor mode"],["TLDR","**List** wireless interfaces\n\n```sudo airmon-ng```\n\n**Enable** monitor mode\n\n```sudo airmon-ng start [wlan0]```\n\nEnable on **specific channel**\n\n```sudo airmon-ng start [wlan0] [6]```\n\n**Disable** monitor mode\n\n```sudo airmon-ng stop [wlan0mon]```\n\n**Kill** interfering processes\n\n```sudo airmon-ng check kill```"],["SYNOPSIS","**airmon-ng** [_start_|_stop_|_check_] [_interface_] [_channel_]"],["DESCRIPTION","**airmon-ng** manages wireless interfaces for monitor mode operation. Monitor mode allows capturing all wireless packets in range, not just those addressed to your device, which is required for wireless security auditing.\n\nThe tool can identify and kill processes that might interfere with monitor mode operation, such as NetworkManager and wpa_supplicant."],["PARAMETERS","**start** _interface_\n> Enable monitor mode on interface\n\n**stop** _interface_\n> Disable monitor mode\n\n**check**\n> List processes that may interfere\n\n**check kill**\n> Kill interfering processes\n\n**channel**\n> Lock interface to specific channel"],["OUTPUT","Without arguments, **airmon-ng** lists the PHY name, interface name, driver in use, and chipset for each detected wireless device."],["CAVEATS","Enabling monitor mode disconnects from any connected network. Some drivers don't support monitor mode. Killing interfering processes may disable normal WiFi connectivity until restarted."],["HISTORY","**airmon-ng** is part of the aircrack-ng suite, evolved from earlier airmon scripts. The \"-ng\" version was released in **2006** with improved driver support and process management."],["INSTALL","```apt: sudo apt install aircrack-ng```\n\n```dnf: sudo dnf install aircrack-ng```\n\n```pacman: sudo pacman -S aircrack-ng```\n\n```apk: sudo apk add aircrack-ng```\n\n```zypper: sudo zypper install aircrack-ng```\n\n```brew: brew install aircrack-ng```\n\n```nix: nix profile install nixpkgs#aircrack-ng```"],["SEE ALSO","[aircrack-ng](/man/aircrack-ng)(1), [airodump-ng](/man/airodump-ng)(1), [aireplay-ng](/man/aireplay-ng)(1), [iw](/man/iw)(8), [iwconfig](/man/iwconfig)(8)"],["RESOURCES","```[Source code](https://github.com/aircrack-ng/aircrack-ng)```\n\n```[Homepage](https://www.aircrack-ng.org/)```\n\n```[Documentation](https://www.aircrack-ng.org/documentation.html)```"]],"e":["aircrack-ng","airodump-ng","aireplay-ng","iw","iwconfig"],"x":"sudo airmon-ng"},"alr-help":{"n":"alr-help","t":"Display help for Alire package manager commands.","s":[["TAGLINE","Display help for Alire package manager commands."],["TLDR","**Display help** for a specific alr command\n\n```alr help [command]```\n\n**List all available** alr commands\n\n```alr help```\n\n**Display global options** and general help\n\n```alr --help```"],["SYNOPSIS","**alr** **help** [_command_]"],["PARAMETERS","**command**\n> The name of the alr subcommand to get help for."],["DESCRIPTION","**alr help** displays detailed help information for Alire (Ada/SPARK package manager) commands. When run without arguments, it shows a list of all available commands. When a command name is provided, it displays detailed usage information for that specific command.\n\nAlire is a source package manager for Ada/SPARK, similar to Rust's cargo or OCaml's opam. The **alr** command-line tool manages dependencies, builds projects, and incorporates libraries from the Alire catalog."],["INSTALL","```apt: sudo apt install alire```\n\n```aur: yay -S alire```\n\n```nix: nix profile install nixpkgs#alire```"],["SEE ALSO","[alr](/man/alr)(1), [alr-version](/man/alr-version)(1)"],["RESOURCES","```[Source code](https://github.com/alire-project/alire)```\n\n```[Homepage](https://alire.ada.dev/)```\n\n```[Documentation](https://alire.ada.dev/docs/)```"]],"e":["alr","alr-version"],"x":"alr help [command]"},"ansible-doc":{"n":"ansible-doc","t":"Display documentation for Ansible modules and plugins","s":[["TAGLINE","Display documentation for Ansible modules and plugins"],["TLDR","**Show** documentation for a module\n\n```ansible-doc [apt]```\n\n**List** all available modules\n\n```ansible-doc -l```\n\nList modules matching **pattern**\n\n```ansible-doc -l | grep [docker]```\n\nShow **snippet** for module usage\n\n```ansible-doc -s [yum]```\n\nShow documentation for **collection** module\n\n```ansible-doc [community.docker.docker_container]```\n\nList **plugins** of a type\n\n```ansible-doc -t [callback] -l```"],["SYNOPSIS","**ansible-doc** [_-l_] [_-s_] [_-t type_] [_module_]"],["DESCRIPTION","**ansible-doc** displays documentation for Ansible modules, plugins, and collections. It shows module parameters, return values, examples, and notes, serving as a quick reference without needing to access online documentation.\n\nThe tool can list all available modules and filter by plugin type, making it useful for discovering available automation options."],["PARAMETERS","**-l** [_filter_], **--list** [_filter_]\n> List available plugins; optional filter can be a namespace or collection name\n\n**-s**, **--snippet**\n> Show a playbook snippet (for inventory, lookup, and module plugin types)\n\n**-t** _type_, **--type** _type_\n> Plugin type to look up (default: module). Choices include module, become, cache, callback, cliconf, connection, httpapi, inventory, lookup, netconf, shell, vars, strategy, test, filter, role, keyword\n\n**-F**, **--list_files**\n> Show plugin names and their source files (implies --list)\n\n**-e** _entry_point_, **--entry-point** _entry_point_\n> Select the entry point for a role (with -t role)\n\n**-j**, **--json**\n> Change output into JSON format\n\n**-M** _path_, **--module-path** _path_\n> Prepend colon-separated path(s) to the module library"],["CONFIGURATION","**ansible.cfg**\n> Main Ansible configuration file, searched in current directory, ~/.ansible.cfg, or /etc/ansible/ansible.cfg.\n\n**ANSIBLE_CONFIG**\n> Environment variable to specify an alternate configuration file path."],["CAVEATS","Documentation quality varies between core and community modules. Some modules may have outdated documentation. Collection modules use fully qualified names."],["HISTORY","**ansible-doc** has been part of Ansible since early versions, providing offline access to module documentation. Collections support was added with Ansible 2.9 in **2019**."],["INSTALL","```dnf: sudo dnf install ansible-core```\n\n```pacman: sudo pacman -S ansible-core```\n\n```apk: sudo apk add ansible-core```\n\n```zypper: sudo zypper install ansible-core```"],["SEE ALSO","[ansible](/man/ansible)(1), [ansible-galaxy](/man/ansible-galaxy)(1), [ansible-playbook](/man/ansible-playbook)(1)"],["RESOURCES","```[Source code](https://github.com/ansible/ansible)```\n\n```[Documentation](https://docs.ansible.com/ansible/latest/cli/ansible-doc.html)```"]],"e":["ansible","ansible-galaxy","ansible-playbook"],"x":"ansible-doc [apt]"},"apptainer-inspect":{"n":"apptainer-inspect","t":"Display metadata from container images.","s":[["TAGLINE","Display metadata from container images."],["TLDR","Show the **labels** of an image (default)\n\n```apptainer inspect [path/to/image.sif]```\n\nShow the **definition file** used to build the image\n\n```apptainer inspect -d [path/to/image.sif]```\n\nShow the **runscript** for the image\n\n```apptainer inspect -r [path/to/image.sif]```\n\nShow the **environment variables** of the image\n\n```apptainer inspect -e [path/to/image.sif]```\n\nShow the **startscript** for the image\n\n```apptainer inspect -s [path/to/image.sif]```\n\nInspect a **specific app** within the container\n\n```apptainer inspect --app [app_name] [path/to/image.sif]```\n\n**List all apps** in the container\n\n```apptainer inspect --list-apps [path/to/image.sif]```\n\nShow **all available data** in JSON format\n\n```apptainer inspect --all [path/to/image.sif]```"],["SYNOPSIS","**apptainer inspect** [_inspect options..._] _image_path_"],["DESCRIPTION","**apptainer inspect** displays metadata embedded in Apptainer container images. This includes labels, the definition file used to build the container, runscripts, environment settings, and information about bundled apps.\n\nThe metadata provides valuable information for understanding container provenance, reproducibility, and intended usage."],["PARAMETERS","**-l, --labels**\n> Show the labels for the image (default behavior)\n\n**-d, --deffile**\n> Display the definition file used to build the container\n\n**-r, --runscript**\n> Display the container's runscript\n\n**-s, --startscript**\n> Display the startscript for the image\n\n**-t, --test**\n> Display the test script for the image\n\n**-e, --environment**\n> Display environment variables set in the container\n\n**-H, --helpfile**\n> Display the runscript helpfile, if it exists\n\n**--app** _name_\n> Inspect a specific SCIF application within the container\n\n**--list-apps**\n> List all SCIF apps installed in the container\n\n**--all**\n> Display all available metadata (implies --json)\n\n**-j, --json**\n> Output results in JSON format"],["INSTALL","```dnf: sudo dnf install apptainer```\n\n```pacman: sudo pacman -S apptainer```\n\n```apk: sudo apk add apptainer```\n\n```zypper: sudo zypper install apptainer```\n\n```brew: brew install apptainer```\n\n```nix: nix profile install nixpkgs#apptainer```"],["SEE ALSO","[apptainer](/man/apptainer)(1), [apptainer-build](/man/apptainer-build)(1), [apptainer-run](/man/apptainer-run)(1), [apptainer-exec](/man/apptainer-exec)(1)"]],"e":["apptainer","apptainer-build","apptainer-run","apptainer-exec"],"x":"apptainer inspect [path/to/image.sif]"},"ark-survival-evolved":{"n":"ark-survival-evolved","t":"Host a dedicated game server for dinosaur survival multiplayer.","s":[["TAGLINE","Host a dedicated game server for dinosaur survival multiplayer."],["TLDR","Start the server with a **specific map**\n\n```[path/to]/ShooterGameServer [TheIsland]```\n\nStart with **session name, passwords**\n\n```[path/to]/ShooterGameServer [TheIsland]?SessionName=[name]?ServerPassword=[pass]?ServerAdminPassword=[admin_pass]```\n\nStart with **specific port** and max player count\n\n```[path/to]/ShooterGameServer [TheIsland]?Port=[7777]?MaxPlayers=[1..70]```\n\nEnable **PvE mode** (disable PvP)\n\n```[path/to]/ShooterGameServer [TheIsland]?ServerPVE=true```\n\nSet **difficulty multiplier**\n\n```[path/to]/ShooterGameServer [TheIsland]?DifficultyOffset=[1.0]```\n\nEnable a **specific event**\n\n```[path/to]/ShooterGameServer [TheIsland] -ActiveEvent=[Summer]```\n\nEnable **crossplay** between Steam and Epic Games\n\n```[path/to]/ShooterGameServer [TheIsland] -crossplay -PublicIPForEpic=[ip_address]```"],["SYNOPSIS","**ShooterGameServer** _map_ [_?options_] [_-flags_]"],["DESCRIPTION","**ShooterGameServer** runs a headless dedicated server for ARK: Survival Evolved, a dinosaur-themed survival game. It allows hosting multiplayer servers with configurable maps, gameplay settings, and mod support.\n\nServer options use URL-style query parameters (joined with **?**) for gameplay settings and command-line flags (prefixed with **-**) for technical configuration."],["PARAMETERS","**SessionName=**_name_\n> Set the server's display name in the server browser\n\n**ServerPassword=**_password_\n> Require a password to join the server\n\n**ServerAdminPassword=**_password_\n> Set the admin password for RCON and in-game admin commands\n\n**ServerPVE=true**\n> Enable PvE mode, disabling player versus player combat\n\n**DifficultyOffset=**_value_\n> Scale difficulty (affects wild creature levels)\n\n**Port=**_number_\n> Network port for the server\n\n**MaxPlayers=**_count_\n> Maximum number of players (1-70)\n\n**-ActiveEvent=**_event_\n> Enable a specific seasonal event\n\n**-automanagedmods**\n> Enable automatic mod downloading and updating (Steam only)\n\n**-crossplay**\n> Enable crossplay between Steam and Epic Games\n\n**-clusterid=**_id_, **-ClusterDirOverride=**_path_\n> Join a server cluster (shared player/dino transfers) using the given cluster ID and shared data directory\n\n**-NoBattlEye**\n> Run the server without BattlEye anti-cheat (clients must also disable it)"],["CONFIGURATION","**/ShooterGame/Saved/Config/LinuxServer/GameUserSettings.ini**\n> Main server configuration including session name, passwords, ports, rates, and gameplay toggles.\n\n**/ShooterGame/Saved/Config/LinuxServer/Game.ini**\n> Advanced gameplay overrides including engram points, loot tables, and per-level stat multipliers."],["CAVEATS","Requires significant system resources (8GB+ RAM recommended). Server files must be installed via SteamCMD. Mods require Steam Workshop integration. Configuration persistence requires editing GameUserSettings.ini and Game.ini files."],["SEE ALSO","[steamcmd](/man/steamcmd)(1), [ark-survival-ascended](/man/ark-survival-ascended)(1)"]],"e":["steamcmd","ark-survival-ascended"],"x":"[path/to]/ShooterGameServer [TheIsland]"},"arkade":{"n":"arkade","t":"Marketplace CLI to install developer tools and Kubernetes apps","s":[["TAGLINE","Marketplace CLI to install developer tools and Kubernetes apps"],["TLDR","**Download** popular CLIs into PATH\n\n```arkade get [kubectl] [kind] [terraform]```\n\n**List** available CLI packages\n\n```arkade get --help```\n\n**Install** a Kubernetes app (OpenFaaS example)\n\n```arkade install [openfaas]```\n\n**Install** a system package (e.g. Go)\n\n```arkade system install [go]```\n\n**Install** a binary from an OCI image\n\n```arkade oci install [ghcr.io/org/tool]```"],["SYNOPSIS","**arkade** *command* [*args*]"],["DESCRIPTION","**arkade** installs pinned versions of common developer CLIs and Kubernetes applications with simple commands. **arkade get** downloads static binaries (kubectl, helm, terraform, jq, …) without package-manager delays; **arkade install** deploys charts/apps to a cluster; **arkade system** and **arkade oci** cover host packages and image-packaged tools. Maintained by Alex Ellis / OpenFaaS community."],["PARAMETERS","**get** *tool*[@version]...\n\n> Download CLI binaries to the arkade bin directory (often **$HOME/.arkade/bin**).\n\n**install** *app*\n\n> Install a Kubernetes application with sensible defaults and flags.\n\n**uninstall** *app*\n\n> Remove a previously installed app.\n\n**system install** *package*\n\n> Install host-level tools (Go, containerd, …).\n\n**oci install** *image* [*path*]\n\n> Extract/install from an OCI container image.\n\n**info** / **chart** / **version**\n\n> Introspection helpers—see **arkade --help**."],["CAVEATS","Downloading binaries from the internet requires trust in upstream release artifacts. Kubernetes installs need a working kubeconfig. Keep **$HOME/.arkade/bin** on your PATH."],["INSTALL","```zypper: sudo zypper install arkade```\n\n```brew: brew install arkade```\n\n```nix: nix profile install nixpkgs#arkade```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [helm](/man/helm)(1), [kind](/man/kind)(1)"],["RESOURCES","```[Source code](https://github.com/alexellis/arkade)```\n\n```[Homepage](https://arkade.dev)```"]],"e":["kubectl","helm","kind"],"x":"arkade get [kubectl] [kind] [terraform]"},"arping":{"n":"arping","t":"Send ARP requests to discover and test network hosts","s":[["TAGLINE","Send ARP requests to discover and test network hosts"],["TLDR","**Ping a host using ARP**\n\n```sudo arping [192.168.1.1]```\n\n**Ping using a specific interface**\n\n```sudo arping -I [eth0] [192.168.1.1]```\n\n**Send a specific number of requests**\n\n```sudo arping -c [5] [192.168.1.1]```\n\n**Detect duplicate IP addresses on the network**\n\n```sudo arping -D [192.168.1.1]```\n\n**Quit after first reply** (check if host is alive)\n\n```sudo arping -f [192.168.1.1]```"],["SYNOPSIS","**arping** [_-c count_] [_-I interface_] [_-D_] _destination_"],["DESCRIPTION","**arping** sends ARP requests to a target host and displays responses. Unlike ICMP ping, ARP operates at layer 2 and cannot be blocked by IP firewalls.\n\nThe tool is useful for verifying layer 2 connectivity, finding MAC addresses, and detecting duplicate IP addresses on a network."],["PARAMETERS","**-c** _count_\n> Stop after sending _count_ ARP REQUEST packets.\n\n**-I** _interface_\n> Network interface to send ARP requests on.\n\n**-D**\n> Duplicate address detection mode (DAD). See RFC 2131.\n\n**-A**\n> ARP reply mode (gratuitous ARP reply).\n\n**-U**\n> Unsolicited ARP mode to update neighbours' ARP caches. No replies are expected.\n\n**-b**\n> Send only MAC-level broadcasts; do not switch to unicast after reply.\n\n**-q**\n> Quiet mode; nothing is displayed.\n\n**-s** _source_\n> Source IP address to use in ARP packets.\n\n**-w** _deadline_\n> Timeout in seconds before exiting regardless of packets sent or received.\n\n**-i** _interval_\n> Interval in seconds between packets.\n\n**-f**\n> Quit after the first reply confirming target is alive.\n\n**-V**\n> Print version and exit."],["CAVEATS","Requires root/sudo for raw socket access. Only works on local network segment. Cannot ping across routers."],["HISTORY","**arping** has existed in various forms since the early **2000s**. The most common implementation is in the **iputils** package maintained as part of Linux networking tools. A separate implementation by Thomas Habets provides additional features."],["INSTALL","```apt: sudo apt install arping```\n\n```apk: sudo apk add iputils-arping```\n\n```brew: brew install arping```\n\n```nix: nix profile install nixpkgs#arping```"],["SEE ALSO","[ping](/man/ping)(8), [arp](/man/arp)(8), [arp-scan](/man/arp-scan)(1), [ip](/man/ip)(8)"]],"e":["ping","arp","arp-scan","ip"],"x":"sudo arping [192.168.1.1]"},"atm":{"n":"atm","t":"Generate and work with MIDI melody files","s":[["TAGLINE","Generate and work with MIDI melody files"],["TLDR","**Show help**\n\n```atm -h```\n\n**Generate a single MIDI** from a pitch sequence\n\n```atm gen single 'C:4,D:4,E:4,F:4,G:4,A:4,B:4,C:5' [test.mid]```\n\n**Brute-force generate** melodies into a gzip tar (depth 2 partitions)\n\n```atm gen tar-gz -p [2] 'C:4,D:4,E:4,F:4,G:4,A:4,B:4,C:5' [8] [output.tar]```\n\n**Locate partition** for a melody\n\n```atm partition -p [2] 'C:4,C:4,C:4,C:4,C:4,C:4,C:4,C:5'```\n\n**Estimate** storage backend sizes\n\n```atm estimate --help```"],["SYNOPSIS","**atm** *subcommand* [*options*] [*args*]"],["DESCRIPTION","**atm** (**atm-cli**) is a command-line tool for generating and working with MIDI files. It was built for All the Music, LLC to help generate large sets of melodies (including brute-force ranges) and to organize output across partitioned storage backends. It uses the **libatm** Rust library.\n\nSubcommands:\n\n- **gen** – generate melodies (single file or bulk backends such as tar/gzip layouts)\n- **partition** – map a pitch sequence to its partition path for a given scheme\n- **estimate** – estimate output size of storage backends\n\nPitch sequences use note:**octave** tokens separated by commas (for example **C:4,D:4,E:4**).\n\nBuild from source: clone the repo, **git submodule update --init**, then **cargo build --release**. Run **cargo run --release -- -h** for usage without installing."],["PARAMETERS","**gen single** *melody* *outfile*\n\n> Write one MIDI file for the given pitch sequence.\n\n**gen** *backend* ...\n\n> Bulk generation into tar, tar-gz, or other backends documented by **atm gen --help**. **-p** sets partition depth (limits files per directory).\n\n**partition** [**-p** *depth*] *melody*\n\n> Print which partition directory holds the melody (default depth 1).\n\n**estimate**\n\n> Help choose storage backends by estimating output size.\n\n**-h**, **--help** / **-V**, **--version**\n\n> Help and version."],["CAVEATS","Tailored for bulk melody generation, not a general DAW or multi-track MIDI editor. Compiling needs a Rust toolchain and submodules. Output volume for long sequences grows factorially; use **estimate** and partitioned backends carefully. License is CC BY 4.0 (not public domain), unlike some ATM datasets."],["SEE ALSO","[timidity](/man/timidity)(1), [fluidsynth](/man/fluidsynth)(1), [sox](/man/sox)(1)"],["RESOURCES","```[Source code](https://github.com/allthemusicllc/atm-cli)```\n\n```[Homepage](http://allthemusic.info)```\n\n```[Documentation](https://allthemusicllc.github.io/atm-cli/atm/index.html)```"]],"e":["timidity","fluidsynth","sox"],"x":"atm -h"},"aws-sqs":{"n":"aws-sqs","t":"Manage Simple Queue Service queues and messages.","s":[["TAGLINE","Manage Simple Queue Service queues and messages."],["TLDR","**Create a new queue**\n\n```aws sqs create-queue --queue-name [queue-name]```\n\n**List all queues**\n\n```aws sqs list-queues```\n\n**Get the URL** of a queue\n\n```aws sqs get-queue-url --queue-name [queue-name]```\n\n**Send a message** to a queue\n\n```aws sqs send-message --queue-url [queue-url] --message-body \"[message content]\"```\n\n**Receive messages** with long polling (waits up to 20s)\n\n```aws sqs receive-message --queue-url [queue-url] --max-number-of-messages [10] --wait-time-seconds [20]```\n\n**Delete a message** after processing\n\n```aws sqs delete-message --queue-url [queue-url] --receipt-handle [handle]```\n\n**Purge all messages** from a queue\n\n```aws sqs purge-queue --queue-url [queue-url]```\n\n**Delete a queue**\n\n```aws sqs delete-queue --queue-url [queue-url]```"],["SYNOPSIS","**aws sqs** _subcommand_ [_options_]"],["DESCRIPTION","**aws sqs** is the AWS CLI interface for Amazon Simple Queue Service (SQS), a fully managed message queuing service for decoupling and scaling distributed systems and serverless applications.\n\nSQS offers two queue types: Standard queues with maximum throughput and at-least-once delivery, and FIFO queues with exactly-once processing and ordered delivery. Messages can be retained for up to 14 days."],["SUBCOMMANDS","**Queue Management**\n> create-queue, delete-queue, list-queues, get-queue-url, get-queue-attributes, set-queue-attributes, purge-queue\n\n**Messaging**\n> send-message, send-message-batch, receive-message, delete-message, delete-message-batch, change-message-visibility, change-message-visibility-batch\n\n**Permissions**\n> add-permission, remove-permission\n\n**Tagging**\n> tag-queue, untag-queue, list-queue-tags\n\n**Dead-letter Queues**\n> list-dead-letter-source-queues, start-message-move-task, cancel-message-move-task, list-message-move-tasks"],["CAVEATS","Messages must be explicitly deleted after processing; SQS does not auto-delete consumed messages. The visibility timeout makes messages temporarily invisible to other consumers while processing. Standard queues may deliver duplicate messages; use FIFO queues for exactly-once processing. Maximum message size is 256KB; use S3 for larger payloads."],["HISTORY","Amazon SQS launched in **July 2006** as one of AWS's first services, predating S3. It was redesigned in **2014** with significant improvements to throughput and latency. FIFO queues were added in **2016** to support ordered message processing."],["INSTALL","```dnf: sudo dnf install aws```\n\n```pacman: sudo pacman -S aws-cli```\n\n```apk: sudo apk add aws-cli```\n\n```zypper: sudo zypper install aws-cli```"],["SEE ALSO","[aws](/man/aws)(1), [aws-sns](/man/aws-sns)(1), [aws-lambda](/man/aws-lambda)(1)"],["RESOURCES","```[Source code](https://github.com/aws/aws-cli)```\n\n```[Homepage](https://aws.amazon.com/cli/)```\n\n```[Documentation](https://docs.aws.amazon.com/cli/latest/reference/sqs/index.html)```"]],"e":["aws","aws-sns","aws-lambda"],"x":"aws sqs create-queue --queue-name [queue-name]"},"az-config":{"n":"az-config","t":"Manage Azure CLI configuration settings","s":[["TAGLINE","Manage Azure CLI configuration settings"],["TLDR","**Get all configurations**\n\n```az config get```\n\n**Get a specific configuration** section\n\n```az config get [core]```\n\n**Get a specific configuration** key\n\n```az config get [core.no_color]```\n\n**Disable color output**\n\n```az config set core.no_color=true```\n\n**Set default resource group** and location\n\n```az config set defaults.group=[MyResourceGroup] defaults.location=[westus2]```\n\n**Enable logging** to file\n\n```az config set logging.enable_log_file=true```\n\n**Unset a configuration**\n\n```az config unset [core.no_color]```\n\n**Set local configuration** for current directory\n\n```az config set defaults.group=[myRG] --local```"],["SYNOPSIS","**az** **config** _subcommand_ [_options_]"],["SUBCOMMANDS","**get**\n> Get a configuration value.\n\n**set**\n> Set a configuration value.\n\n**unset**\n> Unset a configuration value.\n\n**param-persist**\n> Manage parameter persistence.\n\n**param-persist on**\n> Enable parameter persistence.\n\n**param-persist off**\n> Disable parameter persistence.\n\n**param-persist show**\n> Show parameter persistence data.\n\n**param-persist delete**\n> Delete parameter persistence data."],["PARAMETERS","**--local**\n> Apply configuration to local scope (working directory) instead of global."],["DESCRIPTION","**az config** manages Azure CLI configuration settings. It allows customizing CLI behavior including output formatting, default values, telemetry, logging, and error display. Configuration can be set globally or locally per directory.\n\nThe default configuration file is stored at ~/.azure/config."],["CONFIGURATION","**~/.azure/config**\n> Global Azure CLI configuration file with INI-format sections for core, defaults, logging, and other settings.\n\n**.azure/config**\n> Local directory-scoped configuration file, created with the --local flag, overriding global settings for the current project."],["CAVEATS","This command group is marked **experimental** and under development (not deprecated); behavior and options may change. It has been available since Azure CLI 2.10.0. Local configurations are stored in the current working directory and take precedence over the global config."],["INSTALL","```dnf: sudo dnf install azure-cli```\n\n```pacman: sudo pacman -S azure-cli```\n\n```zypper: sudo zypper install azure-cli```\n\n```brew: brew install azure-cli```\n\n```nix: nix profile install nixpkgs#azure-cli```"],["SEE ALSO","[az](/man/az)(1), [az-appconfig](/man/az-appconfig)(1)"],["RESOURCES","```[Source code](https://github.com/Azure/azure-cli)```\n\n```[Documentation](https://learn.microsoft.com/en-us/cli/azure/config)```"]],"e":["az","az-appconfig"],"x":"az config get"},"az-tui":{"n":"az-tui","t":"Terminal UI for managing Azure Container Apps","s":[["TAGLINE","Terminal UI for managing Azure Container Apps"],["TLDR","**Launch the TUI** connected to Azure\n\n```az-tui```\n\n**Launch with mock data** for testing without Azure connectivity\n\n```az-tui --mock```\n\n**Restrict to a specific resource group** before launching\n\n```ACA_RG=\"my-resource-group\" az-tui```\n\n**Display the version** and exit\n\n```az-tui -version```"],["SYNOPSIS","**az-tui** [_options_]"],["PARAMETERS","**-mock**, **-m**\n> Use mock data instead of connecting to Azure CLI\n\n**-version**\n> Print version information and exit"],["DESCRIPTION","**az-tui** is a terminal-based user interface for managing Azure Container Apps, inspired by **K9s**. It provides a keyboard-driven interface for browsing, inspecting, and debugging container applications directly from the terminal without needing the Azure portal.\n\nThe tool follows a hierarchical navigation model: resource groups, container apps, revisions, containers, and environment variables. Users can view detailed application metadata in JSON format, inspect revisions with active status and traffic allocation percentages, stream container logs in real-time, and execute commands within running containers for troubleshooting.\n\nWritten in Go using the Bubble Tea TUI framework, **az-tui** communicates with Azure through the Azure CLI. Navigation uses VIM-inspired key bindings: **/** to filter, **:** to switch contexts, **Enter** to drill down, **Esc** to go back, and **q** to quit. Mode-specific bindings include **r** to refresh, **R** to restart a revision, **l** to view logs, **s** to open a shell, and **v** to display environment variables.\n\nA mock mode (**--mock**) provides synthetic data for development and testing without requiring Azure credentials."],["CONFIGURATION","The **ACA_RG** environment variable can be set to restrict browsing to a specific Azure resource group. If unset, all resource groups across the subscription are displayed."],["CAVEATS","Requires the Azure CLI to be installed and authenticated via **az login**. The Azure Container Apps extension must be installed (**az extension add -n containerapp**). Only manages Azure Container Apps; other Azure services are not supported. The project is in early development."],["HISTORY","**az-tui** was created by **IAL32** and released as an open-source project under the MIT license on GitHub in **August 2024**. It is written in Go and built on the Charm ecosystem (Bubble Tea, Bubbles, Lip Gloss)."],["INSTALL","```dnf: sudo dnf install azure-cli```\n\n```pacman: sudo pacman -S azure-cli```\n\n```zypper: sudo zypper install azure-cli```\n\n```brew: brew install azure-cli```\n\n```nix: nix profile install nixpkgs#azure-cli```"],["SEE ALSO","[az](/man/az)(1), [k9s](/man/k9s)(1), [docker](/man/docker)(1)"]],"e":["az","k9s","docker"],"x":"az-tui"},"boltd":{"n":"boltd","t":"Thunderbolt device management daemon","s":[["TAGLINE","Thunderbolt device management daemon"],["TLDR","**Start the bolt daemon**\n\n```boltd```\n\n**Start in foreground** with debug output\n\n```boltd --replace```\n\n**Check bolt daemon version**\n\n```boltctl --version```\n\n**List connected Thunderbolt devices**\n\n```boltctl list```\n\n**Authorize a device**\n\n```boltctl authorize [device_uuid]```"],["SYNOPSIS","**boltd** [_options_]"],["DESCRIPTION","**boltd** is the system daemon that manages Thunderbolt 3 and USB4 devices on Linux. It handles device authorization, security policies, and stores information about known devices to enable automatic authorization on reconnection.\n\nThunderbolt security levels range from allowing all connections to requiring user authorization for each device. boltd interfaces with the kernel's Thunderbolt subsystem and provides a D-Bus API for device management.\n\nThe daemon is typically started automatically by systemd. Users interact with it through **boltctl**, the command-line interface for managing Thunderbolt devices."],["PARAMETERS","**--replace**\n> Replace existing daemon instance.\n\n**--version**\n> Show version information."],["CAVEATS","Requires appropriate kernel support for Thunderbolt. Security level is set in BIOS/UEFI and may restrict what boltd can authorize. Devices must be physically connected before authorization. Some systems may need Thunderbolt enabled in BIOS."],["HISTORY","**boltd** was developed by **Christian Kellner** at **Red Hat** starting in **2017** to provide Thunderbolt device management on Linux. It was created in response to the introduction of Thunderbolt 3 and its security model that requires explicit device authorization. The project is part of the freedesktop.org ecosystem and has been integrated into major Linux distributions."],["SEE ALSO","[boltctl](/man/boltctl)(1), [udevadm](/man/udevadm)(8)"]],"e":["boltctl","udevadm"],"x":"boltd"},"bootctl":{"n":"bootctl","t":"Control EFI firmware boot settings and systemd-boot","s":[["TAGLINE","Control EFI firmware boot settings and systemd-boot"],["TLDR","Show **firmware and bootloader** information\n\n```bootctl```\n\nList available **bootloader entries**\n\n```bootctl list```\n\nSet flag to **boot into firmware** on next boot\n\n```sudo bootctl reboot-to-firmware true```\n\nSpecify **EFI system partition** path\n\n```bootctl --esp-path /boot/efi/```\n\n**Install** systemd-boot\n\n```sudo bootctl install```\n\n**Update** systemd-boot\n\n```sudo bootctl update```\n\n**Remove** systemd-boot\n\n```sudo bootctl remove```\n\nSet **default boot entry**\n\n```sudo bootctl set-default entry_id```"],["SYNOPSIS","**bootctl** [_OPTIONS_] _COMMAND_"],["DESCRIPTION","**bootctl** controls EFI firmware boot settings and manages the systemd-boot boot loader. It can install, update, and remove systemd-boot, and control boot entries and firmware settings."],["COMMANDS","**status**\n> Show firmware and bootloader status (default)\n\n**list**\n> List available boot entries\n\n**install**\n> Install systemd-boot to EFI system partition\n\n**update**\n> Update systemd-boot in EFI system partition\n\n**remove**\n> Remove systemd-boot from EFI system partition\n\n**is-installed**\n> Check if systemd-boot is installed\n\n**random-seed**\n> Initialize random seed in ESP\n\n**systemd-efi-options [STRING]**\n> Get/set systemd.* EFI options\n\n**reboot-to-firmware [BOOL]**\n> Get/set reboot-to-firmware flag\n\n**set-default ID**\n> Set default boot entry\n\n**set-oneshot ID**\n> Set one-time boot entry\n\n**set-timeout SECS**\n> Set boot menu timeout\n\n**set-timeout-oneshot SECS**\n> Set one-time boot menu timeout"],["PARAMETERS","**--esp-path PATH**\n> Path to EFI system partition\n\n**--boot-path PATH**\n> Path to $BOOT partition\n\n**-p, --print-esp-path**\n> Print ESP path and exit\n\n**-x, --print-boot-path**\n> Print boot partition path and exit\n\n**--no-variables**\n> Do not touch EFI variables\n\n**--graceful**\n> Ignore errors for operations\n\n**--make-entry-directory**\n> Create entry directory during install"],["CAVEATS","Requires an EFI system partition. The default ESP paths checked are /efi/, /boot/, and /boot/efi/. On non-EFI systems, bootctl has limited functionality."],["HISTORY","**bootctl** is part of **systemd**, managing the systemd-boot EFI boot loader as an alternative to GRUB."],["INSTALL","```apt: sudo apt install systemd-boot-tools```"],["SEE ALSO","[kernel-install](/man/kernel-install)(8)"]],"e":["kernel-install"],"x":"bootctl"},"bshell":{"n":"bshell","t":"Bourne Shell interpreter","s":[["TAGLINE","Bourne Shell interpreter"],["TLDR","**Start an interactive Bourne shell**\n\n```bshell```\n\n**Execute a shell script**\n\n```bshell [path/to/script.sh]```\n\n**Execute a command**\n\n```bshell -c \"[command]\"```\n\n**Run in debug mode showing executed commands**\n\n```bshell -x [path/to/script.sh]```\n\n**Run as a restricted shell**\n\n```bshell -r```"],["SYNOPSIS","**bshell** [**-ruvx**] [**-c** _command_] [_script_] [_arguments..._]"],["DESCRIPTION","**bshell** is a Bourne Shell interpreter from the Schily-Tools collection, providing a traditional POSIX-compatible shell environment. It executes commands read from standard input, command strings, or script files.\n\nThe Bourne Shell is the original Unix shell written by Stephen Bourne at Bell Labs. This implementation maintains compatibility with the classic shell behavior while running on modern systems."],["PARAMETERS","**-c** _command_\n> Execute the specified command string\n\n**-r**\n> Start as a restricted shell, limiting certain operations for security\n\n**-u**\n> Treat unset variables as errors and exit\n\n**-v**\n> Print shell input lines as they are read (verbose mode)\n\n**-x**\n> Print commands and arguments as they are executed (debug mode)"],["CAVEATS","The **bshell** command is part of the Schily-Tools package and may not be installed by default on most Linux distributions. Most systems use **bash** or **dash** as **/bin/sh** for Bourne shell compatibility."],["HISTORY","The original Bourne Shell was developed by **Stephen Bourne** at Bell Labs and first appeared in **Version 7 Unix in 1979**. It replaced the Thompson shell and became the foundation for subsequent shells including **ksh**, **bash**, and **zsh**. The Schily-Tools implementation by Jörg Schilling provides a modern port of the classic Bourne Shell."],["INSTALL","```apt: sudo apt install avahi-ui-utils```\n\n```dnf: sudo dnf install avahi-ui-tools```\n\n```apk: sudo apk add avahi-ui-tools```"],["SEE ALSO","[sh](/man/sh)(1), [bash](/man/bash)(1), [dash](/man/dash)(1), [ksh](/man/ksh)(1)"],["RESOURCES","```[Source code](https://sourceforge.net/projects/schilytools/)```"]],"e":["sh","bash","dash","ksh"],"x":"bshell"},"calibre-server":{"n":"calibre-server","t":"Serve ebook libraries over the network","s":[["TAGLINE","Serve ebook libraries over the network"],["TLDR","**Start content server with library**\n\n```calibre-server [/path/to/library]```\n\n**Start on specific port**\n\n```calibre-server --port [8080] [/path/to/library]```\n\n**Enable authentication**\n\n```calibre-server --enable-auth --userdb [users.sqlite] [/path/to/library]```\n\n**Manage user accounts**\n\n```calibre-server --manage-users --userdb [users.sqlite]```\n\n**Enable local write access**\n\n```calibre-server --enable-local-write [/path/to/library]```\n\n**Serve multiple libraries**\n\n```calibre-server [/library1] [/library2]```\n\n**Advertise via Bonjour**\n\n```calibre-server --enable-use-bonjour [/path/to/library]```"],["SYNOPSIS","**calibre-server** [_options_] [_library_path_...]"],["DESCRIPTION","**calibre-server** exposes Calibre ebook libraries over the network. Users can access, browse, and read books directly in a web browser. The server supports OPDS feeds, offline caching, and authentication."],["PARAMETERS","**--port**=_number_\n> Port to listen on (default: 8080)\n\n**--listen-on**=_address_\n> Interface to bind to (default: all)\n\n**--enable-auth**\n> Require password authentication\n\n**--userdb**=_path_\n> Path to SQLite user database\n\n**--manage-users**\n> Interactive user management mode\n\n**--enable-local-write**\n> Allow local clients to modify library\n\n**--enable-use-bonjour**\n> Advertise OPDS via Bonjour/mDNS\n\n**--url-prefix**=_prefix_\n> URL path prefix for reverse proxy\n\n**--daemonize**\n> Run as background daemon\n\n**--pidfile**=_path_\n> Write process ID to file"],["FEATURES","**OPDS Feeds**\n> Compatible with OPDS-based reading apps\n\n**Offline Reading**\n> Browser caches books for offline access\n\n**Remote Access**\n> Access library from any device on network"],["CAVEATS","Enable authentication before exposing to internet. On macOS, binary is inside calibre.app bundle. Use --enable-local-write for calibredb remote access."],["INSTALL","```dnf: sudo dnf install calibre```\n\n```pacman: sudo pacman -S calibre```\n\n```zypper: sudo zypper install calibre```\n\n```nix: nix profile install nixpkgs#calibre```"],["SEE ALSO","[calibredb](/man/calibredb)(1), [calibre](/man/calibre)(1), [ebook-convert](/man/ebook-convert)(1)"],["RESOURCES","```[Source code](https://github.com/kovidgoyal/calibre)```\n\n```[Homepage](https://calibre-ebook.com/)```\n\n```[Documentation](https://manual.calibre-ebook.com/)```"]],"e":["calibredb","calibre","ebook-convert"],"x":"calibre-server [/path/to/library]"},"clang-format":{"n":"clang-format","t":"automatic source code formatter for C-family languages","s":[["TAGLINE","automatic source code formatter for C-family languages"],["TLDR","**Format file and output to stdout**\n\n```clang-format [file.cpp]```\n\n**Format file in place**\n\n```clang-format -i [file.cpp]```\n\n**Format with specific style**\n\n```clang-format --style=[Google] [file.cpp]```\n\n**Format using project config file**\n\n```clang-format --style=file [file.cpp]```\n\n**Format code from stdin**\n\n```echo \"int main(){}\" | clang-format```\n\n**Generate style config file**\n\n```clang-format --style=[LLVM] --dump-config > [.clang-format]```\n\n**Format specific lines only**\n\n```clang-format --lines=[10:20] [file.cpp]```\n\n**Check if file is formatted**\n\n```clang-format --dry-run --Werror [file.cpp]```"],["SYNOPSIS","**clang-format** [_options_] [_file_...]"],["DESCRIPTION","**clang-format** is an automatic code formatting tool for C, C++, Java, JavaScript, JSON, Objective-C, Protobuf, and C# source files. It is part of the LLVM/Clang project and formats code according to configurable style rules.\n\nThe tool ships with several predefined styles (LLVM, Google, Chromium, Mozilla, WebKit, Microsoft, GNU) and supports extensive customization through **.clang-format** configuration files. When using **--style=file**, it searches parent directories for the nearest configuration file.\n\nclang-format integrates with most major editors and CI/CD pipelines. The **--dry-run** option with **--Werror** enables enforcement of formatting standards in automated checks without modifying files."],["PARAMETERS","**-i**\n> Edit files in place\n\n**--style**=_style_\n> Coding style (LLVM, Google, Chromium, Mozilla, WebKit, Microsoft, file)\n\n**--dump-config**\n> Dump configuration to stdout\n\n**--assume-filename**=_name_\n> Filename for language detection when reading stdin\n\n**--lines**=_start:end_\n> Format only specified line range\n\n**--dry-run**\n> Don't write changes, exit with error if changes needed\n\n**--Werror**\n> Treat formatting issues as errors\n\n**--fallback-style**=_style_\n> Style when no .clang-format found\n\n**--verbose**\n> Show files being processed"],["CONFIGURATION","Create **.clang-format** or **_clang-format** in project root:\n\n```yaml\nBasedOnStyle: Google\nIndentWidth: 4\nColumnLimit: 100\n```"],["PREDEFINED STYLES","LLVM, Google, Chromium, Mozilla, WebKit, Microsoft, GNU"],["EDITOR INTEGRATION","Integrations available for VS Code, CLion, Vim, Emacs, Sublime Text, and most major editors."],["CAVEATS","Style \"file\" uses nearest .clang-format in parent directories. Some formatting choices are opinionated and may require configuration adjustments."],["INSTALL","```apt: sudo apt install clang-format```\n\n```apk: sudo apk add clang22-extra-tools```\n\n```brew: brew install clang-format```"],["SEE ALSO","[clang](/man/clang)(1), [clang-tidy](/man/clang-tidy)(1)"],["RESOURCES","```[Source code](https://github.com/llvm/llvm-project)```\n\n```[Documentation](https://clang.llvm.org/docs/ClangFormat.html)```"]],"e":["clang","clang-tidy"],"x":"clang-format [file.cpp]"},"cloudflare-speed-cli":{"n":"cloudflare-speed-cli","t":"Cloudflare-powered internet speed test CLI","s":[["TAGLINE","Cloudflare-powered internet speed test CLI"],["TLDR","**Run** a speed test\n\n```speed-cloudflare-cli```\n\n**Run** without installing via npx\n\n```npx speed-cloudflare-cli```\n\n**Output** results in JSON format\n\n```speed-cloudflare-cli --json```"],["SYNOPSIS","**speed-cloudflare-cli** [**--json**]"],["PARAMETERS","**--json**\n> Output results in JSON format instead of human-readable colored console output."],["DESCRIPTION","**speed-cloudflare-cli** is a Node.js command-line tool that measures the speed and consistency of your internet connection using Cloudflare's speed test infrastructure at **speed.cloudflare.com**. It tests download speed, upload speed, and latency, reporting metrics including jitter.\n\nThe tool performs 20 latency measurements, then tests download speed across 5 payload sizes (100kB to 100MB) and upload speed across 3 payload sizes (11kB to 1MB). Results report the 90th percentile speed in Mbps. Output includes server location (city and IATA code), your IP address, latency statistics (min, max, average, median, jitter), download speed, and upload speed."],["CAVEATS","Requires Node.js and npm. Tests against Cloudflare's nearest edge server; server selection is automatic and cannot be overridden. Results reflect connection quality to Cloudflare's network specifically, which may differ from other speed test services. The package has not been updated since approximately 2021 and may have compatibility issues with newer Node.js versions."],["HISTORY","**speed-cloudflare-cli** was created by **KNawm** as a community-built CLI for Cloudflare's speed test service. Cloudflare launched its browser-based speed test in **September 2020**. The CLI uses the same API endpoints to perform measurements from the terminal. Written in JavaScript under the MIT license. The latest version is 2.0.3."],["INSTALL","```pacman: sudo pacman -S cloudflare-speed-cli```\n\n```brew: brew install cloudflare-speed-cli```"],["SEE ALSO","[speedtest-cli](/man/speedtest-cli)(1), [fast](/man/fast)(1), [iperf](/man/iperf)(1), [speed-test](/man/speed-test)(1)"]],"e":["speedtest-cli","fast","iperf","speed-test"],"x":"speed-cloudflare-cli"},"conda":{"n":"conda","t":"package and environment manager for Python and data science","s":[["TAGLINE","package and environment manager for Python and data science"],["TLDR","**Create** environment\n\n```conda create -n [myenv] [python=3.11]```\n\n**Activate** environment\n\n```conda activate [myenv]```\n\n**Install** package\n\n```conda install [numpy]```\n\n**List** environments\n\n```conda env list```\n\n**Export** environment\n\n```conda env export > [environment.yml]```\n\n**Deactivate** environment\n\n```conda deactivate```"],["SYNOPSIS","**conda** _command_ [_options_]"],["DESCRIPTION","**conda** is a cross-platform package and environment management system originally developed for Python but extended to support R, Ruby, Lua, Scala, Java, JavaScript, C/C++, and other languages. Unlike pip which only manages Python packages, conda handles complete environments including system-level dependencies and compiled libraries, making it particularly valuable for scientific computing where native dependencies are common.\n\nThe environment isolation feature allows multiple projects with conflicting dependency requirements to coexist on the same system. Each conda environment is a directory containing a specific collection of packages, and switching between environments changes which packages are available. This is essential for data science workflows where different projects may require different versions of NumPy, TensorFlow, or other foundational libraries.\n\nConda distributes binary packages rather than building from source, which dramatically speeds up installation and eliminates compilation errors that plague pip-based workflows. The package ecosystem is organized into channels, with conda-forge being the largest community-maintained channel. conda is included in both Anaconda (a large distribution with 1500+ packages) and Miniconda (minimal installer with just conda and Python). The tool has become the de facto standard in data science, machine learning, and scientific computing communities."],["PARAMETERS","**create** **-n** _name_\n> Create new environment\n\n**install** _package_\n> Install package\n\n**update** _package_\n> Update package\n\n**remove** _package_\n> Remove package\n\n**list**\n> List installed packages\n\n**search** _package_\n> Search for package\n\n**env list**\n> List environments\n\n**activate** _name_\n> Activate environment\n\n**deactivate**\n> Deactivate environment"],["CONFIGURATION","**~/.condarc**\n> User-level conda configuration file.\n\n**/opt/conda/.condarc**\n> System-level conda configuration.\n\n**environment.yml**\n> Environment specification file for reproducible environments."],["ENVIRONMENT MANAGEMENT","```bash"],["Create environment with Python","conda create -n myenv python=3.11"],["Create with packages","conda create -n dataenv python=3.11 numpy pandas matplotlib"],["Create from file","conda env create -f environment.yml"],["Clone environment","conda create --clone myenv -n myenv_copy"],["Remove environment","conda env remove -n myenv"],["List all environments","conda env list\n```"],["PACKAGE MANAGEMENT","```bash"],["Install package","conda install numpy"],["Install specific version","conda install numpy=1.24.0"],["Install from channel","conda install -c conda-forge package"],["Update package","conda update numpy"],["Update all packages","conda update --all"],["Remove package","conda remove numpy\n```"],["ENVIRONMENT.YML","```yaml\nname: myenv\nchannels:\n  - conda-forge\n  - defaults\ndependencies:\n  - python=3.11\n  - numpy\n  - pandas\n  - pip:\n    - some-pip-package\n```"],["FEATURES","- Cross-platform\n- Multiple language support\n- Binary package distribution\n- Dependency resolution\n- Environment isolation\n- Channel system\n- Integration with pip"],["CAVEATS","Large disk space usage. Environment activation requires shell integration. Channel priority can cause confusion. Some packages only on conda-forge. Slower than pip for pure Python packages. Mixing conda and pip can cause issues."],["HISTORY","**Conda** was created by Travis Oliphant and Peter Wang at Continuum Analytics (now Anaconda, Inc.) around **2012** for the Anaconda distribution."],["INSTALL","```dnf: sudo dnf install conda```\n\n```nix: nix profile install nixpkgs#conda```"],["SEE ALSO","[pip](/man/pip)(1), [virtualenv](/man/virtualenv)(1), [mamba](/man/mamba)(1)"],["RESOURCES","```[Source code](https://github.com/conda/conda)```\n\n```[Homepage](https://conda.io)```\n\n```[Documentation](https://docs.conda.io/projects/conda/en/stable/)```"]],"e":["pip","virtualenv","mamba"],"x":"conda create -n [myenv] [python=3.11]"},"criu":{"n":"criu","t":"Checkpoint and restore running processes in userspace","s":[["TAGLINE","Checkpoint and restore running processes in userspace"],["TLDR","**Check** that the kernel supports checkpoint/restore\n\n```criu check```\n\n**Checkpoint** a running process tree into an image directory\n\n```sudo criu dump -t [pid] -D [path/to/image_dir] --shell-job```\n\n**Checkpoint** a process while leaving it running, including TCP sockets\n\n```sudo criu dump -t [pid] -D [path/to/image_dir] --tcp-established --leave-running```\n\n**Restore** a process tree from an image directory\n\n```sudo criu restore -D [path/to/image_dir] --shell-job```\n\n**Restore** detached (do not become the parent of the restored tree)\n\n```sudo criu restore -D [path/to/image_dir] -d```\n\n**Serve memory pages** over the network for live migration\n\n```sudo criu page-server -D [path/to/image_dir] --port [1234]```"],["SYNOPSIS","**criu** _command_ [_option_ ...]"],["PARAMETERS","**-t**, **--tree** _pid_\n> PID of the root task to dump.\n\n**-D**, **--images-dir** _dir_\n> Directory where image files are stored or read from.\n\n**-W**, **--work-dir** _dir_\n> Directory for log and working files (defaults to the images directory).\n\n**-v**[_n_], **--verbosity**\n> Set verbosity level (repeatable).\n\n**-R**, **--leave-running**\n> Leave the task running after a successful dump.\n\n**-s**, **--leave-stopped**\n> Leave the tasks stopped after dump (useful for live migration).\n\n**--tcp-established**\n> Checkpoint and restore established TCP connections.\n\n**--shell-job**\n> Allow checkpoint/restore of shell jobs (controlling terminal handling).\n\n**--manage-cgroups**\n> Save and restore cgroup configuration with the process.\n\n**-d**, **--restore-detached**\n> Detach from the restored process tree on success.\n\n**--display-stats**\n> Print dump or restore statistics on completion."],["DESCRIPTION","**criu** (\"Checkpoint and Restore In Userspace\") freezes a running Linux application — or an entire container — and serializes its complete state to a set of image files. Memory, open file descriptors, pipes, sockets, namespaces, and cgroup membership are all captured. A later **criu restore** recreates equivalent processes from those images, optionally on a different host, so the application resumes execution where it left off.\n\nThe bulk of the work happens in userspace. CRIU seizes a process tree via **ptrace**, injects a small \"parasite\" code module (libcompel) to read the process's address space, and writes everything out to the configured image directory. Restore reverses the procedure: it rebuilds namespaces, recreates file descriptors, maps memory, and finally returns control to the original program counter.\n\nCRIU is the foundation for live migration in **OpenVZ**, **LXC/LXD**, and **Podman**, and is used by **runc** and **Kubernetes** to snapshot and migrate containers. It also enables faster cold starts by warming up an application and resuming from the checkpoint on demand."],["CAVEATS","CRIU is Linux-only and typically requires **CAP_SYS_ADMIN** (root). Not every kernel resource is checkpointable: GPU contexts, some netfilter state, and certain external resources may need plugins or are unsupported. Restoring a process generally needs the same PID available, which usually means restoring inside a fresh PID namespace. Always run **criu check** first to confirm that your kernel exposes the required interfaces."],["HISTORY","CRIU was started in **2011** by **Pavel Emelyanov** of the OpenVZ / Virtuozzo team, with the first public release in **July 2012**. By Linux kernel **3.11** (September 2013) the required syscalls and interfaces had been merged upstream, allowing CRIU to run on stock kernels. It is now maintained by the **checkpoint-restore** community on GitHub and continues to receive regular releases."],["INSTALL","```apt: sudo apt install criu```\n\n```dnf: sudo dnf install criu```\n\n```pacman: sudo pacman -S criu```\n\n```zypper: sudo zypper install criu```\n\n```nix: nix profile install nixpkgs#criu```"],["SEE ALSO","[runc](/man/runc)(8), [ptrace](/man/ptrace)(2)"]],"e":["runc","ptrace"],"x":"criu check"},"cs-java":{"n":"cs-java","t":"JDK version manager via Coursier","s":[["TAGLINE","JDK version manager via Coursier"],["TLDR","**List available JDK** distributions\n\n```cs java --available```\n\n**Install a specific JDK** version\n\n```cs java --jvm [adopt:11] --setup```\n\n**Set default Java** version\n\n```cs java --jvm [graalvm-java17] --setup```\n\n**Show current Java** configuration\n\n```cs java --env```\n\n**List installed JDKs**\n\n```cs java --installed```\n\n**Install and use** a specific distribution\n\n```cs java --jvm [temurin:21]```"],["SYNOPSIS","**cs java** [_options_]"],["PARAMETERS","**--available**\n> List all available JDK distributions and versions.\n\n**--jvm** _JVM_\n> Specify JDK distribution and version (e.g., adopt:11, temurin:21).\n\n**--setup**\n> Configure the specified JDK as the default.\n\n**--env**\n> Print environment variables for the current Java setup.\n\n**--installed**\n> List locally installed JDK versions.\n\n**--update**\n> Update to the latest patch version.\n\n**--home**\n> Print the JAVA_HOME path for specified JVM."],["DESCRIPTION","**cs java** is Coursier's Java version manager, providing easy installation and switching between different JDK distributions and versions. It supports numerous distributions including AdoptOpenJDK, Temurin, GraalVM, Corretto, and Zulu.\n\nThe command downloads and manages JDK installations automatically, configuring environment variables to use the selected version. Multiple versions can be installed simultaneously with easy switching between them.\n\nThis tool simplifies Java development environment setup, particularly useful for testing across Java versions or using distribution-specific features like GraalVM's native compilation."],["CAVEATS","The **--setup** option modifies shell configuration files. Some distributions may not be available for all platforms. Large JDK downloads require adequate disk space and network bandwidth. Environment changes require shell restart to take effect."],["HISTORY","cs java was added to Coursier to provide comprehensive JDK management, complementing its Scala tooling capabilities. It follows the pattern of version managers like sdkman and jabba, integrated into the Coursier ecosystem."],["INSTALL","```apt: sudo apt install csound```\n\n```pacman: sudo pacman -S csound```\n\n```zypper: sudo zypper install csound```\n\n```brew: brew install csound```\n\n```nix: nix profile install nixpkgs#csound```"],["SEE ALSO","[cs](/man/cs)(1), [java](/man/java)(1), [sdk](/man/sdk)(1), [update-java-alternatives](/man/update-java-alternatives)(1)"],["RESOURCES","```[Source code](https://github.com/coursier/coursier)```\n\n```[Documentation](https://get-coursier.io/docs/cli-java)```"]],"e":["cs","java","sdk","update-java-alternatives"],"x":"cs java --available"},"dbclient":{"n":"dbclient","t":"lightweight SSH client from the Dropbear suite","s":[["TAGLINE","lightweight SSH client from the Dropbear suite"],["TLDR","**Connect** to a remote host\n\n```dbclient [user]@[host]```\n\nConnect on **specific port**\n\n```dbclient [user]@[host] -p 2222```\n\nConnect using **identity key**\n\n```dbclient -i [path/to/key_file] [user]@[host]```\n\nRun a **command** with TTY allocation\n\n```dbclient [user]@[host] -t [command] [arguments]```\n\n**Forward agent** connections\n\n```dbclient -A [user]@[host]```"],["SYNOPSIS","**dbclient** [_options_] [_user_@]_host_ [_command_]"],["DESCRIPTION","**dbclient** is the SSH client from the Dropbear suite, a lightweight SSH implementation designed for embedded systems and environments with limited resources. It provides standard SSH functionality including remote shell access, command execution, port forwarding, and agent forwarding, all with a significantly smaller binary size and memory footprint than OpenSSH.\n\nDropbear is commonly found on routers, IoT devices, embedded Linux systems, and other resource-constrained environments. The entire suite (server and client) is typically under 110KB compressed, compared to several megabytes for OpenSSH.\n\ndbclient supports the core SSH protocol features: public key authentication, password authentication, local and remote port forwarding, and TTY allocation for interactive sessions. However, it omits some advanced OpenSSH features to maintain its small size.\n\nOne notable difference is key format: dbclient uses Dropbear's own key format rather than OpenSSH's format. The **dropbearconvert** utility can convert between the two formats when needed."],["PARAMETERS","**-p** _port_\n> Connect to specified port (default: 22)\n\n**-i** _keyfile_\n> Identity key file (Dropbear format); multiple allowed\n\n**-l** _user_\n> Login as user on the remote host\n\n**-t**\n> Allocate a PTY\n\n**-T**\n> Don't allocate a PTY\n\n**-A**\n> Forward authentication agent connections\n\n**-L** _listenport:host:port_\n> Local port forwarding\n\n**-R** _listenport:host:port_\n> Remote port forwarding\n\n**-g**\n> Allow non-local hosts to connect to forwarded ports\n\n**-N**\n> Don't request a remote shell\n\n**-f**\n> Fork into the background after authentication\n\n**-y**\n> Always accept unknown hostkeys without prompting\n\n**-W** _windowsize_\n> Per-channel receive window buffer size\n\n**-K** _seconds_\n> Keepalive interval in seconds\n\n**-s**\n> Request the remote command as a subsystem (used for sftp).\n\n**-o** _option_\n> Specify options in OpenSSH config file format (e.g., **-o** \"StrictHostKeyChecking=no\").\n\n**-c** _cipherlist_\n> Comma-separated list of ciphers to enable.\n\n**-m** _MAClist_\n> Comma-separated list of MACs to enable."],["CAVEATS","Uses Dropbear key format, not OpenSSH format. Use **dropbearconvert** to convert keys between formats. Does not support all OpenSSH config options via **-o**. Part of the Dropbear SSH suite, commonly used in embedded Linux systems."],["HISTORY","**dbclient** is part of the **Dropbear** SSH suite, created by **Matt Johnston**. Dropbear was designed for embedded systems where the full OpenSSH suite is too large, with the entire server and client typically under 110KB compressed."],["INSTALL","```apt: sudo apt install dropbear-bin```\n\n```apk: sudo apk add dropbear-dbclient```"],["SEE ALSO","[ssh](/man/ssh)(1), [scp](/man/scp)(1), [dropbear](/man/dropbear)(8), [dropbearkey](/man/dropbearkey)(1), [dropbearconvert](/man/dropbearconvert)(1)"]],"e":["ssh","scp","dropbear","dropbearkey","dropbearconvert"],"x":"dbclient [user]@[host]"},"dbeaver":{"n":"dbeaver","t":"universal database tool and SQL client","s":[["TAGLINE","universal database tool and SQL client"],["TLDR","**Launch DBeaver** GUI\n\n```dbeaver```\n\n**Open specific database** connection\n\n```dbeaver -con \"name=[connection_name]\"```\n\n**Connect with inline parameters**\n\n```dbeaver -con \"driver=postgresql|host=localhost|database=mydb\"```\n\n**Open a SQL file** and connect it to a datasource\n\n```dbeaver -con \"name=[connection]\" -f [script.sql]```\n\n**Run a saved task** (e.g. a data export task)\n\n```dbeaver -runTask \"@[project]:[task]\"```\n\n**Stop a running DBeaver** instance\n\n```dbeaver -stop```"],["SYNOPSIS","**dbeaver** [_options_]"],["PARAMETERS","**-con** _CONNECTION_\n> Connection parameters (name, driver, host, database, user, etc.).\n\n**-f** _FILE_\n> Open the file in the DBeaver UI; with -con, connect it to the datasource.\n\n**-runTask** _@PROJECT:TASK_\n> Execute a saved task; returns an exit code indicating success or failure.\n\n**-var** _NAME=VALUE_\n> Set a variable for -runTask (repeatable).\n\n**-stop**\n> Quit a running DBeaver instance.\n\n**-nosplash**\n> Start without splash screen.\n\n**-data** _DIR_\n> Workspace directory location.\n\n**-nl** _LOCALE_\n> Override system locale.\n\n**-clean**\n> Clear cached data and start fresh.\n\n**--help**\n> Display help information."],["DESCRIPTION","**DBeaver** is a free, universal database tool and SQL client for developers and database administrators. It supports a wide range of databases including PostgreSQL, MySQL, MariaDB, SQLite, Oracle, SQL Server, and many others through JDBC drivers.\n\nThe application provides a graphical interface for database management including SQL editing with syntax highlighting and autocomplete, visual query builder, ER diagrams, data export/import, and schema comparison. It also supports NoSQL databases like MongoDB and Cassandra.\n\nDBeaver's command line can open connections and files in the UI and execute saved tasks (such as data export or backup tasks) via -runTask, which is the supported way to automate DBeaver operations."],["CAVEATS","Requires Java Runtime Environment. Some enterprise features require the paid Enterprise Edition. Large result sets may consume significant memory. Connection configurations are stored in workspace and may need migration between versions."],["HISTORY","DBeaver was created by **Serge Rider** and first released in **2010**. It started as a simple database viewer and evolved into a comprehensive database management tool. The project is maintained by DBeaver Corp with both community and enterprise editions."],["INSTALL","```pacman: sudo pacman -S dbeaver```"],["SEE ALSO","[mysql](/man/mysql)(1), [psql](/man/psql)(1), [sqlite3](/man/sqlite3)(1)"],["RESOURCES","```[Source code](https://github.com/dbeaver/dbeaver)```\n\n```[Homepage](https://dbeaver.io)```\n\n```[Documentation](https://dbeaver.com/docs/dbeaver/)```"]],"e":["mysql","psql","sqlite3"],"x":"dbeaver"},"distccd":{"n":"distccd","t":"distcc server daemon for distributed compilation","s":[["TAGLINE","distcc server daemon for distributed compilation"],["TLDR","**Start distcc daemon**\n\n```distccd --daemon```\n\n**Start with allowed hosts**\n\n```distccd --daemon --allow [192.168.1.0/24]```\n\n**Start in foreground**\n\n```distccd --no-detach --daemon --allow [0.0.0.0/0]```\n\n**Specify listen address**\n\n```distccd --daemon --listen [0.0.0.0]```"],["SYNOPSIS","**distccd** [_options_]"],["PARAMETERS","**--daemon**\n> Run as a daemon.\n\n**--allow** _spec_\n> Allow connections from specified addresses.\n\n**--listen** _addr_\n> Listen on specified address.\n\n**--no-detach**\n> Don't detach from terminal.\n\n**--port** _port_\n> Listen on specified port (default 3632).\n\n**--jobs** _n_\n> Maximum concurrent jobs.\n\n**--log-file** _file_\n> Log to file instead of syslog."],["DESCRIPTION","**distccd** is the distcc server daemon that receives and processes compilation jobs from distcc clients on a network. It accepts preprocessed source code, compiles it using local compilers, and returns the object files to the requesting client.\n\nThe daemon listens on TCP port 3632 by default and should be configured with access control to prevent unauthorized compilation requests. Security is implemented through IP address allowlists specified with the --allow option, as the protocol itself provides no authentication.\n\ndistccd can limit the number of concurrent compilation jobs to prevent system overload. It logs to syslog by default but can be configured to write to specific log files. The daemon typically runs as a dedicated user with limited privileges for security."],["CONFIGURATION","**/etc/default/distccd**\n> Default configuration for distccd daemon on Debian-based systems.\n\n**/etc/sysconfig/distccd**\n> Default configuration for distccd daemon on Red Hat-based systems."],["CAVEATS","Ensure firewall allows connections on port 3632. Use --allow to restrict access to trusted networks only."],["INSTALL","```apt: sudo apt install distcc```\n\n```dnf: sudo dnf install distcc```\n\n```pacman: sudo pacman -S distcc```\n\n```apk: sudo apk add distcc```\n\n```zypper: sudo zypper install distcc```\n\n```brew: brew install distcc```\n\n```nix: nix profile install nixpkgs#distcc```"],["SEE ALSO","[distcc](/man/distcc)(1), [ccache](/man/ccache)(1)"],["RESOURCES","```[Source code](https://github.com/distcc/distcc)```\n\n```[Homepage](https://www.distcc.org)```"]],"e":["distcc","ccache"],"x":"distccd --daemon"},"docker-compose":{"n":"docker-compose","t":"multi-container Docker orchestration","s":[["TAGLINE","multi-container Docker orchestration"],["TLDR","**Start** services\n\n```docker-compose up```\n\n**Start** in background\n\n```docker-compose up -d```\n\n**Stop** services\n\n```docker-compose down```\n\n**View** logs\n\n```docker-compose logs```\n\n**Build** services\n\n```docker-compose build```\n\n**List** containers\n\n```docker-compose ps```"],["SYNOPSIS","**docker-compose** [_options_] _command_"],["DESCRIPTION","**docker-compose** is a tool for defining and running multi-container Docker applications using YAML configuration files. Instead of managing multiple docker run commands with complex flags, Compose lets you declare your entire application stack in a single docker-compose.yml file.\n\nThe tool handles service orchestration, networking, volume management, and dependencies between containers. It automatically creates isolated networks for your application, manages startup order through depends_on directives, and provides convenient commands for viewing logs, executing commands, and managing the entire application lifecycle.\n\nDocker Compose is essential for local development environments where applications require multiple services (web server, database, cache, queue). It ensures consistency across development teams and simplifies the transition from development to production. Modern Docker includes Compose V2 as a native plugin (docker compose) replacing the standalone docker-compose command."],["PARAMETERS","**up** [**-d**]\n> Create and start containers\n\n**down**\n> Stop and remove containers, networks\n\n**build**\n> Build or rebuild services\n\n**start**\n> Start services\n\n**stop**\n> Stop services\n\n**restart**\n> Restart services\n\n**ps**\n> List containers\n\n**logs** [**-f**]\n> View output from containers\n\n**exec** _service_ _command_\n> Execute command in running container\n\n**pull**\n> Pull service images\n\n**config**\n> Validate and view compose file"],["DOCKER-COMPOSE.YML","```yaml\nversion: '3.8'\n\nservices:\n  web:\n    build: .\n    ports:\n      - \"3000:3000\"\n    environment:\n      - DATABASE_URL=postgres://db/myapp\n    depends_on:\n      - db\n    volumes:\n      - .:/app\n\n  db:\n    image: postgres:15\n    environment:\n      POSTGRES_DB: myapp\n      POSTGRES_PASSWORD: secret\n    volumes:\n      - pgdata:/var/lib/postgresql/data\n\nvolumes:\n  pgdata:\n```"],["WORKFLOW","```bash"],["Start all services","docker-compose up"],["Start in background","docker-compose up -d"],["View logs","docker-compose logs -f"],["Stop services","docker-compose down"],["Rebuild and start","docker-compose up --build"],["Execute command","docker-compose exec web bash"],["Run one-off command","docker-compose run web npm test"],["Scale service","docker-compose up --scale worker=3\n```"],["COMMON OPERATIONS","```bash"],["Check status","docker-compose ps"],["Pull latest images","docker-compose pull"],["View configuration","docker-compose config"],["Remove volumes","docker-compose down -v"],["Restart single service","docker-compose restart web"],["View logs for service","docker-compose logs -f web"],["Execute in service","docker-compose exec db psql -U postgres\n```"],["FEATURES","- Multi-container orchestration\n- Network isolation\n- Volume management\n- Environment variable handling\n- Service dependencies\n- Health checks\n- Resource limits"],["FILE LOCATIONS","Default file names (in order):\n1. docker-compose.override.yml\n2. docker-compose.yml\n\nCustom file:\n```bash\ndocker-compose -f custom-compose.yml up\n```"],["CONFIGURATION","**docker-compose.yml**\n> Main configuration file defining services, networks, volumes, and their relationships. Placed in project root directory.\n\n**docker-compose.override.yml**\n> Optional override file automatically loaded alongside docker-compose.yml for local customizations without modifying the main file.\n\n**.env**\n> Environment variables file automatically loaded by Docker Compose to populate variable substitutions in compose files."],["CAVEATS","Not for production orchestration (use Kubernetes). File format versions matter. Environment variables need proper quoting. Network isolation can complicate debugging. Volume permissions issues. Compose V2 (docker compose) vs V1 (docker-compose) differences."],["HISTORY","**docker-compose** was created by Orchard Labs (acquired by Docker) around **2014** as Fig, becoming Docker Compose and integral to Docker development workflows."],["INSTALL","```apt: sudo apt install docker-compose```\n\n```dnf: sudo dnf install docker-compose```\n\n```pacman: sudo pacman -S docker-compose```\n\n```apk: sudo apk add docker-cli-compose```\n\n```zypper: sudo zypper install docker-compose```\n\n```brew: brew install docker-compose```\n\n```nix: nix profile install nixpkgs#docker-compose```"],["SEE ALSO","[docker](/man/docker)(1), [kubectl](/man/kubectl)(1), [podman-compose](/man/podman-compose)(1)"]],"e":["docker","kubectl","podman-compose"],"x":"docker-compose up"},"dolt-status":{"n":"dolt-status","t":"show working tree and staging status","s":[["TAGLINE","show working tree and staging status"],["TLDR","**Show working tree status**\n\n```dolt status```"],["SYNOPSIS","**dolt** **status**"],["DESCRIPTION","**dolt status** displays the current state of the working tree and staging area in a Dolt repository. It categorizes tables into three groups: staged changes ready for commit, unstaged modifications in the working set, and untracked new tables.\n\nThe output helps understand what will be included in the next commit and what changes remain in the working set. This is essential before committing to verify the intended tables are staged and to avoid committing unintended changes.\n\nSimilar to git status, it provides a clear overview of repository state, indicating which tables need attention and what actions (add, commit, or reset) might be appropriate next."],["INSTALL","```pacman: sudo pacman -S dolt```\n\n```brew: brew install dolt```\n\n```nix: nix profile install nixpkgs#dolt```"],["SEE ALSO","[dolt-add](/man/dolt-add)(1)"]],"e":["dolt-add"],"x":"dolt status"},"dropdb":{"n":"dropdb","t":"remove a PostgreSQL database","s":[["TAGLINE","remove a PostgreSQL database"],["TLDR","**Drop a database**\n\n```dropdb [database_name]```\n\n**Drop with confirmation** prompt\n\n```dropdb -i [database_name]```\n\n**Drop on remote** host\n\n```dropdb -h [hostname] -p [5432] [database_name]```\n\n**Drop as specific** user\n\n```dropdb -U [username] [database_name]```\n\n**Drop if exists** (no error if missing)\n\n```dropdb --if-exists [database_name]```\n\n**Force disconnect** active connections\n\n```dropdb --force [database_name]```"],["SYNOPSIS","**dropdb** [_options_] _dbname_"],["PARAMETERS","_DBNAME_\n> Database name to drop.\n\n**-h**, **--host** _HOST_\n> Server hostname.\n\n**-p**, **--port** _PORT_\n> Server port.\n\n**-U**, **--username** _USER_\n> Connect as user.\n\n**-i**, **--interactive**\n> Prompt before drop.\n\n**--if-exists**\n> Don't error if database doesn't exist.\n\n**--force**\n> Terminate existing connections.\n\n**-w**, **--no-password**\n> Never prompt for password.\n\n**-W**, **--password**\n> Force password prompt.\n\n**--maintenance-db** _dbname_\n> Database to connect to for issuing the drop (default: postgres).\n\n**-e**, **--echo**\n> Show SQL command executed.\n\n**-V**, **--version**\n> Print version and exit.\n\n**--help**\n> Display help information."],["DESCRIPTION","**dropdb** is a PostgreSQL utility that removes a database. It's a wrapper around the SQL DROP DATABASE command, providing a convenient command-line interface for database deletion.\n\nThe tool connects to the postgres database to execute the drop. The --force option terminates existing connections to the target database, allowing deletion of databases with active sessions.\n\ndropdb requires appropriate privileges - typically superuser or database owner permissions."],["CAVEATS","Irreversible operation. Cannot drop database with active connections without --force. Requires superuser or owner privileges. Cannot drop template databases."],["HISTORY","dropdb is part of the **PostgreSQL** client utilities, providing command-line database administration since PostgreSQL's early releases. It simplifies common administrative tasks without requiring direct SQL."],["INSTALL","```apk: sudo apk add postgresql17-client```"],["SEE ALSO","[createdb](/man/createdb)(1), [psql](/man/psql)(1), [dropuser](/man/dropuser)(1), [pg_dump](/man/pg_dump)(1)"]],"e":["createdb","psql","dropuser","pg_dump"],"x":"dropdb [database_name]"},"ecryptfs-setup-swap":{"n":"ecryptfs-setup-swap","t":"encrypted swap configuration","s":[["TAGLINE","encrypted swap configuration"],["TLDR","**Setup encrypted swap**\n\n```ecryptfs-setup-swap```\n\n**Setup with specific** swap partition\n\n```ecryptfs-setup-swap [/dev/sda2]```"],["SYNOPSIS","**ecryptfs-setup-swap** [**-f**|**--force**] [_device_]"],["PARAMETERS","_DEVICE_\n> Swap partition to encrypt. If omitted, every active swap device is used.\n\n**-f**, **--force**\n> Set up encrypted swap even if the device already looks encrypted."],["DESCRIPTION","**ecryptfs-setup-swap** configures encrypted swap space using dm-crypt with a random key. This prevents sensitive data from being recoverable from swap after shutdown.\n\nThe script modifies /etc/fstab and /etc/crypttab to enable encrypted swap on boot. A random key is generated each boot, so swap contents are unrecoverable after restart.\n\nEncrypted swap is important when using encrypted home directories, as unencrypted swap could leak decrypted data."],["CAVEATS","Disables hibernation (swap content unrecoverable). Requires root privileges. Existing swap will be reformatted. Must run with system not using swap."],["HISTORY","ecryptfs-setup-swap is part of the **eCryptfs** utilities, addressing the security gap where encrypted filesystem data could leak to unencrypted swap space."],["INSTALL","```apt: sudo apt install ecryptfs-utils```\n\n```dnf: sudo dnf install ecryptfs-utils```\n\n```pacman: sudo pacman -S ecryptfs-utils```\n\n```apk: sudo apk add ecryptfs-utils```\n\n```zypper: sudo zypper install ecryptfs-utils```"],["SEE ALSO","[ecryptfs-setup-private](/man/ecryptfs-setup-private)(1), [ecryptfs](/man/ecryptfs)(7), [cryptsetup](/man/cryptsetup)(8), [swapon](/man/swapon)(8)"],["RESOURCES","```[Source code](https://launchpad.net/ecryptfs)```\n\n```[Homepage](https://www.ecryptfs.org/)```"]],"e":["ecryptfs-setup-private","ecryptfs","cryptsetup","swapon"],"x":"ecryptfs-setup-swap"},"fonttools":{"n":"fonttools","t":"Python toolkit for manipulating font files","s":[["TAGLINE","Python toolkit for manipulating font files"],["TLDR","Show top-level **fonttools** help and subcommands\n\n```fonttools --help```\n\nSubset a TTF to the **Basic Latin** Unicode range\n\n```fonttools subset [path/to/font.ttf] --unicodes=U+0000-007F```"],["SYNOPSIS","**fonttools** [_subcommand_] [_options_] [_file_]"],["PARAMETERS","**subset**\n> Subset font files to include only specific glyphs or Unicode ranges\n\n**ttx**\n> Convert font binaries to/from XML format\n\n**merge**\n> Merge multiple font files into one\n\n**help**\n> Display help for fonttools or a specific subcommand\n\n**--unicodes=**_RANGE_\n> Specify Unicode ranges to include (e.g., U+0000-007F)\n\n**--output-file=**_FILE_\n> Specify output file path\n\n**--help**\n> Display help message"],["DESCRIPTION","**fonttools** is a Python library and command-line toolkit for manipulating font files. It provides tools for converting, subsetting, merging, and inspecting TrueType (TTF), OpenType (OTF), and other font formats.\n\nThe most common use cases include subsetting fonts for web optimization (reducing file size by including only needed glyphs), converting between binary and XML representations for inspection or editing, and merging font families.\n\nRequires Python 3.10 or later. Install via pip with **pip install fonttools**."],["CAVEATS","Some features require optional dependencies like **brotli** for WOFF2 compression or **zopfli** for optimized WOFF compression. Complex font operations may require deep understanding of font table structures."],["HISTORY","fonttools originated in the late 1990s, created by Just van Rossum. It became one of the earliest tools for programmatic font manipulation in Python. The project has evolved significantly and is now maintained by the open-source community on GitHub, becoming a foundational tool in the font development ecosystem."],["INSTALL","```dnf: sudo dnf install fonttools```\n\n```apk: sudo apk add py3-fonttools```\n\n```brew: brew install fonttools```"],["SEE ALSO","[fc-cache](/man/fc-cache)(1), [fc-list](/man/fc-list)(1)"]],"e":["fc-cache","fc-list"],"x":"fonttools --help"},"gh-gist":{"n":"gh-gist","t":"manage code snippets and shareable files","s":[["TAGLINE","manage code snippets and shareable files"],["TLDR","**Create a gist**\n\n```gh gist create [file]```\n\n**Create public gist**\n\n```gh gist create -p [file]```\n\n**Create from stdin**\n\n```echo \"[content]\" | gh gist create```\n\n**List gists**\n\n```gh gist list```\n\n**View a gist**\n\n```gh gist view [gist_id]```\n\n**Edit a gist**\n\n```gh gist edit [gist_id]```\n\n**Delete a gist**\n\n```gh gist delete [gist_id]```\n\n**Clone a gist**\n\n```gh gist clone [gist_id]```"],["SYNOPSIS","**gh** **gist** _command_ [_options_]"],["SUBCOMMANDS","**create**\n> Create a new gist from files or stdin.\n\n**list**\n> List your gists.\n\n**view**\n> View a gist's contents.\n\n**edit**\n> Edit one of your gists.\n\n**delete**\n> Delete a gist.\n\n**clone**\n> Clone a gist locally via git.\n\n**rename**\n> Rename a file in a gist."],["PARAMETERS","**-p**, **--public**\n> Make the gist public (default is secret).\n\n**-d**, **--desc** _text_\n> Gist description.\n\n**-f**, **--filename** _name_\n> Provide a filename for content read from standard input.\n\n**-w**, **--web**\n> Open the gist in a web browser after creating or viewing.\n\n**-r**, **--raw**\n> Print raw, non-rendered content when viewing."],["DESCRIPTION","**gh gist** manages GitHub Gists, a simple way to share code snippets, notes, and small files. Gists can be public or secret, with secret gists hidden from search but accessible via URL.\n\nEach gist supports multiple files and full version history through git. They can be cloned like regular repositories, making them useful for sharing configuration files, scripts, or documentation. Gists support syntax highlighting and can be embedded in websites.\n\nThe CLI provides full lifecycle management from creation through editing and deletion, with support for reading content from stdin for scripting workflows."],["INSTALL","```apt: sudo apt install gh```\n\n```dnf: sudo dnf install gh```\n\n```pacman: sudo pacman -S github-cli```\n\n```apk: sudo apk add github-cli```\n\n```zypper: sudo zypper install gh```\n\n```brew: brew install gh```\n\n```nix: nix profile install nixpkgs#gh```"],["SEE ALSO","[gh](/man/gh)(1), [gist](/man/gist)(1), [git](/man/git)(1)"]],"e":["gh","gist","git"],"x":"gh gist create [file]"},"git-for-each-repo":{"n":"git-for-each-repo","t":"Execute Git commands across multiple repositories","s":[["TAGLINE","Execute Git commands across multiple repositories"],["TLDR","**Run a git command in each repo listed in a config key**\n\n```git for-each-repo --config=[maintenance.repo] [fetch]```\n\n**Run maintenance on all registered repos**\n\n```git for-each-repo --config=[maintenance.repo] maintenance run```\n\n**Continue on failure**\n\n```git for-each-repo --config=[maintenance.repo] --keep-going [gc]```"],["SYNOPSIS","**git** **for-each-repo** **--config**=_key_ [**--keep-going**] [--] _command_ [_args_]"],["PARAMETERS","**--config** _key_\n> Multi-valued config key listing absolute paths to repositories.\n\n**--keep-going**\n> Continue with remaining repositories if the command fails on one. The exit code still indicates failure."],["DESCRIPTION","**git for-each-repo** executes a specified Git command across multiple repositories defined in a Git configuration value. The command reads a multi-valued configuration key where each value is an absolute path to a Git repository, then runs the provided command in each one.\n\nThe primary use case is git-maintenance, which uses for-each-repo to run maintenance tasks like gc, fetch, or repack across all registered repositories on a schedule. Config values are loaded from system, global, and local Git config. If run outside a Git repository, only system and global config is used."],["CONFIGURATION","**~/.gitconfig**\n> Contains multi-valued configuration keys listing repository paths for batch operations."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-maintenance](/man/git-maintenance)(1), [git-config](/man/git-config)(1)"]],"e":["git-maintenance","git-config"],"x":"git for-each-repo --config=[maintenance.repo] [fetch]"},"git-notes":{"n":"git-notes","t":"Add metadata to commits","s":[["TAGLINE","Add metadata to commits"],["TLDR","**Add note to HEAD**\n\n```git notes add -m \"[note text]\"```\n\n**Add note to specific commit**\n\n```git notes add -m \"[note]\" [commit]```\n\n**Show notes** for a commit\n\n```git notes show [commit]```\n\n**List all notes**\n\n```git notes list```\n\n**Edit existing note**\n\n```git notes edit [commit]```\n\n**Append text** to an existing note\n\n```git notes append -m \"[more text]\" [commit]```\n\n**Copy a note** from one commit to another\n\n```git notes copy [from_commit] [to_commit]```\n\n**Remove note**\n\n```git notes remove [commit]```\n\n**Push notes** to remote\n\n```git push origin refs/notes/*```\n\n**Fetch notes** from remote\n\n```git fetch origin refs/notes/*:refs/notes/*```"],["SYNOPSIS","**git notes** [_subcommand_] [_options_]"],["PARAMETERS","**add**\n> Add a new note to the given object (default: HEAD). Fails if a note already exists unless **-f** is used.\n\n**show**\n> Print the note for the given object.\n\n**list**\n> List notes for the given object, or list all notes if no object is specified.\n\n**edit**\n> Edit existing note in the configured editor.\n\n**append**\n> Append new content to an existing note.\n\n**copy** _from_ _to_\n> Copy the note from one object to another.\n\n**remove**\n> Remove notes for the given objects.\n\n**prune**\n> Remove notes attached to objects that no longer exist.\n\n**merge** _ref_\n> Merge notes from another notes ref.\n\n**get-ref**\n> Print the current notes ref.\n\n**-m** _MSG_\n> Use given message as the note text.\n\n**-F** _FILE_\n> Read note text from a file.\n\n**-C** _OBJECT_\n> Reuse note from given object.\n\n**-c** _OBJECT_\n> Reuse and edit note from given object.\n\n**-f**, **--force**\n> Overwrite an existing note.\n\n**--ref** _REF_\n> Use the given notes ref instead of the default **refs/notes/commits**."],["DESCRIPTION","**git notes** adds metadata to commits without modifying them. Notes are stored as separate refs under **refs/notes/**, allowing annotations, code review comments, build status, or other metadata to be attached after commits are created and even after they have been pushed.\n\nNotes can be organized in namespaces using the **--ref** option. Since notes don't change commit hashes, they provide a non-destructive way to enrich commit history with additional context.\n\nThe default notes ref is **refs/notes/commits**, but you can keep separate notes namespaces (e.g. **refs/notes/review**, **refs/notes/build**) by using **--ref** or by setting **core.notesRef** in git config."],["CAVEATS","Notes are stored as separate refs and are not pushed or fetched by default. Use explicit refspecs like **refs/notes/*:refs/notes/*** to sync notes between repositories. Concurrent edits can cause notes-merge conflicts."],["HISTORY","**git notes** was added to Git in version **1.6.6** (December 2009) to allow attaching metadata to commits without rewriting history."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-log](/man/git-log)(1), [git-commit](/man/git-commit)(1), [git-push](/man/git-push)(1), [git-fetch](/man/git-fetch)(1)"]],"e":["git-log","git-commit","git-push","git-fetch"],"x":"git notes add -m \"[note text]\""},"git-show-tree":{"n":"git-show-tree","t":"Display a decorated graph of commits across all branches","s":[["TAGLINE","Display a decorated graph of commits across all branches"],["TLDR","**Show branch tree**\n\n```git show-tree```"],["SYNOPSIS","**git show-tree**"],["DESCRIPTION","**git show-tree** is a git-extras shortcut for `git log --all --graph --decorate --oneline --simplify-by-decoration`. It draws an ASCII commit graph across every branch, showing only the commits that are branch/tag tips or merge points, which makes overall repository topology easy to see at a glance."],["CAVEATS","Part of git-extras package. Only shows commits relevant to the graph shape (tips, merges, tags); use `git log --graph` directly if you need every commit."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-log](/man/git-log)(1), [git-branch](/man/git-branch)(1), [git-extras](/man/git-extras)(1)"],["RESOURCES","```[Source code](https://github.com/tj/git-extras)```\n\n```[Documentation](https://github.com/tj/git-extras/blob/master/Commands.md#git-show-tree)```"]],"e":["git-log","git-branch","git-extras"],"x":"git show-tree"},"gsd-meta-manager":{"n":"gsd-meta-manager","t":"Terminal dashboard for multiple Get-Shit-Done workflow projects","s":[["TAGLINE","Terminal dashboard for multiple Get-Shit-Done workflow projects"],["TLDR","**Launch the TUI** showing all registered GSD projects\n\n```gsd-meta-manager```\n\n**Register a project directory** in the global registry\n\n```gsd-meta-manager add [path/to/project]```\n\n**Register with a custom alias**\n\n```gsd-meta-manager add [path/to/project] [alias]```\n\n**Remove a project** from the registry by name or alias\n\n```gsd-meta-manager remove [project]```\n\n**List all registered projects** on stdout\n\n```gsd-meta-manager list```\n\n**Use a non-default config file**\n\n```gsd-meta-manager --config [path/to/config.json]```\n\n**Show the version**\n\n```gsd-meta-manager --version```"],["SYNOPSIS","**gsd-meta-manager** [_--config FILE_] [_subcommand_] [_args_]\n\n**gsd-meta-manager add** _PATH_ [_ALIAS_]\n\n**gsd-meta-manager remove** _PROJECT_\n\n**gsd-meta-manager list**"],["PARAMETERS","**--config** _FILE_\n> Override the default config file location.\n\n**--version**\n> Print the version number and exit.\n\n**--help**\n> Display the help message."],["SUBCOMMANDS","**add** _PATH_ [_ALIAS_]\n> Register the GSD project at _PATH_ in the global registry. _ALIAS_ sets a custom display name.\n\n**remove** _PROJECT_\n> Remove a project from the registry, identified by its alias or path.\n\n**list**\n> Print all registered projects.\n\n**help** [_SUBCOMMAND_]\n> Show help for a specific subcommand."],["KEY BINDINGS","**j**, **k**\n> Move down or up in the project list.\n\n**Enter**\n> Open the detail view for the selected project.\n\n**Tab**, **Shift+Tab**\n> Switch between detail tabs.\n\n**/**\n> Activate search and filter mode.\n\n**a**\n> Add a new project interactively.\n\n**n**\n> Create a brand-new GSD project.\n\n**d**\n> Delete the selected project from the registry.\n\n**?**\n> Display the in-app help.\n\n**q**\n> Quit the application."],["DESCRIPTION","**gsd-meta-manager** is a Rust terminal UI that aggregates state from many GSD (\"Get Shit Done\") workflow projects into a single dashboard. It reads project state directly from disk, auto-detects running Claude Code sessions tied to GSD projects, and shows progress without any background daemon.\n\nEach registered project can be inspected through a 10-tab detail view: **Phases**, **Roadmap** (rendered as an ASCII DAG), **Backlog**, **Git History**, **Pipeline**, **Queue**, **Sessions**, **Archive**, **Config**, and **Docs**. The intent is to make multi-project status the default view rather than something assembled by hand from many terminals."],["CONFIGURATION","**~/.config/gsd-meta-manager/config.json**\n> Default registry file. Stores the list of registered project paths and aliases. Overridable with **--config**."],["CAVEATS","The tool only reflects state that already lives in each project on disk; it does not run pipelines, edit code, or push commits. Projects must already follow the GSD layout for tabs such as **Phases** and **Roadmap** to render anything meaningful."],["HISTORY","**gsd-meta-manager** was published by **bitcreed** in 2026 under the MIT license. It is distributed through **crates.io** and requires Rust 1.85 or newer to build from source."],["SEE ALSO","[cargo](/man/cargo)(1), [git](/man/git)(1), [tmux](/man/tmux)(1)"]],"e":["cargo","git","tmux"],"x":"gsd-meta-manager"},"gvfs-trash":{"n":"gvfs-trash","t":"GNOME Virtual File System command that moves files and directories","s":[["TAGLINE","GNOME Virtual File System command that moves files and directories"],["TLDR","**Move a file to the trash**\n\n```gvfs-trash [path/to/file]```\n\n**Move multiple files to the trash**\n\n```gvfs-trash [file1] [file2] [file3]```\n\n**Empty the trash**\n\n```gvfs-trash --empty```\n\n**Move files to trash** ignoring errors for non-existent files\n\n```gvfs-trash -f [path/to/file]```\n\n**Read files to trash from stdin** (one per line)\n\n```echo \"[path/to/file]\" | gvfs-trash```"],["SYNOPSIS","**gvfs-trash** [_options_] [_file_...]\n\n**gvfs-trash** --empty"],["PARAMETERS","**-f**, **--force**\n> Ignore nonexistent and non-deletable files\n\n**--empty**\n> Empty the trash\n\n**-h**, **--help**\n> Print help text and exit\n\n**--version**\n> Show version information and exit"],["DESCRIPTION","**gvfs-trash** is a GNOME Virtual File System command that moves files and directories to the user's trash bin instead of permanently deleting them. This provides a safer alternative to **rm** by allowing recovery of accidentally deleted files.\n\nThe trash location depends on the file's filesystem. For files in the user's home directory, trash is stored in **$XDG_DATA_HOME/Trash** (typically **~/.local/share/Trash**). Files on removable drives may have their own trash folders.\n\nWhen called without the **--empty** option, gvfs-trash expects file paths as arguments. If no files are specified, it reads paths from standard input, one per line. Moving files to trash does not free disk space until the trash is emptied."],["CAVEATS","Not all filesystems support the trash concept; files on such systems may be deleted immediately. The command has been deprecated in favor of **gio trash**, which provides the same functionality. Use **gvfs-rm** for permanent deletion or **gvfs-ls trash://** to view trash contents."],["HISTORY","gvfs-trash is part of **GVFS** (GNOME Virtual File System), introduced with **GNOME 2.22** in **2008** as a replacement for the older gnome-vfs library. GVFS provides a unified interface for various filesystems and storage backends in GNOME. The gvfs command-line tools were later deprecated in favor of the **gio** command, which consolidates all GVFS operations under a single tool."],["SEE ALSO","[gio](/man/gio)(1), [trash-cli](/man/trash-cli)(1), [rm](/man/rm)(1), [gvfs-rm](/man/gvfs-rm)(1)"],["RESOURCES","```[Source code](https://gitlab.gnome.org/GNOME/gvfs)```\n\n```[Homepage](https://wiki.gnome.org/Projects/gvfs)```"]],"e":["gio","trash-cli","rm","gvfs-rm"],"x":"gvfs-trash [path/to/file]"},"h2ph":{"n":"h2ph","t":"Convert C header files to Perl header files","s":[["TAGLINE","Convert C header files to Perl header files"],["TLDR","**Convert a C header file to Perl**\n\n```h2ph [header.h]```\n\n**Process all system headers recursively**\n\n```cd /usr/include && h2ph -r -l *.h```\n\n**Output to a specific directory**\n\n```h2ph -d [/usr/lib/perl5] [header.h]```\n\n**Convert a header and all headers it includes**\n\n```h2ph -a [sys/types.h]```"],["SYNOPSIS","**h2ph** [_options_] [_headerfiles_...]"],["PARAMETERS","_HEADERFILES_\n> C header files (.h) to convert to Perl header files (.ph).\n\n**-d** _DIR_\n> Put resulting .ph files beneath the specified directory instead of the default Perl library location.\n\n**-r**\n> Run recursively, converting all .h files in specified directories and their subdirectories.\n\n**-a**\n> Run automagically; convert specified headers as well as any .h files they include.\n\n**-l**\n> Create symbolic links for duplicate output files instead of separate copies.\n\n**-D** _DIR_\n> Search the specified directory for included header files.\n\n**-e**\n> If an error is encountered during conversion, output file names and continue parsing.\n\n**-Q**\n> Quiet mode. Do not print the names of converted files."],["DESCRIPTION","**h2ph** converts C header files (.h) to Perl header files (.ph). It translates **#define** macros and constant definitions into Perl equivalents that can be loaded with **require**.\n\nThe tool is best run from **/usr/include** to convert system headers. It wraps definitions inside **eval** blocks so that you can access the definitions it can translate even if some fail. Output .ph files are placed in Perl's architecture-dependent library directory by default."],["CAVEATS","Only handles simple **#define** constants and macros. Complex C macros, function-like macros, and typedefs are not translated. Output may need manual corrections. The **h2xs** tool is generally preferred for creating Perl extensions from C headers."],["HISTORY","h2ph has been part of the **Perl** distribution since early versions, providing a quick way to access system constants from Perl programs before XS and h2xs became the preferred approach."],["INSTALL","```apt: sudo apt install perl```\n\n```dnf: sudo dnf install perl```\n\n```pacman: sudo pacman -S perl```\n\n```apk: sudo apk add perl-utils```\n\n```zypper: sudo zypper install perl```\n\n```brew: brew install perl```\n\n```nix: nix profile install nixpkgs#perl```"],["SEE ALSO","[h2xs](/man/h2xs)(1), [perl](/man/perl)(1)"]],"e":["h2xs","perl"],"x":"h2ph [header.h]"},"hciemu":{"n":"hciemu","t":"legacy BlueZ tool that emulates a Bluetooth HCI device via the hci_vhci kernel module","s":[["TAGLINE","legacy BlueZ tool that emulates a Bluetooth HCI device via the hci_vhci kernel module"],["TLDR","**Emulate an HCI device with a given local address**\n\n```sudo hciemu [00:11:22:33:44:55]```\n\n**Emulate a specific device type**\n\n```sudo hciemu -d [vhci] [00:11:22:33:44:55]```\n\n**Capture emulated traffic to a snoop file**\n\n```sudo hciemu -s [capture.snoop] [00:11:22:33:44:55]```\n\n**Run in the foreground without detaching**\n\n```sudo hciemu -n [00:11:22:33:44:55]```"],["SYNOPSIS","**hciemu** [_options_] _local-address_"],["PARAMETERS","**-d** _device_\n> Device type to emulate (e.g. vhci).\n\n**-b** _bdaddr_\n> Bluetooth device address to emulate.\n\n**-s** _file_\n> Write captured HCI packets to a snoop file.\n\n**-n**\n> Do not detach from the controlling terminal."],["DESCRIPTION","**hciemu** is a legacy BlueZ test utility that emulates a Bluetooth Host Controller Interface (HCI) device through the **hci_vhci** kernel module. It creates a virtual Bluetooth controller with a given local address, letting the Bluetooth stack and applications be exercised without physical hardware."],["CAVEATS","Requires the **hci_vhci** kernel module and root privileges. This standalone command dates from the pre-BlueZ5 bluez-utils test suite and has been dropped from the tools shipped by current Linux distributions; modern BlueZ only keeps an internal emulator library (used by its own test suite) under the same name, not a user-facing binary. For interactive Bluetooth testing today, use **bluetoothctl**."],["HISTORY","hciemu was part of the original **BlueZ** (bluez-utils) test tools, predating BlueZ5. It has since been removed from mainstream distribution packages."],["SEE ALSO","[hciconfig](/man/hciconfig)(1), [hcitool](/man/hcitool)(1), [bluetoothctl](/man/bluetoothctl)(1)"]],"e":["hciconfig","hcitool","bluetoothctl"],"x":"sudo hciemu [00:11:22:33:44:55]"},"impacket-ping6":{"n":"impacket-ping6","t":"ICMPv6 ping implementation using raw sockets via the Impacket library","s":[["TAGLINE","ICMPv6 ping implementation using raw sockets via the Impacket library"],["TLDR","**Send ICMPv6 echo requests** to a target\n\n```impacket-ping6 [2001:db8::1]```\n\n**Ping IPv6 address with specific source**\n\n```impacket-ping6 -src [2001:db8::50] [2001:db8::1]```"],["SYNOPSIS","**impacket-ping6** [_-h_] [_-src IP_] _target_"],["PARAMETERS","**-src** _IP_\n> Source IPv6 address to use for the ping packets"],["DESCRIPTION","**impacket-ping6** is an ICMPv6 ping implementation using raw sockets via the Impacket library. It sends ICMPv6 echo request packets to the specified IPv6 target and reports responses.\n\nThis tool provides IPv6 ping functionality using Impacket's raw socket capabilities, useful for testing IPv6 connectivity in security testing scenarios."],["CAVEATS","Requires root/administrator privileges to create raw sockets. Target must be an IPv6 address. May be blocked by firewalls that filter ICMPv6 traffic. For most use cases, the standard **ping6** or **ping -6** command is more feature-rich."],["HISTORY","Part of the **Impacket** library by SecureAuth. Provides IPv6 counterpart to impacket-ping for environments using IPv6 networking."],["INSTALL","```pacman: sudo pacman -S impacket```"],["SEE ALSO","[ping](/man/ping)(1), [impacket-ping](/man/impacket-ping)(1), [ping6](/man/ping6)(1)"]],"e":["ping","impacket-ping","ping6"],"x":"impacket-ping6 [2001:db8::1]"},"ipdb":{"n":"ipdb","t":"IPython-enhanced Python debugger","s":[["TAGLINE","IPython-enhanced Python debugger"],["TLDR","**Start ipdb debugger**\n\n```python -m ipdb [script.py]```\n\n**Set breakpoint in code**\n\n```import ipdb; ipdb.set_trace()```\n\n**Post-mortem debugging**\n\n```python -m ipdb -c continue [script.py]```\n\n**Run until exception**\n\n```ipdb.pm()```"],["SYNOPSIS","**python -m ipdb** [_options_] _script_ [_args_]"],["PARAMETERS","**-c** _command_\n> Execute command on start.\n\n_script_\n> Python script to debug."],["DEBUGGER COMMANDS","```\nn (next)      Execute next line\ns (step)      Step into function\nc (continue)  Continue execution\nr (return)    Return from function\nl (list)      List source code\np expr        Print expression\npp expr       Pretty-print expression\nb line        Set breakpoint\ncl            Clear breakpoints\nq (quit)      Quit debugger\nh (help)      Show help\n```"],["DESCRIPTION","**ipdb** is the IPython-enhanced Python debugger. It provides the same interface as pdb but with syntax highlighting, tab completion, and better introspection from IPython.\n\nipdb makes Python debugging more interactive and user-friendly, especially for exploring variables and understanding program state."],["USAGE IN CODE","```python\nimport ipdb\n\ndef problematic_function():\n    x = calculate_something()\n    ipdb.set_trace()  # Debugger starts here\n    return process(x)\n```"],["CAVEATS","Requires ipython package. Breakpoints pause execution. Don't leave set_trace() in production code. Use breakpoint() in Python 3.7+ with PYTHONBREAKPOINT=ipdb.set_trace."],["HISTORY","ipdb was created as a drop-in replacement for pdb that leverages IPython's features. It provides the familiar pdb interface with enhanced usability."],["SEE ALSO","[ipython](/man/ipython)(1), [pudb](/man/pudb)(1)"]],"e":["ipython","pudb"],"x":"python -m ipdb [script.py]"},"iproxy":{"n":"iproxy","t":"creates a TCP proxy that forwards connections from a local port to","s":[["TAGLINE","creates a TCP proxy that forwards connections from a local port to"],["TLDR","**Forward local port 2222 to port 22** on USB-connected iOS device\n\n```iproxy 2222 22```\n\n**Forward port to a specific device** by UDID\n\n```iproxy 2222 22 -u [device_udid]```\n\n**Forward to a network-connected device**\n\n```iproxy 2222 22 -n```\n\n**Create tunnel for SSH** access to iOS device\n\n```iproxy 2222 22 & ssh -p 2222 root@localhost```\n\n**Forward port for debugging** (e.g., lldb debugserver)\n\n```iproxy 1234 1234```"],["SYNOPSIS","**iproxy** [_-u UDID_] [_-n_] [_-d_] [_-h_] _LOCAL_PORT_ _DEVICE_PORT_"],["PARAMETERS","**-u** _UDID_\n> Target device by its unique device identifier\n\n**-n**\n> Connect to network device instead of USB\n\n**-d**\n> Enable debug logging\n\n**-h**\n> Display help information\n\n**-s** _SOURCE_\n> Source address to bind to"],["DESCRIPTION","**iproxy** creates a TCP proxy that forwards connections from a local port to a port on an iOS device connected via USB or network. It uses the usbmuxd protocol to communicate with iOS devices, bypassing the need for network configuration.\n\nThis tool is essential for SSH access to jailbroken iOS devices, remote debugging, and development workflows that require TCP connectivity to iOS. The most common use case is forwarding port 22 for SSH access."],["CAVEATS","Requires **usbmuxd** to be running for USB connections. SSH access requires a jailbroken device with OpenSSH installed. The device must be paired/trusted with the computer. Network mode requires the device to be on the same network."],["HISTORY","**iproxy** is part of the **libusbmuxd** project, which is part of the **libimobiledevice** suite. Libimobiledevice was created around **2009** as a cross-platform library to communicate with iOS devices, providing open-source alternatives to Apple's proprietary protocols."],["INSTALL","```apt: sudo apt install libusbmuxd-tools```\n\n```apk: sudo apk add libusbmuxd-progs```\n\n```zypper: sudo zypper install libusbmuxd-tools```"],["SEE ALSO","[idevice_id](/man/idevice_id)(1), [ideviceinfo](/man/ideviceinfo)(1), [ssh](/man/ssh)(1)"]],"e":["idevice_id","ideviceinfo","ssh"],"x":"iproxy 2222 22"},"jar":{"n":"jar","t":"Java Archive tool","s":[["TAGLINE","Java Archive tool"],["TLDR","**Create JAR file**\n\n```jar cf [output.jar] [files...]```\n\n**Create with manifest**\n\n```jar cfm [output.jar] [MANIFEST.MF] [files...]```\n\n**Extract JAR file**\n\n```jar xf [archive.jar]```\n\n**List contents**\n\n```jar tf [archive.jar]```\n\n**Create executable JAR**\n\n```jar cfe [output.jar] [MainClass] [files...]```\n\n**Update JAR file**\n\n```jar uf [archive.jar] [newfiles...]```\n\n**Extract specific file**\n\n```jar xf [archive.jar] [path/to/file]```\n\n**List contents verbosely**\n\n```jar tvf [archive.jar]```\n\n**Create JAR from directory** using -C to change base dir\n\n```jar cf [output.jar] -C [build/classes] .```"],["SYNOPSIS","**jar** [_options_] [_manifest_] _destination_ _input-files_"],["PARAMETERS","**c**\n> Create new archive.\n\n**x**\n> Extract archive.\n\n**t**\n> List table of contents.\n\n**u**\n> Update existing archive.\n\n**f** _file_\n> Specify archive filename.\n\n**m** _manifest_\n> Include manifest file.\n\n**e** _class_\n> Set entry point (main class).\n\n**v**\n> Verbose output.\n\n**0**\n> Store only (no compression).\n\n**M**\n> Do not create a manifest file.\n\n**i**\n> Generate index information for specified JAR files.\n\n**C** _dir_\n> Change to directory before including the following files.\n\n**--release** _VERSION_\n> (Java 9+) Place following files in a versioned directory for multi-release JARs."],["DESCRIPTION","**jar** is the Java Archive tool. It packages Java class files, resources, and metadata into a single JAR file for distribution and deployment.\n\nJAR files use ZIP format with a manifest (META-INF/MANIFEST.MF) containing metadata. Executable JARs specify a main class in the manifest."],["MANIFEST EXAMPLE","```\nManifest-Version: 1.0\nMain-Class: com.example.Main\nClass-Path: lib/dependency.jar\n```"],["CAVEATS","Order of flags matters when using legacy (non-dashed) syntax. The manifest file must end with a newline. Paths are relative to the current directory unless -C is used. In Java 9+ the `--module-version` and `--release` flags add module/multi-release support."],["HISTORY","The jar tool has been part of the **JDK** since Java 1.1 (1997). It's essential for Java deployment, evolving to support modules in Java 9+ and multi-release JARs."],["SEE ALSO","[java](/man/java)(1), [javac](/man/javac)(1), [jarsigner](/man/jarsigner)(1), [zip](/man/zip)(1), [unzip](/man/unzip)(1), [tar](/man/tar)(1)"]],"e":["java","javac","jarsigner","zip","unzip","tar"],"x":"jar cf [output.jar] [files...]"},"jenkins":{"n":"jenkins","t":"continuous integration and delivery server","s":[["TAGLINE","continuous integration and delivery server"],["TLDR","**Start Jenkins**\n\n```java -jar jenkins.war```\n\n**Start on specific port**\n\n```java -jar jenkins.war --httpPort=[8888]```\n\n**Run as daemon**\n\n```java -jar jenkins.war &```\n\n**Specify home directory**\n\n```JENKINS_HOME=[/var/jenkins] java -jar jenkins.war```\n\n**Use HTTPS**\n\n```java -jar jenkins.war --httpsPort=[8443] --httpsKeyStore=[keystore.jks]```"],["SYNOPSIS","**java -jar jenkins.war** [_options_]"],["PARAMETERS","**--httpPort** _PORT_\n> HTTP port (default 8080).\n\n**--httpsPort** _PORT_\n> HTTPS port.\n\n**--httpsKeyStore** _FILE_\n> SSL keystore file.\n\n**--prefix** _PATH_\n> URL prefix.\n\n**--daemon**\n> Run as daemon.\n\n**--help**\n> Display help information."],["DESCRIPTION","**Jenkins** is a continuous integration and delivery server. It automates building, testing, and deploying software.\n\nThe platform supports pipelines, plugins, and distributed builds. It integrates with version control and deployment systems."],["CAVEATS","Requires Java. Resource intensive. Security configuration important."],["HISTORY","Jenkins was forked from **Hudson** in 2011 by **Kohsuke Kawaguchi**. It became the leading open-source CI/CD server."],["INSTALL","```pacman: sudo pacman -S jenkins```\n\n```brew: brew install jenkins```\n\n```nix: nix profile install nixpkgs#jenkins```"],["SEE ALSO","[java](/man/java)(1), [docker](/man/docker)(1), [gitlab-runner](/man/gitlab-runner)(1)"]],"e":["java","docker","gitlab-runner"],"x":"java -jar jenkins.war"},"jj-config-unset":{"n":"jj-config-unset","t":"removes configuration values from Jujutsu config files","s":[["TAGLINE","removes configuration values from Jujutsu config files"],["TLDR","**Unset user config value**\n\n```jj config unset --user [key]```\n\n**Unset repo config value**\n\n```jj config unset --repo [key]```"],["SYNOPSIS","**jj** **config** **unset** [_options_] _name_"],["PARAMETERS","**--user**\n> Remove from user configuration.\n\n**--repo**\n> Remove from repository configuration."],["DESCRIPTION","**jj config unset** removes configuration values from Jujutsu config files. The value is deleted from the specified config file, allowing defaults or lower-priority configs to take effect."],["INSTALL","```pacman: sudo pacman -S jujutsu```\n\n```apk: sudo apk add jujutsu```\n\n```zypper: sudo zypper install jujutsu```\n\n```brew: brew install jujutsu```\n\n```nix: nix profile install nixpkgs#jujutsu```"],["SEE ALSO","[jj](/man/jj)(1), [jj-config-set](/man/jj-config-set)(1)"]],"e":["jj","jj-config-set"],"x":"jj config unset --user [key]"},"jj-git-remote":{"n":"jj-git-remote","t":"manages Git remotes for Jujutsu repositories","s":[["TAGLINE","manages Git remotes for Jujutsu repositories"],["TLDR","**Add remote**\n\n```jj git remote add [name] [url]```\n\n**List remotes**\n\n```jj git remote list```\n\n**Remove remote**\n\n```jj git remote remove [name]```\n\n**Rename remote**\n\n```jj git remote rename [old] [new]```"],["SYNOPSIS","**jj git remote** _subcommand_ [_options_]"],["PARAMETERS","**add** _NAME_ _URL_\n> Add new remote.\n\n**list**\n> List configured remotes.\n\n**remove** _NAME_\n> Remove remote.\n\n**rename** _OLD_ _NEW_\n> Rename remote.\n\n**--help**\n> Display help information."],["DESCRIPTION","**jj git remote** manages Git remotes for Jujutsu repositories. It configures where to fetch and push changes.\n\nThe commands mirror Git remote management. Changes are stored in Git format for compatibility."],["CAVEATS","Subcommand of jj. Uses Git remote format. Standard Git hosting works."],["HISTORY","jj git remote is part of **Jujutsu**, providing remote management compatible with Git infrastructure."],["INSTALL","```pacman: sudo pacman -S jujutsu```\n\n```apk: sudo apk add jujutsu```\n\n```zypper: sudo zypper install jujutsu```\n\n```brew: brew install jujutsu```\n\n```nix: nix profile install nixpkgs#jujutsu```"],["SEE ALSO","[jj](/man/jj)(1), [jj-git](/man/jj-git)(1), [git-remote](/man/git-remote)(1)"]],"e":["jj","jj-git","git-remote"],"x":"jj git remote add [name] [url]"},"kafkacat":{"n":"kafkacat","t":"command-line Kafka producer and consumer","s":[["TAGLINE","command-line Kafka producer and consumer"],["TLDR","**Produce messages**\n\n```echo \"[message]\" | kafkacat -P -b [localhost:9092] -t [topic]```\n\n**Consume messages**\n\n```kafkacat -C -b [localhost:9092] -t [topic]```\n\n**Consume from beginning**\n\n```kafkacat -C -b [localhost:9092] -t [topic] -o beginning```\n\n**List topics**\n\n```kafkacat -L -b [localhost:9092]```\n\n**Produce from file**\n\n```kafkacat -P -b [localhost:9092] -t [topic] [file.txt]```\n\n**Consume with format**\n\n```kafkacat -C -b [localhost:9092] -t [topic] -f '[%T] %k: %s\\n'```\n\n**Produce with key** using delimiter\n\n```echo \"[key]:[value]\" | kafkacat -P -b [localhost:9092] -t [topic] -K \":\"```\n\n**Consume N messages** and exit\n\n```kafkacat -C -b [localhost:9092] -t [topic] -c [10]```"],["SYNOPSIS","**kafkacat** [_options_]"],["PARAMETERS","**-P**\n> Producer mode.\n\n**-C**\n> Consumer mode.\n\n**-L**\n> Metadata list mode.\n\n**-b** _brokers_\n> Broker list.\n\n**-t** _topic_\n> Topic name.\n\n**-p** _partition_\n> Partition number.\n\n**-o** _offset_\n> Offset: beginning, end, stored, or number.\n\n**-K** _delim_\n> Key delimiter.\n\n**-f** _format_\n> Output format string.\n\n**-c** _count_\n> Exit after consuming count messages.\n\n**-e**\n> Exit at end of topic.\n\n**-G** _group_ _topic_\n> Consumer group mode (high-level balanced consumer).\n\n**-X** _property=value_\n> Set librdkafka property (e.g., security.protocol=SSL)."],["DESCRIPTION","**kafkacat** (also known as kcat) is a command-line Kafka producer and consumer. It's faster and simpler than Kafka's built-in shell scripts, useful for testing and debugging."],["FORMAT STRINGS","```\n%T  - Timestamp\n%k  - Key\n%s  - Message (payload)\n%p  - Partition\n%o  - Offset\n%t  - Topic\n```"],["CAVEATS","Renamed to kcat in newer versions. Requires librdkafka. Not included with Kafka distribution. Some features need specific librdkafka version."],["HISTORY","kafkacat was created by **Magnus Edenhill**, who also created librdkafka. It's been renamed to **kcat** but kafkacat remains widely used."],["INSTALL","```brew: brew install kafkacat```"],["SEE ALSO","[kcat](/man/kcat)(1), [kafka](/man/kafka)(1), [kafka-topics](/man/kafka-topics)(1)"]],"e":["kcat","kafka","kafka-topics"],"x":"echo \"[message]\" | kafkacat -P -b [localhost:9092] -t [topic]"},"kubectl-events":{"n":"kubectl-events","t":"lists cluster events","s":[["TAGLINE","lists cluster events"],["TLDR","**Show events** in the current namespace\n\n```kubectl events```\n\n**Show events** in a specific namespace\n\n```kubectl events -n [namespace]```\n\n**Show events** across all namespaces\n\n```kubectl events -A```\n\n**Events scoped to a specific resource**\n\n```kubectl events --for [pod/pod-name]```\n\n**Watch live events** as they arrive\n\n```kubectl events --watch```\n\n**Only Warnings** (filter by event type)\n\n```kubectl events --types=[Warning]```\n\n**Sort events** chronologically (oldest first)\n\n```kubectl events --sort-by=[lastTimestamp]```\n\n**Output as JSON**\n\n```kubectl events -o json```"],["SYNOPSIS","**kubectl events** [_options_]"],["PARAMETERS","**-n** _NAMESPACE_\n> Target namespace (default: current context's namespace).\n\n**-A**, **--all-namespaces**\n> List events from every namespace.\n\n**--for** _KIND/NAME_\n> Restrict to events whose **involvedObject** matches the given resource (e.g. **pod/web-0**).\n\n**--watch**, **-w**\n> Stream new events instead of exiting after one snapshot.\n\n**--types** _TYPES_\n> Comma-separated event types (Normal, Warning).\n\n**--sort-by** _FIELD_\n> Sort by a JSONPath field, typically **.lastTimestamp** or **.metadata.creationTimestamp**.\n\n**-o** _FORMAT_\n> Output format (wide, json, yaml, jsonpath, ...).\n\n**--no-headers**\n> Suppress the column header line.\n\n**--help**\n> Display help information."],["DESCRIPTION","**kubectl events** is the modern, dedicated subcommand for listing Kubernetes Event objects. It was added as an alpha in **kubectl 1.23** and promoted to GA in **1.27**, replacing the older **kubectl get events** form with a cleaner default output and live-watch support.\n\nEvents are short-lived records emitted by controllers (kubelet, scheduler, controller-manager, custom operators, ...) that describe noteworthy state changes such as pod scheduling, image pulls, liveness-probe failures, or HPA scaling decisions. Use **--for** to follow a specific Pod, Deployment, or Job, **--watch** for live debugging, and **--sort-by** to put the most recent events at the bottom for chronological reading."],["CAVEATS","Events expire after **--event-ttl** (default **1 hour**); historical incidents need an external aggregator (Loki, Elasticsearch, Datadog) or persistent storage of the Events API. The **--for** flag is exact: events emitted on a parent resource (Deployment) are not surfaced when watching the child Pod. **kubectl events** is a separate plugin in some older distributions; on those, fall back to **kubectl get events**."],["HISTORY","**kubectl events** landed as an alpha command in **Kubernetes 1.23** (December 2021), became beta in **1.25**, and reached general availability in **1.27** (April 2023). It was contributed to consolidate the various flags previously needed on **kubectl get events** to make ad-hoc cluster debugging more ergonomic."],["INSTALL","```apt: sudo apt install kubectl```\n\n```pacman: sudo pacman -S kubectl```\n\n```apk: sudo apk add kubectl```\n\n```brew: brew install kubectl```\n\n```nix: nix profile install nixpkgs#kubectl```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [kubectl-describe](/man/kubectl-describe)(1), [kubectl-logs](/man/kubectl-logs)(1)"]],"e":["kubectl","kubectl-describe","kubectl-logs"],"x":"kubectl events"},"lazyworktree":{"n":"lazyworktree","t":"Git worktree management CLI and TUI for the terminal","s":[["TAGLINE","Git worktree management CLI and TUI for the terminal"],["TLDR","**Launch the worktree TUI**\n\n```lazyworktree```\n\n**Create a new worktree**\n\n```lazyworktree create [branch_name]```\n\n**List all worktrees**\n\n```lazyworktree list```\n\n**Delete a worktree**\n\n```lazyworktree delete [branch_name]```\n\n**Run a command in a worktree**\n\n```lazyworktree exec [branch_name] -- [command]```\n\n**Rename the current worktree**\n\n```lazyworktree rename [new_name]```"],["SYNOPSIS","**lazyworktree** [_command_] [_options_]"],["SUBCOMMANDS","**create**\n> Create a new worktree from a branch, PR/MR, or issue.\n\n**list**\n> List all worktrees.\n\n**delete**\n> Delete a worktree without launching the TUI.\n\n**rename**\n> Rename a worktree (single argument renames current directory's worktree).\n\n**exec**\n> Run a command or trigger a custom command key action in a worktree."],["DESCRIPTION","**lazyworktree** is a terminal UI for managing Git worktrees with a keyboard-first workflow. It supports creating, renaming, removing, absorbing, and pruning merged worktrees, as well as staging, committing, diffing, pushing, syncing, and cherry-picking directly from the TUI.\n\nFeatures include CI/CD integration with GitHub Actions and GitLab CI (viewing check status, reading logs, restarting jobs), terminal multiplexer support for tmux and zellij sessions per worktree, OCI container execution (docker/podman), and markdown-based task notes with interactive taskboard."],["CAVEATS","Requires Git with worktree support. CI/CD features require GitHub Actions or GitLab CI configuration."],["HISTORY","**lazyworktree** was created by **chmouel** and is written in **Go** using the BubbleTea framework."],["INSTALL","```zypper: sudo zypper install lazyworktree```\n\n```nix: nix profile install nixpkgs#lazyworktree```"],["SEE ALSO","[git-worktree](/man/git-worktree)(1), [lazygit](/man/lazygit)(1)"]],"e":["git-worktree","lazygit"],"x":"lazyworktree"},"logss":{"n":"logss","t":"CLI tool for log stream splitting and visualization","s":[["TAGLINE","CLI tool for log stream splitting and visualization"],["TLDR","**Split logs by multiple regex patterns**\n\n```tail -f [logfile] | logss -c \"[pattern1]\" -c \"[pattern2]\"```\n\n**Write matched patterns to files**\n\n```tail -f [logfile] | logss -c \"[pattern]\" -o [output_dir/]```\n\n**Get input from a command instead of stdin**\n\n```logss -C \"[command]\" -c \"[pattern]\"```\n\n**Use a configuration file**\n\n```logss -f [config.yaml]```\n\n**Start in vertical view mode**\n\n```tail -f [logfile] | logss -c \"[pattern]\" -V```"],["SYNOPSIS","**logss** [_options_]"],["PARAMETERS","**-c** _PATTERN_\n> Specify a regex pattern to match and split logs by. May be `pattern`, or `pattern,command,timeout` to trigger a shell command on each match.\n\n**-C** _COMMAND_\n> Get input from a shell command instead of stdin.\n\n**-f** _FILE_\n> Load configuration from a YAML file (overrides CLI arguments).\n\n**-o** _OUTPUT_PATH_\n> Write matched lines to files in the specified output path.\n\n**-r** _MS_\n> Define render speed in milliseconds. Default: 100.\n\n**-s**\n> Start in single view mode.\n\n**-t** _N_\n> Number of threads per container for triggers. Default: 1.\n\n**-V**\n> Start in vertical view mode.\n\n**-e**\n> Exit on empty input.\n\n**-h**\n> Print help."],["DESCRIPTION","**logss** is a CLI/TUI tool for visualizing and splitting log streams in real time. It allows you to define multiple regex patterns and view matching log entries in separate panes, with automatic color coding and adjustable render speed.\n\nThe tool reads from stdin (or a command via `-C`) and distributes log lines to different views based on pattern matches. Each pattern container can optionally trigger a shell command when a match occurs, using `__line__` as a placeholder for the matched line. A dedicated container displays the raw unfiltered stream.\n\nInteractive controls include pause/resume, scroll, zoom, show/hide containers, and toggling line wrap."],["CAVEATS","By default reads from stdin; a log source must be piped in or specified with `-C`. Configuration files specified with `-f` override all CLI arguments."],["HISTORY","**logss** was created by **todoesverso** and is written in **Rust**."],["INSTALL","```aur: yay -S logss```"],["SEE ALSO","[grep](/man/grep)(1), [tail](/man/tail)(1), [multitail](/man/multitail)(1)"]],"e":["grep","tail","multitail"],"x":"tail -f [logfile] | logss -c \"[pattern1]\" -c \"[pattern2]\""},"lrzip":{"n":"lrzip","t":"Long range redundancy compression program for large files","s":[["TAGLINE","Long range redundancy compression program for large files"],["TLDR","Compress with **LZMA** (default)\n\n```lrzip [path/to/file]```\n\nCompress with **BZIP2**\n\n```lrzip --bzip2 [path/to/file]```\n\nCompress with **ZPAQ** (extreme)\n\n```lrzip --zpaq [path/to/file]```\n\nCompress with **LZO** (fast)\n\n```lrzip --lzo [path/to/file]```\n\n**Encrypt** compressed file\n\n```lrzip --encrypt [path/to/file]```\n\nSet **thread count**\n\n```lrzip --threads 8 [path/to/file]```\n\n**Decompress** a file\n\n```lrzip -d [path/to/file.lrz]```\n\nCompress and specify **output file**\n\n```lrzip -o [output.lrz] [path/to/file]```"],["SYNOPSIS","**lrzip** [_options_] [_file_]"],["DESCRIPTION","**lrzip** (Long Range ZIP) is designed for compressing large files. It uses long-range redundancy reduction followed by compression, achieving high ratios on files with distant matching data."],["PARAMETERS","**-b, --bzip2**\n> Use BZIP2 compression (balanced)\n\n**-z, --zpaq**\n> Use ZPAQ compression (maximum, slow)\n\n**-l, --lzo**\n> Use LZO compression (fast)\n\n**-g, --gzip**\n> Use GZIP compression\n\n**-e, --encrypt**\n> Encrypt with password\n\n**-p, --threads N**\n> Number of processor threads\n\n**-o, --outfile FILE**\n> Specify output filename\n\n**-d, --decompress**\n> Decompress the file\n\n**-t, --test**\n> Test compressed file integrity\n\n**-i, --info**\n> Show compression information about a compressed file\n\n**-k, --keep**\n> Keep original file\n\n**-L** _N_, **--level** _N_\n> Set compression level 1-9 (default 7)\n\n**-n, --no-compress**\n> Apply only long-range redundancy reduction without backend compression\n\n**-q, --quiet**\n> Quiet mode, reduce output verbosity\n\n**-v, --verbose**\n> Increase verbosity (can be used multiple times)"],["CAVEATS","Most effective on large files (100MB+) with long-range redundant data. ZPAQ compression is extremely slow but achieves best ratios. The default LZMA backend provides a good balance of speed and compression. Files are not compatible with standard zip/gzip tools; use **lrunzip** or **lrzip -d** to decompress. By default, the original file is removed after compression."],["INSTALL","```apt: sudo apt install lrzip```\n\n```pacman: sudo pacman -S lrzip```\n\n```apk: sudo apk add lrzip```\n\n```zypper: sudo zypper install lrzip```\n\n```brew: brew install lrzip```\n\n```nix: nix profile install nixpkgs#lrzip```"],["SEE ALSO","[lrunzip](/man/lrunzip)(1), [lrztar](/man/lrztar)(1), [gzip](/man/gzip)(1), [xz](/man/xz)(1), [bzip2](/man/bzip2)(1), [zstd](/man/zstd)(1)"]],"e":["lrunzip","lrztar","gzip","xz","bzip2","zstd"],"x":"lrzip [path/to/file]"},"modinfo":{"n":"modinfo","t":"extracts and displays information from Linux kernel modules","s":[["TAGLINE","extracts and displays information from Linux kernel modules"],["TLDR","List **all attributes** of a module\n\n```modinfo module_name```\n\nShow **specific field** only\n\n```modinfo -F author module_name```\n\nShow module **filename**\n\n```modinfo -F filename module_name```\n\nShow module **parameters**\n\n```modinfo -F parm module_name```\n\nShow module **license**\n\n```modinfo -F license module_name```"],["SYNOPSIS","**modinfo** [_-0_] [_-F field_] [_-k kernel_] _modulename_|_filename_..."],["DESCRIPTION","**modinfo** extracts and displays information from Linux kernel modules. It can show all attributes or specific fields like author, description, license, parameters, and dependencies."],["PARAMETERS","**-F, --field FIELD**\n> Show only specified field value\n\n**-k, --set-version VERSION**\n> Use modules from specified kernel version\n\n**-b, --basedir DIR**\n> Use specified directory as root for modules\n\n**-0, --null**\n> Separate field values with NUL instead of newline\n\n**-a, --author**\n> Shortcut for -F author\n\n**-d, --description**\n> Shortcut for -F description\n\n**-l, --license**\n> Shortcut for -F license\n\n**-p, --parameters**\n> Shortcut for -F parm\n\n**-n, --filename**\n> Shortcut for -F filename"],["COMMON FIELDS","**filename**: Path to module file\n**author**: Module author\n**description**: Module description\n**license**: License type (GPL, etc.)\n**version**: Module version\n**parm**: Module parameters and descriptions\n**depends**: Module dependencies\n**alias**: Module aliases\n**srcversion**: Source version hash"],["CAVEATS","Module information is embedded at compile time. Not all modules include all fields. Parameters shown by **parm** can be passed to modprobe."],["HISTORY","**modinfo** is part of **kmod**, the kernel module tools replacing older module-init-tools."],["INSTALL","```apt: sudo apt install kmod```\n\n```dnf: sudo dnf install kmod```\n\n```pacman: sudo pacman -S kmod```\n\n```apk: sudo apk add kmod```\n\n```zypper: sudo zypper install kmod```\n\n```brew: brew install kmod```\n\n```nix: nix profile install nixpkgs#kmod```"],["SEE ALSO","[modprobe](/man/modprobe)(8), [lsmod](/man/lsmod)(8), [insmod](/man/insmod)(8)"]],"e":["modprobe","lsmod","insmod"],"x":"modinfo module_name"},"mpg123":{"n":"mpg123","t":"fast, free console MPEG audio player supporting layers 1, 2, and 3","s":[["TAGLINE","fast, free console MPEG audio player supporting layers 1, 2, and 3"],["TLDR","**Play** MP3 files\n\n```mpg123 [path/to/file1.mp3] [path/to/file2.mp3]```\n\nPlay MP3 from **stdin**\n\n```cat [file.mp3] | mpg123 -```\n\nPlay with **shuffle**\n\n```mpg123 -z [path/to/*.mp3]```\n\n**Loop** playback\n\n```mpg123 --loop [3] [path/to/file.mp3]```\n\nOutput to **WAV file**\n\n```mpg123 -w [output.wav] [input.mp3]```\n\nPlay from **URL**\n\n```mpg123 [http://example.com/stream.mp3]```"],["INTERACTIVE CONTROLS","**f** - Forward to next song\n**b** - Back to beginning of song\n**s** - Stop/replay current file\n**.** - Fast forward\n**,** - Rewind\n**+/-** - Volume up/down\n**q** - Quit"],["SYNOPSIS","**mpg123** [_options_] _file-or-URL_..."],["PARAMETERS","**-o, --output _module_**\n> Select audio output module\n\n**-a, --audiodevice _device_**\n> Specify audio device\n\n**-w, --wav _file_**\n> Write output to WAV file\n\n**-s, --stdout**\n> Write raw audio to stdout\n\n**-r, --rate _rate_**\n> Set sample rate\n\n**-m, --mono**\n> Decode to mono\n\n**-z, --shuffle**\n> Shuffle playlist\n\n**-Z, --random**\n> Continuous random playback\n\n**--loop _count_**\n> Repeat playback count times\n\n**-@, --list _file_**\n> Read playlist from file\n\n**-k _frames_**\n> Skip initial frames\n\n**-n _frames_**\n> Decode only specified number of frames\n\n**-C, --control**\n> Enable terminal control keys\n\n**-q, --quiet**\n> Suppress output messages\n\n**-v, --verbose**\n> Increase verbosity\n\n**-t, --test**\n> Test decode without audio output\n\n**-b, --buffer _size_**\n> Audio buffer size in KB\n\n**-E, --equalizer _file_**\n> Apply 32-band equalizer from file\n\n**-2, --2to1**\n> Downsample at 2:1 ratio (halve output sample rate).\n\n**-4, --4to1**\n> Downsample at 4:1 ratio.\n\n**-g, --gain _value_**\n> Set audio hardware output gain.\n\n**-f, --scale _factor_**\n> Set scaling factor for output samples (volume).\n\n**--au _file_**\n> Write output as Sun AU file.\n\n**--cdr _file_**\n> Write output as raw CDR file."],["DESCRIPTION","**mpg123** is a fast, free console MPEG audio player supporting layers 1, 2, and 3. It plays local files, reads from stdin, and streams from HTTP URLs. The player supports various audio output modules including ALSA, PulseAudio, and OSS.\n\nTerminal control mode (-C) enables interactive playback control with keyboard commands for seeking, volume adjustment, and track navigation."],["CAVEATS","MPEG audio decoding is CPU-intensive, especially for layer 3. Network streaming requires bandwidth matching or exceeding the audio bitrate. Since version 1.26.0, returns non-zero exit code if any track fails to parse."],["HISTORY","**mpg123** was originally written by **Michael Hipp** in the mid-1990s and has been continuously developed since. It remains one of the most efficient console MP3 players for Unix-like systems."],["INSTALL","```dnf: sudo dnf install mpg123```\n\n```pacman: sudo pacman -S mpg123```\n\n```apk: sudo apk add mpg123```\n\n```zypper: sudo zypper install mpg123```\n\n```brew: brew install mpg123```\n\n```nix: nix profile install nixpkgs#mpg123```"],["SEE ALSO","[aplay](/man/aplay)(1), [mocp](/man/mocp)(1), [ffplay](/man/ffplay)(1)"]],"e":["aplay","mocp","ffplay"],"x":"mpg123 [path/to/file1.mp3] [path/to/file2.mp3]"},"multitail":{"n":"multitail","t":"allows viewing multiple log files in a single terminal window","s":[["TAGLINE","allows viewing multiple log files in a single terminal window"],["TLDR","**Follow multiple files**\n\n```multitail [file1.log] [file2.log]```\n\n**Split screen horizontally**\n\n```multitail -s 2 [file1.log] [file2.log]```\n\n**Merge files in one window**\n\n```multitail -I [file1.log] -I [file2.log]```\n\n**Follow with color scheme**\n\n```multitail -cS [apache] [access.log]```\n\n**Execute and follow command**\n\n```multitail -l \"[command]\"```\n\n**Follow with timestamp**\n\n```multitail -ts [file.log]```"],["SYNOPSIS","**multitail** [_options_] [_files_...]"],["DESCRIPTION","**multitail** allows viewing multiple log files in a single terminal window. It splits the screen to show different files or merges them with color coding.\n\nThe tool provides filtering, searching, and highlighting capabilities for effective log monitoring."],["PARAMETERS","**-s** _n_\n> Split in n columns.\n\n**-I** _file_\n> Merge file into previous.\n\n**-l** _cmd_\n> Execute command and follow.\n\n**-L** _cmd_\n> Same as -l but restart on exit.\n\n**-cS** _scheme_\n> Color scheme.\n\n**-ts**\n> Add timestamp.\n\n**-f**\n> Follow mode.\n\n**-i** _file_\n> Add file in new window.\n\n**-n** _lines_\n> Number of lines per window.\n\n**-m** _lines_\n> Scrollback buffer size."],["CAVEATS","Complex layouts take practice. Color schemes need configuration. Many files may clutter display. Resource usage scales with files."],["HISTORY","**multitail** was created by **Folkert van Heusden** to extend tail's functionality to multiple files. It addresses the common need to monitor several log files simultaneously."],["INSTALL","```apt: sudo apt install multitail```\n\n```dnf: sudo dnf install multitail```\n\n```apk: sudo apk add multitail```\n\n```zypper: sudo zypper install multitail```\n\n```brew: brew install multitail```\n\n```nix: nix profile install nixpkgs#multitail```"],["SEE ALSO","[tail](/man/tail)(1), [less](/man/less)(1), [lnav](/man/lnav)(1), [watch](/man/watch)(1)"]],"e":["tail","less","lnav","watch"],"x":"multitail [file1.log] [file2.log]"},"newsboat":{"n":"newsboat","t":"terminal RSS/Atom feed reader forked from Newsbeuter","s":[["TAGLINE","terminal RSS/Atom feed reader forked from Newsbeuter"],["TLDR","**Start newsboat**\n\n```newsboat```\n\n**Import OPML feed list**\n\n```newsboat -i [feeds.opml]```\n\n**Export feeds to OPML**\n\n```newsboat -e > [feeds.opml]```\n\n**Use alternate config file**\n\n```newsboat -C [/path/to/config]```\n\n**Use alternate URL file**\n\n```newsboat -u [/path/to/urls]```\n\n**Refresh feeds and exit**\n\n```newsboat -x reload```\n\n**Specify cache file**\n\n```newsboat -c [/path/to/cache.db]```"],["SYNOPSIS","**newsboat** [_-C config_] [_-u urls_] [_-c cache_] [_-i opml_] [_-e_] [_options_]"],["PARAMETERS","**-C**, **--config-file** _file_\n> Alternate config file.\n\n**-u**, **--url-file** _file_\n> Alternate URL file.\n\n**-c**, **--cache-file** _file_\n> Alternate cache database.\n\n**-i**, **--import-from-opml** _file_\n> Import feeds from OPML.\n\n**-e**, **--export-to-opml**\n> Export feeds to OPML on stdout.\n\n**-r**, **--refresh-on-start**\n> Refresh feeds on startup.\n\n**-x** _command_, **--execute** _command_\n> Execute command (reload, print-unread).\n\n**-l** _level_, **--log-level** _level_\n> Logging level (1-6).\n\n**-q**, **--quiet**\n> Quiet startup.\n\n**-v**, **--version**\n> Show version.\n\n**-h**, **--help**\n> Show help."],["KEYBOARD SHORTCUTS","**r**\n> Reload current feed.\n\n**R**\n> Reload all feeds.\n\n**Enter**\n> Open selected item.\n\n**o**\n> Open in browser.\n\n**n / p**\n> Next / previous unread.\n\n**j / k**\n> Move down / up.\n\n**q**\n> Quit / back.\n\n**A**\n> Mark all as read.\n\n**s**\n> Save article.\n\n**/**\n> Search.\n\n**?**\n> Show help."],["DESCRIPTION","**Newsboat** is a terminal RSS/Atom feed reader forked from Newsbeuter. It displays feeds in a customizable text interface with vim-like keybindings.\n\nFeeds are configured in **~/.newsboat/urls**, one URL per line. Tags can be added after URLs for organization. Query feeds allow filtering across multiple feeds by criteria like unread status or age.\n\nThe config file (~/.newsboat/config) controls appearance and behavior: colors, keybindings, browser command, refresh settings, and macros. Extensive customization is possible.\n\nArticles are cached locally in SQLite, enabling offline reading. Automatic cleanup removes old articles. The cache can be shared across machines.\n\nIntegration with external tools includes: opening links in browsers, saving articles, piping content to scripts, and executing shell commands on articles. Podcast support downloads enclosures.\n\nFilter expressions select articles by date, author, title, or content. Bookmarking sends articles to external services or scripts."],["CAVEATS","Text-only interface may not render all HTML content well. Images not displayed (requires external viewer). Feed parsing depends on feed quality. Large numbers of feeds can slow refresh. Some dynamic web content not supported."],["HISTORY","**Newsboat** was forked from **Newsbeuter** in **2017** after Newsbeuter was abandoned. Newsbeuter (German for \"news pirate\") was created by **Andreas Krennmair** around **2007**. The fork maintains active development, adding features while preserving the minimalist, keyboard-driven philosophy. It's become the de facto terminal feed reader for Linux users."],["INSTALL","```apt: sudo apt install newsboat```\n\n```dnf: sudo dnf install newsboat```\n\n```pacman: sudo pacman -S newsboat```\n\n```apk: sudo apk add newsboat```\n\n```zypper: sudo zypper install newsboat```\n\n```brew: brew install newsboat```\n\n```nix: nix profile install nixpkgs#newsboat```"],["SEE ALSO","[rss2email](/man/rss2email)(1), [elfeed](/man/elfeed)(1), [snownews](/man/snownews)(1), [rsstail](/man/rsstail)(1)"]],"e":["rss2email","elfeed","snownews","rsstail"],"x":"newsboat"},"notable":{"n":"notable","t":"markdown-based note-taking application","s":[["TAGLINE","markdown-based note-taking application"],["TLDR","**Start Notable**\n\n```notable```\n\n**Open specific data directory**\n\n```notable --data-dir [~/notes]```\n\n**Import notes**\n\n```notable --import [notes/]```"],["SYNOPSIS","**notable** [_options_]"],["PARAMETERS","**--data-dir** _path_\n> Notes directory.\n\n**--import** _path_\n> Import notes.\n\n**--help**\n> Show help.\n\n**--version**\n> Show version."],["DESCRIPTION","**Notable** is a Markdown-based note-taking application. It stores notes as plain Markdown files, supports tags, attachments, and provides a clean editing interface.\n\nNotable is available as a desktop app built with Electron."],["FEATURES","```\n- Markdown notes\n- Tags and notebooks\n- Split editor/preview\n- KaTeX math support\n- Mermaid diagrams\n- Multi-note editor\n```"],["DATA STRUCTURE","```\nnotes/\n├── Notes/           # Markdown files\n├── Attachments/     # Attached files\n└── notable.json     # Metadata\n```"],["CAVEATS","Electron-based (resource usage). Free version has limitations. Notes stored locally only."],["HISTORY","Notable was created by **Fabio Spampinato** in **2018** as a markdown-native note-taking app inspired by Sublime Text."],["INSTALL","```nix: nix profile install nixpkgs#notable```"],["SEE ALSO","[joplin](/man/joplin)(1), [obsidian](/man/obsidian)(1)"]],"e":["joplin","obsidian"],"x":"notable"},"npm-init":{"n":"npm-init","t":"creates a new package","s":[["TAGLINE","creates a new package"],["TLDR","**Initialize new project**\n\n```npm init```\n\n**Initialize with defaults**\n\n```npm init -y```\n\n**Create with initializer**\n\n```npm init [initializer]```\n\n**Create React app**\n\n```npm init react-app [my-app]```\n\n**Create Vite project**\n\n```npm init vite@latest [my-app]```\n\n**Set default author**\n\n```npm config set init-author-name \"[name]\"```"],["SYNOPSIS","**npm init** [_options_] [_initializer_]"],["PARAMETERS","**-y**, **--yes**\n> Accept all defaults.\n\n**--scope** _scope_\n> Create scoped package.\n\n**-w**, **--workspace**\n> Create workspace.\n\n**initializer**\n> Package to run (create-*)."],["DESCRIPTION","**npm init** creates a new package.json file. Without an initializer, it prompts for package details. With an initializer, it runs that package's setup process.\n\nInitializers like \"react-app\" are shorthand for \"create-react-app\"."],["Interactive","npm init"],["With defaults","npm init -y"],["Using initializer (runs create-react-app)","npm init react-app my-app"],["Scoped package","npm init --scope=@myorg\n```"],["PACKAGE.JSON TEMPLATE","```json\n{\n  \"name\": \"my-package\",\n  \"version\": \"1.0.0\",\n  \"description\": \"\",\n  \"main\": \"index.js\",\n  \"scripts\": { \"test\": \"echo \\\"Error: no test\\\" && exit 1\" },\n  \"keywords\": [],\n  \"author\": \"\",\n  \"license\": \"ISC\"\n}\n```"],["CAVEATS","-y uses defaults which may need editing. Initializers download packages. Workspace mode for monorepos."],["SEE ALSO","[npm](/man/npm)(1), [npm-install](/man/npm-install)(1), [npm-config](/man/npm-config)(1)"],["INSTALL","```pacman: sudo pacman -S npm```\n\n```apk: sudo apk add npm```\n\n```brew: brew install npm```"]],"e":["npm","npm-install","npm-config"],"x":"npm init"},"octez-baker":{"n":"octez-baker","t":"tezos baking daemon","s":[["TAGLINE","tezos baking daemon"],["TLDR","**Start baker**\n\n```octez-baker-[protocol] run with local node [~/.tezos-node] [baker_alias]```\n\n**Run with RPC endpoint**\n\n```octez-baker-[protocol] --endpoint [http://localhost:8732] run with local node [data_dir] [baker]```\n\n**Run with votes**\n\n```octez-baker-[protocol] run with local node [data_dir] [baker] --liquidity-baking-toggle-vote [pass]```\n\n**List known bakers**\n\n```octez-baker-[protocol] list known addresses```"],["SYNOPSIS","**octez-baker-_protocol_** [_options_] _command_"],["PARAMETERS","**run**\n> Run the baker daemon.\n\n**--endpoint** _uri_\n> Node RPC endpoint.\n\n**--liquidity-baking-toggle-vote** _vote_\n> on, off, or pass.\n\n**with local node** _dir_\n> Node data directory."],["DESCRIPTION","**octez-baker** is the Tezos baking daemon. Bakers create new blocks and participate in consensus. The baker monitors the blockchain and produces blocks when selected.\n\nThe command name includes the protocol version (e.g., octez-baker-PtParisA)."],["REQUIREMENTS","```\n- Running octez-node\n- Registered baker key\n- Sufficient stake\n- Key imported to client\n```"],["CAVEATS","Requires registered delegate. Protocol-specific binary. Needs synchronized node. Stake requirements apply."],["HISTORY","The Tezos baker is part of the **Tezos** blockchain developed by **Arthur and Kathleen Breitman**, using liquid proof-of-stake consensus."],["SEE ALSO","[octez-node](/man/octez-node)(1), [octez-client](/man/octez-client)(1)"]],"e":["octez-node","octez-client"],"x":"octez-baker-[protocol] run with local node [~/.tezos-node] [baker_alias]"},"openapi-generator":{"n":"openapi-generator","t":"generates API clients, server stubs, and documentation from OpenAPI","s":[["TAGLINE","generates API clients, server stubs, and documentation from OpenAPI"],["TLDR","**Generate client library**\n\n```openapi-generator generate -i [spec.yaml] -g [python] -o [./client]```\n\n**Generate server stub**\n\n```openapi-generator generate -i [spec.yaml] -g [spring] -o [./server]```\n\n**List available generators**\n\n```openapi-generator list```\n\n**Generate with config file**\n\n```openapi-generator generate -i [spec.yaml] -g [typescript-axios] -c [config.yaml] -o [./client]```\n\n**Validate spec**\n\n```openapi-generator validate -i [spec.yaml]```\n\n**Generate documentation**\n\n```openapi-generator generate -i [spec.yaml] -g [html2] -o [./docs]```"],["SYNOPSIS","**openapi-generator** _command_ [_options_]"],["PARAMETERS","**generate**\n> Generate code.\n\n**validate**\n> Validate spec.\n\n**list**\n> List generators.\n\n**-i** _file_\n> Input spec file.\n\n**-g** _generator_\n> Generator name.\n\n**-o** _dir_\n> Output directory.\n\n**-c** _file_\n> Config file.\n\n**-p** _params_\n> Additional properties."],["DESCRIPTION","**OpenAPI Generator** generates API clients, server stubs, and documentation from OpenAPI (Swagger) specifications. It supports 50+ languages and frameworks.\n\nThe tool is a fork of Swagger Codegen with active community development."],["POPULAR GENERATORS","```\nClients: python, typescript-axios, go, java\nServers: spring, python-flask, go-server\nDocs: html2, markdown\n```"],["CAVEATS","Generated code may need customization. Large specs produce many files. Some generators more mature than others."],["HISTORY","OpenAPI Generator was forked from Swagger Codegen in **2018** by **William Cheng** and others to create a more community-driven project."],["INSTALL","```pacman: sudo pacman -S openapi-generator```\n\n```brew: brew install openapi-generator```"],["SEE ALSO","[oapi-codegen](/man/oapi-codegen)(1)"]],"e":["oapi-codegen"],"x":"openapi-generator generate -i [spec.yaml] -g [python] -o [./client]"},"pamnoraw":{"n":"pamnoraw","t":"converts binary PNM images to plain (ASCII) format","s":[["TAGLINE","converts binary PNM images to plain (ASCII) format"],["TLDR","**Convert PNM image to** plain text format\n\n```pamnoraw [input.ppm] > [output.ppm]```"],["SYNOPSIS","**pamnoraw** [_file_]"],["DESCRIPTION","**pamnoraw** converts binary (raw) PNM images to plain (ASCII) format. Plain format is human-readable but much larger. Equivalent to running `pamtopnm -plain`. Note that PAM images have no plain format, so this tool only operates on PBM, PGM, and PPM inputs. Part of the Netpbm toolkit."],["SEE ALSO","[pamtopnm](/man/pamtopnm)(1), [pnmtoplainpnm](/man/pnmtoplainpnm)(1), [pamfile](/man/pamfile)(1), [netpbm](/man/netpbm)(1)"]],"e":["pamtopnm","pnmtoplainpnm","pamfile","netpbm"],"x":"pamnoraw [input.ppm] > [output.ppm]"},"pg_rewind":{"n":"pg_rewind","t":"synchronize a PostgreSQL data directory with another copy","s":[["TAGLINE","synchronize a PostgreSQL data directory with another copy"],["TLDR","**Rewind target against a running source server**\n\n```pg_rewind -D [target_dir] --source-server=\"host=[host] user=[rewind_user] dbname=[postgres]\"```\n\n**Rewind against a locally shut-down source**\n\n```pg_rewind -D [target_dir] --source-pgdata=[source_dir]```\n\n**Dry run** (no changes written)\n\n```pg_rewind -n -D [target_dir] --source-server=\"[conninfo]\"```\n\n**Rewind and write recovery configuration** for standby mode\n\n```pg_rewind -R -D [target_dir] --source-server=\"[conninfo]\"```\n\n**Show progress** while copying data\n\n```pg_rewind -P -D [target_dir] --source-pgdata=[source_dir]```\n\n**Restore missing WAL files** from the archive\n\n```pg_rewind -c -D [target_dir] --source-server=\"[conninfo]\"```"],["SYNOPSIS","**pg_rewind** [_option_...] **{-D** | **--target-pgdata}** _directory_ **{--source-pgdata**=_directory_ | **--source-server**=_connstr_**}**"],["PARAMETERS","**-D**, **--target-pgdata** _dir_\n> Target data directory to be modified. Must have been cleanly shut down.\n\n**--source-pgdata** _dir_\n> File-system path of a cleanly shut-down source cluster.\n\n**--source-server** _connstr_\n> libpq connection string for a running source server.\n\n**-n**, **--dry-run**\n> Perform all work without modifying the target directory.\n\n**-N**, **--no-sync**\n> Return without waiting for changes to be flushed to disk.\n\n**-P**, **--progress**\n> Show progress while copying files.\n\n**-R**, **--write-recovery-conf**\n> Create `standby.signal` and append connection settings to `postgresql.auto.conf`.\n\n**-c**, **--restore-target-wal**\n> Use `restore_command` to fetch WAL files missing from `pg_wal`.\n\n**--config-file** _file_\n> Main server configuration file for the target cluster.\n\n**--no-ensure-shutdown**\n> Do not run single-user recovery; fail if the target is not cleanly shut down.\n\n**--sync-method** _method_\n> Method used to flush changes: `fsync` (default) or `syncfs`.\n\n**--debug**\n> Print verbose debugging output.\n\n**-V**, **--version**\n> Print version information.\n\n**-?**, **--help**\n> Show help."],["DESCRIPTION","**pg_rewind** resynchronizes a PostgreSQL data directory with another copy of the same cluster after their timelines have diverged. Typical use is to re-attach a former primary as a standby after a failover without taking a full base backup.\n\nIt identifies the point where the timelines diverged, then copies from the source only the blocks that changed in the target after that point, along with all current configuration, WAL, and other required files. The source must have `wal_log_hints` enabled or be initialized with data checksums, and `full_page_writes` must be on."],["CAVEATS","The target cluster must be cleanly shut down. The source must be on the same major version and share the same system identifier. Replication slots, statistics, and contents of `pg_dynshmem` are not copied. Always take a backup of the target before running pg_rewind on production data."],["HISTORY","Introduced in PostgreSQL **9.5** (2016) by Heikki Linnakangas, and moved into the core distribution from a contrib module. In PostgreSQL **13** pg_rewind gained the ability to write recovery configuration (`-R`) and restore missing WAL from the archive (`-c`)."],["INSTALL","```dnf: sudo dnf install postgresql17```\n\n```apk: sudo apk add postgresql17```\n\n```zypper: sudo zypper install postgresql17```"],["SEE ALSO","[pg_basebackup](/man/pg_basebackup)(1), [pg_resetwal](/man/pg_resetwal)(1)"]],"e":["pg_basebackup","pg_resetwal"],"x":"pg_rewind -D [target_dir] --source-server=\"host=[host] user=[rewind_user] dbname=[postgres]\""},"pip-debug":{"n":"pip-debug","t":"Display pip environment debug information","s":[["TAGLINE","Display pip environment debug information"],["TLDR","**Show debug information**\n\n```pip debug```\n\n**Show verbose debug info**\n\n```pip debug --verbose```"],["SYNOPSIS","**pip** **debug** [_options_]"],["PARAMETERS","**-v**, **--verbose**\n> Show detailed debug information including all platform tags and resolved compatibility specs.\n\n**--platform** _platform_\n> Display compatibility tags for the given platform.\n\n**--python-version** _version_\n> Display compatibility tags for the given Python version.\n\n**--implementation** _impl_\n> Display compatibility tags for the given Python implementation (e.g. **cp**, **pp**).\n\n**--abi** _abi_\n> Display compatibility tags for the given ABI."],["DESCRIPTION","**pip debug** prints debugging information about pip and the current Python environment. Output includes the pip version, location, sys.path, the active Python interpreter, the supported wheel compatibility tags, and the configured indexes.\n\nThe command is intended for diagnosing wheel selection problems (e.g. \"no matching distribution found\"), index configuration issues, and SSL/cert misconfiguration. It is **not** a stable interface — output format may change between pip releases and should not be parsed by scripts."],["CAVEATS","Marked as unstable: pip explicitly warns \"This command is only meant for debugging\" and may change format. Combine with **--verbose** to see full tag enumeration."],["INSTALL","```apk: sudo apk add py3-pip```"],["SEE ALSO","[pip](/man/pip)(1), [pip-config](/man/pip-config)(1), [pip-list](/man/pip-list)(1)"]],"e":["pip","pip-config","pip-list"],"x":"pip debug"},"ppmtompeg":{"n":"ppmtompeg","t":"Encode PPM image sequences to MPEG video","s":[["TAGLINE","Encode PPM image sequences to MPEG video"],["TLDR","**Encode PPM frames to MPEG using a parameter file**\n\n```ppmtompeg [path/to/param_file]```\n\n**Encode with statistics output**\n\n```ppmtompeg -stat [path/to/stats.txt] [path/to/param_file]```\n\n**Encode silently** showing only errors\n\n```ppmtompeg -realquiet [path/to/param_file]```\n\n**Encode a specific frame range**\n\n```ppmtompeg -frames [0] [99] [path/to/param_file]```"],["SYNOPSIS","**ppmtompeg** [_options_] _parameter_file_"],["PARAMETERS","_parameter_file_\n> File with encoding parameters.\n\n**-stat** _file_\n> Append encoding statistics to a file.\n\n**-realquiet**\n> Suppress all output except errors.\n\n**-quiet** _n_\n> Limit time-remaining reports to every n seconds.\n\n**-no_frame_summary**\n> Suppress per-frame summary lines.\n\n**-float_dct**\n> Use more accurate but slower floating-point DCT.\n\n**-gop** _num_\n> Encode only a specific numbered GOP.\n\n**-combine_gops**\n> Merge separate GOP files into a single MPEG stream.\n\n**-frames** _first_ _last_\n> Encode only the specified frame range.\n\n**-combine_frames**\n> Merge individual MPEG frames into one stream.\n\n**-nice**\n> Run remote processes at low priority.\n\n**-snr**\n> Include signal-to-noise ratio in statistics."],["DESCRIPTION","**ppmtompeg** encodes PPM image sequences to MPEG-1 video bitstreams. All encoding settings (input files, output file, frame pattern, GOP size, quality) are specified in a parameter file rather than on the command line. The parameter file format is case-sensitive and uses keywords like INPUT_DIR, PATTERN, GOP_SIZE, and BASE_FILE_FORMAT. Part of the Netpbm toolkit. The -gop, -combine_gops, -frames, and -combine_frames options are mutually exclusive."],["CAVEATS","Legacy encoder. Consider ffmpeg for modern video encoding."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[ffmpeg](/man/ffmpeg)(1)"]],"e":["ffmpeg"],"x":"ppmtompeg [path/to/param_file]"},"pulumi-install":{"n":"pulumi-install","t":"Install Pulumi project dependencies and plugins","s":[["TAGLINE","Install Pulumi project dependencies and plugins"],["TLDR","**Install project dependencies and plugins**\n\n```pulumi install```\n\n**Install without language dependencies**\n\n```pulumi install --no-dependencies```\n\n**Install without plugins**\n\n```pulumi install --no-plugins```\n\n**Reinstall all plugins even if they exist**\n\n```pulumi install --reinstall```\n\n**Install with custom parallelism**\n\n```pulumi install --parallel [8]```\n\n**Use language version tools to set up the runtime**\n\n```pulumi install --use-language-version-tools```"],["SYNOPSIS","**pulumi** **install** [_options_]"],["PARAMETERS","**--no-dependencies**\n> Skip installing language dependencies.\n\n**--no-plugins**\n> Skip installing plugins.\n\n**--reinstall**\n> Reinstall plugins even if they already exist.\n\n**--parallel** _int_\n> Max number of concurrent installs (default 4).\n\n**--use-language-version-tools**\n> Use language version tools to setup and install the language runtime."],["DESCRIPTION","**pulumi install** installs packages and plugins required by your Pulumi program or policy pack. If your **Pulumi.yaml** file contains a 'packages' section, this command automatically installs SDKs for all declared packages. Run after cloning a project or updating dependencies."],["INSTALL","```pacman: sudo pacman -S pulumi```\n\n```apk: sudo apk add pulumi```\n\n```brew: brew install pulumi```\n\n```nix: nix profile install nixpkgs#pulumi```"],["SEE ALSO","[pulumi](/man/pulumi)(1), [pulumi-plugin](/man/pulumi-plugin)(1), [pulumi-new](/man/pulumi-new)(1), [pulumi-up](/man/pulumi-up)(1)"]],"e":["pulumi","pulumi-plugin","pulumi-new","pulumi-up"],"x":"pulumi install"},"quiz":{"n":"quiz","t":"Educational quiz game from BSD games","s":[["TAGLINE","Educational quiz game from BSD games"],["TLDR","**Start quiz game**\n\n```quiz```\n\n**Quiz with specific categories**\n\n```quiz [category1] [category2]```\n\n**List available categories**\n\n```quiz -l```"],["SYNOPSIS","**quiz** [_options_] [_categories_]"],["PARAMETERS","_CATEGORIES_\n> Quiz categories.\n\n**-l**\n> List categories.\n\n**-t**\n> Show time for each."],["DESCRIPTION","**quiz** is an educational quiz game from the BSD games collection that tests knowledge across various categories. It presents questions from built-in databases and checks answers interactively, covering topics such as geography, history, science, and language.\n\nCategories can be specified as arguments to focus on particular subjects, and the **-l** flag lists all available quiz databases installed on the system."],["CAVEATS","Part of bsdgames. Various quiz databases."],["HISTORY","quiz is a **BSD games** educational utility."],["SEE ALSO","[bsdgames](/man/bsdgames)(6), [arithmetic](/man/arithmetic)(6)"]],"e":["bsdgames","arithmetic"],"x":"quiz"},"rustup-set":{"n":"rustup-set","t":"Configure rustup settings and defaults","s":[["TAGLINE","Configure rustup settings and defaults"],["TLDR","**Set the default host triple**\n\n```rustup set default-host [x86_64-unknown-linux-gnu]```\n\n**Set the installation profile to minimal**\n\n```rustup set profile minimal```\n\n**Set the installation profile to default** (includes rustfmt and clippy)\n\n```rustup set profile default```\n\n**Enable automatic self-updates**\n\n```rustup set auto-self-update enable```\n\n**Disable automatic self-updates**\n\n```rustup set auto-self-update disable```"],["SYNOPSIS","**rustup** **set** _setting_ _value_"],["SUBCOMMANDS","**default-host** _triple_\n> Set the default host triple for toolchain installations (e.g., x86_64-unknown-linux-gnu).\n\n**profile** _name_\n> Set the default component installation profile. **minimal** includes only rustc, rust-std, and cargo. **default** adds rust-docs, rustfmt, and clippy. **complete** installs all available components.\n\n**auto-self-update** _mode_\n> Control rustup's automatic self-update behavior: **enable**, **disable**, or **check-only**."],["DESCRIPTION","**rustup set** configures rustup settings that affect default behavior for new toolchain installations and updates. The profile setting controls which components are installed by default, and the auto-self-update setting controls whether rustup updates itself when running `rustup update`."],["INSTALL","```apt: sudo apt install rustup```\n\n```dnf: sudo dnf install rustup```\n\n```pacman: sudo pacman -S rustup```\n\n```zypper: sudo zypper install rustup```\n\n```brew: brew install rustup```\n\n```nix: nix profile install nixpkgs#rustup```"],["SEE ALSO","[rustup](/man/rustup)(1), [rustup-show](/man/rustup-show)(1)"]],"e":["rustup","rustup-show"],"x":"rustup set default-host [x86_64-unknown-linux-gnu]"},"snapd":{"n":"snapd","t":"Background service for snap package management","s":[["TAGLINE","Background service for snap package management"],["TLDR","**Start the snapd service**\n\n```sudo systemctl start snapd```\n\n**Enable snapd to start on boot**\n\n```sudo systemctl enable snapd```\n\n**Check snapd service status**\n\n```systemctl status snapd```\n\n**Restart the snapd service**\n\n```sudo systemctl restart snapd```\n\n**Enable classic snap support** (create symlink)\n\n```sudo ln -s /var/lib/snapd/snap /snap```"],["SYNOPSIS","**snapd** [_--version_]"],["DESCRIPTION","**snapd** is the background service that manages snap packages on Linux systems. It handles installation, updates, removal, and security confinement of snaps through a REST API interface.\n\nSnaps are self-contained application packages bundling all dependencies, enabling cross-distribution compatibility. The daemon automatically checks for updates four times daily and applies them in the background.\n\nsnapd enforces application sandboxing using AppArmor and seccomp, mediating access between snaps and the host system. Users interact with snapd through the **snap** command-line client.\n\nThe service must be running before any snap operations can be performed."],["CAVEATS","Classic snap support requires a symlink from /snap to /var/lib/snapd/snap. On SELinux-enabled systems (like Fedora), snap sandboxing is degraded since AppArmor and SELinux cannot both be fully active. Snaps may consume more disk space than traditional packages due to bundled dependencies."],["HISTORY","Snap was developed by **Canonical** as part of Ubuntu, first introduced in **Ubuntu 16.04** (2016). Originally called Snappy, it was designed to provide universal Linux packages that work across distributions with consistent behavior and automatic updates."],["INSTALL","```dnf: sudo dnf install snapd```"],["SEE ALSO","[snap](/man/snap)(8), [flatpak](/man/flatpak)(1), [apparmor](/man/apparmor)(7)"]],"e":["snap","flatpak","apparmor"],"x":"sudo systemctl start snapd"},"ssdeep":{"n":"ssdeep","t":"","s":[["TLDR","**Compute fuzzy hash** of files\n\n```ssdeep [file1] [file2]```\n\n**Compute hashes recursively**\n\n```ssdeep -r [directory/]```\n\n**Compare files** and show matches\n\n```ssdeep -p [file1] [file2] [file3]```\n\n**Match files against known hashes**\n\n```ssdeep -m [known_hashes.txt] [files_to_check]```\n\n**Compare two hash files**\n\n```ssdeep -k [known.txt] [computed.txt]```\n\n**Set match threshold** (0-100)\n\n```ssdeep -p -t [50] [files]```\n\n**Output in CSV format**\n\n```ssdeep -c [files]```"],["SYNOPSIS","**ssdeep** [**-m** _file_] [**-k** _file_] [**-vprdsblcxag**] [**-t** _val_] [_files_]"],["PARAMETERS","**-m** _file_\n> Load known hashes from file and match against computed hashes from input files.\n\n**-k** _file_\n> Compare known signatures in file against pre-computed signatures in input files.\n\n**-p**\n> Pretty matching mode. Compare all input files against each other.\n\n**-d**\n> Match each file against known set, then add its hash to the known set.\n\n**-r**\n> Recursive mode. Traverse all subdirectories.\n\n**-t** _val_\n> Match threshold (0-100). Only display matches above this score. Default: 0.\n\n**-a**\n> Display all matches regardless of score.\n\n**-v**\n> Verbose mode. Print filename to stderr while hashing.\n\n**-b**\n> Use bare filenames (no path) in output.\n\n**-l**\n> Use relative paths in output.\n\n**-c**\n> Output in CSV format.\n\n**-s**\n> Silent mode. Suppress warnings.\n\n**-x**\n> Signature file matching. Treat input files as signature files and compare them against the known set.\n\n**-g**\n> Cluster mode. Group similar files into clusters of related items."],["DESCRIPTION","**ssdeep** computes context-triggered piecewise hashes (CTPH), also called fuzzy hashes. Unlike cryptographic hashes that produce completely different outputs for slightly different inputs, fuzzy hashes can identify files that are similar but not identical.\n\nThe tool is useful for malware analysis, identifying modified documents, finding near-duplicate files, and digital forensics. Two files with a match score above zero share some common sequences of bytes. Scores range from 0 (no match) to 100 (very similar or identical).\n\nOutput format includes the block size, two hash components, and the filename. This output can be saved and used later for matching with the **-m** or **-k** options."],["CAVEATS","Fuzzy hashing is not a replacement for cryptographic hashes when verifying exact file integrity. Small files may not produce meaningful fuzzy hashes. Match scores are approximate; manual verification may be needed for forensic use. The tool cannot detect similarity if files have been significantly restructured."],["HISTORY","**ssdeep** was created by Jesse Kornblum based on the spamsum algorithm developed by Dr. Andrew Tridgell for spam detection. The project provides both a command-line tool and the **libfuzzy** library for programmatic access. It's widely used in digital forensics, malware analysis, and security research. The tool is distributed under the GNU General Public License."],["INSTALL","```apt: sudo apt install ssdeep```\n\n```dnf: sudo dnf install ssdeep```\n\n```pacman: sudo pacman -S ssdeep```\n\n```apk: sudo apk add ssdeep```\n\n```zypper: sudo zypper install ssdeep```\n\n```brew: brew install ssdeep```\n\n```nix: nix profile install nixpkgs#ssdeep```"],["SEE ALSO","[md5sum](/man/md5sum)(1), [sha256sum](/man/sha256sum)(1), [hashdeep](/man/hashdeep)(1)"],["RESOURCES","```[Source code](https://github.com/ssdeep-project/ssdeep)```\n\n```[Homepage](https://ssdeep-project.github.io/ssdeep/)```"]],"e":["md5sum","sha256sum","hashdeep"],"x":"ssdeep [file1] [file2]"},"swapon":{"n":"swapon","t":"Enable swap devices and files","s":[["TAGLINE","Enable swap devices and files"],["TLDR","Show **swap information**\n\n```swapon```\n\n**Enable** a swap area\n\n```sudo swapon /path/to/swapfile```\n\nEnable **all** swap areas from /etc/fstab\n\n```sudo swapon -a```\n\nEnable swap by **label**\n\n```sudo swapon -L swap_label```\n\nEnable swap by **UUID**\n\n```sudo swapon -U uuid```\n\nShow swap **summary**\n\n```swapon -s```"],["SYNOPSIS","**swapon** [_options_] [_specialfile_...]"],["DESCRIPTION","**swapon** enables devices and files for paging and swapping. Swap space can be specified by device path, label (-L), or UUID (-U). The command is typically invoked during system startup to distribute paging activity across multiple storage resources."],["PARAMETERS","**-a, --all**\n> Enable all swap devices marked in /etc/fstab\n\n**-d, --discard[=policy]**\n> Enable trim/discard operations; policy can be 'once' or 'pages'\n\n**-e, --ifexists**\n> Silently skip devices that do not exist\n\n**-f, --fixpgsz**\n> Reinitialize swap space if page size mismatches kernel\n\n**-L label**\n> Use swap partition with the specified label\n\n**-U uuid**\n> Use swap partition with the specified UUID\n\n**-p, --priority priority**\n> Set swap priority (-1 to 32767, higher is preferred)\n\n**-s, --summary**\n> Display swap usage summary (deprecated, use --show)\n\n**--show[=column...]**\n> Display customizable swap area table\n\n**-T, --fstab path**\n> Use alternative fstab file\n\n**-v, --verbose**\n> Enable verbose output"],["CAVEATS","Higher priority swap areas are used first. When areas have equal priority, they are used in round-robin fashion. The discard option can improve SSD performance but may cause delays."],["HISTORY","**swapon** is part of the **util-linux** package. Swap space management has been a core part of Unix-like systems for managing virtual memory."],["INSTALL","```apt: sudo apt install mount```\n\n```apk: sudo apk add util-linux-misc```\n\n```nix: nix profile install nixpkgs#mount```"],["SEE ALSO","[swapoff](/man/swapoff)(8), [mkswap](/man/mkswap)(8), [fstab](/man/fstab)(5), [free](/man/free)(1)"]],"e":["swapoff","mkswap","fstab","free"],"x":"swapon"},"systemctl-is-enabled":{"n":"systemctl-is-enabled","t":"Check unit boot enablement state","s":[["TAGLINE","Check unit boot enablement state"],["TLDR","Check **enablement** state\n\n```systemctl is-enabled [unit]```\n\nCheck **multiple** units\n\n```systemctl is-enabled [unit1 unit2 ...]```\n\n**Quiet** mode (exit code only)\n\n```systemctl is-enabled [unit] -q```\n\nShow **targets** and symlinks\n\n```systemctl is-enabled [unit] -l```"],["SYNOPSIS","**systemctl is-enabled** [_OPTIONS_] _UNIT_..."],["PARAMETERS","**-q, --quiet**\n> Suppress output, return only exit code\n\n**-l, --full**\n> Do not truncate output; also show installation targets in the output\n\n**--system**\n> Check system units (default)\n\n**--user**\n> Check user units"],["DESCRIPTION","**systemctl is-enabled** checks whether unit files are enabled to start at boot. It returns the enablement state: enabled, disabled, static, masked, generated, transient, indirect, or alias.\n\nExit code 0 indicates the unit is enabled or will start at boot via other means."],["ENABLEMENT STATES","**enabled** - Will start at boot\n\n**disabled** - Won't start at boot\n\n**static** - No install section, started as dependency only\n\n**masked** - Completely blocked from starting\n\n**indirect** - Enabled through another unit\n\n**generated** - Generated dynamically by a generator tool\n\n**alias** - Name is an alias for another unit"],["CAVEATS","\"Enabled\" means configured to start, not currently running. Use `is-active` to check runtime state. Static units have no [Install] section and can only be started as dependencies."],["SEE ALSO","[systemctl](/man/systemctl)(1), [systemctl-is-active](/man/systemctl-is-active)(1), [systemctl-enable](/man/systemctl-enable)(1), [systemctl-disable](/man/systemctl-disable)(1), [systemctl-status](/man/systemctl-status)(1)"]],"e":["systemctl","systemctl-is-active","systemctl-enable","systemctl-disable","systemctl-status"],"x":"systemctl is-enabled [unit]"},"systemctl-is-system-running":{"n":"systemctl-is-system-running","t":"Check overall system operational state","s":[["TAGLINE","Check overall system operational state"],["TLDR","Check **system state**\n\n```systemctl is-system-running```\n\n**Quiet** mode (exit code only)\n\n```systemctl is-system-running -q```\n\n**Wait** for boot completion\n\n```systemctl is-system-running --wait```"],["SYNOPSIS","**systemctl is-system-running** [_OPTIONS_]"],["PARAMETERS","**-q, --quiet**\n> Suppress output, return only exit code\n\n**--wait**\n> Wait until boot process is completed before returning"],["DESCRIPTION","**systemctl is-system-running** checks the overall operational state of the system. It reports whether the system has finished booting and whether all units are functioning properly.\n\nPossible states: initializing, starting, running, degraded, maintenance, stopping, offline, unknown. \"Running\" indicates successful boot with all units healthy."],["SYSTEM STATES","**initializing** — Early boot, before basic.target reached.\n\n**starting** — Late boot, before all services have finished.\n\n**running** — System fully operational, all units healthy.\n\n**degraded** — System is running but at least one unit has failed.\n\n**maintenance** — Rescue/emergency mode.\n\n**stopping** — Shutdown in progress.\n\n**offline** — System not booted under systemd (e.g., chroot).\n\n**unknown** — State could not be determined."],["EXIT STATUS","Returns **0** only when the system reports `running`. Any other state returns non-zero, so `systemctl is-system-running -q` is a convenient health-check primitive in scripts."],["CAVEATS","The `--wait` option is useful in scripts that need to wait for complete system startup before proceeding. \"Degraded\" state indicates at least one unit failed."],["HISTORY","The **is-system-running** subcommand provides a high-level health check for the entire system, enabling automated monitoring and boot-completion detection."],["SEE ALSO","[systemctl-status](/man/systemctl-status)(1), [systemctl](/man/systemctl)(1)"]],"e":["systemctl-status","systemctl"],"x":"systemctl is-system-running"},"terragrunt":{"n":"terragrunt","t":"Terraform wrapper for DRY configurations","s":[["TAGLINE","Terraform wrapper for DRY configurations"],["TLDR","**Initialize modules**\n\n```terragrunt init```\n\n**Plan changes**\n\n```terragrunt plan```\n\n**Apply changes**\n\n```terragrunt apply```\n\n**Destroy infrastructure**\n\n```terragrunt destroy```\n\n**Run on all modules**\n\n```terragrunt run-all apply```\n\n**Plan all modules**\n\n```terragrunt run-all plan```\n\n**Show outputs**\n\n```terragrunt output```\n\n**Graph dependencies**\n\n```terragrunt graph-dependencies```"],["SYNOPSIS","**terragrunt** [_init_] [_plan_] [_apply_] [_destroy_] [_run-all_] [_options_]"],["PARAMETERS","**init**\n> Initialize Terraform.\n\n**plan**\n> Show execution plan.\n\n**apply**\n> Apply changes.\n\n**destroy**\n> Destroy infrastructure.\n\n**output**\n> Show outputs.\n\n**run-all** _CMD_\n> Run command on all modules.\n\n**graph-dependencies**\n> Show dependency graph.\n\n**--terragrunt-config** _FILE_\n> Config file.\n\n**--terragrunt-working-dir** _DIR_\n> Working directory.\n\n**--terragrunt-source** _SOURCE_\n> Module source override.\n\n**--terragrunt-non-interactive**\n> Non-interactive mode.\n\n**--terragrunt-parallelism** _N_\n> Parallel operations."],["DESCRIPTION","**terragrunt** is a thin wrapper for Terraform that provides extra tools for keeping configurations DRY and managing multiple modules.\n\nDRY configuration avoids repetition across environments. Common settings are defined once and inherited by modules.\n\nRemote state configuration is centralized. Backend settings don't need repetition in every module.\n\nDependencies between modules are managed automatically. run-all executes commands across multiple modules in dependency order.\n\nBefore and after hooks run custom commands around Terraform operations. This enables validation, notifications, and cleanup.\n\nLock file management handles concurrent access. Multiple team members can work safely on shared infrastructure."],["CONFIGURATION","**terragrunt.hcl**\n> Module-level configuration file defining Terraform source, inputs, dependencies, and backend settings\n\n**TERRAGRUNT_CONFIG**\n> Environment variable to override the default configuration file path\n\n**TERRAGRUNT_DOWNLOAD**\n> Environment variable to set the directory where Terragrunt downloads Terraform source code"],["CAVEATS","Adds complexity over plain Terraform. Learning curve for configuration syntax. Some Terraform features need workarounds."],["HISTORY","**Terragrunt** was created by **Gruntwork** around **2016** to address pain points in managing Terraform at scale. It's widely used for multi-environment, multi-account infrastructure management."],["INSTALL","```pacman: sudo pacman -S terragrunt```\n\n```zypper: sudo zypper install terragrunt```\n\n```brew: brew install terragrunt```\n\n```nix: nix profile install nixpkgs#terragrunt```"],["SEE ALSO","[terraform](/man/terraform)(1), [pulumi](/man/pulumi)(1), [aws](/man/aws)(1)"]],"e":["terraform","pulumi","aws"],"x":"terragrunt init"},"texcount":{"n":"texcount","t":"Count words in LaTeX documents","s":[["TAGLINE","Count words in LaTeX documents"],["TLDR","**Count words**\n\n```texcount [document.tex]```\n\n**Brief output**\n\n```texcount -brief [document.tex]```\n\n**Include subcounts**\n\n```texcount -sub [document.tex]```\n\n**HTML output**\n\n```texcount -html [document.tex]```\n\n**Count multiple files**\n\n```texcount -merge [file1.tex] [file2.tex]```\n\n**Verbose output**\n\n```texcount -v [document.tex]```"],["SYNOPSIS","**texcount** [_-brief_] [_-sub_] [_-html_] [_options_] _files_"],["PARAMETERS","**-brief**\n> Short output.\n\n**-sub**\n> Subcounts by section.\n\n**-html**\n> HTML output.\n\n**-merge**\n> Merge file counts.\n\n**-v**\n> Verbose output.\n\n**-inc**\n> Include input files."],["DESCRIPTION","**texcount** counts words in LaTeX documents by properly parsing TeX syntax rather than counting all whitespace-separated tokens. It distinguishes between text words, header words, caption words, and mathematical formulas, providing accurate counts that exclude macro names and formatting commands.\n\nThe **-sub** option breaks down counts by section, chapter, or other structural elements, which is useful for tracking progress on individual parts of a document. Multiple files can be processed together with **-merge**, and **-inc** follows \\\\input and \\\\include directives to count included files. Output formats include plain text, HTML, and verbose mode showing how each word was classified."],["CAVEATS","LaTeX specific. Custom macros may confuse. Perl required."],["HISTORY","**TeXcount** was created to provide accurate word counts for LaTeX documents by properly parsing TeX syntax."],["INSTALL","```pacman: sudo pacman -S texlive-binextra```\n\n```apk: sudo apk add texlive-binextra```"],["SEE ALSO","[wc](/man/wc)(1), [latex](/man/latex)(1), [detex](/man/detex)(1)"]],"e":["wc","latex","detex"],"x":"texcount [document.tex]"},"tic":{"n":"tic","t":"Compile terminfo terminal descriptions","s":[["TAGLINE","Compile terminfo terminal descriptions"],["TLDR","**Compile** and install terminfo\n\n```tic -xe [terminal] [path/to/terminal.info]```\n\n**Check** terminfo for errors\n\n```tic -c [path/to/terminal.info]```\n\nPrint database **locations**\n\n```tic -D```"],["SYNOPSIS","**tic** [_OPTIONS_] _FILE_"],["PARAMETERS","**-c**\n> Check terminfo file for errors without installing\n\n**-x**\n> Compile and treat unknown capabilities as user-defined\n\n**-e** _NAMES_\n> Compile only specified terminal names\n\n**-D**\n> Print database locations\n\n**-o** _DIR_\n> Write compiled files to specified directory\n\n**-v** _N_\n> Verbose output (0-10)"],["DESCRIPTION","**tic** compiles terminfo source files into the binary format used by ncurses and other terminal-handling libraries. The compiled descriptions are installed into the terminfo database where programs can look them up.\n\nTerminfo describes terminal capabilities like cursor movement, color support, and special keys."],["CAVEATS","Installing to system directories requires root privileges. Syntax errors in source files prevent compilation. Use -c to validate before installing."],["HISTORY","**tic** is part of the **ncurses** library, the standard terminal handling library for Unix-like systems. Terminfo replaced the older termcap format for describing terminal capabilities."],["INSTALL","```apt: sudo apt install ncurses-bin```\n\n```dnf: sudo dnf install ncurses```\n\n```pacman: sudo pacman -S ncurses```\n\n```apk: sudo apk add ncurses```\n\n```brew: brew install ncurses```\n\n```nix: nix profile install nixpkgs#ncurses```"],["SEE ALSO","[infocmp](/man/infocmp)(1), [toe](/man/toe)(1)"]],"e":["infocmp","toe"],"x":"tic -xe [terminal] [path/to/terminal.info]"},"timeout":{"n":"timeout","t":"Run command with time limit","s":[["TAGLINE","Run command with time limit"],["TLDR","**Run a command with a time limit** (30 seconds)\n\n```timeout 30s [command]```\n\n**Run with time limit** in minutes\n\n```timeout 5m [command]```\n\n**Send specific signal** when timeout occurs\n\n```timeout --signal=SIGKILL 10s [command]```\n\n**Kill after additional time** if command doesn't terminate\n\n```timeout --kill-after=10s 30s [command]```\n\n**Preserve exit status** of timed-out command\n\n```timeout --preserve-status 5s [command]```\n\n**Run with timeout** in foreground (for interactive commands)\n\n```timeout --foreground 30s [command]```"],["SYNOPSIS","**timeout** [_options_] _duration_ _command_ [_args_...]"],["PARAMETERS","**-k** _duration_, **--kill-after**=_duration_\n> Send SIGKILL after additional duration if command still running\n\n**-s** _signal_, **--signal**=_signal_\n> Signal to send on timeout (default: SIGTERM)\n\n**--preserve-status**\n> Return command's exit status even on timeout\n\n**--foreground**\n> Don't create new process group (for terminal interaction)\n\n**-v**, **--verbose**\n> Diagnose signal sent to stderr"],["DURATION FORMAT","**s**: Seconds (default)\n**m**: Minutes\n**h**: Hours\n**d**: Days\n\nNumbers can be floating-point: **1.5m** = 90 seconds"],["DESCRIPTION","**timeout** runs a command with a specified time limit. If the command does not complete within the duration, timeout sends a signal (SIGTERM by default) to terminate it. Part of GNU coreutils.\n\nThe default signal (SIGTERM) allows processes to clean up before exiting. For processes that ignore SIGTERM, use **--kill-after** to send SIGKILL after an additional grace period.\n\nExit status is 124 if the command times out, 137 if killed by SIGKILL, or the command's own exit status if it completes within the limit. Use **--preserve-status** to return the signal number + 128 on timeout."],["CAVEATS","Timeout creates a new process group by default, which can interfere with interactive commands or terminal I/O. Use **--foreground** for such cases.\n\nCommands that spawn child processes may leave orphaned children. Timeout only signals the direct child, not grandchildren.\n\nVery short timeouts (sub-second) may not be reliable due to scheduling overhead."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[time](/man/time)(1), [watch](/man/watch)(1), [kill](/man/kill)(1), [sleep](/man/sleep)(1)"]],"e":["time","watch","kill","sleep"],"x":"timeout 30s [command]"},"tldr-lint":{"n":"tldr-lint","t":"Validate TLDR page formatting","s":[["TAGLINE","Validate TLDR page formatting"],["TLDR","**Lint file**\n\n```tldr-lint [page.md]```\n\n**Lint directory**\n\n```tldr-lint [pages/]```\n\n**Verbose output**\n\n```tldr-lint -v [page.md]```\n\n**Format output**\n\n```tldr-lint -f [json] [page.md]```\n\n**Ignore rules**\n\n```tldr-lint -I [TLDR001] [page.md]```"],["SYNOPSIS","**tldr-lint** [_-v_] [_-f format_] [_-I rules_] _files_"],["PARAMETERS","**-v**\n> Verbose output.\n\n**-f** _FORMAT_\n> Output format.\n\n**-I** _RULES_\n> Ignore rules.\n\n**--version**\n> Show version.\n\n**--help**\n> Show help."],["DESCRIPTION","**tldr-lint** validates TLDR pages against the project's formatting guidelines, checking for structural correctness, proper markdown syntax, and style compliance. Each rule is identified by a code (e.g., TLDR001) and can be selectively ignored with the **-I** flag.\n\nThe tool can lint individual files or scan entire directories for batch validation, making it suitable for CI pipelines that enforce consistent page formatting across the tldr-pages repository. Output formats include plain text and JSON for integration with other tools."],["CAVEATS","TLDR format specific. Node.js required. For tldr contributors."],["HISTORY","**tldr-lint** was created for the **tldr-pages** project to validate page formatting consistency."],["INSTALL","```apt: sudo apt install tealdeer```\n\n```dnf: sudo dnf install tldr```\n\n```pacman: sudo pacman -S tealdeer```\n\n```zypper: sudo zypper install tealdeer```\n\n```brew: brew install tealdeer```\n\n```nix: nix profile install nixpkgs#tealdeer```"],["SEE ALSO","[tldr](/man/tldr)(1), [markdownlint](/man/markdownlint)(1)"]],"e":["tldr","markdownlint"],"x":"tldr-lint [page.md]"},"tlmgr-platform":{"n":"tlmgr-platform","t":"Manage TeX Live platform binaries","s":[["TAGLINE","Manage TeX Live platform binaries"],["TLDR","**List platforms**\n\n```tlmgr platform list```\n\n**Add platform**\n\n```tlmgr platform add [x86_64-darwin]```\n\n**Remove platform**\n\n```tlmgr platform remove [i386-linux]```\n\n**Set current platform**\n\n```tlmgr platform set [x86_64-linux]```"],["SYNOPSIS","**tlmgr platform** _action_ [_platform_...]"],["PARAMETERS","**list**\n> List available platforms.\n\n**add** _platform_\n> Add platform binaries.\n\n**remove** _platform_\n> Remove platform binaries.\n\n**set** _platform_\n> Set current platform."],["DESCRIPTION","**tlmgr platform** manages TeX Live platform binaries. Same functionality as tlmgr arch. Install support for multiple operating systems. Part of TeX Live Manager."],["SEE ALSO","[tlmgr-arch](/man/tlmgr-arch)(1), [tlmgr](/man/tlmgr)(1)"]],"e":["tlmgr-arch","tlmgr"],"x":"tlmgr platform list"},"tuir":{"n":"tuir","t":"Terminal user interface for Reddit","s":[["TAGLINE","Terminal user interface for Reddit"],["TLDR","**Launch Reddit browser**\n\n```tuir```\n\n**Open specific subreddit**\n\n```tuir -s [programming]```\n\n**Open with URL**\n\n```tuir [https://reddit.com/r/linux]```\n\n**Open comments**\n\n```tuir -l [post-url]```\n\n**Use different theme**\n\n```tuir --theme [molokai]```"],["SYNOPSIS","**tuir** [_-s subreddit_] [_--theme name_] [_options_] [_url_]"],["PARAMETERS","**-s** _SUBREDDIT_\n> Open subreddit.\n\n**-l** _URL_\n> Open link.\n\n**--theme** _NAME_\n> Color theme.\n\n**--config** _FILE_\n> Config file.\n\n**--help**\n> Show help."],["DESCRIPTION","**tuir** (Terminal UI for Reddit) provides a full-featured Reddit browsing experience in the terminal. It displays subreddits, posts, and threaded comments in an ncurses-based interface navigated with vim-style keybindings.\n\nThe tool supports core Reddit interactions including voting, commenting, subscribing to subreddits, and opening links in an external browser or media viewer. Multiple subreddits can be browsed, and posts can be sorted by hot, new, top, and controversial.\n\nAuthentication via OAuth allows access to subscriptions, saved posts, and user-specific features. Themes can be customized to match terminal color schemes."],["KEY BINDINGS","- **j/k** - Navigate\n- **l/h** - Enter/back\n- **a/z** - Vote up/down\n- **c** - Comment"],["CAVEATS","Reddit API limits. OAuth authentication. Fork of rtv."],["HISTORY","**tuir** (Terminal UI for Reddit) is a maintained fork of **rtv** (Reddit Terminal Viewer) after it was abandoned."],["INSTALL","```nix: nix profile install nixpkgs#tuir```"],["SEE ALSO","[rtv](/man/rtv)(1), [newsboat](/man/newsboat)(1)"]],"e":["rtv","newsboat"],"x":"tuir"},"tw":{"n":"tw","t":"Twigg CLI for trunk-based version control and stacked commits","s":[["TAGLINE","Twigg CLI for trunk-based version control and stacked commits"],["TLDR","**Show** repository status / commit stack\n\n```tw```\n\n**Initialize** Twigg in the current directory\n\n```tw init```\n\n**Clone** a repository\n\n```tw clone [user/repo]```\n\n**Set** your CLI API key\n\n```tw key [tw_key_...]```\n\n**Create a commit** with a title\n\n```tw commit \"[title]\"```\n\n**Push** the current commit stack to the server\n\n```tw push```\n\n**Pull** submitted commits from the server\n\n```tw pull```\n\n**Short status** (conflicts and working tree)\n\n```tw st```\n\n**Rebase** current commit onto a new parent\n\n```tw rebase [target]```\n\n**Show version**\n\n```tw version```"],["SYNOPSIS","**tw** [_command_] [_args_...]"],["PARAMETERS","**init**\n> Initialize Twigg metadata in the current directory.\n\n**clone** _user/repo_\n> Clone a repository from the Twigg server (prompts for a CLI key if needed).\n\n**key** _CLI_KEY_\n> Store the authentication key from user settings (**tw_key_...**).\n\n**commit** _title_\n> Snapshot tracked files into a new local commit with the given title.\n\n**push**\n> Push the current commit and unsubmitted parents until a commit already on the server is reached. May trigger path-based CI.\n\n**pull** [_server_commit_]\n> Pull submitted commits, or a specific pending server commit (server id syntax, e.g. **c/7**).\n\n**rebase** [_source_] _target_\n> Rebase _source_ (default: current commit **@**) onto _target_. Descendants are rebased automatically. Conflicts produce conflicted commits to resolve with amend rather than aborting mid-stack.\n\n**st**, **status**\n> Working-tree and conflict status.\n\n**amend**\n> Create a new version of the current commit after editing (including conflict resolution).\n\n**restore**, **load**, **server**, **ci-list**\n> Restore a previous commit version; load files without committing; set server repository URL; list CI configs that would run for the current commit. See official docs for full flags.\n\n**-a**\n> With the default stack view, show all commit versions / history edges (e.g. rebase lineage).\n\n**version**, **-v**\n> Print the CLI version."],["DESCRIPTION","**tw** is the command-line client for **Twigg**, an open-source version control system and software forge (not a Git wrapper) built for closed-team collaboration. The default workflow is trunk-based development with small stacked commits: each change is a short-lived commit reviewed and submitted independently, stacking on parents rather than long feature branches.\n\nThe CLI manages a local commit graph with local ids (**#N**) and versions (**vM**), server ids (**c/N**), and a current commit (**@**). Day-to-day flow is edit → **tw commit** → **tw push** for review, **tw pull** to update, and **tw rebase** when restacking. Hierarchical **OWNERS**, integrated code review, and path-filtered CI/CD are first-class on the server side.\n\nInstall prebuilt binaries from **twigg.vc** (Linux, macOS Intel/Apple Silicon, Windows) or build with Go: **cd tw && go install** from the monorepo. Hosted service at **https://twigg.vc**; optional Git mirror for interoperability. Source and server components are AGPL-3.0."],["CAVEATS","Not compatible with Git commands in the same working tree model—Twigg is a separate VCS. Authentication uses a generated CLI key, not SSH by default. Conflicted rebases create conflict-bearing commits that block some operations until **amend** resolves them. Self-hosting and local builds need the full monorepo stack (Go, and for servers: node, docker/LXD as applicable). Public repositories were not supported on the hosted forge at first release (GitHub is used as a read-only source mirror for Twigg itself)."],["HISTORY","Developed as a from-scratch VCS and forge emphasizing big-tech-style trunk-based stacked workflows. Released as open source under AGPL-3.0 with documentation at **twigg.vc/docs**. Production use reported for the project's own development and partner companies."],["SEE ALSO","[git](/man/git)(1), [jj](/man/jj)(1), [hg](/man/hg)(1)"],["RESOURCES","```[Source code](https://github.com/twigg-vc/monorepo)```\n\n```[Homepage](https://twigg.vc)```\n\n```[Documentation](https://twigg.vc/docs/v/2/)```"]],"e":["git","jj","hg"],"x":"tw"},"udevadm":{"n":"udevadm","t":"Linux udev device manager administration","s":[["TAGLINE","Linux udev device manager administration"],["TLDR","**Monitor** all device events\n\n```sudo udevadm monitor```\n\n**Print** uevents sent by the kernel\n\n```sudo udevadm monitor -k```\n\n**Print** device events after being processed by udev\n\n```sudo udevadm monitor -u```\n\n**List** attributes of a device\n\n```sudo udevadm info -a [/dev/sda]```\n\n**Reload** all udev rules\n\n```sudo udevadm control -R```\n\n**Trigger** all udev rules to run\n\n```sudo udevadm trigger```\n\n**Test** an event run by simulating device loading\n\n```sudo udevadm test [/dev/sda]```"],["SYNOPSIS","**udevadm** [_command_] [_options_]"],["PARAMETERS","**monitor**\n> Monitor kernel uevents and udev events\n\n**info**\n> Query device information\n\n**control**\n> Control the udev daemon\n\n**trigger**\n> Request device events from the kernel\n\n**settle**\n> Wait for pending udev events\n\n**test**\n> Simulate a udev event\n\n**-k, --kernel**\n> Print kernel uevents\n\n**-u, --udev**\n> Print udev events\n\n**-a, --attribute-walk**\n> Show all device attributes\n\n**-R, --reload**\n> Reload udev rules"],["DESCRIPTION","**udevadm** is the Linux udev management tool for querying and controlling the udev device manager. It can monitor device events, query device attributes, reload rules, and trigger rule processing.\n\nThe tool is essential for debugging udev rules, understanding device properties, and managing hot-plug device handling."],["CAVEATS","Requires root privileges for most operations. The **test** command simulates events without actually running them. Rule changes require **control --reload** to take effect. Part of the systemd/udev suite."],["INSTALL","```apt: sudo apt install udev```\n\n```apk: sudo apk add eudev```\n\n```zypper: sudo zypper install udev```\n\n```nix: nix profile install nixpkgs#udev```"]],"e":[],"x":"sudo udevadm monitor"},"uwsgi":{"n":"uwsgi","t":"Application server for Python web frameworks","s":[["TAGLINE","Application server for Python web frameworks"],["TLDR","**Run a WSGI application** with HTTP server\n\n```uwsgi --http :8080 --wsgi-file [app.py]```\n\n**Run with multiple processes and threads**\n\n```uwsgi --http :8080 --wsgi-file [app.py] --master --processes [4] --threads [2]```\n\n**Run with a Unix socket** for Nginx\n\n```uwsgi --socket [/tmp/uwsgi.sock] --wsgi-file [app.py] --chmod-socket=666```\n\n**Run a Django application**\n\n```uwsgi --http :8080 --module [myproject.wsgi]:application```\n\n**Run from a configuration file**\n\n```uwsgi --ini [uwsgi.ini]```\n\n**Enable stats server**\n\n```uwsgi --http :8080 --wsgi-file [app.py] --stats 127.0.0.1:9191```"],["SYNOPSIS","**uwsgi** [_options_]"],["PARAMETERS","**--http** _address_\n> Enable HTTP server on specified address:port\n\n**--socket** _address_\n> Bind to specified UNIX/TCP socket for uwsgi protocol\n\n**--wsgi-file** _file_\n> Load WSGI application from Python file\n\n**--module** _module_\n> Load WSGI application from specified module\n\n**--master**\n> Enable master process for managing workers\n\n**--processes** _n_\n> Spawn n worker processes\n\n**--threads** _n_\n> Run n threads per worker process\n\n**--chdir** _path_\n> Change to directory before loading application\n\n**--virtualenv** _path_\n> Use specified Python virtualenv\n\n**--ini** _file_\n> Load configuration from INI file\n\n**--stats** _address_\n> Enable stats server on specified address\n\n**--chmod-socket**\n> Set socket file permissions"],["DESCRIPTION","**uWSGI** is an application server that implements the WSGI specification for serving Python web applications. It provides a bridge between web servers like Nginx and Python frameworks like Django, Flask, and Pyramid.\n\nThe server supports multiple protocols: HTTP for direct serving, the uwsgi binary protocol for efficient Nginx communication, and sockets for local IPC. It handles process management, load balancing, and caching.\n\nConfiguration can be provided via command-line options or INI/YAML/JSON files. The master process mode enables graceful reloading and automatic worker respawning."],["CAVEATS","uWSGI has been in maintenance mode since **October 2022**. Consider alternatives like Gunicorn for new projects. The uwsgi protocol (lowercase) is different from the uWSGI application server. Socket permissions require careful configuration when running behind Nginx."],["HISTORY","uWSGI was developed as a full-stack application server supporting multiple languages and protocols. Despite its name coming from WSGI, it expanded to support Ruby (Rack), Perl (PSGI), and other languages. The project became widely adopted for Python deployments."],["INSTALL","```dnf: sudo dnf install uwsgi```\n\n```pacman: sudo pacman -S uwsgi```\n\n```apk: sudo apk add uwsgi```\n\n```zypper: sudo zypper install uwsgi```\n\n```brew: brew install uwsgi```\n\n```nix: nix profile install nixpkgs#uwsgi```"],["SEE ALSO","[gunicorn](/man/gunicorn)(1), [nginx](/man/nginx)(8), [python](/man/python)(1), [pip](/man/pip)(1)"]],"e":["gunicorn","nginx","python","pip"],"x":"uwsgi --http :8080 --wsgi-file [app.py]"},"w":{"n":"w","t":"Show logged-in users and activity","s":[["TAGLINE","Show logged-in users and activity"],["TLDR","**Show who is logged in** and what they're doing\n\n```w```\n\n**Show without header**\n\n```w -h```\n\n**Show for specific user**\n\n```w [username]```\n\n**Short format** (no login time, JCPU, PCPU)\n\n```w -s```\n\n**Show IP addresses** instead of hostnames\n\n```w -i```"],["SYNOPSIS","**w** [**-hisf**] [_user_]"],["PARAMETERS","**-h**, **--no-header**\n> Don't print the header line\n\n**-s**, **--short**\n> Short format; don't print login time, JCPU, or PCPU\n\n**-f**, **--from**\n> Toggle printing the FROM (remote host) field\n\n**-i**, **--ip-addr**\n> Display IP addresses instead of hostnames\n\n**-o**, **--old-style**\n> Old style output (space-separated)\n\n**-u**, **--no-current**\n> Ignore username while calculating current process times"],["OUTPUT COLUMNS","**USER**: Username\n**TTY**: Terminal name\n**FROM**: Remote host (or - for local)\n**LOGIN@**: Login time\n**IDLE**: Idle time\n**JCPU**: Time used by all processes attached to tty\n**PCPU**: Time used by current process\n**WHAT**: Current process command line"],["DESCRIPTION","**w** displays information about currently logged-in users and their processes. The first line shows current time, system uptime, number of users, and load averages (same as **uptime** output).\n\nEach subsequent line shows one logged-in user with their terminal, login time, idle time, CPU usage, and current activity. This gives a quick overview of system usage and user activity.\n\nThe \"WHAT\" column shows the current foreground process for each user, useful for seeing what people are running.\n\nIdle time shows how long since the user's last terminal activity—useful for identifying inactive sessions."],["CAVEATS","The FROM field may be blank or show \"-\" for local logins or when hostname resolution fails.\n\nCPU time columns (JCPU, PCPU) may not accurately reflect all activity, especially for background processes or when using terminal multiplexers.\n\nSome processes may not appear in WHAT if they detach from the terminal."],["INSTALL","```apt: sudo apt install procps```\n\n```dnf: sudo dnf install procps-ng```\n\n```pacman: sudo pacman -S procps-ng```\n\n```apk: sudo apk add procps-ng```\n\n```zypper: sudo zypper install procps```\n\n```brew: brew install procps```\n\n```nix: nix profile install nixpkgs#procps```"],["SEE ALSO","[who](/man/who)(1), [uptime](/man/uptime)(1), [users](/man/users)(1), [last](/man/last)(1), [finger](/man/finger)(1)"]],"e":["who","uptime","users","last","finger"],"x":"w"},"w3m":{"n":"w3m","t":"Text-based web browser and pager","s":[["TAGLINE","Text-based web browser and pager"],["TLDR","**Open a URL**\n\n```w3m [https://example.com]```\n\n**Open a local file**\n\n```w3m [file.html]```\n\n**Dump page as text**\n\n```w3m -dump [https://example.com]```\n\n**Dump with source**\n\n```w3m -dump_source [https://example.com]```\n\n**Render HTML from stdin**\n\n```cat [file.html] | w3m -T text/html```\n\n**Open in monochrome** mode\n\n```w3m -M [https://example.com]```\n\n**Use specific browser width**\n\n```w3m -cols [80] -dump [https://example.com]```\n\n**Open bookmark file**\n\n```w3m -B```"],["SYNOPSIS","**w3m** [_options_] [_URL or file_]"],["DESCRIPTION","**w3m** is a text-based web browser that runs in the terminal. It renders HTML pages with tables, frames, and images (when using appropriate terminal), making it one of the most capable terminal browsers.\n\nThe browser supports tabs, bookmarks, cookies, and form input. It can also be used as a pager (like less) for local HTML files. The -dump option makes it useful for converting HTML to plain text in scripts.\n\nw3m handles both HTTP and HTTPS, supports basic authentication, and can work with external programs for downloading and image display."],["PARAMETERS","**-dump**\n> Dump rendered page to stdout and exit.\n\n**-dump_source**\n> Dump HTML source to stdout.\n\n**-T** _type_\n> Specify content type.\n\n**-cols** _num_\n> Set display width.\n\n**-M**\n> Monochrome mode.\n\n**-B**\n> Open bookmark file.\n\n**-m**\n> Use mouse in certain terminals.\n\n**-cookie**\n> Enable cookies.\n\n**-no-cookie**\n> Disable cookies.\n\n**-pauth** _user:pass_\n> Proxy authentication.\n\n**-N**\n> Open multiple URLs in tabs.\n\n**-I** _encoding_\n> Specify document input character encoding.\n\n**-O** _encoding_\n> Specify output character encoding for dump.\n\n**-o** _option=value_\n> Set configuration option."],["NAVIGATION KEYS","**Space/PgDn**: Page down\n**b/PgUp**: Page up\n**Enter**: Follow link\n**Tab**: Next link\n**U**: Enter URL\n**B**: Back\n**H**: History\n**T**: New tab\n**q**: Quit"],["CAVEATS","No JavaScript support. Some modern web pages won't render correctly. Image support requires compatible terminal. HTTPS may need configuration depending on SSL library."],["HISTORY","**w3m** was created by **Akinori Ito** in **1995** at Tohoku University in Japan. The name originally stood for \"WWW-wo-Miru\" (meaning \"see WWW\" in Japanese). It gained features like table rendering and image support, becoming more capable than earlier text browsers. The project has been maintained by various developers over the years."],["INSTALL","```apt: sudo apt install w3m```\n\n```dnf: sudo dnf install w3m```\n\n```pacman: sudo pacman -S w3m```\n\n```apk: sudo apk add w3m```\n\n```zypper: sudo zypper install w3m```\n\n```brew: brew install w3m```\n\n```nix: nix profile install nixpkgs#w3m```"],["SEE ALSO","[lynx](/man/lynx)(1), [links](/man/links)(1), [elinks](/man/elinks)(1), [curl](/man/curl)(1)"]],"e":["lynx","links","elinks","curl"],"x":"w3m [https://example.com]"},"wpa_supplicant":{"n":"wpa_supplicant","t":"WPA/WPA2 wireless network authentication","s":[["TAGLINE","WPA/WPA2 wireless network authentication"],["TLDR","**Start with config**\n\n```wpa_supplicant -i [wlan0] -c [/etc/wpa_supplicant.conf]```\n\n**Run in foreground**\n\n```wpa_supplicant -i [wlan0] -c [config.conf] -d```\n\n**Background daemon**\n\n```wpa_supplicant -i [wlan0] -c [config.conf] -B```\n\n**Use specific driver**\n\n```wpa_supplicant -i [wlan0] -c [config.conf] -D [nl80211]```"],["SYNOPSIS","**wpa_supplicant** [_-i interface_] [_-c config_] [_-B_] [_-D driver_]"],["PARAMETERS","**-i** _IFACE_\n> Interface name.\n\n**-c** _FILE_\n> Config file.\n\n**-B**\n> Background mode.\n\n**-D** _DRIVER_\n> Driver backend.\n\n**-d**\n> Debug output.\n\n**-P** _FILE_\n> PID file."],["CONFIGURATION","**/etc/wpa_supplicant/wpa_supplicant.conf**\n> Main configuration file containing network definitions, global settings, and credentials. Generate with **wpa_passphrase**."],["DESCRIPTION","**wpa_supplicant** is the standard wireless network authentication daemon for Linux systems. It implements WPA, WPA2, and WPA3 security protocols, handling the key negotiation and authentication process required to connect to protected wireless networks.\n\nThe daemon supports both personal (PSK) and enterprise (802.1X/EAP) authentication methods, and can manage multiple configured networks with automatic selection based on signal strength and priority. Configuration is defined in a config file specifying network SSIDs, security settings, and credentials.\n\nOn most desktop Linux systems, wpa_supplicant runs as a backend service managed by NetworkManager or systemd-networkd rather than being invoked directly by users."],["CAVEATS","Requires root privileges. The configuration file contains plaintext passwords and should have restricted permissions (600). On most desktop systems, wpa_supplicant is managed by NetworkManager or systemd-networkd rather than run directly. Running multiple instances on the same interface will cause conflicts."],["HISTORY","**wpa_supplicant** was created by **Jouni Malinen** for wireless network authentication on Linux."],["INSTALL","```apt: sudo apt install wpasupplicant```\n\n```dnf: sudo dnf install wpa_supplicant```\n\n```pacman: sudo pacman -S wpa_supplicant```\n\n```apk: sudo apk add wpa_supplicant```\n\n```zypper: sudo zypper install wpa_supplicant```\n\n```nix: nix profile install nixpkgs#wpa_supplicant```"],["SEE ALSO","[wpa_cli](/man/wpa_cli)(1), [iwconfig](/man/iwconfig)(8), [NetworkManager](/man/NetworkManager)(8)"]],"e":["wpa_cli","iwconfig","NetworkManager"],"x":"wpa_supplicant -i [wlan0] -c [/etc/wpa_supplicant.conf]"},"xkcdpass":{"n":"xkcdpass","t":"Generate memorable XKCD-style passphrases","s":[["TAGLINE","Generate memorable XKCD-style passphrases"],["TLDR","**Generate passphrase**\n\n```xkcdpass```\n\n**Generate with specific word count**\n\n```xkcdpass -n [6]```\n\n**Generate with delimiter**\n\n```xkcdpass -d \"-\"```\n\n**Generate multiple passphrases**\n\n```xkcdpass -c [5]```\n\n**Use custom wordlist**\n\n```xkcdpass -w [/path/to/wordlist]```\n\n**Filter by word length**\n\n```xkcdpass --min [4] --max [8]```\n\n**Generate with numbers**\n\n```xkcdpass --valid-chars \"[a-z0-9]\"```"],["SYNOPSIS","**xkcdpass** [_-n count_] [_-d delimiter_] [_-w wordlist_] [_options_]"],["PARAMETERS","**-n** _COUNT_\n> Number of words.\n\n**-d** _DELIM_\n> Delimiter between words.\n\n**-w** _FILE_\n> Wordlist file.\n\n**-c** _N_\n> Number of passphrases.\n\n**--min** _N_\n> Minimum word length.\n\n**--max** _N_\n> Maximum word length.\n\n**--valid-chars** _CHARS_\n> Valid characters regex.\n\n**-v**, **--verbose**\n> Show entropy info.\n\n**-a** _ACROSTIC_\n> Acrostic pattern.\n\n**-i**, **--interactive**\n> Interactive mode.\n\n**--help**\n> Show help."],["DESCRIPTION","**xkcdpass** generates memorable passphrases using random words. It's inspired by XKCD comic 936 about password strength.\n\nRandom words from a dictionary create passphrases. Four or more random words provide strong security while remaining memorable.\n\nThe default wordlist contains common English words. Custom wordlists can be used for different languages or specialized vocabularies.\n\nEntropy information shows passphrase strength. Longer passphrases and larger wordlists increase entropy.\n\nAcrostic mode generates passphrases where first letters spell a word. This aids memorization while maintaining randomness.\n\nWord length filters exclude very short or long words. This can improve pronounceability and typing speed."],["CAVEATS","Security depends on wordlist size. Short wordlists reduce entropy. Random selection must be truly random. Not for high-security needs without verification."],["HISTORY","**xkcdpass** was created inspired by **XKCD comic 936** by **Randall Munroe**. The comic demonstrated that random word passphrases are more secure and memorable than complex but short passwords."],["INSTALL","```pacman: sudo pacman -S xkcdpass```\n\n```apk: sudo apk add xkcdpass```\n\n```nix: nix profile install nixpkgs#xkcdpass```"],["SEE ALSO","[pwgen](/man/pwgen)(1), [makepasswd](/man/makepasswd)(1), [apg](/man/apg)(1), [openssl](/man/openssl)(1)"]],"e":["pwgen","makepasswd","apg","openssl"],"x":"xkcdpass"}});
