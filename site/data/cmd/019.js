window.LCL_BUCKET(19,{"adig":{"n":"adig","t":"DNS lookup utility using the c-ares library","s":[["TAGLINE","DNS lookup utility using the c-ares library"],["TLDR","Display **A record** (default) from DNS for a hostname\n\n```adig [example.com]```\n\nDisplay extra **debugging** output\n\n```adig -d [example.com]```\n\nConnect to a **specific DNS server**\n\n```adig -s [1.2.3.4] [example.com]```\n\nUse a specific **TCP port** to connect to a DNS server\n\n```adig -T [port] [example.com]```\n\nUse a specific **UDP port** to connect to a DNS server\n\n```adig -U [port] [example.com]```"],["SYNOPSIS","**adig** [_-d_] [_-s server_] [_-T port_] [_-U port_] [_-t type_] _hostname_\n\nModern c-ares (1.34 and later):\n\n**adig** [_@server_] [_-c class_] [_-p port_] [_-q name_] [_-t type_] [_-x addr_] _name_ [_type_] [_class_]"],["DESCRIPTION","**adig** prints information received from Domain Name System (DNS) servers. It performs DNS lookups and displays the results, similar to **dig** but built on the c-ares asynchronous resolver library.\n\nThe tool supports querying specific record types such as A, AAAA, MX, NS, CNAME, SOA, and others. It can direct queries to a particular DNS server and use custom port numbers for the connection. Debug output is available for troubleshooting resolution issues.\n\nAs of c-ares 1.34, the command-line interface was reworked to closely mimic **dig** from BIND, so the server is given as **@server** and the legacy **-d**, **-T**, and **-U** flags shown below apply to older releases."],["PARAMETERS","**-d**\n> Display extra debugging output (legacy interface)\n\n**-s server**\n> Connect to a specific DNS server IP address. In c-ares 1.34 and later prefer the **@server** syntax\n\n**-T port**\n> Use a specific TCP port to connect to the DNS server (legacy interface)\n\n**-U port**\n> Use a specific UDP port to connect to the DNS server (legacy interface)\n\n**-p port**\n> Send the query to a port other than 53 (c-ares 1.34 and later)\n\n**-t type**\n> Query for a specific record type (A, AAAA, MX, NS, etc.)\n\n**-c class**\n> Set the query class; defaults to IN, may also be HS or CH (c-ares 1.34 and later)\n\n**-x addr**\n> Perform a simplified reverse lookup: set the type to PTR and build the in-addr.arpa query string (c-ares 1.34 and later)"],["CAVEATS","Part of the c-ares library tools. May have different features compared to the more common **dig** command from BIND. The flag set differs between releases: c-ares 1.34 reworked the interface to match **dig**, replacing the older **-d**, **-T**, and **-U** options."],["HISTORY","Part of the **c-ares** asynchronous DNS resolver library, originally forked from the ares library and actively maintained. In release 1.34.0 the **adig** command-line arguments were reworked to mimic **dig** from BIND, and the tool gained support for reading default arguments from an **~/.adigrc** file."],["INSTALL","```apk: sudo apk add c-ares-utils```\n\n```zypper: sudo zypper install c-ares-utils```"],["SEE ALSO","[ahost](/man/ahost)(1), [acountry](/man/acountry)(1), [dig](/man/dig)(1), [host](/man/host)(1), [nslookup](/man/nslookup)(1)"],["RESOURCES","```[Source code](https://github.com/c-ares/c-ares)```\n\n```[Homepage](https://c-ares.org/)```"]],"e":["ahost","acountry","dig","host","nslookup"],"x":"adig [example.com]"},"agent-vault":{"n":"agent-vault","t":"HTTP credential proxy and vault for AI agents","s":[["TAGLINE","HTTP credential proxy and vault for AI agents"],["TLDR","**Start the vault server** and credential proxy (HTTP API + TLS proxy)\n\n```agent-vault server -d```\n\n**Run an AI agent command** with transparent credential brokering\n\n```agent-vault vault run -- [claude]```\n\n**Run any subprocess** through the credential proxy\n\n```agent-vault vault run -- [curl https://api.example.com]```\n\n**Stop the background** server daemon\n\n```agent-vault server stop```\n\n**Check the version**\n\n```agent-vault --version```"],["SYNOPSIS","**agent-vault** _command_ [_subcommand_] [_options_] [**--** _agent_command_]"],["PARAMETERS","**server**\n> Start the HTTP API (default port **14321**) and the TLS-encrypted transparent HTTPS proxy (default port **14322**).\n\n**server** **-d**, **--daemon**\n> Run the server in the background as a daemon.\n\n**server stop**\n> Stop a running server daemon.\n\n**vault run** **--** _command_\n> Execute _command_ with credential brokering enabled. Automatically creates a scoped session, injects **HTTPS_PROXY** and CA-trust environment variables, and transparently proxies outgoing HTTPS requests.\n\n**--config** _FILE_\n> Use an alternative configuration file.\n\n**--port** _PORT_\n> Override the HTTP API port (default **14321**).\n\n**--proxy-port** _PORT_\n> Override the HTTPS proxy port (default **14322**).\n\n**--version**\n> Print the Agent Vault version and exit.\n\n**--help**\n> Show help for Agent Vault or for a specific subcommand."],["DESCRIPTION","**agent-vault** is an HTTP credential proxy and vault designed to keep secrets away from AI agents while still letting them call authenticated APIs. Instead of handing raw API keys or tokens to the agent process, Agent Vault runs the agent as a child process with **HTTPS_PROXY** pointed at a local TLS-terminating proxy. The proxy injects the correct credentials into outbound HTTPS requests for allowed destinations, so the agent never sees the secret itself.\n\nThe server exposes an HTTP API on port **14321** (where credentials, policies, and sessions are managed via a web UI) and a transparent HTTPS proxy on port **14322**. The **agent-vault vault run -- <command>** wrapper spawns a short-lived scoped session, sets **HTTPS_PROXY** and the proxy's CA certificate in the child's environment, and tears the session down when the command exits.\n\nThis reduces the blast radius of a compromised or misbehaving agent: even if the agent is tricked into exfiltration, it cannot leak credentials it never held."],["CONFIGURATION","The web UI at **http://localhost:14321** is used to register credentials, define which hosts an agent session may reach, and inspect session activity. The server persists state under a data directory (e.g. the Docker volume **agent-vault-data** at **/data**)."],["CAVEATS","Requires the agent to honor the **HTTPS_PROXY** environment variable and to trust the proxy's CA certificate; processes that bypass the proxy (e.g. via direct TCP or pinned certificates) will not benefit from credential brokering. The proxy terminates TLS locally, so traffic between the agent and the proxy is re-encrypted by Agent Vault rather than end-to-end to the remote host."],["HISTORY","**agent-vault** is developed by **Infisical** as part of their focus on secrets management for AI workloads, addressing the risk of giving autonomous agents long-lived credentials."],["SEE ALSO","[vault](/man/vault)(1), [mitmproxy](/man/mitmproxy)(1), [curl](/man/curl)(1)"]],"e":["vault","mitmproxy","curl"],"x":"agent-vault server -d"},"androguard":{"n":"androguard","t":"Reverse engineer and analyze Android applications","s":[["TAGLINE","Reverse engineer and analyze Android applications"],["TLDR","**Analyze** an APK file\n\n```androguard analyze [app.apk]```\n\n**Decompile** to Java source\n\n```androguard decompile -o [output_dir] [app.apk]```\n\nShow **manifest permissions** in an APK\n\n```androguard axml [app.apk] | grep permission```\n\n**Disassemble** DEX to smali\n\n```androguard disassemble [app.apk]```\n\nCheck for **signing** issues\n\n```androguard sign [app.apk]```"],["SYNOPSIS","**androguard** _command_ [_options_] _apk_file_"],["DESCRIPTION","**androguard** is a Python tool for reverse engineering, malware analysis, and security assessment of Android applications. It can analyze APK files, decompile DEX code, parse Android manifests, and extract permissions and components.\n\nThe tool provides both command-line utilities and a Python API for programmatic analysis, making it useful for both manual review and automated security scanning."],["PARAMETERS","**analyze**\n> Open an IPython shell to start reverse engineering interactively\n\n**decompile**\n> Decompile an APK to Java source and create control flow graphs\n\n**disassemble**\n> Disassemble Dalvik (DEX) bytecode\n\n**axml**\n> Parse and display the AndroidManifest.xml or other AXML files\n\n**arsc**\n> Decode resources.arsc\n\n**apkid**\n> Print the package name, version code, and version name of an APK\n\n**sign**\n> Print the fingerprints of all certificates inside an APK\n\n**cg**\n> Generate a call graph and export it to a graph format\n\n**-o** _dir_\n> Output directory (required for decompile)\n\n**-i** _file_\n> Input APK (legacy; the APK is usually given as a positional argument)\n\n**-f** _format_\n> Control flow graph format (png, jpg, raw)\n\n**-d** _name_\n> Decompiler to use (default: DAD)\n\n**-l** _regex_\n> Limit processing to methods matching a regex"],["CAVEATS","Decompilation is not always perfect; obfuscated code may be difficult to read. Some native code cannot be analyzed. Analysis of large APKs may consume significant memory."],["HISTORY","**androguard** was developed by Anthony Desnos starting around **2012**. It became one of the primary open-source tools for Android application security analysis."],["INSTALL","```aur: yay -S androguard```\n\n```nix: nix profile install nixpkgs#androguard```"],["SEE ALSO","[apktool](/man/apktool)(1), [jadx](/man/jadx)(1), [dex2jar](/man/dex2jar)(1)"],["RESOURCES","```[Source code](https://github.com/androguard/androguard)```\n\n```[Documentation](https://androguard.readthedocs.io)```"]],"e":["apktool","jadx","dex2jar"],"x":"androguard analyze [app.apk]"},"angryoxide":{"n":"angryoxide","t":"802.11 WiFi attack and penetration testing tool","s":[["TAGLINE","802.11 WiFi attack and penetration testing tool"],["TLDR","**Start scanning** on a wireless interface\n\n```sudo angryoxide -i [wlan0]```\n\n**Scan specific channels**\n\n```sudo angryoxide -i [wlan0] -c [1,6,11]```\n\n**Target a specific network**\n\n```sudo angryoxide -i [wlan0] -t [AA:BB:CC:DD:EE:FF]```\n\n**Run in passive mode** without transmitting attack frames\n\n```sudo angryoxide -i [wlan0] --notransmit```\n\n**Scan an entire band** with auto-hunt\n\n```sudo angryoxide -i [wlan0] -b [5] --autohunt```\n\n**Run headless** with custom output\n\n```sudo angryoxide -i [wlan0] --headless -o [output_prefix]```"],["SYNOPSIS","**sudo angryoxide** **-i** _interface_ [_options_]"],["PARAMETERS","**-i**, **--interface** _interface_\n> Wireless interface to use (required)\n\n**-c**, **--channel** _channels_\n> Specific channel(s) to scan (default: 1,6,11)\n\n**-b**, **--band** _band_\n> Scan an entire band (2, 5, 6, or 60)\n\n**-t** _target_\n> Target specific MAC or SSID (repeatable)\n\n**-w** _target_\n> Whitelist a MAC or SSID from attacks (repeatable)\n\n**--targetlist** _file_\n> Load targets from a file\n\n**--whitelist** _file_\n> Load whitelist from a file\n\n**-o**, **--output** _prefix_\n> Custom output filename prefix\n\n**-r**, **--rate** _level_\n> Attack aggressiveness: 1 (low), 2 (default), 3 (high)\n\n**--notransmit**\n> Passive collection only, no attack frames sent\n\n**--autohunt**\n> Auto-discover target channels, then focus on them\n\n**--headless**\n> Run without the terminal UI\n\n**--autoexit**\n> Exit when all targets have valid hashlines\n\n**--combine**\n> Merge all .hc22000 files into a single output\n\n**--gpsd** _ip:port_\n> Connect to GPS daemon for wardriving\n\n**-h**, **--help**\n> Display help"],["DESCRIPTION","**angryoxide** is an 802.11 WiFi attack tool written in Rust that autonomously collects WPA/WPA2/WPA3 hash material (EAPOL handshakes and PMKIDs) for offline cracking with Hashcat. It features a real-time TUI built with Ratatui showing discovered networks, captured handshakes, and attack status.\n\nThe attack engine performs PMKID collection, hidden SSID retrieval, anonymous reassociation (MFP bypass), Channel Switch Announcement attacks, RSN downgrade, and WiFi 6e disassociation. Output files include Hashcat-compatible .hc22000 hashlines, .pcapng packet captures, and Kismet-compatible .kismetdb databases."],["CAVEATS","Requires root/sudo privileges and a wireless adapter supporting monitor mode and packet injection. Intended strictly for authorized security testing and research. Rate-controlled transmission reduces but does not eliminate detection by wireless IDS."],["HISTORY","**angryoxide** was created by **Ragnt** (rage), inspired by hcxdumptool. Written in Rust and licensed under GPL-3.0, it is under heavy active development."],["INSTALL","```aur: yay -S angryoxide```\n\n```nix: nix profile install nixpkgs#angryoxide```"],["SEE ALSO","[aircrack-ng](/man/aircrack-ng)(1), [airodump-ng](/man/airodump-ng)(1), [hashcat](/man/hashcat)(1), [tcpdump](/man/tcpdump)(1), [iwconfig](/man/iwconfig)(8)"]],"e":["aircrack-ng","airodump-ng","hashcat","tcpdump","iwconfig"],"x":"sudo angryoxide -i [wlan0]"},"appclipcodegen":{"n":"appclipcodegen","t":"Generate and validate Apple App Clip Codes","s":[["TAGLINE","Generate and validate Apple App Clip Codes"],["TLDR","**Generate** an App Clip Code as SVG\n\n```AppClipCodeGenerator generate --url [https://example.com] --type [cam] --foreground [FFFFFF] --background [000000] --output [output.svg]```\n\n**Generate** an NFC-enabled App Clip Code\n\n```AppClipCodeGenerator generate --url [https://example.com] --type [nfc] --foreground [FFFFFF] --background [000000] --output [output.svg]```\n\n**Validate colors** and get suggestions\n\n```AppClipCodeGenerator suggest --foreground [FFFFFF] --background [000000]```"],["SYNOPSIS","**AppClipCodeGenerator** \\<command\\> [_options_]"],["PARAMETERS","**generate**\n> Generate an App Clip Code SVG file\n\n**suggest**\n> Validate color combinations and get suggestions\n\n**--url** _URL_\n> The URL that opens when the App Clip Code is scanned\n\n**--type** _cam|nfc_\n> Code type: **cam** for camera-only scanning or **nfc** for NFC-enabled codes\n\n**--foreground** _RRGGBB_\n> Foreground color as a hex value\n\n**--background** _RRGGBB_\n> Background color as a hex value\n\n**--output** _path_\n> Output path for the generated SVG file"],["DESCRIPTION","**AppClipCodeGenerator** is Apple's official command-line tool for creating and validating App Clip Codes. App Clip Codes are visually distinctive, scannable codes that launch App Clip experiences on iOS devices — lightweight app experiences that can be discovered without installing the full app.\n\nThe tool generates App Clip Codes as SVG files with two code types: **cam** codes that are scannable by the device camera, and **nfc** codes that additionally support Near Field Communication for tap-to-open functionality. The **suggest** command validates that foreground and background color combinations meet Apple's contrast and readability requirements.\n\nApp Clip Codes encode a URL that maps to a registered App Clip experience through Apple's App Store Connect configuration."],["CAVEATS","Available only on **macOS** as part of Apple's developer tools. The tool must be downloaded separately from Apple's App Clip resources page. Generated codes require a registered App Clip experience configured in **App Store Connect** to function. Color combinations must meet Apple's contrast requirements — use the **suggest** command to validate colors before generating."],["HISTORY","AppClipCodeGenerator was released by **Apple** alongside **App Clips** in **iOS 14** (2020). App Clips were introduced at **WWDC 2020** as a way to provide focused app experiences discoverable through Safari, Maps, NFC tags, and QR codes. The code generator tool enables developers to create the proprietary App Clip Code format that is visually distinct from standard QR codes."],["SEE ALSO","[xcodebuild](/man/xcodebuild)(1), [xcrun](/man/xcrun)(1)"]],"e":["xcodebuild","xcrun"],"x":"AppClipCodeGenerator generate --url [https://example.com] --type [cam] --foreground [FFFFFF] --background [000000] --output [output.svg]"},"arc":{"n":"arc","t":"Create and extract ARC format archives","s":[["TAGLINE","Create and extract ARC format archives"],["TLDR","**Create** archive\n\n```arc a [archive.arc] [files]```\n\n**Extract** archive\n\n```arc x [archive.arc]```\n\n**List** archive contents\n\n```arc l [archive.arc]```\n\n**Test** archive integrity\n\n```arc t [archive.arc]```"],["SYNOPSIS","**arc** _command_ [_archive_] [_files_]"],["DESCRIPTION","**arc** creates and manipulates archives in the ARC format, which was widely popular in the MS-DOS era during the 1980s. It supports creating new archives, extracting files, listing contents, and testing archive integrity.\n\nThe tool handles various compression methods used in historical ARC files, including Squeeze, Crunch, and LZW variants. It is primarily used today for accessing legacy archive files rather than creating new ones, as modern formats like zip and tar.gz offer significantly better compression and features."],["PARAMETERS","**a**\n> Add files to archive\n\n**x**\n> Extract files\n\n**e**\n> Extract to current directory\n\n**l**\n> List contents\n\n**t**\n> Test archive\n\n**d**\n> Delete files from archive\n\n**p**\n> Print file contents\n\n**v**\n> View with details"],["CAVEATS","The ARC format is obsolete; use modern formats like zip or tar.gz for new archives. Limited compression compared to modern algorithms. Mainly for legacy compatibility."],["HISTORY","The ARC format was created by System Enhancement Associates in **1985** and was one of the first popular file compression formats for personal computers."],["INSTALL","```apt: sudo apt install arc```\n\n```dnf: sudo dnf install arc```\n\n```aur: yay -S arc```\n\n```zypper: sudo zypper install arc```"],["SEE ALSO","[unzip](/man/unzip)(1), [tar](/man/tar)(1), [gzip](/man/gzip)(1)"],["RESOURCES","```[Source code](https://github.com/hyc/arc)```"]],"e":["unzip","tar","gzip"],"x":"arc a [archive.arc] [files]"},"arduino-builder":{"n":"arduino-builder","t":"Command-line compiler for Arduino sketches","s":[["TAGLINE","Command-line compiler for Arduino sketches"],["TLDR","**Compile** a sketch\n\n```arduino-builder -hardware [/usr/share/arduino/hardware] -tools [/usr/share/arduino/tools-builder] -libraries [/usr/share/arduino/libraries] -fqbn [arduino:avr:uno] [sketch.ino]```\n\nCompile with **verbose** output\n\n```arduino-builder -verbose -fqbn [arduino:avr:uno] [sketch.ino]```"],["SYNOPSIS","**arduino-builder** [_-hardware path_] [_-tools path_] [_-libraries path_] [_-fqbn board_] [_options_] _sketch_"],["DESCRIPTION","**arduino-builder** is the command-line tool that compiles Arduino sketches. It was the build system used by Arduino IDE and can be invoked separately for automated builds.\n\nThe tool handles preprocessing, compilation, and linking of Arduino sketches with required libraries and core files."],["PARAMETERS","**-hardware** _path_\n> Hardware definitions directory\n\n**-tools** _path_\n> Tools (compilers, uploaders) directory\n\n**-libraries** _path_\n> Libraries directory\n\n**-fqbn** _board_\n> Fully qualified board name\n\n**-build-path** _dir_\n> Build output directory\n\n**-verbose**\n> Verbose output\n\n**-warnings** _level_\n> Warning level (none, default, more, all)\n\n**-prefs** _key=value_\n> Build preferences"],["CAVEATS","Deprecated in favor of arduino-cli. Requires specifying multiple paths. Board packages must be installed separately."],["HISTORY","**arduino-builder** was developed as a standalone build tool extracted from the Arduino IDE. It has been superseded by **arduino-cli** for most use cases."],["INSTALL","```apt: sudo apt install arduino-builder```\n\n```aur: yay -S arduino-builder```"],["SEE ALSO","[arduino](/man/arduino)(1), [arduino-cli](/man/arduino-cli)(1), [avr-gcc](/man/avr-gcc)(1)"],["RESOURCES","```[Source code](https://github.com/arduino/arduino-builder)```\n\n```[Homepage](https://www.arduino.cc)```"]],"e":["arduino","arduino-cli","avr-gcc"],"x":"arduino-builder -hardware [/usr/share/arduino/hardware] -tools [/usr/share/arduino/tools-builder] -libraries [/usr/share/arduino/libraries] -fqbn [arduino:avr:uno] [sketch.ino]"},"aspell-autobuildhash":{"n":"aspell-autobuildhash","t":"Rebuild aspell dictionary hash files","s":[["TAGLINE","Rebuild aspell dictionary hash files"],["TLDR","**Rebuild** all aspell hash files\n\n```sudo aspell-autobuildhash```\n\n**Force** a rebuild of every hash file\n\n```sudo aspell-autobuildhash --force```\n\n**Preview** what would be done without changing anything\n\n```sudo aspell-autobuildhash --dry-run```"],["SYNOPSIS","**aspell-autobuildhash** [_--force_]"],["DESCRIPTION","**aspell-autobuildhash** manages the autobuilding of aspell hash files for dictionary packages. It is part of the **dictionaries-common** package and is normally invoked automatically by dpkg triggers after a dictionary is installed or upgraded.\n\nFor each aspell dictionary configured to use it, the script decides whether the hash file must be rebuilt based on the aspell compatibility level, then regenerates the hash files used for fast spell checking."],["PARAMETERS","**--force**\n> Rebuild the hash file for all dicts providing a compat file, regardless of the compatibility levels found.\n\n**--dry-run**\n> Show what would be done, but make no actual changes.\n\n**--debug**\n> Show extra info about the script's internal work; also enables aspell affix validation.\n\n**--triggered**\n> Tell the script that it is running in the dpkg triggers stage."],["CAVEATS","Requires root privileges to write to system dictionary directories. Automatically run by package managers after dictionary installation."],["HISTORY","**aspell-autobuildhash** is provided by the Debian **dictionaries-common** package, written by Agustin Martin, to automatically regenerate aspell hash files after dictionary updates."],["INSTALL","```apt: sudo apt install aspell```\n\n```dnf: sudo dnf install aspell```\n\n```pacman: sudo pacman -S aspell```\n\n```apk: sudo apk add aspell```\n\n```zypper: sudo zypper install aspell```\n\n```brew: brew install aspell```\n\n```nix: nix profile install nixpkgs#aspell```"],["SEE ALSO","[aspell](/man/aspell)(1), [aspell-import](/man/aspell-import)(1)"],["RESOURCES","```[Documentation](https://manpages.debian.org/testing/dictionaries-common/aspell-autobuildhash.8.en.html)```"]],"e":["aspell","aspell-import"],"x":"sudo aspell-autobuildhash"},"aws-kinesis":{"n":"aws-kinesis","t":"Manage real-time data streaming with Kinesis Data Streams.","s":[["TAGLINE","Manage real-time data streaming with Kinesis Data Streams."],["TLDR","**Create a new data stream**\n\n```aws kinesis create-stream --stream-name [my-stream] --shard-count 1```\n\n**List all streams** in the account\n\n```aws kinesis list-streams```\n\n**Put a record** into a stream\n\n```aws kinesis put-record --stream-name [my-stream] --partition-key [key1] --data \"[message data]\"```\n\n**Get a shard iterator** for reading\n\n```aws kinesis get-shard-iterator --stream-name [my-stream] --shard-id [shardId-000000000000] --shard-iterator-type TRIM_HORIZON```\n\n**Get records** from a shard\n\n```aws kinesis get-records --shard-iterator [shard-iterator-value]```\n\n**Describe stream details**\n\n```aws kinesis describe-stream --stream-name [my-stream]```\n\n**Increase stream retention** to 7 days\n\n```aws kinesis increase-stream-retention-period --stream-name [my-stream] --retention-period-hours 168```\n\n**Delete a stream**\n\n```aws kinesis delete-stream --stream-name [my-stream]```"],["SYNOPSIS","**aws kinesis** _command_ [_options_]"],["DESCRIPTION","**aws kinesis** is the AWS CLI interface for Amazon Kinesis Data Streams, a serverless service for real-time data streaming. Kinesis ingests and processes large amounts of data in real-time from sources like website clickstreams, IoT telemetry, and application logs.\n\nData streams consist of shards that provide throughput capacity. Each shard supports 1 MB/sec write and 2 MB/sec read. Kinesis retains data for 24 hours by default, extendable to 365 days for replay scenarios."],["COMMANDS","**create-stream**\n> Create a new data stream\n\n**delete-stream**\n> Delete a stream\n\n**describe-stream**\n> Get stream configuration and status\n\n**list-streams**\n> List all streams\n\n**put-record**\n> Write a single record\n\n**put-records**\n> Write multiple records in a batch\n\n**get-shard-iterator**\n> Get iterator for reading from a shard\n\n**get-records**\n> Retrieve records using an iterator\n\n**update-shard-count**\n> Scale stream capacity\n\n**start-stream-encryption**\n> Enable server-side encryption\n\n**register-stream-consumer**\n> Register an enhanced fan-out consumer\n\n**merge-shards**\n> Merge two adjacent shards\n\n**split-shard**\n> Split a shard into two\n\n**list-tags-for-stream**\n> List tags for a stream"],["CAVEATS","Shard iterators expire after 5 minutes. Each shard has throughput limits; exceeding them causes throttling. Record size is limited to 1 MB. GetRecords returns up to 10 MB or 10,000 records per call. Enhanced fan-out consumers have additional costs but dedicated throughput."],["HISTORY","Amazon Kinesis launched in **November 2013** as a real-time data streaming service. Enhanced fan-out was added in **2018** for dedicated consumer throughput. On-demand capacity mode launched in **2021**, eliminating the need to manage shard counts manually."],["INSTALL","```dnf: sudo dnf install aws```\n\n```pacman: sudo pacman -S aws-cli```\n\n```apk: sudo apk add aws-cli```\n\n```zypper: sudo zypper install aws-cli```"],["SEE ALSO","[aws](/man/aws)(1), [aws-firehose](/man/aws-firehose)(1), [aws-kafka](/man/aws-kafka)(1)"]],"e":["aws","aws-firehose","aws-kafka"],"x":"aws kinesis create-stream --stream-name [my-stream] --shard-count 1"},"az-pipelines":{"n":"az-pipelines","t":"Manage Azure DevOps CI/CD pipelines","s":[["TAGLINE","Manage Azure DevOps CI/CD pipelines"],["TLDR","**List all pipelines** in a project\n\n```az pipelines list --project [MyProject]```\n\n**Show details** of a pipeline\n\n```az pipelines show --name [MyPipeline] --project [MyProject]```\n\n**Create a new YAML pipeline**\n\n```az pipelines create --name [MyPipeline] --repository [MyRepo] --branch [main] --yml-path [azure-pipelines.yml]```\n\n**Run a pipeline** (queue a run)\n\n```az pipelines run --name [MyPipeline] --project [MyProject]```\n\n**Run a pipeline** with parameters and variables\n\n```az pipelines run --name [MyPipeline] --parameters [env=prod] --variables [tag=v1]```\n\n**Delete a pipeline**\n\n```az pipelines delete --id [pipeline_id] --project [MyProject]```\n\n**List pipeline runs**\n\n```az pipelines runs list --project [MyProject]```\n\n**Show details** of a pipeline run\n\n```az pipelines runs show --id [run_id] --project [MyProject]```"],["SYNOPSIS","**az** **pipelines** _subcommand_ [_options_]"],["SUBCOMMANDS","**list**\n> List pipelines.\n\n**show**\n> Show pipeline details.\n\n**create**\n> Create a pipeline.\n\n**delete**\n> Delete a pipeline.\n\n**run**\n> Queue (run) a pipeline.\n\n**update**\n> Update a pipeline.\n\n**runs**\n> Manage pipeline runs (list, show, artifact, tag).\n\n**build**\n> Manage builds and build definitions.\n\n**release**\n> Manage releases and release definitions.\n\n**variable**\n> Manage pipeline variables.\n\n**variable-group**\n> Manage variable groups.\n\n**folder**\n> Manage folders for organizing pipelines.\n\n**agent**\n> Manage agents.\n\n**pool**\n> Manage agent pools.\n\n**queue**\n> Manage agent queues."],["PARAMETERS","**--org**, **--organization**\n> Azure DevOps organization URL, for example `https://dev.azure.com/MyOrg/`. Required unless set as a default with **az devops configure -d organization=URL** or detected from local git config.\n\n**--project**, **-p**\n> Name or ID of the project. Required unless set as a default with **az devops configure -d project=NAME**.\n\n**--detect**\n> Automatically detect the organization from local git context. Accepts **true** or **false**."],["DESCRIPTION","**az pipelines** manages Azure Pipelines, the CI/CD service in Azure DevOps. It provides commands to create, run, and manage YAML pipelines, builds, and releases, as well as agents, pools, queues, variables, and variable groups.\n\nThis command group is part of the **azure-devops** extension and works with Azure DevOps Services."],["CAVEATS","Part of the **azure-devops** extension (Azure CLI 2.30.0 or higher). The extension installs automatically the first time an **az pipelines** command runs, or install it manually with **az extension add --name azure-devops**. Set persistent defaults with **az devops configure -d organization=URL project=NAME** to avoid repeating **--org** and **--project**."],["INSTALL","```dnf: sudo dnf install azure-cli```\n\n```pacman: sudo pacman -S azure-cli```\n\n```zypper: sudo zypper install azure-cli```\n\n```brew: brew install azure-cli```\n\n```nix: nix profile install nixpkgs#azure-cli```"],["SEE ALSO","[azure-cli](/man/azure-cli)(1), [az-devops](/man/az-devops)(1), [az-repos](/man/az-repos)(1), [git](/man/git)(1)"],["RESOURCES","```[Source code](https://github.com/Azure/azure-devops-cli-extension)```\n\n```[Documentation](https://learn.microsoft.com/en-us/cli/azure/pipelines)```\n\n```[Homepage](https://learn.microsoft.com/en-us/cli/azure/)```"]],"e":["azure-cli","az-devops","az-repos","git"],"x":"az pipelines list --project [MyProject]"},"brew-services":{"n":"brew-services","t":"Manage background services for Homebrew packages","s":[["TAGLINE","Manage background services for Homebrew packages"],["TLDR","**List all services**\n\n```brew services list```\n\n**Start a service**\n\n```brew services start [postgresql]```\n\n**Stop a service**\n\n```brew services stop [postgresql]```\n\n**Restart a service**\n\n```brew services restart [postgresql]```\n\n**Run service in foreground**\n\n```brew services run [postgresql]```\n\n**Stop all services**\n\n```brew services stop --all```\n\n**Clean up unused services**\n\n```brew services cleanup```"],["SYNOPSIS","**brew services** [_subcommand_] [_formula_] [_options_]"],["DESCRIPTION","**brew services** manages background services for installed Homebrew formulae. On macOS, it uses launchd with LaunchAgent plist files. On Linux, it uses systemd user units.\n\nServices configured with this command start automatically on user login."],["SUBCOMMANDS","**list**\n> List all managed services and their status\n\n**start** _formula_\n> Start service and configure for login startup\n\n**stop** _formula_\n> Stop service and disable login startup\n\n**restart** _formula_\n> Restart service\n\n**run** _formula_\n> Run service in foreground (no login startup)\n\n**cleanup**\n> Remove unused service files\n\n**info** _formula_\n> Show service information"],["PARAMETERS","**--all**\n> Apply to all services\n\n**--json**\n> Output in JSON format\n\n**--file** _path_\n> Use custom service file"],["SERVICE FILES","**macOS**\n> ~/Library/LaunchAgents/homebrew.mxcl.<formula>.plist\n\n**Linux**\n> ~/.config/systemd/user/homebrew-<formula>.service"],["CAVEATS","On Linux, requires a running user systemd instance. Common use cases include databases (postgresql, mysql), caches (redis), and web servers. Use **run** for development to avoid startup configuration."],["HISTORY","Introduced in Homebrew 0.9.9 (February 2014) for macOS launchd support. Linux/systemd support was added via Linuxbrew and merged into core Homebrew by 2020."],["SEE ALSO","[brew](/man/brew)(1), [launchctl](/man/launchctl)(1), [systemctl](/man/systemctl)(1)"],["RESOURCES","```[Source code](https://github.com/Homebrew/brew)```\n\n```[Homepage](https://brew.sh)```\n\n```[Documentation](https://docs.brew.sh)```"]],"e":["brew","launchctl","systemctl"],"x":"brew services list"},"chezmoi":{"n":"chezmoi","t":"cross-platform dotfile manager with templating","s":[["TAGLINE","cross-platform dotfile manager with templating"],["TLDR","**Initialize** dotfiles\n\n```chezmoi init```\n\n**Add** file to dotfiles\n\n```chezmoi add [~/.bashrc]```\n\n**Apply** changes\n\n```chezmoi apply```\n\n**Edit** dotfile\n\n```chezmoi edit [~/.bashrc]```\n\n**Update** from remote\n\n```chezmoi update```\n\n**Show** differences\n\n```chezmoi diff```"],["SYNOPSIS","**chezmoi** _command_ [_options_]"],["DESCRIPTION","**chezmoi** manages dotfiles across multiple machines. It uses a source directory (typically a git repository at **~/.local/share/chezmoi**) to store the desired state of dotfiles, then applies that state to the home directory.\n\nThe tool supports Go templates for machine-specific configurations, allowing a single source repository to produce different files on different machines based on hostname, OS, or custom data. Secrets can be managed through integrations with password managers like 1Password, Bitwarden, and pass, or via encrypted files using age or gpg.\n\nUnlike symlink-based managers like GNU Stow, chezmoi copies files to their destinations, meaning the target files are ordinary files that work with any tool."],["PARAMETERS","**init** [_repo_]\n> Initialize chezmoi\n\n**add** _target_\n> Add file to source state\n\n**apply** [_target_]\n> Apply changes to destination\n\n**edit** _target_\n> Edit source state\n\n**update**\n> Pull and apply changes\n\n**diff** [_target_]\n> Show differences\n\n**cd**\n> Open shell in source directory\n\n**data**\n> Show template data"],["FEATURES","- Git integration\n- Templating (Go templates)\n- Machine-specific configurations\n- Secret management (encrypted files)\n- Cross-platform support\n- Automatic initialization\n- Dry-run mode\n- Script execution"],["WORKFLOW","```bash"],["Initialize with GitHub repo","chezmoi init https://github.com/username/dotfiles.git"],["Add files to manage","chezmoi add ~/.bashrc\nchezmoi add ~/.vimrc"],["Edit managed file","chezmoi edit ~/.bashrc"],["See what would change","chezmoi diff"],["Apply changes","chezmoi apply"],["Commit and push","chezmoi cd\ngit add .\ngit commit -m \"Update dotfiles\"\ngit push"],["On new machine","chezmoi init --apply https://github.com/username/dotfiles.git\n```"],["TEMPLATING","Use templates for machine-specific configs:\n```"],[".chezmoi.toml.tmpl","{{- if eq .chezmoi.hostname \"work-laptop\" }}\n[data]\n    email = \"work@example.com\"\n{{- else }}\n[data]\n    email = \"personal@example.com\"\n{{- end }}\n```"],["CONFIGURATION","**~/.config/chezmoi/chezmoi.toml**\n> Main configuration file. Sets source directory, merge tool, diff settings, encryption method, and secret manager integrations."],["CAVEATS","Learning curve for templates. Git repo public by default (use private for secrets). Template syntax can be complex. Large binary files not ideal. Requires understanding of Go templates."],["HISTORY","**chezmoi** was created by Tom Payne in **2018** to provide a modern, flexible dotfile manager with templating and cross-platform support."],["INSTALL","```dnf: sudo dnf install chezmoi```\n\n```pacman: sudo pacman -S chezmoi```\n\n```apk: sudo apk add chezmoi```\n\n```zypper: sudo zypper install chezmoi```\n\n```brew: brew install chezmoi```\n\n```nix: nix profile install nixpkgs#chezmoi```"],["SEE ALSO","[dotfiles](/man/dotfiles)(1), [git](/man/git)(1), [stow](/man/stow)(1)"],["RESOURCES","```[Source code](https://github.com/twpayne/chezmoi)```\n\n```[Documentation](https://www.chezmoi.io/)```"]],"e":["dotfiles","git","stow"],"x":"chezmoi init"},"conda-notices":{"n":"conda-notices","t":"Retrieve and display channel notices","s":[["TAGLINE","Retrieve and display channel notices"],["TLDR","**Show channel notices**\n\n```conda notices```\n\n**Show notices from a specific channel**\n\n```conda notices -c [channel_name]```"],["SYNOPSIS","**conda** **notices** [_options_]"],["PARAMETERS","**-c**, **--channel** _CHANNEL_\n> Include the given channel. Can be specified multiple times.\n\n**--use-local**\n> Use locally built packages.\n\n**--override-channels**\n> Do not search default or .condarc channels. Requires **--channel**."],["CONFIGURATION","**~/.condarc**\n> User-level conda configuration for channels, package settings, and solver options.\n\n**/opt/conda/.condarc**\n> System-wide conda configuration."],["DESCRIPTION","**conda notices** retrieves and displays notices from conda channels. Channels can publish important announcements, deprecation warnings, or security advisories that are shown through this command. Notices are cached locally and only fetched again after the cache expires."],["SEE ALSO","[conda](/man/conda)(1), [conda-config](/man/conda-config)(1), [conda-info](/man/conda-info)(1)"]],"e":["conda","conda-config","conda-info"],"x":"conda notices"},"cpx":{"n":"cpx","t":"Modern fast file copy tool with progress bars","s":[["TAGLINE","Modern fast file copy tool with progress bars"],["TLDR","**Copy** a single file\n\n```cpx [source.txt] [dest.txt]```\n\n**Copy** a directory recursively\n\n```cpx -r [source_dir/] [dest_dir/]```\n\n**Copy** with exclusion patterns\n\n```cpx -r -e \"node_modules\" -e \".git\" [project/] [/backup/]```\n\n**Resume** an interrupted transfer\n\n```cpx -r --resume [large_dataset/] [/backup/]```\n\n**Preserve** all file attributes\n\n```cpx -r -p=all [photos/] [/backup/photos/]```\n\n**Parallel** copy with 8 threads\n\n```cpx -r -j 8 [source/] [dest/]```"],["SYNOPSIS","**cpx** [_OPTIONS_] _SOURCE_... _DESTINATION_"],["PARAMETERS","**-r**, **--recursive**\n> Copy directories recursively.\n\n**-j** _N_\n> Number of parallel operations (default: 4).\n\n**--resume**\n> Resume interrupted transfers with checksum verification.\n\n**-f**, **--force**\n> Remove and retry if destination is unavailable.\n\n**-i**, **--interactive**\n> Prompt before overwriting.\n\n**-t**, **--target-directory** _DIR_\n> Copy all sources into the specified directory.\n\n**-e**, **--exclude** _PATTERN_\n> Exclude matching files (glob patterns, comma-separated).\n\n**-s**, **--symbolic-link** [_MODE_]\n> Create symlinks instead of copying (auto/absolute/relative).\n\n**-l**, **--link**\n> Create hard links instead of copying.\n\n**-P**, **--no-dereference**\n> Never follow source symlinks.\n\n**-L**, **--dereference**\n> Always follow source symlinks.\n\n**-p**, **--preserve** [_ATTRS_]\n> Preserve attributes (default/all/mode/timestamps/ownership/links/xattr).\n\n**-b**, **--backup** [_MODE_]\n> Backup existing files (none/simple/numbered/existing).\n\n**--reflink** [_WHEN_]\n> Copy-on-write if supported (auto/always/never).\n\n**--parents**\n> Preserve full source path structure.\n\n**--no-config**\n> Ignore all config files."],["DESCRIPTION","**cpx** is a modern replacement for the traditional **cp** command, built in Rust for performance and safety. It provides parallel file copying (up to 5x faster than standard cp in benchmarks), visual progress bars, the ability to resume interrupted transfers with checksum verification, graceful Ctrl+C handling with resume hints, and gitignore-style exclude patterns."],["CONFIGURATION","Configuration supported at three levels (in priority order): **./cpxconfig.toml** (project-level), **~/.config/cpx/cpxconfig.toml** (user-level), **/etc/cpx/cpxconfig.toml** (system-level). Manage with **cpx config init**, **cpx config show**, and **cpx config path**."],["CAVEATS","Currently only fully supports Linux (kernel 4.5+ recommended). macOS and Windows support is planned but not yet available. The **--resume** feature relies on checksums, which adds overhead for small files. Not a drop-in alias for **cp** in all edge cases."],["HISTORY","**cpx** was created by **Bhuminjay Soni** and publicly introduced around **January 2026**. Written in Rust under the MIT license. The latest version is v0.1.4 (February 2026). Available via Cargo, AUR (as cpx-copy), Nix, and pre-built binaries."],["INSTALL","```nix: nix profile install nixpkgs#cpx```"],["SEE ALSO","[cp](/man/cp)(1), [rsync](/man/rsync)(1), [mv](/man/mv)(1), [pv](/man/pv)(1)"]],"e":["cp","rsync","mv","pv"],"x":"cpx [source.txt] [dest.txt]"},"crane-version":{"n":"crane-version","t":"Display crane version information","s":[["TAGLINE","Display crane version information"],["TLDR","**Show the crane version**\n\n```crane version```"],["SYNOPSIS","**crane** **version**"],["DESCRIPTION","**crane version** displays the version information for the crane CLI tool. Crane is a tool for interacting with remote container images and registries, part of the google/go-containerregistry project."],["INSTALL","```pacman: sudo pacman -S crane```\n\n```apk: sudo apk add crane```\n\n```zypper: sudo zypper install crane```\n\n```brew: brew install crane```\n\n```nix: nix profile install nixpkgs#crane```"],["SEE ALSO","[crane](/man/crane)(1), [crane-cp](/man/crane-cp)(1)"]],"e":["crane","crane-cp"],"x":"crane version"},"daff":{"n":"daff","t":"Data diff tool for comparing tables and CSV files","s":[["TAGLINE","Data diff tool for comparing tables and CSV files"],["TLDR","**Compare two CSV files and show differences**\n\n```daff [file1.csv] [file2.csv]```\n\n**Compare with colored output**\n\n```daff --color [file1.csv] [file2.csv]```\n\n**Save diff output to a file**\n\n```daff --output [changes.csv] [file1.csv] [file2.csv]```\n\n**Apply a patch to update a file**\n\n```daff patch [file.csv] [changes.csv]```\n\n**Apply a patch in place**\n\n```daff patch --inplace [file.csv] [changes.csv]```\n\n**Three-way merge with a common parent**\n\n```daff merge [parent.csv] [local.csv] [remote.csv]```\n\n**Render a diff as HTML**\n\n```daff render --output [diff.html] [diff.csv]```\n\n**Copy/convert between formats**\n\n```daff copy [input.csv] [output.tsv]```"],["SYNOPSIS","**daff** [_command_] [_options_] [_files_]"],["DESCRIPTION","**daff** is a data diffing tool designed for comparing tabular data such as CSV files, database tables, and spreadsheets. It provides git-like diff and patch operations specifically optimized for structured data formats.\n\nThe tool can detect added, removed, and modified rows and columns, represent differences in various formats including HTML and colored terminal output, and apply patches to update data files. It's particularly useful for tracking changes in data exports, database dumps, and collaborative data editing workflows."],["COMMANDS","_a.csv b.csv_ (default)\n> Compare two tables and output differences.\n\n**patch** [**--inplace**] _a.csv patch.csv_\n> Apply a patch file to a table.\n\n**merge** [**--inplace**] _parent.csv a.csv b.csv_\n> Perform a three-way merge of tables.\n\n**render** _diff.csv_\n> Render a diff as HTML.\n\n**trim** _source.csv_\n> Trim whitespace from a table.\n\n**copy** _in.csv out.tsv_\n> Copy and convert between table formats.\n\n**git**\n> Use as a git diff driver for tabular files.\n\n**version**\n> Show daff version."],["OPTIONS","**--color**\n> Force colored output (default: auto-detect)\n\n**--no-color**\n> Disable colored output\n\n**--output** _FILE_\n> Write output to file instead of stdout.\n\n**--input-format** _FORMAT_\n> Input format (e.g., sqlite for comparing databases).\n\n**--www**\n> Open diff in web browser.\n\n**--inplace**\n> Modify input file directly (for patch and merge).\n\n**--unordered**\n> Treat rows as unordered during comparison.\n\n**-h**, **--help**\n> Display help and exit."],["CAVEATS","Data types are inferred from content; explicit type declarations are not supported. Very large files may require significant memory. Comparison of unordered data requires more computational resources. Binary data and special characters may not be handled correctly in all output formats."],["HISTORY","**daff** was originally created by Paul Fitzpatrick as a tool for data versioning and collaboration. The name comes from \"data diff.\" It has been implemented in multiple languages including JavaScript, Python, and Java. The tool was designed to bring version control concepts to data files, making it easier to track changes in datasets over time."],["SEE ALSO","[diff](/man/diff)(1), [patch](/man/patch)(1), [git](/man/git)(1), [csvlens](/man/csvlens)(1)"]],"e":["diff","patch","git","csvlens"],"x":"daff [file1.csv] [file2.csv]"},"delgroup":{"n":"delgroup","t":"remove groups from the system","s":[["TAGLINE","remove groups from the system"],["TLDR","**Delete a group**\n\n```sudo delgroup [groupname]```\n\n**Remove user from group**\n\n```sudo delgroup [username] [groupname]```\n\n**Delete system group**\n\n```sudo delgroup --system [groupname]```\n\n**Delete only if empty**\n\n```sudo delgroup --only-if-empty [groupname]```"],["SYNOPSIS","**delgroup** [_options_] _group_\n\n**delgroup** [_options_] _user_ _group_"],["DESCRIPTION","**delgroup** removes a group from the system or removes a user from a group. It's the Debian/Ubuntu counterpart to groupdel with additional safety features.\n\nWhen called with just a group name, it deletes the group. With both user and group names, it removes the user from that group without deleting the group."],["PARAMETERS","**--system**\n> Only act if the target is a system group (fail otherwise). Also affects creation logic in adduser.\n\n**--only-if-empty**\n> Only remove the group if it has no members.\n\n**--backup**\n> Back up any files deleted (for use with deluser; kept for symmetry).\n\n**--backup-to** _dir_\n> Place backups in _dir_ instead of the current directory.\n\n**--remove-home**\n> (deluser) remove the user's home directory. Listed here only for reference.\n\n**--group**\n> Force removal of a group even when a user name was given. Avoids ambiguous invocations.\n\n**-q**, **--quiet**\n> Suppress informational messages.\n\n**-v**, **--verbose**\n> More verbose progress output.\n\n**--conf** _FILE_\n> Use an alternate `deluser.conf` configuration file.\n\n**--help**\n> Display help information.\n\n**--version**\n> Display version."],["CONFIGURATION","**/etc/deluser.conf**\n> Controls default behavior for deluser and delgroup operations."],["CAVEATS","Requires root privileges. Cannot delete a user's primary group while user exists. Files owned by deleted group keep numeric GID. Debian/Ubuntu specific command."],["HISTORY","**delgroup** is part of the **adduser** package, created for **Debian** Linux to provide friendlier user/group management than the low-level commands. It wraps groupdel and gpasswd with additional checks and configuration options."],["SEE ALSO","[groupdel](/man/groupdel)(1), [deluser](/man/deluser)(1), [addgroup](/man/addgroup)(1), [gpasswd](/man/gpasswd)(1)"]],"e":["groupdel","deluser","addgroup","gpasswd"],"x":"sudo delgroup [groupname]"},"dh_pycentral":{"n":"dh_pycentral","t":"obsolete debhelper for python-central registration","s":[["TAGLINE","obsolete debhelper for python-central registration"],["TLDR","**Register Python modules** with pycentral\n\n```dh_pycentral```\n\n**Process with verbose** output\n\n```dh_pycentral -v```\n\n**Process specific package**\n\n```dh_pycentral -p [package_name]```"],["SYNOPSIS","**dh_pycentral** [_debhelper-options_]"],["PARAMETERS","**-v**, **--verbose**\n> Verbose output.\n\n**-p** _PACKAGE_, **--package** _PACKAGE_\n> Process specified package only.\n\n**-n**, **--no-scripts**\n> Do not modify postinst/prerm scripts."],["DESCRIPTION","**dh_pycentral** was a debhelper command for registering Python modules with python-central, a system for managing Python modules that could be shared between multiple Python versions.\n\nThe tool handled byte-compilation and version-specific installation of Python modules, ensuring they worked correctly with all installed Python versions on the system.\n\nThis command is obsolete as python-central was removed from Debian in favor of dh_python2/dh_python3 which provide better integration with modern Python packaging standards."],["CAVEATS","Obsolete and deprecated. Modern packages should use dh_python3 instead. Only relevant for maintaining legacy packages or understanding historical Debian Python packaging."],["HISTORY","dh_pycentral was part of **python-central**, developed around **2006** for Debian's Python packaging. It was deprecated in favor of python-support and later dh_python2/dh_python3, being removed from Debian after the Python 2 deprecation."],["SEE ALSO","[dh](/man/dh)(1), [python](/man/python)(1)"]],"e":["dh","python"],"x":"dh_pycentral"},"dua":{"n":"dua","t":"fast parallel disk usage analyzer","s":[["TAGLINE","fast parallel disk usage analyzer"],["TLDR","**Analyze current directory**\n\n```dua```\n\n**Analyze specific path**\n\n```dua [/path/to/directory]```\n\n**Interactive mode**\n\n```dua i```\n\n**Interactive with specific path**\n\n```dua i [/path/to/directory]```\n\n**Show apparent size** instead of disk usage\n\n```dua -A [/path/to/directory]```\n\n**Use a specific number of threads**\n\n```dua -t [4] [/path/to/directory]```\n\n**Print sizes in a chosen unit**\n\n```dua -f [MB] [/path/to/directory]```\n\n**Stay on one filesystem**, not crossing mount points\n\n```dua -x [/path/to/directory]```\n\n**Show version**\n\n```dua --version```"],["SYNOPSIS","**dua** [_flags_] [_options_] [_subcommand_] [_input_...]"],["COMMANDS","**i**, **interactive**\n> Launch the terminal user interface.\n\n**a**, **aggregate**\n> Aggregate the space consumed by the given paths. This is the default when no subcommand is given.\n\n**config edit** / **config show-default**\n> Open the configuration file in `$EDITOR`, or print the defaults.\n\n**completions** _SHELL_\n> Generate a shell completion script."],["PARAMETERS","**-A**, **--apparent-size**\n> Show apparent size instead of disk usage.\n\n**-t**, **--threads** _COUNT_\n> Threads to use. **0** means one per logical processor and is the default on Linux; macOS defaults to 3, since more threads measurably hurt there. Set 1 for a single thread.\n\n**-f**, **--format** _FORMAT_\n> Byte format: `metric`, `binary`, `bytes`, `GB`, `GiB`, `MB`, `MiB`. Defaults to binary, except on macOS where it is metric.\n\n**-l**, **--count-hard-links**\n> Count hard-linked files each time they are seen, rather than once.\n\n**-x**, **--stay-on-filesystem**\n> Do not cross filesystems or traverse mount points.\n\n**-i**, **--ignore-dirs** _PATH_...\n> Absolute directories to skip when reached during traversal. On Linux this defaults to `/proc`, `/dev`, `/sys`, and `/run`. A directory passed explicitly as an input path is still scanned.\n\n**--stats**\n> Print traversal statistics to stderr (aggregate mode).\n\n**--no-sort** / **--no-total**\n> Keep the command-line order rather than sorting by size, or omit the total row for multiple inputs.\n\n**-h**, **--help** / **-V**, **--version**\n> Display help, or version information."],["INTERACTIVE KEYS","**j**/**k** or **Down**/**Up**\n> Navigate entries.\n\n**Enter** or **l**\n> Enter directory.\n\n**u** or **h**\n> Go up to parent.\n\n**o**\n> Open in file manager.\n\n**d**\n> Mark for deletion.\n\n**Space**\n> Toggle expansion.\n\n**s**\n> Sort by size.\n\n**r**\n> Refresh.\n\n**g**\n> Go to top.\n\n**G**\n> Go to bottom.\n\n**Ctrl+c** or **q**\n> Quit."],["DESCRIPTION","**dua** (Disk Usage Analyzer) is a fast disk space analyzer with an optional interactive interface. It scans directories and displays sizes.\n\nDefault mode shows aggregate sizes for paths. Interactive mode (**dua i**) provides a navigable tree view for exploring disk usage.\n\nParallel traversal is what makes dua fast: it saturates an SSD rather than walking the tree one directory at a time, and on a large tree it comfortably outruns `du`. On spinning disks the advantage largely disappears, since the bottleneck is seek time rather than CPU.\n\nApparent size (**-A**) is the size of the file's contents. Without it, dua reports the space actually occupied on disk, which is what you care about when freeing space: a sparse file or a heavily compressed btrfs/ZFS dataset can occupy far less than its apparent size, and many small files occupy considerably more, because each one rounds up to a block.\n\nIn interactive mode, mark entries with **d** and they are deleted when you confirm. Deleting from the TUI is dua's real selling point over `du`, and it is deliberately faster than `rm` on large trees."],["CAVEATS","**Deletion in the TUI is permanent.** Nothing goes to a trash folder and there is no undo, so it is worth pausing before confirming a marked selection.\n\nNote that **-l**/**--count-hard-links** *disables* the default deduplication: by default a hard-linked file is counted once, which is usually what you want, and this flag makes each link count in full.\n\nNumbers from dua and `du` will not always agree, and neither is wrong: they differ on whether hard links are counted once, whether mount points are crossed, and whether apparent or on-disk size is reported. Compare like with like before concluding that one of them is lying."],["HISTORY","dua was created by **Sebastian Thiel** (Byron), who also wrote gitoxide, and first released around **2019**. Written in Rust, it belongs to the generation of tools that revisited the classic Unix utilities on the assumption that the machine has many cores and a fast SSD, neither of which was true when `du` was designed. Its distinguishing feature over `ncdu` and `dust` is that deletion is a first-class operation, not an afterthought."],["INSTALL","```apk: sudo apk add dua```\n\n```nix: nix profile install nixpkgs#dua```"],["SEE ALSO","[du](/man/du)(1), [ncdu](/man/ncdu)(1), [dust](/man/dust)(1), [gdu](/man/gdu)(1), [duf](/man/duf)(1)"],["RESOURCES","```[Source code](https://github.com/Byron/dua-cli)```"]],"e":["du","ncdu","dust","gdu","duf"],"x":"dua"},"ec.1s":{"n":"ec.1s","t":"Elliptic Curve key operations","s":[["TAGLINE","Elliptic Curve key operations"],["TLDR","**Generate EC key**\n\n```openssl ec -in [key.pem] -text -noout```\n\n**Generate EC private** key\n\n```openssl ecparam -genkey -name [prime256v1] -out [key.pem]```\n\n**Convert EC key** format\n\n```openssl ec -in [key.pem] -outform DER -out [key.der]```"],["SYNOPSIS","**openssl ec** [_options_]"],["PARAMETERS","**-in** _FILE_\n> Input key file.\n\n**-out** _FILE_\n> Output key file.\n\n**-text**\n> Print key in text form.\n\n**-noout**\n> Don't output encoded key.\n\n**-outform** _FORMAT_\n> Output format: PEM, DER.\n\n**-param_enc** _TYPE_\n> Parameter encoding type.\n\n**--help**\n> Display help information."],["DESCRIPTION","**openssl ec** handles Elliptic Curve (EC) key operations including reading, writing, and converting EC private keys. EC cryptography provides strong security with smaller key sizes compared to RSA.\n\nThe command reads EC keys in various formats and can output them in PEM or DER encoding. It's used for key management in TLS configurations and digital signatures.\n\nEC keys are generated using ecparam with a named curve, then processed with the ec command for format conversion or examination."],["CAVEATS","Curve selection affects security and compatibility. Some curves have known weaknesses. Not all systems support all curves."],["HISTORY","OpenSSL EC support was added as elliptic curve cryptography became standard for TLS. EC offers equivalent security to RSA with significantly smaller keys."],["SEE ALSO","[openssl](/man/openssl)(1)"]],"e":["openssl"],"x":"openssl ec -in [key.pem] -text -noout"},"evolution":{"n":"evolution","t":"GNOME personal information management application","s":[["TAGLINE","GNOME personal information management application"],["TLDR","**Start Evolution**\n\n```evolution```\n\n**Start with specific component**\n\n```evolution --component=[mail|calendar|contacts]```\n\n**Compose new email**\n\n```evolution mailto:[user@example.com]```\n\n**Import file**\n\n```evolution --import [file.ics]```\n\n**Force offline mode**\n\n```evolution --force-offline```"],["SYNOPSIS","**evolution** [_options_] [_URI_]"],["PARAMETERS","**--component** _name_\n> Start with specific component: mail, calendar, contacts, tasks, memos.\n\n**--import** _file_\n> Import file (vCard, iCal, etc.).\n\n**--force-online**\n> Force online mode.\n\n**--force-offline**\n> Force offline mode.\n\n**--quit**\n> Request running instance to quit.\n\n**--express**\n> Start in express mode.\n\n**--help**\n> Display help."],["DESCRIPTION","**Evolution** is the GNOME personal information management application. It integrates email, calendar, contacts, tasks, and memos into a single application, similar to Microsoft Outlook.\n\nEvolution supports multiple email protocols (IMAP, POP, SMTP, Exchange), calendar standards (CalDAV, ICS), and contact formats (CardDAV, LDAP). It includes features like encryption (GPG, S/MIME), spam filtering, and search folders."],["FEATURES","- Multiple email accounts\n- CalDAV and Exchange calendars\n- CardDAV and LDAP contacts\n- Task and memo management\n- GPG and S/MIME encryption\n- Spam filtering"],["CAVEATS","Heavy resource usage compared to simpler clients. Some Exchange features require additional packages. First-time setup wizard is mandatory. Background processes may continue after closing."],["HISTORY","Evolution was originally developed by **Ximian** starting in **1999**, acquired by Novell in 2003, and later became part of the GNOME project. It was one of the first Linux applications to offer comprehensive groupware functionality."],["INSTALL","```apt: sudo apt install evolution```\n\n```dnf: sudo dnf install evolution```\n\n```pacman: sudo pacman -S evolution```\n\n```apk: sudo apk add evolution```\n\n```zypper: sudo zypper install evolution```\n\n```nix: nix profile install nixpkgs#evolution```"],["SEE ALSO","[thunderbird](/man/thunderbird)(1)"]],"e":["thunderbird"],"x":"evolution"},"ex":{"n":"ex","t":"Line-oriented text editor, the predecessor of vi","s":[["TAGLINE","Line-oriented text editor, the predecessor of vi"],["TLDR","**Start ex mode on a file**\n\n```ex [file.txt]```\n\n**Execute a substitution command and quit**\n\n```ex -c \"%s/[old]/[new]/g\" -c \"wq\" [file.txt]```\n\n**Open a file in read-only mode**\n\n```ex -R [file.txt]```\n\n**Edit multiple files sequentially**\n\n```ex [file1.txt] [file2.txt]```"],["SYNOPSIS","**ex** [_options_] [_files_...]"],["PARAMETERS","_FILES_\n> Files to edit.\n\n**-c** _COMMAND_\n> Execute command after loading the first file.\n\n**-s**\n> Silent (batch) mode. Suppress all interactive feedback. Useful for scripting.\n\n**-R**\n> Read-only mode. Prevents writing to the file.\n\n**-r** _FILE_\n> Recover editing session from a swap file after a crash.\n\n**-n**\n> Do not use a swap file (vim implementation).\n\n**-t** _TAG_\n> Edit the file containing the specified tag.\n\n**-V**\n> Verbose mode. Show commands read from stdin.\n\n**-v**\n> Start in visual mode (equivalent to running vi)."],["DESCRIPTION","**ex** is a line-oriented text editor. It provides command-line editing without a visual interface, using the same command language that powers vi's colon (:) commands. Commands are entered at a prompt rather than in a full-screen display.\n\nex is particularly valuable for batch editing and scripted file modifications, allowing search/replace and other transformations without interactive use. When combined with **-s** for silent mode, it can process editing commands from stdin or scripts.\n\nThe ex and vi editors are the same program started in different modes. Running **ex -v** starts visual mode (vi), and running **vi -e** starts line mode (ex)."],["CAVEATS","No visual feedback in ex mode, making complex edits error-prone. Most modern systems provide ex via vim (as **vim -e**). The POSIX specification defines a subset of features; vim's ex mode includes many extensions."],["HISTORY","ex was developed by **Bill Joy** at UC Berkeley in the late 1970s as an extended version of ed. It later gained the visual interface that became vi, with ex remaining as the underlying command mode. ex first appeared in **1BSD** (1978)."],["INSTALL","```pacman: sudo pacman -S gvim```\n\n```apk: sudo apk add gvim```\n\n```zypper: sudo zypper install gvim```"],["SEE ALSO","[vi](/man/vi)(1), [vim](/man/vim)(1), [ed](/man/ed)(1), [sed](/man/sed)(1)"]],"e":["vi","vim","ed","sed"],"x":"ex [file.txt]"},"flutter-pub":{"n":"flutter-pub","t":"Dart package dependency manager for Flutter","s":[["TAGLINE","Dart package dependency manager for Flutter"],["TLDR","**Get dependencies**\n\n```flutter pub get```\n\n**Update dependencies**\n\n```flutter pub upgrade```\n\n**Add package**\n\n```flutter pub add [package_name]```\n\n**Remove package**\n\n```flutter pub remove [package_name]```\n\n**Run build runner**\n\n```flutter pub run build_runner build```"],["SYNOPSIS","**flutter pub** _command_ [_options_] [_arguments_]"],["PARAMETERS","_COMMAND_\n> Operation: get, upgrade, add, remove, run, etc.\n\n**get**\n> Install dependencies from pubspec.yaml.\n\n**upgrade**\n> Update to latest compatible versions.\n\n**add** _PACKAGE_\n> Add package to dependencies.\n\n**remove** _PACKAGE_\n> Remove package from dependencies.\n\n**run** _SCRIPT_\n> Run package script.\n\n**outdated**\n> Show outdated packages.\n\n**deps**\n> Show the dependency tree.\n\n**publish**\n> Publish the package to **pub.dev**.\n\n**downgrade**\n> Downgrade dependencies to the lowest versions allowed by **pubspec.yaml**.\n\n**cache**\n> Inspect, repair, or clean the local pub package cache.\n\n**--help**\n> Display help information."],["DESCRIPTION","**flutter pub** manages Dart package dependencies for Flutter projects. It installs, updates, and resolves packages from pub.dev, the Dart package repository.\n\nThe tool reads pubspec.yaml for dependency specifications and generates pubspec.lock for reproducible builds. It handles transitive dependencies and version conflicts automatically.\n\nflutter pub enables code reuse through thousands of community packages for networking, state management, UI components, and more."],["CONFIGURATION","**pubspec.yaml**\n> Project configuration file defining dependencies, dev dependencies, and package metadata.\n\n**pubspec.lock**\n> Generated lock file ensuring reproducible dependency versions across environments."],["CAVEATS","Version conflicts may require manual resolution. Some packages are platform-specific. Large dependency trees slow first build. **flutter pub run** is deprecated for executable scripts — prefer **dart run** (or **flutter pub run** only when the executable depends on the Flutter SDK)."],["HISTORY","flutter pub wraps the **Dart pub** tool for Flutter development. The pub.dev repository serves both Dart and Flutter packages, managed through the shared package specification format."],["INSTALL","```nix: nix profile install nixpkgs#flutter```"],["SEE ALSO","[flutter](/man/flutter)(1), [dart](/man/dart)(1)"]],"e":["flutter","dart"],"x":"flutter pub get"},"foot":{"n":"foot","t":"fast Wayland terminal emulator","s":[["TAGLINE","fast Wayland terminal emulator"],["TLDR","**Spawn** a terminal\n\n```foot```\n\n**Verify** your config\n\n```foot -C```\n\nStart the **server** (use footclient to start terminal windows)\n\n```foot -s```\n\nDisplay **help**\n\n```foot -h```\n\nDisplay **version**\n\n```foot -v```"],["SYNOPSIS","**foot** [_options_] [_command_ [_args_]]"],["PARAMETERS","**-c**, **--config=**_FILE_\n> Use configuration file _FILE_ instead of the default\n\n**-C**, **--check-config**\n> Verify configuration file and exit\n\n**-s**, **--server**\n> Run as a server; use footclient to spawn terminals\n\n**-t**, **--term=**_TERM_\n> Set TERM environment variable value\n\n**-T**, **--title=**_TITLE_\n> Set initial window title\n\n**-a**, **--app-id=**_ID_\n> Set Wayland app-id\n\n**-w**, **--window-size-pixels=**_WxH_\n> Set initial window size in pixels\n\n**-W**, **--window-size-chars=**_COLSxROWS_\n> Set initial window size in characters\n\n**-f**, **--font=**_FONT_\n> Override font from config\n\n**-h**, **--help**\n> Display help message\n\n**-v**, **--version**\n> Display version information"],["DESCRIPTION","**foot** is a fast, lightweight, and minimalistic terminal emulator designed for Wayland compositors. It emphasizes speed, low memory footprint, and simplicity while providing modern features.\n\nKey features include sixel image support, synchronized rendering to prevent tearing, font ligatures, scrollback search, and URL detection. It can run in server mode where a single daemon handles multiple terminal windows via **footclient**, reducing memory overhead.\n\nConfiguration is done through **~/.config/foot/foot.ini** with extensive customization options for colors, fonts, keybindings, and behavior."],["CONFIGURATION","**~/.config/foot/foot.ini**\n> Main configuration file for colors, fonts, keybindings, scrollback, and terminal behavior.\n\n**~/.config/foot/colors.ini**\n> Optional separate color scheme configuration that can be included from foot.ini."],["CAVEATS","foot is Wayland-only and does not support X11. Some applications expecting xterm-specific behavior may not work correctly. Server mode requires footclient for spawning additional terminals."],["HISTORY","foot was created by Daniel Eklof and first released in **2020**. It was designed from the ground up for Wayland, avoiding the legacy baggage of X11 terminal emulators while achieving exceptional performance."],["INSTALL","```apt: sudo apt install foot```\n\n```dnf: sudo dnf install foot```\n\n```pacman: sudo pacman -S foot```\n\n```apk: sudo apk add foot```\n\n```zypper: sudo zypper install foot```\n\n```brew: brew install foot```\n\n```nix: nix profile install nixpkgs#foot```"],["SEE ALSO","[footclient](/man/footclient)(1), [alacritty](/man/alacritty)(1), [kitty](/man/kitty)(1), [wezterm](/man/wezterm)(1)"],["RESOURCES","```[Source code](https://codeberg.org/dnkl/foot)```"]],"e":["footclient","alacritty","kitty","wezterm"],"x":"foot"},"gcore":{"n":"gcore","t":"generates process core dumps","s":[["TAGLINE","generates process core dumps"],["TLDR","**Generate core dump**\n\n```gcore [pid]```\n\n**Specify output file**\n\n```gcore -o [corefile] [pid]```\n\n**Generate for all** threads\n\n```gcore -a [pid]```"],["SYNOPSIS","**gcore** [_options_] _pid_"],["PARAMETERS","_PID_\n> Process ID to dump.\n\n**-o** _FILE_\n> Output filename prefix.\n\n**-a**\n> Dump all threads.\n\n**--help**\n> Display help information."],["DESCRIPTION","**gcore** generates a core dump of a running process without terminating it. It captures the process memory state for debugging or analysis while the process continues running.\n\nThe tool attaches to the target process temporarily to create the dump. The resulting core file can be analyzed with debuggers like GDB to examine program state.\n\ngcore is useful for debugging production systems without causing downtime."],["CAVEATS","Requires ptrace permissions. Large processes create large dumps. May briefly pause target process."],["HISTORY","gcore is part of **GDB** (GNU Debugger), providing non-destructive core dump generation for debugging running processes without interrupting service."],["INSTALL","```apt: sudo apt install gdb```\n\n```dnf: sudo dnf install gdb```\n\n```pacman: sudo pacman -S gdb```\n\n```apk: sudo apk add gdb```\n\n```zypper: sudo zypper install gdb```\n\n```brew: brew install gdb```\n\n```nix: nix profile install nixpkgs#gdb```"],["SEE ALSO","[gdb](/man/gdb)(1), [kill](/man/kill)(1)"]],"e":["gdb","kill"],"x":"gcore [pid]"},"getrlimit":{"n":"getrlimit","t":"resource limit query and management interface","s":[["TAGLINE","resource limit query and management interface"],["TLDR","**Get resource limits (shell)**\n\n```ulimit -a```\n\n**Get specific limit (open files)**\n\n```ulimit -n```\n\n**Show resource limits of a process**\n\n```prlimit --pid [pid]```\n\n**Set open file limit for a command**\n\n```prlimit --nofile=4096 [command]```"],["SYNOPSIS","**#include <sys/resource.h>**\n\n**int getrlimit(int** _resource_, **struct rlimit** \\*_rlim_**);**\n\n**int setrlimit(int** _resource_, **const struct rlimit** \\*_rlim_**);**"],["PARAMETERS","_resource_\n> Resource type (RLIMIT_NOFILE, RLIMIT_NPROC, etc.).\n\n_rlim_\n> Pointer to rlimit structure with soft/hard limits."],["DESCRIPTION","**getrlimit()** and **setrlimit()** are system calls for querying and setting resource limits for the calling process. These limits control maximum values for various system resources.\n\nThe **ulimit** shell built-in provides command-line access to these limits. Limits have soft (current) and hard (maximum) values."],["RESOURCES","```\nRLIMIT_NOFILE  - Maximum open files\nRLIMIT_NPROC   - Maximum processes\nRLIMIT_AS      - Maximum address space\nRLIMIT_CORE    - Maximum core file size\nRLIMIT_STACK   - Maximum stack size\nRLIMIT_DATA    - Maximum data segment\nRLIMIT_FSIZE   - Maximum file size\nRLIMIT_CPU     - CPU time limit\nRLIMIT_MEMLOCK - Maximum locked memory\n```"],["SHELL ACCESS","```bash\nulimit -n        # Open files\nulimit -u        # Max processes\nulimit -s        # Stack size\nulimit -a        # All limits\nulimit -n 4096   # Set open files limit\n```"],["CONFIGURATION","**/etc/security/limits.conf**\n> PAM configuration for setting default resource limits per user or group.\n\n**/etc/systemd/system.conf**\n> Systemd-wide default resource limits for services and user sessions."],["CAVEATS","System call, not a command. Non-root users cannot raise hard limits. Some limits affect child processes. PAM may set limits at login."],["SEE ALSO","[ulimit](/man/ulimit)(1), [prlimit](/man/prlimit)(1), [limits.conf](/man/limits.conf)(5)"]],"e":["ulimit","prlimit","limits.conf"],"x":"ulimit -a"},"git-pull-request":{"n":"git-pull-request","t":"Create pull request from the command line","s":[["TAGLINE","Create pull request from the command line"],["TLDR","**Create a pull request interactively**\n\n```git pull-request```\n\n**Create a pull request with a title**\n\n```git pull-request -m \"[title]\"```\n\n**Create a pull request targeting a specific base branch**\n\n```git pull-request -b [base_branch]```\n\n**Create a pull request with title and body**\n\n```git pull-request -m \"[title]\" -m \"[body]\"```\n\n**Create a pull request from a specific head branch**\n\n```git pull-request -h [feature_branch] -b [main]```"],["SYNOPSIS","**git** **pull-request** [_options_]"],["PARAMETERS","**-m**, **--message** _text_\n> PR title/message. Use twice to set title and body separately.\n\n**-b**, **--base** _branch_\n> Target base branch for the pull request.\n\n**-h**, **--head** _branch_\n> Source head branch (defaults to current branch).\n\n**-i**, **--issue** _number_\n> Convert an existing issue into a pull request."],["DESCRIPTION","**git pull-request** creates a GitHub pull request from the command line. Part of the **hub** CLI tool (by GitHub), it pushes the current branch if needed and opens a pull request to the specified base branch.\n\nThe command can extract PR title and body from commit messages or prompt for them interactively. When invoked without **-m**, an editor opens for composing the message. The first line becomes the title, and subsequent lines become the body.\n\nNote: The **hub** tool is now in maintenance mode. The official replacement is **gh pr create** from the GitHub CLI, which provides more comprehensive GitHub integration."],["INSTALL","```dnf: sudo dnf install git-pull-request```"],["SEE ALSO","[gh-pr-create](/man/gh-pr-create)(1), [gh](/man/gh)(1), [hub](/man/hub)(1), [git-extras](/man/git-extras)(1), [git-request-pull](/man/git-request-pull)(1), [git-push](/man/git-push)(1)"]],"e":["gh-pr-create","gh","hub","git-extras","git-request-pull","git-push"],"x":"git pull-request"},"gnuradio":{"n":"gnuradio","t":"software-defined radio signal processing toolkit","s":[["TAGLINE","software-defined radio signal processing toolkit"],["TLDR","**Start GNU Radio Companion**\n\n```gnuradio-companion```\n\n**Run flowgraph**\n\n```python3 [flowgraph.py]```\n\n**Check version**\n\n```gnuradio-config-info --version```\n\n**Compile a flowgraph** without launching the GUI\n\n```gnuradio-companion -c [flowgraph.grc]```\n\n**Compile a flowgraph** to a specific output directory\n\n```gnuradio-companion -c -d [output_dir] [flowgraph.grc]```\n\n**Create OOT module**\n\n```gr_modtool newmod [mymodule]```"],["SYNOPSIS","**gnuradio-companion** [_options_] [_file.grc_...]"],["PARAMETERS","_file.grc_\n> GNU Radio Companion flowgraph file.\n\n**-c**, **--compile**\n> Only compile the GRC file to Python without launching the GUI.\n\n**-d**, **--output-dir** _DIR_\n> Output path for the generated Python file.\n\n**-v**, **--version**\n> Show program version number and exit.\n\n**-h**, **--help**\n> Show help message and exit."],["DESCRIPTION","**GNU Radio** is a free software toolkit for signal processing. It provides blocks for building software-defined radios (SDRs) and signal processing systems.\n\nGNU Radio Companion (GRC) is the graphical flowgraph editor. Flowgraphs connect processing blocks to create radio receivers, transmitters, and signal analyzers. Flowgraphs compile to Python code."],["COMPONENTS","```\ngnuradio-companion     Graphical flowgraph editor\ngr_modtool             Out-of-tree module management\ngnuradio-config-info   Show installed GNU Radio details\nuhd_fft                Spectrum analyzer (for USRP)\n```"],["COMMON BLOCKS","- **Signal Sources**: Noise, oscillators, file sources\n- **Filters**: Low/high/band pass, FFT\n- **Modulation**: AM, FM, PSK, QAM\n- **Hardware**: RTL-SDR, USRP, HackRF"],["CONFIGURATION","**~/.gnuradio/config.conf**\n> User configuration file for GNU Radio preferences and block paths."],["CAVEATS","Requires compatible SDR hardware for RF work. Complex signal processing needs understanding of DSP. High sample rates need fast CPU. Python knowledge helpful."],["HISTORY","GNU Radio was started by **Eric Blossom** in **2001**. It became the leading open-source SDR framework, used in academia, amateur radio, and commercial applications. The project is now community-maintained."],["INSTALL","```dnf: sudo dnf install gnuradio```\n\n```pacman: sudo pacman -S gnuradio```\n\n```zypper: sudo zypper install gnuradio```\n\n```brew: brew install gnuradio```\n\n```nix: nix profile install nixpkgs#gnuradio```"],["SEE ALSO","[rtl_sdr](/man/rtl_sdr)(1), [gqrx](/man/gqrx)(1), [python3](/man/python3)(1)"]],"e":["rtl_sdr","gqrx","python3"],"x":"gnuradio-companion"},"grim":{"n":"grim","t":"Wayland-native screenshot tool","s":[["TAGLINE","Wayland-native screenshot tool"],["TLDR","**Screenshot** all outputs\n\n```grim```\n\nScreenshot a **specific output**\n\n```grim -o [output_name]```\n\nScreenshot a **specific region**\n\n```grim -g \"[x],[y] [width]x[height]\"```\n\n**Select** a region and screenshot it (using slurp)\n\n```grim -g \"$(slurp)\"```\n\nUse a **custom filename**\n\n```grim \"[path/to/file.png]\"```\n\nScreenshot and **copy to clipboard**\n\n```grim - | wl-copy```"],["SYNOPSIS","**grim** [_options_] [_output_]"],["PARAMETERS","**-g** _GEOMETRY_\n> Capture specific region (x,y widthxheight format)\n\n**-o** _OUTPUT_\n> Capture specific Wayland output (monitor)\n\n**-s** _FACTOR_\n> Set scale factor\n\n**-t** _TYPE_\n> Output format: png, jpeg, or ppm\n\n**-q** _QUALITY_\n> JPEG quality (1-100)\n\n**-l** _LEVEL_\n> PNG compression level (0-9)\n\n**-c**\n> Include cursor in screenshot"],["DESCRIPTION","**grim** (grab image) is a Wayland-native screenshot tool. It captures images directly from the Wayland compositor, making it compatible with Sway, GNOME on Wayland, and other Wayland-based environments.\n\nFor region selection, grim is commonly paired with **slurp**, which allows interactive selection of screen areas. Output to stdout (**-**) enables piping to clipboard managers like **wl-copy**."],["CAVEATS","Wayland-only; does not work on X11 (use scrot or maim instead). Requires a compatible Wayland compositor with screencopy protocol support. Some compositors may need additional configuration to enable screenshots."],["HISTORY","grim was created by Simon Ser (emersion) as part of the Wayland ecosystem tooling for the Sway window manager. It provides functionality similar to X11 tools like scrot but designed specifically for Wayland's security model."],["INSTALL","```apt: sudo apt install grim```\n\n```dnf: sudo dnf install grim```\n\n```pacman: sudo pacman -S grim```\n\n```apk: sudo apk add grim```\n\n```zypper: sudo zypper install grim```\n\n```nix: nix profile install nixpkgs#grim```"],["SEE ALSO","[slurp](/man/slurp)(1), [wl-copy](/man/wl-copy)(1), [scrot](/man/scrot)(1), [maim](/man/maim)(1)"],["RESOURCES","```[Source code](https://git.sr.ht/~emersion/grim)```"]],"e":["slurp","wl-copy","scrot","maim"],"x":"grim"},"grmon":{"n":"grmon","t":"Terminal monitor for Go process goroutines via pprof","s":[["TAGLINE","Terminal monitor for Go process goroutines via pprof"],["TLDR","**Connect** to default local pprof endpoint\n\n```grmon```\n\n**Connect** to a specific host and port\n\n```grmon -host [localhost:6060]```\n\n**Set refresh interval** in seconds (0 disables auto-refresh)\n\n```grmon -i [2]```\n\n**Use a custom pprof path**\n\n```grmon -endpoint [/debug/pprof]```"],["SYNOPSIS","**grmon** [**-i** *seconds*] [**-host** *host:port*] [**-endpoint** *path*]"],["DESCRIPTION","**grmon** is an interactive terminal UI for watching goroutines in a running Go program. The target process must expose Go's **net/http/pprof** endpoints (or start the small **grmon** agent). **grmon** polls the profile endpoint and displays goroutine counts and stacks; you can pause refresh, filter, sort, and expand individual traces.\n\nDefault target is **localhost:1234** with path **/debug/pprof**. In the target app, either call **grmon.Start()** from the agent package or serve pprof yourself (for example **http.ListenAndServe(\":6060\", nil)** with **_ \"net/http/pprof\"** imported)."],["PARAMETERS","**-i** *seconds*\n\n> Refresh interval (default: 5). **0** disables automatic refresh.\n\n**-host** *host:port*\n\n> Target host (default: **localhost:1234**).\n\n**-endpoint** *path*\n\n> pprof base path (default: **/debug/pprof**).\n\nInteractive keys include **r** refresh, **p** pause, **s** sort, **f** filter, arrows/**j**/**k** move, Enter expand, **t** full-screen trace, **q** quit."],["CAVEATS","The target must expose pprof; binding pprof to a public interface is a security risk. Stack collection has runtime cost. **go get** install paths may differ by Go module mode—prefer current module install instructions from the repository."],["INSTALL","```nix: nix profile install nixpkgs#grmon```"],["SEE ALSO","[go](/man/go)(1), [pprof](/man/pprof)(1), [top](/man/top)(1)"],["RESOURCES","```[Source code](https://github.com/bcicen/grmon)```"]],"e":["go","pprof","top"],"x":"grmon"},"ideviceinfo":{"n":"ideviceinfo","t":"display information from a connected iOS device (libimobiledevice)","s":[["TAGLINE","display information from a connected iOS device (libimobiledevice)"],["TLDR","**Show every available property** of the first connected device\n\n```ideviceinfo```\n\n**Target a specific device by UDID**\n\n```ideviceinfo -u [device-udid]```\n\n**Query a specific key only**\n\n```ideviceinfo -k [ProductVersion]```\n\n**Query a specific Lockdown domain**\n\n```ideviceinfo -q [com.apple.disk_usage]```\n\n**Output as an XML property list**\n\n```ideviceinfo -x```\n\n**Connect to a device over the network** (Wi-Fi pairing)\n\n```ideviceinfo -n```\n\n**Simple/unpaired connection** (skip auto-pairing)\n\n```ideviceinfo -s```"],["SYNOPSIS","**ideviceinfo** [_options_]"],["PARAMETERS","**-u**, **--udid** _UDID_\n> Target a specific device by its UDID. Required when more than one device is connected.\n\n**-q**, **--domain** _NAME_\n> Restrict the query to the given Lockdown domain (e.g. `com.apple.disk_usage`, `com.apple.mobile.battery`, `com.apple.mobile.wireless_lockdown`).\n\n**-k**, **--key** _NAME_\n> Only print the value of the given key. Prints the raw value with no key header.\n\n**-x**, **--xml**\n> Emit output as an Apple XML plist instead of `key: value` lines. Useful for scripting.\n\n**-s**, **--simple**\n> Use a simple Lockdown connection that does not pair with the device. A smaller subset of keys is available.\n\n**-n**, **--network**\n> Connect to a device paired over Wi-Fi instead of USB.\n\n**-d**, **--debug**\n> Verbose debugging output from libimobiledevice.\n\n**-h**, **--help**\n> Show help.\n\n**-v**, **--version**\n> Show version information."],["DESCRIPTION","**ideviceinfo** queries the Lockdown service on a connected iOS (iPhone, iPad, iPod, Apple TV) device and prints its properties. Without a key or domain, it dumps the public root-level keys — `DeviceName`, `ProductType`, `ProductVersion`, `BuildVersion`, `SerialNumber`, `UniqueDeviceID`, `WiFiAddress`, `BluetoothAddress`, and so on.\n\nPer-domain queries (`-q`) unlock protected namespaces like `com.apple.disk_usage` (storage stats) or `com.apple.mobile.battery` (battery info). Most domains require the device to be paired and unlocked."],["CAVEATS","Part of **libimobiledevice**. The device must be **trusted** on the host — i.e. plugged in once while unlocked and the \"Trust this computer?\" prompt accepted. On macOS, Apple's own `usbmuxd` replaces libimobiledevice's; mixing the two can cause pairing conflicts. Over-the-network (`-n`) only works after Wi-Fi sync is enabled in iTunes/Finder."],["HISTORY","**ideviceinfo** ships with the **libimobiledevice** project — a cross-platform reimplementation of Apple's iTunes/Finder device protocols. Created around **2008** by **Jonathan Beck**, it is now maintained by **Nikias Bassen** and contributors."],["INSTALL","```apt: sudo apt install libimobiledevice-utils```\n\n```dnf: sudo dnf install libimobiledevice-utils```\n\n```apk: sudo apk add libimobiledevice-progs```"],["SEE ALSO","[idevice_id](/man/idevice_id)(1), [idevicebackup2](/man/idevicebackup2)(1), [idevicepair](/man/idevicepair)(1), [idevicesyslog](/man/idevicesyslog)(1)"]],"e":["idevice_id","idevicebackup2","idevicepair","idevicesyslog"],"x":"ideviceinfo"},"ip-tuntap":{"n":"ip-tuntap","t":"creates and manages TUN/TAP virtual network interfaces","s":[["TAGLINE","creates and manages TUN/TAP virtual network interfaces"],["TLDR","Show all **TUN/TAP** devices\n\n```ip tuntap```\n\nCreate a **TUN** device\n\n```sudo ip tuntap add dev [tun0] mode tun```\n\nCreate a **TAP** device\n\n```sudo ip tuntap add dev [tap0] mode tap```\n\n**Delete** a TUN/TAP device\n\n```sudo ip tuntap delete dev [tun0] mode tun```\n\nCreate device with specific **owner**\n\n```sudo ip tuntap add dev [tun0] mode tun user [username]```\n\nCreate device with **owner and group**\n\n```sudo ip tuntap add dev [tun0] mode tun user [username] group [groupname]```"],["SYNOPSIS","**ip tuntap** [_command_] [_options_]"],["PARAMETERS","**add** **dev** _NAME_ **mode** _MODE_\n> Create a TUN or TAP device.\n\n**delete** **dev** _NAME_ **mode** _MODE_\n> Remove a TUN or TAP device.\n\n**show** | **list**\n> List existing TUN/TAP devices.\n\n**mode** _tun|tap_\n> Device mode: tun (layer 3, IP packets) or tap (layer 2, Ethernet frames).\n\n**user** _USERNAME_\n> Set device owner by username or UID.\n\n**group** _GROUPNAME_\n> Set device group by name or GID.\n\n**one_queue**\n> Use the old single-queue TUN/TAP driver.\n\n**pi**\n> Enable packet information (prepend a 4-byte header to each packet).\n\n**vnet_hdr**\n> Include a virtio-net header (used with vhost-net).\n\n**multi_queue**\n> Create a multi-queue TUN/TAP device."],["DESCRIPTION","**ip tuntap** creates and manages TUN/TAP virtual network interfaces. TUN devices operate at layer 3 (IP packets) while TAP devices operate at layer 2 (Ethernet frames).\n\nThese virtual interfaces are commonly used by VPN software, virtualization (QEMU/KVM), and network testing tools. Setting user/group ownership allows non-root processes to use the device."],["CAVEATS","Requires root privileges to create devices. Devices persist until deleted or reboot. Applications using the device must be configured to match the device name. Mode must match between add and delete operations."],["HISTORY","TUN/TAP support has been in the Linux kernel since version 2.4. The ip tuntap command provides a modern interface for managing these devices, replacing older methods using openvpn or tunctl."],["SEE ALSO","[ip](/man/ip)(8), [ip-link](/man/ip-link)(8), [openvpn](/man/openvpn)(8)"]],"e":["ip","ip-link","openvpn"],"x":"ip tuntap"},"jj-git-push":{"n":"jj-git-push","t":"pushes changes to Git remotes","s":[["TAGLINE","pushes changes to Git remotes"],["TLDR","**Push current bookmark**\n\n```jj git push```\n\n**Push specific bookmark**\n\n```jj git push --bookmark [name]```\n\n**Push all bookmarks**\n\n```jj git push --all```\n\n**Push to specific remote**\n\n```jj git push --remote [origin]```\n\n**Push a change by creating a bookmark automatically**\n\n```jj git push --change [change_id]```\n\n**Dry run**\n\n```jj git push --dry-run```\n\n**Push tracked bookmarks and delete removed ones**\n\n```jj git push --tracked --deleted```"],["SYNOPSIS","**jj git push** [_options_]"],["PARAMETERS","**-b**, **--bookmark** _NAME_\n> Bookmark to push (can be repeated).\n\n**--all**\n> Push all bookmarks.\n\n**--tracked**\n> Push all tracked bookmarks.\n\n**--deleted**\n> Push bookmarks that have been deleted locally.\n\n**-c**, **--change** _CHANGE_\n> Push this change by creating a bookmark based on the change ID.\n\n**-r**, **--revisions** _REVISIONS_\n> Push bookmarks pointing to the specified revisions.\n\n**--remote** _NAME_\n> Target remote (default: origin).\n\n**--dry-run**\n> Show what would be pushed without actually pushing.\n\n**-h**, **--help**\n> Display help information."],["DESCRIPTION","**jj git push** pushes changes to Git remotes. It translates jj bookmarks to Git branches for pushing. All commits in the range from the remote's current position up to and including the bookmark's target commit are pushed.\n\nThe **--change** flag is convenient for pushing a single change without manually creating and managing a bookmark."],["CAVEATS","Requires a bookmark on the changes to push. The remote must already be configured. Empty revisions produce a warning when bookmarked."],["HISTORY","jj git push is part of **Jujutsu**, enabling collaboration through Git hosting services."],["INSTALL","```pacman: sudo pacman -S jujutsu```\n\n```apk: sudo apk add jujutsu```\n\n```zypper: sudo zypper install jujutsu```\n\n```brew: brew install jujutsu```\n\n```nix: nix profile install nixpkgs#jujutsu```"],["SEE ALSO","[jj](/man/jj)(1), [jj-git](/man/jj-git)(1), [git-push](/man/git-push)(1)"]],"e":["jj","jj-git","git-push"],"x":"jj git push"},"khal":{"n":"khal","t":"terminal calendar application","s":[["TAGLINE","terminal calendar application"],["TLDR","**Show calendar**\n\n```khal```\n\n**List events**\n\n```khal list```\n\n**Add event** (START END SUMMARY)\n\n```khal new [2024-01-15] [10:00] [11:00] [Meeting]```\n\n**Add event interactively**\n\n```khal new -i```\n\n**Search events**\n\n```khal search [keyword]```\n\n**Interactive TUI**\n\n```khal interactive```\n\n**Import ICS file**\n\n```khal import [event.ics]```\n\n**Show events for a date range**\n\n```khal list [2024-01-15] [7d]```"],["SYNOPSIS","**khal** [_options_] _command_"],["PARAMETERS","**list** [_START_] [_END_|_DELTA_]\n> List events for a date range.\n\n**at** _DATETIME_\n> Show events at a specific datetime.\n\n**calendar** [_START_] [_END_]\n> Display a calendar (cal-like) view with events.\n\n**new** _START_ [_END_|_DELTA_] [_TIMEZONE_] _SUMMARY_ [:: _DESCRIPTION_]\n> Create a new event. Use **-i** for interactive entry.\n\n**search** _STRING_\n> Search events matching the given string.\n\n**edit** _STRING_\n> Interactively edit or delete matching events.\n\n**import** _FILE_\n> Import an ICS file.\n\n**printcalendars**\n> List configured calendars.\n\n**printformats**\n> Display date/time format samples.\n\n**configure**\n> Run the initial configuration wizard.\n\n**-a** _CALENDAR_ / **-d** _CALENDAR_\n> Include (**-a**) or exclude (**-d**) a calendar.\n\n**--help**\n> Display help information."],["DESCRIPTION","**khal** is a terminal calendar application. It reads and writes CalDAV calendars through vdirsyncer.\n\nThe tool provides both command-line and interactive interfaces. It supports multiple calendars and recurring events."],["CAVEATS","Requires vdirsyncer for sync. Python-based. Configuration file needed."],["HISTORY","khal was created as a terminal-based calendar client compatible with CalDAV through the vdirsyncer ecosystem."],["INSTALL","```dnf: sudo dnf install khal```\n\n```pacman: sudo pacman -S khal```\n\n```apk: sudo apk add khal```\n\n```brew: brew install khal```\n\n```nix: nix profile install nixpkgs#khal```"],["SEE ALSO","[vdirsyncer](/man/vdirsyncer)(1), [todoman](/man/todoman)(1), [calcurse](/man/calcurse)(1)"]],"e":["vdirsyncer","todoman","calcurse"],"x":"khal"},"laravel-zero":{"n":"laravel-zero","t":"micro-framework for console applications built on Laravel components","s":[["TAGLINE","micro-framework for console applications built on Laravel components"],["TLDR","**Create new Laravel Zero application**\n\n```laravel-zero new [app-name]```\n\n**Build standalone PHAR**\n\n```php [app-name] app:build```\n\n**Install optional component**\n\n```php [app-name] app:install```"],["SYNOPSIS","**laravel-zero** _command_ [_options_]"],["PARAMETERS","**new** _name_\n> Create a new Laravel Zero application.\n\n**--force**\n> Overwrite existing directory."],["DESCRIPTION","**laravel-zero** is a micro-framework for console applications built on Laravel components. It creates standalone command-line applications that can be compiled into single PHAR files. Ideal for building CLI tools with Laravel's elegant syntax."],["INSTALL","```nix: nix profile install nixpkgs#laravel```"],["SEE ALSO","[laravel](/man/laravel)(1), [composer](/man/composer)(1)"]],"e":["laravel","composer"],"x":"laravel-zero new [app-name]"},"lastlog":{"n":"lastlog","t":"Report the most recent login of all users or a specified user","s":[["TAGLINE","Report the most recent login of all users or a specified user"],["TLDR","Display most recent **login of all users**\n\n```lastlog```\n\nDisplay lastlog for a **specific user**\n\n```lastlog --user [username]```\n\nDisplay records **older than** N days\n\n```lastlog --before [7]```\n\nDisplay records **more recent than** N days\n\n```lastlog --time [3]```"],["SYNOPSIS","**lastlog** [_options_]"],["PARAMETERS","**-u**, **--user** _LOGIN|RANGE_\n> Show lastlog record for specified user(s). Can be a login name, numeric UID, or a UID range (UID_MIN-UID_MAX)\n\n**-b**, **--before** _DAYS_\n> Show records older than DAYS\n\n**-t**, **--time** _DAYS_\n> Show records more recent than DAYS\n\n**-C**, **--clear**\n> Clear lastlog record for a user (requires -u)\n\n**-S**, **--set**\n> Set lastlog record for a user to the current time (requires -u)\n\n**-R**, **--root** _CHROOT\\_DIR_\n> Apply changes in the CHROOT_DIR directory and use its configuration files\n\n**-h**, **--help**\n> Display help message and exit"],["DESCRIPTION","**lastlog** reports the most recent login of all users or a specified user by reading /var/log/lastlog. Unlike last, it shows only one record per user representing their most recent login.\n\nThe lastlog file is a sparse file indexed by UID, so it can appear large but doesn't consume much disk space. Users who have never logged in show \"Never logged in\"."],["CAVEATS","The lastlog file is indexed by UID; very high UIDs can make the file appear large. Some systems may not maintain lastlog. System accounts typically show as never logged in."],["HISTORY","lastlog has been part of Unix-like systems for decades, providing a quick way to audit which users have logged in recently and identify dormant accounts."],["INSTALL","```apk: sudo apk add shadow-login```"],["SEE ALSO","[last](/man/last)(1), [lastb](/man/lastb)(1), [faillog](/man/faillog)(8), [who](/man/who)(1), [login](/man/login)(1)"]],"e":["last","lastb","faillog","who","login"],"x":"lastlog"},"ld.so":{"n":"ld.so","t":"dynamic linker/loader","s":[["TAGLINE","dynamic linker/loader"],["TLDR","**Run with library path**\n\n```LD_LIBRARY_PATH=[/path/to/libs] [program]```\n\n**Preload library**\n\n```LD_PRELOAD=[/path/to/lib.so] [program]```\n\n**Debug loading**\n\n```LD_DEBUG=libs [program]```\n\n**Show search paths**\n\n```ldconfig -p```\n\n**Update library cache**\n\n```sudo ldconfig```"],["SYNOPSIS","**ld.so** / **ld-linux.so** is the dynamic linker"],["PARAMETERS","**LD_LIBRARY_PATH**\n> Additional library directories.\n\n**LD_PRELOAD**\n> Libraries to load first.\n\n**LD_DEBUG**\n> Debug options (libs, reloc, files).\n\n**LD_TRACE_LOADED_OBJECTS**\n> List dependencies (like ldd)."],["DESCRIPTION","**ld.so** is the dynamic linker/loader. It loads shared libraries needed by programs at runtime.\n\nThe loader resolves symbols and handles library dependencies. Environment variables control its behavior."],["CAVEATS","Not directly invoked usually. Security implications of LD_PRELOAD. Ignored for setuid binaries."],["HISTORY","ld.so is part of the **glibc** package, implementing dynamic linking for Linux executables."],["INSTALL","```apt: sudo apt install libc-bin```"],["SEE ALSO","[ldd](/man/ldd)(1), [ldconfig](/man/ldconfig)(8), [ld](/man/ld)(1)"]],"e":["ldd","ldconfig","ld"],"x":"LD_LIBRARY_PATH=[/path/to/libs] [program]"},"ledctl":{"n":"ledctl","t":"controls enclosure LEDs for Intel RAID controllers and storage systems","s":[["TAGLINE","controls enclosure LEDs for Intel RAID controllers and storage systems"],["TLDR","Turn on **Locate** LED for devices\n\n```sudo ledctl locate=[/dev/sda,/dev/sdb]```\n\nTurn **off** Locate LED\n\n```sudo ledctl locate_off=[/dev/sda,/dev/sdb]```\n\nTurn off **Status** and **Failure** LEDs\n\n```sudo ledctl off=[/dev/sda,/dev/sdb]```\n\nTurn off **all** LEDs (normal state)\n\n```sudo ledctl normal=[/dev/sda,/dev/sdb]```"],["SYNOPSIS","**ledctl** [_options_] _pattern_=_device_list_ ..."],["PATTERNS","**locate**=_DEVICES_\n> Turn on the Locate LED (blue blinking)\n\n**locate_off**=_DEVICES_\n> Turn off the Locate LED\n\n**off**=_DEVICES_\n> Turn off Status and Failure LEDs\n\n**normal**=_DEVICES_\n> Turn off all LEDs, return to normal state\n\n**rebuild**=_DEVICES_\n> Set rebuild pattern (used during RAID rebuild)\n\n**failure**=_DEVICES_, **disk_failed**=_DEVICES_\n> Set failure pattern (solid red)\n\n**ica**=_DEVICES_, **degraded**=_DEVICES_\n> In a critical array.\n\n**ifa**=_DEVICES_, **failed_array**=_DEVICES_\n> In a failed array.\n\n**hotspare**=_DEVICES_\n> Mark as hot spare.\n\n**pfa**=_DEVICES_\n> Predicted failure analysis.\n\nDevices may be given either comma-separated (e.g. **locate=/dev/sda,/dev/sdb**) or in braces (e.g. **locate={ /dev/sda /dev/sdb }**)."],["OPTIONS","**-L**, **--list-controllers**\n> List detected controllers and their type.\n\n**-x**, **--listed-only**\n> Apply changes only to the listed devices; leave others unchanged.\n\n**-l** _FILE_, **--log**=_FILE_\n> Write log output to the given file.\n\n**--quiet**, **--error**, **--warning**, **--info**, **--debug**, **--all**\n> Set logging verbosity.\n\n**-h**, **--help**\n> Show help.\n\n**-v**, **--version**\n> Show version."],["DESCRIPTION","**ledctl** controls enclosure LEDs for Intel RAID controllers and storage systems. It allows physical identification of disks by blinking the Locate LED, and manages status indicators for disk states.\n\nThis is particularly useful in data centers with many disks, allowing administrators to physically locate specific drives that need attention, replacement, or are part of a rebuild operation."],["CAVEATS","Requires Intel hardware with LED support (AHCI, ISCI, VMD enclosures). Needs root privileges. Not all enclosures support all LED patterns."],["HISTORY","ledctl is part of the ledmon package, developed by Intel for managing storage enclosure LEDs on their RAID and storage hardware platforms."],["INSTALL","```apt: sudo apt install ledmon```\n\n```dnf: sudo dnf install ledmon```\n\n```zypper: sudo zypper install ledmon```\n\n```nix: nix profile install nixpkgs#ledmon```"],["SEE ALSO","[mdadm](/man/mdadm)(8), [smartctl](/man/smartctl)(8)"]],"e":["mdadm","smartctl"],"x":"sudo ledctl locate=[/dev/sda,/dev/sdb]"},"loadkeys":{"n":"loadkeys","t":"load keyboard translation tables for the console","s":[["TAGLINE","load keyboard translation tables for the console"],["TLDR","Load **keyboard layout**\n\n```sudo loadkeys [en|de|dvorak]```\n\nLoad **default** keymap\n\n```sudo loadkeys --default```\n\nLoad keymap for **specific console**\n\n```sudo loadkeys -C /dev/ttyN [layout]```\n\nLoad from **file**\n\n```sudo loadkeys -C /dev/ttyN [path/to/keymap]```\n\nCreate **binary** keymap\n\n```loadkeys --bkeymap > [output.bmap]```\n\n**Parse** keymap without loading\n\n```loadkeys --parse [keymap]```"],["SYNOPSIS","**loadkeys** [_options_] [_keymap_...]"],["DESCRIPTION","**loadkeys** reads keymap files and loads the keyboard translation tables into the kernel, changing the keyboard layout for virtual consoles."],["PARAMETERS","**-d, --default**\n> Load default keymap\n\n**-C, --console DEVICE**\n> Target specific console device\n\n**-m, --mktable**\n> Output kernel source table\n\n**-b, --bkeymap**\n> Output binary keymap\n\n**-p, --parse**\n> Parse keymap without loading\n\n**-q, --quiet**\n> Suppress all normal output\n\n**-u, --unicode**\n> Force Unicode mode\n\n**-a, --ascii**\n> Force conversion to ASCII\n\n**-s, --clearstrings**\n> Clear kernel string table\n\n**-c, --clearcompose**\n> Clear kernel accent table\n\n**-t, --tkeymap** _[shape]_\n> Output a text keymap (optional shape: 2, 4, 8, 16)\n\n**-V, --version**\n> Display version number and exit"],["CAVEATS","Only affects Linux virtual consoles, not X11 or Wayland. Changes apply to all virtual consoles simultaneously and persist until reboot. Anyone with read access to /dev/console can run loadkeys."],["INSTALL","```apt: sudo apt install kbd```\n\n```dnf: sudo dnf install kbd```\n\n```pacman: sudo pacman -S kbd```\n\n```apk: sudo apk add kbd```\n\n```zypper: sudo zypper install kbd```\n\n```nix: nix profile install nixpkgs#kbd```"],["SEE ALSO","[dumpkeys](/man/dumpkeys)(1), [showkey](/man/showkey)(1), [setxkbmap](/man/setxkbmap)(1), [localectl](/man/localectl)(1)"]],"e":["dumpkeys","showkey","setxkbmap","localectl"],"x":"sudo loadkeys [en|de|dvorak]"},"locale-gen":{"n":"locale-gen","t":"compiles locale definitions from `/etc/locale","s":[["TAGLINE","compiles locale definitions from `/etc/locale"],["TLDR","**Generate** locales from /etc/locale.gen\n\n```sudo locale-gen```\n\nGenerate **keeping existing** locales\n\n```sudo locale-gen --keep-existing```"],["SYNOPSIS","**locale-gen** [_options_]"],["DESCRIPTION","**locale-gen** compiles locale definitions from `/etc/locale.gen` into binary locale data. Uncomment desired locales in that file before running this command."],["PARAMETERS","**--keep-existing**\n> Do not delete existing locale data\n\n**--purge**\n> Remove existing locales before generating"],["CAVEATS","Requires root privileges. Edit `/etc/locale.gen` to enable locales before generating. On Debian-based systems, use `dpkg-reconfigure locales` for interactive configuration."],["SEE ALSO","[locale](/man/locale)(1), [localedef](/man/localedef)(1), [localectl](/man/localectl)(1)"]],"e":["locale","localedef","localectl"],"x":"sudo locale-gen"},"lzcat":{"n":"lzcat","t":"decompresses lzip files to standard output","s":[["TAGLINE","decompresses lzip files to standard output"],["TLDR","**Decompress to stdout**\n\n```lzcat [file.lz]```\n\n**Decompress multiple files**\n\n```lzcat [file1.lz] [file2.lz]```\n\n**Decompress and pipe**\n\n```lzcat [archive.tar.lz] | tar -xf -```\n\n**Verbose output**\n\n```lzcat -v [file.lz]```"],["SYNOPSIS","**lzcat** [_options_] [_files_...]"],["PARAMETERS","**-v**, **--verbose**\n> Verbose output.\n\n**-q**, **--quiet**\n> Suppress warnings.\n\n**-t**, **--test**\n> Test integrity.\n\n**--help**\n> Show help.\n\n**--version**\n> Show version."],["DESCRIPTION","**lzcat** decompresses lzip files to standard output. It's equivalent to \"lzip -dc\" and functions like zcat for gzip files.\n\nlzcat is useful for viewing compressed files or piping decompressed data to other programs without creating intermediate files."],["EXAMPLE USAGE","```bash"],["View compressed log","lzcat system.log.lz | less"],["Extract tar archive","lzcat archive.tar.lz | tar xvf -"],["Compare compressed files","diff <(lzcat file1.lz) <(lzcat file2.lz)\n```"],["CAVEATS","Only handles lzip format. For other formats use zcat, xzcat, bzcat. Cannot compress; use lzip for that."],["HISTORY","lzcat is part of the **lzip** package created by **Antonio Diaz Diaz** as a cleaner implementation of LZMA compression."],["INSTALL","```dnf: sudo dnf install xz```\n\n```pacman: sudo pacman -S xz```\n\n```apk: sudo apk add xz```\n\n```zypper: sudo zypper install xz```\n\n```brew: brew install xz```\n\n```nix: nix profile install nixpkgs#xz```"],["SEE ALSO","[lzip](/man/lzip)(1), [zcat](/man/zcat)(1), [xzcat](/man/xzcat)(1), [bzcat](/man/bzcat)(1)"]],"e":["lzip","zcat","xzcat","bzcat"],"x":"lzcat [file.lz]"},"mke2fs":{"n":"mke2fs","t":"creates ext2, ext3, or ext4 filesystems on a device or partition","s":[["TAGLINE","creates ext2, ext3, or ext4 filesystems on a device or partition"],["TLDR","Create an **ext2 filesystem** on a partition\n\n```sudo mke2fs -t ext2 [/dev/sdXY]```\n\nCreate an **ext3 filesystem** on a partition\n\n```sudo mke2fs -t ext3 [/dev/sdXY]```\n\nCreate an **ext4 filesystem** on a partition\n\n```sudo mke2fs -t ext4 [/dev/sdXY]```\n\nCreate ext4 with a **volume label**\n\n```sudo mke2fs -t ext4 -L [my_label] [/dev/sdXY]```\n\n**Dry-run** to preview parameters without writing\n\n```sudo mke2fs -n -t ext4 [/dev/sdXY]```\n\nCreate ext4 with **no reserved blocks** for root\n\n```sudo mke2fs -t ext4 -m 0 [/dev/sdXY]```"],["SYNOPSIS","**mke2fs** [_options_] _device_"],["PARAMETERS","**-t _type_**\n> Filesystem type: ext2, ext3, or ext4\n\n**-L _label_**\n> Set the volume label\n\n**-U _uuid_**\n> Set the filesystem UUID\n\n**-b _size_**\n> Block size in bytes (1024, 2048, or 4096)\n\n**-i _bytes-per-inode_**\n> Bytes per inode ratio (affects number of inodes created)\n\n**-N _number_**\n> Create exactly this many inodes\n\n**-m _percentage_**\n> Reserved blocks percentage for root (default 5%)\n\n**-O _features_**\n> Enable/disable filesystem features\n\n**-c**\n> Check device for bad blocks before creating filesystem (use **-cc** for a read-write test).\n\n**-j**\n> Create the filesystem with an ext3 journal.\n\n**-E** _extended-options_\n> Set extended options (comma-separated), e.g. `discard`, `lazy_itable_init`, `stride`, `stripe_width`.\n\n**-T** _usage-type_\n> Usage type (default, small, big, huge, news, largefile, largefile4) to tune defaults.\n\n**-n**\n> Dry run: show what would be done without actually creating the filesystem (useful to locate backup superblocks).\n\n**-F**\n> Force creation even if the target doesn't look like a partition or is in use. Specify twice to override all safety checks.\n\n**-q**\n> Quiet mode (suppress informational output, useful in scripts).\n\n**-v**\n> Verbose output."],["DESCRIPTION","**mke2fs** creates ext2, ext3, or ext4 filesystems on a device or partition. It initializes the superblock, block groups, inode tables, and other filesystem structures.\n\nThe ext4 filesystem is the current standard for Linux, offering journaling, extents, and improved performance over ext2/ext3. The **-t** option selects the filesystem type and automatically enables appropriate features.\n\nCommonly invoked as **mkfs.ext2**, **mkfs.ext3**, or **mkfs.ext4**, which are symlinks to mke2fs with preset filesystem types. Defaults are read from **/etc/mke2fs.conf**."],["CAVEATS","**Destroys all data** on the target device. Always verify the correct device path before running. The reserved block percentage (default 5%) can be reduced for non-root partitions using **-m 0** or later with **tune2fs**. Creating a filesystem on a mounted partition will corrupt it."],["HISTORY","**mke2fs** was originally written by **Remy Card** as part of the **e2fsprogs** suite for the ext2 filesystem introduced in Linux **1.0 (1993)**. It was later extended to support ext3 (journaling, **2001**) and ext4 (extents, large volumes, **2008**). Current maintenance is led by **Theodore Ts'o**."],["INSTALL","```apt: sudo apt install e2fsprogs```\n\n```dnf: sudo dnf install e2fsprogs```\n\n```pacman: sudo pacman -S e2fsprogs```\n\n```apk: sudo apk add e2fsprogs```\n\n```zypper: sudo zypper install e2fsprogs```\n\n```brew: brew install e2fsprogs```\n\n```nix: nix profile install nixpkgs#e2fsprogs```"],["SEE ALSO","[mkfs](/man/mkfs)(8), [mkfs.ext4](/man/mkfs.ext4)(8), [tune2fs](/man/tune2fs)(8), [e2fsck](/man/e2fsck)(8), [dumpe2fs](/man/dumpe2fs)(8)"]],"e":["mkfs","mkfs.ext4","tune2fs","e2fsck","dumpe2fs"],"x":"sudo mke2fs -t ext2 [/dev/sdXY]"},"mkfs.erofs":{"n":"mkfs.erofs","t":"creates EROFS images from a directory tree","s":[["TAGLINE","creates EROFS images from a directory tree"],["TLDR","Create an **EROFS filesystem** based on the root directory\n\n```mkfs.erofs [image.erofs] [root/]```\n\nCreate an EROFS image with a **specific UUID**\n\n```mkfs.erofs -U [UUID] [image.erofs] [root/]```\n\nCreate a **compressed** EROFS image\n\n```mkfs.erofs -zlz4hc [image.erofs] [root/]```\n\nCreate an EROFS image where **all files are owned by root**\n\n```mkfs.erofs --all-root [image.erofs] [root/]```"],["SYNOPSIS","**mkfs.erofs** [_options_] _image_ _source_"],["PARAMETERS","**-U _uuid_**\n> Set a specific filesystem UUID\n\n**-z _algorithm_**\n> Enable compression (lz4, lz4hc, lzma, deflate, zstd)\n\n**--all-root**\n> Make all files owned by root (UID/GID 0)\n\n**-L _label_**\n> Set the volume label\n\n**-T _timestamp_**\n> Use a fixed timestamp for all files (reproducible builds)\n\n**--exclude-path _pattern_**\n> Exclude files matching the pattern\n\n**-E _options_**\n> Extended options for fine-tuning"],["DESCRIPTION","**mkfs.erofs** creates EROFS (Enhanced Read-Only File System) images from a directory tree. EROFS is a lightweight read-only filesystem designed for performance-sensitive scenarios like Android system partitions, container images, and embedded systems.\n\nThe filesystem supports transparent compression, enabling significant space savings while maintaining fast random read access. Unlike SquashFS, EROFS provides better random access performance due to its fixed-size block design.\n\nOutput is a file containing the complete filesystem image, suitable for mounting via loopback or writing to a read-only partition."],["CAVEATS","EROFS is read-only; the source directory is not modified. Large directories may take time to process with compression enabled. Kernel support (CONFIG_EROFS_FS) is required to mount images. Best suited for scenarios where data doesn't change after creation."],["HISTORY","EROFS was developed by Huawei and merged into the Linux kernel in version 4.19 (2018). It has since been adopted by Android for system partitions and various embedded Linux distributions."],["INSTALL","```apt: sudo apt install erofs-utils```\n\n```dnf: sudo dnf install erofs-utils```\n\n```pacman: sudo pacman -S erofs-utils```\n\n```apk: sudo apk add erofs-utils```\n\n```zypper: sudo zypper install erofs-utils```\n\n```brew: brew install erofs-utils```\n\n```nix: nix profile install nixpkgs#erofs-utils```"],["SEE ALSO","[mksquashfs](/man/mksquashfs)(1), [mount](/man/mount)(8), [losetup](/man/losetup)(8)"]],"e":["mksquashfs","mount","losetup"],"x":"mkfs.erofs [image.erofs] [root/]"},"montage":{"n":"montage","t":"creates composite images by joining multiple images together","s":[["TAGLINE","creates composite images by joining multiple images together"],["TLDR","**Create image grid**\n\n```montage [*.jpg] [output.jpg]```\n\n**Specify tile layout**\n\n```montage [*.jpg] -tile [3x2] [output.jpg]```\n\n**Set thumbnail size**\n\n```montage [*.jpg] -geometry [200x200+5+5] [output.jpg]```\n\n**Add labels**\n\n```montage [*.jpg] -label \"%f\" [output.jpg]```\n\n**Set background color**\n\n```montage [*.jpg] -background [white] [output.jpg]```\n\n**Add border**\n\n```montage [*.jpg] -border [5] -bordercolor [gray] [output.jpg]```"],["SYNOPSIS","**montage** [_options_] _input-files_ _output-file_"],["DESCRIPTION","**montage** creates composite images by joining multiple images together. Part of ImageMagick, it arranges images in a grid, optionally with labels, borders, and shadows.\n\nThe tool is useful for creating contact sheets, thumbnails galleries, and image collages."],["PARAMETERS","**-tile** _cols_x_rows_\n> Grid layout.\n\n**-geometry** _WxH+x+y_\n> Thumbnail size and spacing.\n\n**-label** _string_\n> Label for each image.\n\n**-title** _string_\n> Title for montage.\n\n**-background** _color_\n> Background color.\n\n**-border** _geometry_\n> Border size.\n\n**-bordercolor** _color_\n> Border color.\n\n**-shadow**\n> Add shadow.\n\n**-frame** _geometry_\n> Add frame.\n\n**-mode** _type_\n> Frame style."],["CAVEATS","Many images can use lots of memory. Label formatting has special syntax. Output format from extension. Quality settings affect size."],["HISTORY","**montage** is part of **ImageMagick**, created by **John Cristy** in **1987**. It provides contact sheet functionality that was common in photography workflows."],["INSTALL","```pacman: sudo pacman -S imagemagick```\n\n```apk: sudo apk add imagemagick```\n\n```brew: brew install imagemagick```\n\n```nix: nix profile install nixpkgs#imagemagick```"],["SEE ALSO","[convert](/man/convert)(1), [magick](/man/magick)(1), [identify](/man/identify)(1), [display](/man/display)(1)"]],"e":["convert","magick","identify","display"],"x":"montage [*.jpg] [output.jpg]"},"msgmerge":{"n":"msgmerge","t":"Merge two Uniforum style PO files","s":[["TAGLINE","Merge two Uniforum style PO files"],["TLDR","**Merge translations with template**\n\n```msgmerge [old.po] [new.pot] -o [merged.po]```\n\n**Update PO file in place**\n\n```msgmerge -U [existing.po] [template.pot]```\n\n**Merge without fuzzy matching**\n\n```msgmerge -N [old.po] [new.pot] -o [merged.po]```\n\n**Merge keeping previous msgids** on fuzzy matches\n\n```msgmerge --previous [old.po] [new.pot] -o [merged.po]```\n\n**Backup before update**\n\n```msgmerge -U --backup=numbered [file.po] [template.pot]```\n\n**Use a compendium** of translations\n\n```msgmerge -C [compendium.po] [old.po] [new.pot] -o [merged.po]```\n\n**Quiet mode**\n\n```msgmerge -q [old.po] [new.pot] -o [merged.po]```"],["SYNOPSIS","**msgmerge** [_options_] _def.po_ _ref.pot_"],["PARAMETERS","_DEF.PO_\n> Existing translations file.\n\n_REF.POT_\n> Reference template file.\n\n**-o** _FILE_\n> Output file.\n\n**-U**, **--update**\n> Update def.po in place.\n\n**-N**, **--no-fuzzy-matching**\n> Do not use fuzzy matching. Speeds up operation considerably.\n\n**--previous**\n> Keep previous msgids of translated messages when adding fuzzy markers.\n\n**-C** _FILE_, **--compendium**=_FILE_\n> Additional library of message translations. May be specified more than once.\n\n**--backup**=_CONTROL_\n> Backup control for -U: **none**, **numbered**, **existing**, or **simple**.\n\n**--suffix**=_SUFFIX_\n> Override the usual backup suffix.\n\n**-q**, **--quiet**\n> Suppress progress indicators.\n\n**-v**, **--verbose**\n> Increase verbosity level.\n\n**--no-wrap**\n> Do not break long message lines.\n\n**-s**, **--sort-output**\n> Generate sorted output.\n\n**-F**, **--sort-by-file**\n> Sort output by file location."],["DESCRIPTION","**msgmerge** merges two Uniforum style .po files together. It is part of **GNU gettext**.\n\nThe _def.po_ file is an existing PO file with translations that will be taken over to the newly created file as long as they still match. The _ref.pot_ file is the last created PO template file with up-to-date source references. Comments from _def.po_ are preserved. When an exact match is not found, fuzzy matching is used to produce better results (unless **-N** is specified)."],["CAVEATS","Part of **gettext-tools** package. Fuzzy matches are marked with a **#, fuzzy** comment and should be reviewed. The **-U** flag modifies the input file in place; use **--backup** to create backups before updating."],["HISTORY","msgmerge is part of **GNU gettext**, essential for maintaining translation files across software updates."],["INSTALL","```apt: sudo apt install gettext```\n\n```dnf: sudo dnf install gettext```\n\n```pacman: sudo pacman -S gettext```\n\n```apk: sudo apk add gettext```\n\n```brew: brew install gettext```\n\n```nix: nix profile install nixpkgs#gettext```"],["SEE ALSO","[msginit](/man/msginit)(1), [msgfmt](/man/msgfmt)(1), [msgcat](/man/msgcat)(1), [xgettext](/man/xgettext)(1)"]],"e":["msginit","msgfmt","msgcat","xgettext"],"x":"msgmerge [old.po] [new.pot] -o [merged.po]"},"neo":{"n":"neo","t":"Simulate the digital rain from \"The Matrix\"","s":[["TAGLINE","Simulate the digital rain from \"The Matrix\""],["TLDR","**Run the default Matrix rain animation**\n\n```neo```\n\n**Run with a specific color**\n\n```neo -c [green|red|blue|cyan|gold|rainbow|purple|pink|vaporwave]```\n\n**Run with a specific character set**\n\n```neo --charset [katakana|ascii|cyrillic|greek|braille|runic|binary|hex]```\n\n**Adjust scrolling speed and droplet density**\n\n```neo -S [12] -d [1.5]```\n\n**Display a centered message** within the rain\n\n```neo -m \"[message]\"```\n\n**Run with async scrolling and 256-color mode**\n\n```neo -S 12 -a --colormode=256```"],["SYNOPSIS","**neo** [_options_]"],["PARAMETERS","**-c, --color** _color_\n> Set foreground text color. Available: **green**, **green2**, **green3**, **yellow**, **orange**, **red**, **blue**, **cyan**, **gold**, **rainbow**, **purple**, **pink**, **pink2**, **vaporwave**, **gray**\n\n**-S, --speed** _num_\n> Set the scrolling speed\n\n**-d, --density** _num_\n> Control how many droplets appear onscreen (default: 1.0)\n\n**--charset** _name_\n> Character set to use: **ascii**, **extended**, **english**, **dec**, **digits**, **punc**, **bin**, **hex**, **katakana**, **greek**, **cyrillic**, **arabic**, **hebrew**, **devanagari**, **braille**, **runic**\n\n**--chars** _range_\n> Specify custom Unicode character ranges via hex codes\n\n**--colormode** _mode_\n> Color assignment method: **0** = random (default), **1** = gradient\n\n**-m, --message** _text_\n> Display centered ASCII text revealed as characters stream past\n\n**-a, --async**\n> Enable faster asynchronous scrolling\n\n**-F, --fullwidth**\n> Use fullwidth character rendering\n\n**--noglitch**\n> Disable glitch effects\n\n**-h, --help**\n> Display help information"],["DESCRIPTION","**neo** recreates the iconic digital rain effect from \"The Matrix\" films. Streams of random characters endlessly scroll down the terminal screen, closely mimicking the movie scene where Cypher explains the code to Neo. It imitates finer details such as the half-width katakana characters, uneven colors, glitching, and flickering.\n\nThe animation can be customized with different character sets, color palettes, scrolling speeds, and density settings. Interactive controls allow real-time adjustments using arrow keys for speed and glitch intensity, and number keys for color switching."],["CAVEATS","Performance depends on terminal emulator capabilities. A GPU-accelerated terminal significantly improves rendering. Unicode character sets require appropriate font support. Does not support native Windows; use WSL instead."],["HISTORY","**neo** was created by **st3w** as a modern rewrite of **CMatrix** (originally by Chris Allegretta). It adds **32-bit color** and full **Unicode support**, going beyond the original CMatrix capabilities. The project is available on Linux, macOS, and FreeBSD."],["INSTALL","```apt: sudo apt install neo-cli```\n\n```dnf: sudo dnf install neo```\n\n```nix: nix profile install nixpkgs#neo```"],["SEE ALSO","[cmatrix](/man/cmatrix)(1), [sl](/man/sl)(1)"]],"e":["cmatrix","sl"],"x":"neo"},"npm-rebuild":{"n":"npm-rebuild","t":"rebuilds native addon modules for the current Node","s":[["TAGLINE","rebuilds native addon modules for the current Node"],["TLDR","**Rebuild all native modules**\n\n```npm rebuild```\n\n**Rebuild specific package**\n\n```npm rebuild [package]```\n\n**Rebuild for different Node version**\n\n```npm rebuild --target=[18.0.0]```\n\n**Rebuild globally installed packages**\n\n```npm rebuild -g```"],["SYNOPSIS","**npm rebuild** [_options_] [_packages_...]"],["PARAMETERS","**-g**, **--global**\n> Rebuild global packages.\n\n**--ignore-scripts**\n> Don't run scripts.\n\n**--foreground-scripts**\n> Run scripts in foreground."],["DESCRIPTION","**npm rebuild** rebuilds native addon modules for the current Node.js version. This is necessary after upgrading Node.js or when native modules fail to load.\n\nThe command runs node-gyp rebuild for each package with native code."],["COMMON USE CASES","```bash"],["After Node.js upgrade","npm rebuild"],["Fix broken native module","npm rebuild bcrypt"],["Rebuild with verbose output","npm rebuild --verbose\n```"],["NATIVE MODULES","```\nbcrypt, node-sass, sqlite3\nsharp, canvas, grpc\nleveldown, node-rdkafka\n```"],["CAVEATS","Requires build tools (Python, C++ compiler). May take significant time. Some modules need specific Node versions."],["HISTORY","npm rebuild has been part of npm since early versions, essential for maintaining native addon compatibility across Node.js versions."],["INSTALL","```pacman: sudo pacman -S npm```\n\n```apk: sudo apk add npm```\n\n```brew: brew install npm```"],["SEE ALSO","[npm](/man/npm)(1), [npm-install](/man/npm-install)(1), [node-gyp](/man/node-gyp)(1)"]],"e":["npm","npm-install","node-gyp"],"x":"npm rebuild"},"osm2pgsql":{"n":"osm2pgsql","t":"imports OpenStreetMap data into a PostgreSQL/PostGIS database","s":[["TAGLINE","imports OpenStreetMap data into a PostgreSQL/PostGIS database"],["TLDR","**Import OSM data**\n\n```osm2pgsql -d [database] [file.osm.pbf]```\n\n**Import with slim mode**\n\n```osm2pgsql -d [database] -s [file.osm.pbf]```\n\n**Import with custom style**\n\n```osm2pgsql -d [database] -S [style.lua] [file.osm.pbf]```\n\n**Append new data**\n\n```osm2pgsql -d [database] -a [updates.osc]```\n\n**Set number of processes**\n\n```osm2pgsql -d [database] -j [4] [file.osm.pbf]```"],["SYNOPSIS","**osm2pgsql** [_options_] _file_..."],["PARAMETERS","**-d** _database_\n> Database name.\n\n**-s**, **--slim**\n> Store temp data in database.\n\n**-S** _file_\n> Style file.\n\n**-a**, **--append**\n> Append mode.\n\n**-c**, **--create**\n> Create tables (default).\n\n**-j** _num_\n> Number of processes.\n\n**-C** _MB_\n> Cache size.\n\n**-H** _host_\n> Database host.\n\n**-U** _user_\n> Database user."],["DESCRIPTION","**osm2pgsql** imports OpenStreetMap data into a PostgreSQL/PostGIS database. It's commonly used to create databases for rendering map tiles with Mapnik.\n\nThe tool can handle full planet imports and incremental updates."],["TYPICAL WORKFLOW","```bash"],["Create database","createdb gis\npsql -d gis -c \"CREATE EXTENSION postgis;\""],["Import data","osm2pgsql -d gis -s -C 2000 planet.osm.pbf\n```"],["CAVEATS","Requires PostGIS. Planet imports need substantial resources. Use slim mode for updates."],["HISTORY","osm2pgsql was developed for the **OpenStreetMap** project to enable map rendering from OSM data using PostgreSQL."],["INSTALL","```apt: sudo apt install osm2pgsql```\n\n```dnf: sudo dnf install osm2pgsql```\n\n```pacman: sudo pacman -S osm2pgsql```\n\n```brew: brew install osm2pgsql```\n\n```nix: nix profile install nixpkgs#osm2pgsql```"]],"e":[],"x":"osm2pgsql -d [database] [file.osm.pbf]"},"pacman-r":{"n":"pacman-r","t":"short form of **pacman --remove**, which removes packages from the system","s":[["TAGLINE","short form of **pacman --remove**, which removes packages from the system"],["TLDR","**Remove a package**\n\n```sudo pacman -R [package]```\n\n**Remove a package and its unneeded dependencies**\n\n```sudo pacman -Rs [package]```\n\n**Remove a package, its dependencies, and configuration files**\n\n```sudo pacman -Rns [package]```\n\n**Remove a package without checking dependencies** (dangerous)\n\n```sudo pacman -Rdd [package]```\n\n**List packages that would be removed** (dry run)\n\n```pacman -Rsp [package]```\n\nView documentation for the original command\n\n```tldr pacman remove```"],["SYNOPSIS","**pacman -R** [_options_] _package(s)_"],["PARAMETERS","**-c**, **--cascade**\n> Remove all target packages and packages that depend on them.\n\n**-n**, **--nosave**\n> Remove configuration files that would otherwise be saved as .pacsave files.\n\n**-s**, **--recursive**\n> Remove dependencies not required by other packages.\n\n**-u**, **--unneeded**\n> Remove targets only if they are not required by any other packages.\n\n**-p**, **--print**\n> Print targets instead of performing the operation.\n\n**-d**, **--nodeps**\n> Skip dependency checks. Pass twice (-dd) to also skip required-by checks."],["DESCRIPTION","**pacman -R** is the short form of **pacman --remove**, which removes packages from the system. It handles dependency checking and can optionally remove configuration files and orphaned dependencies."],["CAVEATS","Using **-Rdd** bypasses all dependency checks and can break the system. The **-Rns** combination is the cleanest removal, also removing backup configuration files and orphaned dependencies."],["INSTALL","```apt: sudo apt install pacman-package-manager```\n\n```dnf: sudo dnf install pacman```\n\n```pacman: sudo pacman -S pacman```\n\n```apk: sudo apk add pacman```\n\n```nix: nix profile install nixpkgs#pacman```"],["SEE ALSO","[pacman](/man/pacman)(8), [pacman-remove](/man/pacman-remove)(8)"]],"e":["pacman","pacman-remove"],"x":"sudo pacman -R [package]"},"pct-rollback":{"n":"pct-rollback","t":"restore a Proxmox LXC container to a snapshot","s":[["TAGLINE","restore a Proxmox LXC container to a snapshot"],["TLDR","**Rollback** a container to a named snapshot\n\n```pct rollback [vmid] [snapname]```\n\nRollback and **start** the container after restore\n\n```pct rollback [vmid] [snapname] --start```"],["SYNOPSIS","**pct rollback** _vmid_ _snapname_ [_OPTIONS_]"],["PARAMETERS","**vmid**\n> Numeric ID of the container (100–999999999)\n\n**snapname**\n> Name of the snapshot to restore\n\n**--start**\n> Start the container after a successful rollback (default: off)"],["DESCRIPTION","**pct rollback** restores an LXC container on Proxmox VE to the disk and configuration state captured in a named snapshot. Current container state after the snapshot is discarded.\n\nUse **pct listsnapshot** to see available snapshot names. Rollback can require the container to be stopped depending on storage and snapshot type; check Proxmox documentation for your storage backend."],["CAVEATS","Rollback is destructive for changes made after the snapshot. Ensure you have selected the correct **snapname**. Newer snapshots may be affected when rolling back depending on storage features (for example, linear snapshot chains)."],["HISTORY","**pct rollback** is part of the **Proxmox VE** **pct** tool for managing LXC containers."],["SEE ALSO","[pct](/man/pct)(1), [pct-listsnapshot](/man/pct-listsnapshot)(1), [pct-delsnapshot](/man/pct-delsnapshot)(1)"],["RESOURCES","```[Documentation](https://pve.proxmox.com/pve-docs/pct.1.html)```\n\n```[Homepage](https://www.proxmox.com)```"]],"e":["pct","pct-listsnapshot","pct-delsnapshot"],"x":"pct rollback [vmid] [snapname]"},"pdfseparate":{"n":"pdfseparate","t":"splits a PDF into individual pages","s":[["TAGLINE","splits a PDF into individual pages"],["TLDR","**Extract all pages**\n\n```pdfseparate [document.pdf] [output_%d.pdf]```\n\n**Extract specific page range**\n\n```pdfseparate -f [1] -l [10] [document.pdf] [output_%d.pdf]```\n\n**Extract single page**\n\n```pdfseparate -f [5] -l [5] [document.pdf] [page5.pdf]```"],["SYNOPSIS","**pdfseparate** [_-f first_] [_-l last_] [_options_] _input.pdf_ _output_pattern_"],["PARAMETERS","**-f** _PAGE_\n> First page to extract.\n\n**-l** _PAGE_\n> Last page to extract.\n\n**-v**\n> Print version.\n\n**-h**\n> Print help."],["OUTPUT PATTERN","The output pattern uses %d for page numbers:\n- `page_%d.pdf` produces page_1.pdf, page_2.pdf, etc.\n- `output-%03d.pdf` produces output-001.pdf, output-002.pdf, etc."],["DESCRIPTION","**pdfseparate** splits a PDF into individual pages. Each page becomes a separate PDF file.\n\nThe output pattern must contain %d (or variants like %03d) which is replaced with the page number. This enables organized naming of output files.\n\nPage range options extract subsets of the document. Combined with specific page numbers, single pages can be extracted.\n\nThe resulting files are standalone PDFs containing one page each. They can be recombined using pdfunite or used individually.\n\nAll page content is preserved including annotations, links, and embedded resources."],["CAVEATS","Cannot select non-contiguous pages. Each page is a separate file (no multi-page extraction to single file). Encrypted PDFs need password."],["HISTORY","**pdfseparate** is part of **poppler-utils**, the open-source PDF utilities derived from **Xpdf**. It provides simple page extraction without complex dependencies."],["INSTALL","```apt: sudo apt install poppler-utils```\n\n```dnf: sudo dnf install poppler-utils```\n\n```apk: sudo apk add poppler-utils```\n\n```nix: nix profile install nixpkgs#poppler-utils```"],["SEE ALSO","[pdfunite](/man/pdfunite)(1), [pdftk](/man/pdftk)(1), [qpdf](/man/qpdf)(1), [pdfinfo](/man/pdfinfo)(1)"]],"e":["pdfunite","pdftk","qpdf","pdfinfo"],"x":"pdfseparate [document.pdf] [output_%d.pdf]"},"pillager":{"n":"pillager","t":"Pillage filesystems for sensitive information","s":[["TAGLINE","Pillage filesystems for sensitive information"],["TLDR","**Scan the current directory for secrets**\n\n```pillager hunt .```\n\n**Launch the interactive TUI scanner**\n\n```pillager```"],["SYNOPSIS","**pillager** [_command_] [_options_] [_path_]"],["DESCRIPTION","**pillager** is a filesystem scanner that recursively searches directories for sensitive information such as API keys, passwords, and credentials. It uses concurrent workers for fast scanning and implements Gitleaks rules for pattern matching. Discovered secrets can be sent to remote destinations including S3/MinIO, webhooks, or Sliver C2 loot stores."],["CAVEATS","Intended for defensive, educational, and authorized security research use only."],["HISTORY","**pillager** was created by **Britton Hayes** (brittonhayes) and is written in **Go**."],["INSTALL","```aur: yay -S pillager```"],["SEE ALSO","[gitleaks](/man/gitleaks)(1), [trufflehog](/man/trufflehog)(1), [grep](/man/grep)(1)"]],"e":["gitleaks","trufflehog","grep"],"x":"pillager hunt ."},"poetry-publish":{"n":"poetry-publish","t":"Publish packages to PyPI or repositories","s":[["TAGLINE","Publish packages to PyPI or repositories"],["TLDR","**Publish to PyPI**\n\n```poetry publish```\n\n**Build and publish**\n\n```poetry publish --build```\n\n**Publish to custom repository**\n\n```poetry publish -r [repository]```\n\n**Dry run**\n\n```poetry publish --dry-run```"],["SYNOPSIS","**poetry publish** [_options_]"],["PARAMETERS","**--build**\n> Build the package before publishing.\n\n**-r**, **--repository** _NAME_\n> Target repository (default: pypi). Must match a name configured via **poetry config**.\n\n**-u**, **--username** _USER_\n> Repository username.\n\n**-p**, **--password** _PASS_\n> Repository password.\n\n**--cert** _CERT_\n> Certificate authority to access the repository.\n\n**--client-cert** _CERT_\n> Client certificate to access the repository.\n\n**--dry-run**\n> Simulate publishing without uploading.\n\n**--skip-existing**\n> Ignore errors from files already existing in the repository."],["DESCRIPTION","**poetry publish** uploads built distribution packages (wheel and sdist) to PyPI or a custom repository. The **--build** flag builds the package before publishing in a single step.\n\nUse **-r** to specify an alternative repository configured via **poetry config**. Authentication can be provided via **-u**/**-p** flags, a configured token, or the keyring. The **--dry-run** flag simulates the upload without actually publishing."],["CAVEATS","Requires authentication via username/password, API token, or system keyring. The package must be built first, or use **--build** to build and publish in one step."],["INSTALL","```dnf: sudo dnf install poetry```\n\n```apk: sudo apk add poetry```\n\n```brew: brew install poetry```\n\n```nix: nix profile install nixpkgs#poetry```"],["SEE ALSO","[poetry](/man/poetry)(1), [poetry-build](/man/poetry-build)(1), [poetry-config](/man/poetry-config)(1), [twine](/man/twine)(1)"]],"e":["poetry","poetry-build","poetry-config","twine"],"x":"poetry publish"},"qm-vncproxy":{"n":"qm-vncproxy","t":"Proxy VNC connections to a Proxmox VM","s":[["TAGLINE","Proxy VNC connections to a Proxmox VM"],["TLDR","**Proxy** VNC traffic for a virtual machine\n\n```qm vncproxy 100```"],["SYNOPSIS","**qm vncproxy** _vmid_"],["DESCRIPTION","**qm vncproxy** proxies Virtual Network Computing (VNC) traffic for a virtual machine to stdin/stdout. This enables VNC clients to connect to VMs through the Proxmox VE infrastructure.\n\nThe proxy handles authentication and connection setup for VNC sessions."],["CAVEATS","Typically used internally by the Proxmox web interface. Direct use requires understanding of VNC protocol and connection handling."],["HISTORY","Part of **Proxmox VE** QEMU/KVM management tools. Provides the backend for web-based VNC console access."],["INSTALL","```dnf: sudo dnf install qm```"],["SEE ALSO","[qm](/man/qm)(1), [qm-terminal](/man/qm-terminal)(1)"]],"e":["qm","qm-terminal"],"x":"qm vncproxy 100"},"resticprofile":{"n":"resticprofile","t":"Manage restic backup profiles and schedules","s":[["TAGLINE","Manage restic backup profiles and schedules"],["TLDR","**Run backup**\n\n```resticprofile backup```\n\n**Run specific profile**\n\n```resticprofile -n [profile] backup```\n\n**Show configuration**\n\n```resticprofile show```\n\n**Initialize repository**\n\n```resticprofile -n [profile] init```\n\n**Schedule backups**\n\n```resticprofile schedule```\n\n**Run forget and prune**\n\n```resticprofile forget```\n\n**Unschedule all profiles**\n\n```resticprofile unschedule --all```\n\n**Check repository integrity**\n\n```resticprofile -n [profile] check```\n\n**List snapshots**\n\n```resticprofile snapshots```\n\n**Generate config template**\n\n```resticprofile generate```"],["SYNOPSIS","**resticprofile** [_-n profile_] [_options_] _command_ [_args_]"],["PARAMETERS","**-n**, **--name** _PROFILE_\n> Profile to use.\n\n**-c**, **--config** _FILE_\n> Configuration file.\n\n**-v**, **--verbose**\n> Verbose output.\n\n**-q**, **--quiet**\n> Quiet mode.\n\n**--dry-run**\n> Simulate only.\n\n**-l**, **--log** _FILE_\n> Log to file.\n\n**--no-ansi**\n> Disable ANSI color output."],["COMMANDS","**backup**\n> Run backup.\n\n**forget**\n> Apply retention.\n\n**prune**\n> Remove unused data.\n\n**snapshots**\n> List snapshots.\n\n**restore**\n> Restore files.\n\n**schedule**\n> Manage schedules.\n\n**init**\n> Initialize repository.\n\n**check**\n> Verify repository integrity.\n\n**show**\n> Display parsed configuration.\n\n**unschedule**\n> Remove scheduled tasks.\n\n**status**\n> Show scheduled job status."],["DESCRIPTION","**resticprofile** is a configuration wrapper for the restic backup tool that organizes backup settings into named profiles. Each profile defines a repository location, backup paths, exclusion patterns, retention policies, and scheduling rules, allowing complex backup strategies to be managed through a single configuration file rather than lengthy command-line arguments.\n\nThe tool integrates with system schedulers to automate backup operations. On Linux it creates systemd timers or cron jobs, and on macOS it uses launchd. Retention policies can differ between profiles, so frequently changing data can have short retention while archives keep longer histories. Pre- and post-operation hooks enable notifications, database dumps before backup, and cleanup tasks after completion.\n\nAll standard restic commands (backup, forget, prune, restore, snapshots) can be run through resticprofile with profile-specific settings automatically applied. The **generate** command creates starter configuration templates."],["CONFIGURATION","**profiles.conf** / **profiles.toml** / **profiles.yaml**\n> Default configuration file (searched in current directory) defining backup profiles, repositories, schedules, and retention policies. Format auto-detected by extension."],["CAVEATS","Requires restic to be installed separately. Configuration file format is specific to resticprofile and not interchangeable with restic's own options. The system scheduler used varies by OS: systemd timers on Linux, launchd on macOS, and Task Scheduler on Windows."],["HISTORY","**resticprofile** was created as a configuration wrapper for **restic** backup. It simplifies managing multiple backup profiles and scheduling."],["INSTALL","```brew: brew install resticprofile```\n\n```nix: nix profile install nixpkgs#resticprofile```"],["SEE ALSO","[restic](/man/restic)(1), [borgbackup](/man/borgbackup)(1), [cron](/man/cron)(1), [crontab](/man/crontab)(1)"]],"e":["restic","borgbackup","cron","crontab"],"x":"resticprofile backup"},"ripd":{"n":"ripd","t":"RIP routing protocol daemon","s":[["TAGLINE","RIP routing protocol daemon"],["TLDR","**Start RIP daemon**\n\n```ripd -d```\n\n**Start with config file**\n\n```ripd -d -f [/etc/frr/ripd.conf]```\n\n**Connect to running daemon via VTY**\n\n```vtysh -d ripd```\n\n**Start with VTY bound to localhost**\n\n```ripd -d -A 127.0.0.1```\n\n**Open interactive terminal session**\n\n```ripd -t```"],["SYNOPSIS","**ripd** [_options_]"],["PARAMETERS","**-d**, **--daemon**\n> Run as daemon, forking to background after initialization.\n\n**-f**, **--config_file** _file_\n> Configuration file (default: /etc/frr/ripd.conf).\n\n**-t**, **--terminal**\n> Open an interactive VTY session on the terminal.\n\n**-A**, **--vty_addr** _addr_\n> VTY bind address.\n\n**-P**, **--vty_port** _port_\n> VTY port (default: 2602). Set to 0 to disable.\n\n**-i**, **--pid_file** _file_\n> PID file path (default: /var/run/frr/ripd.pid).\n\n**-z**, **--zclient_path** _path_\n> Override ZAPI socket path for zebra communication.\n\n**-u**, **--user** _user_\n> Run as user.\n\n**-g**, **--group** _group_\n> Run as group."],["DESCRIPTION","**ripd** is the RIP (Routing Information Protocol) daemon from Quagga/FRRouting. It implements RIPv1 and RIPv2 for dynamic routing in small to medium networks."],["EXAMPLES","```bash"],["Start daemon","ripd -d -f /etc/frr/ripd.conf"],["Connect via vtysh","vtysh\nrouter rip\n  network 192.168.1.0/24\n  version 2\n```"],["CONFIGURATION","**/etc/frr/ripd.conf** (FRRouting) / **/etc/quagga/ripd.conf** (Quagga)\n> Main configuration file defining RIP networks, version, redistribution rules, and interface settings."],["CAVEATS","Part of FRRouting (or legacy Quagga). Requires the zebra daemon to be running. RIP has a maximum hop count of 15 and slow convergence. Use OSPF for larger networks."],["HISTORY","ripd is part of **Quagga** routing suite (forked from GNU Zebra), and later **FRRouting** for Unix routing daemons."],["INSTALL","```apk: sudo apk add quagga```"],["SEE ALSO","[zebra](/man/zebra)(8), [ospfd](/man/ospfd)(8), [vtysh](/man/vtysh)(1), [bgpd](/man/bgpd)(8)"]],"e":["zebra","ospfd","vtysh","bgpd"],"x":"ripd -d"},"sha256sum":{"n":"sha256sum","t":"Compute and verify SHA-256 checksums","s":[["TAGLINE","Compute and verify SHA-256 checksums"],["TLDR","**Calculate the SHA256 checksum** of a file\n\n```sha256sum [file]```\n\n**Calculate checksums** for multiple files\n\n```sha256sum [file1] [file2]```\n\n**Read from stdin** and calculate checksum\n\n```echo \"text\" | sha256sum```\n\n**Generate a checksum file** for later verification\n\n```sha256sum [file1] [file2] > [checksums.sha256]```\n\n**Verify files** against a checksum file\n\n```sha256sum -c [checksums.sha256]```\n\n**Verify silently** (only show failures)\n\n```sha256sum -c --quiet [checksums.sha256]```"],["SYNOPSIS","**sha256sum** [_OPTION_]... [_FILE_]..."],["PARAMETERS","**-b**, **--binary**\n> Read in binary mode\n\n**-c**, **--check**\n> Read checksums from file and verify them\n\n**-t**, **--text**\n> Read in text mode (default)\n\n**--tag**\n> Create a BSD-style checksum output\n\n**-z**, **--zero**\n> End each output line with NUL, not newline, and disable filename escaping\n\n**--quiet**\n> Don't print OK for each verified file\n\n**--status**\n> Don't output anything, exit code shows success\n\n**-w**, **--warn**\n> Warn about improperly formatted checksum lines\n\n**--strict**\n> Exit non-zero for improperly formatted checksum lines\n\n**--ignore-missing**\n> Don't fail for missing files during verification\n\n**--help**\n> Display help information\n\n**--version**\n> Display version information"],["DESCRIPTION","**sha256sum** computes and verifies SHA-256 cryptographic hash values. SHA-256 (Secure Hash Algorithm 256-bit) is part of the SHA-2 family and produces a 256-bit (32-byte) hash value, typically rendered as a 64-character hexadecimal number.\n\nThe command is commonly used to verify file integrity and authenticity. Software distributors often provide SHA256 checksums alongside downloads, allowing users to verify that files were not corrupted during transfer or tampered with.\n\nWhen checking files with **-c**, the input file should contain lines in the format produced by sha256sum: the hash, two spaces (or a space and asterisk for binary mode), and the filename."],["CAVEATS","SHA-256 is not designed for password hashing; use **bcrypt** or **argon2** for that purpose. On GNU systems, binary and text modes produce the same checksum. On macOS, the equivalent command is **shasum -a 256**."],["HISTORY","SHA-256 was designed by the **NSA** and published by **NIST** in **2001** as part of the SHA-2 family of hash functions. The **sha256sum** command was added to **GNU Coreutils** to provide a standard Unix utility for computing these hashes. It has become essential for software distribution verification, replacing MD5 and SHA-1 which have known weaknesses. SHA-256 is widely used in security applications, including Bitcoin's proof-of-work system."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[md5sum](/man/md5sum)(1), [sha1sum](/man/sha1sum)(1), [sha512sum](/man/sha512sum)(1), [b2sum](/man/b2sum)(1), [cksum](/man/cksum)(1)"]],"e":["md5sum","sha1sum","sha512sum","b2sum","cksum"],"x":"sha256sum [file]"},"snmpgetnext":{"n":"snmpgetnext","t":"Retrieve next SNMP OID value sequentially","s":[["TAGLINE","Retrieve next SNMP OID value sequentially"],["TLDR","**Get next OID value**\n\n```snmpgetnext -v [2c] -c [public] [host] [OID]```\n\n**Get next with SNMPv3**\n\n```snmpgetnext -v [3] -u [user] -l authPriv -a SHA -A [authpass] -x AES -X [privpass] [host] [OID]```"],["SYNOPSIS","**snmpgetnext** [_options_] _agent_ _oid_..."],["PARAMETERS","**-v** _version_\n> SNMP version (1, 2c, 3).\n\n**-c** _community_\n> Community string.\n\n**-u** _user_\n> SNMPv3 username.\n\n**-l** _level_\n> Security level (noAuthNoPriv, authNoPriv, authPriv).\n\n**-O** _opts_\n> Output format options."],["DESCRIPTION","**snmpgetnext** retrieves the lexicographically next OID value from an SNMP agent. Used for walking MIB trees and discovering available OIDs. Part of Net-SNMP suite."],["INSTALL","```apt: sudo apt install snmp```\n\n```apk: sudo apk add net-snmp-tools```"],["SEE ALSO","[snmpget](/man/snmpget)(1), [snmpwalk](/man/snmpwalk)(1), [snmpbulkget](/man/snmpbulkget)(1)"]],"e":["snmpget","snmpwalk","snmpbulkget"],"x":"snmpgetnext -v [2c] -c [public] [host] [OID]"},"speech-dispatcher":{"n":"speech-dispatcher","t":"Text-to-speech server daemon","s":[["TAGLINE","Text-to-speech server daemon"],["TLDR","**Start speech-dispatcher** daemon\n\n```speech-dispatcher```\n\n**Start in debug mode**\n\n```speech-dispatcher -D```\n\n**Start with a specific communication method**\n\n```speech-dispatcher -c [unix_socket|inet_socket]```\n\n**Start with a custom configuration file**\n\n```speech-dispatcher -C [path/to/speechd.conf]```\n\n**Start with a specific port**\n\n```speech-dispatcher -p [6560]```"],["SYNOPSIS","**speech-dispatcher** [_options_]"],["PARAMETERS","**-d**, **--spawn**\n> Start as a daemon (background process).\n\n**-D**, **--debug**\n> Enable debug mode with verbose output.\n\n**-l**, **--log-level** _level_\n> Set logging level (1-5, higher is more verbose).\n\n**-c**, **--communication-method** _method_\n> Communication method: **unix_socket** or **inet_socket**.\n\n**-S**, **--socket-path** _path_\n> Socket path for unix_socket method.\n\n**-p**, **--port** _port_\n> Port number for inet_socket method (default: 6560).\n\n**-a**, **--address** _address_\n> Network address to listen on.\n\n**-C**, **--config-file** _path_\n> Use a custom configuration file.\n\n**-P**, **--pid-file** _path_\n> PID file location.\n\n**-m**, **--module-dir** _path_\n> Directory for output modules.\n\n**-v**, **--version**\n> Display version information.\n\n**-h**, **--help**\n> Display help information."],["DESCRIPTION","**speech-dispatcher** is a server daemon that manages text-to-speech output on Linux systems. It provides a unified interface for applications to request speech synthesis, handling concurrent requests based on priority levels and abstracting different speech synthesizer backends.\n\nThe daemon listens for client connections and dispatches text to configured speech synthesis modules such as eSpeak NG, Festival, Piper, or others. It supports features like adjustable speech rate, pitch, volume, voice selection, and multiple languages.\n\nApplications communicate with speech-dispatcher through client libraries (libspeechd) or the **spd-say** command-line utility. User-specific configuration is stored in **~/.config/speech-dispatcher/** while system configuration resides in **/etc/speech-dispatcher/**."],["CONFIGURATION","**/etc/speech-dispatcher/speechd.conf**\n> System-wide configuration for default output module, voice, language, and audio settings.\n\n**~/.config/speech-dispatcher/speechd.conf**\n> Per-user configuration overriding system defaults.\n\n**/etc/speech-dispatcher/modules/**\n> Output module configuration files for each speech synthesizer backend (espeak-ng.conf, festival.conf, etc.)."],["CAVEATS","The daemon must be running for clients like **spd-say** to work. At least one speech synthesis backend (output module) must be configured. Audio system compatibility depends on the configured output modules. Running multiple instances requires different socket paths or ports."],["HISTORY","**Speech Dispatcher** was created to provide a unified text-to-speech interface for Linux accessibility and desktop applications. It was designed to work with screen readers and other assistive technologies. The project is developed as part of the Free(b)soft project for accessibility on free operating systems. Development continues with support for new synthesizers and audio systems."],["INSTALL","```apt: sudo apt install speech-dispatcher```\n\n```dnf: sudo dnf install speech-dispatcher```\n\n```pacman: sudo pacman -S speech-dispatcher```\n\n```apk: sudo apk add speech-dispatcher```\n\n```zypper: sudo zypper install speech-dispatcher```"],["SEE ALSO","[spd-say](/man/spd-say)(1), [espeak-ng](/man/espeak-ng)(1), [festival](/man/festival)(1)"]],"e":["spd-say","espeak-ng","festival"],"x":"speech-dispatcher"},"sshpass":{"n":"sshpass","t":"","s":[["TLDR","**SSH with password**\n\n```sshpass -p '[password]' ssh [user@host]```\n\n**SSH with password from file**\n\n```sshpass -f [password_file] ssh [user@host]```\n\n**SSH with password from env var**\n\n```SSHPASS='[password]' sshpass -e ssh [user@host]```\n\n**SCP with password**\n\n```sshpass -p '[password]' scp [file] [user@host:/path/]```\n\n**Rsync with password**\n\n```sshpass -p '[password]' rsync -avz [files] [user@host:/path/]```"],["SYNOPSIS","**sshpass** [_-p password_] [_-f file_] [_-e_] [_options_] _command_ [_args_]"],["PARAMETERS","**-p** _PASSWORD_\n> Password on command line.\n\n**-f** _FILE_\n> Read password from file.\n\n**-e**\n> Read from SSHPASS env variable.\n\n**-d** _FD_\n> Read from file descriptor.\n\n**-P** _PROMPT_\n> Set password prompt pattern.\n\n**-v**\n> Verbose mode.\n\n**-h**\n> Show help.\n\n**-V**\n> Show version."],["DESCRIPTION","**sshpass** provides password authentication for SSH without interactive input. It enables automation where key-based auth isn't available.\n\nPassword sources include command line, files, and environment variables. File-based passwords are safer than command-line exposure.\n\nThe tool works with any program expecting SSH-style password prompts: ssh, scp, rsync, and others.\n\nFirst-time connections need host key handling. Use -o StrictHostKeyChecking=no for automation, though this has security implications.\n\nFor most uses, SSH keys are preferred. sshpass fills gaps where key auth isn't possible or practical."],["CAVEATS","Command-line passwords visible in process list. Less secure than SSH keys. Not recommended for interactive use. May expose passwords in logs."],["HISTORY","**sshpass** was written by **Shachar Shemesh** to address the need for non-interactive SSH password entry in scripts and automation where key-based authentication wasn't feasible."],["INSTALL","```apt: sudo apt install sshpass```\n\n```dnf: sudo dnf install sshpass```\n\n```pacman: sudo pacman -S sshpass```\n\n```apk: sudo apk add sshpass```\n\n```zypper: sudo zypper install sshpass```\n\n```brew: brew install sshpass```\n\n```nix: nix profile install nixpkgs#sshpass```"],["SEE ALSO","[ssh](/man/ssh)(1), [ssh-keygen](/man/ssh-keygen)(1), [expect](/man/expect)(1), [ssh-agent](/man/ssh-agent)(1)"],["RESOURCES","```[Homepage](https://sourceforge.net/projects/sshpass/)```"]],"e":["ssh","ssh-keygen","expect","ssh-agent"],"x":"sshpass -p '[password]' ssh [user@host]"},"streamlit":{"n":"streamlit","t":"Build data web apps from Python scripts","s":[["TAGLINE","Build data web apps from Python scripts"],["TLDR","**Run Streamlit app**\n\n```streamlit run [app.py]```\n\n**Run with specific port**\n\n```streamlit run [app.py] --server.port [8501]```\n\n**Run with arguments**\n\n```streamlit run [app.py] -- [--arg1] [--arg2]```\n\n**Show configuration**\n\n```streamlit config show```\n\n**Create hello app**\n\n```streamlit hello```\n\n**Clear cache**\n\n```streamlit cache clear```\n\n**Show version**\n\n```streamlit version```"],["SYNOPSIS","**streamlit** [_run_] [_config_] [_cache_] [_options_] [_file_] [_-- args_]"],["PARAMETERS","**run** _FILE_\n> Run Streamlit app.\n\n**hello**\n> Run demo app.\n\n**config show**\n> Show configuration.\n\n**cache clear**\n> Clear cache.\n\n**version**\n> Show version.\n\n**--server.port** _PORT_\n> Server port.\n\n**--server.address** _ADDR_\n> Server address.\n\n**--server.headless** _BOOL_\n> Run without browser.\n\n**--browser.serverAddress** _ADDR_\n> Browser server address.\n\n**--theme.base** _THEME_\n> Theme (light/dark).\n\n**--global.developmentMode** _BOOL_\n> Development mode."],["DESCRIPTION","**streamlit** creates web applications from Python scripts. Data apps, dashboards, and ML demos are built without web development knowledge.\n\nApps are Python scripts using Streamlit's API. Widgets like sliders, buttons, and text inputs are added with single function calls.\n\nHot reload automatically updates the app when code changes. The development workflow is fast and interactive.\n\nCaching decorators prevent recomputation of expensive operations. Data loads and model inference are cached across reruns.\n\nDeployment options include Streamlit Community Cloud, which hosts apps from GitHub for free. Self-hosting is also supported.\n\nComponents extend functionality. Custom widgets, visualizations, and integrations are available from the ecosystem."],["CAVEATS","Not suited for complex web apps. Limited layout control. Session state needs explicit management. Large apps may have performance issues."],["HISTORY","**Streamlit** was created by **Adrien Treuille**, **Thiago Teixeira**, and **Amanda Kelly** in **2019**. It rapidly gained popularity for data science prototyping and was acquired by **Snowflake** in **2022**."],["INSTALL","```nix: nix profile install nixpkgs#streamlit```"],["SEE ALSO","[gradio](/man/gradio)(1), [dash](/man/dash)(1), [flask](/man/flask)(1), [python](/man/python)(1)"]],"e":["gradio","dash","flask","python"],"x":"streamlit run [app.py]"},"systool":{"n":"systool","t":"Sysfs device information viewer","s":[["TAGLINE","Sysfs device information viewer"],["TLDR","List all buses, classes, and **root devices**\n\n```systool```\n\nList **bus** devices with all attribute values\n\n```systool -b [pci] -v```\n\nList **class** devices with all attribute values\n\n```systool -c [net] -v```\n\nShow only the **drivers** on a bus\n\n```systool -b [bus] -D```\n\nShow attributes of a specific **module**\n\n```systool -m [module_name] -v```\n\nShow a single named **attribute** for a device\n\n```systool -A [attribute] -b [bus] [device]```"],["SYNOPSIS","**systool** [_options_ [_device_]]"],["PARAMETERS","**-b** _bus_\n> Show information for a specific bus (pci, usb, etc.).\n\n**-c** _class_\n> Show information for a specific class (block, net, drm, etc.).\n\n**-m** _module_\n> Show information for a specific module.\n\n**-a**\n> Show attributes of the requested resource.\n\n**-v**\n> Show all attributes with values.\n\n**-A** _attribute_\n> Show the value of a single named attribute for the requested resource.\n\n**-d**\n> Show only devices.\n\n**-D**\n> Show only drivers.\n\n**-P**\n> Show the device's parent.\n\n**-p**\n> Show the absolute sysfs path to the resource.\n\n**-h**\n> Show usage."],["DESCRIPTION","**systool** displays system device information from sysfs by bus, class, and topology. It provides a convenient interface to browse the `/sys/bus/`, `/sys/class/`, and `/sys/module/` hierarchies.\n\nRun without parameters, **systool** lists all available bus types, device classes, and root devices. Passing a _device_ name after the options limits output to that single device. Available buses can be found in `/sys/bus/` and classes in `/sys/class/`; common buses include pci, usb, and scsi, and common classes include block, net, and drm."],["CAVEATS","Requires the sysfsutils package. Information comes from the kernel's sysfs interface. Some attributes may require root to read."],["HISTORY","**systool** is part of the **sysfsutils** package, providing user-friendly access to the Linux sysfs virtual filesystem that exports kernel device information."],["INSTALL","```apt: sudo apt install sysfsutils```\n\n```dnf: sudo dnf install sysfsutils```\n\n```pacman: sudo pacman -S sysfsutils```\n\n```apk: sudo apk add sysfsutils```\n\n```zypper: sudo zypper install sysfsutils```\n\n```nix: nix profile install nixpkgs#sysfsutils```"],["SEE ALSO","[lspci](/man/lspci)(8), [lsusb](/man/lsusb)(8), [lsmod](/man/lsmod)(8), [udevadm](/man/udevadm)(8)"]],"e":["lspci","lsusb","lsmod","udevadm"],"x":"systool"},"tload":{"n":"tload","t":"Graphical representation of system load average","s":[["TAGLINE","Graphical representation of system load average"],["TLDR","**Show a live ASCII graph** of the system load on the current terminal\n\n```tload```\n\n**Display the graph on a specific tty** (e.g. a secondary virtual console)\n\n```tload [/dev/tty2]```\n\n**Set the vertical scale** (characters between graph ticks — smaller value = larger scale)\n\n```tload -s [1]```\n\n**Update the graph every 5 seconds** instead of the default\n\n```tload -d [5]```\n\n**Combine scale and update interval**\n\n```tload -s 2 -d 2```"],["SYNOPSIS","**tload** [_options_] [_tty_]"],["PARAMETERS","**-s** _NUMBER_, **--scale** _NUMBER_\n> Vertical scale in characters between graph ticks. A smaller value produces a taller, more sensitive graph.\n\n**-d** _SECONDS_, **--delay** _SECONDS_\n> Seconds between redraws. Setting **-d 0** disables the alarm and freezes the display.\n\n**-h**, **--help**\n> Display help text and exit.\n\n**-V**, **--version**\n> Print version information and exit."],["DESCRIPTION","**tload** prints a rolling ASCII graph of the **1-, 5-, and 15-minute load averages** read from **/proc/loadavg** to the specified tty — or to the terminal that started tload if none is given. Each column in the graph represents one sample, so the display scrolls from right to left as new values arrive.\n\nThe current load averages are also printed at the top of the screen in the familiar **uptime** format. tload is especially useful on headless servers, serial consoles, and tmux panes where a full curses-based monitor such as **top** or **htop** would be overkill.\n\nThe utility exits on any keystroke (**q**, **Ctrl-C**, etc.) and redraws cleanly on SIGWINCH when the terminal is resized."],["FILES","**/proc/loadavg**\n> Source file read every update cycle for the three load-average values."],["CAVEATS","Passing **-d 0** sets the internal **alarm(2)** timer to zero, which never fires; the display therefore remains static until the process is killed. The vertical scale is in whole characters, so very quiet systems may show a flat line unless **-s 1** is used."],["HISTORY","**tload** originated in the early **procps** utilities for Linux (mid-1990s) as a lightweight counterpart to **xload** on systems without X11. It is now maintained as part of the **procps-ng** suite alongside **ps**, **top**, **uptime**, and **w**."],["INSTALL","```apt: sudo apt install procps```\n\n```dnf: sudo dnf install procps-ng```\n\n```pacman: sudo pacman -S procps-ng```\n\n```apk: sudo apk add procps-ng```\n\n```zypper: sudo zypper install procps```\n\n```brew: brew install procps```\n\n```nix: nix profile install nixpkgs#procps```"],["SEE ALSO","[uptime](/man/uptime)(1), [top](/man/top)(1), [htop](/man/htop)(1), [w](/man/w)(1), [ps](/man/ps)(1)"]],"e":["uptime","top","htop","w","ps"],"x":"tload"},"unattended-upgrades":{"n":"unattended-upgrades","t":"Automatic security update installer","s":[["TAGLINE","Automatic security update installer"],["TLDR","**Run unattended upgrades** manually\n\n```sudo unattended-upgrades```\n\n**Simulate upgrades** without installing (dry run)\n\n```sudo unattended-upgrades --dry-run```\n\n**Run with verbose output**\n\n```sudo unattended-upgrades -v```\n\n**Run with debug output**\n\n```sudo unattended-upgrades -d```\n\n**Configure automatic updates** interactively\n\n```sudo dpkg-reconfigure -plow unattended-upgrades```\n\n**Check if automatic updates are enabled**\n\n```cat /etc/apt/apt.conf.d/20auto-upgrades```"],["SYNOPSIS","**unattended-upgrades** [**-d**] [**-v**] [**--dry-run**] [**--minimal-upgrade-steps**]"],["PARAMETERS","**-d**, **--debug**\n> Enable extra debug output to /var/log/unattended-upgrades/unattended-upgrades.log.\n\n**-v**, **--verbose**\n> Show verbose output during operation.\n\n**--dry-run**\n> Simulate the upgrade process without actually installing packages.\n\n**--apt-debug**\n> Enable detailed APT/LibAPT output for debugging.\n\n**--minimal-upgrade-steps**\n> Perform upgrade in minimal steps, allowing cancellation with SIGINT between packages. This is the default.\n\n**--no-minimal-upgrade-steps**\n> Perform upgrade without minimal steps (all packages at once).\n\n**-h**, **--help**\n> Display help message."],["DESCRIPTION","**unattended-upgrades** automatically installs security updates and optionally other upgrades on Debian and Ubuntu systems. It runs daily via systemd timers (apt-daily.timer and apt-daily-upgrade.timer) to keep the system patched without manual intervention.\n\nThe tool is configured through apt configuration files, primarily /etc/apt/apt.conf.d/50unattended-upgrades for allowed origins and options, and /etc/apt/apt.conf.d/20auto-upgrades for enabling automatic updates. By default, only security updates are installed automatically.\n\nConfiguration options include email notifications, automatic removal of unused dependencies, automatic reboots if required, and blacklisting specific packages from automatic updates. The tool logs its activity to /var/log/unattended-upgrades/.\n\nBoth **unattended-upgrades** and **unattended-upgrade** commands work identically; one is a symlink to the other."],["CONFIGURATION","**/etc/apt/apt.conf.d/50unattended-upgrades**\n> Main configuration specifying allowed origins, package blacklists, email notifications, and reboot behavior.\n\n**/etc/apt/apt.conf.d/20auto-upgrades**\n> Controls whether automatic updates and unattended upgrades are enabled.\n\n**/var/log/unattended-upgrades/**\n> Log directory containing upgrade history and debug information."],["CAVEATS","Automatic updates can cause issues if packages have breaking changes or require manual configuration. Automatic reboots should be carefully considered for production servers. Some packages may be held back if they require configuration prompts. Check logs regularly to ensure updates are applying successfully."],["HISTORY","**unattended-upgrades** was developed for **Debian** and **Ubuntu** to automate security patching. It became the default automatic update mechanism in Ubuntu, included by default since at least **Ubuntu 16.04**. The tool evolved from cron-based execution to systemd timers for more reliable scheduling and better integration with modern init systems."],["SEE ALSO","[apt](/man/apt)(8), [apt-get](/man/apt-get)(8), [dpkg](/man/dpkg)(1), [systemctl](/man/systemctl)(1)"]],"e":["apt","apt-get","dpkg","systemctl"],"x":"sudo unattended-upgrades"},"wcurl":{"n":"wcurl","t":"Simplified curl wrapper for downloading","s":[["TAGLINE","Simplified curl wrapper for downloading"],["TLDR","**Download file**\n\n```wcurl [https://example.com/file.zip]```\n\n**Download multiple**\n\n```wcurl [url1] [url2]```\n\n**Resume download**\n\n```wcurl -c [url]```\n\n**With output name**\n\n```wcurl -o [filename] [url]```"],["SYNOPSIS","**wcurl** [_-c_] [_-o file_] [_options_] _urls_"],["PARAMETERS","**-c**\n> Continue/resume.\n\n**-o** _FILE_\n> Output filename.\n\n**--help**\n> Show help."],["DESCRIPTION","**wcurl** is a thin wrapper around curl that provides sensible defaults for the common task of downloading files. It automatically enables a progress bar, follows redirects, and uses the remote filename for output, eliminating the need to specify common curl flags for simple downloads.\n\nResume support allows continuing interrupted downloads without manually configuring curl's range options. Multiple URLs can be specified to download several files in sequence, and a custom output filename can be provided when the remote name is not suitable.\n\nThe tool is designed for users who frequently use curl for downloads but find its default behavior (outputting to stdout with no progress indicator) inconvenient for that use case."],["CAVEATS","Wrapper around curl. Less flexible. Download-focused."],["HISTORY","**wcurl** provides a simpler interface to curl for the common case of downloading files."],["INSTALL","```apt: sudo apt install curl```\n\n```dnf: sudo dnf install curl```\n\n```pacman: sudo pacman -S curl```\n\n```apk: sudo apk add curl```\n\n```zypper: sudo zypper install curl```\n\n```brew: brew install curl```\n\n```nix: nix profile install nixpkgs#curl```"],["SEE ALSO","[curl](/man/curl)(1), [wget](/man/wget)(1), [aria2c](/man/aria2c)(1)"]],"e":["curl","wget","aria2c"],"x":"wcurl [https://example.com/file.zip]"},"xdg-desktop-menu":{"n":"xdg-desktop-menu","t":"Install desktop menu entries","s":[["TAGLINE","Install desktop menu entries"],["TLDR","**Install** application to menu\n\n```xdg-desktop-menu install [path/to/file.desktop]```\n\nInstall **without vendor** check\n\n```xdg-desktop-menu install --novendor [path/to/file.desktop]```\n\n**Uninstall** from menu\n\n```xdg-desktop-menu uninstall [path/to/file.desktop]```\n\n**Force update** menu\n\n```xdg-desktop-menu forceupdate --mode [user|system]```"],["SYNOPSIS","**xdg-desktop-menu** _COMMAND_ [_OPTIONS_] [_FILE_]"],["PARAMETERS","**install** _FILE_\n> Install desktop file to menu system\n\n**uninstall** _FILE_\n> Remove desktop file from menu system\n\n**forceupdate**\n> Force menu system to update\n\n**--novendor**\n> Disable vendor prefix check\n\n**--mode** _MODE_\n> Specify user or system mode"],["DESCRIPTION","**xdg-desktop-menu** installs and uninstalls desktop menu items conforming to the FreeDesktop.org Desktop Entry Specification. It handles the registration of applications with the desktop menu system.\n\nThe tool is part of the xdg-utils package that provides standardized desktop integration utilities."],["CAVEATS","Requires valid .desktop files. Menu system must be XDG compliant. Changes may require logout or menu refresh to appear."],["INSTALL","```dnf: sudo dnf install xdg-utils```\n\n```pacman: sudo pacman -S xdg-utils```\n\n```apk: sudo apk add xdg-utils```\n\n```zypper: sudo zypper install xdg-utils```\n\n```nix: nix profile install nixpkgs#xdg-utils```"],["SEE ALSO","[xdg-open](/man/xdg-open)(1), [xdg-mime](/man/xdg-mime)(1), [desktop-file-validate](/man/desktop-file-validate)(1)"]],"e":["xdg-open","xdg-mime","desktop-file-validate"],"x":"xdg-desktop-menu install [path/to/file.desktop]"},"zmore":{"n":"zmore","t":"View compressed files with more pager","s":[["TAGLINE","View compressed files with more pager"],["TLDR","**View a compressed file** one screen at a time\n\n```zmore [file.gz]```\n\n**View with a specific number** of lines per screen\n\n```zmore -[20] [file.gz]```\n\n**Display helpful prompts** instead of beeping on invalid keys\n\n```zmore -d [file.gz]```\n\n**Squeeze multiple blank lines** into one\n\n```zmore -s [file.gz]```\n\n**Start at first occurrence** of a pattern\n\n```zmore +/[pattern] [file.gz]```"],["SYNOPSIS","**zmore** [_-dfls_] [_+linenum_] [_+/pattern_] [_file ..._]"],["PARAMETERS","**-_number_**\n> Number of lines per screen\n\n**-d**\n> Display helpful messages instead of ringing bell on errors\n\n**-f**\n> Count logical lines rather than screen lines (long lines not folded)\n\n**-l**\n> Do not pause after form feed characters\n\n**-s**\n> Squeeze consecutive blank lines into one\n\n**+_linenum_**\n> Start at specified line number\n\n**+/_pattern_**\n> Start two lines before first match of pattern"],["DESCRIPTION","**zmore** is a pager for viewing compressed text files one screen at a time. It works with files compressed using gzip, compress, or pack, and also handles uncompressed files.\n\nIf a file doesn't exist, zmore automatically looks for versions with .gz, .z, or .Z extensions. The command combines decompression with the more pager, displaying --More-- at the bottom of each screenful.\n\nPress space to advance one screen, Enter for one line, or q to quit. When output is not a terminal, zmore behaves like zcat, decompressing to stdout."],["CAVEATS","zmore cannot scroll backward through files; use **zless** for bi-directional scrolling. On SSDs, repeatedly viewing compressed files causes minimal wear, but extracting files does. Set the PAGER environment variable to use an alternative pager like less."],["HISTORY","zmore was created as part of the **gzip** package in the early 1990s to provide convenient viewing of compressed files. It wraps the traditional more pager with on-the-fly decompression."],["INSTALL","```apt: sudo apt install gzip```\n\n```dnf: sudo dnf install gzip```\n\n```pacman: sudo pacman -S gzip```\n\n```apk: sudo apk add gzip```\n\n```zypper: sudo zypper install gzip```\n\n```brew: brew install gzip```\n\n```nix: nix profile install nixpkgs#gzip```"],["SEE ALSO","[zless](/man/zless)(1), [zcat](/man/zcat)(1), [more](/man/more)(1), [gzip](/man/gzip)(1)"]],"e":["zless","zcat","more","gzip"],"x":"zmore [file.gz]"}});
