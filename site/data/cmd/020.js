window.LCL_BUCKET(20,{"add-apt-repository":{"n":"add-apt-repository","t":"Manage APT repository sources","s":[["TAGLINE","Manage APT repository sources"],["TLDR","**Add** a new apt repository\n\n```add-apt-repository [repository_spec]```\n\n**Remove** an apt repository\n\n```add-apt-repository -r [repository_spec]```\n\nAdd a repository **without** updating the package cache afterwards\n\n```add-apt-repository -n [repository_spec]```\n\nAllow **source packages** to be downloaded from the repository\n\n```add-apt-repository -s [repository_spec]```\n\nAdd a **PPA** (Personal Package Archive)\n\n```add-apt-repository ppa:[user]/[ppa-name]```\n\n**List** currently configured repositories\n\n```add-apt-repository -L```"],["SYNOPSIS","**add-apt-repository** [_OPTIONS_] [_LINE_]"],["DESCRIPTION","**add-apt-repository** is a Debian/Ubuntu utility that manages third-party APT repository sources. It can add entries to /etc/apt/sources.list or create individual files in /etc/apt/sources.list.d/, and can also remove previously added repositories.\n\nThe tool is most commonly used to add PPAs (Personal Package Archives) from Launchpad, handling both the source list entry and the associated GPG key import automatically. By default it updates the package cache after modifying sources; pass **-n** to skip this step."],["PARAMETERS","**-r, --remove**\n> Remove specified repository (disables matching lines; deletes empty source files)\n\n**-y, --yes**\n> Assume affirmative responses to prompts\n\n**-n, --no-update**\n> Skip package cache updates after adding repository\n\n**-s, --enable-source**\n> Allow downloading of source packages from the repository\n\n**-c, --component**\n> Specify repository component(s); defaults to 'main' if unspecified\n\n**-p, --pocket**\n> Define pocket type (defaults to release pocket)\n\n**-P, --ppa**\n> Add Ubuntu Launchpad Personal Package Archive (format: ppa:USER/PPA)\n\n**-C, --cloud**\n> Add Ubuntu Cloud Archive (format: cloud-archive:CANAME)\n\n**-U, --uri**\n> Add archive via single URI\n\n**-S, --sourceslist**\n> Add archive using full sources.list format line\n\n**-L, --list**\n> List currently configured repositories\n\n**-l, --login**\n> Authenticate to Launchpad for private PPA access\n\n**--dry-run**\n> Preview changes without executing them\n\n**-d, --debug**\n> Output debug information"],["CAVEATS","This utility is **Debian/Ubuntu specific** and not available on other Linux distributions. Adding third-party repositories may introduce security risks."],["HISTORY","Developed for **Ubuntu** to simplify the process of adding PPAs (Personal Package Archives) and other third-party repositories."],["SEE ALSO","[apt](/man/apt)(8), [apt-get](/man/apt-get)(8), [apt-cache](/man/apt-cache)(8), [apt-key](/man/apt-key)(8)"],["RESOURCES","```[Source code](https://salsa.debian.org/debian/software-properties)```\n\n```[Documentation](https://manpages.ubuntu.com/manpages/noble/man1/add-apt-repository.1.html)```"]],"e":["apt","apt-get","apt-cache","apt-key"],"x":"add-apt-repository [repository_spec]"},"aict":{"n":"aict","t":"Unix-style tools with structured XML/JSON output for AI agents","s":[["TAGLINE","Unix-style tools with structured XML/JSON output for AI agents"],["TLDR","**List** a directory as **XML** (default)\n\n```aict ls [path]```\n\n**List** a directory as **JSON**\n\n```aict ls [path] --json```\n\n**List** with classic **plain** coreutils-style text\n\n```aict ls [path] --plain```\n\n**Search** recursively with structured hits\n\n```aict grep \"[pattern]\" [path] -r```\n\n**Show** file contents with metadata\n\n```aict cat [file]```\n\n**Diff** two files with typed changes\n\n```aict diff [old] [new]```\n\n**Run** the built-in **MCP** server (stdio)\n\n```aict mcp```\n\n**Show help** for all tools\n\n```aict --help```"],["SYNOPSIS","**aict** _tool_ [_args_] [_--xml_|_--json_|_--plain_]"],["DESCRIPTION","**aict** reimplements a suite of common Unix utilities so AI agents can consume **labeled structured output** instead of parsing human-oriented plaintext. By default tools emit XML; **--json** and **--plain** switch format. Fields use consistent conventions: absolute paths, Unix epoch timestamps (often with companion age fields), sizes in bytes plus human form, and structured error elements rather than free-form stderr for many failure cases.\n\nTools span file inspection (**cat**, **head**, **tail**, **file**, **stat**, **wc**), search and compare (**ls**, **find**, **grep**, **diff**), path helpers (**realpath**, **basename**, **dirname**, **pwd**), text processing (**sort**, **uniq**, **cut**, **tr**, **sed**, **awk**), data/archives (**jq**, **tar**), checksums, and system/environment queries (**env**, **system**, **ps**, **df**, **du**). Additional subcommands include **git** helpers, **completions**, **doctor**, and **mcp** for Model Context Protocol exposure over stdio.\n\naict is intentionally **read-only** (no network for MIME/language enrichment beyond local detection) and is written in Go. Structured output is denser in tokens than terse GNU tools but aims to reduce multi-step agent parse-and-retry loops. **--plain** skips enrichment when only raw content is needed."],["PARAMETERS","_tool_\n> One of the built-in tools (e.g. **ls**, **grep**, **cat**, **diff**, **find**, **stat**). See **aict --help** for the full list.\n\n**--xml**\n> Emit XML (default for agent-oriented use).\n\n**--json**\n> Emit JSON with the same structure as the XML form.\n\n**--plain**\n> Emit classic plaintext similar to the original Unix tool, without structured enrichment.\n\n**-r**\n> Recursive (where supported, e.g. **grep**).\n\n**mcp**\n> Start the MCP server over stdio so agents can call tools as native functions.\n\n**doctor**\n> Self-diagnostic for the install and environment.\n\n**completions**\n> Shell completion helpers (bash/zsh/fish).\n\n**--help**\n> List tools and options."],["ENVIRONMENT","**AICT_XML**\n> When set to **1**, prefer XML-oriented behavior globally for aict invocations (as documented by the project for agent environments)."],["CAVEATS","Not a drop-in performance replacement for GNU coreutils or **ripgrep**: enrichment (language/MIME detection, absolute paths) adds latency, especially on large trees or big **grep**/**cat** jobs. Some tools are platform-limited (**ps** is Linux-oriented; **system** is Linux/macOS). Prefer real **rg**/**eza** for human interactive speed; use aict when structured agent consumption is the goal."],["SEE ALSO","[ls](/man/ls)(1), [grep](/man/grep)(1), [cat](/man/cat)(1), [find](/man/find)(1), [jq](/man/jq)(1), [diff](/man/diff)(1)"],["RESOURCES","```[Source code](https://github.com/synseqack/aict)```\n\n```[Homepage](https://synseqack.github.io/aict/)```"]],"e":["ls","grep","cat","find","jq","diff"],"x":"aict ls [path]"},"alias":{"n":"alias","t":"Create shorthand names for commands","s":[["TAGLINE","Create shorthand names for commands"],["TLDR","**List** all aliases\n\n```alias```\n\n**Create** a simple alias\n\n```alias [ll]='ls -la'```\n\nCreate alias with **arguments**\n\n```alias [grep]='grep --color=auto'```\n\n**Remove** an alias\n\n```unalias [ll]```\n\nCreate **persistent** alias (add to ~/.bashrc or ~/.zshrc)\n\n```echo \"alias ll='ls -la'\" >> ~/.bashrc```"],["SYNOPSIS","**alias** [_name_[=_value_]...]"],["DESCRIPTION","**alias** is a shell builtin that creates shorthand names for commands or command sequences. When you type an alias name, the shell substitutes the associated value before execution.\n\nAliases are useful for creating shortcuts for frequently used commands with specific options, reducing typing and preventing repetitive mistakes."],["PARAMETERS","Without arguments, **alias** displays all defined aliases. With **name**, it shows that alias's definition. With **name=value**, it creates or updates an alias."],["CAVEATS","Aliases are not expanded in shell scripts by default. They don't accept arguments in the middle of the expansion (use functions for that). Aliases are shell-specific and must be defined in the shell's configuration file for persistence."],["PERSISTENT ALIASES","To make aliases permanent, add them to your shell configuration.\n\n**Bash**\n> ~/.bashrc or ~/.bash_aliases\n\n**Zsh**\n> ~/.zshrc or ~/.zsh_aliases\n\n**Fish**\n> Use **alias --save** or edit ~/.config/fish/config.fish"],["HISTORY","The **alias** command has been a feature of Unix shells since the C shell (csh) in the late **1970s**. It was adopted by Bash, Zsh, and most other modern shells with similar syntax."],["SEE ALSO","[unalias](/man/unalias)(1), [type](/man/type)(1), [function](/man/function)(1)"]],"e":["unalias","type","function"],"x":"alias"},"arpspoof":{"n":"arpspoof","t":"Forge ARP replies for man-in-the-middle interception.","s":[["TAGLINE","Forge ARP replies for man-in-the-middle interception."],["TLDR","**Poison** all hosts for target\n\n```sudo arpspoof -i wlan0 host_ip```\n\nPoison specific **target**\n\n```sudo arpspoof -i wlan0 -t target_ip host_ip```\n\n**Bidirectional** poisoning\n\n```sudo arpspoof -i wlan0 -r -t target_ip host_ip```"],["SYNOPSIS","**arpspoof** [_-i interface_] [_-c own_|_host_|_both_] [_-t target_] [_-r_] _host_"],["DESCRIPTION","**arpspoof** forges ARP replies to intercept network packets. It sends fake ARP responses to redirect traffic through the attacker's machine, enabling man-in-the-middle attacks for network analysis and security testing."],["PARAMETERS","**-i** _interface_\n> Network interface to use\n\n**-c** _own_|_host_|_both_\n> Which hardware address to use when restoring the ARP configuration on cleanup (default: own)\n\n**-t** _target_\n> Target IP address to poison (poisons all hosts if omitted). May be repeated to specify multiple targets\n\n**-r**\n> Poison both host and target to capture traffic in both directions (only valid in conjunction with -t)\n\n**host**\n> IP address of the host to impersonate (usually the gateway)"],["CAVEATS","This is a penetration testing tool that can disrupt network traffic. Only use on networks you own or have explicit authorization to test. Requires root privileges and IP forwarding enabled for traffic to flow."],["HISTORY","**arpspoof** is part of the **dsniff** package, a collection of network auditing tools for penetration testing and security analysis."],["INSTALL","```apt: sudo apt install dsniff```\n\n```dnf: sudo dnf install dsniff```\n\n```pacman: sudo pacman -S dsniff```\n\n```nix: nix profile install nixpkgs#dsniff```"],["SEE ALSO","[dsniff](/man/dsniff)(8), [tcpdump](/man/tcpdump)(8), [ettercap](/man/ettercap)(8)"],["RESOURCES","```[Source code](https://github.com/tecknicaltom/dsniff)```\n\n```[Homepage](https://www.monkey.org/~dugsong/dsniff/)```"]],"e":["dsniff","tcpdump","ettercap"],"x":"sudo arpspoof -i wlan0 host_ip"},"attr":{"n":"attr","t":"Manipulate extended attributes on filesystem objects","s":[["TAGLINE","Manipulate extended attributes on filesystem objects"],["TLDR","**Set an extended attribute on a file**\n\n```attr -s [attribute_name] -V [value] [path/to/file]```\n\n**Get the value of an extended attribute**\n\n```attr -g [attribute_name] [path/to/file]```\n\n**Remove an extended attribute**\n\n```attr -r [attribute_name] [path/to/file]```\n\n**List all extended attributes on a file**\n\n```attr -l [path/to/file]```\n\n**Set attribute in the user namespace**\n\n```setfattr -n user.[attribute_name] -v [value] [path/to/file]```\n\n**Get all attributes with getfattr**\n\n```getfattr -d [path/to/file]```"],["SYNOPSIS","**attr** [**-LRSq**] **-s** _attrname_ [**-V** _attrvalue_] _pathname_\n\n**attr** [**-LRSq**] **-g** _attrname_ _pathname_\n\n**attr** [**-LRSq**] **-r** _attrname_ _pathname_\n\n**attr** [**-LRSq**] **-l** _pathname_"],["PARAMETERS","**-s** _attrname_\n> Set the named attribute to the value read from stdin. If the attribute already exists, its value is replaced. Combine with **-V** to supply the value on the command line instead.\n\n**-g** _attrname_\n> Get and print to stdout the value associated with the named attribute.\n\n**-r** _attrname_\n> Remove the named attribute if it exists.\n\n**-l**\n> List the names of all attributes on the object together with the number of bytes in each value.\n\n**-V** _attrvalue_\n> Provide the value for a **-s** operation directly, so stdin is not read.\n\n**-L**\n> When the object is a symbolic link, operate on the target of the link rather than the link itself.\n\n**-R**\n> Operate in the root (trusted) attribute namespace instead of the default user namespace. Requires privileges.\n\n**-S**\n> Operate in the security attribute namespace.\n\n**-q**\n> Quiet mode; print error messages to stderr but suppress status messages on stdout."],["DESCRIPTION","**attr** manipulates extended attributes on filesystem objects. Extended attributes are name-value pairs permanently associated with files and directories, providing metadata storage beyond traditional Unix permissions.\n\nAttributes exist in namespaces: **user** for arbitrary user data, **system** for OS features like ACLs, **security** for SELinux labels, and **trusted** for privileged applications. The attr command primarily works with the user namespace.\n\nExtended attributes persist across copy and backup operations when tools support them. Common uses include storing file metadata, checksums, capabilities, and application-specific data without modifying file content.\n\nThe related commands **getfattr** and **setfattr** provide more detailed control and are preferred for scripting."],["CAVEATS","Not all filesystems support extended attributes; ext4, XFS, and Btrfs do when mounted with user_xattr. Attribute size limits vary by filesystem (typically 64KB). Many backup and file transfer tools do not preserve extended attributes by default. The user namespace is limited to processes with read/write access to the file."],["HISTORY","Extended attributes were added to Linux in kernel **2.6** (2003), inspired by similar features in IRIX and other Unix systems. The implementation followed the POSIX 1003.1e draft standard for extended security attributes. The **attr** package providing these utilities has been the standard interface for managing extended attributes since their introduction."],["INSTALL","```apt: sudo apt install attr```\n\n```dnf: sudo dnf install attr```\n\n```pacman: sudo pacman -S attr```\n\n```apk: sudo apk add attr```\n\n```zypper: sudo zypper install attr```\n\n```brew: brew install attr```\n\n```nix: nix profile install nixpkgs#attr```"],["SEE ALSO","[getfattr](/man/getfattr)(1), [setfattr](/man/setfattr)(1), [getfacl](/man/getfacl)(1), [setfacl](/man/setfacl)(1), [lsattr](/man/lsattr)(1), [chattr](/man/chattr)(1)"],["RESOURCES","```[Source code](https://git.savannah.nongnu.org/git/attr.git)```\n\n```[Homepage](https://savannah.nongnu.org/projects/attr)```"]],"e":["getfattr","setfattr","getfacl","setfacl","lsattr","chattr"],"x":"attr -s [attribute_name] -V [value] [path/to/file]"},"aws-kafka":{"n":"aws-kafka","t":"Manage Amazon Managed Streaming for Apache Kafka clusters.","s":[["TAGLINE","Manage Amazon Managed Streaming for Apache Kafka clusters."],["TLDR","**Create a new MSK cluster**\n\n```aws kafka create-cluster --cluster-name [my-cluster] --kafka-version [3.5.1] --number-of-broker-nodes 3 --broker-node-group-info file://[broker-config.json]```\n\n**List all MSK clusters**\n\n```aws kafka list-clusters```\n\n**Describe a cluster** and get its details\n\n```aws kafka describe-cluster --cluster-arn [arn:aws:kafka:region:account:cluster/name/id]```\n\n**Get bootstrap broker endpoints** for client connections\n\n```aws kafka get-bootstrap-brokers --cluster-arn [arn:aws:kafka:region:account:cluster/name/id]```\n\n**Update the number of brokers**\n\n```aws kafka update-broker-count --cluster-arn [arn:aws:kafka:region:account:cluster/name/id] --current-version [K1234] --target-number-of-broker-nodes 6```\n\n**List** all MSK configurations\n\n```aws kafka list-configurations```\n\n**List compatible Kafka versions** for upgrade\n\n```aws kafka get-compatible-kafka-versions --cluster-arn [arn:aws:kafka:region:account:cluster/name/id]```\n\n**Delete a cluster**\n\n```aws kafka delete-cluster --cluster-arn [arn:aws:kafka:region:account:cluster/name/id]```"],["SYNOPSIS","**aws kafka** _command_ [_options_]"],["DESCRIPTION","**aws kafka** is the AWS CLI interface for Amazon Managed Streaming for Apache Kafka (MSK), a fully managed service for running Apache Kafka. MSK handles cluster provisioning, configuration, patching, and monitoring while maintaining compatibility with open-source Kafka.\n\nMSK supports both provisioned and serverless deployment modes, SASL/SCRAM and IAM authentication, encryption at rest and in transit, and integration with AWS services like Lambda, Kinesis Data Firehose, and Glue."],["COMMANDS","**create-cluster**\n> Create a new MSK cluster\n\n**delete-cluster**\n> Delete an MSK cluster\n\n**describe-cluster**\n> Get cluster configuration and status\n\n**list-clusters**\n> List all clusters in the account\n\n**get-bootstrap-brokers**\n> Get broker endpoints for client connections\n\n**update-broker-count**\n> Scale the number of brokers\n\n**update-broker-storage**\n> Increase broker storage capacity\n\n**update-cluster-kafka-version**\n> Upgrade Kafka version\n\n**create-configuration**\n> Create a custom broker configuration\n\n**reboot-broker**\n> Restart a specific broker\n\n**update-cluster-configuration**\n> Update the cluster's broker configuration\n\n**list-cluster-operations**\n> List cluster operations\n\n**list-configurations**\n> List all MSK configurations"],["CAVEATS","Broker count can only be increased, not decreased. Storage can only be increased. Kafka version upgrades are one-way and cannot be rolled back. Cluster creation takes 15-20 minutes. MSK Serverless has different quotas than provisioned clusters."],["HISTORY","Amazon MSK launched in **December 2018** as a managed Kafka service. MSK Connect for managed connectors was added in **2021**, and MSK Serverless launched in **2022** for automatic scaling without capacity planning. Tiered storage for cost optimization came in **2023**."],["INSTALL","```dnf: sudo dnf install aws```\n\n```pacman: sudo pacman -S aws-cli```\n\n```apk: sudo apk add aws-cli```\n\n```zypper: sudo zypper install aws-cli```"],["SEE ALSO","[aws](/man/aws)(1), [aws-kinesis](/man/aws-kinesis)(1), [kafka-topics](/man/kafka-topics)(1)"]],"e":["aws","aws-kinesis","kafka-topics"],"x":"aws kafka create-cluster --cluster-name [my-cluster] --kafka-version [3.5.1] --number-of-broker-nodes 3 --broker-node-group-info file://[broker-config.json]"},"chafa":{"n":"chafa","t":"terminal image viewer using character art","s":[["TAGLINE","terminal image viewer using character art"],["TLDR","**Display image in terminal**\n\n```chafa [image.png]```\n\n**Specify output size**\n\n```chafa -s [80x40] [image.jpg]```\n\n**Use sixel graphics**\n\n```chafa --format sixels [image.png]```\n\n**Use kitty graphics protocol**\n\n```chafa --format kitty [image.gif]```\n\n**Limit colors**\n\n```chafa --colors [256] [image.png]```\n\n**Display animated GIF**\n\n```chafa --animate [animation.gif]```\n\n**ASCII only output**\n\n```chafa --symbols ascii [image.jpg]```"],["SYNOPSIS","**chafa** [_options_] _image_..."],["DESCRIPTION","**chafa** converts images to character art for display directly in the terminal. It uses Unicode block characters, braille patterns, and ASCII art to approximate images at the resolution of the terminal grid.\n\nFor terminals with advanced graphics support, chafa can output using the Sixel, Kitty, or iTerm2 graphics protocols, producing much higher fidelity results. It handles a wide range of image formats including JPEG, PNG, GIF (with animation), AVIF, SVG, WebP, TIFF, and JPEG XL.\n\nOutput can be tuned by adjusting size, color depth, symbol sets, and animation speed. chafa automatically detects terminal capabilities and selects the best available output mode."],["PARAMETERS","**-s**, **--size** _WxH_\n> Output size in characters\n\n**-f**, **--format** _format_\n> Output format: symbols, sixels, kitty, iterm\n\n**-c**, **--colors** _n_\n> Color mode: none, 2, 8, 16, 256, full\n\n**--symbols** _set_\n> Symbol set: all, ascii, block, braille\n\n**--fg** _color_\n> Foreground color\n\n**--bg** _color_\n> Background color\n\n**--animate**\n> Enable animation for GIFs\n\n**--duration** _secs_\n> Animation duration\n\n**--speed** _factor_\n> Animation speed multiplier\n\n**-w**, **--watch**\n> Watch file for changes\n\n**--font-ratio** _ratio_\n> Font width/height ratio"],["SUPPORTED FORMATS","JPEG, PNG, GIF, AVIF, SVG, TIFF, WebP, JPEG XL, QOI, XWD"],["CAVEATS","Output quality depends on terminal capabilities. Sixels require compatible terminal. Best results with Truecolor support."],["INSTALL","```apt: sudo apt install chafa```\n\n```dnf: sudo dnf install chafa```\n\n```pacman: sudo pacman -S chafa```\n\n```apk: sudo apk add chafa```\n\n```zypper: sudo zypper install chafa```\n\n```brew: brew install chafa```\n\n```nix: nix profile install nixpkgs#chafa```"],["SEE ALSO","[catimg](/man/catimg)(1), [timg](/man/timg)(1), [img2txt](/man/img2txt)(1)"],["RESOURCES","```[Source code](https://github.com/hpjansson/chafa)```\n\n```[Homepage](https://hpjansson.org/chafa/)```"]],"e":["catimg","timg","img2txt"],"x":"chafa [image.png]"},"cluster":{"n":"cluster","t":"graph cluster layout tool","s":[["TAGLINE","graph cluster layout tool"],["TLDR","**Generate a cluster layout** from a graph\n\n```cluster [input.dot] -o [output.dot]```\n\n**Cluster with specific algorithm**\n\n```cluster -C [algorithm] [input.dot]```"],["SYNOPSIS","**cluster** [_options_] [_files_]"],["DESCRIPTION","**cluster** is a graph layout tool from the Graphviz package that identifies and highlights clusters (communities or groups) within graph structures. It reads graphs in DOT format and annotates them with cluster information.\n\nThe tool analyzes the connectivity of a graph to detect natural groupings of nodes. These groupings are then represented as subgraph clusters in the output, which can be further processed by other Graphviz layout engines like dot or neato for visualization.\n\nIt is part of the Graphviz suite alongside dot, neato, fdp, sfdp, twopi, and circo, each providing different layout algorithms for graph visualization."],["INSTALL","```apt: sudo apt install graphviz```\n\n```dnf: sudo dnf install graphviz```\n\n```pacman: sudo pacman -S graphviz```\n\n```apk: sudo apk add graphviz```\n\n```zypper: sudo zypper install graphviz```\n\n```brew: brew install graphviz```\n\n```nix: nix profile install nixpkgs#graphviz```"],["SEE ALSO","[dot](/man/dot)(1), [neato](/man/neato)(1), [graphviz](/man/graphviz)(1)"],["RESOURCES","```[Source code](https://gitlab.com/graphviz/graphviz)```\n\n```[Homepage](https://graphviz.org/)```"]],"e":["dot","neato","graphviz"],"x":"cluster [input.dot] -o [output.dot]"},"couchdb":{"n":"couchdb","t":"Document-oriented NoSQL database server","s":[["TAGLINE","Document-oriented NoSQL database server"],["TLDR","**Start CouchDB** server\n\n```couchdb```\n\n**Start in the background**\n\n```couchdb -b```\n\n**Start with a specific configuration** file\n\n```couchdb -c [/path/to/local.ini]```\n\n**Check if CouchDB is running**\n\n```curl http://127.0.0.1:5984/```\n\n**Stop CouchDB** gracefully\n\n```couchdb -d```\n\n**Restart with a delay** if the process crashes\n\n```couchdb -b -r [5]```"],["SYNOPSIS","**couchdb** [_options_]"],["PARAMETERS","**-b**\n> Start in background (detached mode).\n\n**-d**\n> Shutdown a running CouchDB instance.\n\n**-c** _FILE_\n> Use additional configuration file.\n\n**-i**\n> Start an interactive Erlang shell.\n\n**-o** _FILE_\n> Redirect stdout to the specified file.\n\n**-e** _FILE_\n> Redirect stderr to the specified file.\n\n**-p** _FILE_\n> Create PID file at the specified path.\n\n**-r** _SECONDS_\n> Respawn CouchDB after crash with delay.\n\n**-s**\n> Display the status of a background CouchDB process.\n\n**-k**\n> Kill a background CouchDB process (will respawn if -r is set).\n\n**-V**\n> Display version information and exit."],["DESCRIPTION","**Apache CouchDB** is a document-oriented NoSQL database that uses JSON for documents, JavaScript for MapReduce queries, and HTTP for its API. The **couchdb** command starts the database server.\n\nCouchDB is designed for reliability with Multi-Version Concurrency Control (MVCC), ensuring data integrity without locking. It features built-in replication for distributed deployments, making it suitable for offline-first applications that sync when connectivity is available.\n\nThe database provides a RESTful HTTP API for all operations, from creating databases and documents to running queries. The built-in web interface, Fauxton, allows administration and querying through a browser at http://localhost:5984/_utils/."],["CONFIGURATION","**/etc/couchdb/local.ini**\n> Local configuration overrides for network binding, authentication, logging, and performance tuning.\n\n**/etc/couchdb/default.ini**\n> Default system configuration (should not be edited directly; use local.ini for overrides)."],["CAVEATS","Default installation binds to localhost only. For remote access, configuration changes are required in local.ini. The admin party mode (no authentication) is insecure for production. Compaction should be run periodically to reclaim disk space."],["HISTORY","CouchDB was created by Damien Katz, who began the project in **2005** using C++, then rewrote it in Erlang in **2008**. It became an Apache project in **2008** and graduated to top-level project status in **2010**. The name refers to sitting on your \"couch\" and relaxing."],["INSTALL","```pacman: sudo pacman -S couchdb```\n\n```brew: brew install couchdb```"],["SEE ALSO","[curl](/man/curl)(1), [mongod](/man/mongod)(1), [redis-server](/man/redis-server)(1)"]],"e":["curl","mongod","redis-server"],"x":"couchdb"},"cucumber":{"n":"cucumber","t":"behavior-driven development test runner","s":[["TAGLINE","behavior-driven development test runner"],["TLDR","**Run all features**\n\n```cucumber```\n\n**Run specific feature file**\n\n```cucumber [features/login.feature]```\n\n**Run specific scenario by line**\n\n```cucumber [features/login.feature]:[10]```\n\n**Run with specific tag**\n\n```cucumber --tags @[smoke]```\n\n**Run excluding tag**\n\n```cucumber --tags \"not @[wip]\"```\n\n**Generate HTML report**\n\n```cucumber --format html --out [report.html]```\n\n**Dry run** (check syntax only)\n\n```cucumber --dry-run```"],["SYNOPSIS","**cucumber** [_options_] [_files_|_dirs_]"],["DESCRIPTION","**cucumber** runs Behavior-Driven Development (BDD) tests written in Gherkin, a human-readable language for describing software behavior. It connects plain-language scenarios to executable step definitions.\n\nFeatures describe behavior in Given/When/Then format that both developers and stakeholders can understand. Step definitions in Ruby (or other languages) implement the actual test logic."],["PARAMETERS","**-t**, **--tags** _expr_\n> Run scenarios matching tag expression.\n\n**-f**, **--format** _type_\n> Output format (pretty, progress, html, json).\n\n**-o**, **--out** _file_\n> Write output to file.\n\n**-r**, **--require** _path_\n> Require files before execution.\n\n**-d**, **--dry-run**\n> Check syntax without running.\n\n**-s**, **--strict**\n> Fail on undefined or pending steps.\n\n**--retry** _n_\n> Retry failing scenarios n times.\n\n**-p**, **--profile** _name_\n> Use named profile from cucumber.yml.\n\n**--order** _type_\n> Run order (defined, random)."],["CONFIGURATION","**cucumber.yml**\n> Defines reusable profiles with pre-configured options for different test runs."],["CAVEATS","Step definitions must match scenario steps exactly. Shared state between steps can cause flaky tests. Gherkin syntax errors stop execution. Slow for large test suites."],["HISTORY","**Cucumber** was created by **Aslak Hellesoy** in **2008**, inspired by RSpec and JBehave. It popularized BDD by enabling executable specifications in plain language. Originally Ruby-based, implementations now exist for Java, JavaScript, and many other languages."],["INSTALL","```nix: nix profile install nixpkgs#cucumber```"],["SEE ALSO","[rspec](/man/rspec)(1), [jest](/man/jest)(1), [pytest](/man/pytest)(1)"]],"e":["rspec","jest","pytest"],"x":"cucumber"},"dcode":{"n":"dcode","t":"recursively detect and decode encoded strings","s":[["TAGLINE","recursively detect and decode encoded strings"],["TLDR","**Detect and decode** a string recursively\n\n```dcode \"[NjM3YTQyNzQ1YTQ0NGUzMg==]\"```\n\n**Decode a Caesar cipher** with a known offset\n\n```dcode -rot [11] \"[spwwz hzcwo]\"```\n\n**Try all 26 offsets** of a Caesar cipher\n\n```dcode -rot all \"[bpgkta xh qtiitg iwpc sr]\"```\n\n**Reverse** a string\n\n```dcode -rev \"[hello world]\"```\n\n**Decode without third-party services** (skip online hash lookups)\n\n```dcode -s \"[string]\"```"],["SYNOPSIS","**dcode** [_options_] \"_string_\""],["PARAMETERS","**-rot** _offset_|**all**\n> Decode a Caesar cipher with the given offset, or try all 26 offsets.\n\n**-rev**\n> Reverse the supplied string.\n\n**-s**\n> Safe mode: do not query third-party web services for hash lookups."],["DESCRIPTION","**dcode** is the command provided by **Decodify**, a tool that detects the encoding of a string and decodes it, recursively. If a decoded result is itself encoded, Decodify keeps going until it reaches plain text.\n\nSupported encodings and ciphers include Base64, hexadecimal, decimal, binary, URL encoding, FromChar, and Caesar ciphers. It can also identify MD5, SHA1, and SHA2 hashes and attempt to resolve them via online hash lookup services.\n\nThe tool is popular for CTF challenges and security analysis, where layered or unknown encodings are common and manually chaining base64, xxd, and similar tools is tedious."],["CAVEATS","Hash \"decoding\" relies on third-party web lookup services; use -s to keep sensitive data offline. Encoding detection is heuristic and can misidentify short or ambiguous strings. Written in Python and installed from source (git clone, then make install)."],["HISTORY","Decodify was created by security researcher Somdev Sangwan (s0md3v), also known for tools like XSStrike and Photon."],["SEE ALSO","[base64](/man/base64)(1), [xxd](/man/xxd)(1), [uudecode](/man/uudecode)(1)"],["RESOURCES","```[Source code](https://github.com/s0md3v/Decodify)```"]],"e":["base64","xxd","uudecode"],"x":"dcode \"[NjM3YTQyNzQ1YTQ0NGUzMg==]\""},"decode-dimms":{"n":"decode-dimms","t":"decode and display RAM SPD EEPROM information","s":[["TAGLINE","decode and display RAM SPD EEPROM information"],["TLDR","Display **DIMM information**\n\n```decode-dimms```\n\nDisplay DIMMs **side-by-side** for comparison\n\n```decode-dimms --side-by-side```\n\nDecode even if **checksum fails**\n\n```decode-dimms -c```\n\nRead from **hexdump files** instead of live system\n\n```decode-dimms -x```\n\nDisplay **help**\n\n```decode-dimms -h```"],["SYNOPSIS","**decode-dimms** [_options_]"],["DESCRIPTION","**decode-dimms** decodes and displays information stored in the SPD (Serial Presence Detect) EEPROM of RAM modules. This includes memory type, speed, manufacturer, size, and timing information.\n\nUseful for identifying installed memory specifications and verifying RAM compatibility."],["PARAMETERS","**-h**, **--help**\n> Display usage summary.\n\n**-c**, **--checksum**\n> Decode completely even if checksum fails.\n\n**-f**, **--format**\n> Print output in HTML format.\n\n**-b**, **--bodyonly**\n> Don't print HTML header (useful for postprocessing).\n\n**--side-by-side**\n> Display all DIMMs side-by-side if possible.\n\n**-x**\n> Read data from hexdump files instead of the running system.\n\n**-X**\n> Same as -x but treat multibyte hex data as little endian."],["CAVEATS","Requires the eeprom, at24, or ee1004 (for DDR4) kernel module loaded. May need i2c kernel modules. Requires root privileges or proper permissions on i2c devices. Part of the i2c-tools package."],["INSTALL","```apt: sudo apt install i2c-tools```\n\n```dnf: sudo dnf install i2c-tools```\n\n```pacman: sudo pacman -S i2c-tools```\n\n```apk: sudo apk add i2c-tools```\n\n```zypper: sudo zypper install i2c-tools```\n\n```brew: brew install i2c-tools```\n\n```nix: nix profile install nixpkgs#i2c-tools```"],["SEE ALSO","[dmidecode](/man/dmidecode)(8), [lshw](/man/lshw)(1), [free](/man/free)(1), [i2cdetect](/man/i2cdetect)(8)"]],"e":["dmidecode","lshw","free","i2cdetect"],"x":"decode-dimms"},"depmod":{"n":"depmod","t":"generate kernel module dependency files","s":[["TAGLINE","generate kernel module dependency files"],["TLDR","**Generate module dependencies** for the running kernel\n\n```depmod```\n\n**Generate for a specific kernel version**\n\n```depmod [6.8.0-generic]```\n\n**Probe all modules** (the default when no filename is given)\n\n```depmod -a```\n\n**Quick mode**: regenerate only if modules are newer than modules.dep\n\n```depmod -A```\n\n**Preview without writing** (prints to stdout)\n\n```depmod -n```\n\n**Use a staging module tree root**\n\n```depmod -b [/path/to/root] [6.8.0-generic]```\n\n**Report unresolved symbols** against a System.map\n\n```depmod -e -F [/boot/System.map-6.8.0] [6.8.0-generic]```"],["SYNOPSIS","**depmod** [**-aAenvw**] [**-b** _basedir_] [**-F** _System.map_] [**-C** _config_] [_kernel_version_]\n\n**depmod** [_options_] _module_files_..."],["PARAMETERS","_KERNEL_VERSION_\n> Kernel version to process. Defaults to the running kernel (`uname -r`).\n\n**-a**, **--all**\n> Probe all modules. Enabled by default when no filenames are given.\n\n**-A**, **--quick**\n> Exit silently unless a module is newer than modules.dep.\n\n**-b**, **--basedir** _DIR_\n> Prepend _DIR_ to /lib/modules/<version>; useful for staging roots.\n\n**-o**, **--outdir** _DIR_\n> Write generated files to _DIR_ instead of the module tree.\n\n**-e**, **--errsyms**\n> Report symbols a module requires that are not provided by other modules or the kernel.\n\n**-F**, **--filesyms** _System.map_\n> Use _System.map_ to resolve kernel-provided symbols when using **-e**.\n\n**-E**, **--symvers** _Module.symvers_\n> With **-e**, also report symbol version (modversion) mismatches.\n\n**-n**, **--show**, **--dry-run**\n> Print resulting modules.dep and map files to stdout instead of writing them.\n\n**-v**, **--verbose**\n> Print each symbol dependency and providing module.\n\n**-w**\n> Warn about duplicate dependencies, aliases, or symbol versions.\n\n**-C**, **--config** _FILE_\n> Override the default configuration file or directory.\n\n**-V**, **--version**\n> Show version and exit.\n\n**-h**, **--help**\n> Display help information."],["DESCRIPTION","**depmod** generates module dependency files (modules.dep and related files) used by modprobe to load kernel modules and their dependencies automatically. It analyzes all modules in the kernel module directory and determines their interdependencies.\n\nThe tool scans module files for exported and required symbols, building a dependency tree. This information allows modprobe to load prerequisite modules automatically when a module is requested.\n\ndepmod is typically run automatically during kernel installation or module updates. The generated files are stored in /lib/modules/<kernel_version>/."],["CONFIGURATION","**/etc/depmod.conf** or **/etc/depmod.d/*.conf**\n> Configuration files specifying module search paths and overrides."],["CAVEATS","Requires root privileges to write dependency files. Should be run after adding or removing kernel modules. Running kernel modules cannot be unloaded without considering running processes."],["HISTORY","depmod is part of the **kmod** utilities (formerly module-init-tools), used for Linux kernel module management since the early days of loadable kernel modules in the 1990s."],["INSTALL","```apt: sudo apt install kmod```\n\n```dnf: sudo dnf install kmod```\n\n```pacman: sudo pacman -S kmod```\n\n```apk: sudo apk add kmod```\n\n```zypper: sudo zypper install kmod```\n\n```brew: brew install kmod```\n\n```nix: nix profile install nixpkgs#kmod```"],["SEE ALSO","[modprobe](/man/modprobe)(8), [lsmod](/man/lsmod)(8), [insmod](/man/insmod)(8), [rmmod](/man/rmmod)(8), [kmod](/man/kmod)(8)"]],"e":["modprobe","lsmod","insmod","rmmod","kmod"],"x":"depmod"},"diskonaut":{"n":"diskonaut","t":"terminal-based disk space navigator with treemap visualization","s":[["TAGLINE","terminal-based disk space navigator with treemap visualization"],["TLDR","**Analyze current directory**\n\n```diskonaut```\n\n**Analyze specific directory**\n\n```diskonaut [/path/to/directory]```\n\n**Show apparent size** instead of disk usage\n\n```diskonaut --apparent-size [directory]```\n\n**Disable the delete confirmation** prompt\n\n```diskonaut --disable-delete-confirmation [directory]```"],["SYNOPSIS","**diskonaut** [_options_] [_folder_]"],["PARAMETERS","_FOLDER_\n> Directory to analyze (defaults to current directory).\n\n**-a**, **--apparent-size**\n> Show apparent file size instead of disk usage.\n\n**-d**, **--disable-delete-confirmation**\n> Skip the confirmation prompt when deleting files or folders.\n\n**-h**, **--help**\n> Display help information.\n\n**-V**, **--version**\n> Display version information."],["DESCRIPTION","**diskonaut** is a terminal-based disk space navigator that displays directory sizes in an interactive treemap visualization. It allows exploring disk usage with keyboard navigation to quickly identify space-consuming directories and files.\n\nThe treemap view represents directories as nested rectangles proportional to their size, providing an intuitive visual overview of disk space distribution. Navigation allows drilling into directories and viewing detailed size information.\n\ndiskonaut is written in Rust and designed for performance, capable of quickly analyzing large directory trees while maintaining a responsive interface."],["CAVEATS","Requires terminal with sufficient size for meaningful visualization. Very deep directory structures may be difficult to navigate. Initial scan of large filesystems takes time."],["HISTORY","diskonaut was created as a modern, terminal-based alternative to graphical disk usage analyzers. Written in Rust, it combines the accessibility of TUI applications with visual treemap representations popular in GUI tools."],["INSTALL","```dnf: sudo dnf install diskonaut```\n\n```pacman: sudo pacman -S diskonaut```\n\n```brew: brew install diskonaut```"],["SEE ALSO","[ncdu](/man/ncdu)(1), [du](/man/du)(1), [duf](/man/duf)(1), [dust](/man/dust)(1)"],["RESOURCES","```[Source code](https://github.com/imsnif/diskonaut)```"]],"e":["ncdu","du","duf","dust"],"x":"diskonaut"},"doctl-compute-droplet":{"n":"doctl-compute-droplet","t":"manage DigitalOcean Droplet virtual machines","s":[["TAGLINE","manage DigitalOcean Droplet virtual machines"],["TLDR","**List all droplets**\n\n```doctl compute droplet list```\n\n**Create a droplet**\n\n```doctl compute droplet create [name] --size [s-1vcpu-1gb] --image [ubuntu-22-04-x64] --region [nyc1]```\n\n**Get droplet details**\n\n```doctl compute droplet get [droplet_id]```\n\n**Delete a droplet**\n\n```doctl compute droplet delete [droplet_id]```\n\n**SSH into droplet**\n\n```doctl compute ssh [droplet_name]```\n\n**List snapshots** of a droplet\n\n```doctl compute droplet snapshots [droplet_id]```\n\n**Create a droplet with tags** and monitoring\n\n```doctl compute droplet create [name] --size [s-1vcpu-1gb] --image [ubuntu-22-04-x64] --region [nyc1] --tag-name [web] --enable-monitoring```"],["SYNOPSIS","**doctl compute droplet** _command_ [_options_]"],["PARAMETERS","_COMMAND_\n> Operation: list, create, get, delete, ssh, etc.\n\n**list**\n> List all droplets.\n\n**create** _NAME_\n> Create new droplet.\n\n**--size** _SLUG_\n> Droplet size slug.\n\n**--image** _IMAGE_\n> OS image slug or ID.\n\n**--region** _REGION_\n> Data center region.\n\n**--ssh-keys** _IDS_\n> SSH key IDs or fingerprints.\n\n**--vpc-uuid** _UUID_\n> UUID of a non-default VPC to create the Droplet in.\n\n**--enable-backups**\n> Enable automatic daily backups.\n\n**--enable-monitoring**\n> Install the DigitalOcean monitoring agent.\n\n**--tag-name** _TAG_\n> Apply a tag to the Droplet.\n\n**--user-data** _DATA_\n> Shell script or cloud-init data to run on first boot.\n\n**--wait**\n> Wait for Droplet creation to complete before returning.\n\n**delete** _ID_\n> Delete droplet.\n\n**ssh** _NAME_\n> SSH into droplet.\n\n**--help**\n> Display help information."],["DESCRIPTION","**doctl compute droplet** manages DigitalOcean Droplets (virtual machines). It provides full lifecycle control including creation, configuration, monitoring, and deletion of cloud servers.\n\nDroplets can be created with various configurations specifying size, region, image, and networking options. The command supports actions like reboot, power cycle, resize, and snapshot creation.\n\nIntegration with SSH keys enables secure access, and the ssh subcommand provides convenient direct connection to droplets by name."],["CAVEATS","Droplet creation incurs charges immediately. Deletion is irreversible. Some actions require droplet power-off. Region availability varies for sizes and images."],["HISTORY","doctl compute droplet is a core component of the **doctl** CLI, providing command-line management of DigitalOcean's primary compute service since the tool's initial release."],["SEE ALSO","[doctl](/man/doctl)(1), [ssh](/man/ssh)(1)"]],"e":["doctl","ssh"],"x":"doctl compute droplet list"},"elasticsearch-reset-password":{"n":"elasticsearch-reset-password","t":"Reset passwords for built-in Elasticsearch users","s":[["TAGLINE","Reset passwords for built-in Elasticsearch users"],["TLDR","**Reset elastic user password**\n\n```elasticsearch-reset-password -u elastic```\n\n**Reset with auto-generated password**\n\n```elasticsearch-reset-password -u [username] -a```\n\n**Reset interactively**\n\n```elasticsearch-reset-password -u [username] -i```\n\n**Specify batch mode**\n\n```elasticsearch-reset-password -u [username] -b```"],["SYNOPSIS","**elasticsearch-reset-password** **-u** _username_ [_options_]"],["PARAMETERS","**-u**, **--username** _user_\n> Username to reset.\n\n**-a**, **--auto**\n> Reset the password to an auto-generated strong password. This is the default.\n\n**-i**, **--interactive**\n> Prompt for the new password instead of generating one.\n\n**-b**, **--batch**\n> Run without prompting for confirmation.\n\n**-f**, **--force**\n> Run even against an unhealthy cluster.\n\n**--url** _url_\n> Base URL for the Elasticsearch API. Defaults to the value derived from elasticsearch.yml.\n\n**-E** _setting=value_\n> Configure a standard Elasticsearch or X-Pack setting.\n\n**-s**, **--silent**\n> Show minimal output.\n\n**-v**, **--verbose**\n> Show verbose output."],["DESCRIPTION","**elasticsearch-reset-password** resets the password of a native realm user or one of the built-in users such as **elastic** or **kibana_system**. It is the recommended way to recover access when the elastic password is lost.\n\nThe tool must be run on a node of the cluster, as the user that owns the Elasticsearch files, and it needs the cluster to be running. Without **-i** it prints a freshly generated password to the console; that output is the only time the password is shown."],["CAVEATS","Only works when the Elasticsearch security features are enabled, which is the default from version 8. The command talks to the cluster over HTTPS, so a cluster that is down or unreachable cannot have its passwords reset unless **-f** is used against an unhealthy but running cluster."],["SEE ALSO","[elasticsearch](/man/elasticsearch)(1), [elasticsearch-users](/man/elasticsearch-users)(1)"],["RESOURCES","```[Source code](https://github.com/elastic/elasticsearch)```\n\n```[Homepage](https://www.elastic.co/elasticsearch)```\n\n```[Documentation](https://www.elastic.co/docs/reference/elasticsearch/command-line-tools/reset-password)```"]],"e":["elasticsearch","elasticsearch-users"],"x":"elasticsearch-reset-password -u elastic"},"eqn2graph":{"n":"eqn2graph","t":"Equation description to graphic image converter","s":[["TAGLINE","Equation description to graphic image converter"],["TLDR","**Convert equation** to image\n\n```echo \"x = {-b +- sqrt{b sup 2 - 4ac}} over 2a\" | eqn2graph > [equation.png]```\n\n**Specify output format**\n\n```echo \"[equation]\" | eqn2graph --format [svg]```\n\n**Set font size**\n\n```echo \"[equation]\" | eqn2graph -s [14]```"],["SYNOPSIS","**eqn2graph** [_options_]"],["PARAMETERS","**--format** _FORMAT_\n> Output format: png, svg, pdf, etc.\n\n**-s** _SIZE_\n> Font point size.\n\n**-unsafe**\n> Allow unsafe operations.\n\n**-v**\n> Verbose mode."],["DESCRIPTION","**eqn2graph** converts eqn equation descriptions into graphic images. It's a wrapper that processes eqn input through groff and converts the output to various image formats.\n\nThe tool reads equation text from stdin using eqn syntax and produces image files suitable for embedding in documents, web pages, or presentations.\n\neqn2graph is useful for generating standalone equation images without full document processing."],["CAVEATS","Requires groff and an image conversion tool (typically GraphicsMagick or ImageMagick). The default output format is PNG. Uses eqn syntax, not LaTeX math notation."],["HISTORY","eqn2graph is part of the **groff** suite, providing a convenient way to generate equation images from eqn's preprocessor language."],["INSTALL","```apt: sudo apt install groff```\n\n```dnf: sudo dnf install groff```\n\n```pacman: sudo pacman -S groff```\n\n```apk: sudo apk add groff```\n\n```zypper: sudo zypper install groff```\n\n```brew: brew install groff```\n\n```nix: nix profile install nixpkgs#groff```"],["SEE ALSO","[eqn](/man/eqn)(1), [groff](/man/groff)(1)"]],"e":["eqn","groff"],"x":"echo \"x = {-b +- sqrt{b sup 2 - 4ac}} over 2a\" | eqn2graph > [equation.png]"},"exclamation":{"n":"exclamation","t":"shell history expansion syntax","s":[["TAGLINE","shell history expansion syntax"],["TLDR","**Repeat** the last command\n\n```!!```\n\n**Rerun** the last command with sudo\n\n```sudo !!```\n\n**Repeat** the last command starting with a string\n\n```![string]```\n\n**Repeat** command number N from history\n\n```![N]```\n\n**Repeat** the Nth previous command\n\n```!-[N]```\n\n**Reuse** the last argument of the previous command\n\n```[command] !$```\n\n**Repeat** the last command with a substitution\n\n```^[old]^[new]```\n\n**Print** an expansion instead of running it\n\n```!![:p]```"],["SYNOPSIS","**!**_event_[**:**_word_][**:**_modifier_ ...]\n\n**^**_old_**^**_new_[**^**]"],["EVENT DESIGNATORS","**!!**\n> The previous command. Shorthand for **!-1**.\n\n**!n**\n> Command line number n, as shown by **history**.\n\n**!-n**\n> The command n lines back.\n\n**!string**\n> The most recent command starting with _string_.\n\n**!?string?**\n> The most recent command containing _string_. The trailing **?** is optional at end of line.\n\n**!#**\n> The current command line typed so far.\n\n**^old^new**\n> Quick substitution: repeat the previous command replacing the first occurrence of _old_ with _new_. Equivalent to **!!:s/old/new/**."],["WORD DESIGNATORS","Words are numbered from **0**, and are separated from the event by a colon. The colon may be omitted when the designator starts with **^**, **$**, **\\***, **-** or **%**.\n\n**:0**\n> The command name.\n\n**:n**\n> The nth word, counting the command as word 0. So **:1** is the first argument.\n\n**:^**\n> The first argument. Same as **:1**.\n\n**:$**\n> The last argument.\n\n**:%**\n> The word matched by the most recent **?string?** search.\n\n**:\\***\n> All words except the 0th. Same as **:1-$**, and expands to nothing if there is only one word.\n\n**:n-m**\n> The range of words from n to m.\n\n**:n\\***\n> From word n to the last. Same as **:n-$**.\n\n**:n-**\n> From word n to the second-to-last word."],["MODIFIERS","**:h**\n> Head: remove the trailing pathname component, leaving the directory.\n\n**:t**\n> Tail: remove all leading pathname components, leaving the filename.\n\n**:r**\n> Root: remove the trailing file extension.\n\n**:e**\n> Extension: remove all but the extension.\n\n**:s/old/new/**\n> Substitute the first occurrence of _old_ with _new_.\n\n**:gs/old/new/**\n> Substitute all occurrences. **g** is a prefix that repeats the following modifier.\n\n**:&**\n> Repeat the previous substitution.\n\n**:p**\n> Print the resulting command without executing it, and add it to the history.\n\n**:q**\n> Quote the result, protecting it from further expansion.\n\n**:x**\n> Like **:q**, but split into words on whitespace."],["DESCRIPTION","History expansion lets you rebuild a previous command without retyping it. It is a feature of the shell's interactive history mechanism, present in **bash**, **zsh**, **csh** and **tcsh**, and is performed very early: the line is rewritten before quoting, parameter expansion, or any other processing takes place.\n\nA reference has three parts. The **event designator** selects a line from history, an optional **word designator** picks words out of it, and any number of **modifiers** transform the result. So **!!** repeats the whole previous line, **!!:1** takes only its first argument, and **!!:1:h** takes that argument's directory.\n\nSeveral forms are common enough to be worth memorizing. **sudo !!** rerunning the previous command with privileges is the classic. **!$** grabs the last argument of the previous line, which is usually the file you were just working with, making **vim !$** a natural follow-on to **ls some/long/path/file.txt**. **^old^new** fixes a typo in the previous command in place.\n\nBecause expansion happens on the input line rather than at execution, the expanded text is echoed back before it runs, so you can see exactly what the shell made of your reference."],["CAVEATS","The **!** character keeps its special meaning inside **double quotes**. Writing **echo \"hello!\"** or a password containing **!** can trigger an unexpected expansion or an \"event not found\" error. Single quotes suppress it, as does a backslash escape. In bash, **set +H** turns history expansion off entirely, which is a common line in **.bashrc** for people who never use it.\n\nHistory expansion is only enabled in interactive shells, so it is not available in scripts and cannot be relied on for portable scripting.\n\n**!string** silently reruns whatever it matches, which may not be the command you had in mind if your history has moved on. Append **:p** first to see the expansion before committing to it, especially with anything destructive.\n\nDetails differ between shells: zsh supports extra modifiers such as **:G**, and csh's implementation, where the syntax originated, differs in a number of corners."],["HISTORY","History expansion originated in the **C shell**, written by **Bill Joy** at Berkeley in the late **1970s**, and its syntax was carried into **tcsh**, **bash** and **zsh** largely unchanged. In bash it is implemented by the **history library** that ships with **GNU Readline**."],["SEE ALSO","[history](/man/history)(1), [fc](/man/fc)(1), [bash](/man/bash)(1), [zsh](/man/zsh)(1), [csh](/man/csh)(1)"],["RESOURCES","```[Documentation](https://www.gnu.org/software/bash/manual/bash.html#History-Interaction)```"]],"e":["history","fc","bash","zsh","csh"],"x":"!!"},"export":{"n":"export","t":"shell builtin to mark variables for child processes","s":[["TAGLINE","shell builtin to mark variables for child processes"],["TLDR","**Set** a variable and export it in one step\n\n```export [VARIABLE]=[value]```\n\n**Export** an already existing shell variable\n\n```export [VARIABLE]```\n\n**Append** a directory to PATH\n\n```export PATH=\"$PATH:[/path/to/append]\"```\n\n**Remove** the export attribute, keeping the variable in the current shell\n\n```export -n [VARIABLE]```\n\n**Export** a shell function to child processes (bash)\n\n```export -f [function_name]```\n\n**Print** all exported variables in a re-readable form\n\n```export -p```\n\n**Export** several variables at once\n\n```export [EDITOR]=[vim] [PAGER]=[less]```"],["SYNOPSIS","**export** _name_[=_value_] ...\n\n**export** **-p**\n\n**export** [**-fn**] _name_[=_value_] ..."],["DESCRIPTION","**export** marks shell variables so they are placed in the environment of subsequently executed commands. An exported variable becomes an environment variable that every child process inherits; a variable set without **export** stays private to the current shell.\n\nThis is how configuration reaches programs you run: **PATH** tells the shell where to find executables, **EDITOR** tells tools which editor to open, and application-specific variables like **JAVA_HOME** are read by the processes that need them. Assignment and export can be combined (**export FOO=bar**) or split across two statements (**FOO=bar** then **export FOO**), which is the more portable form for very old shells.\n\nThe export attribute belongs to the variable, not to its value: once a name is exported, later assignments to it are exported too, without repeating **export**. **export -n** removes that attribute while leaving the variable defined in the current shell, which is not the same as deleting it; use **unset** for that.\n\nWith no operands, or with **-p**, **export** prints the names and values of all exported variables in a format that can be re-read as shell input."],["PARAMETERS","**-p**\n> Print all exported variables in a form that can be reused as shell input.\n\n**-n**\n> Remove the export attribute from each _name_. The variable stays set in the current shell but is no longer passed to children. Not POSIX; a bash, ksh and zsh extension.\n\n**-f**\n> Treat each _name_ as a shell function rather than a variable. Bash-specific and rarely portable.\n\n**--**\n> End of options; treat the rest as names, even if they start with a dash."],["CAVEATS","**export** only affects the current shell and the processes it starts afterwards. It cannot change the environment of the parent shell or of processes that are already running, which is why a script must be sourced (**. script.sh**) rather than executed if you want its exports to persist in your session.\n\n**export -n** does not unset the variable. The value remains visible to the current shell and to anything using it directly; only inheritance stops. Use **unset** _name_ to remove it entirely.\n\nExported functions (**export -f**) are passed to children through specially encoded environment variables and only work when the child is also bash. This mechanism was the vector for the **Shellshock** vulnerabilities in 2014, and its encoding changed as part of the fix.\n\nOnly **-p** is specified by POSIX. **-f** and **-n** are extensions, so scripts targeting **/bin/sh** should avoid them."],["HISTORY","**export** dates to the **Bourne shell** in the late **1970s** and is standardized by POSIX. It is implemented as a shell builtin in every major shell, because an external program could not modify the shell's own environment. Some systems also ship a **/usr/bin/export** stub for standards compliance, but it has no useful effect."],["INSTALL","```apk: sudo apk add execline```\n\n```brew: brew install execline```\n\n```nix: nix profile install nixpkgs#execline```"],["SEE ALSO","[unset](/man/unset)(1), [env](/man/env)(1), [printenv](/man/printenv)(1), [set](/man/set)(1), [declare](/man/declare)(1), [readonly](/man/readonly)(1), [typeset](/man/typeset)(1), [bash](/man/bash)(1)"],["RESOURCES","```[Documentation](https://www.gnu.org/software/bash/manual/bash.html#index-export)```"]],"e":["unset","env","printenv","set","declare","readonly","typeset","bash"],"x":"export [VARIABLE]=[value]"},"flips":{"n":"flips","t":"IPS and BPS patch creation and application","s":[["TAGLINE","IPS and BPS patch creation and application"],["TLDR","**Create IPS patch**\n\n```flips --create [original.rom] [modified.rom] [patch.ips]```\n\n**Apply IPS patch**\n\n```flips --apply [patch.ips] [input.rom] [output.rom]```\n\n**Create BPS patch**\n\n```flips --create --bps [original] [modified] [patch.bps]```\n\n**Apply BPS patch**\n\n```flips --apply [patch.bps] [input] [output]```"],["SYNOPSIS","**flips** [_options_] [_files_...]"],["PARAMETERS","**--create**\n> Create a patch.\n\n**--apply**\n> Apply a patch.\n\n**--bps**\n> Use BPS format instead of IPS.\n\n**--exact**\n> Create exact BPS patch (slower but guarantees smallest possible output).\n\n**--ips**\n> Use IPS format explicitly."],["DESCRIPTION","**Flips** (Floating IPS) is a patch creation and application tool for binary files, commonly used in ROM hacking and game modification communities. It supports both the legacy IPS (International Patching System) format and the modern BPS (Binary Patching System) format.\n\nThe IPS format has been the standard for ROM patches since the 1990s but has limitations including a 16MB file size restriction and lack of error detection. BPS addresses these issues with support for files of any size, built-in CRC32 checksums for verification, and better compression for smaller patch files.\n\nFlips can create patches by comparing original and modified files, then apply those patches to reproduce the modifications. The BPS format includes metadata and verification to ensure patches apply correctly, preventing corruption from applying patches to incorrect source files."],["INSTALL","```pacman: sudo pacman -S flips```\n\n```nix: nix profile install nixpkgs#flips```"],["SEE ALSO","[bsdiff](/man/bsdiff)(1), [xdelta](/man/xdelta)(1)"]],"e":["bsdiff","xdelta"],"x":"flips --create [original.rom] [modified.rom] [patch.ips]"},"gdmsetup":{"n":"gdmsetup","t":"graphical configuration tool for GDM","s":[["TAGLINE","graphical configuration tool for GDM"],["TLDR","**Open GDM configuration**\n\n```gdmsetup```"],["SYNOPSIS","**gdmsetup** [_options_]"],["PARAMETERS","**--help**\n> Display help information."],["DESCRIPTION","**gdmsetup** is the legacy graphical configuration tool for GDM (GNOME Display Manager), providing a user-friendly interface for managing login screen settings without manual configuration file editing. It allows administrators to configure options like auto-login for specific users, control which users appear in the login list, customize login screen themes, and adjust session timeout behavior.\n\nThe tool presents GDM's configuration options through an intuitive GUI, making it accessible to administrators who prefer graphical tools over command-line configuration. Settings modified through gdmsetup affect the login screen appearance and behavior for all users on the system.\n\nOn modern GNOME systems (GNOME 3 and later), gdmsetup has largely been deprecated in favor of integrating GDM configuration directly into the main GNOME Settings application. Many distributions no longer include gdmsetup, instead providing GDM configuration through dconf-editor or system settings panels. For systems that still use it, gdmsetup requires administrative privileges to modify system-wide login settings."],["CAVEATS","Requires administrative privileges. Deprecated in favor of GNOME Settings. May not be available on all distributions."],["HISTORY","gdmsetup was the traditional GDM configuration tool. Modern GNOME systems integrate GDM settings into the main Settings application, making the standalone tool less common."],["SEE ALSO","[gdm](/man/gdm)(8), [dconf](/man/dconf)(1)"],["RESOURCES","```[Source code](https://gitlab.gnome.org/GNOME/gdm)```"]],"e":["gdm","dconf"],"x":"gdmsetup"},"getcap":{"n":"getcap","t":"Linux capabilities viewer for files","s":[["TAGLINE","Linux capabilities viewer for files"],["TLDR","Get **capabilities**\n\n```getcap path/to/file```\n\n**Recursive** search\n\n```getcap -r path/to/dir```\n\nShow **all** entries\n\n```getcap -v path/to/file```"],["SYNOPSIS","**getcap** [_OPTIONS_] _files_..."],["DESCRIPTION","**getcap** displays the capabilities of specified files. Linux capabilities allow fine-grained privileges instead of running as root, enabling specific elevated permissions without full root access."],["PARAMETERS","**-r**\n> Recursively search directories\n\n**-v**\n> Display all searched entries even if no capabilities set\n\n**-n**\n> Display numeric capability values"],["CAVEATS","Capabilities are an alternative to setuid programs, providing more granular privilege control. Only files with capabilities set will show output by default."],["HISTORY","**getcap** is part of **libcap**, implementing POSIX 1003.1e capabilities on Linux."],["INSTALL","```apt: sudo apt install libcap2-bin```\n\n```apk: sudo apk add libcap-getcap```"],["SEE ALSO","[setcap](/man/setcap)(8), [capabilities](/man/capabilities)(7)"],["RESOURCES","```[Source code](https://git.kernel.org/pub/scm/libs/libcap/libcap.git/)```\n\n```[Documentation](https://manpages.debian.org/getcap)```"]],"e":["setcap","capabilities"],"x":"getcap path/to/file"},"git-missing":{"n":"git-missing","t":"Show commits missing between branches","s":[["TAGLINE","Show commits missing between branches"],["TLDR","**Show commits in current branch missing from another branch and vice versa**\n\n```git missing [branch]```\n\n**Compare two specific branches**\n\n```git missing [branch1] [branch2]```"],["SYNOPSIS","**git** **missing** [_branch1_] [_branch2_]"],["DESCRIPTION","**git missing** shows commits that exist in one branch but not the other. This git-extras command performs a symmetric comparison, revealing what changes are unique to each branch.\n\nIf only one branch is specified, it is compared against the current branch. If no branches are specified, the current branch is compared against the default branch (usually master or main).\n\nThe command is particularly useful before merging, as it clearly shows what commits will be introduced from each side. It essentially performs `git log --left-right --oneline branch1...branch2` to provide a bidirectional view of branch divergence."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-log](/man/git-log)(1), [git-extras](/man/git-extras)(1), [git-cherry](/man/git-cherry)(1), [git-branch](/man/git-branch)(1)"]],"e":["git-log","git-extras","git-cherry","git-branch"],"x":"git missing [branch]"},"git-name-rev":{"n":"git-name-rev","t":"Find symbolic names for commits","s":[["TAGLINE","Find symbolic names for commits"],["TLDR","**Find symbolic name for commit**\n\n```git name-rev [commit]```\n\n**Name multiple commits**\n\n```git name-rev [commit1] [commit2]```\n\n**Annotate commit hashes from stdin**\n\n```git log --oneline | git name-rev --annotate-stdin```\n\n**Only use tags to name a commit**\n\n```git name-rev --tags [commit]```\n\n**Exclude refs matching a pattern**\n\n```git name-rev --exclude=\"refs/tags/*\" [commit]```\n\n**Name all reachable commits from all refs**\n\n```git name-rev --all```"],["SYNOPSIS","**git** **name-rev** [_options_] _commit_...\n**git** **name-rev** [_options_] **--all**\n**git** **name-rev** [_options_] **--annotate-stdin**"],["PARAMETERS","**--annotate-stdin**\n> Transform stdin by substituting commit hashes with their symbolic names. Replaces deprecated --stdin.\n\n**--tags**\n> Only use tags to name the commits.\n\n**--refs** _pattern_\n> Only use refs whose names match a given shell pattern.\n\n**--exclude** _pattern_\n> Do not use any ref whose name matches a given shell pattern.\n\n**--all**\n> List all commits reachable from all refs.\n\n**--name-only**\n> Print only the symbolic name, not the SHA-1.\n\n**--no-undefined**\n> Exit with error when a reference is undefined instead of printing \"undefined\".\n\n**--always**\n> Show uniquely abbreviated commit object as fallback when no name is found."],["DESCRIPTION","**git name-rev** finds symbolic names for given commit hashes, making them more human-readable. It shows the nearest ref (branch or tag) that can describe each commit, along with the number of commits from that ref (e.g., \"main~3\" means three commits before main).\n\nThe `--annotate-stdin` option allows processing commit hashes from pipelines, enabling annotation of `git log` output or other commands that produce commit IDs. Filter options like `--tags` or `--refs` restrict which references are used for naming."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-describe](/man/git-describe)(1), [git-rev-parse](/man/git-rev-parse)(1), [git-log](/man/git-log)(1)"]],"e":["git-describe","git-rev-parse","git-log"],"x":"git name-rev [commit]"},"git-rm":{"n":"git-rm","t":"Remove files from the working tree and index","s":[["TAGLINE","Remove files from the working tree and index"],["TLDR","**Remove file from tracking**\n\n```git rm [file.txt]```\n\n**Remove directory**\n\n```git rm -r [directory/]```\n\n**Remove from index only**\n\n```git rm --cached [file.txt]```\n\n**Force remove**\n\n```git rm -f [file.txt]```\n\n**Dry run**\n\n```git rm -n [file.txt]```"],["SYNOPSIS","**git rm** [_options_] _files_"],["PARAMETERS","_FILES_\n> Files to remove.\n\n**--cached**\n> Remove from index only, keep file.\n\n**-r**\n> Recursive removal.\n\n**-f**, **--force**\n> Force removal.\n\n**-n**, **--dry-run**\n> Show what would be removed.\n\n**--ignore-unmatch**\n> Exit successfully even if no match.\n\n**--help**\n> Display help information."],["DESCRIPTION","**git rm** removes files from the working tree and the index, staging the removal for the next commit. The `--cached` option removes files from tracking while keeping them on disk, which is useful for files that should have been in `.gitignore`.\n\nWithout `--cached`, the file is deleted from both the working tree and the index."],["CAVEATS","Without --cached, deletes file from disk. Removal needs to be committed. Use --cached to untrack without deleting."],["HISTORY","git rm is a core **Git** command for removing files from version control, complementing git add."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-add](/man/git-add)(1), [git-mv](/man/git-mv)(1), [rm](/man/rm)(1)"],["RESOURCES","```[Source code](https://github.com/git/git)```\n\n```[Homepage](https://git-scm.com/)```\n\n```[Documentation](https://git-scm.com/docs/git-rm)```"]],"e":["git-add","git-mv","rm"],"x":"git rm [file.txt]"},"guvcview":{"n":"guvcview","t":"GTK+ webcam viewer and capture application","s":[["TAGLINE","GTK+ webcam viewer and capture application"],["TLDR","**Start webcam viewer**\n\n```guvcview```\n\n**Specify video device**\n\n```guvcview -d [/dev/video0]```\n\n**Request a resolution and format**\n\n```guvcview -x [1920x1080] -f [MJPG]```\n\n**Select the GUI toolkit**\n\n```guvcview -g [gtk3]```\n\n**Capture video straight to a file**\n\n```guvcview -j [output.mkv]```\n\n**Capture a single photo to a file**\n\n```guvcview -i [photo.jpg] -n [1]```"],["SYNOPSIS","**guvcview** [_options_]"],["PARAMETERS","**-d**, **--device** _DEVICE_\n> Video device to use (default: /dev/video0).\n\n**-x**, **--resolution** _WxH_\n> Request a capture resolution (e.g. 640x480).\n\n**-f**, **--format** _FOURCC_\n> Request a video format/FourCC (e.g. MJPG, YUYV).\n\n**-F**, **--fps** _NUM[/DENOM]_\n> Request a frame rate.\n\n**-g**, **--gui** _API_\n> Select the GUI toolkit: none, gtk3, or qt6.\n\n**-r**, **--render** _API_\n> Select the render API: none, sdl, or sfml.\n\n**-m**, **--render_window** _FLAGS_\n> Set render window flags (none, full, max, or WIDTHxHEIGHT).\n\n**-a**, **--audio** _API_\n> Select the audio API: none, port, or pulse.\n\n**-k**, **--audio_device** _INDEX_\n> Select the audio device index for the chosen audio API.\n\n**-j**, **--video** _FILE_\n> Filename for captured video.\n\n**-u**, **--video_codec** _CODEC_\n> Video codec to use (e.g. raw, mjpg, h264, vp80).\n\n**-i**, **--image** _FILE_\n> Filename for captured photo(s).\n\n**-n**, **--photo_total** _N_\n> Total number of photos to capture.\n\n**-t**, **--photo_timer** _SECONDS_\n> Time between captured photos.\n\n**-p**, **--profile** _FILE_\n> Load a saved control profile.\n\n**-e**, **--exit_on_term**\n> Exit the app after video or image capture ends.\n\n**-z**, **--control_panel**\n> Start in control-panel-only mode.\n\n**-w**, **--verbosity** _LEVEL_\n> Set verbosity level (default: 0).\n\n**-v**, **--version**\n> Print version information.\n\n**-h**, **--help**\n> Print help."],["DESCRIPTION","**guvcview** is a video capture and control application for UVC (USB Video Class) compatible webcams on Linux. It provides a live preview along with controls for camera settings such as brightness, contrast, exposure, and white balance, and can capture both video and still photos.\n\nModern versions build against **GTK3** or **Qt6** for the interface (selectable with **-g**) and support multiple render backends (SDL, SFML) and audio APIs (PortAudio, PulseAudio) for recording video with sound."],["CAVEATS","Requires a UVC-compatible camera and a V4L2-capable kernel driver. Some controls and codecs are hardware/driver dependent. Command-line flags and defaults have changed across major guvcview releases; always check `guvcview --help` for the installed version."],["HISTORY","**guvcview** was created by **Paulo Assis** in **2008** as a simple GTK+ webcam viewer for Linux using the V4L2 API. It has since gained Qt6 GUI support and multiple render/audio backends while remaining a lightweight alternative to full video-editing suites for quick webcam preview and capture."],["INSTALL","```apt: sudo apt install guvcview```\n\n```dnf: sudo dnf install guvcview```\n\n```pacman: sudo pacman -S guvcview```\n\n```apk: sudo apk add guvcview```\n\n```zypper: sudo zypper install guvcview```\n\n```nix: nix profile install nixpkgs#guvcview```"],["SEE ALSO","[cheese](/man/cheese)(1), [v4l2-ctl](/man/v4l2-ctl)(1), [ffmpeg](/man/ffmpeg)(1)"],["RESOURCES","```[Homepage](https://guvcview.sourceforge.net/)```\n\n```[Source code](https://sourceforge.net/projects/guvcview/)```"]],"e":["cheese","v4l2-ctl","ffmpeg"],"x":"guvcview"},"hashdeep":{"n":"hashdeep","t":"computes multiple cryptographic hashes simultaneously for files and directories","s":[["TAGLINE","computes multiple cryptographic hashes simultaneously for files and directories"],["TLDR","**Compute MD5 and SHA-256 hashes** of files recursively\n\n```hashdeep -r [path/to/directory]```\n\n**Compute specific hash types** (md5, sha1, sha256, tiger, whirlpool)\n\n```hashdeep -c [sha256] [path/to/files]```\n\n**Create a hash manifest** of files\n\n```hashdeep -r [path/to/directory] > [manifest.txt]```\n\n**Audit files against a known hash manifest**\n\n```hashdeep -r -a -k [manifest.txt] [path/to/directory]```\n\n**Find files matching** hashes in a manifest\n\n```hashdeep -r -m -k [manifest.txt] [path/to/directory]```\n\n**Find files NOT matching** hashes in a manifest\n\n```hashdeep -r -x -k [manifest.txt] [path/to/directory]```\n\n**Show progress indicator** while hashing\n\n```hashdeep -r -e [path/to/directory]```"],["SYNOPSIS","**hashdeep** [_-c alg_] [_-k file_] [_-amxwMXrespblvv_] [_files_]\n\n**hashdeep** -c _alg1_,_alg2_ -r _directory_"],["PARAMETERS","**-c** _ALG_\n> Compute hashes using specified algorithms (md5, sha1, sha256, tiger, whirlpool); default is md5,sha256\n\n**-r**\n> Recursive mode; traverse all subdirectories\n\n**-k** _FILE_\n> Load file of known hashes for comparison\n\n**-a**\n> Audit mode; compare files against known hashes\n\n**-m**\n> Positive matching; only show files that match known hashes\n\n**-x**\n> Negative matching; only show files NOT in known hashes\n\n**-M**\n> Like -m but also print hash of each matching file\n\n**-X**\n> Like -x but also print hash of each non-matching file\n\n**-w**\n> When auditing, display which known file matched\n\n**-e**\n> Display progress indicator and time estimate\n\n**-s**\n> Silent mode; suppress error messages\n\n**-b**\n> Bare mode; strip directory paths from output\n\n**-l**\n> Use relative file paths instead of absolute\n\n**-p** _SIZE_\n> Piecewise mode; break files into chunks before hashing\n\n**-v**\n> Verbose mode; use multiple times for more detail\n\n**-j** _NUM_\n> Use specified number of threads\n\n**-d**\n> Output in DFXML format\n\n**-h**\n> Show help message\n\n**-V**\n> Show version"],["DESCRIPTION","**hashdeep** computes multiple cryptographic hashes simultaneously for files and directories. Unlike single-hash tools, it calculates MD5 and SHA-256 by default, providing stronger verification through multiple algorithms.\n\nThe tool excels at file integrity verification and digital forensics. It can create manifests of known-good files, then audit systems to detect changes, additions, or deletions. Audit mode reports file states: MATCH for identical files, MISMATCH for modified files, MISSING for deleted files, and NEW for added files.\n\nOutput follows the DFXML-compatible hashdeep format, including file size, hash values, and path. This format can be loaded with **-k** for later verification. The tool handles large directory trees efficiently with recursive scanning and optional threading."],["CAVEATS","Hash algorithms like MD5 and SHA-1 have known collision vulnerabilities and should not be relied upon for security-critical verification. Use SHA-256 or stronger algorithms when integrity is paramount. Very large files may take considerable time to hash. The tool reads files sequentially and may be I/O bound."],["HISTORY","hashdeep was written by **Jesse Kornblum** as part of the **md5deep** package of forensic hashing tools. First released in **2003**, it was designed for digital forensics investigators who needed to verify file integrity and detect tampering. The multi-hash capability was added to address weaknesses in individual algorithms. The project is commonly used in law enforcement digital forensics and enterprise data integrity verification."],["INSTALL","```apt: sudo apt install hashdeep```\n\n```zypper: sudo zypper install hashdeep```\n\n```brew: brew install hashdeep```\n\n```nix: nix profile install nixpkgs#hashdeep```"],["SEE ALSO","[md5sum](/man/md5sum)(1), [sha256sum](/man/sha256sum)(1)"]],"e":["md5sum","sha256sum"],"x":"hashdeep -r [path/to/directory]"},"ippeveprinter":{"n":"ippeveprinter","t":"development and testing tool that implements an IPP Everywhere compliant","s":[["TAGLINE","development and testing tool that implements an IPP Everywhere compliant"],["TLDR","**Start an IPP Everywhere printer** for testing\n\n```ippeveprinter [TestPrinter]```\n\n**Start printer on a specific port**\n\n```ippeveprinter -p [8631] [TestPrinter]```\n\n**Create printer with specific command** for processing jobs\n\n```ippeveprinter -c '[/usr/bin/cat > /tmp/printjob.pdf]' [TestPrinter]```\n\n**Start printer with verbose logging**\n\n```ippeveprinter -v [TestPrinter]```\n\n**Create printer that advertises via DNS-SD**\n\n```ippeveprinter -d [TestPrinter]```"],["SYNOPSIS","**ippeveprinter** [_-c command_] [_-d_] [_-p port_] [_-v_] _service-name_"],["PARAMETERS","**-c** _command_\n> Command to run for each print job (receives job data on stdin)\n\n**-d**\n> Advertise the printer using DNS-SD/Bonjour\n\n**-p** _port_\n> Port number to listen on (default: auto-assigned)\n\n**-v**\n> Verbose mode; show detailed logging\n\n**-2**\n> Support 2-sided printing\n\n**-M** _manufacturer_\n> Manufacturer name for the printer\n\n**-m** _model_\n> Model name for the printer\n\n**-i** _iconfile_\n> Icon file for the printer\n\n**-l** _location_\n> Printer location string"],["DESCRIPTION","**ippeveprinter** is a development and testing tool that implements an IPP Everywhere compliant virtual printer. IPP Everywhere is a PWG (Printer Working Group) standard that enables driverless printing.\n\nThe tool creates a network printer that accepts print jobs via the Internet Printing Protocol. Jobs can be processed by a custom command or simply logged for testing purposes. This is useful for testing IPP clients, developing print workflows, or debugging printer discovery."],["CAVEATS","Not intended for production use as a print server. The printer only exists while the command is running. Requires appropriate firewall rules if accepting remote connections. Part of CUPS but functionality varies by version."],["HISTORY","**ippeveprinter** is part of the **CUPS** (Common Unix Printing System) project, originally developed by Michael Sweet at Easy Software Products and later maintained by Apple. IPP Everywhere support was added to facilitate driverless printing standards compliance testing."],["INSTALL","```apt: sudo apt install cups-ipp-utils```\n\n```dnf: sudo dnf install cups-client```\n\n```apk: sudo apk add cups-client```\n\n```zypper: sudo zypper install cups-client```"],["SEE ALSO","[ippfind](/man/ippfind)(1), [ipptool](/man/ipptool)(1), [cupsd](/man/cupsd)(8), [lp](/man/lp)(1)"]],"e":["ippfind","ipptool","cupsd","lp"],"x":"ippeveprinter [TestPrinter]"},"jj":{"n":"jj","t":"jujutsu, a Git-compatible version control system","s":[["TAGLINE","jujutsu, a Git-compatible version control system"],["TLDR","**Initialize a new colocated Git/jj repository** in the current directory\n\n```jj git init --colocate```\n\n**Clone a Git remote**\n\n```jj git clone [https://github.com/owner/repo]```\n\n**Show working-copy status**\n\n```jj status```\n\n**Show the revision graph**\n\n```jj log```\n\n**Create a new (empty) change on top of the current one**\n\n```jj new```\n\n**Describe the current change**\n\n```jj describe -m \"[message]\"```\n\n**Squash the current change into its parent**\n\n```jj squash```\n\n**Move a change to a new parent (rebase)**\n\n```jj rebase -r [revision] -d [destination]```\n\n**Abandon the current change** (drops its content, rebases descendants)\n\n```jj abandon```\n\n**Undo the last operation**\n\n```jj undo```\n\n**Sync with the Git remote**\n\n```jj git fetch && jj git push```"],["SYNOPSIS","**jj** [_global_options_] _command_ [_args_]"],["COMMON COMMANDS","**git init** [--colocate]\n> Initialize a new repository, optionally colocated with Git so both **jj** and **git** see the same working copy.\n\n**git clone** _url_\n> Clone a Git remote into a jj repo.\n\n**status**\n> Show high-level repository / working copy state.\n\n**log**\n> Display the revision graph.\n\n**new** [_revisions_...]\n> Create a new (empty) change on top of the given revisions; defaults to the current working-copy parent.\n\n**describe** [**-m** _message_]\n> Edit the description (commit message) of a change.\n\n**edit** _revision_\n> Move the working copy to an existing revision (replaces \"checkout\" thinking).\n\n**squash** [**--from** _rev_] [**--into** _rev_]\n> Move changes from one revision into another (default: current → its parent).\n\n**rebase** **-r** _rev_ **-d** _dest_\n> Move a revision (and optionally its descendants) onto a new destination.\n\n**abandon** [_revision_]\n> Drop a change; descendants are rebased onto the parent.\n\n**undo**\n> Revert the last operation. Combine with **jj op log** and **jj op restore** for finer control.\n\n**bookmark** _subcommand_\n> Manage named references (jj's equivalent of Git branches): **create**, **move**, **delete**, **track**, **untrack**, **list**.\n\n**git** _subcommand_\n> Git interop: **fetch**, **push**, **import**, **export**, **remote**.\n\n**op log**, **op restore**\n> Inspect and roll back the operation log (every command is recorded)."],["GLOBAL OPTIONS","**-R**, **--repository** _PATH_\n> Operate on the repository at _PATH_.\n\n**--at-operation** _OP_, **--at-op** _OP_\n> Run the command at a previous operation in the operation log (read-only views).\n\n**--no-pager**\n> Disable the pager for this invocation.\n\n**--config-toml** _TOML_\n> Inline configuration overrides."],["DESCRIPTION","**jj** (Jujutsu) is a Git-compatible distributed version control system. Every working-copy change is recorded as a first-class revision, conflicts are stored in commits rather than blocking operations, and there are no branches in the Git sense — instead every commit is reachable through the revision graph and human-friendly **bookmarks** can be attached to any revision.\n\nThe default backend is Git, so a jj repo can be made **colocated** with a real Git repo (`jj git init --colocate`), letting Git tools and other developers continue to interact through plain Git while you use jj locally."],["CAVEATS","The CLI is still pre-1.0 and changing — the **bookmark** subcommand was renamed from **branch** in 0.18, and many flags continue to evolve. **jj git push** by default pushes only the bookmarks that match the local working revisions; explicit pushes use **--bookmark** _name_ or **--all-bookmarks**. Conflicts are stored on disk but the tooling around resolving them is still young."],["HISTORY","**Jujutsu** was created in **2019** at **Google** by **Martin von Zweigbergk** as a personal experiment, open-sourced in **2022**, and is now developed at **github.com/jj-vcs/jj** with a growing community of contributors."],["INSTALL","```pacman: sudo pacman -S jujutsu```\n\n```apk: sudo apk add jujutsu```\n\n```zypper: sudo zypper install jujutsu```\n\n```brew: brew install jujutsu```\n\n```nix: nix profile install nixpkgs#jujutsu```"],["SEE ALSO","[jj-git](/man/jj-git)(1), [jj-log](/man/jj-log)(1), [git](/man/git)(1), [hg](/man/hg)(1)"]],"e":["jj-git","jj-log","git","hg"],"x":"jj git init --colocate"},"jp2a":{"n":"jp2a","t":"converts JPEG images to ASCII art","s":[["TAGLINE","converts JPEG images to ASCII art"],["TLDR","**Convert image to ASCII**\n\n```jp2a [image.jpg]```\n\n**Set output width**\n\n```jp2a --width=[80] [image.jpg]```\n\n**Colored ASCII**\n\n```jp2a --colors [image.jpg]```\n\n**Invert colors**\n\n```jp2a --invert [image.jpg]```\n\n**Custom characters**\n\n```jp2a --chars=\"[.:-=+*#%@]\" [image.jpg]```\n\n**Output to file**\n\n```jp2a [image.jpg] --output=[output.txt]```"],["SYNOPSIS","**jp2a** [_options_] _file_..."],["PARAMETERS","_FILE_\n> Image file(s) to convert.\n\n**--width** _N_\n> Output width in characters.\n\n**--height** _N_\n> Output height.\n\n**--colors**\n> Use ANSI colors.\n\n**--invert**\n> Invert brightness.\n\n**--chars** _STRING_\n> Character ramp to use.\n\n**--output** _FILE_\n> Output file.\n\n**--help**\n> Display help information."],["DESCRIPTION","**jp2a** converts JPEG images to ASCII art. It maps pixel brightness to characters for text-based display.\n\nThe tool supports color output for terminals. It can process multiple images and URLs."],["CAVEATS","JPEG input required. Terminal width affects output. Aspect ratio considerations."],["HISTORY","jp2a was created to convert JPEG images to ASCII art for terminal display and text-based image representation."],["INSTALL","```apt: sudo apt install jp2a```\n\n```pacman: sudo pacman -S jp2a```\n\n```zypper: sudo zypper install jp2a```\n\n```brew: brew install jp2a```\n\n```nix: nix profile install nixpkgs#jp2a```"],["SEE ALSO","[img2txt](/man/img2txt)(1), [cacaview](/man/cacaview)(1)"]],"e":["img2txt","cacaview"],"x":"jp2a [image.jpg]"},"jps":{"n":"jps","t":"lists Java Virtual Machine processes","s":[["TAGLINE","lists Java Virtual Machine processes"],["TLDR","**List Java processes**\n\n```jps```\n\n**Show full class names**\n\n```jps -l```\n\n**Show JVM arguments**\n\n```jps -v```\n\n**Show main class arguments**\n\n```jps -m```\n\n**Show all information**\n\n```jps -lvm```\n\n**Query remote host**\n\n```jps -l [hostname]```"],["SYNOPSIS","**jps** [_options_] [_hostid_]"],["PARAMETERS","_HOSTID_\n> Remote host to query.\n\n**-l**\n> Display the full package name of the main class or the full path to the JAR file.\n\n**-v**\n> Display arguments passed to the JVM.\n\n**-m**\n> Display arguments passed to the main method.\n\n**-q**\n> Suppress class name/arguments; show only local JVM identifiers (PIDs).\n\n**-V**\n> Same as -q: list only local JVM identifiers.\n\n**-help**\n> Display the help message for jps."],["DESCRIPTION","**jps** lists Java Virtual Machine processes. It shows running Java applications with their PIDs.\n\nThe tool is the Java equivalent of ps for Java processes. It provides quick identification of Java applications."],["CAVEATS","Part of JDK. Shows JVM processes only. Requires same user or permissions."],["HISTORY","jps has been part of the **JDK** since Java 5, providing process listing for Java application management."],["SEE ALSO","[java](/man/java)(1), [jstack](/man/jstack)(1), [jmap](/man/jmap)(1), [ps](/man/ps)(1)"]],"e":["java","jstack","jmap","ps"],"x":"jps"},"json5":{"n":"json5","t":"parses JSON5 format and outputs standard JSON","s":[["TAGLINE","parses JSON5 format and outputs standard JSON"],["TLDR","**Parse JSON5 to JSON**\n\n```json5 [file.json5]```\n\n**Convert from stdin**\n\n```echo '{name: \"test\"}' | json5```\n\n**Pretty print**\n\n```json5 --space [2] [file.json5]```\n\n**Validate only**\n\n```json5 --validate [file.json5]```\n\n**Indent with tabs**\n\n```json5 --space t [file.json5]```\n\n**Convert and write to file**\n\n```json5 --out-file [output.json] [file.json5]```"],["SYNOPSIS","**json5** [_options_] [_file_]"],["PARAMETERS","_FILE_\n> JSON5 file to parse. Reads from stdin if omitted.\n\n**-s**, **--space** _N_\n> Number of spaces to indent, or \"t\" for tabs.\n\n**-v**, **--validate**\n> Validate JSON5 syntax without outputting JSON.\n\n**-o**, **--out-file** _FILE_\n> Write output to file instead of stdout.\n\n**-V**, **--version**\n> Display version number.\n\n**-h**, **--help**\n> Display help information."],["DESCRIPTION","**json5** parses JSON5 format and outputs standard JSON. JSON5 extends JSON with comments, trailing commas, and unquoted keys.\n\nThe tool converts more readable JSON5 configs to strict JSON. It validates and formats JSON5 input."],["CAVEATS","Node.js tool. JSON5 is a superset of JSON. Output is standard JSON."],["HISTORY","json5 implements the **JSON5** specification, designed to make JSON more human-friendly for configuration files."],["INSTALL","```brew: brew install json5```"],["SEE ALSO","[jq](/man/jq)(1), [json_pp](/man/json_pp)(1), [jsonlint](/man/jsonlint)(1)"]],"e":["jq","json_pp","jsonlint"],"x":"json5 [file.json5]"},"kite":{"n":"kite","t":"was an AI-powered code completion engine","s":[["TAGLINE","was an AI-powered code completion engine"],["TLDR","**Start Kite engine**\n\n```kite```\n\n**Check status**\n\n```kite status```\n\n**Stop Kite**\n\n```kite stop```\n\n**Install editor plugin**\n\n```kite install [vim|vscode|atom]```"],["SYNOPSIS","**kite** [_command_] [_options_]"],["PARAMETERS","**status**\n> Check service status.\n\n**start**\n> Start Kite engine.\n\n**stop**\n> Stop Kite engine.\n\n**install** _EDITOR_\n> Install editor plugin.\n\n**--help**\n> Display help information."],["DESCRIPTION","**Kite** was an AI-powered code completion engine. It provided intelligent autocomplete for multiple languages.\n\nThe tool integrated with popular editors. It offered documentation and function signatures inline."],["CAVEATS","Service discontinued in 2022. Use alternatives. Historical reference."],["HISTORY","Kite was an AI coding assistant company that discontinued service in **2022**. Consider alternatives like GitHub Copilot."],["SEE ALSO","[copilot](/man/copilot)(1)"]],"e":["copilot"],"x":"kite"},"launchctl":{"n":"launchctl","t":"manages launchd services on macOS","s":[["TAGLINE","manages launchd services on macOS"],["TLDR","**List loaded services**\n\n```launchctl list```\n\n**Load service**\n\n```launchctl load [/path/to/service.plist]```\n\n**Unload service**\n\n```launchctl unload [/path/to/service.plist]```\n\n**Start service**\n\n```launchctl start [com.example.service]```\n\n**Stop service**\n\n```launchctl stop [com.example.service]```\n\n**Bootstrap service** (modern replacement for load)\n\n```sudo launchctl bootstrap gui/[uid] [/path/to/service.plist]```\n\n**Bootout service** (modern replacement for unload)\n\n```sudo launchctl bootout gui/[uid]/[com.example.service]```\n\n**Print service info**\n\n```launchctl print gui/[uid]/[com.example.service]```"],["SYNOPSIS","**launchctl** _subcommand_ [_options_]"],["DESCRIPTION","**launchctl** manages launchd services on macOS. It controls system and user daemons, handling service lifecycle, scheduling, and resource limits.\n\nThe tool replaces traditional init scripts on macOS, using property list (plist) files to define services."],["PARAMETERS","**list**\n> List loaded services.\n\n**load** _plist_\n> Load service (deprecated).\n\n**unload** _plist_\n> Unload service (deprecated).\n\n**start** _label_\n> Start service.\n\n**stop** _label_\n> Stop service.\n\n**bootstrap** _domain_ _plist_\n> Load service (modern).\n\n**bootout** _domain_ _plist_\n> Unload service (modern).\n\n**print** _target_\n> Print service info.\n\n**kickstart** _target_\n> Force start service.\n\n**enable** _service_\n> Enable service.\n\n**disable** _service_\n> Disable service.\n\n**blame** _target_\n> Print reason for service being loaded/started.\n\n**dumpstate**\n> Dump launchd state to stdout."],["CAVEATS","macOS only. The **load/unload** subcommands are deprecated since macOS 10.10; use **bootstrap/bootout** instead. System services require root. GUI services use the user domain (gui/UID)."],["HISTORY","**launchctl** is part of **launchd**, introduced in **Mac OS X 10.4 Tiger** (2005) by **Apple**. It replaced the traditional init, xinetd, and cron systems, unifying daemon management. Dave Zarzycki led its development."],["SEE ALSO","[systemctl](/man/systemctl)(1)"]],"e":["systemctl"],"x":"launchctl list"},"linode-cli-events":{"n":"linode-cli-events","t":"manages Linode account events","s":[["TAGLINE","manages Linode account events"],["TLDR","**List events**\n\n```linode-cli events list```\n\n**View specific event**\n\n```linode-cli events view [event_id]```\n\n**Mark event as read**\n\n```linode-cli events mark-read [event_id]```\n\n**Mark event as seen**\n\n```linode-cli events mark-seen [event_id]```\n\n**List events in JSON format**\n\n```linode-cli events list --json```\n\n**Filter by action**\n\n```linode-cli events list --action [linode_boot]```"],["SYNOPSIS","**linode-cli events** _subcommand_ [_options_]"],["PARAMETERS","_SUBCOMMAND_\n> Events operation.\n\n**list**\n> List all events.\n\n**view** _ID_\n> View specific event.\n\n**mark-read** _ID_\n> Mark event as read.\n\n**mark-seen** _ID_\n> Mark event as seen.\n\n**--action** _TYPE_\n> Filter by event action (e.g., linode_boot, linode_create, linode_reboot).\n\n**--json**\n> Output in JSON format.\n\n**--help**\n> Display help information."],["DESCRIPTION","**linode-cli events** manages Linode account events. Events track actions and changes in your infrastructure.\n\nThe tool shows notifications about instance creation, reboots, resizes, and other activities."],["CAVEATS","Requires authentication. Events have retention limits. Part of linode-cli."],["HISTORY","linode-cli events is part of the **Linode CLI** for monitoring account activity and audit trails."],["INSTALL","```brew: brew install linode-cli```\n\n```nix: nix profile install nixpkgs#linode-cli```"],["SEE ALSO","[linode-cli](/man/linode-cli)(1), [linode-cli-linodes](/man/linode-cli-linodes)(1)"]],"e":["linode-cli","linode-cli-linodes"],"x":"linode-cli events list"},"mdb-sql":{"n":"mdb-sql","t":"SQL interface to MDB Tools for Access databases","s":[["TAGLINE","SQL interface to MDB Tools for Access databases"],["TLDR","**Start interactive SQL session**\n\n```mdb-sql [database.mdb]```\n\n**Execute SQL from stdin**\n\n```echo \"SELECT * FROM [table_name]\" | mdb-sql [database.mdb]```\n\n**Pretty print output** (ASCII table format)\n\n```mdb-sql -p [database.mdb]```\n\n**Execute SQL from a file**\n\n```mdb-sql -i [query.sql] [database.mdb]```\n\n**Specify column delimiter** and suppress headers\n\n```mdb-sql -d [,] -H [database.mdb]```\n\n**Write output to file**\n\n```mdb-sql -o [output.csv] [database.mdb]```"],["SYNOPSIS","**mdb-sql** [_options_] _database_"],["PARAMETERS","_DATABASE_\n> Microsoft Access database file (.mdb or .accdb).\n\n**-H**, **--no-header**\n> Suppress column header row.\n\n**-F**, **--no-footer**\n> Suppress footer row.\n\n**-p**, **--no-pretty-print**\n> Tab-separated output instead of ASCII table format.\n\n**-d**, **--delimiter** _DELIM_\n> Use alternative column delimiter.\n\n**-i**, **--input** _FILE_\n> Read SQL from input file.\n\n**-o**, **--output** _FILE_\n> Write results to output file.\n\n**--version**\n> Print mdbtools version and exit."],["INTERACTIVE COMMANDS","**list tables**: List available tables in the database.\n**describe table** _name_: Display column information for a table.\n**go**: Execute the current SQL batch.\n**reset**: Clear the current SQL batch.\n**connect** _database_: Connect to a database (or switch databases).\n**disconnect**: Disconnect from the current database."],["DESCRIPTION","**mdb-sql** provides an SQL interface to Microsoft Access databases. It can run interactively or process SQL from stdin or files. SQL batches are terminated with the **go** command in interactive mode.\n\nThe tool is part of the **mdbtools** suite. It supports SELECT queries against MDB/ACCDB files."],["CAVEATS","Limited SQL support compared to full Access SQL. Part of mdbtools. May not support all Access features or data types."],["HISTORY","mdb-sql is part of **mdbtools** for accessing Microsoft Access databases on Linux/Unix."],["INSTALL","```apt: sudo apt install mdbtools```\n\n```dnf: sudo dnf install mdbtools```\n\n```apk: sudo apk add mdbtools-utils```\n\n```zypper: sudo zypper install mdbtools```\n\n```brew: brew install mdbtools```\n\n```nix: nix profile install nixpkgs#mdbtools```"],["SEE ALSO","[mdbtools](/man/mdbtools)(1)"]],"e":["mdbtools"],"x":"mdb-sql [database.mdb]"},"mssh":{"n":"mssh","t":"GTK+ based SSH client for issuing commands to multiple servers","s":[["TAGLINE","GTK+ based SSH client for issuing commands to multiple servers"],["TLDR","Connect to **multiple SSH servers**\n\n```mssh [user@host1] [user@host2] [user@host3]```\n\nConnect to a **predefined group** from ~/.mssh_clusters\n\n```mssh -a [alias_name]```\n\nConnect to hosts on **specific ports**\n\n```mssh [user@host1:2222] [user@host2:22]```"],["SYNOPSIS","**mssh** [_options_] (**-a** _alias_ | _hosts_[:_port_])"],["PARAMETERS","**-a, --alias _alias_**\n> Open hosts associated with named alias from configuration\n\n**-h, --help**\n> Display help and exit\n\n**-V, --version**\n> Output version information and exit"],["KEYBOARD SHORTCUTS","**Modifier + Arrow keys**\n> Navigate between terminal windows\n\n**Ctrl + Shift + x**\n> Toggle terminal maximization\n\n**Ctrl + Shift + n**\n> Open dialog to add new hosts"],["CONFIGURATION","Configuration file: **~/.mssh_clusters**\n\nAliases map to space-separated host lists:\n```\nwebservers = web1.example.com web2.example.com web3.example.com\ndbservers = db1.example.com db2.example.com\n```\n\nCommand aliases in curly braces map to text sent to terminals:\n```\n{uptime} = uptime\n{df} = df -h\n```"],["DESCRIPTION","**mssh** (MultiSSH) is a GTK+ based SSH client for issuing commands to multiple servers simultaneously. It displays multiple terminal windows in a grid layout, allowing administrators to type commands once and have them executed across all connected hosts.\n\nThe interface supports navigation between terminals, individual or grouped input, and predefined server groups through the configuration file."],["CAVEATS","Requires a graphical display (X11 or Wayland with XWayland). Commands are sent to all terminals by default; use mouse click to select individual terminals. Host aliases must be defined in ~/.mssh_clusters before use."],["HISTORY","**mssh** was originally created by **Bradley Smith** in 2009 and is currently maintained by **Héctor García**. It provides a graphical alternative to text-based multi-SSH tools like cssh and pdsh."],["INSTALL","```apt: sudo apt install mssh```"],["SEE ALSO","[ssh](/man/ssh)(1), [tmux](/man/tmux)(1)"]],"e":["ssh","tmux"],"x":"mssh [user@host1] [user@host2] [user@host3]"},"npm-find-dupes":{"n":"npm-find-dupes","t":"searches for duplicate packages in node_modules","s":[["TAGLINE","searches for duplicate packages in node_modules"],["TLDR","**Find duplicate packages**\n\n```npm find-dupes```"],["SYNOPSIS","**npm** **find-dupes**"],["DESCRIPTION","**npm find-dupes** searches for duplicate packages in node_modules. Reports packages installed multiple times at different versions. Use npm dedupe to resolve duplicates."],["INSTALL","```pacman: sudo pacman -S npm```\n\n```apk: sudo apk add npm```\n\n```brew: brew install npm```"],["SEE ALSO","[npm](/man/npm)(1), [npm-dedupe](/man/npm-dedupe)(1)"]],"e":["npm","npm-dedupe"],"x":"npm find-dupes"},"oakdecode":{"n":"oakdecode","t":"decode an OAKT printer stream into human readable form","s":[["TAGLINE","decode an OAKT printer stream into human readable form"],["TLDR","**Decode an OAKT printer stream from a file**\n\n```oakdecode < [file.prn]```\n\n**Decode and save decompressed planes as PBM files**\n\n```oakdecode -d [basename] < [file.prn]```\n\n**Decode and save raw planes as JBIG files**\n\n```oakdecode -r [basename] < [file.prn]```\n\n**Decode with file offsets shown in output**\n\n```oakdecode -o < [file.prn]```\n\n**Decode without displaying image records**\n\n```oakdecode -i < [file.prn]```"],["SYNOPSIS","**oakdecode** [_options_] <_OAKT-file_"],["PARAMETERS","**-d** _BASENAME_\n> Save decompressed planes as .pbm files with specified basename.\n\n**-r** _BASENAME_\n> Save raw planes as .jbg files with specified basename.\n\n**-i**\n> Suppress display of image records in output.\n\n**-o**\n> Include file offsets in the output.\n\n**-D** _LEVEL_\n> Set debug verbosity level (default: 0)."],["DESCRIPTION","**oakdecode** decodes an OAKT printer stream into human readable form. It is designed for printers that use the OAKT printer language, such as the HP Color LaserJet 1500. The tool reads the printer stream from standard input and outputs decoded records showing page setup, compression information, and image data in a readable format.\n\nThe tool is part of the foo2oak printer driver suite and is primarily used for debugging and analyzing OAKT format print jobs."],["CAVEATS","Reads from standard input only. Specific to the OAKT printer language used by certain HP Color LaserJet models."],["INSTALL","```apt: sudo apt install printer-driver-foo2zjs```"],["SEE ALSO","[foo2oak](/man/foo2oak)(1), [foo2oak-wrapper](/man/foo2oak-wrapper)(1)"]],"e":["foo2oak","foo2oak-wrapper"],"x":"oakdecode < [file.prn]"},"opencode":{"n":"opencode","t":"open-source AI coding agent that runs in your terminal, desktop, or IDE","s":[["TAGLINE","open-source AI coding agent that runs in your terminal, desktop, or IDE"],["TLDR","**Launch the interactive TUI**\n\n```opencode```\n\n**Open a specific project directory**\n\n```opencode [path/to/project]```\n\n**Run a prompt non-interactively**\n\n```opencode run \"[fix the bug in main.go]\"```\n\n**Use a specific model**\n\n```opencode --model [anthropic/claude-sonnet-4]```\n\n**Continue the last session**\n\n```opencode --continue```\n\n**Resume a specific session**\n\n```opencode --session [session_id]```\n\n**Start the web interface**\n\n```opencode web```\n\n**List available models**\n\n```opencode models```"],["SYNOPSIS","**opencode** [_command_] [_options_] [_project_]"],["PARAMETERS","**--model**, **-m** _PROVIDER/MODEL_\n> Specify the AI model to use (e.g., anthropic/claude-sonnet-4, openai/gpt-4o)\n\n**--continue**, **-c**\n> Resume the last session\n\n**--session**, **-s** _ID_\n> Continue a specific session by ID\n\n**--prompt** _TEXT_\n> Provide an initial prompt when launching\n\n**--agent** _NAME_\n> Select a specific agent (build, plan, or custom)\n\n**--port** _NUMBER_\n> Server port for web or serve commands\n\n**--help**, **-h**\n> Display help information\n\n**--version**, **-v**\n> Show version number"],["COMMANDS","**run**\n> Execute a prompt non-interactively and exit\n\n**serve**\n> Start a headless server for API access\n\n**web**\n> Launch the web interface with HTTP server\n\n**attach**\n> Connect TUI to a remote OpenCode server\n\n**auth login**\n> Configure API keys for providers\n\n**models**\n> Display all available models across configured providers\n\n**session list**\n> Show all saved sessions\n\n**stats**\n> View token usage and cost analytics\n\n**mcp add**\n> Register an MCP (Model Context Protocol) server"],["DESCRIPTION","**OpenCode** is an open-source AI coding agent that runs in your terminal, desktop, or IDE. It provides Claude-Code-level capabilities without locking you to a single model or platform, supporting over 75 AI models including Claude, GPT, Gemini, and local models.\n\nThe default TUI mode offers an interactive chat interface with two built-in agents: **build** (full access for development) and **plan** (read-only for analysis). Switch between them with the Tab key. OpenCode integrates with Language Server Protocol for code intelligence and connects to your GitHub workflow for CI/CD automation.\n\nUnlike cloud-based assistants, OpenCode gives you control over your data. You choose the model provider and what gets shared. Running a local model keeps your entire workflow private."],["CAVEATS","Requires API keys for cloud model providers (configured via **opencode auth login**). Token usage can be significant for large codebases. The tool defaults to launching a TUI which requires a compatible terminal emulator."],["HISTORY","OpenCode was created by **Jay V**, **Frank Wang**, **Dax Raad**, and **Adam Elmore** from the team behind SST (Serverless Stack). It launched on **June 19, 2024** in response to the rise of proprietary AI coding assistants. The project grew to **650,000 monthly users** within five months and has over **70,000 GitHub stars**. It is released under the MIT license."],["INSTALL","```pacman: sudo pacman -S opencode```\n\n```brew: brew install opencode```\n\n```nix: nix profile install nixpkgs#opencode```"],["SEE ALSO","[aider](/man/aider)(1), [claude](/man/claude)(1), [copilot](/man/copilot)(1)"]],"e":["aider","claude","copilot"],"x":"opencode"},"openrgb":{"n":"openrgb","t":"Open source RGB lighting control","s":[["TAGLINE","Open source RGB lighting control"],["TLDR","**Start GUI**\n\n```openrgb```\n\n**List devices**\n\n```openrgb -l```\n\n**Set color on device**\n\n```openrgb -d [0] -c [FF0000]```\n\n**Set mode**\n\n```openrgb -d [0] -m [Static]```\n\n**Apply profile**\n\n```openrgb -p [profile.orp]```\n\n**Set color on a specific zone**\n\n```openrgb -d [0] -z [0] -c [00FF00]```\n\n**Start SDK server**\n\n```openrgb --server```\n\n**Start server on custom port**\n\n```openrgb --server --server-port [1234]```"],["SYNOPSIS","**openrgb** [_options_]"],["PARAMETERS","**-l**, **--list-devices**\n> List all detected RGB devices with their index numbers.\n\n**-d** _INDEX_, **--device** _INDEX_\n> Select device by index number or name. Applies to all devices if omitted.\n\n**-z** _INDEX_, **--zone** _INDEX_\n> Select zone on device.\n\n**-c** _COLOR_, **--color** _COLOR_\n> Set color in hex format (e.g., FF0000 for red).\n\n**-m** _MODE_, **--mode** _MODE_\n> Set lighting mode (e.g., Static, Breathing, Rainbow).\n\n**-s** _SIZE_, **--size** _SIZE_\n> Set zone size (for resizable zones).\n\n**-p** _PROFILE_\n> Load a saved profile.\n\n**--server**\n> Start the SDK server (default port 6742).\n\n**--server-port** _PORT_\n> Set SDK server port (range 1024-65535).\n\n**--client** _IP:PORT_\n> Connect to an SDK server.\n\n**-v**, **--verbose**\n> Print log messages to stdout.\n\n**--startminimized**\n> Start the GUI minimized to system tray.\n\n**--help**\n> Display help information."],["DESCRIPTION","**OpenRGB** is an open source RGB lighting control application that provides a unified interface across many hardware brands including ASUS, Corsair, Logitech, MSI, Razer, and others.\n\nThe tool provides both a GUI and CLI for controlling RGB LEDs on motherboards, RAM, GPUs, peripherals, and other devices. It works cross-platform on Linux, Windows, and macOS, and includes an SDK server for remote control and integration with other applications."],["CAVEATS","Hardware support varies. May need kernel modules. Root for some devices."],["HISTORY","OpenRGB was created for **unified RGB lighting control** across different brands."],["INSTALL","```dnf: sudo dnf install openrgb```\n\n```pacman: sudo pacman -S openrgb```\n\n```apk: sudo apk add openrgb```\n\n```nix: nix profile install nixpkgs#openrgb```"],["SEE ALSO","[i2cdetect](/man/i2cdetect)(1)"]],"e":["i2cdetect"],"x":"openrgb"},"osmium":{"n":"osmium","t":"command-line tool for working with OpenStreetMap data","s":[["TAGLINE","command-line tool for working with OpenStreetMap data"],["TLDR","**Extract area from OSM file**\n\n```osmium extract -b [left],[bottom],[right],[top] [input.osm.pbf] -o [output.osm.pbf]```\n\n**Convert format**\n\n```osmium cat [input.osm.pbf] -o [output.osm.xml]```\n\n**Merge multiple files**\n\n```osmium merge [file1.osm.pbf] [file2.osm.pbf] -o [merged.osm.pbf]```\n\n**Filter by tags**\n\n```osmium tags-filter [input.osm.pbf] -o [output.osm.pbf] nwr/[highway]```\n\n**Show file info**\n\n```osmium fileinfo [file.osm.pbf]```\n\n**Check file integrity**\n\n```osmium check-refs [file.osm.pbf]```\n\n**Sort OSM data**\n\n```osmium sort [input.osm] -o [sorted.osm]```\n\n**Get difference between files**\n\n```osmium diff [old.osm.pbf] [new.osm.pbf]```\n\n**Extract using polygon file**\n\n```osmium extract -p [boundary.poly] [input.osm.pbf] -o [output.osm.pbf]```\n\n**Apply an OsmChange file**\n\n```osmium apply-changes [input.osm.pbf] [changes.osc] -o [output.osm.pbf]```"],["SYNOPSIS","**osmium** _command_ [_-o output_] [_options_] _files_"],["PARAMETERS","**cat**\n> Concatenate/convert files.\n\n**extract**\n> Extract region from file.\n\n**merge**\n> Merge sorted files.\n\n**tags-filter**\n> Filter by tags.\n\n**sort**\n> Sort OSM data.\n\n**diff**\n> Compare files.\n\n**fileinfo**\n> Show file information.\n\n**check-refs**\n> Check referential integrity.\n\n**apply-changes**\n> Apply OsmChange file.\n\n**renumber**\n> Renumber object IDs.\n\n**getid**\n> Get objects by ID.\n\n**time-filter**\n> Filter by timestamp.\n\n**-o** _FILE_\n> Output file.\n\n**-b** _BBOX_\n> Bounding box (left,bottom,right,top).\n\n**-p** _POLYGON_\n> Polygon file for extraction.\n\n**--overwrite**\n> Overwrite existing output.\n\n**-f** _FORMAT_\n> Output format.\n\n**-v**, **--verbose**\n> Verbose output.\n\n**--progress**\n> Show progress bar.\n\n**-e** _STRATEGY_, **--strategy** _STRATEGY_\n> Extraction strategy: simple, complete_ways, or smart."],["DESCRIPTION","**osmium** is a command-line tool for working with OpenStreetMap data. It processes, filters, and transforms OSM files efficiently.\n\nExtract creates regional subsets from larger files. Bounding boxes define rectangular areas. Polygon files enable extraction of complex shapes like administrative boundaries.\n\nTags-filter creates files containing only specific feature types. Tag expressions select highways, buildings, points of interest, or custom combinations.\n\nMerging combines multiple files into one, handling overlapping data correctly. Files must be sorted for merging. The sort command prepares unsorted data.\n\nFormat conversion handles PBF (Protocol Buffer Binary), OSM XML, and OPL (Object Per Line) formats. PBF is compressed and efficient; XML is human-readable.\n\nDiff shows changes between file versions, useful for tracking map edits over time."],["CAVEATS","Large files require significant memory. Some operations need sorted input. Extraction may include incomplete relations. Complex filters have learning curve."],["HISTORY","**osmium** and the libosmium library were developed by **Jochen Topf** starting around **2013**. It became the standard command-line tool for OSM data processing, replacing older tools with better performance and reliability."],["INSTALL","```apt: sudo apt install osmium-tool```\n\n```dnf: sudo dnf install osmium-tool```\n\n```brew: brew install osmium-tool```\n\n```nix: nix profile install nixpkgs#osmium-tool```"],["SEE ALSO","[osm2pgsql](/man/osm2pgsql)(1), [osmconvert](/man/osmconvert)(1), [ogr2ogr](/man/ogr2ogr)(1)"]],"e":["osm2pgsql","osmconvert","ogr2ogr"],"x":"osmium extract -b [left],[bottom],[right],[top] [input.osm.pbf] -o [output.osm.pbf]"},"pammixinterlace":{"n":"pammixinterlace","t":"Mix adjacent lines to reduce interlacing artifacts in video frames","s":[["TAGLINE","Mix adjacent lines to reduce interlacing artifacts in video frames"],["TLDR","**Blend interlaced video fields using default FIR filter**\n\n```pammixinterlace [input.pam] > [output.pam]```\n\n**Use linear blend filter**\n\n```pammixinterlace -filter=linear [input.pam] > [output.pam]```\n\n**Use adaptive filtering to modify only comb-pattern pixels**\n\n```pammixinterlace -adaptive [input.pam] > [output.pam]```\n\n**Combine adaptive mode with a specific filter**\n\n```pammixinterlace -adaptive -filter=ffmpeg [input.pam] > [output.pam]```"],["SYNOPSIS","**pammixinterlace** [**-filter=**{**linear**|**fir**|**ffmpeg**}] [**-adaptive**] [_file_]"],["PARAMETERS","**-filter=**{**linear**|**fir**|**ffmpeg**}\n> Choose the filtering mechanism. **linear** uses a linear-blend formula. **fir** uses a size-5 FIR low-pass filter. **ffmpeg** uses a formula from the ffmpeg documentation. Default is **fir**.\n\n**-adaptive**\n> Enable adaptive filtering mode. Only modifies pixels that are obviously part of a \"comb\" pattern, leaving other pixels unchanged."],["DESCRIPTION","**pammixinterlace** makes each row of the output a mixture of the corresponding row of the input and its two neighbors, using half of the main row and a quarter each of the two neighbor rows. This blending reduces interlacing artifacts (combing) in video frames captured from interlaced sources.\n\nThe tool is useful for deinterlacing video captures for still image use. Part of the Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pamdeinterlace](/man/pamdeinterlace)(1), [pamscale](/man/pamscale)(1), [netpbm](/man/netpbm)(1)"]],"e":["pamdeinterlace","pamscale","netpbm"],"x":"pammixinterlace [input.pam] > [output.pam]"},"pamslice":{"n":"pamslice","t":"extract a single row or column from a Netpbm image as text","s":[["TAGLINE","extract a single row or column from a Netpbm image as text"],["TLDR","**Extract a horizontal slice** (row) from an image\n\n```pamslice -row=[100] [input.pam]```\n\n**Extract a vertical slice** (column) from an image\n\n```pamslice -column=[50] [input.pam]```\n\n**Extract only the red plane** (plane 0) of a row\n\n```pamslice -row=[100] -plane=[0] [input.ppm]```\n\n**Format output for xmgr plotting**\n\n```pamslice -row=[100] -xmgr [input.pam]```"],["SYNOPSIS","**pamslice** {**-row**=_rownumber_ | **-column**=_columnnumber_} [**-plane**=_planenumber_] [**-xmgr**] [_imagefile_]"],["PARAMETERS","_IMAGEFILE_\n> Input PAM, PBM, PGM, or PPM image file. Reads from standard input if omitted.\n\n**-row**=_N_\n> Extract row N as a horizontal slice. Rows are numbered from the top starting with 0.\n\n**-column**=_N_\n> Extract column N as a vertical slice. Columns are numbered from the left starting with 0.\n\n**-plane**=_N_\n> Restrict output to a single color plane. For PPM images: 0=red, 1=green, 2=blue. Omit for all planes.\n\n**-xmgr**\n> Format output as input for the xmgr plotting program by adding header information."],["DESCRIPTION","**pamslice** extracts one line of pixels (a row or column) from a Netpbm image and prints the sample values as ASCII decimal numbers. The first number on each output line is the position index (column number for row slices, row number for column slices), followed by the sample values for each plane.\n\nThis is useful for generating profile data from images, such as plotting intensity values along a scanline for image analysis. The output can be fed directly into plotting tools like xmgr using the **-xmgr** option.\n\nEither **-row** or **-column** must be specified, but not both."],["CAVEATS","Part of Netpbm. Output is plain text only, not an image. Row and column numbering starts at 0."],["HISTORY","pamslice is part of the **Netpbm** toolkit for image manipulation and analysis. It was added as a tool for extracting profile data from images."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pamcut](/man/pamcut)(1), [pamfile](/man/pamfile)(1), [pamtopnm](/man/pamtopnm)(1), [netpbm](/man/netpbm)(1)"]],"e":["pamcut","pamfile","pamtopnm","netpbm"],"x":"pamslice -row=[100] [input.pam]"},"parsehawk":{"n":"parsehawk","t":"Local-first document data extraction CLI","s":[["TAGLINE","Local-first document data extraction CLI"],["TLDR","**Start** the local ParseHawk service stack\n\n```parsehawk start```\n\n\n**Check** the service status\n\n```parsehawk status```\n\n\n**Run a setup** health check\n\n```parsehawk doctor```\n\n\n**Upload** a document\n\n```parsehawk files upload [document.pdf]```\n\n\n**Run a one-shot extraction** on a file using an extractor\n\n```parsehawk extract [document.pdf] --extractor [extractor_id]```\n\n\n**Extract with ad-hoc instructions** and wait for the result\n\n```parsehawk extract [document.pdf] --instructions \"[extract invoice total and date]\" --wait```\n\n\n**Stop** the running service\n\n```parsehawk stop```"],["SYNOPSIS","**parsehawk** \\<command\\> [subcommand] [options]"],["DESCRIPTION","ParseHawk is a local-first document AI tool that turns documents (PDFs, scans, images, text, and Markdown) into validated, structured JSON. By default it runs entirely on the local machine with no third-party API calls, using a quantized extraction model served through vLLM.\n\nThe project ships as three clients over a single backend: a REST API, a web UI, and this command-line interface. The CLI both manages the backend service (**start**, **stop**, **status**) and drives the data workflow (uploading files, defining extractors and schemas, and running extraction jobs). Extraction is zero-shot or few-shot: instead of training a model, the user supplies a JSON Schema, natural-language instructions, and optional examples, and the model returns schema-conformant JSON.\n\nMost data operations require the backend stack to be running, so a typical session begins with **parsehawk start** and ends with **parsehawk stop**."],["COMMANDS","**start**\n\n> Start the ParseHawk service stack.\n\n**stop**\n\n> Stop the running service.\n\n**restart**\n\n> Restart the service.\n\n**status**\n\n> Show whether the service is running.\n\n**doctor**\n\n> Run local setup health checks.\n\n**dev**\n\n> Start in development mode with hot reload.\n\n**extract** \\<file\\>\n\n> Run a one-shot extraction on a file without creating a separate job.\n\n**files upload** \\<file\\>\n\n> Upload a document to the service.\n\n**files list**\n\n> List uploaded files.\n\n**files delete** \\<file_id\\>\n\n> Remove an uploaded file.\n\n**schemas validate** \\<schema.json\\>\n\n> Validate a JSON Schema file.\n\n**extractors create**\n\n> Define a new set of extraction rules.\n\n**extractors list**\n\n> Show available extractors.\n\n**extractors get** \\<extractor_id\\>\n\n> Retrieve the details of an extractor.\n\n**extractors delete** \\<extractor_id\\>\n\n> Remove an extractor.\n\n**jobs create**\n\n> Start an extraction job.\n\n**jobs get** \\<job_id\\>\n\n> Retrieve the status and results of a job.\n\n**config list**\n\n> Display current settings.\n\n**config set** \\<key\\> \\<value\\>\n\n> Change a configuration value."],["PARAMETERS","**--extractor** \\<id\\>\n\n> Select which extractor to use.\n\n**--file-id** \\<id\\>\n\n> Select an already-uploaded file as input.\n\n**--schema** \\<file\\>\n\n> Provide a JSON Schema describing the desired output.\n\n**--instructions** \\<text\\>\n\n> Give natural-language extraction directives.\n\n**--wait**\n\n> Block until the job completes instead of returning immediately.\n\n**--runtime none**\n\n> Skip local model inference."],["CONFIGURATION","Behavior can be tuned through environment variables, which override defaults:\n\n**PARSEHAWK_VLLM_MAX_MODEL_LEN**\n\n> Maximum model context length (for example, 16384).\n\n**PARSEHAWK_VLLM_GPU_MEMORY_UTILIZATION**\n\n> Fraction of GPU memory the model runtime may use (for example, 0.6).\n\n**PARSEHAWK_VLLM_MAX_NUM_SEQS**\n\n> Maximum number of sequences processed in parallel.\n\n**PARSEHAWK_TELEMETRY_DISABLED**\n\n> Set to 1 to opt out of anonymous usage analytics."],["CAVEATS","The CLI controls a persistent backend service, so most operations need **parsehawk start** to have been run first. Running the local model requires significant resources: roughly 16 GB of unified memory on macOS (Apple Silicon) or 16 GB of VRAM on Linux with an NVIDIA GPU, plus Docker. Windows is not supported. Anonymous usage analytics are sent unless disabled via **PARSEHAWK_TELEMETRY_DISABLED**."],["HISTORY","ParseHawk is built in **Python** (FastAPI backend) with a **React**/**Vite** web UI, and runs its default extraction model (NuExtract3) locally through **vLLM**. It is released under the **Apache-2.0** license."],["SEE ALSO","[tesseract](/man/tesseract)(1), [pdftotext](/man/pdftotext)(1), [jq](/man/jq)(1), [docker](/man/docker)(1)"],["RESOURCES","```[Source code](https://github.com/parsehawk/parsehawk)```"]],"e":["tesseract","pdftotext","jq","docker"],"x":"parsehawk start"},"passwd":{"n":"passwd","t":"changes user passwords","s":[["TAGLINE","changes user passwords"],["TLDR","**Change your password**\n\n```passwd```\n\n**Change another user's password** (root)\n\n```passwd [username]```\n\n**Lock a user account**\n\n```passwd -l [username]```\n\n**Unlock a user account**\n\n```passwd -u [username]```\n\n**Force password change** on next login\n\n```passwd -e [username]```\n\n**Set password to expire** in days\n\n```passwd -x [90] [username]```\n\n**Show password status**\n\n```passwd -S [username]```\n\n**Delete user's password** (passwordless)\n\n```passwd -d [username]```"],["SYNOPSIS","**passwd** [_options_] [_username_]"],["DESCRIPTION","**passwd** changes user passwords. When run without arguments by a regular user, it prompts for the current password and then allows setting a new one. Root can change any user's password without knowing the old one.\n\nThe tool enforces password policies including minimum length, complexity requirements, and history restrictions. These policies are configured in /etc/login.defs and PAM configuration.\n\npasswd also manages password aging, allowing administrators to force periodic password changes, expire passwords, and lock/unlock accounts."],["PARAMETERS","**-l**, **--lock**\n> Lock the account password.\n\n**-u**, **--unlock**\n> Unlock the account password.\n\n**-d**, **--delete**\n> Delete password (make passwordless).\n\n**-e**, **--expire**\n> Force password change on next login.\n\n**-n** _days_, **--mindays** _days_\n> Minimum days between changes.\n\n**-x** _days_, **--maxdays** _days_\n> Maximum days before change required.\n\n**-w** _days_, **--warndays** _days_\n> Days before expiry to warn user.\n\n**-i** _days_, **--inactive** _days_\n> Days after expiry before account disabled.\n\n**-S**, **--status**\n> Show password status information.\n\n**-a**, **--all**\n> With -S, show status for all users.\n\n**--stdin**\n> Read password from stdin (non-interactive)."],["CAVEATS","Requires root to change other users' passwords. PAM modules may enforce additional password requirements. Locked accounts can still be accessed via SSH keys. Password policies vary by system configuration."],["HISTORY","**passwd** has been part of Unix since the earliest versions, dating back to **Version 1 Unix** at **Bell Labs** in **1971**. The password file originally contained hashed passwords directly, but modern systems use shadow passwords (/etc/shadow) for security, introduced in **SunOS** in the **1980s**."],["INSTALL","```apt: sudo apt install passwd```\n\n```pacman: sudo pacman -S shadow```\n\n```apk: sudo apk add shadow```\n\n```zypper: sudo zypper install shadow```\n\n```nix: nix profile install nixpkgs#shadow```"],["SEE ALSO","[chage](/man/chage)(1), [usermod](/man/usermod)(8), [login.defs](/man/login.defs)(5)"]],"e":["chage","usermod","login.defs"],"x":"passwd"},"pbmtoicon":{"n":"pbmtoicon","t":"converts PBM bitmaps to Sun icon format used by older Sun workstations","s":[["TAGLINE","converts PBM bitmaps to Sun icon format used by older Sun workstations"],["TLDR","**Convert PBM to Sun icon format**\n\n```pbmtoicon [input.pbm] > [output.icon]```"],["SYNOPSIS","**pbmtoicon** [_file_]"],["DESCRIPTION","**pbmtoicon** converts PBM bitmaps to Sun icon format used by older Sun workstations. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[icontopbm](/man/icontopbm)(1), [pbmtosunicon](/man/pbmtosunicon)(1)"]],"e":["icontopbm","pbmtosunicon"],"x":"pbmtoicon [input.pbm] > [output.icon]"},"pg_dump":{"n":"pg_dump","t":"utility for backing up PostgreSQL databases","s":[["TAGLINE","utility for backing up PostgreSQL databases"],["TLDR","**Dump a database**\n\n```pg_dump [database_name] > [backup.sql]```\n\n**Dump with compression**\n\n```pg_dump -Fc [database_name] > [backup.dump]```\n\n**Dump specific table**\n\n```pg_dump -t [table_name] [database_name] > [table.sql]```\n\n**Dump schema only**\n\n```pg_dump -s [database_name] > [schema.sql]```\n\n**Dump data only**\n\n```pg_dump -a [database_name] > [data.sql]```\n\n**Dump with connection parameters**\n\n```pg_dump -h [host] -p [5432] -U [username] [database_name] > [backup.sql]```\n\n**Dump in directory format** (parallel)\n\n```pg_dump -Fd -j [4] [database_name] -f [backup_dir]```\n\n**Dump excluding table**\n\n```pg_dump --exclude-table=[pattern] [database_name] > [backup.sql]```"],["SYNOPSIS","**pg_dump** [_options_] [_dbname_]"],["DESCRIPTION","**pg_dump** is a utility for backing up PostgreSQL databases. It creates a consistent snapshot of the database at the time the dump begins, even while the database is being used.\n\nThe output can be in SQL script format (plain text) or custom archive formats that support compression and parallel restore. SQL scripts can be restored with psql, while archive formats use pg_restore.\n\npg_dump does not block readers or writers during the dump, making it suitable for production backups."],["PARAMETERS","**-F** _format_\n> Output format (p=plain, c=custom, d=directory, t=tar).\n\n**-f** _file_\n> Output file or directory.\n\n**-t** _table_\n> Dump specific table(s).\n\n**-T** _table_\n> Exclude table(s).\n\n**-n** _schema_\n> Dump specific schema(s).\n\n**-N** _schema_\n> Exclude schema(s).\n\n**-s**, **--schema-only**\n> Dump schema only, no data.\n\n**-a**, **--data-only**\n> Dump data only, no schema.\n\n**-c**, **--clean**\n> Include DROP commands.\n\n**-C**, **--create**\n> Include CREATE DATABASE command.\n\n**-j** _jobs_\n> Parallel dump jobs (directory format).\n\n**-h** _host_\n> Database server host.\n\n**-p** _port_\n> Database server port.\n\n**-U** _user_\n> Connect as user.\n\n**-W**\n> Force password prompt.\n\n**-Z** _level_\n> Compression level (0-9)."],["CAVEATS","Does not dump roles and tablespaces (use pg_dumpall). Plain format cannot be restored in parallel. Custom format recommended for large databases. May require significant disk space for large databases."],["HISTORY","**pg_dump** has been part of PostgreSQL since its earliest releases, evolving from the original **POSTGRES** project at **UC Berkeley** in the **1980s**. It has gained features like parallel dump, custom archive format, and selective backup options as PostgreSQL evolved into a major database system."],["INSTALL","```apk: sudo apk add postgresql17-client```"],["SEE ALSO","[pg_restore](/man/pg_restore)(1), [pg_dumpall](/man/pg_dumpall)(1), [psql](/man/psql)(1), [createdb](/man/createdb)(1)"]],"e":["pg_restore","pg_dumpall","psql","createdb"],"x":"pg_dump [database_name] > [backup.sql]"},"pharo":{"n":"pharo","t":"launcher for Pharo Smalltalk, a modern, open-source Smalltalk environment","s":[["TAGLINE","launcher for Pharo Smalltalk, a modern, open-source Smalltalk environment"],["TLDR","**Launch Pharo image**\n\n```pharo [image.image]```\n\n**Run headless**\n\n```pharo --headless [image.image] [script.st]```\n\n**Evaluate expression**\n\n```pharo [image.image] eval \"[expression]\"```\n\n**Start with GUI**\n\n```pharo-ui [image.image]```"],["SYNOPSIS","**pharo** [_options_] _image_ [_arguments_]"],["PARAMETERS","**--headless**\n> Run without GUI.\n\n**--no-quit**\n> Don't quit after script.\n\n**eval**\n> Evaluate Smalltalk expression.\n\n**--version**\n> Show version.\n\n**image**\n> Pharo image file."],["DESCRIPTION","**pharo** is the launcher for Pharo Smalltalk, a modern, open-source Smalltalk environment. Pharo uses image-based persistence where the entire environment state is saved."],["Launch GUI","pharo-ui Pharo.image"],["Run script headless","pharo --headless Pharo.image script.st"],["Evaluate and print","pharo Pharo.image eval \"3 + 4\""],["Save and quit","pharo Pharo.image eval \"Smalltalk snapshot: true andQuit: true\""],["Run tests","pharo --headless Pharo.image test MyPackage\n```"],["IMAGE MANAGEMENT","```bash"],["Download fresh image","curl https://get.pharo.org/64/ | bash"],["Create clean image","pharo Pharo.image save MyProject\n```"],["CAVEATS","Requires Pharo VM and image. Image files are platform-specific. Changes persist in image."],["HISTORY","Pharo forked from **Squeak** Smalltalk in 2008, focusing on clean, innovative Smalltalk development."],["SEE ALSO","[squeak](/man/squeak)(1)"],["INSTALL","```nix: nix profile install nixpkgs#pharo```"]],"e":["squeak"],"x":"pharo [image.image]"},"pnmsmooth":{"n":"pnmsmooth","t":"Smooth and blur PNM images","s":[["TAGLINE","Smooth and blur PNM images"],["TLDR","**Smooth/blur image**\n\n```pnmsmooth [input.pnm] > [output.pnm]```\n\n**Smooth with custom size**\n\n```pnmsmooth -size [5] [input.pnm] > [output.pnm]```\n\n**Multiple passes**\n\n```pnmsmooth [input.pnm] | pnmsmooth > [output.pnm]```"],["SYNOPSIS","**pnmsmooth** [_options_] [_pnmfile_]"],["PARAMETERS","**-size** _n_\n> Convolution size (default 3).\n\n**-dump** _file_\n> Dump convolution matrix."],["DESCRIPTION","**pnmsmooth** applies a smoothing (blur) filter to a PNM image using convolution. It averages neighboring pixels to reduce noise and sharp edges.\n\nWrapper around pnmconvol with a smoothing kernel."],["EXAMPLES","```bash"],["Basic smoothing","pnmsmooth noisy.ppm > smooth.ppm"],["Stronger smoothing with larger kernel","pnmsmooth -size 5 image.ppm > blurred.ppm"],["Multiple smoothing passes","pnmsmooth image.ppm | pnmsmooth | pnmsmooth > very_smooth.ppm"],["Reduce noise then enhance","pnmsmooth noisy.pgm | pgmenhance > cleaned.pgm\n```"],["CONVOLUTION","Default 3x3 averaging kernel:\n```\n1 1 1\n1 1 1\n1 1 1\n```\n\nLarger sizes create stronger blur effect."],["CAVEATS","Reduces image sharpness. Use pnmconvol for custom kernels. Part of Netpbm."],["HISTORY","pnmsmooth is part of **Netpbm** by **Jef Poskanzer**, providing simple image smoothing via convolution."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pnmconvol](/man/pnmconvol)(1), [pgmenhance](/man/pgmenhance)(1), [netpbm](/man/netpbm)(1)"]],"e":["pnmconvol","pgmenhance","netpbm"],"x":"pnmsmooth [input.pnm] > [output.pnm]"},"ppmtobmp":{"n":"ppmtobmp","t":"Convert PPM images to BMP format","s":[["TAGLINE","Convert PPM images to BMP format"],["TLDR","**Convert PPM to BMP**\n\n```ppmtobmp [input.ppm] > [output.bmp]```\n\n**Create Windows BMP**\n\n```ppmtobmp -windows [input.ppm] > [output.bmp]```\n\n**Create OS/2 BMP**\n\n```ppmtobmp -os2 [input.ppm] > [output.bmp]```\n\n**Force 8 bits per pixel**\n\n```ppmtobmp -bpp 8 [input.ppm] > [output.bmp]```"],["SYNOPSIS","**ppmtobmp** [**-windows**|**-os2**] [**-bpp** _n_] [**-mapfile** _file_] [_ppmfile_]"],["PARAMETERS","**-windows**\n> Produce a Microsoft Windows BMP file. This is the default (since August 2000).\n\n**-os2**\n> Produce an OS/2 BMP file.\n\n**-bpp** _bits_\n> Bits per pixel in the output: 1, 4, 8, or 24. By default ppmtobmp picks the smallest value that can represent all colors in the input.\n\n**-mapfile** _file_\n> Use the given file as the BMP palette (colormap) instead of computing one automatically. Useful when you need specific palette index values."],["DESCRIPTION","**ppmtobmp** converts PPM images to BMP (Windows/OS2 bitmap) format. Part of Netpbm toolkit for image format conversion."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[bmptopnm](/man/bmptopnm)(1), [pnmtopng](/man/pnmtopng)(1)"]],"e":["bmptopnm","pnmtopng"],"x":"ppmtobmp [input.ppm] > [output.bmp]"},"ppmtoterm":{"n":"ppmtoterm","t":"Display PPM images as colored terminal text","s":[["TAGLINE","Display PPM images as colored terminal text"],["TLDR","**Display image in terminal**\n\n```ppmtoterm [input.ppm]```"],["SYNOPSIS","**ppmtoterm** [_file_]"],["DESCRIPTION","**ppmtoterm** displays PPM images directly in the terminal using ANSI color codes. Creates colored block character output. Part of Netpbm toolkit for terminal graphics."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[ppmtosixel](/man/ppmtosixel)(1), [ppmtoascii](/man/ppmtoascii)(1)"]],"e":["ppmtosixel","ppmtoascii"],"x":"ppmtoterm [input.ppm]"},"pueue-add":{"n":"pueue-add","t":"Add commands to the pueue task queue","s":[["TAGLINE","Add commands to the pueue task queue"],["TLDR","**Add command to queue**\n\n```pueue add [command]```\n\n**Add with immediate start**\n\n```pueue add -i [command]```\n\n**Add to specific group**\n\n```pueue add -g [group] [command]```\n\n**Add with delay**\n\n```pueue add --delay \"10min\" [command]```\n\n**Add paused task**\n\n```pueue add --stashed [command]```"],["SYNOPSIS","**pueue** **add** [_options_] _command_"],["PARAMETERS","**-i**, **--immediate**\n> Start immediately.\n\n**-g**, **--group** _name_\n> Add to group.\n\n**--delay** _duration_\n> Delay before starting.\n\n**--stashed**\n> Add as stashed (paused).\n\n**-l**, **--label** _label_\n> Task label.\n\n**-a**, **--after** _ids_\n> Start after other tasks.\n\n**-p**, **--priority** _n_\n> Task priority."],["DESCRIPTION","**pueue add** adds a new command to the task queue. Commands are queued and executed in order. Supports task grouping, dependencies, priorities, and delayed execution."],["INSTALL","```pacman: sudo pacman -S pueue```\n\n```apk: sudo apk add pueue```\n\n```zypper: sudo zypper install pueue```\n\n```brew: brew install pueue```\n\n```nix: nix profile install nixpkgs#pueue```"],["SEE ALSO","[pueue](/man/pueue)(1), [pueue-status](/man/pueue-status)(1)"]],"e":["pueue","pueue-status"],"x":"pueue add [command]"},"pueue-remove":{"n":"pueue-remove","t":"Remove tasks from the pueue queue","s":[["TAGLINE","Remove tasks from the pueue queue"],["TLDR","**Remove task**\n\n```pueue remove [task_id]```\n\n**Remove multiple tasks**\n\n```pueue remove [id1] [id2]```\n\n**Force remove running task**\n\n```pueue kill [task_id] && pueue remove [task_id]```"],["SYNOPSIS","**pueue remove** [_options_] _task_ids_"],["PARAMETERS","_TASK_IDS_\n> Tasks to remove."],["DESCRIPTION","**pueue remove** deletes specific tasks from the pueue queue by their task IDs. Only queued, stashed, or finished tasks can be removed; running tasks must be killed first with **pueue kill** before removal. Use **pueue clean** to bulk-remove all finished tasks instead."],["CAVEATS","Running tasks cannot be removed directly; use **pueue kill** first. Removing a task deletes its log output permanently."],["INSTALL","```pacman: sudo pacman -S pueue```\n\n```apk: sudo apk add pueue```\n\n```zypper: sudo zypper install pueue```\n\n```brew: brew install pueue```\n\n```nix: nix profile install nixpkgs#pueue```"],["SEE ALSO","[pueue](/man/pueue)(1), [pueue-clean](/man/pueue-clean)(1), [pueue-kill](/man/pueue-kill)(1)"]],"e":["pueue","pueue-clean","pueue-kill"],"x":"pueue remove [task_id]"},"quotacheck":{"n":"quotacheck","t":"Scan filesystems and build quota files","s":[["TAGLINE","Scan filesystems and build quota files"],["TLDR","Check quotas on **all** filesystems\n\n```sudo quotacheck --all```\n\n**Force** check (may damage quota files)\n\n```sudo quotacheck --force [mountpoint]```\n\nCheck in **debug** mode\n\n```sudo quotacheck --debug [mountpoint]```\n\nCheck with **verbose** output\n\n```sudo quotacheck --verbose [mountpoint]```\n\nCheck **user** quotas\n\n```sudo quotacheck --user [user] [mountpoint]```\n\nCheck **group** quotas\n\n```sudo quotacheck --group [group] [mountpoint]```"],["SYNOPSIS","**quotacheck** [**-a**] [**-f**] [**-v**] [**-u** _user_] [**-g** _group_] [_mountpoint_]"],["PARAMETERS","**-a, --all**\n> Check all mounted non-NFS filesystems\n\n**-f, --force**\n> Force check even with quotas enabled\n\n**-v, --verbose**\n> Display progress information\n\n**-d, --debug**\n> Enable debug mode\n\n**-u, --user _user_**\n> Check user quotas only\n\n**-g, --group _group_**\n> Check group quotas only\n\n**-c**\n> Create quota files if missing"],["DESCRIPTION","**quotacheck** scans filesystems for disk usage and creates or verifies quota files. It examines each file to determine how much space each user and group consumes.\n\nRunning quotacheck with quotas enabled can corrupt quota files, so quotas should typically be turned off first. The tool creates aquota.user and aquota.group files."],["CAVEATS","Best run with quotas turned off. Force flag can cause data loss. May take long time on large filesystems. Requires root privileges."],["HISTORY","**quotacheck** is part of the Linux disk quota system, derived from BSD Unix quotas. It's essential for initializing and maintaining disk quota enforcement."],["INSTALL","```apt: sudo apt install quota```\n\n```dnf: sudo dnf install quota```\n\n```pacman: sudo pacman -S quota-tools```\n\n```apk: sudo apk add quota-tools```\n\n```zypper: sudo zypper install quota```\n\n```nix: nix profile install nixpkgs#quota```"],["SEE ALSO","[quota](/man/quota)(1), [quotaon](/man/quotaon)(8), [edquota](/man/edquota)(8), [repquota](/man/repquota)(8)"]],"e":["quota","quotaon","edquota","repquota"],"x":"sudo quotacheck --all"},"redis-cli":{"n":"redis-cli","t":"Command-line interface for Redis servers","s":[["TAGLINE","Command-line interface for Redis servers"],["TLDR","**Connect to local Redis server**\n\n```redis-cli```\n\n**Connect to a remote Redis server**\n\n```redis-cli -h [hostname] -p [port]```\n\n**Connect with password authentication**\n\n```redis-cli -a [password]```\n\n**Connect with TLS** and authentication\n\n```redis-cli --tls -h [hostname] -p [port] -a [password]```\n\n**Execute a single command**\n\n```redis-cli [command] [arguments]```\n\n**Get a key value**\n\n```redis-cli GET [key]```\n\n**Set a key value**\n\n```redis-cli SET [key] \"[value]\"```\n\n**List all keys** matching a pattern\n\n```redis-cli KEYS \"*\"```\n\n**Monitor all commands** in real-time\n\n```redis-cli MONITOR```\n\n**Get server statistics**\n\n```redis-cli INFO```"],["SYNOPSIS","**redis-cli** [**-h** _host_] [**-p** _port_] [**-a** _password_] [**-n** _db_] [_command_ [_args_...]]"],["PARAMETERS","**-h** _host_\n> Server hostname (default: 127.0.0.1)\n\n**-p** _port_\n> Server port (default: 6379)\n\n**-a** _password_\n> Password for AUTH command\n\n**-n** _db_\n> Database number to select\n\n**-u** _uri_\n> Connect using Redis URI (redis://...)\n\n**-r** _count_\n> Repeat command count times\n\n**-i** _interval_\n> Interval between repeats (seconds)\n\n**--scan**\n> List keys using SCAN instead of KEYS\n\n**--pipe**\n> Transfer raw Redis protocol from stdin\n\n**--rdb** _file_\n> Transfer RDB dump from server to file\n\n**-c**\n> Enable cluster mode (follow -ASK and -MOVED redirections)\n\n**-s** _socket_\n> Server socket path (overrides hostname and port)\n\n**--tls**\n> Establish a secure TLS connection\n\n**--user** _username_\n> ACL username for AUTH\n\n**--pass** _password_\n> Alias for **-a**\n\n**--csv**\n> Output in CSV format\n\n**--bigkeys**\n> Sample keys looking for keys with many elements\n\n**--memkeys**\n> Sample keys looking for keys consuming the most memory\n\n**--cluster** _command_\n> Execute cluster commands\n\n**--latency**\n> Monitor latency continuously\n\n**--stat**\n> Print rolling stats"],["DESCRIPTION","**redis-cli** is the command-line interface for Redis, an in-memory data structure store used as database, cache, and message broker. It provides interactive and scripted access to Redis servers.\n\nIn interactive mode (no command specified), redis-cli presents a prompt for entering commands. Commands follow Redis protocol: command name followed by arguments, with responses displayed immediately.\n\nCommon operations include key-value storage (GET, SET, DEL), lists (LPUSH, RPOP), sets (SADD, SMEMBERS), hashes (HSET, HGET), and pub/sub messaging (PUBLISH, SUBSCRIBE).\n\nThe tool supports cluster mode, sentinel connections, and various debugging and monitoring features for Redis administration."],["COMMON COMMANDS","**SET** key value: Store a value\n**GET** key: Retrieve a value\n**DEL** key: Delete a key\n**KEYS** pattern: Find keys matching pattern\n**EXPIRE** key seconds: Set key expiration\n**TTL** key: Get remaining time to live\n**FLUSHDB**: Delete all keys in current database\n**INFO**: Server information and statistics\n**PING**: Test connection"],["CAVEATS","Using **KEYS \\*** on production servers with large datasets can block the server. Use **SCAN** instead for iterating keys in production.\n\nPasswords passed with **-a** appear in process listings and shell history. Use **REDISCLI_AUTH** environment variable or interactive AUTH for sensitive environments.\n\nRedis databases are numbered 0-15 by default. Use **-n** to select a database or **SELECT** command interactively."],["INSTALL","```apt: sudo apt install redis-tools```\n\n```apk: sudo apk add valkey-compat```"],["SEE ALSO","[redis-server](/man/redis-server)(1), [redis-benchmark](/man/redis-benchmark)(1), [memcached](/man/memcached)(1)"]],"e":["redis-server","redis-benchmark","memcached"],"x":"redis-cli"},"renux":{"n":"renux","t":"Terminal-based bulk file renamer with TUI","s":[["TAGLINE","Terminal-based bulk file renamer with TUI"],["TLDR","**Launch the file renamer TUI**\n\n```renux```"],["SYNOPSIS","**renux** [_options_]"],["DESCRIPTION","**renux** is a terminal-based bulk file renamer that provides a TUI for renaming multiple files at once. It simplifies batch file renaming operations with an interactive interface."],["HISTORY","**renux** was created by **Andrian Lloyd Maagma** (andrianllmm) and is written in **Python**."],["SEE ALSO","[rename](/man/rename)(1), [mmv](/man/mmv)(1), [nomino](/man/nomino)(1)"]],"e":["rename","mmv","nomino"],"x":"renux"},"robots":{"n":"robots","t":"fight off villainous robots in a grid-based chase game","s":[["TAGLINE","fight off villainous robots in a grid-based chase game"],["TLDR","**Start a game**\n\n```robots```\n\n**Start without scores display**\n\n```robots -s```\n\n**Start at a specific level**\n\n```robots -j```"],["SYNOPSIS","**robots** [_options_]"],["PARAMETERS","**-s**\n> Don't display scores on exit.\n\n**-j**\n> Jump to highest score level.\n\n**-t**\n> Play on a small terminal."],["PREVIEW","```\n . . . . . . . . .\n . + . . . . + . .\n . . . . @ . . . .\n . . + . . . . + .\n . . . . . . . . .\n```"],["DESCRIPTION","**robots** is a grid-based chase game from the **bsdgames** collection. The player must avoid robots that relentlessly chase them across the grid. Robots that collide with each other are destroyed, leaving scrap heaps that also destroy other robots on contact.\n\nMovement uses hjklyubn keys. Press **t** to teleport to a random location, **w** to wait for robots to come to you, or **s** for a safe teleport (limited uses). Strategy involves luring robots into each other and into scrap piles."],["CAVEATS","Part of the bsdgames package. Terminal-based. The number of robots increases with each level."],["HISTORY","**robots** is based on the classic **1984** game **Chase**, which was inspired by the Daleks from Doctor Who. It has been part of BSD games collections since the **1980s**."],["INSTALL","```dnf: sudo dnf install bsd-games```\n\n```pacman: sudo pacman -S bsd-games```\n\n```apk: sudo apk add bsd-games```\n\n```zypper: sudo zypper install bsd-games```"],["SEE ALSO","[worm](/man/worm)(6), [greed](/man/greed)(6), [rogue](/man/rogue)(6)"]],"e":["worm","greed","rogue"],"x":"robots"},"rtl_fm":{"n":"rtl_fm","t":"Receive FM radio with RTL-SDR USB dongle","s":[["TAGLINE","Receive FM radio with RTL-SDR USB dongle"],["TLDR","**Listen to FM radio**\n\n```rtl_fm -f [100.1M] -M wbfm -s 200000 -r 48000 | aplay -f S16_LE -r 48000```\n\n**Narrow FM (ham radio)**\n\n```rtl_fm -f [145.5M] -M fm -s 12000 | aplay -f S16_LE -r 12000```\n\n**AM mode**\n\n```rtl_fm -f [1000k] -M am -s 12000 | aplay -f S16_LE -r 12000```\n\n**Save to file**\n\n```rtl_fm -f [100.1M] -M wbfm -s 200000 > [radio.raw]```"],["SYNOPSIS","**rtl_fm** [_options_]"],["PARAMETERS","**-f** _freq_\n> Center frequency.\n\n**-M** _mode_\n> Demodulation (fm, wbfm, am, usb, lsb).\n\n**-s** _rate_\n> Sample rate.\n\n**-r** _rate_\n> Output sample rate.\n\n**-g** _gain_\n> Gain (0=auto).\n\n**-d** _index_\n> Device index.\n\n**-p** _ppm_\n> PPM frequency error correction for the dongle's crystal oscillator.\n\n**-l** _level_\n> Squelch level; mute output when signal strength is below this threshold.\n\n**-A** _method_\n> Output audio filtering: **fast** (default) or **lut** (lookup table, slower but slightly better quality).\n\n**-E** _option_\n> Enable extra features: **edge** (edge tuning), **dc** (DC blocking filter), **deemp** (FM de-emphasis filter), **direct** (direct sampling mode), **offset** (offset tuning mode)."],["DESCRIPTION","**rtl_fm** is an FM demodulator for RTL-SDR dongles. It receives and demodulates radio signals using inexpensive DVB-T USB receivers (based on the RTL2832U chipset) as software-defined radios. Output is raw 16-bit signed PCM audio on stdout, typically piped to an audio player or file."],["EXAMPLES","```bash"],["FM broadcast","rtl_fm -f 100.1M -M wbfm -s 200000 -r 48000 | aplay -r 48000 -f S16_LE"],["With squelch","rtl_fm -f 145.5M -M fm -s 12000 -l 50 | aplay -r 12000 -f S16_LE"],["Scan frequencies","rtl_fm -f 145.0M:148.0M:25k -M fm -s 12000 | aplay -r 12000 -f S16_LE"],["Record to WAV","rtl_fm -f 100.1M -M wbfm -s 200000 | sox -r 200000 -e s -b 16 -c 1 -t raw - radio.wav rate 48000\n```"],["MODES","```\nwbfm - Wideband FM (broadcast)\nfm   - Narrow FM\nam   - Amplitude modulation\nusb  - Upper sideband\nlsb  - Lower sideband\n```"],["CAVEATS","Requires RTL-SDR dongle. May need PPM correction. Audio player needed for output."],["HISTORY","rtl_fm is part of **rtl-sdr** tools by **osmocom**, enabling software-defined radio with RTL2832U dongles."],["INSTALL","```apt: sudo apt install rtl-sdr```\n\n```dnf: sudo dnf install rtl-sdr```\n\n```pacman: sudo pacman -S rtl-sdr```\n\n```apk: sudo apk add rtl-sdr```\n\n```zypper: sudo zypper install rtl-sdr```\n\n```brew: brew install rtl-sdr```\n\n```nix: nix profile install nixpkgs#rtl-sdr```"],["SEE ALSO","[rtl_sdr](/man/rtl_sdr)(1), [rtl_power](/man/rtl_power)(1), [gqrx](/man/gqrx)(1)"]],"e":["rtl_sdr","rtl_power","gqrx"],"x":"rtl_fm -f [100.1M] -M wbfm -s 200000 -r 48000 | aplay -f S16_LE -r 48000"},"sfdk-make-install":{"n":"sfdk-make-install","t":"Install built files in Sailfish OS build target","s":[["TAGLINE","Install built files in Sailfish OS build target"],["TLDR","**Install** the previously built project into the build target sysroot\n\n```sfdk make-install```\n\n**Install to a custom destination** directory\n\n```sfdk make-install DESTDIR=[path]```\n\n**Install in parallel** using N jobs\n\n```sfdk make-install -j[N]```\n\n**Install verbosely**\n\n```sfdk make-install V=1```"],["SYNOPSIS","**sfdk make-install** [_make_arguments_...]"],["DESCRIPTION","**sfdk make-install** runs the `install` target of the underlying build system (typically **make**, **cmake**, or **qmake**-generated Makefiles) inside the Sailfish OS build target. It is a thin wrapper around **sfdk make** that selects the install target and forwards any additional arguments to the build tool.\n\nThe command is normally executed after **sfdk build** or **sfdk make** as part of the development workflow when staging files for inclusion in an RPM package, or when deploying built artifacts to the build target sysroot for testing."],["PARAMETERS","**DESTDIR=**_path_\n> Stage installation into _path_ rather than the default sysroot prefix. Standard make/autotools convention.\n\n**-j**_N_\n> Run up to _N_ install jobs in parallel.\n\n**V=1**\n> Enable verbose build output."],["CAVEATS","Requires an initialized Sailfish SDK build engine and an active build target configured via **sfdk config**. The project must already be built; **sfdk make-install** does not trigger compilation. Behavior depends on the project's own install rules."],["SEE ALSO","[sfdk](/man/sfdk)(1), [sfdk-make](/man/sfdk-make)(1)"]],"e":["sfdk","sfdk-make"],"x":"sfdk make-install"},"smalltalkci":{"n":"smalltalkci","t":"Continuous integration for Smalltalk projects","s":[["TAGLINE","Continuous integration for Smalltalk projects"],["TLDR","**Run tests**\n\n```smalltalkci```\n\n**Run with config**\n\n```smalltalkci [.smalltalk.ston]```\n\n**Specify Smalltalk**\n\n```smalltalkci -s [Squeak64-5.3]```\n\n**Headless mode**\n\n```smalltalkci --headless```\n\n**Debug mode**\n\n```smalltalkci -d```"],["SYNOPSIS","**smalltalkci** [_-s smalltalk_] [_-d_] [_options_] [_config_]"],["PARAMETERS","**-s** _SMALLTALK_\n> Smalltalk image version to use (e.g., Squeak64-5.3, Pharo-10.0).\n\n**-d**\n> Debug mode for additional output when troubleshooting.\n\n**--headless**\n> Run the Smalltalk VM without graphical display (default for CI).\n\n**--image** _FILE_\n> Use a specific Smalltalk image file.\n\n**--baseline** _NAME_\n> Define the Metacello baseline to load or test.\n\n**--dir** _PATH_\n> Set the working directory for the project. Defaults to the current directory.\n\n**--cache-dir** _PATH_\n> Directory where Smalltalk images and VMs are cached.\n\n**--github-token** _TOKEN_\n> GitHub token for accessing private repositories or increasing API rate limits.\n\n**--no-tracking**\n> Opt out of anonymous build metrics collection.\n\n**--help**\n> Show help."],["DESCRIPTION","**smalltalkci** provides continuous integration testing for Smalltalk projects across multiple dialects including Squeak, Pharo, GemStone/S, and Moose. It automates the process of downloading the appropriate Smalltalk virtual machine, loading project code, running tests, and reporting results.\n\nConfiguration is defined in a **.smalltalk.ston** file using STON (Smalltalk Object Notation) format, specifying which Smalltalk platform to use, packages to load, and test classes to run. The tool integrates with GitHub Actions, Travis CI, and other CI/CD platforms.\n\nTests run in headless mode by default, requiring no graphical display. Debug mode provides additional output for troubleshooting failed builds, and a custom Smalltalk image can be specified for projects with specific environment requirements."],["CAVEATS","Smalltalk-specific. Configuration via .smalltalk.ston required. VM download may be slow on first run as images are cached."],["SEE ALSO","[pharo](/man/pharo)(1), [squeak](/man/squeak)(1)"]],"e":["pharo","squeak"],"x":"smalltalkci"},"systemctl-poweroff":{"n":"systemctl-poweroff","t":"Shut down and power off system","s":[["TAGLINE","Shut down and power off system"],["TLDR","**Power off** the system\n\n```systemctl poweroff```\n\n**Force** immediate poweroff\n\n```systemctl poweroff -f```\n\nPoweroff without **wall** message\n\n```systemctl poweroff -f --no-wall```"],["SYNOPSIS","**systemctl poweroff** [_OPTIONS_]"],["PARAMETERS","**-f, --force**\n> Skip graceful service shutdown\n\n**--force --force** (-ff)\n> Immediate poweroff without cleanup (dangerous)\n\n**--no-wall**\n> Don't send wall message to logged-in users\n\n**--when** _TIME_\n> Schedule poweroff at specific time"],["DESCRIPTION","**systemctl poweroff** shuts down the system and powers off the machine. It gracefully stops all services, unmounts filesystems, and then signals the hardware to power down.\n\nWith `--force`, services are not gracefully stopped. With double `--force`, filesystems aren't unmounted, risking data loss."],["CAVEATS","Requires appropriate privileges. Force mode can cause data loss if filesystems aren't properly synced. Some hardware may not support software poweroff and will halt instead."],["HISTORY","The **poweroff** subcommand provides systemd-native shutdown, integrating with the scheduled shutdown mechanism and wall notification system."],["SEE ALSO","[systemctl-halt](/man/systemctl-halt)(1), [systemctl-reboot](/man/systemctl-reboot)(1), [poweroff](/man/poweroff)(8)"]],"e":["systemctl-halt","systemctl-reboot","poweroff"],"x":"systemctl poweroff"},"tarsnap":{"n":"tarsnap","t":"Encrypted deduplicated online backup","s":[["TAGLINE","Encrypted deduplicated online backup"],["TLDR","**Create backup**\n\n```tarsnap -c -f [backup-name] [/path/to/backup]```\n\n**List archives**\n\n```tarsnap --list-archives```\n\n**List contents of archive**\n\n```tarsnap -t -f [backup-name]```\n\n**Extract archive**\n\n```tarsnap -x -f [backup-name]```\n\n**Delete archive**\n\n```tarsnap -d -f [backup-name]```\n\n**Show statistics**\n\n```tarsnap --print-stats```\n\n**Dry run** (show what would be backed up)\n\n```tarsnap -c --dry-run -f [test] [/path/to/backup]```"],["SYNOPSIS","**tarsnap** [_-c_] [_-x_] [_-t_] [_-d_] [_-f archive_] [_options_] [_paths_]"],["PARAMETERS","**-c**\n> Create archive.\n\n**-x**\n> Extract archive.\n\n**-t**\n> List archive contents.\n\n**-d**\n> Delete archive.\n\n**-f** _ARCHIVE_\n> Archive name.\n\n**--list-archives**\n> List all archives.\n\n**--print-stats**\n> Show usage statistics.\n\n**--dry-run**\n> Simulation mode.\n\n**-v**\n> Verbose.\n\n**--keyfile** _FILE_\n> Key file location.\n\n**--cachedir** _DIR_\n> Cache directory.\n\n**-C** _DIR_\n> Change to directory.\n\n**--include** _PATTERN_\n> Include files matching.\n\n**--exclude** _PATTERN_\n> Exclude files matching."],["DESCRIPTION","**tarsnap** provides secure, deduplicated online backup. Data is encrypted before leaving your computer and stored in the cloud.\n\nDeduplication stores identical blocks only once. Incremental backups are efficient, transferring only new or changed data.\n\nClient-side encryption ensures only you can read your data. Keys are generated locally and never shared with the server.\n\nArchive names are arbitrary strings. Common patterns include hostnames, dates, or descriptive names.\n\nStatistics show storage usage and deduplication efficiency. Billing is based on stored and transferred bytes.\n\nThe tool is command-line only, designed for scripting and automation. Cron jobs commonly run scheduled backups."],["CAVEATS","Requires paid account. Key loss means data loss - no recovery possible. Initial backup can take time. Priced per byte."],["HISTORY","**Tarsnap** was created by **Colin Percival** around **2008**. A former FreeBSD security officer, Percival designed Tarsnap with security as the primary goal. The service emphasizes cryptographic correctness."],["INSTALL","```pacman: sudo pacman -S tarsnap```\n\n```brew: brew install tarsnap```\n\n```nix: nix profile install nixpkgs#tarsnap```"],["SEE ALSO","[tar](/man/tar)(1), [restic](/man/restic)(1), [borg](/man/borg)(1), [duplicity](/man/duplicity)(1)"]],"e":["tar","restic","borg","duplicity"],"x":"tarsnap -c -f [backup-name] [/path/to/backup]"},"termpicker":{"n":"termpicker","t":"Color picker for the terminal","s":[["TAGLINE","Color picker for the terminal"],["TLDR","**Launch the interactive color picker**\n\n```termpicker```"],["SYNOPSIS","**termpicker** [_options_]"],["DESCRIPTION","**termpicker** is a simple color picker designed for truecolor terminals. It lets you create colors using sliders for RGB, HSL, and CMYK, seamlessly convert between color formats, and copy the result to your clipboard in formats including RGB, HEX, HSL, CMYK, and ANSI truecolor.\n\nKeybindings are shown in the UI and their descriptions can be expanded by pressing the **?** key."],["CAVEATS","Requires a terminal with truecolor support. Clipboard copying depends on system clipboard integration."],["HISTORY","**termpicker** was created by **Benjamin Chausse** (ChausseBenjamin) and is written in **Go**."],["SEE ALSO","[pastel](/man/pastel)(1), [sttr](/man/sttr)(1)"]],"e":["pastel","sttr"],"x":"termpicker"},"tlmgr-option":{"n":"tlmgr-option","t":"Configure TeX Live installation options","s":[["TAGLINE","Configure TeX Live installation options"],["TLDR","**Show all options**\n\n```tlmgr option```\n\n**Show specific option**\n\n```tlmgr option [optionname]```\n\n**Set option value**\n\n```tlmgr option [optionname] [value]```\n\n**Show available options**\n\n```tlmgr option showall```"],["SYNOPSIS","**tlmgr option** [_option_ [_value_]]"],["PARAMETERS","**repository**\n> Package repository URL.\n\n**autobackup**\n> Number of backups to keep.\n\n**backupdir**\n> Backup directory.\n\n**docfiles**\n> Install documentation (0/1).\n\n**srcfiles**\n> Install source files (0/1)."],["DESCRIPTION","**tlmgr option** manages TeX Live installation options. Configure repository, backup settings, and what to install. Part of TeX Live Manager."],["SEE ALSO","[tlmgr-conf](/man/tlmgr-conf)(1), [tlmgr](/man/tlmgr)(1)"]],"e":["tlmgr-conf","tlmgr"],"x":"tlmgr option"},"tlmgr-remove":{"n":"tlmgr-remove","t":"Uninstall TeX Live packages","s":[["TAGLINE","Uninstall TeX Live packages"],["TLDR","**Remove package**\n\n```tlmgr remove [package]```\n\n**Remove multiple packages**\n\n```tlmgr remove [package1] [package2]```\n\n**Force removal**\n\n```tlmgr remove --force [package]```\n\n**Dry run**\n\n```tlmgr remove --dry-run [package]```"],["SYNOPSIS","**tlmgr remove** [_options_] _package_..."],["PARAMETERS","**--force**\n> Force removal even if other packages depend on it.\n\n**--dry-run**\n> Show what would be removed.\n\n**--no-depends**\n> Don't remove dependencies.\n\n**--no-depends-at-all**\n> Even more aggressive: do not check for and remove dependent packages even from the same collection."],["DESCRIPTION","**tlmgr remove** uninstalls TeX Live packages, collections, or schemes. By default it prevents removal of packages required by other installed components, ensuring the integrity of the TeX Live installation, unless overridden with `--force`. When TeX Live is installed system-wide, root privileges are required."],["SEE ALSO","[tlmgr-install](/man/tlmgr-install)(1), [tlmgr-update](/man/tlmgr-update)(1), [tlmgr](/man/tlmgr)(1)"]],"e":["tlmgr-install","tlmgr-update","tlmgr"],"x":"tlmgr remove [package]"},"togglesebool":{"n":"togglesebool","t":"Toggle SELinux boolean values","s":[["TAGLINE","Toggle SELinux boolean values"],["TLDR","**Toggle** a SELinux boolean\n\n```sudo togglesebool virt_use_samba```\n\nToggle **multiple** booleans\n\n```sudo togglesebool httpd_enable_homedirs ftpd_full_access```"],["SYNOPSIS","**togglesebool** _boolean_..."],["DESCRIPTION","**togglesebool** flips the current (non-persistent) values of SELinux booleans. If a boolean is currently on, it will be turned off, and vice versa. Changes do not persist across reboots."],["PARAMETERS","**boolean**\n> One or more SELinux boolean names to toggle"],["CAVEATS","This tool has been deprecated and is often removed in favor of setsebool. Changes made with togglesebool are non-persistent and will be lost after reboot. Use setsebool -P for persistent changes."],["HISTORY","**togglesebool** was part of the early SELinux tools for managing boolean values. It has been superseded by **setsebool** which provides more control over persistence."],["INSTALL","```apt: sudo apt install selinux-utils```\n\n```dnf: sudo dnf install libselinux-utils```\n\n```apk: sudo apk add libselinux-utils```"],["SEE ALSO","[getsebool](/man/getsebool)(8), [setsebool](/man/setsebool)(8), [semanage-boolean](/man/semanage-boolean)(8)"]],"e":["getsebool","setsebool","semanage-boolean"],"x":"sudo togglesebool virt_use_samba"},"unoconv":{"n":"unoconv","t":"Convert documents between formats via LibreOffice","s":[["TAGLINE","Convert documents between formats via LibreOffice"],["TLDR","**Convert document to PDF**\n\n```unoconv -f pdf [document.docx]```\n\n**Convert to specific format**\n\n```unoconv -f [odt] [document.docx]```\n\n**Convert multiple files**\n\n```unoconv -f pdf [*.docx]```\n\n**Output to specific directory**\n\n```unoconv -o [/output/dir] -f pdf [document.docx]```\n\n**Start listener daemon**\n\n```unoconv --listener &```\n\n**List available output formats**\n\n```unoconv --show```\n\n**Convert with export options**\n\n```unoconv -f pdf -e PageRange=[1-5] [document.docx]```\n\n**Read from stdin, write to stdout**\n\n```cat [doc.docx] | unoconv --stdin --stdout -f pdf > [output.pdf]```"],["SYNOPSIS","**unoconv** [_options_] _file_...\n\n**unoconv** **--listener** [_options_]"],["PARAMETERS","**-f**, **--format** _format_\n> Output format (default: pdf).\n\n**-o**, **--output** _path_\n> Output directory or filename.\n\n**-e**, **--export** _option_\n> Export filter options (e.g., PageRange=1-5).\n\n**-i**, **--import** _option_\n> Import filter options.\n\n**-t**, **--template** _file_\n> Template for importing styles.\n\n**-l**, **--listener**\n> Start as listener daemon.\n\n**-p**, **--port** _port_\n> Port for listener (default: 2002).\n\n**--server** _address_\n> Server address for listener.\n\n**-n**, **--no-launch**\n> Don't start own listener if none found.\n\n**--stdin**\n> Read input from stdin.\n\n**--stdout**\n> Write output to stdout.\n\n**--show**\n> List available output formats.\n\n**-T**, **--timeout** _seconds_\n> Connection timeout.\n\n**-v**, **--verbose**\n> Verbose output."],["DESCRIPTION","**unoconv** is a command-line document converter using LibreOffice/OpenOffice. It can convert between any formats supported by LibreOffice, including DOC, DOCX, ODT, PDF, HTML, TXT, and many more.\n\nThe tool communicates with a LibreOffice instance via UNO bindings. If no listener is running, unoconv starts a temporary instance automatically. For batch processing, start a persistent listener with --listener.\n\nExport options allow fine-tuning of output, such as page ranges for PDF or image quality settings. Templates can apply consistent styling across conversions.\n\nNote: unoconv is deprecated in favor of unoserver for new implementations."],["CAVEATS","Requires LibreOffice installation. First conversion may be slow while LibreOffice starts. Some format combinations may have limited fidelity. Consider unoserver for new projects."],["HISTORY","**unoconv** was created by Dag Wieers to provide command-line access to LibreOffice's conversion capabilities. It became widely used for automated document processing. The project is now in maintenance mode with unoserver as its successor."],["INSTALL","```dnf: sudo dnf install unoconv```\n\n```pacman: sudo pacman -S unoconv```\n\n```zypper: sudo zypper install unoconv```\n\n```brew: brew install unoconv```\n\n```nix: nix profile install nixpkgs#unoconv```"],["SEE ALSO","[libreoffice](/man/libreoffice)(1), [pandoc](/man/pandoc)(1), [soffice](/man/soffice)(1)"]],"e":["libreoffice","pandoc","soffice"],"x":"unoconv -f pdf [document.docx]"},"vcluster":{"n":"vcluster","t":"Create virtual Kubernetes clusters","s":[["TAGLINE","Create virtual Kubernetes clusters"],["TLDR","**Create cluster**\n\n```vcluster create [name]```\n\n**Connect to cluster**\n\n```vcluster connect [name]```\n\n**List clusters**\n\n```vcluster list```\n\n**Delete cluster**\n\n```vcluster delete [name]```\n\n**Disconnect**\n\n```vcluster disconnect```\n\n**Pause cluster**\n\n```vcluster pause [name]```"],["SYNOPSIS","**vcluster** _command_ [_options_] [_name_]"],["PARAMETERS","**create**\n> Create virtual cluster.\n\n**connect**\n> Connect kubectl.\n\n**list**\n> List clusters.\n\n**delete**\n> Remove cluster.\n\n**disconnect**\n> Disconnect kubectl.\n\n**--namespace** _NS_\n> Kubernetes namespace."],["DESCRIPTION","**vcluster** creates lightweight virtual Kubernetes clusters that run inside existing Kubernetes clusters. Each virtual cluster has its own API server and control plane but shares the underlying host cluster's compute resources and networking.\n\nThis approach provides full Kubernetes isolation for multi-tenancy, development, and testing without the overhead of separate physical clusters. Virtual clusters are created in seconds and can be paused to free resources when not in use."],["CAVEATS","Kubernetes required. Host cluster needed. Loft Labs product."],["HISTORY","**vcluster** was created by **Loft Labs** for running lightweight virtual Kubernetes clusters inside existing clusters."],["INSTALL","```brew: brew install vcluster```\n\n```nix: nix profile install nixpkgs#vcluster```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [k3s](/man/k3s)(1), [kind](/man/kind)(1)"]],"e":["kubectl","k3s","kind"],"x":"vcluster create [name]"},"vue":{"n":"vue","t":"Vue.js project management CLI","s":[["TAGLINE","Vue.js project management CLI"],["TLDR","**Create new project**\n\n```vue create [project-name]```\n\n**Create with preset**\n\n```vue create -p [preset] [project-name]```\n\n**Start GUI**\n\n```vue ui```\n\n**Add plugin**\n\n```vue add [plugin]```\n\n**Invoke generator**\n\n```vue invoke [plugin]```\n\n**Inspect webpack config**\n\n```vue inspect```\n\n**Serve single file**\n\n```vue serve [App.vue]```\n\n**Build single file**\n\n```vue build [App.vue]```"],["SYNOPSIS","**vue** _command_ [_options_] [_args_]"],["PARAMETERS","**create** _NAME_\n> Create project.\n\n**ui**\n> Open GUI.\n\n**add** _PLUGIN_\n> Add plugin.\n\n**invoke** _PLUGIN_\n> Run generator.\n\n**inspect**\n> Show webpack config.\n\n**serve** _FILE_\n> Dev server.\n\n**build** _FILE_\n> Build file.\n\n**-p**, **--preset** _NAME_\n> Project preset.\n\n**-d**, **--default**\n> Use default preset."],["DESCRIPTION","**vue** is the command-line interface for Vue CLI, providing tools for creating, configuring, and managing Vue.js application projects. It scaffolds new projects with interactive prompts or saved presets, setting up build tooling, linting, testing, and other development infrastructure automatically.\n\nThe CLI includes a graphical user interface accessible via `vue ui` for visually managing projects, plugins, dependencies, and build tasks in a browser. Plugins can be added to existing projects to integrate features like Vue Router, Vuex state management, TypeScript support, and testing frameworks.\n\nFor rapid prototyping, `vue serve` and `vue build` can compile individual Vue single-file components without requiring a full project setup. The `vue inspect` command displays the resolved webpack configuration, which is helpful for debugging build issues."],["CAVEATS","Vue CLI is in maintenance mode for Vue 2/3 projects. For new Vue 3 projects, **create-vue** (backed by Vite) is the recommended scaffolding tool. Requires Node.js 8.9 or higher (Node.js 10+ recommended)."],["HISTORY","**Vue CLI** was created by **Evan You** for Vue.js project management. Version 3+ provides plugin-based architecture."],["INSTALL","```nix: nix profile install nixpkgs#vue```"],["SEE ALSO","[npm](/man/npm)(1), [vite](/man/vite)(1), [node](/man/node)(1), [vue-serve](/man/vue-serve)(1), [yarn](/man/yarn)(1)"]],"e":["npm","vite","node","vue-serve","yarn"],"x":"vue create [project-name]"},"xbps-install":{"n":"xbps-install","t":"Install and update Void Linux packages","s":[["TAGLINE","Install and update Void Linux packages"],["TLDR","**Install** a new package\n\n```xbps-install [package]```\n\n**Synchronize** and update all packages\n\n```xbps-install -S -u```"],["SYNOPSIS","**xbps-install** [_options_] [_package_...]"],["PARAMETERS","**-S, --sync**\n> Synchronize repository index\n\n**-u, --update**\n> Update installed packages\n\n**-y, --yes**\n> Assume yes to all questions\n\n**-n, --dry-run**\n> Show what would be done\n\n**-f, --force**\n> Force installation\n\n**-R, --repository _url_**\n> Add repository to search\n\n**-D, --download-only**\n> Download packages only\n\n**-v, --verbose**\n> Verbose output"],["DESCRIPTION","**xbps-install** is the package installation and update utility for XBPS, the package manager used by Void Linux. It handles package installation, dependency resolution, and system updates.\n\nBefore installing or updating packages, synchronize the repository index with **-S** to get the latest package information."],["CAVEATS","Requires root privileges for system-wide installation. Repository sync (**-S**) should be run before updates. Part of the XBPS package management system for Void Linux."],["INSTALL","```apk: sudo apk add xbps```\n\n```nix: nix profile install nixpkgs#xbps```"],["SEE ALSO","[xbps-query](/man/xbps-query)(1), [xbps-remove](/man/xbps-remove)(1), [xbps-reconfigure](/man/xbps-reconfigure)(1)"]],"e":["xbps-query","xbps-remove","xbps-reconfigure"],"x":"xbps-install [package]"},"zsteg":{"n":"zsteg","t":"Detect steganography in PNG and BMP images","s":[["TAGLINE","Detect steganography in PNG and BMP images"],["TLDR","**Analyze image**\n\n```zsteg [image.png]```\n\n**Check all channels**\n\n```zsteg -a [image.png]```\n\n**Extract specific data**\n\n```zsteg -E \"[b1,rgb,lsb]\" [image.png]```\n\n**Verbose output**\n\n```zsteg -v [image.png]```\n\n**Check BMP file**\n\n```zsteg [image.bmp]```\n\n**Limit output**\n\n```zsteg --limit [1000] [image.png]```"],["SYNOPSIS","**zsteg** [_-a_] [_-v_] [_-E extract_] [_options_] _image_"],["PARAMETERS","**-a**, **--all**\n> Check all methods.\n\n**-v**, **--verbose**\n> Verbose output.\n\n**-E** _SPEC_\n> Extract data.\n\n**--limit** _N_\n> Output limit.\n\n**-o** _FILE_\n> Output file.\n\n**--bits** _BITS_\n> Bits to check.\n\n**--order** _ORDER_\n> Bit order."],["DESCRIPTION","**zsteg** detects and extracts data hidden through steganography in PNG and BMP image files. It analyzes multiple color channels (RGB, alpha, and individual colors) across various bit depths to identify concealed content such as text, embedded files, and binary patterns.\n\nThe tool's primary focus is LSB (Least Significant Bit) steganography, where data is hidden in the least significant bits of pixel color values. The **-a** flag performs a comprehensive check across all common methods and channel combinations, while the **-E** flag extracts data from a specific channel and bit configuration.\n\nzsteg is widely used in CTF (Capture The Flag) competitions for solving steganography challenges and in forensic analysis of suspicious image files. It is written in Ruby."],["CAVEATS","PNG and BMP only. False positives possible. Requires Ruby."],["HISTORY","**zsteg** was created for CTF competitions and steganography analysis. It provides quick detection of hidden image data."],["INSTALL","```nix: nix profile install nixpkgs#zsteg```"],["SEE ALSO","[steghide](/man/steghide)(1), [binwalk](/man/binwalk)(1), [exiftool](/man/exiftool)(1)"]],"e":["steghide","binwalk","exiftool"],"x":"zsteg [image.png]"}});
