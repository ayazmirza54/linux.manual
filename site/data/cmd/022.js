window.LCL_BUCKET(22,{"apm":{"n":"apm","t":"Query Advanced Power Management status and battery info.","s":[["TAGLINE","Query Advanced Power Management status and battery info."],["TLDR","**Show** APM status (power/battery)\n\n```apm```\n\nShow **verbose** status\n\n```apm -v```\n\n**Minutes** of battery remaining\n\n```apm -m```\n\n**Continuously monitor** and update status\n\n```apm -M```\n\n**Suspend** the system\n\n```sudo apm -s```"],["SYNOPSIS","**apm** [_-VvmMsSdin_]"],["DESCRIPTION","**apm** queries Advanced Power Management (APM) status on Linux systems. It reports battery charge level, AC adapter status, and power management capabilities.\n\nAPM has largely been superseded by ACPI on modern systems, but the command may still work for compatibility or on older hardware."],["PARAMETERS","**-V**, **--version**\n> Print the version of the program and exit.\n\n**-v**, **--verbose**\n> Also print version information for the APM subsystem.\n\n**-m**, **--minutes**\n> Print the time remaining as a number of minutes instead of hh:mm.\n\n**-M**, **--monitor**\n> Continuously monitor and update the status information.\n\n**-s**, **--suspend**\n> Request that the machine be put into the APM suspend state.\n\n**-S**, **--standby**\n> Request that the machine be put into the APM standby state.\n\n**-d**, **--debug**\n> Print APM status information in a format more useful for debugging."],["CAVEATS","APM is deprecated in favor of ACPI on modern systems. Functionality may be limited or unavailable on ACPI-only systems. Use **acpi** command on modern hardware."],["HISTORY","**apm** was the standard power management interface for Linux until ACPI (Advanced Configuration and Power Interface) became dominant in the mid-**2000s**. The command remains for legacy compatibility."],["SEE ALSO","[acpi](/man/acpi)(1), [upower](/man/upower)(1), [powertop](/man/powertop)(8)"]],"e":["acpi","upower","powertop"],"x":"apm"},"aws-cloudtrail":{"n":"aws-cloudtrail","t":"Track and audit API activity across cloud services.","s":[["TAGLINE","Track and audit API activity across cloud services."],["TLDR","**Look up recent events** by username\n\n```aws cloudtrail lookup-events --lookup-attributes AttributeKey=Username,AttributeValue=[user@example.com]```\n\n**Look up events** by event name\n\n```aws cloudtrail lookup-events --lookup-attributes AttributeKey=EventName,AttributeValue=[RunInstances]```\n\n**Look up events** in a time range\n\n```aws cloudtrail lookup-events --start-time [2024-01-01T00:00:00Z] --end-time [2024-01-02T00:00:00Z]```\n\n**Create a multi-region trail**\n\n```aws cloudtrail create-trail --name [my-trail] --s3-bucket-name [my-bucket] --is-multi-region-trail```\n\n**Start logging** for a trail\n\n```aws cloudtrail start-logging --name [my-trail]```\n\n**List all trails**\n\n```aws cloudtrail describe-trails```\n\n**Get trail status**\n\n```aws cloudtrail get-trail-status --name [my-trail]```"],["SYNOPSIS","**aws cloudtrail** _command_ [_options_]"],["PARAMETERS","**lookup-events**\n> Search management events from the last 90 days\n\n**create-trail**\n> Create a new trail for logging API activity\n\n**describe-trails**\n> List trails and their configuration\n\n**get-trail-status**\n> Get logging status of a trail\n\n**start-logging**\n> Begin logging events to a trail\n\n**stop-logging**\n> Pause logging events to a trail\n\n**update-trail**\n> Modify trail configuration\n\n**delete-trail**\n> Remove a trail (logs remain in S3)\n\n**put-event-selectors**\n> Configure which events to log\n\n**put-insight-selectors**\n> Enable CloudTrail Insights\n\n**--name** _trail-name_\n> Trail name or ARN\n\n**--s3-bucket-name** _bucket_\n> S3 bucket for log storage\n\n**--is-multi-region-trail**\n> Log events from all regions\n\n**--is-organization-trail**\n> Log events for all accounts in organization\n\n**--kms-key-id** _key_\n> KMS key for log encryption\n\n**--lookup-attributes** _attr_\n> Filter events (AttributeKey=Type,AttributeValue=Value)\n\n**--start-time** _timestamp_\n> Start of time range for lookup\n\n**--end-time** _timestamp_\n> End of time range for lookup"],["LOOKUP ATTRIBUTES","**EventId** - Unique event identifier\n\n**EventName** - API action (RunInstances, CreateBucket)\n\n**Username** - IAM user or role name\n\n**ResourceType** - AWS resource type\n\n**ResourceName** - Resource identifier\n\n**EventSource** - Service (ec2.amazonaws.com)\n\n**AccessKeyId** - Access key used"],["DESCRIPTION","**aws cloudtrail** manages AWS CloudTrail, which logs API activity and events across AWS services for auditing, compliance, and security analysis.\n\n**lookup-events** searches management events from the last 90 days without needing a trail configured. It's rate-limited to 2 requests per second per region.\n\n**Trails** deliver logs to S3 buckets for long-term storage. Multi-region trails capture activity from all regions. Organization trails log events for all accounts in AWS Organizations.\n\n**Event selectors** filter which events to log (management events, data events for S3/Lambda, or Insights events for anomaly detection).\n\nLogs include who made the request, services used, actions performed, parameters, and response elements."],["CAVEATS","lookup-events only searches the last 90 days; use Athena on S3 logs for older events. Rate limited to 2 requests/second. Only one lookup attribute per query. Trail creation requires S3 bucket and bucket policy configuration. Log delivery has a delay of several minutes."],["HISTORY","**AWS CloudTrail** launched in **November 2013** as AWS's audit logging service. It became a critical component for security, compliance, and governance. CloudTrail Insights for anomaly detection was added in **2019**. CloudTrail Lake for SQL-based event querying launched in **2022**."],["INSTALL","```dnf: sudo dnf install aws```\n\n```pacman: sudo pacman -S aws-cli```\n\n```apk: sudo apk add aws-cli```\n\n```zypper: sudo zypper install aws-cli```"],["SEE ALSO","[aws](/man/aws)(1), [aws-s3](/man/aws-s3)(1), [aws-athena](/man/aws-athena)(1)"]],"e":["aws","aws-s3","aws-athena"],"x":"aws cloudtrail lookup-events --lookup-attributes AttributeKey=Username,AttributeValue=[user@example.com]"},"azdo":{"n":"azdo","t":"Terminal UI for Azure DevOps","s":[["TAGLINE","Terminal UI for Azure DevOps"],["TLDR","**Start** the interactive TUI\n\n```azdo```\n\n**Try it out** with realistic mock data, no setup required\n\n```azdo demo```\n\n**Set or update** the Azure DevOps personal access token\n\n```azdo auth```\n\n**Show** the version\n\n```azdo --version```"],["SYNOPSIS","**azdo** [_command_] [_options_]"],["DESCRIPTION","**azdo** is a terminal user interface for **Azure DevOps**. It brings pull requests, work items, and pipelines into a keyboard-driven console application so you can review and manage them without switching to the web portal.\n\nFrom the TUI you can list and filter pull requests, view diffs, vote, and add inline comments; browse work items and change their state; and monitor pipeline runs down to individual stages, jobs, and tasks with log viewing. An optional metrics dashboard shows live status and trend analysis, and the tool supports multiple projects, several built-in themes, and state that persists across sessions.\n\nAuthentication uses an Azure DevOps **Personal Access Token (PAT)**, stored in the system keyring (or supplied through the **AZDO_PAT** environment variable). Configuration, including the organization and project names, lives in **~/.config/azdo-tui/config.yaml**."],["PARAMETERS","**demo**\n\n> Launch the interface populated with mock data, so it can be explored without any configuration.\n\n**auth**\n\n> Set or update the stored personal access token.\n\n**--version**\n\n> Print version information and exit.\n\n**--help**\n\n> Show usage information."],["CONFIGURATION","**~/.config/azdo-tui/config.yaml**\n\n> Holds the organization name, the list of projects, and optional settings such as polling interval, theme, and metrics options.\n\n**AZDO_PAT**\n\n> Environment variable used as a fallback source for the personal access token when none is found in the keyring."],["CAVEATS","The personal access token needs the right scopes (typically Build read, Code read/write, and Work Items read/write) for all features to work. azdo is a young, third-party project not affiliated with Microsoft, so its behavior may change between releases."],["SEE ALSO","[az](/man/az)(1), [gh](/man/gh)(1), [glab](/man/glab)(1), [lazygit](/man/lazygit)(1), [tig](/man/tig)(1)"],["RESOURCES","```[Source code](https://github.com/Elpulgo/azdo)```"]],"e":["az","gh","glab","lazygit","tig"],"x":"azdo"},"cargo-rustc":{"n":"cargo-rustc","t":"Compile a package with extra rustc flags","s":[["TAGLINE","Compile a package with extra rustc flags"],["TLDR","**Compile with extra rustc flags**\n\n```cargo rustc -- [rustc_flags]```\n\n**Compile specific binary with flags**\n\n```cargo rustc --bin [name] -- [flags]```\n\n**Compile library with flags**\n\n```cargo rustc --lib -- [-C opt-level=3]```\n\n**Enable specific codegen option**\n\n```cargo rustc -- -C [target-cpu=native]```\n\n**Add link arguments**\n\n```cargo rustc -- -C [link-arg=-fuse-ld=lld]```"],["SYNOPSIS","**cargo rustc** [_options_] [-- _args_]"],["DESCRIPTION","**cargo rustc** compiles the current package and passes extra options directly to the Rust compiler. Unlike `RUSTFLAGS`, which applies flags to all crates in the build, arguments after `--` are passed only to the final compiler invocation for the specified target, not to its dependencies.\n\nThis command is useful for enabling specific codegen options, changing optimization levels, emitting intermediate representations like LLVM IR or assembly, or passing custom linker arguments for a single crate without affecting the rest of the dependency graph."],["PARAMETERS","**--lib**\n> Compile library only\n\n**--bin** _name_\n> Compile specified binary\n\n**--bins**\n> Compile all binaries\n\n**--example** _name_\n> Compile specified example\n\n**--test** _name_\n> Compile specified test\n\n**--bench** _name_\n> Compile specified benchmark\n\n**-r**, **--release**\n> Use release profile\n\n**--profile** _name_\n> Use specified profile\n\n**-p**, **--package** _spec_\n> Package to compile\n\n**--target** _triple_\n> Target platform\n\n**-j**, **--jobs** _n_\n> Parallel jobs\n\n**--features** _features_\n> Enable features\n\n**--all-features**\n> Enable all features"],["RUSTC OPTIONS","Common rustc flags:\n- **-C opt-level=N**: Optimization level\n- **-C target-cpu=NAME**: Target CPU\n- **-C link-arg=ARG**: Linker argument\n- **--emit=TYPE**: Output type (asm, llvm-ir, obj)"],["ENVIRONMENT","**RUSTFLAGS**\n> Pass flags to all rustc invocations"],["CAVEATS","Only one target can be compiled when passing extra arguments. Use filters (--lib, --bin) to select target."],["INSTALL","```apt: sudo apt install cargo```\n\n```dnf: sudo dnf install cargo```\n\n```apk: sudo apk add cargo```\n\n```zypper: sudo zypper install cargo```\n\n```nix: nix profile install nixpkgs#cargo```"],["SEE ALSO","[cargo](/man/cargo)(1), [rustc](/man/rustc)(1), [cargo-build](/man/cargo-build)(1)"],["RESOURCES","```[Source code](https://github.com/rust-lang/cargo)```\n\n```[Documentation](https://doc.rust-lang.org/cargo/commands/cargo-rustc.html)```"]],"e":["cargo","rustc","cargo-build"],"x":"cargo rustc -- [rustc_flags]"},"circumflex":{"n":"circumflex","t":"Hacker News client for the terminal","s":[["TAGLINE","Hacker News client for the terminal"],["TLDR","**Browse** Hacker News interactively\n\n```clx```\n\n**Browse** with Nerd Fonts icons enabled\n\n```clx -n```\n\n**Set** comment width to 80 characters\n\n```clx -c 80```\n\n**Read** comments for a specific submission by ID\n\n```clx comments [33192518]```\n\n**Read** an article in Reader Mode by URL\n\n```clx url [https://example.com/article]```\n\n**Add** a submission to favorites\n\n```clx add [33192518]```\n\n**Clear** browsing history\n\n```clx clear```\n\n**Show** only specific categories\n\n```clx --categories=\"ask,show\"```"],["SYNOPSIS","**clx** [_flags_]\n**clx** [_command_] [_arguments_]"],["PARAMETERS","**-a**, **--auto-expand**\n> Auto-expand all replies in comment threads.\n\n**--categories=**_string_\n> Set visible categories (default: \"top,best,ask,show\").\n\n**-c**, **--comment-width=**_int_\n> Set comment width in characters (default: 70).\n\n**-e**, **--disable-emojis**\n> Disable emoji conversion in output.\n\n**-d**, **--disable-history**\n> Disable marking submissions as read.\n\n**-t**, **--hide-indent**\n> Hide indentation guide bars in comments.\n\n**-n**, **--nerdfonts**\n> Enable Nerd Fonts icons.\n\n**-o**, **--plain-comments**\n> Disable syntax highlighting in comments.\n\n**-p**, **--plain-headlines**\n> Disable syntax highlighting in headlines.\n\n**-v**, **--version**\n> Display version number."],["SUBCOMMANDS","**add** _id_\n> Add a submission to favorites by its Hacker News ID.\n\n**comments** _id_\n> Jump directly to the comment section of a submission.\n\n**article** _id_\n> Read the linked article in Reader Mode.\n\n**url** _url_\n> Open any URL in Reader Mode.\n\n**clear**\n> Delete browsing history."],["DESCRIPTION","**circumflex** (binary name **clx**) is a terminal-based Hacker News client built with the Bubble Tea TUI framework. It lets you browse stories, read comment threads with syntax-aware formatting, and view linked articles in Reader Mode without leaving the terminal.\n\nComments are pretty-printed and piped to **less** with syntax highlighting for code snippets, @mentions, variables, and URLs. Rainbow-colored indentation bars distinguish nesting levels. The tool tracks read history, highlights new comments since last visit, and supports saving favorites as pretty-printed JSON at **~/.config/circumflex/favorites.json**."],["CAVEATS","Requires a modern version of **less** as its pager. Depends on Algolia's Hacker News Search API and cheeaun's unofficial HN API; functionality is impacted if either is down. Uses invisible Unicode characters for custom keybindings within less, which could cause issues with certain terminal configurations. The **CLX_BROWSER** environment variable can override the default browser."],["HISTORY","**circumflex** was created by **bensadeh** and publicly announced on Hacker News in **October 2022**. Written in Go using the Bubble Tea TUI framework, licensed under AGPL-3.0."],["INSTALL","```brew: brew install circumflex```\n\n```nix: nix profile install nixpkgs#circumflex```"],["SEE ALSO","[less](/man/less)(1), [newsboat](/man/newsboat)(1), [w3m](/man/w3m)(1), [lynx](/man/lynx)(1)"]],"e":["less","newsboat","w3m","lynx"],"x":"clx"},"compose":{"n":"compose","t":"open files using mailcap-configured editors","s":[["TAGLINE","open files using mailcap-configured editors"],["TLDR","**Compose** a file using the default mailcap editor\n\n```compose [filename]```\n\nUsing **run-mailcap** directly\n\n```run-mailcap --action=compose [filename]```"],["SYNOPSIS","**compose** _filename_"],["DESCRIPTION","**compose** is a convenience wrapper around run-mailcap's compose action, part of the mailcap system that provides MIME-type-aware file handling on Unix systems. When invoked, it examines the file's MIME type and launches the appropriate editor or composition tool as configured in the system's mailcap files.\n\nThe mailcap mechanism originated as a way to handle email attachments but has evolved into a general-purpose file handler system. It maps MIME types (like text/plain, image/png, or application/pdf) to specific commands that can view, edit, compose, or print those files. The compose action specifically refers to creating or editing files, distinguishing it from view (read-only) or print actions.\n\nThis tool is particularly useful in mail user agents (MUAs) and other applications that need to launch external editors based on content type rather than hardcoding specific applications. Different MIME types can be handled by different specialized tools automatically."],["PARAMETERS","_filename_\n> The file to compose/edit"],["CONFIGURATION","**~/.mailcap**\n> User-specific MIME type to application mappings.\n\n**/etc/mailcap**\n> System-wide MIME type mappings."],["CAVEATS","Depends on mailcap configuration. The actual editor used depends on the file's MIME type and system configuration. Debian/Ubuntu specific."],["INSTALL","```aur: yay -S compose```"],["SEE ALSO","[run-mailcap](/man/run-mailcap)(1), [edit](/man/edit)(1), [view](/man/view)(1)"],["RESOURCES","```[Source code](https://salsa.debian.org/debian/mime-support)```"]],"e":["run-mailcap","edit","view"],"x":"compose [filename]"},"conda-rename":{"n":"conda-rename","t":"environment renaming utility","s":[["TAGLINE","environment renaming utility"],["TLDR","**Rename an environment**\n\n```conda rename -n [old_name] [new_name]```"],["SYNOPSIS","**conda** **rename** [_options_] _new_name_"],["PARAMETERS","**-n**, **--name** _name_\n> Current name of environment to rename.\n\n**-p**, **--prefix** _path_\n> Current path of environment to rename."],["CONFIGURATION","**~/.condarc**\n> User-level conda configuration for channels, package settings, and solver options.\n\n**/opt/conda/.condarc**\n> System-wide conda configuration."],["DESCRIPTION","**conda rename** changes the name of an existing conda environment. This is done by cloning the environment to the new name and removing the old one."],["CAVEATS","The environment must be deactivated before renaming."],["SEE ALSO","[conda](/man/conda)(1), [conda-create](/man/conda-create)(1)"],["RESOURCES","```[Source code](https://github.com/conda/conda)```\n\n```[Documentation](https://docs.conda.io/projects/conda/en/stable/commands/rename.html)```"]],"e":["conda","conda-create"],"x":"conda rename -n [old_name] [new_name]"},"crane-tag":{"n":"crane-tag","t":"add tag alias to existing image","s":[["TAGLINE","add tag alias to existing image"],["TLDR","**Add tag** to an image\n\n```crane tag [image:existing] [new_tag]```\n\n**Tag by digest**\n\n```crane tag [image]@[sha256:abc...] [tag_name]```"],["SYNOPSIS","**crane** **tag** [_options_] _image_ _tag_"],["DESCRIPTION","**crane tag** adds a new tag to an existing image. This creates an alias without copying image data."],["SEE ALSO","[crane](/man/crane)(1), [crane-copy](/man/crane-copy)(1)"],["RESOURCES","```[Source code](https://github.com/google/go-containerregistry)```\n\n```[Documentation](https://github.com/google/go-containerregistry/blob/main/cmd/crane/doc/crane_tag.md)```"]],"e":["crane","crane-copy"],"x":"crane tag [image:existing] [new_tag]"},"debchange":{"n":"debchange","t":"Debian changelog maintenance tool","s":[["TAGLINE","Debian changelog maintenance tool"],["TLDR","Add a new version for **non-maintainer upload**\n\n```debchange -n```\n\n**Append** entry to current version\n\n```debchange -a```\n\nAdd entry to **close a bug**\n\n```debchange --closes [bug_id]```"],["SYNOPSIS","**debchange** [_options_] [_text_]"],["DESCRIPTION","**debchange** (also known as dch) maintains the debian/changelog file of a Debian source package. It handles version numbering, timestamps, and changelog entry formatting according to Debian policy.\n\nEssential for Debian package maintainers to track changes between package versions."],["PARAMETERS","**-n, --nmu**\n> Increment version for non-maintainer upload\n\n**-a, --append**\n> Append to current changelog entry\n\n**--closes** _bug_\n> Add entry to close specified bug\n\n**-i, --increment**\n> Increment version and add new entry\n\n**-v** _version_\n> Set specific version number\n\n**-D** _distribution_\n> Set distribution (stable, unstable, etc.)\n\n**-r, --release**\n> Finalize the changelog for a release\n\n**--create**\n> Create a new debian/changelog file"],["CONFIGURATION","**~/.devscripts**\n> Default configuration options for debchange behavior, including maintainer name and email.\n\n**/etc/devscripts.conf**\n> System-wide devscripts configuration file."],["CAVEATS","Must be run from a Debian source package directory. Requires proper debian/changelog format. Part of devscripts package."],["SEE ALSO","[dch](/man/dch)(1), [dpkg-buildpackage](/man/dpkg-buildpackage)(1), [debuild](/man/debuild)(1)"],["RESOURCES","```[Source code](https://salsa.debian.org/debian/devscripts)```\n\n```[Documentation](https://manpages.debian.org/devscripts/debchange.1)```"]],"e":["dch","dpkg-buildpackage","debuild"],"x":"debchange -n"},"diffimg":{"n":"diffimg","t":"compute a pixel-by-pixel difference image between two input images","s":[["TAGLINE","compute a pixel-by-pixel difference image between two input images"],["TLDR","**Compute the difference** of two images and save it\n\n```diffimg [image1.png] [image2.png] [diff.png]```\n\n**Use it for visual regression tests**\n\n```diffimg [expected.png] [actual.png] [report/diff.png]```"],["SYNOPSIS","**diffimg** _image1_ _image2_ _output_"],["DESCRIPTION","**diffimg** computes the per-pixel difference between two equally sized images and writes the result to a third file. Each output pixel encodes the delta between the corresponding pixels of the two inputs — identical areas appear black, changed areas light up. This makes it an easy drop-in for visual regression checks, before/after comparisons, and QA pipelines.\n\nImplementations vary — some are Python/PIL wrappers, some are Qt-based GUIs with a CLI front end, and some are Go/Rust rewrites. All agree on the basic three-argument signature and on support for PNG, JPEG, and GIF input."],["PARAMETERS","_image1_\n> First input image.\n\n_image2_\n> Second input image. Must be the same dimensions as _image1_.\n\n_output_\n> Path where the difference image will be written. The extension selects the output format."],["SUPPORTED FORMATS","PNG, JPEG, GIF, and (where ImageMagick is available under the hood) PostScript and TIFF."],["CAVEATS","Both images must have the same dimensions; resize or crop first if they don't. Output reflects raw pixel deltas, not perceptual differences — a 1-pixel shift lights up every edge. For perceptual or structural comparisons, use **compare** (ImageMagick) with `-metric SSIM` or a dedicated tool like `pixelmatch`, `odiff`, or `perceptualdiff`.\n\nDifferent tools also named `diffimg` exist — check `diffimg --version` or the package description to know which you have."],["HISTORY","Multiple tools named **diffimg** have circulated since the early 2000s. The most widely packaged is a Python CLI/GUI by **Jonathan Zurflueh (thebulb)**. Other implementations include Qt-based desktop GUIs and Go/Rust rewrites."],["INSTALL","```apt: sudo apt install graphviz```\n\n```dnf: sudo dnf install graphviz```\n\n```pacman: sudo pacman -S graphviz```\n\n```apk: sudo apk add graphviz```\n\n```zypper: sudo zypper install graphviz```\n\n```brew: brew install graphviz```\n\n```nix: nix profile install nixpkgs#graphviz```"],["SEE ALSO","[compare](/man/compare)(1), [diff](/man/diff)(1), [convert](/man/convert)(1)"]],"e":["compare","diff","convert"],"x":"diffimg [image1.png] [image2.png] [diff.png]"},"domainname":{"n":"domainname","t":"Display or set the system's NIS/YP domain name","s":[["TAGLINE","Display or set the system's NIS/YP domain name"],["TLDR","**Show current NIS domain name**\n\n```domainname```\n\n**Set NIS domain name**\n\n```sudo domainname [example.com]```\n\n**Set NIS domain name from a file**\n\n```sudo domainname -F [/etc/nisdomain]```\n\n**Clear domain name**\n\n```sudo domainname \"\"```"],["SYNOPSIS","**domainname** [_name_]"],["DESCRIPTION","**domainname** displays or sets the system's NIS (Network Information Service) domain name. This is different from the DNS domain name and is used for NIS/YP services.\n\nWithout arguments, it shows the current NIS domain. With an argument, it sets the domain name (requires root privileges). The setting is typically not persistent across reboots without additional configuration."],["PARAMETERS","**-v**, **--verbose**\n> Be verbose about actions.\n\n**-F** _file_, **--file** _file_\n> Read domain name from the specified file.\n\n**-y**, **--yp**, **--nis**\n> Display the NIS domain name (default behavior when invoked as domainname).\n\n**-h**, **--help**\n> Display help and exit.\n\n**-V**, **--version**\n> Show version information and exit."],["CAVEATS","Do not confuse this with the DNS domain name; use **dnsdomainname** for that. Setting is not persistent by default across reboots. NIS is deprecated in favor of LDAP and Kerberos. Rarely used on modern systems."],["HISTORY","**domainname** dates back to the early days of **NIS** (originally Yellow Pages, YP), developed at **Sun Microsystems** in the **1980s**. NIS provided centralized authentication and configuration for Unix networks. While largely replaced by LDAP and Kerberos, the command remains for compatibility."],["INSTALL","```apt: sudo apt install hostname```\n\n```dnf: sudo dnf install hostname```\n\n```pacman: sudo pacman -S net-tools```\n\n```apk: sudo apk add net-tools```\n\n```zypper: sudo zypper install hostname```\n\n```brew: brew install net-tools```\n\n```nix: nix profile install nixpkgs#hostname```"],["SEE ALSO","[hostname](/man/hostname)(1), [dnsdomainname](/man/dnsdomainname)(1)"]],"e":["hostname","dnsdomainname"],"x":"domainname"},"dotnet-tool":{"n":"dotnet-tool","t":".NET CLI tool package manager","s":[["TAGLINE",".NET CLI tool package manager"],["TLDR","**Install global tool**\n\n```dotnet tool install -g [tool-name]```\n\n**List global tools**\n\n```dotnet tool list -g```\n\n**Update global tool**\n\n```dotnet tool update -g [tool-name]```\n\n**Uninstall global tool**\n\n```dotnet tool uninstall -g [tool-name]```\n\n**Install local tool**\n\n```dotnet tool install [tool-name]```\n\n**Restore local tools**\n\n```dotnet tool restore```"],["SYNOPSIS","**dotnet tool** _command_ [_options_]"],["PARAMETERS","_COMMAND_\n> Operation: install, list, update, uninstall, restore.\n\n**install** _PACKAGE_\n> Install tool package.\n\n**list**\n> List installed tools.\n\n**update** _PACKAGE_\n> Update tool to latest version.\n\n**uninstall** _PACKAGE_\n> Remove tool.\n\n**restore**\n> Restore local tools from manifest.\n\n**-g**, **--global**\n> Global installation (user-wide).\n\n**--tool-path** _PATH_\n> Custom installation directory.\n\n**--help**\n> Display help information."],["CONFIGURATION","**dotnet-tools.json**\n> Manifest file specifying local tool dependencies and versions for the project."],["DESCRIPTION","**dotnet tool** manages .NET CLI tools, which are NuGet packages containing console applications. Tools can be installed globally (user-wide) or locally (project-specific).\n\nGlobal tools are available from any directory and installed in a user-specific location. Local tools are specified in a manifest file (dotnet-tools.json) and installed per-project.\n\nPopular tools include dotnet-ef (Entity Framework), dotnet-format (code formatting), and various analyzers and generators."],["CAVEATS","Global tools may have version conflicts. Local tools require manifest restoration. Tool versions should match project requirements. PATH configuration needed for global tools."],["HISTORY","dotnet tool was introduced in **.NET Core 2.1** (**2018**) to provide extensibility through NuGet-distributed CLI tools, enabling ecosystem growth beyond the built-in commands."],["INSTALL","```dnf: sudo dnf install dotnet-host```\n\n```pacman: sudo pacman -S dotnet-host```\n\n```apk: sudo apk add dotnet-host```\n\n```brew: brew install dotnet```"],["SEE ALSO","[dotnet](/man/dotnet)(1), [dotnet-ef](/man/dotnet-ef)(1)"]],"e":["dotnet","dotnet-ef"],"x":"dotnet tool install -g [tool-name]"},"elodie":{"n":"elodie","t":"EXIF-based photo, video, and audio organizer","s":[["TAGLINE","EXIF-based photo, video, and audio organizer"],["TLDR","**Import** files into a destination library\n\n```elodie.py import --destination=[\"/path/to/library\"] [\"/path/to/photos\"]```\n\n**Dry-run** an import (no changes)\n\n```elodie.py import --dry-run --destination=[\"/path/to/library\"] [\"/path/to/photos\"]```\n\n**Import** and set location when EXIF lacks it\n\n```elodie.py import --destination=[\"/path/to/library\"] --location=[\"Las Vegas, NV\"] [\"/path/to/photos\"]```\n\n**Update** location EXIF and reorganize\n\n```elodie.py update --location=[\"Las Vegas, NV\"] [file.jpg]...```\n\n**Update** capture time\n\n```elodie.py update --time=[\"2015-04-15\"] [file.jpg]...```\n\n**Regenerate** checksum database\n\n```elodie.py generate-db --source=[\"/path/to/library\"]```\n\n**Verify** library against bit rot\n\n```elodie.py verify```\n\n**Run** plugin batch operations (dry-run)\n\n```elodie.py batch --dry-run```"],["SYNOPSIS","**elodie.py** *command* [*options*] [*paths*...]"],["DESCRIPTION","**Elodie** is a personal EXIF-based assistant for organizing photos, videos, and audio. It reads metadata (via **ExifTool**), copies media into a structured library, can update location/time/title/album tags, and optionally moves originals to the trash after import.\n\nPrimary entry point from the repository is **elodie.py** (run after **pip install -r requirements.txt** in a clone). Requires **ExifTool** on PATH (**exiftool -ver**); video features need ExifTool 10.20+, HEIC 11.50+, and geolocation-to-city translation 13.13+.\n\nLibrary layout and naming are configurable via **~/.elodie/config.ini** (or **ELODIE_APPLICATION_DIRECTORY**). A **hash.json** under **~/.elodie/** tracks SHA-256 signatures for duplicate detection and **verify**."],["PARAMETERS","**import** [*paths*...]\n\n> Copy media into **--destination** (required), reading EXIF. Options include **--source**, **--file**, **--album-from-folder**, **--trash**, **--allow-duplicates**, **--location**, **--time**, **--exclude-regex**, **--debug**, **--dry-run**.\n\n**update** *files*...\n\n> Update EXIF and reorganize. Options: **--album**, **--location**, **--time**, **--title**, **--debug**, **--dry-run**.\n\n**generate-db**\n\n> Rebuild **hash.json** from **--source** (required).\n\n**batch**\n\n> Run **batch()** for all plugins (**--debug**, **--dry-run**).\n\n**verify**\n\n> Check the library against stored checksums for bit rot.\n\nUse **elodie.py** *command* **--help** for full option text."],["CONFIGURATION","Place **config.ini** in **~/.elodie/** (or under **ELODIE_APPLICATION_DIRECTORY**). Sections can define custom folder/file path templates and **[Exclusions]** patterns. Command-line **--exclude-regex** also skips paths at import time. **--location** / **--time** on import overwrite existing EXIF when provided; use them when data is missing or known wrong."],["CAVEATS","Depends on system ExifTool; missing or old ExifTool limits formats and geolocation. MapQuest integration is deprecated in favor of ExifTool. Always prefer **--dry-run** before bulk imports. Default import **copies** files; **--trash** moves sources after success."],["SEE ALSO","[exiftool](/man/exiftool)(1), [exif](/man/exif)(1), [rsync](/man/rsync)(1)"],["RESOURCES","```[Source code](https://github.com/jmathai/elodie)```"]],"e":["exiftool","exif","rsync"],"x":"elodie.py import --destination=[\"/path/to/library\"] [\"/path/to/photos\"]"},"git-bugreport":{"n":"git-bugreport","t":"Generate diagnostic information for reporting Git bugs","s":[["TAGLINE","Generate diagnostic information for reporting Git bugs"],["TLDR","**Generate bug report**\n\n```git bugreport```\n\n**Specify output directory**\n\n```git bugreport --output-directory [path]```\n\n**Specify filename suffix**\n\n```git bugreport --suffix [description]```"],["SYNOPSIS","**git bugreport** [_options_]"],["PARAMETERS","**--output-directory** _DIR_\n> Directory for report file.\n\n**--suffix** _SUFFIX_\n> Suffix for filename.\n\n**-s** _SUFFIX_\n> Short for --suffix.\n\n**--help**\n> Display help information."],["DESCRIPTION","**git bugreport** generates a template for reporting bugs to the Git project. It collects system information, git version, and configuration details needed for debugging.\n\nThe report includes environment information, installed hooks, and space for describing the problem. It outputs to a text file ready for submission to the git mailing list or issue tracker.\n\nThe generated report contains Git version, operating system details, shell information, and a template prompting for steps to reproduce the issue, expected behavior, and actual behavior."],["CAVEATS","For reporting git bugs, not project bugs. May include sensitive config info. Review before submitting."],["HISTORY","git bugreport was added to **Git** to standardize bug reporting and ensure necessary diagnostic information is included."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git](/man/git)(1)"]],"e":["git"],"x":"git bugreport"},"git-mergetool--lib":{"n":"git-mergetool--lib","t":"Internal tool integration library","s":[["TAGLINE","Internal tool integration library"],["TLDR","**Source for tool scripts**\n\n```. \"$(git --exec-path)/git-mergetool--lib\"```"],["SYNOPSIS","**git-mergetool--lib**"],["DESCRIPTION","**git-mergetool--lib** is a shell library used internally by `git-mergetool` and `git-difftool`. It provides common functions for tool selection, invocation, and configuration handling.\n\nThe library is sourced by scripts that need merge or diff tool functionality. It handles tool detection, path configuration, and tool-specific setup. This is internal infrastructure and not intended for direct use."],["CAVEATS","Internal library, not a command. Sourced by other git scripts. Not intended for direct use."],["HISTORY","git-mergetool--lib is part of **Git's** internal infrastructure, providing shared code for merge and diff tool integration."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-mergetool](/man/git-mergetool)(1), [git-difftool](/man/git-difftool)(1)"],["RESOURCES","```[Source code](https://github.com/git/git)```\n\n```[Homepage](https://git-scm.com/)```\n\n```[Documentation](https://git-scm.com/docs/git-mergetool--lib)```"]],"e":["git-mergetool","git-difftool"],"x":". \"$(git --exec-path)/git-mergetool--lib\""},"git-stamp":{"n":"git-stamp","t":"Append an identifier stamp to the last commit message","s":[["TAGLINE","Append an identifier stamp to the last commit message"],["TLDR","**Stamp the last commit** with an id and message\n\n```git stamp [Reviewed-by] \"[Jane Doe]\"```\n\n**Stamp with just an id**\n\n```git stamp [Fixes-123]```\n\n**Replace any previous stamp** with the same id\n\n```git stamp --replace [Reviewed-by] \"[Jane Doe]\"```"],["SYNOPSIS","**git** **stamp** [_-r_ | _--replace_] _id_ [_message_...]"],["PARAMETERS","_ID_\n> Stamp identifier (e.g. `Reviewed-by`, `Fixes`).\n\n_MESSAGE_\n> Optional text appended after the id.\n\n**-r**, **--replace**\n> Remove any existing stamp lines with the same id (case-insensitive) before appending the new one."],["DESCRIPTION","**git stamp** amends the most recent commit, appending a new paragraph of the form `<id> <message>` to its message. Part of git-extras, it is a quick way to tag a commit with metadata like `Reviewed-by`, `Tested-by`, or an issue reference without hand-editing the commit message.\n\nWith `--replace`, any prior paragraph in the message starting with the same id is stripped before the new stamp is added, so re-running the command updates rather than duplicates the stamp."],["CAVEATS","Amends HEAD, so it rewrites the commit hash; don't use it on a commit that has already been pushed and shared."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-commit](/man/git-commit)(1), [git-extras](/man/git-extras)(1)"],["RESOURCES","```[Source code](https://github.com/tj/git-extras)```\n\n```[Documentation](https://github.com/tj/git-extras/blob/master/Commands.md#git-stamp)```"]],"e":["git-commit","git-extras"],"x":"git stamp [Reviewed-by] \"[Jane Doe]\""},"gpclient":{"n":"gpclient","t":"GlobalProtect VPN client for Linux","s":[["TAGLINE","GlobalProtect VPN client for Linux"],["TLDR","**Connect** to a GlobalProtect VPN using a portal server\n\n```gpclient connect [vpn_gateway_url]```\n\n**Disconnect** from the currently connected VPN server\n\n```gpclient disconnect```\n\nLaunch the **graphical user interface** for VPN management\n\n```gpclient launch-gui```\n\nGenerate a **HIP report** (Host Integrity Protection)\n\n```gpclient hip```\n\nUse **OpenSSL workaround** to bypass legacy renegotiation errors\n\n```gpclient connect --fix-openssl [vpn_gateway_url]```\n\n**Ignore TLS errors** during connection\n\n```gpclient connect --ignore-tls-errors [vpn_gateway_url]```\n\nDisplay **version**\n\n```gpclient --version```\n\nDisplay **help** for any command\n\n```gpclient help [command]```"],["SYNOPSIS","**gpclient** _command_ [_options_]"],["PARAMETERS","**connect** _URL_\n> Connect to GlobalProtect VPN gateway\n\n**disconnect**\n> Disconnect from current VPN session\n\n**launch-gui**\n> Open graphical interface\n\n**hip**\n> Generate a HIP (Host Integrity Protection) report\n\n**--fix-openssl**\n> Apply workaround for OpenSSL legacy renegotiation\n\n**--ignore-tls-errors**\n> Skip TLS certificate validation\n\n**-g**, **--gateway** _URL_\n> Connect directly to this gateway instead of prompting\n\n**--auto-gateway**\n> Automatically try gateways in priority order without prompting\n\n**-u**, **--user** _USERNAME_\n> Specify username for authentication\n\n**--browser** [_BROWSER_]\n> Use external browser for SSO authentication (chrome, firefox, default, or remote)\n\n**--as-gateway**\n> Treat the server argument as a gateway instead of a portal"],["DESCRIPTION","**gpclient** is a Linux client for Palo Alto Networks GlobalProtect VPN, built on top of OpenConnect. It provides both command-line and graphical interfaces for connecting to corporate VPN gateways that use GlobalProtect.\n\nThe client supports SAML authentication, two-factor authentication, and both portal and gateway connections. It is an open-source alternative to the official GlobalProtect client, which has limited Linux support."],["CAVEATS","Requires OpenConnect with GlobalProtect support. Some authentication methods (like hardware tokens) may have limited support. The **--ignore-tls-errors** option should only be used for testing as it bypasses security."],["HISTORY","**gpclient** is part of the **GlobalProtect-openconnect** project, created as an open-source solution for Linux users needing to connect to GlobalProtect VPNs. It leverages OpenConnect's GlobalProtect protocol support (added in OpenConnect 8.0). The project was rewritten from Python/Qt to **Rust** and **Tauri** in its 2.x series."],["INSTALL","```nix: nix profile install nixpkgs#gpclient```"],["SEE ALSO","[openconnect](/man/openconnect)(8), [openvpn](/man/openvpn)(8), [nmcli](/man/nmcli)(1)"],["RESOURCES","```[Source code](https://github.com/yuezk/GlobalProtect-openconnect)```"]],"e":["openconnect","openvpn","nmcli"],"x":"gpclient connect [vpn_gateway_url]"},"gpg2":{"n":"gpg2","t":"GnuPG 2 encryption and digital signatures","s":[["TAGLINE","GnuPG 2 encryption and digital signatures"],["TLDR","**Generate key pair**\n\n```gpg2 --full-generate-key```\n\n**List keys**\n\n```gpg2 --list-keys```\n\n**Encrypt file for recipient**\n\n```gpg2 -e -r [recipient@email.com] [file]```\n\n**Decrypt file**\n\n```gpg2 -d [file.gpg]```\n\n**Sign file**\n\n```gpg2 --sign [file]```\n\n**Verify signature**\n\n```gpg2 --verify [file.sig]```\n\n**Export public key**\n\n```gpg2 --armor --export [keyid] > [public.asc]```\n\n**Import a key**\n\n```gpg2 --import [public.asc]```\n\n**Symmetric encryption** with passphrase\n\n```gpg2 -c [file]```"],["SYNOPSIS","**gpg2** [_options_] [_files_]"],["PARAMETERS","**-e**, **--encrypt**\n> Encrypt data.\n\n**-d**, **--decrypt**\n> Decrypt data.\n\n**-s**, **--sign**\n> Sign data.\n\n**--verify**\n> Verify signature.\n\n**-r**, **--recipient** _name_\n> Encrypt for recipient.\n\n**-a**, **--armor**\n> ASCII armored output.\n\n**--gen-key**\n> Generate key pair with default settings.\n\n**--full-generate-key**\n> Generate key pair with full options (algorithm, size, expiry).\n\n**--list-keys**\n> List public keys.\n\n**--list-secret-keys**\n> List private keys.\n\n**--export** _keyid_\n> Export public key.\n\n**--import** _file_\n> Import keys.\n\n**--delete-keys** _keyid_\n> Delete public key from keyring.\n\n**--delete-secret-keys** _keyid_\n> Delete secret key from keyring.\n\n**--keyserver** _server_\n> Keyserver to use.\n\n**-o**, **--output** _file_\n> Write output to file instead of stdout.\n\n**--clearsign**\n> Make a cleartext signature.\n\n**--detach-sign**\n> Make a detached signature.\n\n**-c**, **--symmetric**\n> Encrypt with a symmetric cipher using a passphrase.\n\n**--batch**\n> Run in non-interactive batch mode.\n\n**-q**, **--quiet**\n> Minimize output.\n\n**-v**, **--verbose**\n> Display detailed processing information."],["DESCRIPTION","**gpg2** (GnuPG 2) is the modern version of GNU Privacy Guard, implementing the OpenPGP standard for encryption, digital signatures, and key management. It uses a modular architecture with separate daemons for improved security.\n\nOn most modern systems, **gpg** is aliased to gpg2. The two share keyrings and are functionally equivalent, with gpg2 offering improved architecture and daemon management."],["CONFIGURATION","**~/.gnupg/gpg.conf**\n> User configuration file for default options, preferred algorithms, and keyserver settings.\n\n**~/.gnupg/gpg-agent.conf**\n> Configuration for the gpg-agent daemon handling passphrase caching and key operations."],["CAVEATS","Key management requires understanding of web of trust. Private keys must be protected. Passphrase selection is critical. gpg and gpg2 share keyrings on modern systems."],["HISTORY","GnuPG was created by **Werner Koch** in **1997** as a free replacement for PGP. GPG 2.0 was released in **2006** with a modular architecture. It's a fundamental tool for secure email and software signing."],["INSTALL","```apk: sudo apk add gpg```\n\n```zypper: sudo zypper install gpg2```\n\n```brew: brew install gpg```"],["SEE ALSO","[gpg](/man/gpg)(1), [gpgconf](/man/gpgconf)(1)"]],"e":["gpg","gpgconf"],"x":"gpg2 --full-generate-key"},"jj-revert":{"n":"jj-revert","t":"creates a new commit that undoes the changes from specified revisions","s":[["TAGLINE","creates a new commit that undoes the changes from specified revisions"],["TLDR","**Revert changes from revision**\n\n```jj revert -r [revision]```\n\n**Revert multiple revisions**\n\n```jj revert -r [rev1] -r [rev2]```"],["SYNOPSIS","**jj** **revert** [_options_]"],["PARAMETERS","**-r**, **--revision** _rev_\n> Revision to revert."],["DESCRIPTION","**jj revert** creates a new commit that undoes the changes from specified revisions. The original commits remain in history. Unlike restore, revert creates new commits to reverse changes."],["INSTALL","```pacman: sudo pacman -S jujutsu```\n\n```apk: sudo apk add jujutsu```\n\n```zypper: sudo zypper install jujutsu```\n\n```brew: brew install jujutsu```\n\n```nix: nix profile install nixpkgs#jujutsu```"],["SEE ALSO","[jj](/man/jj)(1), [jj-restore](/man/jj-restore)(1), [jj-undo](/man/jj-undo)(1)"]],"e":["jj","jj-restore","jj-undo"],"x":"jj revert -r [revision]"},"john":{"n":"john","t":"the Ripper, a password security auditing tool","s":[["TAGLINE","the Ripper, a password security auditing tool"],["TLDR","**Crack password file**\n\n```john [hashes.txt]```\n\n**Show cracked passwords**\n\n```john --show [hashes.txt]```\n\n**Use specific wordlist**\n\n```john --wordlist=[wordlist.txt] [hashes.txt]```\n\n**Specify hash format**\n\n```john --format=[md5crypt] [hashes.txt]```\n\n**Incremental mode**\n\n```john --incremental [hashes.txt]```\n\n**Resume cracking session**\n\n```john --restore```"],["SYNOPSIS","**john** [_options_] [_password_files_]"],["PARAMETERS","_PASSWORD_FILES_\n> Files containing hashes.\n\n**--show**\n> Show cracked passwords.\n\n**--wordlist** _FILE_\n> Use wordlist attack.\n\n**--format** _FORMAT_\n> Hash format (md5crypt, sha256, etc.).\n\n**--incremental**\n> Brute force mode.\n\n**--restore**\n> Resume previous session.\n\n**--help**\n> Display help information."],["DESCRIPTION","**john** is John the Ripper, a password security auditing tool. It detects weak passwords through various cracking methods.\n\nThe tool supports many hash formats and attack modes. It's used for security testing and password audits."],["CAVEATS","Authorized use only. CPU/GPU intensive. Jumbo version has more formats."],["HISTORY","John the Ripper was created by **Solar Designer** in 1996 and is maintained by **Openwall** as an open-source password auditing tool."],["INSTALL","```apt: sudo apt install john```\n\n```dnf: sudo dnf install john```\n\n```pacman: sudo pacman -S john```\n\n```apk: sudo apk add john```\n\n```zypper: sudo zypper install john```\n\n```brew: brew install john```\n\n```nix: nix profile install nixpkgs#john```"],["SEE ALSO","[hashcat](/man/hashcat)(1), [unshadow](/man/unshadow)(1), [zip2john](/man/zip2john)(1)"]],"e":["hashcat","unshadow","zip2john"],"x":"john [hashes.txt]"},"jupyterlab":{"n":"jupyterlab","t":"launches JupyterLab, an interactive development environment","s":[["TAGLINE","launches JupyterLab, an interactive development environment"],["TLDR","**Start JupyterLab server**\n\n```jupyter lab```\n\n**Start on specific port**\n\n```jupyter lab --port [8888]```\n\n**Start without opening browser**\n\n```jupyter lab --no-browser```\n\n**Start in specific directory**\n\n```jupyter lab --notebook-dir=[path]```\n\n**Start with specific IP binding**\n\n```jupyter lab --ip=[0.0.0.0]```"],["SYNOPSIS","**jupyter** **lab** [_options_]"],["PARAMETERS","**--port** _number_\n> Port to run server on. Default: 8888.\n\n**--no-browser**\n> Don't open browser automatically.\n\n**--notebook-dir** _path_\n> Directory to serve notebooks from.\n\n**--ip** _address_\n> IP address to bind to. Default: localhost.\n\n**--allow-root**\n> Allow running as root user.\n\n**--core-mode**\n> Start in core mode with no extensions enabled.\n\n**--dev-mode**\n> Start in development mode using local JavaScript packages.\n\n**--app-dir** _path_\n> Start with a particular set of extensions.\n\n**--certfile** _path_\n> SSL/TLS certificate file for HTTPS.\n\n**--help**\n> Show abbreviated command-line options.\n\n**--help-all**\n> Show all available options including detailed ones."],["DESCRIPTION","**jupyter lab** launches JupyterLab, an interactive development environment for working with notebooks, code, and data. It provides a flexible interface for data science, scientific computing, and machine learning workflows with support for multiple programming languages.\n\nJupyterLab is the next-generation web-based interface for Project Jupyter, offering a tabbed workspace with file browser, terminals, text editors, and notebook support all in one environment."],["INSTALL","```dnf: sudo dnf install jupyterlab```\n\n```pacman: sudo pacman -S jupyterlab```\n\n```brew: brew install jupyterlab```"],["SEE ALSO","[jupyter](/man/jupyter)(1), [python](/man/python)(1), [pip](/man/pip)(1)"]],"e":["jupyter","python","pip"],"x":"jupyter lab"},"lavat":{"n":"lavat","t":"Terminal lava lamp animation with metaballs","s":[["TAGLINE","Terminal lava lamp animation with metaballs"],["TLDR","**Run the lava lamp** animation with default settings\n\n```lavat```\n\n**Customize metaball and rim colors**\n\n```lavat -c [green] -k [red]```\n\n**Enable truecolor gradient** mode\n\n```lavat -g -c [00FF00] -k [FF0000]```\n\n**Enable gravity** (balls heat, rise, cool, fall)\n\n```lavat -G```\n\n**Set speed, radius, and ball count**\n\n```lavat -s [7] -r [6] -b [12]```\n\n**Use custom characters** for a 3D-like look\n\n```lavat -F [\".:-=+*#%@\"]```\n\n**Run in party mode** (preset color cycling)\n\n```lavat -p [p3]```"],["SYNOPSIS","**lavat** [_options_]"],["PARAMETERS","**-c** _color_\n> Metaball color. Accepts a named color or hex value (e.g. _red_, _00FF00_).\n\n**-k** _color_\n> Rim color drawn around each metaball.\n\n**-g**\n> Enable gradient/truecolor rendering (requires a truecolor-capable terminal).\n\n**-G**\n> Enable gravity simulation. Metaballs heat at the bottom, rise, cool at the top, then fall.\n\n**-s** _1-10_\n> Animation speed. Default is _5_.\n\n**-r** _1-10_\n> Metaball radius. Default is _5_.\n\n**-R** _1-5_\n> Rim thickness around each metaball.\n\n**-b** _5-20_\n> Number of metaballs. Default is _10_.\n\n**-F** _chars_\n> Custom character set used to render density levels.\n\n**-C**\n> Confine metaballs inside the terminal bounds.\n\n**-p** _p1_|_p2_|_p3_\n> Enable one of three preset party (color-cycling) modes.\n\n**-h**\n> Print help and exit."],["RUNTIME KEYS","**i** / **d**\n> Increase / decrease metaball radius.\n\n**I** / **D**\n> Increase / decrease rim size.\n\n**m** / **l**\n> Add / remove a metaball.\n\n**c** / **k**\n> Cycle metaball / rim color.\n\n**+** / **-**\n> Increase / decrease animation speed.\n\n**p**\n> Toggle party mode.\n\n**q** or **Esc**\n> Quit."],["DESCRIPTION","**lavat** is a terminal program that simulates a lava lamp using **metaballs** rendered with text characters. Each metaball is a soft circular density field; overlapping fields blend smoothly, producing the characteristic blob-merging effect of a lava lamp directly inside the terminal.\n\nThe program supports both standard 256-color mode and a true-color gradient mode (**-g**), plus an optional gravity model (**-G**) that periodically heats balls at the bottom of the display so they rise, cool at the top, and fall again. Colors, radius, speed, and the number of metaballs can be tuned from the command line or adjusted interactively while the animation runs.\n\n**lavat** is primarily used as a decorative screensaver-style toy or a visual backdrop in a spare terminal window. It is not a screen-locking utility and does not require an X session — it runs in any Unix terminal supported by its underlying **Termbox2** rendering library."],["CAVEATS","Gradient mode (**-g**) needs a terminal emulator that supports **24-bit truecolor**. Very large terminal windows combined with many metaballs (**-b 20**) and a large radius can make the animation CPU-bound. Colors passed to **-c** and **-k** must be either recognized named colors or six-digit hex values without a leading _#_."],["HISTORY","**lavat** was written by **AngelJumbo** in **C** on top of the **Termbox2** terminal library and published under the **MIT license** on GitHub. It is inspired by web-based JavaScript lava-lamp demos and joins a family of terminal eye-candy tools such as **aafire**, **cmatrix**, and **asciiquarium**."],["INSTALL","```brew: brew install lavat```\n\n```nix: nix profile install nixpkgs#lavat```"],["SEE ALSO","[aafire](/man/aafire)(1), [cacafire](/man/cacafire)(1), [cmatrix](/man/cmatrix)(1), [asciiquarium](/man/asciiquarium)(1), [pipes.sh](/man/pipes.sh)(1)"]],"e":["aafire","cacafire","cmatrix","asciiquarium","pipes.sh"],"x":"lavat"},"lsblk":{"n":"lsblk","t":"List information about block devices","s":[["TAGLINE","List information about block devices"],["TLDR","List all storage devices in a **tree-like format**\n\n```lsblk```\n\nAlso list **empty devices**\n\n```lsblk -a```\n\nPrint the SIZE column in **bytes**\n\n```lsblk -b```\n\nOutput info about **filesystems**\n\n```lsblk -f```\n\nOutput info about **block-device topology**\n\n```lsblk -t```\n\nUse **ASCII characters** for tree formatting\n\n```lsblk -i```\n\nAdd extra columns to output\n\n```lsblk -o NAME,SIZE,FSTYPE,MOUNTPOINT,MODEL```\n\nOutput in **JSON** format\n\n```lsblk -J```\n\nList information about a **specific device**\n\n```lsblk /dev/sda```"],["SYNOPSIS","**lsblk** [_-a_] [_-b_] [_-f_] [_-t_] [_-o columns_] [_device_...]"],["DESCRIPTION","**lsblk** lists information about all available or specified block devices. It reads the sysfs filesystem and udev database to gather information and displays it in a tree-like format showing the relationship between devices and partitions."],["PARAMETERS","**-a, --all**\n> Also list empty devices\n\n**-b, --bytes**\n> Print SIZE column in bytes\n\n**-f, --fs**\n> Output info about filesystems (FSTYPE, LABEL, UUID, MOUNTPOINT)\n\n**-t, --topology**\n> Output block device topology information\n\n**-i, --ascii**\n> Use ASCII characters for tree formatting\n\n**-l, --list**\n> Produce output in list format\n\n**-o, --output columns**\n> Specify which columns to output\n\n**-e, --exclude list**\n> Exclude devices by major device numbers\n\n**-n, --noheadings**\n> Don't print column headings\n\n**-p, --paths**\n> Print full device paths\n\n**-d, --nodeps**\n> Don't print holder devices or slaves (show only top-level devices)\n\n**-J, --json**\n> Output in JSON format\n\n**-S, --scsi**\n> Output SCSI device information\n\n**-r, --raw**\n> Use raw output format"],["AVAILABLE COLUMNS","NAME, KNAME, MAJ:MIN, FSTYPE, MOUNTPOINT, LABEL, UUID, SIZE, MODEL, SERIAL, TYPE, TRAN, RO, RM, HOTPLUG, ROTA"],["CAVEATS","Some information may require root privileges. Mounted filesystems show mount points; unmounted show blank."],["HISTORY","Part of **util-linux** package. Provides a more readable alternative to /proc/partitions and fdisk -l."],["INSTALL","```apt: sudo apt install util-linux```\n\n```dnf: sudo dnf install util-linux```\n\n```pacman: sudo pacman -S util-linux```\n\n```apk: sudo apk add lsblk```\n\n```zypper: sudo zypper install util-linux```\n\n```brew: brew install util-linux```\n\n```nix: nix profile install nixpkgs#util-linux```"],["SEE ALSO","[blkid](/man/blkid)(8), [fdisk](/man/fdisk)(8), [findmnt](/man/findmnt)(8), [df](/man/df)(1), [mount](/man/mount)(8), [parted](/man/parted)(8)"]],"e":["blkid","fdisk","findmnt","df","mount","parted"],"x":"lsblk"},"miller":{"n":"miller","t":"like awk, sed, cut, join, and sort for name-indexed data such as CSV","s":[["TAGLINE","like awk, sed, cut, join, and sort for name-indexed data such as CSV"],["TLDR","**Convert CSV to JSON**\n\n```mlr --csv --ojson cat [file.csv]```\n\n**Filter records**\n\n```mlr --csv filter '$[field] > [10]' [file.csv]```\n\n**Select columns**\n\n```mlr --csv cut -f [name,age] [file.csv]```\n\n**Sort by field**\n\n```mlr --csv sort -f [age] [file.csv]```\n\n**Aggregate statistics**\n\n```mlr --csv stats1 -a sum,mean -f [value] -g [category] [file.csv]```\n\n**Transform field**\n\n```mlr --csv put '$[total] = $[price] * $[quantity]' [file.csv]```"],["SYNOPSIS","**mlr** [_options_] _verb_ [_verb-options_] _files_..."],["PARAMETERS","**--csv**\n> CSV input format.\n\n**--json**\n> JSON input format.\n\n**--ojson**\n> JSON output format.\n\n**--icsv --ojson**\n> CSV in, JSON out.\n\n**cat**\n> Pass through records.\n\n**filter**\n> Select matching records.\n\n**cut**\n> Select fields.\n\n**sort**\n> Sort records.\n\n**put**\n> Add/modify fields.\n\n**stats1**\n> Aggregate statistics."],["DESCRIPTION","**Miller** (mlr) is like awk, sed, cut, join, and sort for name-indexed data such as CSV, TSV, and JSON. It's designed for processing structured text data from the command line.\n\nMiller handles heterogeneous data formats with consistent syntax."],["FORMAT CONVERSION","```bash\nmlr --icsv --ojson cat data.csv\nmlr --ijson --ocsv cat data.json\nmlr --csv --opprint cat data.csv\n```"],["CAVEATS","DSL syntax differs from awk. Field names are case-sensitive. Large files processed streaming."],["HISTORY","Miller was created by **John Kerl** starting in **2015** to provide a unified tool for structured text processing."],["INSTALL","```dnf: sudo dnf install miller```\n\n```pacman: sudo pacman -S miller```\n\n```zypper: sudo zypper install miller```\n\n```brew: brew install miller```\n\n```nix: nix profile install nixpkgs#miller```"],["SEE ALSO","[jq](/man/jq)(1), [awk](/man/awk)(1), [csvkit](/man/csvkit)(1), [xsv](/man/xsv)(1)"]],"e":["jq","awk","csvkit","xsv"],"x":"mlr --csv --ojson cat [file.csv]"},"minio-server":{"n":"minio-server","t":"runs MinIO object storage","s":[["TAGLINE","runs MinIO object storage"],["TLDR","**Start server**\n\n```minio server [/data]```\n\n**Start with console port**\n\n```minio server --console-address \":[9001]\" [/data]```\n\n**Distributed mode**\n\n```minio server [http://server{1...4}/data]```\n\n**Set credentials**\n\n```MINIO_ROOT_USER=[admin] MINIO_ROOT_PASSWORD=[password] minio server [/data]```\n\n**Specify address**\n\n```minio server --address \":[9000]\" [/data]```"],["SYNOPSIS","**minio server** [_options_] _path_"],["PARAMETERS","_PATH_\n> Data directory or endpoints.\n\n**--address** _ADDR_\n> Server address.\n\n**--console-address** _ADDR_\n> Console address.\n\n**--certs-dir** _DIR_\n> TLS certificates.\n\n**--help**\n> Display help information."],["DESCRIPTION","**minio server** runs MinIO object storage. MinIO is S3-compatible storage server.\n\nThe tool provides high-performance object storage. Supports erasure coding and distributed mode."],["CAVEATS","Requires persistent storage. Set secure credentials. Memory intensive for large deployments."],["HISTORY","MinIO was created to provide high-performance, **S3-compatible** object storage that's Kubernetes-native."],["SEE ALSO","[minio-client](/man/minio-client)(1), [mc](/man/mc)(1), [aws](/man/aws)(1)"]],"e":["minio-client","mc","aws"],"x":"minio server [/data]"},"misa":{"n":"misa","t":"Fast-decompress LZ codec CLI for write-once, read-many data","s":[["TAGLINE","Fast-decompress LZ codec CLI for write-once, read-many data"],["TLDR","**Compress** a file (writes `FILE.misa77`)\n\n```misa compress [file]```\n\n**Decompress** a `.misa77` archive\n\n```misa decompress [file.misa77]```\n\n**Compress** at the **fastest-decode** level\n\n```misa compress -l 0 [file]```\n\n**Compress** with the default **better-ratio** level\n\n```misa compress -l 1 [file]```\n\n**Suggest** tuned parameters for a file (writes `FILE.misap`)\n\n```misa suggest [file]```\n\n**Compress** using a previously suggested **params** file\n\n```misa compress --params [file.misap] [file]```\n\n**Force overwrite** and set output path\n\n```misa compress -f -o [out.misa77] [file]```"],["SYNOPSIS","**misa** **compress** | **decompress** | **suggest** [_options_] _FILE_"],["DESCRIPTION","**misa** is the command-line front end for **misa77**, an LZ-based lossless compression codec aimed at write-once, read-many workloads. It prioritizes very high single-threaded decompression throughput and modest ratios (comparable to high-effort LZ4), at the cost of slower compression. Memory use is bounded: at most about 5 MB across compression modes, and effectively none for decompression.\n\nThe CLI works on **single files only** (no directory recursion or pipe/streaming support). Archive archives as `.misa77`; parameter vectors from `suggest` use `.misap`. Compression level **0** favors decode speed; level **1** (default) favors ratio with similar encode throughput. Experimental flags such as **--adaptive**, **--params**, and **--yolo** offer decode-optimized or autotuned modes and must not be combined with each other or with **--level**.\n\nmisa77 requires a little-endian 64-bit system and a C++20 toolchain to build. The stream format may change while the project is pre-1.0; invalid input is treated as undefined behavior and the tool is experimental."],["PARAMETERS","**compress** _FILE_\n> Compress _FILE_ to _FILE_.misa77 (or **-o** path).\n\n**decompress** _FILE.misa77_\n> Decompress a misa77 archive back to the original name (or **-o** path).\n\n**suggest** _FILE_\n> Sample the input and write a tuned params file (_FILE_.misap) for later **--params** use.\n\n**-l** _N_, **--level** _N_\n> Compression level for **compress** (0 = fastest decode, 1 = default better ratio).\n\n**-o** _PATH_\n> Output path.\n\n**-f**\n> Overwrite existing output without prompting.\n\n**--adaptive**\n> Autotune compression for decode speed (best on homogeneous data); experimental; not combinable with **--level**.\n\n**--params** _FILE.misap_\n> Compress with parameters from **misa suggest**; experimental; not combinable with **--level**.\n\n**--yolo**\n> High-effort, decode-optimized experimental mode; not combinable with **--level**.\n\n**--tune** **loose**|**tight**\n> Tradeoff for **--adaptive** / **suggest** (similar to levels 0/1; default loose).\n\n**--sample** _MB_\n> How much input to sample when picking params (default 2 MB)."],["CAVEATS","File-based only: use **tar** (or similar) first for directories or multi-file bundles. Format may change in 0.x releases. The decoder assumes valid misa77 input; corrupted streams are not hardened. Prefer **misa** for cold storage and read-heavy archives where decompression speed matters more than encode time or maximum ratio."],["SEE ALSO","[lz4](/man/lz4)(1), [zstd](/man/zstd)(1), [gzip](/man/gzip)(1), [tar](/man/tar)(1)"],["RESOURCES","```[Source code](https://github.com/welcome-to-the-sunny-side/misa77)```"]],"e":["lz4","zstd","gzip","tar"],"x":"misa compress [file]"},"mkzftree":{"n":"mkzftree","t":"creates compressed filesystem trees","s":[["TAGLINE","creates compressed filesystem trees"],["TLDR","**Compress directory tree**\n\n```mkzftree [input/] [output/]```\n\n**Set compression level** (1-9, default 9)\n\n```mkzftree -z [6] [input/] [output/]```\n\n**Force** compression of every file (even if larger after compression)\n\n```mkzftree -f [input/] [output/]```\n\n**Verbose output**\n\n```mkzftree -v [input/] [output/]```\n\n**Parallelize** compression with N threads\n\n```mkzftree -p [4] [input/] [output/]```\n\n**Uncompress** a zisofs tree back to plain files\n\n```mkzftree -u [input/] [output/]```"],["SYNOPSIS","**mkzftree** [_options_] _input_ _output_"],["PARAMETERS","_INPUT_\n> Source directory (or file with **-F**).\n\n_OUTPUT_\n> Destination directory/file.\n\n**-z**, **--level** _LEVEL_\n> Compression level 1-9 (default: 9). Lower is faster, higher compresses more.\n\n**-f**, **--force**\n> Always compress, even if the result is larger than the original.\n\n**-u**, **--uncompress**\n> Uncompress a previously compressed tree.\n\n**-p**, **--parallelism** _N_\n> Number of parallel compression threads.\n\n**-F**, **--file**\n> Treat INPUT as a single file rather than a directory tree.\n\n**-x**, **--one-filesystem**\n> Do not cross filesystem boundaries; create directory stubs at mount points.\n\n**-X**, **--strict-one-filesystem**\n> Do not cross filesystem boundaries and do not create stubs.\n\n**-l**, **--local**\n> Do not recurse into subdirectories (still creates the directories).\n\n**-s**, **--sloppy**\n> Relax preservation of file modes, times, and ownership.\n\n**-v**, **--verbose**\n> Increase verbosity.\n\n**-q**, **--quiet**\n> Suppress all messages including errors.\n\n**-h**, **--help**\n> Display help information.\n\n**-w**, **--version**\n> Display version information."],["DESCRIPTION","**mkzftree** creates compressed directory trees suitable for use with transparent decompression on ISO 9660 (zisofs) filesystems. Files in the output tree are individually compressed using zlib, and the directory structure is preserved.\n\nThe compressed trees are designed to be used with **mkisofs** (or **genisoimage**) with the `-z` option to create ISO images where files are transparently decompressed at read time on Linux systems with zisofs support."],["CAVEATS","Part of **zisofs-tools**. Transparent decompression requires Linux kernel zisofs support. Only useful for ISO 9660 filesystem creation; not a general-purpose compression tool."],["HISTORY","mkzftree is part of **zisofs-tools** for creating transparent compression on ISO9660 filesystems."],["INSTALL","```apt: sudo apt install genisoimage```\n\n```dnf: sudo dnf install genisoimage```"],["SEE ALSO","[mkisofs](/man/mkisofs)(1), [genisoimage](/man/genisoimage)(1)"]],"e":["mkisofs","genisoimage"],"x":"mkzftree [input/] [output/]"},"mqtt-sn-dump":{"n":"mqtt-sn-dump","t":"Dump MQTT-SN UDP packets on a port","s":[["TAGLINE","Dump MQTT-SN UDP packets on a port"],["TLDR","**Listen** on the default MQTT-SN port\n\n```mqtt-sn-dump -p [1883]```\n\n**Dump all** packet types\n\n```mqtt-sn-dump -p [1883] -a```\n\n**Verbose** dump\n\n```mqtt-sn-dump -p [1883] -v```\n\n**Debug**\n\n```mqtt-sn-dump -p [1883] -d```"],["SYNOPSIS","**mqtt-sn-dump** [*opts*] **-p** *port*"],["DESCRIPTION","**mqtt-sn-dump** is a diagnostic tool from **mqtt-sn-tools** that displays MQTT-SN packets sent to a local UDP port. It is especially useful for observing QoS **-1** publishes from constrained clients that talk MQTT-SN without a full session.\n\nBuild with **make** on POSIX systems. Related tools: **mqtt-sn-pub**, **mqtt-sn-sub**, **mqtt-sn-serial-bridge**."],["PARAMETERS","**-p** *port*\n\n> UDP port to listen on (default **1883**).\n\n**-a**\n\n> Dump all packet types (not only the default subset).\n\n**-v**\n\n> Verbose messages, including topic name when available.\n\n**-d**\n\n> Increase debug level (repeatable)."],["CAVEATS","Needs permission to bind the chosen UDP port. Shows traffic delivered to this host/port only; it is not a full network sniffer. MQTT-SN specific: not for MQTT TCP traffic (**tcpdump** / **mosquitto** tools cover that)."],["SEE ALSO","[mqtt-sn-pub](/man/mqtt-sn-pub)(1), [mqtt-sn-sub](/man/mqtt-sn-sub)(1), [tcpdump](/man/tcpdump)(1)"],["RESOURCES","```[Source code](https://github.com/njh/mqtt-sn-tools)```"]],"e":["mqtt-sn-pub","mqtt-sn-sub","tcpdump"],"x":"mqtt-sn-dump -p [1883]"},"nbmemoblocks":{"n":"nbmemoblocks","t":"terminal memory matching game","s":[["TAGLINE","terminal memory matching game"],["TLDR","**Play memory game**\n\n```nbmemoblocks```\n\n**Start with difficulty**\n\n```nbmemoblocks -d [easy]```"],["SYNOPSIS","**nbmemoblocks** [_options_]"],["PARAMETERS","**-d** _LEVEL_\n> Difficulty level.\n\n**--help**\n> Display help information."],["PREVIEW","```\n [?] [?] [A] [?]\n [?] [B] [?] [?]\n [A] [?] [?] [B]\n [?] [?] [?] [?]\n```"],["DESCRIPTION","**nbmemoblocks** is a terminal memory matching game. Match pairs of blocks.\n\nThe tool trains memory skills. Uses ncurses interface."],["CAVEATS","Terminal-based game. Part of games collection. Keyboard navigation."],["HISTORY","nbmemoblocks is a **terminal implementation** of the classic memory card matching game."],["SEE ALSO","[nbfifteen](/man/nbfifteen)(1), [nbmines](/man/nbmines)(1), [nbsos](/man/nbsos)(1)"]],"e":["nbfifteen","nbmines","nbsos"],"x":"nbmemoblocks"},"ncat":{"n":"ncat","t":"networking utility from the Nmap project for reading, writing","s":[["TAGLINE","networking utility from the Nmap project for reading, writing"],["TLDR","**Listen** on a port and save to file\n\n```ncat -l [port] > [path/to/file]```\n\n**Listen** with multiple connections (keep open)\n\n```ncat -lk [port]```\n\n**Send file** to remote host\n\n```ncat < [path/to/file] [address] [port]```\n\nAccept connections over **SSL**\n\n```ncat --ssl -lk [port]```\n\n**Connect** to SSL server\n\n```ncat --ssl [host] [port]```\n\n**Check connectivity** with timeout\n\n```ncat -w [seconds] -vz [host] [port]```\n\nCreate a **chat server**\n\n```ncat -l [port] --chat```"],["SYNOPSIS","**ncat** [**-l**] [**-k**] [**--ssl**] [**-w** _timeout_] [**-v**] [**-z**] [_host_] [_port_]"],["PARAMETERS","**-l, --listen**\n> Listen for incoming connections\n\n**-k, --keep-open**\n> Accept multiple connections in listen mode\n\n**--ssl**\n> Use SSL/TLS encryption\n\n**--ssl-cert _file_**\n> Specify SSL certificate file\n\n**--ssl-key _file_**\n> Specify SSL private key file\n\n**-w, --wait _seconds_**\n> Connection timeout in seconds\n\n**-v, --verbose**\n> Enable verbose output\n\n**-z**\n> Zero-I/O mode; report connection status only\n\n**-p, --source-port _port_**\n> Specify source port\n\n**-e, --exec _command_**\n> Execute command after connection\n\n**--chat**\n> Enable chat mode for multiple users\n\n**--proxy _host:port_**\n> Connect through proxy\n\n**--proxy-type _type_**\n> Proxy type: http, socks4, socks5\n\n**-4**\n> Use IPv4 only\n\n**-6**\n> Use IPv6 only"],["DESCRIPTION","**ncat** is a networking utility from the Nmap project for reading, writing, redirecting, and encrypting data across networks. It is a reimplementation of netcat with additional features including SSL support, proxy connections, and IPv6.\n\nNcat can operate as a simple TCP/UDP client or server, port scanner, or network relay. It supports connection brokering, access control, and can be used for file transfer, port forwarding, and network debugging."],["CAVEATS","The --exec option can be dangerous and should be used carefully. SSL connections require proper certificate handling for security. Some features may require root privileges for privileged ports."],["HISTORY","**ncat** was developed as part of the **Nmap** project by Fyodor and contributors. It was designed as a modern replacement for the traditional netcat utility with enhanced features and better security options."],["INSTALL","```apt: sudo apt install ncat```\n\n```dnf: sudo dnf install nmap-ncat```\n\n```apk: sudo apk add nmap-ncat```"],["SEE ALSO","[nc](/man/nc)(1), [netcat](/man/netcat)(1), [nmap](/man/nmap)(1), [socat](/man/socat)(1)"]],"e":["nc","netcat","nmap","socat"],"x":"ncat -l [port] > [path/to/file]"},"nix-repl":{"n":"nix-repl","t":"Start an interactive Nix expression evaluator","s":[["TAGLINE","Start an interactive Nix expression evaluator"],["TLDR","**Start the Nix REPL**\n\n```nix repl```\n\n**Load the nixpkgs package set into scope**\n\n```nix repl --expr 'import <nixpkgs> {}'```\n\n**Open a REPL with a flake's outputs in scope**\n\n```nix repl [.#]```\n\n**Load a Nix file's attributes into scope**\n\n```nix repl --file [default.nix]```"],["SYNOPSIS","**nix** **repl** [_options_] [_files_]"],["PARAMETERS","_FILES_\n> Nix files whose top-level attributes are brought into scope.\n\n**--expr** _EXPR_\n> Evaluate the given expression and bring its attributes into scope.\n\n**--file** _PATH_\n> Load attributes from the given Nix file (deprecated positional form still works).\n\n**--arg** _NAME_ _EXPR_\n> Pass the named argument as a Nix expression to the loaded file or flake.\n\n**--argstr** _NAME_ _STRING_\n> Pass the named argument as a string.\n\n**--help**\n> Display help information."],["REPL COMMANDS","**:?**\n> Show the list of available REPL commands.\n\n**:l** _PATH_\n> Load a Nix file and add its attributes to the scope.\n\n**:r**\n> Reload all loaded files.\n\n**:b** _EXPR_\n> Build a derivation and print its output paths.\n\n**:e** _EXPR_\n> Open the location of the given value in `$EDITOR`.\n\n**:t** _EXPR_\n> Show the type of the given expression.\n\n**:q**\n> Exit the REPL (Ctrl-D also works)."],["DESCRIPTION","**nix repl** starts an interactive read-eval-print loop for the Nix expression language. It is useful for exploring package attributes, debugging expressions, and prototyping configuration.\n\nExpressions are evaluated lazily. The REPL offers tab completion for attribute names and accepts colon-prefixed meta commands (see REPL COMMANDS) in addition to ordinary Nix expressions."],["CAVEATS","Part of the experimental `nix` command (enable with `experimental-features = nix-command flakes`). The older standalone `nix-repl` binary is deprecated in favor of `nix repl`. Use `:?` inside the REPL for the full command list."],["HISTORY","`nix repl` provides interactive evaluation for exploring and debugging Nix expressions. It superseded the separate `nix-repl` tool when the unified `nix` command was introduced."],["SEE ALSO","[nix](/man/nix)(1), [nix-eval](/man/nix-eval)(1), [nix-instantiate](/man/nix-instantiate)(1)"]],"e":["nix","nix-eval","nix-instantiate"],"x":"nix repl"},"omshell":{"n":"omshell","t":"provides OMAPI shell interface","s":[["TAGLINE","provides OMAPI shell interface"],["TLDR","**Start OMAPI shell**\n\n```omshell```\n\n**Connect to server**\n\n```omshell\n> server [127.0.0.1]\n> connect```\n\n**Set key authentication**\n\n```omshell\n> key [keyname] [secret]```\n\n**Create lease**\n\n```omshell\n> new lease\n> set ip-address = [192.168.1.100]\n> create```"],["SYNOPSIS","**omshell**"],["PARAMETERS","**server** _ADDRESS_\n> DHCP server address.\n\n**connect**\n> Connect to server.\n\n**key** _NAME_ _SECRET_\n> Set authentication key.\n\n**new** _OBJECT_\n> Create new object.\n\n**open** _OBJECT_\n> Open existing object."],["DESCRIPTION","**omshell** provides OMAPI shell interface. Manages ISC DHCP server.\n\nThe tool interacts with dhcpd via OMAPI. Allows runtime modifications."],["CAVEATS","Requires OMAPI enabled. Part of ISC DHCP. Authentication recommended."],["HISTORY","omshell was created for **runtime DHCP management** via the OMAPI protocol."],["SEE ALSO","[dhcpd](/man/dhcpd)(1), [dhclient](/man/dhclient)(1)"],["INSTALL","```apt: sudo apt install isc-dhcp-server```"]],"e":["dhcpd","dhclient"],"x":"omshell"},"oomctl":{"n":"oomctl","t":"query information about the systemd userspace out-of-memory killer","s":[["TAGLINE","query information about the systemd userspace out-of-memory killer"],["TLDR","Show the current state of the cgroups and system contexts stored by **systemd-oomd**\n\n```oomctl dump```"],["SYNOPSIS","**oomctl** [_OPTIONS_...] _COMMAND_"],["PARAMETERS","**dump**\n> Show the current state of cgroups and system contexts monitored by systemd-oomd\n\n**-h**, **--help**\n> Print help text and exit\n\n**--version**\n> Print version string and exit\n\n**--no-pager**\n> Do not pipe output into a pager"],["DESCRIPTION","**oomctl** may be used to get information about the various contexts read in by the systemd userspace out-of-memory (OOM) killer, **systemd-oomd**. It displays the cgroup contexts that systemd-oomd is monitoring for memory pressure and swap usage.\n\nThe systemd-oomd service monitors cgroups that have ManagedOOMSwap or ManagedOOMMemoryPressure enabled, using PSI (Pressure Stall Information) statistics to determine when memory pressure exceeds configured thresholds. When limits are exceeded, it selects and terminates a cgroup by sending SIGKILL to all its processes."],["CAVEATS","Requires systemd with full unified cgroup hierarchy (cgroups-v2) and memory accounting enabled for monitored units. The kernel must be compiled with PSI support (Linux 4.20+). System should have swap enabled for systemd-oomd to function optimally; without swap, the system may enter a livelocked state before oomd can respond."],["HISTORY","Introduced in **systemd version 247** (2020) as part of the systemd-oomd userspace OOM killer implementation. Provides a more intelligent alternative to the kernel OOM killer by using PSI metrics to proactively manage memory pressure before the system becomes unresponsive."],["INSTALL","```apt: sudo apt install systemd-oomd```"],["SEE ALSO","[systemctl](/man/systemctl)(1), [journalctl](/man/journalctl)(1)"]],"e":["systemctl","journalctl"],"x":"oomctl dump"},"openbox":{"n":"openbox","t":"lightweight, highly configurable window manager","s":[["TAGLINE","lightweight, highly configurable window manager"],["TLDR","**Start Openbox**\n\n```openbox```\n\n**Start as session manager**\n\n```openbox-session```\n\n**Reconfigure without restart**\n\n```openbox --reconfigure```\n\n**Restart Openbox**\n\n```openbox --restart```\n\n**Exit Openbox**\n\n```openbox --exit```\n\n**Replace the currently running window manager**\n\n```openbox --replace```"],["SYNOPSIS","**openbox** [_options_]"],["PARAMETERS","**--reconfigure**\n> Reload configuration.\n\n**--restart**\n> Restart Openbox.\n\n**--replace**\n> Replace running WM.\n\n**--exit**\n> Exit the running Openbox instance.\n\n**--config-file** _file_\n> Specify the path to the configuration file to use.\n\n**--sm-disable**\n> Do not connect to the session manager.\n\n**--startup** _cmd_\n> Run the specified command after startup.\n\n**--debug**\n> Display debugging output.\n\n**--debug-focus**\n> Display debugging output for focus handling.\n\n**--version**\n> Show the version and exit.\n\n**--help**\n> Show command line options summary and exit."],["DESCRIPTION","**Openbox** is a lightweight, highly configurable window manager. It's known for its minimal resource usage, standards compliance, and extensive customization through XML configuration.\n\nOpenbox can run standalone or as the window manager for desktop environments like LXDE."],["CONFIGURATION FILES","```\n~/.config/openbox/\n├── rc.xml        # Main config\n├── menu.xml      # Right-click menu\n├── autostart     # Startup applications\n└── environment   # Environment variables\n```"],["KEY BINDINGS EXAMPLE","```xml\n\n<keybind key=\"A-Tab\">\n  <action name=\"NextWindow\"/>\n</keybind>\n```"],["CAVEATS","No built-in panel or taskbar. Requires manual configuration. Use with panel (tint2, polybar) for full desktop."],["HISTORY","Openbox was originally a fork of Blackbox, rewritten by **Dana Jansens** starting in **2002** to provide a modern, standards-compliant window manager."],["INSTALL","```apt: sudo apt install openbox```\n\n```dnf: sudo dnf install openbox```\n\n```pacman: sudo pacman -S openbox```\n\n```apk: sudo apk add openbox```\n\n```zypper: sudo zypper install openbox```\n\n```nix: nix profile install nixpkgs#openbox```"],["SEE ALSO","[i3](/man/i3)(1), [polybar](/man/polybar)(1), [feh](/man/feh)(1), [nitrogen](/man/nitrogen)(1)"]],"e":["i3","polybar","feh","nitrogen"],"x":"openbox"},"pam_tty_audit":{"n":"pam_tty_audit","t":"logs TTY keystrokes","s":[["TAGLINE","logs TTY keystrokes"],["TLDR","**Enable TTY auditing**\n\n```session required pam_tty_audit.so enable=*```\n\n**Audit specific users**\n\n```session required pam_tty_audit.so enable=admin,root```\n\n**Disable for users**\n\n```session required pam_tty_audit.so disable=service_account```"],["SYNOPSIS","**pam_tty_audit.so** [_options_]"],["PARAMETERS","**enable=**_PATTERN_\n> Enable TTY auditing for users matching the comma-separated pattern (use `*` for all users).\n\n**disable=**_PATTERN_\n> Disable TTY auditing for the matching users; processed alongside `enable=` so order matters.\n\n**open_only**\n> Set the audit flag only for the session opening, not for the whole login session.\n\n**log_passwd**\n> Also log keystrokes entered while the TTY is in non-echo (password) mode. Disabled by default for privacy.\n\n**debug**\n> Log additional information for debugging via `syslog(3)`."],["DESCRIPTION","**pam_tty_audit** is a PAM session module that toggles the per-process TTY input auditing flag (`task->signal->audit_tty`) at session open and restores it on close. When enabled, every keystroke read from a controlling TTY by the affected processes is recorded by the kernel and forwarded to **auditd** as `TTY` records.\n\nIt is commonly placed in `/etc/pam.d/system-auth` (or distribution-specific equivalent) as a `session` rule and used to satisfy compliance requirements (PCI-DSS, STIG) that mandate logging of administrative shell activity."],["CAVEATS","Requires the kernel `CONFIG_AUDIT_TTY` feature and a running **auditd**. Logging keystrokes raises serious privacy concerns and may capture passwords in non-echo mode if `log_passwd` is set. Audit records are written to `/var/log/audit/audit.log`; review with **aureport --tty** or **ausearch -m TTY**."],["HISTORY","pam_tty_audit provides **keystroke auditing** for compliance requirements."],["SEE ALSO","[pam](/man/pam)(8), [auditd](/man/auditd)(8), [aureport](/man/aureport)(8)"]],"e":["pam","auditd","aureport"],"x":"session required pam_tty_audit.so enable=*"},"paseo":{"n":"paseo","t":"Unified CLI for running parallel AI coding agents across providers","s":[["TAGLINE","Unified CLI for running parallel AI coding agents across providers"],["TLDR","**Start the local daemon** that hosts running agents\n\n```paseo daemon start```\n\n**Launch an agent** with a specified provider and prompt\n\n```paseo run --provider [claude/opus-4.6] \"[implement user authentication]\"```\n\n**Launch an agent in its own git worktree** so its edits stay isolated\n\n```paseo run --worktree [feature-branch] --provider [codex] \"[refactor the payment module]\"```\n\n**List currently running agents** managed by the daemon\n\n```paseo ls```\n\n**Stream live output** from a running agent by ID\n\n```paseo attach [agent_id]```\n\n**Send a follow-up instruction** to an active agent\n\n```paseo send [agent_id] \"[also add unit tests]\"```\n\n**Target a remote daemon** instead of the local one\n\n```paseo run --host [host:port] --provider [opencode] \"[review the diff]\"```"],["SYNOPSIS","**paseo** _subcommand_ [_options_]\n\n**paseo** **run** **--provider** _PROVIDER_ [**--worktree** _BRANCH_] [**--host** _HOST_] _prompt_\n\n**paseo** **ls**\n\n**paseo** **attach** _agent_id_\n\n**paseo** **send** _agent_id_ _message_\n\n**paseo** **daemon** **start** | **stop** | **status**"],["DESCRIPTION","**paseo** is a command-line client and local daemon that orchestrates multiple AI coding agents behind a single interface. Each invocation of **paseo run** spawns an agent process under one of the supported providers (Claude Code, Codex, GitHub Copilot, OpenCode, Pi) and registers it with the daemon, which keeps the agent alive across terminal sessions and exposes it for follow-up messages or live attachment.\n\nA typical workflow is to start the daemon once with **paseo daemon start**, fire off several agents in parallel on different prompts or branches, list them with **paseo ls** to see status and IDs, and then **attach** or **send** to interact with whichever one needs supervision. Because each agent can be pinned to its own git worktree, parallel agents work on isolated branches without stomping on each other's edits.\n\nThe daemon model also enables cross-device usage: a daemon running on a workstation can be reached from a laptop by passing **--host**, so long-running agent sessions outlive any single shell or device. Paseo positions itself as a thin coordination layer above the underlying coding-agent CLIs rather than a separate AI: model selection and prompts are handed straight to the chosen provider."],["PARAMETERS","**--provider** _PROVIDER_\n\n> Required for **paseo run**. Selects the underlying coding agent, e.g. **claude/opus-4.6**, **codex**, **copilot**, **opencode**, **pi**. Use the provider's own model identifier where applicable.\n\n**--worktree** _BRANCH_\n\n> Run the agent inside a dedicated git worktree on _BRANCH_, so its file edits stay isolated from other parallel agents.\n\n**--host** _HOST_\n\n> Route the command to a remote Paseo daemon at _HOST_ (e.g. **host:port**) instead of the local one."],["SUBCOMMANDS","**run**\n\n> Start a new agent under the chosen provider with the given prompt.\n\n**ls**\n\n> List running agents with their IDs, providers, and status.\n\n**attach** _id_\n\n> Stream live output from an agent. Detach to leave the agent running.\n\n**send** _id_ _message_\n\n> Send a follow-up message to an agent that is already running.\n\n**daemon start | stop | status**\n\n> Manage the local Paseo daemon that hosts running agents."],["INSTALLATION","```\nnpm install -g @getpaseo/cli\n```\n\nAfter install, run **paseo daemon start** once to bring up the local daemon."],["CAVEATS","**paseo** is a thin wrapper: it does not replace the underlying provider CLIs, and each provider must be installed and authenticated separately. API costs and rate limits apply per provider as usual.\n\nThe daemon keeps agents alive in the background. Forgetting to **paseo ls** or **paseo daemon stop** can leave long-running agent processes (and their token usage) accumulating silently.\n\nWorktrees created with **--worktree** persist on disk after the agent exits. Clean them up with **git worktree remove** when no longer needed."],["HISTORY","**paseo** is a recent project (first public releases in 2026) from the **getpaseo** team, released under the **AGPL-3.0** license. It emerged alongside the broader trend of multi-agent coding workflows, aiming to provide one interface, one history, and one daemon for users who run Claude Code, Codex, Copilot, OpenCode, and Pi side by side."],["INSTALL","```aur: yay -S paseo```"],["SEE ALSO","[claude](/man/claude)(1), [codex](/man/codex)(1), [copilot](/man/copilot)(1), [git-worktree](/man/git-worktree)(1)"]],"e":["claude","codex","copilot","git-worktree"],"x":"paseo daemon start"},"pct-reboot":{"n":"pct-reboot","t":"reboots a Proxmox LXC container","s":[["TAGLINE","reboots a Proxmox LXC container"],["TLDR","**Reboot** a container\n\n```pct reboot 100```\n\nReboot with a **timeout** of 60 seconds\n\n```pct reboot 100 --timeout 60```"],["SYNOPSIS","**pct reboot** _vmid_ [_OPTIONS_]"],["DESCRIPTION","**pct reboot** shuts down and restarts a Proxmox LXC container. The command performs a clean reboot by first requesting the container to shut down gracefully, then starting it again. If the container does not stop within the timeout period, the operation fails."],["PARAMETERS","**vmid**\n> The numeric ID of the container (100 or higher)\n\n**--timeout** _seconds_\n> Wait this many seconds for the shutdown before failing. The default is 60 seconds."],["CAVEATS","The container must be running for a reboot to succeed. If the container has processes that block shutdown, the reboot may time out. Unlike a hard reset, this performs a graceful shutdown first, so unresponsive containers may need to be stopped and started manually instead."],["HISTORY","**pct reboot** is part of the **Proxmox VE** virtualization platform for managing LXC containers."],["SEE ALSO","[pct](/man/pct)(1), [pct-status](/man/pct-status)(1)"]],"e":["pct","pct-status"],"x":"pct reboot 100"},"pii-shield":{"n":"pii-shield","t":"Detect and mask personally identifiable information in text","s":[["TAGLINE","Detect and mask personally identifiable information in text"],["TLDR","**Detect PII** in an inline string\n\n```pii-shield detect \"Contact john@example.com for help\"```\n\n**Mask** PII by **replacing** it with entity-type labels\n\n```pii-shield mask \"My email is john@example.com\" --strategy [replace]```\n\n**Redact** PII (remove the matching span entirely)\n\n```pii-shield mask \"SSN 123-45-6789\" --strategy [redact]```\n\n**Hash** PII to a stable, deterministic value\n\n```pii-shield mask \"Card 4111-1111-1111-1111\" --strategy [hash]```\n\n**Process a file** and write the masked output to another file\n\n```pii-shield file [input.txt] -o [output.txt] --strategy [redact]```"],["SYNOPSIS","**pii-shield** _command_ _input_ [**--strategy** _STRATEGY_] [**-o** _output_]"],["PARAMETERS","**detect** _TEXT_\n> Print the PII entities found in _TEXT_ (entity type, position, score) without modifying the input.\n\n**mask** _TEXT_\n> Return _TEXT_ with detected PII rewritten according to **--strategy**.\n\n**file** _INPUT_\n> Read PII from _INPUT_ file and emit masked output to **stdout** or to the path given with **-o**.\n\n**--strategy** _STRATEGY_\n> How to rewrite each detected entity. One of:\n>\n> **replace** — substitute with the entity label, e.g. `<EMAIL_ADDRESS>`.\n> **redact** — delete the matching span entirely.\n> **mask** — replace each character with `*` (the default).\n> **hash** — replace with a deterministic hash of the original value.\n\n**-o** _FILE_\n> Write masked output to _FILE_ instead of standard output (used with **file**)."],["DESCRIPTION","**pii-shield** is a command-line front-end to a dual-engine PII detection library. Each input is run through **Microsoft Presidio** locally (using spaCy NER models plus regex patterns), and, when configured, also through **Microsoft Foundry / Azure Language Service** in the cloud. Results from both engines are merged so that high-confidence cloud detections complement the local ones, while traffic stays local when no Azure endpoint is configured.\n\nThe library recognises common entity types out of the box, including person names, email addresses, phone numbers, credit-card numbers, social security numbers, IBAN/bank account numbers, IP addresses and URLs. Detected entities are then transformed by one of four masking strategies (**replace**, **redact**, **mask**, **hash**), making the same tool useful both for safely sharing log/data samples and for producing deterministically-anonymised datasets.\n\nThe CLI is intended for one-off operations and pipeline integration; the same engine is also reachable through a Python API and a REST server for embedding in larger applications."],["CONFIGURATION","Configuration is read from a **.env** file (or the corresponding environment variables) in the working directory. Setting **AZURE_FOUNDRY_ENDPOINT** to your Azure Cognitive Services endpoint and authenticating with **az login** enables the dual-engine path. Leaving **AZURE_FOUNDRY_ENDPOINT** unset restricts detection to the local Presidio engine. Optional variables tune model selection, score thresholds, and the entity allow/deny lists."],["CAVEATS","PII detection is best-effort: false negatives (missed PII) are inevitable with NER and regex, and false positives can mangle legitimate text. Always review high-stakes output. The **hash** strategy preserves equality but reveals frequency patterns and is not a substitute for proper anonymisation. Local mode requires the spaCy models to be downloaded; the first invocation may be slow as models are pulled. Cloud mode sends input text to Azure — confirm this is permitted by your data-handling policy before enabling it."],["HISTORY","**pii-shield** is published by Microsoft as part of the AI Build internal-to-open-source pipeline at **github.com/MSFT-AI-BUILD-INTERNAL/pii-shield**. It builds on **Presidio**, Microsoft's open-source data-protection SDK first released in **2019**, by adding cloud co-detection through **Azure Foundry / Azure Language Service** and exposing the result as a thin CLI."]],"e":[],"x":"pii-shield detect \"Contact john@example.com for help\""},"pikman":{"n":"pikman","t":"PikaOS unified package manager","s":[["TAGLINE","PikaOS unified package manager"],["TLDR","**Search** for a package in native repos\n\n```pikman search [package_name]```\n\n**Install** a package from native repos\n\n```pikman install [package]```\n\n**Install** a package from the **AUR** via container\n\n```pikman --aur install [package]```\n\n**Install** a **Flatpak** package\n\n```pikman --flatpak install [package]```\n\n**Update** the list of available packages\n\n```pikman update```\n\n**Upgrade** all system packages\n\n```pikman upgrade```\n\n**Remove** a package\n\n```pikman remove [package]```\n\n**Initialize** the Arch/AUR container for cross-distro support\n\n```pikman --aur init```"],["SYNOPSIS","**pikman** [_global options_] _command_ [_command options_] [_arguments..._]"],["PARAMETERS","**--arch**, **--aur**\n> Install and manage Arch Linux packages including AUR\n\n**--fedora**, **--dnf**\n> Install and manage Fedora packages\n\n**--alpine**, **--apk**\n> Install and manage Alpine Linux packages\n\n**--flatpak**, **--fl**\n> Install and manage Flatpak packages\n\n**--name** _value_\n> Specify a managed container name\n\n**--help**, **-h**\n> Display help information\n\n**--version**, **-v**\n> Show version number"],["DESCRIPTION","**pikman** is the official package manager for **PikaOS**, a Debian-based Linux distribution. It provides a unified interface for managing packages across multiple packaging systems. On its native PikaOS it wraps **APT** for native package management, but can also install packages from **Arch/AUR**, **Fedora**, and **Alpine** repositories using **APX** with Podman containers under the hood. It also supports **Flatpak** packages. Applications installed from containerized distributions automatically appear in the host's application launcher.\n\npikman handles privilege escalation internally and must **not** be run with sudo. It refuses to run as root."],["COMMANDS","**install**, **i**\n> Install the specified package(s)\n\n**remove**, **r**\n> Remove an installed package\n\n**purge**\n> Fully purge a package including config files\n\n**search**, **s**\n> Search for a package\n\n**show**\n> Show details for a package\n\n**list**, **l**\n> List installed packages (supports **--upgradable** and **--installed** filters)\n\n**update**\n> Update the list of available packages\n\n**upgrade**\n> Upgrade the system by installing/upgrading available packages\n\n**autoremove**\n> Remove all unused packages\n\n**clean**, **cl**\n> Clean the package manager cache\n\n**init**\n> Initialize a managed container for cross-distro support\n\n**enter**\n> Enter the container instance for a given package manager\n\n**export**\n> Export a program's desktop entry from the container\n\n**unexport**\n> Remove a program's desktop entry\n\n**log**\n> Show package manager logs\n\n**run**\n> Run a command inside a managed container\n\n**upgrades**\n> List the available upgrades\n\n**help**, **h**\n> Show a list of commands or help for a specific command"],["CAVEATS","Pikman is designed specifically for **PikaOS** and is not a general-purpose tool for other distributions. Before installing packages from other distributions (Arch, Fedora, Alpine), you must first initialize the respective container with **pikman --aur init** (or **--fedora init**, etc.). For Flatpak packages, use **pikman --flatpak update** instead of **upgrade** to match Flatpak conventions."],["HISTORY","Pikman was created as part of the **PikaOS** project, a Debian-based distribution focused on gaming and desktop use. It is written in **Go** and was originally hosted on GitHub. The repository was archived in **November 2024** with development continuing on PikaOS's own Gitea instance. There is also a GUI counterpart called **Pikman Update Manager** for graphical package management."],["SEE ALSO","[apt](/man/apt)(8), [flatpak](/man/flatpak)(1), [pacman](/man/pacman)(8), [dnf](/man/dnf)(8), [podman](/man/podman)(1)"]],"e":["apt","flatpak","pacman","dnf","podman"],"x":"pikman search [package_name]"},"plasma-apply-desktoptheme":{"n":"plasma-apply-desktoptheme","t":"Apply KDE Plasma desktop themes","s":[["TAGLINE","Apply KDE Plasma desktop themes"],["TLDR","**Apply** a Plasma desktop theme by name\n\n```plasma-apply-desktoptheme [default]```\n\n**List** available desktop themes\n\n```plasma-apply-desktoptheme --list-themes```\n\nShow **help**\n\n```plasma-apply-desktoptheme --help```"],["SYNOPSIS","**plasma-apply-desktoptheme** [**--list-themes**] [_theme_name_]"],["PARAMETERS","**--list-themes**\n> List all available desktop themes\n\n**theme_name**\n> Name of desktop theme to apply\n\n**--help**\n> Display help information"],["DESCRIPTION","**plasma-apply-desktoptheme** changes the Plasma desktop theme from the command line. Desktop themes control the appearance of panels, widgets, and other Plasma-specific UI elements.\n\nThe tool allows automated theme switching, useful for day/night themes or scripted desktop customization."],["CAVEATS","Requires running Plasma session. Desktop themes are separate from color schemes and icon themes. Changes apply immediately to Plasma components."],["HISTORY","**plasma-apply-desktoptheme** is part of **KDE Plasma Workspace**, enabling command-line theming control as part of KDE's comprehensive appearance customization tools."],["INSTALL","```apt: sudo apt install plasma-workspace```\n\n```dnf: sudo dnf install plasma-workspace```\n\n```pacman: sudo pacman -S plasma-workspace```\n\n```apk: sudo apk add plasma-workspace```"],["SEE ALSO","[plasma-apply-colorscheme](/man/plasma-apply-colorscheme)(1), [plasma-apply-lookandfeel](/man/plasma-apply-lookandfeel)(1)"]],"e":["plasma-apply-colorscheme","plasma-apply-lookandfeel"],"x":"plasma-apply-desktoptheme [default]"},"pop":{"n":"pop","t":"Remove top value from stack","s":[["TAGLINE","Remove top value from stack"],["TLDR","**Pop value from stack (Forth)**\n\n```pop```\n\n**Pop in dc calculator**\n\n```echo \"1 2 3 R p\" | dc```"],["SYNOPSIS","**pop** [_options_]"],["PARAMETERS","Stack operation command."],["DESCRIPTION","**pop** removes and discards the top value from a stack. It is a fundamental operation in stack-based programming languages and calculators like Forth and dc.\n\nIn shell contexts, the related command **popd** performs a similar operation on the shell's directory stack. The pop operation is the complement of push."],["CAVEATS","Context-dependent. Part of stack-based languages."],["HISTORY","pop is a fundamental **stack operation** in computer science."],["INSTALL","```zypper: sudo zypper install pop```\n\n```brew: brew install pop```\n\n```nix: nix profile install nixpkgs#pop```"],["SEE ALSO","[dc](/man/dc)(1), [forth](/man/forth)(1), [pushd](/man/pushd)(1)"]],"e":["dc","forth","pushd"],"x":"pop"},"pygettext":{"n":"pygettext","t":"Extract translatable strings from Python code","s":[["TAGLINE","Extract translatable strings from Python code"],["TLDR","**Extract translatable strings**\n\n```pygettext [script.py]```\n\n**Specify output file**\n\n```pygettext -o [messages.pot] [script.py]```\n\n**Extract from directory**\n\n```pygettext -d [domain] [src/]```\n\n**Include docstrings**\n\n```pygettext -D [script.py]```"],["SYNOPSIS","**pygettext** [_options_] _files_..."],["PARAMETERS","**-o**, **--output** _file_\n> Output file name.\n\n**-d**, **--domain** _name_\n> Text domain name.\n\n**-p**, **--output-dir** _dir_\n> Output directory.\n\n**-D**, **--docstrings**\n> Extract docstrings.\n\n**-k** _keyword_\n> Additional keyword to look for.\n\n**-n**, **--add-location**\n> Include file:line comments."],["DESCRIPTION","**pygettext** extracts translatable strings from Python source code. It searches for strings marked with _(), gettext(), and similar functions, creating a POT template file.\n\nPart of Python standard library."],["EXAMPLES","```bash"],["Basic extraction","pygettext -o messages.pot *.py"],["From directory","pygettext -d myapp -p locale src/*.py"],["Include docstrings","pygettext -D -o docs.pot module.py"],["Custom keywords","pygettext -k N_ -k ngettext:1,2 app.py\n```"],["MARKING STRINGS","```python\nfrom gettext import gettext as _\n\nmessage = _(\"This will be translated\")\nprint(_(\"Hello, world!\"))\n```"],["OUTPUT FORMAT (POT)","```\n#: script.py:10\nmsgid \"Hello, world!\"\nmsgstr \"\"\n```"],["CAVEATS","Part of Python. For more features, use xgettext or Babel. Output is template, translators create .po files."],["HISTORY","pygettext is part of **Python** tools for internationalization, implementing gettext extraction for Python."],["SEE ALSO","[xgettext](/man/xgettext)(1), [msgfmt](/man/msgfmt)(1), [gettext](/man/gettext)(1), [babel](/man/babel)(1)"]],"e":["xgettext","msgfmt","gettext","babel"],"x":"pygettext [script.py]"},"qm-set":{"n":"qm-set","t":"Modify Proxmox VM configuration settings","s":[["TAGLINE","Modify Proxmox VM configuration settings"],["TLDR","Set VM **name**\n\n```qm set 100 --name vm_name```\n\nEnable **autostart**\n\n```qm set 100 --autostart 1```\n\nSet **CPU cores**\n\n```qm set 100 --cores 4```\n\nSet **memory** (MB)\n\n```qm set 100 --memory 8192```\n\nAdd **network** device\n\n```qm set 100 --net0 virtio,bridge=vmbr0```\n\n**Delete** device\n\n```qm set 100 --delete device_name```\n\n**GPU passthrough**\n\n```qm set 100 --hostpci0 0000:00:02,x-vga=1 --bios ovmf```"],["SYNOPSIS","**qm set** [_OPTIONS_] _vmid_"],["DESCRIPTION","**qm set** modifies the configuration of a QEMU/KVM virtual machine in Proxmox VE. Changes may require a VM restart to take effect. This command can configure CPU, memory, storage, network, and hardware passthrough options."],["PARAMETERS","**vmid**\n> The numeric ID of the virtual machine\n\n**--name** _string_\n> Set the VM name displayed in the GUI\n\n**--autostart** _boolean_\n> Start VM automatically on host boot\n\n**--cores** _number_\n> Number of CPU cores\n\n**--memory** _megabytes_\n> Amount of RAM in MB\n\n**--net**_N_ _config_\n> Network device configuration (N=0-31)\n\n**--scsi**_N_ _config_\n> SCSI disk configuration\n\n**--hostpci**_N_ _config_\n> PCI device passthrough configuration\n\n**--delete** _list_\n> Comma-separated list of settings to delete"],["CAVEATS","Some changes require stopping the VM first. Hardware passthrough requires IOMMU support. Changes that affect running VMs may be marked as pending until restart."],["HISTORY","**qm set** is part of the **Proxmox VE** virtualization platform for managing QEMU/KVM virtual machines."],["INSTALL","```dnf: sudo dnf install qm```"],["SEE ALSO","[qm](/man/qm)(1), [qm-config](/man/qm-config)(1), [qm-pending](/man/qm-pending)(1)"]],"e":["qm","qm-config","qm-pending"],"x":"qm set 100 --name vm_name"},"qmv":{"n":"qmv","t":"rename files by editing their names in a text editor","s":[["TAGLINE","rename files by editing their names in a text editor"],["TLDR","**Rename files** listed in an editor (dual-column, default)\n\n```qmv [files]```\n\n**Rename everything** in the current directory\n\n```qmv *```\n\n**Pick a specific editor** for this run\n\n```qmv -e [vim] [files]```\n\n**Use single-column format** (source and destination on alternating lines)\n\n```qmv -f sc [files]```\n\n**Use destination-only format**\n\n```qmv -f do [files]```\n\n**Dry run**: show what would happen without renaming\n\n```qmv --dummy [files]```\n\n**Verbose output**\n\n```qmv -v [files]```"],["SYNOPSIS","**qmv** [_options_] [_files_...]"],["PARAMETERS","**-f**, **--format=**_FORMAT_\n> Edit format to use: `dc` (dual-column, default), `sc` (single-column), or `do` (destination-only).\n\n**-o**, **--options=**_OPTIONS_\n> Pass comma-separated options to the selected edit format (e.g. `swap`, `blank`, `autowidth`). Use `--options=help` for a list.\n\n**-e**, **--editor=**_PROGRAM_\n> Text editor to open the file list in. Falls back to `$VISUAL`, `$EDITOR`, then a built-in default.\n\n**--command=**_COMMAND_\n> Use _COMMAND_ to perform renames instead of `mv`.\n\n**--ls=**_PROGRAM_\n> Path to the `ls` program used to list directory contents.\n\n**-i**, **--interactive**\n> Start in command mode rather than invoking the editor immediately.\n\n**-v**, **--verbose**\n> Print each rename as it is performed.\n\n**--dummy**\n> Do everything as usual except the actual renames (dry run).\n\n**--help**\n> Show option summary.\n\n**--version**\n> Show version information."],["FORMATS","**dc** — Dual-column (default): source on the left, destination on the right. Only edit the right side.\n**sc** — Single-column: alternating lines of source and destination.\n**do** — Destination-only: one destination per line; relies on file order matching the source list."],["DESCRIPTION","**qmv** (quick move) opens a list of filenames in a text editor so you can batch-rename by editing the destination column. When the editor exits, qmv compares the edited file with the original and applies each change as a rename. This lets you use familiar editor features — search and replace, macros, multi-cursor — for bulk renames.\n\nqmv detects potential conflicts (cycles, collisions) and resolves them via intermediate renames. If a destination exists, you are prompted before overwriting."],["CAVEATS","Part of the **renameutils** package. Editor must be text-based. Do not edit the source column in the default `dc` format — only the destination side. Renames inside directories not listed will not be applied."],["HISTORY","**qmv** is part of the **renameutils** package created by **Oskar Liljeblad** and released under the GPL. The companion tool **qcp** performs the same workflow for copying files."],["INSTALL","```apt: sudo apt install renameutils```\n\n```dnf: sudo dnf install renameutils```\n\n```pacman: sudo pacman -S renameutils```\n\n```zypper: sudo zypper install renameutils```\n\n```brew: brew install renameutils```\n\n```nix: nix profile install nixpkgs#renameutils```"],["SEE ALSO","[qcp](/man/qcp)(1), [rename](/man/rename)(1), [mmv](/man/mmv)(1), [vidir](/man/vidir)(1), [mv](/man/mv)(1)"]],"e":["qcp","rename","mmv","vidir","mv"],"x":"qmv [files]"},"rbw":{"n":"rbw","t":"Unofficial Bitwarden CLI password manager","s":[["TAGLINE","Unofficial Bitwarden CLI password manager"],["TLDR","**Login to vault**\n\n```rbw login```\n\n**Unlock vault**\n\n```rbw unlock```\n\n**Get password**\n\n```rbw get [entry-name]```\n\n**Get full entry (username, password, notes)**\n\n```rbw get --full [entry-name]```\n\n**Get a specific field**\n\n```rbw get --field username [entry-name]```\n\n**Get TOTP code**\n\n```rbw code [entry-name]```\n\n**List entries**\n\n```rbw list```\n\n**Search entries**\n\n```rbw search [pattern]```\n\n**Sync vault**\n\n```rbw sync```\n\n**Add entry**\n\n```rbw add [entry-name]```\n\n**Generate password and store it**\n\n```rbw generate [entry-name] [username]```"],["SYNOPSIS","**rbw** _command_ [_options_] [_args_]"],["PARAMETERS","**config**\n> Get or set configuration options.\n\n**register**\n> Register this device with the Bitwarden server.\n\n**login**\n> Log in to the Bitwarden server.\n\n**unlock**\n> Unlock the local vault database.\n\n**lock**\n> Lock the vault database.\n\n**sync**\n> Update the local copy of the Bitwarden database.\n\n**get** _NAME_\n> Display the password for a given entry.\n\n**search** _TERM_\n> Search for entries in the vault.\n\n**code** _NAME_\n> Display the TOTP authenticator code for a given entry.\n\n**list**\n> List all entries.\n\n**add** _NAME_\n> Add a new entry.\n\n**edit** _NAME_\n> Modify an existing entry.\n\n**remove** _NAME_\n> Delete an entry.\n\n**generate** _NAME_ _USER_\n> Generate a new password and store it.\n\n**history** _NAME_\n> View password history for an entry.\n\n**stop-agent**\n> Terminate the background agent.\n\n**--full**\n> Output username, password, and notes.\n\n**--field** _FIELD_\n> Output a specific field.\n\n**--folder** _FOLDER_\n> Restrict to a specific folder."],["DESCRIPTION","**rbw** is an unofficial command-line Bitwarden client designed for Unix-style workflows, outputting passwords directly to stdout for easy integration with scripts, dmenu, rofi, and other tools. It communicates with Bitwarden servers through a background agent daemon (**rbw-agent**) that caches the decrypted vault locally for fast retrieval.\n\nThe **get** command retrieves passwords by entry name, while the **--field** flag accesses specific fields like usernames, TOTP codes, or custom notes. The **generate** command creates random passwords and stores them directly in the vault. Vault synchronization with **rbw sync** keeps the local cache current with the server."],["CONFIGURATION","**~/.config/rbw/config.json**\n> Client configuration specifying the Bitwarden server URL, email address, lock timeout, and pinentry program for master password entry.\n\n**rbw-agent**\n> Background daemon that maintains the decrypted vault in memory. Started automatically on first rbw command and locks after the configured timeout."],["CAVEATS","Unofficial client. Some Bitwarden features not supported. Requires rbw-agent daemon."],["HISTORY","**rbw** was created by **Jesse Luehrs** as a command-line Bitwarden client optimized for Unix workflows. It uses a background agent (similar to ssh-agent or gpg-agent) to hold decryption keys in memory, emphasizing scriptability and speed over feature completeness."],["INSTALL","```dnf: sudo dnf install rbw```\n\n```pacman: sudo pacman -S rbw```\n\n```apk: sudo apk add rbw```\n\n```brew: brew install rbw```\n\n```nix: nix profile install nixpkgs#rbw```"],["SEE ALSO","[pass](/man/pass)(1), [bw](/man/bw)(1), [gopass](/man/gopass)(1)"]],"e":["pass","bw","gopass"],"x":"rbw login"},"regctl":{"n":"regctl","t":"Manage container registry images and tags","s":[["TAGLINE","Manage container registry images and tags"],["TLDR","**List tags for an image**\n\n```regctl tag ls [registry/image]```\n\n**Inspect an image** without pulling\n\n```regctl image inspect [registry/image:tag]```\n\n**Copy an image** between registries\n\n```regctl image copy [source/image:tag] [dest/image:tag]```\n\n**Get image digest**\n\n```regctl image digest [registry/image:tag]```\n\n**Delete a tag** from registry\n\n```regctl tag rm [registry/image:tag]```\n\n**Export an image to OCI layout**\n\n```regctl image export [registry/image:tag] [output.tar]```\n\n**List repositories** in a registry\n\n```regctl repo ls [registry]```\n\n**Check Docker Hub rate limits**\n\n```regctl registry ratelimit docker.io```"],["SYNOPSIS","**regctl** _command_ [_subcommand_] [_options_] [_arguments_]"],["PARAMETERS","**image inspect** _ref_\n> Display image configuration and metadata\n\n**image copy** _src_ _dest_\n> Copy image between registries (efficient layer reuse)\n\n**image digest** _ref_\n> Get image digest\n\n**image export** _ref_ _file_\n> Export image to tar file (OCI or Docker format)\n\n**image import** _ref_ _file_\n> Import image from tar file\n\n**tag ls** _repository_\n> List tags in a repository\n\n**tag rm** _ref_\n> Delete a tag from registry\n\n**repo ls** _registry_\n> List repositories in a registry\n\n**manifest get** _ref_\n> Get raw manifest content\n\n**manifest put** _ref_\n> Upload manifest to registry\n\n**blob get** _ref_\n> Download blob content\n\n**registry login** _registry_\n> Authenticate with a registry\n\n**registry ratelimit** _registry_\n> Check rate limit status for a registry\n\n**--platform** _os/arch_\n> Filter by platform (e.g., linux/amd64, linux/arm64)\n\n**--format** _template_\n> Output format (Go template)\n\n**-v**, **--verbosity** _level_\n> Set logging verbosity"],["DESCRIPTION","**regctl** is a CLI tool for interacting with OCI-compliant container registries without requiring a container runtime or privileged access. It provides efficient image operations by working directly with registry APIs.\n\nThe tool excels at copying images between registries, transferring only layers that don't exist at the destination. This makes it efficient for mirroring, promoting images between environments, or synchronizing across air-gapped systems.\n\nRegctl supports multi-platform images, OCI artifacts, and can work with OCI Layout directories as local registry equivalents. It automatically imports credentials from Docker CLI configuration.\n\nCommon workflows include inspecting remote images, copying between registries, managing tags, checking rate limits, and exporting/importing images for offline transfer."],["CONFIGURATION","**~/.regctl/config.json**\n> Main configuration file storing registry credentials, default settings, and registry-specific options.\n\n**~/.docker/config.json**\n> Docker CLI credential store, automatically imported by regctl for registry authentication."],["CAVEATS","Delete operations require registries to support the delete API, which some (like Docker Hub for free accounts) may restrict.\n\nAuthentication is inherited from Docker CLI config by default. Use **regctl registry login** for separate credential management.\n\nMulti-platform images require **--platform** to select a specific architecture when operations cannot handle manifests lists (like some exports).\n\nRate limits apply to Docker Hub and other registries. Use **regctl registry ratelimit** to check current status before bulk operations."],["INSTALL","```nix: nix profile install nixpkgs#regctl```"],["SEE ALSO","[docker](/man/docker)(1), [skopeo](/man/skopeo)(1), [crane](/man/crane)(1), [podman](/man/podman)(1)"]],"e":["docker","skopeo","crane","podman"],"x":"regctl tag ls [registry/image]"},"rhash":{"n":"rhash","t":"Calculate and verify file checksums","s":[["TAGLINE","Calculate and verify file checksums"],["TLDR","**Calculate SHA256 hash**\n\n```rhash --sha256 [file]```\n\n**Calculate multiple hashes**\n\n```rhash --md5 --sha1 --sha256 [file]```\n\n**Calculate all supported hashes**\n\n```rhash --all [file]```\n\n**Verify hash from file**\n\n```rhash -c [hashfile.sha256]```\n\n**Create hash file**\n\n```rhash --sha256 [file] -o [file.sha256]```\n\n**Hash all files recursively**\n\n```rhash -r --sha256 [directory]```\n\n**Output in BSD format**\n\n```rhash --bsd --sha256 [file]```\n\n**Calculate CRC32**\n\n```rhash --crc32 [file]```"],["SYNOPSIS","**rhash** [_--algorithm_] [_-c_] [_-r_] [_-o file_] [_options_] _files_"],["PARAMETERS","**-C**, **--crc32**\n> Calculate CRC32 checksum.\n\n**-M**, **--md5**\n> Calculate MD5 hash.\n\n**-H**, **--sha1**\n> Calculate SHA1 hash.\n\n**--sha256**\n> Calculate SHA-256 hash.\n\n**--sha512**\n> Calculate SHA-512 hash.\n\n**--sha3-256**\n> Calculate SHA3-256 hash.\n\n**--blake2b**\n> Calculate BLAKE2b hash.\n\n**-a**, **--all**\n> Calculate all supported hashes.\n\n**--list-hashes**\n> Display names of supported hash functions.\n\n**-c**, **--check**\n> Verify hashes from file.\n\n**-u**, **--update** _FILE_\n> Add new digests to existing hash file.\n\n**-k**, **--check-embedded**\n> Verify files by CRC32 embedded in filenames.\n\n**-B**, **--benchmark**\n> Run benchmark for selected hash algorithm(s).\n\n**-r**, **--recursive**\n> Process directories recursively.\n\n**--follow**\n> Follow symbolic links when processing recursively.\n\n**-m**, **--message** _TEXT_\n> Calculate hash of a text message instead of a file.\n\n**-o**, **--output** _FILE_\n> Output to file.\n\n**--bsd**\n> Use BSD output format.\n\n**--sfv**\n> Use SFV output format (default).\n\n**--simple**\n> Use simple format (hash followed by filename).\n\n**-g**, **--magnet**\n> Output magnet link.\n\n**-e**, **--embed-crc**\n> Rename file with embedded CRC32 in filename.\n\n**-v**, **--verbose**\n> Show more detailed output.\n\n**-P**, **--percents**\n> Show progress percentage during processing.\n\n**--speed**\n> Display processing speed.\n\n**-p** _FMT_, **--printf** _FMT_\n> Custom output format using format directives.\n\n**--template** _FILE_\n> Read output format from file.\n\n**-b**, **--base64**\n> Output hash in Base64 encoding.\n\n**--hex**\n> Output hash in hexadecimal (default).\n\n**--uppercase**\n> Output hash in uppercase hexadecimal."],["DESCRIPTION","**rhash** (Recursive Hasher) calculates and verifies message digests and checksums for files. It supports numerous hash algorithms including CRC32, MD5, SHA family, SHA3, BLAKE2, BLAKE3, Tiger, TTH, AICH, ed2k, Whirlpool, GOST, and RIPEMD-160.\n\nMultiple hashes can be calculated simultaneously in a single pass, efficient for creating comprehensive hash manifests. The default output format is SFV with CRC32; this can be changed with **--bsd**, **--simple**, **--magnet**, or **--printf**.\n\nVerification mode (-c) reads hash files and checks files against stored values. It reports mismatches and missing files. Update mode modifies existing hash files with new entries.\n\nMagnet link output creates links usable with BitTorrent clients. The link includes file hash, size, and optionally file name.\n\nThe --embed-crc option renames files to include CRC32 in the filename (common for anime/video files). This enables at-a-glance integrity checking.\n\nCustom output formats via --printf allow integration with other tools. Templates use tokens for filename, hash, size, etc."],["CAVEATS","Large files take time to hash. Some algorithms (MD5, SHA1) are cryptographically weak. Not all algorithms supported on all platforms. Hash file formats vary between tools. CRC32 is not collision-resistant."],["HISTORY","**rhash** was created by **Aleksey Kravchenko** around **2005** as a fast, comprehensive hash calculation tool. Written in C, it emphasizes performance and supports more algorithms than most similar tools. The project continues development with regular updates for new hash functions."],["INSTALL","```apt: sudo apt install rhash```\n\n```dnf: sudo dnf install rhash```\n\n```pacman: sudo pacman -S rhash```\n\n```apk: sudo apk add rhash```\n\n```zypper: sudo zypper install rhash```\n\n```brew: brew install rhash```\n\n```nix: nix profile install nixpkgs#rhash```"],["SEE ALSO","[sha256sum](/man/sha256sum)(1), [sha512sum](/man/sha512sum)(1), [sha1sum](/man/sha1sum)(1), [md5sum](/man/md5sum)(1), [shasum](/man/shasum)(1), [cksum](/man/cksum)(1), [b3sum](/man/b3sum)(1)"]],"e":["sha256sum","sha512sum","sha1sum","md5sum","shasum","cksum","b3sum"],"x":"rhash --sha256 [file]"},"runnburn":{"n":"runnburn","t":"Memory-aware GGUF language model inference CLI","s":[["TAGLINE","Memory-aware GGUF language model inference CLI"],["TLDR","**Run** a GGUF model with a one-shot prompt\n\n```runNburn [path/to/model.gguf] \"[prompt]\"```\n\n**Start** interactive chat (omit the prompt)\n\n```runNburn [path/to/model.gguf]```\n\n**Chat** with multi-turn history and sampling options\n\n```runNburn chat --system \"[prompt]\" --max-tokens [n] [path/to/model.gguf]```\n\n**Serve** an OpenAI-compatible HTTP API\n\n```runNburn serve --host 127.0.0.1 --port 8000 --ram-budget 16GiB [path/to/model.gguf]```\n\n**Cap** host working-set memory\n\n```runNburn --ram-budget 16GiB [path/to/model.gguf] \"[prompt]\"```"],["SYNOPSIS","**runNburn** [_options_] _model.gguf_ [_prompt_]\n\n**runNburn** **chat** [_options_] _model.gguf_\n\n**runNburn** **serve** [_options_] _model.gguf_"],["DESCRIPTION","**runNburn** (package/crate name **runNburn**; product binary commonly **runNburn**) is a pre-1.0 Rust inference runtime for quantized GGUF models that are larger than available fast memory. Weights stay file-backed where possible, host residency is bounded by detected or explicit RAM budgets, and optional CUDA or Metal paths accelerate supported operators.\n\nThe same product path covers one-shot generation, interactive chat, and a local OpenAI-compatible HTTP server (**chat/completions**, **responses**, models listing). Architecture-aware support includes Llama/Phi, Gemma, Qwen dense/hybrid/MoE, and other families; recognition does not guarantee every community GGUF variant is fully accelerated.\n\nCPU is the default backend. Build with Cargo feature flags (**cpu**, **cuda**, **metal**, experimental **vulkan**) depending on hardware. Without **--ram-budget**, the engine reserves about one quarter of physical RAM for the OS and uses the remainder as a working-set budget."],["PARAMETERS","**--ram-budget** _size_\n\n> Cap engine-owned host residency (e.g. **16GiB**, **32GB**). Binary and decimal suffixes are accepted. Direct CLI options must appear before the GGUF path.\n\n**chat** / **serve**\n\n> Subcommands for multi-turn REPL and HTTP serving. See **runNburn chat --help** and **runNburn serve --help** for sampling, cache, and bind options.\n\n**serve** options commonly include **--host**, **--port**, **--model-name**, **--response-cache-budget**, and **--api-key-file**. Non-loopback binds require an API key."],["CAVEATS","Pre-1.0; APIs and backend coverage change. **--ram-budget** is not an OS RSS hard limit. The OpenAI surface is partial compatibility, not a full OpenAI API. Vulkan and some mobile paths are experimental. The binary name uses mixed case (**runNburn**) as shipped by the project."],["SEE ALSO","[llama-cli](/man/llama-cli)(1), [ollama](/man/ollama)(1), [curl](/man/curl)(1)"],["RESOURCES","```[Source code](https://github.com/coderredlab/runNburn)```"]],"e":["llama-cli","ollama","curl"],"x":"runNburn [path/to/model.gguf] \"[prompt]\""},"rustup-man":{"n":"rustup-man","t":"View man pages for Rust tools","s":[["TAGLINE","View man pages for Rust tools"],["TLDR","**Open man page**\n\n```rustup man [command]```\n\n**Open cargo man page**\n\n```rustup man cargo```\n\n**Open rustc man page**\n\n```rustup man rustc```"],["SYNOPSIS","**rustup** **man** [_options_] _command_"],["PARAMETERS","**--toolchain** _name_\n> Use specific toolchain."],["DESCRIPTION","**rustup man** opens manual pages for Rust tools. Provides access to documentation for cargo, rustc, and other tools. Requires man pages component."],["SEE ALSO","[rustup](/man/rustup)(1), [rustup-doc](/man/rustup-doc)(1)"]],"e":["rustup","rustup-doc"],"x":"rustup man [command]"},"scapy":{"n":"scapy","t":"Interactive packet manipulation and network tool","s":[["TAGLINE","Interactive packet manipulation and network tool"],["TLDR","**Start the interactive scapy** shell\n\n```scapy```\n\n**Start scapy** with increased verbosity\n\n```scapy -d```\n\n**Start scapy** with a custom startup file\n\n```scapy -c [path/to/startup.py]```\n\n**Start scapy** without running startup files\n\n```scapy -C -P```\n\n**Start scapy** in header-less mode with reduced verbosity\n\n```scapy -H```\n\n**Send a ping packet** and receive the reply\n\n```sr1(IP(dst=\"[8.8.8.8]\")/ICMP())```\n\n**Perform a TCP SYN scan** on a port\n\n```sr1(IP(dst=\"[target]\")/TCP(dport=[80], flags=\"S\"))```"],["SYNOPSIS","**scapy** [**-h**] [**-H**] [**-d**] [**-p** _prestart_file_] [**-P**] [**-c** _startup_file_] [**-C**]"],["PARAMETERS","**-h**\n> Display usage information\n\n**-H**\n> Header-less mode; also reduces verbosity\n\n**-d**\n> Increase log verbosity; can be used multiple times for more detail\n\n**-s** _file_\n> Use file to save/load session values (variables, functions, instances)\n\n**-p** _prestart_file_\n> Use specified file instead of ~/.config/scapy/prestart.py as pre-startup file\n\n**-P**\n> Do not run the prestart file\n\n**-c** _startup_file_\n> Use specified file instead of ~/.config/scapy/startup.py as startup file\n\n**-C**\n> Do not run the startup file"],["DESCRIPTION","**Scapy** is a Python-based interactive packet manipulation program and library. It can forge, decode, send, and capture packets for a wide range of network protocols. Scapy uses the Python interpreter as its command interface, allowing full use of Python syntax including variables, loops, and functions.\n\nThe tool can replace or complement utilities like **hping**, **nmap**, **arpspoof**, **arping**, **tcpdump**, and **tshark**. Unlike traditional tools that reduce responses to simple states like \"open\" or \"closed,\" Scapy provides access to the complete packet data for detailed analysis.\n\nCommon functions include **send()** for layer 3 packets, **sendp()** for layer 2, **sr()** for send-receive, **sr1()** for single response, **sniff()** for packet capture, and **wrpcap()**/**rdpcap()** for PCAP file operations."],["CAVEATS","Scapy requires root privileges for most operations involving raw sockets. It may miss packets under heavy network load. Session saving is limited by Python's marshaling capabilities, so lambda functions and generators cannot be preserved. Interface aliases may not report correct source IPs."],["HISTORY","**Scapy** was created by **Philippe Biondi** in **2003** as a flexible alternative to rigid network tools. It gained popularity in the security community for penetration testing and network research. The project transitioned to Python 3 support and continues to be actively maintained by the security community on GitHub."],["INSTALL","```pacman: sudo pacman -S scapy```\n\n```apk: sudo apk add scapy```\n\n```zypper: sudo zypper install scapy```"],["SEE ALSO","[tcpdump](/man/tcpdump)(1), [wireshark](/man/wireshark)(1), [nmap](/man/nmap)(1), [hping3](/man/hping3)(1), [tshark](/man/tshark)(1)"]],"e":["tcpdump","wireshark","nmap","hping3","tshark"],"x":"scapy"},"scrub":{"n":"scrub","t":"Securely overwrite files, block devices, or free disk space","s":[["TAGLINE","Securely overwrite files, block devices, or free disk space"],["TLDR","Securely **overwrite a file** using the default pattern\n\n```scrub [path/to/file]```\n\nSecurely overwrite **multiple files**\n\n```scrub [path/to/file1] [path/to/file2]```\n\nSecurely overwrite a **block device** or partition\n\n```sudo scrub [/dev/sdX]```\n\nOverwrite a file with a **single-pass random** pattern\n\n```scrub -p random [path/to/file]```\n\nOverwrite a file using the **DoD 5220.22-M** pattern\n\n```scrub -p dod [path/to/file]```\n\nOverwrite a file using the **Gutmann** pattern\n\n```scrub -p gutmann [path/to/file]```\n\n**Remove** a file after successfully overwriting it\n\n```scrub -r [path/to/file]```\n\nSecurely overwrite **free space** under a directory\n\n```scrub -X [path/to/directory]```"],["SYNOPSIS","**scrub** [_OPTIONS_] _special-file_ ...\n\n**scrub** [_OPTIONS_] _file_ ...\n\n**scrub** **-X** [_OPTIONS_] _directory_"],["PARAMETERS","**-v**, **--version**\n> Print scrub version and exit\n\n**-r**, **--remove**\n> Remove the file after scrubbing\n\n**-p**, **--pattern** _PATTERN_\n> Select overwrite patterns (default **nnsa**). See patterns below\n\n**-b**, **--blocksize** _size_\n> I/O block size in bytes (**K**/**M**/**G** suffixes allowed). Default: 4M\n\n**-f**, **--force**\n> Scrub even if the target already has a scrub signature\n\n**-S**, **--no-signature**\n> Do not write a scrub signature after completion\n\n**-X**, **--freespace**\n> Fill _directory_ until the filesystem is full, then scrub those files\n\n**-D**, **--dirent** _newname_\n> After scrubbing, overwrite the directory entry name and rename to _newname_\n\n**-s**, **--device-size** _size_\n> Override detected device size in bytes (**K**/**M**/**G** allowed)\n\n**-L**, **--no-link**\n> If target is a symlink, do not scrub the link target (still remove if **-r**)\n\n**-R**, **--no-hwrand**\n> Do not use a hardware RNG even if available\n\n**-t**, **--no-threads**\n> Do not generate random data in parallel with I/O\n\n**-n**, **--dry-run**\n> Perform checks and reporting without writing to targets\n\n**-h**, **--help**\n> Print a summary of options"],["DESCRIPTION","**scrub** iteratively writes patterns on files or disk devices to make data recovery more difficult. It operates in three modes:\n\n1. **Block/character device** — the entire device is scrubbed (most effective).\n2. **Regular file** — only the file data (and optionally its directory entry name) is overwritten; size is rounded up to the last filesystem block.\n3. **Free space** (**-X**) — creates files under an existing directory until the filesystem is full, then scrubs those files.\n\nDefault pattern **nnsa** follows NNSA Policy Letter NAP-14.1-C (two random passes, then 0x00 with verify), which is reasonable for modern PRML/EPRML disks. Other standards include **dod**, **bsi**, **gutmann**, **schneier**, **usarmy**, single-pass **random**/**fillzero**/**fillff**, and **custom=**_string_."],["PATTERNS","**nnsa** — 4-pass NNSA: random(x2), 0x00, verify (default)\n\n**dod** — 4-pass DoD 5220.22-M: random, 0x00, 0xff, verify\n\n**bsi** — 9-pass German BSI method\n\n**gutmann** — 35-pass Gutmann sequence\n\n**schneier** — 7-pass Schneier: 0x00, 0xff, random(x5)\n\n**pfitzner7** / **pfitzner33** — 7 or 33 random passes\n\n**usarmy** — US Army AR380-19: 0x00, 0xff, random\n\n**fillzero** / **fillff** / **random** / **random2** — short fixed or random passes\n\n**old** / **fastold** — legacy multi-pass sequences\n\n**custom=**_string_ — one pass with C-style escapes (\\xnn, \\nnn)"],["CAVEATS","Scrubbing through a filesystem is limited by the OS and filesystem type. Journaled, log-structured, copy-on-write, versioned, and network filesystems may retain prior data elsewhere; prefer scrubbing the raw device when possible.\n\n**scrub** cannot reach blocks remapped/spared by the disk controller. For ATA drives, firmware security erase via **hdparm** may be more thorough. Physical destruction remains the only strong guarantee against lab recovery.\n\nFree-space scrubbing (**-X**) only helps for blocks the filesystem reassigns to new files. Always verify device size before scrubbing raw devices — wrong targets destroy data irreversibly."],["HISTORY","**scrub** originated at Lawrence Livermore National Laboratory (UCRL-CODE-2003-006) as a GPL disk/file overwrite utility implementing published media sanitization methods (NNSA, DoD, Gutmann, and others)."],["INSTALL","```apt: sudo apt install scrub```\n\n```dnf: sudo dnf install scrub```\n\n```aur: yay -S scrub```\n\n```zypper: sudo zypper install scrub```\n\n```brew: brew install scrub```\n\n```nix: nix profile install nixpkgs#scrub```"],["SEE ALSO","[shred](/man/shred)(1), [wipe](/man/wipe)(1), [wipefs](/man/wipefs)(8), [hdparm](/man/hdparm)(8), [dd](/man/dd)(1)"],["RESOURCES","```[Source code](https://github.com/chaos/scrub)```\n\n```[Documentation](https://manned.org/scrub)```"]],"e":["shred","wipe","wipefs","hdparm","dd"],"x":"scrub [path/to/file]"},"snowsql":{"n":"snowsql","t":"Snowflake cloud data warehouse CLI client","s":[["TAGLINE","Snowflake cloud data warehouse CLI client"],["TLDR","**Connect to Snowflake**\n\n```snowsql -a [account] -u [username]```\n\n**Execute query**\n\n```snowsql -a [account] -u [user] -q \"[SELECT * FROM table]\"```\n\n**Run SQL file**\n\n```snowsql -a [account] -u [user] -f [script.sql]```\n\n**Specify database**\n\n```snowsql -a [account] -u [user] -d [database]```\n\n**Specify warehouse**\n\n```snowsql -a [account] -u [user] -w [warehouse]```\n\n**Specify role**\n\n```snowsql -a [account] -u [user] -r [role]```\n\n**Output in CSV format**\n\n```snowsql -a [account] -u [user] -o output_format=[csv]```\n\n**Use a named connection from config**\n\n```snowsql -c [connection_name]```"],["SYNOPSIS","**snowsql** [_-a account_] [_-u user_] [_-d database_] [_options_]"],["PARAMETERS","**-a**, **--accountname** _ACCOUNT_\n> Snowflake account.\n\n**-u**, **--username** _USER_\n> Username.\n\n**-d**, **--dbname** _DB_\n> Database.\n\n**-w**, **--warehouse** _WH_\n> Warehouse.\n\n**-s**, **--schemaname** _SCHEMA_\n> Schema.\n\n**-q**, **--query** _SQL_\n> Execute query.\n\n**-f**, **--filename** _FILE_\n> Run SQL file.\n\n**-r**, **--rolename** _ROLE_\n> Default role to use.\n\n**-c**, **--connection** _NAME_\n> Use a named connection from config file.\n\n**-D**, **--variable** _KEY=VALUE_\n> Define a variable (referenced as &KEY in queries).\n\n**-o** _OPTION=VALUE_\n> Set option (e.g., output_format=csv, quiet=true, friendly=false).\n\n**--version**\n> Display SnowSQL version.\n\n**--upgrade**\n> Force upgrade to latest SnowSQL version.\n\n**--config** _FILE_\n> Path to configuration file."],["DESCRIPTION","**snowsql** is the official command-line client for Snowflake, a cloud-based data warehouse platform. It provides an interactive SQL shell for querying, managing databases, and performing administrative tasks on Snowflake accounts.\n\nConnections require a Snowflake account name and credentials, which can be provided on the command line, entered interactively, or stored in a configuration file. The client supports specifying database, schema, warehouse, and role context at connection time.\n\nSQL queries can be executed interactively in the shell, passed directly via **-q**, or run from script files with **-f** for batch automation. Output can be formatted as tables, CSV, TSV, or other formats using the **-o output_format** option, making it useful for data export and pipeline integration."],["CONFIGURATION","**~/.snowsql/config**\n> Connection profiles, default settings, and stored credentials for SnowSQL sessions.\n\n**SNOWSQL_ACCOUNT**\n> Environment variable specifying the default Snowflake account name.\n\n**SNOWSQL_PWD**\n> Environment variable specifying the default password.\n\n**SNOWSQL_ROLE**\n> Environment variable specifying the default role."],["CAVEATS","Requires Snowflake account. Large results may be slow. MFA may be required."],["HISTORY","**SnowSQL** is the official CLI for **Snowflake** cloud data warehouse. It provides terminal-based access to Snowflake's SQL engine."],["INSTALL","```nix: nix profile install nixpkgs#snowsql```"],["SEE ALSO","[psql](/man/psql)(1), [mysql](/man/mysql)(1), [bq](/man/bq)(1)"]],"e":["psql","mysql","bq"],"x":"snowsql -a [account] -u [username]"},"st4topgm":{"n":"st4topgm","t":"Convert SBIG ST-4 autoguider images to PGM","s":[["TAGLINE","Convert SBIG ST-4 autoguider images to PGM"],["TLDR","**Convert** an SBIG ST-4 image file to PGM\n\n```st4topgm [input.st4] > [output.pgm]```\n\n**Read from stdin** and convert to PGM\n\n```cat [input.st4] | st4topgm > [output.pgm]```"],["SYNOPSIS","**st4topgm** [_st4file_]"],["DESCRIPTION","**st4topgm** reads an image in the native format used by the Santa Barbara Instrument Group (SBIG) ST-4 astronomical CCD autoguider cameras and writes the data to standard output as a PGM (Portable Graymap) image.\n\nIt is part of the Netpbm toolkit. If no input file is specified, it reads from standard input. The program does not provide any custom options; it honors the common libnetpbm options (e.g., `-quiet`, `-plain`)."],["CAVEATS","ST-4 files are a legacy astronomical imaging format. Use **pgmtost4** to perform the reverse conversion."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pgmtost4](/man/pgmtost4)(1), [sbigtopgm](/man/sbigtopgm)(1)"]],"e":["pgmtost4","sbigtopgm"],"x":"st4topgm [input.st4] > [output.pgm]"},"svnserve":{"n":"svnserve","t":"Lightweight Subversion repository server","s":[["TAGLINE","Lightweight Subversion repository server"],["TLDR","**Start as daemon** serving repositories\n\n```svnserve -d -r [/path/to/repos]```\n\n**Start in foreground** for debugging\n\n```svnserve -d --foreground -r [/path/to/repos]```\n\n**Listen on specific port**\n\n```svnserve -d -r [/path/to/repos] --listen-port [3690]```\n\n**Listen on specific interface**\n\n```svnserve -d -r [/path/to/repos] --listen-host [192.168.1.1]```\n\n**Run in inetd mode**\n\n```svnserve -i```\n\n**Run in tunnel mode** for SSH\n\n```svnserve -t```\n\n**Use custom configuration file**\n\n```svnserve -d -r [/path/to/repos] --config-file [/etc/svnserve.conf]```"],["SYNOPSIS","**svnserve** [_options_]"],["PARAMETERS","**-d**, **--daemon**\n> Run as daemon, accepting TCP connections on svn port (3690).\n\n**-i**, **--inetd**\n> Run in inetd mode using stdin/stdout.\n\n**-t**, **--tunnel**\n> Run in tunnel mode for SSH access, pre-authenticated.\n\n**-X**, **--listen-once**\n> Accept one connection, serve it, and exit.\n\n**-r**, **--root** _path_\n> Set virtual root for served repositories.\n\n**--listen-port** _port_\n> Listen on specified port (default 3690).\n\n**--listen-host** _host_\n> Listen on specified interface or hostname.\n\n**--foreground**\n> Stay in foreground (with -d), for debugging.\n\n**--tunnel-user** _name_\n> Override authenticated username in tunnel mode.\n\n**--config-file** _file_\n> Use specified configuration file.\n\n**--pid-file** _file_\n> Write process ID to file.\n\n**--log-file** _file_\n> Write logs to specified file.\n\n**--threads**\n> Use threads instead of forking.\n\n**--min-threads** _n_\n> Minimum number of threads in pool.\n\n**--max-threads** _n_\n> Maximum number of threads in pool.\n\n**-h**, **--help**\n> Display usage information.\n\n**--version**\n> Display version information."],["DESCRIPTION","**svnserve** is a lightweight server for Subversion repositories using Subversion's custom network protocol. It provides access to repositories via the svn:// and svn+ssh:// URL schemes.\n\nThe server supports multiple operating modes: as a standalone daemon listening on TCP port 3690, as an inetd/xinetd service, or as a tunnel process spawned by SSH. The tunnel mode allows secure access without exposing the svn port.\n\nThe -r option sets a virtual root, restricting client access to repositories under that path and simplifying URLs. Authentication can be configured per-repository or globally using a shared configuration file.\n\nFor high-traffic environments, svnserve supports thread pools instead of fork-per-connection."],["CAVEATS","Uses plain text authentication by default unless tunneled through SSH or configured for SASL. No built-in SSL support; use SSH tunneling or svn+ssh:// for encryption. Access control is less granular than Apache-based serving. Firewall must allow port 3690 for daemon mode."],["HISTORY","**svnserve** was included in Subversion from early versions as a lightweight alternative to Apache-based repository serving. It provides faster setup and lower overhead for smaller deployments. The svn:// protocol was designed for simplicity while svn+ssh:// leverages SSH for authentication and encryption."],["INSTALL","```apt: sudo apt install subversion```\n\n```dnf: sudo dnf install subversion```\n\n```pacman: sudo pacman -S subversion```\n\n```apk: sudo apk add subversion```\n\n```zypper: sudo zypper install subversion```\n\n```brew: brew install subversion```\n\n```nix: nix profile install nixpkgs#subversion```"],["SEE ALSO","[svnadmin](/man/svnadmin)(1), [svn](/man/svn)(1), [sshd](/man/sshd)(8)"]],"e":["svnadmin","svn","sshd"],"x":"svnserve -d -r [/path/to/repos]"},"tako-vm":{"n":"tako-vm","t":"Run untrusted code in isolated Docker containers","s":[["TAGLINE","Run untrusted code in isolated Docker containers"],["TLDR","**Pull the executor image** and verify Docker\n\n```tako-vm setup```\n\n**Start the REST API server**\n\n```tako-vm server```\n\n**Start the server on a custom port**\n\n```tako-vm server --port [8000]```\n\n**Start a local PostgreSQL and the server together** for development\n\n```tako-vm dev up --with-server```\n\n**Show the current configuration**\n\n```tako-vm config```\n\n**Check server health**\n\n```tako-vm status```"],["SYNOPSIS","**tako-vm** [**--config** _file_] _command_ [_options_]"],["DESCRIPTION","**tako-vm** is a self-hosted platform that runs untrusted or AI-generated **Python** in isolated **Docker** containers, with optional **gVisor** sandboxing and seccomp filtering. It bundles job queuing, execution history, retry logic and replay, so there is no need to assemble Redis, Celery or custom infrastructure separately.\n\nEach job runs in its own container with **network isolation by default** and an optional per-job-type allowlist. The server exposes a REST API: code is submitted to an /execute endpoint and run inside the sandbox. PostgreSQL backs job state and is started automatically via Docker when the server runs."],["COMMANDS","**setup**\n> Pull the executor Docker image and verify the Docker installation.\n\n**server** [**--port** _port_]\n> Start the REST API server (auto-starts PostgreSQL via Docker).\n\n**dev up** [**--with-server**]\n> Start a local PostgreSQL for development, optionally with the API server.\n\n**dev status**\n> Check the local PostgreSQL status.\n\n**dev down**\n> Stop the local PostgreSQL.\n\n**config** [**--json**]\n> Show the current configuration, optionally as JSON.\n\n**validate** [_file_]\n> Validate the current or a specified configuration file.\n\n**status**\n> Check server health.\n\n**version**\n> Display version information."],["CAVEAT","Requires a working **Docker** installation; run **tako-vm setup** first. Isolation rests on Docker plus optional gVisor and seccomp, so keep the executor image and Docker up to date when running untrusted code."],["HISTORY","**tako-vm** is an open-source execution platform written in **Python** by developer **las7**, installed with **pip install \"tako-vm[server]\"**. It is distributed under the **Apache-2.0** license."],["SEE ALSO","[docker](/man/docker)(1), [podman](/man/podman)(1), [firejail](/man/firejail)(1), [bwrap](/man/bwrap)(1)"]],"e":["docker","podman","firejail","bwrap"],"x":"tako-vm setup"},"tgutil":{"n":"tgutil","t":"Edit and delete Telegram messages from the command line","s":[["TAGLINE","Edit and delete Telegram messages from the command line"],["TLDR","**Delete all messages matching text in a chat**\n\n```tgutil -n [session] -u [chat_id] -m deleteall --text \"[text]\"```\n\n**Edit a specific message**\n\n```tgutil -n [session] -u [me] -m edit --text \"[old text]\" --newtext \"[new text]\"```\n\n**Edit all matching messages**\n\n```tgutil -n [session] -u [chat_id] -m editall --text \"[old text]\" --newtext \"[new text]\"```\n\n**Delete a single matching message in saved messages**\n\n```tgutil -n [session] -u [me] -m delete --text \"[text]\"```"],["SYNOPSIS","**tgutil** **-n** _session_ **-u** _chat_ **-m** _mode_ **--text** _text_ [**--newtext** _text_]"],["PARAMETERS","**-n**, **--name** _SESSION_\n> Session name (authenticated Telegram account).\n\n**-u**, **--username** _CHAT_\n> Target chat ID or username. Use `me` for Saved Messages.\n\n**-m**, **--mode** _MODE_\n> Operation mode: `edit`, `editall`, `delete`, `deleteall`.\n\n**--text** _TEXT_\n> Text to search for in messages.\n\n**--newtext** _TEXT_\n> Replacement text (required for `edit` and `editall` modes)."],["DESCRIPTION","**tgutil** is a CLI tool for editing and deleting messages in Telegram chats. It is part of the **telegram-cloud** Python package, which provides a suite of tools for interacting with Telegram from the command line.\n\nThe tool operates in four modes: `edit` modifies the first matching message, `editall` modifies all matching messages, `delete` removes the first matching message, and `deleteall` removes all matching messages containing the specified text.\n\nInstall via pip: `pip install telegram-cloud`."],["CAVEATS","Requires prior authentication via **tglogin** with a Telegram API id and hash from https://my.telegram.org. Part of the **telegram-cloud** package. Can only edit or delete your own messages."],["SEE ALSO","[tgsend](/man/tgsend)(1), [tgcloud](/man/tgcloud)(1), [tginfo](/man/tginfo)(1)"]],"e":["tgsend","tgcloud","tginfo"],"x":"tgutil -n [session] -u [chat_id] -m deleteall --text \"[text]\""},"ticker":{"n":"ticker","t":"Terminal stock and crypto price tracker","s":[["TAGLINE","Terminal stock and crypto price tracker"],["TLDR","**Watch stock prices** in real time\n\n```ticker -w [AAPL,MSFT,GOOG]```\n\n**Track stocks** with position tracking from a config file\n\n```ticker```\n\n**Show stock prices** in JSON format\n\n```ticker -w [AAPL,MSFT] --format json```\n\n**Show stock prices** in CSV format\n\n```ticker -w [AAPL,MSFT] --format csv```\n\n**Watch cryptocurrency prices**\n\n```ticker -w [BTC-USD,ETH-USD]```\n\n**Use a custom config file**\n\n```ticker -c [path/to/config.yaml]```"],["SYNOPSIS","**ticker** [**-c** _config_] [**-w** _symbols_] [**--format** _format_] [**--show-fundamentals**] [**--show-tags**] [**--show-separator**]"],["PARAMETERS","**-w**, **--watchlist** _symbols_\n> Comma-separated list of stock or crypto symbols to watch.\n\n**-c**, **--config** _file_\n> Path to the configuration file. Default is ~/.ticker.yaml.\n\n**--format** _format_\n> Output format: default, json, or csv.\n\n**--show-fundamentals**\n> Show fundamental data like P/E ratio and market cap.\n\n**--show-tags**\n> Display tags associated with symbols.\n\n**--show-separator**\n> Show a separator between symbols in output.\n\n**--show-holdings**\n> Display holdings and position values.\n\n**--sort** _field_\n> Sort output by field (e.g., change, changePercent).\n\n**--proxy** _url_\n> Proxy URL for API requests.\n\n**-i**, **--interval** _seconds_\n> Refresh interval in seconds."],["DESCRIPTION","**ticker** is a terminal-based stock and cryptocurrency tracker that displays live price updates and position tracking. Written in Go, it provides real-time quotes from Yahoo Finance and CoinMarketCap, supporting stocks, ETFs, mutual funds, and cryptocurrencies.\n\nThe tool can track both current prices and your portfolio positions with support for multiple cost basis lots. It displays pre-market and post-market prices when available, making it useful for monitoring investments outside regular trading hours.\n\nConfiguration is typically done through a YAML file (~/.ticker.yaml) where you can define watchlists, positions with cost basis, and display preferences. The config file supports grouping symbols with tags and setting up multiple portfolios."],["CAVEATS","Market data from Yahoo Finance may have a slight lag (up to 30 seconds) due to intermediary systems. Real-time data is available for NYSE and NASDAQ, but other exchanges may have additional delays. Cryptocurrency prices are sourced from CoinMarketCap via Yahoo Finance."],["HISTORY","**ticker** was created by **Ani Channarasappa** and first released in **2020**. It was developed to provide a lightweight, terminal-based alternative to web-based stock tracking tools, appealing to developers and terminal enthusiasts who prefer command-line workflows."],["INSTALL","```apt: sudo apt install ticker```\n\n```nix: nix profile install nixpkgs#ticker```"],["SEE ALSO","[curl](/man/curl)(1), [jq](/man/jq)(1)"]],"e":["curl","jq"],"x":"ticker -w [AAPL,MSFT,GOOG]"},"tickrs":{"n":"tickrs","t":"Realtime stock ticker data in your terminal","s":[["TAGLINE","Realtime stock ticker data in your terminal"],["TLDR","**Start with specified ticker symbols**\n\n```tickrs -s [AAPL],[MSFT]```\n\n**Candle chart with 1-month timeframe**\n\n```tickrs -s [AAPL] -c candle -t 1M```\n\n**Show pre/post market hours**\n\n```tickrs -s [TSLA] -p```\n\n**Start in summary mode with volume graphs**\n\n```tickrs -s [AAPL],[GOOGL] --summary --show-volumes```\n\n**Show x-axis labels with custom update interval**\n\n```tickrs -s [MSFT] -x -i [5]```"],["SYNOPSIS","**tickrs** [_flags_] [_options_]"],["PARAMETERS","**-s, --symbols** _SYMBOLS_\n> Comma-separated list of ticker symbols to start with.\n\n**-c, --chart-type** _TYPE_\n> Chart type: line (default), candle, or kagi.\n\n**-t, --time-frame** _FRAME_\n> Time frame: 1D (default), 1W, 1M, 3M, 6M, 1Y, 5Y.\n\n**-i, --update-interval** _SECONDS_\n> Interval to update data from API (default: 1).\n\n**-p, --enable-pre-post**\n> Enable pre/post market hours for graphs.\n\n**--show-volumes**\n> Show volumes graph.\n\n**-x, --show-x-labels**\n> Show x-axis labels.\n\n**--summary**\n> Start in summary mode.\n\n**--hide-prev-close**\n> Hide previous close line on 1D chart.\n\n**--trunc-pre**\n> Truncate pre-market graphing to 30 minutes before open."],["DESCRIPTION","**tickrs** displays realtime ticker data sourced from Yahoo! Finance directly in your terminal. It supports multiple chart types, various time frames, pre/post market hours, volume graphs, and portfolio tracking with profit/loss display."],["HISTORY","**tickrs** was created by **tarkah** and is written in **Rust**."],["INSTALL","```pacman: sudo pacman -S tickrs```\n\n```nix: nix profile install nixpkgs#tickrs```"],["SEE ALSO","[ticker](/man/ticker)(1), [cointop](/man/cointop)(1)"]],"e":["ticker","cointop"],"x":"tickrs -s [AAPL],[MSFT]"},"update-ca-certificates":{"n":"update-ca-certificates","t":"Update SSL certificate authority bundle","s":[["TAGLINE","Update SSL certificate authority bundle"],["TLDR","**Update** the CA certificates bundle\n\n```sudo update-ca-certificates```\n\n**Perform a fresh update**, removing existing symlinks first\n\n```sudo update-ca-certificates --fresh```\n\n**Update with verbose** output showing details\n\n```sudo update-ca-certificates --verbose```"],["SYNOPSIS","**update-ca-certificates** [_options_]"],["PARAMETERS","**-h**, **--help**\n> Show summary of options.\n\n**-v**, **--verbose**\n> Be verbose. Display c_rehash results.\n\n**-f**, **--fresh**\n> Fresh update. Remove all symlinks in **/etc/ssl/certs** before regenerating them."],["DESCRIPTION","**update-ca-certificates** updates the directory **/etc/ssl/certs** to hold SSL certificates and generates **ca-certificates.crt**, a concatenated single-file list of trusted certificate authorities. It is used to maintain the system-wide set of CA certificates that applications rely on for verifying TLS/SSL connections.\n\nThe program reads **/etc/ca-certificates.conf**, where each line specifies a pathname of a CA certificate under **/usr/share/ca-certificates** that should be trusted. Lines beginning with **#** are comments and are ignored. Lines beginning with **!** deselect a certificate, causing it to be deactivated.\n\nCertificates must have a **.crt** extension to be included. All certificates with a **.crt** extension found under **/usr/local/share/ca-certificates** are also included as implicitly trusted, making this the recommended location for adding custom CA certificates.\n\nAfter updating, the program executes hooks in **/etc/ca-certificates/update.d** via **run-parts**, passing lists of added (prefixed with **+**) and removed (prefixed with **-**) certificates."],["CONFIGURATION","To **add a custom CA certificate**, place the **.crt** file in **/usr/local/share/ca-certificates/** and run **update-ca-certificates**. The certificate will be automatically trusted.\n\nTo **disable a certificate** shipped with the system, edit **/etc/ca-certificates.conf** and prefix the corresponding line with **!**, then run **update-ca-certificates**.\n\n```"],["Example /etc/ca-certificates.conf entries","mozilla/DigiCert_Global_Root_G2.crt\n!mozilla/Untrusted_CA.crt\n```"],["CAVEATS","This command is primarily available on **Debian** and **Ubuntu** based distributions. On **Red Hat** and **Fedora** systems, use **update-ca-trust** instead. The command typically requires **root privileges** to modify system certificate directories. Changes only take effect for applications started after the update; running services may need to be restarted to pick up the new certificate bundle."],["HISTORY","**update-ca-certificates** was written for the **Debian** project as part of the **ca-certificates** package, which provides a curated collection of CA root certificates from the **Mozilla NSS** project. The ca-certificates package has been a core component of Debian since the early **2000s**, ensuring that system-wide TLS verification works out of the box. The tool has since been adopted by all Debian derivatives including **Ubuntu** and **Linux Mint**."],["INSTALL","```dnf: sudo dnf install ca-certificates```\n\n```pacman: sudo pacman -S ca-certificates```\n\n```apk: sudo apk add ca-certificates```\n\n```zypper: sudo zypper install ca-certificates```\n\n```brew: brew install ca-certificates```"],["SEE ALSO","[openssl](/man/openssl)(1)"]],"e":["openssl"],"x":"sudo update-ca-certificates"},"vagrant-snapshot":{"n":"vagrant-snapshot","t":"Save and restore Vagrant VM states","s":[["TAGLINE","Save and restore Vagrant VM states"],["TLDR","**Save snapshot**\n\n```vagrant snapshot save [name]```\n\n**Restore snapshot**\n\n```vagrant snapshot restore [name]```\n\n**List snapshots**\n\n```vagrant snapshot list```\n\n**Delete snapshot**\n\n```vagrant snapshot delete [name]```\n\n**Quick save**\n\n```vagrant snapshot push```"],["SYNOPSIS","**vagrant** **snapshot** _command_ [_options_]"],["PARAMETERS","**save** _name_\n> Create named snapshot.\n\n**restore** _name_\n> Restore to snapshot.\n\n**list**\n> List snapshots.\n\n**delete** _name_\n> Remove snapshot.\n\n**push**\n> Quick save (unnamed).\n\n**pop**\n> Restore and delete last push."],["DESCRIPTION","**vagrant snapshot** manages VM snapshots. Save and restore VM states for testing and development. Supports named snapshots and quick push/pop operations."],["SEE ALSO","[vagrant](/man/vagrant)(1)"]],"e":["vagrant"],"x":"vagrant snapshot save [name]"},"vgchange":{"n":"vgchange","t":"Modify LVM volume group attributes","s":[["TAGLINE","Modify LVM volume group attributes"],["TLDR","**Activate** all volume groups\n\n```sudo vgchange -a y```\n\n**Deactivate** a specific volume group\n\n```sudo vgchange -a n [volume_group]```\n\n**Refresh** metadata for all active logical volumes in a volume group\n\n```sudo vgchange --refresh [volume_group]```\n\n**Set** the maximum number of logical volumes for a volume group\n\n```sudo vgchange -l [128] [volume_group]```\n\n**Make** a volume group not resizeable\n\n```sudo vgchange -x n [volume_group]```"],["SYNOPSIS","**vgchange** [_options_] [_volume_group_...]"],["PARAMETERS","**-a, --activate y|n|ay**\n> Activate or deactivate logical volumes (y=yes, n=no, ay=autoactivation)\n\n**-x, --resizeable y|n**\n> Enable or disable adding/removing physical volumes\n\n**-l, --logicalvolume _Number_**\n> Set the maximum number of logical volumes allowed in the volume group\n\n**-p, --maxphysicalvolumes _Number_**\n> Set the maximum number of physical volumes in the volume group\n\n**-s, --physicalextentsize _Size_**\n> Change the physical extent size on physical volumes\n\n**--refresh**\n> Reload metadata for all active logical volumes\n\n**--monitor y|n**\n> Start or stop monitoring logical volumes with dmeventd\n\n**--poll y|n**\n> Control background transformation of logical volumes\n\n**--sysinit**\n> Indicate that the command is being invoked during early system initialization\n\n**-u, --uuid**\n> Generate new random UUID for the volume group\n\n**--addtag _tag_**\n> Add a tag to the volume group\n\n**--deltag _tag_**\n> Delete a tag from the volume group\n\n**-f, --force**\n> Override various checks, confirmations, and protections\n\n**--alloc contiguous|cling|normal|anywhere|inherit**\n> Set the physical extent allocation policy"],["DESCRIPTION","**vgchange** modifies the attributes of LVM volume groups. The most common use is activating or deactivating logical volumes within a volume group, which is necessary before the volumes can be mounted.\n\nVolume group activation makes the logical volumes available to the system. Deactivation is required before physical volumes can be removed or the system can be safely shut down."],["CAVEATS","Requires root privileges. Deactivating a volume group with mounted or active filesystems will fail. Activating with missing physical volumes requires the **--activationmode partial** or **-P** flag. Use **vgscan** to discover volume groups. Part of the LVM2 package."],["INSTALL","```apt: sudo apt install lvm2```\n\n```dnf: sudo dnf install lvm2```\n\n```pacman: sudo pacman -S lvm2```\n\n```apk: sudo apk add lvm2```\n\n```zypper: sudo zypper install lvm2```\n\n```nix: nix profile install nixpkgs#lvm2```"],["SEE ALSO","[lvm](/man/lvm)(8), [vgcreate](/man/vgcreate)(8), [vgdisplay](/man/vgdisplay)(8), [vgscan](/man/vgscan)(8), [vgremove](/man/vgremove)(8), [lvchange](/man/lvchange)(8), [pvchange](/man/pvchange)(8)"]],"e":["lvm","vgcreate","vgdisplay","vgscan","vgremove","lvchange","pvchange"],"x":"sudo vgchange -a y"},"vgs":{"n":"vgs","t":"Report LVM volume group information","s":[["TAGLINE","Report LVM volume group information"],["TLDR","**List** volume groups\n\n```sudo vgs```\n\nList **all** groups\n\n```sudo vgs -a```\n\n**Verbose** output\n\n```sudo vgs -v```\n\nShow **specific** fields\n\n```sudo vgs -o vg_name,vg_size,vg_free```\n\n**Append** field\n\n```sudo vgs -o +field_name```\n\n**No headings**\n\n```sudo vgs --noheadings```\n\nUse **separator**\n\n```sudo vgs --separator =```"],["SYNOPSIS","**vgs** [_OPTIONS_] [_VolumeGroupName_...]"],["DESCRIPTION","**vgs** displays information about volume groups in a configurable table format. It shows group names, physical volumes count, logical volumes count, sizes, and free space."],["PARAMETERS","**-a, --all**\n> Show all volume groups including incomplete ones\n\n**-v, --verbose**\n> Increase verbosity and show additional details\n\n**-o, --options** _fields_\n> Specify which fields to display\n\n**--noheadings**\n> Suppress the heading line in output\n\n**--separator** _char_\n> Use specified character as field separator\n\n**--units** _units_\n> Display sizes in specified units"],["CAVEATS","Incomplete volume groups (missing physical volumes) are hidden by default; use -a to show them. Default output is human-readable; use formatting options for scripting."],["HISTORY","**vgs** is part of **LVM2**, the Linux Logical Volume Manager."],["INSTALL","```apt: sudo apt install lvm2```\n\n```dnf: sudo dnf install lvm2```\n\n```pacman: sudo pacman -S lvm2```\n\n```apk: sudo apk add lvm2```\n\n```zypper: sudo zypper install lvm2```\n\n```nix: nix profile install nixpkgs#lvm2```"],["SEE ALSO","[lvm](/man/lvm)(8), [vgdisplay](/man/vgdisplay)(8), [pvs](/man/pvs)(8), [lvs](/man/lvs)(8)"]],"e":["lvm","vgdisplay","pvs","lvs"],"x":"sudo vgs"},"wacli-messages-context":{"n":"wacli-messages-context","t":"Show messages surrounding a specific WhatsApp message (context)","s":[["TAGLINE","Show messages surrounding a specific WhatsApp message (context)"],["TLDR","**Show surrounding context** for a message\n\n```wacli messages context --chat 491234567890@s.whatsapp.net --id ABC123 --json```\n\n**Control how many messages** before and after\n\n```wacli messages context --chat 491234567890@s.whatsapp.net --id ABC123 --before 3 --after 3 --json```\n\n**Use a different store**\n\n```wacli messages context --chat 491234567890@s.whatsapp.net --id ABC123 --json --store ~/.wacli-work```"],["SYNOPSIS","**wacli** **messages** **context** **--chat** _jid_ **--id** _msg_id_ [--before _n_] [--after _n_] [--json] [--store _dir_]"],["DESCRIPTION","**wacli messages context** returns the target message together with a window of messages immediately before and after it in the same chat. This helps understand conversation flow around a particular message.\n\nDefault store path is `~/.local/state/wacli` on Linux and `~/.wacli` elsewhere."],["PARAMETERS","**--chat** _jid_\n> Chat JID that contains the message.\n\n**--id** _msg_id_\n> ID of the central message.\n\n**--before** _n_\n> Number of preceding messages to include.\n\n**--after** _n_\n> Number of following messages to include.\n\n**--json**\n> JSON output.\n\n**--store** _dir_\n> Alternate store path (default `~/.local/state/wacli` on Linux, `~/.wacli` elsewhere)."],["SEE ALSO","[wacli](/man/wacli)(1), [wacli-messages-show](/man/wacli-messages-show)(1), [wacli-messages-list](/man/wacli-messages-list)(1), [wacli-messages-search](/man/wacli-messages-search)(1)"],["RESOURCES","```[Source code](https://github.com/openclaw/wacli)```\n\n```[Documentation](https://wacli.sh/messages.html)```"]],"e":["wacli","wacli-messages-show","wacli-messages-list","wacli-messages-search"],"x":"wacli messages context --chat 491234567890@s.whatsapp.net --id ABC123 --json"},"weblate":{"n":"weblate","t":"Weblate translation management client","s":[["TAGLINE","Weblate translation management client"],["TLDR","**List projects (using wlc client)**\n\n```wlc list-projects```\n\n**List components**\n\n```wlc list-components [project/component]```\n\n**Download translations**\n\n```wlc download [project/component/language]```\n\n**Upload translation file**\n\n```wlc upload [project/component/language] [file.po]```\n\n**Lock component for editing**\n\n```wlc lock [project/component]```\n\n**Push changes to repository**\n\n```wlc push [project/component]```\n\n**Show component stats**\n\n```wlc stats [project/component]```"],["SYNOPSIS","**wlc** [_options_] _command_ [_arguments_]"],["WLC COMMANDS","**list-projects**: List all projects.\n\n**list-components**: List components in project.\n\n**list-translations**: List translations.\n\n**show**: Show object details.\n\n**download**: Download translation file.\n\n**upload**: Upload translation file.\n\n**lock** / **unlock**: Lock/unlock translations.\n\n**push** / **pull**: Sync with repository.\n\n**commit**: Commit pending changes.\n\n**stats**: Show translation statistics."],["PARAMETERS","**--url** _url_\n> Weblate server URL.\n\n**--key** _key_\n> API key for authentication.\n\n**--format** _fmt_\n> Output format (text, csv, json, html).\n\n**-o** _file_\n> Output to file."],["DESCRIPTION","**wlc** (Weblate Client) manages Weblate translation servers remotely via the REST API. It enables automation of translation workflows, continuous localization, and integration with build systems.\n\nThe client handles common tasks: downloading/uploading translations, triggering repository syncs, locking during releases, and querying statistics. Configuration in ~/.config/weblate stores server URLs and API keys.\n\nServer-side management uses Django's manage.py or the weblate script for database operations, import/export, and administration tasks."],["CONFIGURATION","**~/.config/weblate**\n> Client configuration containing server URLs and API keys for authentication."],["CAVEATS","Requires API key from Weblate server. Rate limits may apply. Some operations require appropriate permissions on the server."],["HISTORY","**Weblate** was created by Michal Čihař as a web-based continuous localization system. The wlc client enables command-line and scripted interaction with Weblate servers, supporting automated translation workflows."],["INSTALL","```zypper: sudo zypper install weblate```\n\n```nix: nix profile install nixpkgs#weblate```"],["SEE ALSO","[gettext](/man/gettext)(1), [msgfmt](/man/msgfmt)(1), [poedit](/man/poedit)(1)"]],"e":["gettext","msgfmt","poedit"],"x":"wlc list-projects"},"wipefs":{"n":"wipefs","t":"Erase filesystem, RAID, or partition-table signatures from devices","s":[["TAGLINE","Erase filesystem, RAID, or partition-table signatures from devices"],["TLDR","**Display** signatures\n\n```sudo wipefs /dev/sda```\n\n**Wipe** all signatures\n\n```sudo wipefs -a /dev/sda```\n\nWipe device and **partitions**\n\n```sudo wipefs -a /dev/sda*```\n\n**Dry run** to show what would be done\n\n```sudo wipefs -a -n /dev/sda```\n\n**Wipe** signatures and create a backup\n\n```sudo wipefs -a -b /dev/sda```\n\n**Force** wipe mounted device\n\n```sudo wipefs -a -f /dev/sda```\n\n**Output** signatures in JSON format\n\n```sudo wipefs -J /dev/sda```"],["SYNOPSIS","**wipefs** [_OPTIONS_] _device_..."],["DESCRIPTION","**wipefs** erases filesystem, RAID, or partition-table signatures from a device. It removes the magic strings that identify the device type without destroying the actual data, allowing the device to be repurposed."],["PARAMETERS","**-a, --all**\n> Wipe all available signatures\n\n**-n, --no-act**\n> Dry run - show what would be done\n\n**-f, --force**\n> Force wiping even if device is in use\n\n**-o, --offset** _offset_\n> Wipe signature at specific offset\n\n**-b, --backup**\n> Create backup files of erased signatures\n\n**-t, --types** _list_\n> Wipe only specified signature types\n\n**-q, --quiet**\n> Suppress output messages after successful signature wipe\n\n**-p, --parsable**\n> Print out in parsable format instead of printable\n\n**-J, --json**\n> Use JSON output format\n\n**-O, --output** _list_\n> Specify which output columns to print"],["CAVEATS","This command removes filesystem identification signatures but does not erase the filesystem itself or any other data from the device. For complete data destruction, use **blkdiscard** or overwrite with **dd**. Wiping a mounted filesystem can cause data loss. The **--force** flag is required to erase nested partition tables on non-whole disk devices."],["HISTORY","**wipefs** is part of the **util-linux** package, providing signature management for block devices."],["INSTALL","```apt: sudo apt install util-linux```\n\n```dnf: sudo dnf install util-linux```\n\n```pacman: sudo pacman -S util-linux```\n\n```apk: sudo apk add wipefs```\n\n```zypper: sudo zypper install util-linux```\n\n```brew: brew install util-linux```\n\n```nix: nix profile install nixpkgs#util-linux```"],["SEE ALSO","[blkid](/man/blkid)(8), [lsblk](/man/lsblk)(8), [blkdiscard](/man/blkdiscard)(8), [dd](/man/dd)(1), [fdisk](/man/fdisk)(8)"]],"e":["blkid","lsblk","blkdiscard","dd","fdisk"],"x":"sudo wipefs /dev/sda"},"xbacklight":{"n":"xbacklight","t":"Adjust screen backlight brightness","s":[["TAGLINE","Adjust screen backlight brightness"],["TLDR","Get current **brightness**\n\n```xbacklight```\n\n**Set** brightness\n\n```xbacklight -set [40]```\n\n**Increase** brightness\n\n```xbacklight -inc [25]```\n\n**Decrease** brightness\n\n```xbacklight -dec [75]```\n\nSet with **fade** effect\n\n```xbacklight -set [100] -time [60000] -steps [60]```"],["SYNOPSIS","**xbacklight** [_OPTIONS_]"],["PARAMETERS","**-get**\n> Print current brightness percentage (default when no action given).\n\n**-set** _PERCENT_\n> Set brightness to percentage.\n\n**-inc** _PERCENT_\n> Increase brightness by percentage.\n\n**-dec** _PERCENT_\n> Decrease brightness by percentage.\n\n**-time** _MS_\n> Fade duration in milliseconds.\n\n**-steps** _N_\n> Number of steps for fade.\n\n**-display** _DISPLAY_\n> X display to use.\n\n**-help**\n> Display help information."],["DESCRIPTION","**xbacklight** adjusts backlight brightness using the X RandR extension's backlight property. When called without arguments, it prints the current brightness level as a percentage. The **-set**, **-inc**, and **-dec** options modify the brightness.\n\nThe **-time** and **-steps** parameters enable smooth fade transitions, spreading the brightness change over the specified duration."],["CAVEATS","X11 only; does not work with Wayland. Requires the RandR backlight property to be exposed by the display driver. Many modern laptops and proprietary drivers (e.g., NVIDIA) do not expose this property. Use **brightnessctl** or **light** as more reliable alternatives that work via sysfs."],["INSTALL","```apt: sudo apt install xbacklight```\n\n```dnf: sudo dnf install xbacklight```\n\n```apk: sudo apk add xbacklight```\n\n```zypper: sudo zypper install xbacklight```\n\n```nix: nix profile install nixpkgs#xbacklight```"],["SEE ALSO","[brightnessctl](/man/brightnessctl)(1), [light](/man/light)(1), [xrandr](/man/xrandr)(1)"]],"e":["brightnessctl","light","xrandr"],"x":"xbacklight"},"xwinwrap":{"n":"xwinwrap","t":"Run a program as desktop background on X11","s":[["TAGLINE","Run a program as desktop background on X11"],["TLDR","Run **video** as background\n\n```xwinwrap -b -nf -ov -- mpv --wid WID --loop --no-audio --no-resume-playback --panscan=1.0 [path/to/video.mp4]```\n\nRun video **fullscreen**\n\n```xwinwrap -b -nf -fs -ov -- mpv --wid WID --loop --no-audio --no-resume-playback --panscan=1.0 [path/to/video.mp4]```\n\nRun with **opacity**\n\n```xwinwrap -b -nf -ov -o 0.8 -- mpv --wid WID --loop --no-audio --no-resume-playback --panscan=1.0 [path/to/video.mp4]```\n\nRun on **specific** monitor\n\n```xwinwrap -g 1600x900+1920+0 -b -nf -ov -- mpv --wid WID --loop --no-audio --no-resume-playback --panscan=1.0 [path/to/video.mkv]```"],["SYNOPSIS","**xwinwrap** [_OPTIONS_] -- _COMMAND_"],["PARAMETERS","**-b**\n> Below other windows\n\n**-nf**\n> No focus\n\n**-fs**\n> Fullscreen\n\n**-ov**\n> Set override redirect\n\n**-o** _OPACITY_\n> Set opacity (0.0 to 1.0)\n\n**-g** _GEOMETRY_\n> Window geometry (WxH+X+Y)\n\n**WID**\n> Window ID placeholder replaced with actual window ID when passed to child process\n\n**-s**\n> Sticky window (appears on all desktops/workspaces)\n\n**-st**\n> Skip taskbar"],["DESCRIPTION","**xwinwrap** runs a program as a desktop background window. It creates a window behind all others and passes its window ID to the child program, enabling video wallpapers and animated backgrounds.\n\nCommonly used with mpv to play videos as desktop backgrounds."],["CAVEATS","X11 only. May not work with all window managers or compositors. Child process must support window ID parameter."],["INSTALL","```nix: nix profile install nixpkgs#xwinwrap```"],["SEE ALSO","[mpv](/man/mpv)(1), [feh](/man/feh)(1), [xdotool](/man/xdotool)(1), [xsetroot](/man/xsetroot)(1)"]],"e":["mpv","feh","xdotool","xsetroot"],"x":"xwinwrap -b -nf -ov -- mpv --wid WID --loop --no-audio --no-resume-playback --panscan=1.0 [path/to/video.mp4]"},"zapier-build":{"n":"zapier-build","t":"Build Zapier integration for deployment","s":[["TAGLINE","Build Zapier integration for deployment"],["TLDR","**Build integration**\n\n```zapier build```"],["SYNOPSIS","**zapier** **build**"],["DESCRIPTION","**zapier build** compiles a Zapier integration for deployment. Builds source files into deployable format. Run before push to verify build succeeds."],["SEE ALSO","[zapier](/man/zapier)(1), [zapier-push](/man/zapier-push)(1)"]],"e":["zapier","zapier-push"],"x":"zapier build"},"zpool-status":{"n":"zpool-status","t":"Display ZFS pool health status","s":[["TAGLINE","Display ZFS pool health status"],["TLDR","**Show status of all pools**\n\n```zpool status```\n\n**Show status of specific pool**\n\n```zpool status [poolname]```\n\n**Show only pools with errors**\n\n```zpool status -x```\n\n**Show verbose error details**\n\n```zpool status -v```\n\n**Show deduplication statistics**\n\n```zpool status -D```\n\n**Output as JSON**\n\n```zpool status -j```"],["SYNOPSIS","**zpool status** [_options_] [_pool_...]"],["PARAMETERS","**-v**\n> Verbose mode; show data error list.\n\n**-x**\n> Only show pools with errors or unavailable.\n\n**-D**\n> Display deduplication statistics.\n\n**-g**\n> Display vdev GUIDs.\n\n**-L**\n> Display real device paths.\n\n**-P**\n> Display full device paths.\n\n**-T** _d_|_u_\n> Timestamp format: d (date) or u (unix).\n\n**-j**\n> Output in JSON format."],["POOL STATES","**ONLINE**: Fully operational.\n\n**DEGRADED**: One or more devices failed but pool operational.\n\n**FAULTED**: Pool completely inaccessible.\n\n**OFFLINE**: Device explicitly taken offline.\n\n**UNAVAIL**: Device cannot be opened.\n\n**REMOVED**: Device physically removed."],["DESCRIPTION","**zpool status** displays detailed health information for ZFS storage pools. It shows device hierarchy, state of each vdev, error counts, and any ongoing scrub or resilver operations.\n\nThe READ, WRITE, and CKSUM columns show error counts for each device. Non-zero values indicate potential hardware problems requiring attention. Regular scrubs help detect silent corruption early.\n\nWhen scrubs or resilvers are active, progress percentage and estimated completion time are displayed. The -x option provides a quick way to check if any pools need attention."],["CAVEATS","Error counts persist until cleared. CKSUM errors may indicate controller or cable issues, not just drive failure. Degraded pools should be repaired promptly to avoid data loss."],["HISTORY","**zpool status** is the primary tool for ZFS pool health monitoring. ZFS was developed by Sun Microsystems and introduced data integrity verification features that make status reporting more informative than traditional storage systems."],["INSTALL","```apt: sudo apt install zfs-fuse```\n\n```apk: sudo apk add zfs```\n\n```nix: nix profile install nixpkgs#zfs```"],["SEE ALSO","[zpool](/man/zpool)(8), [zpool-events](/man/zpool-events)(8), [zfs](/man/zfs)(8)"]],"e":["zpool","zpool-events","zfs"],"x":"zpool status"},"zsoelim":{"n":"zsoelim","t":"Process .so includes in roff source files","s":[["TAGLINE","Process .so includes in roff source files"],["TLDR","**Process .so requests in file**\n\n```zsoelim [file.man]```\n\n**Process from stdin**\n\n```cat [file.man] | zsoelim```\n\n**Process compressed man page**\n\n```zcat [file.man.gz] | zsoelim```"],["SYNOPSIS","**zsoelim** [**-CVh**] [_file_...]"],["PARAMETERS","**-C**, **--compatible**\n> Enable .so requests followed by something other than whitespace\n\n**-V**, **--version**\n> Display version information\n\n**-h**, **--help**\n> Display help message"],["DESCRIPTION","**zsoelim** processes **.so** requests in roff/man page source files. The **.so** directive includes the content of another file, similar to C's #include.\n\nWhen man pages contain lines like **.so man3/other.3**, zsoelim reads and substitutes the referenced file's content. This is used in the man page system to avoid duplicating documentation for aliases.\n\nThe \"z\" prefix indicates support for gzip-compressed input files, typical of the man page system where sources may be stored compressed.\n\nzsoelim searches for included files in the standard man page hierarchy and processes them recursively."],["CAVEATS","Primarily used as part of the man page processing pipeline, rarely invoked directly.\n\nFile paths in .so directives are relative to the man directory structure, not the current directory.\n\nThe tool expects roff/man page format input. It's not a general-purpose file includer."],["SEE ALSO","[soelim](/man/soelim)(1), [man](/man/man)(1), [groff](/man/groff)(1), [zcat](/man/zcat)(1)"]],"e":["soelim","man","groff","zcat"],"x":"zsoelim [file.man]"}});
