window.LCL_BUCKET(24,{"NetworkManager":{"n":"NetworkManager","t":"daemon that manages network connections","s":[["TAGLINE","daemon that manages network connections"],["TLDR","**Show connection status**\n\n```nmcli general status```\n\n**List connections**\n\n```nmcli connection show```\n\n**Connect to WiFi**\n\n```nmcli device wifi connect \"[SSID]\" password \"[password]\"```\n\n**Show WiFi networks**\n\n```nmcli device wifi list```\n\n**Bring up connection**\n\n```nmcli connection up [connection_name]```\n\n**Create static connection**\n\n```nmcli connection add type ethernet con-name [myeth] ifname [eth0] ip4 [192.168.1.10/24] gw4 [192.168.1.1]```"],["SYNOPSIS","**NetworkManager** [_options_]"],["PARAMETERS","**--debug**\n> Debug mode.\n\n**--log-level** _level_\n> Logging level.\n\n**--log-domains** _domains_\n> Logging domains.\n\n**--pid-file** _file_\n> PID file location."],["DESCRIPTION","**NetworkManager** is a daemon that manages network connections. It provides automatic network detection and configuration, handling wired, wireless, mobile broadband, and VPN connections.\n\nNetworkManager is controlled via nmcli, nmtui, or GUI applets."],["CLI TOOL (nmcli)","```bash\nnmcli device                    # List devices\nnmcli connection                # List connections\nnmcli connection modify ...     # Modify connection\nnmcli networking off            # Disable networking\n```"],["CONFIGURATION","```\n/etc/NetworkManager/\n├── NetworkManager.conf\n├── system-connections/\n└── conf.d/\n```"],["CAVEATS","May conflict with other network tools. Connections stored in /etc/NetworkManager/. Some systems use alternatives."],["HISTORY","NetworkManager was developed by **Red Hat** starting in **2004** to simplify network configuration on Linux desktops."],["INSTALL","```apt: sudo apt install network-manager```\n\n```dnf: sudo dnf install NetworkManager```\n\n```pacman: sudo pacman -S networkmanager```\n\n```apk: sudo apk add networkmanager```\n\n```zypper: sudo zypper install NetworkManager```\n\n```nix: nix profile install nixpkgs#networkmanager```"],["SEE ALSO","[nmcli](/man/nmcli)(1), [nmtui](/man/nmtui)(1), [netctl](/man/netctl)(1)"]],"e":["nmcli","nmtui","netctl"],"x":"nmcli general status"},"asciiquarium":{"n":"asciiquarium","t":"Display an animated ASCII art aquarium","s":[["TAGLINE","Display an animated ASCII art aquarium"],["TLDR","**Run** the aquarium\n\n```asciiquarium```\n\nRun in **classic** mode (only the original 1.0 creatures)\n\n```asciiquarium -c```"],["SYNOPSIS","**asciiquarium** [_-c_]"],["PREVIEW","```\n   ><(((°>     .\n  __         ><>\n (__)   ~  ___\n ||||  ~   |#|\n```"],["DESCRIPTION","**asciiquarium** displays an animated ASCII art aquarium in your terminal. It features swimming fish, a castle, seaweed, bubbles, and the occasional shark or sea monster, providing a relaxing screensaver-like animation.\n\nThe program runs until interrupted and adapts to terminal size."],["PARAMETERS","**-c**\n> Classic mode: show only the species present in version 1.0."],["CONTROLS","Press **q** to quit, **r** to redraw all entities, or **p** to toggle pause."],["CAVEATS","Requires Term::Animation Perl module. Works best with 256-color terminal support. High CPU usage on some systems."],["HISTORY","**asciiquarium** was written by Kirk Baucom as a Perl script providing terminal entertainment through animated ASCII art."],["INSTALL","```dnf: sudo dnf install asciiquarium```\n\n```pacman: sudo pacman -S asciiquarium```\n\n```apk: sudo apk add asciiquarium```\n\n```brew: brew install asciiquarium```\n\n```nix: nix profile install nixpkgs#asciiquarium```"],["SEE ALSO","[cmatrix](/man/cmatrix)(1), [sl](/man/sl)(6), [cowsay](/man/cowsay)(1)"],["RESOURCES","```[Source code](https://github.com/cmatsuoka/asciiquarium)```\n\n```[Homepage](https://robobunny.com/projects/asciiquarium/html/)```"]],"e":["cmatrix","sl","cowsay"],"x":"asciiquarium"},"bacon":{"n":"bacon","t":"Background Rust code checker","s":[["TAGLINE","Background Rust code checker"],["TLDR","**Run the default check job in the current project**\n\n```bacon```\n\n**Run a specific job**\n\n```bacon [test]```\n\n**Run clippy lints**\n\n```bacon --job [clippy]```\n\n**List available jobs**\n\n```bacon --list-jobs```\n\n**Run in a specific project directory**\n\n```bacon --path [path/to/project]```\n\n**Initialize a bacon.toml configuration file**\n\n```bacon --init```\n\n**Open the preferences file path**\n\n```bacon --prefs```"],["SYNOPSIS","**bacon** [_options_] [_job_]"],["PARAMETERS","**-j**, **--job** _name_\n> Run the specified job. Can also be passed as a positional argument without the flag when unambiguous.\n\n**--list-jobs**\n> List all available jobs and exit.\n\n**--path** _dir_\n> Set the project directory (default is current directory).\n\n**--config** _file_\n> Use a specific bacon.toml configuration file.\n\n**-w**, **--watch** _path_\n> Additional paths to watch for changes.\n\n**--init**\n> Create a bacon.toml configuration file in the current project if one does not exist.\n\n**--prefs**\n> Create the preferences file if it does not exist and print its path.\n\n**-s**, **--summary**\n> Show a summary of results when finishing.\n\n**--no-default-features**\n> Disable default cargo features.\n\n**--features** _features_\n> Comma-separated list of cargo features to enable.\n\n**--all-features**\n> Enable all available cargo features."],["DESCRIPTION","**bacon** is a background Rust code checker that runs cargo commands in watch mode. It continuously checks your code for compilation errors, warnings, and clippy lints, displaying results in a terminal UI.\n\nThe tool provides immediate feedback during development without manually running cargo commands. When no ambiguity exists, the job name can be passed directly as a positional argument (e.g. `bacon clippy` instead of `bacon --job clippy`).\n\nDefault jobs include **check** (cargo check), **clippy** (clippy lints), **test** (run tests), **doc** (generate documentation), and **run** (build and run). Custom jobs can be defined in bacon.toml."],["CAVEATS","Requires the Rust toolchain to be installed. Continuous checking may cause high CPU usage on large projects. The terminal UI requires a compatible terminal emulator."],["HISTORY","**bacon** was created by Denys Séguret (Canop) around **2021** to provide a better development experience for Rust projects with continuous feedback."],["INSTALL","```apt: sudo apt install bacon```\n\n```pacman: sudo pacman -S bacon```\n\n```brew: brew install bacon```\n\n```nix: nix profile install nixpkgs#bacon```"],["SEE ALSO","[cargo](/man/cargo)(1), [cargo-watch](/man/cargo-watch)(1), [clippy](/man/clippy)(1), [rustc](/man/rustc)(1)"]],"e":["cargo","cargo-watch","clippy","rustc"],"x":"bacon"},"balena":{"n":"balena","t":"Manage IoT device fleets","s":[["TAGLINE","Manage IoT device fleets"],["TLDR","**Login** to Balena Cloud\n\n```balena login```\n\n**List** devices\n\n```balena devices```\n\n**Push** code to device\n\n```balena push [myApp]```\n\n**SSH** into device\n\n```balena ssh [device-uuid]```\n\n**View** logs\n\n```balena logs [device-uuid]```\n\nCreate **new application**\n\n```balena app create [myApp]```"],["SYNOPSIS","**balena** _command_ [_options_]"],["DESCRIPTION","**balena** is the command-line interface for Balena (formerly Resin.io), a platform for deploying and managing IoT device fleets. It enables building, deploying, and monitoring containerized applications on edge devices.\n\nThe CLI provides tools for device provisioning, application deployment, remote access, and fleet management."],["PARAMETERS","**login**\n> Authenticate with Balena Cloud\n\n**push** _application_\n> Deploy code to application\n\n**devices**\n> List devices\n\n**ssh** _device_\n> SSH into device\n\n**logs** _device_\n> View device logs\n\n**scan**\n> Scan for local Balena devices\n\n**os**\n> OS image management commands\n\n**env**\n> Environment variable management\n\n**--application**, **-a** _name_\n> Application name\n\n**--device**, **-d** _uuid_\n> Device UUID"],["FEATURES","- Container-based deployments\n- Over-the-air updates\n- Remote device access\n- Environment variable management\n- Multi-architecture support\n- Device fleet management"],["WORKFLOW","```bash"],["Login","balena login"],["Create app","balena app create myApp"],["Push code","cd my-project\nbalena push myApp"],["Monitor","balena logs <device>\n```"],["CAVEATS","Requires Balena account. Device must run BalenaOS. Internet connectivity needed for cloud features. Some operations require payment plan. Local mode available for offline development."],["HISTORY","**Balena CLI** was developed by Balena (formerly Resin.io) starting around **2013** as a platform for IoT fleet management and edge computing."],["SEE ALSO","[docker](/man/docker)(1), [kubectl](/man/kubectl)(1)"]],"e":["docker","kubectl"],"x":"balena login"},"beet":{"n":"beet","t":"Music library manager and metadata organizer","s":[["TAGLINE","Music library manager and metadata organizer"],["TLDR","**Import** music from a directory and tag from MusicBrainz\n\n```beet import [path/to/directory]```\n\n**Import a single song** to the library\n\n```beet import -s [path/to/file]```\n\n**Query** the library\n\n```beet list [query]```\n\nShow **entire library statistics**\n\n```beet stats```\n\nShow **statistics** for a specific query\n\n```beet stats [query]```"],["SYNOPSIS","**beet** _command_ [_options_] [_arguments_]"],["DESCRIPTION","**beet** (beets) is a command-line music library manager and metadata organizer. It automatically fetches metadata from MusicBrainz, organizes music files, and provides a powerful query interface.\n\nBeets can fix tags, rename files based on metadata, fetch album art, and integrate with plugins for additional functionality like playback, web interface, and format conversion."],["SUBCOMMANDS","**import**\n> Add music to the library with automatic tagging\n\n**list** (or **ls**)\n> Query the music library\n\n**stats**\n> Display library statistics\n\n**update**\n> Update the library when files change\n\n**remove** (or **rm**)\n> Remove items from the library\n\n**move** (or **mv**)\n> Move or rename files in the library\n\n**write**\n> Write metadata changes to files"],["PARAMETERS","**-s, --singletons**\n> Import files as singletons (not as album tracks)\n\n**-A, --noautotag**\n> Do not attempt to automatically tag\n\n**-l, --library** _path_\n> Specify the library database location\n\n**-c, --config** _path_\n> Specify the configuration file"],["CONFIGURATION","**~/.config/beets/config.yaml**\n> Main configuration file for library paths, import behavior, plugins, and metadata preferences."],["CAVEATS","Initial library import of large collections can be time-consuming. MusicBrainz queries have rate limits. Configuration file should be set up for optimal behavior."],["HISTORY","**beets** was created by Adrian Sampson and first released in **2010**. It has grown into a comprehensive music library manager with an active plugin ecosystem."],["INSTALL","```dnf: sudo dnf install beets```\n\n```pacman: sudo pacman -S beets```\n\n```apk: sudo apk add beets```\n\n```zypper: sudo zypper install beets```\n\n```brew: brew install beets```\n\n```nix: nix profile install nixpkgs#beets```"],["SEE ALSO","[id3v2](/man/id3v2)(1), [mp3info](/man/mp3info)(1)"]],"e":["id3v2","mp3info"],"x":"beet import [path/to/directory]"},"blastdbcmd":{"n":"blastdbcmd","t":"Extract sequences from BLAST databases","s":[["TAGLINE","Extract sequences from BLAST databases"],["TLDR","**Get database information**\n\n```blastdbcmd -db [nr] -info```\n\n**List available databases** in a path\n\n```blastdbcmd -list [/path/to/databases] -recursive```\n\n**Extract a sequence** by accession\n\n```blastdbcmd -db [nr] -entry [NP_001234] -out [sequence.fasta]```\n\n**Extract all sequences** from database\n\n```blastdbcmd -db [swissprot] -entry all -out [swissprot.fasta]```\n\n**Extract sequences** from a list file\n\n```blastdbcmd -db [nr] -entry_batch [ids.txt] -out [sequences.fasta]```\n\n**Get sequence in specific format**\n\n```blastdbcmd -db [nr] -entry [NP_001234] -outfmt \"%a %t %s\"```\n\n**Show BLAST database search path**\n\n```blastdbcmd -show_blastdb_search_path```\n\n**Get taxonomy information**\n\n```blastdbcmd -db [nr] -entry [NP_001234] -outfmt \"%a %S %T\"```"],["SYNOPSIS","**blastdbcmd** [_-db database_] [_-entry id_] [_options_]"],["DESCRIPTION","**blastdbcmd** is a utility for extracting sequences and metadata from BLAST databases. It can retrieve individual sequences by accession, extract all sequences, display database statistics, and generate custom reports.\n\nThe tool is part of the NCBI BLAST+ suite and works with databases created by **makeblastdb** or downloaded from NCBI."],["PARAMETERS","**-db** _name_\n> BLAST database name or path\n\n**-entry** _id_\n> Sequence identifier(s) to retrieve; use \"all\" for entire database\n\n**-entry_batch** _file_\n> File containing list of sequence identifiers\n\n**-out** _file_\n> Output file (default: stdout).\n\n**-outfmt** _format_\n> Custom output format string using % tokens.\n\n**-info**\n> Display database information (type, number of sequences, total length, date).\n\n**-list** _path_\n> List databases in specified path.\n\n**-recursive**\n> Search directories recursively (with -list).\n\n**-show_blastdb_search_path**\n> Display BLAST database search paths.\n\n**-dbtype** _type_\n> Database type: nucl (nucleotide) or prot (protein). Needed when both types share a name.\n\n**-target_only**\n> Retrieve only target sequences (no redundant group members).\n\n**-tax_info**\n> Display taxonomy information (requires taxonomy database).\n\n**-range** _start-stop_\n> Extract subsequence range (1-based, inclusive).\n\n**-strand** _strand_\n> Strand to retrieve: plus or minus (nucleotide only).\n\n**-line_length** _N_\n> Line length for FASTA output (default: 80). Use 0 for single-line sequences.\n\n**-long_seqids**\n> Use long sequence identifiers including database and accession.version."],["OUTPUT FORMAT TOKENS","**%a** - Accession\n**%g** - GI number\n**%o** - OID (ordinal ID)\n**%t** - Title (definition line)\n**%s** - Sequence data\n**%l** - Sequence length\n**%T** - Taxonomy ID\n**%S** - Scientific name\n**%L** - Common name\n**%m** - Masking data\n**%h** - Hash value\n**%e** - Membership integer"],["ENVIRONMENT","**BLASTDB**\n> Colon-separated list of directories to search for BLAST databases."],["CAVEATS","Requires pre-formatted BLAST databases created by **makeblastdb** or downloaded from NCBI. Taxonomy information requires the BLAST taxonomy database (taxdb.btd/bti) to be installed. Large extractions may require significant time and disk space. The **-range** option uses 1-based inclusive coordinates."],["INSTALL","```apt: sudo apt install ncbi-blast+```"],["SEE ALSO","[makeblastdb](/man/makeblastdb)(1), [blastn](/man/blastn)(1), [blastp](/man/blastp)(1), [blastx](/man/blastx)(1), [tblastn](/man/tblastn)(1)"]],"e":["makeblastdb","blastn","blastp","blastx","tblastn"],"x":"blastdbcmd -db [nr] -info"},"blight":{"n":"blight","t":"Control display backlight brightness","s":[["TAGLINE","Control display backlight brightness"],["TLDR","**Set** display brightness to a percentage\n\n```blight set [50]```\n\n**Show** current brightness status\n\n```blight status```\n\n**Increase** display brightness by percentage\n\n```blight inc [5]```\n\n**Decrease** display brightness with smooth transition\n\n```blight dec [10] -s```\n\n**List** available backlight devices\n\n```blight list```\n\n**Set brightness** on a specific device\n\n```blight inc [2] -d [nvidia_0]```\n\n**Save** current brightness level for later restore\n\n```blight save```\n\n**Restore** previously saved brightness level\n\n```blight restore```\n\n**Configure** permissions and udev rules\n\n```sudo blight setup```"],["SYNOPSIS","**blight** _command_ [_value_] [_options_]"],["DESCRIPTION","**blight** is a hassle-free CLI utility to manage backlight brightness on Linux. It plays well with hybrid GPU configurations and proprietary drivers, intelligently detecting the active graphics device.\n\nThe utility prioritizes integrated graphics, followed by dedicated Nvidia GPU and ACPI kernel module, so you do not need to manually specify which device is active."],["SUBCOMMANDS","**set** _value_\n> Set brightness to specified percentage\n\n**status**\n> Display current brightness status\n\n**list**\n> Show all available backlight devices\n\n**inc** _value_\n> Increase brightness by percentage\n\n**dec** _value_\n> Decrease brightness by percentage\n\n**save**\n> Store current brightness setting\n\n**restore**\n> Recover previously saved brightness\n\n**setup**\n> Configure write permissions and udev rules (requires sudo)"],["PARAMETERS","**-d**, **--device** _device_\n> Target a specific backlight device (e.g., nvidia_0, amdgpu_bl0)\n\n**-s**, **--sweep**\n> Enable smooth brightness transitions instead of instant changes"],["CAVEATS","Requires write access to sysfs backlight files. Run `sudo blight setup` once to configure udev rules and permissions, or add your user to the video group manually. Only works with displays that expose backlight control through sysfs."],["INSTALL","```apt: sudo apt install blight```"],["SEE ALSO","[xbacklight](/man/xbacklight)(1), [brightnessctl](/man/brightnessctl)(1), [light](/man/light)(1)"]],"e":["xbacklight","brightnessctl","light"],"x":"blight set [50]"},"c++filt":{"n":"c++filt","t":"Demangle C++ and Java symbol names","s":[["TAGLINE","Demangle C++ and Java symbol names"],["TLDR","**Demangle a C++ symbol**\n\n```c++filt [_Z1fv]```\n\n**Demangle from stdin**\n\n```echo \"[_Z3fooi]\" | c++filt```\n\n**Demangle without function parameters**\n\n```c++filt -p [symbol]```\n\n**Demangle with types**\n\n```c++filt -t [symbol]```\n\n**Strip leading underscore**\n\n```c++filt -_ [symbol]```\n\n**Demangle dynamic symbols from ELF**\n\n```readelf -W --dyn-syms [binary] | c++filt```"],["SYNOPSIS","**c++filt** [_options_] [_symbol_...]"],["DESCRIPTION","**c++filt** decodes (demangles) C++ and Java low-level symbol names into their original user-readable form. C++ compilers encode function names to handle overloading, and this tool reverses that process.\n\nWithout arguments, reads symbols from stdin. Can process entire assembler source files containing mangled names."],["PARAMETERS","**-_**, **--strip-underscore**\n> Remove initial underscore from symbols\n\n**-n**, **--no-strip-underscore**\n> Do not remove initial underscore\n\n**-p**, **--no-params**\n> Don't display function parameter types\n\n**-t**, **--types**\n> Demangle types as well as function names\n\n**-s** _format_, **--format**=_format_\n> Specify mangling format (gnu, lucid, arm, hp, edg, gnu-v3, java, gnat, dlang, rust)\n\n**-r**, **--no-recurse-limit**\n> Disable recursion limit (default: 2048 levels)\n\n**--help**\n> Display help\n\n**--version**\n> Display version"],["EXAMPLES","```\n$ c++filt _Z3fooi\nfoo(int)\n\n$ c++filt _ZN3Bar3bazEv\nBar::baz()\n```"],["CAVEATS","Mangled names must be complete tokens - trailing punctuation prevents demangling. Different compilers use different mangling schemes; use -s to specify format."],["INSTALL","```apt: sudo apt install binutils```\n\n```dnf: sudo dnf install binutils```\n\n```pacman: sudo pacman -S binutils```\n\n```apk: sudo apk add binutils```\n\n```zypper: sudo zypper install binutils```\n\n```brew: brew install binutils```\n\n```nix: nix profile install nixpkgs#binutils```"],["SEE ALSO","[nm](/man/nm)(1), [objdump](/man/objdump)(1), [readelf](/man/readelf)(1)"],["RESOURCES","```[Homepage](https://www.gnu.org/software/binutils/)```\n\n```[Documentation](https://sourceware.org/binutils/docs/)```"]],"e":["nm","objdump","readelf"],"x":"c++filt [_Z1fv]"},"cdk":{"n":"cdk","t":"AWS Cloud Development Kit CLI","s":[["TAGLINE","AWS Cloud Development Kit CLI"],["TLDR","**Initialize new CDK project**\n\n```cdk init app --language [typescript]```\n\n**List stacks in app**\n\n```cdk list```\n\n**Synthesize CloudFormation template**\n\n```cdk synth```\n\n**Deploy stack**\n\n```cdk deploy [StackName]```\n\n**Deploy all stacks**\n\n```cdk deploy --all```\n\n**Show diff with deployed stack**\n\n```cdk diff [StackName]```\n\n**Destroy stack**\n\n```cdk destroy [StackName]```"],["SYNOPSIS","**cdk** [_global_options_] _command_ [_arguments_] [_command_options_]"],["DESCRIPTION","**cdk** is the AWS Cloud Development Kit CLI for defining cloud infrastructure as code using familiar programming languages. It synthesizes CloudFormation templates from TypeScript, Python, Java, C#, or Go code and deploys them to AWS.\n\nInstead of writing CloudFormation YAML or JSON by hand, developers define infrastructure using constructs -- reusable, composable classes that represent AWS resources. The CDK synthesizes these constructs into CloudFormation templates, which are then deployed to create and manage the actual AWS resources.\n\nThe workflow typically involves initializing a project with `cdk init`, writing infrastructure code, previewing changes with `cdk diff`, and deploying with `cdk deploy`. The `cdk watch` command enables hot-reloading during development."],["COMMANDS","**init**\n> Create new CDK project from template\n\n**list** (ls)\n> List stacks in CDK app\n\n**synth**\n> Synthesize CloudFormation template\n\n**deploy**\n> Deploy stacks to AWS\n\n**destroy**\n> Delete stacks from AWS\n\n**diff**\n> Show differences between stacks\n\n**bootstrap**\n> Deploy CDK bootstrap stack\n\n**watch**\n> Watch for changes and deploy\n\n**context**\n> Manage cached context values\n\n**docs**\n> Open CDK documentation"],["PARAMETERS","**--all**\n> Deploy/destroy all stacks\n\n**--hotswap**\n> Fast deployment when possible\n\n**--require-approval** _level_\n> Approval level: never, any-change, broadening\n\n**--profile** _name_\n> AWS profile to use\n\n**--region** _region_\n> AWS region\n\n**--output** _dir_\n> Output directory for synthesized templates\n\n**--verbose**, **-v**\n> Verbose output"],["CONFIGURATION","**cdk.json**\n> Project configuration including app entry point, context values, and feature flags."],["CAVEATS","Requires Node.js and AWS credentials configured. Run `cdk bootstrap` once per account/region before first deploy."],["INSTALL","```dnf: sudo dnf install cdk```\n\n```brew: brew install cdk```\n\n```nix: nix profile install nixpkgs#cdk```"],["SEE ALSO","[aws](/man/aws)(1), [terraform](/man/terraform)(1), [pulumi](/man/pulumi)(1)"],["RESOURCES","```[Source code](https://github.com/aws/aws-cdk)```\n\n```[Documentation](https://docs.aws.amazon.com/cdk/v2/guide/cli.html)```"]],"e":["aws","terraform","pulumi"],"x":"cdk init app --language [typescript]"},"chmem":{"n":"chmem","t":"configure memory hotplug state","s":[["TAGLINE","configure memory hotplug state"],["TLDR","**Set a memory block offline**\n\n```sudo chmem -b -d [block_number]```\n\n**Set a memory block online**\n\n```sudo chmem -b -e [block_number]```\n\n**Set a memory range offline**\n\n```sudo chmem -d 0x[start_address]-0x[end_address]```\n\n**Set a memory range online**\n\n```sudo chmem -e 0x[start_address]-0x[end_address]```\n\n**Set memory online in a specific zone**\n\n```sudo chmem -e 0x[start_address] -z [Movable]```"],["SYNOPSIS","**chmem** [_options_] _size_|_range_|_block_"],["DESCRIPTION","**chmem** modifies the state of memory blocks in a Linux system, bringing them online or offline. This is primarily used in virtualized environments for memory hotplug operations.\n\nMemory blocks are chunks of physical memory that can be individually managed. The tool allows dynamic memory management without rebooting."],["PARAMETERS","**-b, --block**\n> Operate on memory blocks by block number\n\n**-d, --disable**\n> Set memory offline\n\n**-e, --enable**\n> Set memory online\n\n**-z, --zone** _zone_\n> Assign memory to a specific zone (DMA, DMA32, Normal, Movable)\n\n**-h, --help**\n> Display help"],["CAVEATS","Requires root privileges. Not all systems support memory hotplug. Memory containing kernel data or in-use pages cannot be offlined. Virtual machines may require specific configuration to support memory hotplug."],["INSTALL","```apt: sudo apt install util-linux```\n\n```dnf: sudo dnf install util-linux```\n\n```pacman: sudo pacman -S util-linux```\n\n```apk: sudo apk add util-linux-misc```\n\n```zypper: sudo zypper install util-linux```\n\n```brew: brew install util-linux```\n\n```nix: nix profile install nixpkgs#util-linux```"],["SEE ALSO","[lsmem](/man/lsmem)(1), [free](/man/free)(1)"]],"e":["lsmem","free"],"x":"sudo chmem -b -d [block_number]"},"claude-thermos":{"n":"claude-thermos","t":"Keep Claude Code prompt cache warm during long subagent waits","s":[["TAGLINE","Keep Claude Code prompt cache warm during long subagent waits"],["TLDR","**Run Claude Code** through the warmer (interactive)\n\n```uvx claude-thermos```\n\n**Pass a one-off prompt** through to Claude\n\n```uvx claude-thermos -p \"[fix the bug]\"```\n\n**Tune idle threshold** before warming starts (seconds)\n\n```uvx claude-thermos --idle [270]```\n\n**Change interval** between warm cycles (seconds)\n\n```uvx claude-thermos --interval [270]```\n\n**Cap warm cycles** per idle episode (or use auto for unlimited)\n\n```uvx claude-thermos --max-cycles [4]```\n\n**Disable warming** for one run via environment\n\n```CLAUDE_WARMER_DISABLE=1 uvx claude-thermos```"],["SYNOPSIS","**claude-thermos** [**--idle** _SECONDS_] [**--interval** _SECONDS_] [**-n** | **--max-cycles** _N_|**auto**] [**--subagent-window** _SECONDS_] [_claude_args_ ...]"],["PARAMETERS","**--idle** _SECONDS_\n> Seconds the main agent must be idle before a warming cycle runs. Default: **270**. Environment: **CLAUDE_WARMER_IDLE_THRESHOLD_SEC**.\n\n**--interval** _SECONDS_\n> Seconds between warming cycles. Default: **270**. Environment: **CLAUDE_WARMER_WARM_INTERVAL_SEC**.\n\n**-n** _N_|**auto**, **--max-cycles** _N_|**auto**\n> Maximum number of warm requests per idle episode, or **auto** for unlimited. Default: **4**. Environment: **CLAUDE_WARMER_WARM_MAX_CYCLES**.\n\n**--subagent-window** _SECONDS_\n> How long a subagent still counts as active after its last traffic. Default: **540**. Environment: **CLAUDE_WARMER_SUBAGENT_ACTIVE_WINDOW_SEC**.\n\n**-V**, **--version**\n> Print version and exit.\n\n**-h**, **--help**\n> Show help.\n\n_claude_args_ ...\n> Remaining arguments are passed through unchanged to the **claude** CLI."],["DESCRIPTION","**claude-thermos** launches Anthropic's **Claude Code** CLI behind a small local reverse proxy that keeps the main agent's prompt cache alive while subagents run. Claude Code's prompt cache has a roughly **5-minute TTL**. When the main agent waits on a subagent longer than that, the main conversation prefix expires and the next turn re-encodes the full history at the more expensive cache-write rate. Warming sends cheap refresh requests (identical cacheable prefix, **max_tokens: 1**) so resume pays a cache read instead of a full rewrite.\n\nThe wrapper points **ANTHROPIC_BASE_URL** at a loopback proxy that observes **/v1/messages** traffic. Lineages (cache prefixes) are keyed by model, tool set, and system text: the first tool-bearing lineage is treated as the main agent; others as subagents. When the main lineage is idle and a subagent is still active, the warmer fires on an interval under the TTL. Warm requests go **directly** to the Anthropic API (not through the proxy) so they do not interfere with real session traffic.\n\nSession telemetry is written under **~/.claude-thermos/logs/**_session_id_**/`events.jsonl`** and **summary.json**, including warm counts and estimated rewrite tokens avoided. Requires **Python 3.11+** and the **claude** binary on **PATH**. Typical invocation is via **uvx** so the package need not be installed permanently."],["CONFIGURATION","**CLAUDE_WARMER_DISABLE**\n> Set to **1** to run Claude without warming for that process.\n\n**CLAUDE_WARMER_IDLE_THRESHOLD_SEC**, **CLAUDE_WARMER_WARM_INTERVAL_SEC**, **CLAUDE_WARMER_WARM_MAX_CYCLES**, **CLAUDE_WARMER_SUBAGENT_ACTIVE_WINDOW_SEC**\n> Environment overrides for the matching CLI flags.\n\n**~/.claude-thermos/logs/**_session_id_**/\n> Per-session **events.jsonl** (request usage and warm decisions) and **summary.json** (rollup savings estimates)."],["CAVEATS","Does not replace **claude**; it wraps it. Warming still incurs small cache-read cost and network traffic to Anthropic. Savings estimates in **summary.json** are in base-input-token units, not dollars—multiply by your model's input price. Behavior depends on Claude Code's request shape and Anthropic's cache TTL remaining about five minutes. Requires a working Claude Code install and valid Anthropic authentication."],["HISTORY","Created by **Iaroslav Zeigerman** as a Python package (**claude-thermos** on PyPI) that uses a local reverse proxy (via **mitmproxy**) to refresh Claude Code prompt-cache prefixes during long subagent waits. Released under the MIT license."],["SEE ALSO","[claude](/man/claude)(1), [uvx](/man/uvx)(1), [uv](/man/uv)(1)"],["RESOURCES","```[Source code](https://github.com/izeigerman/claude-thermos)```"]],"e":["claude","uvx","uv"],"x":"uvx claude-thermos"},"code":{"n":"code","t":"Visual Studio Code editor launcher","s":[["TAGLINE","Visual Studio Code editor launcher"],["TLDR","**Open VS Code** in the current directory\n\n```code .```\n\n**Open a specific file** in VS Code\n\n```code [path/to/file]```\n\n**Open a file at a specific line** and column\n\n```code -g [file.txt]:[line]:[column]```\n\n**Open a new window**\n\n```code -n```\n\n**Open a file in an existing window**\n\n```code -r [path/to/file]```\n\n**Compare two files** in diff mode\n\n```code -d [file1] [file2]```\n\n**Install an extension**\n\n```code --install-extension [publisher.extension]```\n\n**List installed extensions**\n\n```code --list-extensions```"],["SYNOPSIS","**code** [_options_] [_paths_...]"],["PARAMETERS","**-n**, **--new-window**\n> Open a new VS Code window.\n\n**-r**, **--reuse-window**\n> Open files in the last active window.\n\n**-g**, **--goto**\n> Open file at specific line and column (file:line:column).\n\n**-d**, **--diff**\n> Compare two files in diff mode.\n\n**-w**, **--wait**\n> Wait for files to be closed before returning.\n\n**--locale** _LOCALE_\n> Set the display language (e.g., en-US, zh-CN).\n\n**--install-extension** _EXT_\n> Install a VS Code extension by ID.\n\n**--uninstall-extension** _EXT_\n> Uninstall a VS Code extension.\n\n**--list-extensions**\n> List all installed extensions.\n\n**--disable-extensions**\n> Disable all installed extensions for this session.\n\n**--user-data-dir** _DIR_\n> Specify the directory for user data.\n\n**-v**, **--version**\n> Display version information.\n\n**-h**, **--help**\n> Display help information."],["DESCRIPTION","**code** is the command-line interface for Visual Studio Code, Microsoft's popular source code editor. It allows launching the editor, opening files and folders, managing extensions, and integrating VS Code into command-line workflows.\n\nThe CLI supports opening files at specific positions, which is useful for jumping to error locations from build tools or linters. The diff mode provides visual comparison between files. The **--wait** flag enables using VS Code as a Git editor or for other tools that need to wait for file editing completion.\n\nExtension management through the CLI enables scripting VS Code setup and synchronizing development environments. Combined with the Settings Sync feature, this allows reproducible editor configurations across machines."],["CAVEATS","The **code** command must be added to PATH, which can be done via the \"Install 'code' command in PATH\" option from VS Code's Command Palette. On some systems, the command may be **code-insiders** for the Insiders build. Remote development sessions use **code-server** for browser-based access."],["HISTORY","Visual Studio Code was released by **Microsoft** in **April 2015** as a free, open-source editor built on Electron. It rapidly grew to become one of the most popular development environments. The CLI has been part of VS Code since its early releases, enabling terminal-based workflows."],["INSTALL","```pacman: sudo pacman -S code```\n\n```nix: nix profile install nixpkgs#code```"],["SEE ALSO","[vim](/man/vim)(1), [nano](/man/nano)(1), [subl](/man/subl)(1), [atom](/man/atom)(1)"],["RESOURCES","```[Source code](https://github.com/microsoft/vscode)```\n\n```[Documentation](https://code.visualstudio.com/docs/editor/command-line)```"]],"e":["vim","nano","subl","atom"],"x":"code ."},"cpan":{"n":"cpan","t":"Perl module installer from CPAN","s":[["TAGLINE","Perl module installer from CPAN"],["TLDR","**Install a Perl module**\n\n```cpan [Module::Name]```\n\n**Install multiple modules**\n\n```cpan [Module::One] [Module::Two]```\n\n**Start interactive CPAN shell**\n\n```cpan```\n\n**Force install a module** (ignore test failures)\n\n```cpan -f [Module::Name]```\n\n**Install from a specific mirror**\n\n```cpan -M [http://cpan.org] [Module::Name]```\n\n**Upgrade all installed modules**\n\n```cpan -u```\n\n**Get information about a module**\n\n```cpan -D [Module::Name]```\n\n**Install without testing**\n\n```cpan -T [Module::Name]```"],["SYNOPSIS","**cpan** [_-cfFiOstTu_] [_-D module_] [_-M mirror_] [_module ..._]"],["PARAMETERS","**-f**\n> Force installation, ignoring test failures.\n\n**-F**\n> Turn off CPAN.pm's attempts to lock (use with caution).\n\n**-i**\n> Install specified modules (default action).\n\n**-T**\n> Do not test modules before installing.\n\n**-t**\n> Run tests for the specified modules.\n\n**-u**\n> Upgrade all installed modules.\n\n**-D** _module_\n> Display module details and installation status.\n\n**-M** _mirror_\n> Use the specified CPAN mirror.\n\n**-O**\n> Show outdated modules.\n\n**-a**\n> Create an autobundle of installed modules.\n\n**-r**\n> Recompile dynamically loaded modules.\n\n**-j** _file_\n> Use alternate configuration file.\n\n**-g** _module_\n> Download to current directory without installing.\n\n**-v**\n> Print CPAN.pm version."],["DESCRIPTION","**cpan** is the standard command-line interface for installing Perl modules from the Comprehensive Perl Archive Network (CPAN). It handles dependency resolution, downloading, building, testing, and installation of Perl modules and their prerequisites.\n\nOn first run, cpan enters a configuration mode to set up mirrors, build preferences, and other settings. This configuration is stored in **~/.cpan/CPAN/MyConfig.pm**. Users can reconfigure later with **o conf** commands in the interactive shell.\n\nThe interactive shell provides commands for searching (m /pattern/), installing (install Module), examining (look Module), and managing the local CPAN cache. Tab completion and command history are available in the shell.\n\nCPAN.pm downloads module distributions to **~/.cpan/sources/**, extracts and builds them in **~/.cpan/build/**, then installs to the Perl library path. On systems where users lack write access to system Perl directories, consider using **local::lib** or **perlbrew** for user-local installations."],["CONFIGURATION","**~/.cpan/CPAN/MyConfig.pm**\n> Main CPAN configuration storing mirror URLs, build preferences, proxy settings, and installation directories."],["CAVEATS","May require root/sudo for system-wide installation. Some modules require a C compiler or system libraries. Test failures with -f can install broken modules. First run configuration wizard can be confusing. Consider cpanm (App::cpanminus) for simpler usage."],["HISTORY","**CPAN.pm** was created by Andreas König in **1995**, shortly after CPAN itself was established. It became the standard way to install Perl modules, integrated into core Perl distributions. While newer tools like cpanm offer simpler interfaces, CPAN.pm remains the most feature-complete client with extensive configuration options."],["INSTALL","```apt: sudo apt install perl```\n\n```dnf: sudo dnf install perl```\n\n```pacman: sudo pacman -S perl```\n\n```apk: sudo apk add perl-utils```\n\n```zypper: sudo zypper install perl```\n\n```brew: brew install perl```\n\n```nix: nix profile install nixpkgs#perl```"],["SEE ALSO","[cpanm](/man/cpanm)(1), [perl](/man/perl)(1), [perlbrew](/man/perlbrew)(1), [carton](/man/carton)(1)"]],"e":["cpanm","perl","perlbrew","carton"],"x":"cpan [Module::Name]"},"csvlook":{"n":"csvlook","t":"CSV table renderer for terminal viewing","s":[["TAGLINE","CSV table renderer for terminal viewing"],["TLDR","**Display CSV as table**\n\n```csvlook [data.csv]```\n\n**Limit rows displayed**\n\n```csvlook --max-rows [10] [data.csv]```\n\n**Limit column width**\n\n```csvlook --max-column-width [20] [data.csv]```\n\n**Hide line numbers**\n\n```csvlook --no-number [data.csv]```\n\n**Use different delimiter**\n\n```csvlook -d \";\" [data.csv]```\n\n**Read from stdin**\n\n```cat [data.csv] | csvlook```"],["SYNOPSIS","**csvlook** [_options_] _file_"],["DESCRIPTION","**csvlook** renders CSV data as a fixed-width table for terminal viewing. It's part of csvkit and provides a quick way to preview CSV files in a readable format.\n\nThe tool auto-sizes columns based on content and displays data with ASCII borders, making it easy to inspect data structure and values."],["PARAMETERS","**-H**, **--no-header-row**\n> Input has no header row.\n\n**--max-rows** _n_\n> Maximum rows to display.\n\n**--max-column-width** _n_\n> Truncate columns at width.\n\n**--max-columns** _n_\n> Maximum columns to display.\n\n**-y** _n_, **--snifflimit** _n_\n> Rows to sniff for delimiter.\n\n**--no-number**\n> Don't display row numbers.\n\n**-d** _char_\n> Field delimiter.\n\n**-q** _char_\n> Quote character.\n\n**-e** _encoding_\n> Input encoding."],["CAVEATS","Large files may be slow to render. Wide tables may wrap in terminal. Part of csvkit, requires Python installation."],["HISTORY","**csvlook** is part of **csvkit**, created by **Christopher Groskopf** in **2011**. It provides quick data preview capabilities essential for data analysis workflows, complementing other csvkit tools for CSV manipulation."],["SEE ALSO","[csvcut](/man/csvcut)(1), [csvstat](/man/csvstat)(1), [column](/man/column)(1), [less](/man/less)(1)"],["RESOURCES","```[Source code](https://github.com/wireservice/csvkit)```\n\n```[Documentation](https://csvkit.readthedocs.io)```"]],"e":["csvcut","csvstat","column","less"],"x":"csvlook [data.csv]"},"deptrust":{"n":"deptrust","t":"Multi-ecosystem package vulnerability checker","s":[["TAGLINE","Multi-ecosystem package vulnerability checker"],["TLDR","**Check a specific package version**\n\n```deptrust check npm lodash 4.17.20```\n\n**Check the latest version**\n\n```deptrust check pypi requests latest```\n\n**Output results as JSON**\n\n```deptrust check --json cargo serde latest```\n\n**Suggest the safest available version**\n\n```deptrust suggest npm lodash```\n\n**Compare two versions**\n\n```deptrust compare npm lodash 4.17.20 4.17.21```"],["SYNOPSIS","**deptrust** _command_ [_ecosystem_] [_package_] [_version_]"],["PARAMETERS","**check**\n> Query known vulnerabilities for a package version.\n\n**suggest**\n> Return the newest version with an **allow** recommendation.\n\n**compare**\n> Compare risk between two versions of the same package.\n\n**version**\n> Show the installed **deptrust** version.\n\n**mcp**\n> Start the MCP server for agent integrations.\n\n**--json**\n> Emit structured JSON output."],["DESCRIPTION","**deptrust** is a Go CLI that checks package versions for known vulnerabilities across npm, PyPI, crates.io, Go modules, RubyGems, NuGet, Maven, Packagist, pub.dev, CocoaPods, Hex.pm, Hackage, and GitHub Actions. It queries OSV and the GitHub Advisory Database directly — no hosted deptrust service required.\n\nEach check returns a **recommendation**: **block** (critical/high), **review** (medium/unknown or recent publish), or **allow** (no blocking vulnerabilities found). **allow** does not prove a package is safe. JSON output includes advisory coverage, risk score, vulnerability details, and provider errors.\n\nAlso runs as a local MCP server and integrates with Codex and Claude Code via install hooks that vet package commands before they execute."],["CAVEATS","Provider coverage varies by ecosystem; partial or missing coverage returns **unknown**, which should not be treated as safe. GitHub Actions branch refs and major-only tags add review signals even when technically valid."],["SEE ALSO","[npm-audit](/man/npm-audit)(1), [osv-scanner](/man/osv-scanner)(1), [snyk](/man/snyk)(1)"],["RESOURCES","```[Source code](https://github.com/clidey/deptrust)```"]],"e":["npm-audit","osv-scanner","snyk"],"x":"deptrust check npm lodash 4.17.20"},"dnote":{"n":"dnote","t":"multi-device CLI notebook for commands, snippets, and notes","s":[["TAGLINE","multi-device CLI notebook for commands, snippets, and notes"],["TLDR","**Add** a note to a book (opens the configured editor)\n\n```dnote add [book_name]```\n\nAdd a note with **inline content**\n\n```dnote add [book_name] -c \"[note content]\"```\n\n**List** all books, or notes in a book\n\n```dnote view\ndnote view [book_name]```\n\n**Search** notes with full-text search\n\n```dnote find \"[keywords]\"```\n\n**Edit** a note by ID\n\n```dnote edit [note_id]```\n\n**Remove** a note or an entire book\n\n```dnote remove [note_id|book_name]```\n\n**Sync** notes with a Dnote server\n\n```dnote sync```"],["SYNOPSIS","**dnote** _command_ [_options_] [_args_]"],["PARAMETERS","**add** _book_ [**-c** _content_]\n> Add a note to _book_ (aliases: **a**, **n**, **new**). Opens the editor if content is omitted; accepts stdin.\n\n**view** [_book_ [_index_]]\n> List books, notes in a book, or a specific note (alias: **v**)\n\n**edit** _note-id_|_book_\n> Edit a note or rename a book (alias: **e**). **--book** moves a note; **--name** renames a book\n\n**remove** _note-id_|_book_\n> Delete a note or book (aliases: **rm**, **d**, **delete**). **-y** skips confirmation\n\n**find** _keywords_\n> Full-text search; **-b** _book_ limits scope (alias: **f**)\n\n**sync**\n> Synchronize with a configured Dnote server (**--full** for a full sync)\n\n**login** / **logout**\n> Authenticate with or leave a Dnote server\n\n**--dbPath** _path_\n> Use a custom SQLite database path instead of the default under XDG data"],["DESCRIPTION","**dnote** is a command-line notebook for capturing short notes, commands, and snippets organized into **books**. Notes are stored locally in SQLite and can optionally sync to a self-hosted or hosted Dnote server for multi-device access.\n\nContent can be entered via the system editor, **-c**, or stdin (handy for piping command output into a book). Search is full-text across notes. The default database path is typically under **~/.local/share/dnote/** on Linux."],["CONFIGURATION","Editor and API endpoint settings live in the Dnote config used by the CLI. Override the database location with **--dbPath**. Use **dnote login** with **--apiEndpoint** to target a self-hosted server before **dnote sync**."],["SEE ALSO","[nb](/man/nb)(1), [zk](/man/zk)(1)"],["RESOURCES","```[Homepage](https://www.getdnote.com)```\n\n```[Documentation](https://www.getdnote.com/docs/cli/commands/)```\n\n```[Source code](https://github.com/dnote/dnote)```"],["INSTALL","```brew: brew install dnote```\n\n```nix: nix profile install nixpkgs#dnote```"]],"e":["nb","zk"],"x":"dnote add [book_name]"},"dnsmap":{"n":"dnsmap","t":"subdomain brute-force scanner","s":[["TAGLINE","subdomain brute-force scanner"],["TLDR","Scan for subdomains using **internal wordlist**\n\n```dnsmap [example.com]```\n\nSpecify **custom wordlist**\n\n```dnsmap [example.com] -w [path/to/wordlist.txt]```\n\nStore results to **CSV file**\n\n```dnsmap [example.com] -c [path/to/file.csv]```\n\n**Ignore** false positive IPs\n\n```dnsmap [example.com] -i [123.45.67.89,98.76.54.32]```"],["SYNOPSIS","**dnsmap** _domain_ [_options_]"],["DESCRIPTION","**dnsmap** is a subdomain brute-force enumeration tool that uses DNS queries to discover subdomains not listed in public DNS records. It works by attempting to resolve potential subdomain names from a wordlist, identifying which ones successfully resolve to IP addresses.\n\nThe tool comes with a built-in wordlist of common subdomain names (www, mail, ftp, etc.), but custom wordlists can be specified for more thorough enumeration. DNS wildcard configurations can cause false positives where all subdomains resolve to the same IP; the -i flag allows filtering out these known wildcard addresses. Results can be saved to CSV format for further analysis. The tool is commonly used in penetration testing reconnaissance to map an organization's DNS footprint and discover potential attack surfaces like development servers, staging environments, or forgotten subdomains."],["PARAMETERS","**-w** _wordlist_\n> Use custom wordlist file\n\n**-c** _file_\n> Output results to CSV file\n\n**-i** _IPs_\n> Comma-separated IPs to ignore (false positives)\n\n**-d** _delay_\n> Delay between queries (milliseconds)\n\n**-r** _file_\n> Save results to regular file"],["CAVEATS","Use only against authorized domains. May trigger security alerts. DNS wildcards can cause false positives; use -i to filter."],["INSTALL","```apt: sudo apt install dnsmap```\n\n```dnf: sudo dnf install dnsmap```\n\n```brew: brew install dnsmap```\n\n```nix: nix profile install nixpkgs#dnsmap```"],["SEE ALSO","[dnsrecon](/man/dnsrecon)(1), [fierce](/man/fierce)(1), [nslookup](/man/nslookup)(1)"],["RESOURCES","```[Source code](https://github.com/makefu/dnsmap)```"]],"e":["dnsrecon","fierce","nslookup"],"x":"dnsmap [example.com]"},"dopewars":{"n":"dopewars","t":"drug trading strategy game set in New York City","s":[["TAGLINE","drug trading strategy game set in New York City"],["TLDR","**Start a game in text mode**\n\n```dopewars -t```\n\n**Start with a specific player name**\n\n```dopewars -t -n [name]```"],["SYNOPSIS","**dopewars** [_options_]"],["PARAMETERS","**-t**\n> Text-mode (curses) interface.\n\n**-n** _NAME_\n> Set player name.\n\n**-s**\n> Run as server for multiplayer.\n\n**-c**\n> Connect to a multiplayer server.\n\n**-h**\n> Display help."],["PREVIEW","```\n Bronx -> Manhattan\n Cocaine:  $15000\n Heroin:    $6300\n Cash: $2000  Debt: $5500\n Day 1 of 30\n```"],["DESCRIPTION","**dopewars** is a strategy game where the player buys and sells goods across New York City locations over 30 days. The goal is to pay off the loan shark and make as much money as possible by exploiting price differences between neighborhoods.\n\nRandom events like police encounters and market fluctuations add unpredictability. The game features a multiplayer mode for competing against other players over a network."],["CAVEATS","Use **-t** flag for terminal mode. Multiplayer requires a running server. The game theme may be considered controversial."],["HISTORY","**dopewars** is based on **Drug Wars**, a game originally written by **John E. Dell** in **1984** for the TI-83 calculator. The Unix version was created by **Ben Webb** and has been actively maintained since the late **1990s**."],["INSTALL","```pacman: sudo pacman -S dopewars```\n\n```brew: brew install dopewars```\n\n```nix: nix profile install nixpkgs#dopewars```"],["SEE ALSO","[trek](/man/trek)(6), [adventure](/man/adventure)(6)"]],"e":["trek","adventure"],"x":"dopewars -t"},"ect":{"n":"ect","t":"lossless image compression optimizer","s":[["TAGLINE","lossless image compression optimizer"],["TLDR","**Optimize PNG file**\n\n```ect [image.png]```\n\n**Optimize with maximum** compression\n\n```ect -9 [image.png]```\n\n**Optimize multiple files**\n\n```ect [*.png]```\n\n**Optimize recursively**\n\n```ect -recurse [directory]```\n\n**Preserve modification time**\n\n```ect -keep [image.png]```\n\n**Strip metadata for smallest size**\n\n```ect -strip [image.png]```\n\n**Optimize JPEG** losslessly\n\n```ect [image.jpg]```"],["SYNOPSIS","**ect** [_options_] _files_..."],["PARAMETERS","_FILES_\n> Image files to optimize.\n\n**-1** to **-9**\n> Compression level (1 = fast, 9 = slowest/strongest). Default is **-3**.\n\n**-recurse**\n> Process directories recursively.\n\n**-keep**\n> Preserve file modification time.\n\n**-strip**\n> Strip metadata (EXIF, comments, color profiles).\n\n**-progressive**\n> Encode JPEGs as progressive.\n\n**-gzip**\n> Optimize gzip files instead of treating them as images.\n\n**-zip**\n> Optimize ZIP archives.\n\n**-quiet**\n> Suppress non-error output.\n\n**--mt-deflate**[=_N_]\n> Multithread the deflate stage; optionally pin to _N_ threads.\n\n**--mt-file**[=_N_]\n> Process multiple input files in parallel.\n\n**-help**\n> Display help information."],["DESCRIPTION","**ECT** (Efficient Compression Tool) losslessly optimizes PNG, JPEG, and gzip files for smaller sizes. It achieves better compression than many alternatives while remaining lossless.\n\nThe tool combines multiple compression strategies to reduce file sizes without quality loss. For PNG, it optimizes filters and DEFLATE compression. For JPEG, it optimizes Huffman tables.\n\nECT is useful for web optimization, reducing bandwidth and storage while maintaining perfect image quality."],["CAVEATS","High compression levels are very slow (level 9 may take minutes per image). Files are rewritten in place. Use **-strip** if you need the absolute smallest output, since metadata is kept by default."],["HISTORY","ECT was developed as a high-performance lossless image optimizer, combining techniques from multiple compression tools (zopfli, optipng, etc.) into an efficient single tool."],["INSTALL","```aur: yay -S ect```"],["SEE ALSO","[optipng](/man/optipng)(1), [pngcrush](/man/pngcrush)(1), [jpegoptim](/man/jpegoptim)(1)"]],"e":["optipng","pngcrush","jpegoptim"],"x":"ect [image.png]"},"fj":{"n":"fj","t":"Forgejo CLI for the terminal","s":[["TAGLINE","Forgejo CLI for the terminal"],["TLDR","**Log in** to a Forgejo instance\n\n```fj auth login```\n\n**Clone** a repository\n\n```fj repo clone [owner]/[repository]```\n\n**Create** a new issue interactively\n\n```fj issue create```\n\n**Open** an issue in the browser\n\n```fj issue browse [issue_number]```\n\n**Create** a new pull request\n\n```fj pr create```\n\n**Check out** a pull request branch locally\n\n```fj pr checkout [pr_number]```\n\n**List** the releases of a repository\n\n```fj release list```\n\n**Show** the currently logged-in user\n\n```fj whoami```"],["SYNOPSIS","**fj** [_global options_] _command_ [_subcommand_] [_options_]"],["DESCRIPTION","**fj** is the command-line interface for **Forgejo**, a self-hosted Git forge (a community-driven fork of Gitea). It lets you interact with Forgejo instances without leaving the terminal: authenticate against one or more servers, manage repositories, and work with issues, pull requests, and releases.\n\nCommands are grouped into nouns such as **auth**, **repo**, **issue**, **pr**, and **release**, each with its own subcommands and flags. Once authenticated, fj reads the remote of the current Git repository to determine which instance and project to act on, so most commands can be run from inside a clone without extra arguments."],["PARAMETERS","**auth**\n\n> Manage authentication. Use **fj auth login** to store credentials for an instance and **fj auth logout** to remove them.\n\n**repo**\n\n> Work with repositories, including **clone**, **create**, and **view**.\n\n**issue**\n\n> Create, list, view, and browse issues.\n\n**pr**\n\n> Create, list, check out, and review pull requests.\n\n**release**\n\n> List and manage repository releases.\n\n**whoami**\n\n> Display the user account that is currently logged in."],["CAVEATS","fj is under active development and its command surface still changes between releases. It targets Forgejo's API; some operations may not work against older Forgejo or Gitea servers."],["SEE ALSO","[tea](/man/tea)(1), [gh](/man/gh)(1), [glab](/man/glab)(1), [git](/man/git)(1)"],["RESOURCES","```[Source code](https://codeberg.org/forgejo-contrib/forgejo-cli)```"]],"e":["tea","gh","glab","git"],"x":"fj auth login"},"git-clear":{"n":"git-clear","t":"Reset and clean working directory","s":[["TAGLINE","Reset and clean working directory"],["TLDR","**Clear working directory**\n\n```git clear```\n\n**Clear with force**\n\n```git clear -f```"],["SYNOPSIS","**git** **clear** [_options_]"],["PARAMETERS","**-f**, **--force**\n> Force without confirmation."],["DESCRIPTION","**git clear** is a **git-extras** command that combines **git clean -d -f -x** and **git reset --hard** into a single convenience command. It removes all uncommitted changes, tracked and untracked, including files matched by **.gitignore**, returning the repository to a pristine state matching **HEAD**.\n\nThis is the most aggressive of the git-extras clearing commands: **git reset --hard** alone only touches tracked files, and **git-clear-soft** spares ignored files, but **git clear** wipes everything not committed. By default it asks **\"Sure? - This command may delete files that cannot be recovered, including those in .gitignore [y/N]:\"** before proceeding.\n\nThe operation is destructive and cannot be undone, as it permanently deletes uncommitted work. The **-f** flag bypasses the confirmation prompt, allowing automated scripts to clean repositories without user interaction. Use with caution in repositories containing valuable uncommitted work or ignored build artifacts you meant to keep."],["CAVEATS","Irreversibly deletes untracked and ignored files alongside the hard reset; there is no undo. Requires **git-extras** to be installed."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-clear-soft](/man/git-clear-soft)(1), [git-reset](/man/git-reset)(1), [git-clean](/man/git-clean)(1), [git-extras](/man/git-extras)(1)"],["RESOURCES","```[Source code](https://github.com/tj/git-extras)```\n\n```[Documentation](https://github.com/tj/git-extras/blob/master/Commands.md#git-clear)```"]],"e":["git-clear-soft","git-reset","git-clean","git-extras"],"x":"git clear"},"gvgen":{"n":"gvgen","t":"generates various types of graphs in DOT format","s":[["TAGLINE","generates various types of graphs in DOT format"],["TLDR","**Generate a star graph** with 5 vertices\n\n```gvgen -s [5] > star.gv```\n\n**Generate a grid graph**\n\n```gvgen -g [3],[4] > grid.gv```\n\n**Generate a complete graph** on 6 vertices\n\n```gvgen -k [6] > complete.gv```\n\n**Generate a binary tree** of height 4\n\n```gvgen -t [4] > tree.gv```\n\n**Generate a random graph** with 10 vertices\n\n```gvgen -r [10],[0.3] > random.gv```\n\n**Generate a directed cycle** and write to a file\n\n```gvgen -d -c [8] -o [cycle.gv]```"],["SYNOPSIS","**gvgen** [**-dv?**] [_-c n_] [_-C x,y_] [_-g[f] x,y_] [_-h n_] [_-k n_] [_-b x,y_] [_-B x,y_] [_-m n_] [_-M x,y_] [_-p n_] [_-r x,y_] [_-R x_] [_-s n_] [_-S n_] [_-t n_] [_-T x,y_] [_-w n_] [_-n prefix_] [_-N name_] [_-o outfile_]"],["PARAMETERS","**-s** _n_\n> Generate a star on n vertices.\n\n**-g** [**f**]_x_,_y_\n> Generate an x by y grid. If f is given, the grid is folded with edges attaching opposing corner vertices.\n\n**-k** _n_\n> Generate a complete graph on n vertices.\n\n**-c** _n_\n> Generate a cycle with n vertices and edges.\n\n**-p** _n_\n> Generate a path on n vertices.\n\n**-t** _n_\n> Generate a complete binary tree of height n. Use **-t** _h_,_n_ for an n-ary tree.\n\n**-r** _x_,_y_\n> Generate a random graph. The x and y give the number of vertices and the density of the edges (between 0 and 1).\n\n**-R** _x_\n> Generate a random rooted tree on x vertices.\n\n**-h** _n_\n> Generate a hypercube of degree n (2^n vertices).\n\n**-b** _x_,_y_\n> Generate a complete x by y bipartite graph.\n\n**-C** _x_,_y_\n> Generate an x by y cylinder.\n\n**-B** _x_,_y_\n> Generate an x by y ball (a cylinder with two caps).\n\n**-m** _n_\n> Generate a triangular mesh with n vertices on a side.\n\n**-M** _x_,_y_\n> Generate an x by y Moebius strip.\n\n**-T** _x_,_y_\n> Generate an x by y torus.\n\n**-S** _n_\n> Generate a Sierpinski graph of order n.\n\n**-w** _n_\n> Generate a wheel graph on n vertices.\n\n**-d**\n> Make the generated graph directed.\n\n**-n** _prefix_\n> Prepend prefix to integer node names.\n\n**-N** _name_\n> Use name as the name of the graph.\n\n**-o** _outfile_\n> Write output to the specified file instead of stdout.\n\n**-u** _seed_\n> Specify the seed for the random number generator.\n\n**-v**\n> Verbose output.\n\n**-?**\n> Print usage information."],["DESCRIPTION","**gvgen** generates a variety of simple, regularly-structured abstract graphs in DOT format. It creates standard graph structures useful for testing, visualization, and algorithm demonstrations.\n\nThe tool supports stars, grids, trees, complete graphs, cycles, paths, random graphs, hypercubes, bipartite graphs, cylinders, tori, Moebius strips, Sierpinski graphs, and more. Output is in Graphviz DOT format for visualization with layout engines."],["CAVEATS","Part of the Graphviz package. Random graphs vary each run unless a seed is specified with **-u**. Large graphs may be slow to render."],["HISTORY","gvgen was developed as part of the **Graphviz** project to provide sample graph generation for testing and demonstration."],["INSTALL","```apt: sudo apt install graphviz```\n\n```dnf: sudo dnf install graphviz```\n\n```pacman: sudo pacman -S graphviz```\n\n```apk: sudo apk add graphviz```\n\n```zypper: sudo zypper install graphviz```\n\n```brew: brew install graphviz```\n\n```nix: nix profile install nixpkgs#graphviz```"],["SEE ALSO","[dot](/man/dot)(1), [neato](/man/neato)(1), [sfdp](/man/sfdp)(1), [gvpack](/man/gvpack)(1)"]],"e":["dot","neato","sfdp","gvpack"],"x":"gvgen -s [5] > star.gv"},"kexec":{"n":"kexec","t":"allows the system to boot directly into a new kernel without going","s":[["TAGLINE","allows the system to boot directly into a new kernel without going"],["TLDR","**Load** a new kernel\n\n```kexec -l [path/to/kernel] --initrd=[path/to/initrd] --command-line=[arguments]```\n\nLoad kernel with **current boot parameters**\n\n```kexec -l [path/to/kernel] --initrd=[path/to/initrd] --reuse-cmdline```\n\n**Execute** the loaded kernel\n\n```kexec -e```\n\n**Unload** current kexec target kernel\n\n```kexec -u```"],["SYNOPSIS","**kexec** [_options_]"],["PARAMETERS","**-l**, **--load** _KERNEL_\n> Load a new kernel into memory\n\n**-e**, **--exec**\n> Execute the currently loaded kernel\n\n**-u**, **--unload**\n> Unload the loaded kernel\n\n**--initrd** _FILE_\n> Specify initramfs/initrd image\n\n**--command-line** _ARGS_\n> Kernel command line arguments\n\n**--reuse-cmdline**\n> Use current kernel's command line\n\n**-p**, **--load-panic**\n> Load kernel for crash dump (kdump)"],["DESCRIPTION","**kexec** allows the system to boot directly into a new kernel without going through the BIOS/UEFI and bootloader. This enables faster reboots and is essential for kdump (kernel crash dumping).\n\nThe process involves loading a kernel into memory with -l, then executing it with -e. This bypasses hardware initialization, significantly reducing reboot time."],["CAVEATS","Requires CONFIG_KEXEC in the kernel. Some hardware may not work correctly after kexec due to missing initialization. Not all drivers handle kexec transitions properly."],["HISTORY","kexec was developed to enable fast reboots and kernel crash dumping. It was merged into the Linux kernel in version 2.6.13 (**2005**) and has become essential for kdump functionality."],["INSTALL","```apt: sudo apt install kexec-tools```\n\n```dnf: sudo dnf install kexec-tools```\n\n```pacman: sudo pacman -S kexec-tools```\n\n```apk: sudo apk add kexec-tools```\n\n```zypper: sudo zypper install kexec-tools```\n\n```nix: nix profile install nixpkgs#kexec-tools```"],["SEE ALSO","[systemctl](/man/systemctl)(1), [reboot](/man/reboot)(8)"]],"e":["systemctl","reboot"],"x":"kexec -l [path/to/kernel] --initrd=[path/to/initrd] --command-line=[arguments]"},"keychain":{"n":"keychain","t":"manages SSH and GPG agent processes","s":[["TAGLINE","manages SSH and GPG agent processes"],["TLDR","**Start SSH agent**\n\n```eval $(keychain --eval [id_rsa])```\n\n**Add multiple keys**\n\n```eval $(keychain --eval [id_rsa] [id_ed25519])```\n\n**Include GPG keys**\n\n```eval $(keychain --eval --agents ssh,gpg [id_rsa] [GPGKEY])```\n\n**Clear cached keys**\n\n```keychain --clear```\n\n**Quiet mode**\n\n```eval $(keychain -q --eval [id_rsa])```\n\n**Show status**\n\n```keychain -l```"],["SYNOPSIS","**keychain** [_options_] [_keys_...]"],["PARAMETERS","_KEYS_\n> SSH or GPG keys to manage.\n\n**--eval**\n> Output shell eval commands.\n\n**--agents** _LIST_\n> Agent types (ssh, gpg).\n\n**--clear**\n> Clear cached keys.\n\n**-q**, **--quiet**\n> Suppress output.\n\n**-l**, **--list**\n> List cached keys.\n\n**--noask**\n> Set up agent files but do not prompt to add keys if not already loaded.\n\n**--stop** _which_\n> Kill agent processes. Values: **mine** (keychain's agents), **others**, or **all**.\n\n**--timeout** _MINUTES_\n> Set a timeout in minutes for identities added to ssh-agent.\n\n**--nogui**\n> Disable SSH_ASKPASS, forcing ssh-add to prompt on the terminal.\n\n**--ignore-missing**\n> Suppress warnings if specified keys cannot be found.\n\n**--help**\n> Display help information."],["DESCRIPTION","**keychain** is a front-end to ssh-agent and gpg-agent that manages long-running agent processes across login sessions. Rather than starting a new agent each time a shell is opened, keychain checks for an existing agent, reuses it if found, and only starts a new one when necessary, storing the agent's environment variables in files under `~/.keychain` so any shell can inherit them.\n\nThis approach means you only need to enter your passphrase once after a reboot, and all subsequent terminal sessions, cron jobs, and scripts can use the cached keys without further prompting. The tool supports managing both SSH and GPG keys simultaneously via the `--agents` flag and outputs shell-evaluable commands that set the appropriate `SSH_AUTH_SOCK` and `GPG_AGENT_INFO` environment variables."],["CAVEATS","Requires shell integration by adding an `eval $(keychain --eval ...)` line to your shell profile (e.g., `.bash_profile` or `.zshrc`). Agent environment variables are stored in `~/.keychain/`."],["HISTORY","keychain was created by **Daniel Robbins** (Funtoo) to simplify SSH agent management across sessions."],["INSTALL","```dnf: sudo dnf install keychain```\n\n```pacman: sudo pacman -S keychain```\n\n```apk: sudo apk add keychain```\n\n```zypper: sudo zypper install keychain```\n\n```brew: brew install keychain```\n\n```nix: nix profile install nixpkgs#keychain```"],["SEE ALSO","[ssh-agent](/man/ssh-agent)(1), [ssh-add](/man/ssh-add)(1), [ssh](/man/ssh)(1), [gpg](/man/gpg)(1)"]],"e":["ssh-agent","ssh-add","ssh","gpg"],"x":"eval $(keychain --eval [id_rsa])"},"kool":{"n":"kool","t":"simplifies Docker-based development environments","s":[["TAGLINE","simplifies Docker-based development environments"],["TLDR","**Start environment**\n\n```kool start```\n\n**Stop environment**\n\n```kool stop```\n\n**Run command in service**\n\n```kool run [service] [command]```\n\n**Execute in running container**\n\n```kool exec [service] [command]```\n\n**View logs**\n\n```kool logs [service]```\n\n**Create new project from a preset**\n\n```kool create [preset] [project-name]```\n\n**Check running container status**\n\n```kool status```\n\n**Update kool to the latest version**\n\n```kool self-update```"],["SYNOPSIS","**kool** _command_ [_options_]"],["PARAMETERS","**start**\n> Start development environment containers.\n\n**stop**\n> Stop environment containers.\n\n**run** _SERVICE_ _CMD_\n> Run command in a service container.\n\n**exec** _SERVICE_ _CMD_\n> Execute command in a running container.\n\n**logs** _SERVICE_\n> View service logs.\n\n**create** _PRESET_ _NAME_\n> Create project from a preset (e.g. laravel, nextjs, hugo).\n\n**status**\n> Show running container status, ports, and state.\n\n**self-update**\n> Update kool to the latest version.\n\n**preset** _NAME_\n> Auto-generate config files for a framework preset.\n\n**docker** _IMAGE_ [_CMD_]\n> Helper for `docker run` with simplified options.\n\n**--verbose**\n> Increase output verbosity.\n\n**--help**\n> Display help information."],["DESCRIPTION","**kool** simplifies Docker-based development environments. It wraps Docker Compose with developer-friendly commands and provides presets for common frameworks like Laravel, Next.js, Hugo, and Express.js.\n\nThe tool standardizes workflows across projects with a `kool.yml` configuration file."],["CAVEATS","Requires Docker. Presets for common stacks. Wrapper around Docker Compose."],["HISTORY","kool was created to streamline Docker-based development workflows with simpler commands and framework presets."],["INSTALL","```brew: brew install kool```\n\n```nix: nix profile install nixpkgs#kool```"],["SEE ALSO","[docker](/man/docker)(1), [docker-compose](/man/docker-compose)(1), [ddev](/man/ddev)(1)"]],"e":["docker","docker-compose","ddev"],"x":"kool start"},"kotlin":{"n":"kotlin","t":"Run Kotlin scripts and provide a REPL","s":[["TAGLINE","Run Kotlin scripts and provide a REPL"],["TLDR","**Run a Kotlin script**\n\n```kotlin script.kts```\n\n**Run a script with arguments**\n\n```kotlin script.kts [arg1] [arg2]```\n\n**Start the interactive REPL**\n\n```kotlin```\n\n**Evaluate an expression**\n\n```kotlin -e \"println(\\\"Hello\\\")\"```\n\n**Run a script with additional classpath JARs**\n\n```kotlin -cp [lib.jar] script.kts```\n\n**Pass JVM options** (e.g., increase memory)\n\n```kotlin -J-Xmx512m script.kts```"],["SYNOPSIS","**kotlin** [_options_] _script_ [_args_...]"],["PARAMETERS","_SCRIPT_\n> Kotlin script file (.kts).\n\n**-e** _EXPR_\n> Evaluate expression.\n\n**-cp** _PATH_\n> Classpath.\n\n**-J** _ARG_\n> Pass argument to JVM (e.g., -J-Xmx512m).\n\n**-nowarn**\n> Suppress warning messages.\n\n**-version**\n> Display the Kotlin version.\n\n**--help**\n> Display help information."],["DESCRIPTION","**kotlin** is the Kotlin runtime command that executes Kotlin scripts (.kts files) directly on the JVM without requiring a separate compilation step. It also provides an interactive Read-Eval-Print Loop (REPL) when invoked with no arguments, allowing you to experiment with Kotlin expressions and statements interactively.\n\nThe tool can evaluate single expressions from the command line using the `-e` flag, run script files with command-line arguments, and include additional JARs on the classpath for access to external libraries. Since Kotlin scripts run on the JVM, they have full access to the Java standard library and any Java or Kotlin libraries added to the classpath, making it suitable for automation tasks, quick prototyping, and build scripting."],["CAVEATS","Requires JVM. Scripts use .kts extension. Slower startup than compiled code."],["HISTORY","Kotlin was created by **JetBrains** in 2011. The kotlin command runs scripts and provides the REPL."],["INSTALL","```pacman: sudo pacman -S kotlin```\n\n```brew: brew install kotlin```\n\n```nix: nix profile install nixpkgs#kotlin```"],["SEE ALSO","[kotlinc](/man/kotlinc)(1), [java](/man/java)(1), [gradle](/man/gradle)(1)"]],"e":["kotlinc","java","gradle"],"x":"kotlin script.kts"},"kubectl-drain":{"n":"kubectl-drain","t":"evicts pods from a node for maintenance","s":[["TAGLINE","evicts pods from a node for maintenance"],["TLDR","**Drain node**\n\n```kubectl drain [node-name]```\n\n**Drain ignoring DaemonSets**\n\n```kubectl drain [node-name] --ignore-daemonsets```\n\n**Drain with force**\n\n```kubectl drain [node-name] --force```\n\n**Drain and delete local data**\n\n```kubectl drain [node-name] --delete-emptydir-data```\n\n**Drain with timeout**\n\n```kubectl drain [node-name] --timeout=[5m]```\n\n**Dry run drain**\n\n```kubectl drain [node-name] --dry-run=client```"],["SYNOPSIS","**kubectl drain** [_options_] _node_"],["PARAMETERS","_NODE_\n> Node name to drain.\n\n**--ignore-daemonsets**\n> Ignore DaemonSet pods.\n\n**--force**\n> Continue despite errors.\n\n**--delete-emptydir-data**\n> Delete emptyDir data.\n\n**--timeout** _DURATION_\n> Drain timeout.\n\n**--grace-period** _N_\n> Pod termination grace.\n\n**--help**\n> Display help information."],["DESCRIPTION","**kubectl drain** safely evicts all pods from a node in preparation for maintenance operations such as kernel upgrades, hardware repairs, or Kubernetes version updates. It first cordons the node to prevent new pods from being scheduled, then uses the Eviction API to gracefully terminate each pod, allowing their controllers to reschedule them onto other available nodes.\n\nThe command respects PodDisruptionBudgets (PDBs), which means it will block if evicting a pod would violate the application's availability requirements. DaemonSet-managed pods are not evicted by default since they are expected to run on every node; use `--ignore-daemonsets` to proceed without error. Pods with local storage (emptyDir volumes) also require explicit acknowledgment via `--delete-emptydir-data`, as their data will be lost.\n\nAfter maintenance is complete, the node must be uncordoned with `kubectl uncordon` to resume accepting new pod scheduling. A `--timeout` flag allows setting a maximum wait time for the drain to complete before it is aborted."],["CAVEATS","Subcommand of kubectl. Respects PDBs. Use uncordon to restore."],["HISTORY","kubectl drain provides safe node evacuation for **Kubernetes** cluster maintenance operations."],["INSTALL","```apt: sudo apt install kubectl```\n\n```pacman: sudo pacman -S kubectl```\n\n```apk: sudo apk add kubectl```\n\n```brew: brew install kubectl```\n\n```nix: nix profile install nixpkgs#kubectl```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [kubectl-cordon](/man/kubectl-cordon)(1), [kubectl-uncordon](/man/kubectl-uncordon)(1)"]],"e":["kubectl","kubectl-cordon","kubectl-uncordon"],"x":"kubectl drain [node-name]"},"kubie":{"n":"kubie","t":"manages Kubernetes contexts with isolated shells","s":[["TAGLINE","manages Kubernetes contexts with isolated shells"],["TLDR","**Switch context**\n\n```kubie ctx [context]```\n\n**Switch namespace**\n\n```kubie ns [namespace]```\n\n**List contexts**\n\n```kubie ctx```\n\n**List namespaces**\n\n```kubie ns```\n\n**Spawn shell in context**\n\n```kubie ctx [context] -n [namespace]```\n\n**Execute a command** in a specific context without spawning a shell\n\n```kubie exec [context] [namespace] -- [kubectl get pods]```\n\n**Show current context info**\n\n```kubie info ctx```\n\n**Exit kubie shell**\n\n```exit```"],["SYNOPSIS","**kubie** _command_ [_options_]"],["PARAMETERS","**ctx** [_CONTEXT_]\n> Switch or list contexts.\n\n**ns** [_NAMESPACE_]\n> Switch or list namespaces.\n\n**-n** _NAMESPACE_\n> Specify namespace.\n\n**exec** _CONTEXT_ _NAMESPACE_ **--** _CMD_\n> Execute command in context without spawning a shell.\n\n**info** _ITEM_\n> Show current context (ctx) or namespace (ns).\n\n**--help**\n> Display help information."],["DESCRIPTION","**kubie** is a Kubernetes context and namespace manager that isolates each context switch in its own shell session. Unlike kubectx, which modifies the global kubeconfig state, kubie spawns a new subshell with environment variables pointing to a temporary kubeconfig, so each terminal window can independently operate against a different cluster and namespace without interfering with others.\n\nThis isolation model prevents a common source of errors in multi-cluster workflows where a context switch in one terminal unexpectedly affects commands running in another. Kubie can also list available contexts and namespaces, execute one-off commands against a specific context, and integrates with fzf for interactive selection. It is written in Rust for fast startup and minimal overhead."],["CAVEATS","Each context switch spawns a subshell, so deeply nested switches increase shell depth. Requires kubeconfig files to be accessible. Supports fzf for interactive selection if installed."],["HISTORY","kubie was created to provide isolated context management avoiding the issues of global context switching."],["INSTALL","```pacman: sudo pacman -S kubie```\n\n```apk: sudo apk add kubie```\n\n```zypper: sudo zypper install kubie```\n\n```brew: brew install kubie```\n\n```nix: nix profile install nixpkgs#kubie```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [kubectx](/man/kubectx)(1), [kubens](/man/kubens)(1)"]],"e":["kubectl","kubectx","kubens"],"x":"kubie ctx [context]"},"legionlinuxtui":{"n":"legionlinuxtui","t":"Terminal UI for controlling Lenovo Legion laptops on Linux","s":[["TAGLINE","Terminal UI for controlling Lenovo Legion laptops on Linux"],["TLDR","**Run** the TUI (needs root for sysfs/driver I/O)\n\n```sudo legionlinuxtui```\n\n**Build** from source and run\n\n```go build -o build/```\n\n```sudo ./build/legionlinuxtui```\n\n**Run** without installing (from a checkout)\n\n```sudo go run .```"],["SYNOPSIS","**legionlinuxtui**"],["DESCRIPTION","**legionlinuxtui** is a terminal user interface for Lenovo **Legion** laptops on Linux. It reads sensor data and exposes capability toggles (for example conservative mode and power mode) through a full-screen TUI, refreshing sensor information about every 100 ms.\n\nIt depends on the community **Lenovo Legion Linux** kernel drivers (sysfs interfaces) and on **lm_sensors** being set up correctly on the machine. The process needs elevated privileges to talk to drivers and write sysfs values — invoke with **sudo** (or equivalent).\n\nWritten in Go. Packaging is available via Nix (NUR / flake packages from the author); otherwise build with Go ≥ 1.24.4 from the repository."],["PARAMETERS","No command-line options are required. Launch the binary; interaction is entirely through the TUI."],["CAVEATS","Requires Lenovo Legion hardware supported by [LenovoLegionLinux](https://github.com/johnfanv2/LenovoLegionLinux) drivers, plus a working **sensors** setup. Must run with privileges sufficient for driver/sysfs I/O. Not a generic laptop control tool for non-Legion machines."],["HISTORY","**legionlinuxtui** is an open-source Go TUI maintained at **github.com/nooneknowspeter/legionlinuxtui** under the MIT license."],["SEE ALSO","[sensors](/man/sensors)(1), [sensors-detect](/man/sensors-detect)(8), [amdgpu_top](/man/amdgpu_top)(1), [nvtop](/man/nvtop)(1)"],["RESOURCES","```[Source code](https://github.com/nooneknowspeter/legionlinuxtui)```"]],"e":["sensors","sensors-detect","amdgpu_top","nvtop"],"x":"sudo legionlinuxtui"},"links":{"n":"links","t":"text-mode web browser for the terminal","s":[["TAGLINE","text-mode web browser for the terminal"],["TLDR","**Visit** a website\n\n```links https://example.com```\n\n**Dump** rendered page as plain text to stdout\n\n```links -dump https://example.com```\n\n**Dump** with a specific screen **width**\n\n```links -width 80 -dump https://example.com```\n\nBrowse in **anonymous** mode\n\n```links -anonymous https://example.com```\n\nView page **source** code\n\n```links -source https://example.com```"],["SYNOPSIS","**links** [_options_] [_url_]"],["DESCRIPTION","**links** is a text-mode web browser for the terminal. It supports tables, frames, and basic forms rendering, making it useful for browsing documentation or using the web from servers without a graphical environment."],["PARAMETERS","**-anonymous**\n> Apply restrictions for anonymous browsing (no local file access or downloads)\n\n**-dump**\n> Write rendered page as plain text to stdout\n\n**-source**\n> Write page source to stdout\n\n**-width** _number_\n> Set screen width for dump output\n\n**-codepage** _codepage_\n> Convert dump output to specified character set\n\n**-enable-cookies** _0|1_\n> Enable (1) or disable (0) cookies\n\n**-force-html**\n> Treat files with unknown type as HTML rather than text\n\n**-html-numbered-links** _0|1_\n> Number links in text mode for quick selection\n\n**-html-margin** _spaces_\n> Set margin width in text mode\n\n**-no-connect**\n> Run as a separate instance instead of connecting to an existing one\n\n**-download-dir** _path_\n> Set default download directory\n\n**-max-connections** _max_\n> Maximum concurrent connections (default: 10)\n\n**-receive-timeout** _sec_\n> Timeout on receive in seconds (default: 120)"],["NAVIGATION KEYS","**Up/Down**\n> Scroll page\n\n**Tab/Shift+Tab**\n> Navigate between links\n\n**Left/Right**\n> Go back/forward in history\n\n**Enter**\n> Follow selected link\n\n**g**\n> Go to URL\n\n**/**\n> Search text on page\n\n**Esc**\n> Open menu\n\n**d**\n> Download current link\n\n**q**\n> Quit browser"],["INSTALL","```apt: sudo apt install links```\n\n```dnf: sudo dnf install links```\n\n```pacman: sudo pacman -S links```\n\n```apk: sudo apk add links```\n\n```zypper: sudo zypper install links```\n\n```brew: brew install links```"],["SEE ALSO","[links2](/man/links2)(1), [lynx](/man/lynx)(1), [w3m](/man/w3m)(1)"]],"e":["links2","lynx","w3m"],"x":"links https://example.com"},"llm":{"n":"llm","t":"CLI for interacting with large language models","s":[["TAGLINE","CLI for interacting with large language models"],["TLDR","**Chat with default model**\n\n```llm \"[prompt]\"```\n\n**Use specific model**\n\n```llm -m [gpt-4o] \"[prompt]\"```\n\n**Interactive chat session**\n\n```llm chat```\n\n**Continue most recent conversation**\n\n```llm -c \"[follow up]\"```\n\n**List models** (built-in and from plugins)\n\n```llm models```\n\n**Pipe input** to a model\n\n```cat [file.txt] | llm \"[summarize this]\"```\n\n**Use a system prompt**\n\n```llm -s \"[Reply as a pirate]\" \"[Hi there]\"```\n\n**Set an API key**\n\n```llm keys set [openai]```\n\n**Install a plugin** (e.g. Claude support)\n\n```llm install [llm-claude-3]```"],["SYNOPSIS","**llm** [_options_] [_prompt_]\n\n**llm** _subcommand_ [_options_]"],["PARAMETERS","_PROMPT_\n> Text prompt for the model.\n\n**-m** _MODEL_, **--model** _MODEL_\n> Model to use (e.g. **gpt-4o**, **claude-3-5-sonnet**, **llama2**).\n\n**-c**, **--continue**\n> Continue the most recent conversation.\n\n**--cid** _ID_\n> Continue a specific conversation by id.\n\n**-s** _PROMPT_, **--system** _PROMPT_\n> Set a system prompt.\n\n**-t** _NAME_, **--template** _NAME_\n> Use a named prompt template.\n\n**-o** _KEY_ _VALUE_, **--option** _KEY_ _VALUE_\n> Pass a model-specific option (e.g. **-o temperature 0**).\n\n**-a** _PATH_, **--attachment** _PATH_\n> Attach a file (image, PDF, audio) to the prompt for models that support attachments.\n\n**--no-stream**\n> Disable token streaming and only print the final answer.\n\n**--key** _KEY_\n> Use a specific API key for this call."],["SUBCOMMANDS","**chat**\n> Open an interactive chat session.\n\n**models** [**list** | **default** _name_]\n> List available models or set the default model.\n\n**keys** [**set** _key_]\n> Manage API keys.\n\n**logs**\n> Show or query the persistent log of prompts and responses.\n\n**templates**\n> Manage reusable prompt templates.\n\n**install** _package_ / **uninstall** _package_\n> Install or remove an llm plugin (e.g. **llm-claude-3**, **llm-ollama**).\n\n**embed**, **embed-multi**, **similar**\n> Generate and search text embeddings."],["DESCRIPTION","**llm** is a CLI and Python library for interacting with large language models. Out of the box it talks to OpenAI's API; through plugins it also supports Anthropic Claude, Google Gemini, Mistral, local **Ollama**, **llama.cpp** / GGUF, and many other providers and self-hosted models.\n\nEvery prompt and response is logged to a local **SQLite** database (under **~/.config/io.datasette.llm/** on Linux or **~/Library/Application Support/io.datasette.llm/** on macOS) so previous conversations can be searched, exported, and replayed. The tool also supports prompt templates, system prompts, embeddings, similarity search, and pluggable backends."],["CAVEATS","Cloud models require API keys stored via **llm keys set**. Conversation logs are stored in plaintext SQLite, so be mindful of secrets in prompts. Provider support beyond OpenAI requires installing additional plugins (e.g. **llm install llm-claude-3**)."],["HISTORY","**llm** was created by **Simon Willison** to provide unified CLI access to language model APIs and local models, and to make it easy to log and query past prompts."],["INSTALL","```brew: brew install llm```\n\n```nix: nix profile install nixpkgs#llm```"],["SEE ALSO","[ollama](/man/ollama)(1), [chatgpt](/man/chatgpt)(1)"]],"e":["ollama","chatgpt"],"x":"llm \"[prompt]\""},"locale":{"n":"locale","t":"displays information about the current locale settings or lists available","s":[["TAGLINE","displays information about the current locale settings or lists available"],["TLDR","Show current **locale settings**\n\n```locale```\n\nList **all available** locales\n\n```locale --all-locales```\n\nList locales with **metadata**\n\n```locale --all-locales --verbose```\n\nShow **date format**\n\n```locale date_fmt```"],["SYNOPSIS","**locale** [_options_] [_name_...]"],["DESCRIPTION","**locale** displays information about the current locale settings or lists available locales. It shows environment variables that control language, character encoding, date/time formats, and other regional settings."],["PARAMETERS","**-a, --all-locales**\n> List all available locales\n\n**-v, --verbose**\n> Show verbose locale information\n\n**-c, --category-name**\n> Print category names\n\n**-k, --keyword-name**\n> Print keyword names\n\n**-m, --charmaps**\n> List available character maps"],["LOCALE CATEGORIES","**LC_CTYPE**\n> Character classification and case conversion\n\n**LC_COLLATE**\n> String collation order\n\n**LC_MESSAGES**\n> Message translations\n\n**LC_MONETARY**\n> Currency formatting\n\n**LC_NUMERIC**\n> Number formatting\n\n**LC_TIME**\n> Date and time formatting"],["INSTALL","```apt: sudo apt install libc-bin```\n\n```apk: sudo apk add musl-locales```\n\n```nix: nix profile install nixpkgs#locale```"],["SEE ALSO","[localectl](/man/localectl)(1), [locale-gen](/man/locale-gen)(8), [localedef](/man/localedef)(1)"]],"e":["localectl","locale-gen","localedef"],"x":"locale"},"lsipc":{"n":"lsipc","t":"displays information about System V IPC facilities including shared memory","s":[["TAGLINE","displays information about System V IPC facilities including shared memory"],["TLDR","Show **all IPC** facilities\n\n```lsipc```\n\nShow **shared memory** segments\n\n```lsipc --shmems```\n\nShow **message queues**\n\n```lsipc --queues```\n\nShow **semaphore** sets\n\n```lsipc --semaphores```\n\nShow details for **specific ID**\n\n```lsipc --shmems --id [resource_id]```\n\nOutput as **JSON**\n\n```lsipc --json```"],["SYNOPSIS","**lsipc** [_options_]"],["DESCRIPTION","**lsipc** displays information about System V IPC (Inter-Process Communication) facilities including shared memory segments, message queues, and semaphore sets currently active on the system."],["PARAMETERS","**--shmems, -m**\n> Show shared memory segments\n\n**--queues, -q**\n> Show message queues\n\n**--semaphores, -s**\n> Show semaphore sets\n\n**-i, --id ID**\n> Show details for specific resource ID\n\n**-o, --output COLUMNS**\n> Select output columns\n\n**-J, --json**\n> JSON output format\n\n**-r, --raw**\n> Raw output without formatting\n\n**--notruncate**\n> Don't truncate output"],["INSTALL","```apt: sudo apt install util-linux```\n\n```dnf: sudo dnf install util-linux```\n\n```pacman: sudo pacman -S util-linux```\n\n```apk: sudo apk add util-linux-misc```\n\n```zypper: sudo zypper install util-linux```\n\n```brew: brew install util-linux```\n\n```nix: nix profile install nixpkgs#util-linux```"],["SEE ALSO","[ipcs](/man/ipcs)(1), [ipcrm](/man/ipcrm)(1), [ipcmk](/man/ipcmk)(1)"]],"e":["ipcs","ipcrm","ipcmk"],"x":"lsipc"},"luac":{"n":"luac","t":"compiles Lua source code into bytecode","s":[["TAGLINE","compiles Lua source code into bytecode"],["TLDR","**Compile Lua script to bytecode**\n\n```luac -o [output.luac] [script.lua]```\n\n**Compile multiple scripts**\n\n```luac -o [combined.luac] [script1.lua] [script2.lua]```\n\n**Parse and syntax check only**\n\n```luac -p [script.lua]```\n\n**List compiled bytecode**\n\n```luac -l [script.lua]```\n\n**List with full debug info**\n\n```luac -l -l [script.lua]```\n\n**Strip debug info from output**\n\n```luac -s -o [stripped.luac] [script.lua]```\n\n**Show version**\n\n```luac -v```"],["SYNOPSIS","**luac** [_-o output_] [_-l_] [_-p_] [_-s_] [_files_]"],["PARAMETERS","**-o** _FILE_\n> Output compiled bytecode to file.\n\n**-l**\n> List bytecode (use twice for more detail).\n\n**-p**\n> Parse only, don't generate output.\n\n**-s**\n> Strip debug information.\n\n**-v**\n> Print version.\n\n**--**\n> Stop handling options."],["DESCRIPTION","**luac** compiles Lua source code into bytecode. The compiled output loads faster and can be distributed without source code.\n\nSyntax checking with -p validates scripts without creating output. This is useful in build processes and CI pipelines to catch errors early.\n\nThe listing option (-l) shows the bytecode instructions, helping understand Lua's virtual machine and debug compilation issues. Double -l adds local variable and upvalue information.\n\nStripping debug info (-s) reduces file size but removes line numbers and local variable names, making debugging harder. Use for production deployment where size matters.\n\nMultiple input files combine into a single output chunk. When executed, each file's code runs in order.\n\nBytecode format is version-specific and not portable between different Lua versions. Lua 5.1, 5.2, 5.3, and 5.4 have incompatible bytecode formats."],["CAVEATS","Bytecode not portable between Lua versions. Stripping debug info prevents meaningful stack traces. Endianness and integer size may affect portability. LuaJIT uses different bytecode format."],["HISTORY","**luac** has been part of **Lua** since early versions, developed at **PUC-Rio** (Pontifical Catholic University of Rio de Janeiro) in Brazil. Lua was created by **Roberto Ierusalimschy**, **Waldemar Celes**, and **Luiz Henrique de Figueiredo** starting in **1993**. The compiler remains the standard tool for pre-compiling Lua code."],["INSTALL","```apk: sudo apk add lua5.1```"],["SEE ALSO","[lua](/man/lua)(1), [luajit](/man/luajit)(1)"]],"e":["lua","luajit"],"x":"luac -o [output.luac] [script.lua]"},"machine-id":{"n":"machine-id","t":"File containing a unique machine identifier","s":[["TAGLINE","File containing a unique machine identifier"],["TLDR","**View machine ID**\n\n```cat /etc/machine-id```\n\n**Generate new machine ID**\n\n```systemd-machine-id-setup```\n\n**Check if ID exists**\n\n```test -f /etc/machine-id && echo \"exists\"```\n\n**Compare machine IDs**\n\n```diff /etc/machine-id /var/lib/dbus/machine-id```"],["SYNOPSIS","**/etc/machine-id**"],["DESCRIPTION","**machine-id** is a file containing a unique machine identifier established during installation or first boot. It's a 32-character lowercase hexadecimal string without hyphens.\n\nThe ID is used by systemd, D-Bus, and other services to uniquely identify the system. It should be stable across reboots but unique per machine."],["FILE LOCATIONS","```\n/etc/machine-id          - Primary location\n/var/lib/dbus/machine-id - D-Bus symlink\n```"],["GENERATION","```bash"],["Regenerate (for cloned systems)","rm /etc/machine-id\nsystemd-machine-id-setup"],["Or manually","dbus-uuidgen --ensure=/etc/machine-id\n```"],["CAVEATS","Should be unique per machine. Clone VMs need new IDs. Some software uses it for licensing. Not suitable as cryptographic secret."],["HISTORY","machine-id was introduced by **systemd** to provide a standard machine identification mechanism, replacing various distribution-specific solutions."],["SEE ALSO","[systemd-machine-id-setup](/man/systemd-machine-id-setup)(1), [hostnamectl](/man/hostnamectl)(1)"]],"e":["systemd-machine-id-setup","hostnamectl"],"x":"cat /etc/machine-id"},"minicom":{"n":"minicom","t":"text-based serial port communication program for Unix-like systems","s":[["TAGLINE","text-based serial port communication program for Unix-like systems"],["TLDR","Open **serial port**\n\n```sudo minicom -D /dev/ttyUSB0```\n\nOpen with **baud rate**\n\n```sudo minicom -D /dev/ttyUSB0 -b 115200```\n\nOpen **configuration** menu\n\n```sudo minicom -D /dev/ttyUSB0 --setup```\n\n**Capture** output to file\n\n```sudo minicom -D /dev/ttyUSB0 -C [path/to/file]```\n\nDisplay **help**\n\n```minicom --help```"],["SYNOPSIS","**minicom** [_options_]"],["DESCRIPTION","**minicom** is a text-based serial port communication program for Unix-like systems. It is commonly used to communicate with embedded devices, routers, and other serial-enabled hardware."],["PARAMETERS","**-D, --device DEVICE**\n> Serial device to open\n\n**-b, --baudrate BAUD**\n> Baud rate (e.g., 9600, 115200)\n\n**-s, --setup**\n> Enter configuration menu\n\n**-C, --capturefile FILE**\n> Capture output to file\n\n**-o, --noinit**\n> Skip modem initialization\n\n**-w, --wrap**\n> Enable line wrapping\n\n**-H, --displayhex**\n> Display output in hexadecimal\n\n**-S, --script SCRIPT**\n> Run script at startup\n\n**-7, --7bit**\n> Force 7bit mode\n\n**-8, --8bit**\n> Force 8bit mode\n\n**-h, --help**\n> Display help information"],["KEYBOARD SHORTCUTS","**Ctrl+A X**: Exit minicom\n**Ctrl+A Z**: Help menu\n**Ctrl+A S**: Send file\n**Ctrl+A L**: Log capture toggle\n**Ctrl+A O**: Configure minicom\n**Ctrl+A W**: Toggle line wrap"],["CAVEATS","Requires root or dialout group membership. Configuration stored in /etc/minicom/ or ~/.minirc.*."],["INSTALL","```apt: sudo apt install minicom```\n\n```dnf: sudo dnf install minicom```\n\n```pacman: sudo pacman -S minicom```\n\n```apk: sudo apk add minicom```\n\n```zypper: sudo zypper install minicom```\n\n```brew: brew install minicom```\n\n```nix: nix profile install nixpkgs#minicom```"],["SEE ALSO","[screen](/man/screen)(1), [picocom](/man/picocom)(1), [cu](/man/cu)(1)"]],"e":["screen","picocom","cu"],"x":"sudo minicom -D /dev/ttyUSB0"},"mkfs.fat":{"n":"mkfs.fat","t":"creates MS-DOS FAT filesystems on devices or image files","s":[["TAGLINE","creates MS-DOS FAT filesystems on devices or image files"],["TLDR","Create a **FAT filesystem** inside partition Y on device X\n\n```sudo mkfs.fat [/dev/sdXY]```\n\nCreate a FAT filesystem with a **volume name**\n\n```sudo mkfs.fat -n [volume_name] [/dev/sdXY]```\n\nCreate a FAT filesystem with a specific **volume ID**\n\n```sudo mkfs.fat -i [volume_id] [/dev/sdXY]```\n\nSpecify the **FAT type** (12, 16, or 32 bit)\n\n```sudo mkfs.fat -F [12|16|32] [/dev/sdXY]```\n\nUse **4 file allocation tables** instead of the default 2\n\n```sudo mkfs.fat -f 4 [/dev/sdXY]```\n\n**Check device** for bad blocks before creating filesystem\n\n```sudo mkfs.fat -c [/dev/sdXY]```"],["SYNOPSIS","**mkfs.fat** [**-a**] [**-A**] [**-c**] [**-C**] [**-f** _number-of-fats_] [**-F** _fat-size_] [**-i** _volume-id_] [**-l** _bad-block-file_] [**-n** _volume-name_] [**-r** _root-entries_] [**-R** _reserved-sectors_] [**-s** _sectors-per-cluster_] [**-S** _sector-size_] [**-v**] _device_ [_block-count_]"],["PARAMETERS","**-F _fat-size_**\n> Specify FAT type: 12, 16, or 32 bit; auto-selected based on size if omitted\n\n**-n _volume-name_**\n> Set volume label; maximum 11 characters\n\n**-i _volume-id_**\n> Set 32-bit hexadecimal volume ID\n\n**-f _number_**\n> Number of file allocation tables; default is 2\n\n**-s _sectors_**\n> Sectors per cluster; must be a power of 2\n\n**-S _size_**\n> Logical sector size in bytes (512, 1024, 2048, 4096, 8192, 16384, or 32768)\n\n**-c**\n> Check device for bad blocks before creating filesystem\n\n**-l _filename_**\n> Read bad blocks list from file\n\n**-r _entries_**\n> Number of root directory entries; default 112-512 depending on FAT type\n\n**-R _sectors_**\n> Number of reserved sectors; default 1 for FAT12/16, 32 for FAT32\n\n**-a**\n> Disable alignment of data structures\n\n**-A**\n> Toggle Atari filesystem variant\n\n**-C**\n> Create image file instead of using device; requires block-count\n\n**-v**\n> Verbose output mode\n\n**-b _sector_**\n> Location of backup boot sector for FAT32\n\n**-D _number_**\n> BIOS drive number; 0x80+ for hard disks, 0x00-0x7F for floppies\n\n**-I**\n> Ignore safety checks; required for superfloppy format disks"],["DESCRIPTION","**mkfs.fat** creates MS-DOS FAT filesystems on devices or image files. It supports FAT12, FAT16, and FAT32 variants used by DOS, Windows, and most consumer electronics.\n\nThe block-count parameter specifies filesystem size in 1024-byte blocks. When omitted, the tool fills available device space. FAT type is automatically selected based on filesystem size unless explicitly specified with -F.\n\nFAT filesystems use a file allocation table to track cluster allocation. Multiple copies of this table (default 2) provide redundancy. The filesystem remains widely used for USB drives, SD cards, and EFI system partitions due to its universal compatibility."],["CAVEATS","Volume names are limited to 11 characters. The tool does not create bootable filesystems. It refuses to format partitioned devices by default; use -I to override this safety check for superfloppy format disks. FAT32 cannot store files larger than 4GB."],["HISTORY","The FAT filesystem was developed by **Microsoft** starting in **1977** for standalone Disk BASIC. FAT12 appeared with MS-DOS 1.0 in 1981, FAT16 with MS-DOS 3.0 in 1984, and FAT32 with Windows 95 OSR2 in 1996. The mkfs.fat utility is part of the **dosfstools** package maintained for Linux."],["INSTALL","```apt: sudo apt install dosfstools```\n\n```dnf: sudo dnf install dosfstools```\n\n```pacman: sudo pacman -S dosfstools```\n\n```apk: sudo apk add dosfstools```\n\n```zypper: sudo zypper install dosfstools```\n\n```brew: brew install dosfstools```\n\n```nix: nix profile install nixpkgs#dosfstools```"],["SEE ALSO","[mkfs](/man/mkfs)(8), [fatlabel](/man/fatlabel)(8), [mkfs.ext4](/man/mkfs.ext4)(8)"]],"e":["mkfs","fatlabel","mkfs.ext4"],"x":"sudo mkfs.fat [/dev/sdXY]"},"nmblookup":{"n":"nmblookup","t":"queries NetBIOS names and resolves them to IP addresses","s":[["TAGLINE","queries NetBIOS names and resolves them to IP addresses"],["TLDR","**Lookup NetBIOS name**\n\n```nmblookup [hostname]```\n\n**Lookup with broadcast**\n\n```nmblookup -B [192.168.1.255] [hostname]```\n\n**Query specific WINS server**\n\n```nmblookup -U [wins_server] [hostname]```\n\n**Lookup IP address** (reverse lookup)\n\n```nmblookup -A [192.168.1.100]```\n\n**Show master browser**\n\n```nmblookup -M -- -```\n\n**List workgroup members**\n\n```nmblookup -S [WORKGROUP]```\n\n**Recursive query**\n\n```nmblookup -R [hostname]```"],["SYNOPSIS","**nmblookup** [_-B broadcast_] [_-U unicast_] [_-A_] [_-M_] [_options_] _name_"],["PARAMETERS","**-B** _ADDR_\n> Broadcast address.\n\n**-U** _ADDR_\n> Unicast address (WINS server).\n\n**-A**\n> Node status query (reverse lookup).\n\n**-M**\n> Search for master browser.\n\n**-R**\n> Recursive query through WINS.\n\n**-S**\n> Return full node status.\n\n**-d** _LEVEL_\n> Debug level.\n\n**-s** _FILE_\n> Smb.conf file location.\n\n**-r**\n> Use /etc/hosts.\n\n**-T**\n> Translate to DNS names.\n\n**--usage**\n> Show usage."],["DESCRIPTION","**nmblookup** queries NetBIOS names and resolves them to IP addresses. It's the NetBIOS equivalent of nslookup for DNS.\n\nNetBIOS name resolution predates DNS in Windows networks. Machines register names on the network, and nmblookup finds their IP addresses.\n\nBroadcast mode sends queries to the local network segment. WINS (Windows Internet Name Service) provides network-wide resolution through a server.\n\nNode status queries (-A) reveal all registered NetBIOS names for a host. This shows shared resources, workgroup membership, and service names.\n\nMaster browser queries find the computer managing browse lists for a workgroup. This is useful for diagnosing network browsing issues.\n\nThe tool integrates with Samba configuration, using smb.conf settings for defaults."],["CAVEATS","NetBIOS is legacy technology. Broadcast queries only reach local subnet. WINS servers are increasingly rare. Modern networks prefer DNS."],["HISTORY","**nmblookup** is part of the **Samba** suite, developed by **Andrew Tridgell** and the Samba Team since the **1990s**. It provides Unix/Linux tools for interoperating with Windows networks."],["INSTALL","```apt: sudo apt install samba-common-bin```\n\n```dnf: sudo dnf install samba-client```\n\n```apk: sudo apk add samba-client```\n\n```zypper: sudo zypper install samba-client```"],["SEE ALSO","[smbclient](/man/smbclient)(1), [net](/man/net)(8), [nbtscan](/man/nbtscan)(1), [nslookup](/man/nslookup)(1)"]],"e":["smbclient","net","nbtscan","nslookup"],"x":"nmblookup [hostname]"},"nmcli-connection":{"n":"nmcli-connection","t":"manages NetworkManager connection profiles","s":[["TAGLINE","manages NetworkManager connection profiles"],["TLDR","List all **NetworkManager connections**\n\n```nmcli connection```\n\n**Activate** a connection\n\n```nmcli connection up uuid```\n\n**Deactivate** a connection\n\n```nmcli connection down uuid```\n\nCreate an **auto-configured** connection\n\n```nmcli connection add ifname eth0 type ethernet ipv4.method auto ipv6.method auto```\n\nCreate a **static IPv4** connection\n\n```nmcli connection add ifname eth0 type ethernet ip4 10.0.0.7/8 gw4 10.0.0.1 ipv4.dns 10.0.0.1```\n\nCreate a **static IPv6** connection\n\n```nmcli connection add ifname eth0 type ethernet ip6 2001:db8::2/64 gw6 2001:db8::1```\n\nImport a **VPN** connection from file\n\n```nmcli connection import type openvpn file vpn_config.ovpn```"],["SYNOPSIS","**nmcli connection** {_show_|_up_|_down_|_add_|_edit_|_modify_|_clone_|_delete_|_monitor_|_reload_|_load_|_migrate_|_import_|_export_} [_ARGUMENTS_]"],["DESCRIPTION","**nmcli connection** manages NetworkManager connection profiles. It can list, activate, deactivate, create, modify, and delete network connection configurations including Ethernet, Wi-Fi, VPN, and other connection types."],["COMMANDS","**show [id]**\n> List connections or show details of a specific connection\n\n**up id|uuid**\n> Activate a connection\n\n**down id|uuid**\n> Deactivate a connection\n\n**add**\n> Create a new connection profile\n\n**edit [id|uuid]**\n> Launch the interactive connection editor\n\n**modify id|uuid**\n> Change connection properties. Property values support `+`/`-` prefixes to append or remove list items\n\n**clone id|uuid NEW_NAME**\n> Duplicate an existing profile under a new name\n\n**delete id|uuid**\n> Remove a connection profile\n\n**monitor [id|uuid]**\n> Watch activity changes for all or a specific profile in real time\n\n**reload**\n> Reload all connection files from disk\n\n**load filename**\n> Load a single connection file on disk into NetworkManager\n\n**migrate**\n> Migrate profiles from legacy keyfile/ifcfg locations into the default store\n\n**import type TYPE file FILE**\n> Import an external connection (e.g., VPN)\n\n**export id**\n> Export a connection profile"],["COMMON ADD OPTIONS","**ifname**: Interface name to bind the connection\n**type**: Connection type (ethernet, wifi, vpn, bridge, etc.)\n**con-name**: Custom connection name\n**ip4**: IPv4 address with prefix\n**gw4**: IPv4 gateway address\n**ip6**: IPv6 address with prefix\n**gw6**: IPv6 gateway address\n**ipv4.method**: auto, manual, or disabled\n**ipv6.method**: auto, manual, or ignore\n**ipv4.dns**: DNS server addresses"],["COMMON GLOBAL OPTIONS","**--ask**\n> Interactively prompt for any required but missing argument. Not suitable for scripts.\n\n**--wait** _seconds_\n> Override the default timeout when waiting on an operation such as **up**.\n\n**--active**\n> On **show**, list only currently active connections.\n\n**--offline**\n> Edit connection files without going through the running NetworkManager daemon.\n\n**--temporary**\n> On **modify**, keep the change in memory only — discarded at daemon restart."],["CAVEATS","Connection names with spaces require quoting. Use UUIDs when multiple connections share the same name. Changes made with **modify** take effect on the next activation unless **--temporary** is used."],["HISTORY","Part of the **nmcli** command-line interface for **NetworkManager**. Provides scriptable network configuration management."],["INSTALL","```apt: sudo apt install network-manager```\n\n```apk: sudo apk add networkmanager-cli```"],["SEE ALSO","[nmcli](/man/nmcli)(1), [nmcli-device](/man/nmcli)(1), [nmtui](/man/nmtui)(1)"]],"e":["nmcli","nmcli-device","nmtui"],"x":"nmcli connection"},"pam_deny":{"n":"pam_deny","t":"always returns failure","s":[["TAGLINE","always returns failure"],["TLDR","**Deny all authentication**\n\n```auth required pam_deny.so```\n\n**Block account access**\n\n```account required pam_deny.so```\n\n**Disable password changes**\n\n```password required pam_deny.so```"],["SYNOPSIS","**pam_deny.so**"],["PARAMETERS","No parameters. Always returns failure."],["DESCRIPTION","**pam_deny** always returns failure. Used to block authentication.\n\nThe module denies all requests unconditionally. Security failsafe module."],["CAVEATS","No configuration options. Use carefully to avoid lockout."],["HISTORY","pam_deny provides a **universal deny module** for PAM configurations."],["SEE ALSO","[pam](/man/pam)(8), [pam_permit](/man/pam_permit)(8)"]],"e":["pam","pam_permit"],"x":"auth required pam_deny.so"},"pamstretch-gen":{"n":"pamstretch-gen","t":"Scale a Netpbm image by a non-integer factor","s":[["TAGLINE","Scale a Netpbm image by a non-integer factor"],["TLDR","**Scale an image** by a non-integer factor N\n\n```pamstretch-gen [2.5] [input.pam] > [output.pam]```\n\n**Read from standard input**\n\n```cat [input.pam] | pamstretch-gen [1.75] > [output.pam]```"],["SYNOPSIS","**pamstretch-gen** _N_ [_pnmfile_]"],["PARAMETERS","_N_\n> Scale factor (may be a non-integer such as `1.5` or `2.75`). Applied to both dimensions.\n\n_pnmfile_\n> Input Netpbm image file. If omitted, reads from standard input."],["DESCRIPTION","**pamstretch-gen** scales a Netpbm image by an arbitrary (possibly non-integer) factor. Internally it upscales to the next integer multiple with **pamstretch** (which only supports integer factors) and then uses **pamscale** to scale back down to the requested size, producing higher-quality results than pamscale alone for modest upscaling.\n\nThere are no command-line options specific to pamstretch-gen; it recognizes the common libnetpbm options (for example `-quiet` and `-plain` on recent versions)."],["CAVEATS","Requires both **pamstretch** and **pamscale** to be available. Works only with Netpbm (PNM/PAM) images."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pamstretch](/man/pamstretch)(1), [pamscale](/man/pamscale)(1)"]],"e":["pamstretch","pamscale"],"x":"pamstretch-gen [2.5] [input.pam] > [output.pam]"},"pamtotiff":{"n":"pamtotiff","t":"converts PAM images to TIFF format","s":[["TAGLINE","converts PAM images to TIFF format"],["TLDR","**Convert PAM to TIFF**\n\n```pamtotiff [input.pam] > [output.tiff]```\n\n**Convert with LZW compression**\n\n```pamtotiff -lzw [input.pam] > [output.tiff]```\n\n**Convert with JPEG compression**\n\n```pamtotiff -jpeg [input.pam] > [output.tiff]```"],["SYNOPSIS","**pamtotiff** [_options_] [_file_]"],["PARAMETERS","**-none**\n> No compression.\n\n**-lzw**\n> LZW compression.\n\n**-jpeg**\n> JPEG compression.\n\n**-packbits**\n> PackBits compression.\n\n**-append**\n> Append to existing TIFF."],["DESCRIPTION","**pamtotiff** converts PAM images to TIFF format. Supports alpha channel and various compression methods. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[tifftopnm](/man/tifftopnm)(1), [pnmtotiff](/man/pnmtotiff)(1)"]],"e":["tifftopnm","pnmtotiff"],"x":"pamtotiff [input.pam] > [output.tiff]"},"passepartui":{"n":"passepartui","t":"Terminal user interface for the pass password manager","s":[["TAGLINE","Terminal user interface for the pass password manager"],["TLDR","**Launch the password store TUI**\n\n```passepartui```"],["SYNOPSIS","**passepartui** [_options_]"],["DESCRIPTION","**passepartui** is a terminal user interface for the **pass** (password-store) password manager. It provides fast, keyboard-driven navigation with Vim keybindings, password search and filtering, clipboard copy for passwords and one-time passwords, and limited mouse support. It is a read-only interface for accessing your password store — use **pass** directly for adding or deleting entries."],["CAVEATS","Requires **pass** (password-store) to be installed and configured. Does not support creating or deleting password entries."],["HISTORY","**passepartui** was created by **kardwen** and is written in **Rust**."],["INSTALL","```nix: nix profile install nixpkgs#passepartui```"],["SEE ALSO","[pass](/man/pass)(1), [pass-cli](/man/pass-cli)(1), [gopass](/man/gopass)(1)"]],"e":["pass","pass-cli","gopass"],"x":"passepartui"},"paste":{"n":"paste","t":"merges lines from multiple files side by side","s":[["TAGLINE","merges lines from multiple files side by side"],["TLDR","**Merge files side by side**\n\n```paste [file1.txt] [file2.txt]```\n\n**Merge with custom delimiter**\n\n```paste -d \",\" [file1.txt] [file2.txt]```\n\n**Merge lines from single file**\n\n```paste -s [file.txt]```\n\n**Create tab-separated columns**\n\n```paste - - < [file.txt]```\n\n**Merge multiple files**\n\n```paste [file1] [file2] [file3]```"],["SYNOPSIS","**paste** [_options_] _files_..."],["DESCRIPTION","**paste** merges lines from multiple files side by side. Each line from the first file is joined with the corresponding line from subsequent files, separated by tabs.\n\nThe tool is useful for combining data from different sources into columnar format."],["PARAMETERS","**-d** _list_\n> Use characters from list as delimiters.\n\n**-s**, **--serial**\n> Paste one file at a time.\n\n**-z**, **--zero-terminated**\n> Use NUL as line delimiter.\n\n**--help**\n> Display help.\n\n**--version**\n> Display version."],["CAVEATS","Files should have same number of lines for alignment. Delimiter cycles through list. Empty lines produce empty columns."],["HISTORY","**paste** is a traditional Unix utility, part of **POSIX** and **GNU coreutils**. It has been standard in Unix systems since the early days, providing simple tabular data assembly."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[cut](/man/cut)(1), [join](/man/join)(1), [column](/man/column)(1), [pr](/man/pr)(1)"]],"e":["cut","join","column","pr"],"x":"paste [file1.txt] [file2.txt]"},"pgmhist":{"n":"pgmhist","t":"displays a grayscale histogram of a PGM image","s":[["TAGLINE","displays a grayscale histogram of a PGM image"],["TLDR","**Show grayscale histogram**\n\n```pgmhist [input.pgm]```\n\n**Show median value**\n\n```pgmhist -median [input.pgm]```"],["SYNOPSIS","**pgmhist** [_options_] [_file_]"],["PARAMETERS","**-median**\n> Show median gray value.\n\n**-quartile**\n> Show quartile values."],["DESCRIPTION","**pgmhist** displays a grayscale histogram of a PGM image. Shows distribution of gray values. Part of Netpbm toolkit for image analysis."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[ppmhist](/man/ppmhist)(1), [pgmnorm](/man/pgmnorm)(1)"]],"e":["ppmhist","pgmnorm"],"x":"pgmhist [input.pgm]"},"pip-uninstall":{"n":"pip-uninstall","t":"Remove installed Python packages","s":[["TAGLINE","Remove installed Python packages"],["TLDR","**Uninstall a package**\n\n```pip uninstall [package]```\n\n**Uninstall without confirmation prompt**\n\n```pip uninstall -y [package]```\n\n**Uninstall multiple packages** at once\n\n```pip uninstall [package1] [package2]```\n\n**Uninstall every package** listed in a requirements file\n\n```pip uninstall -r [requirements.txt]```\n\n**Uninstall and auto-confirm** from a requirements file\n\n```pip uninstall -y -r [requirements.txt]```"],["SYNOPSIS","**pip** **uninstall** [_options_] _package_...\n\n**pip** **uninstall** [_options_] **-r** _requirements-file_..."],["PARAMETERS","**-r** _FILE_, **--requirement** _FILE_\n> Uninstall all packages listed in the given requirements file. May be used multiple times.\n\n**-y**, **--yes**\n> Don't ask for confirmation of uninstall deletions.\n\n**--root-user-action** _ACTION_\n> Action when pip is run as root (warn, ignore).\n\n**--no-input**\n> Disable prompting for input.\n\n**-v**, **--verbose**\n> Give more output. Repeatable.\n\n**-q**, **--quiet**\n> Give less output."],["DESCRIPTION","**pip uninstall** removes installed Python packages. It prompts for confirmation before removing each package unless `-y` is used.\n\nPip removes the package files, entry-point scripts, and metadata, but does **not** automatically remove dependencies that were installed alongside the package. Packages installed in \"editable\" mode (`pip install -e`) and those installed directly from VCS URLs can also be removed by name.\n\nPackages installed by the system package manager (e.g., `apt`, `dnf`) or those installed without using pip's metadata cannot be uninstalled by pip."],["CAVEATS","Running `pip uninstall` as root on system Python can break the operating system; prefer a virtual environment or `--user` installs. Dependencies are never auto-removed, so orphan packages may remain after uninstalling a top-level package. Use `pip list` or `pip-autoremove` to find orphans."],["INSTALL","```apk: sudo apk add py3-pip```"],["SEE ALSO","[pip](/man/pip)(1), [pip-install](/man/pip-install)(1), [pip3](/man/pip3)(1), [pipx](/man/pipx)(1)"]],"e":["pip","pip-install","pip3","pipx"],"x":"pip uninstall [package]"},"pnmarith":{"n":"pnmarith","t":"Perform pixel arithmetic on PNM images","s":[["TAGLINE","Perform pixel arithmetic on PNM images"],["TLDR","**Add two images**\n\n```pnmarith -add [image1.pnm] [image2.pnm] > [result.pnm]```\n\n**Subtract images**\n\n```pnmarith -subtract [image1.pnm] [image2.pnm] > [result.pnm]```\n\n**Multiply images**\n\n```pnmarith -multiply [image1.pnm] [image2.pnm] > [result.pnm]```\n\n**Take minimum values**\n\n```pnmarith -minimum [image1.pnm] [image2.pnm] > [result.pnm]```"],["SYNOPSIS","**pnmarith** _operation_ _pnmfile1_ _pnmfile2_"],["PARAMETERS","**-add**\n> Add pixel values.\n\n**-subtract**\n> Subtract pixel values.\n\n**-multiply**\n> Multiply pixel values.\n\n**-difference**\n> Absolute difference.\n\n**-minimum**\n> Minimum of pixels.\n\n**-maximum**\n> Maximum of pixels.\n\n**-mean**\n> Average of pixels."],["DESCRIPTION","**pnmarith** performs arithmetic operations on two PNM images pixel by pixel. Images must have same dimensions. Useful for compositing, masking, and image comparison.\n\nSuperseded by pamarith in modern Netpbm."],["EXAMPLES","```bash"],["Add two images (blend)","pnmarith -add image1.ppm image2.ppm > blended.ppm"],["Create difference image","pnmarith -difference frame1.ppm frame2.ppm > diff.ppm"],["Multiply (darken with mask)","pnmarith -multiply image.ppm mask.pgm > masked.ppm"],["Find minimum","pnmarith -minimum img1.ppm img2.ppm > min.ppm\n```"],["CAVEATS","Images must be same dimensions. Results clamped to valid range. Use pamarith for more options."],["HISTORY","pnmarith is part of **Netpbm** by **Jef Poskanzer**, providing basic image arithmetic operations."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pamarith](/man/pamarith)(1), [pnmcat](/man/pnmcat)(1), [pnmcomp](/man/pnmcomp)(1), [netpbm](/man/netpbm)(1)"]],"e":["pamarith","pnmcat","pnmcomp","netpbm"],"x":"pnmarith -add [image1.pnm] [image2.pnm] > [result.pnm]"},"pnmtofits":{"n":"pnmtofits","t":"Convert PNM images to FITS astronomy format","s":[["TAGLINE","Convert PNM images to FITS astronomy format"],["TLDR","**Convert PNM to FITS format**\n\n```pnmtofits [input.pnm] > [output.fits]```"],["SYNOPSIS","**pnmtofits** [_file_]"],["DESCRIPTION","**pnmtofits** converts PNM images to FITS (Flexible Image Transport System) format used in astronomy. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[fitstopnm](/man/fitstopnm)(1), [pamtofits](/man/pamtofits)(1)"]],"e":["fitstopnm","pamtofits"],"x":"pnmtofits [input.pnm] > [output.fits]"},"pproftui":{"n":"pproftui","t":"Interactive terminal UI for Go pprof profiling data","s":[["TAGLINE","Interactive terminal UI for Go pprof profiling data"],["TLDR","**Analyze a CPU profile**\n\n```pproftui [cpu.prof]```\n\n**Focus on your module's code**\n\n```pproftui --module-path=[github.com/your/project] [cpu.prof]```\n\n**Live-fetch CPU profiles from a running server**\n\n```pproftui -live=[http://localhost:6060/debug/pprof/profile?seconds=5] -refresh=[10s]```"],["SYNOPSIS","**pproftui** [_options_] [_profile_file_]"],["PARAMETERS","**--module-path** _PATH_\n> Focus on code from the specified Go module path.\n\n**-live** _URL_\n> Fetch profiles from a running HTTP pprof endpoint.\n\n**-refresh** _DURATION_\n> Refresh interval for live profiling."],["DESCRIPTION","**pproftui** is a terminal-based UI for Go's pprof that makes profiling interactive and intuitive. It provides integrated source code and call graph views with an option to hide Go runtime functions. Its diffing feature highlights resource usage changes between profiles using color-coded indicators."],["CAVEATS","Only works with Go pprof profile data."],["HISTORY","**pproftui** was created by **Oloruntobi1** and is written in **Go**."],["SEE ALSO","[go](/man/go)(1), [pprof](/man/pprof)(1)"]],"e":["go","pprof"],"x":"pproftui [cpu.prof]"},"privoxy":{"n":"privoxy","t":"Privacy-enhancing filtering web proxy","s":[["TAGLINE","Privacy-enhancing filtering web proxy"],["TLDR","**Start privoxy**\n\n```privoxy [/etc/privoxy/config]```\n\n**Start in foreground**\n\n```privoxy --no-daemon [config]```\n\n**Check configuration**\n\n```privoxy --config-test [config]```\n\n**Show version**\n\n```privoxy --version```"],["SYNOPSIS","**privoxy** [_options_] _config-file_"],["DESCRIPTION","**privoxy** is a non-caching web proxy with filtering capabilities. It blocks ads, removes tracking, modifies web content, and enhances privacy.\n\nThe tool works between the browser and the internet, inspecting and modifying HTTP traffic according to configurable rules."],["PARAMETERS","**--no-daemon**\n> Run in foreground.\n\n**--pidfile** _file_\n> PID file location.\n\n**--user** _user_\n> Run as user.\n\n**--chroot**\n> Chroot to home directory.\n\n**--pre-chroot-nslookup** _host_\n> DNS lookup before chroot.\n\n**--config-test**\n> Test configuration.\n\n**--version**\n> Show version.\n\n**--help**\n> Show help."],["CONFIGURATION","**listen-address**: Proxy listen address\n**forward**: Upstream proxy\n**actionsfile**: Action definitions\n**filterfile**: Content filters\n**logfile**: Log file path"],["CAVEATS","HTTPS filtering requires browser configuration. Complex rules need tuning. May break some sites. Performance overhead for filtering."],["HISTORY","**Privoxy** evolved from **Internet Junkbuster** in **2001**, created as a privacy-focused web proxy. It's developed by a team of volunteers and remains one of the most flexible content-filtering proxies."],["INSTALL","```apt: sudo apt install privoxy```\n\n```dnf: sudo dnf install privoxy```\n\n```pacman: sudo pacman -S privoxy```\n\n```apk: sudo apk add privoxy```\n\n```zypper: sudo zypper install privoxy```\n\n```brew: brew install privoxy```\n\n```nix: nix profile install nixpkgs#privoxy```"],["SEE ALSO","[squid](/man/squid)(8), [tinyproxy](/man/tinyproxy)(1), [tor](/man/tor)(1), [dnsmasq](/man/dnsmasq)(1)"]],"e":["squid","tinyproxy","tor","dnsmasq"],"x":"privoxy [/etc/privoxy/config]"},"sam":{"n":"sam","t":"AWS Serverless Application Model CLI","s":[["TAGLINE","AWS Serverless Application Model CLI"],["TLDR","**Initialize** a serverless application\n\n```sam init```\n\nInitialize with specific **runtime**\n\n```sam init --runtime [python3.12]```\n\n**Build** Lambda function code\n\n```sam build```\n\nRun serverless application **locally**\n\n```sam local start-api```\n\n**Invoke** a Lambda function locally\n\n```sam local invoke [FunctionName]```\n\n**Deploy** with guided prompts\n\n```sam deploy --guided```\n\n**Validate** a SAM template\n\n```sam validate```"],["SYNOPSIS","**sam** _command_ [_options_]"],["PARAMETERS","**init**\n> Initialize a new serverless application\n\n**build**\n> Build Lambda function code\n\n**deploy**\n> Deploy application to AWS\n\n**delete**\n> Delete a deployed application and its resources\n\n**validate**\n> Validate a SAM template\n\n**sync**\n> Sync local changes to a deployed application\n\n**logs**\n> Fetch and display Lambda function logs\n\n**local invoke**\n> Invoke a Lambda function locally\n\n**local start-api**\n> Run API Gateway locally\n\n**local start-lambda**\n> Start a local Lambda endpoint\n\n**local generate-event**\n> Generate sample event payloads for testing"],["DESCRIPTION","**sam** is the AWS Serverless Application Model CLI. It provides tools for building, testing, and deploying serverless applications using AWS Lambda, API Gateway, and other AWS services.\n\nThe CLI supports local development and testing before cloud deployment."],["CAVEATS","Requires AWS credentials configured. Local testing requires Docker. Deployment creates AWS resources that may incur costs."],["HISTORY","Developed by **AWS** to simplify serverless application development and deployment using Infrastructure as Code."],["INSTALL","```aur: yay -S sam```"],["SEE ALSO","[aws](/man/aws)(1), [docker](/man/docker)(1)"]],"e":["aws","docker"],"x":"sam init"},"sfz":{"n":"sfz","t":"Simple static file server for local development","s":[["TAGLINE","Simple static file server for local development"],["TLDR","**Serve** the current directory on the default port\n\n```sfz```\n\n**Serve** a path on a port\n\n```sfz -p [8080] [path]```\n\n**Enable CORS** / address bind (see help)\n\n```sfz --help```"],["SYNOPSIS","**sfz** [*options*] [*path*]"],["DESCRIPTION","**sfz** (**S**tatic **F**ile **Z**erver) is a small Rust CLI that serves a directory over HTTP—useful for quick local previews. Note: the original author has indicated limited maintenance; consider alternatives on crates.io for long-term projects."],["PARAMETERS","*path*\n\n> Directory to serve (default: current directory).\n\n**-p**, **--port** *port*\n\n> Listen port.\n\nAdditional flags control address, CORS, and cache headers—**sfz --help**."],["CAVEATS","Not hardened for public Internet exposure. Project maintenance status is limited. Prefer **caddy**/**nginx** for production."],["INSTALL","```nix: nix profile install nixpkgs#sfz```"],["SEE ALSO","[python](/man/python)(1), [caddy](/man/caddy)(1), [miniserve](/man/miniserve)(1)"],["RESOURCES","```[Source code](https://github.com/weihanglo/sfz)```"]],"e":["python","caddy","miniserve"],"x":"sfz"},"sgpt":{"n":"sgpt","t":"AI-powered shell assistant using ChatGPT","s":[["TAGLINE","AI-powered shell assistant using ChatGPT"],["TLDR","**Ask a question**\n\n```sgpt \"[How do I list files?]\"```\n\n**Generate shell command**\n\n```sgpt -s \"[list all PDF files]\"```\n\n**Execute shell command**\n\n```sgpt -se \"[find large files]\"```\n\n**Generate code**\n\n```sgpt -c \"[python function to sort list]\"```\n\n**Chat mode**\n\n```sgpt --chat [session_name] \"[message]\"```\n\n**Use specific role**\n\n```sgpt --role [code] \"[question]\"```\n\n**Pipe input**\n\n```cat [file.txt] | sgpt \"[summarize this]\"```"],["SYNOPSIS","**sgpt** [_-s_] [_-c_] [_--chat name_] [_options_] _prompt_"],["PARAMETERS","**-s**, **--shell**\n> Generate shell command.\n\n**-se**\n> Shell and execute.\n\n**-c**, **--code**\n> Generate code.\n\n**--chat** _NAME_\n> Chat session.\n\n**--role** _ROLE_\n> Use role.\n\n**--model** _MODEL_\n> Specify model.\n\n**--temperature** _TEMP_\n> Response randomness.\n\n**--no-cache**\n> Disable caching."],["DESCRIPTION","**sgpt** (Shell GPT) is a command-line interface for interacting with OpenAI's ChatGPT models, optimized for developer and system administration workflows. It operates in several modes: general queries for quick answers, shell mode (**-s**) for generating executable commands with explanations, and code mode (**-c**) for producing programming solutions with automatic language detection.\n\nChat sessions maintain conversation context across multiple invocations, allowing follow-up questions and iterative refinement. Roles customize the AI's behavior for specific tasks, with predefined roles for common scenarios and support for user-defined custom roles.\n\nThe tool integrates naturally with Unix pipelines, accepting input via stdin to process file contents, command output, or any streamed data through AI analysis. This makes it useful for tasks like summarizing logs, explaining error messages, or generating documentation from code."],["CONFIGURATION","**OPENAI_API_KEY**\n> Environment variable providing the OpenAI API key for authentication.\n\n**~/.config/shell_gpt/.sgptrc**\n> Configuration file for default model, temperature, cache settings, and other preferences.\n\n**~/.config/shell_gpt/roles/**\n> Directory containing custom role definitions as text files."],["CAVEATS","Requires OpenAI API key. Costs per token. Internet connection needed."],["HISTORY","**sgpt** was created by **Farkhod Sadykov** for AI-assisted shell usage. It brings ChatGPT capabilities directly to the command line."],["SEE ALSO","[chatgpt](/man/chatgpt)(1), [github-copilot-cli](/man/github-copilot-cli)(1)"]],"e":["chatgpt","github-copilot-cli"],"x":"sgpt \"[How do I list files?]\""},"speedtest":{"n":"speedtest","t":"Official Ookla internet speed test CLI","s":[["TAGLINE","Official Ookla internet speed test CLI"],["TLDR","**Run speed test**\n\n```speedtest```\n\n**List servers**\n\n```speedtest --servers```\n\n**Use specific server**\n\n```speedtest --server-id [12345]```\n\n**Output as JSON**\n\n```speedtest --format json```\n\n**Accept license**\n\n```speedtest --accept-license```\n\n**Show progress**\n\n```speedtest --progress yes```"],["SYNOPSIS","**speedtest** [_--server-id id_] [_--format fmt_] [_options_]"],["PARAMETERS","**--servers**\n> List nearby servers.\n\n**--server-id** _ID_\n> Select server.\n\n**--format** _FMT_\n> Output format (json, csv).\n\n**--progress** _BOOL_\n> Show progress.\n\n**--accept-license**\n> Accept EULA.\n\n**--accept-gdpr**\n> Accept privacy terms."],["DESCRIPTION","**speedtest** is the official command-line interface from Ookla for measuring internet connection performance. It tests download speed, upload speed, latency (ping), and jitter by connecting to Ookla's global network of test servers.\n\nThe tool automatically selects the nearest server for testing, though specific servers can be chosen by ID. Results include detailed metrics about connection quality, and structured output formats (JSON, CSV) enable integration with monitoring scripts and dashboards.\n\nFirst-run usage requires accepting the Ookla license agreement and GDPR terms. The CLI is a compiled binary distributed separately from the Python-based speedtest-cli, and uses Ookla's native testing methodology for more accurate results."],["CAVEATS","Requires license acceptance. Network dependent. Ookla servers only."],["HISTORY","**speedtest** is the official CLI from **Ookla**, the company behind Speedtest.net internet speed testing."],["INSTALL","```dnf: sudo dnf install speedtest```\n\n```apk: sudo apk add perl-app-speedtest```\n\n```nix: nix profile install nixpkgs#speedtest```"],["SEE ALSO","[speedtest-cli](/man/speedtest-cli)(1), [iperf3](/man/iperf3)(1), [ping](/man/ping)(1)"]],"e":["speedtest-cli","iperf3","ping"],"x":"speedtest"},"swiftc":{"n":"swiftc","t":"Compile Swift source code to executables","s":[["TAGLINE","Compile Swift source code to executables"],["TLDR","**Compile a Swift source file**\n\n```swiftc [file.swift]```\n\n**Compile with a custom output name**\n\n```swiftc -o [program] [file.swift]```\n\n**Compile multiple source files**\n\n```swiftc [main.swift] [utils.swift] -o [program]```\n\n**Compile with debugging symbols**\n\n```swiftc -g [file.swift] -o [program]```\n\n**Compile with optimization**\n\n```swiftc -O [file.swift] -o [program]```\n\n**Show verbose compilation output**\n\n```swiftc -v [file.swift]```\n\n**Compile with size optimization**\n\n```swiftc -Osize [file.swift] -o [program]```\n\n**Emit object file only**\n\n```swiftc -c [file.swift] -o [file.o]```"],["SYNOPSIS","**swiftc** [_options_] [_files_]"],["PARAMETERS","**-o** _file_\n> Specify output file name\n\n**-c**\n> Compile without linking (produce object file)\n\n**-g**\n> Generate debugging symbols\n\n**-O**\n> Compile with optimizations\n\n**-Osize**\n> Compile with optimizations, prioritizing smaller binary size\n\n**-Ounchecked**\n> Compile with optimizations, removing runtime safety checks\n\n**-Onone**\n> Compile without optimizations (default for debug)\n\n**-whole-module-optimization**\n> Optimize across the entire module at once\n\n**-target** _triple_\n> Generate code for the given target triple (e.g., x86_64-apple-macosx10.15)\n\n**-v**, **--verbose**\n> Show verbose output including executed commands\n\n**-emit-executable**\n> Generate an executable (default when linking)\n\n**-emit-library**\n> Generate a dynamic library\n\n**-emit-module**\n> Generate a Swift module\n\n**-D** _flag_\n> Define a conditional compilation flag\n\n**-I** _path_\n> Add import search path\n\n**-L** _path_\n> Add library search path\n\n**-l** _library_\n> Link with specified library\n\n**-h**, **--help**\n> Display help information"],["DESCRIPTION","**swiftc** is the Swift compiler that translates Swift source code into executable binaries, libraries, object files, and modules. It is the command-line interface for building Swift programs outside of Xcode.\n\nThe compiler driver coordinates multiple compilation phases: parsing, type checking, optimization, and code generation. For single-file programs, swiftc produces an executable directly. Multiple source files are compiled and linked together.\n\nswiftc is a symbolic link to the swift executable, which determines its behavior based on how it is invoked. The Swift Package Manager typically handles compilation for complex projects."],["CAVEATS","swiftc requires the Swift toolchain to be installed. On macOS, it is included with Xcode; on Linux, it must be installed separately. Module compatibility depends on Swift version; mixing versions may cause issues."],["HISTORY","Swift was announced by **Apple** at WWDC **2014** and open-sourced in **December 2015**. The compiler was initially written in C++, with ongoing efforts to rewrite components in Swift itself. Swift 6 added major concurrency and safety improvements."],["INSTALL","```apt: sudo apt install swiftlang```"],["SEE ALSO","[swift](/man/swift)(1), [clang](/man/clang)(1), [xcodebuild](/man/xcodebuild)(1), [lldb](/man/lldb)(1)"]],"e":["swift","clang","xcodebuild","lldb"],"x":"swiftc [file.swift]"},"switcheroo-control":{"n":"switcheroo-control","t":"Manage hybrid graphics GPU switching","s":[["TAGLINE","Manage hybrid graphics GPU switching"],["TLDR","**List available GPUs**\n\n```switcherooctl list```\n\n**Launch application on discrete GPU**\n\n```switcherooctl launch [application]```\n\n**Launch on specific GPU** by number\n\n```switcherooctl -g [1] launch [application]```\n\n**Check service status**\n\n```systemctl status switcheroo-control.service```\n\n**Enable the service**\n\n```sudo systemctl enable --now switcheroo-control.service```"],["SYNOPSIS","**switcherooctl** [_options_] [_command_]"],["PARAMETERS","**-g**, **--gpu** _index_\n> GPU index to use for launch command.\n\n**-h**, **--help**\n> Display help information.\n\n**-v**, **--version**\n> Display version information."],["COMMANDS","**list**\n> List known GPUs with their indices and capabilities.\n\n**launch** _command_\n> Launch application on discrete GPU (or specified GPU with -g)."],["DESCRIPTION","**switcherooctl** is the command-line interface for switcheroo-control, a daemon that manages hybrid graphics on Linux laptops with multiple GPUs. It provides a simple way to list available graphics adapters and launch applications on a specific GPU.\n\nOn hybrid graphics systems with both integrated (power-efficient) and discrete (high-performance) GPUs, applications default to the integrated GPU. Using switcherooctl, you can run specific applications on the discrete GPU for better graphics performance.\n\nThe underlying daemon interfaces with the kernel's vga_switcheroo subsystem and provides a D-Bus API that desktop environments like GNOME and KDE use for GUI-based GPU selection.\n\nSetting the environment variable DRI_PRIME=1 achieves a similar effect for individual applications."],["CAVEATS","Requires switcheroo-control.service to be running. Only works on systems with hybrid graphics (typically laptops with Intel/AMD integrated and Nvidia/AMD discrete GPUs). Detection may incorrectly identify GPUs in some configurations. Does not provide mux switching (for systems with hardware mux)."],["HISTORY","**switcheroo-control** was developed for the GNOME desktop to provide a user-friendly interface for hybrid graphics management on Linux. It abstracts the kernel's vga_switcheroo mechanism, allowing desktop environments to offer \"Launch using Discrete Graphics\" options in application menus."],["INSTALL","```dnf: sudo dnf install switcheroo-control```\n\n```pacman: sudo pacman -S switcheroo-control```\n\n```zypper: sudo zypper install switcheroo-control```\n\n```nix: nix profile install nixpkgs#switcheroo-control```"],["SEE ALSO","[prime-run](/man/prime-run)(1), [nvidia-settings](/man/nvidia-settings)(1), [glxinfo](/man/glxinfo)(1)"]],"e":["prime-run","nvidia-settings","glxinfo"],"x":"switcherooctl list"},"systemctl-list-sockets":{"n":"systemctl-list-sockets","t":"List socket-activated units","s":[["TAGLINE","List socket-activated units"],["TLDR","List **active** socket units\n\n```systemctl list-sockets```\n\nShow **socket types**\n\n```systemctl list-sockets --show-types```\n\nList **all** including inactive\n\n```systemctl list-sockets -a```\n\nFilter by **state**\n\n```systemctl list-sockets --state [active|inactive|failed]```\n\nFilter by **pattern**\n\n```systemctl list-sockets [pattern]```"],["SYNOPSIS","**systemctl list-sockets** [_OPTIONS_] [_PATTERN_...]"],["PARAMETERS","**-a, --all**\n> Include inactive and failed socket units\n\n**--state=** _STATE_\n> Filter by state\n\n**--show-types**\n> Display socket types (Stream, Datagram, etc.)\n\n**--no-legend**\n> Suppress header and footer\n\n**--no-pager**\n> Disable pager"],["DESCRIPTION","**systemctl list-sockets** displays socket units currently active in memory, ordered by listening address. Socket units implement socket activation, where services are started on-demand when connections arrive.\n\nThe output shows the listening address (IP:port, path, etc.), the socket unit name, and the unit it activates."],["CAVEATS","Shows systemd-managed sockets only, not all listening sockets on the system. Use `ss` or `netstat` for a complete list. Socket activation may cause startup delays on first connection."],["HISTORY","The **list-sockets** subcommand provides insight into systemd's socket activation infrastructure, which reduces memory usage by starting services only when needed."],["SEE ALSO","[systemctl](/man/systemctl)(1), [systemctl-list-units](/man/systemctl-list-units)(1), [ss](/man/ss)(8), [netstat](/man/netstat)(8)"]],"e":["systemctl","systemctl-list-units","ss","netstat"],"x":"systemctl list-sockets"},"tcc":{"n":"tcc","t":"Tiny C Compiler","s":[["TAGLINE","Tiny C Compiler"],["TLDR","**Compile and run**\n\n```tcc -run [program.c]```\n\n**Compile to executable**\n\n```tcc -o [program] [program.c]```\n\n**Compile to object file**\n\n```tcc -c [program.c]```\n\n**Run C code from stdin**\n\n```echo 'int main(){return 0;}' | tcc -run -```\n\n**Compile with library**\n\n```tcc -o [program] [program.c] -l[m]```\n\n**Run with arguments**\n\n```tcc -run [program.c] -- [arg1] [arg2]```\n\n**Compile as shared library**\n\n```tcc -shared -o [lib.so] [lib.c]```"],["SYNOPSIS","**tcc** [_-run_] [_-o output_] [_-c_] [_-l lib_] [_options_] _files_"],["PARAMETERS","**-run**\n> Compile and run immediately.\n\n**-o** _FILE_\n> Output file.\n\n**-c**\n> Compile to object file only.\n\n**-shared**\n> Create shared library.\n\n**-l** _LIB_\n> Link library.\n\n**-L** _DIR_\n> Library search path.\n\n**-I** _DIR_\n> Include search path.\n\n**-D** _NAME_\n> Define preprocessor macro.\n\n**-g**\n> Generate debug info.\n\n**-b**\n> Enable bounds checking.\n\n**-Wall**\n> Enable warnings.\n\n**-v**\n> Verbose mode."],["DESCRIPTION","**tcc** (Tiny C Compiler) is an extremely fast C compiler that can also serve as a C script interpreter. Its primary feature is compilation speed — TCC compiles several times faster than GCC, making it suitable for rapid development iterations and using C as a scripting language.\n\nThe **-run** flag compiles source code directly into memory and executes it immediately without creating intermediate object files or executables. Arguments after **--** are passed to the compiled program. This enables workflows where C files are treated like scripts, including use in shebangs.\n\nTCC supports most of the C99 standard along with common GCC extensions for compatibility. It can generate executables, object files, and shared libraries. A built-in bounds checker (**-b**) catches buffer overflows and out-of-bounds access at runtime without requiring external tools like Valgrind. Despite its small size, TCC is capable enough to compile itself."],["CAVEATS","Optimization is minimal - generated code is slower. Some C features may differ from GCC. Not suitable for production builds needing performance."],["HISTORY","**TCC** was created by **Fabrice Bellard** around **2001**. Bellard is known for QEMU, FFmpeg, and other influential projects. TCC demonstrates that a full C compiler can be very small and fast."],["INSTALL","```apt: sudo apt install tcc```\n\n```pacman: sudo pacman -S tcc```\n\n```apk: sudo apk add tcc```\n\n```brew: brew install tcc```"],["SEE ALSO","[gcc](/man/gcc)(1), [clang](/man/clang)(1), [make](/man/make)(1)"]],"e":["gcc","clang","make"],"x":"tcc -run [program.c]"},"terraform-taint":{"n":"terraform-taint","t":"Mark a Terraform resource as tainted to force replacement on next apply","s":[["TAGLINE","Mark a Terraform resource as tainted to force replacement on next apply"],["TLDR","**Taint a resource** so it will be recreated on next apply\n\n```terraform taint aws_instance.example```\n\n**Taint even if currently missing** from state\n\n```terraform taint -allow-missing module.foo.aws_instance.bar[0]```\n\n**Taint without state lock**\n\n```terraform taint -lock=false aws_instance.example```"],["SYNOPSIS","**terraform** **taint** [_options_] _address_"],["DESCRIPTION","**terraform taint** marks a resource instance in the Terraform state as \"tainted\". On the next `terraform apply`, Terraform will destroy the existing object and create a replacement.\n\nThis was historically used to force replacement of resources that became inconsistent or corrupted.\n\n**Note:** This command is deprecated. The recommended approach is to use the `-replace` flag on `terraform apply` or `terraform plan`, e.g. `terraform apply -replace=\"aws_instance.example\"`."],["PARAMETERS","**-allow-missing**\n> Succeed even if the resource address is not present in state.\n\n**-lock**=_true|false_\n> Acquire a state lock (default true). Use `-lock=false` to disable.\n\n**-lock-timeout**=_duration_\n> Duration to retry acquiring a state lock (e.g. `3s`).\n\n**-var** '_NAME=VALUE_'\n> Set an input variable value (repeatable).\n\n**-var-file**=_filename_\n> Load variable values from a `.tfvars` file (repeatable)."],["CAVEATS","Tainting is deprecated in modern Terraform versions. Using `-replace` during planning/applying is preferred because it is more explicit and works in a single step. Taint/untaint only affect state and require a separate apply."],["INSTALL","```pacman: sudo pacman -S terraform```\n\n```nix: nix profile install nixpkgs#terraform```"],["SEE ALSO","[terraform](/man/terraform)(1), [terraform-apply](/man/terraform-apply)(1), [terraform-untaint](/man/terraform-untaint)(1)"],["RESOURCES","```[Documentation](https://developer.hashicorp.com/terraform/cli/commands/taint)```"]],"e":["terraform","terraform-apply","terraform-untaint"],"x":"terraform taint aws_instance.example"},"tokei":{"n":"tokei","t":"Fast source code line counter","s":[["TAGLINE","Fast source code line counter"],["TLDR","**Count lines of code in the current directory**\n\n```tokei```\n\n**Count a specific directory**\n\n```tokei [path/to/project]```\n\n**Count multiple directories**\n\n```tokei [path/to/project] [path/to/other]```\n\n**Exclude directories or patterns**\n\n```tokei -e [vendor] -e [node_modules]```\n\n**Show files individually**\n\n```tokei --files```\n\n**Sort by a specific column**\n\n```tokei -s [code]```\n\n**Output as JSON**\n\n```tokei -o json```\n\n**Count only specific languages**\n\n```tokei -t [Rust,Python]```\n\n**Show supported languages**\n\n```tokei --languages```\n\n**Read from a previously saved JSON output**\n\n```tokei [path/to/project] -i [stats.json]```"],["SYNOPSIS","**tokei** [_path_...] [_options_]"],["PARAMETERS","**-e**, **--exclude** _PATTERN_\n> Ignore all files and directories matching the pattern. Uses gitignore syntax. Can be specified multiple times.\n\n**-t**, **--type** _TYPES_\n> Filter output by language type, separated by a comma (e.g. Rust,Markdown).\n\n**-s**, **--sort** _COLUMN_\n> Sort languages by column. Possible values: files, lines, blanks, code, comments. Default: alphabetical by language name.\n\n**-o**, **--output** _FORMAT_\n> Output in a machine-readable format. Possible values: json, yaml, cbor.\n\n**-i**, **--input** _FILE_\n> Read in the output of a previous tokei run and combine it with the current results. Pass a file path or \"stdin\" to read from stdin.\n\n**-f**, **--files**\n> Print statistics for individual files instead of aggregating by language.\n\n**-l**, **--languages**\n> Print a list of all supported languages and their file extensions.\n\n**--hidden**\n> Count hidden files and directories (those beginning with a dot).\n\n**--no-ignore**\n> Don't respect any ignore files (.gitignore, .ignore, etc.). Implies --no-ignore-parent, --no-ignore-dot, and --no-ignore-vcs.\n\n**--no-ignore-dot**\n> Don't respect .ignore and .tokeignore files, including those in parent directories.\n\n**--no-ignore-parent**\n> Don't respect ignore files in parent directories.\n\n**--no-ignore-vcs**\n> Don't respect VCS ignore files (.gitignore, .hgignore, etc.), including those in parent directories.\n\n**-c**, **--columns** _NUM_\n> Set a strict column width for terminal output.\n\n**-v**, **--verbose**\n> Increase log output level. Use up to three times (-vvv) for more detail."],["DESCRIPTION","**tokei** counts lines of code quickly and accurately. It identifies languages by file extension, categorising lines as code, comments, or blank, and groups results by language.\n\nThe tool is written in Rust with parallel file processing, making it capable of counting millions of lines in seconds. Results show a per-language breakdown with a grand total.\n\nLanguage detection supports over 150 languages with correct handling of multi-line comments, nested comments, and strings containing comment-like syntax.\n\nFile filtering respects .gitignore and .ignore files by default, which excludes generated code and dependency directories. Additional patterns can be excluded with **-e**, or a .tokeignore file can be used with the same syntax as .gitignore.\n\nIndividual file mode (**--files**) shows statistics per file rather than aggregated by language, useful for identifying verbose or generated files.\n\nMachine-readable output (JSON, YAML, CBOR) enables integration with other tools and CI/CD pipelines. A previous run's JSON output can be fed back in with **--input** to combine results across separate directory trees."],["CAVEATS","Lines of code is an imperfect metric. Generated code may skew results. Multi-language files (e.g. Markdown with embedded code blocks) are counted once under the primary language but embedded sub-language totals are shown separately. Extension-based detection may misidentify some files. Binary files are skipped. CBOR and YAML output require tokei to be compiled with the corresponding feature flags."],["HISTORY","**tokei** was created by **Erin Power** (XAMPPRocky) around **2016**. The Japanese name (時計) means \"clock.\" Written in Rust for performance, it became a popular alternative to cloc and other line counting tools."],["INSTALL","```apt: sudo apt install tokei```\n\n```dnf: sudo dnf install tokei```\n\n```pacman: sudo pacman -S tokei```\n\n```apk: sudo apk add tokei```\n\n```zypper: sudo zypper install tokei```\n\n```brew: brew install tokei```\n\n```nix: nix profile install nixpkgs#tokei```"],["SEE ALSO","[cloc](/man/cloc)(1), [scc](/man/scc)(1), [wc](/man/wc)(1), [loc](/man/loc)(1)"]],"e":["cloc","scc","wc","loc"],"x":"tokei"},"ufraw-batch":{"n":"ufraw-batch","t":"Batch convert camera RAW photos","s":[["TAGLINE","Batch convert camera RAW photos"],["TLDR","**Convert RAW to JPEG**\n\n```ufraw-batch --out-type=jpeg [image.raw]```\n\n**Convert to TIFF**\n\n```ufraw-batch --out-type=tiff [image.raw]```\n\n**Set output directory**\n\n```ufraw-batch --out-path=[output/] [image.raw]```\n\n**Apply white balance**\n\n```ufraw-batch --wb=camera [image.raw]```\n\n**Batch process**\n\n```ufraw-batch --out-type=jpeg [*.raw]```"],["SYNOPSIS","**ufraw-batch** [_--out-type format_] [_--out-path dir_] [_options_] _files_"],["PARAMETERS","**--out-type** _FORMAT_\n> Output format (jpeg, tiff, png).\n\n**--out-path** _DIR_\n> Output directory.\n\n**--wb** _MODE_\n> White balance.\n\n**--exposure** _STOPS_\n> Exposure adjustment.\n\n**--compression** _N_\n> JPEG quality."],["DESCRIPTION","**ufraw-batch** is the batch processing mode of UFRaw (Unidentified Flying Raw), a tool for reading and converting camera RAW image files. It processes multiple RAW files non-interactively, applying adjustments like white balance, exposure compensation, and color correction before converting to JPEG, TIFF, or PNG output formats.\n\nThe tool supports RAW formats from most major camera manufacturers through the dcraw library. Adjustments can be specified via command-line options or loaded from previously saved UFRaw settings files, enabling consistent processing across large sets of images.\n\nOutput quality, compression, and bit depth are configurable. The tool integrates with GIMP as a plugin for interactive RAW editing."],["CAVEATS","Legacy tool. Consider darktable-cli. Many RAW formats."],["HISTORY","**UFRaw** (Unidentified Flying Raw) was created for reading and manipulating camera RAW images."],["SEE ALSO","[darktable-cli](/man/darktable-cli)(1), [dcraw](/man/dcraw)(1), [rawtherapee-cli](/man/rawtherapee-cli)(1)"]],"e":["darktable-cli","dcraw","rawtherapee-cli"],"x":"ufraw-batch --out-type=jpeg [image.raw]"},"uvcdynctrl":{"n":"uvcdynctrl","t":"Control UVC webcam dynamic settings","s":[["TAGLINE","Control UVC webcam dynamic settings"],["TLDR","**List connected cameras**\n\n```uvcdynctrl -l```\n\n**List available controls for the default device**\n\n```uvcdynctrl -c```\n\n**List available controls for a specific device**\n\n```uvcdynctrl -d [device_name] -c```\n\n**Get the current value of a control**\n\n```uvcdynctrl -g \"[control_name]\"```\n\n**Set a control to a specific value**\n\n```uvcdynctrl -s \"[control_name]\" -- [value]```\n\n**List supported frame formats**\n\n```uvcdynctrl -f```\n\n**Save current control state to a file**\n\n```uvcdynctrl -W [filename]```\n\n**Load control state from a file**\n\n```uvcdynctrl -L [filename]```"],["SYNOPSIS","**uvcdynctrl** [_OPTIONS_] [_VALUES_]"],["PARAMETERS","**-l**, **--list**\n> List all connected UVC cameras.\n\n**-d**, **--device** _NAME_\n> Use a specific device (default: video0).\n\n**-c**, **--clist**\n> List available controls for the selected device.\n\n**-g**, **--get** _CONTROL_\n> Get current value of a control.\n\n**-G**, **--get_raw** _unit_id:selector_\n> Get raw control value by unit ID and selector.\n\n**-s**, **--set** _CONTROL_ _VALUE_\n> Set a control to the given value.\n\n**-S**, **--set_raw** _unit_id:selector_ _VALUE_\n> Set a raw control value (hex string; prefix with (BE) for big-endian).\n\n**-f**, **--formats**\n> List supported frame formats.\n\n**-i**, **--import** _FILE_\n> Import dynamic controls from an XML file.\n\n**-a**, **--addctrl** _VID_\n> Load dynamic controls for a vendor ID from the default system location.\n\n**-W**, **--save** _FILE_\n> Save device control state to a file.\n\n**-L**, **--load** _FILE_\n> Load device control state from a file.\n\n**-v**, **--verbose**\n> Enable verbose output.\n\n**-V**, **--version**\n> Display version information."],["DESCRIPTION","**uvcdynctrl** manages dynamic controls in the uvcvideo Linux kernel driver. It allows adjusting camera settings like brightness, contrast, exposure, white balance, and other UVC-specific controls.\n\nThe tool works with USB Video Class (UVC) compatible webcams and cameras. It can also import custom control definitions from XML files for vendor-specific extensions."],["CAVEATS","Only works with UVC-compatible cameras. Available controls depend on the hardware. For negative values, use **--** before the value to prevent interpretation as an option flag."],["INSTALL","```apt: sudo apt install uvcdynctrl```\n\n```dnf: sudo dnf install uvcdynctrl```\n\n```zypper: sudo zypper install uvcdynctrl```\n\n```nix: nix profile install nixpkgs#uvcdynctrl```"],["SEE ALSO","[v4l2-ctl](/man/v4l2-ctl)(1), [v4l2loopback-ctl](/man/v4l2loopback-ctl)(1)"]],"e":["v4l2-ctl","v4l2loopback-ctl"],"x":"uvcdynctrl -l"},"vortix":{"n":"vortix","t":"TUI for WireGuard and OpenVPN with telemetry","s":[["TAGLINE","TUI for WireGuard and OpenVPN with telemetry"],["TLDR","**Launch the VPN management TUI**\n\n```sudo vortix```\n\n**Import a VPN configuration**\n\n```vortix import [tunnel.conf]```\n\n**Display version and config info**\n\n```vortix info```"],["SYNOPSIS","**vortix** [_command_]"],["DESCRIPTION","**vortix** is a terminal UI for managing WireGuard and OpenVPN connections with real-time telemetry and leak guarding. It auto-detects VPN configuration files and provides live metrics including throughput, latency, jitter, packet loss, and geolocation data. Requires root privileges for VPN operations."],["HISTORY","**vortix** was created by **Harry-kp** and is written in **Rust**."],["INSTALL","```pacman: sudo pacman -S vortix```"],["SEE ALSO","[wg](/man/wg)(1), [openvpn](/man/openvpn)(1), [wg-cmd](/man/wg-cmd)(1)"]],"e":["wg","openvpn","wg-cmd"],"x":"sudo vortix"},"vue-serve":{"n":"vue-serve","t":"Development server for Vue components","s":[["TAGLINE","Development server for Vue components"],["TLDR","**Serve in development mode**\n\n```vue serve```\n\n**Serve specific file**\n\n```vue serve [App.vue]```\n\n**Serve on custom port**\n\n```vue serve --port [8080]```\n\n**Open browser automatically**\n\n```vue serve --open```"],["SYNOPSIS","**vue** **serve** [_options_] [_entry_]"],["PARAMETERS","**-o**, **--open**\n> Open browser automatically\n\n**-c**, **--copy**\n> Copy local URL to clipboard\n\n**-p**, **--port** _port_\n> Port number (default: 8080)\n\n**-h**, **--host** _host_\n> Host address (default: 0.0.0.0)"],["DESCRIPTION","**vue serve** starts a development server with hot-reload for instant prototyping of Vue single-file components without a full project setup. It requires the **@vue/cli-service-global** package to be installed globally.\n\nIn Vue CLI v5, the instant prototyping feature was removed and **vue serve** became an alias for **npm run serve**. For standalone component prototyping, Vue now recommends using **Vite** instead."],["INSTALL","```nix: nix profile install nixpkgs#vue```"],["SEE ALSO","[vue](/man/vue)(1), [vue-build](/man/vue-build)(1), [vite](/man/vite)(1)"]],"e":["vue","vue-build","vite"],"x":"vue serve"},"xwallpaper":{"n":"xwallpaper","t":"Lightweight X11 wallpaper setter","s":[["TAGLINE","Lightweight X11 wallpaper setter"],["TLDR","**Set wallpaper centered**\n\n```xwallpaper --center [image.jpg]```\n\n**Set wallpaper zoomed to fill**\n\n```xwallpaper --zoom [image.jpg]```\n\n**Stretch to fit screen**\n\n```xwallpaper --stretch [image.jpg]```\n\n**Tile wallpaper**\n\n```xwallpaper --tile [image.png]```\n\n**Set for specific output**\n\n```xwallpaper --output [HDMI-1] --zoom [image.jpg]```\n\n**Different wallpapers per output**\n\n```xwallpaper --output [HDMI-1] --zoom [wall1.jpg] --output [eDP-1] --center [wall2.jpg]```\n\n**Run as daemon** for output changes\n\n```xwallpaper --daemon --zoom [image.jpg]```"],["SYNOPSIS","**xwallpaper** [_options_] [_mode_] _file_..."],["PARAMETERS","**--center** _file_\n> Center image without scaling.\n\n**--zoom** _file_\n> Zoom to fill, cropping if needed.\n\n**--maximize** _file_\n> Scale to fit without cropping.\n\n**--stretch** _file_\n> Stretch to fill, ignoring aspect ratio.\n\n**--tile** _file_\n> Tile image from upper left.\n\n**--focus** _file_\n> Focus on selection with --trim.\n\n**--output** _name_\n> Target specific output (use xrandr to list).\n\n**--screen** _screen_\n> Target specific X screen.\n\n**--trim** _WxH[+X+Y]_\n> Trim input image.\n\n**--daemon**\n> Redraw on output changes.\n\n**--clear**\n> Clear wallpaper.\n\n**--no-randr**\n> Disable RandR support.\n\n**--no-atoms**\n> Don't set wallpaper atoms.\n\n**--debug**\n> Enable debug output.\n\n**--version**\n> Display version."],["DESCRIPTION","**xwallpaper** is a wallpaper setting utility for X11. It supports JPEG, PNG, and XPM formats with various scaling modes to fit different display configurations.\n\nThe tool supports multi-monitor setups through RandR, allowing different wallpapers per output. In daemon mode, it monitors output changes and redraws wallpapers automatically when displays are connected or resized.\n\nWallpaper atoms are set to support semi-transparent backgrounds in compatible applications.\n\nFor Wayland, consider using swaybg or wbg instead."],["CAVEATS","X11 only; won't work under pure Wayland. Daemon mode requires RandR support. Image formats depend on build configuration. Some window managers may override wallpaper."],["HISTORY","**xwallpaper** was created by Tobias Stoeckmann as a lightweight, dependency-minimal wallpaper setter for X11. It focuses on correctness and minimal resource usage compared to larger tools like feh."],["INSTALL","```apt: sudo apt install xwallpaper```\n\n```pacman: sudo pacman -S xwallpaper```\n\n```apk: sudo apk add xwallpaper```\n\n```zypper: sudo zypper install xwallpaper```\n\n```nix: nix profile install nixpkgs#xwallpaper```"],["SEE ALSO","[feh](/man/feh)(1), [nitrogen](/man/nitrogen)(1), [hsetroot](/man/hsetroot)(1), [xrandr](/man/xrandr)(1)"]],"e":["feh","nitrogen","hsetroot","xrandr"],"x":"xwallpaper --center [image.jpg]"},"zic":{"n":"zic","t":"Compile timezone data files","s":[["TAGLINE","Compile timezone data files"],["TLDR","**Compile timezone data** to default directory\n\n```zic [path/to/tzdata_file]```\n\n**Compile to a specific** output directory\n\n```zic -d [path/to/output_directory] [path/to/tzdata_file]```\n\n**Compile with leap second** information\n\n```zic -L [path/to/leapseconds] [path/to/tzdata_file]```\n\n**Set local timezone** link\n\n```sudo zic -l [America/New_York]```\n\n**Compile with verbose** warnings\n\n```zic -v [path/to/tzdata_file]```\n\n**Read timezone data from stdin**\n\n```zic -d [path/to/output_directory] -```"],["SYNOPSIS","**zic** [_OPTIONS_] [_FILE..._]"],["PARAMETERS","**-d** _DIRECTORY_\n> Output directory for compiled TZif files (default: system timezone directory).\n\n**-l** _timezone_\n> Use the given timezone as the local time, creating a localtime link.\n\n**-p** _timezone_\n> Use timezone's rules for POSIX-style TZ strings that lack transition rules.\n\n**-L** _leapsecondfile_\n> Read leap second information from the specified file.\n\n**-v**\n> Report warnings about the input data, including years outside the representable range.\n\n**-b** _(slim|fat)_\n> Output backward-compatibility data. \"fat\" generates extra entries for older software. \"slim\" keeps files small (default).\n\n**-r** _@lo/@hi_\n> Limit output to timestamps in the given range.\n\n**-D**\n> Do not create ancestor directories of output files.\n\n**-m** _mode_\n> Set file permissions on created TZif files.\n\n**--version**\n> Output version information and exit."],["DESCRIPTION","**zic** is the timezone compiler. It reads text files describing timezone rules (zone names, UTC offsets, daylight saving transitions) and produces binary TZif (Time Zone Information Format) files used by the C library's localtime() and related functions.\n\nInput files use a specific format with Rule, Zone, and Link lines that define timezone transitions. The compiled binary files are typically installed in /usr/share/zoneinfo/. The zic compiler is normally run as part of the tzdata package installation."],["CAVEATS","Requires properly formatted timezone source files. Typically run as root when writing to system directories. Incorrect data can break time handling system-wide. The **-y** option for year type commands is obsolete and no longer supported in modern versions."],["INSTALL","```apt: sudo apt install libc-bin```\n\n```apk: sudo apk add tzdata-utils```"],["SEE ALSO","[zdump](/man/zdump)(8), [tzselect](/man/tzselect)(8)"]],"e":["zdump","tzselect"],"x":"zic [path/to/tzdata_file]"}});
