window.LCL_BUCKET(27,{"admesh":{"n":"admesh","t":"STL mesh analysis, repair, and transformation tool","s":[["TAGLINE","STL mesh analysis, repair, and transformation tool"],["TLDR","**Display information** about an STL file\n\n```admesh [path/to/model.stl]```\n\n**Repair and output** a fixed STL file\n\n```admesh --write-binary-stl=[output.stl] [input.stl]```\n\n**Convert STL to ASCII format**\n\n```admesh --write-ascii-stl=[output.stl] [input.stl]```\n\n**Fix all defects** in an STL file\n\n```admesh --fill-holes --normal-directions --normal-values [input.stl] --write-binary-stl=[output.stl]```\n\n**Scale a model** by a factor\n\n```admesh --scale=[2.0] [input.stl] --write-binary-stl=[output.stl]```\n\n**Translate a model** along axes\n\n```admesh --translate=[10,20,30] [input.stl] --write-binary-stl=[output.stl]```\n\n**Rotate around the X axis**\n\n```admesh --x-rotate=[90] [input.stl] --write-binary-stl=[output.stl]```"],["SYNOPSIS","**admesh** [_options_] _input.stl_"],["PARAMETERS","**-b** _file_, **--write-binary-stl** _file_\n> Output repaired mesh in binary STL format.\n\n**-a** _file_, **--write-ascii-stl** _file_\n> Output repaired mesh in ASCII STL format.\n\n**--fill-holes**\n> Fill holes in the mesh by adding triangles.\n\n**--normal-directions**\n> Fix normals to point outward consistently.\n\n**--normal-values**\n> Recalculate normal vectors for all facets.\n\n**-e**, **--exact**\n> Check each facet of the mesh for its 3 neighbors using exact vertex matching.\n\n**-n**, **--nearby**\n> Find and connect facets that are almost aligned but do not match exactly due to rounding errors.\n\n**-t** _tolerance_, **--tolerance** _tolerance_\n> Search distance used by **--nearby**. Defaults to the shortest edge length of the mesh.\n\n**-i** _n_, **--iterations** _n_\n> Number of times to run the nearby check with progressively larger tolerances (default: 2).\n\n**-u**, **--remove-unconnected**\n> Remove facets that have zero neighbors after the exact and nearby checks.\n\n**--scale** _factor_\n> Scale the model by the given factor.\n\n**--translate** _x,y,z_\n> Translate the mesh so its minimum x, y, z values are at the given coordinates.\n\n**--x-rotate**, **--y-rotate**, **--z-rotate** _angle_\n> Rotate model counterclockwise about the specified axis (in degrees).\n\n**--xy-mirror**, **--yz-mirror**, **--xz-mirror**\n> Mirror the mesh in the specified plane.\n\n**--merge** _file_\n> Merge another STL file into the input (no automatic translation).\n\n**-v**, **--verbose**\n> Display detailed processing information."],["DESCRIPTION","**ADMesh** is a tool for processing and repairing STL (stereolithography) files used in 3D printing and CAD applications. It diagnoses and fixes common mesh problems including gaps, holes, inverted normals, and disconnected facets that would cause printing failures.\n\nWhen run without output options, ADMesh analyzes the input file and reports statistics including facet count, volume, surface area, and detected errors. The repair algorithms can automatically fix most mesh issues, making it essential for preparing models for 3D printing.\n\nADMesh supports both binary and ASCII STL formats and can convert between them. It also provides transformation operations for scaling, rotating, and translating models without requiring a full CAD application."],["CAVEATS","ADMesh processes only STL files; other 3D formats require conversion first. Very complex repairs may alter the original geometry. The **--exact** and **--nearby** algorithms may produce different results depending on mesh quality. Large files in ASCII format are significantly slower to process than binary."],["HISTORY","ADMesh was originally developed by **Anthony D. Martin** in the **mid-1990s** to repair STL files for rapid prototyping machines. It was open-sourced and has been maintained by the community since. The tool gained renewed popularity with the rise of consumer 3D printing in the **2010s** and is now commonly used alongside slicing software in FDM printing workflows."],["INSTALL","```apt: sudo apt install admesh```\n\n```dnf: sudo dnf install admesh```\n\n```aur: yay -S admesh```\n\n```brew: brew install admesh```"],["SEE ALSO","[meshlab](/man/meshlab)(1), [openscad](/man/openscad)(1), [blender](/man/blender)(1)"],["RESOURCES","```[Source code](https://github.com/admesh/admesh)```\n\n```[Documentation](https://admesh.readthedocs.io/)```"]],"e":["meshlab","openscad","blender"],"x":"admesh [path/to/model.stl]"},"alsamixer":{"n":"alsamixer","t":"Interactive ncurses audio mixer","s":[["TAGLINE","Interactive ncurses audio mixer"],["TLDR","Select the **soundcard** to use\n\n```alsamixer -c [soundcard_number]```\n\nSelect **mixer device** to control\n\n```alsamixer -D [device_name]```\n\nStart in **playback** view mode\n\n```alsamixer -V playback```\n\nStart with **black background**\n\n```alsamixer -B```"],["SYNOPSIS","**alsamixer** [_-c card_] [_-D device_] [_-V mode_]"],["DESCRIPTION","**alsamixer** is an ncurses mixer program for use with the ALSA soundcard drivers. It supports multiple soundcards with multiple devices and provides a graphical interface for adjusting audio levels."],["PARAMETERS","**-c, --card number**\n> Select soundcard (numbered from 0)\n\n**-D, --device id**\n> Select mixer device to control\n\n**-V, --view mode**\n> Set starting view (playback, capture, or all)\n\n**-B, --black-background**\n> Use black background color\n\n**-g, --no-color**\n> Disable colors"],["KEYBOARD SHORTCUTS","**Navigation:**\n> Left/Right arrows or n/p: select channels\n> Up/Down arrows or +/-: adjust volume\n\n**Volume Control:**\n> PageUp/PageDown: increase/decrease by 5\n> Home: set to 100%\n> End: set to 0%\n> 0-9: set to 0-90% volume\n> Q/W/E: raise left/both/right\n> Z/X/C: lower left/both/right\n\n**Muting & Capture:**\n> M: toggle mute (both channels)\n> , or <: mute left channel only\n> . or >: mute right channel only\n> Space: enable recording for current channel\n\n**View Modes:**\n> F1 or ?: help screen\n> F2 or /: system information\n> F3/F4/F5: playback/capture/all modes\n> Tab: toggle views circularly\n> F6 or S: select sound card\n\n**Other:**\n> H: show keyboard shortcuts reminder\n> B or =: balance left/right channels\n> L: redraw screen\n> Esc or Alt+Q: quit"],["CONFIGURATION","**/etc/asound.conf**\n> System-wide ALSA configuration file.\n\n**~/.asoundrc**\n> Per-user ALSA configuration file for custom device definitions and plugin settings."],["CAVEATS","Requires ALSA drivers to be installed and configured. Some options may not be available depending on the soundcard capabilities."],["HISTORY","Part of the **ALSA** (Advanced Linux Sound Architecture) utilities package, which replaced OSS as the standard Linux sound system."],["INSTALL","```apt: sudo apt install alsa-utils```\n\n```dnf: sudo dnf install alsa-utils```\n\n```pacman: sudo pacman -S alsa-utils```\n\n```apk: sudo apk add alsa-utils```\n\n```zypper: sudo zypper install alsa-utils```\n\n```nix: nix profile install nixpkgs#alsa-utils```"],["SEE ALSO","[amixer](/man/amixer)(1), [aplay](/man/aplay)(1), [arecord](/man/arecord)(1)"]],"e":["amixer","aplay","arecord"],"x":"alsamixer -c [soundcard_number]"},"antigen":{"n":"antigen","t":"Zsh plugin manager inspired by Vundle","s":[["TAGLINE","Zsh plugin manager inspired by Vundle"],["TLDR","**Load oh-my-zsh** framework\n\n```antigen use oh-my-zsh```\n\n**Install and load a plugin** bundle\n\n```antigen bundle [git]```\n\n**Load a plugin** from a GitHub repository\n\n```antigen bundle [zsh-users/zsh-autosuggestions]```\n\n**Load a theme** from oh-my-zsh\n\n```antigen theme [robbyrussell]```\n\n**Apply all changes** (required at end of .zshrc)\n\n```antigen apply```\n\n**Update all plugins**\n\n```antigen update```\n\n**List loaded bundles**\n\n```antigen list```\n\n**Clear the cache** and reload configuration\n\n```antigen reset```"],["SYNOPSIS","**antigen** _command_ [_arguments_]"],["PARAMETERS","**bundle** _spec_\n> Install (if needed) and load a plugin; accepts GitHub shorthand (user/repo) or full URLs\n\n**bundles**\n> Bulk load multiple plugins via heredoc syntax (one per line)\n\n**use** _framework_\n> Load a pre-packaged framework like oh-my-zsh or prezto\n\n**theme** _name_\n> Load and activate a zsh theme (call after antigen use)\n\n**apply**\n> Commit the configuration; must be called at the end of bundle declarations\n\n**update** [_bundle-name_]\n> Update specified bundle or all bundles if no name given\n\n**list** [**--long**]\n> Display currently loaded bundles; --long shows detailed information\n\n**revert**\n> Revert all plugins to their state before the last update\n\n**reset**\n> Clear the generated cache to reload configuration changes\n\n**selfupdate**\n> Update Antigen itself to the latest version\n\n**cleanup**\n> Remove plugins that are no longer loaded in configuration\n\n**purge** _bundle_\n> Remove a specific bundle from the system\n\n**snapshot**\n> Record the exact commit of every bundle into a snapshot file for reproducible setups\n\n**restore** _snapshot_\n> Reinstall all bundles at the versions recorded in a snapshot file"],["DESCRIPTION","**Antigen** is a plugin manager for zsh, inspired by Vundle for vim and Pathogen. It simplifies managing shell plugins (called bundles) by handling installation, loading, and updates automatically.\n\nBundles can be loaded from oh-my-zsh, prezto, or any Git repository. The typical workflow involves sourcing antigen.zsh, declaring bundles with **antigen bundle**, optionally loading a theme, and calling **antigen apply** to activate everything.\n\nExample **.zshrc** configuration:\n```\nsource ~/antigen.zsh\nantigen use oh-my-zsh\nantigen bundle git\nantigen bundle zsh-users/zsh-syntax-highlighting\nantigen theme robbyrussell\nantigen apply\n```\n\nAntigen caches compiled configurations for faster shell startup. After modifying bundle declarations, use **antigen reset** to clear the cache and reload changes."],["CAVEATS","Requires zsh version 4.3.11 or higher. Themes from oh-my-zsh may depend on framework functions, so **antigen use oh-my-zsh** should be called before **antigen theme**. Updates downloaded via **antigen update** require opening a new shell to take effect."],["HISTORY","Antigen was created by **Shrikant Sharat Kandula** around **2012** to bring Vundle-style plugin management to zsh. It became one of the first popular zsh plugin managers and helped popularize modular shell configuration. The project continues to be maintained by the zsh-users community on GitHub."],["INSTALL","```aur: yay -S antigen```\n\n```brew: brew install antigen```\n\n```nix: nix profile install nixpkgs#antigen```"],["SEE ALSO","[oh-my-zsh](/man/oh-my-zsh)(1), [zsh](/man/zsh)(1), [zinit](/man/zinit)(1), [zplug](/man/zplug)(1)"],["RESOURCES","```[Source code](https://github.com/zsh-users/antigen)```\n\n```[Documentation](https://github.com/zsh-users/antigen/wiki)```"]],"e":["oh-my-zsh","zsh","zinit","zplug"],"x":"antigen use oh-my-zsh"},"apptainer-verify":{"n":"apptainer-verify","t":"Verify cryptographic signatures on container images","s":[["TAGLINE","Verify cryptographic signatures on container images"],["TLDR","**Verify** a container image using the default PGP keyring\n\n```apptainer verify [path/to/image.sif]```\n\nVerify a container image using a **specific public key** file\n\n```apptainer verify --key [path/to/public.pem] [path/to/image.sif]```\n\nVerify a container image using a **certificate** file\n\n```apptainer verify --certificate [path/to/certificate.pem] [path/to/image.sif]```\n\nVerify **all objects** in the image\n\n```apptainer verify -a [path/to/image.sif]```\n\nVerify a **specific object group** within the image\n\n```apptainer verify -g [group_id] [path/to/image.sif]```\n\nVerify a **specific object by ID** within the image\n\n```apptainer verify -i [object_id] [path/to/image.sif]```\n\nOutput verification results in **JSON format**\n\n```apptainer verify -j [path/to/image.sif]```\n\nVerify using only **local keys** in the keyring\n\n```apptainer verify -l [path/to/image.sif]```\n\nVerify **legacy** (insecure) signatures\n\n```apptainer verify --legacy-insecure [path/to/image.sif]```"],["SYNOPSIS","**apptainer verify** [_options_] _image.sif_"],["DESCRIPTION","**apptainer verify** validates the cryptographic signatures on SIF container images. This confirms that the image has not been tampered with and was signed by a trusted party.\n\nVerification can use PGP public keys, X.509 certificates, or keys fetched from a keyserver. The command exits with a non-zero status if verification fails."],["PARAMETERS","**--key** _file_\n> Path to a public key file for verification\n\n**--certificate** _file_\n> Path to a certificate file for verification\n\n**-a, --all**\n> Verify all objects in the SIF image\n\n**-g, --group-id** _id_\n> Verify only a specific object group\n\n**-i, --sif-id** _id_\n> Verify only a specific object by ID\n\n**-j, --json**\n> Output results in JSON format\n\n**-l, --local**\n> Only verify with local key(s) in keyring\n\n**-u, --url** _string_\n> Specify a URL for a key server\n\n**--legacy-insecure**\n> Enable verification of (insecure) legacy signatures\n\n**--certificate-intermediates** _file_\n> Path to pool of intermediate certificates\n\n**--certificate-roots** _file_\n> Path to pool of root certificates\n\n**--ocsp-verify**\n> Enable online revocation check for certificates"],["CAVEATS","Verification requires the public key or certificate corresponding to the signing key. Unsigned containers will fail verification. Keys from untrusted sources should not be used for verification."],["INSTALL","```dnf: sudo dnf install apptainer```\n\n```pacman: sudo pacman -S apptainer```\n\n```apk: sudo apk add apptainer```\n\n```zypper: sudo zypper install apptainer```\n\n```brew: brew install apptainer```\n\n```nix: nix profile install nixpkgs#apptainer```"],["SEE ALSO","[apptainer](/man/apptainer)(1), [apptainer-sign](/man/apptainer-sign)(1), [gpg](/man/gpg)(1)"]],"e":["apptainer","apptainer-sign","gpg"],"x":"apptainer verify [path/to/image.sif]"},"audit2allow":{"n":"audit2allow","t":"Generate SELinux policy allow rules from audit logs.","s":[["TAGLINE","Generate SELinux policy allow rules from audit logs."],["TLDR","Generate **allow** rules\n\n```sudo audit2allow -a```\n\nFrom **log file**\n\n```sudo audit2allow -i /var/log/audit/audit.log```\n\nCreate **policy module**\n\n```sudo audit2allow -a -M my_module```\n\n**Explain** denials\n\n```sudo audit2allow -a -w```\n\nGenerate **reference** policy\n\n```sudo audit2allow -a -R```\n\nFor specific **service**\n\n```sudo ausearch -m avc -c httpd | audit2allow -M httpd_policy```"],["SYNOPSIS","**audit2allow** [_OPTIONS_]"],["DESCRIPTION","**audit2allow** generates SELinux policy allow rules from audit logs. It reads denial messages from the audit subsystem and creates type enforcement rules that would permit the denied operations.\n\nThe tool can produce simple allow rules for quick troubleshooting or generate complete loadable policy modules with the **-M** option. When used with **-R**, it generates reference policy using standard macros, producing cleaner and more maintainable rules. It is typically used after **audit2why** has identified the root cause of denials."],["PARAMETERS","**-a, --all**\n> Read input from audit and message logs.\n\n**-b, --boot**\n> Read input from audit messages since the last boot.\n\n**-d, --dmesg**\n> Read input from dmesg output.\n\n**-i, --input** _file_\n> Read input from the specified file.\n\n**-l, --lastreload**\n> Read only AVC denials since the last policy reload.\n\n**-m, --module** _name_\n> Generate module output (source, not packaged).\n\n**-M** _name_\n> Generate a loadable policy module package (.pp).\n\n**-o, --output** _file_\n> Append output to the given file.\n\n**-D, --dontaudit**\n> Generate dontaudit rules instead of allow rules.\n\n**-R, --reference**\n> Generate reference policy using installed interface macros.\n\n**-N, --noreference**\n> Do not generate reference policy; use traditional allow rules.\n\n**-w, --why**\n> Translate audit messages into a description of why access was denied.\n\n**-e, --explain**\n> Fully explain the generated output.\n\n**-x, --xperms**\n> Generate extended permission (ioctl) rules.\n\n**-t, --type** _regex_\n> Filter output by type regular expression.\n\n**-C**\n> Generate CIL (Common Intermediate Language) output.\n\n**-r, --requires**\n> Generate require statements for loadable modules.\n\n**-v, --verbose**\n> Enable verbose output."],["CAVEATS","Generated policies should be reviewed before installation. Blindly allowing all denials can create security vulnerabilities. Use audit2why first to understand why denials occurred."],["HISTORY","**audit2allow** is part of **policycoreutils-python-utils**, providing SELinux policy development tools."],["SEE ALSO","[audit2why](/man/audit2why)(1), [ausearch](/man/ausearch)(8), [semodule](/man/semodule)(8)"]],"e":["audit2why","ausearch","semodule"],"x":"sudo audit2allow -a"},"aws-codecommit":{"n":"aws-codecommit","t":"Manage Git repositories hosted on AWS CodeCommit.","s":[["TAGLINE","Manage Git repositories hosted on AWS CodeCommit."],["TLDR","**Create a new repository**\n\n```aws codecommit create-repository --repository-name [my-repo] --repository-description \"[Description]\"```\n\n**List all repositories** in the account\n\n```aws codecommit list-repositories```\n\n**Get details** about a repository\n\n```aws codecommit get-repository --repository-name [my-repo]```\n\n**Clone a repository** using git-remote-codecommit (install with pip install git-remote-codecommit)\n\n```git clone codecommit::[region]://[my-repo]```\n\n**Create a branch** from an existing commit\n\n```aws codecommit create-branch --repository-name [my-repo] --branch-name [feature-branch] --commit-id [abc123]```\n\n**Create a pull request**\n\n```aws codecommit create-pull-request --title \"[PR Title]\" --targets repositoryName=[my-repo],sourceReference=[feature-branch],destinationReference=main```\n\n**Put a file** directly to a repository\n\n```aws codecommit put-file --repository-name [my-repo] --branch-name main --file-content file://[local-file.txt] --file-path [path/in/repo.txt] --commit-message \"[message]\"```"],["SYNOPSIS","**aws codecommit** _command_ [_options_]"],["DESCRIPTION","**aws codecommit** is the AWS CLI interface for AWS CodeCommit, a fully managed source control service hosting secure Git repositories. CodeCommit eliminates the need to operate your own source control system and scales automatically to meet project needs.\n\nCodeCommit supports standard Git functionality including branches, commits, and merges. It adds AWS-specific features like pull request approval rules, repository triggers for event notifications, and integration with AWS IAM for access control."],["COMMANDS","**create-repository**\n> Create a new CodeCommit repository\n\n**delete-repository**\n> Delete a repository\n\n**list-repositories**\n> List all repositories in the account\n\n**get-repository**\n> Get metadata about a repository, including its clone URLs\n\n**create-branch**\n> Create a new branch\n\n**create-pull-request**\n> Create a pull request between branches\n\n**merge-pull-request-by-fast-forward**\n> Merge a pull request using fast-forward\n\n**put-file**\n> Add or update a file in a repository\n\n**get-file**\n> Retrieve file contents as base64\n\n**create-commit**\n> Create a commit with multiple file changes\n\n**create-approval-rule-template**\n> Define approval requirements for pull requests"],["CAVEATS","AWS CodeCommit is no longer available to new customers as of mid-**2024**; existing customers can continue using it. CodeCommit uses AWS IAM for authentication rather than SSH keys or passwords by default. For HTTPS access, configure either the Git credential helper or git-remote-codecommit. Individual files are limited to 2GB and there are quotas on API requests per second."],["HISTORY","AWS CodeCommit launched in **July 2015** as part of the AWS developer tools suite. It provided a Git-compatible managed repository as an alternative to GitHub Enterprise or self-hosted GitLab. Pull request support was added in **2017**, and approval templates for compliance workflows in **2019**."],["INSTALL","```dnf: sudo dnf install aws```\n\n```pacman: sudo pacman -S aws-cli```\n\n```apk: sudo apk add aws-cli```\n\n```zypper: sudo zypper install aws-cli```"],["SEE ALSO","[aws](/man/aws)(1), [git](/man/git)(1), [aws-codebuild](/man/aws-codebuild)(1), [aws-codepipeline](/man/aws-codepipeline)(1)"],["RESOURCES","```[Source code](https://github.com/aws/aws-cli)```\n\n```[Homepage](https://aws.amazon.com/cli/)```\n\n```[Documentation](https://docs.aws.amazon.com/cli/latest/reference/codecommit/index.html)```"]],"e":["aws","git","aws-codebuild","aws-codepipeline"],"x":"aws codecommit create-repository --repository-name [my-repo] --repository-description \"[Description]\""},"bd":{"n":"bd","t":"Navigate to parent directories by name","s":[["TAGLINE","Navigate to parent directories by name"],["TLDR","**Go back** to a parent directory by name (or its first few letters)\n\n```bd [name]```\n\n**Case-sensitive** match\n\n```bd -s [name]```\n\n**Run a command** in a parent directory without changing to it\n\n```ls `bd [name]` ```"],["SYNOPSIS","**bd** [_-si_] _name_"],["DESCRIPTION","**bd** (back directory) is a bash utility for quickly navigating to a parent directory by name instead of typing multiple `cd ../../../` commands. It searches upward in the current path for a directory whose name matches (matching on the first few letters is enough).\n\nIf several ancestors share the same name, bd jumps to the closest one (the immediate parent is not considered a match). It is installed as a shell alias so the directory change persists in your shell:\n\n```alias bd=\". bd -si\"```"],["PARAMETERS","**-s**\n> Case-sensitive matching (the default alias uses **-si** for case-insensitive).\n\n**-i**\n> Case-insensitive matching (no-op when already the default)."],["WORKFLOW","```bash"],["Jump up to 'myapp'","bd myapp"],["Or just the first letters","bd my"],["Use a parent path inline without leaving the current directory","cp file.txt `bd projects`\n```"],["FEATURES","- Prefix matching on parent directory names\n- Bash completion support (via the bundled completion file)\n- Closest-match resolution when names repeat in the path"],["CAVEATS","Requires installation and an alias in your shell config (the alias sources the script so the `cd` sticks). Only walks upward through the current path, not into sibling trees. Built for bash; separate ports exist for zsh and fish. Not available by default on most systems."],["HISTORY","**bd** was created by Vigneshwaran Raveendran (vigneshwaranr) to simplify navigating up directory trees by name."],["INSTALL","```aur: yay -S bd```"],["SEE ALSO","[cd](/man/cd)(1), [pushd](/man/pushd)(1), [z](/man/z)(1)"]],"e":["cd","pushd","z"],"x":"bd [name]"},"blktrace":{"n":"blktrace","t":"Trace block layer I/O events","s":[["TAGLINE","Trace block layer I/O events"],["TLDR","**Trace block I/O** on a device\n\n```blktrace -d [/dev/sda]```\n\n**Trace with output to specific directory**\n\n```blktrace -d [/dev/sda] -D [/tmp/traces]```\n\n**Trace multiple devices**\n\n```blktrace -d [/dev/sda] -d [/dev/sdb]```\n\n**Trace for a specific duration**\n\n```blktrace -d [/dev/sda] -w [10]```\n\n**Trace and pipe to blkparse**\n\n```blktrace -d [/dev/sda] -o - | blkparse -i -```\n\n**Trace with specific buffer size**\n\n```blktrace -d [/dev/sda] -b [1024]```\n\n**Stop tracing** (in another terminal)\n\n```killall blktrace```"],["SYNOPSIS","**blktrace** [_options_]"],["DESCRIPTION","**blktrace** is a Linux block layer I/O tracing utility that captures detailed information about block device operations. It tracks requests from submission through the I/O scheduler to completion, providing visibility into storage subsystem behavior.\n\nThe traces include information like queue insertions, merges, dispatch to driver, and completion events. Each event contains timestamp, process ID, action code, and I/O parameters (sector, size, flags).\n\nblktrace produces binary trace files that are analyzed with blkparse, btt, or other tools. It's invaluable for diagnosing I/O performance issues, understanding workload patterns, and tuning storage configurations."],["PARAMETERS","**-d** _device_\n> Device to trace (can specify multiple).\n\n**-o** _file_\n> Output file base name.\n\n**-D** _dir_\n> Directory for output files.\n\n**-w** _seconds_\n> Trace duration in seconds.\n\n**-b** _size_\n> Per-CPU buffer size in KiB (default 512).\n\n**-n** _num_\n> Number of per-CPU sub-buffers (default 4).\n\n**-a** _action_\n> Add an action to the trace filter mask (e.g. queue, complete).\n\n**-A** _mask_\n> Set the trace filter to the given hex action mask.\n\n**-r** _path_\n> debugfs mount point to use (default /sys/kernel/debug).\n\n**-I** _file_\n> Add the devices listed in _file_ as trace targets.\n\n**-l**, **--listen**\n> Run in server mode to receive traces over the network.\n\n**-h** _host_, **--host** _host_\n> Connect to the given network server when tracing.\n\n**-V**, **-v**\n> Display version."],["CAVEATS","Requires root privileges. Must have debugfs mounted (usually at /sys/kernel/debug). Creates binary trace files per CPU that need blkparse to read. High I/O rates can produce large trace files quickly. May impact performance on very busy systems due to trace overhead."],["HISTORY","**blktrace** was developed by **Jens Axboe** and merged into the Linux kernel in version **2.6.17** (released **2006**). It was created to provide detailed insight into the Linux block I/O layer, replacing older and less detailed tracing methods. The tool has become standard for storage performance analysis on Linux and is part of the blktrace package alongside blkparse and btt."],["INSTALL","```apt: sudo apt install blktrace```\n\n```dnf: sudo dnf install blktrace```\n\n```zypper: sudo zypper install blktrace```\n\n```nix: nix profile install nixpkgs#blktrace```"],["SEE ALSO","[blkparse](/man/blkparse)(1), [btrace](/man/btrace)(8), [btt](/man/btt)(1), [iostat](/man/iostat)(1), [iotop](/man/iotop)(1)"],["RESOURCES","```[Source code](https://git.kernel.org/pub/scm/linux/kernel/git/axboe/blktrace.git)```"]],"e":["blkparse","btrace","btt","iostat","iotop"],"x":"blktrace -d [/dev/sda]"},"brew-outdated":{"n":"brew-outdated","t":"List Homebrew packages with available updates","s":[["TAGLINE","List Homebrew packages with available updates"],["TLDR","**List outdated packages**\n\n```brew outdated```\n\n**List outdated formulae only**\n\n```brew outdated --formula```\n\n**List outdated casks only**\n\n```brew outdated --cask```\n\n**Show verbose version information**\n\n```brew outdated --verbose```\n\n**Output as JSON**\n\n```brew outdated --json```\n\n**Include auto-updating casks**\n\n```brew outdated --greedy```\n\n**Quiet mode (names only)**\n\n```brew outdated --quiet```"],["SYNOPSIS","**brew outdated** [_options_]"],["DESCRIPTION","**brew outdated** lists installed formulae and casks that have newer versions available. By default, version information is shown in interactive shells.\n\nThe command uses locally cached package information. Run **brew update** first to ensure accurate results."],["PARAMETERS","**--formula**\n> List outdated formulae only\n\n**--cask**\n> List outdated casks only\n\n**--verbose, -v**\n> Show detailed version information\n\n**--quiet, -q**\n> List names only (takes precedence over --verbose)\n\n**--json**\n> Output in JSON format (v2 includes both formulae and casks)\n\n**-g, --greedy**\n> Also include outdated casks with version :latest or auto_updates true\n\n**--greedy-latest**\n> Also include outdated casks including those with version :latest\n\n**--greedy-auto-updates**\n> Also include outdated casks including those with auto_updates true\n\n**--fetch-HEAD**\n> Fetch the upstream repository to detect outdated HEAD installations"],["OUTPUT FORMAT","Default: package_name (installed_version) != available_version\n\nQuiet mode: package_name only\n\nJSON: Full metadata including versions and timestamps"],["CAVEATS","Does not perform network requests; relies on local cache from brew update. Casks with auto_updates are excluded unless --greedy is specified. Some casks use :latest version which cannot be compared."],["SEE ALSO","[brew](/man/brew)(1), [brew-update](/man/brew-update)(1), [brew-upgrade](/man/brew-upgrade)(1), [brew-list](/man/brew-list)(1)"],["RESOURCES","```[Source code](https://github.com/Homebrew/brew)```\n\n```[Homepage](https://brew.sh)```\n\n```[Documentation](https://docs.brew.sh)```"]],"e":["brew","brew-update","brew-upgrade","brew-list"],"x":"brew outdated"},"bru":{"n":"bru","t":"Run API requests from Bruno collections","s":[["TAGLINE","Run API requests from Bruno collections"],["TLDR","**Run a single API request**\n\n```bru run [request.bru]```\n\n**Run all requests** in a folder\n\n```bru run [folder/]```\n\n**Run with a specific environment**\n\n```bru run [folder/] --env [production]```\n\n**Run with environment variables**\n\n```bru run [request.bru] --env-var [API_KEY=secret]```\n\n**Generate JSON, JUnit, and HTML reports**\n\n```bru run [folder/] --reporter-json [results.json] --reporter-junit [results.xml] --reporter-html [results.html]```\n\n**Import an OpenAPI spec** as a Bruno collection\n\n```bru import openapi --source [api.yml] --output [~/collections] --collection-name [MyAPI]```\n\n**Run and stop on first failure**\n\n```bru run [folder/] --env [staging] --bail```\n\n**Run with custom CA certificate**\n\n```bru run [request.bru] --cacert [/path/to/ca.crt]```"],["SYNOPSIS","**bru** **run** [_paths..._] [_options_]\n\n**bru** **import** _format_ [_options_]"],["PARAMETERS","**--env** _name_\n> Use a specific environment for the run.\n\n**--env-var** _key=value_\n> Set environment variables for the run.\n\n**--reporter-json** _file_\n> Output results in JSON format.\n\n**--reporter-junit** _file_\n> Output results in JUnit XML format.\n\n**--reporter-html** _file_\n> Output results in HTML format.\n\n**--cacert** _path_\n> Path to a CA certificate file for HTTPS requests.\n\n**--insecure**\n> Skip SSL certificate verification.\n\n**-r**, **--recursive**\n> Recursively run requests in subfolders.\n\n**--bail**\n> Stop execution after a failed request, test, or assertion.\n\n**--sandbox** _mode_\n> JavaScript execution mode: safe (default) or developer.\n\n**--parallel**\n> Run requests in parallel instead of sequentially.\n\n**--delay** _ms_\n> Delay between requests in milliseconds.\n\n**--tests-only**\n> Execute only requests containing tests or active assertions.\n\n**--csv-file-path** _file_\n> Run collection once for each row in a CSV file.\n\n**--env-file** _file_\n> Path to an environment file (.bru or .json)."],["DESCRIPTION","**bru** is the command-line interface for Bruno, an open-source API client similar to Postman or Insomnia. Bruno stores API collections directly in the filesystem using plain text .bru files, making them version-control friendly.\n\nThe CLI enables headless execution of API requests for CI/CD pipelines, automated testing, and scripted operations. Collections can be stored alongside code repositories for easy collaboration."],["CAVEATS","Requires Node.js runtime. Install with **npm install -g @usebruno/cli**. Starting from v3.0.0, the default runtime is Safe Mode; use **--sandbox=developer** to enable external npm packages and filesystem access. The CLI works with Bruno collection folders containing .bru files and optional environment configurations."],["HISTORY","Bruno was created as a lightweight, open-source alternative to proprietary API clients. The project emphasizes storing collections as code in plain text format for better Git integration and developer workflows."],["SEE ALSO","[curl](/man/curl)(1), [httpie](/man/httpie)(1), [newman](/man/newman)(1)"]],"e":["curl","httpie","newman"],"x":"bru run [request.bru]"},"buck":{"n":"buck","t":"Fast multi-language build system","s":[["TAGLINE","Fast multi-language build system"],["TLDR","**Build a target**\n\n```buck build //[path/to:target]```\n\n**Run a binary target**\n\n```buck run //[path/to:target]```\n\n**Run tests**\n\n```buck test //[path/to:target]```\n\n**Query target dependencies**\n\n```buck query \"deps(//[path/to:target])\"```\n\n**List all targets** in a package\n\n```buck targets //[path/to/package]:```\n\n**Build with specific configuration**\n\n```buck build //[target] --config [section.option=value]```\n\n**Clean build artifacts**\n\n```buck clean```\n\n**Show build output path**\n\n```buck build //[target] --show-output```"],["SYNOPSIS","**buck** _command_ [_options_] [_targets_]\n\n**buck2** _command_ [_options_] [_targets_]"],["DESCRIPTION","**Buck** is a fast, multi-language build system developed by Facebook (Meta). It uses a dependency graph to determine which targets need rebuilding, enabling incremental builds. Buck2 is the rewritten successor with improved performance.\n\nBuild rules are defined in BUCK files (or BUILD for Buck2) using a Python-like syntax. Targets are referenced using the //path/to/package:name convention. Buck caches build artifacts and can distribute builds across machines.\n\nBuck supports many languages including C++, Java, Python, Go, Rust, and others through its extensible rule system. It's designed for large monorepo codebases with complex dependency graphs."],["PARAMETERS","**build** _targets_\n> Build specified targets.\n\n**run** _target_\n> Build and run a binary target.\n\n**test** _targets_\n> Build and run tests.\n\n**clean**\n> Delete build artifacts.\n\n**targets** _pattern_\n> List targets matching pattern.\n\n**query** _expression_\n> Query the target graph.\n\n**audit** _subcommand_\n> Audit build configuration.\n\n**install** _target_\n> Build and install (mobile apps).\n\n**--config** _key=value_\n> Override configuration values.\n\n**--show-output**\n> Show output file paths.\n\n**--no-cache**\n> Ignore cached results.\n\n**-j** _jobs_\n> Number of parallel jobs.\n\n**-v** _verbosity_\n> Verbosity level (0-10).\n\n**--show-full-output**\n> Show full output paths including hash.\n\n**--help**\n> Show help information."],["CONFIGURATION","**.buckconfig**\n> Project-level configuration for Buck1, specifying build settings, tool paths, and repository options.\n\n**.buckroot**\n> Marker file indicating the root of a Buck project.\n\n**BUCK** / **BUILD**\n> Per-directory build files defining targets and rules (BUCK for Buck1, BUILD for Buck2)."],["CAVEATS","Requires Java runtime (Buck1) or standalone binary (Buck2). Build files must be in specific format. Large dependency graphs can cause slow initial analysis. Remote execution setup requires additional infrastructure. Buck2 is not fully backward compatible with Buck1."],["HISTORY","**Buck** was developed at **Facebook** starting around **2013** to handle the company's large codebase. It was open-sourced and used by companies like Uber and Airbnb. **Buck2** was announced in **2022** as a complete rewrite in Rust, offering significant performance improvements. Buck2 was open-sourced in **April 2023** and is now the recommended version."],["INSTALL","```aur: yay -S buck```"],["SEE ALSO","[bazel](/man/bazel)(1), [make](/man/make)(1), [ninja](/man/ninja)(1), [gradle](/man/gradle)(1)"]],"e":["bazel","make","ninja","gradle"],"x":"buck build //[path/to:target]"},"claws":{"n":"claws","t":"TUI for AWS resource management","s":[["TAGLINE","TUI for AWS resource management"],["TLDR","**Launch** with default AWS credentials\n\n```claws```\n\n**Use** a specific AWS profile\n\n```claws -p [myprofile]```\n\n**Target** a specific region\n\n```claws -r [us-west-2]```\n\n**Start** directly in the EC2 view\n\n```claws -s ec2```\n\n**Multi-profile** and multi-region mode\n\n```claws -p [dev],[prod] -r [us-east-1],[ap-northeast-1]```\n\n**Safe browsing** with no destructive actions\n\n```claws --read-only```"],["SYNOPSIS","**claws** [_flags_]"],["PARAMETERS","**-p**, **--profile** _name_\n> Specify AWS profile(s); comma-separated or repeated for multiple.\n\n**-r**, **--region** _name_\n> Specify AWS region(s); comma-separated or repeated for multiple.\n\n**-s**, **--service** _name_\n> Start directly on a specific service/view (e.g., dashboard, ec2, rds/snapshots).\n\n**--read-only**\n> Disable all destructive actions."],["DESCRIPTION","**claws** is an interactive terminal UI for managing AWS resources, inspired by **k9s** (the Kubernetes cluster TUI). It supports approximately 70 AWS services and 175 resource types, including EC2, S3, Lambda, RDS, ECS, EKS, IAM, and CloudWatch. It uses vim-style keybindings for navigation.\n\nKey capabilities include starting and stopping EC2 instances, cross-resource navigation (VPCs to subnets, functions to log groups), multi-profile and multi-region switching with parallel queries, fuzzy search and tag-based filtering, resource comparison with side-by-side diff, built-in AI assistant powered by AWS Bedrock, 6 color themes, and copy shortcuts for resource IDs and ARNs."],["KEY BINDINGS","```\nj/k      Navigate up/down     Enter/d  View details\n/        Fuzzy search         :        Command mode\na        Actions menu         A        AI Chat (Bedrock)\nR        Select region(s)     P        Select profile(s)\nTab      Cycle resource type  m        Mark for comparison\ny/Y      Copy ID / ARN        q        Quit\n```"],["CAVEATS","Requires valid AWS credentials configured via **~/.aws/credentials**, environment variables, or SSO. The AI Chat feature requires access to AWS Bedrock. The **--read-only** flag is recommended when browsing production environments to prevent accidental destructive actions."],["HISTORY","**claws** was first released in **December 2024** with rapid iteration through early 2025. Written in Go using the Bubbletea and Lipgloss TUI frameworks with the AWS SDK for Go v2. Licensed under Apache License 2.0."],["INSTALL","```nix: nix profile install nixpkgs#claws```"],["SEE ALSO","[aws](/man/aws)(1), [k9s](/man/k9s)(1), [lazydocker](/man/lazydocker)(1)"]],"e":["aws","k9s","lazydocker"],"x":"claws"},"codealmanac":{"n":"codealmanac","t":"Living wiki for codebases maintained by and for AI coding agents","s":[["TAGLINE","Living wiki for codebases maintained by and for AI coding agents"],["TLDR","**Setup** agent instructions and automation\n\n```codealmanac setup --yes```\n\n**Initialize** the wiki in the current repo\n\n```codealmanac init```\n\n**Search** the wiki\n\n```codealmanac search \"checkout timeout\"```\n\n**Show** a specific page\n\n```codealmanac show getting-started```\n\n**Ingest** material (file, PR, transcript) into the wiki\n\n```codealmanac ingest README.md --using codex```\n\n**Run** the local web viewer\n\n```codealmanac serve```"],["SYNOPSIS","**codealmanac** <command> [options]"],["DESCRIPTION","CodeAlmanac maintains a plain-markdown wiki (`almanac/`) inside your repository that captures decisions, architecture, invariants, gotchas and workflows that source code alone cannot express. AI agents (via local harnesses such as Claude or Codex) keep the wiki up to date through `ingest` and `garden` operations. All data stays local.\n\nHumans and agents use the same local CLI to search, read and validate the wiki."],["COMMANDS","**setup**\n> Install agent instructions and optional automation (sync, garden).\n\n**init**\n> Create the initial almanac/ tree and topics.\n\n**search**, **show**, **topics**, **health**, **validate**\n> Read and inspect the wiki.\n\n**ingest**, **garden**\n> Update the wiki from sources via the configured agent harness.\n\n**sync**, **jobs**, **serve**\n> Automation status, job logs, and local web viewer."],["CAVEATS","- Write operations require a working local agent harness (Codex or Claude) and credentials.\n- The wiki is committed Markdown; the CLI also maintains derived indexes under ~/.codealmanac/.\n- Not a hosted service in the current contract."],["SEE ALSO","[claude](/man/claude)(1), [codex](/man/codex)(1)"],["RESOURCES","```[Source code](https://github.com/AlmanacCode/codealmanac)```\n\n```[Homepage](https://codealmanac.com)```\n\n```[Documentation](https://github.com/AlmanacCode/codealmanac#readme)```"]],"e":["claude","codex"],"x":"codealmanac setup --yes"},"comby":{"n":"comby","t":"structural code search and replace tool","s":[["TAGLINE","structural code search and replace tool"],["TLDR","**Simple replacement**\n\n```comby 'console.log(:[arg])' 'logger.info(:[arg])' [.js]```\n\n**Replace in directory**\n\n```comby 'foo(:[x])' 'bar(:[x])' [src/] -matcher [.py]```\n\n**Preview changes**\n\n```comby 'old' 'new' -match-only```\n\n**Interactive review**\n\n```comby 'pattern' 'replacement' -review```\n\n**In-place modification**\n\n```comby 'old' 'new' -in-place [file.go]```\n\n**Diff output**\n\n```comby 'old' 'new' -diff [file.js]```"],["SYNOPSIS","**comby** _match_template_ _rewrite_template_ [_path_] [_options_]"],["DESCRIPTION","**comby** is a structural code search and replace tool for any language. Uses lightweight templates with holes (:[name]) to match code structures. Understands syntax of code blocks, strings, and comments."],["PARAMETERS","**-matcher** _ext_\n> File extension matcher (.py, .js, etc.)\n\n**-match-only**\n> Only show matches, don't rewrite\n\n**-in-place**\n> Modify files in place\n\n**-diff**\n> Show unified diff output\n\n**-review**\n> Interactive review mode\n\n**-stdin**\n> Read from stdin\n\n**-stdout**\n> Write to stdout\n\n**-count**\n> Count matches only\n\n**-d** _dir_\n> Target directory"],["HOLE SYNTAX","**:[name]**\n> Match any expression\n\n**:[name:e]**\n> Match balanced expression\n\n**:[[name]]**\n> Match across lines\n\n**:[_]**\n> Anonymous hole (discard)"],["CAVEATS","Better than regex for nested structures. Does not support indentation-sensitive languages (Python, Haskell)."],["INSTALL","```brew: brew install comby```\n\n```nix: nix profile install nixpkgs#comby```"],["SEE ALSO","[sed](/man/sed)(1), [ast-grep](/man/ast-grep)(1), [semgrep](/man/semgrep)(1)"],["RESOURCES","```[Source code](https://github.com/comby-tools/comby)```\n\n```[Homepage](https://comby.dev)```\n\n```[Documentation](https://comby.dev/docs/overview)```"]],"e":["sed","ast-grep","semgrep"],"x":"comby 'console.log(:[arg])' 'logger.info(:[arg])' [.js]"},"distrobox-host-exec":{"n":"distrobox-host-exec","t":"execute host commands from inside Distrobox container","s":[["TAGLINE","execute host commands from inside Distrobox container"],["TLDR","**Execute** command on host from inside container\n\n```distrobox-host-exec \"[command]\"```\n\nExecute **ls** on the host\n\n```distrobox-host-exec ls```"],["SYNOPSIS","**distrobox-host-exec** [_command_] [_arguments_]"],["DESCRIPTION","**distrobox-host-exec** executes a command on the host system from inside a Distrobox container. This allows accessing host binaries and tools that aren't available inside the container.\n\nUseful for running host-specific commands like flatpak, snap, or system utilities from within the container environment."],["PARAMETERS","_command_\n> Command to execute on host\n\n_arguments_\n> Arguments to pass to the command"],["CAVEATS","Only works from inside a Distrobox container. The command must exist on the host system. Environment variables may differ between container and host."],["INSTALL","```dnf: sudo dnf install distrobox```\n\n```pacman: sudo pacman -S distrobox```\n\n```apk: sudo apk add distrobox```\n\n```zypper: sudo zypper install distrobox```\n\n```brew: brew install distrobox```\n\n```nix: nix profile install nixpkgs#distrobox```"],["SEE ALSO","[distrobox](/man/distrobox)(1), [distrobox-enter](/man/distrobox-enter)(1)"],["RESOURCES","```[Source code](https://github.com/89luca89/distrobox)```\n\n```[Homepage](https://distrobox.it/)```"]],"e":["distrobox","distrobox-enter"],"x":"distrobox-host-exec \"[command]\""},"docker-image":{"n":"docker-image","t":"manage Docker images","s":[["TAGLINE","manage Docker images"],["TLDR","**List images**\n\n```docker image ls```\n\n**Pull an image**\n\n```docker image pull [image]:[tag]```\n\n**Remove an image**\n\n```docker image rm [image]```\n\n**Build an image**\n\n```docker image build -t [name] [path]```\n\n**Inspect image details**\n\n```docker image inspect [image]```\n\n**Show image history**\n\n```docker image history [image]```\n\n**Remove unused images**\n\n```docker image prune```"],["SYNOPSIS","**docker** **image** _command_ [_options_]"],["SUBCOMMANDS","**ls**\n> List images.\n\n**pull**\n> Download image from registry.\n\n**push**\n> Upload image to registry.\n\n**rm**\n> Remove images.\n\n**build**\n> Build image from Dockerfile.\n\n**inspect**\n> Display detailed information.\n\n**history**\n> Show image layers.\n\n**tag**\n> Create image tag.\n\n**prune**\n> Remove unused images.\n\n**save**\n> Save image to tar archive.\n\n**load**\n> Load image from tar archive.\n\n**import**\n> Import from tarball."],["DESCRIPTION","**docker image** manages Docker images, which are read-only templates used to create containers. Images contain the application code, runtime, libraries, environment variables, and configuration files needed to run software in an isolated environment.\n\nDocker images are built in layers, with each instruction in a Dockerfile creating a new layer. This layering system enables efficient storage through layer sharing between images and faster builds through caching. Images are identified by repository name, optional tag (defaulting to \"latest\"), and a unique SHA256 digest.\n\nThe **docker image** command group provides comprehensive image lifecycle management, from pulling images from registries, building custom images via Dockerfile, tagging for organization, to cleaning up unused images that consume disk space."],["INSTALL","```apt: sudo apt install docker-cli```\n\n```dnf: sudo dnf install docker-cli```\n\n```pacman: sudo pacman -S docker```\n\n```apk: sudo apk add docker-cli```\n\n```zypper: sudo zypper install docker```\n\n```brew: brew install docker```\n\n```nix: nix profile install nixpkgs#docker```"],["SEE ALSO","[docker](/man/docker)(1), [docker-image-ls](/man/docker-image-ls)(1), [docker-image-rm](/man/docker-image-rm)(1), [docker-image-pull](/man/docker-image-pull)(1)"]],"e":["docker","docker-image-ls","docker-image-rm","docker-image-pull"],"x":"docker image ls"},"docker-load":{"n":"docker-load","t":"load Docker images from tar archives","s":[["TAGLINE","load Docker images from tar archives"],["TLDR","**Load image from tar file**\n\n```docker load -i [image.tar]```\n\n**Load from stdin**\n\n```cat [image.tar] | docker load```\n\n**Load a gzip-compressed archive**\n\n```docker load -i [image.tar.gz]```\n\n**Load with quiet output**\n\n```docker load -q -i [image.tar]```\n\n**Load from a remote URL via curl**\n\n```curl -sSL [https://example.com/image.tar.gz] | docker load```"],["SYNOPSIS","**docker** **load** [_options_]"],["PARAMETERS","**-i**, **--input** _file_\n> Read from a tar archive file instead of STDIN. The tarball may be compressed with gzip, bzip2, or xz.\n\n**-q**, **--quiet**\n> Suppress the load output and progress bar.\n\n**--platform** _string_\n> Load only the specified platform image from a multi-platform archive (e.g., linux/amd64)."],["DESCRIPTION","**docker load** loads an image from a tar archive or STDIN. Restores both images and tags saved by **docker save**. This command is the counterpart to docker save, enabling image transfer between systems without using a registry. Useful for air-gapped environments or offline distribution."],["INSTALL","```apt: sudo apt install docker-cli```\n\n```dnf: sudo dnf install docker-cli```\n\n```pacman: sudo pacman -S docker```\n\n```apk: sudo apk add docker-cli```\n\n```zypper: sudo zypper install docker```\n\n```brew: brew install docker```\n\n```nix: nix profile install nixpkgs#docker```"],["SEE ALSO","[docker-image-load](/man/docker-image-load)(1), [docker-save](/man/docker-save)(1), [docker-images](/man/docker-images)(1), [docker-pull](/man/docker-pull)(1), [docker](/man/docker)(1)"]],"e":["docker-image-load","docker-save","docker-images","docker-pull","docker"],"x":"docker load -i [image.tar]"},"doctl-databases-maintenance-window":{"n":"doctl-databases-maintenance-window","t":"configure database cluster maintenance schedules","s":[["TAGLINE","configure database cluster maintenance schedules"],["TLDR","**Get maintenance window**\n\n```doctl databases maintenance-window get [cluster_id]```\n\n**Update maintenance window**\n\n```doctl databases maintenance-window update [cluster_id] --day [monday] --hour [02:00]```"],["SYNOPSIS","**doctl** **databases** **maintenance-window** _command_ [_options_]"],["SUBCOMMANDS","**get**\n> Get maintenance window.\n\n**update**\n> Update maintenance window."],["PARAMETERS","**--day** _string_\n> Day of week (monday through sunday).\n\n**--hour** _string_\n> Hour in UTC (00:00 through 23:00)."],["DESCRIPTION","**doctl databases maintenance-window** manages the maintenance window for DigitalOcean managed database clusters. Maintenance includes updates and patches applied during this window.\n\nDigitalOcean performs automated maintenance on managed databases including security patches, version updates, and system optimizations. The maintenance window defines when these operations can occur, allowing you to schedule them during low-traffic periods to minimize impact on your applications.\n\nYou can configure both the day of the week and the hour (in UTC) when maintenance should begin. Planning the window around your application's usage patterns helps ensure maintenance doesn't affect peak traffic times."],["SEE ALSO","[doctl-databases](/man/doctl-databases)(1)"],["RESOURCES","```[Source code](https://github.com/digitalocean/doctl)```\n\n```[Documentation](https://docs.digitalocean.com/reference/doctl/reference/databases/)```"]],"e":["doctl-databases"],"x":"doctl databases maintenance-window get [cluster_id]"},"doctl-serverless":{"n":"doctl-serverless","t":"manage DigitalOcean Functions serverless computing","s":[["TAGLINE","manage DigitalOcean Functions serverless computing"],["TLDR","**Connect to serverless namespace**\n\n```doctl serverless connect```\n\n**Deploy functions**\n\n```doctl serverless deploy [path]```\n\n**List functions**\n\n```doctl serverless functions list```\n\n**Invoke a function**\n\n```doctl serverless functions invoke [name]```\n\n**Get function logs**\n\n```doctl serverless activations logs [activation_id]```\n\n**Initialize a project**\n\n```doctl serverless init --language [js] [path]```\n\n**Redeploy on every change** while developing\n\n```doctl serverless watch [path]```"],["SYNOPSIS","**doctl** **serverless** _command_ [_arguments_] [_flags_]"],["SUBCOMMANDS","**install**\n> Install the serverless support plugin that the other subcommands depend on.\n\n**upgrade** / **uninstall**\n> Bring the serverless plugin in line with this version of doctl, or remove it.\n\n**connect** [_namespace_]\n> Connect local serverless support to a functions namespace.\n\n**namespaces**\n> Create, list, and delete functions namespaces.\n\n**status**\n> Show whether serverless support is installed and which namespace is connected.\n\n**init** _path_\n> Create a functions project directory. **--language** picks the runtime (`js`, `py`, `go`, `php`, ...) and **--overwrite** replaces an existing directory.\n\n**deploy** _path_\n> Deploy a functions project to the connected namespace.\n\n**watch** _path_\n> Watch a project directory and redeploy incrementally on every change.\n\n**undeploy**\n> Remove deployed functions or packages from the namespace.\n\n**get-metadata** _path_\n> Print the metadata of a functions project.\n\n**functions**\n> Work with the functions in the namespace: **list**, **get**, and **invoke**.\n\n**activations**\n> Retrieve activation records: **list**, **get**, **logs**, and **result**.\n\n**key**\n> Manage access keys for functions namespaces."],["DESCRIPTION","**doctl serverless** manages DigitalOcean Functions, a serverless computing platform that allows deploying and running code without managing infrastructure. Functions respond to events and HTTP requests, scaling automatically based on demand.\n\nThe platform supports multiple programming languages including JavaScript (Node.js), Python, PHP, and Go. Functions are organized into namespaces and deployed as projects containing one or more function definitions. Each function activation is logged and can be inspected for debugging.\n\nServerless computing is ideal for event-driven workloads, API endpoints, background processing, and applications with variable traffic patterns. DigitalOcean Functions handles scaling, load balancing, and infrastructure management automatically.\n\nUnder the hood the platform is built on **Apache OpenWhisk**, which is why projects are described by a `project.yml` file and why every invocation produces an *activation* record with its own ID, logs, and result."],["CAVEATS","The serverless subcommands are not part of the doctl binary itself: **doctl serverless install** downloads a plugin first, and **doctl serverless upgrade** must be re-run after doctl is updated or the commands refuse to work. You also have to **connect** to a namespace before deploying, and a namespace belongs to exactly one region. Functions are subject to platform limits on execution time, memory, and payload size, and long-running or stateful workloads belong in App Platform or a Droplet instead."],["INSTALL","```dnf: sudo dnf install doctl```\n\n```pacman: sudo pacman -S doctl```\n\n```apk: sudo apk add doctl```\n\n```brew: brew install doctl```\n\n```nix: nix profile install nixpkgs#doctl```"],["SEE ALSO","[doctl](/man/doctl)(1), [doctl-apps](/man/doctl-apps)(1)"],["RESOURCES","```[Source code](https://github.com/digitalocean/doctl)```\n\n```[Documentation](https://docs.digitalocean.com/reference/doctl/reference/serverless/)```"]],"e":["doctl","doctl-apps"],"x":"doctl serverless connect"},"es":{"n":"es","t":"Extensible shell with functional programming features","s":[["TAGLINE","Extensible shell with functional programming features"],["TLDR","**Start es shell**\n\n```es```\n\n**Run command**\n\n```es -c \"[echo hello]\"```\n\n**Run script**\n\n```es [script.es]```\n\n**Start without initialization**\n\n```es -p```"],["SYNOPSIS","**es** [_options_] [_script_] [_args_]"],["PARAMETERS","**-c** _command_\n> Execute command string.\n\n**-p**\n> Don't load profile.\n\n**-l**\n> Act as login shell.\n\n**-v**\n> Print input to stderr.\n\n**-x**\n> Print commands before execution.\n\n**-i**\n> Force interactive mode.\n\n**-n**\n> Parse only, don't execute."],["CONFIGURATION","**~/.esrc**\n> User initialization file loaded at startup (unless -p flag is used)."],["DESCRIPTION","**es** is an extensible shell derived from rc (the Plan 9 shell) with first-class functions, lexical scoping, and exception handling. It provides a clean, programmable shell with functional programming features.\n\nUnlike traditional shells, es treats functions as first-class values, supports closures, and has a simpler, more consistent syntax than bash or zsh."],["CAVEATS","Not POSIX-compatible; scripts need rewriting. Smaller community and fewer resources than bash. Some features differ from rc. May not be available on all systems."],["HISTORY","es was created by **Paul Haahr** and **Byron Rakitzis** in the early **1990s** as an enhanced version of rc, the Plan 9 shell. It added functional programming concepts like first-class functions and lexical scoping to shell scripting."],["INSTALL","```apk: sudo apk add es-shell```\n\n```nix: nix profile install nixpkgs#es```"],["SEE ALSO","[rc](/man/rc)(1), [bash](/man/bash)(1), [zsh](/man/zsh)(1), [fish](/man/fish)(1)"]],"e":["rc","bash","zsh","fish"],"x":"es"},"fossil-new":{"n":"fossil-new","t":"alias for fossil init","s":[["TAGLINE","alias for fossil init"],["TLDR","**Create new repository**\n\n```fossil new [repo.fossil]```\n\n**Create with project name**\n\n```fossil new --project-name \"[name]\" [repo.fossil]```\n\n**Create with a specific admin user**\n\n```fossil new -A [username] [repo.fossil]```\n\n**Create using settings from an existing repository as template**\n\n```fossil new --template [existing.fossil] [repo.fossil]```\n\n**Create with project name and description**\n\n```fossil new --project-name \"[name]\" --project-desc \"[description]\" [repo.fossil]```"],["SYNOPSIS","**fossil** **new** [_options_] _repository_"],["PARAMETERS","**-A, --admin-user** _USERNAME_\n> Select the given username as the admin user instead of the current login name.\n\n**--template** _FILE_\n> Copy initial settings from the specified repository file. Almost all settings accessible from the setup page will be copied, but normal users and their permissions will not.\n\n**--project-name** _STRING_\n> Set the name of the project.\n\n**--project-desc** _STRING_\n> Set the description of the project.\n\n**--date-override** _DATETIME_\n> Use the specified datetime as the time of the initial check-in. Accepts \"now\" or \"YYYY-MM-DDTHH:MM:SS.SSS\" format with optional timezone offset.\n\n**--sha1**\n> Use an initial hash policy of SHA1 instead of the default SHA3-256."],["DESCRIPTION","**fossil new** is an alias for **fossil init**. It creates a new Fossil repository file with identical functionality. All options and behaviors are identical to **fossil init**."],["SEE ALSO","[fossil-init](/man/fossil-init)(1), [fossil-commit](/man/fossil-commit)(1)"]],"e":["fossil-init","fossil-commit"],"x":"fossil new [repo.fossil]"},"gdbserver":{"n":"gdbserver","t":"remote debugging stub for GDB","s":[["TAGLINE","remote debugging stub for GDB"],["TLDR","**Start debug server**\n\n```gdbserver :[port] [program]```\n\n**Attach to process**\n\n```gdbserver --attach :[port] [pid]```\n\n**Multi-process mode**\n\n```gdbserver --multi :[port]```\n\n**Debug with arguments**\n\n```gdbserver :[port] [program] [arg1] [arg2]```"],["SYNOPSIS","**gdbserver** _comm_ [_program_] [_args_...]"],["PARAMETERS","_COMM_\n> Connection: :port or host:port.\n\n_PROGRAM_\n> Program to debug.\n\n**--attach** _PID_\n> Attach to running process.\n\n**--multi**\n> Multi-process mode.\n\n**--once**\n> Exit after client disconnects.\n\n**--debug**\n> Enable debug output.\n\n**--help**\n> Display help information."],["DESCRIPTION","**gdbserver** is a lightweight remote debugging stub that runs on the target system, allowing a full GDB debugger running on a different machine to control and debug programs remotely. This architecture is essential for embedded development, cross-compilation workflows, and debugging on resource-constrained devices.\n\nThe server component runs on the target system (embedded device, remote server, or different architecture), while the full GDB client runs on the developer's workstation. Communication occurs over TCP/IP or serial connections, with gdbserver translating GDB's debugging protocol into system-level debugging operations.\n\nThis split architecture minimizes the footprint on the target system, as gdbserver is much smaller than full GDB and requires fewer dependencies. It's particularly valuable for embedded Linux systems, IoT devices, and scenarios where the target lacks sufficient resources for a complete debugging environment.\n\nTypical workflows involve starting gdbserver on the target with the program to debug, then connecting from a GDB client using commands like \"target remote host:port\". The full power of GDB is available remotely, including breakpoints, single-stepping, memory inspection, and core file generation."],["CAVEATS","Requires network access to target. Security of debug connection. Target needs gdbserver installed."],["HISTORY","gdbserver is part of **GDB** (GNU Debugger), enabling remote debugging scenarios since GDB 4.x. It's essential for embedded development and cross-platform debugging."],["INSTALL","```apt: sudo apt install gdbserver```\n\n```dnf: sudo dnf install gdb```\n\n```pacman: sudo pacman -S gdb```\n\n```apk: sudo apk add gdb```\n\n```zypper: sudo zypper install gdbserver```\n\n```brew: brew install gdb```\n\n```nix: nix profile install nixpkgs#gdb```"],["SEE ALSO","[gdb](/man/gdb)(1)"]],"e":["gdb"],"x":"gdbserver :[port] [program]"},"git-revert":{"n":"git-revert","t":"Create commits that undo previous changes","s":[["TAGLINE","Create commits that undo previous changes"],["TLDR","**Revert a commit**\n\n```git revert [commit]```\n\n**Revert multiple commits**\n\n```git revert [commit1] [commit2]```\n\n**Revert without committing**\n\n```git revert -n [commit]```\n\n**Revert merge commit**\n\n```git revert -m [1] [merge_commit]```\n\n**Continue after conflict**\n\n```git revert --continue```\n\n**Abort revert**\n\n```git revert --abort```"],["SYNOPSIS","**git** **revert** [_options_] _commit_..."],["PARAMETERS","**-n**, **--no-commit**\n> Apply revert to working tree and index without creating a commit.\n\n**-e**, **--edit**\n> Edit the commit message before committing (default when run interactively).\n\n**--no-edit**\n> Use the default generated commit message.\n\n**-m** _parent-number_, **--mainline** _parent-number_\n> Specify the mainline parent (1-based) when reverting a merge commit.\n\n**-s**, **--signoff**\n> Add a `Signed-off-by` trailer to the commit message.\n\n**-S**[_keyid_], **--gpg-sign**[=_keyid_]\n> GPG-sign the revert commit.\n\n**--cleanup=**_mode_\n> Control how the commit message is cleaned up (see git-commit).\n\n**--strategy=**_strategy_\n> Use the specified merge strategy.\n\n**-X** _option_, **--strategy-option=**_option_\n> Pass an option through to the merge strategy.\n\n**--continue**\n> Continue the revert operation after resolving conflicts.\n\n**--skip**\n> Skip the current commit and continue with the remaining ones.\n\n**--abort**\n> Cancel the in-progress revert and restore the pre-sequence state.\n\n**--quit**\n> Forget about the current operation; leave index and working tree as-is."],["DESCRIPTION","**git revert** creates new commits that undo the changes introduced by specified commits. Unlike `git reset`, it preserves history by adding inverse commits rather than removing existing ones.\n\nThis makes it safe for shared branches where rewriting history would cause problems. Use `-m` to specify the mainline parent when reverting merge commits."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-reset](/man/git-reset)(1), [git-cherry-pick](/man/git-cherry-pick)(1)"]],"e":["git-reset","git-cherry-pick"],"x":"git revert [commit]"},"go-vet":{"n":"go-vet","t":"Static analysis for Go source code","s":[["TAGLINE","Static analysis for Go source code"],["TLDR","**Check current package**\n\n```go vet```\n\n**Check specific package**\n\n```go vet [package]```\n\n**Check all packages**\n\n```go vet ./...```\n\n**Run specific analyzer**\n\n```go vet -[analyzer] [package]```\n\n**List analyzers**\n\n```go vet -help```"],["SYNOPSIS","**go vet** [_options_] [_packages_]"],["PARAMETERS","_PACKAGES_\n> Packages to check.\n\n**-json**\n> JSON output.\n\n**-c** _N_\n> Display offending line with N lines of context.\n\n**-v**\n> Verbose output.\n\n**-n**\n> Print commands but do not run them.\n\n**-x**\n> Print the commands as they are executed.\n\n**-vettool** _prog_\n> Select a different analysis tool.\n\n**-tags** _TAGS_\n> Build tags."],["DESCRIPTION","**go vet** examines Go source code and reports suspicious constructs that the compiler does not catch. It finds bugs like incorrect printf format strings, unreachable code, and misuse of sync primitives.\n\nThe tool runs multiple analyzers that check for common mistakes. It is part of the standard quality assurance workflow alongside testing and formatting."],["CAVEATS","Not exhaustive; false positives are possible. Use alongside tests and code review. Run `go help vet` for full documentation. Additional analyzers (like shadow) require `-vettool`."],["HISTORY","go vet is part of the **Go** toolchain, providing static analysis for catching common programming errors."],["INSTALL","```apt: sudo apt install gccgo-go```\n\n```dnf: sudo dnf install gcc-go```\n\n```pacman: sudo pacman -S gcc-go```\n\n```apk: sudo apk add gcc-go```\n\n```zypper: sudo zypper install gcc-go```\n\n```brew: brew install go```\n\n```nix: nix profile install nixpkgs#go```"],["SEE ALSO","[go](/man/go)(1), [staticcheck](/man/staticcheck)(1), [golint](/man/golint)(1)"]],"e":["go","staticcheck","golint"],"x":"go vet"},"gshadow":{"n":"gshadow","t":"shadow group password file format","s":[["TAGLINE","shadow group password file format"],["TLDR","**View gshadow (requires root)**\n\n```sudo cat /etc/gshadow```\n\n**Edit gshadow safely**\n\n```sudo vigr -s```\n\n**Check gshadow consistency**\n\n```sudo grpck```"],["DESCRIPTION","**/etc/gshadow** contains encrypted group passwords and group administrator information. It is the shadow file for /etc/group, restricting access to sensitive group information.\n\nEach line follows the format: **groupname:password:admins:members**. The password field uses * for no password set, ! for password disabled, or an encrypted hash like $6$ for SHA-512.\n\nGroup administrators listed in the admins field can add/remove members and change the group password without root privileges using gpasswd."],["FILE FORMAT","```\ngroupname:password:admins:members\n```\n\n**Fields:**\n- **groupname**: Group name (matches /etc/group)\n- **password**: Encrypted password (! or * means disabled)\n- **admins**: Comma-separated list of group administrators\n- **members**: Comma-separated list of group members"],["PASSWORD FIELD","```\n*   - No password set\n!   - Password disabled\n$6$ - SHA-512 encrypted password\n```"],["GROUP ADMINISTRATORS","Administrators can add/remove members and change the group password without root privileges using gpasswd."],["CAVEATS","File permissions must be 0640 or stricter. Edit with vigr -s to prevent corruption. Not all systems use gshadow. Group passwords are rarely used."],["SEE ALSO","[group](/man/group)(5), [gpasswd](/man/gpasswd)(1), [grpck](/man/grpck)(8)"]],"e":["group","gpasswd","grpck"],"x":"sudo cat /etc/gshadow"},"hstr":{"n":"hstr","t":"enhanced shell history browser","s":[["TAGLINE","enhanced shell history browser"],["TLDR","**Launch hstr**\n\n```hstr```\n\n**Search command history**\n\n```hstr [pattern]```\n\n**Show favorites**\n\n```hstr -f```\n\n**Non-interactive search**\n\n```hstr -n [pattern]```\n\n**Add current binding**\n\n```hstr --show-configuration >> ~/.bashrc```"],["SYNOPSIS","**hstr** [_options_] [_pattern_]"],["DESCRIPTION","**hstr** (HiSToRy) is an enhanced shell history browser. It provides a visual interface for searching and selecting commands from history, with ranking by frequency and recency.\n\nThe tool integrates with bash and zsh, replacing or augmenting Ctrl+R with a more powerful search interface."],["PARAMETERS","**-f**, **--favorites**\n> Show favorite commands.\n\n**-n**, **--non-interactive**\n> Non-interactive mode.\n\n**-s**, **--show-configuration**\n> Show shell configuration.\n\n**-b**, **--show-blacklist**\n> Show blacklisted commands.\n\n**--show-zsh-configuration**\n> Show zsh configuration for integration.\n\n**-V**, **--version**\n> Show version."],["ENVIRONMENT","**HSTR_CONFIG**\n> Comma-separated configuration options: hicolor, monochromatic, case-sensitive, raw-history-view, favorites-view, duplicates, verbose-kill, blacklist, keywords-matching, static-favorites, skip-favorites-comments."],["KEYBOARD SHORTCUTS","**Ctrl+R**: Search history\n**Tab** / **Right**: Complete selection\n**Ctrl+F**: Add to favorites\n**Ctrl+/** / **Ctrl+7**: Rotate view (history/favorites/sorted)\n**Ctrl+E**: Toggle regex mode\n**Ctrl+L**: Toggle case sensitivity\n**Del**: Remove command from history"],["CAVEATS","Requires shell configuration. Large history may be slow initially. Favorites stored separately. Works best with large history limit."],["HISTORY","**hstr** was created by **Martin Dvorak** to improve command-line history navigation. It addresses the limitations of basic Ctrl+R search by providing visual feedback and better ranking algorithms."],["INSTALL","```apt: sudo apt install hstr```\n\n```dnf: sudo dnf install hstr```\n\n```apk: sudo apk add hstr```\n\n```brew: brew install hstr```\n\n```nix: nix profile install nixpkgs#hstr```"],["SEE ALSO","[fzf](/man/fzf)(1), [mcfly](/man/mcfly)(1), [history](/man/history)(1), [bash](/man/bash)(1)"]],"e":["fzf","mcfly","history","bash"],"x":"hstr"},"hyprpm":{"n":"hyprpm","t":"plugin manager for Hyprland, a dynamic tiling Wayland compositor","s":[["TAGLINE","plugin manager for Hyprland, a dynamic tiling Wayland compositor"],["TLDR","**Add** a plugin from Git repository\n\n```hyprpm add [git_url]```\n\n**Remove** a plugin\n\n```hyprpm remove [plugin_name]```\n\n**Enable** a plugin\n\n```hyprpm enable [plugin_name]```\n\n**Disable** a plugin\n\n```hyprpm disable [plugin_name]```\n\n**Update** all plugins\n\n```hyprpm update```\n\n**List** all installed plugins\n\n```hyprpm list```\n\n**Force** an operation\n\n```hyprpm -f [operation]```"],["SYNOPSIS","**hyprpm** [_options_] _command_ [_arguments_]"],["PARAMETERS","**add** _URL_\n> Add a plugin from a Git repository URL\n\n**remove** _NAME_\n> Remove an installed plugin\n\n**enable** _NAME_\n> Enable a disabled plugin\n\n**disable** _NAME_\n> Disable an enabled plugin\n\n**update**\n> Update and rebuild all plugins\n\n**list**\n> List all installed plugins\n\n**-f**, **--force**\n> Force the operation even if it would normally fail"],["DESCRIPTION","**hyprpm** is the plugin manager for Hyprland, a dynamic tiling Wayland compositor. It handles downloading, building, enabling, and updating Hyprland plugins from Git repositories.\n\nPlugins extend Hyprland's functionality with features like borders, animations, workspaces modifications, and other compositor enhancements. Plugins are compiled against the current Hyprland version and may need rebuilding after Hyprland updates."],["CAVEATS","Plugins must be compatible with the current Hyprland version. After Hyprland updates, plugins may need to be rebuilt using hyprpm update. Requires build tools for plugin compilation."],["HISTORY","hyprpm was introduced as part of the Hyprland ecosystem to provide a standardized way to manage compositor plugins. Hyprland gained popularity starting in **2022** for its extensive customization options."],["SEE ALSO","[hyprctl](/man/hyprctl)(1), [hyprpaper](/man/hyprpaper)(1), [hyprland](/man/hyprland)(1)"]],"e":["hyprctl","hyprpaper","hyprland"],"x":"hyprpm add [git_url]"},"ibmcloud-ks":{"n":"ibmcloud-ks","t":"manages IBM Cloud Kubernetes Service","s":[["TAGLINE","manages IBM Cloud Kubernetes Service"],["TLDR","**List clusters**\n\n```ibmcloud ks clusters```\n\n**Create cluster**\n\n```ibmcloud ks cluster create classic --name [my-cluster]```\n\n**Get cluster config**\n\n```ibmcloud ks cluster config --cluster [cluster-name]```\n\n**List worker nodes**\n\n```ibmcloud ks workers --cluster [cluster-name]```\n\n**Show cluster info**\n\n```ibmcloud ks cluster get --cluster [cluster-name]```"],["SYNOPSIS","**ibmcloud ks** _command_ [_options_]"],["PARAMETERS","_COMMAND_\n> Kubernetes Service command.\n\n**clusters**\n> List clusters.\n\n**cluster create**\n> Create cluster.\n\n**cluster config**\n> Get kubeconfig.\n\n**workers**\n> List worker nodes.\n\n**cluster get**\n> Show cluster info.\n\n**--help**\n> Display help information."],["DESCRIPTION","**ibmcloud ks** manages IBM Cloud Kubernetes Service. It creates and manages Kubernetes clusters on IBM Cloud infrastructure.\n\nThe tool handles cluster lifecycle, worker pools, and configuration. It integrates with kubectl for cluster access."],["CAVEATS","IKS plugin required. Cluster costs apply. Regional deployment options."],["HISTORY","ibmcloud ks is the Kubernetes Service plugin for **IBM Cloud** CLI."],["SEE ALSO","[ibmcloud](/man/ibmcloud)(1), [kubectl](/man/kubectl)(1), [ibmcloud-cr](/man/ibmcloud-cr)(1)"]],"e":["ibmcloud","kubectl","ibmcloud-cr"],"x":"ibmcloud ks clusters"},"impacket-smbserver":{"n":"impacket-smbserver","t":"creates an SMB server to share files","s":[["TAGLINE","creates an SMB server to share files"],["TLDR","**Start SMB server sharing current directory**\n\n```impacket-smbserver [sharename] .```\n\n**Share specific directory**\n\n```impacket-smbserver [sharename] [/path/to/share]```\n\n**Use SMB2 protocol**\n\n```impacket-smbserver -smb2support [sharename] [path]```\n\n**Require authentication**\n\n```impacket-smbserver -username [user] -password [pass] [sharename] [path]```\n\n**Specify IP to bind**\n\n```impacket-smbserver -ip [0.0.0.0] [sharename] [path]```"],["SYNOPSIS","**impacket-smbserver** [_options_] _sharename_ _path_"],["PARAMETERS","**-smb2support**\n> Enable SMB2 protocol support.\n\n**-username** _user_\n> Require username for auth.\n\n**-password** _pass_\n> Require password for auth.\n\n**-ip** _address_\n> IP address to bind.\n\n**-port** _port_\n> Port to listen on (default 445)."],["DESCRIPTION","**impacket-smbserver** creates an SMB server to share files. Part of the Impacket toolkit. Useful for transferring files to/from Windows systems during penetration testing. Supports SMB1 and SMB2 protocols and optional authentication."],["INSTALL","```pacman: sudo pacman -S impacket```"],["SEE ALSO","[samba](/man/samba)(1), [impacket-smbclient](/man/impacket-smbclient)(1)"]],"e":["samba","impacket-smbclient"],"x":"impacket-smbserver [sharename] ."},"ip6tables-restore":{"n":"ip6tables-restore","t":"Restore IPv6 firewall rules from file","s":[["TAGLINE","Restore IPv6 firewall rules from file"],["TLDR","This command is an alias of **iptables-restore** for IPv6 firewall\n\n```tldr iptables-restore```"],["SYNOPSIS","**ip6tables-restore** [_options_] [_file_]"],["DESCRIPTION","**ip6tables-restore** restores IPv6 firewall rules previously saved by **ip6tables-save**. It is functionally identical to **iptables-restore** but operates on the IPv6 **ip6tables** ruleset instead of the IPv4 **iptables** ruleset."],["INSTALL","```pacman: sudo pacman -S iptables```\n\n```apk: sudo apk add iptables```\n\n```zypper: sudo zypper install iptables```\n\n```brew: brew install iptables```\n\n```nix: nix profile install nixpkgs#iptables```"],["SEE ALSO","[iptables-restore](/man/iptables-restore)(8), [ip6tables](/man/ip6tables)(8), [ip6tables-save](/man/ip6tables-save)(8), [iptables](/man/iptables)(8)"]],"e":["iptables-restore","ip6tables","ip6tables-save","iptables"],"x":"tldr iptables-restore"},"jo":{"n":"jo","t":"creates JSON from shell arguments","s":[["TAGLINE","creates JSON from shell arguments"],["TLDR","**Create JSON object**\n\n```jo name=[value] count:=[42]```\n\n**Create JSON array**\n\n```jo -a [item1] [item2] [item3]```\n\n**Nested objects**\n\n```jo name=[test] config=$(jo debug:=[true])```\n\n**Read from stdin**\n\n```echo \"[value]\" | jo name=@-```\n\n**Read from file**\n\n```jo content=@[file.txt]```\n\n**Pretty print**\n\n```jo -p name=[value]```"],["SYNOPSIS","**jo** [_options_] [_key=value_...]"],["PARAMETERS","_KEY=VALUE_\n> String value assignment.\n\n_KEY:=VALUE_\n> Non-string value (number, boolean, null).\n\n**-a**\n> Create array instead of object.\n\n**-p**\n> Pretty print output.\n\n**@**_FILE_\n> Read value from file.\n\n**@-**\n> Read value from stdin.\n\n**--help**\n> Display help information."],["DESCRIPTION","**jo** creates JSON from shell arguments. It builds objects and arrays from command-line key-value pairs.\n\nThe tool enables JSON generation in shell scripts. Type hints distinguish strings from numbers and booleans."],["CAVEATS","Shell quoting important. Use := for non-strings. Nested structures via subshell."],["HISTORY","jo was created by **Jan-Piet Mens** to simplify JSON creation in shell scripts without complex quoting."],["INSTALL","```apt: sudo apt install jo```\n\n```dnf: sudo dnf install jo```\n\n```pacman: sudo pacman -S jo```\n\n```apk: sudo apk add jo```\n\n```zypper: sudo zypper install jo```\n\n```brew: brew install jo```\n\n```nix: nix profile install nixpkgs#jo```"],["SEE ALSO","[jq](/man/jq)(1), [jc](/man/jc)(1), [gron](/man/gron)(1)"]],"e":["jq","jc","gron"],"x":"jo name=[value] count:=[42]"},"kdiff3":{"n":"kdiff3","t":"file and directory comparison and merge tool","s":[["TAGLINE","file and directory comparison and merge tool"],["TLDR","**Compare two files**\n\n```kdiff3 [file1] [file2]```\n\n**Three-way comparison**\n\n```kdiff3 [base] [file1] [file2]```\n\n**Compare directories**\n\n```kdiff3 [dir1] [dir2]```\n\n**Merge with output**\n\n```kdiff3 [base] [file1] [file2] -o [merged]```\n\n**Auto-select non-conflicting**\n\n```kdiff3 [base] [file1] [file2] --auto```"],["SYNOPSIS","**kdiff3** [_options_] [_files/directories_...]"],["PARAMETERS","**-o** _file_\n> Output merged file.\n\n**--auto**\n> Automatically select non-conflicting changes.\n\n**-m**, **--merge**\n> Merge mode.\n\n**--cs** _setting=value_\n> Configuration setting.\n\n**--qall**\n> Quiet (auto-mode only).\n\n**-L1**, **-L2**, **-L3** _label_\n> Labels for inputs."],["DESCRIPTION","**KDiff3** is a file and directory comparison and merge tool. It supports two-way and three-way comparisons, making it suitable for resolving merge conflicts and reviewing changes.\n\nKDiff3 highlights differences with colors and allows selecting which version to use for each conflict. It integrates with version control systems as a merge tool."],["GIT INTEGRATION","```bash"],["Configure as merge tool","git config --global merge.tool kdiff3\ngit config --global mergetool.kdiff3.path /usr/bin/kdiff3"],["Configure as diff tool","git config --global diff.tool kdiff3\n```"],["CAVEATS","Requires KDE libraries. Three-way merge needs common ancestor. Large files may be slow. Binary files not compared."],["HISTORY","KDiff3 was created by **Joachim Eibl** as a Qt/KDE application for comparing and merging files. It became a popular choice for resolving version control conflicts."],["INSTALL","```apt: sudo apt install kdiff3```\n\n```dnf: sudo dnf install kdiff3```\n\n```pacman: sudo pacman -S kdiff3```\n\n```apk: sudo apk add kdiff3```\n\n```zypper: sudo zypper install kdiff3```\n\n```nix: nix profile install nixpkgs#kdiff3```"],["SEE ALSO","[diff](/man/diff)(1), [meld](/man/meld)(1), [vimdiff](/man/vimdiff)(1), [git-mergetool](/man/git-mergetool)(1)"]],"e":["diff","meld","vimdiff","git-mergetool"],"x":"kdiff3 [file1] [file2]"},"licensecheck":{"n":"licensecheck","t":"scans files for license information and copyright statements","s":[["TAGLINE","scans files for license information and copyright statements"],["TLDR","**Check files for licenses**\n\n```licensecheck [file]```\n\n**Check directory recursively**\n\n```licensecheck -r [directory]```\n\n**Output in machine-readable format**\n\n```licensecheck --copyright -m [file]```\n\n**Check specific file types**\n\n```licensecheck -r --check '*.py' [directory]```\n\n**Show only files with licenses**\n\n```licensecheck -r -l [directory]```"],["SYNOPSIS","**licensecheck** [_options_] _files_..."],["PARAMETERS","**-r**, **--recursive**\n> Check directories recursively.\n\n**-l**, **--lines** _n_\n> Check first n lines (default 60).\n\n**-c**, **--check** _regex_\n> Check only matching filenames.\n\n**-i**, **--ignore** _regex_\n> Ignore matching filenames.\n\n**-m**, **--machine**\n> Machine-readable output.\n\n**--copyright**\n> Show copyright statements.\n\n**-v**, **--verbose**\n> Verbose output."],["DESCRIPTION","**licensecheck** scans files for license information and copyright statements. It examines source code headers to identify the license under which files are distributed.\n\nThe tool recognizes many common open-source licenses including GPL, MIT, BSD, Apache, and others."],["RECOGNIZED LICENSES","```\nGPL, LGPL, AGPL (various versions)\nMIT, BSD (2/3-clause)\nApache, MPL\nArtistic, Perl\nPublic Domain\n```"],["CAVEATS","Heuristic-based detection may miss or misidentify licenses. Custom licenses need manual review. Only checks file headers."],["HISTORY","licensecheck originated in Debian as a tool for checking license compliance in package source code, helping maintainers verify licensing."],["INSTALL","```dnf: sudo dnf install licensecheck```"],["SEE ALSO","[license](/man/license)(1), [reuse](/man/reuse)(1)"]],"e":["license","reuse"],"x":"licensecheck [file]"},"logmerger":{"n":"logmerger","t":"View multiple log files with merged timeline","s":[["TAGLINE","View multiple log files with merged timeline"],["TLDR","**Merge and view log files** interactively\n\n```logmerger [file1.log] [file2.log]```\n\n**Merge logs inline** to stdout\n\n```logmerger --inline [file1.log] [file2.log]```\n\n**Merge logs within a time window**\n\n```logmerger --start \"[timestamp]\" --end \"[timestamp]\" [file1.log] [file2.log]```\n\n**Write merged output** to a file\n\n```logmerger -o [output.log] [file1.log] [file2.log]```\n\n**Add line numbers** in the interactive browser\n\n```logmerger --line_numbers [file1.log] [file2.log]```"],["SYNOPSIS","**logmerger** [_options_] _files_..."],["PARAMETERS","_FILES_\n> One or more log files to merge. Files ending in **.gz**, **.csv**, **.jsonl**, or **.pcap** (experimental) are read accordingly.\n\n**-i**, **--interactive**\n> Show the merged output in the interactive TUI browser. This is the default.\n\n**--inline**\n> Display the merged log data as an inline merge to stdout instead of opening the TUI.\n\n**-o**, **--output** _FILE_\n> Save the merged output to a file. Use **-** for stdout; files ending in **.md** are written as a Markdown table.\n\n**-s**, **--start** _TIME_\n> Start of the time window to select for merging.\n\n**-e**, **--end** _TIME_\n> End of the time window to select for merging.\n\n**-ac**, **--autoclip**\n> Clip merging to the time range of the logs in the first log file.\n\n**--ignore_non_timestamped**\n> Ignore log lines that do not have a timestamp instead of attaching them to the previous entry.\n\n**-w**, **--width** _WIDTH_\n> Total screen width to use for interactive mode.\n\n**-ln**, **--line_numbers**\n> Add a line-number column to the output.\n\n**-enc**, **--encoding** _ENCODING_\n> Encoding to use when reading the log files.\n\n**--timestamp_format** _FORMAT_\n> One or more custom strptime-style timestamp formats to recognize."],["DESCRIPTION","**logmerger** is a TUI utility for viewing a merged display of multiple log files, correlated by timestamp. It reads plain text logs, gzip-compressed logs (**.gz**), CSV (**.csv**) and JSONL (**.jsonl**) files, and has experimental support for packet capture (**.pcap**) files.\n\nLog entries from the various files are sorted by their timestamps and presented in a unified, color-coded display where each file gets its own column for easy identification. Many common timestamp formats are detected automatically; additional formats can be supplied with **--timestamp_format**."],["CAVEATS","Relies on recognizable timestamps in each log file for accurate merging. Lines without a timestamp are attached to the preceding entry unless **--ignore_non_timestamped** is given. pcap support requires the optional dependency, installed with `pip install logmerger[pcap]`."],["HISTORY","**logmerger** is an open-source tool written in **Python** by **Paul McGuire** (ptmcg) and distributed on PyPI as the **logmerger** package (`pip install logmerger`)."],["SEE ALSO","[lnav](/man/lnav)(1), [tail](/man/tail)(1), [sort](/man/sort)(1)"]],"e":["lnav","tail","sort"],"x":"logmerger [file1.log] [file2.log]"},"manpath":{"n":"manpath","t":"determines and displays the search path used to find manual pages","s":[["TAGLINE","determines and displays the search path used to find manual pages"],["TLDR","Display **search path**\n\n```manpath```\n\nShow **global** manpath\n\n```manpath --global```"],["SYNOPSIS","**manpath** [_options_]"],["DESCRIPTION","**manpath** determines and displays the search path used to find manual pages. It examines configuration files and environment variables to construct the path."],["PARAMETERS","**-g, --global**\n> Show entire global manpath\n\n**-d, --debug**\n> Print debugging information\n\n**-q, --quiet**\n> Suppress warnings\n\n**-c, --catpath**\n> Show catpath instead of manpath\n\n**-V, --version**\n> Display version information\n\n**-h, --help**\n> Display help"],["ENVIRONMENT","**MANPATH**: Override default search path\n**PATH**: Used to derive manpath if MANPATH unset"],["CAVEATS","If **MANPATH** is set, **manpath** generally returns its value unaltered (or augmented per the configuration files). Otherwise it derives the path from **/etc/manpath.config** and **PATH**."],["INSTALL","```apt: sudo apt install man-db```\n\n```dnf: sudo dnf install man-db```\n\n```pacman: sudo pacman -S man-db```\n\n```apk: sudo apk add man-db```\n\n```brew: brew install man-db```\n\n```nix: nix profile install nixpkgs#man-db```"],["SEE ALSO","[man](/man/man)(1), [mandb](/man/mandb)(8)"]],"e":["man","mandb"],"x":"manpath"},"memweave":{"n":"memweave","t":"Persistent agent memory stored as Markdown files indexed in SQLite","s":[["TAGLINE","Persistent agent memory stored as Markdown files indexed in SQLite"],["TLDR","**Index** the current workspace, embedding any Markdown files that changed\n\n```memweave index --workspace [.]```\n\n**Force** a full re-embed of every file\n\n```memweave index --workspace [.] --force```\n\n**Index** a single new memory file\n\n```memweave add [memory/2026-04-26.md] --workspace [.]```\n\n**Search** the index with a hybrid keyword + vector query\n\n```memweave search \"[PostgreSQL JSONB]\" --workspace [.]```\n\n**Limit** the number of results and require a minimum score\n\n```memweave search \"[caching layer]\" --max-results [3] --min-score [0.3]```\n\n**Run a keyword-only** search (no embedding cost)\n\n```memweave search \"[Redis]\" --strategy [keyword]```\n\n**Show** machine-readable JSON output\n\n```memweave search \"[topic]\" --json```\n\n**Display** index statistics\n\n```memweave stats --workspace [.]```\n\n**List** all tracked files\n\n```memweave files --workspace [.]```"],["SYNOPSIS","**memweave** _command_ [_options_] [_args_]"],["PARAMETERS","**index**\n> Walk the workspace and embed any Markdown files whose SHA-256 has changed since the last run.\n\n**add** _file_\n> Index a single Markdown file immediately.\n\n**search** _query_\n> Run a hybrid (BM25 keyword + vector) search across the index.\n\n**files**\n> List every tracked file with source labels and chunk counts.\n\n**stats**\n> Print a summary of index state, search mode, cache usage, and staleness warnings.\n\n**-w**, **--workspace** _PATH_\n> Workspace directory to operate on (default **$PWD**).\n\n**--embedding-model** _NAME_\n> Override the embedding model (e.g. **text-embedding-3-small**).\n\n**--force**\n> Skip change detection and reprocess every file.\n\n**--max-results** _N_\n> Maximum number of search hits to return.\n\n**--min-score** _F_\n> Drop hits below a relevance score.\n\n**--source-filter** _NAME_\n> Restrict results to a labeled source (e.g. **sessions**).\n\n**--strategy** _NAME_\n> Search strategy: **hybrid** (default), **keyword**, or **semantic**.\n\n**--mmr-lambda** _F_\n> Maximal Marginal Relevance trade-off between relevance and diversity.\n\n**--decay-half-life-days** _N_\n> Apply temporal decay so older notes rank lower over time.\n\n**--json**\n> Emit JSON output suitable for piping into other tools."],["DESCRIPTION","**memweave** is a Python library and CLI that gives AI agents a persistent, searchable memory whose primary storage format is plain Markdown. Every memory file is hashed, chunked, and embedded into a single local **SQLite** database that combines **FTS5** keyword ranking with **sqlite-vec** vector search, so retrieval works offline and merges keyword and semantic signals in one ranked list.\n\nEach subcommand maps directly onto a method of the underlying **MemWeave** Python class, which makes it natural to compose memweave with shell pipelines, editor hooks, and CI jobs without writing Python. Embeddings are cached by content hash, so re-running **memweave index** is cheap when most files are unchanged, and **memweave search** never invokes an LLM — only the embedding endpoint."],["CONFIGURATION","memweave stores its database alongside the workspace by default. The active embedding model is selected with **--embedding-model** or read from environment variables exposed by the chosen provider (e.g. **OPENAI_API_KEY** for OpenAI embeddings). For the optional file-watcher integration install with the **watch** extra:\n\n```\npip install memweave[watch]\n```"],["CAVEATS","memweave is intentionally zero-infrastructure — there is no server component, sharing model, or access-control layer. The hybrid index assumes Markdown input; binary files are ignored. Because embeddings are cached by content hash, switching embedding models requires **--force** to invalidate prior vectors. There are unrelated projects with similar names; this page documents **sachinsharma9780/memweave**."],["HISTORY","**memweave** was created by **Sachin Sharma** (**sachinsharma9780**) and published on **PyPI** as **memweave** and on GitHub at **github.com/sachinsharma9780/memweave**. It is positioned as a lightweight alternative to vector-database stacks for agentic workflows that already use Markdown as a writing format."],["SEE ALSO","[sqlite3](/man/sqlite3)(1), [pip](/man/pip)(1), [grep](/man/grep)(1), [ripgrep](/man/ripgrep)(1)"]],"e":["sqlite3","pip","grep","ripgrep"],"x":"memweave index --workspace [.]"},"mullvad":{"n":"mullvad","t":"command-line interface for Mullvad VPN, a privacy-focused VPN service","s":[["TAGLINE","command-line interface for Mullvad VPN, a privacy-focused VPN service"],["TLDR","**Connect to VPN**\n\n```mullvad connect```\n\n**Disconnect from VPN**\n\n```mullvad disconnect```\n\n**Check connection status**\n\n```mullvad status```\n\n**Set VPN server location**\n\n```mullvad relay set location [us] [nyc]```\n\n**List available locations**\n\n```mullvad relay list```\n\n**Enable auto-connect on boot**\n\n```mullvad auto-connect set on```\n\n**Enable kill switch**\n\n```mullvad lockdown-mode set on```\n\n**Check account status**\n\n```mullvad account get```"],["SYNOPSIS","**mullvad** _command_ [_subcommand_] [_options_]"],["PARAMETERS","**connect**\n> Establish VPN connection.\n\n**disconnect**\n> Close VPN connection.\n\n**reconnect**\n> Reconnect to VPN.\n\n**status**\n> Show current connection status.\n\n**account** _subcommand_\n> Manage account (login, logout, get, set).\n\n**relay** _subcommand_\n> Configure relay/server selection.\n\n**tunnel** _subcommand_\n> Configure tunnel protocol options.\n\n**dns** _subcommand_\n> Configure DNS settings.\n\n**lockdown-mode** _subcommand_\n> Configure kill switch (block non-VPN traffic).\n\n**auto-connect** _subcommand_\n> Configure auto-connect on startup.\n\n**lan** _subcommand_\n> Allow/block local network access.\n\n**obfuscation** _subcommand_\n> Configure traffic obfuscation.\n\n**split-tunnel** _subcommand_\n> Exclude apps from VPN tunnel.\n\n**bridge** _subcommand_\n> Configure bridge mode for censored networks.\n\n**version**\n> Show version information."],["DESCRIPTION","**mullvad** is the command-line interface for Mullvad VPN, a privacy-focused VPN service based in Sweden. It controls the Mullvad daemon to manage VPN connections and configuration.\n\nThe client supports WireGuard and OpenVPN protocols. WireGuard is default and recommended for performance. Server selection can be automatic or manually set by country, city, or specific server. The relay list shows all available endpoints.\n\nLockdown mode (kill switch) blocks all internet traffic when VPN is disconnected, preventing leaks. LAN access can be allowed for local network resources. Split tunneling excludes specific applications from the VPN tunnel.\n\nDNS options include using Mullvad's DNS, custom servers, or blocking ads/trackers/malware via their DNS. Obfuscation helps bypass VPN blocking in censored networks.\n\nThe account system uses numbered tokens rather than email/password. Anonymous payment options include cash and cryptocurrency."],["CAVEATS","Requires Mullvad subscription. Daemon must be running (mullvad-daemon service). Some features require root. Split tunneling support varies by platform. WireGuard requires kernel module on Linux. Kill switch may disrupt local services. Account tokens should be kept secure."],["HISTORY","**Mullvad** was founded in **2009** in Sweden by Amagicom AB, focusing on privacy from the start. The CLI tool evolved alongside their desktop clients. Mullvad gained attention for pioneering WireGuard VPN support and their anonymous account system. In **2022**, the Swedish police attempted to seize their servers but found no user data due to their no-logging policy. The service has been independently audited multiple times."],["INSTALL","```nix: nix profile install nixpkgs#mullvad```"],["SEE ALSO","[wg](/man/wg)(1), [openvpn](/man/openvpn)(1), [nordvpn](/man/nordvpn)(1)"]],"e":["wg","openvpn","nordvpn"],"x":"mullvad connect"},"ned":{"n":"ned","t":"text editor","s":[["TAGLINE","text editor"],["TLDR","**Edit file**\n\n```ned [file.txt]```\n\n**Edit multiple files**\n\n```ned [file1.txt] [file2.txt]```\n\n**Read-only mode**\n\n```ned -R [file.txt]```"],["SYNOPSIS","**ned** [_options_] [_files_]"],["PARAMETERS","_FILES_\n> Files to edit.\n\n**-R**\n> Read-only mode.\n\n**--help**\n> Display help information."],["DESCRIPTION","**ned** is a text editor. It provides simple text editing functionality.\n\nThe tool is a minimal editor. Basic features for quick edits."],["CAVEATS","Minimal feature set. Less common than nano/vim. Check if installed."],["HISTORY","ned is one of several **simple text editors** available for command-line text editing."],["INSTALL","```aur: yay -S ned```"],["SEE ALSO","[nano](/man/nano)(1), [vim](/man/vim)(1), [ed](/man/ed)(1)"]],"e":["nano","vim","ed"],"x":"ned [file.txt]"},"nix-channel":{"n":"nix-channel","t":"manages Nix channel subscriptions","s":[["TAGLINE","manages Nix channel subscriptions"],["TLDR","**List subscribed channels**\n\n```nix-channel --list```\n\n**Add a channel**\n\n```nix-channel --add [https://nixos.org/channels/nixpkgs-unstable] [nixpkgs]```\n\n**Update all channels**\n\n```nix-channel --update```\n\n**Update a specific channel**\n\n```nix-channel --update [nixpkgs]```\n\n**Remove a channel**\n\n```nix-channel --remove [nixpkgs]```\n\n**Rollback to a previous channel generation**\n\n```nix-channel --rollback```\n\n**List channel generations**\n\n```nix-channel --list-generations```"],["SYNOPSIS","**nix-channel** [_options_]"],["PARAMETERS","**--list**\n> Print names and URLs of all subscribed channels.\n\n**--add** _URL_ [_NAME_]\n> Subscribe to a channel. If _NAME_ is omitted, defaults to the last component of the URL with -stable or -unstable suffixes removed.\n\n**--update** [_NAMES..._]\n> Download Nix expressions of subscribed channels and make them the default for nix-env operations. Optionally update only the named channels.\n\n**--remove** _NAME_\n> Unsubscribe from a channel.\n\n**--rollback** [_GENERATION_]\n> Revert the previous call to `--update`. Optionally specify a generation number.\n\n**--list-generations**\n> Show channel generations.\n\n**--help**\n> Display help information.\n\n**--version**\n> Display version information."],["DESCRIPTION","**nix-channel** manages Nix channel subscriptions. Channels are URLs that point to a Nix expression tarball providing a set of packages and NixOS modules.\n\nThe list of subscribed channels is stored in `~/.nix-channels`. After updating, channel expressions are symlinked from `~/.nix-defexpr/` and become available to `nix-env` and other Nix tools. Nix flakes are the modern alternative to channels."],["CAVEATS","Channels are a legacy approach; Nix flakes are recommended for new projects. Channels are per-user unless configured system-wide on NixOS. Running `--update` requires network access to download channel expressions."],["HISTORY","nix-channel has been part of the **Nix** package manager since its early releases by Eelco Dolstra. It is the traditional mechanism for subscribing to package repositories, predating the flakes system introduced experimentally in Nix 2.4."],["INSTALL","```apt: sudo apt install nix-bin```\n\n```dnf: sudo dnf install nix```\n\n```pacman: sudo pacman -S nix```\n\n```apk: sudo apk add nix```\n\n```zypper: sudo zypper install nix```\n\n```nix: nix profile install nixpkgs#nix```"],["SEE ALSO","[nix](/man/nix)(1), [nix-env](/man/nix-env)(1), [nix-build](/man/nix-build)(1), [nix-shell](/man/nix-shell)(1), [nixos-rebuild](/man/nixos-rebuild)(1)"]],"e":["nix","nix-env","nix-build","nix-shell","nixos-rebuild"],"x":"nix-channel --list"},"npm-ping":{"n":"npm-ping","t":"checks connectivity to npm registry","s":[["TAGLINE","checks connectivity to npm registry"],["TLDR","**Ping default npm registry**\n\n```npm ping```\n\n**Ping custom registry**\n\n```npm ping --registry [https://registry.example.com]```"],["SYNOPSIS","**npm** **ping** [_options_]"],["PARAMETERS","**--registry** _url_\n> Registry URL to ping (default: https://registry.npmjs.org)."],["DESCRIPTION","**npm ping** pings the configured or specified npm registry and verifies authentication if credentials are configured. It outputs the registry URL and response time on success, or an error on failure. Useful for diagnosing registry connectivity issues, verifying custom registry configurations, and troubleshooting npm authentication."],["CAVEATS","Requires network connectivity. If authentication is configured, the ping also verifies token validity."],["INSTALL","```pacman: sudo pacman -S npm```\n\n```apk: sudo apk add npm```\n\n```brew: brew install npm```"],["SEE ALSO","[npm](/man/npm)(1), [npm-config](/man/npm-config)(1)"]],"e":["npm","npm-config"],"x":"npm ping"},"pct-push":{"n":"pct-push","t":"copy a local file into a Proxmox LXC container","s":[["TAGLINE","copy a local file into a Proxmox LXC container"],["TLDR","**Push** a local script into a container\n\n```pct push 100 ./bootstrap.sh /root/bootstrap.sh```\n\nPush a config and set **guest ownership**\n\n```pct push 100 ./nginx.conf /etc/nginx/nginx.conf --user root --group root```\n\nPush with **executable permissions**\n\n```pct push 100 ./setup.sh /usr/local/bin/setup.sh --perms 0755```"],["SYNOPSIS","**pct push** _vmid_ _file_ _destination_ [_OPTIONS_]"],["PARAMETERS","**vmid**\n> The numeric ID of the container (100–999999999)\n\n**file**\n> Path to a local file on the host\n\n**destination**\n> Destination path inside the container to write to\n\n**--user** _name|id_\n> Owner user name or id. When using a name, it must exist inside the container\n\n**--group** _name|id_\n> Owner group name or id. When using a name, it must exist inside the container\n\n**--perms** _mode_\n> File permissions (octal by default; prefix with **0x** for hexadecimal)"],["DESCRIPTION","**pct push** copies a single local file from the Proxmox host into an LXC container. It is commonly used to install bootstrap scripts, drop configuration files, or stage packages without **scp**/SSH into the guest.\n\nAfter a push, **pct exec** is often used to chmod and run the file. Ownership options refer to users and groups **inside** the container when names (not numeric ids) are given."],["CAVEATS","Single-file only; recursive directory push is not supported. Parent directories inside the container must already exist. For large tree copies, mount the filesystem with **pct mount** or use a network share instead. Name-based **--user**/**--group** fail if that account is not defined in the guest."],["HISTORY","**pct push** is part of the **Proxmox VE** **pct** (Proxmox Container Toolkit) for managing LXC containers."],["SEE ALSO","[pct](/man/pct)(1), [pct-pull](/man/pct-pull)(1), [pct-exec](/man/pct-exec)(1), [pct-enter](/man/pct-enter)(1), [pct-mount](/man/pct-mount)(1)"],["RESOURCES","```[Documentation](https://pve.proxmox.com/pve-docs/pct.1.html)```\n\n```[Homepage](https://www.proxmox.com)```"]],"e":["pct","pct-pull","pct-exec","pct-enter","pct-mount"],"x":"pct push 100 ./bootstrap.sh /root/bootstrap.sh"},"pg_archivecleanup":{"n":"pg_archivecleanup","t":"removes old WAL archive files no longer needed by standby servers","s":[["TAGLINE","removes old WAL archive files no longer needed by standby servers"],["TLDR","**Clean old WAL files**\n\n```pg_archivecleanup [archive_dir] [oldest_kept_wal]```\n\n**Dry run**\n\n```pg_archivecleanup -n [archive_dir] [oldest_kept_wal]```\n\n**Clean with extension**\n\n```pg_archivecleanup -x .gz [archive_dir] [oldest_kept_wal]```"],["SYNOPSIS","**pg_archivecleanup** [_options_] _archivelocation_ _oldestkeptwalfile_"],["PARAMETERS","**-b**, **--clean-backup-history**\n> Remove backup history files as well.\n\n**-d**, **--debug**\n> Print debug logging output on stderr.\n\n**-n**, **--dry-run**\n> Print the names of files that would be removed (dry run).\n\n**-V**, **--version**\n> Print version and exit.\n\n**-x**, **--strip-extension** _extension_\n> Strip this extension from filenames before deciding if they should be deleted. Useful for compressed archives (e.g., .gz, .bz2).\n\n**-?**, **--help**\n> Show help and exit."],["DESCRIPTION","**pg_archivecleanup** removes old WAL archive files no longer needed by standby servers. It is typically used as **archive_cleanup_command** in **postgresql.conf** (or recovery.conf in older versions). It removes all WAL files older than the specified _oldestkeptwalfile_."],["INSTALL","```dnf: sudo dnf install postgresql17```\n\n```apk: sudo apk add postgresql17```\n\n```zypper: sudo zypper install postgresql17```"],["SEE ALSO","[pg_basebackup](/man/pg_basebackup)(1), [pg_receivewal](/man/pg_receivewal)(1)"]],"e":["pg_basebackup","pg_receivewal"],"x":"pg_archivecleanup [archive_dir] [oldest_kept_wal]"},"pick":{"n":"pick","t":"Fuzzy interactive selector for stdin lines","s":[["TAGLINE","Fuzzy interactive selector for stdin lines"],["TLDR","**Pick a file** and open it\n\n```find . -type f | pick | xargs xdg-open```\n\n**Re-run a command** from shell history\n\n```eval $(fc -ln 1 | pick)```\n\n**Select one line** from a list\n\n```printf '%s\\n' [a b c] | pick```"],["SYNOPSIS","**pick**"],["DESCRIPTION","**pick** reads newline-separated choices from standard input, presents an interactive fuzzy filter in the terminal, and writes the selected line to standard output. It is designed for shell pipelines and subshells: feed it any list, choose an entry with the keyboard, and pipe the result into another command.\n\nA Vim plugin (**pick.vim**) and packages for many Linux distributions and BSDs are available. Homepage: https://www.basename.se/pick/"],["PARAMETERS","**pick** typically takes no arguments; configuration is via terminal interaction (typing to filter, arrow keys / completion to select, Enter to confirm). See the installed man page **pick(1)** for key bindings and compile-time options."],["CAVEATS","Requires a TTY for interactive use; pure non-interactive pipelines without a terminal will not work. Output is a single selected line (behavior if cancelled depends on version—check exit status in scripts)."],["INSTALL","```apt: sudo apt install pick```\n\n```dnf: sudo dnf install pick```\n\n```apk: sudo apk add mmh```\n\n```brew: brew install pick```\n\n```nix: nix profile install nixpkgs#pick```"],["SEE ALSO","[fzf](/man/fzf)(1), [fzy](/man/fzy)(1), [peco](/man/peco)(1), [selecta](/man/selecta)(1)"],["RESOURCES","```[Source code](https://github.com/mptre/pick)```\n\n```[Homepage](https://www.basename.se/pick/)```"]],"e":["fzf","fzy","peco","selecta"],"x":"find . -type f | pick | xargs xdg-open"},"pnmtile":{"n":"pnmtile","t":"Create tiled patterns from PNM images","s":[["TAGLINE","Create tiled patterns from PNM images"],["TLDR","**Create tiled pattern from image**\n\n```pnmtile [640] [480] [input.pnm] > [output.pnm]```"],["SYNOPSIS","**pnmtile** _width_ _height_ [_file_]"],["PARAMETERS","_width_\n> Output width in pixels.\n\n_height_\n> Output height in pixels."],["DESCRIPTION","**pnmtile** creates an image by tiling (repeating) the input image to fill the specified dimensions. Useful for creating backgrounds. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pnmcat](/man/pnmcat)(1), [pnmmontage](/man/pnmmontage)(1)"]],"e":["pnmcat","pnmmontage"],"x":"pnmtile [640] [480] [input.pnm] > [output.pnm]"},"pushd":{"n":"pushd","t":"Navigate directories using a stack","s":[["TAGLINE","Navigate directories using a stack"],["TLDR","**Push directory to stack**\n\n```pushd [/path/to/dir]```\n\n**Swap top two directories**\n\n```pushd```\n\n**Push without changing directory**\n\n```pushd -n [/path/to/dir]```\n\n**Show directory stack**\n\n```dirs```\n\n**Pop from stack**\n\n```popd```"],["SYNOPSIS","**pushd** [_options_] [_dir_]"],["PARAMETERS","_DIR_\n> Directory to push.\n\n**-n**\n> Add without changing.\n\n**+N**\n> Rotate Nth entry (from left) to top.\n\n**-N**\n> Rotate Nth entry (from right) to top."],["DESCRIPTION","**pushd** is a shell built-in that changes the current directory while saving the previous directory on a stack. This allows quick navigation between multiple working directories without needing to remember or retype full paths.\n\nWhen called with a directory argument, pushd changes to that directory and pushes the old directory onto the stack. Called without arguments, it swaps the top two directories on the stack. The **-n** flag adds a directory to the stack without actually changing to it. Use **dirs** to view the current stack contents, **popd** to return to the previous directory, and **+N** or **-N** notation to rotate to specific stack positions."],["CAVEATS","Shell built-in. Works with popd and dirs."],["HISTORY","**pushd** originated in the **C shell (csh)** and was adopted by **bash**, **zsh**, and other shells as a built-in command for directory stack management."],["SEE ALSO","[popd](/man/popd)(1), [dirs](/man/dirs)(1), [cd](/man/cd)(1)"]],"e":["popd","dirs","cd"],"x":"pushd [/path/to/dir]"},"quarto":{"n":"quarto","t":"Scientific and technical publishing system","s":[["TAGLINE","Scientific and technical publishing system"],["TLDR","**Render document to output format**\n\n```quarto render [document.qmd]```\n\n**Render entire project**\n\n```quarto render```\n\n**Preview document with live reload**\n\n```quarto preview [document.qmd]```\n\n**Create new project**\n\n```quarto create project [website]```\n\n**Publish to Quarto Pub**\n\n```quarto publish quarto-pub```\n\n**Convert Jupyter notebook to Quarto**\n\n```quarto convert [notebook.ipynb]```\n\n**Install extensions**\n\n```quarto add [quarto-ext/fontawesome]```\n\n**Check installation**\n\n```quarto check```"],["SYNOPSIS","**quarto** _command_ [_options_] [_arguments_]"],["PARAMETERS","**render** [_input_]\n> Render document or project.\n\n**preview** [_input_]\n> Live preview with auto-reload.\n\n**create** _type_ [_name_]\n> Create project or extension.\n\n**publish** _provider_\n> Publish to hosting service.\n\n**add** _extension_\n> Install extension.\n\n**remove** _extension_\n> Remove extension.\n\n**update** _extension_\n> Update extension.\n\n**convert** _file_\n> Convert between formats.\n\n**check** [_component_]\n> Verify installation.\n\n**install** _tool_\n> Install optional tools.\n\n**--to** _FORMAT_\n> Output format (html, pdf, docx, etc.).\n\n**--output** _FILE_\n> Output file name.\n\n**--execute**\n> Execute code cells.\n\n**--no-execute**\n> Skip code execution.\n\n**--cache**\n> Cache code execution results.\n\n**--port** _NUM_\n> Preview server port.\n\n**--browser**\n> Open in browser."],["DESCRIPTION","**Quarto** is an open-source publishing system for technical and scientific documents. It extends Pandoc with support for executable code cells in Python, R, Julia, and Observable JS.\n\nDocuments use .qmd (Quarto Markdown) format combining narrative text with code cells. Code executes during rendering, embedding outputs (plots, tables, results) in the final document.\n\nOutput formats include HTML, PDF, Word, presentations (RevealJS, Beamer), websites, and books. Projects organize multiple documents with shared configuration.\n\nThe preview command provides live development with automatic re-rendering on file changes. Hot module replacement updates the browser without full refresh.\n\nExtensions add features: custom formats, shortcodes, filters, and themes. The extension ecosystem provides templates for journals, presentations, and specialized outputs.\n\nPublishing integrates with Quarto Pub, GitHub Pages, Netlify, Posit Connect, and other platforms. Single command deployment updates hosted content."],["CAVEATS","Requires Jupyter or knitr for code execution. Large documents with heavy computation slow rendering. PDF output requires LaTeX installation. Some extensions may break with Quarto updates. Preview server can be resource-intensive."],["HISTORY","**Quarto** was released by **Posit** (formerly RStudio) in **2022** as a next-generation version of R Markdown. It was designed to be language-agnostic, supporting Python and Julia equally with R. The name \"quarto\" refers to a book format and continues the printing/publishing theme from R Markdown."],["INSTALL","```nix: nix profile install nixpkgs#quarto```"],["SEE ALSO","[pandoc](/man/pandoc)(1), [jupyter](/man/jupyter)(1), [rmarkdown](/man/rmarkdown)(1), [marp](/man/marp)(1)"]],"e":["pandoc","jupyter","rmarkdown","marp"],"x":"quarto render [document.qmd]"},"resterm":{"n":"resterm","t":"Terminal API client for HTTP, GraphQL, gRPC, WebSocket, and SSE","s":[["TAGLINE","Terminal API client for HTTP, GraphQL, gRPC, WebSocket, and SSE"],["TLDR","**Launch the API client TUI**\n\n```resterm```\n\n**Open a request file in the TUI**\n\n```resterm --file [path/to/request.http]```\n\n**Run requests directly from the terminal**\n\n```resterm run --file [path/to/request.http]```\n\n**Initialize a new workspace with samples**\n\n```resterm init```\n\n**Run a specific named request in an environment**\n\n```resterm run --file [requests.http] --request [getUser] --env [staging]```\n\n**Import a curl command into an .http file**\n\n```resterm --from-curl \"[curl https://example.com]\"```\n\n**Convert an OpenAPI 3 spec into request files**\n\n```resterm --from-openapi [spec.yaml]```"],["SYNOPSIS","**resterm** [_options_] [_file_]\n\n**resterm** **run** [_options_]\n\n**resterm** **init** [_options_]\n\n**resterm** **collection** _import_|_export_ [_options_]"],["COMMANDS","**run**\n> Execute one or more `.http` / `.rest` requests from the terminal.\n\n**init**\n> Bootstrap a workspace with sample requests and environment files.\n\n**collection export**\n> Export a workspace into a Git-friendly bundle.\n\n**collection import**\n> Import a bundle into another workspace."],["PARAMETERS","**--file** _PATH_\n> Target a specific request file.\n\n**--workspace** _DIR_\n> Specify the workspace directory.\n\n**--env** _NAME_\n> Select an environment from the workspace.\n\n**--env-file** _PATH_\n> Use a dotenv file (opt-in).\n\n**--request** _NAME_\n> Run a specific named request.\n\n**--timeout** _DURATION_\n> Set the request timeout.\n\n**--insecure**\n> Skip TLS certificate verification.\n\n**--follow**\n> Follow redirects.\n\n**--proxy** _URL_\n> Use the specified HTTP/HTTPS proxy.\n\n**--recursive**\n> Process files recursively.\n\n**--compare**\n> Run a request across multiple environments and diff responses.\n\n**--dry-run**\n> Preview actions before executing.\n\n**--force**\n> Replace existing files during import.\n\n**--from-curl** _CMD_\n> Import a curl command as a request.\n\n**--from-openapi** _SPEC_\n> Convert an OpenAPI 3 specification into request files.\n\n**--http-out** _FORMAT_\n> Specify output format for responses.\n\n**--check-update**\n> Check for newer releases.\n\n**--update**\n> Download and install the latest version."],["DESCRIPTION","**resterm** is a keyboard-driven terminal API client supporting HTTP, GraphQL, gRPC, WebSocket, and SSE. Requests are stored in plain `.http` / `.rest` files so they can be version-controlled alongside code.\n\nFeatures include OAuth 2.0 authentication, SSH tunnels, Kubernetes port-forwarding, conditional logic and multi-step workflows, response diffing, profiling, and streaming transcripts for WebSocket and SSE sessions."],["HISTORY","**resterm** was created by **unkn0wn-root** and is written in **Go**."],["INSTALL","```brew: brew install resterm```\n\n```nix: nix profile install nixpkgs#resterm```"],["SEE ALSO","[curl](/man/curl)(1), [posting](/man/posting)(1), [httpie](/man/httpie)(1)"]],"e":["curl","posting","httpie"],"x":"resterm"},"rr":{"n":"rr","t":"Record and replay program execution for debugging","s":[["TAGLINE","Record and replay program execution for debugging"],["TLDR","**Record program execution**\n\n```rr record [./program]```\n\n**Replay recording**\n\n```rr replay```\n\n**List recordings**\n\n```rr ls```\n\n**Replay specific trace**\n\n```rr replay [trace-directory]```\n\n**Record with chaos mode**\n\n```rr record --chaos [./program]```\n\n**Pack trace for sharing**\n\n```rr pack [trace-directory]```"],["SYNOPSIS","**rr** _command_ [_options_] [_program_] [_args_]"],["PARAMETERS","**record**\n> Record execution.\n\n**replay**\n> Replay recording.\n\n**ls**\n> List traces.\n\n**pack**\n> Pack trace.\n\n**--chaos**\n> Randomize scheduling.\n\n**-n**\n> Number of processes.\n\n**-h**\n> Help for command."],["DESCRIPTION","**rr** is a lightweight recording and deterministic replay tool for debugging on Linux. It records a program's execution — including all system calls, signals, and thread scheduling decisions — so that the exact same execution can be replayed any number of times for debugging, producing identical behavior every run.\n\nDuring replay, rr integrates with GDB to provide a standard debugging interface enhanced with time-travel capabilities. Developers can step backwards through execution, set reverse breakpoints, and navigate to any point in the recorded trace, making it dramatically easier to find the root cause of bugs that are difficult to reproduce.\n\nThe **--chaos** flag randomizes thread scheduling during recording to help expose concurrency bugs and race conditions that might not appear under normal scheduling. Recording imposes low overhead, typically running at near-native speed, making it practical for real-world debugging workflows."],["CAVEATS","Linux x86-64 only. Requires perf_event access. Some syscalls unsupported."],["HISTORY","**rr** was developed by **Mozilla** for debugging Firefox. It provides record-and-replay debugging for Linux."],["INSTALL","```apt: sudo apt install rr```\n\n```dnf: sudo dnf install rr```\n\n```zypper: sudo zypper install rr```\n\n```nix: nix profile install nixpkgs#rr```"],["SEE ALSO","[gdb](/man/gdb)(1), [strace](/man/strace)(1), [perf](/man/perf)(1)"]],"e":["gdb","strace","perf"],"x":"rr record [./program]"},"sbt":{"n":"sbt","t":"Scala build tool for JVM projects","s":[["TAGLINE","Scala build tool for JVM projects"],["TLDR","**Start an interactive sbt shell** in the current project\n\n```sbt```\n\n**Compile the project**\n\n```sbt compile```\n\n**Run the main class** of the project\n\n```sbt run```\n\n**Run all tests**\n\n```sbt test```\n\n**Run a specific test class**\n\n```sbt \"testOnly [com.example.MySpec]\"```\n\n**Continuously compile** on file changes\n\n```sbt ~compile```\n\n**Clean and rebuild** the project\n\n```sbt clean compile```\n\n**Package the project** into a JAR file\n\n```sbt package```"],["SYNOPSIS","**sbt** [_options_] [_command_...]"],["PARAMETERS","**-h**, **--help**\n> Display help information\n\n**-v**, **--verbose**\n> Enable verbose logging\n\n**--debug**\n> Enable debug logging\n\n**-no-colors**\n> Disable ANSI color codes in output\n\n**--batch**\n> Disable interactive mode\n\n**--sbt-version** _version_\n> Use specified sbt version\n\n**--java-home** _path_\n> Specify alternate Java installation directory\n\n**-Dkey=val**\n> Pass system property to the JVM\n\n**-J-Xfoo**\n> Pass JVM option directly (e.g., -J-Xmx2048M)\n\n**-mem** _MB_\n> Set memory allocation (e.g., -mem 2048)"],["DESCRIPTION","**sbt** (Scala Build Tool) is the standard build tool for Scala projects. It handles compilation, testing, dependency management, packaging, and publishing. sbt uses a build definition written in Scala itself, typically in **build.sbt**.\n\nsbt operates in two modes: **batch mode** where commands are passed as arguments and sbt exits after execution, and **interactive mode** where sbt starts a shell for continuous interaction. The interactive shell provides faster feedback since the JVM stays running between commands.\n\nKey features include **incremental compilation** (only recompiling changed files), **continuous execution** using the **~** prefix (e.g., ~test runs tests on every file save), and **cross-building** for multiple Scala versions using **+** prefix."],["COMMON COMMANDS","**compile**\n> Compile main sources\n\n**test**\n> Run all tests\n\n**run**\n> Run the main class\n\n**console**\n> Start Scala REPL with project classpath\n\n**clean**\n> Delete generated files (target directory)\n\n**reload**\n> Reload build definition\n\n**update**\n> Resolve and download dependencies\n\n**package**\n> Create JAR file from compiled classes\n\n**publish**\n> Publish artifacts to configured repository\n\n**publishLocal**\n> Publish artifacts to local Ivy repository"],["CAVEATS","sbt can be memory-intensive; large projects may require increasing heap size via **SBT_OPTS** or **.jvmopts**. The first run downloads dependencies and may be slow. Build definitions have their own learning curve with settings, tasks, and scopes."],["HISTORY","sbt was created by **Mark Harrah** in **2008** and has become the de facto build tool for Scala projects. It was originally called \"Simple Build Tool\" but the name was later de-emphasized. Version 1.0 was released in **2017**, bringing significant improvements to build semantics and performance. The project is now maintained by the **Scala Center** and the open-source community, following the transfer from Lightbend in 2023."],["INSTALL","```pacman: sudo pacman -S sbt```\n\n```brew: brew install sbt```\n\n```nix: nix profile install nixpkgs#sbt```"],["SEE ALSO","[scala](/man/scala)(1), [scalac](/man/scalac)(1), [gradle](/man/gradle)(1)"]],"e":["scala","scalac","gradle"],"x":"sbt"},"searchsploit":{"n":"searchsploit","t":"search Exploit Database archive locally","s":[["TAGLINE","search Exploit Database archive locally"],["TLDR","**Search for exploits by keywords**\n\n```searchsploit [apache] [struts]```\n\n**Search in titles only**\n\n```searchsploit -t [oracle] [windows]```\n\n**Perform exact match search**\n\n```searchsploit -e \"[Microsoft IIS 6.0]\"```\n\n**Search by CVE ID**\n\n```searchsploit --cve [2021-44228]```\n\n**Copy exploit to current directory**\n\n```searchsploit -m [39446]```\n\n**Examine/display exploit content**\n\n```searchsploit -x [39446]```\n\n**Show the full path to an exploit and copy to clipboard**\n\n```searchsploit -p [39446]```\n\n**Output results as JSON**\n\n```searchsploit -j [linux kernel] [3.2]```\n\n**Show Exploit-DB.com URLs instead of local paths**\n\n```searchsploit -w [apache] [remote]```\n\n**Exclude terms from results**\n\n```searchsploit [linux kernel] --exclude=\"(PoC)|/dos/\"```\n\n**Search using Nmap XML output**\n\n```searchsploit --nmap [scan_results.xml]```\n\n**Update the database**\n\n```searchsploit -u```"],["SYNOPSIS","**searchsploit** [_options_] _term1_ [_term2_...] [_termN_]\n\n**searchsploit** --cve _CVE-ID_\n\n**searchsploit** -m _EDB-ID_"],["PARAMETERS","**-c**, **--case**\n> Case-sensitive search\n\n**-e**, **--exact**\n> Exact match search\n\n**-s**, **--strict**\n> Strict search; disable fuzzy version matching\n\n**-t**, **--title**\n> Search exploit titles only (default: title and path)\n\n**--exclude** _TERM_\n> Exclude results containing term\n\n**-p**, **--path**\n> Show full path to exploit file\n\n**-m**, **--mirror** _ID_\n> Copy exploit to current directory\n\n**-x**, **--examine** _ID_\n> Display exploit content\n\n**-j**, **--json**\n> Output in JSON format\n\n**-w**, **--www**\n> Show Exploit-DB.com URLs instead of local paths\n\n**--cve** _ID_\n> Search by CVE identifier\n\n**--id**\n> Display EDB-ID rather than local path\n\n**--nmap** _FILE_\n> Search using Nmap XML service version output\n\n**-o**, **--overflow**\n> Allow title overflow in columns\n\n**-v**, **--verbose**\n> Display extended information\n\n**--colour**\n> Disable color highlighting in search results\n\n**-u**, **--update**\n> Update local database\n\n**-h**, **--help**\n> Display help"],["DESCRIPTION","**searchsploit** is a command-line tool for searching the Exploit Database (exploit-db.com) archive locally. It provides offline access to thousands of public exploits, shellcodes, papers, and vulnerability information maintained by Offensive Security.\n\nThe tool searches both exploit titles and file paths by default. Multiple search terms are combined with AND logic—all terms must match. Use quotes for exact phrases and exclude patterns with **--exclude**. Results show the exploit title and local path.\n\nOnce an exploit is found, use **-m** to copy it to your current directory or **-x** to view its content. The **--cve** option searches by CVE identifier for known vulnerabilities. The database requires periodic updates with **-u**."],["CAVEATS","Exploits are provided for authorized security testing only. Many exploits require modification for specific targets. Code should be reviewed and understood before use. Some exploits may be outdated or non-functional. The local database must be updated regularly to include recent exploits."],["HISTORY","SearchSploit is part of the **Exploit Database** project maintained by **Offensive Security**, the creators of Kali Linux. The Exploit Database was launched in **2004** as a public archive of exploits for penetration testers and security researchers. SearchSploit provides offline access to this database, essential for testing in air-gapped environments. The database includes exploits from various sources and continues to grow with community contributions."],["SEE ALSO","[msfconsole](/man/msfconsole)(1), [nmap](/man/nmap)(1), [nikto](/man/nikto)(1)"]],"e":["msfconsole","nmap","nikto"],"x":"searchsploit [apache] [struts]"},"slopo":{"n":"slopo","t":"Embedding-based code duplication detector","s":[["TAGLINE","Embedding-based code duplication detector"],["TLDR","**Initialize a configuration file**\n\n```slopo init```\n\n**Index source code for analysis**\n\n```slopo index```\n\n**Calculate embeddings for indexed code**\n\n```slopo embed```\n\n**Generate a duplication report**\n\n```slopo analyze```\n\n**Validate configuration and show parameters**\n\n```slopo show-config```"],["SYNOPSIS","**slopo** _command_ [_options_]"],["PARAMETERS","**init**\n> Create a configuration file template.\n\n**index**\n> Parse and index code units from **source_dir**.\n\n**embed**\n> Calculate embeddings for indexed code via a configured provider.\n\n**analyze**\n> Find similar code clusters and write a report to **report_dir**.\n\n**show-config**\n> Validate config and display all parameters.\n\n**--config** _PATH_\n> Override the default configuration file location."],["DESCRIPTION","**slopo** is a CLI tool that detects non-exact code duplication using embedding models. Unlike copy-paste scanners, it finds semantically similar snippets spread across modules or distant locations in large files — the kind of duplication hardest for humans and AI agents to spot.\n\nThe workflow is **init** → **index** → **embed** → **analyze**. Indexing parses supported languages into AST-based code units stored in a local SQLite database (**slopo.db**). Embedding calls go through LiteLLM-compatible providers (for example Voyage AI). Analysis compares cosine similarity, groups pairs into clusters, and reranks by distance in the codebase.\n\nSupported languages include Python, TypeScript, JavaScript, Java, Kotlin, C#, Go, and Rust. Reviewed clusters can be added to **slopo.ignore.txt** and excluded from future reports."],["CONFIGURATION","Key settings in the config file:\n\n> **source_dir** — directory to index (required)\n> **source_dir_exclude** — .gitignore-style exclude patterns\n> **db_file** — SQLite database path (default: slopo.db)\n> **report_dir** — output directory for analysis reports\n> **ignore_file** — text file listing reviewed cluster hashes to skip\n> **embedding_model** — model name in LiteLLM format\n> **embedding_dimensions** — vector dimensions for the model\n> **embedding_api_key** — provider key (or set **SLOPO_EMBEDDING_API_KEY**)\n> **similarity_threshold** — minimum cosine similarity between pairs\n> **rerank_threshold** — minimum score after distance-based reranking\n> **body_node_count_threshold** — minimum AST body size for a code unit\n\n**source_dir**, **embedding_model**, **embedding_dimensions**, and **body_node_count_threshold** cannot change after the first index without deleting **slopo.db** and re-indexing."],["CAVEATS","Requires an external embedding API key and network access for the embed step. Exact-copy duplicates are reported but handled differently from merely similar code."],["SEE ALSO","[rg](/man/rg)(1), [ast-grep](/man/ast-grep)(1)"],["RESOURCES","```[Source code](https://github.com/rafal-qa/slopo)```\n\n```[Homepage](https://slopo.dev)```"]],"e":["rg","ast-grep"],"x":"slopo init"},"solana":{"n":"solana","t":"Solana blockchain command-line interface","s":[["TAGLINE","Solana blockchain command-line interface"],["TLDR","**Check wallet balance**\n\n```solana balance```\n\n**Check balance** of a specific address\n\n```solana balance [address]```\n\n**Get current cluster configuration**\n\n```solana config get```\n\n**Set cluster** to devnet, testnet, or mainnet\n\n```solana config set --url [devnet|testnet|mainnet-beta]```\n\n**Create a new keypair**\n\n```solana-keygen new```\n\n**Airdrop SOL** on devnet/testnet\n\n```solana airdrop [amount]```\n\n**Transfer SOL** to another address\n\n```solana transfer [recipient_address] [amount]```\n\n**Deploy a program**\n\n```solana program deploy [path/to/program.so]```"],["SYNOPSIS","**solana** [_flags_] [_options_] _subcommand_"],["PARAMETERS","**-u**, **--url** _URL_\n> URL for Solana's JSON RPC endpoint or cluster moniker (devnet, testnet, mainnet-beta).\n\n**-k**, **--keypair** _KEYPAIR_\n> Filepath or URL to a keypair for signing transactions.\n\n**-C**, **--config** _FILEPATH_\n> Configuration file to use.\n\n**--no-address-labels**\n> Do not use address labels in output.\n\n**--skip-preflight**\n> Skip the preflight transaction simulation.\n\n**--skip-seed-phrase-validation**\n> Skip validation of seed phrases.\n\n**--commitment** _LEVEL_\n> Return information at the selected commitment level (processed, confirmed, finalized).\n\n**--use-quic**\n> Use QUIC protocol when sending transactions.\n\n**--ws** _URL_\n> WebSocket URL for the Solana cluster.\n\n**--output** _FORMAT_\n> Output format: json, json-compact.\n\n**-h**, **--help**\n> Print help information.\n\n**-V**, **--version**\n> Print version information."],["SUBCOMMANDS","**balance**\n> Get your balance or that of a specified address.\n\n**transfer**\n> Transfer SOL between accounts.\n\n**airdrop**\n> Request an airdrop of SOL (devnet/testnet only).\n\n**config**\n> Get and set CLI configuration.\n\n**account**\n> Show account contents.\n\n**address**\n> Get your public key.\n\n**program deploy**\n> Deploy a Solana program.\n\n**stake-account**\n> Manage stake accounts.\n\n**validator-info**\n> Publish or get validator information.\n\n**epoch-info**\n> Get information about the current epoch.\n\n**slot**\n> Get current slot.\n\n**block-height**\n> Get current block height.\n\n**logs**\n> Stream transaction logs.\n\n**confirm**\n> Confirm a transaction by signature."],["DESCRIPTION","**solana** is the command-line interface for interacting with the Solana blockchain. It provides tools for managing wallets, transferring tokens, deploying programs (smart contracts), staking, and querying blockchain state.\n\nThe CLI connects to Solana clusters via JSON-RPC. Three main clusters are available: **mainnet-beta** (production), **testnet** (testing), and **devnet** (development). Configuration is stored in **~/.config/solana/cli/config.yml**.\n\nKeypairs for signing transactions can be generated with **solana-keygen** and are typically stored as JSON files. The CLI is the primary interface for Solana validators and program developers."],["CONFIGURATION","**~/.config/solana/cli/config.yml**\n> CLI configuration file storing default RPC URL, keypair path, and commitment level.\n\n**~/.config/solana/id.json**\n> Default keypair file for signing transactions."],["CAVEATS","Airdrops are only available on devnet and testnet, not mainnet. Transactions require SOL for fees. Private keys should be protected; never share keypair files. The CLI requires network connectivity to the configured cluster. Rate limits may apply on public RPC endpoints."],["HISTORY","**Solana** was founded by Anatoly Yakovenko in **2017** and launched its mainnet beta in **2020**. The CLI has been the primary tool for interacting with Solana since its inception. Development transitioned from Solana Labs to **Anza** (the Agave client) in **2024**, with the CLI continuing as the first place new functionality is deployed. Solana is known for high throughput and low transaction costs."],["INSTALL","```brew: brew install solana```"]],"e":[],"x":"solana balance"},"taskwarrior":{"n":"taskwarrior","t":"Command-line task and todo manager","s":[["TAGLINE","Command-line task and todo manager"],["TLDR","**Add a new task**\n\n```task add [Buy groceries]```\n\n**List all pending tasks**\n\n```task list```\n\n**Mark task as done**\n\n```task [1] done```\n\n**Add task with project and priority**\n\n```task add [description] project:[Home] priority:[H]```\n\n**Add task with due date**\n\n```task add [description] due:[tomorrow]```\n\n**Add task with tags**\n\n```task add [description] +[work] +[urgent]```\n\n**Start working on a task**\n\n```task [1] start```\n\n**View detailed task information**\n\n```task [1] info```\n\n**Delete a task**\n\n```task [1] delete```\n\n**Synchronize tasks**\n\n```task sync```"],["SYNOPSIS","**task** [_filter_] [_command_] [_modifications_]"],["COMMANDS","**add** _description_: Create a new task.\n\n**done**: Mark task(s) as completed.\n\n**delete**: Delete task(s).\n\n**start**: Mark task(s) as started.\n\n**stop**: Mark task(s) as stopped.\n\n**modify**: Change task attributes.\n\n**annotate**: Add note to task.\n\n**edit**: Edit task in text editor.\n\n**list**: Show pending tasks.\n\n**all**: Show all tasks including completed.\n\n**next**: Show most urgent tasks.\n\n**active**: Show started tasks.\n\n**blocked**: Show blocked tasks.\n\n**projects**: List all projects.\n\n**tags**: List all tags.\n\n**reports**: List available reports.\n\n**sync**: Synchronize with a sync server or cloud backend.\n\n**undo**: Revert the last change.\n\n**purge**: Permanently remove deleted tasks.\n\n**shell**: Interactive task shell.\n\n**config**: Modify configuration."],["MODIFICATIONS","**project:**_name_: Assign to project.\n\n**priority:**_H|M|L_: Set priority (High/Medium/Low).\n\n**due:**_date_: Set due date.\n\n**scheduled:**_date_: Set scheduled date.\n\n**until:**_date_: Set expiration date.\n\n**recur:**_freq_: Set recurrence (daily, weekly, monthly).\n\n**wait:**_date_: Hide until date.\n\n**+**_tag_: Add tag.\n\n**-**_tag_: Remove tag.\n\n**depends:**_id_: Set task dependency."],["DESCRIPTION","**Taskwarrior** (the `task` command) is a command-line TODO list manager. It provides flexible task management with support for projects, priorities, tags, due dates, recurrence, and dependencies.\n\nTasks can be filtered using any attribute, dates, or logical expressions. The tool generates various reports including lists, calendars, and burndown charts.\n\nConfiguration is stored in `~/.taskrc` with extensive customization options. The tool supports hooks for automation and sync for sharing tasks across devices.\n\nStarting with **Taskwarrior 3.0** (2024), data is stored in a local SQLite database (`taskchampion.sqlite3`) and synchronization uses a new backend supporting AWS S3, Google Cloud Storage, or `taskchampion-sync-server`. The legacy `taskd` server is no longer supported."],["CAVEATS","First run creates `~/.taskrc`. Task IDs change as tasks are completed; use UUID for permanent references in scripts. Some reports require sufficient terminal width for proper display. Taskwarrior 3.x storage is not compatible with file-sync tools like Syncthing or rsync since data lives in a SQLite database."],["HISTORY","**Taskwarrior** was created by Paul Beckingham and Federico Hernandez. The first version was released in 2006, with the project growing into a task management ecosystem. Taskwarrior 3.0 (2024) replaced the file-based store and `taskd` server with the TaskChampion library and a new sync protocol."],["INSTALL","```zypper: sudo zypper install taskwarrior```"],["SEE ALSO","[timew](/man/timew)(1), [todo.txt-cli](/man/todo.txt-cli)(1)"]],"e":["timew","todo.txt-cli"],"x":"task add [Buy groceries]"},"terminator":{"n":"terminator","t":"Multiple GNOME terminals in one window","s":[["TAGLINE","Multiple GNOME terminals in one window"],["TLDR","**Start** terminator window\n\n```terminator```\n\n**Start** with a fullscreen window\n\n```terminator -f```\n\n**Split** terminals horizontally\n\n```Ctrl+Shift+O```\n\n**Split** terminals vertically\n\n```Ctrl+Shift+E```\n\n**Open** new tab\n\n```Ctrl+Shift+T```"],["SYNOPSIS","**terminator** [_options_]"],["PARAMETERS","**-f, --fullscreen**\n> Start in fullscreen mode\n\n**-m, --maximise**\n> Start maximized\n\n**-b, --borderless**\n> Start without window decorations\n\n**-T, --title _title_**\n> Set window title\n\n**-e, --command _command_**\n> Run command in the terminal\n\n**-l, --layout _layout_**\n> Load a specific layout\n\n**-p, --profile _profile_**\n> Use specified profile\n\n**-g, --config _file_**\n> Use alternate config file"],["DESCRIPTION","**terminator** is a terminal emulator that allows arranging multiple GNOME terminals in a single window. Terminals can be split horizontally and vertically, organized into tabs, and resized dynamically.\n\nKey bindings allow quick navigation and manipulation of the terminal grid. Layouts and profiles can be saved and restored for consistent workspace setups."],["CAVEATS","Requires a graphical environment. Configuration is stored in **~/.config/terminator/**. Some key bindings may conflict with applications running in the terminal."],["INSTALL","```dnf: sudo dnf install terminator```\n\n```pacman: sudo pacman -S terminator```\n\n```apk: sudo apk add terminator```\n\n```zypper: sudo zypper install terminator```\n\n```brew: brew install terminator```\n\n```nix: nix profile install nixpkgs#terminator```"],["SEE ALSO","[gnome-terminal](/man/gnome-terminal)(1), [tmux](/man/tmux)(1), [screen](/man/screen)(1), [konsole](/man/konsole)(1)"]],"e":["gnome-terminal","tmux","screen","konsole"],"x":"terminator"},"uinput":{"n":"uinput","t":"Virtual input device event writer","s":[["TAGLINE","Virtual input device event writer"],["TLDR","**Preview** device description (dry-run)\n\n```uinput -p```\n\nMerge **YAML** device descriptions\n\n```sudo uinput -c [path/to/device1.yaml] [path/to/device2.yaml]```\n\nMerge from **device** nodes\n\n```sudo uinput -d [/dev/input/eventX] [/dev/input/eventY]```"],["SYNOPSIS","**uinput** [_OPTIONS_]"],["PARAMETERS","**-p**\n> Print the merged YAML device description and exit (dry-run preview).\n\n**-c** _FILE..._\n> Merge one or more YAML device-description files into the virtual device.\n\n**-d** _DEVICE..._\n> Merge capabilities cloned from existing `/dev/input/event*` device nodes.\n\n**-h**, **--help**\n> Display help information."],["DESCRIPTION","**uinput** is the writer half of the Interception Tools pipeline: it reads `input_event` records from stdin and dispatches them through a freshly created virtual input device backed by the kernel's `/dev/uinput` interface. The virtual device's capabilities (keys, axes, LEDs, etc.) are derived from the YAML description files passed via `-c`, optionally merged with capabilities cloned from existing devices via `-d`.\n\nTypical usage chains it after **udevmon** and per-device transformation tools (such as **caps2esc**) so that remapped events are re-injected into the input stack as if they came from a normal device."],["CAVEATS","Requires CAP_MKNOD and write access to **/dev/uinput** — usually run as root or via a udev rule that grants the executing user access. Loading the `uinput` kernel module is required (`modprobe uinput`). Misconfigured pipelines can leave the system without keyboard input; have a fallback session ready."],["INSTALL","```apt: sudo apt install interception-tools```\n\n```pacman: sudo pacman -S interception-tools```\n\n```nix: nix profile install nixpkgs#interception-tools```"],["SEE ALSO","[udevmon](/man/udevmon)(1)"]],"e":["udevmon"],"x":"uinput -p"},"wasabi":{"n":"wasabi","t":"privacy-focused Bitcoin wallet with CoinJoin","s":[["TAGLINE","privacy-focused Bitcoin wallet with CoinJoin"],["TLDR","**Start the wallet GUI**\n\n```wassabee```\n\n**Start the headless daemon**\n\n```wassabeed```\n\n**Start the daemon on testnet**\n\n```wassabeed --network testnet```\n\n**Specify a custom data directory**\n\n```wassabeed --datadir [/path/to/data]```"],["SYNOPSIS","**wassabee** [_options_]\n\n**wassabeed** [_options_]"],["PARAMETERS","**--network** _NETWORK_\n> Bitcoin network to use (main, testnet, regtest).\n\n**--datadir** _DIR_\n> Custom data directory path.\n\n**--usetor** _BOOL_\n> Enable or disable Tor routing (default: true).\n\n**--blockonly** _BOOL_\n> Ignore P2P transactions; only process block data.\n\n**--jsonrpcserverenabled** _BOOL_\n> Enable JSON-RPC server for remote control.\n\n**--jsonrpcserverprefix** _URL_\n> JSON-RPC server URL prefix.\n\n**--help**\n> Display help.\n\n**--version**\n> Display version."],["DESCRIPTION","**Wasabi Wallet** is an open-source, privacy-focused Bitcoin wallet that implements **CoinJoin** for transaction anonymization. It uses the **WabiSabi** protocol for trustless, coordinator-based coin mixing.\n\nThe wallet can run as a graphical application (**wassabee**) or as a headless daemon (**wassabeed**) for server deployments. The daemon exposes a JSON-RPC interface for programmatic wallet control. All connections are routed through **Tor** by default for network-level privacy."],["CAVEATS","CoinJoin transactions require a minimum amount and incur coordinator fees. Tor is bundled and used by default. Written in C# (.NET)."],["INSTALL","```aur: yay -S wasabi```"],["SEE ALSO","[electrum](/man/electrum)(1), [lnd](/man/lnd)(1)"]],"e":["electrum","lnd"],"x":"wassabee"},"xfs_repair":{"n":"xfs_repair","t":"Repair XFS filesystem","s":[["TAGLINE","Repair XFS filesystem"],["TLDR","**Repair** partition\n\n```sudo xfs_repair [path/to/partition]```"],["SYNOPSIS","**xfs_repair** [_OPTIONS_] _DEVICE_"],["PARAMETERS","**-n**\n> No modify mode (check only)\n\n**-L**\n> Force log zeroing (data loss risk)\n\n**-v**\n> Verbose output\n\n**-d**\n> Repair dangerously (allow repair on mounted FS)\n\n**-m** _MAXMEM_\n> Maximum memory usage"],["DESCRIPTION","**xfs_repair** repairs an XFS filesystem. It checks and fixes filesystem metadata, directory structures, and allocation information.\n\nThe filesystem must be unmounted before repair. A log replay may be needed first."],["CAVEATS","Requires unmounted filesystem. Using -L can cause data loss. Backup important data before repair. May require running multiple times."],["INSTALL","```apt: sudo apt install xfsprogs```\n\n```dnf: sudo dnf install xfsprogs```\n\n```pacman: sudo pacman -S xfsprogs```\n\n```apk: sudo apk add xfsprogs```\n\n```zypper: sudo zypper install xfsprogs```\n\n```nix: nix profile install nixpkgs#xfsprogs```"],["SEE ALSO","[xfs_admin](/man/xfs_admin)(8), [fsck](/man/fsck)(8), [mkfs.xfs](/man/mkfs.xfs)(8)"]],"e":["xfs_admin","fsck","mkfs.xfs"],"x":"sudo xfs_repair [path/to/partition]"},"yacc":{"n":"yacc","t":"LALR parser generator for C","s":[["TAGLINE","LALR parser generator for C"],["TLDR","**Generate a parser** from a grammar file\n\n```yacc [grammar.y]```\n\n**Generate parser with header file** for token definitions\n\n```yacc -d [grammar.y]```\n\n**Use a custom file prefix** instead of y\n\n```yacc -b [prefix] [grammar.y]```\n\n**Use a custom symbol prefix** instead of yy\n\n```yacc -p [prefix] [grammar.y]```\n\n**Generate verbose output** describing the parser\n\n```yacc -v [grammar.y]```\n\n**Enable debugging** in generated code\n\n```yacc -t [grammar.y]```\n\n**Generate separate files** for code and tables\n\n```yacc -r [grammar.y]```"],["SYNOPSIS","**yacc** [_-dgilrtv_] [_-b prefix_] [_-p prefix_] _filename_"],["PARAMETERS","**-b** _prefix_\n> Use prefix for output filenames instead of **y**.\n\n**-d**\n> Generate header file **y.tab.h** with token definitions.\n\n**-g**\n> Generate graphviz format file **y.dot** for visualization.\n\n**-i**\n> Generate supplementary header file **y.tab.i**.\n\n**-l**\n> Do not insert **#line** directives in generated code.\n\n**-p** _prefix_\n> Use prefix for yacc-generated symbols instead of **yy**.\n\n**-P**\n> Create a reentrant (pure) parser.\n\n**-r**\n> Generate separate files for code (**y.code.c**) and tables (**y.tab.c**).\n\n**-t**\n> Enable debugging statements in compiled code.\n\n**-v**\n> Generate verbose description file **y.output**.\n\n**-V**\n> Print version information."],["DESCRIPTION","**yacc** (Yet Another Compiler Compiler) is an LALR(1) parser generator that reads grammar specifications and generates C code for a parser. The grammar file defines tokens, syntax rules, and semantic actions.\n\nyacc produces **y.tab.c** containing parsing tables and a driver routine. When used with **-d**, it also generates **y.tab.h** with token definitions for use with lex/flex lexers.\n\nOn most Linux systems, **yacc** is actually **bison** (GNU parser generator) or **byacc** (Berkeley Yacc), both compatible with traditional AT&T yacc."],["CAVEATS","yacc reports shift/reduce and reduce/reduce conflicts that may indicate grammar ambiguities. Rules that are never reduced are also reported. The generated parser uses global variables by default; use **-P** for reentrant parsers."],["HISTORY","yacc was written by **Stephen C. Johnson** at Bell Labs and first released in **1975** for Unix Version 6. The name stands for \"Yet Another Compiler Compiler\". **Berkeley Yacc** (byacc) was written by Robert Corbett in **1989** as a public domain implementation. **GNU Bison**, a compatible replacement with extensions, was first released in **1985** and is the standard yacc on GNU/Linux systems."],["INSTALL","```dnf: sudo dnf install bison```\n\n```pacman: sudo pacman -S bison```\n\n```apk: sudo apk add bison```\n\n```zypper: sudo zypper install bison```\n\n```brew: brew install bison```\n\n```nix: nix profile install nixpkgs#bison```"],["SEE ALSO","[bison](/man/bison)(1), [lex](/man/lex)(1), [flex](/man/flex)(1), [cc](/man/cc)(1)"]],"e":["bison","lex","flex","cc"],"x":"yacc [grammar.y]"},"ydotool":{"n":"ydotool","t":"Simulate input on X11 and Wayland","s":[["TAGLINE","Simulate input on X11 and Wayland"],["TLDR","Start **daemon**\n\n```ydotoold```\n\n**Left click**\n\n```ydotool click 0xC0```\n\n**Right click**\n\n```ydotool click 0xC1```\n\nPress **Alt+F4**\n\n```ydotool key 56:1 62:1 62:0 56:0```"],["SYNOPSIS","**ydotool** _COMMAND_ [_OPTIONS_]"],["PARAMETERS","**click** _CODE_\n> Simulate mouse click (0xC0=left, 0xC1=right, 0xC2=middle)\n\n**key** _KEYCODE:STATE..._\n> Simulate key press/release (1=press, 0=release)\n\n**type** _TEXT_\n> Type text string\n\n**mousemove** _X_ _Y_\n> Move mouse to position"],["DESCRIPTION","**ydotool** simulates keyboard and mouse input on Linux. Unlike xdotool, it works on both X11 and Wayland by using the kernel uinput module.\n\nRequires the ydotoold daemon to be running."],["CAVEATS","Requires ydotoold daemon running. Needs access to /dev/uinput. Key codes are hardware scancodes, not X11 keysyms."],["INSTALL","```dnf: sudo dnf install ydotool```\n\n```pacman: sudo pacman -S ydotool```\n\n```zypper: sudo zypper install ydotool```\n\n```nix: nix profile install nixpkgs#ydotool```"],["SEE ALSO","[xdotool](/man/xdotool)(1), [wtype](/man/wtype)(1)"]],"e":["xdotool","wtype"],"x":"ydotoold"},"yum":{"n":"yum","t":"Red Hat package manager","s":[["TAGLINE","Red Hat package manager"],["TLDR","On modern distros (RHEL 8+, Fedora 22+), **yum** is a symlink to **dnf**.\n\n**Install** a package\n\n```sudo yum install [package]```\n\n**Remove** a package\n\n```sudo yum remove [package]```\n\n**Update** all packages\n\n```sudo yum update```\n\n**Update** a specific package\n\n```sudo yum update [package]```\n\n**Search** for a package by name\n\n```yum search [keyword]```\n\n**List installed** packages\n\n```yum list installed```\n\n**Show info** about a package\n\n```yum info [package]```\n\n**Clean** cached data\n\n```yum clean all```"],["SYNOPSIS","**yum** [_options_] [_command_] [_package_...]"],["PARAMETERS","**install** _package_\n> Install one or more packages.\n\n**remove** _package_\n> Remove a package.\n\n**update** [_package_]\n> Update all packages, or a specific package if given.\n\n**search** _keyword_\n> Search package names and descriptions.\n\n**info** _package_\n> Display detailed information about a package.\n\n**list** [_installed_|_available_|_updates_]\n> List packages. Use **installed** for installed, **available** for repo packages, **updates** for available updates.\n\n**clean** [_all_|_packages_|_metadata_]\n> Clean cached data.\n\n**provides** _file_\n> Find which package provides a given file.\n\n**groupinstall** _group_\n> Install a package group.\n\n**repolist** [_all_|_enabled_|_disabled_]\n> List configured repositories.\n\n**history**\n> View transaction history.\n\n**-y**\n> Assume yes to all prompts.\n\n**--enablerepo=**_repo_\n> Temporarily enable a specific repository.\n\n**--disablerepo=**_repo_\n> Temporarily disable a specific repository.\n\n**--nogpgcheck**\n> Skip GPG signature checking."],["DESCRIPTION","**yum** (Yellowdog Updater Modified) is the traditional package manager for Red Hat-based Linux distributions. On modern systems (RHEL 8+, Fedora 22+), **yum** is a symlink to **dnf**, which provides the same functionality with improved performance.\n\nOn older systems like CentOS 7, yum is the native package manager handling package installation, updates, and removal from configured repositories."],["CAVEATS","On modern systems, yum is an alias for dnf. Behavior may differ slightly between legacy yum and dnf. Use **dnf** directly on newer systems."],["SEE ALSO","[dnf](/man/dnf)(8), [rpm](/man/rpm)(8), [apt-get](/man/apt-get)(8), [zypper](/man/zypper)(8)"]],"e":["dnf","rpm","apt-get","zypper"],"x":"sudo yum install [package]"},"yuvtoppm":{"n":"yuvtoppm","t":"Convert Abekas YUV format to PPM","s":[["TAGLINE","Convert Abekas YUV format to PPM"],["TLDR","**Convert Abekas YUV to PPM**\n\n```yuvtoppm [width] [height] [input.yuv] > [output.ppm]```"],["SYNOPSIS","**yuvtoppm** _width_ _height_ [_file_]"],["PARAMETERS","_width_\n> Image width.\n\n_height_\n> Image height."],["DESCRIPTION","**yuvtoppm** converts Abekas YUV format to PPM. Abekas made professional video equipment. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[ppmtoyuv](/man/ppmtoyuv)(1), [yuvsplittoppm](/man/yuvsplittoppm)(1)"]],"e":["ppmtoyuv","yuvsplittoppm"],"x":"yuvtoppm [width] [height] [input.yuv] > [output.ppm]"},"zinit":{"n":"zinit","t":"Fast Zsh plugin manager with Turbo mode","s":[["TAGLINE","Fast Zsh plugin manager with Turbo mode"],["TLDR","**Load a plugin** from GitHub\n\n```zinit light [username/repository]```\n\n**Load a plugin with reporting** (slower but provides info)\n\n```zinit load [username/repository]```\n\n**Install and load Oh-My-Zsh plugin**\n\n```zinit snippet OMZ::plugins/[plugin]/[plugin].plugin.zsh```\n\n**Load with Turbo mode** (async, delayed loading)\n\n```zinit ice wait lucid; zinit light [username/repository]```\n\n**Load a completion** from a repository\n\n```zinit ice as\"completion\"; zinit snippet [url]```\n\n**Update all plugins**\n\n```zinit update --all```\n\n**Update a specific plugin**\n\n```zinit update [username/repository]```\n\n**List installed plugins**\n\n```zinit list```"],["SYNOPSIS","**zinit** [_light_|_load_|_snippet_|_ice_|_update_|_delete_|_list_|_self-update_|_compile_|_uncompile_|_report_] [_arguments_]"],["PARAMETERS","**light** _plugin_\n> Load a plugin in light mode without tracking/reporting (fast).\n\n**load** _plugin_\n> Load a plugin with full tracking and reporting.\n\n**snippet** _url_\n> Load a single file or snippet from a URL or Oh-My-Zsh/Prezto.\n\n**ice** _modifiers..._\n> Set modifiers for the next zinit command (compile, wait, pick, etc.).\n\n**update** [_plugin_|_--all_]\n> Update specified plugin or all plugins.\n\n**delete** _plugin_\n> Remove a plugin.\n\n**list**\n> List all loaded plugins.\n\n**loaded**\n> Show currently loaded plugins.\n\n**self-update**\n> Update zinit itself.\n\n**compile** _plugin_\n> Compile plugin scripts for faster loading.\n\n**uncompile** _plugin_\n> Remove compiled files.\n\n**report** _plugin_\n> Show report for a loaded plugin.\n\n**times**\n> Show loading times for plugins."],["ICE MODIFIERS","**wait**\n> Turbo mode: delay loading (can specify time like wait\"1\").\n\n**lucid**\n> Skip \"Loaded plugin\" message.\n\n**as\"program\"**\n> Add plugin directory to PATH.\n\n**as\"completion\"**\n> Load as completion script.\n\n**pick\"file\"**\n> Select specific file to source.\n\n**from\"gitlab\"**\n> Clone from GitLab instead of GitHub.\n\n**depth\"1\"**\n> Shallow clone with depth 1.\n\n**atload\"command\"**\n> Run command after loading.\n\n**atinit\"command\"**\n> Run command before loading."],["DESCRIPTION","**Zinit** (formerly Zplugin) is a flexible and fast Zsh plugin manager that supports Oh-My-Zsh and Prezto plugins, Turbo mode for deferred loading, and multiple installation methods. It compiles plugins to bytecode for faster shell startup times.\n\nThe key feature is Turbo mode, which loads plugins asynchronously after Zsh starts, dramatically reducing shell initialization time. Ice modifiers provide fine-grained control over how plugins are downloaded, compiled, and sourced.\n\nZinit supports loading from GitHub, GitLab, Bitbucket, or arbitrary URLs. It can handle plugins, completions, scripts, and binary programs. The **light** command provides fast loading without tracking, while **load** enables full reporting capabilities.\n\nConfiguration is typically placed in **.zshrc** after sourcing zinit. Plugins are stored in **~/.zinit/plugins/** by default, with compiled versions cached for performance."],["CAVEATS","Requires Zsh 5.1 or later. Turbo mode may cause race conditions with plugins that depend on each other. Some Oh-My-Zsh plugins require specific OMZ infrastructure to work correctly. Heavy use of ice modifiers can make configuration complex and hard to debug."],["HISTORY","Zinit was created by Sebastian Gniazdowski, originally released as **Zplugin** in **2016**. It was renamed to Zinit in 2019 to avoid confusion with other tools. The project pioneered Turbo mode for asynchronous plugin loading in Zsh. The original repository was archived in 2021, and community forks continue maintenance under **zdharma-continuum/zinit**."],["INSTALL","```brew: brew install zinit```\n\n```nix: nix profile install nixpkgs#zinit```"],["SEE ALSO","[zsh](/man/zsh)(1), [oh-my-zsh](/man/oh-my-zsh)(1), [antibody](/man/antibody)(1), [zplug](/man/zplug)(1), [antigen](/man/antigen)(1)"]],"e":["zsh","oh-my-zsh","antibody","zplug","antigen"],"x":"zinit light [username/repository]"}});
