window.LCL_BUCKET(28,{"anacron":{"n":"anacron","t":"Run periodic jobs on systems not running continuously","s":[["TAGLINE","Run periodic jobs on systems not running continuously"],["TLDR","**Run** anacron jobs now\n\n```sudo anacron -n```\n\nRun in **foreground**\n\n```sudo anacron -d```\n\n**Test** run (don't execute jobs)\n\n```anacron -T```\n\nForce run regardless of **timestamps**\n\n```sudo anacron -f```\n\nUse **specific config** file\n\n```sudo anacron -t [/etc/anacrontab]```"],["SYNOPSIS","**anacron** [_-s_] [_-f_] [_-n_] [_-d_] [_-t anacrontab_] [_-T_]"],["DESCRIPTION","**anacron** is a cron-like job scheduler that doesn't assume the computer is running continuously. It's ideal for laptops and desktop machines that aren't powered on 24/7, ensuring periodic jobs run even if the scheduled time was missed.\n\nUnlike cron, which runs jobs at specific times, anacron runs jobs based on periods (daily, weekly, monthly) and tracks the last run date. If a job's period has elapsed since the last run, it executes the job."],["PARAMETERS","**-f**\n> Force execution ignoring timestamps\n\n**-n**\n> Run jobs now, ignoring delays\n\n**-d**\n> Run in foreground (no daemon)\n\n**-s**\n> Serialize job execution (sequential)\n\n**-T**\n> Test configuration file syntax only\n\n**-t** _file_\n> Use alternate anacrontab file\n\n**-S** _dir_\n> Use alternate spool directory\n\n**-u**\n> Update timestamps only, don't run jobs"],["CONFIGURATION","**/etc/anacrontab**\n> Defines periodic jobs with their period in days, delay in minutes, identifier, and command to execute.\n\n**/var/spool/anacron/**\n> Timestamp files tracking the last execution date of each job."],["CAVEATS","Runs as root by default; user-level jobs require additional configuration. Job delay settings are randomized to avoid resource spikes. Not suitable for time-critical tasks."],["HISTORY","**anacron** was created by Christian Schwarz and first released in **1998** to address cron's unsuitability for machines that aren't always on. Most modern Linux distributions include it alongside traditional cron."],["INSTALL","```apt: sudo apt install anacron```\n\n```dnf: sudo dnf install cronie```\n\n```pacman: sudo pacman -S cronie```\n\n```apk: sudo apk add cronie```\n\n```zypper: sudo zypper install cronie```\n\n```nix: nix profile install nixpkgs#cronie```"],["SEE ALSO","[cron](/man/cron)(8), [crontab](/man/crontab)(1)"]],"e":["cron","crontab"],"x":"sudo anacron -n"},"avr-gcc":{"n":"avr-gcc","t":"GNU C compiler for AVR microcontrollers","s":[["TAGLINE","GNU C compiler for AVR microcontrollers"],["TLDR","**Compile C code** for an AVR microcontroller\n\n```avr-gcc -mmcu=[atmega328p] -o [output.elf] [source.c]```\n\n**Compile with optimization** for size\n\n```avr-gcc -mmcu=[atmega328p] -Os -o [output.elf] [source.c]```\n\n**Compile with debugging** symbols\n\n```avr-gcc -mmcu=[atmega328p] -g -O0 -o [output.elf] [source.c]```\n\n**Generate hex file** for flashing\n\n```avr-gcc -mmcu=[atmega328p] -Os -o [output.elf] [source.c] && avr-objcopy -O ihex [output.elf] [output.hex]```\n\n**Compile with warnings** and link relaxation\n\n```avr-gcc -mmcu=[atmega328p] -Wall -Os -Wl,--relax -o [output.elf] [source.c]```\n\n**Compile for freestanding** environment (no hosted library)\n\n```avr-gcc -mmcu=[atmega328p] -ffreestanding -Os -o [output.elf] [source.c]```"],["SYNOPSIS","**avr-gcc** [_options_] _file_..."],["PARAMETERS","**-mmcu=**_mcu_\n> Target MCU (atmega328p, attiny85, atmega2560, etc.); required for correct code generation\n\n**-Os**\n> Optimize for size (recommended for embedded)\n\n**-O0**, **-O1**, **-O2**, **-O3**\n> Optimization levels (0=none, 3=maximum)\n\n**-g**\n> Include debugging information\n\n**-Wall**\n> Enable all common warnings\n\n**-ffreestanding**\n> Assume freestanding environment (no standard library assumptions)\n\n**-fno-jump-tables**\n> Disable jump tables (required for bootloaders on >64KB devices)\n\n**-mrelax**\n> Enable linker relaxation to use shorter instructions when possible\n\n**-Wl,--relax**\n> Pass relaxation option to linker\n\n**-Wl,-gc-sections**\n> Remove unused code sections to reduce binary size\n\n**-mcall-prologues**\n> Use subroutines for function prologues/epilogues (saves space)\n\n**-DF_CPU=**_freq_\n> Define CPU frequency in Hz (e.g., -DF_CPU=16000000)\n\n**-I**_path_\n> Add include directory\n\n**-L**_path_\n> Add library search directory\n\n**-l**_library_\n> Link with library"],["DESCRIPTION","**avr-gcc** is the GNU Compiler Collection configured for AVR 8-bit microcontrollers. It compiles C and C++ code into machine code for Atmel/Microchip AVR chips used in Arduino and embedded systems.\n\nThe **-mmcu** option is essential as it configures code generation, memory layout, and available instructions for the specific target MCU. Common targets include atmega328p (Arduino Uno), atmega2560 (Arduino Mega), and attiny85.\n\nOutput is typically an ELF file, which is converted to Intel HEX format using **avr-objcopy** for flashing to hardware via tools like **avrdude**.\n\nThe **-Os** optimization is preferred for embedded development as it minimizes code size while maintaining reasonable performance. The **-ffreestanding** flag is appropriate since AVR programs run without an operating system.\n\nAVR-GCC is typically installed as part of the **avr-libc** toolchain, which includes the C library, header files, and additional tools."],["CAVEATS","The **-mmcu** option must match the target hardware exactly. Optimization level **-O3** often increases code size significantly on AVR. Programs should avoid heavy stack usage due to limited RAM. Integer math defaults to 16-bit; use explicit types for larger values."],["HISTORY","**AVR-GCC** emerged from the GCC project's support for the AVR architecture, initially developed by **Denis Chertykov** in the late **1990s**. Combined with **AVR-LibC**, it became the standard open-source toolchain for AVR development. The toolchain gained widespread adoption through the Arduino project starting in **2005**, making AVR programming accessible to hobbyists and educators."],["INSTALL","```apt: sudo apt install gcc-avr```\n\n```dnf: sudo dnf install avr-gcc```\n\n```pacman: sudo pacman -S avr-gcc```\n\n```apk: sudo apk add gcc-avr```"],["SEE ALSO","[gcc](/man/gcc)(1), [avrdude](/man/avrdude)(1), [arduino-cli](/man/arduino-cli)(1)"]],"e":["gcc","avrdude","arduino-cli"],"x":"avr-gcc -mmcu=[atmega328p] -o [output.elf] [source.c]"},"az-appconfig":{"n":"az-appconfig","t":"Manage Azure App Configuration stores","s":[["TAGLINE","Manage Azure App Configuration stores"],["TLDR","**Create an App Configuration store**\n\n```az appconfig create --name [MyAppConfig] --resource-group [MyResourceGroup] --location [westus] --sku [Standard]```\n\n**List all App Configuration stores** in a resource group\n\n```az appconfig list --resource-group [MyResourceGroup]```\n\n**Show details** of an App Configuration store\n\n```az appconfig show --name [MyAppConfig] --resource-group [MyResourceGroup]```\n\n**Set a key-value** in the store\n\n```az appconfig kv set --name [MyAppConfig] --key [myKey] --value [myValue]```\n\n**List key-values** in the store\n\n```az appconfig kv list --name [MyAppConfig]```\n\n**Import configuration** from a file\n\n```az appconfig kv import --name [MyAppConfig] --source file --path [config.json] --format json```\n\n**Enable a feature flag**\n\n```az appconfig feature enable --name [MyAppConfig] --feature [myFeature]```\n\n**Delete an App Configuration store**\n\n```az appconfig delete --name [MyAppConfig] --resource-group [MyResourceGroup]```"],["SYNOPSIS","**az** **appconfig** _subcommand_ [_options_]"],["SUBCOMMANDS","**create**\n> Create an App Configuration store.\n\n**delete**\n> Delete an App Configuration store.\n\n**list**\n> List all App Configuration stores.\n\n**show**\n> Show properties of a store.\n\n**update**\n> Update an App Configuration store.\n\n**kv**\n> Manage key-values (set, get, list, delete, lock, unlock).\n\n**kv import/export**\n> Import/export configurations.\n\n**feature**\n> Manage feature flags (enable, disable, set, delete).\n\n**credential**\n> Manage access keys.\n\n**replica**\n> Manage replicas (Premium tier).\n\n**snapshot**\n> Manage configuration snapshots."],["DESCRIPTION","**az appconfig** manages Azure App Configuration stores, which provide a service to centrally manage application settings and feature flags. App Configuration helps implement the Twelve-Factor App methodology by separating configuration from code.\n\nAvailable SKUs are Free, Developer, Standard, and Premium (default is Standard), with Premium offering additional features like geo-replication via replicas."],["CAVEATS","Requires Azure CLI to be installed and authenticated with **az login**. Some features like replicas are only available in Premium tier."],["INSTALL","```dnf: sudo dnf install azure-cli```\n\n```pacman: sudo pacman -S azure-cli```\n\n```zypper: sudo zypper install azure-cli```\n\n```brew: brew install azure-cli```\n\n```nix: nix profile install nixpkgs#azure-cli```"],["SEE ALSO","[az](/man/az)(1), [az-config](/man/az-config)(1), [az-advisor](/man/az-advisor)(1)"],["RESOURCES","```[Source code](https://github.com/Azure/azure-cli)```\n\n```[Documentation](https://learn.microsoft.com/en-us/cli/azure/appconfig)```"]],"e":["az","az-config","az-advisor"],"x":"az appconfig create --name [MyAppConfig] --resource-group [MyResourceGroup] --location [westus] --sku [Standard]"},"az-cognitiveservices":{"n":"az-cognitiveservices","t":"Manage Azure AI and Cognitive Services accounts","s":[["TAGLINE","Manage Azure AI and Cognitive Services accounts"],["TLDR","**Create a Cognitive Services account**\n\n```az cognitiveservices account create --name [myaccount] --resource-group [myRG] --kind [TextAnalytics] --sku [S1] --location [eastus]```\n\n**List all Cognitive Services accounts**\n\n```az cognitiveservices account list```\n\n**Show account details**\n\n```az cognitiveservices account show --name [myaccount] --resource-group [myRG]```\n\n**List API access keys**\n\n```az cognitiveservices account keys list --name [myaccount] --resource-group [myRG]```\n\n**Regenerate an API key**\n\n```az cognitiveservices account keys regenerate --name [myaccount] --resource-group [myRG] --key-name [key1]```\n\n**List available account kinds**\n\n```az cognitiveservices account list-kinds```\n\n**List available SKUs** for a specific kind and location\n\n```az cognitiveservices account list-skus --kind [TextAnalytics] --location [eastus]```\n\n**Delete an account**\n\n```az cognitiveservices account delete --name [myaccount] --resource-group [myRG]```"],["SYNOPSIS","**az** **cognitiveservices** **account** _subcommand_ [_options_]"],["SUBCOMMANDS","**account create**\n> Create a Cognitive Services account.\n\n**account delete**\n> Delete a Cognitive Services account.\n\n**account list**\n> List all Cognitive Services accounts.\n\n**account show**\n> Show details of a specific account.\n\n**account keys list**\n> List account access keys.\n\n**account keys regenerate**\n> Regenerate account keys.\n\n**account deployment**\n> Manage model deployments.\n\n**account commitment-plan**\n> Manage commitment plans.\n\n**account network-rule**\n> Manage network rules.\n\n**account list-kinds**\n> List available account kinds.\n\n**account list-skus**\n> List available SKUs for a service kind and location."],["DESCRIPTION","**az cognitiveservices** manages Azure Cognitive Services accounts, which provide AI capabilities including vision, speech, language, and decision APIs. It handles account creation, configuration, deployments, and subscription management for services like Text Analytics, Computer Vision, Speech Services, and OpenAI."],["CAVEATS","Requires Azure CLI to be installed and authenticated with **az login**. Different service kinds have different SKU availability and pricing. Some services require accepting responsible AI terms before creation."],["INSTALL","```dnf: sudo dnf install azure-cli```\n\n```pacman: sudo pacman -S azure-cli```\n\n```zypper: sudo zypper install azure-cli```\n\n```brew: brew install azure-cli```\n\n```nix: nix profile install nixpkgs#azure-cli```"],["SEE ALSO","[az](/man/az)(1), [az-config](/man/az-config)(1), [az-account](/man/az-account)(1), [az-group](/man/az-group)(1)"]],"e":["az","az-config","az-account","az-group"],"x":"az cognitiveservices account create --name [myaccount] --resource-group [myRG] --kind [TextAnalytics] --sku [S1] --location [eastus]"},"battop":{"n":"battop","t":"Interactive battery monitor","s":[["TAGLINE","Interactive battery monitor"],["TLDR","Display **battery** information\n\n```battop```\n\nUse **SI** units instead of human-readable values\n\n```battop -u si```\n\nSet the **refresh delay** to 5 seconds\n\n```battop -d 5```"],["SYNOPSIS","**battop** [**-u** _units_] [**-d** _delay_] [**-v**]"],["DESCRIPTION","**battop** is an interactive viewer for system batteries. It displays real-time information about battery status, charge level, capacity, and power consumption in a terminal interface, drawing charts over time. Left and right arrow keys switch between batteries when more than one is present.\n\nIt runs on Linux, macOS, FreeBSD, and DragonFlyBSD."],["PARAMETERS","**-u, --units** _units_\n> Measurement units to display: **human** (default) or **si**.\n\n**-d, --delay** _seconds_\n> Delay between updates, in seconds (default **1**).\n\n**-v, --verbose**\n> Increase verbosity, may be repeated up to five times (**-vvvvv**). Log output goes to stderr."],["KEYBINDINGS","- **Left / Right arrows** - Switch between batteries\n- **q** - Quit"],["CAVEATS","On Linux it reads battery information from /sys/class/power_supply, so it only works on systems with a battery (laptops, tablets). Information accuracy depends on what the battery hardware reports."],["HISTORY","**battop** is written in Rust by svartalf and provides a modern, cross-platform battery monitoring interface."],["INSTALL","```aur: yay -S battop```"],["SEE ALSO","[acpi](/man/acpi)(1), [upower](/man/upower)(1)"],["RESOURCES","```[Source code](https://github.com/svartalf/rust-battop)```"]],"e":["acpi","upower"],"x":"battop"},"bun-upgrade":{"n":"bun-upgrade","t":"Upgrade the runtime itself","s":[["TAGLINE","Upgrade the runtime itself"],["TLDR","**Upgrade Bun** to the latest version\n\n```bun upgrade```\n\n**Upgrade to a specific version**\n\n```bun upgrade --version [1.0.0]```\n\n**Upgrade to the canary channel**\n\n```bun upgrade --canary```"],["SYNOPSIS","**bun** **upgrade** [_options_]"],["PARAMETERS","**--version** _version_\n> Upgrade to a specific version.\n\n**--canary**\n> Upgrade to the latest canary (development) build."],["DESCRIPTION","**bun upgrade** updates the **Bun** JavaScript runtime binary in place to a newer version. By default, it fetches the latest stable release from the official GitHub release feed and replaces the running binary. **--canary** switches to the rolling canary channel that tracks the development branch, useful for trying unreleased features or reproducing bug reports. **--version** pins the upgrade (or downgrade) to a specific published release.\n\nThe command does not touch project-level packages installed under **node_modules** - those are managed by **bun install**. After upgrading, **bun --version** should report the new version. To update Bun managed by a package manager (Homebrew, npm, Scoop, ...), use that manager instead so its records stay in sync."],["CAVEATS","Canary builds may contain bugs or breaking changes; production environments should pin to stable releases. The Bun binary must be writable by the current user; system-wide installs typically require **sudo** or the use of the original installer. **bun upgrade** does not migrate the **bun.lockb** lockfile format if a release introduces a new on-disk format."],["INSTALL","```pacman: sudo pacman -S bun```\n\n```brew: brew install bun```\n\n```nix: nix profile install nixpkgs#bun```"],["SEE ALSO","[bun](/man/bun)(1), [bun-info](/man/bun-info)(1), [npm](/man/npm)(1), [node](/man/node)(1)"]],"e":["bun","bun-info","npm","node"],"x":"bun upgrade"},"cardano-node":{"n":"cardano-node","t":"Core node software for the Cardano blockchain network","s":[["TAGLINE","Core node software for the Cardano blockchain network"],["TLDR","**Run a Cardano node** on mainnet\n\n```cardano-node run --topology [path/to/topology.json] --database-path [path/to/db] --socket-path [path/to/node.socket] --config [path/to/config.json]```\n\n**Run a node** with specific host and port\n\n```cardano-node run --topology [topology.json] --database-path [db/] --socket-path [node.socket] --host-addr [127.0.0.1] --port [3001] --config [config.json]```\n\n**Run a block-producing node** with keys\n\n```cardano-node run --topology [topology.json] --database-path [db/] --socket-path [node.socket] --config [config.json] --shelley-kes-key [kes.skey] --shelley-vrf-key [vrf.skey] --shelley-operational-certificate [node.cert]```\n\n**Run as a relay node** (non-producing)\n\n```cardano-node run --non-producing-node --topology [topology.json] --database-path [db/] --socket-path [node.socket] --config [config.json]```\n\n**Validate the database** before running\n\n```cardano-node run --validate-db --topology [topology.json] --database-path [db/] --socket-path [node.socket] --config [config.json]```"],["SYNOPSIS","**cardano-node** run [_options_]\n\n**cardano-node** --version\n\n**cardano-node** --help"],["PARAMETERS","**--topology** _FILEPATH_\n> Path to the topology.json file describing network peers\n\n**--database-path** _FILEPATH_\n> Directory where blockchain state is stored\n\n**--socket-path** _FILEPATH_\n> Path to the Unix domain socket for IPC communication\n\n**--config** _FILEPATH_\n> Path to the node configuration JSON file\n\n**--host-addr** _IPV4_\n> IPv4 address to bind for incoming connections\n\n**--host-ipv6-addr** _IPV6_\n> IPv6 address to bind for incoming connections\n\n**--port** _PORT_\n> Port number for listening to peer connections\n\n**--shelley-kes-key** _FILEPATH_\n> Path to the KES (Key Evolving Signature) signing key for block production\n\n**--shelley-vrf-key** _FILEPATH_\n> Path to the VRF (Verifiable Random Function) signing key\n\n**--shelley-operational-certificate** _FILEPATH_\n> Path to the operational certificate for stake pool\n\n**--non-producing-node**\n> Start as a relay node even if credentials are specified\n\n**--validate-db**\n> Validate all database files on startup\n\n**--shutdown-on-slot-synced** _SLOT_\n> Shut down after syncing to the specified slot number\n\n**--shutdown-on-block-synced** _BLOCK_\n> Shut down after syncing to the specified block number\n\n**--shutdown-ipc** _FD_\n> Shut down when the inherited file descriptor reaches EOF\n\n**-h**, **--help**\n> Display help information\n\n**--version**\n> Display version information"],["DESCRIPTION","**cardano-node** is the core component of the Cardano blockchain network, implementing the Ouroboros consensus protocol. It maintains a local copy of the blockchain, validates transactions, and participates in the peer-to-peer network.\n\nThe node can operate in two modes: as a relay node that forwards transactions and blocks, or as a block-producing node (stake pool) that creates new blocks when selected by the protocol. Block producers require KES keys, VRF keys, and an operational certificate.\n\nCommunication with the node happens through a Unix domain socket specified by **--socket-path**. The **cardano-cli** tool and other applications use the **CARDANO_NODE_SOCKET_PATH** environment variable to locate this socket for queries and transaction submission."],["CONFIGURATION","**config.json**\n> Main node configuration file specifying protocol parameters, logging, tracing, and network settings.\n\n**topology.json**\n> Defines network peers the node connects to, including relay nodes and block producers."],["CAVEATS","Running a full node requires significant disk space (over 100GB for mainnet) and memory. Initial synchronization can take many hours depending on hardware and network speed. The node must remain running and connected for stake pool operation. Configuration files must match the network (mainnet, preprod, preview) you intend to connect to."],["HISTORY","Cardano-node was developed by **Input Output Global (IOG)**, formerly IOHK, as part of the Cardano blockchain project founded by Charles Hoskinson. The project launched its mainnet in **September 2017** with the Byron era. Major upgrades include the **Shelley** era in **2020** introducing staking and decentralization, **Alonzo** in **2021** adding smart contracts, and continued development through the Basho and Voltaire eras. The node is written in **Haskell** and implements peer-reviewed cryptographic protocols."],["INSTALL","```aur: yay -S cardano-node```"],["SEE ALSO","[cardano-cli](/man/cardano-cli)(1), [cardano-wallet](/man/cardano-wallet)(1)"]],"e":["cardano-cli","cardano-wallet"],"x":"cardano-node run --topology [path/to/topology.json] --database-path [path/to/db] --socket-path [path/to/node.socket] --config [path/to/config.json]"},"ccomps":{"n":"ccomps","t":"decompose graphs into connected components","s":[["TAGLINE","decompose graphs into connected components"],["TLDR","**Print components**, each as a separate graph\n\n```ccomps -x [input.dot]```\n\n**Write each component** to a numbered file derived from outfile\n\n```ccomps -x -o [out.dot] [input.dot]```\n\n**Print only the largest component**\n\n```ccomps -z -x [input.dot]```\n\n**Count the components**, print only counts to stderr\n\n```ccomps -sv [input.dot]```\n\n**Extract the component containing a specific node**\n\n```ccomps -X [node_name] [input.dot]```\n\n**Output components by size range** (at least 5 nodes)\n\n```ccomps -x -X%5- [input.dot]```"],["SYNOPSIS","**ccomps** [**-svnxCe?**] [**-z**] [**-X**[_#%_]_v_] [**-o** _outfile_] [_files_]"],["DESCRIPTION","**ccomps** decomposes graphs into their connected components, printing the components to standard output. By default, every node ends up in some component; with **-C** the cluster subgraph structure is also used to determine connectivity.\n\nFor undirected graphs it computes connected components; for directed graphs it treats edges as undirected (weakly connected). The tool is useful for filtering disconnected subgraphs out of a graph, isolating the component containing a node of interest, or pre-processing graphs before layout with **dot**, **neato**, etc.\n\nIf no input file is given, the graph is read from standard input."],["PARAMETERS","**-s**\n> Silent: print no output graph; useful with **-v** to just check connectivity.\n\n**-x**\n> Print each connected component as a separate graph (instead of embedding them as subgraphs).\n\n**-X** _node_name_\n> Print only the component containing the named node.\n\n**-X#** _index_[**-**[_last_]]\n> Print only the component(s) at the given index, or index range (last optional).\n\n**-X%** _min_[**-**[_max_]]\n> Print only the component(s) whose node count falls in the given size range.\n\n**-z**\n> Sort components by size, largest first. Only meaningful with **-x** or **-X#**.\n\n**-C**\n> Use cluster subgraphs in addition to normal edge connectivity when computing components.\n\n**-e**\n> Do not induce edges in the connected components.\n\n**-n**\n> Do not project subgraph structure: components contain only relevant nodes and edges.\n\n**-v**\n> Verbose: print counts of nodes, edges and components to stderr.\n\n**-o** _outfile_\n> Write output to _outfile_. With multiple components, files are numbered (_outfile_, _outfile_\\_1, ...).\n\n**-?**\n> Print usage information."],["CAVEATS","Input must be in **DOT** format. For directed graphs only weakly connected components are computed. Memory usage scales with graph size. Node names must be unique for the **-X** _node_name_ form to work as expected. The **-X** flag is overloaded: a plain name, a **#**-prefixed index, or a **%**-prefixed size selector."],["HISTORY","**ccomps** is part of the **Graphviz** suite, originally developed at **AT&T Labs Research** starting in the **early 1990s** by **Stephen North**, **Eleftherios Koutsofios**, **Emden Gansner** and others. Graphviz was open-sourced in **2004** and continues to be actively maintained."],["INSTALL","```apt: sudo apt install graphviz```\n\n```dnf: sudo dnf install graphviz```\n\n```pacman: sudo pacman -S graphviz```\n\n```apk: sudo apk add graphviz```\n\n```zypper: sudo zypper install graphviz```\n\n```brew: brew install graphviz```\n\n```nix: nix profile install nixpkgs#graphviz```"],["SEE ALSO","[dot](/man/dot)(1), [neato](/man/neato)(1), [gc](/man/gc)(1), [gvpr](/man/gvpr)(1)"]],"e":["dot","neato","gc","gvpr"],"x":"ccomps -x [input.dot]"},"cec-client":{"n":"cec-client","t":"HDMI-CEC device control client","s":[["TAGLINE","HDMI-CEC device control client"],["TLDR","**List** all CEC adapters\n\n```cec-client -l```\n\nStart an **interactive** CEC session\n\n```sudo cec-client```\n\nSet the **On-Screen Display** name\n\n```sudo cec-client -o [name]```\n\n**Send a single command**\n\n```echo [on 0] | sudo cec-client -s```\n\nSet a device to **standby** (interactive mode)\n\n```standby [0]```\n\n**Turn on** a device (interactive mode)\n\n```on [0]```"],["SYNOPSIS","**cec-client** [_options_]"],["DESCRIPTION","**cec-client** manages CEC (Consumer Electronics Control) connections over HDMI using the libCEC library. CEC is a feature of the HDMI specification that allows devices connected via HDMI to control each other, enabling actions like turning on a TV when a media player starts, or controlling volume across devices with a single remote.\n\nThe tool provides both interactive and scripted control of CEC-enabled devices. In interactive mode, you can send commands like `on`, `standby`, and `tx` (raw CEC frames) to any device on the HDMI bus. For automation, the `-s` flag processes a single command from stdin and exits, making it suitable for use in scripts and home automation setups.\n\nDevices are addressed by logical address (0-15), where 0 is typically the TV, 1 is a recording device, and 5 is an audio system."],["PARAMETERS","**-l, --list-devices**\n> List available CEC adapters\n\n**-o, --osd-name** _name_\n> Set the On-Screen Display name\n\n**-s, --single-command**\n> Execute a single command from stdin and exit\n\n**-d** _device_\n> Use specific CEC device"],["INTERACTIVE COMMANDS","**on** _address_\n> Turn on device at address\n\n**standby** _address_\n> Put device into standby\n\n**tx** _bytes_\n> Transmit raw CEC frame\n\n**scan**\n> Scan the CEC bus for devices"],["CAVEATS","Requires a USB CEC adapter (like Pulse-Eight). Device addresses are 0-15 where 0 is typically the TV. Root privileges required for device access."],["INSTALL","```apt: sudo apt install cec-utils```\n\n```apk: sudo apk add libcec-tools```\n\n```zypper: sudo zypper install cec-client```"],["SEE ALSO","[cec-ctl](/man/cec-ctl)(1)"],["RESOURCES","```[Source code](https://github.com/Pulse-Eight/libcec)```"]],"e":["cec-ctl"],"x":"cec-client -l"},"checkupdates":{"n":"checkupdates","t":"safely check for Arch Linux package updates","s":[["TAGLINE","safely check for Arch Linux package updates"],["TLDR","Synchronize database and **list pending updates**\n\n```checkupdates```\n\nList pending updates **without syncing** the database\n\n```checkupdates -n```\n\nDisplay updates only if **different from last check**\n\n```checkupdates -c```\n\nList updates and **download packages** to cache\n\n```checkupdates -d```\n\nUse a **specific pacman database**\n\n```CHECKUPDATES_DB=[path/to/directory] checkupdates```"],["SYNOPSIS","**checkupdates** [_options_]"],["DESCRIPTION","**checkupdates** safely checks for pending updates in Arch Linux without requiring root privileges. It creates a temporary copy of the sync database to check for updates, avoiding any modifications to the system.\n\nPart of the pacman-contrib package, it provides a safe way to check updates that can be used in scripts and status bars."],["PARAMETERS","**-n, --nosync**\n> Don't synchronize the database, use cached data\n\n**-c, --change**\n> Only display if updates differ from last check\n\n**-d, --download**\n> Download packages to pacman cache while checking\n\n**-h, --help**\n> Display help message"],["ENVIRONMENT","**CHECKUPDATES_DB**\n> Path to use for the temporary database"],["CAVEATS","Arch Linux specific. Requires network access for database synchronization unless using --nosync. The temporary database is stored in a user-writable location."],["SEE ALSO","[pacman](/man/pacman)(8), [paru](/man/paru)(1), [yay](/man/yay)(1)"],["RESOURCES","```[Source code](https://gitlab.archlinux.org/pacman/pacman-contrib)```"]],"e":["pacman","paru","yay"],"x":"checkupdates"},"chroot":{"n":"chroot","t":"Change the root directory for a command","s":[["TAGLINE","Change the root directory for a command"],["TLDR","**Run a command with a different root directory**\n\n```sudo chroot [path/to/new_root] [command]```\n\n**Run an interactive shell in the new root**\n\n```sudo chroot [path/to/new_root]```\n\n**Specify a custom shell to run**\n\n```sudo chroot [path/to/new_root] /bin/bash```\n\n**Specify user and group for the command**\n\n```sudo chroot --userspec=[user:group] [path/to/new_root] [command]```\n\n**Change to a specific directory after chrooting**\n\n```sudo chroot [path/to/new_root] /bin/sh -c \"cd /home && ls\"```"],["SYNOPSIS","**chroot** [_OPTION_] _NEWROOT_ [_COMMAND_ [_ARG_]...]"],["DESCRIPTION","**chroot** changes the root directory of the calling process to the specified path and runs a command within that environment. All pathname lookups starting with **/** will be relative to the new root.\n\nIf no command is specified, chroot runs the shell from the **SHELL** environment variable or defaults to **/bin/sh**.\n\nThe new root directory must contain all necessary files, libraries, and device nodes required by the command being executed."],["PARAMETERS","**NEWROOT**\n> The directory to use as the new root filesystem\n\n**COMMAND**\n> Command to run in the chroot environment\n\n**ARG**\n> Arguments to pass to the command\n\n**--userspec=**_USER:GROUP_\n> Specify user and group (name or ID) to run as\n\n**--groups=**_GROUPS_\n> Specify supplementary groups\n\n**--skip-chdir**\n> Do not change working directory to /"],["CAVEATS","Requires root privileges or **CAP_SYS_CHROOT** capability. Not designed for security sandboxing—privileged users can escape by creating directories and using relative paths. Open file descriptors may provide access outside the chroot.\n\nThe new root must contain essential files: **/bin/sh** or the specified command, required shared libraries (check with **ldd**), and device nodes like **/dev/null** if needed."],["HISTORY","The **chroot** system call originated in **Version 7 Unix** (1979) at Bell Labs. It was initially used for building and testing new system versions. The GNU coreutils version provides the standard command-line interface on Linux systems."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[arch-chroot](/man/arch-chroot)(1), [pivot_root](/man/pivot_root)(8), [unshare](/man/unshare)(1)"],["RESOURCES","```[Source code](https://github.com/coreutils/coreutils)```\n\n```[Documentation](https://www.gnu.org/software/coreutils/manual/html_node/chroot-invocation.html)```"]],"e":["arch-chroot","pivot_root","unshare"],"x":"sudo chroot [path/to/new_root] [command]"},"conda-compare":{"n":"conda-compare","t":"compare environments against specification files","s":[["TAGLINE","compare environments against specification files"],["TLDR","**Compare two environments**\n\n```conda compare [env_name] [environment.yml]```\n\n**Compare current environment** with a spec file\n\n```conda compare [environment.yml]```"],["SYNOPSIS","**conda** **compare** [_env_name_] _file_"],["DESCRIPTION","**conda compare** analyzes the difference between an installed conda environment and an environment specification file (typically environment.yml), reporting discrepancies in package versions, missing packages, or extra packages not specified in the file. This is valuable for environment validation and drift detection.\n\nOver time, conda environments can drift from their original specifications through manual package installations, updates, or dependency resolution changes. The compare command helps identify when an environment no longer matches its documented specification, which can be critical for reproducibility in scientific computing and data science workflows where environment consistency is essential.\n\nThe tool is particularly useful in collaborative settings where environment.yml files are version controlled and shared among team members. By comparing the current environment against the canonical specification, developers can verify they're working in the correct environment state or identify when an environment needs to be rebuilt from the specification to ensure consistency across the team."],["INSTALL","```dnf: sudo dnf install conda```\n\n```nix: nix profile install nixpkgs#conda```"],["SEE ALSO","[conda](/man/conda)(1), [conda-env](/man/conda-env)(1), [conda-list](/man/conda-list)(1)"],["RESOURCES","```[Source code](https://github.com/conda/conda)```\n\n```[Documentation](https://docs.conda.io/projects/conda/en/stable/commands/compare.html)```"]],"e":["conda","conda-env","conda-list"],"x":"conda compare [env_name] [environment.yml]"},"crane-mutate":{"n":"crane-mutate","t":"modify image metadata without rebuilding","s":[["TAGLINE","modify image metadata without rebuilding"],["TLDR","**Add labels** to an image\n\n```crane mutate [image] --label [key=value] -t [new:tag]```\n\n**Set entrypoint**\n\n```crane mutate [image] --entrypoint [/app/start] -t [new:tag]```\n\n**Add environment variables**\n\n```crane mutate [image] --env [KEY=value] -t [new:tag]```"],["SYNOPSIS","**crane** **mutate** [_options_] _image_"],["PARAMETERS","**-t**, **--tag** _tag_\n> Tag for the mutated image.\n\n**--label** _key=value_\n> Add or override a label.\n\n**--entrypoint** _cmd_\n> Set the entrypoint.\n\n**--cmd** _cmd_\n> Set the command.\n\n**--env** _key=value_\n> Add environment variable.\n\n**--user** _user_\n> Set the user.\n\n**--workdir** _dir_\n> Set the working directory."],["DESCRIPTION","**crane mutate** modifies container image configuration without rebuilding. It can change labels, entrypoint, environment variables, and other metadata."],["INSTALL","```pacman: sudo pacman -S crane```\n\n```apk: sudo apk add crane```\n\n```zypper: sudo zypper install crane```\n\n```brew: brew install crane```\n\n```nix: nix profile install nixpkgs#crane```"],["SEE ALSO","[crane](/man/crane)(1), [crane-append](/man/crane-append)(1)"],["RESOURCES","```[Source code](https://github.com/google/go-containerregistry)```\n\n```[Documentation](https://github.com/google/go-containerregistry/blob/main/cmd/crane/doc/crane_mutate.md)```"]],"e":["crane","crane-append"],"x":"crane mutate [image] --label [key=value] -t [new:tag]"},"csv2tsv":{"n":"csv2tsv","t":"CSV to tab-separated values converter","s":[["TAGLINE","CSV to tab-separated values converter"],["TLDR","**Convert CSV to TSV**\n\n```csv2tsv [input.csv] > [output.tsv]```\n\n**Convert from stdin**\n\n```cat [input.csv] | csv2tsv > [output.tsv]```\n\n**Convert input using a custom field delimiter** (e.g. semicolon)\n\n```csv2tsv -c \";\" [input.csv]```\n\n**Concatenate multiple files**, keeping only the first header line\n\n```csv2tsv -H [file1.csv] [file2.csv] > [combined.tsv]```\n\n**Choose the replacement** for tabs and newlines embedded in fields\n\n```csv2tsv --tab-replacement \" \" --newline-replacement \" \" [input.csv]```"],["SYNOPSIS","**csv2tsv** [_options_] [_file_...]"],["PARAMETERS","_FILE_\n> Input CSV file(s). Reads from stdin if none are given or if **-** is specified.\n\n**-H**, **--header**\n> Treat the first line of each file as a header. When concatenating multiple files, only the header of the first file is written.\n\n**-c** _CHR_, **--csv-delim** _CHR_\n> Field delimiter in the CSV input (default: comma).\n\n**-t** _CHR_, **--tsv-delim** _CHR_\n> Field delimiter in the TSV output (default: TAB).\n\n**-r** _STR_, **--tab-replacement** _STR_\n> String to substitute for TAB characters found inside fields (default: space).\n\n**-n** _STR_, **--newline-replacement** _STR_\n> String to substitute for newline characters found inside fields (default: space).\n\n**-q** _CHR_, **--quote** _CHR_\n> Quoting character in the CSV input (default: double quote).\n\n**--help**\n> Display help information.\n\n**--version**\n> Display version information."],["DESCRIPTION","**csv2tsv** converts comma-separated values (CSV) files to tab-separated values (TSV) format. TSV is often easier to process with Unix tools like cut, awk, and sort since tab and newline characters never appear inside fields.\n\nThe tool follows RFC 4180 CSV quoting rules: quoted fields may contain commas, newlines, and escaped quotes. During conversion, any TAB or newline characters found inside a field are replaced (by a space by default) so the resulting TSV has exactly one record per line and a fixed number of columns. The surrounding quotes are removed.\n\nAll three common newline conventions in the input (CR, CRLF, LF) are detected automatically, and output is always written with Unix (LF) newlines.\n\ncsv2tsv is part of eBay's tsv-utils suite and is commonly used as a preprocessing step before applying the suite's other tools or standard Unix text utilities."],["CAVEATS","By design, TAB and newline characters inside fields are replaced rather than escaped, so the conversion is not strictly reversible. Very large files are processed in a streaming fashion for memory efficiency. The tool assumes UTF-8 compatible input."],["HISTORY","csv2tsv is part of **eBay's tsv-utils**, a suite of command-line tools for manipulating large tabular data files written in the D programming language by Jon Degenhardt. The suite was open-sourced by eBay in 2017 and is optimized for speed on large datasets."],["SEE ALSO","[csvkit](/man/csvkit)(1), [cut](/man/cut)(1), [miller](/man/miller)(1), [awk](/man/awk)(1)"],["RESOURCES","```[Source code](https://github.com/eBay/tsv-utils)```\n\n```[Documentation](https://github.com/eBay/tsv-utils/blob/master/docs/tool_reference/csv2tsv.md)```"]],"e":["csvkit","cut","miller","awk"],"x":"csv2tsv [input.csv] > [output.tsv]"},"deallocvt":{"n":"deallocvt","t":"deallocate unused virtual terminal memory","s":[["TAGLINE","deallocate unused virtual terminal memory"],["TLDR","**Deallocate all unused virtual terminals**\n\n```sudo deallocvt```\n\n**Deallocate a specific virtual terminal**\n\n```sudo deallocvt [7]```\n\n**Deallocate a range of virtual terminals**\n\n```sudo deallocvt [7] [8] [9]```\n\n**Target a specific console device**\n\n```sudo deallocvt -C [/dev/tty1]```\n\n**Show version information**\n\n```deallocvt -V```"],["SYNOPSIS","**deallocvt** [_options_] [_N_...]"],["PARAMETERS","_N_\n> Virtual terminal number(s) to deallocate. Without arguments, all unused terminals are freed.\n\n**-C**, **--console=**_DEV_\n> Operate on the specified console device (e.g. `/dev/tty1`).\n\n**-V**, **--version**\n> Print version information and exit.\n\n**-h**, **--help**\n> Print usage information and exit."],["DESCRIPTION","**deallocvt** deallocates kernel memory and data structures for unused virtual consoles. A virtual console is considered unused when it is not the active console and no process or text selection is referring to it.\n\nWhen invoked without arguments, it deallocates all unused virtual terminals. When given specific terminal numbers (corresponding to `/dev/ttyN`), it deallocates only those. A terminal cannot be deallocated if it is the current terminal or if it has active processes."],["CAVEATS","Requires root privileges. Cannot deallocate the current VT or VTs with active processes. Deallocating a VT means it will be reallocated the next time it is used, with a small overhead. Mostly useful on memory-constrained systems."],["HISTORY","**deallocvt** is part of the **kbd** package for Linux console utilities and was originally authored by **Andries Brouwer**. It provides low-level management of Linux virtual terminal resources."],["INSTALL","```apt: sudo apt install kbd```\n\n```dnf: sudo dnf install kbd```\n\n```pacman: sudo pacman -S kbd```\n\n```apk: sudo apk add kbd```\n\n```zypper: sudo zypper install kbd```\n\n```nix: nix profile install nixpkgs#kbd```"],["SEE ALSO","[chvt](/man/chvt)(1), [fgconsole](/man/fgconsole)(1)"]],"e":["chvt","fgconsole"],"x":"sudo deallocvt"},"docker-compose-logs":{"n":"docker-compose-logs","t":"view compose service logs","s":[["TAGLINE","view compose service logs"],["TLDR","**View logs** from all services\n\n```docker compose logs```\n\n**Follow log output**\n\n```docker compose logs -f```\n\n**View logs** for specific service\n\n```docker compose logs [service_name]```\n\n**Show timestamps**\n\n```docker compose logs -t```\n\n**Limit output lines**\n\n```docker compose logs --tail [100]```\n\n**Since specific time**\n\n```docker compose logs --since [1h]```"],["SYNOPSIS","**docker** **compose** **logs** [_options_] [_service..._]"],["PARAMETERS","**-f**, **--follow**\n> Follow log output.\n\n**-t**, **--timestamps**\n> Show timestamps.\n\n**--tail** _n_\n> Number of lines from end.\n\n**--since** _time_\n> Show logs since timestamp or relative time.\n\n**--until** _time_\n> Show logs before timestamp or relative time.\n\n**--no-color**\n> Produce monochrome output."],["DESCRIPTION","**docker compose logs** displays log output from services defined in the Compose file. Aggregates logs from all containers of each service."],["INSTALL","```apt: sudo apt install docker-compose```\n\n```dnf: sudo dnf install docker-compose```\n\n```pacman: sudo pacman -S docker-compose```\n\n```apk: sudo apk add docker-cli-compose```\n\n```zypper: sudo zypper install docker-compose```\n\n```brew: brew install docker-compose```\n\n```nix: nix profile install nixpkgs#docker-compose```"],["SEE ALSO","[docker-compose](/man/docker-compose)(1), [docker-logs](/man/docker-logs)(1)"],["RESOURCES","```[Source code](https://github.com/docker/compose)```\n\n```[Documentation](https://docs.docker.com/reference/cli/docker/compose/)```"]],"e":["docker-compose","docker-logs"],"x":"docker compose logs"},"doctl-kubernetes-cluster":{"n":"doctl-kubernetes-cluster","t":"manage DigitalOcean Kubernetes clusters","s":[["TAGLINE","manage DigitalOcean Kubernetes clusters"],["TLDR","**List Kubernetes clusters**\n\n```doctl kubernetes cluster list```\n\n**Create a cluster**\n\n```doctl kubernetes cluster create [name] --region [nyc1] --node-pool \"name=pool;size=s-2vcpu-4gb;count=3\"```\n\n**Get cluster info**\n\n```doctl kubernetes cluster get [cluster_id]```\n\n**Delete a cluster**\n\n```doctl kubernetes cluster delete [cluster_id]```\n\n**Get kubeconfig**\n\n```doctl kubernetes cluster kubeconfig save [cluster_id]```\n\n**Upgrade cluster version**\n\n```doctl kubernetes cluster upgrade [cluster_id] --version [1.28]```"],["SYNOPSIS","**doctl** **kubernetes** **cluster** _command_ [_options_]"],["SUBCOMMANDS","**list**\n> List clusters.\n\n**create**\n> Create a cluster.\n\n**get**\n> Get cluster details.\n\n**delete**\n> Delete a cluster.\n\n**kubeconfig**\n> Manage kubeconfig.\n\n**upgrade**\n> Upgrade cluster version.\n\n**node-pool**\n> Manage node pools."],["DESCRIPTION","**doctl kubernetes cluster** manages DigitalOcean Kubernetes (DOKS) clusters. It provides complete lifecycle management for managed Kubernetes deployments including creation, configuration, upgrades, and deletion.\n\nThe command handles cluster provisioning with specified node pools, configures kubeconfig files for kubectl access, and manages version upgrades. DOKS abstracts control plane management, allowing users to focus on application deployment while DigitalOcean handles cluster infrastructure, etcd backups, and control plane updates.\n\nCluster operations include scaling node pools, managing worker node composition, and performing in-place Kubernetes version upgrades. The kubeconfig subcommand integrates clusters with local kubectl tooling for seamless cluster access."],["SEE ALSO","[kubectl](/man/kubectl)(1)"]],"e":["kubectl"],"x":"doctl kubernetes cluster list"},"dolt-add":{"n":"dolt-add","t":"stage table changes for commit","s":[["TAGLINE","stage table changes for commit"],["TLDR","**Stage all changes**\n\n```dolt add .```\n\n**Stage a specific table**\n\n```dolt add [table_name]```\n\n**Stage multiple tables**\n\n```dolt add [table1] [table2]```\n\n**Stage everything**, equivalent to `.`\n\n```dolt add -A```\n\n**Stage a table that is ignored** by `dolt_ignore`\n\n```dolt add --force [table_name]```"],["SYNOPSIS","**dolt** **add** [_table_...]"],["PARAMETERS","**-A**, **--all**\n> Stage any and all changes (adds, deletes, and modifications), except for ignored tables.\n\n**-f**, **--force**\n> Allow adding otherwise ignored tables."],["DESCRIPTION","**dolt add** stages table changes for the next commit. Where `git add` operates on files, `dolt add` stages database tables whose rows or schema have been modified, added, or dropped.\n\nThe command marks tables with pending changes to be included in the next commit. Staging allows selective committing across multiple tables, the same index concept as Git's, applied to schema and data modifications instead of file contents.\n\nNaming tables explicitly stages only those; `.` or **-A** stages every modified table in the working set."],["CAVEATS","Tables matched by the `dolt_ignore` system table are skipped even by **-A**; use **--force** to stage one anyway. Staging is only meaningful for the CLI workflow: writes made through `dolt sql-server` land directly in the working set, and depending on the session's `@@dolt_transaction_commit` setting they may be committed without ever passing through the staging area."],["INSTALL","```pacman: sudo pacman -S dolt```\n\n```brew: brew install dolt```\n\n```nix: nix profile install nixpkgs#dolt```"],["SEE ALSO","[dolt](/man/dolt)(1), [dolt-commit](/man/dolt-commit)(1), [dolt-status](/man/dolt-status)(1), [git-add](/man/git-add)(1)"],["RESOURCES","```[Source code](https://github.com/dolthub/dolt)```\n\n```[Documentation](https://www.dolthub.com/docs/cli-reference/cli/)```"]],"e":["dolt","dolt-commit","dolt-status","git-add"],"x":"dolt add ."},"dpkg-buildpackage":{"n":"dpkg-buildpackage","t":"Debian package build tool","s":[["TAGLINE","Debian package build tool"],["TLDR","**Build package**\n\n```dpkg-buildpackage```\n\n**Build unsigned**\n\n```dpkg-buildpackage -us -uc```\n\n**Build binary only**\n\n```dpkg-buildpackage -b```\n\n**Build source only**\n\n```dpkg-buildpackage -S```\n\n**Build with parallelism**\n\n```dpkg-buildpackage -j[4]```\n\n**Build without cleaning**\n\n```dpkg-buildpackage -nc```\n\n**Specify key for signing**\n\n```dpkg-buildpackage -k[keyid]```"],["SYNOPSIS","**dpkg-buildpackage** [_options_]"],["DESCRIPTION","**dpkg-buildpackage** builds Debian packages from source. It orchestrates the full build process including extracting source, running the build system, and creating .deb files.\n\nThe tool handles dependencies, runs debian/rules targets, calls dpkg-source for source packages, and dpkg-genchanges for upload files. It's the standard way to build Debian packages."],["PARAMETERS","**-b**\n> Binary-only build.\n\n**-S**\n> Source-only build.\n\n**-F**\n> Full build (source and binary).\n\n**-us**\n> Don't sign source package.\n\n**-uc**\n> Don't sign changes file.\n\n**-j** _n_\n> Parallel jobs.\n\n**-nc**\n> Don't clean before build.\n\n**-tc**\n> Clean after build.\n\n**-k** _keyid_\n> GPG key for signing.\n\n**-r** _cmd_\n> Use cmd to gain root.\n\n**-d**\n> Don't check dependencies.\n\n**--build=** _type_\n> Specify build type."],["CAVEATS","Requires build dependencies installed. Signing needs GPG key. Must run in package source directory. Clean environment recommended for reproducible builds."],["HISTORY","**dpkg-buildpackage** is part of the **dpkg** suite, the core of Debian package management since **1994**. It has evolved with Debian's packaging practices, adding features for source formats, signing, and cross-compilation over the years."],["INSTALL","```dnf: sudo dnf install dpkg-dev```\n\n```apk: sudo apk add dpkg-dev```"],["SEE ALSO","[dpkg](/man/dpkg)(1), [dpkg-deb](/man/dpkg-deb)(1), [debuild](/man/debuild)(1), [pbuilder](/man/pbuilder)(1)"]],"e":["dpkg","dpkg-deb","debuild","pbuilder"],"x":"dpkg-buildpackage"},"eksctl":{"n":"eksctl","t":"AWS EKS cluster management CLI","s":[["TAGLINE","AWS EKS cluster management CLI"],["TLDR","**Create EKS cluster**\n\n```eksctl create cluster --name [cluster_name]```\n\n**Create with node** configuration\n\n```eksctl create cluster --name [name] --nodes [3] --node-type [t3.medium]```\n\n**List clusters**\n\n```eksctl get clusters```\n\n**Delete cluster**\n\n```eksctl delete cluster --name [cluster_name]```\n\n**Create nodegroup**\n\n```eksctl create nodegroup --cluster [name] --name [ng-name]```\n\n**Scale nodegroup**\n\n```eksctl scale nodegroup --cluster [name] --name [ng-name] --nodes [5]```\n\n**Update kubeconfig**\n\n```eksctl utils write-kubeconfig --cluster [name]```\n\n**Create a cluster from a config file**\n\n```eksctl create cluster -f [cluster.yaml]```\n\n**Upgrade the control plane** to a new Kubernetes version\n\n```eksctl upgrade cluster --name [name] --version [1.31] --approve```"],["SYNOPSIS","**eksctl** _command_ [_options_]"],["PARAMETERS","_COMMAND_\n> Operation: create, get, delete, scale, upgrade, etc.\n\n**create cluster** **--name** _NAME_\n> Create new EKS cluster.\n\n**--nodes** _N_\n> Number of worker nodes.\n\n**--node-type** _TYPE_\n> EC2 instance type for nodes.\n\n**--region** _REGION_\n> AWS region.\n\n**-f**, **--config-file** _FILE_\n> Declarative YAML cluster configuration; most flags have an equivalent field.\n\n**--dry-run**\n> Print the config file eksctl would apply instead of creating anything.\n\n**delete cluster** **--name** _NAME_\n> Delete cluster.\n\n**get clusters**\n> List clusters.\n\n**--help**\n> Display help information."],["DESCRIPTION","**eksctl** is the official CLI for Amazon EKS (Elastic Kubernetes Service). It simplifies creating, managing, and deleting EKS clusters and their associated resources.\n\nThe tool handles the complex AWS infrastructure setup including VPCs, subnets, security groups, and IAM roles. Clusters can be defined declaratively with YAML configurations.\n\neksctl supports advanced features like managed node groups, Fargate profiles, and cluster upgrades."],["CAVEATS","AWS credentials required. Cluster creation takes time. Incurs AWS charges. Some operations require specific IAM permissions."],["HISTORY","eksctl was created by **Weaveworks** in partnership with AWS, released in **2018** to simplify EKS cluster management. It became the official CLI tool for EKS. After Weaveworks wound down in 2024, the project moved to the community-run **eksctl-io** organization, where it continues to be maintained."],["INSTALL","```pacman: sudo pacman -S eksctl```\n\n```zypper: sudo zypper install eksctl```\n\n```brew: brew install eksctl```\n\n```nix: nix profile install nixpkgs#eksctl```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [aws](/man/aws)(1), [helm](/man/helm)(1)"],["RESOURCES","```[Source code](https://github.com/eksctl-io/eksctl)```\n\n```[Homepage](https://eksctl.io/)```"]],"e":["kubectl","aws","helm"],"x":"eksctl create cluster --name [cluster_name]"},"feedreader":{"n":"feedreader","t":"Desktop RSS/Atom news aggregator","s":[["TAGLINE","Desktop RSS/Atom news aggregator"],["TLDR","Print count of **unread** articles\n\n```feedreader --unreadCount```\n\n**Add** a feed URL\n\n```feedreader --addFeed=[feed_url]```\n\n**Grab** a specific article\n\n```feedreader --grabArticle=[article_url]```\n\nDownload **images** from article\n\n```feedreader --url=[feed_url] --grabImages=[article_path]```\n\n**Play** media from URL\n\n```feedreader --playMedia=[article_url]```\n\n**Run** with verbose debug output\n\n```feedreader --verbose```"],["SYNOPSIS","**feedreader** [_options_]"],["DESCRIPTION","**feedreader** is a GTK desktop RSS/Atom feed client. It aggregates news feeds from multiple sources and provides desktop notifications for new articles. The application supports various feed services including Feedly, Inoreader, and local feed reading, with features like article synchronization, tagging, and full-text search.\n\nNote: FeedReader is no longer being maintained. Users are encouraged to migrate to actively developed alternatives like NewsFlash."],["PARAMETERS","**--unreadCount**\n> Show current count of unread articles in the database.\n\n**--addFeed**=_URL_\n> Add the feed to the collection.\n\n**--grabArticle**=_URL_\n> Use the ContentGrabber to grab the given URL.\n\n**--grabImages**=_PATH_\n> Download all images of the html-document.\n\n**--url**=_URL_\n> URL of the article needed for grabImages.\n\n**--playMedia**=_URL_\n> Start media player with the given URL.\n\n**--ping**=_URL_\n> Test the ping function with the given URL.\n\n**--verbose**\n> Output all debug information.\n\n**--about**\n> Show the about dialog.\n\n**--version**\n> Show FeedReader version number."],["CAVEATS","Project is no longer maintained. Consider alternatives like NewsFlash. Supports various feed services like Feedly and Inoreader."],["INSTALL","```aur: yay -S feedreader```"],["SEE ALSO","[newsboat](/man/newsboat)(1)"]],"e":["newsboat"],"x":"feedreader --unreadCount"},"fixmynpm":{"n":"fixmynpm","t":"Audit and repair insecure npm configuration files","s":[["TAGLINE","Audit and repair insecure npm configuration files"],["TLDR","**Check the global ~/.npmrc** for security and configuration issues\n\n```fixmynpm doctor```\n\n**Scan a directory tree** for every .npmrc file and report problems\n\n```fixmynpm audit --root [path/to/projects]```\n\n**Apply recommended fixes** to .npmrc files\n\n```fixmynpm fixer --root [path/to/projects]```\n\nPreview the changes **without writing** anything (dry run)\n\n```fixmynpm fixer --root [path/to/projects] --dry-run```\n\n**Hunt for a specific package** across all node_modules (incident response)\n\n```fixmynpm scan --package \"[lodash@4.17.20]\" --incident```\n\nFind .npmrc files matching a **glob pattern**\n\n```fixmynpm scan --root [~/code] --npmrc```"],["SYNOPSIS","**fixmynpm** _command_ [_options_]"],["DESCRIPTION","**fixmynpm** is a diagnostic CLI for finding and fixing insecure or misconfigured **.npmrc** files. It inspects the global **~/.npmrc**, walks project directories for nested **.npmrc** files, audits each entry against a built-in catalogue of security rules, and can apply fixes automatically. Built-in checks cover registry validation, TLS settings, authentication-token handling, lifecycle-script execution, and dependency-confusion risks.\n\nIn addition to configuration hygiene, **fixmynpm** doubles as an incident-response tool: the **scan** command can recurse through **node_modules** to locate specific compromised packages by name, version range, or bundled-.npmrc presence, making it useful when responding to supply-chain advisories."],["PARAMETERS","**doctor**\n> Inspect ~/.npmrc and report security or configuration violations with suggested fixes.\n\n**audit**\n> Recursively scan a directory tree, listing every .npmrc file and the issues found in each.\n\n**fixer**\n> Apply recommended corrections to .npmrc files discovered during a scan.\n\n**scan**\n> Locate .npmrc files or search node_modules for specific packages (incident-response mode).\n\n**--root** _dir_\n> Starting directory for scan, audit, and fixer (defaults to the user's home directory).\n\n**--package** _glob_\n> Filter scan results to packages matching a name or glob pattern.\n\n**--version** _range_\n> Filter scan results by semver range when combined with **--package**.\n\n**--npmrc**\n> Limit scan output to packages that bundle a .npmrc file.\n\n**--incident**\n> Enable incident-response heuristics, including bundled .npmrc detection inside node_modules.\n\n**--dry-run**\n> Show planned changes without modifying any files."],["CAVEATS","Operates on **.npmrc** files only; it does not modify **package.json**, lockfiles, or installed binaries. The fixer rewrites files in place — pair it with **--dry-run** or version control before running broadly. Incident-response scans can be slow on large workstations because they recurse through every **node_modules** directory below the chosen root."],["HISTORY","**fixmynpm** was created by **madhugb** and announced on **Hacker News in 2026** following a series of npm supply-chain incidents that exposed dangerous defaults in user **.npmrc** files. It is written in **Go** and distributed via **go install github.com/madhugb/fixmynpm@latest**."],["SEE ALSO","[npm](/man/npm)(1), [npm-audit](/man/npm-audit)(1), [yarn](/man/yarn)(1), [pnpm](/man/pnpm)(1)"]],"e":["npm","npm-audit","yarn","pnpm"],"x":"fixmynpm doctor"},"flatpak-remote-info":{"n":"flatpak-remote-info","t":"query information about remote Flatpak packages","s":[["TAGLINE","query information about remote Flatpak packages"],["TLDR","Show **information** about a flatpak in a remote\n\n```flatpak remote-info [remote_name] [com.example.app]```\n\nShow a **log of previous versions** in a remote\n\n```flatpak remote-info --log [remote_name] [com.example.app]```\n\nShow information about a **specific commit**\n\n```flatpak remote-info --commit [COMMIT_HASH] [remote_name] [com.example.app]```\n\nShow only the **commit ID** (machine-readable)\n\n```flatpak remote-info --show-commit [remote_name] [com.example.app]```\n\nShow the **metadata** of a remote application\n\n```flatpak remote-info --show-metadata [remote_name] [com.example.app]```\n\nShow information for a specific **architecture**\n\n```flatpak remote-info --arch [x86_64] [remote_name] [com.example.app]```"],["SYNOPSIS","**flatpak remote-info** [_options_] _remote_ _ref_"],["PARAMETERS","**--user**\n> Use per-user configuration\n\n**--system**\n> Use default system-wide configuration\n\n**--installation** _name_\n> Use specified system-wide installation\n\n**--cached**\n> Use locally cached data when available\n\n**--runtime**\n> Assume ref is a runtime\n\n**--app**\n> Assume ref is an application\n\n**--arch** _arch_\n> Target specific architecture\n\n**--commit** _commit_\n> Show information for a specific commit\n\n**--log**\n> Display version history\n\n**-r, --show-ref**\n> Display the matched ref (machine-readable)\n\n**-c, --show-commit**\n> Display the commit ID (machine-readable)\n\n**-p, --show-parent**\n> Display the parent commit\n\n**-m, --show-metadata**\n> Display the metadata\n\n**-v, --verbose**\n> Output debug information"],["DESCRIPTION","**Flatpak remote-info** displays details about an application or runtime available in a remote repository. It shows information such as the application ID, architecture, branch, commit hash, download size, installed size, and runtime dependencies.\n\nBy default, output is in human-readable format. Using **--show-*** options switches to machine-readable output suitable for scripting.\n\nThe **--log** option is useful for viewing available versions and finding specific commits for downgrade operations or version pinning."],["CAVEATS","Requires network access to query remote repositories unless **--cached** is specified. The cached option only works if the repository metadata has been previously fetched. Some information may not be available for all remotes or refs."],["HISTORY","Part of the **Flatpak** project developed by Alexander Larsson at Red Hat. Flatpak evolved from the xdg-app project around **2015** and has become a leading universal package format for Linux desktop applications."],["SEE ALSO","[flatpak](/man/flatpak)(1), [flatpak-update](/man/flatpak-update)(1), [flatpak-install](/man/flatpak-install)(1)"]],"e":["flatpak","flatpak-update","flatpak-install"],"x":"flatpak remote-info [remote_name] [com.example.app]"},"fstab":{"n":"fstab","t":"filesystem mount configuration file","s":[["TAGLINE","filesystem mount configuration file"],["TLDR","**View fstab**\n\n```cat /etc/fstab```\n\n**Check fstab syntax**\n\n```sudo mount -a```\n\n**Show mounted filesystems**\n\n```findmnt```"],["DESCRIPTION","**/etc/fstab** is a configuration file that defines how filesystems are mounted at boot time. Each line describes a filesystem, mount point, type, options, and backup/check settings.\n\nThe file is read by **mount** and **systemd** to automatically mount filesystems during system startup."],["FILE FORMAT","```\n<device>        <mountpoint>  <type>  <options>           <dump> <pass>\n/dev/sda1       /boot         ext4    defaults            0      2\nUUID=abc123     /home         ext4    defaults,noatime    0      2\n/dev/sda3       none          swap    sw                  0      0\n//server/share  /mnt/share    cifs    credentials=/etc/creds  0  0\n```"],["FIELDS","**device**\n> Device, UUID, LABEL, or network path.\n\n**mountpoint**\n> Where to mount (directory or \"none\" for swap).\n\n**type**\n> Filesystem type (ext4, xfs, btrfs, nfs, cifs, swap).\n\n**options**\n> Mount options, comma-separated.\n\n**dump**\n> Backup flag (usually 0).\n\n**pass**\n> fsck order (0=skip, 1=root, 2=other)."],["COMMON OPTIONS","```\ndefaults    - rw,suid,dev,exec,auto,nouser,async\nnoauto      - Don't mount at boot\nnofail      - Don't fail boot if device missing\nro          - Read-only\nnoexec      - Don't allow execution\nnosuid      - Ignore setuid bits\nnoatime     - Don't update access times\nuser        - Allow non-root users to mount\n```"],["CAVEATS","Errors can prevent boot. Always test with `mount -a` before reboot. Use UUID or LABEL for reliable device identification. Network mounts need special handling."],["SEE ALSO","[mount](/man/mount)(8), [findmnt](/man/findmnt)(8), [blkid](/man/blkid)(8)"]],"e":["mount","findmnt","blkid"],"x":"cat /etc/fstab"},"git-authors":{"n":"git-authors","t":"list all contributors to a repository","s":[["TAGLINE","list all contributors to a repository"],["TLDR","**Print** the author list to standard output\n\n```git authors --list```\n\n**Print** the author list without email addresses\n\n```git authors --list --no-email```\n\n**Append** authors to the repository's AUTHORS file and open it in your editor\n\n```git authors```\n\n**Append** authors to a specific file\n\n```git authors [CONTRIBUTORS]```\n\n**Save** the list to a file yourself\n\n```git authors --list > [AUTHORS]```"],["SYNOPSIS","**git authors** [**--list**] [**--no-email**] [_file_]"],["PARAMETERS","**-l**, **--list**\n> Print the author list to standard output instead of writing it to a file.\n\n**--no-email**\n> Omit email addresses, leaving only names.\n\n_file_\n> File to append to. Ignored with **--list**. Defaults to an existing file matching **\\*authors\\*** or **\\*contributors\\*** in the repository root, or **AUTHORS** if none is found."],["DESCRIPTION","**git authors** collects the unique contributors to a repository from its commit history, sorted by number of commits in descending order. It is part of **git-extras**, a collection of git utilities not shipped with core git.\n\nThe default behaviour is not to print. Run bare, **git authors** **appends** the list to an AUTHORS file and then opens that file in your git editor, which is the workflow the tool was built for: maintaining a credits file in a project. **--list** is the option you want when you just need the names on standard output or in a pipeline.\n\nUnderneath it is a wrapper around **git shortlog -sne**, with awk passes that strip the commit counts and de-duplicate authors by name and by email."],["CAVEATS","Bare **git authors** **appends** rather than overwrites, so running it twice duplicates every entry in the AUTHORS file. It also opens an editor, which makes it unsuitable for scripts and CI; use **--list** there.\n\nIt picks its target file by globbing the repository root for anything matching **\\*authors\\*** or **\\*contributors\\***, case-insensitively, so it can append to an unexpected file in a repository with several such names.\n\nAuthors come from each commit's author field, so contributors who used different names or addresses on different machines appear multiple times. De-duplication is by exact name and exact email; use a **.mailmap** file with **git shortlog** for real canonicalization.\n\n**--no-email** de-duplicates on email first and then drops the addresses, so two distinct people sharing a display name can collapse into one entry."],["HISTORY","**git authors** is part of **git-extras**, created by **TJ Holowaychuk** in **2010** to bundle common git helper scripts. The project is now community-maintained."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-shortlog](/man/git-shortlog)(1), [git-log](/man/git-log)(1), [git-extras](/man/git-extras)(1), [git-blame](/man/git-blame)(1)"],["RESOURCES","```[Source code](https://github.com/tj/git-extras)```\n\n```[Documentation](https://github.com/tj/git-extras/blob/master/Commands.md#git-authors)```"]],"e":["git-shortlog","git-log","git-extras","git-blame"],"x":"git authors --list"},"gosec":{"n":"gosec","t":"Go source code security scanner","s":[["TAGLINE","Go source code security scanner"],["TLDR","**Scan** the current package and everything below it\n\n```gosec ./...```\n\n**Scan** a specific package or directory\n\n```gosec [path/to/package]```\n\n**Write a report** in a given format (json, yaml, csv, html, sarif, ...)\n\n```gosec -fmt [format] -out [path/to/report] ./...```\n\n**Run only** specific rules by ID\n\n```gosec -include [G101,G201] ./...```\n\n**Exclude** specific rules by ID\n\n```gosec -exclude [G104] ./...```\n\n**Skip** one or more directories\n\n```gosec -exclude-dir [vendor] ./...```\n\n**Filter** findings by severity and confidence\n\n```gosec -severity [medium] -confidence [medium] ./...```\n\n**Include** test files in the scan\n\n```gosec -tests ./...```"],["SYNOPSIS","**gosec** [_options_] _package_..."],["DESCRIPTION","**gosec** (Go Security Checker) inspects Go source code for security problems by analyzing the abstract syntax tree (AST) of each package against a set of built-in rules. Each rule has an identifier of the form **Gnnn** (for example **G101** for hardcoded credentials or **G401** for use of weak cryptographic primitives) and reports the file, line, severity, and confidence of every match.\n\nIt is designed to run in continuous-integration pipelines: results can be emitted in machine-readable formats such as JSON, YAML, CSV, SARIF, and JUnit XML, and the exit status can be made non-zero when issues are found. Rule selection, severity and confidence thresholds, directory exclusions, and inline **#nosec** annotations let you tune the signal to a project's needs."],["PARAMETERS","**-fmt** _format_\n\n> Set the output format: text (default), json, yaml, csv, junit-xml, html, sonarqube, golint, sarif.\n\n**-out** _file_\n\n> Write the report to the given file instead of standard output.\n\n**-include** _ids_\n\n> Comma-separated list of rule IDs to run exclusively.\n\n**-exclude** _ids_\n\n> Comma-separated list of rule IDs to skip.\n\n**-exclude-dir** _dir_\n\n> Exclude a directory from the scan; may be repeated.\n\n**-severity** _level_\n\n> Report only issues at the given severity or higher (low, medium, high).\n\n**-confidence** _level_\n\n> Report only issues at the given confidence or higher (low, medium, high).\n\n**-tests**\n\n> Include Go test files (**_test.go**) in the analysis.\n\n**-no-fail**\n\n> Always exit with status 0, even when issues are found."],["CAVEATS","gosec performs static analysis and can produce false positives; use **#nosec** comments or rule exclusions to silence findings you have reviewed. It complements, but does not replace, runtime testing and dependency vulnerability scanning such as govulncheck."],["HISTORY","gosec started life as **gas** (Go AST Scanner) and was later renamed. It is maintained by the **securego** community project and is widely integrated into Go CI tooling, including golangci-lint."],["INSTALL","```dnf: sudo dnf install gosec```\n\n```apk: sudo apk add gosec```\n\n```zypper: sudo zypper install gosec```\n\n```brew: brew install gosec```\n\n```nix: nix profile install nixpkgs#gosec```"],["SEE ALSO","[golangci-lint](/man/golangci-lint)(1), [staticcheck](/man/staticcheck)(1), [semgrep](/man/semgrep)(1), [go](/man/go)(1)"],["RESOURCES","```[Source code](https://github.com/securego/gosec)```"]],"e":["golangci-lint","staticcheck","semgrep","go"],"x":"gosec ./..."},"grn":{"n":"grn","t":"Groff preprocessor for Gremlin graphics","s":[["TAGLINE","Groff preprocessor for Gremlin graphics"],["TLDR","**Process gremlin file**\n\n```grn [file.grn] | groff > output.ps```\n\n**Specify macro file**\n\n```grn -M [macrodir] [file.grn]```\n\n**Compatible mode**\n\n```grn -C [file.grn]```"],["SYNOPSIS","**grn** [_options_] [_file_]"],["PARAMETERS","_FILE_\n> Gremlin input file.\n\n**-M** _DIR_\n> Macro directory.\n\n**-C**\n> Compatible mode with older versions.\n\n**-F** _FILE_\n> Font file.\n\n**--help**\n> Display help information."],["DESCRIPTION","**grn** is a groff preprocessor for Gremlin graphics files. It converts Gremlin vector graphics format to groff commands for embedding line drawings and text in documents.\n\nThe tool was originally developed for SunView and later adapted for groff. It handles line drawings, text labels, and other Gremlin primitives."],["CAVEATS","Part of groff suite. Gremlin format is largely obsolete. Limited to basic vector graphics."],["HISTORY","grn was created at **UC Berkeley** for the Gremlin drawing program, later included in **GNU groff** for compatibility."],["INSTALL","```apt: sudo apt install groff```\n\n```dnf: sudo dnf install groff```\n\n```pacman: sudo pacman -S groff```\n\n```apk: sudo apk add groff```\n\n```zypper: sudo zypper install groff```\n\n```brew: brew install groff```\n\n```nix: nix profile install nixpkgs#groff```"],["SEE ALSO","[groff](/man/groff)(1), [pic](/man/pic)(1), [grap](/man/grap)(1)"],["RESOURCES","```[Source code](https://git.savannah.gnu.org/cgit/groff.git)```\n\n```[Homepage](https://www.gnu.org/software/groff/)```"]],"e":["groff","pic","grap"],"x":"grn [file.grn] | groff > output.ps"},"hardlink":{"n":"hardlink","t":"Consolidate duplicate files by replacing them with hard links","s":[["TAGLINE","Consolidate duplicate files by replacing them with hard links"],["TLDR","Hardlink duplicate files in a directory (dry-run first)\n\n```hardlink --dry-run /path/to/directory```\n\nHardlink duplicates in one or more directories\n\n```hardlink /path/to/dir1 /path/to/dir2```\n\nHardlink with verbose output\n\n```hardlink --verbose /path/to/directory```\n\nOnly consider files larger than a minimum size\n\n```hardlink --minimum-size 1M /path/to/directory```"],["SYNOPSIS","**hardlink** [_options_] _directory_..."],["PARAMETERS","**-n**, **--dry-run**  \n> Do not actually link files, just print what would be done\n\n**-v**, **--verbose**  \n> Print more information about what is being done\n\n**-s**, **--minimum-size** _size_  \n> Only hardlink files larger than _size_ (suffixes K, M, G supported)\n\n**-x**, **--exclude** _pattern_  \n> Skip files matching the given glob pattern (repeatable)\n\n**-i**, **--include** _pattern_  \n> Only consider files matching the given glob pattern\n\n**-f**, **--force**  \n> Link files even across different filesystems (dangerous)"],["DESCRIPTION","**hardlink** finds identical files in the given directories and replaces all but one copy with hard links to the remaining file. This saves disk space when the same content exists in multiple places (common with backups, package caches, build artifacts, etc.).\n\nThe tool compares file contents (not just names or sizes) before linking. It is safe by default: it will not cross filesystem boundaries and will refuse to link files that are not identical.\n\nHard links are directory entries pointing to the same inode. Deleting one name does not affect the others; the data is only removed when the last link is deleted. This makes hardlink particularly useful for deduplicating large static datasets."],["CAVEATS","- Hard links can only be created on the same filesystem.\n- The original file permissions and ownership are preserved on the remaining copy.\n- Running hardlink on active build or database directories can cause surprising results if files are being modified during the scan.\n- Some backup tools and version control systems do not expect hard links and may behave unexpectedly."],["INSTALL","```apt: sudo apt install util-linux```\n\n```dnf: sudo dnf install util-linux```\n\n```pacman: sudo pacman -S util-linux```\n\n```apk: sudo apk add util-linux-misc```\n\n```zypper: sudo zypper install util-linux```\n\n```brew: brew install util-linux```\n\n```nix: nix profile install nixpkgs#util-linux```"],["SEE ALSO","[cp](/man/cp)(1), [ln](/man/ln)(1), [find](/man/find)(1), [fdupes](/man/fdupes)(1), [rdfind](/man/rdfind)(1)"]],"e":["cp","ln","find","fdupes","rdfind"],"x":"hardlink --dry-run /path/to/directory"},"head":{"n":"head","t":"outputs the first 10 lines of each FILE to standard output","s":[["TAGLINE","outputs the first 10 lines of each FILE to standard output"],["TLDR","Display **first 10 lines** of a file\n\n```head path/to/file```\n\nDisplay first **5 lines** of a file\n\n```head -n 5 path/to/file```\n\nDisplay first **100 bytes** of a file\n\n```head -c 100 path/to/file```\n\nDisplay all but the **last 5 lines**\n\n```head -n -5 path/to/file```\n\nDisplay first lines of **multiple files**\n\n```head path/to/file1 path/to/file2```\n\nSuppress **filename headers**\n\n```head -q path/to/file1 path/to/file2```"],["SYNOPSIS","**head** [_OPTION_]... [_FILE_]..."],["DESCRIPTION","**head** outputs the first 10 lines of each FILE to standard output. When processing multiple files, it precedes each with a header containing the filename. If no file is specified or when FILE is \"-\", it reads from standard input."],["PARAMETERS","**-c, --bytes=[-]NUM**\n> Print the first NUM bytes of each file; with leading \"-\", print all but the last NUM bytes\n\n**-n, --lines=[-]NUM**\n> Print the first NUM lines instead of 10; with leading \"-\", print all but the last NUM lines\n\n**-q, --quiet, --silent**\n> Never print headers giving filenames\n\n**-v, --verbose**\n> Always print headers giving filenames\n\n**-z, --zero-terminated**\n> Use NUL as line delimiter instead of newline"],["CAVEATS","NUM may have a multiplier suffix: b (512), kB (1000), K (1024), MB (1000*1000), M (1024*1024), and so on for G, T, P, E, Z, Y. Binary prefixes like KiB, MiB are also supported."],["HISTORY","**head** has been part of Unix since early versions and is included in GNU **coreutils**."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[tail](/man/tail)(1), [cat](/man/cat)(1), [less](/man/less)(1), [more](/man/more)(1)"]],"e":["tail","cat","less","more"],"x":"head path/to/file"},"hut":{"n":"hut","t":"CLI for SourceHut services","s":[["TAGLINE","CLI for SourceHut services"],["TLDR","**Set up hut's configuration** (prompts for an OAuth2 access token)\n\n```hut init```\n\n**List your Git repositories**\n\n```hut git list```\n\n**Create a new Git repository**\n\n```hut git create [repo-name]```\n\n**List build jobs**\n\n```hut builds list```\n\n**List issue trackers**\n\n```hut todo list```\n\n**Create a paste from a file**\n\n```hut paste create [file]```"],["SYNOPSIS","**hut** _service_ _command_ [_options_]"],["PARAMETERS","_SERVICE_\n> SourceHut service to operate on.\n\n_COMMAND_\n> Command within that service (commonly **list**, **create**, **show**, **update**, **delete**).\n\n**builds**\n> Manage build jobs on builds.sr.ht.\n\n**git**\n> Manage Git repositories on git.sr.ht.\n\n**hg**\n> Manage Mercurial repositories on hg.sr.ht.\n\n**lists**\n> Manage mailing lists and patchsets on lists.sr.ht.\n\n**meta**\n> Manage account settings, SSH/PGP keys, and profile info.\n\n**pages**\n> Publish static websites to pages.sr.ht.\n\n**paste**\n> Create and share text pastes on paste.sr.ht.\n\n**todo**\n> Manage issue trackers and tickets on todo.sr.ht.\n\n**graphql**\n> Run a raw GraphQL query against a given service.\n\n**init**\n> Set up hut's configuration file with an OAuth2 token.\n\n**export** / **import**\n> Back up or restore account data.\n\n**--config** _FILE_\n> Use a specific configuration file.\n\n**--instance** _NAME_\n> Select which sr.ht instance to use.\n\n**--debug**\n> Print the underlying GraphQL requests to stderr."],["DESCRIPTION","**hut** is the official command-line client for SourceHut (sr.ht) services. It provides access to builds, Git and Mercurial repositories, issue trackers, mailing lists, pastes, static site hosting, and account management, all backed by each service's GraphQL API.\n\nCommands follow a **hut** _service_ _command_ pattern, so most operations map directly onto one of the sr.ht services. The **graphql** subcommand also allows running arbitrary GraphQL queries against any service for tasks not otherwise covered."],["CAVEATS","Requires a SourceHut account and an OAuth2 access token, configured via **hut init**. Available services and their subcommands vary by sr.ht instance and hut version; run **hut help** or **hut** _service_ **--help** to see what's supported locally."],["HISTORY","hut is the official CLI for **SourceHut** (sr.ht), a lightweight, privacy-focused software development platform created by **Drew DeVault**. It is written in Go and replaced earlier ad-hoc scripts for scripting SourceHut services from the command line."],["INSTALL","```apt: sudo apt install hut```\n\n```dnf: sudo dnf install hut```\n\n```pacman: sudo pacman -S hut```\n\n```apk: sudo apk add hut```\n\n```zypper: sudo zypper install hut```\n\n```brew: brew install hut```\n\n```nix: nix profile install nixpkgs#hut```"],["SEE ALSO","[git](/man/git)(1), [gh](/man/gh)(1), [hub](/man/hub)(1)"]],"e":["git","gh","hub"],"x":"hut init"},"iamb":{"n":"iamb","t":"Matrix chat client for Vim addicts","s":[["TAGLINE","Matrix chat client for Vim addicts"],["TLDR","**Start iamb**\n\n```iamb```\n\n**Start with a specific profile**\n\n```iamb -P [profile_name]```"],["SYNOPSIS","**iamb** [**-P** _profile_] [_options_]"],["DESCRIPTION","**iamb** is a terminal-based **Matrix** chat client with **Vim** keybindings. It supports threads, spaces, end-to-end encryption (E2EE), image previews (in terminals supporting sixels, Kitty, or iTerm2 protocols), and full room management including creating, joining, leaving rooms, and handling invitations.\n\nThe client provides message editing, redaction, reactions, and room navigation using familiar Vim motions and commands. Custom keybindings and multiple profiles are supported."],["CAVEATS","Requires a Matrix homeserver account. End-to-end encryption key management can be complex. Image preview quality depends on terminal capabilities."],["HISTORY","**iamb** was created by **Ulyssa** and is written in **Rust** using the **matrix-rust-sdk**. It was designed for users who prefer modal editing and want a keyboard-driven Matrix client without leaving the terminal."],["INSTALL","```apk: sudo apk add iamb```\n\n```zypper: sudo zypper install iamb```\n\n```brew: brew install iamb```\n\n```nix: nix profile install nixpkgs#iamb```"],["SEE ALSO","[weechat](/man/weechat)(1), [gurk](/man/gurk)(1)"]],"e":["weechat","gurk"],"x":"iamb"},"ideviceenterrecovery":{"n":"ideviceenterrecovery","t":"puts an iOS device into recovery mode","s":[["TAGLINE","puts an iOS device into recovery mode"],["TLDR","**Enter recovery mode**\n\n```ideviceenterrecovery [udid]```\n\n**Enter recovery by name**\n\n```ideviceenterrecovery -n [device-name]```\n\n**Debug mode**\n\n```ideviceenterrecovery -d [udid]```"],["SYNOPSIS","**ideviceenterrecovery** [_options_] _udid_"],["PARAMETERS","_UDID_\n> Device UDID.\n\n**-n** _NAME_\n> Target by device name.\n\n**-d**, **--debug**\n> Debug output.\n\n**--help**\n> Display help information."],["DESCRIPTION","**ideviceenterrecovery** puts an iOS device into recovery mode. Recovery mode is used for firmware restoration and troubleshooting.\n\nThe tool sends the command to enter recovery via USB. The device must be paired and trusted."],["CAVEATS","Device must be paired. Recovery mode needs manual exit. Part of libimobiledevice."],["HISTORY","ideviceenterrecovery is part of **libimobiledevice** for iOS device management on Linux."],["INSTALL","```apt: sudo apt install libimobiledevice-utils```\n\n```dnf: sudo dnf install libimobiledevice-utils```\n\n```apk: sudo apk add libimobiledevice-progs```"],["SEE ALSO","[idevice_id](/man/idevice_id)(1), [idevicepair](/man/idevicepair)(1)"]],"e":["idevice_id","idevicepair"],"x":"ideviceenterrecovery [udid]"},"inkscape":{"n":"inkscape","t":"professional vector graphics editor","s":[["TAGLINE","professional vector graphics editor"],["TLDR","**Open file in GUI**\n\n```inkscape [file.svg]```\n\n**Export to PNG**\n\n```inkscape [input.svg] -o [output.png]```\n\n**Export with specific size**\n\n```inkscape [input.svg] -w [1024] -h [768] -o [output.png]```\n\n**Export to PDF**\n\n```inkscape [input.svg] -o [output.pdf]```\n\n**Convert SVG to EPS**\n\n```inkscape [input.svg] -o [output.eps]```\n\n**Export specific area**\n\n```inkscape [input.svg] --export-area-page -o [output.png]```\n\n**Export drawing area only (crop to content)**\n\n```inkscape [input.svg] -D -o [output.png]```\n\n**Run without GUI**\n\n```inkscape --export-type=[png] [input.svg]```\n\n**Run batch actions from command line**\n\n```inkscape --actions=\"open:[input.svg];export-filename:[output.png];export-do\" --batch-process```"],["SYNOPSIS","**inkscape** [_options_] [_file_...]"],["PARAMETERS","**-o** _FILE_\n> Output filename.\n\n**-w** _WIDTH_\n> Export width in pixels.\n\n**-h** _HEIGHT_\n> Export height in pixels.\n\n**-d** _DPI_\n> Export resolution.\n\n**--export-type** _TYPE_\n> Export format (png, pdf, eps, ps, svg, emf, wmf). Comma-separated for multiple.\n\n**-C**, **--export-area-page**\n> Export entire page area.\n\n**-D**, **--export-area-drawing**\n> Export drawing bounding box only (crop to content).\n\n**-i**, **--export-id** _ID_\n> Export specific object by ID. Semicolon-separated for multiple.\n\n**-l**, **--export-plain-svg**\n> Export as plain SVG without Inkscape-specific namespaces.\n\n**-T**, **--export-text-to-path**\n> Convert text objects to paths on export.\n\n**--actions** _ACTIONS_\n> Execute semicolon-separated actions for batch processing.\n\n**--shell**\n> Enter interactive command line shell mode.\n\n**--batch-process**\n> Process files and close without GUI.\n\n**--pdf-page** _N_\n> Page number to import from multi-page PDF (starting from 1).\n\n**--help**\n> Display help information."],["DESCRIPTION","**Inkscape** is a professional vector graphics editor. It supports SVG as native format and exports to PNG, PDF, EPS, and other formats.\n\nThe application provides comprehensive drawing tools, path operations, and text handling. It can run headless for batch conversions."],["CAVEATS","Large files may be slow. Some SVG features are unsupported. The command-line interface changed significantly in version 1.0 (old flags like `-e` and `-A` were replaced)."],["HISTORY","Inkscape was forked from **Sodipodi** in 2003. It became the leading open-source alternative to Adobe Illustrator."],["INSTALL","```apt: sudo apt install inkscape```\n\n```dnf: sudo dnf install inkscape```\n\n```pacman: sudo pacman -S inkscape```\n\n```apk: sudo apk add inkscape```\n\n```zypper: sudo zypper install inkscape```\n\n```nix: nix profile install nixpkgs#inkscape```"],["SEE ALSO","[inkview](/man/inkview)(1), [convert](/man/convert)(1), [rsvg-convert](/man/rsvg-convert)(1), [svgo](/man/svgo)(1), [gimp](/man/gimp)(1)"]],"e":["inkview","convert","rsvg-convert","svgo","gimp"],"x":"inkscape [file.svg]"},"jj-tag-delete":{"n":"jj-tag-delete","t":"removes tags from the repository","s":[["TAGLINE","removes tags from the repository"],["TLDR","**Delete a tag**\n\n```jj tag delete [tagname]```\n\n**Delete multiple tags**\n\n```jj tag delete [tag1] [tag2]```"],["SYNOPSIS","**jj** **tag** **delete** _name_..."],["DESCRIPTION","**jj tag delete** removes tags from the repository. Tags are lightweight references pointing to specific commits. Deleting a tag does not affect the underlying commit."],["INSTALL","```pacman: sudo pacman -S jujutsu```\n\n```apk: sudo apk add jujutsu```\n\n```zypper: sudo zypper install jujutsu```\n\n```brew: brew install jujutsu```\n\n```nix: nix profile install nixpkgs#jujutsu```"],["SEE ALSO","[jj](/man/jj)(1), [jj-tag](/man/jj-tag)(1), [jj-tag-list](/man/jj-tag-list)(1)"]],"e":["jj","jj-tag","jj-tag-list"],"x":"jj tag delete [tagname]"},"latte-dock":{"n":"latte-dock","t":"dock panel for KDE Plasma that provides an alternative to the default Plasma","s":[["TAGLINE","dock panel for KDE Plasma that provides an alternative to the default Plasma"],["TLDR","**Clear** QML cache\n\n```latte-dock --clear-cache```\n\nImport and load **default layout**\n\n```latte-dock --default-layout```\n\nLoad a **specific layout**\n\n```latte-dock --layout [layout_name]```\n\n**Import** and load a layout from file\n\n```latte-dock --import-layout [path/to/file]```"],["SYNOPSIS","**latte-dock** [_options_]"],["PARAMETERS","**--clear-cache**\n> Clear the QML cache\n\n**--default-layout**\n> Import and load the default layout\n\n**--layout** _NAME_\n> Load a specific layout by name\n\n**--import-layout** _FILE_\n> Import and load a layout from file\n\n**--available-layouts**\n> List available layouts\n\n**--replace**\n> Replace an already running instance of Latte Dock"],["DESCRIPTION","**latte-dock** is a dock panel for KDE Plasma that provides an alternative to the default Plasma panel. It offers macOS-like dock functionality with animations, applets, and customizable layouts.\n\nMultiple layouts can be saved and switched between, allowing different configurations for different activities or workflows. Latte integrates with Plasma's global themes and widgets."],["CAVEATS","KDE Plasma desktop required. Development of Latte Dock has officially ended. The maintainer announced the project is no longer actively developed, and compatibility issues with newer Plasma versions (especially Plasma 6) are expected. Users are encouraged to migrate to Plasma's built-in panel features."],["HISTORY","Latte Dock was created as a more polished dock alternative for KDE Plasma, offering smooth animations and flexible customization. It became popular in the KDE community as a macOS-style dock replacement. The project was officially discontinued by its maintainer."],["INSTALL","```aur: yay -S latte-dock```"],["SEE ALSO","[plasmashell](/man/plasmashell)(1)"]],"e":["plasmashell"],"x":"latte-dock --clear-cache"},"lavinmq":{"n":"lavinmq","t":"Lightweight, high-performance AMQP message broker","s":[["TAGLINE","Lightweight, high-performance AMQP message broker"],["TLDR","**Start the broker** with default settings\n\n```lavinmq```\n\n**Start with a specific configuration file**\n\n```lavinmq --config=[path/to/lavinmq.ini]```\n\n**Specify the data directory** for persistent storage\n\n```lavinmq --data-dir=[path/to/data]```\n\n**Bind to a specific address** and AMQP port\n\n```lavinmq --bind=[0.0.0.0] --amqp-port=[5672]```\n\n**Show version** information\n\n```lavinmq -v```\n\n**Manage the broker** (queues, policies, definitions) via the companion CLI\n\n```lavinmqctl list_queues```\n\n**Export broker definitions** to JSON\n\n```lavinmqctl export_definitions > [definitions.json]```"],["SYNOPSIS","**lavinmq** [_options_]\n\n**lavinmqctl** [_options_] _command_ [_arguments_]"],["PARAMETERS","**-c** _file_, **--config**=_file_\n> Load broker settings from the specified INI configuration file.\n\n**-D** _dir_, **--data-dir**=_dir_\n> Directory for persistent data (queues, messages, definitions).\n\n**-b** _address_, **--bind**=_address_\n> IP or hostname to listen on (default 127.0.0.1).\n\n**--amqp-port**=_port_\n> AMQP listener port (default 5672).\n\n**--amqps-port**=_port_\n> AMQPS (TLS) listener port (default 5671).\n\n**--http-port**=_port_\n> HTTP management UI port (default 15672).\n\n**--https-port**=_port_\n> HTTPS management UI port (default 15671).\n\n**--guest-only-loopback**=_bool_\n> Restrict the default _guest_ user to loopback connections only.\n\n**--log-level**=_level_\n> Logging verbosity (debug, info, warn, error, fatal).\n\n**-h**, **--help**\n> Show usage information and exit.\n\n**-v**, **--version**\n> Print the LavinMQ version and exit."],["DESCRIPTION","**LavinMQ** is an open-source message broker that implements the **AMQP 0-9-1** protocol and is wire-compatible with existing RabbitMQ client libraries. It also speaks **MQTT** and a native streams protocol. Written in **Crystal**, it is designed to handle very high throughput on a single node while keeping memory usage low by relying on the operating system page cache and memory-mapped files.\n\nThe broker ships as two executables. **lavinmq** is the long-running server process that accepts client connections, routes messages between exchanges and queues, and exposes a built-in HTTP management UI. **lavinmqctl** is the administrative command-line tool used to inspect and manage virtual hosts, users, queues, exchanges, bindings, policies, and broker definitions while the server is running.\n\nLavinMQ supports clustering for high availability, replicated queues, federation, shovels, and a streams feature for log-style consumption. Configuration can be provided through an INI file or overridden on the command line, and the broker can be installed from native packages, built from source, or run as the official **cloudamqp/lavinmq** Docker image."],["CONFIGURATION","LavinMQ reads its settings from an INI file (typically **/etc/lavinmq/lavinmq.ini**) divided into sections such as **[main]**, **[mgmt]**, **[amqp]**, and **[mqtt]**. A minimal example:\n\n```\n[main]\ndata_dir = /var/lib/lavinmq\nguest_only_loopback = true\nlog_level = info\n\n[mgmt]\nbind = 0.0.0.0\nport = 15672\ntls_port = 15671\n\n[amqp]\nbind = 0.0.0.0\nport = 5672\ntls_port = 5671\n```\n\nAny flag passed on the command line overrides the matching value from the configuration file. The default user **guest** with password **guest** is restricted to local connections unless **guest_only_loopback** is disabled."],["CAVEATS","The default **guest/guest** account is intended for local testing only and should be replaced before exposing the broker to the network. Although LavinMQ is largely compatible with RabbitMQ clients, some advanced RabbitMQ-specific features and plugins are not implemented. Persistent data is stored on disk per node, so an unclean shutdown of a non-clustered broker can require recovery on the next startup."],["HISTORY","**LavinMQ** was created by **CloudAMQP** (84codes) and first released as open source in **2022**. It originated from the company's experience operating large RabbitMQ clusters and aimed to provide a lighter, faster broker with the same wire protocol. Written in **Crystal** and licensed under the **Apache 2.0** license, the project has since added clustering, MQTT support, and a streams protocol."],["INSTALL","```brew: brew install lavinmq```"],["SEE ALSO","[rabbitmq-server](/man/rabbitmq-server)(1), [rabbitmqctl](/man/rabbitmqctl)(1), [mosquitto](/man/mosquitto)(8)"]],"e":["rabbitmq-server","rabbitmqctl","mosquitto"],"x":"lavinmq"},"limine-scan":{"n":"limine-scan","t":"scan for Limine bootloader installations","s":[["TAGLINE","scan for Limine bootloader installations"],["TLDR","This command is an alias of **limine-entry-tool --scan**.\n\nView documentation for the original command\n\n```tldr limine-entry-tool```"],["DESCRIPTION","**limine-scan** scans block devices for existing Limine bootloader installations and reports their locations. It is provided as a convenience alias for **limine-entry-tool --scan**."],["SEE ALSO","[limine-entry-tool](/man/limine-entry-tool)(1), [limine](/man/limine)(8)"]],"e":["limine-entry-tool","limine"],"x":"tldr limine-entry-tool"},"lxc-destroy":{"n":"lxc-destroy","t":"Delete a Linux container and its configuration","s":[["TAGLINE","Delete a Linux container and its configuration"],["TLDR","**Delete** a stopped container\n\n```sudo lxc-destroy -n [container_name]```\n\n**Force delete** a running container (stops it first)\n\n```sudo lxc-destroy -n [container_name] -f```\n\n**Delete container and all its snapshots**\n\n```sudo lxc-destroy -n [container_name] -s```\n\nDisplay **help**\n\n```lxc-destroy --help```"],["SYNOPSIS","**lxc-destroy** **-n** _name_ [_options_]"],["DESCRIPTION","**lxc-destroy** deletes a Linux container and its configuration. The container must be stopped before destruction unless **-f** is used."],["PARAMETERS","**-n**, **--name** _NAME_\n> Container name to destroy.\n\n**-P**, **--lxcpath** _PATH_\n> Use an alternate container storage directory.\n\n**-f**, **--force**\n> Force destruction of a running container (stops it first).\n\n**-s**, **--snapshots**\n> Also destroy all snapshots of the container.\n\n**-l**, **--logpriority** _LEVEL_\n> Set log priority (FATAL, CRIT, WARN, ERROR, NOTICE, INFO, DEBUG).\n\n**-o**, **--logfile** _FILE_\n> Output to alternate log file.\n\n**-?**, **--help**\n> Display help information."],["CAVEATS","Requires root privileges. Permanently deletes container and its root filesystem. Stop container first unless using --force."],["INSTALL","```apt: sudo apt install lxc```\n\n```dnf: sudo dnf install lxc```\n\n```pacman: sudo pacman -S lxc```\n\n```apk: sudo apk add lxc```\n\n```zypper: sudo zypper install lxc```\n\n```brew: brew install lxc```\n\n```nix: nix profile install nixpkgs#lxc```"],["SEE ALSO","[lxc-create](/man/lxc-create)(1), [lxc-stop](/man/lxc-stop)(1), [lxc-ls](/man/lxc-ls)(1)"]],"e":["lxc-create","lxc-stop","lxc-ls"],"x":"sudo lxc-destroy -n [container_name]"},"minimap2":{"n":"minimap2","t":"versatile sequence aligner for DNA and RNA sequences","s":[["TAGLINE","versatile sequence aligner for DNA and RNA sequences"],["TLDR","**Map long reads to reference**\n\n```minimap2 -a [reference.fa] [reads.fq] > [aligned.sam]```\n\n**Map with preset for ONT reads**\n\n```minimap2 -ax map-ont [reference.fa] [ont_reads.fq] > [aligned.sam]```\n\n**Map PacBio HiFi reads**\n\n```minimap2 -ax map-hifi [reference.fa] [hifi.fq] > [aligned.sam]```\n\n**Map short reads**\n\n```minimap2 -ax sr [reference.fa] [reads_1.fq] [reads_2.fq] > [aligned.sam]```\n\n**Create index**\n\n```minimap2 -d [reference.mmi] [reference.fa]```\n\n**Use multiple threads**\n\n```minimap2 -t [8] -ax map-ont [reference.fa] [reads.fq] > [aligned.sam]```"],["SYNOPSIS","**minimap2** [_options_] _target_ [_query_...]"],["PARAMETERS","**-a**\n> Output SAM format.\n\n**-x** _preset_\n> Preset (map-ont, map-pb, map-hifi, sr, asm5).\n\n**-t** _threads_\n> Number of threads.\n\n**-d** _file_\n> Save index to file.\n\n**-o** _file_\n> Output file.\n\n**-k** _k_\n> K-mer size.\n\n**-w** _w_\n> Minimizer window."],["DESCRIPTION","**minimap2** is a versatile sequence aligner for DNA and RNA sequences. It can align long reads from PacBio or Oxford Nanopore, short reads, and even whole assemblies.\n\nminimap2 is extremely fast and memory-efficient, becoming the standard for long-read alignment."],["PRESETS","```\nmap-ont   - ONT reads to reference\nmap-hifi  - PacBio HiFi reads\nmap-pb    - PacBio CLR reads\nsr        - Short reads\nasm5      - Divergent assembly\nsplice    - Long-read RNA-seq\n```"],["CAVEATS","Output is PAF by default; use -a for SAM. Index size depends on reference. Presets should match data type."],["HISTORY","minimap2 was developed by **Heng Li** (author of BWA) and published in **2018** as a fast, accurate aligner for long reads."],["INSTALL","```apt: sudo apt install minimap2```\n\n```brew: brew install minimap2```\n\n```nix: nix profile install nixpkgs#minimap2```"],["SEE ALSO","[bwa](/man/bwa)(1), [samtools](/man/samtools)(1)"]],"e":["bwa","samtools"],"x":"minimap2 -a [reference.fa] [reads.fq] > [aligned.sam]"},"mkfs.exfat":{"n":"mkfs.exfat","t":"creates an exFAT filesystem on a device","s":[["TAGLINE","creates an exFAT filesystem on a device"],["TLDR","Create **exFAT** filesystem\n\n```sudo mkfs.exfat /dev/sdXY```\n\nCreate with **volume label**\n\n```sudo mkfs.exfat -L [name] /dev/sdXY```\n\nCreate with specific **cluster size**\n\n```sudo mkfs.exfat -c [32K] /dev/sdXY```\n\nPerform a **full format** (zero entire device)\n\n```sudo mkfs.exfat -f /dev/sdXY```\n\nCreate in **quiet mode** (errors only)\n\n```sudo mkfs.exfat -q /dev/sdXY```"],["SYNOPSIS","**mkfs.exfat** [_options_] _device_"],["DESCRIPTION","**mkfs.exfat** creates an exFAT filesystem on a device. exFAT is designed for flash drives and supports large files (>4GB) while being compatible with Windows, macOS, and Linux. It is part of the **exfatprogs** package."],["PARAMETERS","**-L, --volume-label=LABEL**\n> Set volume label\n\n**-U, --volume-guid=GUID**\n> Set volume GUID\n\n**-c, --cluster-size=SIZE**\n> Set cluster size in bytes (must be a power of two); supports k/K for kibibytes and m/M for mebibytes\n\n**-s, --sector-size=SIZE**\n> Set sector size in bytes (512, 1024, 2048, or 4096)\n\n**-b, --boundary-align=SIZE**\n> Alignment for the FAT and the start of the cluster heap\n\n**-f, --full-format**\n> Perform a full format, zeroing the entire disk device\n\n**-K, --no-discard**\n> Do not attempt to discard blocks\n\n**-C, --verify-written**\n> Verify filesystem metadata by reading it back after writing\n\n**--pack-bitmap**\n> Relocate the allocation bitmap so it ends at the alignment boundary following the FAT\n\n**-q, --quiet**\n> Print only error messages\n\n**-v, --verbose**\n> Print verbose debugging information\n\n**-h, --help**\n> Display help and exit\n\n**-V, --version**\n> Display version and exit"],["CAVEATS","All data on device will be lost. exFAT is ideal for USB drives and SD cards requiring files larger than 4GB."],["INSTALL","```apt: sudo apt install exfatprogs```\n\n```dnf: sudo dnf install exfatprogs```\n\n```pacman: sudo pacman -S exfatprogs```\n\n```apk: sudo apk add exfatprogs```\n\n```zypper: sudo zypper install exfatprogs```\n\n```nix: nix profile install nixpkgs#exfatprogs```"],["SEE ALSO","[mkfs](/man/mkfs)(8), [fsck.exfat](/man/fsck.exfat)(8), [mkfs.vfat](/man/mkfs.vfat)(8), [exfatlabel](/man/exfatlabel)(8)"]],"e":["mkfs","fsck.exfat","mkfs.vfat","exfatlabel"],"x":"sudo mkfs.exfat /dev/sdXY"},"mmls":{"n":"mmls","t":"displays disk partition layout","s":[["TAGLINE","displays disk partition layout"],["TLDR","**List partitions**\n\n```mmls [disk.img]```\n\n**List from a device**\n\n```mmls [/dev/sda]```\n\n**Add a column** with partition sizes in bytes\n\n```mmls -B [disk.img]```\n\n**Specify the volume offset** into the image (in sectors)\n\n```mmls -o [63] [disk.img]```\n\n**Force the partition table type**\n\n```mmls -t [gpt] [disk.img]```\n\n**List the supported table types**\n\n```mmls -t list```\n\n**Set the device sector size**\n\n```mmls -b [4096] [disk.img]```"],["SYNOPSIS","**mmls** [_-t mmtype_] [_-o offset_] [_-i imgtype_] [_-b dev_sector_size_] [_-BrvV_] [_-aAmM_] _image_ [_images_]"],["PARAMETERS","_IMAGE_\n> Disk image (or device) to analyze. Split images can be given as multiple file names.\n\n**-t** _TYPE_\n> Partition table type to analyze (for example **dos**, **mac**, **bsd**, **sun**, **gpt**). Use **-t list** to list supported types. Autodetected if omitted.\n\n**-i** _IMGTYPE_\n> Format of the image file (for example **raw**, **ewf**). Use **-i list** to list supported types. Autodetected if omitted.\n\n**-o** _OFFSET_\n> Sector offset into the image where the volume containing the partition table starts.\n\n**-b** _SIZE_\n> Size, in bytes, of the underlying device sectors. Defaults to 512.\n\n**-B**\n> Add a column to the output that gives the size of each partition in bytes.\n\n**-r**\n> Recurse into DOS partitions and look for other partition tables.\n\n**-a**\n> Show only allocated volumes (those listed in the partition table).\n\n**-A**\n> Show only unallocated volumes (gaps not assigned to a partition).\n\n**-m**\n> Show metadata volumes (the sectors holding the partition table itself).\n\n**-M**\n> Hide metadata volumes.\n\n**-v**\n> Verbose debug output to stderr.\n\n**-V**\n> Display version information."],["DESCRIPTION","**mmls** displays the layout of a disk, including the partition tables and unallocated spaces. It is part of The Sleuth Kit (TSK), a collection of command-line tools for digital forensics and disk image analysis.\n\nBy default it shows all volumes, including unallocated regions and metadata, sorted by their starting sector. The starting and ending sector and length of each entry are reported, which makes mmls useful for finding the offset to pass to other TSK tools (such as **fls** or **fsstat**) when examining a single partition. It supports DOS partitions, BSD disk labels, Sun slices, Mac partitions, and GPT."],["CAVEATS","mmls is a read-only forensic tool: it never modifies the image. Sector values reported here are typically used directly as the **-o** offset for other Sleuth Kit tools. For images with a non-standard sector size, set **-b** accordingly or offsets will be wrong."],["HISTORY","mmls is part of **The Sleuth Kit**, a suite of open-source forensic tools for disk image analysis created by **Brian Carrier**, building on the earlier Coroner's Toolkit (TCT)."],["INSTALL","```apt: sudo apt install sleuthkit```\n\n```dnf: sudo dnf install sleuthkit```\n\n```pacman: sudo pacman -S sleuthkit```\n\n```apk: sudo apk add sleuthkit```\n\n```zypper: sudo zypper install sleuthkit```\n\n```brew: brew install sleuthkit```\n\n```nix: nix profile install nixpkgs#sleuthkit```"],["SEE ALSO","[fdisk](/man/fdisk)(8), [parted](/man/parted)(8), [fls](/man/fls)(1)"]],"e":["fdisk","parted","fls"],"x":"mmls [disk.img]"},"mv":{"n":"mv","t":"moves and renames files and directories","s":[["TAGLINE","moves and renames files and directories"],["TLDR","**Move a file to a directory**\n\n```mv [source] [directory/]```\n\n**Rename a file**\n\n```mv [oldname.txt] [newname.txt]```\n\n**Move multiple files to a directory**\n\n```mv [file1] [file2] [directory/]```\n\n**Move with confirmation before overwrite**\n\n```mv -i [source] [destination]```\n\n**Don't overwrite existing files**\n\n```mv -n [source] [destination]```\n\n**Move and show what's being done**\n\n```mv -v [source] [destination]```\n\n**Move with backup of existing destination**\n\n```mv --backup=numbered [source] [destination]```"],["SYNOPSIS","**mv** [_options_] _source_... _destination_"],["PARAMETERS","**-i**, **--interactive**\n> Prompt before overwriting an existing file.\n\n**-f**, **--force**\n> Do not prompt before overwriting. Overrides **-i** and **-n**.\n\n**-n**, **--no-clobber**\n> Do not overwrite an existing file. Overrides **-i** and **-f**.\n\n**-v**, **--verbose**\n> Print the name of each file being moved.\n\n**-u**, **--update**\n> Move only when the source is newer than the destination or the destination does not exist.\n\n**--backup**[=_CONTROL_]\n> Make a backup of each existing destination file. CONTROL can be numbered, existing, simple, or none.\n\n**-S**, **--suffix** _SUFFIX_\n> Override the usual backup suffix (default ~).\n\n**-t**, **--target-directory** _DIRECTORY_\n> Move all sources into the specified directory.\n\n**-T**, **--no-target-directory**\n> Treat destination as a normal file, not a directory."],["DESCRIPTION","**mv** moves and renames files and directories. When the source and destination are on the same filesystem, it performs an atomic rename. When moving across filesystems, it copies the data and then removes the original.\n\nRenaming is simply moving a file within the same directory. Moving a file into a directory places it inside that directory with the same basename.\n\nIf multiple sources are given, the last argument must be a directory and all sources are moved into it."],["CAVEATS","**mv** overwrites existing destination files by default without warning. Use **-i** or **-n** to prevent accidental data loss. When moving across filesystems, the operation is not atomic: the file is copied first, then the original is deleted."],["HISTORY","**mv** is one of the original Unix commands, present since Version 1 AT&T Unix in 1971."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[cp](/man/cp)(1), [rm](/man/rm)(1), [rename](/man/rename)(1), [rsync](/man/rsync)(1), [install](/man/install)(1)"]],"e":["cp","rm","rename","rsync","install"],"x":"mv [source] [directory/]"},"nbreversi":{"n":"nbreversi","t":"terminal reversi (othello) game","s":[["TAGLINE","terminal reversi (othello) game"],["TLDR","**Play reversi against the computer**\n\n```nbreversi```\n\n**Set AI difficulty level**\n\n```nbreversi -p [5]```\n\n**Play two humans**\n\n```nbreversi -1 h -2 h```\n\n**Play without replay prompt**\n\n```nbreversi -n```"],["SYNOPSIS","**nbreversi** [_options_]"],["PARAMETERS","**-1** _type_\n> Set player 1 type: h (human) or c (computer).\n\n**-2** _type_\n> Set player 2 type: h (human) or c (computer).\n\n**-p** _depth_\n> Set AI difficulty/search depth (1-127).\n\n**-n**\n> Don't ask for replay after the game ends.\n\n**-h**\n> Display help information."],["PREVIEW","```\n . . . . . . . .\n . . . . . . . .\n . . . O X . . .\n . . . X O . . .\n . . . . . . . .\n```"],["DESCRIPTION","**nbreversi** is a terminal-based Reversi (Othello) game from the **nbsdgames** collection. Players take turns placing discs on the board. When a disc is placed so that one or more of the opponent's discs are flanked in a straight line, those discs are flipped. The player with the most discs when no more moves can be made wins.\n\nControls: arrow keys or hjkl to move cursor, ENTER to place a disc, q to quit. Mouse input is also supported."],["CAVEATS","Requires a terminal with ncurses support. Part of the nbsdgames collection."],["HISTORY","**nbreversi** is part of **nbsdgames**, a package of 18 text-based terminal games created by **abakh**. Reversi was invented in **1883** by Lewis Waterman and later popularized as **Othello** by Goro Hasegawa in **1971**."],["SEE ALSO","[nbcheckers](/man/nbcheckers)(1), [nbsos](/man/nbsos)(1), [nbmines](/man/nbmines)(1)"]],"e":["nbcheckers","nbsos","nbmines"],"x":"nbreversi"},"netselect":{"n":"netselect","t":"determines the fastest network servers from a list of hosts by measuring","s":[["TAGLINE","determines the fastest network servers from a list of hosts by measuring"],["TLDR","Pick the host with the **lowest measured latency**\n\n```sudo netselect [host1] [host2] [host3]```\n\nReturn the **N fastest** hosts\n\n```sudo netselect -s [N] [host1] [host2] [host3]```\n\nIncrease verbosity for **DNS and probe stats**\n\n```sudo netselect -vv [host1] [host2]```\n\nCap probe **TTL**\n\n```sudo netselect -m [10] [host1] [host2]```\n\nShow built-in **usage** (no hosts)\n\n```netselect```"],["SYNOPSIS","**netselect** [_options_] _hosts_..."],["PARAMETERS","**-v**\n> Verbose mode; show resolution and stats (use -vv for more detail)\n\n**-m _ttl_**\n> Maximum TTL (time to live) for probe packets\n\n**-s _n_**\n> Print the n fastest servers instead of just the fastest\n\n**-t _n_**\n> Number of packets to send per host\n\n**-I**\n> Use ICMP echo instead of UDP"],["DESCRIPTION","**netselect** determines the fastest network servers from a list of hosts by measuring packet round-trip time and routing hops. It sends probe packets and ranks hosts based on a combined score of latency and hop count.\n\nThe output score is calculated as latency multiplied by (number of hops)^3, favoring nearby servers with low latency. Lower scores indicate better servers.\n\nCommonly used to select the fastest Debian mirror or DNS server. The companion tool **netselect-apt** automates mirror selection for Debian systems."],["CAVEATS","Requires root privileges to send raw packets. Results may vary based on network conditions at test time. ICMP packets may be blocked by some firewalls, affecting results. The scoring algorithm favors fewer hops, which isn't always optimal."],["INSTALL","```apt: sudo apt install netselect```\n\n```nix: nix profile install nixpkgs#netselect```"],["SEE ALSO","[netselect-apt](/man/netselect-apt)(1), [ping](/man/ping)(8), [traceroute](/man/traceroute)(8)"]],"e":["netselect-apt","ping","traceroute"],"x":"sudo netselect [host1] [host2] [host3]"},"nix-collect-garbage":{"n":"nix-collect-garbage","t":"removes unreferenced store paths from the Nix store","s":[["TAGLINE","removes unreferenced store paths from the Nix store"],["TLDR","**Collect garbage**\n\n```nix-collect-garbage```\n\n**Delete old generations**\n\n```nix-collect-garbage -d```\n\n**Delete generations older than days**\n\n```nix-collect-garbage --delete-older-than [30d]```\n\n**Dry run**\n\n```nix-collect-garbage --dry-run```"],["SYNOPSIS","**nix-collect-garbage** [_options_]"],["PARAMETERS","**-d**, **--delete-old**\n> Delete old profile generations.\n\n**--delete-older-than** _period_\n> Delete generations older than period.\n\n**--dry-run**\n> Show what would be deleted.\n\n**--max-freed** _bytes_\n> Stop after freeing bytes."],["DESCRIPTION","**nix-collect-garbage** removes unreferenced store paths from the Nix store. Frees disk space by deleting packages not linked from any profile or garbage collection root."],["INSTALL","```apt: sudo apt install nix-bin```\n\n```dnf: sudo dnf install nix```\n\n```pacman: sudo pacman -S nix```\n\n```apk: sudo apk add nix```\n\n```zypper: sudo zypper install nix```\n\n```nix: nix profile install nixpkgs#nix```"],["SEE ALSO","[nix-store](/man/nix-store)(1), [nix-env](/man/nix-env)(1)"]],"e":["nix-store","nix-env"],"x":"nix-collect-garbage"},"pageres":{"n":"pageres","t":"captures screenshots of websites at specified viewport sizes","s":[["TAGLINE","captures screenshots of websites at specified viewport sizes"],["TLDR","**Screenshot a website**\n\n```pageres [https://example.com]```\n\n**Specify viewport size**\n\n```pageres [https://example.com] [1024x768]```\n\n**Multiple sizes**\n\n```pageres [https://example.com] [1024x768] [1920x1080]```\n\n**Crop to viewport** (default is full page)\n\n```pageres [https://example.com] [1024x768] --crop```\n\n**Set output filename**\n\n```pageres [https://example.com] --filename=[screenshot]```\n\n**Delay before capture**\n\n```pageres [https://example.com] --delay=[3]```\n\n**Set output directory**\n\n```pageres [https://example.com] -d [./screenshots]```\n\n**Capture a specific element**\n\n```pageres [https://example.com] --selector=[.header]```\n\n**Hide an element before capture**\n\n```pageres [https://example.com] --hide=[.cookie-banner]```"],["SYNOPSIS","**pageres** [_options_] _url_ [_sizes_...]"],["PARAMETERS","**-d**, **--dest** _DIR_\n> Output directory.\n\n**--filename** _TEMPLATE_\n> Filename template.\n\n**--delay** _SECONDS_\n> Delay before screenshot.\n\n**-c**, **--crop**\n> Crop image to the specified viewport height (default captures the full scrolling page).\n\n**--overwrite**\n> Overwrite existing files.\n\n**--format** _FORMAT_\n> Image format: _png_ (default) or _jpg_.\n\n**--scale** _FACTOR_\n> Pixel-density scale multiplier.\n\n**--selector** _SELECTOR_\n> Capture only the DOM element matching the CSS selector.\n\n**--hide** _SELECTOR_\n> Hide elements matching the selector before capture (repeatable).\n\n**--clickElement** _SELECTOR_\n> Click the matched element before capture.\n\n**--css** _CSS_\n> Inject custom CSS before capture.\n\n**--cookie** _COOKIE_\n> Cookie string (repeatable).\n\n**--header** _HEADER_\n> HTTP header (repeatable).\n\n**--username** _USER_\n> HTTP basic auth username.\n\n**--password** _PASS_\n> HTTP basic auth password.\n\n**--user-agent** _UA_\n> User agent string.\n\n**--transparent**\n> Use a transparent background instead of white.\n\n**--darkMode**\n> Emulate the user's dark color scheme preference.\n\n**--timeout** _SECONDS_\n> Request timeout in seconds.\n\n**-v**, **--verbose**\n> Print detailed error output."],["DESCRIPTION","**pageres** captures screenshots of websites at specified viewport sizes. It's useful for responsive design testing.\n\nMultiple viewport sizes can be specified in a single command. Each size produces a separate image.\n\nFull-page screenshots capture content below the fold. Cropping limits output to visible viewport.\n\nDelays allow JavaScript and animations to complete. Custom CSS can hide or modify elements.\n\nOutput supports PNG and JPEG formats. Filenames can include URL and size variables."],["CAVEATS","Requires headless Chrome/Chromium. JavaScript-heavy sites may need delays. Some sites block automated screenshots."],["HISTORY","**pageres** was created by **Sindre Sorhus** for responsive design testing. It provides a command-line interface for website screenshot automation."],["SEE ALSO","[chromium](/man/chromium)(1), [puppeteer](/man/puppeteer)(1), [shot-scraper](/man/shot-scraper)(1)"]],"e":["chromium","puppeteer","shot-scraper"],"x":"pageres [https://example.com]"},"patina":{"n":"patina","t":"Persistent cognitive backend that learns your judgment","s":[["TAGLINE","Persistent cognitive backend that learns your judgment"],["TLDR","**Initialize Patina** and its local store\n\n```patina init```\n\n**Show items needing action**, new content and pending items\n\n```patina catch-up```\n\n**Start an interactive conversation** in the learned voice\n\n```patina chat```\n\n**Run a background daemon** that ingests and decays on an interval\n\n```patina heartbeat start --interval [30]```\n\n**Start the HTTP server** for gateway integration\n\n```patina serve --port [8321]```"],["SYNOPSIS","**patina** _command_ [_options_]"],["DESCRIPTION","**patina** is a local-first **AI backend** that builds a model of the user over time: a SQLite **belief graph** with context retention and graduated autonomy that needs less instruction the more it is used. It ingests messages (for example from Slack exports), extracts beliefs, tracks priorities and can draft messages in a learned communication style.\n\nIt is organized in three tiers feeding the belief graph: deterministic logic for scoring, decay and queries (Tier 1, usable with no model at all); a local model such as Qwen through **Ollama** for extraction and classification (Tier 2); and a frontier model such as Claude for synthesis and drafting (Tier 3). All data is kept locally under **~/.patina**. patina can also run as an MCP server for hosts such as Claude Code."],["COMMANDS","**init**\n> Initialize Patina and its local store.\n\n**ingest** [**--from-export** _file_]\n> Import data from Slack exports or configured adapters.\n\n**catch-up**\n> Show a unified view of items needing action, new content and pending items.\n\n**priorities**\n> List items ranked by priority quadrant (Q1-Q4).\n\n**draft** [**--to** _name_] [**--context** _text_]\n> Generate a message in the learned voice.\n\n**extract**\n> Extract beliefs from observations using an LLM.\n\n**beliefs** [**--type** _entity_type_]\n> List tracked entities with confidence counts.\n\n**autonomy status**\n> Show the current autonomy level and accuracy metrics.\n\n**autonomy set-level** _N_\n> Manually set the autonomy level (0 to 6).\n\n**heartbeat start** [**--interval** _minutes_]\n> Start a background daemon that runs ingest, decay and escalation cycles.\n\n**chat**\n> Start an interactive conversation (via the Claude Agent SDK).\n\n**serve** [**--port** _port_]\n> Start an HTTP server for gateway integration."],["CONFIGURATION","State is stored locally in **~/.patina/store.db** (the SQLite belief graph) and **~/.patina/config.yaml**. Tier 2 uses a local model through Ollama; Tier 3 uses a frontier model, configured through the config file and environment."],["CAVEAT","Tier 1 works with no model, but belief extraction and drafting (Tiers 2 and 3) need a local and/or frontier LLM configured. patina is early-stage software that manages personal data, so review what an adapter ingests before connecting it."],["HISTORY","**patina** is an open-source project written in **Python** by **Sanctum Origo Systems** and installed with **uv tool install patina**. It is distributed under the **Apache-2.0** license."],["SEE ALSO","[claude](/man/claude)(1), [ollama](/man/ollama)(1), [uv](/man/uv)(1)"]],"e":["claude","ollama","uv"],"x":"patina init"},"pct-pull":{"n":"pct-pull","t":"copy a file from a Proxmox LXC container to the host","s":[["TAGLINE","copy a file from a Proxmox LXC container to the host"],["TLDR","**Pull** a file from a container to the local host\n\n```pct pull 100 /var/log/app.log ./app.log```\n\nPull a config file and set **host ownership**\n\n```pct pull 100 /etc/myapp/config.yml ./config.yml --user root --group root```\n\nPull with **explicit permissions**\n\n```pct pull 100 /tmp/report.pdf ./report.pdf --perms 0644```"],["SYNOPSIS","**pct pull** _vmid_ _path_ _destination_ [_OPTIONS_]"],["PARAMETERS","**vmid**\n> The numeric ID of the container (100–999999999)\n\n**path**\n> Path to a file inside the container to pull\n\n**destination**\n> Destination path on the local (host) system\n\n**--user** _name|id_\n> Owner user name or id for the written host file\n\n**--group** _name|id_\n> Owner group name or id for the written host file\n\n**--perms** _mode_\n> File permissions (octal by default; prefix with **0x** for hexadecimal)"],["DESCRIPTION","**pct pull** copies a single file from a Proxmox VE LXC container to the host filesystem. It is the inverse of **pct push**: the source path is inside the container, and the destination is on the node where **pct** runs.\n\nThis is useful for retrieving logs, configs, or build artifacts without mounting the container root or opening an interactive shell. Ownership and mode of the host-side copy can be set with **--user**, **--group**, and **--perms**."],["CAVEATS","Copies one file at a time (not recursive directories). The container must be accessible on the local node; network/remote pull is not a separate mode. Paths are evaluated in the container's filesystem namespace. On unprivileged containers, ensure the host process has rights to read the guest path via the usual LXC file-copy path."],["HISTORY","**pct pull** is part of the **Proxmox VE** **pct** (Proxmox Container Toolkit) for managing LXC containers."],["SEE ALSO","[pct](/man/pct)(1), [pct-push](/man/pct-push)(1), [pct-exec](/man/pct-exec)(1), [pct-enter](/man/pct-enter)(1), [pct-mount](/man/pct-mount)(1)"],["RESOURCES","```[Documentation](https://pve.proxmox.com/pve-docs/pct.1.html)```\n\n```[Homepage](https://www.proxmox.com)```"]],"e":["pct","pct-push","pct-exec","pct-enter","pct-mount"],"x":"pct pull 100 /var/log/app.log ./app.log"},"pkcs11-tool":{"n":"pkcs11-tool","t":"Manage PKCS#11 cryptographic tokens and smart cards","s":[["TAGLINE","Manage PKCS#11 cryptographic tokens and smart cards"],["TLDR","**List available token slots**\n\n```pkcs11-tool --list-token-slots```\n\n**List objects on a token**\n\n```pkcs11-tool --list-objects```\n\n**List supported mechanisms**\n\n```pkcs11-tool --list-mechanisms```\n\n**Generate an RSA key pair**\n\n```pkcs11-tool --login --keypairgen --key-type rsa:2048 --label [keyname]```\n\n**Sign data with a key**\n\n```pkcs11-tool --login --sign -m RSA-PKCS --id [key_id] --input [data] --output [signature]```\n\n**Initialize a token**\n\n```pkcs11-tool --init-token --label [label] --so-pin [so_pin]```\n\n**Use a specific PKCS#11 library module**\n\n```pkcs11-tool --module [/usr/lib/libpkcs11.so] --list-objects```"],["SYNOPSIS","**pkcs11-tool** [_options_]"],["PARAMETERS","**--list-token-slots**\n> List available slots.\n\n**--list-objects**\n> List objects on the token. Can filter with --label, --id, or --type.\n\n**--list-mechanisms**\n> List mechanisms supported by the token.\n\n**--keypairgen**\n> Generate a key pair on the token.\n\n**--sign**\n> Sign data.\n\n**--verify**\n> Verify signed data.\n\n**--init-token**\n> Initialize a token.\n\n**--init-pin**\n> Initialize the user PIN (first-time setup).\n\n**--change-pin**\n> Change the user PIN.\n\n**--login**, **-l**\n> Authenticate to the token before performing operations.\n\n**--pin** _PIN_\n> Supply the PIN on the command line.\n\n**--so-pin** _PIN_\n> Supply the Security Officer PIN (used for token initialization).\n\n**--key-type** _spec_\n> Key type and length (e.g., rsa:2048, EC:prime256v1).\n\n**--id** _ID_\n> Object ID (hex).\n\n**--label** _LABEL_\n> Object label.\n\n**--slot** _ID_\n> Specify the slot to use.\n\n**--module** _LIB_\n> PKCS#11 library to load.\n\n**--help**\n> Display help."],["DESCRIPTION","**pkcs11-tool** interacts with PKCS#11 cryptographic tokens such as smart cards, USB security keys, and hardware security modules (HSMs). It can list available slots and objects, generate key pairs, sign and verify data, and initialize tokens.\n\nPart of the OpenSC project, it communicates with tokens through PKCS#11 library modules. Different token types require their specific PKCS#11 library, specified with the **--module** option. Operations on private objects typically require PIN authentication."],["CAVEATS","Part of OpenSC. Token/reader dependent. PIN required for operations."],["HISTORY","pkcs11-tool is part of **OpenSC** for PKCS#11 token management."],["INSTALL","```apt: sudo apt install opensc```\n\n```dnf: sudo dnf install opensc```\n\n```pacman: sudo pacman -S opensc```\n\n```apk: sudo apk add opensc```\n\n```zypper: sudo zypper install opensc```\n\n```brew: brew install opensc```\n\n```nix: nix profile install nixpkgs#opensc```"],["SEE ALSO","[p11-kit](/man/p11-kit)(1), [opensc-tool](/man/opensc-tool)(1), [openssl](/man/openssl)(1), [ssh-keygen](/man/ssh-keygen)(1)"]],"e":["p11-kit","opensc-tool","openssl","ssh-keygen"],"x":"pkcs11-tool --list-token-slots"},"ppmquant":{"n":"ppmquant","t":"Reduce PPM image color count","s":[["TAGLINE","Reduce PPM image color count"],["TLDR","**Reduce to N colors**\n\n```ppmquant [256] [input.ppm] > [output.ppm]```\n\n**Reduce to 16 colors**\n\n```ppmquant [16] [input.ppm] > [output.ppm]```\n\n**With Floyd-Steinberg dithering**\n\n```ppmquant -fs [256] [input.ppm] > [output.ppm]```\n\n**Use existing colormap**\n\n```ppmquant -map [palette.ppm] [input.ppm] > [output.ppm]```"],["SYNOPSIS","**ppmquant** [_options_] _ncolors_ [_ppmfile_]"],["PARAMETERS","**ncolors**\n> Maximum number of colors.\n\n**-fs**\n> Floyd-Steinberg dithering.\n\n**-map** _file_\n> Use colormap from file.\n\n**-spreadbrightness**\n> Spread by brightness.\n\n**-spreadluminosity**\n> Spread by luminosity."],["DESCRIPTION","**ppmquant** reduces the number of colors in a PPM image using median-cut quantization. Essential for creating GIF images or reducing file size.\n\nSuperseded by pnmquant in modern Netpbm."],["EXAMPLES","```bash"],["Reduce to 256 colors","ppmquant 256 photo.ppm > reduced.ppm"],["16 colors with dithering","ppmquant -fs 16 image.ppm > dithered.ppm"],["Use web-safe palette","ppmquant -map websafe.ppm image.ppm > websafe.ppm"],["Convert for GIF","ppmquant 256 image.ppm | ppmtogif > image.gif\n```"],["CAVEATS","Use pnmquant for new work. Dithering can increase file size. Part of Netpbm."],["HISTORY","ppmquant is part of **Netpbm** by **Jef Poskanzer**, implementing the median-cut color quantization algorithm by **Paul Heckbert**."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm-extras```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pnmquant](/man/pnmquant)(1), [pnmcolormap](/man/pnmcolormap)(1), [ppmtogif](/man/ppmtogif)(1), [netpbm](/man/netpbm)(1)"]],"e":["pnmquant","pnmcolormap","ppmtogif","netpbm"],"x":"ppmquant [256] [input.ppm] > [output.ppm]"},"pulumi-state":{"n":"pulumi-state","t":"Directly manipulate stack state","s":[["TAGLINE","Directly manipulate stack state"],["TLDR","**Delete resource from state**\n\n```pulumi state delete [urn]```\n\n**Unprotect resource**\n\n```pulumi state unprotect [urn]```\n\n**Rename resource**\n\n```pulumi state rename [urn] [new_name]```\n\n**Upgrade state schema**\n\n```pulumi state upgrade```"],["SYNOPSIS","**pulumi** **state** _command_ [_options_]"],["PARAMETERS","**delete** _urn_\n> Remove resource from state.\n\n**unprotect** _urn_\n> Remove protection from resource.\n\n**rename** _urn_ _name_\n> Rename resource in state.\n\n**upgrade**\n> Upgrade state schema.\n\n**-s**, **--stack** _name_\n> Target stack.\n\n**-y**, **--yes**\n> Skip confirmation.\n\n**--force**\n> Force operation."],["DESCRIPTION","**pulumi state** manipulates stack state directly. Use for state surgery when resources need manual removal or modification. Dangerous operations that bypass normal resource lifecycle."],["CAVEATS","Direct state manipulation can cause drift between state and actual infrastructure. Use with caution."],["INSTALL","```pacman: sudo pacman -S pulumi```\n\n```apk: sudo apk add pulumi```\n\n```brew: brew install pulumi```\n\n```nix: nix profile install nixpkgs#pulumi```"],["SEE ALSO","[pulumi](/man/pulumi)(1), [pulumi-stack](/man/pulumi-stack)(1), [pulumi-import](/man/pulumi-import)(1)"]],"e":["pulumi","pulumi-stack","pulumi-import"],"x":"pulumi state delete [urn]"},"pw-record":{"n":"pw-record","t":"Record audio to file via PipeWire","s":[["TAGLINE","Record audio to file via PipeWire"],["TLDR","This command is an alias of **pw-cat --record**\n\nView documentation for the original command\n\n```tldr pw-cat```"],["SYNOPSIS","**pw-record** [_options_] [_file_]"],["DESCRIPTION","**pw-record** is a convenience alias for **pw-cat --record** that captures audio from PipeWire and saves it to a file. It provides a simple interface for recording audio streams."],["INSTALL","```apt: sudo apt install pipewire-bin```\n\n```apk: sudo apk add pipewire-tools```\n\n```zypper: sudo zypper install pipewire-tools```"],["SEE ALSO","[pw-cat](/man/pw-cat)(1), [pw-play](/man/pw-play)(1), [pipewire](/man/pipewire)(1)"]],"e":["pw-cat","pw-play","pipewire"],"x":"tldr pw-cat"},"pwntools":{"n":"pwntools","t":"Python library for binary exploitation","s":[["TAGLINE","Python library for binary exploitation"],["TLDR","**Start Python with pwntools**\n\n```python -c \"from pwn import *\"```\n\n**Create pwntools script**\n\n```pwn template [binary] --host [host] --port [port]```\n\n**Check binary security**\n\n```checksec [binary]```\n\n**Disassemble shellcode**\n\n```disasm [hexstring]```"],["SYNOPSIS","**pwntools** Python library for exploit development"],["DESCRIPTION","**pwntools** is a CTF framework and exploit development library for Python. It provides utilities for binary exploitation, shellcode generation, and remote connections."],["EXAMPLES","```python\nfrom pwn import *"],["Connect to remote","r = remote('host', 1337)"],["Start local binary","p = process('./binary')"],["Read/write","r.recvline()\nr.sendline(b'payload')"],["Pack integers","payload = p32(0xdeadbeef)  # 32-bit\npayload = p64(0x41414141)  # 64-bit"],["Create shellcode","shellcode = asm(shellcraft.sh())"],["ROP chain","rop = ROP('./binary')\nrop.call('system', ['/bin/sh'])\n```"],["CLI TOOLS","```bash"],["Check binary protections","checksec ./binary"],["Create exploit template","pwn template ./binary > exploit.py"],["Assemble shellcode","pwn asm 'mov eax, 1; int 0x80'"],["Disassemble","pwn disasm '6a68682f2f2f73'"],["Cyclic pattern","pwn cyclic 100\npwn cyclic -l 0x61616164\n```"],["CAVEATS","Python library (pip install pwntools). Requires understanding of exploitation. For CTF and authorized testing only."],["HISTORY","pwntools was created by **Gallopsled** CTF team, becoming the standard Python library for CTF exploit development."],["INSTALL","```brew: brew install pwntools```\n\n```nix: nix profile install nixpkgs#pwntools```"],["SEE ALSO","[pwn](/man/pwn)(1), [checksec](/man/checksec)(1), [gdb](/man/gdb)(1), [radare2](/man/radare2)(1), [ropper](/man/ropper)(1)"]],"e":["pwn","checksec","gdb","radare2","ropper"],"x":"python -c \"from pwn import *\""},"qm-move-disk":{"n":"qm-move-disk","t":"Move Proxmox VM disk between storages","s":[["TAGLINE","Move Proxmox VM disk between storages"],["TLDR","This command is an alias of **qm disk move**\n\nView documentation for the original command\n\n```tldr qm disk```"],["SYNOPSIS","**qm move-disk** _vmid_ _disk_ _storage_ [_options_]"],["DESCRIPTION","**qm move-disk** is an alternative form of **qm disk move** that moves virtual machine disks between storage backends. The hyphenated variant is provided for compatibility."],["SEE ALSO","[qm](/man/qm)(1), [qm-disk](/man/qm-disk)(1)"]],"e":["qm","qm-disk"],"x":"tldr qm disk"},"reddix":{"n":"reddix","t":"Reddit refined for the terminal","s":[["TAGLINE","Reddit refined for the terminal"],["TLDR","**Launch the Reddit TUI**\n\n```reddix```"],["SYNOPSIS","**reddix** [_options_]"],["DESCRIPTION","**reddix** is a terminal user interface for browsing Reddit. It features inline image previews via the Kitty graphics protocol, video playback through mpv, gallery browsing, multi-account support, keyboard-first navigation, smart caching, and an NSFW filter toggle."],["CAVEATS","Requires a Reddit API \"script\" application to be created via the Reddit developer portal. Image previews require a Kitty-compatible terminal."],["HISTORY","**reddix** was created by **ck-zhang** and is written in **Rust**."],["INSTALL","```brew: brew install reddix```\n\n```nix: nix profile install nixpkgs#reddix```"],["SEE ALSO","[tuir](/man/tuir)(1)"]],"e":["tuir"],"x":"reddix"},"repomix":{"n":"repomix","t":"Pack repository into AI-friendly format","s":[["TAGLINE","Pack repository into AI-friendly format"],["TLDR","**Pack repository**\n\n```repomix```\n\n**Pack specific directory**\n\n```repomix [path/to/repo]```\n\n**Custom output file**\n\n```repomix -o [output.txt]```\n\n**Include specific files**\n\n```repomix --include \"[*.ts,*.js]\"```\n\n**Exclude patterns**\n\n```repomix --ignore \"[node_modules,dist]\"```\n\n**XML output format**\n\n```repomix --style xml```"],["SYNOPSIS","**repomix** [_-o file_] [_--include pattern_] [_--ignore pattern_] [_path_]"],["PARAMETERS","**-o**, **--output** _FILE_\n> Output filename.\n\n**--include** _PATTERN_\n> Include patterns.\n\n**--ignore** _PATTERN_\n> Ignore patterns.\n\n**--style** _FORMAT_\n> Output style (plain, xml, markdown).\n\n**--top-files-len** _N_\n> Summary file count.\n\n**-c**, **--config** _FILE_\n> Config file path."],["DESCRIPTION","**repomix** packs an entire code repository into a single text file optimized for use as context in AI language model conversations. It concatenates source files with clear file boundary markers and a directory tree summary, making it easy for LLMs to understand the full structure and content of a codebase.\n\nThe tool automatically respects .gitignore rules to skip irrelevant files like dependencies and build artifacts. Include and exclude patterns provide fine-grained control over which files are packed. Built-in token counting estimates how much of an LLM's context window the output will consume, helping users stay within model limits.\n\nOutput formats include plain text, XML, and Markdown, each with clear file delimiters and optional metadata headers. The tool can be configured via a **repomix.config.json** file for consistent usage across a project."],["CONFIGURATION","**repomix.config.json**\n> Project-level configuration file specifying default output format, include/exclude patterns, and output filename. Loaded automatically from the repository root."],["CAVEATS","Large repos may exceed limits. Binary files skipped. Needs Node.js."],["HISTORY","**repomix** was created to package codebases into a single file optimized for AI language model context windows."],["INSTALL","```brew: brew install repomix```\n\n```nix: nix profile install nixpkgs#repomix```"],["SEE ALSO","[git](/man/git)(1), [tree](/man/tree)(1), [cat](/man/cat)(1)"]],"e":["git","tree","cat"],"x":"repomix"},"scim":{"n":"scim","t":"Terminal spreadsheet with vim keybindings","s":[["TAGLINE","Terminal spreadsheet with vim keybindings"],["TLDR","**Open spreadsheet**\n\n```scim [file.sc]```\n\n**Open CSV file**\n\n```scim --txtdelim=\",\" [file.csv]```\n\n**Start with file**\n\n```scim [file.xlsx]```\n\n**Read-only mode**\n\n```scim -R [file.sc]```"],["SYNOPSIS","**scim** [_--txtdelim delim_] [_-R_] [_options_] [_file_]"],["PARAMETERS","**--txtdelim** _DELIM_\n> Text delimiter.\n\n**-R**\n> Read-only mode.\n\n**--nocurses**\n> No ncurses mode.\n\n**--version**\n> Show version.\n\n**--help**\n> Show help."],["DESCRIPTION","**scim** (sc-im) is a terminal-based spreadsheet calculator with vim-like keybindings and a modal interface. It provides a lightweight alternative to graphical spreadsheet applications, running entirely within an ncurses terminal interface for fast, keyboard-driven operation.\n\nThe spreadsheet supports formulas, mathematical functions, cell references, and ranges similar to conventional spreadsheet software. Files can be imported from and exported to CSV, TSV, and Excel formats, allowing interoperability with other tools. The native **.sc** format preserves all formulas and formatting.\n\nNavigation and editing follow vim conventions: **h/j/k/l** for movement, **i** for insert mode, **=** for formula entry, and **:** for commands like save and quit. This modal approach enables efficient keyboard-only workflows without reaching for a mouse."],["KEY BINDINGS","- **h/j/k/l** - Navigation\n- **i** - Insert mode\n- **=** - Enter formula\n- **:w** - Save\n- **:q** - Quit"],["CAVEATS","Learning curve steep. Limited Excel compatibility. Console only."],["HISTORY","**sc-im** is a spreadsheet calculator based on **sc**, with Vim-like interface and modern improvements."],["INSTALL","```apt: sudo apt install scim```\n\n```dnf: sudo dnf install scim```\n\n```pacman: sudo pacman -S scim```"],["SEE ALSO","[sc](/man/sc)(1), [gnumeric](/man/gnumeric)(1), [calc](/man/calc)(1)"]],"e":["sc","gnumeric","calc"],"x":"scim [file.sc]"},"scrot":{"n":"scrot","t":"Command-line screenshot capture for X11","s":[["TAGLINE","Command-line screenshot capture for X11"],["TLDR","Capture a **screenshot** and save with current date as filename\n\n```scrot```\n\nCapture a screenshot and save as **capture.png**\n\n```scrot capture.png```\n\nCapture a screenshot **interactively** (select region/window)\n\n```scrot -s```\n\nCapture screenshot interactively **without exiting** on keyboard input\n\n```scrot -is```\n\nCapture screenshot with **colored selection line**\n\n```scrot -s -l color=[x11_color]```\n\nCapture the **currently focused window**\n\n```scrot -u```\n\nDisplay a **countdown** of 10 seconds before taking screenshot\n\n```scrot -c -d 10```"],["SYNOPSIS","**scrot** [_options_] [_file_]"],["PARAMETERS","**-s**, **--select**\n> Interactively select a window or rectangle with mouse\n\n**-u**, **--focused**\n> Capture the currently focused window\n\n**-b**, **--border**\n> Include window border with -u\n\n**-d**, **--delay** _sec_\n> Wait _sec_ seconds before taking shot\n\n**-c**, **--count**\n> Display countdown when using delay\n\n**-q**, **--quality** _num_\n> Image quality (1-100) for JPEG\n\n**-t**, **--thumb** _num_\n> Generate thumbnail of specified percentage\n\n**-e**, **--exec** _cmd_\n> Execute command on saved image\n\n**-l**, **--line** _style_\n> Set line style for selection (color, width, etc.)\n\n**-i**, **--ignorekeyboard**\n> Don't exit selection mode on keyboard input\n\n**-p**, **--pointer**\n> Include mouse pointer in screenshot\n\n**-o**, **--overwrite**\n> Overwrite file if it exists"],["DESCRIPTION","**scrot** (SCReenshot) is a simple command-line screen capture utility for X11. It can capture the entire screen, specific windows, or user-selected regions. Screenshots are saved as PNG, JPEG, or GIF files.\n\nThe tool supports delayed captures, thumbnails, and executing commands on the resulting image (useful for automatic uploading or editing). Special format strings allow dynamic filenames based on date, time, and dimensions."],["CAVEATS","X11 only; for Wayland use **grim** with **slurp**. Selection mode may not work with all window managers. Some compositors may cause issues with window captures."],["HISTORY","Originally written by Tom Gilbert, who also created **feh**. A simple, focused screenshot tool that has been widely used in Linux desktop environments, often integrated with keyboard shortcuts for quick captures."],["INSTALL","```apt: sudo apt install scrot```\n\n```dnf: sudo dnf install scrot```\n\n```pacman: sudo pacman -S scrot```\n\n```apk: sudo apk add scrot```\n\n```zypper: sudo zypper install scrot```\n\n```nix: nix profile install nixpkgs#scrot```"],["SEE ALSO","[maim](/man/maim)(1), [grim](/man/grim)(1), [import](/man/import)(1), [flameshot](/man/flameshot)(1)"]],"e":["maim","grim","import","flameshot"],"x":"scrot"},"sem":{"n":"sem","t":"Counting semaphore for parallel job control","s":[["TAGLINE","Counting semaphore for parallel job control"],["TLDR","**Run a command** with default semaphore (mutex, one at a time)\n\n```sem [command]```\n\n**Run up to 4 jobs** in parallel with a named semaphore\n\n```sem -j 4 --id [myid] [command]```\n\n**Run command in foreground** and wait for completion\n\n```sem --fg [command]```\n\n**Wait for all commands** in a semaphore to complete\n\n```sem --id [myid] --wait```\n\n**Run jobs matching CPU core count** plus 2\n\n```sem -j +2 [command]```\n\n**Set a timeout** to acquire semaphore\n\n```sem --semaphore-timeout [10] [command]```\n\n**Parallel compression** of log files\n\n```for i in *.log; do sem -j+0 gzip $i; done; sem --wait```"],["SYNOPSIS","**sem** [**--fg**] [**--id** _id_] [**--semaphore-timeout** _secs_] [**-j** _num_] [**--wait**] _command_"],["PARAMETERS","**--bg**\n> Run command in background. This is the default behavior. sem will not wait for the command to complete before exiting.\n\n**--fg**\n> Run command in foreground. sem waits for the semaphore to be available, runs the command, waits for it to complete, then exits.\n\n**-j** _N_\n> Allow up to N commands to run in parallel. Default is 1 (mutex mode). Use **-j+N** to add N to CPU core count, **-j-N** to subtract, or **-jN%** to use N% of cores.\n\n**--id** _name_, **--semaphorename** _name_\n> Use name as the semaphore identifier. Default is the name of the controlling tty. Commands with the same id share the same semaphore.\n\n**--semaphore-timeout** _secs_\n> If secs > 0: force semaphore acquisition after timeout. If secs < 0: exit if semaphore not available within timeout.\n\n**--wait**\n> Wait for all commands in this semaphore to complete before exiting.\n\n**--pipe**\n> Pass standard input to the command.\n\n**--help**\n> Display help information.\n\n**--version**\n> Display version information."],["DESCRIPTION","**sem** is an alias for **GNU parallel --semaphore**. It acts as a counting semaphore for executing shell commands in parallel with controlled concurrency. When the maximum number of parallel jobs is reached, sem waits for one to complete before starting the next command.\n\nsem is useful for limiting concurrent access to resources, parallelizing loops in shell scripts while preventing system overload, and implementing mutual exclusion for processes that cannot run simultaneously. The semaphore state is stored in **~/.parallel/semaphores/**.\n\nUnlike GNU parallel, sem does not parse the command arguments for job construction; it simply executes the provided command when the semaphore permits."],["CAVEATS","The semaphore identifier defaults to the controlling tty, so commands run from different terminals will use different semaphores unless **--id** is specified. Stale semaphores may remain in **~/.parallel/semaphores/** if processes are killed abnormally. Requires Perl and several Perl modules (Getopt::Long, Symbol, Fcntl)."],["HISTORY","**sem** is part of GNU Parallel, created and maintained by Ole Tange. GNU Parallel was first released in **2010** as a tool for executing jobs in parallel using multiple CPU cores. The semaphore functionality (sem) was included to provide a simple way to limit concurrent execution of shell commands. The project is developed under the GNU Project and continues active maintenance."],["INSTALL","```dnf: sudo dnf install parallel```\n\n```pacman: sudo pacman -S parallel```\n\n```apk: sudo apk add parallel```\n\n```brew: brew install parallel```\n\n```nix: nix profile install nixpkgs#parallel```"],["SEE ALSO","[parallel](/man/parallel)(1), [xargs](/man/xargs)(1), [flock](/man/flock)(1), [nohup](/man/nohup)(1)"]],"e":["parallel","xargs","flock","nohup"],"x":"sem [command]"},"setsid":{"n":"setsid","t":"Run a program in a new session","s":[["TAGLINE","Run a program in a new session"],["TLDR","**Run** a program in a new session\n\n```setsid [program]```\n\n**Run** a program in a new session discarding output and errors\n\n```setsid [program] > /dev/null 2>&1```\n\n**Run** a program creating a new process (fork)\n\n```setsid -f [program]```\n\n**Return** the exit code of the program as setsid's exit code\n\n```setsid -w [program]```\n\n**Run** a program with the current terminal as the controlling terminal\n\n```setsid -c [program]```"],["SYNOPSIS","**setsid** [_options_] _program_ [_arguments_]"],["PARAMETERS","**-c, --ctty**\n> Set the controlling terminal to the current one\n\n**-f, --fork**\n> Always fork, creating a new process\n\n**-w, --wait**\n> Wait for the program to exit and return its exit status\n\n**-h, --help**\n> Display help information\n\n**-V, --version**\n> Display version information"],["DESCRIPTION","**setsid** runs a program in a new session. A session is a collection of process groups, and a new session has no controlling terminal by default. The calling process becomes the session leader.\n\nThis is commonly used to detach programs from the terminal, run daemons, or ensure a process continues running after logout. When combined with output redirection, it effectively backgrounds a process independently."],["CAVEATS","If the calling process is already a session leader, setsid will fail unless **-f** is used. The new session has no controlling terminal unless **-c** is specified. Part of the util-linux package."],["INSTALL","```apt: sudo apt install util-linux```\n\n```dnf: sudo dnf install util-linux```\n\n```pacman: sudo pacman -S util-linux```\n\n```apk: sudo apk add util-linux-misc```\n\n```zypper: sudo zypper install util-linux```\n\n```brew: brew install util-linux```\n\n```nix: nix profile install nixpkgs#util-linux```"],["SEE ALSO","[nohup](/man/nohup)(1), [disown](/man/disown)(1), [screen](/man/screen)(1), [tmux](/man/tmux)(1), [setsid](/man/setsid)(2)"]],"e":["nohup","disown","screen","tmux","setsid"],"x":"setsid [program]"},"shot-scraper":{"n":"shot-scraper","t":"Automated website screenshot tool","s":[["TAGLINE","Automated website screenshot tool"],["TLDR","**Take a screenshot** of a webpage\n\n```shot-scraper [https://example.com]```\n\n**Take a screenshot** with custom dimensions\n\n```shot-scraper [https://example.com] -w [800] -h [600] -o [output.png]```\n\n**Capture a specific element** by CSS selector\n\n```shot-scraper [https://example.com] -s '[.header]' -o [header.png]```\n\n**Take a screenshot** with JavaScript execution before capture\n\n```shot-scraper [https://example.com] --javascript \"[document.body.style.backgroundColor = 'white']\"```\n\n**Take a retina screenshot** (2x scale)\n\n```shot-scraper [https://example.com] --retina -o [output.png]```\n\n**Wait for element** before taking screenshot\n\n```shot-scraper [https://example.com] --wait-for \"[document.querySelector('.loaded')]\"```\n\n**Save as JPEG** with quality setting\n\n```shot-scraper [https://example.com] -o [output.jpg] --quality [80]```\n\n**Interactive mode** to manually navigate before capture\n\n```shot-scraper [https://example.com] --interactive```"],["SYNOPSIS","**shot-scraper** _URL_ [_options_]\n\n**shot-scraper** **install**\n\n**shot-scraper** **javascript** _URL_ _script_ [_options_]\n\n**shot-scraper** **pdf** _URL_ [_options_]\n\n**shot-scraper** **accessibility** _URL_ [_options_]\n\n**shot-scraper** **har** _URL_ [_options_]"],["PARAMETERS","**-o**, **--output** _FILE_\n> Output filename. Use **-** for stdout.\n\n**-w**, **--width** _PIXELS_\n> Browser viewport width. Default: 1280.\n\n**-h**, **--height** _PIXELS_\n> Browser viewport height. Default: full page.\n\n**-s**, **--selector** _SELECTOR_\n> CSS selector to capture instead of full page.\n\n**--selector-all** _SELECTOR_\n> Capture all elements matching the selector.\n\n**--padding** _PIXELS_\n> Add padding around selected elements.\n\n**--js-selector** _EXPRESSION_\n> JavaScript expression to filter elements.\n\n**--wait** _MS_\n> Milliseconds to wait after page load.\n\n**--wait-for** _EXPRESSION_\n> Wait until JavaScript expression is truthy.\n\n**-j**, **--javascript** _CODE_\n> Execute JavaScript before taking screenshot.\n\n**--quality** _PERCENT_\n> Save as JPEG with specified quality (1-100).\n\n**--retina**\n> Use device scale factor of 2.\n\n**--scale-factor** _N_\n> Custom device pixel ratio.\n\n**--omit-background**\n> Transparent background (PNG only).\n\n**-b**, **--browser** _NAME_\n> Browser to use: chromium, firefox, webkit, chrome.\n\n**--browser-arg** _ARG_\n> Pass command-line argument to browser.\n\n**--user-agent** _STRING_\n> Custom User-Agent header.\n\n**-i**, **--interactive**\n> Pause for manual interaction before capture.\n\n**--bypass-csp**\n> Bypass Content-Security-Policy restrictions.\n\n**--log-requests** _FILE_\n> Log HTTP requests as newline-delimited JSON.\n\n**--timeout** _MS_\n> Timeout in milliseconds before failing."],["DESCRIPTION","**shot-scraper** is a command-line utility for taking automated screenshots of websites, built on Playwright. It can capture full pages, specific elements, or execute JavaScript before capturing. The tool is designed for documentation, automated testing, and web scraping workflows.\n\nBeyond screenshots, shot-scraper can execute JavaScript on pages and return results, generate PDFs, export accessibility trees, and capture HAR (HTTP Archive) files. It integrates well with CI/CD pipelines, particularly GitHub Actions, for generating regularly updated screenshots.\n\nBefore first use, run **shot-scraper install** to download the required browser binaries."],["CAVEATS","Requires Python and Playwright browser binaries (installed via **shot-scraper install**). Screenshots default to 1280px width and full page height, which can result in large files for long pages. JavaScript execution timing may require adjustment with **--wait** or **--wait-for** for dynamically loaded content."],["HISTORY","**shot-scraper** was created by Simon Willison and first released in **2022**. It was designed to automate documentation screenshots and web scraping tasks using Playwright's browser automation capabilities. The tool gained popularity for generating automated screenshots in CI/CD pipelines, with notable users including Reuters for data dashboards. Development continues actively with regular releases adding new features."],["INSTALL","```nix: nix profile install nixpkgs#shot-scraper```"],["SEE ALSO","[playwright](/man/playwright)(1), [puppeteer](/man/puppeteer)(1), [chromium](/man/chromium)(1), [firefox](/man/firefox)(1)"]],"e":["playwright","puppeteer","chromium","firefox"],"x":"shot-scraper [https://example.com]"},"sncli":{"n":"sncli","t":"Terminal client for Simplenote","s":[["TAGLINE","Terminal client for Simplenote"],["TLDR","**Launch the interactive console GUI**\n\n```sncli```\n\n**Synchronize notes with Simplenote**\n\n```sncli sync```\n\n**List all notes**\n\n```sncli list```\n\n**Search notes with a regex pattern**\n\n```sncli -r list \"[pattern]\"```\n\n**Create a note with a title**\n\n```sncli -t \"[My Note Title]\" create```\n\n**Edit a note by key**\n\n```sncli -k [note_key] edit```\n\n**Dump a note to stdout**\n\n```sncli -k [note_key] dump```\n\n**Export a note as JSON**\n\n```sncli -k [note_key] export```"],["SYNOPSIS","**sncli** [_options_] [_command_] [_args_]"],["PARAMETERS","**-n**, **--nosync**\n> Do not perform a server sync on startup.\n\n**-r**, **--regex**\n> Treat search string as a regular expression.\n\n**-k** _KEY_, **--key** _KEY_\n> Specify note key for commands that operate on a single note.\n\n**-t** _TITLE_, **--title** _TITLE_\n> Title of note for create command.\n\n**-c** _FILE_, **--config** _FILE_\n> Specify configuration file.\n\n**-v**, **--verbose**\n> Verbose output."],["DESCRIPTION","**sncli** provides both a console GUI with Vi-like keybindings and a scriptable CLI for accessing your Simplenote account. It performs full two-way sync in the background and supports offline editing with automatic re-sync. Features include Google-style and regex search, note pinning, tagging, and fully customizable colors and keybindings.\n\nAvailable commands include: **sync**, **list**, **dump**, **create**, **edit**, **export**, **import**, **trash**, **untrash**, **pin**, **unpin**, **markdown**, **unmarkdown**, and **tag** (get/set/add/rm)."],["HISTORY","**sncli** was created by **insanum** and is written in **Python**."],["INSTALL","```aur: yay -S sncli```"],["SEE ALSO","[nb](/man/nb)(1), [joplin](/man/joplin)(1), [rucola](/man/rucola)(1)"]],"e":["nb","joplin","rucola"],"x":"sncli"},"sslyze":{"n":"sslyze","t":"Fast and powerful SSL/TLS server scanning tool","s":[["TAGLINE","Fast and powerful SSL/TLS server scanning tool"],["TLDR","**Scan a server** for SSL/TLS configuration\n\n```sslyze [www.example.com]```\n\n**Scan multiple servers**\n\n```sslyze [www.example.com] [www.google.com]```\n\n**Test for specific SSL/TLS versions**\n\n```sslyze --sslv2 --sslv3 --tlsv1 --tlsv1_1 --tlsv1_2 --tlsv1_3 [server]```\n\n**Test for Heartbleed vulnerability**\n\n```sslyze --heartbleed [server]```\n\n**Test for all vulnerabilities**\n\n```sslyze --heartbleed --robot --openssl_ccs --compression [server]```\n\n**Check against Mozilla TLS configuration**\n\n```sslyze --mozilla_config=[intermediate] [server]```\n\n**Use STARTTLS** for mail server\n\n```sslyze --starttls [smtp] [mail.example.com]```\n\n**Output to JSON**\n\n```sslyze --json_out=[results.json] [server]```"],["SYNOPSIS","**sslyze** [_options_] _target_ [_target_...]"],["PARAMETERS","**--sslv2**, **--sslv3**\n> Test for SSL 2.0 or SSL 3.0 support.\n\n**--tlsv1**, **--tlsv1_1**, **--tlsv1_2**, **--tlsv1_3**\n> Test for specific TLS version support.\n\n**--certinfo**\n> Retrieve and analyze server certificate.\n\n**--heartbleed**\n> Test for OpenSSL Heartbleed vulnerability.\n\n**--robot**\n> Test for ROBOT vulnerability.\n\n**--openssl_ccs**\n> Test for OpenSSL CCS Injection vulnerability (CVE-2014-0224).\n\n**--compression**\n> Test for TLS compression support (CRIME attack vector).\n\n**--reneg**\n> Test for insecure TLS renegotiation.\n\n**--fallback**\n> Test for TLS_FALLBACK_SCSV downgrade prevention.\n\n**--early_data**\n> Test for TLS 1.3 early data support.\n\n**--starttls** _protocol_\n> Use STARTTLS handshake. Protocols: auto, smtp, xmpp, pop3, imap, ftp, ldap, rdp, postgres.\n\n**--sni** _hostname_\n> Server Name Indication hostname for TLS 1.0+.\n\n**--mozilla_config** _level_\n> Check compliance: **old**, **intermediate**, or **modern**.\n\n**--json_out** _file_\n> Output results to JSON file.\n\n**--targets_in** _file_\n> Read targets from file (one per line).\n\n**--quiet**\n> Suppress output except errors."],["DESCRIPTION","**SSLyze** is a fast SSL/TLS scanning tool that analyzes server configurations to identify security weaknesses. It tests for protocol support, cipher suites, certificate validity, and known vulnerabilities like Heartbleed, ROBOT, and CRIME.\n\nThe tool connects directly to servers and performs real protocol handshakes to verify configurations. It supports STARTTLS for protocols that upgrade to TLS (SMTP, IMAP, etc.) and Server Name Indication for virtual hosting.\n\nSSLyze can check configurations against Mozilla's recommended TLS settings, returning non-zero exit codes for non-compliant servers. This makes it suitable for CI/CD pipeline integration."],["CAVEATS","Scanning servers you don't own may be considered unauthorized testing. Some scans may trigger security alerts on monitored systems. The tool requires network connectivity to target servers. Vulnerability tests are point-in-time assessments; retest after server changes."],["HISTORY","**SSLyze** was created by Alban Diquet (nabla-c0d3) and released as open source. It has become a standard tool for TLS security assessment, used to scan hundreds of thousands of servers. The project is actively maintained with support for new TLS features and vulnerabilities. It's distributed under the GNU Affero General Public License (AGPL)."],["INSTALL","```aur: yay -S sslyze```"],["SEE ALSO","[testssl](/man/testssl)(1), [nmap](/man/nmap)(1), [openssl](/man/openssl)(1), [sslscan](/man/sslscan)(1)"]],"e":["testssl","nmap","openssl","sslscan"],"x":"sslyze [www.example.com]"},"tailscale-set":{"n":"tailscale-set","t":"Configure persistent Tailscale options","s":[["TAGLINE","Configure persistent Tailscale options"],["TLDR","**Accept routes** advertised by other tailnet nodes\n\n```tailscale set --accept-routes```\n\n**Advertise this device as an exit node**\n\n```tailscale set --advertise-exit-node```\n\n**Set the device hostname**\n\n```tailscale set --hostname [name]```\n\n**Allow a non-root user to control tailscaled**\n\n```tailscale set --operator=[$USER]```\n\n**Route this device's traffic through an exit node**\n\n```tailscale set --exit-node [node-name-or-ip]```"],["SYNOPSIS","**tailscale** **set** [_options_]"],["PARAMETERS","**--accept-routes**\n> Accept subnet routes advertised by other nodes.\n\n**--accept-dns**\n> Accept DNS configuration from the admin console.\n\n**--advertise-routes** _CIDRS_\n> Expose physical subnet routes to the tailnet.\n\n**--advertise-exit-node**\n> Offer this node as an exit node.\n\n**--advertise-tags** _TAGS_\n> Apply ACL tags to this device.\n\n**--exit-node** _NODE_\n> Route traffic through the named exit node (empty string clears it).\n\n**--exit-node-allow-lan-access**\n> Allow LAN access while connected to an exit node.\n\n**--hostname** _NAME_\n> Override the device hostname.\n\n**--shields-up**\n> Block all incoming connections.\n\n**--ssh**\n> Enable Tailscale SSH on this device.\n\n**--operator** _USER_\n> Allow the named local user to operate **tailscaled** without sudo.\n\n**--auto-update**\n> Enable automatic Tailscale client updates (where supported)."],["DESCRIPTION","**tailscale set** changes per-node Tailscale options without disrupting the existing connection. Unlike **tailscale up**, which resets every unspecified flag back to its default, **set** modifies only the flags you pass — making it the safer choice for incremental changes."],["INSTALL","```dnf: sudo dnf install tailscale```\n\n```pacman: sudo pacman -S tailscale```\n\n```apk: sudo apk add tailscale```\n\n```zypper: sudo zypper install tailscale```\n\n```brew: brew install tailscale```\n\n```nix: nix profile install nixpkgs#tailscale```"],["SEE ALSO","[tailscale](/man/tailscale)(1), [tailscale-up](/man/tailscale-up)(1)"]],"e":["tailscale","tailscale-up"],"x":"tailscale set --accept-routes"},"timidity":{"n":"timidity","t":"Software MIDI synthesizer and converter","s":[["TAGLINE","Software MIDI synthesizer and converter"],["TLDR","**Play a MIDI file**\n\n```timidity [path/to/file.mid]```\n\n**Play with ncurses interface**\n\n```timidity -in [path/to/file.mid]```\n\n**Convert MIDI to WAV**\n\n```timidity -Ow -o [output.wav] [input.mid]```\n\n**Convert MIDI to WAV on stdout** for piping\n\n```timidity -Ow -o - [input.mid]```\n\n**Play with increased volume**\n\n```timidity -A [150] [path/to/file.mid]```\n\n**Run as ALSA sequencer client**\n\n```timidity -iA```\n\n**Play multiple files** unloading instruments between each\n\n```timidity -U [file1.mid] [file2.mid]```"],["SYNOPSIS","**timidity** [_options_] _file_ [_file_...]"],["PARAMETERS","**-A** _n_\n> Set master volume amplification to n%. Default is 70%, range is 0-800.\n\n**-a**\n> Enable anti-aliasing. Runs samples through a lowpass filter to reduce aliasing noise.\n\n**-B** _n_,_m_\n> Set buffer fragments to n and fragment size to 2^m bytes for audio driver.\n\n**-i** _mode_\n> Interface mode: d (dumb), n (ncurses), g (GTK+), r (MIDI server), A (ALSA sequencer).\n\n**-o** _file_\n> Output to file. Use - for stdout.\n\n**-O** _mode_\n> Output mode: d (audio device), w (RIFF WAVE), a (AIFF), r (raw), u (Sun audio), v (Ogg Vorbis), F (FLAC), l (list MIDI events), M (MOD to MIDI conversion).\n\n**-s** _freq_\n> Set sampling frequency in Hz. Default is 44100.\n\n**-F**\n> Enable fast panning, allowing panning adjustments to affect notes already playing.\n\n**-f**\n> Enable fast envelopes, improving performance at the cost of shorter note release times.\n\n**-p** _voices_\n> Set maximum number of simultaneous voices (polyphony). Default is system-dependent.\n\n**-U**\n> Unload instruments from memory between files to reduce memory usage.\n\n**-c** _file_\n> Read additional configuration file.\n\n**-L** _path_\n> Add path to search for patch files.\n\n**-E** _mode_\n> Set TiMidity++ extension modes for effects and reverb.\n\n**-Q** _channels_\n> Ignore MIDI channels. Comma-separated list of channel numbers (1-16) to mute.\n\n**-T** _n_\n> Adjust tempo by n%. Default is 100.\n\n**-h**\n> Display help message and list of available output modes and interfaces."],["DESCRIPTION","**TiMidity++** is a software MIDI synthesizer that converts MIDI files to audio using SoundFont or GUS patch instruments. It can play MIDI files in real time through an audio device or convert them to audio formats like WAV, AIFF, or raw PCM.\n\nThe program supports Standard MIDI files (.mid), Recomposer files (.rcp, .r36, .g18, .g36), and module files (.mod). It uses Gravis Ultrasound-compatible patch files or SoundFonts (.sf2) to synthesize audio from MIDI data.\n\nTiMidity++ offers multiple interfaces including a dumb terminal interface, ncurses-based interface, GTK+ GUI, and can run as an ALSA sequencer client or MIDI server. This flexibility makes it useful for both playback and as a system-wide MIDI synthesizer."],["CONFIGURATION","**/etc/timidity/timidity.cfg**\n> System-wide configuration specifying SoundFont and patch file paths, default output mode, and audio settings.\n\n**~/.timidity.cfg**\n> Per-user configuration overrides.\n\n**-c** _file_\n> Load an additional configuration file at runtime."],["CAVEATS","TiMidity++ requires SoundFont or patch files to produce sound. Without properly configured instrument files in /etc/timidity/timidity.cfg (or similar), no audio will be generated. Quality and CPU usage depend significantly on the SoundFont files used."],["HISTORY","TiMidity was originally written by **Tuukka Toivonen** in **1995**. The enhanced version **TiMidity++** was developed by **Masanao Izumo** starting in **1999**, adding significant features including multiple output modes, real-time MIDI synthesis, and various user interfaces. It became a popular solution for MIDI playback on Linux systems without hardware synthesizers."],["INSTALL","```apt: sudo apt install timidity```\n\n```zypper: sudo zypper install timidity```\n\n```brew: brew install timidity```\n\n```nix: nix profile install nixpkgs#timidity```"],["SEE ALSO","[fluidsynth](/man/fluidsynth)(1), [aplaymidi](/man/aplaymidi)(1), [aplay](/man/aplay)(1), [pmidi](/man/pmidi)(1)"]],"e":["fluidsynth","aplaymidi","aplay","pmidi"],"x":"timidity [path/to/file.mid]"},"torsocks":{"n":"torsocks","t":"Transparently route traffic through Tor","s":[["TAGLINE","Transparently route traffic through Tor"],["TLDR","**Run** command using Tor\n\n```torsocks [command]```\n\n**Toggle** Tor in shell\n\n```. torsocks [on|off]```\n\nSpawn Tor-enabled **shell**\n\n```torsocks --shell```\n\n**Check** if Tor is enabled\n\n```torsocks show```\n\n**Isolate** traffic through different circuit\n\n```torsocks -i [curl https://check.torproject.org/api/ip]```\n\nConnect to specific **proxy**\n\n```torsocks -a [ip_address] -P [port] [command]```"],["SYNOPSIS","**torsocks** [_OPTIONS_] [_COMMAND_ [_ARGS_...]]"],["PARAMETERS","**--shell**\n> Spawn a new Tor-enabled shell\n\n**-i, --isolate**\n> Isolate traffic through a different Tor circuit\n\n**-a, --address** _IP_\n> Connect to Tor proxy at specified address\n\n**-P, --port** _PORT_\n> Connect to Tor proxy on specified port\n\n**show**\n> Check if current shell is Tor-enabled\n\n**on|off**\n> Toggle Tor routing in current shell"],["DESCRIPTION","**torsocks** routes network traffic from applications through the Tor network by intercepting network calls and redirecting them through Tor's SOCKS proxy. By default, it connects to the Tor daemon running at 127.0.0.1:9050.\n\nThe tool uses LD_PRELOAD to intercept network system calls, providing transparent Tor routing for applications that don't natively support SOCKS proxies."],["CAVEATS","Requires running Tor daemon. Some applications may leak DNS or use protocols incompatible with Tor. UDP traffic is not supported. Applications using raw sockets bypass torsocks."],["HISTORY","**torsocks** was developed as an improved replacement for **torify**, providing better security and functionality for routing traffic through the Tor anonymity network."],["INSTALL","```dnf: sudo dnf install torsocks```\n\n```pacman: sudo pacman -S torsocks```\n\n```apk: sudo apk add torsocks```\n\n```brew: brew install torsocks```\n\n```nix: nix profile install nixpkgs#torsocks```"],["SEE ALSO","[tor](/man/tor)(1), [torify](/man/torify)(1)"]],"e":["tor","torify"],"x":"torsocks [command]"},"tune.exfat":{"n":"tune.exfat","t":"Adjust exFAT filesystem parameters","s":[["TAGLINE","Adjust exFAT filesystem parameters"],["TLDR","Print volume **label**\n\n```tune.exfat -l [/dev/sdXY]```\n\nSet volume **label**\n\n```tune.exfat -L [new_label] [/dev/sdXY]```\n\nPrint volume **GUID**\n\n```tune.exfat -u [/dev/sdXY]```\n\nSet volume **GUID**\n\n```tune.exfat -U [new_guid] [/dev/sdXY]```\n\nPrint volume **serial**\n\n```tune.exfat -i [/dev/sdXY]```\n\nSet volume **serial**\n\n```tune.exfat -I [new_serial] [/dev/sdXY]```"],["SYNOPSIS","**tune.exfat** [_OPTIONS_] _DEVICE_"],["PARAMETERS","**-l, --print-label**\n> Print volume label\n\n**-L, --set-label** _LABEL_\n> Set volume label\n\n**-u, --print-guid**\n> Print volume GUID\n\n**-U, --set-guid** _GUID_\n> Set volume GUID\n\n**-i, --print-serial**\n> Print volume serial number\n\n**-I, --set-serial** _SERIAL_\n> Set volume serial number"],["DESCRIPTION","**tune.exfat** adjusts tunable filesystem parameters on an exFAT filesystem. It can modify the volume label, GUID, and serial number without reformatting the filesystem.\n\nThe device must be unmounted before modifying parameters. This tool is useful for changing volume identification without losing data."],["CAVEATS","Filesystem must be unmounted for modifications. Requires root privileges. Incorrect GUID or serial changes may affect system recognition of the volume."],["INSTALL","```apt: sudo apt install exfatprogs```\n\n```dnf: sudo dnf install exfatprogs```\n\n```pacman: sudo pacman -S exfatprogs```\n\n```apk: sudo apk add exfatprogs```\n\n```zypper: sudo zypper install exfatprogs```\n\n```nix: nix profile install nixpkgs#exfatprogs```"],["SEE ALSO","[mkfs.exfat](/man/mkfs.exfat)(8), [fsck.exfat](/man/fsck.exfat)(8), [dump.exfat](/man/dump.exfat)(8)"]],"e":["mkfs.exfat","fsck.exfat","dump.exfat"],"x":"tune.exfat -l [/dev/sdXY]"},"uprecords":{"n":"uprecords","t":"Display historical system uptime records","s":[["TAGLINE","Display historical system uptime records"],["TLDR","Display **top 10** uptime records\n\n```uprecords```\n\nShow **top N** records\n\n```uprecords -m [25]```\n\nShow **downtime** between reboots\n\n```uprecords -d```\n\nSort by **boot time** (most recent first)\n\n```uprecords -B```\n\nShow **all** records\n\n```uprecords -a```\n\nRun **continuously** in a loop\n\n```uprecords -f```\n\nShow next **milestone**\n\n```uprecords -M```"],["SYNOPSIS","**uprecords** [**-?abBcdfkKMsv**] [**-i** _interval_] [**-m** _count_]"],["PARAMETERS","**-m** _N_\n> Display top N records instead of the default 10.\n\n**-a**\n> Show all records.\n\n**-b**\n> Sort by boot time, oldest first. Implies -s.\n\n**-B**\n> Sort by boot time, newest first. Implies -s.\n\n**-c**\n> Do not show current entry if not in top entries.\n\n**-d**\n> Show downtime between reboots instead of kernel version.\n\n**-f**\n> Run continuously in a loop (update every 5 seconds).\n\n**-i** _INTERVAL_\n> Use INTERVAL seconds for loop instead of 5. Implies -f.\n\n**-k**\n> Sort by kernel/system name. Implies -s.\n\n**-K**\n> Sort by kernel/system name, reversed. Implies -s.\n\n**-M**\n> Show next milestone.\n\n**-s**\n> Do not print extra statistics.\n\n**-w**\n> Do not truncate output.\n\n**-v**\n> Show version information."],["DESCRIPTION","**uprecords** displays a summary of historical uptime records collected by the uptimed(8) daemon. It tracks system uptime across reboots and shows statistics about the longest running periods.\n\nThe tool requires the uptimed daemon to be running to collect uptime data. By default, it shows the top 10 uptime records with extra statistics."],["CAVEATS","Requires uptimed daemon to be running. Historical data depends on how long uptimed has been active. New installations have no historical data."],["INSTALL","```apt: sudo apt install uptimed```\n\n```dnf: sudo dnf install uptimed```\n\n```pacman: sudo pacman -S uptimed```\n\n```apk: sudo apk add uptimed```\n\n```brew: brew install uptimed```\n\n```nix: nix profile install nixpkgs#uptimed```"],["SEE ALSO","[uptime](/man/uptime)(1), [uptimed](/man/uptimed)(8)"]],"e":["uptime","uptimed"],"x":"uprecords"},"wl-paste":{"n":"wl-paste","t":"Paste data from Wayland clipboard","s":[["TAGLINE","Paste data from Wayland clipboard"],["TLDR","**Paste** the contents of the clipboard\n\n```wl-paste```\n\nPaste from the **primary** clipboard (highlighted text)\n\n```wl-paste --primary```\n\n**Write** clipboard contents to a file\n\n```wl-paste > [path/to/file]```\n\nPaste without trailing **newline**\n\n```wl-paste --no-newline```\n\nList available **MIME types** in clipboard\n\n```wl-paste --list-types```\n\nPaste clipboard as a specific **MIME type** (e.g., image)\n\n```wl-paste --type image/png > [path/to/image.png]```\n\n**Pipe** clipboard contents to a command\n\n```wl-paste | [command]```\n\nRun a command each time the **clipboard changes**\n\n```wl-paste --watch [command]```"],["SYNOPSIS","**wl-paste** [_options_]"],["PARAMETERS","**-p**, **--primary**\n> Use the primary selection instead of clipboard\n\n**-n**, **--no-newline**\n> Do not append newline character\n\n**-t**, **--type** _mime-type_\n> Request specific MIME type\n\n**-l**, **--list-types**\n> List available MIME types\n\n**-s**, **--seat** _seat_\n> Pick the seat to work with\n\n**-w**, **--watch** _command_ [_args_...]\n> Run command each time clipboard changes\n\n**-v**, **--version**\n> Display version information\n\n**-h**, **--help**\n> Display help message"],["DESCRIPTION","**wl-paste** retrieves data from the Wayland clipboard and writes it to stdout. It can request specific MIME types for clipboard data, supporting both text and binary content like images.\n\nThe primary selection (text selected with mouse) can be accessed with **--primary**, enabling middle-click paste functionality. The watch mode allows monitoring clipboard changes."],["CAVEATS","Only works under Wayland; use **xclip** or **xsel** for X11. MIME type availability depends on what the copying application provides. Some applications may not populate the primary selection."],["HISTORY","Part of **wl-clipboard**, written by Sergey Bugaev for Wayland-native clipboard access. Provides the paste counterpart to wl-copy, implementing the Wayland data-device protocol for clipboard reading."],["INSTALL","```apt: sudo apt install wl-clipboard```\n\n```dnf: sudo dnf install wl-clipboard```\n\n```pacman: sudo pacman -S wl-clipboard```\n\n```apk: sudo apk add wl-clipboard```\n\n```zypper: sudo zypper install wl-clipboard```\n\n```brew: brew install wl-clipboard```\n\n```nix: nix profile install nixpkgs#wl-clipboard```"],["SEE ALSO","[wl-copy](/man/wl-copy)(1), [xclip](/man/xclip)(1), [xsel](/man/xsel)(1)"]],"e":["wl-copy","xclip","xsel"],"x":"wl-paste"},"xlogo":{"n":"xlogo","t":"Display X Window System logo","s":[["TAGLINE","Display X Window System logo"],["TLDR","**Display X logo**\n\n```xlogo```\n\n**Display with anti-aliased rendering**\n\n```xlogo -render```\n\n**Display with shaped (non-rectangular) window**\n\n```xlogo -shape```\n\n**Display with custom colors**\n\n```xlogo -fg [blue] -bg [white]```\n\n**Display with specific geometry**\n\n```xlogo -geometry [200x200+100+100]```\n\n**Display on remote display**\n\n```xlogo -display [hostname:0]```"],["SYNOPSIS","**xlogo** [_options_]"],["PARAMETERS","**-render**\n> Use RENDER extension for anti-aliased edges.\n\n**-sharp**\n> When used with -render, force sharp edges (1-bit alpha channel).\n\n**-shape**\n> Use non-rectangular window shape.\n\n**-fg** _color_\n> Foreground color (logo color).\n\n**-bg** _color_\n> Background color.\n\n**-geometry** _geom_\n> Window size and position.\n\n**-display** _name_\n> X display to use."],["DESCRIPTION","**xlogo** displays the X Window System logo in a window. It's a minimal X11 application used primarily for testing X server connectivity and verifying that display forwarding is working.\n\nThe program serves as a \"hello world\" equivalent for X11, making it useful for quickly testing if X is running and accessible. If xlogo fails to open, it indicates problems with the X server, DISPLAY variable, or network configuration.\n\nWith -render, the logo uses anti-aliased edges for smoother appearance. The -shape option creates a window that matches the logo outline rather than a rectangular window."],["CAVEATS","Minimal functionality by design. Requires running X server and correct DISPLAY setting. Legacy application; modern testing might use other methods."],["HISTORY","**xlogo** has been part of the X Window System since its early days, providing a simple visual test of X server functionality. Despite its simplicity, it remains useful for basic X11 connectivity testing and demonstrating X11 concepts."],["INSTALL","```apt: sudo apt install x11-apps```\n\n```dnf: sudo dnf install xlogo```\n\n```zypper: sudo zypper install xlogo```\n\n```nix: nix profile install nixpkgs#xlogo```"],["SEE ALSO","[xeyes](/man/xeyes)(1), [xclock](/man/xclock)(1), [xterm](/man/xterm)(1), [xdpyinfo](/man/xdpyinfo)(1)"]],"e":["xeyes","xclock","xterm","xdpyinfo"],"x":"xlogo"},"xwininfo":{"n":"xwininfo","t":"Display X11 window information","s":[["TAGLINE","Display X11 window information"],["TLDR","**Get window info by clicking** on it\n\n```xwininfo```\n\n**Get root window info**\n\n```xwininfo -root```\n\n**Get all info** for a window selected by clicking\n\n```xwininfo -all```\n\n**Get by window ID**\n\n```xwininfo -id [0x12345]```\n\n**Get by window name**\n\n```xwininfo -name \"[Window Name]\"```\n\n**Show the full window tree** recursively from root\n\n```xwininfo -root -tree```\n\n**Display window IDs as integers** instead of hexadecimal\n\n```xwininfo -int```\n\n**Show window manager hints** for a specific window\n\n```xwininfo -wm -id [0x12345]```"],["SYNOPSIS","**xwininfo** [_-help_] [_-id wid_] [_-root_] [_-name wdname_] [_-int_] [_-children_] [_-tree_] [_-stats_] [_-bits_] [_-events_] [_-size_] [_-wm_] [_-shape_] [_-frame_] [_-all_] [_-english_] [_-metric_] [_-display host:dpy_] [_-version_]"],["PARAMETERS","**-root**\n> Select the root window as the target without using the cursor.\n\n**-id** _wid_\n> Specify the target window by its X window ID.\n\n**-name** _wdname_\n> Specify the target window by name.\n\n**-int**\n> Display all X window IDs as integer values. Default is hexadecimal.\n\n**-children**\n> Display the root, parent, and children windows' IDs and names.\n\n**-tree**\n> Like -children, but display all children recursively.\n\n**-stats**\n> Display location and appearance attributes including size, position, depth, and visual info. This is the default if no options are specified.\n\n**-bits**\n> Display bit gravity, window gravity, backing-store, and save-under attributes.\n\n**-events**\n> Display the selected window's event masks.\n\n**-size**\n> Display sizing hints including minimum, maximum, and resize increment sizes.\n\n**-wm**\n> Display window manager hints including input model, icon info, and initial state.\n\n**-shape**\n> Display the window and border shape extents.\n\n**-frame**\n> Consider window manager frames when manually selecting windows.\n\n**-metric**\n> Display dimensions in millimeters as well as number of pixels.\n\n**-english**\n> Display dimensions in inches as well as number of pixels.\n\n**-all**\n> Show all available information.\n\n**-display** _host:dpy_\n> Specify the X server to connect to.\n\n**-version**\n> Print version information and exit."],["DESCRIPTION","**xwininfo** displays detailed information about X11 windows, including geometry (size and position), window ID, depth, visual class, and colormap. When run without arguments, it presents a crosshair cursor for interactive window selection by clicking.\n\nSpecific windows can be targeted using **-id** for a window ID, **-name** for a window title, or **-root** for the root window. The **-tree** option displays the complete window hierarchy, showing how windows are nested within each other. If no information option is specified, **-stats** is assumed. The tool is commonly used for debugging window managers and scripting X11 window operations."],["CAVEATS","X11 only. Does not work on Wayland. Part of the x11-utils package (or xorg-xwininfo on Arch)."],["HISTORY","**xwininfo** is a standard X11 utility for displaying information about windows."],["INSTALL","```apt: sudo apt install x11-utils```\n\n```dnf: sudo dnf install xwininfo```\n\n```apk: sudo apk add xwininfo```\n\n```zypper: sudo zypper install xwininfo```\n\n```brew: brew install xwininfo```\n\n```nix: nix profile install nixpkgs#xwininfo```"],["SEE ALSO","[xprop](/man/xprop)(1), [xdotool](/man/xdotool)(1), [xlsclients](/man/xlsclients)(1), [xdpyinfo](/man/xdpyinfo)(1), [xkill](/man/xkill)(1), [wmctrl](/man/wmctrl)(1)"]],"e":["xprop","xdotool","xlsclients","xdpyinfo","xkill","wmctrl"],"x":"xwininfo"},"zipgrep":{"n":"zipgrep","t":"Search inside ZIP archives","s":[["TAGLINE","Search inside ZIP archives"],["TLDR","**Search in ZIP**\n\n```zipgrep [pattern] [archive.zip]```\n\n**Search specific file**\n\n```zipgrep [pattern] [archive.zip] [file.txt]```\n\n**Case insensitive**\n\n```zipgrep -i [pattern] [archive.zip]```\n\n**Show line numbers**\n\n```zipgrep -n [pattern] [archive.zip]```\n\n**Count matches**\n\n```zipgrep -c [pattern] [archive.zip]```\n\n**List matching files only**\n\n```zipgrep -l [pattern] [archive.zip]```\n\n**Search but exclude files in subdirectories**\n\n```zipgrep [pattern] [archive.zip] *.[ch] -x */*```"],["SYNOPSIS","**zipgrep** [_egrep_options_] _pattern_ _file_[_.zip_] [_file(s)_] [**-x** _xfile(s)_]"],["PARAMETERS","**-i**\n> Case insensitive.\n\n**-n**\n> Show line numbers.\n\n**-c**\n> Count matches.\n\n**-l**\n> List files only.\n\n**-h**\n> No filename prefix.\n\n**-v**\n> Invert match.\n\n_pattern_\n> Search pattern.\n\n_members_\n> Specific files within the archive to search.\n\n**-x** _xfile(s)_\n> Exclude specified archive members from processing. Wildcards match directory separators."],["DESCRIPTION","**zipgrep** searches for text patterns inside ZIP archives without requiring manual extraction. It works as a shell wrapper around **unzip** and **egrep**, piping decompressed file contents through pattern matching to display results in a familiar grep-like format.\n\nPattern matching supports extended regular expressions through egrep. The output shows the matching filename and line for each hit, similar to running grep on extracted contents. Optional member arguments restrict the search to specific files within the archive.\n\nAll options before the ZIP archive filename are passed to egrep, so standard grep options like **-i** for case-insensitive matching, **-n** for line numbers, **-c** for match counts, and **-l** for listing matching filenames are all supported. If the literal filename is not found, the suffix .zip is appended. zipgrep is part of the Info-ZIP suite of utilities."],["CAVEATS","Shell wrapper around unzip/egrep. May be slow on large archives. Part of Info-ZIP."],["HISTORY","**zipgrep** is part of **Info-ZIP** utilities. It provides grep functionality for ZIP contents."],["INSTALL","```apt: sudo apt install unzip```\n\n```dnf: sudo dnf install unzip```\n\n```pacman: sudo pacman -S unzip```\n\n```apk: sudo apk add unzip```\n\n```zypper: sudo zypper install unzip```\n\n```brew: brew install unzip```\n\n```nix: nix profile install nixpkgs#unzip```"],["SEE ALSO","[unzip](/man/unzip)(1), [grep](/man/grep)(1), [zgrep](/man/zgrep)(1)"]],"e":["unzip","grep","zgrep"],"x":"zipgrep [pattern] [archive.zip]"},"zola":{"n":"zola","t":"Fast Rust-based static site generator","s":[["TAGLINE","Fast Rust-based static site generator"],["TLDR","**Create a new site**\n\n```zola init [site_name]```\n\n**Build the site**\n\n```zola build```\n\n**Start development server**\n\n```zola serve```\n\n**Serve on specific port**\n\n```zola serve -p [8080]```\n\n**Build with custom output directory**\n\n```zola build -o [public]```\n\n**Check for errors without building**\n\n```zola check```\n\n**Check site structure, skipping external link validation**\n\n```zola check --skip-external-links```\n\n**Serve with auto-open in browser**\n\n```zola serve --open```\n\n**Initialize site in non-empty directory**\n\n```zola init --force [site_name]```"],["SYNOPSIS","**zola** [_options_] _command_ [_arguments_]"],["SUBCOMMANDS","**init** _name_\n> Create new site scaffolding\n\n**build**\n> Build the static site\n\n**serve**\n> Start local development server with live reload\n\n**check**\n> Validate site without building\n\n**completion** _shell_\n> Generate shell completions"],["PARAMETERS","**-c**, **--config** _file_\n> Path to config file (default: config.toml)\n\n**-r**, **--root** _dir_\n> Site root directory\n\n**-o**, **--output-dir** _dir_\n> Output directory for build\n\n**-p**, **--port** _port_\n> Port for serve command (default: 1111)\n\n**-i**, **--interface** _addr_\n> Interface to bind server (default: 127.0.0.1)\n\n**-u**, **--base-url** _url_\n> Override base URL from config\n\n**--open**\n> Automatically open site in browser (serve only)\n\n**--force**\n> Overwrite existing output directory without prompting\n\n**--drafts**\n> Include draft content\n\n**--skip-external-links**\n> Skip external link validation (check only)"],["DESCRIPTION","**Zola** is a fast static site generator written in Rust. It combines content (Markdown files), templates (Tera templating), and configuration into a static website.\n\nThe site structure includes:\n- **content/** - Markdown files organized as sections\n- **templates/** - Tera HTML templates\n- **static/** - Static assets copied directly\n- **themes/** - Optional downloadable themes\n- **config.toml** - Site configuration\n\nZola features include syntax highlighting, automatic table of contents, taxonomies (tags, categories), shortcodes, and built-in Sass compilation.\n\nThe development server provides live reload, rebuilding automatically when files change."],["CAVEATS","Zola uses Tera templating, which differs from Hugo, Jekyll, or other static site generators. Template migration requires rewriting.\n\nNo plugin system. Extensibility is limited to shortcodes and templates.\n\nLarge sites with many pages may have noticeable build times, though Zola is generally faster than alternatives."],["HISTORY","**Zola** was created by **Vincent Prouillet** and originally named **Gutenberg**. It was renamed to Zola in **2018** (after Emile Zola). Written in Rust, it was designed as a single-binary alternative to static site generators like Hugo and Jekyll, emphasizing simplicity and build speed."],["INSTALL","```pacman: sudo pacman -S zola```\n\n```apk: sudo apk add zola```\n\n```zypper: sudo zypper install zola```\n\n```brew: brew install zola```\n\n```nix: nix profile install nixpkgs#zola```"],["SEE ALSO","[hugo](/man/hugo)(1), [jekyll](/man/jekyll)(1), [mdbook](/man/mdbook)(1)"]],"e":["hugo","jekyll","mdbook"],"x":"zola init [site_name]"}});
