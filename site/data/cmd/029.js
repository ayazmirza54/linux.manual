window.LCL_BUCKET(29,{"aa-update-browser":{"n":"aa-update-browser","t":"Manage browser abstraction includes in AppArmor profiles","s":[["TAGLINE","Manage browser abstraction includes in AppArmor profiles"],["TLDR","**List** available browser abstraction profiles\n\n```sudo aa-update-browser -l```\n\nShow what changes would be made **(dry-run)**\n\n```sudo aa-update-browser -d [path/to/profile]```\n\n**Update** a profile with specific abstractions\n\n```sudo aa-update-browser -u [abstraction1,abstraction2] [path/to/profile]```\n\n**Remove** all abstractions from a profile\n\n```sudo aa-update-browser -u '' [path/to/profile]```"],["SYNOPSIS","**aa-update-browser** [_option_] _profile_"],["DESCRIPTION","**aa-update-browser** updates AppArmor browser profiles to use supported abstractions. This utility helps maintain browser profiles by allowing administrators to add, modify, or remove browser-specific abstraction includes.\n\nBrowser abstractions provide pre-defined access rules for common browser operations, simplifying profile management."],["PARAMETERS","**-l**\n> Show the supported browser abstractions.\n\n**-d**\n> Dry-run. Only show what would be done to the given profile without applying changes.\n\n**-u** _ABSTRACTIONS_\n> Update the given profile with the comma-separated list of ABSTRACTIONS. Specifying '' removes all abstractions.\n\n**-h**\n> Display help information."],["CAVEATS","Modifying browser profiles may affect browser functionality. Test changes in complain mode before enforcing."],["HISTORY","Part of the **AppArmor** utilities package for managing application security profiles on Linux systems."],["SEE ALSO","[aa-logprof](/man/aa-logprof)(8), [aa-enforce](/man/aa-enforce)(8), [aa-complain](/man/aa-complain)(8), [apparmor](/man/apparmor)(7)"],["RESOURCES","```[Source code](https://gitlab.com/apparmor/apparmor)```\n\n```[Homepage](https://apparmor.net/)```\n\n```[Documentation](https://gitlab.com/apparmor/apparmor/-/wikis/home)```"]],"e":["aa-logprof","aa-enforce","aa-complain","apparmor"],"x":"sudo aa-update-browser -l"},"apptainer-cache":{"n":"apptainer-cache","t":"Manage the local Apptainer container cache","s":[["TAGLINE","Manage the local Apptainer container cache"],["TLDR","**List all cached images**\n\n```apptainer cache list```\n\n**List cached images with details**\n\n```apptainer cache list -v```\n\n**List only OCI cached images**\n\n```apptainer cache list -T oci```\n\n**Clean all cached images**\n\n```apptainer cache clean```\n\n**Clean cache without confirmation**\n\n```apptainer cache clean -f```\n\n**Clean cache entries older than 30 days**\n\n```apptainer cache clean -D 30```\n\n**Dry run to see what would be cleaned**\n\n```apptainer cache clean -n```"],["SYNOPSIS","**apptainer cache** [_subcommand_] [_options_]"],["DESCRIPTION","**apptainer cache** manages the local Apptainer container image cache. When pulling or building containers, Apptainer stores intermediate images and layers locally to speed up subsequent operations. This command allows listing cache contents and cleaning up disk space.\n\nThe cache is stored at **$HOME/.apptainer/cache** by default, or at the path specified by the **APPTAINER_CACHEDIR** environment variable."],["SUBCOMMANDS","**list**\n> Display the contents of the local cache, showing size and type of cached images\n\n**clean**\n> Remove items from the local cache to reclaim disk space"],["PARAMETERS","**-T, --type** _strings_\n> Limit operation to specific cache types: **library**, **oci**, **shub**, **blob**, **net**, **oras**, **all** (default: all)\n\n**-v, --verbose**\n> Display detailed information about cached images (list only)\n\n**-D, --days** _int_\n> Remove cache entries older than the specified number of days (clean only)\n\n**-n, --dry-run**\n> Show what would be deleted without actually removing anything (clean only)\n\n**-f, --force**\n> Suppress confirmation prompts and clean immediately (clean only)"],["CAVEATS","Large builds and frequent pulls can consume significant disk space in the cache directory. The cache is per-user and does not affect other users on the system. Cleaning the cache forces re-download of images on next use."],["INSTALL","```dnf: sudo dnf install apptainer```\n\n```pacman: sudo pacman -S apptainer```\n\n```apk: sudo apk add apptainer```\n\n```zypper: sudo zypper install apptainer```\n\n```brew: brew install apptainer```\n\n```nix: nix profile install nixpkgs#apptainer```"],["SEE ALSO","[apptainer](/man/apptainer)(1), [apptainer-build](/man/apptainer-build)(1), [apptainer-pull](/man/apptainer-pull)(1)"]],"e":["apptainer","apptainer-build","apptainer-pull"],"x":"apptainer cache list"},"arecord":{"n":"arecord","t":"Record audio from ALSA sound devices","s":[["TAGLINE","Record audio from ALSA sound devices"],["TLDR","Record in **CD quality** (finish with Ctrl+C)\n\n```arecord -vv -f cd [path/to/file.wav]```\n\nRecord with a **fixed duration** of 10 seconds\n\n```arecord -vv -f cd -d 10 [path/to/file.wav]```\n\nRecord and save as **MP3** (via lame)\n\n```arecord -vv -f cd -t raw | lame -r - [path/to/file.mp3]```\n\n**List** all sound cards and digital audio devices\n\n```arecord -l```\n\nAllow **interactive** interface\n\n```arecord -i```\n\n**Test microphone** with a 5 second sample\n\n```arecord -d 5 test-mic.wav && aplay test-mic.wav && rm test-mic.wav```"],["SYNOPSIS","**arecord** [_-d duration_] [_-f format_] [_-r rate_] [_-c channels_] [_file_]"],["DESCRIPTION","**arecord** is a command-line sound recorder for ALSA soundcard drivers. It captures audio from sound cards and saves it in various formats including WAV, AU, VOC, and raw audio. The tool supports configurable sample rates, bit depths, and channel counts. The shorthand format **cd** sets 16-bit signed little-endian stereo at 44100 Hz. Raw output can be piped to encoders like **lame** for MP3 conversion. It is the recording counterpart to **aplay**."],["PARAMETERS","**-d**, **--duration** _seconds_\n> Stop recording after the specified number of seconds.\n\n**-f**, **--format** _format_\n> Sample format (cd, S16_LE, S16_BE, U8, S32_LE, etc.).\n\n**-r**, **--rate** _rate_\n> Sampling rate in Hz (2000-192000).\n\n**-c**, **--channels** _count_\n> Number of channels (1 for mono, 2 for stereo).\n\n**-t**, **--file-type** _type_\n> File type (wav, raw, au, voc).\n\n**-l**, **--list-devices**\n> List all soundcards and digital audio devices.\n\n**-L**, **--list-pcms**\n> List all PCMs defined.\n\n**-D**, **--device** _name_\n> Select PCM device by name.\n\n**-M**, **--mmap**\n> Use memory-mapped I/O mode for the audio stream.\n\n**-N**, **--nonblock**\n> Open the audio device in non-blocking mode.\n\n**-F**, **--period-time** _microseconds_\n> Distance between interrupts in microseconds.\n\n**-B**, **--buffer-time** _microseconds_\n> Buffer duration in microseconds.\n\n**-V**, **--vumeter** _type_\n> VU meter type (stereo or mono).\n\n**-i**, **--interactive**\n> Allow interactive control via stdin.\n\n**-v**, **--verbose**\n> Verbose mode; use -vv for more detail including VU meter.\n\n**-q**, **--quiet**\n> Quiet mode; suppress messages.\n\n**--disable-resample**\n> Disable automatic rate resample.\n\n**--disable-channels**\n> Disable automatic channel conversions.\n\n**--disable-format**\n> Disable automatic format conversions."],["CAVEATS","Requires ALSA drivers to be installed and configured. The **cd** format is shorthand for 16-bit signed little-endian, 44100 Hz, stereo."],["HISTORY","Part of **ALSA** (Advanced Linux Sound Architecture) utilities package, which replaced OSS as the standard Linux sound system."],["INSTALL","```apt: sudo apt install alsa-utils```\n\n```dnf: sudo dnf install alsa-utils```\n\n```pacman: sudo pacman -S alsa-utils```\n\n```apk: sudo apk add alsa-utils```\n\n```zypper: sudo zypper install alsa-utils```\n\n```nix: nix profile install nixpkgs#alsa-utils```"],["SEE ALSO","[aplay](/man/aplay)(1), [alsamixer](/man/alsamixer)(1), [amixer](/man/amixer)(1)"]],"e":["aplay","alsamixer","amixer"],"x":"arecord -vv -f cd [path/to/file.wav]"},"audtool":{"n":"audtool","t":"Command-line interface for controlling the Audacious media player.","s":[["TAGLINE","Command-line interface for controlling the Audacious media player."],["TLDR","**Get** current song\n\n```audtool current-song```\n\n**Play/Pause** playback\n\n```audtool playback-playpause```\n\n**Skip** to next track\n\n```audtool playlist-advance```\n\nGet **playback status**\n\n```audtool playback-status```\n\nSet **volume**\n\n```audtool set-volume [75]```"],["SYNOPSIS","**audtool** [_instance_] _command_ [_parameter_ ...] ...\n\nCommands may optionally be prefixed with **--** (GNU long-option style); both **current-song** and **--current-song** work."],["DESCRIPTION","**audtool** is a command-line interface for controlling the Audacious media player. It allows remote control, playlist management, and status queries without opening the GUI.\n\nThe tool is useful for scripts, keybindings, and automation."],["PARAMETERS","**playback-play**\n> Start playback\n\n**playback-pause**\n> Pause playback\n\n**playback-playpause**\n> Toggle play/pause\n\n**playback-stop**\n> Stop playback\n\n**playback-status**\n> Show playback status\n\n**playlist-advance**\n> Next track\n\n**playlist-reverse**\n> Previous track\n\n**current-song**\n> Print current song info\n\n**current-song-filename**\n> Print filename of current song\n\n**current-song-length**\n> Print length of the current song in M:SS format\n\n**set-volume** _n_\n> Set volume (0-100)\n\n**get-volume**\n> Get current volume\n\n**playlist-length**\n> Number of tracks in playlist\n\n**playlist-song** _n_\n> Get song at position n"],["CAVEATS","Requires Audacious to be running. Commands fail if player is not active. Some features depend on Audacious version."],["HISTORY","**audtool** was developed alongside Audacious to provide scriptable control, continuing the tradition of beep-media-player-control and xmmsctrl."],["INSTALL","```apt: sudo apt install audacious```\n\n```dnf: sudo dnf install audacious```\n\n```pacman: sudo pacman -S audacious```\n\n```apk: sudo apk add audacious```\n\n```zypper: sudo zypper install audacious```\n\n```brew: brew install audacious```\n\n```nix: nix profile install nixpkgs#audacious```"],["SEE ALSO","[audacious](/man/audacious)(1), [playerctl](/man/playerctl)(1)"],["RESOURCES","```[Source code](https://github.com/audacious-media-player/audacious)```\n\n```[Homepage](https://audacious-media-player.org/)```"]],"e":["audacious","playerctl"],"x":"audtool current-song"},"backlit-kbd":{"n":"backlit-kbd","t":"Control keyboard backlight brightness on Linux","s":[["TAGLINE","Control keyboard backlight brightness on Linux"],["TLDR","**Show current backlight** state and device info\n\n```backlit-kbd info```\n\n**Set brightness** by percentage (0-100)\n\n```backlit-kbd percent [75]```\n\n**Set brightness** by raw level value\n\n```backlit-kbd set [value]```\n\n**Increase or decrease** brightness by a step\n\n```backlit-kbd inc [step]```\n\n```backlit-kbd dec [step]```\n\n**Turn backlight on** at a specific percentage\n\n```backlit-kbd on --percent [40]```\n\n**Turn backlight off**\n\n```backlit-kbd off```\n\n**Blink the keyboard** as a visual notification\n\n```backlit-kbd blink --count [4] --on-ms [100] --off-ms [100]```\n\n**Test without hardware** using the mock backend\n\n```backlit-kbd --mock percent [50]```"],["SYNOPSIS","**backlit-kbd** [_options_] _command_ [_args_]"],["PARAMETERS","**info**\n> Display current backlight state and detected device information.\n\n**set** _value_\n> Set the brightness to a raw level value supported by the device.\n\n**percent** _value_\n> Set the brightness as a percentage between 0 and 100.\n\n**inc** [_step_]\n> Increase brightness by _step_ (default: 1).\n\n**dec** [_step_]\n> Decrease brightness by _step_ (default: 1).\n\n**on** [**--percent** _N_]\n> Turn the backlight on, optionally at a given percentage.\n\n**off**\n> Turn the backlight off.\n\n**blink** [_options_]\n> Run a synchronous blink pattern on the keyboard.\n\n**notify** [_options_]\n> Run an asynchronous notification blink without blocking the shell.\n\n**--mock**\n> Use an in-memory backend for safe testing without writing to hardware.\n\n**--device-path** _PATH_\n> Target a specific sysfs device path instead of auto-discovery.\n\n**--count** _N_\n> Number of blink cycles (used with _blink_ / _notify_).\n\n**--on-ms** _N_\n> Duration in milliseconds for the on state during blink.\n\n**--off-ms** _N_\n> Duration in milliseconds for the off state during blink.\n\n**--level-percent** _N_\n> Brightness level during blink cycles.\n\n**--name** _NAME_\n> Notification identifier (used with _notify_)."],["DESCRIPTION","**backlit-kbd** is a Python command-line utility for controlling the keyboard backlight on Linux. It auto-discovers compatible LED devices exposed by the kernel under **/sys/class/leds/**, typically vendor-specific paths such as **tpacpi::kbd_backlight** (Lenovo ThinkPad), **asus::kbd_backlight** (ASUS), or **dell::kbd_backlight** (Dell).\n\nThe tool exposes high-level commands (_set_, _percent_, _inc_, _dec_, _on_, _off_) for everyday brightness control and a pair of _blink_ / _notify_ commands for producing visual notifications via the keyboard LEDs. A **--mock** flag swaps the backend for an in-memory simulator, making it safe to experiment or integrate into scripts on machines without a real backlight device."],["CONFIGURATION","Installed via **pip install backlit-kbd**. The tool reads brightness paths from **/sys/class/leds/*kbd_backlight*** and usually requires root permissions (or a udev rule granting write access) to change brightness levels. Use **--device-path** to override device auto-discovery."],["CAVEATS","Writing to sysfs brightness files typically requires **root** privileges. Not all laptops expose a keyboard backlight LED; on unsupported hardware the tool will fail to discover a device unless **--mock** is used. Raw level ranges differ per device, so percentage mode is more portable across machines."],["HISTORY","**backlit-kbd** is an open-source Python package hosted on GitHub and published to PyPI, aimed at providing a beginner-friendly CLI for keyboard backlight control on Linux laptops."],["SEE ALSO","[brightnessctl](/man/brightnessctl)(1), [light](/man/light)(1), [xbacklight](/man/xbacklight)(1)"]],"e":["brightnessctl","light","xbacklight"],"x":"backlit-kbd info"},"bdftopcf":{"n":"bdftopcf","t":"Convert BDF bitmap fonts to PCF format","s":[["TAGLINE","Convert BDF bitmap fonts to PCF format"],["TLDR","**Convert BDF font to PCF**\n\n```bdftopcf [font.bdf] > [font.pcf]```\n\n**Convert with output file**\n\n```bdftopcf -o [font.pcf] [font.bdf]```\n\n**Create terminal font**\n\n```bdftopcf -t [font.bdf] > [font.pcf]```\n\n**Set MSB first bit order**\n\n```bdftopcf -m [font.bdf] > [font.pcf]```\n\n**Set glyph padding**\n\n```bdftopcf -p4 [font.bdf] > [font.pcf]```"],["SYNOPSIS","**bdftopcf** [_options_] _fontfile.bdf_"],["DESCRIPTION","**bdftopcf** is an X11 font compiler that converts fonts from Bitmap Distribution Format (BDF) to Portable Compiled Format (PCF). PCF fonts can be read by any architecture while allowing fast reading on the target machine."],["PARAMETERS","**-o** _outputfile_\n> Write output to file instead of stdout\n\n**-t**\n> Create terminal font with uniform glyph sizes for faster rendering\n\n**-i**\n> Inhibit ink metrics computation\n\n**-p**_n_\n> Set glyph padding to n bytes (1, 2, 4, or 8)\n\n**-u**_n_\n> Set scanline unit to n bytes (1, 2, or 4)\n\n**-m**\n> Set font bit order to MSB first\n\n**-l**\n> Set font bit order to LSB first\n\n**-M**\n> Set font byte order to MSB first\n\n**-L**\n> Set font byte order to LSB first"],["CAVEATS","Part of the X.Org font utilities. Output fonts are portable but read faster on matching architectures. Terminal fonts (-t) render faster but require uniform glyph sizing."],["INSTALL","```apt: sudo apt install xfonts-utils```\n\n```dnf: sudo dnf install bdftopcf```\n\n```apk: sudo apk add bdftopcf```\n\n```zypper: sudo zypper install bdftopcf```\n\n```brew: brew install bdftopcf```\n\n```nix: nix profile install nixpkgs#bdftopcf```"],["SEE ALSO","[xfontsel](/man/xfontsel)(1), [xlsfonts](/man/xlsfonts)(1), [fc-cache](/man/fc-cache)(1)"]],"e":["xfontsel","xlsfonts","fc-cache"],"x":"bdftopcf [font.bdf] > [font.pcf]"},"bootc-switch":{"n":"bootc-switch","t":"Switch the base OS to a different container image","s":[["TAGLINE","Switch the base OS to a different container image"],["TLDR","Change base OS to a **container image from registry**\n\n```sudo bootc switch [image]```\n\nSwitch to a new image and **reboot immediately**\n\n```sudo bootc switch --apply [image]```\n\nChange to image from **local container storage**\n\n```sudo bootc switch --transport containers-storage [image]```\n\nChange to image from an **OCI archive**\n\n```sudo bootc switch --transport oci-archive [path/to/image.tar]```"],["SYNOPSIS","**bootc switch** [_options_] _image_"],["DESCRIPTION","**bootc switch** changes the base operating system to a different container image. This allows switching between different OS variants or versions while maintaining a transactional, rollback-capable system.\n\nThe new image is staged and becomes active on the next reboot. The previous deployment is retained for rollback if needed. This operation is similar to `bootc upgrade` but additionally changes the container image reference."],["PARAMETERS","**--transport** _type_\n> Image source type: registry (default), containers-storage, oci-archive\n\n**--apply**\n> Restart or reboot into the new target image immediately after staging. Currently always performs a full reboot; future versions may use a userspace-only restart when no kernel changes are queued.\n\n**--soft-reboot** _mode_\n> Controls soft reboot behaviour when used with --apply. Values: auto (use soft reboot if available, otherwise fall back to full reboot), required (fail if soft reboot is unavailable).\n\n**--enforce-container-sigpolicy**\n> Require that /etc/containers/policy.json includes a default policy enforcing image signatures.\n\n**--retain**\n> Retain a reference to the currently booted image.\n\n**--quiet**\n> Suppress progress output."],["CAVEATS","Switching to incompatible images may result in a non-bootable system. Ensure the target image is compatible with the system's architecture and configuration. Requires root privileges."],["INSTALL","```dnf: sudo dnf install bootc```\n\n```nix: nix profile install nixpkgs#bootc```"],["SEE ALSO","[bootc](/man/bootc)(1), [podman](/man/podman)(1), [skopeo](/man/skopeo)(1), [rpm-ostree](/man/rpm-ostree)(1), [ostree](/man/ostree)(1)"]],"e":["bootc","podman","skopeo","rpm-ostree","ostree"],"x":"sudo bootc switch [image]"},"brew-remove":{"n":"brew-remove","t":"Remove an installed Homebrew package","s":[["TAGLINE","Remove an installed Homebrew package"],["TLDR","**Remove a formula**\n\n```brew remove [formula]```\n\n**Remove a cask**\n\n```brew remove --cask [cask]```\n\n**Remove all files associated with a cask** including preferences and caches\n\n```brew remove --zap [cask]```\n\n**Force removal** even if other formulae depend on it\n\n```brew remove --force [formula]```\n\n**Remove and ignore dependencies**\n\n```brew remove --ignore-dependencies [formula]```"],["SYNOPSIS","**brew** **remove** [_options_] _formula_|_cask_ [...]"],["PARAMETERS","**--cask**\n> Treat all named arguments as casks.\n\n**--formula**\n> Treat all named arguments as formulae.\n\n**--force**, **-f**\n> Delete all installed versions of a formula. For casks, uninstall even if not installed, overwrite existing files, and ignore errors.\n\n**--zap**\n> Remove all files associated with a cask. May remove files which are shared between applications.\n\n**--ignore-dependencies**\n> Do not fail uninstall if dependent formulae exist."],["DESCRIPTION","**brew remove** uninstalls a formula or cask. This is an alias for **brew uninstall**. It removes the installed files but by default preserves configuration files and does not remove dependencies.\n\nFor casks, use **--zap** to perform a more thorough removal including application preferences and caches."],["CAVEATS","Removing a formula that other formulae depend on will fail unless **--ignore-dependencies** is used. The **--zap** option for casks may remove user data."],["SEE ALSO","[brew](/man/brew)(1), [brew-uninstall](/man/brew-uninstall)(1), [brew-rm](/man/brew-rm)(1), [brew-autoremove](/man/brew-autoremove)(1), [brew-cleanup](/man/brew-cleanup)(1)"]],"e":["brew","brew-uninstall","brew-rm","brew-autoremove","brew-cleanup"],"x":"brew remove [formula]"},"cephadm":{"n":"cephadm","t":"container-based Ceph cluster deployment","s":[["TAGLINE","container-based Ceph cluster deployment"],["TLDR","**Bootstrap** a new Ceph cluster\n\n```sudo cephadm bootstrap --mon-ip [monitor_ip]```\n\n**Add** a new host to the cluster\n\n```sudo cephadm add-host [hostname] [ip_address]```\n\n**Deploy** a specific service\n\n```sudo cephadm deploy [service_type] --name [service_name]```\n\nCheck **status** of cluster services\n\n```sudo cephadm shell -- ceph -s```\n\nEnter a **shell** inside the Ceph container\n\n```sudo cephadm shell```\n\n**Remove** a service from the cluster\n\n```sudo cephadm rm-service [service_type] --name [service_name]```"],["SYNOPSIS","**cephadm** [_options_] _command_ [_arguments_]"],["DESCRIPTION","**cephadm** deploys and manages Ceph distributed storage clusters using containers (Podman preferred, Docker supported). It is the modern orchestration tool for Ceph, replacing older deployment methods like ceph-deploy and ceph-ansible.\n\nThe tool handles the complete cluster lifecycle: bootstrapping a new cluster on a single node, adding hosts, deploying daemons (monitors, OSDs, managers, gateways), and upgrading the cluster. Each Ceph daemon runs in its own container, providing isolation and simplified upgrades.\n\nThe `bootstrap` command creates an initial single-node cluster with a monitor and manager, generates admin credentials, and sets up the orchestrator for subsequent operations. The `shell` command provides an interactive environment inside a Ceph container with all management tools available."],["SUBCOMMANDS","**bootstrap**\n> Initialize a new Ceph cluster\n\n**add-host**\n> Add a host to the cluster\n\n**rm-host**\n> Remove a host from the cluster\n\n**deploy**\n> Deploy a daemon\n\n**rm-daemon**\n> Remove a daemon\n\n**shell**\n> Enter interactive shell in Ceph container\n\n**enter**\n> Run a command inside an existing Ceph container\n\n**ls**\n> List all Ceph daemons running on this host\n\n**ps**\n> Show status of daemons (alias of `ls --no-extra-info`)\n\n**check-host**\n> Verify host prerequisites (packages, time sync, container runtime)\n\n**prepare-host**\n> Install missing prerequisites\n\n**gather-facts**\n> Collect host inventory information\n\n**adopt**\n> Adopt a daemon from a legacy (non-cephadm) deployment\n\n**logs**\n> View daemon logs\n\n**rm-cluster**\n> Destroy the local cluster state (dangerous)"],["PARAMETERS","**--mon-ip** _ip_\n> Monitor IP address for bootstrap\n\n**--cluster-network** _CIDR_\n> Restrict the OSD replication network to the given subnet.\n\n**--ssh-user** _USER_\n> User cephadm should SSH to remote hosts as (default `root`).\n\n**--initial-dashboard-user**, **--initial-dashboard-password**\n> Credentials for the Ceph Dashboard created during bootstrap.\n\n**--skip-dashboard**\n> Do not enable the dashboard.\n\n**--fsid** _UUID_\n> Reuse an existing cluster FSID instead of generating one.\n\n**--name** _name_\n> Service or daemon name\n\n**--image** _image_\n> Container image to use\n\n**--docker**\n> Force the use of Docker instead of Podman.\n\n**-v**, **--verbose**\n> Increase logging verbosity."],["CAVEATS","Requires container runtime (Podman preferred, Docker supported). Bootstrap creates initial admin credentials. Services are managed through the orchestrator, not directly."],["INSTALL","```apt: sudo apt install cephadm```\n\n```dnf: sudo dnf install cephadm```\n\n```apk: sudo apk add ceph18-cephadm```\n\n```zypper: sudo zypper install cephadm```"],["SEE ALSO","[ceph](/man/ceph)(8), [podman](/man/podman)(1)"]],"e":["ceph","podman"],"x":"sudo cephadm bootstrap --mon-ip [monitor_ip]"},"clisp":{"n":"clisp","t":"ANSI Common Lisp implementation","s":[["TAGLINE","ANSI Common Lisp implementation"],["TLDR","**Start interactive REPL**\n\n```clisp```\n\n**Run a Lisp file**\n\n```clisp [script.lisp]```\n\n**Evaluate an expression**\n\n```clisp -x \"(print \\\"Hello World\\\")\"```\n\n**Load file and enter REPL**\n\n```clisp -i [script.lisp]```\n\n**Compile a file to bytecode**\n\n```clisp -c [source.lisp]```\n\n**Run without startup messages**\n\n```clisp -q```\n\n**Set memory limits**\n\n```clisp -m [100MB]```"],["SYNOPSIS","**clisp** [_options_] [_files_]"],["PARAMETERS","**-c** _file_\n> Compile file to bytecode (.fas).\n\n**-x** _expression_\n> Execute expression and exit.\n\n**-i** _file_\n> Load file before entering REPL.\n\n**-o** _file_\n> Output file for compilation.\n\n**-q**, **--quiet**\n> Suppress banner and startup messages.\n\n**-m** _size_\n> Set memory size limit.\n\n**-M** _file_\n> Use memory image file.\n\n**-modern**\n> Use modern (lowercase) symbol case.\n\n**-ansi**\n> Comply with ANSI Common Lisp standard.\n\n**-traditional**\n> Use traditional (pre-ANSI) behavior.\n\n**-norc**\n> Do not load the user RC file (~/.clisprc.lisp).\n\n**-repl**\n> Start a REPL regardless of other options.\n\n**-on-error** _action_\n> Action on error: debug, exit, abort, appease.\n\n**-disable-readline**\n> Do not use GNU readline for input.\n\n**--help**\n> Display help.\n\n**--version**\n> Display version information."],["DESCRIPTION","**CLISP** is an implementation of ANSI Common Lisp, providing an interactive development environment and compiler. It features a bytecode compiler, interpreter, debugger, and CLOS (Common Lisp Object System) support.\n\nThe REPL (Read-Eval-Print Loop) provides interactive Lisp development with readline support, history, and completion. Files can be loaded, compiled to bytecode for faster execution, or combined into standalone executables.\n\nCLISP includes extensive standard library support, foreign function interface (FFI) for calling C code, socket and networking functions, and internationalization support including Unicode."],["CAVEATS","Bytecode is not portable across different CLISP versions or platforms. Some Common Lisp libraries may have compatibility issues. Performance is generally slower than compiled implementations like SBCL. The FFI syntax differs from other Lisp implementations."],["HISTORY","CLISP was originally developed by **Bruno Haible** and **Michael Stoll** starting in **1987** at the University of Karlsruhe in Germany. It was designed to be portable and memory-efficient. The project has been maintained by various contributors over the decades and remains one of the most portable Common Lisp implementations, running on many platforms including Unix, Windows, and embedded systems."],["INSTALL","```apt: sudo apt install clisp```\n\n```dnf: sudo dnf install clisp```\n\n```pacman: sudo pacman -S clisp```\n\n```apk: sudo apk add clisp```\n\n```zypper: sudo zypper install clisp```\n\n```brew: brew install clisp```\n\n```nix: nix profile install nixpkgs#clisp```"],["SEE ALSO","[sbcl](/man/sbcl)(1), [ecl](/man/ecl)(1), [quicklisp](/man/quicklisp)(1)"]],"e":["sbcl","ecl","quicklisp"],"x":"clisp"},"crates-tui":{"n":"crates-tui","t":"Terminal UI for exploring crates.io Rust packages","s":[["TAGLINE","Terminal UI for exploring crates.io Rust packages"],["TLDR","**Launch** the TUI to browse crates.io\n\n```crates-tui```\n\n**Search** for a specific crate\n\n```crates-tui [search_term]```\n\n**Copy** crate information to clipboard from within the TUI\n\n```crates-tui```\n> Use `c` to copy the selected crate's information"],["SYNOPSIS","**crates-tui** [_search_term_]"],["DESCRIPTION","**crates-tui** is a terminal user interface application for browsing and exploring Rust packages from crates.io. It provides an interactive way to search for crates, view package details, and copy information to the clipboard without leaving the terminal.\n\nThe interface allows users to navigate through search results, view crate metadata including version, description, and download statistics, and quickly copy relevant information for use in Cargo.toml files or documentation."],["KEYBINDINGS","**↑/↓** or **k/j**\n> Navigate up/down through the crate list\n\n**Enter**\n> View detailed information about the selected crate\n\n**c**\n> Copy the selected crate's information to clipboard\n\n**q**\n> Quit the application\n\n**/**\n> Enter search mode"],["CAVEATS","Requires a stable internet connection to fetch data from crates.io. Clipboard functionality depends on the system's clipboard provider. Some terminal emulators may have limited clipboard support."],["HISTORY","**crates-tui** was developed as a Rust community tool to provide a fast, terminal-based alternative to browsing crates.io through a web browser. It leverages Rust's ecosystem and modern TUI libraries to create an efficient package discovery tool."],["INSTALL","```brew: brew install crates-tui```\n\n```nix: nix profile install nixpkgs#crates-tui```"],["SEE ALSO","[cargo](/man/cargo)(1), [cargo-search](/man/cargo-search)(1), [rustc](/man/rustc)(1)"]],"e":["cargo","cargo-search","rustc"],"x":"crates-tui"},"crl.1s":{"n":"crl.1s","t":"certificate revocation list processor","s":[["TAGLINE","certificate revocation list processor"],["TLDR","**Display CRL information** in text format\n\n```openssl crl -in [crl.pem] -text -noout```\n\n**Convert CRL from PEM** to DER format\n\n```openssl crl -in [crl.pem] -outform DER -out [crl.der]```\n\n**Convert CRL from DER** to PEM format\n\n```openssl crl -in [crl.der] -inform DER -out [crl.pem]```\n\n**Verify CRL signature** with CA certificate\n\n```openssl crl -in [crl.pem] -CAfile [ca.pem] -verify```\n\n**Print the issuer** of the CRL\n\n```openssl crl -in [crl.pem] -issuer -noout```\n\n**Print the last and next update** dates\n\n```openssl crl -in [crl.pem] -lastupdate -nextupdate -noout```"],["SYNOPSIS","**openssl crl** [_options_] [_-in file_] [_-out file_]"],["PARAMETERS","**-in** _FILE_\n> Input CRL file to process.\n\n**-out** _FILE_\n> Output file for the converted CRL.\n\n**-inform** _FORMAT_\n> Input format: PEM or DER.\n\n**-outform** _FORMAT_\n> Output format: PEM or DER.\n\n**-text**\n> Print CRL in human-readable text format.\n\n**-noout**\n> Don't output the encoded CRL.\n\n**-verify**\n> Verify the signature on the CRL.\n\n**-CAfile** _FILE_\n> CA certificate file for verification.\n\n**-issuer**\n> Print the issuer name.\n\n**-lastupdate**\n> Print the last update time.\n\n**-nextupdate**\n> Print the next update time.\n\n**-hash**\n> Print the hash of the CRL issuer name."],["DESCRIPTION","**openssl crl** is the OpenSSL command for processing Certificate Revocation Lists (CRLs). CRLs are lists of digital certificates that have been revoked by the issuing Certificate Authority before their scheduled expiration date.\n\nThe command can parse, convert, and verify CRLs. It supports both PEM (Base64-encoded) and DER (binary) formats. CRL verification ensures the list was signed by the claimed CA and hasn't been tampered with.\n\nCRLs are essential for PKI (Public Key Infrastructure) security, allowing systems to check whether a certificate has been revoked. The command provides detailed information about revoked certificates, revocation dates, and CRL validity periods."],["CAVEATS","CRLs can become large and may impact performance. Modern systems often prefer OCSP (Online Certificate Status Protocol) for real-time revocation checking. CRL verification requires the CA certificate that signed the CRL."],["HISTORY","Certificate Revocation Lists were defined in **X.509** standards and implemented in OpenSSL since its early versions. The CRL format was specified in **RFC 5280**. OpenSSL's crl command provides comprehensive tools for working with this critical PKI component."],["SEE ALSO","[x509](/man/x509)(1), [verify](/man/verify)(1), [openssl](/man/openssl)(1)"]],"e":["x509","verify","openssl"],"x":"openssl crl -in [crl.pem] -text -noout"},"cs":{"n":"cs","t":"Scala artifact fetcher and application launcher","s":[["TAGLINE","Scala artifact fetcher and application launcher"],["TLDR","**Install a Scala application**\n\n```cs install [scala]```\n\n**Launch an application** by artifact coordinates\n\n```cs launch [org.scalameta::metals:latest.stable]```\n\n**Resolve dependencies** for an artifact\n\n```cs resolve [org.typelevel::cats-core:2.9.0]```\n\n**Fetch JARs** for dependencies\n\n```cs fetch [com.lihaoyi::ammonite:2.5.9]```\n\n**Update installed applications**\n\n```cs update```\n\n**List installed applications**\n\n```cs list```\n\n**Setup Coursier** (install default apps)\n\n```cs setup```"],["SYNOPSIS","**cs** _command_ [_options_] [_arguments_]"],["PARAMETERS","**install** _APP_\n> Install an application from the default channels.\n\n**launch** _COORDS_\n> Launch an application by Maven coordinates.\n\n**resolve** _COORDS_\n> Resolve and display dependency tree.\n\n**fetch** _COORDS_\n> Download artifacts for dependencies.\n\n**update**\n> Update all installed applications.\n\n**list**\n> List installed applications.\n\n**setup**\n> Initial setup, installing common Scala tools.\n\n**uninstall** _APP_\n> Remove an installed application.\n\n**java**\n> Manage Java installations.\n\n**--channel** _URL_\n> Add a custom application channel.\n\n**-r** _REPO_\n> Add a custom Maven repository."],["DESCRIPTION","**Coursier** (cs) is a Scala artifact fetcher and application launcher. It resolves Maven and Ivy dependencies, downloads artifacts, and can launch JVM applications directly from their coordinates without manual installation.\n\nThe tool provides fast, parallel downloads with caching. It can install and manage Scala ecosystem tools like sbt, scala, ammonite, and scalafmt. The setup command configures a complete Scala development environment.\n\nCoursier handles complex dependency resolution, including version conflict resolution and exclusions. It integrates with the Scala ecosystem as the recommended way to install Scala tooling and is used by sbt for dependency management."],["CAVEATS","Requires Java Runtime Environment. First runs download dependencies which takes time. Application channels may have varying update frequencies. Large dependency trees can consume significant disk space in the cache."],["HISTORY","Coursier was created by Alexandre Archambault and first released around **2015**. It became the official installation method for Scala tools in **2020**. The tool addresses Java/Scala's dependency management complexity with fast, reliable artifact resolution."],["INSTALL","```apt: sudo apt install csound```\n\n```pacman: sudo pacman -S csound```\n\n```zypper: sudo zypper install csound```\n\n```brew: brew install csound```\n\n```nix: nix profile install nixpkgs#csound```"],["SEE ALSO","[scala](/man/scala)(1), [sbt](/man/sbt)(1), [java](/man/java)(1), [mvn](/man/mvn)(1)"],["RESOURCES","```[Source code](https://github.com/coursier/coursier)```\n\n```[Documentation](https://get-coursier.io/docs/cli-overview)```"]],"e":["scala","sbt","java","mvn"],"x":"cs install [scala]"},"cuda-gdb":{"n":"cuda-gdb","t":"NVIDIA CUDA kernel debugger","s":[["TAGLINE","NVIDIA CUDA kernel debugger"],["TLDR","**Debug a CUDA application**\n\n```cuda-gdb [./program]```\n\n**Debug with arguments**\n\n```cuda-gdb --args [./program] [arg1] [arg2]```\n\n**Attach to running process**\n\n```cuda-gdb -p [pid]```\n\n**Debug core dump**\n\n```cuda-gdb [./program] [core]```\n\n**Run with specific GPU**\n\n```CUDA_VISIBLE_DEVICES=[0] cuda-gdb [./program]```"],["SYNOPSIS","**cuda-gdb** [_options_] [_executable_] [_core_|_pid_]"],["PARAMETERS","**--args**\n> Pass arguments to the debugged program.\n\n**-p** _pid_\n> Attach to running process.\n\n**-x** _file_\n> Execute GDB commands from file.\n\n**-tui**\n> Enable text user interface mode.\n\n**-q**, **--quiet**\n> Suppress startup messages.\n\n**--cuda-use-lockfile=0**\n> Disable GPU lockfile (for multi-instance debugging)."],["CUDA-SPECIFIC COMMANDS","**cuda kernel**\n> Switch to specified kernel.\n\n**cuda block**\n> Switch to specified block.\n\n**cuda thread**\n> Switch to specified thread within block.\n\n**info cuda kernels**\n> List active CUDA kernels.\n\n**info cuda threads**\n> List CUDA threads.\n\n**info cuda devices**\n> List CUDA devices.\n\n**cuda device**\n> Switch to specified device.\n\n**cuda grid**\n> Switch to specified grid.\n\n**info cuda sms**\n> List CUDA streaming multiprocessors.\n\n**info cuda warps**\n> List CUDA warps.\n\n**info cuda lanes**\n> List CUDA lanes.\n\n**info cuda launch trace**\n> Show kernel launch trace.\n\n**set cuda break_on_launch**\n> Break when kernel launches (application or system).\n\n**set cuda memcheck on**\n> Enable memory checking for out-of-bounds accesses."],["DESCRIPTION","**cuda-gdb** is NVIDIA's extension of GNU GDB for debugging CUDA applications. It enables debugging both CPU and GPU code simultaneously, supporting breakpoints, single-stepping, and variable inspection in CUDA kernels.\n\nThe debugger can stop execution at specific threads or blocks within a kernel, inspect device memory, and catch memory errors. It integrates CUDA's parallel execution model into GDB's interface, allowing navigation between thousands of concurrent threads.\n\nFocus modes let developers examine specific blocks and threads. Memory checking catches out-of-bounds accesses and other errors. Integration with NVIDIA Nsight systems provides IDE-based debugging."],["CAVEATS","Requires NVIDIA GPU with compute capability 2.0+. Debugging mode disables some GPU optimizations and forces **-O0** compilation. Only one debugging session per GPU unless using **--cuda-use-lockfile=0**. Some GDB features may not work with GPU code. CUDA kernels must be compiled with **nvcc -g -G** flags (or **-lineinfo** for line-level debugging with optimizations)."],["HISTORY","cuda-gdb was introduced by **NVIDIA** alongside CUDA 2.2 in **2009** to provide kernel debugging capabilities. It extended the familiar GDB interface to handle GPU parallelism, addressing a major need for CUDA developers who previously had limited debugging options. The tool has evolved with each CUDA toolkit release, adding features like memory checking and improved multi-GPU support."],["SEE ALSO","[gdb](/man/gdb)(1), [nvcc](/man/nvcc)(1), [nvidia-smi](/man/nvidia-smi)(1)"]],"e":["gdb","nvcc","nvidia-smi"],"x":"cuda-gdb [./program]"},"cvs":{"n":"cvs","t":"Concurrent Versions System for version control","s":[["TAGLINE","Concurrent Versions System for version control"],["TLDR","**Checkout a module** from the repository\n\n```cvs checkout [module_name]```\n\n**Update working copy** with latest changes\n\n```cvs update```\n\n**Commit changes** with a message\n\n```cvs commit -m \"[commit message]\"```\n\n**Add a new file** to version control\n\n```cvs add [filename]```\n\n**Show differences** from repository version\n\n```cvs diff [filename]```\n\n**View commit history** for a file\n\n```cvs log [filename]```\n\n**Create a branch**\n\n```cvs tag -b [branch_name]```"],["SYNOPSIS","**cvs** [_global-options_] _command_ [_command-options_] [_arguments_]"],["PARAMETERS","**checkout** (co)\n> Get a working copy from the repository.\n\n**update** (up)\n> Update working copy with repository changes.\n\n**commit** (ci)\n> Commit changes to the repository.\n\n**add**\n> Add files to version control.\n\n**remove** (rm)\n> Remove files from version control.\n\n**diff**\n> Show differences between versions.\n\n**log**\n> Show revision history.\n\n**status**\n> Show working copy status.\n\n**tag**\n> Add a symbolic tag to revisions.\n\n**-d** _CVSROOT_\n> Specify the repository root.\n\n**-m** _MESSAGE_\n> Commit message."],["DESCRIPTION","**CVS** (Concurrent Versions System) is a version control system that tracks changes to files over time. It enables multiple developers to work on the same codebase, managing merges and maintaining history of all changes.\n\nCVS uses a client-server architecture where a central repository stores all versions. Developers checkout working copies, make changes locally, and commit them back. The system handles concurrent modifications through optimistic locking and merge resolution.\n\nOperations include checking out code, updating to get others' changes, committing modifications, branching for parallel development, and tagging releases. CVS tracks changes at the file level, storing deltas efficiently."],["CAVEATS","CVS is considered legacy; Git and other distributed VCS are preferred for new projects. It cannot track directory operations well. Atomic commits are not guaranteed across multiple files. Branch and merge operations are more cumbersome than modern systems."],["HISTORY","CVS was developed by Dick Grune in **1986** and later enhanced by Brian Berliner. It was the dominant version control system throughout the 1990s, used by major open-source projects. It was largely superseded by Subversion and later Git in the 2000s."],["INSTALL","```apt: sudo apt install cvs```\n\n```dnf: sudo dnf install cvs```\n\n```pacman: sudo pacman -S cvs```\n\n```apk: sudo apk add cvs```\n\n```zypper: sudo zypper install cvs```\n\n```brew: brew install cvs```\n\n```nix: nix profile install nixpkgs#cvs```"],["SEE ALSO","[svn](/man/svn)(1), [git](/man/git)(1), [rcs](/man/rcs)(1)"],["RESOURCES","```[Source code](https://savannah.nongnu.org/projects/cvs/)```\n\n```[Homepage](https://www.nongnu.org/cvs/)```"]],"e":["svn","git","rcs"],"x":"cvs checkout [module_name]"},"dd":{"n":"dd","t":"Convert and copy files at the block level","s":[["TAGLINE","Convert and copy files at the block level"],["TLDR","Make a **bootable USB** from an ISO and show progress\n\n```sudo dd if=[path/to/file.iso] of=[/dev/usb_drive] status=progress```\n\n**Clone** a drive to another drive with 4 MiB block size\n\n```sudo dd bs=4M conv=fsync if=[/dev/source_drive] of=[/dev/dest_drive]```\n\nGenerate a file with **random bytes**\n\n```dd bs=100 count=1 if=/dev/urandom of=[path/to/random_file]```\n\n**Benchmark** disk write performance\n\n```dd bs=1M count=1024 if=/dev/zero of=[path/to/file_1GB]```\n\nCreate a **system backup** to an IMG file\n\n```sudo dd if=[/dev/drive_device] of=[path/to/file.img] status=progress```\n\n**Restore** from an IMG backup\n\n```sudo dd if=[path/to/file.img] of=[/dev/drive_device] status=progress```\n\n**Skip** bytes at the start of input (e.g., skip first 512 bytes)\n\n```dd if=[input_file] of=[output_file] bs=512 skip=1```"],["SYNOPSIS","**dd** [_OPERAND_]..."],["DESCRIPTION","**dd** converts and copies files at the block level, making it useful for low-level disk operations that bypass filesystem structures. It reads from standard input and writes to standard output by default, but is typically used with if= and of= operands to specify input and output files or devices.\n\nThe tool operates in fixed block sizes (set with bs=), reading and writing data in chunks for efficiency. This block-oriented approach makes dd suitable for creating exact disk images, cloning entire drives, writing bootable USB drives from ISO files, and generating test files of specific sizes.\n\nCommon use cases include forensic disk imaging, data backup and restoration, disk benchmarking, and creating files filled with zeros or random data. The status=progress option (added in GNU coreutils 8.24) provides real-time progress information. Sending a USR1 signal (or INFO signal on BSD) to a running dd process causes it to print I/O statistics to stderr."],["PARAMETERS","**if=**_FILE_\n> Read from FILE instead of stdin.\n\n**of=**_FILE_\n> Write to FILE instead of stdout.\n\n**bs=**_BYTES_\n> Read and write up to BYTES bytes at a time (default: 512). Overrides ibs and obs.\n\n**ibs=**_BYTES_\n> Read up to BYTES bytes at a time (default: 512).\n\n**obs=**_BYTES_\n> Write BYTES bytes at a time (default: 512).\n\n**count=**_N_\n> Copy only N input blocks.\n\n**skip=**_N_\n> Skip N ibs-sized blocks at start of input.\n\n**seek=**_N_\n> Skip N obs-sized blocks at start of output.\n\n**conv=**_CONVS_\n> Comma-separated conversion options: ascii, ebcdic, ibm, block, unblock, lcase, ucase, sparse, swab, sync, excl, nocreat, notrunc, noerror, fdatasync, fsync.\n\n**status=**_LEVEL_\n> Output level: none (suppress everything except errors), noxfer (suppress transfer statistics), progress (show periodic transfer statistics).\n\n**iflag=**_FLAGS_\n> Comma-separated input flags: append, direct, directory, dsync, sync, fullblock, nonblock, noatime, nocache, noctty, nofollow, count_bytes, skip_bytes.\n\n**oflag=**_FLAGS_\n> Comma-separated output flags: append, direct, directory, dsync, sync, nonblock, noatime, nocache, noctty, nofollow, seek_bytes."],["CAVEATS","**Dangerous**: dd will overwrite data without warning. Double-check the **of=** operand before executing. Using wrong device names can destroy entire disks. BYTES may be followed by multiplicative suffixes: c=1, w=2, b=512, kB=1000, K=1024, MB=1000000, M=1048576, and so on for G, T, P, E, Z, Y."],["HISTORY","Part of **GNU Coreutils**. The command name comes from IBM JCL (Job Control Language) where DD stands for \"Data Definition\". Available since Version 5 AT&T Unix (1974)."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[cp](/man/cp)(1), [dcfldd](/man/dcfldd)(1), [pv](/man/pv)(1)"]],"e":["cp","dcfldd","pv"],"x":"sudo dd if=[path/to/file.iso] of=[/dev/usb_drive] status=progress"},"git-credential-cache":{"n":"git-credential-cache","t":"Cache credentials in memory temporarily","s":[["TAGLINE","Cache credentials in memory temporarily"],["TLDR","**Cache credentials for 15 minutes**\n\n```git config credential.helper cache```\n\n**Cache for specific duration**\n\n```git config credential.helper 'cache --timeout=[3600]'```\n\n**Cache with custom socket path**\n\n```git config credential.helper 'cache --socket=[~/.git-credential-cache/socket]'```\n\n**Exit** the cache daemon (clear all cached credentials)\n\n```git credential-cache exit```"],["SYNOPSIS","**git** **credential-cache** [_options_]"],["PARAMETERS","**--timeout** _seconds_\n> Number of seconds to cache credentials (default: 900, i.e. 15 minutes).\n\n**--socket** _path_\n> Path to the Unix socket used to communicate with the cache daemon (default: ~/.cache/git/credential/socket).\n\n**exit**\n> Cause the cache daemon to exit, discarding all cached credentials.\n\n**get**, **store**, **erase**\n> Standard credential helper actions (normally invoked by git itself, not manually)."],["DESCRIPTION","**git credential-cache** is a credential helper that temporarily stores authentication credentials in memory via a daemon process. It provides a balance between security and convenience by avoiding repeated password prompts without permanently storing credentials on disk.\n\nWhen configured as a credential helper, it automatically captures credentials during the first authentication and reuses them for subsequent operations within the timeout period. The default timeout is 15 minutes (900 seconds), configurable with the --timeout option.\n\nThe daemon stores credentials in a Unix domain socket, accessible only to the current user. Credentials automatically expire after the timeout and are never written to disk, making this more secure than git-credential-store. The cache can be manually cleared with git credential-cache exit."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-credential-store](/man/git-credential-store)(1), [git-credential](/man/git-credential)(1)"]],"e":["git-credential-store","git-credential"],"x":"git config credential.helper cache"},"git-for-each-ref":{"n":"git-for-each-ref","t":"Output information on each reference with custom formatting","s":[["TAGLINE","Output information on each reference with custom formatting"],["TLDR","**List all refs**\n\n```git for-each-ref```\n\n**List branches**\n\n```git for-each-ref refs/heads```\n\n**List tags**\n\n```git for-each-ref refs/tags```\n\n**Custom format**\n\n```git for-each-ref --format='%(refname:short) %(objectname:short)' refs/heads```\n\n**Sort by date**\n\n```git for-each-ref --sort=-committerdate refs/heads```\n\n**Show latest N refs**\n\n```git for-each-ref --count=[10] --sort=-committerdate refs/heads```"],["SYNOPSIS","**git** **for-each-ref** [_options_] [_patterns_...]"],["PARAMETERS","**--format** _format_\n> Output format string using %(fieldname) placeholders (default: `%(objectname) %(objecttype)\\t%(refname)`).\n\n**--sort** _key_\n> Sort by field name; prefix with **-** for descending. Repeatable, last key is primary.\n\n**--count** _n_\n> Stop after showing _n_ refs.\n\n**--points-at** _object_\n> Only list refs pointing at the given object.\n\n**--merged**[=_commit_]\n> Only list refs whose tips are reachable from _commit_ (default HEAD).\n\n**--no-merged**[=_commit_]\n> Only list refs whose tips are not reachable from _commit_ (default HEAD).\n\n**--contains**[=_commit_]\n> Only list refs that contain the given commit.\n\n**--exclude** _pattern_\n> Omit refs matching _pattern_.\n\n**--stdin**\n> Read the list of patterns from standard input instead of the argument list.\n\n**--ignore-case**\n> Case-insensitive sorting and filtering.\n\n**--shell**, **--perl**, **--python**, **--tcl**\n> Quote interpolated values as string literals for the given scripting language."],["DESCRIPTION","**git for-each-ref** is a plumbing command that outputs detailed information about Git references (branches, tags, remote branches) in a highly customizable format. It serves as the foundation for many higher-level Git commands and scripts that need to process repository references.\n\nThe format string syntax supports field extraction using %(fieldname) placeholders, with available fields including refname, objecttype, objectname, author, committer, and many others. Sorting capabilities enable ordering references by any field, with support for reverse sorting using a minus prefix.\n\nThis command is primarily used in scripts and Git aliases rather than direct user interaction. The filtering options (--merged, --no-merged, --points-at) provide powerful selection mechanisms for finding references matching specific criteria."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-branch](/man/git-branch)(1), [git-tag](/man/git-tag)(1), [git-rev-parse](/man/git-rev-parse)(1)"],["RESOURCES","```[Source code](https://github.com/git/git)```\n\n```[Homepage](https://git-scm.com/)```\n\n```[Documentation](https://git-scm.com/docs/git-for-each-ref)```"]],"e":["git-branch","git-tag","git-rev-parse"],"x":"git for-each-ref"},"gitstats":{"n":"gitstats","t":"Generate statistics from Git repositories","s":[["TAGLINE","Generate statistics from Git repositories"],["TLDR","**Generate stats for repository**\n\n```gitstats [repo_path] [output_dir]```\n\n**Generate stats for** last 10 commits\n\n```gitstats -c commit_begin='HEAD~10' [repo] [output]```\n\n**Set project name** and max authors\n\n```gitstats -c project_name='[MyProject]' -c max_authors=[20] [repo] [output]```"],["SYNOPSIS","**gitstats** [_options_] _repository_ _output_directory_"],["PARAMETERS","**-c** _key=value_\n> Configuration option. Can be specified multiple times.\n\n### Configuration Keys\n\n**project_name**\n> Project name shown on generated pages. Default: basename of repository directory.\n\n**max_authors**\n> Maximum number of authors to show in the authors list.\n\n**authors_top**\n> How many top authors to show.\n\n**max_domains**\n> Maximum number of domains to show in domains by commits.\n\n**max_ext_length**\n> Maximum file extension length.\n\n**commit_begin**\n> Starting commit for statistics range.\n\n**commit_end**\n> Ending commit for statistics range.\n\n**start_date**\n> Starting date passed with --since to git.\n\n**processes**\n> Number of concurrent processes for extracting data.\n\n**style**\n> CSS stylesheet to use."],["DESCRIPTION","**gitstats** generates statistics from Git repositories and creates HTML reports with graphs showing commit activity, author contributions, file counts, and lines of code over time.\n\nThe output is a static HTML site that can be viewed in any browser, providing a visual overview of project history and contributor patterns."],["INSTALL","```nix: nix profile install nixpkgs#gitstats```"],["SEE ALSO","[git-log](/man/git-log)(1), [git-summary](/man/git-summary)(1)"]],"e":["git-log","git-summary"],"x":"gitstats [repo_path] [output_dir]"},"gnome-sudoku":{"n":"gnome-sudoku","t":"GNOME Sudoku puzzle game","s":[["TAGLINE","GNOME Sudoku puzzle game"],["TLDR","**Start GNOME Sudoku**\n\n```gnome-sudoku```"],["SYNOPSIS","**gnome-sudoku** [_options_]"],["PARAMETERS","**--help**\n> Display help.\n\n**--version**\n> Show version."],["DESCRIPTION","**GNOME Sudoku** is a puzzle game where players fill a 9x9 grid so that each row, column, and 3x3 box contains digits 1-9 exactly once.\n\nThe game generates puzzles at various difficulty levels and provides features like hints, notes (pencil marks), and error highlighting. It saves game progress automatically."],["FEATURES","- Multiple difficulty levels\n- Pencil marks for notes\n- Hint system\n- Error highlighting\n- Game timer\n- Print puzzles"],["CAVEATS","Requires GNOME libraries. Generated puzzles may vary in difficulty even within the same level."],["HISTORY","Sudoku became a worldwide puzzle phenomenon in the **2000s** after being popularized in Japan. GNOME Sudoku is part of the GNOME Games collection, providing a clean, modern implementation."],["INSTALL","```dnf: sudo dnf install gnome-sudoku```\n\n```pacman: sudo pacman -S gnome-sudoku```\n\n```apk: sudo apk add gnome-sudoku```\n\n```zypper: sudo zypper install gnome-sudoku```\n\n```nix: nix profile install nixpkgs#gnome-sudoku```"],["SEE ALSO","[gnome-mines](/man/gnome-mines)(1)"]],"e":["gnome-mines"],"x":"gnome-sudoku"},"gvfs-mime":{"n":"gvfs-mime","t":"manages MIME type associations in GNOME","s":[["TAGLINE","manages MIME type associations in GNOME"],["TLDR","**Show default handler**\n\n```gvfs-mime --query [text/plain]```\n\n**Set default handler**\n\n```gvfs-mime --set [text/html] [firefox.desktop]```\n\n**Query image handler**\n\n```gvfs-mime --query [image/jpeg]```"],["SYNOPSIS","**gvfs-mime** [_options_] _mimetype_ [_handler_]"],["PARAMETERS","_MIMETYPE_\n> MIME type to query or set.\n\n_HANDLER_\n> Desktop file for handler.\n\n**--query**\n> Query default handler.\n\n**--set**\n> Set default handler.\n\n**--help**\n> Display help information."],["DESCRIPTION","**gvfs-mime** manages MIME type associations in GNOME. It queries and sets default applications for opening different file types.\n\nThe tool modifies the user's MIME database to associate applications with content types. Changes affect how files are opened in GNOME."],["CAVEATS","Part of the gvfs-bin command-line tools, deprecated in gvfs 1.31 (2016) and removed entirely in gvfs 1.38 (2018) in favor of the unified **gio** command (`gio mime`). Modern distributions no longer ship gvfs-mime. GNOME specific; uses .desktop file IDs as handlers."],["HISTORY","**gvfs-mime** was part of GVFS's original gvfs-bin utilities, introduced for GNOME alongside GVFS in **2008** (GNOME 2.22). It has since been superseded by **gio mime**."],["SEE ALSO","[gio](/man/gio)(1), [xdg-mime](/man/xdg-mime)(1), [update-mime-database](/man/update-mime-database)(1)"],["RESOURCES","```[Source code](https://gitlab.gnome.org/GNOME/gvfs)```\n\n```[Homepage](https://wiki.gnome.org/Projects/gvfs)```"]],"e":["gio","xdg-mime","update-mime-database"],"x":"gvfs-mime --query [text/plain]"},"hexedit":{"n":"hexedit","t":"terminal-based hex editor for viewing and editing binary files at the byte level","s":[["TAGLINE","terminal-based hex editor for viewing and editing binary files at the byte level"],["TLDR","**Open a file** in hex editor\n\n```hexedit [path/to/file]```\n\n**Open in read-only mode**\n\n```hexedit -r [path/to/file]```\n\n**Open with a specific sector size** (for disks)\n\n```hexedit -s [512] [/dev/sda]```\n\n**Open without showing ASCII column**\n\n```hexedit --nocolor [path/to/file]```"],["SYNOPSIS","**hexedit** [_-s sectorsize_] [_-r_] [_-h_] [_file_]"],["PARAMETERS","**-s**, **--sector** _size_\n> Set sector size for disk editing (default: 512).\n\n**-r**, **--readonly**\n> Open file in read-only mode.\n\n**-h**, **--help**\n> Display help information.\n\n**--nocolor**\n> Disable color output."],["KEYBOARD COMMANDS","**Arrow keys**\n> Navigate through the file.\n\n**Page Up/Down**\n> Move one page up/down.\n\n**Home/End**\n> Go to beginning/end of file.\n\n**Tab**\n> Toggle between hex and ASCII editing.\n\n**Ctrl-G**\n> Go to specific offset (hex or decimal).\n\n**Ctrl-S**\n> Save changes.\n\n**Ctrl-X**\n> Save and exit.\n\n**Ctrl-C**\n> Exit without saving.\n\n**Ctrl-F** or **/**\n> Search forward (hex or ASCII).\n\n**Ctrl-R**\n> Search backward.\n\n**Ctrl-Space**\n> Set mark.\n\n**Ctrl-Y**\n> Paste (fill with copied byte).\n\n**Ctrl-U**\n> Undo all changes.\n\n**F2**\n> Save.\n\n**F3**\n> View modified bytes."],["DESCRIPTION","**hexedit** is a terminal-based hex editor for viewing and editing binary files at the byte level. It displays file contents in hexadecimal alongside ASCII representation, allowing direct modification of individual bytes.\n\nThe interface shows three columns: byte offset on the left, hexadecimal values in the middle, and ASCII characters on the right. Pressing Tab switches between editing hex values and ASCII characters. Modified bytes are highlighted.\n\nNavigation is efficient for both small files and large binaries (or even block devices). The Ctrl-G command accepts offsets in hex (prefix with 0x) or decimal. Search works for both hex patterns and ASCII strings.\n\nFor disk editing, the sector size option (-s) affects how the editor handles reads and writes. Changes to block devices require appropriate permissions and extreme caution.\n\nhexedit uses a simple memory model - changes are held in memory until explicitly saved. The Ctrl-U command reverts all unsaved changes. For large files, it memory-maps the file for efficient access."],["CAVEATS","Editing block devices or system files requires root. No undo history - only revert all changes. Limited to files that fit in virtual memory (or are memory-mappable). Cannot insert or delete bytes, only overwrite. Search patterns must be contiguous."],["HISTORY","**hexedit** was written by Pixel (Pascal Rigaux) and released in the 1990s. It filled the need for a simple, fast hex editor for Unix terminals. Despite its age, it remains popular due to its simplicity, low resource usage, and availability in virtually all Linux distribution repositories."],["INSTALL","```apt: sudo apt install hexedit```\n\n```dnf: sudo dnf install hexedit```\n\n```pacman: sudo pacman -S hexedit```\n\n```zypper: sudo zypper install hexedit```\n\n```brew: brew install hexedit```\n\n```nix: nix profile install nixpkgs#hexedit```"],["SEE ALSO","[xxd](/man/xxd)(1), [hexdump](/man/hexdump)(1), [od](/man/od)(1), [dd](/man/dd)(1)"]],"e":["xxd","hexdump","od","dd"],"x":"hexedit [path/to/file]"},"hw-probe":{"n":"hw-probe","t":"Probe Linux hardware and upload reports to the Linux hardware database","s":[["TAGLINE","Probe Linux hardware and upload reports to the Linux hardware database"],["TLDR","**Probe all hardware** and upload the report to linux-hardware.org\n\n```sudo -E hw-probe -all -upload```\n\n**Probe and run operability checks** (graphics, drive, CPU, memory)\n\n```sudo -E hw-probe -all -check -upload```\n\n**Probe locally** without uploading (saved under `/root/HW_PROBE/`)\n\n```sudo -E hw-probe -all```\n\n**Disable** specific log collectors\n\n```sudo -E hw-probe -all -upload -disable [xdpyinfo,xorg.conf]```\n\n**Tag a probe** with an inventory identifier\n\n```sudo -E hw-probe -all -upload -i [INVENTORY_ID]```\n\n**Generate** a new inventory ID linked to your email\n\n```sudo -E hw-probe -generate-inventory -email [you@example.com]```\n\n**Install a daily cron job** that probes the system and alerts on failures\n\n```sudo -E hw-probe -start```\n\n**Dump and decode** the ACPI tables (requires `acpica-tools`)\n\n```sudo -E hw-probe -all -dump-acpi -decode-acpi -upload```"],["SYNOPSIS","**hw-probe** [_options_]"],["PARAMETERS","**-all**\n> Probe for all hardware. Equivalent to enabling every individual probe collector.\n\n**-probe**\n> Probe for hardware (a lighter probe than **-all**).\n\n**-logs**\n> Collect system logs (dmesg, journal, lspci, lsusb, etc.).\n\n**-log-level** _N_\n> Set log collection verbosity: `mini`, `default`, or `maximal`.\n\n**-upload**\n> Upload the resulting probe to linux-hardware.org and print the public URL.\n\n**-check**\n> Run operability sanity tests on graphics, drive, CPU and memory.\n\n**-check-graphics**\n> Test integrated and discrete graphics with **glxgears**.\n\n**-check-hdd**\n> Test drive read speed with **hdparm**.\n\n**-check-cpu**\n> Test CPU using **dd** and **md5sum**.\n\n**-check-memory**\n> Test RAM with **memtester**.\n\n**-id** _DESC_, **-name** _DESC_\n> Set a custom name or description for the probe.\n\n**-i** _ID_\n> Tag the probe with an inventory identifier.\n\n**-generate-inventory**, **-generate-inventory-id**\n> Request a new inventory ID, linked to **-email**.\n\n**-email** _ADDR_\n> Email address to associate with a generated inventory ID.\n\n**-disable** _A,B,C_\n> Disable specific log collectors (e.g. `xdpyinfo,xorg.conf`).\n\n**-enable** _A,B,C_\n> Force-enable specific log collectors disabled by default.\n\n**-dump-acpi**\n> Dump the ACPI tables.\n\n**-decode-acpi**\n> Decode dumped ACPI tables (requires **acpica-tools**).\n\n**-show**\n> Show summary of probe data on the console.\n\n**-verbose**\n> Print detailed log output during probing.\n\n**-pci-ids** _PATH_, **-usb-ids** _PATH_, **-sdio-ids** _PATH_, **-pnp-ids** _PATH_\n> Use a custom IDs database file when decoding device IDs.\n\n**-import** _DIR_\n> Re-create probes from a directory of previously-saved probe data.\n\n**-start**\n> Install a daily cron job that probes the system and emails on hardware failure.\n\n**-stop**\n> Remove the daily cron job created by **-start**.\n\n**-debug**\n> Enable debug output and keep temporary files.\n\n**-help**\n> Print full usage information."],["DESCRIPTION","**hw-probe** collects a snapshot of a machine's hardware configuration and the related kernel/userspace logs, then optionally uploads it to the public **Linux Hardware Database** at linux-hardware.org. The uploaded probe gets a permanent URL that can be shared on bug trackers, mailing lists, or vendor support requests.\n\nA probe combines the output of standard tools (lspci, lsusb, lscpu, dmidecode, smartctl, lshw, edid, dmesg, Xorg.log, etc.) into a structured, anonymized bundle. The database aggregates these reports to track hardware compatibility across Linux distributions, identify suitable drivers for unsupported devices, and recommend kernels known to work with a given configuration.\n\nThe tool also doubles as a self-monitoring agent: with **-start** it adds a cron job that runs daily, compares the new probe with the previous one and emails the user if a disk SMART error, missing device, or other anomaly appears. It is widely used by distributions, vendors, and end users to diagnose driver issues, plan upgrades, and inventory fleets of machines."],["CAVEATS","Most probes require **root** privileges to read SMART data, DMI tables and X session logs — invoke via `sudo -E` so that environment variables (notably `DISPLAY`) are preserved. A full probe with **-check** can take a minute or more because of memtester and disk timing tests. Uploaded probes are public; they are anonymized but inventory tags and machine names are kept verbatim, so do not include sensitive identifiers in **-id** or **-i**."],["CONFIGURATION","Local probes are written to `/root/HW_PROBE/LATEST/` (or `~/HW_PROBE/LATEST/` for non-root probes). The cron job created by **-start** lives in `/etc/cron.daily/hw-probe`. Decoding ACPI requires the **acpica-tools** package; reading SMART requires **smartmontools**; graphics checks require **mesa-utils** for **glxgears**."],["HISTORY","**hw-probe** was created by **Andrey Ponomarenko** as the front-end client for the Linux Hardware Database project, first released in **2018**. It is written in Perl and ships in the AUR, Debian/Ubuntu, Fedora, openSUSE and FreeBSD ports. The companion site, linux-hardware.org, has since collected millions of public probes used by kernel developers and distribution maintainers to track Linux hardware support over time."],["INSTALL","```dnf: sudo dnf install hw-probe```\n\n```zypper: sudo zypper install hw-probe```\n\n```nix: nix profile install nixpkgs#hw-probe```"],["SEE ALSO","[lshw](/man/lshw)(1), [lspci](/man/lspci)(8), [lsusb](/man/lsusb)(8), [dmidecode](/man/dmidecode)(8), [hwinfo](/man/hwinfo)(8), [smartctl](/man/smartctl)(8), [inxi](/man/inxi)(1)"]],"e":["lshw","lspci","lsusb","dmidecode","hwinfo","smartctl","inxi"],"x":"sudo -E hw-probe -all -upload"},"impacket-getnpusers":{"n":"impacket-getnpusers","t":"finds Active Directory users with \"Do not require Kerberos preauthentication\"","s":[["TAGLINE","finds Active Directory users with \"Do not require Kerberos preauthentication\""],["TLDR","**Find AS-REP roastable users** from a users file\n\n```impacket-GetNPUsers [domain]/ -dc-ip [dc-ip] -usersfile [users.txt]```\n\n**Get hash for specific user** without credentials\n\n```impacket-GetNPUsers [domain]/[user] -dc-ip [dc-ip] -no-pass```\n\n**Request hashes** in hashcat format and save to file\n\n```impacket-GetNPUsers [domain]/ -dc-ip [dc-ip] -usersfile [users.txt] -format hashcat -outputfile [hashes.txt]```\n\n**Enumerate via LDAP** with credentials\n\n```impacket-GetNPUsers [domain]/[user]:[password] -dc-ip [dc-ip] -request```\n\n**Use Kerberos authentication** from ccache\n\n```impacket-GetNPUsers [domain]/[user] -dc-ip [dc-ip] -k -no-pass```"],["SYNOPSIS","**impacket-GetNPUsers** [_options_] _target_"],["PARAMETERS","**-dc-ip** _ip_\n> Domain controller IP address.\n\n**-usersfile** _file_\n> File with usernames to test (one per line).\n\n**-no-pass**\n> Don't ask for password (useful with -k or anonymous queries).\n\n**-request**\n> Request TGT hashes for vulnerable users (default: only list them).\n\n**-format** _type_\n> Hash output format: hashcat or john (default: hashcat).\n\n**-outputfile** _file_\n> Save hashes to file instead of stdout.\n\n**-k**\n> Use Kerberos authentication from ccache (KRB5CCNAME).\n\n**-hashes** _LMHASH:NTHASH_\n> Authenticate using NTLM hashes instead of password.\n\n**-aesKey** _KEY_\n> AES key to use for Kerberos authentication.\n\n**-ts**\n> Add timestamp to logging output.\n\n**-debug**\n> Turn on debug output."],["DESCRIPTION","**impacket-GetNPUsers** lists and requests TGTs for Active Directory users with \"Do not require Kerberos preauthentication\" set (UF_DONT_REQUIRE_PREAUTH). This enables AS-REP Roasting: the retrieved hashes can be cracked offline with hashcat or John the Ripper.\n\nWithout **-usersfile**, the script queries LDAP to automatically enumerate vulnerable accounts (requires valid credentials). With **-usersfile**, it tests each username without needing domain credentials. Part of the Impacket toolkit. For authorized security testing only."],["INSTALL","```pacman: sudo pacman -S impacket```"],["SEE ALSO","[impacket-getuserspns](/man/impacket-getuserspns)(1), [hashcat](/man/hashcat)(1)"]],"e":["impacket-getuserspns","hashcat"],"x":"impacket-GetNPUsers [domain]/ -dc-ip [dc-ip] -usersfile [users.txt]"},"import":{"n":"import","t":"ImageMagick utility for capturing X Window screenshots","s":[["TAGLINE","ImageMagick utility for capturing X Window screenshots"],["TLDR","**Capture window by click** (interactive crosshair selector)\n\n```import screenshot.png```\n\n**Capture entire screen**\n\n```import -window root screenshot.png```\n\n**Capture specific window**\n\n```import -window [window_id] screenshot.png```\n\n**Capture with delay**\n\n```import -pause [5] screenshot.png```\n\n**Capture region**\n\n```import -crop [640x480+100+100] screenshot.png```\n\n**Set image format**\n\n```import screenshot.[jpg|png|gif]```"],["SYNOPSIS","**import** [_options_] _output_file_"],["PARAMETERS","**-window** _ID_\n> Capture specific window ID or \"root\" for full screen.\n\n**-pause** _SECONDS_\n> Delay before capture.\n\n**-crop** _GEOMETRY_\n> Crop to specified geometry.\n\n**-frame**\n> Include window frame.\n\n**-border**\n> Include window border.\n\n**-screen**\n> Capture from virtual root window.\n\n**-silent**\n> Suppress progress monitor.\n\n**--help**\n> Display help information."],["DESCRIPTION","**import** is an ImageMagick utility for capturing X Window screenshots. It can capture the entire screen, specific windows, or user-selected regions.\n\nThe tool supports various output formats determined by file extension. It integrates with the ImageMagick processing pipeline for immediate manipulation."],["CAVEATS","X11 only. Requires ImageMagick. Click-to-select default behavior."],["HISTORY","import is part of the **ImageMagick** suite, created by **John Cristy** in 1987 for X Window screen capture."],["INSTALL","```pacman: sudo pacman -S imagemagick```\n\n```apk: sudo apk add imagemagick```\n\n```brew: brew install imagemagick```\n\n```nix: nix profile install nixpkgs#imagemagick```"],["SEE ALSO","[convert](/man/convert)(1), [display](/man/display)(1), [scrot](/man/scrot)(1)"]],"e":["convert","display","scrot"],"x":"import screenshot.png"},"intelli-shell":{"n":"intelli-shell","t":"IntelliSense-style command template manager for your shell","s":[["TAGLINE","IntelliSense-style command template manager for your shell"],["TLDR","**Search saved commands interactively (shell hotkey)**\n\n```# Press Ctrl+Space in your shell```\n\n**Bookmark the last executed command (shell hotkey)**\n\n```# Press Ctrl+B in your shell```\n\n**Fix the current command with AI (shell hotkey)**\n\n```# Press Ctrl+X in your shell```\n\n**Add a command template from the CLI**\n\n```intelli-shell new \"[command_template]\" -d \"[description]\"```\n\n**Search commands from the CLI**\n\n```intelli-shell search \"[query]\"```\n\n**Import commands from a file or URL**\n\n```intelli-shell import [path_or_url]```\n\n**Export your command library**\n\n```intelli-shell export [path]```"],["SYNOPSIS","**intelli-shell** _subcommand_ [_options_]"],["SUBCOMMANDS","**new**\n> Create a new command template.\n\n**search**\n> Search stored commands (optionally interactively).\n\n**replace**\n> Replace variables of a given command.\n\n**import**\n> Import commands from a file, URL, or Gist.\n\n**export**\n> Export commands to a file.\n\n**fix**\n> Use an LLM to suggest fixes for a command.\n\n**completions**\n> Generate shell completions."],["DESCRIPTION","**intelli-shell** is a cross-platform command template and snippet manager written in **Rust**. It turns your terminal into a searchable library of commands that can be recalled with a keybinding and expanded with parameterized variables.\n\nIt integrates with **Bash**, **Zsh**, **Fish**, **Nushell**, and **PowerShell**. Once the shell hook is installed, **Ctrl+Space** opens an interactive fuzzy search over your stored commands, **Ctrl+B** bookmarks the last executed line, and **Ctrl+X** asks a configured LLM to fix the current line.\n\nTemplates support dynamic variables such as `{{file}}` with optional suggestion generators, workspace-local command sets, and tag-based categorization."],["CAVEATS","Shell integration requires sourcing the hook script provided by **intelli-shell init** in your shell's rc file. AI-powered fixes require configuring an API key (OpenAI, Gemini, Anthropic, etc.) in **~/.config/intelli-shell/config.toml**. Workspace awareness relies on **.intellishell** files in project directories."],["HISTORY","**intelli-shell** is maintained by **lasantosr** and is written in **Rust**. It was created to bring IDE-like autocomplete and command management to the terminal."],["INSTALL","```pacman: sudo pacman -S intelli-shell```\n\n```brew: brew install intelli-shell```\n\n```nix: nix profile install nixpkgs#intelli-shell```"],["SEE ALSO","[fzf](/man/fzf)(1), [navi](/man/navi)(1), [atuin](/man/atuin)(1), [mcfly](/man/mcfly)(1)"]],"e":["fzf","navi","atuin","mcfly"],"x":"# Press Ctrl+Space in your shell"},"jc.json":{"n":"jc.json","t":"parses and validates JSON input","s":[["TAGLINE","parses and validates JSON input"],["TLDR","**Parse generic JSON**\n\n```cat [file.json] | jc --json```\n\n**Validate JSON**\n\n```jc --json < [file.json]```\n\n**Parse with pretty output**\n\n```jc --json -p < [file.json]```\n\n**Parse JSON Lines**\n\n```jc --jsonl < [file.jsonl]```"],["SYNOPSIS","**jc** **--json** [_options_]"],["PARAMETERS","**--json**\n> Parse JSON input.\n\n**--jsonl**\n> Parse JSON Lines format.\n\n**-p**, **--pretty**\n> Pretty print output.\n\n**-r**, **--raw**\n> Raw output mode.\n\n**--help**\n> Display help information."],["DESCRIPTION","**jc --json** parses and validates JSON input. It's the JSON passthrough parser in jc for format validation and normalization.\n\nThe parser accepts JSON from stdin and outputs formatted JSON. Useful for validating or reformatting JSON data."],["CAVEATS","Part of jc toolkit. JSON passthrough parser. Mostly for validation/formatting."],["HISTORY","The JSON parser was added to **jc** to provide a complete set of parsers including JSON input handling."],["SEE ALSO","[jc](/man/jc)(1), [jq](/man/jq)(1), [jsonlint](/man/jsonlint)(1)"]],"e":["jc","jq","jsonlint"],"x":"cat [file.json] | jc --json"},"krdpserver":{"n":"krdpserver","t":"KDE-based RDP server for remote connections to a KDE Plasma desktop","s":[["TAGLINE","KDE-based RDP server for remote connections to a KDE Plasma desktop"],["TLDR","Start RDP server with **username and password**\n\n```krdpserver -u [username] -p [password]```\n\nShare a **specific monitor**\n\n```krdpserver -u [username] -p [password] --monitor [n]```"],["SYNOPSIS","**krdpserver** [_options_]"],["PARAMETERS","**-u**, **--username** _USER_\n> Username for RDP authentication\n\n**-p**, **--password** _PASS_\n> Password for RDP authentication\n\n**--monitor** _N_\n> Share specific monitor by index number\n\n**--port** _PORT_\n> Listen on specified port (default: 3389)\n\n**--certificate** _FILE_\n> TLS certificate file for encrypted connections\n\n**--certificate-key** _FILE_\n> TLS certificate private key file"],["DESCRIPTION","**krdpserver** is a KDE-based RDP (Remote Desktop Protocol) server that allows remote connections to a KDE Plasma desktop. It enables users to access their Plasma session remotely using standard RDP clients.\n\nThe server integrates with KDE's portal system and can share the entire desktop or specific monitors. It uses the RDP protocol for compatibility with Windows Remote Desktop and other RDP clients."],["CAVEATS","Requires KDE Plasma desktop environment. Security depends on strong password selection. Firewall rules may need to be configured for remote access."],["HISTORY","krdpserver is part of the KRDP project developed by KDE for native RDP server support in Plasma. It provides an alternative to VNC for remote desktop access on Linux."],["INSTALL","```apt: sudo apt install krdp```\n\n```dnf: sudo dnf install krdp```\n\n```pacman: sudo pacman -S krdp```\n\n```apk: sudo apk add krdp```"],["SEE ALSO","[vncserver](/man/vncserver)(1)"]],"e":["vncserver"],"x":"krdpserver -u [username] -p [password]"},"libtool":{"n":"libtool","t":"generic library support script that hides the complexity of using shared","s":[["TAGLINE","generic library support script that hides the complexity of using shared"],["TLDR","**Compile** source to libtool object\n\n```libtool compile gcc -c [source.c] -o [source.lo]```\n\n**Link** to create library\n\n```libtool link gcc -o [library.lo] [source.lo]```\n\n**Execute** program with library path\n\n```libtool execute gdb [path/to/program]```\n\n**Install** shared library\n\n```libtool install cp [library.la] [install_dir]```\n\n**Finish** library installation\n\n```libtool finish [install_dir]```\n\n**Uninstall** library\n\n```libtool uninstall [library.la]```\n\n**Clean** uninstalled files\n\n```libtool clean rm [source.lo] [library.la]```"],["SYNOPSIS","**libtool** [_mode_] [_mode-args_]"],["DESCRIPTION","**libtool** is a generic library support script that hides the complexity of using shared libraries behind a consistent, portable interface. It handles platform-specific compilation, linking, and installation of shared libraries."],["PARAMETERS","**compile, c**\n> Compile source files into libtool objects (.lo)\n\n**link, l**\n> Create libraries or executables\n\n**execute, e**\n> Execute program with correct library path\n\n**install, i**\n> Install libraries or executables\n\n**finish, f**\n> Complete installation of libtool libraries\n\n**uninstall, u**\n> Delete installed libraries\n\n**clean, cl**\n> Delete uninstalled libraries"],["CAVEATS","Libtool objects (.lo) and archives (.la) are wrappers containing metadata. Actual compiled files are in `.libs/` subdirectory."],["HISTORY","Created by Gordon Matzigkeit in **1996** as part of the GNU project to simplify portable library creation across Unix-like systems."],["INSTALL","```apt: sudo apt install libtool-bin```\n\n```dnf: sudo dnf install libtool```\n\n```pacman: sudo pacman -S libtool```\n\n```apk: sudo apk add libtool```\n\n```zypper: sudo zypper install libtool```\n\n```brew: brew install libtool```\n\n```nix: nix profile install nixpkgs#libtool```"],["SEE ALSO","[libtoolize](/man/libtoolize)(1), [automake](/man/automake)(1), [autoconf](/man/autoconf)(1)"]],"e":["libtoolize","automake","autoconf"],"x":"libtool compile gcc -c [source.c] -o [source.lo]"},"lit":{"n":"lit","t":"LLVM's integrated tester","s":[["TAGLINE","LLVM's integrated tester"],["TLDR","**Run tests**\n\n```lit [tests/]```\n\n**Run specific test**\n\n```lit [test.py]```\n\n**Verbose output**\n\n```lit -v [tests/]```\n\n**Show all output**\n\n```lit -a [tests/]```\n\n**Parallel execution**\n\n```lit -j [4] [tests/]```\n\n**Filter tests**\n\n```lit --filter [pattern] [tests/]```"],["SYNOPSIS","**lit** [_options_] _tests_"],["PARAMETERS","_TESTS_\n> Test files or directories.\n\n**-v**\n> Verbose output.\n\n**-a**\n> Show all output.\n\n**-j** _N_\n> Number of parallel jobs.\n\n**--filter** _PATTERN_\n> Run matching tests only.\n\n**--no-progress-bar**\n> Disable progress bar.\n\n**--help**\n> Display help information."],["DESCRIPTION","**lit** (LLVM Integrated Tester) is a portable test execution framework designed for running compiler and tool test suites. It discovers test files in directory trees, executes them according to configurable test formats, and reports results with support for parallel execution across multiple threads. Tests are typically small scripts with embedded RUN lines that specify the commands to execute and CHECK lines that define expected output patterns.\n\nThe framework supports multiple test formats including ShTest (shell-script-style tests interpreted by lit itself), GoogleTest binaries, and custom formats defined through Python configuration files. Each test directory contains a `lit.cfg` or `lit.cfg.py` file that specifies the test format, available substitutions, and environment configuration. Although lit was built for LLVM's own test infrastructure, it is a general-purpose tool that can be used to test any command-line program."],["CAVEATS","Primarily for LLVM projects. Requires test configuration. Python-based."],["HISTORY","lit was developed as part of the **LLVM** project to provide a flexible, fast testing framework for compiler tests."],["INSTALL","```apk: sudo apk add llvm-test-utils```\n\n```brew: brew install lit```\n\n```nix: nix profile install nixpkgs#lit```"],["SEE ALSO","[pytest](/man/pytest)(1), [make](/man/make)(1)"]],"e":["pytest","make"],"x":"lit [tests/]"},"lvmpersist":{"n":"lvmpersist","t":"Manage SCSI persistent reservations on LVM physical volumes","s":[["TAGLINE","Manage SCSI persistent reservations on LVM physical volumes"],["TLDR","Start PR on all PVs in a VG with a **local key** (exclusive access by default)\n\n```lvmpersist start --ourkey [0x1234abcd] --vg [vg_name]```\n\nStart PR for a **shared VG** (allow multiple hosts)\n\n```lvmpersist start --ourkey [0x1234abcd] --access sh --vg [vg_name]```\n\n**Stop PR** on a VG and unregister the local key\n\n```lvmpersist stop --ourkey [0x1234abcd] --vg [vg_name]```\n\n**Take over** a local VG by preempting another host while starting PR\n\n```lvmpersist start --ourkey [0xmy_key] --removekey [0xother_key] --vg [vg_name]```\n\n**Remove another host's key** from a shared VG\n\n```lvmpersist remove --ourkey [0xmy_key] --removekey [0xother_key] --vg [vg_name]```\n\n**Show registered keys** and reservations for a VG\n\n```lvmpersist read --vg [vg_name]```\n\nOperate on **specific devices** instead of a VG\n\n```lvmpersist start --ourkey [0x1234abcd] --device [/dev/sdX] --device [/dev/mapper/mpathY]```"],["SYNOPSIS","**lvmpersist** _command_ [_options_]"],["PARAMETERS","**start**\n> Register a key and create a persistent reservation\n\n**stop**\n> Release reservation and unregister the key\n\n**remove**\n> Remove another host's key from devices\n\n**read**\n> Display registered keys and current reservations\n\n**--ourkey _key_**\n> The local host's reservation key (hexadecimal)\n\n**--removekey _key_**\n> Another host's key to preempt or remove\n\n**--vg _vg_name_**\n> Operate on all physical volumes in the specified volume group\n\n**--device _device_**\n> Operate on a specific device (can be repeated)\n\n**--access _type_**\n> Reservation type: ex (exclusive) or sh (shared)"],["DESCRIPTION","**lvmpersist** manages SCSI persistent reservations (PR) on block devices used as LVM physical volumes. Persistent reservations prevent multiple hosts from simultaneously accessing shared storage, protecting against data corruption in clustered or failover configurations.\n\nThe tool can establish exclusive reservations (one host owns access) or shared reservations (multiple hosts coordinate access). Keys identify each host, and reservations can be preempted if a host fails or needs to be forcibly removed.\n\nCommon use cases include SAN-based clusters, high-availability failover pairs, and shared storage environments where fencing is required."],["CAVEATS","Requires SCSI devices that support persistent reservations (SCSI-3 PR). Not all storage arrays implement PR identically; test in your environment. Improper use can cause data loss in shared storage environments. Requires root privileges."],["INSTALL","```dnf: sudo dnf install lvm2```\n\n```pacman: sudo pacman -S lvm2```\n\n```apk: sudo apk add lvm2```\n\n```zypper: sudo zypper install lvm2```\n\n```nix: nix profile install nixpkgs#lvm2```"],["SEE ALSO","[lvm](/man/lvm)(8), [vgchange](/man/vgchange)(8), [pvcreate](/man/pvcreate)(8), [pvs](/man/pvs)(8)"]],"e":["lvm","vgchange","pvcreate","pvs"],"x":"lvmpersist start --ourkey [0x1234abcd] --vg [vg_name]"},"marp":{"n":"marp","t":"creates presentations from Markdown","s":[["TAGLINE","creates presentations from Markdown"],["TLDR","**Convert to HTML**\n\n```marp [slides.md] -o [slides.html]```\n\n**Convert to PDF**\n\n```marp [slides.md] --pdf -o [slides.pdf]```\n\n**Convert to PPTX**\n\n```marp [slides.md] --pptx -o [slides.pptx]```\n\n**Convert slides to images**\n\n```marp [slides.md] --images [png] -o [slides/]```\n\n**Preview with live reload**\n\n```marp -p [slides.md]```\n\n**Use custom theme**\n\n```marp --theme [theme.css] [slides.md] -o [output.html]```\n\n**Start server mode**\n\n```marp -s [slides/]```\n\n**Watch for changes and auto-convert**\n\n```marp -w [slides.md] -o [slides.html]```"],["SYNOPSIS","**marp** [_options_] _file_"],["PARAMETERS","**-o**, **--output** _file_\n> Output filename or directory.\n\n**--pdf**\n> Export as PDF.\n\n**--pptx**\n> Export as PowerPoint.\n\n**--images** [png|jpeg]\n> Export slides as multiple image files.\n\n**--image** [png|jpeg]\n> Export the first slide as a single image.\n\n**--notes**\n> Export presenter notes as a text file.\n\n**-p**, **--preview**\n> Open preview window.\n\n**-s**, **--server** _dir_\n> Start server mode for on-demand conversion.\n\n**--theme** _css_\n> Custom theme CSS file.\n\n**-w**, **--watch**\n> Watch for file changes and auto-convert.\n\n**--pdf-notes**\n> Add presenter notes as PDF annotations.\n\n**--pdf-outlines**\n> Add bookmarks to PDF output.\n\n**--allow-local-files**\n> Allow access to local files during conversion.\n\n**--image-scale** _factor_\n> Scale factor for rendered images (default: 1).\n\n**-I**, **--input-dir** _dir_\n> Base directory for input with output directory preservation.\n\n**-c**, **--config-file** _file_\n> Path to configuration file.\n\n**--engine** _module_\n> Specify Marpit-based conversion engine."],["DESCRIPTION","**Marp** (Markdown Presentation Ecosystem) creates presentations from Markdown. It converts specially formatted Markdown into slides in HTML, PDF, or PowerPoint formats.\n\nSlides are separated by --- and support custom themes, speaker notes, and directives."],["SLIDE FORMAT","```markdown\n---\nmarp: true\ntheme: default\n---"],["Slide 1","Content here\n\n---"],["Slide 2","More content\n```"],["CAVEATS","PDF and image export require Chrome, Edge, or Firefox. Some CSS features are limited in PPTX output. Local file references require **--allow-local-files** for PDF/image conversion."],["HISTORY","Marp was created by **Yuki Hattori** (yhatt) in **2016** as Marp desktop app, later rewritten as Marp Next ecosystem with CLI."],["SEE ALSO","[pandoc](/man/pandoc)(1)"]],"e":["pandoc"],"x":"marp [slides.md] -o [slides.html]"},"mmroff":{"n":"mmroff","t":"Wrapper around groff that resolves cross references in mm documents","s":[["TAGLINE","Wrapper around groff that resolves cross references in mm documents"],["TLDR","**Process an mm document** (default output: PostScript on stdout)\n\n```mmroff [document.mm]```\n\n**Render to PostScript** (passes -Tps through to groff)\n\n```mmroff -Tps [document.mm] > [document.ps]```\n\n**Render to PDF**\n\n```mmroff -Tpdf [document.mm] > [document.pdf]```\n\n**Render to HTML for preview**\n\n```mmroff -Thtml [document.mm] > [document.html]```\n\n**Build only the cross-reference file** and exit\n\n```mmroff -x [document.mm]```"],["SYNOPSIS","**mmroff** [**-x**] _groff_argument_...\n\n**mmroff** **--help**\n\n**mmroff** **--version**"],["PARAMETERS","_GROFF_ARGUMENT_\n> Any option or input file accepted by **groff** (e.g. **-Tps**, **-Tpdf**, **-Thtml**, the input filename). The **-mm** macro package is added automatically.\n\n**-x**\n> Build or update the cross-reference / index file and exit without producing output.\n\n**--help**\n> Show usage and exit.\n\n**--version**\n> Show version and exit."],["DESCRIPTION","**mmroff** is a tiny shell wrapper around **groff** that handles mm cross references and indexes. It calls **groff -mm** twice: first with **-z -rRef=1** to populate the cross-reference and index files, then again normally to typeset the document. The wrapper also expands PSPIC image inclusions.\n\nFor mm documents that do not use cross references, indexes, or embedded PostScript pictures, calling **groff -mm** directly is equivalent and slightly faster."],["CAVEATS","Part of GNU **groff**. Output format is selected with **-T**_device_ exactly as for groff itself; the wrapper does not invent its own format flags. PDF support requires the **-Tpdf** device introduced in groff 1.23. Documents authored with the older AT&T mm macros are not 100% bug-compatible with GNU mm."],["HISTORY","**mmroff** was written by **Jörgen Hägg** of Lund, Sweden, as part of the GNU **mm** macro package contributed to **groff**."],["INSTALL","```apt: sudo apt install groff```\n\n```dnf: sudo dnf install groff```\n\n```pacman: sudo pacman -S groff```\n\n```apk: sudo apk add groff```\n\n```zypper: sudo zypper install groff```\n\n```brew: brew install groff```\n\n```nix: nix profile install nixpkgs#groff```"],["SEE ALSO","[groff](/man/groff)(1), [nroff](/man/nroff)(1), [troff](/man/troff)(1)"]],"e":["groff","nroff","troff"],"x":"mmroff [document.mm]"},"mplayer":{"n":"mplayer","t":"powerful media player","s":[["TAGLINE","powerful media player"],["TLDR","**Play video file**\n\n```mplayer [video.mp4]```\n\n**Play audio file**\n\n```mplayer [audio.mp3]```\n\n**Play fullscreen**\n\n```mplayer -fs [video.mp4]```\n\n**Specify audio output**\n\n```mplayer -ao [alsa] [file]```\n\n**Loop playback**\n\n```mplayer -loop [0] [file]```\n\n**Set start position**\n\n```mplayer -ss [00:01:30] [video.mp4]```\n\n**Dump audio stream**\n\n```mplayer -dumpaudio -dumpfile [out.mp3] [video.mp4]```"],["SYNOPSIS","**mplayer** [_options_] _file_"],["PARAMETERS","_FILE_\n> Media file to play.\n\n**-fs**\n> Fullscreen mode.\n\n**-ao** _DRIVER_\n> Audio output driver.\n\n**-vo** _DRIVER_\n> Video output driver.\n\n**-loop** _N_\n> Loop count (0=infinite).\n\n**-ss** _TIME_\n> Seek to position.\n\n**-dumpaudio**\n> Dump raw audio stream to a file.\n\n**-dumpfile** _FILE_\n> Filename for dumped audio/video stream.\n\n**-speed** _FACTOR_\n> Playback speed multiplier (0.01-100, default: 1.0).\n\n**-quiet**\n> Suppress most status messages.\n\n**-identify**\n> Print media properties (useful for scripting)."],["DESCRIPTION","**mplayer** is a powerful, command-line media player that supports a vast range of audio and video formats. It uses FFmpeg's libavcodec and libavformat for decoding, along with native codecs for some formats.\n\nKeyboard shortcuts control playback: arrow keys seek, **p** pauses, **q** quits, **f** toggles fullscreen, and **+/-** adjusts audio delay. The player can also be controlled via a FIFO pipe or slave mode for scripting."],["CAVEATS","MPlayer is considered legacy software; **mpv** is its actively maintained successor with a modern codebase. Some distributions no longer package MPlayer. Configuration files are in **~/.mplayer/**."],["HISTORY","**MPlayer** was started by **Árpád Gereöffy** in **2000**. It became one of the most popular open-source media players on Linux and influenced several successors including **mpv** (forked in 2013) and **MPlayer2**."],["INSTALL","```apt: sudo apt install mplayer```\n\n```pacman: sudo pacman -S mplayer```\n\n```apk: sudo apk add mplayer```\n\n```brew: brew install mplayer```\n\n```nix: nix profile install nixpkgs#mplayer```"],["SEE ALSO","[mpv](/man/mpv)(1), [vlc](/man/vlc)(1), [ffplay](/man/ffplay)(1)"]],"e":["mpv","vlc","ffplay"],"x":"mplayer [video.mp4]"},"ndctl":{"n":"ndctl","t":"utility for managing Non-Volatile DIMMs on Linux","s":[["TAGLINE","utility for managing Non-Volatile DIMMs on Linux"],["TLDR","Create an **fsdax mode namespace**\n\n```ndctl create-namespace --mode=fsdax```\n\n**Change mode** of a namespace to raw\n\n```ndctl create-namespace --reconfigure=[namespaceX.Y] --mode=raw```\n\n**Check and repair** a sector mode namespace\n\n```ndctl check-namespace --repair [namespaceX.Y]```\n\n**List all** namespaces, regions, and buses\n\n```ndctl list --namespaces --regions --buses --idle```\n\nList namespace with **verbose information**\n\n```ndctl list -vvv --namespace=[namespaceX.Y]```\n\n**Monitor** SMART health events\n\n```ndctl monitor --bus=ACPI.NFIT```\n\n**Destroy** or reset a namespace\n\n```ndctl destroy-namespace --force [namespaceX.Y]```"],["SYNOPSIS","**ndctl** _command_ [_options_] [_namespace_]"],["COMMANDS","**create-namespace**\n> Create a new namespace with specified mode\n\n**destroy-namespace**\n> Remove or reset namespace to initial state\n\n**check-namespace**\n> Check namespace for consistency\n\n**list**\n> List namespaces, regions, buses, and dimms\n\n**monitor**\n> Watch for SMART health events\n\n**enable-namespace**\n> Enable a disabled namespace\n\n**disable-namespace**\n> Disable an active namespace\n\n**enable-region**\n> Enable a region\n\n**disable-region**\n> Disable a region"],["PARAMETERS","**--mode _mode_**\n> Namespace mode: fsdax, devdax, sector, raw\n\n**--reconfigure _namespace_**\n> Reconfigure existing namespace\n\n**--repair**\n> Repair errors found during check\n\n**--force, -f**\n> Force operation without confirmation\n\n**--bus _bus_**\n> Operate on specific bus\n\n**--region _region_**\n> Operate on specific region\n\n**--idle**\n> Include disabled/idle objects in listing\n\n**-v, -vv, -vvv**\n> Increase verbosity level"],["DESCRIPTION","**ndctl** is a utility for managing Non-Volatile DIMMs (NVDIMMs) on Linux. NVDIMMs are persistent memory modules that retain data without power, providing storage-class memory capabilities.\n\nThe tool manages namespaces (logical storage units), regions (physical NVDIMM groupings), and monitors NVDIMM health. Different namespace modes support various use cases: fsdax for filesystem DAX, devdax for device DAX, sector for legacy block access."],["CAVEATS","Operations on active namespaces may require unmounting filesystems first. Destroying namespaces is irreversible. NVDIMM support requires appropriate hardware and kernel configuration. Health monitoring requires ACPI NFIT support."],["HISTORY","**ndctl** was developed by Intel and the Linux community as part of the **pmem** (persistent memory) project. It provides userspace tools for managing NVDIMMs as standardized by ACPI NFIT and the NVDIMM driver subsystem."],["INSTALL","```apt: sudo apt install ndctl```\n\n```dnf: sudo dnf install ndctl```\n\n```pacman: sudo pacman -S ndctl```\n\n```apk: sudo apk add ndctl```\n\n```zypper: sudo zypper install ndctl```"],["SEE ALSO","[daxctl](/man/daxctl)(1), [cxl](/man/cxl)(1)"]],"e":["daxctl","cxl"],"x":"ndctl create-namespace --mode=fsdax"},"netstat":{"n":"netstat","t":"displays network connections, routing tables, interface statistics, masquerade","s":[["TAGLINE","displays network connections, routing tables, interface statistics, masquerade"],["TLDR","**Show all connections** and listening ports\n\n```netstat -a```\n\n**Show listening ports only**\n\n```netstat -l```\n\n**Show TCP connections** with numeric addresses\n\n```netstat -tn```\n\n**Show listening ports with process IDs**\n\n```sudo netstat -tulnp```\n\n**Show routing table**\n\n```netstat -r```\n\n**Show network interface statistics**\n\n```netstat -i```\n\n**Show protocol statistics**\n\n```netstat -s```\n\n**Continuously refresh** connection status\n\n```netstat -c```"],["SYNOPSIS","**netstat** [_options_]\n\n**netstat** {--route|-r} [_options_]\n\n**netstat** {--interfaces|-i} [_options_]\n\n**netstat** {--statistics|-s} [_options_]"],["PARAMETERS","**-a**, **--all**\n> Show both listening and non-listening sockets\n\n**-l**, **--listening**\n> Show only listening sockets\n\n**-t**, **--tcp**\n> Show TCP connections\n\n**-u**, **--udp**\n> Show UDP connections\n\n**-n**, **--numeric**\n> Show numeric addresses instead of resolving names\n\n**-p**, **--program**\n> Show PID and program name for each socket\n\n**-r**, **--route**\n> Display kernel routing table\n\n**-i**, **--interfaces**\n> Display network interface table\n\n**-g**, **--groups**\n> Display multicast group membership\n\n**-s**, **--statistics**\n> Display protocol statistics (SNMP)\n\n**-M**, **--masquerade**\n> Display masqueraded connections\n\n**-c**, **--continuous**\n> Refresh display every second\n\n**-e**, **--extend**\n> Display extended information (use twice for more)\n\n**-o**, **--timers**\n> Include networking timer information\n\n**-v**, **--verbose**\n> Verbose output\n\n**-W**, **--wide**\n> Don't truncate IP addresses\n\n**-4**\n> Show IPv4 only\n\n**-6**\n> Show IPv6 only\n\n**-A** _FAMILY_\n> Address families (inet, inet6, unix, ipx, etc.)"],["DESCRIPTION","**netstat** displays network connections, routing tables, interface statistics, masquerade connections, and multicast memberships. It is a traditional tool for network troubleshooting and monitoring.\n\nWithout options, netstat shows open sockets. The most common usage (**netstat -tulnp**) shows TCP and UDP listening ports with process information and numeric addresses. The routing table (**-r**) shows how packets are directed, while interface statistics (**-i**) show traffic and errors per network interface.\n\nOutput columns include: **Proto** (protocol), **Recv-Q/Send-Q** (data queued), **Local/Foreign Address** (endpoints), **State** (connection state like LISTEN, ESTABLISHED, TIME_WAIT), and optionally **PID/Program** (process using the socket)."],["CAVEATS","netstat is considered obsolete on Linux; **ss** is the recommended replacement with better performance and more features. Showing process information (**-p**) requires root privileges. Name resolution can slow output significantly; use **-n** for faster results. Some options may vary between Unix implementations."],["HISTORY","netstat originated in **BSD Unix** in the early 1980s as part of the original TCP/IP implementation. It became a standard networking tool across all Unix-like systems and Windows. On Linux, netstat was part of the **net-tools** package, which is now deprecated in favor of the **iproute2** suite. The **ss** command from iproute2, introduced around **2001**, provides similar functionality with better performance by reading directly from kernel data structures."],["INSTALL","```apt: sudo apt install net-tools```\n\n```dnf: sudo dnf install net-tools```\n\n```pacman: sudo pacman -S net-tools```\n\n```apk: sudo apk add net-tools```\n\n```zypper: sudo zypper install net-tools```\n\n```brew: brew install net-tools```\n\n```nix: nix profile install nixpkgs#net-tools```"],["SEE ALSO","[ss](/man/ss)(8), [ip](/man/ip)(8), [lsof](/man/lsof)(8), [ifconfig](/man/ifconfig)(8)"]],"e":["ss","ip","lsof","ifconfig"],"x":"netstat -a"},"nix3-run":{"n":"nix3-run","t":"executes packages without installing","s":[["TAGLINE","executes packages without installing"],["TLDR","**Run package**\n\n```nix3 run nixpkgs#[hello]```\n\n**Run from flake**\n\n```nix3 run [.#default]```\n\n**Run with args**\n\n```nix3 run nixpkgs#[cowsay] -- \"[text]\"```"],["SYNOPSIS","**nix3** **run** [_options_] _installable_ [-- _args_]"],["PARAMETERS","_INSTALLABLE_\n> Flake reference.\n\n_ARGS_\n> Program arguments.\n\n**--help**\n> Display help information."],["DESCRIPTION","**nix3 run** executes packages without installing. Alternative name for nix run.\n\nThe tool runs programs temporarily. Downloads and executes from store."],["CAVEATS","Alias for nix run. Network required. Temporary execution."],["HISTORY","nix3 run is the **new Nix CLI** for running packages without system installation."],["SEE ALSO","[nix](/man/nix)(1), [nix-run](/man/nix-run)(1), [nix3-shell](/man/nix3-shell)(1)"]],"e":["nix","nix-run","nix3-shell"],"x":"nix3 run nixpkgs#[hello]"},"openstack-image":{"n":"openstack-image","t":"manages virtual machine images in OpenStack Glance","s":[["TAGLINE","manages virtual machine images in OpenStack Glance"],["TLDR","**List images**\n\n```openstack image list```\n\n**Show image details**\n\n```openstack image show [ubuntu-22.04]```\n\n**Create image from file**\n\n```openstack image create --file [image.qcow2] --disk-format qcow2 [image_name]```\n\n**Delete image**\n\n```openstack image delete [image_name]```"],["SYNOPSIS","**openstack image** _command_ [_options_]"],["PARAMETERS","**list**\n> List images.\n\n**show** _name_\n> Show image details.\n\n**create** _name_\n> Create/upload image.\n\n**delete** _name_\n> Delete image.\n\n**--file** _path_\n> Image file to upload.\n\n**--disk-format** _format_\n> Disk format (qcow2, raw, vmdk, etc.).\n\n**--container-format** _format_\n> Container format (bare, ovf, etc.).\n\n**--public**\n> Make image public."],["DESCRIPTION","**openstack image** manages virtual machine images in OpenStack Glance. Upload, download, and manage boot images for instances. Part of OpenStack unified CLI."],["SEE ALSO","[openstack-server](/man/openstack-server)(1), [openstack](/man/openstack)(1)"]],"e":["openstack-server","openstack"],"x":"openstack image list"},"pacgraph":{"n":"pacgraph","t":"visualizes the dependency relationships between installed packages","s":[["TAGLINE","visualizes the dependency relationships between installed packages"],["TLDR","Produce an **SVG and PNG** graph\n\n```pacgraph```\n\nProduce an **SVG** graph only\n\n```pacgraph -s```\n\nPrint **summary to console**\n\n```pacgraph -c```\n\nOverride the default **filename/location**\n\n```pacgraph -f [path/to/file]```\n\nChange the color of **packages that are not dependencies**\n\n```pacgraph -t [color]```\n\nChange the color of **package dependencies**\n\n```pacgraph -d [color]```\n\nChange the **background color**\n\n```pacgraph -b [color]```\n\nChange the color of **links between packages**\n\n```pacgraph -l [color]```"],["SYNOPSIS","**pacgraph** [_options_]"],["PARAMETERS","**-s**, **--svg**\n> Produce SVG output only (no PNG)\n\n**-c**, **--console**\n> Print summary to console instead of generating graph\n\n**-f**, **--file** _path_\n> Output filename (without extension)\n\n**-t**, **--top** _color_\n> Color for explicitly installed packages\n\n**-d**, **--dep** _color_\n> Color for dependency packages\n\n**-b**, **--background** _color_\n> Background color of the graph\n\n**-l**, **--link** _color_\n> Color for dependency links\n\n**-p**, **--point** _size_\n> Font point size\n\n**-n**, **--no-compression**\n> Disable SVGZ compression\n\n**-e**, **--explicits**\n> Include only explicitly installed packages\n\n**-o**, **--optdeps**\n> Include optional dependencies\n\n**--show-req-by**\n> Show packages that require the listed package (reverse dependencies)\n\n**-m** _repo_\n> Draw only the specified package/repo and its dependencies"],["DESCRIPTION","**pacgraph** visualizes the dependency relationships between installed packages on Arch-based systems. It generates a graph showing packages as nodes sized by their disk usage, with lines connecting packages to their dependencies.\n\nThe output can be an SVG image, PNG image, or console text summary. The tool helps users understand their system's package structure, identify large packages, and visualize dependency chains. Colors distinguish between explicitly installed packages and dependencies."],["CAVEATS","Large systems with many packages produce complex graphs that may be difficult to read. PNG generation requires additional dependencies (typically graphviz or PIL). Do not include file extension in the **-f** option."],["HISTORY","Developed for Arch Linux to provide visual insight into package relationships. Helps users understand disk usage distribution across packages and identify opportunities for cleanup by showing which packages have few dependents."],["INSTALL","```aur: yay -S pacgraph```"],["SEE ALSO","[pacman](/man/pacman)(8), [pactree](/man/pactree)(8), [expac](/man/expac)(1)"]],"e":["pacman","pactree","expac"],"x":"pacgraph"},"pagecast":{"n":"pagecast","t":"Publish Markdown and HTML reports to Cloudflare Pages","s":[["TAGLINE","Publish Markdown and HTML reports to Cloudflare Pages"],["TLDR","**Start** the local admin UI\n\n```npx pagecast```\n\n**Set up** a Cloudflare Pages project\n\n```npx pagecast pages setup --project [name]```\n\n**Publish** an HTML or Markdown file\n\n```npx pagecast publish \"[/absolute/path/to/report.html]\"```\n\n**Publish with** password protection\n\n```npx pagecast publish \"[/absolute/path/to/report.html]\" --password \"[password]\"```\n\n**Deploy** a static site directory\n\n```npx pagecast pages deploy \"[$(pwd)/dist]\" --project [name]```"],["SYNOPSIS","**npx pagecast** [_subcommand_] [_options_]"],["PARAMETERS","**pages setup**\n> Configure a new Cloudflare Pages project\n\n**publish** _file_\n> Publish a single HTML or Markdown report to Cloudflare Pages\n\n**pages deploy** _dir_\n> Deploy a full static site directory\n\n**--project** _name_\n> Cloudflare Pages project name\n\n**--account** _id_\n> Cloudflare account ID (for multi-account setups)\n\n**--password** _pass_\n> Add password protection to the published page\n\n**--no-password**\n> Remove existing password protection from a published page\n\n**--branch** _name_\n> Deployment branch name (default: main)\n\n**--json**\n> Emit machine-readable JSON output for CI/CD and scripting"],["DESCRIPTION","**pagecast** publishes Markdown and HTML reports to the user's own Cloudflare Pages account, making them accessible at a public URL. It provides both a browser-based admin UI (at `http://127.0.0.1:4173`) for managing published links and a CLI interface for automation and CI/CD pipelines.\n\nPassword protection can be added or removed per deployment. The `--json` flag enables structured output for agent and scripted workflows.\n\nNo global installation is required: `npx pagecast` downloads and runs the package on demand using Node.js."],["CAVEATS","Requires Node.js 20 or later and a Cloudflare account. Paths passed to the `publish` subcommand must be absolute. Cloudflare Pages project setup is a one-time prerequisite before publishing."],["SEE ALSO","[npx](/man/npx)(1), [wrangler](/man/wrangler)(1)"],["RESOURCES","```[Source code](https://github.com/Amal-David/pagecast)```"]],"e":["npx","wrangler"],"x":"npx pagecast"},"pgmramp":{"n":"pgmramp","t":"generates grayscale gradient images","s":[["TAGLINE","generates grayscale gradient images"],["TLDR","**Create horizontal gradient**\n\n```pgmramp -lr [256] [256] > [gradient.pgm]```\n\n**Create vertical gradient**\n\n```pgmramp -tb [256] [256] > [gradient.pgm]```\n\n**Create diagonal gradient**\n\n```pgmramp -diagonal [256] [256] > [gradient.pgm]```\n\n**Create circular gradient**\n\n```pgmramp -ellipse [256] [256] > [gradient.pgm]```"],["SYNOPSIS","**pgmramp** _type_ _width_ _height_"],["PARAMETERS","**-lr**\n> Left to right gradient.\n\n**-tb**\n> Top to bottom gradient.\n\n**-rectangle**\n> Rectangle gradient.\n\n**-ellipse**\n> Elliptical gradient.\n\n**-diagonal**\n> Diagonal gradient."],["DESCRIPTION","**pgmramp** generates grayscale gradient images. Creates various gradient patterns for testing and backgrounds. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[ppmpat](/man/ppmpat)(1), [pgmnoise](/man/pgmnoise)(1)"]],"e":["ppmpat","pgmnoise"],"x":"pgmramp -lr [256] [256] > [gradient.pgm]"},"pio-init":{"n":"pio-init","t":"initializes a new PlatformIO project","s":[["TAGLINE","initializes a new PlatformIO project"],["TLDR","**Initialize project in current directory**\n\n```pio init```\n\n**Initialize with specific board**\n\n```pio init --board [uno]```\n\n**Initialize with IDE support**\n\n```pio init --ide [vscode]```\n\n**Initialize in specific directory**\n\n```pio init -d [project_dir]```"],["SYNOPSIS","**pio init** [_options_]"],["PARAMETERS","**-b**, **--board** _id_\n> Board ID to initialize for.\n\n**-d**, **--project-dir** _dir_\n> Project directory.\n\n**--ide** _ide_\n> Generate IDE project files (vscode, atom, etc.).\n\n**-O** _option_\n> Project option in format section.option=value.\n\n**-e**, **--environment** _name_\n> Default environment name."],["DESCRIPTION","**pio init** initializes a new PlatformIO project. Creates project structure with platformio.ini configuration file. Supports multiple boards and IDE integrations for embedded development."],["INSTALL","```aur: yay -S pio```"],["SEE ALSO","[pio-run](/man/pio-run)(1), [pio-boards](/man/pio-boards)(1), [pio-lib](/man/pio-lib)(1)"]],"e":["pio-run","pio-boards","pio-lib"],"x":"pio init"},"piper-tts":{"n":"piper-tts","t":"Fast local neural text-to-speech CLI","s":[["TAGLINE","Fast local neural text-to-speech CLI"],["TLDR","**Download** a voice model\n\n```python3 -m piper.download_voices [en_US-lessac-medium]```\n\n**Synthesize** text from stdin to a WAV file\n\n```echo \"[Hello world.]\" | piper --model [en_US-lessac-medium] --output-file [out.wav]```\n\n**Synthesize** text given inline\n\n```piper --model [en_US-lessac-medium] --output-file [greeting.wav] -- \"[This is a test.]\"```\n\n**Stream raw audio** straight to a player\n\n```echo \"[Hi there.]\" | piper --model [en_US-lessac-medium] --output-raw | aplay -r [22050] -f S16_LE -t raw -```\n\n**Select a speaker** in a multi-speaker model\n\n```piper --model [de_DE-thorsten-medium] --speaker [0] --output-file [de.wav] -- \"[Guten Tag.]\"```\n\n**Use GPU** acceleration\n\n```piper --model [en_US-lessac-medium] --cuda --output-file [gpu.wav] -- \"[Running on GPU.]\"```"],["SYNOPSIS","**piper** **--model** _voice_ [**--output-file** _file_ | **--output-raw**] [_options_]"],["PARAMETERS","**-m**, **--model** _voice_\n> Voice model identifier (e.g. _en_US-lessac-medium_) or path to an ONNX file.\n\n**-f**, **--output-file** _file_\n> Write the synthesized WAV audio to _file_.\n\n**--output-raw**\n> Write raw 16-bit PCM audio to stdout (for piping into a player).\n\n**--input-file** _file_\n> Read input text from _file_ instead of stdin.\n\n**--data-dir** _dir_\n> Directory where downloaded voice files are stored.\n\n**--speaker** _n_\n> Select speaker index for multi-speaker voices.\n\n**--sentence-silence** _seconds_\n> Seconds of silence inserted between sentences.\n\n**--volume** _factor_\n> Output volume multiplier (1.0 is unchanged).\n\n**--cuda**\n> Use the CUDA execution provider (requires onnxruntime-gpu).\n\n**--json-input**\n> Read JSON objects from stdin instead of plain text."],["DESCRIPTION","**piper** is an offline neural text-to-speech engine that runs VITS voice models exported to ONNX. Installing the **piper-tts** Python package provides the **piper** binary, which reads text from standard input (or the file given by **--input-file**) and writes 16-bit PCM WAV audio either to a file or to standard output for streaming.\n\nVoices are distributed separately and downloaded with **python3 -m piper.download_voices**. Models cover many languages and accents, and several are multi-speaker — use **--speaker** to pick a voice. Phonemization is performed via embedded **espeak-ng**, and inline overrides written in double brackets (for example `[[ bˈætmæn ]]`) are accepted for fine-grained pronunciation control.\n\nOutput quality and latency depend on the model variant (_x_low_, _low_, _medium_, _high_). For interactive use, piper is typically wrapped behind a daemon so the model is loaded once instead of on every invocation."],["CAVEATS","Loading the ONNX model has noticeable startup cost; for low-latency applications run piper as a streaming service rather than per-sentence. Output sample rate is fixed by the chosen voice (commonly 22050 Hz) and must match the player's settings when using **--output-raw**. Voice models carry their own licenses."],["HISTORY","**piper** was created by **Michael Hansen** in **November 2022** under the **Rhasspy** umbrella for use in offline voice assistants and is now developed at **Nabu Casa / Open Home Foundation** as the GPL-3.0 **piper1-gpl** project. It powers TTS in **Home Assistant**, the **NVDA** screen reader, and **LocalAI**."],["INSTALL","```nix: nix profile install nixpkgs#piper-tts```"],["SEE ALSO","[espeak-ng](/man/espeak-ng)(1), [festival](/man/festival)(1), [flite](/man/flite)(1)"]],"e":["espeak-ng","festival","flite"],"x":"python3 -m piper.download_voices [en_US-lessac-medium]"},"pkcs8.1s":{"n":"pkcs8.1s","t":"Convert private keys to PKCS#8 format","s":[["TAGLINE","Convert private keys to PKCS#8 format"],["TLDR","**Convert to PKCS#8**\n\n```openssl pkcs8 -topk8 -in [key.pem] -out [key-pkcs8.pem]```\n\n**Convert with encryption**\n\n```openssl pkcs8 -topk8 -in [key.pem] -out [key.p8] -v2 aes256```\n\n**Convert to unencrypted**\n\n```openssl pkcs8 -topk8 -in [key.pem] -out [key.p8] -nocrypt```\n\n**Convert from PKCS#8**\n\n```openssl pkcs8 -in [key.p8] -out [key.pem]```"],["SYNOPSIS","**openssl** **pkcs8** [_options_]"],["PARAMETERS","**-topk8**\n> Convert to PKCS#8.\n\n**-in** _FILE_\n> Input key file.\n\n**-out** _FILE_\n> Output file.\n\n**-nocrypt**\n> No encryption.\n\n**-v2** _CIPHER_\n> Encryption cipher.\n\n**-inform** _FORMAT_\n> Input format."],["DESCRIPTION","**openssl pkcs8** converts private keys between traditional OpenSSL format and the standardized PKCS#8 format. PKCS#8 is a widely supported container format for private keys that works across different cryptographic libraries and programming languages.\n\nThe **-topk8** flag converts from traditional to PKCS#8 format. Keys can be encrypted with various ciphers using **-v2** or left unencrypted with **-nocrypt**. Without **-topk8**, the tool reads PKCS#8 input and outputs in traditional format."],["CAVEATS","Part of OpenSSL. Key format conversion. Encryption optional."],["HISTORY","PKCS#8 support in OpenSSL enables **private key format** conversion."],["SEE ALSO","[openssl](/man/openssl)(1), [openssl-rsa](/man/openssl-rsa)(1)"]],"e":["openssl","openssl-rsa"],"x":"openssl pkcs8 -topk8 -in [key.pem] -out [key-pkcs8.pem]"},"pkgfile":{"n":"pkgfile","t":"Search Arch repositories for file ownership","s":[["TAGLINE","Search Arch repositories for file ownership"],["TLDR","**Synchronize** pkgfile database\n\n```sudo pkgfile --update```\n\nSearch for package **owning a file**\n\n```pkgfile [filename]```\n\n**List files** provided by a package\n\n```pkgfile --list [package]```\n\nList **executables** in a package\n\n```pkgfile --list --binaries [package]```\n\n**Case-insensitive** search\n\n```pkgfile --ignorecase [filename]```\n\nSearch in **bin/sbin** directories only\n\n```pkgfile --binaries [filename]```\n\nSearch with **version** display\n\n```pkgfile --verbose [filename]```\n\nSearch in a **specific repository**\n\n```pkgfile --repo [core] [filename]```"],["SYNOPSIS","**pkgfile** [**--update**] [**--list**] [**--binaries**] [**--verbose**] [**--repo** _repo_] _target_"],["PARAMETERS","**-u, --update**\n> Synchronize pkgfile database\n\n**-l, --list**\n> List files in package\n\n**-b, --binaries**\n> Only search/list executables\n\n**-i, --ignorecase**\n> Case-insensitive matching\n\n**-v, --verbose**\n> Show package version\n\n**-R, --repo** _repo_\n> Search specific repository\n\n**-g, --glob**\n> Enable shell-style glob pattern matching\n\n**-r, --regex**\n> Enable regular expression matching\n\n**-d, --directories**\n> Include directories in search results\n\n**-q, --quiet**\n> Suppress error messages\n\n**-0, --null**\n> Delimit output with null bytes"],["DESCRIPTION","**pkgfile** searches for packages in the official Arch Linux repositories that contain a specific file. It maintains its own database of file-to-package mappings for fast lookups.\n\nThe tool is commonly used to find which package provides a missing command or file. It can also list all files a package contains, useful for understanding package contents before installation."],["CAVEATS","Database must be synchronized before use. Only searches official repositories, not AUR. Database updates require root privileges. Large database may take time to download initially."],["HISTORY","**pkgfile** was created for Arch Linux to provide fast file-to-package lookups. It offers similar functionality to `pacman -F` but with a dedicated database optimized for this purpose."],["INSTALL","```pacman: sudo pacman -S pkgfile```"],["SEE ALSO","[pacman](/man/pacman)(8), [pactree](/man/pactree)(1), [makepkg](/man/makepkg)(8)"]],"e":["pacman","pactree","makepkg"],"x":"sudo pkgfile --update"},"pnmtopng":{"n":"pnmtopng","t":"Convert PNM images to PNG format","s":[["TAGLINE","Convert PNM images to PNG format"],["TLDR","**Convert PNM to PNG**\n\n```pnmtopng [input.pnm] > [output.png]```\n\n**Convert with compression**\n\n```pnmtopng -compression [9] [input.pnm] > [output.png]```\n\n**Add alpha channel**\n\n```pnmtopng -alpha [alpha.pgm] [input.ppm] > [output.png]```\n\n**Create interlaced PNG**\n\n```pnmtopng -interlace [input.pnm] > [output.png]```"],["SYNOPSIS","**pnmtopng** [_options_] [_file_]"],["PARAMETERS","**-alpha** _file_\n> Alpha channel file.\n\n**-compression** _n_\n> Compression level (0-9).\n\n**-interlace**\n> Create interlaced PNG.\n\n**-transparent** _color_\n> Transparent color.\n\n**-gamma** _value_\n> Gamma value."],["DESCRIPTION","**pnmtopng** converts PNM (PBM, PGM, PPM) images to PNG format with lossless compression. It automatically selects the optimal PNG color type based on the input image.\n\nThe **-alpha** option attaches a separate PGM file as an alpha transparency channel. The **-compression** level (0-9) controls the deflate compression effort, and **-interlace** creates Adam7 interlaced PNGs for progressive display. The **-transparent** option designates a specific color as fully transparent. Part of the Netpbm toolkit; superseded by **pamtopng**."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pngtopnm](/man/pngtopnm)(1), [pamtopng](/man/pamtopng)(1)"]],"e":["pngtopnm","pamtopng"],"x":"pnmtopng [input.pnm] > [output.png]"},"podman-run":{"n":"podman-run","t":"Create and start containers from images","s":[["TAGLINE","Create and start containers from images"],["TLDR","**Run a container**\n\n```podman run [image]```\n\n**Run interactively**\n\n```podman run -it [image] [/bin/bash]```\n\n**Run in background**\n\n```podman run -d [image]```\n\n**Run with port mapping**\n\n```podman run -p [8080:80] [image]```\n\n**Run with volume mount**\n\n```podman run -v [/host/path:/container/path] [image]```\n\n**Run with name**\n\n```podman run --name [mycontainer] [image]```\n\n**Run with environment variable**\n\n```podman run -e [VAR=value] [image]```\n\n**Run and auto-remove on exit**\n\n```podman run --rm -it [image] [command]```\n\n**Run with custom network**\n\n```podman run --network [network_name] [image]```"],["SYNOPSIS","**podman run** [_options_] _image_ [_command_]"],["PARAMETERS","_IMAGE_\n> Container image.\n\n**-it**\n> Interactive terminal.\n\n**-d**, **--detach**\n> Run in background.\n\n**-p**, **--publish** _PORT_\n> Port mapping.\n\n**-v**, **--volume** _MOUNT_\n> Volume mount.\n\n**--name** _NAME_\n> Container name.\n\n**-e**, **--env** _VAR_\n> Environment variable.\n\n**--rm**\n> Remove container after exit.\n\n**--network** _MODE_\n> Set network mode (bridge, host, none, or custom network name).\n\n**--restart** _POLICY_\n> Restart policy (no, on-failure[:max], always, unless-stopped).\n\n**-w**, **--workdir** _DIR_\n> Working directory inside the container.\n\n**--user** _USER_\n> Run as specified user (name or UID[:GID]).\n\n**--cap-add** _CAP_\n> Add Linux capabilities.\n\n**--cap-drop** _CAP_\n> Drop Linux capabilities.\n\n**--entrypoint** _CMD_\n> Override image entrypoint.\n\n**--label** _KEY=VALUE_\n> Set metadata label on container."],["DESCRIPTION","**podman run** creates and starts a new container from a specified image. It is the primary command for launching containers, supporting interactive sessions (**-it**), background execution (**-d**), port mapping (**-p**), volume mounts (**-v**), and environment variables (**-e**).\n\nThe **--rm** flag automatically removes the container when it exits. The **--name** option assigns a human-readable name. By default, Podman runs containers rootlessly without requiring a daemon, making it a drop-in replacement for docker run."],["CAVEATS","Runs rootless by default without requiring a daemon. Most Docker CLI flags are compatible. Some features (e.g., certain network modes) may behave differently in rootless mode."],["HISTORY","**podman run** was introduced as part of the Podman project by Red Hat, providing a daemonless, Docker-compatible container runtime."],["INSTALL","```apt: sudo apt install podman```\n\n```dnf: sudo dnf install podman```\n\n```pacman: sudo pacman -S podman```\n\n```apk: sudo apk add podman```\n\n```zypper: sudo zypper install podman```\n\n```brew: brew install podman```\n\n```nix: nix profile install nixpkgs#podman```"],["SEE ALSO","[podman](/man/podman)(1), [podman-exec](/man/podman-exec)(1), [docker-run](/man/docker-run)(1)"]],"e":["podman","podman-exec","docker-run"],"x":"podman run [image]"},"ppmtoarbtxt":{"n":"ppmtoarbtxt","t":"Convert PPM to custom text formats","s":[["TAGLINE","Convert PPM to custom text formats"],["TLDR","**Convert PPM to text using template**\n\n```ppmtoarbtxt [template.txt] [input.ppm] > [output.txt]```"],["SYNOPSIS","**ppmtoarbtxt** _template_ [_file_]"],["PARAMETERS","_template_\n> Template file defining output format."],["DESCRIPTION","**ppmtoarbtxt** converts PPM images to arbitrary text formats using a template. Template specifies how pixel values are formatted. Useful for custom export formats. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[ppmtoascii](/man/ppmtoascii)(1), [ppmtoterm](/man/ppmtoterm)(1)"]],"e":["ppmtoascii","ppmtoterm"],"x":"ppmtoarbtxt [template.txt] [input.ppm] > [output.txt]"},"pycentral":{"n":"pycentral","t":"Deprecated Debian Python module manager","s":[["TAGLINE","Deprecated Debian Python module manager"],["TLDR","**Register Python modules (deprecated)**\n\n```pycentral [action] [package]```"],["SYNOPSIS","**pycentral** _action_ [_options_]"],["PARAMETERS","_ACTION_\n> Action to perform."],["DESCRIPTION","**pycentral** was a Debian tool for managing Python module installations across multiple Python interpreter versions. It handled byte-compilation and module registration for Debian packages containing Python code.\n\nThe tool has been deprecated and replaced by **dh-python** and **pybuild**, which provide more modern and maintainable approaches to Python packaging on Debian-based systems. Existing packages using pycentral should be migrated to dh-python."],["CAVEATS","Deprecated. Use dh-python instead."],["HISTORY","pycentral was a **deprecated Debian** tool for Python module management."],["SEE ALSO","[pybuild](/man/pybuild)(1), [dh-python](/man/dh-python)(1)"]],"e":["pybuild","dh-python"],"x":"pycentral [action] [package]"},"python3":{"n":"python3","t":"Python 3 programming language interpreter","s":[["TAGLINE","Python 3 programming language interpreter"],["TLDR","**Start an interactive** Python shell\n\n```python3```\n\n**Execute a Python script**\n\n```python3 [script.py]```\n\n**Execute a Python command**\n\n```python3 -c \"[print('Hello')]\"```\n\n**Run a module** as a script\n\n```python3 -m [module_name]```\n\n**Start a simple HTTP server** in the current directory\n\n```python3 -m http.server [8000]```\n\n**Install a package** using pip\n\n```python3 -m pip install [package]```\n\n**Check Python version**\n\n```python3 --version```"],["SYNOPSIS","**python3** [_-BdEhiIOqsSuvVWx?_] [_-c command_] [_-m module_] [_-X option_] [_script_] [_args_]"],["PARAMETERS","**-c** _command_\n> Execute Python code passed as a string\n\n**-m** _module_\n> Run library module as a script\n\n**-i**\n> Inspect interactively after running script\n\n**-B**\n> Don't write .pyc bytecode files\n\n**-O**\n> Optimize generated bytecode\n\n**-OO**\n> Remove docstrings in addition to -O optimizations\n\n**-q**\n> Don't print version and copyright on startup\n\n**-s**\n> Don't add user site-packages to sys.path\n\n**-S**\n> Don't import the site module\n\n**-u**\n> Unbuffered binary stdout and stderr\n\n**-v**\n> Verbose mode (trace import statements)\n\n**-V**, **--version**\n> Print Python version and exit\n\n**-W** _arg_\n> Warning control (error, ignore, always, default, module, once)\n\n**-X** _option_\n> Set implementation-specific option\n\n**-h**, **--help**\n> Print help message and exit\n\n**-E**\n> Ignore PYTHON* environment variables"],["DESCRIPTION","**python3** is the interpreter for the Python programming language, version 3.x. It executes Python code either interactively or from script files, supporting both procedural and object-oriented programming paradigms.\n\nWhen invoked without arguments, it starts an interactive REPL (Read-Eval-Print Loop) for experimenting with Python code. With a script file as argument, it executes the script. The **-m** option allows running installed modules as scripts, enabling functionality like starting HTTP servers, running tests, or managing packages with pip.\n\nPython 3 is the current major version of Python, featuring improved Unicode support, print as a function, integer division changes, and many other enhancements over Python 2 (which reached end-of-life in 2020)."],["CAVEATS","On some systems, **python** may still refer to Python 2; use **python3** explicitly to ensure version 3. Virtual environments (**venv**) are recommended for project isolation. The Global Interpreter Lock (GIL) limits true multi-threaded parallelism. Scripts with Windows line endings may cause issues; use **dos2unix** if needed."],["HISTORY","Python was created by **Guido van Rossum** and first released in **1991**. Python 3.0 was released on **December 3, 2008**, introducing significant changes that broke backward compatibility with Python 2. The transition period lasted many years, with Python 2.7's end-of-life officially on **January 1, 2020**. Python has grown to become one of the world's most popular programming languages, widely used in web development, data science, artificial intelligence, scripting, and education."],["INSTALL","```apt: sudo apt install python3-minimal```\n\n```dnf: sudo dnf install python3```\n\n```apk: sudo apk add python3```\n\n```brew: brew install python3```\n\n```nix: nix profile install nixpkgs#python3```"],["SEE ALSO","[pip](/man/pip)(1), [ipython](/man/ipython)(1), [virtualenv](/man/virtualenv)(1), [perl](/man/perl)(1), [ruby](/man/ruby)(1)"]],"e":["pip","ipython","virtualenv","perl","ruby"],"x":"python3"},"r":{"n":"r","t":"Statistical computing and graphics language","s":[["TAGLINE","Statistical computing and graphics language"],["TLDR","**Start R interpreter**\n\n```R```\n\n**Run script**\n\n```R --file=[script.R]```\n\n**Execute and print**\n\n```Rscript -e \"[print(1+1)]\"```\n\n**Run script silently**\n\n```R --slave --file=[script.R]```\n\n**No save on exit**\n\n```R --no-save```"],["SYNOPSIS","**R** [_options_] [_file_]"],["PARAMETERS","_FILE_\n> R script to run.\n\n**--file** _FILE_\n> Execute script.\n\n**--slave**\n> Silent mode.\n\n**--no-save**\n> Don't save workspace.\n\n**--vanilla**\n> No init files.\n\n**-e** _EXPR_\n> Execute expression."],["DESCRIPTION","**R** is a language and interactive environment for statistical computing, data analysis, and graphics. It provides a wide variety of statistical techniques including linear and nonlinear modeling, time-series analysis, classification, and clustering, along with a powerful system for producing publication-quality plots and visualizations.\n\nThe interactive console supports exploratory data analysis with immediate feedback, while scripts can be executed non-interactively via **--file** or through the companion **Rscript** command. R's package ecosystem on CRAN offers thousands of contributed libraries extending its capabilities across fields from bioinformatics to econometrics. The workspace system saves objects between sessions, though **--no-save** and **--vanilla** flags allow clean stateless execution for reproducible workflows."],["CAVEATS","Separate from Rscript. Interactive by default."],["HISTORY","R was created by **Ross Ihaka and Robert Gentleman** at University of Auckland."],["INSTALL","```apt: sudo apt install r-cran-littler```\n\n```pacman: sudo pacman -S r```\n\n```brew: brew install r```"],["SEE ALSO","[python](/man/python)(1)"]],"e":["python"],"x":"R"},"rails-db":{"n":"rails-db","t":"Open database console for Rails application","s":[["TAGLINE","Open database console for Rails application"],["TLDR","**Open a database console** for the current environment\n\n```rails db```\n\n**Open a database console** for production\n\n```rails db -e production```\n\n**Open a database console** with a specific database configuration\n\n```rails db --database=[database_name]```"],["SYNOPSIS","**rails db** [**-e** _environment_] [**--database** _name_] [**-h**]"],["PARAMETERS","**-e**, **--environment** _ENV_\n> Specify the Rails environment (development, test, production)\n\n**--database** _name_\n> Connect to a specific database when multiple databases are configured\n\n**-h**, **--help**\n> Show help information"],["DESCRIPTION","**rails db** (also known as **rails dbconsole**) opens an interactive session with the database configured for your Rails application. It automatically detects the database adapter and launches the appropriate client (psql for PostgreSQL, mysql for MySQL, sqlite3 for SQLite, etc.).\n\nThe command reads database configuration from **config/database.yml** and connects using the credentials and connection details specified there. This provides quick access to the database without manually remembering connection strings."],["CAVEATS","Requires the corresponding database client to be installed on the system (psql, mysql, sqlite3, etc.). The command will fail if the client is not in the PATH.\n\nDatabase credentials are read from the configuration file. Ensure sensitive credentials are properly secured, especially in production environments."],["SEE ALSO","[rails](/man/rails)(1), [rails-console](/man/rails-console)(1), [psql](/man/psql)(1), [mysql](/man/mysql)(1), [sqlite3](/man/sqlite3)(1)"]],"e":["rails","rails-console","psql","mysql","sqlite3"],"x":"rails db"},"riscv64-unknown-elf-gcc":{"n":"riscv64-unknown-elf-gcc","t":"Cross-compile C code for RISC-V targets","s":[["TAGLINE","Cross-compile C code for RISC-V targets"],["TLDR","**Compile for RISC-V**\n\n```riscv64-unknown-elf-gcc -o [output] [source.c]```\n\n**Compile with specific architecture**\n\n```riscv64-unknown-elf-gcc -march=rv64imac -o [output] [source.c]```\n\n**Compile for bare metal**\n\n```riscv64-unknown-elf-gcc -nostdlib -o [output] [source.c]```\n\n**Produce assembly**\n\n```riscv64-unknown-elf-gcc -S [source.c]```"],["SYNOPSIS","**riscv64-unknown-elf-gcc** [_options_] _files_..."],["PARAMETERS","**-march=**_arch_\n> Target architecture (e.g. rv64imac, rv64gc, rv32imac).\n\n**-mabi=**_abi_\n> Integer and floating-point calling convention (e.g. lp64, lp64d, lp64f, ilp32).\n\n**-nostdlib**\n> Do not link standard library or startup files.\n\n**-T** _script_\n> Use the specified linker script.\n\n**-mcmodel=**_model_\n> Code model: medlow (default) or medany.\n\n**-msave-restore**\n> Use smaller but slower prologue/epilogue routines to reduce code size.\n\n**-msmall-data-limit=**_n_\n> Put global and static data smaller than _n_ bytes in a special section.\n\n**--specs=**_file_\n> Use the specified specs file (e.g. nano.specs for newlib-nano).\n\n**-O**_level_\n> Optimization level (0, 1, 2, 3, s, g)."],["DESCRIPTION","**riscv64-unknown-elf-gcc** is the GCC cross-compiler for RISC-V 64-bit bare-metal targets. It compiles C/C++ code for RISC-V processors without an operating system."],["EXAMPLES","```bash"],["Basic compilation","riscv64-unknown-elf-gcc -o hello hello.c"],["With specific ISA extensions","riscv64-unknown-elf-gcc -march=rv64gc -mabi=lp64d -o prog prog.c"],["Bare metal with linker script","riscv64-unknown-elf-gcc -nostdlib -T link.ld -o firmware firmware.c"],["Generate object file","riscv64-unknown-elf-gcc -c -o main.o main.c"],["With newlib","riscv64-unknown-elf-gcc --specs=nano.specs -o app app.c\n```"],["ARCHITECTURES","```\nrv64imac   - Integer, Multiply, Atomic, Compressed\nrv64gc     - General purpose (IMAFDC) - equivalent to rv64imafdC\nrv64imafdc - Same as rv64gc (explicit form)\nrv32imac   - 32-bit: Integer, Multiply, Atomic, Compressed\nrv32gc     - 32-bit general purpose\n```"],["CAVEATS","Cross-compiler for embedded. Requires RISC-V toolchain installation. Different from riscv64-linux-gnu-gcc."],["HISTORY","RISC-V GCC toolchain was developed as part of the **RISC-V** open ISA project from UC Berkeley."],["INSTALL","```apt: sudo apt install gcc-riscv64-unknown-elf```"],["SEE ALSO","[gcc](/man/gcc)(1), [objdump](/man/objdump)(1), [qemu-system-riscv64](/man/qemu-system-riscv64)(1)"]],"e":["gcc","objdump","qemu-system-riscv64"],"x":"riscv64-unknown-elf-gcc -o [output] [source.c]"},"rustup-init.sh":{"n":"rustup-init.sh","t":"Bootstrap installer script for rustup","s":[["TAGLINE","Bootstrap installer script for rustup"],["TLDR","**Install rustup**\n\n```curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh```\n\n**Install without prompts**\n\n```curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y```\n\n**Install nightly toolchain**\n\n```curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- --default-toolchain nightly```\n\n**Install minimal profile without modifying PATH**\n\n```curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --profile minimal --no-modify-path```\n\n**Install with additional target for cross-compilation**\n\n```curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- --target wasm32-unknown-unknown```\n\n**Install with additional components**\n\n```curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- --component rustfmt clippy```"],["SYNOPSIS","**rustup-init.sh** [_options_]"],["PARAMETERS","**-y**\n> Accept defaults without prompting.\n\n**-q**, **--quiet**\n> Disable progress output.\n\n**-v**, **--verbose**\n> Enable verbose output.\n\n**--default-toolchain** _name_\n> Install specific toolchain (stable, beta, nightly, or a version like 1.78.0).\n\n**--default-host** _triple_\n> Set the default host triple (e.g., x86_64-unknown-linux-gnu).\n\n**--no-modify-path**\n> Don't modify the PATH environment variable.\n\n**--profile** _name_\n> Installation profile: minimal (rustc, cargo), default (adds rustfmt, clippy), or complete (all components).\n\n**--component** _name_\n> Add a specific component (e.g., rust-docs, rustfmt, clippy). Can be specified multiple times.\n\n**--target** _triple_\n> Add a cross-compilation target (e.g., wasm32-unknown-unknown). Can be specified multiple times."],["DESCRIPTION","**rustup-init.sh** is the Rust toolchain installer script. Downloads and runs rustup-init to install rustup and the Rust toolchain. This is the official method for installing Rust on Unix systems.\n\nOptions can also be set via environment variables prefixed with RUSTUP_INIT_ (e.g., RUSTUP_INIT_DEFAULT_TOOLCHAIN=nightly)."],["INSTALL","```apt: sudo apt install rustup```\n\n```dnf: sudo dnf install rustup```\n\n```pacman: sudo pacman -S rustup```\n\n```zypper: sudo zypper install rustup```\n\n```brew: brew install rustup```\n\n```nix: nix profile install nixpkgs#rustup```"],["SEE ALSO","[rustup](/man/rustup)(1), [cargo](/man/cargo)(1), [rustc](/man/rustc)(1)"]],"e":["rustup","cargo","rustc"],"x":"curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh"},"rustup-show":{"n":"rustup-show","t":"Show installed Rust toolchains and active version","s":[["TAGLINE","Show installed Rust toolchains and active version"],["TLDR","**Show installed toolchains**\n\n```rustup show```\n\n**Show active toolchain**\n\n```rustup show active-toolchain```\n\n**Show home directory**\n\n```rustup show home```\n\n**Show installation profile**\n\n```rustup show profile```"],["SYNOPSIS","**rustup** **show** [_command_]"],["PARAMETERS","**active-toolchain**\n> Currently active toolchain.\n\n**home**\n> Rustup home directory.\n\n**profile**\n> Default installation profile."],["DESCRIPTION","**rustup show** displays rustup installation information. Shows installed toolchains, active toolchain, and configured settings. Default command shows comprehensive overview."],["INSTALL","```apt: sudo apt install rustup```\n\n```dnf: sudo dnf install rustup```\n\n```pacman: sudo pacman -S rustup```\n\n```zypper: sudo zypper install rustup```\n\n```brew: brew install rustup```\n\n```nix: nix profile install nixpkgs#rustup```"],["SEE ALSO","[rustup](/man/rustup)(1), [rustup-default](/man/rustup-default)(1)"]],"e":["rustup","rustup-default"],"x":"rustup show"},"scala":{"n":"scala","t":"Scala programming language interpreter and REPL","s":[["TAGLINE","Scala programming language interpreter and REPL"],["TLDR","**Start REPL**\n\n```scala```\n\n**Run script**\n\n```scala [script.scala]```\n\n**Run with classpath**\n\n```scala -cp [lib.jar] [script.scala]```\n\n**Evaluate expression**\n\n```scala -e \"println(1 + 2)\"```\n\n**Run compiled class**\n\n```scala [ClassName]```\n\n**With specific JVM memory**\n\n```scala -J-Xmx[2g] [script.scala]```\n\n**Run in explain mode**\n\n```scala -explain [script.scala]```"],["SYNOPSIS","**scala** [_-cp classpath_] [_-e expr_] [_options_] [_script_] [_args_]"],["PARAMETERS","**-cp**, **-classpath** _PATH_\n> Set classpath for class files and libraries.\n\n**-e** _EXPR_\n> Evaluate expression and print the result.\n\n**-J** _FLAG_\n> Pass flag to JVM (e.g., -J-Xmx2g).\n\n**-deprecation**\n> Show deprecation warnings.\n\n**-explain**\n> Explain errors in detail with additional context.\n\n**-feature**\n> Show feature warnings for advanced language features.\n\n**-help**\n> Show help.\n\n**-version**\n> Show version.\n\n**-X**\n> Show advanced compiler options.\n\n**-Y**\n> Show private compiler options."],["DESCRIPTION","**scala** runs Scala programs and provides an interactive REPL. Scala combines object-oriented and functional programming on the JVM.\n\nThe REPL evaluates expressions and shows results with types. Tab completion helps explore APIs. :help shows REPL commands.\n\nScripts run directly without compilation. The Scala 3 runner can execute .scala files as scripts with dependencies.\n\nClasspath specifies libraries and compiled classes. Maven coordinates can specify dependencies in Scala 3.\n\nCompiler explanations help understand complex error messages. The explain mode provides detailed reasoning.\n\nThe language supports type inference, pattern matching, implicits, and higher-order functions. It interoperates with Java seamlessly."],["CAVEATS","Scala 3 differs significantly from Scala 2. JVM startup adds latency. Memory usage can be high. Some features have learning curve."],["HISTORY","**Scala** was designed by **Martin Odersky** at **EPFL** in Switzerland, with version 1.0 released in **2004**. Scala 3 (Dotty), released in **2021**, brought significant language improvements."],["INSTALL","```zypper: sudo zypper install scala```\n\n```brew: brew install scala```\n\n```nix: nix profile install nixpkgs#scala```"],["SEE ALSO","[scalac](/man/scalac)(1), [sbt](/man/sbt)(1), [java](/man/java)(1), [kotlin](/man/kotlin)(1)"]],"e":["scalac","sbt","java","kotlin"],"x":"scala"},"scc":{"n":"scc","t":"Fast source code line counter","s":[["TAGLINE","Fast source code line counter"],["TLDR","**Count lines in current directory**\n\n```scc```\n\n**Count specific directory**\n\n```scc [path/to/dir]```\n\n**Count specific language**\n\n```scc --include-ext [py]```\n\n**Exclude directories**\n\n```scc --exclude-dir [vendor,node_modules]```\n\n**Sort by lines**\n\n```scc --sort lines```\n\n**Output as JSON**\n\n```scc --format json```\n\n**Show complexity estimates**\n\n```scc -w```\n\n**Show by file**\n\n```scc --by-file```"],["SYNOPSIS","**scc** [_--include-ext ext_] [_--exclude-dir dirs_] [_--format fmt_] [_options_] [_paths_]"],["PARAMETERS","**--include-ext** _EXT_\n> Include only extensions.\n\n**--exclude-dir** _DIRS_\n> Exclude directories.\n\n**--sort** _FIELD_\n> Sort by field (files, lines, blanks, code, comments).\n\n**--format** _FMT_\n> Output format (default, json, csv, html, etc.).\n\n**-w**, **--wide**\n> Show complexity and weighted lines.\n\n**--by-file**\n> Show per-file breakdown.\n\n**--no-duplicates**\n> Ignore duplicate files.\n\n**--no-gen**\n> Ignore generated files.\n\n**--no-min**\n> Ignore minified files.\n\n**--cocomo**\n> Show COCOMO estimates.\n\n**-f** _PATTERN_\n> Match filenames.\n\n**-M** _SIZE_\n> Max file size."],["DESCRIPTION","**scc** (Succinct Code Counter) counts lines of code in many languages. It's extremely fast, using parallelism and optimized parsing.\n\nOutput shows files, lines, blanks, comments, and code per language. Wide mode adds complexity estimates based on branching.\n\nLanguage detection uses file extensions and content inspection. Hundreds of languages are recognized, from common to obscure.\n\nGenerated and minified file detection prevents counting boilerplate. Duplicates can be ignored to avoid counting vendored code multiple times.\n\nCOCOMO estimation provides rough effort metrics. These are theoretical but useful for comparisons.\n\nOutput formats support integration with other tools. JSON and CSV enable processing by scripts and dashboards."],["CAVEATS","Complexity estimates are heuristic. Language detection may misidentify some files. Very large codebases may need memory."],["HISTORY","**scc** was created by **Ben Boyter** around **2018** as a faster alternative to cloc and other line counters. Written in Go, it emphasizes speed through parallelism and efficient algorithms."],["INSTALL","```pacman: sudo pacman -S sc-controller```\n\n```apk: sudo apk add sc-controller```\n\n```zypper: sudo zypper install sc-controller```\n\n```brew: brew install scc```\n\n```nix: nix profile install nixpkgs#sc-controller```"],["SEE ALSO","[cloc](/man/cloc)(1), [tokei](/man/tokei)(1), [loc](/man/loc)(1), [wc](/man/wc)(1)"]],"e":["cloc","tokei","loc","wc"],"x":"scc"},"sfdk-device":{"n":"sfdk-device","t":"Manage Sailfish OS development devices","s":[["TAGLINE","Manage Sailfish OS development devices"],["TLDR","**List available devices**\n\n```sfdk device```\n\n**Show device info**\n\n```sfdk device info [device_name]```\n\n**Set default device**\n\n```sfdk device set [device_name]```"],["SYNOPSIS","**sfdk device** [_command_] [_options_]"],["PARAMETERS","**list**\n> List available devices.\n\n**info** _name_\n> Show device information.\n\n**set** _name_\n> Set default device."],["DESCRIPTION","**sfdk device** manages Sailfish OS devices and emulators for deployment and testing. Part of Sailfish SDK."],["SEE ALSO","[sfdk](/man/sfdk)(1), [sfdk-emulator](/man/sfdk-emulator)(1)"]],"e":["sfdk","sfdk-emulator"],"x":"sfdk device"},"spa-monitor":{"n":"spa-monitor","t":"PipeWire SPA device debugging utility","s":[["TAGLINE","PipeWire SPA device debugging utility"],["TLDR","**Load a SPA plugin** and instantiate a device from it\n\n```spa-monitor [path/to/plugin.so]```\n\n**Debug the JACK compatibility plugin**\n\n```spa-monitor /usr/lib/x86_64-linux-gnu/spa-0.2/jack/libspa-jack.so```\n\n**Debug the ALSA plugin**\n\n```spa-monitor /usr/lib/x86_64-linux-gnu/spa-0.2/alsa/libspa-alsa.so```\n\n**Debug the Bluetooth plugin**\n\n```spa-monitor /usr/lib/x86_64-linux-gnu/spa-0.2/bluez5/libspa-bluez5.so```\n\n**Debug the V4L2 video plugin**\n\n```spa-monitor /usr/lib/x86_64-linux-gnu/spa-0.2/v4l2/libspa-v4l2.so```"],["SYNOPSIS","**spa-monitor** _FILE_"],["DESCRIPTION","**spa-monitor** is a small debugging utility shipped with **PipeWire**. It loads a **SPA** (Simple Plugin API) shared-object plugin and instantiates a device object from it so that developers can observe how the plugin enumerates hardware, advertises nodes, and emits events.\n\nThe tool is not intended for end users; it exists to help plugin authors and PipeWire developers verify that a SPA plugin initializes correctly, exposes the expected interfaces, and reports devices on the system bus. Running **spa-monitor** against a plugin prints diagnostic output describing the devices discovered and any errors encountered during instantiation.\n\nTypical usage is pointing it at one of the plugin libraries under _/usr/lib/<triplet>/spa-0.2/_ to inspect how that plugin behaves on the current machine."],["PARAMETERS","_FILE_\n> Path to a SPA plugin shared object (.so) file. The plugin is dlopen'd and a device is instantiated from it."],["CAVEATS","**spa-monitor** is a debugging aid: it is only useful to developers working on PipeWire device plugins. The program takes no flags and produces output that is only meaningful in the context of the SPA plugin API. It is shipped in the _pipewire-bin_ package on Debian/Ubuntu and _pipewire_ on Arch."],["HISTORY","Developed by the **PipeWire** project as part of the SPA plugin framework. PipeWire was created by **Wim Taymans** at **Red Hat** starting in **2015** as a unified server for audio and video on Linux, intended to eventually replace both **PulseAudio** and **JACK** for media handling."],["INSTALL","```apt: sudo apt install pipewire-bin```\n\n```apk: sudo apk add pipewire-spa-tools```\n\n```zypper: sudo zypper install pipewire-spa-tools```"],["SEE ALSO","[pipewire](/man/pipewire)(1), [pw-cli](/man/pw-cli)(1), [pw-dump](/man/pw-dump)(1), [wpctl](/man/wpctl)(1)"]],"e":["pipewire","pw-cli","pw-dump","wpctl"],"x":"spa-monitor [path/to/plugin.so]"},"tailf":{"n":"tailf","t":"Deprecated, use tail -f","s":[["TAGLINE","Deprecated, use tail -f"],["TLDR","This command has been superseded by [tail](/man/tail) -f.\n\nUse `tail -f` instead for following file output."],["SEE ALSO","[tail](/man/tail)(1)"]],"e":["tail"],"x":""},"toolbox-run":{"n":"toolbox-run","t":"Run commands in Toolbx containers","s":[["TAGLINE","Run commands in Toolbx containers"],["TLDR","**Run** command in container\n\n```toolbox run -c [container_name] [command]```\n\nRun in **distribution** container\n\n```toolbox run -d [distribution] -r [release] [command]```\n\nRun **emacs** in Fedora container\n\n```toolbox run -d fedora -r f[version] emacs```"],["SYNOPSIS","**toolbox run** [_OPTIONS_] _COMMAND_ [_ARGS_...]"],["PARAMETERS","**-c, --container** _NAME_\n> Specify container name\n\n**-d, --distro** _DISTRO_\n> Specify distribution\n\n**-r, --release** _RELEASE_\n> Specify distribution release version"],["DESCRIPTION","**toolbox run** executes a command inside an existing Toolbx container without entering an interactive session. The command runs in the container environment and returns to the host when complete.\n\nThis is useful for running individual commands or scripts in a container environment while staying on the host system."],["CAVEATS","Container must exist and be running. Command must be available inside the container. Environment differs from interactive session in some cases."],["INSTALL","```apt: sudo apt install podman-toolbox```\n\n```dnf: sudo dnf install toolbox```\n\n```pacman: sudo pacman -S toolbox```\n\n```zypper: sudo zypper install toolbox```\n\n```nix: nix profile install nixpkgs#toolbox```"],["SEE ALSO","[toolbox](/man/toolbox)(1), [toolbox-enter](/man/toolbox-enter)(1), [toolbox-create](/man/toolbox-create)(1)"]],"e":["toolbox","toolbox-enter","toolbox-create"],"x":"toolbox run -c [container_name] [command]"},"tr":{"n":"tr","t":"Translate or delete characters","s":[["TAGLINE","Translate or delete characters"],["TLDR","**Translate characters** (lowercase to uppercase)\n\n```echo \"hello\" | tr 'a-z' 'A-Z'```\n\n**Delete specific characters**\n\n```echo \"hello 123\" | tr -d '0-9'```\n\n**Squeeze repeated characters**\n\n```echo \"helllo\" | tr -s 'l'```\n\n**Replace newlines with spaces**\n\n```tr '\\n' ' ' < [file]```\n\n**Delete non-printable characters**\n\n```tr -cd '[:print:]' < [file]```\n\n**Translate spaces to tabs**\n\n```tr ' ' '\\t' < [file]```\n\n**Delete all except specified characters**\n\n```echo \"hello123\" | tr -cd '0-9'```"],["SYNOPSIS","**tr** [_options_] _set1_ [_set2_]"],["PARAMETERS","**-d**, **--delete**\n> Delete characters in set1\n\n**-s**, **--squeeze-repeats**\n> Replace repeated characters with single occurrence\n\n**-c**, **-C**, **--complement**\n> Use complement of set1\n\n**-t**, **--truncate-set1**\n> Truncate set1 to length of set2"],["CHARACTER CLASSES","**[:alnum:]**: Letters and digits\n**[:alpha:]**: Letters\n**[:digit:]**: Digits\n**[:lower:]**: Lowercase letters\n**[:upper:]**: Uppercase letters\n**[:space:]**: Whitespace\n**[:blank:]**: Spaces and tabs\n**[:print:]**: Printable characters\n**[:punct:]**: Punctuation\n**[:cntrl:]**: Control characters"],["DESCRIPTION","**tr** translates, deletes, or squeezes characters from standard input, writing to standard output. It operates character-by-character, making it efficient for simple transformations.\n\nTranslation replaces each character in set1 with the corresponding character in set2. If set2 is shorter, its last character is repeated. If longer, excess characters are ignored.\n\nWith **-d**, characters in set1 are deleted. With **-s**, repeated characters in set1 are squeezed to single occurrences. Both can be combined.\n\n**-c** complements set1, meaning \"all characters NOT in set1.\" Useful for keeping only specific characters."],["CAVEATS","Tr operates on characters, not strings or patterns. For pattern-based substitution, use **sed** or **awk**.\n\nCharacter classes in brackets must use the [:class:] syntax. Don't confuse with regex [a-z] which also works but differs from [:lower:] in locales.\n\nTr reads only from stdin; it cannot process files directly. Use redirection or pipes."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[sed](/man/sed)(1), [awk](/man/awk)(1), [cut](/man/cut)(1), [iconv](/man/iconv)(1)"]],"e":["sed","awk","cut","iconv"],"x":"echo \"hello\" | tr 'a-z' 'A-Z'"},"valkey-server":{"n":"valkey-server","t":"In-memory key-value data store server","s":[["TAGLINE","In-memory key-value data store server"],["TLDR","**Start server with defaults**\n\n```valkey-server```\n\n**Start with configuration file**\n\n```valkey-server [/etc/valkey/valkey.conf]```\n\n**Start on specific port**\n\n```valkey-server --port [6379]```\n\n**Start with password**\n\n```valkey-server --requirepass [password]```\n\n**Start with max memory limit**\n\n```valkey-server --maxmemory [256mb]```\n\n**Start in protected mode disabled**\n\n```valkey-server --protected-mode no```\n\n**Start as daemon**\n\n```valkey-server --daemonize yes```\n\n**Start with AOF persistence**\n\n```valkey-server --appendonly yes```\n\n**Start as replica** of another server\n\n```valkey-server --replicaof [127.0.0.1] [6379]```\n\n**Start in sentinel mode**\n\n```valkey-server --sentinel```"],["SYNOPSIS","**valkey-server** [_config-file_] [_options_]"],["PARAMETERS","**--port** _port_\n> Listen port (default: 6379).\n\n**--bind** _address_\n> Bind address (default: 127.0.0.1).\n\n**--requirepass** _password_\n> Require password for connections.\n\n**--maxmemory** _bytes_\n> Maximum memory limit.\n\n**--maxclients** _n_\n> Maximum client connections.\n\n**--daemonize** _yes|no_\n> Run as daemon.\n\n**--pidfile** _file_\n> PID file location.\n\n**--logfile** _file_\n> Log file location.\n\n**--loglevel** _level_\n> Log level: debug, verbose, notice, warning.\n\n**--appendonly** _yes|no_\n> Enable append-only persistence.\n\n**--save** _seconds_ _changes_\n> Snapshot save rules.\n\n**--protected-mode** _yes|no_\n> Enable protected mode.\n\n**--dir** _directory_\n> Working directory for persistence.\n\n**--replicaof** _host_ _port_\n> Start as a replica of the specified server.\n\n**--sentinel**\n> Start in Sentinel mode for high availability monitoring.\n\n**--tls-port** _port_\n> Listen for TLS connections on specified port.\n\n**--io-threads** _n_\n> Number of I/O threads for improved performance (default: 1, disabled).\n\n**--cluster-enabled** _yes|no_\n> Enable cluster mode.\n\n**--dbfilename** _file_\n> Filename for the RDB snapshot (default: dump.rdb)."],["DESCRIPTION","**valkey-server** is the server component of Valkey, an open-source in-memory data store forked from Redis. It provides high-performance key-value storage with support for strings, hashes, lists, sets, sorted sets, and more.\n\nValkey is fully compatible with Redis OSS 7.2 and earlier versions. Existing Redis clients, applications, and tools work with Valkey by simply changing the connection endpoint.\n\nThe server supports persistence through RDB snapshots and AOF (Append Only File). Clustering and replication enable high availability and horizontal scaling.\n\nValkey 8.0+ includes enhanced multi-threaded I/O and improved memory efficiency over Redis."],["CAVEATS","Memory-bound by default; configure maxmemory for production. Protected mode restricts remote access unless disabled. Persistence requires disk space. Compatible symlinks (redis-server) may be available."],["HISTORY","**Valkey** was forked from Redis in 2024 after Redis Labs changed the license to source-available. The Linux Foundation hosts Valkey as a community-driven continuation of open-source Redis. Major contributors include AWS, Google, Oracle, and Ericsson."],["INSTALL","```apt: sudo apt install valkey-server```\n\n```dnf: sudo dnf install valkey```\n\n```pacman: sudo pacman -S valkey```\n\n```apk: sudo apk add valkey```\n\n```zypper: sudo zypper install valkey```\n\n```brew: brew install valkey```\n\n```nix: nix profile install nixpkgs#valkey```"],["SEE ALSO","[valkey-cli](/man/valkey-cli)(1), [redis-server](/man/redis-server)(1), [memcached](/man/memcached)(1)"]],"e":["valkey-cli","redis-server","memcached"],"x":"valkey-server"},"weasyprint":{"n":"weasyprint","t":"Render HTML and CSS to PDF and PNG","s":[["TAGLINE","Render HTML and CSS to PDF and PNG"],["TLDR","**Convert HTML to PDF**\n\n```weasyprint [input.html] [output.pdf]```\n\n**Convert from URL**\n\n```weasyprint [https://example.com] [output.pdf]```\n\n**Convert HTML to PNG**\n\n```weasyprint [input.html] [output.png]```\n\n**Specify stylesheet**\n\n```weasyprint -s [style.css] [input.html] [output.pdf]```\n\n**Set base URL for resources**\n\n```weasyprint -u [https://example.com/] [input.html] [output.pdf]```\n\n**Render at specific resolution**\n\n```weasyprint -r [150] [input.html] [output.png]```\n\n**Multiple stylesheets**\n\n```weasyprint -s [base.css] -s [print.css] [input.html] [output.pdf]```"],["SYNOPSIS","**weasyprint** [_-s stylesheet_] [_-u base_url_] [_-r resolution_] [_options_] _input_ _output_"],["PARAMETERS","**-s**, **--stylesheet** _FILE_\n> Add CSS stylesheet.\n\n**-u**, **--base-url** _URL_\n> Base URL for relative resources.\n\n**-m**, **--media-type** _TYPE_\n> Media type (default: print).\n\n**-r**, **--resolution** _DPI_\n> Resolution for PNG output.\n\n**-a**, **--attachment** _FILE_\n> Attach file to PDF.\n\n**-p**, **--presentational-hints**\n> Follow HTML presentational hints.\n\n**--pdf-identifier** _ID_\n> PDF document identifier.\n\n**--pdf-variant** _VARIANT_\n> PDF variant: pdf/a-1b, pdf/a-2b, pdf/a-3b, pdf/ua-1.\n\n**--pdf-version** _VERSION_\n> PDF version: 1.4, 1.5, 1.6, 1.7, 2.0.\n\n**--pdf-forms**\n> Enable PDF forms.\n\n**--optimize-images**\n> Losslessly optimize images.\n\n**--hinting**\n> Enable font hinting.\n\n**-e**, **--encoding** _ENCODING_\n> Input encoding.\n\n**-v**, **--verbose**\n> Verbose output."],["DESCRIPTION","**WeasyPrint** renders HTML/CSS to PDF and PNG. It's designed for creating printable documents from web content, supporting CSS for paged media.\n\nThe tool processes HTML with full CSS support including modern features: flexbox, grid, CSS variables, and media queries. Print-specific CSS (page breaks, margins, headers/footers) works correctly.\n\nCSS Paged Media enables sophisticated print layouts: multi-column, page numbers, running headers, cross-references, and footnotes. The @page rule controls page size and margins.\n\nPDF output includes internal links, bookmarks from headings, and optional file attachments. PDF/A variants ensure archival quality for long-term storage.\n\nInput can be files, URLs, or stdin. External stylesheets override or extend document styles. The base URL resolves relative resource paths.\n\nImage optimization reduces file size without quality loss. Font embedding ensures consistent rendering regardless of system fonts."],["CAVEATS","Large documents may use significant memory. Some complex CSS may not render identically to browsers. JavaScript not supported - static HTML only. PDF form support is basic. Some font rendering differences from browsers."],["HISTORY","**WeasyPrint** was created by **Kozea** (Simon Sapin and others) around **2011**. It was designed as an alternative to wkhtmltopdf, using native Python libraries instead of a headless browser. The focus on CSS Paged Media makes it particularly suitable for document generation."],["INSTALL","```dnf: sudo dnf install weasyprint```\n\n```apk: sudo apk add weasyprint```\n\n```brew: brew install weasyprint```"],["SEE ALSO","[wkhtmltopdf](/man/wkhtmltopdf)(1), [pandoc](/man/pandoc)(1), [prince](/man/prince)(1), [puppeteer](/man/puppeteer)(1)"]],"e":["wkhtmltopdf","pandoc","prince","puppeteer"],"x":"weasyprint [input.html] [output.pdf]"},"wireman":{"n":"wireman","t":"TUI gRPC client for the terminal","s":[["TAGLINE","TUI gRPC client for the terminal"],["TLDR","**Launch the gRPC client**\n\n```wireman```\n\n**Activate server reflection** (inside the UI, on the selection tab)\n\n```Ctrl+r```"],["SYNOPSIS","**wireman**"],["DESCRIPTION","**WireMan** is a terminal-based gRPC client written in Rust. It reads .proto files from a configuration file and provides a TUI for sending requests, browsing services and methods, and viewing responses.\n\nThe client supports server reflection, server-side streaming, header and metadata management, environment-based token loading via shell scripts, and exporting requests as **grpcurl** commands. Request history is persisted between sessions."],["CONFIGURATION","**~/.config/wireman/wireman.toml**\n> Main configuration file specifying proto file paths, addresses, default headers, and history settings."],["KEY BINDINGS","**Tab**\n> Switch between selection, request, response, and headers tabs.\n\n**Ctrl+r**\n> Activate server reflection mode (on the selection tab).\n\n**Enter**\n> Send the request or confirm a selection.\n\n**?**\n> Show in-app help."],["CAVEATS","Requires a configuration file pointing to local .proto files or a server with reflection enabled. Some advanced gRPC features (e.g. client-side streaming) may have limited UI support."],["HISTORY","**wireman** was created by **preiter93** and is written in Rust. It targets developers who prefer a keyboard-driven terminal workflow over GUI tools like Postman or BloomRPC."],["INSTALL","```pacman: sudo pacman -S wireman```"],["SEE ALSO","[grpcurl](/man/grpcurl)(1), [curl](/man/curl)(1), [protoc](/man/protoc)(1)"]],"e":["grpcurl","curl","protoc"],"x":"wireman"},"yazi":{"n":"yazi","t":"Fast async terminal file manager","s":[["TAGLINE","Fast async terminal file manager"],["TLDR","**Start file manager**\n\n```yazi```\n\n**Open specific directory**\n\n```yazi [/path/to/directory]```\n\n**Change directory on exit**\n\n```ya () { local tmp=\"$(mktemp -t \"yazi-cwd.XXXXX\")\"; yazi \"$@\" --cwd-file=\"$tmp\"; cd \"$(cat \"$tmp\")\"; rm \"$tmp\"; }```\n\n**Start with specific config directory**\n\n```yazi --config-dir [~/.config/yazi]```\n\n**Show debug information**\n\n```yazi --debug```"],["SYNOPSIS","**yazi** [_path_] [_--cwd-file file_] [_--chooser-file file_] [_options_]"],["PARAMETERS","**--cwd-file** _FILE_\n> Write CWD to file on exit.\n\n**--chooser-file** _FILE_\n> Write selected files to file.\n\n**--config-dir** _DIR_\n> Custom config directory.\n\n**--clear-cache**\n> Clear thumbnail cache.\n\n**--debug**\n> Print debug information for bug reports.\n\n**--version**\n> Show version.\n\n**--help**\n> Show help."],["KEYBOARD SHORTCUTS","**h / l**\n> Navigate parent / enter directory.\n\n**j / k**\n> Move down / up.\n\n**gg / G**\n> Go to first / last item.\n\n**Space**\n> Toggle selection.\n\n**Enter**\n> Open file.\n\n**y**\n> Yank (copy) selection.\n\n**d**\n> Delete selection.\n\n**p**\n> Paste.\n\n**r**\n> Rename.\n\n**a**\n> Create file.\n\n**/**\n> Search.\n\n**n / N**\n> Next / previous search result.\n\n**Tab**\n> Switch to next tab.\n\n**q**\n> Quit.\n\n**~**\n> Go to home directory.\n\n**?**\n> Show help."],["DESCRIPTION","**yazi** is a blazingly fast terminal file manager written in Rust. It emphasizes performance through async I/O and modern rendering.\n\nNavigation uses vim-like keybindings by default. Preview pane shows file contents, images (via terminal graphics), videos, PDFs, and archives. Syntax highlighting applies to code files.\n\nSelection works across directories. Multiple files can be yanked and pasted in batch operations. Cut/copy/paste mirrors GUI file manager behavior.\n\nTabs enable multiple directory views. Split panes compare directories side by side. Marks bookmark frequently accessed locations.\n\nPlugins extend functionality through Lua scripting. Themes customize colors and icons. Configuration covers keybindings, sorting, and preview behavior.\n\nImage preview works in terminals supporting graphics protocols (Kitty, iTerm2, Sixel). Fallback uses ASCII art."],["CAVEATS","Terminal graphics require compatible terminal. Some features need external tools (ffmpegthumbnailer for video). Configuration in Lua may have learning curve. Newer project with evolving features. Shell wrapper needed for cd-on-exit."],["HISTORY","**yazi** was created by **sxyazi** around **2023**. The name means \"duck\" in Chinese. Written in Rust with async architecture, it emerged as a faster alternative to ranger and lf. The project gained rapid adoption for its performance and modern features."],["INSTALL","```pacman: sudo pacman -S yazi```\n\n```apk: sudo apk add yazi```\n\n```zypper: sudo zypper install yazi```\n\n```brew: brew install yazi```\n\n```nix: nix profile install nixpkgs#yazi```"],["SEE ALSO","[ranger](/man/ranger)(1), [lf](/man/lf)(1), [nnn](/man/nnn)(1), [vifm](/man/vifm)(1), [ya](/man/ya)(1)"]],"e":["ranger","lf","nnn","vifm","ya"],"x":"yazi"},"zeek":{"n":"zeek","t":"Network security monitoring and analysis framework","s":[["TAGLINE","Network security monitoring and analysis framework"],["TLDR","**Analyze a pcap file**\n\n```zeek -r [capture.pcap]```\n\n**Analyze pcap with specific script**\n\n```zeek -r [capture.pcap] [script.zeek]```\n\n**Capture live traffic on interface**\n\n```sudo zeek -i [eth0]```\n\n**Run with local site policy**\n\n```zeek -r [capture.pcap] local```\n\n**Print version and exit**\n\n```zeek --version```"],["SYNOPSIS","**zeek** [_options_] [_file_...]"],["PARAMETERS","**-r** _file_\n> Read packets from pcap file\n\n**-i** _interface_\n> Capture from network interface\n\n**-C**\n> Ignore checksum errors\n\n**-e** _code_\n> Execute Zeek script code\n\n**-f** _filter_\n> Apply BPF filter\n\n**-N**\n> List all available plug-ins and exit.\n\n**-w** _file_\n> Write raw packets to file.\n\n**-b**, **--bare-mode**\n> Don't load scripts from the base/ directory.\n\n**--parse-only**\n> Parse scripts and exit.\n\n**--version**\n> Print version and exit."],["DESCRIPTION","**Zeek** (formerly Bro) is a powerful network analysis framework focused on security monitoring. Unlike packet sniffers that show raw traffic, Zeek interprets network activity and generates high-level logs about connections, protocols, and detected threats.\n\nZeek processes network traffic (live or from pcap files) and produces structured logs: **conn.log** for connections, **http.log** for HTTP traffic, **dns.log** for DNS queries, **ssl.log** for TLS connections, and many more.\n\nThe tool uses a custom scripting language for defining analysis logic. Scripts can detect intrusions, extract files from traffic, identify protocols, and generate custom logs. The **local.zeek** policy loads site-specific configurations.\n\nOutput logs are tab-separated by default, easily parsed by tools like **zeek-cut** or imported into SIEM systems."],["CAVEATS","Zeek requires significant CPU and memory for high-traffic networks. Tune workers and analysis depth accordingly.\n\nThe scripting language has a learning curve. Start with built-in scripts before writing custom analyzers.\n\nLive capture requires root or appropriate capabilities. Pcap file analysis runs as a normal user.\n\nLog rotation and management should be configured for production deployments."],["INSTALL","```brew: brew install zeek```\n\n```nix: nix profile install nixpkgs#zeek```"],["SEE ALSO","[tcpdump](/man/tcpdump)(1), [wireshark](/man/wireshark)(1), [tshark](/man/tshark)(1), [suricata](/man/suricata)(8)"]],"e":["tcpdump","wireshark","tshark","suricata"],"x":"zeek -r [capture.pcap]"}});
