window.LCL_BUCKET(32,{"apx-pkgmanagers":{"n":"apx-pkgmanagers","t":"Manage package manager configurations for apx subsystems","s":[["TAGLINE","Manage package manager configurations for apx subsystems"],["TLDR","Interactively **create** a new package manager configuration\n\n```apx pkgmanagers new```\n\n**Create** a package manager with specific commands\n\n```apx pkgmanagers new --name [name] --install '[install_cmd]' --remove '[remove_cmd]' --update '[update_cmd]'```\n\n**List** all available package manager configurations\n\n```apx pkgmanagers list```\n\n**Remove** a package manager configuration\n\n```apx pkgmanagers rm --name [name]```\n\nDisplay **information** about a specific package manager\n\n```apx pkgmanagers show [name]```"],["SYNOPSIS","**apx pkgmanagers** _command_ [_options_]"],["DESCRIPTION","**apx pkgmanagers** manages package manager configurations in apx. These configurations define how packages are installed, updated, and removed within subsystems.\n\nUser-created package manager configurations are stored in **~/.local/share/apx/pkgmanagers**. Built-in configurations for common package managers like apt, dnf, and pacman are provided by default."],["SUBCOMMANDS","**new**\n> Create a new package manager configuration (interactively or with flags).\n\n**list**\n> Display all available package manager configurations.\n\n**rm**\n> Remove a package manager configuration.\n\n**show**\n> Display details about a specific package manager."],["PARAMETERS","**--name** _string_\n> Specify the package manager name.\n\n**--install** _string_\n> The install command for the package manager.\n\n**--remove** _string_\n> The remove command for the package manager.\n\n**--update** _string_\n> The update command for the package manager."],["INSTALL","```dnf: sudo dnf install apx```\n\n```aur: yay -S apx```\n\n```nix: nix profile install nixpkgs#apx```"],["SEE ALSO","[apx](/man/apx)(1), [apx-stacks](/man/apx-stacks)(1), [apx-subsystems](/man/apx-subsystems)(1)"]],"e":["apx","apx-stacks","apx-subsystems"],"x":"apx pkgmanagers new"},"arduino-cli":{"n":"arduino-cli","t":"Official command-line tool for Arduino development","s":[["TAGLINE","Official command-line tool for Arduino development"],["TLDR","**List connected boards**\n\n```arduino-cli board list```\n\n**Compile a sketch** for a specific board\n\n```arduino-cli compile --fqbn [arduino:avr:uno] [path/to/sketch]```\n\n**Upload a compiled sketch** to a board\n\n```arduino-cli upload --port [/dev/ttyACM0] --fqbn [arduino:avr:uno] [path/to/sketch]```\n\n**Compile and upload** in one command\n\n```arduino-cli compile --fqbn [arduino:avr:uno] --port [/dev/ttyACM0] -u [path/to/sketch]```\n\n**Create a new sketch**\n\n```arduino-cli sketch new [MySketch]```\n\n**Install a board platform**\n\n```arduino-cli core install [arduino:avr]```\n\n**Search for a library**\n\n```arduino-cli lib search [servo]```\n\n**Install a library**\n\n```arduino-cli lib install \"[Servo]\"```\n\n**Open serial monitor** on a port\n\n```arduino-cli monitor --port [/dev/ttyACM0]```\n\n**Update platform index** and upgrade all installed platforms\n\n```arduino-cli core update-index && arduino-cli core upgrade```"],["SYNOPSIS","**arduino-cli** _command_ [_flags_]"],["PARAMETERS","**board list**\n> List connected Arduino boards with ports and FQBNs\n\n**board listall**\n> List all known board types\n\n**compile**\n> Compile an Arduino sketch\n\n**upload**\n> Upload compiled binaries to a board\n\n**sketch new** _name_\n> Create a new sketch in the sketchbook\n\n**core install** _platform_\n> Install a board platform (e.g., arduino:avr)\n\n**core update-index**\n> Update the platform index\n\n**lib install** _name_\n> Install a library from the library manager\n\n**lib search** _query_\n> Search for libraries\n\n**lib list**\n> List installed libraries\n\n**monitor**\n> Open serial monitor to communicate with the board\n\n**config init**\n> Create a default configuration file\n\n**core upgrade**\n> Upgrade all installed board platforms to the latest version\n\n**-b**, **--fqbn** _string_\n> Fully Qualified Board Name (e.g., arduino:avr:uno)\n\n**-p**, **--port** _string_\n> Port address (e.g., /dev/ttyACM0 or COM3)\n\n**-u**, **--upload**\n> Upload the binary after compilation (use with compile)\n\n**-e**, **--export-binaries**\n> Export compiled binaries to the sketch folder\n\n**--output-dir** _path_\n> Directory for compiled artifacts\n\n**-t**, **--verify**\n> Verify uploaded binary after upload\n\n**-v**, **--verbose**\n> Enable verbose output during compile or upload\n\n**-P**, **--programmer** _name_\n> Use a specific programmer for upload"],["DESCRIPTION","**arduino-cli** is the official command-line tool for Arduino development. It provides board and library management, sketch compilation, upload functionality, and serial monitoring without requiring the Arduino IDE.\n\nThe tool uses Fully Qualified Board Names (FQBNs) to identify target boards, formatted as **vendor:architecture:board** (e.g., arduino:avr:uno). Board platforms must be installed before compiling for them.\n\nTypical workflow: install the target platform with **core install**, compile the sketch with **compile**, and upload with **upload**. The **--upload** flag can be added to compile to perform both steps together.\n\nThe tool supports custom libraries via **--libraries** flag, board configuration via **--build-property**, and integrates well with IDEs like VS Code for Arduino development workflows."],["CONFIGURATION","**~/.arduino15/arduino-cli.yaml**\n> Main configuration file controlling board manager URLs, library paths, logging, and build defaults."],["CAVEATS","Upload requires appropriate permissions for serial ports; on Linux, users typically need membership in the **dialout** or **uucp** group. The **upload** command does not compile automatically; use **compile --upload** for a combined operation. Board platforms must be installed before first use."],["HISTORY","**arduino-cli** was developed by **Arduino** and released in **2018** as a modern replacement for the older Arduino command-line tools. It was created to enable headless Arduino development, support CI/CD pipelines, and provide a foundation for third-party IDE integrations. The tool shares its core with the Arduino IDE 2.0 and is actively maintained."],["INSTALL","```pacman: sudo pacman -S arduino-cli```\n\n```brew: brew install arduino-cli```\n\n```nix: nix profile install nixpkgs#arduino-cli```"],["SEE ALSO","[avrdude](/man/avrdude)(1), [platformio](/man/platformio)(1), [screen](/man/screen)(1), [minicom](/man/minicom)(1)"]],"e":["avrdude","platformio","screen","minicom"],"x":"arduino-cli board list"},"az-storage-blob":{"n":"az-storage-blob","t":"Manage Azure Blob Storage objects","s":[["TAGLINE","Manage Azure Blob Storage objects"],["TLDR","**Upload a file** to blob storage\n\n```az storage blob upload -f [/path/to/file] -c [container-name] -n [blob-name] --account-name [account]```\n\n**Download a blob** to local filesystem\n\n```az storage blob download -f [/path/to/file] -c [container-name] -n [blob-name] --account-name [account]```\n\n**List blobs** in a container\n\n```az storage blob list -c [container-name] --account-name [account] -o table```\n\n**Delete a blob**\n\n```az storage blob delete -c [container-name] -n [blob-name] --account-name [account]```\n\n**Upload multiple files** from a directory\n\n```az storage blob upload-batch -d [container-name] -s [/path/to/directory] --account-name [account]```\n\n**Download multiple blobs** matching a pattern\n\n```az storage blob download-batch -d [./local-dir] -s [container-name] --pattern \"*.txt\" --account-name [account]```\n\n**Generate a SAS token** for a blob\n\n```az storage blob generate-sas -c [container-name] -n [blob-name] --permissions [r] --expiry [2024-12-31T00:00Z] --account-name [account]```\n\n**Set blob access tier**\n\n```az storage blob set-tier -c [container-name] -n [blob-name] --tier [Cool] --account-name [account]```"],["SYNOPSIS","**az storage blob** _subcommand_ [_options_]"],["DESCRIPTION","**az storage blob** manages object storage for unstructured data in Azure Blob Storage. Blobs can store any type of text or binary data including documents, media files, and application data.\n\nThe command group supports uploading, downloading, copying, and managing blob properties, metadata, access tiers, and security settings."],["SUBCOMMANDS","**Transfer Operations**\n> upload, upload-batch, download, download-batch, copy start, copy cancel\n\n**Blob Management**\n> list, delete, delete-batch, exists, show, snapshot, restore\n\n**Properties & Metadata**\n> metadata show, metadata update, update, set-tier, tag list, tag set\n\n**Security**\n> generate-sas, lease acquire, lease break, lease release, lease renew\n\n**Queries**\n> query (SQL queries on blob data)"],["PARAMETERS","**-c, --container-name** _value_\n> Name of the blob container\n\n**-n, --name** _value_\n> Name of the blob\n\n**-f, --file** _value_\n> Path to the local file for upload/download\n\n**--account-name** _value_\n> Storage account name\n\n**--account-key** _value_\n> Storage account key\n\n**--sas-token** _value_\n> Shared Access Signature token\n\n**--tier** _value_\n> Access tier: Hot, Cool, Cold, Archive\n\n**--pattern** _value_\n> Glob pattern for batch operations\n\n**--overwrite**\n> Overwrite existing blob on upload (default: false)\n\n**--auth-mode** _value_\n> Authentication mode: login (Azure AD) or key\n\n**-o**, **--output** _value_\n> Output format: json, table, tsv, yaml, jsonc, none"],["CAVEATS","Authentication is required via **--account-key**, **--sas-token**, **--connection-string**, or **--auth-mode login**. Archive tier blobs must be rehydrated before access. Batch operations respect patterns but do not recursively traverse virtual directories by default. Delete operations mark blobs for deletion; soft-delete policies may allow recovery."],["INSTALL","```dnf: sudo dnf install azure-cli```\n\n```pacman: sudo pacman -S azure-cli```\n\n```zypper: sudo zypper install azure-cli```\n\n```brew: brew install azure-cli```\n\n```nix: nix profile install nixpkgs#azure-cli```"],["SEE ALSO","[az-storage](/man/az-storage)(1), [az-storage-container](/man/az-storage-container)(1), [az-storage-account](/man/az-storage-account)(1)"]],"e":["az-storage","az-storage-container","az-storage-account"],"x":"az storage blob upload -f [/path/to/file] -c [container-name] -n [blob-name] --account-name [account]"},"b3sum":{"n":"b3sum","t":"Compute and verify BLAKE3 cryptographic checksums.","s":[["TAGLINE","Compute and verify BLAKE3 cryptographic checksums."],["TLDR","**Calculate** BLAKE3 checksum\n\n```b3sum [file]```\n\n**Verify** checksums\n\n```b3sum -c [checksums.b3]```\n\nCalculate from **stdin**\n\n```echo \"data\" | b3sum```\n\n**Multiple files**\n\n```b3sum [file1] [file2] [file3]```\n\nGenerate a **keyed hash** (32-byte key read from stdin)\n\n```b3sum --keyed [file] < [keyfile]```\n\n**Derive a key** from a context string\n\n```b3sum --derive-key \"[app 2026-06-19 context]\" [file]```"],["SYNOPSIS","**b3sum** [_options_] [_file_...]"],["DESCRIPTION","**b3sum** computes BLAKE3 cryptographic checksums. BLAKE3 is significantly faster than BLAKE2, MD5, SHA-1, and SHA-2, while maintaining high security. It's optimized for parallelism and can fully utilize modern CPU capabilities.\n\nThe tool provides similar interface to md5sum and sha256sum but with much better performance."],["PARAMETERS","**-c**, **--check**\n> Read BLAKE3 sums from the file(s) and verify them.\n\n**--keyed**\n> Keyed hashing mode; reads a 32-byte key from stdin.\n\n**--derive-key** _context_\n> Key derivation mode using the given context string.\n\n**-l**, **--length** _bytes_\n> Output length in bytes before hex encoding (default: 32).\n\n**--seek** _offset_\n> Start output at the given byte offset (default: 0).\n\n**--num-threads** _n_\n> Maximum number of threads to use.\n\n**--no-mmap**\n> Disable memory mapping of input files.\n\n**--no-names**\n> Omit filenames; output the hash only.\n\n**--raw**\n> Write the raw hash bytes to stdout instead of hex (single input only).\n\n**--tag**\n> Produce BSD-style checksum lines.\n\n**--quiet**\n> When verifying, do not print OK for each successfully verified file."],["FEATURES","- Extremely fast (GBs per second)\n- Parallel processing\n- Unlimited output length\n- Tree hashing for verification\n- Keyed mode for authentication\n- Deterministic output"],["WORKFLOW","```bash"],["Hash files","b3sum *.tar.gz > checksums.b3"],["Verify","b3sum -c checksums.b3"],["Hash stdin","cat largefile | b3sum"],["Use multiple threads","b3sum --num-threads 8 hugefile.bin\n```"],["CAVEATS","Relatively new algorithm (2020). Less widely adopted than SHA-256. Not suitable for password hashing (use argon2 instead). Requires recent software versions."],["HISTORY","**b3sum** was released in **2020** alongside the BLAKE3 specification, created by Jack O'Connor and Samuel Neves as an evolution of BLAKE2 optimized for speed and parallelism."],["INSTALL","```apt: sudo apt install b3sum```\n\n```dnf: sudo dnf install b3sum```\n\n```pacman: sudo pacman -S b3sum```\n\n```apk: sudo apk add b3sum```\n\n```zypper: sudo zypper install b3sum```\n\n```brew: brew install b3sum```\n\n```nix: nix profile install nixpkgs#b3sum```"],["SEE ALSO","[b2sum](/man/b2sum)(1), [sha256sum](/man/sha256sum)(1), [md5sum](/man/md5sum)(1), [xxhsum](/man/xxhsum)(1)"],["RESOURCES","```[Source code](https://github.com/BLAKE3-team/BLAKE3)```\n\n```[Documentation](https://github.com/BLAKE3-team/BLAKE3/blob/master/b3sum/README.md)```"]],"e":["b2sum","sha256sum","md5sum","xxhsum"],"x":"b3sum [file]"},"calligrastage":{"n":"calligrastage","t":"Presentation application from Calligra suite","s":[["TAGLINE","Presentation application from Calligra suite"],["TLDR","**Launch Calligra Stage**\n\n```calligrastage```\n\n**Open a presentation file**\n\n```calligrastage [presentation.odp]```\n\n**Show version information**\n\n```calligrastage --version```\n\n**Show all available options**\n\n```calligrastage --help-all```"],["SYNOPSIS","**calligrastage** [_options_] [_file_]"],["PARAMETERS","**--help**\n> Show help about options.\n\n**--help-qt**\n> Show Qt-specific options.\n\n**--help-kde**\n> Show KDE-specific options.\n\n**--help-all**\n> Show all options.\n\n**--author**\n> Show author information.\n\n**-v**, **--version**\n> Show version information.\n\n**--license**\n> Show license information."],["DESCRIPTION","**calligrastage** is a presentation application from the Calligra office suite. It creates and displays slide presentations with support for transitions, animations, and multimedia.\n\nThe application uses ODF (Open Document Format) and is comparable to Microsoft PowerPoint or LibreOffice Impress."],["INSTALL","```apt: sudo apt install calligrastage```\n\n```dnf: sudo dnf install calligra```\n\n```pacman: sudo pacman -S calligra```\n\n```apk: sudo apk add calligra```"],["SEE ALSO","[calligrawords](/man/calligrawords)(1), [calligrasheets](/man/calligrasheets)(1), [calligraflow](/man/calligraflow)(1), [libreoffice](/man/libreoffice)(1)"]],"e":["calligrawords","calligrasheets","calligraflow","libreoffice"],"x":"calligrastage"},"chalk-cli":{"n":"chalk-cli","t":"terminal string styling with colors and formatting","s":[["TAGLINE","terminal string styling with colors and formatting"],["TLDR","**Style text with color**\n\n```chalk red \"Hello World\"```\n\n**Apply multiple styles**\n\n```chalk red bold \"Important text\"```\n\n**Use template syntax**\n\n```chalk -t \"{red.bold Unicorns} are {blue fun}\"```\n\n**Read from stdin**\n\n```echo \"text\" | chalk --stdin green```\n\n**Negate a style in template**\n\n```chalk -t \"{red text {~red normal}}\"```\n\n**Output without newline**\n\n```chalk -n yellow \"No newline\"```\n\n**Demo all styles**\n\n```chalk --demo```"],["SYNOPSIS","**chalk** [_options_] _style_... _string_\n\n**echo** _string_ **| chalk** **--stdin** _style_..."],["DESCRIPTION","**chalk-cli** is a command-line tool for styling terminal strings with colors and formatting. It wraps the popular Node.js Chalk library, providing access to its styling capabilities from shell scripts and the command line.\n\nStyles include foreground and background colors (standard and bright variants), as well as text modifiers like bold, dim, italic, underline, inverse, and strikethrough. Multiple styles can be combined, and the template syntax allows applying different styles to different parts of a single string.\n\nInput can come from command arguments or stdin, making it useful in shell pipelines for highlighting output or building colorful CLI interfaces."],["PARAMETERS","**-t**, **--template**\n> Use template syntax for nested styles\n\n**--stdin**\n> Read input from stdin\n\n**-n**, **--no-newline**\n> Don't emit newline after output\n\n**--demo**\n> Show demo of all available styles\n\n**-c**, **--color**\n> Force color support"],["STYLES","**Colors**\n> black, red, green, yellow, blue, magenta, cyan, white, gray\n\n**Bright colors**\n> redBright, greenBright, yellowBright, etc.\n\n**Background**\n> bgRed, bgGreen, bgBlue, etc.\n\n**Modifiers**\n> bold, dim, italic, underline, inverse, strikethrough"],["TEMPLATE SYNTAX","Templates allow inline nested styling:\n\n```\n{red.bold text}\n{red red {~red normal}}  # ~ negates style\n```"],["INSTALLATION","```npm install --global chalk-cli```"],["CAVEATS","Requires Node.js. Color support depends on terminal capabilities. Use --color to force colors in pipes."],["INSTALL","```brew: brew install chalk-cli```"],["SEE ALSO","[tput](/man/tput)(1), [printf](/man/printf)(1)"],["RESOURCES","```[Source code](https://github.com/chalk/chalk-cli)```"]],"e":["tput","printf"],"x":"chalk red \"Hello World\""},"cheatshh":{"n":"cheatshh","t":"interactive cheatsheet manager for the terminal","s":[["TAGLINE","interactive cheatsheet manager for the terminal"],["TLDR","**Launch the interactive fuzzy-finder menu**\n\n```cheatshh```\n\n**Create a new command group**\n\n```cheatshh -g```\n\n**Show help**\n\n```cheatshh -h```"],["SYNOPSIS","**cheatshh** [_options_]"],["DESCRIPTION","**cheatshh** is an interactive command-line tool for storing, organizing, and quickly accessing your own command cheatsheets. Rather than memorizing commands, you save them in named groups with custom descriptions and recall them through a fuzzy-finder (fzf) and whiptail menu.\n\nAlongside your stored commands, cheatshh can display **tldr** and **man** pages and pull examples from external sources such as **cheat.sh**, all configurable through its settings. It supports adding, editing, and deleting commands, bookmarking frequently used entries, and customizing colors and automatic man-page display. It is written in shell with Python components and runs on Linux and macOS."],["PARAMETERS","**-g**\n> Create a new command group\n\n**-h**, **--help**\n> Display help information"],["INSTALL","```aur: yay -S cheatshh```"],["SEE ALSO","[tldr](/man/tldr)(1), [man](/man/man)(1), [cheat](/man/cheat)(1), [fzf](/man/fzf)(1)"],["RESOURCES","```[Source code](https://github.com/AnirudhG07/cheatshh)```"]],"e":["tldr","man","cheat","fzf"],"x":"cheatshh"},"clang-tidy":{"n":"clang-tidy","t":"Clang-based C++ linter and static analyzer","s":[["TAGLINE","Clang-based C++ linter and static analyzer"],["TLDR","**Run checks on file**\n\n```clang-tidy [file.cpp] -- -I[include/path]```\n\n**List enabled checks**\n\n```clang-tidy --list-checks```\n\n**List all available checks**\n\n```clang-tidy --list-checks -checks='*'```\n\n**Run specific checks**\n\n```clang-tidy -checks='-*,modernize-*' [file.cpp]```\n\n**Apply automatic fixes**\n\n```clang-tidy --fix [file.cpp]```\n\n**Export fixes to file**\n\n```clang-tidy --export-fixes=[fixes.yaml] [file.cpp]```\n\n**Dump configuration**\n\n```clang-tidy --dump-config```"],["SYNOPSIS","**clang-tidy** [_options_] _file_ [_-- compile_options_]"],["DESCRIPTION","**clang-tidy** is a clang-based C++ linter tool for diagnosing and fixing typical programming errors including style violations, interface misuse, and bugs detectable via static analysis. It is part of the LLVM/Clang extra tools.\n\nThe tool provides hundreds of checks organized into categories such as bugprone, modernize, performance, readability, and cppcoreguidelines. Many checks can automatically apply fixes to source code, making it useful for large-scale code modernization (e.g., migrating to modern C++ idioms).\n\nclang-tidy uses a **.clang-tidy** configuration file for project-level settings and supports inline suppression with NOLINT comments. For large projects, **run-clang-tidy.py** provides parallel execution across multiple files."],["PARAMETERS","**-checks**=_list_\n> Comma-separated list of checks (+/- prefixed globs)\n\n**--list-checks**\n> List enabled checks\n\n**--fix**\n> Apply suggested fixes\n\n**--fix-errors**\n> Apply fixes even if errors occur\n\n**--export-fixes**=_file_\n> Write fixes to YAML file\n\n**--dump-config**\n> Dump configuration to stdout\n\n**--warnings-as-errors**=_list_\n> Treat specified warnings as errors\n\n**-p** _path_\n> Path to compilation database\n\n**--config-file**=_file_\n> Path to .clang-tidy config\n\n**--format-style**=_style_\n> Format style for applied fixes (none, file, llvm, google, webkit, mozilla)\n\n**--extra-arg**=_arg_\n> Additional argument to append to the compiler command line\n\n**--fix-notes**\n> Apply fixes from diagnostic notes (implies --fix)\n\n**--allow-no-checks**\n> Allow empty enabled checks without error"],["CHECK CATEGORIES","**bugprone-***\n> Bug-prone patterns\n\n**modernize-***\n> C++ modernization\n\n**performance-***\n> Performance issues\n\n**readability-***\n> Code readability\n\n**cppcoreguidelines-***\n> C++ Core Guidelines\n\n**clang-analyzer-***\n> Clang Static Analyzer checks\n\n**misc-***\n> Miscellaneous checks\n\n**cert-***\n> CERT secure coding standard"],["CONFIGURATION","Create **.clang-tidy** in project root:\n\n```yaml\nChecks: '-*,modernize-*,bugprone-*'\nWarningsAsErrors: '*'\n```"],["SUPPRESSION","Use comments: NOLINT, NOLINTNEXTLINE, NOLINTBEGIN/NOLINTEND"],["CAVEATS","Requires compilation database for complex projects. Use run-clang-tidy.py for parallel execution."],["INSTALL","```apt: sudo apt install clang-tidy```\n\n```apk: sudo apk add clang22-extra-tools```"],["SEE ALSO","[clang](/man/clang)(1), [clang-format](/man/clang-format)(1)"]],"e":["clang","clang-format"],"x":"clang-tidy [file.cpp] -- -I[include/path]"},"clipse":{"n":"clipse","t":"Configurable TUI clipboard manager for Unix","s":[["TAGLINE","Configurable TUI clipboard manager for Unix"],["TLDR","**Open** the clipboard history TUI\n\n```clipse```\n\n**Start** the background clipboard listener\n\n```clipse -listen```\n\n**Copy text** to system clipboard via pipe\n\n```echo \"hello\" | clipse -c```\n\n**Add text** to clipboard history without copying to system clipboard\n\n```echo \"some data\" | clipse -a```\n\n**Print** current clipboard content to stdout\n\n```clipse -p```\n\n**Wipe** clipboard history except pinned items\n\n```clipse -clear```\n\n**Pause** clipboard monitoring for 5 minutes\n\n```clipse -pause 5m```"],["SYNOPSIS","**clipse** [_flags_] [_command_]"],["PARAMETERS","**-listen**\n> Run a background listener process that monitors clipboard changes.\n\n**--listen-shell**\n> Run the listener in the current terminal for debugging.\n\n**-a** _string_\n> Add string to clipboard history without copying to system clipboard. Accepts stdin.\n\n**-c** _string_\n> Copy string to the system clipboard. Accepts stdin.\n\n**-p**\n> Print the current clipboard content to the console.\n\n**-output-all**\n> Print entire clipboard history to stdout.\n\n**-clear**\n> Wipe all clipboard history except pinned items.\n\n**-clear-all**\n> Wipe the entire clipboard history including pinned items.\n\n**-clear-images**\n> Wipe all images from clipboard history.\n\n**-clear-text**\n> Wipe all text items from clipboard history.\n\n**-clean**\n> Sanitize existing text entries and remove orphaned image entries.\n\n**-kill**\n> Kill any existing background listener processes.\n\n**-pause** _duration_\n> Pause clipboard monitoring for a specified duration (e.g., 5m, 1h).\n\n**-v**\n> Print the version.\n\n**keep**\n> Keep the TUI open after selecting an item to copy.\n\n**-help**\n> Display help information."],["DESCRIPTION","**clipse** is a clipboard manager written in Go that provides a terminal user interface for browsing, searching, and managing clipboard history. It supports both text and images, works on Wayland, X11, and macOS, and is built with the BubbleTea TUI framework.\n\nKey features include fuzzy search and filtering, multi-select for bulk operations, pinning important entries to persist through clears, duplicate filtering, configurable history limits (default 100 entries), auto-paste support, and the ability to exclude specific applications from monitoring. The TUI appearance and keybindings are fully customizable."],["CONFIGURATION","Configuration stored at **~/.config/clipse/configuration.json**. Theme stored separately as **custom_theme.json** in the same directory. Options include **maxHistory**, duplicate filtering, auto-paste keybind, excluded apps/windows, and custom theme colors."],["CAVEATS","On **Wayland**, requires **wl-clipboard** installed for text and image clipboard access. Auto-paste on Wayland requires access to **/dev/uinput**. On **X11**, may require xlib API headers for building from source. The background listener (**clipse -listen**) must be started on login/boot to capture clipboard history."],["HISTORY","**clipse** was created by **savedra1** on GitHub. The first stable release (v1.0.0) arrived in **July 2023** with multi-select, custom theme support, and image/text previews. Version 1.1.0 (October 2023) added custom keybinds, and v1.2.0 (December 2024) brought a major performance overhaul with C API integration for X11/Darwin, auto-paste, and excluded apps support."],["INSTALL","```nix: nix profile install nixpkgs#clipse```"],["SEE ALSO","[xclip](/man/xclip)(1), [xsel](/man/xsel)(1), [wl-copy](/man/wl-copy)(1), [pbcopy](/man/pbcopy)(1)"]],"e":["xclip","xsel","wl-copy","pbcopy"],"x":"clipse"},"conda-update":{"n":"conda-update","t":"package updater","s":[["TAGLINE","package updater"],["TLDR","**Update all packages**\n\n```conda update --all```\n\n**Update specific package**\n\n```conda update [package_name]```\n\n**Update conda itself**\n\n```conda update conda```\n\n**Update in specific environment**\n\n```conda update -n [env_name] [package_name]```\n\n**Update without confirmation**\n\n```conda update -y [package_name]```"],["SYNOPSIS","**conda** **update** [_options_] [_packages..._]"],["PARAMETERS","**--all**\n> Update all installed packages.\n\n**-n**, **--name** _name_\n> Update in named environment.\n\n**-c**, **--channel** _channel_\n> Additional channel to search.\n\n**-y**, **--yes**\n> Don't ask for confirmation.\n\n**--update-deps**\n> Update dependencies."],["CONFIGURATION","**~/.condarc**\n> User-level conda configuration for channels, package settings, and solver options.\n\n**/opt/conda/.condarc**\n> System-wide conda configuration."],["DESCRIPTION","**conda update** updates packages to the latest compatible versions. It respects existing package constraints and may update dependencies as needed."],["CAVEATS","**--all** may fail if there are conflicting package requirements. In such cases, update packages individually."],["INSTALL","```dnf: sudo dnf install conda```\n\n```nix: nix profile install nixpkgs#conda```"],["SEE ALSO","[conda](/man/conda)(1), [conda-install](/man/conda-install)(1), [conda-list](/man/conda-list)(1)"],["RESOURCES","```[Source code](https://github.com/conda/conda)```\n\n```[Homepage](https://docs.conda.io)```\n\n```[Documentation](https://docs.conda.io/projects/conda/en/stable/commands/index.html)```"]],"e":["conda","conda-install","conda-list"],"x":"conda update --all"},"crypttab":{"n":"crypttab","t":"encrypted block device configuration","s":[["TAGLINE","encrypted block device configuration"],["TLDR","**View current crypttab**\n\n```cat /etc/crypttab```\n\n**Basic encrypted partition entry**\n\n```luks_root UUID=[device-uuid] none luks```\n\n**Entry with keyfile**\n\n```luks_data UUID=[device-uuid] /root/keyfile luks```\n\n**Entry with discard/TRIM support**\n\n```luks_ssd UUID=[device-uuid] none luks,discard```\n\n**Entry for swap encryption**\n\n```cryptswap /dev/sdX /dev/urandom swap,cipher=aes-xts-plain64,size=256```"],["SYNOPSIS","**/etc/crypttab**"],["FORMAT","```\nname  device  keyfile  options\n```\n\n**name**: Mapped device name (/dev/mapper/name)\n**device**: Block device, UUID=xxx, or PARTUUID=xxx\n**keyfile**: Path to key or \"none\" for password prompt\n**options**: Comma-separated mount options"],["OPTIONS","**luks**\n> Device is LUKS encrypted (auto-detected usually).\n\n**plain**\n> Plain dm-crypt (no LUKS header).\n\n**swap**\n> Format as encrypted swap (destroys data).\n\n**discard**\n> Allow TRIM/discard passthrough (SSD optimization).\n\n**noauto**\n> Don't unlock at boot.\n\n**nofail**\n> Don't fail boot if device unavailable.\n\n**tries=N**\n> Password attempts before failing.\n\n**timeout=N**\n> Seconds to wait for device.\n\n**cipher=**_cipher_\n> Encryption cipher (for plain mode).\n\n**size=**_bits_\n> Key size in bits.\n\n**keyfile-offset=**_bytes_\n> Offset in keyfile.\n\n**keyfile-size=**_bytes_\n> Bytes to read from keyfile."],["DESCRIPTION","**/etc/crypttab** defines encrypted block devices to be unlocked at boot by systemd-cryptsetup or cryptsetup. Each line describes one encrypted device: its mapped name, source device, key material, and options.\n\nThe file works alongside **/etc/fstab**: crypttab unlocks encrypted devices, then fstab mounts the resulting mapped devices. For LUKS devices, the system prompts for a password at boot unless a keyfile is specified.\n\nUUID-based device identification is recommended over device paths for reliability across hardware changes."],["EXAMPLE","```"],["LUKS root partition, password prompt","luks-root UUID=12345678-1234-1234-1234-123456789abc none luks"],["Data partition with keyfile","luks-data UUID=abcdefab-cdef-abcd-efab-cdefabcdefab /root/data.key luks"],["SSD with TRIM support","luks-ssd UUID=87654321-4321-4321-4321-210987654321 none luks,discard"],["Encrypted swap (random key each boot)","cryptswap /dev/sda2 /dev/urandom swap,cipher=aes-xts-plain64,size=256\n```"],["CAVEATS","Keyfiles should be readable only by root and ideally on an encrypted root partition. The **discard** option may leak information about filesystem usage. Encrypted swap with random keys loses swap contents on reboot. Test crypttab changes carefully to avoid unbootable systems."],["HISTORY","The crypttab format originated in Debian and was later adopted by systemd and other distributions. It was designed to integrate dm-crypt/LUKS encryption with the boot process. The file format has evolved to support LUKS, plain dm-crypt, and various options for key management and performance tuning."],["SEE ALSO","[cryptsetup](/man/cryptsetup)(8), [fstab](/man/fstab)(5), [systemd-cryptsetup](/man/systemd-cryptsetup)(8)"],["RESOURCES","```[Source code](https://github.com/systemd/systemd)```\n\n```[Documentation](https://www.freedesktop.org/software/systemd/man/crypttab.html)```"]],"e":["cryptsetup","fstab","systemd-cryptsetup"],"x":"cat /etc/crypttab"},"fasd":{"n":"fasd","t":"frecency-based file and directory navigation","s":[["TAGLINE","frecency-based file and directory navigation"],["TLDR","**Jump to frecent directory**\n\n```z [pattern]```\n\n**Edit frecent file**\n\n```f [pattern]```\n\n**List frecent directories**\n\n```d```\n\n**List frecent files**\n\n```f```\n\n**Show all frecent paths**\n\n```fasd```\n\n**Interactive selection**\n\n```fasd -si [pattern]```\n\n**Add path to database**\n\n```fasd -A [path]```"],["SYNOPSIS","**fasd** [_options_] [_patterns_...]"],["DESCRIPTION","**fasd** provides quick access to frequently and recently used files and directories. It tracks access patterns and uses a \"frecency\" algorithm combining frequency and recency.\n\nThe tool integrates with the shell to track cd and file access, enabling fast navigation with minimal typing. Aliases like z (directories) and f (files) provide shortcuts."],["PARAMETERS","**-s**\n> Show ranks and paths.\n\n**-l**\n> List paths only.\n\n**-i**\n> Interactive selection.\n\n**-e** _cmd_\n> Execute command on result.\n\n**-a**\n> Match files and directories.\n\n**-d**\n> Match directories only.\n\n**-f**\n> Match files only.\n\n**-r**\n> Match by rank only.\n\n**-t**\n> Match by recency only.\n\n**-A**\n> Add path to database.\n\n**-D**\n> Remove path from database."],["ALIASES (shell init)","**z**: Jump to directory (fasd_cd -d)\n**zz**: Interactive directory select\n**f**: Select file\n**v**: Open frecent file in $EDITOR"],["CONFIGURATION","**~/.fasd**\n> Database file tracking frecency scores for files and directories."],["CAVEATS","Requires shell integration. Database builds over time. Patterns are substrings. May need manual cleanup of old entries."],["HISTORY","**fasd** was created by **Wei Dai** (clvv) inspired by autojump and z. It combined features of both, adding file tracking and extensibility. The project became popular for its speed and flexibility in shell navigation."],["INSTALL","```pacman: sudo pacman -S fasd```\n\n```zypper: sudo zypper install fasd```\n\n```nix: nix profile install nixpkgs#fasd```"],["SEE ALSO","[z](/man/z)(1), [autojump](/man/autojump)(1), [zoxide](/man/zoxide)(1), [fzf](/man/fzf)(1)"],["RESOURCES","```[Source code](https://github.com/clvv/fasd)```"]],"e":["z","autojump","zoxide","fzf"],"x":"z [pattern]"},"foo2hp2600-wrapper":{"n":"foo2hp2600-wrapper","t":"PostScript/PDF to HP ZjStream wrapper script","s":[["TAGLINE","PostScript/PDF to HP ZjStream wrapper script"],["TLDR","**Print PostScript file**\n\n```foo2hp2600-wrapper [file.ps]```\n\n**Set paper size**\n\n```foo2hp2600-wrapper -p [a4] [file.ps]```\n\n**Color printing**\n\n```foo2hp2600-wrapper -c [file.ps]```\n\n**Set resolution**\n\n```foo2hp2600-wrapper -r [600] [file.ps]```\n\n**Print multiple copies**\n\n```foo2hp2600-wrapper -n [3] [file.ps]```\n\n**Monochrome printing**\n\n```foo2hp2600-wrapper [file.ps]```"],["SYNOPSIS","**foo2hp2600-wrapper** [_options_] [_files_...]"],["PARAMETERS","_FILES_\n> PostScript or PDF files to print.\n\n**-p** _PAPER_\n> Paper size (letter, a4, legal).\n\n**-r** _DPI_\n> Resolution (300, 600, 1200).\n\n**-c**\n> Color printing.\n\n**-n** _COPIES_\n> Number of copies.\n\n**-m** _MEDIA_\n> Media type (plain, thick, envelope, transparency).\n\n**-s** _SOURCE_\n> Paper source tray.\n\n**-2**\n> Duplex (two-sided) printing.\n\n**--help**\n> Display help information."],["DESCRIPTION","**foo2hp2600-wrapper** converts PostScript/PDF files to HP ZjStream format for HP Color LaserJet 2600n and similar printers. It wraps Ghostscript and foo2hp for simplified printing.\n\nThe script handles the complete conversion pipeline from input document to printer-ready data. It supports color and monochrome modes with various paper sizes and quality settings.\n\nfoo2hp2600-wrapper provides direct command-line printing for supported HP printers."],["CAVEATS","Requires Ghostscript. Only for supported HP printer models. Color accuracy depends on printer calibration."],["HISTORY","foo2hp2600-wrapper is part of the **foo2zjs** project, providing a user-friendly interface to print documents on HP Color LaserJet printers using reverse-engineered protocols."],["SEE ALSO","[foo2hp](/man/foo2hp)(1), [gs](/man/gs)(1), [lpr](/man/lpr)(1)"]],"e":["foo2hp","gs","lpr"],"x":"foo2hp2600-wrapper [file.ps]"},"forall":{"n":"forall","t":"spec-driven coding agent with machine-checkable proofs","s":[["TAGLINE","spec-driven coding agent with machine-checkable proofs"],["TLDR","**Install** the CLI (requires a published release)\n\n```curl -fsSL https://forall.astrio.app/install.sh | bash```\n\nCheck the **version**\n\n```forall --version```\n\n**Initialize** Forall in a git repository\n\n```forall init```\n\nStart the **interactive agent**\n\n```forall```"],["SYNOPSIS","**forall** [_COMMAND_] [_OPTIONS_]"],["DESCRIPTION","**forall** is a terminal coding agent from Astrio that aims to generate software from specifications together with machine-checkable proofs. It runs as an interactive CLI: after install, sign in with a Forall account API key or bring your own model key (OpenAI / OpenRouter), run **forall init** inside a git repository, and work through the agent workflow in the terminal.\n\nSupported languages currently include TypeScript, Java, and Rust. For teams that prefer another editor agent, Forall also offers a verify-only MCP server (**@astrio/forall-mcp**) that reports verification results without replacing the main coding agent."],["CAVEATS","Install succeeds only when a binary is available on GitHub Releases. The agent needs network access and an API key for model inference. Proof and verification coverage depend on project language and tooling; treat generated proofs as part of a review process, not a substitute for testing."],["HISTORY","**forall** is developed by **Astrio Labs** and released under the Apache-2.0 license."],["SEE ALSO","[git](/man/git)(1), [cargo](/man/cargo)(1), [npm](/man/npm)(1)"],["RESOURCES","```[Source code](https://github.com/astrio-labs/forall)```\n\n```[Homepage](https://forall.astrio.app)```\n\n```[Documentation](https://github.com/astrio-labs/forall/blob/main/docs/getting-started.md)```"]],"e":["git","cargo","npm"],"x":"curl -fsSL https://forall.astrio.app/install.sh | bash"},"forever":{"n":"forever","t":"Node.js process daemon and monitor","s":[["TAGLINE","Node.js process daemon and monitor"],["TLDR","**Start script as daemon**\n\n```forever start [app.js]```\n\n**Stop daemon**\n\n```forever stop [app.js]```\n\n**Stop all daemons**\n\n```forever stopall```\n\n**List running processes**\n\n```forever list```\n\n**Restart script**\n\n```forever restart [app.js]```\n\n**Start with log file**\n\n```forever start -l [forever.log] -o [out.log] -e [err.log] [app.js]```"],["SYNOPSIS","**forever** [_options_] _command_ [_script_] [_args_]"],["PARAMETERS","**start** _script_\n> Start script as daemon.\n\n**stop** _script|pid|index_\n> Stop running process.\n\n**stopall**\n> Stop all processes.\n\n**restart** _script|pid|index_\n> Restart process.\n\n**restartall**\n> Restart all processes.\n\n**list**\n> List running processes.\n\n**logs** _script|pid|index_\n> Show log files.\n\n**-l** _logfile_\n> Log file for forever output.\n\n**-o** _outfile_\n> Stdout log file.\n\n**-e** _errfile_\n> Stderr log file.\n\n**-w**, **--watch**\n> Watch for file changes.\n\n**-m** _max_\n> Maximum restarts.\n\n**-s**, **--silent**\n> Run script silencing stdout and stderr.\n\n**-a**, **--append**\n> Append to log files instead of overwriting.\n\n**--minUptime** _ms_\n> Minimum uptime before considering a restart as a crash (default: 1000ms).\n\n**--killTree**\n> Kill the entire child process tree on stop."],["CONFIGURATION","**~/.forever/config.json**\n> Global configuration for forever settings and defaults."],["DESCRIPTION","**forever** runs Node.js scripts continuously, restarting them automatically if they crash. It's designed for production deployment of Node.js applications, ensuring applications stay running.\n\nThe tool monitors processes and restarts them on exit. It supports logging, multiple instances, and can watch files for development auto-restart."],["CAVEATS","Node.js only. Consider PM2 for production use (more features). Log files can grow large. Watch mode may miss some changes. Process management is basic compared to alternatives."],["HISTORY","forever was created by **Charlie Robbins** and the **Nodejitsu** team in the early **2010s** as one of the first process managers for Node.js. While PM2 has largely replaced it for production use, forever remains simple and functional."],["SEE ALSO","[pm2](/man/pm2)(1), [nodemon](/man/nodemon)(1), [node](/man/node)(1), [npm](/man/npm)(1)"]],"e":["pm2","nodemon","node","npm"],"x":"forever start [app.js]"},"get_iplayer":{"n":"get_iplayer","t":"BBC iPlayer TV and radio downloader","s":[["TAGLINE","BBC iPlayer TV and radio downloader"],["TLDR","**Search for programs**\n\n```get_iplayer \"[search term]\"```\n\n**Download a program**\n\n```get_iplayer --get [index_number]```\n\n**Download by PID**\n\n```get_iplayer --pid [pid]```\n\n**List TV programs**\n\n```get_iplayer --type=tv \"[search]\"```\n\n**List radio programs**\n\n```get_iplayer --type=radio \"[search]\"```\n\n**Set output directory**\n\n```get_iplayer --output [/path/to/dir] --get [index]```\n\n**Download in specific quality**\n\n```get_iplayer --tvmode=best --get [index]```"],["SYNOPSIS","**get_iplayer** [_options_] [_search_...]"],["PARAMETERS","**--get** _index_\n> Download by index number.\n\n**--pid** _pid_\n> Download by program PID.\n\n**--type** _type_\n> Media type (tv, radio, podcast).\n\n**--output** _dir_\n> Output directory.\n\n**--tvmode** _mode_\n> TV quality preference (fhd, hd, sd, web, mobile), comma-delimited in descending preference.\n\n**--radiomode** _mode_\n> Radio quality preference (high, std, med, low).\n\n**--pvr**\n> Run PVR scheduler.\n\n**--refresh**\n> Refresh program cache.\n\n**--url** _URL_\n> Record the PIDs from the specified iPlayer episode URLs.\n\n**--subtitles**\n> Download subtitles when available.\n\n**--audio-only**\n> Only download the audio stream for TV programmes (produces .m4a file).\n\n**--file-prefix** _TEMPLATE_\n> Filename prefix template (excluding directory and extension).\n\n**--attempts** _N_\n> Number of attempts to make or resume a failed connection."],["CONFIGURATION","**~/.get_iplayer/options**\n> User preferences file for default options and settings.\n\n**~/.get_iplayer/presets**\n> Custom quality and recording presets."],["DESCRIPTION","**get_iplayer** downloads BBC iPlayer TV and radio programs from the BBC's streaming service. It provides search functionality across the iPlayer catalog, quality selection, and automated PVR-style scheduling for recording programs.\n\nThe tool can download both on-demand and live content, with support for subtitles and metadata. It maintains a local cache of available programs for faster searching and supports batch downloads.\n\nPrograms on iPlayer are typically available for 30 days after broadcast, though some content may have different availability windows based on BBC rights agreements."],["CAVEATS","Only works with BBC content and typically requires a UK IP address. Programs expire after 30 days on iPlayer (some content has different availability windows). Requires ffmpeg for downloading and converting streams."],["INSTALL","```dnf: sudo dnf install get_iplayer```\n\n```brew: brew install get_iplayer```\n\n```nix: nix profile install nixpkgs#get_iplayer```"],["SEE ALSO","[youtube-dl](/man/youtube-dl)(1), [yt-dlp](/man/yt-dlp)(1), [ffmpeg](/man/ffmpeg)(1)"]],"e":["youtube-dl","yt-dlp","ffmpeg"],"x":"get_iplayer \"[search term]\""},"git-blame-someone-else":{"n":"git-blame-someone-else","t":"rewrite the author and committer of a commit","s":[["TAGLINE","rewrite the author and committer of a commit"],["TLDR","**Reassign** a commit to another author\n\n```git blame-someone-else \"[Author Name] <email@example.com>\" [commit]```\n\n**Reassign** the most recent commit\n\n```git blame-someone-else \"[Name] <email@example.com>\" HEAD```\n\n**Reassign** a commit by hash\n\n```git blame-someone-else \"[Name] <email@example.com>\" [abc123]```"],["SYNOPSIS","**git blame-someone-else** _author_ _commit_"],["PARAMETERS","_author_\n> New identity, in **\"Name &lt;email&gt;\"** format.\n\n_commit_\n> The commit to rewrite."],["DESCRIPTION","**git blame-someone-else** rewrites a commit so that it appears to have been made by someone else. It is a standalone script by **Jay Phelps**, written as a joke, and is not part of git-extras or core git.\n\nMechanically it uses **git filter-branch** with **GIT_AUTHOR_NAME**, **GIT_AUTHOR_EMAIL**, **GIT_COMMITTER_NAME** and **GIT_COMMITTER_EMAIL** overridden for the target commit, then rewrites every commit from that point forward. Both the author and the committer are changed, not just the author.\n\nThe one defensible use is repairing your own attribution: commits made with a misconfigured **user.email**, or from a shared machine, where the recorded identity is simply wrong."],["CAVEATS","This is a joke tool, and its author says so explicitly, along with a request not to run it against a production repository and complain if it eats everything. Treat it accordingly.\n\nIt **rewrites history**. Every commit from the target onwards gets a new hash, so anyone who has already pulled the old commits will have a diverging history, and publishing the result needs a force push. Never run it on a shared branch.\n\nIt changes the **committer** as well as the author, so it does not merely correct attribution, it erases the record of who actually made the commit.\n\nAttributing work to a real person who did not write it is dishonest, and on a repository you do not own it may violate the project's contribution terms. Signed commits do not survive: the rewrite invalidates any GPG signature, which is one of the few technical defences against exactly this.\n\nCorrecting your own identity does not need this tool. **git commit --amend --author=\"Name <email>\"** fixes the most recent commit, and **git rebase -i** with **--reset-author** handles older ones with less collateral damage."],["HISTORY","**git blame-someone-else** was written by **Jay Phelps** in **2016** as a joke. Its README quotes an endorsement from **Linus Torvalds**, which is itself a fabricated commit produced by the tool to demonstrate that it works."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-blame](/man/git-blame)(1), [git-commit](/man/git-commit)(1), [git-filter-branch](/man/git-filter-branch)(1), [git-rebase](/man/git-rebase)(1)"],["RESOURCES","```[Source code](https://github.com/jayphelps/git-blame-someone-else)```"]],"e":["git-blame","git-commit","git-filter-branch","git-rebase"],"x":"git blame-someone-else \"[Author Name] <email@example.com>\" [commit]"},"grok":{"n":"grok","t":"AI-powered terminal coding agent using xAI's API","s":[["TAGLINE","AI-powered terminal coding agent using xAI's API"],["TLDR","**Start an interactive session** in the current directory\n\n```grok```\n\n**Start in a specific directory**\n\n```grok -d [path/to/project]```\n\n**Run a one-off command** and exit (headless mode)\n\n```grok -p \"[prompt]\"```\n\n**Specify a different model**\n\n```grok -m [grok-4-latest]```\n\n**Run with custom API key**\n\n```grok -k [your_api_key]```\n\n**Add an MCP server**\n\n```grok mcp add [server-name] --transport stdio --command bun --args [server.js]```"],["SYNOPSIS","**grok** [_options_] [_prompt_]\n\n**grok** **mcp** **add** _server-name_ [_--transport type_] [_--command cmd_] [_--args args_]"],["PARAMETERS","**-V**, **--version**\n> Display version information.\n\n**-d**, **--directory** _path_\n> Set working directory for the session.\n\n**-k**, **--api-key** _key_\n> Provide xAI Grok API credentials.\n\n**-u**, **--base-url** _url_\n> Custom API endpoint (default: https://api.x.ai/v1).\n\n**-m**, **--model** _model_\n> Specify AI model (grok-4-latest, grok-3-latest, grok-3-fast, grok-3-mini-fast, grok-code-fast-1).\n\n**-p**, **--prompt** _text_\n> Execute single prompt in headless mode and exit.\n\n**--max-tool-rounds** _n_\n> Limit the number of tool execution rounds.\n\n**-h**, **--help**\n> Display help documentation."],["DESCRIPTION","**Grok CLI** is an open-source AI agent that brings xAI's Grok directly into the terminal. It provides a conversational interface for coding assistance, file operations, and shell command execution through natural language.\n\nThe tool operates in two modes: interactive mode for extended sessions with ongoing dialogue, and headless mode (**-p**) for scripted automation and one-off commands. Grok can navigate codebases, view and edit files, run bash commands, and intelligently select appropriate tools for requested tasks."],["CONFIGURATION","**~/.grok/user-settings.json**\n> User-level settings including API key, model preferences, and default behaviors.\n\n**.grok/settings.json**\n> Project-level settings for tool permissions and configuration.\n\n**.grok/GROK.md**\n> Project-level custom instructions loaded automatically."],["CAVEATS","Requires an **xAI API key** which must be obtained from the xAI API Console. The CLI uses the xAI API endpoint by default and requires Node.js version 18 or higher. API usage is subject to xAI's rate limits and billing. The Morph Fast Apply feature for high-speed code editing requires additional configuration."],["HISTORY","Grok CLI emerged as an open-source project in **2024** following xAI's public release of their Grok API. Built by the Superagent AI community, it provides terminal access to xAI's Grok models. The tool gained significant traction in **2025** as a challenger to proprietary coding assistants, with xAI announcing their official **Grok Build** coding agent in **January 2026** to provide first-party CLI support."],["INSTALL","```brew: brew install grok```"],["SEE ALSO","[grok-build](/man/grok-build)(1), [claude](/man/claude)(1), [copilot](/man/copilot)(1), [gh](/man/gh)(1)"]],"e":["grok-build","claude","copilot","gh"],"x":"grok"},"haproxy":{"n":"haproxy","t":"high-performance TCP/HTTP load balancer and proxy","s":[["TAGLINE","high-performance TCP/HTTP load balancer and proxy"],["TLDR","**Start HAProxy**\n\n```haproxy -f [/etc/haproxy/haproxy.cfg]```\n\n**Check configuration**\n\n```haproxy -c -f [/etc/haproxy/haproxy.cfg]```\n\n**Reload configuration** (soft-stop the old process once the new one is ready)\n\n```haproxy -f [config.cfg] -sf $(pidof haproxy)```\n\n**Start in daemon mode**\n\n```haproxy -D -f [config.cfg]```\n\n**Start in master-worker mode** (systemd-friendly, used by the official unit file)\n\n```haproxy -Ws -f [/etc/haproxy/haproxy.cfg] -p [/run/haproxy.pid]```\n\n**Show version**\n\n```haproxy -v```"],["SYNOPSIS","**haproxy** [_options_] **-f** _config_"],["PARAMETERS","**-f** _file_\n> Configuration file.\n\n**-c**\n> Check configuration and exit.\n\n**-D**\n> Daemon mode.\n\n**-sf** _pids_\n> Soft-stop old processes.\n\n**-st** _pids_\n> Hard-stop old processes.\n\n**-p** _pidfile_\n> PID file path.\n\n**-n** _maxconn_\n> Maximum connections.\n\n**-N** _maxconn_\n> Default per-proxy maxconn.\n\n**-d**\n> Debug mode; disables daemon mode and stays in the foreground.\n\n**-V**\n> Verbose mode (cancels -q).\n\n**-q**\n> Quiet mode; suppresses informational messages.\n\n**-v**\n> Display version and build date.\n\n**-W**\n> Master-worker mode: a master process supervises worker process(es) and reloads configuration without dropping connections.\n\n**-Ws**\n> Master-worker mode with systemd `sd_notify` support; used by the official systemd unit.\n\n**-x** _socket_\n> Retrieve listening sockets from an old process (used internally for seamless reloads).\n\n**-C** _dir_\n> Change to this directory before loading configuration files."],["DESCRIPTION","**HAProxy** (High Availability Proxy) is a high-performance TCP/HTTP load balancer and reverse proxy that distributes incoming traffic across pools of backend servers. Its configuration is organized around frontends (which accept client connections on bound addresses and ports) and backends (which define the set of servers that handle requests, along with the balancing algorithm -- round-robin, least-connections, source-hash, and others). Active health checks continuously probe backend servers and automatically remove unhealthy nodes from rotation.\n\nBeyond basic load balancing, HAProxy provides SSL/TLS termination, HTTP header manipulation, content-based routing via ACLs, connection rate limiting, stick tables for session persistence, and a real-time statistics dashboard. It operates on an event-driven, multi-threaded worker model that can handle hundreds of thousands of concurrent connections with low latency and minimal resource consumption, making it a standard choice for high-traffic production environments.\n\nModern deployments typically run it in master-worker mode (**-W**/**-Ws**), where a master process supervises one or more worker processes and can reload the configuration or reload binaries without dropping established connections."],["CONFIG EXAMPLE","```\nfrontend http_front\n    bind *:80\n    default_backend http_back\n\nbackend http_back\n    balance roundrobin\n    server web1 192.168.1.1:80 check\n    server web2 192.168.1.2:80 check\n```"],["CAVEATS","Configuration changes require reload. Complex ACLs need careful testing. Statistics require separate configuration. SSL setup needs certificate management."],["HISTORY","HAProxy was created by **Willy Tarreau** in **2000** and has become one of the most widely deployed load balancers. It powers high-traffic sites including GitHub, Reddit, and Stack Overflow."],["INSTALL","```apt: sudo apt install haproxy```\n\n```dnf: sudo dnf install haproxy```\n\n```pacman: sudo pacman -S haproxy```\n\n```apk: sudo apk add haproxy```\n\n```zypper: sudo zypper install haproxy```\n\n```brew: brew install haproxy```\n\n```nix: nix profile install nixpkgs#haproxy```"],["SEE ALSO","[nginx](/man/nginx)(1), [envoy](/man/envoy)(1), [traefik](/man/traefik)(1)"],["RESOURCES","```[Source code](https://github.com/haproxy/haproxy)```\n\n```[Homepage](https://www.haproxy.org/)```\n\n```[Documentation](https://docs.haproxy.org/)```"]],"e":["nginx","envoy","traefik"],"x":"haproxy -f [/etc/haproxy/haproxy.cfg]"},"hummingbot":{"n":"hummingbot","t":"open-source crypto trading bot framework","s":[["TAGLINE","open-source crypto trading bot framework"],["TLDR","**Start the interactive TUI**\n\n```hummingbot```\n\n**Start with a specific strategy**\n\n```hummingbot -s [strategy_name]```\n\n**Start with a specific config file**\n\n```hummingbot --config-file-name [config.yml]```\n\n**Start with a password**\n\n```hummingbot --config-password [password]```"],["SYNOPSIS","**hummingbot** [_options_]"],["PARAMETERS","**-s** _STRATEGY_, **--strategy** _STRATEGY_\n> Strategy to start with.\n\n**--config-file-name** _FILE_\n> Configuration file to use.\n\n**--config-password** _PASSWORD_\n> Password for encrypted config files.\n\n**-h**, **--help**\n> Display help."],["DESCRIPTION","**hummingbot** is an open-source cryptocurrency trading bot framework that provides an interactive terminal interface for automated trading. It supports strategies such as market making, arbitrage, and liquidity mining across centralized and decentralized exchanges.\n\nThe TUI provides real-time status updates, order tracking, and profit/loss reporting. Users configure strategies through interactive prompts or YAML configuration files. The bot supports paper trading mode for testing strategies without risking funds."],["CAVEATS","Requires API keys from supported exchanges. Trading involves financial risk. Requires Python 3.8+ and various dependencies. Available via Docker or source installation."],["SEE ALSO","[freqtrade](/man/freqtrade)(1), [gocryptotrader](/man/gocryptotrader)(1), [cointop](/man/cointop)(1)"]],"e":["freqtrade","gocryptotrader","cointop"],"x":"hummingbot"},"infection":{"n":"infection","t":"PHP mutation testing framework","s":[["TAGLINE","PHP mutation testing framework"],["TLDR","**Run mutation testing**\n\n```infection```\n\n**Run with specific threads**\n\n```infection --threads=[4]```\n\n**Target specific files**\n\n```infection --filter=[src/Service/]```\n\n**Set minimum score**\n\n```infection --min-msi=[70]```\n\n**Show mutations**\n\n```infection --show-mutations```\n\n**Output to file**\n\n```infection --log-verbosity=all --logger-text=[infection.log]```"],["SYNOPSIS","**infection** [_options_]"],["PARAMETERS","**--threads** _N_\n> Parallel execution threads.\n\n**--filter** _PATH_\n> Filter source files to mutate.\n\n**--min-msi** _N_\n> Minimum Mutation Score Indicator.\n\n**--min-covered-msi** _N_\n> Minimum covered code MSI.\n\n**--show-mutations**\n> Display mutation details.\n\n**--mutators** _LIST_\n> Specific mutators to use.\n\n**--help**\n> Display help information."],["DESCRIPTION","**infection** is a PHP mutation testing framework. It modifies code to test the effectiveness of your test suite.\n\nThe tool creates mutants (modified code) and runs tests against them. Surviving mutants indicate weak test coverage."],["CAVEATS","PHP-only. Requires PHPUnit or Codeception. Resource intensive on large codebases."],["HISTORY","infection was created as a mutation testing tool for **PHP**, inspired by similar tools in other languages like Stryker."],["SEE ALSO","[phpunit](/man/phpunit)(1), [phpspec](/man/phpspec)(1), [pest](/man/pest)(1)"]],"e":["phpunit","phpspec","pest"],"x":"infection"},"intro":{"n":"intro","t":"Introduction to user commands (man page section 1)","s":[["TAGLINE","Introduction to user commands (man page section 1)"],["TLDR","**Read the introduction to user commands**\n\n```man intro```\n\n**Read the introduction for a specific section**\n\n```man [1] intro```\n\n**Search for all intro pages across sections**\n\n```man -k intro```"],["SYNOPSIS","**intro** - introduction to user commands"],["DESCRIPTION","**intro** is an introductory man page present in each section of the manual. Section 1 describes user commands and tools, including file manipulation tools, shells, compilers, web browsers, editors, and other programs that users commonly invoke from the command line.\n\nEach manual section has its own intro page providing an overview of the section's scope:\n\n- **1** - User commands and programs\n- **2** - System calls (kernel interfaces)\n- **3** - Library functions (C library routines)\n- **4** - Special files (devices in /dev)\n- **5** - File formats and conventions\n- **6** - Games and screensavers\n- **7** - Miscellaneous (protocols, conventions, character sets)\n- **8** - System administration commands"],["CAVEATS","Read-only documentation. Content varies by distribution. Part of the **man-pages** package on Linux."],["HISTORY","intro pages have been part of **Unix documentation** since the earliest versions, providing orientation for each manual section. The convention of numbered sections dates back to the original **Unix Programmer's Manual** from **1971**."],["SEE ALSO","[man](/man/man)(1), [apropos](/man/apropos)(1), [whatis](/man/whatis)(1), [manpath](/man/manpath)(1)"]],"e":["man","apropos","whatis","manpath"],"x":"man intro"},"jest":{"n":"jest","t":"JavaScript testing framework with focus on simplicity","s":[["TAGLINE","JavaScript testing framework with focus on simplicity"],["TLDR","**Run all tests**\n\n```jest```\n\n**Run specific test file**\n\n```jest [path/to/test.js]```\n\n**Run tests matching pattern**\n\n```jest -t \"[pattern]\"```\n\n**Run in watch mode**\n\n```jest --watch```\n\n**Generate coverage report**\n\n```jest --coverage```\n\n**Update snapshots**\n\n```jest -u```\n\n**Run tests in parallel**\n\n```jest --maxWorkers=[4]```\n\n**Run only changed tests**\n\n```jest --onlyChanged```"],["SYNOPSIS","**jest** [_options_] [_files_...]"],["DESCRIPTION","**jest** is a JavaScript testing framework with focus on simplicity. It provides test runners, assertions, mocking, and code coverage in a single package.\n\nThe tool features snapshot testing, parallel execution, and intelligent test selection. It works with React, Vue, Node.js, and most JavaScript projects."],["PARAMETERS","**--watch**\n> Watch mode for changed files.\n\n**--watchAll**\n> Watch all files.\n\n**--coverage**\n> Collect code coverage.\n\n**-t** _pattern_\n> Run matching test names.\n\n**-u**, **--updateSnapshot**\n> Update snapshots.\n\n**--maxWorkers** _n_\n> Max parallel workers.\n\n**--onlyChanged**\n> Run only changed tests.\n\n**--bail**\n> Stop on first failure.\n\n**--verbose**\n> Display individual test results.\n\n**--config** _file_\n> Configuration file.\n\n**--ci**\n> CI mode settings."],["CAVEATS","Default test regex may miss files. Snapshots need review. Coverage may miss edge cases. Memory usage high with many tests."],["HISTORY","**Jest** was created by **Facebook** (Meta), initially for testing React applications. Released around **2014**, it evolved from Jasmine roots to become one of the most popular JavaScript testing frameworks, known for its zero-configuration approach."],["SEE ALSO","[npm](/man/npm)(1), [mocha](/man/mocha)(1), [vitest](/man/vitest)(1), [playwright](/man/playwright)(1)"]],"e":["npm","mocha","vitest","playwright"],"x":"jest"},"jhat":{"n":"jhat","t":"Java Heap Analysis Tool — parses HPROF heap dumps and serves them over HTTP","s":[["TAGLINE","Java Heap Analysis Tool — parses HPROF heap dumps and serves them over HTTP"],["TLDR","**Analyze a heap dump** (serves on http://localhost:7000)\n\n```jhat [dump.hprof]```\n\n**Use a custom HTTP port**\n\n```jhat -port [8080] [dump.hprof]```\n\n**Run with more heap** for the analyzer itself\n\n```jhat -J-Xmx8g [dump.hprof]```\n\n**Compare against a baseline dump**\n\n```jhat -baseline [old.hprof] [new.hprof]```\n\n**Disable object reference tracking** (faster, less memory)\n\n```jhat -stack false -refs false [dump.hprof]```"],["SYNOPSIS","**jhat** [_options_] _heap-dump-file_"],["PARAMETERS","**-stack** _true|false_\n> Track object allocation call stacks (default `true`). Disable to speed up load of large dumps.\n\n**-refs** _true|false_\n> Track object references (default `true`). Disable to reduce memory use; most reference queries will be unavailable.\n\n**-port** _port_\n> TCP port for the HTTP server (default 7000).\n\n**-exclude** _file_\n> Path to a file listing data members to exclude from reachability queries.\n\n**-baseline** _file_\n> Specify a baseline dump. Objects present in both dumps are marked \"not new\".\n\n**-debug** _int_\n> Debug level. `0` = off, `1` = parse HPROF, `2` = parse without starting server.\n\n**-version**\n> Print version and exit.\n\n**-J**_flag_\n> Pass _flag_ straight to the JVM running jhat (e.g. `-J-Xmx8g`).\n\n**-help**\n> Show help."],["DESCRIPTION","**jhat** parses a Java heap dump file in HPROF binary format and launches a small HTTP server for browsing the heap. The web UI exposes pre-built queries (instances of a class, reference chains, reachable objects, histograms) and an Object Query Language (OQL) prompt for arbitrary queries over the heap.\n\nHeap dumps can be produced by `jmap -dump`, `jcmd <pid> GC.heap_dump`, `jconsole`, or via `-XX:+HeapDumpOnOutOfMemoryError` on a crashing JVM."],["CAVEATS","**jhat was deprecated in JDK 8 (JEP 241) and removed in JDK 9.** On modern JDKs, use **jcmd**, **jmap -histo**, **VisualVM**, **Eclipse MAT**, or **JProfiler** instead. The HTTP server binds to all interfaces by default — use firewall rules or SSH tunneling to avoid exposing heap contents on a network."],["HISTORY","**jhat** was introduced in **Java 6** as a replacement for the older `hat` tool. It was deprecated in Java 8 and removed in Java 9 in favor of more capable third-party analyzers."],["SEE ALSO","[jmap](/man/jmap)(1), [jstack](/man/jstack)(1), [jps](/man/jps)(1)"]],"e":["jmap","jstack","jps"],"x":"jhat [dump.hprof]"},"jiq":{"n":"jiq","t":"Interactive JSON query tool using jq expressions","s":[["TAGLINE","Interactive JSON query tool using jq expressions"],["TLDR","**Query JSON from stdin**\n\n```echo '[1, 2, 3]' | jiq```\n\n**Query a JSON file**\n\n```cat [path/to/file.json] | jiq```\n\n**Query from a curl response**\n\n```curl -s [https://api.example.com/data] | jiq```"],["SYNOPSIS","**jiq** < _json_input_"],["DESCRIPTION","**jiq** is an interactive JSON query tool that uses **jq** filter expressions. It reads JSON from stdin and provides a terminal interface where you can type jq queries and see the results update in real time as you type. This makes it easy to explore and drill down into complex JSON structures incrementally.\n\njiq requires **jq** to be installed and available in your PATH, as it delegates query execution to jq."],["CAVEATS","Requires **jq** to be installed separately. Only reads from stdin; cannot open files directly. The entire JSON input is loaded into memory."],["HISTORY","**jiq** was created by **fiatjaf** and is written in **Go**. It was inspired by **jid** (JSON incremental digger) but uses jq's query syntax instead of a custom one."],["INSTALL","```nix: nix profile install nixpkgs#jiq```"],["SEE ALSO","[jq](/man/jq)(1), [fx](/man/fx)(1), [jaq](/man/jaq)(1)"]],"e":["jq","fx","jaq"],"x":"echo '[1, 2, 3]' | jiq"},"jql":{"n":"jql","t":"JSON query language tool written in Rust","s":[["TAGLINE","JSON query language tool written in Rust"],["TLDR","**Query a top-level field**\n\n```cat [data.json] | jql '\"name\"'```\n\n**Extract nested field**\n\n```cat [data.json] | jql '\"users\".[0].\"name\"'```\n\n**Select multiple fields**\n\n```cat [data.json] | jql '\"name\" \"age\"'```\n\n**Raw string output** (no JSON quotes)\n\n```cat [data.json] | jql -r '\"name\"'```\n\n**Read from file** instead of stdin\n\n```jql '\"name\"' [data.json]```"],["SYNOPSIS","**jql** [_options_] _selectors_"],["PARAMETERS","_SELECTORS_\n> JQL query expressions (keys must be double-quoted).\n\n**-r**, **--raw-output**\n> Output raw strings without JSON double-quotes.\n\n**-s**, **--stream**\n> Read a stream of JSON lines (one JSON object per line).\n\n**-h**, **--help**\n> Display help information.\n\n**-V**, **--version**\n> Display version information."],["DESCRIPTION","**jql** is a JSON query language CLI tool written in Rust. It provides a simple syntax for extracting and manipulating JSON data. Key selectors must be double-quoted to be JSON-compliant.\n\nThe tool focuses on simplicity over jq's complexity. It supports dot notation for nesting, array indexing with ranges, flattening with **..**, and multiple selectors in a single query. Input is read from stdin or a file argument."],["CAVEATS","Syntax is different from jq and not interchangeable. Key selectors must always be double-quoted. The tool consumes JSON input and outputs JSON back; use **-r** for raw strings."],["HISTORY","jql was created as a simpler alternative to jq with a more intuitive query syntax for common JSON operations."],["INSTALL","```dnf: sudo dnf install jql```\n\n```apk: sudo apk add jql```\n\n```brew: brew install jql```\n\n```nix: nix profile install nixpkgs#jql```"],["SEE ALSO","[jq](/man/jq)(1), [gron](/man/gron)(1), [fx](/man/fx)(1)"]],"e":["jq","gron","fx"],"x":"cat [data.json] | jql '\"name\"'"},"kanban-tui":{"n":"kanban-tui","t":"Terminal-based Kanban board task manager","s":[["TAGLINE","Terminal-based Kanban board task manager"],["TLDR","**Launch the Kanban board**\n\n```kanban-tui```\n\n**Open a specific board file**\n\n```kanban-tui [path/to/kanban.md]```"],["SYNOPSIS","**kanban-tui** [_options_] [_file_]"],["DESCRIPTION","**kanban-tui** is a terminal-based Kanban board for managing tasks and projects. It provides a TUI with columns for different task states (To Do, In Progress, Done) and supports creating, editing, moving, and deleting cards with Vim-like key bindings.\n\nThe tool uses a file-based structure for data storage, supports nested and linked boards (cards that link to other kanban files), and meta-boards that aggregate multiple project boards into a single master view."],["CAVEATS","File format is specific to the tool. Multiple simultaneous editors on the same file may cause conflicts."],["HISTORY","**kanban-tui** was created by **sokinpui** and is written in **Go**. Several alternative Kanban TUI implementations exist in the ecosystem, including Python-based variants using the Textual framework."],["SEE ALSO","[taskwarrior](/man/taskwarrior)(1)"]],"e":["taskwarrior"],"x":"kanban-tui"},"kdialog":{"n":"kdialog","t":"displays KDE dialog boxes from shell scripts, enabling graphical user","s":[["TAGLINE","displays KDE dialog boxes from shell scripts, enabling graphical user"],["TLDR","Display **message box**\n\n```kdialog --msgbox \"message\"```\n\nShow **yes/no** question dialog\n\n```kdialog --yesno \"message\"```\n\nShow **warning** with yes/no/cancel\n\n```kdialog --warningyesnocancel \"message\"```\n\nShow **input** dialog\n\n```kdialog --inputbox \"message\" \"default\"```\n\nShow **password** input dialog\n\n```kdialog --password \"message\"```\n\nShow **combo box** dropdown\n\n```kdialog --combobox \"message\" \"item1\" \"item2\" \"item3\"```\n\nOpen **file chooser** dialog\n\n```kdialog --getopenfilename```\n\nShow **progress bar** dialog\n\n```kdialog --progressbar \"message\"```"],["SYNOPSIS","**kdialog** [_options_] [_arguments_]"],["DESCRIPTION","**kdialog** displays KDE dialog boxes from shell scripts, enabling graphical user interaction in command-line scripts. It provides various dialog types including message boxes, input dialogs, file choosers, and progress bars."],["PARAMETERS","**--msgbox TEXT**\n> Display a message box with OK button\n\n**--yesno TEXT**\n> Display yes/no dialog; returns 0 for yes, 1 for no\n\n**--yesnocancel TEXT**\n> Display yes/no/cancel dialog; returns 0, 1, or 2\n\n**--warningyesno TEXT**\n> Display warning with yes/no buttons\n\n**--warningyesnocancel TEXT**\n> Display warning with yes/no/cancel buttons\n\n**--inputbox TEXT [DEFAULT]**\n> Display input dialog; prints input to stdout\n\n**--password TEXT**\n> Display password input dialog\n\n**--combobox TEXT ITEM...**\n> Display dropdown selection dialog\n\n**--getopenfilename [DIR] [FILTER]**\n> Display file open dialog\n\n**--getsavefilename [DIR] [FILTER]**\n> Display file save dialog\n\n**--getexistingdirectory [DIR]**\n> Display directory selection dialog\n\n**--progressbar TEXT [TOTAL]**\n> Display progress bar; returns D-Bus reference\n\n**--title TEXT**\n> Set dialog window title\n\n**--icon ICON**\n> Set dialog icon"],["CAVEATS","kdialog requires a running KDE/Qt environment. Exit codes vary by dialog type; check return values for user selections. Progress bars return D-Bus references for updating progress from scripts."],["HISTORY","**kdialog** is part of the KDE base applications, providing script access to KDE's native dialog system. It is the KDE equivalent of zenity for GTK environments."],["INSTALL","```apt: sudo apt install kdialog```\n\n```dnf: sudo dnf install kdialog```\n\n```pacman: sudo pacman -S kdialog```\n\n```apk: sudo apk add kdialog```\n\n```zypper: sudo zypper install kdialog```"],["SEE ALSO","[zenity](/man/zenity)(1), [dialog](/man/dialog)(1), [whiptail](/man/whiptail)(1)"]],"e":["zenity","dialog","whiptail"],"x":"kdialog --msgbox \"message\""},"last":{"n":"last","t":"searches back through the /var/log/wtmp file and displays a list of all users","s":[["TAGLINE","searches back through the /var/log/wtmp file and displays a list of all users"],["TLDR","View **all login** history\n\n```last```\n\nView logins for a **specific user**\n\n```last username```\n\nShow **last 10** logins\n\n```last -n 10```\n\nShow logins with **full timestamps**\n\n```last -F```\n\nShow logins with **IP addresses** instead of hostnames\n\n```last -i```\n\nShow logins **since** a specific date\n\n```last -s -7days```\n\nView **system reboots**\n\n```last reboot```\n\nView **failed logins** (requires root)\n\n```lastb```"],["SYNOPSIS","**last** [_options_] [_username_...] [_tty_...]"],["DESCRIPTION","**last** searches back through the /var/log/wtmp file and displays a list of all users logged in and out since that file was created. The related **lastb** command shows failed login attempts from /var/log/btmp."],["PARAMETERS","**-a, --hostlast**\n> Display the hostname in the last column\n\n**-d, --dns**\n> Translate IP addresses to hostnames for remote logins\n\n**-f, --file FILE**\n> Use a different file instead of /var/log/wtmp\n\n**-F, --fulltimes**\n> Print full login and logout times and dates\n\n**-i, --ip**\n> Display IP addresses instead of hostnames\n\n**-n, --limit NUM**\n> Show only the specified number of lines\n\n**-R, --nohostname**\n> Do not display the hostname field\n\n**-s, --since TIME**\n> Display logins since the specified time\n\n**-t, --until TIME**\n> Display logins until the specified time\n\n**-w, --fullnames**\n> Display full user and domain names\n\n**-x, --system**\n> Display system shutdown entries and runlevel changes\n\n**-p, --present TIME**\n> Display who was present at the specified time\n\n**--time-format FORMAT**\n> Define output time format (notime, short, full, iso)"],["CAVEATS","The pseudo-user \"reboot\" logs entries each time the system is restarted, allowing boot history tracking. The wtmp and btmp files must exist; administrators may need to create them using **touch**."],["HISTORY","**last** has been a standard Unix utility since the early BSD systems, providing user login accounting functionality."],["INSTALL","```apt: sudo apt install wtmpdb```\n\n```apk: sudo apk add util-linux-login```\n\n```zypper: sudo zypper install wtmpdb```\n\n```nix: nix profile install nixpkgs#last```"],["SEE ALSO","[lastb](/man/lastb)(1), [who](/man/who)(1), [w](/man/w)(1)"]],"e":["lastb","who","w"],"x":"last"},"lb":{"n":"lb","t":"creates Debian live system images","s":[["TAGLINE","creates Debian live system images"],["TLDR","**Initialize live build**\n\n```lb config```\n\n**Build live image**\n\n```lb build```\n\n**Clean build directory**\n\n```lb clean```\n\n**Configure with options**\n\n```lb config --distribution [bookworm] --archive-areas \"main contrib\"```\n\n**Build a specific image type**\n\n```lb config --binary-images [iso-hybrid]```\n\n**Clean including cache**\n\n```lb clean --cache```"],["SYNOPSIS","**lb** _command_ [_options_]"],["PARAMETERS","**config**\n> Initialize configuration.\n\n**build**\n> Build live image.\n\n**clean**\n> Clean build artifacts.\n\n**--distribution** _NAME_\n> Target distribution.\n\n**--archive-areas** _AREAS_\n> Repository areas (e.g. \"main contrib non-free\").\n\n**--binary-images** _TYPE_\n> Image type: iso, iso-hybrid, netboot, tar, hdd.\n\n**--architecture** _ARCH_\n> Target architecture (defaults to host architecture).\n\n**--bootappend-live** _PARAMS_\n> Boot parameters for live system.\n\n**--cache**\n> Clean cache directories (not cleaned by default).\n\n**--force**\n> Force helper execution, even if stage file exists.\n\n**--quiet**\n> Suppress output messages.\n\n**--verbose**\n> Show detailed output.\n\n**--help**\n> Display help information."],["DESCRIPTION","**lb** (live-build) creates Debian live system images. It builds bootable ISO, netboot, and HDD images.\n\nThe tool supports customization of packages, configs, and hooks. It's the standard tool for Debian live images. By default, `lb clean` removes chroot, binary, and source stages but preserves the cache."],["CAVEATS","Debian/Ubuntu specific. Root required. Disk space needed."],["HISTORY","live-build was created by the **Debian Live** project for building customizable live system images."],["SEE ALSO","[debootstrap](/man/debootstrap)(1), [mksquashfs](/man/mksquashfs)(1)"]],"e":["debootstrap","mksquashfs"],"x":"lb config"},"live-server":{"n":"live-server","t":"development server with live reload","s":[["TAGLINE","development server with live reload"],["TLDR","**Start server in current directory**\n\n```live-server```\n\n**Serve specific directory**\n\n```live-server [public/]```\n\n**Custom port**\n\n```live-server --port=[8080]```\n\n**Open specific file**\n\n```live-server --open=[index.html]```\n\n**Watch specific paths**\n\n```live-server --watch=[src/,public/]```\n\n**Disable browser open**\n\n```live-server --no-browser```\n\n**Serve SPA** (fallback to index.html)\n\n```live-server --entry-file=[index.html]```\n\n**Enable HTTPS and CORS**\n\n```live-server --https=[https-config.js] --cors```"],["SYNOPSIS","**live-server** [_options_] [_directory_]"],["PARAMETERS","_DIRECTORY_\n> Directory to serve (defaults to current).\n\n**--port=**_PORT_\n> Server port (default: 8080, or PORT env var).\n\n**--host=**_HOST_\n> Host address to bind (default: 0.0.0.0, or IP env var).\n\n**--open=**_PATH_\n> Path to open in browser on start.\n\n**--watch=**_PATHS_\n> Comma-separated paths to watch for changes.\n\n**--ignore=**_PATHS_\n> Comma-separated paths to exclude from watching.\n\n**--no-browser**\n> Don't auto-launch browser.\n\n**--browser=**_BROWSER_\n> Specify which browser to open.\n\n**--no-css-inject**\n> Reload full page on CSS changes instead of live-injecting.\n\n**--entry-file=**_PATH_\n> Fallback file for missing requests (useful for SPAs).\n\n**--mount=**_ROUTE:PATH_\n> Map a URL route to a directory.\n\n**--proxy=**_ROUTE:URL_\n> Proxy requests matching route to URL.\n\n**--https=**_PATH_\n> Path to HTTPS configuration module.\n\n**--cors**\n> Enable CORS for all requests.\n\n**--wait=**_MS_\n> Delay (ms) before reloading (default: 100).\n\n**-q**, **--quiet**\n> Suppress log output.\n\n**-V**, **--verbose**\n> Verbose logging.\n\n**-v**, **--version**\n> Show version.\n\n**-h**, **--help**\n> Display help information."],["DESCRIPTION","**live-server** is a lightweight Node.js-based HTTP server designed for front-end development that automatically reloads the browser whenever files in the served directory change. It works by injecting a small script into HTML pages that opens a WebSocket connection back to the server, which triggers a page refresh when filesystem modifications are detected.\n\nThe server requires no configuration to get started and will serve the current directory by default, automatically opening your browser to the root page. You can customize the port, host binding, watched directories, and the initial page to open. Because it is intended strictly for development, it should not be used in production environments."],["CAVEATS","Node.js required. Development use only. Not for production."],["HISTORY","live-server was created to simplify front-end development with automatic browser refresh on file changes."],["INSTALL","```nix: nix profile install nixpkgs#live-server```"],["SEE ALSO","[http-server](/man/http-server)(1), [serve](/man/serve)(1), [browser-sync](/man/browser-sync)(1), [nodemon](/man/nodemon)(1), [python](/man/python)(1)"]],"e":["http-server","serve","browser-sync","nodemon","python"],"x":"live-server"},"lvs":{"n":"lvs","t":"Display information about logical volumes","s":[["TAGLINE","Display information about logical volumes"],["TLDR","**List** logical volumes\n\n```sudo lvs```\n\n**List all** volumes including internal ones\n\n```sudo lvs -a```\n\n**Verbose** output\n\n```sudo lvs -v```\n\nShow **specific** fields\n\n```sudo lvs -o lv_name,vg_name,lv_size```\n\n**Append** a field to the default output\n\n```sudo lvs -o +lv_path```\n\n**Show available fields**\n\n```sudo lvs -o help```\n\n**No headings** with custom separator for scripting\n\n```sudo lvs --noheadings --separator '|'```\n\n**Filter** logical volumes by selection criteria\n\n```sudo lvs -S 'lv_size>1g'```\n\n**JSON output**\n\n```sudo lvs --reportformat json```"],["SYNOPSIS","**lvs** [_OPTIONS_] [_VolumeGroupName_|_LogicalVolume_...]"],["DESCRIPTION","**lvs** displays information about logical volumes in a configurable table format. It provides a quick overview of LV names, sizes, attributes, and related volume groups."],["PARAMETERS","**-a**, **--all**\n> Show all logical volumes including internal ones.\n\n**-v**, **--verbose**\n> Increase verbosity and show additional details. Can be repeated up to 4 times.\n\n**-o**, **--options** _fields_\n> Specify which fields to display. Prefix with `+` to append, `-` to remove, `#` to compact empty fields. Use `-o help` to list available fields.\n\n**-S**, **--select** _criteria_\n> Filter output rows matching selection criteria.\n\n**-O**, **--sort** _fields_\n> Sort output by specified fields. Prefix with `-` for reverse order.\n\n**--noheadings**\n> Suppress the heading line in output.\n\n**--nosuffix**\n> Suppress size unit suffixes in output.\n\n**--separator** _char_\n> Use specified character as field separator.\n\n**--aligned**\n> Use with --separator to align output columns.\n\n**--nameprefixes**\n> Add \"LVM2_\" prefix to field names, producing field=value pairs.\n\n**--segments**\n> Show segment information in default columns.\n\n**--rows**\n> Display columns as rows.\n\n**--units** _units_\n> Display sizes in specified units (b, s, k, m, g, t, p, e, h, H).\n\n**--reportformat** _format_\n> Output format: `basic` or `json`."],["CAVEATS","Some fields require verbose mode to display. Default output is designed for human readability; use `--separator` and `--noheadings` for scripting. Requires root privileges or membership in the appropriate group."],["HISTORY","**lvs** is part of **LVM2**, the Linux Logical Volume Manager, providing quick volume listing functionality."],["INSTALL","```apt: sudo apt install lvm2```\n\n```dnf: sudo dnf install lvm2```\n\n```pacman: sudo pacman -S lvm2```\n\n```apk: sudo apk add lvm2```\n\n```zypper: sudo zypper install lvm2```\n\n```nix: nix profile install nixpkgs#lvm2```"],["SEE ALSO","[lvm](/man/lvm)(8), [lvdisplay](/man/lvdisplay)(8), [lvcreate](/man/lvcreate)(8), [lvrename](/man/lvrename)(8), [pvs](/man/pvs)(8), [vgs](/man/vgs)(8)"]],"e":["lvm","lvdisplay","lvcreate","lvrename","pvs","vgs"],"x":"sudo lvs"},"m4":{"n":"m4","t":"macro processor","s":[["TAGLINE","macro processor"],["TLDR","**Process file**\n\n```m4 [input.m4]```\n\n**Output to file**\n\n```m4 [input.m4] > [output.txt]```\n\n**Define macro**\n\n```m4 -D [NAME=value] [input.m4]```\n\n**Include directory**\n\n```m4 -I [/path/to/includes] [input.m4]```\n\n**Prefix builtins**\n\n```m4 -P [input.m4]```\n\n**Debug output**\n\n```m4 -d [input.m4]```"],["SYNOPSIS","**m4** [_options_] [_files_]"],["PARAMETERS","_FILES_\n> Input files to process.\n\n**-D** _NAME=VALUE_\n> Define macro.\n\n**-U** _NAME_\n> Undefine macro.\n\n**-I** _DIR_\n> Add include path.\n\n**-P**\n> Prefix builtins with m4_.\n\n**-d**\n> Enable debug output.\n\n**--help**\n> Display help information."],["DESCRIPTION","**m4** is a macro processor. It expands macros in text files to generate output.\n\nThe tool is used to preprocess configuration files and source code. It's part of autoconf."],["CAVEATS","POSIX standard. Quoting rules can be tricky. Used by autoconf extensively."],["HISTORY","m4 was developed at **Bell Labs** in the 1970s. The GNU version extends the original POSIX specification."],["INSTALL","```apt: sudo apt install m4```\n\n```dnf: sudo dnf install m4```\n\n```pacman: sudo pacman -S m4```\n\n```apk: sudo apk add m4```\n\n```zypper: sudo zypper install m4```\n\n```brew: brew install m4```\n\n```nix: nix profile install nixpkgs#m4```"],["SEE ALSO","[autoconf](/man/autoconf)(1), [make](/man/make)(1)"]],"e":["autoconf","make"],"x":"m4 [input.m4]"},"ncrack":{"n":"ncrack","t":"high-speed network authentication cracking tool","s":[["TAGLINE","high-speed network authentication cracking tool"],["TLDR","**Crack SSH login**\n\n```ncrack -p [22] --user [root] -P [wordlist.txt] [192.168.1.1]```\n\n**Crack multiple services**\n\n```ncrack [ssh://192.168.1.1,ftp://192.168.1.2]```\n\n**Use username list**\n\n```ncrack -U [users.txt] -P [passwords.txt] [ssh://target]```\n\n**Set timing template**\n\n```ncrack -T[4] [ssh://target]```\n\n**Read targets from file**\n\n```ncrack -iL [hosts.txt] -p [22] --user [admin]```\n\n**Save results to file**\n\n```ncrack -oN [results.txt] [ssh://target] -U [users.txt] -P [passwords.txt]```"],["SYNOPSIS","**ncrack** [_options_] [_target_...]"],["PARAMETERS","**-p** _ports_\n> Target ports.\n\n**--user** _user_\n> Single username.\n\n**-U** _file_\n> Username list.\n\n**-P** _file_\n> Password list.\n\n**-T** _0-5_\n> Timing template.\n\n**-iL** _file_\n> Input target list.\n\n**-oN** _file_\n> Normal output.\n\n**-oX** _file_\n> XML output.\n\n**-v**\n> Verbose."],["DESCRIPTION","**Ncrack** is a high-speed network authentication cracking tool. It supports multiple protocols including SSH, RDP, FTP, Telnet, HTTP, and more.\n\nDesigned by Nmap developers, ncrack uses a modular architecture allowing new protocol modules."],["SUPPORTED PROTOCOLS","```\nssh, rdp, ftp, telnet\nhttp, https, pop3, imap\nsmb, vnc, sip, redis\nmongodb, cassandra, mssql\n```"],["CAVEATS","Use only with authorization. May trigger security alerts. Account lockouts possible. Rate limiting recommended."],["HISTORY","Ncrack was developed by the **Nmap Project** team, initially released in **2009** as a network authentication cracking tool complementing Nmap."],["INSTALL","```apt: sudo apt install ncrack```\n\n```dnf: sudo dnf install ncrack```\n\n```pacman: sudo pacman -S ncrack```\n\n```brew: brew install ncrack```\n\n```nix: nix profile install nixpkgs#ncrack```"],["SEE ALSO","[hydra](/man/hydra)(1), [medusa](/man/medusa)(1), [nmap](/man/nmap)(1), [john](/man/john)(1)"]],"e":["hydra","medusa","nmap","john"],"x":"ncrack -p [22] --user [root] -P [wordlist.txt] [192.168.1.1]"},"nth":{"n":"nth","t":"extracts specific fields from input","s":[["TAGLINE","extracts specific fields from input"],["TLDR","**Extract nth field**\n\n```echo \"[a b c]\" | nth [2]```\n\n**Extract from CSV**\n\n```nth -d \",\" [1] < [file.csv]```\n\n**Extract multiple fields**\n\n```nth [1] [3] [5] < [file.txt]```\n\n**Use custom delimiter**\n\n```nth -d \":\" [1] < [/etc/passwd]```"],["SYNOPSIS","**nth** [_options_] _fields_ [_file_]"],["PARAMETERS","_FIELDS_\n> Field numbers to extract (1-indexed).\n\n**-d** _DELIMITER_\n> Field delimiter (default: whitespace).\n\n**-0**\n> Use null as output separator.\n\n**--help**\n> Display help information."],["DESCRIPTION","**nth** extracts specific fields from input. Simple alternative to awk/cut.\n\nThe tool handles field extraction efficiently. Cleaner syntax than awk."],["CAVEATS","Field numbering starts at 1. Simpler than awk but less powerful."],["HISTORY","nth provides **simple field extraction** as an alternative to awk and cut."],["INSTALL","```nix: nix profile install nixpkgs#nth```"],["SEE ALSO","[cut](/man/cut)(1), [awk](/man/awk)(1), [paste](/man/paste)(1)"]],"e":["cut","awk","paste"],"x":"echo \"[a b c]\" | nth [2]"},"nudoku":{"n":"nudoku","t":"ncurses based sudoku game","s":[["TAGLINE","ncurses based sudoku game"],["TLDR","**Start a new game with default difficulty**\n\n```nudoku```\n\n**Start a game with a specific difficulty**\n\n```nudoku -d [easy|normal|hard]```\n\n**Start a game in black and white mode**\n\n```nudoku -c```\n\n**Load a user-provided puzzle in stream format (dots for empty cells)**\n\n```nudoku -s [..5.3..8.1...6..2.4....7...]```\n\n**Export a puzzle to a PDF file**\n\n```nudoku -p [output.pdf]```\n\n**Export multiple puzzles to a PDF file**\n\n```nudoku -p [output.pdf] -n [10]```"],["SYNOPSIS","**nudoku** [_options_]"],["PARAMETERS","**-d** _DIFFICULTY_\n> Set difficulty: easy, normal, or hard. Default is normal.\n\n**-c**\n> No color mode (black and white). Useful for terminals without color support.\n\n**-s** _STREAM_\n> Provide a user-supplied puzzle in stream format. Use dots (.) for empty fields, digits for filled cells, all in one line (81 characters).\n\n**-p** _FILE_\n> Output a puzzle to a PDF file.\n\n**-n** _COUNT_\n> Number of puzzles to put in the PDF. Only used with **-p**.\n\n**-i** _FILE_\n> Output a single puzzle to a PNG image file.\n\n**-h**\n> Display help information.\n\n**-v**\n> Display version information."],["PREVIEW","```\n 5 3 . | . 7 . | . . .\n 6 . . | 1 9 5 | . . .\n . 9 8 | . . . | . 6 .\n-------+-------+------\n 8 . . | . 6 . | . . 3\n 4 . . | 8 . 3 | . . 1\n 7 . . | . 2 . | . . 6\n```"],["DESCRIPTION","**nudoku** is a terminal-based Sudoku game using ncurses for display. It generates puzzles at three difficulty levels and provides a keyboard-driven interface for solving them.\n\nArrow keys navigate the grid, and number keys fill in cells. The game can also export puzzles to PDF or PNG files for printing or sharing."],["CAVEATS","Requires ncurses. Terminal must support at least 80 columns. PDF and PNG export require the respective compile-time features to be enabled."],["HISTORY","**nudoku** was created by **Michael Jubalh** as a lightweight terminal Sudoku game. The project is hosted on GitHub and packaged for most Linux distributions."],["INSTALL","```dnf: sudo dnf install nudoku```\n\n```zypper: sudo zypper install nudoku```\n\n```brew: brew install nudoku```\n\n```nix: nix profile install nixpkgs#nudoku```"],["SEE ALSO","[moon-buggy](/man/moon-buggy)(1), [nsnake](/man/nsnake)(1), [bastet](/man/bastet)(1), [tetris-bsd](/man/tetris-bsd)(1)"]],"e":["moon-buggy","nsnake","bastet","tetris-bsd"],"x":"nudoku"},"pamsplit":{"n":"pamsplit","t":"splits a multi-image PAM/PNM stream into separate files","s":[["TAGLINE","splits a multi-image PAM/PNM stream into separate files"],["TLDR","**Split** a multi-image PAM/PNM stream into numbered files\n\n```pamsplit [input.pam] \"[image%d.pam]\"```\n\n**Pad** the sequence number to a fixed width\n\n```pamsplit -padname [4] [input.pam] \"[image%d.pam]\"```\n\n**Use default filenames** (image0.pam, image1.pam, ...)\n\n```pamsplit [input.pam]```\n\n**Read from stdin** while writing numbered outputs\n\n```cat [stream.pnm] | pamsplit - \"[frame%d.pnm]\"```"],["SYNOPSIS","**pamsplit** [**-padname** _digits_] [_inputfile_] [_outputpattern_]"],["PARAMETERS","_inputfile_\n> Multi-image PAM/PNM input file. Use **-** or omit to read from stdin.\n\n_outputpattern_\n> Output filename pattern containing a single **%d** which is replaced with the (zero-based) image index. Defaults to **image%d**.\n\n**-padname** _digits_\n> Pad the sequence number with leading zeros to at least _digits_ characters (so **-padname 3** produces image000, image001, ...). Useful for ensuring lexicographic ordering of output files."],["DESCRIPTION","**pamsplit** reads a Netpbm stream that contains multiple concatenated PAM, PNM, PBM, PGM, or PPM images and writes each image to a separate file. The output filenames are generated from a printf-style pattern that includes a **%d** for the image index.\n\nThe tool is the inverse of **pnmcat** / **pamcat** and is part of the **Netpbm** package."],["CAVEATS","The output pattern must contain exactly one **%d** specifier. Existing files at the generated paths are silently overwritten. For PBM/PGM/PPM streams without alpha channels, **pamsplit** preserves the original format of each frame."],["HISTORY","**pamsplit** is part of the **Netpbm** package by Bryan Henderson and others, and supersedes the older **pnmsplit** utility from PBMplus."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pamundice](/man/pamundice)(1), [pnmcat](/man/pnmcat)(1), [netpbm](/man/netpbm)(1)"]],"e":["pamundice","pnmcat","netpbm"],"x":"pamsplit [input.pam] \"[image%d.pam]\""},"paranoia-local":{"n":"paranoia-local","t":"Local MCP server for cross-agent adversarial code review","s":[["TAGLINE","Local MCP server for cross-agent adversarial code review"],["TLDR","**Install** from a clone\n\n```pip install -e .```\n\nWire into **Claude Code** (reviews performed by Codex)\n\n```claude mcp add paranoia -- paranoia-local --engine codex```\n\nWire into **Codex** (reviews performed by Claude Code)\n\n```codex mcp add paranoia -- paranoia-local --engine claude```\n\n**Run the MCP server** with a chosen review engine\n\n```paranoia-local --engine codex```\n\n```paranoia-local --engine claude```\n\n**Custom audit log directory**\n\n```paranoia-local --engine codex --log-dir [path]```"],["SYNOPSIS","**paranoia-local** **--engine** {**codex**|**claude**} [**--log-dir** _DIR_]"],["PARAMETERS","**--engine** **codex**|**claude**\n> Required. Which local coding-agent CLI **performs** reviews — the *other* agent from the caller. From Claude Code use **codex**; from Codex use **claude**.\n\n**--log-dir** _DIR_\n> Audit-log directory (default **~/.paranoia/logs**). Class-closure lineage state does **not** follow this path; it lives under **~/.paranoia/lineages** (or **PARANOIA_STATE_ROOT**)."],["DESCRIPTION","**paranoia-local** is a local Model Context Protocol (MCP) server that gets a cold, adversarial review of code, plans, and decisions from the *other* frontier coding agent. Installed into Claude Code, reviews are performed by Codex; installed into Codex, reviews are performed by Claude Code. The server builds the prompt, runs the reviewer CLI **read-only** with full repository access, and returns a structured critique.\n\nMCP tools include **critique_branch**, **critique_plan**, **query**, **rebut**, and **arbitrate** (the last needs both vendor CLIs). Multi-round work can track defect **classes** across rounds (class closure / lineage state), so loops can converge instead of repeating the same findings. Optional project defaults live in **.paranoia.toml** at the repo root.\n\nReviewers are sandboxed: Codex under its OS **read-only** sandbox; Claude with a restricted tool allowlist and no write tools. No API keys are embedded — the server shells out to CLIs you are already signed into. Reviews consume subscription agentic-usage quota."],["CONFIGURATION","**CLI:** **--engine** (required), **--log-dir**.\n\n**State on disk:**\n\n- **~/.paranoia/logs/** — JSON audit records per call\n- **~/.paranoia/lineages/** — class-closure state (not moved by **--log-dir**)\n- **PARANOIA_STATE_ROOT** — relocate lineage (and related) state\n\n**Repo file:** **.paranoia.toml** (or **[paranoia]** table) for defaults such as **base_ref**, **project_summary**, **stakes**, **isolate**, **converge**, **class_closure**, **model**, **effort**, **web_search**. Call arguments override the file.\n\n**MCP client timeouts:** long reviews need raised tool/startup timeouts in the host config (e.g. Codex **tool_timeout_sec** / **startup_timeout_sec**)."],["CAVEATS","Requires **Python 3.11+**, **git** on **PATH**, and at least one of **codex** (≥ 0.144 recommended) or **claude** installed and signed in; **arbitrate** needs both. Reviews can run for minutes and spend significant subscription quota. Host MCP timeouts that stay at short defaults will kill every call. Class-closure lineage keys must be unique when reviewing plans or non-branch refs."],["HISTORY","**paranoia-local** is written by **Andrew Hillel**, licensed **MIT**, and published as the PyPI / console script **paranoia-local**."],["SEE ALSO","[claude](/man/claude)(1), [codex](/man/codex)(1), [git](/man/git)(1), [pip](/man/pip)(1)"],["RESOURCES","```[Source code](https://github.com/subvertnormality/paranoia-local)```\n\n```[Homepage](https://github.com/subvertnormality/paranoia-local)```\n\n```[Documentation](https://github.com/subvertnormality/paranoia-local/tree/main/docs)```"]],"e":["claude","codex","git","pip"],"x":"pip install -e ."},"pdfpages":{"n":"pdfpages","t":"laTeX package for including external PDF documents","s":[["TAGLINE","laTeX package for including external PDF documents"],["TLDR","**Include PDF in LaTeX**\n\n```\\includepdf{[document.pdf]}```\n\n**Include specific pages**\n\n```\\includepdf[pages={1,3,5}]{[document.pdf]}```\n\n**Multiple pages per sheet**\n\n```\\includepdf[nup=2x2]{[document.pdf]}```\n\n**Include all pages**\n\n```\\includepdf[pages=-]{[document.pdf]}```"],["SYNOPSIS","**\\\\usepackage**{pdfpages}\n\n**\\\\includepdf**[_options_]{_filename_}"],["PARAMETERS","**pages**\n> Page selection (-, 1-5, {1,3,5}, last).\n\n**nup**\n> Pages per sheet layout (2x2, 3x2).\n\n**landscape**\n> Landscape orientation.\n\n**frame**\n> Draw frames around pages.\n\n**scale**\n> Scale factor.\n\n**offset**\n> Position offset."],["DESCRIPTION","**pdfpages** is a LaTeX package for including external PDF documents. It supports multi-page inclusion, n-up printing, and page selection, making it essential for combining documents."],["EXAMPLE","```latex\n\\documentclass{article}\n\\usepackage{pdfpages}\n\n\\begin{document}\n% Include entire PDF\n\\includepdf[pages=-]{external.pdf}\n\n% Include pages 1-3, scaled\n\\includepdf[pages={1-3},scale=0.8]{slides.pdf}\n\n% 2x2 layout\n\\includepdf[nup=2x2,pages=-]{handout.pdf}\n\\end{document}\n```"],["CAVEATS","Only works with pdfLaTeX, XeLaTeX, or LuaLaTeX. PDF must be valid. Large PDFs increase compile time."],["HISTORY","pdfpages was created by **Andreas Matthias** for the LaTeX Project, enabling seamless PDF inclusion."],["SEE ALSO","[pdflatex](/man/pdflatex)(1), [pdfjam](/man/pdfjam)(1), [pdfbook](/man/pdfbook)(1)"]],"e":["pdflatex","pdfjam","pdfbook"],"x":"\\includepdf{[document.pdf]}"},"pio-ci":{"n":"pio-ci","t":"Run continuous integration builds for PlatformIO projects","s":[["TAGLINE","Run continuous integration builds for PlatformIO projects"],["TLDR","**Build library in CI**\n\n```pio ci --lib [.] --board [uno] [examples/Blink]```\n\n**Build with multiple boards**\n\n```pio ci --board [uno] --board [esp32dev] [src/]```\n\n**Build with project config**\n\n```pio ci --project-conf [platformio.ini] [src/]```\n\n**Exclude files** from the build\n\n```pio ci --exclude [src/examples] --board [esp32dev] [src/]```\n\n**Pass project options** inline\n\n```pio ci --board [uno] -O \"lib_deps=[ArduinoJSON]\" [src/]```\n\n**Build with verbose output**\n\n```pio ci --board [uno] --verbose [src/]```"],["SYNOPSIS","**pio ci** [_OPTIONS_] [_SRC_]"],["PARAMETERS","**-l**, **--lib** _path_\n> Source code which will be copied to the build directory's lib folder. Can be specified multiple times.\n\n**-b**, **--board** _id_\n> Build project with automatically pre-generated environments based on board settings. Can be specified multiple times.\n\n**--exclude** _path_\n> Exclude directories and/or files from the build directory. Path must be relative to the PlatformIO project within the build directory. Supports glob patterns.\n\n**-c**, **--project-conf** _file_\n> Build project using a pre-configured platformio.ini file.\n\n**-O**, **--project-option** _option_\n> Pass additional project configuration options. Can be specified multiple times for different options.\n\n**-e**, **--environment** _name_\n> Process specified environments declared in the project configuration. Can be specified multiple times.\n\n**--build-dir** _path_\n> Path to directory where PlatformIO will initialise the new project. Defaults to a temporary directory.\n\n**--keep-build-dir**\n> Don't remove the build directory after the build process.\n\n**-v**, **--verbose**\n> Show detailed information when processing environments."],["DESCRIPTION","**pio ci** runs continuous integration builds for PlatformIO projects. It builds source files against specified boards without requiring an existing project structure. Using the provided source and library contents, PlatformIO initialises a new project in a build directory and processes it via the **pio run** command. Commonly used in CI/CD pipelines."],["INSTALL","```aur: yay -S pio```"],["SEE ALSO","[pio-run](/man/pio-run)(1), [pio-test](/man/pio-test)(1), [pio](/man/pio)(1)"]],"e":["pio-run","pio-test","pio"],"x":"pio ci --lib [.] --board [uno] [examples/Blink]"},"pocker":{"n":"pocker","t":"TUI tool for Docker container management","s":[["TAGLINE","TUI tool for Docker container management"],["TLDR","**Launch the Docker TUI**\n\n```pocker```"],["SYNOPSIS","**pocker** [_options_]"],["DESCRIPTION","**pocker** is a terminal user interface for managing Docker containers. It displays containers, images, logs, attributes, environment variables, and container statistics. Built on docker-py and Textual, it provides a keyboard-driven interface for common Docker operations."],["CAVEATS","Requires Docker to be installed and running."],["HISTORY","**pocker** was created by **pommee** and is written in **Python**."],["SEE ALSO","[docker](/man/docker)(1), [lazydocker](/man/lazydocker)(1), [oxker](/man/oxker)(1)"]],"e":["docker","lazydocker","oxker"],"x":"pocker"},"polars":{"n":"polars","t":"Query and convert data files via CLI","s":[["TAGLINE","Query and convert data files via CLI"],["TLDR","**Read and display CSV file**\n\n```polars read [file.csv]```\n\n**Query with SQL**\n\n```polars sql \"SELECT * FROM '[file.csv]' WHERE value > 100\"```\n\n**Convert CSV to Parquet**\n\n```polars convert [input.csv] [output.parquet]```\n\n**Show schema of file**\n\n```polars schema [file.parquet]```\n\n**Filter and output as JSON**\n\n```polars sql \"SELECT name, score FROM '[data.csv]' ORDER BY score DESC LIMIT 10\" -o json```\n\n**Join two files**\n\n```polars sql \"SELECT * FROM '[a.csv]' JOIN '[b.csv]' ON a.id = b.id\"```"],["SYNOPSIS","**polars** _command_ [_options_] [_args_...]"],["COMMANDS","**read** _file_\n> Read and display data file.\n\n**sql** _query_\n> Execute SQL query on file(s).\n\n**schema** _file_\n> Display schema/column information.\n\n**convert** _input_ _output_\n> Convert between formats (CSV, Parquet, JSON, Arrow)."],["PARAMETERS","**-o**, **--output** _format_\n> Output format: csv, json, parquet, table.\n\n**--delimiter** _char_\n> CSV delimiter character.\n\n**--no-header**\n> CSV has no header row.\n\n**-n**, **--limit** _rows_\n> Limit output rows.\n\n**-h**, **--help**\n> Display help information.\n\n**-V**, **--version**\n> Display version information."],["DESCRIPTION","**polars** is the command-line interface for Polars, a fast DataFrame library. It provides SQL querying and format conversion for data files without writing code.\n\nThe **sql** command executes SQL queries directly on files. Reference files as table names in quotes within the query. Polars' query engine optimizes execution for large datasets.\n\nSupported formats include CSV, Parquet, JSON, and Arrow. The **convert** command transforms between formats, useful for creating optimized Parquet files from CSV sources.\n\nPolars uses Apache Arrow columnar format internally, enabling efficient processing of large datasets with minimal memory overhead. Query optimization includes predicate pushdown and projection."],["CAVEATS","The CLI provides a subset of Polars library features. Complex transformations may require the Python or Rust API. Very large files benefit from Parquet format. SQL dialect has some differences from standard SQL."],["HISTORY","Polars was created by **Ritchie Vink** in **2020** as a fast alternative to pandas. Written in Rust with Python bindings, it quickly gained popularity for performance-critical data processing. The DataFrame library leverages Apache Arrow and lazy evaluation. The CLI tool was added to enable command-line data workflows. Polars has become a leading choice for large-scale data analysis."],["SEE ALSO","[duckdb](/man/duckdb)(1), [xsv](/man/xsv)(1), [miller](/man/miller)(1)"]],"e":["duckdb","xsv","miller"],"x":"polars read [file.csv]"},"procmux":{"n":"procmux","t":"TUI utility for running multiple commands in parallel with switchable terminals","s":[["TAGLINE","TUI utility for running multiple commands in parallel with switchable terminals"],["TLDR","**Launch procmux with default config**\n\n```procmux```\n\n**Launch with a specific config file**\n\n```procmux --config [path/to/procmux.yaml]```\n\n**Launch with a config and an override file**\n\n```procmux --config [path/to/procmux.yaml] --config-override [path/to/override.yaml]```\n\n**Stop a named process via signal**\n\n```procmux signal-stop --name '[process-name]' --config [path/to/procmux.yaml]```\n\n**Start a named process via signal**\n\n```procmux signal-start --name '[process-name]' --config [path/to/procmux.yaml]```\n\n**List all configured processes**\n\n```procmux signal-list --config [path/to/procmux.yaml]```"],["SYNOPSIS","**procmux** [_options_]\n**procmux** _signal-command_ **--name** _name_ **--config** _file_"],["PARAMETERS","**--config** _FILE_\n> Path to the procmux YAML configuration file.\n\n**--config-override** _FILE_\n> Path to a YAML file that overrides values in the main config.\n\n**signal-start** **--name** _NAME_ **--config** _FILE_\n> Start a named process.\n\n**signal-stop** **--name** _NAME_ **--config** _FILE_\n> Stop a named process.\n\n**signal-restart** **--name** _NAME_ **--config** _FILE_\n> Restart a named process.\n\n**signal-stop-running** **--config** _FILE_\n> Stop all currently running processes.\n\n**signal-restart-running** **--config** _FILE_\n> Restart all currently running processes.\n\n**signal-list** **--config** _FILE_\n> List all configured processes."],["DESCRIPTION","**procmux** is a TUI utility for running multiple commands in parallel in easily switchable terminal panes. It is designed to help newcomers to a project browse and run necessary commands while reducing the need to manually manage multiple terminal sessions for long-running processes. Configuration is defined in YAML files.\n\nWhen the signal server is enabled in the configuration, procmux starts an HTTP server that allows processes to be controlled remotely via the signal subcommands. The app supports process filtering, autostart options, descriptions, and meta tags."],["CAVEATS","Processes that rely on interpolation (configured prompt inputs) cannot be started or restarted through the signal subcommands because the required input values are only available inside the interactive TUI."],["HISTORY","**procmux** was created by **napisani** and is written in **Python**."],["SEE ALSO","[tmux](/man/tmux)(1), [mprocs](/man/mprocs)(1), [process-compose](/man/process-compose)(1), [screen](/man/screen)(1)"]],"e":["tmux","mprocs","process-compose","screen"],"x":"procmux"},"psysh":{"n":"psysh","t":"Interactive PHP shell and runtime debugger","s":[["TAGLINE","Interactive PHP shell and runtime debugger"],["TLDR","**Start PHP REPL**\n\n```psysh```\n\n**Start with specific config file**\n\n```psysh -c [config.php]```\n\n**Start with a specific working directory**\n\n```psysh --cwd [path/to/project]```\n\n**Run non-interactively from stdin**\n\n```echo \"echo 'hello';\" | psysh -n```\n\n**Start with compact output**\n\n```psysh --compact```\n\n**Start with autoload warming for better tab completion**\n\n```psysh --warm-autoload```"],["SYNOPSIS","**psysh** [_options_] [_files_...]"],["PARAMETERS","**-c**, **--config** _FILE_\n> Use alternate configuration file.\n\n**--cwd** _PATH_\n> Use alternate working directory.\n\n**-V**, **--version**\n> Display version number.\n\n**-i**, **-a**, **--interactive**\n> Force interactive mode.\n\n**-n**, **--no-interactive**\n> Force non-interactive mode (reads from stdin).\n\n**-r**, **--raw-output**\n> Print var_export-style return values.\n\n**--compact**\n> Run with compact output (minimal whitespace).\n\n**-q**, **--quiet**\n> Suppress most output except results and errors.\n\n**-v**, **-vv**, **-vvv**, **--verbose**\n> Increase message verbosity (up to three levels).\n\n**--color**\n> Force colored output.\n\n**--no-color**\n> Disable colored output.\n\n**--warm-autoload**\n> Pre-load classes from Composer autoloader for better tab completion.\n\n**--yolo**\n> Run with minimal input validation (debugging only).\n\n**-u**, **--self-update**\n> Install newer version (PHAR installation only).\n\n**--update-manual**\n> Download the latest PHP manual for inline documentation.\n\n**-h**, **--help**\n> Display help message."],["DESCRIPTION","**psysh** is a modern interactive PHP shell (REPL) that provides a powerful developer console with syntax highlighting, tab completion, and inline documentation. It allows executing PHP code interactively, inspecting variables, and testing snippets without creating script files.\n\nPsySH also functions as a runtime debugger when integrated into applications. By calling `eval(\\Psy\\sh())` in code, developers can drop into an interactive debugging session with access to the current scope's variables and context. It serves as the foundation for Laravel's **tinker** command."],["CONFIGURATION","**~/.config/psysh/config.php**\n> PHP configuration file for customizing the REPL behavior, including default includes, readline settings, and output pager."],["CAVEATS","Requires PHP. Installed via Composer or as a standalone PHAR."],["HISTORY","PsySH was created by **Justin Hileman** (bobthecow) as a **modern PHP REPL** with advanced features including inline documentation and runtime debugging."],["INSTALL","```brew: brew install psysh```\n\n```nix: nix profile install nixpkgs#psysh```"],["SEE ALSO","[php](/man/php)(1), [composer](/man/composer)(1), [artisan](/man/artisan)(1)"]],"e":["php","composer","artisan"],"x":"psysh"},"pulumi-gen-completion":{"n":"pulumi-gen-completion","t":"Generate shell completion scripts for Pulumi","s":[["TAGLINE","Generate shell completion scripts for Pulumi"],["TLDR","**Generate bash completions**\n\n```pulumi gen-completion bash```\n\n**Generate zsh completions**\n\n```pulumi gen-completion zsh```\n\n**Generate fish completions**\n\n```pulumi gen-completion fish```\n\n**Generate PowerShell completions**\n\n```pulumi gen-completion powershell```\n\n**Install bash completions**\n\n```pulumi gen-completion bash > /etc/bash_completion.d/pulumi```\n\n**Install zsh completions** to fpath directory\n\n```pulumi gen-completion zsh > \"${fpath[1]}/_pulumi\"```"],["SYNOPSIS","**pulumi** **gen-completion** _shell_"],["PARAMETERS","**bash**\n> Generate bash completions.\n\n**zsh**\n> Generate zsh completions.\n\n**fish**\n> Generate fish completions.\n\n**powershell**\n> Generate PowerShell completions."],["DESCRIPTION","**pulumi gen-completion** generates shell completion scripts for Pulumi commands. Outputs completion code to stdout for installation in shell configuration."],["INSTALL","```pacman: sudo pacman -S pulumi```\n\n```apk: sudo apk add pulumi```\n\n```brew: brew install pulumi```\n\n```nix: nix profile install nixpkgs#pulumi```"],["SEE ALSO","[pulumi](/man/pulumi)(1)"]],"e":["pulumi"],"x":"pulumi gen-completion bash"},"pw-dump":{"n":"pw-dump","t":"Export PipeWire state as JSON","s":[["TAGLINE","Export PipeWire state as JSON"],["TLDR","Print **JSON representation** of PipeWire's current state\n\n```pw-dump```\n\nPrint JSON representation of a **specific object**\n\n```pw-dump [object_id]```\n\nDump current state **monitoring changes**\n\n```pw-dump -m```\n\nDump state of **remote instance** to a file\n\n```pw-dump -r [remote_name] > [path/to/dump.json]```\n\nSet **color configuration**\n\n```pw-dump -C [never|always|auto]```"],["SYNOPSIS","**pw-dump** [_options_] [_id_]"],["PARAMETERS","_id_\n> Dump only the specified object ID\n\n**-m**, **--monitor**\n> Monitor for changes and dump again on updates\n\n**-r**, **--remote** _name_\n> Connect to remote PipeWire instance\n\n**-N**, **--no-colors**\n> Disable color output.\n\n**-C**, **--color** _mode_\n> Color mode: never, always, or auto.\n\n**--version**\n> Show version information.\n\n**-h**, **--help**\n> Display help message."],["DESCRIPTION","**pw-dump** outputs PipeWire's current state as JSON, including nodes, devices, modules, ports, links, and other objects. This comprehensive dump is useful for debugging, scripting, and understanding the current audio/video configuration.\n\nThe output includes object properties, formats, parameters, and relationships between components. The monitor mode keeps running and outputs changes as they occur."],["CAVEATS","Output can be very large on systems with many devices. Use object ID to filter to specific objects. JSON output is suitable for processing with jq or similar tools."],["HISTORY","Part of **PipeWire**, providing introspection capabilities for the multimedia framework. Complements pw-cli for automated and scripted queries."],["INSTALL","```apt: sudo apt install pipewire-bin```\n\n```apk: sudo apk add pipewire-tools```\n\n```zypper: sudo zypper install pipewire-tools```"],["SEE ALSO","[pw-cli](/man/pw-cli)(1), [pw-dot](/man/pw-dot)(1), [pw-mon](/man/pw-mon)(1), [pw-top](/man/pw-top)(1)"]],"e":["pw-cli","pw-dot","pw-mon","pw-top"],"x":"pw-dump"},"qjoypad":{"n":"qjoypad","t":"Map gamepad input to keyboard and mouse","s":[["TAGLINE","Map gamepad input to keyboard and mouse"],["TLDR","**Start** QJoyPad\n\n```qjoypad```\n\nSpecify **device directory**\n\n```qjoypad --device=[path/to/directory]```\n\nStart **without tray** icon\n\n```qjoypad --notray```\n\n**Force** tray icon\n\n```qjoypad --force-tray```\n\n**Update** device list\n\n```qjoypad --update```\n\nLoad specific **layout**\n\n```qjoypad \"[layout_name]\"```"],["SYNOPSIS","**qjoypad** [**--device** _path_] [**--notray**] [**--force-tray**] [**--update**] [_layout_]"],["PARAMETERS","**--device _path_**\n> Directory to look for devices\n\n**--notray**\n> Don't show system tray icon\n\n**--force-tray**\n> Force window manager to use tray icon\n\n**--update**\n> Update running instance's device list\n\n**layout**\n> Layout name to load"],["DESCRIPTION","**qjoypad** maps gamepad and joystick input to keyboard keystrokes and mouse actions. It allows using gamepads in applications that don't natively support them.\n\nThe tool provides a graphical interface for creating and managing input mappings. Layouts can be saved and loaded for different games or applications."],["CAVEATS","Requires evdev or joystick input devices. Some games may conflict with remapping. Layouts stored per-user. May need udev rules for device access."],["HISTORY","**qjoypad** was created to enable gamepad use in Linux applications lacking native support. It's particularly useful for emulators and games that only support keyboard input."],["INSTALL","```apt: sudo apt install qjoypad```\n\n```nix: nix profile install nixpkgs#qjoypad```"],["SEE ALSO","[antimicrox](/man/antimicrox)(1), [xboxdrv](/man/xboxdrv)(1), [jstest](/man/jstest)(1)"]],"e":["antimicrox","xboxdrv","jstest"],"x":"qjoypad"},"qm-delsnapshot":{"n":"qm-delsnapshot","t":"Delete Proxmox VM snapshots","s":[["TAGLINE","Delete Proxmox VM snapshots"],["TLDR","**Delete** a snapshot\n\n```qm delsnapshot 100 snapshot_name```\n\n**Force** delete from configuration (even if disk removal fails)\n\n```qm delsnapshot 100 snapshot_name --force 1```"],["SYNOPSIS","**qm delsnapshot** _vmid_ _snapname_ [_options_]"],["PARAMETERS","**--force** _boolean_\n> Force removal from config file, even if removing disk snapshots fails"],["DESCRIPTION","**qm delsnapshot** deletes virtual machine snapshots in Proxmox VE. Snapshots capture the VM state at a point in time and can be removed when no longer needed to reclaim storage space.\n\nThe force option allows removing snapshot references from configuration even if the underlying disk snapshot cannot be deleted."],["CAVEATS","Deleting snapshots is irreversible. Ensure the snapshot is no longer needed before deletion. Force deletion may leave orphaned disk data."],["HISTORY","Part of **Proxmox VE** QEMU/KVM management tools for virtual machine administration."],["INSTALL","```dnf: sudo dnf install qm```"],["SEE ALSO","[qm](/man/qm)(1), [qm-snapshot](/man/qm-snapshot)(1), [qm-listsnapshot](/man/qm-listsnapshot)(1), [qm-rollback](/man/qm-rollback)(1)"]],"e":["qm","qm-snapshot","qm-listsnapshot","qm-rollback"],"x":"qm delsnapshot 100 snapshot_name"},"qpdldecode":{"n":"qpdldecode","t":"Decode Samsung QPDL printer data format","s":[["TAGLINE","Decode Samsung QPDL printer data format"],["TLDR","**Decode QPDL printer format**\n\n```qpdldecode [input.qpdl] > [output.pbm]```\n\n**Show file info**\n\n```qpdldecode -i [input.qpdl]```"],["SYNOPSIS","**qpdldecode** [_options_] [_file_]"],["PARAMETERS","_FILE_\n> Input QPDL file.\n\n**-i**\n> Show information only."],["DESCRIPTION","**qpdldecode** decodes data in Samsung's proprietary QPDL (Quick Page Description Language) print format, converting it to standard image formats like PBM for analysis or reprocessing. It is distributed as part of the SpliX driver package for Samsung laser printers.\n\nThe tool can also display metadata about QPDL files without converting them, using the **-i** flag to show page count, resolution, and other format details."],["CAVEATS","Samsung printer specific. Part of splix driver."],["HISTORY","qpdldecode is part of **splix** Samsung printer drivers."],["INSTALL","```apt: sudo apt install printer-driver-foo2zjs```"],["SEE ALSO","[cups](/man/cups)(1)"]],"e":["cups"],"x":"qpdldecode [input.qpdl] > [output.pbm]"},"rcsdiff":{"n":"rcsdiff","t":"Compare RCS file revisions","s":[["TAGLINE","Compare RCS file revisions"],["TLDR","**Compare working file with latest revision**\n\n```rcsdiff [file]```\n\n**Compare specific revisions**\n\n```rcsdiff -r[1.1] -r[1.2] [file]```\n\n**Unified diff format**\n\n```rcsdiff -u [file]```\n\n**Context diff format**\n\n```rcsdiff -c [file]```\n\n**Compare with specific revision**\n\n```rcsdiff -r[1.5] [file]```\n\n**Side by side**\n\n```rcsdiff -y [file]```"],["SYNOPSIS","**rcsdiff** [_-r rev1_] [_-r rev2_] [_-u_] [_-c_] _file_"],["PARAMETERS","**-r** _REV_\n> Revision to compare.\n\n**-u**\n> Unified diff output.\n\n**-c**\n> Context diff output.\n\n**-y**\n> Side by side.\n\n**-q**\n> Quiet mode.\n\n**-n**\n> RCS format output."],["DESCRIPTION","**rcsdiff** compares revisions of files managed by the Revision Control System (RCS). Without a **-r** flag, it compares the current working file against the latest checked-in revision, showing local modifications. With one **-r** flag it compares the working file against a specific revision, and with two **-r** flags it compares two historical revisions directly.\n\nThe tool supports all standard diff output formats including unified (**-u**), context (**-c**), side-by-side (**-y**), and normal diff. It passes through options to the underlying diff command, making it a convenient wrapper for comparing version-controlled file histories."],["CAVEATS","RCS is legacy system. Consider Git instead. File-level versioning only."],["HISTORY","**rcsdiff** is part of **RCS** (Revision Control System), created by **Walter Tichy** in 1982 at Purdue University."],["INSTALL","```apt: sudo apt install rcs```\n\n```dnf: sudo dnf install rcs```\n\n```pacman: sudo pacman -S rcs```\n\n```apk: sudo apk add rcs```\n\n```zypper: sudo zypper install rcs```\n\n```brew: brew install rcs```\n\n```nix: nix profile install nixpkgs#rcs```"],["SEE ALSO","[rcs](/man/rcs)(1), [ci](/man/ci)(1), [co](/man/co)(1), [diff](/man/diff)(1)"]],"e":["rcs","ci","co","diff"],"x":"rcsdiff [file]"},"sh":{"n":"sh","t":"POSIX-compliant command interpreter","s":[["TAGLINE","POSIX-compliant command interpreter"],["TLDR","**Start an interactive shell**\n\n```sh```\n\n**Execute a script**\n\n```sh [script.sh]```\n\n**Execute commands from a string**\n\n```sh -c \"[command1; command2]\"```\n\n**Execute script with verbose output** (show commands)\n\n```sh -x [script.sh]```\n\n**Check syntax without executing**\n\n```sh -n [script.sh]```\n\n**Read commands from stdin**\n\n```echo \"echo hello\" | sh```"],["SYNOPSIS","**sh** [**-aCefimnsuvx**] [_script_ [_argument_...]]"],["PARAMETERS","**-a**\n> Export all variables that are modified or created to the environment\n\n**-C**\n> Prevent output redirection from overwriting existing files (noclobber)\n\n**-c** _string_\n> Execute commands from string\n\n**-e**\n> Exit immediately if a command exits with non-zero status\n\n**-f**\n> Disable filename globbing (wildcard expansion)\n\n**-i**\n> Interactive shell\n\n**-m**\n> Enable job control (default for interactive shells)\n\n**-n**\n> Read commands but do not execute (syntax check)\n\n**-s**\n> Read commands from standard input\n\n**-u**\n> Treat unset variables as an error\n\n**-v**\n> Print shell input lines as they are read\n\n**-x**\n> Print commands and arguments as they are executed\n\n**+**_option_\n> Turn off option"],["DESCRIPTION","**sh** is the POSIX-compliant command interpreter (shell). It provides the standard shell scripting environment and is guaranteed to be available on all Unix-like systems.\n\nOn many Linux systems, **/bin/sh** is a symbolic link to another shell (dash, bash, etc.) running in POSIX-compatibility mode. This provides both compatibility and performance benefits.\n\nShell scripts beginning with **#!/bin/sh** use POSIX sh, ensuring maximum portability. Scripts needing bash-specific features should use **#!/bin/bash** instead.\n\nThe shell reads commands from standard input, a file, or the **-c** argument. It supports variables, control flow (if, while, for, case), functions, pipelines, and I/O redirection."],["POSIX FEATURES","**Variables**: NAME=value, $NAME, ${NAME}\n**Quoting**: 'literal', \"interpolated\", \\escape\n**Conditionals**: if-then-elif-else-fi, case-esac\n**Loops**: while-do-done, for-in-do-done, until-do-done\n**Functions**: name() { commands; }\n**Tests**: [ condition ], test condition\n**Arithmetic**: $((expression))"],["CONFIGURATION","**~/.profile**\n> Per-user login shell initialization file, executed for login shells.\n\n**/etc/profile**\n> System-wide login shell initialization file, executed before ~/.profile.\n\n**ENV**\n> Environment variable pointing to a file executed at interactive shell startup."],["CAVEATS","POSIX sh lacks many bash features: arrays, [[ ]], brace expansion, process substitution, many string manipulations. Write portable scripts or explicitly require bash.\n\nThe **-e** option can cause unexpected exits. Commands in conditions (if, while) or with || / && don't trigger exit on failure.\n\nDifferent systems link /bin/sh to different implementations (dash, bash, ksh). Test scripts on target systems or use explicit interpreters."],["INSTALL","```apt: sudo apt install dash```\n\n```dnf: sudo dnf install dash```\n\n```pacman: sudo pacman -S dash```\n\n```apk: sudo apk add busybox-binsh```\n\n```zypper: sudo zypper install dash```\n\n```brew: brew install dash```\n\n```nix: nix profile install nixpkgs#dash```"],["SEE ALSO","[bash](/man/bash)(1), [dash](/man/dash)(1), [zsh](/man/zsh)(1), [ksh](/man/ksh)(1), [csh](/man/csh)(1)"]],"e":["bash","dash","zsh","ksh","csh"],"x":"sh"},"shopt":{"n":"shopt","t":"Toggle Bash shell behavior options","s":[["TAGLINE","Toggle Bash shell behavior options"],["TLDR","**List all shell options** with their status\n\n```shopt```\n\n**Enable a shell option**\n\n```shopt -s [optname]```\n\n**Disable a shell option**\n\n```shopt -u [optname]```\n\n**Check if an option is set** (quiet mode)\n\n```shopt -q [optname] && echo \"enabled\"```\n\n**Enable extended globbing**\n\n```shopt -s extglob```\n\n**Enable case-insensitive globbing**\n\n```shopt -s nocaseglob```\n\n**Include dotfiles in glob patterns**\n\n```shopt -s dotglob```\n\n**Print options in reusable format**\n\n```shopt -p```"],["SYNOPSIS","**shopt** [_-pqsu_] [_-o_] [_optname ..._]"],["PARAMETERS","**-p**\n> Print shell options in a format reusable as input\n\n**-q**\n> Quiet mode; suppress output, return status only\n\n**-s**\n> Set (enable) the specified options\n\n**-u**\n> Unset (disable) the specified options\n\n**-o**\n> Restrict to options also settable with set -o"],["COMMON OPTIONS","**cdspell**\n> Auto-correct minor spelling errors in cd arguments\n\n**dotglob**\n> Include files starting with . in glob patterns\n\n**extglob**\n> Enable extended pattern matching operators\n\n**globstar**\n> Enable ** recursive directory matching\n\n**histappend**\n> Append to history file instead of overwriting\n\n**nocaseglob**\n> Case-insensitive filename globbing\n\n**nullglob**\n> Empty glob patterns expand to nothing instead of themselves\n\n**expand_aliases**\n> Enable alias expansion (default in interactive shells)"],["DESCRIPTION","**shopt** is a Bash builtin that enables or disables shell options for the current session. These options modify shell behavior including globbing, history handling, directory navigation, and command completion.\n\nWithout arguments, shopt lists all options and their current status. Use **-s** to enable and **-u** to disable specific options. The **-q** flag tests options silently, setting exit status 0 if enabled, 1 if disabled.\n\nOptions set with shopt persist only for the current session. Add shopt commands to **~/.bashrc** for permanent configuration."],["CAVEATS","shopt is Bash-specific and not available in other shells like zsh, dash, or ksh, which have their own option mechanisms. The **-o** flag provides compatibility with options from the set builtin."],["HISTORY","shopt is a Bash builtin introduced in **Bash 2.0** (1996) to provide a cleaner interface for shell options compared to the set builtin. It continues to be the primary method for configuring Bash behavior."],["SEE ALSO","[set](/man/set)(1), [bash](/man/bash)(1)"]],"e":["set","bash"],"x":"shopt"},"sightmap":{"n":"sightmap","t":"YAML corpus and CLI for agent web-app context","s":[["TAGLINE","YAML corpus and CLI for agent web-app context"],["TLDR","**Install** the CLI globally\n\n```npm install -g @sightmap/sightmap```\n\n**Install** agent skills into the local harness\n\n```sightmap skills install```\n\n**Start** a Chrome session for the project corpus\n\n```sightmap browser start```\n\n**Snapshot** a URL with coverage of unlabeled nodes\n\n```sightmap snapshot --coverage --url '[https://example.com/path]'```\n\n**Validate** and **lint** the **.sightmap/** corpus\n\n```sightmap validate```\n\n```sightmap lint --warn-only```\n\n**Report** per-view coverage health\n\n```sightmap report```"],["SYNOPSIS","**sightmap** _command_ [_options_]"],["DESCRIPTION","**sightmap** is the reference CLI for the Sightmap open format: a **.sightmap/** directory of YAML that names a web app's views, components, and API requests, with optional freeform **memory** notes for runtime quirks that source code does not record.\n\nThe CLI drives a live browser via the Chrome DevTools Protocol to produce annotated accessibility snapshots (component names layered onto the tree) and structured interaction events. Agents author and maintain the corpus in an edit-verify loop: snapshot, fix orphaned interactive nodes, re-snapshot until coverage is complete.\n\nInstall via npm (**@sightmap/sightmap**) or build from the Go sources in the monorepo. Skills for coding agents can be installed with **sightmap skills install**."],["COMMANDS","**browser start** / **status**\n\n> Launch Chrome plus a local corpus server (default port 7891) that hot-reloads YAML, or show session status.\n\n**snapshot** [**--coverage**] [**--url** _url_]\n\n> Capture an annotated ARIA snapshot; with **--coverage**, print T1/T2/T3 labeling stats and unlabeled clusters.\n\n**sel-probe** _selector_\n\n> Verify a CSS selector candidate against the live page.\n\n**validate**\n\n> Structural YAML correctness for the corpus (non-zero exit on error).\n\n**lint** [**--warn-only**]\n\n> Advisory style checks on the corpus.\n\n**capture** [**--all**]\n\n> Refresh saved captures for view URLs.\n\n**report**\n\n> Per-view T1/T2/T3 health table.\n\n**skills install**\n\n> Extract embedded authoring/browser skills into **~/.agents/skills/** (or equivalent).\n\n**version**\n\n> Print the CLI version."],["CAVEATS","Requires a graphical Chrome/Chromium session for browser commands. Coverage quality depends on stable selectors and a maintained corpus. Sightmap enriches agent context; it does not replace application tests or accessibility audits."],["SEE ALSO","[chromium](/man/chromium)(1), [npm](/man/npm)(1)"],["RESOURCES","```[Source code](https://github.com/sightmap/sightmap)```\n\n```[Homepage](https://sightmap.org)```\n\n```[Documentation](https://docs.sightmap.org)```"]],"e":["chromium","npm"],"x":"npm install -g @sightmap/sightmap"},"sirtopnm":{"n":"sirtopnm","t":"Convert Solitaire image recorder files to PNM format","s":[["TAGLINE","Convert Solitaire image recorder files to PNM format"],["TLDR","**Convert** a Solitaire image to PNM\n\n```sirtopnm [input.sir] > [output.pnm]```\n\n**Convert** and pipe to a PNG encoder\n\n```sirtopnm [input.sir] | pnmtopng > [output.png]```"],["SYNOPSIS","**sirtopnm** [_sirfile_]"],["DESCRIPTION","**sirtopnm** reads a Solitaire Image Recorder file (`.sir`) as input and produces a PNM (Portable Any Map) image as output on standard output. The Solitaire Image Recorder was a high-resolution film recorder used for producing slides and transparencies from digital images.\n\nIf no input file is specified, **sirtopnm** reads from standard input. The output format (PBM, PGM, or PPM) depends on the content of the input image.\n\nThis tool is part of the **Netpbm** toolkit, a collection of image manipulation utilities for converting between various image formats."],["PARAMETERS","_sirfile_\n> Path to the Solitaire Image Recorder file to convert. Reads from standard input if omitted."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pnmtosir](/man/pnmtosir)(1), [sgitopnm](/man/sgitopnm)(1), [anytopnm](/man/anytopnm)(1), [pnmtopng](/man/pnmtopng)(1), [pnmtojpeg](/man/pnmtojpeg)(1)"]],"e":["pnmtosir","sgitopnm","anytopnm","pnmtopng","pnmtojpeg"],"x":"sirtopnm [input.sir] > [output.pnm]"},"snmpdf":{"n":"snmpdf","t":"Display remote disk usage via SNMP","s":[["TAGLINE","Display remote disk usage via SNMP"],["TLDR","**Show disk usage via SNMP**\n\n```snmpdf -v [2c] -c [public] [host]```\n\n**Human readable output**\n\n```snmpdf -v [2c] -c [public] -Ch [host]```\n\n**Human readable SI format output**\n\n```snmpdf -v [2c] -c [public] -CH [host]```\n\n**Use UCD-SNMP-MIB dskTable instead of default**\n\n```snmpdf -v [2c] -c [public] -Cu [host]```"],["SYNOPSIS","**snmpdf** [_options_] _agent_"],["PARAMETERS","**-v** _version_\n> SNMP version (1, 2c, 3).\n\n**-c** _community_\n> Community string.\n\n**-Ch**\n> Human readable sizes (MiB, GiB, TiB).\n\n**-CH**\n> Human readable sizes in SI format (MB, GB, TB).\n\n**-Cu**\n> Use UCD-SNMP-MIB dskTable instead of default HOST-RESOURCES-MIB hrStorageTable."],["DESCRIPTION","**snmpdf** displays disk space and storage usage from remote hosts by querying SNMP agents, providing functionality similar to the **df** command but over the network. It reads the Host Resources MIB (hrStorageTable) to report filesystem sizes, used space, and availability.\n\nThe tool queries storage entries including physical disks, RAM, virtual memory, and other storage types reported by the SNMP agent. The **-Ch** flag formats output in human-readable sizes, and **-Cu** forces use of the UCD-SNMP-MIB dskTable instead of the default hrStorageTable.\n\nsnmpdf is part of the **Net-SNMP** suite and is useful for monitoring disk usage across many hosts from a central location without requiring SSH access or agents beyond the standard SNMP daemon."],["CAVEATS","Requires a running SNMP agent on the remote host. The default hrStorageTable may report virtual memory and other non-disk storage types. Community strings and SNMP credentials must match the agent configuration. SNMPv3 authentication adds complexity but is recommended for security."],["HISTORY","**snmpdf** is part of the **Net-SNMP** suite, which evolved from the UCD-SNMP project originally developed at the University of California, Davis."],["INSTALL","```apt: sudo apt install snmp```\n\n```apk: sudo apk add net-snmp-tools```"],["SEE ALSO","[snmpget](/man/snmpget)(1), [snmpwalk](/man/snmpwalk)(1), [snmpset](/man/snmpset)(1), [df](/man/df)(1)"]],"e":["snmpget","snmpwalk","snmpset","df"],"x":"snmpdf -v [2c] -c [public] [host]"},"snmptranslate":{"n":"snmptranslate","t":"Translate between SNMP OID formats","s":[["TAGLINE","Translate between SNMP OID formats"],["TLDR","**Translate a numeric OID** to its textual name\n\n```snmptranslate [.1.3.6.1.2.1.1.1.0]```\n\n**Translate a textual OID** to numeric form\n\n```snmptranslate -On [sysDescr.0]```\n\n**Show full OID details** including description\n\n```snmptranslate -Td [sysDescr]```\n\n**Display the MIB tree** starting from a node\n\n```snmptranslate -Tp [system]```\n\n**Search for OIDs** by regex pattern\n\n```snmptranslate -TB [pattern]```\n\n**Show all OIDs** in the loaded MIBs\n\n```snmptranslate -Ta```\n\n**Load additional MIB** for translation\n\n```snmptranslate -m [+MY-MIB] [OID]```"],["SYNOPSIS","**snmptranslate** [_options_] _OID_"],["PARAMETERS","**-On**\n> Print OID in numeric form only.\n\n**-Of**\n> Print full OID path.\n\n**-OS**\n> Print the last symbolic element of the OID.\n\n**-Td**\n> Print detailed information including description, type, and access.\n\n**-Tp**\n> Print a tree of subtree rooted at the OID.\n\n**-Ta**\n> Print all OIDs in the loaded MIBs.\n\n**-TB** _pattern_\n> Search for OIDs matching the regex pattern.\n\n**-m** _MIBLIST_\n> Colon-separated list of MIB modules to load. Use **+MIB** to add to defaults.\n\n**-M** _PATHLIST_\n> Colon-separated list of directories to search for MIBs.\n\n**-IR**\n> Use random access lookup for better performance.\n\n**-h**\n> Display help information."],["DESCRIPTION","**snmptranslate** is a utility for translating between numeric and textual forms of SNMP Object Identifiers (OIDs). It uses MIB (Management Information Base) files to perform translations and can display detailed information about OIDs including their descriptions, syntax, and access permissions.\n\nThe tool is useful for understanding MIB structure, exploring available OIDs, and converting between human-readable names and numeric dotted notation used in SNMP operations. It does not communicate with any SNMP agent; all translations are performed locally using loaded MIB files.\n\nMIBs define the structure and meaning of OIDs. Common MIBs like SNMPv2-MIB and IF-MIB are loaded by default, but vendor-specific MIBs may need to be explicitly loaded."],["CAVEATS","Translation accuracy depends on having the correct MIBs installed and loaded. Vendor-specific OIDs require the corresponding vendor MIBs. The **-m** option with a **+** prefix adds to default MIBs; without **+**, it replaces them. MIB files should be placed in standard directories or specified via **-M**."],["HISTORY","**snmptranslate** is part of the **Net-SNMP** project, an open-source suite of SNMP tools and libraries. It provides a way to work with OIDs without network communication, making it useful for MIB exploration, documentation, and scripting. The tool has been part of Net-SNMP since its early versions."],["INSTALL","```apt: sudo apt install snmp```\n\n```apk: sudo apk add net-snmp-tools```"],["SEE ALSO","[snmpget](/man/snmpget)(1), [snmpset](/man/snmpset)(1), [snmpwalk](/man/snmpwalk)(1)"]],"e":["snmpget","snmpset","snmpwalk"],"x":"snmptranslate [.1.3.6.1.2.1.1.1.0]"},"snmpwalk":{"n":"snmpwalk","t":"Walk SNMP MIB trees retrieving all values","s":[["TAGLINE","Walk SNMP MIB trees retrieving all values"],["TLDR","**Walk entire MIB**\n\n```snmpwalk -v2c -c [community] [host]```\n\n**Walk specific OID**\n\n```snmpwalk -v2c -c [public] [host] [1.3.6.1.2.1.1]```\n\n**SNMPv3 with auth** and encryption\n\n```snmpwalk -v3 -u [user] -l authPriv -a SHA -A [authpass] -x AES -X [privpass] [host]```\n\n**Show numeric OIDs**\n\n```snmpwalk -On -v2c -c [community] [host]```\n\n**Walk system tree**\n\n```snmpwalk -v2c -c [public] [host] system```\n\n**Output values only**\n\n```snmpwalk -Oqv -v2c -c [public] [host] [oid]```"],["SYNOPSIS","**snmpwalk** [_-v version_] [_-c community_] [_options_] _host_ [_oid_]"],["PARAMETERS","**-v** _VERSION_\n> SNMP version (1, 2c, 3).\n\n**-c** _COMMUNITY_\n> Community string.\n\n**-u** _USER_\n> SNMPv3 username.\n\n**-l** _LEVEL_\n> Security level.\n\n**-A** _PASS_\n> Auth passphrase.\n\n**-X** _PASS_\n> Privacy passphrase.\n\n**-On**\n> Display OIDs numerically.\n\n**-Oq**\n> Quick print (removes type information).\n\n**-Ov**\n> Display values only, without OID prefix.\n\n**-Cr**\n> Do not check returned OIDs are increasing (for buggy agents).\n\n**-Of**\n> Display full OIDs (not abbreviated).\n\n**-Cc**\n> Do not check returned OIDs for correct subtree.\n\n**-a** _PROTOCOL_\n> SNMPv3 authentication protocol (MD5, SHA, SHA-224, SHA-256, SHA-384, SHA-512).\n\n**-x** _PROTOCOL_\n> SNMPv3 privacy protocol (DES, AES, AES-192, AES-256).\n\n**-t** _TIMEOUT_\n> Timeout in seconds for each request (default 1).\n\n**-r** _RETRIES_\n> Number of retries (default 5)."],["DESCRIPTION","**snmpwalk** retrieves a subtree of management values from an SNMP agent by issuing successive GETNEXT requests. Starting from a specified OID, it walks through the MIB tree and displays all values until it reaches the end of the subtree, providing a comprehensive view of device management data.\n\nThe tool supports SNMPv1, v2c, and v3. For v1 and v2c, community strings provide authentication, with \"public\" being a common read-only default. SNMPv3 adds robust security through username-based authentication (MD5/SHA) and encryption (DES/AES).\n\nThe starting OID determines the scope of the walk. Walking from the root retrieves all available data, while specifying a subtree like **system** or **interfaces** limits results. Output formatting options like **-On** (numeric OIDs) and **-Oq** (quick print) make output easier to parse in scripts."],["CAVEATS","Community strings are plaintext. v3 recommended for security. Large walks may be slow."],["HISTORY","**snmpwalk** is part of **Net-SNMP**, the standard open-source SNMP implementation. It's essential for network device monitoring and discovery."],["INSTALL","```apt: sudo apt install snmp```\n\n```apk: sudo apk add net-snmp-tools```"],["SEE ALSO","[snmpget](/man/snmpget)(1), [snmpset](/man/snmpset)(1), [snmptranslate](/man/snmptranslate)(1)"]],"e":["snmpget","snmpset","snmptranslate"],"x":"snmpwalk -v2c -c [community] [host]"},"spiel":{"n":"spiel","t":"Richly-styled terminal presentations using Python","s":[["TAGLINE","Richly-styled terminal presentations using Python"],["TLDR","**View the built-in demo deck**\n\n```spiel demo present```\n\n**Present a custom deck file**\n\n```python [deck.py]```"],["SYNOPSIS","**spiel** _command_ [_options_]"],["DESCRIPTION","**spiel** is a Python framework for building and presenting richly-styled slide decks in your terminal using the Rich library. Slides are defined programmatically with Python decorators, supporting images, animations, custom transitions, and a watch mode that live-reloads during editing."],["HISTORY","**spiel** was created by **Josh Karpel** and is written in **Python**."],["INSTALL","```zypper: sudo zypper install spiel```"],["SEE ALSO","[slides](/man/slides)(1), [patat](/man/patat)(1), [pysentation](/man/pysentation)(1)"]],"e":["slides","patat","pysentation"],"x":"spiel demo present"},"systemctl-switch-root":{"n":"systemctl-switch-root","t":"Switch to real root filesystem","s":[["TAGLINE","Switch to real root filesystem"],["TLDR","**Switch** to new root filesystem\n\n```systemctl switch-root [/path/to/new_root]```\n\nSwitch and run **specific init**\n\n```systemctl switch-root [/path/to/new_root] [/sbin/init]```\n\nSwitch with **verbose** output\n\n```systemctl switch-root [/path/to/new_root] -v```"],["SYNOPSIS","**systemctl switch-root** _ROOT_ [_INIT_]"],["PARAMETERS","**-v, --verbose**\n> Enable verbose output\n\n_ROOT_\n> Path to the new root filesystem\n\n_INIT_\n> Init binary to execute (default: systemd)"],["DESCRIPTION","**systemctl switch-root** transitions the system from an initramfs to the real root filesystem and executes a new system manager. This is the final step of the boot process after the initramfs has set up the real root.\n\nThis command is typically used internally by the initramfs to complete the boot process, not called manually."],["CAVEATS","Intended for initramfs use during boot. Incorrect use can render the system unbootable. The new root must contain a valid init system. All processes from the old root are terminated."],["HISTORY","The **switch-root** subcommand formalizes the initramfs-to-real-root transition in systemd, replacing the traditional pivot_root operations."],["SEE ALSO","[systemctl](/man/systemctl)(1), [pivot_root](/man/pivot_root)(2)"]],"e":["systemctl","pivot_root"],"x":"systemctl switch-root [/path/to/new_root]"},"systemd-ac-power":{"n":"systemd-ac-power","t":"Check AC power connection status","s":[["TAGLINE","Check AC power connection status"],["TLDR","Check **AC power** status\n\n```systemd-ac-power```\n\nCheck with **verbose** output\n\n```systemd-ac-power -v```"],["SYNOPSIS","**systemd-ac-power** [_OPTIONS_]"],["PARAMETERS","**-v, --verbose**\n> Print \"yes\" or \"no\" to stdout in addition to exit code\n\n**-h, --help**\n> Display help"],["DESCRIPTION","**systemd-ac-power** reports whether the computer is connected to an external power source (AC mains). It returns exit code 0 when on AC power and non-zero when on battery.\n\nThis is useful in scripts and systemd unit conditions to change behavior based on power state."],["EXIT STATUS","**0** - Running on AC power\n\n**non-zero** - Running on battery or unable to determine"],["CAVEATS","Requires proper power supply detection by the kernel. Some desktop systems without batteries always report AC power. Virtual machines may report power state incorrectly."],["HISTORY","**systemd-ac-power** provides a simple, scriptable interface to power supply status, enabling power-aware behavior in systemd units and shell scripts."],["INSTALL","```apt: sudo apt install systemd```\n\n```dnf: sudo dnf install systemd```\n\n```pacman: sudo pacman -S systemd```\n\n```zypper: sudo zypper install systemd```\n\n```brew: brew install systemd```\n\n```nix: nix profile install nixpkgs#systemd```"],["SEE ALSO","[systemctl](/man/systemctl)(1), [upower](/man/upower)(1)"]],"e":["systemctl","upower"],"x":"systemd-ac-power"},"tldr":{"n":"tldr","t":"Simplified community-maintained man pages","s":[["TAGLINE","Simplified community-maintained man pages"],["TLDR","**Show tldr page for a command**\n\n```tldr [command]```\n\n**Update the local cache**\n\n```tldr --update```\n\n**List all available pages**\n\n```tldr --list```\n\n**Show page for specific platform**\n\n```tldr -p [linux] [command]```\n\n**Search for pages matching** a keyword\n\n```tldr --search [keyword]```"],["SYNOPSIS","**tldr** [_options_] [_command_]"],["PARAMETERS","**-u**, **--update**\n> Update the local page cache\n\n**-l**, **--list**\n> List all available commands\n\n**-p**, **--platform** _platform_\n> Show page for specific platform (linux, osx, windows, sunos, android)\n\n**-L**, **--language** _lang_\n> Show page in specified language\n\n**-v**, **--version**\n> Display version\n\n**-h**, **--help**\n> Display help"],["DESCRIPTION","**tldr** provides simplified, community-maintained man pages focused on practical examples. Instead of comprehensive documentation, tldr pages show common use cases with copy-paste ready commands.\n\nThe name stands for \"Too Long; Didn't Read,\" reflecting its philosophy of brevity over completeness. Each page contains a brief description followed by example commands with explanations.\n\nPages are maintained by an open-source community and cover thousands of commands across multiple platforms. The cache is stored locally and can be updated with **--update**.\n\nMultiple client implementations exist (Node.js, Python, Rust, Go, etc.) all using the same page repository. Install whichever client suits your environment."],["CAVEATS","Tldr pages are community-maintained and may not cover every option or edge case. For comprehensive documentation, consult **man** pages or official documentation.\n\nSome commands have platform-specific pages. Use **--platform** to see pages for other operating systems.\n\nThe local cache must be updated periodically with **--update** to get new and updated pages."],["HISTORY","The tldr-pages project was started in **2013** to address the problem of overly complex man pages. It grew into a community project with thousands of contributors maintaining practical command examples across all major platforms."],["INSTALL","```apt: sudo apt install tealdeer```\n\n```dnf: sudo dnf install tldr```\n\n```pacman: sudo pacman -S tealdeer```\n\n```zypper: sudo zypper install tealdeer```\n\n```brew: brew install tealdeer```\n\n```nix: nix profile install nixpkgs#tealdeer```"],["SEE ALSO","[man](/man/man)(1), [info](/man/info)(1), [cheat](/man/cheat)(1), [help](/man/help)(1)"]],"e":["man","info","cheat","help"],"x":"tldr [command]"},"todo":{"n":"todo","t":"Simple command-line task management","s":[["TAGLINE","Simple command-line task management"],["TLDR","**Add task**\n\n```todo add \"[task description]\"```\n\n**List tasks**\n\n```todo list```\n\n**Complete task**\n\n```todo done [id]```\n\n**Remove task**\n\n```todo rm [id]```\n\n**Show task**\n\n```todo show [id]```\n\n**Clear completed**\n\n```todo clear```"],["SYNOPSIS","**todo** _command_ [_options_] [_args_]"],["PARAMETERS","**add**\n> Add task.\n\n**list**\n> Show tasks.\n\n**done**\n> Mark complete.\n\n**rm**\n> Remove task.\n\n**show**\n> Show details.\n\n**clear**\n> Remove done."],["DESCRIPTION","**todo** is a simple command-line task manager for creating, listing, completing, and removing tasks. It provides a minimal interface for quick task entry and progress tracking without the complexity of full-featured project management tools.\n\nMultiple implementations exist under the name \"todo,\" each with slightly different features and storage backends. The core workflow is consistent: add tasks with descriptions, mark them done when completed, and clear finished items from the list."],["CAVEATS","Multiple tools named todo. Check which installed. Features vary."],["HISTORY","Various **todo** implementations exist for command-line task management, each with slightly different features."],["INSTALL","```apt: sudo apt install devtodo```\n\n```pacman: sudo pacman -S todoman```\n\n```apk: sudo apk add todoman```\n\n```brew: brew install todoman```\n\n```nix: nix profile install nixpkgs#devtodo```"],["SEE ALSO","[task](/man/task)(1), [todo.sh](/man/todo.sh)(1), [todoman](/man/todoman)(1)"]],"e":["task","todo.sh","todoman"],"x":"todo add \"[task description]\""},"twopi":{"n":"twopi","t":"Graphviz radial graph layout engine","s":[["TAGLINE","Graphviz radial graph layout engine"],["TLDR","**Generate a radial layout** and output as PNG\n\n```twopi -Tpng -o [output.png] [input.dot]```\n\n**Render to SVG format**\n\n```twopi -Tsvg -o [output.svg] [input.dot]```\n\n**Specify a root node** for the center\n\n```twopi -Groot=[node_name] -Tpng -o [output.png] [input.dot]```\n\n**Set the radial distance** between rings (in inches)\n\n```twopi -Granksep=[1.5] -Tpng -o [output.png] [input.dot]```\n\n**Eliminate node overlaps** using Voronoi diagrams\n\n```twopi -Goverlap=false -Tpng -o [output.png] [input.dot]```\n\n**Process from stdin**\n\n```cat [graph.dot] | twopi -Tpng > [output.png]```"],["SYNOPSIS","**twopi** [**-G**_name_=_value_] [**-N**_name_=_value_] [**-E**_name_=_value_] [**-T**_format_] [**-o** _outfile_] [_files_]"],["PARAMETERS","**-T** _format_\n> Output format: png, svg, pdf, ps, jpg, gif, and many others.\n\n**-o** _file_\n> Output file name. If omitted, output goes to stdout.\n\n**-G**_name_=_value_\n> Set graph attribute. Common attributes: root, ranksep, overlap.\n\n**-N**_name_=_value_\n> Set default node attribute.\n\n**-E**_name_=_value_\n> Set default edge attribute.\n\n**-K** _layout_\n> Specify layout engine (default: twopi).\n\n**-V**\n> Print version information.\n\n**-s** _scale_\n> Set input scale to _scale_. If omitted, a default value of 72 is used.\n\n**-?**\n> Print usage information."],["DESCRIPTION","**twopi** is a Graphviz layout program that draws graphs using a radial layout algorithm. One node is chosen as the center and placed at the origin, with remaining nodes arranged on concentric circles at increasing distances based on their graph distance from the center.\n\nNodes at distance 1 from the center appear on the first ring, nodes at distance 2 on the second ring, and so forth. This layout is particularly effective for visualizing hierarchies, network topologies, and trees where relationships radiate from a central point.\n\nThe center node can be specified with the **root** attribute; otherwise, twopi randomly picks one of the nodes furthest from a leaf node (a node of degree 1). If no leaf nodes exist, an arbitrary node is chosen. The spacing between rings is controlled by **ranksep** (default 0.75 inches). Node overlaps can be resolved using the **overlap** attribute: **false** uses Voronoi diagrams, **scale** uniformly scales up the layout, and **true** (the default) does no repositioning.\n\nInput must be in DOT graph language format. twopi is part of the Graphviz suite alongside dot, neato, circo, fdp, and sfdp."],["CAVEATS","For large graphs, radial layouts can become cluttered as outer rings contain exponentially more nodes. The automatic center selection may not always produce optimal results; manually specifying the root node often improves layout quality. Memory usage scales with graph size."],["HISTORY","**twopi** was developed as part of **Graphviz** at **AT&T Labs Research** in the **1990s**. The radial layout algorithm is based on work by Graham Wills presented at the Graph Drawing Symposium in **1997**. Graphviz was open-sourced in **2000** and continues to be actively maintained."],["INSTALL","```apt: sudo apt install graphviz```\n\n```dnf: sudo dnf install graphviz```\n\n```pacman: sudo pacman -S graphviz```\n\n```apk: sudo apk add graphviz```\n\n```zypper: sudo zypper install graphviz```\n\n```brew: brew install graphviz```\n\n```nix: nix profile install nixpkgs#graphviz```"],["SEE ALSO","[dot](/man/dot)(1), [neato](/man/neato)(1), [circo](/man/circo)(1), [fdp](/man/fdp)(1), [sfdp](/man/sfdp)(1)"]],"e":["dot","neato","circo","fdp","sfdp"],"x":"twopi -Tpng -o [output.png] [input.dot]"},"ufw-deny":{"n":"ufw-deny","t":"Block traffic through Uncomplicated Firewall","s":[["TAGLINE","Block traffic through Uncomplicated Firewall"],["TLDR","**Deny** all traffic on a port\n\n```sudo ufw deny [port]```\n\nDeny traffic for a **protocol** on a port\n\n```sudo ufw deny [port]/[protocol]```\n\nDeny all traffic **from** a source address\n\n```sudo ufw deny from [source_address]```\n\nDeny all traffic from a **subnet**\n\n```sudo ufw deny from 192.168.13.0/24```\n\nDeny **UDP** from one host to another on a port\n\n```sudo ufw deny from 192.168.1.12 to 192.168.1.100 port 8080 proto udp```\n\nDeny with a **comment** for documentation\n\n```sudo ufw deny in 23/tcp comment \"block telnet\"```\n\nDeny **incoming** traffic on an interface by protocol\n\n```sudo ufw deny in on eth0 to 192.168.1.100 proto igmp```\n\n**Simulate** a deny rule without applying it\n\n```sudo ufw --dry-run deny 80/tcp```"],["SYNOPSIS","**ufw** [_--dry-run_] **deny** [_rule_]"],["PARAMETERS","**deny**\n> Drop matching traffic (inserts a deny/DROP rule)\n\n_port_[**/**_protocol_]\n> Simple form: port number, optional **/tcp** or **/udp**\n\n**from** _address_\n> Match source address or network (CIDR)\n\n**to** _address_\n> Match destination address\n\n**port** _port_\n> Destination port (or range) when using full rule syntax\n\n**proto** _protocol_\n> Protocol: **tcp**, **udp**, **igmp**, **gre**, etc.\n\n**in** / **out**\n> Direction of traffic\n\n**on** _interface_\n> Limit rule to a network interface\n\n**comment** '_text_'\n> Attach a human-readable comment to the rule\n\n**--dry-run**\n> Show what would change without applying it"],["DESCRIPTION","**ufw deny** adds a deny rule to Uncomplicated Firewall so matching packets are dropped (similar to iptables DROP). Rules can be simple port denials (`ufw deny 23/tcp`), source or network blocks (`ufw deny from 203.0.113.0/24`), or full five-tuple style rules with source, destination, port, protocol, and interface.\n\nDeny rules are useful for blocking known bad hosts, unused services, or entire subnets while keeping a default allow policy elsewhere. Prefer **deny** when you want silent drops; use **reject** (via `ufw reject`) when you want the peer to receive an explicit refusal. Use `ufw status numbered` to list rules and `ufw delete` to remove them by number."],["CAVEATS","Requires root or sudo. Rule order matters when combined with allows — more specific rules should be ordered carefully (`ufw insert`). A deny rule does not override a more specific earlier allow if packet matching selects the allow first. Application profile names must match installed profiles under `/etc/ufw/applications.d/`. Blocking yourself from SSH while managing a remote host can lock you out; prefer `--dry-run` and ensure alternate access first."],["HISTORY","Part of **ufw** (Uncomplicated Firewall), the Ubuntu-originated frontend for iptables/nftables."],["INSTALL","```dnf: sudo dnf install ufw```\n\n```pacman: sudo pacman -S ufw```\n\n```apk: sudo apk add ufw```\n\n```zypper: sudo zypper install ufw```"],["SEE ALSO","[ufw](/man/ufw)(8), [ufw-allow](/man/ufw-allow)(8), [ufw-enable](/man/ufw-enable)(8), [ufw-disable](/man/ufw-disable)(8), [iptables](/man/iptables)(8), [nftables](/man/nftables)(8)"],["RESOURCES","```[Source code](https://git.launchpad.net/ufw)```\n\n```[Documentation](https://help.ubuntu.com/community/UFW)```"]],"e":["ufw","ufw-allow","ufw-enable","ufw-disable","iptables","nftables"],"x":"sudo ufw deny [port]"},"varnishstat":{"n":"varnishstat","t":"Display Varnish cache statistics","s":[["TAGLINE","Display Varnish cache statistics"],["TLDR","**Display live statistics**\n\n```varnishstat```\n\n**Print statistics once and exit**\n\n```varnishstat -1```\n\n**Output as JSON**\n\n```varnishstat -j```\n\n**Output as XML**\n\n```varnishstat -x```\n\n**Show specific fields**\n\n```varnishstat -f [MAIN.cache_hit,MAIN.cache_miss]```\n\n**List available fields**\n\n```varnishstat -l```\n\n**Connect to specific instance**\n\n```varnishstat -n [instance_name]```"],["SYNOPSIS","**varnishstat** [_options_]"],["PARAMETERS","**-1**\n> Print statistics once and exit.\n\n**-j**\n> Output as JSON format.\n\n**-x**\n> Output as XML format.\n\n**-f** _field_\n> Filter to specific fields (glob pattern).\n\n**-I** _glob_\n> Include fields matching glob.\n\n**-X** _glob_\n> Exclude fields matching glob.\n\n**-l**\n> List available field names.\n\n**-n** _name_\n> Connect to specified varnishd instance.\n\n**-t** _seconds_\n> Timeout for acquiring shared memory lock."],["CURSES INTERFACE","The default curses mode shows live statistics with columns:\n\n**Current**: Current value.\n\n**Change**: Per-second change rate.\n\n**Average**: Runtime average.\n\n**Avg_10/100/1000**: Moving averages over update intervals."],["DESCRIPTION","**varnishstat** displays real-time statistics from a running Varnish cache server. It shows cache hit/miss rates, connection counts, backend health, and numerous performance counters.\n\nIn curses mode, statistics update continuously with the selected counter's description at the bottom. One-shot mode (-1) prints all counters once for scripting. JSON and XML outputs enable integration with monitoring systems.\n\nKey metrics include client requests, cache hit ratio, backend connections, and memory usage. Monitoring these helps identify performance bottlenecks and capacity issues."],["CAVEATS","Requires access to Varnish shared memory. Statistics reset on Varnish restart. Field names may change between versions. Some counters require interpretation (averages vs totals)."],["HISTORY","**varnishstat** was developed by Poul-Henning Kamp as part of the Varnish Cache project. Varnish was created for the Norwegian newspaper VG in 2006 and has become one of the most widely deployed HTTP accelerators."],["INSTALL","```apt: sudo apt install varnish```\n\n```dnf: sudo dnf install varnish```\n\n```apk: sudo apk add varnish```\n\n```brew: brew install varnish```\n\n```nix: nix profile install nixpkgs#varnish```"],["SEE ALSO","[varnishd](/man/varnishd)(1), [varnishhist](/man/varnishhist)(1), [varnishlog](/man/varnishlog)(1)"]],"e":["varnishd","varnishhist","varnishlog"],"x":"varnishstat"},"virt-qemu-run":{"n":"virt-qemu-run","t":"Run QEMU VMs without libvirtd","s":[["TAGLINE","Run QEMU VMs without libvirtd"],["TLDR","**Run** QEMU virtual machine\n\n```virt-qemu-run [path/to/guest.xml]```\n\nRun with **state** directory\n\n```virt-qemu-run -r [path/to/directory] [path/to/guest.xml]```\n\nRun with **verbose** output\n\n```virt-qemu-run -v [path/to/guest.xml]```\n\nDisplay **help**\n\n```virt-qemu-run -h```"],["SYNOPSIS","**virt-qemu-run** [_OPTIONS_] _GUEST.XML_"],["PARAMETERS","**-r** _DIR_, **--root** _DIR_\n> Store VM state files in the specified directory. Must be removed manually after shutdown. When omitted, a temporary directory is created and cleaned up automatically when the VM exits.\n\n**-s** _SECRET-XML-FILE_,_SECRET-VALUE-FILE_, **--secret** _SECRET-XML-FILE_,_SECRET-VALUE-FILE_\n> Load a secret into the secret driver. The XML file defines the secret (UUID must match one referenced in the domain XML); the value file contains the secret data. May be repeated.\n\n**-v**, **--verbose**\n> Display verbose startup information.\n\n**-h**, **--help**\n> Display help information."],["DESCRIPTION","**virt-qemu-run** is an experimental tool to run a QEMU guest virtual machine independent of the **libvirtd** daemon. It provides a way to run VMs using libvirt domain XML definitions without requiring the full libvirt infrastructure. The guest runs in the foreground and the command exits when the guest shuts down.\n\nThis is useful for testing, embedded use cases, or running isolated VMs without daemon overhead. Exit status is 0 on successful shutdown and non-zero on failure."],["CAVEATS","Experimental tool; behavior may change between releases. Guest XML must be valid libvirt format. Some features dependent on libvirtd (such as the full device management API) may not work. Not recommended for production use."],["INSTALL","```apt: sudo apt install libvirt-daemon-driver-qemu```\n\n```dnf: sudo dnf install libvirt-daemon-driver-qemu```\n\n```apk: sudo apk add libvirt-client```\n\n```zypper: sudo zypper install libvirt-daemon-driver-qemu```"],["SEE ALSO","[virt-manager](/man/virt-manager)(1), [virsh](/man/virsh)(1), [qemu](/man/qemu)(1)"]],"e":["virt-manager","virsh","qemu"],"x":"virt-qemu-run [path/to/guest.xml]"},"webpack":{"n":"webpack","t":"Static module bundler for JavaScript","s":[["TAGLINE","Static module bundler for JavaScript"],["TLDR","**Build the project** using default configuration\n\n```webpack```\n\n**Build with a specific configuration file**\n\n```webpack --config [webpack.config.js]```\n\n**Build for production**\n\n```webpack --mode production```\n\n**Build for development**\n\n```webpack --mode development```\n\n**Watch for changes** and rebuild\n\n```webpack --watch```\n\n**Specify output directory**\n\n```webpack --output-path [path/to/dist]```\n\n**Start the development server**\n\n```webpack serve```\n\n**Show build statistics**\n\n```webpack --stats verbose```"],["SYNOPSIS","**webpack** [_command_] [_entries..._] [_options_]"],["PARAMETERS","**build**, **bundle**, **b**\n> Run webpack to bundle the project (default command).\n\n**serve**, **server**, **s**\n> Run webpack dev server.\n\n**watch**, **w**\n> Run webpack and watch for file changes.\n\n**init**, **create**, **c**\n> Initialize a new webpack project.\n\n**info**, **i**\n> Output information about your system.\n\n**configtest**, **t**\n> Validate a webpack configuration file.\n\n**-c**, **--config** _file_\n> Path to webpack configuration file.\n\n**--config-name** _name_\n> Name of the configuration to use.\n\n**-m**, **--merge**\n> Merge multiple configurations.\n\n**--mode** _mode_\n> Set mode: **production**, **development**, or **none**.\n\n**-o**, **--output-path** _path_\n> Output directory (absolute path).\n\n**-w**, **--watch**\n> Watch mode, rebuild on file change.\n\n**--env** _value_\n> Environment variables passed to config function.\n\n**--stats** _preset_\n> Stats output level: errors-only, minimal, normal, verbose.\n\n**-v**, **--version**\n> Display version information.\n\n**-h**, **--help**\n> Display help message."],["DESCRIPTION","**webpack** is a static module bundler for modern JavaScript applications. It builds a dependency graph from entry points and bundles modules into optimized output files for browsers.\n\nThe CLI is provided by **webpack-cli** and wraps webpack's configuration options. Most options can be specified via command line or in **webpack.config.js**. CLI options override configuration file settings.\n\nwebpack supports code splitting, lazy loading, tree shaking, and various loaders and plugins for processing different file types (CSS, images, TypeScript, etc.)."],["CAVEATS","webpack-cli must be installed separately from webpack core (**npm install webpack webpack-cli**). The dev server (**webpack serve**) requires webpack-dev-server. Complex configurations are better maintained in config files rather than CLI arguments."],["HISTORY","webpack was created by **Tobias Koppers** and first released in **March 2012**. It revolutionized JavaScript build tooling by introducing concepts like code splitting and hot module replacement. webpack 4 (2018) focused on zero-config defaults, and webpack 5 (2020) improved build performance and module federation. The CLI was separated into webpack-cli starting with webpack 4."],["INSTALL","```brew: brew install webpack```"],["SEE ALSO","[npm](/man/npm)(1), [vite](/man/vite)(1), [rollup](/man/rollup)(1), [esbuild](/man/esbuild)(1), [parcel](/man/parcel)(1)"]],"e":["npm","vite","rollup","esbuild","parcel"],"x":"webpack"},"xfs_admin":{"n":"xfs_admin","t":"Modify XFS filesystem parameters","s":[["TAGLINE","Modify XFS filesystem parameters"],["TLDR","Display **label**\n\n```sudo xfs_admin -l [/dev/sdX]```\n\nSet **label**\n\n```sudo xfs_admin -L \"[label]\" [/dev/sdX]```\n\nDisplay **UUID**\n\n```sudo xfs_admin -u [/dev/sdX]```\n\nSet **UUID**\n\n```sudo xfs_admin -U [uuid] [/dev/sdX]```\n\n**Generate** new UUID\n\n```sudo xfs_admin -U generate [/dev/sdX]```"],["SYNOPSIS","**xfs_admin** [_OPTIONS_] _DEVICE_"],["PARAMETERS","**-l, --list**\n> Display filesystem label\n\n**-L, --Label** _LABEL_\n> Set filesystem label\n\n**-u, --uuid**\n> Display filesystem UUID\n\n**-U, --UUID** _UUID_\n> Set filesystem UUID (or \"generate\" for new)\n\n**-c** _0|1_\n> Enable/disable lazy-counters"],["DESCRIPTION","**xfs_admin** modifies parameters of an XFS filesystem. It can change the label and UUID of unmounted XFS filesystems.\n\nThe filesystem must be unmounted before modifying parameters."],["CAVEATS","Changing UUID may affect fstab and boot configurations. Requires root privileges. XFS filesystems only."],["INSTALL","```apt: sudo apt install xfsprogs```\n\n```dnf: sudo dnf install xfsprogs```\n\n```pacman: sudo pacman -S xfsprogs```\n\n```apk: sudo apk add xfsprogs-extra```\n\n```zypper: sudo zypper install xfsprogs```\n\n```nix: nix profile install nixpkgs#xfsprogs```"],["SEE ALSO","[xfs_repair](/man/xfs_repair)(8), [mkfs.xfs](/man/mkfs.xfs)(8), [tune2fs](/man/tune2fs)(8)"]],"e":["xfs_repair","mkfs.xfs","tune2fs"],"x":"sudo xfs_admin -l [/dev/sdX]"},"xkbcomp":{"n":"xkbcomp","t":"Compile XKB keyboard descriptions","s":[["TAGLINE","Compile XKB keyboard descriptions"],["TLDR","**Compile keymap for current display**\n\n```xkbcomp [keymap.xkb] $DISPLAY```\n\n**Extract current keymap to file**\n\n```xkbcomp $DISPLAY [output.xkb]```\n\n**Compile to binary keymap file**\n\n```xkbcomp -xkm [keymap.xkb] -o [keymap.xkm]```\n\n**Generate C header from keymap**\n\n```xkbcomp -C [keymap.xkb] -o [keymap.h]```\n\n**Compile with additional include path**\n\n```xkbcomp -I[/usr/share/X11/xkb] [keymap.xkb] $DISPLAY```\n\n**Verbose compilation**\n\n```xkbcomp -v 10 [keymap.xkb] $DISPLAY```"],["SYNOPSIS","**xkbcomp** [_options_] _source_ [_destination_]"],["PARAMETERS","**-xkb**\n> Output XKB source format.\n\n**-xkm**\n> Output compiled binary format.\n\n**-C**\n> Output C header file.\n\n**-I** _dir_\n> Add directory to include search path.\n\n**-o** _file_\n> Specify output file name.\n\n**-v** _level_\n> Set verbosity level (0-10).\n\n**-w** _level_\n> Set warning level.\n\n**-dflts**\n> Compute defaults for missing components."],["DESCRIPTION","**xkbcomp** compiles XKB (X Keyboard Extension) keyboard descriptions into binary format or uploads them directly to an X server. It's the core tool for customizing keyboard layouts beyond what setxkbmap provides.\n\nThe tool can extract the current keymap from a running X server, modify it, and upload changes. Output formats include XKB source (.xkb), compiled binary (.xkm), and C headers for embedding in applications.\n\nXKB provides advanced keyboard features including multiple layouts, dead keys, compose sequences, modifier handling, and accessibility options that simpler tools like xmodmap cannot achieve."],["CAVEATS","Complex syntax requires understanding XKB architecture. Without $DISPLAY, only creates files without loading. Errors can leave keyboard in unusable state; keep backup layout accessible."],["HISTORY","**xkbcomp** is part of the X Keyboard Extension (XKB), introduced in X11R6 to replace the limited keyboard handling of earlier X versions. XKB provides a comprehensive framework for keyboard configuration that supports internationalization and accessibility features."],["INSTALL","```apt: sudo apt install x11-xkb-utils```\n\n```dnf: sudo dnf install xkbcomp```\n\n```apk: sudo apk add xkbcomp```\n\n```zypper: sudo zypper install xkbcomp```\n\n```brew: brew install xkbcomp```\n\n```nix: nix profile install nixpkgs#xkbcomp```"],["SEE ALSO","[setxkbmap](/man/setxkbmap)(1), [xmodmap](/man/xmodmap)(1), [xev](/man/xev)(1)"]],"e":["setxkbmap","xmodmap","xev"],"x":"xkbcomp [keymap.xkb] $DISPLAY"},"yt-fts":{"n":"yt-fts","t":"Full-text search YouTube channel subtitles from the CLI","s":[["TAGLINE","Full-text search YouTube channel subtitles from the CLI"],["TLDR","**Download** a channel's subtitles into the DB\n\n```yt-fts download \"[https://www.youtube.com/@channel]\"```\n\n**Search** for a phrase\n\n```yt-fts search \"[keyword]\"```\n\n**Semantic search** (embeddings; needs API key)\n\n```yt-fts vsearch \"[query]\"```"],["SYNOPSIS","**yt-fts** *command* [*options*]"],["DESCRIPTION","**yt-fts** scrapes subtitles with **yt-dlp**, stores them in SQLite, and supports keyword and optional semantic search that returns timestamped YouTube URLs. Install with **pip install yt-fts**.\n\n**Note:** Upstream has described the project as abandoned; it may still work but expects limited support."],["PARAMETERS","**download** *channel_url*\n\n> Fetch subtitles (**--jobs** for parallelism).\n\n**search** *query*\n\n> Full-text search.\n\n**vsearch** *query*\n\n> Vector/semantic search when configured.\n\n**update** / **list** / **export** related commands\n\n> Maintain the local library—**yt-fts --help**."],["CAVEATS","Abandoned upstream risk. YouTube may rate-limit or require cookies. Semantic features need OpenAI (or compatible) API keys and cost money. Respect YouTube ToS."],["SEE ALSO","[yt-dlp](/man/yt-dlp)(1), [sqlite3](/man/sqlite3)(1)"],["RESOURCES","```[Source code](https://github.com/NotJoeMartinez/yt-fts)```"]],"e":["yt-dlp","sqlite3"],"x":"yt-fts download \"[https://www.youtube.com/@channel]\""},"zenmap":{"n":"zenmap","t":"Graphical interface for Nmap scanner","s":[["TAGLINE","Graphical interface for Nmap scanner"],["TLDR","**Launch GUI**\n\n```zenmap```\n\n**Scan a target**\n\n```zenmap -t [target]```\n\n**Open existing scan results**\n\n```zenmap -f [results.xml]```"],["SYNOPSIS","**zenmap** [_options_] [_results_file_]"],["PARAMETERS","**-t**, **--target** _target_\n> Start with specified target\n\n**-p**, **--profile** _profile_\n> Start with specified scan profile\n\n**-f**, **--file** _file_\n> Open scan results file\n\n**-n**, **--nmap** _path_\n> Path to nmap executable\n\n**-v**, **--verbose**\n> Increase verbosity\n\n**--help**\n> Display help message"],["DESCRIPTION","**Zenmap** is the official graphical user interface for Nmap, the network security scanner. It provides visual network topology mapping, scan comparison, and profile management to make Nmap more accessible.\n\nThe interface includes predefined scan profiles (Intense scan, Quick scan, Ping scan, etc.) and allows creating custom profiles. Scan results are displayed in multiple views: Nmap output, ports/hosts tables, topology map, and host details.\n\nZenmap can save scans to its database for later comparison, helping track network changes over time. The topology view visualizes discovered hosts and their relationships.\n\nScan profiles translate to Nmap command lines, visible in the interface. This helps users learn Nmap's options while using the GUI."],["CAVEATS","Many scan types require root/administrator privileges for raw socket access.\n\nScanning networks without authorization may be illegal. Only scan networks you own or have explicit permission to test.\n\nThe GTK-based interface may not be available on all systems. nmap command-line works everywhere.\n\nZenmap development is less active than Nmap itself. Some newer Nmap features may not have GUI support."],["INSTALL","```pacman: sudo pacman -S zenmap```\n\n```nix: nix profile install nixpkgs#zenmap```"],["SEE ALSO","[nmap](/man/nmap)(1), [masscan](/man/masscan)(1), [netstat](/man/netstat)(8)"]],"e":["nmap","masscan","netstat"],"x":"zenmap"},"zhist":{"n":"zhist","t":"Zsh history store with directory and exit-status context","s":[["TAGLINE","Zsh history store with directory and exit-status context"],["TLDR","**Hook zhist into zsh** (add to `.zshrc`)\n\n```eval \"$(zhist init)\"```\n\n**Import** an existing zsh history file once\n\n```zhist import ~/.zsh_history```\n\n**List** stored commands for the fzf picker (newest first, deduplicated)\n\n```zhist list```\n\n**List** only commands recorded in the current directory\n\n```zhist list -dir \"$PWD\"```\n\n**Print** the full command text for an entry\n\n```zhist get -id [id]```\n\n**Delete** one stored entry\n\n```zhist delete -id [id]```\n\n**Delete every entry** with the same command text\n\n```zhist delete -id [id] -all```\n\n**Record** a command from stdin with directory and exit status\n\n```print -r -- \"[command]\" | zhist add -dir \"$PWD\" -exit 0```"],["SYNOPSIS","**zhist** **init**\n\n**zhist** **add** **-dir** _dir_ **-exit** _n_ [**-ts** _unix_]\n\n**zhist** **list** [**-dir** _dir_]\n\n**zhist** **get** **-id** _id_\n\n**zhist** **delete** **-id** _id_ [**-all**]\n\n**zhist** **import** _file_"],["DESCRIPTION","**zhist** is a zsh-only shell history tool written in Go. It replaces the zsh history file as the persistent store and records each command together with its working directory and exit status. Native zsh history keeps only the command and a timestamp; zhist keeps that extra context so the picker can highlight failures and switch between global history and the current directory.\n\nThe interactive UI is **fzf**, not a custom TUI. `eval \"$(zhist init)\"` emits zsh hooks and keybindings that call the **zhist** binary: a **preexec**/**precmd** pair appends each command via **zhist add**, and **ctrl-r** (plus bare up/down on an empty line) opens **zhist list** in fzf. Failed commands render in red. **ctrl-g** toggles global versus directory-scoped history.\n\nEntries are appended as JSON lines to **~/.local/share/zhist/history.jsonl** (mode **0600**), or to the path in **ZHIST_FILE**. Imported zsh **EXTENDED_HISTORY** lines have no directory or exit status; they show a blank directory and never render red."],["PARAMETERS","**init**\n> Print the zsh integration script (hooks, fzf picker, and keybindings). Intended for `eval \"$(zhist init)\"`.\n\n**add** **-dir** _dir_ **-exit** _n_ [**-ts** _unix_]\n> Append one entry. Command text is read from stdin. **-ts** defaults to the current Unix time.\n\n**list** [**-dir** _dir_]\n> Print entries for fzf: newest first, one unique command per line, tab-separated id / relative time / command. **-dir** keeps only entries recorded in that directory.\n\n**get** **-id** _id_\n> Print the full command for an entry (including embedded newlines).\n\n**delete** **-id** _id_ [**-all**]\n> Delete the entry with that id. **-all** also deletes every other entry with the same command text.\n\n**import** _file_\n> Import a zsh **EXTENDED_HISTORY** file (`: timestamp:elapsed;command` lines). Prints `imported N entries`."],["KEY BINDINGS","These bindings are installed by **zhist init**:\n\n**ctrl-r**\n> Open the history picker.\n\n**up** / **down**\n> Open the picker when the line is empty; otherwise step native line history.\n\n**ctrl-g**\n> Toggle global versus current-directory history.\n\n**ctrl-d**\n> Delete the selected entry.\n\n**ctrl-x**\n> Delete all entries with the same command.\n\n**tab**\n> Accept and leave the command on the line.\n\n**ctrl-/**\n> Toggle the command preview pane. Visibility is remembered in **${XDG_STATE_HOME:-$HOME/.local/state}/zhist/preview-hidden**."],["CONFIGURATION","Add to **~/.zshrc** after any plugin that binds **ctrl-r** or the arrow keys (the last bind wins):\n\n```\neval \"$(zhist init)\"\n```\n\nSkip recording by first word with a **HIST_EXCLUDE** array. Matching is exact and case-sensitive on the first word only (`ls` skips `ls -la` but not `lsd`). A leading space also skips the command, matching **HIST_IGNORE_SPACE**.\n\n```\nHIST_EXCLUDE=(cd ls clear pwd exit)\n```\n\n**ZHIST_FILE**\n> Override the JSONL store path (default **~/.local/share/zhist/history.jsonl**).\n\nBecause zhist owns persistence, keep native zsh history in memory only:\n\n```\nunset HISTFILE\nHISTSIZE=100000\nSAVEHIST=0\n```\n\nDo not set **SHARE_HISTORY**, **INC_APPEND_HISTORY**, or **EXTENDED_HISTORY**; they only affect the history file zhist replaces."],["CAVEATS","zsh only; there is no bash or fish integration. The picker requires **fzf** 0.45 or newer. `eval \"$(zhist init)\"` must run after atuin, zsh-history-substring-search, or other widgets that bind the same keys. Building from source via **go install** needs the Go version declared in the module. Imported history has no directory or exit status. The record hook prepends itself to **precmd_functions** and passes **$?** through so later prompt hooks still see the real exit status."],["HISTORY","**zhist** was created by **overflowy** in **August 2026**. It is written in Go and released under the MIT license. The store is JSON Lines; the search UI is delegated to fzf."],["SEE ALSO","[atuin](/man/atuin)(1), [mcfly](/man/mcfly)(1), [hishtory](/man/hishtory)(1), [hstr](/man/hstr)(1), [fzf](/man/fzf)(1), [history](/man/history)(1), [zsh](/man/zsh)(1)"],["RESOURCES","```[Source code](https://github.com/overflowy/zhist)```"]],"e":["atuin","mcfly","hishtory","hstr","fzf","history","zsh"],"x":"eval \"$(zhist init)\""}});
