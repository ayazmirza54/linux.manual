window.LCL_BUCKET(33,{"aide":{"n":"aide","t":"Detect file system changes for intrusion detection","s":[["TAGLINE","Detect file system changes for intrusion detection"],["TLDR","**Initialize** the database\n\n```sudo aide -i```\n\n**Check** the database for inconsistencies\n\n```sudo aide -C```\n\n**Compare** two databases according to the config file\n\n```sudo aide -E```\n\n**Update** the database non-interactively\n\n```sudo aide -u```\n\nDefine a **config file** to override the default aide.conf\n\n```sudo aide -c [path/to/config_file]```\n\nUse **regex** to limit AIDE to a specific string\n\n```sudo aide -l [regex]```\n\nSet the **log level**\n\n```sudo aide -L [warning] -C```"],["SYNOPSIS","**aide** [_parameters_] _command_"],["DESCRIPTION","**AIDE** (Advanced Intrusion Detection Environment) is a file and directory integrity checker used for intrusion detection. It builds a database of file attributes including permissions, inode numbers, timestamps, file sizes, link counts, and checksums using algorithms like SHA-256 and SHA-512.\n\nOnce an initial database is created, AIDE can compare the current state of the file system against the stored snapshot to detect unauthorized modifications, new files, or deleted files. Rules in the configuration file define which directories to monitor and which attributes to check for each path."],["PARAMETERS","**-i, --init**\n> Initialize the database; must be moved to the appropriate place before using --check\n\n**-C, --check**\n> Check the database for inconsistencies; requires an initialized database (this is the default command)\n\n**-u, --update**\n> Check and update the database non-interactively; input and output databases must be different\n\n**-E, --compare**\n> Compare two databases as defined in config file\n\n**-n, --dry-init**\n> Traverse the file system and report matched entries without writing a database\n\n**-D, --config-check**\n> Stop after reading configuration file to check for errors\n\n**-c, --config file**\n> Specify alternate configuration file (use '-' for stdin)\n\n**-l, --limit regex**\n> Restrict operations to entries matching a regex pattern\n\n**-L, --log-level level**\n> Set the log level, overriding the config file (replaces the removed --verbose option)\n\n**-B, --before**\n> Set config parameters before file reading\n\n**-A, --after**\n> Set config parameters after file reading\n\n**-v, --version**\n> Print version information and exit"],["CONFIGURATION","**/etc/aide/aide.conf**\n> Main configuration file defining which files to monitor, database locations, and check rules."],["CAVEATS","Exit codes combine: 1 (new files), 2 (removed files), and 4 (changed files). The database must be stored securely, preferably on read-only media. The **-r/--report** and **-V/--verbose** options were removed in AIDE 0.17; use the **report_url** and **report_level** configuration options (or **-L/--log-level**) instead."],["HISTORY","Originally developed as an open-source alternative to proprietary file integrity checkers like Tripwire. Since version 0.17 the report and verbose command-line flags were replaced by configuration directives and the **-L/--log-level** option."],["INSTALL","```apt: sudo apt install aide```\n\n```dnf: sudo dnf install aide```\n\n```aur: yay -S aide```\n\n```zypper: sudo zypper install aide```\n\n```brew: brew install aide```\n\n```nix: nix profile install nixpkgs#aide```"],["SEE ALSO","[tripwire](/man/tripwire)(8), [md5sum](/man/md5sum)(1), [sha256sum](/man/sha256sum)(1)"],["RESOURCES","```[Source code](https://github.com/aide/aide)```\n\n```[Homepage](https://aide.github.io/)```"]],"e":["tripwire","md5sum","sha256sum"],"x":"sudo aide -i"},"ansible":{"n":"ansible","t":"Agentless IT automation and configuration management","s":[["TAGLINE","Agentless IT automation and configuration management"],["TLDR","**Ping** all hosts in inventory\n\n```ansible all -m ping```\n\nExecute a **command** on hosts\n\n```ansible [webservers] -a \"uptime\"```\n\nExecute with **sudo**\n\n```ansible [webservers] -a \"apt update\" --become```\n\nRun an **ad-hoc module**\n\n```ansible [hosts] -m [apt] -a \"name=nginx state=present\" --become```\n\nUse **specific inventory**\n\n```ansible -i [inventory.ini] all -m ping```"],["SYNOPSIS","**ansible** _pattern_ [_-m module_] [_-a args_] [_-i inventory_] [_options_]"],["DESCRIPTION","**ansible** is an agentless IT automation tool that executes tasks on remote systems over SSH. It uses a push-based model, requiring no software installation on managed nodes beyond Python and SSH access.\n\nFor ad-hoc commands, ansible executes modules against hosts matching a pattern. For complex automation, use ansible-playbook with YAML playbooks."],["PARAMETERS","**-m** _module_, **--module-name** _module_\n> Name of the module to execute (default: command)\n\n**-a** _args_, **--args** _args_\n> Module arguments as key=value or JSON\n\n**-i** _inventory_, **--inventory** _inventory_\n> Inventory file/path or comma-separated host list\n\n**-b**, **--become**\n> Run operations with privilege escalation (become)\n\n**-K**, **--ask-become-pass**\n> Prompt for privilege escalation password\n\n**-u** _user_, **--user** _user_\n> Connect as this user\n\n**-k**, **--ask-pass**\n> Prompt for SSH connection password\n\n**-f** _forks_, **--forks** _forks_\n> Number of parallel processes (default: 5)\n\n**-l** _pattern_, **--limit** _pattern_\n> Further limit selected hosts to an additional pattern\n\n**-v**, **-vvv**, **--verbose**\n> Increase verbosity (repeat for more detail)\n\n**-C**, **--check**\n> Dry run; predict changes without applying them\n\n**--diff**\n> Show differences in changed files (works with --check)\n\n**--list-hosts**\n> Output a list of matching hosts; do not execute"],["CONFIGURATION","**/etc/ansible/ansible.cfg**\n> System-wide Ansible configuration, including default module path, connection settings, and privilege escalation.\n\n**~/.ansible.cfg**\n> Per-user Ansible configuration overriding system defaults.\n\n**ansible.cfg**\n> Project-level configuration in the current directory, highest priority.\n\n**/etc/ansible/hosts**\n> Default inventory file listing managed hosts and groups."],["CAVEATS","Requires Python on managed nodes. SSH key-based authentication is recommended. Windows hosts require WinRM instead of SSH. Large inventories benefit from using ansible-playbook."],["HISTORY","**Ansible** was created by Michael DeHaan and released in **2012**. Red Hat acquired Ansible Inc. in **2015**. It has become one of the most popular configuration management and automation tools."],["INSTALL","```dnf: sudo dnf install ansible-core```\n\n```pacman: sudo pacman -S ansible-core```\n\n```apk: sudo apk add ansible-core```\n\n```zypper: sudo zypper install ansible-core```\n\n```brew: brew install ansible```\n\n```nix: nix profile install nixpkgs#ansible```"],["SEE ALSO","[ansible-playbook](/man/ansible-playbook)(1), [ansible-inventory](/man/ansible-inventory)(1), [ansible-vault](/man/ansible-vault)(1), [ansible-doc](/man/ansible-doc)(1)"],["RESOURCES","```[Source code](https://github.com/ansible/ansible)```\n\n```[Documentation](https://docs.ansible.com/ansible/latest/cli/ansible.html)```"]],"e":["ansible-playbook","ansible-inventory","ansible-vault","ansible-doc"],"x":"ansible all -m ping"},"apptainer-build":{"n":"apptainer-build","t":"Build Apptainer container images from definition files or remote sources.","s":[["TAGLINE","Build Apptainer container images from definition files or remote sources."],["TLDR","Build a container from a **definition file**\n\n```apptainer build [path/to/image.sif] [path/to/definition.def]```\n\nBuild a container from **Docker Hub**\n\n```apptainer build [path/to/image.sif] docker://[image]:[tag]```\n\nBuild a container from the **Container Library**\n\n```apptainer build [path/to/image.sif] library://[user/collection/container]:[tag]```\n\nBuild a **writable sandbox** directory instead of an image file\n\n```apptainer build -s [path/to/directory] docker://[image]:[tag]```\n\nBuild a container **without using the cache**\n\n```apptainer build --disable-cache [path/to/image.sif] docker://[image]:[tag]```\n\n**Force overwrite** an existing image file\n\n```apptainer build -F [path/to/image.sif] [path/to/definition.def]```\n\nBuild using **fakeroot** for unprivileged builds\n\n```apptainer build -f [path/to/image.sif] [path/to/definition.def]```"],["SYNOPSIS","**apptainer build** [_options_] _image_ _build-spec_"],["DESCRIPTION","**apptainer build** creates Apptainer container images from various sources including definition files, Docker Hub, OCI registries, and the Sylabs Container Library. The resulting SIF (Singularity Image Format) file is a portable, immutable container image.\n\nDefinition files (**.def**) provide a reproducible way to specify base images, software installation steps, environment variables, and runscripts. The sandbox mode creates a writable directory structure useful for development and testing."],["PARAMETERS","**-s, --sandbox**\n> Build a writable directory instead of an immutable SIF image\n\n**-F, --force**\n> Overwrite an existing image file without prompting\n\n**-f, --fakeroot**\n> Build using fakeroot, appearing to run as root (default for unprivileged definition builds).\n\n**--disable-cache**\n> Do not use or create cached images during the build process.\n\n**--no-cleanup**\n> Retain the temporary build directory after a failed build (for debugging).\n\n**-u, --update**\n> Run the definition over an existing sandbox container, rerunning sections such as %post.\n\n**-T, --notest**\n> Build the container without running the %test section.\n\n**--section** _name_\n> Run only the given definition sections instead of all of them (e.g. post, environment).\n\n**-e, --encrypt**\n> Build an image with an encrypted filesystem (set APPTAINER_ENCRYPTION_PASSPHRASE or APPTAINER_ENCRYPTION_PEM_PATH).\n\n**--build-arg** _key=value_\n> Replace {{ key }} placeholders in the definition file with the given value.\n\n**--arch** _arch_\n> Architecture to build for (default: amd64)."],["CONFIGURATION","**/etc/apptainer/apptainer.conf**\n> Main configuration file controlling build behavior, security settings, bind paths, and fakeroot mappings."],["CAVEATS","Building containers often requires elevated privileges or fakeroot configuration. Some base images may not be compatible with fakeroot builds. Large builds can consume significant disk space in the cache directory."],["INSTALL","```dnf: sudo dnf install apptainer```\n\n```pacman: sudo pacman -S apptainer```\n\n```apk: sudo apk add apptainer```\n\n```zypper: sudo zypper install apptainer```\n\n```brew: brew install apptainer```\n\n```nix: nix profile install nixpkgs#apptainer```"],["SEE ALSO","[apptainer](/man/apptainer)(1), [apptainer-pull](/man/apptainer-pull)(1), [apptainer-run](/man/apptainer-run)(1), [apptainer-exec](/man/apptainer-exec)(1), [apptainer-shell](/man/apptainer-shell)(1), [docker](/man/docker)(1)"],["RESOURCES","```[Source code](https://github.com/apptainer/apptainer)```\n\n```[Homepage](https://apptainer.org)```\n\n```[Documentation](https://apptainer.org/docs/)```"]],"e":["apptainer","apptainer-pull","apptainer-run","apptainer-exec","apptainer-shell","docker"],"x":"apptainer build [path/to/image.sif] [path/to/definition.def]"},"ark":{"n":"ark","t":"Create, view, and extract archives graphically.","s":[["TAGLINE","Create, view, and extract archives graphically."],["TLDR","**Extract** a specific archive into the current directory\n\n```ark -b [path/to/archive]```\n\nExtract an archive into a **specific directory**\n\n```ark -b -o [path/to/directory] [path/to/archive]```\n\n**Create** an archive and add specific files to it\n\n```ark -t [path/to/archive] [path/to/file1] [path/to/file2]```"],["SYNOPSIS","**ark** [_options_] [_archive_] [_files_]"],["DESCRIPTION","**ark** is KDE's graphical archiving tool that supports creating, viewing, extracting, and modifying archives. It handles numerous formats including tar, gzip, bzip2, zip, rar, 7z, and many others through plugins.\n\nWhen run with command-line options, ark can operate in batch mode without displaying its GUI, making it useful for scripting and automation."],["PARAMETERS","**-b, --batch**\n> Use batch mode (no GUI) to extract archives\n\n**-o, --destination** _directory_\n> Extract to the specified directory\n\n**-t, --add-to** _archive_\n> Add files to an archive, creating it if necessary\n\n**-c, --add**\n> Add files to an existing archive\n\n**-e, --extract**\n> Open the extraction dialog\n\n**-d, --dialog**\n> Show a dialog to choose extraction options instead of extracting silently\n\n**-f, --changetofirstpath**\n> Use the first archive entry's path as the destination (with **-b**)"],["CAVEATS","Some archive formats require additional plugins or external programs. RAR creation requires **rar** (proprietary), while extraction works with the free **unrar**. Format support depends on installed KDE archive plugins."],["INSTALL","```apt: sudo apt install ark```\n\n```dnf: sudo dnf install ark```\n\n```pacman: sudo pacman -S ark```\n\n```apk: sudo apk add ark```\n\n```zypper: sudo zypper install ark```\n\n```brew: brew install ark```\n\n```nix: nix profile install nixpkgs#ark```"],["SEE ALSO","[tar](/man/tar)(1), [unzip](/man/unzip)(1), [7z](/man/7z)(1)"],["RESOURCES","```[Source code](https://invent.kde.org/utilities/ark)```\n\n```[Homepage](https://apps.kde.org/ark/)```"]],"e":["tar","unzip","7z"],"x":"ark -b [path/to/archive]"},"atuin":{"n":"atuin","t":"Magical shell history with sync and search","s":[["TAGLINE","Magical shell history with sync and search"],["TLDR","**Search** command history interactively\n\n```atuin search -i [query]```\n\n**Import** existing history from the current shell\n\n```atuin import auto```\n\n**Sync** history to server\n\n```atuin sync```\n\nShow **statistics** about shell usage\n\n```atuin stats```\n\n**Login** to sync server\n\n```atuin login```\n\n**Register** a new sync account\n\n```atuin register -u [username] -e [email] -p [password]```\n\n**List** recent command history\n\n```atuin history list```\n\nRun **diagnostics** to check for common issues\n\n```atuin doctor```"],["SYNOPSIS","**atuin** _command_ [_options_]"],["DESCRIPTION","**atuin** replaces your shell history with a SQLite database, providing powerful search, context tracking, and optional end-to-end encrypted sync across machines.\n\nIt records additional context like exit status, duration, working directory, and hostname, making history search more powerful than traditional Ctrl+R."],["PARAMETERS","**search** [_query_]\n> Interactive history search. Supports filtering by directory, session, host, or globally.\n\n**import** _source_\n> Import existing history (auto, bash, zsh, fish, nu, xonsh, resh, and others).\n\n**sync**\n> Synchronize history with the Atuin server (upload and download).\n\n**login**\n> Authenticate with sync server.\n\n**register**\n> Register a new account on the Atuin sync server.\n\n**logout**\n> Remove authentication.\n\n**key**\n> Display or set the encryption key used for end-to-end encrypted sync.\n\n**stats**\n> Show usage statistics and command frequency.\n\n**history** _subcommand_\n> History management (list, delete, etc.).\n\n**init** _shell_\n> Output shell setup code for bash, zsh, fish, or nu.\n\n**doctor**\n> Run diagnostics to check for common issues with the installation.\n\n**info**\n> Display system and configuration information.\n\n**dotfiles**\n> Manage and sync shell aliases and environment variables."],["SHELL INTEGRATION","Add to shell rc file:\n```bash\neval \"$(atuin init bash)\"  # or zsh, fish\n```\n\nOptionally disable specific keybindings:\n```bash\neval \"$(atuin init zsh --disable-up-arrow)\"\n```"],["CONFIGURATION","**~/.config/atuin/config.toml**\n> Main configuration file for search settings, sync options, database path, filter mode, search mode, and UI preferences."],["CAVEATS","Requires shell initialization code. Sync feature is optional (can be used fully offline). Database can grow large over time. Some shells require specific configuration for proper integration."],["HISTORY","**atuin** was created by Ellie Huxtable and released in **2021**, offering modern shell history with end-to-end encrypted sync, statistics, and improved search."],["INSTALL","```apt: sudo apt install atuin```\n\n```dnf: sudo dnf install atuin```\n\n```pacman: sudo pacman -S atuin```\n\n```apk: sudo apk add atuin```\n\n```zypper: sudo zypper install atuin```\n\n```brew: brew install atuin```\n\n```nix: nix profile install nixpkgs#atuin```"],["SEE ALSO","[history](/man/history)(1), [mcfly](/man/mcfly)(1), [hstr](/man/hstr)(1), [fzf](/man/fzf)(1), [bash](/man/bash)(1), [zsh](/man/zsh)(1)"]],"e":["history","mcfly","hstr","fzf","bash","zsh"],"x":"atuin search -i [query]"},"bcachefs-device":{"n":"bcachefs-device","t":"Manage devices in a bcachefs filesystem","s":[["TAGLINE","Manage devices in a bcachefs filesystem"],["TLDR","**Evacuate** data from a device before removing it\n\n```bcachefs device evacuate [path/to/device]```\n\n**Remove** a device from the filesystem permanently\n\n```bcachefs device remove [path/to/device]```\n\n**Add and format** a new device with a label\n\n```sudo bcachefs device add --label [group].[name] [path/to/mountpoint] [path/to/device]```"],["SYNOPSIS","**bcachefs device** _command_ [_options_] _device_"],["DESCRIPTION","**bcachefs device** manages devices within a running bcachefs filesystem. It allows adding new devices to expand storage, evacuating data before removal, and permanently removing devices.\n\nDevice labels organize storage into groups (like ssd or hdd) and individual device names, enabling tiered storage configurations."],["SUBCOMMANDS","**add**\n> Add a device to an existing filesystem\n\n**remove**\n> Remove a device from a filesystem\n\n**online**\n> Re-add a device to a running filesystem\n\n**offline**\n> Take a device offline without removing it\n\n**evacuate**\n> Move all data off of a given device\n\n**set-state** _new-state_ _device_\n> Set the device state to rw, ro, failed, or spare\n\n**resize**\n> Resize the filesystem on a device"],["PARAMETERS","**-l, --label** _label_\n> Assign a disk label to categorize the device (e.g., ssd.cache, hdd.bulk)\n\n**--fs_size** _size_\n> Size of filesystem to use on the device (add)\n\n**--bucket** _size_\n> Set the bucket size (add)\n\n**--discard**\n> Enable discards on the device (add)\n\n**-f, --force**\n> Force the operation even if data redundancy would be degraded, or use a device that appears already formatted\n\n**-F, --force-metadata**\n> Force removal even if some metadata could not be migrated (remove)\n\n**--force-if-data-lost**\n> Force a state change even if data will be lost (set-state)\n\n**-o, --offline**\n> Operate on an offline device (set-state)"],["CAVEATS","Removing devices may take considerable time depending on data volume. Ensure sufficient free space on remaining devices before evacuating. Device operations require the filesystem to be mounted."],["INSTALL","```dnf: sudo dnf install bcachefs-tools```\n\n```pacman: sudo pacman -S bcachefs-tools```\n\n```apk: sudo apk add bcachefs-tools```\n\n```nix: nix profile install nixpkgs#bcachefs-tools```"],["SEE ALSO","[bcachefs](/man/bcachefs)(8), [mkfs](/man/mkfs)(8), [fsck](/man/fsck)(8), [mount](/man/mount)(8)"],["RESOURCES","```[Homepage](https://bcachefs.org)```\n\n```[Source code](https://evilpiepirate.org/git/bcachefs-tools.git)```"]],"e":["bcachefs","mkfs","fsck","mount"],"x":"bcachefs device evacuate [path/to/device]"},"bg":{"n":"bg","t":"Resume suspended jobs in the background","s":[["TAGLINE","Resume suspended jobs in the background"],["TLDR","**Resume the most recently stopped job in the background**\n\n```bg```\n\n**Resume a specific job by number**\n\n```bg %[1]```\n\n**Resume a job whose command begins with a string**\n\n```bg %[string]```"],["SYNOPSIS","**bg** [_job-spec_]"],["DESCRIPTION","**bg** resumes suspended jobs in the background. It is a shell built-in command that continues execution of jobs that were stopped (typically with Ctrl+Z) while allowing continued use of the terminal. If no job is specified, the most recently stopped job is resumed.\n\nThe command is part of POSIX shell job control functionality and is available in bash, zsh, ksh, and other POSIX-compatible shells."],["PARAMETERS","_job-spec_\n> Job identifier (%1, %2, etc.)\n\nIf no job specified, bg operates on the most recently stopped job."],["JOB SPECIFICATIONS","**%n**\n> Job number n\n\n**%string**\n> Job whose command begins with string\n\n**%?string**\n> Job whose command contains string\n\n**%%** or **%+**\n> Current job\n\n**%-**\n> Previous job"],["WORKFLOW","```bash"],["Start long-running command","./long_process"],["Suspend with Ctrl+Z","[Ctrl+Z]"],["Resume in background","bg"],["Or resume specific job","bg %1"],["List jobs","jobs"],["Bring to foreground","fg %1\n```"],["CAVEATS","Only works with job control enabled shells. Backgrounded jobs may stop if they try to read from terminal. Output still goes to terminal unless redirected. Jobs terminate when shell exits unless using nohup or disown."],["HISTORY","**bg** has been part of job control in Unix shells since the C shell (csh) introduced the feature in the late **1970s**."],["SEE ALSO","[fg](/man/fg)(1), [jobs](/man/jobs)(1), [disown](/man/disown)(1), [nohup](/man/nohup)(1)"]],"e":["fg","jobs","disown","nohup"],"x":"bg"},"caddy":{"n":"caddy","t":"Web server with automatic HTTPS","s":[["TAGLINE","Web server with automatic HTTPS"],["TLDR","**Start** web server\n\n```caddy run```\n\n**Serve** current directory\n\n```caddy file-server```\n\n**Reverse** proxy\n\n```caddy reverse-proxy --from [example.com] --to [localhost:8080]```\n\n**Validate** Caddyfile\n\n```caddy validate```\n\n**Reload** configuration\n\n```caddy reload```"],["SYNOPSIS","**caddy** _command_ [_options_]"],["DESCRIPTION","**caddy** is a modern web server with automatic HTTPS. It obtains and renews TLS certificates automatically via Let's Encrypt, provides HTTP/2 and HTTP/3 support, and uses a simple configuration format.\n\nThe server is designed for ease of use while maintaining high performance and security."],["PARAMETERS","**run**\n> Start Caddy server\n\n**start**\n> Start Caddy in background\n\n**stop**\n> Stop Caddy server\n\n**reload**\n> Reload configuration\n\n**validate**\n> Validate Caddyfile\n\n**file-server**\n> Quick file server mode\n\n**reverse-proxy**\n> Quick reverse proxy mode\n\n**--config** _file_\n> Configuration file (default: Caddyfile)\n\n**--adapter** _name_\n> Config adapter (caddyfile, json)"],["CADDYFILE","Simple configuration:\n```\nexample.com {\n    root * /var/www/html\n    file_server\n}\n```\n\nReverse proxy:\n```\nexample.com {\n    reverse_proxy localhost:8080\n}\n```"],["FEATURES","- Automatic HTTPS (Let's Encrypt)\n- HTTP/2 and HTTP/3 support\n- Reverse proxy\n- Load balancing\n- File server\n- Template engine\n- Dynamic configuration via API\n- Plugin system"],["QUICK COMMANDS","```bash"],["Serve current directory","caddy file-server"],["Serve on specific port","caddy file-server --listen :8080"],["Reverse proxy","caddy reverse-proxy --from :80 --to :8080"],["Start with Caddyfile","caddy run"],["Validate config","caddy validate --config Caddyfile\n```"],["CONFIGURATION","**Caddyfile**\n> Default configuration file in the current directory, defining sites, routes, and server behavior"],["CAVEATS","Automatic HTTPS requires port 80/443 access. Let's Encrypt rate limits apply. Certificate storage needs disk space. Memory usage higher than nginx. Plugin ecosystem smaller than alternatives. Version 2.x not compatible with v1 configs."],["HISTORY","**Caddy** was created by Matt Holt in **2015** as a web server prioritizing ease of use with automatic HTTPS, with version 2.0 rewritten in **2019**."],["INSTALL","```apt: sudo apt install caddy```\n\n```dnf: sudo dnf install caddy```\n\n```pacman: sudo pacman -S caddy```\n\n```apk: sudo apk add caddy```\n\n```zypper: sudo zypper install caddy```\n\n```brew: brew install caddy```\n\n```nix: nix profile install nixpkgs#caddy```"],["SEE ALSO","[nginx](/man/nginx)(8), [apache2](/man/apache2)(8), [traefik](/man/traefik)(1)"],["RESOURCES","```[Source code](https://github.com/caddyserver/caddy)```\n\n```[Homepage](https://caddyserver.com/)```\n\n```[Documentation](https://caddyserver.com/docs/)```"]],"e":["nginx","apache2","traefik"],"x":"caddy run"},"diff-so-fancy":{"n":"diff-so-fancy","t":"human-readable git diff formatter","s":[["TAGLINE","human-readable git diff formatter"],["TLDR","**Pipe git diff** through diff-so-fancy\n\n```git diff | diff-so-fancy```\n\n**Configure git** to use diff-so-fancy\n\n```git config --global core.pager \"diff-so-fancy | less --tabs=4 -RFX\"```\n\n**Use as git diff tool**\n\n```git config --global interactive.diffFilter \"diff-so-fancy --patch\"```\n\n**Show file changes** with improved output\n\n```diff-so-fancy < [changes.diff]```"],["SYNOPSIS","**diff-so-fancy** [_options_]"],["DESCRIPTION","**diff-so-fancy** transforms git diff output into a more human-readable format. It improves the visual presentation of diffs with better line highlighting, cleaner headers, and removed chunk markers.\n\nThe tool uses terminal colors effectively to highlight what changed within lines, making code reviews faster and easier. It's designed as a git pager replacement."],["PARAMETERS","**--patch**\n> Use patch-compatible output mode.\n\n**--no-colors**\n> Disable color output.\n\n**--colors**\n> Enable color output.\n\n**--set-defaults**\n> Configure git to use diff-so-fancy."],["CONFIGURATION","**~/.gitconfig**\n> Git configuration for integrating diff-so-fancy as the default pager."],["GIT CONFIGURATION","```\ngit config --global core.pager \"diff-so-fancy | less --tabs=4 -RFX\"\ngit config --global color.ui true\ngit config --global color.diff-highlight.oldNormal \"red bold\"\ngit config --global color.diff-highlight.oldHighlight \"red bold 52\"\ngit config --global color.diff-highlight.newNormal \"green bold\"\ngit config --global color.diff-highlight.newHighlight \"green bold 22\"\n```"],["CAVEATS","Requires Perl. Modified output not suitable for patch application. Some themes may need color adjustment. Large diffs may be slower."],["HISTORY","**diff-so-fancy** was created by **Seth Vargo** and is maintained by **so-fancy**. It emerged from the desire for better diff visualization in terminals, building on git's diff-highlight script with additional formatting improvements."],["INSTALL","```dnf: sudo dnf install diff-so-fancy```\n\n```pacman: sudo pacman -S diff-so-fancy```\n\n```zypper: sudo zypper install diff-so-fancy```\n\n```brew: brew install diff-so-fancy```\n\n```nix: nix profile install nixpkgs#diff-so-fancy```"],["SEE ALSO","[git-diff](/man/git-diff)(1), [delta](/man/delta)(1), [colordiff](/man/colordiff)(1), [diff](/man/diff)(1)"],["RESOURCES","```[Source code](https://github.com/so-fancy/diff-so-fancy)```"]],"e":["git-diff","delta","colordiff","diff"],"x":"git diff | diff-so-fancy"},"diffoscope":{"n":"diffoscope","t":"deep recursive file comparison tool","s":[["TAGLINE","deep recursive file comparison tool"],["TLDR","**Compare two files**\n\n```diffoscope [file1] [file2]```\n\n**Compare and output HTML report**\n\n```diffoscope --html [report.html] [file1] [file2]```\n\n**Compare with limited depth**\n\n```diffoscope --max-diff-block-lines [100] [file1] [file2]```\n\n**Compare directories recursively**\n\n```diffoscope [dir1] [dir2]```\n\n**Output JSON report**\n\n```diffoscope --json [report.json] [file1] [file2]```\n\n**Compare Debian packages**\n\n```diffoscope [package1.deb] [package2.deb]```\n\n**Exclude specific file patterns**\n\n```diffoscope --exclude \"[*.pyc]\" [dir1] [dir2]```"],["SYNOPSIS","**diffoscope** [_options_] _path1_ _path2_"],["PARAMETERS","**--html** _file_\n> Write HTML report to file.\n\n**--text** _file_\n> Write text report to file.\n\n**--json** _file_\n> Write JSON report to file.\n\n**--max-diff-block-lines** _n_\n> Maximum lines per diff block.\n\n**--max-report-size** _bytes_\n> Maximum report size.\n\n**--exclude** _pattern_\n> Exclude files matching pattern.\n\n**--exclude-directory-metadata**\n> Ignore directory metadata differences.\n\n**--fuzzy-threshold** _n_\n> Fuzzy matching threshold (0-400).\n\n**--new-file**\n> Treat absent files as empty.\n\n**--no-default-limits**\n> Remove default size limits.\n\n**--diff-tool** _tool_\n> External diff tool to use."],["DESCRIPTION","**diffoscope** performs deep comparison of files, recursively unpacking archives and examining contents. Unlike simple diff, it understands file formats and shows meaningful differences inside compressed files, images, binaries, and more.\n\nThe tool supports hundreds of file formats: archives (tar, zip, deb, rpm), binaries (ELF, PE), images (PNG, JPEG), documents (PDF), and many others. It uses specialized tools to decode each format before comparison.\n\ndiffoscope was created to debug reproducible build issues, helping identify why two builds of the same source produce different binaries. Output can be terminal text, HTML for web viewing, or JSON for programmatic processing."],["CAVEATS","Full functionality requires many external tools (binutils, unzip, etc.). Large files or deep archives may be slow to process. Some proprietary formats aren't supported. Memory usage can be high for large comparisons."],["HISTORY","diffoscope was created by the **Reproducible Builds** project, specifically by **Lunar** and others, starting in **2014**. Originally named \"debbindiff\" for Debian package comparison, it was renamed and generalized. The tool is central to the reproducible builds effort, helping developers understand why builds differ and fix the sources of non-determinism."],["INSTALL","```dnf: sudo dnf install diffoscope```\n\n```pacman: sudo pacman -S diffoscope```\n\n```apk: sudo apk add diffoscope```\n\n```zypper: sudo zypper install diffoscope```\n\n```brew: brew install diffoscope```\n\n```nix: nix profile install nixpkgs#diffoscope```"],["SEE ALSO","[diff](/man/diff)(1), [cmp](/man/cmp)(1)"]],"e":["diff","cmp"],"x":"diffoscope [file1] [file2]"},"dmsetup":{"n":"dmsetup","t":"device-mapper low-level administration","s":[["TAGLINE","device-mapper low-level administration"],["TLDR","**List all device** mapper devices\n\n```dmsetup ls```\n\n**Show device status**\n\n```dmsetup status [device_name]```\n\n**Show detailed information**\n\n```dmsetup info```\n\n**Show device table**\n\n```dmsetup table [device_name]```\n\n**Create a device** mapper target\n\n```dmsetup create [name] --table \"[0 size linear /dev/sda 0]\"```\n\n**Remove a device**\n\n```dmsetup remove [device_name]```\n\n**Suspend a device**\n\n```dmsetup suspend [device_name]```"],["SYNOPSIS","**dmsetup** _command_ [_options_] [_device_name_]"],["PARAMETERS","_COMMAND_\n> Operation: ls, status, info, table, create, remove, etc.\n\n**ls**\n> List device mapper devices.\n\n**status** [_DEVICE_]\n> Show device status.\n\n**info** [_DEVICE_]\n> Show detailed information.\n\n**table** [_DEVICE_]\n> Show device table.\n\n**create** _NAME_\n> Create new device.\n\n**remove** _DEVICE_\n> Remove device.\n\n**suspend**/**resume** _DEVICE_\n> Suspend/resume device.\n\n**--table** _TABLE_\n> Device table specification."],["DESCRIPTION","**dmsetup** is the low-level administration tool for the Linux device-mapper. Device-mapper provides a generic way to create virtual block devices that map onto physical devices through various target types.\n\nThe tool can create, configure, and manage device-mapper devices including linear mappings, striped volumes, snapshots, mirrors, and more. It's the foundation underlying LVM, dm-crypt, and other Linux storage technologies.\n\nWhile typically used indirectly through LVM or cryptsetup, dmsetup provides direct access for debugging, custom configurations, or understanding device-mapper internals."],["CAVEATS","Requires root privileges. Incorrect use can cause data loss. Tables must specify valid targets and parameters. Some operations require suspended devices."],["HISTORY","dmsetup is part of the **device-mapper** component of **LVM2**, developed primarily by **Red Hat** and **Sistina Software**. Device-mapper was merged into the Linux kernel in **2003**, providing the foundation for flexible storage management."],["INSTALL","```apt: sudo apt install dmsetup```\n\n```dnf: sudo dnf install device-mapper```\n\n```pacman: sudo pacman -S device-mapper```\n\n```apk: sudo apk add device-mapper```\n\n```zypper: sudo zypper install device-mapper```\n\n```brew: brew install device-mapper```"],["SEE ALSO","[lvm](/man/lvm)(8), [cryptsetup](/man/cryptsetup)(8), [dmraid](/man/dmraid)(8)"],["RESOURCES","```[Source code](https://gitlab.com/lvmteam/lvm2)```\n\n```[Homepage](https://sourceware.org/lvm2/)```"]],"e":["lvm","cryptsetup","dmraid"],"x":"dmsetup ls"},"dnf-module":{"n":"dnf-module","t":"manage modularity streams for packages","s":[["TAGLINE","manage modularity streams for packages"],["TLDR","View **modularity overview**\n\n```dnf module list```\n\nView modularity of **specific package**\n\n```dnf module list [package_name]```\n\n**Enable** a module stream\n\n```sudo dnf module enable [package_name]:[stream]```\n\n**Install** a specific module version\n\n```dnf module install [package_name]:[stream]```"],["SYNOPSIS","**dnf module** _command_ [_module:stream_]"],["DESCRIPTION","**dnf module** manages package modularity, allowing multiple versions of software to coexist in repositories. Modules group packages with a specific purpose, and streams represent different versions.\n\nModularity is used in RHEL 8+ and Fedora for software like Python, Node.js, and PHP where multiple versions need to be available."],["PARAMETERS","**list**\n> List available modules\n\n**enable** _module:stream_\n> Enable a module stream\n\n**disable** _module_\n> Disable a module\n\n**install** _module:stream_\n> Install module packages\n\n**remove** _module_\n> Remove module packages\n\n**reset** _module_\n> Reset module state"],["CAVEATS","Only one stream of a module can be active at a time. Enabling a stream doesn't install packages; use install for that. Available on RHEL 8+ and recent Fedora."],["INSTALL","```pacman: sudo pacman -S dnf```"],["SEE ALSO","[dnf](/man/dnf)(8), [dnf-install](/man/dnf-install)(8)"],["RESOURCES","```[Source code](https://github.com/rpm-software-management/dnf)```\n\n```[Documentation](https://dnf.readthedocs.io/en/latest/command_ref.html)```"]],"e":["dnf","dnf-install"],"x":"dnf module list"},"dvd+rw-booktype":{"n":"dvd+rw-booktype","t":"modify DVD booktype for compatibility","s":[["TAGLINE","modify DVD booktype for compatibility"],["TLDR","**Query current booktype** settings of the drive and loaded media\n\n```dvd+rw-booktype [/dev/dvd]```\n\nSet the booktype of the **currently loaded disc** to DVD-ROM\n\n```dvd+rw-booktype -dvd-rom-spec -media [/dev/dvd]```\n\nMake the drive **default to DVD-ROM booktype** for every future recording\n\n```dvd+rw-booktype -dvd-rom-spec -unit [/dev/dvd]```\n\nSet the drive default **only for DVD+R** recordings\n\n```dvd+rw-booktype -dvd-rom-spec -unit+r [/dev/dvd]```\n\nRestore the **native booktype** of the loaded DVD+RW disc\n\n```dvd+rw-booktype -dvd+rw-spec -media [/dev/dvd]```\n\n**Print drive inquiry** information\n\n```dvd+rw-booktype -inq [/dev/dvd]```"],["SYNOPSIS","**dvd+rw-booktype** [**-dvd-rom-spec**|**-dvd+rw-spec**|**-dvd+r-spec**|**-inq**] [**-media**|**-unit**|**-unit+rw**|**-unit+r**] _device_"],["PARAMETERS","_device_\n> DVD drive device path, e.g. /dev/dvd or /dev/sr0.\n\n**-dvd-rom-spec**\n> Use the DVD-ROM book type specification.\n\n**-dvd+rw-spec**\n> Use the native DVD+RW book type specification.\n\n**-dvd+r-spec**\n> Use the native DVD+R book type specification.\n\n**-inq**\n> Print drive inquiry information and exit.\n\n**-media**\n> Apply the chosen specification to the currently loaded disc.\n\n**-unit**\n> Store the chosen specification as the drive default for future recordings.\n\n**-unit+rw**\n> Store the drive default for DVD+RW recordings only.\n\n**-unit+r**\n> Store the drive default for DVD+R recordings only."],["DESCRIPTION","**dvd+rw-booktype** changes the book type field recorded on DVD+R and DVD+RW media. The book type is what a player reads to decide which kind of disc it is holding, so it directly affects playback compatibility.\n\nPresenting a DVD+R or DVD+RW disc as DVD-ROM makes many older standalone players and set-top boxes accept it, because those units predate the DVD+ formats. A specification flag chooses the identity to write; a target flag chooses whether it applies to the loaded disc (**-media**) or becomes the drive's default for future burns (**-unit** and its variants).\n\nRunning the command with only a device argument prints the current drive and media settings without changing anything."],["CAVEATS","Not every drive supports book type modification, and support often depends on the firmware. Changing the book type of a DVD+R disc is a one-way operation: the field is written into the disc's control data and cannot be rewritten. DVD+RW media can be changed repeatedly. The command does not affect DVD-R/-RW media at all."],["HISTORY","dvd+rw-booktype is part of the **dvd+rw-tools** package, developed for managing DVD+R/RW media on Linux. Booktype modification became important for compatibility when DVD+ format competed with DVD-."],["INSTALL","```apt: sudo apt install dvd+rw-tools```\n\n```dnf: sudo dnf install dvd+rw-tools```\n\n```pacman: sudo pacman -S dvd+rw-tools```\n\n```apk: sudo apk add dvd+rw-tools```\n\n```zypper: sudo zypper install dvd+rw-tools```\n\n```brew: brew install dvd+rw-tools```"],["SEE ALSO","[growisofs](/man/growisofs)(1), [dvd+rw-mediainfo](/man/dvd+rw-mediainfo)(1)"]],"e":["growisofs","dvd+rw-mediainfo"],"x":"dvd+rw-booktype [/dev/dvd]"},"esc-m":{"n":"esc-m","t":"Decode min12xxw printer driver output for debugging","s":[["TAGLINE","Decode min12xxw printer driver output for debugging"],["TLDR","**Decode a driver data stream** piped from stdin\n\n```min12xxw < [document.pbm] | esc-m```\n\n**Inspect a captured** printer data file\n\n```esc-m < [captured.prn]```"],["SYNOPSIS","**esc-m**"],["DESCRIPTION","**esc-m** is a small debugging filter shipped with the **min12xxw** printer driver. It reads a driver data stream on standard input and prints a human-readable description of the ESC-M (compressed page) commands it contains to standard output.\n\nThe tool takes no arguments and no options: it is only meant to help developers and users understand what the min12xxw driver emits for a KonicaMinolta PagePro 1200W-class printer, for example when diagnosing a printing problem or verifying the compressed raster output."],["CAVEATS","esc-m is a diagnostic aid, not a print command; it does not talk to a printer. It only understands the data format produced by the min12xxw driver, so feeding it arbitrary files produces meaningless output. It is packaged together with min12xxw (Debian's printer-driver-min12xxw) rather than as a standalone tool."],["INSTALL","```apt: sudo apt install printer-driver-min12xxw```"],["SEE ALSO","[min12xxw](/man/min12xxw)(1)"],["RESOURCES","```[Homepage](http://www.hinterbergen.de/mala/min12xxw/)```"]],"e":["min12xxw"],"x":"min12xxw < [document.pbm] | esc-m"},"fcitx5":{"n":"fcitx5","t":"modern input method framework","s":[["TAGLINE","modern input method framework"],["TLDR","**Start fcitx5** input method daemon\n\n```fcitx5```\n\n**Start in daemon mode** (background)\n\n```fcitx5 -d```\n\n**Start with verbose output** for debugging\n\n```fcitx5 -v```\n\n**Replace an existing fcitx5** instance\n\n```fcitx5 -r```\n\n**Display version** information\n\n```fcitx5 --version```"],["SYNOPSIS","**fcitx5** [**-drvh**] [**--replace**] [**--verbose**]"],["PARAMETERS","**-d**, **--daemon**\n> Run as a background daemon.\n\n**-r**, **--replace**\n> Replace an existing fcitx5 instance.\n\n**-v**, **--verbose**\n> Enable verbose output for debugging.\n\n**-h**, **--help**\n> Display help message.\n\n**--version**\n> Display version information."],["DESCRIPTION","**Fcitx5** is a flexible input method framework for Unix-like systems, primarily used for entering Chinese, Japanese, Korean (CJK), and other languages requiring complex input methods. It is the successor to Fcitx and features a modular, plugin-based architecture.\n\nThe framework provides a lightweight core with additional language support via addons. Input method engines for various languages can be installed separately (e.g., fcitx5-chinese-addons for Chinese, fcitx5-anthy for Japanese, fcitx5-hangul for Korean).\n\nFor proper operation, environment variables should be set in the desktop session:\n\n```\nXMODIFIERS=@im=fcitx\nGTK_IM_MODULE=fcitx\nQT_IM_MODULE=fcitx\n```\n\nFcitx5 supports both X11 and Wayland, with native Wayland text-input protocol support for better integration."],["CONFIGURATION","**~/.config/fcitx5/**\n> User configuration directory containing profile settings, input method configurations, and addon settings.\n\n**~/.local/share/fcitx5/**\n> User data directory for themes, dictionaries, and custom data.\n\n**/etc/xdg/fcitx5/**\n> System-wide configuration directory."],["CAVEATS","Different desktop environments may require different configuration methods for autostart and environment variables. On Wayland, the native text-input protocol is preferred over legacy input method modules. Some applications may require the GTK or Qt IM modules for proper input method support."],["HISTORY","Fcitx (Flexible Context Input X) was originally developed for Chinese input on Linux. **Fcitx5** is a complete rewrite released under LGPL-2.1+, offering improved architecture, better Wayland support, and enhanced extensibility. The modular design allows input methods, themes, and features to be added as independent plugins."],["INSTALL","```apt: sudo apt install fcitx5```\n\n```dnf: sudo dnf install fcitx5```\n\n```pacman: sudo pacman -S fcitx5```\n\n```apk: sudo apk add fcitx5```\n\n```zypper: sudo zypper install fcitx5```\n\n```nix: nix profile install nixpkgs#fcitx5```"],["SEE ALSO","[ibus](/man/ibus)(1)"]],"e":["ibus"],"x":"fcitx5"},"fs_cli":{"n":"fs_cli","t":"FreeSWITCH command-line interface","s":[["TAGLINE","FreeSWITCH command-line interface"],["TLDR","Connect to the **local FreeSWITCH** instance with an interactive session\n\n```fs_cli```\n\nConnect to a **remote FreeSWITCH** server\n\n```fs_cli -H [host] -P [port] -p [password]```\n\nExecute a **single command** and exit\n\n```fs_cli -x \"[command]\"```\n\nShow FreeSWITCH **system status**\n\n```fs_cli -x \"status\"```\n\n**Reload** FreeSWITCH XML configuration\n\n```fs_cli -x \"reloadxml\"```\n\nCheck if a **module is loaded**\n\n```fs_cli -x \"module_exists [module_name]\"```\n\nShow **active calls**\n\n```fs_cli -x \"show calls\"```\n\n**Retry** connection on failure\n\n```fs_cli -r```"],["SYNOPSIS","**fs_cli** [_options_]"],["PARAMETERS","**-H**, **--host** _HOST_\n> Connect to remote FreeSWITCH server at _HOST_\n\n**-P**, **--port** _PORT_\n> Connect to Event Socket on _PORT_ (default: 8021)\n\n**-p**, **--password** _PASSWORD_\n> Event Socket password (default: ClueCon)\n\n**-x**, **--execute** _COMMAND_\n> Execute command and exit\n\n**-r**, **--retry**\n> Retry connection on failure\n\n**-l**, **--loglevel** _LEVEL_\n> Set log level (0-7)\n\n**-q**, **--quiet**\n> Disable logging to console\n\n**-d**, **--debug** _LEVEL_\n> Debug level (0-7)"],["CONFIGURATION","**/etc/freeswitch/autoload_configs/event_socket.conf.xml**\n> Event Socket Layer configuration controlling the listen address, port, and authentication password for ESL connections."],["DESCRIPTION","**fs_cli** is the FreeSWITCH command-line interface client that connects to a running FreeSWITCH server via the Event Socket Layer (ESL). It provides interactive access to FreeSWITCH's API for managing calls, configurations, and system status.\n\nThe tool can operate in interactive mode for real-time control or execute single commands via the **-x** option for scripting and automation. It connects to the ESL port (default 8021) using configured credentials.\n\nCommon operations include checking call status, reloading configurations, managing channels, and monitoring system health."],["CAVEATS","Requires the Event Socket module to be loaded and properly configured in FreeSWITCH. Default password \"ClueCon\" should be changed in production. Network access to ESL port must be properly secured."],["HISTORY","fs_cli is part of the FreeSWITCH project, an open-source communications platform started in **2006** by Anthony Minessale II. It was created as a modern, scalable alternative to Asterisk for building telephony applications."],["INSTALL","```apk: sudo apk add freeswitch```\n\n```brew: brew install freeswitch```\n\n```nix: nix profile install nixpkgs#freeswitch```"],["SEE ALSO","[asterisk](/man/asterisk)(8)"]],"e":["asterisk"],"x":"fs_cli"},"ganache":{"n":"ganache","t":"personal Ethereum blockchain for development","s":[["TAGLINE","personal Ethereum blockchain for development"],["TLDR","**Start Ganache CLI**\n\n```ganache```\n\n**Start on specific port**\n\n```ganache -p [8545]```\n\n**Start with specific accounts**\n\n```ganache -a [20]```\n\n**Start with mnemonic**\n\n```ganache -m \"[word1 word2 ... word12]\"```\n\n**Fork mainnet**\n\n```ganache -f [https://mainnet.infura.io/v3/KEY]```\n\n**Start with deterministic accounts**\n\n```ganache -d```"],["SYNOPSIS","**ganache** [_options_]"],["PARAMETERS","**-p**, **--port** _port_\n> Port to listen on (default 8545).\n\n**-h**, **--host** _host_\n> Hostname (default 127.0.0.1).\n\n**-a**, **--accounts** _num_\n> Number of accounts to generate (default: 10).\n\n**-e**, **--defaultBalanceEther** _amount_\n> Default account balance in ether (default: 100).\n\n**-m**, **--mnemonic** _phrase_\n> BIP39 mnemonic for HD wallet.\n\n**-s**, **--seed** _seed_\n> Seed to use for HD wallet account generation.\n\n**-d**, **--deterministic**\n> Generate deterministic addresses and mnemonic.\n\n**-n**, **--secure**\n> Lock available accounts by default.\n\n**-u**, **--unlock** _accounts_\n> Specify accounts to unlock.\n\n**-f**, **--fork** _url_\n> Fork from network at URL.\n\n**-b**, **--blockTime** _seconds_\n> Block time for auto-mining (default: 0, instant mining).\n\n**-g**, **--gasPrice** _price_\n> Gas price in wei (default: 20000000000).\n\n**-l**, **--gasLimit** _limit_\n> Block gas limit (default: 6721975).\n\n**-i**, **--networkId** _id_\n> Network ID.\n\n**--chain.chainId** _id_\n> Chain ID.\n\n**--db** _path_\n> Directory to save chain database for persistence.\n\n**--verbose**\n> Log all incoming requests to stdout.\n\n**--allowUnlimitedContractSize**\n> Allow unlimited contract sizes for debugging."],["DESCRIPTION","**Ganache** is a personal Ethereum blockchain for development. It simulates full client behavior, providing instant transaction mining, configurable accounts, and blockchain forking.\n\nDevelopers use Ganache to test smart contracts locally before deploying to testnets or mainnet. It integrates with Truffle, Hardhat, and other development frameworks."],["CAVEATS","Not for production. Behavior may differ from mainnet. State is lost on restart (use --db for persistence). Forking requires archive node access. **Ganache was deprecated in December 2023** when Consensys sunset both Truffle and Ganache. Developers are encouraged to migrate to Hardhat or Foundry."],["HISTORY","Ganache was developed by **Truffle Suite** (part of Consensys). It evolved from TestRPC and was renamed Ganache around **2017**. The current version (Ganache 7+) was a complete rewrite supporting forking and improved compatibility. In **September 2023**, Consensys announced the sunset of both Truffle and Ganache, with codebases archived in December 2023."],["SEE ALSO","[hardhat](/man/hardhat)(1), [truffle](/man/truffle)(1), [geth](/man/geth)(1)"]],"e":["hardhat","truffle","geth"],"x":"ganache"},"gh-agent":{"n":"gh-agent","t":"GitHub Copilot CLI custom agent invocation","s":[["TAGLINE","GitHub Copilot CLI custom agent invocation"],["TLDR","**Start interactive Copilot CLI session with a custom agent**\n\n```gh copilot --agent [agent_name]```\n\n**Run a custom agent with a prompt non-interactively**\n\n```gh copilot --agent [security-auditor] --prompt \"[Check /src/app/validator.go]\"```\n\n**Invoke an agent interactively using the slash command**\n\n```/agent [agent_name]```\n\n**Delegate a task to Copilot coding agent**\n\n```/delegate [task description]```"],["SYNOPSIS","**gh** **copilot** **--agent** _name_ [**--prompt** _text_] [_options_]"],["PARAMETERS","**--agent** _name_\n> Specify the custom agent to use (matches the .agent.md filename).\n\n**--prompt** _text_\n> Provide a prompt for programmatic (non-interactive) usage.\n\n**--allow-tool** _tool_\n> Specify tools the agent can use (shell, write, url, MCP server tools)."],["DESCRIPTION","**gh agent** functionality is provided through GitHub Copilot CLI's custom agent system, accessible via `gh copilot --agent` or the `/agent` slash command in an interactive session. Agents are defined by Markdown files with an `.agent.md` extension that specify the agent's name, description, prompt instructions, and available tools.\n\nCustom agents can be defined at user level (~/.copilot/agents/), repository level (.github/agents/), or organization level ({org}/.github/agents/). In case of naming conflicts, user-level agents override repository-level, which override organization-level.\n\nThe `/delegate` slash command commits unstaged changes to a new branch and delegates the task to the Copilot coding agent, which opens a draft pull request and works in the background."],["INSTALL","```apt: sudo apt install gh```\n\n```dnf: sudo dnf install gh```\n\n```pacman: sudo pacman -S github-cli```\n\n```apk: sudo apk add github-cli```\n\n```zypper: sudo zypper install gh```\n\n```brew: brew install gh```\n\n```nix: nix profile install nixpkgs#gh```"],["SEE ALSO","[gh-copilot](/man/gh-copilot)(1), [gh](/man/gh)(1)"]],"e":["gh-copilot","gh"],"x":"gh copilot --agent [agent_name]"},"ghost":{"n":"ghost","t":"Command-line interface for the Ghost publishing platform","s":[["TAGLINE","Command-line interface for the Ghost publishing platform"],["TLDR","**Start Ghost in development**\n\n```ghost start```\n\n**Start in production**\n\n```ghost start --production```\n\n**Stop Ghost**\n\n```ghost stop```\n\n**Restart Ghost**\n\n```ghost restart```\n\n**Check status**\n\n```ghost status```\n\n**Install Ghost**\n\n```ghost install```\n\n**Update Ghost**\n\n```ghost update```"],["SYNOPSIS","**ghost** _command_ [_options_]"],["SUBCOMMANDS","**install**\n> Install Ghost.\n\n**start**\n> Start Ghost.\n\n**stop**\n> Stop Ghost.\n\n**restart**\n> Restart Ghost.\n\n**status**\n> Show status.\n\n**update**\n> Update Ghost.\n\n**setup**\n> Configure Ghost.\n\n**config**\n> View/set configuration.\n\n**log**\n> View logs.\n\n**doctor**\n> Check installation.\n\n**run**\n> Start Ghost without process manager (for debugging).\n\n**backup**\n> Create a zip backup of site data.\n\n**uninstall**\n> Remove Ghost installation and data."],["PARAMETERS","**--production**\n> Run in production mode.\n\n**-D**, **--development**\n> Run in development mode.\n\n**-d**, **--dir** _path_\n> Installation directory.\n\n**--enable**\n> Restart Ghost automatically on server reboot.\n\n**--no-prompt**\n> Skip confirmation prompts."],["DESCRIPTION","**ghost** is the command-line interface for Ghost, a modern open-source publishing platform built on Node.js. It provides complete lifecycle management for Ghost installations, including initial setup, updates, configuration, and process management.\n\nThe CLI automates database setup, SSL certificate installation, systemd service configuration, and nginx proxy setup. It handles migrations during updates and provides diagnostic tools through the doctor command."],["CONFIGURATION","**config.production.json**\n> Production environment configuration for database, URL, mail, and server settings.\n\n**config.development.json**\n> Development environment configuration."],["INSTALL","```nix: nix profile install nixpkgs#ghost```"],["SEE ALSO","[node](/man/node)(1), [npm](/man/npm)(1), [nginx](/man/nginx)(1), [systemctl](/man/systemctl)(1)"]],"e":["node","npm","nginx","systemctl"],"x":"ghost start"},"git-diff-index":{"n":"git-diff-index","t":"Compare tree to working tree or index","s":[["TAGLINE","Compare tree to working tree or index"],["TLDR","**Compare index to commit**\n\n```git diff-index HEAD```\n\n**Compare working tree to commit**\n\n```git diff-index [commit]```\n\n**Check for differences**\n\n```git diff-index --quiet HEAD```\n\n**Raw output format**\n\n```git diff-index --raw HEAD```"],["SYNOPSIS","**git** **diff-index** [_options_] _tree-ish_ [_files_...]"],["PARAMETERS","**--cached**\n> Compare tree to index only, instead of the working tree.\n\n**-m**\n> Treat files not checked out (present in the index but missing from the working tree) as modified rather than deleted.\n\n**--merge-base**\n> Use the merge base of _tree-ish_ and HEAD as the starting point for comparison.\n\n**--raw**\n> Raw diff output (this is the default for this command).\n\n**--quiet**\n> Exit with 1 if differences.\n\n**--name-only**\n> Show file names only.\n\n**--name-status**\n> Show file names and status."],["DESCRIPTION","**git diff-index** compares a tree object to the working tree or index, operating as a low-level plumbing command used internally by git diff for commit comparisons. It accepts a tree-ish (commit, branch, tag, or tree object) and compares it against either the index (with --cached) or the working tree.\n\nThe tool is particularly useful in automation scenarios that need to detect whether files have changed since a specific commit, such as CI/CD pipelines checking for uncommitted modifications or scripts validating clean working directories. Its --quiet flag enables simple boolean checks for changes without processing full diff output."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-diff](/man/git-diff)(1), [git-diff-tree](/man/git-diff-tree)(1)"]],"e":["git-diff","git-diff-tree"],"x":"git diff-index HEAD"},"git-effort":{"n":"git-effort","t":"Display commit activity and development effort per file","s":[["TAGLINE","Display commit activity and development effort per file"],["TLDR","**Show effort for all files**\n\n```git effort```\n\n**Show effort for a specific directory**\n\n```git effort [src/]```\n\n**Only show files with more than N commits**\n\n```git effort --above [50]```\n\n**Show effort for a specific file**\n\n```git effort [path/to/file.js]```"],["SYNOPSIS","**git effort** [_options_] [_path_]"],["PARAMETERS","_PATH_\n> File or directory path to analyze. When omitted, analyzes the entire repository.\n\n**--above** _N_\n> Only show files with more than N commits. Useful for filtering out low-activity files.\n\n**--help**\n> Display help information."],["DESCRIPTION","**git effort** displays commit activity for files over time, showing which files receive the most attention through commit frequency and active days. The output ranks files by commit count and active days, helping identify hotspots and understand maintenance patterns in the codebase.\n\nPart of the git-extras suite, it reveals which areas of the codebase receive ongoing development effort, useful for planning code reviews and understanding project dynamics."],["CAVEATS","Part of git-extras package. Can be slow on large repos. Measures commits, not lines changed."],["HISTORY","git effort is part of **git-extras**, created by **TJ Holowaychuk** to visualize development effort distribution."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-fame](/man/git-fame)(1), [git-summary](/man/git-summary)(1), [git-extras](/man/git-extras)(1), [git-shortlog](/man/git-shortlog)(1)"]],"e":["git-fame","git-summary","git-extras","git-shortlog"],"x":"git effort"},"gping":{"n":"gping","t":"Graphical ping with live terminal graph","s":[["TAGLINE","Graphical ping with live terminal graph"],["TLDR","**Ping a host with graph**\n\n```gping [example.com]```\n\n**Ping multiple hosts**\n\n```gping [example.com] [google.com] [cloudflare.com]```\n\n**Ping with custom interval**\n\n```gping -i [0.5] [example.com]```\n\n**Execute command and graph results**\n\n```gping --cmd \"[curl -so /dev/null -w '%{time_total}' example.com]\"```\n\n**Ping with custom buffer size**\n\n```gping -b [100] [example.com]```\n\n**Force IPv4**\n\n```gping -4 [example.com]```\n\n**Set graph color**\n\n```gping --color [blue] [example.com]```\n\n**Use simple graphics mode** (for terminals without braille support)\n\n```gping -s [example.com]```"],["SYNOPSIS","**gping** [_options_] _host_..."],["PARAMETERS","**-i**, **--interval** _seconds_\n> Ping interval. Default: 0.2.\n\n**-b**, **--buffer** _size_\n> Number of data points in graph. Default: 300.\n\n**-n**, **--watch-interval** _seconds_\n> Interval for command execution graph.\n\n**-s**, **--simple-graphics**\n> Use dot character instead of braille.\n\n**--vertical-margin** _lines_\n> Vertical margin above graph.\n\n**--horizontal-margin** _cols_\n> Horizontal margin beside graph.\n\n**-c**, **--color** _color_\n> Graph line color (repeatable).\n\n**-4**\n> Force IPv4.\n\n**-6**\n> Force IPv6.\n\n**--cmd** _command_\n> Execute command and graph output as float.\n\n**-h**, **--help**\n> Display help information.\n\n**-V**, **--version**\n> Display version information."],["COLORS","red, green, blue, yellow, cyan, magenta, white, black"],["DESCRIPTION","**gping** is a graphical ping tool that displays latency as a live updating graph in the terminal. Multiple hosts can be pinged simultaneously, each displayed with different colors for easy comparison.\n\nThe **--cmd** option executes arbitrary commands and graphs their numeric output, useful for graphing any time-series metric like response times or resource usage. Simple graphics mode (**-s**) uses basic characters for terminals lacking Unicode braille support."],["CAVEATS","Requires ICMP permissions (may need root/sudo on Linux). Terminal must support 256 colors for best display. Some terminals may not render braille characters correctly."],["HISTORY","gping was created by **orf** and first released around **2020**. Written in Rust, it was designed as a modern alternative to traditional ping with visual feedback. The project gained popularity for its intuitive display and multi-host comparison capability."],["INSTALL","```apt: sudo apt install gping```\n\n```pacman: sudo pacman -S gping```\n\n```apk: sudo apk add gping```\n\n```zypper: sudo zypper install gping```\n\n```brew: brew install gping```\n\n```nix: nix profile install nixpkgs#gping```"],["SEE ALSO","[ping](/man/ping)(1), [mtr](/man/mtr)(1), [trippy](/man/trippy)(1), [fping](/man/fping)(1)"]],"e":["ping","mtr","trippy","fping"],"x":"gping [example.com]"},"hg-clone":{"n":"hg-clone","t":"creates a copy of a Mercurial repository","s":[["TAGLINE","creates a copy of a Mercurial repository"],["TLDR","**Clone repository**\n\n```hg clone [url]```\n\n**Clone to specific directory**\n\n```hg clone [url] [directory]```\n\n**Clone specific branch**\n\n```hg clone -b [branch] [url]```\n\n**Clone with specific revision**\n\n```hg clone -r [revision] [url]```\n\n**Clone without working directory**\n\n```hg clone -U [url]```"],["SYNOPSIS","**hg clone** [_options_] _source_ [_dest_]"],["PARAMETERS","_SOURCE_\n> Repository URL or path.\n\n_DEST_\n> Destination directory.\n\n**-b**, **--branch** _BRANCH_\n> Clone specific branch.\n\n**-r**, **--rev** _REV_\n> Clone up to revision.\n\n**-U**, **--noupdate**\n> Clone without working directory.\n\n**-u**, **--updaterev** _REV_\n> Check out the given revision, tag, or branch instead of the tip.\n\n**--pull**\n> Use pull protocol to copy metadata.\n\n**--stream**\n> Clone with minimal data processing (faster, but skips revlog delta reconstruction).\n\n**-e**, **--ssh** _CMD_\n> Specify ssh command to use.\n\n**--insecure**\n> Do not verify the server certificate.\n\n**--help**\n> Display help information."],["DESCRIPTION","**hg clone** creates a copy of a Mercurial repository. It downloads the entire history and creates a working directory.\n\nThe command supports various protocols including SSH, HTTP, and local paths. It can clone specific branches or revisions."],["CAVEATS","Full history downloaded by default. Large repos take time. Network required for remote repos."],["HISTORY","Clone is a core **Mercurial** command since version 1.0, providing distributed repository copying."],["INSTALL","```apt: sudo apt install mercurial```\n\n```dnf: sudo dnf install mercurial```\n\n```pacman: sudo pacman -S mercurial```\n\n```apk: sudo apk add mercurial```\n\n```zypper: sudo zypper install mercurial```\n\n```brew: brew install mercurial```\n\n```nix: nix profile install nixpkgs#mercurial```"],["SEE ALSO","[hg](/man/hg)(1), [hg-pull](/man/hg-pull)(1), [hg-push](/man/hg-push)(1), [hg-init](/man/hg-init)(1)"],["RESOURCES","```[Source code](https://foss.heptapod.net/mercurial/mercurial-devel)```\n\n```[Documentation](https://wiki.mercurial-scm.org/)```"]],"e":["hg","hg-pull","hg-push","hg-init"],"x":"hg clone [url]"},"homeshick":{"n":"homeshick","t":"git-based dotfile synchronizer written in Bash","s":[["TAGLINE","git-based dotfile synchronizer written in Bash"],["TLDR","**Create** a new castle (dotfiles repository)\n\n```homeshick generate [castle_name]```\n\n**Track** a file in your castle\n\n```homeshick track [castle_name] [path/to/file]```\n\n**Go** to a castle directory\n\n```homeshick cd [castle_name]```\n\n**Clone** a castle from GitHub\n\n```homeshick clone [github_username]/[repository_name]```\n\n**Symlink** all files from a castle\n\n```homeshick link [castle_name]```\n\n**List** installed castles\n\n```homeshick list```\n\n**Update** all castles from their remotes\n\n```homeshick pull```"],["SYNOPSIS","**homeshick** [_options_] _command_ [_arguments_]"],["PARAMETERS","**generate** _CASTLE_ [_CASTLE_...]\n> Create a new castle (dotfiles repository)\n\n**track** _CASTLE_ _FILE_ [_FILE_...]\n> Move a file/folder into a castle and replace it with a symlink\n\n**cd** _CASTLE_\n> Change directory to a castle's repository, for running git commands directly\n\n**clone** _URL_...\n> Clone a castle from a Git repository (supports GitHub \"user/repo\" shorthand)\n\n**link**, **symlink** [_CASTLE_...]\n> Symlink all files in the given castles' (or all castles') home folders into $HOME\n\n**pull** [_CASTLE_...]\n> Run git pull for the given castles (or all castles) and their submodules\n\n**check**, **updates** [_CASTLE_...]\n> Check whether castles are behind, ahead, or up to date with their remote\n\n**refresh**\n> Check remotes for updates, skipping castles checked within the last week\n\n**list**, **ls**\n> List all installed castles\n\n**-q**, **--quiet**\n> Suppress status output except when input is required\n\n**-s**, **--skip**\n> Skip conflicting files in $HOME instead of overwriting them\n\n**-f**, **--force**\n> Overwrite conflicting files in $HOME automatically\n\n**-b**, **--batch**\n> Run non-interactively, accepting default answers\n\n**-v**, **--verbose**\n> Print additional diagnostic output"],["DESCRIPTION","**homeshick** is a Git-based dotfile synchronizer written in Bash. It uses \"castles\" (Git repositories) to manage dotfiles, creating symlinks from the repository to your home directory.\n\nEach castle is a Git repository stored in **~/.homesick/repos/**. Files tracked in a castle (with **track**) are moved into the repository and replaced with symlinks pointing back to the repository copy. This allows version control and easy synchronization across machines. Multiple castles can be installed side by side, letting you combine shared dotfile collections (e.g. an oh-my-zsh castle) with your own."],["CAVEATS","Requires Bash 3+ and Git 1.5+. Symlinks may not work correctly on some filesystems or in containers. Conflicts can arise when multiple castles contain the same file; use **-f/--force** or **-s/--skip** to resolve them non-interactively."],["HISTORY","homeshick was created by **Anders Ingemann**, with the repository dating to **2012**, as a pure Bash alternative to homesick (a Ruby gem). The name combines \"home\" (dotfiles) with \"shell\" (Bash implementation)."],["INSTALL","```zypper: sudo zypper install homeshick```\n\n```brew: brew install homeshick```"],["SEE ALSO","[chezmoi](/man/chezmoi)(1), [stow](/man/stow)(8), [git](/man/git)(1)"],["RESOURCES","```[Source code](https://github.com/andsens/homeshick)```\n\n```[Documentation](https://github.com/andsens/homeshick/wiki)```"]],"e":["chezmoi","stow","git"],"x":"homeshick generate [castle_name]"},"ispell":{"n":"ispell","t":"interactive spell checker","s":[["TAGLINE","interactive spell checker"],["TLDR","**Check file interactively**\n\n```ispell [file.txt]```\n\n**Check from stdin**\n\n```echo \"wrods\" | ispell -a```\n\n**List misspellings**\n\n```ispell -l < [file.txt]```\n\n**Use specific dictionary**\n\n```ispell -d [american] [file.txt]```\n\n**Check TeX/LaTeX file**\n\n```ispell -t [document.tex]```\n\n**Expand affix flags** from stdin\n\n```echo \"[BOTH/R]\" | ispell -e```"],["SYNOPSIS","**ispell** [_options_] _file_..."],["PARAMETERS","**-a**\n> Pipe mode for programs.\n\n**-l**\n> List misspelled words only.\n\n**-d** _DICT_\n> Use specific dictionary.\n\n**-t**\n> TeX/LaTeX mode.\n\n**-n**\n> nroff/troff mode.\n\n**-H**\n> HTML mode.\n\n**-b**\n> Create a backup file (.bak) of the input file.\n\n**-x**\n> Don't create a backup file.\n\n**-B**\n> Report run-together words as spelling errors.\n\n**-C**\n> Consider run-together words as legal compounds.\n\n**-M**\n> Display a one-line mini-menu of options at the bottom of the screen.\n\n**-N**\n> Suppress the mini-menu.\n\n**-c**\n> Suggest corrections for words read from stdin.\n\n**-e**[_1-5_]\n> Expand affix flags from stdin. Optional level controls output detail.\n\n**-S**\n> Sort the list of guesses by probable correctness.\n\n**-W** _N_\n> Specify length of words to consider always legal (default 3).\n\n**-p** _FILE_\n> Personal dictionary file.\n\n**-L** _LINES_\n> Number of context lines to display."],["DESCRIPTION","**ispell** is an interactive spell checker. It highlights misspellings and offers corrections for text files. In interactive mode, each misspelled word is displayed with context, and the user can choose a suggested correction, type a replacement, or add the word to a personal dictionary.\n\nThe tool supports multiple languages and input formats including plain text, TeX/LaTeX, nroff/troff, and HTML. It can run interactively or in pipe mode (-a) for scripting and integration with other programs."],["CAVEATS","Largely replaced by aspell/hunspell. Dictionary availability varies. Format-specific modes needed."],["HISTORY","ispell was created by **Ralph E. Gorin** at Stanford in 1971, later rewritten as International Ispell by **Geoff Kuenning**."],["INSTALL","```apt: sudo apt install ispell```\n\n```pacman: sudo pacman -S ispell```\n\n```apk: sudo apk add aspell-compat```\n\n```zypper: sudo zypper install ispell```\n\n```brew: brew install ispell```\n\n```nix: nix profile install nixpkgs#ispell```"],["SEE ALSO","[aspell](/man/aspell)(1), [hunspell](/man/hunspell)(1), [spell](/man/spell)(1)"]],"e":["aspell","hunspell","spell"],"x":"ispell [file.txt]"},"jtbl":{"n":"jtbl","t":"converts JSON to ASCII tables for terminal display","s":[["TAGLINE","converts JSON to ASCII tables for terminal display"],["TLDR","**Display JSON as table**\n\n```cat [data.json] | jtbl```\n\n**Truncate long values**\n\n```cat [data.json] | jtbl -t```\n\n**No headers**\n\n```cat [data.json] | jtbl -n```\n\n**Markdown table**\n\n```cat [data.json] | jtbl -m```\n\n**Wrap long text** at column count\n\n```cat [data.json] | jtbl -w [80]```\n\n**Rotate output** (one row per page, columns become rows)\n\n```cat [data.json] | jtbl -r```"],["SYNOPSIS","**jtbl** [_options_]"],["PARAMETERS","**-t**, **--truncate**\n> Truncate long cell values to fit terminal width.\n\n**-n**, **--no-headers**\n> Don't print column headers.\n\n**-m**, **--markdown**\n> Output as a Markdown table.\n\n**-c**, **--csv**\n> Output as CSV.\n\n**-H**, **--html**\n> Output as an HTML table.\n\n**-r**, **--rotate**\n> Rotate output so columns become rows (useful for wide tables).\n\n**-w** _COLS_, **--wrap**=_COLS_\n> Wrap long cell values at the specified column count.\n\n**-q**, **--quiet**\n> Suppress warning messages.\n\n**-v**, **--version**\n> Show version.\n\n**--help**\n> Display help information."],["DESCRIPTION","**jtbl** converts JSON to ASCII tables for terminal display. It formats JSON arrays as readable tables.\n\nThe tool pairs well with jc for displaying command output. It supports Markdown output for documentation."],["CAVEATS","Expects array of objects. Python-based. Works with jc output."],["HISTORY","jtbl was created by **Kelly Brazil** as a companion to jc for displaying JSON data as formatted tables."],["INSTALL","```brew: brew install jtbl```\n\n```nix: nix profile install nixpkgs#jtbl```"],["SEE ALSO","[jc](/man/jc)(1), [jq](/man/jq)(1), [column](/man/column)(1)"]],"e":["jc","jq","column"],"x":"cat [data.json] | jtbl"},"kreadconfig5":{"n":"kreadconfig5","t":"reads values from KDE Plasma 5 configuration files","s":[["TAGLINE","reads values from KDE Plasma 5 configuration files"],["TLDR","Read a key from **global configuration**\n\n```kreadconfig5 --group [group_name] --key [key_name]```\n\nRead a key from **specific file**\n\n```kreadconfig5 --file [path/to/file] --group [group_name] --key [key_name]```\n\nCheck **systemd boot** setting\n\n```kreadconfig5 --file startkderc --group General --key systemdBoot```"],["SYNOPSIS","**kreadconfig5** [_options_]"],["PARAMETERS","**--file** _FILE_\n> Configuration file to read from\n\n**--group** _GROUP_\n> Configuration group (section)\n\n**--key** _KEY_\n> Key name to read\n\n**--default** _VALUE_\n> Default value if key doesn't exist"],["DESCRIPTION","**kreadconfig5** reads values from KDE Plasma 5 configuration files. KConfig is KDE's configuration system, storing settings in INI-style files with groups and key-value pairs.\n\nConfiguration files are typically stored in ~/.config/ with names like kdeglobals, kwinrc, plasmarc, etc. The tool is useful for scripting and automation involving KDE settings."],["CAVEATS","KDE 5 specific; KDE 6 uses kreadconfig6. File paths can be relative to ~/.config or absolute. Groups and keys are case-sensitive."],["HISTORY","kreadconfig5 is part of the KDE Frameworks, providing command-line access to KDE's configuration system. It complements kwriteconfig5 for reading settings in scripts."],["INSTALL","```apt: sudo apt install libkf5config-bin```\n\n```pacman: sudo pacman -S kconfig5```\n\n```apk: sudo apk add kconfig5```"],["SEE ALSO","[kwriteconfig5](/man/kwriteconfig5)(1), [dconf](/man/dconf)(1)"]],"e":["kwriteconfig5","dconf"],"x":"kreadconfig5 --group [group_name] --key [key_name]"},"maildir":{"n":"maildir","t":"mail storage format where each email is stored as a separate file in","s":[["TAGLINE","mail storage format where each email is stored as a separate file in"],["TLDR","**Create Maildir structure**\n\n```mkdir -p [~/Maildir]/{cur,new,tmp}```\n\n**List new messages**\n\n```ls [~/Maildir/new/]```\n\n**Move message to read**\n\n```mv [~/Maildir/new/message] [~/Maildir/cur/]```\n\n**Check Maildir permissions**\n\n```ls -la [~/Maildir/]```"],["SYNOPSIS","**Maildir/** - Directory-based email storage format"],["DESCRIPTION","**Maildir** is a mail storage format where each email is stored as a separate file in a directory structure. It consists of three subdirectories: cur (read), new (unread), and tmp (in-progress delivery).\n\nMaildir avoids file locking issues of mbox format and allows concurrent access from multiple programs."],["DIRECTORY STRUCTURE","```\nMaildir/\n├── cur/     # Read messages\n├── new/     # Unread messages\n├── tmp/     # Temporary (delivery)\n└── .Folder/ # Subfolders (ISTRSTRSTRSTRSTRSTRSTRSTRSTRSTRSTR\n    ├── cur/\n    ├── new/\n    └── tmp/\n```"],["FILE NAMING","```"],["Format: time.pid.hostname:2,flags","1234567890.12345.hostname:2,S\n\nFlags: S=Seen, R=Replied, F=Flagged, T=Trashed, D=Draft\n```"],["CAVEATS","More files than mbox. Filesystem must support many small files. Some tools expect specific naming. Subfolders prefixed with dot."],["HISTORY","Maildir was developed by **Daniel J. Bernstein** for his **qmail** MTA in **1995** as a reliable alternative to the traditional mbox format."],["SEE ALSO","[mutt](/man/mutt)(1), [dovecot](/man/dovecot)(1), [offlineimap](/man/offlineimap)(1)"]],"e":["mutt","dovecot","offlineimap"],"x":"mkdir -p [~/Maildir]/{cur,new,tmp}"},"mkfs.cramfs":{"n":"mkfs.cramfs","t":"creates a compressed ROM filesystem inside a partition","s":[["TAGLINE","creates a compressed ROM filesystem inside a partition"],["TLDR","Create **cramfs** from directory\n\n```sudo mkfs.cramfs [path/to/directory] /dev/sdXY```\n\nCreate with **volume name**\n\n```sudo mkfs.cramfs -n [volume_name] [path/to/directory] /dev/sdXY```"],["SYNOPSIS","**mkfs.cramfs** [_options_] _directory_ _device_"],["DESCRIPTION","**mkfs.cramfs** creates a compressed ROM filesystem (cramfs) inside a partition. Cramfs is a simple, read-only filesystem designed for small embedded systems and boot ROMs."],["PARAMETERS","**-n NAME**\n> Set volume name (max 16 characters)\n\n**-E**\n> Treat warnings as errors\n\n**-e EDITION**\n> Set edition number\n\n**-b BLOCKSIZE**\n> Block size (default: PAGE_SIZE)\n\n**-s, --sortbyname**\n> Sort directory entries by name"],["CAVEATS","Read-only filesystem. Maximum file size of 16MB. Maximum filesystem size of 256MB. Designed for embedded use."],["INSTALL","```apt: sudo apt install util-linux-extra```\n\n```apk: sudo apk add util-linux-misc```"],["SEE ALSO","[mkfs](/man/mkfs)(8), [mount](/man/mount)(8)"]],"e":["mkfs","mount"],"x":"sudo mkfs.cramfs [path/to/directory] /dev/sdXY"},"namespaces":{"n":"namespaces","t":"Overview of Linux kernel namespaces for resource isolation","s":[["TAGLINE","Overview of Linux kernel namespaces for resource isolation"],["TLDR","**List all namespaces**\n\n```lsns```\n\n**Enter namespace of process**\n\n```nsenter -t [PID] -a```\n\n**Create new mount namespace**\n\n```unshare --mount [command]```\n\n**Create new network namespace**\n\n```ip netns add [name]```\n\n**Run in new user namespace**\n\n```unshare --user --map-root-user [command]```"],["SYNOPSIS","**namespaces** - Linux kernel namespace overview"],["DESCRIPTION","**Namespaces** are a Linux kernel feature that wraps global system resources in an abstraction layer, making it appear to processes within a namespace that they have their own isolated instance of the resource. They are fundamental to container technologies like Docker and LXC.\n\nEach namespace type isolates a different aspect of the system."],["NAMESPACE TYPES","```\nmnt    - Mount points\npid    - Process IDs\nnet    - Network devices, stacks, ports\nipc    - System V IPC, POSIX message queues\nuts    - Hostname and NIS domain name\nuser   - User and group IDs\ncgroup - Cgroup root directory\ntime   - Boot and monotonic clocks\n```"],["RELATED TOOLS","```\nunshare   - Create new namespace\nnsenter   - Enter existing namespace\nlsns      - List namespaces\nip netns  - Network namespace management\n```"],["SYSTEM CALLS","```\nclone()    - Create process in new namespace\nunshare()  - Disassociate from current namespace\nsetns()    - Join an existing namespace\nioctl()    - Discover namespace relationships\n```"],["CAVEATS","Some operations require root/capabilities. Namespace limits exist. User namespaces have security implications."],["HISTORY","Namespaces were incrementally added to Linux starting with mount namespaces in kernel **2.4.19** (2002). Full container support matured around kernel **3.8** (2013)."],["SEE ALSO","[unshare](/man/unshare)(1), [nsenter](/man/nsenter)(1), [lsns](/man/lsns)(8), [clone](/man/clone)(2), [ip](/man/ip)(8), [cgroups](/man/cgroups)(7)"]],"e":["unshare","nsenter","lsns","clone","ip","cgroups"],"x":"lsns"},"npm-access":{"n":"npm-access","t":"manages access levels for published packages on the npm registry","s":[["TAGLINE","manages access levels for published packages on the npm registry"],["TLDR","**Set package to public**\n\n```npm access public [package]```\n\n**Set package to restricted**\n\n```npm access restricted [package]```\n\n**Grant read-only access**\n\n```npm access grant read-only [scope:team] [package]```\n\n**Grant read-write access**\n\n```npm access grant read-write [scope:team] [package]```\n\n**Revoke access**\n\n```npm access revoke [scope:team] [package]```\n\n**List package permissions**\n\n```npm access ls-packages [scope:team]```"],["SYNOPSIS","**npm access** _command_ [_options_]"],["PARAMETERS","**public**\n> Make package public.\n\n**restricted**\n> Make package private.\n\n**grant** _level_\n> Grant team access.\n\n**revoke**\n> Remove team access.\n\n**ls-packages**\n> List packages for user/team.\n\n**ls-collaborators**\n> List package collaborators."],["DESCRIPTION","**npm access** manages access levels for published packages on the npm registry. It controls whether packages are public or private and manages team permissions.\n\nThis command is relevant for npm organizations and scoped packages."],["Make scoped package public","npm access public @myorg/mypackage"],["Grant team access","npm access grant read-write @myorg:developers @myorg/mypackage"],["List collaborators","npm access ls-collaborators @myorg/mypackage\n```"],["CAVEATS","Requires authentication. Private packages need paid npm account. Scoped packages default to restricted."],["HISTORY","npm access was added to support npm Organizations and private packages, enabling team-based access control."],["SEE ALSO","[npm](/man/npm)(1), [npm-publish](/man/npm-publish)(1), [npm-owner](/man/npm-owner)(1)"],["INSTALL","```pacman: sudo pacman -S npm```\n\n```apk: sudo apk add npm```\n\n```brew: brew install npm```"]],"e":["npm","npm-publish","npm-owner"],"x":"npm access public [package]"},"npm-audit":{"n":"npm-audit","t":"scans for security vulnerabilities","s":[["TAGLINE","scans for security vulnerabilities"],["TLDR","**Audit dependencies**\n\n```npm audit```\n\n**Fix vulnerabilities**\n\n```npm audit fix```\n\n**Force fix (breaking changes)**\n\n```npm audit fix --force```\n\n**Output as JSON**\n\n```npm audit --json```\n\n**Audit production only**\n\n```npm audit --omit=dev```\n\n**Set minimum severity level**\n\n```npm audit --audit-level=[high]```\n\n**Dry run fix** (preview changes without applying)\n\n```npm audit fix --dry-run```"],["SYNOPSIS","**npm** **audit** [_options_]"],["PARAMETERS","**fix**\n> Automatically install compatible updates to fix vulnerabilities.\n\n**--force**\n> Force updates to latest version, even with breaking changes.\n\n**--json**\n> Output results in JSON format.\n\n**--omit** _TYPE_\n> Omit dependency type from audit (dev, optional, or peer). Replaces deprecated --production flag.\n\n**--audit-level** _LEVEL_\n> Minimum severity to trigger non-zero exit: low, moderate, high, critical.\n\n**--dry-run**\n> Preview what audit fix would change without applying.\n\n**--package-lock-only**\n> Only modify package-lock.json, skip node_modules updates.\n\n**--parseable**\n> Display vulnerabilities in parseable output format.\n\n**-h**, **--help**\n> Display help information."],["DESCRIPTION","**npm audit** scans project dependencies for known security vulnerabilities by checking them against the npm advisory database. It reports the severity level (low, moderate, high, critical), the vulnerable package, and the dependency path.\n\nThe **npm audit fix** subcommand automatically installs compatible updates to resolve vulnerabilities. Using **--force** allows major version updates that may introduce breaking changes. The command exits with a non-zero code if vulnerabilities are found, making it suitable for CI/CD pipelines."],["CAVEATS","Requires network access to check the npm advisory database. The --force flag may install updates with breaking changes; always review with --dry-run first. The --production flag is deprecated in npm 8+; use --omit=dev instead."],["HISTORY","npm audit was introduced in **npm 6** (2018) to provide automated security vulnerability scanning. It replaced the third-party `nsp` (Node Security Platform) tool that npm acquired."],["INSTALL","```pacman: sudo pacman -S npm```\n\n```apk: sudo apk add npm```\n\n```brew: brew install npm```"],["SEE ALSO","[npm](/man/npm)(1), [npm-fund](/man/npm-fund)(1), [snyk](/man/snyk)(1)"]],"e":["npm","npm-fund","snyk"],"x":"npm audit"},"odpscmd-resource":{"n":"odpscmd-resource","t":"manages MaxCompute (ODPS) project resources","s":[["TAGLINE","manages MaxCompute (ODPS) project resources"],["TLDR","**List all resources in the current project**\n\n```odpscmd resource list```\n\n**Add a JAR resource**\n\n```odpscmd resource add jar [file.jar]```\n\n**Add a Python resource**\n\n```odpscmd resource add py [file.py]```\n\n**Add a file resource with a comment**\n\n```odpscmd resource add file [data.csv] -c \"[description]\"```\n\n**Add an archive resource**\n\n```odpscmd resource add archive [file.tar.gz]```\n\n**Describe a resource**\n\n```odpscmd resource desc [resource_name]```\n\n**Delete a resource**\n\n```odpscmd resource drop [resource_name]```"],["SYNOPSIS","**odpscmd** **resource** _command_ [_options_]"],["PARAMETERS","**list**\n> List all resources in the current project.\n\n**add** _TYPE_ _FILE_ [**-c** _COMMENT_]\n> Upload a resource. TYPE is one of: jar, py, file, archive, table.\n\n**drop** _NAME_\n> Delete a resource by name.\n\n**desc** _NAME_\n> Show metadata and details of a resource.\n\n**-c** _COMMENT_\n> Add a comment or description when uploading a resource.\n\n**-f**\n> Force overwrite if a resource with the same name already exists.\n\n**--help**\n> Display help information."],["DESCRIPTION","**odpscmd resource** manages resources within Alibaba Cloud MaxCompute (formerly ODPS) projects. Resources are files uploaded to a project that can be referenced by MapReduce jobs, UDFs (User-Defined Functions), and Graph jobs.\n\nSupported resource types include JAR files for Java-based UDFs and MapReduce programs, Python files for PyODPS UDFs, plain files for data, and archives (tar.gz, zip) for bundled dependencies. Table resources reference existing MaxCompute tables.\n\nResources must be uploaded before they can be used in function definitions or job submissions. Each resource has a unique name within a project and can include an optional comment for documentation purposes."],["CAVEATS","Individual resource files have a size limit (typically 500 MB, but varies by configuration). Resource names must be unique within a project. The **-f** flag is needed to overwrite an existing resource. Part of the odpscmd CLI which requires a valid MaxCompute project configuration."],["HISTORY","odpscmd resource is part of the **MaxCompute Console** (odpscmd), Alibaba Cloud's command-line tool for managing MaxCompute projects, originally known as ODPS (Open Data Processing Service)."],["INSTALL","```aur: yay -S odpscmd```"],["SEE ALSO","[odpscmd](/man/odpscmd)(1), [odpscmd-func](/man/odpscmd-func)(1), [odpscmd-auth](/man/odpscmd-auth)(1), [odpscmd-tunnel](/man/odpscmd-tunnel)(1)"]],"e":["odpscmd","odpscmd-func","odpscmd-auth","odpscmd-tunnel"],"x":"odpscmd resource list"},"pacman-deptest":{"n":"pacman-deptest","t":"checks each dependency specified and returns a list of dependencies that are","s":[["TAGLINE","checks each dependency specified and returns a list of dependencies that are"],["TLDR","Print **package names** of dependencies that are not installed\n\n```pacman -T package1 package2```\n\nCheck if installed package satisfies the given **minimum version**\n\n```pacman -T \"bash>=5\"```\n\nCheck if a **later version** of a package is installed\n\n```pacman -T \"bash>5\"```\n\nDisplay **help**\n\n```pacman -Th```"],["SYNOPSIS","**pacman --deptest** [_options_] [_package(s)_]"],["PARAMETERS","**-T**, **--deptest**\n> Check dependencies specified and return unsatisfied ones\n\n**-h**, **--help**\n> Display help for this operation"],["DESCRIPTION","**pacman --deptest** checks each dependency specified and returns a list of dependencies that are not currently satisfied on the system. This is useful for scripts to determine what packages need to be installed.\n\nVersion comparisons can use operators like >=, >, =, <, and <= to check for specific version requirements."],["CAVEATS","This operation only checks local database; it does not query sync databases for available packages."],["HISTORY","Part of **pacman**, the package manager for Arch Linux and derivatives."],["INSTALL","```apt: sudo apt install pacman-package-manager```\n\n```dnf: sudo dnf install pacman```\n\n```pacman: sudo pacman -S pacman```\n\n```apk: sudo apk add pacman```\n\n```nix: nix profile install nixpkgs#pacman```"],["SEE ALSO","[pacman](/man/pacman)(8), [pacman-sync](/man/pacman-sync)(8)"]],"e":["pacman","pacman-sync"],"x":"pacman -T package1 package2"},"phan":{"n":"phan","t":"PHP static analyzer","s":[["TAGLINE","PHP static analyzer"],["TLDR","**Analyze PHP project**\n\n```phan```\n\n**Analyze specific directory**\n\n```phan --directory [src/]```\n\n**Generate baseline**\n\n```phan --save-baseline [.phan/baseline.php]```\n\n**Use baseline**\n\n```phan --load-baseline [.phan/baseline.php]```\n\n**Output format**\n\n```phan --output-mode [json]```"],["SYNOPSIS","**phan** [_options_]"],["PARAMETERS","**--directory** _DIR_\n> Directory to analyze.\n\n**--config-file** _FILE_\n> Configuration file.\n\n**--save-baseline** _FILE_\n> Save baseline.\n\n**--load-baseline** _FILE_\n> Load baseline.\n\n**--output-mode** _FORMAT_\n> Output format.\n\n**--help**\n> Display help."],["DESCRIPTION","**phan** is a PHP static analyzer. Finds bugs through type checking.\n\nThe tool performs deep analysis. Supports PHP 7/8 features."],["CAVEATS","Requires PHP with AST extension. Configuration recommended."],["HISTORY","Phan was created for **advanced PHP static analysis** with type inference."],["INSTALL","```aur: yay -S phan```"],["SEE ALSO","[phpstan](/man/phpstan)(1), [psalm](/man/psalm)(1), [php](/man/php)(1)"]],"e":["phpstan","psalm","php"],"x":"phan"},"pnminterp":{"n":"pnminterp","t":"Enlarge PNM images by interpolation","s":[["TAGLINE","Enlarge PNM images by interpolation"],["TLDR","**Interpolate image (scale up)**\n\n```pnminterp [factor] [input.pnm] > [output.pnm]```\n\n**Double image size**\n\n```pnminterp 2 [input.pnm] > [output.pnm]```\n\n**Triple image size**\n\n```pnminterp 3 [input.pnm] > [output.pnm]```"],["SYNOPSIS","**pnminterp** _n_ [_file_]"],["PARAMETERS","_N_\n> Scaling factor (integer multiplier).\n\n_FILE_\n> Input PNM file."],["DESCRIPTION","**pnminterp** enlarges PNM images by an integer factor using bilinear interpolation. Unlike simple pixel replication (as done by pnmenlarge), interpolation produces smoother results by computing weighted averages of neighboring pixels.\n\nThe scaling factor must be a positive integer, producing an output image that is N times larger in each dimension. This tool is a simplified interface to pamscale's interpolation mode. Part of the Netpbm toolkit; superseded by **pamscale**."],["CAVEATS","Only integer scale factors. Part of Netpbm suite."],["HISTORY","pnminterp was created as part of **Netpbm** for image interpolation."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pamscale](/man/pamscale)(1), [pnmscale](/man/pnmscale)(1), [pamenlarge](/man/pamenlarge)(1)"]],"e":["pamscale","pnmscale","pamenlarge"],"x":"pnminterp [factor] [input.pnm] > [output.pnm]"},"postconf":{"n":"postconf","t":"Display and modify Postfix configuration","s":[["TAGLINE","Display and modify Postfix configuration"],["TLDR","Use alternate **config directory**\n\n```postconf -c [path/to/config_dir]```\n\n**Edit** configuration parameters\n\n```postconf -e```\n\nShow **default** settings\n\n```postconf -d```\n\nShow parameters from **specific class**\n\n```postconf -C [builtin|service|user|all]```\n\nList **SASL** plugin types\n\n```postconf -a```\n\nList **lookup table** types\n\n```postconf -m```"],["SYNOPSIS","**postconf** [**-c** _dir_] [**-d**] [**-e**] [**-C** _class_] [_parameters_...]"],["PARAMETERS","**-c _directory_**\n> Alternate configuration directory\n\n**-d**\n> Show default parameter values\n\n**-e**\n> Edit main.cf with name=value pairs\n\n**-C _class_**\n> Filter by parameter class\n\n**-a**\n> List available SASL plugin types\n\n**-m**\n> List available lookup table types\n\n**-n**\n> Show only parameters with non-default values"],["DESCRIPTION","**postconf** displays and modifies Postfix mail server configuration. It reads and writes main.cf parameters, shows default values, and reports configuration warnings.\n\nThe tool validates parameter names and warns about typos. It's essential for administering Postfix without manually editing configuration files."],["CAVEATS","Changes via -e require postfix reload to take effect. Some parameters require restart. Modifying running server config requires care. Backup configuration before major changes."],["HISTORY","**postconf** is part of **Postfix**, the mail transfer agent created by **Wietse Venema** as a secure alternative to Sendmail. It provides safe configuration management for the complex mail system."],["INSTALL","```apt: sudo apt install postfix```\n\n```dnf: sudo dnf install postfix```\n\n```pacman: sudo pacman -S postfix```\n\n```apk: sudo apk add postfix```\n\n```zypper: sudo zypper install postfix```\n\n```nix: nix profile install nixpkgs#postfix```"],["SEE ALSO","[postfix](/man/postfix)(1), [postqueue](/man/postqueue)(1), [postmap](/man/postmap)(1)"]],"e":["postfix","postqueue","postmap"],"x":"postconf -c [path/to/config_dir]"},"ppmquantall":{"n":"ppmquantall","t":"Quantize multiple PPM images to a shared color palette","s":[["TAGLINE","Quantize multiple PPM images to a shared color palette"],["TLDR","**Quantize images to same 256-color palette**\n\n```ppmquantall [256] [*.ppm]```\n\n**Quantize with output to new files** (using extension)\n\n```ppmquantall -ext [.quant] [256] [*.ppm]```"],["SYNOPSIS","**ppmquantall** [**-ext** _extension_] _ncolors_ _files_..."],["PARAMETERS","**-ext** _extension_\n> Write output files with this extension appended instead of overwriting originals.\n\n_ncolors_\n> Maximum number of colors in the shared palette.\n\n_files_\n> PPM files to quantize."],["DESCRIPTION","**ppmquantall** takes multiple PPM images as input, chooses the best _ncolors_ colors to represent all images, maps existing colors to the new palette, and overwrites the input files with the quantized versions. Useful for creating GIF animations where all frames must share a single palette.\n\nPart of the Netpbm toolkit. Superseded by **pnmquantall** (Netpbm 10.58, March 2012), which handles all Netpbm input formats."],["CAVEATS","Modifies input files in place unless **-ext** is specified. Use **-ext** to preserve originals."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm-extras```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pnmquantall](/man/pnmquantall)(1), [ppmquant](/man/ppmquant)(1)"]],"e":["pnmquantall","ppmquant"],"x":"ppmquantall [256] [*.ppm]"},"pppoe":{"n":"pppoe","t":"Manage PPP over Ethernet connections","s":[["TAGLINE","Manage PPP over Ethernet connections"],["TLDR","**Start PPPoE connection**\n\n```pppoe -I [eth0]```\n\n**Debug mode**\n\n```pppoe -I [eth0] -D [/tmp/pppoe.log]```\n\n**Specify AC name**\n\n```pppoe -I [eth0] -A [ac_name]```"],["SYNOPSIS","**pppoe** [_options_]"],["PARAMETERS","**-I** _INTERFACE_\n> Ethernet interface.\n\n**-D** _FILE_\n> Debug log file.\n\n**-A** _NAME_\n> Access concentrator name.\n\n**-S** _NAME_\n> Service name.\n\n**-T** _SECONDS_\n> Timeout."],["DESCRIPTION","**pppoe** is the user-space PPP over Ethernet client that establishes PPPoE sessions for DSL broadband connections. It handles the discovery and session phases of the PPPoE protocol, communicating with the ISP's access concentrator over a specified Ethernet interface.\n\nThe tool is typically invoked indirectly through **pppoe-connect** or **pppoe-start** wrapper scripts rather than called directly. It works in conjunction with **pppd** which handles the PPP negotiation once the Ethernet session is established."],["CAVEATS","Usually managed by pppoe-connect. Requires root."],["HISTORY","pppoe implements **PPP over Ethernet** for DSL connections."],["INSTALL","```apt: sudo apt install pppoe```\n\n```dnf: sudo dnf install rp-pppoe```\n\n```pacman: sudo pacman -S rp-pppoe```\n\n```apk: sudo apk add rp-pppoe```\n\n```zypper: sudo zypper install rp-pppoe```\n\n```nix: nix profile install nixpkgs#rp-pppoe```"],["SEE ALSO","[pppoe-connect](/man/pppoe-connect)(1), [pppoe-setup](/man/pppoe-setup)(8), [pppd](/man/pppd)(8)"]],"e":["pppoe-connect","pppoe-setup","pppd"],"x":"pppoe -I [eth0]"},"ps-nvm":{"n":"ps-nvm","t":"Node.js version manager for PowerShell","s":[["TAGLINE","Node.js version manager for PowerShell"],["TLDR","**List Node versions (PowerShell)**\n\n```ps-nvm list```\n\n**Install Node version**\n\n```ps-nvm install [18.0.0]```\n\n**Use specific version**\n\n```ps-nvm use [18.0.0]```\n\n**Set default version**\n\n```ps-nvm default [18.0.0]```"],["SYNOPSIS","**ps-nvm** _command_ [_version_]"],["PARAMETERS","**list**\n> List installed versions.\n\n**install** _VERSION_\n> Install Node version.\n\n**use** _VERSION_\n> Switch to version.\n\n**default** _VERSION_\n> Set default version.\n\n**uninstall** _VERSION_\n> Remove version."],["DESCRIPTION","**ps-nvm** is a Node.js version manager designed natively for PowerShell on Windows. It allows installing, switching between, and managing multiple Node.js versions on a single system, similar to nvm on Unix but built specifically for the PowerShell environment.\n\nThe tool downloads and manages Node.js binaries, adjusting the system PATH to point to the selected version. A default version can be set to persist across new PowerShell sessions."],["CAVEATS","PowerShell only; Windows-specific alternative to nvm. Installed as a PowerShell module. Requires administrative privileges for some operations."],["HISTORY","ps-nvm was created for **PowerShell** Node.js version management."],["SEE ALSO","[nvm](/man/nvm)(1), [node](/man/node)(1), [volta](/man/volta)(1)"]],"e":["nvm","node","volta"],"x":"ps-nvm list"},"puffin":{"n":"puffin","t":"Terminal dashboard for personal finance management with hledger","s":[["TAGLINE","Terminal dashboard for personal finance management with hledger"],["TLDR","**Launch the finance dashboard** using the journal from **$LEDGER_FILE**\n\n```puffin```\n\n**Launch the v3 UI**\n\n```puffin -v3```"],["SYNOPSIS","**puffin** [_-v3_]"],["DESCRIPTION","**puffin** is a terminal dashboard for personal finance management that integrates hledger reports into a TUI. It displays income statements, balance sheets, and register data with filtering by account, date, period, and depth. Multi-commodity tracking is supported.\n\nPuffin does not accept its own **--file**/**--config** flags; it picks up the journal via **hledger**'s own configuration (the **$LEDGER_FILE** environment variable or **~/.hledger.journal**). An optional **puffin.json** file configures custom report tabs (with **name**, **cmd**, and **locked** fields); the v3 UI reads a simpler config containing only a **journalFile** field. Press **?** inside the TUI to display the keybinding help."],["CAVEATS","Requires **hledger** to be installed and configured with journal files. All filtering flags passed on custom report commands must be valid **hledger** arguments."],["HISTORY","**puffin** was created by **siddhantac** and is written in **Go**."],["INSTALL","```nix: nix profile install nixpkgs#puffin```"],["SEE ALSO","[hledger](/man/hledger)(1), [ledger](/man/ledger)(1)"]],"e":["hledger","ledger"],"x":"puffin"},"pulumi-config":{"n":"pulumi-config","t":"Manage Pulumi stack configuration values","s":[["TAGLINE","Manage Pulumi stack configuration values"],["TLDR","**Set configuration value**\n\n```pulumi config set [key] [value]```\n\n**Set secret value**\n\n```pulumi config set --secret [key] [value]```\n\n**Get configuration value**\n\n```pulumi config get [key]```\n\n**List all configuration**\n\n```pulumi config```\n\n**Remove configuration**\n\n```pulumi config rm [key]```"],["SYNOPSIS","**pulumi** **config** [_command_] [_options_]"],["PARAMETERS","**set** _key_ _value_\n> Set configuration value.\n\n**get** _key_\n> Get configuration value.\n\n**rm** _key_\n> Remove configuration.\n\n**refresh**\n> Update config from state.\n\n**--secret**\n> Encrypt value.\n\n**--plaintext**\n> Store unencrypted.\n\n**-s**, **--stack** _name_\n> Target stack.\n\n**--json**\n> Output as JSON."],["DESCRIPTION","**pulumi config** manages stack configuration values. Configuration includes parameters like cloud regions, instance sizes, and secrets. Secrets are encrypted in state files."],["INSTALL","```pacman: sudo pacman -S pulumi```\n\n```apk: sudo apk add pulumi```\n\n```brew: brew install pulumi```\n\n```nix: nix profile install nixpkgs#pulumi```"],["SEE ALSO","[pulumi](/man/pulumi)(1), [pulumi-stack](/man/pulumi-stack)(1)"]],"e":["pulumi","pulumi-stack"],"x":"pulumi config set [key] [value]"},"pygettext3.4":{"n":"pygettext3.4","t":"Python 3.4 translatable string extraction","s":[["TAGLINE","Python 3.4 translatable string extraction"],["TLDR","**Extract translatable strings**\n\n```pygettext3.4 [file.py]```\n\n**Output to specific file**\n\n```pygettext3.4 -o [messages.pot] [file.py]```\n\n**Extract from directory**\n\n```pygettext3.4 -d [domain] [*.py]```"],["SYNOPSIS","**pygettext3.4** [_options_] [_files_]"],["PARAMETERS","_FILES_\n> Python files to extract from.\n\n**-o** _FILE_\n> Output file.\n\n**-d** _DOMAIN_\n> Text domain.\n\n**-k** _KEYWORD_\n> Additional keyword."],["DESCRIPTION","**pygettext3.4** is the Python 3.4 version of pygettext, which extracts translatable strings from Python source files and generates POT (Portable Object Template) files for internationalization. It scans for strings wrapped in **_()**, **gettext()**, and similar marker functions.\n\nThis version-specific binary targets Python 3.4 code. For current development, use the latest **pygettext** or the more feature-rich **xgettext** and **Babel** alternatives."],["CAVEATS","Python 3.4 specific. Use latest pygettext3."],["HISTORY","pygettext3.4 is **Python 3.4's** string extraction tool."],["SEE ALSO","[pygettext](/man/pygettext)(1), [xgettext](/man/xgettext)(1), [gettext](/man/gettext)(1)"]],"e":["pygettext","xgettext","gettext"],"x":"pygettext3.4 [file.py]"},"qm-destroy":{"n":"qm-destroy","t":"Permanently remove a Proxmox virtual machine","s":[["TAGLINE","Permanently remove a Proxmox virtual machine"],["TLDR","**Destroy** a specific virtual machine\n\n```qm destroy 100```\n\nDestroy and remove all **unreferenced disks** from enabled storages\n\n```qm destroy 100 --destroy-unreferenced-disks```\n\nDestroy and **purge** from backup, replication jobs, and HA configuration\n\n```qm destroy 100 --purge```\n\nDestroy with **purge** and remove unreferenced disks\n\n```qm destroy 100 --purge --destroy-unreferenced-disks```\n\n**Force** destroy ignoring locks (root only)\n\n```sudo qm destroy 100 --skiplock```"],["SYNOPSIS","**qm destroy** _vmid_ [_OPTIONS_]"],["PARAMETERS","**--destroy-unreferenced-disks** _boolean_\n> If set, additionally destroy all disks not referenced in the config but with a matching VMID from all enabled storages. Default: 0.\n\n**--purge** _boolean_\n> Remove VMID from configurations, like backup and replication jobs and HA.\n\n**--skiplock** _boolean_\n> Ignore locks. Only root is allowed to use this option."],["DESCRIPTION","**qm destroy** permanently removes a virtual machine and all associated volumes from Proxmox VE. The operation also removes any VM-specific permissions and firewall rules.\n\nThe **--purge** option removes the VMID from backup jobs, replication jobs, and high availability configuration. The **--destroy-unreferenced-disks** option removes disks that are not referenced in the VM configuration but have a matching VMID across all enabled storages. The **--skiplock** option bypasses safety locks but requires root privileges."],["CAVEATS","Destruction is permanent and cannot be undone. Ensure backups exist before destroying important VMs. The VMID must be in the range 100-999999999."],["HISTORY","Part of **Proxmox VE** QEMU/KVM management tools for virtual machine administration."],["INSTALL","```dnf: sudo dnf install qm```"],["SEE ALSO","[qm](/man/qm)(1), [qm-create](/man/qm-create)(1), [qm-clone](/man/qm-clone)(1), [qm-stop](/man/qm-stop)(1), [qm-shutdown](/man/qm-shutdown)(1)"]],"e":["qm","qm-create","qm-clone","qm-stop","qm-shutdown"],"x":"qm destroy 100"},"qm-reboot":{"n":"qm-reboot","t":"Reboot a Proxmox virtual machine","s":[["TAGLINE","Reboot a Proxmox virtual machine"],["TLDR","**Reboot a virtual machine**\n\n```qm reboot [vmid]```\n\n**Reboot with a graceful shutdown timeout**\n\n```qm reboot [vmid] --timeout [seconds]```"],["SYNOPSIS","**qm reboot** _vmid_ [_options_]"],["PARAMETERS","**--timeout** _seconds_\n> Wait at most _seconds_ for the guest to shut down gracefully before forcing a restart. Integer, 0 or greater."],["DESCRIPTION","**qm reboot** reboots a virtual machine by shutting it down gracefully and then starting it again. Any pending configuration changes are applied during the restart cycle.\n\nThe _vmid_ is the unique integer identifier of the VM (100–999999999). The graceful shutdown relies on ACPI support in the guest OS. If the guest does not respond within the timeout period, the VM is forcefully stopped and restarted."],["CAVEATS","Requires ACPI support in the guest OS for a graceful shutdown. Without ACPI, the shutdown signal may be ignored. Use `qm reset` for a hard reset that does not rely on ACPI."],["HISTORY","Part of the **Proxmox VE** QEMU/KVM management toolset for virtual machine administration on the Proxmox hypervisor platform."],["INSTALL","```dnf: sudo dnf install qm```"],["SEE ALSO","[qm](/man/qm)(1), [qm-reset](/man/qm-reset)(1), [qm-shutdown](/man/qm-shutdown)(1), [qm-start](/man/qm-start)(1), [qm-stop](/man/qm-stop)(1)"]],"e":["qm","qm-reset","qm-shutdown","qm-start","qm-stop"],"x":"qm reboot [vmid]"},"raspi-config":{"n":"raspi-config","t":"Raspberry Pi system configuration utility","s":[["TAGLINE","Raspberry Pi system configuration utility"],["TLDR","Start **raspi-config** utility\n\n```sudo raspi-config```"],["SYNOPSIS","**raspi-config** [_nonint_options_]"],["DESCRIPTION","**raspi-config** is the official configuration utility for Raspberry Pi running Raspberry Pi OS. It provides an ncurses-based menu interface for common system settings.\n\nConfiguration options include network settings, display options, interface enabling (SSH, VNC, SPI, I2C), performance tuning, localization, and system updates. Many settings that would require manual file editing are accessible through this tool."],["MENU CATEGORIES","- **System Options**: hostname, password, boot behavior, network boot\n- **Display Options**: resolution, overscan, pixel doubling\n- **Interface Options**: SSH, VNC, SPI, I2C, Serial, 1-Wire, GPIO remote\n- **Performance Options**: GPU memory, fan control, overlay filesystem\n- **Localization Options**: locale, timezone, keyboard, WLAN country\n- **Advanced Options**: expand filesystem, network proxy, boot order"],["CONFIGURATION","**/boot/firmware/config.txt**\n> Hardware configuration file controlling GPU memory, display settings, overlays, and boot parameters modified by raspi-config.\n\n**/etc/default/locale**\n> System locale settings managed through the localization menu."],["CAVEATS","Raspberry Pi specific, not available on other systems. Requires root privileges. Some changes require reboot. Non-interactive mode available for scripting."],["HISTORY","**raspi-config** was developed by the Raspberry Pi Foundation as part of Raspberry Pi OS to simplify configuration for users unfamiliar with Linux command-line administration."],["SEE ALSO","[pinout](/man/pinout)(1), [vcgencmd](/man/vcgencmd)(1), [raspi-gpio](/man/raspi-gpio)(1)"]],"e":["pinout","vcgencmd","raspi-gpio"],"x":"sudo raspi-config"},"recsel":{"n":"recsel","t":"Query and filter GNU recutils database records","s":[["TAGLINE","Query and filter GNU recutils database records"],["TLDR","**Select all records** from a recfile\n\n```recsel [file.rec]```\n\n**Select records matching a condition**\n\n```recsel -e \"[field] = '[value]'\" [file.rec]```\n\n**Select records with multiple conditions**\n\n```recsel -e \"[field1] = '[value]' && [field2] > [number]\" [file.rec]```\n\n**Select specific fields only**\n\n```recsel -p [field1],[field2] [file.rec]```\n\n**Select records from a specific type**\n\n```recsel -t [record_type] [file.rec]```\n\n**Count matching records**\n\n```recsel -c -e \"[condition]\" [file.rec]```\n\n**Sort records by a field**\n\n```recsel -S [field] [file.rec]```"],["SYNOPSIS","**recsel** [_options_] [**-e** _expression_] [**-t** _type_] [_file_...]"],["PARAMETERS","**-e** _expression_\n> Select records matching the expression\n\n**-t** _type_\n> Select records of specified type only\n\n**-p** _fields_\n> Print only specified fields (comma-separated)\n\n**-P** _fields_\n> Print specified fields without record separators\n\n**-c**, **--count**\n> Print count of matching records\n\n**-C**, **--collapse**\n> Collapse multiple field values into one\n\n**-S** _field_\n> Sort records by specified field\n\n**-G** _field_\n> Group records by specified field\n\n**-n** _max_\n> Return at most max records\n\n**-R** _random_\n> Return random records\n\n**-d**, **--print-descriptors**\n> Print record descriptors"],["DESCRIPTION","**recsel** is part of GNU Recutils, a set of tools for managing plain-text databases using the rec format. It selects and prints records from recfiles based on expressions and criteria.\n\nRecfiles are human-readable text files where each record is a collection of named fields separated by blank lines. The format is simple, versionable with git, and editable with any text editor.\n\nSelection expressions support comparison operators (=, !=, <, >, <=, >=), logical operators (&&, ||, !), pattern matching (~), and field existence checks. Expressions operate on field values within each record.\n\nOutput can be restricted to specific fields with **-p**, sorted with **-S**, and limited with **-n**. The tool handles multiple record types within a single file using **-t**."],["EXPRESSION SYNTAX","**field = \"value\"**: Exact string match\n**field ~ \"pattern\"**: Regular expression match\n**field < number**: Numeric comparison\n**field && field2**: Logical AND\n**#field**: Field exists\n**!#field**: Field does not exist"],["CAVEATS","Field names and string values in expressions must follow recutils quoting rules. Strings with spaces require quotes.\n\nNumeric comparisons require fields to contain valid numbers. Non-numeric values cause comparison failures.\n\nLarge recfiles may be slow to process compared to proper databases, but the simplicity and portability often outweigh performance concerns for moderate data sizes."],["INSTALL","```dnf: sudo dnf install recutils```\n\n```apk: sudo apk add recutils```\n\n```brew: brew install recutils```\n\n```nix: nix profile install nixpkgs#recutils```"],["SEE ALSO","[recins](/man/recins)(1), [recdel](/man/recdel)(1), [recset](/man/recset)(1), [recfmt](/man/recfmt)(1), [rec2csv](/man/rec2csv)(1)"]],"e":["recins","recdel","recset","recfmt","rec2csv"],"x":"recsel [file.rec]"},"rom":{"n":"rom","t":"Play game ROMs in the terminal via libretro","s":[["TAGLINE","Play game ROMs in the terminal via libretro"],["TLDR","**Play** a ROM (first launch may offer to fetch the core)\n\n```rom [path/to/game.sfc]```\n\n**Resume** a recently played game from its newest save state\n\n```rom --resume```\n\n**Use** a specific libretro core\n\n```rom --core [path/to/core.so] [path/to/game.gb]```\n\n**Fullscreen** in the terminal window\n\n```rom --fullscreen [path/to/game.gba]```\n\n**Integer scale** (inline mode) and **recolor** to the terminal theme\n\n```rom --scale [3] --recolor [hue] [path/to/game.nes]```\n\n**Print** key bindings for a ROM\n\n```rom --keys [path/to/game.sfc]```\n\n**Disable** audio\n\n```rom --no-audio [path/to/game.md]```"],["SYNOPSIS","**rom** [_options_] _rom-file_\n\n**rom** **--resume**"],["PARAMETERS","**--resume**\n> Pick from recently played games and load the newest save state.\n\n**--core** _path_\n> Use a specific libretro core (.so on Linux, .dylib on macOS).\n\n**--fullscreen**\n> Fill the terminal window.\n\n**--scale** _n_\n> Integer zoom in inline mode, 1–8 (default 2); change live with **[** / **]**.\n\n**--slot** _n_\n> Initial save-state slot, 0–9.\n\n**--no-audio**\n> Disable audio.\n\n**--recolor** _mode_\n> Terminal-theme recoloring: **off**, **hue**, **nearest**, **duotone**, **tint**, or **dither**.\n\n**--recolor-strength** _0..1_\n> Blend recoloring with the original palette.\n\n**--keys**\n> Print current key bindings (optionally with a ROM path for effective settings).\n\n**--selftest** _n_\n> Run _n_ frames without a terminal (for testing).\n\n**--shot** _file_\n> Save the final self-test frame as BMP.\n\n**--force**\n> Skip terminal graphics detection."],["DESCRIPTION","**rom** is a small **libretro** frontend for **macOS** and **Linux** that plays console and classic PC game ROMs **inline in the terminal**. It renders native pixels with the **kitty graphics protocol** (works in **Ghostty** or **kitty**, including under **tmux** with passthrough enabled), supports real key-release events, save states, audio, fast-forward, and live terminal-theme recoloring.\n\nThe tool does **not** ship games, BIOS files, or emulator cores. Opening a ROM for a platform without a core installed prompts to shallow-clone and build the matching libretro core into **~/.config/rom/cores/** (requires git, make, and a C/C++ toolchain). Cores are also searched under **./cores/** and next to the executable.\n\nSupported extensions map to common cores, including SNES (**.sfc**/**.smc**), NES (**.nes**), Game Boy/Color (**.gb**/**.gbc**), GBA (**.gba**), Genesis (**.md**/**.gen**), PC Engine (**.pce**), N64 (**.n64**/**.z64**), Doom-family WADs (**.wad**), and Wolfenstein data (**.wl6**, etc.). Only run software you are legally entitled to use."],["CONFIGURATION","**~/.config/rom/config**\n> Main settings: scale, terminal scaling, recolor mode, audio/focus behavior, key bindings, and optional per-system (**snes**, **nes**, **gb**, **gba**, **genesis**, **pce**, **n64**, **doom**, **wolf3d**) and **[core.**_system_**]** libretro option sections.\n\n**~/.config/rom/cores/**\n> Installed libretro core libraries.\n\n**~/.config/rom/saves/** / **~/.config/rom/states/**\n> Battery saves and save states.\n\n**~/.config/rom/games/** / **~/.config/rom/recent**\n> Per-ROM volume/scale memory and the resume list.\n\n**tmux**: enable graphics passthrough once with **tmux set -g allow-passthrough all** (or set it in **~/.tmux.conf**)."],["CAVEATS","Requires a terminal that supports the **kitty graphics protocol** (Ghostty or kitty). Audio uses CoreAudio on macOS and ALSA on Linux. OpenGL cores are supported on macOS via offscreen CGL; Linux is software-rendered until an EGL backend lands. No Vulkan cores. Player 1 keyboard only. ROMs, BIOS, and cores are not redistributed—obtain them legally yourself."],["HISTORY","**rom** is an open-source libretro frontend by **jhickner** that targets terminal-native play: kitty-protocol frame output, save states, and optional palette recoloring to match the host terminal theme."],["SEE ALSO","[retroarch](/man/retroarch)(1), [emulationstation](/man/emulationstation)(1), [kitty](/man/kitty)(1)"],["RESOURCES","```[Source code](https://github.com/jhickner/rom)```"]],"e":["retroarch","emulationstation","kitty"],"x":"rom [path/to/game.sfc]"},"slim":{"n":"slim","t":"Lightweight Ruby HTML template compiler","s":[["TAGLINE","Lightweight Ruby HTML template compiler"],["TLDR","**Convert a Slim template** to HTML\n\n```slimrb [input.slim]```\n\n**Read Slim from stdin** and output HTML\n\n```echo \"[h1 Hello]\" | slimrb -s```\n\n**Convert Slim to ERB** format\n\n```slimrb -e [input.slim]```\n\n**Output pretty-printed HTML** for debugging\n\n```slimrb -p [input.slim]```\n\n**Compile to Ruby code** without executing\n\n```slimrb -c [input.slim]```\n\n**Generate Rails-compatible code**\n\n```slimrb --rails [input.slim]```\n\n**Pass local variables** to the template\n\n```slimrb -l \"{name: 'World'}\" [input.slim]```\n\n**Load a Ruby library** before processing\n\n```slimrb -r [library] [input.slim]```"],["SYNOPSIS","**slimrb** [_options_] [_input_file_]"],["PARAMETERS","**-s**, **--stdin**\n> Read input from standard input instead of a file\n\n**-c**, **--compile**\n> Compile template to Ruby code without executing\n\n**-e**, **--erb**\n> Convert Slim template to ERB format\n\n**--rails**\n> Generate Rails-compatible code (implies --compile)\n\n**-p**, **--pretty**\n> Output pretty-printed HTML with indentation\n\n**-r**, **--require** _library_\n> Load a Ruby library or Slim plugin before processing\n\n**-o**, **--option** _name=code_\n> Set Slim engine options\n\n**-l**, **--locals** _Hash|YAML|JSON_\n> Pass local variables to the template\n\n**--trace**\n> Show full traceback on errors\n\n**-v**, **--version**\n> Display version information\n\n**-h**, **--help**\n> Show help message"],["DESCRIPTION","**slimrb** is the command-line tool for the Slim template language. Slim is a lightweight Ruby templating engine that reduces HTML syntax to essential parts while maintaining readability. It uses indentation for nesting, eliminating the need for closing tags.\n\nSlim syntax is concise: **div.container** becomes **\\<div class=\"container\"\\>**, and **h1= title** outputs the value of **title** in an h1 tag. Attributes use parentheses: **a(href=url) Link**.\n\nThe tool can render templates to HTML, convert them to ERB for use in other contexts, or compile them to raw Ruby code for inspection. The **--pretty** option is useful for debugging, producing formatted HTML output.\n\nSlim integrates with Rails, Sinatra, and any Rack-based framework through Tilt. It is one of the fastest Ruby template engines, comparable to Erubis in production mode."],["CAVEATS","Slim requires Ruby to be installed. Template syntax is whitespace-sensitive; mixing tabs and spaces can cause unexpected results. Some advanced Ruby expressions may need explicit output markers."],["HISTORY","**Slim** was created by **Andrew Stone**, **Fred Wu**, and **Daniel Mendler** around **2010** as a cleaner alternative to ERB and Haml. The project aimed to provide minimal syntax while maintaining full Ruby integration. It uses the Temple framework for parsing and compilation."],["INSTALL","```apt: sudo apt install slim```\n\n```dnf: sudo dnf install slim```\n\n```pacman: sudo pacman -S slim```\n\n```apk: sudo apk add slim```\n\n```zypper: sudo zypper install slim```"],["SEE ALSO","[erb](/man/erb)(1), [haml](/man/haml)(1), [pug](/man/pug)(1), [ruby](/man/ruby)(1)"]],"e":["erb","haml","pug","ruby"],"x":"slimrb [input.slim]"},"snapper":{"n":"snapper","t":"Filesystem snapshot management for Btrfs","s":[["TAGLINE","Filesystem snapshot management for Btrfs"],["TLDR","List snapshot **configurations**\n\n```snapper list-configs```\n\n**Create** a configuration\n\n```snapper -c [config] create-config [path/to/directory]```\n\nCreate a **snapshot** with description\n\n```snapper -c [config] create -d \"[description]\"```\n\n**List** snapshots for a config\n\n```snapper -c [config] list```\n\n**Delete** a snapshot\n\n```snapper -c [config] delete [snapshot_number]```\n\nDelete a **range** of snapshots\n\n```snapper -c [config] delete [snapshot1]-[snapshot2]```\n\nShow **differences** between snapshots\n\n```snapper -c [config] status [snapshot1]..[snapshot2]```\n\n**Rollback** to a snapshot\n\n```snapper -c [config] rollback [snapshot_number]```"],["SYNOPSIS","**snapper** [**-c** _config_] _command_ [_OPTIONS_]"],["PARAMETERS","**-c, --config** _name_\n> Use specified configuration\n\n**list-configs**\n> List all configurations\n\n**create-config** _path_\n> Create new configuration for subvolume\n\n**delete-config**\n> Delete a configuration\n\n**create**\n> Create a new snapshot\n\n**delete** _number_\n> Delete snapshot(s)\n\n**list**\n> List snapshots\n\n**status** _num1..num2_\n> Show changes between snapshots\n\n**diff** _num1..num2_\n> Show file differences\n\n**undochange** _num1..num2_ _files_\n> Undo changes between snapshots\n\n**rollback** [_number_]\n> Rollback system to snapshot\n\n**cleanup** _algorithm_\n> Run cleanup algorithm (number, timeline, empty-pre-post)\n\n**setup-quota**\n> Set up quota (btrfs only)\n\n**-d, --description** _text_\n> Snapshot description\n\n**-t, --type** _type_\n> Snapshot type (single, pre, post)\n\n**-u, --userdata** _key=value_\n> Set snapshot metadata\n\n**-s, --sync**\n> Sync filesystem after delete"],["DESCRIPTION","**snapper** is a filesystem snapshot management tool primarily used with Btrfs. It creates, compares, and manages snapshots, enabling system rollback and file recovery. Snapper supports automatic snapshot timelines with configurable retention policies.\n\nEach configuration manages snapshots for a specific subvolume or logical volume. Snapshots can be created manually or automatically before/after system changes. The cleanup algorithms automatically remove old snapshots based on age or count limits."],["CONFIGURATION","**/etc/snapper/configs/**\n> Per-subvolume configuration files defining snapshot types, cleanup algorithms, and retention policies (timeline and number limits).\n\n**/etc/sysconfig/snapper**\n> Global snapper settings including the list of active configurations (SNAPPER_CONFIGS)."],["CAVEATS","Rollback is only supported on Btrfs with proper system configuration. Snapshots consume disk space; monitor usage with **btrfs filesystem df**. Non-root users need configuration permissions set via ALLOW_USERS. The /.snapshots directory must have correct permissions."],["HISTORY","**snapper** was created by **Arvin Schnell** at **openSUSE/SUSE** and first released around **2011**. It was designed to leverage Btrfs snapshot capabilities for system recovery and became a key component of openSUSE and SUSE Linux Enterprise. Support for thin-provisioned LVM was added later."],["INSTALL","```apt: sudo apt install snapper```\n\n```dnf: sudo dnf install snapper```\n\n```pacman: sudo pacman -S snapper```\n\n```zypper: sudo zypper install snapper```\n\n```nix: nix profile install nixpkgs#snapper```"],["SEE ALSO","[btrfs](/man/btrfs)(8), [btrfs-subvolume](/man/btrfs-subvolume)(8), [timeshift](/man/timeshift)(1), [lvcreate](/man/lvcreate)(8)"]],"e":["btrfs","btrfs-subvolume","timeshift","lvcreate"],"x":"snapper list-configs"},"sqlmap":{"n":"sqlmap","t":"Automated SQL injection detection tool","s":[["TAGLINE","Automated SQL injection detection tool"],["TLDR","**Test a URL for SQL injection**\n\n```sqlmap -u \"[https://example.com/page?id=1]\"```\n\n**Test with increased detection level**\n\n```sqlmap -u \"[url?id=1]\" --level [5] --risk [3]```\n\n**Enumerate databases**\n\n```sqlmap -u \"[url?id=1]\" --dbs```\n\n**Dump a specific table**\n\n```sqlmap -u \"[url?id=1]\" -D [database] -T [table] --dump```\n\n**Test POST request** with data\n\n```sqlmap -u \"[url]\" --data=\"[username=admin&password=test]\"```\n\n**Load request from file** (captured from proxy)\n\n```sqlmap -r [request.txt]```\n\n**Get an OS shell** through SQL injection\n\n```sqlmap -u \"[url?id=1]\" --os-shell```\n\n**Run in batch mode** (no prompts)\n\n```sqlmap -u \"[url?id=1]\" --batch```"],["SYNOPSIS","**sqlmap** [_options_] -u _URL_\n\n**sqlmap** [_options_] -r _REQUEST_FILE_"],["PARAMETERS","**-u** _URL_\n> Target URL with injectable parameter\n\n**-r** _FILE_\n> Load HTTP request from file\n\n**--data** _DATA_\n> POST data string\n\n**--cookie** _COOKIE_\n> HTTP Cookie header value\n\n**-p** _PARAM_\n> Testable parameter(s)\n\n**--level** _LEVEL_\n> Test thoroughness (1-5, default: 1)\n\n**--risk** _RISK_\n> Test risk level (1-3, default: 1)\n\n**--technique** _TECH_\n> Injection techniques (B, E, U, S, T, Q)\n\n**--dbms** _DBMS_\n> Force specific database type\n\n**--dbs**\n> Enumerate databases\n\n**--tables**\n> Enumerate tables\n\n**--columns**\n> Enumerate columns\n\n**--dump**\n> Dump table entries\n\n**-D** _DB_\n> Target database\n\n**-T** _TABLE_\n> Target table\n\n**-C** _COLUMNS_\n> Target columns\n\n**--os-shell**\n> Get interactive OS shell\n\n**--os-cmd** _CMD_\n> Execute OS command\n\n**--batch**\n> Non-interactive mode\n\n**--threads** _NUM_\n> Concurrent requests (default: 1)\n\n**-v** _LEVEL_\n> Verbosity level (0-6)\n\n**--wizard**\n> Interactive wizard for beginners"],["DESCRIPTION","**sqlmap** is an automated SQL injection and database takeover tool. It detects and exploits SQL injection vulnerabilities in web applications, supporting a wide range of database systems including MySQL, PostgreSQL, Oracle, Microsoft SQL Server, and SQLite.\n\nThe tool uses various injection techniques: boolean-based blind, error-based, UNION query, stacked queries, time-based blind, and inline queries. It can enumerate databases, tables, columns, and dump data. Advanced features include OS access through database functions.\n\nDetection levels (**--level**) control test thoroughness—higher levels test more injection points including cookies and headers. Risk levels (**--risk**) control whether potentially harmful tests (like heavy time delays) are used."],["CAVEATS","Only use with explicit authorization—unauthorized testing is illegal. High level/risk settings can cause false positives or server issues. Some injection types may modify database data. Web application firewalls may block or rate-limit requests. Results should be verified manually for accuracy."],["HISTORY","sqlmap was created by **Daniele Bellucci** and **Bernardo Damele A. G.** with development starting in **2006**. It became one of the most widely used penetration testing tools for SQL injection. The project is open-source, written in Python, and continues active development. sqlmap is included by default in Kali Linux and other security-focused distributions."],["INSTALL","```pacman: sudo pacman -S sqlmap```\n\n```zypper: sudo zypper install sqlmap```\n\n```brew: brew install sqlmap```\n\n```nix: nix profile install nixpkgs#sqlmap```"],["SEE ALSO","[burpsuite](/man/burpsuite)(1), [nikto](/man/nikto)(1), [skipfish](/man/skipfish)(1), [wpscan](/man/wpscan)(1)"]],"e":["burpsuite","nikto","skipfish","wpscan"],"x":"sqlmap -u \"[https://example.com/page?id=1]\""},"stat":{"n":"stat","t":"Display file and filesystem metadata","s":[["TAGLINE","Display file and filesystem metadata"],["TLDR","**Display file status**\n\n```stat [file]```\n\n**Display in terse format**\n\n```stat -t [file]```\n\n**Display filesystem status**\n\n```stat -f [file]```\n\n**Custom format output**\n\n```stat -c \"%n: %s bytes\" [file]```\n\n**Show access time**\n\n```stat -c \"%x\" [file]```\n\n**Show modification time**\n\n```stat -c \"%y\" [file]```\n\n**Show permissions in octal**\n\n```stat -c \"%a\" [file]```\n\n**Show all info for multiple files**\n\n```stat [file1] [file2] [file3]```\n\n**Follow symbolic links**\n\n```stat -L [symlink]```"],["SYNOPSIS","**stat** [_options_] _file_..."],["DESCRIPTION","**stat** displays detailed information about files or filesystems. It shows metadata including size, permissions, ownership, timestamps, inode number, and device information.\n\nThe default output includes file name, size, blocks, device, inode, links, access permissions, ownership, and access/modify/change times. Custom format strings allow selecting specific fields.\n\nstat is useful for scripting when specific file attributes are needed, or for investigating file properties not shown by ls."],["PARAMETERS","**-f**, **--file-system**\n> Display filesystem status instead of file.\n\n**-L**, **--dereference**\n> Follow symbolic links.\n\n**-t**, **--terse**\n> Print terse output.\n\n**-c** _format_, **--format=** _format_\n> Use custom format string.\n\n**--printf=** _format_\n> Like --format, but interpret backslash escapes and no trailing newline."],["FORMAT SEQUENCES (FILE)","**%a**: Access rights in octal\n**%A**: Access rights in human readable form\n**%F**: File type\n**%g**: Group ID\n**%G**: Group name\n**%h**: Number of hard links\n**%i**: Inode number\n**%n**: File name\n**%s**: Size in bytes\n**%u**: User ID\n**%U**: User name\n**%x**: Access time\n**%y**: Modification time\n**%z**: Change time"],["FORMAT SEQUENCES (FILESYSTEM)","**%a**: Free blocks\n**%b**: Total blocks\n**%f**: Free blocks (non-root)\n**%n**: File name\n**%s**: Block size\n**%T**: Filesystem type"],["CAVEATS","Output format differs between GNU stat (Linux) and BSD stat (macOS). Some filesystems don't support all attributes. Access times may be disabled on some systems for performance."],["HISTORY","**stat** has different origins on different systems. The GNU version (part of **coreutils**) was written by **Michael Meskes** in **2001**. BSD systems have their own implementation with slightly different syntax. The command provides a standard interface to the stat() system call."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[ls](/man/ls)(1), [file](/man/file)(1), [touch](/man/touch)(1), [chmod](/man/chmod)(1)"]],"e":["ls","file","touch","chmod"],"x":"stat [file]"},"steghide":{"n":"steghide","t":"Hide data within image and audio files","s":[["TAGLINE","Hide data within image and audio files"],["TLDR","**Embed** data in an image\n\n```steghide embed -cf [path/to/image.jpg] -ef [path/to/data.txt]```\n\n**Extract** data from a file\n\n```steghide extract -sf [path/to/image.jpg]```\n\nDisplay **file information**\n\n```steghide info [path/to/file.jpg]```\n\nEmbed with **maximum compression**\n\n```steghide embed -cf [path/to/image.jpg] -ef [path/to/data.txt] -z 9```\n\nList **encryption algorithms**\n\n```steghide encinfo```\n\nEmbed with **specific encryption**\n\n```steghide embed -cf [path/to/image.jpg] -ef [path/to/data.txt] -e blowfish cbc```\n\nExtract to **specific file**\n\n```steghide extract -sf [path/to/image.jpg] -xf [path/to/output.txt]```"],["SYNOPSIS","**steghide** _command_ [_arguments_]"],["COMMANDS","**embed**\n> Hide data in a cover file\n\n**extract**\n> Retrieve hidden data from stego file\n\n**info**\n> Display file information and detect embedded data\n\n**encinfo**\n> List available encryption algorithms and modes"],["PARAMETERS","**-cf, --coverfile** _file_\n> Cover file for embedding\n\n**-ef, --embedfile** _file_\n> File to embed (stdin if omitted)\n\n**-sf, --stegofile** _file_\n> Stego file (input for extract, output for embed)\n\n**-xf, --extractfile** _file_\n> Output filename for extracted data\n\n**-e, --encryption** _algo_ [_mode_]\n> Encryption algorithm and mode (default: rijndael-128 cbc)\n\n**-z, --compress** _level_\n> Compression level 1-9\n\n**-Z, --dontcompress**\n> Skip compression\n\n**-p, --passphrase** _pass_\n> Supply passphrase\n\n**-K, --nochecksum**\n> Omit CRC32 checksum\n\n**-N, --dontembedname**\n> Don't embed original filename\n\n**-f, --force**\n> Overwrite existing files\n\n**-v, --verbose**\n> Detailed output\n\n**-q, --quiet**\n> Suppress messages"],["DESCRIPTION","**steghide** is a steganography tool that hides data within JPEG, BMP, WAV, and AU files. It compresses and encrypts the secret data before embedding it in positions that preserve the cover file's statistical properties, making detection difficult.\n\nThe tool uses a graph-theoretic approach to find optimal embedding positions. Default encryption is AES-128 in CBC mode with a user-provided passphrase. The embedded data's integrity is verified using CRC32 checksums."],["CAVEATS","Cover file capacity depends on file size and content; **info** command shows available space. Only JPEG, BMP, WAV, and AU formats are supported as cover files. Heavily compressed images have less embedding capacity. Strong passphrases are essential for security. The cover file is modified in place unless **-sf** specifies a separate output."],["HISTORY","**steghide** was created by **Stefan Hetzl** and first released in **2003**. It remains one of the most widely used open-source steganography tools, commonly used in CTF competitions, security research, and privacy applications. The tool is available in most Linux distribution repositories."],["INSTALL","```apt: sudo apt install steghide```\n\n```dnf: sudo dnf install steghide```\n\n```nix: nix profile install nixpkgs#steghide```"],["SEE ALSO","[stegsnow](/man/stegsnow)(1), [outguess](/man/outguess)(1), [openstego](/man/openstego)(1)"]],"e":["stegsnow","outguess","openstego"],"x":"steghide embed -cf [path/to/image.jpg] -ef [path/to/data.txt]"},"systemd-resolve":{"n":"systemd-resolve","t":"Deprecated DNS resolver, use resolvectl","s":[["TAGLINE","Deprecated DNS resolver, use resolvectl"],["TLDR","**View** documentation for the renamed command\n\n```tldr resolvectl```"],["SYNOPSIS","**systemd-resolve** [_options_] [_hostname_|_address_|_record_]"],["DESCRIPTION","**systemd-resolve** has been renamed to **resolvectl** in newer versions of systemd. The functionality for resolving domain names, IPv4/IPv6 addresses, DNS resource records, and services is now provided by the resolvectl command."],["CAVEATS","This command is deprecated. Use **resolvectl** instead for DNS resolution and management of systemd-resolved."],["SEE ALSO","[resolvectl](/man/resolvectl)(1)"]],"e":["resolvectl"],"x":"tldr resolvectl"},"terraform-init":{"n":"terraform-init","t":"Initialize a Terraform working directory","s":[["TAGLINE","Initialize a Terraform working directory"],["TLDR","**Initialize the current directory**\n\n```terraform init```\n\n**Initialize and upgrade** providers and modules\n\n```terraform init -upgrade```\n\n**Initialize with backend configuration** from a file\n\n```terraform init -backend-config=[path/to/config.hcl]```\n\n**Initialize without configuring** a backend\n\n```terraform init -backend=false```\n\n**Initialize and migrate state** to a new backend\n\n```terraform init -migrate-state```\n\n**Initialize from a module source**\n\n```terraform init -from-module=[source]```"],["SYNOPSIS","**terraform** **init** [_options_]"],["PARAMETERS","**-upgrade**\n> Upgrade modules and plugins to the latest allowed versions.\n\n**-backend** _true|false_\n> Enable or disable backend configuration. Default is **true**.\n\n**-backend-config** _path_\n> Provide backend configuration via file path or key=value pair. Can be specified multiple times.\n\n**-reconfigure**\n> Reconfigure the backend, ignoring any saved configuration. Prevents state migration.\n\n**-migrate-state**\n> Attempt to copy existing state to the new backend with interactive prompts.\n\n**-force-copy**\n> Suppress migration prompts and automatically copy state.\n\n**-from-module** _source_\n> Copy a module into the target directory before initialization.\n\n**-get** _true|false_\n> Download child modules. Default is **true**.\n\n**-plugin-dir** _path_\n> Force plugin installation to read plugins only from the specified directory.\n\n**-lockfile** _mode_\n> Set lockfile mode. Use **readonly** to verify checksums without updating the lockfile.\n\n**-input** _true|false_\n> Enable or disable interactive prompts. Default is **true**.\n\n**-lock** _true|false_\n> Lock state files during operations. Default is **true**.\n\n**-lock-timeout** _duration_\n> Duration to retry acquiring a state lock. Default is **0s**.\n\n**-no-color**\n> Disable color codes in the output.\n\n**-json**\n> Enable machine-readable JSON output."],["DESCRIPTION","**terraform init** initializes a working directory containing Terraform configuration files. This is the first command to run after writing a new configuration or cloning an existing one from version control. It is safe to run multiple times and will not delete existing configuration or state.\n\nThe command performs several initialization steps: it configures the backend for storing state, downloads and installs provider plugins declared in the configuration, and downloads child modules referenced in the configuration. Each of these steps can be individually controlled with flags."],["CAVEATS","When changing backends, you must choose between **-reconfigure** (discard old state) and **-migrate-state** (copy state to new backend). Running init without either flag when the backend has changed will produce an error. The **-plugin-dir** option disables the default plugin download behavior, so the directory must contain all required providers."],["HISTORY","**Terraform** was created by **Mitchell Hashimoto** and released by **HashiCorp** in **2014**. The init command was introduced to manage the growing complexity of provider plugins and module dependencies. Provider plugin management was significantly reworked in **Terraform 0.13** (2020) with the introduction of the dependency lock file and the public Terraform Registry as the default plugin source."],["INSTALL","```pacman: sudo pacman -S terraform```\n\n```nix: nix profile install nixpkgs#terraform```"],["SEE ALSO","[terraform](/man/terraform)(1), [terraform-plan](/man/terraform-plan)(1), [terraform-apply](/man/terraform-apply)(1)"]],"e":["terraform","terraform-plan","terraform-apply"],"x":"terraform init"},"tgpt":{"n":"tgpt","t":"Terminal AI chatbot client","s":[["TAGLINE","Terminal AI chatbot client"],["TLDR","**Ask question**\n\n```tgpt \"[What is Linux?]\"```\n\n**Interactive mode**\n\n```tgpt -i```\n\n**Code mode**\n\n```tgpt -c \"[Write a Python hello world]\"```\n\n**Multiline input**\n\n```tgpt -m```\n\n**With specific provider**\n\n```tgpt --provider [openai] \"[question]\"```\n\n**Generate image**\n\n```tgpt -img \"[A sunset over mountains]\"```"],["SYNOPSIS","**tgpt** [_-i_] [_-c_] [_--provider name_] [_options_] [_prompt_]"],["PARAMETERS","**-i**\n> Interactive mode.\n\n**-c**\n> Code generation.\n\n**-m**\n> Multiline input.\n\n**--provider** _NAME_\n> AI provider.\n\n**-img**\n> Image generation.\n\n**-s**\n> Silent mode."],["DESCRIPTION","**tgpt** is a terminal-based client for interacting with AI language models from multiple providers including OpenAI, Anthropic, and others. It supports one-shot prompts, interactive conversation mode (**-i**), and specialized code generation mode (**-c**).\n\nThe tool can also generate images using models like DALL-E via the **-img** flag. Some providers work without requiring API keys, while others need keys configured for authentication. Multiline input mode (**-m**) allows composing longer prompts directly in the terminal."],["CAVEATS","API key may be needed. Network required. Provider-specific features."],["HISTORY","**tgpt** was created as a terminal-based interface to various AI language models without requiring API keys for some providers."],["INSTALL","```pacman: sudo pacman -S tgpt```\n\n```brew: brew install tgpt```\n\n```nix: nix profile install nixpkgs#tgpt```"],["SEE ALSO","[chatgpt-cli](/man/chatgpt-cli)(1), [sgpt](/man/sgpt)(1)"]],"e":["chatgpt-cli","sgpt"],"x":"tgpt \"[What is Linux?]\""},"tinifier":{"n":"tinifier","t":"CLI tool for compressing images via TinyPNG API","s":[["TAGLINE","CLI tool for compressing images via TinyPNG API"],["TLDR","**Compress all images in a directory**\n\n```tinifier --api-key [KEY] [./images/]```\n\n**Compress recursively with 4 threads**\n\n```tinifier -t 4 -r --api-key [KEY] [./images/]```\n\n**Compress preserving file modification time and EXIF data**\n\n```tinifier --preserve-time --api-key [KEY] [photo.png]```\n\n**Compress only specific file extensions**\n\n```tinifier --api-key [KEY] --ext [png,webp] [./images/]```\n\n**Skip files with less than 5% size reduction**\n\n```tinifier --api-key [KEY] --skip-if-diff-less [5] [./images/]```\n\n**Keep original files alongside compressed versions**\n\n```tinifier --api-key [KEY] --keep-original-file [./images/]```"],["SYNOPSIS","**tinifier** [_options_] _path_..."],["PARAMETERS","**-k**, **--api-key** _KEY_\n> TinyPNG API key. Multiple keys separated by commas for automatic switching when quotas are exceeded.\n\n**-t**, **--threads** _N_\n> Number of parallel threads (default: 16).\n\n**-r**, **--recursive**\n> Search for files in listed directories recursively.\n\n**-e**, **--ext** _EXTENSIONS_\n> File extensions to compress (default: png,jpeg,jpg,webp,avif).\n\n**-p**, **--preserve-time**\n> Preserve the original file modification date/time including EXIF data.\n\n**--keep-original-file**\n> Leave the original uncompressed file alongside the compressed one.\n\n**--skip-if-diff-less** _N_\n> Skip files if compression saves less than N percent (default: 1).\n\n**--max-errors** _N_\n> Maximum number of errors before stopping (default: 10, 0 to disable).\n\n**--retry-attempts** _N_\n> Number of retry attempts for operations (default: 3).\n\n**--delay-between-retries** _DURATION_\n> Delay between retry attempts (default: 1s).\n\n**-c**, **--config-file** _PATH_\n> Path to a configuration file."],["DESCRIPTION","**tinifier** compresses images using the TinyPNG API with parallel processing. It supports PNG, JPEG, WebP, and AVIF files. Multiple API keys can be provided for automatic switching when quotas are exceeded. The tool can recursively scan directories and preserves original file metadata on request."],["CAVEATS","Requires a TinyPNG API key (free tier: 500 compressions/month). Files are uploaded to TinyPNG servers for compression. Network connectivity is required."],["HISTORY","**tinifier** was created by **tarampampam** and is written in **Go**."],["INSTALL","```aur: yay -S tinifier```"],["SEE ALSO","[optipng](/man/optipng)(1), [jpegoptim](/man/jpegoptim)(1), [pngquant](/man/pngquant)(1), [imagemagick](/man/imagemagick)(1)"]],"e":["optipng","jpegoptim","pngquant","imagemagick"],"x":"tinifier --api-key [KEY] [./images/]"},"toe":{"n":"toe","t":"List terminfo terminal type entries","s":[["TAGLINE","List terminfo terminal type entries"],["TLDR","**List all terminal types**\n\n```toe```\n\n**List all entries including incomplete**\n\n```toe -a```\n\n**List with directory headers**\n\n```toe -h```\n\n**Show only primary names**\n\n```toe -s```\n\n**Scan specific directory**\n\n```toe [/usr/share/terminfo]```\n\n**List entries from multiple directories**\n\n```toe [/usr/share/terminfo] [~/.terminfo]```"],["SYNOPSIS","**toe** [_options_] [_directory_...]"],["PARAMETERS","**-a**\n> List all entries, including incomplete ones.\n\n**-h**\n> Display directory header as each directory is entered.\n\n**-s**\n> Short list: show only the first alias for each entry.\n\n**-u** _file_\n> Write report to file with sorted list of dependencies.\n\n**-v**\n> Display version and exit.\n\n**-V**\n> Display version and settings, then exit."],["DESCRIPTION","**toe** (Table Of Entries) lists terminal type descriptions available in the terminfo database. It's part of the ncurses package and helps identify what terminal capabilities are installed on a system.\n\nThe terminfo database contains compiled descriptions of terminal capabilities used by applications to properly interact with different terminal types. Each entry describes features like cursor movement, color support, function keys, and other terminal-specific behaviors.\n\nWithout arguments, toe scans the default terminfo directory. With directory arguments, it scans those directories instead. The output shows terminal type names with brief descriptions."],["CAVEATS","Only lists entries in terminfo format, not old termcap files. The default directory location varies by distribution. Some minimal installations may have limited terminfo entries. Not portable to non-ncurses systems."],["HISTORY","**toe** was developed as part of the ncurses library, which is a free software emulation of curses. The command name is a developer's pun: tic, tac (now tack), toe, forming the classic game reference. The terminfo system replaced the older termcap database to overcome size limitations and improve performance."],["INSTALL","```apt: sudo apt install ncurses-bin```\n\n```dnf: sudo dnf install ncurses```\n\n```pacman: sudo pacman -S ncurses```\n\n```apk: sudo apk add ncurses```\n\n```brew: brew install ncurses```\n\n```nix: nix profile install nixpkgs#ncurses```"],["SEE ALSO","[tic](/man/tic)(1), [infocmp](/man/infocmp)(1), [tput](/man/tput)(1), [tset](/man/tset)(1)"]],"e":["tic","infocmp","tput","tset"],"x":"toe"},"toolong":{"n":"toolong","t":"View, tail, merge, and search log files","s":[["TAGLINE","View, tail, merge, and search log files"],["TLDR","**Open and view a log file**\n\n```tl [access.log]```\n\n**Merge and view multiple logs by timestamp**\n\n```tl [server.log] [app.log] --merge```\n\n**Pipe live output into toolong**\n\n```tail -f [/var/log/syslog] | tl```\n\n**Tail a log file** (start at the end)\n\n```tl --tail [access.log]```"],["SYNOPSIS","**tl** [_options_] [_files_...]"],["PARAMETERS","**--merge**\n> Merge multiple log files by timestamp.\n\n**--tail**\n> Start at the end of the file.\n\n**-h**, **--help**\n> Display help information.\n\n**-V**, **--version**\n> Display version information."],["DESCRIPTION","**toolong** is a terminal application for viewing and analyzing log files, including JSONL. It can open multi-gigabyte files instantly, live-tail logs, auto-detect timestamps for merging multiple files, handle compressed .bz/.bz2 files, and syntax-highlight common log formats."],["HISTORY","**toolong** was created by **Will McGugan** (Textualize) and is written in **Python** using the Textual framework."],["INSTALL","```nix: nix profile install nixpkgs#toolong```"],["SEE ALSO","[lnav](/man/lnav)(1), [less](/man/less)(1), [multitail](/man/multitail)(1)"]],"e":["lnav","less","multitail"],"x":"tl [access.log]"},"unexpand":{"n":"unexpand","t":"Convert spaces to tabs in text files","s":[["TAGLINE","Convert spaces to tabs in text files"],["TLDR","**Convert leading spaces to tabs** in a file\n\n```unexpand [file]```\n\n**Convert all spaces** (not just leading) to tabs\n\n```unexpand -a [file]```\n\n**Use a specific tab width**\n\n```unexpand -t [4] [file]```\n\n**Convert and save to a new file**\n\n```unexpand [input.txt] > [output.txt]```\n\n**Process multiple files**\n\n```unexpand [file1] [file2]```\n\n**Read from stdin**\n\n```cat [file] | unexpand```"],["SYNOPSIS","**unexpand** [_options_] [_file ..._]"],["PARAMETERS","**-a**, **--all**\n> Convert all sequences of two or more spaces to tabs, not just leading spaces.\n\n**-t** _n_, **--tabs=**_n_\n> Set tab stops every n columns (default: 8).\n\n**-t** _list_, **--tabs=**_list_\n> Set tab stops at specified column positions (comma-separated).\n\n**--first-only**\n> Convert only leading blanks (default behavior).\n\n**--help**\n> Display help message and exit.\n\n**--version**\n> Display version information and exit."],["DESCRIPTION","**unexpand** converts spaces to tabs in files, writing to standard output. By default, it only converts leading spaces (at the beginning of lines) into the maximum number of tabs followed by the minimum spaces needed to fill the same columns.\n\nWith **-a**, all sequences of spaces (not just leading) are converted. Tab stops default to every 8 columns but can be changed with **-t**.\n\nWhen no file is specified or when file is **-**, unexpand reads from standard input."],["CAVEATS","By default, unexpand only converts leading spaces, unlike **expand** which converts all tabs by default. This asymmetry can cause unexpected results when round-tripping between the two commands. Use **-a** for consistent all-space conversion."],["HISTORY","unexpand is part of **GNU coreutils**, written by **David MacKenzie**. The command originated in early Unix systems as a companion to the expand utility. Both commands address the historical need to convert between tabs and spaces for different terminal and printer requirements."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[expand](/man/expand)(1), [cat](/man/cat)(1), [pr](/man/pr)(1), [col](/man/col)(1)"]],"e":["expand","cat","pr","col"],"x":"unexpand [file]"},"vault":{"n":"vault","t":"HashiCorp secrets management tool","s":[["TAGLINE","HashiCorp secrets management tool"],["TLDR","**Start the Vault server** in development mode\n\n```vault server -dev```\n\n**Login to Vault** with a token\n\n```vault login [token]```\n\n**Write a secret** to the key-value store\n\n```vault kv put secret/[path] [key]=[value]```\n\n**Read a secret**\n\n```vault kv get secret/[path]```\n\n**List secrets** at a path\n\n```vault kv list secret/```\n\n**Delete a secret**\n\n```vault kv delete secret/[path]```\n\n**Enable a secrets engine**\n\n```vault secrets enable -path=[path] [engine]```\n\n**Check Vault status**\n\n```vault status```"],["SYNOPSIS","**vault** [_global_options_] _command_ [_command_options_] [_arguments_]"],["PARAMETERS","**login**\n> Authenticate to Vault and obtain a token.\n\n**kv put**\n> Write secrets to the key-value secrets engine.\n\n**kv get**\n> Read secrets from the key-value secrets engine.\n\n**kv delete**\n> Delete secrets from the key-value secrets engine.\n\n**kv list**\n> List secret keys at a path.\n\n**secrets enable**\n> Enable a secrets engine at a path.\n\n**secrets disable**\n> Disable a secrets engine.\n\n**auth enable**\n> Enable an authentication method.\n\n**operator init**\n> Initialize a new Vault cluster.\n\n**operator unseal**\n> Unseal the Vault.\n\n**operator seal**\n> Seal the Vault.\n\n**status**\n> Print the current status of Vault.\n\n**server**\n> Start a Vault server.\n\n**token** _subcommand_\n> Manage tokens (capabilities, create, lookup, renew, revoke).\n\n**policy** _subcommand_\n> Manage policies (delete, fmt, list, read, write).\n\n**audit** _subcommand_\n> Manage audit devices (disable, enable, list).\n\n**lease** _subcommand_\n> Manage leases (lookup, renew, revoke).\n\n**read** _path_\n> Read data from Vault.\n\n**write** _path_ _data_\n> Write data to Vault.\n\n**delete** _path_\n> Delete data from Vault.\n\n**list** _path_\n> List data from Vault.\n\n**path-help** _path_\n> Show help for a specific API path.\n\n**-address** _url_\n> Address of the Vault server.\n\n**-token** _token_\n> Vault token for authentication.\n\n**-format** _format_\n> Output format: table, json, yaml.\n\n**-help**\n> Display help for a command."],["DESCRIPTION","**Vault** by HashiCorp is a secrets management tool that securely stores and controls access to tokens, passwords, certificates, API keys, and other secrets. It provides encryption as a service, dynamic secrets generation, and detailed audit logging.\n\nThe CLI is a wrapper around the HTTP API and supports interactive and scripted use. Configuration can be set via environment variables: **VAULT_ADDR** for the server address, **VAULT_TOKEN** for authentication, **VAULT_NAMESPACE** for enterprise namespaces, and **VAULT_FORMAT** for output format (table, json, yaml, jsonx).\n\nVault uses a seal/unseal mechanism for security. After initialization, Vault is sealed and requires unseal keys to become operational. In dev mode (**vault server -dev**), Vault starts unsealed for testing."],["CAVEATS","The dev server stores data in memory and is not for production use. Unsealing requires a threshold of key shares; losing too many shares can make the Vault unrecoverable. Token management is critical for security; leaked tokens should be immediately revoked."],["HISTORY","Vault was created by **HashiCorp** and first released in **April 2015**. It was designed to address the challenge of managing secrets in modern infrastructure. Key features like dynamic secrets, leasing, and renewal were core from the beginning. Vault has grown to support numerous authentication methods and secrets engines, becoming a standard tool for secrets management in DevOps and cloud-native environments."],["INSTALL","```pacman: sudo pacman -S vault```\n\n```nix: nix profile install nixpkgs#vault```"],["SEE ALSO","[consul](/man/consul)(1), [terraform](/man/terraform)(1), [gpg](/man/gpg)(1), [pass](/man/pass)(1)"]],"e":["consul","terraform","gpg","pass"],"x":"vault server -dev"},"vultr-cli":{"n":"vultr-cli","t":"Official command-line client for the Vultr API","s":[["TAGLINE","Official command-line client for the Vultr API"],["TLDR","**List regions**\n\n```vultr-cli regions```\n\n**List plans**\n\n```vultr-cli plans```\n\n**List instances**\n\n```vultr-cli instance list```\n\n**Account info**\n\n```vultr-cli account```\n\n**JSON output**\n\n```vultr-cli -o json instance list```\n\n**Show version**\n\n```vultr-cli version```\n\n**Help for a resource**\n\n```vultr-cli instance --help```"],["SYNOPSIS","**vultr-cli** [*flags*] *command*"],["DESCRIPTION","**vultr-cli** is the official command-line interface for [Vultr](https://www.vultr.com/) cloud services. It covers account and billing, instances, bare metal, block and object storage, DNS, firewalls, Kubernetes, load balancers, snapshots, SSH keys, VPCs, CDNs, databases, and more.\n\nInstall via GitHub releases, **go install github.com/vultr/vultr-cli/v3@latest**, distro packages (for example Arch **pacman -S vultr-cli**), Homebrew, or Docker. Authenticate with a Vultr API key (see project docs for config env/file)."],["PARAMETERS","Global flags:\n\n**-o**, **--output** text|json|yaml\n\n> Output format (default **text**).\n\n**--config** *path*\n\n> Path to config file.\n\nResource commands (each has subcommands like **list**, **create**, **delete**): **account**, **apps**, **backups**, **bare-metal**, **billing**, **block-storage**, **cdn**, **container-registry**, **database**, **dns**, **firewall**, **inference**, **instance**, **iso**, **kubernetes**, **load-balancer**, **logs**, **marketplace**, **object-storage**, **os**, **plans**, **regions**, **reserved-ip**, **script**, **snapshot**, **ssh-key**, **user**, **vpc**, **version**, **completion**.\n\nUse **vultr-cli** *command* **--help** for flags and examples."],["CONFIGURATION","Requires a Vultr API token with appropriate permissions. Prefer environment or config-file storage over shell history. Exact variable and file names are documented upstream for your installed version."],["CAVEATS","Destructive instance and storage commands are irreversible. API rate limits apply. Keep the CLI updated when Vultr adds API resources."],["INSTALL","```dnf: sudo dnf install vultr-cli```\n\n```pacman: sudo pacman -S vultr-cli```\n\n```brew: brew install vultr-cli```\n\n```nix: nix profile install nixpkgs#vultr-cli```"],["SEE ALSO","[doctl](/man/doctl)(1), [linode-cli](/man/linode-cli)(1), [hcloud](/man/hcloud)(1), [aws](/man/aws)(1)"],["RESOURCES","```[Source code](https://github.com/vultr/vultr-cli)```"]],"e":["doctl","linode-cli","hcloud","aws"],"x":"vultr-cli regions"},"wacli-messages-search":{"n":"wacli-messages-search","t":"Search WhatsApp message history using full-text search","s":[["TAGLINE","Search WhatsApp message history using full-text search"],["TLDR","**Search all messages** for a keyword\n\n```wacli messages search \"meeting notes\" --json```\n\n**Search within a specific chat**\n\n```wacli messages search \"project\" --chat 491234567890@s.whatsapp.net --json```\n\n**Search messages from a sender**\n\n```wacli messages search \"report\" --from 491234567890@s.whatsapp.net --json```\n\n**Search by message type** (image, video, etc.)\n\n```wacli messages search \"\" --type image --json```\n\n**Search documents that have media**\n\n```wacli messages search \"invoice\" --has-media --type document --json```\n\n**Limit and filter by date**\n\n```wacli messages search \"budget\" --chat 491234567890@s.whatsapp.net --after 2026-01-01 --limit 5 --json```"],["SYNOPSIS","**wacli** **messages** **search** _query_ [--chat _jid_] [--from _jid_] [--has-media] [--type _type_] [--forwarded] [--starred] [--limit _n_] [--after _date_] [--before _date_] [--json] [--store _dir_]"],["DESCRIPTION","**wacli messages search** performs a full-text search over the locally synced message bodies. It uses SQLite FTS5 when the binary was built with `-tags sqlite_fts5`, and falls back to `LIKE` otherwise. Optional filters cover chat, sender, date range, media, type, and starred/forwarded flags.\n\nSearch works completely offline once the store has been synced. Status broadcasts are not included.\n\nDefault store path is `~/.local/state/wacli` on Linux and `~/.wacli` elsewhere."],["PARAMETERS","_query_\n> Search terms. Supports simple terms; quote phrases as needed for the shell.\n\n**--chat** _jid_\n> Limit search to one chat.\n\n**--from** _jid_\n> Limit to messages from this sender.\n\n**--has-media**\n> Only messages that include media.\n\n**--type** _type_\n> Filter by content type: `text`, `image`, `video`, `audio`, or `document`.\n\n**--forwarded**\n> Only forwarded messages.\n\n**--starred**\n> Only starred messages.\n\n**--after**, **--before** _date_\n> Date filters (RFC3339 or `YYYY-MM-DD`).\n\n**--limit** _n_\n> Maximum results.\n\n**--json**\n> JSON output.\n\n**--store** _dir_\n> Override the store directory (cannot be combined with `--account`)."],["CAVEATS","Search only covers messages that have been synced into the local store. History is best-effort; use `wacli history backfill` for older messages."],["SEE ALSO","[wacli](/man/wacli)(1), [wacli-messages-list](/man/wacli-messages-list)(1), [wacli-messages-show](/man/wacli-messages-show)(1), [wacli-messages-context](/man/wacli-messages-context)(1)"],["RESOURCES","```[Source code](https://github.com/openclaw/wacli)```\n\n```[Documentation](https://wacli.sh/messages.html)```"]],"e":["wacli","wacli-messages-list","wacli-messages-show","wacli-messages-context"],"x":"wacli messages search \"meeting notes\" --json"},"wayland-info":{"n":"wayland-info","t":"Display information about a Wayland compositor","s":[["TAGLINE","Display information about a Wayland compositor"],["TLDR","**Show** all globals the compositor advertises\n\n```wayland-info```\n\n**Filter** the output to a single interface\n\n```wayland-info -i [wl_output]```\n\n**Connect** to a specific Wayland display\n\n```wayland-info -d [wayland-1]```\n\n**Save** the report to a file\n\n```wayland-info > [path/to/file.txt]```"],["SYNOPSIS","**wayland-info** [**-d** _display_] [**-i** _interface_]..."],["DESCRIPTION","**wayland-info** connects to a running **Wayland** compositor and prints the global objects it exposes through the registry, together with their interface name and version. For well-known interfaces it also decodes and prints extra detail, such as the modes, geometry, scale, and transform of each **wl_output**, the formats supported by **wl_shm**, and the available seats and their capabilities.\n\nIt is the Wayland equivalent of tools like **xrandr** or **glxinfo** for X11, and is useful for checking which protocols and protocol versions a compositor supports, debugging multi-monitor setups, and confirming that a session is actually running under Wayland. The tool is part of the **wayland-utils** package and supersedes the older **weston-info**."],["PARAMETERS","**-d** _display_, **--display** _display_\n\n> Connect to the named Wayland display instead of the one in the **WAYLAND_DISPLAY** environment variable.\n\n**-i** _interface_, **--interface** _interface_\n\n> Print information only for globals matching the given interface name. May be repeated to select several interfaces.\n\n**-h**, **--help**\n\n> Show usage information."],["CAVEATS","The tool must be run inside a Wayland session (or with **WAYLAND_DISPLAY**/**-d** pointing at a reachable compositor socket); it cannot report on an X11 session. The exact set of decoded interfaces depends on the installed version of wayland-utils."],["INSTALL","```apt: sudo apt install wayland-utils```\n\n```dnf: sudo dnf install wayland-utils```\n\n```pacman: sudo pacman -S wayland-utils```\n\n```apk: sudo apk add wayland-utils```\n\n```zypper: sudo zypper install wayland-utils```\n\n```nix: nix profile install nixpkgs#wayland-utils```"],["SEE ALSO","[swaymsg](/man/swaymsg)(1), [weston](/man/weston)(1), [drm_info](/man/drm_info)(1), [glxinfo](/man/glxinfo)(1), [xrandr](/man/xrandr)(1)"],["RESOURCES","```[Source code](https://gitlab.freedesktop.org/wayland/wayland-utils)```\n\n```[Homepage](https://wayland.freedesktop.org)```"]],"e":["swaymsg","weston","drm_info","glxinfo","xrandr"],"x":"wayland-info"},"who":{"n":"who","t":"Show logged-in users","s":[["TAGLINE","Show logged-in users"],["TLDR","**Show who is logged in**\n\n```who```\n\n**Show current user** only\n\n```who am i```\n\n**Show with column headers**\n\n```who -H```\n\n**Show login time and idle time**\n\n```who -u```\n\n**Show all available information**\n\n```who -a```\n\n**Count logged in users**\n\n```who -q```\n\n**Show system boot time**\n\n```who -b```"],["SYNOPSIS","**who** [_options_] [_file_] [**am i**]"],["PARAMETERS","**-a**, **--all**\n> Print all available information\n\n**-b**, **--boot**\n> Time of last system boot\n\n**-d**, **--dead**\n> Print dead processes\n\n**-H**, **--heading**\n> Print column headers\n\n**-l**, **--login**\n> Print system login processes\n\n**-q**, **--count**\n> Only usernames and count of logged-in users\n\n**-r**, **--runlevel**\n> Print current runlevel\n\n**-p**, **--process**\n> Print active processes spawned by init\n\n**-s**, **--short**\n> Print only name, line, and time (default)\n\n**-t**, **--time**\n> Print last system clock change\n\n**-T**, **-w**, **--mesg**\n> Add user's message status (+, -, ?)\n\n**-u**, **--users**\n> List users logged in\n\n**--lookup**\n> Attempt to canonicalize hostnames via DNS\n\n**am i**\n> Print only current terminal's user info"],["DESCRIPTION","**who** displays information about users currently logged into the system. It reads from **/var/run/utmp** (or another specified file) which tracks login sessions.\n\nThe basic output shows username, terminal line, and login time. Additional options reveal idle time, process IDs, and message status (whether the user accepts write/talk messages).\n\n**who am i** shows information about the original login session. Unlike **whoami** (which shows the effective user), **who am i** displays the user who originally logged in, even after **su** to another account.\n\nThe command can read historical login data from **/var/log/wtmp** to show past logins."],["CAVEATS","Who shows login sessions, not all user processes. Users may have processes running without active login sessions.\n\nRemote connections may show as pseudo-terminals (pts/N) rather than physical terminals (tty).\n\nThe utmp file format varies between Unix systems. Some information may not be available on all platforms."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[w](/man/w)(1), [whoami](/man/whoami)(1), [users](/man/users)(1), [last](/man/last)(1), [finger](/man/finger)(1)"]],"e":["w","whoami","users","last","finger"],"x":"who"},"xdg-mime":{"n":"xdg-mime","t":"Query and manage MIME type associations","s":[["TAGLINE","Query and manage MIME type associations"],["TLDR","**Display** the MIME type of a file\n\n```xdg-mime query filetype [path/to/file]```\n\n**Display** the default application for opening PNGs\n\n```xdg-mime query default image/png```\n\n**Display** the default application for a file's MIME type\n\n```xdg-mime query default $(xdg-mime query filetype [path/to/file])```\n\n**Set** an application as default for specific MIME types\n\n```xdg-mime default [imv.desktop] image/png image/jpeg```"],["SYNOPSIS","**xdg-mime** _command_ [_arguments_]"],["PARAMETERS","**query filetype _file_**\n> Display the MIME type of a file\n\n**query default _mimetype_**\n> Display the default application for a MIME type\n\n**default _application_ _mimetype_...**\n> Set default application for MIME types\n\n**install _mimetypes-file_**\n> Install a MIME type definition\n\n**uninstall _mimetypes-file_**\n> Uninstall a MIME type definition"],["DESCRIPTION","**xdg-mime** queries and manages MIME types according to the XDG (freedesktop.org) standard. It can detect file types, query default applications, and set application associations.\n\nThe tool integrates with the desktop environment's MIME type handling, storing user preferences in **~/.config/mimeapps.list**."],["CAVEATS","Application associations use .desktop file names. Changes may require restarting applications to take effect. Works with XDG-compliant desktop environments."],["INSTALL","```dnf: sudo dnf install xdg-utils```\n\n```pacman: sudo pacman -S xdg-utils```\n\n```apk: sudo apk add xdg-utils```\n\n```zypper: sudo zypper install xdg-utils```\n\n```nix: nix profile install nixpkgs#xdg-utils```"],["SEE ALSO","[xdg-open](/man/xdg-open)(1), [xdg-settings](/man/xdg-settings)(1), [update-mime-database](/man/update-mime-database)(1)"]],"e":["xdg-open","xdg-settings","update-mime-database"],"x":"xdg-mime query filetype [path/to/file]"},"xset":{"n":"xset","t":"Configure X server preferences","s":[["TAGLINE","Configure X server preferences"],["TLDR","**Disable** screensaver\n\n```xset s off```\n\n**Disable** bell sound\n\n```xset b off```\n\nSet screensaver **timeout**\n\n```xset s 3600 3600```\n\n**Disable** DPMS\n\n```xset -dpms```\n\n**Enable** DPMS\n\n```xset +dpms```\n\n**Query** current settings\n\n```xset q```\n\nSet **keyboard repeat** rate\n\n```xset r rate 200 30```\n\nSet **LED** state\n\n```xset led 3```"],["SYNOPSIS","**xset** [_-display display_] [_options_]"],["DESCRIPTION","**xset** is a user preference utility for X. It allows setting various user preference options for the X server, including keyboard, mouse, screen saver, and DPMS (Energy Star) settings."],["PARAMETERS","**s [timeout [cycle]]**\n> Set screen saver timeout and cycle time in seconds\n\n**s on|off|default**\n> Enable, disable, or reset screen saver\n\n**s blank|noblank**\n> Set screen saver to blank or pattern mode\n\n**s expose|noexpose**\n> Allow/disallow screen exposure events\n\n**-dpms**\n> Disable DPMS (Display Power Management Signaling)\n\n**+dpms**\n> Enable DPMS\n\n**dpms standby suspend off**\n> Set DPMS timeouts in seconds\n\n**dpms force on|standby|suspend|off**\n> Force DPMS state immediately\n\n**b [volume [pitch [duration]]]**\n> Set bell parameters\n\n**b on|off**\n> Enable or disable bell\n\n**r [keycode] on|off**\n> Enable or disable auto-repeat for keys\n\n**r rate [delay [rate]]**\n> Set keyboard repeat delay (ms) and rate (per second)\n\n**led [on|off]**\n> Set all LEDs on or off\n\n**led N**\n> Turn LED N on\n\n**-led N**\n> Turn LED N off\n\n**fp= path,...**\n> Set font path\n\n**fp default**\n> Reset font path to default\n\n**q**\n> Query current settings\n\n**-display display**\n> Specify X display"],["CAVEATS","Settings are temporary and reset when X restarts. For persistent settings, add xset commands to ~/.xprofile or session startup scripts. DPMS settings may be overridden by desktop environment power management."],["HISTORY","**xset** is part of the core X.Org utilities, providing command-line access to X server preferences that were historically set through X resources or window manager configurations."],["INSTALL","```apt: sudo apt install x11-xserver-utils```\n\n```dnf: sudo dnf install xset```\n\n```apk: sudo apk add xset```\n\n```zypper: sudo zypper install xset```\n\n```nix: nix profile install nixpkgs#xset```"],["SEE ALSO","[xrandr](/man/xrandr)(1), [setxkbmap](/man/setxkbmap)(1), [xmodmap](/man/xmodmap)(1)"]],"e":["xrandr","setxkbmap","xmodmap"],"x":"xset s off"}});
