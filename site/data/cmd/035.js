window.LCL_BUCKET(35,{"acme.sh":{"n":"acme.sh","t":"shell-based ACME SSL/TLS certificate client","s":[["TAGLINE","shell-based ACME SSL/TLS certificate client"],["TLDR","**Issue** a certificate using webroot mode\n\n```acme.sh --issue -d [example.com] -w [/var/www/html]```\n\nIssue a **wildcard certificate** using DNS mode\n\n```acme.sh --issue -d [example.com] -d [*.example.com] --dns [dns_cf]```\n\n**Install** a certificate\n\n```acme.sh --install-cert -d [example.com] --key-file [/path/to/key.pem] --fullchain-file [/path/to/cert.pem] --reloadcmd \"[systemctl reload nginx]\"```\n\n**Renew** all certificates\n\n```acme.sh --renew-all```\n\n**List** all issued certificates\n\n```acme.sh --list```"],["SYNOPSIS","**acme.sh** _--issue_|_--install-cert_|_--renew_ [_-d domain_] [_options_]"],["DESCRIPTION","**acme.sh** is a pure Unix shell ACME client for obtaining free SSL/TLS certificates from Let's Encrypt and other ACME-compliant certificate authorities. It supports multiple validation methods including webroot, standalone, DNS, and Apache/Nginx plugins.\n\nThe tool automatically handles certificate renewal through cron jobs and supports wildcard certificates via DNS validation. It requires no dependencies beyond a POSIX-compliant shell and common utilities like curl or wget."],["PARAMETERS","**--issue**\n> Issue a new certificate\n\n**--install-cert**\n> Install issued certificate to specified locations\n\n**--renew**\n> Renew a specific certificate\n\n**--renew-all**\n> Renew all issued certificates\n\n**--list**\n> List all issued certificates\n\n**-d** _domain_\n> Domain name (can be repeated for multiple domains/SANs)\n\n**-w** _path_\n> Webroot path for HTTP validation\n\n**--dns** _provider_\n> Use DNS validation with specified provider (dns_cf, dns_aws, etc.)\n\n**--standalone**\n> Use standalone mode (starts temporary web server)\n\n**--nginx**\n> Use Nginx mode for validation\n\n**--apache**\n> Use Apache mode for validation\n\n**--key-file** _path_\n> Path to install private key\n\n**--fullchain-file** _path_\n> Path to install full certificate chain\n\n**--reloadcmd** _cmd_\n> Command to reload service after installation\n\n**--force**\n> Force renewal regardless of expiry\n\n**--revoke** **-d** _domain_\n> Revoke a certificate\n\n**--remove** **-d** _domain_\n> Remove a certificate from the renewal list\n\n**--server** _url_\n> Specify ACME server (default: Let's Encrypt). Use letsencrypt, zerossl, buypass, or a custom URL.\n\n**--upgrade**\n> Upgrade acme.sh to the latest version"],["CONFIGURATION","**~/.acme.sh/account.conf**\n> Main configuration file storing default settings, CA server, email, and DNS API credentials.\n\n**~/.acme.sh/[domain]/[domain].conf**\n> Per-domain configuration storing issuance parameters, validation method, and renewal settings.\n\n**~/.acme.sh/[domain]/[domain].cer**\n> Issued certificate file for the domain.\n\n**~/.acme.sh/[domain]/[domain].key**\n> Private key file for the domain."],["CAVEATS","DNS validation requires API credentials for your DNS provider. Webroot mode requires the web server to be running and accessible. Standalone mode requires ports 80/443 to be available. Automatic renewal depends on cron; ensure it's running."],["HISTORY","**acme.sh** was created by Neil Pang (Neilpang) and first released in **2015** shortly after Let's Encrypt announced their service. It was designed as a lightweight, dependency-free alternative to Certbot, becoming one of the most popular ACME clients for shell environments."],["INSTALL","```pacman: sudo pacman -S acme.sh```\n\n```apk: sudo apk add acme.sh```\n\n```brew: brew install acme.sh```"],["SEE ALSO","[certbot](/man/certbot)(1), [openssl](/man/openssl)(1), [nginx](/man/nginx)(8), [apache2](/man/apache2)(8)"]],"e":["certbot","openssl","nginx","apache2"],"x":"acme.sh --issue -d [example.com] -w [/var/www/html]"},"adb-uninstall":{"n":"adb-uninstall","t":"Remove an application from an Android device","s":[["TAGLINE","Remove an application from an Android device"],["TLDR","**Uninstall** an application\n\n```adb uninstall [com.example.app]```\n\nUninstall but **keep data** and cache\n\n```adb uninstall -k [com.example.app]```\n\nUninstall for a **specific user**\n\n```adb uninstall --user [user_id] [com.example.app]```\n\nUninstall **only if version code matches**\n\n```adb uninstall --versionCode [version_code] [com.example.app]```"],["SYNOPSIS","**adb uninstall** [_options_] _package_"],["DESCRIPTION","**adb uninstall** removes an installed Android application from the device. The package is specified by its full package name (e.g., com.example.app), not the APK filename.\n\nBy default, uninstalling removes the app along with all its data, cache, and associated files for all users on the device. Use the **-k** flag to preserve application data."],["PARAMETERS","**-k**\n> Keep the data and cache directories after package removal.\n\n**--user** _user_id_\n> Remove the package only for the given user. Default removes for all users.\n\n**--versionCode** _code_\n> Only uninstall if the installed app has the given version code.\n\n**package**\n> Full package name of the application to remove."],["CAVEATS","System applications cannot be uninstalled without root access or using **adb shell pm uninstall -k --user 0**. The package name is case-sensitive. Pre-installed apps may be disabled instead of fully removed."],["HISTORY","**adb uninstall** has been available since the initial Android SDK release in **2008**, providing a simple way to remove applications during development and testing."],["INSTALL","```apt: sudo apt install adb```\n\n```dnf: sudo dnf install android-tools```\n\n```pacman: sudo pacman -S android-tools```\n\n```apk: sudo apk add android-tools-adb```\n\n```zypper: sudo zypper install android-tools```\n\n```nix: nix profile install nixpkgs#android-tools```"],["SEE ALSO","[adb](/man/adb)(1), [adb-install](/man/adb-install)(1), [adb-shell-pm](/man/adb-shell-pm)(1)"]],"e":["adb","adb-install","adb-shell-pm"],"x":"adb uninstall [com.example.app]"},"adscript":{"n":"adscript","t":"Audio and digital signal processing language compiler","s":[["TAGLINE","Audio and digital signal processing language compiler"],["TLDR","**Compile** an ADscript file\n\n```adscript [script.ad]```\n\nCompile with **output file**\n\n```adscript [script.ad] -o [output]```\n\n**Run** an ADscript directly\n\n```adscript -r [script.ad]```"],["SYNOPSIS","**adscript** [_options_] _file_"],["DESCRIPTION","**adscript** is a compiler for the ADscript programming language, a domain-specific language designed for audio and digital signal processing applications. It generates optimized code for audio processing tasks.\n\nADscript combines functional programming concepts with audio-specific primitives, making it suitable for developing audio plugins, synthesis engines, and signal processing algorithms."],["PARAMETERS","**-o** _file_\n> Specify output file name\n\n**-r**\n> Run the script directly instead of compiling\n\n**-O** _level_\n> Optimization level (0-3)\n\n**-v**, **--verbose**\n> Enable verbose output\n\n**--help**\n> Display help information"],["CAVEATS","ADscript is a specialized language; general-purpose programming may be better suited to other languages. Performance depends on target platform and optimization settings."],["HISTORY","ADscript was developed for audio programming applications, providing a high-level abstraction over low-level audio processing operations while maintaining performance characteristics needed for real-time audio."],["SEE ALSO","[csound](/man/csound)(1)"]],"e":["csound"],"x":"adscript [script.ad]"},"apgbfm":{"n":"apgbfm","t":"Manage bloom filters for password checking","s":[["TAGLINE","Manage bloom filters for password checking"],["TLDR","**Create** a new bloom filter from a dictionary file\n\n```apgbfm -f [filter.bf] -d [dictionary.txt]```\n\n**Create** an empty bloom filter for a specific number of words\n\n```apgbfm -f [filter.bf] -n [50000]```\n\n**Check** if a word exists in the filter\n\n```apgbfm -f [filter.bf] -c [password]```\n\n**Add** a single word to an existing filter\n\n```apgbfm -f [filter.bf] -a [word]```\n\n**Add** words from a dictionary file to an existing filter\n\n```apgbfm -f [filter.bf] -A [dictionary.txt]```\n\n**Display** filter information\n\n```apgbfm -i [filter.bf]```"],["SYNOPSIS","**apgbfm** **-f** _filter_ **-n** _numofwords_ [**-q**] [**-s**]\n**apgbfm** **-f** _filter_ **-d** _dictfile_ [**-q**] [**-s**]\n**apgbfm** **-f** _filter_ **-a** _word_ [**-q**]\n**apgbfm** **-f** _filter_ **-A** _dictfile_ [**-q**]\n**apgbfm** **-f** _filter_ **-c** _word_ [**-q**]\n**apgbfm** **-f** _filter_ **-C** _dictfile_ [**-q**]\n**apgbfm** **-i** _filter_\n**apgbfm** [**-v**] [**-h**]"],["DESCRIPTION","**apgbfm** (APG Bloom Filter Manager) creates and manages bloom filters for the apg password generator. Bloom filters enable fast dictionary checking without loading entire dictionaries into memory. It uses SHA-1 as its hash function.\n\nThis tool can be used standalone or with apg/apgd to avoid generating passwords that appear in common password dictionaries."],["PARAMETERS","**-f** _filter_\n> Bloom filter filename to use\n\n**-n** _numofwords_\n> Create new empty filter for the specified number of words\n\n**-d** _dictfile_\n> Create new filter from dictionary file\n\n**-a** _word_\n> Add a single word to the filter\n\n**-A** _dictfile_\n> Add all words from dictionary file to the filter\n\n**-c** _word_\n> Check if a word exists in the filter\n\n**-C** _dictfile_\n> Check every word from dictionary file against the filter\n\n**-i** _filter_\n> Display filter information\n\n**-s**\n> Create filter in case-insensitive mode\n\n**-q**\n> Quiet mode\n\n**-v**\n> Print version information\n\n**-h**\n> Print help information"],["CAVEATS","Bloom filters have false positives but no false negatives. A rejected password might not actually be in the dictionary, but an accepted one definitely isn't. Filter size affects accuracy."],["HISTORY","**apgbfm** was created as a companion tool to apg, using bloom filters to efficiently check generated passwords against large dictionaries."],["INSTALL","```apt: sudo apt install apg```\n\n```dnf: sudo dnf install apg```\n\n```aur: yay -S apg```\n\n```apk: sudo apk add apg```\n\n```zypper: sudo zypper install apg```\n\n```nix: nix profile install nixpkgs#apg```"],["SEE ALSO","[apg](/man/apg)(1)"]],"e":["apg"],"x":"apgbfm -f [filter.bf] -d [dictionary.txt]"},"apk":{"n":"apk","t":"Alpine Linux package manager","s":[["TAGLINE","Alpine Linux package manager"],["TLDR","**Update** repository indexes and upgrade all packages\n\n```apk upgrade -U```\n\nOnly update **repository indexes**\n\n```apk update```\n\n**Install** a new package\n\n```apk add [package]```\n\n**Install** without caching the index (common in Dockerfiles)\n\n```apk add --no-cache [package]```\n\n**Remove** a package\n\n```apk del [package]```\n\n**Repair/Reinstall** a package without modifying main dependencies\n\n```apk fix [package]```\n\n**Search** for packages by name with descriptions\n\n```apk search -v [keyword]```\n\nSearch for packages by **description**\n\n```apk search -d [keyword]```\n\nDisplay **information** about a specific package\n\n```apk info [package]```"],["SYNOPSIS","**apk** [_options_] _command_ [_arguments_]"],["DESCRIPTION","**apk** (Alpine Package Keeper) is the package management tool for Alpine Linux. It handles installation, upgrade, and removal of packages, as well as repository management and system maintenance."],["PARAMETERS","**add**\n> Add or modify constraints and commit changes\n\n**del**\n> Remove constraints and commit changes\n\n**fix**\n> Fix, reinstall, or upgrade packages without modifying world\n\n**update**\n> Update repository indexes\n\n**upgrade**\n> Install upgrades available from repositories\n\n**cache**\n> Manage the local package cache\n\n**search**\n> Search for packages by name or description\n\n**info**\n> Display detailed information about packages\n\n**list**\n> List packages matching a pattern or criteria\n\n**query**\n> Interrogate installed database and indexes\n\n**policy**\n> Show repository policy for packages\n\n**version**\n> Compare package versions or check for available upgrades\n\n**fetch**\n> Download packages from repositories to the current directory\n\n**stats**\n> Show statistics about the package database\n\n**-U, --update-cache**\n> Update package lists before the operation (alias for --cache-max-age 0)\n\n**-v, --verbose**\n> Print more verbose information (repeat for more detail)\n\n**-q, --quiet**\n> Print less information\n\n**-d, --description**\n> Search in package descriptions\n\n**--no-cache**\n> Do not use or update any local cache; fetch the index directly\n\n**--allow-untrusted**\n> Install packages with untrusted or missing signatures\n\n**-X, --repository** _url_\n> Specify a supplemental repository\n\n**-p, --root** _dir_\n> Manage a filesystem rooted at the given directory"],["CONFIGURATION","**/etc/apk/repositories**\n> List of package repository URLs, one per line.\n\n**/etc/apk/world**\n> List of explicitly installed packages and version constraints.\n\n**/etc/apk/keys/**\n> Directory containing trusted repository signing keys."],["CAVEATS","Alpine Linux specific; not available on other distributions. Uses a minimal package format designed for size and simplicity."],["HISTORY","Developed for **Alpine Linux**, a security-oriented, lightweight Linux distribution. Alpine is widely used in container environments due to its small footprint."],["INSTALL","```dnf: sudo dnf install apk-tools```\n\n```pacman: sudo pacman -S apk-tools```\n\n```apk: sudo apk add apk-tools```\n\n```zypper: sudo zypper install apk-tools```\n\n```nix: nix profile install nixpkgs#apk-tools```"],["SEE ALSO","[apt](/man/apt)(8), [dnf](/man/dnf)(8), [pacman](/man/pacman)(8)"],["RESOURCES","```[Source code](https://github.com/alpinelinux/apk-tools)```\n\n```[Documentation](https://wiki.alpinelinux.org/wiki/Alpine_Package_Keeper)```"]],"e":["apt","dnf","pacman"],"x":"apk upgrade -U"},"arthas-trace":{"n":"arthas-trace","t":"Trace Java method execution paths and timing.","s":[["TAGLINE","Trace Java method execution paths and timing."],["TLDR","**Trace** method execution\n\n```trace [com.example.MyClass] [myMethod]```\n\nTrace with **condition**\n\n```trace [com.example.MyClass] [myMethod] '#cost > 100'```\n\nTrace **all methods** in class\n\n```trace [com.example.MyClass] *```\n\nTrace with **invocation count**\n\n```trace -n [5] [com.example.MyClass] [myMethod]```\n\nTrace using **regex** class/method matching\n\n```trace -E [com\\.example\\.(Foo|Bar)] [myMethod]```"],["SYNOPSIS","**trace** [_-n count_] [_-E_] [_--skipJDKMethod value_] _class-pattern_ _method-pattern_ [_condition-express_]"],["DESCRIPTION","**trace** is an Arthas command that traces method execution paths and timing. It shows the call hierarchy within a method invocation, highlighting which sub-methods consume the most time.\n\nThis is invaluable for identifying performance bottlenecks without adding logging or profiling instrumentation."],["PARAMETERS","**class-pattern**\n> Class name (supports wildcards)\n\n**method-pattern**\n> Method name (supports wildcards)\n\n**-n** _count_\n> Stop after count invocations (default: 100)\n\n**-E**\n> Enable regular-expression matching for the class and method patterns (default is wildcard matching)\n\n**--skipJDKMethod** _value_\n> Skip tracing JDK methods (default: true)\n\n**--exclude-class-pattern** _pattern_\n> Exclude classes matching this pattern from the trace\n\n**-m** _count_, **--maxMatch** _count_\n> Maximum number of matched classes to instrument (default: 50)\n\n**-v**\n> Print verbose detail of the condition-express evaluation\n\n**condition-express**\n> OGNL condition (e.g., '#cost > 100' traces only invocations slower than 100ms)"],["OUTPUT","Shows call tree with:\n- Method names\n- Execution time\n- Nested call structure"],["CAVEATS","Deep traces may produce verbose output. Performance overhead increases with trace depth. Use conditions to limit output."],["HISTORY","**trace** is a core diagnostic command in Arthas, essential for performance troubleshooting in production Java applications."],["INSTALL","```aur: yay -S arthas```"],["SEE ALSO","[arthas](/man/arthas)(1), [arthas-watch](/man/arthas-watch)(1)"],["RESOURCES","```[Source code](https://github.com/alibaba/arthas)```\n\n```[Homepage](https://arthas.aliyun.com)```\n\n```[Documentation](https://arthas.aliyun.com/en/doc/trace.html)```"]],"e":["arthas","arthas-watch"],"x":"trace [com.example.MyClass] [myMethod]"},"az-disk":{"n":"az-disk","t":"Manage Azure managed disks","s":[["TAGLINE","Manage Azure managed disks"],["TLDR","**Create a managed disk with a specific size**\n\n```az disk create --name [MyDisk] --resource-group [MyResourceGroup] --size-gb [128]```\n\n**Create a disk from a snapshot**\n\n```az disk create --name [MyDisk] --resource-group [MyResourceGroup] --source [snapshot_id]```\n\n**Create a Premium SSD disk**\n\n```az disk create --name [MyDisk] --resource-group [MyResourceGroup] --size-gb [128] --sku Premium_LRS```\n\n**List all managed disks in a resource group**\n\n```az disk list --resource-group [MyResourceGroup]```\n\n**Show details of a managed disk**\n\n```az disk show --name [MyDisk] --resource-group [MyResourceGroup]```\n\n**Update disk size**\n\n```az disk update --name [MyDisk] --resource-group [MyResourceGroup] --size-gb [256]```\n\n**Grant read access to a managed disk for export**\n\n```az disk grant-access --name [MyDisk] --resource-group [MyResourceGroup] --duration-in-seconds [3600] --access-level Read```\n\n**Revoke access to a managed disk**\n\n```az disk revoke-access --name [MyDisk] --resource-group [MyResourceGroup]```\n\n**Delete a managed disk**\n\n```az disk delete --name [MyDisk] --resource-group [MyResourceGroup] --yes```"],["SYNOPSIS","**az** **disk** _subcommand_ [_options_]"],["SUBCOMMANDS","**create**\n> Create a managed disk.\n\n**delete**\n> Delete a managed disk.\n\n**list**\n> List managed disks.\n\n**show**\n> Get information about a managed disk.\n\n**update**\n> Update a managed disk.\n\n**grant-access**\n> Grant read or read-write access to a disk.\n\n**revoke-access**\n> Revoke access to a disk.\n\n**wait**\n> Wait for a disk to reach a specific state."],["PARAMETERS","**--name** **-n**\n> The name of the managed disk.\n\n**--resource-group** **-g**\n> Name of resource group.\n\n**--size-gb** **-z**\n> Size in GB of the managed disk.\n\n**--source**\n> Source disk, snapshot, or URI to create the disk from.\n\n**--sku**\n> Storage type: Standard_LRS, Premium_LRS, StandardSSD_LRS, UltraSSD_LRS, PremiumV2_LRS, Premium_ZRS, StandardSSD_ZRS.\n\n**--location** **-l**\n> Location of the disk.\n\n**--zone**\n> Availability zone to create the disk in.\n\n**--encryption-type**\n> Encryption type: EncryptionAtRestWithPlatformKey (default), EncryptionAtRestWithCustomerKey.\n\n**--access-level**\n> Access level for grant-access: Read or Write.\n\n**--duration-in-seconds**\n> Time in seconds for which the SAS access URI is valid."],["DESCRIPTION","**az disk** manages Azure managed disks. Managed disks are block-level storage volumes managed by Azure and used with Azure Virtual Machines. They simplify disk management by handling storage account management automatically.\n\nSupported storage types include Standard HDD (Standard_LRS), Standard SSD (StandardSSD_LRS), Premium SSD (Premium_LRS), Premium SSD v2 (PremiumV2_LRS), and Ultra Disk (UltraSSD_LRS). Zone-redundant options (Premium_ZRS, StandardSSD_ZRS) provide cross-zone replication."],["CAVEATS","Requires Azure CLI to be installed and authenticated with **az login**. Disks can only be attached to VMs in the same region. Resizing a disk up requires the VM to be deallocated or the disk to be unattached; disks cannot be shrunk. Disk encryption defaults to platform-managed keys unless customer-managed keys are configured."],["HISTORY","**az disk** is part of the **Azure CLI** (`az`), developed by **Microsoft** for managing Azure resources from the command line. Managed disks were introduced in Azure to simplify disk management by eliminating the need to manage storage accounts separately."],["INSTALL","```dnf: sudo dnf install azure-cli```\n\n```pacman: sudo pacman -S azure-cli```\n\n```zypper: sudo zypper install azure-cli```\n\n```brew: brew install azure-cli```\n\n```nix: nix profile install nixpkgs#azure-cli```"],["SEE ALSO","[az](/man/az)(1), [az-vm](/man/az-vm)(1), [az-image](/man/az-image)(1)"]],"e":["az","az-vm","az-image"],"x":"az disk create --name [MyDisk] --resource-group [MyResourceGroup] --size-gb [128]"},"bwa":{"n":"bwa","t":"Map DNA sequences against a reference genome","s":[["TAGLINE","Map DNA sequences against a reference genome"],["TLDR","**Index** the reference genome\n\n```bwa index [path/to/reference.fa]```\n\nMap **single-end reads** to indexed genome\n\n```bwa mem -t 32 [path/to/reference.fa] [path/to/read.fq.gz] | gzip > [path/to/alignment.sam.gz]```\n\nMap **pair-end reads** to indexed genome\n\n```bwa mem -t 32 [path/to/reference.fa] [path/to/read_1.fq.gz] [path/to/read_2.fq.gz] | gzip > [path/to/alignment.sam.gz]```\n\nMap with **Picard compatibility** (mark shorter splits as secondary)\n\n```bwa mem -M -t 32 [path/to/reference.fa] [path/to/read_1.fq.gz] [path/to/read_2.fq.gz] | gzip > [path/to/alignment.sam.gz]```\n\nMap with **FASTA/Q comments** appended\n\n```bwa mem -C -t 32 [path/to/reference.fa] [path/to/read_1.fq.gz] [path/to/read_2.fq.gz] | gzip > [path/to/alignment.sam.gz]```"],["SYNOPSIS","**bwa** _command_ [_options_] [_arguments_]"],["DESCRIPTION","**bwa** (Burrows-Wheeler Aligner) is a software package for mapping low-divergent DNA sequences against a large reference genome, such as the human genome. It uses the Burrows-Wheeler Transform to build an index of the reference.\n\nThe **mem** algorithm is recommended for most applications, supporting reads from 70bp to a few megabases and providing accurate mapping."],["SUBCOMMANDS","**index**\n> Build index from reference genome\n\n**mem**\n> Map reads using BWA-MEM algorithm\n\n**aln**\n> Align reads (older algorithm)\n\n**samse/sampe**\n> Generate SAM from aln output"],["PARAMETERS","**-t** _threads_\n> Number of CPU threads\n\n**-M**\n> Mark shorter splits as secondary (Picard compatible)\n\n**-C**\n> Append FASTA/Q comment to output\n\n**-R** _string_\n> Read group header line\n\n**-o** _file_\n> Output file name"],["CAVEATS","Indexing large genomes requires significant memory and time. Output is uncompressed SAM by default; pipe through gzip for storage. Quality of results depends on read quality and reference completeness."],["HISTORY","**BWA** was developed by Heng Li and first published in **2009**. The MEM algorithm was introduced in **2013** and has become the preferred method for most mapping tasks."],["INSTALL","```apt: sudo apt install bwa```\n\n```dnf: sudo dnf install bwa```\n\n```brew: brew install bwa```\n\n```nix: nix profile install nixpkgs#bwa```"],["SEE ALSO","[samtools](/man/samtools)(1), [bowtie2](/man/bowtie2)(1), [minimap2](/man/minimap2)(1)"],["RESOURCES","```[Source code](https://github.com/lh3/bwa)```\n\n```[Homepage](https://bio-bwa.sourceforge.net/)```"]],"e":["samtools","bowtie2","minimap2"],"x":"bwa index [path/to/reference.fa]"},"caffeine-indicator":{"n":"caffeine-indicator","t":"System tray toggle to prevent idle sleep","s":[["TAGLINE","System tray toggle to prevent idle sleep"],["TLDR","Start the **caffeine indicator** applet\n\n```caffeine-indicator```"],["SYNOPSIS","**caffeine-indicator**"],["DESCRIPTION","**caffeine-indicator** provides a system tray indicator that allows manual control over desktop idleness inhibition. Clicking the indicator toggles prevention of screen saver, suspend, and other idle actions.\n\nUnlike the automatic caffeine daemon, this provides manual toggle control."],["INSTALL","```zypper: sudo zypper install caffeine```\n\n```nix: nix profile install nixpkgs#caffeine```"],["SEE ALSO","[caffeine](/man/caffeine)(1), [caffeinate](/man/caffeinate)(1)"],["RESOURCES","```[Homepage](https://launchpad.net/caffeine)```"]],"e":["caffeine","caffeinate"],"x":"caffeine-indicator"},"cardano-cli":{"n":"cardano-cli","t":"Command-line interface for the Cardano blockchain","s":[["TAGLINE","Command-line interface for the Cardano blockchain"],["TLDR","**Query tip** (latest block info)\n\n```cardano-cli query tip --mainnet```\n\n**Generate payment keys**\n\n```cardano-cli address key-gen --verification-key-file [payment.vkey] --signing-key-file [payment.skey]```\n\n**Build a payment address**\n\n```cardano-cli address build --payment-verification-key-file [payment.vkey] --out-file [payment.addr] --mainnet```\n\n**Query UTxOs** for an address\n\n```cardano-cli query utxo --address [addr1...] --mainnet```\n\n**Build a transaction**\n\n```cardano-cli transaction build --tx-in [txhash#index] --tx-out [addr]+[lovelace] --change-address [addr] --mainnet --out-file [tx.raw]```\n\n**Sign a transaction**\n\n```cardano-cli transaction sign --tx-body-file [tx.raw] --signing-key-file [payment.skey] --mainnet --out-file [tx.signed]```\n\n**Submit a transaction**\n\n```cardano-cli transaction submit --tx-file [tx.signed] --mainnet```"],["SYNOPSIS","**cardano-cli** _command_ [_subcommand_] [_options_]"],["DESCRIPTION","**cardano-cli** is the command-line interface for interacting with the Cardano blockchain. It provides functionality for key generation, address creation, transaction building, and querying the blockchain state.\n\nThe tool interfaces with a running cardano-node to query blockchain data and submit transactions. It supports both mainnet and various test networks (preprod, preview). Operations include managing stake keys, delegating to stake pools, minting native tokens, and participating in on-chain governance (Conway era).\n\nCardano uses an extended UTxO (EUTxO) model where transactions consume and produce unspent transaction outputs. The CLI handles the complexity of building valid transactions with proper fees and balancing. As of version 10.11, the default era is Conway."],["PARAMETERS","**query**\n> Query blockchain state (tip, utxo, protocol-parameters).\n\n**address**\n> Address commands (key-gen, build).\n\n**transaction**\n> Transaction commands (build, sign, submit).\n\n**stake-address**\n> Stake address and delegation commands.\n\n**stake-pool**\n> Stake pool operations.\n\n**governance**\n> Governance commands: actions, DRep registration, committee, and voting (Conway era).\n\n**key**\n> Key utility commands.\n\n**node**\n> Node operational certificate and key-evolving signature commands.\n\n**hash**\n> Hashing utilities for files and scripts.\n\n**genesis**\n> Genesis block commands.\n\n**--mainnet**\n> Use mainnet network magic.\n\n**--testnet-magic** _num_\n> Use testnet with specific magic number.\n\n**--socket-path** _path_\n> Path to node socket (or CARDANO_NODE_SOCKET_PATH env).\n\n**--out-file** _file_\n> Output file for results.\n\n**--signing-key-file** _file_\n> Private key for signing.\n\n**--verification-key-file** _file_\n> Public key file."],["CAVEATS","Requires a running and synced cardano-node. Socket path must be accessible. Transactions require correct fee calculation and change output. Keys should be stored securely; loss means loss of funds. Testnet tokens have no real value."],["HISTORY","**cardano-cli** is developed by **Input Output Global (IOG)** as part of the Cardano blockchain project founded by **Charles Hoskinson**. Cardano mainnet launched in **September 2017** (Byron era). The Shelley era in **2020** introduced staking. The Alonzo upgrade in **September 2021** added smart contract support. The tool continues to evolve with each protocol upgrade."],["SEE ALSO","[cardano-node](/man/cardano-node)(1), [cardano-wallet](/man/cardano-wallet)(1)"]],"e":["cardano-node","cardano-wallet"],"x":"cardano-cli query tip --mainnet"},"ccze":{"n":"ccze","t":"A robust log colorizer","s":[["TAGLINE","A robust log colorizer"],["TLDR","**Colorize log file**\n\n```cat [/var/log/syslog] | ccze```\n\n**Follow log with colors**\n\n```tail -f [/var/log/syslog] | ccze```\n\n**Raw ANSI output for less**\n\n```cat [logfile] | ccze -A | less -R```\n\n**HTML output**\n\n```cat [logfile] | ccze -h > [output.html]```\n\n**Disable lookups for speed**\n\n```cat [logfile] | ccze -A -o nolookups | less -R```\n\n**List available plugins**\n\n```ccze -l```\n\n**Colorize using only a specific plugin**\n\n```cat [logfile] | ccze -p [httpd]```\n\n**Use an alternate configuration file**\n\n```cat [logfile] | ccze -F [~/.my_cczerc] -A```"],["SYNOPSIS","**ccze** [_options_]"],["DESCRIPTION","**ccze** is a robust log colorizer written in C, designed as a drop-in replacement for **colorize** with better performance and lower resource usage. It reads log data from standard input and outputs colorized text using ncurses, raw ANSI escape codes, or HTML.\n\nThe tool uses a plugin architecture to recognize and colorize different log formats including syslog, Apache httpd, Postfix, Squid, Exim, fetchmail, procmail, and vsftpd. Each plugin understands the structure of its target log format and applies appropriate colors to timestamps, hostnames, severity levels, and other fields.\n\nOutput defaults to ncurses mode but the `-A` flag produces raw ANSI output suitable for piping to `less -R` or other tools."],["PARAMETERS","**-a** _PLUGIN=ARGUMENTS_, **--argument** _PLUGIN=ARGUMENTS_\n> Pass whitespace-separated arguments to the specified plugin.\n\n**-A**, **--raw-ansi**\n> Generate raw ANSI color sequences instead of using ncurses.\n\n**-c** _KEY=COLOR_, **--color** _KEY=COLOR_\n> Set the color of a keyword, as in configuration files.\n\n**-C**, **--convert-date**\n> Convert UNIX timestamps to human-readable date format (e.g. in squid or oops logs).\n\n**-F** _rcfile_, **--rcfile** _rcfile_\n> Read the specified file as configuration instead of the defaults.\n\n**-h**, **--html**\n> Generate HTML output instead of console colors.\n\n**-l**, **--list-plugins**\n> List all available (loaded) plugins with their type and description.\n\n**-m** _mode_, **--mode** _mode_\n> Set output mode: _curses_, _ansi_, or _html_.\n\n**-o** _option_, **--options** _option_\n> Toggle a feature. Available options: **scroll**, **wordcolor**, **lookups**, **transparent**, **cssfile**. Prefix with \"no\" to disable (e.g., **nolookups**). All are enabled by default except cssfile.\n\n**-p** _plugin_, **--plugin** _plugin_\n> Load only the specified plugin(s) for a slight speedup.\n\n**-r**, **--remove-facility**\n> Remove syslog-ng facility level information from log lines.\n\n**-V**, **--version**\n> Display version information.\n\n**--help**\n> Display usage summary and exit."],["CONFIGURATION","**/etc/cczerc**\n> System-wide configuration.\n\n**~/.cczerc**\n> Per-user configuration."],["PLUGINS","Located in /usr/lib/ccze and ~/.ccze. Support for:\n- syslog, httpd, postfix\n- squid, exim, fetchmail\n- procmail, vsftpd, xferlog\n- ulogd, apm"],["CAVEATS","Use **-o nolookups** for faster processing of long logs. When enabled (the default), lookups resolve usernames and service names but slow down colorization significantly."],["INSTALL","```apt: sudo apt install ccze```\n\n```dnf: sudo dnf install ccze```\n\n```zypper: sudo zypper install ccze```\n\n```nix: nix profile install nixpkgs#ccze```"],["SEE ALSO","[tail](/man/tail)(1), [less](/man/less)(1), [multitail](/man/multitail)(1), [journalctl](/man/journalctl)(1)"]],"e":["tail","less","multitail","journalctl"],"x":"cat [/var/log/syslog] | ccze"},"chkfont":{"n":"chkfont","t":"FIGlet font file validator","s":[["TAGLINE","FIGlet font file validator"],["TLDR","**Check a FIGlet font file for errors**\n\n```chkfont [path/to/font.flf]```\n\n**Check multiple font files**\n\n```chkfont [font1.flf] [font2.flf]```\n\n**Check a font from the standard FIGlet directory**\n\n```chkfont /usr/share/figlet/[big.flf]```"],["SYNOPSIS","**chkfont** _fontfile..._"],["DESCRIPTION","**chkfont** validates FIGlet 2.0 and later font files (**.flf**) for format errors. It examines font file structure and reports common problems without modifying the files.\n\nThe utility is primarily used by font designers and editors to verify font files before distribution or to diagnose issues with custom FIGlet fonts."],["PARAMETERS","**fontfile**\n> One or more FIGlet font files to check. Files should have the **.flf** extension"],["CAVEATS","Does not work on compressed font files (**.flf.gz**). Decompress fonts before checking with **gunzip**.\n\nOnly validates FIGlet font format, not FIGlet control files or other font formats."],["HISTORY","**chkfont** is part of the FIGlet project, which was originally written by Glenn Chappell and Ian Chai in 1991. FIGlet creates large text banners from ordinary text using special fonts. The current version is maintained by Claudio Matsuoka."],["INSTALL","```apt: sudo apt install figlet```\n\n```dnf: sudo dnf install figlet```\n\n```pacman: sudo pacman -S figlet```\n\n```apk: sudo apk add figlet```\n\n```zypper: sudo zypper install figlet```\n\n```brew: brew install figlet```\n\n```nix: nix profile install nixpkgs#figlet```"],["SEE ALSO","[figlet](/man/figlet)(6), [figlist](/man/figlist)(6), [showfigfonts](/man/showfigfonts)(6), [toilet](/man/toilet)(1)"],["RESOURCES","```[Source code](https://github.com/cmatsuoka/figlet)```\n\n```[Homepage](http://www.figlet.org)```"]],"e":["figlet","figlist","showfigfonts","toilet"],"x":"chkfont [path/to/font.flf]"},"cinnamon":{"n":"cinnamon","t":"Traditional Linux desktop environment","s":[["TAGLINE","Traditional Linux desktop environment"],["TLDR","**Start Cinnamon** desktop (typically via display manager)\n\n```cinnamon```\n\n**Replace** the current window manager with Cinnamon\n\n```cinnamon --replace```\n\n**Replace** the window manager without session manager connection\n\n```cinnamon --replace --sm-disable```\n\n**Display version** information\n\n```cinnamon --version```"],["SYNOPSIS","**cinnamon** [**--replace**] [**--sm-disable**] [**-d** _DISPLAY_] [**--screen** _SCREEN_]"],["DESCRIPTION","**Cinnamon** is a desktop environment for Linux that provides a traditional user experience with modern features. It includes the Muffin window manager, panel, application menu, desktop icons, and a comprehensive settings framework.\n\nThe cinnamon command is typically invoked by **cinnamon-session** during login via a display manager. Running it directly in an existing session can cause conflicts.\n\nCinnamon supports extensive customization through applets (panel widgets), desklets (desktop widgets), extensions, and themes."],["PARAMETERS","**--replace**\n> Replace the currently running window manager\n\n**-d**, **--display** _DISPLAY_\n> X display to use\n\n**--screen** _SCREEN_\n> X screen to use\n\n**--sm-disable**\n> Disable connection to the session manager\n\n**--sm-client-id** _ID_\n> Specify session management ID\n\n**--sm-save-file** _FILE_\n> Initialize session from a saved session file\n\n**--sync**\n> Make X calls synchronous\n\n**--version**\n> Print version and exit"],["CAVEATS","Do not run cinnamon directly within an existing Cinnamon session, as this creates conflicts with the running desktop. The desktop is normally started by **cinnamon-session** through a display manager.\n\nCinnamon does not support using a different window manager; Muffin is integral to its operation."],["HISTORY","**Cinnamon** was developed by the **Linux Mint** team starting in **2011** as a fork of GNOME 3's GNOME Shell. It was created to provide a more traditional desktop experience after GNOME 3 moved to a significantly different interface paradigm. The window manager Muffin was forked from GNOME's Mutter in version 1.2 (January 2012)."],["INSTALL","```apt: sudo apt install cinnamon```\n\n```dnf: sudo dnf install cinnamon```\n\n```pacman: sudo pacman -S cinnamon```\n\n```zypper: sudo zypper install cinnamon```\n\n```nix: nix profile install nixpkgs#cinnamon```"],["SEE ALSO","[muffin](/man/muffin)(1), [nemo](/man/nemo)(1), [cinnamon-launcher](/man/cinnamon-launcher)(1)"]],"e":["muffin","nemo","cinnamon-launcher"],"x":"cinnamon"},"complete":{"n":"complete","t":"specify bash command argument completion","s":[["TAGLINE","specify bash command argument completion"],["TLDR","**Complete with files**\n\n```complete -f [command]```\n\n**Complete with directories**\n\n```complete -d [command]```\n\n**Complete with word list**\n\n```complete -W '[start stop restart]' [myservice]```\n\n**Complete using function**\n\n```complete -F [_my_completion] [mycommand]```\n\n**Remove completion**\n\n```complete -r [command]```\n\n**List all completions**\n\n```complete -p```\n\n**Complete with no space after**\n\n```complete -o nospace -W '[opt1 opt2]' [command]```"],["SYNOPSIS","**complete** [_options_] [_name_...]"],["DESCRIPTION","**complete** specifies how arguments for commands should be completed. Bash builtin for programmable completion. Defines completion specifications (compspecs) for commands."],["PARAMETERS","**-a**\n> Complete with aliases\n\n**-b**\n> Complete with builtins\n\n**-c**\n> Complete with commands\n\n**-d**\n> Complete with directories\n\n**-f**\n> Complete with files\n\n**-g**\n> Complete with groups\n\n**-j**\n> Complete with jobs\n\n**-k**\n> Complete with keywords\n\n**-s**\n> Complete with services\n\n**-u**\n> Complete with users\n\n**-v**\n> Complete with variables\n\n**-A** _action_\n> Use action for completion\n\n**-W** _wordlist_\n> Complete from wordlist\n\n**-F** _function_\n> Call shell function\n\n**-C** _command_\n> Run command for completions\n\n**-G** _pattern_\n> Glob pattern for files\n\n**-X** _pattern_\n> Filter out matches\n\n**-P** _prefix_\n> Add prefix to results\n\n**-S** _suffix_\n> Add suffix to results\n\n**-p**\n> Print current completions\n\n**-r**\n> Remove completion spec"],["COMP-OPTIONS (-o)","**filenames**: Treat as filenames (add slashes, quote)\n\n**nospace**: Don't append space after completion\n\n**nosort**: Don't sort alphabetically\n\n**bashdefault**: Fall back to bash defaults\n\n**default**: Fall back to readline defaults"],["CAVEATS","Bash builtin only. Completions stored per-session. Use /etc/bash_completion.d/ for persistent completions."],["SEE ALSO","[compgen](/man/compgen)(1), [bash](/man/bash)(1)"],["RESOURCES","```[Source code](https://git.savannah.gnu.org/cgit/bash.git)```\n\n```[Documentation](https://www.gnu.org/software/bash/manual/)```"]],"e":["compgen","bash"],"x":"complete -f [command]"},"conda-create":{"n":"conda-create","t":"create isolated conda environments","s":[["TAGLINE","create isolated conda environments"],["TLDR","**Create a new environment** with Python\n\n```conda create --name [env_name] python=[3.11]```\n\n**Create with specific packages**\n\n```conda create --name [env_name] [numpy] [pandas]```\n\n**Create from environment file**\n\n```conda create --name [env_name] --file [requirements.txt]```\n\n**Create environment at specific path**\n\n```conda create --prefix [/path/to/env] python=[3.11]```\n\n**Clone an existing environment**\n\n```conda create --name [new_env] --clone [existing_env]```"],["SYNOPSIS","**conda** **create** [_options_] [_packages..._]"],["PARAMETERS","**-n**, **--name** _name_\n> Name of the new environment.\n\n**-p**, **--prefix** _path_\n> Full path to environment location.\n\n**--file** _file_\n> Read package versions from file.\n\n**--clone** _env_\n> Clone an existing environment.\n\n**-c**, **--channel** _channel_\n> Additional channel to search for packages.\n\n**-y**, **--yes**\n> Don't ask for confirmation."],["DESCRIPTION","**conda create** initializes a new isolated conda environment, which is a self-contained directory structure containing a specific collection of packages and their dependencies. Environments are conda's fundamental mechanism for managing multiple projects with potentially conflicting package requirements on the same system.\n\nThe creation process involves specifying the environment name (or prefix path), the desired Python version, and any initial packages to install. Conda performs dependency resolution to ensure all specified packages and their dependencies are compatible, then downloads and installs the required packages into the new environment directory. Environments can be created with minimal packages and later extended, or fully specified upfront with a complete package set.\n\nThe --clone option provides a way to duplicate existing environments, which is useful for experimentation or creating backup environments. The --file option allows reading package specifications from a file, enabling reproducible environment creation. Environments created with --prefix can exist anywhere on the filesystem, while named environments (--name) are stored in conda's default envs directory. Once created, environments must be activated before their packages become available to the shell."],["CAVEATS","Environment names cannot contain spaces. Use **--prefix** for environments outside the default envs directory."],["INSTALL","```dnf: sudo dnf install conda```\n\n```nix: nix profile install nixpkgs#conda```"],["SEE ALSO","[conda](/man/conda)(1), [conda-activate](/man/conda-activate)(1), [conda-remove](/man/conda-remove)(1)"],["RESOURCES","```[Source code](https://github.com/conda/conda)```\n\n```[Documentation](https://docs.conda.io/projects/conda/en/stable/commands/create.html)```"]],"e":["conda","conda-activate","conda-remove"],"x":"conda create --name [env_name] python=[3.11]"},"counter-strike-2":{"n":"counter-strike-2","t":"Multiplayer FPS dedicated server","s":[["TAGLINE","Multiplayer FPS dedicated server"],["TLDR","Run a **casual game server**\n\n```[path/to]/cs2 -dedicated +game_type 0 +game_mode 0 +map [de_dust2]```\n\nRun a **competitive game server**\n\n```[path/to]/cs2 -dedicated +game_type 0 +game_mode 1 +map [de_dust2]```\n\nRun with specific **IP, port, and max players**\n\n```[path/to]/cs2 -dedicated +map [de_dust2] -ip [1.2.3.4] -port [27015] -maxplayers [10]```\n\nRun with **Steam authentication token**\n\n```[path/to]/cs2 -dedicated +map [de_dust2] +sv_setsteamaccount [token]```"],["SYNOPSIS","**cs2** [_options_] [+_commands_]"],["DESCRIPTION","**Counter-Strike 2** dedicated server hosts headless game servers for multiplayer matches. It runs without a graphical interface and can be managed via console commands or RCON (Remote Console) for remote administration.\n\nThe server can be configured through command-line parameters, console commands, and configuration files. It supports custom game modes, maps, plugins, and administrative controls. Dedicated servers are essential for hosting competitive matches, community servers, and private games with specific rule sets.\n\nServer performance depends on player count, tick rate, and available system resources. The dedicated server is distributed via SteamCMD and requires Steam authentication to run."],["PARAMETERS","**-dedicated**\n> Run as dedicated server (headless, no GUI)\n\n**+map** _mapname_\n> Specify the map to load (e.g., de_dust2, de_mirage)\n\n**+game_type** _type_\n> Game type (0 = Classic, 1 = Arms Race/Demolition/Deathmatch)\n\n**+game_mode** _mode_\n> Game mode (varies by game_type, e.g., 0 = Casual, 1 = Competitive)\n\n**+mapgroup** _group_\n> Map group to use (e.g., mg_active, mg_allclassic)\n\n**-maxplayers** _count_\n> Maximum number of players\n\n**-ip** _address_\n> Server IP address to bind\n\n**-port** _port_\n> Server port (default: 27015)\n\n**-insecure**\n> Disable VAC anti-cheat\n\n**-usercon**\n> Enable RCON remote console administration\n\n**+hostname** _name_\n> Set server display name\n\n**+sv_lan** _0|1_\n> Toggle LAN-only mode (1 = LAN, 0 = internet)\n\n**+sv_password** _password_\n> Require a password to join the server\n\n**+rcon_password** _password_\n> Set RCON administrative password\n\n**+sv_setsteamaccount** _token_\n> Game Server Login Token for Steam authentication"],["CONSOLE COMMANDS","**quit**\n> Shut down the server\n\n**changelevel** _map_\n> Change to a different map\n\n**status**\n> Show server status"],["CONFIGURATION","**game/csgo/cfg/server.cfg**\n> Server configuration including hostname, passwords, game rules, and administrative settings."],["CAVEATS","Requires Steam and valid game files. Server hardware requirements depend on player count. Network configuration (port forwarding, firewall) needed for public servers."],["SEE ALSO","[steamcmd](/man/steamcmd)(1)"]],"e":["steamcmd"],"x":"[path/to]/cs2 -dedicated +game_type 0 +game_mode 0 +map [de_dust2]"},"cpufreq-info":{"n":"cpufreq-info","t":"display CPU frequency scaling information","s":[["TAGLINE","display CPU frequency scaling information"],["TLDR","Show CPU frequency **info for all CPUs**\n\n```cpufreq-info```\n\nShow info for **specific CPU**\n\n```cpufreq-info -c 0```\n\nShow **hardware limits** (min/max frequency)\n\n```cpufreq-info -l```\n\nShow current **policy** in table format\n\n```cpufreq-info -o```\n\nShow available **governors**\n\n```cpufreq-info -g```\n\nShow **current frequency** (human-readable)\n\n```cpufreq-info -f -m```\n\nShow frequency from **hardware** (root only)\n\n```sudo cpufreq-info -w -m```\n\nShow **driver** in use\n\n```cpufreq-info -d```"],["SYNOPSIS","**cpufreq-info** [_options_]"],["DESCRIPTION","**cpufreq-info** is a utility that prints CPU frequency scaling information from the kernel. It shows current frequencies, available governors, hardware limits, and other cpufreq-related data."],["PARAMETERS","**-c, --cpu CPU**\n> Target a specific processor number\n\n**-f, --freq**\n> Display current CPU frequency as reported by kernel\n\n**-w, --hwfreq**\n> Read current frequency directly from hardware (root only)\n\n**-l, --hwlimits**\n> Show minimum and maximum hardware frequency limits\n\n**-d, --driver**\n> Show the active cpufreq kernel driver\n\n**-p, --policy**\n> Display the current frequency scaling policy\n\n**-g, --governors**\n> List available frequency governors\n\n**-a, --related-cpus**\n> Show CPUs sharing the same hardware frequency\n\n**-s, --stats**\n> Display frequency scaling statistics\n\n**-y, --latency**\n> Show maximum latency for frequency transitions\n\n**-o, --proc**\n> Display settings in /proc/cpufreq format\n\n**-m, --human**\n> Format output in human-readable form (for -f, -w, -s, -y)"],["CAVEATS","Multiple output-specific options cannot be combined in a single invocation. The -w option requires root privileges to read frequency directly from hardware. The -o option is incompatible with -c."],["HISTORY","**cpufreq-info** is part of the **cpufrequtils** package, providing user-space utilities for Linux CPU frequency scaling."],["INSTALL","```apk: sudo apk add cpufrequtils```\n\n```nix: nix profile install nixpkgs#cpufrequtils```"],["SEE ALSO","[cpufreq-set](/man/cpufreq-set)(1), [cpupower](/man/cpupower)(1), [auto-cpufreq](/man/auto-cpufreq)(1)"],["RESOURCES","```[Source code](https://git.kernel.org/pub/scm/utils/kernel/cpufreq/cpufrequtils.git/)```"]],"e":["cpufreq-set","cpupower","auto-cpufreq"],"x":"cpufreq-info"},"crun":{"n":"crun","t":"fast OCI container runtime","s":[["TAGLINE","fast OCI container runtime"],["TLDR","**Run a container** from OCI bundle\n\n```crun run [container_id]```\n\n**Create a container** without starting it\n\n```crun create [container_id]```\n\n**Start a created container**\n\n```crun start [container_id]```\n\n**List running containers**\n\n```crun list```\n\n**Execute a command** in a running container\n\n```crun exec [container_id] [command]```\n\n**Delete a container**\n\n```crun delete [container_id]```\n\n**Get container state**\n\n```crun state [container_id]```\n\n**Pause all processes** in a container\n\n```crun pause [container_id]```\n\n**Show processes** running in a container\n\n```crun ps [container_id]```"],["SYNOPSIS","**crun** [_global-options_] _command_ [_command-options_] [_arguments_]"],["PARAMETERS","**run** _ID_\n> Create and start a container from an OCI bundle.\n\n**create** _ID_\n> Create a container but don't start it.\n\n**start** _ID_\n> Start a previously created container.\n\n**exec** _ID_ _CMD_\n> Execute a command in a running container.\n\n**delete** _ID_\n> Delete a container.\n\n**kill** _ID_ [_SIGNAL_]\n> Send a signal to the container's init process.\n\n**list**\n> List containers.\n\n**state** _ID_\n> Output the state of a container.\n\n**pause** _ID_\n> Pause all processes in the container.\n\n**resume** _ID_\n> Resume the processes in the container.\n\n**ps** _ID_\n> Show the processes running in a container.\n\n**update** _ID_\n> Update container resource constraints.\n\n**spec**\n> Create a new OCI bundle specification.\n\n**checkpoint** _ID_\n> Checkpoint a running container using CRIU.\n\n**--root** _DIR_\n> Root directory for container state.\n\n**--debug**\n> Produce verbose output.\n\n**--log**=_DESTINATION_\n> Define destination for error and warning messages (file:PATH, journald:ID, or syslog:ID).\n\n**--systemd-cgroup**\n> Use systemd for configuring cgroups.\n\n**--cgroup-manager**=_MANAGER_\n> Specify cgroup manager (cgroupfs, systemd, or disabled)."],["DESCRIPTION","**crun** is a fast and low-memory footprint OCI container runtime written in C. It is fully compatible with the OCI (Open Container Initiative) runtime specification and can be used as a drop-in replacement for runc.\n\ncrun implements all required OCI runtime operations including container creation, execution, and lifecycle management. It supports Linux namespaces, cgroups v1 and v2, seccomp, and other container isolation features.\n\nThe runtime is designed to be lightweight and efficient, with significantly faster startup times and lower memory usage compared to Go-based runtimes. It's used by container engines like Podman and Buildah."],["CAVEATS","Requires root privileges or appropriate user namespace configuration. OCI bundle must be properly configured with config.json and rootfs. crun operates on OCI bundles, not container images directly."],["HISTORY","crun was developed by Giuseppe Scrivano at Red Hat as a faster alternative to runc. First released around **2019**, it gained adoption due to its performance benefits. It became the default runtime for Podman and is supported by Kubernetes through CRI-O."],["INSTALL","```apt: sudo apt install crun```\n\n```dnf: sudo dnf install crun```\n\n```pacman: sudo pacman -S crun```\n\n```apk: sudo apk add crun```\n\n```zypper: sudo zypper install crun```\n\n```brew: brew install crun```\n\n```nix: nix profile install nixpkgs#crun```"],["SEE ALSO","[runc](/man/runc)(1), [podman](/man/podman)(1), [buildah](/man/buildah)(1), [containerd](/man/containerd)(1), [docker](/man/docker)(1)"]],"e":["runc","podman","buildah","containerd","docker"],"x":"crun run [container_id]"},"cupsreject":{"n":"cupsreject","t":"prevent printer from accepting new jobs","s":[["TAGLINE","prevent printer from accepting new jobs"],["TLDR","**Reject new print jobs** for a printer\n\n```cupsreject [printer_name]```\n\n**Reject with reason message**\n\n```cupsreject -r \"[printer offline]\" [printer_name]```"],["SYNOPSIS","**cupsreject** [_options_] _destination(s)_"],["PARAMETERS","**-r** _reason_\n> Set reason message for rejecting.\n\n**-h** _server[:port]_\n> Connect to specific server."],["DESCRIPTION","**cupsreject** instructs the printing system to reject new print jobs for the specified destinations. Existing jobs in the queue are not affected.\n\nUse **cupsaccept** to accept jobs again. Requires administrator privileges."],["INSTALL","```apt: sudo apt install cups-client```\n\n```dnf: sudo dnf install cups-client```\n\n```pacman: sudo pacman -S cups```\n\n```apk: sudo apk add cups```\n\n```zypper: sudo zypper install cups-client```\n\n```brew: brew install cups```\n\n```nix: nix profile install nixpkgs#cups```"],["SEE ALSO","[cupsaccept](/man/cupsaccept)(1), [cupsdisable](/man/cupsdisable)(1)"],["RESOURCES","```[Source code](https://github.com/OpenPrinting/cups)```\n\n```[Homepage](https://www.cups.org)```\n\n```[Documentation](https://openprinting.github.io/cups/)```"]],"e":["cupsaccept","cupsdisable"],"x":"cupsreject [printer_name]"},"dirac":{"n":"dirac","t":"Efficient AI coding agent CLI","s":[["TAGLINE","Efficient AI coding agent CLI"],["TLDR","**Start an interactive coding task** with a prompt\n\n```dirac \"[describe the task]\"```\n\n**Authenticate** with your AI provider\n\n```dirac auth```\n\n**View and resume** previous tasks\n\n```dirac history```\n\n**Run in plan mode** to display the strategy before execution\n\n```dirac -p \"[refactor the authentication module]\"```\n\n**Auto-approve** all actions without confirmation (Yolo mode)\n\n```dirac -y \"[fix all lint errors]\"```\n\n**Pipe input** into Dirac for review\n\n```git diff | dirac \"Review these changes\"```"],["SYNOPSIS","**dirac** [_options_] [_command_] [_prompt_]"],["PARAMETERS","**-p**, **--plan**\n> Plan Mode: display the strategy before executing any actions.\n\n**-y**, **--yes**\n> Yolo Mode: auto-approve all actions without confirmation prompts.\n\n**auth**\n> Authenticate against the configured AI provider.\n\n**history**\n> List previous tasks and resume any of them."],["DESCRIPTION","**dirac** is a terminal-based AI coding agent designed for context efficiency and reduced API cost. It operates with an approval-based workflow, letting it read and write files, execute terminal commands, and drive a headless browser while keeping the user in control.\n\nThe agent emphasises hash-anchored edits, AST manipulation, and massively parallel operations to minimise tokens spent per change. According to the project, this lowers API spend by 50–80% versus comparable agents while maintaining or improving code quality.\n\nDirac supports multiple model providers, selectable via environment variables, so it can be paired with Anthropic, OpenAI, Google, xAI, Mistral, Groq, OpenRouter, or self-hosted backends."],["CONFIGURATION","API keys are read from the environment:\n\n```\nANTHROPIC_API_KEY\nOPENAI_API_KEY\nOPENROUTER_API_KEY\nGEMINI_API_KEY\nGROQ_API_KEY\nMISTRAL_API_KEY\nXAI_API_KEY\nHF_TOKEN\n```\n\nSet the variable for the provider you want to use before launching **dirac**. The active provider is chosen based on the keys present."],["CAVEATS","Approval prompts can be skipped with **-y**, but doing so allows file writes and shell execution without confirmation. Use only in disposable environments or trusted projects. AI agents may misinterpret intent on large refactors; always review diffs before committing."],["HISTORY","Dirac is distributed via npm as **dirac-cli**. It positions itself as a more cost-efficient alternative to other terminal coding agents by combining hash-anchored edits, AST-aware operations, and aggressive context curation."],["INSTALL","```zypper: sudo zypper install dirac```\n\n```brew: brew install dirac```"],["SEE ALSO","[claude](/man/claude)(1), [vtcode](/man/vtcode)(1), [aider](/man/aider)(1)"]],"e":["claude","vtcode","aider"],"x":"dirac \"[describe the task]\""},"display":{"n":"display","t":"Display images on an X Window System","s":[["TAGLINE","Display images on an X Window System"],["TLDR","**Display an image**\n\n```display [image.jpg]```\n\n**Display with specific** window title\n\n```display -title \"[My Image]\" [image.jpg]```\n\n**Display and resize**\n\n```display -resize [800x600] [image.jpg]```\n\n**Display multiple images** as slideshow\n\n```display [*.jpg]```\n\n**Display from URL**\n\n```display \"[https://example.com/image.jpg]\"```\n\n**Display with window geometry**\n\n```display -geometry [+100+100] [image.jpg]```"],["SYNOPSIS","**magick display** [_options_] _input-file_"],["PARAMETERS","_FILE_\n> Image file(s) to display.\n\n**-resize** _GEOMETRY_\n> Resize image to specified dimensions.\n\n**-title** _STRING_\n> Window title.\n\n**-geometry** _GEOMETRY_\n> Window position and size.\n\n**-delay** _centiseconds_\n> Delay between images in slideshow (in hundredths of a second).\n\n**-update** _seconds_\n> Detect when image file is modified and redisplay.\n\n**-loop** _iterations_\n> Loop images then exit.\n\n**-immutable**\n> Prevent image modifications.\n\n**-verbose**\n> Print detailed information about the image.\n\n**-help**\n> Display help information."],["DESCRIPTION","**display** is an ImageMagick program that displays images on an X Window System. It provides an interactive viewer with zoom, pan, and basic editing capabilities. In ImageMagick 7, it is invoked as **magick display**.\n\nThe viewer supports numerous image formats through ImageMagick's format handling. Interactive features include zooming, rotating, cropping, color adjustment, and annotation. Multiple images can be viewed as a slideshow.\n\ndisplay includes a command menu accessible by clicking on the image, providing access to transformations, effects, and file operations. Modified images can be saved in various formats."],["CAVEATS","Requires X11 display. Resource-intensive for large images. Feature-rich interface may be complex for simple viewing. Part of ImageMagick which has had security advisories."],["HISTORY","display is part of **ImageMagick**, created by **John Cristy** in **1987**. ImageMagick has been the premier open-source image manipulation suite for decades, with display providing interactive visualization capabilities."],["INSTALL","```pacman: sudo pacman -S imagemagick```\n\n```apk: sudo apk add imagemagick```\n\n```brew: brew install imagemagick```\n\n```nix: nix profile install nixpkgs#imagemagick```"],["SEE ALSO","[feh](/man/feh)(1), [eog](/man/eog)(1), [convert](/man/convert)(1), [identify](/man/identify)(1), [magick](/man/magick)(1), [mogrify](/man/mogrify)(1)"]],"e":["feh","eog","convert","identify","magick","mogrify"],"x":"display [image.jpg]"},"doc-engine":{"n":"doc-engine","t":"Zero-config Markdown to PDF documentation engine using Typst","s":[["TAGLINE","Zero-config Markdown to PDF documentation engine using Typst"],["TLDR","**Generate a PDF** from README.md with zero configuration\n\n```doc-engine build```\n\n**Convert a specific file** and override title and author\n\n```doc-engine build CONTRIBUTING.md -o guide.pdf -t \"Guide\" -a \"Team\"```\n\n**Use a different template** and accent color\n\n```doc-engine build --template modern --accent teal```\n\n**Check for Markdown errors** without generating PDF\n\n```doc-engine build --dry-run```\n\n**Install via pipx**\n\n```pipx install doc-engine-cli```"],["SYNOPSIS","**doc-engine** build [INPUT_FILE] [options]\n\n**doc-engine** info\n\n**doc-engine** --help | --version"],["DESCRIPTION","**doc-engine** is a CLI tool that converts Markdown files into professionally styled PDF documents using Typst as the rendering backend. It auto-detects README.md, extracts metadata from Git, and produces high-quality technical or academic reports complete with cover page, table of contents, and syntax-highlighted code.\n\nIt requires no LaTeX, Pandoc, or manual configuration. The tool supports five built-in templates and custom accent colors. It can also perform source error checking with precise line/column reporting."],["PARAMETERS","**build** [_INPUT_FILE_]\n\n> Convert the Markdown file to PDF. Defaults to auto-detecting README.md in the current directory.\n\n**-o**, **--output** _file.pdf_\n\n> Output file path. Default: <input>_doc.pdf\n\n**-t**, **--title** _title_\n\n> Document title. Defaults to first # heading or Git-derived.\n\n**-a**, **--author** _name_\n\n> Author name. Defaults to `git config user.name`.\n\n**--template** _name_\n\n> Template: academic (default), modern, minimal, technical, book.\n\n**--accent** _color_\n\n> Accent color (hex or named: blue, teal, rose, etc.).\n\n**--bib** _file.bib_\n\n> Bibliography file for IEEE-style references section.\n\n**--dry-run**\n\n> Validate Markdown and report errors without producing PDF.\n\n**--open**\n\n> Open the generated PDF after creation.\n\n**--no-branding**\n\n> Omit \"doc-engine\" attribution from footer."],["EXAMPLES","Basic zero-config usage in a project directory:\n\n```doc-engine build```\n\nGenerate with custom options:\n\n```doc-engine build docs/api.md -o api.pdf --template technical --accent \"#7c3aed\"```"],["CAVEATS","Images in Markdown are rendered only as alt text (remote images not downloaded or embedded). Complex layouts may require reviewing the output PDF. Requires Python 3.10+ and the Typst Python bindings (installed automatically)."],["SEE ALSO","[typst](/man/typst)(1), [pandoc](/man/pandoc)(1), [weasyprint](/man/weasyprint)(1)"],["RESOURCES","```[Source code](https://github.com/leonardosalasd/doc-engine-cli)```\n\n```[Documentation](https://github.com/leonardosalasd/doc-engine-cli#readme)```"]],"e":["typst","pandoc","weasyprint"],"x":"doc-engine build"},"docker-container-ls":{"n":"docker-container-ls","t":"list containers on the system","s":[["TAGLINE","list containers on the system"],["TLDR","**List running containers**\n\n```docker container ls```\n\n**List all containers**\n\n```docker container ls -a```\n\n**List with specific format**\n\n```docker container ls --format \"{{.Names}}: {{.Status}}\"```\n\n**Filter by status**\n\n```docker container ls -f status=[running|exited|paused]```\n\n**Show only IDs**\n\n```docker container ls -q```\n\n**Filter by name**\n\n```docker container ls -f name=[container_name]```\n\n**Show the last created container**\n\n```docker container ls --latest```"],["SYNOPSIS","**docker** **container** **ls** [_options_]"],["PARAMETERS","**-a**, **--all**\n> Show all containers (default shows running).\n\n**-q**, **--quiet**\n> Only display container IDs.\n\n**-f**, **--filter** _filter_\n> Filter output based on conditions (e.g., status, name, label, ancestor, network, exited).\n\n**--format** _string_\n> Format output using Go template.\n\n**-n**, **--last** _n_\n> Show n last created containers (includes all states).\n\n**-l**, **--latest**\n> Show the most recently created container (includes all states).\n\n**-s**, **--size**\n> Display total file sizes.\n\n**--no-trunc**\n> Do not truncate output."],["DESCRIPTION","**docker container ls** lists containers on the Docker host, providing information about container IDs, images, commands, creation time, status, ports, and names. By default, only running containers are displayed, but the **-a** flag reveals all containers regardless of state.\n\nThis command is equivalent to the legacy **docker ps** command and serves as the primary interface for viewing container inventory. Filtering and formatting options make it suitable for scripting and automation workflows."],["INSTALL","```apt: sudo apt install docker-cli```\n\n```dnf: sudo dnf install docker-cli```\n\n```pacman: sudo pacman -S docker```\n\n```apk: sudo apk add docker-cli```\n\n```zypper: sudo zypper install docker```\n\n```brew: brew install docker```\n\n```nix: nix profile install nixpkgs#docker```"],["SEE ALSO","[docker-ps](/man/docker-ps)(1), [docker-container](/man/docker-container)(1), [docker-container-run](/man/docker-container-run)(1), [docker-container-rm](/man/docker-container-rm)(1)"]],"e":["docker-ps","docker-container","docker-container-run","docker-container-rm"],"x":"docker container ls"},"dolt-commit":{"n":"dolt-commit","t":"record staged database table changes","s":[["TAGLINE","record staged database table changes"],["TLDR","**Commit staged changes**\n\n```dolt commit -m \"[message]\"```\n\n**Commit all changes** including unstaged modified tables\n\n```dolt commit -am \"[message]\"```\n\n**Amend last commit**\n\n```dolt commit --amend```\n\n**Commit with a specific author**\n\n```dolt commit -m \"[message]\" --author \"[Name <email>]\"```\n\n**Create an empty commit** (e.g., for triggering CI)\n\n```dolt commit --allow-empty -m \"[message]\"```"],["SYNOPSIS","**dolt** **commit** [_options_]"],["PARAMETERS","**-m**, **--message** _string_\n> Commit message.\n\n**-a**, **--all**\n> Stage all modified tables and commit.\n\n**--amend**\n> Amend the previous commit instead of creating a new one.\n\n**--author** _string_\n> Override the commit author (format: \"Name <email>\").\n\n**--date** _string_\n> Override the commit date.\n\n**--allow-empty**\n> Allow creating a commit with no changes.\n\n**--force**\n> Force the commit, ignoring any warnings.\n\n**-A**, **--ALL**\n> Stage all tables (including new and untracked) and commit."],["DESCRIPTION","**dolt commit** records changes to the database repository by creating a new commit with staged table modifications. It captures a snapshot of the current state of all staged tables, preserving the data and schema at that moment in the repository's history.\n\nEach commit requires a message describing the changes, creating an audit trail of database evolution over time. The command mirrors Git's commit workflow but operates on database tables instead of files, enabling version control for data.\n\nCommits can be amended, authored by different users, and can include all modified tables automatically with the -a flag. Use -A (uppercase) to also include new untracked tables. Empty commits are rejected by default to prevent meaningless history entries."],["INSTALL","```pacman: sudo pacman -S dolt```\n\n```brew: brew install dolt```\n\n```nix: nix profile install nixpkgs#dolt```"],["SEE ALSO","[dolt-add](/man/dolt-add)(1), [dolt-status](/man/dolt-status)(1), [dolt-branch](/man/dolt-branch)(1), [dolt-merge](/man/dolt-merge)(1)"]],"e":["dolt-add","dolt-status","dolt-branch","dolt-merge"],"x":"dolt commit -m \"[message]\""},"dot":{"n":"dot","t":"hierarchical directed graph renderer","s":[["TAGLINE","hierarchical directed graph renderer"],["TLDR","**Render graph to** PNG image\n\n```dot -Tpng [graph.dot] -o [output.png]```\n\n**Render to SVG**\n\n```dot -Tsvg [graph.dot] -o [output.svg]```\n\n**Render to PDF**\n\n```dot -Tpdf [graph.dot] -o [output.pdf]```\n\n**Render from stdin**\n\n```echo \"digraph { A -> B }\" | dot -Tpng -o [output.png]```\n\n**Use specific layout** engine\n\n```dot -Kneato [graph.dot] -Tpng -o [output.png]```\n\n**Render with custom** DPI\n\n```dot -Gdpi=[300] -Tpng [graph.dot] -o [output.png]```\n\n**Name outputs automatically** from the input files\n\n```dot -Tpng -O [graph1.dot] [graph2.dot]```\n\n**Print the computed layout** back out as DOT, with coordinates\n\n```dot -Tdot [graph.dot] -o [laid_out.dot]```\n\n**List the output formats** this build supports\n\n```dot -T?```"],["SYNOPSIS","**dot** [**-K**_engine_] [**-T**_format_] [**-o**_outfile_] [_files_...]"],["PARAMETERS","_FILES_\n> Input files in the DOT language. Reads standard input when none are given.\n\n**-T**_FORMAT_[**:**_RENDERER_]\n> Output format: `png`, `svg`, `pdf`, `ps`, `json`, `dot`, `plain`, and many more. **-T?** lists the ones your build supports.\n\n**-o** _FILE_\n> Write output to _FILE_ instead of standard output.\n\n**-O**\n> Derive each output file name from the input name plus the format suffix. Useful for batches.\n\n**-K**_ENGINE_\n> Layout engine to use instead of the default: `dot`, `neato`, `fdp`, `sfdp`, `circo`, `twopi`, `osage`, `patchwork`. This is what lets one binary act as any of them.\n\n**-G**_NAME_=_VALUE_\n> Set a graph attribute, e.g. **-Grankdir=LR** or **-Gdpi=300**.\n\n**-N**_NAME_=_VALUE_\n> Set a default node attribute, e.g. **-Nshape=box**.\n\n**-E**_NAME_=_VALUE_\n> Set a default edge attribute, e.g. **-Ecolor=blue**.\n\n**-n**[**1**|**2**]\n> No-op layout: take node positions from the input's `pos` attributes rather than computing them.\n\n**-x**\n> Reduce the graph before laying it out.\n\n**-y**\n> Invert the y coordinate in the output.\n\n**-v**\n> Verbose: report timing and progress.\n\n**-q**[_LEVEL_]\n> Suppress warnings.\n\n**-V**\n> Print the Graphviz version and exit."],["DESCRIPTION","**dot** draws **directed** graphs using a hierarchical, layered layout: it assigns nodes to ranks so that edges point consistently in one direction, then orders nodes within each rank to reduce crossings. That makes it the right choice for anything with a natural flow, such as call graphs, build dependencies, state machines, and org charts.\n\nIt reads the **DOT language**, a small declarative syntax where `digraph { a -> b }` is a complete program, and writes to a wide range of formats: raster (`png`, `jpg`), vector (`svg`, `pdf`, `ps`), and data (`json`, `plain`, and `dot` itself, which round-trips the graph with computed coordinates so another tool can render it).\n\ndot is one face of the Graphviz layout family. The others differ only in algorithm, and any of them can be selected from this binary with **-K**: `neato` and `fdp` use force-directed placement for undirected graphs, `circo` a circular layout, `twopi` a radial one, and `sfdp` a scalable multilevel variant for very large graphs."],["CAVEATS","Hierarchical layout scales poorly: past a few thousand nodes, dot becomes slow and the result is usually unreadable regardless: reach for `sfdp` and filtering instead. Layout is deterministic but not stable, so a small edit to the input can rearrange the whole picture, which makes rendered diagrams awkward to diff. Which output formats and fonts are available depends on how the package was built, hence **-T?**. Note also that `dot` is both the program and the name of the language, so \"a dot file\" may mean either."],["HISTORY","dot grew out of graph-drawing research at **AT&T Bell Labs**, and later AT&T Labs Research, beginning around **1991**; the layered algorithm it implements descends from the work of Sugiyama and, in this lineage, from Gansner, Koutsofios, North, and Vo. Graphviz was released as open source in **2000**, and its DOT language has since become a lingua franca for graph description, emitted by profilers, compilers, documentation generators, and countless ad-hoc scripts."],["INSTALL","```apt: sudo apt install graphviz```\n\n```dnf: sudo dnf install graphviz```\n\n```pacman: sudo pacman -S graphviz```\n\n```apk: sudo apk add graphviz```\n\n```zypper: sudo zypper install graphviz```\n\n```brew: brew install graphviz```\n\n```nix: nix profile install nixpkgs#graphviz```"],["SEE ALSO","[neato](/man/neato)(1), [circo](/man/circo)(1), [fdp](/man/fdp)(1), [twopi](/man/twopi)(1), [graphviz](/man/graphviz)(7)"],["RESOURCES","```[Source code](https://gitlab.com/graphviz/graphviz)```\n\n```[Homepage](https://graphviz.org)```\n\n```[Documentation](https://graphviz.org/doc/info/command.html)```"]],"e":["neato","circo","fdp","twopi","graphviz"],"x":"dot -Tpng [graph.dot] -o [output.png]"},"dotty":{"n":"dotty","t":"removed interactive DOT graph viewer and editor","s":[["TAGLINE","removed interactive DOT graph viewer and editor"],["TLDR","**dotty has been removed from Graphviz.** Use a maintained viewer instead.\n\nView a graph interactively with **xdot**\n\n```xdot [graph.dot]```\n\nRender to **SVG** and open it in a browser\n\n```dot -Tsvg [graph.dot] -o [graph.svg]```\n\nEdit graphs visually with **Gephi**, **yEd**, or a live-preview editor\n\n```dot -Tx11 [graph.dot]```"],["SYNOPSIS","**dotty** [**-V**] [**-f**] [**-lm** _mode_] [_files_...]"],["PARAMETERS","_FILES_\n> DOT files to open.\n\n**-V**\n> Print the version and exit.\n\n**-f**\n> Load and reformat the graph even if it already has layout coordinates.\n\n**-lm** _MODE_\n> Set the layout mode: **sync** or **async**."],["DESCRIPTION","**dotty** was an interactive graph viewer and editor for the DOT language, shipped with Graphviz. Unlike **dot**, which renders a static image, dotty opened a window in which a graph could be panned, zoomed, edited, and re-laid-out, with nodes and edges added or deleted by mouse.\n\nIt was written not in C but in **lefty**, a small programmable graphics language that also came with Graphviz. lefty handled the window and the interaction, and shelled out to `dot` or `neato` for layout. That design made dotty highly scriptable, since its entire behaviour was source code you could edit, but it also tied it to a language nobody else used.\n\n**This command no longer exists.** lefty, and with it dotty and lneato, were disabled in the build system in **September 2021**, dropped from the packages published on graphviz.org in **January 2022**, and finally deleted from the repository. Current Graphviz releases do not contain any of them. The code remains in the project's git history for anyone who needs to build an old version."],["CAVEATS","If a distribution still ships **dotty**, it is packaging an old Graphviz release. The command requires X11, cannot be built against current Graphviz sources, and receives no fixes.\n\nFor interactive viewing, **xdot** is the usual replacement: it is actively maintained, reads the same DOT files, and offers pan, zoom, and click-to-navigate. For editing graphs by hand, general graph editors such as yEd or Gephi are far more capable than dotty ever was, and for a quick look, rendering to SVG with `dot -Tsvg` and opening the result in a browser costs nothing."],["HISTORY","dotty came out of the original Graphviz work at **AT&T Bell Labs** and later AT&T Labs Research, in the early **1990s**, when an interactive X11 viewer was the only convenient way to explore a large graph. It was written by Eleftherios Koutsofios on top of his **lefty** language.\n\nIts removal was unusually cautious even by Graphviz standards. The maintainers suspected lefty had been unused for years but, given how widely Graphviz is deployed, staged the removal across several years so that anyone still depending on it would have time to object. Almost nobody did."],["INSTALL","```apt: sudo apt install graphviz```\n\n```dnf: sudo dnf install graphviz```\n\n```pacman: sudo pacman -S graphviz```\n\n```zypper: sudo zypper install graphviz```\n\n```brew: brew install graphviz```\n\n```nix: nix profile install nixpkgs#graphviz```"],["SEE ALSO","[dot](/man/dot)(1), [xdot](/man/xdot)(1), [neato](/man/neato)(1), [graphviz](/man/graphviz)(7)"],["RESOURCES","```[Source code](https://gitlab.com/graphviz/graphviz)```\n\n```[Homepage](https://graphviz.org)```\n\n```[Documentation](https://graphviz.org/docs/cli/dotty/)```"]],"e":["dot","xdot","neato","graphviz"],"x":"xdot [graph.dot]"},"dsniff":{"n":"dsniff","t":"network password sniffer","s":[["TAGLINE","network password sniffer"],["TLDR","**Sniff passwords** on interface\n\n```sudo dsniff -i [eth0]```\n\n**Read from capture file**\n\n```dsniff -p [capture.pcap]```\n\n**Write to file**\n\n```sudo dsniff -i [eth0] -w [output.txt]```\n\n**Enable automatic protocol detection**\n\n```sudo dsniff -i [eth0] -m```\n\n**Read from a previously saved session file**\n\n```dsniff -r [savefile]```\n\n**Sniff with custom trigger definitions**\n\n```sudo dsniff -i [eth0] -t [80/tcp=http]```"],["SYNOPSIS","**dsniff** [_options_]"],["DESCRIPTION","**dsniff** is a password sniffer that passively monitors network traffic to capture authentication credentials transmitted in cleartext. It supports a comprehensive array of protocols including FTP, Telnet, HTTP, SNMP, IMAP, POP, LDAP, rlogin, RPC, NFS, X11, Oracle, PostgreSQL, and many others.\n\nThe tool operates by analyzing packet streams and extracting usernames and passwords as they traverse the network. It performs automatic protocol detection and TCP stream reassembly to correctly capture credentials from multi-packet exchanges.\n\ndsniff was designed for authorized network security auditing and penetration testing, demonstrating the significant security risks of transmitting credentials over unencrypted protocols. Its existence helped drive the widespread adoption of encrypted alternatives like SSH and HTTPS. The tool remains relevant for security assessments and identifying legacy systems using insecure authentication methods."],["PARAMETERS","**-i** _interface_\n> Network interface to sniff.\n\n**-p** _file_\n> Read from pcap file.\n\n**-w** _file_\n> Write output to file.\n\n**-m**\n> Enable automatic protocol detection.\n\n**-n**\n> Don't resolve hostnames.\n\n**-c**\n> Perform half-duplex TCP stream reassembly.\n\n**-s** _snaplen_\n> Analyze at most the first snaplen bytes of each TCP connection (default 1024).\n\n**-f** _services_\n> Load trigger definitions from a services configuration file.\n\n**-t** _trigger[,...]_\n> Load triggers from a comma-separated list, specified as port/proto=service.\n\n**-r** _savefile_\n> Read previously captured sessions from a saved file.\n\n**-d**\n> Enable debugging.\n\n_expression_\n> Specify a tcpdump(8) filter expression to select traffic to sniff."],["SUPPORTED PROTOCOLS","FTP, Telnet, SMTP, HTTP, POP, poppass, NNTP, IMAP, SNMP, LDAP, Rlogin, RIP, OSPF, PPTP MS-CHAP, NFS, VRRP, YP/NIS, SOCKS, X11, CVS, IRC, AIM, ICQ, Napster, PostgreSQL, Meeting Maker, Citrix ICA, Symantec pcAnywhere, NAI Sniffer, Microsoft SMB, Oracle SQL*Net, Sybase, Microsoft SQL"],["CAVEATS","Only for authorized security testing. Encrypted protocols (SSH, HTTPS) not captured. Requires root/admin privileges. May violate network policies. Protocol detection not perfect."],["HISTORY","**dsniff** was created by **Dug Song** around **2000** as part of a suite of network auditing tools. It demonstrated the dangers of cleartext authentication, contributing to the push for encrypted protocols. The dsniff suite includes related tools like arpspoof and macof."],["INSTALL","```apt: sudo apt install dsniff```\n\n```dnf: sudo dnf install dsniff```\n\n```pacman: sudo pacman -S dsniff```\n\n```nix: nix profile install nixpkgs#dsniff```"],["SEE ALSO","[tcpdump](/man/tcpdump)(1), [wireshark](/man/wireshark)(1), [arpspoof](/man/arpspoof)(1), [ettercap](/man/ettercap)(8), [tcpreplay](/man/tcpreplay)(1)"]],"e":["tcpdump","wireshark","arpspoof","ettercap","tcpreplay"],"x":"sudo dsniff -i [eth0]"},"expr":{"n":"expr","t":"evaluate arithmetic and string expressions in shell","s":[["TAGLINE","evaluate arithmetic and string expressions in shell"],["TLDR","**Evaluate arithmetic** expression\n\n```expr [5] + [3]```\n\n**Multiply** (escape the operator)\n\n```expr [5] \\* [3]```\n\n**String length**\n\n```expr length \"[string]\"```\n\n**Substring extraction** (1-indexed)\n\n```expr substr \"[string]\" [1] [5]```\n\n**Pattern matching**\n\n```expr \"[string]\" : '[regex]'```\n\n**Compare values** (escape the operator)\n\n```expr [10] \\> [5]```\n\n**Find index** of first character match\n\n```expr index \"[string]\" \"[chars]\"```"],["SYNOPSIS","**expr** _expression_"],["PARAMETERS","_EXPRESSION_\n> Mathematical or string expression to evaluate.\n\n**+**, **-**, **\\***, **/**, **%**\n> Arithmetic operators (multiply must be escaped).\n\n**=**, **!=**, **\\<**, **\\>**, **\\<=**, **\\>=**\n> Comparison operators (escape < and >).\n\n**length** _STRING_\n> Return string length.\n\n**substr** _STRING_ _POS_ _LEN_\n> Extract substring (1-indexed).\n\n**index** _STRING_ _CHARS_\n> Find first occurrence of characters.\n\n**match** _STRING_ _REGEX_\n> Pattern match (same as STRING : REGEX).\n\n**|**\n> Logical OR: ARG1 | ARG2 returns ARG1 if non-null and non-zero, else ARG2.\n\n**&**\n> Logical AND: ARG1 & ARG2 returns ARG1 if both are non-null and non-zero, else 0.\n\n**--help**\n> Display help information.\n\n**--version**\n> Display version information."],["DESCRIPTION","**expr** evaluates expressions and outputs the result. It handles integer arithmetic, string operations, and comparisons. Results are printed to standard output with exit status indicating boolean results.\n\nOperators must be passed as separate arguments, with shell metacharacters escaped. For arithmetic, expr only handles integers. String operations include length, substring extraction, and regex matching.\n\nexpr is often used in shell scripts for calculations and string manipulation, though modern shells provide built-in alternatives."],["CAVEATS","Operators need escaping (\\* for multiply, \\> for comparison). Only integer arithmetic supported. Returns exit code 1 for zero/false results. Regex uses basic regular expressions only."],["HISTORY","expr originated in **Version 7 Unix** (1979) as a general-purpose expression evaluator. It remains part of POSIX and GNU coreutils, though many of its functions are now handled by shell builtins."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[bc](/man/bc)(1), [test](/man/test)(1), [bash](/man/bash)(1)"]],"e":["bc","test","bash"],"x":"expr [5] + [3]"},"fabric":{"n":"fabric","t":"Python SSH automation and deployment tool","s":[["TAGLINE","Python SSH automation and deployment tool"],["TLDR","**Run a shell command on a remote host**\n\n```fab -H [host] -- [command]```\n\n**Run a task defined in fabfile.py**\n\n```fab [taskname]```\n\n**Run a task on a specific host**\n\n```fab -H [user@host] [taskname]```\n\n**List all available tasks**\n\n```fab --list```\n\n**Run a task using a specific SSH key**\n\n```fab -i [~/.ssh/key] -H [host] [taskname]```\n\n**Show detailed help for a specific task**\n\n```fab -d [taskname]```"],["SYNOPSIS","**fab** [_options_] [_task_[:_arg_,...]]..."],["PARAMETERS","**-H**, **--hosts** _hosts_\n> Comma-separated host list.\n\n**-i** _key_\n> SSH identity file.\n\n**-u**, **--user** _user_\n> SSH username.\n\n**-l**, **--list**\n> List available tasks.\n\n**-p**, **--password**\n> Prompt for SSH password.\n\n**-d** _task_\n> Show task docstring.\n\n**--** _command_\n> Run shell command directly.\n\n**-c**, **--config** _file_\n> Config file path.\n\n**-r**, **--roles** _roles_\n> Roles to operate on."],["DESCRIPTION","**Fabric** is a Python library and command-line tool for executing shell commands on remote servers over SSH. It simplifies deployment, system administration, and automation tasks.\n\nTasks are defined in a **fabfile.py** using Python. Fabric 2.x provides a cleaner API than version 1.x, with Connection objects and a simpler task decorator."],["FABFILE EXAMPLE","```python\nfrom fabric import task\n\n@task\ndef deploy(c):\n    c.run(\"git pull\")\n    c.run(\"pip install -r requirements.txt\")\n    c.run(\"systemctl restart myapp\")\n\n@task\ndef uptime(c):\n    c.run(\"uptime\")\n```"],["CAVEATS","Version 2.x is significantly different from 1.x. Requires SSH access. Python 3 required for Fabric 2+. Complex deployments may benefit from Ansible or similar tools."],["HISTORY","Fabric was created by **Jeff Forcier** in **2009** as a simpler alternative to Capistrano for Python deployments. Version 2.0, released in **2018**, was a complete rewrite with a modernized API."],["INSTALL","```pacman: sudo pacman -S fabric```\n\n```brew: brew install fabric```"],["SEE ALSO","[ansible](/man/ansible)(1), [ssh](/man/ssh)(1)"]],"e":["ansible","ssh"],"x":"fab -H [host] -- [command]"},"getenforce":{"n":"getenforce","t":"SELinux enforcement mode checker","s":[["TAGLINE","SELinux enforcement mode checker"],["TLDR","Print SELinux mode: **Enforcing**, **Permissive**, or **Disabled**\n\n```getenforce```"],["SYNOPSIS","**getenforce**"],["DESCRIPTION","**getenforce** displays the current mode of SELinux (Security-Enhanced Linux). The command outputs one of three possible values:\n\n**Enforcing** - SELinux security policy is enforced. Access violations are blocked and logged.\n\n**Permissive** - SELinux security policy is not enforced but violations are logged. Useful for troubleshooting and policy development.\n\n**Disabled** - SELinux is completely disabled.\n\nThis is a quick way to check SELinux status without parsing configuration files or using more verbose tools like **sestatus**."],["CAVEATS","Returns \"Disabled\" if SELinux is not compiled into the kernel or if it was disabled at boot. The mode can be changed at runtime with **setenforce** (between Enforcing and Permissive only), but changing to/from Disabled requires a reboot."],["HISTORY","getenforce is part of the SELinux project, which was developed by the NSA (National Security Agency) and released as open source in **2000**. It was integrated into the Linux kernel in version 2.6 in **2003** and became the default security module for Red Hat Enterprise Linux and Fedora."],["INSTALL","```apt: sudo apt install selinux-utils```\n\n```dnf: sudo dnf install libselinux-utils```\n\n```apk: sudo apk add libselinux-utils```"],["SEE ALSO","[setenforce](/man/setenforce)(1), [sestatus](/man/sestatus)(8)"]],"e":["setenforce","sestatus"],"x":"getenforce"},"git-bisect":{"n":"git-bisect","t":"use binary search to find the commit that introduced a bug","s":[["TAGLINE","use binary search to find the commit that introduced a bug"],["TLDR","**Start** a bisect session\n\n```git bisect start```\n\n**Start** with the bad and good commits given up front\n\n```git bisect start [HEAD] [v1.0]```\n\n**Mark** the current commit as broken\n\n```git bisect bad```\n\n**Mark** a commit as working\n\n```git bisect good [commit]```\n\n**Skip** a commit that cannot be tested\n\n```git bisect skip```\n\n**Automate** the search with a test script\n\n```git bisect run [./test.sh]```\n\n**Automate** with an inline test command\n\n```git bisect run [make test]```\n\n**Show** what has been marked so far\n\n```git bisect log```\n\n**End** the session and return to where you started\n\n```git bisect reset```"],["SYNOPSIS","**git bisect** _subcommand_ [_options_]"],["PARAMETERS","**start** [_bad_ [_good_...]] [**--**] [_paths_...]\n> Begin a session. Optionally give the bad and good commits immediately, and limit the search to _paths_.\n\n**bad** [_commit_]\n> Mark a commit as containing the bug. Defaults to the current commit.\n\n**good** [_commit_...]\n> Mark one or more commits as free of the bug.\n\n**new** [_commit_], **old** [_commit_]\n> Aliases for **bad** and **good**, for hunting any change in behaviour rather than a bug.\n\n**terms** **--term-old**=_term_ **--term-new**=_term_\n> Use your own words, e.g. **--term-old=fast --term-new=slow**.\n\n**skip** [_commit_...]\n> Skip commits that cannot be tested, e.g. ones that do not build.\n\n**run** _cmd_ [_args_...]\n> Run _cmd_ at each step and mark automatically from its exit status.\n\n**reset** [_commit_]\n> End the session and return to the original branch, or to _commit_.\n\n**log**\n> Print the log of what has been marked.\n\n**replay** _logfile_\n> Replay a bisect log, resuming a previous session.\n\n**visualize**, **view**\n> Show the remaining candidate commits in gitk or **git log**.\n\n**--first-parent**\n> Follow only the first parent of merge commits.\n\n**--no-checkout**\n> Do not check out the tree; just update **BISECT_HEAD**. Useful when checkout is expensive."],["DESCRIPTION","**git bisect** finds the commit that introduced a problem by binary search. You tell it one commit where the bug exists and one where it does not; it checks out a commit in the middle, you test and report the result, and it halves the remaining range each time. The search is logarithmic, so roughly **10** tests suffice for **1000** commits and **20** for a million.\n\nThe **good**/**bad** vocabulary assumes you are hunting a bug, but the mechanism is general: any property that changed at some point can be bisected. **old**/**new**, and the custom **--term-old**/**--term-new**, exist for hunting a performance regression, a behaviour change, or even the commit that fixed something.\n\n**git bisect run** is where the command earns its reputation. Given a script that exits **0** for good and non-zero for bad, git drives the whole search unattended. The special exit code **125** means \"cannot test this commit\", producing the same effect as **skip**, and any exit code between **126** and **255** aborts the session. Because **run** takes a command line rather than only a file, **git bisect run make test** works directly.\n\nWhen the search finishes, git reports the first bad commit and leaves you at it. **git bisect reset** returns you to where you were."],["CAVEATS","Exit code **1** means bad, but so do most failures, including a compile error or a missing dependency. A test script that cannot distinguish \"the bug is present\" from \"this commit does not build\" will blame the wrong commit. Return **125** for untestable commits.\n\nAvoid exit code **255** in a test script: it aborts the bisect. **grep** returns **1** when it finds nothing, which is convenient, but a script ending in a bare **grep** inverts the meaning you probably intended, so be explicit about the exit status.\n\nBisect checks out arbitrary historical commits, so a dirty working tree can block the checkout or silently contaminate the test. Commit or stash first. Build artifacts and generated files also survive between checkouts and can mask the real result: clean them in the test script.\n\n**skip** does not narrow the range as effectively as a real answer, and skipping a whole region can leave git unable to identify a single culprit, reporting a range of candidates instead.\n\nThe result is only the first commit where your test fails, which is not always the commit that caused the problem. A bug can be introduced latent and only exposed later by an unrelated change.\n\nIf history contains merges, the first bad commit may be a merge, and the actual change lives on the merged branch. **--first-parent** limits the search to the mainline, which is often what you want on a repository that merges feature branches."],["HISTORY","**git bisect** was added by **Linus Torvalds** in **2005**, in git's first year, initially as a shell script. **git bisect run** followed shortly after, contributed by **Christian Couder**, who also added the **old**/**new** terminology in git **2.7** (**2015**) for bisecting changes that are not bugs. The implementation was gradually rewritten in C over subsequent releases."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git](/man/git)(1), [git-log](/man/git-log)(1), [git-blame](/man/git-blame)(1), [git-annotate](/man/git-annotate)(1), [git-stash](/man/git-stash)(1)"],["RESOURCES","```[Source code](https://github.com/git/git)```\n\n```[Homepage](https://git-scm.com/)```\n\n```[Documentation](https://git-scm.com/docs/git-bisect)```"]],"e":["git","git-log","git-blame","git-annotate","git-stash"],"x":"git bisect start"},"git-changelog":{"n":"git-changelog","t":"Generate formatted changelogs from commit history","s":[["TAGLINE","Generate formatted changelogs from commit history"],["TLDR","**Generate/update the changelog file** (auto-detected, or History.md)\n\n```git changelog```\n\n**Write the changelog to a specific file**\n\n```git changelog [CHANGELOG.md]```\n\n**Print the changelog to stdout** instead of writing a file\n\n```git changelog --stdout```\n\n**List commits since a start tag**, plainly (no headers)\n\n```git changelog --list --start-tag [v1.0.0]```\n\n**Exclude merge commits**\n\n```git changelog --no-merges```\n\n**Replace the changelog** instead of appending to it\n\n```git changelog --prune-old```"],["SYNOPSIS","**git changelog** [_options_] [_file_]"],["PARAMETERS","_FILE_\n> Output filename; defaults to an auto-detected Change*/History* file, or **History.md**.\n\n**-a**, **--all**\n> Include every commit, ignoring **--start-tag**/**--start-commit**/**--final-tag**.\n\n**-l**, **--list**\n> Plain list of commits, without section titles/dates.\n\n**-t**, **--tag** _LABEL_\n> Label to use as the section title for the most-recent, untagged commits.\n\n**-s**, **--start-tag** _TAG_\n> Oldest tag to start the commit range from.\n\n**--start-commit** _COMMIT_\n> Like **--start-tag**, but starts from a commit instead of a tag.\n\n**-f**, **--final-tag** _TAG_\n> Newest tag to end the commit range at.\n\n**-n**, **--no-merges**\n> Exclude merge commits.\n\n**-m**, **--merges-only**\n> Include only merge commits, with subject and body.\n\n**-p**, **--prune-old**\n> Replace the existing changelog file instead of appending to it.\n\n**-x**, **--stdout**\n> Write to standard output instead of a file.\n\n**-h**, **--help**, **?**\n> Display help information."],["DESCRIPTION","**git changelog** generates a changelog from git tags (annotated or lightweight) and commit messages. It formats commits into release notes, grouped into sections by tag, suitable for a project's CHANGELOG.\n\nIf no tags exist, every commit is included; otherwise only commits since the most recent tag are shown by default (use **--start-tag**/**--final-tag** to target a specific range). An existing Change*/History* file in the current directory is auto-detected and its content appended after the newly generated section, unless **--prune-old** is given.\n\nCommit formatting is controlled via the **changelog.format** (default `  * %s`) and **changelog.mergeformat** git config options."],["CAVEATS","Part of the git-extras package. Quality depends on commit messages. Tag-based grouping requires annotated or lightweight tags to already exist in the repository."],["HISTORY","git changelog is part of **git-extras**, created by **TJ Holowaychuk** to automate changelog generation from repository history."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-log](/man/git-log)(1), [git-tag](/man/git-tag)(1)"],["RESOURCES","```[Source code](https://github.com/tj/git-extras)```\n\n```[Documentation](https://github.com/tj/git-extras/blob/master/Commands.md#git-changelog)```"]],"e":["git-log","git-tag"],"x":"git changelog"},"hardhat":{"n":"hardhat","t":"ethereum development environment","s":[["TAGLINE","ethereum development environment"],["TLDR","**Create new project**\n\n```npx hardhat init```\n\n**Compile contracts**\n\n```npx hardhat compile```\n\n**Run tests**\n\n```npx hardhat test```\n\n**Start local node**\n\n```npx hardhat node```\n\n**Run deployment script**\n\n```npx hardhat run [scripts/deploy.js]```\n\n**Deploy to network**\n\n```npx hardhat run [scripts/deploy.js] --network [sepolia]```\n\n**Open console**\n\n```npx hardhat console```"],["SYNOPSIS","**npx hardhat** [_task_] [_options_]"],["PARAMETERS","**compile**\n> Compile Solidity contracts.\n\n**test**\n> Run test suite.\n\n**node**\n> Start local Ethereum node.\n\n**run** _script_\n> Run JavaScript script.\n\n**console**\n> Open interactive console.\n\n**clean**\n> Clear cache and artifacts.\n\n**--network** _name_\n> Target network.\n\n**--config** _file_\n> Path to a Hardhat config file.\n\n**--verbose**\n> Enable Hardhat verbose logging.\n\n**--show-stack-traces**\n> Show full stack traces.\n\n**--max-memory** _size_\n> Maximum amount of memory that Hardhat can use.\n\n**--version**\n> Show Hardhat version.\n\n**--help**\n> Show help message, or a task's help if its name is provided."],["DESCRIPTION","**Hardhat** is an Ethereum development environment. It provides a local blockchain, debugging tools, and a plugin ecosystem for compiling, testing, and deploying smart contracts.\n\nHardhat features console.log for Solidity debugging, network forking, and TypeScript support. It's the most popular choice for professional Ethereum development."],["CONFIGURATION","```javascript\n// hardhat.config.js\nmodule.exports = {\n  solidity: \"0.8.19\",\n  networks: {\n    sepolia: {\n      url: process.env.RPC_URL,\n      accounts: [process.env.PRIVATE_KEY]\n    }\n  }\n};\n```"],["CAVEATS","Requires Node.js. Network configuration needs RPC URLs. Private keys should use environment variables. Gas estimation may differ from mainnet."],["HISTORY","Hardhat was created by **Nomic Foundation** (formerly Nomic Labs) as a successor to Buidler. It became the leading Ethereum development framework, known for its developer experience and debugging capabilities."],["INSTALL","```aur: yay -S hardhat```"],["SEE ALSO","[truffle](/man/truffle)(1), [ganache](/man/ganache)(1), [forge](/man/forge)(1), [npx](/man/npx)(1)"]],"e":["truffle","ganache","forge","npx"],"x":"npx hardhat init"},"hciconfig":{"n":"hciconfig","t":"configures Bluetooth devices","s":[["TAGLINE","configures Bluetooth devices"],["TLDR","**Show all Bluetooth interfaces**\n\n```hciconfig```\n\n**Show specific interface**\n\n```hciconfig [hci0]```\n\n**Bring interface up**\n\n```sudo hciconfig [hci0] up```\n\n**Bring interface down**\n\n```sudo hciconfig [hci0] down```\n\n**Enable device scanning**\n\n```sudo hciconfig [hci0] piscan```\n\n**Reset interface**\n\n```sudo hciconfig [hci0] reset```\n\n**Show detailed info**\n\n```hciconfig -a```"],["SYNOPSIS","**hciconfig** [_options_] [_device_] [_command_]"],["DESCRIPTION","**hciconfig** configures Bluetooth devices. It displays and modifies HCI (Host Controller Interface) device settings, similar to ifconfig for network interfaces.\n\nThe tool manages Bluetooth adapter state, visibility, and various parameters like name, class, and scan modes."],["PARAMETERS","**-a**\n> Show all information.\n\n**up**\n> Bring device up.\n\n**down**\n> Bring device down.\n\n**reset**\n> Reset device.\n\n**piscan**\n> Enable page and inquiry scan.\n\n**noscan**\n> Disable scanning.\n\n**iscan**\n> Enable inquiry scan only.\n\n**pscan**\n> Enable page scan only.\n\n**name** _name_\n> Set local name.\n\n**class** _class_\n> Set device class.\n\n**auth**\n> Enable authentication.\n\n**noauth**\n> Disable authentication."],["CAVEATS","Deprecated in favor of bluetoothctl. Requires root for configuration. Changes may not persist after reboot. Interface naming may vary."],["HISTORY","**hciconfig** is part of **BlueZ**, the official Linux Bluetooth stack. BlueZ was created by **Maxim Krasnyansky** and **Marcel Holtmann**, and is maintained by the community with Intel contributions. While hciconfig is being phased out, it remains useful for low-level Bluetooth configuration."],["INSTALL","```apt: sudo apt install bluez```\n\n```dnf: sudo dnf install bluez```\n\n```pacman: sudo pacman -S bluez```\n\n```apk: sudo apk add bluez-deprecated```\n\n```zypper: sudo zypper install bluez```\n\n```brew: brew install bluez```\n\n```nix: nix profile install nixpkgs#bluez```"],["SEE ALSO","[bluetoothctl](/man/bluetoothctl)(1), [hcitool](/man/hcitool)(1), [btmgmt](/man/btmgmt)(1), [rfkill](/man/rfkill)(1)"]],"e":["bluetoothctl","hcitool","btmgmt","rfkill"],"x":"hciconfig"},"hl":{"n":"hl","t":"Fast log viewer and processor for JSON and logfmt","s":[["TAGLINE","Fast log viewer and processor for JSON and logfmt"],["TLDR","**View JSON logs** from a file\n\n```hl [path/to/logfile.json]```\n\n**Filter logs by level**\n\n```hl -l [error] [path/to/logfile]```\n\n**Filter by field value**\n\n```hl -f [key]=[value] [path/to/logfile]```\n\n**Stream logs** without pager\n\n```hl -P [path/to/logfile]```\n\n**Filter by time range**\n\n```hl --since \"[2024-01-01]\" --until \"[2024-01-02]\" [path/to/logfile]```\n\n**Show logs in local timezone**\n\n```hl -L [path/to/logfile]```\n\n**Read from stdin**\n\n```cat [path/to/logfile] | hl```"],["SYNOPSIS","**hl** [**-l** _level_] [**-f** _filter_] [**--since** _time_] [**--until** _time_] [**-P**] [**-L**] [_file_...]"],["PARAMETERS","**-l** _LEVEL_\n> Filter by log level (debug, info, warn, error)\n\n**-f** _KEY=VALUE_\n> Filter by field value\n\n**--since** _TIME_\n> Show logs after this timestamp\n\n**--until** _TIME_\n> Show logs before this timestamp\n\n**-P**\n> Disable pager (streaming mode)\n\n**-L**\n> Show timestamps in local timezone\n\n**-Z** _ZONE_\n> Show timestamps in specified timezone\n\n**--allow-prefix**\n> Process logs with non-JSON prefixes"],["DESCRIPTION","**hl** is a high-performance log viewer and processor that converts **JSON** logs and **logfmt** logs into clear human-readable format. It automatically detects log formats and renders them with color-coded output for quick analysis.\n\nThe tool supports field-based filtering, log level filtering, and timestamp range filtering. It automatically integrates with a pager (defaulting to **less**) and supports theme customization through configuration files."],["CAVEATS","Designed for structured log formats (JSON, logfmt); plain text logs are passed through as-is. Very large log files benefit from the tool's streaming mode to avoid buffering the entire file in memory."],["HISTORY","**hl** was created by **pamburus** and is written in **Rust**. It was designed as a blazing-fast alternative to tools like **jq** for log analysis, with built-in understanding of common log formats and levels."],["INSTALL","```pacman: sudo pacman -S hl```\n\n```brew: brew install hl```"],["SEE ALSO","[jq](/man/jq)(1), [less](/man/less)(1), [tailspin](/man/tailspin)(1)"]],"e":["jq","less","tailspin"],"x":"hl [path/to/logfile.json]"},"ifdata":{"n":"ifdata","t":"retrieves network interface information in a script-friendly format","s":[["TAGLINE","retrieves network interface information in a script-friendly format"],["TLDR","**Get IP address**\n\n```ifdata -pa [eth0]```\n\n**Get netmask**\n\n```ifdata -pn [eth0]```\n\n**Get broadcast address**\n\n```ifdata -pb [eth0]```\n\n**Get MAC address**\n\n```ifdata -ph [eth0]```\n\n**Get MTU**\n\n```ifdata -pm [eth0]```\n\n**Check if interface exists**\n\n```ifdata -e [eth0]```\n\n**Get network address**\n\n```ifdata -pN [eth0]```"],["SYNOPSIS","**ifdata** [_options_] _interface_"],["PARAMETERS","**-e**\n> Test if interface exists (exit code).\n\n**-pa**\n> Print IP address.\n\n**-pn**\n> Print netmask.\n\n**-pN**\n> Print network address.\n\n**-pb**\n> Print broadcast address.\n\n**-pm**\n> Print MTU.\n\n**-ph**\n> Print hardware (MAC) address.\n\n**-pf**\n> Print flags."],["DESCRIPTION","**ifdata** retrieves network interface information in a script-friendly format. Part of the moreutils package. Unlike ifconfig or ip, it returns single clean values without parsing. Exit codes indicate success/failure for conditional testing in scripts."],["INSTALL","```apt: sudo apt install moreutils```\n\n```dnf: sudo dnf install moreutils```\n\n```pacman: sudo pacman -S moreutils```\n\n```apk: sudo apk add moreutils```\n\n```zypper: sudo zypper install moreutils```\n\n```brew: brew install moreutils```\n\n```nix: nix profile install nixpkgs#moreutils```"],["SEE ALSO","[ip](/man/ip)(1), [ifconfig](/man/ifconfig)(1)"]],"e":["ip","ifconfig"],"x":"ifdata -pa [eth0]"},"impala":{"n":"impala","t":"terminal user interface for managing Wi-Fi networks","s":[["TAGLINE","terminal user interface for managing Wi-Fi networks"],["TLDR","Launch in **station mode**\n\n```impala```\n\nLaunch in **Access Point** mode\n\n```impala -m ap```\n\n**Navigate** between sections\n\n```Tab / Shift+Tab```\n\n**Select** a network\n\n```Space```\n\nShow **hotkeys**\n\n```?```"],["SYNOPSIS","**impala** [_options_]"],["DESCRIPTION","**impala** is a terminal user interface (TUI) for managing Wi-Fi networks. It provides an interactive way to scan, connect, and manage wireless connections without using the command line directly."],["PARAMETERS","**-m, --mode MODE**\n> Set operating mode: \"station\" (default) or \"ap\" (Access Point)\n\n**-h, --help**\n> Display help information\n\n**-V, --version**\n> Display version information"],["CAVEATS","Requires appropriate wireless interface and permissions. Access Point mode requires a compatible wireless adapter that supports AP mode. Uses iwd (iNet Wireless Daemon) as the backend."],["HISTORY","**impala** was created to provide a user-friendly TUI for wireless network management, making it easier to manage Wi-Fi connections in terminal environments."],["INSTALL","```pacman: sudo pacman -S impala```\n\n```apk: sudo apk add impala```\n\n```zypper: sudo zypper install impala```\n\n```nix: nix profile install nixpkgs#impala```"],["SEE ALSO","[iwctl](/man/iwctl)(1), [nmtui](/man/nmtui)(1), [nmcli](/man/nmcli)(1), [wpa_cli](/man/wpa_cli)(8)"]],"e":["iwctl","nmtui","nmcli","wpa_cli"],"x":"impala"},"jpegoptim":{"n":"jpegoptim","t":"optimizes JPEG files by removing metadata and optionally reducing quality","s":[["TAGLINE","optimizes JPEG files by removing metadata and optionally reducing quality"],["TLDR","**Optimize JPEG losslessly**\n\n```jpegoptim [image.jpg]```\n\n**Optimize to target quality**\n\n```jpegoptim -m [85] [image.jpg]```\n\n**Optimize to target size** (kilobytes or percentage)\n\n```jpegoptim --size=[500k] [image.jpg]```\n\n**Strip all metadata**\n\n```jpegoptim --strip-all [image.jpg]```\n\n**Optimize all JPEGs in directory**\n\n```jpegoptim [*.jpg]```\n\n**Write optimized copy** to another directory, keeping the original\n\n```jpegoptim -d [output_dir] [image.jpg]```\n\n**Simulate** (print results without writing)\n\n```jpegoptim -n [image.jpg]```"],["SYNOPSIS","**jpegoptim** [_options_] _files_..."],["DESCRIPTION","**jpegoptim** optimizes JPEG files by removing metadata and optionally reducing quality. It can produce smaller files through lossless optimization or lossy quality reduction.\n\nThe tool is useful for web images, photo galleries, and reducing storage requirements while maintaining acceptable visual quality."],["PARAMETERS","**-m** _quality_, **--max**=_quality_\n> Set maximum image quality factor (0-100). Disables the default lossless mode.\n\n**-S** _size_, **--size**=_size_\n> Try to optimize to the given size, in kilobytes (e.g. 500k) or as a percentage (1%-99%). Disables lossless mode.\n\n**-d** _dir_, **--dest**=_dir_\n> Write optimized files to an alternative destination directory instead of overwriting the originals.\n\n**-f**, **--force**\n> Force optimization even if the result is larger than the original.\n\n**-T** _threshold_, **--threshold**=_threshold_\n> Keep the old file if the size gain is below the given threshold percentage.\n\n**-n**, **--noaction**\n> Simulate: print results without modifying any file.\n\n**-o**, **--overwrite**\n> Overwrite an existing target file in the destination directory (only meaningful with -d).\n\n**-p**, **--preserve**\n> Preserve file modification and access times.\n\n**-r**, **--retry**\n> Recursively re-optimize until the file size stops shrinking.\n\n**-w** _max_, **--workers**=_max_\n> Set the maximum number of parallel threads (default 1).\n\n**-t**, **--totals**\n> Print totals after processing all files.\n\n**--strip-all**\n> Strip all markers (metadata) from output.\n\n**--strip-com**\n> Strip comment markers.\n\n**--strip-exif**\n> Strip Exif data.\n\n**--strip-icc**\n> Strip ICC color profile markers.\n\n**--strip-xmp**\n> Strip XMP markers.\n\n**--all-progressive**\n> Force all output files to be progressive (use --all-normal for baseline).\n\n**--stdin**, **--stdout**\n> Read input from standard input / write output to standard output.\n\n**-q**, **--quiet**\n> Quiet mode."],["CAVEATS","Lossy optimization permanent. Stripping ICC may affect colors. Some metadata useful for organization. Progressive encoding may differ."],["HISTORY","**jpegoptim** was created by **Timo Kokkonen** as a command-line JPEG optimizer. It provides a simpler alternative to jpegtran for common optimization tasks, focusing on ease of use for batch processing."],["INSTALL","```apt: sudo apt install jpegoptim```\n\n```dnf: sudo dnf install jpegoptim```\n\n```pacman: sudo pacman -S jpegoptim```\n\n```apk: sudo apk add jpegoptim```\n\n```zypper: sudo zypper install jpegoptim```\n\n```brew: brew install jpegoptim```\n\n```nix: nix profile install nixpkgs#jpegoptim```"],["SEE ALSO","[jpegtran](/man/jpegtran)(1), [optipng](/man/optipng)(1), [convert](/man/convert)(1), [exiftool](/man/exiftool)(1)"]],"e":["jpegtran","optipng","convert","exiftool"],"x":"jpegoptim [image.jpg]"},"ksvgtopng5":{"n":"ksvgtopng5","t":"converts SVG files to PNG format at a specified resolution","s":[["TAGLINE","converts SVG files to PNG format at a specified resolution"],["TLDR","Convert **SVG to PNG**\n\n```ksvgtopng5 [width] [height] [path/to/file.svg] [output.png]```"],["SYNOPSIS","**ksvgtopng5** _width_ _height_ _input.svg_ _output.png_"],["DESCRIPTION","**ksvgtopng5** converts SVG (Scalable Vector Graphics) files to PNG (Portable Network Graphics) format at a specified resolution. It is part of the KDE CLI tools and uses Qt's SVG rendering."],["PARAMETERS","**width**\n> Output image width in pixels\n\n**height**\n> Output image height in pixels\n\n**input.svg**\n> Path to input SVG file (should be absolute path)\n\n**output.png**\n> Path for output PNG file"],["CAVEATS","The SVG input path should be an absolute path for reliable operation. Complex SVG features may not render identically to web browsers."],["INSTALL","```apt: sudo apt install kde-cli-tools```\n\n```dnf: sudo dnf install kde-cli-tools```\n\n```pacman: sudo pacman -S kde-cli-tools```\n\n```apk: sudo apk add kde-cli-tools```"],["SEE ALSO","[rsvg-convert](/man/rsvg-convert)(1), [inkscape](/man/inkscape)(1), [convert](/man/convert)(1)"]],"e":["rsvg-convert","inkscape","convert"],"x":"ksvgtopng5 [width] [height] [path/to/file.svg] [output.png]"},"kubectl-create":{"n":"kubectl-create","t":"creates Kubernetes resources from files or command-line input","s":[["TAGLINE","creates Kubernetes resources from files or command-line input"],["TLDR","**Create resource from file**\n\n```kubectl create -f [manifest.yaml]```\n\n**Create namespace**\n\n```kubectl create namespace [name]```\n\n**Create secret**\n\n```kubectl create secret generic [name] --from-literal=[key]=[value]```\n\n**Create configmap from file**\n\n```kubectl create configmap [name] --from-file=[path]```\n\n**Create deployment**\n\n```kubectl create deployment [name] --image=[image:tag]```"],["SYNOPSIS","**kubectl** **create** _resource_ [_options_]"],["PARAMETERS","**-f**, **--filename** _file_\n> Create from file or URL.\n\n**namespace** _name_\n> Create a namespace.\n\n**deployment** _name_\n> Create a deployment.\n\n**secret** _type_ _name_\n> Create a secret.\n\n**configmap** _name_\n> Create a configmap.\n\n**--dry-run** _mode_\n> Preview without creating (client, server).\n\n**-o**, **--output** _format_\n> Output format."],["DESCRIPTION","**kubectl create** creates Kubernetes resources from files or command-line input. Unlike apply, create fails if the resource already exists. Supports creating various resource types including deployments, services, secrets, and configmaps."],["INSTALL","```apt: sudo apt install kubectl```\n\n```pacman: sudo pacman -S kubectl```\n\n```apk: sudo apk add kubectl```\n\n```brew: brew install kubectl```\n\n```nix: nix profile install nixpkgs#kubectl```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [kubectl-apply](/man/kubectl-apply)(1)"]],"e":["kubectl","kubectl-apply"],"x":"kubectl create -f [manifest.yaml]"},"licensor":{"n":"licensor","t":"generates license text for open source projects","s":[["TAGLINE","generates license text for open source projects"],["TLDR","**Generate MIT license** with a copyright holder name\n\n```licensor MIT \"[Your Name]\"```\n\n**Generate Apache license** and save to file\n\n```licensor Apache-2.0 > LICENSE```\n\n**Generate GPL license**\n\n```licensor GPL-3.0```\n\n**Generate license** with exception expression\n\n```licensor \"Apache-2.0 WITH LLVM-exception\" --skip-optional```\n\n**List available licenses**\n\n```licensor --licenses```"],["SYNOPSIS","**licensor** _license_ [_name_] [_options_]"],["PARAMETERS","_LICENSE_\n> SPDX license identifier (MIT, Apache-2.0, GPL-3.0, etc.). Supports exception expressions.\n\n_NAME_\n> Copyright holder name (optional, for licenses that include it).\n\n**--licenses**\n> List all available SPDX license identifiers.\n\n**--skip-optional**\n> Omit optional sections from the license text.\n\n**--help**\n> Display help information."],["DESCRIPTION","**licensor** writes license text to stdout given an SPDX license identifier. All licenses are compiled into the binary. If the provided ID isn't found, similar ones will be suggested.\n\nThe tool outputs properly formatted license text with copyright holder and year filled in."],["CAVEATS","Uses SPDX identifiers. Rust-based tool. May need installation via cargo."],["HISTORY","licensor was created by **Raphael Theriault** (raftario) as a **Rust-based** license text generator using SPDX license identifiers."],["INSTALL","```brew: brew install licensor```"],["SEE ALSO","[license](/man/license)(1), [cargo](/man/cargo)(1)"]],"e":["license","cargo"],"x":"licensor MIT \"[Your Name]\""},"linode-cli-domains":{"n":"linode-cli-domains","t":"manages DNS domains on Linode","s":[["TAGLINE","manages DNS domains on Linode"],["TLDR","**List domains**\n\n```linode-cli domains list```\n\n**Create domain**\n\n```linode-cli domains create --domain [example.com] --type master --soa_email [admin@example.com]```\n\n**View domain**\n\n```linode-cli domains view [domain_id]```\n\n**Delete domain**\n\n```linode-cli domains delete [domain_id]```\n\n**List domain records**\n\n```linode-cli domains records-list [domain_id]```"],["SYNOPSIS","**linode-cli domains** _command_ [_options_]"],["PARAMETERS","**list**\n> List all domains.\n\n**create**\n> Create domain.\n\n**view** _id_\n> View domain details.\n\n**delete** _id_\n> Delete domain.\n\n**records-list** _id_\n> List DNS records.\n\n**records-create** _id_\n> Create DNS record.\n\n**--domain** _name_\n> Domain name.\n\n**--type** _type_\n> Domain type (master, slave)."],["DESCRIPTION","**linode-cli domains** manages DNS domains on Linode. Create, update, and delete domains and DNS records. Part of Linode CLI for cloud infrastructure management."],["INSTALL","```brew: brew install linode-cli```\n\n```nix: nix profile install nixpkgs#linode-cli```"],["SEE ALSO","[linode-cli](/man/linode-cli)(1)"]],"e":["linode-cli"],"x":"linode-cli domains list"},"linode-cli-object-storage":{"n":"linode-cli-object-storage","t":"manages Linode Object Storage","s":[["TAGLINE","manages Linode Object Storage"],["TLDR","**List buckets**\n\n```linode-cli object-storage buckets-list```\n\n**Create bucket**\n\n```linode-cli object-storage buckets-create --region [us-east] --label [my-bucket]```\n\n**Delete bucket**\n\n```linode-cli object-storage buckets-delete [cluster] [bucket]```\n\n**List access keys**\n\n```linode-cli object-storage keys-list```\n\n**Create access key**\n\n```linode-cli object-storage keys-create --label [my-key]```"],["SYNOPSIS","**linode-cli object-storage** _command_ [_options_]"],["PARAMETERS","**buckets-list**\n> List all buckets.\n\n**buckets-create**\n> Create bucket.\n\n**buckets-delete** _cluster_ _bucket_\n> Delete bucket.\n\n**keys-list**\n> List access keys.\n\n**keys-create**\n> Create access key.\n\n**--region** _id_\n> Region ID (replaces deprecated --cluster).\n\n**--label** _name_\n> Bucket or key label."],["DESCRIPTION","**linode-cli object-storage** manages Linode Object Storage (S3-compatible). Create buckets, manage access keys, and configure storage. Part of Linode CLI."],["SEE ALSO","[linode-cli](/man/linode-cli)(1), [s3cmd](/man/s3cmd)(1)"]],"e":["linode-cli","s3cmd"],"x":"linode-cli object-storage buckets-list"},"longbridge":{"n":"longbridge","t":"Terminal client for Longbridge market data and trading","s":[["TAGLINE","Terminal client for Longbridge market data and trading"],["TLDR","**Log in** to a Longbridge account (opens a browser for OAuth)\n\n```longbridge auth login```\n\n**Show a real-time quote** for a symbol (CODE.MARKET notation)\n\n```longbridge quote [NVDA.US]```\n\n**Show a candlestick (K-line) chart** for a symbol\n\n```longbridge kline [700.HK]```\n\n**List current positions**\n\n```longbridge positions```\n\n**Show portfolio profit and loss**\n\n```longbridge portfolio```\n\n**Fetch the latest news** for a symbol\n\n```longbridge news [AAPL.US]```\n\n**Output machine-readable JSON** for scripting or piping into jq\n\n```longbridge quote [TSLA.US] --format json```"],["SYNOPSIS","**longbridge** _command_ [_arguments_] [**--format** _json_]"],["DESCRIPTION","**longbridge** is a command-line client for **Longbridge Securities** that exposes the Longbridge OpenAPI from the terminal. It covers real-time and historical market data, fundamentals, account and portfolio management, and order placement through a single binary, with over a hundred subcommands mapping onto the underlying API endpoints.\n\nSecurities are addressed with **CODE.MARKET** notation, for example `TSLA.US` for a US stock, `700.HK` for a Hong Kong listing, or `600519.SH` for a Shanghai A-share. Most commands accept **--format json** so their output can be scripted, piped into tools such as `jq`, or fed to an AI agent; without it, results are printed as human-readable tables and charts.\n\nAuthentication uses OAuth 2.0 through the Longbridge SDK. Running `longbridge auth login` opens a browser to authorize the account, after which the SDK stores and refreshes the token, so individual commands do not need manual credentials."],["COMMANDS","**auth login**\n\n> Authorize a Longbridge account via OAuth and store the session token.\n\n**quote** _symbol_\n\n> Print a real-time quote for the given symbol.\n\n**kline** _symbol_\n\n> Display a candlestick (K-line) chart used for technical analysis.\n\n**positions**\n\n> List the account's current holdings.\n\n**portfolio**\n\n> Show portfolio overview and profit-and-loss metrics.\n\n**news** _symbol_\n\n> Show recent news for a symbol.\n\n**financial-report** _symbol_\n\n> Retrieve a company's financial statements.\n\nAdditional command groups cover depth and filings, options and warrants, IPOs, watchlists, trading, statements, insider trades, recurring investments, short selling, and stock screening."],["PARAMETERS","**--format** _json_\n\n> Emit machine-readable JSON instead of formatted terminal output. Useful for scripting and piping into `jq`."],["CAVEATS","longbridge places real orders and exposes live account data, so trading subcommands move real money and carry the usual market risk. It requires a funded Longbridge account and OpenAPI access, and is only useful in markets and instrument types that Longbridge supports. Quote entitlements depend on the account's market-data subscriptions."],["HISTORY","The tool is developed and open-sourced by **Longbridge**, a financial-technology company, under the **Apache License 2.0**. It is written in **Rust** and pairs a scriptable CLI with an interactive terminal UI, positioning itself as an \"AI-native\" front end to the Longbridge OpenAPI."],["SEE ALSO","[curl](/man/curl)(1), [jq](/man/jq)(1)"],["RESOURCES","```[Source code](https://github.com/longbridge/longbridge-terminal)```\n\n```[Documentation](https://open.longbridge.com/docs/cli)```"]],"e":["curl","jq"],"x":"longbridge auth login"},"lsb_release":{"n":"lsb_release","t":"displays Linux Standard Base and distribution-specific information","s":[["TAGLINE","displays Linux Standard Base and distribution-specific information"],["TLDR","Print **all information**\n\n```lsb_release --all```\n\nPrint **description**\n\n```lsb_release --description```\n\nPrint **OS ID** in short format\n\n```lsb_release -is```\n\nPrint **release and codename**\n\n```lsb_release -rcs```"],["SYNOPSIS","**lsb_release** [_options_]"],["DESCRIPTION","**lsb_release** displays Linux Standard Base (LSB) and distribution-specific information about the installed Linux distribution, including distributor ID, description, release number, and codename."],["PARAMETERS","**-a, --all**\n> Print all available information\n\n**-d, --description**\n> Print distribution description\n\n**-i, --id**\n> Print distributor ID\n\n**-r, --release**\n> Print release number\n\n**-c, --codename**\n> Print codename\n\n**-s, --short**\n> Short output format (omit field names)\n\n**-v, --version**\n> Print LSB version"],["CAVEATS","Not available on all distributions. Some distributions provide this via /etc/os-release file instead."],["INSTALL","```dnf: sudo dnf install lsb_release```\n\n```apk: sudo apk add lsb-release-minimal```"],["SEE ALSO","[hostnamectl](/man/hostnamectl)(1), [uname](/man/uname)(1)"]],"e":["hostnamectl","uname"],"x":"lsb_release --all"},"mole":{"n":"mole","t":"CLI application to easily create SSH tunnels","s":[["TAGLINE","CLI application to easily create SSH tunnels"],["TLDR","**Create a local SSH tunnel**\n\n```mole start local --source :[local_port] --destination [host]:[port] --server [user]@[ssh_server]```\n\n**Create a tunnel with verbose output**\n\n```mole start local --verbose --source :[local_port] --destination [host]:[port] --server [user]@[ssh_server]```\n\n**Create a tunnel using a specific SSH key**\n\n```mole start local --source :[local_port] --destination [host]:[port] --server [user]@[ssh_server] --key [~/.ssh/id_rsa]```\n\n**Save a tunnel as an alias for reuse**\n\n```mole add alias local [name] --source :[local_port] --destination [host]:[port] --server [user]@[ssh_server]```\n\n**Start a saved tunnel alias**\n\n```mole start alias [name]```\n\n**Show all saved aliases**\n\n```mole show alias```"],["SYNOPSIS","**mole** _command_ [_subcommand_] [_options_]"],["PARAMETERS","**--source** _[HOST:]PORT_\n> Local address and port to listen on. Auto-selected if not specified.\n\n**--destination** _HOST:PORT_\n> Remote destination to forward to.\n\n**--server** _USER@HOST[:PORT]_\n> SSH server to tunnel through.\n\n**--key** _path_\n> Path to the SSH private key file.\n\n**--verbose**\n> Enable verbose logging output.\n\n**--detach**\n> Run mole in the background.\n\n**--insecure**\n> Skip SSH host key validation.\n\n**--connection-retries** _N_\n> Number of reconnection attempts if the SSH connection drops.\n\n**--retry-wait** _duration_\n> Time to wait between reconnection attempts."],["DESCRIPTION","**mole** is a CLI application for creating SSH tunnels focused on resiliency and user experience. It forwards local ports to remote addresses through SSH servers, supports multiple tunnels over a single connection, and leverages SSH config file settings.\n\nSubcommands include **start** (start a tunnel or alias), **add** (save a tunnel alias), **delete** (remove an alias), **show** (display alias details), **stop** (stop a running tunnel), and **version**.\n\nMole keeps idle connections alive with synthetic packets and automatically reconnects if the SSH connection drops. Tunnel configurations can be saved as aliases for quick reuse."],["CAVEATS","Requires SSH access to the tunnel server. Authentication uses the system's SSH configuration and keys."],["HISTORY","**mole** was created by **davrodpin** and is written in **Go**."],["INSTALL","```brew: brew install mole```\n\n```nix: nix profile install nixpkgs#mole```"],["SEE ALSO","[ssh](/man/ssh)(1), [autossh](/man/autossh)(1)"]],"e":["ssh","autossh"],"x":"mole start local --source :[local_port] --destination [host]:[port] --server [user]@[ssh_server]"},"nfsstat":{"n":"nfsstat","t":"displays statistics about NFS activity","s":[["TAGLINE","displays statistics about NFS activity"],["TLDR","**Show all NFS statistics**\n\n```nfsstat```\n\n**Show server statistics**\n\n```nfsstat -s```\n\n**Show client statistics**\n\n```nfsstat -c```\n\n**Show specific NFS version**\n\n```nfsstat -3```\n\n**Show RPC statistics**\n\n```nfsstat -r```\n\n**Show mounted filesystems**\n\n```nfsstat -m```\n\n**Zero statistics**\n\n```sudo nfsstat -z```"],["SYNOPSIS","**nfsstat** [_options_]"],["PARAMETERS","**-s**, **--server**\n> Server statistics only.\n\n**-c**, **--client**\n> Client statistics only.\n\n**-n**, **--nfs**\n> NFS statistics only.\n\n**-r**, **--rpc**\n> RPC statistics only.\n\n**-2**, **-3**, **-4**\n> NFS version (v2, v3, v4).\n\n**-m**, **--mounts**\n> Show mount information.\n\n**-z**, **--zero**\n> Zero statistics.\n\n**-l**, **--list**\n> Print statistics in list format.\n\n**-o**, **--since**\n> Show statistics since last reset with nfsstat -z.\n\n**-S**, **--sort** _field_\n> Sort NFS stats by field."],["DESCRIPTION","**nfsstat** displays statistics about NFS (Network File System) activity. It shows RPC and NFS statistics for both client and server operations.\n\nStatistics include operation counts, errors, and cache performance."],["OUTPUT SECTIONS","```\nServer RPC stats - Server RPC activity\nClient RPC stats - Client RPC activity\nServer NFS stats - NFS operations served\nClient NFS stats - NFS operations made\n```"],["EXAMPLE OUTPUT","```\nServer nfs v3:\nnull         getattr      setattr      lookup\n1000         50000        1000         25000\n```"],["CAVEATS","Statistics reset on reboot. Requires NFS kernel modules. May need root for some options."],["HISTORY","nfsstat has been part of NFS tools since the early implementations at **Sun Microsystems** in the 1980s."],["INSTALL","```apt: sudo apt install nfs-common```\n\n```dnf: sudo dnf install nfs-utils```\n\n```pacman: sudo pacman -S nfs-utils```\n\n```apk: sudo apk add nfs-utils```\n\n```nix: nix profile install nixpkgs#nfs-utils```"],["SEE ALSO","[nfsd](/man/nfsd)(8), [showmount](/man/showmount)(8), [rpcinfo](/man/rpcinfo)(8)"]],"e":["nfsd","showmount","rpcinfo"],"x":"nfsstat"},"npm-exec":{"n":"npm-exec","t":"Run a command from a local or remote npm package","s":[["TAGLINE","Run a command from a local or remote npm package"],["TLDR","**Run a local package binary**\n\n```npm exec [command]```\n\n**Run a command with arguments**\n\n```npm exec -- [command] [args]```\n\n**Run a package from the registry**\n\n```npm exec -p [package] -- [command]```\n\n**Run a specific package version**\n\n```npm exec -p [package]@[version] -- [command]```\n\n**Run with multiple packages available**\n\n```npm exec -p [pkg1] -p [pkg2] -- [command]```\n\n**Run a shell command string in the package environment**\n\n```npm exec -c '[shell_command]'```\n\n**Auto-accept installation prompts for remote packages**\n\n```npm exec -y -p [package] -- [command]```"],["SYNOPSIS","**npm exec** [_options_] [-- _command_ [_args_...]]"],["PARAMETERS","**-p**, **--package** _pkg_\n> Package to install (can be specified multiple times).\n\n**-c**, **--call** _cmd_\n> Shell command string to run in the package environment.\n\n**-y**, **--yes**\n> Skip confirmation prompts when downloading remote packages.\n\n**--no**\n> Refuse to install packages not already available locally.\n\n**-w**, **--workspace** _name_\n> Run in the context of the specified workspace.\n\n**--workspaces**\n> Run in the context of all configured workspaces.\n\n**--include-workspace-root**\n> Include the workspace root when using --workspaces."],["DESCRIPTION","**npm exec** runs a command from a local or remote npm package. It is similar to **npx** but integrated directly into the npm CLI. The command can run locally installed packages or temporarily download and execute packages from the npm registry.\n\nThe double dash (**--**) separates npm exec options from the command and its arguments. Without **--**, npm may interpret arguments as its own options. When run without positional arguments or **--call**, it opens an interactive shell with the package environment configured in the PATH.\n\nWhen **--package** is not specified, npm exec will try to determine the executable from the first positional argument, matching it against packages in the local project or the npm registry."],["CAVEATS","Unlike **npx**, npm exec requires **--** before the command when passing arguments. May prompt for confirmation when downloading remote packages unless **-y** or **--no** is specified. The **--call** option runs the command in a shell, so shell syntax like pipes and redirects is supported."],["HISTORY","npm exec was added in **npm 7** as a built-in alternative to npx, providing similar functionality with better npm integration and workspace support."],["INSTALL","```pacman: sudo pacman -S npm```\n\n```apk: sudo apk add npm```\n\n```brew: brew install npm```"],["SEE ALSO","[npm](/man/npm)(1), [npx](/man/npx)(1), [npm-run-script](/man/npm-run-script)(1), [npm-init](/man/npm-init)(1), [npm-install](/man/npm-install)(1)"]],"e":["npm","npx","npm-run-script","npm-init","npm-install"],"x":"npm exec [command]"},"obsidian":{"n":"obsidian","t":"Markdown-based knowledge base and note-taking app","s":[["TAGLINE","Markdown-based knowledge base and note-taking app"],["TLDR","**Launch Obsidian**\n\n```obsidian```\n\n**Open a specific vault** by name\n\n```obsidian \"obsidian://open?vault=[vault_name]\"```\n\n**Open a specific file** in a vault\n\n```obsidian \"obsidian://open?vault=[vault_name]&file=[path/to/note]\"```\n\n**Create a new note** in a vault\n\n```obsidian \"obsidian://new?vault=[vault_name]&name=[note_name]\"```\n\n**Create a new note with content**\n\n```obsidian \"obsidian://new?vault=[vault_name]&name=[note_name]&content=[Hello world]\"```\n\n**Search within a vault**\n\n```obsidian \"obsidian://search?vault=[vault_name]&query=[search_term]\"```"],["SYNOPSIS","**obsidian** [_options_] [_URI_]"],["PARAMETERS","**--version**\n> Print version information and exit.\n\n**--help**\n> Display help information.\n\n**--disable-gpu**\n> Disable GPU hardware acceleration.\n\n**--enable-logging**\n> Enable logging to the console."],["URI ACTIONS","**obsidian://open**\n> Open a vault or file. Parameters: **vault** (name or ID), **file** (path relative to vault root), **path** (absolute filesystem path).\n\n**obsidian://new**\n> Create a new note. Parameters: **vault**, **name** (note name), **content** (note body), **overwrite** (true to overwrite existing), **append** (true to append if exists).\n\n**obsidian://search**\n> Open the search pane with a query. Parameters: **vault**, **query** (search string).\n\n**obsidian://hook-get-address**\n> Retrieve the current note's name and URI for use with link-based automation tools."],["DESCRIPTION","**Obsidian** is a knowledge management and note-taking application that operates on a local folder of plain Markdown files. It emphasizes linking between notes to build a personal knowledge graph, displayed as an interactive graph view. All data is stored locally in plaintext, making notes future-proof and accessible with any text editor.\n\nThe application supports a rich plugin ecosystem, with core plugins for backlinks, tags, templates, daily notes, and canvas boards, as well as a community plugin marketplace. Vaults are the fundamental unit of organization, each corresponding to a directory on the filesystem.\n\nObsidian uses a URI scheme (**obsidian://**) for inter-application communication and automation, allowing external scripts and tools to open vaults, create notes, and perform searches."],["CAVEATS","Obsidian is **proprietary software** with a free tier for personal use; commercial use requires a paid license. The application is built on Electron, resulting in higher memory usage than native alternatives. Community plugins run with full local filesystem access and should be reviewed before installation. Syncing between devices requires either Obsidian Sync (paid) or third-party solutions like Git or Syncthing."],["HISTORY","Obsidian was created by **Shida Li** and **Erica Xu**, who previously worked on Dynalist (an outliner app). The first public release came in **March 2020**. The application quickly gained a large community due to its local-first approach and extensible plugin system. Obsidian **1.0** was released in **October 2022**, adding significant features including the Canvas view. A mobile version for iOS and Android was released in **July 2021**."],["INSTALL","```pacman: sudo pacman -S obsidian```\n\n```nix: nix profile install nixpkgs#obsidian```"],["SEE ALSO","[vim](/man/vim)(1), [joplin](/man/joplin)(1)"]],"e":["vim","joplin"],"x":"obsidian"},"oniux":{"n":"oniux","t":"experimental tool from the Tor Project that isolates arbitrary applications","s":[["TAGLINE","experimental tool from the Tor Project that isolates arbitrary applications"],["TLDR","**Isolate** an application and route traffic over Tor\n\n```oniux [command]```\n\n**Query** a website through Tor\n\n```oniux curl [https://example.com]```\n\nQuery an **onion site**\n\n```oniux curl [http://example.onion]```\n\nRun an entire **shell** in torified isolation\n\n```oniux bash```\n\nIsolate **graphical applications**\n\n```oniux hexchat```"],["SYNOPSIS","**oniux** [_options_] _command_ [_args_...]"],["DESCRIPTION","**oniux** is an experimental tool from the Tor Project that isolates arbitrary applications and routes their network traffic through the Tor network. It uses Linux namespaces to create network isolation, ensuring that applications cannot leak traffic outside of Tor.\n\nUnlike traditional torification methods (like torsocks), oniux provides stronger isolation by placing applications in a separate network namespace where only Tor traffic is permitted. This makes it suitable for applications that might otherwise bypass SOCKS proxy settings.\n\nThe tool can handle both regular websites and .onion hidden services, making it useful for privacy-focused workflows and accessing Tor-only resources."],["CAVEATS","This is **experimental software** from the Tor Project and may have bugs or security issues. Network namespace isolation requires appropriate Linux kernel support and privileges. Some applications may not function correctly when isolated, particularly those that require specific network configurations or local service access. Does not anonymize application-layer data leaks (hostname, username, etc.)."],["HISTORY","Developed by the **Tor Project** as part of their efforts to improve application-level Tor integration on Linux. Uses Linux namespace technology (similar to containers) to provide stronger isolation guarantees than traditional LD_PRELOAD-based torification approaches like torsocks."],["INSTALL","```nix: nix profile install nixpkgs#oniux```"],["SEE ALSO","[tor](/man/tor)(1), [torsocks](/man/torsocks)(1), [torify](/man/torify)(1)"]],"e":["tor","torsocks","torify"],"x":"oniux [command]"},"pamtoxvmini":{"n":"pamtoxvmini","t":"converts PAM/PNM images to XV visual schnauzer thumbnail format","s":[["TAGLINE","converts PAM/PNM images to XV visual schnauzer thumbnail format"],["TLDR","**Convert PAM to XV thumbnail**\n\n```pamtoxvmini [input.pam] > [output.xv]```"],["SYNOPSIS","**pamtoxvmini** [_file_]"],["DESCRIPTION","**pamtoxvmini** converts PAM/PNM images to XV visual schnauzer thumbnail format. XV was a popular Unix image viewer in the 1990s. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[xvminitoppm](/man/xvminitoppm)(1), [pnmscale](/man/pnmscale)(1)"]],"e":["xvminitoppm","pnmscale"],"x":"pamtoxvmini [input.pam] > [output.xv]"},"pbmtext":{"n":"pbmtext","t":"renders text as a PBM bitmap image","s":[["TAGLINE","renders text as a PBM bitmap image"],["TLDR","**Create text image**\n\n```pbmtext \"[Hello World]\" > [output.pbm]```\n\n**Create with specific font**\n\n```pbmtext -font [fixed] \"[Hello]\" > [output.pbm]```\n\n**Create with built-in font**\n\n```pbmtext -builtin [fixed] \"[Hello]\" > [output.pbm]```"],["SYNOPSIS","**pbmtext** [_options_] [_text_]"],["PARAMETERS","**-font** _file_\n> BDF font file.\n\n**-builtin** _name_\n> Built-in font (fixed, bdf).\n\n**-space** _pixels_\n> Space between characters.\n\n**-lspace** _pixels_\n> Line spacing.\n\n**-nomargins**\n> No margins around text."],["DESCRIPTION","**pbmtext** renders text as a PBM bitmap image. Part of Netpbm toolkit for creating text graphics. Supports BDF fonts and built-in fonts."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pbmtextps](/man/pbmtextps)(1), [ppmlabel](/man/ppmlabel)(1)"]],"e":["pbmtextps","ppmlabel"],"x":"pbmtext \"[Hello World]\" > [output.pbm]"},"pcap":{"n":"pcap","t":"file format used by libpcap for storing network packet data","s":[["TAGLINE","file format used by libpcap for storing network packet data"],["TLDR","**Read pcap with tcpdump**\n\n```tcpdump -r [capture.pcap]```\n\n**Read with tshark**\n\n```tshark -r [capture.pcap]```\n\n**Write pcap**\n\n```tcpdump -i [eth0] -w [capture.pcap]```\n\n**Filter and read**\n\n```tcpdump -r [capture.pcap] 'port 80'```\n\n**Convert with editcap**\n\n```editcap [input.pcap] [output.pcapng]```"],["SYNOPSIS","**pcap** - packet capture file format"],["DESCRIPTION","**pcap** (Packet Capture) is the file format used by libpcap for storing network packet data. It's the standard format for network captures, used by tcpdump, Wireshark, and many security tools."],["FILE STRUCTURE","```\nGlobal Header (24 bytes)\n├── Magic Number (0xa1b2c3d4)\n├── Version\n├── Timestamp offset\n├── Snap length\n└── Link type\n\nPacket Records\n├── Timestamp\n├── Captured length\n├── Original length\n└── Packet data\n```"],["TOOLS","```\ntcpdump    - CLI capture/read\nwireshark  - GUI analysis\ntshark     - CLI analysis\neditcap    - Edit captures\nmergecap   - Merge captures\n```"],["CAVEATS","pcap is binary format. pcapng offers more features. Large captures need significant storage."],["HISTORY","The pcap format and libpcap were developed at **Lawrence Berkeley Laboratory** by **Van Jacobson**, **Craig Leres**, and **Steven McCanne**."],["SEE ALSO","[tcpdump](/man/tcpdump)(1), [wireshark](/man/wireshark)(1), [tshark](/man/tshark)(1), [editcap](/man/editcap)(1)"]],"e":["tcpdump","wireshark","tshark","editcap"],"x":"tcpdump -r [capture.pcap]"},"pdsh":{"n":"pdsh","t":"Parallel distributed shell — run commands on many hosts at once","s":[["TAGLINE","Parallel distributed shell — run commands on many hosts at once"],["TLDR","**Run a command** on a list of hosts\n\n```pdsh -w [host1,host2,host3] [command]```\n\n**Use a host range** (compressed numeric ranges)\n\n```pdsh -w [host[1-10]] [command]```\n\n**Force the SSH** remote command module\n\n```pdsh -R ssh -w [hosts] [command]```\n\n**Read target hosts from a file**\n\n```pdsh -w ^[hosts.txt] [command]```\n\n**Limit the fanout** (concurrent connections)\n\n```pdsh -f [16] -w [hosts] [command]```\n\n**Pipe output through dshbak** to group results per host\n\n```pdsh -w [hosts] [command] | dshbak -c```"],["SYNOPSIS","**pdsh** [_options_] [_command_]"],["PARAMETERS","**-w** _HOSTS_\n> Target host list. Accepts comma-separated names, compressed ranges (host[1-10]), or `^file` to read from a file.\n\n**-x** _HOSTS_\n> Exclude these hosts from the target list.\n\n**-R** _MODULE_\n> Remote command module: `ssh`, `rsh`, `mrsh`, `exec`, `krb4`, etc. Default depends on build.\n\n**-l** _USER_\n> Remote username to log in as.\n\n**-t** _SECONDS_\n> Connect timeout per host. Default: 10.\n\n**-u** _SECONDS_\n> Per-host command timeout (kills slow commands).\n\n**-f** _NUMBER_\n> Maximum number of concurrent (fanout) connections. Default: 32.\n\n**-N**\n> Suppress the \"hostname:\" prefix on each line of output.\n\n**-S**\n> Return the largest remote exit status as the pdsh exit code.\n\n**-g** _GROUP_\n> Run on a host group defined in dshgroup or genders.\n\n**-a**\n> Target all hosts (requires a working host module like genders or machines).\n\n**-q**\n> Display the list of targeted hosts and exit without running."],["DESCRIPTION","**pdsh** is a high-performance, parallel remote shell utility. It launches the requested command on many hosts in parallel and merges their stdout/stderr back to the user, prefixed with each host's name.\n\nIt was developed at LLNL for cluster administration and supports multiple \"remote command modules\" (ssh, rsh, mrsh, etc.) selected at build time or via **-R**. Targets can be supplied as explicit lists, compressed numeric ranges, files, dshgroups, or via genders/SLURM modules.\n\nThe companion utilities **pdcp** (parallel copy), **rpdcp** (reverse parallel copy), and **dshbak** (group identical output across hosts) round out a small toolkit aimed at scripted cluster operations."],["CAVEATS","Requires the chosen remote shell (typically SSH with key-based auth) to work non-interactively on every target. Output from many hosts is interleaved — pipe through **dshbak** to group it. Quoting can be tricky because the shell on both ends interprets the command string."],["HISTORY","**pdsh** originated at **Lawrence Livermore National Laboratory** in the late 1990s as a successor to IBM's DSH for managing large Linux clusters. It is maintained on GitHub and remains widely used in HPC administration."],["INSTALL","```apt: sudo apt install pdsh```\n\n```dnf: sudo dnf install pdsh```\n\n```apk: sudo apk add pdsh```\n\n```zypper: sudo zypper install pdsh```\n\n```brew: brew install pdsh```\n\n```nix: nix profile install nixpkgs#pdsh```"],["SEE ALSO","[ssh](/man/ssh)(1), [pssh](/man/pssh)(1), [ansible](/man/ansible)(1), [rsync](/man/rsync)(1)"]],"e":["ssh","pssh","ansible","rsync"],"x":"pdsh -w [host1,host2,host3] [command]"},"pint":{"n":"pint","t":"validates Prometheus alerting and recording rules","s":[["TAGLINE","validates Prometheus alerting and recording rules"],["TLDR","**Lint Prometheus rules**\n\n```pint lint [rules.yaml]```\n\n**Check rules against live Prometheus**\n\n```pint -c [config.yaml] lint [rules.yaml]```\n\n**Watch for changes**\n\n```pint watch [rules/]```\n\n**Show rule problems**\n\n```pint lint --output=json [rules.yaml]```\n\n**CI mode**\n\n```pint ci```\n\n**List available checks**\n\n```pint checks```"],["SYNOPSIS","**pint** [_-c config_] _command_ [_options_] [_files_]"],["PARAMETERS","**lint**\n> Check rules for problems.\n\n**watch**\n> Watch and lint continuously.\n\n**ci**\n> CI/CD mode.\n\n**checks**\n> List available checks.\n\n**-c**, **--config** _FILE_\n> Configuration file.\n\n**--output** _FORMAT_\n> Output format.\n\n**--no-color**\n> Disable colors.\n\n**-v**, **--verbose**\n> Verbose output."],["CHECKS","**promql/syntax** - Query syntax\n**promql/series** - Series existence\n**alerts/for** - Alert duration\n**alerts/template** - Template errors\n**rule/duplicate** - Duplicate rules"],["DESCRIPTION","**pint** validates Prometheus alerting and recording rules, catching errors before deployment. It goes beyond basic syntax checking by querying a live Prometheus server to verify that referenced metrics actually exist and that selectors return data.\n\nKey capabilities include: PromQL syntax validation, series existence checks against live Prometheus, alert template validation (annotation and label rendering), duplicate rule detection, and cost estimation for expensive queries.\n\nThe **ci** mode is designed for pull request workflows — it only reports problems in changed files, making it practical for large rule repositories. The **watch** mode continuously monitors rule files and re-checks on changes."],["CAVEATS","Live checks require network access to a Prometheus server. Some checks may produce false positives for metrics that are intermittently scraped. Configuration is required to connect pint to Prometheus instances."],["HISTORY","**pint** was created by **Cloudflare** for validating their Prometheus rules at scale. It provides deeper analysis than Prometheus's built-in rule checking."],["INSTALL","```pacman: sudo pacman -S pint```\n\n```brew: brew install pint```"],["SEE ALSO","[promtool](/man/promtool)(1), [prometheus](/man/prometheus)(1), [amtool](/man/amtool)(1)"]],"e":["promtool","prometheus","amtool"],"x":"pint lint [rules.yaml]"},"ppmlabel":{"n":"ppmlabel","t":"Add text labels to PPM images","s":[["TAGLINE","Add text labels to PPM images"],["TLDR","**Add text label to image**\n\n```ppmlabel -text \"[Hello World]\" [input.ppm] > [output.ppm]```\n\n**Add label at specific position**\n\n```ppmlabel -x [10] -y [20] -text \"[Label]\" [input.ppm] > [output.ppm]```\n\n**Add label with custom color and size**\n\n```ppmlabel -color [white] -size [24] -text \"[Label]\" [input.ppm] > [output.ppm]```\n\n**Add rotated label**\n\n```ppmlabel -angle [45] -text \"[Watermark]\" [input.ppm] > [output.ppm]```\n\n**Add label with background highlight**\n\n```ppmlabel -background [black] -color [yellow] -text \"[Warning]\" [input.ppm] > [output.ppm]```\n\n**Add multiple labels** using a script file\n\n```ppmlabel -file [labels.txt] [input.ppm] > [output.ppm]```"],["SYNOPSIS","**ppmlabel** [_options_] [_file_]"],["PARAMETERS","**-text** _string_\n> Text string to render on the image.\n\n**-file** _filename_\n> Read label commands from a file, one per line.\n\n**-x** _pixels_\n> Horizontal position of the label from the left edge.\n\n**-y** _pixels_\n> Vertical position of the label baseline from the top edge.\n\n**-color** _color_\n> Text color (Netpbm color name or #rrggbb hex value).\n\n**-background** _color_\n> Background color behind the text. Default is transparent (no background).\n\n**-angle** _degrees_\n> Rotation angle in degrees counterclockwise. Default is 0.\n\n**-size** _pixels_\n> Font height in pixels. Default depends on the built-in font."],["DESCRIPTION","**ppmlabel** adds text labels to PPM (Portable Pixmap) images. It reads a PPM image, renders one or more text strings at specified positions with optional color, size, and rotation, and writes the result as a new PPM image. Part of the Netpbm toolkit.\n\nMultiple labels can be placed on a single image by specifying multiple **-text** options or by using the **-file** option to read label specifications from a file. Each label can have independent position, color, size, and angle settings."],["CAVEATS","Uses a built-in bitmap font; does not support TrueType or external fonts. For more advanced text rendering, consider **pbmtext** piped through **pnmcomp**. Input and output are PPM format only."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pbmtext](/man/pbmtext)(1), [pnmcomp](/man/pnmcomp)(1), [ppmdraw](/man/ppmdraw)(1), [ppmchange](/man/ppmchange)(1)"]],"e":["pbmtext","pnmcomp","ppmdraw","ppmchange"],"x":"ppmlabel -text \"[Hello World]\" [input.ppm] > [output.ppm]"},"pppoe-sniff":{"n":"pppoe-sniff","t":"Capture PPPoE packets for debugging","s":[["TAGLINE","Capture PPPoE packets for debugging"],["TLDR","**Sniff PPPoE traffic**\n\n```pppoe-sniff -I [eth0]```\n\n**Verbose output**\n\n```pppoe-sniff -I [eth0] -V```"],["SYNOPSIS","**pppoe-sniff** [_options_]"],["PARAMETERS","**-I** _INTERFACE_\n> Network interface.\n\n**-V**\n> Verbose output."],["DESCRIPTION","**pppoe-sniff** captures and displays PPPoE discovery and session packets on a network interface for diagnostic purposes. It shows PADI, PADO, PADR, PADS, and PADT packets exchanged between PPPoE clients and access concentrators.\n\nThis tool is useful for troubleshooting PPPoE connection failures by verifying that discovery packets are being sent and responses received. The verbose mode provides additional detail about packet contents."],["CAVEATS","Requires root privileges. Debugging tool only."],["HISTORY","pppoe-sniff provides **PPPoE traffic analysis** for troubleshooting."],["INSTALL","```apt: sudo apt install pppoe```\n\n```dnf: sudo dnf install rp-pppoe```\n\n```pacman: sudo pacman -S rp-pppoe```\n\n```apk: sudo apk add rp-pppoe```\n\n```zypper: sudo zypper install rp-pppoe```\n\n```nix: nix profile install nixpkgs#rp-pppoe```"],["SEE ALSO","[pppoe](/man/pppoe)(8), [pppoe-discovery](/man/pppoe-discovery)(8), [tcpdump](/man/tcpdump)(1)"]],"e":["pppoe","pppoe-discovery","tcpdump"],"x":"pppoe-sniff -I [eth0]"},"proton":{"n":"proton","t":"Valve's Windows game compatibility layer for Linux","s":[["TAGLINE","Valve's Windows game compatibility layer for Linux"],["TLDR","**Run a Windows executable via Proton**\n\n```proton run [game.exe]```\n\n**Run using a specific Proton version directly**\n\n```~/.steam/steam/steamapps/common/Proton\\ [7.0]/proton run [game.exe]```\n\n**Enable Proton log to $HOME/steam-$APPID.log**\n\n```PROTON_LOG=1 proton run [game.exe]```\n\n**Force OpenGL instead of Vulkan for DirectX translation**\n\n```PROTON_USE_WINED3D=1 proton run [game.exe]```\n\n**Disable esync and fsync (for kernels that do not support them)**\n\n```PROTON_NO_ESYNC=1 PROTON_NO_FSYNC=1 proton run [game.exe]```\n\n**Wait for a previous process to exit, then run**\n\n```proton waitforexitandrun [game.exe]```"],["SYNOPSIS","**proton** _verb_ [_arguments_...]"],["VERBS","**run**\n> Run a Windows executable under Proton.\n\n**waitforexitandrun**\n> Wait for any lingering Wine server to exit, then run the executable. Used by Steam when switching prefixes.\n\n**runinprefix**\n> Run a program inside the currently configured Proton prefix without spawning a new one.\n\n**destroyprefix**\n> Delete the current Proton prefix, forcing Steam to recreate it.\n\n**getcompatpath**\n> Print the pfx compatdata path for the game.\n\n**getnativepath**\n> Print the native (Linux) path that corresponds to a Windows path."],["ENVIRONMENT VARIABLES","**STEAM_COMPAT_CLIENT_INSTALL_PATH**\n> Required. Path to the Steam client install (usually **~/.steam/steam**).\n\n**STEAM_COMPAT_DATA_PATH**\n> Required. Directory where Proton stores the pfx, configuration, and caches.\n\n**PROTON_LOG**\n> Enable user-friendly logging to **$HOME/steam-$APPID.log**.\n\n**PROTON_USE_WINED3D**\n> Use WineD3D (OpenGL) instead of DXVK/VKD3D for DirectX.\n\n**PROTON_NO_ESYNC**, **PROTON_NO_FSYNC**\n> Disable the esync or fsync synchronization primitives.\n\n**PROTON_ENABLE_NVAPI**\n> Enable NVIDIA NVAPI so DLSS and Reflex work with supported GPUs.\n\n**PROTON_FORCE_LARGE_ADDRESS_AWARE**\n> Mark 32-bit executables as large-address-aware.\n\n**PROTON_DUMP_DEBUG_COMMANDS**\n> Dump helper scripts for re-running the game with winedbg, strace, etc."],["DESCRIPTION","**Proton** is Valve's compatibility layer for running Windows games on Linux through the Steam client. It bundles a patched **Wine**, the **DXVK** Direct3D 9/10/11 → Vulkan translator, **VKD3D-Proton** for Direct3D 12, **FAudio** for XAudio2, and various runtime libraries. Steam normally invokes proton automatically via the \"Steam Play\" setting; direct invocation is rare and primarily useful for troubleshooting.\n\nEach game has its own Wine prefix under **$STEAM_COMPAT_DATA_PATH/pfx**. The **proton** script can be called directly with a verb such as **run** once the required environment variables are set."],["CAVEATS","Running proton outside Steam requires setting **STEAM_COMPAT_CLIENT_INSTALL_PATH** and **STEAM_COMPAT_DATA_PATH**. Not all Windows software runs successfully — consult ProtonDB for compatibility reports. Games with kernel-level anti-cheat frequently refuse to run under Proton."],["HISTORY","Proton was announced by **Valve** in **August 2018** as part of the Steam Play initiative, in collaboration with **CodeWeavers** and the DXVK author **Philip Rebohle**. Proton Experimental and Proton-GE (community fork) continue to receive frequent updates."],["SEE ALSO","[wine](/man/wine)(1), [steam](/man/steam)(1), [winetricks](/man/winetricks)(1)"]],"e":["wine","steam","winetricks"],"x":"proton run [game.exe]"},"rawtopgm":{"n":"rawtopgm","t":"Convert raw grayscale data to PGM format","s":[["TAGLINE","Convert raw grayscale data to PGM format"],["TLDR","**Convert raw grayscale to PGM**\n\n```rawtopgm [width] [height] [input.raw] > [output.pgm]```\n\n**Convert with header bytes to skip**\n\n```rawtopgm -headerskip [512] [width] [height] [input.raw] > [output.pgm]```"],["SYNOPSIS","**rawtopgm** [_options_] _width_ _height_ [_file_]"],["PARAMETERS","_width_\n> Image width in pixels.\n\n_height_\n> Image height in pixels.\n\n**-headerskip** _BYTES_\n> Skip the first _BYTES_ bytes of the input before reading pixel data (for files with proprietary headers).\n\n**-rowskip** _BYTES_\n> Skip _BYTES_ at the end of each scanline (e.g., padding to a row stride).\n\n**-bpp** _N_\n> Bytes per pixel: _1_ (default, 8-bit) or _2_ (16-bit big-endian samples).\n\n**-littleendian**\n> When **-bpp 2**, interpret 16-bit samples as little-endian.\n\n**-maxval** _N_\n> Override the maxval written to the output PGM (default _255_, or _65535_ when **-bpp 2**).\n\n**-topbottom**\n> Treat row 0 as the top of the image (default; pair with **-bottomtop** to flip).\n\n**-bottomtop**\n> Treat row 0 as the bottom of the image."],["DESCRIPTION","**rawtopgm** converts raw grayscale byte data into Portable Graymap (PGM) format by interpreting a stream of pixel values according to specified image dimensions. Since raw data contains no metadata, the width and height must be provided as arguments so the tool knows how to arrange the bytes into a proper image grid.\n\nThe **-headerskip** option allows skipping file headers that precede the actual pixel data, and **-bpp** handles 16-bit samples for higher dynamic range images. It is part of the Netpbm image processing toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[rawtoppm](/man/rawtoppm)(1), [pgmtopbm](/man/pgmtopbm)(1)"]],"e":["rawtoppm","pgmtopbm"],"x":"rawtopgm [width] [height] [input.raw] > [output.pgm]"},"reflex":{"n":"reflex","t":"Run commands when files change","s":[["TAGLINE","Run commands when files change"],["TLDR","**Watch and run command**\n\n```reflex [command]```\n\n**Watch specific pattern**\n\n```reflex -r '\\.go$' [go build]```\n\n**Watch directory**\n\n```reflex -d [src/] [command]```\n\n**Multiple patterns**\n\n```reflex -r '\\.go$' -r '\\.html$' [command]```\n\n**Start without initial run**\n\n```reflex -s [command]```\n\n**Decoration mode**\n\n```reflex -d [.] -- [sh -c \"echo changed\"]```\n\n**Exclude pattern**\n\n```reflex -r '\\.go$' -R 'vendor/' [command]```"],["SYNOPSIS","**reflex** [_-r regex_] [_-d dir_] [_-R exclude_] [_options_] _command_"],["PARAMETERS","**-r** _REGEX_\n> Include pattern.\n\n**-R** _REGEX_\n> Exclude pattern.\n\n**-d** _DIR_\n> Watch directory.\n\n**-s**\n> Start service (no initial run).\n\n**-g** _GLOB_\n> Glob pattern.\n\n**-c** _CONFIG_\n> Config file.\n\n**--all**\n> Match all files.\n\n**--decoration** _MODE_\n> Output decoration."],["DESCRIPTION","**reflex** watches files and runs commands on changes. It's a flexible file watcher for development.\n\nRegex patterns filter which files trigger. Multiple patterns combine for complex matching.\n\nService mode runs a long-running process. The process restarts on changes.\n\nExclusions ignore vendor directories or build output. Prevents unnecessary rebuilds.\n\nConfiguration files define complex watch rules. Multiple watchers in one file."],["CONFIGURATION","**.reflex**\n> Project-level configuration file defining watch rules, one per line, with regex patterns and commands. Loaded automatically when present in the current directory."],["CAVEATS","Polling fallback on some filesystems. Rapid changes may batch. Initial run may not be wanted."],["HISTORY","**Reflex** was created as a simple, flexible file watcher. It focuses on development workflow automation with minimal configuration."],["INSTALL","```apt: sudo apt install reflex```\n\n```apk: sudo apk add reflex```\n\n```brew: brew install reflex```\n\n```nix: nix profile install nixpkgs#reflex```"],["SEE ALSO","[entr](/man/entr)(1), [watchman](/man/watchman)(1), [fswatch](/man/fswatch)(1), [inotifywait](/man/inotifywait)(1)"]],"e":["entr","watchman","fswatch","inotifywait"],"x":"reflex [command]"},"rpmsign":{"n":"rpmsign","t":"Sign RPM packages with GPG keys","s":[["TAGLINE","Sign RPM packages with GPG keys"],["TLDR","**Sign RPM package**\n\n```rpmsign --addsign [package.rpm]```\n\n**Sign with specific key**\n\n```rpmsign --addsign --key-id [KEYID] [package.rpm]```\n\n**Re-sign package**\n\n```rpmsign --resign [package.rpm]```\n\n**Delete signature**\n\n```rpmsign --delsign [package.rpm]```\n\n**Sign multiple packages**\n\n```rpmsign --addsign [*.rpm]```"],["SYNOPSIS","**rpmsign** **--addsign**|**--resign** [_options_] _PACKAGE_FILE_...\n\n**rpmsign** **--delsign** _PACKAGE_FILE_...\n\n**rpmsign** **--delfilesign** _PACKAGE_FILE_..."],["PARAMETERS","**--addsign**\n> Add signature to package.\n\n**--resign**\n> Replace existing signature.\n\n**--delsign**\n> Delete all OpenPGP signatures.\n\n**--delfilesign**\n> Delete all IMA and fsverity file signatures.\n\n**--key-id** _KEYID_\n> GPG key ID, overriding %_openpgp_sign_id configuration.\n\n**--signfiles**\n> Sign package files using the configured digest algorithm and RSA key.\n\n**--fskpath** _KEY_\n> File signing key path, used with --signfiles.\n\n**--signverity**\n> Sign package files with fsverity signatures.\n\n**--certpath** _CERT_\n> Certificate for use with --signverity.\n\n**--rpmv3**\n> Add RPM V3 header+payload signature on V4 packages for compatibility with rpm < 4.14.\n\n**--rpmv4**\n> Add RPM V4 header signature on V6 packages for rpm 4.x compatibility.\n\n**-D** _\"MACRO EXPR\"_\n> Define RPM macro."],["DESCRIPTION","**rpmsign** adds or manages OpenPGP signatures on RPM packages. Signing packages allows verification of authenticity and integrity. It supports both traditional package signing and file-level IMA/fsverity signing.\n\nPart of the RPM package manager. The signing key is configured via the **%_openpgp_sign_id** macro (or legacy **%_gpg_name**)."],["EXAMPLES","```bash"],["Sign package","rpmsign --addsign mypackage-1.0-1.x86_64.rpm"],["Sign with specific key","rpmsign --addsign --key-id ABCD1234 package.rpm"],["Re-sign (replace signature)","rpmsign --resign package.rpm"],["Sign all RPMs","rpmsign --addsign *.rpm"],["Define signing identity via macro","rpmsign -D \"_gpg_name Your Name\" --addsign package.rpm"],["Delete file signatures","rpmsign --delfilesign package.rpm"],["Verify signature","rpm -K package.rpm\n```"],["CONFIGURATION","**~/.rpmmacros**\n> User-level RPM macro file where **%_openpgp_sign_id** (or legacy **%_gpg_name**) sets the default signing identity and **%_gpg_path** specifies the GnuPG keyring directory.\n\n**/etc/rpm/macros**\n> System-wide RPM macro overrides for signing defaults shared across all users."],["CAVEATS","Requires GPG key. Passphrase needed (or gpg-agent). Signature added to header."],["HISTORY","rpmsign is part of **RPM** package manager, originally developed at **Red Hat** for package authentication."],["INSTALL","```apt: sudo apt install rpm```\n\n```dnf: sudo dnf install rpm```\n\n```apk: sudo apk add rpm```\n\n```zypper: sudo zypper install rpm```\n\n```brew: brew install rpm```\n\n```nix: nix profile install nixpkgs#rpm```"],["SEE ALSO","[rpm](/man/rpm)(8), [rpmbuild](/man/rpmbuild)(8), [rpmkeys](/man/rpmkeys)(8), [gpg](/man/gpg)(1)"]],"e":["rpm","rpmbuild","rpmkeys","gpg"],"x":"rpmsign --addsign [package.rpm]"},"runal":{"n":"runal","t":"Creative coding environment for the terminal","s":[["TAGLINE","Creative coding environment for the terminal"],["TLDR","**Run a creative coding sketch**\n\n```runal [sketch.js]```\n\n**Export animation as a GIF**\n\n```runal --export gif [sketch.js]```\n\n**Run with a custom canvas size**\n\n```runal --width [80] --height [40] [sketch.js]```"],["SYNOPSIS","**runal** [_options_] _script_"],["PARAMETERS","**--export** _FORMAT_\n> Export output to a file. Supported formats: png, gif, mp4.\n\n**--width** _COLUMNS_\n> Set canvas width in columns.\n\n**--height** _ROWS_\n> Set canvas height in rows."],["DESCRIPTION","**runal** is a text-based creative coding environment similar to Processing or p5.js, but rendering everything as text in the terminal. It can be programmed with JavaScript or used as a Go package. It supports 2D shapes, trigonometry, colors, and can export to PNG, GIF, or MP4."],["HISTORY","**runal** was created by **Xavier Godart** (emprcl) and is written in **Go**."],["INSTALL","```aur: yay -S runal```"],["SEE ALSO","[figlet](/man/figlet)(1), [terminaltexteffects](/man/terminaltexteffects)(1), [t-rec](/man/t-rec)(1)"]],"e":["figlet","terminaltexteffects","t-rec"],"x":"runal [sketch.js]"},"salt-master":{"n":"salt-master","t":"Salt central management daemon","s":[["TAGLINE","Salt central management daemon"],["TLDR","**Start the Salt master daemon** in the foreground\n\n```salt-master```\n\n**Start the Salt master** in the background (daemonized)\n\n```salt-master -d```\n\n**Start with a specific configuration directory**\n\n```salt-master -c [/etc/salt]```\n\n**Start with debug logging** to console\n\n```salt-master -l debug```\n\n**Start with a specific log file**\n\n```salt-master --log-file=[/var/log/salt/master]```"],["SYNOPSIS","**salt-master** [_options_]"],["PARAMETERS","**-d**, **--daemon**\n> Run the Salt master as a daemon in the background\n\n**-c** _DIR_, **--config-dir**=_DIR_\n> Specify the configuration directory (default: /etc/salt)\n\n**-l** _LEVEL_, **--log-level**=_LEVEL_\n> Console log level: info, warning, error, debug, trace, garbage, none\n\n**--log-file**=_FILE_\n> Specify the log file path\n\n**--log-file-level**=_LEVEL_\n> Log file logging level\n\n**--pidfile**=_FILE_\n> Specify the location of the pidfile\n\n**-u** _USER_, **--user**=_USER_\n> Specify user to run salt-master\n\n**-h**, **--help**\n> Print help message and exit\n\n**--version**\n> Print version information"],["DESCRIPTION","**salt-master** is the central control daemon for SaltStack infrastructure. It manages minion keys, distributes configurations, executes remote commands, and serves files to connected minions.\n\nThe master listens on two ports: 4505 for publishing commands to minions via ZeroMQ, and 4506 for receiving returns and file server requests. Configuration is stored in **/etc/salt/master**.\n\nKey components include the job cache for tracking command execution, the file server for distributing states and files, and the PKI infrastructure for secure minion authentication. The master supports worker threads for handling large numbers of minions."],["CONFIGURATION","**/etc/salt/master**\n> Main master configuration file controlling worker threads, file server roots, pillar data, and network settings.\n\n**/etc/salt/master.d/**\n> Directory for additional configuration files that are merged with the main master config.\n\n**/srv/salt/**\n> Default file server root directory for Salt states, formulas, and files served to minions.\n\n**/srv/pillar/**\n> Default directory for pillar data providing secure per-minion configuration variables."],["CAVEATS","Firewall rules must allow ports 4505 and 4506 for master-minion communication. Large deployments may require tuning worker_threads in the configuration. The master must be running before minions can connect and have their keys accepted."],["HISTORY","salt-master is part of **SaltStack**, created by Thomas Hatch in **2011**. Originally developed at a startup, SaltStack grew into a major configuration management platform. VMware acquired SaltStack in **2020**, and the project continues as the open-source Salt Project."],["INSTALL","```dnf: sudo dnf install salt-master```\n\n```apk: sudo apk add salt-lts-master```\n\n```zypper: sudo zypper install salt-master```"],["SEE ALSO","[salt](/man/salt)(1), [salt-minion](/man/salt-minion)(8), [salt-key](/man/salt-key)(1), [salt-call](/man/salt-call)(1)"]],"e":["salt","salt-minion","salt-key","salt-call"],"x":"salt-master"},"sbigtopgm":{"n":"sbigtopgm","t":"convert an SBIG astronomical CCD image to PGM","s":[["TAGLINE","convert an SBIG astronomical CCD image to PGM"],["TLDR","**Convert an SBIG image** to PGM\n\n```sbigtopgm [image.sbig] > [image.pgm]```\n\n**Read from stdin** in a pipeline\n\n```cat [image.sbig] | sbigtopgm > [image.pgm]```\n\n**Convert and stretch contrast** with pnmnorm\n\n```sbigtopgm [image.sbig] | pnmnorm > [stretched.pgm]```\n\n**Convert and encode** as PNG\n\n```sbigtopgm [image.sbig] | pnmtopng > [image.png]```"],["SYNOPSIS","**sbigtopgm** [_sbigfile_]"],["DESCRIPTION","**sbigtopgm** reads an astronomical CCD image in the native SBIG (Santa Barbara Instrument Group) Type 3 file format and writes the pixel values out as a PGM (portable graymap) image on standard output. When no filename is given, the program reads from standard input.\n\nThe SBIG Type 3 format is produced by the CCDOPS software bundled with SBIG ST-series CCD cameras. It consists of an ASCII header (with flexible capitalization and tolerant line endings) followed by a 16-bit grayscale pixel array. **sbigtopgm** preserves the full 16-bit precision of the source, so the output PGM has a maxval matching the original ADU range. Part of the **Netpbm** toolkit."],["CAVEATS","There is no **pgmtosbig** going the other way in upstream Netpbm. The output is always 16-bit even when the camera reports less precision. Astronomy-specific FITS files should be converted with **fitstopnm** instead."],["HISTORY","**sbigtopgm** was contributed to **Netpbm** to support amateur and professional astronomy workflows on Unix systems where SBIG's own CCDOPS software was not available. It has remained largely unchanged since its addition."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[fitstopnm](/man/fitstopnm)(1), [pnmtopng](/man/pnmtopng)(1), [pamtopnm](/man/pamtopnm)(1), [pnmnorm](/man/pnmnorm)(1)"]],"e":["fitstopnm","pnmtopng","pamtopnm","pnmnorm"],"x":"sbigtopgm [image.sbig] > [image.pgm]"},"sirix-shell":{"n":"sirix-shell","t":"interactive JSONiq/XQuery shell for SirixDB","s":[["TAGLINE","interactive JSONiq/XQuery shell for SirixDB"],["TLDR","**Start** the interactive shell\n\n```sirix-shell```\n\nStore a document via a **query** (example session)\n\n```jn:store('mydb','resource','{\"key\": \"value\"}')```\n\n**Read** a stored value\n\n```jn:doc('mydb','resource').key```"],["SYNOPSIS","**sirix-shell**"],["DESCRIPTION","**sirix-shell** is an interactive REPL for **SirixDB** queries. It accepts multi-line JSONiq/XQuery input; an empty line executes the statement, and Control-D exits. Use it to store and open documents, run temporal and bitemporal queries, and explore revision history without packaging each expression as a one-shot **sirix-cli** invocation.\n\nSirixDB extends JSONiq/XQuery (via Brackit) with functions for opening revisions by number or timestamp, navigating node history, and computing structured diffs between revisions. The shell is built from the **sirix-query** module and can also be produced as a GraalVM native binary for fast startup."],["CAVEATS","Requires a configured SirixDB environment and a Java runtime (or native image). Multi-line entry is terminated by a blank line; accidental empty lines execute incomplete input. For scripted automation, prefer **sirix-cli** over the interactive shell."],["HISTORY","**sirix-shell** is part of the open-source **SirixDB** project, using the Brackit query engine for JSONiq/XQuery."],["SEE ALSO","[sirix-cli](/man/sirix-cli)(1), [java](/man/java)(1), [jq](/man/jq)(1)"],["RESOURCES","```[Source code](https://github.com/sirixdb/sirix)```\n\n```[Homepage](https://sirix.io)```\n\n```[Documentation](https://sirix.io/docs/)```"]],"e":["sirix-cli","java","jq"],"x":"sirix-shell"},"sndcpy":{"n":"sndcpy","t":"Forward Android device audio to computer","s":[["TAGLINE","Forward Android device audio to computer"],["TLDR","**Forward audio** from a connected Android device\n\n```sndcpy```\n\n**Forward audio** from a specific device by serial\n\n```sndcpy [serial]```\n\n**Stop audio forwarding**\n\n> Press **Ctrl+C** in the terminal (or disconnect the device on Windows)"],["SYNOPSIS","**sndcpy** [_serial_]"],["PARAMETERS","_serial_\n> The serial number of the Android device to forward audio from. Optional if only one device is connected.\n\n**SNDCPY_PORT**\n> Environment variable to override the default forwarding port (default: 28200)."],["DESCRIPTION","**sndcpy** forwards audio from an Android device to a computer over USB or TCP/IP. It works as a companion to **scrcpy** (screen copy), enabling both video and audio forwarding from Android devices. The name is a portmanteau of \"sound\" and \"copy.\"\n\nThe tool installs a small app on the Android device that captures audio output and streams it to VLC on the computer. Audio continues to play on the device as well, and volume can be controlled independently on both device and computer.\n\nsndcpy requires Android 10 or later due to its reliance on the AudioPlaybackCapture API introduced in that version. VLC must be installed on the computer to receive and play the audio stream."],["CAVEATS","This is a proof-of-concept with limitations. Apps targeting Android 9 or earlier cannot have their audio captured by default. Apps can also explicitly disable audio capture by setting **allowAudioPlaybackCapture=\"false\"** in their manifest. Network jitter may cause VLC to increase buffering, resulting in audio delay. For reliable audio forwarding across all apps, Bluetooth pairing between device and computer is recommended as an alternative."],["HISTORY","**sndcpy** was created by Romain Vimont (rom1v), the same developer who created **scrcpy** for Android screen mirroring. It was released as a proof-of-concept to demonstrate Android 10's new audio capture capabilities. While functional, the project has not seen active development as the author considers Bluetooth audio forwarding a more practical solution for most use cases."],["SEE ALSO","[scrcpy](/man/scrcpy)(1), [adb](/man/adb)(1), [vlc](/man/vlc)(1)"]],"e":["scrcpy","adb","vlc"],"x":"sndcpy"},"systemctl-soft-reboot":{"n":"systemctl-soft-reboot","t":"Restart userspace without kernel reboot","s":[["TAGLINE","Restart userspace without kernel reboot"],["TLDR","**Soft reboot** (userspace only)\n\n```systemctl soft-reboot```\n\n**Force** soft reboot\n\n```systemctl soft-reboot -f```\n\n**Schedule** soft reboot\n\n```systemctl soft-reboot --when \"[timestamp]\"```\n\n**Cancel** scheduled soft reboot\n\n```systemctl soft-reboot --when cancel```"],["SYNOPSIS","**systemctl soft-reboot** [_OPTIONS_]"],["PARAMETERS","**-f, --force**\n> Force reboot even if inhibitors are present\n\n**--when** _TIMESTAMP_\n> Schedule reboot at specified time or cancel"],["DESCRIPTION","**systemctl soft-reboot** shuts down and restarts userspace while keeping the kernel running. This is faster than a full reboot as it skips BIOS/firmware initialization and kernel boot.\n\nThe kernel continues running; only systemd and all user services are restarted. This is useful for quickly testing service configurations or applying updates that don't require a kernel change."],["CAVEATS","Requires kernel support. Not suitable when kernel updates need to be applied. Some hardware state may persist inappropriately. Newer feature requiring recent systemd versions."],["HISTORY","The **soft-reboot** subcommand was added in systemd **254** to support faster system restarts for scenarios where only userspace changes need to take effect."],["SEE ALSO","[systemctl-reboot](/man/systemctl-reboot)(1), [systemctl-kexec](/man/systemctl-kexec)(1), [systemctl](/man/systemctl)(1)"]],"e":["systemctl-reboot","systemctl-kexec","systemctl"],"x":"systemctl soft-reboot"},"tailscale-up":{"n":"tailscale-up","t":"Connect device to Tailscale network","s":[["TAGLINE","Connect device to Tailscale network"],["TLDR","**Connect to Tailscale**\n\n```tailscale up```\n\n**Connect with exit node**\n\n```tailscale up --exit-node=[node]```\n\n**Connect accepting routes**\n\n```tailscale up --accept-routes```\n\n**Connect with SSH enabled**\n\n```tailscale up --ssh```\n\n**Advertise as exit node**\n\n```tailscale up --advertise-exit-node```"],["SYNOPSIS","**tailscale** **up** [_options_]"],["PARAMETERS","**--accept-routes**\n> Accept advertised routes.\n\n**--advertise-exit-node**\n> Offer as exit node.\n\n**--exit-node** _node_\n> Route traffic through node.\n\n**--hostname** _name_\n> Device hostname.\n\n**--shields-up**\n> Block incoming connections.\n\n**--ssh**\n> Enable Tailscale SSH.\n\n**--login-server** _url_\n> Custom control server.\n\n**--authkey** _key_\n> Pre-auth key.\n\n**--accept-dns**\n> Accept DNS configuration from the admin console.\n\n**--advertise-routes** _CIDR_\n> Expose physical subnet routes to the tailnet.\n\n**--advertise-tags** _TAGS_\n> Give tagged permissions to this device.\n\n**--force-reauth**\n> Force re-authentication.\n\n**--reset**\n> Reset unspecified settings to their defaults.\n\n**--qr**\n> Generate a QR code for the login URL.\n\n**--exit-node-allow-lan-access**\n> Allow LAN access while using an exit node.\n\n**--operator** _USER_\n> Unix username to operate tailscaled without sudo.\n\n**--timeout** _DURATION_\n> Maximum wait time for service initialization."],["DESCRIPTION","**tailscale up** connects the device to a Tailscale network. Authenticates and joins the tailnet. First run opens browser for authentication unless authkey is provided.\n\nTo change settings on an already-connected device, prefer **tailscale set** which only updates specified flags, whereas **tailscale up** applies OS defaults for all unspecified flags."],["INSTALL","```dnf: sudo dnf install tailscale```\n\n```pacman: sudo pacman -S tailscale```\n\n```apk: sudo apk add tailscale```\n\n```zypper: sudo zypper install tailscale```\n\n```brew: brew install tailscale```\n\n```nix: nix profile install nixpkgs#tailscale```"],["SEE ALSO","[tailscale](/man/tailscale)(1), [tailscale-set](/man/tailscale-set)(1)"]],"e":["tailscale","tailscale-set"],"x":"tailscale up"},"timetrap":{"n":"timetrap","t":"Simple command-line time tracking","s":[["TAGLINE","Simple command-line time tracking"],["TLDR","**Start timer**\n\n```timetrap in \"[task description]\"```\n\n**Stop timer**\n\n```timetrap out```\n\n**Show current**\n\n```timetrap now```\n\n**Display today**\n\n```timetrap display```\n\n**Switch sheet**\n\n```timetrap sheet [project]```\n\n**List sheets**\n\n```timetrap list```\n\n**Edit entry**\n\n```timetrap edit```"],["SYNOPSIS","**timetrap** _command_ [_options_] [_args_]"],["PARAMETERS","**in**\n> Start tracking.\n\n**out**\n> Stop tracking.\n\n**now**\n> Current status.\n\n**display**\n> Show entries.\n\n**sheet**\n> Switch timesheet.\n\n**list**\n> List sheets.\n\n**edit**\n> Edit entries."],["DESCRIPTION","**timetrap** is a simple command-line time tracking tool that uses a clock in/out model. You start a timer with **in** and stop it with **out**, and entries are stored in a local SQLite database for reliable, portable storage.\n\nTime entries are organized into sheets, which act as separate timesheets for different projects or clients. Switching between sheets with the **sheet** command makes it easy to track time across multiple activities without mixing entries.\n\nThe **display** command shows logged entries with calculated durations, and reporting features provide time summaries for billing or review. Entries can be edited after the fact to correct mistakes or add notes."],["CAVEATS","Ruby required. Command-line only. Manual entry."],["HISTORY","**timetrap** was created as a simple command-line time tracking tool using SQLite for storage."],["INSTALL","```nix: nix profile install nixpkgs#timetrap```"],["SEE ALSO","[watson](/man/watson)(1), [timew](/man/timew)(1)"]],"e":["watson","timew"],"x":"timetrap in \"[task description]\""},"tlmgr-pinning":{"n":"tlmgr-pinning","t":"Pin TeX Live packages to repositories","s":[["TAGLINE","Pin TeX Live packages to repositories"],["TLDR","**List pinned packages**\n\n```tlmgr pinning show```\n\n**Pin package to repository**\n\n```tlmgr pinning add [repo] [package]```\n\n**Unpin package**\n\n```tlmgr pinning remove [repo] [package]```"],["SYNOPSIS","**tlmgr pinning** _action_ [_repo_] [_glob_...]"],["PARAMETERS","**show**\n> Show current pinnings.\n\n**add** _repo_ _glob_\n> Pin packages matching glob to repository.\n\n**remove** _repo_ _glob_\n> Remove pinning."],["DESCRIPTION","**tlmgr pinning** manages package pinning to specific repositories. Ensures certain packages always come from preferred sources. Part of TeX Live Manager for multi-repository control."],["SEE ALSO","[tlmgr-repository](/man/tlmgr-repository)(1), [tlmgr](/man/tlmgr)(1)"]],"e":["tlmgr-repository","tlmgr"],"x":"tlmgr pinning show"},"vagrant-plugin":{"n":"vagrant-plugin","t":"Manage Vagrant plugins and extensions","s":[["TAGLINE","Manage Vagrant plugins and extensions"],["TLDR","**List plugins**\n\n```vagrant plugin list```\n\n**Install plugin**\n\n```vagrant plugin install [name]```\n\n**Uninstall plugin**\n\n```vagrant plugin uninstall [name]```\n\n**Update plugins**\n\n```vagrant plugin update```\n\n**Repair plugins**\n\n```vagrant plugin repair```"],["SYNOPSIS","**vagrant** **plugin** _command_ [_options_]"],["PARAMETERS","**list**\n> List installed plugins.\n\n**install** _name_\n> Install plugin.\n\n**uninstall** _name_\n> Remove plugin.\n\n**update** _name_\n> Update plugin.\n\n**repair**\n> Fix plugin issues.\n\n**expunge**\n> Remove all plugins.\n\n**--plugin-version** _ver_\n> Specific version."],["DESCRIPTION","**vagrant plugin** manages Vagrant plugins. Plugins extend Vagrant with providers, provisioners, and commands. Install from RubyGems or local files."],["INSTALL","```apt: sudo apt install vagrant```\n\n```dnf: sudo dnf install vagrant```\n\n```nix: nix profile install nixpkgs#vagrant```"],["SEE ALSO","[vagrant](/man/vagrant)(1)"]],"e":["vagrant"],"x":"vagrant plugin list"},"vboxmanage-extpack":{"n":"vboxmanage-extpack","t":"Manage VirtualBox extension packs","s":[["TAGLINE","Manage VirtualBox extension packs"],["TLDR","**Install an extension pack**\n\n```vboxmanage extpack install [extension.vbox-extpack]```\n\n**Install and replace an existing version**\n\n```vboxmanage extpack install --replace [extension.vbox-extpack]```\n\n**Uninstall an extension pack by name**\n\n```vboxmanage extpack uninstall \"[Oracle VM VirtualBox Extension Pack]\"```\n\n**Force uninstall an extension pack**\n\n```vboxmanage extpack uninstall --force \"[Oracle VM VirtualBox Extension Pack]\"```\n\n**List installed extension packs**\n\n```vboxmanage list extpacks```\n\n**Clean up temporary files from failed operations**\n\n```vboxmanage extpack cleanup```"],["SYNOPSIS","**vboxmanage** **extpack** _command_ [_options_]"],["PARAMETERS","**install** [**--replace**] _tarball_\n> Install an extension pack from the specified file. Fails if an older version is already installed unless --replace is used.\n\n**uninstall** [**--force**] _name_\n> Uninstall an extension pack by name. Succeeds even if the pack is not present.\n\n**cleanup**\n> Remove temporary files and directories left behind by failed install or uninstall operations.\n\n**--replace**\n> Used with install. Uninstall any existing version before installing the new one.\n\n**--force**\n> Used with uninstall. Override most refusals to uninstall an extension pack."],["DESCRIPTION","**vboxmanage extpack** manages VirtualBox extension packs. Extension packs add features like USB 2.0/3.0, VirtualBox RDP, and disk encryption. Installing or uninstalling extension packs typically requires administrator privileges."],["SEE ALSO","[vboxmanage](/man/vboxmanage)(1), [vboxmanage-list](/man/vboxmanage-list)(1)"]],"e":["vboxmanage","vboxmanage-list"],"x":"vboxmanage extpack install [extension.vbox-extpack]"},"vboxmanage-unregistervm":{"n":"vboxmanage-unregistervm","t":"Remove VM from VirtualBox registry","s":[["TAGLINE","Remove VM from VirtualBox registry"],["TLDR","**Unregister a VM by name**\n\n```vboxmanage unregistervm [vmname]```\n\n**Unregister a VM by UUID**\n\n```vboxmanage unregistervm [uuid]```\n\n**Unregister and delete all associated VM files**\n\n```vboxmanage unregistervm [vmname] --delete```\n\n**Unregister and delete all files including attached DVDs and floppies**\n\n```vboxmanage unregistervm [vmname] --delete-all```"],["SYNOPSIS","**vboxmanage** **unregistervm** _uuid_ | _vmname_ [**--delete** | **--delete-all**]"],["PARAMETERS","**--delete**\n> Delete all VM files including hard disk images (that are not shared with other VMs), the machine XML definition file, and snapshot files.\n\n**--delete-all**\n> Delete all files described in --delete, as well as all DVDs and floppy images located in the VM folder and attached only to this VM."],["DESCRIPTION","**vboxmanage unregistervm** removes a virtual machine from the VirtualBox registry. The VM can be identified by its name or UUID. Without --delete, only the registration is removed while all VM files are kept on disk. With --delete or --delete-all, the associated files are permanently removed."],["CAVEATS","The --delete and --delete-all options permanently remove VM files and cannot be undone. Shared disk images used by other VMs are not deleted."],["SEE ALSO","[vboxmanage](/man/vboxmanage)(1), [vboxmanage-registervm](/man/vboxmanage-registervm)(1), [vboxmanage-list](/man/vboxmanage-list)(1), [vboxmanage-showvminfo](/man/vboxmanage-showvminfo)(1)"]],"e":["vboxmanage","vboxmanage-registervm","vboxmanage-list","vboxmanage-showvminfo"],"x":"vboxmanage unregistervm [vmname]"},"vkcubepp":{"n":"vkcubepp","t":"Vulkan cube demo in C++","s":[["TAGLINE","Vulkan cube demo in C++"],["TLDR","**Run Vulkan cube demo**\n\n```vkcubepp```\n\n**Run with validation layers**\n\n```vkcubepp --validate```\n\n**Run on specific GPU**\n\n```vkcubepp --gpu_number [0]```\n\n**Run in fullscreen**\n\n```vkcubepp --fullscreen```\n\n**Suppress frame count output**\n\n```vkcubepp --suppress_popups```\n\n**Display help**\n\n```vkcubepp --help```"],["SYNOPSIS","**vkcubepp** [_options_]"],["PARAMETERS","**--validate**\n> Enable Vulkan validation layers.\n\n**--gpu_number** _n_\n> Select GPU by index.\n\n**--fullscreen**\n> Run in fullscreen mode.\n\n**--present_mode** _mode_\n> Set presentation mode.\n\n**--suppress_popups**\n> Suppress informational popups.\n\n**--width** _n_\n> Window width in pixels.\n\n**--height** _n_\n> Window height in pixels.\n\n**--help**, **-h**\n> Display help."],["DESCRIPTION","**vkcubepp** displays a spinning 3D cube rendered using the Vulkan graphics API. It's a simple test application to verify that Vulkan drivers and libraries are correctly installed and functioning.\n\nPart of the Vulkan-Tools package from the Khronos Group, vkcubepp is written in C++ (vkcube is the C version). Both produce the same visual output but demonstrate different API usage patterns.\n\nRunning with --validate enables Vulkan validation layers, useful for developers debugging Vulkan applications. The cube should spin smoothly if Vulkan is working correctly."],["CAVEATS","Requires Vulkan-capable GPU and drivers. May fail silently if Vulkan is misconfigured. Part of vulkan-tools package. Window system support (X11/Wayland) depends on build options."],["HISTORY","**vkcubepp** is part of the official Vulkan-Tools repository maintained by the Khronos Group. Vulkan was released in 2016 as a successor to OpenGL, providing lower-level GPU access. The cube demo serves as a minimal Vulkan test case."],["INSTALL","```apt: sudo apt install vulkan-tools```\n\n```dnf: sudo dnf install vulkan-tools```\n\n```pacman: sudo pacman -S vulkan-tools```\n\n```apk: sudo apk add vulkan-tools```\n\n```zypper: sudo zypper install vulkan-tools```\n\n```brew: brew install vulkan-tools```\n\n```nix: nix profile install nixpkgs#vulkan-tools```"],["SEE ALSO","[vulkaninfo](/man/vulkaninfo)(1), [vkcube](/man/vkcube)(1), [glxinfo](/man/glxinfo)(1), [glxgears](/man/glxgears)(1)"]],"e":["vulkaninfo","vkcube","glxinfo","glxgears"],"x":"vkcubepp"},"xz":{"n":"xz","t":"LZMA2 compression utility","s":[["TAGLINE","LZMA2 compression utility"],["TLDR","**Compress a file**\n\n```xz [file]```\n\n**Decompress a file**\n\n```xz -d [file.xz]```\n\n**Compress with maximum compression**\n\n```xz -9 [file]```\n\n**Compress keeping the original file**\n\n```xz -k [file]```\n\n**Compress to stdout** (for piping)\n\n```xz -c [file] > [file.xz]```\n\n**Decompress to stdout**\n\n```xz -dc [file.xz]```\n\n**List compressed file info**\n\n```xz -l [file.xz]```\n\n**Test compressed file integrity**\n\n```xz -t [file.xz]```"],["SYNOPSIS","**xz** [_options_] [_file_...]"],["PARAMETERS","**-d**, **--decompress**\n> Decompress files\n\n**-z**, **--compress**\n> Compress files (default)\n\n**-k**, **--keep**\n> Keep original file\n\n**-c**, **--stdout**\n> Write to stdout, keep original file\n\n**-f**, **--force**\n> Force compression/decompression\n\n**-t**, **--test**\n> Test compressed file integrity\n\n**-l**, **--list**\n> List information about compressed files\n\n**-0** to **-9**\n> Compression level (0=fast, 9=best compression)\n\n**-e**, **--extreme**\n> More compression (slower)\n\n**-T** _N_, **--threads**=_N_\n> Use N threads (0 = auto-detect cores)\n\n**-v**, **--verbose**\n> Verbose output\n\n**-q**, **--quiet**\n> Suppress warnings"],["DESCRIPTION","**xz** is a general-purpose compression tool using the LZMA2 algorithm. It provides high compression ratios, often better than gzip and bzip2, though compression is slower.\n\nBy default, xz compresses files and replaces them with .xz versions. Use **-k** to keep originals or **-c** to write to stdout for pipelines.\n\nMulti-threading support (**-T**) significantly speeds up compression on multi-core systems. Decompression is single-threaded but fast.\n\nThe compression level affects both ratio and memory usage. Level 9 requires significantly more memory than lower levels, both for compression and decompression."],["CAVEATS","High compression levels (7-9) use substantial memory. Level 9 may require over 600MB for compression and 65MB for decompression.\n\nFor archive distribution, consider xz's memory requirements on target systems. Lower compression levels are more universally usable.\n\nxz format is not compatible with gzip or bzip2. Use appropriate tools (**gzip**, **bzip2**) for those formats."],["HISTORY","**xz** was developed by **Lasse Collin** and **Igor Pavlov** (LZMA SDK). The xz format and liblzma were released in **2009** as successors to the LZMA format used by 7-Zip. xz is now widely used for compressing Linux kernel tarballs and package archives."],["INSTALL","```apt: sudo apt install xz-utils```\n\n```dnf: sudo dnf install xz```\n\n```pacman: sudo pacman -S xz```\n\n```apk: sudo apk add xz```\n\n```zypper: sudo zypper install xz```\n\n```brew: brew install xz```\n\n```nix: nix profile install nixpkgs#xz```"],["SEE ALSO","[gzip](/man/gzip)(1), [bzip2](/man/bzip2)(1), [zstd](/man/zstd)(1), [tar](/man/tar)(1), [xzcat](/man/xzcat)(1)"]],"e":["gzip","bzip2","zstd","tar","xzcat"],"x":"xz [file]"},"ykman-oath":{"n":"ykman-oath","t":"Manage YubiKey OATH accounts","s":[["TAGLINE","Manage YubiKey OATH accounts"],["TLDR","**List OATH accounts**\n\n```ykman oath accounts list```\n\n**Add TOTP account**\n\n```ykman oath accounts add [name] [secret]```\n\n**Generate code**\n\n```ykman oath accounts code [name]```\n\n**Delete account**\n\n```ykman oath accounts delete [name]```"],["SYNOPSIS","**ykman** **oath** _command_ [_options_]"],["PARAMETERS","**accounts** _cmd_\n> Manage OATH accounts.\n\n**info**\n> Show OATH information.\n\n**access** _cmd_\n> Manage password protection.\n\n**reset**\n> Reset OATH application."],["DESCRIPTION","**ykman oath** manages TOTP/HOTP accounts on YubiKey. Store 2FA secrets securely on hardware. Generate one-time passwords for authentication."],["INSTALL","```dnf: sudo dnf install yubikey-manager```\n\n```pacman: sudo pacman -S yubikey-manager```\n\n```apk: sudo apk add yubikey-manager```\n\n```zypper: sudo zypper install yubikey-manager```\n\n```brew: brew install ykman```\n\n```nix: nix profile install nixpkgs#yubikey-manager```"],["SEE ALSO","[ykman](/man/ykman)(1), [ykman-fido](/man/ykman-fido)(1)"]],"e":["ykman","ykman-fido"],"x":"ykman oath accounts list"},"youplot":{"n":"youplot","t":"Draw plots on the terminal from command-line data","s":[["TAGLINE","Draw plots on the terminal from command-line data"],["TLDR","**Draw a bar chart from TSV data**\n\n```cat [data.tsv] | uplot bar```\n\n**Draw a line plot from CSV**\n\n```cat [data.csv] | uplot line -d,```\n\n**Plot a histogram**\n\n```seq 100 | awk '{print rand()}' | uplot hist```"],["SYNOPSIS","**uplot** _subcommand_ [_options_]"],["DESCRIPTION","**YouPlot** is a CLI tool that renders bar charts, line plots, scatter plots, histograms, and boxplots directly in the terminal using Unicode characters. It reads tab-separated or custom-delimited data from stdin, making it ideal for piping output from other commands."],["HISTORY","**YouPlot** was created by **kojix2** (Red Data Tools) and is written in **Ruby**."],["INSTALL","```brew: brew install youplot```\n\n```nix: nix profile install nixpkgs#youplot```"],["SEE ALSO","[gnuplot](/man/gnuplot)(1), [termgraph](/man/termgraph)(1)"]],"e":["gnuplot","termgraph"],"x":"cat [data.tsv] | uplot bar"}});
