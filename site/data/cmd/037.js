window.LCL_BUCKET(37,{"adb-reboot":{"n":"adb-reboot","t":"Reboot a connected Android device","s":[["TAGLINE","Reboot a connected Android device"],["TLDR","**Reboot** the device normally\n\n```adb reboot```\n\nReboot into **bootloader/fastboot** mode\n\n```adb reboot bootloader```\n\nReboot into **recovery** mode\n\n```adb reboot recovery```\n\nReboot into **sideload** mode\n\n```adb reboot sideload```\n\nReboot into sideload mode and **automatically reboot** when done\n\n```adb reboot sideload-auto-reboot```"],["SYNOPSIS","**adb reboot** [_bootloader_|_recovery_|_sideload_|_sideload-auto-reboot_]"],["DESCRIPTION","**adb reboot** restarts the connected Android device. Without arguments, it performs a normal reboot. With mode arguments, it can reboot into special modes like bootloader (fastboot), recovery, or sideload for flashing firmware or installing updates."],["PARAMETERS","**bootloader**\n> Reboot into bootloader/fastboot mode for low-level flashing\n\n**recovery**\n> Reboot into recovery mode for system recovery or updates\n\n**sideload**\n> Reboot into sideload mode for ADB sideloading\n\n**sideload-auto-reboot**\n> Reboot into sideload mode and automatically reboot after sideload completes"],["CAVEATS","Bootloader and recovery modes may require unlocked bootloader or specific device states. Some devices use vendor-specific boot modes. Rebooting to bootloader terminates the adb connection; use **fastboot** commands afterward."],["HISTORY","**adb reboot** has been available since the initial Android SDK release in **2008**. Boot mode options were added to facilitate ROM development and device recovery operations."],["INSTALL","```apt: sudo apt install adb```\n\n```dnf: sudo dnf install android-tools```\n\n```pacman: sudo pacman -S android-tools```\n\n```apk: sudo apk add android-tools-adb```\n\n```zypper: sudo zypper install android-tools```\n\n```nix: nix profile install nixpkgs#android-tools```"],["SEE ALSO","[adb](/man/adb)(1), [adb-shell](/man/adb-shell)(1), [fastboot](/man/fastboot)(1)"]],"e":["adb","adb-shell","fastboot"],"x":"adb reboot"},"amass-track":{"n":"amass-track","t":"Track changes in external attack surface over time","s":[["TAGLINE","Track changes in external attack surface over time"],["TLDR","**Track** changes between enumerations\n\n```amass track -d [example.com]```\n\nTrack since **specific date**\n\n```amass track -d [example.com] -since \"2024-01-01\"```\n\nCompare with **previous enumeration**\n\n```amass track -d [example.com] -last [2]```\n\nOutput **differences** to file\n\n```amass track -d [example.com] -o [changes.txt]```"],["SYNOPSIS","**amass track** [_-d domain_] [_-since date_] [_-last n_] [_options_]"],["DESCRIPTION","**amass track** compares results from different enumeration runs to identify changes in an organization's external attack surface. It detects new subdomains, removed assets, and infrastructure changes.\n\nThis is useful for continuous monitoring of an organization's digital footprint, alerting security teams to potential shadow IT or unauthorized changes.\n\nNote that **track** is a legacy subcommand. In Amass v4 it was moved out of the core **amass** binary into the separate oam-tools package (as **oam_track**), and the workflow was reworked again in Amass v5. On current Amass installations the **amass track** subcommand may not be available; check the version with **amass -version**."],["PARAMETERS","**-d** _domain_\n> Domain to track\n\n**-since** _date_\n> Compare against enumerations since date\n\n**-last** _n_\n> Compare against the last n enumerations\n\n**-history**\n> Show all historical changes\n\n**-o** _file_\n> Output file\n\n**-dir** _path_\n> Database directory"],["CONFIGURATION","**~/.config/amass/config.ini**\n> Amass configuration file defining data sources, API keys, and scope settings."],["CAVEATS","Requires multiple enumeration runs stored in the database. Detection quality depends on consistency of enumeration settings. Large organizations may have frequent legitimate changes. The **track** subcommand is deprecated: it was removed from the core **amass** binary in v4 (moved to oam-tools as **oam_track**) and reworked again in v5, so it may be unavailable on current installations."],["HISTORY","**amass track** was introduced to support continuous monitoring use cases, allowing security teams to detect attack surface changes over time. With the Amass v4 redesign the subcommand was split out into the oam-tools package as **oam_track**, and the tracking workflow was further reworked in Amass v5."],["INSTALL","```aur: yay -S amass```\n\n```brew: brew install amass```\n\n```nix: nix profile install nixpkgs#amass```"],["SEE ALSO","[amass](/man/amass)(1), [amass-enum](/man/amass-enum)(1), [amass-intel](/man/amass-intel)(1), [amass-db](/man/amass-db)(1)"],["RESOURCES","```[Source code](https://github.com/owasp-amass/amass)```\n\n```[Documentation](https://github.com/owasp-amass/amass/blob/master/doc/user_guide.md)```"]],"e":["amass","amass-enum","amass-intel","amass-db"],"x":"amass track -d [example.com]"},"amcheck":{"n":"amcheck","t":"Verify Amanda backup configuration before a run","s":[["TAGLINE","Verify Amanda backup configuration before a run"],["TLDR","**Run all checks** (client and server) for a configuration\n\n```amcheck [config_name]```\n\n**Run server-side checks** only (local and tape)\n\n```amcheck -s [config_name]```\n\n**Run client host checks** only\n\n```amcheck -c [config_name]```\n\n**Run local tests** on the server (permissions, etc.)\n\n```amcheck -l [config_name]```\n\n**Run tape tests** on the server\n\n```amcheck -t [config_name]```\n\n**Check specific clients** only\n\n```amcheck -c [config_name] [client1] [client2]```\n\n**Override a configuration option**\n\n```amcheck -o [tapetype=EXABYTE] [config_name]```"],["SYNOPSIS","**amcheck** [**-am**] [**-w**] [**-sclt**] [**-M** _address_] [**--client-verbose**] [**-o** _configoption_] _config_ [_host_ [_disk_]...]"],["PARAMETERS","**-s**\n> Run server-side checks (equivalent to -lt)\n\n**-c**\n> Run client host checks; optionally specify specific clients\n\n**-l**\n> Run local tests on the server host (permissions, configuration)\n\n**-t**\n> Run tape tests on the server host (verify correct tape is mounted)\n\n**-m**\n> Send results via email to the address specified in amanda.conf\n\n**-M** _address_\n> Send results to the specified email address\n\n**-w**\n> Enable a more thorough tape check that overwrites the tape label\n\n**-a**\n> Skip client checks if they fail\n\n**--client-verbose**\n> Print verbose client output\n\n**-o** _name=value_\n> Override a configuration option from amanda.conf\n\n_config_\n> Amanda configuration name to check\n\n_host_\n> Specific client hostname to check\n\n_disk_\n> Specific disk expression to check"],["DESCRIPTION","**amcheck** performs self-checks on both the Amanda backup server and client hosts before a backup run. It helps identify potential issues that could cause backup failures, such as incorrect tape mounting, permission problems, or unreachable clients.\n\nOn the server side, amcheck verifies that the correct tape is mounted for the next scheduled backup and checks local configuration and permissions. On client hosts, it confirms that the Amanda client daemon is running and that the backup server has proper permissions to access the filesystems scheduled for backup.\n\nThe tool is typically run before scheduled backups or manually by administrators to verify the backup infrastructure is ready. Results can be displayed on standard output or sent via email using the **-m** option. The default behavior runs both client and server checks (**-cs**).\n\nClient checks have a configurable timeout (default 30 seconds) controlled by the **ctimeout** setting in amanda.conf."],["CONFIGURATION","**/etc/amanda/<config>/amanda.conf**\n> Amanda server configuration defining tape devices, holding disks, dump types, and scheduling parameters.\n\n**/etc/amanda/<config>/disklist**\n> List of client hosts and filesystems to back up."],["CAVEATS","The **-w** option performs a destructive tape check that overwrites the tape label; use with caution. Client checks require network connectivity and running amandad service on clients. Server checks require appropriate permissions to access tape devices and configuration files."],["HISTORY","Amanda (Advanced Maryland Automatic Network Disk Archiver) was developed at the **University of Maryland** starting in **1991** by James da Silva. It became one of the most widely used open-source backup solutions for Unix systems. The project has been maintained by the Amanda community and **Zmanda** (later acquired by Carbonite) and continues to be actively developed."],["INSTALL","```apt: sudo apt install amanda-server```\n\n```dnf: sudo dnf install amanda-server```"],["SEE ALSO","[amdump](/man/amdump)(8), [amrecover](/man/amrecover)(8)"]],"e":["amdump","amrecover"],"x":"amcheck [config_name]"},"aws-s3-cp":{"n":"aws-s3-cp","t":"Copy files to and from S3 buckets","s":[["TAGLINE","Copy files to and from S3 buckets"],["TLDR","**Copy a local file** to an S3 bucket\n\n```aws s3 cp [file.txt] s3://[bucket-name]/```\n\n**Download an S3 object** to local filesystem\n\n```aws s3 cp s3://[bucket-name]/[file.txt] [./local-file.txt]```\n\n**Copy an object** between S3 locations\n\n```aws s3 cp s3://[source-bucket]/[file.txt] s3://[dest-bucket]/[file.txt]```\n\n**Recursively copy** a directory to S3\n\n```aws s3 cp [./local-dir] s3://[bucket-name]/[prefix/] --recursive```\n\n**Copy with specific storage class**\n\n```aws s3 cp [file.txt] s3://[bucket-name]/ --storage-class [STANDARD_IA]```\n\n**Copy excluding certain files**\n\n```aws s3 cp [./local-dir] s3://[bucket-name]/ --recursive --exclude \"*.log\"```\n\n**Upload with server-side encryption**\n\n```aws s3 cp [file.txt] s3://[bucket-name]/ --sse aws:kms --sse-kms-key-id [key-id]```\n\n**Preview copy operation** without executing\n\n```aws s3 cp [./local-dir] s3://[bucket-name]/ --recursive --dryrun```"],["SYNOPSIS","**aws s3 cp** _source_ _destination_ [_options_]"],["DESCRIPTION","**aws s3 cp** copies files between the local filesystem and Amazon S3, or between S3 locations. The source and destination can be a LocalPath (local file/directory) or an S3Uri (s3://bucket-name/key).\n\nThe command supports single file transfers, recursive directory copies, and streaming data to/from S3. It handles multipart uploads automatically for large files and can preserve or modify object metadata during copies."],["PARAMETERS","**--recursive**\n> Copy all files and objects under the specified directory or S3 prefix\n\n**--dryrun**\n> Display operations that would be performed without executing them\n\n**--quiet**\n> Suppress all output during the copy operation\n\n**--include** _pattern_\n> Include files matching the pattern (used with --exclude)\n\n**--exclude** _pattern_\n> Exclude files matching the pattern from the copy operation\n\n**--acl** _value_\n> Set access control list: private, public-read, public-read-write, authenticated-read, bucket-owner-full-control\n\n**--storage-class** _value_\n> Specify storage class: STANDARD, REDUCED_REDUNDANCY, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER, DEEP_ARCHIVE, GLACIER_IR\n\n**--sse** _value_\n> Enable server-side encryption: AES256 or aws:kms\n\n**--sse-kms-key-id** _value_\n> KMS key ID for server-side encryption with AWS KMS\n\n**--metadata** _value_\n> Custom metadata to store with the S3 object (JSON format)\n\n**--metadata-directive** _value_\n> COPY or REPLACE: whether to copy source metadata or replace it (S3-to-S3 copies)\n\n**--content-type** _value_\n> Override the MIME type of the uploaded object\n\n**--expires** _value_\n> Set the Expires header (e.g., 2024-12-31T23:59:59Z)\n\n**--no-overwrite**\n> Prevent overwriting existing files at the destination\n\n**--expected-size** _value_\n> Expected stream size in bytes (required for streams over 50GB)\n\n**--follow-symlinks** / **--no-follow-symlinks**\n> Follow symbolic links when copying (default: follow)"],["CAVEATS","When copying directories recursively, use trailing slashes consistently to control prefix behavior. Pattern matching with **--include** and **--exclude** is order-dependent. Copying to/from Glacier storage class requires special flags (**--force-glacier-transfer**). Streaming operations from stdin require **--expected-size** for files larger than 50GB."],["INSTALL","```dnf: sudo dnf install aws```\n\n```pacman: sudo pacman -S aws-cli```\n\n```apk: sudo apk add aws-cli```\n\n```zypper: sudo zypper install aws-cli```"],["SEE ALSO","[aws-s3](/man/aws-s3)(1), [aws-s3-sync](/man/aws-s3-sync)(1), [aws-s3-mv](/man/aws-s3-mv)(1), [aws-s3api](/man/aws-s3api)(1)"],["RESOURCES","```[Source code](https://github.com/aws/aws-cli)```\n\n```[Homepage](https://aws.amazon.com/cli/)```\n\n```[Documentation](https://docs.aws.amazon.com/cli/latest/reference/s3/cp.html)```"]],"e":["aws-s3","aws-s3-sync","aws-s3-mv","aws-s3api"],"x":"aws s3 cp [file.txt] s3://[bucket-name]/"},"aws-s3-presign":{"n":"aws-s3-presign","t":"Generate pre-signed URLs for temporary S3 object access","s":[["TAGLINE","Generate pre-signed URLs for temporary S3 object access"],["TLDR","**Generate a pre-signed URL** with default 1-hour expiration\n\n```aws s3 presign s3://[bucket-name]/[file.txt]```\n\n**Generate a pre-signed URL** valid for 1 week (maximum)\n\n```aws s3 presign s3://[bucket-name]/[file.txt] --expires-in 604800```\n\n**Generate a pre-signed URL** valid for 5 minutes\n\n```aws s3 presign s3://[bucket-name]/[file.txt] --expires-in 300```\n\n**Generate a pre-signed URL** for a specific region\n\n```aws s3 presign s3://[bucket-name]/[file.txt] --region [us-west-2]```"],["SYNOPSIS","**aws s3 presign** _S3Uri_ [_--expires-in value_] [_options_]"],["DESCRIPTION","**aws s3 presign** generates a pre-signed URL for an Amazon S3 object. Anyone with the pre-signed URL can retrieve the S3 object using an HTTP GET request without AWS credentials.\n\nPre-signed URLs are useful for sharing private S3 objects temporarily, embedding download links in applications, or providing time-limited access to files. All pre-signed URLs use Signature Version 4 (SigV4) authentication."],["PARAMETERS","**S3Uri**\n> The S3 URI of the object to generate a pre-signed URL for (s3://bucket-name/key)\n\n**--expires-in** _value_\n> Number of seconds until the pre-signed URL expires (default: 3600, maximum: 604800)\n\n**--region** _value_\n> The AWS region of the bucket (required for SigV4; overrides config/env settings).\n\n**--profile** _name_\n> Use a specific named profile from the credentials file.\n\n**--endpoint-url** _url_\n> Override the default endpoint URL (useful for S3-compatible services)."],["CAVEATS","The maximum expiration time is 604800 seconds (7 days). The region must be configured explicitly since all pre-signed URLs use SigV4 authentication. Pre-signed URLs produced by **aws s3 presign** grant read-only (GET) access; uploads require **aws s3api** with SigV4 query parameters or SDK helpers. The URL expires based on the credentials used to sign it; if using temporary credentials (STS), the URL cannot outlive those credentials."],["INSTALL","```dnf: sudo dnf install aws```\n\n```pacman: sudo pacman -S aws-cli```\n\n```apk: sudo apk add aws-cli```\n\n```zypper: sudo zypper install aws-cli```"],["SEE ALSO","[aws-s3](/man/aws-s3)(1), [aws-s3-cp](/man/aws-s3-cp)(1), [aws-s3api](/man/aws-s3api)(1)"]],"e":["aws-s3","aws-s3-cp","aws-s3api"],"x":"aws s3 presign s3://[bucket-name]/[file.txt]"},"bindfs":{"n":"bindfs","t":"Mount directories with altered ownership and permissions via FUSE","s":[["TAGLINE","Mount directories with altered ownership and permissions via FUSE"],["TLDR","**Mount** a directory to another location\n\n```sudo bindfs /path/to/source /path/to/mount```\n\nMount with **user mapping**\n\n```sudo bindfs --map=user1/user2 /path/to/source /path/to/mount```\n\nForce all files to appear owned by a specific **user and group**\n\n```sudo bindfs --force-user=user --force-group=group /path/to/source /path/to/mount```\n\nMount with modified **permissions**\n\n```sudo bindfs --perms=0755 /path/to/source /path/to/mount```\n\nMount as **read-only**\n\n```bindfs -o ro /path/to/source /path/to/mount```\n\n**Unmount**\n\n```sudo umount /path/to/mount```"],["SYNOPSIS","**bindfs** [_OPTIONS_] _source_ _mountpoint_"],["DESCRIPTION","**bindfs** mounts a directory to another location with different ownership and permissions. It uses FUSE to create a view of a directory tree with modified permissions."],["PARAMETERS","**--map=** _user1/user2_\n> Map ownership from user1 to user2.\n\n**--force-user=** _user_\n> Force all files to appear owned by user.\n\n**--force-group=** _group_\n> Force all files to appear owned by group.\n\n**--perms=** _mode_\n> Set permission bits for all files (octal or symbolic).\n\n**--create-as-user**\n> Change owner/group of new files to the caller's uid/gid.\n\n**--resolve-symlinks**\n> Resolve symbolic links in the source directory.\n\n**--enable-ioctl**\n> Forward ioctl calls to the underlying filesystem.\n\n**-r**, **-o ro**\n> Mount as read-only.\n\n**-o** _options_\n> Pass FUSE mount options.\n\n**--multithreaded**\n> Run bindfs in multithreaded mode."],["CAVEATS","Changes to ownership mapping are bidirectional - newly created files are mapped back. Requires FUSE support. Use umount to unmount the bindfs filesystem."],["HISTORY","**bindfs** is a FUSE filesystem for mounting directories with altered permissions."],["INSTALL","```apt: sudo apt install bindfs```\n\n```dnf: sudo dnf install bindfs```\n\n```zypper: sudo zypper install bindfs```\n\n```brew: brew install bindfs```\n\n```nix: nix profile install nixpkgs#bindfs```"],["SEE ALSO","[mount](/man/mount)(8), [fusermount](/man/fusermount)(1)"]],"e":["mount","fusermount"],"x":"sudo bindfs /path/to/source /path/to/mount"},"bioradtopgm":{"n":"bioradtopgm","t":"Convert Bio-Rad confocal microscope images to PGM format","s":[["TAGLINE","Convert Bio-Rad confocal microscope images to PGM format"],["TLDR","**Convert a Biorad confocal file** to PGM\n\n```bioradtopgm [bioradfile.pic] > [output.pgm]```\n\n**Extract a specific image** from a multi-image file\n\n```bioradtopgm -imagenum [0] [bioradfile.pic] > [output.pgm]```\n\n**Preserve original intensity values** without scaling\n\n```bioradtopgm -truegrayscale [bioradfile.pic] > [output.pgm]```\n\n**Read from stdin** and convert to JPEG\n\n```cat [bioimage.pic] | bioradtopgm | pnmtojpeg > [output.jpg]```\n\n**Show image info** without converting\n\n```bioradtopgm [bioradfile.pic]```"],["SYNOPSIS","**bioradtopgm** [**-imagenum** _n_] [**-truegrayscale**] [**-quiet**] [_bioradfile_]"],["PARAMETERS","**-imagenum** _n_\n> Select which image to extract from a multi-image file. Images are numbered starting at zero.\n\n**-truegrayscale**\n> Preserve original intensity values without scaling. Important for scientific applications requiring photometric accuracy.\n\n**-quiet**\n> Suppress informational messages on standard error."],["DESCRIPTION","**bioradtopgm** converts Biorad confocal microscope image files (PIC format) to PGM (Portable GrayMap) format. It is part of the Netpbm package of image manipulation utilities.\n\nIf no file argument is provided, the program reads from standard input. Output is written to standard output. For multi-image files, running without **-imagenum** displays information about the file's dimensions and image count.\n\nBy default, intensity values are scaled to span the full 0-255 range for better visual contrast."],["CAVEATS","If the output image appears upside down, pipe it through **pamflip -tb** to correct orientation. Output is in PGM format; use additional Netpbm tools like **pnmtojpeg** or **pnmtopng** to convert to common formats."],["HISTORY","Part of the Netpbm toolkit, **bioradtopgm** was developed to enable conversion of proprietary Bio-Rad confocal microscope data into open, interchangeable formats for scientific and medical research workflows."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pamflip](/man/pamflip)(1), [pnmtojpeg](/man/pnmtojpeg)(1), [pnmtopng](/man/pnmtopng)(1), [netpbm](/man/netpbm)(1)"]],"e":["pamflip","pnmtojpeg","pnmtopng","netpbm"],"x":"bioradtopgm [bioradfile.pic] > [output.pgm]"},"cgclassify":{"n":"cgclassify","t":"move running processes to a cgroup","s":[["TAGLINE","move running processes to a cgroup"],["TLDR","Move a process to the **CPU cgroup** \"student\"\n\n```cgclassify -g [cpu:student] [1234]```\n\nMove a process based on **/etc/cgrules.conf**\n\n```cgclassify [1234]```\n\nMove to cgroup with **sticky** mode\n\n```cgclassify --sticky -g [cpu:/student] [1234]```"],["SYNOPSIS","**cgclassify** [_options_] _pid_..."],["DESCRIPTION","**cgclassify** moves running processes to control groups (cgroups). This allows changing resource limits and accounting for already-running processes without restarting them.\n\nWhen used without the -g option, processes are classified according to rules in **/etc/cgrules.conf**."],["PARAMETERS","**-g** _controllers:path_\n> Move process to specified cgroup hierarchy\n\n**--sticky**\n> Prevent cgred daemon from reclassifying the process\n\n**--cancel-sticky**\n> Allow cgred to manage the process again"],["CONFIGURATION","**/etc/cgrules.conf**\n> Rules for automatic cgroup classification when -g is not specified.\n\n**/etc/cgconfig.conf**\n> Cgroup hierarchy and controller configuration."],["CAVEATS","Requires cgroups v1 tools (libcgroup). For cgroups v2, use different tools. Moving processes between cgroups may affect their resource access immediately."],["INSTALL","```apt: sudo apt install cgroup-tools```\n\n```apk: sudo apk add cgroup-tools```"],["SEE ALSO","[cgcreate](/man/cgcreate)(1), [cgexec](/man/cgexec)(1), [cgroups](/man/cgroups)(7)"],["RESOURCES","```[Source code](https://github.com/libcgroup/libcgroup)```"]],"e":["cgcreate","cgexec","cgroups"],"x":"cgclassify -g [cpu:student] [1234]"},"countmail":{"n":"countmail","t":"counts messages in a mailbox","s":[["TAGLINE","counts messages in a mailbox"],["TLDR","**Count messages** in the default mailbox\n\n```countmail```\n\n**Count messages in a specific** mailbox file\n\n```countmail [/var/mail/user]```\n\n**Count messages in a Maildir**\n\n```countmail [~/Maildir]```\n\n**Show unread message count** only\n\n```countmail --unread```\n\n**Display count for multiple** mailboxes\n\n```countmail [mailbox1] [mailbox2]```\n\n**Output in machine-readable** format\n\n```countmail --quiet [mailbox]```"],["SYNOPSIS","**countmail** [_options_] [_mailbox_...]"],["PARAMETERS","_MAILBOX_\n> Path to mailbox file or Maildir to count. Uses default mailbox if omitted.\n\n**--unread**\n> Count only unread messages.\n\n**--read**\n> Count only read messages.\n\n**--quiet**\n> Output only the number, no labels.\n\n**--mbox**\n> Treat the path as mbox format.\n\n**--maildir**\n> Treat the path as Maildir format.\n\n**-h**, **--help**\n> Display help information."],["DESCRIPTION","**countmail** is a simple utility for counting messages in mailboxes. It supports both traditional mbox format files and Maildir directories, automatically detecting the format when possible.\n\nThe tool is useful for status bars, scripts, and notifications that need to display unread mail counts. It can process multiple mailboxes and output results suitable for parsing by other programs.\n\ncountmail reads mailbox headers efficiently without loading entire messages, making it fast even for large mailboxes. It can distinguish between read and unread messages based on standard mail flags."],["CAVEATS","Mailbox format detection may fail for unusual configurations; use explicit **--mbox** or **--maildir** flags when needed. File locking is not performed, so counts may be slightly off if the mailbox is being modified concurrently."],["HISTORY","countmail is part of the tradition of simple Unix mail utilities. Various implementations exist to provide this basic functionality, often as part of larger mail handling toolkits or as standalone scripts for desktop integration."],["SEE ALSO","[mail](/man/mail)(1), [from](/man/from)(1), [mutt](/man/mutt)(1), [fetchmail](/man/fetchmail)(1), [procmail](/man/procmail)(1)"]],"e":["mail","from","mutt","fetchmail","procmail"],"x":"countmail"},"cygcheck":{"n":"cygcheck","t":"Cygwin diagnostic and package query tool","s":[["TAGLINE","Cygwin diagnostic and package query tool"],["TLDR","**Generate full system** diagnostic report\n\n```cygcheck -s```\n\n**Check dependencies** of an executable\n\n```cygcheck [program.exe]```\n\n**Find which package** owns a file\n\n```cygcheck -f [/usr/bin/bash.exe]```\n\n**List files installed** by a package\n\n```cygcheck -l [coreutils]```\n\n**Check for package problems**\n\n```cygcheck -c```\n\n**Show Cygwin version** information\n\n```cygcheck -V```\n\n**Verbose dependency check**\n\n```cygcheck -v [program.exe]```"],["SYNOPSIS","**cygcheck** [_options_] [_program_|_package_]"],["PARAMETERS","**-s**, **--sysinfo**\n> Generate comprehensive system information report.\n\n**-c** [_PACKAGE_], **--check-setup**\n> Check installed packages for problems.\n\n**-f** _FILE_, **--find-package**\n> Find which package installed a file.\n\n**-l** _PACKAGE_, **--list-package**\n> List files installed by a package.\n\n**-p** _PATTERN_, **--package-query**\n> Search for packages by pattern.\n\n**-v**, **--verbose**\n> Verbose output (show full paths).\n\n**-r**, **--registry**\n> Include registry information in report.\n\n**-V**, **--version**\n> Display version information.\n\n**-h**, **--help**\n> Display help information."],["DESCRIPTION","**cygcheck** is a diagnostic utility for Cygwin, the Unix-like environment for Windows. It provides system information, checks executable dependencies, and manages package information.\n\nThe system report (**-s**) is invaluable for troubleshooting Cygwin issues, collecting details about installed packages, DLL versions, mount points, environment variables, and Windows system information. This report is often requested when seeking support.\n\nDependency checking helps identify missing DLLs that prevent programs from running. Package management queries help track file ownership and verify installation integrity."],["CAVEATS","Only works within Cygwin environment on Windows. System reports can be lengthy; redirect to file for bug reports. Package checking requires the setup database from Cygwin installation."],["HISTORY","cygcheck was developed as part of the **Cygwin** project, which was started by Cygnus Solutions in **1995** to provide Unix tools on Windows. The utility became essential for diagnosing common Cygwin configuration and dependency issues."],["SEE ALSO","[ldd](/man/ldd)(1), [dpkg](/man/dpkg)(1)"]],"e":["ldd","dpkg"],"x":"cygcheck -s"},"debugbrief":{"n":"debugbrief","t":"Turn a debugging session into an evidence-backed Markdown report","s":[["TAGLINE","Turn a debugging session into an evidence-backed Markdown report"],["TLDR","**Set up** debugbrief in the current project\n\n```debugbrief init```\n\n**Start** a new debugging session\n\n```debugbrief start \"[Login returns 500]\"```\n\n**Run a command** and record its output as evidence\n\n```debugbrief run -- [pytest tests/test_login.py]```\n\n**Add a note** during the session\n\n```debugbrief note \"[suspect the auth middleware]\"```\n\n**Re-run** the last captured command\n\n```debugbrief redo```\n\n**Finish** the session and generate the report\n\n```debugbrief end --mode [pr]```"],["SYNOPSIS","**debugbrief** _command_ [_arguments_]"],["DESCRIPTION","**debugbrief** records what you do while debugging and turns it into an evidence-backed Markdown report suitable for a pull request, a handoff, or an incident note. Unlike AI summarizers, the report is built only from recorded evidence: the commands you ran, their output, the files that changed, and the notes you added.\n\nA typical workflow is **init** once per project, then **start** to open a session, **run** to execute and capture commands, **note** to jot observations, and **end** to produce the report. The generated document includes a summary of test results and file changes, a red-to-green analysis with timing, a command timeline, error observations, and attempted fixes. Reports can be rendered in **pr**, **handoff**, or **incident** styles.\n\ndebugbrief is a Python tool, typically installed with **pipx install debugbrief** (also available via **uv tool install debugbrief** or **pip install debugbrief**)."],["COMMANDS","**init**\n> Configure the project and display the workflow.\n\n**start**  _title_\n> Begin a new debugging session with the given title.\n\n**note**  _text_\n> Record an observation in the active session.\n\n**run** **--** _command_\n> Execute a command and capture its output as evidence.\n\n**redo**\n> Re-execute the most recently captured command.\n\n**end** [**--mode** _pr_|_handoff_|_incident_]\n> Generate and save the report, then close the session.\n\n**status**\n> Show details of the active session.\n\n**preview** [**--mode** _..._]\n> Display the report without closing the session.\n\n**cancel** [**--yes**]\n> Abandon the active session.\n\n**doctor** [**--fix**]\n> Validate project health and optionally repair it.\n\n**recover**\n> Fix a broken session pointer.\n\n**last**\n> Locate the most recent report.\n\n**open**\n> Display the most recent report.\n\n**list**\n> Enumerate recorded sessions.\n\n**show**  _id_\n> Display a specific recorded session."],["CAVEATS","The report reflects only what was captured through debugbrief; commands run outside **debugbrief run** are not included as evidence. Because output is stored verbatim, review reports for secrets or sensitive data before sharing them."],["SEE ALSO","[script](/man/script)(1), [asciinema](/man/asciinema)(1), [git](/man/git)(1)"],["RESOURCES","```[Source code](https://github.com/harihkk/Debug-Brief)```"]],"e":["script","asciinema","git"],"x":"debugbrief init"},"distrobox":{"n":"distrobox","t":"Run Linux distributions in containers with host integration","s":[["TAGLINE","Run Linux distributions in containers with host integration"],["TLDR","**Create a new container**\n\n```distrobox create --name [mycontainer] --image [ubuntu:latest]```\n\n**List all containers**\n\n```distrobox list```\n\n**Enter a container**\n\n```distrobox enter [mycontainer]```\n\n**Run a command inside a container**\n\n```distrobox enter [mycontainer] -- [command]```\n\n**Export an app from the container to the host**\n\n```distrobox-export --app [application]```\n\n**Upgrade all containers**\n\n```distrobox upgrade --all```\n\n**Stop a container**\n\n```distrobox stop [mycontainer]```\n\n**Remove a container**\n\n```distrobox rm [mycontainer]```"],["SYNOPSIS","**distrobox** [_global_options_] _subcommand_ [_options_] [_arguments_]"],["PARAMETERS","**create**\n> Create a new container.\n\n**enter**\n> Enter an existing container.\n\n**list**\n> List containers created with distrobox.\n\n**rm**\n> Remove a container.\n\n**stop**\n> Stop a running container.\n\n**upgrade**\n> Upgrade one or more containers.\n\n**--help**, **-h**\n> Display help information.\n\n**--root**\n> Run distrobox as root (using sudo/doas)."],["DESCRIPTION","**distrobox** allows running any Linux distribution inside a terminal container while tightly integrating with the host OS. Containers share the home directory, storage, and hardware access (including graphics and audio).\n\nBuilt on top of Podman or Docker, it provides seamless access to different distributions' package managers and software. Applications installed inside containers can be exported to appear in the host's application menu."],["INSTALL","```dnf: sudo dnf install distrobox```\n\n```pacman: sudo pacman -S distrobox```\n\n```apk: sudo apk add distrobox```\n\n```zypper: sudo zypper install distrobox```\n\n```brew: brew install distrobox```\n\n```nix: nix profile install nixpkgs#distrobox```"],["SEE ALSO","[distrobox-create](/man/distrobox-create)(1), [distrobox-enter](/man/distrobox-enter)(1), [distrobox-list](/man/distrobox-list)(1), [distrobox-rm](/man/distrobox-rm)(1), [distrobox-stop](/man/distrobox-stop)(1), [distrobox-upgrade](/man/distrobox-upgrade)(1), [distrobox-export](/man/distrobox-export)(1), [distrobox-host-exec](/man/distrobox-host-exec)(1), [podman](/man/podman)(1), [docker](/man/docker)(1)"]],"e":["distrobox-create","distrobox-enter","distrobox-list","distrobox-rm","distrobox-stop","distrobox-upgrade","distrobox-export","distrobox-host-exec","podman","docker"],"x":"distrobox create --name [mycontainer] --image [ubuntu:latest]"},"dump":{"n":"dump","t":"ext2/ext3/ext4 filesystem backup utility","s":[["TAGLINE","ext2/ext3/ext4 filesystem backup utility"],["TLDR","**Backup filesystem to file**\n\n```dump -0uf [/backup/root.dump] [/dev/sda1]```\n\n**Backup with compression**\n\n```dump -0uf - [/] | gzip > [backup.dump.gz]```\n\n**Incremental backup (level 1)**\n\n```dump -1uf [/backup/root.1.dump] [/]```\n\n**Backup to tape device**\n\n```dump -0uf [/dev/st0] [/home]```\n\n**List files in dump archive** (using restore)\n\n```restore -tf [backup.dump]```"],["SYNOPSIS","**dump** [_options_] _filesystem_"],["PARAMETERS","**-0** through **-9**\n> Dump level; 0 is full, 1-9 are incremental.\n\n**-u**\n> Update /etc/dumpdates after successful dump.\n\n**-f** _file_\n> Output file or device.\n\n**-a**\n> Auto-size; bypass tape length calculation.\n\n**-h** _level_\n> Honor nodump flag for levels above this.\n\n**-L** _label_\n> Label for dump volume.\n\n**-z** _level_\n> Compress output with zlib.\n\n**-M**\n> Multi-volume; prompt for new tape."],["CONFIGURATION","**/etc/dumpdates**\n> Records dump history tracking dump levels and timestamps for incremental backup management.\n\n**/etc/fstab**\n> Defines filesystem mount points that dump uses to identify filesystems by name rather than device."],["DESCRIPTION","**dump** creates backups of ext2/ext3/ext4 filesystems. It operates at the filesystem level, reading disk blocks directly, which provides complete backups including file metadata and special files.\n\nDump supports incremental backups through levels 0-9. Level 0 is a full backup; higher levels back up files changed since the last backup at a lower level. The /etc/dumpdates file tracks backup history."],["DUMP LEVELS","```\nLevel 0: Full backup (all files)\nLevel 1: Files changed since level 0\nLevel 2: Files changed since level 1\n...and so on\n```\n\nCommon strategy: Weekly level 0, daily level 1."],["CAVEATS","Filesystem-specific; works with ext2/3/4 but not XFS or Btrfs. Cannot back up mounted filesystems reliably in all cases. Use **restore** command to recover files. Modern alternatives like rsync or borg may be preferred."],["HISTORY","dump dates back to early Unix at **Bell Labs** in the **1970s**. It was one of the original backup utilities, designed for tape backups. The Linux version (dump/restore) was adapted from 4.4BSD and continues the traditional interface while supporting modern ext filesystems."],["INSTALL","```apt: sudo apt install dump```\n\n```dnf: sudo dnf install dump```\n\n```zypper: sudo zypper install dump```\n\n```nix: nix profile install nixpkgs#dump```"],["SEE ALSO","[tar](/man/tar)(1), [rsync](/man/rsync)(1), [dd](/man/dd)(1)"]],"e":["tar","rsync","dd"],"x":"dump -0uf [/backup/root.dump] [/dev/sda1]"},"edky":{"n":"edky","t":"Convert Ed25519 public keys between encodings","s":[["TAGLINE","Convert Ed25519 public keys between encodings"],["TLDR","**List** supported public key encoding formats\n\n```edky list```\n\n**Convert** an Iroh key to a libp2p/multibase key\n\n```edky convert -f iroh -t libp2p [key]```\n\n**Convert** a libp2p key back to Iroh (base32z)\n\n```edky convert -f libp2p -t iroh [key]```\n\n**Convert** a NEAR key to raw hex\n\n```edky convert -f near -t hex [key]```\n\n**Convert** hex to a NEAR-style key\n\n```edky convert -f hex -t near [key]```\n\n**Parse** a key in a given encoding\n\n```edky parse -f openssh [key]```"],["SYNOPSIS","**edky** [_options_] [_command_] [_args_]"],["DESCRIPTION","**edky** (pronounced \"ed-key\") converts **Ed25519** public keys between common encoding formats used across cryptography and peer-to-peer stacks. It is available as a command-line tool and as libraries for Rust, TypeScript/JavaScript, Dart, Python, and Ruby.\n\nSupported encodings for **--from** / **--to** include **hex** / **base16**, **base32z**, **base58**, **base64**, **base64url**, **multibase**, and ecosystem aliases such as **iroh**, **libp2p**, **ipfs**, **near**, **openssh**, and **asimov**. Subcommands are **list** (enumerate formats), **convert** (re-encode keys), and **parse** (validate and inspect keys).\n\nInstall the CLI via Cargo with the **cli** feature (**cargo install edky --locked --features=cli**) or **cargo binstall edky**. Default input and output encoding for convert/parse is **hex** when not specified."],["PARAMETERS","**list**\n> Print supported public key encoding format names.\n\n**convert** [**-f** _FROM_] [**-t** _TO_] [_INPUTS_…]\n> Convert each input key from encoding _FROM_ to encoding _TO_. Defaults: **hex** → **hex**.\n\n**parse** [**-f** _FROM_] [_INPUTS_…]\n> Parse keys in encoding _FROM_ (default **hex**).\n\n**-f**, **--from** _FORMAT_\n> Input encoding format.\n\n**-t**, **--to** _FORMAT_\n> Output encoding format (**convert** only).\n\n**-d**, **--debug**\n> Enable debugging output.\n\n**-v**, **--verbose**\n> Increase verbosity (repeatable).\n\n**-V**, **--version**\n> Print version information.\n\n**--license**\n> Show license information.\n\n**--color** _auto|always|never_\n> Control color output (default **auto**).\n\n**-h**, **--help**\n> Print help."],["CAVEATS","Operates on **public** keys only; it does not convert private keys or sign data. Encoding aliases may map to the same wire format (for example **iroh** and **base32z**, or **libp2p** / **ipfs** / **multibase** for multibase-encoded DID keys). Invalid input length or alphabet for the chosen format causes an error."],["HISTORY","**edky** is public-domain (Unlicense) software by Arto Bendiken, aimed at interop among ASIMOV, IPFS, Iroh, libp2p, NEAR, OpenSSH, and related Ed25519 encodings."],["SEE ALSO","[ssh-keygen](/man/ssh-keygen)(1), [age-keygen](/man/age-keygen)(1), [openssl](/man/openssl)(1)"],["RESOURCES","```[Source code](https://github.com/artob/edky)```\n\n```[Homepage](https://edky.dev)```"]],"e":["ssh-keygen","age-keygen","openssl"],"x":"edky list"},"faker":{"n":"faker","t":"localized fake data generation library CLI","s":[["TAGLINE","localized fake data generation library CLI"],["TLDR","**Generate fake name**\n\n```faker name```\n\n**Generate fake email**\n\n```faker email```\n\n**Generate with locale**\n\n```faker -l [de_DE] address```\n\n**Multiple outputs**\n\n```faker -r [10] name```\n\n**Generate specific type**\n\n```faker phone_number```"],["SYNOPSIS","**faker** [_options_] _provider_ [_method_]"],["PARAMETERS","_PROVIDER_\n> Data provider: name, address, email, phone_number, etc.\n\n**-l** _LOCALE_, **--locale** _LOCALE_\n> Locale for generated data (e.g., en_US, de_DE, ja_JP).\n\n**-r** _COUNT_, **--repeat** _COUNT_\n> Number of values to generate.\n\n**-s** _SEED_, **--seed** _SEED_\n> Random seed for reproducibility.\n\n**-o** _FORMAT_, **--output** _FORMAT_\n> Output format: csv, json, tab.\n\n**--help**\n> Display help information."],["DESCRIPTION","**faker** is a command-line tool for generating fake data like names, addresses, emails, and phone numbers. It's part of the Faker Python library and supports localized data for many countries.\n\nThe tool provides numerous data providers covering personal information, company details, internet data, dates, and more. Localization ensures generated data matches regional formats and naming conventions.\n\nfaker is widely used for testing, database seeding, and development when realistic but non-sensitive data is needed."],["CAVEATS","Generated data may accidentally match real information. Quality varies by locale. Some providers may be incomplete for certain regions."],["HISTORY","Faker originated as a **PHP library** inspired by Perl's Data::Faker. The Python version by **joke2k** became widely popular, providing the faker CLI and extensive provider ecosystem for test data generation."],["INSTALL","```apk: sudo apk add py3-faker```\n\n```brew: brew install faker```"],["SEE ALSO","[python](/man/python)(1), [pip](/man/pip)(1)"]],"e":["python","pip"],"x":"faker name"},"fg":{"n":"fg","t":"bring job to foreground","s":[["TAGLINE","bring job to foreground"],["TLDR","**Resume** the current job in the foreground\n\n```fg```\n\n**Resume** job number 1\n\n```fg %[1]```\n\n**Resume** the job whose command starts with a string\n\n```fg %[vim]```\n\n**Resume** the job whose command contains a string\n\n```fg %?[config]```\n\n**Resume** the previous job\n\n```fg %-```"],["SYNOPSIS","**fg** [_jobspec_]"],["PARAMETERS","_jobspec_\n> The job to resume. Defaults to the current job when omitted.\n\n**%N**\n> Job number N.\n\n**%string**\n> The job whose command line starts with _string_.\n\n**%?string**\n> The job whose command line contains _string_.\n\n**%%** or **%+**\n> The current job: the one most recently suspended or backgrounded.\n\n**%-**\n> The previous job."],["DESCRIPTION","**fg** resumes a suspended or background job, brings it into the foreground and makes it the terminal's controlling process, so it receives your keystrokes and signals again. The shell waits for the job to finish or stop before printing another prompt.\n\nJobs reach the background either by suspending a running command with **Ctrl+Z**, or by starting it with a trailing **&**. **jobs** lists them with their numbers and status; **fg** picks one back up and **bg** restarts one in the background instead. With no argument, **fg** resumes the current job, the one marked **+** in the **jobs** output.\n\nThe **%** prefix is a job specification, not a process ID. It is interpreted by the shell against its own job table, which is why job numbers are meaningful only inside the shell that started them.\n\nThe exit status of **fg** is that of the resumed command, or non-zero if job control is disabled or the job does not exist."],["CAVEATS","**fg** requires job control, which is enabled by default in interactive shells but not in scripts. In a non-interactive shell it fails with \"no job control\"; **set -m** can enable it if the shell supports doing so.\n\nJob numbers are reused as jobs finish, so a **%N** that was correct a moment ago can refer to a different job later. Prefer **%string** in anything you type from memory.\n\nA backgrounded job keeps running and can still write to your terminal, interleaving its output with your prompt. A job stopped by **Ctrl+Z** is genuinely paused (**SIGTSTP**) and makes no progress until it is resumed.\n\nIf a background job tries to read from the terminal it is stopped with **SIGTTIN**, showing as \"stopped (tty input)\"; bring it forward with **fg** to let it read.\n\nJobs removed from the table with **disown** can no longer be reached by **fg**."],["HISTORY","Job control originated in the **C shell** at Berkeley around **1980**, and was later adopted by **ksh**, **bash** and other Bourne shell derivatives. **fg** is standardized by **POSIX** as part of the optional User Portability Utilities job control feature. It is necessarily a shell builtin, since only the shell knows its own job table."],["SEE ALSO","[bg](/man/bg)(1), [jobs](/man/jobs)(1), [disown](/man/disown)(1), [wait](/man/wait)(1), [kill](/man/kill)(1), [nohup](/man/nohup)(1), [bash](/man/bash)(1)"],["RESOURCES","```[Documentation](https://www.gnu.org/software/bash/manual/bash.html#Job-Control-Builtins)```"]],"e":["bg","jobs","disown","wait","kill","nohup","bash"],"x":"fg"},"frida":{"n":"frida","t":"Dynamic instrumentation toolkit for developers and security researchers","s":[["TAGLINE","Dynamic instrumentation toolkit for developers and security researchers"],["TLDR","**Attach** to a running process by name\n\n```frida [process_name]```\n\n**Attach** to a process by **PID**\n\n```frida -p [pid]```\n\n**Spawn** a new process and instrument it\n\n```frida -f [/path/to/binary]```\n\n**Load** a JavaScript instrumentation script\n\n```frida -l [script.js] [process_name]```\n\n**Attach** to an app on a **USB-connected** device\n\n```frida -U -n [app_name]```\n\n**Spawn** an app on a USB device and **pause** the main thread\n\n```frida -U -f [com.example.app] --pause```\n\n**Evaluate** inline JavaScript code against a process\n\n```frida -e \"console.log('hello')\" [process_name]```"],["SYNOPSIS","**frida** [_options_] [_target_]"],["PARAMETERS","**-f** _TARGET_, **--file** _TARGET_\n> Spawn FILE as a new process\n\n**-F**, **--attach-frontmost**\n> Attach to the frontmost application\n\n**-n** _NAME_, **--attach-name** _NAME_\n> Attach to process by name\n\n**-p** _PID_, **--attach-pid** _PID_\n> Attach to process by PID\n\n**-U**, **--usb**\n> Connect to USB device\n\n**-R**, **--remote**\n> Connect to remote frida-server\n\n**-H** _HOST_, **--host** _HOST_\n> Connect to remote frida-server on HOST\n\n**-D** _ID_, **--device** _ID_\n> Connect to device with the given ID\n\n**-l** _SCRIPT_, **--load** _SCRIPT_\n> Load JavaScript SCRIPT (may be specified multiple times)\n\n**-e** _CODE_, **--eval** _CODE_\n> Evaluate JavaScript CODE\n\n**-c** _URI_, **--codeshare** _URI_\n> Load script from Frida CodeShare\n\n**-C** _CMODULE_, **--cmodule** _CMODULE_\n> Load a C module\n\n**-P** _JSON_, **--parameters** _JSON_\n> Parameters as JSON, same as Gadget\n\n**--runtime** {**qjs**,**v8**}\n> Script runtime to use\n\n**--debug**\n> Enable Node.js compatible script debugger\n\n**-q**, **--quiet**\n> Quiet mode (no prompt), quit after -l and -e\n\n**-t** _SECS_, **--timeout** _SECS_\n> Seconds to wait before terminating in quiet mode\n\n**-o** _FILE_, **--output** _FILE_\n> Output to log file\n\n**--pause**\n> Leave main thread paused after spawning program\n\n**--kill-on-exit**\n> Kill the spawned program when Frida exits\n\n**--eternalize**\n> Eternalize the script before exit\n\n**--auto-perform**\n> Wrap entered code with Java.perform\n\n**--no-auto-reload**\n> Disable auto reload of scripts on file change"],["DESCRIPTION","**frida** is the interactive REPL (Read-Eval-Print Loop) interface to the Frida dynamic instrumentation toolkit. It allows injecting JavaScript snippets into running processes for runtime analysis, debugging, and modification. You can attach to running processes, spawn new ones, load scripts, hook functions, and inspect or modify application behavior at runtime.\n\nFrida works across **Windows**, **macOS**, **GNU/Linux**, **iOS**, **Android**, **FreeBSD**, and **QNX**. For remote targets (phones, embedded devices), a **frida-server** binary must be running on the target device."],["CAVEATS","Injecting into other processes typically requires **root** or elevated privileges. On Android, frida-server must run as root. On iOS, a jailbreak is typically needed. Many applications (especially mobile banking and DRM-protected apps) implement Frida detection mechanisms and may terminate when instrumentation is detected. Bugs in injected scripts can crash the target process. Frida should only be used on software you own or have explicit authorization to test."],["HISTORY","Frida was created by **Ole Andre Vadla Ravnas** who began building a generic code instrumentation library in C around **2008**. The project was publicly released in **2014**, with Frida 1.6.0 (May 2014) adding Android support. The project is sponsored by **NowSecure**. Today Frida supports bindings for Node.js, Python, Swift, .NET, Go, and C, and is a standard tool referenced in the **OWASP Mobile Security Testing Guide**."],["SEE ALSO","[frida-ps](/man/frida-ps)(1), [frida-trace](/man/frida-trace)(1), [gdb](/man/gdb)(1), [strace](/man/strace)(1)"]],"e":["frida-ps","frida-trace","gdb","strace"],"x":"frida [process_name]"},"fuser":{"n":"fuser","t":"identify processes using files or sockets","s":[["TAGLINE","identify processes using files or sockets"],["TLDR","Find processes accessing a **file or directory**\n\n```fuser path/to/file```\n\nShow **verbose** output with user and command\n\n```fuser -v path/to/file```\n\nFind processes using a **TCP port**\n\n```fuser 80/tcp```\n\nFind processes using a **UDP port**\n\n```fuser 53/udp```\n\n**Kill** processes accessing a file\n\n```fuser -k path/to/file```\n\nFind processes accessing a **mounted filesystem**\n\n```fuser -m /mnt/usb```\n\nKill processes on a **TCP port**\n\n```fuser -k 8080/tcp```"],["SYNOPSIS","**fuser** [_-fuv_] [_-a_|_-s_] [_-4_|_-6_] [_-c_|_-m_|_-n space_] [_-k_ [_-i_] [_-SIGNAL_]] _name_..."],["DESCRIPTION","**fuser** displays the PIDs of processes using specified files or file systems. It can identify which processes have files open, executables running, or sockets in use, and optionally terminate them."],["ACCESS TYPE INDICATORS","**c**: Current directory\n**e**: Executable being run\n**f**: Open file\n**F**: Open file for writing\n**r**: Root directory\n**m**: Memory-mapped file or shared library"],["PARAMETERS","**-a, --all**\n> Display all specified files, including those not accessed\n\n**-k, --kill**\n> Kill processes accessing the file (SIGKILL by default)\n\n**-i, --interactive**\n> Ask for confirmation before killing\n\n**-m, --mount NAME**\n> Target processes accessing a mounted filesystem\n\n**-M, --ismountpoint**\n> Only match if target is a mount point\n\n**-n, --namespace SPACE**\n> Select namespace: file, tcp, or udp\n\n**-u, --user**\n> Include process owner usernames\n\n**-v, --verbose**\n> Display processes in ps-like format\n\n**-s, --silent**\n> Operate silently\n\n**-4, --ipv4**\n> Search IPv4 sockets only\n\n**-6, --ipv6**\n> Search IPv6 sockets only\n\n**-SIGNAL**\n> Send specified signal instead of SIGKILL\n\n**-l, --list-signals**\n> List available signal names"],["CAVEATS","The **-k** option kills processes with SIGKILL by default, which cannot be caught. Use **-i** to confirm before killing. Root privileges may be needed to see all processes."],["HISTORY","**fuser** is part of the **psmisc** package, providing tools for managing processes by file access."],["INSTALL","```apt: sudo apt install psmisc```\n\n```dnf: sudo dnf install psmisc```\n\n```pacman: sudo pacman -S psmisc```\n\n```apk: sudo apk add psmisc```\n\n```zypper: sudo zypper install psmisc```\n\n```nix: nix profile install nixpkgs#psmisc```"],["SEE ALSO","[lsof](/man/lsof)(8), [kill](/man/kill)(1), [killall](/man/killall)(1), [ps](/man/ps)(1)"],["RESOURCES","```[Source code](https://gitlab.com/psmisc/psmisc)```"]],"e":["lsof","kill","killall","ps"],"x":"fuser path/to/file"},"git-count-objects":{"n":"git-count-objects","t":"Report repository object counts and sizes","s":[["TAGLINE","Report repository object counts and sizes"],["TLDR","**Count objects**\n\n```git count-objects```\n\n**Verbose count**\n\n```git count-objects -v```\n\n**Human readable sizes**\n\n```git count-objects -vH```"],["SYNOPSIS","**git count-objects** [_options_]"],["PARAMETERS","**-v**, **--verbose**\n> Show detailed breakdown.\n\n**-H**, **--human-readable**\n> Human-readable sizes.\n\n**--help**\n> Display help information."],["DESCRIPTION","**git count-objects** reports the number of unpacked object files and their disk consumption, to help decide when it is a good time to repack.\n\nThe verbose mode (**-v**) shows a detailed breakdown: count (loose objects), size (disk space of loose objects in KiB), in-pack (number of packed objects), packs (number of pack files), size-pack (disk space of packs in KiB), prune-packable (loose objects also present in packs), and garbage (files in the object directory that are neither valid loose objects nor valid packs).\n\nThe **-H** flag presents sizes in human-readable format (KiB, MiB, GiB), making it easier to assess storage at a glance without manual unit conversion."],["CAVEATS","Core git command. Unpacked objects indicate recent activity. Large counts may indicate need for gc."],["HISTORY","git count-objects is a core **Git** command for repository maintenance, helping administrators understand storage usage."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-gc](/man/git-gc)(1), [git-prune](/man/git-prune)(1), [git-fsck](/man/git-fsck)(1)"]],"e":["git-gc","git-prune","git-fsck"],"x":"git count-objects"},"gitbook":{"n":"gitbook","t":"Build books and documentation from Markdown","s":[["TAGLINE","Build books and documentation from Markdown"],["TLDR","**Initialize new book**\n\n```gitbook init```\n\n**Serve book locally**\n\n```gitbook serve```\n\n**Build static site**\n\n```gitbook build```\n\n**Build to specific output directory**\n\n```gitbook build [.] [output_dir]```\n\n**Serve with a specific version**\n\n```gitbook build --gitbook=[2.0.1]```\n\n**Install plugins**\n\n```gitbook install```\n\n**List locally installed versions**\n\n```gitbook ls```\n\n**List remote versions available** on NPM\n\n```gitbook ls-remote```\n\n**Install a specific version**\n\n```gitbook fetch [version]```"],["SYNOPSIS","**gitbook** _command_ [_options_]"],["PARAMETERS","**init**\n> Initialize book structure with README.md and SUMMARY.md.\n\n**serve**\n> Build and serve book on localhost:4000.\n\n**build**\n> Build static website.\n\n**install**\n> Install plugins from book.json.\n\n**pdf**\n> Generate PDF (requires Calibre).\n\n**epub**\n> Generate ePub.\n\n**mobi**\n> Generate Mobi.\n\n**ls**\n> List locally installed GitBook versions.\n\n**ls-remote**\n> List remote versions available on NPM.\n\n**fetch** _version_\n> Download and install a specific version.\n\n**update**\n> Update to the latest GitBook version.\n\n**uninstall** _version_\n> Remove a specific installed version.\n\n**--gitbook** _version_\n> Force a specific GitBook version for the command.\n\n**-d**, **--debug**\n> Enable verbose error output with stack traces.\n\n**--log** _level_\n> Log level: debug, info, warn, error, disabled."],["DESCRIPTION","**gitbook** is a command-line tool for building books and documentation from Markdown files. It generates static HTML sites or other formats like PDF and ePub.\n\nThe tool uses a `book.json` configuration file and organizes content through `SUMMARY.md` for structure. Plugins extend functionality with custom features."],["CONFIGURATION","**book.json**\n> Project configuration file defining title, description, plugins, and build settings.\n\n**GITBOOK_DIR**\n> Environment variable to specify custom directory for storing GitBook versions (default: ~/.gitbook)."],["CAVEATS","Legacy CLI is no longer under active development. Requires older Node.js versions (may not work with Node 12+). Only supports gitbook versions >=2.0.0. Plugins may be unmaintained. PDF/ePub/Mobi generation requires Calibre."],["HISTORY","**GitBook** was created by **Samy Pesse** and **Aaron O'Mullan** around **2014**. It became popular for technical documentation and books. The company pivoted to a commercial platform, leaving the CLI as a legacy tool. Community forks continue development."],["SEE ALSO","[mdbook](/man/mdbook)(1), [mkdocs](/man/mkdocs)(1), [hugo](/man/hugo)(1), [pandoc](/man/pandoc)(1)"]],"e":["mdbook","mkdocs","hugo","pandoc"],"x":"gitbook init"},"gnucash":{"n":"gnucash","t":"personal and small-business financial accounting","s":[["TAGLINE","personal and small-business financial accounting"],["TLDR","**Launch GnuCash**\n\n```gnucash```\n\n**Open a specific file**\n\n```gnucash [file.gnucash]```\n\n**Launch without loading last file**\n\n```gnucash --nofile```\n\n**Enable debugging mode**\n\n```gnucash --debug```\n\n**Show version**\n\n```gnucash --version```\n\n**Enable extra features**\n\n```gnucash --extra```"],["SYNOPSIS","**gnucash** [_options_] [_datafile_]"],["PARAMETERS","**-h**, **--help**\n> Show help message.\n\n**-v**, **--version**\n> Show GnuCash version.\n\n**--nofile**\n> Do not load last opened file.\n\n**--debug**\n> Enable debugging mode.\n\n**--extra**\n> Enable extra/development features.\n\n**--log** _arg_\n> Log level overrides (e.g., \"modulename=debug\").\n\n**--logto** _file_\n> File to log into.\n\n**--help-gtk**\n> Show GTK options."],["DESCRIPTION","**GnuCash** is a personal and small-business financial accounting application. It supports double-entry bookkeeping, stock/bond/mutual fund accounts, scheduled transactions, and financial reports. **GnuCash** can import OFX, QIF, and CSV files and track income, expenses, bank accounts, and investments."],["CONFIGURATION","**~/.local/share/gnucash/**\n> User data directory containing saved reports, custom settings, and metadata.\n\n**~/.config/gnucash/**\n> User configuration directory for preferences and UI state."],["INSTALL","```apt: sudo apt install gnucash```\n\n```dnf: sudo dnf install gnucash```\n\n```pacman: sudo pacman -S gnucash```\n\n```apk: sudo apk add gnucash```\n\n```zypper: sudo zypper install gnucash```\n\n```nix: nix profile install nixpkgs#gnucash```"],["SEE ALSO","[gnucash-cli](/man/gnucash-cli)(1), [ledger](/man/ledger)(1), [hledger](/man/hledger)(1)"],["RESOURCES","```[Source code](https://github.com/Gnucash/gnucash)```\n\n```[Homepage](https://www.gnucash.org/)```"]],"e":["gnucash-cli","ledger","hledger"],"x":"gnucash"},"gobuster":{"n":"gobuster","t":"Directory and DNS brute-forcing tool","s":[["TAGLINE","Directory and DNS brute-forcing tool"],["TLDR","**Directory brute-force**\n\n```gobuster dir -u [https://example.com] -w [wordlist.txt]```\n\n**DNS subdomain enumeration**\n\n```gobuster dns -d [example.com] -w [subdomains.txt]```\n\n**Vhost discovery**\n\n```gobuster vhost -u [https://example.com] -w [wordlist.txt]```\n\n**With file extensions**\n\n```gobuster dir -u [https://example.com] -w [wordlist.txt] -x [php,html,txt]```\n\n**With custom threads**\n\n```gobuster dir -u [https://example.com] -w [wordlist.txt] -t [50]```\n\n**Follow redirects**\n\n```gobuster dir -u [https://example.com] -w [wordlist.txt] -r```"],["SYNOPSIS","**gobuster** _mode_ [_options_]"],["PARAMETERS","**dir**\n> Directory/file brute-forcing mode.\n\n**dns**\n> DNS subdomain enumeration mode.\n\n**vhost**\n> Virtual host discovery mode.\n\n**fuzz**\n> Generic fuzzing mode using a **FUZZ** keyword placeholder in the URL, headers, or body.\n\n**s3**\n> Enumerate open Amazon S3 buckets.\n\n**gcs**\n> Enumerate open Google Cloud Storage buckets.\n\n**tftp**\n> Enumerate files on a TFTP server.\n\n**-u** _url_\n> Target URL.\n\n**-w** _file_\n> Wordlist file.\n\n**-t** _n_\n> Number of threads.\n\n**-x** _extensions_\n> File extensions to search.\n\n**-r**\n> Follow redirects.\n\n**-k**\n> Skip TLS certificate verification.\n\n**-o** _file_\n> Output file.\n\n**-s** _codes_\n> Status codes to consider valid.\n\n**-b** _codes_\n> Status codes to blacklist."],["DESCRIPTION","**gobuster** is a fast directory and DNS brute-forcing tool written in Go. It discovers hidden directories, files, and subdomains through wordlist-based enumeration.\n\nThe tool is designed for web application security testing, helping identify hidden content that may reveal vulnerabilities or sensitive information. It supports multiple modes including directory enumeration, DNS subdomain brute-forcing, and virtual host discovery."],["CAVEATS","Only for authorized security testing. May trigger WAF rules. Large wordlists slow on remote hosts. Respect rate limits."],["HISTORY","**gobuster** was created by **OJ Reeves** as a fast alternative to tools like DirBuster. Written in Go for concurrency and performance, it became a standard tool in penetration testing and bug bounty hunting."],["INSTALL","```apt: sudo apt install gobuster```\n\n```dnf: sudo dnf install gobuster```\n\n```pacman: sudo pacman -S gobuster```\n\n```brew: brew install gobuster```\n\n```nix: nix profile install nixpkgs#gobuster```"],["SEE ALSO","[dirb](/man/dirb)(1), [ffuf](/man/ffuf)(1), [wfuzz](/man/wfuzz)(1), [feroxbuster](/man/feroxbuster)(1)"],["RESOURCES","```[Source code](https://github.com/OJ/gobuster)```"]],"e":["dirb","ffuf","wfuzz","feroxbuster"],"x":"gobuster dir -u [https://example.com] -w [wordlist.txt]"},"ibmcloud-version":{"n":"ibmcloud-version","t":"displays the IBM Cloud CLI version","s":[["TAGLINE","displays the IBM Cloud CLI version"],["TLDR","**Show version**\n\n```ibmcloud version```\n\n**Show with details**\n\n```ibmcloud version --all```"],["SYNOPSIS","**ibmcloud version** [_options_]"],["PARAMETERS","**--all**\n> Show all version info.\n\n**--help**\n> Display help information."],["DESCRIPTION","**ibmcloud version** displays the IBM Cloud CLI version. It shows the installed CLI version and SDK information.\n\nThe command helps verify installation and check for updates. Use before reporting issues."],["CAVEATS","Simple informational command. Check for updates separately. Part of ibmcloud CLI."],["HISTORY","ibmcloud version is part of the **IBM Cloud** CLI."],["SEE ALSO","[ibmcloud](/man/ibmcloud)(1)"]],"e":["ibmcloud"],"x":"ibmcloud version"},"img2pdf":{"n":"img2pdf","t":"converts images to PDF without re-encoding, preserving the original image","s":[["TAGLINE","converts images to PDF without re-encoding, preserving the original image"],["TLDR","**Convert images to PDF**\n\n```img2pdf [image1.jpg] [image2.png] -o [output.pdf]```\n\n**Convert all images in directory**\n\n```img2pdf [*.jpg] -o [output.pdf]```\n\n**Set page size to A4**\n\n```img2pdf --pagesize [A4] [image.jpg] -o [output.pdf]```\n\n**Fit image to page** while maintaining aspect ratio\n\n```img2pdf --fit [into] --pagesize [A4] [image.jpg] -o [output.pdf]```\n\n**Set specific image size**\n\n```img2pdf --imgsize [150mmx100mm] [image.jpg] -o [output.pdf]```\n\n**Convert with custom page margins**\n\n```img2pdf --border [1cm:1cm] [image.jpg] -o [output.pdf]```\n\n**Set PDF title and author**\n\n```img2pdf --title \"[Document Title]\" --author \"[Author Name]\" [image.jpg] -o [output.pdf]```\n\n**Output a PDF/A-1b compliant document**\n\n```img2pdf --pdfa [image.jpg] -o [output.pdf]```\n\n**Pipe image from stdin**\n\n```cat [image.jpg] | img2pdf -o [output.pdf]```"],["SYNOPSIS","**img2pdf** [_options_] [_images ..._] **-o** _output.pdf_"],["PARAMETERS","**-o**, **--output** _file_\n> Output PDF file (required).\n\n**--pagesize** _size_\n> Page size: A4, Letter, Legal, or WxH (e.g., 210mmx297mm).\n\n**--imgsize** _LxL_\n> Image size on the PDF page: WxH with units (e.g., 150mmx100mm).\n\n**--fit** _mode_\n> Fit mode: into, fill, exact, shrink, enlarge.\n\n**--border** _margin_\n> Page borders/margins (e.g., 1cm, 1cm:2cm).\n\n**--rotation** _angle_\n> Rotate pages: auto, none, ifvalid, 0, 90, 180, 270.\n\n**--title** _string_\n> PDF title metadata.\n\n**--author** _string_\n> PDF author metadata.\n\n**--subject** _string_\n> PDF subject metadata.\n\n**--keywords** _string_\n> PDF keywords metadata.\n\n**--creationdate** _date_\n> PDF creation date.\n\n**--moddate** _date_\n> PDF modification date.\n\n**--viewer-panes** _panes_\n> Initial viewer panes: outlines, thumbs.\n\n**--viewer-initial-page** _n_\n> Initial page to display.\n\n**--viewer-fullscreen**\n> Open in fullscreen mode.\n\n**-D**, **--nodate**\n> Suppress timestamps for deterministic output.\n\n**-S**, **--colorspace** _space_\n> Force colorspace: RGB, L (grayscale).\n\n**-a**, **--auto-orient**\n> Match page orientation to input image.\n\n**--pdfa**\n> Output a PDF/A-1b compliant document.\n\n**--first-frame-only**\n> Convert only the first frame of multi-frame images (e.g., animated GIF, multi-page TIFF).\n\n**--pillow-limit-break**\n> Disable Pillow's decompression bomb size limit for very large images.\n\n**--from-file** _FILE_\n> Read list of input images from FILE (NUL-separated paths).\n\n**-v**, **--verbose**\n> Enable verbose mode, printing messages on standard error."],["DESCRIPTION","**img2pdf** converts images to PDF without re-encoding, preserving the original image quality. Unlike tools that rasterize images, img2pdf embeds the original JPEG, PNG, TIFF, or other image data directly into the PDF structure.\n\nThis lossless approach means a 5MB JPEG becomes approximately a 5MB PDF (plus minimal PDF overhead), with no quality loss. Re-encoding tools would either lose quality or produce much larger files at equivalent quality.\n\nSupported input formats include JPEG, JPEG2000, PNG (including transparency), TIFF, GIF, and others. The tool automatically handles colorspace, bit depth, and multi-page TIFFs.\n\nPage sizing offers flexible options: natural image size (at specified DPI), fixed page sizes with fit modes, or explicit dimensions. The fit modes control how images are placed: **into** fits inside the page, **fill** fills the page (may crop), **exact** stretches to exact size, **shrink** only shrinks larger images, **enlarge** only enlarges smaller images.\n\nMultiple images become multi-page PDFs in argument order. Piping from stdin enables integration with image processing pipelines."],["CAVEATS","Only works with raster images (no vector formats). Some viewers may struggle with large embedded images. Not all PDF features are supported (no encryption, compression control is limited). Pillow limits input image size by default; use **--pillow-limit-break** to override."],["HISTORY","**img2pdf** was created by Johannes Schauer Marin Rodrigues to address the common problem of converting images to PDF without quality loss. Existing tools like ImageMagick would re-encode images, causing quality degradation or size bloat. The tool is written in Python and published as a pip package."],["INSTALL","```pacman: sudo pacman -S img2pdf```\n\n```apk: sudo apk add py3-img2pdf```\n\n```brew: brew install img2pdf```\n\n```nix: nix profile install nixpkgs#img2pdf```"],["SEE ALSO","[convert](/man/convert)(1), [pdftk](/man/pdftk)(1), [pdfunite](/man/pdfunite)(1), [tesseract](/man/tesseract)(1)"]],"e":["convert","pdftk","pdfunite","tesseract"],"x":"img2pdf [image1.jpg] [image2.png] -o [output.pdf]"},"iwinfo":{"n":"iwinfo","t":"OpenWrt utility for retrieving wireless interface information","s":[["TAGLINE","OpenWrt utility for retrieving wireless interface information"],["TLDR","**List** all wireless interfaces\n\n```iwinfo```\n\nDisplay **detailed information** for an interface\n\n```iwinfo [interface] info```\n\n**Scan** for nearby networks\n\n```iwinfo [interface] scan```\n\nList **connected devices**\n\n```iwinfo [interface] assoclist```\n\nList supported **channels**\n\n```iwinfo [interface] freqlist```\n\nList available **transmit power** levels\n\n```iwinfo [interface] txpowerlist```"],["SYNOPSIS","**iwinfo** [_interface_] [_command_]"],["PARAMETERS","**info**\n> Display detailed interface information\n\n**scan**\n> Scan for nearby wireless networks\n\n**assoclist**\n> List associated (connected) clients\n\n**freqlist**\n> List supported frequencies/channels\n\n**txpowerlist**\n> List available transmit power levels\n\n**countrylist**\n> List supported country codes"],["DESCRIPTION","**iwinfo** is an OpenWrt utility for retrieving wireless interface information. It provides a unified interface to query wireless driver capabilities and status, abstracting differences between various wireless drivers.\n\nThe tool displays connection status, signal strength, encryption type, and other wireless parameters. It's particularly useful for monitoring access point clients and scanning for networks."],["CAVEATS","Specific to OpenWrt/LEDE systems. Output format may vary depending on the wireless driver. Some features require specific driver support."],["HISTORY","iwinfo was developed for OpenWrt to provide a consistent interface for wireless information across different hardware and drivers used in router firmware."],["INSTALL","```aur: yay -S iwinfo```"],["SEE ALSO","[iw](/man/iw)(8), [uci](/man/uci)(1)"]],"e":["iw","uci"],"x":"iwinfo"},"kubetail":{"n":"kubetail","t":"tails logs from multiple pods simultaneously","s":[["TAGLINE","tails logs from multiple pods simultaneously"],["TLDR","**Tail pods matching a name pattern**\n\n```kubetail [pod-name-prefix]```\n\n**Tail pods by label selector**\n\n```kubetail -l [app=myapp]```\n\n**Tail in a specific namespace**\n\n```kubetail [pattern] -n [namespace]```\n\n**Tail a specific container** (repeatable)\n\n```kubetail [pattern] -c [container]```\n\n**Show only the last N minutes of logs**\n\n```kubetail [pattern] -s [10m]```\n\n**Show timestamps**\n\n```kubetail [pattern] --timestamps```\n\n**Tail with regex matching**\n\n```kubetail \"[^app1|.*demo.*]\" --regex```\n\n**Dry-run: list matching pods without tailing**\n\n```kubetail [pattern] -d```"],["SYNOPSIS","**kubetail** [_pod-pattern_] [_options_]"],["PARAMETERS","_PATTERN_\n> Pod name pattern (prefix match by default, regex with --regex).\n\n**-c**, **--container** _NAME_\n> Container name (repeatable for multiple containers).\n\n**-t**, **--context** _CONTEXT_\n> Kubernetes context to use.\n\n**-l**, **--selector** _SELECTOR_\n> Label selector (e.g., app=myapp).\n\n**-n**, **--namespace** _NAMESPACE_\n> Target namespace.\n\n**-e**, **--regex** _MODE_\n> Match mode: regex or substring.\n\n**-s**, **--since** _DURATION_\n> Show logs since relative duration (e.g., 10m, 1h).\n\n**-p**, **--previous** _BOOL_\n> Retrieve logs from previous pod instances.\n\n**-f**, **--follow** _BOOL_\n> Follow log stream (default true).\n\n**-d**, **--dry-run**\n> Print matching pods/containers without tailing.\n\n**-k**, **--colored-output** _MODE_\n> Coloring mode: pod, line (default), loglevel, or false.\n\n**-z**, **--skip-colors** _LIST_\n> Comma-separated color indices to skip.\n\n**-P**, **--prefix** _BOOL_\n> Toggle pod name prefix on each line.\n\n**-b**, **--line-buffered** _BOOL_\n> Control line buffering.\n\n**-j**, **--jq** _SELECTOR_\n> Apply jq selector to JSON log lines.\n\n**-r**, **--cluster** _NAME_\n> Kubeconfig cluster name.\n\n**--tail** _LINES_\n> Limit number of recent lines per pod.\n\n**--timestamps**\n> Display timestamps on log lines.\n\n**-i**, **--show-color-index** _BOOL_\n> Display color index with pod prefix.\n\n**-v**, **--version**\n> Show version.\n\n**-h**, **--help**\n> Display help information."],["DESCRIPTION","**kubetail** is a Bash script that aggregates and tails log output from multiple Kubernetes pods simultaneously. It matches pods by name prefix, regex, or label selector and streams their logs in a single terminal, using color-coded output to distinguish lines from different pods.\n\nThe tool is useful when debugging distributed applications or microservices where related log entries are spread across several pod replicas. Under the hood, kubetail spawns multiple `kubectl logs --follow` processes and merges their output, optionally filtering by namespace, container, or duration.\n\nDefaults for most flags can be set via environment variables such as KUBETAIL_NAMESPACE, KUBETAIL_SINCE, KUBETAIL_TAIL, and KUBETAIL_COLORED_OUTPUT."],["CAVEATS","Third-party Bash script (not part of kubectl). Requires `kubectl` configured with cluster access. For Go-based alternatives with similar functionality, see stern."],["HISTORY","kubetail was created by Johan Haleby to simplify tailing logs from multiple Kubernetes pods with a single command."],["INSTALL","```brew: brew install kubetail```\n\n```nix: nix profile install nixpkgs#kubetail```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [stern](/man/stern)(1), [k9s](/man/k9s)(1)"]],"e":["kubectl","stern","k9s"],"x":"kubetail [pod-name-prefix]"},"lazysql":{"n":"lazysql","t":"Cross-platform TUI database management tool","s":[["TAGLINE","Cross-platform TUI database management tool"],["TLDR","**Launch the database TUI**\n\n```lazysql```\n\n**Connect to a database**\n\n```lazysql -u [connection_string]```"],["SYNOPSIS","**lazysql** [_options_]"],["DESCRIPTION","**lazysql** is a cross-platform TUI database management tool that supports MySQL, PostgreSQL, and SQLite. It provides an SQL query editor with syntax highlighting, clipboard support, tab-based navigation, and Vim-style keybindings.\n\nFeatures include an interactive query editor, sortable result tables, clipboard integration for copying query results or cell values, and a user-friendly interface for browsing database schemas and executing queries without leaving the terminal."],["CAVEATS","Requires a running database server for MySQL and PostgreSQL connections. SQLite works with local files."],["HISTORY","**lazysql** was created by **Jorge Rojas** (jorgerojas26) and is written in **Go**. It was inspired by lazygit."],["INSTALL","```brew: brew install lazysql```\n\n```nix: nix profile install nixpkgs#lazysql```"],["SEE ALSO","[mysql](/man/mysql)(1), [psql](/man/psql)(1), [sqlite3](/man/sqlite3)(1)"]],"e":["mysql","psql","sqlite3"],"x":"lazysql"},"libtoolize":{"n":"libtoolize","t":"prepares a package for using libtool by copying necessary auxiliary files into","s":[["TAGLINE","prepares a package for using libtool by copying necessary auxiliary files into"],["TLDR","Initialize project for **libtool** with copy and force\n\n```libtoolize --copy --force```"],["SYNOPSIS","**libtoolize** [_options_]"],["DESCRIPTION","**libtoolize** prepares a package for using libtool by copying necessary auxiliary files (ltmain.sh, config.guess, etc.) into the project directory. It integrates libtool seamlessly into autotools-based build systems."],["PARAMETERS","**-c, --copy**\n> Copy files instead of creating symbolic links\n\n**-f, --force**\n> Overwrite existing files\n\n**-i, --install**\n> Install libtool support files\n\n**-n, --dry-run**\n> Show what would be done without doing it\n\n**--automake**\n> Work silently with automake\n\n**--ltdl**\n> Install libltdl support files"],["CAVEATS","Should be run before autoreconf or aclocal when setting up autotools projects. Creates or modifies m4 macro files."],["INSTALL","```dnf: sudo dnf install libtool```\n\n```pacman: sudo pacman -S libtool```\n\n```apk: sudo apk add libtool```\n\n```zypper: sudo zypper install libtool```\n\n```brew: brew install libtool```\n\n```nix: nix profile install nixpkgs#libtool```"],["SEE ALSO","[libtool](/man/libtool)(1), [autoreconf](/man/autoreconf)(1), [automake](/man/automake)(1)"]],"e":["libtool","autoreconf","automake"],"x":"libtoolize --copy --force"},"lsinitrd":{"n":"lsinitrd","t":"displays the contents of an initramfs image","s":[["TAGLINE","displays the contents of an initramfs image"],["TLDR","Show contents of **current kernel** initramfs\n\n```lsinitrd```\n\nShow contents for **specific kernel**\n\n```lsinitrd --kver [kernel_version]```\n\nShow contents of **specific image**\n\n```lsinitrd [path/to/initramfs.img]```\n\nList **included modules**\n\n```lsinitrd --mod```\n\n**Unpack** initramfs to current directory\n\n```lsinitrd --unpack```"],["SYNOPSIS","**lsinitrd** [_options_] [_image_]"],["DESCRIPTION","**lsinitrd** displays the contents of an initramfs (initial RAM filesystem) image. It lists files, modules, and drivers included in the image, which is essential for troubleshooting boot issues."],["PARAMETERS","**-k, --kver VERSION**\n> Show initramfs for specified kernel version\n\n**-m, --mod**\n> List included kernel modules\n\n**--unpack**\n> Extract initramfs contents to current directory\n\n**-f, --file FILE**\n> Show contents of specific file in image\n\n**-l, --list**\n> List files in initramfs"],["CAVEATS","Part of dracut tools. Initramfs location varies by distribution (/boot or /boot/efi). May require root for some operations."],["INSTALL","```apt: sudo apt install dracut-core```\n\n```apk: sudo apk add dracut-core```"],["SEE ALSO","[dracut](/man/dracut)(8), [mkinitcpio](/man/mkinitcpio)(8), [update-initramfs](/man/update-initramfs)(8)"]],"e":["dracut","mkinitcpio","update-initramfs"],"x":"lsinitrd"},"mqtt_check.py":{"n":"mqtt_check.py","t":"nagios plugin for MQTT monitoring","s":[["TAGLINE","nagios plugin for MQTT monitoring"],["TLDR","**Check MQTT broker**\n\n```mqtt_check.py -H [broker.example.com]```\n\n**Check with authentication**\n\n```mqtt_check.py -H [host] -u [user] -p [password]```\n\n**Check specific port**\n\n```mqtt_check.py -H [host] -P [1883]```\n\n**Check with SSL**\n\n```mqtt_check.py -H [host] --ssl```\n\n**Set timeout**\n\n```mqtt_check.py -H [host] -t [10]```"],["SYNOPSIS","**mqtt_check.py** [_options_]"],["PARAMETERS","**-H** _HOST_\n> MQTT broker hostname.\n\n**-P** _PORT_\n> MQTT broker port.\n\n**-u** _USER_\n> Username for authentication.\n\n**-p** _PASSWORD_\n> Password for authentication.\n\n**--ssl**\n> Use SSL/TLS connection.\n\n**-t** _SECONDS_\n> Connection timeout.\n\n**--help**\n> Display help information."],["DESCRIPTION","**mqtt_check.py** is a Nagios plugin for MQTT monitoring. It checks MQTT broker availability.\n\nThe tool verifies broker connectivity. Returns status codes for monitoring systems."],["CAVEATS","Nagios plugin format. Requires paho-mqtt. Python script."],["HISTORY","mqtt_check.py was created for **Nagios/Icinga** monitoring to provide MQTT broker health checks."],["INSTALL","```apk: sudo apk add py3-impacket```"],["SEE ALSO","[mosquitto](/man/mosquitto)(1), [mosquitto_pub](/man/mosquitto_pub)(1), [nagios](/man/nagios)(1)"]],"e":["mosquitto","mosquitto_pub","nagios"],"x":"mqtt_check.py -H [broker.example.com]"},"mvn-archetype":{"n":"mvn-archetype","t":"creates new Maven projects from templates","s":[["TAGLINE","creates new Maven projects from templates"],["TLDR","**Generate a project interactively** (default)\n\n```mvn archetype:generate```\n\n**Generate with specific archetype**\n\n```mvn archetype:generate -DarchetypeGroupId=[org.apache.maven.archetypes] -DarchetypeArtifactId=[maven-archetype-quickstart]```\n\n**Generate non-interactively with full coordinates**\n\n```mvn archetype:generate -DinteractiveMode=false -DgroupId=[com.example] -DartifactId=[myapp] -DarchetypeArtifactId=[maven-archetype-quickstart]```\n\n**Filter available archetypes by keyword**\n\n```mvn archetype:generate -Dfilter=[spring-boot]```\n\n**Create an archetype from the current project**\n\n```mvn archetype:create-from-project```\n\n**Update the local archetype catalog**\n\n```mvn archetype:update-local-catalog```"],["SYNOPSIS","**mvn archetype:**_goal_ [_-Dproperty=value_ ...]"],["PARAMETERS","**-DarchetypeGroupId=**_id_\n> Group ID of the archetype to use (e.g. `org.apache.maven.archetypes`).\n\n**-DarchetypeArtifactId=**_id_\n> Artifact ID of the archetype (e.g. `maven-archetype-quickstart`).\n\n**-DarchetypeVersion=**_version_\n> Version of the archetype.\n\n**-DarchetypeCatalog=**_catalog_\n> Comma-separated catalog list: `remote`, `local`, `internal` (default: `remote,local`).\n\n**-DgroupId=**_id_\n> Group ID of the generated project.\n\n**-DartifactId=**_id_\n> Artifact ID of the generated project.\n\n**-Dversion=**_version_\n> Version of the generated project (default `1.0-SNAPSHOT`).\n\n**-Dpackage=**_name_\n> Base Java package (defaults to the group ID).\n\n**-DinteractiveMode=**_bool_\n> Prompt for missing values when `true` (default follows `settings.xml`).\n\n**-Dfilter=**_expr_\n> Filter displayed archetypes by `groupId:artifactId` substring.\n\n**-DoutputDirectory=**_dir_\n> Directory where the project is created (default: current directory)."],["GOALS","**archetype:generate**\n> Create a new project from an archetype.\n\n**archetype:create-from-project**\n> Turn the current project into a reusable archetype.\n\n**archetype:crawl**\n> Scan a local Maven repository and build a catalog file.\n\n**archetype:integration-test**\n> Run the archetype's integration tests.\n\n**archetype:jar**\n> Package the current archetype project as a JAR.\n\n**archetype:update-local-catalog**\n> Refresh the local archetype catalog.\n\n**archetype:help**\n> Show plugin help. Use `-Ddetail=true -Dgoal=generate` for details."],["DESCRIPTION","**mvn archetype:generate** creates new Maven projects from templates called archetypes. An archetype bundles a project skeleton, `pom.xml`, and source/resource templates that are expanded with Velocity and user-supplied properties.\n\nCommon archetypes include `maven-archetype-quickstart` (a minimal Java app), `maven-archetype-webapp` (a servlet WAR), and third-party templates such as Spring Boot or Quarkus starters. Archetypes are discovered from remote catalogs, the local repository, and an internal fallback catalog.\n\nWhen run without parameters the plugin enters interactive mode, listing numbered archetypes and prompting for coordinates."],["INSTALL","```dnf: sudo dnf install maven```\n\n```pacman: sudo pacman -S maven```\n\n```apk: sudo apk add maven```\n\n```zypper: sudo zypper install maven```\n\n```brew: brew install maven```\n\n```nix: nix profile install nixpkgs#maven```"],["SEE ALSO","[mvn](/man/mvn)(1), [maven](/man/maven)(1), [mvn-compile](/man/mvn-compile)(1), [mvn-package](/man/mvn-package)(1), [mvn-dependency](/man/mvn-dependency)(1)"]],"e":["mvn","maven","mvn-compile","mvn-package","mvn-dependency"],"x":"mvn archetype:generate"},"ng-build":{"n":"ng-build","t":"Compile an Angular application","s":[["TAGLINE","Compile an Angular application"],["TLDR","**Build for production**\n\n```ng build --configuration production```\n\n**Build with source maps**\n\n```ng build --source-map```\n\n**Build a specific project** in a workspace\n\n```ng build [project-name]```\n\n**Watch mode** to rebuild on file changes\n\n```ng build --watch```\n\n**Set output path**\n\n```ng build --output-path [dist/custom]```\n\n**Build with a custom base href**\n\n```ng build --base-href [/my-app/]```\n\n**Generate stats JSON** for bundle analysis\n\n```ng build --stats-json```\n\n**Development build**\n\n```ng build --configuration development```"],["SYNOPSIS","**ng** **build** [_project_] [_options_]"],["PARAMETERS","_PROJECT_\n> The name of the project to build, as specified in angular.json.\n\n**-c**, **--configuration** _name_\n> One or more named builder configurations (comma-separated), as defined in angular.json.\n\n**--source-map**\n> Generate source maps for scripts and styles (default: false).\n\n**--watch**\n> Rebuild on file changes (default: false).\n\n**--output-path** _dir_\n> Output directory relative to workspace root.\n\n**--base-href** _url_\n> Base URL for the application being built.\n\n**--aot**\n> Build using Ahead of Time compilation (default: true).\n\n**--optimization**\n> Enable optimization including minification, tree-shaking, and dead-code elimination (default: true).\n\n**--output-hashing** _mode_\n> Cache-busting hashing mode: none, all, media, or bundles (default: none).\n\n**--delete-output-path**\n> Delete the output path before building (default: true).\n\n**--stats-json**\n> Generate a stats.json file for bundle analysis (default: false).\n\n**--progress**\n> Log progress to the console while building (default: true).\n\n**--extract-licenses**\n> Extract all licenses to a separate file (default: true).\n\n**--subresource-integrity**\n> Enable subresource integrity validation (default: false).\n\n**--verbose**\n> Add detailed output logging (default: false).\n\n**--named-chunks**\n> Use file name for lazy loaded chunks (default: false).\n\n**--poll** _ms_\n> File watching poll time period in milliseconds.\n\n**--ssr**\n> Enable Server-Side Rendering at runtime (default: false).\n\n**--prerender**\n> Enable Static Site Generation (prerendering) during build.\n\n**--help**\n> Display help information."],["DESCRIPTION","**ng build** compiles an Angular application or library into an output directory. It handles TypeScript compilation, bundling, minification, tree-shaking, and other optimizations to produce deployment-ready artifacts.\n\nThe application builder uses esbuild as the build tool. Default configuration options are specified in the workspace configuration file (angular.json) or can be overridden with a named alternative configuration. Command-line options override those set in angular.json.\n\nA \"production\" configuration with full optimizations is typically defined by default when creating a project with the CLI. A \"development\" configuration with faster builds and source maps is also available."],["CAVEATS","Part of **Angular CLI**. Build configuration is managed in angular.json. Option names on the command line use dash-case, while in the configuration file they use camelCase. Some options can only be set through angular.json. The `--prod` flag was removed in Angular 12; use `--configuration production` instead."],["HISTORY","**ng build** is part of **Angular CLI**, first released alongside Angular 2 in **2016**. The build system was originally based on Webpack, but modern versions of Angular (17+) have migrated to **esbuild** for significantly faster build times."],["SEE ALSO","[ng](/man/ng)(1), [ng-serve](/man/ng-serve)(1), [webpack](/man/webpack)(1)"]],"e":["ng","ng-serve","webpack"],"x":"ng build --configuration production"},"nl":{"n":"nl","t":"numbers lines from files or standard input","s":[["TAGLINE","numbers lines from files or standard input"],["TLDR","**Number non-blank lines** in a file\n\n```nl [path/to/file]```\n\nRead from **stdin**\n\n```[command] | nl```\n\nNumber **all lines including blank** lines\n\n```nl -b a [path/to/file]```\n\nNumber only lines matching a **regex pattern**\n\n```nl -b p'[pattern]' [path/to/file]```\n\nUse a specific **increment** for line numbering\n\n```nl -i [increment] [path/to/file]```\n\nSpecify the line numbering **format** (right/left justified, with/without zeros)\n\n```nl -n [rz|ln|rn]```\n\nSpecify the line numbering **width** (6 by default)\n\n```nl -w [col_width] [path/to/file]```\n\nUse a specific **separator** between numbers and lines\n\n```nl -s \"[separator]\" [path/to/file]```"],["SYNOPSIS","**nl** [_options_] [_file_]"],["PARAMETERS","**-b, --body-numbering _style_**\n> Numbering style: a (all), t (non-blank, default), n (none), p_regex_ (pattern)\n\n**-h, --header-numbering _style_**\n> Numbering style for header lines (default: n)\n\n**-f, --footer-numbering _style_**\n> Numbering style for footer lines (default: n)\n\n**-i, --line-increment _n_**\n> Increment for line numbers (default: 1)\n\n**-l, --join-blank-lines _n_**\n> Group of n empty lines counted as one\n\n**-n, --number-format _format_**\n> Format: ln (left, no zeros), rn (right, no zeros, default), rz (right, with zeros)\n\n**-p, --no-renumber**\n> Do not reset line numbers at logical pages\n\n**-w, --number-width _n_**\n> Width of line number column (default: 6)\n\n**-s, --number-separator _string_**\n> String between number and line (default: TAB)\n\n**-v, --starting-line-number _n_**\n> First line number (default: 1)\n\n**-d, --section-delimiter _chars_**\n> Characters for logical page delimiters (default: \\\\:)"],["DESCRIPTION","**nl** numbers lines from files or standard input. By default, it numbers only non-blank lines, preserving blank lines in the output without numbers.\n\nThe command recognizes logical page sections (header, body, footer) delimited by special character sequences, allowing different numbering styles for each section. Most commonly, it's used simply to add line numbers to file contents.\n\nOutput format is customizable: numbers can be left or right justified, padded with spaces or zeros, and separated from text by any string."],["CAVEATS","Different from **cat -n**, which numbers all lines including blank ones. The logical page feature (sections) is rarely used but can cause unexpected behavior if input contains the delimiter characters (\\\\: by default)."],["HISTORY","nl originated in System V Unix and is specified by POSIX. It has been part of the GNU coreutils since the project's inception."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[cat](/man/cat)(1), [pr](/man/pr)(1), [head](/man/head)(1), [tail](/man/tail)(1)"]],"e":["cat","pr","head","tail"],"x":"nl [path/to/file]"},"paclock":{"n":"paclock","t":"manages the libalpm database lock file used by pacman to prevent simultaneous","s":[["TAGLINE","manages the libalpm database lock file used by pacman to prevent simultaneous"],["TLDR","**Lock** the database\n\n```sudo paclock```\n\nWrite the **lock file path** to stdout (without locking)\n\n```paclock --print```\n\n**Unlock** the database\n\n```sudo paclock --unlock```\n\nDisplay **help**\n\n```paclock --help```"],["SYNOPSIS","**paclock** [_options_]"],["PARAMETERS","**--unlock**\n> Remove the database lock instead of creating it\n\n**--print**\n> Print lock file path without taking any action\n\n**--run** _command_\n> Run command with the database locked, then unlock\n\n**--config** _file_\n> Use alternate pacman configuration file\n\n**--dbpath** _path_\n> Use alternate database path\n\n**--root** _path_\n> Use alternate installation root\n\n**--help**\n> Display help information\n\n**--version**\n> Display version information"],["DESCRIPTION","**paclock** manages the libalpm database lock file used by pacman to prevent simultaneous package management operations. It allows manually locking or unlocking the database, which is useful for scripts that need exclusive database access or for recovering from stale locks.\n\nThe lock file prevents multiple pacman processes from modifying the package database concurrently, which could cause corruption. Normally pacman handles locking automatically, but paclock provides direct control when needed."],["CAVEATS","Requires root privileges to modify the lock file. Manually unlocking while pacman is running can cause database corruption. Use with caution; prefer letting pacman manage locks automatically unless troubleshooting specific issues."],["HISTORY","Part of **pacutils**, a collection of utilities for pacman by Andrew Gregory. Provides explicit lock management for advanced use cases and recovery from stale lock files left by crashed pacman processes."],["SEE ALSO","[pacman](/man/pacman)(8), [pactrans](/man/pactrans)(8), [flock](/man/flock)(1)"]],"e":["pacman","pactrans","flock"],"x":"sudo paclock"},"paclog":{"n":"paclog","t":"filters and displays entries from the pacman log file","s":[["TAGLINE","filters and displays entries from the pacman log file"],["TLDR","Display the **entire pacman log**\n\n```paclog```\n\nDisplay pacman-style logged **commandline entries**\n\n```paclog --commandline```\n\nDisplay log events for a **specific package**\n\n```paclog --package [package_name]```\n\nDisplay package actions of a **specific type**\n\n```paclog --action [install|reinstall|upgrade|downgrade|remove|all]```\n\nDisplay only **errors, warnings, and notes**\n\n```paclog --warnings```\n\nDisplay the list of **installed packages** according to the log\n\n```paclog --pkglist```"],["SYNOPSIS","**paclog** [_options_]"],["PARAMETERS","**--commandline**\n> Show command-line entries from log\n\n**--package _name_**\n> Filter entries for a specific package\n\n**--action _type_**\n> Filter by action: install, reinstall, upgrade, downgrade, remove, all\n\n**--warnings**\n> Show only warnings, errors, and notes\n\n**--pkglist**\n> List packages that are currently installed according to log\n\n**--after _date_**\n> Show entries after specified date\n\n**--before _date_**\n> Show entries before specified date\n\n**--logfile _file_**\n> Use alternate log file\n\n**--help**\n> Display help information\n\n**--version**\n> Display version information"],["DESCRIPTION","**paclog** filters and displays entries from the pacman log file (/var/log/pacman.log). It provides structured access to package management history, making it easy to see when packages were installed, upgraded, or removed.\n\nPart of the **pacutils** collection, paclog can filter by package name, action type, date range, or severity level. This is useful for troubleshooting system issues by reviewing recent package changes or tracking the history of a specific package.\n\nThe **--pkglist** option reconstructs the current installed package list by replaying the log, useful for verification or recovery scenarios."],["CAVEATS","Depends on an intact pacman.log file. Log rotation or clearing will lose historical data. Part of the pacutils package (not installed by default on Arch). Date parsing requires specific formats."],["SEE ALSO","[pacman](/man/pacman)(8), [pactree](/man/pactree)(8), [pacsift](/man/pacsift)(1), [pacinfo](/man/pacinfo)(1)"]],"e":["pacman","pactree","pacsift","pacinfo"],"x":"paclog"},"pam_faillock":{"n":"pam_faillock","t":"PAM module that denies access after multiple failed authentication","s":[["TAGLINE","PAM module that denies access after multiple failed authentication"],["TLDR","**Show failed login attempts**\n\n```faillock --user [username]```\n\n**Reset failed count**\n\n```faillock --user [username] --reset```\n\n**Show all users' status**\n\n```faillock```\n\n**Configure lockout (in pam file)**\n\n```auth required pam_faillock.so deny=5 unlock_time=900```"],["SYNOPSIS","**faillock** [_options_]"],["PARAMETERS","**--user** _name_\n> Specific user.\n\n**--reset**\n> Reset failure count.\n\n**--dir** _path_\n> Tally directory."],["DESCRIPTION","**pam_faillock** is a PAM module that denies access after multiple failed authentication attempts. It provides brute-force protection by temporarily locking accounts.\n\nThe faillock command queries and resets the failure records."],["PAM CONFIGURATION","```"],["/etc/pam.d/system-auth","auth     required  pam_faillock.so preauth silent deny=5 unlock_time=900\nauth     required  pam_unix.so\nauth     required  pam_faillock.so authfail deny=5 unlock_time=900\n\naccount  required  pam_faillock.so\n```"],["PARAMETERS","```\ndeny=N        - Lock after N failures\nunlock_time=S - Unlock after S seconds\nfail_interval - Time window for failures\neven_deny_root - Also lock root\n```"],["CAVEATS","Can lock out legitimate users. Root lockout dangerous. Test configuration carefully."],["HISTORY","pam_faillock replaced pam_tally2 in modern Linux distributions for tracking and enforcing login failure policies."],["SEE ALSO","[pam](/man/pam)(8), [faillock](/man/faillock)(8), [pam.d](/man/pam.d)(5)"]],"e":["pam","faillock","pam.d"],"x":"faillock --user [username]"},"pam_limits":{"n":"pam_limits","t":"sets resource limits for user sessions","s":[["TAGLINE","sets resource limits for user sessions"],["TLDR","**Enable resource limits in a PAM service file**\n\n```session required pam_limits.so```\n\n**Set a hard limit on open file descriptors for all users**\n\n```echo \"* hard nofile 65535\" >> /etc/security/limits.conf```\n\n**Set soft and hard process limits for a group**\n\n```echo \"@developers - nproc 4096\" >> /etc/security/limits.conf```\n\n**Drop in a per-application limits override**\n\n```echo \"nginx hard nofile 100000\" > /etc/security/limits.d/nginx.conf```"],["SYNOPSIS","**pam_limits.so** [_options_]"],["PARAMETERS","**conf=**_FILE_\n> Use an alternate limits configuration file instead of `/etc/security/limits.conf`.\n\n**debug**\n> Print debug information to the system log.\n\n**change_uid**\n> Change to the user's UID before reading the limits file. Useful when the limits file is only accessible by the target user.\n\n**utmp_early**\n> Read the utmp entry before the module runs. Works around broken applications that allocate utmp entries before PAM completes.\n\n**noaudit**\n> Do not report exceeded maximum login count to the audit subsystem.\n\n**set_all**\n> Set unspecified limits from the PID 1 process limits. Not recommended on systemd systems, as PID 1 limits differ from kernel defaults.\n\nConfiguration is read from `/etc/security/limits.conf` and drop-in files in `/etc/security/limits.d/*.conf`."],["DESCRIPTION","**pam_limits** is a PAM session module that enforces per-user and per-group resource limits at login time. It reads limit definitions from `/etc/security/limits.conf` and any `*.conf` files in `/etc/security/limits.d/`, which are applied in lexicographic order.\n\nEach rule in the configuration has the format: `domain type item value`. The domain is a username, `@groupname`, `*` (all users), or a UID/GID range. The type is `soft` (user-adjustable ceiling), `hard` (kernel-enforced maximum), or `-` (set both). Common items include `nofile` (open file descriptors), `nproc` (processes), `memlock` (locked memory in KB), `stack` (stack size in KB), `cpu` (CPU time in minutes), `as` (address space in KB), `maxlogins` (concurrent logins per user), and `priority` (scheduling priority).\n\nIndividual user entries take precedence over group entries. Users including root (uid=0) are subject to these limits. Use `unlimited`, `infinity`, or `-1` as the value to remove a limit (not valid for `priority`, `nice`, or `nonewprivs`).\n\nLimits only take effect for new login sessions; running processes are not affected."],["CAVEATS","Session module only — must be placed in the `session` stack of a PAM service file (e.g. `/etc/pam.d/common-session`). Limits are applied per-session at login; changes require the user to log out and back in.\n\nRoot (uid=0) is affected by these limits like any other user.\n\nSystemd services ignore `/etc/security/limits.conf`; use `LimitNOFILE=` and related directives in the unit file instead.\n\nThis module must not be called from a multithreaded application."],["SEE ALSO","[limits.conf](/man/limits.conf)(5), [pam](/man/pam)(8), [ulimit](/man/ulimit)(1), [sysctl](/man/sysctl)(8)"]],"e":["limits.conf","pam","ulimit","sysctl"],"x":"session required pam_limits.so"},"pamtopam":{"n":"pamtopam","t":"copies PAM images, normalizing format details","s":[["TAGLINE","copies PAM images, normalizing format details"],["TLDR","**Normalize PAM image format**\n\n```pamtopam [input.pam] > [output.pam]```"],["SYNOPSIS","**pamtopam** [_file_]"],["DESCRIPTION","**pamtopam** copies PAM images, normalizing format details. Useful for ensuring consistent PAM format or as a no-op filter in pipelines. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pamtopnm](/man/pamtopnm)(1)"]],"e":["pamtopnm"],"x":"pamtopam [input.pam] > [output.pam]"},"paramspider":{"n":"paramspider","t":"discovers URL parameters by mining web archives","s":[["TAGLINE","discovers URL parameters by mining web archives"],["TLDR","**Find parameters for domain**\n\n```paramspider -d [example.com]```\n\n**Exclude specific file extensions**\n\n```paramspider -d [example.com] --exclude [woff,css,js,png,jpg]```\n\n**Use custom placeholder for parameter values**\n\n```paramspider -d [example.com] -p \"[FUZZ]\"```\n\n**Set output directory**\n\n```paramspider -d [example.com] -o [results/]```\n\n**Spider a list of domains from a file**\n\n```paramspider -l [domains.txt]```"],["SYNOPSIS","**paramspider** -d _domain_ [_options_]"],["PARAMETERS","**-d**, **--domain** _domain_\n> Target domain.\n\n**-l**, **--list** _file_\n> File containing a list of domains.\n\n**-o**, **--output** _dir_\n> Output directory (default: ./results).\n\n**--exclude** _exts_\n> Exclude URLs with specific extensions (comma-separated).\n\n**-p**, **--placeholder** _str_\n> Placeholder for parameter values (default: FUZZ).\n\n**--level** _level_\n> Find nested parameters (e.g., high).\n\n**-q**, **--quiet**\n> Quiet mode, suppress URL output to screen.\n\n**-s**, **--subs**\n> Include subdomains."],["DESCRIPTION","**paramspider** discovers URL parameters by mining web archives. It queries archive.org's Wayback Machine to find historical URLs with parameters for a target domain.\n\nUseful for finding hidden parameters, endpoints, and potential injection points during security testing."],["OUTPUT FORMAT","```\nhttps://example.com/page?id=FUZZ\nhttps://example.com/search?q=FUZZ&page=FUZZ\n```"],["CAVEATS","Requires internet access. Results from archived URLs. May find outdated parameters. Use responsibly."],["HISTORY","ParamSpider was created by **Devansh Batham** as a tool for bug bounty hunters and penetration testers to discover parameters."],["SEE ALSO","[waybackurls](/man/waybackurls)(1), [gau](/man/gau)(1), [arjun](/man/arjun)(1)"]],"e":["waybackurls","gau","arjun"],"x":"paramspider -d [example.com]"},"pkgconf":{"n":"pkgconf","t":"Query library compile and link flags","s":[["TAGLINE","Query library compile and link flags"],["TLDR","**Get compiler flags**\n\n```pkgconf --cflags [library]```\n\n**Get linker flags**\n\n```pkgconf --libs [library]```\n\n**Check if library exists**\n\n```pkgconf --exists [library] && echo \"Found\"```\n\n**Get version**\n\n```pkgconf --modversion [library]```\n\n**List all packages**\n\n```pkgconf --list-all```"],["SYNOPSIS","**pkgconf** [_options_] _packages_..."],["PARAMETERS","**--cflags**\n> Compiler flags.\n\n**--libs**\n> Linker flags.\n\n**--exists**\n> Check existence.\n\n**--modversion**\n> Package version.\n\n**--list-all**\n> List all packages.\n\n**--variable** _name_\n> Get variable value.\n\n**--print-requires**\n> Show dependencies."],["DESCRIPTION","**pkgconf** is a program that helps configure compiler and linker flags for libraries. It's a drop-in replacement for pkg-config with improved performance and features."],["EXAMPLES","```bash"],["Compile with library","gcc $(pkgconf --cflags --libs openssl) -o prog prog.c"],["Check version requirement","pkgconf --atleast-version=1.1.0 openssl"],["Get library directory","pkgconf --variable=libdir openssl"],["In Makefile","CFLAGS += $(shell pkgconf --cflags gtk+-3.0)\nLDFLAGS += $(shell pkgconf --libs gtk+-3.0)\n```"],["ENVIRONMENT","```bash\nPKG_CONFIG_PATH   # Additional .pc file paths\nPKG_CONFIG_LIBDIR # Override default paths\n```"],["CAVEATS","Reads .pc files. Compatible with pkg-config. Used in most build systems."],["HISTORY","pkgconf was created by **William Pitcock** as a lighter, faster alternative to pkg-config from freedesktop.org."],["INSTALL","```apt: sudo apt install pkgconf-bin```\n\n```dnf: sudo dnf install pkgconf```\n\n```pacman: sudo pacman -S pkgconf```\n\n```apk: sudo apk add pkgconf```\n\n```zypper: sudo zypper install pkgconf```\n\n```brew: brew install pkgconf```\n\n```nix: nix profile install nixpkgs#pkgconf```"],["SEE ALSO","[pkg-config](/man/pkg-config)(1), [gcc](/man/gcc)(1), [make](/man/make)(1)"]],"e":["pkg-config","gcc","make"],"x":"pkgconf --cflags [library]"},"pnmcomp":{"n":"pnmcomp","t":"Composite PNM images with transparency","s":[["TAGLINE","Composite PNM images with transparency"],["TLDR","**Composite images**\n\n```pnmcomp [overlay.pnm] [background.pnm] > [output.pnm]```\n\n**With alpha mask**\n\n```pnmcomp -alpha=[mask.pgm] [overlay.pnm] [background.pnm] > [output.pnm]```\n\n**Position overlay**\n\n```pnmcomp -xoff=[100] -yoff=[50] [overlay.pnm] [background.pnm] > [output.pnm]```"],["SYNOPSIS","**pnmcomp** [_options_] _overlay_ _background_"],["PARAMETERS","_OVERLAY_\n> Foreground image.\n\n_BACKGROUND_\n> Background image.\n\n**-alpha** _FILE_\n> Alpha mask.\n\n**-xoff** _PIXELS_\n> X offset.\n\n**-yoff** _PIXELS_\n> Y offset.\n\n**-align** _POS_\n> Alignment."],["DESCRIPTION","**pnmcomp** composites two PNM images by overlaying one on top of another with optional alpha transparency. An alpha mask (PGM file) controls the blending between overlay and background pixels.\n\nPosition offsets (**-xoff**, **-yoff**) control where the overlay is placed on the background. Part of the Netpbm toolkit; superseded by **pamcomp** in newer versions."],["CAVEATS","Part of Netpbm. Use pamcomp for newer version."],["HISTORY","pnmcomp is part of **Netpbm** for image compositing."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pamcomp](/man/pamcomp)(1), [pnmpaste](/man/pnmpaste)(1), [netpbm](/man/netpbm)(1)"]],"e":["pamcomp","pnmpaste","netpbm"],"x":"pnmcomp [overlay.pnm] [background.pnm] > [output.pnm]"},"pnmtops":{"n":"pnmtops","t":"Convert PNM images to PostScript","s":[["TAGLINE","Convert PNM images to PostScript"],["TLDR","**Convert PNM to PostScript**\n\n```pnmtops [input.pnm] > [output.ps]```\n\n**Create EPS file**\n\n```pnmtops -nocenter -noturn -nosetpage [input.pnm] > [output.eps]```\n\n**Convert with specific DPI**\n\n```pnmtops -dpi [300] [input.pnm] > [output.ps]```\n\n**Scale to fit page**\n\n```pnmtops -scale [0.5] [input.pnm] > [output.ps]```"],["SYNOPSIS","**pnmtops** [_options_] [_file_]"],["PARAMETERS","**-dpi** _n_\n> Image resolution.\n\n**-scale** _factor_\n> Scale factor.\n\n**-width** _inches_\n> Maximum width.\n\n**-height** _inches_\n> Maximum height.\n\n**-nocenter**\n> Don't center on page.\n\n**-noturn**\n> Don't rotate.\n\n**-nosetpage**\n> Don't set page size."],["DESCRIPTION","**pnmtops** converts PNM images to PostScript format, suitable for printing or embedding in documents. The output can be either a full-page PostScript document or an Encapsulated PostScript (EPS) file.\n\nThe **-dpi** and **-scale** options control the output size, while **-width** and **-height** set maximum dimensions. Use **-nocenter**, **-noturn**, and **-nosetpage** together to create minimal EPS suitable for inclusion in other documents. Part of the Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pstopnm](/man/pstopnm)(1), [pnmtopng](/man/pnmtopng)(1)"]],"e":["pstopnm","pnmtopng"],"x":"pnmtops [input.pnm] > [output.ps]"},"pocount":{"n":"pocount","t":"Count translation statistics in PO files","s":[["TAGLINE","Count translation statistics in PO files"],["TLDR","**Count translation statistics**\n\n```pocount [file.po]```\n\n**Count multiple files**\n\n```pocount [*.po]```\n\n**Short format output**\n\n```pocount --short [file.po]```\n\n**CSV output**\n\n```pocount --csv [file.po]```"],["SYNOPSIS","**pocount** [_options_] [_files_]"],["PARAMETERS","_FILES_\n> PO translation files.\n\n**--short**\n> Short summary format.\n\n**--csv**\n> CSV output format.\n\n**--incomplete**\n> Only show incomplete files.\n\n**--full**\n> Full detailed output."],["DESCRIPTION","**pocount** counts translation statistics in PO (Portable Object) files, reporting the number of translated, fuzzy, and untranslated strings and words. It provides a quick overview of translation progress for localization projects.\n\nOutput can be formatted as a short summary (**--short**), CSV (**--csv**), or full detail (**--full**). The **--incomplete** flag filters to only show files that still need translation work. Part of the **translate-toolkit** suite."],["CAVEATS","Requires PO format files. Part of translate-toolkit."],["HISTORY","pocount was created as part of **translate-toolkit** for translation statistics."],["SEE ALSO","[msgfmt](/man/msgfmt)(1), [pofilter](/man/pofilter)(1), [gettext](/man/gettext)(1)"]],"e":["msgfmt","pofilter","gettext"],"x":"pocount [file.po]"},"ppmtomap":{"n":"ppmtomap","t":"Extract colormap from PPM image","s":[["TAGLINE","Extract colormap from PPM image"],["TLDR","**Extract colormap from image**\n\n```ppmtomap [input.ppm] > [colormap.ppm]```\n\n**Sort colors by frequency**\n\n```ppmtomap -sort [input.ppm] > [colormap.ppm]```"],["SYNOPSIS","**ppmtomap** [_options_] [_file_]"],["PARAMETERS","**-sort**\n> Sort colors by frequency.\n\n**-square**\n> Make output square."],["DESCRIPTION","**ppmtomap** extracts the unique colors from a PPM image as a one-row colormap image. Useful for analyzing or transferring palettes. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[ppmhist](/man/ppmhist)(1), [pnmcolormap](/man/pnmcolormap)(1)"]],"e":["ppmhist","pnmcolormap"],"x":"ppmtomap [input.ppm] > [colormap.ppm]"},"pulumi-up":{"n":"pulumi-up","t":"Deploy infrastructure changes","s":[["TAGLINE","Deploy infrastructure changes"],["TLDR","**Deploy stack**\n\n```pulumi up```\n\n**Deploy with auto-approval**\n\n```pulumi up --yes```\n\n**Deploy specific stack**\n\n```pulumi up -s [stack]```\n\n**Deploy with preview**\n\n```pulumi up --diff```\n\n**Deploy specific targets**\n\n```pulumi up --target [urn]```"],["SYNOPSIS","**pulumi** **up** [_options_]"],["PARAMETERS","**-s**, **--stack** _name_\n> Target stack.\n\n**-y**, **--yes**\n> Skip confirmation.\n\n**--diff**\n> Show detailed diff.\n\n**--target** _urn_\n> Update specific resources.\n\n**--refresh**\n> Refresh before update.\n\n**-p**, **--parallel** _n_\n> Parallelism level.\n\n**--skip-preview**\n> Skip preview step.\n\n**--replace** _urn_\n> Force replacement of resources.\n\n**-f**, **--skip-preview**\n> Skip preview."],["DESCRIPTION","**pulumi up** creates or updates infrastructure. Compares desired state with current infrastructure and applies necessary changes. The primary command for deploying Pulumi programs."],["INSTALL","```pacman: sudo pacman -S pulumi```\n\n```apk: sudo apk add pulumi```\n\n```brew: brew install pulumi```\n\n```nix: nix profile install nixpkgs#pulumi```"],["SEE ALSO","[pulumi](/man/pulumi)(1), [pulumi-preview](/man/pulumi-preview)(1), [pulumi-destroy](/man/pulumi-destroy)(1)"]],"e":["pulumi","pulumi-preview","pulumi-destroy"],"x":"pulumi up"},"pulumi-update":{"n":"pulumi-update","t":"Alias for deploying infrastructure changes","s":[["TAGLINE","Alias for deploying infrastructure changes"],["TLDR","**Update stack (alias for up)**\n\n```pulumi update```\n\n**Update with auto-approval**\n\n```pulumi update --yes```\n\n**Update a specific stack**\n\n```pulumi update --stack [stack_name]```\n\n**Preview changes as JSON**\n\n```pulumi update --json```"],["SYNOPSIS","**pulumi** **update** [_options_]"],["DESCRIPTION","**pulumi update** is an alias for **pulumi up**. Creates or updates infrastructure by comparing desired state with current resources and applying changes."],["INSTALL","```pacman: sudo pacman -S pulumi```\n\n```apk: sudo apk add pulumi```\n\n```brew: brew install pulumi```\n\n```nix: nix profile install nixpkgs#pulumi```"],["SEE ALSO","[pulumi](/man/pulumi)(1), [pulumi-up](/man/pulumi-up)(1), [pulumi-preview](/man/pulumi-preview)(1), [pulumi-destroy](/man/pulumi-destroy)(1)"]],"e":["pulumi","pulumi-up","pulumi-preview","pulumi-destroy"],"x":"pulumi update"},"qemu-system-x86_64":{"n":"qemu-system-x86_64","t":"Emulate x86_64 systems with QEMU","s":[["TAGLINE","Emulate x86_64 systems with QEMU"],["TLDR","**Boot ISO image**\n\n```qemu-system-x86_64 -cdrom [image.iso] -boot d```\n\n**Boot disk image**\n\n```qemu-system-x86_64 -hda [disk.qcow2]```\n\n**Boot with memory and CPU**\n\n```qemu-system-x86_64 -m [2G] -smp [2] -hda [disk.qcow2]```\n\n**Boot with KVM acceleration**\n\n```qemu-system-x86_64 -enable-kvm -m [4G] -hda [disk.qcow2]```\n\n**Boot with network**\n\n```qemu-system-x86_64 -hda [disk.qcow2] -nic user,hostfwd=tcp::2222-:22```"],["SYNOPSIS","**qemu-system-x86_64** [_options_] [_disk_image_]"],["PARAMETERS","**-m** _size_\n> RAM size (e.g., 2G, 4096M).\n\n**-smp** _cpus_\n> Number of CPUs.\n\n**-hda** _file_\n> Primary hard disk image.\n\n**-cdrom** _file_\n> CD-ROM image.\n\n**-boot** _order_\n> Boot order (c=disk, d=cdrom).\n\n**-enable-kvm**\n> Enable KVM acceleration.\n\n**-nic** _options_\n> Network configuration.\n\n**-nographic**\n> Disable graphical output.\n\n**-vnc** _display_\n> Enable VNC server.\n\n**-cpu** _model_\n> CPU model to emulate.\n\n**-drive** _options_\n> Define a drive (file, format, if, media).\n\n**-serial** _device_\n> Redirect serial port."],["DESCRIPTION","**qemu-system-x86_64** is a full-system emulator for the x86_64 architecture. It can run complete operating systems including Linux, Windows, and BSD. With **KVM** hardware acceleration on Linux, it achieves near-native performance.\n\nQEMU emulates CPU, memory, storage, network, and display devices. It supports multiple disk image formats (qcow2, raw, vmdk), various network configurations, and both graphical and serial console output. It is the backend for higher-level virtualization tools like libvirt, virt-manager, and Proxmox."],["CAVEATS","KVM requires hardware virtualization support (Intel VT-x/AMD-V) and the kvm kernel module. Without KVM, emulation is significantly slower. High memory and CPU allocation may impact the host system."],["HISTORY","**QEMU** was created by **Fabrice Bellard** and first released in **2003**. KVM hardware acceleration was merged into the Linux kernel in **2007**, making QEMU the standard userspace component of Linux virtualization."],["INSTALL","```apt: sudo apt install qemu-system-x86```\n\n```dnf: sudo dnf install qemu-system-x86```\n\n```pacman: sudo pacman -S qemu-system-x86```\n\n```apk: sudo apk add qemu-system-x86_64```"],["SEE ALSO","[qemu-img](/man/qemu-img)(1), [qemu-system-i386](/man/qemu-system-i386)(1)"]],"e":["qemu-img","qemu-system-i386"],"x":"qemu-system-x86_64 -cdrom [image.iso] -boot d"},"qm-cloudinit":{"n":"qm-cloudinit","t":"Manage Cloud-Init settings for Proxmox VMs","s":[["TAGLINE","Manage Cloud-Init settings for Proxmox VMs"],["TLDR","Generate a **cloudinit file** for a specific configuration type\n\n```qm cloudinit dump 100 user```\n\nGenerate **network** configuration\n\n```qm cloudinit dump 100 network```\n\nGenerate **meta** configuration\n\n```qm cloudinit dump 100 meta```"],["SYNOPSIS","**qm cloudinit** dump _vmid_ _type_"],["PARAMETERS","**dump** _vmid_ _type_\n> Generate cloudinit file for specified type (meta, network, user)"],["DESCRIPTION","**qm cloudinit** manages Cloud-Init settings for Proxmox VE virtual machines. Cloud-Init provides initial configuration for VMs including network setup, user accounts, and custom scripts.\n\nThe dump subcommand generates Cloud-Init configuration files for inspection and debugging."],["CAVEATS","Requires Cloud-Init to be installed and configured in the VM image. Configuration types must be properly defined in the VM settings."],["HISTORY","Part of **Proxmox VE** QEMU/KVM management tools. Cloud-Init integration allows automated VM provisioning similar to cloud environments."],["INSTALL","```dnf: sudo dnf install qm```"],["SEE ALSO","[qm](/man/qm)(1), [qm-create](/man/qm-create)(1), [cloud-init](/man/cloud-init)(1)"]],"e":["qm","qm-create","cloud-init"],"x":"qm cloudinit dump 100 user"},"qrtool":{"n":"qrtool","t":"Command-line utility for encoding and decoding QR codes","s":[["TAGLINE","Command-line utility for encoding and decoding QR codes"],["TLDR","**Encode text to a QR code PNG**\n\n```qrtool encode \"[text]\" -o [output.png]```\n\n**Encode text as terminal ASCII art**\n\n```qrtool encode \"[text]\" -t ansi```\n\n**Decode a QR code from an image**\n\n```qrtool decode [image.png]```\n\n**Encode as SVG with custom colors**\n\n```qrtool encode \"[text]\" -t svg --foreground \"#000000\" --background \"#ffffff\" -o [output.svg]```\n\n**Generate a Micro QR code**\n\n```qrtool encode \"[text]\" --variant micro -o [output.png]```\n\n**Read from stdin and output QR code**\n\n```echo \"[text]\" | qrtool encode -o [output.png]```"],["SYNOPSIS","**qrtool** _command_ [_options_]"],["PARAMETERS","**encode** _TEXT_\n> Encode text into a QR code. Reads from stdin when TEXT is omitted.\n\n**decode** _FILE_\n> Decode a QR code from an image file.\n\n**-t**, **--type** _FORMAT_\n> Output format: png (default), svg, eps, pic, ansi, ascii, unicode, and color variants.\n\n**-o**, **--output** _FILE_\n> Output file path (stdout if omitted).\n\n**-r**, **--read-from** _FILE_\n> Read input data from a file instead of the command line.\n\n**--variant** _TYPE_\n> QR code type: `normal` (default), `micro`, or `rmqr`.\n\n**--foreground** _COLOR_\n> Foreground color in CSS format (default: black).\n\n**--background** _COLOR_\n> Background color in CSS format (default: white).\n\n**-v**, **--symbol-version** _N_\n> Symbol version: 1-40 for normal, 1-4 for micro. Auto-selected if omitted.\n\n**-l**, **--error-correction-level** _LEVEL_\n> Error correction level: `l`, `m`, `q`, or `h`.\n\n**--mode** _MODE_\n> Encoding mode: `numeric`, `alphanumeric`, `byte`, or `kanji`.\n\n**--mask** _N_\n> Mask pattern 0-7. Auto-selected if omitted.\n\n**-s**, **--size** _N_\n> Module size in pixels for raster/vector outputs.\n\n**-m**, **--margin** _N_\n> Margin width in modules (default: 4 for normal, 2 for others).\n\n**--verbose**\n> Print QR code metadata to stderr."],["DESCRIPTION","**qrtool** is a command-line utility for encoding text into QR codes and decoding QR codes from images. It supports multiple output formats including PNG, SVG, EPS, ANSI terminal art, and Unicode, plus QR code variants including standard, Micro QR, and rMQR. It reads from stdin and writes to stdout for Unix pipeline integration."],["HISTORY","**qrtool** was created by **sorairolake** and is written in **Rust**."],["INSTALL","```pacman: sudo pacman -S qrtool```\n\n```zypper: sudo zypper install qrtool```\n\n```brew: brew install qrtool```\n\n```nix: nix profile install nixpkgs#qrtool```"],["SEE ALSO","[qrc](/man/qrc)(1), [qrencode](/man/qrencode)(1), [zbarimg](/man/zbarimg)(1)"]],"e":["qrc","qrencode","zbarimg"],"x":"qrtool encode \"[text]\" -o [output.png]"},"rand.1s":{"n":"rand.1s","t":"Generate cryptographically secure random bytes","s":[["TAGLINE","Generate cryptographically secure random bytes"],["TLDR","**Generate 32 random bytes** in hexadecimal\n\n```openssl rand -hex 32```\n\n**Generate 32 random bytes** in base64 encoding\n\n```openssl rand -base64 32```\n\n**Generate raw random bytes** and write to a file\n\n```openssl rand -out [random.bin] 256```\n\n**Generate a random password** (24 base64 characters)\n\n```openssl rand -base64 18```\n\n**Generate 1 kilobyte of random data**\n\n```openssl rand -out [random.bin] 1K```"],["SYNOPSIS","**openssl rand** [**-hex**] [**-base64**] [**-out** _file_] _num_"],["PARAMETERS","**-hex**\n> Output random bytes as hexadecimal string (2 characters per byte)\n\n**-base64**\n> Output random bytes encoded in base64\n\n**-out** _file_\n> Write output to file instead of standard output\n\n**-rand** _file_\n> Use specified file(s) as additional random seed source\n\n**-help**\n> Display usage information\n\n_num_\n> Number of random bytes to generate (supports K/M/G/T suffixes)"],["DESCRIPTION","**openssl rand** generates cryptographically secure pseudo-random bytes using OpenSSL's CSPRNG (Cryptographically Secure Pseudo-Random Number Generator). The output provides 256 bits of security when properly seeded from the operating system's entropy source.\n\nThe command is commonly used to generate random passwords, encryption keys, initialization vectors, and other security tokens. Without encoding options, raw binary bytes are output, which may not display properly in terminals.\n\nOn modern operating systems, OpenSSL automatically seeds from trusted system entropy sources (/dev/urandom on Unix-like systems). The command fails if sufficient entropy is unavailable."],["CAVEATS","Raw output (without **-hex** or **-base64**) contains binary data that may include non-printable characters. Pipe through encoding for terminal display or shell usage.\n\nThe number of output characters differs from the number of bytes: hex output is 2x the byte count, base64 output is approximately 4/3x the byte count (plus padding).\n\nFor password generation, base64 encoding includes characters (+, /, =) that may need escaping in some contexts."],["SEE ALSO","[openssl](/man/openssl)(1), [dd](/man/dd)(1), [head](/man/head)(1)"]],"e":["openssl","dd","head"],"x":"openssl rand -hex 32"},"raxml":{"n":"raxml","t":"Maximum likelihood phylogenetic tree inference","s":[["TAGLINE","Maximum likelihood phylogenetic tree inference"],["TLDR","**Basic phylogenetic analysis**\n\n```raxml -s [alignment.phy] -n [output_name] -m GTRGAMMA```\n\n**Rapid bootstrap analysis**\n\n```raxml -f a -s [alignment.phy] -n [run] -m GTRGAMMA -x [12345] -N [100]```\n\n**Parsimony starting tree**\n\n```raxml -y -s [alignment.phy] -n [parsimony] -m GTRGAMMA```\n\n**Multiple threads**\n\n```raxmlHPC-PTHREADS -T [4] -s [alignment.phy] -n [run] -m GTRGAMMA```"],["SYNOPSIS","**raxml** [_options_] **-s** _alignment_ **-n** _name_"],["PARAMETERS","**-s** _file_\n> Alignment file (PHYLIP format).\n\n**-n** _name_\n> Output file name.\n\n**-m** _model_\n> Substitution model.\n\n**-f** _algorithm_\n> Algorithm to execute.\n\n**-x** _seed_\n> Random seed for bootstrap.\n\n**-N** _num_\n> Number of runs/bootstraps.\n\n**-T** _threads_\n> Number of threads (PTHREADS version).\n\n**-p** _seed_\n> Parsimony random seed."],["DESCRIPTION","**RAxML** (Randomized Axelerated Maximum Likelihood) infers phylogenetic trees using maximum likelihood. It's widely used in evolutionary biology for large-scale phylogenetic analyses."],["EXAMPLES","```bash"],["ML search","raxml -s alignment.phy -n result -m GTRGAMMA -p 12345"],["Rapid bootstrap + ML search","raxml -f a -s alignment.phy -n boot -m GTRGAMMA -x 12345 -p 12345 -N 100"],["Protein sequences","raxml -s proteins.phy -n protein_tree -m PROTGAMMAAUTO"],["Partitioned analysis","raxml -s alignment.phy -q partitions.txt -n partitioned -m GTRGAMMA\n```"],["MODELS","```\nGTRGAMMA      - GTR + Gamma (DNA)\nPROTGAMMAAUTO - Auto-select protein model\nGTRCAT        - Faster approximation\n```"],["CAVEATS","Computationally intensive. Use raxmlHPC-PTHREADS or MPI version for large datasets. Superseded by RAxML-NG."],["HISTORY","RAxML was developed by **Alexandros Stamatakis** for high-performance phylogenetic inference, first released in 2004."],["INSTALL","```nix: nix profile install nixpkgs#raxml```"],["SEE ALSO","[iqtree](/man/iqtree)(1)"]],"e":["iqtree"],"x":"raxml -s [alignment.phy] -n [output_name] -m GTRGAMMA"},"samtools":{"n":"samtools","t":"Manipulate SAM/BAM/CRAM sequence alignment files","s":[["TAGLINE","Manipulate SAM/BAM/CRAM sequence alignment files"],["TLDR","**View BAM file**\n\n```samtools view [alignment.bam]```\n\n**Convert SAM to BAM**\n\n```samtools view -b [alignment.sam] -o [alignment.bam]```\n\n**Sort BAM file**\n\n```samtools sort [input.bam] -o [sorted.bam]```\n\n**Index BAM file**\n\n```samtools index [sorted.bam]```\n\n**View specific region**\n\n```samtools view [sorted.bam] [chr1:1000-2000]```\n\n**Count alignments**\n\n```samtools view -c [alignment.bam]```\n\n**Generate statistics**\n\n```samtools flagstat [alignment.bam]```\n\n**Merge BAM files**\n\n```samtools merge [output.bam] [input1.bam] [input2.bam]```"],["SYNOPSIS","**samtools** _command_ [_-b_] [_-o output_] [_-@ threads_] [_options_] [_file_] [_region_]"],["PARAMETERS","**view**\n> View/convert SAM/BAM/CRAM.\n\n**sort**\n> Sort alignments.\n\n**index**\n> Create BAM index.\n\n**merge**\n> Merge sorted files.\n\n**flagstat**\n> Statistics from FLAG field.\n\n**stats**\n> Comprehensive statistics.\n\n**idxstats**\n> Per-reference statistics.\n\n**faidx**\n> Index FASTA file.\n\n**depth**\n> Compute depth at each position.\n\n**mpileup**\n> Generate pileup for variants.\n\n**coverage**\n> Calculate coverage statistics.\n\n**fastq**\n> Extract FASTQ from BAM.\n\n**collate**\n> Shuffle and group alignments by name.\n\n**calmd**\n> Recalculate MD/NM tags.\n\n**-b**\n> Output BAM format.\n\n**-S**\n> Input is SAM (ignored, format is auto-detected).\n\n**-o** _FILE_\n> Output file.\n\n**-@** _NUM_, **--threads** _NUM_\n> Number of threads.\n\n**-f** _FLAGS_\n> Only include reads with FLAGS.\n\n**-F** _FLAGS_\n> Exclude reads with FLAGS.\n\n**-q** _MAPQ_\n> Minimum mapping quality.\n\n**-h**\n> Include header."],["DESCRIPTION","**samtools** manipulates alignments in SAM (Sequence Alignment/Map) format and its binary equivalent BAM. It's essential for next-generation sequencing data analysis.\n\nSAM/BAM files contain sequence reads aligned to reference genomes. Each record includes read name, position, mapping quality, CIGAR string (alignment operations), and optional tags.\n\nThe view command converts between formats and filters alignments. Sorted BAM files with indices enable random access to genomic regions. Most downstream tools require sorted, indexed BAM.\n\nStatistics commands (flagstat, stats, idxstats) summarize alignment characteristics: mapping rates, insert sizes, coverage distributions. These quality metrics guide analysis decisions.\n\nPileup output (mpileup) aggregates alignments at each position for variant calling. Coverage commands calculate read depth across regions.\n\nCRAM format provides better compression than BAM with reference-based encoding. Samtools handles CRAM transparently."],["CAVEATS","Large BAM files require significant memory for some operations. Threading helps but some commands are single-threaded. Unsorted BAM limits available operations. Index required for random access. Reference needed for CRAM files."],["HISTORY","**samtools** was developed by **Heng Li** at the Wellcome Sanger Institute, released around **2009**. It defined the SAM/BAM formats that became standards for sequence alignment. The project is maintained by the samtools/htslib team, part of the broader bioinformatics ecosystem built on these formats."],["INSTALL","```apt: sudo apt install samtools```\n\n```dnf: sudo dnf install samtools```\n\n```zypper: sudo zypper install samtools```\n\n```brew: brew install samtools```\n\n```nix: nix profile install nixpkgs#samtools```"],["SEE ALSO","[bcftools](/man/bcftools)(1), [bwa](/man/bwa)(1), [bedtools](/man/bedtools)(1), [tabix](/man/tabix)(1)"]],"e":["bcftools","bwa","bedtools","tabix"],"x":"samtools view [alignment.bam]"},"semanage-fcontext":{"n":"semanage-fcontext","t":"Manage SELinux file context labeling rules","s":[["TAGLINE","Manage SELinux file context labeling rules"],["TLDR","**List all file labeling rules**\n\n```sudo semanage fcontext -l```\n\n**List all user-defined file labeling rules without headings**\n\n```sudo semanage fcontext -l -C -n```\n\n**Add a rule that labels paths matching a regex**\n\n```sudo semanage fcontext -a -t [samba_share_t] '/mnt/share(/.*)?'```\n\n**Add a rule for a specific file type (f=file, d=directory)**\n\n```sudo semanage fcontext -a -f [d] -t [httpd_sys_content_t] '/srv/www(/.*)?'```\n\n**Create a labeling equivalence between paths**\n\n```sudo semanage fcontext -a -e /[path/to/ref] /[path/to/target]```\n\n**Modify an existing rule to change the type**\n\n```sudo semanage fcontext -m -t [httpd_sys_rw_content_t] '/srv/www(/.*)?'```\n\n**Delete a user-defined rule using its regex**\n\n```sudo semanage fcontext -d '/mnt/share(/.*)?'```\n\n**Delete all local customizations**\n\n```sudo semanage fcontext -D```"],["SYNOPSIS","**semanage fcontext** [**-l**|**-a**|**-d**|**-m**|**-D**] [_options_] _file_spec_"],["PARAMETERS","**-l**, **--list**\n> List all file context rules.\n\n**-a**, **--add**\n> Add a new file context rule.\n\n**-d**, **--delete**\n> Delete a file context rule.\n\n**-m**, **--modify**\n> Modify an existing rule.\n\n**-D**, **--deleteall**\n> Remove all local customizations.\n\n**-t** _TYPE_, **--type** _TYPE_\n> SELinux type to assign to matching files.\n\n**-f** _TYPE_, **--ftype** _TYPE_\n> File type to match: f (regular file), d (directory), c (character device), b (block device), s (socket), l (symbolic link), p (named pipe). Default is all file types.\n\n**-s** _SEUSER_, **--seuser** _SEUSER_\n> SELinux user name for the context.\n\n**-r** _RANGE_, **--range** _RANGE_\n> MLS/MCS security range (for MLS/MCS systems only).\n\n**-e** _PATH_, **--equal** _PATH_\n> Create equivalency rule: substitute target path with the given reference path when generating default labels.\n\n**-C**, **--locallist**\n> Show only locally customized rules.\n\n**-n**, **--noheading**\n> Omit column headings from output.\n\n**-N**, **--noreload**\n> Do not reload policy after commit.\n\n**-E**, **--extract**\n> Extract customizable commands for use within a transaction.\n\n**-S** _STORE_, **--store** _STORE_\n> Select an alternate SELinux policy store."],["DESCRIPTION","**semanage fcontext** manages persistent file context labeling rules in SELinux. File contexts determine what SELinux type label files receive, which controls what confined domains can access them.\n\nRules use PCRE regular expressions to match file paths. After adding or modifying rules, run **restorecon** on the affected directories to apply the new labels."],["CAVEATS","Requires root privileges. Rules are persistent but not applied automatically; use **restorecon -Rv** to apply. Equivalency rules (**-e**) are useful when a directory should have the same contexts as a reference path. The **-f** file type option defaults to all files if not specified."],["SEE ALSO","[semanage](/man/semanage)(8), [restorecon](/man/restorecon)(8), [matchpathcon](/man/matchpathcon)(8), [chcon](/man/chcon)(1), [semanage-boolean](/man/semanage-boolean)(8), [semanage-port](/man/semanage-port)(8)"]],"e":["semanage","restorecon","matchpathcon","chcon","semanage-boolean","semanage-port"],"x":"sudo semanage fcontext -l"},"sgitopnm":{"n":"sgitopnm","t":"Convert SGI images to PNM format","s":[["TAGLINE","Convert SGI images to PNM format"],["TLDR","**Convert SGI image to PNM**\n\n```sgitopnm [input.rgb] > [output.pnm]```\n\n**Extract specific channel** (0=red, 1=green, 2=blue)\n\n```sgitopnm -channel [0] [input.rgb] > [output.pgm]```\n\n**Convert with verbose output** showing image information\n\n```sgitopnm -verbose [input.rgb] > [output.pnm]```"],["SYNOPSIS","**sgitopnm** [_options_] [_file_]"],["PARAMETERS","**-channel** _n_\n> Extract specific channel (0=red, 1=green, 2=blue).\n\n**-verbose**\n> Print image information."],["DESCRIPTION","**sgitopnm** converts Silicon Graphics image format (SGI RGB) to PNM (Portable aNyMap). SGI images typically use .rgb, .rgba, or .bw extensions. Single-channel images produce PGM output; multi-channel images produce PPM. Part of the Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pnmtosgi](/man/pnmtosgi)(1), [rletopnm](/man/rletopnm)(1)"]],"e":["pnmtosgi","rletopnm"],"x":"sgitopnm [input.rgb] > [output.pnm]"},"snmpstatus":{"n":"snmpstatus","t":"Retrieve SNMP agent status summary","s":[["TAGLINE","Retrieve SNMP agent status summary"],["TLDR","**Show host status**\n\n```snmpstatus -v [2c] -c [public] [host]```\n\n**Show status with SNMPv3**\n\n```snmpstatus -v [3] -u [user] -l authPriv -a SHA -A [authpass] [host]```"],["SYNOPSIS","**snmpstatus** [_options_] _agent_"],["PARAMETERS","**-v** _version_\n> SNMP version (1, 2c, 3).\n\n**-c** _community_\n> Community string.\n\n**-u** _user_\n> SNMPv3 username.\n\n**-l** _level_\n> Security level."],["DESCRIPTION","**snmpstatus** retrieves status information from an SNMP agent. Shows system description, uptime, and interface summary. Quick health check for network devices. Part of Net-SNMP suite."],["INSTALL","```apt: sudo apt install snmp```\n\n```apk: sudo apk add net-snmp-tools```"],["SEE ALSO","[snmpget](/man/snmpget)(1), [snmpwalk](/man/snmpwalk)(1)"]],"e":["snmpget","snmpwalk"],"x":"snmpstatus -v [2c] -c [public] [host]"},"sprof":{"n":"sprof","t":"Read and display shared object profiling data","s":[["TAGLINE","Read and display shared object profiling data"],["TLDR","**Set up profiling** for a shared library by running a program with profiling enabled\n\n```LD_PROFILE=[libfoo.so.1] LD_PROFILE_OUTPUT=[/tmp/prof] [./myprogram]```\n\n**Display flat profile and call graph** (default output)\n\n```sprof [libfoo.so.1] [libfoo.so.1.profile]```\n\n**Display only the flat profile**\n\n```sprof -p [libfoo.so.1] [libfoo.so.1.profile]```\n\n**Display only the call graph**\n\n```sprof -q [libfoo.so.1] [libfoo.so.1.profile]```\n\n**Display call pairs**\n\n```sprof -c [libfoo.so.1] [libfoo.so.1.profile]```"],["SYNOPSIS","**sprof** [_option_ ...] _shared-object-path_ [_profile-data-path_]"],["PARAMETERS","**-c**, **--call-pairs**\n> Print a list of pairs of call paths for the interfaces exported by the shared object, along with the number of times each path is used.\n\n**-p**, **--flat-profile**\n> Generate a flat profile of all of the functions in the monitored object, with counts and ticks.\n\n**-q**, **--graph**\n> Generate a call graph.\n\n**-?**, **--help**\n> Display a summary of command-line options and arguments and exit.\n\n**--usage**\n> Display a short usage message and exit.\n\n**-V**, **--version**\n> Display the program version and exit."],["DESCRIPTION","**sprof** displays a profiling summary for a shared object (shared library) specified as its first command-line argument. The profiling summary is created using previously generated profiling data in the optional second argument. If the profiling data path is omitted, sprof looks for a file named **\\<soname\\>.profile** in the current directory.\n\nProfiling data is generated by setting the **LD_PROFILE** environment variable to the soname of the shared library and optionally **LD_PROFILE_OUTPUT** to a directory for the profile data file. Running any application that loads the library then causes the dynamic linker to collect profiling data automatically.\n\nIf none of **-c**, **-p**, or **-q** is specified, the default behavior is to display both a flat profile and a call graph."],["CAVEATS","Only one shared library can be profiled at a time via **LD_PROFILE**. Do not compile with **-pg** when using sprof; the gprof profiling flag interferes with sprof and produces zeroes for execution times. sprof is a GNU extension and is not specified by POSIX. Requires glibc 2.5-34 or newer; older versions may fail with missing internal flags."],["HISTORY","**sprof** was written by **Ulrich Drepper** as part of the **GNU C Library (glibc)**, first appearing around **glibc 2.0** in **1997**. It was created to fill the gap left by **gprof**, which cannot profile shared libraries. The profiling data collection is handled by the glibc dynamic linker itself, making sprof unique in its ability to profile shared objects without recompilation."],["INSTALL","```apt: sudo apt install libc-devtools```"],["SEE ALSO","[gprof](/man/gprof)(1), [ldd](/man/ldd)(1), [ld](/man/ld)(1), [perf](/man/perf)(1)"]],"e":["gprof","ldd","ld","perf"],"x":"LD_PROFILE=[libfoo.so.1] LD_PROFILE_OUTPUT=[/tmp/prof] [./myprogram]"},"strip":{"n":"strip","t":"Remove symbols from object files","s":[["TAGLINE","Remove symbols from object files"],["TLDR","**Strip** all symbols from a file (in place)\n\n```strip [path/to/file]```\n\nStrip and save to **output file**\n\n```strip [path/to/input] -o [path/to/output]```\n\nStrip **debug symbols** only\n\n```strip -d [path/to/file.o]```\n\nStrip **unneeded** symbols\n\n```strip --strip-unneeded [path/to/file]```\n\n**Keep** specific symbols\n\n```strip -K [symbol_name] [path/to/file]```\n\n**Remove** specific section\n\n```strip -R [section_name] [path/to/file]```\n\nKeep only **debug** information\n\n```strip --only-keep-debug [path/to/file]```"],["SYNOPSIS","**strip** [_OPTIONS_] _objfile_..."],["PARAMETERS","**-s, --strip-all**\n> Remove all symbols\n\n**-g, -S, -d, --strip-debug**\n> Remove debugging symbols only\n\n**--strip-unneeded**\n> Remove unneeded symbols for relocation\n\n**--only-keep-debug**\n> Keep only debugging sections\n\n**-K, --keep-symbol** _name_\n> Preserve specific symbol (repeatable)\n\n**-N, --strip-symbol** _name_\n> Remove specific symbol (repeatable)\n\n**-R, --remove-section** _name_\n> Remove named section (wildcards allowed)\n\n**--keep-section** _pattern_\n> Retain matching sections\n\n**-x, --discard-all**\n> Remove all non-global symbols\n\n**-X, --discard-locals**\n> Remove compiler-generated local symbols\n\n**-o** _file_\n> Write output to file (single input only)\n\n**-p, --preserve-dates**\n> Keep access/modification timestamps\n\n**-D, --enable-deterministic-archives**\n> Use zero for UID/GID/timestamps in archives\n\n**--strip-dwo**\n> Remove DWARF .dwo sections (split debug info)\n\n**--strip-section-headers**\n> Strip ELF section headers (ELF files only)\n\n**--remove-relocations** _pattern_\n> Remove relocation information from matching sections\n\n**--keep-file-symbols**\n> Retain symbols identifying source file names\n\n**--keep-section-symbols**\n> Retain symbols identifying section names\n\n**-M, --merge-notes**\n> Merge and remove duplicate notes in ELF files\n\n**-w, --wildcard**\n> Allow shell-style wildcards in symbol names for -K and -N\n\n**-v, --verbose**\n> List all processed files\n\n**--help**\n> Display help\n\n**--version**\n> Display version"],["DESCRIPTION","**strip** removes symbols and other data from object files and executables, reducing file size and making reverse engineering more difficult. It modifies files in-place by default. Archives (.a files) can also be stripped.\n\nStripping debug symbols is common for release builds to reduce binary size while retaining functionality. The **--only-keep-debug** option creates separate debug files that can be used with debuggers while keeping production binaries small."],["CAVEATS","Stripping modifies files in place; back up originals if needed. Over-stripping can break dynamic linking or debugging capabilities. Use **--strip-debug** for libraries that may be linked against. Static analysis and debugging become difficult after stripping."],["HISTORY","**strip** is part of GNU **binutils**, the collection of binary tools maintained by the Free Software Foundation. The command originated in early Unix at Bell Labs in the **1970s** as a way to reduce executable size on systems with limited storage. GNU binutils provides the modern implementation used on Linux."],["INSTALL","```apt: sudo apt install binutils```\n\n```dnf: sudo dnf install binutils```\n\n```pacman: sudo pacman -S binutils```\n\n```apk: sudo apk add binutils```\n\n```zypper: sudo zypper install binutils```\n\n```brew: brew install binutils```\n\n```nix: nix profile install nixpkgs#binutils```"],["SEE ALSO","[objcopy](/man/objcopy)(1), [objdump](/man/objdump)(1), [readelf](/man/readelf)(1), [nm](/man/nm)(1), [ar](/man/ar)(1), [size](/man/size)(1)"]],"e":["objcopy","objdump","readelf","nm","ar","size"],"x":"strip [path/to/file]"},"surge":{"n":"surge","t":"Instant static website publishing platform","s":[["TAGLINE","Instant static website publishing platform"],["TLDR","**Deploy current directory**\n\n```surge```\n\n**Deploy specific directory**\n\n```surge [./dist]```\n\n**Deploy to custom domain**\n\n```surge [./dist] [mysite.surge.sh]```\n\n**Deploy with explicit flags**\n\n```surge --project [./dist] --domain [mysite.surge.sh]```\n\n**List deployed projects**\n\n```surge list```\n\n**Remove deployment**\n\n```surge teardown [mysite.surge.sh]```\n\n**Login**\n\n```surge login```\n\n**Logout**\n\n```surge logout```\n\n**Show current user**\n\n```surge whoami```"],["SYNOPSIS","**surge** [_directory_] [_domain_]"],["PARAMETERS","_directory_\n> Path to deploy.\n\n_domain_\n> Custom domain.\n\n**list**\n> Show projects.\n\n**teardown** _DOMAIN_\n> Remove project.\n\n**login**\n> Authenticate.\n\n**logout**\n> Log out.\n\n**whoami**\n> Show user.\n\n**token**\n> Get auth token for CI/CD use.\n\n**--project** _path_\n> Specify project directory.\n\n**--domain** _domain_\n> Specify target domain.\n\n**--token** _token_\n> Provide auth token (for CI/CD)."],["DESCRIPTION","**surge** is a static website publishing platform that deploys web projects with a single command. Point it at a directory containing HTML, CSS, JavaScript, and other static assets, and it instantly publishes to a *.surge.sh subdomain or a custom domain.\n\nAll deployments include free SSL certificates for HTTPS. Custom domains are supported at no additional cost, requiring only a DNS CNAME record. The platform is designed for front-end developers who need fast, simple hosting without server configuration.\n\nAuthentication tokens enable automated deployments in CI/CD pipelines. The list command shows all deployed projects, and teardown removes a deployment. Surge is particularly popular for publishing documentation sites, single-page applications, and project demos."],["CAVEATS","Static sites only; no server-side code. Free tier has limitations. Custom domains require a DNS CNAME record pointing to na-east1.surge.sh."],["INSTALL","```nix: nix profile install nixpkgs#surge```"],["SEE ALSO","[netlify](/man/netlify)(1), [vercel](/man/vercel)(1), [firebase](/man/firebase)(1)"]],"e":["netlify","vercel","firebase"],"x":"surge"},"svgo":{"n":"svgo","t":"Node.js-based SVG optimization tool","s":[["TAGLINE","Node.js-based SVG optimization tool"],["TLDR","**Optimize SVG**\n\n```svgo [input.svg] -o [output.svg]```\n\n**Optimize in place**\n\n```svgo [file.svg]```\n\n**Optimize directory**\n\n```svgo -f [./icons] -o [./optimized]```\n\n**Show file info**\n\n```svgo [file.svg] --show-plugins```\n\n**Use config file**\n\n```svgo --config [svgo.config.js] [file.svg]```\n\n**Disable plugin**\n\n```svgo --disable=[removeViewBox] [file.svg]```\n\n**Enable plugin**\n\n```svgo --enable=[removeDimensions] [file.svg]```\n\n**Pretty print output**\n\n```svgo --pretty [file.svg]```"],["SYNOPSIS","**svgo** [_-o output_] [_-f folder_] [_--config file_] [_options_] _files_"],["PARAMETERS","**-o**, **--output** _FILE_\n> Output file.\n\n**-f**, **--folder** _DIR_\n> Input folder.\n\n**--config** _FILE_\n> Config file.\n\n**--disable** _PLUGIN_\n> Disable plugin.\n\n**--enable** _PLUGIN_\n> Enable plugin.\n\n**--pretty**\n> Pretty print.\n\n**--multipass**\n> Multiple passes.\n\n**-q**, **--quiet**\n> Quiet output."],["DESCRIPTION","**svgo** (SVG Optimizer) is a Node.js-based tool for optimizing SVG files through a plugin architecture. Each plugin handles a specific optimization such as removing metadata, simplifying path data, merging redundant elements, cleaning up attributes, or removing empty containers.\n\nPlugins can be individually enabled or disabled through a configuration file or command-line flags, allowing fine-grained control over which optimizations are applied. The multipass option runs optimizations repeatedly until no further improvements are found. Folder mode batch-processes entire directories of SVG files.\n\nSVGO is widely used in front-end build pipelines and icon library workflows to minimize SVG file sizes before deployment. Pretty-print mode reformats output for readability when human-editable SVGs are needed."],["CONFIGURATION","**svgo.config.js**\n> Project configuration file defining enabled/disabled plugins and their options."],["CAVEATS","Some optimizations may affect rendering. Test output visually. Complex SVGs need care."],["HISTORY","**SVGO** (SVG Optimizer) is a Node.js tool for SVG optimization. It's widely used in build processes and icon libraries."],["INSTALL","```pacman: sudo pacman -S svgo```\n\n```brew: brew install svgo```\n\n```nix: nix profile install nixpkgs#svgo```"],["SEE ALSO","[imagemin](/man/imagemin)(1), [inkscape](/man/inkscape)(1)"]],"e":["imagemin","inkscape"],"x":"svgo [input.svg] -o [output.svg]"},"systemctl-suspend":{"n":"systemctl-suspend","t":"Suspend system to RAM","s":[["TAGLINE","Suspend system to RAM"],["TLDR","**Suspend** the system\n\n```systemctl suspend```\n\n**Force** suspend\n\n```systemctl suspend -f```"],["SYNOPSIS","**systemctl suspend** [_OPTIONS_]"],["PARAMETERS","**-f, --force**\n> Force suspend even if inhibitors are present\n\n**--no-wall**\n> Don't send wall message to users"],["DESCRIPTION","**systemctl suspend** puts the system into suspend mode (suspend-to-RAM), a low-power state where the system contents are kept in memory. Wake-up is fast since the system state doesn't need to be restored from disk.\n\nPower is maintained to RAM to preserve the system state. This is the standard sleep mode for laptops and desktops."],["CAVEATS","System state is lost if power fails while suspended. Battery drain continues (slowly) during suspend. Some hardware may not support suspend reliably. Wake devices (keyboard, power button) must be configured."],["HISTORY","The **suspend** subcommand integrates system sleep into the systemd command interface, coordinating with power management and inhibitor locks."],["SEE ALSO","[systemctl-hibernate](/man/systemctl-hibernate)(1), [systemctl-hybrid-sleep](/man/systemctl-hybrid-sleep)(1), [systemctl-suspend-then-hibernate](/man/systemctl-suspend-then-hibernate)(1)"]],"e":["systemctl-hibernate","systemctl-hybrid-sleep","systemctl-suspend-then-hibernate"],"x":"systemctl suspend"},"task":{"n":"task","t":"Command-line task management system","s":[["TAGLINE","Command-line task management system"],["TLDR","**Add a new task**\n\n```task add \"[description]\"```\n\n**Add a task with project and due date**\n\n```task add \"[description]\" project:[project_name] due:[tomorrow]```\n\n**List all pending tasks**\n\n```task list```\n\n**Mark a task as done**\n\n```task [task_id] done```\n\n**Modify an existing task**\n\n```task [task_id] modify priority:[H] due:[friday]```\n\n**Delete a task**\n\n```task [task_id] delete```\n\n**Show detailed info** about a task\n\n```task [task_id] info```\n\n**Start working on a task**\n\n```task [task_id] start```"],["SYNOPSIS","**task** [_global_options_] [_filter_] _command_ [_arguments_]"],["PARAMETERS","**add**\n> Add a new task to the task list.\n\n**done**\n> Mark a task as completed.\n\n**delete**\n> Delete a task from the list.\n\n**modify**\n> Modify attributes of an existing task.\n\n**start**\n> Mark a task as started (active).\n\n**stop**\n> Mark a task as no longer active.\n\n**annotate**\n> Add an annotation to a task.\n\n**list**\n> Display pending tasks using the default report.\n\n**info**\n> Show all data and metadata for a task.\n\n**undo**\n> Revert the most recent change.\n\n**log**\n> Add a task that is already completed.\n\n**duplicate**\n> Create a copy of a task.\n\n**projects**\n> List all projects with task counts.\n\n**tags**\n> List all tags in use.\n\n**--rc** _file_\n> Use an alternative configuration file.\n\n**--force**\n> Suppress confirmation prompts.\n\n**--verbose**\n> Show more detailed output.\n\n**--quiet**\n> Suppress non-essential output.\n\n**--debug**\n> Enable debugging output.\n\n**--version**\n> Display version information."],["DESCRIPTION","**Taskwarrior** is a command-line task management application that maintains a list of tasks with support for projects, tags, priorities, due dates, and custom attributes. It provides powerful filtering and reporting capabilities.\n\nTasks can have attributes like **project**, **priority** (H/M/L), **due** dates, **tags**, **wait** dates, and user-defined attributes. Filters allow selecting tasks based on any combination of attributes. Reports provide customizable views of task data.\n\nConfiguration is stored in **~/.taskrc** and task data in **~/.task/**. These locations can be overridden via environment variables **TASKRC** and **TASKDATA**. All commands can be abbreviated as long as the prefix is unique."],["CONFIGURATION","**~/.taskrc**\n> Main configuration file controlling reports, colors, aliases, and default behaviors\n\n**~/.task/**\n> Default data directory containing task database, undo log, and backlog for sync\n\n**TASKRC**\n> Environment variable to override the configuration file location\n\n**TASKDATA**\n> Environment variable to override the data directory location"],["CAVEATS","Task IDs are temporary and may change after sync or undo operations. For scripts and automation, use UUIDs instead. The **undo** command only reverts the most recent operation. Synchronization with a Taskserver requires additional setup."],["HISTORY","Taskwarrior was created by **Paul Beckingham** and first released in **2006**. It has grown from a simple todo list to a sophisticated task management system with sync capabilities, hooks, and extensive customization. The project is maintained by the **Gothenburg Bit Factory** and has inspired numerous frontends and integrations. Version 3.0, released in **2024**, introduced significant architectural changes and improved performance."],["INSTALL","```apt: sudo apt install taskwarrior```\n\n```dnf: sudo dnf install task```\n\n```pacman: sudo pacman -S task```\n\n```apk: sudo apk add go-task-task```\n\n```zypper: sudo zypper install taskwarrior```\n\n```brew: brew install task```"],["SEE ALSO","[todo.txt-cli](/man/todo.txt-cli)(1), [remind](/man/remind)(1), [calcurse](/man/calcurse)(1)"]],"e":["todo.txt-cli","remind","calcurse"],"x":"task add \"[description]\""},"tsx":{"n":"tsx","t":"Fast TypeScript execution with esbuild","s":[["TAGLINE","Fast TypeScript execution with esbuild"],["TLDR","**Run a TypeScript file**\n\n```tsx [script.ts]```\n\n**Run with watch mode**\n\n```tsx watch [script.ts]```\n\n**Start TypeScript REPL**\n\n```tsx```\n\n**Run as Node.js loader**\n\n```node --import tsx [script.ts]```\n\n**Run with environment variables**\n\n```tsx [script.ts] --env-file [.env]```\n\n**Pass arguments to script**\n\n```tsx [script.ts] -- [arg1] [arg2]```"],["SYNOPSIS","**tsx** [_options_] [_file_] [_args_...]\n\n**tsx** **watch** [_options_] [_file_]"],["PARAMETERS","**watch**\n> Watch mode: restart on file changes.\n\n**--tsconfig** _file_\n> Path to tsconfig.json file.\n\n**--env-file** _file_\n> Load environment variables from file.\n\n**--no-cache**\n> Disable transform caching.\n\n**--help**\n> Display help information.\n\n**--version**\n> Display version information."],["DESCRIPTION","**tsx** (TypeScript Execute) is an enhanced Node.js runtime that seamlessly runs TypeScript and ESM files. It uses esbuild for fast transpilation, making it ideal for development and scripting.\n\nThe tool requires zero configuration and works without a tsconfig.json file. It supports both CommonJS and ES modules, handles path aliases from tsconfig.json, and includes a watch mode for development.\n\ntsx is a drop-in replacement for node, supporting all Node.js command-line flags. It transpiles TypeScript on-the-fly without writing JavaScript files to disk.\n\nNote that tsx focuses on execution speed and does not perform type checking. Use tsc or your editor for type checking.\n\nInstall globally: `npm install -g tsx`"],["CAVEATS","Does not perform type checking. Adds slight startup overhead for transpilation. For production, pre-compile TypeScript with tsc. Works best with dynamically linked Node.js features."],["HISTORY","**tsx** was created by Hiroki Osame (privatenumber) as a modern alternative to ts-node. It prioritizes speed and simplicity, leveraging esbuild for near-instant transpilation. The project has become popular for running TypeScript during development."],["INSTALL","```nix: nix profile install nixpkgs#tsx```"],["SEE ALSO","[ts-node](/man/ts-node)(1), [node](/man/node)(1), [tsc](/man/tsc)(1), [esbuild](/man/esbuild)(1)"]],"e":["ts-node","node","tsc","esbuild"],"x":"tsx [script.ts]"},"ttyd":{"n":"ttyd","t":"Share terminal sessions over the web","s":[["TAGLINE","Share terminal sessions over the web"],["TLDR","**Share a bash shell** over the web\n\n```ttyd bash```\n\n**Share on a specific port**\n\n```ttyd -p [8080] bash```\n\n**Share with authentication**\n\n```ttyd -c [username]:[password] bash```\n\n**Share in read-only mode**\n\n```ttyd -R bash```\n\n**Share vim** editor over the web\n\n```ttyd vim [file.txt]```\n\n**Share a tmux session** with multiple clients\n\n```ttyd tmux new -A -s [session_name]```\n\n**Share with SSL** encryption\n\n```ttyd --ssl --ssl-cert [cert.pem] --ssl-key [key.pem] bash```\n\n**Bind to specific interface**\n\n```ttyd -i [eth0] bash```"],["SYNOPSIS","**ttyd** [_options_] _command_ [_arguments_...]"],["PARAMETERS","**-p**, **--port** _port_\n> Port to listen on (default: 7681, use 0 for random)\n\n**-i**, **--interface** _interface_\n> Network interface to bind (e.g., eth0) or Unix socket path\n\n**-c**, **--credential** _user:pass_\n> Basic authentication credentials\n\n**-u**, **--uid** _uid_\n> User ID to run command with\n\n**-g**, **--gid** _gid_\n> Group ID to run command with\n\n**-R**, **--readonly**\n> Do not allow clients to write to the TTY\n\n**-W**, **--writable**\n> Allow clients to write (readonly by default with multiple clients)\n\n**-s**, **--signal** _signal_\n> Signal to send on exit (default: 1, SIGHUP)\n\n**--ssl**\n> Enable SSL/TLS encryption\n\n**--ssl-cert** _file_\n> SSL certificate file path\n\n**--ssl-key** _file_\n> SSL key file path\n\n**-O**, **--once**\n> Accept only one client and exit when disconnected\n\n**-d**, **--debug** _level_\n> Set log level (0-9, default: 7)"],["DESCRIPTION","**ttyd** shares a terminal session over the web using WebSockets. It starts a web server (default port 7681) that serves a terminal interface accessible from any web browser. This enables remote terminal access, collaboration, and demonstrations without requiring SSH setup.\n\nAny command can be shared, not just shells. Running **ttyd vim** provides a web-based vim editor. Combining with **tmux** allows multiple users to share the same session simultaneously.\n\nThe tool supports basic authentication via **-c**, SSL encryption for secure connections, and can restrict clients to read-only mode. It handles Unicode, CJK characters, and supports file transfer via ZMODEM.\n\nttyd works on Linux, macOS, FreeBSD, OpenBSD, and Windows."],["CAVEATS","By default, ttyd provides no authentication; always use **-c** or firewall rules in production. Multiple clients connecting without tmux each get separate sessions. The default port 7681 may need firewall configuration for external access."],["HISTORY","**ttyd** was created by **Shuanglei Tao** (tsl0922) and first released around **2016**. Written in C, it uses libwebsockets for WebSocket communication and xterm.js for the browser-based terminal. The project was inspired by older tools like shellinabox but with modern web technologies."],["INSTALL","```dnf: sudo dnf install ttyd```\n\n```pacman: sudo pacman -S ttyd```\n\n```apk: sudo apk add ttyd```\n\n```zypper: sudo zypper install ttyd```\n\n```brew: brew install ttyd```\n\n```nix: nix profile install nixpkgs#ttyd```"],["SEE ALSO","[tmux](/man/tmux)(1), [screen](/man/screen)(1), [gotty](/man/gotty)(1), [ssh](/man/ssh)(1)"]],"e":["tmux","screen","gotty","ssh"],"x":"ttyd bash"},"urpmq":{"n":"urpmq","t":"Query Mageia package information","s":[["TAGLINE","Query Mageia package information"],["TLDR","Display package **info**\n\n```urpmq -i [package]```\n\nShow **dependencies**\n\n```urpmq --requires [package]```\n\nShow **recursive** dependencies\n\n```urpmq -d [package]```\n\nList missing dependencies for **RPM**\n\n```sudo urpmq -d -m --sources [path/to/file.rpm]```\n\nList **media** with URLs\n\n```urpmq --list-media --list-url```\n\n**Fuzzy** search with group and release\n\n```urpmq -g -r -y [keyword]```\n\n**Exact** name search\n\n```urpmq -g -r [package]```"],["SYNOPSIS","**urpmq** [_OPTIONS_] [_PACKAGE_...]"],["PARAMETERS","**-i**\n> Display package information\n\n**--requires**\n> Show direct dependencies\n\n**-d, --requires-recursive**\n> Show all dependencies recursively\n\n**-m**\n> Show sources for dependencies\n\n**--sources**\n> Print package sources\n\n**--list-media**\n> List configured media\n\n**--list-url**\n> Show media URLs\n\n**-g**\n> Show package group\n\n**-r**\n> Show release information\n\n**-y, --fuzzy**\n> Fuzzy match package names"],["DESCRIPTION","**urpmq** queries information about packages and media in Mageia Linux. It can display package details, dependencies, and configured repository information.\n\nPart of the urpmi package management suite for Mageia."],["CAVEATS","Mageia specific. Some operations require root privileges. Recursive dependency queries may produce long output."],["SEE ALSO","[urpmi](/man/urpmi)(8), [urpmf](/man/urpmf)(8), [urpme](/man/urpme)(8)"]],"e":["urpmi","urpmf","urpme"],"x":"urpmq -i [package]"},"uuinfo":{"n":"uuinfo","t":"Debug and inspect unique identifiers (UUID, ULID, etc.)","s":[["TAGLINE","Debug and inspect unique identifiers (UUID, ULID, etc.)"],["TLDR","**Inspect a UUID**\n\n```uuinfo [01941f29-7c00-7aaa-aaaa-aaaaaaaaaaaa]```\n\n**Pipe a UUID from stdin**\n\n```echo \"[some-uuid]\" | uuinfo -```\n\n**Output as JSON**\n\n```uuinfo -o json [id]```"],["SYNOPSIS","**uuinfo** [_options_] _ID_"],["DESCRIPTION","**uuinfo** parses and displays detailed information about compound unique identifiers including UUID versions 1-8, ULID, Snowflake, and more. It shows color-coded bit breakdowns, timestamps, entropy, and version information. Supports auto-detection and forced format parsing."],["HISTORY","**uuinfo** was created by **Ronaldo Ferreira** (Racum) and is written in **Rust**."],["SEE ALSO","[uuidgen](/man/uuidgen)(1)"]],"e":["uuidgen"],"x":"uuinfo [01941f29-7c00-7aaa-aaaa-aaaaaaaaaaaa]"},"vpod":{"n":"vpod","t":"Instant Linux sandbox running on a WASM RISC-V VM","s":[["TAGLINE","Instant Linux sandbox running on a WASM RISC-V VM"],["TLDR","**Start an interactive shell** using the default snapshot\n\n```vpod```\n\n**Start a shell** from a specific snapshot\n\n```vpod start [alpine:latest]```\n\n**Download a snapshot** so it is available locally\n\n```vpod pull [alpine:latest]```\n\n**List** the snapshots already available\n\n```vpod list```\n\n**Install** with the official script\n\n```curl -fsSL https://install.vpod.sh | sh```"],["SYNOPSIS","**vpod** [_command_] [_snapshot_]"],["DESCRIPTION","**vpod** gives an untrusted process an instant, throwaway Linux environment. Each vpod is a RISC-V virtual machine (the RV64GC instruction set) compiled to WebAssembly, so the whole sandbox runs inside a portable WASM runtime rather than relying on the host kernel, containers, or hardware virtualization.\n\nWhen started, a vpod boots from a snapshot, a saved VM state that is ready in under a second, and drops the user into an interactive shell. Because execution stays inside the WebAssembly sandbox, code running in a vpod is isolated from the host and behaves the same on Linux, macOS, and Windows without per-platform setup.\n\nIn addition to the command-line tool, the project ships a Python SDK (installable with **pip install vpod**) that creates persistent sandboxes programmatically, which is aimed at running AI-agent generated or otherwise untrusted code safely."],["PARAMETERS","**start** _SNAPSHOT_\n> Start an interactive shell from the named snapshot instead of the default.\n\n**pull** _SNAPSHOT_\n> Download a snapshot and store it locally for later use.\n\n**list**\n> List the snapshots that are available locally."],["CAVEATS","There is no hardware acceleration, so CPU-intensive workloads run slower than native execution. GPU and accelerator interfaces (CUDA, Metal, ML accelerators) are not available inside the sandbox."],["HISTORY","**vpod** is developed by capsulerun and released under the Apache License 2.0. It is written primarily in Rust, with the RISC-V guest executed through a WebAssembly runtime."],["SEE ALSO","[bubblewrap](/man/bubblewrap)(1), [docker](/man/docker)(1), [firejail](/man/firejail)(1), [qemu](/man/qemu)(1), [wasmtime](/man/wasmtime)(1)"],["RESOURCES","```[Source code](https://github.com/capsulerun/vpod)```"]],"e":["bubblewrap","docker","firejail","qemu","wasmtime"],"x":"vpod"},"waitress-serve":{"n":"waitress-serve","t":"Pure-Python WSGI production server","s":[["TAGLINE","Pure-Python WSGI production server"],["TLDR","**Serve a WSGI app on the default port 8080**\n\n```waitress-serve [myapp:app]```\n\n**Bind to a specific host and port**\n\n```waitress-serve --host=[127.0.0.1] --port=[8080] [myapp:app]```\n\n**Listen on multiple addresses**\n\n```waitress-serve --listen=[127.0.0.1:8080] --listen=[127.0.0.1:8443] [myapp:app]```\n\n**Use an application factory function**\n\n```waitress-serve --call [myapp:create_app]```\n\n**Listen on a Unix socket**\n\n```waitress-serve --unix-socket=[/tmp/app.sock] [myapp:app]```\n\n**Set number of worker threads**\n\n```waitress-serve --threads=[8] [myapp:app]```\n\n**Serve under a URL prefix (behind a reverse proxy)**\n\n```waitress-serve --url-prefix=[/api] [myapp:app]```"],["SYNOPSIS","**waitress-serve** [_--host addr_] [_--port port_] [_options_] _module:app_"],["PARAMETERS","**--host** _ADDR_\n> Hostname or IP address to listen on. Default: `0.0.0.0`.\n\n**--port** _PORT_\n> TCP port to listen on. Default: `8080`.\n\n**--listen** _HOST:PORT_\n> Bind to a specific host:port pair. May be repeated for multiple listeners. Supports `*` as a wildcard host.\n\n**--threads** _N_\n> Number of worker threads to handle requests. Default: `4`.\n\n**--unix-socket** _PATH_\n> Path to a Unix domain socket (not available on Windows).\n\n**--unix-socket-perms** _OCTAL_\n> Octal permissions for the Unix socket. Default: `600`.\n\n**--url-scheme** _SCHEME_\n> Value for `wsgi.url_scheme`. Default: `http`.\n\n**--url-prefix** _PREFIX_\n> Value for `SCRIPT_NAME` (for apps mounted at a sub-path). Default: empty.\n\n**--ident** _STRING_\n> Server identity sent in the `Server` response header. Default: `waitress`.\n\n**--call**\n> Treat the positional argument as a callable factory that returns the WSGI app, rather than using it directly as the app.\n\n**--connection-limit** _N_\n> Maximum number of simultaneous connections. Default: `100`.\n\n**--channel-timeout** _SECONDS_\n> Timeout for inactive connections. Default: `120`.\n\n**--trusted-proxy** _IP_\n> IP address of a trusted reverse proxy that may supply forwarding headers."],["DESCRIPTION","**waitress-serve** is a production-quality WSGI server for Python web applications, implemented entirely in pure Python with no C extension dependencies. It serves frameworks like Flask, Django, and Pyramid and runs on any platform including Windows, Linux, and macOS.\n\nThe server uses a multi-threaded architecture with a configurable number of worker threads to handle concurrent requests. It accepts connections on a TCP port or Unix socket and supports URL prefix configuration for applications mounted at sub-paths behind a reverse proxy.\n\nWaitress is designed to be simple and reliable for production deployments, requiring minimal configuration while providing solid performance. It is commonly used behind a reverse proxy like Nginx for static file serving and SSL termination."],["CAVEATS","WSGI only. No ASGI support. Configure behind proxy."],["HISTORY","**Waitress** was created by the **Pylons Project** as a production-quality pure-Python WSGI server."],["INSTALL","```apk: sudo apk add py3-waitress```"],["SEE ALSO","[gunicorn](/man/gunicorn)(1), [uwsgi](/man/uwsgi)(1), [uvicorn](/man/uvicorn)(1), [hypercorn](/man/hypercorn)(1), [flask](/man/flask)(1)"]],"e":["gunicorn","uwsgi","uvicorn","hypercorn","flask"],"x":"waitress-serve [myapp:app]"},"xman":{"n":"xman","t":"Graphical manual page viewer","s":[["TAGLINE","Graphical manual page viewer"],["TLDR","**Start** xman\n\n```xman```\n\nOpen **file**\n\n```xman -helpfile [filename]```\n\nShow **both** panes\n\n```xman -bothshown```"],["SYNOPSIS","**xman** [_OPTIONS_]"],["PARAMETERS","**-helpfile** _FILE_\n> Use specified helpfile instead of the default\n\n**-bothshown**\n> Show both directory and manual page panes simultaneously\n\n**-notopbox**\n> Start without the top menu box with the three buttons\n\n**-geometry** _WxH+X+Y_\n> Set size and location of the top menu window\n\n**-pagesize** _WxH+X+Y_\n> Set size and location of manual page windows"],["DESCRIPTION","**xman** is a manual page browser for the X Window System. It provides a graphical interface to man pages with a directory listing of available pages organized by section. The initial window is intentionally small so it can be left running throughout a login session. Pressing Control-S opens a prompt for a specific manual page to display.\n\nThe **MANPATH** environment variable controls the search path for manual pages."],["HISTORY","**xman** is one of the original X11 utilities, providing a GUI alternative to the terminal-based man command."],["CAVEATS","X11 only. Interface is dated compared to modern alternatives. Limited search capabilities."],["INSTALL","```apt: sudo apt install x11-apps```\n\n```zypper: sudo zypper install xman```"],["SEE ALSO","[man](/man/man)(1), [info](/man/info)(1), [yelp](/man/yelp)(1)"]],"e":["man","info","yelp"],"x":"xman"},"zotero":{"n":"zotero","t":"Open-source reference management software","s":[["TAGLINE","Open-source reference management software"],["TLDR","**Launch Zotero**\n\n```zotero```\n\n**Open specific profile**\n\n```zotero -P [profile_name]```\n\n**Open with profile manager**\n\n```zotero -ProfileManager```\n\n**Import from file**\n\n```zotero -import [file.bib]```\n\n**Open URL in Zotero**\n\n```zotero zotero://select/library/items/[ITEM_KEY]```"],["SYNOPSIS","**zotero** [_options_] [_URL_]"],["PARAMETERS","**-P** _profile_\n> Start with specified profile\n\n**-ProfileManager**\n> Open profile manager at startup\n\n**-import** _file_\n> Import from specified file\n\n**-url** _url_\n> Open specified URL\n\n**-jsconsole**\n> Open JavaScript console\n\n**-purgecaches**\n> Clear all caches on startup\n\n**-ZoteroDebugText**\n> Enable debug output to terminal\n\n**--help**\n> Display help message"],["DESCRIPTION","**Zotero** is a free, open-source reference management software. It helps collect, organize, cite, and share research materials including articles, books, reports, web pages, and more.\n\nKey features:\n- Import references from databases, library catalogs, and web pages\n- Organize with collections, tags, and saved searches\n- Generate citations and bibliographies in various formats\n- Sync library across devices via Zotero account\n- Collaborate through shared group libraries\n\nZotero integrates with word processors (Word, LibreOffice, Google Docs) for in-document citation insertion and automatic bibliography generation.\n\nData is stored locally by default, with optional cloud sync. Browser connectors capture references directly from web pages."],["CAVEATS","Storage sync (for PDFs) has limited free quota. Self-hosting via WebDAV is possible.\n\nSome publisher sites require specific browser connector configurations for proper metadata capture.\n\nLarge libraries with many attachments can consume significant disk space.\n\nOffline access requires library data to be synced locally."],["INSTALL","```apk: sudo apk add zotero```\n\n```nix: nix profile install nixpkgs#zotero```"],["SEE ALSO","[jabref](/man/jabref)(1), [mendeley](/man/mendeley)(1), [papis](/man/papis)(1)"]],"e":["jabref","mendeley","papis"],"x":"zotero"}});
