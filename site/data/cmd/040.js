window.LCL_BUCKET(40,{"animate.im6":{"n":"animate.im6","t":"Display image sequences as animations (ImageMagick 6)","s":[["TAGLINE","Display image sequences as animations (ImageMagick 6)"],["TLDR","**Animate** images (ImageMagick 6)\n\n```animate.im6 [image1.png] [image2.png]```\n\nPlay **animated GIF**\n\n```animate.im6 [animation.gif]```\n\nSet **frame delay**\n\n```animate.im6 -delay [10] [*.png]```\n\nSet **infinite loop**\n\n```animate.im6 -loop [0] [animation.gif]```"],["SYNOPSIS","**animate.im6** [_options_] _files_"],["DESCRIPTION","**animate.im6** is the ImageMagick 6 version of the animate command, used on systems where both ImageMagick 6 and 7 are installed. It displays sequences of images as animations in an X Window.\n\nThe .im6 suffix distinguishes it from the ImageMagick 7 version, allowing both versions to coexist on the same system."],["PARAMETERS","**-delay** _ticks_\n> Delay between frames (centiseconds)\n\n**-loop** _count_\n> Loop count (0 = infinite)\n\n**-geometry** _WxH_\n> Display geometry\n\n**-coalesce**\n> Merge GIF animation layers\n\n**-remote**\n> Enable remote control"],["CAVEATS","This is the ImageMagick 6 version; syntax may differ slightly from ImageMagick 7. Requires X Window System. Some options may be deprecated in newer versions."],["HISTORY","The .im6 naming convention was introduced when ImageMagick 7 was released in **2016**, allowing parallel installation of both major versions."],["SEE ALSO","[animate](/man/animate)(1), [display.im6](/man/display.im6)(1)"]],"e":["animate","display.im6"],"x":"animate.im6 [image1.png] [image2.png]"},"avahi-autoipd":{"n":"avahi-autoipd","t":"Acquire IPv4 link-local addresses automatically","s":[["TAGLINE","Acquire IPv4 link-local addresses automatically"],["TLDR","**Start** the daemon on an interface to auto-configure an IPv4LL address\n\n```avahi-autoipd [interface]```\n\n**Run in background** (daemonize) after startup\n\n```avahi-autoipd -D [interface]```\n\n**Kill** a running daemon on an interface\n\n```avahi-autoipd --kill [interface]```\n\n**Refresh** and re-announce the acquired IP address\n\n```avahi-autoipd --refresh [interface]```\n\n**Check** if daemon is running on an interface\n\n```avahi-autoipd --check [interface]```\n\n**Wait** for successful IP acquisition before detaching\n\n```avahi-autoipd --wait [interface]```"],["SYNOPSIS","**avahi-autoipd** [_options_] _interface_"],["PARAMETERS","**-D, --daemonize**\n> Daemonize after startup; implies syslog logging\n\n**-k, --kill**\n> Terminate running daemon on specified interface\n\n**-r, --refresh**\n> Re-announce acquired IP address (equivalent to SIGHUP)\n\n**-c, --check**\n> Return 0 if daemon is running on interface\n\n**-s, --syslog**\n> Log to syslog instead of STDERR\n\n**--debug**\n> Enable verbose mode for detailed logging\n\n**--no-drop-root**\n> Retain root privileges (not recommended for security)\n\n**--no-chroot**\n> Skip chroot operation\n\n**-S, --start=**_address_\n> Acquire the specified IPv4LL address\n\n**-w, --wait**\n> Wait for successful IP acquisition before detaching\n\n**--force-bind**\n> Acquire IPv4LL address even if routable address is present\n\n**--no-proc-title**\n> Don't modify process name during operation\n\n**-h, --help**\n> Display help information\n\n**-v, --version**\n> Show version details"],["DESCRIPTION","**avahi-autoipd** implements IPv4LL (_Dynamic Configuration of IPv4 Link-Local Addresses_), a protocol defined in **IETF RFC3927** for automatic IP address configuration from the link-local **169.254.0.0/16** range without requiring a central server.\n\nPrimarily intended for **ad-hoc networks** which lack a DHCP server, it serves as a core component of the **Zeroconf stack**. The daemon can operate standalone or integrate with DHCP clients as a fallback mechanism when DHCP is unavailable.\n\nWhen started, **avahi-autoipd** probes the network for address conflicts, acquires an available link-local address, and continuously monitors for conflicts. Upon successful acquisition, it executes **/etc/avahi/avahi-autoipd.action** script to configure the network interface.\n\nThe daemon responds to signals: **SIGINT** and **SIGTERM** trigger shutdown, while **SIGHUP** forces re-announcement of the current IP address."],["CAVEATS","Link-local addresses (**169.254.0.0/16**) only enable communication within the same network segment, with no routing to other networks. Won't assign addresses if a routable IP already exists unless **--force-bind** is used."],["HISTORY","Part of the **Avahi** project, developed as a free software implementation of Apple's Bonjour/Zeroconf protocols. IPv4LL support was standardized in **RFC3927** in **2005**, with **avahi-autoipd** emerging as a lightweight standalone implementation for Linux systems."],["INSTALL","```apt: sudo apt install avahi-autoipd```\n\n```dnf: sudo dnf install avahi-autoipd```\n\n```zypper: sudo zypper install avahi-autoipd```"],["SEE ALSO","[avahi-daemon](/man/avahi-daemon)(8), [dhclient](/man/dhclient)(8), [ip](/man/ip)(8), [ifconfig](/man/ifconfig)(8)"],["RESOURCES","```[Homepage](https://avahi.org/)```\n\n```[Source code](https://github.com/avahi/avahi)```"]],"e":["avahi-daemon","dhclient","ip","ifconfig"],"x":"avahi-autoipd [interface]"},"black":{"n":"black","t":"Opinionated Python code formatter","s":[["TAGLINE","Opinionated Python code formatter"],["TLDR","**Format** Python file\n\n```black [script.py]```\n\nFormat **entire project**\n\n```black [src/]```\n\n**Check** without modifying\n\n```black --check [script.py]```\n\n**Diff** instead of rewriting\n\n```black --diff [script.py]```\n\nSet **line length**\n\n```black -l [100] [script.py]```"],["SYNOPSIS","**black** [_options_] _files_"],["DESCRIPTION","**black** is an opinionated Python code formatter. It enforces a consistent style by reformatting code automatically with minimal configuration. Black's approach is to eliminate debate about formatting by providing one correct way.\n\nThe tool integrates with editors and CI pipelines to ensure consistent Python code style."],["PARAMETERS","**--check**\n> Check if files would be reformatted (exit 1 if yes)\n\n**--diff**\n> Show diff instead of rewriting\n\n**-l**, **--line-length** _n_\n> Line length (default: 88)\n\n**--target-version** _py_\n> Python version (py37, py38, py39, etc.)\n\n**--exclude** _pattern_\n> Files/directories to exclude\n\n**--include** _pattern_\n> Files/directories to include\n\n**--quiet**\n> Minimal output\n\n**--verbose**\n> Verbose output\n\n**--color**/**--no-color**\n> Colored output"],["CONFIGURATION","**pyproject.toml**\n> Project-level configuration under `[tool.black]` section. Supports `line-length`, `target-version`, `include`, and `exclude` settings."],["CAVEATS","Opinionated (limited configuration). May conflict with other formatters. 88 character default line length differs from PEP 8's 79. Some formatting choices controversial. Requires Python 3.6+."],["HISTORY","**Black** was created by Łukasz Langa in **2018** to provide deterministic Python formatting, eliminating formatting debates and reducing diff noise."],["INSTALL","```apt: sudo apt install black```\n\n```dnf: sudo dnf install black```\n\n```apk: sudo apk add black```\n\n```brew: brew install black```\n\n```nix: nix profile install nixpkgs#black```"],["SEE ALSO","[autopep8](/man/autopep8)(1), [isort](/man/isort)(1), [flake8](/man/flake8)(1)"]],"e":["autopep8","isort","flake8"],"x":"black [script.py]"},"cargo-fmt":{"n":"cargo-fmt","t":"Format Rust source code with rustfmt","s":[["TAGLINE","Format Rust source code with rustfmt"],["TLDR","**Format current package**\n\n```cargo fmt```\n\n**Format all packages in workspace**\n\n```cargo fmt --all```\n\n**Check formatting without changes**\n\n```cargo fmt --check```\n\n**Format specific package**\n\n```cargo fmt -p [package]```\n\n**Show diff of changes**\n\n```cargo fmt -- --emit diff```\n\n**Format with verbose output**\n\n```cargo fmt --verbose```"],["SYNOPSIS","**cargo fmt** [_options_] [-- _rustfmt_options_]"],["DESCRIPTION","**cargo fmt** formats all bin and lib files of the current crate using rustfmt. Formats .rs files in src/, tests/, examples/, and integration tests. Reformats code in-place by default."],["PARAMETERS","**--all**\n> Format all packages in workspace\n\n**--check**\n> Check formatting without modifying files (for CI)\n\n**-p**, **--package** _name_\n> Format specific package\n\n**--manifest-path** _path_\n> Path to Cargo.toml\n\n**-v**, **--verbose**\n> Verbose output\n\n**-q**, **--quiet**\n> Suppress output\n\n**-- --emit** _mode_\n> Output mode: files, stdout, diff\n\n**-- --edition** _year_\n> Override Rust edition"],["CONFIGURATION","**rustfmt.toml** or **.rustfmt.toml**\n> Project-level formatting configuration for rustfmt options like max_width, tab_spaces, and edition."],["CAVEATS","Edition automatically read from Cargo.toml. Direct rustfmt defaults to 2015 edition. Set style_edition in config for consistency."],["INSTALL","```apt: sudo apt install rustup```\n\n```dnf: sudo dnf install rustup```\n\n```pacman: sudo pacman -S rustup```\n\n```apk: sudo apk add rustfmt```\n\n```zypper: sudo zypper install rustup```\n\n```brew: brew install rustup```\n\n```nix: nix profile install nixpkgs#rustup```"],["SEE ALSO","[cargo](/man/cargo)(1), [rustfmt](/man/rustfmt)(1), [cargo-clippy](/man/cargo-clippy)(1)"]],"e":["cargo","rustfmt","cargo-clippy"],"x":"cargo fmt"},"cat":{"n":"cat","t":"concatenate and display file contents","s":[["TAGLINE","concatenate and display file contents"],["TLDR","Print the contents of a file to **stdout**\n\n```cat [path/to/file]```\n\n**Concatenate** several files into an output file\n\n```cat [file1] [file2] > [output_file]```\n\n**Append** several files to an output file\n\n```cat [file1] [file2] >> [output_file]```\n\n**Number** all output lines\n\n```cat -n [path/to/file]```\n\nDisplay **all characters** including tabs and line endings\n\n```cat -A [path/to/file]```\n\nPass file contents to another program through **stdin**\n\n```cat [path/to/file] | [program]```"],["SYNOPSIS","**cat** [_-n_] [_-A_] [_-b_] [_-s_] [_file_...]"],["DESCRIPTION","**cat** (concatenate) reads files sequentially and writes their contents to standard output. It is one of the most frequently used Unix utilities, serving as the standard way to display file contents, combine multiple files, and pipe data to other commands in shell pipelines.\n\nWhen given multiple file arguments, cat concatenates them in order, making it useful for joining split files or appending content. With no file arguments or a dash (-), it reads from standard input, which allows it to function as a simple pass-through in pipelines. Various flags control output formatting, including line numbering and display of non-printing characters."],["PARAMETERS","**-n, --number**\n> Number all output lines\n\n**-b, --number-nonblank**\n> Number non-blank output lines only\n\n**-s, --squeeze-blank**\n> Suppress repeated empty output lines\n\n**-A, --show-all**\n> Equivalent to -vET; show all characters\n\n**-E, --show-ends**\n> Display $ at end of each line\n\n**-T, --show-tabs**\n> Display TAB characters as ^I\n\n**-v, --show-nonprinting**\n> Use ^ and M- notation for non-printing characters\n\n**-e**\n> Equivalent to -vE\n\n**-t**\n> Equivalent to -vT"],["CAVEATS","For large files, consider using **less** or **head/tail** instead. Using cat to pipe a single file to another command (UUOC - Useless Use of Cat) is often unnecessary."],["HISTORY","Part of **GNU Coreutils**. Originated in **1971** for first Edition Unix by Ken Thompson at Bell Labs. One of the oldest and most fundamental Unix utilities."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[less](/man/less)(1), [more](/man/more)(1), [head](/man/head)(1), [tail](/man/tail)(1), [tac](/man/tac)(1)"],["RESOURCES","```[Source code](https://github.com/coreutils/coreutils)```\n\n```[Documentation](https://www.gnu.org/software/coreutils/manual/html_node/cat-invocation.html)```"]],"e":["less","more","head","tail","tac"],"x":"cat [path/to/file]"},"compseq":{"n":"compseq","t":"calculate nucleotide and amino acid composition","s":[["TAGLINE","calculate nucleotide and amino acid composition"],["TLDR","Count word frequencies with **interactive prompt**\n\n```compseq [path/to/file.fasta]```\n\nCount **amino acid pairs** from protein sequence\n\n```compseq [path/to/input_protein.fasta] -word 2 [path/to/output_file.comp]```\n\nCount **hexanucleotides**, ignoring zero counts\n\n```compseq [path/to/input_dna.fasta] -word 6 [path/to/output_file.comp] -nozero```\n\nCount **codons** in a specific reading frame\n\n```compseq -sequence [path/to/input_rna.fasta] -word 3 [path/to/output_file.comp] -nozero -frame [1]```\n\n**Compare** frequencies to a previous run\n\n```compseq -sequence [path/to/file.fasta] -word 3 [path/to/output.comp] -nozero -infile [path/to/previous.comp]```\n\nCalculate **expected frequencies** from input\n\n```compseq -sequence [path/to/file.fasta] -word 3 [path/to/output.comp] -nozero -calcfreq```"],["SYNOPSIS","**compseq** [_options_] _sequence_ _outfile_"],["DESCRIPTION","**compseq** is a bioinformatics tool from the EMBOSS (European Molecular Biology Open Software Suite) package that performs k-mer frequency analysis on DNA, RNA, and protein sequences. It reads sequences in FASTA format and generates compositional statistics showing how often each possible word (k-mer) of a specified length appears in the sequence.\n\nThe tool is widely used in molecular biology for multiple applications: analyzing codon usage bias in genes, identifying sequence composition patterns that indicate functional or structural elements, and comparing sequence characteristics across organisms or genomic regions. By specifying different word sizes, researchers can examine dinucleotide frequencies (word size 2), codon frequencies (word size 3), or longer oligonucleotide patterns.\n\ncompseq can calculate both observed frequencies and expected frequencies based on the overall base composition, making it useful for identifying statistically significant deviations from random distribution. The reading frame parameter allows analysis of coding sequences in specific frames, critical for studying codon usage patterns in protein-coding genes."],["PARAMETERS","**-word** _size_\n> Word size to count (e.g., 2 for dinucleotides, 3 for codons)\n\n**-frame** _number_\n> Reading frame (1, 2, or 3)\n\n**-nozero**\n> Ignore zero counts in output\n\n**-infile** _file_\n> Compare to previous compseq output\n\n**-calcfreq**\n> Calculate expected frequencies from input\n\n**-help**\n> Display help"],["CAVEATS","Part of the EMBOSS suite. Input must be valid FASTA format. Large word sizes can produce very large output tables."],["INSTALL","```apt: sudo apt install emboss```\n\n```nix: nix profile install nixpkgs#emboss```"]],"e":[],"x":"compseq [path/to/file.fasta]"},"cpufreq-aperf":{"n":"cpufreq-aperf","t":"calculate actual CPU frequency using hardware counters","s":[["TAGLINE","calculate actual CPU frequency using hardware counters"],["TLDR","Start calculating for **all CPUs** with 1 second interval\n\n```sudo cpufreq-aperf```\n\nCalculate for **specific CPU**\n\n```sudo cpufreq-aperf -c [1]```\n\nSet **refresh interval** to 3 seconds\n\n```sudo cpufreq-aperf -i [3]```\n\nCalculate **only once**\n\n```sudo cpufreq-aperf -o```"],["SYNOPSIS","**cpufreq-aperf** [_options_]"],["DESCRIPTION","**cpufreq-aperf** calculates the average CPU frequency over a time period using APERF/MPERF MSR registers. This provides actual operating frequency rather than the requested frequency.\n\nUseful for verifying CPU frequency scaling and turbo boost behavior."],["PARAMETERS","**-c, --cpu** _cpu_\n> Specific CPU to monitor\n\n**-i, --interval** _seconds_\n> Refresh interval in seconds\n\n**-o, --once**\n> Calculate once and exit"],["CAVEATS","Requires root privileges. Only works on CPUs that support APERF/MPERF counters (Intel and AMD processors). Part of cpufrequtils."],["INSTALL","```apk: sudo apk add cpufrequtils```\n\n```nix: nix profile install nixpkgs#cpufrequtils```"],["SEE ALSO","[cpufreq-info](/man/cpufreq-info)(1), [cpufreq-set](/man/cpufreq-set)(1), [cpupower](/man/cpupower)(1)"],["RESOURCES","```[Source code](https://git.kernel.org/pub/scm/utils/kernel/cpufreq/cpufrequtils.git/)```"]],"e":["cpufreq-info","cpufreq-set","cpupower"],"x":"sudo cpufreq-aperf"},"crane-index":{"n":"crane-index","t":"manage multi-platform image indexes","s":[["TAGLINE","manage multi-platform image indexes"],["TLDR","**Show index** subcommands\n\n```crane index --help```"],["SYNOPSIS","**crane** **index** _subcommand_ [_options_]"],["SUBCOMMANDS","**append**\n> Add an image to a multi-platform index.\n\n**filter**\n> Filter platforms from an index."],["DESCRIPTION","**crane index** manages multi-platform image indexes (manifest lists). It allows creating and modifying indexes that reference platform-specific images."],["SEE ALSO","[crane](/man/crane)(1), [crane-index-append](/man/crane-index-append)(1), [crane-index-filter](/man/crane-index-filter)(1)"],["RESOURCES","```[Source code](https://github.com/google/go-containerregistry)```\n\n```[Documentation](https://github.com/google/go-containerregistry/blob/main/cmd/crane/doc/crane_index.md)```"]],"e":["crane","crane-index-append","crane-index-filter"],"x":"crane index --help"},"cu":{"n":"cu","t":"serial port terminal communication tool","s":[["TAGLINE","serial port terminal communication tool"],["TLDR","**Open** a serial port\n\n```sudo cu -l [/dev/ttyXYZ]```\n\nOpen serial port with **specific baud rate**\n\n```sudo cu -l [/dev/ttyXYZ] -s [115200]```\n\nOpen with **half-duplex** mode (local echo)\n\n```sudo cu -l [/dev/ttyXYZ] -s [115200] -h```\n\nOpen with **parity** and no flow control\n\n```sudo cu -l [/dev/ttyXYZ] -s [115200] --parity=[even] -f --nostop```\n\n**Exit** the cu session\n\n```<Enter><~><.>```"],["SYNOPSIS","**cu** [_options_] [_system_|_phone_|_host_]"],["DESCRIPTION","**cu** (Call Up) connects to another system acting as a dial-in terminal. It's commonly used for serial console access to embedded devices, routers, switches, and other hardware.\n\nThe escape sequence to disconnect is: press Enter, then ~ (tilde), then . (period)."],["PARAMETERS","**-l, --line** _device_\n> Serial device to use\n\n**-s, --speed** _baud_\n> Baud rate (e.g., 9600, 115200)\n\n**-h, --halfduplex**\n> Echo characters locally\n\n**--parity** _type_\n> Parity: even, odd, or none\n\n**-f, --nortscts**\n> Disable hardware flow control\n\n**--nostop**\n> Disable software flow control"],["CAVEATS","Usually requires root or membership in the dialout/uucp groups. The escape character (~) must be at the start of a line. cu is part of the UUCP package (commonly GNU/Taylor UUCP), not coreutils."],["INSTALL","```apt: sudo apt install cu```\n\n```dnf: sudo dnf install cu```"],["SEE ALSO","[screen](/man/screen)(1), [minicom](/man/minicom)(1), [picocom](/man/picocom)(1)"],["RESOURCES","```[Homepage](https://www.gnu.org/software/uucp/)```"]],"e":["screen","minicom","picocom"],"x":"sudo cu -l [/dev/ttyXYZ]"},"cupsenable":{"n":"cupsenable","t":"resume printer job processing","s":[["TAGLINE","resume printer job processing"],["TLDR","**Enable a printer**\n\n```cupsenable [printer_name]```\n\n**Enable and release held jobs**\n\n```cupsenable --release [printer_name]```\n\n**Enable a printer on a remote server**\n\n```cupsenable -h [server:port] [printer_name]```\n\n**Authenticate as a specific user**\n\n```cupsenable -U [username] [printer_name]```\n\n**Force an encrypted connection**\n\n```cupsenable -E [printer_name]```"],["SYNOPSIS","**cupsenable** [**-E**] [**-U** _username_] [**-c**] [**-h** _server[:port]_] [**--release**] _destination(s)_"],["PARAMETERS","**-E**\n> Force encryption of the connection to the server.\n\n**-U** _username_\n> Use the specified username when connecting to the server.\n\n**-c**\n> Cancel all jobs on the named destination.\n\n**-h** _server[:port]_\n> Connect to the specified server and port.\n\n**--release**\n> Release previously held jobs for printing. Use after **cupsdisable --hold** to resume printing."],["DESCRIPTION","**cupsenable** starts the named printers or classes, allowing them to process queued jobs. This reverses the effect of **cupsdisable**.\n\nUnlike the System V versions (_enable_ / _disable_), the CUPS commands have been renamed to avoid conflicts with the bash(1) built-ins. Printer and class names are not case-sensitive and may contain any printable character except SPACE, TAB, \"/\", or \"#\"."],["CAVEATS","Depending on the CUPS configuration, the command may prompt for an administrator password rather than requiring the root user. The **--release** flag is commonly paired with **cupsdisable --hold** for scheduled maintenance."],["INSTALL","```apt: sudo apt install cups-client```\n\n```dnf: sudo dnf install cups-client```\n\n```apk: sudo apk add cups-client```\n\n```zypper: sudo zypper install cups-client```"],["SEE ALSO","[cupsdisable](/man/cupsdisable)(1), [cupsaccept](/man/cupsaccept)(1), [cupsreject](/man/cupsreject)(1), [cancel](/man/cancel)(1), [lp](/man/lp)(1), [lpadmin](/man/lpadmin)(1), [lpstat](/man/lpstat)(1)"]],"e":["cupsdisable","cupsaccept","cupsreject","cancel","lp","lpadmin","lpstat"],"x":"cupsenable [printer_name]"},"dircolors":{"n":"dircolors","t":"color scheme configurator for ls","s":[["TAGLINE","color scheme configurator for ls"],["TLDR","**Output shell commands** to set LS_COLORS\n\n```dircolors```\n\n**Use a specific database file**\n\n```dircolors [/path/to/dircolors.db]```\n\n**Output for Bourne shell**\n\n```dircolors -b```\n\n**Output for C shell**\n\n```dircolors -c```\n\n**Print default database**\n\n```dircolors -p```"],["SYNOPSIS","**dircolors** [_options_] [_file_]"],["PARAMETERS","**-b**, **--sh**, **--bourne-shell**\n> Output Bourne shell commands.\n\n**-c**, **--csh**, **--c-shell**\n> Output C shell commands.\n\n**-p**, **--print-database**\n> Print the default database."],["CONFIGURATION","**~/.dircolors** or **~/.dir_colors**\n> User-specific color configuration database mapping file types and extensions to colors.\n\n**/etc/DIR_COLORS**\n> System-wide default color configuration."],["DESCRIPTION","**dircolors** outputs shell commands to set the LS_COLORS environment variable, which controls the colors used by **ls** and other tools. It reads a configuration file that maps file types and extensions to colors.\n\nTypically invoked in shell configuration files: **eval \"$(dircolors)\"**."],["CAVEATS","Output must be evaluated by the shell (e.g., `eval \"$(dircolors)\"`) to take effect. The default database covers common file types; customize with `dircolors -p > ~/.dircolors` and edit. Only affects tools that read LS_COLORS (primarily GNU ls and compatible tools)."],["HISTORY","**dircolors** is part of GNU coreutils and has been included in Linux distributions since the early days of colorized terminal output."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[ls](/man/ls)(1), [dir](/man/dir)(1)"]],"e":["ls","dir"],"x":"dircolors"},"dlltool":{"n":"dlltool","t":"Windows DLL import library creator","s":[["TAGLINE","Windows DLL import library creator"],["TLDR","**Create import library** from def file\n\n```dlltool -d [library.def] -l [library.a]```\n\n**Create def file** from DLL\n\n```dlltool -z [output.def] [library.dll]```\n\n**Create DLL from** def and objects\n\n```dlltool -d [library.def] -e [exports.o] [objects.o]```\n\n**Specify machine type**\n\n```dlltool -m [i386] -d [library.def] -l [library.a]```\n\n**Create both import** lib and exports\n\n```dlltool -d [library.def] -l [library.a] -e [exports.o]```"],["SYNOPSIS","**dlltool** [_options_] [_file_...]"],["PARAMETERS","**-d**, **--input-def** _FILE_\n> Read the named .def file.\n\n**-l**, **--output-lib** _FILE_\n> Generate an import library.\n\n**-e**, **--output-exp** _FILE_\n> Generate an exports file.\n\n**-z**, **--output-def** _FILE_\n> Generate a .def file (from DLL or objects).\n\n**-y**, **--output-delaylib** _FILE_\n> Create a delay-import library.\n\n**-m**, **--machine** _MACHINE_\n> Target machine type (e.g. i386, x86_64, arm).\n\n**-D**, **--dllname** _NAME_\n> Name of input DLL recorded in the library.\n\n**-U**, **--add-underscore**\n> Prepend an underscore to all exported symbols.\n\n**-k**, **--kill-at**\n> Strip @n suffix from stdcall exported names.\n\n**-A**, **--add-stdcall-alias**\n> Add aliases for stdcall symbols without @n.\n\n**--export-all-symbols**\n> Export all global and weak symbols.\n\n**-I**, **--identify** _FILE_\n> Report DLL name associated with an import library.\n\n**-v**, **--verbose**\n> Describe what is being done.\n\n**--help**\n> Display help information."],["DESCRIPTION","**dlltool** is a GNU binutils tool for creating files needed to build and use Dynamic Link Libraries (DLLs) on Windows. It processes module definition (.def) files and creates import libraries and exports files.\n\nImport libraries allow linking against DLLs at compile time. The tool generates the necessary stubs that resolve at runtime. Export files define symbols that a DLL makes available to other programs.\n\ndlltool is essential for cross-compiling Windows software on Unix systems and for creating DLLs with MinGW and similar toolchains."],["CAVEATS","Windows-specific tool. Def file syntax must be correct. Machine type must match target architecture. Import library format may vary between toolchains."],["HISTORY","dlltool is part of **GNU binutils**, developed for supporting Windows DLL creation in cross-compilation environments. It enables building Windows software using GNU toolchains like MinGW."],["SEE ALSO","[ld](/man/ld)(1), [nm](/man/nm)(1), [objdump](/man/objdump)(1), [windres](/man/windres)(1)"]],"e":["ld","nm","objdump","windres"],"x":"dlltool -d [library.def] -l [library.a]"},"docker-container-diff":{"n":"docker-container-diff","t":"show container filesystem changes","s":[["TAGLINE","show container filesystem changes"],["TLDR","**Show filesystem changes** in a container\n\n```docker container diff [container]```"],["SYNOPSIS","**docker** **container** **diff** _container_"],["DESCRIPTION","**docker container diff** inspects changes to files or directories on a container's filesystem. Shows files that were added (A), changed (C), or deleted (D) compared to the base image."],["INSTALL","```apt: sudo apt install docker-cli```\n\n```dnf: sudo dnf install docker-cli```\n\n```pacman: sudo pacman -S docker```\n\n```apk: sudo apk add docker-cli```\n\n```zypper: sudo zypper install docker```\n\n```brew: brew install docker```\n\n```nix: nix profile install nixpkgs#docker```"],["SEE ALSO","[docker-diff](/man/docker-diff)(1), [docker-container](/man/docker-container)(1)"],["RESOURCES","```[Source code](https://github.com/docker/cli)```\n\n```[Documentation](https://docs.docker.com/reference/cli/docker/)```"]],"e":["docker-diff","docker-container"],"x":"docker container diff [container]"},"ejabberdctl":{"n":"ejabberdctl","t":"ejabberd XMPP server control utility","s":[["TAGLINE","ejabberd XMPP server control utility"],["TLDR","**Start ejabberd server**\n\n```sudo ejabberdctl start```\n\n**Stop ejabberd server**\n\n```sudo ejabberdctl stop```\n\n**Check server status**\n\n```ejabberdctl status```\n\n**Register new user**\n\n```ejabberdctl register [user] [domain] [password]```\n\n**Unregister user**\n\n```ejabberdctl unregister [user] [domain]```\n\n**List registered users**\n\n```ejabberdctl registered_users [domain]```\n\n**Restart server**\n\n```sudo ejabberdctl restart```"],["SYNOPSIS","**ejabberdctl** [_options_] _command_ [_args_]"],["PARAMETERS","**start**\n> Start ejabberd server.\n\n**stop**\n> Stop ejabberd server.\n\n**restart**\n> Restart ejabberd server.\n\n**status**\n> Show server status.\n\n**register** _user_ _host_ _password_\n> Register new user.\n\n**unregister** _user_ _host_\n> Remove user.\n\n**registered_users** _host_\n> List users on host.\n\n**connected_users**\n> List online users.\n\n**kick_user** _user_ _host_\n> Disconnect user.\n\n**reload_config**\n> Reload configuration.\n\n**backup** _file_\n> Backup database.\n\n**restore** _file_\n> Restore database."],["CONFIGURATION","**/etc/ejabberd/ejabberd.yml**\n> Main ejabberd configuration file controlling server behavior, virtual hosts, and modules."],["DESCRIPTION","**ejabberdctl** is the command-line administration tool for ejabberd, an open-source XMPP (Jabber) instant messaging server. It manages server operations, user accounts, and configuration.\n\nThe tool communicates with the running ejabberd node through Erlang RPC, allowing administration without accessing the web interface. It supports all administrative functions available through the web admin."],["CAVEATS","Requires ejabberd to be installed and configured. Some commands need root privileges. Server must be running for most commands. Database changes may require restart. Erlang environment must be properly configured."],["HISTORY","ejabberd was developed by **Alexey Shchepin** starting in **2002**, written in Erlang for scalability and fault tolerance. It became one of the most widely deployed XMPP servers, used by WhatsApp and other messaging platforms at scale."],["INSTALL","```apt: sudo apt install ejabberd```\n\n```pacman: sudo pacman -S ejabberd```\n\n```apk: sudo apk add ejabberd```\n\n```brew: brew install ejabberd```\n\n```nix: nix profile install nixpkgs#ejabberd```"],["SEE ALSO","[prosodyctl](/man/prosodyctl)(1)"]],"e":["prosodyctl"],"x":"sudo ejabberdctl start"},"gcrane-copy":{"n":"gcrane-copy","t":"copies container images between registries","s":[["TAGLINE","copies container images between registries"],["TLDR","**Copy image between registries**\n\n```gcrane copy [source_image] [dest_image]```\n\n**Recursively copy a repository**\n\n```gcrane copy -r [source_repo] [dest_repo]```\n\n**Copy with platform filter**\n\n```gcrane copy --platform linux/amd64 [source] [dest]```\n\n**Copy with parallel jobs**\n\n```gcrane copy --jobs [4] [source] [dest]```"],["SYNOPSIS","**gcrane** **copy** [_options_] _src_ _dst_"],["PARAMETERS","**-r**, **--recursive**\n> Recursively copy repositories (copy all tags and sub-repositories).\n\n**--platform** _platform_\n> Platform to copy (e.g., linux/amd64). Use `all` to copy all platforms.\n\n**--jobs** _num_\n> Number of parallel copy jobs.\n\n**--allow-nondistributable-artifacts**\n> Allow pushing non-distributable (foreign) layers.\n\n**--insecure**\n> Allow image references to be fetched without TLS."],["DESCRIPTION","**gcrane copy** copies container images between registries, supporting both single images and repository-wide operations. It handles multi-architecture images and maintains all image metadata during transfer.\n\nThe command can copy individual tagged images or use the **-r** flag to recursively copy all tags and sub-repositories. Platform filtering allows selective copying for specific architectures. Parallel job execution speeds up bulk copy operations. gcrane copy is commonly used for backup, geo-replication between regions or projects, and promoting images from staging to production registries.\n\n**gcrane** extends **crane** with Google Container Registry and Artifact Registry specific features such as recursive repository copy."],["INSTALL","```zypper: sudo zypper install gcrane```\n\n```nix: nix profile install nixpkgs#gcrane```"],["SEE ALSO","[gcrane](/man/gcrane)(1), [crane-copy](/man/crane-copy)(1)"]],"e":["gcrane","crane-copy"],"x":"gcrane copy [source_image] [dest_image]"},"get-nodeversions":{"n":"get-nodeversions","t":"Node.js version discovery tool","s":[["TAGLINE","Node.js version discovery tool"],["TLDR","**List locally installed versions**\n\n```Get-NodeVersions```\n\n**List remote versions** available to install\n\n```Get-NodeVersions -Remote```\n\n**Filter versions** by semver range\n\n```Get-NodeVersions -Remote -Filter \">=18.0.0 <19.0.0\"```\n\n**List local versions** matching a filter\n\n```Get-NodeVersions -Filter \">=16.0.0\"```"],["SYNOPSIS","**Get-NodeVersions** [**-Remote**] [**-Filter** _range_]"],["PARAMETERS","**-Remote**\n> List versions available for download from the Node.js release server instead of locally installed versions.\n\n**-Filter** _range_\n> A semver version range to filter results (e.g., \">=18.0.0 <19.0.0\")."],["DESCRIPTION","**Get-NodeVersions** lists Node.js versions installed locally or available remotely for installation. It is part of the **ps-nvm** (PowerShell Node Version Manager) module.\n\nVersions are returned as SemVer.Version objects sorted from highest to lowest, and can be compared with PowerShell comparison operators. Use **-Remote** to query the Node.js release server and **-Filter** to narrow results by semver range."],["CAVEATS","The **-Remote** flag requires network access to query the Node.js release server. This command is specific to ps-nvm; other version managers use different commands (e.g., `nvm ls-remote`)."],["HISTORY","get-nodeversions is a utility provided by the **ps-nvm** PowerShell module for Node.js version management."],["SEE ALSO","[nvm](/man/nvm)(1), [fnm](/man/fnm)(1), [node](/man/node)(1), [ps-nvm](/man/ps-nvm)(1)"]],"e":["nvm","fnm","node","ps-nvm"],"x":"Get-NodeVersions"},"gettext":{"n":"gettext","t":"internationalization message catalog lookup","s":[["TAGLINE","internationalization message catalog lookup"],["TLDR","**Display translated string**\n\n```gettext [domain] \"[message]\"```\n\n**Translate from catalog**\n\n```TEXTDOMAIN=[domain] gettext \"[message]\"```\n\n**With context**\n\n```gettext -c \"[context]\" \"[message]\"```\n\n**Plural forms**\n\n```ngettext \"[singular]\" \"[plural]\" [count]```"],["SYNOPSIS","**gettext** [_options_] [_domain_] _msgid_"],["PARAMETERS","_DOMAIN_\n> Message catalog domain.\n\n_MSGID_\n> Message to translate.\n\n**-d** _DOMAIN_\n> Text domain.\n\n**-e**\n> Enable escape interpretation.\n\n**-E**\n> Preserve but don't interpret backslashes.\n\n**-c** _CONTEXT_\n> Message context.\n\n**--help**\n> Display help information."],["DESCRIPTION","**gettext** retrieves translated strings from message catalogs. It's the runtime component of the GNU internationalization system, looking up translations based on the current locale.\n\nThe tool searches .mo files for translations, returning the original if none found. It enables shell scripts and programs to produce localized output.\n\ngettext enables internationalization of command-line applications."],["CONFIGURATION","**/usr/share/locale/[LANG]/LC_MESSAGES/[domain].mo**\n> Compiled message catalogs containing translations for specific domains and locales."],["CAVEATS","Requires compiled .mo catalogs. LANG/LC_MESSAGES must be set. Missing translations return original."],["HISTORY","gettext was developed by **GNU** for internationalization. It became the standard Unix localization system, enabling software to support multiple languages."],["INSTALL","```apt: sudo apt install gettext-base```\n\n```dnf: sudo dnf install gettext```\n\n```pacman: sudo pacman -S gettext```\n\n```apk: sudo apk add gettext```\n\n```brew: brew install gettext```\n\n```nix: nix profile install nixpkgs#gettext```"],["SEE ALSO","[xgettext](/man/xgettext)(1), [msgfmt](/man/msgfmt)(1), [envsubst](/man/envsubst)(1)"],["RESOURCES","```[Homepage](https://www.gnu.org/software/gettext/)```\n\n```[Documentation](https://www.gnu.org/software/gettext/manual/gettext.html)```"]],"e":["xgettext","msgfmt","envsubst"],"x":"gettext [domain] \"[message]\""},"gh-accessibility":{"n":"gh-accessibility","t":"manage accessibility settings for GitHub CLI","s":[["TAGLINE","manage accessibility settings for GitHub CLI"],["TLDR","**Show accessibility guidance and settings**\n\n```gh accessibility```\n\n**Open the GitHub Accessibility site in the browser**\n\n```gh accessibility --web```\n\n**Enable accessible 4-bit color palette**\n\n```gh config set accessible_colors enabled```\n\n**Enable accessible prompter for screen readers**\n\n```gh config set accessible_prompter enabled```\n\n**Disable motion-based spinners in favor of text indicators**\n\n```gh config set spinner disabled```"],["SYNOPSIS","**gh** **accessibility** [_flags_]"],["PARAMETERS","**-w**, **--web**\n> Open the GitHub Accessibility site in the default web browser.\n\n**--help**\n> Show help for command."],["DESCRIPTION","**gh accessibility** displays guidance on GitHub CLI's accessibility experiences and how to enable them. It is aliased as **gh a11y**.\n\nThe command describes four areas of accessibility support:\n\n**Accessible colors** — enables a 4-bit ANSI color palette with increased contrast based on terminal background (dark or light). This allows users to customize colors through their terminal preferences. Enable with `gh config set accessible_colors enabled` or the `GH_ACCESSIBLE_COLORS` environment variable.\n\n**Label colors** — displays issue and pull request label colors using their custom RGB hex values in terminals with true color support. Enable with `gh config set color_labels enabled` or the `GH_COLOR_LABELS` environment variable.\n\n**Accessible prompter** — replaces cursor-manipulating interactive prompts with non-interactive equivalents that work correctly with speech synthesizers and braille displays. Enable with `gh config set accessible_prompter enabled` or the `GH_ACCESSIBLE_PROMPTER` environment variable.\n\n**Text-based spinners** — replaces animated spinner characters with plain text progress indicators, removing motion that may cause discomfort or be misread by screen readers. Enable with `gh config set spinner disabled` or the `GH_SPINNER_DISABLED` environment variable."],["CAVEATS","Accessibility features were introduced as a **public preview** in GitHub CLI v2.72.0. Individual features must be enabled explicitly via `gh config set` or environment variables; running `gh accessibility` alone only displays the guidance text."],["INSTALL","```apt: sudo apt install gh```\n\n```dnf: sudo dnf install gh```\n\n```pacman: sudo pacman -S github-cli```\n\n```apk: sudo apk add github-cli```\n\n```zypper: sudo zypper install gh```\n\n```brew: brew install gh```\n\n```nix: nix profile install nixpkgs#gh```"],["SEE ALSO","[gh](/man/gh)(1), [gh-config](/man/gh-config)(1), [gh-environment](/man/gh-environment)(1)"]],"e":["gh","gh-config","gh-environment"],"x":"gh accessibility"},"gh-alias":{"n":"gh-alias","t":"GitHub CLI command alias management","s":[["TAGLINE","GitHub CLI command alias management"],["TLDR","**List aliases**\n\n```gh alias list```\n\n**Set an alias**\n\n```gh alias set [name] \"[command]\"```\n\n**Delete an alias**\n\n```gh alias delete [name]```\n\n**Set with shell expansion**\n\n```gh alias set --shell [name] \"[shell command]\"```"],["SYNOPSIS","**gh** **alias** _command_ [_options_]"],["SUBCOMMANDS","**list**\n> List defined aliases.\n\n**set**\n> Create an alias.\n\n**delete**\n> Remove an alias (use **--all** to remove all).\n\n**import**\n> Import aliases from a YAML file (or stdin with `-`)."],["PARAMETERS","**--shell**\n> Expand as shell command."],["DESCRIPTION","**gh alias** manages command aliases for the GitHub CLI. Create shortcuts for frequently used or complex commands to streamline your GitHub workflow.\n\nAliases can simplify multi-step operations, provide shorthand for common tasks, and customize the CLI to match your development patterns. Shell aliases support piping and advanced command composition, while regular aliases work with gh subcommands and flags."],["INSTALL","```apt: sudo apt install gh```\n\n```dnf: sudo dnf install gh```\n\n```pacman: sudo pacman -S github-cli```\n\n```apk: sudo apk add github-cli```\n\n```zypper: sudo zypper install gh```\n\n```brew: brew install gh```\n\n```nix: nix profile install nixpkgs#gh```"],["SEE ALSO","[gh](/man/gh)(1), [gh-config](/man/gh-config)(1)"]],"e":["gh","gh-config"],"x":"gh alias list"},"git-diff":{"n":"git-diff","t":"Show differences between commits and files","s":[["TAGLINE","Show differences between commits and files"],["TLDR","**Show unstaged changes**\n\n```git diff```\n\n**Show staged changes**\n\n```git diff --staged```\n\n**Diff between branches**\n\n```git diff [branch1]..[branch2]```\n\n**Diff specific file**\n\n```git diff [file.txt]```\n\n**Show word-level diff**\n\n```git diff --word-diff```\n\n**Show stat summary**\n\n```git diff --stat```\n\n**Diff between commits**\n\n```git diff [commit1] [commit2]```\n\n**Diff against merge base** (changes on a branch since it diverged)\n\n```git diff [main]...[feature-branch]```"],["SYNOPSIS","**git diff** [_options_] [_commit_] [**--**] [_path_...]"],["PARAMETERS","**--staged**, **--cached**\n> Compare staged changes to last commit.\n\n**--stat**\n> Show diffstat summary.\n\n**--word-diff**\n> Show word-level changes.\n\n**--color-words**\n> Colored word diff.\n\n**--name-only**\n> Show only changed file names.\n\n**--name-status**\n> Show names and change status.\n\n**-U** _n_, **--unified** _n_\n> Lines of context.\n\n**--no-index**\n> Compare files outside repository.\n\n**--diff-filter** _filter_\n> Filter by change type (A, D, M, R).\n\n**-b**, **--ignore-space-change**\n> Ignore whitespace changes.\n\n**-w**, **--ignore-all-space**\n> Ignore all whitespace.\n\n**-M**, **--find-renames**\n> Detect and report renamed files.\n\n**-C**, **--find-copies**\n> Detect and report copied files."],["DESCRIPTION","**git diff** shows differences between commits, branches, working directory, and staging area. It is essential for reviewing changes before committing and understanding what changed between versions.\n\nBy default, it shows unstaged changes (working directory vs index). With --staged, it shows what will be committed. When given two commits or branches, it shows all changes between them."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git](/man/git)(1), [git-log](/man/git-log)(1), [diff](/man/diff)(1), [vimdiff](/man/vimdiff)(1)"]],"e":["git","git-log","diff","vimdiff"],"x":"git diff"},"gradle-projects":{"n":"gradle-projects","t":"Gradle subproject hierarchy viewer","s":[["TAGLINE","Gradle subproject hierarchy viewer"],["TLDR","**List all subprojects**\n\n```gradle projects```\n\n**List with wrapper**\n\n```./gradlew projects```\n\n**List for specific project**\n\n```gradle :[subproject]:projects```"],["SYNOPSIS","**gradle** **projects**"],["DESCRIPTION","**gradle projects** displays a hierarchical list of all subprojects in a multi-project Gradle build. The output shows the root project and all its subprojects with their paths, helping navigate complex builds with multiple modules."],["INSTALL","```pacman: sudo pacman -S gradle```\n\n```apk: sudo apk add gradle```\n\n```brew: brew install gradle```\n\n```nix: nix profile install nixpkgs#gradle```"],["SEE ALSO","[gradle](/man/gradle)(1), [gradle-tasks](/man/gradle-tasks)(1)"],["RESOURCES","```[Documentation](https://docs.gradle.org/current/userguide/command_line_interface.html)```"]],"e":["gradle","gradle-tasks"],"x":"gradle projects"},"gtf":{"n":"gtf","t":"calculates VESA Generalized Timing Formula modelines","s":[["TAGLINE","calculates VESA Generalized Timing Formula modelines"],["TLDR","**Generate modeline for 1920x1080@60Hz**\n\n```gtf 1920 1080 60```\n\n**Generate for specific refresh rate**\n\n```gtf [1280] [1024] [75]```\n\n**Output in X format**\n\n```gtf 1920 1080 60 -x```\n\n**Verbose output**\n\n```gtf 1920 1080 60 -v```"],["SYNOPSIS","**gtf** _h-pixels_ _v-pixels_ _refresh_ [_options_]"],["PARAMETERS","_H-PIXELS_\n> Horizontal resolution.\n\n_V-PIXELS_\n> Vertical resolution.\n\n_REFRESH_\n> Refresh rate in Hz.\n\n**-x**\n> Output in X11 modeline format.\n\n**-v**, **--verbose**\n> Verbose output.\n\n**--help**\n> Display help information."],["DESCRIPTION","**gtf** calculates VESA Generalized Timing Formula modelines. It generates the timing parameters needed for X11 configuration at specific resolutions.\n\nThe tool outputs modelines suitable for xorg.conf. It's useful for setting up custom display modes not automatically detected."],["CAVEATS","Modern systems auto-detect modes. May damage older monitors. Use cvt for newer standard."],["HISTORY","gtf was developed for **XFree86/X.Org** to generate VESA GTF compliant modelines for monitor configuration."],["INSTALL","```apt: sudo apt install xserver-xorg-core```\n\n```pacman: sudo pacman -S xorg-server```\n\n```apk: sudo apk add xorg-server```\n\n```brew: brew install xorg-server```\n\n```nix: nix profile install nixpkgs#xorg-server```"],["SEE ALSO","[cvt](/man/cvt)(1), [xrandr](/man/xrandr)(1)"]],"e":["cvt","xrandr"],"x":"gtf 1920 1080 60"},"gvedit":{"n":"gvedit","t":"Simple graph editor and viewer for Graphviz","s":[["TAGLINE","Simple graph editor and viewer for Graphviz"],["TLDR","**Open gvedit**\n\n```gvedit```\n\n**Open a graph file**\n\n```gvedit [graph.gv]```\n\n**Open multiple graph files**\n\n```gvedit [file1.gv] [file2.gv]```\n\n**Open with verbose output**\n\n```gvedit -v [graph.gv]```"],["SYNOPSIS","**gvedit** [**-v**] [**-s**] [**-?**] [_file_ ...]"],["PARAMETERS","**-s**\n> Scale node position coordinates from points to inches. Useful when layout algorithms (fdp, neato) receive input with coordinates in points.\n\n**-v**\n> Enable verbose mode for additional output.\n\n**-?**\n> Display usage information and exit."],["DESCRIPTION","**gvedit** is a simple graph editor and viewer that is part of the Graphviz package. It provides a GUI for creating, viewing, editing, and processing DOT language graph files. Multiple graphs can be open simultaneously, each displayed in its own text window. Features include instant previewing, attribute editing via dialogs, and saving graph settings for future use."],["SEE ALSO","[dot](/man/dot)(1), [neato](/man/neato)(1), [fdp](/man/fdp)(1), [dotty](/man/dotty)(1), [xdot](/man/xdot)(1)"]],"e":["dot","neato","fdp","dotty","xdot"],"x":"gvedit"},"htmlq":{"n":"htmlq","t":"extracts data from HTML using CSS selectors","s":[["TAGLINE","extracts data from HTML using CSS selectors"],["TLDR","**Extract elements by selector**\n\n```cat [page.html] | htmlq [.class-name]```\n\n**Get attribute value**\n\n```htmlq -a href [a.link] < [page.html]```\n\n**Get text content**\n\n```htmlq -t [p] < [page.html]```\n\n**Pretty print HTML**\n\n```htmlq -p [body] < [page.html]```\n\n**Remove nodes before extracting**\n\n```htmlq --remove-nodes [.unwanted] [div.content] < [page.html]```\n\n**From URL via curl**\n\n```curl -s [url] | htmlq [selector]```"],["SYNOPSIS","**htmlq** [_options_] _selector_"],["PARAMETERS","_SELECTOR_\n> CSS selector.\n\n**-a**, **--attribute** _ATTR_\n> Only return this attribute's value from selected elements.\n\n**-t**, **--text**\n> Output only the text content of selected elements.\n\n**-p**, **--pretty**\n> Pretty-print the serialised HTML output.\n\n**-b**, **--base** _URL_\n> Use this URL as the base for relative links.\n\n**-B**, **--detect-base**\n> Detect the base URL from the document's `<base>` tag.\n\n**-f**, **--filename** _FILE_\n> Input file (defaults to stdin).\n\n**-o**, **--output** _FILE_\n> Output file (defaults to stdout).\n\n**-r**, **--remove-nodes** _SELECTOR_\n> Remove matching nodes before output. May be specified multiple times.\n\n**-w**, **--ignore-whitespace**\n> When printing text nodes, ignore whitespace-only nodes.\n\n**-h**, **--help**\n> Display help information.\n\n**-V**, **--version**\n> Show version information."],["DESCRIPTION","**htmlq** extracts data from HTML using CSS selectors. It's like jq for HTML, providing command-line HTML parsing.\n\nThe tool reads HTML from stdin or files and outputs matching elements. It supports extracting text, attributes, and formatted HTML."],["CAVEATS","Does not validate HTML; malformed input may produce unexpected results. CSS selector support covers most common patterns but may not support all CSS4 pseudo-selectors. Input is read entirely into memory, so very large HTML documents may be slow."],["HISTORY","htmlq was created as a command-line HTML querying tool inspired by jq for JSON processing."],["INSTALL","```pacman: sudo pacman -S htmlq```\n\n```apk: sudo apk add htmlq```\n\n```brew: brew install htmlq```\n\n```nix: nix profile install nixpkgs#htmlq```"],["SEE ALSO","[pup](/man/pup)(1), [jq](/man/jq)(1), [xmllint](/man/xmllint)(1), [curl](/man/curl)(1)"]],"e":["pup","jq","xmllint","curl"],"x":"cat [page.html] | htmlq [.class-name]"},"htop":{"n":"htop","t":"interactive process viewer and system monitor","s":[["TAGLINE","interactive process viewer and system monitor"],["TLDR","**Start htop**\n\n```htop```\n\n**Show processes for a specific user**\n\n```htop -u [username]```\n\n**Show only processes containing string**\n\n```htop -F [string]```\n\n**Start with tree view**\n\n```htop -t```\n\n**Sort by memory usage**\n\n```htop -s PERCENT_MEM```\n\n**Monochrome mode**\n\n```htop -C```\n\n**Show help**\n\n```htop -h```"],["SYNOPSIS","**htop** [_options_]"],["DESCRIPTION","**htop** is an interactive, ncurses-based process viewer and system monitor designed as a more capable alternative to the traditional top command. Its full-screen interface displays per-core CPU utilization, memory, and swap usage as color-coded bar meters at the top, followed by a scrollable, sortable table of all running processes. Unlike top, htop supports full mouse interaction and intuitive keyboard shortcuts for everyday tasks.\n\nUsers can filter the process list in real time (F4), search for processes by name (F3), and toggle a tree view (F5) that reveals parent-child process hierarchies. Sending signals to processes is straightforward -- pressing F9 presents a menu of signals (SIGTERM, SIGKILL, SIGHUP, etc.) to send to one or more tagged processes. Process priority (nice value) can be adjusted directly, and the setup screen (F2) allows full customization of which columns and meters are displayed, persisted across sessions."],["PARAMETERS","**-u** _user_, **--user=** _user_\n> Show only processes of specified user.\n\n**-p** _pid_, **--pid=** _pid_\n> Show only specified PIDs.\n\n**-t**, **--tree**\n> Start in tree view mode.\n\n**-s** _column_, **--sort-key=** _column_\n> Sort by specified column.\n\n**-d** _delay_, **--delay=** _delay_\n> Update interval in tenths of seconds.\n\n**-C**, **--no-color**\n> Monochrome mode.\n\n**-F** _string_, **--filter=** _string_\n> Show only processes containing string.\n\n**-H**, **--highlight-changes**\n> Highlight new/changed processes.\n\n**--readonly**\n> Disable process manipulation."],["KEYBOARD CONTROLS","**F1** / **h**: Help screen\n**F2** / **S**: Setup menu\n**F3** / **/**: Search\n**F4** / **\\\\**: Filter\n**F5** / **t**: Tree view toggle\n**F6** / **>**: Sort column menu\n**F9** / **k**: Kill process\n**F10** / **q**: Quit\n**Space**: Tag process\n**U**: Untag all\n**c**: Tag and children"],["CAVEATS","Some features require root privileges (changing priorities, killing other users' processes). High refresh rates may impact system performance. Process information is a snapshot and may change between refreshes."],["HISTORY","**htop** was created by **Hisham Muhammad** in **2004** as a more user-friendly process viewer for Linux. It addressed common complaints about top's interface and lack of interactivity. The project was later forked as **htop-dev** by a community group after maintenance lapsed. htop has become the de facto interactive process viewer on Linux systems."],["INSTALL","```apt: sudo apt install htop```\n\n```dnf: sudo dnf install htop```\n\n```pacman: sudo pacman -S htop```\n\n```apk: sudo apk add htop```\n\n```zypper: sudo zypper install htop```\n\n```brew: brew install htop```\n\n```nix: nix profile install nixpkgs#htop```"],["SEE ALSO","[top](/man/top)(1), [ps](/man/ps)(1), [pstree](/man/pstree)(1), [atop](/man/atop)(1), [btop](/man/btop)(1)"]],"e":["top","ps","pstree","atop","btop"],"x":"htop"},"ip-maddress":{"n":"ip-maddress","t":"manages link-layer multicast addresses","s":[["TAGLINE","manages link-layer multicast addresses"],["TLDR","**List** all multicast addresses\n\n```ip maddress```\n\nList multicast addresses for a **specific device**\n\n```ip maddress show dev [eth0]```\n\n**Join** a multicast group statically\n\n```sudo ip maddress add [33:33:00:00:00:02] dev [eth0]```\n\n**Leave** a multicast group\n\n```sudo ip maddress delete [33:33:00:00:00:02] dev [eth0]```\n\nDisplay **help**\n\n```ip maddress help```"],["SYNOPSIS","**ip maddress** [_command_] [_options_]"],["PARAMETERS","**show** [**dev** _DEVICE_]\n> Display multicast addresses (optionally for specific device)\n\n**add** _ADDRESS_ **dev** _DEVICE_\n> Join a multicast group on specified device\n\n**delete** _ADDRESS_ **dev** _DEVICE_\n> Leave a multicast group on specified device\n\n**help**\n> Display help information"],["DESCRIPTION","**ip maddress** manages link-layer multicast addresses. It displays which multicast groups a device is subscribed to and allows manual addition or removal of static multicast memberships.\n\nMulticast addresses enable one-to-many communication, where a single packet can be received by multiple hosts that have joined the multicast group. This is commonly used for service discovery, streaming, and cluster communication."],["CAVEATS","Adding and deleting multicast addresses requires root privileges. Changes are not persistent across reboots. Application-level multicast (IGMP) is handled separately."],["HISTORY","The ip maddress command is part of iproute2, the modern replacement for the older net-tools package. iproute2 was developed by Alexey Kuznetsov starting in **1999** to provide a unified interface to Linux networking features."],["SEE ALSO","[ip](/man/ip)(8), [ip-link](/man/ip-link)(8), [ip-address](/man/ip-address)(8)"]],"e":["ip","ip-link","ip-address"],"x":"ip maddress"},"jaq":{"n":"jaq","t":"Fast jq clone focused on correctness and speed","s":[["TAGLINE","Fast jq clone focused on correctness and speed"],["TLDR","**Filter JSON** from stdin\n\n```echo '{\"name\": \"world\"}' | jaq '.name'```\n\n**Process a JSON file**\n\n```jaq '.[0]' [path/to/file.json]```\n\n**Filter and format output**\n\n```jaq -r '.[] | .name' [path/to/file.json]```\n\n**Read YAML input**\n\n```jaq --from yaml '.' [path/to/file.yaml]```\n\n**Convert JSON to YAML**\n\n```jaq --to yaml '.' [path/to/file.json]```"],["SYNOPSIS","**jaq** [**-r**] [**--from** _format_] [**--to** _format_] _filter_ [_file_...]"],["PARAMETERS","**-r**, **--raw-output**\n> Print strings without JSON quoting.\n\n**-R**, **--raw-input**\n> Read input as raw strings, one per line.\n\n**-s**, **--slurp**\n> Collect all input values into a single array.\n\n**-c**, **--compact-output**\n> Emit JSON on a single line.\n\n**-S**, **--sort-keys**\n> Sort object keys in the output.\n\n**--indent** _N_\n> Use _N_ spaces per indent level (default **2**).\n\n**--tab**\n> Use a tab for each indent level.\n\n**-C**, **--color-output**\n> Force colored output even when stdout is not a TTY.\n\n**-M**, **--monochrome-output**\n> Disable colored output.\n\n**-i**, **--in-place**\n> Rewrite each input file with the result of the filter.\n\n**--from** _FORMAT_\n> Input format: **json**, **yaml**, **cbor**, **toml**, **xml**, **csv**, **tsv**, **raw**, **raw0**, **xjon**.\n\n**--to** _FORMAT_\n> Output format (same set as **--from**).\n\n**-n**, **--null-input**\n> Use **null** as the input value."],["DESCRIPTION","**jaq** is a clone of the JSON processing tool **jq**, focused on correctness, speed, and simplicity. It is available as both a command-line program (a near drop-in replacement for jq) and as a Rust library. Beyond standard JSON, jaq supports **YAML**, **CBOR**, **TOML**, and **XML** formats.\n\njaq aims to provide more correct and predictable behavior than jq in edge cases, while maintaining compatibility for common usage. Its core has been security-audited by **Radically Open Security**."],["CAVEATS","While highly compatible with jq, some edge-case behaviors differ intentionally for correctness. Not all jq built-in functions are implemented. Custom jq modules are not supported."],["HISTORY","**jaq** was created by **Michael Färber** (01mf02) and is written in **Rust**. It was motivated by concerns about jq's start-up time and edge-case correctness, and has been benchmarked to be faster than jq on most operations."],["INSTALL","```pacman: sudo pacman -S jaq```\n\n```zypper: sudo zypper install jaq```\n\n```brew: brew install jaq```\n\n```nix: nix profile install nixpkgs#jaq```"],["SEE ALSO","[jq](/man/jq)(1), [yq](/man/yq)(1)"]],"e":["jq","yq"],"x":"echo '{\"name\": \"world\"}' | jaq '.name'"},"kitex":{"n":"kitex","t":"generates Go code from Thrift or Protobuf IDL files","s":[["TAGLINE","generates Go code from Thrift or Protobuf IDL files"],["TLDR","**Generate service code**\n\n```kitex -module [module] [idl_file.thrift]```\n\n**Generate client code**\n\n```kitex -module [module] -client [idl_file.thrift]```\n\n**Specify service name**\n\n```kitex -module [module] -service [svc] [idl.thrift]```\n\n**Use Protobuf**\n\n```kitex -module [module] [idl.proto]```\n\n**Specify IDL include path**\n\n```kitex -module [module] -I [path/to/includes] [idl.thrift]```\n\n**Reuse existing kitex_gen directory**\n\n```kitex -module [module] -service [svc] -use [import/path/to/kitex_gen] [idl.thrift]```"],["SYNOPSIS","**kitex** [_options_] _idl_file_"],["PARAMETERS","_IDL_FILE_\n> Thrift or Protobuf IDL file.\n\n**-module** _NAME_\n> Go module name.\n\n**-service** _NAME_\n> Service name.\n\n**-I** _PATH_\n> Add search path for IDL includes. Can be specified multiple times.\n\n**-use** _PATH_\n> Skip generating kitex_gen and use the specified import path instead.\n\n**-v**, **-verbose**\n> Output more logs during generation.\n\n**-type** _TYPE_\n> Specify IDL type when it cannot be detected from the file extension.\n\n**-combine-service**\n> Combine all services in the IDL into a single service (Thrift only).\n\n**-gen-path** _DIR_\n> Specify output directory for generated code (default: kitex_gen).\n\n**-thrift** _VALUE_\n> Pass an argument to the thriftgo compiler.\n\n**-protobuf** _VALUE_\n> Pass an argument to the protoc compiler."],["DESCRIPTION","**kitex** is a code generation tool for the Kitex high-performance RPC framework, part of ByteDance's open-source CloudWeGo ecosystem. It reads Thrift or Protocol Buffers interface definition language (IDL) files and generates Go scaffolding code for both servers and clients, including service interfaces, request/response types, serialization logic, and a runnable project skeleton.\n\nThe generated code is designed for building Go microservices with features like connection pooling, load balancing, and circuit breaking built into the Kitex framework. By default, kitex produces both server and client code, but the `-client` flag restricts output to client stubs only. Custom code templates can be provided via the `-template` option to control the structure and style of the generated project."],["CAVEATS","Part of CloudWeGo. Go-specific. Thrift or Protobuf IDL required."],["HISTORY","kitex is the code generator for **Kitex**, a high-performance RPC framework created by **ByteDance**."],["INSTALL","```brew: brew install kitex```\n\n```nix: nix profile install nixpkgs#kitex```"],["SEE ALSO","[thrift](/man/thrift)(1), [protoc](/man/protoc)(1), [go](/man/go)(1)"]],"e":["thrift","protoc","go"],"x":"kitex -module [module] [idl_file.thrift]"},"knife":{"n":"knife","t":"command-line tool for Chef infrastructure automation","s":[["TAGLINE","command-line tool for Chef infrastructure automation"],["TLDR","**Bootstrap node**\n\n```knife bootstrap [host] -x [user] -P [password]```\n\n**List nodes**\n\n```knife node list```\n\n**Show node info**\n\n```knife node show [node_name]```\n\n**Upload cookbook**\n\n```knife cookbook upload [cookbook]```\n\n**Search nodes**\n\n```knife search node \"role:[webserver]\"```\n\n**SSH to nodes**\n\n```knife ssh \"role:[webserver]\" \"[command]\"```"],["SYNOPSIS","**knife** _command_ [_subcommand_] [_options_]"],["PARAMETERS","**bootstrap** _HOST_\n> Bootstrap Chef on node.\n\n**node** _CMD_\n> Node management.\n\n**cookbook** _CMD_\n> Cookbook operations.\n\n**search** _TYPE_ _QUERY_\n> Search Chef server.\n\n**ssh** _QUERY_ _CMD_\n> Run command via SSH.\n\n**-x** _USER_\n> SSH username.\n\n**--help**\n> Display help information."],["DESCRIPTION","**knife** is the command-line tool for Chef infrastructure automation. It manages nodes, cookbooks, and Chef server.\n\nThe tool bootstraps nodes for configuration management. It uploads recipes and queries infrastructure."],["CAVEATS","Requires Chef Workstation. Chef server access needed. Ruby-based."],["HISTORY","knife is part of **Chef**, the infrastructure automation platform created by **Opscode** (now Progress Chef)."],["SEE ALSO","[chef](/man/chef)(1), [berkshelf](/man/berks)(1)"]],"e":["chef","berkshelf"],"x":"knife bootstrap [host] -x [user] -P [password]"},"koboldcpp":{"n":"koboldcpp","t":"Local AI text generation server and inference engine","s":[["TAGLINE","Local AI text generation server and inference engine"],["TLDR","**Launch** with a GGUF model file\n\n```koboldcpp --model [path/to/model.gguf]```\n\n**Launch** with GPU acceleration using CUDA\n\n```koboldcpp --model [path/to/model.gguf] --usecuda --gpulayers [35]```\n\n**Launch** with Vulkan GPU support on a custom port\n\n```koboldcpp --model [path/to/model.gguf] --usevulkan --gpulayers [35] --port [8080]```\n\n**Run a single prompt** without starting a server\n\n```koboldcpp --model [path/to/model.gguf] --prompt \"[What is the meaning of life?]\"```\n\n**Launch in CLI interactive mode** without the web UI\n\n```koboldcpp --model [path/to/model.gguf] --cli```\n\n**Load a saved configuration** file\n\n```koboldcpp --config [path/to/config.kcpps]```"],["SYNOPSIS","**koboldcpp** [_options_] [**--model** _model_path_]"],["PARAMETERS","**--model** _path_\n> Specify the GGUF/GGML model file to load\n\n**--config** _file_\n> Load a .kcpps configuration file\n\n**--usecuda**\n> Enable NVIDIA CUDA GPU acceleration\n\n**--usevulkan**\n> Enable Vulkan GPU acceleration (AMD/NVIDIA)\n\n**--gpulayers** _n_\n> Number of model layers to offload to GPU\n\n**--threads** _n_\n> Set CPU thread count for inference\n\n**--contextsize** _n_\n> Set maximum context length in tokens\n\n**--port** _n_\n> Change server port (default: 5001)\n\n**--host** _addr_\n> Bind to a specific IP address\n\n**--multiuser** _n_\n> Enable multiuser mode with _n_ concurrent slots\n\n**--password** _key_\n> Require API authentication with the given key\n\n**--cli**\n> Launch interactive command-line interface without starting a server\n\n**--prompt** _text_\n> Run a single prompt, print output, and exit\n\n**--benchmark**\n> Run performance benchmarking mode\n\n**--flashattention**\n> Enable flash attention for improved performance\n\n**--smartcontext**\n> Enable smart context handling to reduce reprocessing\n\n**--usemmap**\n> Enable memory-mapped file I/O for model loading\n\n**--usemlock**\n> Force model to remain in RAM (prevent swapping)\n\n**--ssl**\n> Enable SSL for HTTPS connections\n\n**--remotetunnel**\n> Enable remote tunnel access for sharing the server\n\n**--sdmodel** _path_\n> Load a Stable Diffusion model for image generation\n\n**--noavx2**\n> Compatibility mode for older CPUs without AVX2\n\n**--showgui**\n> Show the GUI launcher even when command-line flags are used\n\n**--skiplauncher**\n> Skip the GUI launcher and start the server directly\n\n**--quiet**\n> Suppress console output\n\n**--whispermodel** _path_\n> Load a Whisper model for speech-to-text\n\n**--ttsmodel** _path_\n> Load a text-to-speech model\n\n**--mmproj** _path_\n> Load a multimodal projector file for vision capabilities\n\n**--lora** _path_\n> Apply a LoRA adapter to the base model\n\n**--help**\n> Display all available commands"],["DESCRIPTION","**koboldcpp** is a self-contained AI text generation server that runs large language models locally. Built on top of **llama.cpp**, it provides a bundled web UI (KoboldAI Lite) and supports all GGML and GGUF model formats. It requires no external dependencies and runs as a single executable.\n\nThe server exposes an API compatible with KoboldAI and OpenAI formats, making it usable with a wide range of frontends and applications. It supports CPU inference as well as GPU acceleration through **CUDA** (NVIDIA), **Vulkan** (AMD/NVIDIA), and **Metal** (Apple Silicon).\n\nBeyond text generation, koboldcpp supports **image generation** (Stable Diffusion), **speech recognition** (Whisper), and **text-to-speech**, all within the same executable. The bundled web UI offers multiple interaction modes including chat, instruct, adventure, and story writing."],["CONFIGURATION","When launched without arguments, koboldcpp opens a **GUI launcher** for interactive configuration. Settings can be saved to and loaded from **.kcpps** configuration files. Command-line flags override GUI settings when both are used.\n\nKey configuration considerations include **GPU layer offloading** (more layers on GPU means faster inference but requires more VRAM), **context size** (larger contexts use more memory), and **thread count** (typically set to the number of physical CPU cores)."],["CAVEATS","Model files can be very large (several GB to over 100 GB) and require significant RAM or VRAM. GPU acceleration requires appropriate drivers and hardware support. Performance varies significantly based on model size, quantization level, and available hardware. The Vulkan backend is more broadly compatible but generally slower than CUDA on NVIDIA hardware. Flash attention requires compatible model architectures."],["HISTORY","KoboldCpp was created by a developer known as **LostRuins** (alias **Concedo**) and first released on **March 16, 2023** as a fork of **llama.cpp** combined with the KoboldAI interface. It was designed to provide a simple, self-contained way to run large language models locally without complex setup. The project grew rapidly alongside the open-source LLM movement, continuously adding features like multi-modal support, GPU backends, and image generation capabilities. It is licensed under **AGPL-3.0**."],["INSTALL","```nix: nix profile install nixpkgs#koboldcpp```"],["SEE ALSO","[ollama](/man/ollama)(1)"]],"e":["ollama"],"x":"koboldcpp --model [path/to/model.gguf]"},"kscreen-console":{"n":"kscreen-console","t":"command-line tool for querying KScreen display configuration","s":[["TAGLINE","command-line tool for querying KScreen display configuration"],["TLDR","Show **output information**\n\n```kscreen-console outputs```\n\nShow **configuration file paths**\n\n```kscreen-console config```\n\nOutput configuration as **JSON**\n\n```kscreen-console json```\n\n**Monitor** for display changes\n\n```kscreen-console monitor```\n\nGenerate **bug report** data\n\n```kscreen-console bug```\n\nDisplay **help**\n\n```kscreen-console --help```"],["SYNOPSIS","**kscreen-console** [_command_] [_options_]"],["DESCRIPTION","**kscreen-console** is a command-line tool for querying KScreen's display configuration on KDE Plasma. It provides diagnostic information about connected outputs, configuration files, and display changes. It communicates via DBus with the running KScreen service."],["PARAMETERS","**outputs**\n> Show KScreen output information and configuration.\n\n**config**\n> Show paths to KScreen configuration files.\n\n**json**\n> Show current configuration in JSON format.\n\n**monitor**\n> Monitor for display configuration changes.\n\n**bug**\n> Generate output suitable for bug reports.\n\n**-v**, **--version**\n> Show version information.\n\n**--help-all**\n> Display help including Qt-specific options.\n\n**-h**, **--help**\n> Display help information."],["CAVEATS","Requires an active KDE Plasma session with KScreen enabled. Communicates via DBus with the running KScreen service."],["INSTALL","```apt: sudo apt install kscreen```\n\n```dnf: sudo dnf install kscreen```\n\n```pacman: sudo pacman -S kscreen```\n\n```apk: sudo apk add kscreen```"],["SEE ALSO","[kscreen-doctor](/man/kscreen-doctor)(1), [xrandr](/man/xrandr)(1)"]],"e":["kscreen-doctor","xrandr"],"x":"kscreen-console outputs"},"lambo":{"n":"lambo","t":"automates Laravel project creation","s":[["TAGLINE","automates Laravel project creation"],["TLDR","**Create new Laravel project**\n\n```lambo new [project-name]```\n\n**Create with options**\n\n```lambo new [project-name] --editor=[code]```\n\n**Create with database**\n\n```lambo new [project-name] --create-db```\n\n**Create with authentication**\n\n```lambo new [project-name] --auth```\n\n**Skip common prompts**\n\n```lambo new [project-name] --quiet```"],["SYNOPSIS","**lambo new** [_options_] _name_"],["PARAMETERS","_NAME_\n> Project name.\n\n**--editor** _EDITOR_\n> Open in editor.\n\n**--create-db**\n> Create database.\n\n**--auth**\n> Install authentication.\n\n**--quiet**\n> Skip prompts.\n\n**--browser** _BROWSER_\n> Open in browser.\n\n**--help**\n> Display help information."],["DESCRIPTION","**lambo** automates Laravel project creation. It runs multiple setup steps with a single command.\n\nThe tool handles git init, composer install, and common configurations. It streamlines new project setup."],["CAVEATS","Requires Laravel installer. PHP and Composer needed. macOS-focused."],["HISTORY","lambo was created by **Matt Stauffer** at Tighten to speed up Laravel project initialization."],["SEE ALSO","[laravel](/man/laravel)(1), [composer](/man/composer)(1), [php](/man/php)(1)"]],"e":["laravel","composer","php"],"x":"lambo new [project-name]"},"lemurs":{"n":"lemurs","t":"Customizable TUI display and login manager","s":[["TAGLINE","Customizable TUI display and login manager"],["TLDR","**Start the display manager**\n\n```lemurs```"],["SYNOPSIS","**lemurs** [_options_]"],["DESCRIPTION","**lemurs** is a terminal-based display and login manager for Linux and BSD systems. It uses PAM (Pluggable Authentication Modules) for authentication and can serve as the front-end for TTY, X11, or Wayland sessions. It works both with and without systemd.\n\nSession environments are configured by placing scripts in **/etc/lemurs/wms** for X11 xinitrc scripts and **/etc/lemurs/wayland** for Wayland compositor scripts."],["CONFIGURATION","**/etc/lemurs/config.toml**\n> Main configuration file for customizing the login screen appearance and behavior.\n\n**/etc/lemurs/wms/**\n> Directory for X11 session scripts.\n\n**/etc/lemurs/wayland/**\n> Directory for Wayland session scripts."],["CAVEATS","Requires PAM for authentication. Session scripts must be executable. Some desktop environments may require additional configuration to work with a TUI-based display manager."],["HISTORY","**lemurs** was created by **coastalwhite** and is written in **Rust**. It is available in the Arch Linux extra repository."],["INSTALL","```pacman: sudo pacman -S lemurs```\n\n```nix: nix profile install nixpkgs#lemurs```"],["SEE ALSO","[ly](/man/ly)(1), [gdm](/man/gdm)(1), [startx](/man/startx)(1)"]],"e":["ly","gdm","startx"],"x":"lemurs"},"lnko":{"n":"lnko","t":"Simple stow-like dotfile linker","s":[["TAGLINE","Simple stow-like dotfile linker"],["TLDR","**Link packages to the home directory**\n\n```lnko link [package1] [package2]```\n\n**Link packages from a specific source to a target directory**\n\n```lnko link -d [~/dotfiles] -t [~] [package_name]```\n\n**Unlink a package**\n\n```lnko unlink [package_name]```\n\n**Preview changes without creating symlinks**\n\n```lnko link -n [package_name]```\n\n**Show status of all packages**\n\n```lnko status```\n\n**Remove orphaned symlinks**\n\n```lnko clean```\n\n**Force-overwrite conflicting files**\n\n```lnko link -f [package_name]```"],["SYNOPSIS","**lnko** _command_ [_options_] [_package_ ...]"],["PARAMETERS","**link**\n> Create relative symlinks from source packages to the target directory.\n\n**unlink**\n> Remove symlinks for specified packages.\n\n**status**\n> Display the current state of all packages and their symlinks.\n\n**clean**\n> Remove orphaned symlinks pointing to non-existent targets.\n\n**-d**, **--dir** _dir_\n> Specify source directory containing packages (default: current directory).\n\n**-t**, **--target** _dir_\n> Specify target directory for symlinks (default: $HOME).\n\n**-n**, **--dry-run**\n> Preview changes without creating symlinks.\n\n**-v**, **--verbose**\n> Output detailed debugging information.\n\n**-b**, **--backup**\n> Auto-resolve conflicts by backing up existing files to .lnko-backup/.\n\n**-s**, **--skip**\n> Auto-resolve conflicts by skipping conflicting files.\n\n**-f**, **--force**\n> Auto-resolve conflicts by force-overwriting.\n\n**--ignore** _pattern_\n> Exclude files matching pattern (repeatable).\n\n**--no-folding**\n> Prevent directory folding into symlinks."],["DESCRIPTION","**lnko** is a simple stow-like dotfile linker that creates relative symlinks from a source directory containing packages to a target directory. Each package is a directory whose contents mirror the target structure.\n\nIt supports tree folding, ignore patterns, dry-run mode, and multiple conflict resolution strategies. When conflicts are detected, lnko provides an interactive prompt with backup, skip, overwrite, and diff options unless an auto-resolve flag is given. Compatible with existing GNU Stow symlinks."],["CAVEATS","Requires Lua and LuaFileSystem to be installed."],["HISTORY","**lnko** was created by **luanvil** and is written in **Lua**."],["SEE ALSO","[stow](/man/stow)(8), [ln](/man/ln)(1)"]],"e":["stow","ln"],"x":"lnko link [package1] [package2]"},"mc-scan":{"n":"mc-scan","t":"Static scanner for AI agent tools and capabilities (part of MakerChecker)","s":[["TAGLINE","Static scanner for AI agent tools and capabilities (part of MakerChecker)"],["TLDR","**Scan** a directory or tools file for risky agent capabilities\n\n```mc-scan .```\n\n**Output** machine-readable JSON\n\n```mc-scan . --json```\n\n**Generate** a shareable Markdown report\n\n```mc-scan . --markdown```\n\n**Write** a deny-by-default policy file\n\n```mc-scan init .```\n\n**Generate** governance scaffold code with `--fix`\n\n```mc-scan . --fix```\n\n**Fail** CI on catastrophic findings\n\n```mc-scan . --fail-on CATASTROPHIC```\n\n**Run** an optional LLM deep scan (private with Ollama)\n\n```mc-scan . --llm --llm-provider ollama```"],["SYNOPSIS","**mc-scan** [_options_] _path_  \n**mc-scan** init [_path_] [_options_]"],["DESCRIPTION","`mc-scan` (also available as `npx @makerchecker/scan`) is a zero-dependency, fully offline static analysis tool that inspects an AI agent's tools, function schemas, and source code to discover what actions the agent is capable of performing.\n\nIt parses OpenAI-style tool definitions, MCP `tools/list` output, LangChain decorators, Vercel AI SDK tools, and similar declarations in JavaScript, TypeScript, and Python. It classifies each capability against a database of real AI agent incidents and reports risk tiers:\n\n- **CATASTROPHIC**: irreversible harm (delete data, exec code, move funds, etc.)\n- **HIGH / MEDIUM**: significant but scoped impact\n\nIt also detects dangerous **composition risks** (e.g., read-untrusted + shell-exec = RCE channel).\n\nThe tool can emit JSON, Markdown reports, enforce policy files, and with `--fix` emit ready-to-use governance wrappers (deny-by-default + separation of duties) for the `@makerchecker/embedded` runtime.\n\nIt is part of the larger MakerChecker security gateway project but the scanner can be used standalone in development, audits, and CI."],["PARAMETERS","**_path_**  \n> Directory or JSON file containing agent tool definitions (defaults to current directory).\n\n**--json**  \n> Emit machine-readable JSON instead of terminal report.\n\n**--markdown**, **--md**  \n> Emit a shareable Markdown results card.\n\n**--policy** _file_, **-p** _file_  \n> Load an existing `mc-policy.json` to evaluate against.\n\n**--fail-on** _SEVERITY_  \n> Exit non-zero if any finding meets or exceeds the severity (CATASTROPHIC, HIGH, etc.). Useful for CI.\n\n**init** [_path_]  \n> Generate a deny-by-default `mc-policy.json` (or custom path with `--policy`).\n\n**--fix**  \n> After scanning, write a governance scaffold (`mc-governance.mjs` or via `--out`) using the embedded primitives.\n\n**--out** _file_, **-o** _file_  \n> Output path for files written by `--fix`.\n\n**--llm**  \n> Enable optional LLM deep scan (re-ranks tools or scans source for latent capabilities). Never lowers static severity.\n\n**--llm-provider** _id_  \n> anthropic (default), openai, openrouter, together, groq, ollama, or openai-compatible.\n\n**--llm-model** _id_  \n> Override model for the chosen provider.\n\n**--llm-base-url** _url_  \n> Custom endpoint (for Ollama or gateways).\n\n**--llm-key** _key_  \n> API key (prefer provider environment variables).\n\n**--llm-no-key**  \n> Explicitly run without a key where supported.\n\n**-h**, **--help**  \n> Show usage.\n\n**--version**, **-v**  \n> Print version."],["CAVEATS","The default scan is completely local and deterministic. `--llm` mode is opt-in and non-deterministic; it does not affect `--fail-on` exit codes.\n\n\"scan\" and \"mc-scan\" binaries are provided by `@makerchecker/scan`. The generic name \"scan\" may conflict with other tools on PATH; prefer `mc-scan` or the npx form.\n\nLLM scans that use cloud providers send tool names/descriptions (and optionally code snippets) to the provider. Use `--llm-provider ollama` for fully private operation.\n\nThe tool only analyzes declared interfaces and some source; runtime behavior or heavily obfuscated code may not be fully covered."],["HISTORY","MakerChecker and its `mc-scan` component were created to provide practical, auditable guardrails for agentic AI systems after a series of high-profile agent failures. The scanner emphasizes offline operation and integration with both development workflows and production gateways."],["SEE ALSO","[deptrust](/man/deptrust)(1), [semgrep](/man/semgrep)(1)"],["RESOURCES","```[Source code](https://github.com/makerchecker/MakerChecker)```\n\n```[Homepage](https://makerchecker.ai)```\n\n```[Documentation](https://makerchecker.ai/docs)```"]],"e":["deptrust","semgrep"],"x":"mc-scan ."},"mm2gv":{"n":"mm2gv","t":"converts Matrix Market format to Graphviz","s":[["TAGLINE","converts Matrix Market format to Graphviz"],["TLDR","**Convert matrix market to graph**\n\n```mm2gv [matrix.mtx] -o [graph.gv]```\n\n**Output DOT format**\n\n```mm2gv -T dot [matrix.mtx]```\n\n**Undirected graph**\n\n```mm2gv -u [matrix.mtx]```"],["SYNOPSIS","**mm2gv** [_options_] _file_"],["PARAMETERS","_FILE_\n> Matrix Market file.\n\n**-o** _FILE_\n> Output file.\n\n**-T** _FORMAT_\n> Output format.\n\n**-u**\n> Undirected graph.\n\n**--help**\n> Display help information."],["DESCRIPTION","**mm2gv** converts Matrix Market format to Graphviz. Matrix Market is a sparse matrix format.\n\nThe tool is part of Graphviz. It enables visualization of sparse matrices as graphs."],["CAVEATS","Part of Graphviz. Matrix Market format only. For sparse matrices."],["HISTORY","mm2gv is part of **Graphviz** for converting Matrix Market sparse matrices to graph format."],["INSTALL","```apt: sudo apt install graphviz```\n\n```dnf: sudo dnf install graphviz```\n\n```pacman: sudo pacman -S graphviz```\n\n```apk: sudo apk add graphviz```\n\n```zypper: sudo zypper install graphviz```\n\n```brew: brew install graphviz```\n\n```nix: nix profile install nixpkgs#graphviz```"],["SEE ALSO","[dot](/man/dot)(1), [gvpr](/man/gvpr)(1), [neato](/man/neato)(1)"]],"e":["dot","gvpr","neato"],"x":"mm2gv [matrix.mtx] -o [graph.gv]"},"module":{"n":"module","t":"provides a user interface to the Environment Modules package, enabling dynamic","s":[["TAGLINE","provides a user interface to the Environment Modules package, enabling dynamic"],["TLDR","Display **available modules**\n\n```module avail```\n\n**Search** for a module by name\n\n```module avail [module_name]```\n\n**Load** a module\n\n```module load [module_name]```\n\nDisplay **loaded modules**\n\n```module list```\n\n**Unload** a specific module\n\n```module unload [module_name]```\n\n**Unload all** loaded modules\n\n```module purge```\n\n**Save** the current set of loaded modules\n\n```module save [collection_name]```\n\n**Restore** a saved collection\n\n```module restore [collection_name]```"],["SYNOPSIS","**module** [_switches_] _sub-command_ [_sub-command-args_]"],["PARAMETERS","**avail [_pattern_]**\n> List available modulefiles matching optional pattern\n\n**load _modulefile_...**\n> Load modules into the current environment\n\n**unload _modulefile_...**\n> Remove modules from the environment\n\n**list [_pattern_]**\n> List currently loaded modules\n\n**purge [-f]**\n> Unload all loaded modules\n\n**reload [-f]**\n> Unload then reload all modules\n\n**switch _old_ _new_**\n> Replace one module with another\n\n**save [_collection_]**\n> Save current environment state to collection\n\n**restore [_collection_]**\n> Restore saved environment collection\n\n**savelist**\n> List saved collections\n\n**display _modulefile_**\n> Show modulefile contents and effects\n\n**whatis _string_**\n> Search modulefile descriptions\n\n**use _path_...**\n> Add directories to modulefile search path\n\n**--json, -j**\n> Output in JSON format\n\n**--long, -l**\n> Long format output\n\n**--terse, -t**\n> Short format output\n\n**-v, --verbose**\n> Enable verbose messages"],["DESCRIPTION","**module** provides a user interface to the Environment Modules package, enabling dynamic modification of the shell environment through modulefiles. Each modulefile contains configuration for applications, typically modifying PATH, MANPATH, LD_LIBRARY_PATH, and other environment variables.\n\nThe system allows users to load and unload software configurations without manual environment variable management. Modulefiles are interpreted as Tcl scripts and can set variables, create aliases, and define dependencies.\n\nCollections allow saving and restoring sets of loaded modules, useful for switching between project configurations or workflows."],["CAVEATS","Modulefiles must begin with the magic cookie #%Module. Loading conflicting modules may cause unexpected behavior unless dependencies are properly defined. The module command modifies the current shell environment; changes are lost when the shell exits unless saved to shell initialization files."],["HISTORY","The **Environment Modules** system originated at DOE computing facilities in the early 1990s. **Lmod**, a Lua-based implementation, was developed at the Texas Advanced Computing Center (TACC) and is widely used on HPC systems. Both provide compatible user interfaces for managing software environments."]],"e":[],"x":"module avail"},"nix-flake-info":{"n":"nix-flake-info","t":"shows flake metadata","s":[["TAGLINE","shows flake metadata"],["TLDR","**Show flake info**\n\n```nix flake info```\n\n**Show remote flake info**\n\n```nix flake info [github:NixOS/nixpkgs]```\n\n**Show info as JSON**\n\n```nix flake info --json```"],["SYNOPSIS","**nix** **flake** **info** [_options_] [_flake-url_]"],["PARAMETERS","_FLAKE-URL_\n> Flake reference URL.\n\n**--json**\n> Output as JSON.\n\n**--help**\n> Display help information."],["DESCRIPTION","**nix flake info** shows flake metadata. Displays inputs and description.\n\nThe command shows flake URL. Lists locked inputs and revisions."],["CAVEATS","Renamed to metadata in newer versions. Flakes must be enabled. Network for remote."],["HISTORY","nix flake info was part of early **flakes** implementation, now superseded by metadata."],["INSTALL","```apt: sudo apt install nix-bin```\n\n```dnf: sudo dnf install nix```\n\n```pacman: sudo pacman -S nix```\n\n```apk: sudo apk add nix```\n\n```zypper: sudo zypper install nix```\n\n```nix: nix profile install nixpkgs#nix```"],["SEE ALSO","[nix-flake](/man/nix-flake)(1), [nix-flake-show](/man/nix-flake-show)(1), [nix](/man/nix)(1)"]],"e":["nix-flake","nix-flake-show","nix"],"x":"nix flake info"},"nix3-shell":{"n":"nix3-shell","t":"Start a shell providing specific packages","s":[["TAGLINE","Start a shell providing specific packages"],["TLDR","**Enter a shell with a package available**\n\n```nix shell nixpkgs#[hello]```\n\n**Enter a shell with multiple packages**\n\n```nix shell nixpkgs#[git] nixpkgs#[curl]```\n\n**Run a single command with a package and exit**\n\n```nix shell nixpkgs#[jq] --command jq --version```\n\n**Enter a shell with a package from a specific flake**\n\n```nix shell github:[owner/repo]#[package]```\n\n**Enter a shell with a package** from a specific nixpkgs revision\n\n```nix shell github:NixOS/nixpkgs/[nixos-24.05]#[hello]```"],["SYNOPSIS","**nix** **shell** [_options_] _installables_"],["PARAMETERS","_INSTALLABLES_\n> Flake references specifying packages to make available (e.g., nixpkgs#hello).\n\n**--command**, **-c** _CMD_ [_ARGS_]\n> Run a command in the shell environment instead of starting an interactive shell.\n\n**--ignore-environment**, **-i**\n> Clear the entire environment (except those specified with --keep).\n\n**--keep** _NAME_\n> Keep the specified environment variable when using --ignore-environment.\n\n**--keep-going**\n> Continue building other derivations if one fails.\n\n**--impure**\n> Allow access to mutable paths and environment variables.\n\n**--override-input** _INPUT_ _FLAKEREF_\n> Override a specific flake input. Implies --no-write-lock-file.\n\n**--help**\n> Display help information."],["DESCRIPTION","**nix shell** starts a shell environment with the specified packages available in `$PATH`. It is the flake-based replacement for `nix-shell -p` and does not require a shell.nix or default.nix file.\n\nPackages are specified as flake references (e.g., `nixpkgs#jq`). The packages are built or fetched as needed and made available only in the spawned shell session. Nothing is permanently installed."],["CAVEATS","Requires the experimental `nix-command` and `flakes` features to be enabled. Packages are not permanently installed; they are only available in the spawned shell session."],["SEE ALSO","[nix](/man/nix)(1), [nix-shell](/man/nix-shell)(1), [nix3-develop](/man/nix3-develop)(1), [nix3-run](/man/nix3-run)(1), [nix3-build](/man/nix3-build)(1)"]],"e":["nix","nix-shell","nix3-develop","nix3-run","nix3-build"],"x":"nix shell nixpkgs#[hello]"},"numastat":{"n":"numastat","t":"displays memory statistics for NUMA systems","s":[["TAGLINE","displays memory statistics for NUMA systems"],["TLDR","**Show NUMA statistics**\n\n```numastat```\n\n**Show per-process NUMA memory**\n\n```numastat -p [pid]```\n\n**Show per-node memory info**\n\n```numastat -m```\n\n**Show compact output**\n\n```numastat -c```\n\n**Show statistics for command**\n\n```numastat -p $(pgrep [process_name])```"],["SYNOPSIS","**numastat** [_options_] [_pattern_|_pid_...]"],["PARAMETERS","**-p** _pid_\n> Show per-process stats.\n\n**-m**\n> Show memory info.\n\n**-c**\n> Compact output.\n\n**-n**\n> Show original format.\n\n**-s** _node_\n> Sort by node.\n\n**-z**\n> Skip zero values."],["DESCRIPTION","**numastat** displays memory statistics for NUMA (Non-Uniform Memory Access) systems. It shows per-node memory allocation, hits, misses, and process memory distribution.\n\nUnderstanding NUMA statistics helps optimize application performance on multi-socket systems."],["STATISTICS EXPLAINED","```\nnuma_hit   - Allocation on intended node\nnuma_miss  - Allocation on different node\nnuma_foreign - Other node allocations here\ninterleave_hit - Interleave policy success\nlocal_node - Local allocations\nother_node - Remote allocations\n```"],["EXAMPLE OUTPUT","```\n                 node0       node1\nnuma_hit        1234567      987654\nnuma_miss           100         200\nlocal_node      1234467      987454\nother_node          100         200\n```"],["CAVEATS","Only relevant on NUMA systems. Statistics cumulative since boot. Requires /sys/devices/system/node/."],["HISTORY","numastat is part of **numactl**, providing NUMA statistics similar to the original SGI IRIX numastat command."],["INSTALL","```apt: sudo apt install numactl```\n\n```dnf: sudo dnf install numactl```\n\n```pacman: sudo pacman -S numactl```\n\n```apk: sudo apk add numactl-tools```\n\n```zypper: sudo zypper install numactl```\n\n```brew: brew install numactl```\n\n```nix: nix profile install nixpkgs#numactl```"],["SEE ALSO","[numactl](/man/numactl)(8), [migratepages](/man/migratepages)(8)"]],"e":["numactl","migratepages"],"x":"numastat"},"nvidia_oc":{"n":"nvidia_oc","t":"Overclock NVIDIA GPUs on Linux (X11 and Wayland)","s":[["TAGLINE","Overclock NVIDIA GPUs on Linux (X11 and Wayland)"],["TLDR","**Apply** power limit and clock offsets\n\n```nvidia_oc set --index [0] --power-limit [200000] --freq-offset [160] --mem-offset [850] --min-clock [0] --max-clock [2000]```\n\n**Show help**\n\n```nvidia_oc --help```"],["SYNOPSIS","**nvidia_oc** **set** [*options*]"],["DESCRIPTION","**nvidia_oc** is a Rust command-line tool to overclock NVIDIA GPUs on Linux using NVML. It targets both X11 and Wayland, where many older overclocking tools only support X11.\n\nTypical workflow: download a release binary, run **set** with GPU index and power/clock parameters, optionally install a systemd unit so settings re-apply at boot."],["PARAMETERS","**set**\n\n> Apply overclock / power settings.\n\n**--index** *n*\n\n> GPU index (0 for the first device).\n\n**--power-limit** *milliwatts*\n\n> Power limit in milliwatts (example: **200000** for 200 W; confirm units for your build).\n\n**--freq-offset** *mhz*\n\n> GPU core frequency offset.\n\n**--mem-offset** *mhz*\n\n> Memory frequency offset.\n\n**--min-clock** / **--max-clock** *mhz*\n\n> Clock range bounds.\n\nSee **nvidia_oc --help** and **nvidia_oc set --help** for the full flag set of your binary."],["CAVEATS","Requires NVIDIA drivers and permissions to change device clocks (often root). Incorrect settings can crash the GPU/driver or reduce stability; raise values carefully. Manufacturer and OEM power limits may clamp your settings. Overclocking can void warranties and increase heat/power draw. Wayland/X11 session details still depend on driver support."],["INSTALL","```nix: nix profile install nixpkgs#nvidia_oc```"],["SEE ALSO","[nvidia-smi](/man/nvidia-smi)(1), [nvidia-settings](/man/nvidia-settings)(1)"],["RESOURCES","```[Source code](https://github.com/Dreaming-Codes/nvidia_oc)```"]],"e":["nvidia-smi","nvidia-settings"],"x":"nvidia_oc set --index [0] --power-limit [200000] --freq-offset [160] --mem-offset [850] --min-clock [0] --max-clock [2000]"},"nyxt":{"n":"nyxt","t":"keyboard-oriented web browser","s":[["TAGLINE","keyboard-oriented web browser"],["TLDR","**Start browser**\n\n```nyxt```\n\n**Open URL**\n\n```nyxt [https://example.com]```\n\n**Start with config**\n\n```nyxt --config [config.lisp]```\n\n**Start in headless mode**\n\n```nyxt --headless```"],["SYNOPSIS","**nyxt** [_options_] [_url_]"],["PARAMETERS","_URL_\n> URL to open.\n\n**--config** _FILE_\n> Use configuration file.\n\n**--headless**\n> Run without GUI.\n\n**--script** _FILE_\n> Execute Lisp script.\n\n**--help**\n> Display help information.\n\n**--version**\n> Display version."],["DESCRIPTION","**nyxt** is a keyboard-oriented web browser. Extensible with Common Lisp.\n\nThe browser features programmable keybindings and commands. Inspired by Emacs and Vim."],["CAVEATS","Common Lisp configuration. Learning curve for customization. WebKitGTK backend."],["HISTORY","Nyxt was created as a **keyboard-driven browser** for power users, inspired by Emacs."],["INSTALL","```pacman: sudo pacman -S nyxt```\n\n```apk: sudo apk add nyxt```\n\n```zypper: sudo zypper install nyxt```\n\n```nix: nix profile install nixpkgs#nyxt```"],["SEE ALSO","[firefox](/man/firefox)(1), [qutebrowser](/man/qutebrowser)(1), [surf](/man/surf)(1)"]],"e":["firefox","qutebrowser","surf"],"x":"nyxt"},"odpscmd-tunnel":{"n":"odpscmd-tunnel","t":"Bulk data transfer for MaxCompute tables","s":[["TAGLINE","Bulk data transfer for MaxCompute tables"],["TLDR","**Upload a file to a table**\n\n```odpscmd tunnel upload [file.csv] [project.table]```\n\n**Download a table to a file**\n\n```odpscmd tunnel download [project.table] [output.csv]```\n\n**Upload to a specific partition**\n\n```odpscmd tunnel upload [file.csv] [table/pt=value]```\n\n**Resume a failed upload session**\n\n```odpscmd tunnel resume [session_id]```\n\n**Upload with custom delimiters and multiple threads**\n\n```odpscmd tunnel upload [file.csv] [table] -fd [,] -rd [\\n] -t [4]```\n\n**Download a limited number of records**\n\n```odpscmd tunnel download [project.table] [output.csv] -limit [1000]```"],["SYNOPSIS","**odpscmd** **tunnel** [_command_] [_options_]"],["PARAMETERS","**upload** _FILE_ _TABLE_\n> Upload local file data to a MaxCompute table.\n\n**download** _TABLE_ _FILE_\n> Download table data to a local file.\n\n**resume** _ID_\n> Resume an interrupted upload session.\n\n**-fd** _CHAR_\n> Field delimiter (default: comma).\n\n**-rd** _CHAR_\n> Record delimiter (default: newline).\n\n**-t** _NUM_\n> Number of threads for parallel transfer (default: 1).\n\n**-bs** _SIZE_\n> Block size in MiB (default: 100).\n\n**-cp** _BOOL_\n> Enable compression (default: true).\n\n**-cf** _BOOL_\n> Treat the file as CSV format.\n\n**-h** _BOOL_\n> Include a header row in output.\n\n**-limit** _NUM_\n> Limit the number of records to download.\n\n**-dbr** _BOOL_\n> Discard bad records instead of failing.\n\n**-mbr** _NUM_\n> Maximum number of bad records allowed (default: 1000).\n\n**-acp** _BOOL_\n> Auto-create partition if it does not exist.\n\n**-te** _ENDPOINT_\n> Specify a custom Tunnel endpoint."],["DESCRIPTION","**odpscmd tunnel** provides high-speed bulk data transfer between local files and MaxCompute (formerly ODPS) tables. It supports uploading and downloading large datasets with configurable parallelism, compression, and delimiters.\n\nEach block of data corresponds to an HTTP request. Multiple blocks can be uploaded in parallel and atomically -- success or failure of one block does not affect others. Transfer speeds typically range from 1 MB/s to 20 MB/s depending on network conditions."],["CAVEATS","Resumable uploads require the session ID from the original upload. Bandwidth limits may apply based on the MaxCompute project configuration. The tool is part of the MaxCompute Console (odpscmd) suite."],["HISTORY","**odpscmd tunnel** is part of the MaxCompute Console provided by **Alibaba Cloud** for bulk data operations on MaxCompute (originally known as ODPS)."],["INSTALL","```aur: yay -S odpscmd```"],["SEE ALSO","[odpscmd](/man/odpscmd)(1), [odps-tunnel](/man/odps-tunnel)(1)"]],"e":["odpscmd","odps-tunnel"],"x":"odpscmd tunnel upload [file.csv] [project.table]"},"pamenlarge":{"n":"pamenlarge","t":"enlarges a Netpbm image N times by duplicating pixels","s":[["TAGLINE","enlarges a Netpbm image N times by duplicating pixels"],["TLDR","**Enlarge image 2x**\n\n```pamenlarge 2 [input.pam] > [output.pam]```\n\n**Enlarge image 4x**\n\n```pamenlarge 4 [input.pam] > [output.pam]```\n\n**Enlarge using named option**\n\n```pamenlarge -scale=[3] [input.pam] > [output.pam]```\n\n**Enlarge with different x/y factors**\n\n```pamenlarge -xscale=[3] -yscale=[2] [input.pam] > [output.pam]```\n\n**Enlarge horizontally only**\n\n```pamenlarge -xscale=[4] [input.pam] > [output.pam]```"],["SYNOPSIS","**pamenlarge** [**-scale=**_N_] [**-xscale=**_N_] [**-yscale=**_N_] [_pamfile_]\n\n**pamenlarge** _N_ [_pamfile_]"],["PARAMETERS","**-scale=**_N_\n> Scale factor applied both horizontally and vertically. Cannot be combined with -xscale or -yscale.\n\n**-xscale=**_N_\n> Horizontal scale factor. If -yscale is not given, vertical scaling defaults to 1.\n\n**-yscale=**_N_\n> Vertical scale factor. If -xscale is not given, horizontal scaling defaults to 1.\n\n_N_\n> Positional scale factor (legacy syntax). Equivalent to -scale=N."],["DESCRIPTION","**pamenlarge** reads a Netpbm image, replicates its pixels N times, and produces an enlarged Netpbm image. Each pixel becomes an N x N block of identical pixels.\n\nThis produces a blocky/pixelated look, useful for pixel art or simple scaling. For smooth scaling, use **pamscale** instead. For scale factors of 3 or more, applying **pnmsmooth** afterward can reduce pixelation.\n\nFor PBM images, optimized algorithms exist for scale factors up to 10. Running multiple passes with smaller factors can be faster than a single large factor."],["EXAMPLE","```bash"],["Double the size","pamenlarge 2 small.ppm > large.ppm"],["Triple horizontal, double vertical","pamenlarge -xscale=3 -yscale=2 input.ppm > output.ppm\n```"],["COMPARISON","```\npamenlarge - Fast, integer factors, blocky\npamscale   - Smooth scaling, any factor\n```"],["CAVEATS","Integer factors only. No interpolation. Large factors create big files quickly. The -scale, -xscale, and -yscale options were added in Netpbm 10.86 (March 2019); older versions only support the positional argument syntax."],["HISTORY","**pamenlarge** was added to Netpbm in version **10.25** (October 2004) as a replacement for the older **pnmenlarge**."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pamscale](/man/pamscale)(1), [pnmenlarge](/man/pnmenlarge)(1), [pamstretch](/man/pamstretch)(1)"]],"e":["pamscale","pnmenlarge","pamstretch"],"x":"pamenlarge 2 [input.pam] > [output.pam]"},"pkeyutl.1s":{"n":"pkeyutl.1s","t":"Perform low-level public key operations","s":[["TAGLINE","Perform low-level public key operations"],["TLDR","**Sign data using a private key (with raw input hashing)**\n\n```openssl pkeyutl -sign -rawin -digest sha256 -in [data] -inkey [key.pem] -out [signature]```\n\n**Verify a signature**\n\n```openssl pkeyutl -verify -rawin -digest sha256 -in [data] -sigfile [sig] -inkey [key.pem]```\n\n**Encrypt data using a public key with OAEP padding**\n\n```openssl pkeyutl -encrypt -in [data] -pubin -inkey [pub.pem] -pkeyopt rsa_padding_mode:oaep -out [encrypted]```\n\n**Decrypt data using a private key**\n\n```openssl pkeyutl -decrypt -in [encrypted] -inkey [key.pem] -out [decrypted]```\n\n**Derive a shared secret via ECDH**\n\n```openssl pkeyutl -derive -inkey [key.pem] -peerkey [peer_pub.pem] -out [secret]```"],["SYNOPSIS","**openssl** **pkeyutl** [_options_]"],["PARAMETERS","**-sign**\n> Sign the input data and output the signed result. Requires a private key.\n\n**-verify**\n> Verify the input data against a signature file specified with **-sigfile**.\n\n**-verifyrecover**\n> Recover the original data from a signature. RSA only.\n\n**-encrypt**\n> Encrypt the input data using a public key.\n\n**-decrypt**\n> Decrypt the input data using a private key.\n\n**-derive**\n> Derive a shared secret using a peer public key (ECDH/DH).\n\n**-encap**\n> Encapsulate a shared secret to a peer's public key (KEM operations, e.g. ML-KEM).\n\n**-decap**\n> Decapsulate to recover a shared secret (KEM operations, e.g. ML-KEM).\n\n**-in** _FILE_\n> Input file (stdin if not specified).\n\n**-out** _FILE_\n> Output file (stdout if not specified).\n\n**-secret** _FILE_\n> Output file for the shared secret in encapsulation/decapsulation operations.\n\n**-inkey** _FILE_|_URI_\n> Input key file. Private key by default; use **-pubin** or **-certin** to supply a public key.\n\n**-sigfile** _FILE_\n> Signature file, required for **-verify** operations.\n\n**-peerkey** _FILE_\n> Peer public key file, used with **-derive** for ECDH/DH key agreement.\n\n**-pubin**\n> Treat the key supplied via **-inkey** as a public key.\n\n**-certin**\n> Treat the key supplied via **-inkey** as a certificate from which the public key is extracted.\n\n**-keyform** _PEM_|_DER_|_P12_\n> Format of the key file. Default is PEM.\n\n**-peerform** _PEM_|_DER_|_P12_\n> Format of the peer key file. Default is PEM.\n\n**-passin** _ARG_\n> Password source for encrypted private keys (e.g. `pass:secret`, `env:VAR`, `file:path`).\n\n**-rawin**\n> Treat input as raw unhashed data. The tool will hash it internally using the algorithm specified by **-digest**. Required for Ed25519/Ed448 and recommended for RSA-PSS signing.\n\n**-digest** _ALGORITHM_\n> Hash algorithm to apply when **-rawin** is set (e.g. `sha256`, `sha512`). Defaults to SHA-256 for RSA/DSA/ECDSA and SM3 for SM2.\n\n**-pkeyopt** _opt_:_value_\n> Set an algorithm-specific option. May be repeated. Common options:\n>\n> - `rsa_padding_mode:pkcs1|oaep|pss|none` — RSA padding mode (default: `pkcs1`)\n> - `rsa_oaep_md:digest` — Hash for OAEP label (default: `sha1`)\n> - `rsa_mgf1_md:digest` — MGF1 digest for PSS or OAEP\n> - `rsa_pss_saltlen:len|digest|max|auto` — PSS salt length\n> - `distid:string` — SM2 distinguishing ID (must match for sign/verify)\n\n**-kdf** _ALGORITHM_\n> Key derivation function to use (e.g. `TLS1-PRF`, `HKDF`).\n\n**-kdflen** _LENGTH_\n> Output length in bytes for the KDF operation.\n\n**-rev**\n> Reverse the input buffer byte order before processing. Incompatible with **-rawin**.\n\n**-hexdump**\n> Hex dump the output data.\n\n**-asn1parse**\n> Parse and display the output as ASN.1."],["DESCRIPTION","**openssl pkeyutl** performs low-level public key cryptographic operations including signing, verification, encryption, decryption, key derivation, and key encapsulation/decapsulation (KEM). It is algorithm-agnostic and works with any key type supported by OpenSSL, including RSA, EC, DSA, Ed25519, Ed448, X25519, X448, SM2, and post-quantum algorithms such as ML-DSA and ML-KEM.\n\nUnlike higher-level OpenSSL commands, it operates directly on data, offering fine-grained control over padding schemes, digest selection, and key agreement protocols. By default it expects pre-digested input; pass **-rawin** with **-digest** to have the tool hash the data itself before operating.\n\nFor Ed25519 and Ed448, the entire input file is loaded into memory and operated on in a single shot; these algorithms do not support pre-hashing."],["CAVEATS","Part of OpenSSL. Input size for non-raw operations is limited to `EVP_MAX_MD_SIZE` (64 bytes); use **-rawin** for arbitrary-length inputs. RSA PKCS#1 v1.5 decryption applies implicit rejection (returning deterministic random plaintext on padding failure) to mitigate Bleichenbacher attacks. Use OAEP padding (`-pkeyopt rsa_padding_mode:oaep`) for new RSA encryption applications. Ed25519/Ed448 sign and verify load the whole file into memory."],["HISTORY","**openssl pkeyutl** provides public key operations across all algorithms and supersedes the older **rsautl** subcommand for RSA-specific work. KEM operations (**-encap**/**-decap**) and post-quantum algorithm support were added in OpenSSL 3.x."],["SEE ALSO","[openssl](/man/openssl)(1), [openssl-pkey](/man/openssl-pkey)(1), [openssl-genpkey](/man/openssl-genpkey)(1), [openssl-dgst](/man/openssl-dgst)(1)"]],"e":["openssl","openssl-pkey","openssl-genpkey","openssl-dgst"],"x":"openssl pkeyutl -sign -rawin -digest sha256 -in [data] -inkey [key.pem] -out [signature]"},"pm2":{"n":"pm2","t":"Production process manager for Node.js","s":[["TAGLINE","Production process manager for Node.js"],["TLDR","**Start application**\n\n```pm2 start [app.js]```\n\n**Start with name**\n\n```pm2 start [app.js] --name \"[app_name]\"```\n\n**List processes**\n\n```pm2 list```\n\n**Stop process**\n\n```pm2 stop [name|id]```\n\n**Restart process**\n\n```pm2 restart [name|id]```\n\n**View logs**\n\n```pm2 logs```\n\n**Monitor processes**\n\n```pm2 monit```"],["SYNOPSIS","**pm2** [_command_] [_options_]"],["PARAMETERS","**start** _APP_\n> Start application.\n\n**stop** _ID_\n> Stop process.\n\n**restart** _ID_\n> Restart process.\n\n**list**\n> List processes.\n\n**logs**\n> View logs.\n\n**monit**\n> Monitor dashboard.\n\n**--name** _NAME_\n> Process name."],["DESCRIPTION","**pm2** is a production process manager for Node.js applications. It keeps applications running continuously with automatic restart on crashes, built-in load balancing via cluster mode, and centralized log management.\n\nThe tool provides a monitoring dashboard (**monit**), process listing, log viewing, and startup script generation for system boot persistence. It can manage multiple applications simultaneously, each with independent configurations for instances, environment variables, and restart strategies."],["CONFIGURATION","**ecosystem.config.js**\n> Application configuration file defining processes, environment variables, and deployment settings.\n\n**~/.pm2/**\n> PM2 home directory containing logs, process database, and module data.\n\n**PM2_HOME**\n> Environment variable to override the default PM2 home directory."],["CAVEATS","Node.js specific. Requires npm install. Daemon-based."],["HISTORY","PM2 was created for **production Node.js** process management."],["INSTALL","```pacman: sudo pacman -S pm2```\n\n```nix: nix profile install nixpkgs#pm2```"],["SEE ALSO","[node](/man/node)(1), [forever](/man/forever)(1), [nodemon](/man/nodemon)(1)"]],"e":["node","forever","nodemon"],"x":"pm2 start [app.js]"},"prime-run":{"n":"prime-run","t":"Run commands on dedicated Nvidia GPU","s":[["TAGLINE","Run commands on dedicated Nvidia GPU"],["TLDR","Run command on **dedicated Nvidia GPU**\n\n```prime-run [command]```\n\nRun a **game** on the Nvidia GPU\n\n```prime-run steam```\n\n**Verify** Nvidia card is used\n\n```prime-run glxinfo | grep \"OpenGL renderer\"```\n\nCheck **Vulkan** renderer\n\n```prime-run vulkaninfo | grep \"deviceName\"```"],["SYNOPSIS","**prime-run** _command_ [_arguments_...]"],["DESCRIPTION","**prime-run** executes programs using a dedicated Nvidia GPU on hybrid graphics systems (Optimus). It is a thin wrapper that sets the environment variables **__NV_PRIME_RENDER_OFFLOAD=1**, **__NV_PRIME_RENDER_OFFLOAD_PROVIDER=NVIDIA-G0**, **__GLX_VENDOR_LIBRARY_NAME=nvidia**, and **__VK_LAYER_NV_optimus=NVIDIA_only** before executing the given command.\n\nThe tool is part of PRIME render offload, allowing selective use of the powerful GPU for specific applications while conserving power with integrated graphics for general use."],["CAVEATS","Requires nvidia-prime package and proper driver setup. Only works with Nvidia proprietary drivers. Some applications may not respect the environment variables. Wayland support varies by compositor."],["HISTORY","**prime-run** was introduced with NVIDIA's PRIME render offload support in driver version 435. It provides a simpler alternative to Bumblebee for hybrid graphics on Linux laptops."],["SEE ALSO","[nvidia-smi](/man/nvidia-smi)(1), [optirun](/man/optirun)(1), [switcheroo-control](/man/switcheroo-control)(1)"]],"e":["nvidia-smi","optirun","switcheroo-control"],"x":"prime-run [command]"},"pulumi-version":{"n":"pulumi-version","t":"Display the installed Pulumi CLI version","s":[["TAGLINE","Display the installed Pulumi CLI version"],["TLDR","**Show Pulumi version**\n\n```pulumi version```"],["SYNOPSIS","**pulumi** **version**"],["PARAMETERS","**--color** _mode_\n> Colorize output. Choices: `always`, `never`, `raw`, `auto` (default: `auto`).\n\n**--non-interactive**\n> Disable interactive mode for all commands.\n\n**--logflow**\n> Flow log settings to child processes such as plugins.\n\n**--logtostderr**\n> Log to stderr instead of to files.\n\n**-v**, **--verbose** _level_\n> Enable verbose logging at the given level."],["DESCRIPTION","**pulumi version** prints the version number of the installed Pulumi CLI. Useful for verifying the installation and checking whether an upgrade is available."],["INSTALL","```pacman: sudo pacman -S pulumi```\n\n```apk: sudo apk add pulumi```\n\n```brew: brew install pulumi```\n\n```nix: nix profile install nixpkgs#pulumi```"],["SEE ALSO","[pulumi](/man/pulumi)(1), [pulumi-about](/man/pulumi-about)(1)"]],"e":["pulumi","pulumi-about"],"x":"pulumi version"},"pybind11":{"n":"pybind11","t":"Create Python bindings for C++ code","s":[["TAGLINE","Create Python bindings for C++ code"],["TLDR","**Get include path**\n\n```python -m pybind11 --includes```\n\n**Get CMake directory**\n\n```python -m pybind11 --cmakedir```\n\n**Compile binding manually**\n\n```c++ -O3 -shared $(python3 -m pybind11 --includes) [example.cpp] -o [example.so]```"],["SYNOPSIS","**python -m pybind11** [_options_]"],["PARAMETERS","**--includes**\n> Include flags for compiler.\n\n**--cmakedir**\n> CMake module path."],["DESCRIPTION","**pybind11** is a header-only library for creating Python bindings of C++ code. It enables seamless interoperability between Python and C++ with minimal boilerplate."],["EXAMPLE BINDING","```cpp\n#include <pybind11/pybind11.h>\n\nint add(int a, int b) {\n    return a + b;\n}\n\nPYBIND11_MODULE(example, m) {\n    m.def(\"add\", &add, \"Add two numbers\");\n}\n```"],["CMAKE SETUP","```cmake\nfind_package(pybind11 REQUIRED)\npybind11_add_module(example example.cpp)\n```"],["COMPILE MANUALLY","```bash"],["Get flags","CFLAGS=$(python3 -m pybind11 --includes)\nSUFFIX=$(python3-config --extension-suffix)"],["Compile","c++ -O3 -Wall -shared -std=c++11 -fPIC \\\n    $CFLAGS example.cpp -o example$SUFFIX\n```"],["CAVEATS","Header-only (no linking needed). Requires C++11+. Performance overhead minimal compared to pure C API."],["HISTORY","pybind11 was created by **Wenzel Jakob** as a lightweight alternative to Boost.Python, focusing on C++11 features."],["INSTALL","```pacman: sudo pacman -S pybind11```\n\n```brew: brew install pybind11```"],["SEE ALSO","[cython](/man/cython)(1), [swig](/man/swig)(1), [python](/man/python)(1)"]],"e":["cython","swig","python"],"x":"python -m pybind11 --includes"},"qgis":{"n":"qgis","t":"Open-source Geographic Information System","s":[["TAGLINE","Open-source Geographic Information System"],["TLDR","**Launch QGIS**\n\n```qgis```\n\n**Open a specific project file**\n\n```qgis --project [path/to/project.qgs]```\n\n**Open QGIS with specific layers**\n\n```qgis [path/to/file.shp] [path/to/raster.tif]```\n\n**Export a map snapshot** to an image file\n\n```qgis --snapshot [output.png] --width [1024] --height [768] --project [project.qgs]```\n\n**Launch without splash screen** and without plugins\n\n```qgis --nologo --noplugins```\n\n**Export project layers to DXF**\n\n```qgis --dxf-export [output.dxf] --project [project.qgs]```\n\n**Use a specific user profile**\n\n```qgis --profile [profilename]```\n\n**Run a Python script on startup**\n\n```qgis --code [path/to/script.py]```"],["SYNOPSIS","**qgis** [_options_] [_file_ ...]"],["PARAMETERS","**-p**, **--project** _projectfile_\n> Load the given QGIS project file.\n\n**-s**, **--snapshot** _filename_\n> Emit a snapshot of loaded datasets to the given image file.\n\n**-w**, **--width** _width_\n> Width of snapshot to emit.\n\n**-h**, **--height** _height_\n> Height of snapshot to emit.\n\n**-e**, **--extent** _xmin,ymin,xmax,ymax_\n> Set initial map extent.\n\n**-n**, **--nologo**\n> Hide the splash screen on startup.\n\n**-V**, **--noversioncheck**\n> Don't check for new version at startup.\n\n**-P**, **--noplugins**\n> Don't restore plugins on startup.\n\n**--nopython**\n> Disable Python support entirely.\n\n**-B**, **--skipbadlayers**\n> Don't prompt for missing layers.\n\n**-C**, **--nocustomization**\n> Don't apply GUI customization.\n\n**-z**, **--customizationfile** _path_\n> Use given ini file as GUI customization.\n\n**-f**, **--code** _path_\n> Run given Python file on load.\n\n**--profile** _name_\n> Load a named profile from the profiles folder.\n\n**-S**, **--profiles-path** _path_\n> Path to store user profile folders.\n\n**-l**, **--lang** _language_\n> Use specified language for the interface.\n\n**-d**, **--defaultui**\n> Reset user UI settings to default.\n\n**--hide-browser**\n> Hide the browser widget.\n\n**--dxf-export** _filename.dxf_\n> Emit DXF output of loaded datasets.\n\n**--dxf-extent** _xmin,ymin,xmax,ymax_\n> Set extent for DXF export.\n\n**--dxf-symbology-mode** _mode_\n> Symbology mode for DXF export: none, symbollayer, or feature.\n\n**-v**, **--version**\n> Display version information and exit.\n\n**--help**\n> Display help text."],["DESCRIPTION","**QGIS** is a cross-platform, free and open-source Geographic Information System licensed under the GNU GPLv2+. It supports viewing, editing, and analysis of geospatial data. Written primarily in C++ using the Qt framework, it features extensive Python plugin support.\n\nQGIS handles vector formats (Shapefiles, GeoJSON, GML, KML, GeoPackage, PostGIS, and all OGR-supported formats), raster formats (GeoTIFF, ERDAS IMG, and all GDAL-supported formats), and database sources. It integrates with GRASS GIS for advanced analytical functions and uses GDAL/OGR for data format translation. Features include map composition, spatial analysis, digitizing, geoprocessing, and a rich plugin ecosystem.\n\nFor headless or batch processing, use the companion tool **qgis_process** instead."],["CONFIGURATION","User settings stored in **~/.config/QGIS/qgis.conf**. QGIS supports multiple user profiles, each with separate settings, plugins, and project templates. Use **--profile** to switch between profiles or **--profiles-path** to specify a custom profiles directory."],["CAVEATS","QGIS is primarily a GUI application; command-line usage is limited to launching with options, taking snapshots, and DXF export. Python plugins may affect startup time; use **--noplugins** or **--nopython** for faster launches. Large projects with many layers can consume significant memory. The **--configpath** option from QGIS 2.x was replaced by **--profiles-path** in QGIS 3.x."],["HISTORY","Development began in **February 2002** by **Gary Sherman** as **Quantum GIS**, initially a simple PostGIS data viewer for Linux. The first public release came in **July 2002**. It became an **OSGeo** incubator project in **2007**, and version 1.0 was released in **January 2009**. In **September 2013**, version 2.0 was released and the project was officially renamed from Quantum GIS to **QGIS**. Gary Sherman received the **Sol Katz Award** for open-source GIS contributions in **2014**."],["INSTALL","```apt: sudo apt install qgis```\n\n```dnf: sudo dnf install qgis```\n\n```pacman: sudo pacman -S qgis```\n\n```zypper: sudo zypper install qgis```\n\n```nix: nix profile install nixpkgs#qgis```"],["SEE ALSO","[gdalinfo](/man/gdalinfo)(1), [ogr2ogr](/man/ogr2ogr)(1), [ogrinfo](/man/ogrinfo)(1), [gdalwarp](/man/gdalwarp)(1), [gdal_translate](/man/gdal_translate)(1)"]],"e":["gdalinfo","ogr2ogr","ogrinfo","gdalwarp","gdal_translate"],"x":"qgis"},"runcon":{"n":"runcon","t":"Run commands with specified SELinux context","s":[["TAGLINE","Run commands with specified SELinux context"],["TLDR","Print current **security context**\n\n```runcon```\n\nSpecify **domain** to run command in\n\n```runcon -t domain_t command```\n\nSpecify context **role**\n\n```runcon -r role_r command```\n\nSpecify **full context**\n\n```runcon user_u:role_r:domain_t command```"],["SYNOPSIS","**runcon** [_options_] [_context_] _command_ [_args_...]"],["PARAMETERS","**-u**, **--user** _user_\n> Specify SELinux user identity.\n\n**-r**, **--role** _role_\n> Specify SELinux role.\n\n**-t**, **--type** _type_\n> Specify SELinux domain/type (requires the same role and user).\n\n**-l**, **--range** _range_\n> Specify SELinux MLS/MCS level range.\n\n**--compute**\n> Compute the target process security context before modifying.\n\n**--help**\n> Display help information.\n\n**--version**\n> Output version information."],["DESCRIPTION","**runcon** runs a program in a different SELinux security context. It allows executing commands with specific SELinux user, role, and type labels.\n\nThis is useful for testing SELinux policies and running programs in restricted contexts."],["CAVEATS","Requires SELinux to be enabled. Transitions must be allowed by SELinux policy. Not all context transitions are permitted."],["HISTORY","Part of **GNU Coreutils**, providing SELinux context manipulation for command execution."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[secon](/man/secon)(1), [run_init](/man/run_init)(8), [chcon](/man/chcon)(1)"]],"e":["secon","run_init","chcon"],"x":"runcon"},"sc":{"n":"sc","t":"Terminal-based spreadsheet calculator","s":[["TAGLINE","Terminal-based spreadsheet calculator"],["TLDR","**Start sc** with a new spreadsheet\n\n```sc```\n\n**Open an existing spreadsheet file**\n\n```sc [file.sc]```\n\n**Start sc-im** (improved version)\n\n```sc-im```\n\n**Open a CSV file** in sc-im\n\n```sc-im [file.csv]```\n\n**Open an Excel file** in sc-im\n\n```sc-im [file.xlsx]```"],["SYNOPSIS","**sc** [_-rxnCqS_] [_file_]"],["PARAMETERS","**-r**\n> Read-only mode\n\n**-x**\n> Disable X11 functionality\n\n**-n**\n> No prompting for commands\n\n**-C**\n> Start with color disabled\n\n**-q**\n> Quiet mode"],["KEYBOARD COMMANDS","**h, j, k, l**\n> Move left, down, up, right (vi-style)\n\n**g** _cell_\n> Go to specified cell (e.g., gA1, gD3)\n\n**e**\n> Enter edit mode for current cell\n\n**=**\n> Enter a formula\n\n**x**\n> Delete current cell contents\n\n**dd**\n> Delete entire row\n\n**yy**\n> Yank (copy) row\n\n**p**\n> Paste yanked content\n\n**ZZ**\n> Save and quit\n\n**ZQ**\n> Quit without saving"],["DESCRIPTION","**sc** is a terminal-based spreadsheet calculator with vi-style keybindings. It supports formulas, macros, and external functions while operating entirely in text mode. Spreadsheets are saved as human-readable command scripts.\n\nThe modern successor **sc-im** (Spreadsheet Calculator Improvised) extends sc with undo/redo, support for 65,536 rows and 702 columns, CSV/XLSX import/export, and Lua scripting. Both programs share similar vi-like navigation and editing commands.\n\nSpreadsheet files can be exported to plain text, LaTeX, or CSV formats for use with other applications."],["CAVEATS","sc uses a unique file format (.sc) that stores spreadsheets as command sequences. The original sc development stalled in 2002; sc-im is the actively maintained fork for modern systems."],["HISTORY","Development of sc began in **1981** by James Gosling and Mark Weiser, making it one of the oldest open-source spreadsheet applications. Chuck Martin later added modifications. In **2015**, development resumed under the name **sc-im** with significant enhancements."],["INSTALL","```apt: sudo apt install sc```\n\n```dnf: sudo dnf install sc```\n\n```apk: sudo apk add sc```\n\n```nix: nix profile install nixpkgs#sc```"],["SEE ALSO","[sc-im](/man/sc-im)(1), [gnumeric](/man/gnumeric)(1), [bc](/man/bc)(1), [vi](/man/vi)(1)"]],"e":["sc-im","gnumeric","bc","vi"],"x":"sc"},"scanadf":{"n":"scanadf","t":"Batch scan from automatic document feeder","s":[["TAGLINE","Batch scan from automatic document feeder"],["TLDR","**Scan all pages from the document feeder**\n\n```scanadf -o [output_%d.pnm]```\n\n**Scan a specific number of pages**\n\n```scanadf -e [5] -o [page_%d.pnm]```\n\n**Scan starting from a specific page number**\n\n```scanadf -s [1] -o [scan_%d.pnm]```\n\n**Scan with a specific device**\n\n```scanadf -d [device_name] -o [output_%d.pnm]```\n\n**Run a script after each page** is scanned\n\n```scanadf -S [process.sh] -o [page_%d.pnm]```\n\n**Scan without overwriting existing files**\n\n```scanadf -N -o [output_%d.pnm]```"],["SYNOPSIS","**scanadf** [_-d device_] [_-o file_] [_-s num_] [_-e num_] [_-S script_] [_device-options_]"],["PARAMETERS","**-d** _device_, **--device-name**=_device_\n> Use the specified scanner device\n\n**-o** _file_, **--output-file**=_file_\n> Output filename format; %d is replaced with page number\n\n**-s** _num_, **--start-count**=_num_\n> Page number of first scanned image\n\n**-e** _num_, **--end-count**=_num_\n> Last page number to scan\n\n**-S** _script_, **--scan-script**=_script_\n> Script to run after each image is acquired\n\n**-N**, **--no-overwrite**\n> Prevent overwriting existing image files\n\n**-r**, **--raw**\n> Write raw image data without interpretation\n\n**-L**, **--list-devices**\n> List available scanner devices\n\n**-v**, **--verbose**\n> Increase verbosity level"],["DESCRIPTION","**scanadf** is a command-line tool for acquiring multiple images from scanners with automatic document feeders (ADF). It scans pages sequentially until the feeder is empty, writing each page to a numbered output file.\n\nThe tool uses the SANE interface and supports any scanner with a SANE backend. Output is typically in PNM format (PBM, PGM, or PPM depending on scan mode). Scanner-specific options like resolution and source can be passed directly.\n\nSome scanners require specific source options: UMAX uses **--source=\"Automatic Document Feeder\"**, HP uses **--scantype=ADF**."],["CAVEATS","The scanner backend must support ADF and return SANE_STATUS_NO_DOCS when the feeder is empty. Using scanadf with flatbed-only scanners will repeatedly scan the same page; always use **-e** to limit page count. The **%d** placeholder in output filenames is required for multiple pages."],["HISTORY","scanadf is part of the **SANE** (Scanner Access Now Easy) project, providing specialized support for document feeders. It complements scanimage by handling the multi-page workflow common in document scanning scenarios."],["SEE ALSO","[scanimage](/man/scanimage)(1), [sane-find-scanner](/man/sane-find-scanner)(1), [convert](/man/convert)(1)"]],"e":["scanimage","sane-find-scanner","convert"],"x":"scanadf -o [output_%d.pnm]"},"shuf":{"n":"shuf","t":"Randomly shuffle input lines","s":[["TAGLINE","Randomly shuffle input lines"],["TLDR","**Shuffle lines of a file**\n\n```shuf [file]```\n\n**Pick N random lines**\n\n```shuf -n [5] [file]```\n\n**Generate random numbers in range**\n\n```shuf -i [1-100]```\n\n**Pick N random numbers from range**\n\n```shuf -i [1-100] -n [10]```\n\n**Shuffle command arguments**\n\n```shuf -e [item1] [item2] [item3]```\n\n**Output with custom delimiter**\n\n```shuf -e -z [items] | xargs -0```\n\n**Repeat output** (with replacement)\n\n```shuf -r -n [10] -e [a] [b] [c]```\n\n**Use specific random seed**\n\n```shuf --random-source=[/dev/urandom] [file]```"],["SYNOPSIS","**shuf** [_-n count_] [_-i range_] [_-e args_] [_-r_] [_options_] [_file_]"],["PARAMETERS","**-n** _NUM_, **--head-count** _NUM_\n> Output at most NUM lines.\n\n**-i** _LO-HI_, **--input-range** _LO-HI_\n> Generate numbers from LO to HI.\n\n**-e**, **--echo**\n> Treat arguments as input lines.\n\n**-r**, **--repeat**\n> Output can repeat (with replacement).\n\n**-z**, **--zero-terminated**\n> Use NUL as line delimiter.\n\n**-o** _FILE_, **--output** _FILE_\n> Write to file instead of stdout.\n\n**--random-source** _FILE_\n> Get random bytes from file."],["DESCRIPTION","**shuf** randomly permutes input lines. It reads input, shuffles the order, and outputs all lines in random sequence.\n\nWithout options, shuf outputs all input lines in random order. The -n option limits output to the first N lines after shuffling - effectively random sampling without replacement.\n\nInput range mode (-i) generates sequential numbers and shuffles them. Combined with -n, this selects random numbers from a range. Useful for generating lottery numbers, random IDs, or sampling.\n\nEcho mode (-e) shuffles command-line arguments instead of file lines. This enables shuffling small lists without creating temporary files.\n\nRepeat mode (-r) enables sampling with replacement - the same line can appear multiple times in output. This is useful for bootstrap sampling or simulation.\n\nThe random source option ensures reproducible shuffling when given a deterministic source, useful for testing."],["CAVEATS","Loads entire input into memory. Very large files may cause memory issues. Default randomness is good but not cryptographic. Without -n, outputs entire input. Different from sort -R which may group identical lines."],["HISTORY","**shuf** is part of **GNU coreutils**, providing command-line random shuffling. While Unix systems had various random line selection tools, shuf provides comprehensive shuffling with range generation and sampling options."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[sort](/man/sort)(1), [head](/man/head)(1), [tail](/man/tail)(1), [sample](/man/sample)(1)"]],"e":["sort","head","tail","sample"],"x":"shuf [file]"},"steamosctl":{"n":"steamosctl","t":"SteamOS system and hardware management tool","s":[["TAGLINE","SteamOS system and hardware management tool"],["TLDR","**Get all system properties**\n\n```steamosctl get-all-properties```\n\n**Get the current fan control state**\n\n```steamosctl get-fan-control-state```\n\n**Set fan control** to OS-managed\n\n```steamosctl set-fan-control-state os```\n\n**Get current TDP limit**\n\n```steamosctl get-tdp-limit```\n\n**Set TDP limit** in watts\n\n```steamosctl set-tdp-limit [15]```\n\n**Set GPU performance level**\n\n```steamosctl set-gpu-performance-level [auto|low|high|manual]```\n\n**Set max battery charge level**\n\n```steamosctl set-max-charge-level [80]```\n\n**Get the device model**\n\n```steamosctl get-device-model```"],["SYNOPSIS","**steamosctl** \\<command\\> [arguments]"],["DESCRIPTION","**steamosctl** is a command-line utility for managing SteamOS system hardware and software settings. It communicates with the **steamos-manager** daemon over D-Bus to control hardware features on devices running SteamOS, including the Steam Deck and other supported handhelds.\n\nThe steamos-manager system consists of a user daemon on the session bus and a root daemon on the system bus for privileged operations. steamosctl provides command-line access to all exposed functionality including fan control, CPU and GPU tuning, TDP limits, Wi-Fi configuration, battery management, and firmware updates."],["COMMANDS","**get-fan-control-state** / **set-fan-control-state** _\\<bios|os\\>_\n> Get or set the fan control mode.\n\n**get-cpu-scaling-governor** / **set-cpu-scaling-governor** _\\<governor\\>_\n> Get or set the CPU scaling governor.\n\n**get-cpu-boost-state** / **set-cpu-boost-state** _\\<enabled|disabled\\>_\n> Get or set CPU boost.\n\n**get-gpu-performance-level** / **set-gpu-performance-level** _\\<auto|low|high|manual|profile_peak\\>_\n> Get or set GPU performance level.\n\n**get-gpu-power-profile** / **set-gpu-power-profile** _\\<profile\\>_\n> Get or set GPU power profile.\n\n**get-manual-gpu-clock** / **set-manual-gpu-clock** _\\<freq\\>_\n> Get or set manual GPU clock frequency in MHz.\n\n**get-tdp-limit** / **set-tdp-limit** _\\<watts\\>_\n> Get or set TDP limit.\n\n**get-tdp-limit-min** / **get-tdp-limit-max**\n> Get minimum or maximum allowed TDP values.\n\n**get-performance-profile** / **set-performance-profile** _\\<profile\\>_\n> Get or set performance profile.\n\n**get-wifi-backend** / **set-wifi-backend** _\\<iwd|wpa_supplicant\\>_\n> Get or set Wi-Fi backend.\n\n**get-wifi-power-management-state** / **set-wifi-power-management-state** _\\<enabled|disabled\\>_\n> Get or set Wi-Fi power management.\n\n**get-hdmi-cec-state** / **set-hdmi-cec-state** _\\<disabled|control-only|control-and-wake\\>_\n> Get or set HDMI-CEC state.\n\n**get-max-charge-level** / **set-max-charge-level** _\\<1-100|-1\\>_\n> Get or set max battery charge level. Use -1 for default.\n\n**update-bios**\n> Update the BIOS firmware.\n\n**update-dock**\n> Update dock firmware.\n\n**trim-devices**\n> Trim applicable storage drives.\n\n**prepare-factory-reset** _\\<user|os|all\\>_\n> Prepare a factory reset.\n\n**reload-config**\n> Reload configuration from disk.\n\n**get-all-properties**\n> Get all properties from the manager.\n\n**get-device-model**\n> Get the model and variant of the device."],["CAVEATS","Only available on SteamOS. Requires the **steamos-manager** daemon to be running. Some operations like firmware updates and factory resets require elevated privileges through the system D-Bus daemon. The tool has no traditional man page; documentation lives in the source code."],["HISTORY","**steamosctl** was developed by **Valve** with contributions from **Collabora** and **Igalia** as part of the **steamos-manager** project. Written in **Rust**, it was open-sourced in **May 2025** coinciding with the SteamOS 3.7 release. The tool provides a standardized D-Bus interface enabling third-party handheld devices to support SteamOS hardware management features."],["SEE ALSO","[steamos-update](/man/steamos-update)(1), [cpupower](/man/cpupower)(1), [powerprofilesctl](/man/powerprofilesctl)(1)"]],"e":["steamos-update","cpupower","powerprofilesctl"],"x":"steamosctl get-all-properties"},"stl2gts":{"n":"stl2gts","t":"Convert STL 3D meshes to GTS format","s":[["TAGLINE","Convert STL 3D meshes to GTS format"],["TLDR","**Convert STL to GTS**\n\n```stl2gts < [input.stl] > [output.gts]```\n\n**With verbose output**\n\n```stl2gts -v < [input.stl] > [output.gts]```\n\n**Help**\n\n```stl2gts --help```"],["SYNOPSIS","**stl2gts** [_-v_] < _input.stl_ > _output.gts_"],["PARAMETERS","**-v**, **--verbose**\n> Verbose output.\n\n**--help**\n> Show help.\n\n**--version**\n> Show version."],["DESCRIPTION","**stl2gts** converts 3D mesh files from the widely-used STL (Stereolithography) format into GTS (GNU Triangulated Surface) format. STL files represent 3D surfaces as collections of triangular facets and are commonly used in CAD software, 3D printing, and computational geometry.\n\nThe GTS format is designed for efficient manipulation and analysis of triangulated surfaces. Unlike STL, GTS stores topological information about vertex and edge connectivity, enabling operations like mesh refinement, boolean operations, and surface analysis that would be difficult with raw triangle soup.\n\nThe tool reads STL data from standard input and writes GTS output to standard output, following the Unix pipeline convention. It is part of the GTS Library toolkit, which provides a collection of utilities for triangulated surface mesh processing and computational geometry."],["CAVEATS","Stdin/stdout only. Binary STL not supported. Part of gts-bin."],["HISTORY","**stl2gts** is part of the **GTS Library** (GNU Triangulated Surface Library) for mesh manipulation and analysis."],["INSTALL","```apt: sudo apt install libgts-bin```"],["SEE ALSO","[gts2stl](/man/gts2stl)(1), [meshlab](/man/meshlab)(1), [admesh](/man/admesh)(1)"]],"e":["gts2stl","meshlab","admesh"],"x":"stl2gts < [input.stl] > [output.gts]"},"terraform-plan":{"n":"terraform-plan","t":"Preview Terraform infrastructure changes","s":[["TAGLINE","Preview Terraform infrastructure changes"],["TLDR","**Create execution plan**\n\n```terraform plan```\n\n**Save plan to file**\n\n```terraform plan -out=[plan.tfplan]```\n\n**Plan with variable**\n\n```terraform plan -var=\"[key]=[value]\"```\n\n**Plan destroy**\n\n```terraform plan -destroy```\n\n**Plan specific target**\n\n```terraform plan -target=[resource]```"],["SYNOPSIS","**terraform** **plan** [_options_]"],["PARAMETERS","**-out** _file_\n> Save plan to file.\n\n**-var** _name=value_\n> Set variable.\n\n**-var-file** _file_\n> Variable file.\n\n**-target** _resource_\n> Target specific resource.\n\n**-destroy**\n> Plan destruction.\n\n**-refresh-only**\n> Only refresh state.\n\n**-detailed-exitcode**\n> Exit 2 if changes planned."],["DESCRIPTION","**terraform plan** creates an execution plan showing proposed changes. Compares current state with configuration. Review before applying to understand infrastructure changes."],["INSTALL","```pacman: sudo pacman -S terraform```\n\n```nix: nix profile install nixpkgs#terraform```"],["SEE ALSO","[terraform](/man/terraform)(1), [terraform-apply](/man/terraform-apply)(1)"]],"e":["terraform","terraform-apply"],"x":"terraform plan"},"thunderbird":{"n":"thunderbird","t":"Mozilla email and calendar client","s":[["TAGLINE","Mozilla email and calendar client"],["TLDR","**Start Thunderbird**\n\n```thunderbird```\n\n**Compose new message**\n\n```thunderbird -compose \"[to='email@example.com',subject='Hello']\"```\n\n**Open profile manager**\n\n```thunderbird -ProfileManager```\n\n**Use specific profile**\n\n```thunderbird -P [profile-name]```\n\n**Safe mode**\n\n```thunderbird -safe-mode```\n\n**Check for new mail**\n\n```thunderbird -mail```\n\n**Open address book**\n\n```thunderbird -addressbook```"],["SYNOPSIS","**thunderbird** [_-compose opts_] [_-P profile_] [_options_]"],["PARAMETERS","**-compose** _OPTS_\n> Compose a new message. Options include to, cc, bcc, subject, body, attachment as comma-separated key=value pairs.\n\n**-P** _NAME_\n> Start with the specified profile.\n\n**-ProfileManager**\n> Open the profile manager to create or select profiles.\n\n**-safe-mode**\n> Start in safe mode with all extensions disabled for troubleshooting.\n\n**-mail**\n> Open the mail client and check for new messages.\n\n**-addressbook**\n> Open the address book.\n\n**-no-remote**\n> Do not accept or send remote commands; run a new instance.\n\n**-search**\n> Open the global search tab."],["DESCRIPTION","**thunderbird** is Mozilla's free, open-source email client that supports multiple email accounts, calendar integration, an address book, RSS feed reading, and built-in OpenPGP encryption for secure messaging.\n\nThe application supports IMAP, POP3, and SMTP protocols, and can be extended with add-ons for additional functionality. It uses profiles to store settings and data, manageable through the **-ProfileManager** flag, and can be launched in **-safe-mode** to troubleshoot issues with extensions."],["CAVEATS","Desktop application. Profile-based storage. Memory usage can be high with many accounts or large mailboxes."],["HISTORY","**Thunderbird** was originally developed by **Mozilla Foundation** as a companion to Firefox. Since 2020, it has been maintained by the **MZLA Technologies** subsidiary. It was rebranded as **Thunderbird Supernova** with the major UI refresh in version 115 (2023)."],["INSTALL","```apt: sudo apt install thunderbird```\n\n```dnf: sudo dnf install thunderbird```\n\n```pacman: sudo pacman -S thunderbird```\n\n```apk: sudo apk add thunderbird```\n\n```nix: nix profile install nixpkgs#thunderbird```"],["SEE ALSO","[mutt](/man/mutt)(1), [evolution](/man/evolution)(1), [firefox](/man/firefox)(1), [neomutt](/man/neomutt)(1)"]],"e":["mutt","evolution","firefox","neomutt"],"x":"thunderbird"},"tlsx":{"n":"tlsx","t":"Fast TLS certificate and cipher probing","s":[["TAGLINE","Fast TLS certificate and cipher probing"],["TLDR","**Check TLS**\n\n```tlsx -u [example.com]```\n\n**Show certificate**\n\n```tlsx -u [example.com] -san -cn```\n\n**Multiple hosts**\n\n```tlsx -l [hosts.txt]```\n\n**JSON output**\n\n```tlsx -u [example.com] -json```\n\n**Check specific port**\n\n```tlsx -u [example.com:8443]```\n\n**Show cipher**\n\n```tlsx -u [example.com] -cipher```"],["SYNOPSIS","**tlsx** [_-u host_] [_-l file_] [_-json_] [_options_]"],["PARAMETERS","**-u** _HOST_\n> Target host.\n\n**-l** _FILE_\n> Host list file.\n\n**-san**\n> Show Subject Alt Names.\n\n**-cn**\n> Show Common Name.\n\n**-cipher**\n> Show cipher suites.\n\n**-json**\n> JSON output."],["DESCRIPTION","**tlsx** is a fast TLS probing tool that connects to hosts and inspects their TLS configurations, certificates, and supported cipher suites. It can extract certificate details including subject names, Subject Alternative Names (SANs), expiration dates, and issuer information.\n\nThe tool supports scanning multiple hosts in parallel from a file list, making it suitable for large-scale TLS reconnaissance. Output can be formatted as JSON for integration with other security tools. It is part of the ProjectDiscovery toolkit commonly used in security assessments."],["CAVEATS","Network access required. Some hosts may block. Go-based tool."],["HISTORY","**tlsx** was created by **ProjectDiscovery** for fast TLS probing and certificate analysis."],["INSTALL","```brew: brew install tlsx```\n\n```nix: nix profile install nixpkgs#tlsx```"],["SEE ALSO","[openssl](/man/openssl)(1), [testssl](/man/testssl)(1), [sslyze](/man/sslyze)(1)"]],"e":["openssl","testssl","sslyze"],"x":"tlsx -u [example.com]"},"transmission-daemon":{"n":"transmission-daemon","t":"Headless BitTorrent client daemon","s":[["TAGLINE","Headless BitTorrent client daemon"],["TLDR","**Start the daemon**\n\n```transmission-daemon```\n\n**Start with authentication**\n\n```transmission-daemon --auth --username [user] --password [pass]```\n\n**Start with web interface port**\n\n```transmission-daemon --port [9091]```\n\n**Specify download directory**\n\n```transmission-daemon --download-dir [/path/to/downloads]```\n\n**Allow specific IP addresses**\n\n```transmission-daemon --allowed \"[127.0.0.1,192.168.*.*]\"```\n\n**Watch directory for torrents**\n\n```transmission-daemon --watch-dir [/path/to/watch]```\n\n**Run in foreground**\n\n```transmission-daemon -f```\n\n**Require encryption**\n\n```transmission-daemon --encryption-required```"],["SYNOPSIS","**transmission-daemon** [_options_]"],["PARAMETERS","**-g**, **--config-dir** _dir_\n> Configuration directory location.\n\n**-a**, **--allowed** _list_\n> Comma-separated whitelist of allowed IPs (supports wildcards).\n\n**-b**, **--blocklist**\n> Enable peer blocklists.\n\n**-t**, **--auth**\n> Require authentication.\n\n**-u**, **--username** _name_\n> Authentication username.\n\n**-v**, **--password** _pass_\n> Authentication password.\n\n**-p**, **--port** _port_\n> RPC/web interface port (default: 9091).\n\n**-w**, **--download-dir** _dir_\n> Default download directory.\n\n**--incomplete-dir** _dir_\n> Directory for incomplete downloads.\n\n**--watch-dir** _dir_\n> Watch directory for new .torrent files.\n\n**-er**, **--encryption-required**\n> Require encrypted connections.\n\n**-ep**, **--encryption-preferred**\n> Prefer encrypted connections.\n\n**-f**, **--foreground**\n> Run in foreground.\n\n**--dump-settings**\n> Dump current settings to stderr.\n\n**-h**, **--help**\n> Display help."],["DESCRIPTION","**transmission-daemon** is a headless BitTorrent client that runs as a background daemon. It provides full BitTorrent functionality controlled via RPC interface, accessible through the web interface or transmission-remote command.\n\nThe daemon supports magnet links, DHT, peer exchange, encryption, and bandwidth scheduling. Configuration is stored in settings.json in the config directory.\n\nThe web interface (default: http://localhost:9091) provides a graphical way to manage torrents. For command-line control, use transmission-remote to add, remove, and manage torrents.\n\nStop the daemon before editing settings.json manually, as changes are overwritten on exit."],["CONFIGURATION","**~/.config/transmission-daemon/settings.json**\n> Main configuration file in JSON format containing all daemon settings including download paths, speed limits, peer options, and RPC authentication.\n\n**-g**, **--config-dir** _dir_\n> Override the default configuration directory location.\n\n**TRANSMISSION_HOME**\n> Environment variable to override the default config directory path."],["CAVEATS","Settings file is overwritten when daemon exits. Web interface requires authentication in production. Default port 9091 should be firewalled from public access. Blocklist updates require daemon restart."],["HISTORY","**Transmission** was created in 2005 and is one of the oldest actively maintained BitTorrent clients. The daemon mode enables headless operation for servers and NAS devices. It is known for its lightweight resource usage and cross-platform support."],["INSTALL","```apt: sudo apt install transmission-daemon```\n\n```dnf: sudo dnf install transmission-daemon```\n\n```apk: sudo apk add transmission-daemon```\n\n```zypper: sudo zypper install transmission-daemon```"],["SEE ALSO","[transmission-remote](/man/transmission-remote)(1), [transmission-cli](/man/transmission-cli)(1), [rtorrent](/man/rtorrent)(1)"]],"e":["transmission-remote","transmission-cli","rtorrent"],"x":"transmission-daemon"},"tt":{"n":"tt","t":"Terminal-based typing speed test","s":[["TAGLINE","Terminal-based typing speed test"],["TLDR","**Start a typing test** with default 50 words\n\n```tt```\n\n**Start a test with custom word count**\n\n```tt -n [100]```\n\n**Start a timed test** (seconds)\n\n```tt -t [60]```\n\n**Use quote mode** with English quotes\n\n```tt -quotes en```\n\n**Group words** into segments\n\n```tt -n [50] -g [10]```\n\n**Apply a custom theme**\n\n```tt -theme [gruvbox]```\n\n**Use a custom word list**\n\n```tt -words [english1000]```\n\n**Log results to CSV**\n\n```tt -csv >> [~/wpm.csv]```"],["SYNOPSIS","**tt** [_options_] [_file_]"],["PARAMETERS","**-n** _number_\n> Number of words in the test (default: 50)\n\n**-t** _seconds_\n> Time limit for the test in seconds\n\n**-g** _number_\n> Group words into segments of specified size\n\n**-quotes** _name_\n> Enable quote mode with specified quote list\n\n**-words** _name_\n> Use custom or built-in word list\n\n**-theme** _name_\n> Apply custom or built-in theme\n\n**-csv**\n> Output results in CSV format\n\n**-raw**\n> Raw output mode for scripting\n\n**-showwpm**\n> Display live WPM while typing\n\n**-help**\n> Display help information"],["DESCRIPTION","**tt** is a terminal-based typing test written in Go. It generates tests from randomly selected words and measures typing speed and accuracy. By default, it uses the top 1000 English words.\n\nWhen given a file path, tt uses that file as input, treating each paragraph as a separate test segment. This allows practicing with custom text or code samples.\n\nAfter each test, tt displays statistics including characters per minute (CPM), words per minute (WPM), accuracy percentage, and specific mistakes made. Results can be logged to CSV for tracking progress over time.\n\nCustom themes and word lists can be placed in **~/.tt/themes** and **~/.tt/words** directories. The tool is designed to be scriptable and integrate with other Unix tools."],["KEY BINDINGS","**Escape**\n> Restart current test\n\n**Ctrl+C**\n> Exit\n\n**Right Arrow**\n> Next test\n\n**Left Arrow**\n> Previous test"],["CAVEATS","tt requires a terminal with proper Unicode support for accurate character display. Very narrow terminals may affect word wrapping and display. The default word list focuses on common English words; programmers may want custom lists with symbols."],["HISTORY","**tt** was created by **lemnos** and is hosted on GitHub. Written in **Go**, it was designed as a minimal, scriptable typing test for terminal users who prefer command-line tools over web-based alternatives like monkeytype or typeracer."],["INSTALL","```brew: brew install tt```\n\n```nix: nix profile install nixpkgs#tt```"],["SEE ALSO","[ttyper](/man/ttyper)(1), [gtypist](/man/gtypist)(1), [typespeed](/man/typespeed)(1)"]],"e":["ttyper","gtypist","typespeed"],"x":"tt"},"uv-add":{"n":"uv-add","t":"Add dependencies to a uv Python project","s":[["TAGLINE","Add dependencies to a uv Python project"],["TLDR","**Add dependency**\n\n```uv add [package]```\n\n**Add with version constraint**\n\n```uv add [package]>=[version]```\n\n**Add dev dependency**\n\n```uv add --dev [package]```\n\n**Add optional dependency**\n\n```uv add --optional [group] [package]```"],["SYNOPSIS","**uv** **add** [_options_] _packages_..."],["PARAMETERS","**--dev**\n> Add as development dependency.\n\n**--optional** _group_\n> Add to optional group.\n\n**--frozen**\n> Don't update lockfile.\n\n**--no-sync**\n> Don't sync environment."],["DESCRIPTION","**uv add** adds dependencies to a Python project. Updates pyproject.toml and uv.lock. Automatically syncs the virtual environment with new dependencies. Part of uv, an extremely fast Python package manager."],["INSTALL","```dnf: sudo dnf install uv```\n\n```pacman: sudo pacman -S uv```\n\n```apk: sudo apk add uv```\n\n```brew: brew install uv```\n\n```nix: nix profile install nixpkgs#uv```"],["SEE ALSO","[uv](/man/uv)(1), [uv-remove](/man/uv-remove)(1), [uv-sync](/man/uv-sync)(1)"]],"e":["uv","uv-remove","uv-sync"],"x":"uv add [package]"},"vagrant-upload":{"n":"vagrant-upload","t":"Upload files to Vagrant guest machines","s":[["TAGLINE","Upload files to Vagrant guest machines"],["TLDR","**Upload file to VM**\n\n```vagrant upload [source] [destination]```\n\n**Upload to specific VM**\n\n```vagrant upload [source] [destination] [name]```\n\n**Upload with compression**\n\n```vagrant upload --compress [source] [destination]```"],["SYNOPSIS","**vagrant** **upload** [_options_] _source_ [_destination_] [_name_]"],["PARAMETERS","**--compress**\n> Compress before upload.\n\n**--temporary**\n> Upload to temp directory."],["DESCRIPTION","**vagrant upload** copies files from host to guest machine. Supports compression for large files. Destination defaults to home directory if not specified."],["SEE ALSO","[vagrant](/man/vagrant)(1), [vagrant-ssh](/man/vagrant-ssh)(1), [scp](/man/scp)(1)"]],"e":["vagrant","vagrant-ssh","scp"],"x":"vagrant upload [source] [destination]"},"vgrep":{"n":"vgrep","t":"Interactive grep with editor integration","s":[["TAGLINE","Interactive grep with editor integration"],["TLDR","**Search pattern**\n\n```vgrep \"[pattern]\"```\n\n**Search in directory**\n\n```vgrep \"[pattern]\" [path]```\n\n**Open results interactively**\n\n```vgrep -s \"[pattern]\"```\n\n**Show specific result**\n\n```vgrep --show [index]```\n\n**No git ignore**\n\n```vgrep --no-git \"[pattern]\"```"],["SYNOPSIS","**vgrep** [_-s_] [_--show index_] [_options_] _pattern_ [_path_]"],["PARAMETERS","**-s**\n> Interactive mode.\n\n**--show** _INDEX_\n> Show result by index.\n\n**--no-git**\n> Don't use gitignore.\n\n**-n**\n> Line numbers.\n\n**-i**\n> Case insensitive."],["DESCRIPTION","**vgrep** is a pager for grep results that assigns an index number to each match, allowing you to quickly open specific results in your editor by number. It respects .gitignore rules by default and provides both batch and interactive modes.\n\nIn interactive mode (**-s**), results can be browsed and opened directly. The **--show** flag opens a previously indexed result without re-running the search, making it efficient for navigating through matches in a codebase."],["CAVEATS","Go implementation. Editor config needed. Different from grep."],["HISTORY","**vgrep** was created for quickly searching code and opening results in an editor by index."],["INSTALL","```dnf: sudo dnf install vgrep```\n\n```zypper: sudo zypper install vgrep```\n\n```brew: brew install vgrep```\n\n```nix: nix profile install nixpkgs#vgrep```"],["SEE ALSO","[grep](/man/grep)(1), [rg](/man/rg)(1), [ag](/man/ag)(1)"]],"e":["grep","rg","ag"],"x":"vgrep \"[pattern]\""},"vzdump":{"n":"vzdump","t":"Proxmox VE virtual machine backup tool","s":[["TAGLINE","Proxmox VE virtual machine backup tool"],["TLDR","**Backup** a single VM\n\n```vzdump [vm_id]```\n\nBackup **multiple** VMs\n\n```vzdump [101] [102] [103]```\n\nBackup with a specific **mode** (snapshot, stop, or suspend)\n\n```vzdump [vm_id] --mode [snapshot|stop|suspend]```\n\nBackup **all** VMs with email notification\n\n```vzdump --all --mode snapshot --mailto [root@example.com]```\n\nBackup with **zstd compression** to a specific directory\n\n```vzdump [vm_id] --dumpdir [/mnt/backup] --mode snapshot --compress zstd```\n\nBackup all VMs **excluding** specific ones\n\n```vzdump --all --exclude [101,102]```\n\nBackup with **retention policy**\n\n```vzdump [vm_id] --mode snapshot --prune-backups keep-last=5,keep-daily=7```"],["SYNOPSIS","**vzdump** [_OPTIONS_] _VMID_..."],["PARAMETERS","**--mode** _MODE_\n> Backup mode: snapshot (default, minimal downtime), stop (highest consistency, VM stops), or suspend (compatibility).\n\n**--dumpdir** _DIR_\n> Store resulting backup files in DIR instead of the default location.\n\n**--storage** _STORAGE_ID_\n> Store resulting backup files to the specified Proxmox storage.\n\n**--mailto** _EMAIL_\n> Send notification email to address. Deprecated in favor of notification system.\n\n**--all**\n> Backup all known guest systems on the host.\n\n**--exclude** _VMIDS_\n> Exclude specified VM IDs (assumes --all).\n\n**--pool** _POOL_\n> Backup all guest systems in the specified pool.\n\n**--compress** _TYPE_\n> Compression type: 0 (none, default), gzip, lzo, or zstd.\n\n**--zstd** _N_\n> Zstandard thread count (default: 1). 0 uses half of available cores.\n\n**--pigz** _N_\n> Use parallel gzip. N=1 uses half of available cores; N>1 uses N threads.\n\n**--prune-backups** _RETENTION_\n> Retention policy (e.g., keep-last=N,keep-daily=N,keep-weekly=N,keep-monthly=N,keep-yearly=N). Default: keep-all=1.\n\n**--maxfiles** _N_\n> Deprecated. Maximum number of backup files per VM. Use --prune-backups instead.\n\n**--protected**\n> Mark backup as protected to prevent automatic removal.\n\n**--bwlimit** _KiB/s_\n> Limit I/O bandwidth in KiB/s (default: 0, unlimited).\n\n**--ionice** _N_\n> Set IO priority (0-8) when using the BFQ scheduler (default: 7).\n\n**--tmpdir** _DIR_\n> Store temporary files in DIR.\n\n**--lockwait** _MIN_\n> Maximum minutes to wait for the global lock (default: 180).\n\n**--stopwait** _MIN_\n> Maximum minutes to wait until the guest system stops (default: 10).\n\n**--script** _FILE_\n> Execute hook script during backup.\n\n**--notes-template** _TEMPLATE_\n> Template for backup notes. Variables: {{vmid}}, {{guestname}}, {{node}}, {{cluster}}.\n\n**--quiet**\n> Suppress verbose output.\n\n**--stdexcludes**\n> Exclude temporary files and logs (default: enabled)."],["DESCRIPTION","**vzdump** is a backup utility for Proxmox VE virtual machines (KVM) and containers (LXC). It creates consistent backups that can be restored later using **qmrestore** (VMs) or **pct restore** (containers).\n\nThree backup modes are available: **snapshot** (default, minimal downtime using LVM/ZFS/QEMU snapshots), **stop** (highest consistency, stops the guest during backup), and **suspend** (legacy compatibility mode). Backups are stored in /var/lib/vz/dump/ by default, or to a configured Proxmox storage."],["CAVEATS","Proxmox VE specific. Large VMs require significant backup storage. Snapshot mode requires LVM, ZFS, or QEMU snapshot support. The --maxfiles option is deprecated in favor of --prune-backups. Global configuration can be set in /etc/vzdump.conf."],["SEE ALSO","[qmrestore](/man/qmrestore)(1), [pct](/man/pct)(1), [qm](/man/qm)(1), [pvesm](/man/pvesm)(1)"]],"e":["qmrestore","pct","qm","pvesm"],"x":"vzdump [vm_id]"},"yard":{"n":"yard","t":"Ruby documentation generator","s":[["TAGLINE","Ruby documentation generator"],["TLDR","**Generate docs**\n\n```yard doc```\n\n**Start server**\n\n```yard server```\n\n**List undocumented**\n\n```yard stats --list-undoc```\n\n**Generate for specific files**\n\n```yard doc [lib/**/*.rb]```\n\n**Generate for gems**\n\n```yard gems```\n\n**Show documentation for a class or method**\n\n```yard ri [ClassName]```"],["SYNOPSIS","**yard** _command_ [_options_]"],["PARAMETERS","**doc**\n> Generate docs.\n\n**server**\n> Start server.\n\n**stats**\n> Show statistics.\n\n**gems**\n> Document gems.\n\n**ri**\n> Show documentation.\n\n**diff**\n> Show changes in documentation.\n\n**--list-undoc**\n> List undocumented objects."],["DESCRIPTION","**yard** (Yay! A Ruby Documentation Tool) generates API documentation from Ruby source code. It parses specially formatted comments above classes, modules, and methods to produce browsable HTML documentation with cross-references and search.\n\nYARD uses its own tag syntax (such as **@param**, **@return**, **@example**) within comments to describe method signatures, return types, and usage examples. It also supports Markdown and other markup formats for free-form documentation within comments.\n\nThe **server** subcommand starts a local web server for live documentation browsing during development. The **stats** subcommand reports documentation coverage, and **--list-undoc** identifies undocumented code. Templates control the HTML output layout and can be customized or replaced entirely."],["CAVEATS","Ruby-specific. YARD syntax in comments. Gem required."],["HISTORY","**YARD** (Yay! A Ruby Documentation Tool) was created as a modern replacement for RDoc."],["SEE ALSO","[rdoc](/man/rdoc)(1), [ri](/man/ri)(1), [ruby](/man/ruby)(1), [gem](/man/gem)(1), [bundle](/man/bundle)(1)"]],"e":["rdoc","ri","ruby","gem","bundle"],"x":"yard doc"},"ybacklight":{"n":"ybacklight","t":"Screen backlight brightness control","s":[["TAGLINE","Screen backlight brightness control"],["TLDR","**Get brightness**\n\n```ybacklight -get```\n\n**Set brightness**\n\n```ybacklight -set [50]```\n\n**Increase brightness**\n\n```ybacklight -inc [10]```\n\n**Decrease brightness**\n\n```ybacklight -dec [10]```"],["SYNOPSIS","**ybacklight** [_-get_] [_-set value_] [_-inc value_] [_-dec value_]"],["PARAMETERS","**-get**\n> Show current level.\n\n**-set** _VAL_\n> Set to percentage.\n\n**-inc** _VAL_\n> Increase by amount.\n\n**-dec** _VAL_\n> Decrease by amount."],["DESCRIPTION","**ybacklight** controls the display backlight brightness from the command line. It provides a simple interface for getting, setting, and adjusting the screen brightness level as a percentage value.\n\nBrightness can be set to an absolute percentage with **-set**, or adjusted relative to the current level with **-inc** and **-dec**. The **-get** option queries the current brightness level.\n\nThe tool serves as an alternative to **xbacklight**, providing similar functionality for systems where xbacklight does not work correctly due to driver or hardware differences."],["CAVEATS","Hardware dependent. May need permissions. Backlight control."],["HISTORY","**ybacklight** is a backlight control utility, similar to xbacklight, for adjusting display brightness."],["INSTALL","```aur: yay -S ybacklight```"],["SEE ALSO","[xbacklight](/man/xbacklight)(1), [brightnessctl](/man/brightnessctl)(1), [light](/man/light)(1)"]],"e":["xbacklight","brightnessctl","light"],"x":"ybacklight -get"}});
