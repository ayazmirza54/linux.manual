window.LCL_BUCKET(41,{"authselect":{"n":"authselect","t":"Configure system authentication sources via profiles","s":[["TAGLINE","Configure system authentication sources via profiles"],["TLDR","**List available profiles**\n\n```authselect list```\n\n**Show current configuration**\n\n```authselect current```\n\n**Select the SSSD profile** for LDAP/FreeIPA authentication\n\n```sudo authselect select sssd --force```\n\n**Select SSSD profile** with fingerprint authentication\n\n```sudo authselect select sssd with-fingerprint --force```\n\n**Select the Winbind profile** for Active Directory\n\n```sudo authselect select winbind --force```\n\n**Enable a feature** on current profile\n\n```sudo authselect enable-feature with-mkhomedir```\n\n**Backup current configuration**\n\n```sudo authselect backup [backup-name]```\n\n**Restore from backup**\n\n```sudo authselect backup-restore [backup-name]```"],["SYNOPSIS","**authselect** _command_ [_options_]"],["PARAMETERS","**list**\n> List available profiles\n\n**show** _profile_\n> Display information about a profile\n\n**current**\n> Show currently selected profile and features\n\n**select** _profile_ [_features_]\n> Activate a profile with optional features\n\n**enable-feature** _feature_\n> Enable a feature on the current profile\n\n**disable-feature** _feature_\n> Disable a feature on the current profile\n\n**backup** [_name_]\n> Backup current system configuration\n\n**backup-restore** _name_\n> Restore configuration from backup\n\n**backup-list**\n> List available backups\n\n**opt-out**\n> Remove authselect management of configuration\n\n**--force**\n> Overwrite existing non-authselect configuration\n\n**-b**, **--backup**\n> Create backup before making changes\n\n**-q**, **--quiet**\n> Suppress output messages"],["PROFILES","**sssd**\n> System Security Services Daemon for LDAP, FreeIPA, Active Directory\n\n**winbind**\n> Samba Winbind for direct Active Directory integration\n\n**nis**\n> Legacy Network Information Service compatibility\n\n**minimal**\n> Local users and groups only (system files)"],["COMMON FEATURES","**with-mkhomedir**\n> Automatically create home directories on first login\n\n**with-fingerprint**\n> Enable fingerprint authentication\n\n**with-smartcard**\n> Enable smart card authentication\n\n**with-faillock**\n> Enable account lockout after failed attempts\n\n**with-sudo**\n> Enable SSSD as source for sudo rules"],["DESCRIPTION","**authselect** configures system authentication sources by managing PAM stack and nsswitch.conf files through predefined profiles. It replaced **authconfig** starting with Fedora 28 and RHEL 8.\n\nProfiles define how users are authenticated and where identity information is retrieved. The **sssd** profile is most common for enterprise environments using LDAP, FreeIPA, or Active Directory. The **winbind** profile provides an alternative for Active Directory integration.\n\nAuthselect only configures PAM and nsswitch; it does not configure the underlying daemons (SSSD, Winbind). Use tools like **realm join** or **ipa-client-install** to set up domain membership, which automatically configure authselect.\n\nCustom profiles can be created by copying and modifying existing profiles in **/etc/authselect/custom/**."],["CONFIGURATION","**/etc/authselect/authselect.conf**\n> Records the currently active profile and enabled features.\n\n**/etc/authselect/custom/**\n> Directory for user-created custom profiles. Copy and modify existing profiles here."],["CAVEATS","Using **--force** is required when switching from manually configured systems. Do not modify authselect profiles configured by **ipa-client-install** or **realm join**. Changes to nsswitch.conf or PAM files outside authselect will be overwritten on profile changes."],["HISTORY","**authselect** was developed by Red Hat and introduced in **Fedora 28** (2018) as a replacement for authconfig. It was designed to provide a simpler, more maintainable approach to authentication configuration using predefined profiles rather than individual option flags. By **Fedora 35**, authconfig was fully removed, making authselect the standard tool."],["INSTALL","```apt: sudo apt install authselect```\n\n```dnf: sudo dnf install authselect```\n\n```aur: yay -S authselect```"],["SEE ALSO","[pam](/man/pam)(8)"]],"e":["pam"],"x":"authselect list"},"autocli":{"n":"autocli","t":"Fetch structured data from websites and local tools via CLI","s":[["TAGLINE","Fetch structured data from websites and local tools via CLI"],["TLDR","**Run** a site command (examples evolve with catalogs)\n\n```autocli [site] [subcommand]```\n\n**Install** via install script\n\n```curl -fsSL https://raw.githubusercontent.com/nashsu/autocli/main/scripts/install.sh | sh```\n\n**Help** / list commands\n\n```autocli --help```"],["SYNOPSIS","**autocli** *command* [*args*]"],["DESCRIPTION","**autocli** is a Rust rewrite of OpenCLI-style “public commands” for fetching information from many websites (social, video, news, etc.) and integrating with local tools (**gh**, **docker**, **kubectl**) from one binary. Emphasizes low memory use and a static binary install.\n\nCommand catalogs grow over time—use **autocli --help** and upstream docs for the current surface."],["PARAMETERS","*command*\n\n> Site or tool integration name.\n\nGlobal flags cover JSON output, browser-backed commands, and auth where needed."],["CAVEATS","Scraping sites can break when HTML changes and may violate third-party ToS. Browser-driven modes are heavier. Review privacy when fetching personal data."],["SEE ALSO","[curl](/man/curl)(1), [gh](/man/gh)(1), [http](/man/http)(1)"],["RESOURCES","```[Source code](https://github.com/nashsu/AutoCLI)```"]],"e":["curl","gh","http"],"x":"autocli [site] [subcommand]"},"aws-cognito-identity":{"n":"aws-cognito-identity","t":"Manage federated identity pools for AWS resource access.","s":[["TAGLINE","Manage federated identity pools for AWS resource access."],["TLDR","**List identity pools** in the current region\n\n```aws cognito-identity list-identity-pools --max-results [20]```\n\n**Create a new identity pool**\n\n```aws cognito-identity create-identity-pool --identity-pool-name [pool_name] --allow-unauthenticated-identities```\n\n**Describe an identity pool** by ID\n\n```aws cognito-identity describe-identity-pool --identity-pool-id [region:pool-id]```\n\n**Get an identity ID** for unauthenticated access\n\n```aws cognito-identity get-id --identity-pool-id [region:pool-id]```\n\n**Get temporary AWS credentials** for an identity\n\n```aws cognito-identity get-credentials-for-identity --identity-id [region:identity-id]```\n\n**Delete an identity pool**\n\n```aws cognito-identity delete-identity-pool --identity-pool-id [region:pool-id]```\n\n**Link a login provider** to an existing identity\n\n```aws cognito-identity get-id --identity-pool-id [region:pool-id] --logins [provider]=[token]```"],["SYNOPSIS","**aws cognito-identity** _subcommand_ [_options_]"],["DESCRIPTION","**aws cognito-identity** is a subcommand of the AWS CLI that manages Amazon Cognito Identity Pools (Federated Identities). It provides temporary AWS credentials to users authenticated through identity providers like Amazon, Facebook, Google, SAML, or Cognito User Pools.\n\nIdentity pools map user identities to IAM roles, enabling secure access to AWS services from mobile apps and web applications. Each identity pool can support both authenticated and unauthenticated (guest) access with different IAM permissions.\n\nThe service issues unique identity IDs and vends temporary AWS credentials through the STS service, allowing fine-grained access control without embedding long-term credentials in applications."],["PARAMETERS","**list-identity-pools**\n> List all identity pools for the account.\n\n**create-identity-pool**\n> Create a new identity pool with specified settings.\n\n**describe-identity-pool**\n> Get details about an identity pool.\n\n**update-identity-pool**\n> Modify identity pool configuration.\n\n**delete-identity-pool**\n> Remove an identity pool.\n\n**delete-identities**\n> Delete previously merged or associated identities.\n\n**describe-identity**\n> Get details about a particular identity, including its external IDs.\n\n**get-id**\n> Get or create an identity ID for a user.\n\n**get-credentials-for-identity**\n> Get temporary AWS credentials for an identity.\n\n**get-identity-pool-roles**\n> Get the roles configured for an identity pool.\n\n**set-identity-pool-roles**\n> Set the roles for an identity pool.\n\n**get-open-id-token**\n> Get an OpenID token for an identity.\n\n**get-open-id-token-for-developer-identity**\n> Get token for developer-authenticated identity.\n\n**list-identities**\n> List identities in a specific identity pool.\n\n**lookup-developer-identity**\n> Retrieve the identity ID associated with a developer identity.\n\n**tag-resource**\n> Assign tags to an identity pool.\n\n**untag-resource**\n> Remove tags from an identity pool.\n\n**--identity-pool-id** _id_\n> The identity pool ID (format: region:uuid).\n\n**--identity-pool-name** _name_\n> Name for the identity pool.\n\n**--allow-unauthenticated-identities**\n> Enable guest access without authentication.\n\n**--no-allow-unauthenticated-identities**\n> Disable guest access.\n\n**--logins** _map_\n> Map of provider names to tokens for authentication.\n\n**--max-results** _number_\n> Maximum number of results to return."],["CAVEATS","Identity pool IDs include the region prefix (e.g., us-east-1:xxxxxxxx). Credentials expire after 1 hour by default. Requires IAM roles to be configured for both authenticated and unauthenticated access. Do not confuse Cognito Identity Pools with Cognito User Pools, which handle user registration and sign-in directly."],["HISTORY","Amazon Cognito Identity was announced at **AWS re:Invent 2014** and launched in **July 2014** as part of the mobile services suite. It was designed to simplify secure access to AWS resources from mobile applications. The service evolved to support additional identity providers, SAML federation, and enhanced flow for developer-authenticated identities."],["INSTALL","```dnf: sudo dnf install aws```\n\n```pacman: sudo pacman -S aws-cli```\n\n```apk: sudo apk add aws-cli```\n\n```zypper: sudo zypper install aws-cli```"],["SEE ALSO","[aws](/man/aws)(1), [aws-cognito-idp](/man/aws-cognito-idp)(1), [aws-sts](/man/aws-sts)(1), [aws-iam](/man/aws-iam)(1)"]],"e":["aws","aws-cognito-idp","aws-sts","aws-iam"],"x":"aws cognito-identity list-identity-pools --max-results [20]"},"aws-glue":{"n":"aws-glue","t":"Manage serverless ETL jobs and data catalog","s":[["TAGLINE","Manage serverless ETL jobs and data catalog"],["TLDR","**Create a crawler** to discover data schema\n\n```aws glue create-crawler --name [my-crawler] --role [arn:aws:iam::account:role/glue-role] --database-name [my-database] --targets S3Targets=[{Path=s3://my-bucket/data/}]```\n\n**Start a crawler**\n\n```aws glue start-crawler --name [my-crawler]```\n\n**List all databases** in the Data Catalog\n\n```aws glue get-databases```\n\n**List tables** in a database\n\n```aws glue get-tables --database-name [my-database]```\n\n**Get table schema** from the catalog\n\n```aws glue get-table --database-name [my-database] --name [my-table]```\n\n**Create and start a Glue job**\n\n```aws glue create-job --name [my-job] --role [arn:aws:iam::account:role/glue-role] --command Name=glueetl,ScriptLocation=s3://my-bucket/scripts/job.py```\n\n**Start a job run** with arguments\n\n```aws glue start-job-run --job-name [my-job] --arguments '{\"--input_path\":\"s3://bucket/input\",\"--output_path\":\"s3://bucket/output\"}'```\n\n**Get job run status**\n\n```aws glue get-job-run --job-name [my-job] --run-id [jr_abc123]```"],["SYNOPSIS","**aws glue** _command_ [_options_]"],["DESCRIPTION","**aws glue** is the AWS CLI interface for AWS Glue, a serverless data integration service for ETL (extract, transform, load) workloads. Glue discovers, prepares, and combines data for analytics, machine learning, and application development.\n\nKey components include the **Data Catalog** (central metadata repository), **Crawlers** (automatic schema discovery), **Jobs** (ETL scripts in Python or Scala), and **Triggers** (job orchestration). Glue integrates with S3, Redshift, RDS, and other data stores."],["COMMANDS","**create-database**\n> Create a database in the Data Catalog\n\n**create-table**\n> Register a table definition in the catalog\n\n**create-crawler**\n> Create a crawler for schema discovery\n\n**start-crawler**\n> Run a crawler to populate the catalog\n\n**get-databases**\n> List databases in the Data Catalog\n\n**get-tables**\n> List tables in a database\n\n**get-table**\n> Get schema details for a table\n\n**create-job**\n> Create an ETL job\n\n**start-job-run**\n> Execute a job\n\n**get-job-run**\n> Check job run status\n\n**create-trigger**\n> Create a job trigger\n\n**get-crawlers**\n> List all crawlers"],["CAVEATS","Crawlers can take significant time on large datasets. Job cold start adds latency; use job bookmarks for incremental processing. DPU (Data Processing Unit) costs accumulate during job runs. The Data Catalog has limits on tables per database (200,000)."],["HISTORY","AWS Glue launched in **August 2017** as a serverless ETL service. Glue Studio for visual ETL authoring came in **2020**. Data Quality features were added in **2022**, and Glue for Ray (distributed Python) launched in **2023** for data science workloads."],["INSTALL","```dnf: sudo dnf install aws```\n\n```pacman: sudo pacman -S aws-cli```\n\n```apk: sudo apk add aws-cli```\n\n```zypper: sudo zypper install aws-cli```"],["SEE ALSO","[aws](/man/aws)(1), [aws-athena](/man/aws-athena)(1), [aws-s3](/man/aws-s3)(1), [aws-redshift](/man/aws-redshift)(1)"],["RESOURCES","```[Source code](https://github.com/aws/aws-cli)```\n\n```[Homepage](https://aws.amazon.com/cli/)```\n\n```[Documentation](https://docs.aws.amazon.com/cli/latest/reference/glue/index.html)```"]],"e":["aws","aws-athena","aws-s3","aws-redshift"],"x":"aws glue create-crawler --name [my-crawler] --role [arn:aws:iam::account:role/glue-role] --database-name [my-database] --targets S3Targets=[{Path=s3://my-bucket/data/}]"},"basilk":{"n":"basilk","t":"Terminal kanban board for task management","s":[["TAGLINE","Terminal kanban board for task management"],["TLDR","**Launch** the kanban board\n\n```basilk```\n\n**Show the version** number\n\n```basilk --version```"],["SYNOPSIS","**basilk** [**--version**]"],["PARAMETERS","**--version**\n> Display the version number and exit"],["DESCRIPTION","**basilk** is a terminal user interface (TUI) application for managing tasks with minimal kanban logic. It organizes work into projects, each containing tasks that move through three status columns: **Up Next**, **On Going**, and **Done**. Tasks can also be assigned a priority level from 1 (highest) to 3 (lowest), or 0 for no priority. When a task is marked as Done, its priority is automatically reset.\n\nWritten in Rust using the ratatui library, basilk provides vim-style navigation throughout. In the projects view, use **j**/**k** or arrow keys to navigate, **n** to create a project, **r** to rename, **d** to delete, and **Enter** to open a project. In the tasks view, **n** creates a task, **r** renames, **d** deletes, **Enter** changes the task status, and **p** changes priority. Press **Esc** or **h** to return to the projects view, and **q** to quit.\n\nProjects display a color-coded completion indicator based on the ratio of done tasks: dark gray at 0%, magenta up to 50%, yellow up to 99%, and green at 100%. All data is stored in a versioned JSON file with automatic schema migration between versions."],["CONFIGURATION","Configuration is stored as a TOML file named **config.toml** in the application data directory. If the file does not exist, it is created automatically with default values.\n\n**[ui]**\n> **show_help** = true -- Toggle the help text displayed in the interface\n\nData is stored in platform-specific directories: **~/.config/basilk** on Linux, **~/Library/Application Support/basilk** on macOS, and **AppData\\Roaming\\basilk** on Windows."],["CAVEATS","The project is in beta and may contain bugs. The CLI accepts only the **--version** flag; all other interaction happens within the TUI. There is no import or export functionality beyond the internal JSON storage."],["HISTORY","**basilk** was created by **GabAlpha** as an open-source Rust project, dual-licensed under MIT and Apache 2.0. Version 0.2.1 was released in March 2025. It is available via cargo, Homebrew, AUR, and x-cmd."],["INSTALL","```aur: yay -S basilk```\n\n```nix: nix profile install nixpkgs#basilk```"],["SEE ALSO","[taskwarrior](/man/taskwarrior)(1), [todo.sh](/man/todo.sh)(1), [jira](/man/jira)(1), [task](/man/task)(1)"]],"e":["taskwarrior","todo.sh","jira","task"],"x":"basilk"},"bear":{"n":"bear","t":"Generate compilation database for clang tooling","s":[["TAGLINE","Generate compilation database for clang tooling"],["TLDR","**Generate** compilation database\n\n```bear -- make```\n\n**Append** to existing database\n\n```bear --append -- make clean all```\n\n**Custom output** file\n\n```bear --output [compile_commands.json] -- ninja```"],["SYNOPSIS","**bear** [_options_] **--** _build-command_"],["DESCRIPTION","**bear** (Build EAR) generates a compilation database (compile_commands.json) by intercepting compiler calls during a build. This database enables tools like clangd, clang-tidy, and IDEs to understand the project structure without custom configuration.\n\nThe tool works with any build system by recording actual compiler invocations. From version 3 onward, the build command must be separated from bear's own options by a `--`; older 2.4.x packages omit it."],["PARAMETERS","**-o** _file_, **--output** _file_\n> Output file (default: compile_commands.json)\n\n**-a**, **--append**\n> Append results to an existing database instead of overwriting it\n\n**-c** _file_, **--config** _file_\n> Read settings (output formatting, compiler/source filters) from a configuration file\n\n**-h**, **--help**\n> Print help information\n\n**-V**, **--version**\n> Print version information"],["WORKFLOW","```bash"],["With Make","bear -- make"],["With CMake/Ninja","bear -- ninja"],["Clean build","bear -- make clean all"],["Append incremental build","bear --append -- make module\n```"],["FEATURES","- Build system agnostic\n- Intercepts compiler calls\n- Supports parallel builds\n- Works with make, ninja, etc.\n- No build system modification needed"],["COMPILATION DATABASE","Generated compile_commands.json format:\n```json\n[\n  {\n    \"directory\": \"/path/to/project\",\n    \"command\": \"gcc -c file.c -o file.o\",\n    \"file\": \"file.c\"\n  }\n]\n```"],["CAVEATS","Requires running full build. May not capture all compilation units if cached. Some build systems have native support (CMake: -DCMAKE_EXPORT_COMPILE_COMMANDS=ON). Different interception methods on different platforms."],["HISTORY","**bear** was created by László Nagy (rizsotto) around **2012** to generate compilation databases for C/C++ projects using any build system. Version 3 introduced the `--` separator syntax, and version 4 rewrote the tool in Rust."],["INSTALL","```apt: sudo apt install bear```\n\n```dnf: sudo dnf install bear```\n\n```pacman: sudo pacman -S bear```\n\n```apk: sudo apk add bear```\n\n```brew: brew install bear```\n\n```nix: nix profile install nixpkgs#bear```"],["SEE ALSO","[compiledb](/man/compiledb)(1), [cmake](/man/cmake)(1), [clangd](/man/clangd)(1), [make](/man/make)(1), [ninja](/man/ninja)(1), [clang](/man/clang)(1)"],["RESOURCES","```[Source code](https://github.com/rizsotto/Bear)```\n\n```[Documentation](https://github.com/rizsotto/Bear/blob/master/README.md)```"]],"e":["compiledb","cmake","clangd","make","ninja","clang"],"x":"bear -- make"},"bpython":{"n":"bpython","t":"Enhanced interactive Python interpreter","s":[["TAGLINE","Enhanced interactive Python interpreter"],["TLDR","**Start interactive Python session**\n\n```bpython```\n\n**Execute a script then enter interactive mode**\n\n```bpython -i [script.py]```\n\n**Execute a command**\n\n```bpython -c \"[print('hello')]\"```\n\n**Start without banner**\n\n```bpython -q```"],["SYNOPSIS","**bpython** [_options_] [_file_]"],["DESCRIPTION","**bpython** is an enhanced Python REPL (Read-Eval-Print-Loop) with features like syntax highlighting, auto-completion, automatic indentation, and inline documentation. It provides a user-friendly interface for interactive Python development.\n\nThe tool improves the standard Python interactive interpreter experience with modern IDE-like features."],["PARAMETERS","**-i**\n> Run file then interactive mode\n\n**-q**\n> Quiet mode (no banner)\n\n**-c** _command_\n> Execute command\n\n**--config** _file_\n> Configuration file"],["FEATURES","- Syntax highlighting\n- Auto-completion as you type\n- Expected parameter list\n- Inline documentation (F1)\n- Rewind/replay execution (Ctrl+R)\n- Save session to file (F2)\n- Undo (Ctrl+C)\n- Pastebin integration (F8)\n- vi/emacs key bindings"],["KEYBINDINGS","- **F1** - Show documentation\n- **F2** - Save to file\n- **F8** - Send to pastebin\n- **Ctrl+R** - Rewind/replay\n- **Ctrl+C** - Undo last input\n- **Tab** - Auto-completion\n- **Ctrl+D** - Exit"],["CONFIGURATION","**~/.config/bpython/config**\n> Main configuration file for syntax highlighting, colors, key bindings, and behavior settings."],["CAVEATS","Not a drop-in replacement for complex scripts. Some extensions may not work. Slower startup than standard Python. Replay feature has limitations. Not suitable for production use."],["HISTORY","**bpython** was created by Bob Farrell and others around **2008** to provide a more user-friendly Python REPL experience."],["INSTALL","```pacman: sudo pacman -S bpython```\n\n```brew: brew install bpython```"],["SEE ALSO","[python](/man/python)(1), [ipython](/man/ipython)(1), [ptpython](/man/ptpython)(1)"]],"e":["python","ipython","ptpython"],"x":"bpython"},"cosign":{"n":"cosign","t":"Container image signing and verification tool","s":[["TAGLINE","Container image signing and verification tool"],["TLDR","**Sign container image (keyless)**\n\n```cosign sign [registry/image@sha256:digest]```\n\n**Sign with local key**\n\n```cosign sign --key [cosign.key] [registry/image@sha256:digest]```\n\n**Verify image signature (keyless)**\n\n```cosign verify --certificate-identity=[email] --certificate-oidc-issuer=[issuer] [image]```\n\n**Verify with public key**\n\n```cosign verify --key [cosign.pub] [registry/image@sha256:digest]```\n\n**Generate key pair**\n\n```cosign generate-key-pair```\n\n**Sign with annotation**\n\n```cosign sign -a [key=value] [registry/image@sha256:digest]```\n\n**Attach SBOM to image**\n\n```cosign attach sbom --sbom [sbom.json] [registry/image@sha256:digest]```"],["SYNOPSIS","**cosign** _command_ [_options_] [_arguments_]"],["DESCRIPTION","**cosign** is Sigstore's tool for signing and verifying container images and software artifacts. It supports keyless signing using OIDC identity or traditional key-based signing, providing cryptographic guarantees about image authenticity and integrity.\n\nThe tool integrates with container registries to attach signatures and attestations as OCI artifacts. Keyless signing leverages short-lived certificates tied to identity providers like GitHub, Google, or Microsoft, eliminating the need to manage long-term signing keys. Signatures are recorded in Sigstore's transparency log (Rekor) for public verification and auditability.\n\ncosign also handles SBOMs (Software Bill of Materials), attestations, and other metadata, enabling comprehensive supply chain security. It works with various key management systems including cloud providers' KMS services and hardware security modules."],["COMMANDS","**sign**\n> Sign container image\n\n**verify**\n> Verify image signature\n\n**generate-key-pair**\n> Generate signing key pair\n\n**attach**\n> Attach artifacts (SBOM, attestation) to image\n\n**download**\n> Download signatures/artifacts\n\n**triangulate**\n> Find signature location for image\n\n**copy**\n> Copy signatures between images\n\n**clean**\n> Remove signatures from image"],["PARAMETERS","**--key** _path|uri_\n> Key for signing/verification\n\n**--certificate** _path_\n> Certificate for signing\n\n**--certificate-chain** _path_\n> Certificate chain\n\n**--certificate-identity** _identity_\n> Expected identity in certificate\n\n**--certificate-oidc-issuer** _issuer_\n> Expected OIDC issuer\n\n**-a** _key=value_\n> Add annotation to signature"],["KEYLESS SIGNING","Uses OIDC identity (GitHub, Google, etc.) with short-lived keys. Signatures recorded in transparency log."],["KMS SUPPORT","Supports AWS KMS, GCP KMS, Azure Key Vault, HashiCorp Vault via URI format."],["CAVEATS","Always sign by digest (@sha256:...) not tag. Multiple signatures can be attached to one image. Keyless requires internet for OIDC flow."],["INSTALL","```apt: sudo apt install cosign```\n\n```pacman: sudo pacman -S cosign```\n\n```apk: sudo apk add cosign```\n\n```zypper: sudo zypper install cosign```\n\n```brew: brew install cosign```\n\n```nix: nix profile install nixpkgs#cosign```"],["SEE ALSO","[docker](/man/docker)(1), [podman](/man/podman)(1)"]],"e":["docker","podman"],"x":"cosign sign [registry/image@sha256:digest]"},"ctop":{"n":"ctop","t":"Real-time container metrics monitor","s":[["TAGLINE","Real-time container metrics monitor"],["TLDR","Show only **active** containers\n\n```ctop -a```\n\n**Reverse** container sort order\n\n```ctop -r```\n\n**Invert** the default colors\n\n```ctop -i```\n\nDisplay **help**\n\n```ctop -h```"],["SYNOPSIS","**ctop** [_options_]"],["DESCRIPTION","**ctop** provides a top-like interface for monitoring container metrics in real-time. It displays CPU usage, memory consumption, network I/O, and block I/O statistics for all containers on the system.\n\nThe tool works with Docker, containerd, and other container runtimes by connecting to their respective sockets. The interface is fully interactive, allowing users to sort containers by different metrics, filter active vs all containers, and drill down into individual container logs and stats.\n\nctop is particularly useful for DevOps workflows, debugging performance issues, and monitoring containerized applications without needing to remember complex docker stats commands. It provides a consolidated view across all containers similar to how htop works for system processes."],["PARAMETERS","**-a**\n> Show only active (running) containers\n\n**-r**\n> Reverse sort order\n\n**-i**\n> Invert default colors\n\n**-h**\n> Display help\n\n**-s** _field_\n> Select initial sort field"],["KEYBINDINGS","**q**\n> Quit\n\n**s**\n> Select sort field\n\n**r**\n> Reverse sort order\n\n**Enter**\n> View container details"],["CAVEATS","Requires access to the container runtime socket (usually /var/run/docker.sock). May need to run with sudo or as a user in the docker group. Performance metrics depend on the container runtime's capabilities."],["HISTORY","**ctop** was created by **bcicen** and released as an open-source project in **2016**. It quickly gained popularity in the container community as a user-friendly alternative to docker stats, providing a more intuitive interface for container monitoring."],["INSTALL","```pacman: sudo pacman -S ctop```\n\n```apk: sudo apk add ctop```\n\n```brew: brew install ctop```\n\n```nix: nix profile install nixpkgs#ctop```"],["SEE ALSO","[docker](/man/docker)(1), [top](/man/top)(1), [htop](/man/htop)(1)"],["RESOURCES","```[Source code](https://github.com/bcicen/ctop)```"]],"e":["docker","top","htop"],"x":"ctop -a"},"discover":{"n":"discover","t":"hardware identification system for Linux","s":[["TAGLINE","hardware identification system for Linux"],["TLDR","**Run hardware discovery**\n\n```discover```\n\n**Discover specific** device types\n\n```discover [video]```\n\n**List all hardware** classes\n\n```discover --list```\n\n**Show detailed information**\n\n```discover -v [device_type]```\n\n**Format output** as machine-readable data\n\n```discover --format data [network]```"],["SYNOPSIS","**discover** [_options_] [_type_...]"],["PARAMETERS","_TYPE_\n> Device type to discover: video, network, audio, etc.\n\n**--list**\n> List available device types.\n\n**-v**, **--verbose**\n> Verbose output.\n\n**--data-version** _VERSION_\n> Output data format version.\n\n**--format** _FORMAT_\n> Output format (human, data, etc.).\n\n**--help**\n> Display help information."],["DESCRIPTION","**discover** is a hardware identification system for Linux that identifies hardware devices and maps them to kernel modules, X drivers, or other software components. It queries system buses to detect attached hardware.\n\nThe tool is used in installation and configuration systems to automatically detect hardware and load appropriate drivers. It maintains a database mapping device IDs to driver names and configuration data.\n\ndiscover can identify devices across various buses including PCI, USB, and others, providing information needed for automatic hardware configuration."],["CAVEATS","Database may not include newest hardware. Some devices require manufacturer-specific detection. Results depend on kernel support and permissions. May miss devices on non-standard buses."],["HISTORY","discover was developed for the **Progeny** and **Debian** distributions as part of automatic hardware detection and configuration systems. It emerged in the early 2000s when automatic hardware detection became important for user-friendly Linux installations."],["INSTALL","```apt: sudo apt install discover```\n\n```pacman: sudo pacman -S discover```"],["SEE ALSO","[lspci](/man/lspci)(8), [lsusb](/man/lsusb)(8), [hwinfo](/man/hwinfo)(8), [udevadm](/man/udevadm)(8)"]],"e":["lspci","lsusb","hwinfo","udevadm"],"x":"discover"},"doctl-databases-pool":{"n":"doctl-databases-pool","t":"Manage PostgreSQL connection pools on DigitalOcean","s":[["TAGLINE","Manage PostgreSQL connection pools on DigitalOcean"],["TLDR","**List all connection pools** for a database cluster\n\n```doctl databases pool list [cluster_id]```\n\n**Create a connection pool**\n\n```doctl databases pool create [cluster_id] [pool_name] --db [database] --user [user] --size [10] --mode [transaction]```\n\n**Get details of a specific pool**\n\n```doctl databases pool get [cluster_id] [pool_name]```\n\n**Update a connection pool**\n\n```doctl databases pool update [cluster_id] [pool_name] --size [20]```\n\n**Delete a connection pool**\n\n```doctl databases pool delete [cluster_id] [pool_name]```"],["SYNOPSIS","**doctl** **databases** **pool** _command_ [_options_]"],["SUBCOMMANDS","**list**\n> List all connection pools for a database cluster.\n\n**create**\n> Create a connection pool (requires name, database, user, and size).\n\n**get**\n> Get details of a specific connection pool.\n\n**update**\n> Update an existing connection pool's settings.\n\n**delete**\n> Delete a connection pool."],["PARAMETERS","**--db** _string_\n> Target database name for the pool.\n\n**--user** _string_\n> Database username for pool authentication.\n\n**--size** _int_\n> Pool size (number of connections to maintain).\n\n**--mode** _string_\n> Pool mode: transaction (default, fastest), session (preserves session state), or statement.\n\n**--format** _string_\n> Customize output columns."],["DESCRIPTION","**doctl databases pool** manages connection pools for PostgreSQL managed databases on DigitalOcean. Connection pools use PgBouncer to provide connection multiplexing, significantly improving performance for applications with many concurrent clients.\n\nPools act as intermediaries between applications and the database, maintaining a set number of reusable database connections. This reduces overhead from repeatedly opening and closing connections. Pool modes determine connection behavior: **transaction** mode releases connections after each transaction (fastest for stateless queries), **session** mode holds connections for the entire client session, and **statement** mode releases after each individual statement."],["INSTALL","```dnf: sudo dnf install doctl```\n\n```pacman: sudo pacman -S doctl```\n\n```apk: sudo apk add doctl```\n\n```brew: brew install doctl```\n\n```nix: nix profile install nixpkgs#doctl```"],["SEE ALSO","[doctl-databases](/man/doctl-databases)(1), [doctl-databases-db](/man/doctl-databases-db)(1), [doctl-databases-user](/man/doctl-databases-user)(1)"]],"e":["doctl-databases","doctl-databases-db","doctl-databases-user"],"x":"doctl databases pool list [cluster_id]"},"dosbox":{"n":"dosbox","t":"DOS emulator for classic games and applications","s":[["TAGLINE","DOS emulator for classic games and applications"],["TLDR","**Start DOSBox** with a mounted directory\n\n```dosbox -c \"mount c [/path/to/games]\"```\n\n**Run a DOS program directly**\n\n```dosbox [path/to/program.exe]```\n\n**Start DOSBox with a specific configuration**\n\n```dosbox -conf [path/to/dosbox.conf]```\n\n**Start in fullscreen mode**\n\n```dosbox -fullscreen```\n\n**Exit after running a program**\n\n```dosbox [program.exe] -exit```\n\n**Start with specific CPU cycles**\n\n```dosbox -c \"cycles [max]\"```\n\n**Mount a CD-ROM image**\n\n```dosbox -c \"imgmount d [path/to/image.iso] -t cdrom\"```\n\n**Run a batch file at startup**\n\n```dosbox -c \"mount c .\" -c \"c:\" -c \"[autoexec.bat]\"```"],["SYNOPSIS","**dosbox** [_-conf config_] [_-c command_] [_-fullscreen_] [_-exit_] [_program_] [_arguments_]"],["PARAMETERS","**-conf** _file_\n> Load the specified configuration file.\n\n**-c** _command_\n> Execute a DOS command after startup. Can be specified multiple times.\n\n**-fullscreen**\n> Start in fullscreen mode.\n\n**-exit**\n> Exit DOSBox when the program terminates.\n\n**-machine** _type_\n> Emulate a specific machine type (hercules, cga, tandy, pcjr, ega, svga_s3).\n\n**-noconsole**\n> Start without the console window (Windows only).\n\n**-startmapper**\n> Start the keymapper immediately.\n\n**-noautoexec**\n> Skip the [autoexec] section in the config file.\n\n**-scaler** _scaler_\n> Use the specified scaler for graphics output.\n\n**-forcescaler** _scaler_\n> Force the specified scaler even if not optimal.\n\n**-securemode**\n> Run in secure mode, disabling mount and other potentially dangerous commands.\n\n**-userconf**\n> Load user configuration file.\n\n**-version**\n> Print version information.\n\n**-lang** _file_\n> Use the specified language file.\n\n**-printconf**\n> Print the location of the default configuration file.\n\n**-editconf** _program_\n> Open the configuration file with the specified editor.\n\n**-resetconf**\n> Remove the default configuration file.\n\n**-resetmapper**\n> Remove the default mapper file.\n\n**-opencaptures** _program_\n> Open the captures folder with the specified program.\n\n**-socket** _n_\n> Pass a socket number to nullmodem emulation."],["DESCRIPTION","**DOSBox** is an emulator that recreates an IBM PC compatible computer with DOS. It allows running classic DOS games and applications on modern operating systems by emulating the x86 CPU, graphics, sound, and input hardware from that era.\n\nThe emulator provides a command prompt where standard DOS commands work: **dir**, **cd**, **copy**, etc. Host directories are accessed by mounting them as virtual drives. The **mount c /path/to/folder** command makes a folder appear as drive C: inside DOSBox.\n\nConfiguration files control emulation settings including CPU speed (cycles), graphics rendering, sound card emulation (Sound Blaster, AdLib, etc.), and memory allocation. The default configuration file is in **~/.dosbox/** on Linux or the user profile on Windows.\n\nKey mappings can be customized with the built-in keymapper (Ctrl+F1). Common controls include Alt+Enter for fullscreen toggle, Ctrl+F10 to release mouse capture, and Ctrl+F12/F11 to adjust CPU speed. DOSBox can mount disk images (ISO, CUE/BIN) for games requiring CD-ROMs."],["CONFIGURATION","**~/.dosbox/dosbox-*.conf**\n> Primary configuration file controlling CPU, graphics, sound, and autoexec settings.\n\n**/etc/dosbox.conf**\n> System-wide configuration file (Linux)."],["CAVEATS","Not all DOS software is compatible; very old or very demanding programs may not work correctly. CPU cycle settings may need adjustment for proper game speed. Some copy protection schemes may not work. DOSBox is designed for gaming, not running DOS as a primary operating system."],["HISTORY","**DOSBox** was started by Peter Veenstra and Sjoerd van der Berg in **2002** to run old DOS games on modern systems. It became one of the most popular DOS emulators, supporting Windows, Linux, macOS, and other platforms. Active development continued through various versions, with DOSBox Staging and DOSBox-X emerging as enhanced forks with additional features."],["INSTALL","```apt: sudo apt install dosbox```\n\n```dnf: sudo dnf install dosbox-staging```\n\n```pacman: sudo pacman -S dosbox```\n\n```apk: sudo apk add dosbox-staging```\n\n```zypper: sudo zypper install dosbox```\n\n```brew: brew install dosbox-staging```\n\n```nix: nix profile install nixpkgs#dosbox```"],["SEE ALSO","[qemu](/man/qemu)(1), [wine](/man/wine)(1)"]],"e":["qemu","wine"],"x":"dosbox -c \"mount c [/path/to/games]\""},"dub":{"n":"dub","t":"D language package manager and build tool","s":[["TAGLINE","D language package manager and build tool"],["TLDR","**Build project**\n\n```dub build```\n\n**Run project**\n\n```dub run```\n\n**Initialize new project**\n\n```dub init [name]```\n\n**Add dependency**\n\n```dub add [package]```\n\n**Update dependencies**\n\n```dub upgrade```\n\n**Run tests**\n\n```dub test```\n\n**Build documentation**\n\n```dub build --build=docs```\n\n**Fetch dependencies only**\n\n```dub fetch [package]```"],["SYNOPSIS","**dub** _command_ [_options_]"],["SUBCOMMANDS","**build**\n> Build project.\n\n**run**\n> Build and run.\n\n**test**\n> Run unit tests.\n\n**init**\n> Initialize new project.\n\n**add**\n> Add dependency.\n\n**remove**\n> Remove dependency.\n\n**upgrade**\n> Upgrade dependencies.\n\n**fetch**\n> Fetch package.\n\n**clean**\n> Clean build artifacts.\n\n**describe**\n> Show project description.\n\n**search**\n> Search the package registry.\n\n**list**\n> List installed packages."],["COMMON OPTIONS","**--build=**_TYPE_\n> Build type: debug, release, release-debug, unittest, profile, docs.\n\n**--compiler=**_NAME_\n> Use a specific compiler (dmd, ldc2, gdc).\n\n**--arch=**_ARCH_\n> Force a specific target architecture (e.g. x86_64).\n\n**--config=**_NAME_\n> Use a specific build configuration from dub.json.\n\n**-v**, **--verbose**\n> Print verbose output."],["CONFIGURATION","**dub.json** or **dub.sdl**\n> Project configuration file defining dependencies, build settings, and metadata.\n\n**~/.dub/settings.json**\n> User-level DUB settings including custom registry URLs and default paths."],["DESCRIPTION","**dub** is the official package manager and build tool for the D programming language. It handles dependency management, project compilation, testing, and distribution, providing a complete project lifecycle management solution for D developers.\n\nThe tool uses a JSON or SDL (Simple Declarative Language) configuration file (dub.json or dub.sdl) to describe project structure, dependencies, build configurations, and metadata. Dependencies are automatically fetched from the DUB registry (code.dlang.org) and cached locally.\n\ndub supports multiple build configurations (debug, release, unittest), allowing developers to define different compiler flags and dependencies for various scenarios. It can generate project files for IDEs and integrates with D compilers (dmd, ldc, gdc) automatically.\n\nThe package ecosystem revolves around semantic versioning, with dub resolving dependency trees and selecting compatible versions. Projects can specify exact versions, version ranges, or branch references for dependencies. The upgrade command updates dependencies while respecting version constraints."],["INSTALL","```apt: sudo apt install dub```\n\n```dnf: sudo dnf install dub```\n\n```pacman: sudo pacman -S dub```\n\n```apk: sudo apk add dub```\n\n```zypper: sudo zypper install dub```\n\n```brew: brew install dub```\n\n```nix: nix profile install nixpkgs#dub```"],["SEE ALSO","[dmd](/man/dmd)(1), [ldc](/man/ldc)(1)"]],"e":["dmd","ldc"],"x":"dub build"},"dvc-commit":{"n":"dvc-commit","t":"record changes to DVC-tracked files","s":[["TAGLINE","record changes to DVC-tracked files"],["TLDR","**Commit changes to .dvc files**\n\n```dvc commit```\n\n**Commit specific file**\n\n```dvc commit [data.csv.dvc]```\n\n**Commit with message**\n\n```dvc commit -m \"[message]\"```\n\n**Force commit**\n\n```dvc commit -f```"],["SYNOPSIS","**dvc** **commit** [_options_] [_targets_...]"],["PARAMETERS","**-f**, **--force**\n> Force commit even if unchanged.\n\n**-m**, **--message** _msg_\n> Commit message.\n\n**-R**, **--recursive**\n> Commit recursively."],["DESCRIPTION","**dvc commit** updates .dvc metadata files to reflect changes made to tracked data files in the workspace. After modifying a DVC-tracked file, its hash no longer matches the hash stored in the .dvc file. Running dvc commit computes the new hash and updates the .dvc file accordingly.\n\nThis is analogous to git commit but for DVC-tracked data: it records that you intentionally changed the file and want to version this new state. The command moves the new version into the DVC cache and updates the metadata.\n\nUse dvc commit after editing data files, adding content to tracked directories, or replacing tracked files. The updated .dvc file can then be committed to Git, creating a new data version in your project history."],["INSTALL","```brew: brew install dvc```\n\n```nix: nix profile install nixpkgs#dvc```"],["SEE ALSO","[dvc-add](/man/dvc-add)(1)"]],"e":["dvc-add"],"x":"dvc commit"},"emacsclient":{"n":"emacsclient","t":"connect to running Emacs server for fast editing","s":[["TAGLINE","connect to running Emacs server for fast editing"],["TLDR","**Open file** in existing Emacs frame\n\n```emacsclient [file.txt]```\n\n**Open file** in terminal (no window)\n\n```emacsclient -nw [file.txt]```\n\n**Open file** in new frame\n\n```emacsclient -c [file.txt]```\n\n**Open file** without waiting for edit to finish\n\n```emacsclient -n [file.txt]```\n\n**Evaluate Emacs Lisp** expression\n\n```emacsclient -e \"(message \\\"Hello\\\")\"```\n\n**Start alternate editor** if no server\n\n```emacsclient -a \"\" [file.txt]```\n\n**Create new frame** and don't wait\n\n```emacsclient -cn [file.txt]```\n\n**Connect to specific socket**\n\n```emacsclient -s [server_name] [file.txt]```"],["SYNOPSIS","**emacsclient** [_options_] [_files_...]"],["DESCRIPTION","**emacsclient** connects to an existing Emacs server process to open files or evaluate Lisp expressions. This allows quick file editing without starting a new Emacs instance, dramatically reducing startup time.\n\nThe Emacs server must be started first with M-x server-start or by adding (server-start) to the init file. emacsclient then communicates with this server to open files in existing or new frames.\n\nThis workflow is common for using Emacs as an external editor for other programs (git, crontab) or as a quick command-line editor while maintaining a persistent Emacs session."],["PARAMETERS","**-n**, **--no-wait**\n> Return immediately, don't wait for editing.\n\n**-c**, **--create-frame**\n> Create a new graphical frame.\n\n**-nw**, **-t**, **--tty**\n> Open in terminal, not graphical frame.\n\n**-e**, **--eval**\n> Evaluate argument as Emacs Lisp.\n\n**-a** _editor_, **--alternate-editor=** _editor_\n> Use editor if no server (empty string starts daemon).\n\n**-s** _name_, **--socket-name=** _name_\n> Connect to named server socket.\n\n**-f** _file_, **--server-file=** _file_\n> Use server file for TCP connection.\n\n**-q**, **--quiet**\n> Don't display messages.\n\n**--version**\n> Display version information.\n\n**--help**\n> Display help information."],["CONFIGURATION","**~/.emacs.d/server/**\n> Server socket and authentication files for Emacs server connections."],["CAVEATS","Requires Emacs server to be running (or use -a to start daemon). Terminal mode (-nw) requires appropriate terminal. Socket permissions may prevent access from different users. Server file needed for TCP connections."],["HISTORY","**emacsclient** has been part of **GNU Emacs** since the early versions, with the server/client architecture designed by **Richard Stallman** and contributors. The Emacs server mode was introduced to allow external programs to request editing and to reduce the overhead of starting Emacs repeatedly. The feature has evolved to support both Unix sockets and TCP connections."],["INSTALL","```dnf: sudo dnf install emacsclient```\n\n```apk: sudo apk add emacs-gtk3```"],["SEE ALSO","[emacs](/man/emacs)(1), [vim](/man/vim)(1), [nano](/man/nano)(1)"],["RESOURCES","```[Source code](https://cgit.git.savannah.gnu.org/cgit/emacs.git)```\n\n```[Homepage](https://www.gnu.org/software/emacs/)```\n\n```[Documentation](https://www.gnu.org/software/emacs/manual/html_node/emacs/Emacs-Server.html)```"]],"e":["emacs","vim","nano"],"x":"emacsclient [file.txt]"},"fprintd-delete":{"n":"fprintd-delete","t":"remove enrolled fingerprint data","s":[["TAGLINE","remove enrolled fingerprint data"],["TLDR","Remove **all fingerprints** for a specific user\n\n```fprintd-delete [username]```\n\nRemove a **specific fingerprint** for a specific user\n\n```fprintd-delete [username] -f [left-thumb|left-index-finger|right-thumb|...]```\n\nDisplay **help**\n\n```fprintd-delete```"],["SYNOPSIS","**fprintd-delete** [_options_] _username_"],["PARAMETERS","**-f**, **--finger** _FINGER_\n> Specify which finger to delete: left-thumb, left-index-finger, left-middle-finger, left-ring-finger, left-little-finger, right-thumb, right-index-finger, right-middle-finger, right-ring-finger, right-little-finger"],["DESCRIPTION","**fprintd-delete** removes enrolled fingerprint data from the fprintd database. Without the **--finger** option, all fingerprints for the specified user are removed.\n\nThis command communicates with the fprintd daemon via D-Bus to perform the deletion. Fingerprint data is stored in **/var/lib/fprint** and requires appropriate permissions to modify."],["CAVEATS","Deleting fingerprints cannot be undone. Users must re-enroll their fingerprints using **fprintd-enroll** if needed. Root privileges may be required to delete fingerprints for other users."],["INSTALL","```apt: sudo apt install fprintd```\n\n```dnf: sudo dnf install fprintd```\n\n```pacman: sudo pacman -S fprintd```\n\n```apk: sudo apk add fprintd```\n\n```zypper: sudo zypper install fprintd```\n\n```nix: nix profile install nixpkgs#fprintd```"],["SEE ALSO","[fprintd](/man/fprintd)(1), [fprintd-enroll](/man/fprintd-enroll)(1), [fprintd-list](/man/fprintd-list)(1), [fprintd-verify](/man/fprintd-verify)(1)"],["RESOURCES","```[Source code](https://gitlab.freedesktop.org/libfprint/fprintd)```"]],"e":["fprintd","fprintd-enroll","fprintd-list","fprintd-verify"],"x":"fprintd-delete [username]"},"genhtml":{"n":"genhtml","t":"HTML coverage report generator from lcov trace files","s":[["TAGLINE","HTML coverage report generator from lcov trace files"],["TLDR","**Generate HTML report**\n\n```genhtml [coverage.info] -o [html_output]```\n\n**Add title**\n\n```genhtml --title \"[Coverage Report]\" [coverage.info] -o [html_output]```\n\n**Show branch coverage**\n\n```genhtml --branch-coverage [coverage.info] -o [html_output]```\n\n**Prefix path**\n\n```genhtml --prefix [/src] [coverage.info] -o [html_output]```"],["SYNOPSIS","**genhtml** [_options_] _tracefile_..."],["PARAMETERS","_TRACEFILE_\n> lcov coverage data file.\n\n**-o** _DIR_, **--output-directory** _DIR_\n> Output directory.\n\n**--title** _TEXT_\n> Report title.\n\n**--branch-coverage**\n> Include branch coverage.\n\n**--prefix** _PATH_\n> Remove source prefix.\n\n**--legend**\n> Include coverage legend.\n\n**--help**\n> Display help information."],["DESCRIPTION","**genhtml** creates HTML coverage reports from lcov trace files. It generates navigable reports showing line, function, and branch coverage with source highlighting.\n\nThe tool processes coverage data to produce visual reports for reviewing test coverage. Multiple trace files can be combined into a single report.\n\ngenhtml is the visualization component of the lcov coverage toolkit."],["CAVEATS","Requires lcov trace files. Large codebases produce large reports. Browser needed for viewing."],["HISTORY","genhtml is part of **lcov**, the Linux Test Project coverage tool. It provides HTML visualization for coverage data collected by lcov from gcov."],["INSTALL","```dnf: sudo dnf install lcov```\n\n```pacman: sudo pacman -S lcov```\n\n```apk: sudo apk add lcov```\n\n```zypper: sudo zypper install lcov```\n\n```brew: brew install lcov```\n\n```nix: nix profile install nixpkgs#lcov```"],["SEE ALSO","[lcov](/man/lcov)(1), [gcov](/man/gcov)(1)"],["RESOURCES","```[Source code](https://github.com/linux-test-project/lcov)```"]],"e":["lcov","gcov"],"x":"genhtml [coverage.info] -o [html_output]"},"gifsicle":{"n":"gifsicle","t":"create, edit, and optimize GIF images and animations","s":[["TAGLINE","create, edit, and optimize GIF images and animations"],["TLDR","**Optimize** a GIF for smaller file size\n\n```gifsicle -O3 [input.gif] -o [output.gif]```\n\n**Shrink** further with lossy compression\n\n```gifsicle -O3 --lossy=[80] [input.gif] -o [output.gif]```\n\n**Resize** a GIF\n\n```gifsicle --resize [320x240] [input.gif] -o [output.gif]```\n\n**Resize** to a width, keeping the aspect ratio\n\n```gifsicle --resize-width [320] [input.gif] -o [output.gif]```\n\n**Select** a range of frames\n\n```gifsicle [input.gif] '#0-9' -o [frames.gif]```\n\n**Combine** images into an animation that loops forever\n\n```gifsicle --delay=[10] --loopcount=forever [frame*.gif] -o [output.gif]```\n\n**Change** animation speed\n\n```gifsicle --delay=[5] [input.gif] -o [faster.gif]```\n\n**Reduce** the number of colors\n\n```gifsicle --colors [64] [input.gif] -o [output.gif]```\n\n**Show** information about a GIF\n\n```gifsicle --info [input.gif]```\n\n**Optimize** files in place\n\n```gifsicle -bO3 [*.gif]```"],["SYNOPSIS","**gifsicle** [_options_, _frames_, and _filenames_] ..."],["PARAMETERS","**-o**, **--output** _file_\n> Write output to _file_. Use **-** for standard output.\n\n**-O**, **--optimize**[=_level_]\n> Optimize output. **1** stores only the changed portion of each frame (the default when **-O** is given without a level), **2** also uses transparency, **3** tries several methods and picks the best, which is slower.\n\n**--lossy**[=_lossiness_]\n> Alter colors to shrink the file at the cost of artifacts. Higher values mean smaller files and more noise. Default **20**.\n\n**-U**, **--unoptimize**\n> Expand each frame to a full-size image. Useful before editing individual frames.\n\n**--resize** _WxH_\n> Resize to exact dimensions. Use **_** for a dimension to preserve the aspect ratio.\n\n**--resize-width** _W_, **--resize-height** _H_\n> Resize to a width or height, preserving the aspect ratio.\n\n**--resize-fit** _WxH_\n> Shrink to fit inside a rectangle, preserving the aspect ratio. Does nothing if the GIF already fits.\n\n**--resize-fit-width** _W_, **--resize-fit-height** _H_\n> Shrink to fit a width or height, preserving the aspect ratio.\n\n**--resize-method** _method_\n> Resampling method: **sample** (fast, noisy when shrinking), **mix** (default, better quality), **box**, **catrom**, **mitchell**, **lanczos2**, **lanczos3**.\n\n**--scale** _Xfactor_[x_Yfactor_]\n> Scale by a factor, e.g. **0.5** for half size. _Yfactor_ defaults to _Xfactor_.\n\n**--crop** _x1,y1+WxH_, **--crop** _x1,y1-x2,y2_\n> Crop to a region. Zero dimensions extend to the image edge; negative values retract from it.\n\n**--crop-transparency**\n> Crop away transparent borders.\n\n**-d**, **--delay** _time_\n> Frame delay in hundredths of a second.\n\n**-l**, **--loopcount**[=_count_]\n> Set the Netscape loop extension to _count_, or **forever**. **--loopcount** alone means forever. The default is **--no-loopcount**, which adds no loop extension.\n\n**-k**, **--colors** _num_\n> Reduce to _num_ colors, between **2** and **256**.\n\n**--color-method** _method_\n> Color reduction method: **diversity** (default, xv's algorithm), **blend-diversity**, **median-cut** (Heckbert's algorithm).\n\n**-f**, **--dither**[=_method_]\n> Dither when the colormap changes. Methods include **floyd-steinberg** (default), **atkinson**, **ordered**, **halftone**, **ro64**, **o3**, **o4**, **o8**.\n\n**--rotate-90**, **--rotate-180**, **--rotate-270**\n> Rotate the image.\n\n**--flip-horizontal**, **--flip-vertical**\n> Flip the image.\n\n**-e**, **--explode**\n> Write each frame to a separate file named _file_**.**_number_.\n\n**-m**, **--merge**\n> Merge mode: combine inputs into one output. This is the default.\n\n**-b**, **--batch**\n> Batch mode: modify each input file in place.\n\n**-I**, **--info**\n> Print information about the input GIFs instead of writing an output file.\n\n**--careful**\n> Write slightly larger GIFs that avoid bugs in some other GIF readers, notably older Java and Internet Explorer versions.\n\n**--conserve-memory**\n> Use less memory at the cost of speed, for very large GIFs.\n\n**-w**, **--no-warnings**\n> Suppress warning messages."],["DESCRIPTION","**gifsicle** manipulates GIF images and animations from the command line: it merges files into animations, explodes animations into frames, edits individual frames in place, and reduces file sizes, all without a GUI.\n\nIts arguments are order-dependent, which is what distinguishes it from most tools. Options, frame selections and filenames are read left to right, and an option applies to the input files that follow it. Frame selections use **#** notation attached to a file: **'#0'** is the first frame, **'#0-5'** a range, **'#-1'** the last. Quote them, since **#** starts a comment in most shells.\n\nOptimization is the feature most people come for. **-O3** tries several encodings per frame and keeps the smallest, typically cutting an unoptimized animation substantially by storing only inter-frame differences, losslessly. When that is not enough, **--lossy** trades exactness for size by allowing nearby colors to be merged, and **--colors** shrinks the palette outright.\n\nBy default **gifsicle** runs in merge mode, combining all inputs into a single output written with **-o**. **--batch** switches to editing each input file in place, and **--explode** writes one file per frame."],["CAVEATS","Input must already be a GIF. **gifsicle** does not read PNG, JPEG or video, so pair it with **ffmpeg** or ImageMagick's **convert** to produce the GIF first, then optimize it here.\n\nBecause arguments are positional, **gifsicle --colors 64 a.gif b.gif** and **gifsicle a.gif --colors 64 b.gif** are not the same command. Options placed after a file do not apply to it retroactively.\n\nThe default is **--no-loopcount**, so an animation you assemble will play once unless you pass **--loopcount=forever**. Note also that _count_ is the number of repetitions *after* the first play-through, so **--loopcount=3** shows the animation four times.\n\n**-b** rewrites your originals with no backup. Confirm the result on a copy before running it across a directory.\n\n**--lossy** and **--colors** are not reversible, and repeatedly re-optimizing an already-lossy GIF compounds the artifacts.\n\nLarge animations can use a lot of memory, particularly with **-U**, which expands every frame to full size; **--conserve-memory** trades speed for footprint."],["HISTORY","**gifsicle** was written by **Eddie Kohler** and first released in **1997**, growing out of his earlier work on GIF tools while he was a graduate student. It became a standard tool for GIF manipulation and web optimization, and is the engine behind many \"shrink this GIF\" services. Development continues on GitHub, with **--lossy** added in **2015** based on work from the **giflossy** fork."],["INSTALL","```apt: sudo apt install gifsicle```\n\n```dnf: sudo dnf install gifsicle```\n\n```pacman: sudo pacman -S gifsicle```\n\n```apk: sudo apk add gifsicle```\n\n```zypper: sudo zypper install gifsicle```\n\n```brew: brew install gifsicle```\n\n```nix: nix profile install nixpkgs#gifsicle```"],["SEE ALSO","[convert](/man/convert)(1), [ffmpeg](/man/ffmpeg)(1), [optipng](/man/optipng)(1), [pngquant](/man/pngquant)(1), [imagemagick](/man/imagemagick)(1)"],["RESOURCES","```[Source code](https://github.com/kohler/gifsicle)```\n\n```[Homepage](https://www.lcdf.org/gifsicle/)```\n\n```[Documentation](https://www.lcdf.org/gifsicle/man.html)```"]],"e":["convert","ffmpeg","optipng","pngquant","imagemagick"],"x":"gifsicle -O3 [input.gif] -o [output.gif]"},"git-unpack-file":{"n":"git-unpack-file","t":"Extract blob contents to a temporary file","s":[["TAGLINE","Extract blob contents to a temporary file"],["TLDR","**Unpack blob to temp file**\n\n```git unpack-file [blob_hash]```"],["SYNOPSIS","**git** **unpack-file** _blob_"],["DESCRIPTION","**git unpack-file** creates a temporary file with a blob's contents and prints the filename. It is a low-level plumbing command for accessing blob contents outside of the working tree, used internally during merges and by scripts that need direct access to a blob's data."],["CAVEATS","Creates a file named **.merge_file_XXXXXX** in the current directory. Git does not remove it automatically; the caller is responsible for deleting it once done."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-cat-file](/man/git-cat-file)(1), [git-hash-object](/man/git-hash-object)(1)"],["RESOURCES","```[Documentation](https://git-scm.com/docs/git-unpack-file)```\n\n```[Source code](https://github.com/git/git)```"]],"e":["git-cat-file","git-hash-object"],"x":"git unpack-file [blob_hash]"},"gnucash-cli":{"n":"gnucash-cli","t":"command-line interface for GnuCash reports and quotes","s":[["TAGLINE","command-line interface for GnuCash reports and quotes"],["TLDR","**Update quotes for all securities**\n\n```gnucash-cli --quotes get [file.gnucash]```\n\n**Show Finance::Quote version and sources**\n\n```gnucash-cli --quotes info```\n\n**Dump quotes for specific securities**\n\n```gnucash-cli --quotes dump [namespace] [symbol1] [symbol2]```\n\n**List available reports**\n\n```gnucash-cli --report list```\n\n**Run a report**\n\n```gnucash-cli --report run --name \"[Balance Sheet]\" [file.gnucash]```\n\n**Export report to file**\n\n```gnucash-cli --report run --name \"[report]\" --output-file [output.html] [file.gnucash]```"],["SYNOPSIS","**gnucash-cli** [_options_] [_datafile_]"],["PARAMETERS","**-h**, **--help**\n> Show help message.\n\n**-v**, **--version**\n> Show GnuCash version.\n\n**-Q**, **--quotes** _command_\n> Quotes mode: info, get, dump.\n\n**--namespace** _regexp_\n> Regular expression for commodity namespace (for quotes dump).\n\n**-R**, **--report** _command_\n> Report mode: run, show, list.\n\n**--name** _name_\n> Report name for run command.\n\n**--output-file** _file_\n> Output file for report.\n\n**--export-type** _type_\n> Export format type.\n\n**--debug**\n> Enable debugging mode.\n\n**--extra**\n> Enable developer/debugging features.\n\n**--log** _arg_\n> Log level overrides (can be specified multiple times).\n\n**--logto** _file_\n> File to log into (use \"stderr\" or \"stdout\" for those streams).\n\n**-V**, **--verbose**\n> Show extended Finance::Quote parameters (with quotes dump)."],["DESCRIPTION","**gnucash-cli** is the command-line interface for GnuCash, a personal and small business financial accounting application. It operates in two modes: quotes mode for fetching and updating security prices, and report mode for generating financial reports. This allows automation of price updates and report generation without opening the GUI."],["CAVEATS","Quotes functionality requires Finance::Quote to be properly configured. Database URIs are supported for MySQL and PostgreSQL backends."],["INSTALL","```apt: sudo apt install gnucash```\n\n```dnf: sudo dnf install gnucash```\n\n```pacman: sudo pacman -S gnucash```\n\n```apk: sudo apk add gnucash```\n\n```zypper: sudo zypper install gnucash```\n\n```nix: nix profile install nixpkgs#gnucash```"],["SEE ALSO","[gnucash](/man/gnucash)(1), [ledger](/man/ledger)(1)"]],"e":["gnucash","ledger"],"x":"gnucash-cli --quotes get [file.gnucash]"},"gpg-tui":{"n":"gpg-tui","t":"Terminal user interface for GnuPG key management","s":[["TAGLINE","Terminal user interface for GnuPG key management"],["TLDR","**Launch gpg-tui**\n\n```gpg-tui```\n\n**Specify keyring to use**\n\n```gpg-tui --homedir [~/.gnupg]```\n\n**Use specific style**\n\n```gpg-tui --style [colored]```\n\n**Export output format**\n\n```gpg-tui --output-format [minimal]```"],["SYNOPSIS","**gpg-tui** [_options_]"],["PARAMETERS","**--homedir** _DIR_\n> Use _DIR_ as the GnuPG home directory (overrides **GNUPGHOME**).\n\n**-c**, **--color** _COLOR_\n> Set the accent color (e.g. _gray_, _red_, _green_, _blue_, or a hex value).\n\n**-s**, **--style** _STYLE_\n> Set the table border style: _plain_, _bold_, _double_, or _light_.\n\n**-d**, **--detail-level** _LEVEL_\n> Initial detail level for keys: _minimum_, _standard_, or _full_.\n\n**-r**, **--no-tty**\n> Run without TTY interaction (useful when piping).\n\n**-x**, **--select** _SELECTION_\n> Pre-select rows on startup (key IDs or fingerprints).\n\n**--splash**\n> Show the splash screen at startup.\n\n**-h**, **--help**\n> Show help information.\n\n**-V**, **--version**\n> Show version."],["DESCRIPTION","**gpg-tui** is a terminal user interface for managing GnuPG keys. Written in Rust, it provides an interactive interface for listing, viewing, importing, exporting, signing, editing, and deleting keys without memorizing complex gpg command-line options.\n\nThe tool reads from the standard GnuPG keyring and supports multiple visual styles for display customization."],["INSTALL","```pacman: sudo pacman -S gpg-tui```\n\n```apk: sudo apk add gpg-tui```\n\n```zypper: sudo zypper install gpg-tui```\n\n```brew: brew install gpg-tui```\n\n```nix: nix profile install nixpkgs#gpg-tui```"],["SEE ALSO","[gpg](/man/gpg)(1), [gpg2](/man/gpg2)(1)"]],"e":["gpg","gpg2"],"x":"gpg-tui"},"grype":{"n":"grype","t":"vulnerability scanner for container images and filesystems","s":[["TAGLINE","vulnerability scanner for container images and filesystems"],["TLDR","**Scan container image**\n\n```grype [image:tag]```\n\n**Scan directory**\n\n```grype dir:[path]```\n\n**Scan SBOM file**\n\n```grype sbom:[sbom.json]```\n\n**Output in JSON**\n\n```grype [image] -o json```\n\n**Fail CI if high or critical vulnerabilities found**\n\n```grype [image] --fail-on high```\n\n**Show only vulnerabilities with available fixes**\n\n```grype [image] --only-fixed```\n\n**Scan and exclude specific paths**\n\n```grype dir:[path] --exclude \"[glob_pattern]\"```"],["SYNOPSIS","**grype** [_options_] _source_"],["PARAMETERS","**-o**, **--output** _format_\n> Report format (table, json, cyclonedx, cyclonedx-json, sarif, template).\n\n**-f**, **--fail-on** _severity_\n> Return exit code 2 if vulnerability found at severity level or higher (negligible, low, medium, high, critical).\n\n**--only-fixed**\n> Show only vulnerabilities with available fixes.\n\n**--only-notfixed**\n> Show only vulnerabilities without available fixes.\n\n**--by-cve**\n> Organize results by CVE rather than original vulnerability ID.\n\n**--add-cpes-if-none**\n> Generate CPEs for packages that lack them.\n\n**-s**, **--scope** _scope_\n> Layer analysis scope (squashed, all-layers).\n\n**--exclude** _glob_\n> Exclude paths matching the given glob pattern.\n\n**--platform** _platform_\n> Container platform specifier (e.g., linux/arm64).\n\n**-q**, **--quiet**\n> Suppress logging output.\n\n**-v**, **--verbose**\n> Increase verbosity (-v for info, -vv for debug).\n\n**-c**, **--config** _file_\n> Specify configuration file.\n\n**--file** _path_\n> Write report output to a file instead of stdout.\n\n**--vex** _document_\n> Apply VEX documents to filter results."],["DESCRIPTION","**grype** is a vulnerability scanner for container images and filesystems. It identifies known security vulnerabilities in packages and dependencies by comparing against vulnerability databases.\n\nThe tool integrates with CI/CD pipelines and produces reports in various formats. It works with the same sources as Syft for comprehensive software composition analysis."],["SOURCE TYPES","**image**: Container image (default)\n**dir:**: Local directory\n**file:**: Single file\n**sbom:**: SBOM file (Syft, CycloneDX, SPDX)\n**registry:**: Remote container registry"],["CAVEATS","Database updates needed regularly. False positives possible. Coverage depends on ecosystem. Large images scan slowly."],["HISTORY","**grype** was created by **Anchore** as part of their open source security tooling alongside Syft. It emerged around **2020** to address container security scanning needs with a focus on accuracy and integration."],["INSTALL","```dnf: sudo dnf install grype```\n\n```apk: sudo apk add grype```\n\n```zypper: sudo zypper install grype```\n\n```brew: brew install grype```\n\n```nix: nix profile install nixpkgs#grype```"],["SEE ALSO","[syft](/man/syft)(1), [trivy](/man/trivy)(1), [snyk](/man/snyk)(1)"]],"e":["syft","trivy","snyk"],"x":"grype [image:tag]"},"gvmap":{"n":"gvmap","t":"creates geographical-style maps from graphs","s":[["TAGLINE","creates geographical-style maps from graphs"],["TLDR","**Create map from graph**\n\n```gvmap [input.gv] > map.gv```\n\n**Include edges in the output**\n\n```gvmap -e [input.gv] > map.gv```\n\n**Specify a color scheme** (1: pastel, 2: blue-yellow, 3: white-red)\n\n```gvmap -c [2] [input.gv] > colored.gv```\n\n**Specify output file**\n\n```gvmap -o [output.gv] [input.gv]```\n\n**Use top-level cluster subgraphs for clustering**\n\n```gvmap -D [input.gv] > map.gv```"],["SYNOPSIS","**gvmap** [_options_] [_files_]"],["PARAMETERS","_FILES_\n> Input DOT files.\n\n**-o** _FILE_\n> Output file.\n\n**-e**\n> Include edges in the final output.\n\n**-c** _K_\n> Use color scheme K (1: pastel, 2: blue-yellow, 3: white-red, etc.).\n\n**-C** _K_\n> Maximum number of clusters. Default 0 (no limit).\n\n**-D**\n> Use top-level cluster subgraphs to determine clustering.\n\n**-O**\n> Do NOT do color assignment optimization.\n\n**-a** _K_\n> Average number of artificial points added along bounding box of labels.\n\n**-m** _V_\n> Generate a margin of V points around the drawing.\n\n**-r** _K_\n> Number of random points used to define sea and lake boundaries. 0 for auto.\n\n**-s** _V_\n> Depth of sea and lake shores in points. 0 for auto.\n\n**-t** _N_\n> Make N attempts to improve cluster contiguity.\n\n**-v**\n> Verbose mode.\n\n**-z** _COLOR_\n> Set polygon line color. Default is black.\n\n**-h**\n> Print usage and exit."],["DESCRIPTION","**gvmap** takes as input a graph in DOT format with node position information (produced by a Graphviz layout engine such as sfdp) and groups related nodes into clusters, drawing them as geographical-style map regions.\n\nThe tool analyzes graph structure to cluster nodes into countries or regions, adds visual styling with color schemes, and can include edges in the output. It is typically used as part of a pipeline with **sfdp** and **gvmap.sh**."],["CAVEATS","Part of Graphviz package. Input graphs must have node position information; run a layout engine like **sfdp** first. The **gvmap.sh** wrapper script handles the full pipeline automatically."],["HISTORY","gvmap was developed as part of **Graphviz** at **AT&T Labs** for creating geographical-style graph visualizations."],["INSTALL","```apt: sudo apt install graphviz```\n\n```dnf: sudo dnf install graphviz```\n\n```pacman: sudo pacman -S graphviz```\n\n```apk: sudo apk add graphviz```\n\n```zypper: sudo zypper install graphviz```\n\n```brew: brew install graphviz```\n\n```nix: nix profile install nixpkgs#graphviz```"],["SEE ALSO","[dot](/man/dot)(1), [neato](/man/neato)(1), [sfdp](/man/sfdp)(1)"]],"e":["dot","neato","sfdp"],"x":"gvmap [input.gv] > map.gv"},"hanky":{"n":"hanky","t":"CLI and pipeline framework for loading flash cards into Anki","s":[["TAGLINE","CLI and pipeline framework for loading flash cards into Anki"],["TLDR","**Import a CSV** of cards into a deck (Basic note type by default)\n\n```hanky pipe [words.csv] --into [english::vocab]```\n\n**Import every matching file** under a directory into nested decks\n\n```hanky pipe-dir [french/] [\"*.csv\"] -r```\n\n**Override the Anki note type** / model name\n\n```hanky pipe [cards.csv] -m [Basic] --into [deck]```\n\n**Dry-run** processors without writing the collection\n\n```hanky pipe [words.csv] --dry-run```\n\n**Verbose report** of each processed card\n\n```hanky pipe [words.csv] -v --into [deck]```\n\n**Run a custom pipeline script** the same way as the hanky binary\n\n```python3 [my_script.py] pipe [words.csv] --into [english::vocab]```"],["SYNOPSIS","**hanky** **pipe** [**-m** _MODEL_] [**--into** _DECK_] [**--fail-fast**] [**--dry-run**] [**-v**] [**--args** _K=V_ ...] _file_\n\n**hanky** **pipe-dir** [**-m** _MODEL_] [**-r**] [**--fail-fast**] [**--dry-run**] [**-v**] [**--args** _K=V_ ...] _dir_ _pattern_"],["PARAMETERS","**pipe** _file_\n> Load cards from a single file (**.csv**, **.json**, or a registered extension) and add them to Anki.\n\n**pipe-dir** _dir_ _pattern_\n> Load many files under _dir_ matching the glob _pattern_. Deck names are derived from relative paths (e.g. **french/animals.csv** → **french::animals**).\n\n**-m** _MODEL_, **--model** _MODEL_\n> Override the Anki model (note type) used for new cards. Standalone **hanky** defaults to **Basic**; pipeline scripts set the model in **HankyPipeline(...)**.\n\n**--into** _DECK_\n> Destination deck for **pipe**. Defaults to the input filename without extension.\n\n**-r**, **--recursive**\n> For **pipe-dir**, also descend into subdirectories.\n\n**--fail-fast**\n> Stop and raise on the first card that cannot be added instead of skipping and reporting at the end.\n\n**--dry-run**\n> Run card processors and report results without modifying the Anki collection (no decks, media, cards, or backup).\n\n**-v**, **--verbose**\n> Print each card dict after processors run, plus errors in the final report.\n\n**--args** _K=V_\n> Key=value pairs forwarded to custom card processors (pipeline scripts only). May be repeated."],["DESCRIPTION","**hanky** is a Python CLI and library that reads flash cards from files (or in-script sources), optionally transforms them through user-defined **card processors**, and writes notes into a local **Anki** collection. It is aimed at ETL-style workflows: scrape or OCR material, enrich fields (translations, examples, TTS audio), then bulk-load into decks.\n\nBuilt-in **pipe** / **pipe-dir** commands work with CSV and JSON out of the box when column or key names match the target note type fields. For custom logic, install the package, write a small script that registers **@hanky.card_processor** functions on a **HankyPipeline**, and call **hanky.run()** so the same CLI surface is available as **python3 my_script.py pipe ...**. Processors may attach audio media via **CardMedia** (**.mp3**, **.oga**, **.opus**, **.wav**, **.weba**, **.aac**). Custom file loaders can be registered for formats such as Excel.\n\nhanky only **adds** cards, media, and decks. Note types (models) must already exist in the collection; create them in the Anki UI first. Requires Python **3.11+** and a local Anki collection database. Not affiliated with AnkiWeb or the Anki project."],["CONFIGURATION","**~/.config/hanky/hanky.toml**\n> Optional TOML config. Needed mainly if you do not use the default Anki profile (**User 1**) or you are on Windows.\n\n**ANKI_DB_PATH**\n> Path to the Anki collection SQLite file (**collection.anki2**). Defaults to the **User 1** profile under the platform Anki2 data directory.\n\n**DO_SAFETY_CHECK**\n> When true (default), refuse to open the collection if another process (e.g. Anki) appears to hold it.\n\n**ALLOW_DUPLICATES**\n> When false (default), skip cards that duplicate existing notes.\n\n**BACKUP_FOLDER**\n> Directory for collection backups before writes. Default: **~/.local/share/hanky/backups**.\n\nA **Config** object passed to **HankyPipeline** in a script overrides file settings."],["CAVEATS","Close Anki (or disable the safety check only if you accept corruption risk) before writing the collection. Note types must pre-exist; hanky does not create models. Duplicate detection and field names depend on your note type. Windows and non-default profiles almost always need **ANKI_DB_PATH**. Custom processors that call external APIs (translation, TTS, vision) need their own credentials and dependencies."],["HISTORY","Written by **HBA** as an open-source Python package on PyPI under the MIT license. Provides both a **hanky** console script and a library API for programmable Anki import pipelines."],["SEE ALSO","[anki](/man/anki)(1), [python](/man/python)(1), [pip](/man/pip)(1)"],["RESOURCES","```[Source code](https://github.com/Haeata-Ash/hanky)```"]],"e":["anki","python","pip"],"x":"hanky pipe [words.csv] --into [english::vocab]"},"inputattach":{"n":"inputattach","t":"attach a serial line to an input-layer device","s":[["TAGLINE","attach a serial line to an input-layer device"],["TLDR","Attach a **Pulse8 CEC** adapter\n\n```sudo inputattach --pulse8-cec /dev/ttyACM0```\n\nAttach a **Wacom W8001** pen-only tablet at 19200 baud\n\n```sudo inputattach --baud 19200 --w8001 /dev/ttyS0```\n\nAttach an **ELO touchscreen** and fork into the background\n\n```sudo inputattach --daemon --elo /dev/ttyS0```\n\nAttach a **Microsoft serial mouse**\n\n```sudo inputattach --microsoft /dev/ttyS0```\n\nList **supported devices**\n\n```inputattach --help```"],["SYNOPSIS","**inputattach** [_options_] _mode_ _device_"],["PARAMETERS","**--daemon**\n> Fork into the background after attaching.\n\n**--always**\n> Ignore initialization failures and keep the device attached.\n\n**--noinit**\n> Skip device initialization.\n\n**--baud** _RATE_\n> Override the default serial baud rate.\n\n**--help**\n> Display help and list all supported device modes.\n\nMode flags (select one):\n\n**--microsoft**, **--mouseman**, **--intellimouse**, **--mousesystems**\n> Common serial mouse protocols.\n\n**--elo**, **--mtouch**, **--touchit213**, **--fujitsu**\n> Serial touchscreen protocols.\n\n**--w8001**, **--wacom_iv**, **--wacom_v**\n> Wacom serial tablet protocols.\n\n**--pulse8-cec**, **--rainshadow-cec**\n> HDMI-CEC adapters that present as serial devices.\n\n**--magellan**, **--spaceorb**, **--spaceball**\n> 6-DOF serial input devices.\n\n**--sunkbd**, **--lkkbd**, **--stowaway**\n> Serial keyboard protocols."],["DESCRIPTION","**inputattach** attaches a serial line to the Linux input subsystem. It reads raw bytes from the named serial device, decodes them according to the chosen protocol, and feeds the resulting events into the kernel where they appear as standard input devices under `/dev/input/`.\n\nThe program is part of the **linuxconsoletools** package. After attaching, devices can be used with libinput, evdev, X.Org, or any other consumer of the Linux input layer. With **--daemon**, inputattach detaches from the controlling terminal so it can be started from rc scripts or systemd units.\n\nFor Wacom W8001 hardware, the default rate of 38400 bps covers pen-and-touch and touch-only devices. Pen-only devices need **--baud 19200**."],["CAVEATS","Usually requires root or write access to the serial node. The mode flag must match the connected hardware; the wrong protocol will produce no events or garbage. Once attached, the device remains owned by inputattach until the process exits."],["HISTORY","inputattach is maintained as part of **linuxconsoletools** (formerly joyutils). The Wacom variants are also distributed by the **linuxwacom** project for use with serial Wacom tablets."],["INSTALL","```apt: sudo apt install inputattach```\n\n```dnf: sudo dnf install linuxconsoletools```\n\n```apk: sudo apk add linuxconsoletools```\n\n```zypper: sudo zypper install linuxconsoletools```"],["SEE ALSO","[evtest](/man/evtest)(1), [libinput](/man/libinput)(1)"]],"e":["evtest","libinput"],"x":"sudo inputattach --pulse8-cec /dev/ttyACM0"},"jj-git-fetch":{"n":"jj-git-fetch","t":"fetches changes from Git remotes into a Jujutsu repository","s":[["TAGLINE","fetches changes from Git remotes into a Jujutsu repository"],["TLDR","**Fetch from default remote**\n\n```jj git fetch```\n\n**Fetch from specific remote**\n\n```jj git fetch --remote [origin]```\n\n**Fetch all remotes**\n\n```jj git fetch --all-remotes```\n\n**Fetch specific branch**\n\n```jj git fetch --branch [main]```"],["SYNOPSIS","**jj** **git** **fetch** [_options_]"],["PARAMETERS","**--remote** _name_\n> Remote to fetch from.\n\n**--all-remotes**\n> Fetch from all remotes.\n\n**--branch** _name_\n> Fetch specific branch."],["DESCRIPTION","**jj git fetch** fetches changes from Git remotes into a Jujutsu repository. Updates remote-tracking branches. Works with the Git backend to sync with remote repositories."],["INSTALL","```pacman: sudo pacman -S jujutsu```\n\n```apk: sudo apk add jujutsu```\n\n```zypper: sudo zypper install jujutsu```\n\n```brew: brew install jujutsu```\n\n```nix: nix profile install nixpkgs#jujutsu```"],["SEE ALSO","[jj](/man/jj)(1), [jj-git-push](/man/jj-git-push)(1)"]],"e":["jj","jj-git-push"],"x":"jj git fetch"},"joshuto":{"n":"joshuto","t":"Ranger-like terminal file manager written in Rust","s":[["TAGLINE","Ranger-like terminal file manager written in Rust"],["TLDR","**Launch joshuto** in the current directory\n\n```joshuto```\n\n**Launch in a specific directory**\n\n```joshuto [path/to/directory]```"],["SYNOPSIS","**joshuto** [_options_] [_path_]"],["DESCRIPTION","**joshuto** is a terminal file manager inspired by **ranger**, written in Rust. It features a three-pane layout showing the parent directory, current directory, and a file preview pane. Navigation uses **Vim-like** key bindings, and the tool supports file previews via customizable preview scripts similar to ranger's.\n\nKey features include mouse support (enabled by default), clipboard integration (via xsel/xclip/wl-clipboard), **fzf** integration for fuzzy finding, **zoxide** integration for directory jumping, bulk rename operations, and fully customizable key bindings and themes through TOML configuration files."],["CONFIGURATION","Configuration files are stored in **$XDG_CONFIG_HOME/joshuto/** and include **joshuto.toml** (general settings), **keymap.toml** (key bindings), **mimetype.toml** (file associations), and **theme.toml** (colors and styles)."],["CAVEATS","Requires Rust 1.67+ for building from source. File preview scripts need to be configured separately for rich previews. Clipboard support requires external clipboard tools."],["HISTORY","**joshuto** was created by **kamiyaa** and is written in **Rust**. It was designed as a modern, fast alternative to ranger that leverages Rust's performance while maintaining a familiar file manager workflow."],["INSTALL","```brew: brew install joshuto```\n\n```nix: nix profile install nixpkgs#joshuto```"],["SEE ALSO","[ranger](/man/ranger)(1), [lf](/man/lf)(1), [nnn](/man/nnn)(1), [yazi](/man/yazi)(1)"]],"e":["ranger","lf","nnn","yazi"],"x":"joshuto"},"jot":{"n":"jot","t":"generates sequential or random data","s":[["TAGLINE","generates sequential or random data"],["TLDR","**Generate sequence**\n\n```jot [10]```\n\n**Generate range**\n\n```jot [10] [1] [100]```\n\n**Generate with step**\n\n```jot - [1] [100] [5]```\n\n**Random numbers**\n\n```jot -r [5] [1] [100]```\n\n**Repeated string**\n\n```jot -b [string] [5]```\n\n**Formatted output**\n\n```jot -w \"[%02d]\" [10]```"],["SYNOPSIS","**jot** [_options_] [_reps_ [_begin_ [_end_ [_step_]]]]"],["PARAMETERS","_REPS_\n> Number of repetitions.\n\n_BEGIN_\n> Starting value.\n\n_END_\n> Ending value.\n\n_STEP_\n> Increment.\n\n**-r**\n> Random values.\n\n**-b** _STRING_\n> Repeat string.\n\n**-w** _FORMAT_\n> Printf format.\n\n**-s** _STRING_\n> Separator.\n\n**--help**\n> Display help information."],["DESCRIPTION","**jot** generates sequential or random data. It produces numbers, characters, or formatted strings.\n\nThe tool is useful for generating test data and sequences. It supports various output formats."],["CAVEATS","BSD utility. Not in GNU coreutils. Use seq for portability."],["HISTORY","jot originated in **BSD Unix** as a data generator utility. It provides more flexibility than seq."],["INSTALL","```apt: sudo apt install athena-jot```\n\n```apk: sudo apk add outils-jot```\n\n```brew: brew install jot```\n\n```nix: nix profile install nixpkgs#jot```"],["SEE ALSO","[seq](/man/seq)(1), [yes](/man/yes)(1), [printf](/man/printf)(1)"]],"e":["seq","yes","printf"],"x":"jot [10]"},"jupyter-lab":{"n":"jupyter-lab","t":"CLI for JupyterLab, the next-generation web interface for Project Jupyter","s":[["TAGLINE","CLI for JupyterLab, the next-generation web interface for Project Jupyter"],["TLDR","**Start JupyterLab**\n\n```jupyter lab```\n\n**Start on specific port**\n\n```jupyter lab --port [8889]```\n\n**Start without opening browser**\n\n```jupyter lab --no-browser```\n\n**Start with specific notebook directory**\n\n```jupyter lab --notebook-dir=[/path/to/notebooks]```\n\n**Allow external connections**\n\n```jupyter lab --ip [0.0.0.0]```\n\n**Generate configuration file**\n\n```jupyter lab --generate-config```\n\n**List running servers**\n\n```jupyter lab list```\n\n**Stop a server**\n\n```jupyter lab stop [8888]```"],["SYNOPSIS","**jupyter lab** [_options_]\n\n**jupyter lab** build|clean|path|paths|workspaces"],["COMMANDS","**jupyter lab**\n> Start JupyterLab server.\n\n**jupyter lab build**\n> Build JupyterLab application.\n\n**jupyter lab clean**\n> Remove build artifacts.\n\n**jupyter lab list**\n> List running JupyterLab servers.\n\n**jupyter lab stop** _port_\n> Stop server on port.\n\n**jupyter lab path**\n> Show JupyterLab paths.\n\n**jupyter lab workspaces** list|export|import\n> Manage workspaces."],["PARAMETERS","**--port** _port_\n> Server port. Default: 8888.\n\n**--ip** _address_\n> IP address to bind. Default: localhost.\n\n**--no-browser**\n> Don't open browser on startup.\n\n**--notebook-dir** _path_\n> Root directory for notebooks.\n\n**--app-dir** _path_\n> JupyterLab application directory.\n\n**--generate-config**\n> Generate default configuration file.\n\n**--config** _file_\n> Path to configuration file.\n\n**--ContentsManager.allow_hidden** _bool_\n> Allow access to hidden files.\n\n**--ServerApp.token** _token_\n> Authentication token.\n\n**--ServerApp.password** _hash_\n> Hashed password for access.\n\n**--collaborative**\n> Enable real-time collaboration.\n\n**--allow-root**\n> Allow the server to be run from the root user.\n\n**--debug**\n> Set debug level logging.\n\n**--help-all**\n> Show all configurable options and exit."],["DESCRIPTION","**jupyter lab** is the CLI for JupyterLab, the next-generation web interface for Project Jupyter. It provides a flexible, extensible IDE for notebooks, code, and data.\n\nJupyterLab runs a server exposing the web interface. Access via browser with the token shown at startup. Multiple users can connect; collaboration mode enables shared editing.\n\nThe interface supports notebooks, text editors, terminals, and extensions. File browser navigates the **--notebook-dir** tree. Multiple documents open in tabs with split views.\n\nExtensions add functionality. Use **jupyter labextension** for management. **jupyter lab build** compiles assets after extension changes.\n\nConfiguration via **jupyter_lab_config.py** sets defaults. Generate template with **--generate-config**. Common settings include token/password, allowed origins, and kernel settings."],["CAVEATS","Server exposes full filesystem access from notebook-dir. Production deployments need proper authentication and HTTPS. Extensions may require rebuild. Memory usage scales with kernel count."],["HISTORY","JupyterLab development started around **2016** as the successor to Jupyter Notebook interface. Version 1.0 released in **2019** after extensive beta period. It's developed by **Project Jupyter**, which evolved from IPython. The project is part of the broader Jupyter ecosystem including kernels for many languages. JupyterLab 4.0 (2023) brought performance improvements and real-time collaboration."],["INSTALL","```dnf: sudo dnf install jupyterlab```\n\n```pacman: sudo pacman -S jupyterlab```\n\n```apk: sudo apk add jupyterlab```\n\n```brew: brew install jupyterlab```"],["SEE ALSO","[jupyter](/man/jupyter)(1), [ipython](/man/ipython)(1), [jupyterlite](/man/jupyterlite)(1)"]],"e":["jupyter","ipython","jupyterlite"],"x":"jupyter lab"},"kinit":{"n":"kinit","t":"obtains Kerberos tickets from the Key Distribution Center","s":[["TAGLINE","obtains Kerberos tickets from the Key Distribution Center"],["TLDR","**Get Kerberos ticket**\n\n```kinit [username]```\n\n**Get ticket for specific principal**\n\n```kinit [username@REALM]```\n\n**Specify keytab file**\n\n```kinit -k -t [keytab.file] [principal]```\n\n**Get forwardable ticket**\n\n```kinit -f [username]```\n\n**Set ticket lifetime**\n\n```kinit -l [1h] [username]```\n\n**Renew existing ticket**\n\n```kinit -R```"],["SYNOPSIS","**kinit** [_options_] [_principal_]"],["DESCRIPTION","**kinit** obtains Kerberos tickets from the Key Distribution Center (KDC). The ticket-granting ticket (TGT) enables authentication to Kerberos-protected services without repeated password entry.\n\nThe tool is essential for Kerberos authentication in enterprise environments, accessing services like NFS, SSH, and Active Directory."],["PARAMETERS","**-k**\n> Use keytab file.\n\n**-t** _keytab_\n> Keytab file path.\n\n**-l** _lifetime_\n> Ticket lifetime.\n\n**-r** _lifetime_\n> Renewable lifetime.\n\n**-f**\n> Get forwardable ticket.\n\n**-F**\n> Non-forwardable ticket.\n\n**-p**\n> Get proxiable ticket.\n\n**-R**\n> Renew existing ticket.\n\n**-c** _cache_\n> Credentials cache.\n\n**-S** _service_\n> Service principal."],["CAVEATS","Requires KDC access. Tickets expire and need renewal. Keytabs need protection. Clock sync required."],["HISTORY","**kinit** is part of **MIT Kerberos** and **Heimdal** implementations. Kerberos was developed at **MIT** in the **1980s** as part of Project Athena, named after the three-headed dog guarding Hades in Greek mythology."],["INSTALL","```apk: sudo apk add heimdal```\n\n```zypper: sudo zypper install kinit```\n\n```brew: brew install heimdal```\n\n```nix: nix profile install nixpkgs#heimdal```"]],"e":[],"x":"kinit [username]"},"kubectl-autoscale":{"n":"kubectl-autoscale","t":"creates a Horizontal Pod Autoscaler for a deployment, replica set","s":[["TAGLINE","creates a Horizontal Pod Autoscaler for a deployment, replica set"],["TLDR","**Create HPA for deployment**\n\n```kubectl autoscale deployment [name] --min=[2] --max=[10] --cpu-percent=[80]```\n\n**Autoscale replication controller**\n\n```kubectl autoscale rc [name] --max=[5]```\n\n**Create HPA with specific name**\n\n```kubectl autoscale deployment [name] --name=[hpa-name] --max=[10]```"],["SYNOPSIS","**kubectl** **autoscale** _type_ _name_ [_options_]"],["PARAMETERS","**--min** _count_\n> Minimum number of replicas.\n\n**--max** _count_\n> Maximum number of replicas.\n\n**--cpu-percent** _percent_\n> Target CPU utilization percentage.\n\n**--name** _name_\n> Name for the HPA resource.\n\n**-n**, **--namespace** _name_\n> Kubernetes namespace."],["DESCRIPTION","**kubectl autoscale** creates a Horizontal Pod Autoscaler (HPA) for a deployment, replica set, or replication controller. Automatically scales the number of pods based on CPU utilization or custom metrics."],["INSTALL","```apt: sudo apt install kubectl```\n\n```pacman: sudo pacman -S kubectl```\n\n```apk: sudo apk add kubectl```\n\n```brew: brew install kubectl```\n\n```nix: nix profile install nixpkgs#kubectl```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [kubectl-scale](/man/kubectl-scale)(1)"]],"e":["kubectl","kubectl-scale"],"x":"kubectl autoscale deployment [name] --min=[2] --max=[10] --cpu-percent=[80]"},"lint-staged":{"n":"lint-staged","t":"Run linters on git staged files","s":[["TAGLINE","Run linters on git staged files"],["TLDR","**Run on staged files**\n\n```npx lint-staged```\n\n**Run with config file**\n\n```npx lint-staged --config [.lintstagedrc.json]```\n\n**Run in debug mode**\n\n```npx lint-staged --debug```\n\n**Dry run (no changes)**\n\n```npx lint-staged --dry-run```\n\n**Run on changed files between two branches**\n\n```npx lint-staged --diff=\"[branch1]...[branch2]\"```\n\n**Run with verbose output**\n\n```npx lint-staged --verbose```"],["SYNOPSIS","**lint-staged** [_options_]"],["PARAMETERS","**--config**, **-c** _file_\n> Path to config file or npm package name.\n\n**--debug**, **-d**\n> Enable debug mode.\n\n**--dry-run**\n> Show what would run without executing.\n\n**--diff** _ref_\n> Override the default --staged flag of git diff to get list of files.\n\n**--diff-filter** _filter_\n> Override the default --diff-filter=ACMR of git diff.\n\n**--concurrent**, **-p** _number_\n> Control concurrency of tasks (default: true for parallel).\n\n**--no-stash**\n> Disable creating backup stash, leave modifications in index.\n\n**--hide-unstaged**\n> Hide unstaged changes to tracked files before running tasks.\n\n**--quiet**, **-q**\n> Suppress all CLI output except from tasks.\n\n**--relative**, **-r**\n> Pass filepaths relative to process.cwd() to tasks.\n\n**--verbose**\n> Show task output even when tasks succeed.\n\n**--allow-empty**\n> Allow creating empty git commits when tasks revert all staged changes.\n\n**--cwd** _dir_\n> Run all tasks in a specific directory.\n\n**--max-arg-length** _number_\n> Override the maximum length of the generated command string."],["DESCRIPTION","**lint-staged** runs linters on git staged files. It's typically used with husky or lefthook to ensure code quality before commits by running formatters and linters only on changed files.\n\nThis makes pre-commit hooks fast by avoiding linting the entire codebase."],["CONFIGURATION","```json\n// package.json\n{\n  \"lint-staged\": {\n    \"*.js\": [\"eslint --fix\", \"prettier --write\"],\n    \"*.css\": \"stylelint --fix\"\n  }\n}\n```"],["CAVEATS","Requires git repository. Stashes unstaged changes during run. May conflict with partial staging. Works best with auto-fixing linters."],["HISTORY","lint-staged was created by **Andrey Okonetchnikov** in **2016** to solve the problem of slow pre-commit hooks by targeting only staged files."],["INSTALL","```nix: nix profile install nixpkgs#lint-staged```"],["SEE ALSO","[husky](/man/husky)(1), [eslint](/man/eslint)(1), [prettier](/man/prettier)(1), [lefthook](/man/lefthook)(1)"]],"e":["husky","eslint","prettier","lefthook"],"x":"npx lint-staged"},"lpmove":{"n":"lpmove","t":"moves print jobs between queues","s":[["TAGLINE","moves print jobs between queues"],["TLDR","**Move job to another printer**\n\n```lpmove [job_id] [destination_printer]```\n\n**Move all jobs from printer**\n\n```lpmove [source_printer] [destination_printer]```\n\n**Move specific job**\n\n```lpmove [printer]-[job_id] [destination]```"],["SYNOPSIS","**lpmove** [_options_] _job_ _destination_\n\n**lpmove** [_options_] _source_ _destination_"],["PARAMETERS","_JOB_\n> Job ID number or old destination and job ID (e.g., oldprinter-123).\n\n_SOURCE_\n> Source printer name (moves all jobs from this printer).\n\n_DESTINATION_\n> Target printer name.\n\n**-E**\n> Force encryption when connecting to the server.\n\n**-U _username_**\n> Specify an alternate username.\n\n**-h _server[:port]_**\n> Specify an alternate server."],["DESCRIPTION","**lpmove** moves print jobs between queues. It transfers jobs from one printer to another.\n\nThe tool is useful when a printer fails or needs maintenance. Jobs continue waiting in the new queue."],["CAVEATS","Requires administrative access. Jobs must be pending. CUPS must be running."],["HISTORY","lpmove is part of **CUPS** for moving print jobs between printer queues."],["INSTALL","```apt: sudo apt install cups-client```\n\n```dnf: sudo dnf install cups-client```\n\n```apk: sudo apk add cups-client```\n\n```zypper: sudo zypper install cups-client```"],["SEE ALSO","[cancel](/man/cancel)(1), [lp](/man/lp)(1), [lpq](/man/lpq)(1), [lpr](/man/lpr)(1), [lprm](/man/lprm)(1), [lpadmin](/man/lpadmin)(8), [lpstat](/man/lpstat)(1)"]],"e":["cancel","lp","lpq","lpr","lprm","lpadmin","lpstat"],"x":"lpmove [job_id] [destination_printer]"},"mailq":{"n":"mailq","t":"displays the mail queue","s":[["TAGLINE","displays the mail queue"],["TLDR","**Show mail queue**\n\n```mailq```\n\n**Show queue summary**\n\n```mailq -p```\n\n**Show verbose output**\n\n```mailq -v```"],["SYNOPSIS","**mailq** [_options_]"],["PARAMETERS","**-p**\n> Print queue summary.\n\n**-v**\n> Verbose output.\n\n**--help**\n> Display help information."],["DESCRIPTION","**mailq** displays the mail queue. It shows messages waiting to be delivered.\n\nThe tool is usually a symlink to sendmail or postfix. It lists queued, deferred, and held mail."],["CAVEATS","MTA dependent output. May need root for full details. Part of MTA package."],["HISTORY","mailq provides a standard interface to view mail queues across different **Mail Transfer Agents** (MTAs)."],["INSTALL","```apt: sudo apt install dma```\n\n```dnf: sudo dnf install postfix```\n\n```pacman: sudo pacman -S postfix```\n\n```apk: sudo apk add postfix```\n\n```zypper: sudo zypper install postfix```\n\n```nix: nix profile install nixpkgs#dma```"],["SEE ALSO","[sendmail](/man/sendmail)(8), [postqueue](/man/postqueue)(1), [postfix](/man/postfix)(1)"]],"e":["sendmail","postqueue","postfix"],"x":"mailq"},"mcheck":{"n":"mcheck","t":"Heap consistency checking for malloc","s":[["TAGLINE","Heap consistency checking for malloc"],["TLDR","**Enable heap checking in a C program** (call before any malloc)\n\n```mcheck(NULL)```\n\n**Enable heap checking with a custom error handler**\n\n```mcheck([handler_func])```\n\n**Enable pedantic checking on every allocation**\n\n```mcheck_pedantic(NULL)```\n\n**Check all allocated blocks immediately**\n\n```mcheck_check_all()```\n\n**Explicitly check a specific allocated block**\n\n```mprobe([ptr])```\n\n**Link with mcheck to enable automatic checking**\n\n```cc [program.c] -lmcheck```"],["SYNOPSIS","```c\n#include <mcheck.h>\n\nint mcheck(void (*abortfunc)(enum mcheck_status mstatus));\nint mcheck_pedantic(void (*abortfunc)(enum mcheck_status mstatus));\nvoid mcheck_check_all(void);\nenum mcheck_status mprobe(void *ptr);\n```"],["PARAMETERS","**mcheck** _abortfunc_\n> Install debugging hooks for malloc. Pass NULL to use the default handler that prints a message and calls abort(). Must be called before the first malloc().\n\n**mcheck_pedantic** _abortfunc_\n> Like mcheck(), but performs consistency checks on all allocated blocks on every call to malloc(), realloc(), or free(). Very slow.\n\n**mcheck_check_all**\n> Trigger an immediate consistency check on all allocated blocks. Only effective if mcheck() was called first.\n\n**mprobe** _ptr_\n> Explicitly check the block pointed to by ptr for consistency."],["DESCRIPTION","The **mcheck** family of functions installs debugging hooks for the **malloc**(3) memory allocation functions in glibc. These hooks perform consistency checks on the heap, detecting common errors such as double-free, buffer overruns that corrupt malloc bookkeeping data, and use of freed memory.\n\nThe **mcheck()** function must be called before the first call to malloc(). Linking the program with **-lmcheck** inserts an implicit call to mcheck(NULL) before the first allocation, which is easier than modifying source code.\n\nThe **mprobe()** function returns a status indicating the block's state: **MCHECK_OK** (no inconsistency), **MCHECK_HEAD** (memory before the block was clobbered), **MCHECK_TAIL** (memory after the block was clobbered), or **MCHECK_FREE** (the block was freed twice)."],["CAVEATS","The **mcheck()** function must be called before any memory allocation; otherwise it returns -1. These functions are glibc-specific and non-portable. The **MALLOC_CHECK_** environment variable provides similar functionality without recompilation. The **mcheck_pedantic()** function is very slow and should only be used for targeted debugging. Deprecated in newer glibc versions in favor of **MALLOC_CHECK_** and tools like valgrind."],["HISTORY","The **mcheck** functions are part of the **GNU C Library** (glibc) and originate from early Unix malloc debugging facilities. They are documented in the glibc manual under \"Heap Consistency Checking\"."],["INSTALL","```apt: sudo apt install mtools```\n\n```dnf: sudo dnf install mtools```\n\n```pacman: sudo pacman -S mtools```\n\n```apk: sudo apk add mtools```\n\n```zypper: sudo zypper install mtools```\n\n```brew: brew install mtools```\n\n```nix: nix profile install nixpkgs#mtools```"],["SEE ALSO","[valgrind](/man/valgrind)(1)"]],"e":["valgrind"],"x":"mcheck(NULL)"},"megatools-dl":{"n":"megatools-dl","t":"downloads files and folders from mega","s":[["TAGLINE","downloads files and folders from mega"],["TLDR","**Download files** from a mega.nz link into the current directory\n\n```megatools-dl [https://mega.nz/...]```\n\nDownload files into a **specific directory**\n\n```megatools-dl --path [path/to/directory] [https://mega.nz/...]```\n\n**Interactively choose** which files to download\n\n```megatools-dl --choose-files [https://mega.nz/...]```\n\n**Limit the download speed** in KiB/s\n\n```megatools-dl --limit-speed [speed] [https://mega.nz/...]```"],["SYNOPSIS","**megatools-dl** [_options_] _URL_"],["PARAMETERS","**--path _directory_**\n> Download files to the specified directory\n\n**--choose-files**\n> Interactively select which files to download from folders\n\n**--limit-speed _KiB/s_**\n> Limit download bandwidth\n\n**--no-progress**\n> Disable progress indicator\n\n**--print-names**\n> Print downloaded file names to stdout"],["DESCRIPTION","**megatools-dl** downloads files and folders from mega.nz cloud storage links. Part of the **megatools** suite, it provides command-line access to Mega's file hosting service without requiring an account for public links.\n\nThe tool handles both file links and folder links, automatically creating directory structures for folder downloads. It supports resuming interrupted downloads and can limit bandwidth consumption.\n\nFor protected links or private files, authentication can be provided via configuration file or command-line options from the megatools suite."],["CAVEATS","Mega.nz link formats change occasionally; ensure megatools is up to date. Large downloads may be subject to Mega's transfer quotas. Password-protected links require the decryption key in the URL or entered interactively."],["SEE ALSO","[wget](/man/wget)(1), [curl](/man/curl)(1), [aria2c](/man/aria2c)(1)"]],"e":["wget","curl","aria2c"],"x":"megatools-dl [https://mega.nz/...]"},"mkfs.bcachefs":{"n":"mkfs.bcachefs","t":"creates a bcachefs filesystem on one or more devices","s":[["TAGLINE","creates a bcachefs filesystem on one or more devices"],["TLDR","Create a **bcachefs filesystem** on a partition\n\n```sudo mkfs.bcachefs [/dev/sdXY]```\n\nCreate a bcachefs filesystem with a **volume label**\n\n```sudo mkfs.bcachefs -L [volume_label] [/dev/sdXY]```\n\nCreate a bcachefs filesystem with **zstd compression** enabled\n\n```sudo mkfs.bcachefs --compression=zstd [/dev/sdXY]```\n\nCreate a bcachefs filesystem with **encryption** (prompts for passphrase)\n\n```sudo mkfs.bcachefs --encrypted [/dev/sdXY]```\n\nCreate a **multi-device** bcachefs filesystem with 2 replicas\n\n```sudo mkfs.bcachefs --replicas=2 [/dev/sdX] [/dev/sdY]```\n\nForce creation even if the device **already contains a filesystem**\n\n```sudo mkfs.bcachefs -f [/dev/sdXY]```"],["SYNOPSIS","**mkfs.bcachefs** [_options_] _devices_..."],["PARAMETERS","**-L, --fs_label=_label_**\n> Set the filesystem volume label\n\n**-U, --uuid=_uuid_**\n> Create the filesystem with the specified UUID\n\n**-f, --force**\n> Force the filesystem to be created, even if the device already contains a filesystem\n\n**-q, --quiet**\n> Only print errors\n\n**-v, --verbose**\n> Verbose filesystem initialization\n\n**--replicas=_n_**\n> Set both data and metadata replicas (shorthand for setting both at once)\n\n**--data_replicas=_n_**\n> Number of data replicas\n\n**--metadata_replicas=_n_**\n> Number of metadata replicas\n\n**--metadata_checksum=_type_**\n> Metadata checksum algorithm (none, crc32c, crc64, xxhash); default: crc32c\n\n**--data_checksum=_type_**\n> Data checksum algorithm (none, crc32c, crc64, xxhash); default: crc32c\n\n**--compression=_type_**\n> Foreground compression algorithm (none, lz4, gzip, zstd); default: none\n\n**--background_compression=_type_**\n> Background compression algorithm applied to existing data (none, lz4, gzip, zstd)\n\n**--encrypted**\n> Enable whole-filesystem encryption (chacha20/poly1305); passphrase will be prompted for\n\n**--no_passphrase**\n> When used with --encrypted, do not encrypt the master encryption key\n\n**--nocow**\n> Enable no-copy-on-write mode: writes are done in-place when possible. Implicitly disables checksumming, compression, and encryption. Snapshots and reflinks still use COW.\n\n**--errors=_action_**\n> Action to take on filesystem error: continue, ro, or panic\n\n**--metadata_target=_target_**\n> Device or label to prefer for metadata writes (used in multi-device tiered setups)\n\n**--foreground_target=_target_**\n> Device or label to prefer for foreground (synchronous) writes\n\n**--background_target=_target_**\n> Device or label to move data to in the background\n\n**--promote_target=_target_**\n> Device or label to promote data to on read (SSD caching tier)\n\n**--discard**\n> Enable discard/TRIM support on the device\n\n**--fs_size=_size_**\n> Limit the filesystem to _size_ bytes on the subsequent device\n\n**--bucket=_size_**\n> Bucket size for the device; must be greater than the btree node size\n\n**--durability=_n_**\n> Consider data written to this device as already replicated _n_ times\n\n**--btree_node_size=_size_**\n> Btree node size; default: 256k\n\n**--block_size=_size_**\n> Filesystem block size in bytes (e.g. 4k)\n\n**--acl**\n> Enable POSIX ACL support\n\n**--usrquota**\n> Enable user quotas\n\n**--grpquota**\n> Enable group quotas\n\n**--prjquota**\n> Enable project quotas"],["DESCRIPTION","**mkfs.bcachefs** creates a bcachefs filesystem on one or more devices. It is equivalent to **bcachefs format**. Bcachefs is a modern copy-on-write filesystem for Linux with features including checksumming, compression, encryption, snapshots, reflinks, and multi-device support with storage tiering.\n\nThe filesystem can span multiple devices with configurable replication, providing RAID-like redundancy without a separate volume manager. Different storage tiers (e.g. fast NVMe SSD and slow HDD) can be combined in a single filesystem using the target options (--foreground_target, --promote_target, --background_target).\n\nAll persistent filesystem-wide options can be set at format time and many can also be changed later with **bcachefs set-fs-option**."],["CAVEATS","**Destroys all data** on the target devices. Use **-f** to override the safety check if a device already has a filesystem. Encryption requires setting a passphrase at creation time; if lost, data cannot be recovered. The **--nocow** flag disables checksums and compression and cannot be selectively reversed per-file. The **--erasure_code** option exists but is not yet safe for use."],["HISTORY","Bcachefs was developed by Kent Overstreet, evolving from the bcache block caching layer. After years of out-of-tree development, it was merged into the Linux kernel mainline in version 6.7 (released December 2023)."],["INSTALL","```dnf: sudo dnf install bcachefs-tools```\n\n```pacman: sudo pacman -S bcachefs-tools```\n\n```apk: sudo apk add bcachefs-tools```\n\n```nix: nix profile install nixpkgs#bcachefs-tools```"],["SEE ALSO","[bcachefs](/man/bcachefs)(8), [bcachefs-device](/man/bcachefs-device)(8), [mkfs](/man/mkfs)(8), [mount](/man/mount)(8), [blkid](/man/blkid)(8), [cryptsetup](/man/cryptsetup)(8)"]],"e":["bcachefs","bcachefs-device","mkfs","mount","blkid","cryptsetup"],"x":"sudo mkfs.bcachefs [/dev/sdXY]"},"msguniq":{"n":"msguniq","t":"removes duplicate translations from PO files","s":[["TAGLINE","removes duplicate translations from PO files"],["TLDR","**Remove duplicate messages**\n\n```msguniq [input.po] -o [output.po]```\n\n**Use first occurrence**\n\n```msguniq -u [input.po] -o [output.po]```\n\n**Report duplicates**\n\n```msguniq -d [input.po]```\n\n**Sort output**\n\n```msguniq -s [input.po] -o [output.po]```\n\n**Unique by msgid and msgctxt**\n\n```msguniq --use-first [input.po] -o [output.po]```"],["SYNOPSIS","**msguniq** [_options_] [_inputfile_]"],["PARAMETERS","_INPUTFILE_\n> PO file to process.\n\n**-o** _FILE_\n> Output file.\n\n**-u**\n> Keep only unique messages.\n\n**-d**\n> Report duplicate messages.\n\n**-s**\n> Sort output.\n\n**--use-first**\n> Use first of duplicates.\n\n**--help**\n> Display help information."],["DESCRIPTION","**msguniq** removes duplicate translations from PO files. It's part of GNU gettext.\n\nThe tool unifies message entries. Handles duplicates by combining or selecting."],["CAVEATS","Part of gettext-tools. Different duplicates merged. May need manual review."],["HISTORY","msguniq is part of **GNU gettext**, helping maintain clean translation files."],["INSTALL","```apt: sudo apt install gettext```\n\n```dnf: sudo dnf install gettext```\n\n```pacman: sudo pacman -S gettext```\n\n```apk: sudo apk add gettext```\n\n```brew: brew install gettext```\n\n```nix: nix profile install nixpkgs#gettext```"],["SEE ALSO","[msgcat](/man/msgcat)(1), [msgmerge](/man/msgmerge)(1), [msgfilter](/man/msgfilter)(1)"]],"e":["msgcat","msgmerge","msgfilter"],"x":"msguniq [input.po] -o [output.po]"},"netris":{"n":"netris","t":"network-capable Tetris game","s":[["TAGLINE","network-capable Tetris game"],["TLDR","**Play Tetris solo**\n\n```netris```\n\n**Host networked game**\n\n```netris -w```\n\n**Connect to host**\n\n```netris -c [host]```\n\n**Set difficulty**\n\n```netris -l [5]```\n\n**Two player local**\n\n```netris -2```"],["SYNOPSIS","**netris** [_options_]"],["PARAMETERS","**-w**\n> Wait for an opponent (server mode).\n\n**-c** _HOST_\n> Connect to opponent at _HOST_ (client mode).\n\n**-p** _PORT_\n> Use the given TCP port (default 9284).\n\n**-l** _LEVEL_\n> Starting level (1–9).\n\n**-s** _SPEED_\n> Initial drop speed (higher is faster).\n\n**-k** _KEYS_\n> Use a custom keyset string.\n\n**-F**\n> \"Fair\" mode — both players get the same piece sequence.\n\n**-D**\n> Do not show the next piece preview.\n\n**-i** _SECS_\n> Request a game invitation timeout.\n\n**-r** _PATH_\n> Run a robot opponent executable.\n\n**-S** _FILE_\n> Save (or replay) the game session to/from _FILE_.\n\n**-2**\n> Two-player local mode on the same terminal.\n\n**-h**\n> Display help information."],["DESCRIPTION","**netris** is a network-capable Tetris game. Play solo or against others over network.\n\nThe tool provides classic Tetris gameplay. Supports head-to-head competition."],["CAVEATS","Terminal game. Network play requires setup. Classic Tetris rules."],["HISTORY","netris was created to provide **networked Tetris** gameplay over the internet using TCP/IP."],["INSTALL","```brew: brew install netris```\n\n```nix: nix profile install nixpkgs#netris```"],["SEE ALSO","[bastet](/man/bastet)(1), [tetrinet](/man/tetrinet)(1), [bsdgames](/man/bsdgames)(1)"]],"e":["bastet","tetrinet","bsdgames"],"x":"netris"},"nrm":{"n":"nrm","t":"npm registry manager","s":[["TAGLINE","npm registry manager"],["TLDR","**List registries**\n\n```nrm ls```\n\n**Use registry**\n\n```nrm use [taobao]```\n\n**Add custom registry**\n\n```nrm add [name] [url]```\n\n**Delete registry**\n\n```nrm del [name]```\n\n**Test registry speed**\n\n```nrm test```\n\n**Show current registry**\n\n```nrm current```"],["SYNOPSIS","**nrm** [_command_] [_options_]"],["PARAMETERS","_COMMAND_\n> nrm subcommand.\n\n**ls**\n> List registries.\n\n**use** _NAME_\n> Switch registry.\n\n**add** _NAME_ _URL_\n> Add registry.\n\n**del** _NAME_\n> Remove registry.\n\n**test**\n> Test speeds.\n\n**--help**\n> Display help information."],["DESCRIPTION","**nrm** is npm registry manager. Quickly switch between npm registries.\n\nThe tool manages registry URLs. Includes common registries preset."],["CAVEATS","Node.js tool. Install with npm install -g. Modifies .npmrc."],["HISTORY","nrm was created to **easily switch** between different npm registries."],["INSTALL","```brew: brew install nrm```\n\n```nix: nix profile install nixpkgs#nrm```"],["SEE ALSO","[npm](/man/npm)(1), [npm-config](/man/npm-config)(1), [yarn](/man/yarn)(1)"]],"e":["npm","npm-config","yarn"],"x":"nrm ls"},"nuttcp":{"n":"nuttcp","t":"network performance measurement tool","s":[["TAGLINE","network performance measurement tool"],["TLDR","**Start server**\n\n```nuttcp -S```\n\n**Test throughput to server**\n\n```nuttcp [server_ip]```\n\n**Test UDP throughput**\n\n```nuttcp -u [server_ip]```\n\n**Specify bandwidth limit**\n\n```nuttcp -R [100M] [server_ip]```\n\n**Test for specific duration**\n\n```nuttcp -T [30] [server_ip]```\n\n**Receiver mode** (reverse direction test)\n\n```nuttcp -r [server_ip]```\n\n**Use multiple parallel streams**\n\n```nuttcp -N [4] [server_ip]```\n\n**Test with interval reporting** every second\n\n```nuttcp -i [1] [server_ip]```"],["SYNOPSIS","**nuttcp** [_options_] [_host_]"],["PARAMETERS","**-S**\n> Server mode.\n\n**-u**\n> UDP mode (default TCP).\n\n**-R** _rate_\n> Rate limit (K, M, G).\n\n**-T** _time_\n> Test duration.\n\n**-t**\n> Transmitter mode.\n\n**-r**\n> Receiver mode.\n\n**-p** _port_\n> Data port.\n\n**-P** _port_\n> Control port.\n\n**-w** _size_\n> Window size (socket buffer size).\n\n**-N** _nstream_\n> Number of parallel data streams (default 1, max 128).\n\n**-i** _interval_\n> Interval reporting in seconds.\n\n**-l** _buflen_\n> Length of network read/write buffer in bytes.\n\n**-s**\n> Use stdin/stdout for data (enables disk-to-network transfers).\n\n**-F**\n> Flip direction of data connection open (useful with firewalls/NAT).\n\n**-b**\n> Brief one-line output (default format)."],["DESCRIPTION","**nuttcp** is a network performance measurement tool. It measures TCP and UDP throughput between systems, providing detailed statistics about network performance.\n\nnuttcp is similar to iperf but includes features such as server mode, rate limiting, multiple parallel streams, and timer-based usage. It also supports IPv6, IPv4 multicast, and setting TOS/DSCP bits."],["EXAMPLE OUTPUT","```\n   84.8594 MB /  10.00 sec =   71.1477 Mbps 0 retrans\n```"],["CAVEATS","Requires server on remote end. Firewall must allow ports. UDP tests may show packet loss. Results affected by other traffic."],["HISTORY","nuttcp was developed by **Bill Fink** at NLANR/DAST as a network testing tool, building on the earlier ttcp."],["INSTALL","```apt: sudo apt install nuttcp```\n\n```dnf: sudo dnf install nuttcp```\n\n```apk: sudo apk add nuttcp```\n\n```brew: brew install nuttcp```\n\n```nix: nix profile install nixpkgs#nuttcp```"],["SEE ALSO","[iperf3](/man/iperf3)(1), [iperf](/man/iperf)(1), [netperf](/man/netperf)(1), [tc](/man/tc)(8)"]],"e":["iperf3","iperf","netperf","tc"],"x":"nuttcp -S"},"nvtop":{"n":"nvtop","t":"ncurses-based GPU process monitor, similar to htop but for GPUs","s":[["TAGLINE","ncurses-based GPU process monitor, similar to htop but for GPUs"],["TLDR","**Monitor all GPUs**\n\n```nvtop```\n\n**Monitor specific GPU**\n\n```nvtop -i [0]```\n\n**Monitor multiple GPUs**\n\n```nvtop -i [0,1,2]```\n\n**Delay between updates**\n\n```nvtop -d [2]```\n\n**No color output**\n\n```nvtop -C```"],["SYNOPSIS","**nvtop** [_options_]"],["DESCRIPTION","**nvtop** is an ncurses-based GPU process monitor, similar to htop but for GPUs. It displays real-time GPU utilization, memory usage, temperature, and running processes.\n\nThe tool supports a wide range of accelerators: NVIDIA (Kepler and newer, proprietary driver), AMD (amdgpu, kernel 5.14+), Intel (i915/Xe, kernel 5.19+), Qualcomm Adreno (msm, kernel 6.0+), Apple Silicon (M1/M2, experimental), Broadcom VideoCore on the Raspberry Pi, Rockchip NPUs, Huawei Ascend, MetaX and Enflame cards."],["PARAMETERS","**-i** _ids_\n> GPU IDs to monitor.\n\n**-d** _seconds_\n> Delay between updates.\n\n**-s** _sort_\n> Sort by (pid, type, rate, mem, etc.).\n\n**-C**\n> No color.\n\n**-p**\n> Show only processes.\n\n**-f** _file_\n> Log to file.\n\n**--no-header**\n> Hide header.\n\n**-h**, **--help**\n> Show help and exit.\n\n**-v**, **--version**\n> Print version information and exit."],["KEYBOARD SHORTCUTS","**F1/q**: Quit\n**F2**: Setup (interactive options window)\n**F3**: Search\n**F4**: Filter\n**F6**: Sort\n**F9**: Kill process\n**F12**: Save preferences for future sessions"],["CAVEATS","NVIDIA needs driver 410+. AMD needs ROCm. Intel needs i915 driver. Feature support varies by GPU vendor."],["HISTORY","**nvtop** was created by **Maxime Schmitt** to provide an htop-like experience for GPU monitoring. It started with NVIDIA support and later added AMD and Intel GPU support."],["INSTALL","```apt: sudo apt install nvtop```\n\n```dnf: sudo dnf install nvtop```\n\n```pacman: sudo pacman -S nvtop```\n\n```zypper: sudo zypper install nvtop```\n\n```brew: brew install nvtop```"],["SEE ALSO","[nvidia-smi](/man/nvidia-smi)(1), [gpustat](/man/gpustat)(1), [htop](/man/htop)(1), [radeontop](/man/radeontop)(1), [amdgpu_top](/man/amdgpu_top)(1)"]],"e":["nvidia-smi","gpustat","htop","radeontop","amdgpu_top"],"x":"nvtop"},"oxipng":{"n":"oxipng","t":"multithreaded PNG optimization tool written in Rust","s":[["TAGLINE","multithreaded PNG optimization tool written in Rust"],["TLDR","**Optimize PNG file**\n\n```oxipng [image.png]```\n\n**Optimize with maximum compression**\n\n```oxipng -o max [image.png]```\n\n**Optimize and write to a different file**\n\n```oxipng --out [output.png] [input.png]```\n\n**Optimize all PNGs in directory**\n\n```oxipng [*.png]```\n\n**Strip all metadata**\n\n```oxipng --strip all [image.png]```\n\n**Use multiple threads**\n\n```oxipng -t [4] [image.png]```\n\n**Reduce to 8-bit palette if possible**\n\n```oxipng --reduce [image.png]```\n\n**Dry run** (show savings without writing)\n\n```oxipng --pretend [image.png]```"],["SYNOPSIS","**oxipng** [_-o level_] [_--strip mode_] [_-t threads_] [_--reduce_] [_options_] _files_"],["PARAMETERS","**-o**, **--opt** _LEVEL_\n> Optimization level (0-6, max).\n\n**-s**, **--strip** _MODE_\n> Strip metadata: none, safe (non-rendering metadata), all (everything non-critical). -s alone is shorthand for --strip safe.\n\n**-a**, **--alpha**\n> Additional alpha optimizations.\n\n**-t**, **--threads** _NUM_\n> Number of threads.\n\n**--out** _DIR_\n> Output directory.\n\n**--dir** _DIR_\n> Recursively process directory.\n\n**-r**, **--recursive**\n> Process directories recursively.\n\n**--reduce**\n> Attempt bit depth reduction.\n\n**--interlace** _MODE_\n> Interlacing: 0 (none), 1 (Adam7), keep.\n\n**--pretend**\n> Dry run, show savings without writing.\n\n**-b**, **--backup**\n> Keep backup of original file.\n\n**-f** _NUM_, **--filters** _NUM_\n> Filter strategies to try (0=None, 1=Sub, 2=Up, 3=Average, 4=Paeth, 5=MinSum, 6=Entropy, 7=Bigrams, 8=BigEnt, 9=Brute). Supports comma-separated lists or ranges (e.g., 0-5).\n\n**-Z**, **--zopfli**\n> Use Zopfli for better compression (much slower).\n\n**--timeout** _seconds_\n> Maximum time to spend optimizing a single file.\n\n**--check**\n> Verify output integrity.\n\n**-q**, **--quiet**\n> Quiet mode.\n\n**-v**, **--verbose**\n> Verbose output."],["DESCRIPTION","**oxipng** is a multithreaded PNG optimization tool written in Rust. It reduces PNG file sizes through lossless recompression without affecting image quality.\n\nOptimization levels (0-6) control the trade-off between compression and speed. Level 2 is default, providing good compression quickly. Level 6 and \"max\" try more strategies for maximum compression but take longer.\n\nThe tool tries different PNG filter strategies and compression parameters to find the smallest output. Metadata stripping further reduces size - \"safe\" removes non-essential chunks, \"all\" removes everything except critical image data.\n\nBit depth reduction automatically converts 16-bit to 8-bit or RGBA to palette when possible without quality loss. This can significantly reduce file sizes for suitable images.\n\nZopfli mode uses a more exhaustive compression algorithm for ~5% better compression at the cost of much longer processing time. This is useful for images that will be served repeatedly."],["CAVEATS","Cannot reduce file size if PNG is already optimally compressed. Zopfli mode is very slow. Maximum optimization level uses significant CPU time. Some PNG features may not be fully supported. Large images need substantial memory."],["HISTORY","**oxipng** was created by **Josh Holmer** around **2016** as a Rust rewrite of OptiPNG. The goal was to provide a faster, multithreaded PNG optimizer with modern code. It has become popular for build pipelines and asset optimization, particularly in web development where PNG size directly affects page load times."],["INSTALL","```dnf: sudo dnf install oxipng```\n\n```pacman: sudo pacman -S oxipng```\n\n```apk: sudo apk add oxipng```\n\n```zypper: sudo zypper install oxipng```\n\n```brew: brew install oxipng```\n\n```nix: nix profile install nixpkgs#oxipng```"],["SEE ALSO","[optipng](/man/optipng)(1), [pngcrush](/man/pngcrush)(1), [pngquant](/man/pngquant)(1), [zopflipng](/man/zopflipng)(1)"]],"e":["optipng","pngcrush","pngquant","zopflipng"],"x":"oxipng [image.png]"},"pacman-q":{"n":"pacman-q","t":"short form of **pacman --query**, which queries the local package database","s":[["TAGLINE","short form of **pacman --query**, which queries the local package database"],["TLDR","This command is an alias of **pacman --query**\n\nView documentation for the original command\n\n```tldr pacman query```"],["SYNOPSIS","**pacman -Q** [_options_] [_package(s)_]"],["DESCRIPTION","**pacman -Q** is the short form of **pacman --query**, which queries the local package database. It allows listing installed packages, checking package information, and querying files owned by packages."],["INSTALL","```apt: sudo apt install pacman-package-manager```\n\n```dnf: sudo dnf install pacman```\n\n```pacman: sudo pacman -S pacman```\n\n```apk: sudo apk add pacman```\n\n```nix: nix profile install nixpkgs#pacman```"],["SEE ALSO","[pacman](/man/pacman)(8), [pacman-query](/man/pacman-query)(8)"]],"e":["pacman","pacman-query"],"x":"tldr pacman query"},"pacman4console":{"n":"pacman4console","t":"text-based console game inspired by the original Pacman arcade game","s":[["TAGLINE","text-based console game inspired by the original Pacman arcade game"],["TLDR","Start a game at **Level 1**\n\n```pacman4console```\n\nStart a game on a **certain level** (there are nine official levels)\n\n```pacman4console --level=5```\n\nStart the **level editor**, saving to a specified file\n\n```pacman4consoleedit [path/to/level_file]```\n\nPlay a **custom level**\n\n```pacman4console --level=path/to/level_file```"],["SYNOPSIS","**pacman4console** [_options_]"],["PARAMETERS","**--level** _level_\n> Start on specified level number (1-9) or custom level file path"],["PREVIEW","```\n ##############\n #o....#......#\n #.###.#.####.#\n #.#...........#\n #.#.#.###.#.#.#\n #C..#.....#...#\n ##############\n```"],["DESCRIPTION","**pacman4console** is a text-based console game inspired by the original Pacman arcade game. It runs in the terminal using ASCII characters to render the maze, ghosts, and player character.\n\nThe game includes nine official levels and supports custom levels through the pacman4consoleedit level editor.\n\nUse the arrow keys or WASD to move Pacman through the maze. Collect all dots to complete a level. Avoid ghosts or eat power pellets to temporarily chase them. Press **q** to quit during gameplay."],["CAVEATS","Requires a terminal that supports the ncurses library. Terminal size must be sufficient to display the game maze properly."],["HISTORY","Created as a terminal-based recreation of the classic Pacman game, designed to run on console and embedded systems."],["INSTALL","```aur: yay -S pacman4console```"],["SEE ALSO","[sl](/man/sl)(1), [moon-buggy](/man/moon-buggy)(1), [nsnake](/man/nsnake)(1), [ninvaders](/man/ninvaders)(1), [2048](/man/2048)(1)"]],"e":["sl","moon-buggy","nsnake","ninvaders","2048"],"x":"pacman4console"},"pampop9":{"n":"pampop9","t":"creates Pop Art style image","s":[["TAGLINE","creates Pop Art style image"],["TLDR","**Apply Pop Art effect**\n\n```pampop9 [input.pam] > [output.pam]```\n\n**Create 3x3 color variations**\n\n```pampop9 [photo.pam] > [popart.pam]```"],["SYNOPSIS","**pampop9** [_file_]"],["PARAMETERS","_FILE_\n> Input PAM/PNM image."],["DESCRIPTION","**pampop9** creates Pop Art style image. Generates 3x3 grid of color variations.\n\nThe tool produces Andy Warhol style output. Part of Netpbm."],["CAVEATS","Part of Netpbm. Output 3x original size. Artistic effect."],["HISTORY","pampop9 is part of **Netpbm** for creating Pop Art style color variations."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[ppmquant](/man/ppmquant)(1), [netpbm](/man/netpbm)(1)"]],"e":["ppmquant","netpbm"],"x":"pampop9 [input.pam] > [output.pam]"},"pct-listsnapshot":{"n":"pct-listsnapshot","t":"list snapshots of a Proxmox LXC container","s":[["TAGLINE","list snapshots of a Proxmox LXC container"],["TLDR","**List** all snapshots for a container\n\n```pct listsnapshot [vmid]```"],["SYNOPSIS","**pct listsnapshot** _vmid_"],["PARAMETERS","**vmid**\n> Numeric ID of the container (100–999999999)"],["DESCRIPTION","**pct listsnapshot** lists all snapshots for a Proxmox VE LXC container. Output typically includes snapshot names and related metadata so you can choose a snapshot for **pct rollback** or **pct delsnapshot**."],["HISTORY","**pct listsnapshot** is part of the **Proxmox VE** **pct** tool for managing LXC containers."],["SEE ALSO","[pct](/man/pct)(1), [pct-delsnapshot](/man/pct-delsnapshot)(1), [pct-rollback](/man/pct-rollback)(1)"],["RESOURCES","```[Documentation](https://pve.proxmox.com/pve-docs/pct.1.html)```\n\n```[Homepage](https://www.proxmox.com)```"]],"e":["pct","pct-delsnapshot","pct-rollback"],"x":"pct listsnapshot [vmid]"},"pipenv":{"n":"pipenv","t":"Python dependency management with virtual environments","s":[["TAGLINE","Python dependency management with virtual environments"],["TLDR","**Create virtual environment and install packages**\n\n```pipenv install [requests]```\n\n**Install dev dependencies**\n\n```pipenv install --dev [pytest]```\n\n**Install from Pipfile**\n\n```pipenv install```\n\n**Activate virtual environment**\n\n```pipenv shell```\n\n**Run command in virtual environment**\n\n```pipenv run [python script.py]```\n\n**Generate requirements.txt**\n\n```pipenv requirements > requirements.txt```\n\n**Show dependency graph**\n\n```pipenv graph```\n\n**Remove virtual environment**\n\n```pipenv --rm```"],["SYNOPSIS","**pipenv** _command_ [_options_] [_packages_]"],["PARAMETERS","**install** [_packages_]\n> Install packages and add to Pipfile.\n\n**uninstall** _packages_\n> Remove packages from environment.\n\n**lock**\n> Generate Pipfile.lock.\n\n**sync**\n> Install packages from Pipfile.lock.\n\n**shell**\n> Spawn shell in virtual environment.\n\n**run** _command_\n> Run command in virtual environment.\n\n**graph**\n> Show dependency graph.\n\n**check**\n> Check for security vulnerabilities.\n\n**requirements**\n> Generate requirements.txt output.\n\n**update** [_packages_]\n> Update packages.\n\n**clean**\n> Remove packages not in Pipfile.lock.\n\n**--dev**, **-d**\n> Include dev dependencies.\n\n**--python** _VERSION_\n> Specify Python version.\n\n**--venv**\n> Show virtual environment path.\n\n**--rm**\n> Remove virtual environment.\n\n**--where**\n> Show project home.\n\n**--three** / **--two**\n> Use Python 3 / Python 2.\n\n**--site-packages**\n> Enable system site-packages."],["DESCRIPTION","**pipenv** combines pip and virtualenv into a single workflow. It manages project dependencies with Pipfile/Pipfile.lock files, similar to npm's package.json or Ruby's Bundler.\n\nThe Pipfile replaces requirements.txt with a more structured format. It separates production and development dependencies. The Pipfile.lock ensures deterministic builds by pinning exact versions and hashes.\n\nVirtual environments are created automatically per project. The shell command activates the environment, while run executes commands within it without manual activation. Environment location is determined by the project directory.\n\nSecurity features include hash verification of packages and vulnerability scanning via check command. The graph command visualizes the dependency tree, helping understand transitive dependencies.\n\nPipenv integrates with .env files, automatically loading environment variables. This keeps secrets separate from code while making them available during development."],["CONFIGURATION","**Pipfile**\n> Project dependency specification file in the project root.\n\n**Pipfile.lock**\n> Lockfile with pinned versions and hashes for reproducible installs.\n\n**.env**\n> Environment variables file automatically loaded by pipenv.\n\n**PIPENV_VENV_IN_PROJECT**\n> Create virtual environment inside project directory when set.\n\n**PIPENV_NO_INHERIT**\n> Do not inherit parent shell's environment variables.\n\n**PIPENV_CACHE_DIR**\n> Override the default pip cache directory location."],["CAVEATS","Lock resolution can be slow for complex dependencies. Not as fast as newer tools like Poetry or uv. Hash verification requires packages have published hashes. Some edge cases in dependency resolution. May conflict with system-wide pip packages."],["HISTORY","**pipenv** was created by **Kenneth Reitz** (author of requests) around **2017**. It aimed to bring the developer experience of npm/Bundler to Python. PyPA endorsed it as a recommended tool. While newer alternatives like Poetry have emerged, pipenv remains widely used for its simplicity and integration with pip."],["INSTALL","```brew: brew install pipenv```\n\n```nix: nix profile install nixpkgs#pipenv```"],["SEE ALSO","[pip](/man/pip)(1), [poetry](/man/poetry)(1), [virtualenv](/man/virtualenv)(1), [uv](/man/uv)(1)"]],"e":["pip","poetry","virtualenv","uv"],"x":"pipenv install [requests]"},"plandex":{"n":"plandex","t":"AI coding agent for complex multi-file tasks","s":[["TAGLINE","AI coding agent for complex multi-file tasks"],["TLDR","**Start the interactive REPL**\n\n```plandex```\n\n**Describe a task or ask a question**\n\n```plandex tell \"[add a cancel button to the left of the submit button]\"```\n\n**Load files into context**\n\n```plandex load [path/to/file.py]```\n\n**Apply pending changes to project files**\n\n```plandex apply```\n\n**Apply changes and commit to git**\n\n```plandex tell -a -c \"[fix the authentication bug]\"```\n\n**View plan history**\n\n```plandex log```\n\n**Rewind to a previous step**\n\n```plandex rewind [commit_hash]```\n\n**Use the shorthand alias**\n\n```pdx tell \"[your prompt]\"```"],["SYNOPSIS","**plandex** [_command_] [_options_]\n\n**pdx** [_command_] [_options_]"],["PARAMETERS","**tell**, **t**\n> Describe a task, ask a question, or chat\n\n**load**, **l**\n> Load files, directories, URLs, notes, or piped data into context\n\n**apply**, **ap**\n> Apply plan changes to project files\n\n**rewind**\n> Go back to a previous step in the plan history\n\n**log**\n> View the plan's history of changes"],["TELL OPTIONS","**-f** _FILE_\n> Read prompt from a file\n\n**-a**, **--apply**\n> Automatically apply changes after generation\n\n**-c**, **--commit**\n> Commit changes to git when used with --apply\n\n**--auto-load-context**\n> Automatically load context using project map"],["DESCRIPTION","**Plandex** is an open-source AI coding agent designed for large projects and real-world tasks. It can plan and execute complex coding tasks that span many steps and touch dozens of files, handling up to 2 million tokens of context directly.\n\nThe tool uses a cumulative diff review sandbox that keeps AI-generated changes separate from your project files until you're ready to apply them. This allows easy rollback and debugging. Plandex supports multiple AI providers including Anthropic, OpenAI, Google, and open-source models.\n\nKey features include automated debugging of terminal commands, fast project map generation with tree-sitter (30+ languages), and configurable autonomy levels from full automation to step-by-step review. The REPL mode offers fuzzy auto-complete for commands and file loading."],["CONFIGURATION","**~/.plandex-home/**\n> Plandex data directory for plans, context, and settings.\n\n**OPENAI_API_KEY**\n> API key for OpenAI models.\n\n**ANTHROPIC_API_KEY**\n> API key for Anthropic Claude models."],["CAVEATS","Windows is only supported via WSL; the tool does not work in CMD or PowerShell. Requires API keys from supported providers. Large context operations may consume significant tokens. The sandbox approach means changes are not immediately reflected in project files until applied."],["HISTORY","Plandex was founded by **Dane Schneider** in **2024** in San Francisco. The open-source release was announced in **April 2024**. Schneider previously founded EnvkeyConfig (YC W18). The project has gained over 14,000 GitHub stars and is designed to address the challenges of AI-assisted development on large codebases."],["SEE ALSO","[claude](/man/claude)(1), [aider](/man/aider)(1), [codex](/man/codex)(1), [opencode](/man/opencode)(1)"]],"e":["claude","aider","codex","opencode"],"x":"plandex"},"pmap":{"n":"pmap","t":"Report memory map of a process","s":[["TAGLINE","Report memory map of a process"],["TLDR","Print **memory map** for a process\n\n```pmap 1234```\n\nShow the **extended format**\n\n```pmap -x 1234```\n\nShow the **device format**\n\n```pmap -d 1234```\n\nLimit results to a **memory address range**\n\n```pmap -A low,high 1234```\n\nPrint memory maps for **multiple processes**\n\n```pmap 1234 5678```\n\nShow **all kernel-provided** information\n\n```pmap -XX 1234```"],["SYNOPSIS","**pmap** [_options_] _pid_ [...]"],["DESCRIPTION","**pmap** reports the memory map of a process or processes. It displays how memory is allocated and used by each process, including mapped files, heap, stack, and shared libraries."],["PARAMETERS","**-x, --extended**\n> Display extended format with additional details\n\n**-d, --device**\n> Show device format output\n\n**-q, --quiet**\n> Suppress header and footer lines\n\n**-A, --range low,high**\n> Restrict results to specified address range\n\n**-X**\n> Provide additional details beyond -x option\n\n**-XX**\n> Display all kernel-provided information\n\n**-p, --show-path**\n> Include full file paths in mapping column\n\n**-c, --read-rc**\n> Load default configuration\n\n**-C, --read-rc-from file**\n> Load configuration from specified file\n\n**-n, --create-rc**\n> Generate new default configuration\n\n**-N, --create-rc-to file**\n> Generate configuration to specified file"],["CAVEATS","Exit status 0 indicates success, 1 indicates failure, and 42 means not all requested processes could be found. Memory values are shown in kilobytes by default."],["HISTORY","**pmap** is part of the **procps-ng** package, providing process filesystem utilities for Linux. It reads information from /proc/[pid]/maps and related files."],["INSTALL","```apt: sudo apt install procps```\n\n```dnf: sudo dnf install procps-ng```\n\n```pacman: sudo pacman -S procps-ng```\n\n```apk: sudo apk add procps-ng```\n\n```zypper: sudo zypper install procps```\n\n```brew: brew install procps```\n\n```nix: nix profile install nixpkgs#procps```"],["SEE ALSO","[ps](/man/ps)(1), [pgrep](/man/pgrep)(1), [proc](/man/proc)(5)"]],"e":["ps","pgrep","proc"],"x":"pmap 1234"},"pnmgamma":{"n":"pnmgamma","t":"Apply gamma correction to PNM images","s":[["TAGLINE","Apply gamma correction to PNM images"],["TLDR","**Apply gamma correction**\n\n```pnmgamma [gamma] [input.pnm] > [output.pnm]```\n\n**Brighten image**\n\n```pnmgamma 2.2 [input.pnm] > [output.pnm]```\n\n**Darken image**\n\n```pnmgamma 0.5 [input.pnm] > [output.pnm]```\n\n**Per-channel gamma**\n\n```pnmgamma [r_gamma] [g_gamma] [b_gamma] [input.pnm] > [output.pnm]```"],["SYNOPSIS","**pnmgamma** [_options_] _gamma_ [_file_]"],["PARAMETERS","_GAMMA_\n> Gamma correction value.\n\n_FILE_\n> Input PNM file.\n\n**-ungamma**\n> Remove gamma correction.\n\n**-cieramp**\n> Use CIE luminance ramp."],["DESCRIPTION","**pnmgamma** applies gamma correction to PNM images, adjusting the brightness curve by raising each pixel value to a power function. Values greater than 1 brighten the image while values less than 1 darken it.\n\nPer-channel gamma values can be specified separately for red, green, and blue. The **-ungamma** option reverses a previously applied gamma correction. Part of the Netpbm toolkit; superseded by **pamgamma**."],["CAVEATS","Values >1 brighten, <1 darken. Part of Netpbm suite."],["HISTORY","pnmgamma was created as part of **Netpbm** for gamma correction operations."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pamgamma](/man/pamgamma)(1), [pnmnorm](/man/pnmnorm)(1), [ppmchange](/man/ppmchange)(1)"]],"e":["pamgamma","pnmnorm","ppmchange"],"x":"pnmgamma [gamma] [input.pnm] > [output.pnm]"},"pw-reserve":{"n":"pw-reserve","t":"Reserve audio devices for exclusive use","s":[["TAGLINE","Reserve audio devices for exclusive use"],["TLDR","**Reserve** a device (audio devices only)\n\n```pw-reserve -n audio0```\n\n**Monitor** a device instead of reserving it\n\n```pw-reserve -n audio0 -m```\n\nDisplay **help**\n\n```pw-reserve -h```"],["SYNOPSIS","**pw-reserve** [_options_]"],["PARAMETERS","**-n**, **--name** _device_\n> Specify device name to reserve (e.g., audio0, audio1)\n\n**-m**, **--monitor**\n> Monitor device instead of reserving it\n\n**-h**, **--help**\n> Display help message"],["DESCRIPTION","**pw-reserve** reserves a device for exclusive use by PipeWire. This implements the D-Bus device reservation protocol, allowing PipeWire to coordinate device access with other audio servers.\n\nCurrently only audio devices are supported. Device reservation prevents conflicts when multiple audio systems attempt to access the same hardware."],["CAVEATS","Only audio devices are supported currently. Device names follow the audioN naming convention. Reservation requires D-Bus access."],["HISTORY","Part of **PipeWire**, the modern multimedia framework for Linux. Implements the org.freedesktop.ReserveDevice specification for audio device sharing."],["INSTALL","```apt: sudo apt install pipewire-bin```\n\n```apk: sudo apk add pipewire-tools```\n\n```zypper: sudo zypper install pipewire-tools```"],["SEE ALSO","[pipewire](/man/pipewire)(1), [pw-cli](/man/pw-cli)(1)"]],"e":["pipewire","pw-cli"],"x":"pw-reserve -n audio0"},"qm-wait":{"n":"qm-wait","t":"Wait for a Proxmox VM to stop","s":[["TAGLINE","Wait for a Proxmox VM to stop"],["TLDR","**Wait** until the virtual machine is stopped\n\n```qm wait [vmid]```\n\nWait with **timeout** of 60 seconds\n\n```qm wait --timeout 60 [vmid]```\n\n**Shutdown** then wait for VM to stop\n\n```qm shutdown [vmid] && qm wait --timeout 60 [vmid]```"],["SYNOPSIS","**qm wait** [_options_] _vmid_"],["PARAMETERS","**--timeout** _seconds_\n> Maximum time to wait synchronously before returning. Set to 0 to deactivate. If reached, the command returns with the PID."],["DESCRIPTION","**qm wait** blocks until a virtual machine reaches stopped state. This is useful in scripts to ensure a VM has fully stopped before performing subsequent operations.\n\nCombined with shutdown, it provides a way to gracefully stop VMs and wait for completion."],["CAVEATS","Without timeout, the command may block indefinitely if the VM doesn't stop. Always use timeout in scripts for predictable behavior."],["HISTORY","Part of **Proxmox VE** QEMU/KVM management tools for virtual machine administration."],["INSTALL","```dnf: sudo dnf install qm```"],["SEE ALSO","[qm](/man/qm)(1), [qm-shutdown](/man/qm-shutdown)(1), [qm-stop](/man/qm-stop)(1)"]],"e":["qm","qm-shutdown","qm-stop"],"x":"qm wait [vmid]"},"rdp_check.py":{"n":"rdp_check.py","t":"Test RDP authentication credentials","s":[["TAGLINE","Test RDP authentication credentials"],["TLDR","**Test RDP credentials** against a target\n\n```rdp_check.py [domain]/[username]:[password]@[target_ip]```\n\n**Test credentials using NTLM hashes**\n\n```rdp_check.py [domain]/[username]@[target_ip] -hashes [LMHASH]:[NTHASH]```\n\n**Test with debug output**\n\n```rdp_check.py [domain]/[username]:[password]@[target_ip] -debug```\n\n**Test over IPv6**\n\n```rdp_check.py [domain]/[username]:[password]@[target_ip] -6```"],["SYNOPSIS","**rdp_check.py** [**-h**] [**-hashes** _LMHASH:NTHASH_] [**-6**] [**-debug**] _target_"],["PARAMETERS","_target_\n> Target specification: [[domain/]username[:password]@]<hostname or IP>\n\n**-hashes** _LMHASH:NTHASH_\n> Authenticate using NTLM hashes instead of password\n\n**-6**, **--ipv6**\n> Connect using IPv6\n\n**-debug**\n> Enable debug output\n\n**-ts**\n> Add timestamps to log output\n\n**-h**, **--help**\n> Show help message"],["DESCRIPTION","**rdp_check.py** is an Impacket tool that tests whether credentials are valid against a Windows host via RDP (Remote Desktop Protocol). It implements portions of MS-RDPBCGR and MS-CREDSSP to perform CredSSP authentication.\n\nThe tool is useful for validating credentials during authorized security assessments, verifying RDP access, and testing pass-the-hash attacks against RDP-enabled systems.\n\nIt performs authentication only and does not establish a full RDP session. Successful authentication indicates the credentials are valid, but does not guarantee interactive desktop access (which may be restricted by group policy or user rights)."],["CAVEATS","**Authorization required**: Only use this tool against systems you have explicit permission to test. Unauthorized access attempts are illegal.\n\nThe tool does not complete a full RDP handshake, so successful authentication does not guarantee GUI access. Network Level Authentication (NLA) settings and user rights assignments may still prevent actual login.\n\nPass-the-hash attacks (**-hashes**) require the target to accept NTLM authentication. Modern configurations may restrict NTLM in favor of Kerberos.\n\nImpacket tools require Python and the impacket library. Install via **pip install impacket** or use Kali Linux which includes impacket-scripts."],["INSTALL","```apk: sudo apk add py3-impacket```"],["SEE ALSO","[xfreerdp](/man/xfreerdp)(1), [rdesktop](/man/rdesktop)(1), [nmap](/man/nmap)(1), [crackmapexec](/man/crackmapexec)(1)"]],"e":["xfreerdp","rdesktop","nmap","crackmapexec"],"x":"rdp_check.py [domain]/[username]:[password]@[target_ip]"},"reame":{"n":"reame","t":"CPU-first local LLM inference server with persistent caches","s":[["TAGLINE","CPU-first local LLM inference server with persistent caches"],["TLDR","**List** catalog models and what is already on disk\n\n```reame list```\n\n**Download**, autoconfigure, and **chat** with a model\n\n```reame run [qwen2.5-1.5b]```\n\nOne-shot **prompt**\n\n```reame run [qwen2.5-1.5b] \"[Explain mmap]\"```\n\nServe an **OpenAI-compatible API**\n\n```reame run [qwen2.5-1.5b] --serve```\n\nUse the **Conclave** (best-of-N majority) for a harder question\n\n```reame run [qwen2.5-1.5b] \"[question?]\" --best-of [5]```\n\nRun with an explicit **config** file\n\n```reame --config [config/reame.conf] --serve```"],["SYNOPSIS","**reame** [_options_]\n\n**reame** **run** _model_ [_prompt_] [_options_]\n\n**reame** **list**"],["DESCRIPTION","**reame** is an LLM inference server built on **llama.cpp**, optimized for cheap CPU hardware. Its design goal is to avoid recomputing work: shared prompt prefixes are snapshotted to disk, completed generations feed an on-disk archive used for drafting later requests, and optional speculative decoding adapts to measured acceptance.\n\n**reame run** resolves a catalog name or local GGUF path, downloads into **~/.reame/models** on first use, and picks threads, KV settings, and cache paths for the host. **--serve** exposes OpenAI-compatible **completions** and **chat/completions** (with SSE streaming), sessions, metrics, and health endpoints. Advanced features include interleaved multi-user batches, grammar-driven drafting, **--best-of** consensus (**Conclave**), and an optional **reame arca** shared memory daemon."],["PARAMETERS","**list**\n> Show model catalog and local downloads\n\n**run** _model_ [_prompt_]\n> Chat or one-shot; **--serve** for API mode; **--best-of** _N_ for majority voting\n\n**--config** _file_\n> Path to reame configuration (model path, threads, cache, server, speculative, …)\n\n**--prompt** / **--max-tokens** / **--serve**\n> Direct inference and server flags when not using **run**"],["CONFIGURATION","INI-style config sections include **[model]**, **[memory]** (KV quantization), **[speculative]**, **[cache]** (disk budget), **[server]** (port, API key, parallel users), and optional **[arca]** remote cache. Homebrew: **brew install reame** from the project's tap."],["CAVEATS","CPU-focused; not a general GPU or multi-model manager like Ollama. Quality is workload-dependent—best for extraction, classification, and repetitive domain tasks where caching pays off. Young project; pin versions for production."],["SEE ALSO","[ollama](/man/ollama)(1), [llama-cli](/man/llama-cli)(1)"],["RESOURCES","```[Source code](https://github.com/swellweb/reame)```\n\n```[Documentation](https://github.com/swellweb/reame/blob/main/docs)```"]],"e":["ollama","llama-cli"],"x":"reame list"},"rtl_sdr":{"n":"rtl_sdr","t":"Capture raw radio samples with RTL-SDR dongle","s":[["TAGLINE","Capture raw radio samples with RTL-SDR dongle"],["TLDR","**Capture to file**\n\n```rtl_sdr -f [100M] [capture.bin]```\n\n**Set sample rate**\n\n```rtl_sdr -f [100M] -s [2.4M] [capture.bin]```\n\n**Set gain**\n\n```rtl_sdr -f [100M] -g [40] [capture.bin]```\n\n**Capture duration**\n\n```rtl_sdr -f [100M] -n [1000000] [capture.bin]```\n\n**Frequency offset correction**\n\n```rtl_sdr -f [100M] -p [55] [capture.bin]```\n\n**Select device**\n\n```rtl_sdr -d [0] -f [100M] [capture.bin]```\n\n**Direct sampling mode**\n\n```rtl_sdr -f [3M] -D [2] [capture.bin]```"],["SYNOPSIS","**rtl_sdr** [_-f freq_] [_-s rate_] [_-g gain_] [_options_] _output_"],["PARAMETERS","**-f** _FREQ_\n> Center frequency (Hz).\n\n**-s** _RATE_\n> Sample rate (Hz).\n\n**-g** _GAIN_\n> Gain (0 for auto).\n\n**-n** _SAMPLES_\n> Number of samples.\n\n**-p** _PPM_\n> Frequency correction.\n\n**-d** _INDEX_\n> Device index.\n\n**-D** _MODE_\n> Direct sampling mode.\n\n**-S**\n> Enable synchronous mode."],["DESCRIPTION","**rtl_sdr** captures raw radio signals using RTL-SDR dongles, which are inexpensive USB devices based on the RTL2832U chipset originally designed for DVB-T television reception. By reading raw I/Q (in-phase/quadrature) samples from the device, it enables software-defined radio experimentation across a wide frequency range, typically 24 MHz to 1766 MHz depending on the tuner chip.\n\nThe tool writes raw 8-bit I/Q sample data to a file or stdout, which can then be processed by other applications to decode various signal types including FM radio, ADS-B aircraft tracking, weather satellites, and pager systems. The sample rate parameter controls the bandwidth captured, with higher rates covering more spectrum but requiring more USB bandwidth and storage.\n\nGain and frequency correction parameters help optimize reception quality. Setting gain to zero enables automatic gain control, while manual values allow fine-tuning for specific signals. The PPM correction compensates for the oscillator frequency error inherent in low-cost dongles, which can be calibrated against a known reference signal."],["CAVEATS","Requires RTL-SDR compatible device. USB bandwidth limits sample rate. Heat affects frequency stability."],["HISTORY","**rtl_sdr** is part of **librtlsdr**, enabling RTL2832U-based DVB-T dongles for SDR. Created by **Steve Markgraf** and others, it revolutionized accessible software-defined radio."],["INSTALL","```apt: sudo apt install rtl-sdr```\n\n```dnf: sudo dnf install rtl-sdr```\n\n```pacman: sudo pacman -S rtl-sdr```\n\n```apk: sudo apk add rtl-sdr```\n\n```zypper: sudo zypper install rtl-sdr```\n\n```brew: brew install rtl-sdr```\n\n```nix: nix profile install nixpkgs#rtl-sdr```"],["SEE ALSO","[rtl_fm](/man/rtl_fm)(1), [rtl_power](/man/rtl_power)(1), [gqrx](/man/gqrx)(1), [gnuradio](/man/gnuradio)(1)"]],"e":["rtl_fm","rtl_power","gqrx","gnuradio"],"x":"rtl_sdr -f [100M] [capture.bin]"},"runlim":{"n":"runlim","t":"Measure and limit resource usage of commands","s":[["TAGLINE","Measure and limit resource usage of commands"],["TLDR","Print **time and memory** usage of a command\n\n```runlim command arguments```\n\nLog statistics to a **file**\n\n```runlim -o path/to/file command arguments```\n\nLimit **CPU time** (seconds)\n\n```runlim -t 60 command arguments```\n\nLimit **real-time** (seconds)\n\n```runlim -r 120 command arguments```\n\nLimit **memory space** (MB)\n\n```runlim -s 1024 command arguments```\n\nCombine **time and space limits** with output logging\n\n```runlim -t 300 -s 2048 -o path/to/log command arguments```"],["SYNOPSIS","**runlim** [_options_] _command_ [_arguments_...]"],["PARAMETERS","**-o** _FILE_, **--output-file**=_FILE_\n> Overwrite or create FILE for output logging\n\n**-t** _NUM_, **--time-limit**=_NUM_\n> Set CPU time limit to NUM seconds\n\n**-r** _NUM_, **--real-time-limit**=_NUM_\n> Set real (wall clock) time limit to NUM seconds\n\n**-s** _NUM_, **--space-limit**=_NUM_\n> Set memory limit to NUM megabytes\n\n**-k**, **--kill**\n> Propagate signals to child processes\n\n**-h**, **--help**\n> Show summary of options\n\n**--version**\n> Show version of program"],["DESCRIPTION","**runlim** executes a command while sampling and limiting its time and memory usage, including all child processes. It samples resource consumption every 100 milliseconds via the /proc filesystem and logs status information to stderr every second.\n\nThe tool is useful for benchmarking, automated testing, and preventing runaway processes from consuming excessive resources."],["CAVEATS","Linux-specific; relies on /proc filesystem. Resource measurements are sampled, not exact."],["HISTORY","Written by **Armin Biere** and **Toni Jussila**. Commonly used in SAT solver competitions and automated testing environments."],["INSTALL","```apt: sudo apt install runlim```"],["SEE ALSO","[time](/man/time)(1), [timeout](/man/timeout)(1), [ulimit](/man/ulimit)(1)"]],"e":["time","timeout","ulimit"],"x":"runlim command arguments"},"shiny-mirrors":{"n":"shiny-mirrors","t":"Manage Manjaro Linux mirror lists","s":[["TAGLINE","Manage Manjaro Linux mirror lists"],["TLDR","Get **status** of current mirrors\n\n```shiny-mirrors status```\n\n**Generate** a mirror list using defaults\n\n```sudo shiny-mirrors refresh```\n\nDisplay current **configuration**\n\n```shiny-mirrors config show```\n\nSwitch to a different **branch** interactively\n\n```sudo shiny-mirrors config --branch```\n\nRefresh mirrors for a **specific country**\n\n```sudo shiny-mirrors refresh --country [country_code]```"],["SYNOPSIS","**shiny-mirrors** _command_ [_OPTIONS_]"],["PARAMETERS","**status**\n> Display current mirror status and health\n\n**refresh**\n> Generate and update the mirror list based on configured criteria\n\n**config show**\n> Display the current configuration file\n\n**config --branch**\n> Interactively switch to a different Manjaro branch (stable, testing, unstable)\n\n**--country** _code_\n> Filter mirrors by country code\n\n**--protocol** _protocol_\n> Filter by protocol (https, http)\n\n**--help**\n> Display usage information"],["DESCRIPTION","**shiny-mirrors** generates and manages pacman mirror lists for Manjaro Linux. It tests available mirrors for speed and reliability, then creates an optimized mirror list for package downloads.\n\nThe tool selects mirrors based on geographic location, response time, and synchronization status. After running shiny-mirrors, you must synchronize your database and update your system using **sudo pacman -Syyu** to apply the new mirror configuration."],["CAVEATS","Requires root privileges for refresh operations that modify system files. Mirror selection may vary based on network conditions at test time. Always run **pacman -Syyu** after refreshing mirrors to avoid partial upgrades. Branch switching affects which package versions are available."],["HISTORY","**shiny-mirrors** is a Manjaro-specific tool developed as a modern replacement for **pacman-mirrors**. It is designed to provide faster and more reliable mirror management for Manjaro Linux users. The tool is actively maintained as part of the Manjaro ecosystem."],["INSTALL","```aur: yay -S shiny-mirrors```"],["SEE ALSO","[pacman](/man/pacman)(8), [pacman-mirrors](/man/pacman-mirrors)(1), [reflector](/man/reflector)(1)"]],"e":["pacman","pacman-mirrors","reflector"],"x":"shiny-mirrors status"},"slapt-src":{"n":"slapt-src","t":"Build and install SlackBuild packages","s":[["TAGLINE","Build and install SlackBuild packages"],["TLDR","**Update** the list of available slackbuilds\n\n```slapt-src -u```\n\n**List** all available slackbuilds\n\n```slapt-src -l```\n\n**Install** a slackbuild (fetch, build, install)\n\n```slapt-src -i [slackbuild_name]```\n\n**Search** for slackbuilds by name or description\n\n```slapt-src -s [search_term]```\n\n**Show** information about a slackbuild\n\n```slapt-src -w [slackbuild_name]```"],["SYNOPSIS","**slapt-src** [_OPTIONS_] [_slackbuild_name_...]"],["PARAMETERS","**-u, --update**\n> Update the list of available slackbuilds from configured sources\n\n**-l, --list**\n> List all available slackbuilds\n\n**-i, --install** _name_\n> Fetch, build, and install the specified slackbuild(s)\n\n**-s, --search** _term_\n> Search for slackbuilds by name or description\n\n**-w, --show** _name_\n> Display detailed information about a slackbuild\n\n**-b, --build** _name_\n> Build slackbuild without installing\n\n**-f, --fetch** _name_\n> Download slackbuild sources only\n\n**--yes**\n> Assume yes to all prompts\n\n**--no**\n> Assume no to all prompts\n\n**-c, --config** _file_\n> Use alternate configuration file\n\n**-h, --help**\n> Display help information"],["DESCRIPTION","**slapt-src** automates building packages from SlackBuilds, providing a workflow similar to binary package management with **slapt-get**. It handles querying, retrieving, building, and installing SlackBuild packages from configured repositories.\n\nSlackBuild sources are configured in the **slapt-srcrc** configuration file, which specifies repository locations (SOURCE), build directory (BUILDDIR), package extension (PKGEXT), and package tag (PKGTAG)."],["CONFIGURATION","**/etc/slapt-get/slapt-srcrc**\n> Configuration file defining SlackBuild repository URLs (SOURCE), build directory (BUILDDIR), package extension (PKGEXT), and package tag (PKGTAG)."],["CAVEATS","Requires **slapt-get** and **curl** as dependencies. SlackBuilds are source packages that compile on your system, which can be time-consuming for large packages. Build dependencies must be satisfied before compilation. Configuration must point to valid SlackBuild repositories."],["HISTORY","**slapt-src** was created by **Jason Woodward** (jaos) as a companion to **slapt-get** for Slackware Linux. The project began around **2010** to provide an apt-like experience for SlackBuild management. It is released under the GPL-2.0 license and remains actively maintained."],["SEE ALSO","[slapt-get](/man/slapt-get)(8), [sbopkg](/man/sbopkg)(8), [slackpkg](/man/slackpkg)(8)"]],"e":["slapt-get","sbopkg","slackpkg"],"x":"slapt-src -u"},"steamos-boot-install":{"n":"steamos-boot-install","t":"Finalize SteamOS installation on boot","s":[["TAGLINE","Finalize SteamOS installation on boot"],["TLDR","This command is an alias of [steamos-finalize-install](/man/steamos-finalize-install)."],["SEE ALSO","[steamos-finalize-install](/man/steamos-finalize-install)(1)"]],"e":["steamos-finalize-install"],"x":""},"stern":{"n":"stern","t":"Multi-pod Kubernetes log tailing","s":[["TAGLINE","Multi-pod Kubernetes log tailing"],["TLDR","**Tail pod logs**\n\n```stern [pod-query]```\n\n**Tail with namespace**\n\n```stern -n [namespace] [pod-query]```\n\n**All namespaces**\n\n```stern --all-namespaces [pod-query]```\n\n**Specific container**\n\n```stern -c [container] [pod-query]```\n\n**Since duration**\n\n```stern --since [10m] [pod-query]```\n\n**Show timestamps**\n\n```stern -t [pod-query]```\n\n**Regex pod matching**\n\n```stern --selector [app=nginx]```\n\n**Output format**\n\n```stern -o json [pod-query]```"],["SYNOPSIS","**stern** [_-n namespace_] [_-c container_] [_--since duration_] [_options_] _pod-query_"],["PARAMETERS","**-n**, **--namespace** _NS_\n> Kubernetes namespace.\n\n**--all-namespaces**, **-A**\n> All namespaces.\n\n**-c**, **--container** _NAME_\n> Container name.\n\n**--since** _DURATION_\n> Logs since duration.\n\n**-t**, **--timestamps**\n> Show timestamps.\n\n**-o**, **--output** _FORMAT_\n> Output format.\n\n**-s**, **--selector** _SELECTOR_\n> Label selector.\n\n**--tail** _N_\n> Lines per container."],["DESCRIPTION","**stern** allows you to tail log output from multiple Kubernetes pods and containers simultaneously. It accepts a pod name query (which can be a regular expression) and streams log lines from all matching pods, automatically including new pods as they appear and removing terminated ones.\n\nEach pod and container is assigned a distinct color in the output, making it easy to visually distinguish which log lines come from which source. Stern supports filtering by namespace, container name, and label selectors, and can output logs in various formats including JSON for structured processing.\n\nThe tool is particularly useful for debugging microservices where related functionality spans multiple pods or deployments. Unlike `kubectl logs`, which can only follow a single pod at a time, stern aggregates logs across any number of matching pods and gracefully handles pod restarts and scaling events in real time."],["CAVEATS","Requires kubectl access. Many pods may overwhelm. Network latency affects streaming."],["HISTORY","**stern** was created by **Wercker** (now Oracle) for Kubernetes log tailing. It simplifies viewing logs from multiple pods simultaneously."],["INSTALL","```pacman: sudo pacman -S stern```\n\n```zypper: sudo zypper install stern```\n\n```brew: brew install stern```\n\n```nix: nix profile install nixpkgs#stern```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [kubetail](/man/kubetail)(1), [k9s](/man/k9s)(1)"]],"e":["kubectl","kubetail","k9s"],"x":"stern [pod-query]"},"systemd-sysext":{"n":"systemd-sysext","t":"Manage system extension images","s":[["TAGLINE","Manage system extension images"],["TLDR","**Show current merge status**\n\n```systemd-sysext status```\n\n**List** installed extension images\n\n```systemd-sysext list```\n\n**Merge** extensions into /usr and /opt\n\n```systemd-sysext merge```\n\n**Unmerge** all extensions\n\n```systemd-sysext unmerge```\n\n**Refresh** (unmerge then merge)\n\n```systemd-sysext refresh```\n\n**Merge ignoring version compatibility checks**\n\n```systemd-sysext merge --force```"],["SYNOPSIS","**systemd-sysext** [_OPTIONS_] _COMMAND_"],["COMMANDS","**status**\n> Show current merge status. Default when invoked without a command.\n\n**list**\n> List installed extension images.\n\n**merge**\n> Overlay extension images onto /usr and /opt using overlayfs.\n\n**unmerge**\n> Remove extension overlays.\n\n**refresh**\n> Unmerge then merge (reload extensions after installing or removing images)."],["PARAMETERS","**--root=** _PATH_\n> Operate relative to the specified root directory.\n\n**--force**\n> Ignore version incompatibilities when merging.\n\n**--mutable=** _MODE_\n> Set mutability mode (disabled, auto, yes, import, ephemeral, ephemeral-import).\n\n**--no-reload**\n> Do not reload daemon after merge, unmerge, or refresh.\n\n**--no-pager**\n> Do not pipe output into a pager.\n\n**--no-legend**\n> Do not print column headers and footer hints.\n\n**--json=** _MODE_\n> Output as JSON (short, pretty, or off)."],["DESCRIPTION","**systemd-sysext** manages system extension images that overlay files onto `/usr/` and `/opt/`. Extensions are read-only images (DDI format) that add software to the base system without modifying the root filesystem.\n\nExtensions are stored in `/var/lib/extensions/` or `/run/extensions/` and are merged using overlayfs."],["CAVEATS","Extensions must match the host OS version. The base /usr must be immutable or read-only for overlay to work. Changes require refresh to take effect."],["HISTORY","**systemd-sysext** enables modular system composition, supporting immutable base images with layered extensions for additional software."],["INSTALL","```apt: sudo apt install systemd```\n\n```dnf: sudo dnf install systemd```\n\n```pacman: sudo pacman -S systemd```\n\n```zypper: sudo zypper install systemd```\n\n```brew: brew install systemd```\n\n```nix: nix profile install nixpkgs#systemd```"],["SEE ALSO","[systemd-confext](/man/systemd-confext)(8), [systemctl](/man/systemctl)(1), [portablectl](/man/portablectl)(1), [machinectl](/man/machinectl)(1)"]],"e":["systemd-confext","systemctl","portablectl","machinectl"],"x":"systemd-sysext status"},"type":{"n":"type","t":"Display how a command name is interpreted","s":[["TAGLINE","Display how a command name is interpreted"],["TLDR","**Show the type** of a command\n\n```type [command]```\n\n**Show all locations** of a command (aliases, functions, builtins, files)\n\n```type -a [command]```\n\n**Show only the type** (alias, keyword, function, builtin, or file)\n\n```type -t [command]```\n\n**Show the path** to an executable (like which)\n\n```type -P [command]```\n\n**Check if command is** a builtin, alias, or function\n\n```type -t [cd]```\n\n**Show path only** if it's a file on disk\n\n```type -p [command]```\n\n**Suppress function lookup**\n\n```type -f [command]```"],["SYNOPSIS","**type** [**-afptP**] _name_..."],["PARAMETERS","**-a**\n> Display all locations containing an executable; includes aliases, builtins, and functions\n\n**-f**\n> Suppress shell function lookup\n\n**-p**\n> Return disk file path only if type would return \"file\"; empty otherwise\n\n**-P**\n> Force PATH search even for aliases, builtins, or functions\n\n**-t**\n> Output single word: alias, keyword, function, builtin, file, or empty"],["DESCRIPTION","**type** is a shell builtin that displays how a command name would be interpreted. It identifies whether a name is an alias, shell keyword, function, builtin command, or external executable file.\n\nWithout options, type prints a descriptive message showing the command type and location. With **-t**, it outputs just the type category as a single word, useful for scripting.\n\nThe **-a** option shows all interpretations of a name, revealing if multiple commands exist with the same name (e.g., an alias shadowing an executable). The **-P** option forces a PATH search, useful for finding the executable even when an alias or function exists.\n\ntype is more informative than **which** because it understands shell internals like aliases, functions, and builtins, not just files in PATH."],["EXIT STATUS","**0**\n> All names were found\n\n**1**\n> One or more names were not found"],["EXAMPLES","**Identify command types**\n```\n$ type ls\nls is aliased to `ls --color=auto'\n\n$ type cd\ncd is a shell builtin\n\n$ type -t for\nkeyword\n```"],["CAVEATS","type is a shell builtin, so its behavior may vary slightly between shells (bash, zsh, etc.). For POSIX compatibility, use **command -v** instead. The **-a** option includes aliases which only exist in interactive shells."],["HISTORY","The **type** command has been a shell builtin since the Bourne shell in early Unix. It was designed to help users understand command resolution, especially important as shells gained features like aliases and functions that could shadow executables. The command is specified in POSIX."],["SEE ALSO","[which](/man/which)(1), [whence](/man/whence)(1), [command](/man/command)(1), [whereis](/man/whereis)(1), [bash](/man/bash)(1)"]],"e":["which","whence","command","whereis","bash"],"x":"type [command]"},"unbound-checkconf":{"n":"unbound-checkconf","t":"Validate Unbound DNS configuration files","s":[["TAGLINE","Validate Unbound DNS configuration files"],["TLDR","**Check default configuration file**\n\n```unbound-checkconf```\n\n**Check specific configuration file**\n\n```unbound-checkconf [/etc/unbound/unbound.conf]```\n\n**Print specific option value**\n\n```unbound-checkconf -o [verbosity]```\n\n**Check configuration quietly** (no output on success)\n\n```unbound-checkconf -q [/etc/unbound/unbound.conf]```"],["SYNOPSIS","**unbound-checkconf** [_options_] [_configfile_]"],["PARAMETERS","**-h**\n> Display help and exit.\n\n**-f**\n> Print full pathname with chroot applied. Use with the -o option.\n\n**-o** _option_\n> Print the value of a specific option to stdout.\n\n**-q**\n> Quiet mode, suppress output on success."],["DESCRIPTION","**unbound-checkconf** validates the Unbound DNS resolver configuration file for syntax errors and configuration problems. It checks the specified file or the default configuration location if no file is given.\n\nThe tool verifies configuration syntax, checks for valid option values, and ensures referenced files (like key files and certificates) exist and are readable. Running this before restarting Unbound prevents service disruption from configuration errors.\n\nThe -o option extracts specific configuration values, useful for scripting or verifying settings. For disabled options, an empty line is printed."],["EXIT STATUS","Returns 0 if the configuration is valid, 1 if errors are found. Error messages indicate the nature and location of problems."],["CAVEATS","Some errors (like missing auto-generated files) only appear at runtime. Does not validate remote server connectivity. Default configuration path varies by installation."],["HISTORY","**unbound-checkconf** is part of the Unbound DNS resolver developed by NLnet Labs. Unbound was designed as a modern, secure, validating recursive DNS resolver with DNSSEC support, intended as an alternative to BIND for resolver-only deployments."],["INSTALL","```apt: sudo apt install unbound```\n\n```dnf: sudo dnf install unbound```\n\n```pacman: sudo pacman -S unbound```\n\n```apk: sudo apk add unbound```\n\n```zypper: sudo zypper install unbound```\n\n```brew: brew install unbound```\n\n```nix: nix profile install nixpkgs#unbound```"],["SEE ALSO","[unbound](/man/unbound)(8), [unbound-control](/man/unbound-control)(8), [unbound-host](/man/unbound-host)(1)"]],"e":["unbound","unbound-control","unbound-host"],"x":"unbound-checkconf"},"wg-quick":{"n":"wg-quick","t":"Quick WireGuard VPN tunnel setup","s":[["TAGLINE","Quick WireGuard VPN tunnel setup"],["TLDR","**Bring up a WireGuard tunnel**\n\n```sudo wg-quick up [wg0]```\n\n**Tear down a WireGuard tunnel**\n\n```sudo wg-quick down [wg0]```\n\n**Bring up a tunnel from a specific config file**\n\n```sudo wg-quick up [/etc/wireguard/wg0.conf]```\n\n**Save current interface state to config**\n\n```sudo wg-quick save [wg0]```"],["SYNOPSIS","**wg-quick** [**up**|**down**|**save**|**strip**] _interface_"],["PARAMETERS","**up _interface_**\n> Bring up a WireGuard interface using config file\n\n**down _interface_**\n> Tear down a WireGuard interface\n\n**save _interface_**\n> Save current configuration to config file\n\n**strip _interface_**\n> Output configuration without wg-quick-specific options"],["DESCRIPTION","**wg-quick** is a convenience wrapper for setting up WireGuard VPN tunnels quickly. It reads configuration files from **/etc/wireguard/** and handles interface creation, routing, and DNS configuration automatically.\n\nConfiguration files are named **[interface].conf** (e.g., **/etc/wireguard/wg0.conf**) and contain both WireGuard settings and wg-quick-specific options like Address, DNS, and routing rules."],["CAVEATS","Requires root privileges. Configuration files must exist in **/etc/wireguard/**. The script modifies routing tables and may affect network connectivity. DNS changes require resolvconf or systemd-resolved support."],["HISTORY","**wg-quick** is part of the WireGuard tools, created by **Jason A. Donenfeld**. WireGuard was initially developed as a Linux kernel module and merged into the mainline Linux kernel in version **5.6** (March 2020). The wg-quick script simplifies the setup that would otherwise require multiple **wg** and **ip** commands."],["INSTALL","```apt: sudo apt install wireguard-tools```\n\n```dnf: sudo dnf install wireguard-tools```\n\n```pacman: sudo pacman -S wireguard-tools```\n\n```apk: sudo apk add wireguard-tools-wg-quick```\n\n```zypper: sudo zypper install wireguard-tools```\n\n```brew: brew install wireguard-tools```\n\n```nix: nix profile install nixpkgs#wireguard-tools```"],["SEE ALSO","[wg](/man/wg)(8), [ip](/man/ip)(8)"]],"e":["wg","ip"],"x":"sudo wg-quick up [wg0]"},"yes":{"n":"yes","t":"Repeatedly output a string","s":[["TAGLINE","Repeatedly output a string"],["TLDR","**Output 'y' repeatedly** (for auto-confirming prompts)\n\n```yes | [command]```\n\n**Output a custom string repeatedly**\n\n```yes \"[custom text]\"```\n\n**Auto-answer 'no' to all prompts**\n\n```yes n | [command]```\n\n**Output for a limited number of lines**\n\n```yes | head -n [10]```"],["SYNOPSIS","**yes** [_string_...]"],["PARAMETERS","_string..._\n> One or more strings to output repeatedly, joined by spaces (default: \"y\").\n\n**--help**\n> Display help and exit.\n\n**--version**\n> Display version and exit."],["DESCRIPTION","**yes** outputs a string repeatedly until killed or the pipe closes. By default, it outputs \"y\" followed by a newline, endlessly.\n\nThe primary use is piping to commands that ask for confirmation, automatically answering \"y\" (or any specified response) to all prompts. This is useful for scripting commands that don't have native batch/quiet modes.\n\nWhen the receiving command closes its stdin (by exiting), yes terminates automatically due to the broken pipe.\n\nWithout piping, yes runs indefinitely and must be interrupted with Ctrl+C."],["EXAMPLES","```bash"],["Auto-confirm all prompts","yes | apt upgrade"],["Auto-answer 'no'","yes n | rm -i *.txt"],["Generate test data","yes \"test line\" | head -1000 > test.txt"],["Stress test (generates output as fast as possible)","yes > /dev/null\n```"],["CAVEATS","Using yes bypasses safety prompts. Ensure you understand what you're auto-confirming before piping yes to destructive commands.\n\nSome commands detect non-interactive input and refuse to run or behave differently. Use command-specific flags (like **-y** or **--yes**) when available.\n\nyes runs at maximum speed, which can generate significant CPU load when not piped to anything useful. Upon a write error (e.g., broken pipe), yes exits with status 1."],["HISTORY","**yes** is part of **GNU coreutils**, written by **David MacKenzie**. It originates from Unix System V and has been part of coreutils since the early days of the project."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[true](/man/true)(1), [false](/man/false)(1), [head](/man/head)(1), [seq](/man/seq)(1), [timeout](/man/timeout)(1)"]],"e":["true","false","head","seq","timeout"],"x":"yes | [command]"},"zgrep":{"n":"zgrep","t":"Search gzip-compressed files for patterns","s":[["TAGLINE","Search gzip-compressed files for patterns"],["TLDR","**Search for pattern in gzipped file**\n\n```zgrep \"[pattern]\" [file.gz]```\n\n**Case-insensitive search**\n\n```zgrep -i \"[pattern]\" [file.gz]```\n\n**Show line numbers**\n\n```zgrep -n \"[pattern]\" [file.gz]```\n\n**Search recursively in compressed files**\n\n```zgrep -r \"[pattern]\" [directory]```\n\n**Count matching lines**\n\n```zgrep -c \"[pattern]\" [file.gz]```\n\n**Show only filenames with matches**\n\n```zgrep -l \"[pattern]\" [*.gz]```"],["SYNOPSIS","**zgrep** [_grep-options_] [**-e**] _pattern_ [_file_...]"],["PARAMETERS","**-i**\n> Case-insensitive matching\n\n**-n**\n> Show line numbers\n\n**-l**\n> List filenames with matches only\n\n**-c**\n> Count matching lines\n\n**-v**\n> Invert match (show non-matching lines)\n\n**-h**\n> Suppress filename in output\n\n**-r**, **-R**\n> Recursive search\n\n**-E**\n> Extended regular expressions (like egrep)\n\n**-F**\n> Fixed string matching (like fgrep)\n\n**-e** _pattern_\n> Specify pattern"],["DESCRIPTION","**zgrep** searches for patterns in gzip-compressed files without manual decompression. It's equivalent to **gunzip -c file.gz | grep pattern** but more convenient.\n\nThe tool automatically detects whether files are compressed and handles them appropriately. This makes it safe to use on directories containing mixed compressed and uncompressed files.\n\nzgrep supports the same options as grep, including basic and extended regular expressions. For explicit regex modes, use **zegrep** (extended) or **zfgrep** (fixed strings).\n\nMultiple files can be searched, with filenames shown by default when multiple files match."],["CAVEATS","Only gzip compression is supported natively. For other formats:\n- bzip2: use **bzgrep**\n- xz: use **xzgrep**\n- zstd: pipe through **zstdcat**\n\nLarge compressed files must be fully decompressed to search, using CPU and potentially significant memory.\n\nOn some systems, zgrep is a wrapper script that may have slightly different behavior than native grep."],["INSTALL","```apt: sudo apt install gzip```\n\n```dnf: sudo dnf install gzip```\n\n```pacman: sudo pacman -S gzip```\n\n```apk: sudo apk add gzip```\n\n```zypper: sudo zypper install gzip```\n\n```brew: brew install gzip```\n\n```nix: nix profile install nixpkgs#gzip```"],["SEE ALSO","[grep](/man/grep)(1), [zegrep](/man/zegrep)(1), [zfgrep](/man/zfgrep)(1), [zcat](/man/zcat)(1), [gzip](/man/gzip)(1), [bzgrep](/man/bzgrep)(1)"]],"e":["grep","zegrep","zfgrep","zcat","gzip","bzgrep"],"x":"zgrep \"[pattern]\" [file.gz]"}});
