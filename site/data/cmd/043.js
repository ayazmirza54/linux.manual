window.LCL_BUCKET(43,{"adb-pair":{"n":"adb-pair","t":"Pair with an Android device for wireless debugging","s":[["TAGLINE","Pair with an Android device for wireless debugging"],["TLDR","**Pair** with a device using pairing code\n\n```adb pair [192.168.1.100]:[port]```"],["SYNOPSIS","**adb pair** _host_:_port_ [_pairing_code_]"],["DESCRIPTION","**adb pair** establishes a secure wireless debugging connection with an Android 11+ device using a pairing code. This eliminates the need for initial USB connection to enable wireless debugging.\n\nTo use this feature, enable \"Wireless debugging\" in Developer Options on the device, then tap \"Pair device with pairing code\" to get the pairing port and code. After pairing, use **adb connect** with the connection port shown in Wireless debugging settings."],["PARAMETERS","**host**\n> IP address of the Android device\n\n**port**\n> Pairing port shown on the device (different from connection port)\n\n**pairing_code**\n> Six-digit code shown on device (prompted if not provided)"],["CAVEATS","Only available on Android 11 (API 30) and later. The pairing port is different from the connection port used with **adb connect**. Pairing codes expire quickly. Both devices must be on the same network."],["HISTORY","Wireless debugging with pairing was introduced in Android 11, released in **2020**. This feature was designed to improve the developer experience by removing the requirement for initial USB connection."],["INSTALL","```apt: sudo apt install adb```\n\n```dnf: sudo dnf install android-tools```\n\n```pacman: sudo pacman -S android-tools```\n\n```apk: sudo apk add android-tools-adb```\n\n```zypper: sudo zypper install android-tools```\n\n```nix: nix profile install nixpkgs#android-tools```"],["SEE ALSO","[adb](/man/adb)(1), [adb-connect](/man/adb-connect)(1), [adb-disconnect](/man/adb-disconnect)(1)"],["RESOURCES","```[Source code](https://android.googlesource.com/platform/packages/modules/adb/)```\n\n```[Documentation](https://developer.android.com/tools/adb)```"]],"e":["adb","adb-connect","adb-disconnect"],"x":"adb pair [192.168.1.100]:[port]"},"agentico":{"n":"agentico","t":"TUI orchestrator for long-running AI coding agents","s":[["TAGLINE","TUI orchestrator for long-running AI coding agents"],["TLDR","**Launch the dashboard** and manage features from the terminal\n\n```agentico```\n\n**Install or update** the binary in place\n\n```agentico update```\n\n**Start with specific AI providers** only\n\n```agentico --providers claude,codex```"],["SYNOPSIS","**agentico** [_options_]"],["DESCRIPTION","**agentico** is the command-line interface for Agentic Orchestrator, a terminal-based workflow manager that turns a high-level feature request into a durable engineering pipeline. It coordinates AI coding agents through isolated Git worktrees, structured planning artifacts, implementation loops, code review, and pull-request publishing, so several complex features can run in parallel without mixing state.\n\nOn first launch, agentico walks through a welcome flow to select workspace directories, then opens an interactive dashboard. From there you create features, watch active work, answer approval prompts, and review checkpoints. The orchestrator routes phases to the best available model across installed provider CLIs such as **claude**, **codex**, and **opencode**, while **git** and **gh** handle repository and pull-request operations.\n\nPipeline depth is chosen per feature: Medium skips upfront research for small changes, Large adds knowledge-base, inquiry, research, and design phases, and Moonshot keeps the same depth with stricter review gates. Human checkpoints can pause on inquiry, research, design, roadmap, and publish decisions."],["PARAMETERS","**--providers** _LIST_\n> Restrict orchestration to specific provider CLIs (comma-separated), such as `claude`, `codex`, or `opencode`.\n\n**update**\n> Update agentico using the same installation method originally used (Homebrew, release binary, or Go install)."],["CAVEATS","agentico requires **git** and the **gh** CLI with GitHub authentication. At least one supported AI provider CLI must be installed, authenticated, and recent enough; missing or outdated providers are skipped at startup with a notice. The tool is designed around multi-repo Git worktrees and long-running agent sessions, so disk space, API quotas, and provider availability matter for large parallel workloads."],["HISTORY","Agentic Orchestrator was open-sourced by **DoorDash OSS** in 2026 as a Go-based TUI for supervising agentic coding workflows. The user-facing binary was named **agentico** to distinguish the local CLI from the broader orchestration system."],["SEE ALSO","[claude](/man/claude)(1), [codex](/man/codex)(1), [gh](/man/gh)(1), [git](/man/git)(1)"],["RESOURCES","```[Source code](https://github.com/doordash-oss/agentic-orchestrator)```"]],"e":["claude","codex","gh","git"],"x":"agentico"},"astyle":{"n":"astyle","t":"Source code formatter for C, C++, C#, and Java","s":[["TAGLINE","Source code formatter for C, C++, C#, and Java"],["TLDR","**Format** C++ code\n\n```astyle [file.cpp]```\n\nFormat with **specific style**\n\n```astyle --style=google [file.cpp]```\n\nFormat **in place** (overwrite)\n\n```astyle --suffix=none [file.cpp]```\n\nFormat **recursively**\n\n```astyle --recursive \"*.cpp,*.h\"```\n\nFormat with **custom options**\n\n```astyle --indent=spaces=4 --style=kr [file.cpp]```"],["SYNOPSIS","**astyle** [_--style=name_] [_--indent=type_] [_options_] _files_"],["DESCRIPTION","**astyle** (Artistic Style) is a source code indenter and formatter for C, C++, C++/CLI, Objective-C, C#, and Java. It reformats code to follow consistent style guidelines, improving readability and maintainability.\n\nThe tool supports various predefined styles (Google, Mozilla, GNU, etc.) and extensive customization."],["PARAMETERS","**--style=**_name_\n> Predefined style: allman, java, kr, stroustrup, whitesmith, vtk, ratliff, gnu, linux, horstmann, 1tbs, google, mozilla, webkit, pico, lisp.\n\n**--indent=**_type_\n> Indentation: spaces=n, tab, force-tab=n.\n\n**--attach-braces** / **--break-braces**\n> Attach or break opening braces from headers (replaces the older **--brackets** option).\n\n**--indent-classes**\n> Indent class blocks\n\n**--indent-switches**\n> Indent switch blocks\n\n**--pad-oper**\n> Pad operators with spaces\n\n**--pad-header**\n> Pad headers (if, for, while)\n\n**--unpad-paren**\n> Remove padding inside parentheses\n\n**--suffix=**_suffix_\n> Backup file suffix (none to overwrite)\n\n**--recursive**\n> Process directories recursively\n\n**-n**, **--suffix=none**\n> Don't create backup files\n\n**--dry-run**\n> Show changes without applying"],["CONFIGURATION","**~/.astylerc**\n> User-level default options file. Each line contains one option (e.g., --style=google).\n\n**.astylerc**\n> Project-level options file in the current directory, overrides user defaults."],["CAVEATS","Always backup code before formatting entire projects. May conflict with version control diffs. Team should agree on style before applying. Some styles may clash with existing code conventions."],["HISTORY","**Artistic Style** was created by Tal Davidson and Jim Pattee, with the first release in **1998**. It has become a standard code formatting tool for C-family languages."],["INSTALL","```apt: sudo apt install astyle```\n\n```dnf: sudo dnf install astyle```\n\n```pacman: sudo pacman -S astyle```\n\n```apk: sudo apk add astyle```\n\n```zypper: sudo zypper install astyle```\n\n```brew: brew install astyle```\n\n```nix: nix profile install nixpkgs#astyle```"],["SEE ALSO","[clang-format](/man/clang-format)(1), [indent](/man/indent)(1), [uncrustify](/man/uncrustify)(1)"]],"e":["clang-format","indent","uncrustify"],"x":"astyle [file.cpp]"},"auditd":{"n":"auditd","t":"Linux audit daemon for tracking security-relevant events.","s":[["TAGLINE","Linux audit daemon for tracking security-relevant events."],["TLDR","**Start** audit daemon\n\n```sudo auditd```\n\nRun in the **foreground** for debugging\n\n```sudo auditd -f```\n\nDo **not fork** (for systemd or inittab)\n\n```sudo auditd -n```\n\nUse an **alternate config directory**\n\n```sudo auditd -c [/path/to/config_dir]```\n\nStart without changing the kernel **enabled flag**\n\n```sudo auditd -s nochange```"],["SYNOPSIS","**auditd** [_-f_] [_-l_] [_-n_] [_-s disable|enable|nochange_] [_-c config_dir_]"],["DESCRIPTION","**auditd** is the Linux Audit daemon that collects audit events from the kernel and writes them to disk. It's part of the Linux Audit framework for tracking security-relevant events.\n\nThe daemon logs file accesses, system calls, authentication events, and other activities based on configured rules."],["PARAMETERS","**-f**\n> Leave the daemon in the foreground for debugging. Messages also go to stderr rather than the audit log.\n\n**-l**\n> Allow the daemon to follow symbolic links for config files.\n\n**-n**\n> Do not fork. Useful for running from inittab or systemd.\n\n**-s** _disable|enable|nochange_\n> Set the kernel audit enabled flag state at startup. The default enables the flag on start and disables it on termination. The flag can also be changed at runtime with **auditctl**.\n\n**-c** _config_dir_\n> Use an alternate config directory. The same directory is also passed to the dispatcher (default: /etc/audit/)."],["CONFIGURATION","**/etc/audit/auditd.conf**\n> Main daemon configuration controlling log file location, retention, disk space handling, and dispatcher settings.\n\n**/etc/audit/rules.d/**\n> Directory containing audit rule files compiled by **augenrules** into the active ruleset. Rules define which system calls and file accesses to monitor.\n\n**/etc/audit/plugins.d/**\n> Directory of plugin (dispatcher) configuration files for routing events to external programs."],["CAVEATS","Requires root privileges. Heavy auditing can impact performance, and log files grow quickly with verbose rules. On modern systems the daemon should be started, stopped, and reloaded through its service manager (for example **systemctl** or **service auditd rotate**) rather than by sending signals directly."],["HISTORY","**auditd** was developed as part of the Linux Audit project to meet Common Criteria security requirements, becoming part of the mainline kernel in **2.6.6** (2004)."],["INSTALL","```apt: sudo apt install auditd```\n\n```dnf: sudo dnf install audit```\n\n```pacman: sudo pacman -S audit```\n\n```apk: sudo apk add audit```\n\n```zypper: sudo zypper install audit```\n\n```nix: nix profile install nixpkgs#audit```"],["SEE ALSO","[auditctl](/man/auditctl)(8), [ausearch](/man/ausearch)(8), [aureport](/man/aureport)(8)"],["RESOURCES","```[Source code](https://github.com/linux-audit/audit-userspace)```"]],"e":["auditctl","ausearch","aureport"],"x":"sudo auditd"},"autoprefixer":{"n":"autoprefixer","t":"Add vendor prefixes to CSS automatically","s":[["TAGLINE","Add vendor prefixes to CSS automatically"],["TLDR","**Process a CSS file and add vendor prefixes**\n\n```autoprefixer [input.css] -o [output.css]```\n\n**Process CSS from stdin**\n\n```cat [input.css] | autoprefixer > [output.css]```\n\n**Specify browser targets**\n\n```autoprefixer [input.css] -o [output.css] --browsers \"last 2 versions, > 1%\"```\n\n**Process multiple files in place**\n\n```autoprefixer [file1.css] [file2.css]```\n\n**Generate source maps**\n\n```autoprefixer [input.css] -o [output.css] --map```\n\n**Show which prefixes would be added**\n\n```autoprefixer --info```"],["SYNOPSIS","**autoprefixer** [_options_] [_files_]"],["PARAMETERS","**-o**, **--output** _file_\n> Write output to the specified file.\n\n**-d**, **--dir** _directory_\n> Output to a directory (for multiple files).\n\n**--map**\n> Generate source maps.\n\n**--no-map**\n> Disable source map generation.\n\n**-b**, **--browsers** _list_\n> Browser targets (Browserslist query).\n\n**--ignore** _pattern_\n> Glob patterns for files to ignore.\n\n**--replace**\n> Modify files in place (default for multiple files).\n\n**--info**\n> Display browser list and prefixes information.\n\n**--no-inline**\n> Do not inline source maps.\n\n**--env** _environment_\n> Specify Browserslist environment."],["DESCRIPTION","**Autoprefixer** is a PostCSS plugin that automatically adds vendor prefixes to CSS properties based on current browser usage data. It parses CSS and adds prefixes like **-webkit-**, **-moz-**, and **-ms-** where needed for cross-browser compatibility.\n\nThe tool uses Browserslist to determine which prefixes are needed based on target browser specifications. It not only adds required prefixes but also removes outdated ones that are no longer necessary, keeping CSS clean and efficient.\n\nAutoprefixer handles CSS properties, values, at-rules, and selectors. It understands which browsers support which features and adds only the necessary prefixes based on the specified browser targets, typically configured in a **browserslist** file or **package.json**."],["CONFIGURATION","**.browserslistrc**\n> Defines target browsers for prefix generation using Browserslist query syntax.\n\n**postcss.config.js**\n> PostCSS configuration file where Autoprefixer can be loaded as a plugin with options."],["CAVEATS","Browser support data depends on Can I Use database updates. Very new CSS features may not be recognized immediately. The tool cannot fix non-standard browser behaviors, only add standard prefixes. Some prefixes have behavioral differences that prefixing cannot resolve."],["HISTORY","Autoprefixer was created by **Andrey Sitnik** in **2013** as a solution to the tedious manual process of adding vendor prefixes. It became one of the most widely used PostCSS plugins and is integrated into most modern build tools including webpack, Vite, and Parcel. The tool revolutionized CSS workflow by eliminating manual prefix management using real browser usage data from **caniuse.com**."],["INSTALL","```nix: nix profile install nixpkgs#autoprefixer```"],["SEE ALSO","[postcss](/man/postcss)(1), [cssnano](/man/cssnano)(1)"]],"e":["postcss","cssnano"],"x":"autoprefixer [input.css] -o [output.css]"},"binsider":{"n":"binsider","t":"TUI tool for analyzing ELF binaries","s":[["TAGLINE","TUI tool for analyzing ELF binaries"],["TLDR","**Analyze an ELF binary**\n\n```binsider [path/to/binary]```\n\n**Analyze a binary** starting on the static analysis tab\n\n```binsider -t static [path/to/binary]```\n\n**Analyze a binary** with a custom minimum string length\n\n```binsider -n [8] [path/to/binary]```\n\n**Analyze a binary** starting on the strings tab with a minimum length of 8\n\n```binsider -t strings -n 8 [path/to/binary]```"],["SYNOPSIS","**binsider** [_options_] [_file_...]"],["PARAMETERS","**-n**, **--min-len** _value_\n> Minimum length of strings to extract (default: 15)\n\n**-t**, **--tab** _tab_\n> The initial application tab to open (default: general)\n\n**--accent-color** _color_\n> Accent color of the application\n\n**-h**, **--help**\n> Display help information\n\n**-V**, **--version**\n> Display version information"],["DESCRIPTION","**binsider** is a terminal user interface for performing both static and dynamic analysis of ELF (Executable and Linkable Format) binary files. It combines the functionality of several standard Unix utilities into a single interactive interface, acting as a swiss army knife for reverse engineers.\n\nThe **general analysis** tab displays file metadata such as size, ownership, permissions, and timestamps (similar to **stat**), as well as linked shared libraries (similar to **ldd**). Selecting a shared library allows recursive analysis of its dependencies.\n\nThe **static analysis** tab examines the ELF structure without executing the binary, displaying file headers, notes, program headers, section headers, symbols, dynamic symbols, dynamic linking information, and relocations (similar to **readelf**).\n\nThe **dynamic analysis** tab executes the binary while tracing system calls and signals (similar to **strace** and **ltrace**), showing process IDs, call arguments, and return values. An execution summary provides timing data, call frequency, and error counts. This feature is powered by the **lurk** tracing library.\n\nThe **strings** tab extracts embedded strings from the binary (similar to **strings**), useful for discovering URLs, credentials, or other sensitive data. The minimum string length can be adjusted interactively with +/- keys or via the **-n** flag.\n\nThe **hexdump** tab provides a structured hex viewer with a rich dashboard. It supports keyboard-driven navigation, searching, jumping to specific byte offsets, and direct hex editing with the ability to save modifications back to the file. This feature is powered by the **heh** hex editor.\n\nBuilt with Rust and the Ratatui TUI framework, binsider uses keyboard-driven navigation throughout. Tabs are switched with Tab/Shift+Tab, items navigated with h/j/k/l or arrow keys, and searches initiated with the forward slash key."],["CAVEATS","Dynamic analysis requires executing the target binary and is only available on Linux. Hex editing modifies the binary in-place, so backups should be made before saving changes. The file must be opened in read-write mode for editing to work. ELF analysis features are specific to the ELF format and do not apply to other binary formats such as PE or Mach-O."],["HISTORY","**binsider** was created by **Orhun Parmaksiz** and first released in **September 2024**. It is written in Rust and dual-licensed under the Apache License 2.0 and MIT License. The project is hosted on GitHub and continues active development, with its website and documentation at https://binsider.dev."],["INSTALL","```pacman: sudo pacman -S binsider```\n\n```zypper: sudo zypper install binsider```\n\n```brew: brew install binsider```\n\n```nix: nix profile install nixpkgs#binsider```"],["SEE ALSO","[readelf](/man/readelf)(1), [strace](/man/strace)(1), [ltrace](/man/ltrace)(1), [strings](/man/strings)(1), [ldd](/man/ldd)(1), [objdump](/man/objdump)(1), [hexdump](/man/hexdump)(1), [stat](/man/stat)(1), [nm](/man/nm)(1), [file](/man/file)(1)"]],"e":["readelf","strace","ltrace","strings","ldd","objdump","hexdump","stat","nm","file"],"x":"binsider [path/to/binary]"},"btrfs-subvolume":{"n":"btrfs-subvolume","t":"Manage btrfs subvolumes and snapshots","s":[["TAGLINE","Manage btrfs subvolumes and snapshots"],["TLDR","**Create** subvolume\n\n```sudo btrfs subvolume create path/to/new_subvolume```\n\n**List** subvolumes\n\n```sudo btrfs subvolume list path/to/filesystem```\n\n**Delete** subvolume\n\n```sudo btrfs subvolume delete path/to/subvolume```\n\nCreate **read-only** snapshot\n\n```sudo btrfs subvolume snapshot -r path/to/source path/to/target```\n\nCreate **read-write** snapshot\n\n```sudo btrfs subvolume snapshot path/to/source path/to/target```\n\n**Show** subvolume details\n\n```sudo btrfs subvolume show path/to/subvolume```"],["SYNOPSIS","**btrfs subvolume** _command_ [_OPTIONS_] [_path_]"],["DESCRIPTION","**btrfs subvolume** manages btrfs subvolumes and snapshots. Subvolumes are separately mountable filesystem trees that can have independent snapshot and quota policies. Snapshots are point-in-time copies that initially share all data with their source."],["PARAMETERS","**create**\n> Create a new empty subvolume\n\n**list**\n> List all subvolumes in the filesystem\n\n**delete**\n> Delete a subvolume or snapshot\n\n**snapshot**\n> Create a snapshot of an existing subvolume\n\n**show**\n> Display detailed information about a subvolume\n\n**-r, --readonly**\n> Create a read-only snapshot\n\n**get-default**\n> Get the default subvolume\n\n**set-default**\n> Set the default subvolume"],["CAVEATS","Deleting a subvolume does not immediately free space; asynchronous cleanup runs in the background. Snapshots are not recursive - nested subvolumes must be snapshotted separately."],["HISTORY","**btrfs subvolume** is part of the **btrfs-progs** package for managing btrfs filesystems on Linux."],["INSTALL","```apt: sudo apt install btrfs-progs```\n\n```dnf: sudo dnf install btrfs-progs```\n\n```pacman: sudo pacman -S btrfs-progs```\n\n```apk: sudo apk add btrfs-progs```\n\n```brew: brew install btrfs-progs```\n\n```nix: nix profile install nixpkgs#btrfs-progs```"],["SEE ALSO","[btrfs](/man/btrfs)(8), [btrfs-filesystem](/man/btrfs-filesystem)(8)"],["RESOURCES","```[Source code](https://github.com/kdave/btrfs-progs)```\n\n```[Documentation](https://btrfs.readthedocs.io/)```"]],"e":["btrfs","btrfs-filesystem"],"x":"sudo btrfs subvolume create path/to/new_subvolume"},"bzdiff":{"n":"bzdiff","t":"Compare bzip2 compressed files","s":[["TAGLINE","Compare bzip2 compressed files"],["TLDR","**Compare two bzip2 compressed files**\n\n```bzdiff [file1.bz2] [file2.bz2]```\n\n**Compare compressed file with uncompressed version**\n\n```bzdiff [file.bz2]```\n\n**Compare with diff options**\n\n```bzdiff -u [file1.bz2] [file2.bz2]```\n\n**Compare using cmp instead of diff**\n\n```bzcmp [file1.bz2] [file2.bz2]```"],["SYNOPSIS","**bzdiff** [_diff_options_] _file1_ [_file2_]\n\n**bzcmp** [_cmp_options_] _file1_ [_file2_]"],["DESCRIPTION","**bzdiff** compares bzip2 compressed files by decompressing them and passing the content to diff. All options are passed directly to the underlying diff command.\n\n**bzcmp** works similarly but uses cmp instead of diff for byte-by-byte comparison."],["PARAMETERS","All parameters are passed directly to **diff** or **cmp**:\n\n**-u**\n> Unified diff format\n\n**-c**\n> Context diff format\n\n**-i**\n> Ignore case differences\n\n**-w**\n> Ignore whitespace\n\n**-q**\n> Report only whether files differ"],["BEHAVIOR","**Single file**\n> Compares file1 with file1.bz2 (uncompressed version)\n\n**Two files**\n> Decompresses both if needed and compares"],["EXIT STATUS","Returns the exit status from diff or cmp, preserving the comparison result."],["CAVEATS","Error messages refer to temporary filenames instead of original filenames. Files are decompressed to temporary storage during comparison."],["INSTALL","```apt: sudo apt install bzip2```\n\n```dnf: sudo dnf install bzip2```\n\n```pacman: sudo pacman -S bzip2```\n\n```apk: sudo apk add bzip2```\n\n```zypper: sudo zypper install bzip2```\n\n```brew: brew install bzip2```\n\n```nix: nix profile install nixpkgs#bzip2```"],["SEE ALSO","[diff](/man/diff)(1), [cmp](/man/cmp)(1), [bzgrep](/man/bzgrep)(1), [bzmore](/man/bzmore)(1), [bzip2](/man/bzip2)(1)"],["RESOURCES","```[Source code](https://gitlab.com/bzip2/bzip2)```\n\n```[Homepage](https://sourceware.org/bzip2/)```"]],"e":["diff","cmp","bzgrep","bzmore","bzip2"],"x":"bzdiff [file1.bz2] [file2.bz2]"},"cbxy-editor":{"n":"cbxy-editor","t":"Browser UI to create or edit comic panel boxes","s":[["TAGLINE","Browser UI to create or edit comic panel boxes"],["TLDR","**Edit panel boxes** for a comic in the browser\n\n```cbxy-editor [path/to/book.cbz]```\n\n**Bind** to a specific host and port\n\n```cbxy-editor [path/to/book.cbz] --host [127.0.0.1] --port [8766]```\n\n**Do not open** a browser window automatically\n\n```cbxy-editor [path/to/book.cbz] --no-open```\n\n**Install** the cbxy tools\n\n```pip install cbxy```"],["SYNOPSIS","**cbxy-editor** [_options_] _comic_"],["DESCRIPTION","**cbxy-editor** starts a local web UI to create or correct panel rectangles and save a **.cbxy** sidecar beside the comic. Use it after automatic detection with **cbxy-detect**, or to author panel geometry from scratch. Coordinates are stored as normalized page fractions in per-page JSON inside the **.cbxy** ZIP.\n\nInput can be a **.cbz**, **.cbr**, an image folder, or a single page image. Default bind address is **127.0.0.1** on port **8766** (distinct from **cbxy-reader**'s default **8765**). Part of the **cbxy** Python package."],["PARAMETERS","_comic_\n> Path to a **.cbz**, **.cbr**, image folder, or single page image.\n\n**--host** _address_\n> Bind address (default: **127.0.0.1**).\n\n**--port** _port_\n> Listen port (default: **8766**).\n\n**--no-open**\n> Do not open a browser window automatically."],["CAVEATS","The editor is a local browser UI, not a headless batch tool—use **cbxy-detect** for automated pipelines. Binding beyond loopback exposes the editor on the network. Saved **.cbxy** files should stay paired with the matching comic archive for **cbxy-reader** guided mode."],["HISTORY","**cbxy** is a Python reference implementation of a comic panel geometry sidecar format by **ngafar**, published under the MIT license with **cbxy-detect**, **cbxy-reader**, and **cbxy-editor**."],["SEE ALSO","[cbxy-detect](/man/cbxy-detect)(1), [cbxy-reader](/man/cbxy-reader)(1), [pip](/man/pip)(1), [uv](/man/uv)(1)"],["RESOURCES","```[Source code](https://github.com/ngafar/cbxy)```\n\n```[Documentation](https://github.com/ngafar/cbxy#cbxy-editor)```"]],"e":["cbxy-detect","cbxy-reader","pip","uv"],"x":"cbxy-editor [path/to/book.cbz]"},"checkrestart":{"n":"checkrestart","t":"find processes using outdated shared libraries","s":[["TAGLINE","find processes using outdated shared libraries"],["TLDR","**Check for processes using deleted libraries**\n\n```sudo checkrestart```\n\n**Show verbose output with deleted file details**\n\n```sudo checkrestart -v```\n\n**Only report processes belonging to a package**\n\n```sudo checkrestart -p```\n\n**Show terse output suitable for monitoring tools**\n\n```sudo checkrestart -t```\n\n**Output in machine-readable tab-separated format**\n\n```sudo checkrestart -m```\n\n**Exclude a specific package from results**\n\n```sudo checkrestart -i [package_name]```"],["SYNOPSIS","**checkrestart** [_options_]"],["PARAMETERS","**-v**, **--verbose**\n> Verbose output, enables -f and shows reasons for exclusions.\n\n**-f**, **--show-files**\n> List the deleted files and which program is using them.\n\n**-d**, **--debug**\n> Include debugging details in output, enables -v.\n\n**-t**, **--terse**\n> Terse single-line output suitable for Nagios and similar monitoring tools.\n\n**-m**, **--machine**\n> Machine-readable output; each line is tab-separated.\n\n**-p**, **--package**\n> Only report processes that belong to a package.\n\n**-a**, **--all**\n> Prevent exclude.conf and local-exclude.conf from being read.\n\n**-n**, **--no-lsof**\n> Do not use lsof(8) even if available.\n\n**-i** _REGEXP_, **--exclude-package** _REGEXP_\n> Ignore services associated with the matching package name.\n\n**-x** [_TYPE:_]_REGEXP_, **--exclude** [_TYPE:_]_REGEXP_\n> Exclude items matching the pattern (types: package, unit, program, pid, file).\n\n**-e** _PID_\n> Exclude processes with this PID.\n\n**-b** _FILE_, **--blocklistfile** _FILE_\n> Ignore deleted files matching patterns in FILE.\n\n**-h**, **--help**\n> Display help information."],["DESCRIPTION","**checkrestart** identifies processes that are still using old (deleted) versions of libraries after upgrades. When system libraries are updated, running processes continue using the old versions loaded in memory. This tool finds these processes so they can be restarted to use the new libraries.\n\nAfter package updates, especially security patches, critical services may still run vulnerable code until restarted. checkrestart scans **/proc/*/maps** for references to deleted library files and reports affected processes with suggested restart commands.\n\nThe tool integrates with system init systems to suggest appropriate service restart commands (**systemctl restart**, **service restart**). For non-service processes, it reports the process name and PID."],["CAVEATS","Requires root privileges to scan all processes. Some processes may be difficult to restart safely (X server, session manager). The tool cannot detect all cases where restart is needed. Kernel updates require a full reboot, not just process restart."],["HISTORY","**checkrestart** is part of the **debian-goodies** package, a collection of utilities for Debian systems. It was created to help system administrators identify which services need restarting after library updates, a common concern in security-conscious environments. Similar functionality exists in other tools like **needs-restarting** in the Red Hat ecosystem."],["SEE ALSO","[needrestart](/man/needrestart)(1), [lsof](/man/lsof)(8), [apt](/man/apt)(8)"]],"e":["needrestart","lsof","apt"],"x":"sudo checkrestart"},"checkupdates-aur":{"n":"checkupdates-aur","t":"check for AUR package updates","s":[["TAGLINE","check for AUR package updates"],["TLDR","List pending updates for **AUR packages**\n\n```checkupdates-aur```\n\nList pending updates in **debug mode**\n\n```CHECKUPDATES_DEBUG=1 checkupdates-aur```"],["SYNOPSIS","**checkupdates-aur** [_options_]"],["DESCRIPTION","**checkupdates-aur** checks for pending updates from the Arch User Repository (AUR). It compares locally installed AUR packages against the latest versions available in the AUR.\n\nThis tool complements the standard checkupdates command by covering packages not in the official repositories."],["PARAMETERS","**--help**\n> Display help message"],["ENVIRONMENT","**CHECKUPDATES_DEBUG**\n> Set to 1 to enable debug output"],["CAVEATS","Arch Linux specific. Only checks packages that were originally installed from the AUR. Requires network access to query the AUR API."],["SEE ALSO","[checkupdates](/man/checkupdates)(1), [yay](/man/yay)(1), [paru](/man/paru)(1), [pacman](/man/pacman)(8)"]],"e":["checkupdates","yay","paru","pacman"],"x":"checkupdates-aur"},"collectd":{"n":"collectd","t":"system performance statistics collection daemon","s":[["TAGLINE","system performance statistics collection daemon"],["TLDR","**Test** the configuration file and exit\n\n```collectd -t```\n\n**Test plugin** data collection functionality\n\n```collectd -T```\n\n**Start** collectd daemon\n\n```collectd```\n\nSpecify a **custom configuration** file\n\n```collectd -C [path/to/file]```\n\nSpecify a **custom PID file**\n\n```collectd -P [path/to/file]```\n\nRun in **foreground** (don't fork)\n\n```collectd -f```"],["SYNOPSIS","**collectd** [_options_]"],["DESCRIPTION","**collectd** is a daemon that collects, transfers, and stores system performance statistics. It supports numerous input plugins for collecting metrics and output plugins for storing or forwarding data.\n\nCommon use cases include monitoring system resources, network statistics, and application metrics."],["PARAMETERS","**-t**\n> Test configuration file syntax\n\n**-T**\n> Test plugin data collection\n\n**-C** _file_\n> Use specified configuration file\n\n**-P** _file_\n> Write PID to specified file\n\n**-f**\n> Don't fork into background\n\n**-h**\n> Display help and version"],["CONFIGURATION","**/etc/collectd.conf**\n> Main configuration file controlling plugins, intervals, and output destinations.\n\n**~/.collectdrc**\n> User-specific configuration file."],["CAVEATS","Configuration requires careful setup of input and output plugins. Some plugins may require additional libraries. Running without proper output plugins will collect data but not store it."],["INSTALL","```apt: sudo apt install collectd-core```\n\n```dnf: sudo dnf install collectd```\n\n```apk: sudo apk add collectd```\n\n```zypper: sudo zypper install collectd```\n\n```brew: brew install collectd```\n\n```nix: nix profile install nixpkgs#collectd```"],["SEE ALSO","[collectdmon](/man/collectdmon)(1)"]],"e":["collectdmon"],"x":"collectd -t"},"crackmapexec":{"n":"crackmapexec","t":"post-exploitation tool for Windows/AD networks","s":[["TAGLINE","post-exploitation tool for Windows/AD networks"],["TLDR","**Enumerate SMB shares**\n\n```crackmapexec smb [192.168.1.0/24] -u [user] -p [password] --shares```\n\n**Check credentials** against hosts\n\n```crackmapexec smb [hosts.txt] -u [user] -p [password]```\n\n**Execute command**\n\n```crackmapexec smb [target] -u [user] -p [password] -x \"[whoami]\"```\n\n**Dump SAM hashes**\n\n```crackmapexec smb [target] -u [admin] -p [password] --sam```\n\n**Pass the hash**\n\n```crackmapexec smb [target] -u [user] -H [ntlm_hash]```\n\n**Enumerate users**\n\n```crackmapexec smb [target] -u [user] -p [password] --users```"],["SYNOPSIS","**crackmapexec** _protocol_ _target_ [_options_]"],["DESCRIPTION","**crackmapexec** (CME) is a post-exploitation tool for penetration testing Windows/Active Directory environments. It automates common tasks like credential validation, share enumeration, and command execution across multiple hosts.\n\nThe tool supports SMB, WinRM, LDAP, MSSQL, and SSH protocols. It's designed for authorized security assessments and red team operations."],["PARAMETERS","**-u** _user_\n> Username for authentication.\n\n**-p** _password_\n> Password for authentication.\n\n**-H** _hash_\n> NTLM hash for pass-the-hash.\n\n**-d** _domain_\n> Domain name.\n\n**-x** _command_\n> Execute command.\n\n**-X** _command_\n> Execute PowerShell command.\n\n**--shares**\n> Enumerate SMB shares.\n\n**--users**\n> Enumerate domain users.\n\n**--sam**\n> Dump SAM database.\n\n**--lsa**\n> Dump LSA secrets.\n\n**--local-auth**\n> Use local authentication.\n\n**-M** _module_\n> Run specific module."],["CAVEATS","Only for authorized penetration testing. May trigger security alerts. Requires valid credentials or hashes. Some features need admin privileges. Network policies may block access.\n\n**Deprecated**: the upstream project has stopped releasing updates; a community fork called **NetExec** (`nxc`) is the actively maintained successor and is a drop-in replacement."],["HISTORY","**crackmapexec** was created by **byt3bl33d3r** (Marcello Salvati) around **2015**. It evolved from the need for a unified tool for Windows network penetration testing. The project is widely used in the security community and is actively maintained."],["SEE ALSO","[impacket](/man/impacket)(1), [nmap](/man/nmap)(1), [enum4linux](/man/enum4linux)(1)"]],"e":["impacket","nmap","enum4linux"],"x":"crackmapexec smb [192.168.1.0/24] -u [user] -p [password] --shares"},"diff":{"n":"diff","t":"line-by-line file comparison tool","s":[["TAGLINE","line-by-line file comparison tool"],["TLDR","**Compare** files\n\n```diff [file1.txt] [file2.txt]```\n\n**Unified** format\n\n```diff -u [file1.txt] [file2.txt]```\n\n**Compare** directories\n\n```diff -r [dir1] [dir2]```\n\n**Side-by-side** comparison\n\n```diff -y [file1.txt] [file2.txt]```\n\n**Ignore** whitespace\n\n```diff -w [file1.txt] [file2.txt]```"],["SYNOPSIS","**diff** [_options_] _file1_ _file2_"],["DESCRIPTION","**diff** compares files line by line and outputs the differences. It's essential for version control, code review, and understanding changes between file versions.\n\nThe tool forms the basis of patch files and is used by version control systems."],["PARAMETERS","**-u**, **--unified**\n> Unified diff format (recommended)\n\n**-c**, **--context**\n> Context diff format\n\n**-y**, **--side-by-side**\n> Side-by-side comparison\n\n**-r**, **--recursive**\n> Recursively compare directories\n\n**-q**, **--brief**\n> Report only if files differ\n\n**-s**, **--report-identical-files**\n> Report when files are identical\n\n**-w**, **--ignore-all-space**\n> Ignore whitespace changes\n\n**-b**, **--ignore-space-change**\n> Ignore whitespace amount changes\n\n**-B**, **--ignore-blank-lines**\n> Ignore blank line changes\n\n**-i**, **--ignore-case**\n> Case-insensitive comparison\n\n**-N**, **--new-file**\n> Treat missing files as empty"],["OUTPUT FORMATS","**Normal format:**\n```\n3c3\n< old line\n---\n> new line\n```\n\n**Unified format (-u):**\n```\n--- file1.txt\n+++ file2.txt\n@@ -1,3 +1,3 @@\n context\n-old line\n+new line\n context\n```\n\n**Context format (-c):**\n```\n*** file1.txt\n--- file2.txt\n***************\n*** 1,3 ****\n  context\n! old line\n  context\n--- 1,3 ----\n  context\n! new line\n  context\n```"],["WORKFLOW","```bash"],["Basic comparison","diff file1.txt file2.txt"],["Unified diff (best for patches)","diff -u original.txt modified.txt"],["Save as patch","diff -u original.txt modified.txt > changes.patch"],["Compare directories","diff -r old_version/ new_version/"],["Ignore whitespace","diff -w file1.txt file2.txt"],["Brief output","diff -q file1.txt file2.txt"],["Side by side","diff -y file1.txt file2.txt | less\n```"],["CREATING PATCHES","```bash"],["Create patch","diff -u old.c new.c > fix.patch"],["Apply patch","patch old.c < fix.patch"],["Reverse patch","patch -R new.c < fix.patch\n```"],["DIRECTORY COMPARISON","```bash"],["Compare directories","diff -r dir1/ dir2/"],["Exclude files","diff -r --exclude=\"*.log\" dir1/ dir2/"],["Brief directory diff","diff -rq dir1/ dir2/\n```"],["EXIT STATUS","- **0** - Files are identical\n- **1** - Files differ\n- **2** - Error occurred"],["CAVEATS","Large files can be slow. Binary files show as \"differ\" without details. Whitespace handling varies. Line endings (CRLF vs LF) can cause issues. Not suitable for binary comparison (use cmp)."],["HISTORY","**diff** was written by **James W. Hunt** and **Doug McIlroy** at Bell Labs around **1974**, with the algorithm published in their joint paper in **1976**."],["INSTALL","```apt: sudo apt install diffutils```\n\n```dnf: sudo dnf install diffutils```\n\n```pacman: sudo pacman -S diffutils```\n\n```apk: sudo apk add diffutils```\n\n```zypper: sudo zypper install diffutils```\n\n```brew: brew install diffutils```\n\n```nix: nix profile install nixpkgs#diffutils```"],["SEE ALSO","[patch](/man/patch)(1), [git-diff](/man/git-diff)(1), [cmp](/man/cmp)(1), [vimdiff](/man/vimdiff)(1)"]],"e":["patch","git-diff","cmp","vimdiff"],"x":"diff [file1.txt] [file2.txt]"},"distcc":{"n":"distcc","t":"distributed C/C++ compilation across network hosts","s":[["TAGLINE","distributed C/C++ compilation across network hosts"],["TLDR","**Compile with distcc**\n\n```distcc [gcc] -c [file.c] -o [file.o]```\n\n**Set hosts for distribution**\n\n```export DISTCC_HOSTS=\"[host1] [host2] [localhost]\"```\n\n**Run make with distcc**\n\n```make -j [8] CC=\"distcc gcc\"```\n\n**Monitor compilation**\n\n```distccmon-text [1]```\n\n**Show host status**\n\n```distcc --show-hosts```"],["SYNOPSIS","**distcc** [_compiler_] [_args_]"],["DESCRIPTION","**distcc** distributes C/C++ compilation across multiple machines on a network. It sends preprocessing output to remote hosts for compilation, speeding up large builds by parallelizing across available CPUs.\n\nThe tool acts as a compiler wrapper, deciding whether to compile locally or remotely based on host availability and load. It's transparent to build systems that support CC variable override."],["PARAMETERS","**--show-hosts**\n> Print the list of hosts distcc would use, then exit.\n\n**--scan-includes**\n> Print the list of files that would be sent to the server, then exit.\n\n**--version**\n> Display the distcc client version.\n\n**-j**\n> Print the concurrency level (number of servers times per-host limit).\n\n**--help**\n> Display help information."],["ENVIRONMENT","**DISTCC_HOSTS**: Space-separated list of hosts\n**DISTCC_DIR**: Override state directory\n**DISTCC_LOG**: Log file path\n**DISTCC_VERBOSE**: Enable verbose logging"],["HOST SPECIFICATION","**hostname**: Simple hostname\n**hostname:port**: With custom port\n**hostname/limit**: Limit jobs to host\n**@hostname**: SSH mode\n**--localslots=N**: Local job slots"],["CONFIGURATION","**~/.distcc/hosts**\n> List of distcc hosts to use for distributed compilation.\n\n**/etc/distcc/hosts**\n> System-wide distcc host configuration.\n\n**~/.distcc/state**\n> State directory for distcc logs and temporary files."],["CAVEATS","Requires same compiler version on all hosts. Network latency adds overhead for small files. Preprocessing always done locally. Headers must be available locally."],["HISTORY","**distcc** was created by **Martin Pool** in **2002** at Linuxcare. It addressed the need to speed up Linux kernel and large project builds. The tool became widely used in open source development and was later maintained by the community."],["INSTALL","```apt: sudo apt install distcc```\n\n```dnf: sudo dnf install distcc```\n\n```pacman: sudo pacman -S distcc```\n\n```apk: sudo apk add distcc```\n\n```zypper: sudo zypper install distcc```\n\n```brew: brew install distcc```\n\n```nix: nix profile install nixpkgs#distcc```"],["SEE ALSO","[distccd](/man/distccd)(1), [ccache](/man/ccache)(1), [make](/man/make)(1), [gcc](/man/gcc)(1)"],["RESOURCES","```[Source code](https://github.com/distcc/distcc)```\n\n```[Homepage](https://www.distcc.org)```"]],"e":["distccd","ccache","make","gcc"],"x":"distcc [gcc] -c [file.c] -o [file.o]"},"dmevent_tool":{"n":"dmevent_tool","t":"device-mapper event monitoring control","s":[["TAGLINE","device-mapper event monitoring control"],["TLDR","**List registered events**\n\n```dmevent_tool -l```\n\n**Register device** for monitoring\n\n```dmevent_tool -R [device_name]```\n\n**Unregister device** from monitoring\n\n```dmevent_tool -U [device_name]```\n\n**Check monitor status**\n\n```dmevent_tool -s [device_name]```"],["SYNOPSIS","**dmevent_tool** [_options_] [_device_]"],["PARAMETERS","_DEVICE_\n> Device mapper device name.\n\n**-l**, **--list**\n> List registered devices.\n\n**-R**, **--register**\n> Register device for monitoring.\n\n**-U**, **--unregister**\n> Unregister device from monitoring.\n\n**-s**, **--status**\n> Show device monitoring status.\n\n**-h**, **--help**\n> Display help information."],["DESCRIPTION","**dmevent_tool** is a utility for interacting with the device-mapper event daemon (dmeventd). It allows registering and unregistering device mapper devices for event monitoring and querying their status.\n\nThe device-mapper event daemon monitors DM devices for events like failures, threshold crossings, or configuration changes. When events occur, dmeventd can trigger configured responses such as alerting or automatic repair.\n\nThis tool is typically used with LVM thin provisioning, RAID, and other device-mapper-based storage configurations that benefit from event-driven monitoring."],["CAVEATS","Requires dmeventd running. Root privileges needed. Only works with device-mapper devices. Event handling depends on configured plugins."],["HISTORY","dmevent_tool is part of the **LVM2** device-mapper tools developed by **Red Hat**. The device-mapper event monitoring infrastructure provides reactive storage management for enterprise Linux deployments."],["INSTALL","```pacman: sudo pacman -S dmraid```\n\n```apk: sudo apk add dmraid```\n\n```zypper: sudo zypper install dmraid```\n\n```nix: nix profile install nixpkgs#dmraid```"],["SEE ALSO","[dmsetup](/man/dmsetup)(1), [lvm](/man/lvm)(1)"]],"e":["dmsetup","lvm"],"x":"dmevent_tool -l"},"done":{"n":"done","t":"Shell reserved word that closes a loop body","s":[["TAGLINE","Shell reserved word that closes a loop body"],["TLDR","Close a **for** loop\n\n```for f in *.txt; do echo \"$f\"; done```\n\nClose a **while** loop\n\n```while read -r line; do echo \"$line\"; done < [file.txt]```\n\nClose an **until** loop\n\n```until ping -c1 [host]; do sleep 1; done```\n\n**Redirect the output** of an entire loop\n\n```for i in 1 2 3; do echo \"$i\"; done > [out.txt]```\n\n**Pipe a whole loop** into another command\n\n```for i in 1 2 3; do echo \"$i\"; done | sort -r```"],["SYNOPSIS","**for** _name_ [**in** _words_]; **do** _commands_; **done**\n\n**while** _condition_; **do** _commands_; **done**\n\n**until** _condition_; **do** _commands_; **done**\n\n**select** _name_ [**in** _words_]; **do** _commands_; **done**"],["DESCRIPTION","**done** is a shell reserved word, not a program: it terminates the body of a **for**, **while**, **until**, or **select** loop that was opened with **do**. Every **do** must be matched by a **done**, and the pair brackets the commands that run on each iteration.\n\nBecause **done** ends a compound command, redirections and pipes written after it apply to the loop as a whole rather than to the last command inside it. `done > out.txt` sends every iteration's output to the file, opening it once, which is both faster and more correct than redirecting on each pass through the loop.\n\nThe word is only special where a command is expected. Anywhere else, for instance as an argument, it is an ordinary string, which is why `echo done` simply prints the word.\n\nThe exit status of a loop is the exit status of the last command run in its body, or 0 if the body never ran."],["CAVEATS","**done** is a reserved word, so it cannot be used as a function or command name without quoting. A **do** with no matching **done** leaves the shell waiting for more input, which shows up as a hung script or a continuation prompt rather than a clear syntax error.\n\nIt is not universal across shells: **fish** and **csh** close their loops with **end**, so `done` belongs to the Bourne family (sh, bash, dash, ksh, zsh). It is also unrelated to `task done` in Taskwarrior, where `done` is an ordinary subcommand of that program rather than shell syntax."],["HISTORY","The `do ... done` bracketing comes from the **Bourne shell** in Unix Version 7 (**1979**). Stephen Bourne took the style from ALGOL 68, where compound statements are closed by reversing the opening keyword, the same instinct that gives the shell `if ... fi` and `case ... esac`. Loops never acquired a reversed `od`, so they were given the plain word `done` instead. POSIX standardised the syntax, and it behaves identically in every Bourne-family shell today."],["INSTALL","```aur: yay -S done```"],["SEE ALSO","[for](/man/for)(1), [while](/man/while)(1), [until](/man/until)(1), [break](/man/break)(1), [continue](/man/continue)(1), [bash](/man/bash)(1)"]],"e":["for","while","until","break","continue","bash"],"x":"for f in *.txt; do echo \"$f\"; done"},"drupal":{"n":"drupal","t":"CLI for Drupal code generation and site management","s":[["TAGLINE","CLI for Drupal code generation and site management"],["TLDR","**List all available commands**\n\n```drupal list```\n\n**Generate module scaffolding** (interactive)\n\n```drupal generate:module```\n\n**Generate a controller class**\n\n```drupal generate:controller```\n\n**Clear and rebuild all caches**\n\n```drupal cache:rebuild```\n\n**Install and enable a module**\n\n```drupal module:install [module_name]```\n\n**Run pending database updates**\n\n```drupal update:execute```\n\n**Check site status and configuration**\n\n```drupal site:status```"],["SYNOPSIS","**drupal** _command_ [_options_] [_arguments_]"],["PARAMETERS","_COMMAND_\n> Drupal Console command to execute.\n\n**generate:module**\n> Generate module scaffolding.\n\n**generate:controller**\n> Generate controller class.\n\n**cache:rebuild**\n> Clear all caches.\n\n**module:install** _NAME_\n> Install and enable module.\n\n**site:status**\n> Show site information.\n\n**update:execute**\n> Run database updates.\n\n**--help**\n> Display help information."],["CONFIGURATION","**~/.console/config.yml**\n> User-level configuration for Drupal Console, including default options and preferences.\n\n**drupal.yml**\n> Project-specific configuration file in Drupal root, defining module paths and custom settings."],["DESCRIPTION","**Drupal Console** is a CLI tool for generating boilerplate code, interacting with, and debugging Drupal 8+ applications. Built on the Symfony Console component, it accelerates development through code generation and provides utilities for common administrative tasks.\n\nThe generate commands create scaffolding for modules, plugins, controllers, forms, and other Drupal components following best practices. Interactive prompts guide through configuration options.\n\nDrupal Console also provides debugging tools, cache management, configuration import/export, and database operations from the command line."],["CAVEATS","Requires Drupal 8+ installation. Must be run from the Drupal root directory. Some commands require database access. The project is no longer actively maintained; **Drush** has adopted many of its features and is the recommended CLI tool for modern Drupal development."],["HISTORY","Drupal Console was created as a code generation and developer tool for **Drupal 8**, released around **2015**. It was inspired by Symfony Console and brought modern CLI development practices to Drupal."],["INSTALL","```pacman: sudo pacman -S drupal```\n\n```nix: nix profile install nixpkgs#drupal```"],["SEE ALSO","[drush](/man/drush)(1), [composer](/man/composer)(1), [php](/man/php)(1)"]],"e":["drush","composer","php"],"x":"drupal list"},"duckdb":{"n":"duckdb","t":"embedded analytical database engine","s":[["TAGLINE","embedded analytical database engine"],["TLDR","**Start an interactive SQL shell**\n\n```duckdb```\n\n**Open or create a database file**\n\n```duckdb [path/to/database.db]```\n\n**Execute a SQL query and exit**\n\n```duckdb -c \"[SELECT * FROM table_name]\"```\n\n**Query a CSV file directly**\n\n```duckdb -c \"[SELECT * FROM 'data.csv']\"```\n\n**Query a Parquet file**\n\n```duckdb -c \"[SELECT * FROM 'data.parquet']\"```\n\n**Execute SQL from a file**\n\n```duckdb -f [path/to/script.sql]```\n\n**Export query results to CSV**\n\n```duckdb -csv -c \"[COPY (SELECT * FROM table) TO 'output.csv' (HEADER)]\"```\n\n**Start in read-only mode**\n\n```duckdb -readonly [database.db]```\n\n**Output results as JSON**\n\n```duckdb -json -c \"[SELECT * FROM 'data.csv' LIMIT 10]\"```"],["SYNOPSIS","**duckdb** [_options_] [_database_file_] [_sql_commands_]"],["PARAMETERS","**-c** _COMMAND_\n> Execute the specified SQL command and exit.\n\n**-s** _COMMAND_\n> Execute the specified SQL statement and exit (alias for **-c**).\n\n**-cmd** _COMMAND_\n> Run command before reading stdin.\n\n**-f** _FILENAME_\n> Execute SQL from file (after processing ~/.duckdbrc).\n\n**-init** _FILENAME_\n> Run script on startup instead of ~/.duckdbrc.\n\n**-readonly**\n> Open database in read-only mode.\n\n**-no-stdin**\n> Exit after processing options instead of reading stdin.\n\n**-json**\n> Output results in JSON format.\n\n**-csv**\n> Output results in CSV format.\n\n**-table**\n> Output results as ASCII table.\n\n**-box**\n> Output results with box-drawing characters (default).\n\n**-markdown**\n> Output results as Markdown table.\n\n**-line**\n> Output results in line mode (one value per line).\n\n**-column**\n> Output results in columnar format.\n\n**-ascii**\n> Output results in ASCII table format.\n\n**-html**\n> Output results in HTML format.\n\n**-list**\n> Output results in list format.\n\n**-separator** _SEP_\n> Set column separator (default: |).\n\n**-newline** _SEP_\n> Set row separator (default: \\n).\n\n**-nullvalue** _TEXT_\n> Set text shown for NULL values.\n\n**-header**\n> Include column headers in output.\n\n**-noheader**\n> Exclude column headers from output.\n\n**-echo**\n> Print commands before execution.\n\n**-bail**\n> Stop after hitting an error.\n\n**-batch**\n> Force batch I/O.\n\n**-interactive**\n> Force interactive I/O.\n\n**-unsigned**\n> Allow loading of unsigned extensions.\n\n**-nofollow**\n> Refuse to open symbolic links to database files.\n\n**-version**\n> Print version and exit.\n\n**-help**\n> Display available options."],["CONFIGURATION","**~/.duckdbrc**\n> Initialization file with SQL commands executed on shell startup, used for setting preferences and defaults."],["DESCRIPTION","**DuckDB** is an embedded analytical database designed for fast online analytical processing (OLAP) workloads. It can run entirely in-process without a separate server, making it ideal for data analysis and scripting.\n\nA key feature is the ability to query files directly without importing: CSV, Parquet, JSON, and other formats can be used in SQL queries with automatic type detection. This makes DuckDB excellent for exploratory data analysis and ETL tasks.\n\nThe database supports standard SQL with analytical extensions including window functions, CTEs, and complex aggregations. It provides high performance through vectorized execution and columnar storage, optimized for aggregation queries over large datasets.\n\nIn interactive mode, DuckDB provides a full-featured SQL shell with tab completion, command history, and dot-commands for settings. Results can be output in various formats including tables, JSON, CSV, and Markdown. The default output mode is **duckbox**, a box-drawing format.\n\nDuckDB can be used as a library in Python, R, Java, Node.js, and other languages, or standalone via the CLI. Database files are portable across platforms and versions."],["CAVEATS","In-memory databases are lost when the process exits. Write operations lock the database file, limiting concurrent write access. Very large datasets may exceed available memory without proper configuration. Some SQL syntax differs slightly from other databases."],["HISTORY","**DuckDB** was created by Mark Raasveldt and Hannes Mühleisen at CWI Amsterdam (the research institute where PostgreSQL originated). Development started around **2018** with the goal of creating an embeddable analytical database akin to \"SQLite for analytics.\" The project gained significant adoption in the data science community starting in **2020**."],["INSTALL","```pacman: sudo pacman -S duckdb```\n\n```brew: brew install duckdb```\n\n```nix: nix profile install nixpkgs#duckdb```"],["SEE ALSO","[sqlite3](/man/sqlite3)(1), [psql](/man/psql)(1), [csvq](/man/csvq)(1), [jq](/man/jq)(1)"]],"e":["sqlite3","psql","csvq","jq"],"x":"duckdb"},"flask-unsign":{"n":"flask-unsign","t":"Flask session cookie manipulation and cracking","s":[["TAGLINE","Flask session cookie manipulation and cracking"],["TLDR","**Decode a Flask session cookie**\n\n```flask-unsign --decode --cookie \"[cookie_value]\"```\n\n**Brute force secret key**\n\n```flask-unsign --unsign --cookie \"[cookie]\" --wordlist [wordlist.txt]```\n\n**Sign a cookie with known key**\n\n```flask-unsign --sign --cookie \"{'user':'admin'}\" --secret \"[key]\"```\n\n**Fetch and decode** a cookie directly from a URL\n\n```flask-unsign --decode --server https://[example.com]/```\n\n**Show cookie without verification**\n\n```flask-unsign --decode --cookie \"[cookie]\" --no-verify```"],["SYNOPSIS","**flask-unsign** [_options_]"],["PARAMETERS","**--decode**\n> Decode a session cookie and print its contents.\n\n**--unsign**\n> Brute-force the secret key from a signed session cookie.\n\n**--sign**\n> Sign a cookie value with a known secret key.\n\n**--cookie** _value_\n> Session cookie value to operate on.\n\n**--server** _url_\n> Automatically fetch the session cookie from the given URL.\n\n**--secret**, **-S** _key_\n> Secret key used to sign or verify a cookie.\n\n**--wordlist** _file_\n> Wordlist file to use for brute-forcing the secret key.\n\n**--threads** _num_\n> Number of threads to use during brute-forcing.\n\n**--no-verify**\n> Skip signature verification when decoding.\n\n**--salt** _salt_\n> Custom salt string (default: `cookie-session`).\n\n**--legacy**\n> Use the older itsdangerous signing algorithm for compatibility with older Flask apps.\n\n**--no-literal-eval**\n> Disable Python literal evaluation of wordlist entries."],["DESCRIPTION","**Flask-unsign** is a security testing tool for analyzing and manipulating Flask session cookies. Flask stores session data in cryptographically signed cookies, and this tool can decode the contents, attempt to recover the secret key through brute-force attacks, and craft custom signed cookies.\n\nThe tool is primarily used in web application security assessments to test Flask applications for weak secret keys. If the secret key can be recovered, attackers could forge arbitrary session data, potentially leading to privilege escalation or authentication bypass vulnerabilities.\n\nFlask-unsign supports multithreaded brute-forcing, custom wordlists, and both encoding and decoding operations. It can work with sessions even when the signature verification fails, allowing inspection of tampered or expired cookies."],["INSTALL","```nix: nix profile install nixpkgs#flask-unsign```"],["SEE ALSO","[flask](/man/flask)(1), [hashcat](/man/hashcat)(1), [john](/man/john)(1)"]],"e":["flask","hashcat","john"],"x":"flask-unsign --decode --cookie \"[cookie_value]\""},"gif2webp":{"n":"gif2webp","t":"Convert GIF images to WebP format","s":[["TAGLINE","Convert GIF images to WebP format"],["TLDR","Encode a GIF as WebP and **write the output file**\n\n```gif2webp [input.gif] -o [output.webp]```"],["SYNOPSIS","**gif2webp** [_options_] _input.gif_ **-o** _output.webp_"],["PARAMETERS","**-o** _FILE_\n> Output WebP filename\n\n**-q** _QUALITY_\n> Quality factor (0-100), higher is better quality\n\n**-m** _METHOD_\n> Compression method (0-6), higher is slower but better\n\n**-lossy**\n> Use lossy compression (default is lossless for GIF)\n\n**-mixed**\n> Use mixed lossy/lossless compression per frame\n\n**-min_size**\n> Minimize output size (slower)\n\n**-loop_compatibility**\n> Use compatibility mode for GIF loop count\n\n**-f** _NUM_\n> Filter strength (0-100), for lossy compression\n\n**-metadata** _STRING_\n> Comma-separated metadata to copy (all, none, icc, xmp)\n\n**-v**\n> Verbose output"],["DESCRIPTION","**gif2webp** converts GIF images, including animated GIFs, to WebP format. WebP typically provides better compression than GIF while maintaining quality, resulting in smaller file sizes suitable for web use.\n\nFor animated GIFs, the tool preserves frame timing, loop count, and transparency. It supports both lossless compression (preserving exact pixel values) and lossy compression for even smaller files. The tool is part of Google's libwebp package."],["CAVEATS","Lossy compression may introduce artifacts, especially in images with sharp edges or text. Some older browsers don't support animated WebP. Very long or complex animations may take significant time to convert."],["HISTORY","gif2webp is part of the WebP project developed by Google. WebP was announced in **2010** as a new image format for the web, offering both lossy and lossless compression. The animated WebP format was added later to provide an alternative to animated GIFs."],["INSTALL","```apt: sudo apt install webp```\n\n```dnf: sudo dnf install libwebp-tools```\n\n```apk: sudo apk add libwebp-tools```\n\n```zypper: sudo zypper install libwebp-tools```\n\n```brew: brew install webp```"],["SEE ALSO","[cwebp](/man/cwebp)(1), [dwebp](/man/dwebp)(1), [convert](/man/convert)(1)"]],"e":["cwebp","dwebp","convert"],"x":"gif2webp [input.gif] -o [output.webp]"},"git-feature":{"n":"git-feature","t":"Create and manage feature branches with standardized workflow","s":[["TAGLINE","Create and manage feature branches with standardized workflow"],["TLDR","**Create feature branch**\n\n```git feature [feature_name]```\n\n**Create feature branch with remote tracking**\n\n```git feature [feature_name] -r [origin]```\n\n**Create feature branch from a specific start point**\n\n```git feature [feature_name] --from [develop]```\n\n**Finish feature branch (merge and delete)**\n\n```git feature finish [feature_name]```\n\n**Finish with squash merge**\n\n```git feature finish --squash [feature_name]```"],["SYNOPSIS","**git** **feature** [**-a** _prefix_] [**-s** _separator_] [**-r** [_remote_]] [**--from** _start_point_] _name_...\n\n**git** **feature** **finish** [**--squash**] _name_..."],["PARAMETERS","**-a** _PREFIX_, **--alias** _PREFIX_\n> Use a custom branch prefix instead of \"feature\".\n\n**-s** _SEPARATOR_, **--separator** _SEPARATOR_\n> Use a custom separator instead of \"/\".\n\n**-r** [_REMOTE_], **--remote** [_REMOTE_]\n> Set up a remote tracking branch (defaults to origin).\n\n**--from** _START_POINT_\n> Create the feature branch from a specific commit or branch.\n\n**--squash**\n> Perform a squash merge when finishing."],["SUBCOMMANDS","**finish**\n> Merge feature branch into the current branch and delete it."],["DESCRIPTION","**git feature** provides high-level commands for working with feature branches following a standardized naming convention. As part of the git-extras suite, it simplifies the common workflow of creating, developing, and completing feature work in separate branches.\n\nWhen creating a feature branch, the command automatically applies a \"feature/\" prefix to maintain consistent naming across the repository. The finish subcommand handles merging the feature back into the main development branch and cleaning up afterward, encapsulating multiple manual git operations into a single command."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-create-branch](/man/git-create-branch)(1), [git-extras](/man/git-extras)(1), [git-branch](/man/git-branch)(1), [git-flow](/man/git-flow)(1)"]],"e":["git-create-branch","git-extras","git-branch","git-flow"],"x":"git feature [feature_name]"},"git-rename-remote":{"n":"git-rename-remote","t":"Rename a Git remote","s":[["TAGLINE","Rename a Git remote"],["TLDR","**Rename remote**\n\n```git rename-remote [old_name] [new_name]```"],["SYNOPSIS","**git** **rename-remote** _old_name_ _new_name_"],["DESCRIPTION","**git rename-remote** renames a Git remote. Part of git-extras, it is a convenience wrapper around `git remote rename`."],["CAVEATS","Requires git-extras to be installed. The standard `git remote rename` command provides equivalent functionality and is available in all Git installations."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-remote](/man/git-remote)(1), [git-extras](/man/git-extras)(1)"]],"e":["git-remote","git-extras"],"x":"git rename-remote [old_name] [new_name]"},"gitk":{"n":"gitk","t":"Graphical Git repository browser","s":[["TAGLINE","Graphical Git repository browser"],["TLDR","**Start gitk**\n\n```gitk```\n\n**Show all branches**\n\n```gitk --all```\n\n**Show specific file history**\n\n```gitk [file.txt]```\n\n**Show specific range**\n\n```gitk [v1.0..v2.0]```\n\n**Show since date**\n\n```gitk --since=\"[2 weeks ago]\"```\n\n**Select a specific commit on load**\n\n```gitk --select-commit=[HEAD]```\n\n**Trace the history of a function**\n\n```gitk -L:[function_name]:[file.c]```"],["SYNOPSIS","**gitk** [_options_] [_revision-range_] [_--_] [_path_...]"],["PARAMETERS","_REVISION-RANGE_\n> Commits to display (e.g. `v1.0..v2.0`).\n\n_PATH_...\n> Limit to commits touching the given paths.\n\n**--all**\n> Show all refs (branches, tags, etc.) as if listed on the command line.\n\n**--branches**[=_PATTERN_]\n> Show all branches, optionally limited to a glob pattern.\n\n**--tags**[=_PATTERN_]\n> Show all tags, optionally limited to a glob pattern.\n\n**--remotes**[=_PATTERN_]\n> Show all remote-tracking branches, optionally limited to a glob pattern.\n\n**--since** _DATE_\n> Show commits more recent than the given date.\n\n**--until** _DATE_\n> Show commits older than the given date.\n\n**--date-order**\n> Sort commits by date rather than topologically.\n\n**--merge**\n> After a conflicted merge, show commits that modify the conflicted files on both branches.\n\n**--left-right**\n> Mark commits with `<` or `>` to indicate which side of a symmetric difference they come from.\n\n**--full-history**\n> Don't prune history when filtering by path.\n\n**--simplify-merges**\n> Used with **--full-history** to remove needless merges from the result.\n\n**-L** _start_,_end_:_file_ / **-L**:_funcname_:_file_\n> Trace the evolution of a line range or function within a file.\n\n**--argscmd** _COMMAND_\n> Command run each time gitk refreshes the revision range; its output lists additional revisions to show.\n\n**--select-commit** _REF_\n> Select the given commit after loading the graph (default `HEAD`).\n\n**--help**\n> Display help information."],["DESCRIPTION","**gitk** is a graphical repository browser for Git. It displays commit history in a visual graph, showing branches, merges, and commit details in a GUI window.\n\nThe interface shows the commit graph, file changes, and diff content. It supports search, filtering, and visual exploration of repository history."],["CAVEATS","Requires Tcl/Tk. Interface is dated. May not be installed by default."],["HISTORY","gitk was written by **Paul Mackerras** and is one of the original **Git** GUI tools, providing visual history browsing since Git's early days."],["INSTALL","```dnf: sudo dnf install gitk```\n\n```apk: sudo apk add git-gitk```\n\n```zypper: sudo zypper install gitk```"],["SEE ALSO","[git-gui](/man/git-gui)(1), [tig](/man/tig)(1), [git-log](/man/git-log)(1)"],["RESOURCES","```[Documentation](https://git-scm.com/docs/gitk)```"]],"e":["git-gui","tig","git-log"],"x":"gitk"},"go-list":{"n":"go-list","t":"list Go packages and module information","s":[["TAGLINE","list Go packages and module information"],["TLDR","**List current package**\n\n```go list```\n\n**List all packages**\n\n```go list ./...```\n\n**List as JSON**\n\n```go list -json [package]```\n\n**List with dependencies**\n\n```go list -deps [package]```\n\n**List modules instead of packages**\n\n```go list -m all```\n\n**Check for module updates**\n\n```go list -m -u all```\n\n**Custom format output**\n\n```go list -f '{{.ImportPath}}: {{.Dir}}'```\n\n**List available versions of a module**\n\n```go list -m -versions [module]```"],["SYNOPSIS","**go** **list** [**-f** _format_] [**-json**] [**-m**] [_flags_] [_packages_]"],["PARAMETERS","**-f** _format_\n> Custom output format using Go template syntax. Default is **{{.ImportPath}}**.\n\n**-json**\n> Output in JSON format. Optionally accepts comma-separated field names to limit output.\n\n**-m**\n> List modules instead of packages.\n\n**-deps**\n> Include all dependencies in depth-first post-order traversal.\n\n**-e**\n> Include erroneous packages without printing errors to standard error.\n\n**-u**\n> Show available updates (with **-m**). Also shows deprecation and retraction info.\n\n**-find**\n> Identify packages without resolving dependencies. Imports and Deps lists will be empty.\n\n**-compiled**\n> Set CompiledGoFiles to the Go source files presented to the compiler, including generated code from CgoFiles and SwigFiles.\n\n**-export**\n> Set the Export field to a file containing up-to-date export information and the BuildID field.\n\n**-test**\n> Report test binaries and their recompiled dependencies for the named packages.\n\n**-versions**\n> Set the Module's Versions field to all known versions (with **-m**).\n\n**-retracted**\n> Report information about retracted module versions (with **-m**)."],["DESCRIPTION","**go list** displays information about Go packages and modules. It enumerates packages, their source directories, import paths, and dependencies. With **-m**, it lists modules instead of packages. The **-f** flag allows custom formatting using Go template syntax with access to package struct fields like ImportPath, Dir, Deps, and GoFiles."],["INSTALL","```apt: sudo apt install gccgo-go```\n\n```dnf: sudo dnf install gcc-go```\n\n```pacman: sudo pacman -S gcc-go```\n\n```apk: sudo apk add gcc-go```\n\n```zypper: sudo zypper install gcc-go```\n\n```brew: brew install go```\n\n```nix: nix profile install nixpkgs#go```"],["SEE ALSO","[go](/man/go)(1), [go-mod](/man/go-mod)(1), [go-build](/man/go-build)(1), [go-get](/man/go-get)(1)"]],"e":["go","go-mod","go-build","go-get"],"x":"go list"},"goobook":{"n":"goobook","t":"Google Contacts command-line interface","s":[["TAGLINE","Google Contacts command-line interface"],["TLDR","Complete **OAuth2** setup so goobook can read Google Contacts\n\n```goobook authenticate```\n\nExport all contacts as **XML** on stdout\n\n```goobook dump_contacts```"],["SYNOPSIS","**goobook** _command_ [_arguments_]"],["PARAMETERS","**authenticate**\n> Authenticate with Google and store OAuth2 credentials\n\n**query** _QUERY_\n> Search contacts matching query\n\n**add**\n> Add contact from email headers (stdin)\n\n**dump_contacts**\n> Export all contacts to XML format\n\n**reload**\n> Reload and cache contacts from Google"],["DESCRIPTION","**goobook** provides command-line access to Google Contacts, designed primarily for integration with the mutt email client. It allows searching contacts for email address completion and adding new contacts from email headers.\n\nFor mutt integration, add to **.muttrc**:\n```\nset query_command=\"goobook query %s\"\n```\n\nConfiguration is stored in **~/.goobookrc** and includes Google API credentials obtained through the OAuth2 authentication flow."],["CONFIGURATION","**~/.goobookrc**\n> Configuration file containing Google API credentials, OAuth2 tokens, and cache settings."],["CAVEATS","Requires Google API credentials and OAuth2 authentication. Google's API quotas may limit frequent queries. Contact changes may take time to sync. The Google Contacts API access requires proper OAuth scopes."],["HISTORY","goobook was created to bridge the gap between command-line email clients like mutt and Google's contact management. It provides a simple interface to Google Contacts that works well with Unix text-processing tools and email workflows."],["INSTALL","```apk: sudo apk add goobook```\n\n```nix: nix profile install nixpkgs#goobook```"],["SEE ALSO","[mutt](/man/mutt)(1)"]],"e":["mutt"],"x":"goobook authenticate"},"hazelnut":{"n":"hazelnut","t":"Terminal-based automated file organizer","s":[["TAGLINE","Terminal-based automated file organizer"],["TLDR","**Launch the TUI to manage rules**\n\n```hazelnut```\n\n**Start the background daemon to watch folders**\n\n```hazelnutd```\n\n**Initialize a new configuration**\n\n```hazelnut init```\n\n**List all configured rules**\n\n```hazelnut list```\n\n**Dry-run rules on a directory**\n\n```hazelnut run --dir [~/Downloads]```\n\n**Apply rules on a directory**\n\n```hazelnut run --dir [~/Downloads] --apply```\n\n**Use a custom config file**\n\n```hazelnut --config [path/to/config.toml]```"],["SYNOPSIS","**hazelnut** [_command_] [_options_]\n\n**hazelnutd** [_options_]"],["PARAMETERS","**init**\n> Initialize a new configuration file.\n\n**list**\n> List all configured rules.\n\n**run**\n> Dry-run or apply rules on a directory.\n\n**update**\n> Check for updates.\n\n**--config** _path_\n> Specify a custom config file.\n\n**--dir** _path_\n> Specify a directory to process (used with run).\n\n**--apply**\n> Apply rules instead of dry-running (used with run)."],["DESCRIPTION","**hazelnut** is a terminal-based automated file organizer inspired by **Hazel** for macOS. It watches folders and organizes files based on user-defined rules. The tool consists of two components: a **TUI** for creating and managing rules interactively, and a **daemon** (hazelnutd) that runs in the background applying rules continuously.\n\nRules are defined in human-readable **TOML** configuration files and can trigger actions like moving, copying, renaming, archiving, deleting, trashing, or running custom commands on matched files. Matching conditions include file extension, name pattern, size, and age."],["CONFIGURATION","Rules are stored in TOML config files. Each rule specifies a watch folder, file matching conditions (by extension, name pattern, size, age), and actions to perform. Multiple rules can match the same file and all matching rules execute in order."],["CAVEATS","The daemon (hazelnutd) is only available on **macOS** and **Linux**; on Windows only the TUI is available. File watching relies on OS-level filesystem notification APIs, which may have limitations on certain filesystems."],["HISTORY","**hazelnut** was created by **Ricardo Dantas** and is written in **Rust** using the **Ratatui** TUI library. It was inspired by the popular macOS automation app Hazel, bringing similar file organization capabilities to the terminal."],["INSTALL","```pacman: sudo pacman -S hazelnut```"],["SEE ALSO","[watch](/man/watch)(1), [find](/man/find)(1), [mv](/man/mv)(1)"]],"e":["watch","find","mv"],"x":"hazelnut"},"help2man":{"n":"help2man","t":"generates man pages from program --help and --version output","s":[["TAGLINE","generates man pages from program --help and --version output"],["TLDR","**Generate a man page from a program's help output**\n\n```help2man [program] > [program.1]```\n\n**Generate with a specific man page section number**\n\n```help2man -s [1] [program] > [program.1]```\n\n**Add a name/description for the NAME section**\n\n```help2man -n \"program description\" [program] > [program.1]```\n\n**Include additional text from an include file**\n\n```help2man --include [extra.h2m] [program] > [program.1]```\n\n**Write output directly to a file**\n\n```help2man -o [program.1] [program]```\n\n**Suppress \"info\" pointer in the generated page**\n\n```help2man -N [program] > [program.1]```"],["SYNOPSIS","**help2man** [_options_] _executable_"],["PARAMETERS","_EXECUTABLE_\n> Program whose --help and --version output will be used to generate the man page.\n\n**-n**, **--name** _STRING_\n> Short description for the NAME section.\n\n**-s**, **--section** _NUM_\n> Man page section number (default: 1).\n\n**-i**, **--include** _FILE_\n> Include extra material from file. The file uses `[section]` headers to place text.\n\n**-I**, **--opt-include** _FILE_\n> Like **-i** but does not fail if file is missing.\n\n**-N**, **--no-info**\n> Suppress the \"see also info page\" pointer.\n\n**-o**, **--output** _FILE_\n> Write output to file instead of stdout.\n\n**-m**, **--manual** _TEXT_\n> Name of the manual (e.g., \"User Commands\").\n\n**-S**, **--source** _TEXT_\n> Source of the program (e.g., \"FSF\", \"Debian\").\n\n**-L**, **--locale** _STRING_\n> Set locale for fetching help output (default: \"C\").\n\n**-l**, **--libtool**\n> Drop libtool `lt-` prefix from program name.\n\n**-p**, **--info-page** _TEXT_\n> Name of Texinfo manual to reference.\n\n**-h**, **--help-option** _STRING_\n> Alternative help option string to use instead of --help.\n\n**-v**, **--version-option** _STRING_\n> Alternative version option string to use instead of --version.\n\n**--version-string** _STRING_\n> Use this version string instead of running the program's version option.\n\n**--no-discard-stderr**\n> Include stderr output when parsing option output.\n\n**--version**\n> Show help2man version."],["DESCRIPTION","**help2man** generates man pages from a program's `--help` and `--version` output. It parses the structured help text and formats it as troff-style man page source, creating basic documentation automatically.\n\nThe tool produces minimal man pages suitable for programs that follow GNU conventions for help output. Additional content can be added via include files (`.h2m`), which support inserting text before or after automatically generated sections using `[section]` headers."],["CAVEATS","Output quality depends heavily on the format of the program's `--help` output. Works best with GNU-style option formatting (leading dashes, aligned descriptions). Programs with non-standard help text may produce poorly formatted man pages that require manual editing. Does not document features not mentioned in `--help` output."],["HISTORY","help2man was created by **Brendan O'Dea** as part of the **GNU** project to automate man page generation for GNU utilities during the build process."],["INSTALL","```apt: sudo apt install help2man```\n\n```dnf: sudo dnf install help2man```\n\n```pacman: sudo pacman -S help2man```\n\n```apk: sudo apk add help2man```\n\n```zypper: sudo zypper install help2man```\n\n```brew: brew install help2man```\n\n```nix: nix profile install nixpkgs#help2man```"],["SEE ALSO","[man](/man/man)(1), [pod2man](/man/pod2man)(1), [ronn](/man/ronn)(1)"]],"e":["man","pod2man","ronn"],"x":"help2man [program] > [program.1]"},"hg":{"n":"hg","t":"distributed version control system","s":[["TAGLINE","distributed version control system"],["TLDR","**Initialize repository**\n\n```hg init```\n\n**Clone repository**\n\n```hg clone [url]```\n\n**Show status**\n\n```hg status```\n\n**Add files**\n\n```hg add [files]```\n\n**Commit changes**\n\n```hg commit -m \"[message]\"```\n\n**View log**\n\n```hg log```\n\n**Pull changes**\n\n```hg pull```\n\n**Push changes**\n\n```hg push```"],["SYNOPSIS","**hg** _command_ [_options_]"],["PARAMETERS","_COMMAND_\n> Mercurial command.\n\n**init**\n> Create repository.\n\n**clone**\n> Clone repository.\n\n**status**\n> Show working directory status.\n\n**add**\n> Add files.\n\n**commit**\n> Commit changes.\n\n**log**\n> Show history.\n\n**pull**\n> Pull from remote.\n\n**push**\n> Push to remote.\n\n**-R**, **--repository** _REPO_\n> Run as if started in the given repository.\n\n**-v**, **--verbose**\n> Increase amount of output.\n\n**-q**, **--quiet**\n> Suppress output.\n\n**--version**\n> Output version information.\n\n**--help**\n> Display help information."],["DESCRIPTION","**Mercurial** (hg) is a distributed version control system. It tracks changes in source code and supports branching, merging, and collaboration.\n\nMercurial is similar to Git but with different command syntax and philosophy. It emphasizes simplicity and consistency in its interface."],["CONFIGURATION","Mercurial reads settings from several **hgrc** files, in order: the system-wide file (e.g. **/etc/mercurial/hgrc**), the per-user file (**~/.hgrc**), and the per-repository file (**.hg/hgrc**). Later files override earlier ones. Common sections include **[ui]** for username and editor, **[paths]** for remote aliases, and **[extensions]** to enable built-in or third-party extensions."],["CAVEATS","Less popular than Git. Different workflow. Extension system for features."],["HISTORY","Mercurial was created by **Matt Mackall** in **2005**, shortly after Git, as another solution for Linux kernel development."],["INSTALL","```apt: sudo apt install mercurial```\n\n```dnf: sudo dnf install mercurial```\n\n```pacman: sudo pacman -S mercurial```\n\n```apk: sudo apk add mercurial```\n\n```zypper: sudo zypper install mercurial```\n\n```brew: brew install mercurial```\n\n```nix: nix profile install nixpkgs#mercurial```"],["SEE ALSO","[git](/man/git)(1), [svn](/man/svn)(1), [hg-clone](/man/hg-clone)(1), [hg-commit](/man/hg-commit)(1), [hg-init](/man/hg-init)(1)"],["RESOURCES","```[Source code](https://foss.heptapod.net/mercurial/mercurial-devel)```\n\n```[Homepage](https://www.mercurial-scm.org/)```\n\n```[Documentation](https://wiki.mercurial-scm.org/)```"]],"e":["git","svn","hg-clone","hg-commit","hg-init"],"x":"hg init"},"httpflow":{"n":"httpflow","t":"captures and displays HTTP traffic in real-time","s":[["TAGLINE","captures and displays HTTP traffic in real-time"],["TLDR","**Capture HTTP traffic on interface**\n\n```sudo httpflow -i [eth0]```\n\n**Filter by port**\n\n```sudo httpflow -i [eth0] 'port 80'```\n\n**Save to file**\n\n```sudo httpflow -i [eth0] -w [output]```\n\n**Read from pcap**\n\n```httpflow -r [capture.pcap]```"],["SYNOPSIS","**httpflow** [_options_] [_filter_]"],["PARAMETERS","_FILTER_\n> BPF filter expression.\n\n**-i** _INTERFACE_\n> Network interface.\n\n**-r** _FILE_\n> Read from pcap file.\n\n**-w** _DIR_\n> Write output directory.\n\n**-u** _URL_\n> Filter by URL pattern.\n\n**-d**\n> Print debug info.\n\n**--help**\n> Display help information."],["DESCRIPTION","**httpflow** captures and displays HTTP traffic in real-time. It reconstructs HTTP requests and responses from network packets.\n\nThe tool is useful for debugging, monitoring, and analyzing web traffic. It displays headers, bodies, and request/response pairs."],["CAVEATS","Requires root for live capture. Cannot decode HTTPS. Privacy considerations apply."],["HISTORY","**httpflow** is an open-source utility built on **libpcap** that reassembles TCP streams to reconstruct HTTP requests and responses for real-time traffic analysis and debugging."],["INSTALL","```brew: brew install httpflow```"],["SEE ALSO","[tcpdump](/man/tcpdump)(1), [wireshark](/man/wireshark)(1), [mitmproxy](/man/mitmproxy)(1)"],["RESOURCES","```[Source code](https://github.com/six-ddc/httpflow)```"]],"e":["tcpdump","wireshark","mitmproxy"],"x":"sudo httpflow -i [eth0]"},"i7z":{"n":"i7z","t":"real-time reporting tool for Intel Core i3, i5, and i7 processors","s":[["TAGLINE","real-time reporting tool for Intel Core i3, i5, and i7 processors"],["TLDR","**Start** i7z monitoring (requires root)\n\n```sudo i7z```"],["SYNOPSIS","**i7z** [_options_]"],["DESCRIPTION","**i7z** is a real-time reporting tool for Intel Core i3, i5, and i7 processors. It displays CPU frequency, temperature, turbo boost states, and power management information in a console-based interface.\n\nThe tool reads CPU-specific MSRs (Model Specific Registers) to provide accurate information about processor performance states, including per-core frequencies and C-state residency."],["CAVEATS","Only works with Intel Core i3, i5, and i7 processors (Sandy Bridge and later). Requires root privileges to access CPU MSRs. May not work correctly with very new processor generations. Has been superseded by more modern tools like turbostat."],["HISTORY","i7z was developed to provide detailed Intel processor monitoring when existing tools lacked support for newer Intel processor features like Turbo Boost. Development started around **2009** with the introduction of the Intel Core i7 processor family."],["INSTALL","```apt: sudo apt install i7z```\n\n```nix: nix profile install nixpkgs#i7z```"],["SEE ALSO","[turbostat](/man/turbostat)(8), [cpupower](/man/cpupower)(1), [sensors](/man/sensors)(1)"]],"e":["turbostat","cpupower","sensors"],"x":"sudo i7z"},"idevicebackup2":{"n":"idevicebackup2","t":"creates and restores backups of iOS devices running iOS 4 and later","s":[["TAGLINE","creates and restores backups of iOS devices running iOS 4 and later"],["TLDR","**Create full backup**\n\n```idevicebackup2 backup [backup-dir]```\n\n**Restore from backup**\n\n```idevicebackup2 restore [backup-dir]```\n\n**List files in backup**\n\n```idevicebackup2 list [backup-dir]```\n\n**Backup specific device**\n\n```idevicebackup2 -u [device-udid] backup [backup-dir]```\n\n**Force full backup**\n\n```idevicebackup2 backup --full [backup-dir]```\n\n**Enable backup encryption**\n\n```idevicebackup2 encryption on [password]```"],["SYNOPSIS","**idevicebackup2** [_OPTIONS_] _CMD_ [_CMDOPTIONS_] _DIRECTORY_"],["SUBCOMMANDS","**backup**\n> Create device backup. Use --full to force full backup.\n\n**restore**\n> Restore from backup. Supports --system, --settings, --remove, --copy, --skip-apps, --no-reboot, --password.\n\n**list**\n> List files of last completed backup in CSV format.\n\n**unback**\n> Unpack a completed backup.\n\n**encryption** on|off [_PWD_]\n> Enable or disable backup encryption.\n\n**changepw** [_OLD_] [_NEW_]\n> Change backup password on target device.\n\n**cloud** on|off\n> Enable or disable cloud use (requires iCloud account).\n\n**info**\n> Show details about last completed backup."],["PARAMETERS","**-u**, **--udid** _UDID_\n> Target specific device by UDID.\n\n**-s**, **--source** _UDID_\n> Use backup data from device specified by UDID.\n\n**-i**, **--interactive**\n> Request passwords interactively on the command line.\n\n**-n**, **--network**\n> Connect to network device.\n\n**-d**, **--debug**\n> Enable communication debugging."],["DESCRIPTION","**idevicebackup2** creates and restores backups of iOS devices running iOS 4 and later. Part of the libimobiledevice suite. Supports encrypted backups, incremental backups, and restoring backups from different devices."],["INSTALL","```apt: sudo apt install libimobiledevice-utils```\n\n```dnf: sudo dnf install libimobiledevice-utils```\n\n```apk: sudo apk add libimobiledevice-progs```"],["SEE ALSO","[ideviceinfo](/man/ideviceinfo)(1), [idevicebackup](/man/idevicebackup)(1)"]],"e":["ideviceinfo","idevicebackup"],"x":"idevicebackup2 backup [backup-dir]"},"ip6tables":{"n":"ip6tables","t":"IPv6 packet filter administration tool","s":[["TAGLINE","IPv6 packet filter administration tool"],["TLDR","**List** all rules\n\n```sudo ip6tables -L```\n\n**List** rules with line numbers\n\n```sudo ip6tables -L --line-numbers```\n\n**Accept** traffic on port\n\n```sudo ip6tables -A INPUT -p tcp --dport 22 -j ACCEPT```\n\n**Drop** traffic from address\n\n```sudo ip6tables -A INPUT -s 2001:db8::1 -j DROP```\n\n**Delete** rule by number\n\n```sudo ip6tables -D INPUT 1```\n\n**Flush** all rules\n\n```sudo ip6tables -F```"],["SYNOPSIS","**ip6tables** [_OPTIONS_] [_chain_] [_rule-specification_]"],["DESCRIPTION","**ip6tables** is the IPv6 packet filter administration tool. It has the same syntax and functionality as iptables but operates on IPv6 traffic. It manages rules for filtering, NAT, and packet mangling."],["PARAMETERS","**-L, --list**\n> List all rules in the selected chain.\n\n**-A, --append**\n> Append a rule to the end of chain.\n\n**-D, --delete**\n> Delete a rule from chain.\n\n**-I, --insert**\n> Insert a rule at position.\n\n**-R, --replace**\n> Replace a rule in the selected chain.\n\n**-F, --flush**\n> Flush (delete all rules in) a chain.\n\n**-N, --new-chain**\n> Create a user-defined chain.\n\n**-X, --delete-chain**\n> Delete a user-defined chain.\n\n**-P, --policy**\n> Set default policy for a chain (ACCEPT/DROP).\n\n**-Z, --zero**\n> Zero the packet and byte counters.\n\n**-p, --protocol**\n> Protocol to match (tcp, udp, icmpv6).\n\n**--dport**\n> Destination port.\n\n**--sport**\n> Source port.\n\n**-s, --source**\n> Source address.\n\n**-d, --destination**\n> Destination address.\n\n**-i, --in-interface**\n> Interface a packet was received on.\n\n**-o, --out-interface**\n> Interface a packet is going to be sent on.\n\n**-j, --jump**\n> Target (ACCEPT, DROP, REJECT, LOG, etc.).\n\n**-v, --verbose**\n> Verbose output (show counters and interfaces).\n\n**-n, --numeric**\n> Show numeric addresses and ports instead of resolving names.\n\n**--line-numbers**\n> Show rule numbers when listing."],["CAVEATS","IPv6 and IPv4 rules are managed separately. Rules are not persistent by default - use ip6tables-save/restore. Consider migrating to nftables for unified IPv4/IPv6 management."],["HISTORY","**ip6tables** is the IPv6 counterpart to **iptables**, part of the netfilter framework. It is being superseded by nftables."],["INSTALL","```pacman: sudo pacman -S iptables```\n\n```apk: sudo apk add iptables```\n\n```zypper: sudo zypper install iptables```\n\n```brew: brew install iptables```\n\n```nix: nix profile install nixpkgs#iptables```"],["SEE ALSO","[iptables](/man/iptables)(8), [ip6tables-save](/man/ip6tables-save)(8), [nft](/man/nft)(8)"]],"e":["iptables","ip6tables-save","nft"],"x":"sudo ip6tables -L"},"keyd":{"n":"keyd","t":"key remapping daemon for Linux using kernel-level input","s":[["TAGLINE","key remapping daemon for Linux using kernel-level input"],["TLDR","**Start** and enable the keyd service\n\n```systemctl enable keyd --now```\n\nDisplay **keypress** information (monitor mode)\n\n```sudo keyd monitor```\n\nDisplay keypress events with **timestamps**\n\n```sudo keyd monitor -t```\n\n**Reload** configuration files\n\n```sudo keyd reload```\n\n**List** all valid key names\n\n```keyd list-keys```\n\nCreate a **temporary binding**\n\n```sudo keyd bind \"[pressed_key] = [output_key]\"```\n\n**Reset** all temporary bindings\n\n```sudo keyd bind reset```\n\n**Input** text as if typed on the keyboard\n\n```sudo keyd input \"[text]\"```"],["SYNOPSIS","**keyd** [_options_] [_command_]"],["PARAMETERS","**monitor** [**-t**]\n> Display keypress information in real-time. If -t is supplied, also prints time since the last event in milliseconds.\n\n**listen**\n> Print layer state changes of the running keyd daemon to stdout. Useful for scripting.\n\n**reload**\n> Reset bindings and reload configuration from /etc/keyd.\n\n**list-keys**\n> Display all valid key names.\n\n**bind** reset|_BINDING_ [_BINDING_...]\n> Apply the supplied key bindings, or reset all bindings.\n\n**input** [**-t** _timeout_] _text_ [_text_...]\n> Input the supplied text as keyboard events. If no arguments are given, read from stdin. Timeout in microseconds sets delay between emitted events.\n\n**-v**, **--version**\n> Print the current version and exit.\n\n**-h**, **--help**\n> Print help and exit."],["DESCRIPTION","**keyd** is a key remapping daemon that operates at the kernel level using evdev. It allows remapping keys system-wide, independent of the display server (works with X11, Wayland, and virtual consoles).\n\nConfiguration files in /etc/keyd/ define key mappings, layers, and macros. The daemon intercepts input events and transforms them according to the configuration before passing them to applications. Features include layering, oneshot modifiers, and macros."],["CAVEATS","Requires root privileges for most operations. Configuration changes require a reload via `keyd reload`. Works at a lower level than X11 keymaps, so remappings apply everywhere including virtual consoles."],["HISTORY","keyd was developed as a modern, simple key remapping solution that works universally across display servers. It provides functionality similar to tools like xmodmap but operates at the kernel input level."],["INSTALL","```apt: sudo apt install onak```\n\n```pacman: sudo pacman -S keyd```\n\n```apk: sudo apk add keyd```\n\n```zypper: sudo zypper install keyd```\n\n```brew: brew install keyd```\n\n```nix: nix profile install nixpkgs#keyd```"],["SEE ALSO","[xmodmap](/man/xmodmap)(1), [setxkbmap](/man/setxkbmap)(1), [evtest](/man/evtest)(1), [systemctl](/man/systemctl)(1)"]],"e":["xmodmap","setxkbmap","evtest","systemctl"],"x":"systemctl enable keyd --now"},"kinfocenter":{"n":"kinfocenter","t":"KDE information center application that displays detailed system information","s":[["TAGLINE","KDE information center application that displays detailed system information"],["TLDR","Open the **GUI**\n\n```kinfocenter```\n\n**List** all available KCM modules\n\n```kinfocenter --list```\n\nDisplay **version** information\n\n```kinfocenter -v```"],["SYNOPSIS","**kinfocenter** [_options_]"],["PARAMETERS","**--list**\n> List all available KCM (KDE Control Module) modules.\n\n**-h**, **--help**\n> Display help message.\n\n**-v**, **--version**\n> Display version information."],["DESCRIPTION","**kinfocenter** is the KDE Plasma information center. It provides a centralized view of hardware and software configuration including CPU, memory, graphics, audio, network, and USB devices.\n\nThe application loads various KCM modules, each providing information about different system aspects. It's useful for troubleshooting and understanding system capabilities."],["CAVEATS","Only available on KDE Plasma desktop. Some information modules may require additional packages. Hardware detection depends on kernel and driver support."],["HISTORY","kinfocenter has been part of KDE since the early KDE 3 era, evolving with each major KDE release to provide comprehensive system information in a user-friendly interface."],["INSTALL","```apt: sudo apt install kinfocenter```\n\n```dnf: sudo dnf install kinfocenter```\n\n```pacman: sudo pacman -S kinfocenter```\n\n```apk: sudo apk add kinfocenter```"],["SEE ALSO","[inxi](/man/inxi)(1), [lshw](/man/lshw)(1), [systemctl](/man/systemctl)(1)"]],"e":["inxi","lshw","systemctl"],"x":"kinfocenter"},"kpackagetool6":{"n":"kpackagetool6","t":"Install, list, and remove KDE Plasma 6 packages","s":[["TAGLINE","Install, list, and remove KDE Plasma 6 packages"],["TLDR","**List all available package types**\n\n```kpackagetool6 --list-types```\n\n**Install a package from a directory or file**\n\n```kpackagetool6 -t [Plasma/Applet] -i [path/to/package]```\n\n**Upgrade an installed package**\n\n```kpackagetool6 -t [Plasma/Applet] -u [path/to/package]```\n\n**List installed plasmoids (widgets)**\n\n```kpackagetool6 -t Plasma/Applet -l```\n\n**List globally installed plasmoids (all users)**\n\n```kpackagetool6 -t Plasma/Applet -l -g```\n\n**Remove a plasmoid by name**\n\n```kpackagetool6 -t Plasma/Applet -r \"[name]\"```\n\n**Show information about an installed package**\n\n```kpackagetool6 -t [Plasma/Applet] -s \"[name]\"```"],["SYNOPSIS","**kpackagetool6** [_options_]"],["PARAMETERS","**--list-types**\n> List all available package types.\n\n**-t**, **--type** _TYPE_\n> Package type (Plasma/Applet, Plasma/Theme, Plasma/Wallpaper, Plasma/LookAndFeel, etc.).\n\n**-i**, **--install** _PATH_\n> Install package from a directory or file.\n\n**-u**, **--upgrade** _PATH_\n> Upgrade an existing package.\n\n**-r**, **--remove** _NAME_\n> Remove an installed package.\n\n**-l**, **--list**\n> List installed packages of the given type.\n\n**-s**, **--show** _NAME_\n> Show information about an installed package.\n\n**-g**, **--global**\n> Operate on global (system-wide) packages.\n\n**-p**, **--packageroot** _PATH_\n> Absolute path to the package root instead of standard data directories.\n\n**--appstream-metainfo** _PATH_\n> Output AppStream metadata for the package."],["DESCRIPTION","**kpackagetool6** manages KDE Plasma 6 packages including plasmoids (widgets), themes, wallpapers, KWin scripts, and other Plasma components. It is the KDE Frameworks 6 version of the package management tool.\n\nPackage types include Plasma/Applet (widgets), Plasma/Theme, Plasma/Wallpaper, Plasma/LookAndFeel, KWin/Script, and more. User packages are stored in `~/.local/share/plasma/`. It manages packages installed via KPackage or store.kde.org, not system packages from distribution repositories."],["CAVEATS","KDE 6 specific; for KDE 5 use kpackagetool5. Some packages may require Plasma restart to take effect. Global installation requires root privileges."],["HISTORY","kpackagetool6 is part of KDE Frameworks 6, the successor to KDE Frameworks 5. It continues the modular package management system for Plasma components."],["INSTALL","```apt: sudo apt install kpackagetool6```\n\n```pacman: sudo pacman -S kpackage```\n\n```apk: sudo apk add kpackage```\n\n```zypper: sudo zypper install kpackage```"],["SEE ALSO","[kpackagetool5](/man/kpackagetool5)(1)"]],"e":["kpackagetool5"],"x":"kpackagetool6 --list-types"},"linode-cli-volumes":{"n":"linode-cli-volumes","t":"manages Linode block storage volumes","s":[["TAGLINE","manages Linode block storage volumes"],["TLDR","**List volumes**\n\n```linode-cli volumes list```\n\n**Create volume**\n\n```linode-cli volumes create --label [my-volume] --size [20] --region [us-east]```\n\n**Attach to Linode**\n\n```linode-cli volumes attach [volume_id] --linode_id [linode_id]```\n\n**Detach volume**\n\n```linode-cli volumes detach [volume_id]```\n\n**View volume**\n\n```linode-cli volumes view [volume_id]```\n\n**Resize volume**\n\n```linode-cli volumes resize [volume_id] --size [40]```\n\n**Delete volume**\n\n```linode-cli volumes delete [volume_id]```"],["SYNOPSIS","**linode-cli volumes** _subcommand_ [_options_]"],["PARAMETERS","_SUBCOMMAND_\n> Volume operation.\n\n**list**\n> List all volumes.\n\n**create**\n> Create new volume.\n\n**attach** _ID_\n> Attach to Linode.\n\n**detach** _ID_\n> Detach from Linode.\n\n**view** _ID_\n> View volume details.\n\n**resize** _ID_\n> Resize volume (can only increase size).\n\n**clone** _ID_\n> Clone volume to a new volume.\n\n**delete** _ID_\n> Delete volume.\n\n**--label** _NAME_\n> Volume name.\n\n**--size** _GB_\n> Size in gigabytes (min: 10, max: 10240).\n\n**--region** _REGION_\n> Region to create volume in (e.g., us-east).\n\n**--linode_id** _ID_\n> Linode to attach volume to on creation.\n\n**--format** _FORMAT_\n> Output format: json or text.\n\n**--help**\n> Display help information."],["DESCRIPTION","**linode-cli volumes** manages Linode block storage volumes. Volumes provide persistent storage for instances.\n\nThe tool creates, attaches, resizes, and manages block storage independent of Linode lifecycle."],["CAVEATS","Requires authentication. Billed by size. Same region as Linode for attach."],["HISTORY","linode-cli volumes is part of the **Linode CLI** for managing block storage volumes on Linode cloud."],["INSTALL","```brew: brew install linode-cli```\n\n```nix: nix profile install nixpkgs#linode-cli```"],["SEE ALSO","[linode-cli](/man/linode-cli)(1), [linode-cli-linodes](/man/linode-cli-linodes)(1)"]],"e":["linode-cli","linode-cli-linodes"],"x":"linode-cli volumes list"},"lsof":{"n":"lsof","t":"lists open files","s":[["TAGLINE","lists open files"],["TLDR","**List all open files**\n\n```lsof```\n\n**Files opened by user**\n\n```lsof -u [username]```\n\n**Files opened by process**\n\n```lsof -p [pid]```\n\n**Files using port**\n\n```lsof -i :[port]```\n\n**Network connections**\n\n```lsof -i```\n\n**Files in directory**\n\n```lsof +D [/path/to/dir]```\n\n**Process using file**\n\n```lsof [/path/to/file]```\n\n**Find unlinked-but-open files** (useful when disk is full)\n\n```sudo lsof +L1```\n\n**Get only PIDs** for piping to kill\n\n```lsof -t -i :[port]```\n\n**Watch a connection** every 5 seconds\n\n```lsof -i :[port] -r [5]```"],["SYNOPSIS","**lsof** [_options_] [_files_]"],["PARAMETERS","_FILES_\n> Specific files to check.\n\n**-u** _USER_\n> Files opened by user.\n\n**-p** _PID_\n> Files opened by process.\n\n**-i** [_ADDR_]\n> Network connections.\n\n**+D** _DIR_\n> Files in directory.\n\n**-c** _COMMAND_\n> Files opened by processes whose command name starts with _COMMAND_.\n\n**-n**\n> Inhibit conversion of network numbers to host names (faster).\n\n**-P**\n> Inhibit conversion of port numbers to service names.\n\n**-t**\n> Terse output: list PIDs only (useful for piping to **kill**).\n\n**-F** _FIELDS_\n> Field-formatted output for machine parsing (e.g., **-Fpcu** for PID, command, user).\n\n**-r** [_seconds_]\n> Repeat mode: re-list every _seconds_ (default 15) until interrupted.\n\n**+L1**\n> Show only files with link count less than 1 (i.e. unlinked but still open — useful for finding \"deleted but open\" files filling a disk).\n\n**--help**\n> Display help information."],["DESCRIPTION","**lsof** (List Open Files) is a diagnostic tool that reports all files currently opened by running processes. In Unix-like systems, the concept of a \"file\" extends well beyond regular files on disk -- it includes network sockets, Unix domain sockets, pipes, device files, and directories. Each open file is represented internally by a file descriptor, and lsof maps these descriptors back to the processes that hold them.\n\nThis makes lsof an essential troubleshooting tool for a wide range of scenarios. It can identify which process is listening on a specific TCP/UDP port (`-i :port`), find processes preventing a filesystem from being unmounted (`+D /mount`), or reveal network connections established by a particular program. The output includes the process name, PID, user, file descriptor number, file type, and the file path or network address, providing a comprehensive view of how processes interact with system resources."],["CAVEATS","Without root, **lsof** only sees files opened by your own processes. Output can be very long; combine with **-c**, **-u**, or **-i** to filter. **+D** descends recursively and may be slow on large trees — prefer **+d** for non-recursive listing."],["HISTORY","lsof was created by **Vic Abell** in 1988 and has become a standard Unix diagnostic tool."],["INSTALL","```apt: sudo apt install lsof```\n\n```dnf: sudo dnf install lsof```\n\n```pacman: sudo pacman -S lsof```\n\n```apk: sudo apk add lsof```\n\n```zypper: sudo zypper install lsof```\n\n```brew: brew install lsof```\n\n```nix: nix profile install nixpkgs#lsof```"],["SEE ALSO","[fuser](/man/fuser)(1), [netstat](/man/netstat)(8), [ss](/man/ss)(8), [ps](/man/ps)(1)"]],"e":["fuser","netstat","ss","ps"],"x":"lsof"},"lua":{"n":"lua","t":"programming language interpreter","s":[["TAGLINE","programming language interpreter"],["TLDR","**Start interactive interpreter**\n\n```lua```\n\n**Run script**\n\n```lua [script.lua]```\n\n**Execute code**\n\n```lua -e \"[print('Hello')]\"```\n\n**Run with arguments**\n\n```lua [script.lua] [arg1] [arg2]```\n\n**Show version**\n\n```lua -v```\n\n**Interactive after script**\n\n```lua -i [script.lua]```"],["SYNOPSIS","**lua** [_options_] [_script_] [_args_]"],["PARAMETERS","_SCRIPT_\n> Lua script file.\n\n_ARGS_\n> Script arguments.\n\n**-e** _CODE_\n> Execute string.\n\n**-i**\n> Interactive mode after script.\n\n**-l** _MODULE_\n> Require module.\n\n**-v**\n> Show version information.\n\n**-W**\n> Turn warnings on.\n\n**--help**\n> Display help information."],["DESCRIPTION","**lua** is the interpreter for the Lua programming language. Lua is a lightweight, high-performance, embeddable scripting language designed for extending applications. It features automatic memory management, first-class functions, and a simple but powerful data structuring mechanism (tables).\n\nThe interpreter runs Lua scripts and provides an interactive REPL for testing code. Lua is widely used for game scripting (World of Warcraft, Roblox), embedded systems, configuration (Neovim, Nginx via OpenResty), and as an extension language in applications like Redis and Wireshark."],["CAVEATS","Multiple versions exist (5.1-5.4). Some incompatibilities between versions. Embedding varies."],["HISTORY","Lua was created in 1993 at **PUC-Rio** in Brazil, designed as a lightweight embeddable scripting language."],["INSTALL","```dnf: sudo dnf install lua```\n\n```pacman: sudo pacman -S lua```\n\n```apk: sudo apk add lua5.1```\n\n```brew: brew install lua```\n\n```nix: nix profile install nixpkgs#lua```"],["SEE ALSO","[luajit](/man/luajit)(1), [luac](/man/luac)(1)"]],"e":["luajit","luac"],"x":"lua"},"mcdu":{"n":"mcdu","t":"Fast disk usage analyzer and developer cleanup TUI","s":[["TAGLINE","Fast disk usage analyzer and developer cleanup TUI"],["TLDR","**Scan the current directory**\n\n```mcdu```\n\n**Scan a specific path**\n\n```mcdu [path/to/directory]```"],["SYNOPSIS","**mcdu** [_options_] [_path_]"],["DESCRIPTION","**mcdu** is a fast, modern disk usage analyzer with a TUI. It sorts directories by size with color-coded visualization and vim-style navigation. It searches for build artifacts and caches across **18+ ecosystems** including node_modules, Cargo target, and Docker build artifacts.\n\nOn macOS, it detects orphaned application data in ~/Library. For safe deletion, it supports dry runs, double confirmation, and JSON audit logs."],["CAVEATS","Deletion of build artifacts requires rebuilding those projects. Use dry run mode to preview before deleting."],["HISTORY","**mcdu** was created by **mikalv** and is written in **Rust**."],["INSTALL","```aur: yay -S mcdu```"],["SEE ALSO","[ncdu](/man/ncdu)(1), [du](/man/du)(1), [kondo](/man/kondo)(1)"]],"e":["ncdu","du","kondo"],"x":"mcdu"},"mdpdf":{"n":"mdpdf","t":"Markdown to PDF converter","s":[["TAGLINE","Markdown to PDF converter"],["TLDR","**Convert** a Markdown file to PDF\n\n```mdpdf [input.md] -o [output.pdf]```\n\n**Convert** with a specific paper size\n\n```mdpdf [input.md] -o [output.pdf] --paper [A4]```\n\n**Set PDF metadata** (title and author)\n\n```mdpdf [input.md] -o [output.pdf] --title \"[title]\" --author \"[author]\"```\n\n**Add a header** to each page\n\n```mdpdf [input.md] -o [output.pdf] --header \"[header text]\"```\n\n**Add a footer** to each page\n\n```mdpdf [input.md] -o [output.pdf] --footer \"[footer text]\"```\n\n**Convert multiple** input files into a single PDF\n\n```mdpdf [file1.md] [file2.md] -o [combined.pdf]```"],["SYNOPSIS","**mdpdf** [_options_] [_INPUTS_]..."],["PARAMETERS","**-o, --output** _FILE_\n> Destination path for the PDF output (required)\n\n**-h, --header** _template_\n> Set a header template for each page\n\n**-f, --footer** _template_\n> Set a footer template for each page\n\n**-t, --title** _TEXT_\n> PDF title metadata\n\n**-s, --subject** _TEXT_\n> PDF subject metadata\n\n**-a, --author** _TEXT_\n> PDF author metadata\n\n**-k, --keywords** _TEXT_\n> PDF keywords metadata\n\n**-p, --paper** _[letter|A4]_\n> Paper size (default: letter)\n\n**--version**\n> Display version information\n\n**--help**\n> Show help message"],["DESCRIPTION","**mdpdf** is a command-line application for converting Markdown files to PDF. It supports CommonMark markdown syntax and produces clean, left-aligned PDF output using base-14 PDF fonts.\n\nThe tool converts Markdown headings into PDF bookmarks for easy navigation, transforms file links into PDF attachments, and embeds images with optional captions and width specifications. Multiple input files can be combined into a single output PDF.\n\nmdpdf is designed for simplicity and minimal dependencies, relying on **commonmark** for Markdown parsing, **PyMuPDF** for PDF generation, and **click** for the command-line interface."],["CAVEATS","mdpdf uses a one-size-fits-all styling approach with no custom CSS or template support. Users requiring advanced formatting control, complex layouts, or custom styling should consider alternatives like **Pandoc** with LaTeX, **WeasyPrint**, or **md-to-pdf**."],["HISTORY","mdpdf was created by **Norman Lorrain** and written in **Python**. It is released under the **MIT** license and available via **pip** from PyPI. The project focuses on providing a straightforward, dependency-light Markdown-to-PDF conversion workflow."],["SEE ALSO","[pandoc](/man/pandoc)(1), [wkhtmltopdf](/man/wkhtmltopdf)(1), [groff](/man/groff)(1)"]],"e":["pandoc","wkhtmltopdf","groff"],"x":"mdpdf [input.md] -o [output.pdf]"},"metasploit":{"n":"metasploit","t":"penetration testing platform for developing, testing, and executing exploits","s":[["TAGLINE","penetration testing platform for developing, testing, and executing exploits"],["TLDR","**Start Metasploit** console\n\n```msfconsole```\n\n**Start without** banner\n\n```msfconsole -q```\n\n**Execute** a resource script at startup\n\n```msfconsole -r [script.rc]```\n\n**Initialize** the database\n\n```msfdb init```\n\n**Run a specific module** with inline commands\n\n```msfconsole -x \"use [exploit/multi/handler]; set RHOSTS [target]; run\"```\n\n**Generate a payload** with msfvenom\n\n```msfvenom -p [windows/meterpreter/reverse_tcp] LHOST=[ip] LPORT=[port] -f [exe] -o [payload.exe]```\n\n**List available payloads**\n\n```msfvenom --list payloads```\n\n**Search for modules** by keyword\n\n```msfconsole -q -x \"search [type:exploit] [platform:windows]\"```"],["SYNOPSIS","**msfconsole** [_options_]"],["PARAMETERS","**-q**, **--quiet**\n> Don't print banner.\n\n**-r** _file_\n> Resource script file.\n\n**-x** _cmd_\n> Execute command.\n\n**-o** _file_\n> Output to file.\n\n**-L**\n> List all available modules, payloads, encoders, or nops.\n\n**-n** _nopsled_\n> Prepend a nopsled of given size to the payload.\n\n**-e** _encoder_\n> Encoder to use.\n\n**-p** _payload_\n> Payload to use (msfvenom).\n\n**-f** _format_\n> Output format (msfvenom): exe, elf, raw, python, c, etc."],["DESCRIPTION","**Metasploit Framework** is a penetration testing platform for developing, testing, and executing exploits. It includes a large database of public exploits and payloads for security testing.\n\nThe framework includes msfconsole (interactive shell), msfvenom (payload generator), and auxiliary tools."],["COMMON COMMANDS","```\nsearch <term>     - Search modules\nuse <module>      - Select module\ninfo              - Module details\nshow options      - Display module options\nset <opt> <val>   - Set option\nrun / exploit     - Execute module\nsessions          - List active sessions\nback              - Exit current module\n```"],["CAVEATS","Requires authorization for use. Database setup recommended. Regular updates needed. Antivirus may flag payloads."],["HISTORY","Metasploit was created by **H.D. Moore** in **2003** as a portable network tool. It was acquired by **Rapid7** in **2009** and remains the most popular penetration testing framework."],["INSTALL","```pacman: sudo pacman -S metasploit```\n\n```nix: nix profile install nixpkgs#metasploit```"],["SEE ALSO","[nmap](/man/nmap)(1), [nikto](/man/nikto)(1), [sqlmap](/man/sqlmap)(1), [hydra](/man/hydra)(1), [wireshark](/man/wireshark)(1)"]],"e":["nmap","nikto","sqlmap","hydra","wireshark"],"x":"msfconsole"},"mkhomedir_helper":{"n":"mkhomedir_helper","t":"helper program for the **pam_mkhomedir** PAM module","s":[["TAGLINE","helper program for the **pam_mkhomedir** PAM module"],["TLDR","Create a **home directory** for a user based on /etc/skel with umask 022\n\n```sudo mkhomedir_helper [username]```\n\nCreate a home directory with **custom umask** (037 = owner all, group read)\n\n```sudo mkhomedir_helper [username] [037]```\n\nCreate a home directory based on a **custom skeleton** directory\n\n```sudo mkhomedir_helper [username] [umask] [path/to/skeleton_directory]```\n\nCreate a home directory with **specific permissions**\n\n```sudo mkhomedir_helper [username] [umask] [/etc/skel] [0700]```"],["SYNOPSIS","**mkhomedir_helper** _user_ [_umask_ [_path-to-skel_ [_home_mode_ [_path-to-vendor-skel_]]]]"],["PARAMETERS","**user**\n> Username for which to create the home directory\n\n**umask**\n> File creation mask for contents; default is 0022\n\n**path-to-skel**\n> Skeleton directory to copy contents from; default is /etc/skel\n\n**home_mode**\n> Permissions for the home directory itself; computed from umask if not specified\n\n**path-to-vendor-skel**\n> Secondary skeleton directory; when set, home directory is populated from path-to-skel first, then from this directory"],["DESCRIPTION","**mkhomedir_helper** is a helper program for the **pam_mkhomedir** PAM module. It creates home directories and populates them with the contents of the specified skeleton directory.\n\nThe tool copies files from the skeleton directory (typically /etc/skel) to the new home directory, applying the specified umask to file permissions. It sets ownership to the target user and creates standard configuration files like .bashrc and .profile.\n\nThe helper is architecturally separated from the PAM module for security reasons, allowing SELinux domain transitions during execution and preventing direct access from login domains to home directory contents."],["CAVEATS","The tool never modifies existing home directories, preventing accidental overwrites. It requires root privileges to execute. The skeleton directory must exist and be readable. Users must already exist in the system before running this command."],["HISTORY","**mkhomedir_helper** is part of the **Linux-PAM** (Pluggable Authentication Modules) project. It was designed as a security-conscious helper binary for the pam_mkhomedir module, enabling automatic home directory creation during user login for centrally-managed authentication systems like LDAP."],["INSTALL","```apt: sudo apt install libpam-modules-bin```\n\n```apk: sudo apk add linux-pam```\n\n```brew: brew install linux-pam```\n\n```nix: nix profile install nixpkgs#linux-pam```"],["SEE ALSO","[useradd](/man/useradd)(1), [adduser](/man/adduser)(1)"]],"e":["useradd","adduser"],"x":"sudo mkhomedir_helper [username]"},"mklost+found":{"n":"mklost+found","t":"creates a lost+found directory in the current working directory","s":[["TAGLINE","creates a lost+found directory in the current working directory"],["TLDR","Create a preallocated **lost+found** directory (for fsck)\n\n```mklost+found```"],["SYNOPSIS","**mklost+found**"],["DESCRIPTION","**mklost+found** creates a lost+found directory in the current working directory on ext2, ext3, and ext4 filesystems. The tool pre-allocates disk blocks for the directory to support filesystem recovery operations.\n\nWhen e2fsck performs filesystem recovery, it stores recovered file fragments in the lost+found directory. Pre-allocating blocks prevents the recovery tool from needing to allocate space during the recovery process, which could fail if the filesystem is severely damaged."],["PARAMETERS","This command takes no options or arguments. It creates the directory in the current working directory."],["CAVEATS","The lost+found directory typically already exists at the root of each ext filesystem created by mke2fs. This utility creates additional instances in other locations when needed. Must be run on an ext2/ext3/ext4 filesystem. Requires appropriate permissions to create directories."],["HISTORY","**mklost+found** was created by **Remy Card** and is currently maintained by **Theodore Ts'o**. It is part of the **e2fsprogs** package, the standard utilities for managing ext filesystems on Linux."],["INSTALL","```apt: sudo apt install e2fsprogs```\n\n```dnf: sudo dnf install e2fsprogs```\n\n```pacman: sudo pacman -S e2fsprogs```\n\n```apk: sudo apk add e2fsprogs-extra```\n\n```zypper: sudo zypper install e2fsprogs```\n\n```brew: brew install e2fsprogs```\n\n```nix: nix profile install nixpkgs#e2fsprogs```"],["SEE ALSO","[e2fsck](/man/e2fsck)(8), [mke2fs](/man/mke2fs)(8), [fsck](/man/fsck)(8)"]],"e":["e2fsck","mke2fs","fsck"],"x":"mklost+found"},"mkpasswd":{"n":"mkpasswd","t":"generates password hashes suitable for /etc/shadow or similar uses","s":[["TAGLINE","generates password hashes suitable for /etc/shadow or similar uses"],["TLDR","**Generate password hash**\n\n```mkpasswd [password]```\n\n**Generate with specific method**\n\n```mkpasswd -m sha-512 [password]```\n\n**Generate with salt**\n\n```mkpasswd -S [salt] [password]```\n\n**Generate random password**\n\n```mkpasswd -l [16]```\n\n**List available methods**\n\n```mkpasswd -m help```"],["SYNOPSIS","**mkpasswd** [_options_] [_password_]"],["DESCRIPTION","**mkpasswd** generates password hashes suitable for /etc/shadow or similar uses. It can use various hashing algorithms and optionally generate random passwords.\n\nThe tool is useful for creating encrypted passwords for system configuration files and automated provisioning."],["PARAMETERS","**-m** _method_\n> Hash method (sha-512, sha-256, md5, des).\n\n**-S** _salt_\n> Specify salt value.\n\n**-R** _rounds_\n> SHA rounds count.\n\n**-l** _length_\n> Generate random password of length.\n\n**-s**, **--stdin**\n> Read password from stdin.\n\n**-5**\n> Use MD5 (shortcut).\n\n**-P** _fd_\n> Read password from file descriptor."],["CAVEATS","Different versions have different options. whois package version differs from expect package. Strong methods recommended (sha-512). Avoid storing plaintext passwords."],["HISTORY","**mkpasswd** exists in multiple implementations. The whois package version is common on Debian/Ubuntu, while others may have the expect package version. Both provide password hashing but with different options."],["INSTALL","```apt: sudo apt install whois```\n\n```dnf: sudo dnf install whois```\n\n```pacman: sudo pacman -S whois```\n\n```apk: sudo apk add expect```\n\n```zypper: sudo zypper install whois```\n\n```brew: brew install whois```\n\n```nix: nix profile install nixpkgs#whois```"],["SEE ALSO","[passwd](/man/passwd)(1), [openssl](/man/openssl)(1), [chpasswd](/man/chpasswd)(1)"]],"e":["passwd","openssl","chpasswd"],"x":"mkpasswd [password]"},"muffin-window-demo":{"n":"muffin-window-demo","t":"demonstrates Muffin window types","s":[["TAGLINE","demonstrates Muffin window types"],["TLDR","**Run window demo**\n\n```muffin-window-demo```\n\n**Demo specific window type**\n\n```muffin-window-demo --type=[dialog]```"],["SYNOPSIS","**muffin-window-demo** [_options_]"],["PARAMETERS","**--type** _TYPE_\n> Window type to demonstrate.\n\n**--help**\n> Display help information."],["DESCRIPTION","**muffin-window-demo** demonstrates Muffin window types. It shows different window decorations.\n\nThe tool displays sample windows. Used for testing and theme development."],["CAVEATS","Development tool. Cinnamon/Muffin specific. For testing window decorations."],["HISTORY","muffin-window-demo is part of **Muffin** development tools for testing window manager behavior."],["SEE ALSO","[muffin](/man/muffin)(1), [muffin-theme-viewer](/man/muffin-theme-viewer)(1), [cinnamon](/man/cinnamon)(1)"]],"e":["muffin","muffin-theme-viewer","cinnamon"],"x":"muffin-window-demo"},"nix3-repl":{"n":"nix3-repl","t":"starts interactive Nix shell","s":[["TAGLINE","starts interactive Nix shell"],["TLDR","**Start REPL**\n\n```nix3 repl```\n\n**Load nixpkgs**\n\n```nix3 repl '<nixpkgs>'```\n\n**Load flake**\n\n```nix3 repl [.#]```"],["SYNOPSIS","**nix3** **repl** [_options_] [_files_]"],["PARAMETERS","_FILES_\n> Files to load.\n\n**--help**\n> Display help information."],["DESCRIPTION","**nix3 repl** starts interactive Nix shell. Alternative name for nix repl.\n\nThe tool evaluates expressions. Tab completion and history available."],["CAVEATS","Alias for nix repl. :? for help. Expression evaluation."],["HISTORY","nix3 repl provides **interactive evaluation** for Nix expressions and exploration."],["SEE ALSO","[nix](/man/nix)(1), [nix-repl](/man/nix-repl)(1), [nix3-eval](/man/nix3-eval)(1)"]],"e":["nix","nix-repl","nix3-eval"],"x":"nix3 repl"},"npm-rb":{"n":"npm-rb","t":"Rebuild packages with native addons","s":[["TAGLINE","Rebuild packages with native addons"],["TLDR","**Rebuild all packages**\n\n```npm rb```\n\n**Rebuild specific package**\n\n```npm rb [package-name]```\n\n**Rebuild global packages**\n\n```npm rb -g```\n\n**Rebuild multiple specific packages**\n\n```npm rb [package1] [package2]```"],["SYNOPSIS","**npm** **rb** [_packages_]"],["PARAMETERS","_PACKAGES_\n> Packages to rebuild.\n\n**-g**\n> Rebuild global packages.\n\n**--help**\n> Display help information."],["DESCRIPTION","**npm rb** is an alias for **npm rebuild**. It recompiles native addon modules (C/C++ bindings) using node-gyp.\n\nCommonly needed after upgrading Node.js versions, as native addons are compiled against a specific Node.js ABI. Also useful after manually modifying a package's source code in node_modules."],["CAVEATS","Alias for npm rebuild. Requires build tools (make, gcc/g++ or equivalent) to be installed. Only affects packages with native addons; pure JavaScript packages are unaffected."],["INSTALL","```pacman: sudo pacman -S npm```\n\n```apk: sudo apk add npm```\n\n```brew: brew install npm```"],["SEE ALSO","[npm](/man/npm)(1), [npm-rebuild](/man/npm-rebuild)(1), [npm-install](/man/npm-install)(1), [node-gyp](/man/node-gyp)(1)"]],"e":["npm","npm-rebuild","npm-install","node-gyp"],"x":"npm rb"},"npm":{"n":"npm","t":"node Package Manager","s":[["TAGLINE","node Package Manager"],["TLDR","**Install dependencies**\n\n```npm install```\n\n**Install package**\n\n```npm install [package]```\n\n**Install globally**\n\n```npm install -g [package]```\n\n**Install dev dependency**\n\n```npm install -D [package]```\n\n**Run script**\n\n```npm run [script-name]```\n\n**Start application**\n\n```npm start```\n\n**Run tests**\n\n```npm test```\n\n**Update packages**\n\n```npm update```\n\n**Initialize project**\n\n```npm init```\n\n**Clean install from lockfile**\n\n```npm ci```\n\n**List installed packages**\n\n```npm ls```\n\n**Check for outdated packages**\n\n```npm outdated```\n\n**Run security audit**\n\n```npm audit```\n\n**Uninstall a package**\n\n```npm uninstall [package]```\n\n**Publish a package**\n\n```npm publish```"],["SYNOPSIS","**npm** [_command_] [_options_]"],["PARAMETERS","_COMMAND_\n> npm command.\n\n**install**\n> Install packages.\n\n**uninstall**\n> Remove packages.\n\n**run** _SCRIPT_\n> Run package script.\n\n**update**\n> Update packages.\n\n**init**\n> Create package.json.\n\n**ci**\n> Clean install from package-lock.json (for CI/CD).\n\n**audit**\n> Run security vulnerability audit.\n\n**ls**\n> List installed packages.\n\n**outdated**\n> Check for outdated packages.\n\n**publish**\n> Publish package to registry.\n\n**-g**\n> Global installation.\n\n**-D**, **--save-dev**\n> Save as development dependency.\n\n**--help**\n> Display help information."],["DESCRIPTION","**npm** is the Node Package Manager and the default package manager for Node.js. It manages JavaScript packages and dependencies using package.json and package-lock.json files.\n\nnpm provides access to the world's largest software registry with over two million packages."],["CAVEATS","Requires Node.js. package-lock.json for determinism. Network dependent."],["HISTORY","npm was created by **Isaac Schlueter** in 2010 and is the default package manager for Node.js."],["INSTALL","```pacman: sudo pacman -S npm```\n\n```apk: sudo apk add npm```\n\n```brew: brew install npm```"],["SEE ALSO","[node](/man/node)(1), [npx](/man/npx)(1), [yarn](/man/yarn)(1), [pnpm](/man/pnpm)(1)"]],"e":["node","npx","yarn","pnpm"],"x":"npm install"},"oma":{"n":"oma","t":"modern package management frontend for dpkg-based Linux distributions","s":[["TAGLINE","modern package management frontend for dpkg-based Linux distributions"],["TLDR","Enter the **interactive** package management interface\n\n```sudo oma```\n\n**Install** a package\n\n```sudo oma install [package_name]```\n\n**Remove** a package\n\n```sudo oma remove [package_name]```\n\n**Search** for a package\n\n```oma search [keyword]```\n\nShow **detailed information** for a package\n\n```oma show [package_name]```\n\n**Upgrade** all installed packages to their latest versions\n\n```sudo oma upgrade```\n\n**Update** the list of available packages\n\n```sudo oma refresh```\n\nList **files** in a package\n\n```oma files [package_name]```\n\n**Fix broken** dependencies\n\n```sudo oma fix-broken```\n\nDisplay **help**\n\n```oma help```"],["SYNOPSIS","**oma** [_command_] [_options_] [_package_...]"],["PARAMETERS","**install** _package_\n> Install one or more packages\n\n**remove** _package_\n> Remove one or more packages\n\n**upgrade**\n> Upgrade all installed packages to newest versions\n\n**refresh**\n> Update the package list from repositories\n\n**search** _keyword_\n> Search for packages matching keyword\n\n**show** _package_\n> Display detailed information about a package\n\n**list**\n> List installed packages\n\n**depends** _package_\n> Show package dependencies\n\n**rdepends** _package_\n> Show reverse dependencies\n\n**files** _package_\n> List files in the specified package\n\n**provides** _pattern_\n> Find packages providing files matching the pattern\n\n**fix-broken**\n> Fix broken dependencies\n\n**pick** _package_\n> Install a specific version of a package\n\n**mark** _package_\n> Mark package status (manual, auto, hold, unhold)\n\n**download** _package_\n> Download package(s) without installing\n\n**history**\n> Show package operation history\n\n**clean**\n> Clean downloaded package cache\n\n**help**\n> Display help information"],["DESCRIPTION","**oma** is a modern package management frontend for dpkg-based Linux distributions, developed by the AOSC (Anthon Open Source Community) project. It provides an interactive text-based interface for managing packages, with support for dependency resolution and system upgrades. Pre-installed on AOSC OS, it is also available for Debian, Ubuntu, Deepin, openKylin, and other dpkg-based distributions.\n\nThe tool features multi-threaded package downloads, smart search, and safeguards against accidental removal of essential packages. It aims to provide a user-friendly experience while maintaining compatibility with the underlying dpkg/apt infrastructure."],["CAVEATS","Requires root privileges (via sudo) for operations that modify the system. Designed primarily for AOSC OS but may work on other dpkg-based distributions with appropriate configuration."],["HISTORY","Developed by the **AOSC (Anthon Open Source Community)** as part of their effort to create a streamlined Linux distribution experience. The name \"oma\" is derived from various meanings across languages, reflecting AOSC's international community."],["SEE ALSO","[apt](/man/apt)(8), [apt-get](/man/apt-get)(8), [dpkg](/man/dpkg)(1), [aptitude](/man/aptitude)(8)"]],"e":["apt","apt-get","dpkg","aptitude"],"x":"sudo oma"},"op":{"n":"op","t":"1Password CLI (command-line password manager and secrets)","s":[["TAGLINE","1Password CLI (command-line password manager and secrets)"],["TLDR","**Sign in** to a 1Password account\n\n```op signin```\n\n**List all items** in a vault\n\n```op item list --vault [vault_name]```\n\n**Get details** of a specific item\n\n```op item get \"[item_name]\"```\n\n**Get specific fields** from an item\n\n```op item get \"[item_name]\" --fields label=username,label=password```\n\n**Read a secret** using a secret reference\n\n```op read \"op://[vault]/[item]/[field]\"```\n\n**Run a command** with secrets injected as environment variables\n\n```op run --env-file [.env] -- [command]```\n\n**Create a new item** in a vault\n\n```op item create --category login --title \"[title]\" --vault [vault_name]```\n\n**List all vaults** in the account\n\n```op vault list```"],["SYNOPSIS","**op** [_command_] [_subcommand_] [_flags_]"],["PARAMETERS","**account**\n> Manage locally configured 1Password accounts\n\n**item**\n> Perform CRUD operations on vault items (get, list, create, edit, delete)\n\n**vault**\n> Manage vaults and their permissions\n\n**document**\n> Manage document-type items (get, list, create, edit, delete)\n\n**read**\n> Resolve and retrieve secret references\n\n**run**\n> Pass secrets as environment variables to a subprocess\n\n**inject**\n> Insert secrets into configuration files or templates\n\n**signin**\n> Authenticate to a 1Password account\n\n**signout**\n> End the current session\n\n**whoami**\n> Display information about the signed-in account\n\n**completion**\n> Generate shell completion scripts\n\n**--format** _json_\n> Output results in JSON format for parsing with tools like jq\n\n**--fields** _label=field_\n> Specify particular fields to retrieve from an item\n\n**--vault** _name_\n> Target a specific vault for the operation\n\n**--cache=false**\n> Disable daemon caching (useful in restricted environments)"],["DESCRIPTION","**op** is the official command-line interface for 1Password (packaged as **1password-cli**). It enables secure access to passwords, credentials, and sensitive data directly from the terminal.\n\nThe CLI follows a noun-verb command structure where the first argument is typically a resource type (item, vault, user) followed by an action (get, list, create). It supports both interactive and scripted workflows, making it suitable for development automation and CI/CD pipelines.\n\nKey capabilities include retrieving credentials using secret references in the format **op://vault/item/field**, injecting secrets into environment variables for running processes, and managing vault contents programmatically. The tool uses daemon-based caching on Unix systems to improve performance and reduce API calls.\n\nItems can be referenced by name or by their 26-character alphanumeric ID, with IDs being more stable and efficient for automation. Output can be formatted as JSON for integration with tools like **jq**."],["CAVEATS","Requires a 1Password account and must be authenticated before most operations. The daemon caching feature stores encrypted data in memory, which may not be suitable for all security environments—use **--cache=false** to disable. Service accounts have different authentication flows than personal accounts. This page documents the **CLI** only (binary **op** / package **1password-cli**), not the 1Password desktop GUI."],["HISTORY","1Password was founded by **AgileBits** in **2006** as a password manager for macOS. The command-line interface was first released in **2017** to support developer workflows and automation. Version 2 of the CLI, released in **2022**, introduced the current noun-verb command structure, secret references, and improved service account support for CI/CD integration."],["INSTALL","```aur: yay -S 1password-cli```\n\n```nix: nix profile install nixpkgs#_1password-cli```\n\n```brew: brew install --cask 1password-cli```"],["SEE ALSO","[pass](/man/pass)(1), [gopass](/man/gopass)(1), [bitwarden-cli](/man/bitwarden-cli)(1), [bw](/man/bw)(1), [vault](/man/vault)(1), [gpg](/man/gpg)(1)"],["RESOURCES","```[Homepage](https://1password.com/)```\n\n```[Documentation](https://developer.1password.com/docs/cli/)```"]],"e":["pass","gopass","bitwarden-cli","bw","vault","gpg"],"x":"op signin"},"podman-image":{"n":"podman-image","t":"Manage container images in Podman","s":[["TAGLINE","Manage container images in Podman"],["TLDR","**List images**\n\n```podman image list```\n\n**Remove image**\n\n```podman image rm [image]```\n\n**Pull image**\n\n```podman image pull [image:tag]```\n\n**Inspect image**\n\n```podman image inspect [image]```\n\n**Build image**\n\n```podman image build -t [name] [path]```\n\n**Prune unused images**\n\n```podman image prune```"],["SYNOPSIS","**podman** **image** _command_ [_options_]"],["PARAMETERS","**list**\n> List images.\n\n**rm** _image_\n> Remove image.\n\n**pull** _image_\n> Pull image from registry.\n\n**push** _image_\n> Push image to registry.\n\n**build**\n> Build image from Dockerfile.\n\n**inspect** _image_\n> Show image details.\n\n**prune**\n> Remove unused images.\n\n**tag** _src_ _dest_\n> Tag an image."],["DESCRIPTION","**podman image** manages container images. Subcommand for Podman's image operations. Supports pulling, building, tagging, and removing images. Compatible with OCI and Docker image formats."],["INSTALL","```apt: sudo apt install podman```\n\n```dnf: sudo dnf install podman```\n\n```pacman: sudo pacman -S podman```\n\n```apk: sudo apk add podman```\n\n```zypper: sudo zypper install podman```\n\n```brew: brew install podman```\n\n```nix: nix profile install nixpkgs#podman```"],["SEE ALSO","[podman](/man/podman)(1), [podman-pull](/man/podman-pull)(1)"]],"e":["podman","podman-pull"],"x":"podman image list"},"poetry-install":{"n":"poetry-install","t":"Install project dependencies from lock file","s":[["TAGLINE","Install project dependencies from lock file"],["TLDR","**Install all dependencies**\n\n```poetry install```\n\n**Install without dev dependencies**\n\n```poetry install --without dev```\n\n**Install only specific groups**\n\n```poetry install --only main,test```\n\n**Install with extras**\n\n```poetry install --extras \"[extra1 extra2]\"```\n\n**Sync environment exactly**\n\n```poetry install --sync```"],["SYNOPSIS","**poetry install** [_options_]"],["PARAMETERS","**--without** _groups_\n> Exclude dependency groups.\n\n**--only** _groups_\n> Install only these groups.\n\n**--extras**, **-E** _extras_\n> Install extras.\n\n**--sync**\n> Sync environment (remove unlisted).\n\n**--no-root**\n> Don't install project itself.\n\n**--dry-run**\n> Show what would be installed.\n\n**-v**, **-vv**, **-vvv**\n> Verbosity levels."],["DESCRIPTION","**poetry install** reads pyproject.toml and poetry.lock to install project dependencies. It creates a virtual environment if needed and ensures reproducible installations."],["EXAMPLES","```bash"],["Install everything","poetry install"],["Production only","poetry install --without dev,test"],["Only dev dependencies","poetry install --only dev"],["Don't install the project itself","poetry install --no-root"],["Synchronize (remove extras)","poetry install --sync"],["Dry run","poetry install --dry-run\n```"],["LOCK FILE","- poetry.lock ensures reproducible installs\n- Generated on first install\n- Updated by poetry lock or poetry update"],["CAVEATS","Requires pyproject.toml. Creates virtual environment by default. Use --sync to match lock exactly."],["HISTORY","poetry install is part of **Poetry** by **Sébastien Eustace**, providing deterministic Python dependency installation."],["INSTALL","```dnf: sudo dnf install poetry```\n\n```apk: sudo apk add poetry```\n\n```brew: brew install poetry```\n\n```nix: nix profile install nixpkgs#poetry```"],["SEE ALSO","[poetry-add](/man/poetry-add)(1), [poetry-update](/man/poetry-update)(1), [poetry-lock](/man/poetry-lock)(1)"]],"e":["poetry-add","poetry-update","poetry-lock"],"x":"poetry install"},"polybar-msg":{"n":"polybar-msg","t":"Send IPC messages to running Polybar","s":[["TAGLINE","Send IPC messages to running Polybar"],["TLDR","**Send action to module**\n\n```polybar-msg action [module] [action]```\n\n**Toggle module visibility**\n\n```polybar-msg action [module] toggle```\n\n**Send hook to IPC module**\n\n```polybar-msg hook [module] [hook_index]```\n\n**Quit polybar**\n\n```polybar-msg cmd quit```"],["SYNOPSIS","**polybar-msg** [_options_] _command_ [_args_...]"],["PARAMETERS","**action** _module_ _action_\n> Send action to module.\n\n**hook** _module_ _index_\n> Trigger IPC hook.\n\n**cmd** _command_\n> Send command (quit, restart, hide, show, toggle).\n\n**-p** _pid_\n> Target specific polybar PID."],["DESCRIPTION","**polybar-msg** sends IPC messages to running polybar instances. Controls modules, triggers hooks, and manages polybar lifecycle. Used for dynamic bar updates and scripted interactions."],["INSTALL","```apt: sudo apt install polybar```\n\n```dnf: sudo dnf install polybar```\n\n```pacman: sudo pacman -S polybar```\n\n```apk: sudo apk add polybar```\n\n```zypper: sudo zypper install polybar```\n\n```nix: nix profile install nixpkgs#polybar```"],["SEE ALSO","[polybar](/man/polybar)(1)"]],"e":["polybar"],"x":"polybar-msg action [module] [action]"},"ppmtojpeg":{"n":"ppmtojpeg","t":"Convert PPM images to JPEG format","s":[["TAGLINE","Convert PPM images to JPEG format"],["TLDR","**Convert PPM to JPEG**\n\n```ppmtojpeg [input.ppm] > [output.jpg]```\n\n**Convert with specific quality** (default: 75)\n\n```ppmtojpeg -quality [90] [input.ppm] > [output.jpg]```\n\n**Create a progressive JPEG**\n\n```ppmtojpeg -progressive [input.ppm] > [output.jpg]```\n\n**Convert with optimized Huffman encoding**\n\n```ppmtojpeg -optimize [input.ppm] > [output.jpg]```\n\n**Convert with grayscale output**\n\n```ppmtojpeg -grayscale [input.ppm] > [output.jpg]```"],["SYNOPSIS","**ppmtojpeg** [_options_] [_file_]"],["PARAMETERS","**-quality** _n_\n> JPEG quality factor (0-100, default: 75). Higher values produce better quality but larger files.\n\n**-progressive**\n> Create a progressive JPEG (loads in multiple passes).\n\n**-optimize**\n> Optimize Huffman coding tables for smaller file size at the cost of slower compression.\n\n**-grayscale**\n> Create a grayscale JPEG regardless of input color.\n\n**-density** _DPI_\n> Set pixel density in dots per inch.\n\n**-smooth** _n_\n> Smooth input image (1-100) to reduce dithering artifacts."],["DESCRIPTION","**ppmtojpeg** converts PPM (Portable Pixel Map) images to JPEG/JFIF format. It is an alias for **pnmtojpeg** and is part of the Netpbm toolkit. The command reads from a file or stdin and writes JPEG data to stdout.\n\nThe default quality of 75 provides a good balance between file size and visual quality. Values above 95 produce diminishing returns in quality while significantly increasing file size."],["CAVEATS","Output is written to stdout and must be redirected to a file. JPEG is a lossy format; converting from PPM to JPEG and back will lose quality. The **-optimize** flag increases encoding time but typically reduces file size by 5-10%."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pnmtojpeg](/man/pnmtojpeg)(1), [jpegtopnm](/man/jpegtopnm)(1), [cjpeg](/man/cjpeg)(1), [netpbm](/man/netpbm)(1)"]],"e":["pnmtojpeg","jpegtopnm","cjpeg","netpbm"],"x":"ppmtojpeg [input.ppm] > [output.jpg]"},"processing":{"n":"processing","t":"Visual arts programming language and IDE","s":[["TAGLINE","Visual arts programming language and IDE"],["TLDR","**Launch Processing IDE**\n\n```processing```\n\n**Run sketch from command line**\n\n```processing-java --sketch=[path/to/sketch] --run```\n\n**Export as application**\n\n```processing-java --sketch=[path] --export```\n\n**Build without running**\n\n```processing-java --sketch=[path] --build```"],["SYNOPSIS","**processing** [_options_]\n\n**processing-java** [_options_] **--sketch=**_path_"],["PARAMETERS","**--run**\n> Run the sketch.\n\n**--build**\n> Build without running.\n\n**--export**\n> Export as application.\n\n**--present**\n> Run in presentation mode.\n\n**--platform** _os_\n> Target platform.\n\n**--output** _dir_\n> Output directory."],["DESCRIPTION","**Processing** is a visual programming language and IDE for learning to code in the context of visual arts. It simplifies Java for creative coding."],["EXAMPLES","```bash"],["Launch IDE","processing"],["Run sketch","processing-java --sketch=/path/to/sketch --run"],["Export for all platforms","processing-java --sketch=/path/to/sketch --export"],["Build only","processing-java --sketch=/path/to/sketch --build\n```"],["SKETCH EXAMPLE","```java\nvoid setup() {\n  size(400, 400);\n}\n\nvoid draw() {\n  background(255);\n  ellipse(mouseX, mouseY, 50, 50);\n}\n```"],["CAVEATS","Requires Java. IDE-focused workflow. Sketches are in ~/sketchbook by default."],["HISTORY","Processing was created by **Casey Reas** and **Ben Fry** at MIT Media Lab in 2001 for teaching programming in visual context."],["INSTALL","```nix: nix profile install nixpkgs#processing```"],["SEE ALSO","[p5](/man/p5)(1), [java](/man/java)(1)"]],"e":["p5","java"],"x":"processing"},"pvesm-add":{"n":"pvesm-add","t":"Add a storage backend definition in Proxmox VE","s":[["TAGLINE","Add a storage backend definition in Proxmox VE"],["TLDR","Add a **directory** storage\n\n```pvesm add dir [storage_name] --path [path/to/directory]```\n\nAdd an **LVM** storage\n\n```pvesm add lvm [storage_name] --vgname [volume_group_name]```\n\nAdd an **LVM-Thin** storage\n\n```pvesm add lvmthin [storage_name] --vgname [volume_group_name] --thinpool [logical_volume_name]```"],["SYNOPSIS","**pvesm add** _type_ _storage_ [_OPTIONS_]"],["PARAMETERS","**type**\n> Storage type (e.g. `dir`, `lvm`, `lvmthin`, `nfs`, `cifs`, `zfspool`, `rbd`, `iscsi`)\n\n**storage**\n> Unique storage identifier (cluster-wide name)\n\n**--path** _path_\n> Directory path (for `dir` type)\n\n**--vgname** _name_\n> Volume group name (for LVM types)\n\n**--thinpool** _name_\n> Thin pool logical volume name (for `lvmthin`)\n\n**--content** _types_\n> Allowed content types (e.g. `images`, `iso`, `backup`, `vztmpl`, `rootdir`)\n\n**--nodes** _list_\n> Restrict storage to listed cluster nodes"],["DESCRIPTION","**pvesm add** registers a new storage definition in the Proxmox VE cluster. Storage backends hold VM disks, container volumes, ISOs, templates, and backups. Definitions are stored cluster-wide and become available to `qm` and `pct` once reachable from a node.\n\nSupported types include local directories, NFS, CIFS, LVM, LVM-thin, ZFS pools, Ceph RBD, and iSCSI, among others. Options vary by type; see `man pvesm` for the full matrix."],["CAVEATS","Adding a definition does not create the underlying storage if it does not exist (e.g. LVM volume groups must already exist). Paths and network shares must be reachable from every node where the storage is enabled. Duplicate storage IDs are rejected."],["HISTORY","Part of **Proxmox VE** storage management (`pvesm`)."],["SEE ALSO","[pvesm](/man/pvesm)(1), [qm](/man/qm)(1), [pct](/man/pct)(1)"],["RESOURCES","```[Homepage](https://www.proxmox.com)```\n\n```[Documentation](https://pve.proxmox.com/pve-docs/pvesm.1.html)```"]],"e":["pvesm","qm","pct"],"x":"pvesm add dir [storage_name] --path [path/to/directory]"},"qrttoppm":{"n":"qrttoppm","t":"Convert QRT ray tracer output to PPM","s":[["TAGLINE","Convert QRT ray tracer output to PPM"],["TLDR","**Convert QRT ray tracer output to PPM**\n\n```qrttoppm [input.qrt] > [output.ppm]```"],["SYNOPSIS","**qrttoppm** [_file_]"],["DESCRIPTION","**qrttoppm** converts images from the QRT ray tracer output format to PPM (Portable Pixmap) format. QRT was an early ray tracing program, and this tool is part of the Netpbm image processing toolkit, enabling legacy QRT renders to be converted into a widely supported format for viewing or further processing."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[mtvtoppm](/man/mtvtoppm)(1), [rawtoppm](/man/rawtoppm)(1), [ppmtopgm](/man/ppmtopgm)(1)"]],"e":["mtvtoppm","rawtoppm","ppmtopgm"],"x":"qrttoppm [input.qrt] > [output.ppm]"},"qsub":{"n":"qsub","t":"Submit jobs to batch queue systems","s":[["TAGLINE","Submit jobs to batch queue systems"],["TLDR","**Submit** script with defaults\n\n```qsub [script.sh]```\n\nSet **walltime** limit\n\n```qsub -l walltime=[1]:[30]:[00] [script.sh]```\n\nRequest **nodes and cores**\n\n```qsub -l nodes=[2]:ppn=[4] [script.sh]```\n\nSubmit to specific **queue**\n\n```qsub -q [queue_name] [script.sh]```"],["SYNOPSIS","**qsub** [**-l** _resources_] [**-q** _queue_] [**-N** _name_] [**-o** _output_] _script_"],["PARAMETERS","**-l _resource_=_value_**\n> Request resource (walltime, nodes, mem, etc.)\n\n**-q _queue_**\n> Submit to specific queue\n\n**-N _name_**\n> Job name\n\n**-o _path_**\n> Standard output file path\n\n**-e _path_**\n> Standard error file path\n\n**-j oe**\n> Join stdout and stderr\n\n**-M _email_**\n> Email address for notifications\n\n**-m _events_**\n> Email on events (b=begin, e=end, a=abort)\n\n**-V**\n> Export all environment variables to the job\n\n**-I**\n> Submit as interactive job\n\n**-S _path_**\n> Shell to use for the job script\n\n**-a _date_time_**\n> Defer execution until specified date/time\n\n**-v _variable_list_**\n> Export specific environment variables to the job"],["DESCRIPTION","**qsub** submits batch jobs to TORQUE or PBS (Portable Batch System) queue managers. It's used in high-performance computing clusters to schedule jobs for execution when resources become available.\n\nJobs are scripts that specify resource requirements and commands to execute. The queue manager allocates nodes, CPUs, and memory according to the request."],["CAVEATS","Resource limits vary by queue and cluster configuration. Jobs may wait in queue for available resources. Exceeding walltime kills the job. Environment variables may not be inherited."],["HISTORY","**qsub** originated with PBS (Portable Batch System) developed at NASA. TORQUE is an open-source derivative. Similar commands exist in Slurm (sbatch) and other job schedulers."],["INSTALL","```apt: sudo apt install gridengine-client```"],["SEE ALSO","[qstat](/man/qstat)(1), [qdel](/man/qdel)(1), [pbsnodes](/man/pbsnodes)(1), [sbatch](/man/sbatch)(1)"]],"e":["qstat","qdel","pbsnodes","sbatch"],"x":"qsub [script.sh]"},"rage":{"n":"rage","t":"Simple modern file encryption tool","s":[["TAGLINE","Simple modern file encryption tool"],["TLDR","**Encrypt file with passphrase**\n\n```rage -p -o [encrypted.age] [file.txt]```\n\n**Encrypt with recipient**\n\n```rage -r [age1...] -o [encrypted.age] [file.txt]```\n\n**Decrypt file**\n\n```rage -d -i [key.txt] [encrypted.age]```\n\n**Generate new key**\n\n```rage-keygen -o [key.txt]```\n\n**Encrypt with SSH key**\n\n```rage -R [~/.ssh/id_ed25519.pub] [file.txt]```\n\n**Armor output**\n\n```rage -a -r [age1...] [file.txt]```\n\n**Encrypt to multiple recipients**\n\n```rage -r [age1...] -r [age1...] -o [encrypted.age] [file.txt]```\n\n**Decrypt with passphrase**\n\n```rage -d -p [encrypted.age]```\n\n**Encrypt stdin**\n\n```echo \"secret\" | rage -r [age1...] -o [secret.age]```"],["SYNOPSIS","**rage** [_-e_|_-d_] [_-r recipient_] [_-i identity_] [_-o output_] [_options_] [_file_]"],["PARAMETERS","**-e**, **--encrypt**\n> Encrypt mode.\n\n**-d**, **--decrypt**\n> Decrypt mode.\n\n**-r**, **--recipient** _KEY_\n> Recipient public key.\n\n**-R**, **--recipients-file** _FILE_\n> File with recipients.\n\n**-i**, **--identity** _FILE_\n> Identity/private key.\n\n**-p**, **--passphrase**\n> Use passphrase.\n\n**-a**, **--armor**\n> ASCII armor output.\n\n**-o**, **--output** _FILE_\n> Output file. If omitted, writes to stdout.\n\n**-j** _PLUGINS_\n> Use the given age plugin."],["DESCRIPTION","**rage** is a Rust implementation of the age encryption tool. It provides simple, modern file encryption.\n\nPublic key encryption uses age keys or SSH keys. Recipients are specified by their public key.\n\nPassphrase mode uses scrypt for key derivation. No key management needed for simple use.\n\nASCII armor produces text output. Safe for email or other text channels.\n\nMultiple recipients allow group encryption. Any recipient can decrypt."],["CAVEATS","Uses the age format; not compatible with GPG. Key management is manual with no key servers or web of trust. Requires the recipient's public key or a passphrase for encryption."],["HISTORY","**rage** is a Rust implementation of **age** (Actually Good Encryption), designed by Filippo Valsorda. Age aims to be a simpler alternative to GPG."],["INSTALL","```pacman: sudo pacman -S rage```\n\n```apk: sudo apk add rage```\n\n```zypper: sudo zypper install rage```\n\n```brew: brew install rage```\n\n```nix: nix profile install nixpkgs#rage```"],["SEE ALSO","[age](/man/age)(1), [age-keygen](/man/age-keygen)(1), [gpg](/man/gpg)(1), [ssh-keygen](/man/ssh-keygen)(1), [openssl](/man/openssl)(1)"]],"e":["age","age-keygen","gpg","ssh-keygen","openssl"],"x":"rage -p -o [encrypted.age] [file.txt]"},"rankmirrors":{"n":"rankmirrors","t":"Rank Arch Linux pacman mirrors by speed","s":[["TAGLINE","Rank Arch Linux pacman mirrors by speed"],["TLDR","**Rank** mirrorlist\n\n```rankmirrors [/etc/pacman.d/mirrorlist]```\n\nOutput top **N** mirrors\n\n```rankmirrors -n [5] [/etc/pacman.d/mirrorlist]```\n\n**Verbose** output\n\n```rankmirrors -v [/etc/pacman.d/mirrorlist]```\n\nTest **specific URL**\n\n```rankmirrors -u [url]```\n\nShow **response times** only\n\n```rankmirrors -t [/etc/pacman.d/mirrorlist]```"],["SYNOPSIS","**rankmirrors** [**-n** _number_] [**-v**] [**-t**] [**-u** _url_] _mirrorlist_"],["PARAMETERS","**-n _number_**\n> Output only top N mirrors\n\n**-v, --verbose**\n> Verbose output during ranking\n\n**-t, --times**\n> Output response times instead of mirrorlist\n\n**-u, --url _url_**\n> Test specific URL only\n\n**-r, --repo _repo_**\n> Specify repository for testing"],["DESCRIPTION","**rankmirrors** tests Arch Linux pacman package mirrors by measuring connection speed and response time, then outputs a sorted mirrorlist with the fastest mirrors first. It connects to each mirror in the input file, downloads a small test file, and ranks results by transfer rate, helping users optimize package download performance for their geographic location.\n\nThe output follows the standard mirrorlist format and can be redirected to replace **/etc/pacman.d/mirrorlist** directly. The **-n** flag limits output to only the top N fastest mirrors, and **-t** displays raw response times for comparison without generating a new mirrorlist."],["CAVEATS","Testing takes time as each mirror is contacted. Network conditions may vary. Part of pacman-contrib, not pacman itself. Consider reflector for automated updates."],["HISTORY","**rankmirrors** has been part of Arch Linux tooling to help users optimize their mirror selection. While still functional, many users now prefer reflector for automated mirror management."],["INSTALL","```aur: yay -S rankmirrors```"],["SEE ALSO","[pacman](/man/pacman)(8), [reflector](/man/reflector)(1), [pacman-mirrors](/man/pacman-mirrors)(1)"]],"e":["pacman","reflector","pacman-mirrors"],"x":"rankmirrors [/etc/pacman.d/mirrorlist]"},"semanage-login":{"n":"semanage-login","t":"Map Linux users to SELinux users","s":[["TAGLINE","Map Linux users to SELinux users"],["TLDR","**List** all login mappings\n\n```sudo semanage login -l```\n\n**Add** a login mapping (Linux user to SELinux user)\n\n```sudo semanage login -a -s selinux_user linux_username```\n\n**Delete** a login mapping\n\n```sudo semanage login -d linux_username```\n\n**Modify** an existing mapping\n\n```sudo semanage login -m -s selinux_user linux_username```\n\nAdd with **MLS/MCS range**\n\n```sudo semanage login -a -s user_u -r s0-s0:c0.c1023 linux_username```\n\nList only **customized** mappings\n\n```sudo semanage login -l -C```"],["SYNOPSIS","**semanage login** [_options_]"],["PARAMETERS","**-l**, **--list**\n> List login mappings.\n\n**-a**, **--add**\n> Add a new login mapping.\n\n**-d**, **--delete**\n> Delete a login mapping.\n\n**-m**, **--modify**\n> Modify an existing login mapping.\n\n**-s**, **--seuser** _user_\n> SELinux user to map to.\n\n**-r**, **--range** _range_\n> MLS/MCS security range (e.g., s0-s0:c0.c1023).\n\n**-C**, **--locallist**\n> Show only local customizations.\n\n**-n**, **--noheading**\n> Do not print heading when listing.\n\n**-N**, **--noreload**\n> Do not reload policy after commit.\n\n**-S** _STORE_, **--store** _STORE_\n> Select an alternate SELinux policy store to manage.\n\n**-D**, **--deleteall**\n> Remove all local customizations.\n\n**-E**, **--extract**\n> Extract customizable commands."],["DESCRIPTION","**semanage login** manages mappings between Linux users and SELinux users. When a Linux user logs in, this mapping determines their SELinux security context.\n\nDifferent SELinux users have different roles and permissions, allowing fine-grained access control. Prefix the Linux user name with **%** to indicate a group mapping (e.g., **%wheel**)."],["CAVEATS","Changes affect new login sessions. Existing sessions retain their original context."],["HISTORY","Part of **policycoreutils**, providing SELinux policy management tools."],["SEE ALSO","[semanage](/man/semanage)(8), [semanage-user](/man/semanage-user)(8), [semanage-fcontext](/man/semanage-fcontext)(8), [sestatus](/man/sestatus)(8)"]],"e":["semanage","semanage-user","semanage-fcontext","sestatus"],"x":"sudo semanage login -l"},"sendmail":{"n":"sendmail","t":"Send email via mail transfer agent","s":[["TAGLINE","Send email via mail transfer agent"],["TLDR","**Send an email** from command line\n\n```echo \"Message body\" | sendmail [recipient@example.com]```\n\n**Send email with subject** (using mail headers)\n\n```printf \"Subject: Test\\n\\nMessage body\" | sendmail [recipient@example.com]```\n\n**Send email from a file**\n\n```sendmail [recipient@example.com] < [message.txt]```\n\n**Process mail queue**\n\n```sendmail -q```\n\n**Display mail queue**\n\n```sendmail -bp```\n\n**Test configuration** without sending\n\n```sendmail -bv [recipient@example.com]```"],["SYNOPSIS","**sendmail** [**-flags**] [_address_...]"],["PARAMETERS","**-t**\n> Read recipients from message headers (To:, Cc:, Bcc:)\n\n**-f** _address_\n> Set envelope sender address\n\n**-F** _name_\n> Set full name of sender\n\n**-i**\n> Ignore dots alone on lines\n\n**-q**\n> Process saved messages in queue\n\n**-bp**\n> Print mail queue summary\n\n**-bv**\n> Verify addresses without sending\n\n**-bd**\n> Run as daemon\n\n**-bs**\n> Use SMTP protocol on stdin/stdout\n\n**-v**\n> Verbose mode\n\n**-O** _option_=_value_\n> Set mail server option"],["DESCRIPTION","**sendmail** is the traditional Unix mail transfer agent (MTA) interface. While the original sendmail program is complex, most Linux systems use compatible alternatives (Postfix, Exim) that provide the same command-line interface.\n\nThe command accepts a message on standard input and delivers it to the specified recipients. Headers can be included in the input; if **-t** is used, recipients are read from the To/Cc/Bcc headers instead of command-line arguments.\n\nFor simple email sending, the message format includes optional headers followed by a blank line and the body. Headers like Subject:, From:, To: are standard.\n\nThe mail queue holds messages awaiting delivery. Use **-q** to trigger queue processing and **-bp** (or **mailq**) to view queue status."],["MESSAGE FORMAT","```\nFrom: sender@example.com\nTo: recipient@example.com\nSubject: Test Email\n\nThis is the message body.\n```"],["CONFIGURATION","**/etc/mail/sendmail.cf**\n> Main sendmail configuration file (or equivalent for Postfix/Exim compatibility layer).\n\n**/etc/aliases**\n> Mail alias database mapping local addresses to recipients or programs.\n\n**/var/spool/mqueue/**\n> Mail queue directory holding messages awaiting delivery."],["CAVEATS","Modern systems often use Postfix or Exim providing sendmail-compatible interfaces. The actual MTA may be different from traditional sendmail.\n\nMany systems require proper configuration before sendmail can deliver external mail. Without an MTA configured, mail may only work locally.\n\nAvoid using sendmail for bulk mail or automation without proper authentication, SPF, DKIM, and DMARC configuration to prevent spam classification."],["HISTORY","Sendmail was written by **Eric Allman** at UC Berkeley, with the first version released in **1983**. It became the dominant Unix MTA and established the standard command-line interface that alternatives like Postfix and Exim maintain for compatibility."],["INSTALL","```apt: sudo apt install dma```\n\n```dnf: sudo dnf install postfix```\n\n```pacman: sudo pacman -S postfix```\n\n```apk: sudo apk add postfix```\n\n```zypper: sudo zypper install postfix```\n\n```nix: nix profile install nixpkgs#dma```"],["SEE ALSO","[mail](/man/mail)(1), [postfix](/man/postfix)(1), [mailq](/man/mailq)(1), [newaliases](/man/newaliases)(1)"]],"e":["mail","postfix","mailq","newaliases"],"x":"echo \"Message body\" | sendmail [recipient@example.com]"},"snyk":{"n":"snyk","t":"Find and fix security vulnerabilities","s":[["TAGLINE","Find and fix security vulnerabilities"],["TLDR","**Test project for vulnerabilities**\n\n```snyk test```\n\n**Test specific file**\n\n```snyk test --file=[package.json]```\n\n**Monitor project continuously**\n\n```snyk monitor```\n\n**Test container image**\n\n```snyk container test [image:tag]```\n\n**Test IaC files**\n\n```snyk iac test [/path/to/terraform/]```\n\n**Test code for issues**\n\n```snyk code test```\n\n**Authenticate**\n\n```snyk auth```\n\n**Ignore specific vulnerability**\n\n```snyk ignore --id=[SNYK-JS-LODASH-567746]```"],["SYNOPSIS","**snyk** _command_ [_--file file_] [_--severity-threshold level_] [_options_] [_target_]"],["PARAMETERS","**test**\n> Test project for vulnerabilities.\n\n**monitor**\n> Record project snapshot for monitoring.\n\n**container test** _IMAGE_\n> Test container image.\n\n**container monitor** _IMAGE_\n> Monitor container image.\n\n**iac test** _PATH_\n> Test Infrastructure as Code.\n\n**code test**\n> Test source code (SAST).\n\n**auth** [_TOKEN_]\n> Authenticate with Snyk.\n\n**ignore**\n> Ignore an issue.\n\n**wizard**\n> Interactive remediation.\n\n**fix**\n> Apply remediation to fix vulnerabilities.\n\n**sbom**\n> Generate a Software Bill of Materials (SBOM).\n\n**policy**\n> Manage .snyk policy file.\n\n**--file** _FILE_\n> Manifest file to test.\n\n**--severity-threshold** _LEVEL_\n> Only report: low, medium, high, critical.\n\n**--json**\n> Output as JSON.\n\n**--sarif**\n> Output as SARIF.\n\n**--all-projects**\n> Test all projects in directory.\n\n**--dev**\n> Include dev dependencies in the scan.\n\n**--fail-on** _TYPE_\n> Fail on: all, upgradable, patchable.\n\n**--prune-repeated-subdependencies**\n> Prune repeated dependencies.\n\n**-d**, **--debug**\n> Debug output.\n\n**--org** _ORG_\n> Specify organization."],["DESCRIPTION","**Snyk** finds and fixes vulnerabilities in code, dependencies, containers, and infrastructure as code. It integrates into development workflows, CI/CD pipelines, and provides continuous monitoring.\n\nDependency testing scans package manifests (package.json, requirements.txt, pom.xml, etc.) against Snyk's vulnerability database. Results show CVEs, severity, and available fixes.\n\nContainer scanning analyzes Docker images layer by layer, identifying vulnerable packages in base images and application dependencies. Recommendations include secure base image alternatives.\n\nInfrastructure as Code testing catches misconfigurations in Terraform, CloudFormation, Kubernetes, and other IaC files before deployment. Issues include security groups, encryption settings, and access controls.\n\nStatic analysis (code test) finds security issues in source code: injection flaws, hardcoded secrets, and other vulnerabilities.\n\nThe monitor command creates a snapshot in Snyk's service for ongoing vulnerability alerting as new CVEs are discovered."],["CAVEATS","Requires account for full functionality. Some features need paid plans. Internet required for database access. Large projects take time to scan. False positives possible. Not all package ecosystems fully supported."],["HISTORY","**Snyk** was founded in **2015** by Guy Podjarny. The name is \"kyns\" (open source security) reversed. Starting with JavaScript dependency scanning, it expanded to containers, IaC, and code analysis. The company raised significant funding and became a leader in developer security tools."],["INSTALL","```nix: nix profile install nixpkgs#snyk```"],["SEE ALSO","[npm-audit](/man/npm-audit)(1), [safety](/man/safety)(1), [trivy](/man/trivy)(1), [checkov](/man/checkov)(1), [docker](/man/docker)(1)"]],"e":["npm-audit","safety","trivy","checkov","docker"],"x":"snyk test"},"stunnel":{"n":"stunnel","t":"Universal TLS/SSL tunneling proxy","s":[["TAGLINE","Universal TLS/SSL tunneling proxy"],["TLDR","**Start stunnel with a configuration file**\n\n```stunnel [/etc/stunnel/stunnel.conf]```\n\n**Read configuration from a file descriptor** (e.g. stdin)\n\n```stunnel -fd [0]```\n\n**Display version and compile-time defaults**\n\n```stunnel -version```\n\n**Show available socket options**\n\n```stunnel -sockets```\n\n**Show available TLS options**\n\n```stunnel -options```\n\n**Display help**\n\n```stunnel -help```"],["SYNOPSIS","**stunnel** [_configfile_] | **-fd** _n_ | **-help** | **-version** | **-sockets** | **-options**"],["PARAMETERS","**configfile**\n> Path to the configuration file to use\n\n**-fd** _n_\n> Read configuration from file descriptor _n_ (Unix only)\n\n**-help**\n> Display help message\n\n**-version**\n> Display version information and compile-time defaults\n\n**-sockets**\n> Show default socket options\n\n**-options**\n> Show supported TLS/SSL options compiled into OpenSSL"],["CONFIGURATION","Stunnel is driven entirely by a configuration file. The file uses INI-style syntax: global directives appear at the top, then one or more named service sections in `[service_name]` blocks. Lines beginning with `;` are comments.\n\n**Global directives (selected)**\n\n**foreground**\n> Keep the process in the foreground and log to stderr instead of syslog. Values: `yes | no`. Default: `no`\n\n**pid**\n> Path to the PID file. An empty value disables PID file creation. Path is relative to `chroot` if set (Unix only)\n\n**chroot**\n> Chroot jail directory. Stunnel drops privileges and changes root to this directory after startup (Unix only)\n\n**setuid**\n> Unix user to run as after startup (Unix only)\n\n**setgid**\n> Unix group to run as after startup (Unix only)\n\n**debug**\n> Logging verbosity level. Format: `[FACILITY.]LEVEL`. Levels: emerg(0), alert(1), crit(2), err(3), warning(4), notice(5), info(6), debug(7). Default: `notice`\n\n**output**\n> Append log messages to the specified file instead of syslog\n\n**syslog**\n> Enable or disable logging to syslog. Values: `yes | no`. Default: `yes`\n\n**Service-level directives (selected)**\n\n**accept**\n> Accept connections on the specified address. Format: `[HOST:]PORT`. Omit `accept` in inetd mode\n\n**connect**\n> Forward connections to the specified address. Format: `[HOST:]PORT`. Default host: `localhost`. Multiple entries are supported for round-robin load balancing\n\n**client**\n> Enable client mode — the remote end is expected to speak TLS. Values: `yes | no`. Default: `no` (server mode)\n\n**cert**\n> Path to the PEM or PKCS#12 certificate chain file. Required in server mode; optional in client mode unless the server requires client certificates\n\n**key**\n> Path to the private key file. Defaults to the value of `cert` when omitted. The file should only be readable by its owner\n\n**CAfile**\n> Load trusted CA certificates from a file. Used with `verifyChain` or `verifyPeer`\n\n**CApath**\n> Load trusted CA certificates from a directory. Certificate files must be named in the OpenSSL hash format (`XXXXXXXX.0`)\n\n**verifyChain**\n> Verify the peer certificate chain starting from a trusted root CA. Values: `yes | no`. Default: `no`\n\n**verifyPeer**\n> Verify the end-entity (leaf) peer certificate. Values: `yes | no`. Default: `no`\n\n**checkHost**\n> Verify the hostname in the peer certificate's Subject Alternative Names. Multiple entries allowed. Requires OpenSSL 1.0.2+\n\n**sni**\n> Server Name Indication. In client mode: SNI hostname to send. In server mode: `SERVICE_NAME:SERVER_NAME_PATTERN` for virtual hosting. Requires OpenSSL 1.0.0+\n\n**protocol**\n> Application-level protocol to negotiate TLS with. Supported: `cifs`, `connect`, `imap`, `ldap`, `nntp`, `pgsql`, `pop3`, `proxy`, `smtp`, `socks`\n\n**exec**\n> Execute a local inetd-style program instead of connecting to a remote host. The program's stdin/stdout are used for the unencrypted side of the tunnel\n\n**execArgs**\n> Whitespace-separated argument list for `exec`, including the program name as `$0`\n\n**delay**\n> Delay DNS resolution for `connect` until a client connects. Values: `yes | no`. Default: `no`\n\n**ciphers**\n> Colon-delimited list of TLS 1.2 and earlier cipher suites to allow\n\n**ciphersuites**\n> Colon-delimited list of TLS 1.3 ciphersuites. Default: `TLS_CHACHA20_POLY1305_SHA256:TLS_AES_256_GCM_SHA384:TLS_AES_128_GCM_SHA256`. Requires OpenSSL 1.1.1+\n\n**sslVersionMin**\n> Minimum TLS protocol version. Requires OpenSSL 1.1.0+. Default: `TLSv1`\n\n**sslVersionMax**\n> Maximum TLS protocol version. Requires OpenSSL 1.1.0+. Default: `all`\n\n**securityLevel**\n> OpenSSL security level (0–5). Level 2 requires at least 112-bit security (default). Requires OpenSSL 1.1.0+\n\n**transparent**\n> Enable transparent proxy mode. Values: `none`, `source`, `destination`, `both`. Requires kernel and routing support (Unix only)\n\n**socket**\n> Set a socket option. Format: `a|l|r:OPTION=VALUE[:VALUE]` where `a`=accept socket, `l`=local socket, `r`=remote socket"],["DESCRIPTION","**stunnel** is a TLS/SSL proxy that wraps arbitrary TCP connections with encryption without requiring any changes to the application code. It uses OpenSSL for all cryptographic operations and supports modern TLS versions.\n\nIn **server mode** (the default), stunnel listens for incoming TLS-encrypted connections and forwards them unencrypted to a backend service (e.g. a plain-text IMAP or SMTP daemon). In **client mode** (`client = yes`), it accepts unencrypted connections from local clients and establishes a TLS-encrypted tunnel to a remote TLS server.\n\nStunnel reads a configuration file that contains global settings and one or more named service sections. Each service section defines an independent tunnel. Multiple tunnels can run within a single stunnel process.\n\nIt supports inetd-style operation (omit `accept`; stunnel inherits the socket), daemon mode, and launch by systemd. Unix signals control a running daemon: `SIGHUP` reloads the configuration, `SIGUSR1` reopens the log file, and `SIGTERM`/`SIGQUIT`/`SIGINT` shut it down.\n\n**FTP limitation**: stunnel cannot tunnel the FTP data channel because FTP uses dynamically negotiated secondary ports."],["EXAMPLES","**Wrap a plain IMAP daemon with TLS (server mode)**\n\n```\n[imapd]\naccept  = 993\nexec    = /usr/sbin/imapd\nexecArgs = imapd\ncert    = /etc/stunnel/server.pem\n```\n\n**Forward encrypted IMAP to a plain local daemon (server mode)**\n\n```\n[imapd]\naccept  = 993\nconnect = 143\ncert    = /etc/stunnel/server.pem\n```\n\n**Wrap outgoing connections to a remote TLS server (client mode)**\n\n```\n[imap-client]\nclient  = yes\naccept  = 127.0.0.1:143\nconnect = mail.example.com:993\n```\n\n**SOCKS proxy tunnel (client and server pair)**\n\n```\n; Server side\n[socks-server]\nprotocol = socks\naccept   = 9080\ncert     = /etc/stunnel/server.pem\n\n; Client side\n[socks-client]\nclient   = yes\naccept   = 127.0.0.1:1080\nconnect  = vpnserver.example.com:9080\nverifyChain = yes\nCAfile   = /etc/stunnel/ca.pem\n```"],["CAVEATS","SSLv2 and SSLv3 are disabled by default due to known security vulnerabilities. Certificate chain verification (`verifyChain = yes`) and hostname checking (`checkHost`) should be enabled in production client configurations to prevent man-in-the-middle attacks. Transparent proxy mode requires specific kernel support and routing configuration. The `compression` option poses a security risk (CRIME-style attacks) when the application allows attacker-controlled plaintext."],["HISTORY","stunnel was created by **Michał Trojnara** in **1998** as a universal TLS wrapper for TCP services. It remains in active development and supports modern TLS 1.3 and current OpenSSL APIs."],["INSTALL","```apt: sudo apt install stunnel4```\n\n```dnf: sudo dnf install stunnel```\n\n```pacman: sudo pacman -S stunnel```\n\n```apk: sudo apk add stunnel```\n\n```zypper: sudo zypper install stunnel```\n\n```brew: brew install stunnel```\n\n```nix: nix profile install nixpkgs#stunnel```"],["SEE ALSO","[openssl](/man/openssl)(1), [openssl-s_client](/man/openssl-s_client)(1), [certbot](/man/certbot)(1), [socat](/man/socat)(1), [haproxy](/man/haproxy)(1)"]],"e":["openssl","openssl-s_client","certbot","socat","haproxy"],"x":"stunnel [/etc/stunnel/stunnel.conf]"},"superfile":{"n":"superfile","t":"Pretty fancy and modern terminal file manager","s":[["TAGLINE","Pretty fancy and modern terminal file manager"],["TLDR","**Launch the file manager**\n\n```spf```\n\n**Open in a specific directory**\n\n```spf [/path/to/dir]```\n\n**Open multiple panels at given paths**\n\n```spf [/path/one] [/path/two]```\n\n**Print configuration and data file paths**\n\n```spf path-list```\n\n**Use a custom config file**\n\n```spf --config-file [/path/to/config.toml]```\n\n**Use a custom hotkey file**\n\n```spf --hotkey-file [/path/to/hotkeys.toml]```\n\n**Print version**\n\n```spf --version```"],["SYNOPSIS","**spf** [_options_] [_PATH_...]\n\n**spf** **path-list** [**--lastdir-file**]"],["PARAMETERS","**--config-file**, **-c** _PATH_\n> Use a different configuration file.\n\n**--hotkey-file**, **-hf** _PATH_\n> Use a different hotkey configuration file.\n\n**--chooser-file**, **-cf** _PATH_\n> Write the path of opened files to this file and exit.\n\n**--print-last-dir**, **-pld**\n> Print the last directory to stdout on exit (for cd-on-quit integration).\n\n**--fix-hotkeys**, **-fh**\n> Add any missing hotkeys to the hotkey config file.\n\n**--fix-config-file**, **-fch**\n> Add any missing fields to the config file.\n\n**--debug-info**, **-di**\n> Print debug information.\n\n**--version**, **-v**\n> Print version.\n\n**path-list**, **pl**\n> Print paths to configuration, hotkeys, log, and data directories."],["DESCRIPTION","**superfile** is a terminal-based file manager featuring multiple panels for viewing directories simultaneously, customizable themes, Vim keybindings, a clipboard viewer, and a process list. Available on Linux, macOS, and Windows.\n\nPanels can be split and navigated with Tab. Press **>** to open the built-in command prompt for SPF-mode commands such as `split` and `open <PATH>`."],["HISTORY","**superfile** was created by **yorukot** and is written in **Go** using the Bubble Tea framework."],["INSTALL","```pacman: sudo pacman -S superfile```\n\n```brew: brew install superfile```\n\n```nix: nix profile install nixpkgs#superfile```"],["SEE ALSO","[ranger](/man/ranger)(1), [lf](/man/lf)(1), [nnn](/man/nnn)(1), [yazi](/man/yazi)(1), [vifm](/man/vifm)(1)"]],"e":["ranger","lf","nnn","yazi","vifm"],"x":"spf"},"swupd":{"n":"swupd","t":"Clear Linux software update and bundle manager","s":[["TAGLINE","Clear Linux software update and bundle manager"],["TLDR","**Update** to latest version\n\n```sudo swupd update```\n\nCheck for **available updates**\n\n```swupd check-update```\n\n**List** installed bundles\n\n```swupd bundle-list```\n\n**Search** for a package\n\n```swupd search -b [package]```\n\n**Install** a bundle\n\n```sudo swupd bundle-add [bundle]```\n\n**Remove** a bundle\n\n```sudo swupd bundle-remove [bundle]```\n\n**Verify** and repair system files\n\n```sudo swupd verify```\n\nShow **system info**\n\n```swupd info```"],["SYNOPSIS","**swupd** _command_ [_OPTIONS_]"],["COMMANDS","**update** [_version_]\n> Update to latest or specified version\n\n**check-update**\n> Check for available updates\n\n**bundle-list** [**--all**]\n> List installed (or all available) bundles\n\n**bundle-add** _bundle_\n> Install a bundle\n\n**bundle-remove** _bundle_\n> Remove a bundle\n\n**search** _term_\n> Find bundles containing search term\n\n**info**\n> Display version and update server info\n\n**diagnose**\n> Verify system files against manifest\n\n**repair**\n> Fix detected file mismatches\n\n**autoupdate**\n> Enable/disable automatic updates\n\n**clean**\n> Remove cached files"],["PARAMETERS","**-b**\n> Search for bundles (with search)\n\n**--all**\n> Show all available bundles (with bundle-list)\n\n**--force**\n> Force operation\n\n**--no-scripts**\n> Skip post-update scripts\n\n**-h, --help**\n> Display help"],["DESCRIPTION","**swupd** is the software update and bundle management tool for Clear Linux. Unlike traditional package managers, swupd manages bundles (collections of related packages) and uses file-level versioning similar to git, where each OS version represents a complete, consistent snapshot.\n\nUpdates are highly efficient, downloading only changed files using binary deltas. The tool can verify system integrity, repair corrupted files, and manage automatic updates."],["CAVEATS","Requires network connectivity to update servers. Bundles include all dependencies; you cannot install individual packages. OS version numbers are not semantic; each represents a complete system state. Some bundles are large."],["HISTORY","**swupd** was developed by **Intel** for **Clear Linux**, a performance-optimized Linux distribution first released in **2015**. Clear Linux pioneered stateless computing and efficient update mechanisms. The swupd tool exemplifies Clear Linux's approach to software management with atomic updates and file-level verification."],["SEE ALSO","[flatpak](/man/flatpak)(1), [dnf](/man/dnf)(8), [apt](/man/apt)(8)"]],"e":["flatpak","dnf","apt"],"x":"sudo swupd update"},"systemsettings":{"n":"systemsettings","t":"KDE Plasma system settings GUI","s":[["TAGLINE","KDE Plasma system settings GUI"],["TLDR","Open settings **GUI**\n\n```systemsettings```\n\n**List** all KCM modules\n\n```systemsettings --list```\n\nDisplay **help**\n\n```systemsettings -h```"],["SYNOPSIS","**systemsettings** [_OPTIONS_] [_MODULE_]"],["PARAMETERS","**--list**\n> List all available KCM (KDE Control Module) modules\n\n**-h, --help**\n> Display help information"],["DESCRIPTION","**systemsettings** is the central configuration program for KDE Plasma desktop. It provides a graphical interface to configure system settings including appearance, workspace behavior, hardware, network, and applications.\n\nSettings are organized into categories with individual KCM modules handling specific configuration areas."],["CAVEATS","Requires KDE Plasma desktop environment. Some modules require root privileges for system-wide changes. Changes take effect immediately or after logout depending on the setting."],["HISTORY","**systemsettings** has been the primary configuration tool for KDE since KDE 4, replacing the earlier KControl application. It continues as the settings interface in KDE Plasma 5 and 6."],["INSTALL","```apt: sudo apt install systemsettings```\n\n```pacman: sudo pacman -S systemsettings```\n\n```apk: sudo apk add systemsettings```"],["SEE ALSO","[kwriteconfig5](/man/kwriteconfig5)(1)"]],"e":["kwriteconfig5"],"x":"systemsettings"},"tee":{"n":"tee","t":"Read from stdin and write to stdout and files","s":[["TAGLINE","Read from stdin and write to stdout and files"],["TLDR","**Write stdin to file and stdout**\n\n```echo \"text\" | tee [file]```\n\n**Append to file** instead of overwriting\n\n```echo \"text\" | tee -a [file]```\n\n**Write to multiple files**\n\n```echo \"text\" | tee [file1] [file2]```\n\n**Write command output to file** while viewing it\n\n```[command] | tee [output.log]```\n\n**Use with sudo** to write to protected files\n\n```echo \"text\" | sudo tee [/etc/file]```\n\n**Write to file and pipe** to another command\n\n```[command] | tee [file] | [another_command]```\n\n**Suppress stdout** (write to file only)\n\n```[command] | tee [file] > /dev/null```"],["SYNOPSIS","**tee** [_options_] [_file_...]"],["PARAMETERS","**-a**, **--append**\n> Append to files instead of overwriting\n\n**-i**, **--ignore-interrupts**\n> Ignore SIGINT (interrupt signal)\n\n**-p**\n> Operate in a more appropriate mode with pipes (default MODE: warn-nopipe)\n\n**--output-error**[=_mode_]\n> Set behavior on write error (warn, warn-nopipe, exit, exit-nopipe)\n\n**--help**\n> Display help and exit\n\n**--version**\n> Display version and exit"],["DESCRIPTION","**tee** reads from standard input and writes to both standard output and one or more files simultaneously. Named after the T-shaped pipe fitting in plumbing, it \"splits\" the data stream.\n\nCommon uses include logging command output while still viewing it, saving intermediate pipeline results for debugging, and writing to files requiring elevated privileges via sudo.\n\nWithout **-a**, tee overwrites existing files. With multiple files, the same content is written to all of them. If a FILE is **-**, tee copies again to standard output.\n\nTee continues the pipeline—output goes to both files and stdout, allowing further processing with additional pipe stages."],["CAVEATS","For writing to protected files with sudo, use **sudo tee** rather than **sudo echo >** because redirection happens in the current shell before sudo.\n\nIn pipelines with **set -o pipefail**, tee failures can go unnoticed. Use **--output-error=exit** for strict error handling.\n\nWhen combined with commands that buffer output, you may not see immediate results. Use **stdbuf** or command-specific unbuffering options if needed."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[cat](/man/cat)(1), [script](/man/script)(1), [pv](/man/pv)(1), [sponge](/man/sponge)(1)"]],"e":["cat","script","pv","sponge"],"x":"echo \"text\" | tee [file]"},"tilde":{"n":"tilde","t":"Shell tilde expansion shorthand","s":[["TAGLINE","Shell tilde expansion shorthand"],["TLDR","**Home directory**\n\n```cd ~```\n\n**Another user's home directory**\n\n```cd ~[username]```\n\n**Previous directory**\n\n```cd ~-```\n\n**Current directory**\n\n```echo ~+```\n\n**Case modification (bash 4+)**\n\n```echo ${var~~}```"],["SYNOPSIS","**~**\n**~**_username_\n**~+**\n**~-**"],["TILDE EXPANSIONS","**~**\n> Current user's home directory ($HOME)\n\n**~username**\n> Specified user's home directory\n\n**~+**\n> Current working directory ($PWD)\n\n**~-**\n> Previous working directory ($OLDPWD)"],["PARAMETER EXPANSION (bash 4+)","**${var^}**\n> Uppercase first character\n\n**${var^^}**\n> Uppercase all characters\n\n**${var,}**\n> Lowercase first character\n\n**${var,,}**\n> Lowercase all characters\n\n**${var~}**\n> Toggle case of first character\n\n**${var~~}**\n> Toggle case of all characters"],["DESCRIPTION","**~** (tilde) is expanded by the shell to directory paths. It's a shorthand that avoids hardcoding paths like /home/user.\n\nTilde expansion only occurs at the start of a word or after **:**. In the middle of text, **~** is literal.\n\n```bash\ncd ~                    # Go home\ncd ~/Documents          # Home subdirectory\ncd ~root                # Root's home\nPATH=$PATH:~/bin        # After : works\n```\n\nThe **~+** and **~-** forms mirror the **pushd/popd** directory stack, allowing quick navigation between recent directories."],["EXAMPLES","```bash"],["Copy to home","cp file.txt ~"],["Use another user's home","sudo cp config ~www-data/"],["Toggle between directories","cd /var/log\ncd /etc\ncd ~-     # Back to /var/log\ncd ~-     # Back to /etc"],["Case toggle (bash 4+)","name=\"Hello\"\necho ${name~~}    # hELLO\n```"],["CAVEATS","Tilde only expands when unquoted and at word start. **\"~\"** is literal, not home.\n\n**~user** looks up the user in the password database, not just string concatenation.\n\n**~-** is empty if no previous directory (new shell)."],["INSTALL","```zypper: sudo zypper install tilde```"],["SEE ALSO","[cd](/man/cd)(1), [bash](/man/bash)(1), [pwd](/man/pwd)(1), [pushd](/man/pushd)(1)"]],"e":["cd","bash","pwd","pushd"],"x":"cd ~"},"unshadow":{"n":"unshadow","t":"Merge passwd and shadow files for auditing","s":[["TAGLINE","Merge passwd and shadow files for auditing"],["TLDR","Combine **system** passwd and shadow\n\n```sudo unshadow /etc/passwd /etc/shadow```\n\nCombine **arbitrary** files\n\n```sudo unshadow [path/to/passwd] [path/to/shadow]```"],["SYNOPSIS","**unshadow** _PASSWD_FILE_ _SHADOW_FILE_"],["DESCRIPTION","**unshadow** combines /etc/passwd and /etc/shadow files to create a traditional Unix password file format. This unified format is required by password cracking tools like John the Ripper.\n\nThe utility is part of the John the Ripper project and is used in authorized password security auditing to prepare password hashes for analysis."],["CAVEATS","Requires root privileges to read shadow file. Should only be used for authorized security testing. Output contains sensitive password hashes."],["HISTORY","**unshadow** was developed as part of the **John the Ripper** password cracker project to handle modern Unix systems that separate password hashes into the shadow file."],["INSTALL","```apt: sudo apt install john```\n\n```dnf: sudo dnf install john```\n\n```pacman: sudo pacman -S john```\n\n```apk: sudo apk add john```\n\n```zypper: sudo zypper install john```\n\n```brew: brew install john```\n\n```nix: nix profile install nixpkgs#john```"],["SEE ALSO","[john](/man/john)(8), [passwd](/man/passwd)(5)"]],"e":["john","passwd"],"x":"sudo unshadow /etc/passwd /etc/shadow"},"uv-help":{"n":"uv-help","t":"Display uv command help information","s":[["TAGLINE","Display uv command help information"],["TLDR","**Show general help**\n\n```uv help```\n\n**Help for specific command**\n\n```uv help [command]```\n\n**Help for subcommand**\n\n```uv help [command] [subcommand]```"],["SYNOPSIS","**uv** **help** [_command_...]"],["DESCRIPTION","**uv help** displays help information for uv commands. Shows usage, options, and examples for the specified command."],["INSTALL","```dnf: sudo dnf install uv```\n\n```pacman: sudo pacman -S uv```\n\n```apk: sudo apk add uv```\n\n```brew: brew install uv```\n\n```nix: nix profile install nixpkgs#uv```"],["SEE ALSO","[uv](/man/uv)(1)"]],"e":["uv"],"x":"uv help"},"vagrant-status":{"n":"vagrant-status","t":"Display Vagrant machine state","s":[["TAGLINE","Display Vagrant machine state"],["TLDR","**Show VM status**\n\n```vagrant status```\n\n**Show specific VM status**\n\n```vagrant status [name]```"],["SYNOPSIS","**vagrant** **status** [_options_] [_name_]"],["DESCRIPTION","**vagrant status** displays the current state of Vagrant machines. Shows whether VMs are running, halted, suspended, or not created. Run from directory containing Vagrantfile."],["INSTALL","```apt: sudo apt install vagrant```\n\n```dnf: sudo dnf install vagrant```\n\n```nix: nix profile install nixpkgs#vagrant```"],["SEE ALSO","[vagrant](/man/vagrant)(1), [vagrant-global-status](/man/vagrant-global-status)(1)"]],"e":["vagrant","vagrant-global-status"],"x":"vagrant status"},"vcftools":{"n":"vcftools","t":"Analyze genomic variant call format files","s":[["TAGLINE","Analyze genomic variant call format files"],["TLDR","**Filter VCF file** by chromosome and output new VCF\n\n```vcftools --vcf [input.vcf] --chr [chr1] --recode --out [output]```\n\n**Calculate allele frequency**\n\n```vcftools --vcf [input.vcf] --freq --out [output]```\n\n**Extract specific individuals**\n\n```vcftools --vcf [input.vcf] --keep [individuals.txt] --recode --out [output]```\n\n**Filter by minimum quality score**\n\n```vcftools --vcf [input.vcf] --minQ [30] --recode --out [output]```\n\n**Calculate depth statistics** per individual\n\n```vcftools --vcf [input.vcf] --depth --out [output]```\n\n**Filter by minor allele frequency**\n\n```vcftools --vcf [input.vcf] --maf [0.05] --recode --out [output]```\n\n**Read compressed VCF** file\n\n```vcftools --gzvcf [input.vcf.gz] --freq --out [output]```"],["SYNOPSIS","**vcftools** [**--vcf** _file_ | **--gzvcf** _file_ | **--bcf** _file_] [**--out** _prefix_] [_options_]"],["PARAMETERS","**--vcf** _file_\n> Input VCF file (v4.0, v4.1, or v4.2).\n\n**--gzvcf** _file_\n> Input compressed (gzipped) VCF file.\n\n**--bcf** _file_\n> Input BCF2 format file.\n\n**--out** _prefix_\n> Output file prefix. Results are written to prefix.extension.\n\n**--recode**\n> Output a new VCF file after applying filters.\n\n**--recode-INFO-all**\n> Retain all INFO fields in recoded output.\n\n**--chr** _name_\n> Process only variants on specified chromosome.\n\n**--keep** _file_\n> Retain only individuals listed in file (one ID per line).\n\n**--remove** _file_\n> Remove individuals listed in file.\n\n**--maf** _float_\n> Filter by minimum minor allele frequency.\n\n**--minQ** _int_\n> Minimum variant quality score.\n\n**--freq**\n> Calculate allele frequencies.\n\n**--depth**\n> Calculate mean depth per individual.\n\n**--relatedness**\n> Calculate pairwise relatedness statistics.\n\n**--hap-r2**\n> Calculate linkage disequilibrium statistics using phased haplotypes."],["DESCRIPTION","**VCFtools** is a suite of utilities for analyzing Variant Call Format (VCF) and Binary Call Format (BCF) files, the standard formats for storing genomic sequence variations. It provides comprehensive tools for filtering, manipulating, and computing statistics from variant data.\n\nThe tool supports filtering variants by quality scores, allele frequencies, missing data, genomic regions, and individual samples. It calculates population genetics statistics including allele frequencies, nucleotide diversity, Fst, linkage disequilibrium, and relatedness measures.\n\nVCFtools can convert between formats, compare VCF files, and extract subsets of data for downstream analysis. Output files use the prefix specified by **--out** with appropriate extensions for each analysis type."],["CAVEATS","Large VCF files can consume significant memory. Some operations require the input to be sorted by chromosome and position. Compressed files should use bgzip compression (not gzip) for optimal performance with indexing. Binary BCF format is faster for repeated analyses."],["HISTORY","**VCFtools** was developed by **Adam Auton** and **Anthony Marcketta** at Cornell University, with the first release around **2011**. It was created to address the need for efficient VCF manipulation as next-generation sequencing became widespread. The tool has become a standard in bioinformatics pipelines for variant analysis and quality control."],["INSTALL","```apt: sudo apt install vcftools```\n\n```dnf: sudo dnf install vcftools```\n\n```brew: brew install vcftools```\n\n```nix: nix profile install nixpkgs#vcftools```"],["SEE ALSO","[bcftools](/man/bcftools)(1), [tabix](/man/tabix)(1), [bgzip](/man/bgzip)(1), [samtools](/man/samtools)(1)"]],"e":["bcftools","tabix","bgzip","samtools"],"x":"vcftools --vcf [input.vcf] --chr [chr1] --recode --out [output]"},"webstorm":{"n":"webstorm","t":"JetBrains JavaScript IDE launcher","s":[["TAGLINE","JetBrains JavaScript IDE launcher"],["TLDR","**Open project**\n\n```webstorm [path/to/project]```\n\n**Open file at line**\n\n```webstorm --line [42] [file.js]```\n\n**Compare files**\n\n```webstorm diff [file1.js] [file2.js]```\n\n**Open in new window**\n\n```webstorm --new-window [path/to/project]```\n\n**Wait until closed**\n\n```webstorm --wait [file.js]```"],["SYNOPSIS","**webstorm** [_--line n_] [_--new-window_] [_options_] [_path_]"],["PARAMETERS","**--line** _N_\n> Open at line.\n\n**--column** _N_\n> Open at column.\n\n**--new-window**\n> Force new window.\n\n**--wait**\n> Wait until closed.\n\n**diff**\n> Compare files."],["DESCRIPTION","**webstorm** is the command-line launcher for JetBrains WebStorm, a professional IDE for JavaScript and web development. It opens projects, files, and directories directly from the terminal, with options to specify the line and column position for precise cursor placement.\n\nThe launcher supports opening files in an existing WebStorm instance or forcing a new window, and includes a diff mode for side-by-side file comparison. The --wait flag blocks the terminal until the file is closed, making it suitable for use as a Git editor or merge tool.\n\nThe command is typically installed via WebStorm's \"Create Command-line Launcher\" option in the Tools menu, which places it in the system PATH."],["CAVEATS","WebStorm installation required. Commercial software. Launcher location varies."],["HISTORY","**WebStorm** is developed by **JetBrains** as a professional IDE for JavaScript and web development."],["INSTALL","```aur: yay -S webstorm```"],["SEE ALSO","[phpstorm](/man/phpstorm)(1), [idea](/man/idea)(1), [code](/man/code)(1)"]],"e":["phpstorm","idea","code"],"x":"webstorm [path/to/project]"},"xfce4-terminal":{"n":"xfce4-terminal","t":"XFCE terminal emulator","s":[["TAGLINE","XFCE terminal emulator"],["TLDR","**Open** new terminal\n\n```xfce4-terminal```\n\nSet **title**\n\n```xfce4-terminal --initial-title \"[initial_title]\"```\n\nOpen new **tab**\n\n```xfce4-terminal --tab```\n\n**Execute** command\n\n```xfce4-terminal --command \"[command_with_args]\"```\n\nExecute and **hold**\n\n```xfce4-terminal --command \"[command_with_args]\" --hold```\n\nOpen **multiple** tabs\n\n```xfce4-terminal --tab --command \"[command1]\" --tab --command \"[command2]\"```"],["SYNOPSIS","**xfce4-terminal** [_OPTIONS_]"],["PARAMETERS","**--initial-title** _TITLE_\n> Set initial window title\n\n**--tab**\n> Open new tab\n\n**--command** _COMMAND_\n> Execute command in terminal\n\n**--hold**\n> Keep terminal open after command exits\n\n**--working-directory** _DIR_\n> Set working directory\n\n**--geometry** _WxH+X+Y_\n> Window geometry\n\n**--fullscreen**\n> Start in fullscreen mode"],["DESCRIPTION","**xfce4-terminal** is the terminal emulator for the XFCE desktop environment. It provides tabs, configurable appearance, and drop-down mode support.\n\nThe terminal supports multiple profiles and can execute commands on startup."],["CAVEATS","Designed for XFCE but works on other desktops. Requires GTK and VTE libraries."],["INSTALL","```apt: sudo apt install xfce4-terminal```\n\n```dnf: sudo dnf install xfce4-terminal```\n\n```pacman: sudo pacman -S xfce4-terminal```\n\n```apk: sudo apk add xfce4-terminal```\n\n```zypper: sudo zypper install xfce4-terminal```\n\n```nix: nix profile install nixpkgs#xfce4-terminal```"],["SEE ALSO","[gnome-terminal](/man/gnome-terminal)(1), [konsole](/man/konsole)(1), [xterm](/man/xterm)(1)"]],"e":["gnome-terminal","konsole","xterm"],"x":"xfce4-terminal"},"xq":{"n":"xq","t":"XML processor using jq syntax","s":[["TAGLINE","XML processor using jq syntax"],["TLDR","**Convert XML to JSON**\n\n```xq . [file.xml]```\n\n**Extract value using jq syntax**\n\n```xq '.[key]' [file.xml]```\n\n**Convert and format XML**\n\n```cat [file.xml] | xq .```\n\n**Convert JSON back to XML**\n\n```xq -x . [file.json]```\n\n**Process multiple XML files**\n\n```xq . [a.xml] [b.xml]```\n\n**Stream large XML document**\n\n```xq . --xml-item-depth [2] [large.xml]```\n\n**Wrap output in XML root element**\n\n```xq -x --xml-root [root] . [file.json]```"],["SYNOPSIS","**xq** [_options_] _jq-filter_ [_file_...]"],["PARAMETERS","**-x**, **--xml-output**\n> Transcode output back to XML.\n\n**--xml-item-depth** _n_\n> Stream items at depth n (for large documents).\n\n**--xml-dtd**\n> Preserve Document Type Definition.\n\n**--xml-root** _name_\n> Wrap XML output in element with this name.\n\n**--xml-force-list** _element_\n> Force element to always be a list.\n\n**-i**, **--in-place**\n> Edit files in place.\n\n**-c**, **--compact-output**\n> Compact JSON output.\n\n**-r**, **--raw-output**\n> Raw string output.\n\n**--version**\n> Display version.\n\n**-h**, **--help**\n> Display help."],["DESCRIPTION","**xq** is a command-line XML processor that wraps jq. It transcodes XML documents to JSON using xmltodict, pipes them through jq for processing, and optionally converts the result back to XML.\n\nThe tool enables using jq's powerful JSON query syntax on XML data. All standard jq filters and operations work on the converted JSON representation.\n\nFor large XML documents, the --xml-item-depth option enables streaming mode, processing elements at a specified depth without loading the entire document into memory.\n\nEntity expansion and DTD resolution are disabled by default for security. Install via: `pip install yq` or `brew install python-yq`"],["CAVEATS","XML attribute ordering may not be preserved. Some XML features have limited JSON representation. Part of the yq package (not the Go-based yq). Requires Python and jq."],["HISTORY","**xq** was created as part of the yq project by Andrey Kislyuk to provide jq-like functionality for YAML and XML. It fills the gap for command-line XML processing using the familiar jq syntax."],["INSTALL","```apt: sudo apt install xq```\n\n```dnf: sudo dnf install xq```\n\n```apk: sudo apk add xq```\n\n```brew: brew install xq```\n\n```nix: nix profile install nixpkgs#xq```"],["SEE ALSO","[jq](/man/jq)(1), [yq](/man/yq)(1), [xmllint](/man/xmllint)(1), [xmlstarlet](/man/xmlstarlet)(1)"]],"e":["jq","yq","xmllint","xmlstarlet"],"x":"xq . [file.xml]"},"yadm-gitconfig":{"n":"yadm-gitconfig","t":"Manage yadm repository git configuration","s":[["TAGLINE","Manage yadm repository git configuration"],["TLDR","**Show yadm git config**\n\n```yadm gitconfig --list```\n\n**Set yadm git config**\n\n```yadm gitconfig [setting] [value]```"],["SYNOPSIS","**yadm** **gitconfig** [_options_] [_name_] [_value_]"],["DESCRIPTION","**yadm gitconfig** manages yadm repository's git configuration. Separate from system git config. Sets user, remote, and other git settings specifically for the yadm repo."],["INSTALL","```pacman: sudo pacman -S yadm```\n\n```apk: sudo apk add yadm```\n\n```zypper: sudo zypper install yadm```\n\n```brew: brew install yadm```\n\n```nix: nix profile install nixpkgs#yadm```"],["SEE ALSO","[yadm](/man/yadm)(1), [yadm-config](/man/yadm-config)(1), [git-config](/man/git-config)(1)"]],"e":["yadm","yadm-config","git-config"],"x":"yadm gitconfig --list"},"ziggity":{"n":"ziggity","t":"Fast keyboard-driven terminal UI for Git written in Zig","s":[["TAGLINE","Fast keyboard-driven terminal UI for Git written in Zig"],["TLDR","**Open ziggity** in the current Git repository\n\n```ziggity```\n\n**Install via Homebrew** (macOS / Linux)\n\n```brew install simoarpe/ziggity/ziggity```\n\n**Build from source** (requires Zig 0.16)\n\n```zig build -Doptimize=ReleaseSafe```\n\n**Point at a shared config file** from the shell\n\n```export ZIGGITY_CONFIG=\"$HOME/.config/ziggity/config.ini\"```\n\n**Show in-app keybindings** (once running)\n\n```?```\n\n**Quit**\n\n```q```"],["SYNOPSIS","**ziggity**"],["DESCRIPTION","**ziggity** is a terminal user interface (TUI) for Git that provides a lazygit-style workflow in a small Zig binary. It drives plain **git** as a subprocess (no libgit2) and uses libvaxis for the UI. Split panels cover Status, Files, Branches, Commits, and Stash, with a live diff preview and a context-sensitive footer.\n\nCore operations are keyboard-driven: stage or unstage whole files or individual hunks and lines, commit with an in-app summary/body editor, manage branches and tags, stash with optional messages, fetch/pull/push without blocking the UI, and run interactive rebases through a visual plan editor. Additional features include a real **git log --graph** viewer, multi-selection ranges, custom patch building, merge-conflict resolution, GPG signature checks, worktree and submodule navigation, remappable keys, and themeable colours.\n\nRun **ziggity** inside any Git repository. Press **?** for the keybinding overlay (scrolled to the focused panel) and **q** to quit. Prebuilt static binaries are published for macOS, Linux (musl), and Windows; Homebrew installs the macOS/Linux package from the project's tap."],["KEYBOARD SHORTCUTS","**1**–**5**: Focus Status / Files / Branches / Commits / Stash\n**h** **l** / arrows: Move focus between side panels\n**j** **k** / arrows: Move selection\n**tab**: Focus the Diff panel (and back)\n**space**: Stage file · checkout branch · apply stash (by focus)\n**c**: Commit\n**a**: Stage/unstage all (Files)\n**d** / **D**: Discard menu / discard all\n**f** / **p** / **P**: Fetch / pull / push\n**i**: Interactive rebase plan editor\n**ctrl+l**: Commit graph viewer\n**ctrl+z**: Undo last operation\n**?**: Keybindings help\n**q**: Quit"],["CONFIGURATION","**ZIGGITY_CONFIG**\n> Environment variable pointing at an INI config file applied globally. There is no auto-loaded XDG path; set this in the shell profile to share settings across repositories.\n\n**\\<repo\\>/.ziggity.ini**\n> Per-repository INI file that overrides **ZIGGITY_CONFIG**. Without any config file, settings fall back to defaults and per-repo auto-detection (notably the editor).\n\nCommon settings include panel layout (**side_panel_width_percent**, **expand_focused_side_panel**), commit editor guides (**commit_summary_limit**, **commit_body_guide**), refresh/fetch intervals, editor presets (**editor_preset**, **editor_command**), remappable keys (**key.\\<name\\>**), theme colours (**color.\\<name\\>** as terminal palette indices 0–255), and custom commands (**command.\\<key\\>**)."],["CAVEATS","Requires **git** on **PATH** at runtime. Destructive history operations (hard reset, force push, rebase) should be used carefully. macOS prebuilt binaries may need a one-time quarantine clear (**xattr -d com.apple.quarantine**). Windows builds are cross-compiled and experimental. Feature parity with lazygit is intentionally incomplete in places (for example, redo after undo is not implemented)."],["HISTORY","**ziggity** was created by **Simone Arpe** and is written in **Zig**. It aims for a lazygit-like feel without being a line-by-line port, emphasizing a small dependency-light binary, explicit memory ownership, and plain git subprocesses."],["SEE ALSO","[git](/man/git)(1), [lazygit](/man/lazygit)(1), [gitui](/man/gitui)(1), [tig](/man/tig)(1)"],["RESOURCES","```[Source code](https://github.com/simoarpe/ziggity)```"]],"e":["git","lazygit","gitui","tig"],"x":"ziggity"}});
