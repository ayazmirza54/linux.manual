window.LCL_BUCKET(45,{"aconnect":{"n":"aconnect","t":"ALSA MIDI port connection manager","s":[["TAGLINE","ALSA MIDI port connection manager"],["TLDR","**List** all MIDI input and output ports\n\n```aconnect -l```\n\n**Connect** two MIDI ports\n\n```aconnect [sender_client]:[port] [receiver_client]:[port]```\n\n**Disconnect** two MIDI ports\n\n```aconnect -d [sender_client]:[port] [receiver_client]:[port]```\n\nDisconnect **all** connections\n\n```aconnect -x```\n\nList only **input** ports\n\n```aconnect -i```"],["SYNOPSIS","**aconnect** [_-options_] _sender_ _receiver_\n\n**aconnect** **-d** [_-options_] _sender_ _receiver_\n\n**aconnect** **-i**|**-o** [_-options_]\n\n**aconnect** **-x**"],["DESCRIPTION","**aconnect** is an ALSA (Advanced Linux Sound Architecture) utility for connecting and disconnecting MIDI ports. It manages the ALSA sequencer connections that route MIDI data between applications, hardware MIDI interfaces, and virtual MIDI ports.\n\nThe tool is essential for setting up MIDI routing in Linux audio workflows, allowing you to connect MIDI controllers to synthesizers, route between DAWs, or chain MIDI effects processors."],["PARAMETERS","**-l**, **--list**\n> List current MIDI ports and connections\n\n**-i**, **--input**\n> List only input (readable) ports\n\n**-o**, **--output**\n> List only output (writable) ports\n\n**-d**, **--disconnect**\n> Disconnect the specified ports\n\n**-x**, **--removeall**\n> Remove all connections\n\n**-e**, **--exclusive**\n> Connect ports exclusively (no other connections allowed)\n\n**-r**, **--real** _queue_\n> Convert time-stamps using the given real-time queue\n\n**-t**, **--tick** _queue_\n> Convert time-stamps using the given tick queue"],["CAVEATS","Connections made with aconnect are not persistent; they're lost when the MIDI device is disconnected or the system reboots. For persistent connections, use tools like **qjackctl** or write startup scripts. Client numbers may change between sessions."],["HISTORY","**aconnect** was developed as part of the ALSA project in the late **1990s**. It provided a command-line interface for the ALSA sequencer system, which replaced the older OSS (Open Sound System) MIDI handling in Linux."],["INSTALL","```apt: sudo apt install alsa-utils```\n\n```dnf: sudo dnf install alsa-utils```\n\n```pacman: sudo pacman -S alsa-utils```\n\n```apk: sudo apk add alsa-utils```\n\n```zypper: sudo zypper install alsa-utils```\n\n```nix: nix profile install nixpkgs#alsa-utils```"],["SEE ALSO","[amidi](/man/amidi)(1), [aplaymidi](/man/aplaymidi)(1), [arecordmidi](/man/arecordmidi)(1), [aseqdump](/man/aseqdump)(1)"],["RESOURCES","```[Source code](https://github.com/alsa-project/alsa-utils)```\n\n```[Homepage](https://www.alsa-project.org/)```"]],"e":["amidi","aplaymidi","arecordmidi","aseqdump"],"x":"aconnect -l"},"atool":{"n":"atool","t":"Manage archives in various formats with a unified interface","s":[["TAGLINE","Manage archives in various formats with a unified interface"],["TLDR","**Extract an archive (auto-detects format)**\n\n```atool -x [archive.tar.gz]```\n\n**List archive contents**\n\n```atool -l [archive.zip]```\n\n**Create an archive from files or directories**\n\n```atool -a [output.tar.gz] [file1] [file2]```\n\n**Extract into a specific directory**\n\n```atool -X [/path/to/dir] [archive.zip]```\n\n**Extract each archive in the current directory**\n\n```atool -e -x *.zip```\n\n**Print a file from an archive to standard output**\n\n```atool -c [archive.tar.gz] [path/inside/archive]```\n\n**Show the diff between two archives**\n\n```atool -d [old.tar.gz] [new.tar.gz]```"],["SYNOPSIS","**atool** [_-lxacdX_] [_options_] _archive_ [_files_...]"],["PARAMETERS","**-l**, **--list**\n> List the contents of an archive.\n\n**-x**, **--extract**\n> Extract files from an archive. By default a unique subdirectory is created if the archive is not already tidy.\n\n**-X** _PATH_, **--extract-to=**_PATH_\n> Extract the archive into the given directory.\n\n**-a**, **--add**\n> Create a new archive from the supplied files. The archive format is inferred from the output file's extension.\n\n**-c**, **--cat**\n> Extract a single file from the archive to standard output.\n\n**-d**, **--diff**\n> Extract two archives and run **diff**(1) on their contents.\n\n**-e**, **--each**\n> Perform the requested action on each of the remaining arguments individually (useful for batch extraction).\n\n**-F** _EXT_, **--format=**_EXT_\n> Override automatic format detection.\n\n**-O** _OPT_, **--format-option=**_OPT_\n> Pass an extra option to the underlying archiver (for example `-O-mx=9` to set 7z compression level).\n\n**-D**, **--subdir**\n> Always create a subdirectory when extracting, even if the archive already contains a top-level directory.\n\n**-f**, **--force**\n> Overwrite existing files or archives without asking.\n\n**-S**, **--simulate**\n> Show what would be done without modifying the filesystem.\n\n**-E**, **--explain**\n> Print the underlying commands that atool runs.\n\n**-p**, **--page**\n> Pipe output through a pager.\n\n**-q**, **--quiet**\n> Decrease verbosity.\n\n**-v**, **--verbose**\n> Increase verbosity.\n\n**-o** _KEY=VAL_, **--option=**_KEY=VAL_\n> Override a configuration setting on the command line.\n\n**--config=**_FILE_\n> Load a non-default configuration file."],["RELATED COMMANDS","**aunpack** _archive_\n> Shortcut for `atool -x`; also cleans up if the archive is untidy.\n\n**apack** _archive_ _files..._\n> Shortcut for `atool -a`.\n\n**als** _archive_\n> Shortcut for `atool -l`.\n\n**acat** _archive_ _file_\n> Shortcut for `atool -c`.\n\n**adiff** _archive1_ _archive2_\n> Shortcut for `atool -d`.\n\n**arepack** _old_ _new_\n> Convert one archive format to another in a single step."],["SUPPORTED FORMATS","- tar (plain, gzip, bzip2, xz, lzma, zstd, compress)\n- zip, jar, war\n- rar, 7z, lzh, arj, arc\n- ar, cpio, shar\n- deb, rpm (for listing/extraction via backend tools)"],["CONFIGURATION","**~/.atoolrc**\n> Per-user configuration file that sets defaults and format preferences.\n\n**/etc/atoolrc**\n> System-wide configuration file."],["CAVEATS","Requires the appropriate backend tools to be installed (tar, unzip, unrar, 7z, zstd, ...); atool only orchestrates them. Archive format detection is primarily based on file extension, so **-F** may be needed when extensions are missing or misleading."],["HISTORY","**atool** is written in Perl by **Oskar Liljeblad** and is distributed by the GNU project (nongnu.org/atool). It was created to provide a unified front end for the large number of archive formats found on Unix systems."],["INSTALL","```dnf: sudo dnf install atool```\n\n```pacman: sudo pacman -S atool```\n\n```zypper: sudo zypper install atool```\n\n```brew: brew install atool```\n\n```nix: nix profile install nixpkgs#atool```"],["SEE ALSO","[tar](/man/tar)(1), [unzip](/man/unzip)(1), [7z](/man/7z)(1), [unrar](/man/unrar)(1), [unar](/man/unar)(1), [zip](/man/zip)(1), [gzip](/man/gzip)(1), [bzip2](/man/bzip2)(1), [xz](/man/xz)(1), [zstd](/man/zstd)(1)"]],"e":["tar","unzip","7z","unrar","unar","zip","gzip","bzip2","xz","zstd"],"x":"atool -x [archive.tar.gz]"},"blastx":{"n":"blastx","t":"Translated nucleotide-to-protein sequence search","s":[["TAGLINE","Translated nucleotide-to-protein sequence search"],["TLDR","**Search translated nucleotide** against protein database\n\n```blastx -query [sequences.fasta] -db [nr] -out [results.txt]```\n\n**Output in tabular format**\n\n```blastx -query [sequences.fasta] -db [swissprot] -outfmt 6 -out [results.tsv]```\n\n**Output in XML format**\n\n```blastx -query [sequences.fasta] -db [nr] -outfmt 5 -out [results.xml]```\n\n**Set E-value threshold**\n\n```blastx -query [sequences.fasta] -db [nr] -evalue [0.001] -out [results.txt]```\n\n**Limit number of alignments**\n\n```blastx -query [sequences.fasta] -db [nr] -max_target_seqs [10] -out [results.txt]```\n\n**Use multiple threads**\n\n```blastx -query [sequences.fasta] -db [nr] -num_threads [8] -out [results.txt]```\n\n**Show help**\n\n```blastx -help```"],["SYNOPSIS","**blastx** [_-query file_] [_-db database_] [_-out file_] [_options_]"],["DESCRIPTION","**blastx** translates a nucleotide query sequence in all six reading frames and searches it against a protein database. It is part of the NCBI BLAST+ suite for sequence similarity searching.\n\nThis tool is useful for identifying protein homologs of nucleotide sequences, annotating genes, and finding coding regions in DNA sequences."],["PARAMETERS","**-query** _file_\n> Input nucleotide sequence file (FASTA format)\n\n**-db** _name_\n> Protein database to search against (e.g., nr, swissprot)\n\n**-out** _file_\n> Output file for results\n\n**-outfmt** _format_\n> Output format: 0=pairwise, 5=XML, 6=tabular, 7=tabular with comments\n\n**-evalue** _value_\n> E-value threshold for reporting hits (default: 10)\n\n**-max_target_seqs** _n_\n> Maximum number of aligned sequences to keep\n\n**-num_threads** _n_\n> Number of threads/CPUs to use\n\n**-word_size** _n_\n> Word size for initial match\n\n**-matrix** _name_\n> Scoring matrix (e.g., BLOSUM62, PAM250)\n\n**-query_gencode** _n_\n> Genetic code for query translation (default: 1).\n\n**-seg** _method_\n> Filter query sequence for low-complexity regions (default: 12 2.2 2.5).\n\n**-task** _name_\n> Task to execute: blastx (default) or blastx-fast.\n\n**-remote**\n> Execute search remotely on NCBI servers."],["CAVEATS","Requires pre-formatted BLAST databases; use **makeblastdb** to create them. Large databases like nr require significant disk space and memory. Translation in six frames increases computational time compared to blastn."],["HISTORY","BLAST (Basic Local Alignment Search Tool) was originally developed at NCBI by **Stephen Altschul** and colleagues in **1990**. The BLAST+ suite, including blastx, was released in **2009** with improved performance and new features."],["INSTALL","```apt: sudo apt install ncbi-blast+```"],["SEE ALSO","[blastn](/man/blastn)(1), [blastp](/man/blastp)(1), [tblastn](/man/tblastn)(1), [tblastx](/man/tblastx)(1), [makeblastdb](/man/makeblastdb)(1), [blastdbcmd](/man/blastdbcmd)(1)"]],"e":["blastn","blastp","tblastn","tblastx","makeblastdb","blastdbcmd"],"x":"blastx -query [sequences.fasta] -db [nr] -out [results.txt]"},"byobu":{"n":"byobu","t":"Terminal multiplexer with enhanced status bar and keybindings","s":[["TAGLINE","Terminal multiplexer with enhanced status bar and keybindings"],["TLDR","**Start** byobu session\n\n```byobu```\n\n**Create** new window\n\n```Ctrl-A c```\n\n**Switch** windows\n\n```Ctrl-A n```\n\n**Detach** session\n\n```Ctrl-A d```\n\n**List** sessions\n\n```byobu-select-session```"],["SYNOPSIS","**byobu** [_options_]"],["DESCRIPTION","**byobu** is a text-based window manager and terminal multiplexer. It's a wrapper around tmux or screen that adds a status bar, keybindings, and configuration for easier terminal session management.\n\nThe tool is popular on Ubuntu and provides an accessible interface for managing multiple terminal sessions."],["PARAMETERS","**-S** _name_\n> Create named session\n\n**-ls**\n> List sessions\n\n**-r**\n> Reattach to session\n\n**-d**\n> Detach session"],["KEYBINDINGS","**F2** - New window\n**F3/F4** - Previous/next window\n**F6** - Detach\n**F7** - Scrollback mode\n**F8** - Rename window\n**F9** - Configuration menu\n**Shift-F2** - Horizontal split\n**Ctrl-F2** - Vertical split\n\n**Ctrl-A** prefix:\n- **c** - Create window\n- **n/p** - Next/previous window\n- **d** - Detach\n- **[** - Copy mode"],["FEATURES","- Pre-configured tmux/screen\n- Informative status bar\n- F-key bindings\n- Session management\n- UTF-8 support\n- Color schemes\n- Notification system"],["STATUS BAR","Shows:\n- Hostname\n- Load average\n- Memory usage\n- CPU usage\n- Disk space\n- Network activity\n- Date/time"],["WORKFLOW","```bash"],["Start session","byobu"],["Reattach","byobu attach\n```"],["CAVEATS","F-keys may conflict with terminal emulator. Backend (tmux vs screen) affects features. Status bar uses some resources. Learning curve for advanced features. Default keybindings differ from plain tmux."],["HISTORY","**Byobu** was created by Dustin Kirkland around **2008** to provide a more user-friendly interface to GNU screen, later adding tmux support."],["CONFIGURATION","**~/.byobu/**\n> User configuration directory containing keybindings, status bar settings, and profile preferences\n\n**~/.byobu/keybindings**\n> Custom keybinding definitions\n\n**~/.byobu/status**\n> Status bar configuration (which elements to display)\n\n**~/.byobu/statusrc**\n> Status bar refresh rate and display options\n\n**~/.byobu/.tmux.conf**\n> Byobu-specific tmux configuration overrides"],["INSTALL","```dnf: sudo dnf install byobu```\n\n```apk: sudo apk add byobu```\n\n```zypper: sudo zypper install byobu```\n\n```brew: brew install byobu```\n\n```nix: nix profile install nixpkgs#byobu```"],["SEE ALSO","[tmux](/man/tmux)(1), [screen](/man/screen)(1), [zellij](/man/zellij)(1)"],["RESOURCES","```[Source code](https://github.com/dustinkirkland/byobu)```\n\n```[Homepage](https://www.byobu.org)```"]],"e":["tmux","screen","zellij"],"x":"byobu"},"cargo-set-version":{"n":"cargo-set-version","t":"Change a Rust package version in Cargo.toml","s":[["TAGLINE","Change a Rust package version in Cargo.toml"],["TLDR","**Set** an exact version\n\n```cargo set-version [1.0.0]```\n\n**Bump major** version\n\n```cargo set-version --bump major```\n\n**Bump minor** version\n\n```cargo set-version --bump minor```\n\n**Bump patch** version\n\n```cargo set-version --bump patch```\n\n**Apply to whole workspace**\n\n```cargo set-version --workspace --bump patch```\n\n**Dry-run**\n\n```cargo set-version -n --bump minor```"],["SYNOPSIS","**cargo set-version** [*options*] [*TARGET*]"],["DESCRIPTION","**cargo set-version** (from **cargo-edit**) changes the **version** field in a local **Cargo.toml**. You can set an absolute version or bump major/minor/patch. Workspace-wide updates are supported.\n\nInstall with **cargo install cargo-edit**."],["PARAMETERS","*TARGET*\n\n> Absolute version to set (for example **1.2.3**), when not using **--bump**.\n\n**--bump** major|minor|patch|...\n\n> Increment the corresponding semver component.\n\n**-m**, **--metadata** *METADATA*\n\n> Set the version metadata field (for example build metadata).\n\n**-p**, **--package** *PKGID*\n\n> Select a package in a workspace.\n\n**--workspace**\n\n> Modify all packages in the workspace.\n\n**--exclude** *EXCLUDE*\n\n> Skip crates when using workspace mode.\n\n**-n**, **--dry-run**\n\n> Print changes without writing files.\n\n**--manifest-path** *PATH*\n\n> Path to the manifest to edit.\n\n**--offline**\n\n> Run without network access.\n\n**--locked**\n\n> Require Cargo.toml to be up to date with the lockfile as configured.\n\nSee **cargo set-version --help** for the complete flag set."],["CAVEATS","Does not publish crates; it only edits manifests. Coordinate version bumps with tags and changelog process separately. Workspace bumps can touch many crates at once; use **--dry-run** first."],["INSTALL","```pacman: sudo pacman -S cargo-edit```\n\n```apk: sudo apk add cargo-edit```\n\n```brew: brew install cargo-edit```\n\n```nix: nix profile install nixpkgs#cargo-edit```"],["SEE ALSO","[cargo-upgrade](/man/cargo-upgrade)(1), [cargo-add](/man/cargo-add)(1), [cargo](/man/cargo)(1)"],["RESOURCES","```[Source code](https://github.com/killercup/cargo-edit)```"]],"e":["cargo-upgrade","cargo-add","cargo"],"x":"cargo set-version [1.0.0]"},"cbxy-reader":{"n":"cbxy-reader","t":"Browser comic reader with optional guided panel view","s":[["TAGLINE","Browser comic reader with optional guided panel view"],["TLDR","**Open a comic** in the browser\n\n```cbxy-reader [path/to/book.cbz]```\n\n**Bind** to a specific host and port\n\n```cbxy-reader [path/to/book.cbz] --host [127.0.0.1] --port [8765]```\n\n**Do not open** a browser window automatically\n\n```cbxy-reader [path/to/book.cbz] --no-open```\n\n**Install** the cbxy tools\n\n```uv tool install cbxy```"],["SYNOPSIS","**cbxy-reader** [_options_] _comic_"],["DESCRIPTION","**cbxy-reader** starts a local web server and opens a comic in the browser. If a sibling **.cbxy** sidecar is present (panel geometry produced by **cbxy-detect** or **cbxy-editor**), the reader supports guided panel-by-panel navigation; without a sidecar it shows full pages only.\n\nInput can be a **.cbz**, **.cbr**, an image folder, or a single page image. Default bind address is **127.0.0.1** on port **8765**. Part of the **cbxy** Python package."],["PARAMETERS","_comic_\n> Path to a **.cbz**, **.cbr**, image folder, or single page image.\n\n**--host** _address_\n> Bind address (default: **127.0.0.1**).\n\n**--port** _port_\n> Listen port (default: **8765**).\n\n**--no-open**\n> Do not open a browser window automatically."],["CAVEATS","Guided panel view requires a matching **.cbxy** next to the comic (or discoverable by the tool's conventions). Binding to non-localhost interfaces exposes the reader on the network—keep the default loopback bind unless you intend that. The process must stay running while you read."],["HISTORY","**cbxy** is a Python reference implementation of a comic panel geometry sidecar format by **ngafar**, published under the MIT license with **cbxy-detect**, **cbxy-reader**, and **cbxy-editor**."],["SEE ALSO","[cbxy-detect](/man/cbxy-detect)(1), [cbxy-editor](/man/cbxy-editor)(1), [pip](/man/pip)(1), [uv](/man/uv)(1)"],["RESOURCES","```[Source code](https://github.com/ngafar/cbxy)```\n\n```[Documentation](https://github.com/ngafar/cbxy#cbxy-reader)```"]],"e":["cbxy-detect","cbxy-editor","pip","uv"],"x":"cbxy-reader [path/to/book.cbz]"},"ccache":{"n":"ccache","t":"compiler cache for faster recompilation","s":[["TAGLINE","compiler cache for faster recompilation"],["TLDR","**Show cache statistics**\n\n```ccache -s```\n\n**Show verbose cache statistics** including hit rates\n\n```ccache -sv```\n\n**Clear the cache**\n\n```ccache -C```\n\n**Set maximum cache size**\n\n```ccache -M [5G]```\n\n**Show current configuration**\n\n```ccache -p```\n\n**Zero statistics counters**\n\n```ccache -z```\n\n**Run compiler through ccache**\n\n```ccache [gcc] -c [file.c]```\n\n**Set a configuration value**\n\n```ccache -o [max_size=10G]```\n\n**Show the cache directory path**\n\n```ccache -k cache_dir```"],["SYNOPSIS","**ccache** [_options_]\n\n**ccache** _compiler_ [_compiler_options_]"],["DESCRIPTION","**ccache** is a compiler cache that speeds up recompilation by caching previous compilations. When the same compilation is detected, it returns the cached result instead of recompiling.\n\nSupports GCC, Clang, MSVC and similar compilers for C, C++, Objective-C, CUDA, and assembler."],["PARAMETERS","**-s**, **--show-stats**\n> Show cache statistics summary.\n\n**-sv**\n> Show verbose statistics including hit/miss details.\n\n**-z**, **--zero-stats**\n> Zero statistics counters.\n\n**-C**, **--clear**\n> Clear entire cache.\n\n**-M**, **--max-size** _size_\n> Set maximum cache size (e.g., 5G, 500M).\n\n**-F**, **--max-files** _count_\n> Set maximum number of files in the cache.\n\n**-p**, **--show-config**\n> Show current configuration with origins.\n\n**-k**, **--get-config** _key_\n> Get a single configuration value.\n\n**-o**, **--set-config** _key=value_\n> Set a configuration value persistently.\n\n**-d**, **--directory** _path_\n> Operate on specified cache directory instead of the default.\n\n**--cleanup**\n> Clean up cache to stay within size limit.\n\n**--evict-older-than** _age_\n> Remove files older than given age (e.g., 30d, 24h).\n\n**-V**, **--version**\n> Show version."],["SETUP","**Symlink method:**\n```\nln -s /usr/bin/ccache /usr/local/bin/gcc\nln -s /usr/bin/ccache /usr/local/bin/g++\n```\n\n**Environment variable:**\n```\nexport CC=\"ccache gcc\"\nexport CXX=\"ccache g++\"\n```"],["CONFIGURATION","**~/.config/ccache/ccache.conf**\n> Primary configuration file controlling cache size, compression, compiler settings, and remote storage.\n\n**/etc/ccache.conf**\n> System-wide configuration defaults."],["ENVIRONMENT","**CCACHE_DIR**\n> Path to the cache directory (default: ~/.cache/ccache or ~/.ccache).\n\n**CCACHE_CONFIGPATH**\n> Path to the configuration file.\n\n**CCACHE_MAXSIZE**\n> Maximum cache size (overrides config file).\n\n**CCACHE_DISABLE** / **CCACHE_NODISABLE**\n> Disable or re-enable ccache."],["REMOTE CACHING","Supports remote caching via HTTP, Redis, or NFS for sharing across build machines."],["CAVEATS","Only caches single-file compilations. Multi-file compilation and linking fall back to the real compiler. Produces identical output to direct compilation. Precompiled headers require special configuration."],["INSTALL","```apt: sudo apt install ccache```\n\n```dnf: sudo dnf install ccache```\n\n```pacman: sudo pacman -S ccache```\n\n```apk: sudo apk add ccache```\n\n```zypper: sudo zypper install ccache```\n\n```brew: brew install ccache```\n\n```nix: nix profile install nixpkgs#ccache```"],["SEE ALSO","[gcc](/man/gcc)(1), [clang](/man/clang)(1), [make](/man/make)(1), [distcc](/man/distcc)(1), [sccache](/man/sccache)(1), [cmake](/man/cmake)(1)"]],"e":["gcc","clang","make","distcc","sccache","cmake"],"x":"ccache -s"},"codecov":{"n":"codecov","t":"Code coverage report uploader for the Codecov service","s":[["TAGLINE","Code coverage report uploader for the Codecov service"],["TLDR","**Upload coverage report with token**\n\n```codecovcli upload-process -t [token]```\n\n**Upload a specific coverage file**\n\n```codecovcli do-upload -t [token] -f [coverage.xml]```\n\n**Upload with coverage flags**\n\n```codecovcli do-upload -t [token] -F [unittests]```\n\n**Upload for a specific commit SHA**\n\n```codecovcli do-upload -t [token] -C [commit_sha]```\n\n**Upload with verbose output**\n\n```codecovcli -v upload-process -t [token]```\n\n**Create commit and upload in one step**\n\n```codecovcli upload-process -t [token] -r [owner/repo] --git-service [github]```"],["SYNOPSIS","**codecovcli** [_global-options_] _command_ [_options_]"],["PARAMETERS","**-t** _TOKEN_\n> Repository upload token. Can also be set via CODECOV_TOKEN environment variable.\n\n**-f**, **--file** _FILE_\n> Specific coverage report file to upload.\n\n**-F**, **--flag** _NAME_\n> Flag to group coverage (e.g., unittests, integration). May be specified multiple times.\n\n**-C**, **--sha** _SHA_\n> Commit SHA to associate with the upload.\n\n**-B**, **--branch** _NAME_\n> Branch name for the upload.\n\n**-r**, **--slug** _OWNER/REPO_\n> Repository slug (owner/repo).\n\n**-n**, **--name** _NAME_\n> Custom upload name.\n\n**--git-service** _SERVICE_\n> Git hosting service (github, gitlab, bitbucket, github_enterprise, gitlab_enterprise, bitbucket_server).\n\n**-v**, **--verbose**\n> Enable verbose logging.\n\n**-Z**, **--fail-on-error**\n> Exit with non-zero status on upload failure.\n\n**-s**, **--dir** _DIR_\n> Search for coverage files in a specific directory.\n\n**--enterprise-url** _URL_\n> Upload host for Codecov Self-Hosted instances.\n\n**--disable-search**\n> Disable automatic coverage file discovery."],["DESCRIPTION","**codecovcli** is the command-line interface for the Codecov code coverage service. It uploads coverage reports generated by testing frameworks and provides insights through dashboards and pull request comments.\n\nThe CLI automatically detects CI environments (GitHub Actions, GitLab CI, Jenkins, etc.) and extracts commit, branch, and build information. It searches for coverage files in common formats (lcov, cobertura, jacoco, etc.) and uploads them.\n\nKey commands include **upload-process** (a wrapper that creates a commit, creates a report, and uploads in one step), **do-upload** (upload only), **create-commit**, **create-report**, and **send-notifications**."],["CAVEATS","Requires a Codecov account and repository token (or tokenless upload for public repos). Coverage format must be supported (lcov, cobertura, clover, etc.). Private repositories require authentication. The older Bash uploader script has been deprecated in favor of this CLI."],["HISTORY","Codecov was founded in **2014** by **Steve Peak** and **Eli Hooten** to provide code coverage analytics for development teams. In **2022**, Codecov was acquired by **Sentry**. The original Bash uploader was replaced by the Python-based **codecovcli** for improved security and functionality."],["SEE ALSO","[lcov](/man/lcov)(1), [gcov](/man/gcov)(1)"]],"e":["lcov","gcov"],"x":"codecovcli upload-process -t [token]"},"conda-deactivate":{"n":"conda-deactivate","t":"deactivate the current conda environment","s":[["TAGLINE","deactivate the current conda environment"],["TLDR","**Deactivate the current environment**\n\n```conda deactivate```"],["SYNOPSIS","**conda** **deactivate**"],["DESCRIPTION","**conda deactivate** exits the currently active conda environment, reversing the shell modifications made by conda activate. This restores the PATH and environment variables to their previous state, either returning to the base conda environment or, if environments were nested through multiple activation calls, returning to the previously active environment.\n\nThe deactivation process removes the current environment's bin directory from PATH, unsets environment-specific variables like CONDA_PREFIX, and executes any deactivation scripts that packages may have installed. The shell prompt is typically updated to remove the environment name indicator, providing visual confirmation that the environment has been deactivated.\n\nUnlike some environment management systems that require explicit cleanup, conda deactivate is safe to call at any time and handles the state management internally. If no environment is currently active beyond the base environment, deactivating simply returns to a non-conda shell state. Conda supports nested environments, so multiple activate calls can be unwound with corresponding deactivate calls, though this is an advanced usage pattern."],["INSTALL","```dnf: sudo dnf install conda```\n\n```nix: nix profile install nixpkgs#conda```"],["SEE ALSO","[conda](/man/conda)(1), [conda-activate](/man/conda-activate)(1), [conda-create](/man/conda-create)(1), [conda-env](/man/conda-env)(1)"]],"e":["conda","conda-activate","conda-create","conda-env"],"x":"conda deactivate"},"conda-init":{"n":"conda-init","t":"shell integration setup","s":[["TAGLINE","shell integration setup"],["TLDR","**Initialize conda** for the current shell\n\n```conda init```\n\n**Initialize for specific shell**\n\n```conda init [bash|zsh|fish|powershell]```\n\n**Initialize for all shells**\n\n```conda init --all```\n\n**Reverse initialization**\n\n```conda init --reverse```"],["SYNOPSIS","**conda** **init** [_options_] [_shells..._]"],["PARAMETERS","**--all**\n> Initialize for all available shells.\n\n**--reverse**\n> Undo shell initialization.\n\n**-d**, **--dry-run**\n> Show what would be done without making changes."],["CONFIGURATION","**~/.condarc**\n> User-level conda configuration for channels, package settings, and solver options.\n\n**/opt/conda/.condarc**\n> System-wide conda configuration."],["DESCRIPTION","**conda init** sets up shell integration for conda, modifying shell configuration files to enable the **conda activate** command and automatic base environment activation.\n\nThis must be run once after conda installation for full functionality."],["CAVEATS","Requires restarting the shell or sourcing the configuration file after running."],["INSTALL","```dnf: sudo dnf install conda```\n\n```nix: nix profile install nixpkgs#conda```"],["SEE ALSO","[conda](/man/conda)(1), [conda-activate](/man/conda-activate)(1)"],["RESOURCES","```[Source code](https://github.com/conda/conda)```\n\n```[Documentation](https://docs.conda.io/projects/conda/en/stable/commands/init.html)```"]],"e":["conda","conda-activate"],"x":"conda init"},"crl2pkcs7.1s":{"n":"crl2pkcs7.1s","t":"CRL to PKCS#7 format converter","s":[["TAGLINE","CRL to PKCS#7 format converter"],["TLDR","**Create PKCS#7 from CRL** and certificates\n\n```openssl crl2pkcs7 -in [crl.pem] -certfile [cert.pem] -out [result.p7b]```\n\n**Create PKCS#7 from CRL** only (no certificates)\n\n```openssl crl2pkcs7 -in [crl.pem] -nocrl -out [result.p7b]```\n\n**Create PKCS#7 from multiple** CRLs\n\n```openssl crl2pkcs7 -in [crl1.pem] -in [crl2.pem] -out [result.p7b]```\n\n**Output in DER format**\n\n```openssl crl2pkcs7 -in [crl.pem] -outform DER -out [result.p7b]```\n\n**Include certificate chain**\n\n```openssl crl2pkcs7 -nocrl -certfile [chain.pem] -out [certs.p7b]```"],["SYNOPSIS","**openssl crl2pkcs7** [_options_] [_-in file_] [_-out file_]"],["PARAMETERS","**-in** _FILE_\n> Input CRL file (can be specified multiple times).\n\n**-out** _FILE_\n> Output PKCS#7 file.\n\n**-certfile** _FILE_\n> File containing certificates to include.\n\n**-nocrl**\n> Don't include the CRL in the output (certificates only).\n\n**-inform** _FORMAT_\n> Input CRL format: PEM or DER.\n\n**-outform** _FORMAT_\n> Output PKCS#7 format: PEM or DER."],["DESCRIPTION","**openssl crl2pkcs7** converts Certificate Revocation Lists (CRLs) and X.509 certificates into PKCS#7 format. PKCS#7 is a standard format for storing cryptographic data, often used for certificate bundles and signed data.\n\nThe command is useful for creating PKCS#7 structures that combine CRLs with their associated CA certificates. This format is commonly required by certain applications and protocols for distributing revocation information along with certificate chains.\n\nThe **-nocrl** option allows creating PKCS#7 files containing only certificates, which is useful for distributing certificate bundles in a widely-supported format."],["CAVEATS","The PKCS#7 output is a \"degenerate\" form containing only certificates and/or CRLs, with no signed content. Some applications expect the .p7b or .p7c extension for these files. The certificates in the output are not validated; any PEM certificates in the certfile are included."],["HISTORY","PKCS#7 was defined by RSA Security as part of the Public-Key Cryptography Standards. OpenSSL's crl2pkcs7 command provides conversion between the X.509 CRL format and PKCS#7, enabling interoperability with systems that use the PKCS#7 container format."],["SEE ALSO","[openssl](/man/openssl)(1), [openssl-crl](/man/openssl-crl)(1), [openssl-x509](/man/openssl-x509)(1), [x509](/man/x509)(1)"]],"e":["openssl","openssl-crl","openssl-x509","x509"],"x":"openssl crl2pkcs7 -in [crl.pem] -certfile [cert.pem] -out [result.p7b]"},"dari-docs":{"n":"dari-docs","t":"Test documentation clarity with simulated developer agents","s":[["TAGLINE","Test documentation clarity with simulated developer agents"],["TLDR","**Check** local docs against a developer task\n\n```dari-docs check . --managed --task \"[Install the SDK and make a first API call]\"```\n\n**Optimize** docs and download proposed edits\n\n```dari-docs optimize . --managed --wait --task \"[Install the SDK and make a first API call]\"```\n\n**Check** a public docs URL instead of a local directory\n\n```dari-docs check [https://docs.example.com] --managed --task \"[Set up auth]\"```\n\n**Authenticate** with the hosted dari.dev service\n\n```dari-docs auth login```\n\n**Wait** for a managed run to complete inside the same invocation\n\n```dari-docs check . --managed --wait --task \"[...]\"```"],["SYNOPSIS","**dari-docs** _command_ [_path_|_url_] [_options_]\n\nCommands: **check**, **optimize**, **auth**"],["PARAMETERS","**check** _PATH_|_URL_\n> Run tester agents against the docs at _PATH_ (local directory) or _URL_ (public docs site) and report where they got stuck.\n\n**optimize** _PATH_|_URL_\n> Run tester agents and then editor agents; download proposed doc edits into **.dari-docs/updated/** without touching the repo.\n\n**auth login** / **auth logout**\n> Authenticate (or sign out) against the hosted dari.dev Docs service.\n\n**--task** _STRING_\n> The developer task the agents should attempt using only the docs. Repeat the flag for multiple tasks.\n\n**--managed**\n> Submit the run to hosted Dari Docs agents (default mode). Without it, the CLI expects a **self-managed** setup driven by **dari.yml**.\n\n**--wait**\n> Block until the run finishes; print the report and (for **optimize**) download proposed edits.\n\n**--run-id** _ID_\n> Resume waiting on or fetch results for a previously submitted run.\n\n**--help**\n> Show usage for the CLI or a subcommand."],["DESCRIPTION","**dari-docs** is a CLI for measuring whether technical documentation is clear enough for an AI agent (and, by proxy, a new developer) to complete real tasks unaided. It packages a local docs directory (or accepts a public docs URL) and submits it along with one or more **--task** descriptions to hosted tester agents. The agents read only the provided docs and attempt the task, reporting the exact points where they got blocked, misled, or had to guess.\n\n**check** stops at that diagnostic report. **optimize** additionally runs editor agents that propose concrete documentation edits to fix the issues, downloading them into **.dari-docs/updated/** for human review before any change lands in the repo.\n\nThe default **--managed** mode uses Dari's hosted tester and editor agents and is the fastest way to get value. **Self-managed** mode (configured through a **dari.yml** file) lets teams customize agent prompts, skills, setup scripts, and execution environment within their own dari.dev organization."],["CONFIGURATION","**dari.yml**\n> Project-level configuration for self-managed runs. Defines tester/editor agent prompts, skills, setup commands, and the docs surface to expose. Only read when **--managed** is not set.\n\n**.dari-docs/**\n> Created by the CLI inside the target directory. **updated/** holds proposed edits from **optimize**; review and copy into the repo manually.\n\n**dari.dev account**\n> Both managed and self-managed modes require authentication via **dari-docs auth login**, which stores a token in the user's config directory."],["CAVEATS","Runs are billed against a dari.dev account and require network access; **--managed** sends the bundled docs to Dari's hosted agents, so private documentation may need self-managed mode or a private deployment. Proposed edits are agent-generated suggestions and should be reviewed for factual accuracy before merging. Public-URL inputs are fetched by the agents themselves, so paywalled or auth-gated docs cannot be evaluated that way."],["HISTORY","**dari-docs** is developed by **mupt-ai** and lives at **github.com/mupt-ai/dari-docs**, part of the broader **dari.dev** platform for agent-driven documentation quality. It launched as the agent-era successor to traditional docs-linting tools, shifting the measure of quality from style conformance to task completability."],["SEE ALSO","[vale](/man/vale)(1), [markdownlint](/man/markdownlint)(1), [alex](/man/alex)(1)"]],"e":["vale","markdownlint","alex"],"x":"dari-docs check . --managed --task \"[Install the SDK and make a first API call]\""},"deluge-console":{"n":"deluge-console","t":"Text-based console interface for the Deluge BitTorrent client","s":[["TAGLINE","Text-based console interface for the Deluge BitTorrent client"],["TLDR","**Start interactive** console\n\n```deluge-console```\n\n**Connect to daemon** and list torrents\n\n```deluge-console \"connect; info\"```\n\n**Add a torrent** file or magnet link\n\n```deluge-console \"add [file.torrent]\"```\n\n**Pause a torrent**\n\n```deluge-console \"pause [torrent_id]\"```\n\n**Resume a torrent**\n\n```deluge-console \"resume [torrent_id]\"```\n\n**Remove a torrent**\n\n```deluge-console \"rm [torrent_id]\"```\n\n**Show torrent details**\n\n```deluge-console \"info -v [torrent_id]\"```\n\n**Run multiple commands** with semicolon separator\n\n```deluge-console \"connect [host:port]; add [file.torrent]; info\"```"],["SYNOPSIS","**deluge-console** [_options_] [_command_]"],["PARAMETERS","_COMMAND_\n> Console command to execute. If omitted, starts interactive curses mode. Multiple commands can be separated with semicolons.\n\n**connect** [_HOST_:_PORT_]\n> Connect to a Deluge daemon.\n\n**info** [**-v**]\n> Show torrent information. Use **-v** for verbose details.\n\n**add** _TORRENT_\n> Add a torrent file or magnet link.\n\n**pause** _ID_\n> Pause a torrent.\n\n**resume** _ID_\n> Resume a paused torrent.\n\n**rm** _ID_\n> Remove a torrent.\n\n**del** _ID_\n> Delete a torrent (alias for rm).\n\n**move** _ID_ _PATH_\n> Move a torrent's download location.\n\n**recheck** _ID_\n> Force a recheck of torrent data.\n\n**reannounce** _ID_\n> Force a reannounce to trackers.\n\n**config**\n> Show or set daemon configuration.\n\n**status**\n> Show session status and transfer rates.\n\n**halt**\n> Shut down the connected daemon.\n\n**--help**\n> Display help information."],["DESCRIPTION","**deluge-console** is the text-based console interface for the Deluge BitTorrent client. Deluge uses a client/server model, with **deluged** as the daemon process and **deluge-console** as the terminal front-end.\n\nIn interactive mode, it presents a curses-based navigable interface for managing torrents with keyboard shortcuts. In command mode, it accepts commands as arguments for non-interactive operation, useful for scripts and automation. Commands can be passed directly on the command line and chained with semicolons.\n\nThe console connects to the Deluge daemon either locally or remotely, providing the same functionality as the GTK client through a terminal interface."],["CAVEATS","Requires the **deluged** daemon to be running. Interactive mode needs a terminal with curses support. Remote connections require proper authentication configuration. Command syntax differs from the GTK interface."],["HISTORY","**deluge-console** was developed as part of the Deluge project to provide terminal access to the BitTorrent client. It addresses the need for headless server management and scripted torrent operations."],["INSTALL","```dnf: sudo dnf install deluge```\n\n```pacman: sudo pacman -S deluge```\n\n```apk: sudo apk add deluge```\n\n```zypper: sudo zypper install deluge```\n\n```nix: nix profile install nixpkgs#deluge```"],["SEE ALSO","[deluge](/man/deluge)(1), [deluged](/man/deluged)(1), [transmission-remote](/man/transmission-remote)(1)"]],"e":["deluge","deluged","transmission-remote"],"x":"deluge-console"},"dfc":{"n":"dfc","t":"disk usage display with color-coded graphs","s":[["TAGLINE","disk usage display with color-coded graphs"],["TLDR","**Display disk usage** with color-coded graph bars\n\n```dfc```\n\n**Show all filesystems** including pseudo and special\n\n```dfc -a```\n\n**Display in a specific unit** (K, M, G, T, P, E, Z, Y)\n\n```dfc -u [G]```\n\n**Show only specific filesystem types**\n\n```dfc -t [ext4,btrfs]```\n\n**Export output as CSV**\n\n```dfc -e csv```\n\n**Sort output** by filesystem name\n\n```dfc -q [name]```\n\n**Show used size** and mount options\n\n```dfc -d -o```\n\n**Display without colors**\n\n```dfc -c never```"],["SYNOPSIS","**dfc** [_-abdfhilmMnosTvwW_] [_-c when_] [_-e format_] [_-p fsname_] [_-q sortby_] [_-t fstype_] [_-u unit_]"],["PARAMETERS","**-a**\n> Show all filesystems (do not omit any).\n\n**-b**\n> Do not show the graph bar.\n\n**-c** _when_\n> Color mode: always, auto, or never.\n\n**-d**\n> Show used size.\n\n**-e** _format_\n> Export to format: csv, html, json, tex, or text.\n\n**-f**\n> Override auto-adjust behavior by forcing information to be displayed.\n\n**-h**\n> Display help.\n\n**-i**\n> Show information about inodes instead of disk space.\n\n**-l**\n> Show only local filesystems.\n\n**-m**\n> Use SI units (powers of 1000 instead of 1024).\n\n**-M**\n> Do not print \"mounted on\".\n\n**-n**\n> Do not print header.\n\n**-o**\n> Show mount options.\n\n**-p** _fsname_\n> Filter by filesystem name (prefix with - to exclude).\n\n**-q** _sortby_\n> Sort output by: name, type, or mount.\n\n**-s**\n> Sum the total usage.\n\n**-t** _fstype_\n> Filter by filesystem types (comma-separated, prefix with - to exclude).\n\n**-T**\n> Show filesystem type.\n\n**-u** _unit_\n> Force display unit (h, b, k, m, g, t, p, e, z, y).\n\n**-w**\n> Use a wider bar for the graph.\n\n**-W**\n> Wide path name (avoid truncation of mount points)."],["DESCRIPTION","**dfc** (disk free color) is an enhanced df command that displays filesystem disk space usage with color-coded bar graphs. It provides a more visual and readable alternative to the traditional df output.\n\nThe graph bars change color based on usage levels: typically green for low usage, yellow for moderate, and red for high usage. This visual representation makes it easy to quickly identify filesystems approaching capacity.\n\nOutput can be customized extensively through column selection, sorting, and filtering. The tool supports multiple export formats including CSV, JSON, and HTML for integration with other tools or reporting systems. Filesystem type filtering allows focusing on specific storage types.\n\nConfiguration options can be set in **~/.config/dfc/dfcrc** or **/etc/dfc.conf** for persistent preferences. Color thresholds and graph characters can be customized in the configuration file."],["CAVEATS","Colors require a terminal with color support. Very long mount point names may be truncated in default mode (use -W for full paths). Some virtual filesystems may show unexpected values. Graph width adapts to terminal width."],["HISTORY","**dfc** was created by Robin Hahling as a colorful, user-friendly replacement for the traditional df command. The project focuses on making disk space monitoring more visual and accessible. It's written in C and available in repositories of most major Linux distributions."],["INSTALL","```apt: sudo apt install dfc```\n\n```dnf: sudo dnf install dfc```\n\n```pacman: sudo pacman -S dfc```\n\n```apk: sudo apk add dfc```\n\n```zypper: sudo zypper install dfc```\n\n```brew: brew install dfc```\n\n```nix: nix profile install nixpkgs#dfc```"],["SEE ALSO","[df](/man/df)(1), [du](/man/du)(1), [ncdu](/man/ncdu)(1), [dust](/man/dust)(1), [duf](/man/duf)(1)"],["RESOURCES","```[Source code](https://github.com/rolinh/dfc)```"]],"e":["df","du","ncdu","dust","duf"],"x":"dfc"},"docker-diff":{"n":"docker-diff","t":"Inspect changes to files or directories on a container's filesystem","s":[["TAGLINE","Inspect changes to files or directories on a container's filesystem"],["TLDR","**Show filesystem changes in a container**\n\n```docker diff [container_name]```\n\n**Show changes using container ID**\n\n```docker diff [container_id]```"],["SYNOPSIS","**docker** **diff** _container_"],["OUTPUT FIELDS","**A**\n> A file or directory was added.\n\n**C**\n> A file or directory was changed (modified content or metadata).\n\n**D**\n> A file or directory was deleted."],["DESCRIPTION","**docker diff** inspects changes to files or directories on a container's filesystem since the container was created from its base image. The output categorizes each change with a single-character prefix: **A** for added, **C** for changed/modified, and **D** for deleted.\n\nThis command is useful for understanding what modifications have occurred within a running or stopped container, which helps in debugging, auditing changes, or determining what should be captured in a new image via **docker commit**. Only changes in the container's writable layer are reported; the underlying image layers remain unchanged.\n\nYou can reference the container by name or by full/shortened container ID."],["CAVEATS","The diff operation examines the entire container filesystem, so it may take time for containers with extensive modifications. No filtering options are available; the full diff is always shown."],["HISTORY","**docker diff** has been available since the early versions of Docker. It is an alias for **docker container diff**, which is the preferred form in newer Docker CLI versions."],["INSTALL","```apt: sudo apt install docker-cli```\n\n```dnf: sudo dnf install docker-cli```\n\n```pacman: sudo pacman -S docker```\n\n```apk: sudo apk add docker-cli```\n\n```zypper: sudo zypper install docker```\n\n```brew: brew install docker```\n\n```nix: nix profile install nixpkgs#docker```"],["SEE ALSO","[docker-container-diff](/man/docker-container-diff)(1), [docker-commit](/man/docker-commit)(1), [docker-inspect](/man/docker-inspect)(1), [docker-cp](/man/docker-cp)(1)"]],"e":["docker-container-diff","docker-commit","docker-inspect","docker-cp"],"x":"docker diff [container_name]"},"docker":{"n":"docker","t":"container platform for application deployment","s":[["TAGLINE","container platform for application deployment"],["TLDR","**Run** container\n\n```docker run [image]```\n\n**List** containers\n\n```docker ps```\n\n**Build** image\n\n```docker build -t [name] [.]```\n\n**Pull** image\n\n```docker pull [image]```\n\n**Stop** container\n\n```docker stop [container]```\n\n**Remove** container\n\n```docker rm [container]```\n\n**Execute** command in container\n\n```docker exec -it [container] [bash]```"],["SYNOPSIS","**docker** _command_ [_options_]"],["DESCRIPTION","**docker** is a platform for developing, shipping, and running applications in containers. Containers package applications with their dependencies into standardized units, ensuring consistency across different environments from development to production.\n\nDocker has revolutionized application deployment and development workflows by solving the \"it works on my machine\" problem. It uses OS-level virtualization to isolate applications, making them portable, efficient, and easy to scale. Containers share the host kernel but maintain isolated filesystems, processes, and networking.\n\nThe Docker ecosystem includes Docker Engine (the runtime), Docker Hub (image registry), and a comprehensive CLI for managing containers, images, networks, and volumes. Docker images are built in layers, making them efficient to distribute and update. The platform supports both single-container applications and complex multi-service architectures."],["PARAMETERS","**run** [_options_] _image_\n> Create and start container\n\n**ps** [**-a**]\n> List containers\n\n**build** **-t** _name_ _path_\n> Build image from Dockerfile\n\n**pull** _image_\n> Pull image from registry\n\n**push** _image_\n> Push image to registry\n\n**exec** _container_ _command_\n> Execute command in running container\n\n**logs** _container_\n> Fetch container logs\n\n**stop** _container_\n> Stop running container\n\n**start** _container_\n> Start stopped container\n\n**rm** _container_\n> Remove container\n\n**rmi** _image_\n> Remove image\n\n**images**\n> List images"],["RUN OPTIONS","**-d**, **--detach**\n> Run in background\n\n**-it**\n> Interactive with TTY\n\n**-p** _host_**:**_container_\n> Publish port\n\n**-v** _host_**:**_container_\n> Mount volume\n\n**--name** _name_\n> Assign name to container\n\n**-e** _var_**=**_value_\n> Set environment variable\n\n**--rm**\n> Automatically remove on exit\n\n**--network** _network_\n> Connect to network"],["WORKFLOW","```bash"],["Run container","docker run -d -p 80:80 --name webserver nginx"],["Interactive container","docker run -it ubuntu bash"],["With environment variables","docker run -e DATABASE_URL=postgres://... myapp"],["With volume mount","docker run -v $(pwd):/app -w /app node npm install"],["Build image","docker build -t myapp:latest ."],["Push to registry","docker tag myapp:latest user/myapp:latest\ndocker push user/myapp:latest"],["View logs","docker logs -f container_name"],["Execute command","docker exec -it container_name bash"],["Clean up","docker stop container_name\ndocker rm container_name\ndocker rmi image_name\n```"],["DOCKERFILE","```dockerfile\nFROM node:18\nWORKDIR /app\nCOPY package*.json ./\nRUN npm install\nCOPY . .\nEXPOSE 3000\nCMD [\"node\", \"server.js\"]\n```"],["COMMON OPERATIONS","```bash"],["List running containers","docker ps"],["List all containers","docker ps -a"],["List images","docker images"],["Remove all stopped containers","docker container prune"],["Remove unused images","docker image prune"],["System-wide cleanup","docker system prune -a"],["Inspect container","docker inspect container_name"],["Container stats","docker stats\n```"],["CONFIGURATION","**/etc/docker/daemon.json**\n> Docker daemon configuration including storage drivers, logging, registry mirrors, and resource limits.\n\n**~/.docker/config.json**\n> User-level configuration for registry authentication, default command settings, and CLI preferences."],["CAVEATS","Requires Docker daemon running. Root or docker group membership needed. Networking can be complex. Volume permissions issues common. Images can be large. Security considerations for production. Different behavior on different operating systems."],["HISTORY","**Docker** was created by Solomon Hykes at dotCloud in **2013**, becoming open-source and revolutionizing application containerization."],["INSTALL","```apt: sudo apt install docker-cli```\n\n```dnf: sudo dnf install docker-cli```\n\n```pacman: sudo pacman -S docker```\n\n```apk: sudo apk add docker-cli```\n\n```zypper: sudo zypper install docker```\n\n```brew: brew install docker```\n\n```nix: nix profile install nixpkgs#docker```"],["SEE ALSO","[docker-compose](/man/docker-compose)(1), [podman](/man/podman)(1), [kubectl](/man/kubectl)(1)"],["RESOURCES","```[Source code](https://github.com/docker/cli)```\n\n```[Homepage](https://www.docker.com/)```\n\n```[Documentation](https://docs.docker.com/reference/cli/docker/)```"]],"e":["docker-compose","podman","kubectl"],"x":"docker run [image]"},"dstat":{"n":"dstat","t":"versatile resource statistics tool","s":[["TAGLINE","versatile resource statistics tool"],["TLDR","Display **CPU, disk, net, paging, system** statistics\n\n```dstat```\n\nDisplay statistics every **5 seconds, 4 times**\n\n```dstat 5 4```\n\nDisplay **CPU and memory** only\n\n```dstat -c -m```\n\n**List** available plugins\n\n```dstat --list```\n\nDisplay **top memory and CPU** processes\n\n```dstat --top-mem --top-cpu```\n\nDisplay **battery** information\n\n```dstat --battery --battery-remain```"],["SYNOPSIS","**dstat** [_options_] [_delay_] [_count_]"],["DESCRIPTION","**dstat** is a versatile system resource statistics generator that unifies the functionality of vmstat, iostat, netstat, and ifstat in a single tool. It provides real-time monitoring of CPU, memory, disk I/O, network, processes, and other system resources with colorful, easy-to-read output.\n\nThe tool's plugin architecture allows for extensible monitoring capabilities beyond the standard metrics. Users can display top CPU and memory consumers, battery information, and custom statistics through third-party plugins. Output updates at configurable intervals, making it suitable for both quick system checks and continuous monitoring.\n\ndstat's strength lies in its ability to correlate metrics across different subsystems in one view. For example, you can simultaneously monitor CPU usage, disk I/O, and network traffic to understand system bottlenecks.\n\nImportant note: dstat is deprecated and no longer maintained. The Python 2 codebase has not been updated for modern systems. Users should consider alternatives like dool (a maintained fork), or use the traditional sysstat tools (vmstat, iostat, sar) which continue to be actively maintained."],["PARAMETERS","**-c, --cpu**\n> CPU statistics\n\n**-m, --mem**\n> Memory statistics\n\n**-d, --disk**\n> Disk statistics\n\n**-n, --net**\n> Network statistics\n\n**--top-cpu**\n> Process using most CPU\n\n**--top-mem**\n> Process using most memory\n\n**--list**\n> List available plugins\n\n**-a, --all**\n> Equivalent to **-cdngy**, the default set\n\n**-f, --full**\n> Expand aggregated statistics into one column per CPU, disk, or interface\n\n**--output** _file_\n> Also write the statistics to a CSV file"],["CAVEATS","**dstat is deprecated and unmaintained.** It was written for Python 2, and while distributions carry patches to keep it importable under Python 3, it breaks on modern systems in ways nobody upstream is fixing. Some plugins fail outright, and a few crash on newer kernels.\n\nThe direct replacement is **dool**, a maintained Python 3 fork that keeps the same command-line interface, so `dool -cdngy` behaves like the dstat you remember. Otherwise, the sysstat suite (`vmstat`, `iostat`, `sar`, `pidstat`) covers the same ground and is actively maintained, at the cost of needing several commands where dstat used one.\n\nNote also that the first line of output is an average since boot rather than a measurement of the interval, which is a trap it shares with `vmstat`: discard it, or use a count of at least two."],["HISTORY","dstat was written by **Dag Wieers** in **2004**, and its pitch was straightforward: `vmstat`, `iostat`, `netstat`, and `ifstat` each showed you one part of the picture, in a different format, on a different schedule, and correlating them by eye across four terminals was miserable. dstat put them in one aligned, colour-coded, plugin-extensible table with a shared timeline, which turned out to be exactly what people wanted when hunting a bottleneck. Its dependence on Python 2 eventually outlived Python 2 itself, and the project was retired in favour of the **dool** fork."],["INSTALL","```apt: sudo apt install pcp```\n\n```dnf: sudo dnf install pcp```\n\n```pacman: sudo pacman -S pcp```\n\n```zypper: sudo zypper install pcp```\n\n```brew: brew install pcp```"],["SEE ALSO","[vmstat](/man/vmstat)(8), [iostat](/man/iostat)(1), [sar](/man/sar)(1), [pidstat](/man/pidstat)(1), [htop](/man/htop)(1)"],["RESOURCES","```[Source code](https://github.com/dagwieers/dstat)```"]],"e":["vmstat","iostat","sar","pidstat","htop"],"x":"dstat"},"efibootdump":{"n":"efibootdump","t":"UEFI boot entry decoder","s":[["TAGLINE","UEFI boot entry decoder"],["TLDR","Dump boot entries from **file**\n\n```efibootdump -f [/sys/firmware/efi/efivars/Boot0000-8be4df61-93ca-11d2-aa0d-00e098032b8c]```\n\nDump a boot entry by **variable name**\n\n```efibootdump [Boot0000]```"],["SYNOPSIS","**efibootdump** [**-f**|**--file**] _variable_|_file_..."],["DESCRIPTION","**efibootdump** decodes EFI load options such as Boot0000 or BootOrder and prints them in human-readable form: the description, attributes, and the device path of the loader.\n\nIt accepts either the name of a boot variable, which it reads from the running firmware, or a file containing the raw variable data when **-f** is given. This makes it useful both for inspecting the live boot configuration and for examining a variable dumped from another machine."],["PARAMETERS","_variable_|_file_\n> The boot variable name, or a file path when **-f** is used.\n\n**-f**, **--file**\n> Treat the arguments as files containing raw variable data rather than variable names."],["CAVEATS","Only works on UEFI systems, and reading live variables requires efivarfs to be mounted at /sys/firmware/efi/efivars. Part of the **efivar** package, not efibootmgr, so it may need to be installed separately. Root privileges are usually needed to read the raw variable files."],["INSTALL","```apt: sudo apt install efibootmgr```\n\n```dnf: sudo dnf install efibootmgr```\n\n```pacman: sudo pacman -S efibootmgr```\n\n```apk: sudo apk add efibootmgr```\n\n```zypper: sudo zypper install efibootmgr```\n\n```nix: nix profile install nixpkgs#efibootmgr```"],["SEE ALSO","[efibootmgr](/man/efibootmgr)(8), [efivar](/man/efivar)(1)"],["RESOURCES","```[Source code](https://github.com/rhboot/efivar)```"]],"e":["efibootmgr","efivar"],"x":"efibootdump -f [/sys/firmware/efi/efivars/Boot0000-8be4df61-93ca-11d2-aa0d-00e098032b8c]"},"enca":{"n":"enca","t":"character encoding detection and conversion","s":[["TAGLINE","character encoding detection and conversion"],["TLDR","**Detect encoding** of file\n\n```enca [file.txt]```\n\n**Detect with language** hint\n\n```enca -L [czech] [file.txt]```\n\n**Convert encoding**\n\n```enca -x [UTF-8] [file.txt]```\n\n**Detect and show** confidence\n\n```enca -d [file.txt]```\n\n**Process multiple files**\n\n```enca -L [russian] [*.txt]```"],["SYNOPSIS","**enca** [_options_] [_files_...]"],["PARAMETERS","_FILES_\n> Files to analyze.\n\n**-L** _LANGUAGE_\n> Hint language for detection.\n\n**-x** _ENCODING_\n> Convert to specified encoding.\n\n**-d**\n> Show detailed detection info.\n\n**-g**, **--guess**\n> Output best guess only.\n\n**-i**, **--info**\n> Show available encodings.\n\n**--help**\n> Display help information."],["DESCRIPTION","**enca** (Extremely Naive Charset Analyser) detects character encodings of text files using language-based statistical heuristics, and can convert files between encodings by piping through **iconv**. It is particularly strong on legacy 8-bit charsets used for Slavic and Central/Eastern European languages (ISO-8859-2/5, KOI8-R, CP1250/1251, Mazovia, T.61, ...), where simpler tools like **file -i** struggle.\n\nDetection works best with a language hint passed via **-L**; without it, **enca** falls back to a general profile and may return ambiguous matches. The output is one detected encoding per file by default, or extended information with **-d** or **-v**. Conversion is performed in place with **-x** _ENCODING_, which calls **iconv** under the hood; pair it with **--cstocs** for transliteration when the target charset lacks specific characters."],["CAVEATS","Detection is heuristic, not deterministic. Short files may be ambiguous. Works best with specific language hints. Some encodings indistinguishable."],["HISTORY","enca was developed for handling the encoding diversity in Central/Eastern European computing, where many incompatible character sets were historically used for the same languages."],["INSTALL","```apt: sudo apt install enca```\n\n```dnf: sudo dnf install enca```\n\n```pacman: sudo pacman -S enca```\n\n```apk: sudo apk add enca```\n\n```zypper: sudo zypper install enca```\n\n```brew: brew install enca```\n\n```nix: nix profile install nixpkgs#enca```"],["SEE ALSO","[file](/man/file)(1), [iconv](/man/iconv)(1), [chardet](/man/chardet)(1)"]],"e":["file","iconv","chardet"],"x":"enca [file.txt]"},"fls":{"n":"fls","t":"list files and directories from disk images","s":[["TAGLINE","list files and directories from disk images"],["TLDR","**List files in** image\n\n```fls [disk.img]```\n\n**List deleted files**\n\n```fls -d [disk.img]```\n\n**Recursive listing**\n\n```fls -r [disk.img]```\n\n**List specific directory**\n\n```fls [disk.img] [inode]```\n\n**List with file** types\n\n```fls -p [disk.img]```"],["SYNOPSIS","**fls** [_options_] _image_ [_inode_]"],["PARAMETERS","_IMAGE_\n> Disk image file.\n\n_INODE_\n> Starting inode (default: root).\n\n**-r**\n> Recursive listing.\n\n**-d**\n> Show deleted entries.\n\n**-l**\n> Long format output.\n\n**-p**\n> Show full paths.\n\n**-m** _PREFIX_\n> Output in mactime format.\n\n**-o** _OFFSET_\n> Partition offset.\n\n**--help**\n> Display help information."],["DESCRIPTION","**fls** lists file and directory names from disk images. It's part of The Sleuth Kit forensics toolkit and examines filesystem structures directly without mounting.\n\nThe tool shows regular and deleted file entries, useful for data recovery and forensic analysis. It works with various filesystems including NTFS, FAT, ext, and HFS+.\n\nfls enables examining disk images without modifying their contents, preserving forensic integrity."],["CAVEATS","Requires raw or forensic disk images. Deleted file recovery depends on filesystem state. Large images may be slow."],["HISTORY","fls is part of **The Sleuth Kit** created by Brian Carrier. It evolved from earlier forensic tools and provides cross-platform filesystem analysis for digital investigations."],["INSTALL","```apt: sudo apt install sleuthkit```\n\n```dnf: sudo dnf install sleuthkit```\n\n```pacman: sudo pacman -S sleuthkit```\n\n```apk: sudo apk add sleuthkit```\n\n```zypper: sudo zypper install sleuthkit```\n\n```brew: brew install sleuthkit```\n\n```nix: nix profile install nixpkgs#sleuthkit```"],["SEE ALSO","[ils](/man/ils)(1), [mmls](/man/mmls)(1)"]],"e":["ils","mmls"],"x":"fls [disk.img]"},"git-cherry":{"n":"git-cherry","t":"Find commits not merged upstream","s":[["TAGLINE","Find commits not merged upstream"],["TLDR","**Show unpicked commits**\n\n```git cherry [upstream] [branch]```\n\n**Show with commit messages**\n\n```git cherry -v [upstream]```\n\n**Compare branches**\n\n```git cherry [main] [feature]```"],["SYNOPSIS","**git cherry** [_options_] [_upstream_] [_head_]"],["PARAMETERS","_UPSTREAM_\n> Branch to compare against.\n\n_HEAD_\n> Branch to check (default: current).\n\n_LIMIT_\n> Only report commits reachable from LIMIT, further limiting output.\n\n**-v**\n> Show commit subjects.\n\n**--abbrev** _N_\n> Abbreviate commit hashes.\n\n**--help**\n> Display help information."],["DESCRIPTION","**git cherry** finds commits not yet applied to upstream. It compares commits between branches, identifying which commits from your branch are missing from upstream based on patch content, not commit hashes.\n\nOutput uses **+** for commits not in upstream and **-** for equivalent commits already present. This helps identify what still needs to be merged or cherry-picked.\n\nThis command is particularly useful for reviewing branch status before merging, ensuring that all intended changes have been integrated and identifying any commits that may have been lost during rebasing."],["CAVEATS","Compares patch content, not commit IDs. Rebased commits show as missing. Works best for linear histories."],["HISTORY","git cherry is a core **Git** command for comparing branches by patch equivalence, named after the cherry-pick operation it helps plan."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-cherry-pick](/man/git-cherry-pick)(1), [git-log](/man/git-log)(1), [git-rebase](/man/git-rebase)(1)"],["RESOURCES","```[Source code](https://github.com/git/git)```\n\n```[Homepage](https://git-scm.com/)```\n\n```[Documentation](https://git-scm.com/docs/git-cherry)```"]],"e":["git-cherry-pick","git-log","git-rebase"],"x":"git cherry [upstream] [branch]"},"git-hooks":{"n":"git-hooks","t":"Scripts triggered at Git workflow points","s":[["TAGLINE","Scripts triggered at Git workflow points"],["TLDR","**Create pre-commit hook**\n\n```echo '#!/bin/sh' > .git/hooks/pre-commit && chmod +x .git/hooks/pre-commit```\n\n**List available hooks**\n\n```ls .git/hooks/*.sample```\n\n**Enable sample hook**\n\n```mv .git/hooks/pre-commit.sample .git/hooks/pre-commit```"],["DESCRIPTION","**Git hooks** are scripts that run automatically at key points in the Git workflow. They enable automation of tasks like linting, testing, and commit message validation.\n\nHooks are stored in **.git/hooks/** and must be executable. Common hooks include pre-commit (before commit), prepare-commit-msg (edit message), commit-msg (validate message), and pre-push (before push)."],["HOOK TYPES","**Client-side hooks:**\n```\npre-commit         Run before commit\nprepare-commit-msg Edit commit message template\ncommit-msg         Validate commit message\npost-commit        After commit completes\npre-rebase         Before rebase starts\npost-checkout      After checkout or switch\npost-merge         After merge completes\npre-push           Before push\n```\n\n**Server-side hooks:**\n```\npre-receive        Before accepting push\nupdate             Per-branch pre-receive\npost-receive       After push completes\npost-update        After all refs updated\n```"],["CONFIGURATION","**.git/hooks/**\n> Default directory containing hook scripts for the repository. Scripts must be executable.\n\n**core.hooksPath**\n> Git config variable to override the hooks directory (since Git 2.9). Set with `git config core.hooksPath <path>`."],["CAVEATS","Hooks are not version-controlled in .git/hooks. Use tools like Husky or pre-commit to share hooks. Hooks can be bypassed with --no-verify. Keep hooks fast to avoid workflow friction."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git](/man/git)(1), [git-commit](/man/git-commit)(1), [husky](/man/husky)(1), [pre-commit](/man/pre-commit)(1)"]],"e":["git","git-commit","husky","pre-commit"],"x":"echo '#!/bin/sh' > .git/hooks/pre-commit && chmod +x .git/hooks/pre-commit"},"git-ls-files":{"n":"git-ls-files","t":"List files in index and working tree","s":[["TAGLINE","List files in index and working tree"],["TLDR","**List tracked files**\n\n```git ls-files```\n\n**List untracked files**\n\n```git ls-files --others```\n\n**List ignored files**\n\n```git ls-files --ignored --exclude-standard```\n\n**List modified files**\n\n```git ls-files --modified```\n\n**List deleted files**\n\n```git ls-files --deleted```"],["SYNOPSIS","**git ls-files** [_options_] [_files_]"],["PARAMETERS","**--cached**, **-c**\n> Show staged files (default).\n\n**--modified**, **-m**\n> Show modified files.\n\n**--deleted**, **-d**\n> Show deleted files.\n\n**--others**, **-o**\n> Show untracked files.\n\n**--ignored**\n> Show ignored files.\n\n**--exclude-standard**\n> Use standard exclusions.\n\n**--stage**, **-s**\n> Show staging info.\n\n**-x** _pattern_, **--exclude** _pattern_\n> Skip files matching pattern.\n\n**-z**\n> Terminate entries with NUL instead of newline, for safe scripting.\n\n**--full-name**\n> Show paths relative to the repository root, not the current directory.\n\n**--help**\n> Display help information."],["DESCRIPTION","**git ls-files** lists files in the index and working tree. It can show tracked, untracked, ignored, modified, and deleted files, making it valuable for scripting and automation.\n\nThe command provides low-level access to Git's file tracking state. Various flags control which file categories to display. It is commonly used in scripts to enumerate files matching certain criteria, such as finding all untracked files or listing everything ignored by `.gitignore`."],["CAVEATS","Shows index state, not commits. Output format varies by options. Useful for scripting."],["HISTORY","git ls-files is a core **Git** plumbing command for inspecting the index, used both directly and by other git commands."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-status](/man/git-status)(1), [git-ls-tree](/man/git-ls-tree)(1)"],["RESOURCES","```[Source code](https://github.com/git/git)```\n\n```[Homepage](https://git-scm.com/)```\n\n```[Documentation](https://git-scm.com/docs/git-ls-files)```"]],"e":["git-status","git-ls-tree"],"x":"git ls-files"},"gomi":{"n":"gomi","t":"Trash CLI tool written in Go","s":[["TAGLINE","Trash CLI tool written in Go"],["TLDR","**Move files to trash**\n\n```gomi [file1] [file2]```\n\n**Interactively restore a file from trash**\n\n```gomi -r```\n\n**List trash contents**\n\n```gomi -l```\n\n**Empty the trash permanently**\n\n```gomi --empty```"],["SYNOPSIS","**gomi** [_options_] _files_"],["PARAMETERS","_FILES_\n> Files or directories to move to trash.\n\n**-r**, **--restore**\n> Interactively select and restore files from trash.\n\n**-l**, **--list**\n> List all files currently in the trash.\n\n**--empty**\n> Permanently delete all files in the trash.\n\n**--help**\n> Display help information.\n\n**--version**\n> Display version information."],["DESCRIPTION","**gomi** is a trash CLI written in Go that moves files to a trash directory instead of permanently deleting them. It provides an undo mechanism for file deletion with an interactive fuzzy-finder restore interface powered by fzf-style selection.\n\nThe tool uses the XDG Trash specification on Linux and integrates with system trash on macOS. Files can be restored to their original location through the interactive restore interface."],["CAVEATS","Trashed files still consume disk space until the trash is emptied. Behavior differs from `rm` which permanently deletes files. Cross-filesystem trashing may copy files instead of moving them. Trash should be emptied periodically to reclaim space."],["INSTALL","```brew: brew install gomi```\n\n```nix: nix profile install nixpkgs#gomi```"],["SEE ALSO","[rm](/man/rm)(1), [trash-cli](/man/trash-cli)(1)"]],"e":["rm","trash-cli"],"x":"gomi [file1] [file2]"},"herdr":{"n":"herdr","t":"Terminal multiplexer built for AI coding agents","s":[["TAGLINE","Terminal multiplexer built for AI coding agents"],["TLDR","**Start** herdr in the current project\n\n```herdr```\n\n**Detach** from a session (default prefix style)\n\n```[ctrl+b] q```\n\n**Reattach** later\n\n```herdr```\n\n**Install** via script\n\n```curl -fsSL https://herdr.dev/install.sh | sh```"],["SYNOPSIS","**herdr** [*options*] [*command*]"],["DESCRIPTION","**herdr** is an agent-oriented terminal multiplexer: a single Rust binary that runs inside your existing terminal, shows live panes for coding agents (Claude Code, Codex, and others), and keeps sessions running after detach—including over SSH. It supports keyboard and mouse, tmux-style prefix keys, splits, plugins, and a socket API so agents can spawn panes and wait on each other.\n\nDocs: https://herdr.dev/docs/"],["PARAMETERS","**herdr**\n\n> Attach to the session for the current workspace or create one.\n\nAdditional subcommands and flags (remote attach, plugin management, etc.) are version-specific—run **herdr --help** and see the online quick start.\n\nCommon UX: prefix **ctrl+b** then **q** to detach; run **herdr** again to reattach."],["CAVEATS","Agent integrations depend on which tools you install. Socket API and plugins expand the attack surface—treat untrusted plugins carefully. Not a drop-in replacement for every **tmux** workflow; learn herdr's own keybindings."],["INSTALL","```brew: brew install herdr```\n\n```nix: nix profile install nixpkgs#herdr```"],["SEE ALSO","[tmux](/man/tmux)(1), [zellij](/man/zellij)(1), [gemini](/man/gemini)(1)"],["RESOURCES","```[Source code](https://github.com/ogulcancelik/herdr)```\n\n```[Homepage](https://herdr.dev)```\n\n```[Documentation](https://herdr.dev/docs/)```"]],"e":["tmux","zellij","gemini"],"x":"herdr"},"hexchat":{"n":"hexchat","t":"graphical IRC client for Linux, Windows, and macOS","s":[["TAGLINE","graphical IRC client for Linux, Windows, and macOS"],["TLDR","**Start HexChat**\n\n```hexchat```\n\n**Connect to server**\n\n```hexchat [irc://irc.libera.chat/channel]```\n\n**Start minimized**\n\n```hexchat --minimize=2```\n\n**Specify config directory**\n\n```hexchat -d [~/.config/hexchat]```"],["SYNOPSIS","**hexchat** [_options_] [_url_]"],["PARAMETERS","_url_\n> IRC URL to connect to.\n\n**-d**, **--cfgdir** _dir_\n> Configuration directory.\n\n**-n**, **--no-plugins**\n> Don't load plugins.\n\n**--minimize**=_level_\n> Start window minimized: **0** = none, **1** = window minimized, **2** = tray-only.\n\n**-a**, **--no-auto**\n> Don't auto-connect to any servers on startup.\n\n**-e**, **--existing**\n> Open in existing instance.\n\n**--url** _url_\n> Connect to IRC URL.\n\n**-v**, **--version**\n> Show version."],["DESCRIPTION","**HexChat** is a graphical IRC client for Linux, Windows, and macOS. It provides a tabbed interface for multiple channels and servers, with features like DCC file transfer, scripting, and extensive customization.\n\nHexChat supports plugins in C, Perl, Python, and Lua. It's a fork of XChat, continuing development with modern features and active maintenance."],["FEATURES","- Tabbed interface for channels\n- DCC file transfers\n- SSL/TLS support\n- Scripting (Python, Perl, Lua)\n- Spell checking\n- Desktop notifications\n- Auto-join and reconnect"],["CAVEATS","Configuration stored in ~/.config/hexchat. IRC connections may need SSL. Some networks require registration. Plugin compatibility varies."],["HISTORY","HexChat was forked from **XChat** in **2009** when XChat's Windows builds became shareware. It's maintained by a community of developers and is the most popular graphical IRC client on Linux."],["INSTALL","```apt: sudo apt install hexchat```\n\n```dnf: sudo dnf install hexchat```\n\n```zypper: sudo zypper install hexchat```\n\n```nix: nix profile install nixpkgs#hexchat```"],["SEE ALSO","[irssi](/man/irssi)(1), [weechat](/man/weechat)(1)"]],"e":["irssi","weechat"],"x":"hexchat"},"idevicepair":{"n":"idevicepair","t":"manages pairing with iOS devices","s":[["TAGLINE","manages pairing with iOS devices"],["TLDR","**Pair with device**\n\n```idevicepair pair```\n\n**Check pair status**\n\n```idevicepair validate```\n\n**Unpair device**\n\n```idevicepair unpair```\n\n**List paired devices**\n\n```idevicepair list```\n\n**Target specific device**\n\n```idevicepair -u [udid] pair```"],["SYNOPSIS","**idevicepair** [_options_] _command_"],["PARAMETERS","_COMMAND_\n> Operation (pair, validate, unpair, list).\n\n**pair**\n> Pair with device.\n\n**validate**\n> Check pairing status.\n\n**unpair**\n> Remove pairing.\n\n**list**\n> List paired devices.\n\n**-u** _UDID_\n> Target device.\n\n**--help**\n> Display help information."],["DESCRIPTION","**idevicepair** manages pairing with iOS devices. Pairing establishes trust between computer and device.\n\nThe tool handles the trust dialog workflow. Pairing is required before most device operations work."],["CAVEATS","Requires device trust approval. Part of libimobiledevice. USB connection needed."],["HISTORY","idevicepair is part of **libimobiledevice** for establishing trusted connections with iOS devices."],["INSTALL","```apt: sudo apt install libimobiledevice-utils```\n\n```dnf: sudo dnf install libimobiledevice-utils```\n\n```apk: sudo apk add libimobiledevice-progs```"],["SEE ALSO","[idevice_id](/man/idevice_id)(1), [ideviceinfo](/man/ideviceinfo)(1), [idevicename](/man/idevicename)(1)"]],"e":["idevice_id","ideviceinfo","idevicename"],"x":"idevicepair pair"},"ip":{"n":"ip","t":"modern Linux networking command for showing and manipulating routing","s":[["TAGLINE","modern Linux networking command for showing and manipulating routing"],["TLDR","List interfaces with **detailed info**\n\n```ip address```\n\nList interfaces with **brief** network layer info\n\n```ip -br address```\n\nList interfaces with brief **link layer** info\n\n```ip -br link```\n\nDisplay the **routing table**\n\n```ip route```\n\nShow **neighbors** (ARP table)\n\n```ip neighbour```\n\nMake an interface **up/down**\n\n```sudo ip link set [ethX] up```\n```sudo ip link set [ethX] down```\n\n**Add/Delete** an IP address to an interface\n\n```sudo ip address add [ip_address]/[mask] dev [ethX]```\n```sudo ip address delete [ip_address]/[mask] dev [ethX]```\n\nAdd a **default route**\n\n```sudo ip route add default via [ip_address] dev [ethX]```"],["SYNOPSIS","**ip** [_-br_] [_-c_] [_-4_|_-6_] _object_ [_command_]"],["DESCRIPTION","**ip** is the modern Linux networking command for showing and manipulating routing, devices, policy routing, and tunnels. It replaces the older **ifconfig**, **route**, and **arp** commands."],["PARAMETERS","**address, a**\n> Display and manage IP addresses\n\n**link, l**\n> Display and manage network interfaces\n\n**route, r**\n> Display and manage routing table\n\n**neighbour, n**\n> Display and manage ARP/neighbor cache\n\n**tunnel**\n> Display and manage IP tunnels\n\n**maddr**\n> Display and manage multicast addresses\n\n**rule**\n> Display and manage routing policy database\n\n**-br, -brief**\n> Print output in brief format\n\n**-c, -color**\n> Use color output\n\n**-4**\n> IPv4 only\n\n**-6**\n> IPv6 only\n\n**-s, -stats**\n> Output more statistics\n\n**-o, -oneline**\n> Output each record on a single line"],["CAVEATS","Changes made with **ip** are not persistent across reboots. Use network configuration files or NetworkManager for persistent changes."],["HISTORY","Part of the **iproute2** package, introduced in **Linux 2.2** as a replacement for the older net-tools package (ifconfig, route, arp)."],["INSTALL","```apt: sudo apt install iproute2```\n\n```pacman: sudo pacman -S iproute2```\n\n```apk: sudo apk add iproute2-minimal```\n\n```zypper: sudo zypper install iproute2```\n\n```brew: brew install iproute2```\n\n```nix: nix profile install nixpkgs#iproute2```"],["SEE ALSO","[ifconfig](/man/ifconfig)(8), [route](/man/route)(8), [arp](/man/arp)(8), [ss](/man/ss)(8), [ip-address](/man/ip-address)(8), [ip-route](/man/ip-route)(8)"]],"e":["ifconfig","route","arp","ss","ip-address","ip-route"],"x":"ip address"},"jupyterlite":{"n":"jupyterlite","t":"builds static JupyterLab sites that run entirely in the browser using","s":[["TAGLINE","builds static JupyterLab sites that run entirely in the browser using"],["TLDR","**Build JupyterLite site**\n\n```jupyter lite build```\n\n**Build with specific content**\n\n```jupyter lite build --contents [notebooks/]```\n\n**Serve built site locally**\n\n```jupyter lite serve```\n\n**Build with additional packages**\n\n```jupyter lite build --piplite-wheels [package.whl]```\n\n**Clean build artifacts**\n\n```jupyter lite clean```\n\n**Check configuration**\n\n```jupyter lite status```\n\n**Build with custom output**\n\n```jupyter lite build --output-dir [dist/]```\n\n**Initialize configuration**\n\n```jupyter lite init```"],["SYNOPSIS","**jupyter lite** _command_ [_options_]"],["COMMANDS","**build**\n> Build JupyterLite static site.\n\n**serve**\n> Serve built site locally.\n\n**init**\n> Initialize configuration files.\n\n**status**\n> Show build configuration status.\n\n**clean**\n> Remove build artifacts.\n\n**archive**\n> Create archive of built site.\n\n**list**\n> List addons and their status."],["PARAMETERS","**--contents** _path_\n> Path to content directory for notebooks.\n\n**--output-dir** _path_\n> Output directory for built site. Default: _output.\n\n**--piplite-wheels** _path_\n> Include pip wheels for piplite.\n\n**--pyodide** _url_\n> Custom Pyodide distribution URL.\n\n**--port** _port_\n> Port for serve command. Default: 8000.\n\n**--lite-dir** _path_\n> Source directory for JupyterLite files.\n\n**--settings-overrides** _file_\n> JSON file with settings overrides.\n\n**--no-sourcemaps**\n> Disable source maps in build."],["DESCRIPTION","**jupyter lite** builds static JupyterLab sites that run entirely in the browser using WebAssembly. No server required—deploy to any static hosting.\n\n**build** generates HTML, JavaScript, and assets in the output directory. Include notebooks with **--contents** to pre-populate the file browser. The resulting site runs JupyterLab with Pyodide kernel.\n\nThe Pyodide kernel executes Python in WebAssembly. Pre-install packages with **--piplite-wheels** or install at runtime via piplite. Many scientific Python packages are available.\n\n**serve** starts a local HTTP server for testing. The build output can deploy to GitHub Pages, Netlify, or any static host. No backend infrastructure needed.\n\nConfiguration via **jupyter_lite_config.json** customizes settings, extensions, and build behavior. **init** creates template files."],["CAVEATS","Not all Python packages work in Pyodide (no native extensions without WASM builds). Performance is slower than native Python. Large notebooks may hit browser memory limits. Some JupyterLab extensions are incompatible."],["HISTORY","JupyterLite was released in **2021** by the **Project Jupyter** team, leveraging Pyodide for browser-based Python. It enables sharing interactive notebooks without server infrastructure. The project grew from experiments with WebAssembly Python interpreters. JupyterLite enables educational content, documentation, and lightweight demos. Development continues alongside JupyterLab improvements."],["INSTALL","```nix: nix profile install nixpkgs#jupyterlite```"],["SEE ALSO","[jupyter-lab](/man/jupyter-lab)(1), [jupyter](/man/jupyter)(1)"]],"e":["jupyter-lab","jupyter"],"x":"jupyter lite build"},"linkerd":{"n":"linkerd","t":"CLI for Linkerd, a lightweight service mesh for Kubernetes","s":[["TAGLINE","CLI for Linkerd, a lightweight service mesh for Kubernetes"],["TLDR","**Check CLI and control plane versions**\n\n```linkerd version```\n\n**Validate cluster configuration**\n\n```linkerd check```\n\n**Install Linkerd** control plane\n\n```linkerd install | kubectl apply -f -```\n\n**Inject sidecar proxy** into deployment\n\n```linkerd inject [deployment.yaml] | kubectl apply -f -```\n\n**View real-time traffic** statistics\n\n```linkerd stat deploy```\n\n**Open Linkerd dashboard**\n\n```linkerd viz dashboard```\n\n**View top traffic** by route\n\n```linkerd viz top deploy/[name]```\n\n**Check proxy status** for a namespace\n\n```linkerd diagnostics proxy-metrics -n [namespace] [pod-name]```\n\n**Generate Linkerd upgrade manifest**\n\n```linkerd upgrade | kubectl apply -f -```"],["SYNOPSIS","**linkerd** _command_ [_options_]"],["COMMANDS","**install**\n> Generate Linkerd control plane installation manifest.\n\n**check**\n> Validate installation and cluster configuration.\n\n**inject**\n> Add Linkerd proxy sidecar to Kubernetes resources.\n\n**uninject**\n> Remove Linkerd proxy from resources.\n\n**upgrade**\n> Generate upgrade manifest for control plane.\n\n**uninstall**\n> Generate manifest to remove Linkerd.\n\n**version**\n> Show CLI and control plane versions.\n\n**identity**\n> View workload identity certificates.\n\n**diagnostics**\n> Troubleshooting and debugging commands.\n\n**completion**\n> Generate shell completion scripts."],["VIZ EXTENSION COMMANDS","**viz install**\n> Install observability extension.\n\n**viz dashboard**\n> Open web dashboard.\n\n**viz stat**\n> Display traffic statistics.\n\n**viz top**\n> Show real-time traffic by route.\n\n**viz tap**\n> Live stream of requests.\n\n**viz edges**\n> Show connections between resources.\n\n**viz routes**\n> Display per-route metrics."],["PARAMETERS","**--context** _name_\n> Kubernetes context to use.\n\n**--kubeconfig** _path_\n> Path to kubeconfig file.\n\n**--api-addr** _address_\n> Override API server address.\n\n**--namespace**, **-n** _namespace_\n> Target namespace.\n\n**--linkerd-namespace** _namespace_\n> Linkerd control plane namespace.\n\n**--set** _key=value_\n> Override configuration values.\n\n**--values** _file_\n> Path to values file for configuration.\n\n**--verbose**\n> Enable verbose output.\n\n**--as** _user_\n> Impersonate a Kubernetes user.\n\n**-o**, **--output** _format_\n> Output format: table, json, yaml."],["DESCRIPTION","**linkerd** is the CLI for Linkerd, a lightweight service mesh for Kubernetes. It manages installation, proxy injection, and provides observability tools for microservices traffic.\n\nThe **install** command generates Kubernetes manifests for the control plane. Pipe output to **kubectl apply** for deployment. Use **check** to validate prerequisites and installation health.\n\nSidecar proxy injection with **inject** adds the Linkerd proxy to pods, enabling mTLS, traffic metrics, and load balancing. Injection can be automated via namespace annotations.\n\nThe **viz** extension provides observability features. The **dashboard** opens a web UI showing service topology and metrics. Commands like **stat**, **top**, and **tap** provide CLI-based traffic analysis.\n\nLinkerd uses mutual TLS by default, automatically encrypting traffic between meshed services without application changes."],["CAVEATS","Some commands require the viz extension to be installed separately. The CLI version should match the control plane version. Proxy injection requires pod restart to take effect. Some features require cluster admin privileges."],["HISTORY","Linkerd was originally created at **Buoyant** in **2016** as one of the first service meshes. Version 2 was a complete rewrite in Rust and Go, released in **2018**, focusing on simplicity and low resource overhead. Linkerd joined the CNCF and graduated in **2021**. It remains popular for its lightweight footprint and operational simplicity compared to alternatives."],["INSTALL","```brew: brew install linkerd```\n\n```nix: nix profile install nixpkgs#linkerd```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [istioctl](/man/istioctl)(1), [helm](/man/helm)(1)"]],"e":["kubectl","istioctl","helm"],"x":"linkerd version"},"markdownlint":{"n":"markdownlint","t":"style checker and linter for Markdown files","s":[["TAGLINE","style checker and linter for Markdown files"],["TLDR","**Lint Markdown file**\n\n```markdownlint [file.md]```\n\n**Lint directory**\n\n```markdownlint [docs/]```\n\n**Fix issues automatically**\n\n```markdownlint --fix [file.md]```\n\n**Use config file**\n\n```markdownlint --config [.markdownlint.json] [file.md]```\n\n**Ignore specific rules**\n\n```markdownlint --disable [MD013] [MD033] -- [file.md]```\n\n**Output JSON format**\n\n```markdownlint --json [file.md]```"],["SYNOPSIS","**markdownlint** [_options_] _files_..."],["PARAMETERS","**-f**, **--fix**\n> Fix issues automatically.\n\n**-c**, **--config** _file_\n> Configuration file.\n\n**-d**, **--disable** _rules_\n> Disable rules.\n\n**-e**, **--enable** _rules_\n> Enable rules.\n\n**-i**, **--ignore** _pattern_\n> Ignore files.\n\n**-o**, **--output** _file_\n> Output file.\n\n**-j**, **--json**\n> JSON output."],["DESCRIPTION","**markdownlint** is a style checker and linter for Markdown files. It enforces consistent Markdown formatting based on configurable rules.\n\nThe tool checks for issues like inconsistent heading styles, line length, trailing spaces, and proper list formatting."],["CONFIGURATION","```json\n// .markdownlint.json\n{\n  \"MD013\": { \"line_length\": 120 },\n  \"MD033\": false,\n  \"MD041\": false\n}\n```"],["COMMON RULES","```\nMD001 - Heading increment\nMD013 - Line length\nMD022 - Heading blank lines\nMD033 - Inline HTML\n```"],["CAVEATS","CLI (markdownlint-cli) separate from library. Rules may conflict with some Markdown processors. Auto-fix limited to some rules."],["HISTORY","markdownlint was created by **David Anson** as a Node.js library, with the CLI wrapper (markdownlint-cli) developed separately."],["INSTALL","```pacman: sudo pacman -S markdownlint```"],["SEE ALSO","[prettier](/man/prettier)(1), [markdown](/man/markdown)(1), [vale](/man/vale)(1)"]],"e":["prettier","markdown","vale"],"x":"markdownlint [file.md]"},"mcfly":{"n":"mcfly","t":"intelligent shell history search tool that uses a small neural network","s":[["TAGLINE","intelligent shell history search tool that uses a small neural network"],["TLDR","**Initialize mcfly in bash**\n\n```eval \"$(mcfly init bash)\"```\n\n**Initialize mcfly in zsh**\n\n```eval \"$(mcfly init zsh)\"```\n\n**Initialize mcfly in fish**\n\n```mcfly init fish | source```\n\n**Search history for a pattern**\n\n```mcfly search [pattern]```\n\n**Add a command to history**\n\n```mcfly add \"[command]\"```\n\n**Train the suggestion engine**\n\n```mcfly train```"],["SYNOPSIS","**mcfly** _command_ [_options_]"],["DESCRIPTION","**mcfly** is an intelligent shell history search tool written in Rust that uses a small neural network to suggest the most likely command based on context. It considers working directory, recent commands, exit status, and command patterns.\n\nThe tool replaces Ctrl+R with a smarter search that prioritizes commands based on relevance rather than recency alone. It stores history in a SQLite database, which can be rebuilt if deleted."],["PARAMETERS","**search** [_pattern_]\n> Search history for matching commands.\n\n**add** _command_\n> Add a command to the mcfly history database.\n\n**train**\n> Train the neural network suggestion engine.\n\n**move**\n> Record a directory having been moved.\n\n**init** _shell_\n> Output shell initialization script (bash, zsh, or fish).\n\n**--mcfly-history** _path_\n> Shell history file to read from when adding or searching.\n\n**--session-id** _id_\n> Session ID to record or search under.\n\n**-d**, **--debug**\n> Enable debug output.\n\n**-h**, **--help**\n> Display help information.\n\n**-V**, **--version**\n> Display version information."],["CAVEATS","Requires shell integration to replace Ctrl+R. The neural network needs time to learn usage patterns; initial suggestions may not be optimal. The SQLite database grows with usage. Set **MCFLY_FUZZY** (integer, 2-5 recommended) to enable fuzzy matching. Set **MCFLY_RESULTS** to control the number of results shown (default: 10). Set **MCFLY_HISTORY_LIMIT** to limit search depth."],["HISTORY","**mcfly** was created by **Andrew Cantino** to bring intelligent command suggestion to the shell. It uses machine learning principles to provide context-aware history search, going beyond simple text matching."],["INSTALL","```pacman: sudo pacman -S mcfly```\n\n```apk: sudo apk add mcfly```\n\n```zypper: sudo zypper install mcfly```\n\n```brew: brew install mcfly```\n\n```nix: nix profile install nixpkgs#mcfly```"],["SEE ALSO","[hstr](/man/hstr)(1), [fzf](/man/fzf)(1), [atuin](/man/atuin)(1), [history](/man/history)(1), [bash](/man/bash)(1), [zsh](/man/zsh)(1)"]],"e":["hstr","fzf","atuin","history","bash","zsh"],"x":"eval \"$(mcfly init bash)\""},"mencoder":{"n":"mencoder","t":"encodes video from MPlayer","s":[["TAGLINE","encodes video from MPlayer"],["TLDR","**Convert video**\n\n```mencoder [input.avi] -o [output.mp4] -ovc lavc -oac mp3lame```\n\n**Extract audio**\n\n```mencoder [video.avi] -o [audio.mp3] -ovc copy -oac mp3lame```\n\n**Resize video**\n\n```mencoder [input.avi] -o [output.avi] -vf scale=[640:480] -ovc lavc -oac copy```\n\n**Two-pass encoding**\n\n```mencoder [input.avi] -o /dev/null -ovc lavc -lavcopts vpass=1 && mencoder [input.avi] -o [output.avi] -ovc lavc -lavcopts vpass=2```\n\n**Join videos**\n\n```mencoder -ovc copy -oac copy -o [joined.avi] [file1.avi] [file2.avi]```"],["SYNOPSIS","**mencoder** [_options_] _input_ -o _output_"],["PARAMETERS","_INPUT_\n> Input media file.\n\n**-o** _FILE_\n> Output file.\n\n**-ovc** _CODEC_\n> Video codec.\n\n**-oac** _CODEC_\n> Audio codec.\n\n**-vf** _FILTERS_\n> Video filters.\n\n**--help**\n> Display help information."],["DESCRIPTION","**mencoder** encodes video from MPlayer. It converts between formats and applies filters.\n\nThe tool is part of MPlayer project. Superseded by FFmpeg in many cases."],["CAVEATS","MPlayer project (legacy). FFmpeg often preferred. Complex options."],["HISTORY","mencoder was the encoding companion to **MPlayer**, providing video conversion capabilities."],["INSTALL","```apt: sudo apt install mencoder```\n\n```pacman: sudo pacman -S mencoder```\n\n```apk: sudo apk add mplayer```\n\n```brew: brew install mplayer```\n\n```nix: nix profile install nixpkgs#mplayer```"],["SEE ALSO","[ffmpeg](/man/ffmpeg)(1), [mplayer](/man/mplayer)(1), [handbrake](/man/handbrake)(1)"]],"e":["ffmpeg","mplayer","handbrake"],"x":"mencoder [input.avi] -o [output.mp4] -ovc lavc -oac mp3lame"},"mktemp":{"n":"mktemp","t":"Create temporary files or directories safely and print their names","s":[["TAGLINE","Create temporary files or directories safely and print their names"],["TLDR","Create an empty **temporary file** and print its path\n\n```mktemp```\n\nCreate a temporary file in a **custom directory**\n\n```mktemp -p [/path/to/directory]```\n\nUse a **custom template** (Xs replaced with random characters)\n\n```mktemp [/tmp/example.XXXXXXXX]```\n\nCreate a temporary file with a **specific suffix**\n\n```mktemp --suffix [.txt]```\n\nCreate an empty **temporary directory**\n\n```mktemp -d```\n\n**Dry run**: print name without creating\n\n```mktemp -u```"],["SYNOPSIS","**mktemp** [**-d**] [**-u**] [**-q**] [**-p** _dir_] [**--suffix** _suff_] [_template_]"],["PARAMETERS","**-d**, **--directory**\n> Create a directory instead of a file.\n\n**-u**, **--dry-run**\n> Print name without creating anything (unsafe; see CAVEATS).\n\n**-q**, **--quiet**\n> Suppress error messages on creation failure.\n\n**-p** _dir_, **--tmpdir**[=_dir_]\n> Create temporary file relative to the specified directory; defaults to $TMPDIR or /tmp.\n\n**-t**\n> Interpret template as a filename relative to the temp directory (deprecated).\n\n**--suffix** _suff_\n> Append suffix to template; suffix must not contain slashes."],["DESCRIPTION","**mktemp** creates temporary files or directories safely and prints their paths. The template must include at least 3 consecutive X characters in its final component, which are replaced with random alphanumeric characters to ensure uniqueness.\n\nWhen no template is provided, mktemp defaults to tmp.XXXXXXXXXX in the system temp directory. Files are created with u+rw permissions and directories with u+rwx, both modified by umask.\n\nThe safe creation prevents race conditions where another process might create a file between checking for existence and creating. This is critical for secure script writing."],["CAVEATS","The -u (dry-run) option is unsafe for production scripts because another process could create the file between printing the name and actual use. Always let mktemp create the file directly. Temporary files persist until explicitly deleted; clean up in scripts using trap."],["HISTORY","**mktemp** originated in **OpenBSD** and was later adopted by other systems. The GNU version is part of **coreutils**. The command provides a safe interface to the mkstemp(3) and mkdtemp(3) library functions."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[rm](/man/rm)(1), [trap](/man/trap)(1)"]],"e":["rm","trap"],"x":"mktemp"},"mqtt-sn-pub":{"n":"mqtt-sn-pub","t":"Publish MQTT-SN messages from the command line","s":[["TAGLINE","Publish MQTT-SN messages from the command line"],["TLDR","**Publish** a message to a topic\n\n```mqtt-sn-pub -t [topic/name] -m [\"hello\"]```\n\n**Publish** to a remote host and port\n\n```mqtt-sn-pub -h [192.168.1.10] -p [1883] -t [sensors/temp] -m [\"22.5\"]```\n\n**Publish** with QoS 1 and retain\n\n```mqtt-sn-pub -t [topic] -m [\"payload\"] -q [1] -r```\n\n**Publish** file contents as payload\n\n```mqtt-sn-pub -t [topic] -f [payload.bin]```\n\n**Publish** lines from stdin\n\n```mqtt-sn-pub -t [topic] -l```\n\n**Publish** one full stdin message\n\n```mqtt-sn-pub -t [topic] -s```\n\n**Null** (zero-length) message\n\n```mqtt-sn-pub -t [topic] -n```"],["SYNOPSIS","**mqtt-sn-pub** [*opts*] **-t** *topic* **-m** *message*"],["DESCRIPTION","**mqtt-sn-pub** is part of **mqtt-sn-tools**, a set of small C utilities for the MQTT-SN (MQTT for Sensor Networks) protocol. It publishes a message to an MQTT-SN gateway or broker over UDP.\n\nBuild with **make** on a POSIX system from the upstream repository. Related tools: **mqtt-sn-sub**, **mqtt-sn-dump**, **mqtt-sn-serial-bridge**.\n\nSupported features in the suite include QoS 0, 1, and -1, keepalives, retained and empty messages, clean/unclean sessions, and optional forwarder encapsulation (MQTT-SN v1.2). Packets must be 255 bytes or less; there is no QoS 2, LWT, or automatic gateway discovery."],["PARAMETERS","**-t** *topic*\n\n> Topic name to publish to.\n\n**-T** *topicid*\n\n> Pre-defined topic ID instead of a name.\n\n**-m** *message*\n\n> Message payload string.\n\n**-f** *file*\n\n> Read payload from a file.\n\n**-l**\n\n> Read stdin, one message per line.\n\n**-s**\n\n> Read one whole message from stdin.\n\n**-n**\n\n> Send a null (zero length) message.\n\n**-h** *host*\n\n> Gateway/broker host (default **127.0.0.1**).\n\n**-p** *port*\n\n> UDP port (default **1883**).\n\n**-q** *qos*\n\n> Quality of Service: **0**, **1**, or **-1** (default 0).\n\n**-r**\n\n> Retained message.\n\n**-k** *seconds*\n\n> Keepalive interval (default 10).\n\n**-i** *clientid*\n\n> Client ID (default **mqtt-sn-tools-** plus process id).\n\n**-d**\n\n> Increase debug level (repeatable).\n\n**--fe** / **--wlnid**\n\n> Forwarder encapsulation and wireless node id options.\n\n**--cport** *port*\n\n> Source port for outgoing packets."],["CAVEATS","MQTT-SN is not MQTT over TCP; you need an MQTT-SN gateway. Packet size cap is 255 bytes. No automatic retry of lost packets. Default port 1883 matches many gateways but confirm your deployment."],["SEE ALSO","[mqtt-sn-sub](/man/mqtt-sn-sub)(1), [mqtt-sn-dump](/man/mqtt-sn-dump)(1), [mosquitto_pub](/man/mosquitto_pub)(1)"],["RESOURCES","```[Source code](https://github.com/njh/mqtt-sn-tools)```"]],"e":["mqtt-sn-sub","mqtt-sn-dump","mosquitto_pub"],"x":"mqtt-sn-pub -t [topic/name] -m [\"hello\"]"},"mutter":{"n":"mutter","t":"GNOME window manager","s":[["TAGLINE","GNOME window manager"],["TLDR","**Start Mutter**\n\n```mutter```\n\n**Replace running window manager**\n\n```mutter --replace```\n\n**Run in wayland mode**\n\n```mutter --wayland```\n\n**Run in nested mode**\n\n```mutter --nested```\n\n**Display version**\n\n```mutter --version```"],["SYNOPSIS","**mutter** [_options_]"],["PARAMETERS","**--replace**\n> Replace running window manager.\n\n**--wayland**\n> Run as Wayland compositor.\n\n**--nested**\n> Run nested in another session.\n\n**--version**\n> Show version information.\n\n**--help**\n> Display help information."],["DESCRIPTION","**mutter** is the GNOME window manager. It handles window management and compositing.\n\nThe tool supports both X11 and Wayland. Core component of GNOME Shell."],["CAVEATS","GNOME specific. Requires GPU compositing. Usually run via GNOME Shell."],["HISTORY","Mutter is derived from **Metacity** and became the standard GNOME window manager with GNOME 3."],["INSTALL","```apt: sudo apt install mutter```\n\n```dnf: sudo dnf install mutter```\n\n```pacman: sudo pacman -S mutter```\n\n```apk: sudo apk add mutter```\n\n```zypper: sudo zypper install mutter```\n\n```nix: nix profile install nixpkgs#mutter```"],["SEE ALSO","[muffin](/man/muffin)(1)"]],"e":["muffin"],"x":"mutter"},"ng":{"n":"ng","t":"angular CLI","s":[["TAGLINE","angular CLI"],["TLDR","**Create new project**\n\n```ng new [project-name]```\n\n**Serve application**\n\n```ng serve```\n\n**Build application**\n\n```ng build```\n\n**Generate component**\n\n```ng generate component [name]```\n\n**Generate service**\n\n```ng generate service [name]```\n\n**Run tests**\n\n```ng test```\n\n**Run e2e tests**\n\n```ng e2e```\n\n**Add library**\n\n```ng add [package-name]```"],["SYNOPSIS","**ng** [_command_] [_options_]"],["PARAMETERS","_COMMAND_\n> Angular CLI command.\n\n**new** _NAME_\n> Create new project.\n\n**serve**\n> Start dev server.\n\n**build**\n> Build project.\n\n**generate** _TYPE_ _NAME_\n> Generate code.\n\n**test**\n> Run unit tests.\n\n**--help**\n> Display help information."],["DESCRIPTION","**ng** is the Angular CLI. It creates and manages Angular applications.\n\nThe tool scaffolds projects. Handles building, testing, and deployment."],["CAVEATS","Node.js required. TypeScript-based. Configuration in angular.json."],["HISTORY","Angular CLI was created by **Google** to provide tooling for Angular framework development."],["SEE ALSO","[npm](/man/npm)(1), [node](/man/node)(1), [nx](/man/nx)(1)"]],"e":["npm","node","nx"],"x":"ng new [project-name]"},"notmuch":{"n":"notmuch","t":"mail indexer and searcher","s":[["TAGLINE","mail indexer and searcher"],["TLDR","**Initial setup**\n\n```notmuch setup```\n\n**Index new mail**\n\n```notmuch new```\n\n**Search messages**\n\n```notmuch search [from:sender@example.com]```\n\n**Show message**\n\n```notmuch show [thread:id]```\n\n**Tag messages**\n\n```notmuch tag +[important] [search-terms]```\n\n**Count messages**\n\n```notmuch count [tag:inbox]```\n\n**Reply to message**\n\n```notmuch reply [thread:id]```"],["SYNOPSIS","**notmuch** [_command_] [_options_]"],["PARAMETERS","_COMMAND_\n> Notmuch subcommand.\n\n**setup**\n> Initial configuration.\n\n**new**\n> Index new messages.\n\n**search** _QUERY_\n> Search messages.\n\n**show** _ID_\n> Display message.\n\n**tag** _TAGS_ _QUERY_\n> Modify tags.\n\n**--help**\n> Display help information."],["DESCRIPTION","**notmuch** is a mail indexer and searcher. Provides fast full-text search.\n\nThe tool indexes Maildir messages. Used with various email clients."],["CAVEATS","Maildir format only. Xapian backend. Frontend clients available."],["HISTORY","notmuch was created by **Carl Worth** for fast email search, inspired by Gmail's search."],["INSTALL","```apt: sudo apt install notmuch```\n\n```dnf: sudo dnf install notmuch```\n\n```pacman: sudo pacman -S notmuch```\n\n```apk: sudo apk add notmuch```\n\n```zypper: sudo zypper install notmuch```\n\n```brew: brew install notmuch```\n\n```nix: nix profile install nixpkgs#notmuch```"],["SEE ALSO","[mu](/man/mu)(1), [mairix](/man/mairix)(1), [mutt](/man/mutt)(1)"]],"e":["mu","mairix","mutt"],"x":"notmuch setup"},"nvm":{"n":"nvm","t":"node Version Manager","s":[["TAGLINE","node Version Manager"],["TLDR","**Install Node.js version**\n\n```nvm install [version]```\n\n**Install latest LTS**\n\n```nvm install --lts```\n\n**Use specific version**\n\n```nvm use [version]```\n\n**List installed versions**\n\n```nvm ls```\n\n**List available versions**\n\n```nvm ls-remote```\n\n**Set default version**\n\n```nvm alias default [version]```"],["SYNOPSIS","**nvm** [_command_] [_options_]"],["PARAMETERS","**install** _VERSION_\n> Install Node.js version.\n\n**use** _VERSION_\n> Switch to version.\n\n**ls**\n> List installed versions.\n\n**ls-remote**\n> List available versions.\n\n**alias** _NAME_ _VERSION_\n> Create version alias.\n\n**uninstall** _VERSION_\n> Remove version.\n\n**--help**\n> Display help information."],["DESCRIPTION","**nvm** is Node Version Manager. Manages multiple Node.js installations.\n\nThe tool enables version switching per project. Shell-based version management."],["CAVEATS","Shell function not executable. Source in profile. Per-shell activation required."],["HISTORY","nvm was created to allow **easy switching** between Node.js versions for development."],["INSTALL","```pacman: sudo pacman -S nvm```\n\n```brew: brew install nvm```"],["SEE ALSO","[node](/man/node)(1), [npm](/man/npm)(1), [fnm](/man/fnm)(1), [n](/man/n)(1)"]],"e":["node","npm","fnm","n"],"x":"nvm install [version]"},"pio-access":{"n":"pio-access","t":"manages access permissions for PlatformIO Registry packages","s":[["TAGLINE","manages access permissions for PlatformIO Registry packages"],["TLDR","**Grant package access**\n\n```pio access grant [maintainer] [user] [package]```\n\n**Revoke access**\n\n```pio access revoke [maintainer] [user] [package]```\n\n**List access permissions**\n\n```pio access list [package]```"],["SYNOPSIS","**pio access** _command_ [_options_]"],["PARAMETERS","**grant** _level_ _user_ _package_\n> Grant access (guest, maintainer, admin).\n\n**revoke** _level_ _user_ _package_\n> Revoke access level.\n\n**list** _package_\n> List package permissions.\n\n**private** _package_\n> Make package private.\n\n**public** _package_\n> Make package public."],["DESCRIPTION","**pio access** manages access permissions for PlatformIO Registry packages. Controls who can view, download, and modify published packages. Requires PlatformIO account."],["SEE ALSO","[pio-account](/man/pio-account)(1), [pio-pkg](/man/pio-pkg)(1)"]],"e":["pio-account","pio-pkg"],"x":"pio access grant [maintainer] [user] [package]"},"pixi":{"n":"pixi","t":"Fast cross-platform conda package manager","s":[["TAGLINE","Fast cross-platform conda package manager"],["TLDR","**Initialize new project**\n\n```pixi init [project_name]```\n\n**Add dependency**\n\n```pixi add [numpy]```\n\n**Add dev dependency**\n\n```pixi add --dev [pytest]```\n\n**Run a task**\n\n```pixi run [test]```\n\n**Install dependencies**\n\n```pixi install```\n\n**Activate shell**\n\n```pixi shell```\n\n**Create global tool installation**\n\n```pixi global install [ruff]```\n\n**Show project info**\n\n```pixi info```\n\n**Search for packages**\n\n```pixi search [numpy]```"],["SYNOPSIS","**pixi** _command_ [_options_] [_arguments_]"],["PARAMETERS","**init** [_name_]\n> Initialize new project with pixi.toml.\n\n**add** _packages_\n> Add dependencies to project.\n\n**remove** _packages_\n> Remove dependencies.\n\n**install**\n> Install all dependencies.\n\n**run** _task_\n> Run a project task.\n\n**shell**\n> Activate environment shell.\n\n**task** _subcommand_\n> Manage project tasks.\n\n**project** _subcommand_\n> Manage project configuration.\n\n**global** _subcommand_\n> Manage global tool installations.\n\n**update**\n> Update dependencies.\n\n**list**\n> List project dependencies.\n\n**tree**\n> Show dependency tree.\n\n**info**\n> Show project information.\n\n**search** _query_\n> Search for packages in configured channels.\n\n**clean**\n> Remove the environment and cache.\n\n**--dev**, **-d**\n> Operate on dev dependencies.\n\n**--platform** _PLATFORM_\n> Target platform.\n\n**--feature** _FEATURE_\n> Target feature.\n\n**--manifest-path** _PATH_\n> Path to pixi.toml.\n\n**--frozen**\n> Use existing lockfile without updates.\n\n**--locked**\n> Require lockfile to be up to date."],["DESCRIPTION","**pixi** is a fast, cross-platform package manager built on the conda ecosystem. It manages Python and native dependencies together, providing reproducible environments from a single lockfile.\n\nProjects use pixi.toml for configuration, similar to Cargo.toml. Dependencies from conda-forge, PyPI, and custom channels can be mixed. The lockfile (pixi.lock) pins exact versions for reproducibility.\n\nMulti-platform support generates separate lockfiles per platform, enabling the same project to work on Linux, macOS, and Windows without manual intervention.\n\nTasks defined in pixi.toml provide npm-like scripting. Commands like test, build, and lint are configured once and run with pixi run. Task dependencies chain commands together.\n\nGlobal installations (pixi global install) work like pipx, creating isolated environments for CLI tools. This keeps development tools separate from project dependencies."],["CONFIGURATION","**pixi.toml**\n> Project configuration file defining dependencies, tasks, and channels.\n\n**pixi.lock**\n> Lockfile with pinned versions for reproducible environments per platform.\n\n**~/.pixi/config.toml**\n> Global configuration for default channels, authentication, and cache settings.\n\n**PIXI_HOME**\n> Override the default Pixi home directory."],["CAVEATS","Newer tool, ecosystem still growing. Some conda packages may not work perfectly. Mixing PyPI and conda dependencies requires care. Large environments take time to solve. Platform-specific packages need proper feature flags."],["HISTORY","**pixi** was created by **prefix.dev** (makers of mamba) and released in **2023**. Written in Rust, it aims to combine the best of conda, pip, and cargo into a unified tool. It leverages the fast dependency resolver from rattler-build and integrates with the conda ecosystem while adding modern project management features."],["INSTALL","```pacman: sudo pacman -S pixi```\n\n```brew: brew install pixi```\n\n```nix: nix profile install nixpkgs#pixi```"],["SEE ALSO","[conda](/man/conda)(1), [mamba](/man/mamba)(1), [poetry](/man/poetry)(1), [cargo](/man/cargo)(1), [pip](/man/pip)(1)"]],"e":["conda","mamba","poetry","cargo","pip"],"x":"pixi init [project_name]"},"plasma-apply-colorscheme":{"n":"plasma-apply-colorscheme","t":"Apply KDE Plasma color schemes","s":[["TAGLINE","Apply KDE Plasma color schemes"],["TLDR","**List** available color schemes\n\n```plasma-apply-colorscheme --list-schemes```\n\n**Apply** a color scheme\n\n```plasma-apply-colorscheme BreezeLight```\n\nApply **dark** theme\n\n```plasma-apply-colorscheme BreezeDark```\n\nDisplay **help**\n\n```plasma-apply-colorscheme --help```"],["SYNOPSIS","**plasma-apply-colorscheme** [_options_] [_scheme_name_]"],["DESCRIPTION","**plasma-apply-colorscheme** switches the KDE Plasma desktop color scheme. It allows changing themes from the command line without opening System Settings."],["PARAMETERS","**--list-schemes**\n> List all available color schemes\n\n**scheme_name**\n> Name of the color scheme to apply\n\n**--help**\n> Display help information"],["CAVEATS","Requires a running Plasma desktop session. Color scheme names are case-sensitive. Changes take effect immediately without requiring logout."],["HISTORY","**plasma-apply-colorscheme** is part of the **KDE Plasma** desktop environment, providing command-line access to appearance settings."],["INSTALL","```apt: sudo apt install plasma-workspace```\n\n```dnf: sudo dnf install plasma-workspace```\n\n```pacman: sudo pacman -S plasma-workspace```\n\n```apk: sudo apk add plasma-workspace```"],["SEE ALSO","[plasma-apply-wallpaperimage](/man/plasma-apply-wallpaperimage)(1), [plasma-apply-cursortheme](/man/plasma-apply-cursortheme)(1)"]],"e":["plasma-apply-wallpaperimage","plasma-apply-cursortheme"],"x":"plasma-apply-colorscheme --list-schemes"},"pngcheck":{"n":"pngcheck","t":"Verify PNG file integrity and structure","s":[["TAGLINE","Verify PNG file integrity and structure"],["TLDR","**Check PNG file** for integrity\n\n```pngcheck [image.png]```\n\n**Verbose** output (per-chunk details)\n\n```pngcheck -v [image.png]```\n\n**Check multiple files**\n\n```pngcheck [*.png]```\n\n**Colorized** output\n\n```pngcheck -c [image.png]```\n\n**Print text chunks** (titles, comments, etc.)\n\n```pngcheck -t [image.png]```\n\n**Quiet mode** - only show errors\n\n```pngcheck -q [image.png]```\n\n**Search for embedded PNGs** in a file\n\n```pngcheck -s [file]```"],["SYNOPSIS","**pngcheck** [**-cfpqstvx7**] _file_..."],["PARAMETERS","_FILE_\n> PNG, JNG, or MNG file(s) to check.\n\n**-v**\n> Verbose mode; show details of each chunk.\n\n**-c**\n> Colorize text output.\n\n**-f**\n> Force continuation after major errors.\n\n**-p**\n> Print contents of PLTE, tRNS, hIST, sPLT, and PPLT chunks.\n\n**-q**\n> Quiet mode; only show errors and warnings.\n\n**-s**\n> Search for sub-images (embedded PNGs/MNGs) in a file.\n\n**-t**\n> Print contents of tEXt, zTXt, and iTXt text chunks.\n\n**-x**\n> Search for and extract sub-images (use with **-s**).\n\n**-7**\n> Print tEXt chunks in 7-bit clean ASCII (escape high bytes).\n\n**--help**\n> Display help."],["DESCRIPTION","**pngcheck** verifies the integrity and structure of PNG, JNG, and MNG image files. It validates CRC checksums, checks chunk ordering, and reports structural errors or corruption.\n\nIn verbose mode it lists each chunk with dimensions, color type, bit depth, compression method, and filter type. With **-t** it prints embedded text metadata (tEXt, zTXt, iTXt). With **-s** it can locate PNG/MNG images embedded in other files, and **-x** can extract them."],["CAVEATS","PNG/JNG/MNG formats. Read-only analysis."],["HISTORY","pngcheck was created for **PNG file validation** and inspection."],["INSTALL","```apt: sudo apt install pngcheck```\n\n```dnf: sudo dnf install pngcheck```\n\n```zypper: sudo zypper install pngcheck```\n\n```brew: brew install pngcheck```\n\n```nix: nix profile install nixpkgs#pngcheck```"],["SEE ALSO","[pngcrush](/man/pngcrush)(1), [optipng](/man/optipng)(1), [file](/man/file)(1)"]],"e":["pngcrush","optipng","file"],"x":"pngcheck [image.png]"},"pnmmargin":{"n":"pnmmargin","t":"Add solid color borders to PNM images","s":[["TAGLINE","Add solid color borders to PNM images"],["TLDR","**Add border around image**\n\n```pnmmargin [10] [input.pnm] > [output.pnm]```\n\n**Add colored border**\n\n```pnmmargin -color [blue] [20] [input.pnm] > [output.pnm]```"],["SYNOPSIS","**pnmmargin** [_options_] _size_ [_file_]"],["PARAMETERS","_size_\n> Border width in pixels.\n\n**-color** _color_\n> Border color.\n\n**-white**\n> White border.\n\n**-black**\n> Black border.\n\n**-plain**\n> Produce output in plain PNM format."],["DESCRIPTION","**pnmmargin** adds a uniform solid-color border around all four sides of a PNM image. The border width is specified in pixels and is applied equally on all sides.\n\nIf no color option is specified, the program makes a guess. Use **-color** to specify a custom color, or the shortcuts **-white** and **-black** for common choices. This is a convenience wrapper around **pnmpad**. For different margins on each side, use **pnmpad** directly. To remove borders, use **pamcut**. Part of the Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pnmpad](/man/pnmpad)(1), [pamcut](/man/pamcut)(1), [pnmcat](/man/pnmcat)(1), [pnmcrop](/man/pnmcrop)(1)"]],"e":["pnmpad","pamcut","pnmcat","pnmcrop"],"x":"pnmmargin [10] [input.pnm] > [output.pnm]"},"pvw":{"n":"pvw","t":"Terminal-based port viewer","s":[["TAGLINE","Terminal-based port viewer"],["TLDR","**Launch the port viewer TUI**\n\n```pvw```"],["SYNOPSIS","**pvw** [_options_]"],["DESCRIPTION","**pvw** is a terminal-based port viewer that provides a simple and intuitive interface to monitor ports in use on your system. It features keyboard-based navigation for inspecting open and closed ports."],["HISTORY","**pvw** was created by **allyring** and is written in **Go**."],["INSTALL","```aur: yay -S pvw```"],["SEE ALSO","[ss](/man/ss)(8), [netstat](/man/netstat)(8), [lsof](/man/lsof)(8)"]],"e":["ss","netstat","lsof"],"x":"pvw"},"qcp":{"n":"qcp","t":"Copy files by editing the destination filenames in a text editor","s":[["TAGLINE","Copy files by editing the destination filenames in a text editor"],["TLDR","**Edit filenames** and copy matching files in the current directory\n\n```qcp```\n\n**Copy specific files**, editing their destination names in $EDITOR\n\n```qcp [*.txt]```\n\n**Use dual-column format** (source and destination on the same line)\n\n```qcp --format=dual [*.jpg]```\n\n**Preview what would happen** without actually copying\n\n```qcp --dummy [*.log]```\n\n**Recursive listing** like `ls -R`\n\n```qcp -R [src/]```"],["SYNOPSIS","**qcp** [_options_] [_file_...]"],["PARAMETERS","**-f**, **--format=**_FORMAT_\n> Edit format: **destination** (default, edit only the new name), **dual** (two columns with source on the left), or **single** (single column, shared source/destination).\n\n**-o**, **--options=**_OPTIONS_\n> Comma-separated format options (e.g. **source-comments**, **autodetect**).\n\n**-i**, **--interactive**\n> Drop into a command-mode prompt after editing instead of applying immediately.\n\n**-e**, **--editor=**_PROGRAM_\n> Override the editor (defaults to **$VISUAL**, then **$EDITOR**, then **vi**).\n\n**--ls=**_PROGRAM_\n> Use an alternative **ls** implementation to generate the file list.\n\n**--dummy**\n> Print the operations that would be performed but do not copy anything.\n\n**-v**, **--verbose**\n> Show each copy as it is performed.\n\n**--help**, **--version**\n> Display help or version information.\n\nMany **ls**-style flags are passed through: **-a**, **-A**, **-B**, **-d**, **-r**, **-R**, **-S**, **-t**, **-u**, **-U**, **-X**, **--sort=**_WORD_, **--time=**_WORD_."],["DESCRIPTION","**qcp** (quick copy) opens a temporary buffer in **$EDITOR** listing the files you supplied (or the contents of the current directory if none are given) and lets you type the destination names in place. When you save and exit, **qcp** copies each source to the edited destination.\n\nIt is the copy-oriented sibling of **qmv**, the rename utility from the **renameutils** package by Oskar Liljeblad. Because all edits happen in a text editor, common editor features — search and replace, multi-cursor editing, macros — become powerful batch-copy primitives."],["CAVEATS","Part of the **renameutils** package. Not related to any \"quick copy\" progress-bar utility; **qcp** does not display transfer progress. Existing destination files are overwritten without prompting by default — use **--dummy** first if you are unsure."],["INSTALL","```apt: sudo apt install renameutils```\n\n```dnf: sudo dnf install renameutils```\n\n```pacman: sudo pacman -S renameutils```\n\n```zypper: sudo zypper install renameutils```\n\n```brew: brew install renameutils```\n\n```nix: nix profile install nixpkgs#renameutils```"],["SEE ALSO","[qmv](/man/qmv)(1), [cp](/man/cp)(1), [mv](/man/mv)(1), [rsync](/man/rsync)(1)"]],"e":["qmv","cp","mv","rsync"],"x":"qcp"},"qm":{"n":"qm","t":"Manage Proxmox VE virtual machines","s":[["TAGLINE","Manage Proxmox VE virtual machines"],["TLDR","**List** all VMs\n\n```qm list```\n\n**Create** a VM\n\n```qm create 100 --scsi0 local-lvm:4 --net0 e1000 --cdrom local:iso/image.iso```\n\n**Show** VM configuration\n\n```qm config 100```\n\n**Start** a VM\n\n```qm start 100```\n\n**Shutdown** and **wait**\n\n```qm shutdown 100 && qm wait 100```\n\n**Destroy** a VM\n\n```qm destroy 100 --purge```"],["SYNOPSIS","**qm** _command_ [_OPTIONS_] [_vmid_]"],["DESCRIPTION","**qm** is the QEMU/KVM Virtual Machine Manager for Proxmox VE. It provides a comprehensive command-line interface for creating, configuring, starting, stopping, and managing virtual machines."],["PARAMETERS","**list**\n> List all virtual machines\n\n**create** _vmid_\n> Create a new virtual machine\n\n**config** _vmid_\n> Display VM configuration\n\n**set** _vmid_\n> Modify VM configuration\n\n**start** _vmid_\n> Start a virtual machine\n\n**stop** _vmid_\n> Stop a virtual machine (hard shutdown)\n\n**shutdown** _vmid_\n> Gracefully shutdown via ACPI\n\n**destroy** _vmid_\n> Remove a virtual machine\n\n**snapshot** _vmid_ _name_\n> Create a snapshot\n\n**rollback** _vmid_ _name_\n> Rollback to a snapshot\n\n**clone** _vmid_ _newid_\n> Clone a virtual machine\n\n**migrate** _vmid_ _target_\n> Migrate VM to another node\n\n**--purge**\n> Remove all related resources when destroying"],["CONFIGURATION","**/etc/pve/qemu-server/<vmid>.conf**\n> Per-VM configuration file containing hardware settings, disk definitions, network interfaces, and boot options.\n\n**/etc/pve/datacenter.cfg**\n> Cluster-wide datacenter configuration affecting default VM settings and migration behavior."],["CAVEATS","VM IDs must be unique across the Proxmox cluster. Operations may require root privileges. Some commands require the VM to be in a specific state (running or stopped)."],["HISTORY","**qm** is part of the **Proxmox VE** virtualization platform, which combines KVM hypervisor and LXC containers with a web-based management interface."],["INSTALL","```dnf: sudo dnf install qm```"],["SEE ALSO","[qm-start](/man/qm-start)(1), [qm-stop](/man/qm-stop)(1), [qm-config](/man/qm-config)(1), [pct](/man/pct)(1)"]],"e":["qm-start","qm-stop","qm-config","pct"],"x":"qm list"},"rain":{"n":"rain","t":"Interactive AWS CloudFormation deployment tool","s":[["TAGLINE","Interactive AWS CloudFormation deployment tool"],["TLDR","**List all CloudFormation stacks** in the current region\n\n```rain ls```\n\n**Deploy a CloudFormation template**\n\n```rain deploy [template.yaml] [stack-name]```\n\n**Get the template from a running stack**\n\n```rain cat [stack-name]```\n\n**Show event logs** for a stack\n\n```rain logs [stack-name]```\n\n**Format a CloudFormation template**\n\n```rain fmt [template.yaml]```\n\n**Compare two templates**\n\n```rain diff [template1.yaml] [template2.yaml]```\n\n**Delete a CloudFormation stack**\n\n```rain rm [stack-name]```\n\n**Predict deployment failures** before deploying\n\n```rain forecast [template.yaml] [stack-name]```"],["SYNOPSIS","**rain** _command_ [_options_] [_arguments_]"],["PARAMETERS","**ls**\n> List CloudFormation stacks or changesets in the current region\n\n**deploy** _template_ _stack-name_\n> Deploy a CloudFormation stack from a local template\n\n**cat** _stack-name_\n> Retrieve and display the template of a running stack\n\n**logs** _stack-name_\n> Show the event log for the specified stack\n\n**rm** _stack-name_\n> Delete a CloudFormation stack\n\n**fmt** _template_\n> Format CloudFormation templates to a consistent style\n\n**diff** _template1_ _template2_\n> Compare two CloudFormation templates\n\n**build** _resource-type_\n> Create CloudFormation templates for specified resource types\n\n**forecast** _template_ _stack-name_\n> Predict potential deployment failures\n\n**pkg** _template_\n> Package local artifacts for deployment (replacement for aws cloudformation package)\n\n**bootstrap**\n> Create the S3 artifacts bucket for deployments\n\n**--region**, **-r** _region_\n> Specify AWS region\n\n**--profile**, **-p** _profile_\n> Use a specific AWS profile\n\n**--experimental**, **-x**\n> Enable experimental commands\n\n**--help**, **-h**\n> Display help information"],["DESCRIPTION","**rain** is a development workflow tool for AWS CloudFormation that simplifies template authoring, validation, and stack management. It provides a more user-friendly interface than the standard AWS CLI for CloudFormation operations.\n\nKey improvements over the AWS CLI include live deployment progress with colored output, automatic template formatting, template comparison, and deployment failure prediction. Rain can detect potential issues before deployment, saving time and reducing failed stack operations.\n\nThe tool supports CloudFormation modules and can package local artifacts like Lambda code for deployment. It handles the complexity of S3 bucket management for artifacts automatically."],["CAVEATS","Requires AWS credentials configured via environment variables, AWS CLI profiles, or IAM roles. Ensure the AWS account has appropriate CloudFormation and related service permissions.\n\nSome commands like **forecast** are experimental and require the **-x** flag. Experimental features may change between versions.\n\nRain operates on one region at a time. Use **--region** to specify a different region than the default."],["HISTORY","Rain was developed by the **AWS CloudFormation team** and released as an open-source tool to improve the CloudFormation development experience. The name is a play on \"what happens when you have a lot of CloudFormation.\" It is written in Go and actively maintained on GitHub."],["INSTALL","```brew: brew install rain```\n\n```nix: nix profile install nixpkgs#rain```"],["SEE ALSO","[aws](/man/aws)(1), [terraform](/man/terraform)(1), [sam](/man/sam)(1), [cdk](/man/cdk)(1)"]],"e":["aws","terraform","sam","cdk"],"x":"rain ls"},"rdap":{"n":"rdap","t":"Query domain and IP registration data","s":[["TAGLINE","Query domain and IP registration data"],["TLDR","**Query domain**\n\n```rdap [example.com]```\n\n**Query IP address**\n\n```rdap [192.0.2.1]```\n\n**Query ASN**\n\n```rdap [AS15169]```\n\n**JSON output**\n\n```rdap -j [example.com]```\n\n**WHOIS-style output**\n\n```rdap -w [example.com]```\n\n**Verbose output**\n\n```rdap -v [example.com]```\n\n**Use specific RDAP server**\n\n```rdap -s [https://rdap.verisign.com/com/v1] [example.com]```"],["SYNOPSIS","**rdap** [_options_] _query_"],["PARAMETERS","**-j**, **--json**\n> Output JSON, pretty-printed\n\n**-r**, **--raw**\n> Output raw server response\n\n**-w**, **--whois**\n> Output WHOIS style (domain queries only)\n\n**-v**, **--verbose**\n> Print verbose messages on stderr\n\n**-s**, **--server**=_URL_\n> Use specific RDAP server\n\n**-T**, **--timeout**=_SECS_\n> Timeout after specified seconds (default: 30)\n\n**-k**, **--insecure**\n> Disable SSL certificate verification\n\n**--text**\n> Output plain text tree format (default)\n\n**-t**, **--type**=_TYPE_\n> RDAP query type (normally auto-detected): domain, ip, autnum, entity, nameserver, url"],["DESCRIPTION","**rdap** queries Registration Data Access Protocol servers for domain, IP, and ASN registration information. RDAP is the modern replacement for WHOIS with structured data and authentication support."],["EXAMPLES","```bash"],["Domain lookup","rdap example.com"],["IP address lookup","rdap 8.8.8.8"],["ASN lookup","rdap AS15169"],["JSON output","rdap -j google.com"],["Specific server","rdap -s https://rdap.verisign.com/com/v1 example.com"],["IPv6 address","rdap 2001:4860:4860::8888\n```"],["QUERY TYPES","Query type is normally auto-detected, but can be specified with **--type**:\n\n```\ndomain     - Domain registration\nip         - IP address/network\nautnum     - Autonomous System Number\nentity     - Registrant entity\nnameserver - Name server\nurl        - Direct RDAP URL\n```"],["CAVEATS","Not all TLDs support RDAP. Some data may require authentication. Successor to WHOIS."],["HISTORY","RDAP was developed by the **IETF** starting in 2012 as a structured, secure replacement for the WHOIS protocol."],["INSTALL","```apt: sudo apt install rdap```\n\n```zypper: sudo zypper install rdap```\n\n```brew: brew install rdap```\n\n```nix: nix profile install nixpkgs#rdap```"],["SEE ALSO","[whois](/man/whois)(1), [dig](/man/dig)(1), [host](/man/host)(1)"]],"e":["whois","dig","host"],"x":"rdap [example.com]"},"rolldice":{"n":"rolldice","t":"Roll virtual dice from the command line","s":[["TAGLINE","Roll virtual dice from the command line"],["TLDR","Roll a single **20 sided** dice\n\n```rolldice d20```\n\nRoll two six sided dice and **drop the lowest**\n\n```rolldice 2d6s1```\n\nRoll with a **modifier**\n\n```rolldice 2d20+5```\n\nRoll a dice **multiple times**\n\n```rolldice 2xd20```"],["SYNOPSIS","**rolldice** _dice_specification_"],["PARAMETERS","Dice are specified in **NdS** format where:\n> N = number of dice, d = dice indicator, S = number of sides\n\n**s**_N_\n> Drop lowest N rolls\n\n**+**_N_ or **-**_N_\n> Add or subtract modifier\n\n**x**\n> Repeat roll multiple times"],["DESCRIPTION","**rolldice** simulates rolling dice with various configurations. It supports standard dice notation used in tabletop gaming, including modifiers, dropping dice, and multiple rolls.\n\nThe tool is useful for tabletop RPG players, game developers, or anyone needing random dice results."],["CAVEATS","Uses system random number generator. Results are pseudorandom, not cryptographically secure."],["HISTORY","Created as a utility for tabletop gaming and random number generation using standard dice notation."],["INSTALL","```brew: brew install rolldice```"],["SEE ALSO","[shuf](/man/shuf)(1), [random](/man/random)(4)"]],"e":["shuf","random"],"x":"rolldice d20"},"spark":{"n":"spark","t":"generate sparkline graphs from numbers in the terminal","s":[["TAGLINE","generate sparkline graphs from numbers in the terminal"],["TLDR","**Generate a sparkline from numbers**\n\n```spark [1] [5] [22] [13] [53]```\n\n**Generate a sparkline from comma-separated input via stdin**\n\n```echo \"[1,2,3,4,5]\" | spark```\n\n**Generate a sparkline from a file**\n\n```spark < [data.txt]```\n\n**Visualize random data from a sequence**\n\n```seq [100] | sort -R | head -20 | spark```\n\n**Display help**\n\n```spark -h```"],["SYNOPSIS","**spark** [**-h**] [_numbers_...]"],["DESCRIPTION","**spark** generates sparkline graphs from a list of numbers, rendering them as Unicode block characters (▁▂▃▄▅▆▇█) in the terminal. Values are mapped proportionally across eight height levels, with the minimum value getting the shortest bar and the maximum the tallest.\n\nNumbers can be provided as command-line arguments, piped via stdin, or read from a file. Input supports comma-separated, space-separated, and newline-separated formats, making it easy to integrate with other Unix tools through pipes.\n\nThe output is plain text using standard Unicode characters, so it works in any terminal that supports Unicode. Sparklines are useful for visualizing trends in data at a glance without requiring a full graphing tool."],["CAVEATS","Terminal must support Unicode. Limited resolution (8 levels). No axes or labels. Written as a bash shell script with no dependencies beyond bash."],["HISTORY","**spark** was created by **Zach Holman** as a simple bash script for command-line data visualization. The concept inspired many alternative implementations in Ruby, Python, Haskell, Java, and other languages."],["INSTALL","```brew: brew install spark```\n\n```nix: nix profile install nixpkgs#spark```"],["SEE ALSO","[gnuplot](/man/gnuplot)(1), [termgraph](/man/termgraph)(1)"]],"e":["gnuplot","termgraph"],"x":"spark [1] [5] [22] [13] [53]"},"stolonctl":{"n":"stolonctl","t":"Stolon PostgreSQL HA cluster manager","s":[["TAGLINE","Stolon PostgreSQL HA cluster manager"],["TLDR","**Get cluster status**\n\n```stolonctl --cluster-name [cluster] status```\n\n**Initialize cluster**\n\n```stolonctl --cluster-name [cluster] init```\n\n**Get cluster spec**\n\n```stolonctl --cluster-name [cluster] spec```\n\n**Update spec**\n\n```stolonctl --cluster-name [cluster] update --patch '[{\"key\":\"value\"}]'```\n\n**Failkeeper**\n\n```stolonctl --cluster-name [cluster] failkeeper [keeper-id]```"],["SYNOPSIS","**stolonctl** [_--cluster-name name_] _command_ [_options_]"],["PARAMETERS","**--cluster-name** _NAME_\n> Cluster name.\n\n**status**\n> Show status.\n\n**init**\n> Initialize cluster.\n\n**spec**\n> Show specification.\n\n**update**\n> Update configuration.\n\n**--store-backend** _TYPE_\n> Backend type."],["DESCRIPTION","**stolonctl** is the command-line management interface for Stolon, a cloud-native PostgreSQL high-availability manager. It provides administrative control over Stolon clusters, including viewing cluster status, initializing new clusters, updating configuration specifications, and triggering manual failovers.\n\nThe tool communicates with Stolon's store backend (typically etcd or Consul) to read and modify cluster state. The cluster specification defines replication settings, synchronous replication policies, automatic failover behavior, and PostgreSQL configuration parameters. Changes applied through stolonctl are picked up by the Stolon components (sentinels, keepers, and proxies) running in the cluster.\n\nStolonctl is essential for day-to-day operations such as inspecting which keeper is the current master, forcing a failover to a different keeper, and adjusting cluster parameters without downtime. It is commonly deployed alongside Kubernetes-based Stolon installations but also works with standalone deployments."],["CAVEATS","Stolon deployment required. Kubernetes or etcd. PostgreSQL specific."],["HISTORY","**stolonctl** is the CLI for **Stolon**, a cloud-native PostgreSQL high-availability manager."],["SEE ALSO","[psql](/man/psql)(1), [pg_ctl](/man/pg_ctl)(1), [kubectl](/man/kubectl)(1)"]],"e":["psql","pg_ctl","kubectl"],"x":"stolonctl --cluster-name [cluster] status"},"systemctl-preset-all":{"n":"systemctl-preset-all","t":"Reset all units to preset defaults","s":[["TAGLINE","Reset all units to preset defaults"],["TLDR","Reset **all** units to preset defaults\n\n```sudo systemctl preset-all```\n\n**Enable** only mode\n\n```sudo systemctl preset-all --preset-mode enable-only```\n\n**Disable** only mode\n\n```sudo systemctl preset-all --preset-mode disable-only```\n\n**Quiet** mode\n\n```sudo systemctl preset-all -q```"],["SYNOPSIS","**systemctl preset-all** [_OPTIONS_]"],["PARAMETERS","**--preset-mode=** _MODE_\n> full (default), enable-only, or disable-only\n\n**-q, --quiet**\n> Suppress output, return only exit code\n\n**--user**\n> Operate on user units"],["DESCRIPTION","**systemctl preset-all** resets the enablement state of all installed unit files to the defaults defined in preset policy files. This applies distribution default settings to every unit on the system.\n\nThis is typically run once after initial system installation or when resetting to distribution defaults."],["CAVEATS","This is a system-wide operation that can change many service enablement states. Use `--preset-mode` to limit to only enabling or disabling. Backup your current configuration before running."],["HISTORY","The **preset-all** subcommand provides a way to reset the entire system to distribution-default service configuration, useful for recovery or standardization."],["SEE ALSO","[systemctl-preset](/man/systemctl-preset)(1), [systemctl-enable](/man/systemctl-enable)(1)"]],"e":["systemctl-preset","systemctl-enable"],"x":"sudo systemctl preset-all"},"systemd-tty-ask-password-agent":{"n":"systemd-tty-ask-password-agent","t":"Handle boot-time password requests","s":[["TAGLINE","Handle boot-time password requests"],["TLDR","**List** pending password requests\n\n```systemd-tty-ask-password-agent --list```\n\n**Watch** and process requests continuously\n\n```systemd-tty-ask-password-agent --watch```\n\n**Query** user for pending passwords\n\n```systemd-tty-ask-password-agent --query```\n\nForward to **wall** message\n\n```systemd-tty-ask-password-agent --wall```"],["SYNOPSIS","**systemd-tty-ask-password-agent** [_OPTIONS_]"],["PARAMETERS","**--list**\n> List pending password requests\n\n**--watch**\n> Continuously process password requests\n\n**--query**\n> Process pending requests by querying user on current TTY\n\n**--wall**\n> Forward requests as wall messages\n\n**--console**\n> Ask on console instead of current TTY"],["DESCRIPTION","**systemd-tty-ask-password-agent** handles password requests from systemd services. During boot, services like disk encryption may need passwords; this agent collects and forwards them.\n\nThe agent monitors password request files and presents prompts to users on appropriate terminals."],["CAVEATS","Used primarily during boot for disk encryption passwords. Multiple agents can run simultaneously. The --wall option broadcasts to all terminals."],["HISTORY","**systemd-tty-ask-password-agent** provides the user-facing component of systemd's password query infrastructure, enabling early-boot password prompts without a full login environment."],["INSTALL","```apt: sudo apt install systemd```\n\n```dnf: sudo dnf install systemd```\n\n```pacman: sudo pacman -S systemd```\n\n```zypper: sudo zypper install systemd```\n\n```brew: brew install systemd```\n\n```nix: nix profile install nixpkgs#systemd```"],["SEE ALSO","[systemd-ask-password](/man/systemd-ask-password)(1), [cryptsetup](/man/cryptsetup)(8)"]],"e":["systemd-ask-password","cryptsetup"],"x":"systemd-tty-ask-password-agent --list"},"ttar":{"n":"ttar","t":"Tiny freestanding tar archiver written in C","s":[["TAGLINE","Tiny freestanding tar archiver written in C"],["TLDR","**Create an archive** from files\n\n```ttar c [archive.tar] [file1] [file2]```\n\n**Extract** an archive\n\n```ttar x [archive.tar]```"],["SYNOPSIS","**ttar** **c** _archive.tar_ _files..._\n\n**ttar** **x** _archive.tar_"],["DESCRIPTION","**ttar** (tiny tar) is a minimal tar archiver written in freestanding C. It uses only raw system calls, with no libc, no external libraries, and no other dependencies, producing a very small static binary (about 2.4 KB on arm64). It targets multiple architectures including arm64, arm-v7, mips, riscv, x86, and i386.\n\nThe tool supports just two operations: creating an uncompressed tar archive from a list of files, and extracting an existing tar archive. It is aimed at minimal, embedded, or bootstrap environments where a full **tar** implementation is unavailable or too large."],["PARAMETERS","**c** _archive.tar_ _files..._\n> Create _archive.tar_ containing the listed files.\n\n**x** _archive.tar_\n> Extract the contents of _archive.tar_ into the current directory."],["CAVEATS","The mode must be a single **c** or **x** as the first argument; any other value exits with an error. ttar does not compress archives and does not provide listing, appending, or filtering options found in full tar implementations. Use a dedicated compressor such as **gzip** on the resulting archive if compression is needed."],["HISTORY","**ttar** was released as a demonstration of a libc-free, syscall-only archiver in pure C, emphasizing an extremely small binary footprint. It is distributed under the GPL-3.0 license."],["SEE ALSO","[tar](/man/tar)(1), [cpio](/man/cpio)(1), [pax](/man/pax)(1), [ar](/man/ar)(1), [gzip](/man/gzip)(1)"],["RESOURCES","```[Source code](https://github.com/Ferki-git-creator/ttar-tiny-tar-archivist)```"]],"e":["tar","cpio","pax","ar","gzip"],"x":"ttar c [archive.tar] [file1] [file2]"},"update-manager":{"n":"update-manager","t":"Ubuntu graphical software update manager","s":[["TAGLINE","Ubuntu graphical software update manager"],["TLDR","**Launch the Software Updater** GUI\n\n```update-manager```\n\n**Check for distribution upgrade**\n\n```update-manager -c```\n\n**Check for updates** and launch GUI\n\n```update-manager -d```\n\n**Launch with no focus** (for notifications)\n\n```update-manager --no-focus-on-map```\n\n**Check for updates from proposed repository**\n\n```update-manager -p```"],["SYNOPSIS","**update-manager** [**-c**|**--check-dist-upgrades**] [**-d**|**--devel-release**] [**-p**|**--proposed**] [**--no-focus-on-map**]"],["PARAMETERS","**-c**, **--check-dist-upgrades**\n> Check if a new distribution release is available for upgrade.\n\n**-d**, **--devel-release**\n> Check for upgrades to the latest development release.\n\n**-p**, **--proposed**\n> Include updates from the proposed repository (pre-release updates).\n\n**--no-focus-on-map**\n> Do not focus the window when it appears. Useful for background notifications.\n\n**--no-update**\n> Do not check for updates when starting.\n\n**-h**, **--help**\n> Show help message."],["DESCRIPTION","**update-manager** is Ubuntu's graphical frontend for the apt package management system. Known as \"Software Updater\" in the application menu, it provides a user-friendly interface for checking and installing system updates.\n\nThe tool checks for available package updates, displays them with descriptions and sizes, and allows users to selectively install updates. It also detects when new Ubuntu releases are available and offers to perform distribution upgrades.\n\nupdate-manager handles security updates, regular package updates, and major version upgrades. It integrates with Ubuntu's notification system to alert users when updates are available. For servers or command-line environments, the equivalent functionality is provided by **apt** or **apt-get**."],["CAVEATS","Requires a graphical environment (X11 or Wayland). For headless servers, use apt-get or apt instead. Distribution upgrades should be performed with adequate preparation and backups. Some updates may require a system restart to take effect."],["HISTORY","**update-manager** was developed by Canonical as part of Ubuntu's user-friendly desktop experience. It first appeared in early Ubuntu releases around **2004-2005** to make system updates accessible to users unfamiliar with command-line package management. The tool has evolved to include distribution upgrade capabilities and integration with Ubuntu's Long Term Support (LTS) release cycle."],["SEE ALSO","[apt](/man/apt)(8), [apt-get](/man/apt-get)(8), [do-release-upgrade](/man/do-release-upgrade)(8), [unattended-upgrades](/man/unattended-upgrades)(8)"]],"e":["apt","apt-get","do-release-upgrade","unattended-upgrades"],"x":"update-manager"},"vagrant-box":{"n":"vagrant-box","t":"Manage Vagrant base box images","s":[["TAGLINE","Manage Vagrant base box images"],["TLDR","**List boxes**\n\n```vagrant box list```\n\n**Add box**\n\n```vagrant box add [name]```\n\n**Remove box**\n\n```vagrant box remove [name]```\n\n**Update boxes**\n\n```vagrant box update```\n\n**Prune old versions**\n\n```vagrant box prune```"],["SYNOPSIS","**vagrant** **box** _command_ [_options_]"],["PARAMETERS","**list**\n> List installed boxes.\n\n**add** _name_\n> Download and add box.\n\n**remove** _name_\n> Remove box.\n\n**update**\n> Update boxes.\n\n**prune**\n> Remove old box versions.\n\n**outdated**\n> Check for updates.\n\n**--provider** _name_\n> Specify provider.\n\n**--box-version** _version_\n> Specify version."],["DESCRIPTION","**vagrant box** manages Vagrant boxes. Boxes are base images used to create virtual machines. Download from Vagrant Cloud or import from local files."],["INSTALL","```apt: sudo apt install vagrant```\n\n```dnf: sudo dnf install vagrant```\n\n```nix: nix profile install nixpkgs#vagrant```"],["SEE ALSO","[vagrant](/man/vagrant)(1), [vagrant-init](/man/vagrant-init)(1)"]],"e":["vagrant","vagrant-init"],"x":"vagrant box list"},"varnishlog":{"n":"varnishlog","t":"Display Varnish HTTP request logs","s":[["TAGLINE","Display Varnish HTTP request logs"],["TLDR","**Show live logs**\n\n```varnishlog```\n\n**Include only specific tag**\n\n```varnishlog -i [ReqURL]```\n\n**Filter by query**\n\n```varnishlog -q \"[RespStatus == 500]\"```\n\n**Client requests only**\n\n```varnishlog -c```\n\n**Backend requests only**\n\n```varnishlog -b```\n\n**Group by request**\n\n```varnishlog -g request```"],["SYNOPSIS","**varnishlog** [_-c_] [_-b_] [_-q query_] [_-i tag_] [_options_]"],["PARAMETERS","**-c**\n> Show client-side transactions only.\n\n**-b**\n> Show backend-side transactions only.\n\n**-q** _QUERY_\n> VSL query expression to filter transactions.\n\n**-i** _TAGLIST_\n> Include only records with specified tags (comma-separated).\n\n**-I** _TAGREGEX_\n> Include only records matching tag and regex pattern.\n\n**-x** _TAGLIST_\n> Exclude records with specified tags.\n\n**-X** _TAGREGEX_\n> Exclude records matching tag and regex pattern.\n\n**-g** _MODE_\n> Grouping mode (raw, vxid, request, session).\n\n**-d**\n> Process old log entries first.\n\n**-w** _FILENAME_\n> Write output to file."],["DESCRIPTION","**varnishlog** reads the Varnish Shared Memory Log (VSL) and displays detailed request and response information in real time. It shows the full lifecycle of HTTP transactions including client requests, backend fetches, cache decisions, and response headers.\n\nThe tool provides powerful filtering through the VSL query language, allowing you to isolate specific transactions by status code, URL pattern, or any logged field. Client-side (-c) and backend-side (-b) views can be shown independently, and grouping modes organize output by request, session, or raw log entries. This makes it the primary debugging tool for understanding Varnish cache behavior."],["CAVEATS","Requires a running Varnish instance. Familiarity with VSL query language is helpful for effective filtering. Output can be very verbose without filters."],["HISTORY","**varnishlog** is part of **Varnish Cache**, displaying the Varnish Shared Memory Log for debugging."],["INSTALL","```apt: sudo apt install varnish```\n\n```dnf: sudo dnf install varnish```\n\n```apk: sudo apk add varnish```\n\n```brew: brew install varnish```\n\n```nix: nix profile install nixpkgs#varnish```"],["SEE ALSO","[varnishhist](/man/varnishhist)(1), [varnishstat](/man/varnishstat)(1), [varnishd](/man/varnishd)(1)"]],"e":["varnishhist","varnishstat","varnishd"],"x":"varnishlog"},"virsh-pool-destroy":{"n":"virsh-pool-destroy","t":"Stop a running storage pool","s":[["TAGLINE","Stop a running storage pool"],["TLDR","**Stop a running storage pool by name**\n\n```virsh pool-destroy [pool]```"],["SYNOPSIS","**virsh** **pool-destroy** _pool_"],["DESCRIPTION","**virsh pool-destroy** stops (deactivates) a running storage pool. Despite the name, it does not delete the pool's data or its persistent configuration. The pool must be active for this command to succeed. After destroying a pool, it can be restarted with **pool-start**. To remove the configuration, use **pool-undefine**. To delete the underlying storage data, use **pool-delete** (the pool must be stopped first)."],["SEE ALSO","[virsh](/man/virsh)(1), [virsh-pool-start](/man/virsh-pool-start)(1), [virsh-pool-delete](/man/virsh-pool-delete)(1), [virsh-pool-undefine](/man/virsh-pool-undefine)(1)"]],"e":["virsh","virsh-pool-start","virsh-pool-delete","virsh-pool-undefine"],"x":"virsh pool-destroy [pool]"},"viu":{"n":"viu","t":"Display images in the terminal","s":[["TAGLINE","Display images in the terminal"],["TLDR","**Display image**\n\n```viu [image.png]```\n\n**Set width**\n\n```viu -w [80] [image.png]```\n\n**Set height**\n\n```viu -h [40] [image.png]```\n\n**Display multiple images**\n\n```viu [image1.png] [image2.png]```\n\n**Use blocks instead of half-blocks**\n\n```viu -b [image.png]```\n\n**Transparent background**\n\n```viu -t [image.png]```\n\n**From stdin**\n\n```cat [image.png] | viu -```"],["SYNOPSIS","**viu** [_-w width_] [_-h height_] [_options_] _files_"],["PARAMETERS","**-w**, **--width** _N_\n> Output width.\n\n**-h**, **--height** _N_\n> Output height.\n\n**-b**, **--blocks**\n> Use full blocks.\n\n**-t**, **--transparent**\n> Transparent background.\n\n**-n**, **--name**\n> Show filename.\n\n**-r**, **--recursive**\n> Recursive directory.\n\n**-f**, **--frame-rate** _N_\n> GIF frame rate."],["DESCRIPTION","**viu** is a terminal image viewer written in Rust that renders images directly in the terminal using Unicode half-block characters and 24-bit ANSI color codes. It supports common image formats including PNG, JPEG, GIF, WebP, and BMP, displaying them at a resolution determined by the terminal's character grid.\n\nOutput dimensions can be controlled with width and height options, and aspect ratio is preserved by default when only one dimension is specified. An alternative block rendering mode uses full block characters for a different visual density, and transparent backgrounds can be enabled for images with alpha channels.\n\nAnimated GIFs play directly in the terminal with configurable frame rates. The tool also reads from standard input, enabling pipeline integration where images can be fetched with curl or generated by other programs and piped directly to viu for display."],["CAVEATS","Quality depends on terminal. True color support improves output. Large images slow."],["HISTORY","**viu** is a Rust-based terminal image viewer. It provides fast, high-quality image display in modern terminals."],["INSTALL","```pacman: sudo pacman -S viu```\n\n```apk: sudo apk add viu```\n\n```zypper: sudo zypper install viu```\n\n```brew: brew install viu```\n\n```nix: nix profile install nixpkgs#viu```"],["SEE ALSO","[chafa](/man/chafa)(1), [catimg](/man/catimg)(1), [tiv](/man/tiv)(1)"]],"e":["chafa","catimg","tiv"],"x":"viu [image.png]"},"warpd":{"n":"warpd","t":"Modal keyboard-driven virtual pointer","s":[["TAGLINE","Modal keyboard-driven virtual pointer"],["TLDR","Start the warpd **daemon** in the background\n\n```warpd```\n\nStart in the **foreground** for debugging\n\n```warpd -f```\n\nRun in **hint** mode (label screen locations)\n\n```warpd --hint```\n\nRun in **grid** mode (divide screen into quadrants)\n\n```warpd --grid```\n\nRun in **normal** mode (vi-like cursor movement)\n\n```warpd --normal```\n\nRun in hint mode and **exit** after selection (oneshot)\n\n```warpd --hint --oneshot```\n\n**List** all configurable options\n\n```warpd --list-options```\n\nUse a custom **config** file\n\n```warpd -c [path/to/config]```"],["SYNOPSIS","**warpd** [_OPTIONS_]"],["PARAMETERS","**-f**, **--foreground**\n> Run in the foreground (useful for debugging).\n\n**-l**, **--list-keys**\n> Print a list of valid keys which can be used as config values.\n\n**--list-options**\n> Print all configurable options.\n\n**-v**, **--version**\n> Display version information.\n\n**-c**, **--config** _file_\n> Use the provided config file (use - for stdin).\n\n**--hint**\n> Run in daemonless hint mode.\n\n**--hint2**\n> Run in two-stage hint mode.\n\n**--grid**\n> Run in grid mode (divides screen into quadrants for selection).\n\n**--normal**\n> Run in normal mode (vi-like cursor movement).\n\n**--history**\n> Run in history hint mode (hints over previously selected targets).\n\n**--screen**\n> Run in screen selection mode (for multi-monitor setups).\n\n**--oneshot**\n> Exit after mode completion instead of dropping into normal mode.\n\n**--click** _button_\n> Send a mouse click and exit.\n\n**--move** _'x y'_\n> Position pointer at the specified coordinates."],["KEY BINDINGS","**h**, **j**, **k**, **l**\n> Move cursor left, down, up, right (normal mode).\n\n**H**, **M**, **L**\n> Jump to left edge, middle, right edge of screen.\n\n**e**, **y**\n> Scroll down, scroll up.\n\n**m**\n> Left click.\n\n**,**\n> Middle click.\n\n**.**\n> Right click.\n\n**n**\n> Activate hint mode from normal mode."],["DESCRIPTION","**warpd** is a modal keyboard-driven pointer manipulation program. It provides three main modes for moving the mouse cursor without a physical mouse, using an interaction model inspired by vi.\n\n**Normal mode** (activated by default with A-M-c) provides vi-like cursor movement using hjkl keys, with support for numeric multipliers to control movement distance. **Hint mode** (A-M-x) overlays labeled hints on screen locations, allowing quick pointer warping by typing the corresponding label. **Grid mode** (A-M-g) divides the screen into a 2x2 grid, recursively subdividing on each selection to narrow down the target location.\n\nAdditional modes include **history mode** which shows hints over previously selected targets, and **screen mode** for navigating between multiple monitors. Configuration is done via `~/.config/warpd/config`."],["CAVEATS","Requires X11 or Wayland (with wlroots-based compositors). The daemon must be running for activation key bindings to work. Key bindings may conflict with other applications. Hint mode may not label all clickable elements depending on the platform."],["INSTALL","```nix: nix profile install nixpkgs#warpd```"],["SEE ALSO","[xdotool](/man/xdotool)(1), [keynav](/man/keynav)(1), [xbindkeys](/man/xbindkeys)(1)"]],"e":["xdotool","keynav","xbindkeys"],"x":"warpd"},"xargs":{"n":"xargs","t":"Build and execute commands from stdin","s":[["TAGLINE","Build and execute commands from stdin"],["TLDR","**Run command with arguments from stdin**\n\n```echo [file1 file2] | xargs rm```\n\n**Use custom delimiter**\n\n```echo \"[a:b:c]\" | xargs -d \":\" echo```\n\n**Run with placeholder**\n\n```find . -name \"*.txt\" | xargs -I {} cp {} [/backup/]```\n\n**Run in parallel**\n\n```find . -name \"*.jpg\" | xargs -P [4] -I {} convert {} {}.png```\n\n**Handle filenames with spaces**\n\n```find . -name \"*.txt\" -print0 | xargs -0 rm```\n\n**Limit arguments per command**\n\n```echo {1..100} | xargs -n [10] echo```\n\n**Prompt before execution**\n\n```find . -name \"*.tmp\" | xargs -p rm```\n\n**Skip execution if input is empty** (GNU extension)\n\n```echo \"\" | xargs -r echo \"not empty\"```"],["SYNOPSIS","**xargs** [_-I replace_] [_-n max-args_] [_-P max-procs_] [_-0_] [_-d delim_] [_command_]"],["PARAMETERS","**-I** _REPLACE_\n> Replace string in command.\n\n**-n** _NUM_, **--max-args** _NUM_\n> Maximum arguments per command.\n\n**-P** _NUM_, **--max-procs** _NUM_\n> Parallel processes.\n\n**-0**, **--null**\n> Input items are null-terminated.\n\n**-d** _DELIM_, **--delimiter** _DELIM_\n> Input delimiter character.\n\n**-p**, **--interactive**\n> Prompt before each execution.\n\n**-t**, **--verbose**\n> Print commands before execution.\n\n**-r**, **--no-run-if-empty**\n> Don't run if input is empty.\n\n**-L** _NUM_, **--max-lines** _NUM_\n> Use NUM lines per command.\n\n**-s** _NUM_, **--max-chars** _NUM_\n> Maximum command line length.\n\n**-a** _FILE_, **--arg-file** _FILE_\n> Read arguments from file.\n\n**-x**, **--exit**\n> Exit if command line too long.\n\n**--show-limits**\n> Show system limits."],["DESCRIPTION","**xargs** builds and executes commands from standard input. It converts input into arguments for a command, handling batching, parallelization, and argument limits.\n\nBy default, xargs appends all input as arguments to a single command invocation. The -n option limits arguments per invocation, running the command multiple times.\n\nThe -I flag enables argument placement anywhere in the command. {} (or any placeholder) is replaced with each input item. This runs the command once per input item.\n\nParallel execution (-P) runs multiple commands simultaneously. Combined with -n or -I, this processes items concurrently, utilizing multiple CPU cores.\n\nNull-delimited input (-0) handles filenames with spaces, newlines, or special characters safely. Use with find -print0 or similar tools.\n\nWithout -r, xargs runs the command even with empty input. The -r flag prevents this, useful when empty input would cause errors."],["CAVEATS","Special characters in filenames cause issues without -0. Shell features (pipes, redirects) need wrapper scripts. Argument limit depends on system. Error handling across parallel jobs is limited. Order not preserved with -P."],["HISTORY","**xargs** originated in **PWB/UNIX** in the **1970s**. It addressed the limitation of shell command line length - programs couldn't accept unlimited arguments. The tool became essential for chaining Unix commands, enabling \"do something to many files\" patterns that define Unix philosophy."],["INSTALL","```apt: sudo apt install findutils```\n\n```dnf: sudo dnf install findutils```\n\n```pacman: sudo pacman -S findutils```\n\n```apk: sudo apk add findutils```\n\n```zypper: sudo zypper install findutils```\n\n```brew: brew install findutils```\n\n```nix: nix profile install nixpkgs#findutils```"],["SEE ALSO","[find](/man/find)(1), [parallel](/man/parallel)(1), [apply](/man/apply)(1)"]],"e":["find","parallel","apply"],"x":"echo [file1 file2] | xargs rm"},"xml-depyx":{"n":"xml-depyx","t":"Convert PYX notation back to XML","s":[["TAGLINE","Convert PYX notation back to XML"],["TLDR","**Convert PYX to XML**\n\n```xml depyx [file.pyx]```\n\n**Convert from stdin**\n\n```cat [file.pyx] | xml depyx```"],["SYNOPSIS","**xml depyx** [_options_] [_file_]"],["PARAMETERS","_file_\n> Input PYX file."],["DESCRIPTION","**xml depyx** converts PYX notation back to XML. It is part of the **xmlstarlet** toolkit and reconstructs XML documents from the line-oriented PYX format (based on ESIS, ISO 8879) after text processing with line-oriented tools such as **awk**, **sed**, or **grep**.\n\nThe inverse operation is performed by **xml pyx** (alias **xml p2x**), which converts XML into PYX."],["INSTALL","```dnf: sudo dnf install xmlstarlet```\n\n```pacman: sudo pacman -S xmlstarlet```\n\n```apk: sudo apk add xmlstarlet```\n\n```zypper: sudo zypper install xmlstarlet```\n\n```brew: brew install xmlstarlet```\n\n```nix: nix profile install nixpkgs#xmlstarlet```"],["SEE ALSO","[xml-pyx](/man/xml-pyx)(1), [xml-p2x](/man/xml-p2x)(1), [xml-format](/man/xml-format)(1), [xml-select](/man/xml-select)(1)"]],"e":["xml-pyx","xml-p2x","xml-format","xml-select"],"x":"xml depyx [file.pyx]"},"yt-dlp":{"n":"yt-dlp","t":"Feature-rich video downloader fork","s":[["TAGLINE","Feature-rich video downloader fork"],["TLDR","**Download a video**\n\n```yt-dlp [https://youtube.com/watch?v=VIDEO_ID]```\n\n**Download audio only** (best quality)\n\n```yt-dlp -x [url]```\n\n**Download audio as MP3**\n\n```yt-dlp -x --audio-format mp3 [url]```\n\n**List available formats**\n\n```yt-dlp -F [url]```\n\n**Download specific format**\n\n```yt-dlp -f [format_code] [url]```\n\n**Download best video + audio**\n\n```yt-dlp -f \"bestvideo+bestaudio\" [url]```\n\n**Download entire playlist**\n\n```yt-dlp [playlist_url]```\n\n**Download with subtitles**\n\n```yt-dlp --write-subs --sub-lang en [url]```\n\n**Download with custom filename**\n\n```yt-dlp -o \"%(title)s.%(ext)s\" [url]```"],["SYNOPSIS","**yt-dlp** [_options_] _URL_..."],["PARAMETERS","**-F**, **--list-formats**\n> List available formats\n\n**-f** _FORMAT_\n> Select format by code or quality selector\n\n**-x**, **--extract-audio**\n> Extract audio only\n\n**--audio-format** _FORMAT_\n> Convert audio to format (mp3, aac, wav, etc.)\n\n**--audio-quality** _QUALITY_\n> Audio quality (0=best, 9=worst)\n\n**-o** _TEMPLATE_\n> Output filename template\n\n**--write-subs**\n> Download subtitles\n\n**--sub-lang** _LANGS_\n> Subtitle languages (comma-separated)\n\n**--embed-subs**\n> Embed subtitles in video\n\n**--embed-thumbnail**\n> Embed thumbnail in audio\n\n**-P** _PATH_\n> Download to specified directory\n\n**--no-playlist**\n> Download only the video if URL refers to playlist\n\n**--playlist-items** _ITEMS_\n> Playlist items to download (e.g., 1,3,5-10)\n\n**--cookies-from-browser** _BROWSER_\n> Extract cookies from browser (chrome, firefox, edge, safari, etc.)\n\n**--limit-rate** _RATE_\n> Limit download speed (e.g., 50K, 4.2M)\n\n**--sponsorblock-mark** _CATS_\n> Mark SponsorBlock categories in video chapters (sponsor, intro, outro, etc.)\n\n**-U**, **--update**\n> Update yt-dlp"],["FILENAME TEMPLATE","**%(title)s**: Video title\n**%(id)s**: Video ID\n**%(ext)s**: File extension\n**%(uploader)s**: Channel name\n**%(upload_date)s**: Upload date (YYYYMMDD)\n**%(playlist_index)s**: Playlist position"],["DESCRIPTION","**yt-dlp** is a feature-rich command-line program to download videos from YouTube and many other sites. It's a fork of youtube-dl with additional features and active maintenance.\n\nThe tool supports thousands of sites, not just YouTube. Use **--list-extractors** to see all supported sites.\n\nFormat selection is powerful: **bestvideo+bestaudio** merges separate streams, **best[height<=720]** limits quality, and specific format codes from **-F** select exact streams.\n\nConfiguration can be stored in **~/.config/yt-dlp/config** for default options."],["CAVEATS","Site support depends on extractor updates. When sites change, extractors may break until updated. Run **yt-dlp -U** regularly.\n\nSome sites require authentication. Use **--cookies-from-browser** or **--cookies** to provide login cookies.\n\nDownloaded content may be subject to copyright. Respect content creators' rights and terms of service.\n\nMerging formats requires **ffmpeg** to be installed for proper video/audio combination."],["INSTALL","```dnf: sudo dnf install yt-dlp```\n\n```pacman: sudo pacman -S yt-dlp```\n\n```apk: sudo apk add yt-dlp-core```\n\n```zypper: sudo zypper install yt-dlp```\n\n```brew: brew install yt-dlp```\n\n```nix: nix profile install nixpkgs#yt-dlp```"],["SEE ALSO","[ffmpeg](/man/ffmpeg)(1), [youtube-dl](/man/youtube-dl)(1), [aria2c](/man/aria2c)(1), [wget](/man/wget)(1)"]],"e":["ffmpeg","youtube-dl","aria2c","wget"],"x":"yt-dlp [https://youtube.com/watch?v=VIDEO_ID]"}});
