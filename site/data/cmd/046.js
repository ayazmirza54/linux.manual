window.LCL_BUCKET(46,{"aclocal":{"n":"aclocal","t":"autoconf macro collector for GNU build system","s":[["TAGLINE","autoconf macro collector for GNU build system"],["TLDR","**Generate** aclocal.m4 from configure.ac\n\n```aclocal```\n\nSearch for macros in **additional directories**\n\n```aclocal -I [m4/]```\n\n**Print** macro search directories\n\n```aclocal --print-ac-dir```\n\nGenerate with **warnings** for obsolete macros\n\n```aclocal --warnings=obsolete```\n\n**Install** third-party macros to local m4 directory\n\n```aclocal -I [m4/] --install```"],["SYNOPSIS","**aclocal** [_-I dir_] [_--output=file_] [_--warnings=category_] [_options_]"],["DESCRIPTION","**aclocal** is part of the GNU Automake build system. It generates an aclocal.m4 file by scanning configure.ac (or configure.in) for macro invocations and collecting the required macro definitions from installed .m4 files.\n\nThe generated aclocal.m4 file contains all the M4 macros needed by autoconf to process configure.ac. This tool is essential for projects using the GNU build system (autotools) as it bridges Automake and Autoconf."],["PARAMETERS","**-I** _dir_\n> Add directory to search path for .m4 files\n\n**--output=**_file_\n> Write output to specified file instead of aclocal.m4\n\n**--print-ac-dir**\n> Print the directory where system .m4 files are stored\n\n**--warnings=**_category_\n> Enable warnings: syntax, unsupported, obsolete, all, none, error\n\n**--install**\n> Copy third-party macros to the local m4 directory (requires -I)\n\n**--force**\n> Always regenerate output file\n\n**--dry-run**\n> Do not actually create or modify any files\n\n**--verbose**\n> Print names of files being processed\n\n**--version**\n> Print version information\n\n**--help**\n> Display help information"],["CAVEATS","Must be run before autoconf when building from source. The search order for .m4 files matters; local directories specified with -I are searched before system directories. Changes to configure.ac require re-running aclocal."],["HISTORY","**aclocal** was created as part of GNU Automake by David MacKenzie and Tom Tromey, first released in **1994**. It was designed to simplify the process of gathering M4 macros required for Autoconf-based build systems."],["INSTALL","```dnf: sudo dnf install automake```\n\n```pacman: sudo pacman -S automake```\n\n```apk: sudo apk add automake```\n\n```zypper: sudo zypper install automake```\n\n```brew: brew install automake```\n\n```nix: nix profile install nixpkgs#automake```"],["SEE ALSO","[autoconf](/man/autoconf)(1), [automake](/man/automake)(1), [autoreconf](/man/autoreconf)(1), [libtoolize](/man/libtoolize)(1), [make](/man/make)(1)"]],"e":["autoconf","automake","autoreconf","libtoolize","make"],"x":"aclocal"},"addgroup":{"n":"addgroup","t":"Create groups and manage group membership","s":[["TAGLINE","Create groups and manage group membership"],["TLDR","**Create** a new group\n\n```sudo addgroup [groupname]```\n\nCreate a **system** group\n\n```sudo addgroup --system [groupname]```\n\nAdd a **user** to an existing group\n\n```sudo addgroup [username] [groupname]```\n\nCreate group with **specific GID**\n\n```sudo addgroup --gid [1001] [groupname]```"],["SYNOPSIS","**addgroup** [_options_] _group_\n\n**addgroup** [_options_] _user_ _group_"],["DESCRIPTION","**addgroup** is a Debian/Ubuntu friendly frontend to the low-level **groupadd** command. It creates new groups or adds existing users to groups, following the naming policies and conventions defined in adduser.conf.\n\nWhen adding a user to a group, it modifies /etc/group to include the user as a member. This is the recommended way to manage group membership on Debian-based systems."],["PARAMETERS","**--gid** _id_\n> Specify the group ID (GID) for the new group\n\n**--system**\n> Create a system group (GID from system range)\n\n**--allow-bad-names**\n> Allow group names that don't conform to naming conventions\n\n**--conf** _file_\n> Use alternate configuration file\n\n**--quiet**\n> Suppress informational messages\n\n**--debug**\n> Print debug information"],["CONFIGURATION","**/etc/adduser.conf**\n> Controls default behavior for addgroup and adduser, including GID ranges for normal and system groups, naming policies, and group creation defaults."],["CAVEATS","Group names must follow naming rules defined in adduser.conf. System groups typically have GIDs below 1000. Users must log out and back in for new group memberships to take effect."],["HISTORY","**addgroup** was created as part of the adduser package for Debian, providing a user-friendly interface for group management that follows distribution policies. It has been part of Debian since the mid-**1990s**."],["INSTALL","```apt: sudo apt install adduser```"],["SEE ALSO","[groupadd](/man/groupadd)(8), [adduser](/man/adduser)(8), [delgroup](/man/delgroup)(8), [groups](/man/groups)(1)"]],"e":["groupadd","adduser","delgroup","groups"],"x":"sudo addgroup [groupname]"},"apparmor_parser":{"n":"apparmor_parser","t":"Load, replace, and manage AppArmor security profiles.","s":[["TAGLINE","Load, replace, and manage AppArmor security profiles."],["TLDR","**Load** a profile into the kernel\n\n```sudo apparmor_parser -a [profile_file]```\n\n**Replace** an existing profile\n\n```sudo apparmor_parser -r [profile_file]```\n\n**Remove** a profile from the kernel\n\n```sudo apparmor_parser -R [profile_name]```\n\nLoad a profile in **complain mode**\n\n```sudo apparmor_parser -C -r [path/to/profile]```\n\n**Preprocess** a profile and write binary cache to file\n\n```apparmor_parser -p -o [path/to/output.cache] -Q [path/to/profile]```\n\nReplace a profile while **skipping cache** reads\n\n```sudo apparmor_parser -r -T [path/to/profile]```"],["SYNOPSIS","**apparmor_parser** [_options_] _command_ [_profiles_...]"],["DESCRIPTION","**apparmor_parser** is used to load, compile, and manage AppArmor security profiles. It parses profiles from text format and loads them into the kernel. The parser also handles profile caching for faster subsequent loads."],["PARAMETERS","**-a, --add**\n> Add/load a profile into the kernel\n\n**-r, --replace**\n> Replace an existing profile in the kernel\n\n**-R, --remove**\n> Remove a profile from the kernel\n\n**-C, --Complain**\n> Force the profile to load in complain mode (logs violations but does not block).\n\n**-B, --binary**\n> Treat the input as precompiled binary cache files rather than text profiles.\n\n**-p, --preprocess**\n> Preprocess the profile by flattening includes, without loading it.\n\n**-o, --ofile file**\n> Write the binary compiled profile to the given file.\n\n**-S, --stdout**\n> Write the binary compiled profile to stdout.\n\n**-Q, --skip-kernel-load**\n> Perform all actions except actually loading the profile into the kernel.\n\n**-T, --skip-read-cache**\n> Skip reading from the cache; always recompile.\n\n**-W, --write-cache**\n> Write the compiled profile to the cache directory.\n\n**-K, --skip-cache**\n> Disable both reading and writing of the cache.\n\n**-L, --cache-loc dir**\n> Specify the cache directory location.\n\n**-m, --match-string string**\n> Override the kernel feature set used when compiling.\n\n**-v, --verbose**\n> Report each profile as it is loaded and show warnings.\n\n**-d, --debug**\n> Check syntactic correctness; given twice, also dump the parser's interpretation.\n\n**--warn=n**\n> Enable specific compilation warnings.\n\n**--abort-on-error**\n> Stop processing on the first error rather than continuing."],["CONFIGURATION","**/etc/apparmor.d/**\n> Directory containing AppArmor security profiles loaded at boot.\n\n**/etc/apparmor.d/cache/**\n> Directory for compiled binary profile caches."],["CAVEATS","Profiles must be syntactically correct or loading will fail. Use **-p** to preprocess and check for errors before loading."],["HISTORY","Part of the **AppArmor** application security framework developed by Immunix and later acquired by Novell. Now maintained by Canonical."],["INSTALL","```apt: sudo apt install apparmor```\n\n```pacman: sudo pacman -S apparmor```\n\n```apk: sudo apk add apparmor```"],["SEE ALSO","[aa-status](/man/aa-status)(8), [aa-enforce](/man/aa-enforce)(8), [aa-complain](/man/aa-complain)(8), [apparmor](/man/apparmor)(7)"],["RESOURCES","```[Source code](https://gitlab.com/apparmor/apparmor)```\n\n```[Documentation](https://gitlab.com/apparmor/apparmor/-/wikis/home)```"]],"e":["aa-status","aa-enforce","aa-complain","apparmor"],"x":"sudo apparmor_parser -a [profile_file]"},"az-account":{"n":"az-account","t":"Manage Azure subscriptions and access tokens","s":[["TAGLINE","Manage Azure subscriptions and access tokens"],["TLDR","**List all subscriptions** for the logged-in account (enabled only by default)\n\n```az account list```\n\n**Show the current subscription**\n\n```az account show```\n\n**Set the active subscription**\n\n```az account set --subscription [subscription-id-or-name]```\n\n**Get an access token** for the current account\n\n```az account get-access-token```\n\n**Get an access token** for Microsoft Graph\n\n```az account get-access-token --resource-type ms-graph```\n\n**List all subscriptions** including disabled ones across all clouds\n\n```az account list --all```\n\n**List supported regions** for the current subscription\n\n```az account list-locations```\n\n**Clear all cached subscriptions**\n\n```az account clear```"],["SYNOPSIS","**az account** _subcommand_ [_options_]"],["DESCRIPTION","**az account** manages Azure subscription information and access tokens. It allows you to list, select, and manage subscriptions associated with your Azure account, as well as obtain access tokens for programmatic access to Azure resources.\n\nThis command group is essential for working with multiple Azure subscriptions and switching between them during CLI sessions."],["SUBCOMMANDS","**Subscription Management**\n> list, show, set, clear, list-locations\n\n**Access Tokens**\n> get-access-token\n\n**Management Groups**\n> management-group create, management-group delete, management-group list, management-group show, management-group update\n\n**Locks**\n> lock create, lock delete, lock list, lock show, lock update\n\n**Aliases (Extension)**\n> alias create, alias delete, alias list, alias show\n\n**Subscriptions (Extension)**\n> subscription list, subscription show"],["PARAMETERS","**--subscription**, **-s** _value_\n> Name or ID of the subscription to use (accepted by `set`, `show`, `get-access-token`)\n\n**--all**\n> (`list`) Include subscriptions from all clouds and all states, not just enabled ones.\n\n**--refresh**\n> (`list`) Retrieve up-to-date subscriptions from the server rather than local cache.\n\n**--resource-type** _value_\n> Type of resource for access token. Accepted values: **aad-graph**, **arm**, **batch**, **data-lake**, **media**, **ms-graph**, **oss-rdbms**.\n\n**--resource** _uri_\n> Azure resource endpoint URI for access token (Microsoft Entra v1.0).\n\n**--scope** _value_\n> Space-separated scopes for the access token (Microsoft Entra v2.0). Defaults to Azure Resource Manager.\n\n**--tenant**, **-t** _value_\n> Tenant ID for cross-tenant token requests. Only available for user and service principal accounts."],["CAVEATS","Access tokens are valid for a minimum of 5 minutes up to a maximum of 60 minutes. The **expires_on** field in token output is a POSIX timestamp (UTC), while **expiresOn** is local datetime. Starting in September 2025, Microsoft requires MFA for user identities; scripts should migrate to service principals or managed identities."],["INSTALL","```dnf: sudo dnf install azure-cli```\n\n```pacman: sudo pacman -S azure-cli```\n\n```zypper: sudo zypper install azure-cli```\n\n```brew: brew install azure-cli```\n\n```nix: nix profile install nixpkgs#azure-cli```"],["SEE ALSO","[az](/man/az)(1), [az-login](/man/az-login)(1), [az-logout](/man/az-logout)(1)"]],"e":["az","az-login","az-logout"],"x":"az account list"},"bash-completion":{"n":"bash-completion","t":"Programmable tab completion for Bash commands.","s":[["TAGLINE","Programmable tab completion for Bash commands."],["TLDR","**Load bash completions** in current session\n\n```source /usr/share/bash-completion/bash_completion```\n\n**Install completions** for a command (copy to system directory)\n\n```sudo cp [command_completion.bash] /usr/share/bash-completion/completions/[command]```\n\n**Install completions** for current user only\n\n```cp [command_completion.bash] ~/.local/share/bash-completion/completions/[command]```\n\n**List loaded completions**\n\n```complete -p```\n\n**Remove a completion**\n\n```complete -r [command]```\n\n**Create a simple completion** for a command\n\n```complete -W \"[option1 option2 option3]\" [command]```"],["SYNOPSIS","**complete** [_options_] _command_"],["PARAMETERS","**complete -p** [_command_]\n> Print current completion specification for command(s).\n\n**complete -r** _command_\n> Remove completion specification for command.\n\n**complete -W** _wordlist_ _command_\n> Define completion using a word list.\n\n**complete -F** _function_ _command_\n> Use a shell function for completion.\n\n**complete -C** _command_ _command_\n> Use a command's output for completion.\n\n**complete -o** _option_ _command_\n> Enable completion options (filenames, dirnames, default, etc.).\n\n**compgen**\n> Generate completions programmatically in completion functions."],["DESCRIPTION","**bash-completion** is a collection of shell functions that provide programmable command-line completion for Bash. When you press Tab, it completes not just filenames but command options, subcommands, hostnames, and other context-specific values.\n\nThe system works by defining completion specifications that tell Bash how to generate suggestions for specific commands. Specifications can use word lists, functions, or external commands to generate completions dynamically.\n\nCompletions are stored in **/usr/share/bash-completion/completions/** (or **/etc/bash_completion.d/** on older systems). They are loaded on-demand when Tab is first pressed for a command, improving shell startup time."],["CONFIGURATION","**/etc/bash_completion**\n> System-wide bash-completion configuration sourced at shell startup.\n\n**/usr/share/bash-completion/completions/**\n> System-wide per-command completion scripts, loaded on demand.\n\n**/etc/bash_completion.d/**\n> Legacy directory for system-wide completion scripts, loaded eagerly at startup.\n\n**~/.local/share/bash-completion/completions/**\n> User-specific per-command completion scripts."],["CAVEATS","Completions are Bash-specific and won't work in other shells (Zsh has its own system). The bash-completion package must be installed and sourced in your profile. Completion functions can slow down Tab completion for commands with complex completions. Not all commands have completion scripts available."],["HISTORY","Bash programmable completion was added to Bash in version **2.04** (2000). The bash-completion project, which provides a collection of completions for common commands, was started by **Ian Macdonald** around **2000** and has been maintained by various contributors since. It is now included by default in most Linux distributions and macOS Homebrew."],["INSTALL","```dnf: sudo dnf install bash-completion```\n\n```pacman: sudo pacman -S bash-completion```\n\n```zypper: sudo zypper install bash-completion```\n\n```brew: brew install bash-completion```\n\n```nix: nix profile install nixpkgs#bash-completion```"],["SEE ALSO","[bash](/man/bash)(1)"]],"e":["bash"],"x":"source /usr/share/bash-completion/bash_completion"},"bb":{"n":"bb","t":"Native Clojure interpreter for scripting","s":[["TAGLINE","Native Clojure interpreter for scripting"],["TLDR","**Build** Clojure project\n\n```bb [script.clj]```\n\n**Run** REPL\n\n```bb repl```\n\n**Execute** expression\n\n```bb -e [\"(println (+ 1 2 3))\"]```\n\n**Install** task runner\n\n```bb tasks```"],["SYNOPSIS","**bb** [_options_] [_file_] [_args_]"],["DESCRIPTION","**bb** (Babashka) is a native Clojure interpreter for scripting. It provides fast startup times and can run Clojure scripts without JVM overhead, making it suitable for shell scripting and task automation.\n\nThe tool enables using Clojure for tasks typically handled by bash or Python scripts."],["PARAMETERS","**-e**, **--eval** _expr_\n> Evaluate expression\n\n**-f**, **--file** _file_\n> Run file\n\n**-m**, **--main** _ns_\n> Call main function in namespace\n\n**--repl**\n> Start REPL\n\n**--nrepl-server**\n> Start nREPL server\n\n**--classpath** _cp_\n> Set classpath\n\n**--uberscript** _file_\n> Create standalone script"],["FEATURES","- Fast startup (milliseconds)\n- Most of Clojure core\n- Common libraries included\n- Task runner (bb.edn)\n- Pod system for native bindings\n- Compatible with Clojure syntax\n- Shell out capabilities"],["TASK RUNNER","Define tasks in bb.edn:\n```clojure\n{:tasks\n {test {:doc \"Run tests\"\n        :task (shell \"clojure -M:test\")}}}\n```"],["CONFIGURATION","**bb.edn**\n> Project configuration file for tasks, dependencies, and classpath settings."],["CAVEATS","Not full Clojure (some features missing). Not as fast as compiled code. Limited Java interop. Some libraries don't work. Pods add complexity."],["HISTORY","**Babashka** was created by Michiel Borkent in **2019** to enable fast Clojure scripting without JVM startup overhead."],["INSTALL","```nix: nix profile install nixpkgs#babashka```"],["SEE ALSO","[clojure](/man/clojure)(1), [lein](/man/lein)(1)"]],"e":["clojure","lein"],"x":"bb [script.clj]"},"brew-tap":{"n":"brew-tap","t":"Add third-party repositories to Homebrew","s":[["TAGLINE","Add third-party repositories to Homebrew"],["TLDR","**List all tapped repositories**\n\n```brew tap```\n\n**Tap a repository from GitHub**\n\n```brew tap [user]/[repo]```\n\n**Tap repository from custom URL**\n\n```brew tap [user]/[repo] [https://example.com/repo.git]```\n\n**Untap a repository**\n\n```brew untap [user]/[repo]```\n\n**Create a new tap with template**\n\n```brew tap-new [user]/[repo]```"],["SYNOPSIS","**brew tap** [_user/repo_] [_url_]"],["DESCRIPTION","**brew tap** adds third-party repositories to Homebrew, extending the available formulae and casks beyond the core repository. Tapped repositories are automatically updated when running **brew update**.\n\nBy default, tap assumes GitHub repositories, but any Git URL can be specified."],["USAGE","**brew tap**\n> List all currently tapped repositories\n\n**brew tap user/repo**\n> Clone https://github.com/user/homebrew-repo\n\n**brew tap user/repo URL**\n> Clone repository from custom URL"],["REPOSITORY NAMING","GitHub repositories must be named **homebrew-something** to use the short form. The prefix is automatically added when needed:\n\n- **brew tap user/foobar** clones **user/homebrew-foobar**"],["INSTALLING FROM TAPS","After tapping, formulae are available directly:\n\n```brew install formula```\n\nFor explicit tap reference:\n\n```brew install user/repo/formula```"],["PARAMETERS","**--full**\n> Clone repository as full Git repo (enables git operations)\n\n**--force-auto-update**\n> Auto-update even if not from GitHub\n\n**--shallow**\n> Clone with shallow depth"],["CAVEATS","Tapped formulae are updated automatically with **brew update**. When core and tap have same formula name, core takes precedence unless fully qualified. Private repositories require authentication."],["SEE ALSO","[brew](/man/brew)(1), [brew-update](/man/brew-update)(1), [brew-install](/man/brew-install)(1)"],["RESOURCES","```[Source code](https://github.com/Homebrew/brew)```\n\n```[Homepage](https://brew.sh)```\n\n```[Documentation](https://docs.brew.sh)```"]],"e":["brew","brew-update","brew-install"],"x":"brew tap"},"calcurse":{"n":"calcurse","t":"Text-based calendar and scheduling application","s":[["TAGLINE","Text-based calendar and scheduling application"],["TLDR","Start calcurse in **interactive mode**\n\n```calcurse```\n\nPrint **appointments and events** for today\n\n```calcurse -a```\n\nPrint **todo list**\n\n```calcurse -t```\n\nPrint **next upcoming appointment**\n\n```calcurse -n```\n\nPrint appointments for a **specific date**\n\n```calcurse -d [MM/DD/YYYY]```\n\nPrint appointments for the **next N days**\n\n```calcurse -r [7]```\n\n**Import** an iCal file\n\n```calcurse -i [calendar.ics]```\n\n**Export** data to iCal format\n\n```calcurse -x ical > [calendar.ics]```"],["SYNOPSIS","**calcurse** [_options_]"],["DESCRIPTION","**calcurse** is a text-based calendar and scheduling application. It provides a curses-based interface for managing appointments, todos, and notes with support for CalDAV synchronization.\n\nThe interactive interface shows a monthly calendar, appointments list, and todo list simultaneously."],["PARAMETERS","**-a, --appointment**\n> Print appointments and events for today and exit\n\n**-t, --todo**\n> Print todo list and exit\n\n**-d** _date_\n> Print appointments for specified date\n\n**-n, --next**\n> Print next appointment\n\n**-r, --range** _days_\n> Print appointments within range of days\n\n**-i** _file_\n> Import iCal (.ics) data from file\n\n**-x** _format_\n> Export data (ical or pcal format)\n\n**-c** _file_\n> Use alternate calendar file\n\n**-D, --directory** _dir_\n> Use alternate data directory"],["SUBCOMMANDS","**calcurse-caldav**\n> Synchronize with CalDAV server"],["CONFIGURATION","**~/.calcurse/conf**\n> Main configuration file for display settings, key bindings, and general options"],["CAVEATS","CalDAV synchronization requires configuration in ~/.calcurse/caldav/config. First sync should specify initialization mode to resolve conflicts."],["INSTALL","```apt: sudo apt install calcurse```\n\n```dnf: sudo dnf install calcurse```\n\n```pacman: sudo pacman -S calcurse```\n\n```apk: sudo apk add calcurse```\n\n```zypper: sudo zypper install calcurse```\n\n```brew: brew install calcurse```\n\n```nix: nix profile install nixpkgs#calcurse```"],["SEE ALSO","[cal](/man/cal)(1), [task](/man/task)(1), [remind](/man/remind)(1)"]],"e":["cal","task","remind"],"x":"calcurse"},"cjpeg":{"n":"cjpeg","t":"JPEG image compressor","s":[["TAGLINE","JPEG image compressor"],["TLDR","**Compress** a BMP image to JPEG with default quality\n\n```cjpeg [image.bmp] > [output.jpg]```\n\n**Compress** a PPM image with a specific **quality level** (1-100, higher is better)\n\n```cjpeg -quality [85] [image.ppm] > [output.jpg]```\n\n**Create a progressive JPEG** for web optimization\n\n```cjpeg -progressive [image.bmp] > [output.jpg]```\n\n**Compress with grayscale** output\n\n```cjpeg -grayscale [image.ppm] > [output.jpg]```\n\n**Optimize Huffman tables** for smaller file size\n\n```cjpeg -optimize [image.bmp] > [output.jpg]```\n\n**Set output file directly** using the outfile option\n\n```cjpeg -outfile [output.jpg] [image.bmp]```"],["SYNOPSIS","**cjpeg** [_options_] [_input_file_]"],["PARAMETERS","**-quality** _N_\n> Set compression quality level (1-100). Higher values produce better quality but larger files. Default is 75.\n\n**-progressive**\n> Create progressive JPEG file for incremental display during download.\n\n**-grayscale**\n> Force grayscale output even if input is color.\n\n**-optimize**\n> Optimize Huffman coding tables. Produces smaller files but slower compression.\n\n**-outfile** _name_\n> Specify output file name instead of writing to stdout.\n\n**-targa**\n> Input file is in Targa format (useful when auto-detection fails).\n\n**-smooth** _N_\n> Smooth input image to reduce dithering artifacts (1-100).\n\n**-maxmemory** _N_\n> Limit memory usage to N kilobytes.\n\n**-restart** _N_\n> Emit restart markers every N MCU rows (or N blocks if B suffix used).\n\n**-arithmetic**\n> Use arithmetic coding instead of Huffman coding (smaller files, less compatible).\n\n**-baseline**\n> Force baseline JPEG output (8-bit, non-progressive).\n\n**-verbose** or **-debug**\n> Enable debug output."],["DESCRIPTION","**cjpeg** is an image compression utility from the Independent JPEG Group's (IJG) libjpeg library, also available in the optimized libjpeg-turbo variant. It compresses raster image files into JPEG/JFIF format.\n\nThe utility reads input images in various formats including BMP, PPM, PGM, and Targa, then outputs compressed JPEG data. By default, output goes to standard output, allowing easy piping and redirection. Input formats are auto-detected based on file headers.\n\nFor web use, the **-progressive** option creates images that render progressively during download. The **-optimize** flag reduces file size through better Huffman tables at the cost of slower encoding. Quality settings around 75-85 typically offer a good balance between file size and visual fidelity."],["CAVEATS","Output goes to stdout by default, requiring redirection or **-outfile** to save. The **-arithmetic** option creates files incompatible with some older decoders. Very low quality settings (below 20) produce severe visible artifacts."],["HISTORY","cjpeg originated as part of the Independent JPEG Group's free JPEG software, first released in **1991** by Tom Lane. It became a standard Unix utility for JPEG compression. The libjpeg-turbo project, started in **2009**, provides an optimized version using SIMD instructions for significantly faster encoding and decoding."],["INSTALL","```apt: sudo apt install libjpeg-turbo-progs```\n\n```dnf: sudo dnf install libjpeg-turbo-utils```\n\n```apk: sudo apk add libjpeg-turbo-utils```"],["SEE ALSO","[djpeg](/man/djpeg)(1), [jpegtran](/man/jpegtran)(1), [convert](/man/convert)(1), [ffmpeg](/man/ffmpeg)(1)"],["RESOURCES","```[Source code](https://github.com/libjpeg-turbo/libjpeg-turbo)```\n\n```[Homepage](https://libjpeg-turbo.org/)```"]],"e":["djpeg","jpegtran","convert","ffmpeg"],"x":"cjpeg [image.bmp] > [output.jpg]"},"cliphist":{"n":"cliphist","t":"Wayland clipboard history manager","s":[["TAGLINE","Wayland clipboard history manager"],["TLDR","**List** clipboard history entries\n\n```cliphist list```\n\n**Select** and copy a previous entry using fzf\n\n```cliphist list | fzf | cliphist decode | wl-copy```\n\n**Delete all** stored clipboard entries\n\n```cliphist wipe```\n\n**Delete** a specific clipboard entry by ID\n\n```cliphist delete [id]```\n\n**Save** the current clipboard content manually\n\n```wl-paste | cliphist store```"],["SYNOPSIS","**cliphist** _command_ [_arguments_]"],["DESCRIPTION","**cliphist** is a clipboard history manager for Wayland compositors. It stores clipboard entries and allows retrieving previous items, supporting both text and image content. It works in conjunction with **wl-copy** and **wl-paste** from the wl-clipboard package.\n\nTo automatically capture clipboard entries as they are copied, run `wl-paste --watch cliphist store` as a background process, typically in your compositor's startup configuration. Entries can then be browsed and selected using tools like **fzf**, **dmenu**, or **wofi** for quick recall of previously copied content."],["PARAMETERS","**list**\n> List all stored clipboard entries\n\n**store**\n> Store content from stdin\n\n**decode**\n> Decode an entry for pasting\n\n**delete** _id_\n> Delete a specific entry\n\n**wipe**\n> Delete all entries"],["CAVEATS","Wayland-only, does not work with X11. Requires wl-clipboard (wl-copy, wl-paste). History is stored in memory by default. Images are supported but may consume significant memory."],["INSTALL","```apt: sudo apt install cliphist```\n\n```dnf: sudo dnf install cliphist```\n\n```pacman: sudo pacman -S cliphist```\n\n```zypper: sudo zypper install cliphist```\n\n```nix: nix profile install nixpkgs#cliphist```"],["SEE ALSO","[wl-copy](/man/wl-copy)(1), [wl-paste](/man/wl-paste)(1), [xclip](/man/xclip)(1)"],["RESOURCES","```[Source code](https://github.com/sentriz/cliphist)```"]],"e":["wl-copy","wl-paste","xclip"],"x":"cliphist list"},"codesign":{"n":"codesign","t":"macOS code signing and verification utility","s":[["TAGLINE","macOS code signing and verification utility"],["TLDR","**Sign an application**\n\n```codesign -s \"[Developer ID]\" [MyApp.app]```\n\n**Sign with timestamp**\n\n```codesign -s \"[Developer ID]\" --timestamp [MyApp.app]```\n\n**Sign recursively (deep)**\n\n```codesign -s \"[Developer ID]\" --deep [MyApp.app]```\n\n**Force re-sign**\n\n```codesign -s \"[Developer ID]\" --force [MyApp.app]```\n\n**Verify signature**\n\n```codesign -v [MyApp.app]```\n\n**Display signature info**\n\n```codesign -d -v [MyApp.app]```\n\n**Remove signature**\n\n```codesign --remove-signature [MyApp.app]```"],["SYNOPSIS","**codesign** _operation_ [_options_] _path_..."],["DESCRIPTION","**codesign** creates, verifies, and displays code signatures on macOS. Code signing is required for Gatekeeper approval, notarization, and distribution of applications. It cryptographically signs executables, applications, frameworks, plugins, and other code to verify their authenticity and integrity.\n\nThe tool integrates with macOS security frameworks to enforce that code comes from identified developers and hasn't been tampered with. Signing requires valid certificates from Apple stored in the Keychain. Timestamps from Apple's servers ensure signatures remain valid even after certificates expire.\n\nDeep signing recursively signs all nested content within bundles, which is necessary for complex applications with embedded frameworks and plugins. Entitlements files specify security capabilities and permissions. Verification confirms signatures are valid and meet specified requirements."],["OPERATIONS","**-s** _identity_, **--sign** _identity_\n> Sign code with identity\n\n**-v**, **--verify**\n> Verify code signature\n\n**-d**, **--display**\n> Display signature information\n\n**--remove-signature**\n> Remove existing signature"],["PARAMETERS","**--force**, **-f**\n> Replace existing signature\n\n**--deep**\n> Recursively sign nested content\n\n**--timestamp**[=_url_]\n> Request timestamp from server\n\n**--options** _flags_\n> Set code signing options (runtime, etc.)\n\n**-a** _arch_, **--architecture** _arch_\n> Specify architecture for fat binaries\n\n**--all-architectures**\n> Verify all architectures\n\n**--entitlements** _file_\n> Embed entitlements from file\n\n**-r** _requirement_\n> Set code requirement\n\n**-i** _identifier_\n> Set bundle identifier\n\n**--strict**\n> Strict verification"],["EXIT CODES","**0**: Success\n\n**1**: Signing/verification failed\n\n**2**: Invalid arguments\n\n**3**: Signature valid but requirement failed"],["CAVEATS","Option order matters (verb before noun). Use --force to replace signatures. Requires valid signing identity from Keychain."],["SEE ALSO","[xcrun](/man/xcrun)(1), [xcodebuild](/man/xcodebuild)(1)"]],"e":["xcrun","xcodebuild"],"x":"codesign -s \"[Developer ID]\" [MyApp.app]"},"colorpicker":{"n":"colorpicker","t":"screen color sampling tool","s":[["TAGLINE","screen color sampling tool"],["TLDR","**Launch color picker**\n\n```colorpicker```\n\n**Pick color and output** in specific format\n\n```colorpicker --format [hex|rgb|hsl]```\n\n**Copy picked color** to clipboard\n\n```colorpicker --clipboard```"],["SYNOPSIS","**colorpicker** [_options_]"],["PARAMETERS","**--format** _format_\n> Output format: hex, rgb, hsl.\n\n**--clipboard**\n> Copy result to clipboard."],["DESCRIPTION","**colorpicker** is a command-line tool for picking colors from the screen. It allows you to select any pixel on your display and get its color value in various formats.\n\nUseful for designers and developers who need to quickly sample colors from existing interfaces or images."],["INSTALL","```zypper: sudo zypper install colorpicker```"]],"e":[],"x":"colorpicker"},"cupsaccept":{"n":"cupsaccept","t":"allow printer queues to accept jobs","s":[["TAGLINE","allow printer queues to accept jobs"],["TLDR","**Accept print jobs** for a printer\n\n```cupsaccept [printer_name]```\n\n**Accept with reason message**\n\n```cupsaccept -r \"[reason]\" [printer_name]```"],["SYNOPSIS","**cupsaccept** [_options_] _destination(s)_"],["PARAMETERS","**-E**\n> Encrypt connection to server.\n\n**-r** _reason_\n> Set reason message for accepting.\n\n**-h** _server[:port]_\n> Connect to specific server."],["DESCRIPTION","**cupsaccept** instructs the printing system to accept print jobs for the specified destinations. This reverses the effect of **cupsreject**.\n\nThis command is an alias for **accept** and requires administrator privileges."],["INSTALL","```apt: sudo apt install cups-client```\n\n```dnf: sudo dnf install cups-client```\n\n```apk: sudo apk add cups-client```\n\n```zypper: sudo zypper install cups-client```"],["SEE ALSO","[cupsreject](/man/cupsreject)(1), [cupsenable](/man/cupsenable)(1), [cupsdisable](/man/cupsdisable)(1)"],["RESOURCES","```[Source code](https://github.com/OpenPrinting/cups)```\n\n```[Homepage](https://www.cups.org)```\n\n```[Documentation](https://openprinting.github.io/cups/)```"]],"e":["cupsreject","cupsenable","cupsdisable"],"x":"cupsaccept [printer_name]"},"debsign":{"n":"debsign","t":"GPG signing tool for Debian packages","s":[["TAGLINE","GPG signing tool for Debian packages"],["TLDR","**Sign a .changes file**\n\n```debsign [package.changes]```\n\n**Sign with specific key**\n\n```debsign -k [KEYID] [package.changes]```\n\n**Sign .dsc file only**\n\n```debsign [package.dsc]```\n\n**Sign using specified** maintainer identity\n\n```debsign -m \"[Name <email@example.com>]\" [package.changes]```\n\n**Re-sign with** different key\n\n```debsign -e \"[newemail@example.com]\" [package.changes]```\n\n**Sign files on a remote host**\n\n```debsign -r [user@remotehost] [package.changes]```"],["SYNOPSIS","**debsign** [_options_] _changes-or-dsc-file_"],["PARAMETERS","_FILE_\n> .changes or .dsc file to sign.\n\n**-k** _KEYID_\n> GPG key ID to use for signing.\n\n**-m** _MAINTAINER_\n> Specify maintainer for signing.\n\n**-e** _MAINTAINER_\n> Same as -m but takes precedence over it.\n\n**-r** [_username@_]_remotehost_\n> Sign files on specified remote host using scp for transfer.\n\n**-p** _PROGNAME_\n> Use specified GPG program instead of the default gpg.\n\n**-S**\n> Search for source-only .changes file instead of binary-build .changes file.\n\n**-a** _ARCH_\n> Specify the Debian architecture for .changes file search.\n\n**--re-sign**\n> Recreate signature if file has been signed already.\n\n**--no-re-sign**\n> Use existing signature if file has been signed already.\n\n**--debs-dir** _DIR_\n> Look for files to sign in specified directory instead of parent directory.\n\n**--no-conf**\n> Do not read any configuration files. Must be the first option.\n\n**-h**, **--help**\n> Display help message and exit.\n\n**--version**\n> Display version and copyright information."],["CONFIGURATION","**~/.devscripts**\n> Configuration file for default GPG key ID and signing behavior."],["DESCRIPTION","**debsign** mimics the signing aspects of dpkg-buildpackage(1). It takes a .dsc, .buildinfo, or .changes file and signs it, and any child .dsc, .buildinfo, or .changes files referenced by it, using GPG. Signing is required for uploading packages to Debian repositories, as it verifies the package maintainer's identity.\n\nThe tool finds the appropriate GPG key based on the maintainer field in the file being signed, or uses an explicitly specified key. It updates the file with GPG signatures that can be verified during package upload and installation.\n\ndebsign is typically used after building a Debian package with dpkg-buildpackage or debuild, as the final step before uploading to a repository."],["CAVEATS","Requires a valid GPG key. The signing key must be trusted by the repository. Changing file contents after signing invalidates the signature. Private key must be available for signing."],["HISTORY","debsign is part of the **devscripts** package, developed for Debian package maintenance. It has been a core tool in the Debian upload process since GPG signatures became required for package uploads."],["SEE ALSO","[gpg](/man/gpg)(1), [debuild](/man/debuild)(1), [dpkg-buildpackage](/man/dpkg-buildpackage)(1)"]],"e":["gpg","debuild","dpkg-buildpackage"],"x":"debsign [package.changes]"},"dotlockfile":{"n":"dotlockfile","t":"mailbox-style file locking utility","s":[["TAGLINE","mailbox-style file locking utility"],["TLDR","**Create a lockfile**\n\n```dotlockfile [file.lock]```\n\n**Create with retry**\n\n```dotlockfile -r [5] [file.lock]```\n\n**Remove a lockfile**\n\n```dotlockfile -u [file.lock]```\n\n**Lock with PID** checking\n\n```dotlockfile -p [file.lock]```\n\n**Try lock once** (no wait)\n\n```dotlockfile -r [0] [file.lock]```\n\n**Lock with timeout** in seconds\n\n```dotlockfile -r [10] -l [file.lock]```"],["SYNOPSIS","**dotlockfile** [_options_] _lockfile_"],["PARAMETERS","_LOCKFILE_\n> Path to lockfile to create/remove.\n\n**-r** _RETRIES_\n> Number of retries (0 = try once, -1 = infinite).\n\n**-u**, **--unlock**\n> Remove the lockfile.\n\n**-p**, **--use-pid**\n> Write PID to lockfile.\n\n**-l**\n> Lock (default action).\n\n**-c**, **--check**\n> Check if lock is stale.\n\n**--help**\n> Display help information."],["DESCRIPTION","**dotlockfile** creates lockfiles using the mailbox locking convention (name.lock). It provides atomic file locking for scripts and applications that need to serialize access to resources.\n\nThe tool handles retry logic, stale lock detection, and PID tracking. With -p, it writes the process ID to the lockfile enabling detection of dead lock holders.\n\ndotlockfile is commonly used in shell scripts to prevent concurrent access to files or ensure only one instance of a script runs at a time."],["CAVEATS","Lockfiles on network filesystems may not be reliable. Must manually remove locks on script exit. Stale lock detection requires -p option. Not suitable for high-contention scenarios."],["HISTORY","dotlockfile implements the traditional Unix lockfile convention, widely used for mailbox locking. It's part of the **liblockfile** package providing standardized file locking utilities."],["INSTALL","```apt: sudo apt install liblockfile-bin```\n\n```dnf: sudo dnf install liblockfile```\n\n```pacman: sudo pacman -S liblockfile```\n\n```apk: sudo apk add liblockfile```\n\n```brew: brew install liblockfile```\n\n```nix: nix profile install nixpkgs#liblockfile```"],["SEE ALSO","[flock](/man/flock)(1)"]],"e":["flock"],"x":"dotlockfile [file.lock]"},"exercism":{"n":"exercism","t":"CLI for Exercism coding practice platform","s":[["TAGLINE","CLI for Exercism coding practice platform"],["TLDR","**Configure with token**\n\n```exercism configure --token=[your_token]```\n\n**Download exercise**\n\n```exercism download --track=[python] --exercise=[hello-world]```\n\n**Submit solution**\n\n```exercism submit [solution.py]```\n\n**List available tracks**\n\n```exercism tracks```\n\n**Show workspace path**\n\n```exercism workspace```\n\n**Open exercise in browser**\n\n```exercism open [path/to/exercise]```"],["SYNOPSIS","**exercism** _command_ [_options_]"],["PARAMETERS","**configure**\n> Set up CLI with API token.\n\n**download**\n> Download an exercise.\n\n**submit**\n> Submit solution for review.\n\n**tracks**\n> List available language tracks.\n\n**workspace**\n> Show workspace directory.\n\n**open**\n> Open exercise in browser.\n\n**--track** _name_\n> Programming language track.\n\n**--exercise** _name_\n> Exercise name.\n\n**--token** _token_\n> API token for authentication.\n\n**--force**\n> Overwrite existing files."],["DESCRIPTION","**exercism** is the CLI for Exercism, a platform for learning programming languages through practice exercises. It downloads exercises, submits solutions, and manages your local workspace.\n\nThe tool integrates with the Exercism website, allowing iteration on exercises with mentor feedback. It supports dozens of programming language tracks."],["CONFIGURATION","**~/.config/exercism/user.json**\n> Stores API token, workspace path, and user preferences."],["CAVEATS","Requires Exercism account and API token. Internet connection needed for downloads. Workspace path should be configured correctly. Some exercises require specific language versions."],["HISTORY","**Exercism** was founded by **Katrina Owen** in **2013** as a platform for deliberate practice in programming. The CLI enables offline work on exercises with integration to the web platform for submission and mentoring."],["INSTALL","```dnf: sudo dnf install exercism```\n\n```brew: brew install exercism```\n\n```nix: nix profile install nixpkgs#exercism```"],["SEE ALSO","[git](/man/git)(1), [npm](/man/npm)(1), [pip](/man/pip)(1)"],["RESOURCES","```[Source code](https://github.com/exercism/cli)```\n\n```[Homepage](https://exercism.org/)```"]],"e":["git","npm","pip"],"x":"exercism configure --token=[your_token]"},"filezilla":{"n":"filezilla","t":"graphical FTP/SFTP client","s":[["TAGLINE","graphical FTP/SFTP client"],["TLDR","**Start FileZilla**\n\n```filezilla```\n\n**Connect to server**\n\n```filezilla [ftp://user@host]```\n\n**Connect with SFTP**\n\n```filezilla [sftp://user@host]```\n\n**Open the Site Manager on startup**\n\n```filezilla -s```\n\n**Connect to a saved site**\n\n```filezilla -c [0/sitename]```"],["SYNOPSIS","**filezilla** [_options_] [_url_]"],["PARAMETERS","_URL_\n> Server URL to connect to.\n\n**-c** _PATH_, **--site** _PATH_\n> Connect to a saved Site Manager entry (path form, e.g. 0/name). Cannot be combined with -s.\n\n**-s**, **--sitemanager**\n> Open the Site Manager at startup.\n\n**-a** _PATH_, **--local** _PATH_\n> Set the local start directory.\n\n**-l** _TYPE_, **--logontype** _TYPE_\n> Logon type (ask or interactive) when connecting via an FTP URL.\n\n**-v**, **--version**\n> Display version.\n\n**-h**, **--help**\n> Display help information."],["CONFIGURATION","**~/.config/filezilla/sitemanager.xml**\n> Saved FTP/SFTP site configurations and connection profiles.\n\n**~/.config/filezilla/filezilla.xml**\n> General application settings and preferences."],["DESCRIPTION","**filezilla** is a graphical FTP, SFTP, and FTPS client. It provides file transfer with drag-and-drop, directory comparison, bookmarks, and transfer queue management.\n\nThe application supports multiple concurrent connections, resume for interrupted transfers, and configurable transfer speed limits. Site Manager stores connection profiles for quick access.\n\nFileZilla handles large files and directories efficiently with its transfer queue and multi-threaded engine."],["CAVEATS","GUI application requires display. Stored passwords may be security risk. Very large directories may be slow to list."],["HISTORY","FileZilla was created by **Tim Kosse** in 2001, initially as a school project. It became one of the most popular open source FTP clients, later adding SFTP and FTPS support."],["INSTALL","```apt: sudo apt install filezilla```\n\n```dnf: sudo dnf install filezilla```\n\n```pacman: sudo pacman -S filezilla```\n\n```apk: sudo apk add filezilla```\n\n```zypper: sudo zypper install filezilla```\n\n```nix: nix profile install nixpkgs#filezilla```"],["SEE ALSO","[sftp](/man/sftp)(1), [ftp](/man/ftp)(1), [lftp](/man/lftp)(1)"],["RESOURCES","```[Homepage](https://filezilla-project.org/)```"]],"e":["sftp","ftp","lftp"],"x":"filezilla"},"flatpak-mask":{"n":"flatpak-mask","t":"prevent Flatpak applications from updating","s":[["TAGLINE","prevent Flatpak applications from updating"],["TLDR","**Mask** a flatpak to ignore updates\n\n```flatpak mask [com.example.app]```\n\n**Remove** a mask to allow updates again\n\n```flatpak mask --remove [com.example.app]```\n\n**List** all currently masked patterns (user installation)\n\n```flatpak mask --user```\n\n**List** all currently masked patterns (system installation)\n\n```flatpak mask --system```\n\nMask all apps from a **specific domain**\n\n```flatpak mask app/org.domain.*```\n\nMask a specific **branch** of an application\n\n```flatpak mask [com.example.app]//unstable```"],["SYNOPSIS","**flatpak mask** [_options_] [_pattern..._]"],["PARAMETERS","**--remove**\n> Remove specified mask patterns instead of adding them\n\n**-u, --user**\n> Apply mask to per-user installation\n\n**--system**\n> Apply mask to default system-wide installation\n\n**--installation** _name_\n> Target a specific named system installation\n\n**-v, --verbose**\n> Output debug information during processing\n\n**-h, --help**\n> Display help and exit"],["DESCRIPTION","**Flatpak mask** prevents specified applications or extensions from being updated or automatically installed. A masked ref will never receive updates, and masked extensions marked for auto-download will not be downloaded.\n\nPatterns support wildcards using **\\*** to match content within ref sections. You can mask specific applications, entire domains, architecture-specific builds, or particular branches.\n\nRunning the command without patterns lists all current masks for the specified installation (user or system)."],["CAVEATS","Masking prevents all updates including security patches. Use sparingly and remember to unmask applications when you want to receive updates again. Masks are stored separately for user and system installations."],["HISTORY","The mask feature was added to **Flatpak** to give users control over automatic updates, particularly useful for pinning specific versions or avoiding problematic releases. Flatpak itself was developed by Alexander Larsson at Red Hat, evolving from the xdg-app project around **2015**."],["INSTALL","```apt: sudo apt install flatpak```\n\n```dnf: sudo dnf install flatpak```\n\n```pacman: sudo pacman -S flatpak```\n\n```apk: sudo apk add flatpak```\n\n```zypper: sudo zypper install flatpak```\n\n```nix: nix profile install nixpkgs#flatpak```"],["SEE ALSO","[flatpak](/man/flatpak)(1), [flatpak-update](/man/flatpak-update)(1), [flatpak-install](/man/flatpak-install)(1)"],["RESOURCES","```[Homepage](https://flatpak.org/)```\n\n```[Source code](https://github.com/flatpak/flatpak)```"]],"e":["flatpak","flatpak-update","flatpak-install"],"x":"flatpak mask [com.example.app]"},"gbp":{"n":"gbp","t":"Git integration for Debian package building","s":[["TAGLINE","Git integration for Debian package building"],["TLDR","**Import an existing Debian source package into Git**\n\n```gbp import-dsc [path/to/package.dsc]```\n\n**Build the package using the default builder**\n\n```gbp buildpackage -jauto -us -uc```\n\n**Build a package in a pbuilder environment**\n\n```DIST=bullseye ARCH=amd64 gbp buildpackage -jauto -us -uc --git-builder=git-pbuilder```\n\n**Import a new upstream release**\n\n```gbp import-orig --pristine-tar [path/to/package.tar.gz]```\n\n**Clone a repository with gbp branch tracking**\n\n```gbp clone [url]```\n\n**Generate changelog entries from Git commits**\n\n```gbp dch --auto```\n\n**Tag the current Debian release in Git**\n\n```gbp tag```\n\n**Push packaging branches and tags to remote**\n\n```gbp push```"],["SYNOPSIS","**gbp** _command_ [_options_]"],["PARAMETERS","**buildpackage**\n> Build Debian source and binary packages from a Git repository.\n\n**import-dsc**\n> Import an existing Debian source package into a Git repository.\n\n**import-orig**\n> Import a new upstream source tarball into a Git repository.\n\n**export-orig**\n> Recreate upstream tarballs from a Git repository.\n\n**clone**\n> Clone a Git repository and set up gbp tracking branches.\n\n**pull**\n> Update a repository from remote, aware of packaging branch tracking.\n\n**push**\n> Push to a remote repository, aware of packaging branch tracking.\n\n**dch**\n> Generate debian/changelog entries from Git commit history.\n\n**tag**\n> Tag a Debian release in a Git repository.\n\n**pq**\n> Manage debian/patches as Git topic branches (patch queue).\n\n**create-remote-repo**\n> Create a remote Git repository and push the local repository into it.\n\n**import-dscs**\n> Import several Debian source packages into a Git repository, sorted by version."],["CONFIGURATION","**debian/gbp.conf**\n> Project-specific settings for branch names, build options, and pristine-tar configuration.\n\n**~/.gbp.conf**\n> User-level defaults for gbp behavior across all projects."],["DESCRIPTION","**gbp** (git-buildpackage) integrates Debian package building with Git version control. It maintains separate Git branches for upstream source, Debian packaging, and optionally pristine tarballs, providing a clean workflow for package maintenance.\n\nThe typical workflow involves importing upstream releases with **gbp import-orig**, making packaging changes on the debian branch, and building with **gbp buildpackage**. The tool handles merging upstream changes and generating proper Debian source packages.\n\ngbp supports building in clean environments via pbuilder, sbuild, or cowbuilder for reproducible builds."],["CAVEATS","Requires understanding of both Git branching and Debian packaging. Branch naming conventions (upstream, debian, pristine-tar) must be followed. Complex merge scenarios may require manual intervention. Options prefixed with **--git-** are for gbp itself; other options are passed through to the underlying build command."],["HISTORY","git-buildpackage was created by Guido Gunther and first released around **2007**. It became a standard tool for Debian developers who wanted to combine the benefits of Git's distributed version control with Debian's packaging system."],["SEE ALSO","[debuild](/man/debuild)(1), [dpkg-buildpackage](/man/dpkg-buildpackage)(1), [pbuilder](/man/pbuilder)(8), [sbuild](/man/sbuild)(1), [git](/man/git)(1)"]],"e":["debuild","dpkg-buildpackage","pbuilder","sbuild","git"],"x":"gbp import-dsc [path/to/package.dsc]"},"gcloud-sql-backups-describe":{"n":"gcloud-sql-backups-describe","t":"displays backup information","s":[["TAGLINE","displays backup information"],["TLDR","**Describe a backup**\n\n```gcloud sql backups describe [backup_id] --instance [instance_name]```\n\n**Describe in JSON format**\n\n```gcloud sql backups describe [backup_id] --instance [instance] --format json```"],["SYNOPSIS","**gcloud** **sql** **backups** **describe** _backup_id_ **--instance**=_instance_ [_options_]"],["PARAMETERS","**--instance** _name_\n> Cloud SQL instance name.\n\n**--format** _format_\n> Output format."],["DESCRIPTION","**gcloud sql backups describe** displays detailed information about a specific Cloud SQL backup. It retrieves metadata including backup status, type (on-demand or automatic), creation timestamp, completion time, and backup size.\n\nThe command is useful for verifying backup completion, checking backup integrity before restoration, and auditing backup operations. Output can be formatted as JSON or YAML for programmatic processing or integration with automation scripts."]],"e":[],"x":"gcloud sql backups describe [backup_id] --instance [instance_name]"},"gh-status":{"n":"gh-status","t":"print information about relevant issues, pull requests and notifications","s":[["TAGLINE","print information about relevant issues, pull requests and notifications"],["TLDR","**Show** your status dashboard across subscribed repositories\n\n```gh status```\n\n**Limit** the report to one organization\n\n```gh status --org [orgname]```\n\n**Exclude** repositories from the report\n\n```gh status --exclude [owner/repo],[owner/other-repo]```"],["SYNOPSIS","**gh** **status** [_flags_]"],["PARAMETERS","**-o**, **--org** _string_\n> Report status within an organization.\n\n**-e**, **--exclude** _strings_\n> Comma separated list of repositories to exclude, in **owner/name** format."],["DESCRIPTION","**gh status** prints an overview of your GitHub work across the repositories you subscribe to. The output is split into four panes: **Assigned Issues**, **Assigned Pull Requests**, **Review Requests**, and **Mentions**, plus a **Repository Activity** section listing new issues, pull requests and comments.\n\nThe point is to answer \"what needs my attention\" without visiting the web dashboard or running several **gh issue list** and **gh pr list** invocations by hand. Because the report is built from your subscriptions rather than a fixed repository, it works from any directory, including outside a git repository.\n\n**--org** narrows every pane to a single organization, which is the usual way to separate work from personal activity. **--exclude** drops noisy repositories from the results."],["CAVEATS","The report only covers repositories you are subscribed to via **notifications**, so activity in repositories you merely have access to will not appear. Mentions come from your notification stream and are subject to its retention, so older mentions drop off.\n\nBuilding the report makes several API calls, and on accounts with many subscriptions it can take a few seconds and consume a noticeable share of your rate limit."],["HISTORY","**gh status** was added as a core command in **GitHub CLI 2.7.0** (**March 2022**), contributed by **vilmibm**, who had previously explored the same idea as a **gh** extension."],["INSTALL","```apt: sudo apt install gh```\n\n```dnf: sudo dnf install gh```\n\n```pacman: sudo pacman -S github-cli```\n\n```apk: sudo apk add github-cli```\n\n```zypper: sudo zypper install gh```\n\n```brew: brew install gh```\n\n```nix: nix profile install nixpkgs#gh```"],["SEE ALSO","[gh](/man/gh)(1), [gh-issue](/man/gh-issue)(1), [gh-pr](/man/gh-pr)(1), [gh-search](/man/gh-search)(1)"],["RESOURCES","```[Source code](https://github.com/cli/cli)```\n\n```[Homepage](https://cli.github.com/)```\n\n```[Documentation](https://cli.github.com/manual/gh_status)```"]],"e":["gh","gh-issue","gh-pr","gh-search"],"x":"gh status"},"git-instaweb":{"n":"git-instaweb","t":"Browse repository via web interface","s":[["TAGLINE","Browse repository via web interface"],["TLDR","**Start web interface**\n\n```git instaweb```\n\n**Start with specific HTTP daemon**\n\n```git instaweb --httpd=[lighttpd]```\n\n**Start on a specific port**\n\n```git instaweb --port=[8080]```\n\n**Start bound to localhost only**\n\n```git instaweb --local```\n\n**Stop the web server**\n\n```git instaweb --stop```\n\n**Restart the web server**\n\n```git instaweb --restart```"],["SYNOPSIS","**git** **instaweb** [_options_]"],["PARAMETERS","**-d**, **--httpd** _daemon_\n> HTTP daemon to use (lighttpd, apache2, mongoose, plackup, python, webrick). Default: lighttpd.\n\n**-p**, **--port** _port_\n> Port number to bind. Default: 1234.\n\n**-b**, **--browser** _browser_\n> Web browser to use for viewing.\n\n**-m**, **--module-path** _path_\n> Module path (only needed for Apache). Default: /usr/lib/apache2/modules.\n\n**--start**\n> Start the httpd instance and exit.\n\n**--stop**\n> Stop the httpd instance and exit.\n\n**--restart**\n> Restart the httpd instance and exit.\n\n**-l**, **--local**\n> Bind to localhost (127.0.0.1) only."],["DESCRIPTION","**git instaweb** instantly browses the working repository in gitweb by starting a temporary local web server. It provides a quick way to view repository history, branches, and files through a web interface without setting up a permanent server.\n\nThe command launches a web server (lighttpd by default, or apache2, mongoose, plackup, python, webrick) on port 1234 and opens the repository in your default browser. The interface shows commits, file browsing, blame annotations, and diff viewing. When done, use `--stop` to shut down the server.\n\nThis is particularly useful for demonstrating repository history to others or when you prefer a graphical view but don't want to use a full GUI application. The web interface is read-only and doesn't require network access."],["INSTALL","```dnf: sudo dnf install git-instaweb```"],["SEE ALSO","[gitweb](/man/gitweb)(1), [git-daemon](/man/git-daemon)(1)"]],"e":["gitweb","git-daemon"],"x":"git instaweb"},"git-mktree":{"n":"git-mktree","t":"Create tree object from ls-tree format","s":[["TAGLINE","Create tree object from ls-tree format"],["TLDR","**Create tree from ls-tree output**\n\n```git ls-tree HEAD | git mktree```\n\n**Create tree from file**\n\n```git mktree < [tree-listing.txt]```\n\n**Allow missing objects**\n\n```git mktree --missing```"],["SYNOPSIS","**git mktree** [_options_]"],["PARAMETERS","**--missing**\n> Allow missing objects.\n\n**-z**\n> NUL-terminated input.\n\n**--batch**\n> Process multiple trees.\n\n**--help**\n> Display help information."],["DESCRIPTION","**git mktree** creates a tree object from ls-tree formatted input. It reads file modes, object types, hashes, and names from stdin, creating a new tree object containing those entries. The command is the inverse of `git ls-tree`.\n\nThis plumbing tool enables programmatic tree creation and manipulation in scripts. Input must be properly formatted with each line specifying mode, type, hash, and filename. With `--batch`, it accepts multiple tree descriptions separated by blank lines and writes one tree object per batch, printing each resulting hash."],["CAVEATS","Plumbing command. Input must be properly formatted. Objects must exist unless --missing."],["HISTORY","git mktree is a core **Git** plumbing command for tree object creation, enabling programmatic manipulation of git's tree structure."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-ls-tree](/man/git-ls-tree)(1), [git-write-tree](/man/git-write-tree)(1)"],["RESOURCES","```[Source code](https://github.com/git/git)```\n\n```[Homepage](https://git-scm.com/)```\n\n```[Documentation](https://git-scm.com/docs/git-mktree)```"]],"e":["git-ls-tree","git-write-tree"],"x":"git ls-tree HEAD | git mktree"},"git-setup":{"n":"git-setup","t":"Initialize a repo and commit all existing files","s":[["TAGLINE","Initialize a repo and commit all existing files"],["TLDR","**Create a new repo** in the current directory with an initial commit\n\n```git setup```\n\n**Setup a repo** in a specific **directory**\n\n```git setup [path/to/directory]```"],["SYNOPSIS","**git setup** [_directory_]"],["PARAMETERS","_DIRECTORY_\n> Directory to set up (default: current directory)."],["DESCRIPTION","**git setup** initializes a new Git repository and makes an initial commit with all existing files. Part of the **git-extras** package, it combines `git init`, `git add .`, and `git commit -m \"Initial commit\"` into a single command.\n\nThis is useful for quickly turning an existing directory of files into a Git repository with a clean starting snapshot."],["CAVEATS","Part of the **git-extras** package and must be installed separately. Commits all files immediately, so ensure a **.gitignore** file is in place first to avoid tracking unwanted files."],["HISTORY","git setup is part of **git-extras**, a collection of useful Git utilities created by **TJ Holowaychuk**. It combines common initialization steps for new repositories."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-init](/man/git-init)(1), [git-add](/man/git-add)(1), [git-commit](/man/git-commit)(1), [git-extras](/man/git-extras)(1), [git-ignore](/man/git-ignore)(1)"]],"e":["git-init","git-add","git-commit","git-extras","git-ignore"],"x":"git setup"},"git-summary":{"n":"git-summary","t":"Show repository summary with contribution statistics","s":[["TAGLINE","Show repository summary with contribution statistics"],["TLDR","**Show repository summary** with commit statistics per author\n\n```git summary```\n\n**Show line-based statistics** instead of commits\n\n```git summary --line```\n\n**Show line statistics for a specific path**\n\n```git summary --line [path/to/dir]```\n\n**Dedup authors by email**\n\n```git summary --dedup-by-email```\n\n**Exclude merge commits**\n\n```git summary --no-merges```\n\n**Show output in tabular format**\n\n```git summary --output-style tabular```"],["SYNOPSIS","**git** **summary** [_options_] [_path_]"],["PARAMETERS","**--line**\n> Show line statistics instead of commits. Can take an optional path to filter.\n\n**--dedup-by-email**\n> Deduplicate authors by email address.\n\n**--no-merges**\n> Exclude merge commits from the summary.\n\n**--output-style** _style_\n> Output format: tabular or oneline."],["DESCRIPTION","**git summary** outputs a repository overview including project age, number of active days, total commits, and per-author contribution statistics. Part of git-extras, it provides a quick snapshot of project activity."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-log](/man/git-log)(1), [git-shortlog](/man/git-shortlog)(1), [git-extras](/man/git-extras)(1)"]],"e":["git-log","git-shortlog","git-extras"],"x":"git summary"},"httpx":{"n":"httpx","t":"fast HTTP toolkit for probing web servers","s":[["TAGLINE","fast HTTP toolkit for probing web servers"],["TLDR","**Probe live hosts**\n\n```cat [hosts.txt] | httpx```\n\n**Check with title**\n\n```httpx -l [hosts.txt] -title```\n\n**Show status codes**\n\n```httpx -l [hosts.txt] -status-code```\n\n**Check specific ports**\n\n```httpx -l [hosts.txt] -ports [80,443,8080]```\n\n**Follow redirects**\n\n```httpx -l [hosts.txt] -follow-redirects```\n\n**Output JSON**\n\n```httpx -l [hosts.txt] -json -o [output.json]```"],["SYNOPSIS","**httpx** [_options_]"],["PARAMETERS","**-l**, **-list** _FILE_\n> Input file containing the list of hosts to process.\n\n**-u**, **-target** _HOST_\n> Probe a specific target host directly instead of reading a list.\n\n**-title**\n> Display the page title.\n\n**-sc**, **-status-code**\n> Display the response status code.\n\n**-td**, **-tech-detect**\n> Display detected technologies (based on the Wappalyzer dataset).\n\n**-ip**\n> Display the resolved host IP.\n\n**-p**, **-ports** _PORTS_\n> Ports to probe, using nmap-style syntax (e.g. `http:1,2-10,https:443`).\n\n**-x** _METHOD_\n> HTTP method(s) to probe; use `all` to try every method.\n\n**-H** _HEADER_\n> Custom HTTP header to send with each request.\n\n**-fr**, **-follow-redirects**\n> Follow HTTP redirects.\n\n**-mc**, **-match-code** _CODES_\n> Only show responses matching the given status codes (e.g. `-mc 200,301`).\n\n**-fc**, **-filter-code** _CODES_\n> Exclude responses matching the given status codes.\n\n**-t**, **-threads** _N_\n> Number of concurrent threads (default 50).\n\n**-rl**, **-rate-limit** _N_\n> Maximum requests per second (default 150).\n\n**-timeout** _SECONDS_\n> Request timeout (default 10).\n\n**-retries** _N_\n> Number of retries on failure.\n\n**-silent**\n> Only print results, suppressing the banner and stats.\n\n**-json**\n> Store output in JSONL(ines) format.\n\n**-o**, **-output** _FILE_\n> File to write results to.\n\n**-help**\n> Display help information."],["DESCRIPTION","**httpx** is a fast, multi-purpose HTTP toolkit built on Go's `retryablehttp` library for running many probes against a list of hosts at once. It checks which hosts are alive and can report status codes, titles, response headers, TLS data, detected technologies, screenshots, and more.\n\nBy default it probes with the **HTTPS** scheme first and falls back to **HTTP** only if HTTPS is unreachable; **-no-fallback** shows both. The tool is built for reconnaissance and security testing workflows, processing large host lists concurrently and integrating with the rest of the ProjectDiscovery toolchain (**nuclei**, **subfinder**, etc.)."],["CAVEATS","Intended for reconnaissance and security testing against systems you're authorized to test. Requires Go to build from source; prebuilt binaries are available. Flags are single-dash (e.g. `-json`, not `--json`)."],["HISTORY","httpx was created by **ProjectDiscovery** as part of its open-source security reconnaissance toolkit, alongside tools like **nuclei** and **subfinder**."],["INSTALL","```apk: sudo apk add py3-httpx```\n\n```brew: brew install httpx```\n\n```nix: nix profile install nixpkgs#httpx```"],["SEE ALSO","[nuclei](/man/nuclei)(1), [nmap](/man/nmap)(1), [httprobe](/man/httprobe)(1)"],["RESOURCES","```[Source code](https://github.com/projectdiscovery/httpx)```\n\n```[Homepage](https://projectdiscovery.io)```\n\n```[Documentation](https://docs.projectdiscovery.io/opensource/httpx)```"]],"e":["nuclei","nmap","httprobe"],"x":"cat [hosts.txt] | httpx"},"jj-bisect":{"n":"jj-bisect","t":"performs binary search through commit history to find which commit introduced","s":[["TAGLINE","performs binary search through commit history to find which commit introduced"],["TLDR","**Start bisect session**\n\n```jj bisect start```\n\n**Mark current revision as good**\n\n```jj bisect good```\n\n**Mark current revision as bad**\n\n```jj bisect bad```\n\n**Reset bisect session**\n\n```jj bisect reset```"],["SYNOPSIS","**jj** **bisect** _subcommand_ [_options_]"],["PARAMETERS","**start**\n> Begin bisect session.\n\n**good** [_revision_]\n> Mark revision as good.\n\n**bad** [_revision_]\n> Mark revision as bad.\n\n**reset**\n> End bisect session.\n\n**skip**\n> Skip current revision."],["DESCRIPTION","**jj bisect** performs binary search through commit history to find which commit introduced a bug. Marks commits as good or bad to narrow down the problematic change. Similar to git bisect but integrated with Jujutsu's revision model."],["INSTALL","```pacman: sudo pacman -S jujutsu```\n\n```apk: sudo apk add jujutsu```\n\n```zypper: sudo zypper install jujutsu```\n\n```brew: brew install jujutsu```\n\n```nix: nix profile install nixpkgs#jujutsu```"],["SEE ALSO","[jj](/man/jj)(1), [jj-log](/man/jj-log)(1)"]],"e":["jj","jj-log"],"x":"jj bisect start"},"kitty":{"n":"kitty","t":"GPU-accelerated terminal emulator","s":[["TAGLINE","GPU-accelerated terminal emulator"],["TLDR","**Start kitty**\n\n```kitty```\n\n**Start with a specific command**\n\n```kitty [command]```\n\n**Start in a specific directory**\n\n```kitty -d [/path/to/directory]```\n\n**Start with a session layout**\n\n```kitty --session [session.conf]```\n\n**Override a configuration option**\n\n```kitty -o [font_size=14]```\n\n**Open a new window via remote control**\n\n```kitty @ new-window```\n\n**Open a new tab via remote control**\n\n```kitty @ new-tab```\n\n**List all windows as JSON**\n\n```kitty @ ls```"],["SYNOPSIS","**kitty** [_options_] [_command_]"],["PARAMETERS","_COMMAND_\n> Command to run in the terminal.\n\n**@**\n> Remote control prefix for sending commands to a running kitty instance.\n\n**--config** _FILE_, **-c** _FILE_\n> Path to configuration file(s) to use.\n\n**--override** _OPTION=VALUE_, **-o** _OPTION=VALUE_\n> Override individual configuration options.\n\n**--start-as** _MODE_\n> Window mode: normal, fullscreen, maximized, minimized.\n\n**--directory** _DIR_, **-d** _DIR_\n> Change to the specified directory when launching.\n\n**--session** _FILE_\n> Path to a file containing the startup session (tabs, windows, layout, programs).\n\n**--single-instance**, **-1**\n> Open a new window in an existing kitty instance if one is running.\n\n**--listen-on** _ADDRESS_\n> Listen on the specified address for remote control messages.\n\n**--title** _TITLE_, **-T** _TITLE_\n> Set the OS window title.\n\n**--class** _CLS_\n> Set the WM_CLASS window property (or Wayland app id).\n\n**--detach**\n> Detach from the controlling terminal."],["DESCRIPTION","**kitty** is a fast, feature-rich GPU-accelerated terminal emulator that renders using OpenGL for smooth performance. It supports inline image display, font ligatures, Unicode, multiple layouts, tabs and windows, and extensible kitten programs for specialized tasks. It is highly configurable via kitty.conf and scriptable through its remote control protocol."],["CAVEATS","Requires GPU with OpenGL 3.3+ support. Uses its own terminfo entry (xterm-kitty), which may need to be installed on remote servers for SSH sessions. Remote control requires --listen-on or allow_remote_control in kitty.conf."],["HISTORY","kitty was created by **Kovid Goyal** as a fast, feature-rich terminal emulator leveraging modern GPU capabilities."],["INSTALL","```apt: sudo apt install kitty```\n\n```dnf: sudo dnf install kitty```\n\n```pacman: sudo pacman -S kitty```\n\n```apk: sudo apk add kitty```\n\n```zypper: sudo zypper install kitty```\n\n```nix: nix profile install nixpkgs#kitty```"],["SEE ALSO","[alacritty](/man/alacritty)(1), [wezterm](/man/wezterm)(1), [tmux](/man/tmux)(1)"]],"e":["alacritty","wezterm","tmux"],"x":"kitty"},"loadndisdriver-1.9":{"n":"loadndisdriver-1.9","t":"Userspace NDIS driver loader for the ndiswrapper kernel module (version 1.9)","s":[["TAGLINE","Userspace NDIS driver loader for the ndiswrapper kernel module (version 1.9)"],["TLDR","This is an internal support tool used by the ndiswrapper kernel module. It should not be invoked directly by users. Use **ndiswrapper** to manage NDIS drivers instead.\n\n**Install a Windows driver using ndiswrapper instead**\n\n```sudo ndiswrapper -i [path/to/driver.inf]```\n\n**List installed NDIS drivers**\n\n```ndiswrapper -l```"],["SYNOPSIS","**loadndisdriver-1.9**"],["DESCRIPTION","**loadndisdriver-1.9** is a version-specific support program for the **ndiswrapper** Linux kernel module. The ndiswrapper kernel module uses it to load Windows NDIS drivers that have been installed via the **ndiswrapper**(8) tool.\n\nThis tool should not be used directly. Its options are internal and subject to change. It expects to find NDIS driver files in the **/etc/ndiswrapper** directory."],["CAVEATS","Not intended for direct use. The ndiswrapper approach to wireless networking is largely deprecated in favor of native Linux drivers."],["SEE ALSO","[loadndisdriver](/man/loadndisdriver)(8), [ndiswrapper](/man/ndiswrapper)(8), [ndiswrapper-1.9](/man/ndiswrapper-1.9)(8)"]],"e":["loadndisdriver","ndiswrapper","ndiswrapper-1.9"],"x":"sudo ndiswrapper -i [path/to/driver.inf]"},"mbind":{"n":"mbind","t":"system call that sets the NUMA memory policy for a specified memory range","s":[["TAGLINE","system call that sets the NUMA memory policy for a specified memory range"],["TLDR","**Set NUMA policy for memory range (C code)**\n\n```mbind([addr], [length], MPOL_BIND, [&nodemask], [maxnode], 0)```\n\n**Bind memory to specific node**\n\n```mbind([ptr], [size], MPOL_BIND, [&mask], [maxnode], MPOL_MF_MOVE)```\n\n**Interleave memory across nodes**\n\n```mbind([addr], [len], MPOL_INTERLEAVE, [&nodemask], [maxnode], 0)```"],["SYNOPSIS","**long mbind**(void *_addr_, unsigned long _len_, int _mode_, const unsigned long *_nodemask_, unsigned long _maxnode_, unsigned _flags_)"],["PARAMETERS","**addr**\n> Starting address of memory range.\n\n**len**\n> Length of memory range.\n\n**mode**\n> MPOL_DEFAULT, MPOL_BIND, MPOL_INTERLEAVE, MPOL_PREFERRED.\n\n**nodemask**\n> Bitmask of NUMA nodes.\n\n**maxnode**\n> Maximum node number + 1.\n\n**flags**\n> MPOL_MF_STRICT, MPOL_MF_MOVE, MPOL_MF_MOVE_ALL."],["DESCRIPTION","**mbind** is a system call that sets the NUMA memory policy for a specified memory range. It controls which NUMA nodes are used for memory allocation within that range.\n\nThis is used for performance optimization on NUMA systems by controlling memory locality."],["POLICIES","```\nMPOL_DEFAULT    - Use process default\nMPOL_BIND       - Strict binding to nodes\nMPOL_INTERLEAVE - Round-robin across nodes\nMPOL_PREFERRED  - Prefer specified node\n```"],["CAVEATS","Requires NUMA hardware. Only affects future allocations unless flags specify migration. Privileged flags may require CAP_SYS_NICE."],["HISTORY","mbind was added to Linux kernel **2.6.7** as part of NUMA memory policy support, developed primarily by **Andi Kleen** at SUSE."],["SEE ALSO","[numactl](/man/numactl)(8), [numastat](/man/numastat)(8)"]],"e":["numactl","numastat"],"x":"mbind([addr], [length], MPOL_BIND, [&nodemask], [maxnode], 0)"},"msmtp":{"n":"msmtp","t":"lightweight SMTP client for sending email from the command line","s":[["TAGLINE","lightweight SMTP client for sending email from the command line"],["TLDR","**Send email**\n\n```echo \"[message]\" | msmtp [recipient@example.com]```\n\n**Send email with subject**\n\n```echo -e \"Subject: [Hello]\\n\\n[Body]\" | msmtp [recipient@example.com]```\n\n**Send from specific account**\n\n```msmtp -a [account_name] [recipient@example.com]```\n\n**Test configuration**\n\n```msmtp --serverinfo```\n\n**Check account configuration**\n\n```msmtp --configure [account_name]```\n\n**Send file as message**\n\n```msmtp [recipient@example.com] < [message.txt]```\n\n**Print configuration**\n\n```msmtp -P```"],["SYNOPSIS","**msmtp** [_-a account_] [_--serverinfo_] [_options_] [_recipients_]"],["PARAMETERS","**-a** _ACCOUNT_, **--account** _ACCOUNT_\n> Use specified account.\n\n**-f** _ADDRESS_, **--from** _ADDRESS_\n> Set From address.\n\n**-C** _FILE_, **--file** _FILE_\n> Use configuration file.\n\n**--serverinfo**\n> Print server information.\n\n**-P**, **--pretend**\n> Print configuration, don't send.\n\n**--configure** _ACCOUNT_\n> Show account configuration.\n\n**-d**, **--debug**\n> Debug mode.\n\n**-t**, **--read-recipients**\n> Read recipients from message.\n\n**-X** _FILE_, **--logfile** _FILE_\n> Log to file.\n\n**--tls**\n> Use TLS encryption.\n\n**--tls-starttls**\n> Use STARTTLS.\n\n**--host** _HOST_\n> SMTP server host.\n\n**--port** _PORT_\n> SMTP server port.\n\n**--user** _USER_\n> Authentication username.\n\n**--passwordeval** _CMD_\n> Get password from command."],["DESCRIPTION","**msmtp** is a lightweight SMTP client for sending email from the command line. It works as a sendmail replacement, enabling scripts and applications to send mail through external SMTP servers.\n\nConfiguration in ~/.msmtprc defines accounts with server settings, authentication, and TLS options. Multiple accounts can be configured for different mail servers or identities.\n\nPassword handling supports several methods: plaintext (not recommended), passwordeval to run a command (e.g., password manager), or system keyring integration.\n\nTLS is supported via STARTTLS or direct TLS connection. Certificate verification can be configured to use system CA certificates or specific files.\n\nAs a sendmail replacement, msmtp can be symlinked to /usr/sbin/sendmail, making it the default mail transport for the system. Many applications use sendmail for outgoing mail.\n\nIntegration with mutt, git, and other tools is straightforward - they just need to know the sendmail path or can use msmtp directly."],["CAVEATS","Requires SMTP server access. Password storage needs consideration. Some providers require app-specific passwords. TLS certificate issues may occur. Queue/offline sending requires msmtpd or msmtp-queue."],["HISTORY","**msmtp** was written by **Martin Lambers** starting around **2000** as a simple sendmail replacement. It focused on being lightweight and configurable, supporting modern SMTP with authentication and TLS. The project remains actively maintained."],["INSTALL","```apt: sudo apt install msmtp```\n\n```dnf: sudo dnf install msmtp```\n\n```pacman: sudo pacman -S msmtp```\n\n```apk: sudo apk add msmtp```\n\n```zypper: sudo zypper install msmtp```\n\n```brew: brew install msmtp```\n\n```nix: nix profile install nixpkgs#msmtp```"],["SEE ALSO","[sendmail](/man/sendmail)(1), [mutt](/man/mutt)(1), [mailx](/man/mailx)(1)"]],"e":["sendmail","mutt","mailx"],"x":"echo \"[message]\" | msmtp [recipient@example.com]"},"mutt":{"n":"mutt","t":"text-based email client","s":[["TAGLINE","text-based email client"],["TLDR","**Start Mutt**\n\n```mutt```\n\n**Send email from command line**\n\n```echo \"[body]\" | mutt -s \"[subject]\" [recipient@example.com]```\n\n**Send with attachment**\n\n```mutt -s \"[subject]\" -a [file.pdf] -- [recipient@example.com]```\n\n**Open specific mailbox**\n\n```mutt -f [/path/to/mailbox]```\n\n**Send with CC**\n\n```mutt -s \"[subject]\" -c [cc@example.com] [to@example.com]```\n\n**Use alternate config**\n\n```mutt -F [~/.muttrc.alt]```"],["SYNOPSIS","**mutt** [_options_] [_address_]"],["PARAMETERS","_ADDRESS_\n> Email recipient address.\n\n**-s** _SUBJECT_\n> Email subject.\n\n**-a** _FILE_\n> Attach file.\n\n**-c** _ADDRESS_\n> CC address.\n\n**-f** _MAILBOX_\n> Open mailbox.\n\n**-F** _FILE_\n> Config file.\n\n**--help**\n> Display help information."],["DESCRIPTION","**mutt** is a text-based email client. It's highly configurable with Vim-like keybindings.\n\nThe tool supports IMAP, POP3, and local mail. Features threading, PGP, and MIME."],["CAVEATS","Configuration required. Learning curve for keybindings. Text-based interface."],["HISTORY","Mutt was created by **Michael Elstrøm** in 1995 as a fork of EstrøM, becoming a popular terminal mail client."],["INSTALL","```apt: sudo apt install mutt```\n\n```dnf: sudo dnf install mutt```\n\n```pacman: sudo pacman -S mutt```\n\n```apk: sudo apk add mutt```\n\n```zypper: sudo zypper install mutt```\n\n```brew: brew install mutt```\n\n```nix: nix profile install nixpkgs#mutt```"],["SEE ALSO","[neomutt](/man/neomutt)(1), [alpine](/man/alpine)(1), [mailx](/man/mailx)(1)"]],"e":["neomutt","alpine","mailx"],"x":"mutt"},"neoss":{"n":"neoss","t":"User-friendly socket statistics with a TUI","s":[["TAGLINE","User-friendly socket statistics with a TUI"],["TLDR","**View active socket connections**\n\n```neoss```\n\n**Install neoss globally via npm**\n\n```npm install -g neoss```"],["SYNOPSIS","**neoss**"],["DESCRIPTION","**neoss** is a user-friendly TUI that provides detailed TCP and UDP socket statistics. It aims to replace the usual **ss** command for basic usage with a clear and simple terminal interface that allows you to sort, refresh, and navigate through results.\n\nThe TUI displays in-use sockets with their respective statistics including local and remote addresses, states, and associated processes. You can select any data point to get an explanation of its meaning."],["CAVEATS","Requires elevated privileges (sudo) for full process information. Requires Node.js and npm to install. Available as an npm package via `npm install -g neoss`."],["HISTORY","**neoss** was created by **PabloLec** and is written in **TypeScript**."],["INSTALL","```aur: yay -S neoss```"],["SEE ALSO","[ss](/man/ss)(8), [netstat](/man/netstat)(8)"]],"e":["ss","netstat"],"x":"neoss"},"odps-auth":{"n":"odps-auth","t":"manages MaxCompute authentication","s":[["TAGLINE","manages MaxCompute authentication"],["TLDR","**Login to ODPS**\n\n```odps auth login```\n\n**Configure credentials**\n\n```odps auth config --access-id [id] --access-key [key]```\n\n**Show current user**\n\n```odps auth whoami```\n\n**Logout**\n\n```odps auth logout```"],["SYNOPSIS","**odps** **auth** [_command_] [_options_]"],["PARAMETERS","**login**\n> Authenticate to ODPS.\n\n**logout**\n> Remove credentials.\n\n**config**\n> Configure authentication.\n\n**whoami**\n> Show current identity.\n\n**--access-id** _ID_\n> Access key ID.\n\n**--access-key** _KEY_\n> Access key secret.\n\n**--help**\n> Display help information."],["DESCRIPTION","**odps auth** manages MaxCompute authentication. Configures access credentials.\n\nThe tool handles login and credential storage. Part of ODPS CLI."],["CAVEATS","Credentials stored locally. Part of Alibaba Cloud ODPS tools."],["HISTORY","odps auth provides **authentication management** for Alibaba Cloud MaxCompute."],["SEE ALSO","[odps](/man/odps)(1), [odpscmd](/man/odpscmd)(1)"]],"e":["odps","odpscmd"],"x":"odps auth login"},"otel-cli":{"n":"otel-cli","t":"OpenTelemetry CLI for sending traces, metrics, and logs","s":[["TAGLINE","OpenTelemetry CLI for sending traces, metrics, and logs"],["TLDR","**Send a span** for a command\n\n```otel-cli exec --service [my-app] --name [build] -- [make]```\n\n**Emit a server span** manually\n\n```otel-cli span --service [api] --name [handle-request]```\n\n**Status** / configuration check\n\n```otel-cli status```"],["SYNOPSIS","**otel-cli** *command* [*options*]"],["DESCRIPTION","**otel-cli** is a command-line client for OpenTelemetry. Wrap processes with **otel-cli exec** to create spans around builds and scripts, or emit spans/events directly for shell-based observability pipelines. Configure the OTLP endpoint via environment variables (e.g. **OTEL_EXPORTER_OTLP_ENDPOINT**) or flags."],["PARAMETERS","**exec** **--** *command*...\n\n> Run a command inside a span.\n\n**span**\n\n> Create/end spans with attributes.\n\n**status**\n\n> Show configuration / connectivity.\n\n**--service** *name* / **--name** *span*\n\n> Common identity attributes.\n\nSee **otel-cli --help** for vendor-specific exporters and W3C traceparent propagation helpers."],["CAVEATS","Requires a reachable OpenTelemetry collector or backend. Incorrect endpoints silently drop telemetry. Clock skew affects span timing."],["INSTALL","```brew: brew install otel-cli```\n\n```nix: nix profile install nixpkgs#otel-cli```"],["SEE ALSO","[curl](/man/curl)(1), [time](/man/time)(1)"],["RESOURCES","```[Source code](https://github.com/equinix-labs/otel-cli)```"]],"e":["curl","time"],"x":"otel-cli exec --service [my-app] --name [build] -- [make]"},"outguess":{"n":"outguess","t":"steganography tool that hides data in JPEG images","s":[["TAGLINE","steganography tool that hides data in JPEG images"],["TLDR","**Embed data in image**\n\n```outguess -d [secret.txt] [cover.jpg] [output.jpg]```\n\n**Extract hidden data**\n\n```outguess -r [stego.jpg] [output.txt]```\n\n**Embed with password**\n\n```outguess -k \"[password]\" -d [secret.txt] [cover.jpg] [output.jpg]```\n\n**Extract with password**\n\n```outguess -k \"[password]\" -r [stego.jpg] [output.txt]```\n\n**Check embedding capacity**\n\n```outguess -c [cover.jpg]```"],["SYNOPSIS","**outguess** [_options_] [_input_] [_output_]"],["PARAMETERS","**-d** _file_\n> Data file to embed.\n\n**-r**\n> Extract mode.\n\n**-k** _key_\n> Password/key.\n\n**-c**\n> Check capacity.\n\n**-F**\n> Force embedding.\n\n**-e**\n> Use error correction."],["DESCRIPTION","**OutGuess** is a steganography tool that hides data in JPEG images. It preserves image statistics to resist statistical analysis, making detection harder than simpler methods.\n\nOutGuess modifies DCT coefficients in ways that maintain expected statistical distributions."],["EXAMPLE","```bash"],["Hide message","outguess -k \"secret\" -d message.txt photo.jpg stego.jpg"],["Recover message","outguess -k \"secret\" -r stego.jpg recovered.txt\n```"],["CAVEATS","JPEG only. Repeated embedding degrades image. Capacity depends on image complexity. Password provides encryption."],["HISTORY","OutGuess was developed by **Niels Provos** as a steganographic tool resistant to statistical steganalysis techniques."],["INSTALL","```apt: sudo apt install outguess```\n\n```nix: nix profile install nixpkgs#outguess```"],["SEE ALSO","[steghide](/man/steghide)(1), [openstego](/man/openstego)(1)"]],"e":["steghide","openstego"],"x":"outguess -d [secret.txt] [cover.jpg] [output.jpg]"},"pamcut":{"n":"pamcut","t":"extracts a rectangular region from a PAM or PNM image","s":[["TAGLINE","extracts a rectangular region from a PAM or PNM image"],["TLDR","**Cut region from image**\n\n```pamcut [x] [y] [width] [height] [input.pam] > [output.pam]```\n\n**Cut from coordinates**\n\n```pamcut -left [100] -top [50] -width [200] -height [150] [input.pam] > [output.pam]```\n\n**Cut right portion**\n\n```pamcut -right [100] -bottom [100] -width [200] -height [200] [input.pam] > [output.pam]```\n\n**Pad if outside bounds**\n\n```pamcut -pad -left [-10] -top [-10] -width [300] -height [300] [input.pam] > [output.pam]```"],["SYNOPSIS","**pamcut** [_options_] [_x_ _y_ _width_ _height_] [_pamfile_]"],["PARAMETERS","**-left** _x_\n> Left edge position.\n\n**-right** _x_\n> Right edge position.\n\n**-top** _y_\n> Top edge position.\n\n**-bottom** _y_\n> Bottom edge position.\n\n**-width** _w_\n> Output width.\n\n**-height** _h_\n> Output height.\n\n**-pad**\n> Pad with black if outside."],["DESCRIPTION","**pamcut** extracts a rectangular region from a PAM or PNM image. Coordinates can be specified from any corner using combinations of left/right and top/bottom."],["EXAMPLE","```bash"],["Cut 100x100 from top-left corner","pamcut 0 0 100 100 photo.ppm > crop.ppm"],["Cut center region","pamcut -left 100 -top 100 -width 200 -height 200 input.ppm > center.ppm\n```"],["CAVEATS","Coordinates are 0-indexed. Without -pad, region must be within image bounds."],["HISTORY","pamcut is part of **Netpbm**, evolving from the original PBMplus toolkit created by **Jef Poskanzer**."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pnmcut](/man/pnmcut)(1), [pamcomp](/man/pamcomp)(1), [pamscale](/man/pamscale)(1)"]],"e":["pnmcut","pamcomp","pamscale"],"x":"pamcut [x] [y] [width] [height] [input.pam] > [output.pam]"},"pdffonts":{"n":"pdffonts","t":"lists fonts used in PDF documents","s":[["TAGLINE","lists fonts used in PDF documents"],["TLDR","**List fonts in PDF**\n\n```pdffonts [document.pdf]```\n\n**List fonts with page range**\n\n```pdffonts -f [1] -l [10] [document.pdf]```\n\n**Substitute font names**\n\n```pdffonts -subst [document.pdf]```\n\n**List from specific page**\n\n```pdffonts -f [5] [document.pdf]```"],["SYNOPSIS","**pdffonts** [_-f first_] [_-l last_] [_-subst_] [_options_] _file.pdf_"],["PARAMETERS","**-f** _PAGE_\n> First page to examine.\n\n**-l** _PAGE_\n> Last page to examine.\n\n**-subst**\n> Show font substitutions.\n\n**-opw** _PASSWORD_\n> Owner password.\n\n**-upw** _PASSWORD_\n> User password.\n\n**-v**\n> Print version.\n\n**-h**\n> Print help."],["OUTPUT COLUMNS","**name**\n> Font name.\n\n**type**\n> Font type (Type 1, TrueType, CID, etc.).\n\n**emb**\n> Embedded (yes/no).\n\n**sub**\n> Subset (yes/no).\n\n**uni**\n> Unicode mapping (yes/no).\n\n**object ID**\n> PDF object identifier."],["DESCRIPTION","**pdffonts** lists fonts used in PDF documents. It shows font names, types, and embedding status.\n\nEmbedded fonts are included in the PDF, ensuring consistent display regardless of installed fonts. Non-embedded fonts rely on system fonts, which may cause appearance differences.\n\nSubset fonts contain only characters used in the document, reducing file size. Full fonts include all glyphs.\n\nFont types include Type 1 (PostScript), TrueType, CID fonts (for Asian languages), and Type 3 (bitmap/user-defined).\n\nUnicode mapping indicates whether text can be reliably extracted or searched. Missing Unicode mapping affects copy/paste and accessibility.\n\nPage range options examine specific sections without processing the entire document."],["CAVEATS","Part of poppler-utils package. May not show all font details. Password needed for encrypted PDFs. Font names may be cryptic for subsets."],["HISTORY","**pdffonts** is part of **poppler-utils**, derived from the **Xpdf** project created by **Derek Noonburg**. Poppler became the standard PDF rendering library for many open-source applications."],["INSTALL","```apt: sudo apt install poppler-utils```\n\n```dnf: sudo dnf install poppler-utils```\n\n```apk: sudo apk add poppler-utils```\n\n```nix: nix profile install nixpkgs#poppler-utils```"],["SEE ALSO","[pdfinfo](/man/pdfinfo)(1), [pdftotext](/man/pdftotext)(1), [pdftops](/man/pdftops)(1), [pdftocairo](/man/pdftocairo)(1)"]],"e":["pdfinfo","pdftotext","pdftops","pdftocairo"],"x":"pdffonts [document.pdf]"},"pdfgrep":{"n":"pdfgrep","t":"searches for text patterns in PDF files, similar to grep but for PDFs","s":[["TAGLINE","searches for text patterns in PDF files, similar to grep but for PDFs"],["TLDR","**Search for pattern in PDF**\n\n```pdfgrep \"[pattern]\" [file.pdf]```\n\n**Case-insensitive search showing page numbers**\n\n```pdfgrep -in \"[pattern]\" [file.pdf]```\n\n**Search recursively in directory**\n\n```pdfgrep -r \"[pattern]\" [/path/to/pdfs/]```\n\n**Count matches per file**\n\n```pdfgrep -c \"[pattern]\" [*.pdf]```\n\n**Print only filenames with matches**\n\n```pdfgrep -l \"[pattern]\" [*.pdf]```\n\n**Search with multiple patterns**\n\n```pdfgrep -e \"[pattern1]\" -e \"[pattern2]\" [file.pdf]```\n\n**Limit search to a page range**\n\n```pdfgrep --page-range=[1-10] \"[pattern]\" [file.pdf]```\n\n**Print only the matched text**\n\n```pdfgrep -o \"[pattern]\" [file.pdf]```"],["SYNOPSIS","**pdfgrep** [_OPTIONS_] _PATTERN_ _FILE_...\n\n**pdfgrep** [_OPTIONS_] {**-e** _PATTERN_|**-f** _FILE_}... _FILE_...\n\n**pdfgrep** [_OPTIONS_] **-r**|**-R** _PATTERN_ [_FILE_|_DIR_...]"],["PARAMETERS","**-e** _PATTERN_, **--regexp**=_PATTERN_\n> Specify a search pattern. Can be used multiple times to match any of several patterns.\n\n**-f** _FILE_, **--file**=_FILE_\n> Read patterns from a file, one per line.\n\n**-i**, **--ignore-case**\n> Case-insensitive matching.\n\n**-F**, **--fixed-strings**\n> Treat the pattern as a fixed string (no regular expression interpretation).\n\n**-P**, **--perl-regexp**\n> Use Perl-compatible regular expressions (PCRE2).\n\n**-n**, **--page-number**[=_TYPE_]\n> Prefix each match with its page number. _TYPE_ is `index` (default) or `label`.\n\n**-c**, **--count**\n> Print match count per file instead of matched lines.\n\n**-p**, **--page-count**\n> Print match count per page (implies **-n**).\n\n**-l**, **--files-with-matches**\n> Print only filenames that contain a match.\n\n**-L**, **--files-without-match**\n> Print only filenames that contain no match.\n\n**-o**, **--only-matching**\n> Print only the matched portion of each line.\n\n**-H**, **--with-filename**\n> Print the filename with each match (default when searching multiple files).\n\n**-h**, **--no-filename**\n> Suppress filename prefix in output.\n\n**-Z**, **--null**\n> Use a null byte instead of a colon to separate the filename from the rest of the output line. Useful for filenames containing colons or spaces.\n\n**--match-prefix-separator** _SEP_\n> Use _SEP_ as the separator between the match prefix (filename, page number) and the matched line, instead of the default colon.\n\n**-r**, **--recursive**\n> Search all PDF files under each directory recursively. Symlinks are followed only when specified on the command line.\n\n**-R**, **--dereference-recursive**\n> Like **-r**, but follow all symlinks.\n\n**--include**=_GLOB_\n> Only search files whose names match _GLOB_ (default: `*.pdf`).\n\n**--exclude**=_GLOB_\n> Skip files whose names match _GLOB_.\n\n**-A** _NUM_, **--after-context**=_NUM_\n> Print _NUM_ lines of context after each match.\n\n**-B** _NUM_, **--before-context**=_NUM_\n> Print _NUM_ lines of context before each match.\n\n**-C** _NUM_, **--context**=_NUM_\n> Print _NUM_ lines of context before and after each match.\n\n**--page-range**=_RANGE_\n> Limit the search to the specified page range (e.g., `1-10,15`).\n\n**-m** _NUM_, **--max-count**=_NUM_\n> Stop after _NUM_ matches per file.\n\n**--password**=_PASSWORD_\n> Use _PASSWORD_ to decrypt a password-protected PDF.\n\n**--color** _WHEN_\n> Colorize output: `auto` (default), `always`, or `never`.\n\n**--cache**\n> Cache rendered page text to speed up repeated searches on the same files.\n\n**--unac**\n> Remove accents and ligatures from both the search pattern and the document text. Useful for matching words like \"ae\" against the ligature \"æ\".\n\n**--warn-empty**\n> Warn when a PDF contains no searchable text (e.g., scanned images without OCR).\n\n**-q**, **--quiet**\n> Suppress all output. Exit status indicates whether a match was found.\n\n**-V**, **--version**\n> Print version information."],["DESCRIPTION","**pdfgrep** searches for text patterns in PDF files using the Poppler library for text extraction. It provides a familiar grep-like interface for PDF documents.\n\nText is extracted from each page and matched against the given regular expression. By default pdfgrep uses PCRE2 for pattern matching. Fixed-string matching is available via **-F**.\n\nPage number output (**-n**) helps locate matches within a document. Restricting the search to a page range (**--page-range**) speeds up searches on large files. Context lines (**-C**) show surrounding text to aid understanding of a match.\n\nRecursive search (**-r**) processes entire directory trees. Combined with **--include** and **--exclude**, this enables targeted searches across document collections. Multiple patterns can be specified with repeated **-e** options or read from a file with **-f**.\n\nThe **--unac** option is useful when PDFs use typographic ligatures or accented characters that differ from the search term. The **--cache** option stores extracted text to accelerate repeated searches."],["EXIT STATUS","**0**\n> One or more matches were found.\n\n**1**\n> No matches were found.\n\n**2**\n> An error occurred."],["CAVEATS","Text extraction quality depends on the PDF's internal structure. Scanned PDFs without embedded text require OCR preprocessing before pdfgrep can search them (use **--warn-empty** to detect such files). Complex multi-column layouts may not extract in reading order. Encrypted PDFs require the correct **--password**."],["HISTORY","**pdfgrep** was written by **Hans-Peter Deifel** starting around **2010**. It uses the Poppler library for PDF parsing and provides a grep-compatible interface for searching PDF text content."],["INSTALL","```apt: sudo apt install pdfgrep```\n\n```dnf: sudo dnf install pdfgrep```\n\n```pacman: sudo pacman -S pdfgrep```\n\n```apk: sudo apk add pdfgrep```\n\n```zypper: sudo zypper install pdfgrep```\n\n```brew: brew install pdfgrep```\n\n```nix: nix profile install nixpkgs#pdfgrep```"],["SEE ALSO","[grep](/man/grep)(1), [pdftotext](/man/pdftotext)(1), [ripgrep](/man/ripgrep)(1), [pdfinfo](/man/pdfinfo)(1)"]],"e":["grep","pdftotext","ripgrep","pdfinfo"],"x":"pdfgrep \"[pattern]\" [file.pdf]"},"personality":{"n":"personality","t":"Set the process execution domain via system call","s":[["TAGLINE","Set the process execution domain via system call"],["TLDR","**Disable ASLR for debugging (via setarch)**\n\n```setarch $(uname -m) -R [command]```\n\n**Run 32-bit binary on 64-bit system**\n\n```linux32 [command]```\n\n**Run 64-bit binary**\n\n```linux64 [command]```\n\n**Set legacy memory layout**\n\n```setarch $(uname -m) -L [command]```"],["SYNOPSIS","```c\n#include <sys/personality.h>\n\nint personality(unsigned long persona);\n```"],["PARAMETERS","**PER_LINUX**\n> Standard Linux execution domain.\n\n**PER_LINUX32**\n> 32-bit execution mode on 64-bit kernels.\n\n**ADDR_NO_RANDOMIZE**\n> Disable Address Space Layout Randomization (ASLR).\n\n**MMAP_PAGE_ZERO**\n> Map the zero page (for SVr4 binaries).\n\n**ADDR_COMPAT_LAYOUT**\n> Use legacy virtual memory layout.\n\n**READ_IMPLIES_EXEC**\n> Readable mappings become executable (PROT_READ implies PROT_EXEC).\n\n**ADDR_LIMIT_32BIT**\n> Limit address space to 32 bits.\n\n**STICKY_TIMEOUTS**\n> Use SVr4-style timeout handling in select(2)."],["DESCRIPTION","**personality**(2) is a Linux system call that sets the process execution domain, telling the kernel to emulate behavior of other UNIX flavors or to toggle compatibility flags. It is primarily invoked indirectly through wrapper commands such as **setarch**, **linux32**, and **linux64**.\n\nCommon use cases include disabling ASLR to produce reproducible memory addresses during debugging, running legacy 32-bit binaries under a 64-bit kernel, and emulating older UNIX memory layouts for compatibility."],["CAVEATS","Linux-specific. There is no standalone **personality** CLI binary; use **setarch**(8) or its aliases. Disabling security features such as ASLR reduces security and should be limited to debugging contexts."],["HISTORY","The personality(2) system call originated in early Linux 1.2/2.x to support binary compatibility with SCO, SVr4, Solaris, and other UNIX systems whose binaries could run under Linux via iBCS2."],["SEE ALSO","[setarch](/man/setarch)(8), [linux32](/man/linux32)(8), [linux64](/man/linux64)(8)"]],"e":["setarch","linux32","linux64"],"x":"setarch $(uname -m) -R [command]"},"pgmslice":{"n":"pgmslice","t":"extracts a row or column from a PGM image and outputs the gray values","s":[["TAGLINE","extracts a row or column from a PGM image and outputs the gray values"],["TLDR","**Extract row or column slice from image**\n\n```pgmslice -row [100] [input.pgm]```\n\n**Extract column slice**\n\n```pgmslice -col [200] [input.pgm]```"],["SYNOPSIS","**pgmslice** [_options_] [_file_]"],["PARAMETERS","**-row** _n_\n> Extract horizontal slice at row n.\n\n**-col** _n_\n> Extract vertical slice at column n."],["DESCRIPTION","**pgmslice** extracts a row or column from a PGM image and outputs the gray values. Useful for analyzing image cross-sections. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pamcut](/man/pamcut)(1), [pgmhist](/man/pgmhist)(1)"]],"e":["pamcut","pgmhist"],"x":"pgmslice -row [100] [input.pgm]"},"pkgutil":{"n":"pkgutil","t":"Query and manage macOS package receipts","s":[["TAGLINE","Query and manage macOS package receipts"],["TLDR","**List installed packages**\n\n```pkgutil --pkgs```\n\n**Show package info**\n\n```pkgutil --pkg-info [com.apple.pkg.Safari]```\n\n**List package files**\n\n```pkgutil --files [com.apple.pkg.Safari]```\n\n**Find which package owns a file**\n\n```pkgutil --file-info [/usr/bin/python3]```\n\n**Forget package receipt** (does not uninstall files)\n\n```sudo pkgutil --forget [com.example.package]```\n\n**Expand a package for inspection**\n\n```pkgutil --expand [package.pkg] [expanded_dir]```"],["SYNOPSIS","**pkgutil** [_options_] [_packages_]"],["PARAMETERS","**--pkgs**\n> List all packages.\n\n**--pkg-info** _id_\n> Show package information.\n\n**--files** _id_\n> List installed files.\n\n**--verify** _id_\n> Verify package installation.\n\n**--forget** _id_\n> Remove package receipt.\n\n**--file-info** _path_\n> Show which package installed a specific file.\n\n**--only-files**\n> List only files (not directories) in --files output.\n\n**--only-dirs**\n> List only directories (not files) in --files output.\n\n**--regexp**\n> Match package-id arguments as regular expressions.\n\n**--expand** _pkg_ _dir_\n> Expand flat package into a directory for inspection.\n\n**--flatten** _pkg_ _dir_\n> Flatten an expanded package into a flat package.\n\n**--bom** _path_\n> Extract BOM files from a flat package.\n\n**--volume** _path_\n> Target volume (default: /).\n\n**-f**, **--force**\n> Skip confirmation for destructive operations.\n\n**-v**, **--verbose**\n> Human-readable output with extra context."],["DESCRIPTION","**pkgutil** is macOS's package receipt management utility. It queries the installer database to list installed packages, show package metadata, verify installation integrity, and list files belonging to a package.\n\nThe **--forget** command removes a package receipt from the database without deleting any installed files, useful for cleaning up stale entries. The **--expand** command extracts a .pkg archive for inspection. Package identifiers use reverse-DNS notation (e.g., com.apple.pkg.Safari).\n\nNote that pkgutil manages receipts only - it cannot install or uninstall packages. Use **installer** for installation and manual removal for uninstallation."],["EXAMPLES","```bash"],["List all Apple packages","pkgutil --pkgs | grep com.apple"],["Get package details","pkgutil --pkg-info com.apple.pkg.Safari"],["List package files","pkgutil --files com.apple.pkg.Safari | head"],["Find package owning file","pkgutil --file-info /usr/bin/python3"],["Expand pkg for inspection","pkgutil --expand package.pkg expanded_dir\n```"],["CAVEATS","macOS-specific. --forget doesn't uninstall, only removes receipt. Some packages are protected."],["HISTORY","pkgutil is Apple's command-line interface for the macOS Installer package system, managing .pkg installations."],["SEE ALSO","[brew](/man/brew)(1)"]],"e":["brew"],"x":"pkgutil --pkgs"},"powerprofilesctl":{"n":"powerprofilesctl","t":"Switch system power profiles","s":[["TAGLINE","Switch system power profiles"],["TLDR","**List** available power profiles\n\n```powerprofilesctl list```\n\n**Set** a power profile\n\n```powerprofilesctl set [performance|balanced|power-saver]```"],["SYNOPSIS","**powerprofilesctl** [**list**|**set** _profile_|**get**]"],["COMMANDS","**list**\n> List all available power profiles\n\n**get**\n> Get current active profile\n\n**set _profile_**\n> Set the active power profile"],["PARAMETERS","**profile**\n> Profile name: performance, balanced, or power-saver"],["DESCRIPTION","**powerprofilesctl** manages system power profiles via the power-profiles-daemon. It allows switching between performance and power-saving modes to balance energy consumption and system responsiveness.\n\nProfiles affect CPU governor, GPU power management, and other hardware power settings. The daemon exposes profiles over D-Bus for desktop integration."],["CAVEATS","Requires power-profiles-daemon running. Available profiles depend on hardware support. May conflict with other power management tools (TLP, etc.). GNOME/KDE integrate this via GUI settings."],["HISTORY","**power-profiles-daemon** was developed by **Bastien Nocera** for freedesktop.org. It provides a simple, standardized interface for power management that desktop environments can use."],["INSTALL","```apt: sudo apt install power-profiles-daemon```\n\n```dnf: sudo dnf install power-profiles-daemon```\n\n```pacman: sudo pacman -S power-profiles-daemon```\n\n```apk: sudo apk add power-profiles-daemon```\n\n```zypper: sudo zypper install power-profiles-daemon```\n\n```nix: nix profile install nixpkgs#power-profiles-daemon```"],["SEE ALSO","[tlp](/man/tlp)(1), [cpupower](/man/cpupower)(1), [tuned-adm](/man/tuned-adm)(8)"]],"e":["tlp","cpupower","tuned-adm"],"x":"powerprofilesctl list"},"ppmtotga":{"n":"ppmtotga","t":"Convert PPM images to TGA format","s":[["TAGLINE","Convert PPM images to TGA format"],["TLDR","**Convert PPM to TGA**\n\n```ppmtotga [input.ppm] > [output.tga]```\n\n**Convert with compression**\n\n```ppmtotga -rle [input.ppm] > [output.tga]```"],["SYNOPSIS","**ppmtotga** [_options_] [_file_]"],["PARAMETERS","**-rle**\n> Run-length encoding compression.\n\n**-norle**\n> No compression."],["DESCRIPTION","**ppmtotga** converts PPM images to TGA (Targa) format. TGA is commonly used in game development and 3D graphics. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[tgatoppm](/man/tgatoppm)(1), [ppmtobmp](/man/ppmtobmp)(1)"]],"e":["tgatoppm","ppmtobmp"],"x":"ppmtotga [input.ppm] > [output.tga]"},"process-compose":{"n":"process-compose","t":"Scheduler and orchestrator for non-containerized applications","s":[["TAGLINE","Scheduler and orchestrator for non-containerized applications"],["TLDR","**Start all processes defined in process-compose.yaml**\n\n```process-compose```\n\n**Start with a specific config file**\n\n```process-compose -f [path/to/config.yaml]```\n\n**Run in headless mode with API**\n\n```process-compose --tui=false```"],["SYNOPSIS","**process-compose** [_options_] [_command_]"],["DESCRIPTION","**process-compose** is a simple and flexible scheduler and orchestrator for managing non-containerized applications. Its configuration syntax follows the docker-compose style using YAML. It offers a TUI and CLI mode, process recovery policies, dependency management, per-process environment variables, log caching, scheduled processes (cron and interval-based), namespace support, process replicas, REST API with token authentication, and themes."],["HISTORY","**process-compose** was created by **F1bonacc1** and is written in **Go**."],["INSTALL","```nix: nix profile install nixpkgs#process-compose```"],["SEE ALSO","[docker-compose](/man/docker-compose)(1), [supervisord](/man/supervisord)(1), [mprocs](/man/mprocs)(1)"]],"e":["docker-compose","supervisord","mprocs"],"x":"process-compose"},"ps2ps":{"n":"ps2ps","t":"Optimize and normalize PostScript files","s":[["TAGLINE","Optimize and normalize PostScript files"],["TLDR","**Optimize PostScript file**\n\n```ps2ps [input.ps] [output.ps]```\n\n**Convert with specific options**\n\n```ps2ps -dSAFER [input.ps] [output.ps]```"],["SYNOPSIS","**ps2ps** [_options_] _input_ _output_"],["PARAMETERS","_INPUT_\n> Input PostScript file.\n\n_OUTPUT_\n> Output PostScript file.\n\n**-dSAFER**\n> Safer mode."],["DESCRIPTION","**ps2ps** optimizes and normalizes PostScript files by reading them through the Ghostscript interpreter and rewriting the output as clean, canonical PostScript. This process can reduce file size, resolve compatibility issues, and simplify complex PostScript code for use with printers or other tools that struggle with the original.\n\nThe tool is part of the Ghostscript suite and is essentially a wrapper around **gs** with PostScript output enabled. If no output filename is given, the result is written to stdout."],["CAVEATS","Part of Ghostscript suite. May alter formatting."],["HISTORY","ps2ps is part of **Ghostscript** for PostScript optimization."],["INSTALL","```apt: sudo apt install ghostscript```\n\n```dnf: sudo dnf install ghostscript```\n\n```pacman: sudo pacman -S ghostscript```\n\n```apk: sudo apk add ghostscript```\n\n```zypper: sudo zypper install ghostscript```\n\n```brew: brew install ghostscript```\n\n```nix: nix profile install nixpkgs#ghostscript```"],["SEE ALSO","[gs](/man/gs)(1), [ps2pdf](/man/ps2pdf)(1), [ps2epsi](/man/ps2epsi)(1)"]],"e":["gs","ps2pdf","ps2epsi"],"x":"ps2ps [input.ps] [output.ps]"},"pueue-group":{"n":"pueue-group","t":"Manage pueue task execution groups","s":[["TAGLINE","Manage pueue task execution groups"],["TLDR","**List groups**\n\n```pueue group```\n\n**Create group**\n\n```pueue group add [name]```\n\n**Remove group**\n\n```pueue group remove [name]```\n\n**Set parallel tasks**\n\n```pueue parallel [N] --group [name]```"],["SYNOPSIS","**pueue group** [_subcommand_] [_options_]"],["PARAMETERS","**add** _NAME_\n> Create group.\n\n**remove** _NAME_\n> Delete group."],["DESCRIPTION","**pueue group** manages task execution groups, which allow organizing tasks into separate queues with independent parallelism settings. Groups can be created with **add** and removed with **remove**. Each group runs its tasks independently, so a CPU-bound group can be limited to one task while an I/O-bound group runs many in parallel."],["CAVEATS","Default group exists. Part of pueue."],["HISTORY","**pueue group** is part of the **pueue** task manager, providing task grouping for independent queue management."],["INSTALL","```pacman: sudo pacman -S pueue```\n\n```apk: sudo apk add pueue```\n\n```zypper: sudo zypper install pueue```\n\n```brew: brew install pueue```\n\n```nix: nix profile install nixpkgs#pueue```"],["SEE ALSO","[pueue](/man/pueue)(1), [pueue-parallel](/man/pueue-parallel)(1)"]],"e":["pueue","pueue-parallel"],"x":"pueue group"},"pydocstyle":{"n":"pydocstyle","t":"Check Python docstring conventions","s":[["TAGLINE","Check Python docstring conventions"],["TLDR","**Check docstrings**\n\n```pydocstyle [module.py]```\n\n**Check directory**\n\n```pydocstyle [src/]```\n\n**Select conventions**\n\n```pydocstyle --convention [google] [module.py]```\n\n**Ignore specific errors**\n\n```pydocstyle --ignore [D100,D101] [module.py]```\n\n**Select specific errors**\n\n```pydocstyle --select [D200,D201] [module.py]```\n\n**Show source**\n\n```pydocstyle --source [module.py]```\n\n**Count errors only**\n\n```pydocstyle --count [module.py]```"],["SYNOPSIS","**pydocstyle** [_--convention name_] [_--ignore codes_] [_--select codes_] [_options_] _files_"],["PARAMETERS","**--convention** _NAME_\n> Convention (pep257, numpy, google).\n\n**--ignore** _CODES_\n> Errors to ignore.\n\n**--select** _CODES_\n> Errors to check.\n\n**--source**\n> Show source code.\n\n**--explain**\n> Explain errors.\n\n**--count**\n> Count only.\n\n**--match** _PATTERN_\n> Only check files whose names match the regular expression _PATTERN_ (default: `(?!test_).*\\.py`).\n\n**--match-dir** _PATTERN_\n> Only recurse into directories whose names match the regular expression _PATTERN_ (default: `[^\\.].*`).\n\n**--add-ignore** _CODES_\n> Add codes to the existing ignore list (does not replace).\n\n**--add-select** _CODES_\n> Add codes to the existing select list (does not replace).\n\n**--config** _FILE_\n> Use the specified configuration file instead of auto-discovery.\n\n**-d**, **--debug**\n> Print debug information.\n\n**-v**, **--verbose**\n> Print status information during run."],["CONVENTIONS","**pep257** - PEP 257 conventions\n**numpy** - NumPy docstring style\n**google** - Google Python style"],["DESCRIPTION","**pydocstyle** checks Python docstring conventions. It enforces consistent documentation style.\n\nConventions define expected format. PEP 257, NumPy, and Google styles are supported.\n\nError codes identify specific issues. D100 series for modules, D200 for formatting, etc.\n\nSource display shows problematic code. Helps identify and fix issues quickly.\n\nConfiguration files set project defaults. pyproject.toml and setup.cfg supported."],["CONFIGURATION","**pyproject.toml**\n> Project configuration with a `[tool.pydocstyle]` section for convention, match patterns, and ignore/select rules.\n\n**setup.cfg**\n> Alternative project configuration with a `[pydocstyle]` section supporting the same options.\n\n**.pydocstyle**\n> Standalone configuration file for pydocstyle settings."],["CAVEATS","Style preferences vary. May need tuning for project needs. Not all edge cases covered.\n\n**Deprecated:** as of late 2023 pydocstyle is no longer actively maintained; its checks were reimplemented in **Ruff** under the `D` prefix, which is the recommended replacement."],["HISTORY","**pydocstyle** started as **pep257** before expanding to support multiple conventions (PEP 257, NumPy, Google). Development slowed with the rise of **Ruff**, which provides a faster, drop-in implementation of pydocstyle's rules. The PyCQA project announced deprecation in November 2023, recommending migration to Ruff."],["SEE ALSO","[ruff](/man/ruff)(1), [pylint](/man/pylint)(1), [flake8](/man/flake8)(1), [mypy](/man/mypy)(1), [black](/man/black)(1)"]],"e":["ruff","pylint","flake8","mypy","black"],"x":"pydocstyle [module.py]"},"rubish":{"n":"rubish","t":"UNIX shell written in pure Ruby","s":[["TAGLINE","UNIX shell written in pure Ruby"],["TLDR","**Start** an interactive Rubish session\n\n```rubish```\n\n**Run** a single command and exit\n\n```rubish -c '[echo hello]'```\n\n**Execute** a shell script file\n\n```rubish [script.sh]```\n\n**Start** in restricted mode, disabling Ruby integration for untrusted scripts\n\n```rubish -r```\n\n**Pipe** command output through Ruby methods\n\n```rubish -c '[ls().sort.uniq]'```"],["SYNOPSIS","**rubish** [_-c command_] [_-r_] [_script_] [_arguments..._]"],["DESCRIPTION","**rubish** is an interactive UNIX shell implemented entirely in Ruby. It parses familiar bash syntax and compiles it to Ruby for execution, so existing scripts keep working while the full Ruby language becomes available on the command line. It can be used as a login shell via **chsh**.\n\nThe shell blurs the line between shell and scripting language. External commands return objects that can be chained with dot notation (for example **ls().sort.uniq**), output can be processed line by line with Ruby iterators such as **.each** and **.map**, and Ruby expressions may be used as conditions in **if**, **while**, and **until** by wrapping them in **{ }**. A line beginning with a capital letter is evaluated directly as Ruby code, and functions can be defined with Ruby's **def...end** syntax.\n\nFor familiarity it also supports a number of zsh conveniences, including **setopt**, **autoload**, and abbreviated path expansion, along with custom dynamically-generated prompts and lazy loading of slow initializations in background threads."],["PARAMETERS","**-c** _command_\n> Execute _command_ as a single string and exit.\n\n**-r**\n> Restricted mode: disable Ruby integration features for running untrusted scripts."],["CONFIGURATION","Rubish is configured in Ruby rather than a bespoke config language. Prompts are defined as Ruby functions for fully dynamic prompt strings, options can be toggled with zsh-style **setopt**, and expensive startup work can be deferred with lazy loading so the shell stays responsive."],["CAVEATS","Rubish requires a compatible Ruby interpreter to be available. As a young project it is best treated as experimental for daily use, and the deep Ruby integration is a security concern when running untrusted input, which is why **-r** restricted mode exists."],["HISTORY","**rubish** was created by **Atsushi Matsuda** (amatsuda), a prominent Ruby on Rails core contributor, as a shell written in pure Ruby. It is released as open-source software under the **MIT** license."],["SEE ALSO","[bash](/man/bash)(1), [zsh](/man/zsh)(1), [fish](/man/fish)(1), [ruby](/man/ruby)(1), [irb](/man/irb)(1)"]],"e":["bash","zsh","fish","ruby","irb"],"x":"rubish"},"see":{"n":"see","t":"View files using mailcap MIME handlers","s":[["TAGLINE","View files using mailcap MIME handlers"],["TLDR","**View** a file using its MIME type handler\n\n```see [filename]```\n\n**View** a file with an explicit MIME type\n\n```see [mime/type]:[filename]```\n\n**Display** the command that would be used without executing it\n\n```see --norun [filename]```"],["SYNOPSIS","**see** [**--norun**] [**--nopager**] [**--debug**] [_mime-type_**:**[_encoding_**:**]_filename_]"],["PARAMETERS","**--debug**\n> Turn on extra information to find out what is happening\n\n**--nopager**\n> Ignore any \"copiousoutput\" directive and send output to STDOUT\n\n**--norun**\n> Display the found command without actually executing it"],["DESCRIPTION","**see** is an alias for **run-mailcap --action=view**. It opens files using the appropriate viewer based on MIME type as configured in the mailcap database (~/.mailcap and /etc/mailcap).\n\nEach file can be specified as its mime-type, its encoding (e.g. compression), and filename together, separated by colons. If the mime-type is not specified, it is determined from the filename extension using the mime.types database."],["CAVEATS","Behavior depends on mailcap configuration. Some MIME types may not have configured viewers. Part of the **mailcap** (or **mime-support**) package."],["INSTALL","```nix: nix profile install nixpkgs#see```"],["SEE ALSO","[run-mailcap](/man/run-mailcap)(1), [edit](/man/edit)(1), [compose](/man/compose)(1), [print](/man/print)(1), [xdg-open](/man/xdg-open)(1)"]],"e":["run-mailcap","edit","compose","print","xdg-open"],"x":"see [filename]"},"soelim":{"n":"soelim","t":"Resolve file inclusions in roff documents","s":[["TAGLINE","Resolve file inclusions in roff documents"],["TLDR","**Process a file** and replace .so requests with included file contents\n\n```soelim [path/to/file]```\n\n**Process with additional include directories**\n\n```soelim -I [path/to/includes] [path/to/file]```\n\n**Process without emitting .lf requests**\n\n```soelim -r [path/to/file]```\n\n**Output for TeX processing** with TeX-style comments\n\n```soelim -t [path/to/file]```\n\n**Process multiple files**\n\n```soelim [file1] [file2] [file3]```"],["SYNOPSIS","**soelim** [**-Crtv**] [**-I** _dir_] [_file_...]"],["PARAMETERS","**-C**\n> Recognize .so requests even if characters other than space or newline follow.\n\n**-I** _dir_\n> Add directory to the search path for included files. Can be specified multiple times.\n\n**-r**\n> Do not emit **.lf** line number requests. Useful for non-groff files.\n\n**-t**\n> Emit TeX-style comment lines (**%**) instead of **.lf** requests for file and line tracking.\n\n**-v**, **--version**\n> Print version number and exit.\n\n**--help**\n> Display usage message and exit."],["DESCRIPTION","**soelim** is a preprocessor for the groff document formatting system. It processes roff input files and replaces **.so** (source) requests with the actual contents of the referenced files, recursively flattening a tree of included documents.\n\nThe **.so** request in roff documents is used to include other files, similar to **#include** in C. However, these inclusions are normally processed by troff (the formatter) rather than preprocessors. soelim allows included files to be preprocessed before formatting, which is necessary when included files contain preprocessor directives.\n\nsoelim is typically invoked automatically by groff when the **-s** option is used. It reads from standard input if no files are specified."],["CAVEATS","The **.so** request must have no whitespace between the dot and \"so\" for soelim to recognize it. If whitespace is present, only groff/troff will process the request, and soelim will ignore it. The included file path is relative to the current directory unless **-I** paths are specified.\n\nIf both **-r** and **-t** are given, the last one specified controls the behavior."],["HISTORY","**soelim** is part of the GNU **groff** (GNU troff) package, which is the GNU implementation of the traditional Unix document formatting system troff. The groff system has been developed since the late 1980s as a free software replacement for the proprietary Unix troff. soelim provides compatibility with traditional troff preprocessing workflows."],["INSTALL","```apt: sudo apt install groff-base```\n\n```dnf: sudo dnf install groff-base```\n\n```pacman: sudo pacman -S groff```\n\n```apk: sudo apk add groff```\n\n```zypper: sudo zypper install groff```\n\n```brew: brew install groff```\n\n```nix: nix profile install nixpkgs#groff```"],["SEE ALSO","[groff](/man/groff)(1), [troff](/man/troff)(1), [nroff](/man/nroff)(1), [eqn](/man/eqn)(1), [tbl](/man/tbl)(1), [refer](/man/refer)(1), [man](/man/man)(1)"]],"e":["groff","troff","nroff","eqn","tbl","refer","man"],"x":"soelim [path/to/file]"},"stlink":{"n":"stlink","t":"Open-source STM32 programming toolset","s":[["TAGLINE","Open-source STM32 programming toolset"],["TLDR","**Flash firmware to STM32**\n\n```st-flash write [firmware.bin] 0x08000000```\n\n**Read flash memory to file**\n\n```st-flash read [output.bin] 0x08000000 [size]```\n\n**Erase flash memory**\n\n```st-flash erase```\n\n**Display programmer and MCU information**\n\n```st-info --probe```\n\n**Flash with reset before and after programming**\n\n```st-flash --reset write [firmware.bin] 0x08000000```\n\n**Reset the target MCU**\n\n```st-flash reset```\n\n**Flash Intel HEX file**\n\n```st-flash --format ihex write [firmware.hex]```"],["SYNOPSIS","**st-flash** [_options_] {read|write|erase|reset} [_file_] [_address_] [_size_]\n\n**st-info** [_options_]"],["PARAMETERS","**--reset**\n> Trigger a reset both before and after flashing.\n\n**--connect-under-reset**\n> Connect to target while held in reset.\n\n**--hot-plug**\n> Connect to target without reset.\n\n**--format** _format_\n> File format: binary or ihex (Intel HEX).\n\n**--flash** _size_\n> Override detected flash size (in bytes with optional k/M suffix).\n\n**--serial** _serial_\n> Use specific ST-LINK by serial number.\n\n**--freq** _khz_\n> Set SWD frequency in kHz.\n\n**--opt**\n> Enable ignore ending empty bytes optimization.\n\n**--debug**\n> Enable debug output.\n\n**--version**\n> Display version information.\n\n**--probe** (st-info)\n> Display information about connected programmer and target.\n\n**--chipid** (st-info)\n> Display chip ID of connected target.\n\n**--descr** (st-info)\n> Display description of connected target.\n\n**--flash** (st-info)\n> Display amount of flash memory available.\n\n**--sram** (st-info)\n> Display amount of SRAM available.\n\n**--pagesize** (st-info)\n> Display flash page size.\n\n**--serial** (st-info)\n> Display serial code of the programmer."],["DESCRIPTION","**stlink** is an open source toolset for programming and debugging STM32 microcontrollers using ST-LINK programmers. It provides command-line utilities that work with ST-LINK/V1, V2, V2-1, V3, and compatible clone programmers.\n\nThe toolset includes **st-flash** for reading and writing flash memory, and **st-info** for querying programmer and target information. It communicates with the STM32 target via SWD (Serial Wire Debug) or JTAG protocols.\n\nThe tools support binary and Intel HEX file formats. Flash operations can target the main flash memory, option bytes, or other memory regions. The **--connect-under-reset** option helps with targets that have problematic firmware or locked debug access."],["CAVEATS","Requires appropriate udev rules on Linux for non-root access. Some ST-LINK clones may have compatibility issues. The V1 programmer has limited functionality compared to V2/V3. Option byte manipulation can brick devices if done incorrectly. For complex programming needs, consider STM32CubeProgrammer CLI."],["HISTORY","The **stlink** project was created as an open source alternative to STMicroelectronics' proprietary ST-LINK Utility. It originated from the texane/stlink repository and is now maintained by the stlink-org community on GitHub. The project enables STM32 development on Linux and macOS where official tools had limited support."],["INSTALL","```dnf: sudo dnf install stlink```\n\n```pacman: sudo pacman -S stlink```\n\n```zypper: sudo zypper install stlink```\n\n```brew: brew install stlink```\n\n```nix: nix profile install nixpkgs#stlink```"],["SEE ALSO","[st-flash](/man/st-flash)(1), [st-info](/man/st-info)(1), [openocd](/man/openocd)(1), [avrdude](/man/avrdude)(1)"]],"e":["st-flash","st-info","openocd","avrdude"],"x":"st-flash write [firmware.bin] 0x08000000"},"swig":{"n":"swig","t":"Generate language bindings for C/C++ code","s":[["TAGLINE","Generate language bindings for C/C++ code"],["TLDR","**Generate Python wrapper**\n\n```swig -python [interface.i]```\n\n**Generate with C++**\n\n```swig -c++ -python [interface.i]```\n\n**Specify output file**\n\n```swig -python -o [wrap.c] [interface.i]```\n\n**Include directory**\n\n```swig -I[/usr/include] -python [interface.i]```\n\n**Generate Java wrapper**\n\n```swig -java [interface.i]```"],["SYNOPSIS","**swig** [_-python|-java|-ruby_] [_-c++_] [_-o file_] [_options_] _interface.i_"],["PARAMETERS","**-python**\n> Python target.\n\n**-java**\n> Java target.\n\n**-ruby**\n> Ruby target.\n\n**-c++**\n> C++ mode.\n\n**-o** _FILE_\n> Output file.\n\n**-I** _DIR_\n> Include path.\n\n**-module** _NAME_\n> Module name."],["DESCRIPTION","**swig** (Simplified Wrapper and Interface Generator) generates wrapper code that allows C and C++ libraries to be called from other programming languages. It reads interface definition files (.i) that describe which functions, classes, and types to expose, and produces the glue code needed for the target language.\n\nSupported target languages include Python, Java, Ruby, Perl, PHP, Lua, Go, JavaScript, C#, and many others. SWIG handles automatic type conversion and data marshaling between C/C++ and the target language, including complex types, pointers, arrays, and object-oriented constructs like inheritance and templates.\n\nInterface files use a syntax similar to C/C++ header files with additional SWIG directives for controlling the wrapping process. SWIG can also parse actual C/C++ header files directly for simpler cases."],["CAVEATS","Learning curve for interface files. C/C++ knowledge needed. Build system integration."],["HISTORY","**SWIG** (Simplified Wrapper and Interface Generator) was created by **David Beazley** for connecting C/C++ with scripting languages."],["INSTALL","```apt: sudo apt install swig```\n\n```dnf: sudo dnf install swig```\n\n```pacman: sudo pacman -S swig```\n\n```apk: sudo apk add swig```\n\n```zypper: sudo zypper install swig```\n\n```brew: brew install swig```\n\n```nix: nix profile install nixpkgs#swig```"],["SEE ALSO","[cython](/man/cython)(1), [pybind11](/man/pybind11)(1), [ctypes](/man/ctypes)(1)"]],"e":["cython","pybind11","ctypes"],"x":"swig -python [interface.i]"},"systemctl-list-machines":{"n":"systemctl-list-machines","t":"List registered machines and containers","s":[["TAGLINE","List registered machines and containers"],["TLDR","List **all** machines\n\n```systemctl list-machines```\n\nList **specific** machine\n\n```systemctl list-machines [machine]```\n\nList **multiple** machines\n\n```systemctl list-machines [machine1 machine2 ...]```\n\nFilter by **pattern**\n\n```systemctl list-machines [pattern*]```"],["SYNOPSIS","**systemctl list-machines** [_PATTERN_...]"],["DESCRIPTION","**systemctl list-machines** displays the host and all running local containers and virtual machines registered with systemd-machined. It shows the machine name, state, and number of failed units.\n\nThis provides an overview of all systemd-managed machines, including the local host and systemd-nspawn containers."],["CAVEATS","Only shows machines registered with systemd-machined. External containers (Docker, Podman without systemd integration) are not listed. The local host is always shown as the first entry."],["HISTORY","The **list-machines** subcommand integrates container and VM management into the standard systemctl interface, providing unified monitoring across the host and its managed machines."],["SEE ALSO","[systemctl](/man/systemctl)(1), [machinectl](/man/machinectl)(1), [systemd-nspawn](/man/systemd-nspawn)(1)"]],"e":["systemctl","machinectl","systemd-nspawn"],"x":"systemctl list-machines"},"terminalizer":{"n":"terminalizer","t":"Record terminal sessions as GIF","s":[["TAGLINE","Record terminal sessions as GIF"],["TLDR","**Start recording** to a YAML file\n\n```terminalizer record [filename]```\n\n**Record with a custom config** file\n\n```terminalizer record [filename] -c [config.yml]```\n\n**Record with a custom command** instead of the default shell\n\n```terminalizer record [filename] -d \"[bash -l]\"```\n\n**Play a recording**\n\n```terminalizer play [filename.yml]```\n\n**Render recording** to an animated GIF\n\n```terminalizer render [filename.yml] -o [output.gif]```\n\n**Generate global config** file\n\n```terminalizer init```\n\n**Share recording online**\n\n```terminalizer share [filename.yml]```\n\n**Generate a web player** page for the recording\n\n```terminalizer generate [filename.yml]```"],["SYNOPSIS","**terminalizer** _command_ [_options_] [_file_]"],["PARAMETERS","**record** _FILE_\n> Record a terminal session into a YAML file.\n\n**play** _FILE_\n> Replay a recorded session in the terminal.\n\n**render** _FILE_\n> Render a recording to an animated GIF.\n\n**generate** _FILE_\n> Generate a shareable HTML web player link.\n\n**share** _FILE_\n> Upload the recording and share it on terminalizer.com.\n\n**init**\n> Create a global config file at `~/.terminalizer/config.yml`.\n\n**config**\n> Print the default config to stdout.\n\n**-c** _FILE_, **--config** _FILE_\n> Use a custom config file.\n\n**-d** _COMMAND_, **--command** _COMMAND_\n> Command to run instead of the default shell (record).\n\n**-k**, **--skip-sharing**\n> Skip the sharing prompt after recording.\n\n**-o** _FILE_, **--output** _FILE_\n> Output file path (render).\n\n**-q** _NUM_, **--quality** _NUM_\n> GIF quality 1-100 (render).\n\n**-s** _NUM_, **--step** _NUM_\n> Frame step count; higher values reduce GIF size (render).\n\n**-r** _NUM_, **--real-timing**\n> Play using original recording speeds (play).\n\n**-h**, **--help**\n> Display help information."],["DESCRIPTION","**terminalizer** records terminal sessions and renders them as animated GIFs for sharing in documentation, README files, and tutorials. It captures both keystrokes and terminal output, storing recordings in an editable YAML format.\n\nRecordings can be played back in the terminal, rendered to GIF locally, or shared online via terminalizer.com. The YAML format allows editing timing, removing mistakes, and customizing the appearance including colors, fonts, window frame style, and dimensions before rendering the final animation."],["CAVEATS","Requires **Node.js** (installed via `npm install -g terminalizer`). GIF rendering uses Electron and can be CPU- and memory-intensive. Resulting GIFs may be large; tune `quality` and `step` in the config or via flags to balance size and smoothness."],["HISTORY","**Terminalizer** was created by **Mohammad Fares (faressoft)** and first released in **2017** as an npm package. It provides an alternative to asciinema with the benefit of producing shareable GIF animations without needing a dedicated player."],["SEE ALSO","[asciinema](/man/asciinema)(1), [script](/man/script)(1), [ttyrec](/man/ttyrec)(1)"]],"e":["asciinema","script","ttyrec"],"x":"terminalizer record [filename]"},"todoist":{"n":"todoist","t":"Command-line Todoist service client","s":[["TAGLINE","Command-line Todoist service client"],["TLDR","**Add task**\n\n```todoist add \"[task description]\"```\n\n**List tasks**\n\n```todoist list```\n\n**Complete task**\n\n```todoist close [task-id]```\n\n**Add with due date**\n\n```todoist add \"[task]\" --date \"[tomorrow]\"```\n\n**Add to project**\n\n```todoist add \"[task]\" --project \"[Project Name]\"```\n\n**Sync**\n\n```todoist sync```"],["SYNOPSIS","**todoist** _command_ [_--date date_] [_--project name_] [_options_]"],["PARAMETERS","**add**\n> Create task.\n\n**list**\n> Show tasks.\n\n**close**\n> Complete task.\n\n**sync**\n> Sync with server.\n\n**--date** _DATE_\n> Due date.\n\n**--project** _NAME_\n> Target project.\n\n**--priority** _N_\n> Priority level."],["DESCRIPTION","**todoist** is a command-line client for the Todoist task management service, providing terminal access to your cloud-synced task lists. Tasks can be created with natural language due dates like \"tomorrow\" or \"next week,\" organized into projects, and assigned priority levels.\n\nThe tool syncs with the Todoist cloud service, so changes made from the CLI are immediately available on all other devices. An API token is required for authentication and is typically configured on first use."],["CAVEATS","Todoist account required. API token needed. Internet dependent."],["HISTORY","Various **todoist** CLI clients exist for the **Todoist** task management service, providing terminal access."],["INSTALL","```brew: brew install todoist```\n\n```nix: nix profile install nixpkgs#todoist```"],["SEE ALSO","[task](/man/task)(1), [todo.sh](/man/todo.sh)(1), [todoman](/man/todoman)(1)"]],"e":["task","todo.sh","todoman"],"x":"todoist add \"[task description]\""},"trust":{"n":"trust","t":"Manage system trust policy certificate store","s":[["TAGLINE","Manage system trust policy certificate store"],["TLDR","**List** trust policy store items\n\n```trust list```\n\nList information about **specific items** in the trust policy store\n\n```trust list --filter=[blocklist|ca-anchors|certificates|trust-policy]```\n\n**Store** a trust anchor in the trust policy store\n\n```trust anchor [path/to/certificate.crt]```\n\n**Remove** an anchor from the trust policy store\n\n```trust anchor --remove [path/to/certificate.crt]```\n\n**Extract** trust policy to a directory\n\n```trust extract --format=x509-directory --filter=ca-anchors [path/to/directory]```\n\nDisplay **help** for a subcommand\n\n```trust [subcommand] --help```"],["SYNOPSIS","**trust** _command_ [_options_]"],["PARAMETERS","**list**\n> List trust policy store items\n\n**anchor** _file_\n> Add a trust anchor to the store\n\n**anchor** **--remove** _file_\n> Remove a trust anchor\n\n**extract** **--format=**_format_ _path_\n> Extract trust anchors in specified format.\n\n**extract-compat**\n> Extract trust policy in a format compatible with the system's native tools.\n\n**--filter=**_type_\n> Filter by type (ca-anchors, blocklist, certificates, trust-policy).\n\n**--format=**_format_\n> Output format (x509-file, x509-directory, pem-file, pem-bundle, java-cacerts, openssl-bundle, openssl-directory, etc.).\n\n**--purpose=**_purpose_\n> Filter by purpose (server-auth, client-auth, email, code-signing)."],["DESCRIPTION","**trust** manages the shared system trust policy store, which contains trusted CA certificates, blocklisted certificates, and trust policies. It allows administrators to add, remove, and extract trust anchors used for TLS/SSL verification across the system.\n\nChanges made with trust affect all applications that use the p11-kit trust module, providing a unified way to manage certificates rather than configuring each application individually."],["CAVEATS","May require root privileges to modify the system trust store if no user-specific store is available. Changes may require applications to be restarted to take effect. The store format and location varies by distribution."],["HISTORY","Part of **p11-kit**, developed as part of the FreeDesktop.org project to provide a standard way to manage trust anchors across Linux distributions. Replaces distribution-specific methods like update-ca-certificates."],["INSTALL","```apt: sudo apt install p11-kit```\n\n```dnf: sudo dnf install p11-kit```\n\n```pacman: sudo pacman -S p11-kit```\n\n```apk: sudo apk add p11-kit-trust```\n\n```zypper: sudo zypper install p11-kit```\n\n```brew: brew install p11-kit```\n\n```nix: nix profile install nixpkgs#p11-kit```"],["SEE ALSO","[update-ca-trust](/man/update-ca-trust)(8), [update-ca-certificates](/man/update-ca-certificates)(8), [openssl](/man/openssl)(1), [p11-kit](/man/p11-kit)(8)"]],"e":["update-ca-trust","update-ca-certificates","openssl","p11-kit"],"x":"trust list"},"tuime":{"n":"tuime","t":"Colorful and customizable TUI clock","s":[["TAGLINE","Colorful and customizable TUI clock"],["TLDR","**Display the default clock**\n\n```tuime```\n\n**Custom time format**\n\n```tuime --format \"[%H:%M:%S]\"```\n\n**Custom color and font**\n\n```tuime --color [red] --font [font-block]```"],["SYNOPSIS","**tuime** [_options_]"],["DESCRIPTION","**tuime** is a simple, colorful TUI clock for your terminal. It supports customizable time formats, multiple fonts (block, simple, 3D, etc.), gradient color support, and UTC offset configuration."],["HISTORY","**tuime** was created by **Nathan Dawit** (nthnd) and is written in **Rust**."],["INSTALL","```aur: yay -S tuime```"],["SEE ALSO","[tock](/man/tock)(1), [tty-clock](/man/tty-clock)(1)"]],"e":["tock","tty-clock"],"x":"tuime"},"uc":{"n":"uc","t":"Unicode-aware uppercase filter","s":[["TAGLINE","Unicode-aware uppercase filter"],["TLDR","**Uppercase** a string\n\n```echo \"[straße]\" | uc```\n\n**Uppercase** the contents of a file\n\n```uc < [input.txt] > [output.txt]```\n\n**Round-trip** through lowercase and back\n\n```cat [file.txt] | uc | lc```\n\n**Uppercase ligatures** correctly\n\n```printf \"[ﬃ]\\n\" | uc```"],["SYNOPSIS","**uc** [_file_ ...]"],["DESCRIPTION","**uc** reads text from standard input (or the named files), applies the full Unicode **toUpper** case mapping, and writes the result to standard output. Unlike a naive **tr 'a-z' 'A-Z'**, it handles case folds that change length (German **ß** → **SS**, the **ﬃ** ligature → **FFI**) and respects language-specific rules for Greek, Cyrillic, Armenian, and other scripts.\n\nIt ships as one of roughly thirty small filter scripts in **Tom Christiansen's Unicode::Tussle** Perl distribution, alongside companions such as **lc** (lowercase), **tc** (titlecase), **nfd**/**nfc**/**nfkd**/**nfkc** (normalization), **ucsort**, **uniwc**, and **tcgrep** — together forming a Unicode-correct replacement for many GNU coreutils."],["CAVEATS","The name **uc** is ambiguous and collides with Perl's built-in **uc()** function and a number of unrelated tools on other platforms. The script is not installed by default on most distributions; install the full set with **cpanm Unicode::Tussle**. Because the mapping is locale-independent, language-tailored rules (for example Turkish dotted/dotless **I**) are not applied."],["HISTORY","**Unicode::Tussle** grew out of a collection of one-off scripts by **Tom Christiansen** presented at **OSCON 2011** and is now packaged on **CPAN** by **brian d foy**. The distribution is the standard reference for \"Unicode coreutils\" in Perl."],["SEE ALSO","[tc](/man/tc)(1), [tr](/man/tr)(1), [iconv](/man/iconv)(1)"]],"e":["tc","tr","iconv"],"x":"echo \"[straße]\" | uc"},"vala":{"n":"vala","t":"Compile and run Vala source code directly","s":[["TAGLINE","Compile and run Vala source code directly"],["TLDR","**Run a Vala program** directly\n\n```vala [file.vala]```\n\n**Run with a package** dependency\n\n```vala --pkg [gtk4] [file.vala]```\n\n**Run with multiple** source files\n\n```vala --pkg [gio-2.0] [file1.vala] [file2.vala]```\n\n**Pass arguments** to the program\n\n```vala [file.vala] -- [arg1] [arg2]```"],["SYNOPSIS","**vala** [_--pkg name_] [_options_] _files_ [-- _args_]"],["PARAMETERS","**--pkg** _NAME_\n> Include package (resolved via pkg-config).\n\n**--vapidir** _DIR_\n> Look for VAPI files in the given directory.\n\n**--girdir** _DIR_\n> Look for GIR files in the given directory.\n\n**--version**\n> Display version number."],["DESCRIPTION","**vala** compiles and immediately runs Vala source code without producing persistent output files. It is equivalent to running **valac --run** and is useful for scripting and quick testing. For producing compiled binaries or C source files, use **valac** instead.\n\nVala provides a modern, C#-like syntax with features such as type inference, lambda expressions, and signals while compiling down to plain C code that uses the GObject type system. The **--pkg** flag links against system libraries discovered through pkg-config."],["CAVEATS","GNOME ecosystem. Generates C. Requires GLib."],["HISTORY","**Vala** was created by **Jürg Billeter** for GNOME development, compiling to C with GObject support."],["INSTALL","```apt: sudo apt install valac-bin```\n\n```dnf: sudo dnf install vala```\n\n```pacman: sudo pacman -S vala```\n\n```apk: sudo apk add vala```\n\n```zypper: sudo zypper install vala```\n\n```brew: brew install vala```\n\n```nix: nix profile install nixpkgs#vala```"],["SEE ALSO","[valac](/man/valac)(1), [gcc](/man/gcc)(1), [pkg-config](/man/pkg-config)(1), [meson](/man/meson)(1)"]],"e":["valac","gcc","pkg-config","meson"],"x":"vala [file.vala]"},"vignore":{"n":"vignore","t":"Visualize files matched by ignore rules","s":[["TAGLINE","Visualize files matched by ignore rules"],["TLDR","**Visualize ignored files in a project**\n\n```vignore -d [/path/to/project]```\n\n**Visualize ignored files in the current directory**\n\n```vignore -d .```"],["SYNOPSIS","**vignore** [**-h**] **-d** _DIFFPATH_"],["PARAMETERS","**-d** _DIFFPATH_\n> Path to the project directory to analyze.\n\n**-h**, **--help**\n> Show help message and exit."],["DESCRIPTION","**vignore** visualizes which files are matched by ignore rules such as .gitignore, .dockerignore, or .fdignore. It provides instant visual feedback to help developers understand their ignore patterns without guesswork. Requires **fd** to be installed."],["CAVEATS","Requires **fd** (fd-find) to be installed and available in PATH. Only supports ignore file formats that fd recognizes."],["HISTORY","**vignore** was created by **James4Ever0** and is written in **Python**."],["SEE ALSO","[fd](/man/fd)(1), [tree](/man/tree)(1), [git](/man/git)(1)"]],"e":["fd","tree","git"],"x":"vignore -d [/path/to/project]"},"virt-clone":{"n":"virt-clone","t":"Clone libvirt virtual machines","s":[["TAGLINE","Clone libvirt virtual machines"],["TLDR","**Clone** a VM with automatically generated name, storage, and MAC\n\n```virt-clone --original [vm_name] --auto-clone```\n\n**Clone** a VM with a specific new name and storage path\n\n```virt-clone --original [vm_name] --name [new_vm_name] --file [path/to/new_disk.qcow2]```\n\n**Clone** a VM with a random MAC address\n\n```virt-clone --original [vm_name] --name [new_vm_name] --auto-clone --mac RANDOM```\n\n**Clone and replace** an existing VM with the same name\n\n```virt-clone --original [vm_name] --name [new_vm_name] --auto-clone --replace```\n\n**Clone without copying storage** (reference existing disks)\n\n```virt-clone --original [vm_name] --name [new_vm_name] --preserve-data```\n\n**Print the generated clone XML** without performing the clone\n\n```virt-clone --original [vm_name] --auto-clone --print-xml```"],["SYNOPSIS","**virt-clone** [_options_]"],["PARAMETERS","**-o**, **--original** _name_\n> Name of the original VM to clone (must be shut off).\n\n**--original-xml** _file_\n> Libvirt guest XML file to use as the original guest.\n\n**-n**, **--name** _name_\n> Name for the new cloned VM.\n\n**--auto-clone**\n> Automatically generate new name, storage, and MAC.\n\n**-f**, **--file** _path_\n> Path for the new disk image. Repeat for multi-disk guests.\n\n**-m**, **--mac** _address_\n> MAC address for the new VM (or RANDOM).\n\n**-u**, **--uuid** _uuid_\n> UUID for the new guest (default: randomly generated).\n\n**--preserve-data**\n> Do not clone storage; preserve disk images and reference them.\n\n**--reflink**\n> Use copy-on-write reflinks if the filesystem supports it.\n\n**--nonsparse**\n> Fully allocate new storage instead of creating sparse files.\n\n**--force-copy** _target_\n> Force cloning of the specified disk target (e.g. 'hdc', 'sda').\n\n**--replace**\n> Shut down and remove any existing guest with the target name.\n\n**--print-xml**\n> Print generated clone XML and exit without cloning.\n\n**-c**, **--connect** _uri_\n> Connect to specified hypervisor URI.\n\n**-d**, **--debug**\n> Print debugging information."],["DESCRIPTION","**virt-clone** creates a copy of an existing libvirt virtual machine. It clones the VM configuration and can duplicate or create new disk images, generating unique identifiers for the clone.\n\nThe tool handles creating new UUIDs, MAC addresses, and storage paths to avoid conflicts with the original VM. It is commonly used for creating test environments or deploying multiple similar VMs. Disks marked as readonly or shareable are skipped by default unless **--force-copy** is used."],["CAVEATS","Requires libvirt and an appropriate virtualization backend (KVM, QEMU, etc.). The original VM must be shut off for a consistent clone. Cloning large disk images can take significant time and requires sufficient storage space. Use **--nonsparse** for full allocation if the filesystem does not support sparse files well."],["INSTALL","```dnf: sudo dnf install virt-install```\n\n```pacman: sudo pacman -S virt-install```\n\n```apk: sudo apk add virt-install```\n\n```zypper: sudo zypper install virt-install```"],["SEE ALSO","[virt-install](/man/virt-install)(1), [virsh](/man/virsh)(1), [virt-manager](/man/virt-manager)(1)"]],"e":["virt-install","virsh","virt-manager"],"x":"virt-clone --original [vm_name] --auto-clone"},"vscli":{"n":"vscli","t":"CLI/TUI for launching VS Code dev containers","s":[["TAGLINE","CLI/TUI for launching VS Code dev containers"],["TLDR","**Open the current directory in VS Code**\n\n```vscli open```\n\n**Open a specific project directory**\n\n```vscli open [path/to/project]```\n\n**Open with Cursor editor instead of VS Code**\n\n```vscli open --command cursor .```\n\n**Force opening as a dev container**\n\n```vscli open --behavior force-container```\n\n**Browse recently opened projects interactively**\n\n```vscli recent```\n\n**Dry run to see what would be launched without opening**\n\n```vscli open --dry-run```"],["SYNOPSIS","**vscli** [_options_] _command_ [_arguments_]"],["SUBCOMMANDS","**open** [_PATH_] [_ARGS_]\n> Open a project in VS Code. Auto-detects dev container configuration. Defaults to current directory.\n\n**recent**\n> Open an interactive list of recently used workspaces."],["PARAMETERS","**--command** _EDITOR_\n> Specify which editor command to use (e.g., code, code-insiders, cursor).\n\n**--behavior** _BEHAVIOR_\n> Change launch behavior: force-container or force-classic.\n\n**-s**, **--history-path** _PATH_\n> Override the default path to the history file.\n\n**-d**, **--dry-run**\n> Show what would be launched without actually opening the editor.\n\n**-v**, **--verbose**\n> Increase logging verbosity.\n\n**-q**, **--quiet**\n> Decrease logging verbosity.\n\n**-h**, **--help**\n> Display help information.\n\n**-V**, **--version**\n> Display version information."],["DESCRIPTION","**vscli** simplifies launching VS Code projects with a focus on dev containers. It auto-detects whether a project uses dev containers and launches accordingly. When a project contains multiple dev container configurations, vscli prompts you to select one. It supports multiple editors including VS Code, VS Code Insiders, and Cursor. Tracks recent projects for quick access via an interactive TUI list."],["HISTORY","**vscli** was created by **michidk** and is written in **Rust**. It can be installed via Cargo, Homebrew, Chocolatey, or winget."],["INSTALL","```brew: brew install vscli```"],["SEE ALSO","[code](/man/code)(1)"]],"e":["code"],"x":"vscli open"},"weston":{"n":"weston","t":"Reference Wayland compositor","s":[["TAGLINE","Reference Wayland compositor"],["TLDR","**Start Weston compositor** (requires seat access)\n\n```weston```\n\n**Start with a specific backend**\n\n```weston --backend=[drm|wayland|x11]```\n\n**Start with a specific shell**\n\n```weston --shell=[desktop|fullscreen|kiosk]```\n\n**Start in windowed mode** under X11\n\n```weston --backend=x11```\n\n**Start nested** inside another Wayland compositor\n\n```weston --backend=wayland```\n\n**Run with Xwayland** for X11 client compatibility\n\n```weston --xwayland```\n\n**Specify display socket name**\n\n```weston -S [wayland-1]```\n\n**Start with specific configuration file**\n\n```weston -c [path/to/weston.ini]```"],["SYNOPSIS","**weston** [**-B**=_backend_] [**--shell**=_shell_] [**-S**=_name_] [**-c**=_file_] [_options_]"],["PARAMETERS","**-B**, **--backend**=_backend_\n> Compositor backend: drm (native), x11, wayland, headless, rdp, vnc, pipewire.\n\n**--shell**=_shell_\n> Shell plugin: desktop (default), fullscreen, kiosk, ivi.\n\n**-S**, **--socket**=_name_\n> Wayland socket name for clients to connect.\n\n**-c**, **--config**=_file_\n> Path to configuration file. Default is $XDG_CONFIG_HOME/weston.ini or ~/.config/weston/weston.ini.\n\n**--no-config**\n> Do not read configuration file.\n\n**--modules**=_module_\n> Load additional modules (comma-separated).\n\n**--renderer**=_name_\n> Select rendering engine (gl, pixman, vulkan, noop).\n\n**--xwayland**\n> Enable X11 application support via Xwayland.\n\n**-i**, **--idle-time**=_seconds_\n> Idle timeout before screen saver.\n\n**--log**=_file_\n> Log file path.\n\n**-l**, **--logger-scopes**=_scopes_\n> Comma-separated logger scopes to enable.\n\n**--debug**\n> Enable debug protocol extension.\n\n**--width**=_pixels_\n> Initial output width (windowed backends).\n\n**--height**=_pixels_\n> Initial output height (windowed backends).\n\n**--scale**=_factor_\n> Output scale factor.\n\n**-h**, **--help**\n> Display help message.\n\n**--version**\n> Display version information."],["DESCRIPTION","**Weston** is the reference Wayland compositor, serving as both a display server and window manager. It demonstrates Wayland protocol capabilities and provides a functional desktop environment for testing and embedded systems.\n\nWeston supports multiple backends for different deployment scenarios: DRM for native Linux framebuffer access, X11 or Wayland backends for running nested inside other display servers, and headless/RDP/VNC backends for remote or testing purposes.\n\nDifferent shells provide various user interface styles: desktop shell for traditional workstation use, fullscreen shell for single-application kiosks, and IVI shell for in-vehicle infotainment systems. Configuration is done through weston.ini.\n\nWeston requires access to input devices and display hardware through a seat manager (seatd or elogind). For native mode, the user must have appropriate permissions via the seat mechanism."],["CAVEATS","Requires Linux kernel mode setting (KMS) for native operation. Running directly requires seat management setup (seatd or elogind). Some features depend on GPU driver support for EGL and DRM. weston-launch is deprecated since version 10.0.0."],["HISTORY","**Weston** was developed alongside the **Wayland** protocol at **freedesktop.org**, with initial development led by **Kristian Høgsberg** starting around **2010**. It served as the proving ground for Wayland concepts and remains the reference implementation. While production desktops typically use compositors like GNOME's Mutter or KDE's KWin, Weston continues development for testing, embedded systems, and automotive applications."],["INSTALL","```apt: sudo apt install weston```\n\n```dnf: sudo dnf install weston```\n\n```pacman: sudo pacman -S weston```\n\n```apk: sudo apk add weston```\n\n```zypper: sudo zypper install weston```\n\n```nix: nix profile install nixpkgs#weston```"],["SEE ALSO","[sway](/man/sway)(1), [Xwayland](/man/Xwayland)(1), [xrandr](/man/xrandr)(1)"]],"e":["sway","Xwayland","xrandr"],"x":"weston"},"yumdownloader":{"n":"yumdownloader","t":"Download RPM packages without installing","s":[["TAGLINE","Download RPM packages without installing"],["TLDR","This command is an alias of [dnf download](/man/dnf-download).\n\nHistorical YUM package downloader for Fedora installations, now deprecated."],["SEE ALSO","[dnf](/man/dnf)(8), [yum](/man/yum)(8)"]],"e":["dnf","yum"],"x":""}});
