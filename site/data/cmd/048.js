window.LCL_BUCKET(48,{"abtop":{"n":"abtop","t":"htop-style monitor for AI coding agent sessions","s":[["TAGLINE","htop-style monitor for AI coding agent sessions"],["TLDR","**Monitor** agent sessions\n\n```abtop```\n\n**Install** via cargo or install script\n\n```cargo install abtop```"],["SYNOPSIS","**abtop** [*options*]"],["DESCRIPTION","**abtop** watches local AI coding agent processes (e.g. Claude Code, Codex CLI): sessions, token/context usage, rate limits, and related ports—in a live TUI similar in spirit to **htop**. Useful when juggling multiple agent terminals."],["PARAMETERS","Interactive TUI after launch. Optional flags select refresh intervals and filters—**abtop --help**."],["CAVEATS","Depends on how agents expose metrics; not all tools report tokens the same way. Process introspection may need permissions on locked-down systems."],["INSTALL","```aur: yay -S abtop```\n\n```nix: nix profile install nixpkgs#abtop```"],["SEE ALSO","[htop](/man/htop)(1), [btop](/man/btop)(1), [ps](/man/ps)(1)"],["RESOURCES","```[Source code](https://github.com/graykode/abtop)```"]],"e":["htop","btop","ps"],"x":"abtop"},"argos-translate":{"n":"argos-translate","t":"Offline neural machine translation","s":[["TAGLINE","Offline neural machine translation"],["TLDR","**Translate** text between languages\n\n```argos-translate --from-lang [en] --to-lang [es] \"[Hello, world]\"```\n\n**Pipe input** for translation\n\n```echo \"[Text]\" | argos-translate --from-lang [en] --to-lang [de]```\n\n**Translate from a file**\n\n```argos-translate --from-lang [en] --to-lang [fr] < [input.txt]```\n\n**Install** a language package (via argospm)\n\n```argospm install translate-[en]_[es]```\n\n**List installed** language packages\n\n```argospm list```\n\n**Update** the package index and **search** available packages\n\n```argospm update && argospm search --from-lang [en]```"],["SYNOPSIS","**argos-translate** **--from-lang** _code_ **--to-lang** _code_ [_text_]\n\n**argospm** _subcommand_ [_options_]"],["DESCRIPTION","**argos-translate** is an open-source, offline translation tool based on OpenNMT neural machine translation models. It runs entirely on the local machine, providing privacy-preserving translation without sending data to cloud services.\n\nLanguages are provided as downloadable packages named **translate-_from_\\__to_** (e.g. `translate-en_es`). Packages are managed with the companion **argospm** command, which supports update, search, install, list, and remove subcommands."],["PARAMETERS","**-f** _code_, **--from-lang** _code_\n> ISO 639-1 source language code (e.g. `en`, `es`, `fr`).\n\n**-t** _code_, **--to-lang** _code_\n> ISO 639-1 target language code.\n\n**-h**, **--help**\n> Show help and exit."],["CAVEATS","Language packages must be installed via **argospm** before translation works. Translation quality varies by language pair and may not match commercial services. First-run translations are slower due to model loading; subsequent calls in the same process are faster. Large documents are memory- and CPU-intensive."],["HISTORY","**Argos Translate** was created by Argos Open Tech as an open-source, privacy-respecting alternative to cloud translation services, using CTranslate2 and OpenNMT models. It also powers the self-hosted LibreTranslate web API."],["INSTALL","```aur: yay -S argos-translate```"],["SEE ALSO","[trans](/man/trans)(1)"]],"e":["trans"],"x":"argos-translate --from-lang [en] --to-lang [es] \"[Hello, world]\""},"braindrop":{"n":"braindrop","t":"TUI client for Raindrop.io bookmarks","s":[["TAGLINE","TUI client for Raindrop.io bookmarks"],["TLDR","**Launch** the Raindrop.io TUI\n\n```braindrop```\n\n**Specify API token**\n\n```braindrop --api-key [YOUR_TOKEN]```\n\n**Use a custom theme**\n\n```braindrop --theme [textual-dark]```"],["SYNOPSIS","**braindrop** [_options_]"],["DESCRIPTION","**braindrop** is a terminal user interface client for **Raindrop.io**, a popular cloud bookmark manager. It allows users to browse, search, and manage their Raindrop.io bookmark collections directly from the terminal.\n\nThe TUI provides keyboard-driven navigation through collections, searching across bookmarks, and opening saved links in a browser. It communicates with the Raindrop.io API to sync and display bookmark data."],["CONFIGURATION","Requires a **Raindrop.io API token** for authentication. The token is typically configured on first launch or via a configuration file."],["CAVEATS","Requires an active **Raindrop.io** account and API access. Bookmark changes are synced to the cloud service. Internet connectivity is required for all operations."],["SEE ALSO","[buku](/man/buku)(1), [bmm](/man/bmm)(1)"]],"e":["buku","bmm"],"x":"braindrop"},"btrbk":{"n":"btrbk","t":"Backup tool for btrfs snapshots and subvolumes","s":[["TAGLINE","Backup tool for btrfs snapshots and subvolumes"],["TLDR","Print **statistics** about subvolumes and snapshots\n\n```sudo btrbk stats```\n\n**List** configured subvolumes and snapshots\n\n```sudo btrbk list```\n\nPrint what would happen in a **dry run**\n\n```sudo btrbk -v dryrun```\n\n**Run backup** routines with progress\n\n```sudo btrbk --progress -v run```\n\nOnly create **snapshots** for configured subvolumes\n\n```sudo btrbk snapshot```\n\n**Resume** incomplete backups\n\n```sudo btrbk resume```\n\nUse a specific **config** file\n\n```sudo btrbk -c [/path/to/btrbk.conf] run```"],["SYNOPSIS","**btrbk** [_options_] _command_ [_filter_]"],["DESCRIPTION","**btrbk** is a backup tool for btrfs filesystems that creates and manages snapshots and remote backups. It uses btrfs send/receive for efficient incremental backups and supports complex retention policies.\n\nConfiguration is defined in **/etc/btrbk/btrbk.conf**, specifying source subvolumes, snapshot locations, and backup targets including remote hosts via SSH."],["SUBCOMMANDS","**run**\n> Create snapshots and run backup routines\n\n**snapshot**\n> Only create snapshots (no backups)\n\n**resume**\n> Resume incomplete backups\n\n**dryrun**\n> Show what would be done without making changes\n\n**stats**\n> Display statistics\n\n**list**\n> List configured items\n\n**prune**\n> Delete outdated backups and snapshots according to retention policy\n\n**clean**\n> Delete incomplete (garbled) backups\n\n**archive** _source_ _target_\n> Consolidate backups into a single target directory"],["PARAMETERS","**-v**, **--verbose**\n> Increase verbosity\n\n**-q**, **--quiet**\n> Suppress non-error output\n\n**--progress**\n> Show progress during send/receive transfers\n\n**-n**, **--dry-run**\n> Same as dryrun command\n\n**-c** _file_\n> Use alternate configuration file\n\n**-p**, **--preserve**\n> Preserve all snapshots and backups (no cleanup)\n\n**-t**, **--table**\n> Print output in table format\n\n**-l**, **--loglevel** _level_\n> Set log verbosity level\n\n**-h**, **--help**\n> Print synopsis and list of commands"],["CONFIGURATION","**/etc/btrbk/btrbk.conf**\n> Main configuration file defining source subvolumes, snapshot locations, backup targets, and retention policies."],["CAVEATS","Only works with btrfs filesystems. Remote backups require SSH access and btrfs on the remote host. Retention policies should be tested with dryrun first."],["INSTALL","```dnf: sudo dnf install btrbk```\n\n```pacman: sudo pacman -S btrbk```\n\n```apk: sudo apk add btrbk```\n\n```nix: nix profile install nixpkgs#btrbk```"],["SEE ALSO","[btrfs](/man/btrfs)(8), [btrfs-send](/man/btrfs-send)(8), [snapper](/man/snapper)(8)"]],"e":["btrfs","btrfs-send","snapper"],"x":"sudo btrbk stats"},"bun":{"n":"bun","t":"All-in-one JavaScript runtime and toolkit","s":[["TAGLINE","All-in-one JavaScript runtime and toolkit"],["TLDR","**Run** JavaScript file\n\n```bun [script.js]```\n\n**Install** dependencies\n\n```bun install```\n\n**Add** package\n\n```bun add [react]```\n\n**Run** script from package.json\n\n```bun run [dev]```\n\n**Create** new project\n\n```bun create [react] [my-app]```\n\n**Test** files\n\n```bun test```"],["SYNOPSIS","**bun** _command_ [_options_]"],["DESCRIPTION","**bun** is an all-in-one JavaScript runtime, package manager, bundler, and test runner. It's designed as a drop-in replacement for Node.js with significantly faster startup times and package installation.\n\nThe tool aims to be a complete toolkit for JavaScript/TypeScript development with performance as a priority."],["PARAMETERS","**run** _file_\n> Execute JavaScript/TypeScript file\n\n**install**\n> Install dependencies from package.json\n\n**add** _package_\n> Add package to dependencies\n\n**remove** _package_\n> Remove package\n\n**test**\n> Run tests\n\n**build** _file_\n> Bundle for production\n\n**create** _template_\n> Create new project\n\n**upgrade**\n> Upgrade Bun itself"],["FEATURES","- Fast JavaScript runtime (using JavaScriptCore)\n- Built-in TypeScript support\n- Package manager (npm-compatible)\n- Bundler (esbuild-compatible)\n- Test runner\n- Native web APIs\n- Hot reloading\n- SQLite support"],["PERFORMANCE","- 4x faster npm install\n- 2-3x faster script execution vs Node.js\n- Near-instant startup"],["WORKFLOW","```bash"],["Create project","bun create react my-app\ncd my-app"],["Install deps","bun install"],["Run dev server","bun run dev"],["Add package","bun add tailwindcss"],["Run tests","bun test"],["Build for production","bun build ./src/index.ts --outdir ./dist\n```"],["COMPATIBILITY","- Drop-in Node.js replacement (most APIs)\n- npm package ecosystem\n- Jest-compatible test runner\n- ESM and CommonJS support"],["CONFIGURATION","**bunfig.toml**\n> Project-level and global configuration for Bun. Controls package installation behavior, runtime settings, test runner options, and bundler defaults. Searched in the project root directory, or globally at ~/.bunfig.toml."],["CAVEATS","Not 100% Node.js compatible. Some native modules don't work. Not all npm packages tested. Windows is supported natively since v1.1 (April 2024)."],["HISTORY","**Bun** was created by Jarred Sumner and first released in **2021** as a fast all-in-one JavaScript toolkit built on Zig and JavaScriptCore."],["INSTALL","```pacman: sudo pacman -S bun```\n\n```brew: brew install bun```\n\n```nix: nix profile install nixpkgs#bun```"],["SEE ALSO","[node](/man/node)(1), [npm](/man/npm)(1), [deno](/man/deno)(1)"],["RESOURCES","```[Source code](https://github.com/oven-sh/bun)```\n\n```[Homepage](https://bun.sh)```\n\n```[Documentation](https://bun.sh/docs)```"]],"e":["node","npm","deno"],"x":"bun [script.js]"},"cardano-wallet":{"n":"cardano-wallet","t":"Wallet server and CLI for managing Cardano wallets","s":[["TAGLINE","Wallet server and CLI for managing Cardano wallets"],["TLDR","**Start the wallet server**\n\n```cardano-wallet serve --port [8090] --node-socket [/path/to/node.socket] --database [/path/to/wallet-db] --mainnet```\n\n**Create a wallet** from recovery phrase\n\n```cardano-wallet wallet create from-recovery-phrase [wallet_name]```\n\n**List all wallets**\n\n```cardano-wallet wallet list```\n\n**Get wallet balance**\n\n```cardano-wallet wallet get --id [wallet_id]```\n\n**Generate a receiving address**\n\n```cardano-wallet address list --id [wallet_id]```\n\n**Create a transaction**\n\n```cardano-wallet transaction create --id [wallet_id] --payment [addr1...@amount]```\n\n**Submit a transaction**\n\n```cardano-wallet transaction submit --id [wallet_id] --transaction [tx_binary]```"],["SYNOPSIS","**cardano-wallet** _command_ [_options_]"],["DESCRIPTION","**cardano-wallet** is a HTTP server and CLI for managing Cardano wallets. It provides hierarchical deterministic (HD) wallet functionality with a RESTful API, enabling applications to create wallets, manage addresses, and submit transactions.\n\nThe wallet server connects to a cardano-node to query blockchain state and submit transactions. It maintains a local database of wallet state, tracking UTxOs and transaction history. The server supports multiple wallets and provides coin selection for transaction building.\n\nWallets are restored from 15 or 24-word recovery phrases following BIP39/CIP11 standards. The CLI provides both interactive commands and script-friendly output formats."],["PARAMETERS","**serve**\n> Start the wallet HTTP server.\n\n**wallet create**\n> Create a new wallet.\n\n**wallet list**\n> List all wallets.\n\n**wallet get**\n> Get wallet details.\n\n**wallet delete**\n> Delete a wallet.\n\n**address list**\n> List wallet addresses.\n\n**transaction create**\n> Build a transaction.\n\n**transaction submit**\n> Submit a signed transaction.\n\n**transaction list**\n> List wallet transactions.\n\n**stake-pool list**\n> List available stake pools.\n\n**--port** _port_\n> HTTP server port.\n\n**--node-socket** _path_\n> Path to node socket.\n\n**--database** _path_\n> Wallet database directory.\n\n**--mainnet**\n> Connect to mainnet.\n\n**--testnet** _magic_\n> Connect to testnet.\n\n**--id** _wallet_id_\n> Wallet identifier."],["CAVEATS","Requires a synced cardano-node. Database must be persisted for wallet recovery. Recovery phrase must be kept secure and offline. The server exposes sensitive operations, so should be properly secured. Syncing wallet state for restored wallets can take time."],["HISTORY","**cardano-wallet** was developed by **Input Output Global (IOG)** as the official wallet backend for Cardano. It was introduced during the **Shelley era** in **2020** to provide a standard wallet implementation. The tool supports the evolving Cardano protocol including staking, native tokens, and smart contract interactions."],["INSTALL","```aur: yay -S cardano-wallet```"],["SEE ALSO","[cardano-cli](/man/cardano-cli)(1), [cardano-node](/man/cardano-node)(1)"]],"e":["cardano-cli","cardano-node"],"x":"cardano-wallet serve --port [8090] --node-socket [/path/to/node.socket] --database [/path/to/wallet-db] --mainnet"},"cardiag":{"n":"cardiag","t":"Audio ML pipeline to diagnose car faults from sound recordings","s":[["TAGLINE","Audio ML pipeline to diagnose car faults from sound recordings"],["TLDR","**Check prerequisites** and environment\n\n```cardiag doctor```\n\n**Train a model** using bundled fixtures (no external data)\n\n```cardiag train --fixtures```\n\n**Diagnose a clip** and get fault verdict, zone, and ranked parts\n\n```cardiag diagnose clip.wav```\n\n**Run the web UI** for interactive analysis\n\n```cardiag serve --model models```\n\n**Isolate mechanical sound** from a noisy recording (no model)\n\n```cardiag clean clip.wav```\n\n**Triage engine vs running gear**\n\n```cardiag triage clip.wav```"],["SYNOPSIS","**cardiag** {doctor,train,diagnose,triage,clean,inspect,ingest,scrape,serve,...} [options] [args]"],["DESCRIPTION","**cardiag** is an end-to-end audio machine-learning pipeline for triaging mechanical faults from audio recordings (e.g. phone clips or scraped social media). It isolates the relevant mechanical sound span, embeds it with a frozen CLAP model, and applies small calibrated linear classifiers.\n\nThe tool reports:\n\n- Whether something sounds wrong (fault vs normal)\n- Rough location in the car (6 zones)\n- Ranked shortlist of likely parts (12+ families)\n- Honest `UNCERTAIN` when the input does not support a confident call\n\nIt is explicitly a **triage aid**, not a safety-critical or definitive diagnostic. All numbers are honest (grouped-by-video cross-validation) and the pipeline is designed to be inspectable."],["INSTALLATION","Install from the repository:\n\n```bash\ngit clone https://github.com/adam-s/car-diagnosis\ncd car-diagnosis\nuv venv && source .venv/bin/activate\nuv pip install -e \".[scrape,web,dev,viz]\"\n```\n\nRequires Python 3.11+ and (for full use) additional optional extras. Playback/visualization is optional."],["USAGE","Run `cardiag --help` or `cardiag <subcommand> --help` for details.\n\nCommon workflow for a new clip:\n\n1. `cardiag doctor`\n2. `cardiag diagnose my-recording.wav --json`\n3. `cardiag inspect my-recording.wav -o report.html` (visual + audio spans)\n\nTraining your own data:\n\n- `cardiag ingest ./my_clips --kind fault --cause wheel_bearing`\n- `cardiag scrape youtube|tiktok ...`\n- `cardiag train`\n\nThe `serve` subcommand starts a local web UI for drag-and-drop or URL-based analysis. Add `--json` to inference commands for machine-readable output."],["CAVEATS","- Performance is modest on crude phone/social audio (literature ceiling for the problem). It reaches ~0.79 AUROC fault/normal and top-3 part accuracy 45-65 % in honest evaluation.\n- Models are joblib artifacts; only load ones you trust.\n- Not a replacement for professional diagnosis."],["RESOURCES","```[Source code](https://github.com/adam-s/car-diagnosis)```\n\n```[Documentation](https://github.com/adam-s/car-diagnosis/blob/main/docs/MODEL_CARD.md)```"]],"e":[],"x":"cardiag doctor"},"claumon":{"n":"claumon","t":"Local dashboard for Claude usage and rate limits","s":[["TAGLINE","Local dashboard for Claude usage and rate limits"],["TLDR","**Start the dashboard** (opens a local web UI)\n\n```claumon```"],["SYNOPSIS","**claumon** [_options_]"],["DESCRIPTION","**claumon** is a local monitoring tool for Claude Pro and Max users. Started from the terminal as a single, zero-configuration Go binary, it launches a local web dashboard (by default at http://localhost:3131) that shows live API rate-limit gauges, forecasted usage with credible intervals, per-session token and cost breakdowns with multi-day trends, and a browser for Claude memory files with health scoring.\n\nIt reads rate-limit information from the Claude OAuth API and presents calibrated usage forecasts so heavy users can see how close they are to their limits and how their consumption is trending over time."],["CAVEATS","Intended for Claude Pro and Max accounts and depends on Claude's OAuth API, so the data it shows reflects that service and may break if the upstream API changes. The dashboard is served locally; it is not a hosted service."],["HISTORY","**claumon** is written in **Go** and distributed as a single self-contained binary, installable via Homebrew. The name is a contraction of \"Claude monitor.\""],["SEE ALSO","[claude](/man/claude)(1), [top](/man/top)(1), [htop](/man/htop)(1)"],["RESOURCES","```[Source code](https://github.com/fabioconcina/claumon)```\n\n```[Homepage](https://claumon.fabioconcina.com)```"]],"e":["claude","top","htop"],"x":"claumon"},"dep":{"n":"dep","t":"legacy dependency management for Go","s":[["TAGLINE","legacy dependency management for Go"],["TLDR","**Initialize dependency management** for project\n\n```dep init```\n\n**Add a dependency**\n\n```dep ensure -add [github.com/pkg/errors]```\n\n**Update all dependencies**\n\n```dep ensure -update```\n\n**Update specific dependency**\n\n```dep ensure -update [github.com/pkg/errors]```\n\n**Check dependency status**\n\n```dep status```\n\n**Visualize dependency tree**\n\n```dep status -dot | dot -Tpng -o deps.png```"],["SYNOPSIS","**dep** _command_ [_options_]"],["PARAMETERS","_COMMAND_\n> Operation: init, ensure, status, prune, check.\n\n**init**\n> Initialize new project with dep.\n\n**ensure**\n> Ensure dependencies are satisfied.\n\n**-add** _PACKAGE_\n> Add a dependency.\n\n**-update** [_PACKAGE_]\n> Update dependencies.\n\n**status**\n> Show status of dependencies.\n\n**prune**\n> Remove unused dependencies.\n\n**-v**\n> Verbose output.\n\n**--help**\n> Display help information."],["DESCRIPTION","**dep** was the official dependency management tool for Go before Go modules became the standard. It manages project dependencies through Gopkg.toml and Gopkg.lock files, ensuring reproducible builds.\n\nThe tool resolves transitive dependencies, handles version constraints, and vendors dependencies in the vendor directory. The ensure command is the primary interface for adding, updating, and synchronizing dependencies.\n\ndep uses semantic import versioning and supports various version constraint syntaxes including semver ranges, branches, and revisions."],["CONFIGURATION","**Gopkg.toml**\n> Project dependency constraints and metadata configuration.\n\n**Gopkg.lock**\n> Generated lock file with exact dependency versions and hashes."],["CAVEATS","Superseded by Go modules (go mod) in Go 1.11+. New projects should use Go modules. Migration path available from dep to modules. Only supports projects in GOPATH."],["HISTORY","dep was developed by the Go community and released in **2016** as the official experiment in dependency management. It served as a bridge solution until Go modules were introduced in Go 1.11 (**2018**), after which dep was placed in maintenance mode."],["INSTALL","```aur: yay -S dep```"],["SEE ALSO","[go](/man/go)(1)"]],"e":["go"],"x":"dep init"},"docker-stop":{"n":"docker-stop","t":"Stop one or more running Docker containers (alias)","s":[["TAGLINE","Stop one or more running Docker containers (alias)"],["TLDR","**Stop a container** (alias of `docker container stop`)\n\n```docker stop mycontainer```\n\n**Stop multiple containers**\n\n```docker stop container1 container2```\n\n**Stop with a custom timeout** (seconds before SIGKILL)\n\n```docker stop --timeout 30 mycontainer```\n\n**Send a specific stop signal**\n\n```docker stop --signal SIGINT mycontainer```"],["SYNOPSIS","**docker** **stop** [_options_] _container_ [_container_...]"],["DESCRIPTION","**docker stop** is an alias for `docker container stop`. It stops one or more running containers by sending a termination signal (`SIGTERM` by default, or the container's `STOPSIGNAL` / `--signal`) and waiting for a graceful shutdown. If the process does not exit within the timeout (default 10 seconds on Linux unless configured otherwise), `SIGKILL` is sent.\n\nFor full option details, prefer the canonical form documented under `docker container stop`."],["PARAMETERS","**-t**, **--timeout** _seconds_\n> Seconds to wait before killing the container after the stop signal. Set to `-1` to wait indefinitely.\n\n**-s**, **--signal** _signal_\n> Signal to send to the container (e.g. `SIGTERM`, `SIGINT`)."],["INSTALL","```apt: sudo apt install docker-cli```\n\n```dnf: sudo dnf install docker-cli```\n\n```pacman: sudo pacman -S docker```\n\n```apk: sudo apk add docker-cli```\n\n```zypper: sudo zypper install docker```\n\n```brew: brew install docker```\n\n```nix: nix profile install nixpkgs#docker```"],["SEE ALSO","[docker-container-stop](/man/docker-container-stop)(1), [docker-start](/man/docker-start)(1), [docker-container-start](/man/docker-container-start)(1), [docker-rm](/man/docker-rm)(1), [docker-ps](/man/docker-ps)(1)"],["RESOURCES","```[Documentation](https://docs.docker.com/reference/cli/docker/container/stop/)```"]],"e":["docker-container-stop","docker-start","docker-container-start","docker-rm","docker-ps"],"x":"docker stop mycontainer"},"duf":{"n":"duf","t":"modern disk usage and free space utility","s":[["TAGLINE","modern disk usage and free space utility"],["TLDR","**Show disk usage** for all mounted filesystems\n\n```duf```\n\n**Show only local filesystems**\n\n```duf --only local```\n\n**Show specific mount points**\n\n```duf [/] [/home]```\n\n**Show all filesystems** including special\n\n```duf --all```\n\n**Hide specific filesystem types**\n\n```duf --hide-fs tmpfs,devtmpfs```\n\n**Output as JSON**\n\n```duf --json```\n\n**Sort by usage percentage**\n\n```duf --sort usage```\n\n**Show inodes** instead of disk space\n\n```duf --inodes```\n\n**Choose which columns** to display\n\n```duf --output mountpoint,size,used,avail,usage```"],["SYNOPSIS","**duf** [_options_] [_paths_...]"],["DESCRIPTION","**duf** (Disk Usage/Free) is a modern replacement for the df command with a colorful, user-friendly terminal interface. It displays disk usage and free space for mounted filesystems in an organized table format.\n\nThe tool provides a clear visual representation with color-coded usage bars, making it easy to identify nearly-full filesystems at a glance. It groups filesystems by type (local, network, special) for better organization.\n\nduf automatically handles various filesystem types and units, displaying sizes in human-readable format. It supports multiple output formats including JSON for scripting."],["PARAMETERS","**--all**\n> Include pseudo, duplicate, and inaccessible filesystems.\n\n**--only** _devices_ / **--hide** _devices_\n> Show only, or hide, specific **device groups**: `local`, `network`, `fuse`, `special`, `loops`, `binds`. Comma-separated.\n\n**--only-fs** _types_ / **--hide-fs** _types_\n> Show only, or hide, specific **filesystem types** such as `ext4` or `tmpfs`.\n\n**--only-mp** _paths_ / **--hide-mp** _paths_\n> Show only, or hide, specific **mount points**. Wildcards are supported.\n\n**--inodes**\n> List inode usage instead of block usage.\n\n**--json**\n> Output every device as JSON.\n\n**--output** _fields_\n> Choose the columns to display, comma-separated.\n\n**--sort** _field_\n> Sort by a column. Defaults to `mountpoint`.\n\n**--style** _style_\n> Table style: `unicode` or `ascii`.\n\n**--theme** _theme_\n> Colour theme: `dark`, `light`, or `ansi`.\n\n**--width** _n_\n> Maximum output width.\n\n**--avail-threshold** _sizes_\n> Colouring thresholds (yellow, red) for the available column. Defaults to `10G,1G`.\n\n**--usage-threshold** _fractions_\n> Colouring thresholds (yellow, red) for the usage bars, as fractions from 0 to 1. Defaults to `0.5,0.9`.\n\n**--warnings**\n> Print warnings to stderr."],["CAVEATS","The grouping flags are easy to mix up. **--only** and **--hide** take *device groups* (`local`, `network`, `fuse`, `special`, `loops`, `binds`), not filesystem types or paths. For filesystem types use **--only-fs**/**--hide-fs**, and for paths **--only-mp**/**--hide-mp**.\n\nduf is a display tool, not a `df` replacement in scripts: the table is meant for humans, and colour and box-drawing characters will corrupt anything that parses it. Use **--json** when the output feeds another program.\n\nBy default it omits pseudo-filesystems, so a mount you expect to see may need **--all** to appear. A hung network filesystem can make duf block, exactly as `df` does, since both must stat the mount point."],["HISTORY","**duf** was written by **Christian Muehlhaeuser** in **2020** in Go, one of a wave of tools that took a venerable Unix utility and asked what it would look like if the terminal had colour, Unicode, and a wide screen, which it had not when `df` was written. It caught on quickly, since reading `df` output is a small daily irritation for a great many people and duf's grouped, colour-coded table removes it."],["INSTALL","```apt: sudo apt install duf```\n\n```dnf: sudo dnf install duf```\n\n```pacman: sudo pacman -S duf```\n\n```zypper: sudo zypper install duf```\n\n```brew: brew install duf```\n\n```nix: nix profile install nixpkgs#duf```"],["SEE ALSO","[df](/man/df)(1), [du](/man/du)(1), [ncdu](/man/ncdu)(1), [dust](/man/dust)(1), [dua](/man/dua)(1)"],["RESOURCES","```[Source code](https://github.com/muesli/duf)```"]],"e":["df","du","ncdu","dust","dua"],"x":"duf"},"express":{"n":"express","t":"Node.js web application scaffolding tool","s":[["TAGLINE","Node.js web application scaffolding tool"],["TLDR","**Create Express app with generator**\n\n```npx express-generator [myapp]```\n\n**Create with view engine**\n\n```npx express-generator --view=[pug] [myapp]```\n\n**Create with CSS preprocessor**\n\n```npx express-generator --css=[sass] [myapp]```\n\n**Create without view engine**\n\n```npx express-generator --no-view [myapp]```\n\n**Create with git ignore**\n\n```npx express-generator --git [myapp]```"],["SYNOPSIS","**express-generator** [_options_] [_dir_]"],["PARAMETERS","**--view** _engine_\n> View engine: pug, ejs, hbs, hjs, twig.\n\n**--css** _engine_\n> CSS preprocessor: sass, less, stylus, compass.\n\n**-e**, **--ejs**\n> Use EJS template engine.\n\n**--pug**\n> Use Pug template engine.\n\n**--hbs**\n> Use Handlebars template engine.\n\n**--no-view**\n> Generate without view engine.\n\n**-c** _engine_\n> CSS engine (same as --css).\n\n**--git**\n> Add .gitignore.\n\n**-f**, **--force**\n> Force on non-empty directory."],["DESCRIPTION","**express-generator** scaffolds Express.js web applications. It creates the basic directory structure, package.json, and boilerplate code for starting an Express server.\n\nExpress is a minimal Node.js web framework. The generator provides a conventional starting point with routes, views (optional), static files, and error handling."],["GENERATED STRUCTURE","```\nmyapp/\n├── app.js          # Main application\n├── package.json    # Dependencies\n├── bin/www         # Server entry point\n├── public/         # Static files\n├── routes/         # Route handlers\n└── views/          # Templates\n```"],["CAVEATS","Generator provides minimal setup. Production needs additional configuration (security, logging, etc.). Express 5 is in development; generator may lag. Consider alternatives like Fastify or Nest.js for larger projects."],["HISTORY","Express was created by **TJ Holowaychuk** in **2010** and became the most popular Node.js web framework. The express-generator was separated from the main package to allow the core framework to remain minimal."],["INSTALL","```aur: yay -S express```"],["SEE ALSO","[node](/man/node)(1), [npm](/man/npm)(1), [nodemon](/man/nodemon)(1)"],["RESOURCES","```[Source code](https://github.com/expressjs/generator)```\n\n```[Homepage](https://expressjs.com/)```"]],"e":["node","npm","nodemon"],"x":"npx express-generator [myapp]"},"false":{"n":"false","t":"return failure exit status","s":[["TAGLINE","return failure exit status"],["TLDR","**Return failure status**\n\n```false```\n\n**Use in conditional**\n\n```if false; then echo \"never\"; fi```\n\n**Chain with or**\n\n```false || echo \"false returned failure\"```\n\n**Infinite loop idiom** (until never succeeds)\n\n```until false; do :; done```"],["SYNOPSIS","**false**"],["PARAMETERS","**--help**\n> Display help information.\n\n**--version**\n> Display version information."],["DESCRIPTION","**false** does nothing and returns a failure exit status (1). It's the counterpart to true and is used in shell scripts for flow control, testing, and as a placeholder.\n\nThe command takes no operands and always exits with status 1, indicating failure. This makes it useful in conditional statements, loops that should never execute, and testing error handling.\n\nfalse is a POSIX-standard command and shell builtin, providing a guaranteed failure exit status."],["CAVEATS","As a builtin, behavior may vary slightly between shells. Exit status is always 1 (or non-zero). Does absolutely nothing else."],["HISTORY","false has been part of Unix since **Version 7** (1979). It's one of the simplest Unix commands, existing solely to return a failure status. It's specified by POSIX and implemented as both a standalone utility and shell builtin."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[true](/man/true)(1), [test](/man/test)(1), [bash](/man/bash)(1)"],["RESOURCES","```[Source code](https://git.savannah.gnu.org/cgit/coreutils.git)```\n\n```[Homepage](https://www.gnu.org/software/coreutils/)```"]],"e":["true","test","bash"],"x":"false"},"freqtrade":{"n":"freqtrade","t":"cryptocurrency trading bot framework","s":[["TAGLINE","cryptocurrency trading bot framework"],["TLDR","**Start trading** with a configuration and strategy\n\n```freqtrade trade --config [config.json] --strategy [StrategyName]```\n\n**Run backtesting** on historical data\n\n```freqtrade backtesting --config [config.json] --strategy [StrategyName]```\n\n**Download historical data** for backtesting\n\n```freqtrade download-data --config [config.json] --pairs [BTC/USDT] --timeframes [1h]```\n\n**Create a new configuration file** interactively\n\n```freqtrade new-config```\n\n**Optimize strategy parameters** with hyperopt\n\n```freqtrade hyperopt --config [config.json] --strategy [StrategyName] --hyperopt-loss [SharpeHyperOptLoss]```\n\n**List available exchanges**\n\n```freqtrade list-exchanges```\n\n**List trading pairs** on an exchange\n\n```freqtrade list-pairs --config [config.json] --exchange [binance]```\n\n**Start the web UI**\n\n```freqtrade webserver --config [config.json]```"],["SYNOPSIS","**freqtrade** _command_ [_options_]\n\n**freqtrade** trade [_-c config_] [_-s strategy_]\n\n**freqtrade** backtesting [_-c config_] [_-s strategy_]"],["PARAMETERS","**-c**, **--config** _PATH_\n> Specify configuration file (default: config.json)\n\n**-s**, **--strategy** _NAME_\n> Specify strategy class name\n\n**--strategy-path** _PATH_\n> Additional strategy lookup path\n\n**-d**, **--datadir** _PATH_\n> Path to historical data directory\n\n**--userdir** _PATH_\n> Path to user data directory\n\n**-v**, **--verbose**\n> Verbose mode (-vv for more, -vvv for all)\n\n**--logfile** _FILE_\n> Log to specified file\n\n**--no-color**\n> Disable colorized output\n\n**--db-url** _URL_\n> Database URL for trade storage\n\n**--dry-run**\n> Run in simulation mode without real trades\n\n**-V**, **--version**\n> Show version number\n\n**-h**, **--help**\n> Show help message"],["CONFIGURATION","**config.json**\n> Main configuration file specifying exchange, API keys, trading parameters, stake amount, and strategy settings.\n\n**user_data/strategies/**\n> Directory containing custom trading strategy Python classes."],["COMMANDS","**trade**\n> Start live or dry-run trading\n\n**backtesting**\n> Test strategy on historical data\n\n**hyperopt**\n> Optimize strategy parameters\n\n**download-data**\n> Download historical OHLCV data\n\n**new-config**\n> Create new configuration interactively\n\n**create-userdir**\n> Create user data directory structure\n\n**list-exchanges**\n> Print available exchanges\n\n**list-pairs**\n> Print available trading pairs\n\n**list-strategies**\n> Print available strategies\n\n**list-timeframes**\n> Print available timeframes\n\n**show-trades**\n> Display trades from database\n\n**plot-dataframe**\n> Plot strategy signals on chart\n\n**plot-profit**\n> Plot profit over time\n\n**webserver**\n> Start the FreqUI web interface\n\n**install-ui**\n> Install the FreqUI web interface"],["DESCRIPTION","**freqtrade** is an open-source cryptocurrency trading bot written in Python. It supports automated trading on major exchanges including Binance, Kraken, Coinbase, and many others through the CCXT library.\n\nThe bot executes user-defined trading strategies that specify entry and exit conditions based on technical indicators. Strategies are Python classes that can be backtested against historical data before live deployment. The hyperopt feature uses machine learning to optimize strategy parameters.\n\nFreqtrade operates in dry-run mode by default, simulating trades without real money. It can be controlled via Telegram bot or the FreqUI web interface. Trade data is stored in a SQLite database for analysis and the plot commands visualize strategy performance."],["CAVEATS","Cryptocurrency trading carries significant financial risk. Backtesting results do not guarantee future performance. Exchange APIs may have rate limits that affect bot operation. Strategies require thorough testing in dry-run mode before live trading. Configuration files contain API keys and should be secured properly. The bot requires continuous operation and stable network connectivity."],["HISTORY","Freqtrade was started in **2017** as a community-driven open-source project. Written in **Python 3**, it grew from a simple trading bot to a comprehensive trading framework with backtesting, optimization, and visualization tools. The project gained significant traction during the 2020-2021 cryptocurrency bull market. It introduced FreqAI in **2022** for machine learning-based adaptive strategies. Development continues actively on GitHub with regular releases."],["SEE ALSO","[ccxt](/man/ccxt)(1), [python](/man/python)(1)"]],"e":["ccxt","python"],"x":"freqtrade trade --config [config.json] --strategy [StrategyName]"},"fzf-make":{"n":"fzf-make","t":"Fuzzy-find and run make, npm, just, and task targets","s":[["TAGLINE","Fuzzy-find and run make, npm, just, and task targets"],["TLDR","**Pick and run** a target in the current project\n\n```fzf-make```\n\n**Show help**\n\n```fzf-make --help```"],["SYNOPSIS","**fzf-make** [*options*]"],["DESCRIPTION","**fzf-make** opens a fuzzy finder with a preview window so you can select and execute build/task targets. Supported task systems:\n\n- **make** (including **include** directives)\n- **npm** / **pnpm** / **yarn** (including yarn workspaces scripts)\n- **just** recipes\n- **task** (Taskfile; uses **task --list-all --json**, including **includes**)\n\nRun it in the project directory that contains the relevant **Makefile**, **package.json**, **justfile**, or **Taskfile**.\n\nInstall via Homebrew, AUR, Nix, **cargo install --locked fzf-make**, or Linux release binaries (**linux_amd64** / **linux_arm64**)."],["PARAMETERS","See **fzf-make --help** for flags supported by your version (history, config, and UI options evolve between releases). Primary usage is interactive with no required arguments."],["CAVEATS","Needs a terminal UI and the underlying tool (**make**, **npm**, **task**, etc.) installed for the project type you select. Behaviour depends on how targets are declared; malformed Makefiles or Taskfiles may yield incomplete lists."],["INSTALL","```brew: brew install fzf-make```\n\n```nix: nix profile install nixpkgs#fzf-make```"],["SEE ALSO","[make](/man/make)(1), [just](/man/just)(1), [task](/man/task)(1), [fzf](/man/fzf)(1)"],["RESOURCES","```[Source code](https://github.com/kyu08/fzf-make)```"]],"e":["make","just","task","fzf"],"x":"fzf-make"},"gem":{"n":"gem","t":"package manager for Ruby libraries","s":[["TAGLINE","package manager for Ruby libraries"],["TLDR","**Install gem**\n\n```gem install [package_name]```\n\n**Install specific version**\n\n```gem install [package_name] -v [1.2.3]```\n\n**List installed gems**\n\n```gem list```\n\n**Update gem**\n\n```gem update [package_name]```\n\n**Uninstall gem**\n\n```gem uninstall [package_name]```"],["SYNOPSIS","**gem** _command_ [_options_] [_arguments_]"],["PARAMETERS","_COMMAND_\n> Operation: install, uninstall, list, update, search, etc.\n\n**install** _GEM_\n> Install gem package.\n\n**uninstall** _GEM_\n> Remove gem.\n\n**list**\n> List installed gems.\n\n**update**\n> Update gems.\n\n**search** _TERM_\n> Search for gems.\n\n**-v** _VERSION_\n> Specify version.\n\n**--help**\n> Display help information."],["CONFIGURATION","**~/.gemrc**\n> User-specific gem configuration including default gem sources and installation options.\n\n**/etc/gemrc**\n> System-wide gem configuration for all users."],["DESCRIPTION","**gem** is RubyGems' command-line package manager for installing, managing, and distributing Ruby libraries and applications. It provides access to RubyGems.org, the central repository hosting hundreds of thousands of community-contributed Ruby packages called \"gems\".\n\nThe tool handles dependency resolution automatically, installing required gems and their dependencies in the correct order. It supports installing specific versions, updating existing gems, and managing multiple versions of the same gem simultaneously. Each gem includes metadata specifying its dependencies, Ruby version requirements, and other constraints.\n\ngem installs packages either system-wide (requiring sudo/administrator privileges) or in user-specific locations. It integrates with bundler for project-specific dependency management, allowing different projects to use different gem versions without conflict. The Gemfile and Gemfile.lock workflow, managed by bundler, has become the standard for Ruby applications.\n\nBeyond installation, gem provides commands for searching available packages, viewing documentation, building and publishing your own gems, and managing local gem caches. It's the cornerstone of Ruby's package ecosystem and an essential tool for all Ruby developers.\n\nRubyGems has been part of Ruby's standard library since version 1.9, making gem available with every Ruby installation."],["CAVEATS","System gems may require sudo. Conflicts possible without bundler. Version resolution can be complex."],["HISTORY","RubyGems was created in 2004 to standardize Ruby library distribution. It became part of Ruby's standard library in Ruby 1.9, making gem the official Ruby package manager."],["INSTALL","```dnf: sudo dnf install ruby```\n\n```pacman: sudo pacman -S ruby```\n\n```apk: sudo apk add ruby```\n\n```zypper: sudo zypper install ruby```\n\n```brew: brew install ruby```\n\n```nix: nix profile install nixpkgs#ruby```"],["SEE ALSO","[bundle](/man/bundle)(1), [ruby](/man/ruby)(1), [rvm](/man/rvm)(1)"],["RESOURCES","```[Homepage](https://rubygems.org/)```\n\n```[Documentation](https://guides.rubygems.org/command-reference/)```\n\n```[Source code](https://github.com/rubygems/rubygems)```"]],"e":["bundle","ruby","rvm"],"x":"gem install [package_name]"},"git-magic":{"n":"git-magic","t":"Commit changes with an auto-generated commit message","s":[["TAGLINE","Commit changes with an auto-generated commit message"],["TLDR","**Commit staged changes** with a generated message\n\n```git magic```\n\n**Stage all changes** (with confirmation) and commit\n\n```git magic -a```\n\n**Commit and push**\n\n```git magic -p```\n\n**Commit and force-push**\n\n```git magic -p -f```\n\n**Prepend a custom title** before the generated message\n\n```git magic -m \"[title]\"```\n\n**Edit the generated message** before committing\n\n```git magic -e```"],["SYNOPSIS","**git** **magic** [_-a_] [_-m_ _msg_] [_-e_] [_-p_] [_-f_]"],["PARAMETERS","**-a**\n> Stage all changes (after confirmation) before committing.\n\n**-m** _msg_\n> Prepend a custom message; the generated summary is appended after it.\n\n**-e**\n> Open the editor to review the message instead of committing with `--no-edit`.\n\n**-p**\n> Push after committing.\n\n**-f**\n> Force-push; only takes effect combined with `-p`.\n\n**-h**\n> Display usage information."],["DESCRIPTION","**git magic** is a git-extras command that commits changes using an automatically generated commit message built from `git status --porcelain -uno`, so the message ends up listing the files that changed. By default the commit happens without opening an editor (`--no-edit`).\n\nPassing `-a` shows `git status`, asks for confirmation, then stages everything before committing. `-m` lets you prepend a real message before the auto-generated summary, `-e` opens an editor to adjust the final message, and `-p` (optionally with `-f` for a force-push) pushes immediately after the commit."],["CAVEATS","Must be run inside a git working tree; it changes to the repository's top level automatically. The generated message is just a list of changed files, not a meaningful description, so this is best suited for quick WIP commits rather than history you intend to keep readable."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-commit](/man/git-commit)(1), [git-extras](/man/git-extras)(1)"],["RESOURCES","```[Source code](https://github.com/tj/git-extras)```\n\n```[Documentation](https://github.com/tj/git-extras/blob/master/Commands.md#git-magic)```"]],"e":["git-commit","git-extras"],"x":"git magic"},"git-mergetool":{"n":"git-mergetool","t":"Visual conflict resolution tool","s":[["TAGLINE","Visual conflict resolution tool"],["TLDR","**Run merge tool**\n\n```git mergetool```\n\n**Use specific tool**\n\n```git mergetool --tool=[vimdiff]```\n\n**Resolve specific file**\n\n```git mergetool [file.txt]```\n\n**Don't prompt**\n\n```git mergetool --no-prompt```\n\n**Use configured tool**\n\n```git mergetool -y```"],["SYNOPSIS","**git mergetool** [_options_] [_file_]"],["PARAMETERS","_FILE_\n> Specific file to resolve.\n\n**--tool** _TOOL_\n> Use specified merge tool.\n\n**--tool-help**\n> List available tools.\n\n**-y**, **--no-prompt**\n> Don't prompt before each file.\n\n**--prompt**\n> Prompt before each file.\n\n**-g**, **--gui**\n> Use `merge.guitool` instead of `merge.tool`.\n\n**--help**\n> Display help information."],["DESCRIPTION","**git mergetool** runs a visual merge conflict resolution tool for each conflicted file. It launches configured tools like vimdiff, meld, or kdiff3, providing a three-way merge interface showing the base, local, and remote versions side by side.\n\nAfter saving the merged result in the tool, the file is marked as resolved. Multiple conflicted files are processed sequentially, and you can choose to skip individual files."],["CONFIGURATION","**~/.gitconfig**\n> Configure default merge tool and tool-specific settings.\n\n```\n[merge]\n    tool = vimdiff\n[mergetool]\n    prompt = false\n    keepBackup = false\n```"],["CAVEATS","Requires configured merge tool. Creates .orig backup files. Some tools need installation."],["HISTORY","git mergetool is a core **Git** command providing integration with external merge tools, essential for complex conflict resolution."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-merge](/man/git-merge)(1), [git-difftool](/man/git-difftool)(1), [vimdiff](/man/vimdiff)(1)"],["RESOURCES","```[Source code](https://github.com/git/git)```\n\n```[Homepage](https://git-scm.com/)```\n\n```[Documentation](https://git-scm.com/docs/git-mergetool)```"]],"e":["git-merge","git-difftool","vimdiff"],"x":"git mergetool"},"git-show-ref":{"n":"git-show-ref","t":"List references in the local repository","s":[["TAGLINE","List references in the local repository"],["TLDR","**List all refs**\n\n```git show-ref```\n\n**Show heads only**\n\n```git show-ref --heads```\n\n**Show tags only**\n\n```git show-ref --tags```\n\n**Verify ref exists**\n\n```git show-ref --verify refs/heads/main```\n\n**Quiet mode**\n\n```git show-ref -q --verify refs/heads/main```"],["SYNOPSIS","**git show-ref** [**--head**] [**-d** | **--dereference**] [**-s** | **--hash**[=_N_]] [**--abbrev**[=_N_]] [**--branches**] [**--tags**] [--] [_pattern_...]\n\n**git show-ref** **--verify** [**-q** | **--quiet**] [**-d** | **--dereference**] [**-s** | **--hash**[=_N_]] [**--abbrev**[=_N_]] [--] [_ref_...]\n\n**git show-ref** **--exists** _ref_\n\n**git show-ref** **--exclude-existing**[=_pattern_]"],["PARAMETERS","_PATTERN_\n> Match refs whose end matches the pattern (matched in complete parts).\n\n**--head**\n> Include the HEAD reference, which is filtered out by default.\n\n**--branches**\n> Limit to local branches (refs/heads). Replaces the older **--heads**, which still works as an alias.\n\n**--tags**\n> Limit to local tags (refs/tags).\n\n**--verify**\n> Require the argument to be an exact ref path (e.g. refs/heads/main). Errors if the ref does not exist.\n\n**--exists** _ref_\n> Check whether a ref exists. Exit code 0 = exists, 2 = missing, 1 = error.\n\n**--exclude-existing**[=_pattern_]\n> Filter mode. Reads refs from stdin and prints those that do not exist locally, optionally limited to refs matching the pattern suffix.\n\n**-q**, **--quiet**\n> Suppress output. Use the exit code only.\n\n**-s**, **--hash**[=_N_]\n> Print only the object name (optionally abbreviated to _N_ hex digits), not the ref name.\n\n**--abbrev**[=_N_]\n> Abbreviate the printed object name to _N_ hex digits (default uses **core.abbrev**).\n\n**-d**, **--dereference**\n> For tag objects, also print the dereferenced commit, suffixed with **^{}**."],["DESCRIPTION","**git show-ref** lists references in the local repository, showing SHA-1 hashes and ref names for branches, tags, and other refs. It is a plumbing command commonly used in scripts.\n\nThe verify mode checks whether a specific ref exists without listing all refs, returning an appropriate exit code."],["CAVEATS","Plumbing command. Shows local refs only. Use git ls-remote for remote refs."],["HISTORY","git show-ref is a core **Git** plumbing command for reference inspection, commonly used in scripts."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-ls-remote](/man/git-ls-remote)(1), [git-for-each-ref](/man/git-for-each-ref)(1)"]],"e":["git-ls-remote","git-for-each-ref"],"x":"git show-ref"},"go-version":{"n":"go-version","t":"Print Go version and binary build info","s":[["TAGLINE","Print Go version and binary build info"],["TLDR","**Show Go version**\n\n```go version```\n\n**Show binary's Go version**\n\n```go version [binary]```\n\n**Show version of all binaries**\n\n```go version -m [binary]```"],["SYNOPSIS","**go version** [_options_] [_files_]"],["PARAMETERS","_FILES_\n> Binary files to inspect.\n\n**-m**\n> Print each executable's embedded module version information (added in Go 1.13).\n\n**-v**\n> Report unrecognized files (otherwise non-Go files are silently skipped when scanning a directory).\n\n**--help**\n> Display help information."],["DESCRIPTION","**go version** prints the Go runtime version of the toolchain when invoked without arguments. When given file arguments — executables or directories of executables — it reports the Go version each binary was built with, and with **-m** also dumps the build-time module dependency graph (`go.mod`-equivalent info) embedded in the binary.\n\nDirectory arguments are walked recursively; non-Go executables are skipped silently unless **-v** is given."],["CAVEATS","Module info is only embedded for binaries built by Go 1.13+. Heavily stripped binaries (e.g., built with `-ldflags=\"-w -s\"` and then run through external strippers that remove the Go build info section) may not report any version."],["HISTORY","go version is part of the **Go** toolchain, extended to report binary build information for debugging."],["INSTALL","```apt: sudo apt install gccgo-go```\n\n```dnf: sudo dnf install gcc-go```\n\n```pacman: sudo pacman -S gcc-go```\n\n```apk: sudo apk add gcc-go```\n\n```zypper: sudo zypper install gcc-go```\n\n```brew: brew install go```\n\n```nix: nix profile install nixpkgs#go```"],["SEE ALSO","[go](/man/go)(1), [go-build](/man/go-build)(1)"]],"e":["go","go-build"],"x":"go version"},"goaccess":{"n":"goaccess","t":"Real-time web log analyzer and interactive viewer","s":[["TAGLINE","Real-time web log analyzer and interactive viewer"],["TLDR","**Analyze** one or more log files in interactive mode\n\n```goaccess [path/to/logfile1] [path/to/logfile2] ...```\n\nUse a specific **log-format** or pre-defined format\n\n```goaccess [path/to/logfile] --log-format=[format|combined]```\n\nAnalyze a log from **stdin**\n\n```tail -f [path/to/logfile] | goaccess -```\n\nAnalyze a log and write to **HTML** file in real-time\n\n```goaccess [path/to/logfile] -o [path/to/file.html] --real-time-html```"],["SYNOPSIS","**goaccess** [_options_] _logfile_"],["PARAMETERS","**-o**, **--output** _FILE_\n> Output to specified file (HTML, JSON, or CSV based on extension)\n\n**--log-format** _FORMAT_\n> Specify log format string or use predefined: COMBINED, COMMON, etc.\n\n**--real-time-html**\n> Enable real-time HTML report updates via WebSocket\n\n**--ws-url** _URL_\n> WebSocket URL for real-time HTML\n\n**-a**, **--agent-list**\n> Enable full user-agent parsing\n\n**-d**, **--with-output-resolver**\n> Enable IP resolver for geolocation\n\n**-e**, **--exclude-ip** _IP_\n> Exclude specific IP from statistics\n\n**--ignore-panel** _PANEL_\n> Ignore specific panel in output\n\n**--geoip-database** _FILE_\n> GeoIP database for geolocation"],["DESCRIPTION","**goaccess** is an open-source real-time web log analyzer and interactive viewer. It parses web server logs (Apache, Nginx, etc.) and displays statistics in a terminal dashboard or generates HTML/JSON/CSV reports.\n\nFeatures include real-time analysis with streaming logs, geolocation lookup, user agent parsing, and detailed metrics for visitors, requests, referrers, 404 errors, operating systems, and browsers.\n\nThe interactive terminal mode provides a ncurses-based dashboard with drill-down capabilities. HTML reports can update in real-time via WebSocket connections."],["CONFIGURATION","**~/.goaccessrc**\n> User-level configuration file for default options like log format and output settings.\n\n**/etc/goaccess/goaccess.conf**\n> System-wide configuration file with default log format, date/time formats, and panel settings."],["CAVEATS","Log format must match the actual log format; mismatches cause parsing failures. Real-time HTML requires a running goaccess process. Large log files may require significant memory for processing."],["HISTORY","goaccess was created by Gerardo Orellana and first released in **2009**. It gained popularity as a lightweight, privacy-respecting alternative to cloud-based analytics services, particularly useful for quick server log analysis."],["INSTALL","```apt: sudo apt install goaccess```\n\n```dnf: sudo dnf install goaccess```\n\n```pacman: sudo pacman -S goaccess```\n\n```apk: sudo apk add goaccess```\n\n```zypper: sudo zypper install goaccess```\n\n```brew: brew install goaccess```\n\n```nix: nix profile install nixpkgs#goaccess```"],["SEE ALSO","[awstats](/man/awstats)(1), [nginx](/man/nginx)(8), [apache2](/man/apache2)(8)"]],"e":["awstats","nginx","apache2"],"x":"goaccess [path/to/logfile1] [path/to/logfile2] ..."},"impacket-mqtt_check":{"n":"impacket-mqtt_check","t":"simple tool that checks if an MQTT broker allows connections, optionally","s":[["TAGLINE","simple tool that checks if an MQTT broker allows connections, optionally"],["TLDR","**Check MQTT broker** for anonymous access\n\n```impacket-mqtt_check [192.168.1.100]```\n\n**Check MQTT on a specific port**\n\n```impacket-mqtt_check [192.168.1.100] -port [1883]```\n\n**Check MQTT with credentials**\n\n```impacket-mqtt_check [192.168.1.100] -user [username] -password [password]```"],["SYNOPSIS","**impacket-mqtt_check** [_-h_] [_-port PORT_] [_-user USERNAME_] [_-password PASSWORD_] _target_"],["PARAMETERS","**-port** _PORT_\n> MQTT broker port (default: 1883)\n\n**-user** _USERNAME_\n> Username for MQTT authentication\n\n**-password** _PASSWORD_\n> Password for MQTT authentication"],["DESCRIPTION","**impacket-mqtt_check** is a simple tool that checks if an MQTT (Message Queuing Telemetry Transport) broker allows connections, optionally testing for anonymous access. MQTT is a lightweight messaging protocol commonly used in IoT devices and applications.\n\nThe tool attempts to connect to the specified broker and reports whether the connection succeeds, helping identify misconfigured brokers that allow unauthenticated access."],["CAVEATS","Only tests connection capability, does not enumerate topics or messages. MQTT over TLS (port 8883) may require additional configuration. Some brokers may allow connection but restrict actions based on ACLs."],["HISTORY","Part of the **Impacket** library by SecureAuth. Added to address the growing security concerns around IoT protocols, particularly MQTT brokers exposed to the internet without proper authentication."],["INSTALL","```pacman: sudo pacman -S impacket```"],["SEE ALSO","[mosquitto_sub](/man/mosquitto_sub)(1), [mosquitto_pub](/man/mosquitto_pub)(1), [nmap](/man/nmap)(1)"]],"e":["mosquitto_sub","mosquitto_pub","nmap"],"x":"impacket-mqtt_check [192.168.1.100]"},"jc":{"n":"jc","t":"converts command output to JSON format","s":[["TAGLINE","converts command output to JSON format"],["TLDR","**Parse command output**\n\n```[command] | jc --[parser]```\n\n**Parse ps output**\n\n```ps aux | jc --ps```\n\n**Parse ls output**\n\n```ls -l | jc --ls```\n\n**Parse ifconfig**\n\n```ifconfig | jc --ifconfig```\n\n**Parse with pretty output**\n\n```df | jc --df -p```\n\n**List available parsers**\n\n```jc -a```\n\n**Parse file contents**\n\n```jc --[parser] < [file.txt]```"],["SYNOPSIS","**jc** [_options_] --_parser_ [_file_]"],["PARAMETERS","**--_PARSER_**\n> Parser name (ps, ls, df, ifconfig, etc.).\n\n**-a**, **--about**\n> List available parsers.\n\n**-p**, **--pretty**\n> Pretty print JSON output.\n\n**-r**, **--raw**\n> Raw parser output.\n\n**-q**, **--quiet**\n> Suppress warnings.\n\n**-m**, **--monochrome**\n> Disable colored output.\n\n**-M**, **--meta-out**\n> Include metadata in output (timestamp, parser name, exit codes).\n\n**-C**, **--force-color**\n> Force colored output even when piping.\n\n**-d**, **--debug**\n> Enable debug mode (use -dd for verbose).\n\n**-y**, **--yaml-out**\n> Output in YAML format instead of JSON.\n\n**-s**, **--slurp**\n> Combine multiple lines into a JSON array.\n\n**-u**, **--unbuffer**\n> Disable output buffering for streaming.\n\n**-v**, **--version**\n> Display version information.\n\n**--help**\n> Display help information."],["DESCRIPTION","**jc** converts the output of dozens of standard Unix commands into structured JSON or YAML format, enabling piping to JSON-aware tools like jq for automated processing and scripting."],["CAVEATS","Parser accuracy varies. Not all command versions supported. May need updates for format changes."],["HISTORY","jc was created by **Kelly Brazil** to enable JSON-based automation from traditional Unix command output."],["INSTALL","```dnf: sudo dnf install jc```\n\n```pacman: sudo pacman -S jc```\n\n```apk: sudo apk add jc```\n\n```zypper: sudo zypper install jc```\n\n```brew: brew install jc```\n\n```nix: nix profile install nixpkgs#jc```"],["SEE ALSO","[jq](/man/jq)(1), [jo](/man/jo)(1), [gron](/man/gron)(1), [mlr](/man/mlr)(1)"]],"e":["jq","jo","gron","mlr"],"x":"[command] | jc --[parser]"},"jj-config-list":{"n":"jj-config-list","t":"displays all Jujutsu configuration values","s":[["TAGLINE","displays all Jujutsu configuration values"],["TLDR","**List all config values**\n\n```jj config list```\n\n**List user config only**\n\n```jj config list --user```\n\n**List repo config only**\n\n```jj config list --repo```\n\n**List with config source including overridden values**\n\n```jj config list --include-overridden```\n\n**List including default values**\n\n```jj config list --include-defaults```\n\n**List workspace-level config only**\n\n```jj config list --workspace```\n\n**List values using a custom template**\n\n```jj config list -T [template]```"],["SYNOPSIS","**jj** **config** **list** [_options_]"],["PARAMETERS","**--user**\n> Show user configuration only.\n\n**--repo**\n> Show repository configuration only.\n\n**--workspace**\n> Show workspace configuration only.\n\n**--include-overridden**\n> Include values overridden by higher-priority configs.\n\n**--include-defaults**\n> Include default values in the output.\n\n**-T**, **--template** _TEMPLATE_\n> Render output with a custom template."],["DESCRIPTION","**jj config list** displays all Jujutsu configuration values. Shows merged configuration from all sources (user, repo, workspace) by default. Useful for debugging configuration issues and understanding effective settings.\n\nAvailable template fields: **name** (config name), **value** (TOML value), **overridden** (boolean), **source** (origin), **path** (config file path)."],["INSTALL","```pacman: sudo pacman -S jujutsu```\n\n```apk: sudo apk add jujutsu```\n\n```zypper: sudo zypper install jujutsu```\n\n```brew: brew install jujutsu```\n\n```nix: nix profile install nixpkgs#jujutsu```"],["SEE ALSO","[jj](/man/jj)(1), [jj-config-get](/man/jj-config-get)(1), [jj-config-set](/man/jj-config-set)(1), [jj-config-edit](/man/jj-config-edit)(1)"]],"e":["jj","jj-config-get","jj-config-set","jj-config-edit"],"x":"jj config list"},"jj-resolve":{"n":"jj-resolve","t":"helps resolve merge conflicts","s":[["TAGLINE","helps resolve merge conflicts"],["TLDR","**Resolve conflicts**\n\n```jj resolve```\n\n**Resolve specific file**\n\n```jj resolve [path/to/file]```\n\n**List conflicted files**\n\n```jj resolve --list```\n\n**Resolve with specific tool**\n\n```jj resolve --tool [meld]```"],["SYNOPSIS","**jj resolve** [_options_] [_path_]"],["PARAMETERS","_PATH_\n> Specific file to resolve.\n\n**-l**, **--list**\n> List files with conflicts instead of launching a merge tool.\n\n**--tool** _NAME_\n> Merge tool to use. The tool must be configured in `ui.merge-editor` or `merge-tools.<name>`. Mutually exclusive with **--list**.\n\n**-r**, **--revision** _REVSET_\n> Revision whose conflicts should be resolved (default `@`).\n\n**--help**\n> Display help information."],["DESCRIPTION","**jj resolve** helps resolve merge conflicts. It launches a merge tool for conflicted files.\n\nThe command identifies and processes files with conflict markers. Jujutsu tracks conflicts as first-class states."],["CAVEATS","Subcommand of jj. Requires configured merge tool. Conflicts persist until resolved."],["HISTORY","jj resolve is part of **Jujutsu**, supporting its unique approach to treating conflicts as normal repository states."],["INSTALL","```pacman: sudo pacman -S jujutsu```\n\n```apk: sudo apk add jujutsu```\n\n```zypper: sudo zypper install jujutsu```\n\n```brew: brew install jujutsu```\n\n```nix: nix profile install nixpkgs#jujutsu```"],["SEE ALSO","[jj](/man/jj)(1), [jj-squash](/man/jj-squash)(1), [jj-status](/man/jj-status)(1), [jj-new](/man/jj-new)(1), [jj-describe](/man/jj-describe)(1)"]],"e":["jj","jj-squash","jj-status","jj-new","jj-describe"],"x":"jj resolve"},"libinput":{"n":"libinput","t":"library and command-line tool for handling input devices on Linux","s":[["TAGLINE","library and command-line tool for handling input devices on Linux"],["TLDR","**List** all input devices\n\n```sudo libinput list-devices```\n\n**Debug** input events\n\n```sudo libinput debug-events```\n\nLaunch **debug GUI**\n\n```sudo libinput debug-gui```\n\nDebug **tablet** devices\n\n```sudo libinput debug-tablet```\n\nDisplay **help**\n\n```libinput --help```"],["SYNOPSIS","**libinput** [_--help_|_--version_] _command_ [_args_]"],["SUBCOMMANDS","**list-devices**\n> List all devices recognized by libinput, including their capabilities.\n\n**debug-events** [_--verbose_] [_--device_ _DEV_]\n> Print all input events from devices to stdout.\n\n**debug-gui**\n> Show a graphical visualization of input events for connected devices.\n\n**debug-tablet**\n> Print tablet axis and stylus state for connected tablets.\n\n**measure** _feature_\n> Measure device-specific properties such as touchpad pressure or tablet pressure curve.\n\n**analyze** _subcommand_\n> Analyze recorded events from a libinput record file.\n\n**record** [_options_] [_device_]\n> Record input events to a YAML file for later replay or analysis.\n\n**replay** _file_\n> Replay events from a previously recorded file."],["PARAMETERS","**-h**, **--help**\n> Display help information.\n\n**--version**\n> Display version information."],["DESCRIPTION","**libinput** is a library and command-line tool for handling input devices on Linux. It provides a unified interface for mice, keyboards, touchpads, and tablets on both X11 and Wayland systems.\n\nThe CLI uses a verb-based interface, with subcommands such as **list-devices** to enumerate devices, **debug-events** to monitor input, and **record**/**replay** to capture and reproduce input traces for debugging."],["CAVEATS","Most commands require root privileges (or membership in the **input** group) to access input devices. The **debug-gui** subcommand requires a graphical environment."],["INSTALL","```apt: sudo apt install libinput-tools```\n\n```dnf: sudo dnf install libinput```\n\n```pacman: sudo pacman -S libinput-tools```\n\n```apk: sudo apk add libinput```\n\n```zypper: sudo zypper install libinput-tools```\n\n```nix: nix profile install nixpkgs#libinput```"],["SEE ALSO","[xinput](/man/xinput)(1), [evtest](/man/evtest)(1)"]],"e":["xinput","evtest"],"x":"sudo libinput list-devices"},"lotus-miner":{"n":"lotus-miner","t":"Filecoin storage provider CLI","s":[["TAGLINE","Filecoin storage provider CLI"],["TLDR","**Initialize a new miner**\n\n```lotus-miner init```\n\n**Start the miner daemon**\n\n```lotus-miner run```\n\n**Check miner status**\n\n```lotus-miner info```\n\n**List storage deals**\n\n```lotus-miner storage-deals list```\n\n**Show sector status**\n\n```lotus-miner sectors status [sectorNumber]```"],["SYNOPSIS","**lotus-miner** [_command_] [_options_]"],["PARAMETERS","**init**\n> Initialize a new storage miner.\n\n**run**\n> Start the storage miner daemon.\n\n**info**\n> Display miner information and status.\n\n**stop**\n> Stop the running miner daemon.\n\n**config**\n> Manage miner configuration.\n\n**storage-deals**\n> Manage storage deals.\n\n**sectors**\n> Manage and inspect sectors.\n\n**auth**\n> Manage RPC authentication tokens.\n\n**backup**\n> Create a metadata backup.\n\n**--help**, **-h**\n> Show help.\n\n**--version**, **-v**\n> Print version."],["DESCRIPTION","**lotus-miner** is the storage provider component of the **Lotus** Filecoin implementation. It manages the lifecycle of storage deals, sector sealing, and proof generation for participating as a storage provider on the Filecoin network.\n\nThe miner communicates with a running **lotus** daemon (full node) and handles pledging storage capacity, accepting storage deals from clients, and submitting storage proofs to the blockchain."],["CAVEATS","Requires a running **lotus** full node daemon. Storage mining requires significant disk space, GPU for proof generation, and a pledged FIL balance. Written in Go."],["SEE ALSO","[geth](/man/geth)(1), [ipfs](/man/ipfs)(1)"]],"e":["geth","ipfs"],"x":"lotus-miner init"},"lowriter":{"n":"lowriter","t":"word processor component of LibreOffice","s":[["TAGLINE","word processor component of LibreOffice"],["TLDR","**Start Writer**\n\n```lowriter```\n\n**Open document**\n\n```lowriter [document.odt]```\n\n**Convert to PDF**\n\n```lowriter --convert-to pdf [document.odt]```\n\n**Convert to DOCX**\n\n```lowriter --convert-to docx [document.odt]```\n\n**Batch convert multiple files**\n\n```lowriter --convert-to pdf --outdir [output/] [*.odt]```\n\n**Print document**\n\n```lowriter -p [document.odt]```"],["SYNOPSIS","**lowriter** [_options_] [_files_...]"],["PARAMETERS","**--convert-to** _format_\n> Convert to specified format.\n\n**--outdir** _dir_\n> Output directory for conversions.\n\n**-p**\n> Print document.\n\n**--pt** _printer_\n> Print to specific printer.\n\n**--headless**\n> Run without GUI.\n\n**--view**\n> Open in read-only mode."],["DESCRIPTION","**lowriter** is the word processor component of LibreOffice. It provides full-featured document editing, formatting, and publishing capabilities compatible with Microsoft Word formats.\n\nThe command line interface enables batch conversions and scripting."],["SUPPORTED FORMATS","```\nInput:  ODT, DOC, DOCX, RTF, TXT, HTML\nOutput: PDF, ODT, DOCX, RTF, TXT, HTML\n```"],["CAVEATS","GUI requires display (use --headless for servers). Conversion quality depends on format complexity. Large documents may need increased memory."],["HISTORY","LibreOffice Writer evolved from OpenOffice.org Writer, which originated from StarWriter by **Star Division** (acquired by Sun Microsystems in 1999). LibreOffice forked in **2010**."],["INSTALL","```apt: sudo apt install libreoffice-writer```\n\n```dnf: sudo dnf install libreoffice-writer```\n\n```apk: sudo apk add libreoffice-writer```\n\n```zypper: sudo zypper install libreoffice-writer```"],["SEE ALSO","[libreoffice](/man/libreoffice)(1), [unoconv](/man/unoconv)(1)"]],"e":["libreoffice","unoconv"],"x":"lowriter"},"lpr":{"n":"lpr","t":"submits print jobs","s":[["TAGLINE","submits print jobs"],["TLDR","**Print file**\n\n```lpr [file]```\n\n**Print to specific printer**\n\n```lpr -P [printer] [file]```\n\n**Print multiple copies**\n\n```lpr -# [3] [file]```\n\n**Print from stdin**\n\n```cat [file] | lpr```\n\n**Remove file after printing**\n\n```lpr -r [file]```\n\n**Title for job**\n\n```lpr -T \"[title]\" [file]```"],["SYNOPSIS","**lpr** [_options_] [_files_]"],["PARAMETERS","_FILES_\n> Files to print.\n\n**-P** _PRINTER_\n> Destination printer.\n\n**-#** _COPIES_\n> Number of copies.\n\n**-r**\n> Remove file after printing.\n\n**-T** _TITLE_\n> Job title.\n\n**-o** _OPTION_\n> Printer option.\n\n**--help**\n> Display help information."],["DESCRIPTION","**lpr** submits files for printing. It sends one or more files to the default printer or a printer specified with **-P**. Files are queued and printed in the order submitted.\n\nOn modern Linux systems, lpr is provided by **CUPS** (Common Unix Printing System). Without arguments, it reads from stdin, making it useful in pipelines (e.g., `ls | lpr`). Printer options like paper size and duplex can be set with **-o**."],["CAVEATS","Requires CUPS. Use lp for System V style. Options are printer-specific."],["HISTORY","lpr originated in **BSD Unix** as the line printer spooler, now implemented by CUPS."],["INSTALL","```apt: sudo apt install lpr```\n\n```dnf: sudo dnf install cups-client```\n\n```apk: sudo apk add cups-client```\n\n```zypper: sudo zypper install cups-client```"],["SEE ALSO","[lp](/man/lp)(1), [lpq](/man/lpq)(1), [lprm](/man/lprm)(1)"]],"e":["lp","lpq","lprm"],"x":"lpr [file]"},"mdns-scanner":{"n":"mdns-scanner","t":"TUI for scanning networks and mapping IPs to mDNS hostnames","s":[["TAGLINE","TUI for scanning networks and mapping IPs to mDNS hostnames"],["TLDR","**Scan the local network**\n\n```mdns-scanner```"],["SYNOPSIS","**mdns-scanner** [_options_]"],["DESCRIPTION","**mdns-scanner** is a terminal UI application that scans the local network for active hosts and resolves their IP addresses to **multicast DNS** (mDNS) hostnames or DNS-SD service instances. It enumerates non-loopback network interfaces, performs ARP/ICMP-based host discovery, then issues mDNS queries on the **224.0.0.251** (IPv4) and **ff02::fb** (IPv6) link-local multicast groups on UDP port **5353**.\n\nThe TUI displays discovered hosts in a navigable list with their IP addresses and any resolved **\\*.local** hostnames or service records, making it useful for finding printers, Chromecasts, Apple devices, HomeKit accessories, and other Bonjour/Avahi-aware hardware on a LAN."],["CAVEATS","Only discovers devices that respond to mDNS queries. Devices without an mDNS responder (or with mDNS blocked by firewall rules) will not be resolved, though they may still appear by IP. Multicast traffic does not cross routed segments by default, so the tool only sees the directly-attached broadcast domain."],["HISTORY","**mdns-scanner** was created by **CramBL** and is written in **Rust**. It is published on **crates.io** and distributed through **cargo install mdns-scanner**."],["INSTALL","```nix: nix profile install nixpkgs#mdns-scanner```"],["SEE ALSO","[avahi-browse](/man/avahi-browse)(1), [nmap](/man/nmap)(1), [arp-scan](/man/arp-scan)(1)"]],"e":["avahi-browse","nmap","arp-scan"],"x":"mdns-scanner"},"mogrify.im6":{"n":"mogrify.im6","t":"ImageMagick 6 version of mogrify","s":[["TAGLINE","ImageMagick 6 version of mogrify"],["TLDR","**Resize all JPEG images in place to fit within 800x600**\n\n```mogrify.im6 -resize [800x600] [*.jpg]```\n\n**Convert all JPEG images to PNG format**\n\n```mogrify.im6 -format [png] [*.jpg]```\n\n**Convert format and write output to a specific directory**\n\n```mogrify.im6 -format [png] -path [output_dir] [*.jpg]```\n\n**Rotate all images by 90 degrees clockwise**\n\n```mogrify.im6 -rotate [90] [*.jpg]```\n\n**Set JPEG compression quality (0-100)**\n\n```mogrify.im6 -quality [80] [*.jpg]```\n\n**Strip all metadata and profiles from images**\n\n```mogrify.im6 -strip [*.jpg]```\n\n**Crop images to a specific region**\n\n```mogrify.im6 -crop [640x480+50+50] [*.jpg]```"],["SYNOPSIS","**mogrify.im6** [_options_] _files_"],["PARAMETERS","_FILES_\n> Image files to modify in place.\n\n**-resize** _GEOMETRY_\n> Resize image to fit within the given dimensions (e.g., 800x600, 50%).\n\n**-format** _TYPE_\n> Convert image to the specified format (e.g., png, gif, tiff). Output files are written alongside originals with a new extension.\n\n**-path** _DIRECTORY_\n> Write output files to the specified directory instead of overwriting originals.\n\n**-rotate** _DEGREES_\n> Rotate image by the given number of degrees clockwise.\n\n**-quality** _VALUE_\n> Set compression quality (0-100 for JPEG/PNG, higher is better quality).\n\n**-strip**\n> Remove all image profiles, comments, and metadata.\n\n**-crop** _GEOMETRY_\n> Crop image to the specified geometry (WxH+X+Y).\n\n**-blur** _RADIUS_x_SIGMA_\n> Apply Gaussian blur with given radius and sigma.\n\n**-colorspace** _TYPE_\n> Set the image colorspace (e.g., sRGB, Gray, CMYK).\n\n**-auto-orient**\n> Rotate image based on EXIF orientation tag and remove the tag.\n\n**-thumbnail** _GEOMETRY_\n> Resize and strip profiles, optimized for creating thumbnails.\n\n**--help**\n> Display help information."],["DESCRIPTION","**mogrify.im6** is the ImageMagick 6 version of mogrify. It modifies images in place, unlike convert which writes to a different output file. It is designed for batch processing, applying the same set of transformations to multiple files at once.\n\nWhen using **-format** to change the output type, new files are created with the appropriate extension. Use **-path** to write converted files to a separate directory and avoid overwriting originals."],["CAVEATS","Overwrites original files by default unless **-format** or **-path** is used. This is the ImageMagick 6 specific variant; use **mogrify** for ImageMagick 7."],["SEE ALSO","[mogrify](/man/mogrify)(1), [convert](/man/convert)(1), [identify](/man/identify)(1), [composite.im6](/man/composite.im6)(1), [identify.im6](/man/identify.im6)(1)"]],"e":["mogrify","convert","identify","composite.im6","identify.im6"],"x":"mogrify.im6 -resize [800x600] [*.jpg]"},"mt":{"n":"mt","t":"Control magnetic tape drive operations","s":[["TAGLINE","Control magnetic tape drive operations"],["TLDR","Check **status** of a tape drive\n\n```mt -f [/dev/nst0] status```\n\n**Rewind** tape to beginning\n\n```mt -f [/dev/nst0] rewind```\n\n**Forward space** a number of files\n\n```mt -f [/dev/nst0] fsf [count]```\n\n**Rewind and position** at beginning of specified file\n\n```mt -f [/dev/nst0] asf [count]```\n\nPosition tape at **end of data**\n\n```mt -f [/dev/nst0] eod```\n\n**Rewind and eject** tape\n\n```mt -f [/dev/nst0] eject```\n\nWrite **EOF mark** at current position\n\n```mt -f [/dev/nst0] eof```"],["SYNOPSIS","**mt** [**-f** _device_] _operation_ [_count_]"],["PARAMETERS","**-f _device_, -t _device_**\n> Specify tape device (e.g., /dev/nst0, /dev/st0)\n\n**-h**\n> List available commands\n\n**-V, --version**\n> Display version information"],["OPERATIONS","**status**\n> Print tape drive status and position\n\n**rewind**\n> Rewind tape to beginning\n\n**fsf _count_**\n> Forward space specified number of files\n\n**bsf _count_**\n> Backward space specified number of files\n\n**fsr _count_**\n> Forward space specified number of records\n\n**bsr _count_**\n> Backward space specified number of records\n\n**asf _count_**\n> Rewind and position at beginning of specified file\n\n**eod, seod**\n> Space to end of valid data\n\n**weof _count_, eof**\n> Write EOF (end-of-file) marks\n\n**eject, offline, rewoffl**\n> Rewind and unload/eject tape\n\n**erase**\n> Erase entire tape (long operation)\n\n**retension**\n> Rewind, wind to end, rewind again (conditions tape)\n\n**setblk _size_**\n> Set hardware block size in bytes (0 for variable)\n\n**seek _block_**\n> Seek to specified block number (SCSI)\n\n**tell**\n> Print current block number (SCSI)"],["DESCRIPTION","**mt** controls magnetic tape drive operations, commonly used with LTO (Linear Tape-Open) and other SCSI tape drives. It provides commands for positioning, rewinding, ejecting, and writing control marks on tape media.\n\nThe device is specified with -f option or via the TAPE environment variable. Use /dev/nstX for non-rewinding devices (preserves position after operations) or /dev/stX for auto-rewinding devices."],["CAVEATS","Tape operations can be slow, especially erase and retension. Using the wrong device (st vs nst) can cause unexpected tape rewinding. Some operations are SCSI-specific. Ensure correct device permissions for non-root access."],["HISTORY","**mt** (magnetic tape) has been a standard Unix utility since early Unix systems for controlling tape drives. The Linux version supports additional SCSI tape features beyond traditional operations."],["INSTALL","```dnf: sudo dnf install mt-st```\n\n```apk: sudo apk add mt-st```\n\n```zypper: sudo zypper install mt-st```\n\n```nix: nix profile install nixpkgs#mt-st```"],["SEE ALSO","[tar](/man/tar)(1), [dd](/man/dd)(1), [st](/man/st)(4)"]],"e":["tar","dd","st"],"x":"mt -f [/dev/nst0] status"},"nice":{"n":"nice","t":"runs a command with modified scheduling priority","s":[["TAGLINE","runs a command with modified scheduling priority"],["TLDR","**Run with lower priority**\n\n```nice [command]```\n\n**Run with specific niceness**\n\n```nice -n [10] [command]```\n\n**Run with highest priority (root)**\n\n```nice -n [-20] [command]```\n\n**Run with lowest priority**\n\n```nice -n [19] [command]```\n\n**Show current niceness**\n\n```nice```"],["SYNOPSIS","**nice** [_options_] [_command_]"],["PARAMETERS","_COMMAND_\n> Command to run.\n\n**-n**, **--adjustment=**_N_\n> Add integer _N_ to the niceness (default **10**). Negative values raise priority.\n\n**--help**\n> Display help information.\n\n**--version**\n> Output version information."],["DESCRIPTION","**nice** runs a command with a modified CPU scheduling niceness. Higher niceness means the process is \"nicer\" and receives less CPU time (lower priority).\n\nNiceness ranges from **-20** (highest priority) to **19** (lowest priority). With no command, nice prints the current niceness of the shell to standard output. When given a command with no **-n**, nice applies the default adjustment of **+10**."],["CAVEATS","Negative values require root. Default adjustment is 10. Affects CPU scheduling only."],["HISTORY","nice is a classic **Unix** command for controlling process scheduling priority since early Unix."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[renice](/man/renice)(1), [ionice](/man/ionice)(1), [chrt](/man/chrt)(1)"]],"e":["renice","ionice","chrt"],"x":"nice [command]"},"nload":{"n":"nload","t":"console network traffic monitor","s":[["TAGLINE","console network traffic monitor"],["TLDR","**Monitor all interfaces**\n\n```nload```\n\n**Monitor specific interface**\n\n```nload [eth0]```\n\n**Monitor multiple interfaces**\n\n```nload [eth0] [wlan0]```\n\n**Set refresh interval**\n\n```nload -t [500]```\n\n**Show in bits** instead of bytes\n\n```nload -u b```"],["SYNOPSIS","**nload** [_options_] [_devices_...]"],["DESCRIPTION","**nload** is a console network traffic monitor. It displays incoming and outgoing traffic using graphs and provides real-time bandwidth usage information.\n\nThe tool shows current, average, minimum, and maximum traffic rates, making it easy to monitor network activity."],["PARAMETERS","**-a** _period_\n> Average calculation period (seconds).\n\n**-t** _interval_\n> Refresh interval (milliseconds).\n\n**-u** _unit_\n> Unit for display (h=human, b=bits, B=bytes, k=KB, m=MB, g=GB).\n\n**-U** _unit_\n> Unit for total traffic.\n\n**-m**\n> Don't use multiple units.\n\n**devices**\n> Devices to monitor."],["KEYS","**Left/Right**: Switch devices\n**F2**: Options\n**F5**: Save options\n**F6**: Reload options\n**q**: Quit"],["CAVEATS","Shows instantaneous rates which may fluctuate. Total counts reset on restart. Device names must be exact. VPN interfaces may need configuration."],["HISTORY","**nload** was created by **Roland Riegel** as a simple network load monitor. It fills the niche for quick bandwidth visualization without the complexity of full network analyzers."],["INSTALL","```apt: sudo apt install nload```\n\n```dnf: sudo dnf install nload```\n\n```pacman: sudo pacman -S nload```\n\n```apk: sudo apk add nload```\n\n```zypper: sudo zypper install nload```\n\n```brew: brew install nload```\n\n```nix: nix profile install nixpkgs#nload```"],["SEE ALSO","[iftop](/man/iftop)(1), [bmon](/man/bmon)(1), [vnstat](/man/vnstat)(1), [nethogs](/man/nethogs)(1)"]],"e":["iftop","bmon","vnstat","nethogs"],"x":"nload"},"nmcli-networking":{"n":"nmcli-networking","t":"controls the overall networking state managed by NetworkManager","s":[["TAGLINE","controls the overall networking state managed by NetworkManager"],["TLDR","Show the **networking status** of NetworkManager\n\n```nmcli networking```\n\n**Enable or disable** networking and all interfaces managed by NetworkManager\n\n```nmcli networking [on|off]```\n\nShow the **last known connectivity** state\n\n```nmcli networking connectivity```\n\nShow the **current connectivity** state (performs a check)\n\n```nmcli networking connectivity check```"],["SYNOPSIS","**nmcli networking** [_command_]"],["PARAMETERS","**on**\n> Enable networking and all NetworkManager-managed interfaces\n\n**off**\n> Disable networking and deactivate all connections\n\n**connectivity**\n> Show connectivity state without performing a check\n\n**connectivity check**\n> Perform a connectivity check and show the result"],["DESCRIPTION","**nmcli networking** controls the overall networking state managed by NetworkManager. It provides a master switch to enable or disable all network interfaces at once.\n\nThe connectivity states are:\n- **full**: Connected with full internet access\n- **limited**: Connected to a network but no internet (captive portal, firewall)\n- **portal**: Behind a captive portal requiring login\n- **none**: No network connection\n\nThe **connectivity check** actively probes connectivity by accessing a known URL, while without \"check\" it reports the cached state from the last probe."],["CAVEATS","Disabling networking affects all NetworkManager-managed connections but not interfaces managed by other tools. The connectivity check depends on network accessibility to connectivity check servers (configurable in NetworkManager.conf). Some networks block these probes."],["INSTALL","```apt: sudo apt install network-manager```\n\n```apk: sudo apk add networkmanager-cli```"],["SEE ALSO","[nmcli](/man/nmcli)(1), [nmcli-general](/man/nmcli-general)(1), [nmcli-radio](/man/nmcli-radio)(1), [NetworkManager](/man/NetworkManager)(8)"]],"e":["nmcli","nmcli-general","nmcli-radio","NetworkManager"],"x":"nmcli networking"},"nomad":{"n":"nomad","t":"workload orchestrator by HashiCorp","s":[["TAGLINE","workload orchestrator by HashiCorp"],["TLDR","**Start development agent**\n\n```nomad agent -dev```\n\n**Run a job**\n\n```nomad job run [job.nomad]```\n\n**Show job status**\n\n```nomad job status [job_name]```\n\n**Stop a job**\n\n```nomad job stop [job_name]```\n\n**List running jobs**\n\n```nomad job status```\n\n**Show node status**\n\n```nomad node status```\n\n**View allocation logs**\n\n```nomad alloc logs [alloc_id]```"],["SYNOPSIS","**nomad** [_options_] _command_ [_args_]"],["DESCRIPTION","**nomad** is a workload orchestrator by HashiCorp. It deploys and manages applications across a cluster, supporting containers, VMs, and standalone executables.\n\nThe tool provides scheduling, service discovery, and rolling updates. It integrates with Consul and Vault for service mesh and secrets management."],["PARAMETERS","**agent**\n> Run Nomad agent.\n\n**job run** _file_\n> Submit a job.\n\n**job plan** _file_\n> Dry-run a job and show scheduling impact.\n\n**job status** [_job_]\n> Job status.\n\n**job stop** _job_\n> Stop a job.\n\n**node status**\n> Node information.\n\n**alloc status** _id_\n> Allocation details.\n\n**alloc logs** _id_\n> Allocation logs.\n\n**server members**\n> Server cluster info.\n\n**-dev**\n> Development mode.\n\n**-address** _addr_\n> Nomad API address (default: http://127.0.0.1:4646).\n\n**-region** _region_\n> Region to query.\n\n**-namespace** _ns_\n> Target namespace.\n\n**-token** _token_\n> ACL token to use."],["CAVEATS","Cluster setup requires planning. ACLs needed for production. Resource limits important. Consul recommended for service discovery."],["HISTORY","**Nomad** was released by **HashiCorp** in **2015** as a simpler alternative to Kubernetes for workload scheduling. It focuses on operational simplicity while supporting multiple workload types beyond containers."],["INSTALL","```pacman: sudo pacman -S nomad```\n\n```nix: nix profile install nixpkgs#nomad```"],["SEE ALSO","[consul](/man/consul)(1), [vault](/man/vault)(1), [docker](/man/docker)(1), [kubectl](/man/kubectl)(1)"]],"e":["consul","vault","docker","kubectl"],"x":"nomad agent -dev"},"openstack-subnet":{"n":"openstack-subnet","t":"manages IP subnets within OpenStack networks","s":[["TAGLINE","manages IP subnets within OpenStack networks"],["TLDR","**List subnets**\n\n```openstack subnet list```\n\n**Create subnet**\n\n```openstack subnet create --network [network] --subnet-range [192.168.1.0/24] [subnet_name]```\n\n**Show subnet details**\n\n```openstack subnet show [subnet_name]```\n\n**Delete subnet**\n\n```openstack subnet delete [subnet_name]```"],["SYNOPSIS","**openstack subnet** _command_ [_options_]"],["PARAMETERS","**list**\n> List subnets.\n\n**create** _name_\n> Create subnet.\n\n**delete** _name_\n> Delete subnet.\n\n**show** _name_\n> Show subnet details.\n\n**--network** _network_\n> Parent network.\n\n**--subnet-range** _cidr_\n> IP address range in CIDR.\n\n**--gateway** _ip_\n> Gateway IP address.\n\n**--dhcp** / **--no-dhcp**\n> Enable or disable DHCP on the subnet (default: _enabled_).\n\n**--dns-nameserver** _ip_\n> DNS server address (repeat for multiple).\n\n**--allocation-pool** _start=IP,end=IP_\n> Restrict DHCP allocation to a range within the subnet (repeatable).\n\n**--ip-version** _4|6_\n> IP version of the subnet (default: _4_).\n\n**--ipv6-address-mode** _MODE_\n> IPv6 address mode: _slaac_, _dhcpv6-stateful_, or _dhcpv6-stateless_.\n\n**--ipv6-ra-mode** _MODE_\n> IPv6 router advertisement mode (same values as **--ipv6-address-mode**).\n\n**--host-route** _destination=CIDR,gateway=IP_\n> Add a static host route (repeatable).\n\n**--subnet-pool** _NAME_\n> Allocate the range from a subnet pool instead of specifying **--subnet-range** directly."],["DESCRIPTION","**openstack subnet** manages IP subnets within OpenStack networks. Configure IP ranges, DHCP, DNS, and routing for virtual networks. Part of OpenStack unified CLI."],["SEE ALSO","[openstack-network](/man/openstack-network)(1), [openstack-port](/man/openstack-port)(1)"]],"e":["openstack-network","openstack-port"],"x":"openstack subnet list"},"pacman-u":{"n":"pacman-u","t":"short form of **pacman --upgrade**, install or upgrade from local package files","s":[["TAGLINE","short form of **pacman --upgrade**, install or upgrade from local package files"],["TLDR","This command is an alias of **pacman --upgrade**. See the full documentation:\n\n```tldr pacman upgrade```\n\n**Install a local package file**\n\n```sudo pacman -U [package.pkg.tar.zst]```\n\n**Install multiple local packages** with globbing\n\n```sudo pacman -U [*.pkg.tar.zst]```\n\n**Install a package from a URL**\n\n```sudo pacman -U [https://example.com/package.pkg.tar.zst]```\n\n**Downgrade from the pacman cache**\n\n```sudo pacman -U [/var/cache/pacman/pkg/package-oldver-x86_64.pkg.tar.zst]```\n\n**Install without dependency version checks**\n\n```sudo pacman -Udd [package.pkg.tar.zst]```"],["SYNOPSIS","**pacman** {**-U** | **--upgrade**} [_options_] _file_|_url_ ..."],["DESCRIPTION","**pacman -U** (long form `--upgrade`) installs or upgrades a package from a local package file or URL, bypassing the configured repositories. This is how you install locally built packages (from `makepkg`/AUR helpers), downgrades from the cache, and packages downloaded by hand.\n\nUnlike `-S`, which resolves names against the sync databases, `-U` operates directly on a `.pkg.tar.zst` (or `.pkg.tar.xz`, `.pkg.tar.gz`) file. Dependencies are still resolved against the sync databases, so a full refresh via `pacman -Sy` first is often advisable."],["COMMON OPTIONS","**-d**, **--nodeps**\n> Skip dependency version checks. Doubling (`-dd`) skips dependency checking entirely.\n\n**--asdeps**\n> Mark the installed package as a dependency (not explicitly requested).\n\n**--asexplicit**\n> Mark the installed package as explicitly installed.\n\n**--needed**\n> Skip install if the same version is already installed.\n\n**--overwrite** _GLOB_\n> Overwrite conflicting files matching _GLOB_ (use with caution).\n\n**--noconfirm**\n> Do not ask for any confirmation.\n\n**-v**, **--verbose**\n> Verbose output."],["CAVEATS","Requires root privileges (use `sudo`). The package must target the same architecture and be signed by a trusted key, or pass `--nosigcheck` (dangerous). Downgrading may break packages that depend on the newer version."],["INSTALL","```apt: sudo apt install pacman-package-manager```\n\n```dnf: sudo dnf install pacman```\n\n```pacman: sudo pacman -S pacman```\n\n```apk: sudo apk add pacman```\n\n```nix: nix profile install nixpkgs#pacman```"],["SEE ALSO","[pacman](/man/pacman)(8), [pacman-upgrade](/man/pacman-upgrade)(8), [pacman-key](/man/pacman-key)(8), [makepkg](/man/makepkg)(8)"]],"e":["pacman","pacman-upgrade","pacman-key","makepkg"],"x":"tldr pacman upgrade"},"pam_wheel":{"n":"pam_wheel","t":"restricts su to wheel group","s":[["TAGLINE","restricts su to wheel group"],["TLDR","**Require wheel group for su**\n\n```auth required pam_wheel.so```\n\n**Require wheel for root**\n\n```auth required pam_wheel.so root_only```\n\n**Use specific group**\n\n```auth required pam_wheel.so group=admin```\n\n**Trust wheel members**\n\n```auth sufficient pam_wheel.so trust```"],["SYNOPSIS","**pam_wheel.so** [_options_]"],["PARAMETERS","**root_only**\n> Only apply for su to root.\n\n**group=**_NAME_\n> Use alternate group.\n\n**trust**\n> Trust group members (no password).\n\n**deny**\n> Deny access to group members."],["DESCRIPTION","**pam_wheel** restricts su to wheel group. Controls root access.\n\nThe module requires group membership. Traditional Unix security."],["CAVEATS","Auth module. Wheel group required. BSD-style security."],["HISTORY","pam_wheel provides **wheel group restriction** for su command access."],["SEE ALSO","[pam](/man/pam)(8), [su](/man/su)(1), [pam_rootok](/man/pam_rootok)(8)"]],"e":["pam","su","pam_rootok"],"x":"auth required pam_wheel.so"},"peerindex":{"n":"peerindex","t":"Extract and analyze peer information from BitTorrent files","s":[["TAGLINE","Extract and analyze peer information from BitTorrent files"],["TLDR","**Analyze torrent peer data**\n\n```peerindex [file.torrent]```\n\n**Show verbose output**\n\n```peerindex -v [file.torrent]```\n\n**Export peer list**\n\n```peerindex -o [peers.txt] [file.torrent]```\n\n**Analyze multiple torrents**\n\n```peerindex [file1.torrent] [file2.torrent]```"],["SYNOPSIS","**peerindex** [_-v_] [_-o file_] [_options_] _torrent-files_"],["PARAMETERS","**-v**, **--verbose**\n> Verbose output.\n\n**-o**, **--output** _FILE_\n> Output file.\n\n**-q**, **--quiet**\n> Minimal output.\n\n**-h**, **--help**\n> Show help."],["DESCRIPTION","**peerindex** extracts and analyzes peer information from BitTorrent files. It shows tracker URLs and embedded peer data.\n\nTorrent files may contain cached peer addresses. This reveals swarm participants without connecting to trackers.\n\nTracker analysis shows all announce URLs. Multi-tracker setups display tier information.\n\nThe tool aids in understanding torrent structure. Useful for network forensics and research."],["CAVEATS","Peer data may be outdated. Not all torrents contain peer caches. Analysis is passive and doesn't contact network."],["HISTORY","**peerindex** is part of BitTorrent analysis toolsets used for network research and forensics."],["SEE ALSO","[transmission-show](/man/transmission-show)(1), [aria2c](/man/aria2c)(1), [btshowmetainfo](/man/btshowmetainfo)(1)"]],"e":["transmission-show","aria2c","btshowmetainfo"],"x":"peerindex [file.torrent]"},"pre-commit":{"n":"pre-commit","t":"Manage multi-language git hook scripts","s":[["TAGLINE","Manage multi-language git hook scripts"],["TLDR","**Install hooks into repository**\n\n```pre-commit install```\n\n**Run all hooks manually**\n\n```pre-commit run --all-files```\n\n**Run specific hook**\n\n```pre-commit run [hook_id]```\n\n**Update hook versions**\n\n```pre-commit autoupdate```\n\n**Create sample config**\n\n```pre-commit sample-config```\n\n**Install commit-msg hooks**\n\n```pre-commit install --hook-type commit-msg```\n\n**Run on specific files**\n\n```pre-commit run --files [file1] [file2]```\n\n**Clean cached hook environments**\n\n```pre-commit clean```"],["SYNOPSIS","**pre-commit** _command_ [_--all-files_] [_--files files_] [_options_]"],["PARAMETERS","**install**\n> Install git hooks.\n\n**uninstall**\n> Remove git hooks.\n\n**run** [_hook_id_]\n> Run hooks manually.\n\n**autoupdate**\n> Update hooks to latest versions.\n\n**sample-config**\n> Generate sample configuration.\n\n**clean**\n> Clean cached environments.\n\n**gc**\n> Garbage collect unused environments.\n\n**try-repo** _REPO_\n> Try hooks from repository.\n\n**--all-files**, **-a**\n> Run on all files, not just staged.\n\n**--files** _FILES_\n> Run on specific files.\n\n**--hook-stage** _STAGE_\n> Run hooks for stage: pre-commit, pre-push, commit-msg, post-commit, etc.\n\n**--hook-type** _TYPE_\n> Install hooks for type.\n\n**--color** _WHEN_\n> Color output: auto, always, never.\n\n**--verbose**, **-v**\n> Verbose output."],["DESCRIPTION","**pre-commit** manages and runs git hooks as code quality checks. Hooks run automatically before commits, catching issues before they enter the repository.\n\nConfiguration lives in .pre-commit-config.yaml, listing repositories and hooks to use. Hooks can check formatting, lint code, validate files, or run any custom command.\n\nEach hook runs in an isolated environment, automatically installed and cached. This ensures consistent behavior regardless of developer's local setup. Language-specific environments (Python, Node, Ruby, etc.) are supported.\n\nCommon hooks include: trailing-whitespace, end-of-file-fixer, check-yaml, check-json, black, flake8, prettier, eslint. The hook ecosystem provides ready-made checks for most needs.\n\nAutoupdate fetches latest hook versions from repositories. The --all-files flag runs against the entire codebase, useful for CI or initial cleanup. Stage-specific hooks (commit-msg, pre-push) enable additional checks."],["CAVEATS","Initial hook installation can be slow. Requires hooks to be installed per-clone. Some hooks may conflict with each other. Large files slow down commits. Breaking changes in hook updates possible. CI and local environments should match."],["HISTORY","**pre-commit** was created by **Anthony Sottile** at Yelp around **2014**. It addressed the problem of managing git hooks across teams - previously, hooks were often uncommitted or inconsistent. The framework standardized hook management and spawned a large ecosystem of shareable hooks."],["INSTALL","```dnf: sudo dnf install pre-commit```\n\n```pacman: sudo pacman -S pre-commit```\n\n```apk: sudo apk add pre-commit```\n\n```brew: brew install pre-commit```\n\n```nix: nix profile install nixpkgs#pre-commit```"],["SEE ALSO","[git-hooks](/man/git-hooks)(5), [husky](/man/husky)(1), [lefthook](/man/lefthook)(1), [lint-staged](/man/lint-staged)(1)"]],"e":["git-hooks","husky","lefthook","lint-staged"],"x":"pre-commit install"},"prs":{"n":"prs","t":"TUI for tracking GitHub pull requests from the terminal","s":[["TAGLINE","TUI for tracking GitHub pull requests from the terminal"],["TLDR","**Launch with config file** (defaults to query mode)\n\n```prs```\n\n**Run in query mode** with GitHub search syntax\n\n```prs -q '[type:pr author:@me state:open]'```\n\n**Run in repos mode** with an explicit repo list\n\n```prs -m repos --repos='[owner/repo1,owner/repo2]'```\n\n**Limit number of results**\n\n```prs -q '[type:pr state:open]' -n [30]```\n\n**Use a custom config file**\n\n```prs --config-path [path/to/prs.yml]```"],["SYNOPSIS","**prs** [_options_]"],["PARAMETERS","**-m**, **--mode** _MODE_\n> Operating mode: `query` (search PRs with GitHub search syntax, default) or `repos` (browse a predefined repository list).\n\n**-q**, **--query** _QUERY_\n> GitHub search query to run in query mode. Same syntax as github.com's PR search.\n\n**--repos** _LIST_\n> Comma-separated list of `owner/repo` entries used in repos mode.\n\n**-n**, **--num** _N_\n> Maximum number of PRs to fetch per page.\n\n**--config-path** _FILE_\n> Path to the YAML configuration file (default `~/.config/prs/prs.yml`).\n\n**-h**, **--help**\n> Show help."],["CONFIGURATION","Configuration sources, in order of precedence: command-line flags → environment variables (`PRS_QUERY`, `PRS_REPOS`, `PRS_MODE`, `PRS_NUM`) → YAML config file.\n\nThe YAML file lets you store reusable queries and repo lists so `prs` can be launched with no arguments."],["KEYBOARD SHORTCUTS","**General**\n\n- `q` / `esc` / `ctrl+c` — go back\n- `Q` — quit from anywhere\n- `?` — open help view\n\n**PR list view**\n\n- `⏎` / `tab` — switch to timeline view\n- `ctrl+s` — switch to repo list (repos mode)\n- `d` — open PR details\n- `ctrl+d` — show PR diff\n- `ctrl+v` — show PR details using `gh`\n- `ctrl+r` — reload list\n- `ctrl+b` — open PR in browser\n\n**Details / timeline**\n\n- `h` / `l` / `←` / `→` — navigate sections\n- `J` / `K` / `[` / `]` — move between PRs"],["DESCRIPTION","**prs** is a terminal user interface for watching GitHub pull requests without leaving the terminal. Query mode runs arbitrary GitHub PR searches; repos mode cycles through a curated repo list and shows each repo's open PRs. The tool shells out to `gh` for some actions (details, diff view) and uses the GitHub API for listings."],["CAVEATS","Requires authentication via the `gh` CLI or a `GITHUB_TOKEN` in the environment. API rate limits apply when fetching large result sets."],["HISTORY","**prs** was created by **dhth** and is written in **Go**. Source lives at github.com/dhth/prs."],["INSTALL","```apt: sudo apt install prs```\n\n```brew: brew install prs```\n\n```nix: nix profile install nixpkgs#prs```"],["SEE ALSO","[gh](/man/gh)(1), [git](/man/git)(1), [hub](/man/hub)(1)"]],"e":["gh","git","hub"],"x":"prs"},"pulsemixer":{"n":"pulsemixer","t":"CLI and TUI mixer for PulseAudio","s":[["TAGLINE","CLI and TUI mixer for PulseAudio"],["TLDR","**Launch TUI mixer**\n\n```pulsemixer```\n\n**Get volume**\n\n```pulsemixer --get-volume```\n\n**Set volume**\n\n```pulsemixer --set-volume [50]```\n\n**Change volume by relative amount**\n\n```pulsemixer --change-volume [+10]```\n\n**Change volume with a maximum cap**\n\n```pulsemixer --change-volume [+10] --max-volume [100]```\n\n**Mute/unmute**\n\n```pulsemixer --toggle-mute```\n\n**List sinks**\n\n```pulsemixer --list-sinks```\n\n**Set volume on a specific sink**\n\n```pulsemixer --id [sink-1] --set-volume [75]```"],["SYNOPSIS","**pulsemixer** [_options_]"],["PARAMETERS","**--get-volume**\n> Print current volume.\n\n**--set-volume** _n_\n> Set volume (0-100).\n\n**--get-mute**\n> Print mute status.\n\n**--toggle-mute**\n> Toggle mute.\n\n**--mute**\n> Mute.\n\n**--unmute**\n> Unmute.\n\n**--list-sinks**\n> List output devices.\n\n**--list-sources**\n> List input devices.\n\n**--change-volume** _+-n_\n> Change volume by a relative amount.\n\n**--set-volume-all** _n:n_\n> Set volume for every channel separately.\n\n**--max-volume** _n_\n> Cap volume at n when using --change-volume.\n\n**--id** _id_\n> Target specific sink/source.\n\n**--server** _server_\n> Connect to a specific PulseAudio server.\n\n**--color** _n_\n> Colorize output (0=none, 1=selected, 2=full).\n\n**--no-mouse**\n> Disable mouse support in TUI.\n\n**-l**, **--list**\n> List all sinks, sources, and sink inputs."],["DESCRIPTION","**pulsemixer** is a CLI and TUI mixer for PulseAudio. It provides both an interactive interface and command-line controls for managing audio volumes and routing."],["EXAMPLES","```bash"],["Interactive mode","pulsemixer"],["Set to 75%","pulsemixer --set-volume 75"],["Change specific sink","pulsemixer --id sink-1 --set-volume 50"],["Toggle mute","pulsemixer --toggle-mute"],["Get current volume","vol=$(pulsemixer --get-volume | cut -d' ' -f1)\n```"],["TUI KEYS","```\nh/l, Left/Right  - Adjust volume\nj/k, Up/Down     - Select\nm                - Toggle mute\nSpace            - Lock/unlock channels\n1-9, 0           - Set volume 10%-90%, 100%\nEnter            - Context menu\nF1/F2/F3         - Switch mode (output/input/cards)\nq, Esc           - Quit\n```"],["CAVEATS","Requires PulseAudio. Also works with PipeWire's PulseAudio compatibility."],["HISTORY","pulsemixer was created by **GeorgeFilipkin** as a convenient CLI/TUI for PulseAudio control."],["INSTALL","```pacman: sudo pacman -S pulsemixer```\n\n```apk: sudo apk add pulsemixer```\n\n```nix: nix profile install nixpkgs#pulsemixer```"],["SEE ALSO","[pactl](/man/pactl)(1), [pavucontrol](/man/pavucontrol)(1), [amixer](/man/amixer)(1)"]],"e":["pactl","pavucontrol","amixer"],"x":"pulsemixer"},"pvcreate":{"n":"pvcreate","t":"Initialize disks for LVM use","s":[["TAGLINE","Initialize disks for LVM use"],["TLDR","**Initialize** a partition as a physical volume\n\n```sudo pvcreate /dev/[sda1]```\n\n**Initialize multiple** devices at once\n\n```sudo pvcreate /dev/[sdb1] /dev/[sdc1]```\n\n**Force** creation without confirmation prompts\n\n```sudo pvcreate -ff /dev/[sda1]```\n\n**Initialize with a specific** metadata size\n\n```sudo pvcreate --metadatasize [512k] /dev/[sda1]```"],["SYNOPSIS","**pvcreate** [_OPTIONS_] _PhysicalVolume_..."],["DESCRIPTION","**pvcreate** initializes a disk or partition for use as a physical volume in LVM. This writes LVM metadata to the device, preparing it to be added to a volume group."],["PARAMETERS","**-f, --force**\n> Override various checks, confirmations, and protections. Use twice (`-ff`) to forcibly overwrite an existing physical volume.\n\n**-y, --yes**\n> Answer yes to all prompts automatically.\n\n**-Z, --zero** _y|n_\n> Controls if the first 4 sectors (2048 bytes) of the device are wiped. Default is yes unless `--restorefile` or `--uuid` are specified.\n\n**--metadatasize** _size_\n> Approximate amount of space to set aside for each metadata area on the PV.\n\n**--metadatacopies** _0|1|2_\n> Number of metadata area copies on this PV. 0 stores no copies, 1 stores at the front only, 2 stores copies at both front and end.\n\n**--metadataignore** _y|n_\n> Whether to ignore metadata areas on this PV. Useful when creating a large number of PVs to limit the number of metadata copies.\n\n**--dataalignment** _size_\n> Align the start of the data area to a multiple of this size.\n\n**--dataalignmentoffset** _size_\n> Shift the start of the data area by this additional offset.\n\n**-u, --uuid** _uuid_\n> Specify a UUID for the physical volume. Without this option, a random UUID is generated. Required when restoring metadata with vgcfgrestore.\n\n**--restorefile** _file_\n> Use metadata from a backup file to ensure consistent PV placement when restoring a volume group.\n\n**--bootloaderareasize** _size_\n> Reserve space for a bootloader between the LVM metadata and the first physical extent.\n\n**--labelsector** _number_\n> Sector number where the LVM2 label is written (default 1).\n\n**--setphysicalvolumesize** _size_\n> Override the automatically detected device size.\n\n**-v, --verbose**\n> Set verbose level. Can be repeated up to 4 times to increase detail."],["CAVEATS","All existing data on the device will be destroyed. The device should not be mounted or in use. Physical volumes must be created before being added to volume groups. Using `-ff` will override the check for existing filesystem signatures."],["HISTORY","**pvcreate** is part of **LVM2**, the Linux Logical Volume Manager."],["INSTALL","```apt: sudo apt install lvm2```\n\n```dnf: sudo dnf install lvm2```\n\n```pacman: sudo pacman -S lvm2```\n\n```apk: sudo apk add lvm2```\n\n```zypper: sudo zypper install lvm2```\n\n```nix: nix profile install nixpkgs#lvm2```"],["SEE ALSO","[lvm](/man/lvm)(8), [vgcreate](/man/vgcreate)(8), [pvdisplay](/man/pvdisplay)(8), [pvremove](/man/pvremove)(8), [pvs](/man/pvs)(8), [pvscan](/man/pvscan)(8), [lvcreate](/man/lvcreate)(8)"]],"e":["lvm","vgcreate","pvdisplay","pvremove","pvs","pvscan","lvcreate"],"x":"sudo pvcreate /dev/[sda1]"},"qemu-system-riscv64":{"n":"qemu-system-riscv64","t":"Emulate 64-bit RISC-V systems with QEMU","s":[["TAGLINE","Emulate 64-bit RISC-V systems with QEMU"],["TLDR","**Boot a RISC-V kernel on the generic virt machine**\n\n```qemu-system-riscv64 -machine virt -kernel [kernel] -append \"root=/dev/vda\"```\n\n**Boot with an explicit firmware (OpenSBI) and kernel**\n\n```qemu-system-riscv64 -machine virt -bios [opensbi.bin] -kernel [kernel]```\n\n**Run with 2 GB RAM, 4 SMP cores and a raw disk image**\n\n```qemu-system-riscv64 -machine virt -m [2G] -smp [4] -drive file=[disk.img],format=raw```\n\n**Run headless with a serial console on stdio**\n\n```qemu-system-riscv64 -machine virt -nographic -kernel [kernel]```\n\n**Attach a user-mode network with SSH port forwarding**\n\n```qemu-system-riscv64 -machine virt -netdev user,id=net0,hostfwd=tcp::[2222]-:22 -device virtio-net-device,netdev=net0```\n\n**List supported machine types**\n\n```qemu-system-riscv64 -machine help```"],["SYNOPSIS","**qemu-system-riscv64** [_options_] [_disk_image_]"],["PARAMETERS","**-machine** _type_\n> Machine type to emulate (e.g. **virt**, **sifive_u**, **sifive_e**, **spike**).\n\n**-cpu** _model_\n> CPU model (e.g. **rv64**, **sifive-u54**). Default is **rv64**.\n\n**-smp** _n_\n> Number of virtual CPUs.\n\n**-m** _size_\n> RAM size (e.g. **512M**, **2G**).\n\n**-kernel** _file_\n> Bootable kernel image (ELF, uImage, or raw).\n\n**-bios** _file_\n> Firmware image loaded before the kernel (default: OpenSBI).\n\n**-initrd** _file_\n> Initial ramdisk image.\n\n**-append** _args_\n> Kernel command-line arguments.\n\n**-drive** _options_\n> Define a block device (e.g. **file=disk.img,format=raw,if=virtio**).\n\n**-netdev** _options_\n> Define a backend network (e.g. user, tap).\n\n**-device** _options_\n> Add a device (e.g. **virtio-net-device**, **virtio-blk-device**).\n\n**-nographic**\n> Disable graphical output; redirect serial/console to stdio.\n\n**-serial** _dev_\n> Redirect serial port (e.g. **stdio**, **mon:stdio**, **file:log**).\n\n**-display** _type_\n> Display backend (**none**, **gtk**, **sdl**, **vnc**).\n\n**-S**\n> Freeze CPU at startup (useful with **-s** for gdb).\n\n**-s**\n> Shortcut for **-gdb tcp::1234**."],["DESCRIPTION","**qemu-system-riscv64** is the QEMU system emulator for 64-bit RISC-V guests. It can run bare-metal code, bootloaders, or full Linux distributions on a range of emulated boards. The default **virt** machine is a paravirtualized board designed for virtualization, with VirtIO devices, a CLINT/PLIC interrupt controller and optional OpenSBI firmware.\n\nOther boards model real hardware such as the **sifive_u** (SiFive HiFive Unleashed), **sifive_e** and **spike** simulator. The emulator supports the standard RV64GC ISA plus a large number of optional extensions (H, V, Zicbom, Zba, Zbb, etc.) that can be enabled with the **-cpu** option."],["CAVEATS","KVM acceleration on RISC-V requires a host CPU with the H extension; otherwise emulation is TCG-only and considerably slower than native. Some guest OSes expect a specific **-bios** firmware; mixing versions of OpenSBI and Linux can result in boot failures. When using **-nographic**, pressing **Ctrl-A X** exits the emulator."],["INSTALL","```apt: sudo apt install qemu-system-riscv```\n\n```dnf: sudo dnf install qemu-system-riscv```\n\n```pacman: sudo pacman -S qemu-system-riscv```\n\n```apk: sudo apk add qemu-system-riscv64```"],["SEE ALSO","[qemu-system-x86_64](/man/qemu-system-x86_64)(1), [qemu-img](/man/qemu-img)(1)"]],"e":["qemu-system-x86_64","qemu-img"],"x":"qemu-system-riscv64 -machine virt -kernel [kernel] -append \"root=/dev/vda\""},"qm-import":{"n":"qm-import","t":"Import a foreign VM image into Proxmox VE","s":[["TAGLINE","Import a foreign VM image into Proxmox VE"],["TLDR","**Import** a disk image into a new or existing VM storage layout\n\n```qm import [100] [storage_name]:import/[file_name] --storage [destination_storage]```"],["SYNOPSIS","**qm import** _vmid_ _source_ [_OPTIONS_]"],["PARAMETERS","**vmid**\n> Target virtual machine ID\n\n**source**\n> Source volume or path (often under a storage `import` content location)\n\n**--storage** _storage_\n> Destination storage for imported disks"],["DESCRIPTION","**qm import** imports supported virtual machine images (from VMware, VirtualBox, and similar hypervisors) into Proxmox VE. Source images are typically placed on a storage that allows `import` content, then referenced as `storage:import/filename`.\n\nThe command creates or attaches disk images on the destination storage and prepares the VM for configuration under `qm`. Exact supported formats and workflow follow the Proxmox import docs for your VE version."],["CAVEATS","Imported guests often need driver or boot adjustments (VirtIO vs legacy hardware). Large images require enough free space on the target storage. Prefer stopping the source VM before exporting for a consistent image."],["HISTORY","Part of **Proxmox VE** QEMU/KVM management (`qm`)."],["INSTALL","```dnf: sudo dnf install qm```"],["SEE ALSO","[qm](/man/qm)(1), [qm-importovf](/man/qm-importovf)(1), [qm-importdisk](/man/qm-importdisk)(1)"],["RESOURCES","```[Homepage](https://www.proxmox.com)```\n\n```[Documentation](https://pve.proxmox.com/pve-docs/qm.1.html)```"]],"e":["qm","qm-importovf","qm-importdisk"],"x":"qm import [100] [storage_name]:import/[file_name] --storage [destination_storage]"},"ruff-format":{"n":"ruff-format","t":"Format Python code with Ruff","s":[["TAGLINE","Format Python code with Ruff"],["TLDR","**Format current directory**\n\n```ruff format```\n\n**Format specific files**\n\n```ruff format [file.py]```\n\n**Check formatting without changes**\n\n```ruff format --check```\n\n**Show diff**\n\n```ruff format --diff```"],["SYNOPSIS","**ruff** **format** [_options_] [_files_...]"],["PARAMETERS","**--check**\n> Check without modifying.\n\n**--diff**\n> Show formatting diff.\n\n**--config** _file_\n> Configuration file.\n\n**--line-length** _n_\n> Maximum line length."],["DESCRIPTION","**ruff format** is the code formatting subcommand of Ruff, providing a Black-compatible Python formatter written in Rust. It enforces consistent code style by reformatting Python source files according to opinionated rules covering indentation, line length, string quoting, and whitespace.\n\nThe **--check** flag validates formatting without modifying files, returning a non-zero exit code if changes would be made, which is useful for CI pipelines. The **--diff** flag displays the exact changes that would be applied. Configuration options like **--line-length** can be set per-project in pyproject.toml or ruff.toml."],["INSTALL","```dnf: sudo dnf install ruff```\n\n```pacman: sudo pacman -S ruff```\n\n```apk: sudo apk add ruff```\n\n```brew: brew install ruff```\n\n```nix: nix profile install nixpkgs#ruff```"],["SEE ALSO","[ruff](/man/ruff)(1), [ruff-check](/man/ruff-check)(1), [black](/man/black)(1)"]],"e":["ruff","ruff-check","black"],"x":"ruff format"},"rustup-help":{"n":"rustup-help","t":"Display help for rustup commands","s":[["TAGLINE","Display help for rustup commands"],["TLDR","**Show top-level help**\n\n```rustup help```\n\n**Help for a specific** subcommand\n\n```rustup help [command]```\n\n**Help for a nested** subcommand\n\n```rustup help [toolchain] [install]```\n\n**Help for the** override subcommand\n\n```rustup help override```"],["SYNOPSIS","**rustup** **help** [_subcommand_...]"],["PARAMETERS","_subcommand_\n> One or more subcommand names to drill into. Without arguments, rustup prints its top-level help."],["DESCRIPTION","**rustup help** prints usage, options, and the list of subcommands for **rustup** or any of its subcommands. It is equivalent to running the command with **--help**, but accepts nested subcommand names as positional arguments instead of flags."],["INSTALL","```apt: sudo apt install rustup```\n\n```dnf: sudo dnf install rustup```\n\n```pacman: sudo pacman -S rustup```\n\n```zypper: sudo zypper install rustup```\n\n```brew: brew install rustup```\n\n```nix: nix profile install nixpkgs#rustup```"],["SEE ALSO","[rustup](/man/rustup)(1)"]],"e":["rustup"],"x":"rustup help"},"rustup-target":{"n":"rustup-target","t":"Manage Rust cross-compilation targets","s":[["TAGLINE","Manage Rust cross-compilation targets"],["TLDR","**List targets**\n\n```rustup target list```\n\n**Add target**\n\n```rustup target add [wasm32-unknown-unknown]```\n\n**Add ARM target**\n\n```rustup target add aarch64-unknown-linux-gnu```\n\n**Remove target**\n\n```rustup target remove [target]```"],["SYNOPSIS","**rustup** **target** _command_ [_options_]"],["PARAMETERS","**list**\n> List available targets.\n\n**add** _target_\n> Install target.\n\n**remove** _target_\n> Uninstall target.\n\n**--toolchain** _name_\n> Target specific toolchain."],["DESCRIPTION","**rustup target** manages cross-compilation targets. Add targets to compile for different platforms (ARM, WebAssembly, Windows, etc.) from your current system."],["INSTALL","```apt: sudo apt install rustup```\n\n```dnf: sudo dnf install rustup```\n\n```pacman: sudo pacman -S rustup```\n\n```zypper: sudo zypper install rustup```\n\n```brew: brew install rustup```\n\n```nix: nix profile install nixpkgs#rustup```"],["SEE ALSO","[rustup](/man/rustup)(1), [rustup-toolchain](/man/rustup-toolchain)(1)"]],"e":["rustup","rustup-toolchain"],"x":"rustup target list"},"screen":{"n":"screen","t":"Terminal multiplexer with persistent sessions","s":[["TAGLINE","Terminal multiplexer with persistent sessions"],["TLDR","**Start a new screen session**\n\n```screen```\n\n**Start a named session**\n\n```screen -S [session_name]```\n\n**List active sessions**\n\n```screen -ls```\n\n**Reattach to a session**\n\n```screen -r [session_name]```\n\n**Detach from current session** (inside screen)\n\n```Ctrl+a d```\n\n**Create a new window** (inside screen)\n\n```Ctrl+a c```\n\n**Switch between windows** (inside screen)\n\n```Ctrl+a n``` (next) or ```Ctrl+a p``` (previous)\n\n**Split screen horizontally**\n\n```Ctrl+a S```\n\n**Kill current window**\n\n```Ctrl+a k```"],["SYNOPSIS","**screen** [**-S** _name_] [**-r** [_name_]] [**-ls**] [**-d**] [_command_]"],["PARAMETERS","**-S** _name_\n> Create session with specified name\n\n**-r** [_name_]\n> Reattach to a detached session\n\n**-R**\n> Reattach if possible, otherwise start new session\n\n**-d**\n> Detach a running session\n\n**-D**\n> Detach and logout\n\n**-ls**, **-list**\n> List all sessions\n\n**-x**\n> Attach to a session that is already attached (multi-display)\n\n**-dm**\n> Start in detached mode (for scripts)\n\n**-X** _command_\n> Send command to a running session\n\n**-L**\n> Enable logging\n\n**-h** _lines_\n> Set scrollback buffer size"],["KEY BINDINGS","All commands are prefixed with **Ctrl+a** (command character):\n\n**Ctrl+a c**: Create new window\n**Ctrl+a n/p**: Next/previous window\n**Ctrl+a \"**: List windows\n**Ctrl+a 0-9**: Switch to window number\n**Ctrl+a d**: Detach from session\n**Ctrl+a k**: Kill current window\n**Ctrl+a S**: Split horizontally\n**Ctrl+a |**: Split vertically\n**Ctrl+a Tab**: Switch between regions\n**Ctrl+a X**: Close current region\n**Ctrl+a [**: Enter copy/scrollback mode\n**Ctrl+a ]**: Paste\n**Ctrl+a ?**: Show key bindings"],["DESCRIPTION","**screen** is a terminal multiplexer that allows running multiple terminal sessions within a single window. Sessions can be detached and reattached, making them persistent across disconnections—essential for remote work and long-running processes.\n\nEach screen session can contain multiple windows, each running its own shell or program. Windows can be split into regions to view multiple windows simultaneously.\n\nSessions persist even when you disconnect, whether intentionally or due to network issues. This makes screen invaluable for running processes on remote servers that must survive connection drops.\n\nCopy mode allows scrolling through terminal history and copying text between windows or to the clipboard."],["CONFIGURATION","**~/.screenrc**\n> Per-user configuration file for setting default options, key bindings, startup windows, and visual settings.\n\n**/etc/screenrc**\n> System-wide configuration file applied to all users before the per-user file.\n\n**SCREENDIR**\n> Environment variable overriding the default directory for session sockets (default: /tmp/screens/S-username)."],["CAVEATS","Screen's default configuration may conflict with some terminal features. Customize via **~/.screenrc**.\n\nThe command prefix **Ctrl+a** conflicts with readline's beginning-of-line. Remap with **escape** in .screenrc if needed.\n\nNested screen sessions require pressing the prefix twice (**Ctrl+a a**) to send commands to the inner session.\n\nFor modern alternatives with more features, consider **tmux**."],["HISTORY","Screen was originally written by **Oliver Laumann** in **1987** and has been maintained by the GNU project. It was one of the first terminal multiplexers and established the paradigm that tmux and others follow."],["INSTALL","```apt: sudo apt install screen```\n\n```dnf: sudo dnf install screen```\n\n```pacman: sudo pacman -S screen```\n\n```apk: sudo apk add screen```\n\n```zypper: sudo zypper install screen```\n\n```brew: brew install screen```\n\n```nix: nix profile install nixpkgs#screen```"],["SEE ALSO","[tmux](/man/tmux)(1), [byobu](/man/byobu)(1), [dtach](/man/dtach)(1), [nohup](/man/nohup)(1)"]],"e":["tmux","byobu","dtach","nohup"],"x":"screen"},"sfdk-init":{"n":"sfdk-init","t":"Initialize new Sailfish OS projects","s":[["TAGLINE","Initialize new Sailfish OS projects"],["TLDR","**Initialize a new QML application project**\n\n```sfdk init --type qtquick2-app```\n\n**Initialize a project with a specific name**\n\n```sfdk init --type qtquick2-app --name [myapp]```\n\n**List available project templates**\n\n```sfdk init --help```"],["SYNOPSIS","**sfdk init** [_options_]"],["PARAMETERS","**--type** _type_, **-t** _type_\n> Project template type (e.g., qtquick2-app).\n\n**--name** _name_\n> Project name."],["DESCRIPTION","**sfdk init** initializes a new Sailfish OS project from templates. Part of the Sailfish SDK command-line tools."],["SEE ALSO","[sfdk](/man/sfdk)(1), [sfdk-config](/man/sfdk-config)(1)"]],"e":["sfdk","sfdk-config"],"x":"sfdk init --type qtquick2-app"},"shellcheck":{"n":"shellcheck","t":"Static analysis tool for shell scripts","s":[["TAGLINE","Static analysis tool for shell scripts"],["TLDR","**Check a shell script**\n\n```shellcheck [script.sh]```\n\n**Check multiple scripts**\n\n```shellcheck [script1.sh] [script2.sh]```\n\n**Check with specific shell dialect**\n\n```shellcheck --shell=[bash|sh|dash|ksh] [script.sh]```\n\n**Output in different format** (JSON, GCC, etc.)\n\n```shellcheck --format=[json|gcc|checkstyle] [script.sh]```\n\n**Exclude specific warnings**\n\n```shellcheck --exclude=[SC2034,SC2086] [script.sh]```\n\n**Check script from stdin**\n\n```cat [script.sh] | shellcheck -```\n\n**Enable optional checks**\n\n```shellcheck --enable=all [script.sh]```"],["SYNOPSIS","**shellcheck** [_options_] [_script_...]"],["PARAMETERS","**-s**, **--shell** _dialect_\n> Specify shell dialect (sh, bash, dash, ksh)\n\n**-f**, **--format** _format_\n> Output format (tty, gcc, json, checkstyle, diff, quiet)\n\n**-e**, **--exclude** _codes_\n> Exclude specific error codes (comma-separated)\n\n**-i**, **--include** _codes_\n> Include only specific error codes\n\n**--enable** _checks_\n> Enable optional checks (all, require-variable-braces, etc.)\n\n**-x**, **--external-sources**\n> Follow and check sourced files\n\n**-a**, **--check-sourced**\n> Check sourced files for issues\n\n**-S**, **--severity** _level_\n> Minimum severity (error, warning, info, style)\n\n**-V**, **--version**\n> Display version\n\n**-C**, **--color** _when_\n> Colorize output (auto, always, never).\n\n**--wiki-link-count** _n_\n> Include wiki links for first n warnings."],["DESCRIPTION","**shellcheck** is a static analysis tool for shell scripts. It detects bugs, syntax issues, and pitfalls in bash/sh/ksh/dash scripts, providing explanations and suggestions for fixes.\n\nEach warning has a code (e.g., SC2086) linking to detailed wiki documentation explaining the issue, why it matters, and how to fix it. This makes shellcheck excellent for learning shell scripting best practices.\n\nThe tool catches common issues like unquoted variables, deprecated syntax, command substitution pitfalls, and potential security vulnerabilities. It distinguishes between different shell dialects and their specific features.\n\nIntegration is available for most editors (VS Code, Vim, Emacs, Sublime) and CI systems, enabling automated script checking."],["COMMON WARNINGS","**SC2086**: Double quote to prevent globbing and word splitting\n**SC2034**: Variable appears unused\n**SC2046**: Quote to prevent word splitting\n**SC2006**: Use $(...) instead of backticks\n**SC2035**: Use ./\\* so patterns don't expand to options\n**SC2164**: Use cd ... || exit in case cd fails"],["CONFIGURATION","**.shellcheckrc**\n> Project-level configuration file for default options, disabled warnings, and shell dialect. Also read from ~/.shellcheckrc."],["CAVEATS","ShellCheck may produce false positives when variables are used indirectly or sourced from external files. Use **# shellcheck disable=SC####** comments to suppress specific warnings.\n\nSome legitimate shell patterns trigger warnings. Review each warning before disabling; often the suggested fix is genuinely better.\n\nThe **--external-sources** option requires caution as it can execute arbitrary code when checking scripts that source files."],["HISTORY","**ShellCheck** was created by **Vidar Holen** and first released around **2012**. It is written in **Haskell** and has become the de facto standard linter for shell scripts, widely integrated into CI pipelines and editor plugins."],["INSTALL","```apt: sudo apt install shellcheck```\n\n```pacman: sudo pacman -S shellcheck```\n\n```apk: sudo apk add shellcheck```\n\n```brew: brew install shellcheck```\n\n```nix: nix profile install nixpkgs#shellcheck```"],["SEE ALSO","[bash](/man/bash)(1), [sh](/man/sh)(1), [lint](/man/lint)(1), [ksh](/man/ksh)(1)"]],"e":["bash","sh","lint","ksh"],"x":"shellcheck [script.sh]"},"snakemake":{"n":"snakemake","t":"Reproducible workflow management system","s":[["TAGLINE","Reproducible workflow management system"],["TLDR","**Run a workflow** from a Snakefile\n\n```snakemake```\n\n**Dry run** to show planned execution without running\n\n```snakemake -n```\n\n**Run with a specific number of cores**\n\n```snakemake --cores [4]```\n\n**Build a specific target**\n\n```snakemake [target_file]```\n\n**Show the execution plan** with shell commands\n\n```snakemake -n -p```\n\n**Generate a DAG visualization** of the workflow\n\n```snakemake --dag | dot -Tpng > dag.png```\n\n**Run with a specific Snakefile**\n\n```snakemake --snakefile [path/to/Snakefile]```\n\n**Force re-execution** of all rules\n\n```snakemake --forceall```"],["SYNOPSIS","**snakemake** [_options_] [_targets_]"],["PARAMETERS","**-n**, **--dry-run**\n> Show the execution plan without actually running any jobs.\n\n**-p**, **--printshellcmds**\n> Print shell commands that will be executed.\n\n**--cores**, **-c** _N_\n> Use at most N CPU cores in parallel. If N is omitted, uses all available cores.\n\n**-s**, **--snakefile** _FILE_\n> Specify the Snakefile. Default: Snakefile in current directory.\n\n**-d**, **--directory** _DIR_\n> Execute workflow in the specified directory.\n\n**--forceall**, **-F**\n> Force re-execution of all rules.\n\n**--forcerun**, **-R** _rules_\n> Force re-execution of specific rules.\n\n**--until**, **-U** _rules_\n> Run only until the specified rules.\n\n**--dag**\n> Output the directed acyclic graph of jobs in DOT format.\n\n**--rulegraph**\n> Output the dependency graph of rules in DOT format.\n\n**--config** _key=value_\n> Set or override config values.\n\n**--configfile** _FILE_\n> Specify a config file in YAML or JSON format.\n\n**--profile** _PROFILE_\n> Use a workflow profile for execution settings.\n\n**--cluster** _CMD_\n> Execute jobs on a cluster using the given submit command.\n\n**--use-conda**\n> Use conda environments defined in rules.\n\n**--use-singularity**\n> Use Singularity containers defined in rules.\n\n**-q**, **--quiet**\n> Suppress output except warnings and errors.\n\n**--help**\n> Display help information."],["DESCRIPTION","**Snakemake** is a workflow management system for creating reproducible and scalable data analyses. Workflows are defined using a Python-based domain-specific language in files called \"Snakefiles\" that describe rules for transforming input files into output files.\n\nSnakemake automatically determines the execution order by building a directed acyclic graph (DAG) of jobs based on file dependencies. It only re-executes rules when input files are newer than outputs, avoiding redundant computations. Workflows can seamlessly scale from single workstations to clusters and cloud environments without modification.\n\nThe tool is particularly popular in bioinformatics and data science for its ability to handle complex pipelines with many interdependent steps. It supports conda environments, containers (Docker/Singularity), and various cluster execution backends."],["CAVEATS","Requires Python 3.7+. The Snakefile must be valid Python syntax. When using **--cores** without a number, all available cores are used which may impact system responsiveness. Cluster execution requires appropriate configuration for the target scheduler. Conda and container integration require those tools to be installed separately."],["HISTORY","**Snakemake** was created by Johannes Köster and first published in **2012** in the journal Bioinformatics. The name is a play on \"make\" (the build automation tool) and Python (\"snake\"). It was designed to address the reproducibility challenges in computational biology workflows. The project has grown significantly, with hundreds of citations per year and integration with major bioinformatics communities. Development continues actively with regular releases."],["INSTALL","```brew: brew install snakemake```\n\n```nix: nix profile install nixpkgs#snakemake```"],["SEE ALSO","[make](/man/make)(1), [nextflow](/man/nextflow)(1), [dvc](/man/dvc)(1)"]],"e":["make","nextflow","dvc"],"x":"snakemake"},"ufw-allow":{"n":"ufw-allow","t":"Allow traffic through Uncomplicated Firewall","s":[["TAGLINE","Allow traffic through Uncomplicated Firewall"],["TLDR","**Allow** all traffic on a port\n\n```sudo ufw allow [port]```\n\nAllow traffic for a **protocol** on a port\n\n```sudo ufw allow [port]/[protocol]```\n\nAllow a known **application profile**\n\n```sudo ufw allow OpenSSH```\n\nAllow all traffic **from** a source address\n\n```sudo ufw allow from [source_address]```\n\nAllow **TCP** from one host to another on port 443\n\n```sudo ufw allow from 192.168.1.12 to 192.168.1.100 port 443 proto tcp```\n\nAllow with a **comment** for documentation\n\n```sudo ufw allow 5432 comment \"PostgreSQL\"```\n\n**Simulate** an allow rule without applying it\n\n```sudo ufw --dry-run allow 80/tcp```"],["SYNOPSIS","**ufw** [_--dry-run_] **allow** [_rule_]"],["PARAMETERS","**allow**\n> Permit matching traffic (inserts an allow rule)\n\n_port_[**/**_protocol_]\n> Simple form: port number, optional **/tcp** or **/udp**\n\n**from** _address_\n> Match source address or network (CIDR)\n\n**to** _address_\n> Match destination address\n\n**port** _port_\n> Destination port (or range) when using full rule syntax\n\n**proto** _protocol_\n> Protocol: **tcp**, **udp**, **gre**, etc.\n\n**in** / **out**\n> Direction of traffic\n\n**on** _interface_\n> Limit rule to a network interface\n\n**comment** '_text_'\n> Attach a human-readable comment to the rule\n\n**--dry-run**\n> Show what would change without applying it"],["DESCRIPTION","**ufw allow** adds an allow rule to Uncomplicated Firewall so matching packets are accepted. Rules can be simple port allows (`ufw allow 22/tcp`), service or application profiles (`ufw allow OpenSSH`), or full five-tuple style rules with source, destination, port, protocol, and interface.\n\nAllow rules are typically configured **before** `ufw enable` on remote hosts so SSH and other admin access remain available. Use `ufw status numbered` to list rules and `ufw delete` to remove them by number."],["CAVEATS","Requires root or sudo. Order and default policies (`ufw default deny incoming`) matter — an allow rule only helps if the default policy would otherwise block the traffic. Application profile names must match installed profiles under `/etc/ufw/applications.d/`."],["HISTORY","Part of **ufw** (Uncomplicated Firewall), the Ubuntu-originated frontend for iptables/nftables."],["INSTALL","```dnf: sudo dnf install ufw```\n\n```pacman: sudo pacman -S ufw```\n\n```apk: sudo apk add ufw```\n\n```zypper: sudo zypper install ufw```"],["SEE ALSO","[ufw](/man/ufw)(8), [ufw-enable](/man/ufw-enable)(8), [ufw-disable](/man/ufw-disable)(8), [iptables](/man/iptables)(8), [nftables](/man/nftables)(8)"],["RESOURCES","```[Source code](https://git.launchpad.net/ufw)```\n\n```[Documentation](https://help.ubuntu.com/community/UFW)```"]],"e":["ufw","ufw-enable","ufw-disable","iptables","nftables"],"x":"sudo ufw allow [port]"},"uv-venv":{"n":"uv-venv","t":"Create Python virtual environments","s":[["TAGLINE","Create Python virtual environments"],["TLDR","**Create virtual environment**\n\n```uv venv```\n\n**Create with specific Python**\n\n```uv venv --python [3.11]```\n\n**Create in specific directory**\n\n```uv venv [path]```\n\n**Create with seed packages**\n\n```uv venv --seed```"],["SYNOPSIS","**uv** **venv** [_options_] [_path_]"],["PARAMETERS","**--python** _version_\n> Python version.\n\n**--seed**\n> Install pip and setuptools.\n\n**--system-site-packages**\n> Access system packages.\n\n**--prompt** _name_\n> Custom prompt."],["DESCRIPTION","**uv venv** creates Python virtual environments. Creates isolated environments for package installation. Much faster than python -m venv or virtualenv."],["INSTALL","```dnf: sudo dnf install uv```\n\n```pacman: sudo pacman -S uv```\n\n```apk: sudo apk add uv```\n\n```brew: brew install uv```\n\n```nix: nix profile install nixpkgs#uv```"],["SEE ALSO","[uv](/man/uv)(1), [virtualenv](/man/virtualenv)(1)"]],"e":["uv","virtualenv"],"x":"uv venv"},"wakeonlan":{"n":"wakeonlan","t":"Send Wake-on-LAN magic packets","s":[["TAGLINE","Send Wake-on-LAN magic packets"],["TLDR","**Wake computer**\n\n```wakeonlan [00:11:22:33:44:55]```\n\n**Wake with broadcast address**\n\n```wakeonlan -i [192.168.1.255] [00:11:22:33:44:55]```\n\n**Wake on specific port**\n\n```wakeonlan -p [9] [00:11:22:33:44:55]```\n\n**Wake from file**\n\n```wakeonlan -f [hosts.txt]```\n\n**Verbose output**\n\n```wakeonlan -v [00:11:22:33:44:55]```"],["SYNOPSIS","**wakeonlan** [_-i address_] [_-p port_] [_-f file_] [_options_] _mac_"],["PARAMETERS","**-i** _ADDRESS_\n> Broadcast address.\n\n**-p** _PORT_\n> UDP port (default 9).\n\n**-f** _FILE_\n> Read MACs from file.\n\n**-v**\n> Verbose output."],["DESCRIPTION","**wakeonlan** sends Wake-on-LAN (WoL) magic packets to power on remote computers over the network. A magic packet is a specially formatted UDP broadcast containing the target machine's MAC address repeated 16 times, which the network interface card recognizes even while the system is powered off.\n\nThe tool sends packets to the network broadcast address on UDP port 9 by default, reaching sleeping hosts on the local subnet. A custom broadcast address can be specified for directed broadcasts across subnets, and an alternative port can be used for systems configured on port 7.\n\nMultiple machines can be woken at once by reading MAC addresses from a file with one address per line. The target computer must have WoL support enabled in its BIOS/UEFI settings and network interface driver, and must remain physically connected to the network."],["REQUIREMENTS","- Target NIC supports WoL\n- WoL enabled in BIOS/UEFI\n- WoL enabled in OS\n- Computer connected to network"],["CAVEATS","Only works on local network (unless forwarded). BIOS settings required. Some NICs need driver config."],["HISTORY","**Wake-on-LAN** was developed by AMD and HP in **1996**. wakeonlan provides command-line access to this power management feature."],["INSTALL","```pacman: sudo pacman -S wakeonlan```\n\n```apk: sudo apk add wakeonlan```\n\n```brew: brew install wakeonlan```\n\n```nix: nix profile install nixpkgs#wakeonlan```"],["SEE ALSO","[etherwake](/man/etherwake)(1), [ethtool](/man/ethtool)(8)"]],"e":["etherwake","ethtool"],"x":"wakeonlan [00:11:22:33:44:55]"},"wezterm-cli":{"n":"wezterm-cli","t":"WezTerm terminal control CLI","s":[["TAGLINE","WezTerm terminal control CLI"],["TLDR","**Start new terminal**\n\n```wezterm start```\n\n**Connect to SSH**\n\n```wezterm ssh [user@host]```\n\n**Connect to serial port**\n\n```wezterm serial [/dev/ttyUSB0]```\n\n**Spawn new tab**\n\n```wezterm cli spawn```\n\n**Split pane**\n\n```wezterm cli split-pane --right```\n\n**List panes**\n\n```wezterm cli list```"],["SYNOPSIS","**wezterm** [_command_] [_options_]"],["PARAMETERS","**start**\n> Start new terminal.\n\n**ssh** _destination_\n> SSH connection.\n\n**serial** _port_\n> Serial port connection.\n\n**cli spawn**\n> Create new tab.\n\n**cli split-pane**\n> Split current pane.\n\n**cli list**\n> List windows/tabs/panes.\n\n**--config** _key=value_\n> Override configuration."],["CONFIGURATION","**~/.wezterm.lua** or **~/.config/wezterm/wezterm.lua**\n> Lua-based configuration file for keybindings, fonts, colors, multiplexer settings, and SSH domains."],["DESCRIPTION","**wezterm** is a GPU-accelerated terminal emulator and multiplexer. It supports tabs, split panes, SSH and serial connections, and image display (sixel, iTerm2 protocol). Configuration is done via Lua scripts, enabling dynamic behavior and complex key mappings.\n\nThe `wezterm cli` subcommands control a running wezterm instance, allowing scripted pane management similar to tmux."],["CAVEATS","The `cli` subcommands require a running wezterm instance with the mux server enabled. Lua configuration errors prevent the terminal from starting; use `wezterm --config-file` to test alternate configs."],["INSTALL","```pacman: sudo pacman -S wezterm```\n\n```apk: sudo apk add wezterm-common```\n\n```zypper: sudo zypper install wezterm```\n\n```nix: nix profile install nixpkgs#wezterm```"],["SEE ALSO","[alacritty](/man/alacritty)(1), [kitty](/man/kitty)(1), [tmux](/man/tmux)(1)"]],"e":["alacritty","kitty","tmux"],"x":"wezterm start"},"zabbix_proxy":{"n":"zabbix_proxy","t":"Distributed Zabbix monitoring data proxy","s":[["TAGLINE","Distributed Zabbix monitoring data proxy"],["TLDR","**Start proxy daemon**\n\n```sudo systemctl start zabbix-proxy```\n\n**Run in foreground**\n\n```zabbix_proxy -f```\n\n**Use alternate config file**\n\n```zabbix_proxy -c [/etc/zabbix/zabbix_proxy.conf]```\n\n**Display runtime diagnostics**\n\n```zabbix_proxy -R diaginfo```\n\n**Reload configuration cache**\n\n```zabbix_proxy -R config_cache_reload```\n\n**Display version**\n\n```zabbix_proxy -V```"],["SYNOPSIS","**zabbix_proxy** [_options_]"],["PARAMETERS","**-c**, **--config** _file_\n> Use alternate configuration file.\n\n**-f**, **--foreground**\n> Run in foreground.\n\n**-R**, **--runtime-control** _option_\n> Runtime control command.\n\n**-h**, **--help**\n> Display help.\n\n**-V**, **--version**\n> Display version."],["RUNTIME CONTROL","**config_cache_reload**: Reload configuration cache.\n\n**housekeeper_execute**: Start housekeeper.\n\n**log_level_increase**: Increase log level.\n\n**log_level_decrease**: Decrease log level.\n\n**diaginfo**: Write diagnostic info to log."],["DESCRIPTION","**zabbix_proxy** is a daemon that collects monitoring data and forwards it to a Zabbix server. It's used for distributed monitoring of remote locations and reduces load on the central server.\n\nThe proxy collects data from agents, stores it locally in its database, then transmits to the server. This architecture survives temporary network outages and centralizes data collection for remote sites.\n\nProxies can operate in active mode (initiating connections to server) or passive mode (accepting connections from server). Each proxy requires its own database (SQLite, MySQL, or PostgreSQL)."],["CAVEATS","Requires separate database from server. Cannot share database with server. Configuration changes may require cache reload. Memory buffer recommended for performance in Zabbix 7.0+."],["HISTORY","**zabbix_proxy** was added to Zabbix to enable distributed monitoring architectures. It allows organizations to monitor remote sites without direct server connectivity and reduces WAN bandwidth usage by compressing and batching data transfers."],["INSTALL","```apt: sudo apt install zabbix-proxy-mysql```\n\n```dnf: sudo dnf install zabbix-proxy-mysql```\n\n```apk: sudo apk add zabbix-mysql```\n\n```zypper: sudo zypper install zabbix-proxy-mysql```"],["SEE ALSO","[zabbix_server](/man/zabbix_server)(8), [zabbix_agentd](/man/zabbix_agentd)(8), [zabbix_get](/man/zabbix_get)(1)"]],"e":["zabbix_server","zabbix_agentd","zabbix_get"],"x":"sudo systemctl start zabbix-proxy"},"zless":{"n":"zless","t":"View compressed files with less pager","s":[["TAGLINE","View compressed files with less pager"],["TLDR","**View a gzip compressed file**\n\n```zless [file.gz]```\n\n**View multiple compressed files sequentially**\n\n```zless [file1.gz] [file2.gz]```\n\n**Search for a pattern inside a compressed file**\n\n```zless [file.gz]```\n\nthen type **/**_pattern_ and press **Enter**\n\n**Jump to the last line of a compressed file**\n\n```zless [file.gz]```\n\nthen press **G**"],["SYNOPSIS","**zless** [_file ..._]"],["PARAMETERS","All parameters and commands available in **less** are also available in zless, including:\n\n**Space**\n> Scroll forward one screen.\n\n**b**\n> Scroll backward one screen.\n\n**/**_pattern_\n> Search forward for pattern.\n\n**?**_pattern_\n> Search backward for pattern.\n\n**n**\n> Repeat previous search.\n\n**N**\n> Repeat previous search in reverse direction.\n\n**g**\n> Go to first line.\n\n**G**\n> Go to last line.\n\n**q**\n> Quit.\n\n**h**\n> Display help."],["DESCRIPTION","**zless** is part of the **gzip** package. It works by setting `LESSOPEN` to pipe files through `gzip -cdfq` before passing them to **less**. This handles gzip (.gz) compressed files, as well as uncompressed files (gzip passes them through unchanged).\n\nThe command provides the same interactive viewing experience as less, including forward and backward navigation, searching, and line jumping. Files are decompressed on the fly without creating temporary files.\n\nFor broader format support (bzip2, xz, zstd, etc.), use **less** with **lesspipe** configured as the `LESSOPEN` preprocessor instead."],["CAVEATS","zless is a gzip-specific tool and does not natively support bzip2, xz, lzma, or other formats. For those, use **less** with **lesspipe** or pipe via the appropriate decompressor (e.g. `xz -dc file.xz | less`). For tar.gz archives, zless will only decompress the gzip layer and display the raw tar data; use **tar -tzf** to list contents instead. zless cannot read compressed data from standard input; files must be given as arguments."],["HISTORY","zless is part of the **gzip** package and was created as a companion to the zcat and zmore utilities. It combines the decompression capabilities of gzip with the advanced paging features of **less**, which became the preferred pager over **more** due to its ability to scroll backward and search. The command is included in most Unix-like systems as part of the standard gzip distribution."],["INSTALL","```apt: sudo apt install gzip```\n\n```dnf: sudo dnf install gzip```\n\n```pacman: sudo pacman -S gzip```\n\n```apk: sudo apk add gzip```\n\n```zypper: sudo zypper install gzip```\n\n```brew: brew install gzip```\n\n```nix: nix profile install nixpkgs#gzip```"],["SEE ALSO","[less](/man/less)(1), [zcat](/man/zcat)(1), [zmore](/man/zmore)(1), [zgrep](/man/zgrep)(1), [gzip](/man/gzip)(1)"]],"e":["less","zcat","zmore","zgrep","gzip"],"x":"zless [file.gz]"},"zoxide":{"n":"zoxide","t":"Smarter cd command that learns directory habits","s":[["TAGLINE","Smarter cd command that learns directory habits"],["TLDR","**Jump to highest ranked directory matching pattern**\n\n```z [pattern]```\n\n**Jump to directory matching multiple patterns**\n\n```z [foo] [bar]```\n\n**Interactive selection with fzf**\n\n```zi [pattern]```\n\n**Add a directory to the database**\n\n```zoxide add [/path/to/dir]```\n\n**Remove a directory from the database**\n\n```zoxide remove [/path/to/dir]```\n\n**List all entries**\n\n```zoxide query -l```\n\n**Show entry scores**\n\n```zoxide query -ls```\n\n**Generate shell initialization (add to shell profile)**\n\n```eval \"$(zoxide init [bash|zsh|fish])\"```"],["SYNOPSIS","**zoxide** _subcommand_ [_options_] [_arguments_]\n\n**z** [_pattern_...]\n\n**zi** [_pattern_...]"],["SUBCOMMANDS","**add** _path_\n> Add directory to database\n\n**edit**\n> Edit database interactively\n\n**import** _file_\n> Import from autojump/z database\n\n**init** _shell_\n> Generate shell initialization\n\n**query** [_pattern_...]\n> Search database\n\n**remove** _path_\n> Remove directory from database"],["PARAMETERS (query)","**-l**, **--list**\n> List matches instead of selecting best\n\n**-s**, **--score**\n> Show scores with results\n\n**-i**, **--interactive**\n> Interactive selection (requires fzf)\n\n**--exclude** _path_\n> Exclude path from results"],["DESCRIPTION","**zoxide** is a smarter cd command that learns your habits. It tracks directory visits and uses frecency (frequency + recency) to rank directories, allowing quick navigation with minimal typing.\n\nAfter shell integration, **z foo** jumps to the most likely directory containing \"foo\". Multiple patterns narrow results: **z foo bar** finds paths with both patterns in order.\n\nThe **zi** command opens fzf for interactive selection when multiple matches exist.\n\nShell initialization: add **eval \"$(zoxide init bash)\"** (or zsh, fish, etc.) to your shell profile.\n\nzoxide is a faster, more modern alternative to **autojump** and **z.sh**, written in Rust."],["CAVEATS","Requires shell initialization to track directories and provide z/zi commands.\n\nDatabase needs time to learn your patterns. New installations have limited effectiveness.\n\nInteractive mode requires fzf to be installed.\n\nDifferent from the **z** shell plugin but can import its database."],["INSTALL","```apt: sudo apt install zoxide```\n\n```dnf: sudo dnf install zoxide```\n\n```pacman: sudo pacman -S zoxide```\n\n```apk: sudo apk add zoxide```\n\n```zypper: sudo zypper install zoxide```\n\n```brew: brew install zoxide```\n\n```nix: nix profile install nixpkgs#zoxide```"],["SEE ALSO","[cd](/man/cd)(1), [z](/man/z)(1), [autojump](/man/autojump)(1), [fzf](/man/fzf)(1)"]],"e":["cd","z","autojump","fzf"],"x":"z [pattern]"}});
