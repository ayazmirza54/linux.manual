window.LCL_BUCKET(50,{"age":{"n":"age","t":"Simple and modern file encryption","s":[["TAGLINE","Simple and modern file encryption"],["TLDR","**Encrypt** a file with a passphrase\n\n```age -p [file.txt] > [file.txt.age]```\n\n**Decrypt** a file with passphrase\n\n```age -d [file.txt.age] > [file.txt]```\n\nEncrypt with **recipient's public key**\n\n```age -r [age1publickey...] [file.txt] > [file.txt.age]```\n\nEncrypt with **multiple recipients**\n\n```age -r [key1] -r [key2] [file.txt] > [file.txt.age]```\n\nEncrypt using **SSH public key**\n\n```age -R [~/.ssh/id_ed25519.pub] [file.txt] > [file.txt.age]```\n\nDecrypt with **identity file**\n\n```age -d -i [key.txt] [file.txt.age] > [file.txt]```"],["SYNOPSIS","**age** [_-e_|_-d_] [_-a_] [_-i identity_] [_-r recipient_] [_-R file_] [_-o output_] [_input_]"],["DESCRIPTION","**age** is a simple, modern file encryption tool with small explicit keys, no config options, and composable with Unix pipes. It's designed as a replacement for GPG with a cleaner interface and fewer footguns.\n\nage supports encryption to one or more recipients, passphrase encryption, and can use SSH keys for recipient-based encryption. The format is designed to be simple and auditable."],["PARAMETERS","**-e**, **--encrypt**\n> Encrypt input (default operation)\n\n**-d**, **--decrypt**\n> Decrypt input\n\n**-p**, **--passphrase**\n> Encrypt with passphrase\n\n**-r**, **--recipient** _recipient_\n> Encrypt to recipient public key (repeatable).\n\n**-R**, **--recipients-file** _file_\n> Read recipients from file, one per line (repeatable).\n\n**-i**, **--identity** _identity_\n> Identity (private key) file for decryption (repeatable). May also be used with **-e** for SSH or encrypted identities.\n\n**-o** _file_\n> Output file (default: stdout)\n\n**-a**, **--armor**\n> Use ASCII armored format"],["CAVEATS","age does not sign files; use age + signify or minisign for authenticated encryption. Keys are not interchangeable with GPG keys. No built-in key management; keys are just files."],["HISTORY","**age** was designed by Ben Cartwright-Cox and Filippo Valsorda and first released in **2019**. It was created to address the complexity and usability issues of GPG while providing a secure, simple encryption tool suitable for modern use cases."],["INSTALL","```apt: sudo apt install age```\n\n```dnf: sudo dnf install age```\n\n```pacman: sudo pacman -S age```\n\n```apk: sudo apk add age```\n\n```zypper: sudo zypper install age```\n\n```brew: brew install age```\n\n```nix: nix profile install nixpkgs#age```"],["SEE ALSO","[age-keygen](/man/age-keygen)(1), [gpg](/man/gpg)(1), [ssh-keygen](/man/ssh-keygen)(1)"],["RESOURCES","```[Source code](https://github.com/FiloSottile/age)```\n\n```[Homepage](https://age-encryption.org/)```"]],"e":["age-keygen","gpg","ssh-keygen"],"x":"age -p [file.txt] > [file.txt.age]"},"airodump-ng-oui-update":{"n":"airodump-ng-oui-update","t":"Update the IEEE OUI manufacturer database","s":[["TAGLINE","Update the IEEE OUI manufacturer database"],["TLDR","**Update** the OUI database\n\n```sudo airodump-ng-oui-update```"],["SYNOPSIS","**airodump-ng-oui-update**"],["DESCRIPTION","**airodump-ng-oui-update** downloads the latest IEEE OUI (Organizationally Unique Identifier) database file. This database maps MAC address prefixes to manufacturer names, allowing airodump-ng to display device manufacturers.\n\nThe tool fetches the database from IEEE's website and installs it in the aircrack-ng data directory, typically /usr/share/aircrack-ng/ or similar."],["CAVEATS","Requires internet connectivity and root privileges. The IEEE OUI database is updated periodically; run this command occasionally to get accurate manufacturer information for newer devices."],["HISTORY","**airodump-ng-oui-update** is a helper script included with the aircrack-ng suite, added to simplify keeping the OUI database current without manually downloading and placing files."],["INSTALL","```apt: sudo apt install aircrack-ng```\n\n```dnf: sudo dnf install aircrack-ng```\n\n```pacman: sudo pacman -S aircrack-ng```\n\n```apk: sudo apk add aircrack-ng```\n\n```zypper: sudo zypper install aircrack-ng```\n\n```brew: brew install aircrack-ng```\n\n```nix: nix profile install nixpkgs#aircrack-ng```"],["SEE ALSO","[airodump-ng](/man/airodump-ng)(1), [aircrack-ng](/man/aircrack-ng)(1)"],["RESOURCES","```[Source code](https://github.com/aircrack-ng/aircrack-ng)```\n\n```[Homepage](https://www.aircrack-ng.org/)```\n\n```[Documentation](https://www.aircrack-ng.org/documentation.html)```"]],"e":["airodump-ng","aircrack-ng"],"x":"sudo airodump-ng-oui-update"},"aws":{"n":"aws","t":"Unified command-line interface for Amazon Web Services","s":[["TAGLINE","Unified command-line interface for Amazon Web Services"],["TLDR","**List** S3 buckets\n\n```aws s3 ls```\n\n**Upload** file to S3\n\n```aws s3 cp [file.txt] s3://[bucket-name]/```\n\n**List** EC2 instances\n\n```aws ec2 describe-instances```\n\n**Get** caller identity\n\n```aws sts get-caller-identity```\n\n**Invoke** Lambda function\n\n```aws lambda invoke --function-name [MyFunction] [output.json]```\n\nUse **specific profile**\n\n```aws --profile [production] s3 ls```\n\n**Configure** AWS credentials\n\n```aws configure```\n\n**Filter output** with JMESPath query\n\n```aws ec2 describe-instances --query \"Reservations[*].Instances[*].[InstanceId,State.Name]\" --output table```"],["SYNOPSIS","**aws** [_options_] _service_ _command_ [_parameters_]"],["DESCRIPTION","**aws** is the unified command-line interface for Amazon Web Services. It provides direct access to AWS services including EC2, S3, Lambda, DynamoDB, and hundreds of others through a consistent interface.\n\nThe CLI enables automation, scripting, and management of AWS resources from the terminal without using the web console."],["PARAMETERS","**--profile** _name_\n> Use specific AWS credentials profile\n\n**--region** _region_\n> Override default region\n\n**--output** _format_\n> Output format (json, text, table, yaml)\n\n**--query** _query_\n> JMESPath query to filter output\n\n**--debug**\n> Enable debug logging\n\n**--no-cli-pager**\n> Disable output paging\n\n**--endpoint-url** _url_\n> Override service endpoint\n\n**--no-verify-ssl**\n> Disable SSL verification\n\n**--no-paginate**\n> Disable automatic pagination of results\n\n**--cli-binary-format** _format_\n> Control binary parameter encoding (raw-in-base64-out or raw-in-raw-out)\n\n**--version**\n> Display AWS CLI version"],["COMMON SERVICES","- **s3** - Object storage\n- **ec2** - Virtual servers\n- **lambda** - Serverless functions\n- **dynamodb** - NoSQL database\n- **iam** - Identity and access management\n- **cloudformation** - Infrastructure as code\n- **sts** - Security token service\n- **logs** - CloudWatch logs"],["CONFIGURATION","**~/.aws/credentials**\n> Access keys and secret keys for named profiles.\n\n**~/.aws/config**\n> Region, output format, and profile settings including role assumptions and SSO configuration."],["CAVEATS","Requires valid AWS credentials configured. API rate limits apply. Some operations are destructive and immediate. Large outputs may be truncated. Costs may apply for API calls. Use --dry-run when available."],["HISTORY","The **AWS CLI** was released by Amazon in **2013** to provide a unified interface replacing multiple service-specific tools. Version 2 was released in **2020** with improved installation and features."],["INSTALL","```dnf: sudo dnf install aws```\n\n```pacman: sudo pacman -S aws-cli```\n\n```apk: sudo apk add aws-cli```\n\n```zypper: sudo zypper install aws-cli```"],["SEE ALSO","[aws-configure](/man/aws-configure)(1), [aws-s3](/man/aws-s3)(1), [aws-vault](/man/aws-vault)(1), [terraform](/man/terraform)(1)"]],"e":["aws-configure","aws-s3","aws-vault","terraform"],"x":"aws s3 ls"},"az-acr":{"n":"az-acr","t":"Manage Azure Container Registries","s":[["TAGLINE","Manage Azure Container Registries"],["TLDR","**Create a container registry**\n\n```az acr create -n [registry-name] -g [resource-group] --sku [Standard]```\n\n**Log in** to a registry\n\n```az acr login -n [registry-name]```\n\n**List all registries** in the subscription\n\n```az acr list -o table```\n\n**Build and push an image**\n\n```az acr build -t [image:tag] -r [registry-name] [./source-directory]```\n\n**Import an image** from another registry\n\n```az acr import -n [registry-name] --source [source-registry.azurecr.io/repo:tag]```\n\n**List repositories** in a registry\n\n```az acr repository list -n [registry-name] -o table```\n\n**Show registry details**\n\n```az acr show -n [registry-name]```\n\n**Check registry health**\n\n```az acr check-health -n [registry-name]```"],["SYNOPSIS","**az acr** _subcommand_ [_options_]"],["DESCRIPTION","**az acr** manages Azure Container Registries (ACR), a private Docker registry service for storing and managing container images. ACR integrates with Azure Kubernetes Service, App Service, and other Azure services.\n\nThe command group supports registry creation, image management, authentication, geo-replication, automated builds with ACR Tasks, and advanced features like artifact streaming and supply chain security."],["SUBCOMMANDS","**Registry Management**\n> create, delete, update, show, list, show-usage, show-endpoints, check-name, check-health\n\n**Authentication**\n> login, credential show, credential renew, token create, token delete\n\n**Image Operations**\n> build, run, import, repository list, repository show, repository show-tags, repository delete, manifest list, manifest delete\n\n**Tasks**\n> task create, task run, task list, task show, task update, task logs\n\n**Replication**\n> replication create, replication delete, replication list, replication show\n\n**Advanced**\n> webhook create, webhook list, private-endpoint-connection, network-rule add, scope-map create, config retention update"],["PARAMETERS","**-n, --name** _value_\n> Name of the container registry\n\n**-g, --resource-group** _value_\n> Name of the resource group\n\n**--sku** _value_\n> Registry SKU: Basic, Standard, or Premium\n\n**--admin-enabled** _{true,false}_\n> Enable the admin user (username and password login). Disabled by default.\n\n**-t, --image** _value_\n> Image name and tag (name:tag format)\n\n**-r, --registry** _value_\n> Registry name for build operations"],["CAVEATS","Registry names must be globally unique and 5-50 characters. The **Premium** SKU is required for geo-replication and private endpoints. ACR Tasks pricing is based on compute time. Log in tokens expire after a period; use **az acr login** to refresh."],["HISTORY","Azure Container Registry launched in **2017** as a managed Docker registry service. It has expanded to support Helm charts, OCI artifacts, and integrated security scanning with Microsoft Defender for Cloud."],["INSTALL","```dnf: sudo dnf install azure-cli```\n\n```pacman: sudo pacman -S azure-cli```\n\n```zypper: sudo zypper install azure-cli```\n\n```brew: brew install azure-cli```\n\n```nix: nix profile install nixpkgs#azure-cli```"],["SEE ALSO","[az](/man/az)(1), [az-aks](/man/az-aks)(1), [docker](/man/docker)(1)"],["RESOURCES","```[Source code](https://github.com/Azure/azure-cli)```\n\n```[Documentation](https://learn.microsoft.com/en-us/cli/azure/acr)```"]],"e":["az","az-aks","docker"],"x":"az acr create -n [registry-name] -g [resource-group] --sku [Standard]"},"claude":{"n":"claude","t":"AI-powered agentic coding assistant CLI","s":[["TAGLINE","AI-powered agentic coding assistant CLI"],["TLDR","**Start an interactive session** in the current directory\n\n```claude```\n\n**Start with an initial prompt**\n\n```claude \"[prompt]\"```\n\n**Run a one-off command** and exit (non-interactive)\n\n```claude -p \"[prompt]\"```\n\n**Continue the most recent** conversation\n\n```claude -c```\n\n**Resume a specific session** by ID or name\n\n```claude -r \"[session_id]\" \"[prompt]\"```\n\n**Pipe input** to Claude\n\n```cat [file.txt] | claude -p \"[analyze this code]\"```\n\n**Output response as JSON**\n\n```claude -p \"[prompt]\" --output-format json```\n\n**Specify a different model**\n\n```claude --model [opus]```"],["SYNOPSIS","**claude** [_options_] [_prompt_]\n\n**claude** **update**\n\n**claude** **mcp** [_subcommand_]"],["PARAMETERS","**-p**, **--print**\n> Print mode: process prompt and exit without interactive session.\n\n**-c**, **--continue**\n> Continue the most recent conversation.\n\n**-r** _ID_, **--resume** _ID_\n> Resume a specific conversation by session ID or name.\n\n**-v**, **--version**\n> Display version information.\n\n**--model** _MODEL_\n> Specify the Claude model to use (aliases: sonnet, opus, haiku, or full model name).\n\n**--max-turns** _N_\n> Maximum number of agentic turns in non-interactive mode.\n\n**--max-budget-usd** _AMOUNT_\n> Maximum dollar amount for API calls (print mode only).\n\n**--output-format** _FORMAT_\n> Output format: text, json, or stream-json.\n\n**--input-format** _FORMAT_\n> Input format for print mode.\n\n**--tools** _TOOLS_\n> Restrict available built-in tools (e.g., \"Bash,Edit,Read\").\n\n**--allowedTools** _TOOLS_\n> Tools that execute without prompting.\n\n**--disallowedTools** _TOOLS_\n> Tools removed from context.\n\n**--permission-mode** _MODE_\n> Begin in specified permission mode (e.g., plan).\n\n**--system-prompt** _PROMPT_\n> Replace the entire default system prompt.\n\n**--append-system-prompt** _PROMPT_\n> Append additional instructions to the default prompt.\n\n**--add-dir** _PATHS_\n> Add additional working directories.\n\n**--mcp-config** _FILE_\n> Load MCP servers from JSON configuration file.\n\n**--chrome**\n> Enable Chrome browser integration.\n\n**--ide**\n> Auto-connect to IDE on startup.\n\n**--remote**\n> Create web session on claude.ai.\n\n**--teleport**\n> Resume web session in local terminal.\n\n**--verbose**\n> Enable verbose logging output.\n\n**--debug** _FILTER_\n> Enable debug mode with optional filtering.\n\n**--dangerously-skip-permissions**\n> Skip permission prompts (use with caution in trusted environments).\n\n**--help**\n> Display help information."],["SLASH COMMANDS","**/init**\n> Initialize and generate a CLAUDE.md file for the project.\n\n**/clear**\n> Reset conversation context.\n\n**/model**\n> Switch between available models.\n\n**/compact**\n> Manually compress conversation history.\n\n**/cost**\n> Display session cost information.\n\n**/help**\n> Display available commands and options."],["DESCRIPTION","**Claude Code** is Anthropic's official command-line interface for interacting with Claude AI. It provides an agentic coding assistant that can read, write, and execute code directly in your development environment.\n\nThe tool operates in two main modes: interactive mode for extended sessions with ongoing dialogue, and print mode (-p) for scripted automation and one-off commands. Claude can navigate codebases, edit files, run shell commands, and perform complex multi-step development tasks.\n\nClaude Code integrates with your terminal environment, understanding project context through file access and command execution. It maintains conversation history across sessions, allowing you to resume previous discussions. The tool supports piping input from other commands and outputting structured JSON for integration with other tools.\n\nSpecial syntax includes **@** for file references and **!** for direct shell command execution. The CLI supports MCP (Model Context Protocol) servers for extending capabilities and can connect to IDEs and Chrome for enhanced integration."],["CONFIGURATION","**CLAUDE.md**\n> Project-level instructions and context loaded automatically from the working directory.\n\n**~/.claude/settings.json**\n> Global user settings including permissions, allowed tools, and default behaviors.\n\n**.claude/settings.json**\n> Project-level settings for permissions and tool configuration."],["CAVEATS","Requires authentication via Anthropic API key or active Claude subscription. Commands executed by Claude run with your user permissions. The **--dangerously-skip-permissions** flag bypasses safety prompts and should only be used in controlled CI/CD environments. Session persistence and API costs apply based on usage."],["HISTORY","Claude Code was released by **Anthropic** in **February 2025** as part of their developer tools initiative. It represents a shift toward AI-assisted development workflows, building on Claude's code understanding capabilities to provide an agentic terminal experience. The tool continues active development with regular updates adding features like sub-agents, Chrome integration, and MCP support."],["INSTALL","```aur: yay -S claude```"],["SEE ALSO","[copilot](/man/copilot)(1), [gh](/man/gh)(1), [git](/man/git)(1)"]],"e":["copilot","gh","git"],"x":"claude"},"codecrafters":{"n":"codecrafters","t":"hands-on software engineering challenges platform CLI","s":[["TAGLINE","hands-on software engineering challenges platform CLI"],["TLDR","**Log in** to CodeCrafters\n\n```codecrafters login```\n\n**Run tests** for the current challenge stage\n\n```codecrafters test```\n\n**Submit your solution** and progress to the next stage\n\n```codecrafters submit```\n\n**Show current challenge status**\n\n```codecrafters status```\n\n**View the current challenge instructions**\n\n```codecrafters instructions```\n\n**Clone a challenge** repository to start\n\n```codecrafters clone [challenge-slug]```"],["SYNOPSIS","**codecrafters** _command_ [_options_]"],["PARAMETERS","**login**\n> Authenticate with your CodeCrafters account.\n\n**test**\n> Run tests for the current challenge stage locally.\n\n**submit**\n> Submit your solution to CodeCrafters for evaluation.\n\n**status**\n> Display the current progress and stage information.\n\n**instructions**\n> Show the instructions for the current challenge stage.\n\n**clone** _SLUG_\n> Clone a challenge repository to begin working on it.\n\n**logout**\n> Log out from your CodeCrafters account.\n\n**--help**\n> Display help information.\n\n**--version**\n> Display version information."],["DESCRIPTION","**codecrafters** is the command-line interface for the CodeCrafters platform, which provides challenges for building real-world software from scratch. Projects include building your own Redis, Git, Docker, SQLite, and other complex systems.\n\nThe CLI handles authentication, test execution, and solution submission. It integrates with the CodeCrafters platform to track progress through multi-stage challenges, where each stage introduces new requirements and complexity.\n\nChallenges are completed by implementing code that passes increasingly sophisticated tests. The local test command provides quick feedback during development, while submit validates against the full test suite on CodeCrafters' servers."],["CAVEATS","Requires an active CodeCrafters account. Some challenges require a paid subscription. The CLI needs network connectivity to submit solutions and verify progress. Challenge repositories must be cloned from CodeCrafters to include necessary test configurations."],["HISTORY","CodeCrafters was founded to provide hands-on learning experiences for experienced developers. The platform launched with challenges recreating popular software tools, allowing developers to deeply understand their internals. The CLI was developed to provide a seamless local development experience integrated with the platform."],["INSTALL","```aur: yay -S codecrafters```"],["SEE ALSO","[git](/man/git)(1), [exercism](/man/exercism)(1)"],["RESOURCES","```[Source code](https://github.com/codecrafters-io/cli)```\n\n```[Documentation](https://docs.codecrafters.io/)```"]],"e":["git","exercism"],"x":"codecrafters login"},"conda-clean":{"n":"conda-clean","t":"remove unused packages and cached files","s":[["TAGLINE","remove unused packages and cached files"],["TLDR","**Remove unused packages and caches**\n\n```conda clean --all```\n\n**Remove only package tarballs**\n\n```conda clean --tarballs```\n\n**Remove only unused packages**\n\n```conda clean --packages```\n\n**Remove index cache**\n\n```conda clean --index-cache```\n\n**Dry run** without removing\n\n```conda clean --all --dry-run```"],["SYNOPSIS","**conda** **clean** [_options_]"],["PARAMETERS","**-a**, **--all**\n> Remove all (index cache, lock files, tarballs, unused packages).\n\n**-t**, **--tarballs**\n> Remove cached package tarballs.\n\n**-p**, **--packages**\n> Remove unused packages.\n\n**-i**, **--index-cache**\n> Remove cached channel index files.\n\n**--dry-run**\n> Show what would be removed without removing."],["DESCRIPTION","**conda clean** reclaims disk space by removing various types of cached data that conda accumulates during package operations. Over time, conda's cache can grow to multiple gigabytes as it stores package tarballs, extracted packages, and index metadata for all downloaded packages.\n\nThe cache system exists to speed up package installation by reusing previously downloaded packages, but once packages are installed in environments, the cached copies are no longer strictly necessary. The --packages option removes extracted package files that aren't currently used by any environment, while --tarballs removes the compressed archives. The --index-cache option clears channel metadata that will be re-downloaded on the next conda operation.\n\nUsing --all is the most aggressive option, clearing all cache types and freeing the maximum amount of disk space. The --dry-run flag allows previewing what would be removed without actually deleting anything, which is useful for understanding how much space can be reclaimed. This command is safe to run as it never touches installed packages in actual environments, only the centralized package cache."],["INSTALL","```dnf: sudo dnf install conda```\n\n```nix: nix profile install nixpkgs#conda```"],["SEE ALSO","[conda](/man/conda)(1), [conda-remove](/man/conda-remove)(1)"],["RESOURCES","```[Source code](https://github.com/conda/conda)```\n\n```[Documentation](https://docs.conda.io/projects/conda/en/stable/commands/clean.html)```"]],"e":["conda","conda-remove"],"x":"conda clean --all"},"conda-doctor":{"n":"conda-doctor","t":"environment health checker","s":[["TAGLINE","environment health checker"],["TLDR","**Check environment health**\n\n```conda doctor```\n\n**Check a specific environment**\n\n```conda doctor --name [env_name]```"],["SYNOPSIS","**conda** **doctor** [_options_]"],["PARAMETERS","**-n**, **--name** _name_\n> Name of environment to check.\n\n**-p**, **--prefix** _path_\n> Path to environment to check."],["CONFIGURATION","**~/.condarc**\n> User-level conda configuration for channels, package settings, and solver options.\n\n**/opt/conda/.condarc**\n> System-wide conda configuration."],["DESCRIPTION","**conda doctor** runs diagnostic checks on a conda environment to detect potential issues such as corrupted packages, missing files, or inconsistent package states. It verifies environment integrity by checking package metadata, file checksums, and dependency consistency.\n\nThis command is particularly useful when environments behave unexpectedly, packages fail to import, or after manual modifications to environment directories. It can identify problems that may not be obvious from package listings alone."],["INSTALL","```dnf: sudo dnf install conda```\n\n```nix: nix profile install nixpkgs#conda```"],["SEE ALSO","[conda](/man/conda)(1), [conda-info](/man/conda-info)(1)"],["RESOURCES","```[Source code](https://github.com/conda/conda)```\n\n```[Documentation](https://docs.conda.io/projects/conda/en/stable/commands/doctor.html)```"]],"e":["conda","conda-info"],"x":"conda doctor"},"consul":{"n":"consul","t":"service discovery and configuration tool","s":[["TAGLINE","service discovery and configuration tool"],["TLDR","**Start agent in development mode**\n\n```consul agent -dev```\n\n**List datacenter members**\n\n```consul members```\n\n**Register service**\n\n```consul services register [service.json]```\n\n**Deregister service**\n\n```consul services deregister -id=[service-id]```\n\n**List services in catalog**\n\n```consul catalog services```\n\n**DNS lookup for service**\n\n```dig @127.0.0.1 -p 8600 [web.service.consul]```\n\n**Store key-value pair**\n\n```consul kv put [key] [value]```\n\n**Get key-value**\n\n```consul kv get [key]```\n\n**Join cluster**\n\n```consul join [192.168.1.100]```\n\n**Validate configuration files**\n\n```consul validate [/etc/consul.d/]```\n\n**Reload agent configuration**\n\n```consul reload```"],["SYNOPSIS","**consul** [_options_] _command_ [_arguments_]"],["DESCRIPTION","**consul** is HashiCorp's service discovery and configuration tool. It provides service mesh, health checking, key/value storage, multi-datacenter support, and secure service-to-service communication."],["COMMANDS","**agent**\n> Run a Consul agent\n\n**members**\n> List cluster members\n\n**join**\n> Join cluster via agent address\n\n**leave**\n> Gracefully leave cluster\n\n**catalog**\n> Interact with service catalog\n\n**services**\n> Register/deregister services\n\n**kv**\n> Key/value store operations\n\n**connect**\n> Service mesh functionality\n\n**acl**\n> Access control list management\n\n**watch**\n> Watch for changes in data views and invoke a process\n\n**event**\n> Fire a custom user event\n\n**exec**\n> Remote execution on cluster nodes\n\n**operator**\n> Cluster-level operator tools\n\n**snapshot**\n> Save/restore cluster state\n\n**reload**\n> Reload agent configuration files\n\n**validate**\n> Validate configuration files\n\n**monitor**\n> Stream logs from a running agent\n\n**debug**\n> Create debugging archive"],["PARAMETERS","**-dev**\n> Development mode (single node, no persistence)\n\n**-server**\n> Run agent in server mode\n\n**-bind** _addr_\n> Address for internal cluster communications (default: 0.0.0.0)\n\n**-token** _token_\n> ACL token for authentication\n\n**-token-file** _file_\n> File containing the ACL token\n\n**-datacenter** _dc_\n> Target datacenter\n\n**-http-addr** _addr_\n> Consul HTTP API address\n\n**-auto-reload-config**\n> Automatically reload configuration when files change"],["CONFIGURATION","**/etc/consul.d/**\n> Configuration directory for Consul agent settings, services, and checks in HCL or JSON format."],["ENVIRONMENT","**CONSUL_HTTP_ADDR**\n> HTTP API address\n\n**CONSUL_HTTP_TOKEN**\n> ACL token\n\n**CONSUL_CACERT**\n> CA certificate path\n\n**CONSUL_HTTP_SSL**\n> Use HTTPS when communicating with Consul"],["DNS INTERFACE","Default DNS port: 8600\n\n```dig @127.0.0.1 -p 8600 web.service.consul```"],["CAVEATS","ACL system requires token for most operations when enabled. Development mode stores everything in memory. Production requires proper clustering configuration."],["INSTALL","```pacman: sudo pacman -S consul```\n\n```nix: nix profile install nixpkgs#consul```"],["SEE ALSO","[vault](/man/vault)(1), [nomad](/man/nomad)(1), [terraform](/man/terraform)(1)"]],"e":["vault","nomad","terraform"],"x":"consul agent -dev"},"cradle-install":{"n":"cradle-install","t":"Install the Cradle PHP framework and its packages","s":[["TAGLINE","Install the Cradle PHP framework and its packages"],["TLDR","**Run the interactive installer with defaults**\n\n```cradle install```\n\n**Force install, overwriting existing files**\n\n```cradle install -f```\n\n**Install without running SQL setup**\n\n```cradle install --skip-sql```\n\n**Install without updating package versions**\n\n```cradle install --skip-versioning```\n\n**Install without creating cache directories**\n\n```cradle install --skip-mkdir```\n\n**Install without adjusting file permissions**\n\n```cradle install --skip-chmod```\n\n**Supply database credentials on the command line**\n\n```cradle install -h [127.0.0.1] -u [root] -p [password]```"],["SYNOPSIS","**cradle** **install** [_options_]"],["PARAMETERS","**-f**, **--force**\n> Force installation, overwriting any previously installed files.\n\n**--skip-sql**\n> Skip the SQL setup step (databases, seed data, schema import).\n\n**--skip-versioning**\n> Skip updating package version metadata.\n\n**--skip-mkdir**\n> Skip creation of cache, upload, and log directories.\n\n**--skip-chmod**\n> Skip setting filesystem permissions on generated directories.\n\n**-h** _HOST_\n> Database host used when SQL setup is enabled.\n\n**-u** _USER_\n> Database username.\n\n**-p** _PASSWORD_\n> Database password."],["DESCRIPTION","**cradle install** is the bootstrap subcommand of the **Cradle** PHP framework CLI. It walks through the first-run setup: importing SQL schemas, writing configuration under `config/`, creating cache and upload directories, applying file permissions, and registering active packages with the framework.\n\nThe command is idempotent when **-f** is not given; existing configuration and files are preserved, and individual phases can be turned off with the various `--skip-*` flags for use in Docker images, CI, or partial redeploys."],["CAVEATS","Must be run from the root of a Cradle project. Credentials passed via **-p** on the command line are visible in the process list; prefer environment variables or a prepared `config/settings.php` when security matters."],["HISTORY","**cradle install** is part of the **CradlePHP** project (github.com/CradlePHP). It started as an experimental admin package manager and was merged into the core `cradle` CLI as the standard installer."],["SEE ALSO","[cradle](/man/cradle)(1), [composer](/man/composer)(1), [php](/man/php)(1)"]],"e":["cradle","composer","php"],"x":"cradle install"},"dhcpd":{"n":"dhcpd","t":"ISC DHCP server daemon for IP address assignment","s":[["TAGLINE","ISC DHCP server daemon for IP address assignment"],["TLDR","**Start DHCP server**\n\n```sudo dhcpd```\n\n**Start on specific interface**\n\n```sudo dhcpd [eth0]```\n\n**Start in foreground** (debug)\n\n```sudo dhcpd -f -d```\n\n**Test configuration file**\n\n```sudo dhcpd -t```\n\n**Use alternate config file**\n\n```sudo dhcpd -cf [/path/to/dhcpd.conf]```\n\n**Use alternate lease file**\n\n```sudo dhcpd -lf [/path/to/dhcpd.leases]```"],["SYNOPSIS","**dhcpd** [_options_] [_interfaces_]"],["PARAMETERS","**-f**\n> Run in foreground.\n\n**-d**\n> Log to stderr (implies -f).\n\n**-t**\n> Test configuration and exit.\n\n**-T**\n> Test lease file and exit.\n\n**-cf** _file_\n> Use alternate configuration file.\n\n**-lf** _file_\n> Use alternate lease database.\n\n**-pf** _file_\n> Use alternate PID file.\n\n**-4** / **-6**\n> Force IPv4 or IPv6 mode.\n\n**-p** _port_\n> Listen on alternate UDP port (default 67).\n\n**-s** _address_\n> Send replies to specified address instead of broadcast.\n\n**-q**\n> Quiet startup (suppress copyright message).\n\n**-user** _user_\n> Setuid to user after privileged operations.\n\n**-group** _group_\n> Setgid to group after privileged operations.\n\n**-chroot** _dir_\n> Chroot to directory.\n\n**-tf** _tracefile_\n> Log startup state and transactions for debugging.\n\n**--no-pid**\n> Don't write PID file.\n\n**--version**\n> Display version and exit."],["CONFIGURATION","**/etc/dhcp/dhcpd.conf**:\n```\nsubnet 192.168.1.0 netmask 255.255.255.0 {\n  range 192.168.1.100 192.168.1.200;\n  option routers 192.168.1.1;\n  option domain-name-servers 8.8.8.8, 8.8.4.4;\n  default-lease-time 600;\n  max-lease-time 7200;\n}\n\nhost server1 {\n  hardware ethernet 00:11:22:33:44:55;\n  fixed-address 192.168.1.10;\n}\n```"],["DESCRIPTION","**dhcpd** is the Internet Systems Consortium (ISC) DHCP server daemon. It assigns IP addresses and network configuration to clients on local networks using the Dynamic Host Configuration Protocol.\n\nThe server maintains a lease database tracking which addresses are assigned to which clients. Configuration defines address pools, lease durations, and options like gateway, DNS servers, and domain names.\n\nStatic assignments can reserve specific addresses for known MAC addresses. The server can also provide PXE boot options, custom options for specific vendors, and failover configurations for high availability."],["CAVEATS","Requires root privileges and must bind to port 67. Lease file must be writable. Multiple DHCP servers on the same network cause conflicts unless configured for failover. IPv6 support (dhcpd -6) uses different configuration syntax."],["HISTORY","ISC DHCP was originally developed by **Ted Lemon** at the Internet Systems Consortium, with first release in **1997**. It became the reference DHCP implementation for Unix systems. ISC announced end-of-life for ISC DHCP in **2022**, recommending migration to **Kea**, the next-generation ISC DHCP server."],["INSTALL","```apt: sudo apt install isc-dhcp-server```"],["SEE ALSO","[dhclient](/man/dhclient)(8)"]],"e":["dhclient"],"x":"sudo dhcpd"},"distrobox-upgrade":{"n":"distrobox-upgrade","t":"upgrade Distrobox containers using native package managers","s":[["TAGLINE","upgrade Distrobox containers using native package managers"],["TLDR","**Upgrade** a container\n\n```distrobox-upgrade [container_name]```\n\nUpgrade **all** containers\n\n```distrobox-upgrade -a```\n\nUpgrade **specific** containers\n\n```distrobox-upgrade [container1] [container2]```\n\nRun upgrade in the background with **root** privileges\n\n```distrobox-upgrade --root [container_name]```"],["SYNOPSIS","**distrobox-upgrade** [_options_] [_containers_...]"],["DESCRIPTION","**distrobox-upgrade** upgrades one or more Distrobox containers using each container's native package manager. It runs the appropriate update commands (apt, dnf, pacman, etc.) based on the container's distribution."],["PARAMETERS","**-a**, **--all**\n> Upgrade all containers.\n\n**--root**\n> Launch podman/docker/lilipod with root privileges. Useful for containers created with --root.\n\n**-v**, **--verbose**\n> Show more verbosity.\n\n_containers_\n> Space-separated list of container names to upgrade."],["CAVEATS","Each container is upgraded using its own package manager. Large upgrades may take significant time and disk space."],["INSTALL","```dnf: sudo dnf install distrobox```\n\n```pacman: sudo pacman -S distrobox```\n\n```apk: sudo apk add distrobox```\n\n```zypper: sudo zypper install distrobox```\n\n```brew: brew install distrobox```\n\n```nix: nix profile install nixpkgs#distrobox```"],["SEE ALSO","[distrobox](/man/distrobox)(1), [distrobox-enter](/man/distrobox-enter)(1), [distrobox-create](/man/distrobox-create)(1), [distrobox-list](/man/distrobox-list)(1)"]],"e":["distrobox","distrobox-enter","distrobox-create","distrobox-list"],"x":"distrobox-upgrade [container_name]"},"echo":{"n":"echo","t":"display text output","s":[["TAGLINE","display text output"],["TLDR","**Print text**\n\n```echo \"[Hello World]\"```\n\n**Print without trailing newline**\n\n```echo -n \"[text]\"```\n\n**Interpret escape sequences**\n\n```echo -e \"[Line 1\\nLine 2]\"```\n\n**Print an environment variable**\n\n```echo $[PATH]```\n\n**Write text to a file**\n\n```echo \"[content]\" > [file.txt]```\n\n**Append text to a file**\n\n```echo \"[more content]\" >> [file.txt]```"],["SYNOPSIS","**echo** [_options_] [_string_...]"],["DESCRIPTION","**echo** displays a line of text to standard output. It's one of the most basic and frequently used commands, essential for scripts, logging, and displaying information to users.\n\nThe command exists as both a shell built-in and standalone program."],["PARAMETERS","**-n**\n> Don't output trailing newline\n\n**-e**\n> Enable interpretation of backslash escapes\n\n**-E**\n> Disable interpretation of backslash escapes (default)"],["ESCAPE SEQUENCES","(With **-e** flag):\n\n**\\n** - Newline\n**\\t** - Tab\n**\\r** - Carriage return\n**\\a** - Alert (bell)\n**\\b** - Backspace\n**\\\\\\\\** - Backslash\n**\\\\\"** - Double quote\n**\\xHH** - Hexadecimal byte\n**\\0NNN** - Octal byte"],["CAVEATS","Behavior differs between shells and between the shell built-in and `/bin/echo`. The **-e** flag is not POSIX and is not supported by all implementations. For portable scripts, prefer **printf** which has consistent behavior across platforms. Most shells (bash, zsh, dash) provide echo as a built-in that may behave differently from the external binary."],["HISTORY","**echo** has been part of Unix since Version 2 in **1972**, though its behavior has varied across different Unix variants."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[printf](/man/printf)(1), [cat](/man/cat)(1), [tee](/man/tee)(1)"]],"e":["printf","cat","tee"],"x":"echo \"[Hello World]\""},"eopkg":{"n":"eopkg","t":"Package manager for Solus Linux","s":[["TAGLINE","Package manager for Solus Linux"],["TLDR","**Refresh and upgrade** every installed package\n\n```sudo eopkg upgrade```\n\n**Install** one or more packages\n\n```sudo eopkg install [package]```\n\n**Search** the repository index\n\n```sudo eopkg search [search_term]```"],["SYNOPSIS","**eopkg** _command_ [_options_] [_packages_]"],["DESCRIPTION","**eopkg** is the package manager for Solus Linux, an independent distribution. It handles all aspects of package management including installation, updates, removal, and repository management using the .eopkg package format.\n\nThe tool evolved from the PiSi package manager originally developed for Pardus Linux, with significant improvements and optimizations for Solus. eopkg provides dependency resolution, package rollback capabilities, and maintains a local database of installed packages. It integrates with Solus's rolling release model to provide continuous updates while maintaining system stability."],["PARAMETERS","**install** _packages_\n> Install packages (short alias: **it**)\n\n**upgrade**\n> Update all packages (short alias: **up**)\n\n**search** _term_\n> Search for packages (short alias: **sr**)\n\n**remove** _packages_\n> Remove packages (short alias: **rm**)\n\n**info** _package_\n> Show package information\n\n**list-installed**\n> List installed packages (short alias: **li**)\n\n**list-available**\n> List packages available in the repositories (short alias: **la**)\n\n**update-repo**\n> Refresh the local copy of the repository index (short alias: **ur**)\n\n**history**\n> Show the transaction history\n\n**history -t** _number_\n> Roll the system back to a previous transaction\n\n**check** _package_\n> Verify the integrity of installed files"],["CAVEATS","Solus Linux specific. Not compatible with other distributions. Requires root privileges for most operations. Refresh the index with `eopkg update-repo` before installing or upgrading if it may be stale."],["SEE ALSO","[apt](/man/apt)(8), [dnf](/man/dnf)(8), [pacman](/man/pacman)(8)"],["RESOURCES","```[Source code](https://github.com/getsolus/eopkg)```\n\n```[Homepage](https://getsol.us/)```\n\n```[Documentation](https://help.getsol.us/docs/user/package-management/basics)```"]],"e":["apt","dnf","pacman"],"x":"sudo eopkg upgrade"},"gcloud-projects":{"n":"gcloud-projects","t":"Google Cloud project management","s":[["TAGLINE","Google Cloud project management"],["TLDR","**List projects**\n\n```gcloud projects list```\n\n**Create a project**\n\n```gcloud projects create [project_id]```\n\n**Describe a project**\n\n```gcloud projects describe [project_id]```\n\n**Delete a project**\n\n```gcloud projects delete [project_id]```\n\n**Set IAM policy**\n\n```gcloud projects set-iam-policy [project_id] [policy.json]```"],["SYNOPSIS","**gcloud** **projects** _command_ [_options_]"],["SUBCOMMANDS","**list**\n> List projects the active account has permission to view.\n\n**create** _PROJECT_ID_ [**--name=**_NAME_] [**--organization=**_ORG_] [**--folder=**_FOLDER_]\n> Create a new project. ID must be 6–30 characters, lowercase letters, digits, or hyphens.\n\n**describe** _PROJECT_ID_\n> Show metadata (name, number, lifecycle state, labels) for a project.\n\n**delete** _PROJECT_ID_\n> Mark a project for deletion (it is soft-deleted for 30 days before permanent removal).\n\n**get-iam-policy** _PROJECT_ID_\n> Print the IAM policy attached to the project.\n\n**set-iam-policy** _PROJECT_ID_ _POLICY_FILE_\n> Replace the IAM policy with the one defined in the JSON or YAML file.\n\n**add-iam-policy-binding** _PROJECT_ID_ **--member=**_MEMBER_ **--role=**_ROLE_\n> Add a single member/role binding to the project's IAM policy.\n\n**remove-iam-policy-binding** _PROJECT_ID_ **--member=**_MEMBER_ **--role=**_ROLE_\n> Remove a member/role binding from the project's IAM policy.\n\n**update** _PROJECT_ID_\n> Update project metadata such as display name or labels.\n\n**move** _PROJECT_ID_ **--organization=**_ORG_ | **--folder=**_FOLDER_\n> Move a project to a different organization or folder.\n\n**undelete** _PROJECT_ID_\n> Restore a soft-deleted project within the 30-day grace period."],["DESCRIPTION","**gcloud projects** provides commands for managing Google Cloud projects, which serve as the fundamental organizational unit in GCP. Every resource in Google Cloud belongs to exactly one project, and projects provide the basis for billing, access control, and resource isolation.\n\nThe command group supports the full project lifecycle including creation, deletion, and restoration of soft-deleted projects. It also handles IAM policy management at the project level, controlling which principals have access to project resources.\n\nProjects contain settings like project ID (immutable), project name (changeable), and project number (assigned by Google). Understanding project management is essential for organizing resources and managing costs in Google Cloud, as billing is tracked per project."],["SEE ALSO","[gcloud](/man/gcloud)(1), [gcloud-config](/man/gcloud-config)(1)"]],"e":["gcloud","gcloud-config"],"x":"gcloud projects list"},"ghq":{"n":"ghq","t":"Manage remote repository clones in a structured directory hierarchy","s":[["TAGLINE","Manage remote repository clones in a structured directory hierarchy"],["TLDR","**Clone a repository**\n\n```ghq get [https://github.com/owner/repo]```\n\n**Clone a repository via SSH**\n\n```ghq get -p [owner/repo]```\n\n**List managed repositories**\n\n```ghq list```\n\n**List with full paths**\n\n```ghq list -p```\n\n**Get repository root**\n\n```ghq root```\n\n**Create new repository**\n\n```ghq create [owner]/[repo]```\n\n**Remove a local repository**\n\n```ghq rm [owner/repo]```\n\n**Update an already cloned repository**\n\n```ghq get -u [owner/repo]```"],["SYNOPSIS","**ghq** _command_ [_options_] [_arguments_]"],["PARAMETERS","**get** _URL_\n> Clone a repository. Alias: clone.\n\n**list** [_query_]\n> List local repositories, optionally filtered by query.\n\n**root**\n> Show repository root path.\n\n**create** _NAME_\n> Create a new repository locally.\n\n**rm** _NAME_\n> Remove a local repository clone.\n\n**-p**\n> Clone via SSH (get) or show full paths (list).\n\n**-u**, **--update**\n> Update existing repository (pull --ff-only).\n\n**--shallow**\n> Shallow clone (depth 1, Git only).\n\n**--branch** _BRANCH_\n> Clone a specific branch.\n\n**--bare**\n> Perform a bare clone (Git only).\n\n**--no-recursive**\n> Do not clone git submodules.\n\n**-e**, **--exact**\n> Require exact query match (list).\n\n**--dry-run**\n> Show path without deleting (rm).\n\n**--vcs** _VCS_\n> Specify VCS backend: git, svn, hg, darcs, fossil, bzr.\n\n**--help**\n> Display help information."],["DESCRIPTION","**ghq** manages remote repository clones in a structured directory hierarchy. It clones repositories into organized paths based on their URLs, enabling consistent repository organization across projects.\n\nThe tool integrates with shell workflows and fuzzy finders like **fzf** for quick repository navigation. It supports GitHub, GitLab, Bitbucket, and custom Git hosts."],["CONFIGURATION","**~/.gitconfig**\n> Configure repository root path with `ghq.root` setting, default user with `ghq.user`, and per-URL VCS with `ghq.<url>.vcs`.\n\n**GHQ_ROOT**\n> Environment variable to override all ghq.root settings."],["CAVEATS","Requires git. Default root is ~/ghq or configurable via ghq.root in gitconfig or GHQ_ROOT env var. URL parsing determines directory structure. Supports multiple VCS backends beyond Git."],["HISTORY","ghq was created by **motemen** as a tool for organizing Git repositories, inspired by go get's directory structure conventions."],["INSTALL","```pacman: sudo pacman -S ghq```\n\n```brew: brew install ghq```\n\n```nix: nix profile install nixpkgs#ghq```"],["SEE ALSO","[git](/man/git)(1), [fzf](/man/fzf)(1), [gh](/man/gh)(1)"]],"e":["git","fzf","gh"],"x":"ghq get [https://github.com/owner/repo]"},"git-restore":{"n":"git-restore","t":"Restore working tree files or unstage changes","s":[["TAGLINE","Restore working tree files or unstage changes"],["TLDR","**Restore working tree file**\n\n```git restore [file]```\n\n**Restore from specific commit**\n\n```git restore --source=[commit] [file]```\n\n**Unstage file**\n\n```git restore --staged [file]```\n\n**Restore all files**\n\n```git restore .```\n\n**Restore both staged and working tree**\n\n```git restore --staged --worktree [file]```\n\n**Interactive restore**\n\n```git restore -p [file]```\n\n**Restore your side of a conflicted merge**\n\n```git restore --ours [file]```\n\n**Restore the incoming side of a conflicted merge**\n\n```git restore --theirs [file]```"],["SYNOPSIS","**git** **restore** [_options_] [_files_...]"],["PARAMETERS","**--source** _tree_\n> Restore from tree.\n\n**-s** _tree_\n> Short for --source.\n\n**--staged**\n> Restore index (unstage).\n\n**--worktree**\n> Restore working tree.\n\n**-S**\n> Short for --staged.\n\n**-W**\n> Short for --worktree.\n\n**-p**, **--patch**\n> Interactive mode.\n\n**--ours** / **--theirs**\n> During a conflicted merge, restore the current branch's or the merged-in branch's version.\n\n**-m**, **--merge**\n> Recreate the conflicted merge state in the file."],["DESCRIPTION","**git restore** restores working tree files or unstages changes from the index. Introduced in Git 2.23 to provide a clearer alternative to the file-restoration aspects of `git checkout`.\n\nUse `--staged` to unstage files, `--worktree` (the default) to discard working tree changes, or both together to restore a file completely to a previous state."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-checkout](/man/git-checkout)(1), [git-reset](/man/git-reset)(1)"],["RESOURCES","```[Source code](https://github.com/git/git)```\n\n```[Homepage](https://git-scm.com/)```\n\n```[Documentation](https://git-scm.com/docs/git-restore)```"]],"e":["git-checkout","git-reset"],"x":"git restore [file]"},"gitlab-ctl":{"n":"gitlab-ctl","t":"Control Omnibus GitLab services","s":[["TAGLINE","Control Omnibus GitLab services"],["TLDR","**Start GitLab**\n\n```sudo gitlab-ctl start```\n\n**Stop GitLab**\n\n```sudo gitlab-ctl stop```\n\n**Restart GitLab**\n\n```sudo gitlab-ctl restart```\n\n**Show status**\n\n```sudo gitlab-ctl status```\n\n**Reconfigure GitLab**\n\n```sudo gitlab-ctl reconfigure```\n\n**View logs**\n\n```sudo gitlab-ctl tail```\n\n**View specific service logs**\n\n```sudo gitlab-ctl tail [nginx]```"],["SYNOPSIS","**gitlab-ctl** _command_ [_options_]"],["SUBCOMMANDS","**start**\n> Start all services.\n\n**stop**\n> Stop all services.\n\n**restart**\n> Restart all services.\n\n**status**\n> Show service status.\n\n**reconfigure**\n> Apply configuration.\n\n**tail**\n> View logs.\n\n**hup**\n> Send HUP signal.\n\n**console**\n> Rails console."],["DESCRIPTION","**gitlab-ctl** controls an Omnibus GitLab installation. It manages all GitLab services (nginx, PostgreSQL, Redis, Puma, Sidekiq, etc.), applies configuration changes from `/etc/gitlab/gitlab.rb`, and provides debugging tools.\n\nThe `reconfigure` command is used after modifying `gitlab.rb` to apply the new settings."],["CONFIGURATION","**/etc/gitlab/gitlab.rb**\n> Main configuration file for the Omnibus GitLab installation, controlling all services and settings."],["SEE ALSO","[gitlab-backup](/man/gitlab-backup)(1)"]],"e":["gitlab-backup"],"x":"sudo gitlab-ctl start"},"gnome-calculator":{"n":"gnome-calculator","t":"GNOME desktop calculator application","s":[["TAGLINE","GNOME desktop calculator application"],["TLDR","Launch the **GNOME Calculator GUI**\n\n```gnome-calculator```\n\n**Solve** an equation without launching the GUI\n\n```gnome-calculator --solve \"[2^5 * 2 + 5]\"```\n\nLaunch in **programming mode**\n\n```gnome-calculator --mode programming```\n\nLaunch in **financial mode**\n\n```gnome-calculator --mode financial```\n\nDisplay **version**\n\n```gnome-calculator --version```"],["SYNOPSIS","**gnome-calculator** [_options_]"],["PARAMETERS","**-s**, **--solve** _EQUATION_\n> Solve equation and print result without launching GUI.\n\n**-e** _EQUATION_\n> Equivalent to --solve.\n\n**-m**, **--mode** _MODE_\n> Start in specific mode: basic, advanced, financial, programming.\n\n**--version**\n> Display version information.\n\n**--help**\n> Display help message."],["DESCRIPTION","**gnome-calculator** is the official calculator application for the GNOME desktop environment. It provides multiple modes: Basic for simple arithmetic, Advanced for scientific functions and trigonometry, Financial for interest and currency conversion, and Programming for bitwise operations and base conversion.\n\nThe calculator supports variables, unit conversions, and complex mathematical expressions. The **--solve** option allows using it as a command-line calculator without opening the GUI window."],["CAVEATS","Requires GTK and GLib libraries. Some advanced features may require additional libraries. Currency conversion requires network access for current rates."],["HISTORY","gnome-calculator evolved from the original gcalctool, which was part of GNOME since its early versions. The modern gnome-calculator was rewritten in Vala and introduced with GNOME 3.0 in **2011**, providing a cleaner interface and improved functionality."],["INSTALL","```apt: sudo apt install gnome-calculator```\n\n```dnf: sudo dnf install gnome-calculator```\n\n```pacman: sudo pacman -S gnome-calculator```\n\n```apk: sudo apk add gnome-calculator```\n\n```zypper: sudo zypper install gnome-calculator```\n\n```nix: nix profile install nixpkgs#gnome-calculator```"],["SEE ALSO","[bc](/man/bc)(1), [dc](/man/dc)(1), [qalc](/man/qalc)(1), [expr](/man/expr)(1)"]],"e":["bc","dc","qalc","expr"],"x":"gnome-calculator"},"grepathy":{"n":"grepathy","t":"Distill AI coding-agent decisions into commit-ready why files","s":[["TAGLINE","Distill AI coding-agent decisions into commit-ready why files"],["TLDR","**Initialize** hooks and `.ai/why` in a repository\n\n```grepathy init```\n\n**Show status** of distillation and health\n\n```grepathy status```\n\n**Run health checks** (doctor)\n\n```grepathy doctor```\n\n**Show why entries** that apply to a path\n\n```grepathy context [path]```\n\n**Distill and commit** why notes now (never pushes)\n\n```grepathy sync```\n\n**Disable** grepathy hooks temporarily\n\n```grepathy off```\n\n**Re-enable** grepathy hooks\n\n```grepathy on```\n\n**Remove** grepathy from the project\n\n```grepathy uninstall```"],["SYNOPSIS","**grepathy** _command_ [_options_]"],["DESCRIPTION","**grepathy** records the *why* behind agent-written code so humans and future agents can review decisions without digging through chat transcripts. It reads coding-agent session transcripts **locally**, extracts decisions, and writes a per-branch markdown file under **.ai/why/** that you can commit with the code.\n\nIt is designed to run via git/Claude Code hooks rather than mid-task agent logging: after work on a branch, grepathy distills a why-pack, shows it for human review, and respects permanent edits or deletions of entries. It does not block pushes, does not stage unrelated files, and never pushes for you. A pointer in project agent docs and a pre-edit hook can inject relevant why entries when an agent touches matching paths.\n\nPrivacy-oriented: transcripts stay on the machine; only the sanitized markdown summary is meant for the repo. Summaries avoid quoting user messages, strip secrets, and pass deterministic scanners before you review.\n\nWriting why files currently targets Claude Code session data; anything can read the committed markdown. Requires Node.js 20+ for install and use via **npx grepathy** or a local build."],["PARAMETERS","**init**\n> Install hooks and directories. Safe to re-run. May offer to backfill existing sessions.\n\n**status**\n> Report distillation state and what looks stale.\n\n**doctor**\n> Health checks for installation and configuration.\n\n**context** _path_\n> Print why-pack entries that apply to the given file or path.\n\n**sync**\n> Distill and commit why content immediately (still never pushes).\n\n**distill**\n> Run distillation without the full sync flow (see **--help**).\n\n**repair**\n> Repair grepathy state or hooks.\n\n**off** / **on**\n> Temporarily disable or re-enable hooks.\n\n**uninstall**\n> Remove grepathy integration from the repository."],["CAVEATS","Does not stop agents from refactoring away important code; it only captures reasoning that is not visible in the source. Write-side support is Claude Code-oriented today. Why files are only as good as the summarizer and your review before commit. Empty or expired local transcripts cannot be recovered after the agent deletes them."],["SEE ALSO","[git](/man/git)(1), [claude](/man/claude)(1)"],["RESOURCES","```[Source code](https://github.com/evansjp/grepathy)```"]],"e":["git","claude"],"x":"grepathy init"},"hledger-import":{"n":"hledger-import","t":"adds new transactions from CSV or other data files to a journal","s":[["TAGLINE","adds new transactions from CSV or other data files to a journal"],["TLDR","**Import transactions from one or more CSV files**\n\n```hledger import [bank.csv]```\n\n**Preview what would be imported, without changing the journal**\n\n```hledger import --dry-run [bank.csv]```\n\n**Import into a specific journal file**\n\n```hledger import -f [ledger.journal] [bank.csv]```\n\n**Use a specific CSV rules file**\n\n```hledger import --rules [bank.rules] [bank.csv]```\n\n**Import from every .rules file in the journal's rules/ directory**\n\n```hledger import```\n\n**Mark existing transactions as already imported, without importing them**\n\n```hledger import --catchup [bank.csv]```"],["SYNOPSIS","**hledger import** [_options_] [_files_]"],["PARAMETERS","_FILES_\n> Data file(s) to import from (CSV/SSV/TSV or any format hledger can read). If omitted, imports from every **.rules** file found in the journal's **rules/** directory.\n\n**-f**, **--file** _[FMT:]FILE_\n> Journal file to append imported transactions to. Defaults to $LEDGER_FILE or ~/.hledger.journal.\n\n**--rules** _RULESFILE_\n> Use rules defined in this file for converting the CSV/SSV/TSV input. If not given, hledger looks for **FILE.csv.rules** next to each FILE.csv.\n\n**-g**, **--get**\n> Fetch new data first, by running the **get** command (any **data/getdata** and **prices/getprices** scripts).\n\n**--catchup**\n> Don't import anything; just record all current transactions as already imported, so future imports skip them.\n\n**--dry-run**\n> Show the transactions that would be imported, in journal format, without writing them.\n\n**--layout=hledger1|**_COL_\n> How posting amounts should be aligned: right-aligned as in hledger 1 (default), or with decimal marks aligned at column COL."],["DESCRIPTION","**hledger import** detects new transactions in one or more CSV (or other supported format) data files and appends them to the main journal, which should be in journal format. It's most often used for CSV files downloaded from banks, converted using the same CSV rules hledger's file reader normally uses.\n\nUnlike piping a CSV file through **hledger print**, **import** avoids re-importing transactions already seen: for each input file it remembers the latest record date processed, in a hidden **.latest.FILE** file next to it, and skips anything at or before that date on the next run. Run with **--dry-run** first to preview what would be added, especially the first time you import from a file.\n\nIf run with no file arguments, **import** looks in a **rules/** directory next to the main journal and reads every **.rules** file found there (skipping any whose name starts with **.** or **_**), applying each one to its corresponding data file."],["CAVEATS","Correct field mapping usually needs a matching CSV rules file; without one, hledger falls back to a generic one-field-per-column reading that rarely produces useful account names. Overlap detection relies on stable file names, dates, and row ordering across downloads; occasional violations are usually harmless if you import often. Part of the hledger suite."],["HISTORY","**import** was added to **hledger** to automate recording of bank/CSV statement downloads into a plain text journal without manual copy-pasting or duplicate entries."],["INSTALL","```apt: sudo apt install hledger```\n\n```dnf: sudo dnf install hledger```\n\n```pacman: sudo pacman -S hledger```\n\n```apk: sudo apk add hledger```\n\n```zypper: sudo zypper install hledger```\n\n```brew: brew install hledger```\n\n```nix: nix profile install nixpkgs#hledger```"],["SEE ALSO","[hledger](/man/hledger)(1), [hledger-add](/man/hledger-add)(1), [hledger-print](/man/hledger-print)(1)"]],"e":["hledger","hledger-add","hledger-print"],"x":"hledger import [bank.csv]"},"http":{"n":"http","t":"user-friendly HTTP client","s":[["TAGLINE","user-friendly HTTP client"],["TLDR","**GET request**\n\n```http [https://api.example.com/users]```\n\n**POST with JSON**\n\n```http POST [url] name=value```\n\n**POST with form data**\n\n```http --form POST [url] field=value```\n\n**Custom headers**\n\n```http [url] Authorization:\"Bearer [token]\"```\n\n**Download file**\n\n```http --download [url]```\n\n**Authenticated request**\n\n```http --auth [user]:[password] [url]```\n\n**Verbose output**\n\n```http --verbose [url]```\n\n**Use a named session**\n\n```http --session=[name] [url]```"],["SYNOPSIS","**http** [_options_] [_method_] _url_ [_data_]"],["PARAMETERS","_METHOD_\n> HTTP method (GET, POST, PUT, DELETE).\n\n_URL_\n> Request URL.\n\n_DATA_\n> Request data (key=value pairs).\n\n**-d**, **--download**\n> Download file.\n\n**-f**, **--form**\n> Form encoded data.\n\n**-j**, **--json**\n> JSON data.\n\n**-v**, **--verbose**\n> Verbose output.\n\n**-o**, **--output** _FILE_\n> Output file.\n\n**-a**, **--auth** _USER:PASS_\n> Basic or digest authentication credentials.\n\n**-A**, **--auth-type** _TYPE_\n> Authentication type: basic, digest, bearer.\n\n**--session** _NAME_\n> Create or reuse a named session for cookies and headers.\n\n**-p**, **--print** _FLAGS_\n> Control output: H (request headers), B (request body), h (response headers), b (response body).\n\n**--pretty** _STYLE_\n> Output formatting: all, colors, format, none.\n\n**--verify** _CERT_\n> SSL certificate verification. Use **no** to skip.\n\n**--proxy** _PROTOCOL:URL_\n> Use a proxy for requests.\n\n**--follow**\n> Follow HTTP redirects.\n\n**--help**\n> Display help information."],["DESCRIPTION","**HTTPie** (http) is a user-friendly HTTP client. It provides intuitive syntax, colorized output, and JSON support for API interaction.\n\nThe tool simplifies making HTTP requests from the command line. It automatically formats responses and supports sessions and authentication."],["CAVEATS","Python package. Different from curl syntax. JSON by default."],["HISTORY","HTTPie was created by **Jakub Roztocil** as a more user-friendly alternative to curl for API testing."],["INSTALL","```dnf: sudo dnf install httpie```\n\n```pacman: sudo pacman -S httpie```\n\n```apk: sudo apk add httpie```\n\n```zypper: sudo zypper install httpie```\n\n```brew: brew install httpie```\n\n```nix: nix profile install nixpkgs#httpie```"],["SEE ALSO","[curl](/man/curl)(1), [wget](/man/wget)(1), [https](/man/https)(1), [xh](/man/xh)(1), [curlie](/man/curlie)(1)"]],"e":["curl","wget","https","xh","curlie"],"x":"http [https://api.example.com/users]"},"hunk":{"n":"hunk","t":"Review-first terminal diff viewer for agentic coding","s":[["TAGLINE","Review-first terminal diff viewer for agentic coding"],["TLDR","**Open** hunk on a git repo\n\n```hunk```\n\n**Install** via npm or Homebrew\n\n```npm i -g hunk```"],["SYNOPSIS","**hunk** [*options*] [*path*]"],["DESCRIPTION","**hunk** is a terminal diff review tool aimed at developers and coding agents: navigate hunks, review changes with a keyboard-first UI, and integrate into agentic workflows. Install from npm, Homebrew, or upstream releases (**modem-dev/hunk**)."],["PARAMETERS","Default UI reviews the working tree / staged changes. Flags for path, staging, and theme are listed in **hunk --help**."],["CAVEATS","Relatively new; UX may change quickly. Requires a git repository for typical use."],["INSTALL","```brew: brew install hunk```\n\n```nix: nix profile install nixpkgs#hunk```"],["SEE ALSO","[git-diff](/man/git-diff)(1), [delta](/man/delta)(1), [difft](/man/difft)(1)"],["RESOURCES","```[Source code](https://github.com/modem-dev/hunk)```"]],"e":["git-diff","delta","difft"],"x":"hunk"},"ident":{"n":"ident","t":"extracts RCS keywords from files","s":[["TAGLINE","extracts RCS keywords from files"],["TLDR","**Show RCS keywords**\n\n```ident [file]```\n\n**Process multiple files**\n\n```ident [file1] [file2]```\n\n**Quiet mode**\n\n```ident -q [file]```\n\n**Show version keyword only**\n\n```ident -V [file]```"],["SYNOPSIS","**ident** [_options_] [_files_]"],["PARAMETERS","_FILES_\n> Files to scan.\n\n**-q**\n> Quiet mode.\n\n**-V**\n> Show version only.\n\n**--help**\n> Display help information."],["DESCRIPTION","**ident** extracts RCS keywords from files. It finds and displays embedded version control information like $Id$ and $Revision$.\n\nThe tool scans binary and text files for RCS keyword patterns. It's used to identify file versions in builds."],["CAVEATS","Part of RCS package. Keywords must be expanded. Historical tool."],["HISTORY","ident is part of **RCS** (Revision Control System), predating modern version control systems."],["INSTALL","```apt: sudo apt install rcs```\n\n```dnf: sudo dnf install rcs```\n\n```pacman: sudo pacman -S rcs```\n\n```apk: sudo apk add rcs```\n\n```zypper: sudo zypper install rcs```\n\n```brew: brew install rcs```\n\n```nix: nix profile install nixpkgs#rcs```"],["SEE ALSO","[rcs](/man/rcs)(1), [ci](/man/ci)(1), [co](/man/co)(1)"]],"e":["rcs","ci","co"],"x":"ident [file]"},"idevicecrashreport":{"n":"idevicecrashreport","t":"move crash reports from a device to a local directory","s":[["TAGLINE","move crash reports from a device to a local directory"],["TLDR","**Move crash reports** from device to a local directory (removes from device)\n\n```idevicecrashreport [output-dir]```\n\n**Copy crash reports** but keep them on the device\n\n```idevicecrashreport -k [output-dir]```\n\n**Extract raw crash reports** into separate .crash files\n\n```idevicecrashreport -e [output-dir]```\n\n**Filter crash reports** by name (case sensitive)\n\n```idevicecrashreport -f [app-name] [output-dir]```\n\n**Remove all crash logs** from device without copying\n\n```idevicecrashreport --remove-all```\n\n**Target a specific device** by UDID\n\n```idevicecrashreport -u [device-udid] [output-dir]```\n\n**Connect to a network device**\n\n```idevicecrashreport -n [output-dir]```"],["SYNOPSIS","**idevicecrashreport** [_OPTIONS_] _DIRECTORY_"],["PARAMETERS","**-u**, **--udid** _UDID_\n> Target specific device by UDID.\n\n**-n**, **--network**\n> Connect to network device.\n\n**-e**, **--extract**\n> Extract raw crash report into separate '.crash' files.\n\n**-k**, **--keep**\n> Copy but do not remove crash reports from device.\n\n**-f**, **--filter** _NAME_\n> Filter crash reports by NAME (case sensitive).\n\n**--remove-all**\n> Remove all crash log files without copying. Can be used with -f to only remove matching files.\n\n**-d**, **--debug**\n> Enable communication debugging.\n\n**-h**, **--help**\n> Print usage information.\n\n**-v**, **--version**\n> Print version information."],["DESCRIPTION","**idevicecrashreport** is a simple utility to move crash reports from an iOS device to a local directory. Part of the libimobiledevice suite. By default it moves reports (deleting them from the device); use **-k** to copy instead. It downloads .crash, .ips, and related diagnostic files. Useful for debugging app crashes without iTunes or Xcode.\n\nOutput lines are prefixed with \"Link:\", \"Copy:\", or \"Move:\" depending on whether a symlink was created, a file was copied, or moved from the device."],["INSTALL","```apt: sudo apt install libimobiledevice-utils```\n\n```dnf: sudo dnf install libimobiledevice-utils```\n\n```apk: sudo apk add libimobiledevice-progs```"],["SEE ALSO","[ideviceinfo](/man/ideviceinfo)(1), [idevicediagnostics](/man/idevicediagnostics)(1), [idevicesyslog](/man/idevicesyslog)(1)"]],"e":["ideviceinfo","idevicediagnostics","idevicesyslog"],"x":"idevicecrashreport [output-dir]"},"immich":{"n":"immich","t":"self-hosted photo and video backup solution","s":[["TAGLINE","self-hosted photo and video backup solution"],["TLDR","**Start Immich server**\n\n```docker compose up -d```\n\n**Stop Immich**\n\n```docker compose down```\n\n**View logs**\n\n```docker compose logs -f```\n\n**Backup database**\n\n```docker exec immich_postgres pg_dump > backup.sql```"],["SYNOPSIS","**immich** runs as Docker containers"],["PARAMETERS","Configured via docker-compose.yml and .env file.\n\n**UPLOAD_LOCATION**\n> Photo storage path.\n\n**DB_PASSWORD**\n> Database password.\n\n**IMMICH_VERSION**\n> Version tag."],["DESCRIPTION","**Immich** is a self-hosted photo and video backup solution. It provides Google Photos-like functionality with mobile apps and web interface.\n\nThe platform supports face recognition, geolocation, and automatic organization. It's designed for personal photo management."],["CAVEATS","Runs via Docker. Resource intensive. Mobile apps available."],["HISTORY","Immich was created by **Alex Tran** as an open-source alternative to Google Photos."],["INSTALL","```nix: nix profile install nixpkgs#immich```"],["SEE ALSO","[immich-cli](/man/immich-cli)(1), [docker](/man/docker)(1)"]],"e":["immich-cli","docker"],"x":"docker compose up -d"},"jj-config-edit":{"n":"jj-config-edit","t":"opens the Jujutsu configuration file in your default editor","s":[["TAGLINE","opens the Jujutsu configuration file in your default editor"],["TLDR","**Edit user config in editor**\n\n```jj config edit --user```\n\n**Edit repository config**\n\n```jj config edit --repo```"],["SYNOPSIS","**jj** **config** **edit** [_options_]"],["PARAMETERS","**--user**\n> Edit user-level configuration.\n\n**--repo**\n> Edit repository-level configuration."],["DESCRIPTION","**jj config edit** opens the Jujutsu configuration file in your default editor. User config applies globally while repo config applies only to the current repository. Configuration uses TOML format."],["INSTALL","```pacman: sudo pacman -S jujutsu```\n\n```apk: sudo apk add jujutsu```\n\n```zypper: sudo zypper install jujutsu```\n\n```brew: brew install jujutsu```\n\n```nix: nix profile install nixpkgs#jujutsu```"],["SEE ALSO","[jj](/man/jj)(1), [jj-config-get](/man/jj-config-get)(1), [jj-config-set](/man/jj-config-set)(1)"]],"e":["jj","jj-config-get","jj-config-set"],"x":"jj config edit --user"},"jj-git-clone":{"n":"jj-git-clone","t":"clones a Git repository into a Jujutsu workspace","s":[["TAGLINE","clones a Git repository into a Jujutsu workspace"],["TLDR","**Clone repository**\n\n```jj git clone [url]```\n\n**Clone to specific directory**\n\n```jj git clone [url] [directory]```\n\n**Clone with colocated Git**\n\n```jj git clone --colocate [url]```"],["SYNOPSIS","**jj git clone** [_options_] _url_ [_directory_]"],["PARAMETERS","_URL_\n> Git repository URL.\n\n_DIRECTORY_\n> Target directory.\n\n**--colocate**\n> Create colocated jj/Git repo.\n\n**--help**\n> Display help information."],["DESCRIPTION","**jj git clone** clones a Git repository into a Jujutsu workspace. It fetches all branches and creates the jj internal structure.\n\nThe colocate option keeps Git and jj data together. This enables using both tools on the same repository."],["CAVEATS","Subcommand of jj. Creates jj workspace. Colocate for dual Git/jj use."],["HISTORY","jj git clone is part of **Jujutsu**, enabling seamless cloning from any Git hosting service."],["INSTALL","```pacman: sudo pacman -S jujutsu```\n\n```apk: sudo apk add jujutsu```\n\n```zypper: sudo zypper install jujutsu```\n\n```brew: brew install jujutsu```\n\n```nix: nix profile install nixpkgs#jujutsu```"],["SEE ALSO","[jj](/man/jj)(1), [jj-git](/man/jj-git)(1), [git-clone](/man/git-clone)(1)"]],"e":["jj","jj-git","git-clone"],"x":"jj git clone [url]"},"jj-undo":{"n":"jj-undo","t":"reverses the last repository operation","s":[["TAGLINE","reverses the last repository operation"],["TLDR","**Undo last operation**\n\n```jj undo```\n\n**Undo the last two operations**\n\n```jj undo 2```\n\n**Show the operation log to find operation IDs**\n\n```jj operation log```\n\n**Restore to a specific operation by ID**\n\n```jj operation restore [op_id]```"],["SYNOPSIS","**jj undo** [_options_] [_count_]"],["PARAMETERS","_COUNT_\n> Number of operations to undo.\n\n**-R**, **--repository** _path_\n> Path to the repository to operate on.\n\n**--what** _what_\n> What portions of the local state to restore (can be `repo` or `remote-tracking`).\n\n**--help**\n> Display help information."],["DESCRIPTION","**jj undo** reverses the last repository operation by creating a new operation that restores the previous state from the operation log. Unlike `jj operation restore`, which discards intermediate operations, `jj undo` preserves the full operation history.\n\nThe command enables safe experimentation with history. Any jj operation can be undone, including merges, rebases, and bookmark changes."],["CAVEATS","Subcommand of jj. Undoes jj operations only. Operation log has retention limit."],["HISTORY","jj undo is part of **Jujutsu**, leveraging its operation log for powerful undo capabilities."],["SEE ALSO","[jj](/man/jj)(1), [jj-operation](/man/jj-operation)(1), [jj-restore](/man/jj-restore)(1)"]],"e":["jj","jj-operation","jj-restore"],"x":"jj undo"},"jless":{"n":"jless","t":"command-line JSON viewer with vim-like navigation","s":[["TAGLINE","command-line JSON viewer with vim-like navigation"],["TLDR","**View a JSON file**\n\n```jless [file.json]```\n\n**View JSON from stdin**\n\n```cat [file.json] | jless```\n\n**View YAML file**\n\n```jless --yaml [file.yaml]```\n\n**Start with data mode** (default)\n\n```jless -m data [file.json]```\n\n**Start with line mode**\n\n```jless -m line [file.json]```\n\n**Truncate long strings**\n\n```jless --truncate [file.json]```\n\n**Open in scrolloff mode**\n\n```jless --scrolloff [5] [file.json]```"],["SYNOPSIS","**jless** [_options_] [_file_]"],["PARAMETERS","**-m**, **--mode** _mode_\n> Initial viewing mode: data or line.\n\n**--yaml**\n> Parse input as YAML instead of JSON.\n\n**--json**\n> Parse input as JSON (default).\n\n**--truncate**\n> Truncate long strings in display.\n\n**--scrolloff** _lines_\n> Minimum lines above/below cursor.\n\n**-n**, **--line-numbers**\n> Show line numbers.\n\n**-N**, **--no-line-numbers**\n> Hide line numbers.\n\n**-h**, **--help**\n> Display help information.\n\n**-V**, **--version**\n> Display version information."],["KEYBOARD COMMANDS","**j**/**k** or **Down**/**Up**\n> Move cursor down/up.\n\n**h**/**l** or **Left**/**Right**\n> Collapse/expand object or move out/in.\n\n**H**/**L**\n> Focus parent/first child.\n\n**g**/**G**\n> Jump to first/last element.\n\n**Space**\n> Toggle collapse of current node.\n\n**c**\n> Collapse all nodes.\n\n**e**\n> Expand all nodes.\n\n**/**\n> Search forward.\n\n**?**\n> Search backward.\n\n**n**/**N**\n> Next/previous search result.\n\n**y**\n> Yank (copy) current value.\n\n**.**\n> Repeat last command.\n\n**:q** or **q**\n> Quit."],["DESCRIPTION","**jless** is a command-line JSON viewer with vim-like navigation. It provides syntax highlighting, collapsible nodes, and search functionality for exploring JSON and YAML data.\n\nTwo viewing modes exist: **data mode** presents a tree view with collapsible objects and arrays, while **line mode** shows raw formatted output. Switch between them or start with **-m**.\n\nNavigation follows vim conventions. Use **h**/**j**/**k**/**l** for movement, **Space** to toggle node expansion, and **c**/**e** for bulk collapse/expand. Search with **/** and navigate results with **n**/**N**.\n\nThe **yank** command copies values at the current position to the clipboard, useful for extracting specific data from large structures.\n\njless handles large files efficiently through lazy parsing and rendering, making it suitable for multi-megabyte JSON files."],["CAVEATS","Requires terminal with 256-color support for best display. Some terminals may not support clipboard integration for yank. Very deeply nested structures may affect navigation performance. YAML support requires explicit --yaml flag."],["HISTORY","jless was created by **Paul Julius Martinez** and first released in **2022**. Written in Rust, it was designed to fill the gap for a dedicated JSON browsing tool with vim-style navigation. The project gained popularity as a complement to jq for interactive JSON exploration. It continues development with YAML support and other improvements."],["INSTALL","```pacman: sudo pacman -S jless```\n\n```apk: sudo apk add jless```\n\n```zypper: sudo zypper install jless```\n\n```brew: brew install jless```\n\n```nix: nix profile install nixpkgs#jless```"],["SEE ALSO","[jq](/man/jq)(1), [gojq](/man/gojq)(1), [fx](/man/fx)(1), [less](/man/less)(1)"]],"e":["jq","gojq","fx","less"],"x":"jless [file.json]"},"jupytext":{"n":"jupytext","t":"converts Jupyter notebooks to text formats","s":[["TAGLINE","converts Jupyter notebooks to text formats"],["TLDR","**Convert a notebook to a Python percent script**\n\n```jupytext --to py:percent [notebook.ipynb]```\n\n**Convert a Python script back to a notebook**\n\n```jupytext --to ipynb [script.py]```\n\n**Convert a notebook to Markdown and write to a specific output file**\n\n```jupytext --to md --output [output.md] [notebook.ipynb]```\n\n**Pair a notebook with a Python percent script for version control**\n\n```jupytext --set-formats ipynb,py:percent [notebook.ipynb]```\n\n**Sync all paired representations, updating whichever file is outdated**\n\n```jupytext --sync [notebook.ipynb]```\n\n**Pipe the text representation through black to reformat in place**\n\n```jupytext --pipe black [notebook.ipynb]```\n\n**Convert a Markdown notebook to ipynb and execute it**\n\n```jupytext --to notebook --execute [notebook.md]```"],["SYNOPSIS","**jupytext** [_options_] _notebook_ [_notebook_ ...]"],["PARAMETERS","**--to** _FORMAT_\n> Target format. Common values: `py`, `py:percent`, `py:light`, `ipynb`, `md`, `md:myst`, `Rmd`. The part after `:` selects the sub-format.\n\n**--from** _FORMAT_\n> Explicitly specify the source format, required when reading from stdin.\n\n**-o** _FILE_, **--output** _FILE_\n> Output file path. Use `-` to write to stdout.\n\n**--sync**\n> Sync all paired representations of a notebook based on file timestamps, updating whichever copy is outdated.\n\n**--set-formats** _FORMATS_\n> Pair the notebook with one or more additional formats (e.g. `ipynb,py:percent`). Writes pairing metadata into the notebook.\n\n**--update**\n> When converting to an existing `.ipynb` file, preserve existing outputs and metadata and only update the input cells.\n\n**--pipe** _CMD_\n> Pipe the text representation of the notebook through an external command (e.g. `black` or `isort`). Use `{}` as a placeholder for a temporary file when the command does not accept stdin.\n\n**--pipe-fmt** _FORMAT_\n> Format to use when piping (default: `py:percent`).\n\n**--check** _CMD_\n> Run a command on the text representation and fail if it returns a non-zero exit code. Use `{}` as a placeholder for a temporary file.\n\n**--execute**\n> Execute the notebook cells after conversion (requires `nbconvert`).\n\n**--set-kernel** _NAME_\n> Set the kernel in the notebook YAML header. Use `-` to use the kernel matching the current Python executable.\n\n**--update-metadata** _JSON_\n> Update notebook-level metadata with the provided JSON string (e.g. `'{\"kernelspec\":{\"name\":\"python3\"}}'`).\n\n**--opt** _KEY=VALUE_\n> Set a jupytext option, such as `comment_magics=false`.\n\n**--test**\n> Test that the round-trip conversion (notebook -> text -> notebook) is lossless.\n\n**--test-strict**\n> Like `--test`, but performs a strict byte-for-byte comparison of the two notebooks.\n\n**--help**\n> Display help information."],["DESCRIPTION","**Jupytext** converts Jupyter notebooks to plain-text formats such as Python scripts and Markdown files, and back again. This makes notebooks suitable for version control, code review, and editing in a standard IDE.\n\nThe most common workflow is to pair a `.ipynb` notebook with a `.py` or `.md` file using `--set-formats`. Once paired, running `--sync` keeps both files up to date: the paired text file captures inputs cleanly for version control, while the `.ipynb` retains cell outputs.\n\nThe `percent` format (`py:percent`) is the recommended script format. Cells are delimited by `# %%` markers and the files are valid Python. The `light` format uses minimal markers. Markdown-based formats include plain `md`, `md:myst` (MyST Markdown, compatible with Jupyter Book), and `Rmd` (R Markdown).\n\nJupytext integrates with JupyterLab as an extension: text notebooks open with a notebook icon and can be launched directly. It also works with pre-commit hooks for automated formatting."],["CAVEATS","Cell outputs (images, tables, rich output) are not stored in text formats and are lost on conversion unless paired with an `.ipynb` file. The `.ipynb` file must exist or be regenerated by executing the notebook to restore outputs."],["HISTORY","Jupytext was created by **Marc Wouts** to solve version control challenges with Jupyter notebooks. It began as a JupyterLab extension and later added a full command-line interface."],["INSTALL","```brew: brew install jupytext```"],["SEE ALSO","[jupyter](/man/jupyter)(1), [jupyter-lab](/man/jupyter-lab)(1), [black](/man/black)(1), [isort](/man/isort)(1)"]],"e":["jupyter","jupyter-lab","black","isort"],"x":"jupytext --to py:percent [notebook.ipynb]"},"just.js":{"n":"just.js","t":"small, embeddable V8 JavaScript runtime for Linux","s":[["TAGLINE","small, embeddable V8 JavaScript runtime for Linux"],["TLDR","Start a **REPL** (interactive shell)\n\n```just```\n\n**Run** a JavaScript file\n\n```just [path/to/file.js]```\n\n**Evaluate** JavaScript code\n\n```just eval \"[code]\"```\n\n**Initialize** a new project\n\n```just init [project_name]```\n\n**Build** a JavaScript application into executable\n\n```just build [path/to/file.js] --static```"],["SYNOPSIS","**just** [_options_] [_file_]"],["PARAMETERS","**eval** _CODE_\n> Evaluate JavaScript code string\n\n**init** _NAME_\n> Initialize a new project directory\n\n**build** _FILE_\n> Build JavaScript into executable\n\n**--static**\n> Create statically linked executable"],["DESCRIPTION","**just** is a small, embeddable V8 JavaScript runtime for Linux. It provides a minimal runtime environment for executing JavaScript applications with direct access to Linux system calls.\n\nThe runtime is designed to be lightweight and fast-starting, suitable for serverless functions, command-line tools, and embedded applications. It can compile JavaScript into standalone executables."],["CAVEATS","Linux-only runtime. API differs from Node.js; applications may need modification. Smaller ecosystem compared to Node.js or Deno."],["HISTORY","just-js was created as a minimal V8 runtime for Linux, focusing on low overhead and direct system call access rather than the broader API compatibility of Node.js."],["SEE ALSO","[node](/man/node)(1), [deno](/man/deno)(1), [bun](/man/bun)(1)"]],"e":["node","deno","bun"],"x":"just"},"lue":{"n":"lue","t":"Terminal eBook reader with text-to-speech","s":[["TAGLINE","Terminal eBook reader with text-to-speech"],["TLDR","**Read an eBook**\n\n```lue [path/to/book.epub]```\n\n**Resume reading the last opened book**\n\n```lue```\n\n**Start the navigation guide**\n\n```lue --guide```\n\n**Read with a specific TTS model**\n\n```lue --tts [kokoro] [path/to/book.epub]```\n\n**Read with a specific voice and speed**\n\n```lue --voice [en-US-AriaNeural] --speed [1.5] [path/to/book.epub]```\n\n**Read a PDF with cleaning filter** (removes page numbers, headers)\n\n```lue --filter [path/to/book.pdf]```"],["SYNOPSIS","**lue** [_options_] [_file_]"],["PARAMETERS","**-k**, **--key** _LAYOUT_\n> Specify the keyboard layout to use.\n\n**--guide**\n> Launch the interactive navigation guide.\n\n**--tts** _MODEL_\n> Use specific TTS model (edge, kokoro, or none).\n\n**--voice** _VOICE_\n> Use a specific voice (e.g., \"en-US-AriaNeural\").\n\n**--speed** _SPEED_\n> Set the speech speed (e.g., 1.5).\n\n**--lang** _CODE_\n> Specify a language code.\n\n**--over** _SECONDS_\n> Seconds of overlap between sentences.\n\n**--filter**\n> Enable PDF cleaning filter (removes page numbers, headers, footnotes)."],["DESCRIPTION","**lue** is a terminal-based eBook reader with audiobook-quality text-to-speech support. It handles multiple formats including **EPUB**, **PDF**, **DOCX**, **HTML**, **RTF**, **TXT**, and **MD**.\n\nThe interface offers three UI complexity modes (Minimal, Medium, and Full) that can be cycled through with the **v** key. Two built-in keyboard layouts are available for navigation."],["CAVEATS","Text-to-speech quality depends on the system's TTS engine. PDF rendering in the terminal may not preserve complex layouts."],["HISTORY","**lue** was created by **superstarryeyes** and is written in **Python**. It can be installed via pip with `pip install lue-reader`."],["INSTALL","```nix: nix profile install nixpkgs#lue```"],["SEE ALSO","[bat](/man/bat)(1), [less](/man/less)(1)"]],"e":["bat","less"],"x":"lue [path/to/book.epub]"},"maildrop":{"n":"maildrop","t":"mail delivery agent with filtering","s":[["TAGLINE","mail delivery agent with filtering"],["TLDR","**Process mail with filter**\n\n```maildrop [filter.rc]```\n\n**Deliver mail**\n\n```maildrop < [message.eml]```\n\n**Test filter**\n\n```maildrop -V [9] [filter.rc] < [message.eml]```\n\n**Use specific maildir**\n\n```maildrop -d [user] [filter.rc]```\n\n**Show version**\n\n```maildrop -v```"],["SYNOPSIS","**maildrop** [_options_] [_filterfile_]"],["PARAMETERS","_FILTERFILE_\n> Mail filter rules file.\n\n**-d** _USER_\n> Deliver as user.\n\n**-V** _LEVEL_\n> Verbose level.\n\n**-v**\n> Show version.\n\n**-f** _ADDRESS_\n> From address.\n\n**--help**\n> Display help information."],["DESCRIPTION","**maildrop** is a mail delivery agent with filtering. It sorts incoming mail using rules.\n\nThe tool delivers mail to maildirs or mboxes. Filter rules support conditions and actions."],["CAVEATS","Requires filter configuration. Part of courier-maildrop. Maildir preferred format."],["HISTORY","maildrop is part of the **Courier Mail Server** project, providing flexible mail filtering and delivery."],["INSTALL","```apt: sudo apt install maildrop```\n\n```dnf: sudo dnf install maildrop```\n\n```nix: nix profile install nixpkgs#maildrop```"],["SEE ALSO","[procmail](/man/procmail)(1), [dovecot](/man/dovecot)(1), [postfix](/man/postfix)(1)"]],"e":["procmail","dovecot","postfix"],"x":"maildrop [filter.rc]"},"minetest":{"n":"minetest","t":"open-source voxel game engine and sandbox game similar to Minecraft","s":[["TAGLINE","open-source voxel game engine and sandbox game similar to Minecraft"],["TLDR","**Start Minetest client**\n\n```minetest```\n\n**Connect to server**\n\n```minetest --address [server.example.com] --port [30000]```\n\n**Start with specific game**\n\n```minetest --gameid [minetest_game]```\n\n**Run in terminal mode**\n\n```minetest --terminal```"],["SYNOPSIS","**minetest** [_options_]"],["PARAMETERS","**--address** _host_\n> Server address to connect to.\n\n**--port** _port_\n> Server port (default 30000).\n\n**--name** _name_\n> Player name.\n\n**--password** _pass_\n> Player password.\n\n**--gameid** _id_\n> Game ID to use.\n\n**--world** _path_\n> World directory.\n\n**--config** _file_\n> Configuration file.\n\n**--terminal**\n> Run in terminal mode.\n\n**--go**\n> Disable main menu and connect directly to the specified server.\n\n**--server**\n> Run as a dedicated server (headless).\n\n**--map-dir** _path_\n> World map directory when running as server.\n\n**--logfile** _file_\n> Log to file (in addition to console).\n\n**--run-unittests**\n> Run built-in unit tests and exit.\n\n**--videomodes**\n> Show available video modes.\n\n**--speedtests**\n> Run speed tests and exit.\n\n**--info**\n> Print info and exit."],["DESCRIPTION","**minetest** is an open-source voxel game engine and sandbox game similar to Minecraft. It supports both client and dedicated server modes, multiplayer, modding, and custom content. The engine is highly extensible through Lua scripting and content packages.\n\nGames, mods, and texture packs are managed through the built-in content browser or placed in **~/.minetest/** (Linux), **~/Library/Application Support/minetest/** (macOS). The default game is **Minetest Game**, but many community games exist.\n\nConfiguration is stored in **minetest.conf** in the user data directory."],["CAVEATS","The **--go** flag requires **--address** to be set. Running as server with **--server** requires a world directory (defaults to the first world found). The client requires OpenGL support."],["INSTALL","```dnf: sudo dnf install minetest```\n\n```pacman: sudo pacman -S luanti```\n\n```apk: sudo apk add luanti```\n\n```zypper: sudo zypper install luanti```\n\n```nix: nix profile install nixpkgs#luanti```"],["SEE ALSO","[minetestserver](/man/minetestserver)(1)"]],"e":["minetestserver"],"x":"minetest"},"monitui":{"n":"monitui","t":"TUI for managing Hyprland monitor layouts","s":[["TAGLINE","TUI for managing Hyprland monitor layouts"],["TLDR","**Launch the monitor manager**\n\n```monitui```"],["SYNOPSIS","**monitui** [_options_]"],["DESCRIPTION","**monitui** is a terminal tool for managing Hyprland monitor layouts without hand-editing config files. It allows users to visually arrange displays, adjust resolution and rotation settings, and save multiple configuration presets for different scenarios (desk, dock, travel).\n\nFeatures include mouse-draggable monitor positioning, workspace assignment to monitors, and confirmation safeguards to prevent problematic layouts."],["CAVEATS","Only works with the Hyprland window manager on Linux. Requires a running Hyprland session."],["HISTORY","**monitui** was created by **Nathaniel Fargo** and is written in **Rust**."],["INSTALL","```aur: yay -S monitui```"],["SEE ALSO","[hyprctl](/man/hyprctl)(1)"]],"e":["hyprctl"],"x":"monitui"},"nix3-flake":{"n":"nix3-flake","t":"manages Nix flakes","s":[["TAGLINE","manages Nix flakes"],["TLDR","**Show flake outputs**\n\n```nix3 flake show```\n\n**Update inputs**\n\n```nix3 flake update```\n\n**Initialize flake**\n\n```nix3 flake init```\n\n**Check flake**\n\n```nix3 flake check```"],["SYNOPSIS","**nix3** **flake** [_command_] [_options_]"],["PARAMETERS","_COMMAND_\n> Flake subcommand.\n\n**show**\n> Display outputs.\n\n**update**\n> Update inputs.\n\n**init**\n> Create new flake.\n\n**check**\n> Validate flake.\n\n**--help**\n> Display help information."],["DESCRIPTION","**nix3 flake** manages Nix flakes. Alternative name for nix flake commands.\n\nThe tool handles flake.nix files. Manages reproducible package definitions."],["CAVEATS","Alias for nix flake. Experimental features. Lock file management."],["HISTORY","nix3 flake is part of the **Nix 3.x** flake system for hermetic builds."],["SEE ALSO","[nix](/man/nix)(1), [nix-flake](/man/nix-flake)(1), [nix3-build](/man/nix3-build)(1)"]],"e":["nix","nix-flake","nix3-build"],"x":"nix3 flake show"},"pcapfix":{"n":"pcapfix","t":"repairs corrupted packet captures","s":[["TAGLINE","repairs corrupted packet captures"],["TLDR","**Fix corrupted pcap**\n\n```pcapfix [corrupted.pcap]```\n\n**Fix and save to new file**\n\n```pcapfix -o [fixed.pcap] [corrupted.pcap]```\n\n**Verbose output**\n\n```pcapfix -v [corrupted.pcap]```\n\n**Deep scan mode**\n\n```pcapfix -d [corrupted.pcap]```"],["SYNOPSIS","**pcapfix** [_options_] _file_"],["PARAMETERS","_FILE_\n> Corrupted pcap file.\n\n**-o** _OUTPUT_\n> Output file.\n\n**-v**\n> Verbose mode.\n\n**-d**\n> Deep scan.\n\n**-n**\n> Fix pcapng files."],["DESCRIPTION","**pcapfix** repairs corrupted packet captures. Fixes pcap and pcapng files.\n\nThe tool recovers network captures. Handles truncation and corruption."],["CAVEATS","May not recover all data. Backup originals. Supports pcap and pcapng."],["HISTORY","pcapfix was created for **recovering corrupted** packet capture files."],["INSTALL","```apt: sudo apt install pcapfix```\n\n```nix: nix profile install nixpkgs#pcapfix```"],["SEE ALSO","[tcpdump](/man/tcpdump)(1), [wireshark](/man/wireshark)(1), [editcap](/man/editcap)(1)"]],"e":["tcpdump","wireshark","editcap"],"x":"pcapfix [corrupted.pcap]"},"pdbedit":{"n":"pdbedit","t":"manages the Samba user database, allowing administrators to add","s":[["TAGLINE","manages the Samba user database, allowing administrators to add"],["TLDR","**List** all Samba users with their settings\n\n```sudo pdbedit --list --verbose```\n\n**Add** an existing Unix user to Samba\n\n```sudo pdbedit --user [username] --create```\n\n**Remove** a Samba user\n\n```sudo pdbedit --user [username] --delete```\n\n**Reset** a Samba user's failed password counter\n\n```sudo pdbedit --user [username] --bad-password-count-reset```"],["SYNOPSIS","**pdbedit** [_options_]"],["PARAMETERS","**-L**, **--list**\n> List all Samba user accounts\n\n**-v**, **--verbose**\n> Enable verbose listing mode\n\n**-u**, **--user** _username_\n> Specify the user to operate on\n\n**-a**, **--create**\n> Add a new user account (prompts for password)\n\n**-x**, **--delete**\n> Delete specified user account\n\n**-z**, **--bad-password-count-reset**\n> Reset bad password count for user\n\n**-t**, **--password-from-stdin**\n> Read password from stdin\n\n**-w**, **--smbpasswd-style**\n> Use smbpasswd-style output\n\n**-e**, **--export**\n> Export user database to stdout\n\n**-i**, **--import**\n> Import user database from stdin\n\n**-b**, **--backend** _name_\n> Use specified passdb backend\n\n**-P**, **--account-policy**\n> Display or change account policy\n\n**--policies-reset**\n> Reset account policies to default"],["DESCRIPTION","**pdbedit** manages the Samba user database (SAM database), allowing administrators to add, modify, and delete Samba user accounts. It provides more detailed control than smbpasswd, including access to user flags, password policies, and database import/export.\n\nUsers must exist in the Unix system before being added to Samba. The tool can operate on different passdb backends including tdbsam (default) and ldapsam for LDAP integration."],["CAVEATS","Requires root privileges. User must exist in Unix passwd database before adding to Samba. For simple user management, smbpasswd may be easier. Backend configuration depends on smb.conf settings."],["HISTORY","Part of the **Samba** suite since Samba 3.0, replacing older password database tools. Provides a unified interface for managing various Samba passdb backends as Samba moved away from the legacy smbpasswd file format."],["INSTALL","```apt: sudo apt install samba```\n\n```dnf: sudo dnf install samba```\n\n```pacman: sudo pacman -S samba```\n\n```apk: sudo apk add samba-common-tools```\n\n```zypper: sudo zypper install samba```\n\n```brew: brew install samba```\n\n```nix: nix profile install nixpkgs#samba```"],["SEE ALSO","[smbpasswd](/man/smbpasswd)(8), [samba](/man/samba)(7)"]],"e":["smbpasswd","samba"],"x":"sudo pdbedit --list --verbose"},"pg_amcheck":{"n":"pg_amcheck","t":"checks PostgreSQL database corruption using Access Method verification","s":[["TAGLINE","checks PostgreSQL database corruption using Access Method verification"],["TLDR","**Check all databases**\n\n```pg_amcheck --all```\n\n**Check a specific database**\n\n```pg_amcheck -d [database]```\n\n**Check a specific table with verbose output**\n\n```pg_amcheck -d [database] -t [table] -v```\n\n**Check with heap verification for all indexed values**\n\n```pg_amcheck -d [database] --heapallindexed```\n\n**Check using multiple parallel connections**\n\n```pg_amcheck --all --jobs [4]```"],["SYNOPSIS","**pg_amcheck** [_options_] [_dbname_]"],["PARAMETERS","**-a**, **--all**\n> Check all databases.\n\n**-d**, **--database** _name_\n> Database to check.\n\n**-t**, **--table** _table_\n> Check specific table.\n\n**-i**, **--index** _index_\n> Check specific index.\n\n**-s**, **--schema** _schema_\n> Check tables and indexes in the specified schema only.\n\n**-v**, **--verbose**\n> Print a message for each relation being checked and increase detail for errors.\n\n**--heapallindexed**\n> Verify the presence of all heap tuples as index tuples in each checked index.\n\n**--parent-check**\n> Use bt_index_parent_check for additional parent/child relationship verification.\n\n**--checkunique**\n> For indexes with unique constraints, verify no duplicate visible entries exist.\n\n**--rootdescend**\n> Re-find tuples on leaf level via root page search for each tuple. Implies --parent-check.\n\n**--install**\n> Install any missing extensions required to check the database (currently amcheck).\n\n**-j**, **--jobs** _num_\n> Use num concurrent connections to the server.\n\n**--progress**\n> Show progress information including completed relations and sizes.\n\n**-w**, **--no-password**\n> Never prompt for a password.\n\n**-W**, **--password**\n> Force a password prompt before connecting."],["DESCRIPTION","**pg_amcheck** checks for corruption in one or more PostgreSQL databases by running amcheck's verification functions against all relations in the target databases. It detects physical corruption in tables and B-tree indexes, such as pages with invalid headers, missing or duplicate tuples, and broken internal links. Available since PostgreSQL 14."],["INSTALL","```apk: sudo apk add postgresql17-client```"],["SEE ALSO","[pg_checksums](/man/pg_checksums)(1)"]],"e":["pg_checksums"],"x":"pg_amcheck --all"},"plasma-apply-cursortheme":{"n":"plasma-apply-cursortheme","t":"Apply KDE Plasma cursor themes","s":[["TAGLINE","Apply KDE Plasma cursor themes"],["TLDR","**Apply** a cursor theme by name\n\n```plasma-apply-cursortheme [breeze_cursors]```\n\n**List** installed cursor themes\n\n```plasma-apply-cursortheme --list-themes```\n\nShow **help**\n\n```plasma-apply-cursortheme --help```"],["SYNOPSIS","**plasma-apply-cursortheme** [**--list-themes**] [_theme_name_]"],["PARAMETERS","**--list-themes**\n> List all available cursor themes\n\n**theme_name**\n> Name of cursor theme to apply\n\n**--help**\n> Display help information"],["DESCRIPTION","**plasma-apply-cursortheme** changes the cursor theme of KDE Plasma desktop from the command line. It provides the same functionality as the System Settings cursor configuration but in a scriptable form.\n\nCursor themes affect the mouse pointer appearance throughout the desktop and applications. Changes apply immediately without requiring session restart."],["CAVEATS","Requires running Plasma session. Some applications may need restart to display new cursor. Theme names are case-sensitive."],["HISTORY","**plasma-apply-cursortheme** is part of **KDE Plasma Workspace**, providing command-line control over cursor appearance for scripting and automation purposes."],["INSTALL","```apt: sudo apt install plasma-workspace```\n\n```dnf: sudo dnf install plasma-workspace```\n\n```pacman: sudo pacman -S plasma-workspace```\n\n```apk: sudo apk add plasma-workspace```"],["SEE ALSO","[plasma-apply-colorscheme](/man/plasma-apply-colorscheme)(1), [plasma-apply-desktoptheme](/man/plasma-apply-desktoptheme)(1)"]],"e":["plasma-apply-colorscheme","plasma-apply-desktoptheme"],"x":"plasma-apply-cursortheme [breeze_cursors]"},"pnmtojpeg":{"n":"pnmtojpeg","t":"Convert PNM images to JPEG format","s":[["TAGLINE","Convert PNM images to JPEG format"],["TLDR","**Convert PNM to JPEG**\n\n```pnmtojpeg [input.pnm] > [output.jpg]```\n\n**Convert with quality**\n\n```pnmtojpeg -quality [85] [input.pnm] > [output.jpg]```\n\n**Create progressive JPEG**\n\n```pnmtojpeg -progressive [input.pnm] > [output.jpg]```\n\n**Optimize Huffman tables**\n\n```pnmtojpeg -optimize [input.pnm] > [output.jpg]```"],["SYNOPSIS","**pnmtojpeg** [_options_] [_file_]"],["PARAMETERS","**-quality** _n_\n> Quality (0-100, default 75).\n\n**-progressive**\n> Create progressive JPEG.\n\n**-optimize**\n> Optimize Huffman tables.\n\n**-grayscale**\n> Force grayscale output.\n\n**-baseline**\n> Force baseline JPEG."],["DESCRIPTION","**pnmtojpeg** converts PNM (PBM, PGM, PPM) images to JPEG/JFIF format. The **-quality** option controls the compression-to-quality tradeoff, with higher values producing larger files with better image quality.\n\nThe **-progressive** option creates progressive JPEGs that render in increasing detail during loading. The **-optimize** flag reduces file size by computing optimal Huffman coding tables. The **-grayscale** option forces single-channel output even for color inputs. Part of the Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[jpegtopnm](/man/jpegtopnm)(1), [pnmtopng](/man/pnmtopng)(1)"]],"e":["jpegtopnm","pnmtopng"],"x":"pnmtojpeg [input.pnm] > [output.jpg]"},"powerstat":{"n":"powerstat","t":"Measure system power consumption statistics","s":[["TAGLINE","Measure system power consumption statistics"],["TLDR","Measure power with **defaults**\n\n```powerstat```\n\nMeasure with custom **interval and samples**\n\n```powerstat [10] [20]```\n\nUse **RAPL** interface\n\n```powerstat -R [10] [20]```\n\nShow **histogram**\n\n```powerstat -H [10] [20]```\n\nEnable **all** statistics\n\n```powerstat -a [10] [20]```"],["SYNOPSIS","**powerstat** [**-a**] [**-H**] [**-R**] [_interval_] [_samples_]"],["PARAMETERS","**interval**\n> Seconds between measurements (default: 10)\n\n**samples**\n> Number of measurements (default: 10)\n\n**-a**\n> Enable all statistics gathering\n\n**-H**\n> Show histogram of measurements\n\n**-R**\n> Use Intel RAPL interface instead of battery\n\n**-d**\n> Show distribution statistics\n\n**-z**\n> Show zero/idle power estimate"],["DESCRIPTION","**powerstat** measures system power consumption using battery discharge rate or Intel's RAPL (Running Average Power Limit) interface. It provides statistical analysis of power usage over time.\n\nThe tool is useful for evaluating power efficiency of applications, kernel settings, or hardware configurations on laptops and servers."],["CAVEATS","Battery mode requires battery-powered system. RAPL requires Intel CPU with RAPL support. Measurements may vary based on system load. Run multiple samples for accuracy."],["HISTORY","**powerstat** was written by **Colin Ian King** as part of his power management testing tools. It's commonly used in Ubuntu's power testing and optimization efforts."],["INSTALL","```apt: sudo apt install powerstat```\n\n```dnf: sudo dnf install powerstat```\n\n```zypper: sudo zypper install powerstat```\n\n```nix: nix profile install nixpkgs#powerstat```"],["SEE ALSO","[powertop](/man/powertop)(8), [turbostat](/man/turbostat)(8), [cpupower](/man/cpupower)(1)"]],"e":["powertop","turbostat","cpupower"],"x":"powerstat"},"pwd":{"n":"pwd","t":"Display current working directory path","s":[["TAGLINE","Display current working directory path"],["TLDR","**Print working directory**\n\n```pwd```\n\n**Print physical path (no symlinks)**\n\n```pwd -P```\n\n**Print logical path (with symlinks)**\n\n```pwd -L```"],["SYNOPSIS","**pwd** [_options_]"],["PARAMETERS","**-L**\n> Logical path (follow symlinks).\n\n**-P**\n> Physical path (resolve symlinks)."],["DESCRIPTION","**pwd** prints the absolute path of the current working directory. It is one of the most basic shell commands, used to confirm your location in the filesystem when navigating between directories.\n\nThe **-P** flag resolves symbolic links to display the physical path, while **-L** (the default in most shells) preserves symbolic link components in the path. Both a shell built-in version and a standalone binary from GNU coreutils exist; the built-in is used by default in interactive shells."],["CAVEATS","Shell built-in and external command. Behavior may differ."],["HISTORY","pwd is a **classic Unix** command for directory display."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[cd](/man/cd)(1), [dirs](/man/dirs)(1), [basename](/man/basename)(1)"]],"e":["cd","dirs","basename"],"x":"pwd"},"pydoc":{"n":"pydoc","t":"View Python module documentation","s":[["TAGLINE","View Python module documentation"],["TLDR","**View documentation** for a module\n\n```pydoc [module]```\n\n**View documentation** for a class or function\n\n```pydoc [module.ClassName]```\n\n**Search module synopses** for a keyword\n\n```pydoc -k [keyword]```\n\n**Start a local documentation HTTP server**\n\n```pydoc -p [8080]```\n\n**Start server and open** in the default browser\n\n```pydoc -b```\n\n**Write HTML documentation** to a file in the current directory\n\n```pydoc -w [module]```"],["SYNOPSIS","**pydoc** [**-k** _keyword_] [**-p** _port_] [**-b**] [**-w**] [_name_...]"],["PARAMETERS","_name_\n> Module, package, class, function, or keyword to document.\n\n**-k** _keyword_\n> Search synopsis lines of all available modules for keyword.\n\n**-p** _port_\n> Start HTTP documentation server on the given port.\n\n**-b**\n> Start HTTP server and open a web browser to its page.\n\n**-w** _name_\n> Write HTML documentation for the module to a file in the current directory.\n\n**-n** _host_\n> Hostname for the HTTP server (default: localhost). Python 3.11+."],["DESCRIPTION","**pydoc** is Python's built-in documentation tool that displays module documentation generated from docstrings and introspection. It can show documentation for modules, classes, functions, and keywords in the terminal, search across all installed modules with **-k**, or generate static HTML files with **-w**.\n\nThe **-p** flag starts a local HTTP documentation server that provides a browsable interface to all installed Python modules, and **-b** opens this server automatically in the default web browser. pydoc is part of the Python standard library and provides the same functionality as the interactive **help()** function.\n\nThe **PAGER** environment variable controls which program is used for terminal display. The **BROWSER** environment variable controls which browser is used with **-b**."],["CAVEATS","Documentation quality depends on the presence of docstrings in the module source. Importing a module to inspect it may execute module-level code. The **-n** flag for specifying the server hostname requires Python 3.11+."],["HISTORY","**pydoc** has been part of the Python standard library since **Python 2.1** (2001). It was written by **Ka-Ping Yee** as a built-in documentation viewer, providing the same functionality as the interactive **help()** function."],["SEE ALSO","[python](/man/python)(1), [python3](/man/python3)(1), [pydoc3](/man/pydoc3)(1)"]],"e":["python","python3","pydoc3"],"x":"pydoc [module]"},"sftp":{"n":"sftp","t":"Secure interactive file transfer over SSH","s":[["TAGLINE","Secure interactive file transfer over SSH"],["TLDR","**Connect to a remote server**\n\n```sftp [user]@[hostname]```\n\n**Connect using a specific port**\n\n```sftp -P [port] [user]@[hostname]```\n\n**Upload a file**\n\n```sftp> put [local_file] [remote_path]```\n\n**Download a file**\n\n```sftp> get [remote_file] [local_path]```\n\n**Upload a directory recursively**\n\n```sftp> put -r [local_directory]```\n\n**Download a directory recursively**\n\n```sftp> get -r [remote_directory]```\n\n**List remote directory**\n\n```sftp> ls```\n\n**Change remote directory**\n\n```sftp> cd [remote_path]```"],["SYNOPSIS","**sftp** [**-P** _port_] [**-i** _identity_file_] [_user_**@**]_host_[**:**_path_]"],["PARAMETERS","**-P** _port_\n> Connect to specified port (note: uppercase P, unlike ssh)\n\n**-i** _identity_file_\n> Use specified private key file\n\n**-b** _batchfile_\n> Batch mode; read commands from file\n\n**-C**\n> Enable compression\n\n**-r**\n> Recursively copy directories (with get/put)\n\n**-v**\n> Verbose mode\n\n**-o** _option_\n> Pass option to SSH (e.g., -o \"ProxyJump=jump-host\")"],["INTERACTIVE COMMANDS","**ls** [_path_]: List remote directory\n**lls** [_path_]: List local directory\n**cd** _path_: Change remote directory\n**lcd** _path_: Change local directory\n**pwd**: Print remote working directory\n**lpwd**: Print local working directory\n**get** [**-r**] _remote_ [_local_]: Download file/directory\n**put** [**-r**] _local_ [_remote_]: Upload file/directory\n**mkdir** _path_: Create remote directory\n**rmdir** _path_: Remove remote directory\n**rm** _file_: Delete remote file\n**rename** _old_ _new_: Rename remote file\n**chmod** _mode_ _file_: Change remote file permissions\n**chown** _uid_ _file_: Change remote file owner\n**!** _command_: Execute local shell command\n**exit** or **quit**: Close connection"],["DESCRIPTION","**sftp** is the SSH File Transfer Protocol client, providing secure interactive file transfer over SSH connections. It offers a familiar FTP-like interface while using SSH for authentication and encryption.\n\nUnlike FTP, SFTP encrypts all data including credentials and file contents. It uses a single connection (no separate data channel) making it firewall-friendly.\n\nThe interface supports both interactive use and batch mode (**-b**) for scripting. Tab completion works for both local and remote paths in most implementations.\n\nSFTP can transfer binary and text files without mode switches and preserves file attributes like permissions and timestamps."],["CONFIGURATION","**~/.ssh/config**\n> SSH client configuration file; settings like Host aliases, IdentityFile, ProxyJump, and Port apply to sftp connections.\n\n**~/.ssh/known_hosts**\n> Database of accepted server host keys, shared with ssh and scp.\n\n**/etc/ssh/ssh_config**\n> System-wide SSH client configuration applied to all users."],["CAVEATS","Port is specified with uppercase **-P**, not lowercase **-p** like SSH. This is a common source of confusion.\n\nSFTP is different from FTPS (FTP over SSL/TLS). SFTP uses SSH; FTPS uses the FTP protocol with TLS encryption.\n\nFor automated transfers, use batch mode (**-b**) or consider **rsync** over SSH for more sophisticated synchronization.\n\nSymbolic links are followed by default. Use **get -P** or **put -P** to preserve symlinks as symlinks."],["INSTALL","```apt: sudo apt install openssh-client```\n\n```apk: sudo apk add openssh-client-common```"],["SEE ALSO","[scp](/man/scp)(1), [ssh](/man/ssh)(1), [rsync](/man/rsync)(1), [ftp](/man/ftp)(1)"]],"e":["scp","ssh","rsync","ftp"],"x":"sftp [user]@[hostname]"},"singularity":{"n":"singularity","t":"HPC container runtime without root privileges","s":[["TAGLINE","HPC container runtime without root privileges"],["TLDR","**Run container**\n\n```singularity run [container.sif]```\n\n**Execute command**\n\n```singularity exec [container.sif] [command]```\n\n**Shell into container**\n\n```singularity shell [container.sif]```\n\n**Build from definition**\n\n```singularity build [output.sif] [definition.def]```\n\n**Pull from registry**\n\n```singularity pull [docker://ubuntu:latest]```\n\n**Build sandbox**\n\n```singularity build --sandbox [directory] [docker://ubuntu]```"],["SYNOPSIS","**singularity** _command_ [_options_] _container_ [_args_]"],["PARAMETERS","**run**\n> Run container.\n\n**exec**\n> Execute command.\n\n**shell**\n> Interactive shell.\n\n**build**\n> Build container.\n\n**pull**\n> Download image.\n\n**--sandbox**\n> Writable directory.\n\n**--fakeroot**\n> Rootless build."],["DESCRIPTION","**Singularity** (now **Apptainer**) is a container runtime designed for high-performance computing (HPC) environments where Docker's root-privilege model is not practical. Containers run as the invoking user without requiring a daemon or elevated privileges, making it safe for shared multi-tenant clusters.\n\nUnlike Docker, Singularity integrates tightly with the host system by default, sharing the user's home directory, network, and other filesystems. This makes it straightforward to use existing data and tools inside containers. Images are stored as single SIF files that are portable and can be built from Docker images, definition files, or pulled directly from registries.\n\nThe tool enables reproducible science by packaging complete software environments into immutable containers that produce consistent results across different computing systems."],["CAVEATS","Not Docker-compatible at runtime (no Docker socket, no daemon). Building SIF images traditionally requires root or user namespaces; use **--fakeroot** to build without root. Now maintained as two forks: **Apptainer** (Linux Foundation) and **SingularityCE** (Sylabs)."],["HISTORY","**Singularity** was created at **Lawrence Berkeley Lab** for running containers in HPC environments without privileged access."],["INSTALL","```dnf: sudo dnf install apptainer```\n\n```pacman: sudo pacman -S apptainer```\n\n```apk: sudo apk add apptainer```\n\n```zypper: sudo zypper install apptainer```\n\n```brew: brew install apptainer```\n\n```nix: nix profile install nixpkgs#apptainer```"],["SEE ALSO","[docker](/man/docker)(1), [podman](/man/podman)(1), [apptainer](/man/apptainer)(1)"]],"e":["docker","podman","apptainer"],"x":"singularity run [container.sif]"},"skate":{"n":"skate","t":"Personal command-line key-value store","s":[["TAGLINE","Personal command-line key-value store"],["TLDR","**Set a value**\n\n```skate set [key] [value]```\n\n**Get a value**\n\n```skate get [key]```\n\n**List all keys**\n\n```skate list```\n\n**Delete a key**\n\n```skate delete [key]```\n\n**Set from stdin**\n\n```echo \"[value]\" | skate set [key]```\n\n**Use specific database**\n\n```skate set [key] [value] -d [mydb]```\n\n**Sync to GitHub**\n\n```skate sync```\n\n**Link to GitHub gist**\n\n```skate link [gist_url]```"],["SYNOPSIS","**skate** _command_ [_-d database_] [_options_] [_args_]"],["PARAMETERS","**set** _KEY_ _VALUE_\n> Store value.\n\n**get** _KEY_\n> Retrieve value.\n\n**list**\n> List all keys.\n\n**delete** _KEY_\n> Remove key.\n\n**sync**\n> Sync with remote.\n\n**link** _URL_\n> Link to gist.\n\n**-d**, **--database** _NAME_\n> Database name.\n\n**--help**\n> Show help."],["DESCRIPTION","**skate** is a personal key-value store that provides simple, fast storage directly from the command line. It stores arbitrary text data such as API keys, notes, code snippets, and configuration values, accessible by named keys.\n\nData can be organized across multiple named databases using the **-d** flag. The **sync** command backs up data to a GitHub Gist, enabling sharing across machines. Values can be set from command arguments or piped from stdin, making it easy to store command output directly."],["CAVEATS","Data stored locally by default. Gist sync requires GitHub auth. Not for sensitive secrets."],["HISTORY","**Skate** is part of **Charm** tools, created by Charm for beautiful terminal experiences. It provides simple, elegant key-value storage."],["INSTALL","```pacman: sudo pacman -S skate```\n\n```apk: sudo apk add skate```\n\n```brew: brew install skate```\n\n```nix: nix profile install nixpkgs#skate```"],["SEE ALSO","[pass](/man/pass)(1), [secret-tool](/man/secret-tool)(1)"]],"e":["pass","secret-tool"],"x":"skate set [key] [value]"},"squeue":{"n":"squeue","t":"View Slurm job queue status","s":[["TAGLINE","View Slurm job queue status"],["TLDR","**View** the job queue\n\n```squeue```\n\n**View** jobs queued by a specific user\n\n```squeue -u [username]```\n\n**View** the queue and refresh every 5 seconds\n\n```squeue -i 5```\n\n**View** the queue with expected start times\n\n```squeue --start```"],["SYNOPSIS","**squeue** [_options_]"],["PARAMETERS","**-u, --user _username_**\n> Show only jobs owned by specified user\n\n**-i, --iterate _seconds_**\n> Refresh display at specified interval\n\n**--start**\n> Show expected start time for pending jobs\n\n**-j, --jobs _job_list_**\n> Show specific job IDs\n\n**-p, --partition _name_**\n> Show jobs in specific partition\n\n**-t, --states _states_**\n> Filter by job state (pending, running, etc.)\n\n**-l, --long**\n> Long output format\n\n**-o, --format _format_**\n> Customize output format\n\n**-S, --sort _field_**\n> Sort output by field"],["DESCRIPTION","**squeue** displays information about jobs in the Slurm scheduling queue. It shows job ID, partition, name, user, state, time, nodes, and node list for jobs awaiting or running on the cluster.\n\nCommon job states include: **PD** (pending), **R** (running), **CG** (completing), **CD** (completed), **F** (failed), and **CA** (cancelled)."],["CAVEATS","Shows only jobs visible to the querying user based on Slurm's access controls. Very large job queues may benefit from filtering options. The **--start** estimated times are approximations."],["INSTALL","```apt: sudo apt install slurm-client```"],["SEE ALSO","[sbatch](/man/sbatch)(1), [sinfo](/man/sinfo)(1), [scancel](/man/scancel)(1), [scontrol](/man/scontrol)(1)"]],"e":["sbatch","sinfo","scancel","scontrol"],"x":"squeue"},"stratis":{"n":"stratis","t":"Managed pool-based Linux storage","s":[["TAGLINE","Managed pool-based Linux storage"],["TLDR","**Create** a storage pool\n\n```sudo stratis pool create [pool_name] [/dev/sdX] [/dev/sdY]```\n\n**Create** a filesystem in a pool\n\n```sudo stratis filesystem create [pool_name] [volume_name]```\n\n**List** all filesystems\n\n```sudo stratis filesystem list```\n\n**Add** a device to existing pool\n\n```sudo stratis pool add-data [pool_name] [/dev/sdZ]```\n\n**Create** a snapshot\n\n```sudo stratis filesystem snapshot [pool_name] [source_fs] [snapshot_name]```\n\n**Destroy** a filesystem\n\n```sudo stratis filesystem destroy [pool_name] [volume_name]```\n\n**List** pools\n\n```sudo stratis pool list```"],["SYNOPSIS","**stratis** _command_ [_subcommand_] [_arguments_]"],["COMMANDS","**pool create** _name_ _devices_...\n> Create storage pool from block devices\n\n**pool list**\n> List existing pools\n\n**pool add-data** _pool_ _device_\n> Add device to pool\n\n**pool destroy** _pool_\n> Remove a pool (requires no filesystems)\n\n**pool rename** _old_ _new_\n> Rename a pool\n\n**pool init-cache** _pool_ _devices_\n> Initialize SSD cache tier\n\n**filesystem create** _pool_ _name_\n> Create thin filesystem from pool\n\n**filesystem list**\n> List filesystems\n\n**filesystem snapshot** _pool_ _source_ _name_\n> Create filesystem snapshot\n\n**filesystem destroy** _pool_ _name_\n> Remove filesystem\n\n**filesystem rename** _pool_ _old_ _new_\n> Rename filesystem\n\n**blockdev list**\n> List block devices in pools\n\n**daemon version**\n> Show stratisd version"],["DESCRIPTION","**stratis** is a local storage management tool providing ZFS/Btrfs-like features using Linux devicemapper and XFS. It simplifies storage management by abstracting pools of block devices that can be dynamically allocated to thin-provisioned XFS filesystems.\n\nThe stratisd daemon must be running before using stratis commands. Filesystems appear at **/dev/stratis/pool_name/filesystem_name** and support features like snapshots, thin provisioning, and pool expansion."],["MOUNTING","Mount manually:\n\n```\nmount /dev/stratis/pool_name/fs_name /mnt/target\n```\n\nAdd to /etc/fstab:\n\n```\n/dev/stratis/pool/fs /mnt xfs defaults,x-systemd.requires=stratisd.service 0 0\n```"],["CAVEATS","Requires stratisd service running. Uses XFS exclusively; other filesystems not supported. Pool devices should be dedicated; partitions with existing data will be overwritten. Snapshots share space with source until modified."],["HISTORY","**Stratis** was developed by **Red Hat** starting around **2017** to provide easy-to-use storage management for Linux. It was designed to deliver modern storage features without the complexity of LVM or the kernel integration requirements of Btrfs/ZFS. Stratis became available in Fedora 28 and RHEL 8."],["SEE ALSO","[lvm](/man/lvm)(8), [btrfs](/man/btrfs)(8), [xfs](/man/xfs)(5), [systemctl](/man/systemctl)(1)"]],"e":["lvm","btrfs","xfs","systemctl"],"x":"sudo stratis pool create [pool_name] [/dev/sdX] [/dev/sdY]"},"streamer":{"n":"streamer","t":"Capture video from V4L devices","s":[["TAGLINE","Capture video from V4L devices"],["TLDR","**Capture a single JPEG image**\n\n```streamer -o [image.jpeg]```\n\n**Capture from specific video device**\n\n```streamer -c /dev/video[0] -o [image.jpeg]```\n\n**Record video to AVI file**\n\n```streamer -o [video.avi] -f mjpeg -t [00:30]```\n\n**Capture with specific resolution**\n\n```streamer -s [640x480] -o [image.jpeg]```\n\n**Capture sequence of images**\n\n```streamer -o [frame000.jpeg] -t [00:10] -r [5]```\n\n**Record with audio**\n\n```streamer -o [video.avi] -f mjpeg -F stereo -t [01:00]```\n\n**Set color depth**\n\n```streamer -b [24] -o [image.jpeg]```"],["SYNOPSIS","**streamer** [_options_]"],["PARAMETERS","**-o** _file_\n> Output filename. Use numbered pattern (file000.jpeg) for sequences.\n\n**-c** _device_\n> Video capture device (default: /dev/video0).\n\n**-s** _WxH_\n> Capture resolution (e.g., 320x240, 640x480, 720x480).\n\n**-r** _fps_\n> Frames per second for video capture.\n\n**-t** _time_\n> Recording duration in HH:MM:SS or MM:SS format.\n\n**-f** _format_\n> Output format: jpeg, ppm, pgm, mjpeg, rgb24, raw, avi.\n\n**-F** _audio_\n> Audio format: mono or stereo.\n\n**-b** _bpp_\n> Color depth in bits per pixel (15, 16, 24, or 32).\n\n**-n** _norm_\n> TV norm: ntsc, pal, or secam.\n\n**-i** _input_\n> Video input source number.\n\n**-q**\n> Quiet mode, suppress output messages.\n\n**-h**\n> Display help information."],["DESCRIPTION","**streamer** is a command-line utility for capturing still images and video from webcams, TV tuners, and other Video4Linux devices. It is part of the xawtv package and provides scriptable video capture without requiring a graphical interface.\n\nThe tool can capture single images, timed sequences of images, or continuous video recordings. Output formats include JPEG for images and MJPEG AVI for video. When capturing image sequences, numbered filenames are generated automatically if the output filename contains zeros (e.g., frame000.jpeg).\n\nAudio capture is supported for video recordings when using compatible capture devices. The tool is useful for time-lapse photography, surveillance, and automated image capture scenarios."],["CAVEATS","Requires Video4Linux (V4L/V4L2) compatible capture device. Some distributions package streamer separately from xawtv as xawtv-tools or v4l-tools. Format support depends on hardware capabilities. For modern webcams, consider using ffmpeg which offers more format options."],["HISTORY","**streamer** was developed as part of the xawtv project, a suite of analog TV and video capture tools for Linux. It provided command-line capture functionality alongside the graphical xawtv application. The tool predates many modern capture utilities and remains useful for simple V4L-based capture tasks."],["INSTALL","```apt: sudo apt install streamer```"],["SEE ALSO","[ffmpeg](/man/ffmpeg)(1), [v4l2-ctl](/man/v4l2-ctl)(1), [cheese](/man/cheese)(1)"]],"e":["ffmpeg","v4l2-ctl","cheese"],"x":"streamer -o [image.jpeg]"},"systemd-hwdb":{"n":"systemd-hwdb","t":"Manage udev hardware database","s":[["TAGLINE","Manage udev hardware database"],["TLDR","**Update** hardware database\n\n```systemd-hwdb update```\n\n**Query** database for modalias\n\n```systemd-hwdb query [modalias]```\n\nUpdate with **strict** error checking\n\n```systemd-hwdb -s update```\n\nUpdate in **/usr/lib/udev**\n\n```systemd-hwdb --usr update```\n\nUpdate for **alternate root**\n\n```systemd-hwdb -r [/path/to/root] update```"],["SYNOPSIS","**systemd-hwdb** [_OPTIONS_] _COMMAND_"],["COMMANDS","**update**\n> Compile hwdb source files into binary database\n\n**query** _MODALIAS_\n> Query database for a specific modalias"],["PARAMETERS","**-s, --strict**\n> Return non-zero exit on parsing errors\n\n**--usr**\n> Update database in /usr/lib/udev instead of /etc/udev\n\n**-r, --root=** _PATH_\n> Operate on alternate root filesystem"],["DESCRIPTION","**systemd-hwdb** manages the udev hardware database, which maps hardware identifiers (modalias) to properties and metadata. The database is compiled from text files in `/usr/lib/udev/hwdb.d/` and `/etc/udev/hwdb.d/` into a binary format for fast lookup.\n\nThis database is used by udev rules to identify hardware and set appropriate properties."],["CAVEATS","Must run `systemd-hwdb update` after modifying hwdb source files. The binary database is architecture-specific. Changes in /etc override /usr/lib entries."],["HISTORY","**systemd-hwdb** replaces the older udev hwdb management, providing hardware identification data used throughout the system for device setup."],["INSTALL","```apt: sudo apt install udev```\n\n```zypper: sudo zypper install udev```\n\n```nix: nix profile install nixpkgs#udev```"],["SEE ALSO","[udevadm](/man/udevadm)(8), [hwdb](/man/hwdb)(7)"]],"e":["udevadm","hwdb"],"x":"systemd-hwdb update"},"systemd-stdio-bridge":{"n":"systemd-stdio-bridge","t":"Bridge stdio to D-Bus connection","s":[["TAGLINE","Bridge stdio to D-Bus connection"],["TLDR","Forward to **system** bus\n\n```systemd-stdio-bridge```\n\nForward to **user** bus\n\n```systemd-stdio-bridge --user```\n\nForward to **container** bus\n\n```systemd-stdio-bridge -M [container]```\n\nForward to **custom** bus address\n\n```systemd-stdio-bridge -p unix:path=[/path/to/socket]```"],["SYNOPSIS","**systemd-stdio-bridge** [_OPTIONS_]"],["PARAMETERS","**--user**\n> Connect to user D-Bus session\n\n**--system**\n> Connect to system D-Bus (default)\n\n**-M, --machine=** _CONTAINER_\n> Connect to D-Bus in specified container\n\n**-p, --bus-path=** _ADDRESS_\n> Connect to custom D-Bus address"],["DESCRIPTION","**systemd-stdio-bridge** implements a proxy between stdin/stdout and a D-Bus connection. It expects an open connection via stdin/stdout and creates a new connection to the specified bus.\n\nThis enables remote D-Bus access via SSH or similar transport mechanisms, bridging external connections to the local bus."],["CAVEATS","Expects a properly formatted D-Bus connection on stdin. Used primarily for remote D-Bus access scenarios. Requires appropriate permissions on the target bus."],["HISTORY","**systemd-stdio-bridge** enables remote D-Bus access patterns, supporting tools like `busctl` and `systemctl` when operating on remote systems via SSH."],["INSTALL","```apt: sudo apt install systemd```\n\n```dnf: sudo dnf install systemd```\n\n```pacman: sudo pacman -S systemd```\n\n```zypper: sudo zypper install systemd```\n\n```brew: brew install systemd```\n\n```nix: nix profile install nixpkgs#systemd```"],["SEE ALSO","[busctl](/man/busctl)(1), [dbus-daemon](/man/dbus-daemon)(1)"]],"e":["busctl","dbus-daemon"],"x":"systemd-stdio-bridge"},"tea":{"n":"tea","t":"Gitea command-line client","s":[["TAGLINE","Gitea command-line client"],["TLDR","**Log in to a Gitea instance**\n\n```tea login add --url [https://gitea.example.com] --token [your_token]```\n\n**List issues** in the current repository\n\n```tea issues```\n\n**Create a new issue**\n\n```tea issues create --title \"[Bug report]\" --body \"[Description]\"```\n\n**List pull requests**\n\n```tea pulls```\n\n**Create a pull request**\n\n```tea pulls create --title \"[Feature]\" --base [main] --head [feature-branch]```\n\n**Checkout a pull request** locally\n\n```tea pulls checkout [42]```\n\n**List repositories**\n\n```tea repos```\n\n**Clone a repository**\n\n```tea clone [owner/repo]```"],["SYNOPSIS","**tea** _command_ [_subcommand_] [_options_]"],["COMMANDS","**logins**\n> Manage Gitea server authentications (add, list, delete, default)\n\n**logout**\n> Remove authentication from a Gitea server\n\n**whoami**\n> Display currently authenticated user\n\n**issues**\n> Create, list, and manage issues (create, list, edit, close, reopen)\n\n**pulls**, **pr**\n> Manage pull requests (create, list, checkout, merge, approve, reject)\n\n**repos**\n> Display and manage repositories (list, create, fork, delete, migrate)\n\n**branches**\n> Examine and manage branches (list, protect, unprotect)\n\n**labels**\n> Manage issue labels (create, list, update, delete)\n\n**milestones**\n> Track project milestones (create, list, close, reopen)\n\n**releases**\n> Manage releases (create, list, delete, edit, assets)\n\n**organizations**\n> Manage organizations (list, create, delete)\n\n**times**\n> Track time on issues\n\n**notifications**\n> View and manage notifications\n\n**comment**\n> Add comments to issues or pull requests\n\n**open**\n> Open repository in web browser\n\n**clone**\n> Clone a repository"],["PARAMETERS","**--login**, **-l** _name_\n> Use a specific login configuration\n\n**--repo**, **-r** _owner/repo_\n> Specify repository (default: detected from current directory)\n\n**--output**, **-o** _format_\n> Output format: simple, table, csv, tsv, yaml, json\n\n**--help**, **-h**\n> Show help for command\n\n**--version**, **-v**\n> Show version"],["DESCRIPTION","**tea** is a command-line client for interacting with Gitea servers. It provides functionality similar to GitHub's **gh** CLI, allowing management of repositories, issues, pull requests, and other entities directly from the terminal.\n\nThe tool uses context from the current git repository when available, detecting the remote Gitea server and repository automatically. It works best with an upstream/fork workflow where the local main branch tracks the upstream repository.\n\nMultiple Gitea instances can be configured via **tea logins add**, with one set as default. Configuration is stored in **$XDG_CONFIG_HOME/tea** (typically ~/.config/tea).\n\nUseful commands include **tea pulls checkout** to fetch and switch to a PR branch locally, and **tea open** to quickly open the current repository in a web browser."],["CAVEATS","tea assumes local git state is published to the remote before performing operations. Some commands require appropriate permissions on the Gitea server. The tool is designed specifically for Gitea and may not work with other forges."],["HISTORY","**tea** was developed by the **Gitea** project as the official command-line interface for Gitea servers. Written in **Go**, it uses the Gitea SDK to interact with the Gitea API. The project is hosted on Gitea's own platform at gitea.com/gitea/tea."],["INSTALL","```apt: sudo apt install tea```\n\n```pacman: sudo pacman -S tea```\n\n```apk: sudo apk add tea```\n\n```zypper: sudo zypper install tea```\n\n```brew: brew install tea```\n\n```nix: nix profile install nixpkgs#tea```"],["SEE ALSO","[git](/man/git)(1), [gh](/man/gh)(1), [glab](/man/glab)(1), [hub](/man/hub)(1)"]],"e":["git","gh","glab","hub"],"x":"tea login add --url [https://gitea.example.com] --token [your_token]"},"tegratop":{"n":"tegratop","t":"TUI monitoring tool for NVIDIA Jetson boards","s":[["TAGLINE","TUI monitoring tool for NVIDIA Jetson boards"],["TLDR","**Launch the Jetson monitoring dashboard**\n\n```tegratop```"],["SYNOPSIS","**tegratop**"],["DESCRIPTION","**tegratop** is a top-like TUI tool for monitoring NVIDIA Jetson boards. It displays GPU utilization, CPU cores, memory usage, fan speed, power consumption, disk space, engine status, and network traffic on a single page."],["CAVEATS","Requires an NVIDIA Jetson board. May need elevated privileges for full sensor access."],["HISTORY","**tegratop** was created by **Badr** (pythops) and is written in **Rust**."],["INSTALL","```aur: yay -S tegratop```"],["SEE ALSO","[nvtop](/man/nvtop)(1), [htop](/man/htop)(1)"]],"e":["nvtop","htop"],"x":"tegratop"},"tilda":{"n":"tilda","t":"Drop-down terminal emulator","s":[["TAGLINE","Drop-down terminal emulator"],["TLDR","**Start Tilda terminal**\n\n```tilda```\n\n**Start with D-Bus support** for Wayland\n\n```tilda --dbus```\n\n**Toggle specific Tilda instance**\n\n```tilda -T [0]```\n\n**Start hidden**\n\n```tilda --hidden```\n\n**Use specific config file**\n\n```tilda -c [~/.config/tilda/config_0]```\n\n**Show version**\n\n```tilda --version```\n\n**Display help**\n\n```tilda --help```"],["SYNOPSIS","**tilda** [_options_]"],["PARAMETERS","**--dbus**\n> Enable D-Bus interface for window toggling.\n\n**-T**, **--toggle** _n_\n> Toggle visibility of instance n.\n\n**--hidden**\n> Start with window hidden.\n\n**-c**, **--config** _file_\n> Use specified configuration file.\n\n**-C**\n> Open configuration wizard (deprecated, use right-click menu).\n\n**--version**\n> Display version information.\n\n**-h**, **--help**\n> Display help information."],["KEYBOARD SHORTCUTS","**F1**: Pull down/up terminal (default hotkey)\n\n**Shift+Ctrl+T**: Open new tab\n\n**Shift+Ctrl+W**: Close current tab\n\n**Shift+Ctrl+PageUp**: Move tab left\n\n**Shift+Ctrl+PageDown**: Move tab right\n\n**Shift+Ctrl+C**: Copy selected text\n\n**Shift+Ctrl+V**: Paste clipboard\n\n**Shift+Ctrl+F**: Search\n\n**F11**: Toggle fullscreen\n\n**F12**: Toggle transparency\n\n**Shift+Ctrl+Q**: Quit Tilda"],["DESCRIPTION","**Tilda** is a GTK-based drop-down terminal emulator for Linux. It slides down from the top of the screen when activated by a hotkey, similar to the console in Quake-style games.\n\nThe terminal supports tabs, transparency, customizable appearance, and can be configured to auto-hide when losing focus. Configuration is done through the preferences dialog accessible by right-clicking the terminal window.\n\nMultiple Tilda instances can run simultaneously, each with its own configuration file (config_0, config_1, etc.) stored in ~/.config/tilda/. The D-Bus interface enables integration with desktop environments for global hotkey support, particularly useful on Wayland."],["CAVEATS","On Wayland, requires --dbus flag and desktop environment hotkey configuration. The first instance uses config_0. Global hotkey may conflict with other applications. Some transparency features require a compositing window manager."],["HISTORY","**Tilda** was inspired by the drop-down console found in first-person shooter games like Quake. It was created to provide quick terminal access without managing separate windows. The project continues to be maintained with support for modern features like Wayland compatibility."],["INSTALL","```apt: sudo apt install tilda```\n\n```dnf: sudo dnf install tilda```\n\n```pacman: sudo pacman -S tilda```\n\n```apk: sudo apk add tilda```\n\n```zypper: sudo zypper install tilda```\n\n```nix: nix profile install nixpkgs#tilda```"],["SEE ALSO","[guake](/man/guake)(1), [yakuake](/man/yakuake)(1), [gnome-terminal](/man/gnome-terminal)(1), [xterm](/man/xterm)(1)"]],"e":["guake","yakuake","gnome-terminal","xterm"],"x":"tilda"},"toktop":{"n":"toktop","t":"LLM token usage monitor in the terminal","s":[["TAGLINE","LLM token usage monitor in the terminal"],["TLDR","**Launch the LLM usage dashboard**\n\n```toktop```"],["SYNOPSIS","**toktop** [_options_]"],["DESCRIPTION","**toktop** is a terminal dashboard for monitoring OpenAI and Anthropic API token usage and costs. It allows account administrators to track LLM API usage without switching to web dashboards."],["HISTORY","**toktop** was created by **htin1** and is written in **Rust**."],["INSTALL","```brew: brew install toktop```"],["SEE ALSO","[htop](/man/htop)(1), [tenere](/man/tenere)(1), [aichat](/man/aichat)(1)"]],"e":["htop","tenere","aichat"],"x":"toktop"},"unsquashfs":{"n":"unsquashfs","t":"Extract files from squashfs filesystems","s":[["TAGLINE","Extract files from squashfs filesystems"],["TLDR","**Extract** to squashfs-root\n\n```unsquashfs [filesystem.squashfs]```\n\nExtract to **specific** directory\n\n```unsquashfs -d [path/to/directory] [filesystem.squashfs]```\n\nShow **filenames** during extraction\n\n```unsquashfs -i [filesystem.squashfs]```\n\nShow files with **attributes**\n\n```unsquashfs -li [filesystem.squashfs]```\n\n**List** files without extracting\n\n```unsquashfs -l [filesystem.squashfs]```\n\n**List** files with attributes\n\n```unsquashfs -ll [filesystem.squashfs]```\n\n**Extract** specific files or directories\n\n```unsquashfs [filesystem.squashfs] [path/to/file] [path/to/dir]```\n\nDisplay filesystem **superblock** information\n\n```unsquashfs -s [filesystem.squashfs]```"],["SYNOPSIS","**unsquashfs** [_OPTIONS_] _FILESYSTEM_"],["PARAMETERS","**-d, -dest** _DIR_\n> Extract to specified directory\n\n**-i, -info**\n> Display filenames during extraction\n\n**-li, -linfo**\n> Display filenames and attributes during extraction\n\n**-l, -ls**\n> List files without extracting\n\n**-ll, -lls**\n> List files with attributes without extracting\n\n**-f, -force**\n> Overwrite files that already exist\n\n**-s, -stat**\n> Display filesystem superblock information\n\n**-n, -no-progress**\n> Don't display the progress bar\n\n**-p** _NUMBER_, **-processors** _NUMBER_\n> Use NUMBER processors for decompression\n\n**-e** _FILE_, **-ef** _FILE_\n> List of directories or files to extract, one per line"],["DESCRIPTION","**unsquashfs** extracts and lists files from squashfs compressed filesystems. Squashfs is a read-only compressed filesystem commonly used for Linux distributions, live CDs, and embedded systems.\n\nBy default, files are extracted to a squashfs-root directory in the current working directory."],["CAVEATS","Extraction may require significant disk space. Some filesystems contain device nodes requiring root. Original permissions and ownership are preserved when run as root."],["INSTALL","```apt: sudo apt install squashfs-tools```\n\n```dnf: sudo dnf install squashfs-tools```\n\n```pacman: sudo pacman -S squashfs-tools```\n\n```apk: sudo apk add squashfs-tools```"],["SEE ALSO","[mksquashfs](/man/mksquashfs)(1), [sqfscat](/man/sqfscat)(1)"]],"e":["mksquashfs","sqfscat"],"x":"unsquashfs [filesystem.squashfs]"},"userdbctl":{"n":"userdbctl","t":"Query systemd user and group database","s":[["TAGLINE","Query systemd user and group database"],["TLDR","List all **users**\n\n```userdbctl user```\n\nShow **specific** user\n\n```userdbctl user [username]```\n\nList all **groups**\n\n```userdbctl group```\n\nShow **specific** group\n\n```userdbctl group [groupname]```\n\nList **services** providing definitions\n\n```userdbctl services```"],["SYNOPSIS","**userdbctl** [_OPTIONS_] _COMMAND_ [_NAME_]"],["COMMANDS","**user** [_NAME_]\n> List users or show specific user\n\n**group** [_NAME_]\n> List groups or show specific group\n\n**services**\n> List services providing user/group definitions"],["DESCRIPTION","**userdbctl** inspects users, groups, and group memberships on the system. It provides a unified interface to query user and group information from various sources including /etc/passwd, LDAP, and systemd-homed.\n\nThe tool aggregates information from all services registered with systemd-userdbd."],["CAVEATS","Systemd-based systems only. Output format differs from traditional getent. Some sources may require additional services running."],["INSTALL","```apt: sudo apt install systemd-userdbd```"],["SEE ALSO","[getent](/man/getent)(1), [id](/man/id)(1), [homectl](/man/homectl)(1)"]],"e":["getent","id","homectl"],"x":"userdbctl user"},"weathr":{"n":"weathr","t":"Terminal weather app with ASCII animations","s":[["TAGLINE","Terminal weather app with ASCII animations"],["TLDR","**Display current weather** for your location (auto-detected via IP)\n\n```weathr```\n\n**Use imperial units** (Fahrenheit, mph, inches)\n\n```weathr --imperial```\n\n**Simulate a specific weather condition**\n\n```weathr --simulate [rain|snow|thunderstorm|fog|clear|...]```\n\n**Simulate nighttime** weather\n\n```weathr --simulate [condition] --night```\n\n**Hide the status HUD** and show only the animation\n\n```weathr --hide-hud```\n\n**Add falling leaves** effect\n\n```weathr --leaves```"],["SYNOPSIS","**weathr** [**--imperial** | **--metric**] [**--simulate** _condition_] [**--night**] [**--leaves**] [**--auto-location**] [**--hide-location**] [**--hide-hud**] [**--silent**]"],["PARAMETERS","**--simulate** _condition_\n> Simulate a weather condition instead of using real data. Conditions: **clear**, **partly-cloudy**, **cloudy**, **overcast**, **fog**, **drizzle**, **rain**, **freezing-rain**, **rain-showers**, **snow**, **snow-grains**, **snow-showers**, **thunderstorm**, **thunderstorm-hail**\n\n**--night**\n> Simulate nighttime visuals (moon, stars, fireflies)\n\n**--leaves**\n> Add falling leaves animation (autumn effect)\n\n**--imperial**\n> Use imperial units: Fahrenheit, mph, inches\n\n**--metric**\n> Use metric units: Celsius, km/h, mm (default)\n\n**--auto-location**\n> Auto-detect location via IP geolocation (ipinfo.io)\n\n**--hide-location**\n> Hide location coordinates from the display\n\n**--hide-hud**\n> Hide the status HUD and show only the ASCII animation\n\n**--silent**\n> Suppress non-error messages\n\n**-h**, **--help**\n> Print help information\n\n**-V**, **--version**\n> Print version information"],["DESCRIPTION","**weathr** is a terminal weather application that displays real-time weather data with animated ASCII art. It fetches current conditions from the **Open-Meteo** API and renders them as colorful terminal animations including rain, snow, thunderstorms, flying airplanes, drifting clouds, and birds.\n\nThe display adapts to time of day: daytime shows the sun, gliding clouds, V-shaped birds, and bright colors, while nighttime features the moon, twinkling stars, gliding clouds, fireflies, and shooting stars. A status HUD shows the weather category, temperature, wind speed, and precipitation.\n\n**weathr** supports cross-platform use on Linux, macOS, Windows, and NixOS. It respects the **NO_COLOR** environment variable for accessibility."],["CONFIGURATION","Configuration is stored in a TOML file:\n> **Linux**: ```~/.config/weathr/config.toml```\n> **macOS**: ```~/Library/Application Support/weathr/config.toml```\n> **Windows**: ```~/AppData/Roaming/weathr/config.toml```\n\nExample configuration:\n\n```\nhide_hud = false\nsilent = false\n\n[location]\nlatitude = 40.7128\nlongitude = -74.0060\nauto = false\nhide = false\n\n[units]\ntemperature = \"celsius\"\nwind_speed = \"kmh\"\nprecipitation = \"mm\"\n```\n\n**temperature**: ```celsius``` or ```fahrenheit```\n**wind_speed**: ```kmh```, ```ms```, ```mph```, or ```kn``` (knots)\n**precipitation**: ```mm``` or ```inch```\n\nSetting **auto = true** under **[location]** uses IP-based geolocation. Providing explicit **latitude** and **longitude** avoids external API calls for privacy."],["CAVEATS","Location auto-detection relies on **ipinfo.io** for IP geolocation, which may be inaccurate behind VPNs or proxies. Weather data comes from **Open-Meteo**, which is free but may have reduced accuracy in remote regions. The application requires a terminal with truecolor support for the best visual experience; it falls back to basic colors when **COLORTERM** is not set to \"truecolor\" or \"24bit\"."],["HISTORY","**weathr** was created by **Veirt** and written in **Rust**. The project is hosted on GitHub and licensed under **GPL-3.0-or-later**. Weather data is sourced from **Open-Meteo.com** (CC BY 4.0 licensed). The latest release is **v1.3.0** from February 2026."],["INSTALL","```nix: nix profile install nixpkgs#weathr```"],["SEE ALSO","[ansiweather](/man/ansiweather)(1), [wego](/man/wego)(1), [curl](/man/curl)(1)"]],"e":["ansiweather","wego","curl"],"x":"weathr"},"xmlto":{"n":"xmlto","t":"Convert XML to various output formats","s":[["TAGLINE","Convert XML to various output formats"],["TLDR","**Convert to HTML**\n\n```xmlto html [document.xml]```\n\n**Convert to PDF**\n\n```xmlto pdf [document.xml]```\n\n**Convert to man page**\n\n```xmlto man [document.xml]```\n\n**Output directory**\n\n```xmlto -o [output/] html [document.xml]```\n\n**With stylesheet**\n\n```xmlto -x [custom.xsl] html [document.xml]```"],["SYNOPSIS","**xmlto** [_-o dir_] [_-x stylesheet_] [_options_] _format_ _file_"],["PARAMETERS","**-o** _DIR_\n> Output directory.\n\n**-x** _XSL_\n> Custom stylesheet.\n\n**-v**\n> Verbose.\n\n**--skip-validation**\n> Skip DTD validation.\n\n_format_\n> Output format."],["DESCRIPTION","**xmlto** is a frontend tool for converting XML documents to various output formats using XSL stylesheets. It supports generating HTML, PDF, man pages, plain text, and other formats from XML source files, with particular strength in processing DocBook documentation.\n\nThe tool acts as a wrapper around XSLT processors and formatting backends, handling the details of stylesheet selection and tool invocation. Custom stylesheets can be provided with the **-x** option to override the default conversion behavior and tailor the output to specific requirements.\n\nxmlto is commonly used in software documentation workflows where source documentation is maintained in DocBook XML format and needs to be published in multiple output formats for different audiences."],["CAVEATS","Requires DocBook XSL stylesheets and an XSLT processor (xsltproc) to be installed. PDF output additionally requires a formatting backend like FOP or dblatex. Primarily designed for DocBook XML; other XML vocabularies need custom stylesheets."],["HISTORY","**xmlto** was created for converting XML documents, particularly DocBook, to various output formats."],["INSTALL","```apt: sudo apt install xmlto```\n\n```dnf: sudo dnf install xmlto```\n\n```pacman: sudo pacman -S xmlto```\n\n```apk: sudo apk add xmlto```\n\n```zypper: sudo zypper install xmlto```\n\n```brew: brew install xmlto```\n\n```nix: nix profile install nixpkgs#xmlto```"],["SEE ALSO","[xsltproc](/man/xsltproc)(1), [docbook2html](/man/docbook2html)(1), [pandoc](/man/pandoc)(1)"]],"e":["xsltproc","docbook2html","pandoc"],"x":"xmlto html [document.xml]"},"xxhsum":{"n":"xxhsum","t":"Print or check xxHash non-cryptographic checksums","s":[["TAGLINE","Print or check xxHash non-cryptographic checksums"],["TLDR","Calculate **XXH64 checksum** (default)\n\n```xxhsum [path/to/file]```\n\nCalculate **XXH32 checksum**\n\n```xxhsum -H0 [path/to/file]```\n\nCalculate **XXH128 checksum**\n\n```xxhsum -H2 [path/to/file]```\n\n**Verify checksums** from a file\n\n```xxhsum -c [path/to/checksums.xxh]```\n\nRun **benchmark**\n\n```xxhsum -b```"],["SYNOPSIS","**xxhsum** [_OPTION_]... [_FILE_...]\n\n**xxhsum -b** [_OPTION_]..."],["PARAMETERS","**-H** _HASHTYPE_\n> Hash algorithm: 0 (XXH32), 1 (XXH64, default), 2 (XXH128), 3 (XXH3). Also accepts 32, 64, 128 as aliases.\n\n**-b**\n> Activate benchmark mode\n\n**-B** _BLOCKSIZE_\n> Set benchmark block size in bytes (default: 102400)\n\n**-i** _ITERATIONS_\n> Set number of benchmark iterations (default: 3)\n\n**-c, --check** _FILE_\n> Verify checksums from file\n\n**-q, --quiet**\n> Suppress OK messages during verification\n\n**-w, --warn**\n> Warn about malformed checksum lines\n\n**--strict**\n> Return error on invalid checksum lines\n\n**--status**\n> Silent verification; indicate result only via exit code\n\n**--tag**\n> Output in BSD-style format\n\n**--little-endian**\n> Display checksum in little-endian format (default is big-endian)\n\n**-h, --help**\n> Display help\n\n**-V, --version**\n> Display version"],["DESCRIPTION","**xxhsum** prints or checks xxHash (32, 64, or 128 bit) checksums. xxHash is an extremely fast non-cryptographic hash function, suitable for integrity checking where speed is important.\n\nWhen no FILE is specified, reads from standard input. The command line syntax is similar to md5sum(1).\n\nEquivalent shortcut commands: **xxh32sum** (same as xxhsum -H0), **xxh64sum** (same as xxhsum -H1), **xxh128sum** (same as xxhsum -H2)."],["CAVEATS","Not cryptographically secure. Should not be used for security-sensitive applications. For security, use SHA-256 or similar."],["INSTALL","```apt: sudo apt install xxhash```\n\n```dnf: sudo dnf install xxhash```\n\n```pacman: sudo pacman -S xxhash```\n\n```apk: sudo apk add xxhash```\n\n```zypper: sudo zypper install xxhash```\n\n```brew: brew install xxhash```\n\n```nix: nix profile install nixpkgs#xxhash```"],["SEE ALSO","[md5sum](/man/md5sum)(1), [sha256sum](/man/sha256sum)(1), [b2sum](/man/b2sum)(1), [cksum](/man/cksum)(1)"]],"e":["md5sum","sha256sum","b2sum","cksum"],"x":"xxhsum [path/to/file]"},"yesod":{"n":"yesod","t":"Haskell type-safe web framework CLI","s":[["TAGLINE","Haskell type-safe web framework CLI"],["TLDR","**Create project**\n\n```yesod init```\n\n**Run development server**\n\n```yesod devel```\n\n**Add handler**\n\n```yesod add-handler [HandlerName]```\n\n**Generate Keter bundle**\n\n```yesod keter```"],["SYNOPSIS","**yesod** _command_ [_options_]"],["PARAMETERS","**init**\n> Scaffold a new Yesod project interactively.\n\n**devel**\n> Run the development server with automatic recompilation on file changes.\n\n**add-handler**\n> Generate a new route handler with boilerplate code.\n\n**keter**\n> Build a Keter deployment bundle (.keter archive).\n\n**test**\n> Run the project's test suite.\n\n**version**\n> Display yesod-bin version information.\n\n**-v, --verbose**\n> Enable verbose output.\n\n**--help**\n> Show usage information."],["DESCRIPTION","**yesod** is the command-line tool for the Yesod web framework, which builds type-safe web applications in Haskell. It provides project scaffolding, development server management, and deployment bundling.\n\nThe **init** command creates a new project with a complete directory structure, configuration, and dependencies. The **devel** subcommand starts a development server that watches for file changes and recompiles automatically, providing live reload during development.\n\nRoute handlers can be added with **add-handler**, which generates boilerplate code following Yesod conventions. The **keter** command packages the application into a deployment bundle for the Keter web application deployment system."],["CAVEATS","Haskell knowledge required. Stack/Cabal needed. Learning curve."],["HISTORY","**Yesod** was created by **Michael Snoyman** as a type-safe Haskell web framework."],["INSTALL","```apt: sudo apt install yesod```"],["SEE ALSO","[stack](/man/stack)(1), [cabal](/man/cabal)(1), [ghc](/man/ghc)(1)"]],"e":["stack","cabal","ghc"],"x":"yesod init"}});
