window.LCL_BUCKET(51,{"aa-cleanprof":{"n":"aa-cleanprof","t":"Clean AppArmor profiles by removing redundant rules","s":[["TAGLINE","Clean AppArmor profiles by removing redundant rules"],["TLDR","**Clean** a profile to remove unused rules\n\n```sudo aa-cleanprof [profile_name]```\n\nClean **multiple profiles** at once\n\n```sudo aa-cleanprof [profile1] [profile2]```\n\nSpecify the **directory** containing profiles\n\n```sudo aa-cleanprof -d /path/to/profiles [profile_name]```\n\nRun **silently** without prompts\n\n```sudo aa-cleanprof -s [profile_name]```\n\nPrevent profile **reload** after cleaning\n\n```sudo aa-cleanprof --no-reload [profile_name]```"],["SYNOPSIS","**aa-cleanprof** _executable_ [_executable_ ...] [_-d /path/to/profiles_] [_--no-reload_] [_-s_]"],["DESCRIPTION","**aa-cleanprof** removes superfluous rules from AppArmor security profiles, including rules that are already covered by an include directive or another rule. The utility also reorders rules to group similar ones together and removes all comments from the profile file."],["PARAMETERS","**-d, --dir /path/to/profiles**\n> Specifies the directory containing AppArmor profiles; defaults to /etc/apparmor.d\n\n**--no-reload**\n> Prevents the profile from being reloaded after modifications\n\n**-s, --silent**\n> Automatically overwrites the profile without prompting the user\n\n**-h, --help**\n> Display help information"],["CAVEATS","This utility removes all comments from profile files, which may eliminate useful documentation or notes within the profiles."],["HISTORY","Part of the **AppArmor** utilities package for managing application security profiles on Linux systems."],["INSTALL","```apk: sudo apk add apparmor-utils```\n\n```zypper: sudo zypper install apparmor-utils```\n\n```nix: nix profile install nixpkgs#apparmor-utils```"],["SEE ALSO","[aa-logprof](/man/aa-logprof)(8), [aa-genprof](/man/aa-genprof)(8), [aa-enforce](/man/aa-enforce)(8), [aa-complain](/man/aa-complain)(8), [aa-disable](/man/aa-disable)(8), [apparmor](/man/apparmor)(7)"],["RESOURCES","```[Source code](https://gitlab.com/apparmor/apparmor)```\n\n```[Homepage](https://apparmor.net/)```\n\n```[Documentation](https://gitlab.com/apparmor/apparmor/-/wikis/home)```"]],"e":["aa-logprof","aa-genprof","aa-enforce","aa-complain","aa-disable","apparmor"],"x":"sudo aa-cleanprof [profile_name]"},"abduco":{"n":"abduco","t":"lightweight terminal session manager","s":[["TAGLINE","lightweight terminal session manager"],["TLDR","**Create** a new session with a command\n\n```abduco -c [session_name] [command]```\n\n**Attach** to an existing session\n\n```abduco -a [session_name]```\n\n**List** all active sessions\n\n```abduco```\n\nCreate a session and **detach** immediately\n\n```abduco -n [session_name] [command]```"],["SYNOPSIS","**abduco** [_-e detach-key_] [_-r_] [_-f_] [_-l_] _-c|-A|-a_ _session_ [_command_ [_args_]]"],["DESCRIPTION","**abduco** (Latin: \"to lead away, carry off\") is a lightweight session management tool that provides terminal session detachment and reattachment. It allows processes to continue running when you disconnect from a terminal and reattach later.\n\nUnlike **screen** or **tmux**, abduco focuses solely on session management without window multiplexing or split panes. This minimalist approach results in a small, fast, and reliable tool. For multiplexing features, it pairs well with **dvtm**."],["PARAMETERS","**-c** _session_\n> Create a new session with the given name\n\n**-a** _session_\n> Attach to an existing session (read-only if already attached)\n\n**-A** _session_\n> Attach to existing session or create if it doesn't exist\n\n**-n** _session_\n> Create a new detached session\n\n**-e** _key_\n> Set the detach key (default: Ctrl+\\\\)\n\n**-r**\n> Enable read-only mode when attaching\n\n**-f**\n> Force creation even if session exists\n\n**-l**\n> Low bandwidth mode (reduced screen updates)"],["CAVEATS","**abduco** only handles session management; it does not provide window splitting or multiplexing. For those features, combine it with **dvtm**. Sessions are stored in /tmp or $ABDUCO_SOCKET_DIR and may be lost on system reboot unless configured otherwise."],["HISTORY","**abduco** was written by Marc Andre Tanner and first released in **2014**. It was designed as a minimal alternative to GNU Screen and tmux, following the Unix philosophy of doing one thing well. The tool is often paired with **dvtm** (dynamic virtual terminal manager) by the same author."],["INSTALL","```dnf: sudo dnf install abduco```\n\n```pacman: sudo pacman -S abduco```\n\n```apk: sudo apk add abduco```\n\n```brew: brew install abduco```\n\n```nix: nix profile install nixpkgs#abduco```"],["SEE ALSO","[tmux](/man/tmux)(1), [screen](/man/screen)(1), [dvtm](/man/dvtm)(1), [dtach](/man/dtach)(1)"]],"e":["tmux","screen","dvtm","dtach"],"x":"abduco -c [session_name] [command]"},"amdgpu_top":{"n":"amdgpu_top","t":"Monitor AMD GPU utilization in real time","s":[["TAGLINE","Monitor AMD GPU utilization in real time"],["TLDR","**Start the TUI** to monitor AMD GPU usage\n\n```amdgpu_top```\n\n**Launch the graphical interface** mode\n\n```amdgpu_top --gui```\n\n**Launch simple TUI mode** similar to nvidia-smi\n\n```amdgpu_top --smi```\n\n**Dump AMDGPU info** (specifications, VRAM, PCI, VBIOS)\n\n```amdgpu_top -d```\n\n**Output JSON formatted data** for scripting\n\n```amdgpu_top -d --json```\n\n**Display GPU processes** and memory usage per process\n\n```amdgpu_top -p```\n\n**Select a specific GPU** by instance number\n\n```amdgpu_top -i [0]```\n\n**Dump GPU metrics** for all AMD GPUs\n\n```amdgpu_top --gpu-metrics```"],["SYNOPSIS","**amdgpu_top** [_options_]"],["PARAMETERS","**-d**, **--dump**\n> Dump AMDGPU info including specifications, VRAM, PCI, ResizableBAR, VBIOS, and video caps.\n\n**--list**\n> Display a list of available AMDGPU devices.\n\n**-J**, **--json**\n> Output JSON formatted data. Can be combined with the -d or --gpu-metrics option.\n\n**--gui**\n> Launch graphical interface mode.\n\n**--smi**\n> Launch simple TUI mode similar to nvidia-smi or rocm-smi.\n\n**-p**, **--process**\n> Dump all GPU processes and memory usage per process.\n\n**-i** _INDEX_\n> Select GPU instance by index number.\n\n**--pci** _PATH_\n> Specify PCI path in domain:bus:dev.func format.\n\n**--apu**, **--select-apu**\n> Select APU instance.\n\n**--single**, **--single-gpu**\n> Display only the selected APU/GPU.\n\n**--no-pc**\n> Disable reading performance counters (GRBM, GRBM2). Useful to avoid deactivating power saving features.\n\n**-gm**, **--gpu_metrics**, **--gpu-metrics**\n> Dump gpu_metrics for all AMD GPUs.\n\n**--pp_table**, **--pp-table**\n> Dump power/performance table from sysfs and VBIOS for all AMD GPUs.\n\n**--drm-info**\n> Dump DRM information.\n\n**--json_fifo**, **--json-fifo** _PATH_\n> Output JSON formatted data to a FIFO (named pipe) for other applications and scripts.\n\n**--decode-gm**, **--decode-gpu-metrics** _PATH_\n> Decode the specified gpu_metrics file.\n\n**-s** _MS_\n> Set refresh interval in milliseconds for JSON mode (default: 1000ms).\n\n**-n** _COUNT_\n> Set maximum iteration count for JSON mode (0 = infinite).\n\n**-u**, **--update-process-index** _SEC_\n> Update interval in seconds for fdinfo (default: 5s).\n\n**--dark**, **--dark-mode**\n> Enable dark mode for TUI/GUI.\n\n**--light**, **--light-mode**\n> Enable light mode for TUI/GUI.\n\n**--gl**, **--opengl**\n> Use OpenGL API for GUI backend.\n\n**--vk**, **--vulkan**\n> Use Vulkan API for GUI backend.\n\n**-V**, **--version**\n> Print version information.\n\n**-h**, **--help**\n> Print help information."],["DESCRIPTION","**amdgpu_top** is an AMD GPU monitoring tool that displays real-time utilization data similar to htop but specifically for AMD graphics hardware. It gathers information from performance counters (GRBM, GRBM2), sensors, fdinfo, and the AMDGPU driver to provide comprehensive GPU metrics.\n\nThe tool offers three display modes: a full-featured TUI (default), a simplified SMI-style TUI, and a graphical interface. It monitors GPU activity, VRAM usage, temperature, power consumption, fan speed, and per-process GPU utilization."],["TUI KEYBINDINGS","**g** - Toggle GRBM performance counter\n**r** - Toggle GRBM2 performance counter\n**v** - Toggle VRAM/GTT usage display\n**f** - Toggle fdinfo display\n**n** - Toggle sensors display\n**m** - Toggle GPU metrics display\n**h** - Change update interval (high=100ms, low=1000ms)\n**P** - Sort fdinfo by PID\n**M** - Sort fdinfo by VRAM usage\n**G** - Sort fdinfo by GFX usage\n**R** - Reverse sort order\n**q** - Quit application"],["CAVEATS","Reading performance counters (GRBM, GRBM2) may prevent the GPU from entering power-saving states on some APUs. Use **--no-pc** to disable performance counter reading if this is a concern. Requires the AMDGPU kernel driver and appropriate permissions to access GPU sysfs entries."],["HISTORY","**amdgpu_top** was created by Umio-Yasuno as a modern alternative to radeontop, written in **Rust**. It provides features that radeontop lacks, such as temperature monitoring, GPU metrics, and a graphical interface. The project is actively maintained and available on GitHub."],["INSTALL","```pacman: sudo pacman -S amdgpu_top```\n\n```zypper: sudo zypper install amdgpu_top```\n\n```nix: nix profile install nixpkgs#amdgpu_top```"],["SEE ALSO","[radeontop](/man/radeontop)(1), [nvtop](/man/nvtop)(1), [nvidia-smi](/man/nvidia-smi)(1), [htop](/man/htop)(1)"]],"e":["radeontop","nvtop","nvidia-smi","htop"],"x":"amdgpu_top"},"amixer":{"n":"amixer","t":"Control ALSA soundcard mixer from the command line","s":[["TAGLINE","Control ALSA soundcard mixer from the command line"],["TLDR","Turn up the **master volume** by 10%\n\n```amixer -D pulse sset Master 10%+```\n\nTurn down the **master volume** by 10%\n\n```amixer -D pulse sset Master 10%-```\n\n**Mute** the master channel\n\n```amixer -D pulse sset Master mute```\n\n**Unmute** the master channel\n\n```amixer -D pulse sset Master unmute```\n\nList all **simple mixer controls**\n\n```amixer scontrols```"],["SYNOPSIS","**amixer** [_-option_] [_cmd_]"],["DESCRIPTION","**amixer** is a command-line mixer control for the ALSA soundcard driver. It can adjust volume levels, toggle mute states, and query mixer control settings for any available soundcard. Running it without arguments displays current mixer settings for the default soundcard.\n\nThe tool provides both simple and full mixer interfaces. The simple interface (commands prefixed with **s**) uses human-readable control names like \"Master\" or \"PCM\", while the full interface operates on numeric control identifiers. Volume can be specified as absolute values, percentages, or decibels."],["PARAMETERS","**-c card**\n> Select card number (e.g., 0, 1, 2)\n\n**-D device**\n> Select device name (default: 'default')\n\n**-s, --stdin**\n> Read and execute commands from stdin sequentially\n\n**-q**\n> Quiet mode; suppress output\n\n**-R**\n> Raw value for percentage evaluation (default)\n\n**-M**\n> Mapped volume for percentage evaluation"],["COMMANDS","**scontrols**\n> List all simple mixer controls\n\n**scontents**\n> List simple mixer controls with their contents\n\n**set/sset NAME PARAMETER**\n> Configure mixer control contents (supports percentage, dB values, mute/unmute/toggle)\n\n**get/sget NAME**\n> Display simple mixer control contents\n\n**controls**\n> List all card controls\n\n**contents**\n> List card controls with their contents\n\n**info**\n> Show mixer device information"],["CAVEATS","Mixer control names vary between sound cards. Use **scontrols** to list available controls for your card."],["HISTORY","Part of **ALSA** (Advanced Linux Sound Architecture) utilities package."],["INSTALL","```apt: sudo apt install alsa-utils```\n\n```dnf: sudo dnf install alsa-utils```\n\n```pacman: sudo pacman -S alsa-utils```\n\n```apk: sudo apk add alsa-utils```\n\n```zypper: sudo zypper install alsa-utils```\n\n```nix: nix profile install nixpkgs#alsa-utils```"],["SEE ALSO","[alsamixer](/man/alsamixer)(1), [aplay](/man/aplay)(1), [arecord](/man/arecord)(1)"]],"e":["alsamixer","aplay","arecord"],"x":"amixer -D pulse sset Master 10%+"},"arttime":{"n":"arttime","t":"ASCII/text art clock, timer, and pattern time manager","s":[["TAGLINE","ASCII/text art clock, timer, and pattern time manager"],["TLDR","**Start** arttime (art + clock)\n\n```arttime```\n\n**Timer mode** / pattern timers\n\n```arttime -t [25m]```\n\n**Select** art style interactively\n\n```arttime```"],["SYNOPSIS","**arttime** [*options*]"],["DESCRIPTION","**arttime** blends looping text/ASCII art with a clock, timer, and pattern-based time manager in the terminal. Aimed at delightful focus sessions rather than minimal stopwatches. Install via upstream install scripts or packages documented in the repository."],["PARAMETERS","**-t** *duration*\n\n> Timer-related modes (see **arttime -h** for pattern syntax).\n\nInteractive keybindings change art, toggle views, and control timers once running. Full option list: **arttime --help**."],["CAVEATS","Needs a UTF-8 capable terminal with adequate size for art. Not a scheduling daemon—process must keep running."],["INSTALL","```brew: brew install arttime```"],["SEE ALSO","[pomodoro](/man/pomodoro)(1), [tty-clock](/man/tty-clock)(1), [peaclock](/man/peaclock)(1)"],["RESOURCES","```[Source code](https://github.com/poetaman/arttime)```"]],"e":["pomodoro","tty-clock","peaclock"],"x":"arttime"},"asciinema":{"n":"asciinema","t":"Record and replay terminal sessions","s":[["TAGLINE","Record and replay terminal sessions"],["TLDR","**Record** terminal session (filename required in 3.0+)\n\n```asciinema rec [recording.cast]```\n\n**Play** recorded session\n\n```asciinema play [recording.cast]```\n\n**Upload** to asciinema.org\n\n```asciinema upload [recording.cast]```\n\nRecord with **title**\n\n```asciinema rec -t \"[My recording]\" [recording.cast]```\n\nRecord with **idle time limit**\n\n```asciinema rec --idle-time-limit=[2] [recording.cast]```\n\n**Live stream** a terminal session\n\n```asciinema stream -l```"],["SYNOPSIS","**asciinema** _rec_|_play_|_cat_|_convert_|_upload_|_stream_|_session_|_auth_ [_options_] [_file_]"],["DESCRIPTION","**asciinema** records and replays terminal sessions. It captures terminal output with timing information, creating lightweight recordings that can be shared or embedded on websites.\n\nUnlike video screen recording, asciinema recordings are text-based, allowing copy-paste from playback and producing small file sizes."],["PARAMETERS","**rec** _file_\n> Record terminal session. In 3.0+ the filename is required, and rec no longer uploads (use the upload command).\n\n**play** _file_\n> Playback recorded session\n\n**cat** _file..._\n> Concatenate two or more recordings into one (3.0+).\n\n**convert** _input_ _output_\n> Convert a recording to another asciicast format (3.0+).\n\n**upload** _file_\n> Upload recording to asciinema.org\n\n**stream**\n> Live stream the terminal session (3.0+).\n\n**session**\n> Generic session that can record, stream, or both (3.0+).\n\n**auth**\n> Authenticate this install with your asciinema server account\n\n**-t** _title_, **--title** _title_\n> Recording title\n\n**-c** _cmd_, **--command** _cmd_\n> Command to record (default: $SHELL)\n\n**--idle-time-limit** _sec_\n> Maximum idle time between frames\n\n**-i** _sec_\n> Shorthand for --idle-time-limit\n\n**-s** _speed_, **--speed** _speed_\n> Playback speed multiplier\n\n**--cols** _n_ / **--rows** _n_\n> Override terminal dimensions"],["CONFIGURATION","**~/.config/asciinema/config.toml**\n> User configuration file (TOML) in CLI 3.0+ for default recording settings, server URL, and authentication. Earlier 2.x releases used **~/.config/asciinema/config**."],["CAVEATS","Recordings capture exactly what appears in terminal, including sensitive data. Review before uploading. Playback requires terminal with same capabilities as recording."],["HISTORY","**asciinema** was created by Marcin Kulik, with the first version released around **2011**. It has become the standard tool for terminal recording and sharing."],["INSTALL","```dnf: sudo dnf install asciinema```\n\n```pacman: sudo pacman -S asciinema```\n\n```apk: sudo apk add asciinema```\n\n```zypper: sudo zypper install asciinema```\n\n```brew: brew install asciinema```\n\n```nix: nix profile install nixpkgs#asciinema```"],["SEE ALSO","[script](/man/script)(1), [ttyrec](/man/ttyrec)(1), [agg](/man/agg)(1)"],["RESOURCES","```[Source code](https://github.com/asciinema/asciinema)```\n\n```[Homepage](https://asciinema.org)```\n\n```[Documentation](https://docs.asciinema.org)```"]],"e":["script","ttyrec","agg"],"x":"asciinema rec [recording.cast]"},"btrfs-version":{"n":"btrfs-version","t":"Display btrfs-progs version information","s":[["TAGLINE","Display btrfs-progs version information"],["TLDR","Display the **btrfs-progs version**\n\n```btrfs version```\n\nDisplay **help** for the subcommand\n\n```btrfs version --help```\n\nEquivalent **global flag**\n\n```btrfs --version```"],["SYNOPSIS","**btrfs** **version**"],["DESCRIPTION","**btrfs version** prints the version of the userspace **btrfs-progs** package providing the `btrfs` command. The kernel module version is independent and can be inspected via `/sys/module/btrfs/version` or `modinfo btrfs`.\n\nThe output is useful when reporting bugs, comparing features against the documented changelog, or scripting feature detection. Recent releases (6.x) also expose built-in features through `mkfs.btrfs --version`, `btrfs-convert --version`, and similar per-tool flags."],["CAVEATS","Reports the userspace tools version only, not the running kernel's btrfs implementation. Mismatched userspace and kernel versions are supported but some on-disk features may be unavailable."],["INSTALL","```apt: sudo apt install btrfs-progs```\n\n```dnf: sudo dnf install btrfs-progs```\n\n```pacman: sudo pacman -S btrfs-progs```\n\n```apk: sudo apk add btrfs-progs```\n\n```brew: brew install btrfs-progs```\n\n```nix: nix profile install nixpkgs#btrfs-progs```"],["SEE ALSO","[btrfs](/man/btrfs)(8)"]],"e":["btrfs"],"x":"btrfs version"},"buzzphrase":{"n":"buzzphrase","t":"Generate random corporate buzzword phrases","s":[["TAGLINE","Generate random corporate buzzword phrases"],["TLDR","**Generate a random buzzphrase**\n\n```buzzphrase```\n\n**Generate multiple buzzphrases**\n\n```buzzphrase [5]```"],["SYNOPSIS","**buzzphrase** [_count_]"],["DESCRIPTION","**buzzphrase** generates random corporate buzzword phrases, combining jargon terms to create meaningless but official-sounding business speak. Useful for humor, parody documents, or testing text fields.\n\nThe generated phrases combine adjectives, verbs, and nouns from a predefined list of corporate jargon."],["SEE ALSO","[fortune](/man/fortune)(1), [cowsay](/man/cowsay)(1)"],["RESOURCES","```[Source code](https://github.com/atomantic/buzzphrase)```"]],"e":["fortune","cowsay"],"x":"buzzphrase"},"coffee":{"n":"coffee","t":"CoffeeScript compiler and runtime","s":[["TAGLINE","CoffeeScript compiler and runtime"],["TLDR","**Compile a CoffeeScript file** to JavaScript\n\n```coffee -c [script.coffee]```\n\n**Compile and output** to a specific directory\n\n```coffee -c -o [output/] [script.coffee]```\n\n**Watch and compile** files on change\n\n```coffee -w -c [script.coffee]```\n\n**Run a CoffeeScript file** directly\n\n```coffee [script.coffee]```\n\n**Start an interactive REPL**\n\n```coffee```\n\n**Evaluate CoffeeScript code** from command line\n\n```coffee -e \"[console.log 'Hello']\"```\n\n**Compile all files** in a directory\n\n```coffee -c [src/]```\n\n**Print compiled JavaScript** without saving\n\n```coffee -p [script.coffee]```"],["SYNOPSIS","**coffee** [_options_] [_path/to/script.coffee_] [_arguments_]"],["PARAMETERS","**-c**, **--compile**\n> Compile CoffeeScript to JavaScript files.\n\n**-o** _DIR_, **--output** _DIR_\n> Output directory for compiled JavaScript.\n\n**-w**, **--watch**\n> Watch files for changes and recompile.\n\n**-p**, **--print**\n> Print compiled JavaScript to stdout.\n\n**-e** _CODE_, **--eval** _CODE_\n> Evaluate a string of CoffeeScript code.\n\n**-b**, **--bare**\n> Compile without the top-level function safety wrapper.\n\n**-m**, **--map**\n> Generate source maps alongside JavaScript.\n\n**-M**, **--inline-map**\n> Embed source maps within the JavaScript file.\n\n**-n**, **--nodes**\n> Print the parse tree (AST) of the source.\n\n**-t**, **--transpile**\n> Pipe output through Babel for ES5 compatibility.\n\n**--no-header**\n> Suppress the \"Generated by CoffeeScript\" header.\n\n**-h**, **--help**\n> Display help information.\n\n**-v**, **--version**\n> Display version information."],["DESCRIPTION","**coffee** is the command-line compiler and runtime for CoffeeScript, a programming language that compiles to JavaScript. CoffeeScript provides a cleaner syntax inspired by Ruby and Python, featuring significant whitespace, implicit returns, and syntactic sugar for common patterns.\n\nThe compiler can output JavaScript files, run CoffeeScript directly, or provide an interactive REPL for experimentation. The watch mode enables live development workflows where files are automatically recompiled when saved.\n\nCoffeeScript compiles to readable JavaScript that follows best practices. Source maps allow debugging in the original CoffeeScript even when running the compiled JavaScript. The bare option removes the IIFE wrapper for compatibility with module systems."],["CAVEATS","CoffeeScript 2 outputs modern JavaScript (ES6+) which may require transpilation for older browsers via **--transpile**. Significant whitespace requires consistent indentation (spaces or tabs, but not mixed). CoffeeScript usage has declined significantly since ES6 adopted many of its features natively."],["HISTORY","CoffeeScript was created by Jeremy Ashkenas and released in **December 2009**. It gained significant popularity in the early 2010s, particularly in the Ruby on Rails community. While usage has declined with ES6's adoption of many features CoffeeScript pioneered, it remains maintained and in use."],["SEE ALSO","[node](/man/node)(1), [babel](/man/babel)(1), [tsc](/man/tsc)(1)"]],"e":["node","babel","tsc"],"x":"coffee -c [script.coffee]"},"dhall":{"n":"dhall","t":"programmable configuration language with guaranteed termination","s":[["TAGLINE","programmable configuration language with guaranteed termination"],["TLDR","**Evaluate a Dhall expression**\n\n```dhall <<< \"[expression]\"```\n\n**Evaluate a Dhall file**\n\n```dhall < [config.dhall]```\n\n**Convert Dhall to JSON**\n\n```dhall-to-json <<< \"[expression]\"```\n\n**Convert Dhall to YAML**\n\n```dhall-to-yaml < [config.dhall]```\n\n**Type check a Dhall file**\n\n```dhall type < [config.dhall]```\n\n**Format a Dhall file**\n\n```dhall format < [config.dhall]```\n\n**Convert JSON to Dhall**\n\n```json-to-dhall '[Type]' < [data.json]```"],["SYNOPSIS","**dhall** [_command_] [_options_]"],["PARAMETERS","**type**\n> Infer and display the type of an expression.\n\n**normalize**\n> Normalize an expression.\n\n**format**\n> Format Dhall source code.\n\n**lint**\n> Improve Dhall expressions.\n\n**diff**\n> Show differences between two expressions.\n\n**hash**\n> Compute semantic hash of expression.\n\n**freeze**\n> Add integrity checks to imports.\n\n**encode** / **decode**\n> Binary encoding/decoding.\n\n**--explain**\n> Provide detailed error explanations.\n\n**--ascii**\n> Use ASCII output instead of Unicode."],["DESCRIPTION","**Dhall** is a programmable configuration language that guarantees termination. It provides the power of a programming language (functions, types, imports) while ensuring configurations always produce a result without infinite loops.\n\nDhall expressions can be converted to JSON, YAML, or other formats for consumption by applications. The language features a strong type system that catches configuration errors at evaluation time.\n\nKey features include: remote imports with integrity checking (content-addressed dependencies), a total type system (no runtime errors), and built-in functions for common transformations."],["EXAMPLE","```dhall\n-- config.dhall\nlet Config = { name : Text, port : Natural }\n\nlet defaults = { name = \"myapp\", port = 8080 }\n\nin  defaults // { port = 9000 }\n```"],["CAVEATS","Learning curve for those unfamiliar with typed functional programming. Large configurations can be slow to evaluate. Not all tools natively support Dhall; conversion to JSON/YAML adds a build step. Import caching behavior may surprise users."],["HISTORY","Dhall was created by **Gabriella Gonzalez** and first released in **2016**. The name comes from a Hindu grain. It was designed to address the limitations of YAML and JSON for complex configurations while avoiding the Turing-completeness issues that make other templating languages unpredictable. The language has gained adoption for Kubernetes configurations and infrastructure-as-code."],["INSTALL","```apt: sudo apt install dhall```\n\n```dnf: sudo dnf install dhall```\n\n```pacman: sudo pacman -S dhall```\n\n```brew: brew install dhall```\n\n```nix: nix profile install nixpkgs#dhall```"],["SEE ALSO","[jq](/man/jq)(1), [yq](/man/yq)(1), [jsonnet](/man/jsonnet)(1)"],["RESOURCES","```[Source code](https://github.com/dhall-lang/dhall-lang)```\n\n```[Homepage](https://dhall-lang.org)```\n\n```[Documentation](https://docs.dhall-lang.org)```"]],"e":["jq","yq","jsonnet"],"x":"dhall <<< \"[expression]\""},"djxl":{"n":"djxl","t":"JPEG XL decoder to multiple formats","s":[["TAGLINE","JPEG XL decoder to multiple formats"],["TLDR","**Decode JPEG XL** to PNG\n\n```djxl [image.jxl] [output.png]```\n\n**Decode to JPEG**\n\n```djxl [image.jxl] [output.jpg]```\n\n**Decode to PPM**\n\n```djxl [image.jxl] [output.ppm]```\n\n**Show image info** only\n\n```djxl --info [image.jxl]```\n\n**Decode with specific** number of threads\n\n```djxl --num_threads [4] [image.jxl] [output.png]```\n\n**Decode specific frame** from animation\n\n```djxl --frame [5] [animation.jxl] [frame5.png]```"],["SYNOPSIS","**djxl** [_options_] _input.jxl_ [_output_]"],["PARAMETERS","_INPUT_\n> JPEG XL file to decode.\n\n_OUTPUT_\n> Output file (format inferred from extension).\n\n**--info**\n> Show image information only.\n\n**--num_threads** _N_\n> Number of decoding threads.\n\n**--frame** _N_\n> Extract specific frame from animation.\n\n**--jpeg**\n> Reconstruct exact JPEG if losslessly recompressed.\n\n**--bits_per_sample** _N_\n> Output bit depth.\n\n**--help**\n> Display help information."],["DESCRIPTION","**djxl** is the reference JPEG XL decoder, converting JPEG XL images to other formats. It's part of the libjxl reference implementation and supports all JPEG XL features including animation, HDR, and lossless JPEG reconstruction.\n\nJPEG XL files created by losslessly recompressing JPEG can be decoded back to the exact original JPEG bytes using the --jpeg flag. This allows JPEG XL to serve as a reversible compression layer.\n\nThe decoder supports various output formats with automatic format detection based on file extension, and can extract metadata and individual frames from animated images."],["CAVEATS","Output format support depends on build configuration. Some HDR features may require specific output formats. JPEG reconstruction requires original was losslessly compressed. Large images may use significant memory."],["HISTORY","djxl is part of the **libjxl** reference implementation of JPEG XL, developed by the **JPEG Committee** with contributions from Google. JPEG XL was standardized in **2022** as a next-generation image format combining features from Google's PIK and Cloudinary's FUIF."],["INSTALL","```apt: sudo apt install libjxl-tools```\n\n```apk: sudo apk add libjxl-tools```\n\n```zypper: sudo zypper install libjxl-tools```"],["SEE ALSO","[cjxl](/man/cjxl)(1), [djpeg](/man/djpeg)(1), [convert](/man/convert)(1)"],["RESOURCES","```[Source code](https://github.com/libjxl/libjxl)```\n\n```[Homepage](https://jpegxl.info/)```"]],"e":["cjxl","djpeg","convert"],"x":"djxl [image.jxl] [output.png]"},"dnf5-group":{"n":"dnf5-group","t":"manage package groups in DNF5","s":[["TAGLINE","manage package groups in DNF5"],["TLDR","**List all available and installed groups**\n\n```dnf5 group list```\n\n**List only installed groups**\n\n```dnf5 group list --installed```\n\n**Show detailed info about a group**\n\n```dnf5 group info [group_name]```\n\n**Install a group (mandatory and default packages)**\n\n```dnf5 group install [group_name]```\n\n**Install a group including optional packages**\n\n```dnf5 group install --with-optional [group_name]```\n\n**Remove a group and its exclusive packages**\n\n```dnf5 group remove [group_name]```\n\n**Upgrade a group**\n\n```dnf5 group upgrade [group_name]```"],["SYNOPSIS","**dnf5 group** {**list**|**info**} [_options_] [_group-spec_...]\n\n**dnf5 group** {**install**|**remove**|**upgrade**} [_options_] _group-spec_..."],["PARAMETERS","**list**\n> List all matching groups, either installed or available.\n\n**info**\n> Print detailed information about groups. Accepts the same options as list.\n\n**install**\n> Mark the group installed and install its mandatory and default packages.\n\n**remove**\n> Mark the group removed and remove packages that do not belong to another installed group.\n\n**upgrade**\n> Upgrade the group definition and the packages belonging to the group.\n\n**--available**\n> Show only available (not installed) groups.\n\n**--installed**\n> Show only installed groups.\n\n**--hidden**\n> Show also hidden groups.\n\n**--contains-pkgs** _PACKAGE_NAME_\n> Show only groups containing packages with specified names. Supports globs.\n\n**--with-optional**\n> Include optional packages from the group (install only).\n\n**--no-packages**\n> Operate on the group without manipulating any packages (install/remove)."],["DESCRIPTION","**dnf5 group** manages virtual collections of packages in DNF5. Groups bundle related packages together, such as \"Development Tools\" or \"Server\".\n\nNote: From Fedora 37 to 40 (inclusive), `dnf` runs DNF v4 while `dnf5` runs DNF v5. Starting with Fedora 41, `dnf` is an alias for `dnf5`."],["INSTALL","```dnf: sudo dnf install dnf5```\n\n```pacman: sudo pacman -S dnf5```\n\n```zypper: sudo zypper install dnf5```\n\n```nix: nix profile install nixpkgs#dnf5```"],["SEE ALSO","[dnf-group](/man/dnf-group)(8), [dnf5](/man/dnf5)(8)"]],"e":["dnf-group","dnf5"],"x":"dnf5 group list"},"docker-compose-up":{"n":"docker-compose-up","t":"start compose services","s":[["TAGLINE","start compose services"],["TLDR","**Start services**\n\n```docker compose up```\n\n**Start in detached mode**\n\n```docker compose up -d```\n\n**Build images before starting**\n\n```docker compose up --build```\n\n**Start specific services**\n\n```docker compose up [service1] [service2]```\n\n**Force recreate containers**\n\n```docker compose up --force-recreate```\n\n**Scale a service**\n\n```docker compose up --scale [service]=[3]```\n\n**Remove orphans**\n\n```docker compose up --remove-orphans```"],["SYNOPSIS","**docker** **compose** **up** [_options_] [_service..._]"],["PARAMETERS","**-d**, **--detach**\n> Run containers in the background and print container names.\n\n**--build**\n> Build images before starting containers.\n\n**--no-build**\n> Don't build images even if they are missing.\n\n**--pull** _policy_\n> Pull image before running: _always_, _missing_ (default), or _never_.\n\n**--force-recreate**\n> Recreate containers even if configuration and image are unchanged.\n\n**--no-recreate**\n> Don't recreate containers that already exist.\n\n**--always-recreate-deps**\n> Recreate dependent containers. Incompatible with **--no-recreate**.\n\n**-V**, **--renew-anon-volumes**\n> Recreate anonymous volumes instead of retrieving data from previous containers.\n\n**--no-deps**\n> Don't start linked (dependent) services.\n\n**--scale** _service=num_\n> Scale the given service to _num_ instances (overrides deploy.replicas).\n\n**--remove-orphans**\n> Remove containers for services not defined in the Compose file.\n\n**--wait**\n> Wait for services to be _running_ or _healthy_. Implies **--detach**.\n\n**--wait-timeout** _seconds_\n> Maximum time to wait for services when using **--wait**.\n\n**--abort-on-container-exit**\n> Stop all containers if any container stops. Incompatible with **-d**.\n\n**--abort-on-container-failure**\n> Stop all containers if any container exited with non-zero status.\n\n**--attach** _service_\n> Restrict log output to the specified service(s).\n\n**--no-attach** _service_\n> Do not attach (stream logs from) the specified service(s).\n\n**-t**, **--timeout** _seconds_\n> Shutdown timeout in seconds when stopping attached containers.\n\n**--timestamps**\n> Show timestamps in container logs.\n\n**--quiet-pull**\n> Pull images without printing progress information.\n\n**--no-start**\n> Don't start the services after creating them."],["DESCRIPTION","**docker compose up** builds, (re)creates, starts, and attaches to containers for the services defined in _compose.yaml_ / _docker-compose.yml_. It also creates networks and volumes declared in the file.\n\nWhen run without **-d**, it streams aggregated logs from all attached containers and stops them on Ctrl+C. With **--wait** or **-d**, the command returns as soon as services are up (or healthy, with **--wait**)."],["CAVEATS","If the Compose file has changed, containers are recreated unless **--no-recreate** is set. Using **--force-recreate** and **-V** together discards all existing data in anonymous volumes for recreated services."],["INSTALL","```apt: sudo apt install docker-compose```\n\n```dnf: sudo dnf install docker-compose```\n\n```pacman: sudo pacman -S docker-compose```\n\n```apk: sudo apk add docker-cli-compose```\n\n```zypper: sudo zypper install docker-compose```\n\n```brew: brew install docker-compose```\n\n```nix: nix profile install nixpkgs#docker-compose```"],["SEE ALSO","[docker-compose](/man/docker-compose)(1), [docker-compose-down](/man/docker-compose-down)(1)"]],"e":["docker-compose","docker-compose-down"],"x":"docker compose up"},"docker-image-load":{"n":"docker-image-load","t":"load images from tar archives","s":[["TAGLINE","load images from tar archives"],["TLDR","**Load an image from a tar archive**\n\n```docker image load -i [image.tar]```\n\n**Load an image from stdin**\n\n```cat [image.tar] | docker image load```\n\n**Load an image with quiet output**\n\n```docker image load -q -i [image.tar]```\n\n**Load only a specific platform variant**\n\n```docker image load --platform [linux/amd64] -i [image.tar]```"],["SYNOPSIS","**docker** **image** **load** [_options_]"],["PARAMETERS","**-i**, **--input** _file_\n> Read from tar archive file instead of STDIN.\n\n**-q**, **--quiet**\n> Suppress load output.\n\n**--platform** _value_\n> Load only the given platform variant(s), formatted as \"os[/arch[/variant]]\" (e.g., linux/amd64). Can be specified multiple times."],["DESCRIPTION","**docker image load** loads a Docker image from a tar archive (even if compressed with gzip, bzip2, xz, or zstd) created by **docker image save**, restoring the image layers and metadata to the local Docker daemon.\n\nThe tar archive contains all image layers, configuration, and manifest data needed to fully reconstruct the image. Loading an image preserves its repository tags and digests exactly as they were when saved. This makes **docker image load** ideal for air-gapped environments, backup/restore workflows, and distributing images via physical media or secure file transfer.\n\nThe command can read from a file specified with **-i** or from stdin, enabling flexible integration with compression tools and network transfers."],["CAVEATS","The tar archive must have been created with **docker image save** or a compatible tool. Loading an image does not pull missing layers from a registry. Images are loaded into the local Docker daemon only."],["HISTORY","**docker image load** is the modern syntax for the **docker load** command, introduced as part of Docker's CLI restructuring to group commands under management categories."],["INSTALL","```apt: sudo apt install docker-cli```\n\n```dnf: sudo dnf install docker-cli```\n\n```pacman: sudo pacman -S docker```\n\n```apk: sudo apk add docker-cli```\n\n```zypper: sudo zypper install docker```\n\n```brew: brew install docker```\n\n```nix: nix profile install nixpkgs#docker```"],["SEE ALSO","[docker-load](/man/docker-load)(1), [docker-image-save](/man/docker-image-save)(1), [docker-image](/man/docker-image)(1), [docker](/man/docker)(1)"]],"e":["docker-load","docker-image-save","docker-image","docker"],"x":"docker image load -i [image.tar]"},"docker-top":{"n":"docker-top","t":"Display the running processes of a container","s":[["TAGLINE","Display the running processes of a container"],["TLDR","**Show running processes in container**\n\n```docker top [container]```\n\n**Show with BSD-style ps options (full info)**\n\n```docker top [container] aux```\n\n**Show with specific columns**\n\n```docker top [container] -o pid,user,comm```\n\n**Use container ID instead of name**\n\n```docker top [container_id]```"],["SYNOPSIS","**docker top** _CONTAINER_ [_ps OPTIONS_]"],["PARAMETERS","_CONTAINER_\n> Name or ID of a running container.\n\n_ps OPTIONS_\n> Optional **ps**(1) flags forwarded to the host's ps to format output (e.g., `aux`, `-eo pid,comm`)."],["DESCRIPTION","**docker top** displays the processes running inside a container by querying the host's process list and filtering for that container's namespace. It is conceptually the same as **docker container top** and accepts arbitrary **ps**(1) options to control the output format.\n\nThe container must be running; stopped containers cannot be inspected with this command. Output reflects the host's view of the processes (host PIDs), not the in-container PIDs."],["CAVEATS","Process IDs shown are the host's PIDs, not the PIDs as seen from inside the container. The available **ps** options depend on the host's ps implementation (procps on Linux). Does not work on Windows containers in the same way."],["INSTALL","```apt: sudo apt install docker-cli```\n\n```dnf: sudo dnf install docker-cli```\n\n```pacman: sudo pacman -S docker```\n\n```apk: sudo apk add docker-cli```\n\n```zypper: sudo zypper install docker```\n\n```brew: brew install docker```\n\n```nix: nix profile install nixpkgs#docker```"],["SEE ALSO","[docker](/man/docker)(1), [docker-container-top](/man/docker-container-top)(1), [docker-ps](/man/docker-ps)(1), [docker-stats](/man/docker-stats)(1), [ps](/man/ps)(1), [top](/man/top)(1)"]],"e":["docker","docker-container-top","docker-ps","docker-stats","ps","top"],"x":"docker top [container]"},"dowe":{"n":"dowe","t":"Fullstack compiler and runtime for multi-target apps","s":[["TAGLINE","Fullstack compiler and runtime for multi-target apps"],["TLDR","**Install** the CLI (macOS/Linux)\n\n```curl -fsSL https://get.dowe.dev/install | bash```\n\n**Initialize** a blank project\n\n```dowe init --template blank```\n\n**Develop** server and web targets with watch\n\n```dowe dev --target server --target web```\n\n**Run** project tests\n\n```dowe test```\n\n**Deploy** a distribution (static, Docker, or Cloudflare)\n\n```dowe deploy```\n\n**Print** the installed version\n\n```dowe version```"],["SYNOPSIS","**dowe** _command_ [_options_]"],["DESCRIPTION","**dowe** is the command-line interface for Dowe, a Rust compiler and runtime that builds fullstack products from a unified declarative source graph (**.dowe** files). One project model can target server (Rust), web (SSR HTML/JS/CSS), desktop windows, Android (Jetpack Compose), and iOS (SwiftUI).\n\nDowe Source Format is a compiler input, not a JavaScript application runtime: Node.js, **node_modules**, React, and similar stacks are not required to compile and run. The compiler validates routes, types, and platform boundaries before generating deterministic artifacts under **.dowe/**."],["COMMANDS","**init** [**--template** blank|crud] [**--i18n**]\n\n> Scaffold a new project. **crud** adds a fullstack CRUD starter; **--i18n** includes translation catalogs.\n\n**dev** [**--target** _name_]...\n\n> Compile, watch, and run selected development targets. Interactive target selection if no flags are given.\n\n**test**\n\n> Discover and run native **.dowe** literal tests.\n\n**deploy**\n\n> Produce static, Docker, or Cloudflare distributions.\n\n**icons**\n\n> Generate versioned icon sets for web, desktop, Android, and iOS.\n\n**agent**\n\n> Install or update public authoring guidance for coding agents.\n\n**codegraph**\n\n> Inspect ownership, dependencies, modularity, and duplication.\n\n**database** / **cache** / **vector**\n\n> Manage Dowe Database, Cache, and Vector instances and data.\n\n**upgrade**\n\n> Upgrade through the official release channel.\n\n**version**\n\n> Print the CLI version."],["CAVEATS","Early-access toolchain; target support and APIs may change. Mobile generators produce native projects that still need platform SDKs (Android/iOS) for device builds. Do not edit **.dowe/** as source — it is generated."],["SEE ALSO","[cargo](/man/cargo)(1), [rustc](/man/rustc)(1)"],["RESOURCES","```[Source code](https://github.com/usedowe/dowe-lang)```\n\n```[Homepage](https://dowe.dev)```\n\n```[Documentation](https://dowe.dev)```"]],"e":["cargo","rustc"],"x":"curl -fsSL https://get.dowe.dev/install | bash"},"factorio":{"n":"factorio","t":"Headless multiplayer game server","s":[["TAGLINE","Headless multiplayer game server"],["TLDR","**Create** a new save file\n\n```[path/to]/factorio --create [path/to/save_file.zip]```\n\n**Start** a Factorio server\n\n```[path/to]/factorio --start-server [path/to/save_file.zip]```\n\n**Start server with settings**\n\n```[path/to]/factorio --start-server [path/to/save_file.zip] --server-settings [path/to/server-settings.json]```\n\nCreate save with **specific map settings**\n\n```[path/to]/factorio --create [path/to/save.zip] --map-gen-settings [path/to/map_gen_settings.json] --map-settings [path/to/map_settings.json]```\n\n**Shut** the server gracefully (interactive)\n\n```/quit```\n\nDisplay **help**\n\n```[path/to]/factorio [-h|--help]```"],["SYNOPSIS","**factorio** [_options_]"],["DESCRIPTION","**factorio** runs the Factorio game in headless server mode for multiplayer. It creates and hosts game worlds without requiring a graphical interface.\n\nUsed for dedicated Factorio servers and automated testing."],["PARAMETERS","**--create** _file_\n> Create new save file\n\n**--start-server** _file_\n> Start server with save file\n\n**--map-gen-settings** _file_\n> Map generation settings JSON\n\n**--map-settings** _file_\n> Map settings JSON\n\n**-h, --help**\n> Display help\n\n**--port** _number_\n> Server port number (default: 34197).\n\n**--server-settings** _file_\n> Server configuration JSON file (name, description, visibility, passwords).\n\n**--server-adminlist** _file_\n> JSON file listing admin usernames.\n\n**--server-banlist** _file_\n> JSON file listing banned usernames.\n\n**--console-log** _file_\n> Write server console output to a log file.\n\n**--mod-directory** _dir_\n> Override mod directory path."],["CAVEATS","Requires Factorio game installation. Server settings configured via JSON files. Headless mode requires no X server. Commercial game with separate license. Default port is 34197/UDP."],["INSTALL","```nix: nix profile install nixpkgs#factorio```"],["SEE ALSO","[screen](/man/screen)(1), [tmux](/man/tmux)(1)"]],"e":["screen","tmux"],"x":"[path/to]/factorio --create [path/to/save_file.zip]"},"fc-query":{"n":"fc-query","t":"extract font file information","s":[["TAGLINE","extract font file information"],["TLDR","**Query font file**\n\n```fc-query [font.ttf]```\n\n**Show specific property**\n\n```fc-query --format '%{family}\\n' [font.ttf]```\n\n**List all properties**\n\n```fc-query --format '%{=unparse}\\n' [font.ttf]```\n\n**Query font index**\n\n```fc-query --index [0] [font.ttc]```"],["SYNOPSIS","**fc-query** [_options_] _font-file_"],["PARAMETERS","_FONT-FILE_\n> Font file to query (.ttf, .otf, .ttc).\n\n**-f**, **--format** _FORMAT_\n> Output format string.\n\n**-i**, **--index** _N_\n> Only query the face at the given index of each file.\n\n**-b**, **--ignore-blanks**\n> Ignore blank glyphs when computing supported languages.\n\n**--help**\n> Display help information."],["DESCRIPTION","**fc-query** extracts and displays information from font files without installing them. It's part of fontconfig and shows font properties like family, style, weight, and supported languages.\n\nThe tool queries individual font files directly rather than the system font cache. Format strings allow extracting specific properties. For font collections (.ttc), specific fonts can be selected by index.\n\nfc-query is useful for examining fonts before installation or scripting font management tasks."],["CAVEATS","Only reads single files, not directories. Format string syntax requires learning. Some fonts may have incomplete metadata."],["HISTORY","fc-query is part of **fontconfig**, the font configuration library created by Keith Packard for X.org. It provides programmatic access to font metadata used by the fontconfig system."],["INSTALL","```apt: sudo apt install fontconfig```\n\n```dnf: sudo dnf install fontconfig```\n\n```pacman: sudo pacman -S fontconfig```\n\n```apk: sudo apk add fontconfig```\n\n```zypper: sudo zypper install fontconfig```\n\n```brew: brew install fontconfig```\n\n```nix: nix profile install nixpkgs#fontconfig```"],["SEE ALSO","[fc-scan](/man/fc-scan)(1), [fc-list](/man/fc-list)(1), [fc-cache](/man/fc-cache)(1)"],["RESOURCES","```[Source code](https://gitlab.freedesktop.org/fontconfig/fontconfig)```\n\n```[Homepage](https://www.freedesktop.org/wiki/Software/fontconfig/)```"]],"e":["fc-scan","fc-list","fc-cache"],"x":"fc-query [font.ttf]"},"findfs":{"n":"findfs","t":"locate filesystems by label or UUID","s":[["TAGLINE","locate filesystems by label or UUID"],["TLDR","**Find device by filesystem label**\n\n```findfs LABEL=[label]```\n\n**Find device by filesystem UUID**\n\n```findfs UUID=[uuid]```\n\n**Find device by GPT partition label**\n\n```findfs PARTLABEL=[partition_label]```\n\n**Find device by GPT partition UUID**\n\n```findfs PARTUUID=[partition_uuid]```"],["SYNOPSIS","**findfs** _tag_=_value_"],["DESCRIPTION","**findfs** finds a filesystem by label or UUID and prints the device path. It searches all block devices for matching filesystem metadata.\n\nUsed in boot scripts and mount operations to locate filesystems reliably."],["PARAMETERS","**LABEL=**_label_\n> Search by filesystem label\n\n**UUID=**_uuid_\n> Search by filesystem UUID\n\n**PARTLABEL=**_label_\n> Search by GPT/MAC partition label\n\n**PARTUUID=**_uuid_\n> Search by GPT partition UUID"],["CAVEATS","Part of util-linux. PARTUUID only available on GPT partition tables. Labels and UUIDs must be unique for reliable matching."],["INSTALL","```apt: sudo apt install util-linux```\n\n```dnf: sudo dnf install util-linux```\n\n```pacman: sudo pacman -S util-linux```\n\n```apk: sudo apk add util-linux-misc```\n\n```zypper: sudo zypper install util-linux```\n\n```brew: brew install util-linux```\n\n```nix: nix profile install nixpkgs#util-linux```"],["SEE ALSO","[blkid](/man/blkid)(8), [lsblk](/man/lsblk)(8), [fstab](/man/fstab)(5)"]],"e":["blkid","lsblk","fstab"],"x":"findfs LABEL=[label]"},"fish":{"n":"fish","t":"friendly interactive shell","s":[["TAGLINE","friendly interactive shell"],["TLDR","**Start fish shell**\n\n```fish```\n\n**Execute command**\n\n```fish -c \"[echo hello]\"```\n\n**Run script**\n\n```fish [script.fish]```\n\n**Start without config**\n\n```fish --no-config```\n\n**Check syntax**\n\n```fish -n [script.fish]```"],["SYNOPSIS","**fish** [_options_] [_file_] [_arguments_...]"],["PARAMETERS","_FILE_\n> Script file to execute.\n\n**-c** _COMMAND_\n> Execute command and exit.\n\n**-n**, **--no-execute**\n> Syntax check only.\n\n**--no-config**\n> Skip configuration files.\n\n**-i**, **--interactive**\n> Force interactive mode.\n\n**-l**, **--login**\n> Act as login shell.\n\n**-p**, **--profile** _FILE_\n> Output profiling data.\n\n**--help**\n> Display help information."],["CONFIGURATION","**~/.config/fish/config.fish**\n> User configuration file loaded on shell startup.\n\n**~/.config/fish/fish_variables**\n> Universal variables stored persistently across sessions.\n\n**~/.config/fish/functions/*.fish**\n> User-defined functions, one per file."],["DESCRIPTION","**fish** (Friendly Interactive Shell) is a smart, user-friendly shell with syntax highlighting, autosuggestions, and tab completions that work out of the box.\n\nUnlike bash or zsh, fish doesn't require extensive configuration to be useful. It features web-based configuration, informative error messages, and discoverable features.\n\nfish uses a cleaner scripting syntax that differs from POSIX shells, prioritizing usability over compatibility."],["CAVEATS","Not POSIX-compatible. Scripts need fish-specific syntax. Some commands differ from bash/zsh."],["HISTORY","fish was created by **Axel Liljencrantz** in 2005 to address shell usability issues. It prioritizes interactive use and discoverability, embracing incompatibility with POSIX to improve user experience."],["INSTALL","```apt: sudo apt install fish```\n\n```dnf: sudo dnf install fish```\n\n```pacman: sudo pacman -S fish```\n\n```apk: sudo apk add fish```\n\n```zypper: sudo zypper install fish```\n\n```brew: brew install fish```\n\n```nix: nix profile install nixpkgs#fish```"],["SEE ALSO","[bash](/man/bash)(1), [zsh](/man/zsh)(1), [fisher](/man/fisher)(1)"],["RESOURCES","```[Homepage](https://fishshell.com/)```\n\n```[Source code](https://github.com/fish-shell/fish-shell)```"]],"e":["bash","zsh","fisher"],"x":"fish"},"fly":{"n":"fly","t":"CLI for Concourse CI system","s":[["TAGLINE","CLI for Concourse CI system"],["TLDR","**Set build target**\n\n```fly -t [target] set-pipeline```\n\n**Trigger job**\n\n```fly -t [target] trigger-job -j [pipeline/job]```\n\n**Watch job logs**\n\n```fly -t [target] watch -j [pipeline/job]```\n\n**Login to Concourse**\n\n```fly -t [target] login -c [https://ci.example.com]```\n\n**Sync fly version**\n\n```fly -t [target] sync```"],["SYNOPSIS","**fly** [_options_] _command_ [_arguments_]"],["PARAMETERS","_COMMAND_\n> Operation: login, set-pipeline, trigger-job, watch, etc.\n\n**-t**, **--target** _TARGET_\n> Concourse target name saved in ~/.flyrc.\n\n**login**\n> Authenticate with a Concourse server (use **-c** _url_ for concourse URL, **-n** _team_ for team).\n\n**logout**\n> Clear saved token for a target.\n\n**targets**\n> List all configured targets and their token expiration.\n\n**set-pipeline**\n> Create or update a pipeline from YAML (**-p** _pipeline_ **-c** _config.yml_).\n\n**get-pipeline**\n> Output a pipeline's current configuration (**-p** _pipeline_).\n\n**destroy-pipeline**\n> Remove a pipeline (**-p** _pipeline_).\n\n**unpause-pipeline**, **pause-pipeline**\n> Enable or disable a pipeline.\n\n**trigger-job**\n> Start job execution (**-j** _pipeline/job_).\n\n**watch**\n> Stream a build's output (**-j** _pipeline/job_ or **-b** _build_).\n\n**execute**\n> Run a one-off task locally (**-c** _task.yml_).\n\n**sync**\n> Update local fly binary to match server version.\n\n**--help**\n> Display help information."],["DESCRIPTION","**fly** is the CLI for Concourse CI, a container-based continuous integration system. It manages pipelines, triggers builds, and interacts with Concourse servers.\n\nThe tool handles pipeline configuration, job management, and build monitoring. Each Concourse instance is registered as a named target for easy switching between CI servers.\n\nfly enables scriptable CI/CD workflows and provides real-time access to build logs and status."],["CAVEATS","Version must match server. Targets need periodic re-authentication. Pipeline YAML syntax is specific."],["HISTORY","fly is the CLI for **Concourse CI**, created by Pivotal (now VMware). Concourse pioneered treating CI pipelines as code with a focus on containers and reproducibility."],["INSTALL","```zypper: sudo zypper install fly```\n\n```nix: nix profile install nixpkgs#fly```"],["SEE ALSO","[concourse](/man/concourse)(1), [jenkins](/man/jenkins)(1), [flyctl](/man/flyctl)(1)"]],"e":["concourse","jenkins","flyctl"],"x":"fly -t [target] set-pipeline"},"foo2oak":{"n":"foo2oak","t":"PBM to OAKT converter for Oak Technology printers","s":[["TAGLINE","PBM to OAKT converter for Oak Technology printers"],["TLDR","**Convert PBM to** OAKT format\n\n```foo2oak [options] < [input.pbm] > [output.prn]```\n\n**Set resolution**\n\n```foo2oak -r [600] < [input.pbm] > [output.prn]```\n\n**Set paper size**\n\n```foo2oak -p [letter] < [input.pbm] > [output.prn]```\n\n**Color mode**\n\n```foo2oak -c < [input.ppm] > [output.prn]```"],["SYNOPSIS","**foo2oak** [_options_] < _input_ > _output_"],["PARAMETERS","**-r** _DPI_\n> Resolution.\n\n**-p** _PAPER_\n> Paper size (letter, a4, legal).\n\n**-c**\n> Color mode.\n\n**-m** _MEDIA_\n> Media type.\n\n**-n** _COPIES_\n> Number of copies.\n\n**-d** _DUPLEX_\n> Duplex mode.\n\n**--help**\n> Display help information."],["DESCRIPTION","**foo2oak** converts PBM/PPM images to OAKT protocol for certain HP and Kyocera printers. It encodes raster data for printers using Oak Technology chips.\n\nThe tool manages the proprietary OAKT encoding required by these printers, handling page setup and color conversion. It functions as a CUPS filter backend.\n\nfoo2oak supports printers like HP LaserJet 1000 and similar models using Oak Technology controllers."],["CAVEATS","Only for printers with Oak Technology chipsets. Printer model support varies. Not all features available."],["HISTORY","foo2oak is part of **foo2zjs**, supporting printers using Oak Technology's OAKT protocol through reverse-engineered drivers."],["INSTALL","```apt: sudo apt install printer-driver-foo2zjs```\n\n```dnf: sudo dnf install foo2oak```"],["SEE ALSO","[foo2oak-wrapper](/man/foo2oak-wrapper)(1), [foo2zjs](/man/foo2zjs)(1)"],["RESOURCES","```[Source code](https://github.com/OpenPrinting/foo2zjs)```"]],"e":["foo2oak-wrapper","foo2zjs"],"x":"foo2oak [options] < [input.pbm] > [output.prn]"},"foo2slx-wrapper":{"n":"foo2slx-wrapper","t":"PostScript/PDF to SLX wrapper script","s":[["TAGLINE","PostScript/PDF to SLX wrapper script"],["TLDR","**Print PostScript file**\n\n```foo2slx-wrapper [file.ps]```\n\n**Set paper size**\n\n```foo2slx-wrapper -p [a4] [file.ps]```\n\n**Color printing**\n\n```foo2slx-wrapper -c [file.ps]```\n\n**Set resolution**\n\n```foo2slx-wrapper -r [600] [file.ps]```"],["SYNOPSIS","**foo2slx-wrapper** [_options_] [_files_...]"],["PARAMETERS","_FILES_\n> PostScript or PDF files to print.\n\n**-p** _PAPER_\n> Paper size (letter, a4, legal).\n\n**-r** _DPI_\n> Resolution.\n\n**-c**\n> Color printing.\n\n**-n** _COPIES_\n> Number of copies.\n\n**-m** _MEDIA_\n> Media type.\n\n**--help**\n> Display help information."],["DESCRIPTION","**foo2slx-wrapper** converts PostScript/PDF documents to SLX format for Lexmark printers. It combines Ghostscript rasterization with foo2slx encoding.\n\nThe wrapper handles the conversion pipeline for easy printing. It provides a command-line interface for supported Lexmark printers.\n\nfoo2slx-wrapper simplifies printing to SLX protocol printers."],["CAVEATS","Requires Ghostscript. Only for SLX-compatible Lexmark models. Feature support varies."],["HISTORY","foo2slx-wrapper is part of the **foo2zjs** driver collection, providing easy printing for Lexmark printers using the SLX protocol."],["INSTALL","```apt: sudo apt install printer-driver-foo2zjs```\n\n```dnf: sudo dnf install foo2slx```"],["SEE ALSO","[foo2slx](/man/foo2slx)(1), [gs](/man/gs)(1), [lpr](/man/lpr)(1)"],["RESOURCES","```[Source code](https://github.com/OpenPrinting/foo2zjs)```"]],"e":["foo2slx","gs","lpr"],"x":"foo2slx-wrapper [file.ps]"},"func":{"n":"func","t":"Azure Functions development CLI","s":[["TAGLINE","Azure Functions development CLI"],["TLDR","**Create new function**\n\n```func new```\n\n**Start local server**\n\n```func start```\n\n**Deploy to Azure**\n\n```func azure functionapp publish [appname]```\n\n**Create function from** template\n\n```func new --template \"[HTTP trigger]\" --name [MyFunction]```\n\n**List templates**\n\n```func templates list```"],["SYNOPSIS","**func** _command_ [_options_]"],["PARAMETERS","_COMMAND_\n> Operation: new, start, deploy, init, etc.\n\n**new**\n> Create new function.\n\n**start**\n> Start local Functions host.\n\n**init**\n> Initialize function project.\n\n**azure functionapp publish** _NAME_\n> Deploy to Azure.\n\n**templates list**\n> Show available templates.\n\n**--help**\n> Display help information."],["CONFIGURATION","**local.settings.json**\n> Local development settings for runtime, connections, and app settings.\n\n**host.json**\n> Global configuration for all functions in a function app."],["DESCRIPTION","**func** is the Azure Functions Core Tools CLI for developing serverless functions locally. It creates, runs, and deploys Azure Functions across multiple languages.\n\nThe tool provides local development with debugging support, mimicking the Azure environment. It handles project scaffolding, dependency management, and deployment workflows.\n\nfunc supports JavaScript, Python, C#, Java, and other languages for Azure Functions development."],["CAVEATS","Requires Azure account for deployment. Local emulator has limitations. Language support varies by version."],["HISTORY","func is part of **Azure Functions Core Tools** from Microsoft, enabling local development of serverless functions for the Azure platform."],["INSTALL","```nix: nix profile install nixpkgs#func```"],["SEE ALSO","[az](/man/az)(1), [dotnet](/man/dotnet)(1)"],["RESOURCES","```[Source code](https://github.com/Azure/azure-functions-core-tools)```"]],"e":["az","dotnet"],"x":"func new"},"gcloud-auth":{"n":"gcloud-auth","t":"Google Cloud authentication management","s":[["TAGLINE","Google Cloud authentication management"],["TLDR","**Login interactively**\n\n```gcloud auth login```\n\n**Activate service account**\n\n```gcloud auth activate-service-account --key-file=[key.json]```\n\n**List accounts**\n\n```gcloud auth list```\n\n**Print access token**\n\n```gcloud auth print-access-token```\n\n**Revoke credentials**\n\n```gcloud auth revoke [account]```"],["SYNOPSIS","**gcloud auth** _command_ [_options_]"],["PARAMETERS","_COMMAND_\n> Operation: login, list, revoke, etc.\n\n**login**\n> Authenticate with user account.\n\n**activate-service-account**\n> Authenticate with service account.\n\n**list**\n> List authenticated accounts.\n\n**print-access-token**\n> Output current access token.\n\n**revoke** _ACCOUNT_\n> Revoke credentials.\n\n**--key-file** _FILE_\n> Service account key file.\n\n**application-default**\n> Manage Application Default Credentials for local development and client libraries.\n\n**configure-docker**\n> Register gcloud as a Docker credential helper for pushing/pulling to Artifact Registry and Container Registry.\n\n**--help**\n> Display help information."],["DESCRIPTION","**gcloud auth** manages authentication and authorization for Google Cloud Platform access. All gcloud operations require valid credentials, which this command group provides and maintains.\n\nFor interactive use, the login command initiates a browser-based OAuth flow where you authenticate with your Google account. For automation and server environments, service accounts can be activated using JSON key files, providing non-interactive authentication suitable for CI/CD pipelines.\n\nThe command supports multiple concurrent authenticated accounts, allowing you to switch between different Google accounts or between user accounts and service accounts. Only one account is active at a time, but switching is quick and preserves all credentials. Access tokens can be printed for use with external tools that need to authenticate to Google Cloud APIs."],["CAVEATS","Browser required for user login. Service account keys are sensitive. Token expiration requires re-authentication."],["HISTORY","gcloud auth is part of the **Google Cloud SDK**, providing unified authentication management for all gcloud commands and GCP API access."],["SEE ALSO","[gcloud](/man/gcloud)(1), [gcloud-config](/man/gcloud-config)(1)"]],"e":["gcloud","gcloud-config"],"x":"gcloud auth login"},"gcloud-compute":{"n":"gcloud-compute","t":"Google Compute Engine resource management","s":[["TAGLINE","Google Compute Engine resource management"],["TLDR","**List VM instances**\n\n```gcloud compute instances list```\n\n**Create instance**\n\n```gcloud compute instances create [name] --zone=[zone]```\n\n**SSH to instance**\n\n```gcloud compute ssh [instance] --zone=[zone]```\n\n**Stop instance**\n\n```gcloud compute instances stop [name] --zone=[zone]```\n\n**List disks**\n\n```gcloud compute disks list```"],["SYNOPSIS","**gcloud compute** _resource_ _command_ [_options_]"],["SUBCOMMANDS","**instances** _CMD_\n> VM instance lifecycle (create, list, start, stop, delete, describe).\n\n**disks** _CMD_\n> Persistent disk operations.\n\n**images** _CMD_\n> Manage custom and public VM images.\n\n**snapshots** _CMD_\n> Create and manage disk snapshots.\n\n**networks** _CMD_\n> VPC networks, subnets and peering.\n\n**firewall-rules** _CMD_\n> Manage VPC firewall rules.\n\n**ssh** _INSTANCE_\n> SSH into an instance (automatically manages keys).\n\n**scp** _SRC_ _DEST_\n> Copy files to or from an instance over SSH."],["PARAMETERS","**--zone** _ZONE_\n> Compute zone for the resource (e.g. us-central1-a).\n\n**--region** _REGION_\n> Compute region for regional resources.\n\n**--project** _PROJECT_\n> Google Cloud project ID to target.\n\n**--format** _FORMAT_\n> Output format (json, yaml, table, value).\n\n**--filter** _EXPR_\n> Filter results by expression.\n\n**--help**\n> Display help information."],["DESCRIPTION","**gcloud compute** is the command-line interface for Google Compute Engine, Google's Infrastructure-as-a-Service offering. It provides complete control over virtual machines, persistent disks, networks, load balancers, and other infrastructure primitives.\n\nThe instances subcommand handles VM lifecycle operations including creation with customizable machine types, starting and stopping instances, and deletion. Network resources like VPCs, subnets, firewall rules, and routes are managed through their respective subcommands.\n\nSpecial convenience features include built-in SSH access via gcloud compute ssh, which automatically manages SSH keys and connection details, and gcloud compute scp for secure file transfers. The command group supports advanced features like custom images, snapshots, instance groups, and managed instance groups for autoscaling. All operations respect zones and regions, which must be specified either per-command or via configuration defaults."],["CAVEATS","Resources incur costs. Zone selection affects availability. Some operations take time."],["HISTORY","gcloud compute is part of the **Google Cloud SDK** for managing Compute Engine, Google's Infrastructure-as-a-Service offering for running virtual machines."],["SEE ALSO","[gcloud](/man/gcloud)(1), [gcloud-container](/man/gcloud-container)(1)"]],"e":["gcloud","gcloud-container"],"x":"gcloud compute instances list"},"gh-api":{"n":"gh-api","t":"authenticated HTTP requests to GitHub REST and GraphQL APIs","s":[["TAGLINE","authenticated HTTP requests to GitHub REST and GraphQL APIs"],["TLDR","**Make GET request**\n\n```gh api [/repos/owner/repo]```\n\n**Make POST request**\n\n```gh api -X POST [/repos/owner/repo/issues] -f title=\"[title]\"```\n\n**With JSON body**\n\n```gh api [/endpoint] --input [data.json]```\n\n**Use GraphQL**\n\n```gh api graphql -f query='[query]'```\n\n**Paginate results**\n\n```gh api [/endpoint] --paginate```\n\n**Output specific field**\n\n```gh api [/repos/owner/repo] --jq '.[field]'```"],["SYNOPSIS","**gh** **api** [_options_] _endpoint_"],["PARAMETERS","**-X**, **--method** _method_\n> HTTP method.\n\n**-f**, **--raw-field** _key=value_\n> Add a string parameter.\n\n**-F**, **--field** _key=value_\n> Add a typed parameter (non-string values, file references with @, booleans, null).\n\n**--input** _file_\n> Read body from file.\n\n**--jq** _query_\n> Filter JSON output.\n\n**--paginate**\n> Fetch all pages of results.\n\n**--slurp**\n> With --paginate, combine all pages into a single JSON array.\n\n**-i**, **--include**\n> Include HTTP response headers in the output.\n\n**-p**, **--preview** _name_\n> Opt into a GitHub API preview feature.\n\n**-H**, **--header** _header_\n> Add HTTP header.\n\n**-t**, **--template** _template_\n> Format output with a Go template.\n\n**--hostname** _host_\n> Make request against a specific GitHub hostname (for GHES).\n\n**--cache** _duration_\n> Cache the response (e.g., 1h, 30m)."],["DESCRIPTION","**gh api** makes authenticated HTTP requests to the GitHub API. Supports both REST and GraphQL APIs with automatic authentication."],["INSTALL","```apt: sudo apt install gh```\n\n```dnf: sudo dnf install gh```\n\n```pacman: sudo pacman -S github-cli```\n\n```apk: sudo apk add github-cli```\n\n```zypper: sudo zypper install gh```\n\n```brew: brew install gh```\n\n```nix: nix profile install nixpkgs#gh```"],["SEE ALSO","[gh](/man/gh)(1), [curl](/man/curl)(1)"]],"e":["gh","curl"],"x":"gh api [/repos/owner/repo]"},"git-browse-ci":{"n":"git-browse-ci","t":"open the CI page of a repository in the browser","s":[["TAGLINE","open the CI page of a repository in the browser"],["TLDR","**Open** the CI page for the current branch's remote\n\n```git browse-ci```\n\n**Open** the CI page for a specific remote\n\n```git browse-ci [upstream]```"],["SYNOPSIS","**git browse-ci** [_remote_]"],["PARAMETERS","_remote_\n> The remote whose CI page to open. Defaults to the remote configured for the current branch, falling back to **origin**."],["DESCRIPTION","**git browse-ci** is a git-extras helper that opens the continuous integration page for your repository in a web browser, saving a trip through the hosting provider's web interface.\n\nIt works purely from the remote URL. The remote is normalized from either SSH (**git@host:owner/repo.git**) or HTTP form into a browsable URL, and a per-host suffix is appended to reach the CI section:\n\n- **GitHub** hosts get **/actions**\n- **GitLab** hosts get **/-/pipelines**\n- **Bitbucket** hosts get **/addon/pipelines/home**\n\nThe resulting URL is handed to the platform's opener: **open** on macOS, **start** on Git-Bash, **powershell.exe start** under WSL, and **xdg-open** on Linux and the BSDs."],["CAVEATS","Only **GitHub**, **GitLab** and **Bitbucket** are recognized, and the match is a substring test against the remote URL. Self-hosted instances on a domain that does not contain one of those three words are not detected, and neither are standalone CI services such as CircleCI, Travis CI, Jenkins or Drone, regardless of what the repository actually uses.\n\nWhen the host is not recognized the CI URL ends up empty, and rather than reporting the problem the command asks the browser to open nothing.\n\nThe URL is built by string manipulation on the remote, so it is a guess, not a lookup. It does not check whether CI is configured, or even whether the page exists.\n\nSSH remotes are rewritten to **http://** rather than **https://**, relying on the host to redirect."],["HISTORY","**git browse-ci** is part of **git-extras**, the collection of git helper scripts started by **TJ Holowaychuk** in **2010** and now community-maintained."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-browse](/man/git-browse)(1), [git-extras](/man/git-extras)(1), [git-remote](/man/git-remote)(1), [gh-run](/man/gh-run)(1), [xdg-open](/man/xdg-open)(1)"],["RESOURCES","```[Source code](https://github.com/tj/git-extras)```\n\n```[Documentation](https://github.com/tj/git-extras/blob/master/Commands.md#git-browse-ci)```"]],"e":["git-browse","git-extras","git-remote","gh-run","xdg-open"],"x":"git browse-ci"},"git-xargs":{"n":"git-xargs","t":"Make the same change across many GitHub repositories","s":[["TAGLINE","Make the same change across many GitHub repositories"],["TLDR","**Run a command** in repos matching a query and open PRs\n\n```git-xargs --branch-name [update] --github-org [org] --commit-message \"[msg]\" -- [make] [format]```\n\n**Target** repos from a file\n\n```git-xargs --repos [repos.txt] --branch-name [chore] --commit-message \"[msg]\" -- [./script.sh]```\n\n**Dry run**\n\n```git-xargs --dry-run --github-org [org] -- [true]```"],["SYNOPSIS","**git-xargs** [*options*] **--** *command* [*args*...]"],["DESCRIPTION","**git-xargs** (Gruntwork) clones selected GitHub repositories, runs an arbitrary command in each, and can commit, push, and open pull requests with the results. Built for platform teams applying mechanical refactors or policy files across an organization."],["PARAMETERS","**--github-org** *org* / **--repos** *file* / repo selectors\n\n> Choose target repositories.\n\n**--branch-name** *name*\n\n> Branch for commits/PRs.\n\n**--commit-message** *msg*\n\n> Commit message when changes exist.\n\n**--pr-title** / **--pr-body**\n\n> Pull request fields.\n\n**--dry-run**\n\n> Print actions without modifying remotes.\n\n**--** *command*\n\n> Command executed inside each clone.\n\nRequires GitHub authentication (token with repo scope)."],["CAVEATS","Powerful and potentially destructive—test with **--dry-run** and a single repo first. Respect branch protection and CODEOWNERS. Disk and API rate limits matter at org scale."],["INSTALL","```brew: brew install git-xargs```"],["SEE ALSO","[multi-gitter](/man/multi-gitter)(1), [gh](/man/gh)(1), [git](/man/git)(1)"],["RESOURCES","```[Source code](https://github.com/gruntwork-io/git-xargs)```"]],"e":["multi-gitter","gh","git"],"x":"git-xargs --branch-name [update] --github-org [org] --commit-message \"[msg]\" -- [make] [format]"},"gleam":{"n":"gleam","t":"Gleam programming language compiler and build tool","s":[["TAGLINE","Gleam programming language compiler and build tool"],["TLDR","**Create new project**\n\n```gleam new [project_name]```\n\n**Build project**\n\n```gleam build```\n\n**Run project**\n\n```gleam run```\n\n**Run tests**\n\n```gleam test```\n\n**Add a Hex dependency**\n\n```gleam add [package_name]```\n\n**Remove a dependency**\n\n```gleam remove [package_name]```\n\n**Format source code**\n\n```gleam format```\n\n**Generate HTML documentation**\n\n```gleam docs build```"],["SYNOPSIS","**gleam** _command_ [_options_]"],["PARAMETERS","**new** _NAME_\n> Create a new project with standard directory structure.\n\n**build**\n> Compile the project and its dependencies.\n\n**run**\n> Build and run the project.\n\n**test**\n> Build and run the test suite.\n\n**add** _PACKAGE_\n> Add a Hex package dependency.\n\n**remove** _PACKAGE_\n> Remove a dependency.\n\n**check**\n> Type-check the project without producing build artifacts.\n\n**format**\n> Format source code files.\n\n**docs build**\n> Generate HTML documentation.\n\n**docs publish**\n> Publish documentation to HexDocs.\n\n**publish**\n> Publish the package to Hex.\n\n**shell**\n> Start an Erlang shell with the project loaded.\n\n**export erlang-shipment**\n> Export a standalone Erlang release for deployment.\n\n**--target** _TARGET_\n> Compile target: erlang or javascript.\n\n**--help**\n> Display help information."],["DESCRIPTION","**gleam** is the build tool and compiler for the Gleam programming language, a type-safe functional language that compiles to Erlang and JavaScript. It manages projects, dependencies, compilation, testing, and documentation.\n\nGleam integrates with the Hex package manager for the Erlang/Elixir ecosystem and can interoperate with Erlang and Elixir code. The JavaScript target allows Gleam to run in browsers and Node.js/Deno/Bun."],["CONFIGURATION","**gleam.toml**\n> Project configuration file defining name, version, target, dependencies, and build options."],["CAVEATS","Requires Erlang/OTP for the BEAM target. The JavaScript target needs Node.js, Deno, or Bun. Gleam has no runtime exceptions by design -- exhaustive pattern matching is enforced at compile time."],["HISTORY","Gleam was created by **Louis Pilfold** starting in **2018** to bring static type safety to the BEAM virtual machine ecosystem while maintaining compatibility with Erlang and Elixir libraries. Version 1.0 was released in **March 2024**."],["INSTALL","```pacman: sudo pacman -S gleam```\n\n```apk: sudo apk add gleam```\n\n```zypper: sudo zypper install gleam```\n\n```brew: brew install gleam```\n\n```nix: nix profile install nixpkgs#gleam```"],["SEE ALSO","[elixir](/man/elixir)(1), [cargo](/man/cargo)(1), [mix](/man/mix)(1)"]],"e":["elixir","cargo","mix"],"x":"gleam new [project_name]"},"gnome-panel":{"n":"gnome-panel","t":"traditional GNOME desktop panel and taskbar","s":[["TAGLINE","traditional GNOME desktop panel and taskbar"],["TLDR","**Start GNOME Panel**\n\n```gnome-panel```\n\n**Replace running panel**\n\n```gnome-panel --replace```"],["SYNOPSIS","**gnome-panel** [_options_]"],["PARAMETERS","**--replace**\n> Replace running panel.\n\n**--version**\n> Print version information and exit\n\n**--help**\n> Display help information"],["DESCRIPTION","**gnome-panel** provides the classic GNOME 2-style panel used by the **GNOME Flashback** session. It hosts the application menu, window list, system tray, clock, notification area, workspace switcher, and any number of applets (PanelApplets), arranged across one or more screen-edge panels. Layouts, applets, and applet preferences are stored in **dconf** under **/org/gnome/gnome-panel/**.\n\nThe panel is launched automatically when a user logs into the GNOME Flashback session; **--replace** stops a running instance and starts a fresh one, useful after editing configuration or installing new applets."],["CAVEATS","Part of GNOME Flashback. Modern GNOME (3.x and later) uses **gnome-shell** instead, which does not load gnome-panel applets. Requires a working DBus session bus, **gnome-settings-daemon**, and the panel's GSettings schemas."],["HISTORY","gnome-panel was the main **GNOME 2** panel, now maintained as part of GNOME Flashback for users preferring the classic desktop paradigm."],["INSTALL","```apt: sudo apt install gnome-panel```\n\n```dnf: sudo dnf install gnome-panel```\n\n```pacman: sudo pacman -S gnome-panel```\n\n```apk: sudo apk add gnome-panel```\n\n```nix: nix profile install nixpkgs#gnome-panel```"],["SEE ALSO","[gnome-calculator](/man/gnome-calculator)(1), [gnome-screenshot](/man/gnome-screenshot)(1), [gsettings](/man/gsettings)(1)"]],"e":["gnome-calculator","gnome-screenshot","gsettings"],"x":"gnome-panel"},"godap":{"n":"godap","t":"TUI for LDAP directory browsing","s":[["TAGLINE","TUI for LDAP directory browsing"],["TLDR","**Connect** to LDAP server\n\n```godap [ldap://server:389]```\n\n**Connect** with authentication\n\n```godap -D [bind-dn] -W [ldap://server]```\n\n**Search** specific base DN\n\n```godap -b [ou=users,dc=example,dc=com] [ldap://server]```"],["SYNOPSIS","**godap** [_options_] [_ldap-uri_]"],["PARAMETERS","**-D, --bind-dn** _DN_\n> Bind DN for authentication\n\n**-W**\n> Prompt for bind password\n\n**-w, --password** _PASSWORD_\n> Bind password (insecure)\n\n**-b, --base-dn** _DN_\n> Base DN for search\n\n**-H, --uri** _URI_\n> LDAP server URI\n\n**-Z, --starttls**\n> Use StartTLS\n\n**-x, --simple-auth**\n> Use simple authentication\n\n**-v, --version**\n> Display version and exit\n\n**-h, --help**\n> Display help and exit"],["DESCRIPTION","**godap** is a terminal user interface for browsing LDAP directories. It allows users to navigate the directory tree, view entries, and perform searches interactively.\n\nThe tool supports various LDAP authentication methods, TLS/SSL connections, and displays LDAP entries in a readable format."],["KEYBINDINGS","**↑/↓**\n> Navigate entries\n\n**Enter**\n> View entry details\n\n**n**\n> New search\n\n**/ or s**\n> Search current context\n\n**b**\n> Go back\n\n**q**\n> Quit"],["CAVEATS","Requires LDAP server access. Bind credentials must have appropriate permissions. Large directories may have performance issues. TLS certificate validation depends on system configuration."],["HISTORY","**godap** was created to provide an interactive way to browse LDAP directories from the terminal, similar to GUI LDAP browsers."],["INSTALL","```apk: sudo apk add godap```\n\n```brew: brew install godap```"],["SEE ALSO","[ldapsearch](/man/ldapsearch)(1)"]],"e":["ldapsearch"],"x":"godap [ldap://server:389]"},"grpck":{"n":"grpck","t":"verify integrity of group files","s":[["TAGLINE","verify integrity of group files"],["TLDR","**Check group file integrity**\n\n```sudo grpck```\n\n**Read-only mode**\n\n```sudo grpck -r```\n\n**Sort by GID**\n\n```sudo grpck -s```\n\n**Check specific files**\n\n```sudo grpck [/etc/group] [/etc/gshadow]```"],["SYNOPSIS","**grpck** [_options_] [_group_] [_shadow_]"],["PARAMETERS","_GROUP_\n> Group file to check (default /etc/group).\n\n_SHADOW_\n> Shadow group file (default /etc/gshadow).\n\n**-r**\n> Read-only mode, report only.\n\n**-s**\n> Sort entries by GID.\n\n**-R** _DIR_\n> Chroot directory.\n\n**--help**\n> Display help information."],["DESCRIPTION","**grpck** verifies the integrity of group information files. It checks for duplicate entries, valid field formats, and consistency between /etc/group and /etc/gshadow.\n\nThe tool can automatically fix some issues when run interactively. It is essential for maintaining system security and proper group functionality."],["CAVEATS","Requires root access. Interactive fixes need confirmation. Back up files before repair."],["HISTORY","grpck was developed as part of the **shadow** password suite for Unix/Linux system administration."],["INSTALL","```apt: sudo apt install passwd```\n\n```pacman: sudo pacman -S shadow```\n\n```apk: sudo apk add shadow```\n\n```zypper: sudo zypper install shadow```\n\n```nix: nix profile install nixpkgs#shadow```"],["SEE ALSO","[pwck](/man/pwck)(8), [groups](/man/groups)(1), [gpasswd](/man/gpasswd)(1)"],["RESOURCES","```[Source code](https://github.com/shadow-maint/shadow)```"]],"e":["pwck","groups","gpasswd"],"x":"sudo grpck"},"grubby":{"n":"grubby","t":"command-line bootloader configuration tool for Red Hat systems","s":[["TAGLINE","command-line bootloader configuration tool for Red Hat systems"],["TLDR","Add kernel boot **arguments** to all kernel menu entries\n\n```sudo grubby --update-kernel=ALL --args 'quiet console=ttyS0'```\n\n**Remove** existing arguments from the default kernel entry\n\n```sudo grubby --update-kernel=DEFAULT --remove-args quiet```\n\n**List** all kernel menu entries\n\n```sudo grubby --info=ALL```"],["SYNOPSIS","**grubby** [_options_]"],["PARAMETERS","**--info=**_KERNEL_\n> Display boot information for specified kernel (or ALL)\n\n**--update-kernel=**_KERNEL_\n> Update specified kernel (ALL, DEFAULT, or path)\n\n**--args=**_ARGS_\n> Add kernel arguments\n\n**--remove-args=**_ARGS_\n> Remove kernel arguments\n\n**--set-default=**_KERNEL_\n> Set default boot kernel\n\n**--default-kernel**\n> Display path of default kernel\n\n**--default-index**\n> Display index of default kernel\n\n**--add-kernel=**_PATH_\n> Add a new kernel entry (requires **--title**)\n\n**--remove-kernel=**_PATH_\n> Remove a kernel entry\n\n**--title=**_TITLE_\n> Set title for new kernel entry\n\n**--initrd=**_PATH_\n> Set the initrd for a new kernel entry\n\n**--copy-default**\n> Copy arguments and root device from the current default entry\n\n**--make-default**\n> Make the newly added entry the default\n\n**--grub2**\n> Use grub2-style config (default on x86_64/BLS systems)"],["DESCRIPTION","**grubby** is a command-line tool for updating and modifying bootloader configuration on Red Hat-based systems. It works with GRUB, GRUB2, and zipl (for s390 systems), providing a unified interface for boot configuration changes.\n\nThe tool modifies kernel command-line arguments, manages boot entries, and sets default kernels without directly editing configuration files. On modern Fedora/RHEL systems using GRUB2 with BLS (Boot Loader Specification), grubby modifies the individual kernel entry files in /boot/loader/entries/."],["CAVEATS","Behavior varies between GRUB Legacy, GRUB2, and BLS configurations. Some options may not work with all bootloader configurations. Changes take effect on next boot. Always verify configuration after changes."],["HISTORY","grubby was developed by Red Hat to simplify bootloader management across different configurations. It became essential for automated system management and kernel updates on Red Hat-based distributions. The original **rhboot/grubby** project was retired in 2022; with the adoption of BLS, its functionality now lives on as a compatible shell script maintained in Fedora's dist-git."],["INSTALL","```dnf: sudo dnf install grubby```"],["SEE ALSO","[grub-mkconfig](/man/grub-mkconfig)(8), [grub-set-default](/man/grub-set-default)(8), [kernel-install](/man/kernel-install)(8)"],["RESOURCES","```[Source code](https://github.com/rhboot/grubby)```"]],"e":["grub-mkconfig","grub-set-default","kernel-install"],"x":"sudo grubby --update-kernel=ALL --args 'quiet console=ttyS0'"},"http-server":{"n":"http-server","t":"simple static file server","s":[["TAGLINE","simple static file server"],["TLDR","**Start server**\n\n```http-server```\n\n**Specify port**\n\n```http-server -p [8080]```\n\n**Specify directory**\n\n```http-server [./public]```\n\n**Enable CORS**\n\n```http-server --cors```\n\n**Enable gzip**\n\n```http-server -g```\n\n**SSL server**\n\n```http-server -S -C [cert.pem] -K [key.pem]```"],["SYNOPSIS","**http-server** [_options_] [_path_]"],["PARAMETERS","_PATH_\n> Directory to serve.\n\n**-p**, **--port** _PORT_\n> Port number (default 8080).\n\n**-a** _ADDRESS_\n> Listen address.\n\n**-c** _SECONDS_\n> Cache time.\n\n**--cors**\n> Enable CORS headers.\n\n**-g**, **--gzip**\n> Enable gzip.\n\n**-S**, **--ssl**\n> Enable SSL.\n\n**-C** _FILE_\n> SSL certificate file (default: cert.pem).\n\n**-K** _FILE_\n> SSL key file (default: key.pem).\n\n**-o**\n> Open browser after starting.\n\n**-P**, **--proxy** _URL_\n> Proxy unresolvable requests to this URL.\n\n**--no-dotfiles**\n> Do not serve dotfiles."],["DESCRIPTION","**http-server** is a simple, zero-configuration static file server for Node.js. It serves files from a directory over HTTP, making it ideal for quickly testing frontend builds, sharing files on a local network, or previewing static sites.\n\nInstall via `npm install -g http-server`. Serves the current directory by default on port 8080."],["CAVEATS","Requires Node.js and npm. Not suitable for production use — provides no authentication, rate limiting, or hardened security. For production static file serving, use nginx or a CDN."],["HISTORY","http-server is a popular **npm** package for quickly serving static files during development."],["INSTALL","```brew: brew install http-server```\n\n```nix: nix profile install nixpkgs#http-server```"],["SEE ALSO","[serve](/man/serve)(1), [python](/man/python)(1), [live-server](/man/live-server)(1)"]],"e":["serve","python","live-server"],"x":"http-server"},"impacket-secretsdump":{"n":"impacket-secretsdump","t":"extracts credentials from Windows systems via SAM, LSA secrets, cached","s":[["TAGLINE","extracts credentials from Windows systems via SAM, LSA secrets, cached"],["TLDR","**Dump secrets from domain controller**\n\n```impacket-secretsdump [domain]/[user]:[password]@[target]```\n\n**Dump using NTLM hash**\n\n```impacket-secretsdump -hashes :[hash] [domain]/[user]@[target]```\n\n**Dump from local SAM file**\n\n```impacket-secretsdump -sam [SAM] -system [SYSTEM] LOCAL```\n\n**Dump NTDS.dit**\n\n```impacket-secretsdump -ntds [ntds.dit] -system [SYSTEM] LOCAL```\n\n**Just DCSync (no registry)**\n\n```impacket-secretsdump -just-dc [domain]/[user]:[password]@[dc]```"],["SYNOPSIS","**impacket-secretsdump** [_options_] _target_"],["PARAMETERS","**-hashes** _lm:nt_\n> Use NTLM hashes.\n\n**-sam** _file_\n> Local SAM file.\n\n**-system** _file_\n> Local SYSTEM hive.\n\n**-ntds** _file_\n> NTDS.dit file.\n\n**-just-dc**\n> Extract only NTDS.dit data via DCSync.\n\n**-just-dc-ntlm**\n> Extract only NTLM hashes.\n\n**-k**\n> Use Kerberos authentication."],["DESCRIPTION","**impacket-secretsdump** extracts credentials from Windows systems via SAM, LSA secrets, cached credentials, and NTDS.dit. Part of the Impacket toolkit. Supports DCSync for domain controllers. For authorized penetration testing and security assessments only."],["CAVEATS","Highly sensitive operation. Requires administrator/domain admin access. For authorized security testing only."],["INSTALL","```pacman: sudo pacman -S impacket```"],["SEE ALSO","[impacket-psexec](/man/impacket-psexec)(1)"]],"e":["impacket-psexec"],"x":"impacket-secretsdump [domain]/[user]:[password]@[target]"},"inference-snap":{"n":"inference-snap","t":"Local generative AI chat via Ubuntu inference snaps","s":[["TAGLINE","Local generative AI chat via Ubuntu inference snaps"],["TLDR","**Start** an interactive terminal chat session\n\n```inference-snap chat```\n\n**Show** the active engine and chat server status\n\n```inference-snap status```\n\n**Select** a hardware engine and matching model\n\n```sudo inference-snap use-engine [cuda|cpu|...]```\n\n**Print** host hardware details for model selection\n\n```sudo inference-snap show-machine```\n\n**Read** the running chat server configuration\n\n```inference-snap get```"],["SYNOPSIS","**inference-snap** **chat**\n\n**inference-snap** **status**\n\n**inference-snap** **get**\n\n**sudo inference-snap** **use-engine** _engine_\n\n**sudo inference-snap** **show-machine**"],["DESCRIPTION","**inference-snap** is the command-line interface for Ubuntu **Inference Snaps**: packaged generative AI models tuned for local CPU, GPU, or NPU hardware. Each snap ships model weights and a runtime that auto-detects the host and exposes a local chat API.\n\nThe **chat** subcommand starts a terminal conversation and, on first use, brings up the background chat server. Supported model families include **deepseek-r1**, **gemma3**, **gemma4**, **nemotron3-nano**, and **qwen-vl**, depending on which inference snap is installed.\n\n**use-engine** switches the execution backend (for example **cuda** on NVIDIA GPUs) and downloads the model variant appropriate for that engine. **show-machine** reports RAM, architecture, and device details to help pick an engine. **get** prints server settings such as **http.host** and **http.port**; **status** summarizes engine choice and service health.\n\nInference snaps also expose an OpenAI-compatible HTTP API for IDEs and other tools, but **inference-snap** itself is the snap-managed CLI for chat and administration."],["CAVEATS","Requires a supported Ubuntu inference snap installed via **snap**. Engine changes and hardware inspection need **sudo**. First **chat** start may download model weights and take noticeable time."],["SEE ALSO","[snap](/man/snap)(8), [curl](/man/curl)(1)"]],"e":["snap","curl"],"x":"inference-snap chat"},"isutf8":{"n":"isutf8","t":"checks whether files contain valid UTF-8 encoded text","s":[["TAGLINE","checks whether files contain valid UTF-8 encoded text"],["TLDR","**Check if file is valid UTF-8**\n\n```isutf8 [file]```\n\n**Check multiple files**\n\n```isutf8 [file1] [file2] [file3]```\n\n**Quiet mode (exit code only)**\n\n```isutf8 -q [file]```\n\n**List invalid files only**\n\n```isutf8 -l [files...]```"],["SYNOPSIS","**isutf8** [_options_] _file_..."],["PARAMETERS","**-q**, **--quiet**\n> No output, only exit code.\n\n**-l**, **--list**\n> List files that are not valid UTF-8.\n\n**-h**, **--help**\n> Show help."],["DESCRIPTION","**isutf8** checks whether files contain valid UTF-8 encoded text. Part of the moreutils package. Returns exit code 0 if valid UTF-8, 1 if invalid. Useful in scripts to validate file encoding before processing."],["INSTALL","```apt: sudo apt install moreutils```\n\n```dnf: sudo dnf install moreutils```\n\n```pacman: sudo pacman -S moreutils```\n\n```apk: sudo apk add moreutils```\n\n```zypper: sudo zypper install moreutils```\n\n```brew: brew install moreutils```\n\n```nix: nix profile install nixpkgs#moreutils```"],["SEE ALSO","[file](/man/file)(1), [iconv](/man/iconv)(1)"]],"e":["file","iconv"],"x":"isutf8 [file]"},"kaggle-models":{"n":"kaggle-models","t":"manages Kaggle machine learning models from the command line","s":[["TAGLINE","manages Kaggle machine learning models from the command line"],["TLDR","**List models**\n\n```kaggle models list```\n\n**Get model details**\n\n```kaggle models get -m [owner/model-name]```\n\n**Initialize model metadata**\n\n```kaggle models init -p [path]```\n\n**Create a new model**\n\n```kaggle models create -p [path]```\n\n**Download model instance version**\n\n```kaggle models instances versions download -m [owner/model/framework/instance] -v [version]```"],["SYNOPSIS","**kaggle** **models** _subcommand_ [_options_]"],["PARAMETERS","**list**\n> List available models.\n\n**get** **-m** _model_\n> Get model details.\n\n**init**\n> Initialize model metadata.\n\n**create**\n> Create new model.\n\n**update**\n> Update an existing model.\n\n**delete**\n> Delete a model.\n\n**instances**\n> Manage model instances (get, init, create, update, delete, files).\n\n**instances versions**\n> Manage model instance versions (create, download, delete, files)."],["DESCRIPTION","**kaggle models** manages Kaggle machine learning models from the command line. Part of the Kaggle CLI, it provides access to pre-trained models and allows publishing custom models for the community.\n\nThe command hierarchy has three levels: **models** (top-level repositories), **instances** (specific framework implementations), and **instance versions** (versioned snapshots). The shorthand **kaggle m** can be used instead of **kaggle models**."],["SEE ALSO","[kaggle](/man/kaggle)(1), [kaggle-datasets](/man/kaggle-datasets)(1), [kaggle-competitions](/man/kaggle-competitions)(1), [kaggle-kernels](/man/kaggle-kernels)(1)"]],"e":["kaggle","kaggle-datasets","kaggle-competitions","kaggle-kernels"],"x":"kaggle models list"},"lazyjj":{"n":"lazyjj","t":"TUI for the Jujutsu version control system","s":[["TAGLINE","TUI for the Jujutsu version control system"],["TLDR","**Launch the TUI** in the current repository\n\n```lazyjj```\n\n**Open a specific repository**\n\n```lazyjj -p [path/to/repo]```"],["SYNOPSIS","**lazyjj** [_options_]"],["DESCRIPTION","**lazyjj** is a TUI for the Jujutsu (jj) version control system. It presents repository history, working files, and bookmarks interactively through three main panels: Log, Files, and Bookmarks.\n\nThe interface supports creating, editing, squashing, abandoning, fetching, pushing, describing, and bookmarking changes. The Log tab shows the revset list alongside a panel for viewing color-word and git diffs. It works as a front-end that invokes the **jj** CLI and renders repository state."],["CAVEATS","Requires **jj** version 0.33.0 or higher for compatibility with the template language features used for output parsing."],["HISTORY","**lazyjj** was created by **Cretezy** and is written in **Rust** using the ratatui library."],["INSTALL","```pacman: sudo pacman -S lazyjj```\n\n```brew: brew install lazyjj```\n\n```nix: nix profile install nixpkgs#lazyjj```"],["SEE ALSO","[jj](/man/jj)(1), [lazygit](/man/lazygit)(1)"]],"e":["jj","lazygit"],"x":"lazyjj"},"lla":{"n":"lla","t":"Blazing fast ls replacement with superpowers","s":[["TAGLINE","Blazing fast ls replacement with superpowers"],["TLDR","**List files** in the current directory\n\n```lla```\n\n**List files in long format**\n\n```lla -l```\n\n**List files as a tree**\n\n```lla -t```\n\n**List files in a table layout**\n\n```lla -T```\n\n**List files in a grid**\n\n```lla -g```"],["SYNOPSIS","**lla** [_options_] [_path_...]"],["PARAMETERS","**-l**, **--long**\n> Display in long format with detailed information.\n\n**-t**, **--tree**\n> Display as a tree structure.\n\n**-T**, **--table**\n> Display in table layout.\n\n**-g**, **--grid**\n> Display in grid layout.\n\n**-s**, **--sort** _CRITERIA_\n> Sort by criteria (name, size, date)."],["DESCRIPTION","**lla** is a modern **ls** replacement built in Rust with multiple view modes (default, long, tree, table, grid), Git integration with status visualization, smart search with complex filtering patterns (OR, AND, NOT, XOR), regex support, and content search.\n\nAdditional features include a diff command for directory or file comparisons, timeline view, storage analysis, interactive directory navigation with bookmarks and history, size filters with human-readable units, and time filters supporting ISO dates and relative durations."],["CAVEATS","Some features like Git integration require a Git repository. Plugin ecosystem extends functionality but adds complexity."],["HISTORY","**lla** was created by **chaqchase** and is written in **Rust**."],["INSTALL","```brew: brew install lla```\n\n```nix: nix profile install nixpkgs#lla```"],["SEE ALSO","[ls](/man/ls)(1), [eza](/man/eza)(1), [lsd](/man/lsd)(1)"]],"e":["ls","eza","lsd"],"x":"lla"},"logshark":{"n":"logshark","t":"CLI and TUI debugger for JSON logs","s":[["TAGLINE","CLI and TUI debugger for JSON logs"],["TLDR","**Stream and debug JSON logs**\n\n```logshark```\n\n**Pipe logs from a command**\n\n```cat [logfile.json] | logshark```"],["SYNOPSIS","**logshark** [_options_]"],["DESCRIPTION","**logshark** is a command-line tool for debugging JSON logs. It integrates with Beats and Logstash to provide real-time log analysis, featuring JSON formatting, pretty printing with syntax highlighting, and event metrics.\n\nThe TUI mode allows navigating through a list of log entries with colorful formatting, making it easier to debug and analyze structured log data from Elastic Stack and similar logging pipelines."],["CAVEATS","Designed specifically for JSON-formatted logs. Non-JSON log formats are not supported."],["HISTORY","**logshark** was created by **ugosan** and is written in **Go**."],["SEE ALSO","[jq](/man/jq)(1), [lnav](/man/lnav)(1), [loggo](/man/loggo)(1)"]],"e":["jq","lnav","loggo"],"x":"logshark"},"minitest":{"n":"minitest","t":"testing framework for Ruby that provides a complete suite of testing facilities","s":[["TAGLINE","testing framework for Ruby that provides a complete suite of testing facilities"],["TLDR","**Run all tests**\n\n```ruby -Ilib:test [test/test_*.rb]```\n\n**Run specific test file**\n\n```ruby -Ilib:test [test/test_example.rb]```\n\n**Run tests with verbose output**\n\n```ruby -Ilib:test [test/test_example.rb] --verbose```\n\n**Run specific test method**\n\n```ruby -Ilib:test [test/test_example.rb] --name [test_method_name]```\n\n**Run with pattern**\n\n```ruby -Ilib:test [test/test_example.rb] --name /[pattern]/```"],["SYNOPSIS","**ruby** -Ilib:test _test_file_ [_options_]"],["PARAMETERS","**--verbose**\n> Verbose output.\n\n**--name** _pattern_\n> Run matching tests.\n\n**--seed** _n_\n> Random seed.\n\n**--pride**\n> Rainbow output."],["DESCRIPTION","**Minitest** is a testing framework for Ruby that provides a complete suite of testing facilities. It includes unit tests, specs, mocking, and benchmarking.\n\nMinitest is bundled with Ruby and is known for being fast and simple."],["TEST EXAMPLE","```ruby\nrequire 'minitest/autorun'\n\nclass TestExample < Minitest::Test\n  def setup\n    @value = 1\n  end\n\n  def test_addition\n    assert_equal 2, @value + 1\n  end\nend\n```"],["SPEC SYNTAX","```ruby\ndescribe Example do\n  it \"works\" do\n    _(1 + 1).must_equal 2\n  end\nend\n```"],["CAVEATS","Bundled with Ruby since 1.9. Tests run in random order by default. Fewer features than RSpec but faster."],["HISTORY","Minitest was created by **Ryan Davis** (zenspider) as a minimal testing framework, included in Ruby's standard library since Ruby **1.9**."],["SEE ALSO","[rspec](/man/rspec)(1), [rake](/man/rake)(1), [ruby](/man/ruby)(1)"]],"e":["rspec","rake","ruby"],"x":"ruby -Ilib:test [test/test_*.rb]"},"mount.steamos":{"n":"mount.steamos","t":"mounts or unmounts SteamOS filesystem partitions, used primarily for accessing","s":[["TAGLINE","mounts or unmounts SteamOS filesystem partitions, used primarily for accessing"],["TLDR","**Mount** all necessary partitions from a device to a target directory\n\n```sudo mount.steamos [/dev/sdX] [/mnt]```\n\nMount with options to **exclude specific partitions** (e.g. /home, overlays)\n\n```sudo mount.steamos -o nohome,nooverlay [/dev/sdX] [/mnt]```\n\n**Unmount** all partitions mounted under a target directory\n\n```sudo mount.steamos -u [/mnt]```\n\nDisplay **help**\n\n```mount.steamos -h```"],["SYNOPSIS","**mount.steamos** [_options_] _device_ _mountpoint_"],["PARAMETERS","**-o, --options _opts_**\n> Mount options (nohome, nooverlay, etc.)\n\n**-u, --unmount**\n> Unmount all partitions under the specified directory\n\n**-h, --help**\n> Display help information"],["DESCRIPTION","**mount.steamos** mounts or unmounts SteamOS filesystem partitions, used primarily for accessing Steam Deck storage when booted from another operating system or recovery media.\n\nSteamOS uses a complex partition layout with multiple overlays for system updates and user data separation. This tool handles the specific mount requirements, including the A/B root partitions, overlay filesystems, and home directory.\n\nThe command simplifies what would otherwise require multiple mount commands with specific options for each partition."],["CAVEATS","Requires root privileges. Designed specifically for SteamOS partition layouts (Steam Deck). The device should be a complete disk device (e.g., /dev/sda), not individual partitions. Ensure SteamOS is not running when mounting its partitions from another system."],["SEE ALSO","[mount](/man/mount)(8), [steamos-dump-info](/man/steamos-dump-info)(1), [umount](/man/umount)(8)"]],"e":["mount","steamos-dump-info","umount"],"x":"sudo mount.steamos [/dev/sdX] [/mnt]"},"mpirun":{"n":"mpirun","t":"launches MPI parallel programs","s":[["TAGLINE","launches MPI parallel programs"],["TLDR","**Run MPI program**\n\n```mpirun -np [4] [program]```\n\n**Run on multiple hosts**\n\n```mpirun -np [8] --hostfile [hosts.txt] [program]```\n\n**Run with specific slots per host**\n\n```mpirun -np [4] --map-by node [program]```"],["SYNOPSIS","**mpirun** [_options_] _program_ [_args_...]"],["PARAMETERS","**-np** _n_\n> Number of processes.\n\n**--hostfile** _file_\n> File with host list.\n\n**--host** _hosts_\n> Comma-separated hosts.\n\n**--map-by** _policy_\n> Process mapping (node, slot, core).\n\n**--bind-to** _policy_\n> Process binding.\n\n**-x** _var_\n> Export environment variable."],["DESCRIPTION","**mpirun** launches MPI parallel programs. Starts multiple processes across nodes for distributed computing. Part of OpenMPI or MPICH implementations."],["INSTALL","```dnf: sudo dnf install openmpi```\n\n```pacman: sudo pacman -S openmpi```\n\n```apk: sudo apk add openmpi```\n\n```brew: brew install openmpi```\n\n```nix: nix profile install nixpkgs#openmpi```"],["SEE ALSO","[mpiexec](/man/mpiexec)(1), [mpicc](/man/mpicc)(1)"]],"e":["mpiexec","mpicc"],"x":"mpirun -np [4] [program]"},"msggrep":{"n":"msggrep","t":"searches PO files for messages","s":[["TAGLINE","searches PO files for messages"],["TLDR","**Search for messages**\n\n```msggrep -K -e \"[pattern]\" [input.po] -o [output.po]```\n\n**Search in translations**\n\n```msggrep -T -e \"[pattern]\" [input.po]```\n\n**Search in source references**\n\n```msggrep -N \"[file.c]\" [input.po]```\n\n**Invert match**\n\n```msggrep -v -K -e \"[pattern]\" [input.po]```\n\n**Case insensitive search**\n\n```msggrep -K -e \"[pattern]\" -i [input.po]```"],["SYNOPSIS","**msggrep** [_options_] [_inputfile_]"],["PARAMETERS","_INPUTFILE_\n> PO file to search.\n\n**-K**\n> Search in msgid (key).\n\n**-T**\n> Search in msgstr (translation).\n\n**-e** _PATTERN_\n> Regular expression pattern.\n\n**-N** _FILE_\n> Search by source reference.\n\n**-v**\n> Invert match.\n\n**--help**\n> Display help information."],["DESCRIPTION","**msggrep** searches PO files for messages. It's part of GNU gettext.\n\nThe tool filters messages by pattern. Extracts matching entries to new PO file."],["CAVEATS","Part of gettext-tools. Regex syntax. Use -K for keys, -T for translations."],["HISTORY","msggrep is part of **GNU gettext**, providing search functionality for translation files."],["INSTALL","```apt: sudo apt install gettext```\n\n```dnf: sudo dnf install gettext```\n\n```pacman: sudo pacman -S gettext```\n\n```apk: sudo apk add gettext```\n\n```brew: brew install gettext```\n\n```nix: nix profile install nixpkgs#gettext```"],["SEE ALSO","[msgfilter](/man/msgfilter)(1), [msgconv](/man/msgconv)(1), [grep](/man/grep)(1)"]],"e":["msgfilter","msgconv","grep"],"x":"msggrep -K -e \"[pattern]\" [input.po] -o [output.po]"},"ng-completion":{"n":"ng-completion","t":"sets up shell autocompletion for Angular CLI commands","s":[["TAGLINE","sets up shell autocompletion for Angular CLI commands"],["TLDR","**Enable shell completion**\n\n```ng completion```\n\n**Generate completion script**\n\n```ng completion script```"],["SYNOPSIS","**ng completion** [_command_]"],["PARAMETERS","**script**\n> Output completion script."],["DESCRIPTION","**ng completion** sets up shell autocompletion for Angular CLI commands. Enables tab completion for ng commands, options, and arguments in supported shells."],["SEE ALSO","[ng](/man/ng)(1)"]],"e":["ng"],"x":"ng completion"},"nim":{"n":"nim","t":"programming language compiler","s":[["TAGLINE","programming language compiler"],["TLDR","**Compile to executable**\n\n```nim c [program.nim]```\n\n**Compile and run**\n\n```nim c -r [program.nim]```\n\n**Release build**\n\n```nim c -d:release [program.nim]```\n\n**Generate C source without linking**\n\n```nim c --compileOnly [program.nim]```\n\n**Compile to JavaScript**\n\n```nim js [program.nim]```\n\n**Compile to C++**\n\n```nim cpp [program.nim]```\n\n**Check syntax and semantics**\n\n```nim check [program.nim]```\n\n**Generate HTML documentation**\n\n```nim doc [program.nim]```"],["SYNOPSIS","**nim** [_command_] [_options_] _file_"],["PARAMETERS","_COMMAND_\n> Compilation command.\n\n**c** / **compile**\n> Compile to C (default backend).\n\n**cpp**\n> Compile to C++.\n\n**js**\n> Compile to JavaScript.\n\n**objc**\n> Compile to Objective-C.\n\n**check**\n> Validate syntax and semantics without code generation.\n\n**doc**\n> Generate HTML documentation from source.\n\n**-r**, **--run**\n> Run the program after compilation.\n\n**-c**, **--compileOnly**\n> Generate code without linking.\n\n**-d:release**\n> Enable release optimizations.\n\n**-d:danger**\n> Disable all runtime checks for maximum speed.\n\n**-o:**_FILE_\n> Set the output filename.\n\n**--opt:**_speed|size|none_\n> Optimization target.\n\n**--mm:**_arc|orc|refc_\n> Select memory management strategy.\n\n**--threads:on**\n> Enable multithreading support.\n\n**-f**, **--forceBuild**\n> Force full rebuild of all modules.\n\n**--help**\n> Display help information."],["DESCRIPTION","**nim** is the Nim programming language compiler. It compiles to C, JavaScript, and other backends.\n\nThe tool produces efficient executables. Features Python-like syntax with static typing."],["CAVEATS","Requires C compiler for native builds. Garbage collected by default. Growing ecosystem."],["HISTORY","Nim was created by **Andreas Rumpf** starting in 2005, originally named Nimrod."],["INSTALL","```pacman: sudo pacman -S nim```\n\n```apk: sudo apk add nim```\n\n```zypper: sudo zypper install nim```\n\n```brew: brew install nim```\n\n```nix: nix profile install nixpkgs#nim```"],["SEE ALSO","[nimble](/man/nimble)(1), [gcc](/man/gcc)(1), [python](/man/python)(1)"]],"e":["nimble","gcc","python"],"x":"nim c [program.nim]"},"nm":{"n":"nm","t":"lists symbols from object files","s":[["TAGLINE","lists symbols from object files"],["TLDR","**List symbols**\n\n```nm [binary]```\n\n**Show only external symbols**\n\n```nm -g [binary]```\n\n**Show undefined symbols**\n\n```nm -u [binary]```\n\n**Show defined symbols**\n\n```nm -U [binary]```\n\n**Demangle C++ symbols**\n\n```nm -C [binary]```\n\n**Sort by address**\n\n```nm -n [binary]```\n\n**Show symbol sizes**\n\n```nm -S [binary]```"],["SYNOPSIS","**nm** [_options_] _files_"],["PARAMETERS","_FILES_\n> Object files or libraries.\n\n**-g**\n> External symbols only.\n\n**-u**\n> Undefined symbols only.\n\n**-U**\n> Defined symbols only.\n\n**-C**\n> Demangle C++ names.\n\n**-n**\n> Sort by address.\n\n**-S**\n> Print symbol sizes.\n\n**--help**\n> Display help information."],["DESCRIPTION","**nm** lists symbols from object files. Shows function and variable names.\n\nThe tool inspects binaries. Essential for debugging and reverse engineering."],["CAVEATS","Requires debug symbols for best results. Part of binutils. Platform variations."],["HISTORY","nm is a classic **Unix** utility from early Unix systems for examining object file symbols."],["INSTALL","```apt: sudo apt install binutils```\n\n```dnf: sudo dnf install binutils```\n\n```pacman: sudo pacman -S binutils```\n\n```apk: sudo apk add binutils```\n\n```zypper: sudo zypper install binutils```\n\n```brew: brew install binutils```\n\n```nix: nix profile install nixpkgs#binutils```"],["SEE ALSO","[objdump](/man/objdump)(1), [readelf](/man/readelf)(1), [strings](/man/strings)(1)"]],"e":["objdump","readelf","strings"],"x":"nm [binary]"},"npm-ci":{"n":"npm-ci","t":"installs from package-lock","s":[["TAGLINE","installs from package-lock"],["TLDR","**Clean install from lockfile**\n\n```npm ci```\n\n**Install production only**\n\n```npm ci --production```\n\n**Install with legacy peer deps**\n\n```npm ci --legacy-peer-deps```\n\n**Silent install**\n\n```npm ci --silent```"],["SYNOPSIS","**npm** **ci** [_options_]"],["PARAMETERS","**--production**\n> Skip devDependencies (npm 6; use --omit=dev in npm 7+).\n\n**--omit** _TYPE_\n> Omit dependency type (dev, optional, peer).\n\n**--legacy-peer-deps**\n> Ignore peer dep conflicts.\n\n**--silent**\n> Suppress output.\n\n**--help**\n> Display help information."],["DESCRIPTION","**npm ci** performs a clean install of dependencies based exactly on the package-lock.json file. It is designed for automated environments like CI/CD pipelines where reproducibility is critical.\n\nThe command deletes any existing node_modules directory before installing, ensuring a clean state. Unlike **npm install**, it never modifies the lockfile — if package.json and package-lock.json are out of sync, it fails with an error rather than silently updating."],["CAVEATS","Requires package-lock.json. Deletes node_modules. Fails on lockfile mismatch."],["HISTORY","npm ci was added to **npm 5.7** for deterministic builds in continuous integration pipelines."],["INSTALL","```pacman: sudo pacman -S npm```\n\n```apk: sudo apk add npm```\n\n```brew: brew install npm```"],["SEE ALSO","[npm](/man/npm)(1), [npm-install](/man/npm-install)(1), [package-lock.json](/man/package-lock.json)(5)"]],"e":["npm","npm-install","package-lock.json"],"x":"npm ci"},"npm-deprecate":{"n":"npm-deprecate","t":"marks a package version as deprecated on the npm registry","s":[["TAGLINE","marks a package version as deprecated on the npm registry"],["TLDR","**Deprecate package version**\n\n```npm deprecate [package]@[version] \"[message]\"```\n\n**Deprecate version range**\n\n```npm deprecate [package]@\"<[1.0.0]\" \"[message]\"```\n\n**Deprecate all versions**\n\n```npm deprecate [package] \"[message]\"```\n\n**Remove deprecation**\n\n```npm deprecate [package]@[version] \"\"```"],["SYNOPSIS","**npm deprecate** _pkg_@_version_ _message_"],["PARAMETERS","**pkg@version**\n> Package and version(s) to deprecate.\n\n**message**\n> Deprecation message (empty to remove)."],["DESCRIPTION","**npm deprecate** marks a package version as deprecated on the npm registry. Users installing deprecated versions will see a warning message.\n\nThis is commonly used to discourage use of old or vulnerable versions while still allowing installation."],["Deprecate specific version","npm deprecate my-package@1.0.0 \"Critical bug, please upgrade\""],["Deprecate old versions","npm deprecate my-package@\"<2.0.0\" \"Version 1.x is no longer supported\""],["Remove deprecation","npm deprecate my-package@1.0.0 \"\"\n```"],["USE CASES","```\n- Security vulnerabilities\n- Breaking changes\n- Package renamed\n- End of support\n```"],["CAVEATS","Requires publish access to package. Doesn't prevent installation. Empty message removes deprecation."],["HISTORY","npm deprecate was added to provide a softer alternative to unpublishing, allowing maintainers to warn users without breaking existing installations."],["SEE ALSO","[npm](/man/npm)(1), [npm-publish](/man/npm-publish)(1), [npm-unpublish](/man/npm-unpublish)(1)"],["INSTALL","```pacman: sudo pacman -S npm```\n\n```apk: sudo apk add npm```\n\n```brew: brew install npm```"]],"e":["npm","npm-publish","npm-unpublish"],"x":"npm deprecate [package]@[version] \"[message]\""},"npm-help-search":{"n":"npm-help-search","t":"searches npm help documentation","s":[["TAGLINE","searches npm help documentation"],["TLDR","**Search help topics**\n\n```npm help-search [term]```\n\n**Search with long output**\n\n```npm help-search -l [term]```"],["SYNOPSIS","**npm** **help-search** _text_..."],["PARAMETERS","**-l**, **--long**\n> Show full descriptions."],["DESCRIPTION","**npm help-search** searches npm help documentation. Finds help topics matching search terms. Useful for discovering relevant npm commands and concepts."],["INSTALL","```pacman: sudo pacman -S npm```\n\n```apk: sudo apk add npm```\n\n```brew: brew install npm```"],["SEE ALSO","[npm](/man/npm)(1), [npm-help](/man/npm-help)(1)"]],"e":["npm","npm-help"],"x":"npm help-search [term]"},"npm-hook":{"n":"npm-hook","t":"manages npm registry webhooks","s":[["TAGLINE","manages npm registry webhooks"],["TLDR","**List hooks**\n\n```npm hook ls```\n\n**Add hook**\n\n```npm hook add [package] [url] [secret]```\n\n**Remove hook**\n\n```npm hook rm [id]```\n\n**Update hook**\n\n```npm hook update [id] [url] [secret]```"],["SYNOPSIS","**npm** **hook** [_command_] [_options_]"],["PARAMETERS","_COMMAND_\n> Hook subcommand.\n\n**ls**\n> List hooks.\n\n**add** _PKG_ _URL_\n> Add webhook.\n\n**rm** _ID_\n> Remove hook.\n\n**update** _ID_\n> Update hook.\n\n**--help**\n> Display help information."],["DESCRIPTION","**npm hook** manages npm registry webhooks. Triggers on package events.\n\nThe command configures notifications. Useful for CI/CD integration."],["CAVEATS","Requires authentication. Registry webhooks. Enterprise feature."],["HISTORY","npm hook enables **webhook management** for automated responses to npm registry events."],["INSTALL","```pacman: sudo pacman -S npm```\n\n```apk: sudo apk add npm```\n\n```brew: brew install npm```"],["SEE ALSO","[npm](/man/npm)(1), [npm-publish](/man/npm-publish)(1)"]],"e":["npm","npm-publish"],"x":"npm hook ls"},"npm-query":{"n":"npm-query","t":"queries dependency graph with selectors","s":[["TAGLINE","queries dependency graph with selectors"],["TLDR","**Query dependencies**\n\n```npm query \"[name=lodash]\"```\n\n**Find dev dependencies**\n\n```npm query \":type(dev)\"```\n\n**Find outdated**\n\n```npm query \":outdated(major)\"```\n\n**Find vulnerabilities**\n\n```npm query \":vuln\"```\n\n**Complex query**\n\n```npm query \"[name^=@babel]\"```"],["SYNOPSIS","**npm** **query** _selector_"],["PARAMETERS","_SELECTOR_\n> CSS-like query selector. Supports pseudo-classes (`:root`, `:type(dev)`, `:outdated`, `:deprecated`, `:vuln`, `:attr`, `:semver`, `:path`), attribute selectors (`[name=foo]`, `[name^=@scope]`, `[keywords=cli]`) and combinators (` `, `>`, `,`).\n\n**--global**\n> Query the globally-installed tree instead of the project.\n\n**--workspace** _NAME_\n> Restrict the query to the given workspace(s).\n\n**--workspaces**\n> Apply across every workspace.\n\n**--include-workspace-root**\n> Include the workspace root package in the results.\n\n**--package-lock-only**\n> Read from `package-lock.json` only, do not touch `node_modules`.\n\n**--expect-result-count** _N_, **--expect-results** _BOOL_\n> Make npm exit non-zero if the result count does not match.\n\n**--help**\n> Display help information."],["DESCRIPTION","**npm query** queries dependency graph with selectors. CSS-like syntax for packages.\n\nThe command filters dependencies. Powerful package searching."],["CAVEATS","CSS selector syntax. Added in npm 8.16. Complex queries possible."],["HISTORY","npm query was added to provide **CSS-like querying** of the dependency tree."],["INSTALL","```pacman: sudo pacman -S npm```\n\n```apk: sudo apk add npm```\n\n```brew: brew install npm```"],["SEE ALSO","[npm](/man/npm)(1), [npm-ls](/man/npm-ls)(1), [npm-explain](/man/npm-explain)(1)"]],"e":["npm","npm-ls","npm-explain"],"x":"npm query \"[name=lodash]\""},"nseq.1s":{"n":"nseq.1s","t":"generates sequences of numbers","s":[["TAGLINE","generates sequences of numbers"],["TLDR","**Generate sequence numbers**\n\n```nseq [10]```\n\n**Sequence with start**\n\n```nseq [5] [10]```\n\n**Sequence with step**\n\n```nseq [0] [2] [10]```"],["SYNOPSIS","**nseq** [_first_] [_increment_] _last_"],["PARAMETERS","_FIRST_\n> Starting number.\n\n_INCREMENT_\n> Step value.\n\n_LAST_\n> Ending number.\n\n**--help**\n> Display help information."],["DESCRIPTION","**nseq** generates sequences of numbers. OpenSSL utility variant.\n\nThe tool outputs numeric sequences. Part of OpenSSL tools."],["CAVEATS","OpenSSL utility. Similar to seq. Less common than standard seq."],["HISTORY","nseq is part of **OpenSSL** utilities, providing sequence generation functionality."],["SEE ALSO","[seq](/man/seq)(1), [openssl](/man/openssl)(1)"]],"e":["seq","openssl"],"x":"nseq [10]"},"openssl-genrsa":{"n":"openssl-genrsa","t":"generates RSA private keys","s":[["TAGLINE","generates RSA private keys"],["TLDR","**Generate 2048-bit RSA key**\n\n```openssl genrsa -out [private.key] 2048```\n\n**Generate 4096-bit key**\n\n```openssl genrsa -out [private.key] 4096```\n\n**Generate encrypted key with AES-256**\n\n```openssl genrsa -aes256 -out [private.key] 4096```\n\n**Generate key with a specific public exponent**\n\n```openssl genrsa -F4 -out [private.key] 2048```\n\n**Generate key to stdout**\n\n```openssl genrsa 2048```"],["SYNOPSIS","**openssl** **genrsa** [_options_] [_bits_]"],["PARAMETERS","**-out** _file_\n> Output file for private key. Writes to stdout if omitted.\n\n**-aes256**, **-aes192**, **-aes128**, **-des3**, **-des**\n> Encrypt the output key with the specified cipher. Prompts for a passphrase.\n\n**-passout** _arg_\n> Password source for encryption (e.g. **pass:password**, **file:pathname**, **env:var**, **fd:number**, **stdin**).\n\n**-F4**\n> Use 65537 (0x10001) as the public exponent (default).\n\n**-3**\n> Use 3 as the public exponent.\n\n**-traditional**\n> Write the key in the traditional PKCS#1 format instead of PKCS#8.\n\n**-verbose**\n> Print extra details during key generation.\n\n_bits_\n> Key size in bits (default 2048). Minimum 512, but 2048 or higher is recommended."],["DESCRIPTION","**openssl genrsa** generates RSA private keys. The key can optionally be encrypted with a passphrase. Common key sizes are 2048 and 4096 bits. Output is in PEM format by default (PKCS#8 in OpenSSL 3.x, PKCS#1 in older versions).\n\nThis command is considered a legacy convenience wrapper. The more general **openssl genpkey -algorithm RSA** is preferred in OpenSSL 3.x and supports additional options."],["CAVEATS","Key sizes below 2048 bits are considered insecure. Some applications require the traditional PKCS#1 format; use **-traditional** if needed. In OpenSSL 3.x the default output format changed from PKCS#1 to PKCS#8, which may affect older tools."],["INSTALL","```apt: sudo apt install openssl```\n\n```dnf: sudo dnf install openssl```\n\n```pacman: sudo pacman -S openssl```\n\n```apk: sudo apk add openssl```\n\n```zypper: sudo zypper install openssl```\n\n```brew: brew install openssl```\n\n```nix: nix profile install nixpkgs#openssl```"],["SEE ALSO","[openssl](/man/openssl)(1), [openssl-rsa](/man/openssl-rsa)(1), [openssl-genpkey](/man/openssl-genpkey)(1)"]],"e":["openssl","openssl-rsa","openssl-genpkey"],"x":"openssl genrsa -out [private.key] 2048"},"pamfix":{"n":"pamfix","t":"repairs corrupted Netpbm images","s":[["TAGLINE","repairs corrupted Netpbm images"],["TLDR","**Clip sample values that exceed the maxval**\n\n```pamfix -clip [corrupted.pam] > [fixed.pam]```\n\n**Raise the maxval to accommodate out-of-range values**\n\n```pamfix -changemaxval [corrupted.pam] > [fixed.pam]```\n\n**Truncate the image at the first invalid sample value**\n\n```pamfix -truncate [corrupted.pam] > [fixed.pam]```\n\n**Fix with verbose output**\n\n```pamfix -clip -verbose [corrupted.pam] > [fixed.pam]```"],["SYNOPSIS","**pamfix** [_options_] [_file_]"],["PARAMETERS","_FILE_\n> Input PAM/PNM file. Reads from standard input if not specified.\n\n**-clip**\n> Clip all pixel values that exceed the maxval stated in the header to the maxval. Cannot be combined with **-changemaxval**.\n\n**-changemaxval**\n> Raise the maxval to accommodate pixel values that exceed the maxval stated in the header, lowering the fraction represented by every sample. Cannot be combined with **-clip**.\n\n**-truncate**\n> Truncate the image at the first invalid sample value. When neither **-clip** nor **-changemaxval** is specified, this is the default behavior for out-of-range values.\n\n**-verbose**\n> Report details of the repair to standard error."],["DESCRIPTION","**pamfix** reads a stream that is mostly a Netpbm image but may have certain types of corruptions and produces a valid Netpbm image that preserves much of the information in the original. It handles two primary corruption types: truncated streams and illegally large sample values. Part of the Netpbm package."],["CAVEATS","You cannot specify both **-clip** and **-changemaxval**. May not fix all types of corruption. Some data loss is possible with truncation."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pamfixtrunc](/man/pamfixtrunc)(1), [pamfile](/man/pamfile)(1), [pamvalidate](/man/pamvalidate)(1), [pamcut](/man/pamcut)(1), [netpbm](/man/netpbm)(1)"]],"e":["pamfixtrunc","pamfile","pamvalidate","pamcut","netpbm"],"x":"pamfix -clip [corrupted.pam] > [fixed.pam]"},"pbmreduce":{"n":"pbmreduce","t":"reduces PBM bitmap size by an integer factor","s":[["TAGLINE","reduces PBM bitmap size by an integer factor"],["TLDR","**Reduce bitmap by factor**\n\n```pbmreduce [4] [input.pbm] > [output.pbm]```\n\n**Reduce with Floyd-Steinberg dithering**\n\n```pbmreduce -floyd [4] [input.pbm] > [output.pbm]```"],["SYNOPSIS","**pbmreduce** [_options_] _n_ [_file_]"],["PARAMETERS","_n_\n> Reduction factor.\n\n**-floyd**\n> Use Floyd-Steinberg dithering.\n\n**-threshold**\n> Use simple threshold (default)."],["DESCRIPTION","**pbmreduce** reduces PBM bitmap size by an integer factor. Can use threshold or dithering for the reduction. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pbmpscale](/man/pbmpscale)(1), [pamscale](/man/pamscale)(1)"]],"e":["pbmpscale","pamscale"],"x":"pbmreduce [4] [input.pbm] > [output.pbm]"},"pct-restore":{"n":"pct-restore","t":"restores an LXC container from a Proxmox backup file","s":[["TAGLINE","restores an LXC container from a Proxmox backup file"],["TLDR","**Restore container from backup**\n\n```pct restore [VMID] [/path/to/backup.tar.zst]```\n\n**Restore with specific storage**\n\n```pct restore [VMID] [backup.tar.zst] --storage [local-lvm]```\n\n**Restore without starting**\n\n```pct restore [VMID] [backup.tar.zst] --start 0```\n\n**Restore with new hostname**\n\n```pct restore [VMID] [backup.tar.zst] --hostname [newname]```\n\n**Restore unprivileged**\n\n```pct restore [VMID] [backup.tar.zst] --unprivileged 1```"],["SYNOPSIS","**pct restore** _vmid_ _backup_ [_options_]"],["PARAMETERS","**vmid**\n> New container ID.\n\n**backup**\n> Backup file path.\n\n**--storage** _name_\n> Target storage.\n\n**--hostname** _name_\n> Set hostname.\n\n**--start** _bool_\n> Start after restore.\n\n**--unprivileged** _bool_\n> Unprivileged container.\n\n**--rootfs** _spec_\n> Root filesystem spec (e.g., local-lvm:20 for 20GB).\n\n**--memory** _MB_\n> Set container memory limit.\n\n**--pool** _name_\n> Add the container to the specified resource pool.\n\n**--force** _bool_\n> Allow overwriting existing container.\n\n**--ignore-unpack-errors** _bool_\n> Ignore errors during backup unpacking."],["DESCRIPTION","**pct restore** restores an LXC container from a Proxmox backup file. It can restore to a new VMID, different storage, or with modified settings."],["EXAMPLE","```bash"],["Restore to container 200","pct restore 200 /var/lib/vz/dump/vzdump-lxc-100.tar.zst"],["Restore with 20GB disk","pct restore 200 backup.tar.zst --rootfs local-lvm:20\n```"],["CAVEATS","VMID must not exist. Storage must have space. Network config may need adjustment."],["HISTORY","pct restore is part of **Proxmox VE**'s backup and restore infrastructure, working with vzdump backups."],["SEE ALSO","[pct](/man/pct)(1), [pct-enter](/man/pct-enter)(1), [vzdump](/man/vzdump)(1), [qmrestore](/man/qmrestore)(1)"]],"e":["pct","pct-enter","vzdump","qmrestore"],"x":"pct restore [VMID] [/path/to/backup.tar.zst]"},"pg_ctl":{"n":"pg_ctl","t":"utility for initializing, starting, stopping, and controlling a PostgreSQL","s":[["TAGLINE","utility for initializing, starting, stopping, and controlling a PostgreSQL"],["TLDR","**Start PostgreSQL**\n\n```pg_ctl start -D [/var/lib/postgresql/data]```\n\n**Stop PostgreSQL**\n\n```pg_ctl stop -D [/var/lib/postgresql/data]```\n\n**Restart PostgreSQL**\n\n```pg_ctl restart -D [/var/lib/postgresql/data]```\n\n**Reload configuration**\n\n```pg_ctl reload -D [/var/lib/postgresql/data]```\n\n**Check status**\n\n```pg_ctl status -D [/var/lib/postgresql/data]```"],["SYNOPSIS","**pg_ctl** _action_ [**-D** _datadir_] [_options_]"],["PARAMETERS","**start**\n> Start the server.\n\n**stop**\n> Stop the server.\n\n**restart**\n> Stop then start.\n\n**reload**\n> Reload configuration.\n\n**status**\n> Check server status.\n\n**-D** _directory_\n> Data directory.\n\n**-m** _mode_\n> Shutdown mode (smart, fast, immediate).\n\n**-l** _file_\n> Log file.\n\n**-w**\n> Wait for completion."],["DESCRIPTION","**pg_ctl** is a utility for initializing, starting, stopping, and controlling a PostgreSQL database server. It's the preferred method for managing PostgreSQL services."],["Start with logging","pg_ctl start -D /data -l /var/log/postgresql.log"],["Fast shutdown","pg_ctl stop -D /data -m fast"],["Initialize new cluster","pg_ctl initdb -D /data"],["Promote standby","pg_ctl promote -D /data\n```"],["SHUTDOWN MODES","```\nsmart     - Wait for clients to disconnect\nfast      - Rollback active transactions (default)\nimmediate - Abort immediately (may corrupt)\n```"],["CAVEATS","Must run as PostgreSQL user. Data directory required. Use systemctl on systemd systems."],["HISTORY","pg_ctl is part of **PostgreSQL**, the open source database originally from UC Berkeley's POSTGRES project."],["SEE ALSO","[postgres](/man/postgres)(1), [pg_dump](/man/pg_dump)(1), [pg_restore](/man/pg_restore)(1), [initdb](/man/initdb)(1)"],["INSTALL","```dnf: sudo dnf install postgresql17```\n\n```apk: sudo apk add postgresql17```\n\n```zypper: sudo zypper install postgresql17```"]],"e":["postgres","pg_dump","pg_restore","initdb"],"x":"pg_ctl start -D [/var/lib/postgresql/data]"},"pio-upgrade":{"n":"pio-upgrade","t":"Upgrade PlatformIO Core to latest version","s":[["TAGLINE","Upgrade PlatformIO Core to latest version"],["TLDR","**Upgrade** PlatformIO Core to the latest **stable** version\n\n```pio upgrade```\n\n**Upgrade** to the latest **development** version\n\n```pio upgrade --dev```"],["SYNOPSIS","**pio upgrade** [_options_]"],["PARAMETERS","**--dev**\n> Upgrade to the latest development version instead of stable. Provides access to new features before official release but may be less stable."],["DESCRIPTION","**pio upgrade** checks for and upgrades the PlatformIO Core CLI tool itself to the latest release. By default it installs the latest stable version. Use **--dev** to install the development version for testing new features before they are officially released.\n\nUnlike **pio update**, which updates project platform packages and libraries, **pio upgrade** targets the underlying PlatformIO Core installation."],["CAVEATS","Requires an internet connection to download the update. May require elevated permissions depending on the installation method (e.g., system-wide pip install). After upgrading, restart any running PlatformIO processes or IDE integrations."],["INSTALL","```aur: yay -S pio```"],["SEE ALSO","[pio-update](/man/pio-update)(1), [pio](/man/pio)(1), [pip](/man/pip)(1)"]],"e":["pio-update","pio","pip"],"x":"pio upgrade"},"pkgstats":{"n":"pkgstats","t":"Submit and query Arch package usage statistics","s":[["TAGLINE","Submit and query Arch package usage statistics"],["TLDR","**Submit** installed package data anonymously\n\n```pkgstats submit```\n\n**Dump JSON** of what would be submitted without sending\n\n```pkgstats submit --dump-json```\n\n**Search** for packages by name\n\n```pkgstats search [term]```\n\n**Compare** package popularity across multiple packages\n\n```pkgstats show [firefox] [chromium]```\n\nDisplay **help**\n\n```pkgstats --help```"],["SYNOPSIS","**pkgstats** _subcommand_ [_options_] [_packages_...]"],["COMMANDS","**submit**\n> Collect and send installed package list, architecture, and mirror info anonymously.\n\n**search** _term_\n> Search for packages and display their popularity metrics.\n\n**show** _packages_...\n> Compare popularity statistics for the given packages."],["PARAMETERS","**--dump-json**\n> With `submit`, print the JSON payload that would be sent instead of submitting.\n\n**-h**, **--help**\n> Display help information.\n\n**-V**, **--version**\n> Display version information."],["CONFIGURATION","**/etc/pkgstats.yaml**\n> Optional configuration file used to filter packages or mirrors from submissions using glob patterns."],["DESCRIPTION","**pkgstats** collects and submits anonymous statistics about installed packages to help the Arch Linux community understand package usage patterns. The data helps maintainers prioritize development efforts.\n\nUsers can also query the collected statistics to see package popularity and compare different packages. Submission is voluntary and privacy-preserving."],["CAVEATS","Requires internet access for submission and queries. Data is anonymous but includes package list and architecture. Opt-in participation only."],["HISTORY","**pkgstats** is maintained by **Arch Linux DE** community to gather package usage statistics. It helps the Arch community make informed decisions about package maintenance and repository organization."],["INSTALL","```pacman: sudo pacman -S pkgstats```"],["SEE ALSO","[pacman](/man/pacman)(8), [pkgfile](/man/pkgfile)(1)"]],"e":["pacman","pkgfile"],"x":"pkgstats submit"},"pnminvert":{"n":"pnminvert","t":"Invert colors of PNM images","s":[["TAGLINE","Invert colors of PNM images"],["TLDR","**Invert image colors**\n\n```pnminvert [input.pnm] > [output.pnm]```\n\n**Invert from stdin**\n\n```cat [image.pnm] | pnminvert > [output.pnm]```"],["SYNOPSIS","**pnminvert** [_file_]"],["DESCRIPTION","**pnminvert** produces a photographic negative of a PNM image by inverting all pixel values. Each sample value is replaced with the maximum value minus the original value, effectively swapping light and dark.\n\nThe tool works with all PNM types: PBM (black/white bits are flipped), PGM (grayscale values are inverted), and PPM (each color channel is independently inverted). If no file is given, input is read from stdin. Output is always written to stdout.\n\nPart of the Netpbm toolkit. The more general **paminvert** supersedes this tool and handles PAM images as well."],["CAVEATS","`pnminvert` has no options of its own beyond the common libnetpbm options. Output must be redirected to a file; it is always written to stdout."],["HISTORY","**pnminvert** was created as part of the **Netpbm** package for portable bitmap image manipulation."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[paminvert](/man/paminvert)(1), [pnmarith](/man/pnmarith)(1), [pnmgamma](/man/pnmgamma)(1)"]],"e":["paminvert","pnmarith","pnmgamma"],"x":"pnminvert [input.pnm] > [output.pnm]"},"poetry-build":{"n":"poetry-build","t":"Build Python distribution packages","s":[["TAGLINE","Build Python distribution packages"],["TLDR","**Build package**\n\n```poetry build```\n\n**Build wheel only**\n\n```poetry build -f wheel```\n\n**Build sdist only**\n\n```poetry build -f sdist```"],["SYNOPSIS","**poetry build** [_options_]"],["PARAMETERS","**-f**, **--format** _FORMAT_\n> Build format (wheel, sdist)."],["DESCRIPTION","**poetry build** creates distributable Python packages from the current project. By default it generates both a wheel (.whl) and a source distribution (sdist/tarball) in the **dist/** directory.\n\nUse **-f wheel** to build only the wheel format or **-f sdist** for only the source distribution. The build process uses the project metadata from **pyproject.toml** including name, version, dependencies, and entry points."],["CAVEATS","Outputs to dist/ directory. Requires valid pyproject.toml."],["HISTORY","poetry build provides **Python package** building functionality."],["INSTALL","```dnf: sudo dnf install poetry```\n\n```apk: sudo apk add poetry```\n\n```brew: brew install poetry```\n\n```nix: nix profile install nixpkgs#poetry```"],["SEE ALSO","[poetry](/man/poetry)(1), [poetry-publish](/man/poetry-publish)(1)"]],"e":["poetry","poetry-publish"],"x":"poetry build"},"ppmcie":{"n":"ppmcie","t":"Generate CIE chromaticity diagram images","s":[["TAGLINE","Generate CIE chromaticity diagram images"],["TLDR","**Generate CIE chromaticity diagram**\n\n```ppmcie > [output.ppm]```\n\n**Specify size**\n\n```ppmcie -size [512] > [output.ppm]```\n\n**Generate with specific illuminant**\n\n```ppmcie -illuminant [D65] > [output.ppm]```"],["SYNOPSIS","**ppmcie** [_options_]"],["PARAMETERS","**-size** _N_\n> Output size in pixels.\n\n**-illuminant** _NAME_\n> White point illuminant.\n\n**-spectral** _N_\n> Spectral type."],["DESCRIPTION","**ppmcie** generates a CIE chromaticity diagram as a PPM image, showing the full gamut of visible colors plotted on the standard CIE 1931 xy color space. The diagram is a standard reference in color science for visualizing color gamuts and comparing display or printing capabilities.\n\nOptions control the output size, illuminant white point, and whether spectral wavelength labels are included. The generated image can be used for color science education, display calibration documentation, or as a reference overlay for gamut comparisons. Part of the Netpbm toolkit."],["CAVEATS","Color science tool. Part of Netpbm suite."],["HISTORY","ppmcie was created as part of **Netpbm** for color science visualization."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[ppm](/man/ppm)(5), [ppmgamma](/man/ppmgamma)(1)"]],"e":["ppm","ppmgamma"],"x":"ppmcie > [output.ppm]"},"python3.4":{"n":"python3.4","t":"Python 3.4 interpreter with asyncio and pathlib","s":[["TAGLINE","Python 3.4 interpreter with asyncio and pathlib"],["TLDR","**Run Python 3.4 script**\n\n```python3.4 [script.py]```\n\n**Interactive mode**\n\n```python3.4```\n\n**Execute command**\n\n```python3.4 -c \"[print('hello')]\"```\n\n**Run module**\n\n```python3.4 -m [module]```"],["SYNOPSIS","**python3.4** [_options_] [_script_] [_args_]"],["PARAMETERS","**-c** _command_\n> Execute command.\n\n**-m** _module_\n> Run library module.\n\n**-i**\n> Interactive after script.\n\n**-u**\n> Unbuffered I/O.\n\n**-O**\n> Optimize bytecode.\n\n**-B**\n> Don't write bytecode.\n\n**-v**\n> Verbose imports.\n\n**-V**\n> Print version."],["DESCRIPTION","**python3.4** is the Python 3.4 interpreter. Released in 2014, Python 3.4 introduced asyncio, enum, pathlib, and other features. Now superseded by newer Python 3 versions."],["EXAMPLES","```bash"],["Run script","python3.4 myscript.py"],["Check version","python3.4 --version"],["Create virtual environment","python3.4 -m venv myenv"],["Run simple server","python3.4 -m http.server 8000\n```"],["FEATURES IN 3.4","```python\nimport asyncio         # Async I/O\nimport enum            # Enumerations\nimport pathlib         # Object-oriented paths\nimport statistics      # Basic statistics\nimport selectors       # I/O multiplexing\n```"],["CAVEATS","Python 3.4 is EOL (March 2019). Use Python 3.10+ for current development. Older versions lack security updates."],["HISTORY","Python 3.4 was released March 16, 2014. Each Python version has ~5 year support window."],["SEE ALSO","[python3](/man/python3)(1), [pip](/man/pip)(1), [venv](/man/venv)(1)"]],"e":["python3","pip","venv"],"x":"python3.4 [script.py]"},"qm-move_disk":{"n":"qm-move_disk","t":"Move Proxmox VM disk between storages","s":[["TAGLINE","Move Proxmox VM disk between storages"],["TLDR","This command is an alias of **qm disk move**\n\nView documentation for the original command\n\n```tldr qm disk```"],["SYNOPSIS","**qm move_disk** _vmid_ _disk_ _storage_ [_options_]"],["DESCRIPTION","**qm move_disk** is an alternative form of **qm disk move** that moves virtual machine disks between storage backends. The underscore variant is provided for compatibility."],["INSTALL","```dnf: sudo dnf install qm```"],["SEE ALSO","[qm](/man/qm)(1), [qm-disk](/man/qm-disk)(1)"]],"e":["qm","qm-disk"],"x":"tldr qm disk"},"radeontop":{"n":"radeontop","t":"Monitor AMD Radeon GPU utilization in real time","s":[["TAGLINE","Monitor AMD Radeon GPU utilization in real time"],["TLDR","Show **GPU utilization**\n\n```radeontop```\n\nEnable **colored** output\n\n```radeontop -c```\n\nSelect specific **GPU** by bus number\n\n```radeontop -b [bus_number]```\n\nSet **sample rate** (samples per second)\n\n```radeontop -t [10]```\n\n**Dump** output to a file\n\n```radeontop -d [output.txt]```"],["SYNOPSIS","**radeontop** [_options_]"],["PARAMETERS","**-c**, **--color**\n> Enable colorized output.\n\n**-b**, **--bus** _bus_\n> Select GPU by PCI bus number. For lspci output `01:00.0`, the bus value is `1`.\n\n**-t**, **--ticks** _ticks_\n> Number of samples per second. Default is 120. Higher values increase accuracy and overhead.\n\n**-d**, **--dump** _file_\n> Dump data to a file instead of displaying it. Use `-` for stdout.\n\n**-i**, **--dump-interval** _seconds_\n> Wait N seconds between data output lines when dumping.\n\n**-l**, **--limit** _limit_\n> Quit after dumping this many lines. Only valid in dump mode.\n\n**-p**, **--path** _device_\n> Open a specific DRM device node (e.g. `/dev/dri/card0`).\n\n**-m**, **--mem**\n> Force use of `/dev/mem` path if autodetection fails. Useful with the proprietary driver.\n\n**-v**, **--version**\n> Display program version.\n\n**-h**, **--help**\n> Display help."],["DESCRIPTION","**radeontop** displays real-time utilization statistics for AMD Radeon GPUs. It shows usage percentages for various GPU components including graphics pipe, shaders, texture units, and video decode/encode engines.\n\nThe tool reads from kernel debugfs interfaces and presents the data in a top-like format, useful for monitoring GPU workload during gaming or compute tasks."],["CAVEATS","May require root privileges on some systems. Only works with AMD Radeon GPUs using radeon or amdgpu drivers. Older GPUs may not support all metrics."],["HISTORY","**radeontop** was created by **Chris Blaas** to provide GPU monitoring for AMD graphics cards on Linux, similar to nvidia-smi for NVIDIA cards."],["INSTALL","```apt: sudo apt install radeontop```\n\n```dnf: sudo dnf install radeontop```\n\n```pacman: sudo pacman -S radeontop```\n\n```apk: sudo apk add radeontop```\n\n```zypper: sudo zypper install radeontop```\n\n```nix: nix profile install nixpkgs#radeontop```"],["SEE ALSO","[amdgpu_top](/man/amdgpu_top)(1), [nvidia-smi](/man/nvidia-smi)(1), [glxinfo](/man/glxinfo)(1), [lspci](/man/lspci)(8)"]],"e":["amdgpu_top","nvidia-smi","glxinfo","lspci"],"x":"radeontop"},"shfmt":{"n":"shfmt","t":"Format and lint shell scripts","s":[["TAGLINE","Format and lint shell scripts"],["TLDR","**Format shell script**\n\n```shfmt [script.sh]```\n\n**Format and overwrite**\n\n```shfmt -w [script.sh]```\n\n**Check if formatted** (exit non-zero if not)\n\n```shfmt -d [script.sh]```\n\n**Use 4-space indent**\n\n```shfmt -i [4] [script.sh]```\n\n**Use tabs for indent**\n\n```shfmt -i [0] [script.sh]```\n\n**Format all scripts in directory**\n\n```shfmt -w [directory/]```\n\n**Format as bash** (not POSIX)\n\n```shfmt -ln bash [script.sh]```\n\n**Binary operator at line start**\n\n```shfmt -bn [script.sh]```"],["SYNOPSIS","**shfmt** [_-w_] [_-d_] [_-i indent_] [_-ln language_] [_options_] [_files_]"],["PARAMETERS","**-w**, **--write**\n> Write result to file instead of stdout.\n\n**-d**, **--diff**\n> Show diff and exit with error if not formatted.\n\n**-l**, **--list**\n> List files that differ from formatted.\n\n**-i** _N_, **--indent** _N_\n> Indent spaces (0 for tabs).\n\n**-ln** _LANG_, **--language-dialect** _LANG_\n> Shell variant: bash, posix, mksh, bats.\n\n**-bn**, **--binary-next-line**\n> Binary operators start new lines.\n\n**-ci**, **--case-indent**\n> Indent case statement bodies.\n\n**-sr**, **--redirect-operators**\n> Redirect operators follow next line.\n\n**-fn**, **--func-next-line**\n> Function brace on next line.\n\n**-kp**, **--keep-padding**\n> Keep column alignment padding.\n\n**-s**, **--simplify**\n> Simplify code.\n\n**-mn**, **--minify**\n> Minify output.\n\n**-f**, **--find**\n> Find shell files recursively.\n\n**--apply-ignore**\n> Apply .shfmtignore files."],["DESCRIPTION","**shfmt** formats shell scripts. It parses and rewrites scripts with consistent styling, similar to gofmt for Go or prettier for JavaScript.\n\nThe tool understands multiple shell dialects: POSIX sh, bash, mksh, and bats. Detection is automatic from shebang or can be specified explicitly. Different dialects support different syntax.\n\nIndent options control spacing: -i 2 for two spaces, -i 4 for four, -i 0 for tabs. Consistent indentation is the most impactful formatting change.\n\nWrite mode (-w) modifies files in place. Diff mode (-d) checks formatting without changing, useful in CI to enforce style. The exit code indicates whether formatting was needed.\n\nThe simplify option (-s) removes unnecessary syntax: unneeded quotes, redundant semicolons, POSIX-compatible alternatives. This produces cleaner, more portable code.\n\nMinify mode (-mn) removes all non-essential whitespace, useful for embedded scripts or size-constrained environments."],["CAVEATS","May change script behavior in edge cases. Some heredoc formatting can be tricky. Not all style preferences are configurable. Parser may reject valid but unusual syntax. Backup files before mass formatting."],["HISTORY","**shfmt** was created by **Daniel Martí** (mvdan) around **2016**. Written in Go, it was inspired by gofmt's approach to code formatting. The project fills a gap - while other languages have standard formatters, shell scripts traditionally lacked one. It's widely adopted in CI pipelines and editor integrations."],["INSTALL","```apt: sudo apt install shfmt```\n\n```pacman: sudo pacman -S shfmt```\n\n```apk: sudo apk add shfmt```\n\n```zypper: sudo zypper install shfmt```\n\n```brew: brew install shfmt```\n\n```nix: nix profile install nixpkgs#shfmt```"],["SEE ALSO","[shellcheck](/man/shellcheck)(1), [bash](/man/bash)(1), [sh](/man/sh)(1), [prettier](/man/prettier)(1)"]],"e":["shellcheck","bash","sh","prettier"],"x":"shfmt [script.sh]"},"startplasma-wayland":{"n":"startplasma-wayland","t":"Start KDE Plasma on Wayland","s":[["TAGLINE","Start KDE Plasma on Wayland"],["TLDR","**Start KDE Plasma** on Wayland from console\n\n```startplasma-wayland```\n\n**Start with DBus session** wrapper (if needed)\n\n```dbus-run-session startplasma-wayland```\n\n**Start using the recommended wrapper script**\n\n```/usr/lib/plasma-dbus-run-session-if-needed startplasma-wayland```\n\n**Start with explicit session type**\n\n```XDG_SESSION_TYPE=wayland startplasma-wayland```"],["SYNOPSIS","**startplasma-wayland** [_options_]"],["DESCRIPTION","**startplasma-wayland** starts the KDE Plasma desktop environment using the Wayland display protocol. It initializes the KWin Wayland compositor, Plasma shell, and associated desktop services.\n\nThis command is typically invoked automatically by display managers (SDDM, GDM) when selecting the \"Plasma (Wayland)\" session, but can also be started manually from a virtual console (TTY) for debugging or minimal setups.\n\nWayland provides improved security, better HiDPI support, and smoother graphics compared to X11. Legacy X11 applications run through XWayland compatibility layer automatically."],["CAVEATS","Starting from a console may require the DBus session wrapper depending on your system configuration. Some X11-only features like global keyboard shortcuts from certain applications may not work. Screen recording and remote desktop require portal support. GPU drivers must have proper Wayland support. Not all applications support Wayland natively; they run via XWayland."],["HISTORY","**startplasma-wayland** was introduced as KDE Plasma gained Wayland support. KDE began Wayland development in **2014** with the KWin compositor, reaching production-ready status around KDE Plasma 5.20 in **2020**. Plasma 6, released in **2024**, made Wayland the default session. The command replaced older X11-only startup methods as part of KDE's transition to Wayland."],["INSTALL","```apt: sudo apt install plasma-workspace```\n\n```dnf: sudo dnf install plasma-workspace```\n\n```pacman: sudo pacman -S plasma-workspace```\n\n```apk: sudo apk add plasma-workspace```"],["SEE ALSO","[startplasma-x11](/man/startplasma-x11)(1), [plasmashell](/man/plasmashell)(1)"]],"e":["startplasma-x11","plasmashell"],"x":"startplasma-wayland"},"stress-ng":{"n":"stress-ng","t":"Advanced system stress testing framework","s":[["TAGLINE","Advanced system stress testing framework"],["TLDR","**Stress CPU**\n\n```stress-ng --cpu [4] --timeout [60s]```\n\n**Stress memory**\n\n```stress-ng --vm [2] --vm-bytes [1G] --timeout [60s]```\n\n**Stress disk I/O**\n\n```stress-ng --io [4] --timeout [60s]```\n\n**Combined stress test**\n\n```stress-ng --cpu [2] --vm [2] --io [2] --timeout [60s]```\n\n**Matrix operations** (CPU intensive)\n\n```stress-ng --matrix [4] --timeout [60s]```\n\n**Show available stressors**\n\n```stress-ng --stressors```\n\n**Metrics output**\n\n```stress-ng --cpu [4] --metrics --timeout [60s]```\n\n**Limit CPU usage**\n\n```stress-ng --cpu [4] --cpu-load [50] --timeout [60s]```"],["SYNOPSIS","**stress-ng** [_--cpu n_] [_--vm n_] [_--io n_] [_--timeout t_] [_options_]"],["PARAMETERS","**--cpu** _N_\n> CPU stress workers.\n\n**--cpu-load** _PERCENT_\n> Target CPU load.\n\n**--vm** _N_\n> Memory stress workers.\n\n**--vm-bytes** _SIZE_\n> Memory per worker.\n\n**--io** _N_\n> I/O stress workers.\n\n**--hdd** _N_\n> Disk write workers.\n\n**--matrix** _N_\n> Matrix computation.\n\n**--timeout** _TIME_\n> Test duration.\n\n**--metrics**\n> Show statistics.\n\n**--verbose**\n> Verbose output.\n\n**--stressors**\n> List stressors.\n\n**--class** _CLASS_\n> Run stressor class.\n\n**--sequential** _N_\n> Run stressors sequentially.\n\n**--all** _N_\n> Run all stressors."],["DESCRIPTION","**stress-ng** tests system stability under various loads. It exercises CPU, memory, I/O, and other subsystems to find hardware or software issues.\n\nCPU stressors include integer, floating-point, matrix operations, and cryptographic algorithms. Different stressors stress different CPU components.\n\nMemory tests allocate, write, and verify RAM. They can uncover bad memory, cooling issues, and memory controller problems.\n\nI/O stressors exercise filesystem and block device operations. They reveal disk, controller, and driver problems.\n\nMetrics show operations per second and system statistics. These help quantify performance and identify bottlenecks.\n\nCombined workloads simulate realistic multi-component stress. Thermal and power issues often appear only under mixed loads."],["CAVEATS","Can damage unstable hardware. High temperatures possible. May trigger OOM killer. Use with cooling monitoring."],["HISTORY","**stress-ng** was written by **Colin Ian King** at **Canonical** as an enhanced version of stress. It provides many more stressor types and is used for kernel and hardware testing."],["INSTALL","```apt: sudo apt install stress-ng```\n\n```dnf: sudo dnf install stress-ng```\n\n```pacman: sudo pacman -S stress-ng```\n\n```apk: sudo apk add stress-ng```\n\n```zypper: sudo zypper install stress-ng```\n\n```brew: brew install stress-ng```\n\n```nix: nix profile install nixpkgs#stress-ng```"],["SEE ALSO","[stress](/man/stress)(1), [sysbench](/man/sysbench)(1), [memtester](/man/memtester)(8), [fio](/man/fio)(1)"]],"e":["stress","sysbench","memtester","fio"],"x":"stress-ng --cpu [4] --timeout [60s]"},"systemctl-list-units":{"n":"systemctl-list-units","t":"List loaded units and their states","s":[["TAGLINE","List loaded units and their states"],["TLDR","List **active** units\n\n```systemctl list-units```\n\nList **all** units\n\n```systemctl list-units -a```\n\nFilter by **type**\n\n```systemctl list-units -t [service|socket|timer]```\n\nFilter by **state**\n\n```systemctl list-units --state [running|listening|dead]```\n\nFilter by **pattern**\n\n```systemctl list-units '[pattern*]'```\n\nOutput without **pager**\n\n```systemctl list-units --no-pager```\n\nOutput without **headers**\n\n```systemctl list-units --no-legend```\n\nList only **failed** units\n\n```systemctl list-units --failed```\n\nList units **with full names** (no ellipsis)\n\n```systemctl list-units --full```"],["SYNOPSIS","**systemctl list-units** [_OPTIONS_] [_PATTERN_...]"],["PARAMETERS","**-a, --all**\n> Include inactive units and units following other units\n\n**-t, --type=** _TYPE_\n> Filter by unit type (comma-separated list, e.g., service, socket, timer)\n\n**--state=** _STATE_\n> Filter by LOAD, SUB, or ACTIVE state (comma-separated list)\n\n**--failed**\n> Show only failed units (equivalent to --state=failed)\n\n**-l, --full**\n> Do not ellipsize unit names, descriptions, or other fields in the output\n\n**-r, --recursive**\n> Also show units of local containers\n\n**--with-dependencies**\n> Show specified units and their dependencies\n\n**--no-pager**\n> Disable pager output\n\n**--no-legend**\n> Suppress header and footer (useful for scripts)"],["DESCRIPTION","**systemctl list-units** displays units that systemd currently has loaded in memory. By default, it shows units that are active, have pending jobs, or have failed.\n\nThe output shows the unit name, load state, active state, sub-state, and description. This is the primary command for viewing current system state."],["OUTPUT COLUMNS","**UNIT** - Unit name\n\n**LOAD** - Load state (loaded, not-found, error)\n\n**ACTIVE** - Active state (active, inactive, failed)\n\n**SUB** - Sub-state (running, exited, dead, listening)\n\n**DESCRIPTION** - Unit description"],["CAVEATS","Only shows currently loaded units. Use `list-unit-files` to see all installed units. Failed units remain visible until acknowledged with `systemctl reset-failed`."],["HISTORY","The **list-units** subcommand is the primary status overview command in systemd, replacing various init script listing mechanisms."],["SEE ALSO","[systemctl-list-unit-files](/man/systemctl-list-unit-files)(1), [systemctl-list-timers](/man/systemctl-list-timers)(1), [systemctl-status](/man/systemctl-status)(1), [systemctl](/man/systemctl)(1), [journalctl](/man/journalctl)(1)"]],"e":["systemctl-list-unit-files","systemctl-list-timers","systemctl-status","systemctl","journalctl"],"x":"systemctl list-units"},"systemd-socket-activate":{"n":"systemd-socket-activate","t":"Test socket activation of daemons","s":[["TAGLINE","Test socket activation of daemons"],["TLDR","**Listen on a port** and launch a command on connection\n\n```systemd-socket-activate -l [8080] [command]```\n\n**Listen on multiple** ports for a command\n\n```systemd-socket-activate -l [8080] -l [8081] [command]```\n\n**Accept connections** and spawn a new instance for each\n\n```systemd-socket-activate -l [8080] -a [command]```\n\n**Use datagram (UDP)** sockets instead of stream (TCP)\n\n```systemd-socket-activate -l [8080] -d [command]```\n\n**Set environment variables** for the launched process\n\n```systemd-socket-activate -l [8080] -E [VAR=value] [command]```\n\n**Run in inetd** compatibility mode\n\n```systemd-socket-activate -l [8080] --inetd [command]```"],["SYNOPSIS","**systemd-socket-activate** [_options_] _command_ [_arguments_]"],["PARAMETERS","**-l** _ADDRESS_, **--listen=**_ADDRESS_\n> Listen on the specified address (port number or host:port).\n\n**-a**, **--accept**\n> Launch a new instance of the command for each connection. Cannot be combined with --now.\n\n**-d**, **--datagram**\n> Listen on a datagram socket (SOCK_DGRAM) instead of a stream socket. Cannot be combined with --seqpacket.\n\n**--seqpacket**\n> Listen on a sequential packet socket (SOCK_SEQPACKET) instead of a stream socket. Cannot be combined with --datagram.\n\n**-E** _VAR[=VALUE]_, **--setenv=**_VAR[=VALUE]_\n> Set an environment variable for the launched process. If no value given, inherits from the current environment.\n\n**--fdname=**_NAME[:NAME...]_\n> Specify names for the file descriptors passed. Enables use of sd_listen_fds_with_names(3).\n\n**--inetd**\n> Use the inetd protocol for passing file descriptors (as stdin/stdout) instead of the $LISTEN_FDS protocol.\n\n**--now**\n> Start the command immediately instead of waiting for a connection. Cannot be combined with --accept.\n\n**-h**, **--help**\n> Display help information.\n\n**--version**\n> Display version information."],["DESCRIPTION","**systemd-socket-activate** is a test and development tool for socket activation. It listens on sockets and launches a specified command when connections arrive, simulating systemd's socket activation feature without needing to configure full socket and service unit files.\n\nBy default it listens on a stream (TCP) socket. Use **--datagram** for UDP or **--seqpacket** for sequential packet sockets. The activated program receives the socket file descriptors via the **$LISTEN_FDS** protocol (or via stdin/stdout in **--inetd** mode)."],["CAVEATS","Primarily intended for testing, not production use. For production, configure proper systemd socket and service units. The service must support socket activation protocol. Part of the systemd suite."],["INSTALL","```apt: sudo apt install systemd```\n\n```dnf: sudo dnf install systemd```\n\n```pacman: sudo pacman -S systemd```\n\n```zypper: sudo zypper install systemd```\n\n```brew: brew install systemd```\n\n```nix: nix profile install nixpkgs#systemd```"],["SEE ALSO","[systemctl](/man/systemctl)(1), [systemd-run](/man/systemd-run)(1), [systemd-analyze](/man/systemd-analyze)(1)"]],"e":["systemctl","systemd-run","systemd-analyze"],"x":"systemd-socket-activate -l [8080] [command]"},"tlmgr-key":{"n":"tlmgr-key","t":"Manage TeX Live GPG verification keys","s":[["TAGLINE","Manage TeX Live GPG verification keys"],["TLDR","**List GPG keys**\n\n```tlmgr key list```\n\n**Add key**\n\n```tlmgr key add [keyfile]```\n\n**Remove key**\n\n```tlmgr key remove [keyid]```"],["SYNOPSIS","**tlmgr key** _action_ [_args_]"],["PARAMETERS","**list**\n> List installed keys.\n\n**add** _file_\n> Add GPG key from file.\n\n**remove** _keyid_\n> Remove key by ID."],["DESCRIPTION","**tlmgr key** manages GPG keys for TeX Live package verification. Ensures packages are from trusted sources. Part of TeX Live Manager security features."],["SEE ALSO","[tlmgr-repository](/man/tlmgr-repository)(1), [tlmgr](/man/tlmgr)(1)"]],"e":["tlmgr-repository","tlmgr"],"x":"tlmgr key list"},"toolbox-init-container":{"n":"toolbox-init-container","t":"Initialize Toolbx container internals","s":[["TAGLINE","Initialize Toolbx container internals"],["TLDR","**Initialize** a running container\n\n```toolbox init-container --gid [gid] --home [home] --shell [shell] --uid [uid] --user [user]```"],["SYNOPSIS","**toolbox init-container** [_OPTIONS_]"],["PARAMETERS","**--gid** _GID_\n> Group ID of the user\n\n**--home** _PATH_\n> Home directory path\n\n**--home-link**\n> Create symbolic link for home\n\n**--media-link**\n> Create symbolic link for media\n\n**--mnt-link**\n> Create symbolic link for mnt\n\n**--monitor-host**\n> Monitor host for configuration changes\n\n**--shell** _SHELL_\n> User's login shell\n\n**--uid** _UID_\n> User ID\n\n**--user** _NAME_\n> Username"],["DESCRIPTION","**toolbox init-container** initializes a running Toolbx container. This is an internal command executed automatically when a container starts and should not be run manually by users.\n\nThe command sets up the container environment to match the host system's user configuration, creating necessary symbolic links and configuring user identity."],["CAVEATS","This command cannot be run on the host system. It is designed for internal use by the Toolbx container initialization process only."],["SEE ALSO","[toolbox](/man/toolbox)(1), [toolbox-create](/man/toolbox-create)(1)"]],"e":["toolbox","toolbox-create"],"x":"toolbox init-container --gid [gid] --home [home] --shell [shell] --uid [uid] --user [user]"},"trace-cmd":{"n":"trace-cmd","t":"Linux kernel Ftrace tracing frontend","s":[["TAGLINE","Linux kernel Ftrace tracing frontend"],["TLDR","Display tracing **status**\n\n```sudo trace-cmd stat```\n\nList available **tracers**\n\n```sudo trace-cmd list -t```\n\n**Start** tracing with plugin\n\n```sudo trace-cmd start -p [function|function_graph|irqsoff|wakeup]```\n\n**View** trace output\n\n```sudo trace-cmd show```\n\n**Stop** tracing\n\n```sudo trace-cmd stop```\n\n**Clear** trace buffers\n\n```sudo trace-cmd clear```\n\n**Record** a trace of specific events\n\n```sudo trace-cmd record -e [sched_switch]```\n\n**Record** function graph tracing for a specific function\n\n```sudo trace-cmd record -p function_graph -g [function_name]```\n\nDisplay **recorded** trace from file\n\n```trace-cmd report```\n\nList available **events**\n\n```sudo trace-cmd list -e```"],["SYNOPSIS","**trace-cmd** _COMMAND_ [_OPTIONS_]"],["COMMANDS","**stat**\n> Display tracing system status\n\n**start**\n> Start the kernel tracer\n\n**stop**\n> Stop the kernel tracer\n\n**show**\n> View current trace output\n\n**clear**\n> Clear trace buffers\n\n**record**\n> Record a trace to file\n\n**report**\n> Display recorded trace\n\n**list**\n> List available tracers, events, and functions"],["PARAMETERS","**-p** _PLUGIN_\n> Specify tracer plugin (function, function_graph, irqsoff, wakeup, etc.)\n\n**-e** _EVENT_\n> Enable a trace event (can be specified multiple times)\n\n**-g** _FUNCTION_\n> Trace calls starting from a specific function (used with function_graph)\n\n**-o** _FILE_\n> Write trace output to specified file (default: trace.dat)\n\n**-t**\n> List available tracers (used with list subcommand)\n\n**-F**\n> Trace only the child process after a fork"],["DESCRIPTION","**trace-cmd** is a utility for interacting with the Linux kernel's Ftrace tracing framework. It provides a user-friendly interface for configuring and capturing kernel traces, analyzing function calls, timing, and system events.\n\nThe tool supports various tracer plugins including function tracing, function call graphs, and latency tracers for debugging and performance analysis."],["CAVEATS","Requires root privileges. Tracing can impact system performance. Large traces consume significant memory and storage. Some features depend on kernel configuration."],["HISTORY","**trace-cmd** was created by **Steven Rostedt** as a front-end for the Linux kernel's Ftrace framework, simplifying the process of kernel tracing and analysis."],["INSTALL","```apt: sudo apt install trace-cmd```\n\n```dnf: sudo dnf install trace-cmd```\n\n```pacman: sudo pacman -S trace-cmd```\n\n```zypper: sudo zypper install trace-cmd```\n\n```nix: nix profile install nixpkgs#trace-cmd```"],["SEE ALSO","[trace-cmd-list](/man/trace-cmd-list)(1), [trace-cmd-record](/man/trace-cmd-record)(1), [trace-cmd-report](/man/trace-cmd-report)(1), [perf](/man/perf)(1), [strace](/man/strace)(1)"]],"e":["trace-cmd-list","trace-cmd-record","trace-cmd-report","perf","strace"],"x":"sudo trace-cmd stat"},"transcrypt":{"n":"transcrypt","t":"Transparent Git file encryption","s":[["TAGLINE","Transparent Git file encryption"],["TLDR","**Initialize encryption**\n\n```transcrypt```\n\n**With specific cipher**\n\n```transcrypt -c [aes-256-cbc]```\n\n**Show credentials**\n\n```transcrypt -d```\n\n**Flush credentials**\n\n```transcrypt -f```\n\n**Rekey repository**\n\n```transcrypt -r```\n\n**Uninstall**\n\n```transcrypt -u```"],["SYNOPSIS","**transcrypt** [_-c cipher_] [_-d_] [_-f_] [_-r_] [_options_]"],["PARAMETERS","**-c** _CIPHER_\n> Encryption cipher.\n\n**-d**\n> Display credentials.\n\n**-f**\n> Flush credentials.\n\n**-r**\n> Rekey repository.\n\n**-u**\n> Uninstall.\n\n**-p** _PASSWORD_\n> Set password."],["DESCRIPTION","**transcrypt** provides transparent encryption for files within a Git repository using OpenSSL symmetric ciphers. It leverages Git's built-in clean and smudge filter mechanisms to automatically encrypt files when they are staged and decrypt them when checked out, making the process seamless for daily development workflows.\n\nThe tool encrypts sensitive files on push so they are stored encrypted in the remote repository, while automatically decrypting them on pull for local use. It supports any cipher available through OpenSSL, with AES-256-CBC as the default. Credentials are managed through Git's credential helper system, and the repository can be rekeyed if a password needs to be changed.\n\nEach clone of the repository requires initialization with the correct password to access encrypted files. The encryption configuration is stored in `.gitattributes`, which specifies which files should be encrypted."],["CAVEATS","Requires setup per clone. Password management. Git-specific."],["HISTORY","**transcrypt** was created for transparently encrypting files in a Git repository using OpenSSL."],["INSTALL","```brew: brew install transcrypt```\n\n```nix: nix profile install nixpkgs#transcrypt```"],["SEE ALSO","[git-crypt](/man/git-crypt)(1), [age](/man/age)(1), [gpg](/man/gpg)(1)"]],"e":["git-crypt","age","gpg"],"x":"transcrypt"},"trap":{"n":"trap","t":"Handle shell signals and cleanup","s":[["TAGLINE","Handle shell signals and cleanup"],["TLDR","**List** available signal names\n\n```trap -l```\n\nList current **trap commands** and their signals\n\n```trap```\n\n**Execute** a command when a signal is received\n\n```trap 'echo \"Caught signal SIGHUP\"' SIGHUP```\n\n**Remove** trap commands for signals\n\n```trap - SIGHUP SIGINT```\n\n**Ignore** a signal\n\n```trap '' SIGINT```"],["SYNOPSIS","**trap** [_command_] [_signal_...]\n\n**trap** **-l**\n\n**trap** **-p** [_signal_...]"],["PARAMETERS","**-l**\n> List signal names and numbers\n\n**-p** [_signal_]\n> Print trap commands for specified signals\n\n_command_\n> Command to execute when signal is received\n\n**-** _signal_\n> Reset signal to default behavior\n\n**''** _signal_\n> Ignore the signal"],["DESCRIPTION","**trap** is a shell builtin that executes commands when the shell receives signals. It's commonly used in scripts for cleanup operations (removing temp files), handling interrupts gracefully, and ignoring signals that would otherwise terminate the script.\n\nCommon signals include SIGINT (Ctrl+C), SIGTERM (termination request), SIGHUP (hangup), EXIT (script exit), and ERR (command error). The EXIT pseudo-signal executes on normal script exit."],["CAVEATS","Trap is a shell builtin, not an external command. Behavior varies slightly between shells (bash, dash, zsh). Some signals (SIGKILL, SIGSTOP) cannot be trapped. Traps are inherited by subshells but not by external commands."],["HISTORY","Originated in the **Bourne shell** in Unix V7 (1979). The concept of trapping signals comes from the Unix signal handling mechanism. Bash and other modern shells have extended the original functionality with pseudo-signals like ERR and DEBUG."],["INSTALL","```apk: sudo apk add execline```\n\n```brew: brew install execline```\n\n```nix: nix profile install nixpkgs#execline```"],["SEE ALSO","[kill](/man/kill)(1), [bash](/man/bash)(1)"]],"e":["kill","bash"],"x":"trap -l"},"uigen":{"n":"uigen","t":"Render a SaaS-style UI directly from an OpenAPI specification at runtime","s":[["TAGLINE","Render a SaaS-style UI directly from an OpenAPI specification at runtime"],["TLDR","**Scaffold a new uigen project** in the current directory\n\n```npx @uigen-dev/cli init```\n\nServe the **UI from an OpenAPI spec** on http://localhost:4400\n\n```npx @uigen-dev/cli serve [openapi.yaml]```\n\nRoute API calls to a **backend running locally**\n\n```npx @uigen-dev/cli serve [openapi.yaml] --proxy-base [http://localhost:8000]```\n\n**Bundle for production** deployment\n\n```npx @uigen-dev/cli build --spec [openapi.yaml]```\n\nUse an **explicit spec flag** instead of a positional argument\n\n```npx @uigen-dev/cli serve --spec [api/openapi.yaml]```"],["SYNOPSIS","**uigen** _subcommand_ [_spec_] [_options_]"],["DESCRIPTION","**uigen** is an OpenAPI-to-UI compiler. It reads an OpenAPI 3.x specification, parses it into a framework-agnostic Intermediate Representation containing resources, operations, schemas, authentication flows, and pagination strategies, and renders a complete React frontend at runtime. There is no code-generation step — the rendered UI is recomputed each time the server starts (or on hot reload), so changes to the spec are reflected immediately.\n\nOut of the box uigen produces table views with sorting, filtering, and pagination; create and edit forms with validation derived from JSON schemas; detail views with cross-resource links; charts; file uploads; and authentication flows for OAuth 2.0, Bearer tokens, and API keys. Theme customization and AI-native \"agent skills\" let downstream tools (e.g. Claude Code) extend the generated app without touching generated source files."],["PARAMETERS","**init**\n> Scaffold a new project including configuration files, agent skills, and an example OpenAPI spec.\n\n**serve** [_spec_]\n> Start a development server rendering the UI from _spec_ at runtime. Defaults to **http://localhost:4400**.\n\n**build** [_spec_]\n> Produce a production bundle of the rendered UI for static or server-side hosting.\n\n**--spec** _file_\n> Alternative way to specify the OpenAPI file when the subcommand expects no positional argument.\n\n**--proxy-base** _url_\n> Forward API calls made by the rendered UI to a backend at _url_ (useful in local development).\n\n**--port** _n_\n> Override the default development-server port (4400)."],["CONFIGURATION","**uigen.config.json**\n> Project-level configuration written by **init**: theme overrides, authentication defaults, and renderer plugins.\n\n**.uigen/**\n> Cache directory holding the parsed Intermediate Representation between runs."],["CAVEATS","Renders **React** today; Svelte and Vue renderers are on the roadmap but not yet shipped. Because rendering is **runtime-only**, the OpenAPI spec must remain reachable at startup — there is no compiled JS bundle independent of the spec. Complex polymorphic schemas (deeply nested **oneOf**/**anyOf**) may render generically and need manual templates."],["HISTORY","**uigen** was released by **darula-hpp** in **2026** as part of a broader trend toward spec-driven, AI-native SaaS frontends. It deliberately avoids the boilerplate generated by traditional **swagger-codegen** by keeping rendering at runtime and is distributed through **npm** as **@uigen-dev/cli**."],["SEE ALSO","[swagger-codegen](/man/swagger-codegen)(1), [openapi-generator](/man/openapi-generator)(1), [npx](/man/npx)(1)"]],"e":["swagger-codegen","openapi-generator","npx"],"x":"npx @uigen-dev/cli init"},"unrtf":{"n":"unrtf","t":"RTF to other formats converter","s":[["TAGLINE","RTF to other formats converter"],["TLDR","Convert an RTF file to **HTML**\n\n```unrtf --html [document.rtf] > [document.html]```\n\nConvert an RTF file to **plain text**\n\n```unrtf --text [document.rtf] > [document.txt]```\n\nConvert an RTF file to **LaTeX**\n\n```unrtf --latex [document.rtf] > [document.tex]```\n\nConvert without extracting **embedded pictures**\n\n```unrtf --nopict --html [document.rtf] > [document.html]```\n\nConvert with **verbose** output\n\n```unrtf --verbose --html [document.rtf]```\n\nConvert from **stdin**\n\n```cat [document.rtf] | unrtf --text```"],["SYNOPSIS","**unrtf** [_--html_|_--text_|_--vt_|_--latex_|_--rtf_] [_options_] [_file..._]"],["PARAMETERS","**--html**\n> Select HTML output (default)\n\n**--text**\n> Select plain ASCII text output\n\n**--vt**\n> Select text output with VT100 terminal escape codes\n\n**--latex**\n> Select LaTeX document output\n\n**--rtf**\n> Select RTF output (typically more compact than input)\n\n**--nopict**\n> Disable extraction of embedded pictures to the current directory\n\n**--noremap**\n> Disable charset conversion (only works for 8-bit character sets)\n\n**--verbose**\n> Print additional processing information\n\n**--quiet**\n> Suppress leading comments in the output\n\n**-t** _tags_file_\n> Specify an output configuration file to use\n\n**-P** _config_search_path_\n> Specify directories (colon-separated) to search for configuration files. Defaults to /usr/share/unrtf/\n\n**--version**\n> Print program version\n\n**--help**\n> Display help information"],["DESCRIPTION","**unrtf** is a GNU command-line converter that transforms documents in **Rich Text Format (RTF)** into other formats including HTML, plain text, LaTeX, and VT100-escaped text. It supports tables, fonts, hyperlinks, paragraph alignment, foreground and background colors. Embedded images are extracted as separate files unless disabled with **--nopict**. If no file is specified, unrtf reads from standard input. Output is written to standard output.\n\nSince version 0.21.0, all codepage conversion is done through the **iconv** library and output control is managed through runtime configuration files, making it highly customizable."],["CONFIGURATION","Output is controlled via runtime configuration files located in **/usr/share/unrtf/**. Custom tag files can be specified with **-t** and alternate search paths with **-P**. The SYMBOL font mapping is stored in **/usr/share/unrtf/SYMBOL.charmap**."],["CAVEATS","Multiple security vulnerabilities (CVEs) have been found in older versions, including out-of-bounds memory access when processing maliciously crafted RTF files. Always use an up-to-date version. The **--noremap** option only works for 8-bit character sets. PostScript output was removed in version 0.19.5 due to defects. Not all RTF features are fully supported; complex documents with advanced formatting or unusual encodings may not convert perfectly."],["HISTORY","Originally written by **Zachary T. Smith** around **2000** as a program called **rtf2htm**, it was later renamed to **UnRTF** and adopted as an official **GNU project** under the Free Software Foundation. Other contributors include David F. Davey and Jean-Francois Dockes. The latest release version is **0.21.11**. Licensed under the **GNU General Public License**."],["INSTALL","```apt: sudo apt install unrtf```\n\n```dnf: sudo dnf install unrtf```\n\n```pacman: sudo pacman -S unrtf```\n\n```apk: sudo apk add unrtf```\n\n```brew: brew install unrtf```\n\n```nix: nix profile install nixpkgs#unrtf```"],["SEE ALSO","[pandoc](/man/pandoc)(1), [iconv](/man/iconv)(1), [enscript](/man/enscript)(1)"]],"e":["pandoc","iconv","enscript"],"x":"unrtf --html [document.rtf] > [document.html]"},"var-dump-server":{"n":"var-dump-server","t":"Symfony PHP variable dump receiver","s":[["TAGLINE","Symfony PHP variable dump receiver"],["TLDR","**Start server on the default address**\n\n```var-dump-server```\n\n**Start on a custom host and port**\n\n```var-dump-server --host=[127.0.0.1:9912]```\n\n**Output dumps in HTML format to a file**\n\n```var-dump-server --format=html > [dump.html]```"],["SYNOPSIS","**var-dump-server** [_--host address_] [_--format fmt_] [_options_]"],["PARAMETERS","**--host** _ADDR_\n> Bind address and port (default: 127.0.0.1:9912, or the VAR_DUMPER_SERVER env var).\n\n**--format** _FMT_\n> Output format (cli or html).\n\n**--help**\n> Show help."],["DESCRIPTION","**var-dump-server** starts a server that collects and displays variable dumps from PHP applications using the Symfony VarDumper component. Instead of dumping variables directly into the browser or console output, the application sends them to this centralized server for structured display.\n\nThis approach keeps debug output separate from application responses, making it particularly useful for debugging API endpoints, AJAX requests, and command-line scripts. The server formats dumps with syntax highlighting and collapsible structures, providing a cleaner debugging experience than inline var_dump output."],["CAVEATS","Symfony/PHP specific. VarDumper required. Development tool."],["HISTORY","**var-dump-server** is part of **Symfony VarDumper** component for receiving and displaying variable dumps from PHP applications."],["SEE ALSO","[php](/man/php)(1), [symfony](/man/symfony)(1)"]],"e":["php","symfony"],"x":"var-dump-server"},"wifite":{"n":"wifite","t":"Automated wireless network security auditing","s":[["TAGLINE","Automated wireless network security auditing"],["TLDR","**Start wifite** in interactive mode\n\n```sudo wifite```\n\n**Target only WPA networks**\n\n```sudo wifite --wpa```\n\n**Target only WPS-enabled networks**\n\n```sudo wifite --wps```\n\n**Specify wireless interface**\n\n```sudo wifite -i [wlan0]```\n\n**Attack networks with minimum signal strength**\n\n```sudo wifite --power [50]```\n\n**Target specific network** by ESSID\n\n```sudo wifite --essid \"[NetworkName]\"```\n\n**Use specific wordlist** for cracking\n\n```sudo wifite --dict [/path/to/wordlist.txt]```\n\n**Scan 5GHz channels**\n\n```sudo wifite --5ghz```"],["SYNOPSIS","**wifite** [_options_]"],["PARAMETERS","**-i** _INTERFACE_\n> Wireless interface to use\n\n**-c** _CHANNEL_\n> Channel(s) to scan (e.g., 1,3-6)\n\n**-5**, **--5ghz**\n> Include 5GHz channels\n\n**-p** _POWER_, **--power** _POWER_\n> Minimum signal strength (dBm)\n\n**--essid** _NAME_\n> Target specific network by name\n\n**--bssid** _MAC_\n> Target specific network by MAC address\n\n**--wpa**\n> Target only WPA-encrypted networks\n\n**--wps**\n> Target only WPS-enabled networks\n\n**--wep**\n> Target only WEP-encrypted networks\n\n**--dict** _FILE_\n> Wordlist for password cracking\n\n**--pmkid**\n> Use PMKID capture attack\n\n**--new-hs**\n> Capture new handshake, ignore existing\n\n**--pixie**\n> Use Pixie Dust attack on WPS\n\n**--bully**\n> Use Bully for WPS attacks\n\n**--reaver**\n> Use Reaver for WPS attacks\n\n**--clients-only**\n> Only show networks with clients\n\n**--nodeauths**\n> Passive mode; no deauthentication\n\n**--daemon**\n> Return interface to managed mode on exit\n\n**--infinite**\n> Enable infinite attack mode\n\n**-v**, **--verbose**\n> Show more output and commands\n\n**-h**, **--help**\n> Show help"],["DESCRIPTION","**wifite** is an automated wireless network auditing tool that simplifies Wi-Fi security testing. It automates scanning for networks, capturing handshakes, and attempting to crack passwords using various attack methods.\n\nThe tool supports multiple attack vectors: WPA handshake capture with dictionary attacks, WPS PIN brute-forcing, Pixie Dust attacks against vulnerable WPS implementations, and legacy WEP attacks. It integrates with tools like Aircrack-ng, Reaver, Bully, and Hashcat.\n\nWifite handles putting the wireless adapter into monitor mode, scanning for targets, and sequentially attempting attacks. Results are displayed in an interactive interface that shows network details and attack progress."],["CAVEATS","Requires a wireless adapter supporting monitor mode and packet injection. Root privileges are required. Only test networks you own or have explicit permission to audit—unauthorized access is illegal. Some attacks (deauthentication) disrupt legitimate network users. Success depends on network configuration and password complexity."],["HISTORY","Wifite was originally created by **derv82** to automate common Wi-Fi auditing tasks that previously required manual use of multiple tools. Wifite2, a complete rewrite, added Python 3 support, improved attack methods, and better handling of modern wireless security. The tool became popular in the security community for penetration testing and is included by default in Kali Linux."],["INSTALL","```pacman: sudo pacman -S wifite```"],["SEE ALSO","[aircrack-ng](/man/aircrack-ng)(1), [reaver](/man/reaver)(1), [bully](/man/bully)(1), [kismet](/man/kismet)(1)"]],"e":["aircrack-ng","reaver","bully","kismet"],"x":"sudo wifite"},"xcaddy":{"n":"xcaddy","t":"Build custom Caddy with modules","s":[["TAGLINE","Build custom Caddy with modules"],["TLDR","**Build Caddy with modules**\n\n```xcaddy build --with [github.com/module]```\n\n**Build specific Caddy version**\n\n```xcaddy build [v2.7.0] --with [github.com/module]```\n\n**Build a module at a specific version**\n\n```xcaddy build --with [github.com/module@v1.0.0]```\n\n**Replace a module with a local copy** for development\n\n```xcaddy build --with [github.com/module=../local/path]```\n\n**Run Caddy with a module** for development and testing\n\n```xcaddy run --config [Caddyfile]```\n\n**Write the binary to a path**\n\n```xcaddy build --output [/path/to/caddy] --with [github.com/module]```"],["SYNOPSIS","**xcaddy** **build** [_caddy_version_] [_--output file_] [_--with module[@version][=replacement]_]... [_options_]\n\n**xcaddy** **run** [_caddy_args_...]\n\n**xcaddy** **version**"],["PARAMETERS","**build**\n> Build a custom Caddy binary. An optional Caddy version (e.g. v2.7.0 or a Git ref) can be given as the first argument.\n\n**run**\n> Build and run Caddy with the modules from the current Go module, passing any remaining arguments to the caddy command. Intended for plugin development.\n\n**version**\n> Print the xcaddy version.\n\n**--with** _MODULE[@VERSION][=REPLACEMENT]_\n> Add a Caddy module. Repeat to include several. An optional @version pins the module, and =replacement points it at a local path or fork.\n\n**--replace** _OLD=NEW_\n> Add a Go module replace directive without importing the module (for transitive dependencies).\n\n**--output** _FILE_\n> Path of the output binary. Defaults to ./caddy.\n\n**--embed** _[ALIAS]:PATH_\n> Embed a directory into the binary, accessible at the given alias.\n\n**--pgo** _FILE_\n> Profile-guided optimization profile to pass to the Go compiler."],["DESCRIPTION","**xcaddy** is a build tool for creating custom Caddy web server binaries with additional modules compiled in. It uses the Go toolchain to compile Caddy from source with the specified plugins included, producing a single binary with all desired functionality.\n\nThe build command compiles a specific Caddy version with one or more modules, while the run command temporarily builds and runs Caddy with the specified modules for development and testing without producing a permanent binary. Multiple modules can be included using repeated --with flags."],["CAVEATS","Requires a working Go toolchain. The Caddy core version can also be set with the **CADDY_VERSION** environment variable. Other variables tune the build: **XCADDY_RACE_DETECTOR=1**, **XCADDY_DEBUG=1**, **XCADDY_SKIP_CLEANUP=1**, and **XCADDY_GO_BUILD_FLAGS**."],["HISTORY","**xcaddy** was created by the **Caddy** project for building custom Caddy binaries with plugins."],["INSTALL","```apk: sudo apk add xcaddy```\n\n```nix: nix profile install nixpkgs#xcaddy```"],["SEE ALSO","[caddy](/man/caddy)(1), [go](/man/go)(1)"]],"e":["caddy","go"],"x":"xcaddy build --with [github.com/module]"},"xonsh":{"n":"xonsh","t":"Python-powered cross-platform shell","s":[["TAGLINE","Python-powered cross-platform shell"],["TLDR","**Start xonsh**\n\n```xonsh```\n\n**Run command**\n\n```xonsh -c \"[print('hello')]\"```\n\n**Run script**\n\n```xonsh [script.xsh]```\n\n**Interactive Python mode**\n\n```xonsh -i```"],["SYNOPSIS","**xonsh** [_-c command_] [_-i_] [_options_] [_file_]"],["PARAMETERS","**-c** _CMD_\n> Execute command.\n\n**-i**\n> Interactive mode.\n\n**--login**\n> Login shell.\n\n**--help**\n> Show help."],["DESCRIPTION","**xonsh** is a cross-platform shell that combines the capabilities of Python with the familiarity of traditional Unix shells. It allows users to seamlessly mix Python expressions and standard shell commands in the same session, using Python syntax as the primary language while supporting conventional shell operations.\n\nThe shell provides context-aware tab completion, syntax highlighting, and a customizable prompt. Shell commands run natively, and Python code can be used anywhere for variable manipulation, control flow, and data processing. Scripts use the **.xsh** extension and can contain both Python and shell syntax.\n\nxonsh runs on any platform where Python is available, making it a portable choice for users who want Python's expressiveness integrated directly into their shell workflow."],["CAVEATS","Python required. Different from bash. Learning curve."],["HISTORY","**xonsh** was created to combine the power of Python with the familiarity of shell scripting."],["INSTALL","```dnf: sudo dnf install xonsh```\n\n```pacman: sudo pacman -S xonsh```\n\n```zypper: sudo zypper install xonsh```\n\n```brew: brew install xonsh```\n\n```nix: nix profile install nixpkgs#xonsh```"],["SEE ALSO","[bash](/man/bash)(1), [zsh](/man/zsh)(1), [python](/man/python)(1), [fish](/man/fish)(1)"]],"e":["bash","zsh","python","fish"],"x":"xonsh"},"xtrace":{"n":"xtrace","t":"Trace X11 protocol communication between client and server","s":[["TAGLINE","Trace X11 protocol communication between client and server"],["TLDR","**Trace an X11 client** and print all protocol messages\n\n```xtrace [xeyes]```\n\n**Forward to a specific** real display\n\n```xtrace --display [:0] [firefox]```\n\n**Set a custom fake** display name\n\n```xtrace --fakedisplay [:42] [xclock]```\n\n**Keep xtrace running** after the launched client exits\n\n```xtrace --keeprunning [xterm]```\n\n**Wait for a later client** to connect before exiting\n\n```xtrace --waitforclient --fakedisplay [:42]```\n\n**Write trace output** to a file\n\n```xtrace --outfile [trace.log] [xeyes]```\n\n**Show relative timestamps** for each protocol message\n\n```xtrace --relative-timestamps [xclock]```"],["SYNOPSIS","**xtrace** [_options_] [_command_ [_arguments_ ...]]"],["PARAMETERS","**-d**, **--display** _DISPLAY_\n> Real X server to forward connections to (defaults to **$DISPLAY**).\n\n**-D**, **--fakedisplay** _DISPLAY_\n> Fake display name xtrace listens on (default **:9**).\n\n**-k**, **--keeprunning**\n> Keep running after the launched client disconnects.\n\n**-s**, **--stopwhendone**\n> Terminate when all clients have disconnected (default behavior).\n\n**-W**, **--waitforclient**\n> Do not exit even if the launched program terminates without connecting.\n\n**-c**, **--copyauthentication**\n> Copy X authentication cookies from the real display (default).\n\n**-n**, **--nocopyauthentication**\n> Do not copy authentication data to the fake display.\n\n**-f** _file_\n> Read authentication data from _file_.\n\n**-F** _file_\n> Write generated authentication data to _file_.\n\n**-o**, **--outfile** _FILE_\n> Write trace output to _FILE_ instead of stdout.\n\n**-b**, **--buffered**\n> Buffer trace output for higher performance.\n\n**-w**, **--readwritedebug**\n> Include byte counts for every read and write operation.\n\n**--timestamps**\n> Prefix each entry with an absolute wall-clock timestamp.\n\n**--relative-timestamps**\n> Prefix each entry with time elapsed since the previous entry.\n\n**--monotonic-timestamps**\n> Prefix each entry with a monotonic timestamp source."],["DESCRIPTION","**xtrace** is a debugging tool that inspects the X11 wire protocol traffic between an X client (such as a GUI application) and an X server (such as **Xorg** or **Xwayland**). It starts a fake X server on a chosen display number, forwards every connection to the real X server pointed to by **$DISPLAY** (or **--display**), and prints a human-readable log of each request, reply, event, and error that flows across the wire.\n\nThe launched program has its **$DISPLAY** environment variable rewritten to the fake display, so it transparently connects through xtrace. This makes it possible to see, for example, which X requests an application issues on startup, what events the server delivers, and where protocol errors occur — without modifying the application itself.\n\nWhen no command is given (typically combined with **--waitforclient**), xtrace simply listens on the fake display and logs any client that later connects to it, which is useful for attaching to applications that are started by other means."],["CAVEATS","xtrace traces only the X11 protocol; it does not see GLX, DRI3, shared-memory (**MIT-SHM**) buffers, or other channels that bypass the wire. Output can be very verbose — buffering (**-b**) and targeted runs are recommended. Under Wayland, only XWayland clients are traceable."],["HISTORY","**xtrace** was written by **Bernhard R. Link** and is packaged in Debian and Ubuntu as the **xtrace** package. It is commonly used by X11 developers and package maintainers to debug protocol-level issues in clients and toolkits."],["INSTALL","```apt: sudo apt install xtrace```\n\n```nix: nix profile install nixpkgs#xtrace```"],["SEE ALSO","[xauth](/man/xauth)(1), [Xorg](/man/Xorg)(1), [xdpyinfo](/man/xdpyinfo)(1), [xev](/man/xev)(1), [strace](/man/strace)(1)"]],"e":["xauth","Xorg","xdpyinfo","xev","strace"],"x":"xtrace [xeyes]"},"yay":{"n":"yay","t":"Arch Linux AUR helper","s":[["TAGLINE","Arch Linux AUR helper"],["TLDR","**Update** all packages from repos and the AUR\n\n```yay```\n\nInteractively **search and install** by name or term\n\n```yay [search_term]```\n\n**Install** a package without confirmation prompts\n\n```yay -S [package] --noconfirm```\n\n**Search** repos and AUR for a keyword\n\n```yay -Ss [keyword]```\n\n**Remove** a package with unused deps and configs\n\n```yay -Rns [package]```\n\nClean **orphans** tracked by yay\n\n```yay -Yc```\n\nClear **pacman/yay package caches**\n\n```yay -Scc```\n\nPrint package/system **stats**\n\n```yay -Ps```"],["SYNOPSIS","**yay** \\<operation\\> [options] [targets]"],["DESCRIPTION","Yay is a Pacman wrapper with AUR support. It passes options to Makepkg and Pacman after resolving packages to install/upgrade."],["YAY OPERATIONS","**-Y, --yay**\n\n> Perform yay specific operations. This is the default if no other operation is selected.\n\n**-P, --show**\n\n> Perform yay specific print operations.\n\n**-G, --getpkgbuild**\n\n> Downloads PKGBUILD from ABS or AUR. The ABS can only be used for Arch Linux repositories\n\nIf **no arguments are provided** 'yay -Syu' will be performed.\n\nIf **no operation is selected** -Y will be assumed."],["EXTENDED PACMAN OPERATIONS","**-S, -Si, -Sl, -Ss, -Su, -Sc, -Qu**\n\n> These operations are extended to support both AUR and repo packages.\n\n**-Sc**\n\n> Yay will also clean cached AUR package and any untracked Files in the cache. Cleaning untracked files will wipe any downloaded sources or built packages but will keep already downloaded vcs sources.\n\n**-R**\n\n> Yay will also remove cached data about devel packages."],["YAY OPTIONS (APPLY TO -Y AND --YAY)","**<NO OPTION>**\n\n> Displays a list of packages matching the search terms and prompts the user on which packages to install (yogurt mode).\n\n**--gendb**\n\n> Generate development package database. Tracks the latest commit for each development package, when there is a new commit Yay will know to update. This is done per package whenever a package is synced. This option should only be used when migrating to Yay from another AUR helper.\n\n**-c, --clean**\n\n> Remove unneeded dependencies."],["SHOW OPTIONS (APPLY TO -P AND --SHOW)","**-c, --complete**\n\n> Print a list of all AUR and repo packages. This allows shell completion and is not intended to be used directly by the user.\n\n**-f, --fish**\n\n> During complete adjust the output for the fish shell.\n\n**-d, --defaultconfig**\n\n> Print default yay configuration.\n\n**-g, --currentconfig**\n\n> Print current yay configuration.\n\n**-n, --numberupgrades**\n\n> Deprecated, use  **yay -Qu**  and  **wc -l**  instead.\n\n**-s, --stats**\n\n> Displays information about installed packages and system health. If there are orphaned, or out-of-date packages, or packages that no longer exist on the AUR; warnings will be displayed.\n\n**-u, --upgrades**\n\n> Deprecated, use  **yay -Qu**  instead.\n\n**-w, --news**\n\n> Print new news from the Archlinux homepage. News is considered new if it is newer than the build date of all native packages. Pass this twice to show all available news.\n\n**-q, --quiet**\n\n> Only show titles when printing news."],["PERMANENT CONFIGURATION SETTINGS","**--save**\n\n> Causes the following options to be saved back to the config file. This provides an easy way to change config options without directly editing the file.\n\n**--aururl**\n\n> Set an alternative AUR URL. This is mostly useful for users in China who wish to use https://aur.tuna.tsinghua.edu.cn/.\n\n**--builddir <dir>**\n\n> Directory to use for Building AUR Packages. This directory is also used as the AUR cache when deciding if Yay should skip builds.\n\n**--editor <command>**\n\n> Editor to use when editing PKGBUILDs. If this is not set the  **EDITOR**  environment variable will be checked, followed by  **VISUAL**. If none of these are set Yay will prompt the user for an editor.\n\n**--editorflags <flags>**\n\n> Passes arguments to the editor. These flags get passed to every instance where the editor is called by Yay. Arguments are split on whitespace before being passed to the editor. Multiple arguments may be passed by supplying a space separated list that is quoted by the shell.\n\n**--makepkg <command>**\n\n> The command to use for  **makepkg**  calls. This can be a command in  **PATH**  or an absolute path to the file.\n\n**--pacman <command>**\n\n> The command to use for  **pacman**  calls. This can be a command in  **PATH**  or an absolute path to the file.\n\n**--tar <command>**\n\n> The command to use for  **bsdtar**  calls. This can be a command in  **PATH**  or an absolute path to the file.\n\n**--git <command>**\n\n> The command to use for  **git**  calls. This can be a command in  **PATH**  or an absolute path to the file.\n\n**--gpg <command>**\n\n> The command to use for  **gpg**  calls. This can be a command in  **PATH**  or an absolute path to the file.\n\n**--config <file>**\n\n> The pacman config file to use.\n\n**--makepkgconf <file>**\n\n> The config file for makepkg to use. If this is not set then the default config file will be used.\n\n**--nomakepkgconf**\n\n> Reset the makepkg config file back to its default.\n\n**--requestsplitn <number>**\n\n> The maximum amount of packages to request per AUR query. The higher the number the faster AUR requests will be. Requesting too many packages in one AUR query will cause an error. This should only make a noticeable difference with very large requests (>500) packages.\n\n**--completioninterval <days>**\n\n> Time in days to refresh the completion cache. Setting this to 0 will cause the cache to be refreshed every time, while setting this to -1 will cause the cache to never be refreshed.\n\n**--sortby <votes|popularity|id|baseid|name|base|submitted|modified>**\n\n> Sort AUR results by a specific field during search.\n\n**--searchby <name|name-desc|maintainer|depends|checkdepends|makedepends|optdepends>**\n\n> Search for AUR packages by querying the specified field.\n\n**--answerclean <All|None|Installed|NotInstalled|...>**\n\n> Set a predetermined answer for the clean build menu question. This answer will be used instead of reading from standard input but will be parsed exactly the same.\n\n**--answerdiff <All|None|Installed|NotInstalled|...>**\n\n> Set a predetermined answer for the edit diff menu question. This answer will be used instead of reading from standard input but will be parsed exactly the same.\n\n**--answeredit <All|None|Installed|NotInstalled|...>**\n\n> Set a predetermined answer for the edit pkgbuild menu question. This answer will be used instead of reading from standard input but will be parsed exactly the same.\n\n**--answerupgrade <Repo|^Repo|None|...>**\n\n> Set a predetermined answer for the upgrade menu question. Selects which package ranges or repos to omit for updates. This answer will be used instead of reading from standard input but will be treated exactly the same.\n\n**--noanswerclean**\n\n> Unset the answer for the clean build menu.\n\n**--noanswerdiff**\n\n> Unset the answer for the diff menu.\n\n**--noansweredit**\n\n> Unset the answer for the edit pkgbuild menu.\n\n**--noanswerupgrade**\n\n> Unset the answer for the upgrade menu.\n\n**--cleanmenu**\n\n> Show the clean menu. This menu gives you the chance to fully delete the downloaded build files from Yay's cache before redownloading a fresh copy.\n\n**--diffmenu**\n\n> Show the diff menu. This menu gives you the option to view diffs from build files before building.\n\nDiffs are shown via  **git diff**  which uses less by default. This behaviour can be changed via git's config, the  **$GIT_PAGER**  or  **$PAGER**  environment variables.\n\n**--editmenu**\n\n> Show the edit menu. This menu gives you the option to edit or view PKGBUILDs before building.\n\n**Warning**: Yay resolves dependencies ahead of time via the RPC. It is not recommended to edit pkgbuild variables unless you know what you are doing.\n\n**--upgrademenu**\n\n> Show a detailed list of updates in a similar format to VerbosePkgLists. Upgrades can also be skipped using numbers, number ranges or repo names. Additionally ^ can be used to invert the selection.\n\n**Warning**: It is not recommended to skip updates from the repositories as this can lead to partial upgrades. This feature is intended to easily skip AUR updates on the fly that may be broken or have a long compile time. Ultimately it is up to the user what upgrades they skip.\n\n**--nocleanmenu**\n\n> Do not show the clean menu.\n\n**--nodiffmenu**\n\n> Do not show the diff menu.\n\n**--noeditmenu**\n\n> Do not show the edit menu.\n\n**--noupgrademenu**\n\n> Do not show the upgrade menu.\n\n**--askremovemake**\n\n> Ask to remove makedepends after installing packages.\n\n**--removemake**\n\n> Remove makedepends after installing packages.\n\n**--noremovemake**\n\n> Do not remove makedepends after installing packages.\n\n**--topdown**\n\n> Display repository packages first and then AUR packages.\n\n**--bottomup**\n\n> Show AUR packages first and then repository packages.\n\n**--singlelineresults**\n\n> Override pacman's usual double-line search result format and list each result on its own line.\n\n**--doublelineresults**\n\n> Follow pacman's double-line search result format and list each result using two lines.\n\n**--devel**\n\n> During sysupgrade also check AUR development packages for updates. Currently only Git packages are supported.\n\nDevel checking is done using  **git ls-remote**. The newest commit hash is compared against the hash at install time. This allows devel updates to be checked almost instantly and not require the original pkgbuild to be downloaded.\n\nThe slower pacaur-like devel checks can be implemented manually by piping a list of packages into yay (see  **examples**).\n\n**--nodevel**\n\n> Do not check for development packages updates during sysupgrade.\n\n**--cleanafter**\n\n> Remove untracked files after installation.\n\nUntracked files are removed with the exception of directories. This allows VCS packages to easily pull an update instead of having to reclone the entire repo.\n\n**--nocleanafter**\n\n> Do not remove package sources after successful Install.\n\n**--timeupdate**\n\n> During sysupgrade also compare the build time of installed packages against the last modification time of each package's AUR page.\n\n**--notimeupdate**\n\n> Do not consider build times during sysupgrade.\n\n**--redownload**\n\n> Always download pkgbuilds of targets even when a copy is available in cache.\n\n**--redownloadall**\n\n> Always download pkgbuilds of all AUR packages even when a copy is available in cache.\n\n**--noredownload**\n\n> When downloading pkgbuilds if the pkgbuild is found in cache and is equal or newer than the AUR's version use that instead of downloading a new one.\n\n**--provides**\n\n> Look for matching providers when searching for AUR packages. When multiple providers are found a menu will appear prompting you to pick one. This increases dependency resolve time although this should not be noticeable.\n\n**--noprovides**\n\n> Do not look for matching providers when searching for AUR packages. Yay will never show its provider menu but Pacman will still show its provider menu for repo packages.\n\n**--pgpfetch**\n\n> Prompt to import unknown PGP keys from the  **validpgpkeys**  field of each PKGBUILD.\n\n**--nopgpfetch**\n\n> Do not prompt to import unknown PGP keys. This is likely to cause a build failure unless using options such as  **--skippgpcheck**  or a customized gpg config.\n\n**--useask**\n\n> Use pacman's --ask flag to automatically confirm package conflicts. Yay lists conflicts ahead of time. It is possible that Yay does not detect a conflict, causing a package to be removed without the user's confirmation. However, this is very unlikely.\n\n**--nouseask**\n\n> Manually resolve package conflicts during the install. Packages which do not conflict will not need to be confined manually.\n\n**--combinedupgrade**\n\n> During sysupgrade, Yay will first perform a refresh, then show its combined menu of repo and AUR packages that will be upgraded. Then after reviewing the pkgbuilds, the repo and AUR upgrade will start with no need for manual intervention.\n\nIf Yay exits for any reason After the refresh without upgrading. It is then the user's responsibility to either resolve the reason Yay exited or run a sysupgrade through pacman directly.\n\n**--nocombinedupgrade**\n\n> During sysupgrade, Pacman -Syu will be called, then the AUR upgrade will start. This means the upgrade menu and pkgbuild review will be performed after the sysupgrade has finished.\n\n**--batchinstall**\n\n> When building and installing AUR packages instead of installing each package after building, queue each package for install. Then once either all packages are built or a package in the build queue is needed as a dependency to build another package, install all the packages in the install queue.\n\n**--nobatchinstall**\n\n> Always install AUR packages immediately after building them.\n\n**--rebuild**\n\n> Always build target packages even when a copy is available in cache.\n\n**--rebuildall**\n\n> Always build all AUR packages even when a copy is available in cache.\n\n**--rebuildtree**\n\n> When installing an AUR package rebuild and reinstall all of its AUR dependencies recursively, even the ones already installed. This flag allows you to easily rebuild packages against your current system's libraries if they have become incompatible.\n\n**--norebuild**\n\n> When building packages if the package is found in cache and is an equal version to the one wanted skip the package build and use the existing package.\n\n**--mflags <flags>**\n\n> Passes arguments to makepkg. These flags get passed to every instance where makepkg is called by Yay. Arguments are split on whitespace before being passed to makepkg. Multiple arguments may be passed by supplying a space separated list that is quoted by the shell.\n\n**--gpgflags <flags>**\n\n> Passes arguments to gpg. These flags get passed to every instance where gpg is called by Yay. Arguments are split on whitespace before being passed to gpg. Multiple arguments may be passed by supplying a space separated list that is quoted by the shell.\n\n**--sudo <command>**\n\n> The command to use for  **sudo**  calls. This can be a command in  **PATH**  or an absolute path to the file. The sudoloop is not guaranteed to work with a custom  **sudo**  command.\n\n**--sudoflags <flags>**\n\n> Passes arguments to sudo. These flags get passed to every instance where sudo is called by Yay. Arguments are split on whitespace before being passed to sudo. Multiple arguments may be passed by supplying a space separated list that is quoted by the shell.\n\n**--sudoloop**\n\n> Loop sudo calls in the background to prevent sudo from timing out during long builds.\n\n**--nosudoloop**\n\n> Do not loop sudo calls in the background."],["CAVEAT","Like other AUR helpers, yay facilitates installing **user-submitted packages** from the Arch User Repository, which **aren't officially vetted** and could potentially contain malicious code—always review the PKGBUILD file before proceeding with installation to mitigate security risks."],["HISTORY","The yay command, short for \"**Yet Another Yogurt**,\" is an AUR (Arch User Repository) helper tool written in **Go**, designed to simplify installing and managing packages on **Arch Linux** and its derivatives. It was created by developer Jguer and first initiated in **September 2016**."],["INSTALL","```nix: nix profile install nixpkgs#yay```"],["SEE ALSO","[makepkg](/man/makepkg)(8), [pacman](/man/pacman)(8)"],["RESOURCES","```[Source code](https://github.com/Jguer/yay)```\n\n```[Documentation](https://github.com/Jguer/yay/wiki)```"]],"e":["makepkg","pacman"],"x":"yay"},"zfgrep":{"n":"zfgrep","t":"Search gzip-compressed files for fixed strings","s":[["TAGLINE","Search gzip-compressed files for fixed strings"],["TLDR","**Search for literal string in gzipped file**\n\n```zfgrep \"[string]\" [file.gz]```\n\n**Case-insensitive search**\n\n```zfgrep -i \"[string]\" [file.gz]```\n\n**Show line numbers**\n\n```zfgrep -n \"[string]\" [file.gz]```\n\n**Count matching lines**\n\n```zfgrep -c \"[string]\" [file.gz]```\n\n**List files containing matches**\n\n```zfgrep -l \"[string]\" [*.gz]```"],["SYNOPSIS","**zfgrep** [_grep-options_] [**-e**] _pattern_ [_file_...]"],["PARAMETERS","**-i**\n> Case-insensitive matching\n\n**-n**\n> Show line numbers\n\n**-l**\n> List filenames with matches only\n\n**-c**\n> Count matching lines\n\n**-v**\n> Invert match (show non-matching lines)\n\n**-h**\n> Suppress filename in output\n\n**-e** _pattern_\n> Specify pattern\n\n**-x**\n> Match whole lines only"],["DESCRIPTION","**zfgrep** searches for fixed strings in gzip-compressed files without manual decompression. It's equivalent to **zcat file.gz | fgrep pattern** but more convenient.\n\nThe \"f\" indicates fixed string matching (like **grep -F**). Patterns are interpreted literally, not as regular expressions. This is faster than regex matching and useful when searching for strings containing special characters.\n\nzfgrep automatically handles both compressed and uncompressed files, making it safe for mixed file sets.\n\nMultiple patterns can be specified, one per line, using **-e** or by newline-separating them."],["CAVEATS","Only gzip compression is supported. Use **bzfgrep** for bzip2 files or pipe through appropriate decompressors for other formats.\n\nFixed string matching means special characters like **.**,**\\***, **[** are literal. Use **zgrep** or **zegrep** if regex is needed.\n\nProcessing large compressed files requires CPU for decompression."],["INSTALL","```apt: sudo apt install gzip```\n\n```dnf: sudo dnf install gzip```\n\n```pacman: sudo pacman -S gzip```\n\n```apk: sudo apk add gzip```\n\n```zypper: sudo zypper install gzip```\n\n```brew: brew install gzip```\n\n```nix: nix profile install nixpkgs#gzip```"],["SEE ALSO","[zgrep](/man/zgrep)(1), [zegrep](/man/zegrep)(1), [fgrep](/man/fgrep)(1), [zcat](/man/zcat)(1), [gzip](/man/gzip)(1)"]],"e":["zgrep","zegrep","fgrep","zcat","gzip"],"x":"zfgrep \"[string]\" [file.gz]"},"zfs":{"n":"zfs","t":"Manage ZFS filesystems and snapshots","s":[["TAGLINE","Manage ZFS filesystems and snapshots"],["TLDR","**List all datasets**\n\n```zfs list```\n\n**Create a new filesystem**\n\n```sudo zfs create [pool/dataset]```\n\n**Create a snapshot**\n\n```sudo zfs snapshot [pool/dataset@snapshot_name]```\n\n**List snapshots**\n\n```zfs list -t snapshot```\n\n**Rollback to snapshot**\n\n```sudo zfs rollback [pool/dataset@snapshot_name]```\n\n**Set a property**\n\n```sudo zfs set [compression=lz4] [pool/dataset]```\n\n**Get property value**\n\n```zfs get [compression] [pool/dataset]```\n\n**Destroy a dataset**\n\n```sudo zfs destroy [pool/dataset]```"],["SYNOPSIS","**zfs** _command_ [_options_] [_arguments_]"],["SUBCOMMANDS","**list** [**-t** _type_] [**-o** _properties_] [_dataset_]\n> List datasets and properties\n\n**create** [**-p**] _dataset_\n> Create filesystem or volume (-p creates parents)\n\n**destroy** [**-r**] _dataset_\n> Destroy dataset (-r recursive)\n\n**snapshot** _dataset@name_\n> Create snapshot\n\n**rollback** _dataset@snapshot_\n> Rollback to snapshot\n\n**clone** _snapshot_ _dataset_\n> Create clone from snapshot\n\n**send** _snapshot_\n> Generate stream for replication\n\n**receive** _dataset_\n> Receive stream into dataset\n\n**set** _property=value_ _dataset_\n> Set property\n\n**get** _property_ _dataset_\n> Get property value\n\n**mount** _dataset_\n> Mount filesystem\n\n**unmount** _dataset_\n> Unmount filesystem"],["DESCRIPTION","**zfs** manages ZFS filesystems, snapshots, clones, and volumes. ZFS is a combined filesystem and volume manager with advanced features like copy-on-write, snapshots, checksums, and built-in compression.\n\nKey concepts:\n- **Datasets** are filesystems or volumes within a pool\n- **Snapshots** are read-only point-in-time copies\n- **Clones** are writable copies created from snapshots\n- **Properties** control behavior (compression, quota, mountpoint)\n\nZFS uses hierarchical datasets: **pool/parent/child** inherits properties from parent. Properties can be set locally to override inheritance.\n\nCommon properties include **compression**, **quota**, **reservation**, **recordsize**, **atime**, and **mountpoint**."],["CAVEATS","Most operations require root privileges.\n\n**zfs destroy** is irreversible. Snapshots protect against accidental deletion but must be created proactively.\n\nZFS can consume significant memory for caching (ARC). Tune **zfs_arc_max** on memory-constrained systems.\n\nSnapshots are cheap but not free. Many snapshots with high data churn can consume significant space."],["INSTALL","```apt: sudo apt install zfs-fuse```\n\n```apk: sudo apk add zfs```\n\n```nix: nix profile install nixpkgs#zfs```"],["SEE ALSO","[zpool](/man/zpool)(8), [zdb](/man/zdb)(8), [zfs-send](/man/zfs-send)(8), [zfs-receive](/man/zfs-receive)(8)"]],"e":["zpool","zdb","zfs-send","zfs-receive"],"x":"zfs list"},"znew":{"n":"znew","t":"Convert compress (.Z) files to gzip format","s":[["TAGLINE","Convert compress (.Z) files to gzip format"],["TLDR","**Convert .Z file to .gz**\n\n```znew [file.Z]```\n\n**Convert multiple files**\n\n```znew [*.Z]```\n\n**Keep original file**\n\n```znew -K [file.Z]```\n\n**Use best compression**\n\n```znew -9 [file.Z]```\n\n**Force conversion even if .gz exists**\n\n```znew -f [file.Z]```"],["SYNOPSIS","**znew** [**-ftv9PK**] _file.Z_..."],["PARAMETERS","**-f**\n> Force recompression even if .gz file already exists\n\n**-t**\n> Test new file integrity before deleting original\n\n**-v**\n> Verbose output\n\n**-9**\n> Use best compression level\n\n**-P**\n> Use pipes for conversion (slower but uses less disk space)\n\n**-K**\n> Keep the original .Z file"],["DESCRIPTION","**znew** recompresses files from the old compress format (.Z) to gzip format (.gz). The compress utility used LZW compression, while gzip uses DEFLATE which typically achieves better compression ratios.\n\nThe tool is useful for converting legacy compressed archives to the more modern and efficient gzip format. After conversion, the original .Z file is deleted by default.\n\nznew verifies the integrity of the new .gz file before removing the original when using the **-t** flag, providing safety against corruption."],["HISTORY","The compress utility and .Z format date from the 1980s. gzip was created in 1992 as a free alternative that avoided LZW patent issues. znew was created to facilitate migration from the older format."],["CAVEATS","Only converts .Z files to .gz. Does not handle other compression formats.\n\nWithout **-K**, the original file is deleted after successful conversion.\n\nThe **-P** option uses more CPU but less temporary disk space.\n\nModern systems rarely encounter .Z files, making this tool primarily useful for archival work."],["INSTALL","```apt: sudo apt install gzip```\n\n```dnf: sudo dnf install gzip```\n\n```pacman: sudo pacman -S gzip```\n\n```apk: sudo apk add gzip```\n\n```zypper: sudo zypper install gzip```\n\n```brew: brew install gzip```\n\n```nix: nix profile install nixpkgs#gzip```"],["SEE ALSO","[gzip](/man/gzip)(1), [gunzip](/man/gunzip)(1), [zcat](/man/zcat)(1), [compress](/man/compress)(1)"]],"e":["gzip","gunzip","zcat","compress"],"x":"znew [file.Z]"}});
