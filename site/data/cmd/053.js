window.LCL_BUCKET(53,{"7za":{"n":"7za","t":"standalone file archiver from the 7-Zip family","s":[["TAGLINE","standalone file archiver from the 7-Zip family"],["TLDR","**Create archive**\n\n```7za a [archive.7z] [files...]```\n\n**Extract archive**\n\n```7za x [archive.7z]```\n\n**List archive contents**\n\n```7za l [archive.7z]```\n\n**Create zip archive**\n\n```7za a -tzip [archive.zip] [files]```\n\n**Extract with password**\n\n```7za x -p[password] [archive.7z]```"],["SYNOPSIS","**7za** _command_ [_switches_] _archive_ [_files_...]"],["COMMANDS","**a**\n> Add files to archive\n\n**x**\n> Extract with full paths\n\n**e**\n> Extract (flat, no paths)\n\n**l**\n> List archive contents\n\n**t**\n> Test archive integrity\n\n**d**\n> Delete from archive\n\n**u**\n> Update archive"],["PARAMETERS","**-o**_dir_\n> Output directory\n\n**-p**_password_\n> Set password\n\n**-mx=**_n_\n> Compression level (0-9)\n\n**-t**_type_\n> Archive type (7z, zip, gzip, bzip2, xz, tar)\n\n**-r**\n> Recurse subdirectories\n\n**-y**\n> Assume yes to queries\n\n**-si**\n> Read from stdin\n\n**-so**\n> Write to stdout\n\n**-mhe=on**\n> Encrypt archive headers (7z format)"],["DESCRIPTION","**7za** is the standalone version of the 7-Zip command-line tool. Unlike **7z** which may use external plugins, 7za includes all codecs in a single executable.\n\n7za supports fewer formats than the full 7z but is more portable as a single binary. It handles 7z, ZIP, GZIP, BZIP2, XZ, TAR, and CAB formats.\n\nThe command syntax is identical to **7z**. For scripts that only need common formats, 7za is often preferred for its simplicity and reliability."],["CAVEATS","7za supports fewer formats than 7z. For RAR, CAB, or ISO extraction, use the full 7z command.\n\nThe \"a\" in 7za stands for \"alone\" (standalone), not \"alpha\" or \"advanced\".\n\nPerformance is identical to 7z for supported formats."],["INSTALL","```apt: sudo apt install 7zip```\n\n```dnf: sudo dnf install 7zip```\n\n```pacman: sudo pacman -S 7zip```\n\n```zypper: sudo zypper install 7zip```\n\n```brew: brew install 7zip```"],["SEE ALSO","[7z](/man/7z)(1), [7zr](/man/7zr)(1), [tar](/man/tar)(1), [gzip](/man/gzip)(1)"]],"e":["7z","7zr","tar","gzip"],"x":"7za a [archive.7z] [files...]"},"airpaste":{"n":"airpaste","t":"Pipe data between two machines on the local network using mDNS discovery","s":[["TAGLINE","Pipe data between two machines on the local network using mDNS discovery"],["TLDR","**Wait** for a peer to connect (prints received data to stdout)\n\n```airpaste```\n\nSend **text** to the waiting peer\n\n```echo \"Hello\" | airpaste```\n\nSend a **file**\n\n```airpaste < [file.txt]```\n\nReceive and **save** to file\n\n```airpaste > [received.txt]```\n\nUse a **named channel** so only peers with the same name connect\n\n```airpaste [channel_name]```"],["SYNOPSIS","**airpaste** [_name_]"],["DESCRIPTION","**airpaste** is a 1-to-1 network pipe that auto-discovers another peer on the same local network using mDNS (multicast DNS), so you do not need to know the other machine's IP address. Data is transferred over TCP.\n\nRun **airpaste** on two machines: whatever you pipe into one is streamed to the other. Pass an optional channel name so that only peers using the same name connect to each other. It works across different operating systems and requires no server setup."],["PARAMETERS","_name_\n> Optional channel name; only peers using the same name connect to each other"],["CAVEATS","Only works on local networks where mDNS multicast is reachable. Data moves over the network without encryption and could be captured, so it is intended for use on trusted networks only. It connects two peers at a time, not a broadcast to many."],["HISTORY","**airpaste** was created by Mathias Buus (mafintosh) as a simple way to pipe data and files between machines during development, leveraging mDNS for zero-configuration networking. It is written in JavaScript and distributed via npm."],["SEE ALSO","[nc](/man/nc)(1), [wormhole](/man/wormhole)(1), [croc](/man/croc)(1)"],["RESOURCES","```[Source code](https://github.com/mafintosh/airpaste)```\n\n```[Homepage](https://github.com/mafintosh/airpaste)```"]],"e":["nc","wormhole","croc"],"x":"airpaste"},"autoexpect":{"n":"autoexpect","t":"Generate Expect scripts by recording interactive sessions","s":[["TAGLINE","Generate Expect scripts by recording interactive sessions"],["TLDR","Spawn a shell and **generate a script** from the session\n\n```autoexpect```\n\nRun a command and **generate a script** from it\n\n```autoexpect [command]```\n\nGenerate a script and **save to a specific file**\n\n```autoexpect -f [path/to/file] [command]```\n\n**Use prompt mode** to match only the last line of output\n\n```autoexpect -p [command]```\n\n**Use conservative mode** with slight delays between characters\n\n```autoexpect -c [command]```"],["SYNOPSIS","**autoexpect** [_options_] [_command_...]"],["DESCRIPTION","**autoexpect** watches an interactive session and generates an Expect script that can replay the session automatically. It records user input and program responses, creating a script suitable for automating repetitive interactive tasks.\n\nThe generated script is saved as **script.exp** by default. This script can be modified and reused to automate the recorded interaction."],["PARAMETERS","**-f** _file_\n> Save the generated script to the specified file instead of script.exp.\n\n**-p**\n> Enable prompt mode. Autoexpect only matches the last line of program output (usually the prompt), making scripts less sensitive to output changes.\n\n**-P** _key_\n> Define a key to toggle prompt mode on and off during the session.\n\n**-c**\n> Enable conservative mode. Autoexpect pauses briefly (one tenth of a second) before sending each character, improving reliability on slow systems.\n\n**-C** _key_\n> Define a key to toggle conservative mode on and off during the session.\n\n**-Q** _key_\n> Define a quote character to enter characters that autoexpect would otherwise consume as toggle keys.\n\n**-quiet**\n> Disable informational messages produced by autoexpect."],["CAVEATS","Generated scripts may need manual editing for robustness. Passwords and sensitive data entered during recording are captured in the script. The timing of interactions may need adjustment for reliable playback."],["HISTORY","**autoexpect** is part of the Expect package, created by Don Libes at NIST. Expect was first released in **1990** and has been widely used for automating interactive applications."],["INSTALL","```apt: sudo apt install expect```\n\n```dnf: sudo dnf install expect```\n\n```pacman: sudo pacman -S expect```\n\n```apk: sudo apk add expect```\n\n```zypper: sudo zypper install expect```\n\n```brew: brew install expect```\n\n```nix: nix profile install nixpkgs#expect```"],["SEE ALSO","[expect](/man/expect)(1), [ssh](/man/ssh)(1)"]],"e":["expect","ssh"],"x":"autoexpect"},"bcachefs":{"n":"bcachefs","t":"Next-generation copy-on-write filesystem tools","s":[["TAGLINE","Next-generation copy-on-write filesystem tools"],["TLDR","**Format** a partition with bcachefs\n\n```sudo bcachefs format [path/to/partition]```\n\n**Mount** a bcachefs filesystem\n\n```sudo bcachefs mount [path/to/partition] [path/to/mountpoint]```\n\nCreate a **RAID with SSD cache** and HDD storage\n\n```sudo bcachefs format -l ssd.ssd1 [path/to/ssd] -l hdd.hdd1 [path/to/hdd] --replicas 1 --foreground_target ssd --promote_target ssd --background_target hdd```\n\nMount a **multidevice filesystem**\n\n```sudo bcachefs mount [path/to/partition1]:[path/to/partition2] [path/to/mountpoint]```\n\nDisplay **disk usage**\n\n```bcachefs fs usage -h [path/to/mountpoint]```\n\nSet **replicas** after formatting\n\n```sudo bcachefs set-fs-option --metadata_replicas [2] --data_replicas [2] [path/to/partition]```\n\nForce **rereplicate** all files\n\n```sudo bcachefs data rereplicate [path/to/mountpoint]```\n\nCreate a **snapshot** of a directory\n\n```bcachefs subvolume snapshot [path/to/directory] [path/to/snapshot]```"],["SYNOPSIS","**bcachefs** _command_ [_options_] [_arguments_]"],["DESCRIPTION","**bcachefs** is a next-generation copy-on-write filesystem for Linux that combines features of btrfs and ZFS with the performance characteristics of bcache. It provides checksumming, compression, encryption, snapshots, and multi-device support.\n\nThe filesystem supports tiered storage with automatic data movement between fast (SSD) and slow (HDD) devices, making it suitable for hybrid storage configurations."],["SUBCOMMANDS","**format**\n> Create a new bcachefs filesystem.\n\n**mount**\n> Mount a bcachefs filesystem.\n\n**fsck**\n> Check and repair a filesystem.\n\n**fs usage**\n> Display filesystem disk usage.\n\n**device**\n> Manage devices in a filesystem (add, remove, evacuate, set state).\n\n**subvolume**\n> Manage subvolumes and snapshots.\n\n**data rereplicate**\n> Rewrite data so all extents meet their replication requirements.\n\n**set-fs-option**\n> Change filesystem options on a mounted or offline filesystem.\n\n**show-super**\n> Print the contents of a filesystem superblock.\n\n**unlock**\n> Unlock an encrypted filesystem before mounting."],["CAVEATS","bcachefs was merged into the mainline Linux kernel in version **6.7** (released January 2024). Older kernels require building the external module. The filesystem is still under active development; keep backups of important data. The **bcachefs** userspace tools must match the on-disk format expected by your kernel."],["HISTORY","bcachefs was developed by Kent Overstreet, who also created bcache. Development began around **2015** as an evolution of bcache into a full-fledged filesystem, and it was merged into mainline Linux in kernel **6.7** in early **2024**."],["INSTALL","```dnf: sudo dnf install bcachefs-tools```\n\n```pacman: sudo pacman -S bcachefs-tools```\n\n```apk: sudo apk add bcachefs-tools```\n\n```nix: nix profile install nixpkgs#bcachefs-tools```"],["SEE ALSO","[bcachefs-device](/man/bcachefs-device)(8), [mkfs](/man/mkfs)(8), [fsck](/man/fsck)(8), [mount](/man/mount)(8), [btrfs](/man/btrfs)(8)"],["RESOURCES","```[Homepage](https://bcachefs.org)```\n\n```[Source code](https://evilpiepirate.org/git/bcachefs-tools.git)```\n\n```[Documentation](https://bcachefs-docs.readthedocs.io/en/latest/)```"]],"e":["bcachefs-device","mkfs","fsck","mount","btrfs"],"x":"sudo bcachefs format [path/to/partition]"},"bettercap":{"n":"bettercap","t":"Network attack and monitoring framework","s":[["TAGLINE","Network attack and monitoring framework"],["TLDR","**Start** interactive session\n\n```bettercap -iface [eth0]```\n\n**Run** caplet script\n\n```bettercap -caplet [script.cap]```\n\n**ARP spoofing**\n\n```bettercap -iface [eth0] -eval [\"set arp.spoof.targets 192.168.1.10; arp.spoof on\"]```\n\n**Start** web UI\n\n```bettercap -caplet http-ui```"],["SYNOPSIS","**bettercap** [_options_]"],["DESCRIPTION","**bettercap** is the Swiss Army knife for WiFi, Bluetooth Low Energy, wireless HID hijacking, CAN-bus, and IPv4/IPv6 network reconnaissance and MITM attacks. Written in Go, it provides man-in-the-middle attacks, network reconnaissance, packet sniffing, credential harvesting, and a REST API and web UI through a modular architecture.\n\nThe tool is designed for security research, penetration testing, and network analysis. It must be run as root."],["PARAMETERS","**-iface** _name_\n> Network interface to bind to; if empty the default interface is auto-selected\n\n**-caplet** _file_\n> Read commands from this file and run them in the interactive session\n\n**-eval** _code_\n> Run one or more commands (separated by ;) in the interactive session\n\n**-autostart** _modules_\n> Comma-separated list of modules to auto-start\n\n**-script** _file_\n> Load a session script\n\n**-gateway-override** _ip_\n> Use the provided IP address instead of the detected default gateway\n\n**-env-file** _file_\n> Load environment variables from this file\n\n**-caplets-path** _dir_\n> Alternative base path for caplets\n\n**-no-history**\n> Disable the interactive session history file\n\n**-no-colors**\n> Disable output color effects\n\n**-debug**\n> Print debug messages\n\n**-silent**\n> Suppress all logs which are not errors\n\n**-version**\n> Print the version and exit"],["MODULES","**net.recon**\n> Network reconnaissance\n\n**net.probe**\n> Active host discovery\n\n**arp.spoof**\n> ARP spoofing attacks\n\n**dns.spoof**\n> DNS spoofing\n\n**http.proxy**\n> HTTP transparent proxy\n\n**https.proxy**\n> HTTPS transparent proxy\n\n**wifi**\n> WiFi attacks and monitoring\n\n**ble**\n> Bluetooth Low Energy attacks\n\n**packet.proxy**\n> Raw packet manipulation"],["INTERACTIVE COMMANDS","```"],["Enable ARP spoofing","> set arp.spoof.targets 192.168.1.0/24\n> arp.spoof on"],["HTTP proxy with SSL stripping","> set http.proxy.sslstrip true\n> http.proxy on"],["Network scan","> net.probe on\n> net.show\n```"],["CAPLETS","Scripts for automation:\n```"],["quick-recon.cap","net.probe on\nsleep 10\nnet.show\n```"],["CAVEATS","Requires root privileges. Illegal without authorization. Can disrupt network traffic. Detection by IDS/IPS systems. WiFi attacks require monitor mode capable adapter. Use only in authorized security assessments."],["HISTORY","**bettercap** was created by Simone Margaritelli (evilsocket) in **2016** as a modern replacement for ettercap, rewritten in Go for better performance."],["INSTALL","```apt: sudo apt install bettercap```\n\n```pacman: sudo pacman -S bettercap```\n\n```zypper: sudo zypper install bettercap```\n\n```brew: brew install bettercap```\n\n```nix: nix profile install nixpkgs#bettercap```"],["SEE ALSO","[ettercap](/man/ettercap)(8), [arpspoof](/man/arpspoof)(8), [nmap](/man/nmap)(1), [aircrack-ng](/man/aircrack-ng)(1), [wireshark](/man/wireshark)(1)"],["RESOURCES","```[Source code](https://github.com/bettercap/bettercap)```\n\n```[Homepage](https://www.bettercap.org/)```"]],"e":["ettercap","arpspoof","nmap","aircrack-ng","wireshark"],"x":"bettercap -iface [eth0]"},"bombadillo":{"n":"bombadillo","t":"Terminal browser for Gopher, Gemini, and Finger protocols","s":[["TAGLINE","Terminal browser for Gopher, Gemini, and Finger protocols"],["TLDR","**Launch** Bombadillo\n\n```bombadillo```\n\n**Open** a specific Gopher URL\n\n```bombadillo [gopher://example.com]```\n\n**Open** a Gemini URL\n\n```bombadillo [gemini://example.com]```"],["SYNOPSIS","**bombadillo** [_options_] [_url_]"],["DESCRIPTION","**bombadillo** is a non-web browser designed for the terminal. It supports the **Gopher**, **Gemini**, and **Finger** protocols, as well as local file browsing. It provides a vim-like modal interface with command-line navigation for exploring the \"small internet\" — alternative protocols to the modern web.\n\nThe browser renders text content directly in the terminal, supports bookmarks, history navigation, and configurable settings. It can handle TLS connections for Gemini and provides a distraction-free reading experience."],["PARAMETERS","**-v**\n> Display version information and exit\n\n**-t**\n> Set the terminal window title to \"Bombadillo\"\n\n**-h**\n> Display usage help and exit"],["CONFIGURATION","Configuration is stored in **~/.config/bombadillo/config.ini**. Settings include default protocol, home URL, certificate handling, and display preferences. Settings can also be changed at runtime using the **:set** command."],["CAVEATS","Does not support HTTP/HTTPS natively — it is specifically designed for alternative protocols, though HTTP/HTTPS can be opened via a configured external handler. Gemini TLS certificate handling uses trust-on-first-use (TOFU). Media files are passed to external handlers. Navigation uses vim-like keybindings: `j`/`k` scroll, `b` goes back, `f` goes forward, `g`/`G` jump to top/bottom, and `q` quits."],["HISTORY","**Bombadillo** was created by **sloum** (on tildegit) and first released around **2019**. Written in **Go**, it was developed as part of the growing interest in the Gemini protocol and the small-internet movement. The name references the character Tom Bombadil from Tolkien's works."],["INSTALL","```apt: sudo apt install bombadillo```\n\n```apk: sudo apk add bombadillo```\n\n```brew: brew install bombadillo```\n\n```nix: nix profile install nixpkgs#bombadillo```"],["SEE ALSO","[lynx](/man/lynx)(1), [w3m](/man/w3m)(1)"]],"e":["lynx","w3m"],"x":"bombadillo"},"btrfs-rescue":{"n":"btrfs-rescue","t":"Recovery tools for damaged btrfs filesystems","s":[["TAGLINE","Recovery tools for damaged btrfs filesystems"],["TLDR","**Rebuild** the filesystem metadata tree (very slow)\n\n```sudo btrfs rescue chunk-recover [path/to/partition]```\n\nFix **device size alignment** problems\n\n```sudo btrfs rescue fix-device-size [path/to/partition]```\n\n**Recover corrupted superblock** from copies\n\n```sudo btrfs rescue super-recover [path/to/partition]```\n\nRecover from **interrupted transactions**\n\n```sudo btrfs rescue zero-log [path/to/partition]```\n\nCreate the **btrfs control device**\n\n```sudo btrfs rescue create-control-device```"],["SYNOPSIS","**btrfs rescue** _subcommand_ [_options_] _device_"],["DESCRIPTION","**btrfs rescue** provides recovery tools for damaged btrfs filesystems. These are last-resort commands when normal mounting or btrfs check fails.\n\nEach subcommand addresses specific failure modes like corrupted metadata, superblock damage, or interrupted transactions. The filesystem must be unmounted for all operations."],["SUBCOMMANDS","**chunk-recover**\n> Scan and rebuild chunk tree (very slow, hours to days)\n\n**fix-device-size**\n> Fix device size mismatches preventing mount\n\n**super-recover**\n> Restore superblock from backup copies\n\n**zero-log**\n> Clear the transaction log to fix log replay failures\n\n**create-control-device**\n> Create /dev/btrfs-control when mknod unavailable"],["CAVEATS","These are emergency recovery tools. Always backup any recoverable data first. chunk-recover can take extremely long on large filesystems. Some operations may cause data loss. The filesystem must be unmounted."],["INSTALL","```apt: sudo apt install btrfs-progs```\n\n```dnf: sudo dnf install btrfs-progs```\n\n```pacman: sudo pacman -S btrfs-progs```\n\n```apk: sudo apk add btrfs-progs```\n\n```brew: brew install btrfs-progs```\n\n```nix: nix profile install nixpkgs#btrfs-progs```"],["SEE ALSO","[btrfs](/man/btrfs)(8), [btrfs-check](/man/btrfs-check)(8), [btrfs-restore](/man/btrfs-restore)(8)"],["RESOURCES","```[Source code](https://github.com/kdave/btrfs-progs)```\n\n```[Documentation](https://btrfs.readthedocs.io/)```"]],"e":["btrfs","btrfs-check","btrfs-restore"],"x":"sudo btrfs rescue chunk-recover [path/to/partition]"},"cargo-watch":{"n":"cargo-watch","t":"Watch source files and run Cargo commands on changes","s":[["TAGLINE","Watch source files and run Cargo commands on changes"],["TLDR","**Watch and rebuild** on changes\n\n```cargo watch```\n\n**Watch and run tests**\n\n```cargo watch -x test```\n\n**Watch and run a specific example**\n\n```cargo watch -x \"run --example [example_name]\"```\n\n**Watch and run multiple commands**\n\n```cargo watch -x check -x test -x run```\n\n**Watch with clear screen** before each run\n\n```cargo watch -c```\n\n**Watch specific files or directories**\n\n```cargo watch -w [src/] -w [tests/]```\n\n**Ignore specific patterns**\n\n```cargo watch -i \"*.txt\" -i \"target/\"```\n\n**Watch with shell command**\n\n```cargo watch -s \"echo 'Changed!' && cargo build\"```"],["SYNOPSIS","**cargo watch** [_options_] [**-x** _command_]..."],["DESCRIPTION","**cargo-watch** is a Cargo subcommand that watches project source files and runs Cargo commands when files change. It provides a convenient development workflow for continuous compilation, testing, or running.\n\nBy default, cargo watch runs `cargo check` on changes. Multiple commands can be chained with `-x` flags and will run in sequence. The tool debounces rapid file changes to avoid excessive rebuilds.\n\nIt watches all files that Cargo considers part of the project, including src/, tests/, benches/, examples/, and Cargo.toml. Custom watch paths and ignore patterns can be specified."],["PARAMETERS","**-x** _command_\n> Cargo command to run (default: check).\n\n**-s** _command_\n> Shell command to run.\n\n**-c**, **--clear**\n> Clear screen before each run.\n\n**-w** _path_\n> Watch specific path (can repeat).\n\n**-i** _pattern_\n> Ignore files matching pattern.\n\n**-d** _delay_\n> Debounce delay in seconds.\n\n**--poll**\n> Use polling instead of events.\n\n**--postpone**\n> Postpone first run until change.\n\n**-q**, **--quiet**\n> Suppress output from watch itself.\n\n**--no-gitignore**\n> Don't use .gitignore patterns.\n\n**--why**\n> Show which file triggered the run.\n\n**-B** _cmd_\n> Run command before watched command.\n\n**-N**\n> Send desktop notification on finish."],["CONFIGURATION","**.ignore**\n> Project-level ignore patterns for watched files (uses gitignore syntax)."],["CAVEATS","Requires installation via `cargo install cargo-watch`. File system events may not work in all environments (use --poll as fallback). Rapid consecutive saves may be debounced into single runs. Large projects may benefit from increased debounce delay."],["HISTORY","**cargo-watch** was created by **Félix Saparelli** (passcod) in **2015** to provide file watching functionality for Rust development. It built upon the notify crate for cross-platform file system events. The tool has become a standard part of many Rust developers' workflows, inspired by similar tools in other ecosystems like nodemon for Node.js."],["INSTALL","```pacman: sudo pacman -S cargo-watch```\n\n```apk: sudo apk add cargo-watch```\n\n```brew: brew install cargo-watch```\n\n```nix: nix profile install nixpkgs#cargo-watch```"],["SEE ALSO","[cargo](/man/cargo)(1), [cargo-check](/man/cargo-check)(1), [watchexec](/man/watchexec)(1), [entr](/man/entr)(1)"]],"e":["cargo","cargo-check","watchexec","entr"],"x":"cargo watch"},"clonezilla":{"n":"clonezilla","t":"disk cloning and imaging solution","s":[["TAGLINE","disk cloning and imaging solution"],["TLDR","**Boot Clonezilla and start interactive mode**\n\n```clonezilla```\n\n**Save disk to image**\n\n```ocs-sr -q2 -c -j2 -z1p -i 2000 -fsck-y -senc -p reboot savedisk [image_name] [sda]```\n\n**Restore image to disk**\n\n```ocs-sr -g auto -e1 auto -e2 -r -j2 -c -p reboot restoredisk [image_name] [sda]```\n\n**Clone disk to disk**\n\n```ocs-onthefly -g auto -e1 auto -e2 -r -j2 -sfsck -k -p reboot -f [sda] -d [sdb]```\n\n**Save partition to image**\n\n```ocs-sr -q2 -c -j2 -z1p -i 2000 -fsck-y -senc -p reboot saveparts [image_name] [sda1]```\n\n**List available images**\n\n```ocs-sr -l```"],["SYNOPSIS","**ocs-sr** [_options_] _action_ _image_name_ _device_"],["PARAMETERS","**savedisk** _name_ _disk_\n> Save entire disk to image.\n\n**restoredisk** _name_ _disk_\n> Restore image to disk.\n\n**saveparts** _name_ _parts_\n> Save partitions to image.\n\n**restoreparts** _name_ _parts_\n> Restore partitions from image.\n\n**-q2**\n> Use partclone for imaging.\n\n**-c**\n> Confirm before action.\n\n**-j2**\n> Clone hidden data between MBR and partition.\n\n**-z1p**\n> Use parallel gzip compression.\n\n**-i** _size_\n> Split image into files of given MB.\n\n**-fsck-y**\n> Run fsck automatically.\n\n**-senc**\n> Encrypt the image.\n\n**-g auto**\n> Reinstall grub automatically.\n\n**-p** _action_\n> Post-action: reboot, poweroff, or choose."],["DESCRIPTION","**Clonezilla** is a disk cloning and imaging solution similar to Norton Ghost. It creates backup images of entire disks or partitions and can restore them to the same or different hardware.\n\nClonezilla boots from USB or CD into a Linux environment with specialized imaging tools. It supports imaging filesystems (ext4, NTFS, HFS+, etc.) efficiently by copying only used blocks. Raw mode supports any filesystem.\n\nTwo modes are available: **Clonezilla Live** for imaging single machines, and **Clonezilla SE (Server Edition)** for mass deployment via network using PXE boot."],["SUPPORTED FILESYSTEMS","**Linux**: ext2/3/4, XFS, Btrfs, JFS, ReiserFS\n**Windows**: NTFS, FAT12/16/32, exFAT\n**macOS**: HFS+, APFS (limited)\n**Others**: UFS, VMFS, minix"],["CAVEATS","Target disk/partition must be equal or larger than source for differential imaging. Encrypted partitions may require raw mode (larger images). UEFI and Secure Boot may require configuration. Network deployment requires DHCP and TFTP infrastructure."],["HISTORY","Clonezilla was created by **Steven Shiau** and the DRBL (Diskless Remote Boot in Linux) project at the **National Center for High-Performance Computing** in Taiwan, first released in **2004**. Built on Partclone, Partimage, and other open-source tools, it became a popular free alternative to commercial disk imaging solutions. Both Live and Server editions remain actively developed."],["INSTALL","```pacman: sudo pacman -S clonezilla```"],["SEE ALSO","[dd](/man/dd)(1), [partclone](/man/partclone)(8), [rsync](/man/rsync)(1), [ddrescue](/man/ddrescue)(1)"]],"e":["dd","partclone","rsync","ddrescue"],"x":"clonezilla"},"ctrlaltdel":{"n":"ctrlaltdel","t":"control Ctrl+Alt+Del key combination behavior","s":[["TAGLINE","control Ctrl+Alt+Del key combination behavior"],["TLDR","**Get** current setting\n\n```ctrlaltdel```\n\nSet Ctrl+Alt+Del to reboot **immediately** (hard)\n\n```sudo ctrlaltdel hard```\n\nSet Ctrl+Alt+Del to reboot **gracefully** (soft)\n\n```sudo ctrlaltdel soft```"],["SYNOPSIS","**ctrlaltdel** [_hard_|_soft_]"],["DESCRIPTION","**ctrlaltdel** controls the kernel's behavior when Ctrl+Alt+Del is pressed on the console. The setting determines whether the system reboots immediately or allows processes to shut down gracefully.\n\nWith \"hard\", the system reboots immediately. With \"soft\", SIGINT is sent to PID 1 (init/systemd), which handles a graceful shutdown."],["PARAMETERS","**hard**\n> Reboot immediately without preparation\n\n**soft**\n> Send SIGINT to init for graceful reboot"],["CAVEATS","Requires root privileges to change the setting. Part of util-linux. On modern systemd systems, systemd handles the Ctrl+Alt+Del signal regardless of this setting."],["HISTORY","This control exists from early Unix days when physical console access was common. The three-key combination was chosen to be difficult to press accidentally."],["INSTALL","```apt: sudo apt install util-linux-extra```\n\n```apk: sudo apk add util-linux-misc```"],["SEE ALSO","[reboot](/man/reboot)(8), [shutdown](/man/shutdown)(8), [systemctl](/man/systemctl)(1)"],["RESOURCES","```[Source code](https://github.com/util-linux/util-linux)```"]],"e":["reboot","shutdown","systemctl"],"x":"ctrlaltdel"},"doas":{"n":"doas","t":"minimal privilege escalation tool","s":[["TAGLINE","minimal privilege escalation tool"],["TLDR","**Run a command as root**\n\n```doas [command]```\n\n**Run a command as another user**\n\n```doas -u [username] [command]```\n\n**Start a root shell**\n\n```doas -s```\n\n**Start a shell as another user**\n\n```doas -u [username] -s```\n\n**Run without requiring password** (if configured)\n\n```doas -n [command]```\n\n**Check if doas.conf** allows the command\n\n```doas -C [/etc/doas.conf] [command]```\n\n**Clear cached credentials**\n\n```doas -L```"],["SYNOPSIS","**doas** [_-nLs_] [_-a style_] [_-C config_] [_-u user_] _command_ [_args_]"],["PARAMETERS","**-n**\n> Non-interactive mode; fail if password is required.\n\n**-L**\n> Clear any persisted authentication credentials.\n\n**-s**\n> Execute the shell from SHELL environment variable or from /etc/passwd.\n\n**-u** _user_\n> Run the command as the specified user (default: root).\n\n**-a** _style_\n> Use the specified authentication style.\n\n**-C** _config_\n> Parse and check the specified config file and exit.\n\n_command_\n> The command to execute with elevated privileges."],["DESCRIPTION","**doas** (dedicated openbsd application subexecutor) is a minimal program for running commands as another user, typically root. It originated in OpenBSD as a simpler, more secure alternative to sudo with a significantly smaller codebase and attack surface.\n\nConfiguration is done through **/etc/doas.conf** with simple rules specifying who can run what. A basic rule might be: **permit persist :wheel** to allow members of the wheel group to execute commands as root with credential caching.\n\nThe configuration syntax is straightforward: each line is a rule with **permit** or **deny**, optional keywords like **nopass** (no password), **persist** (cache credentials), **keepenv** (preserve environment), and specifications for user, group, and allowed commands.\n\nUnlike sudo's extensive feature set, doas focuses on the core use case of privilege escalation with minimal complexity. This makes it easier to audit and reduces potential security vulnerabilities. Many Linux distributions now package doas as a sudo alternative."],["CONFIGURATION","**/etc/doas.conf**\n> Main configuration file specifying authorization rules. Each line contains a rule with permit/deny, optional keywords (nopass, persist, keepenv), and user/group/command specifications."],["CAVEATS","Configuration syntax differs from sudo; migration requires rewriting rules. Fewer features than sudo (no fine-grained command logging, no complex per-command options). Some software may expect sudo specifically. Must be configured before use (no default permissive configuration)."],["HISTORY","**doas** was created by Ted Unangst and introduced in **OpenBSD 5.8** in **2015** as a replacement for sudo in the base system. The design goal was to provide necessary functionality with minimal code (around 1000 lines versus sudo's 100,000+). It has been ported to Linux and other systems, gaining popularity among security-conscious users."],["INSTALL","```apt: sudo apt install opendoas```\n\n```dnf: sudo dnf install opendoas```\n\n```pacman: sudo pacman -S opendoas```\n\n```apk: sudo apk add doas```\n\n```nix: nix profile install nixpkgs#doas```"],["SEE ALSO","[sudo](/man/sudo)(8), [su](/man/su)(1)"]],"e":["sudo","su"],"x":"doas [command]"},"docker-container-stop":{"n":"docker-container-stop","t":"Stop one or more running Docker containers","s":[["TAGLINE","Stop one or more running Docker containers"],["TLDR","**Stop a container** gracefully (SIGTERM then SIGKILL after timeout)\n\n```docker container stop mycontainer```\n\n**Stop multiple containers**\n\n```docker container stop container1 container2```\n\n**Stop with a custom timeout** (seconds before SIGKILL)\n\n```docker container stop --timeout 30 mycontainer```\n\n**Send a specific stop signal**\n\n```docker container stop --signal SIGINT mycontainer```"],["SYNOPSIS","**docker** **container** **stop** [_options_] _container_ [_container_...]"],["DESCRIPTION","**docker container stop** stops one or more running containers. It sends `SIGTERM` (or the signal specified by `--signal` / the container's `STOPSIGNAL`) to the main process inside the container. If the process does not exit within the timeout (default 10 seconds on Linux, 30 on Windows unless configured with `--stop-timeout` at create/run time), `SIGKILL` is sent.\n\nThis is the preferred way to stop containers cleanly. The alias `docker stop` is equivalent."],["PARAMETERS","**-t**, **--timeout** _seconds_\n> Seconds to wait for the container to stop after sending the stop signal before forcibly killing it with `SIGKILL`. Set to `-1` to wait indefinitely. Default: daemon default (10s Linux / 30s Windows) or the container's configured stop timeout.\n\n**-s**, **--signal** _signal_\n> Signal to send to the container (e.g. `SIGTERM`, `SIGINT`, `SIGKILL`, or a number). Default is the container's configured stop signal or `SIGTERM`."],["CAVEATS","If the main process ignores SIGTERM, the container may not stop until the timeout triggers SIGKILL. Stopped containers remain on disk and can be restarted with `docker start` / `docker container start`, or removed with `docker rm`."],["INSTALL","```apt: sudo apt install docker-cli```\n\n```dnf: sudo dnf install docker-cli```\n\n```pacman: sudo pacman -S docker```\n\n```apk: sudo apk add docker-cli```\n\n```zypper: sudo zypper install docker```\n\n```brew: brew install docker```\n\n```nix: nix profile install nixpkgs#docker```"],["SEE ALSO","[docker-stop](/man/docker-stop)(1), [docker-start](/man/docker-start)(1), [docker-container-start](/man/docker-container-start)(1), [docker-rm](/man/docker-rm)(1), [docker-ps](/man/docker-ps)(1)"],["RESOURCES","```[Documentation](https://docs.docker.com/reference/cli/docker/container/stop/)```"]],"e":["docker-stop","docker-start","docker-container-start","docker-rm","docker-ps"],"x":"docker container stop mycontainer"},"docker-container-top":{"n":"docker-container-top","t":"display running processes inside a container","s":[["TAGLINE","display running processes inside a container"],["TLDR","**Show running processes** in a container\n\n```docker container top [container]```\n\n**Show with custom ps options**\n\n```docker container top [container] aux```"],["SYNOPSIS","**docker** **container** **top** _container_ [_ps options_]"],["DESCRIPTION","**docker container top** displays the running processes inside a container, similar to the Unix **ps** command but scoped to a specific container's process namespace. This provides visibility into what is actually executing within the container at any given moment.\n\nThe command accepts standard **ps** options to customize the output format and control which process attributes are displayed. This is particularly useful for debugging containers, verifying that expected processes are running, and investigating resource usage at the process level."],["INSTALL","```apt: sudo apt install docker-cli```\n\n```dnf: sudo dnf install docker-cli```\n\n```pacman: sudo pacman -S docker```\n\n```apk: sudo apk add docker-cli```\n\n```zypper: sudo zypper install docker```\n\n```brew: brew install docker```\n\n```nix: nix profile install nixpkgs#docker```"],["SEE ALSO","[docker-top](/man/docker-top)(1), [docker-container-stats](/man/docker-container-stats)(1)"],["RESOURCES","```[Source code](https://github.com/docker/cli)```\n\n```[Documentation](https://docs.docker.com/reference/cli/docker/)```"]],"e":["docker-top","docker-container-stats"],"x":"docker container top [container]"},"droid":{"n":"droid","t":"Factory.ai terminal coding agent","s":[["TAGLINE","Factory.ai terminal coding agent"],["TLDR","**Start an interactive session**\n\n```droid```\n\n**Execute a single prompt** non-interactively (read-only by default)\n\n```droid exec \"[analyze the auth system and propose a plan]\"```\n\n**Allow safe file edits**\n\n```droid exec --auto low \"[add JSDoc comments to all functions]\"```\n\n**Full autonomy**, including commits and pushes\n\n```droid exec --auto high \"[fix the bug, run tests, commit and push]\"```\n\n**Read the prompt from a file**\n\n```droid exec -f [prompt.txt]```\n\n**Get JSON output** for scripting\n\n```droid exec -o json \"[your prompt]\"```\n\n**Continue an existing session**\n\n```droid exec -s [session_id] \"[keep going]\"```\n\n**Work in an isolated git worktree**\n\n```droid exec --worktree \"[refactor the parser]\"```"],["SYNOPSIS","**droid** [_options_]\n\n**droid** **exec** [_options_] [_prompt_]\n\n**droid** _subcommand_ [_options_]"],["COMMANDS","**droid**\n> Start the interactive REPL.\n\n**exec**\n> Run a task non-interactively. This is the scripting entry point.\n\n**search**, **find**\n> Search across local sessions: messages, documents, and tool results.\n\n**mcp**\n> Manage Model Context Protocol servers.\n\n**plugin**\n> Install, uninstall, and update plugins, and manage marketplaces.\n\n**computer**\n> Register and manage Bring-Your-Own-Machine computers.\n\n**daemon**\n> Run the Factory daemon server.\n\n**update**\n> Update the CLI to the latest version."],["EXEC OPTIONS","**--auto** _LEVEL_\n> Autonomy level: **low**, **medium**, or **high**. Omitting it leaves the agent read-only.\n\n**-f**, **--file** _PATH_\n> Read the prompt from a file.\n\n**-m**, **--model** _ID_\n> Select a specific model.\n\n**-r**, **--reasoning-effort** _LEVEL_\n> Override reasoning effort: `off`, `none`, `low`, `medium`, or `high`.\n\n**-o**, **--output-format** _FORMAT_\n> Output format: `text` (default), `json`, `stream-json`, or `stream-jsonrpc`.\n\n**--input-format** _FORMAT_\n> Input format. `stream-jsonrpc` enables multi-turn sessions.\n\n**-s**, **--session-id** _ID_\n> Continue an existing session.\n\n**--fork**\n> Fork an existing session into a new copy and resume that.\n\n**-w**, **--worktree**\n> Run the session in an isolated git worktree, leaving the working tree untouched.\n\n**--cwd** _PATH_\n> Execute from a specific working directory.\n\n**--use-spec**\n> Start in specification mode: plan before executing.\n\n**--spec-model** _ID_ / **--spec-reasoning-effort** _LEVEL_\n> Use a different model or reasoning effort for the planning phase.\n\n**--enabled-tools** _IDS_ / **--disabled-tools** _IDS_ / **--list-tools**\n> Force-enable tools, disable them for this run, or print the available ones and exit.\n\n**--mission**\n> Run in mission mode, orchestrating multiple agents. **--worker-model** and **--validator-model**, with their matching reasoning-effort flags, control the sub-agents.\n\n**--append-system-prompt** _TEXT_ / **--append-system-prompt-file** _PATH_\n> Append custom instructions to the system prompt.\n\n**--tag** _TAG_\n> Attach a session tag. Repeatable.\n\n**--skip-permissions-unsafe**\n> Skip every permission prompt. Dangerous; see CAVEATS.\n\n**-v**, **--version** / **-h**, **--help**\n> Print the version, or show help."],["CONFIGURATION","**.factory/droids/**\n> Custom sub-agent definitions, as markdown files with YAML frontmatter."],["DESCRIPTION","**droid** is Factory.ai's coding agent for the terminal. It runs in two shapes: an interactive REPL, and **droid exec**, which takes a prompt, works until the task is done, and exits, which is what makes it usable from scripts and CI.\n\nPermissions are graduated rather than all-or-nothing. Without **--auto** the agent is effectively read-only: it can inspect the codebase and produce a plan, but not change it. **--auto low** permits file edits and little else. **--auto medium** additionally auto-approves reversible actions such as installing dependencies and running tests, while still asking before anything risky. **--auto high** hands over everything, including commits and pushes. The sensible progression is to start low on an unfamiliar repository, see what the agent actually proposes to touch, and raise the level once you trust it.\n\n**--use-spec** inverts the flow: the agent writes a specification first and only then implements against it, optionally using a stronger model for the planning step via **--spec-model**. **--mission** goes further, orchestrating several agents and splitting work between worker and validator roles.\n\nCustom sub-agents (\"Custom Droids\") are markdown files with YAML frontmatter under `.factory/droids/`, so a repository can ship its own specialised agents alongside its code."],["CAVEATS","**--skip-permissions-unsafe** disables every safety check, including the confirmations before destructive commands. It is meant for throwaway containers, and running it on a machine you care about, or with live credentials in the environment, is exactly the mistake its name warns about.\n\n**--auto high** can rewrite history, force-push, and run arbitrary commands without asking. Combining it with a session that has push access to a shared branch is a real risk, and **--worktree** is the cheap mitigation, since it isolates the agent's changes from your working tree.\n\nThe tool needs a Factory.ai account, and prompts, code context, and tool results are sent to Factory's service and to the underlying model providers. That has obvious implications for proprietary code and is worth checking against your organisation's policy before pointing it at a private repository."],["HISTORY","**droid** is the CLI of **Factory.ai**, a company building agent tooling aimed at enterprise engineering teams rather than individual developers, which shows in its emphasis on session management, worktree isolation, multi-agent missions, and model choice. It is one of a crowded field of terminal coding agents that appeared from 2024 onward, alongside Claude Code, Codex CLI, Aider, and others."],["SEE ALSO","[claude](/man/claude)(1), [codex](/man/codex)(1), [aider](/man/aider)(1), [opencode](/man/opencode)(1), [gemini](/man/gemini)(1)"],["RESOURCES","```[Homepage](https://factory.ai)```\n\n```[Documentation](https://docs.factory.ai/reference/cli-reference)```"]],"e":["claude","codex","aider","opencode","gemini"],"x":"droid"},"elasticsearch-create-enrollment-token":{"n":"elasticsearch-create-enrollment-token","t":"Generate enrollment tokens for nodes and Kibana","s":[["TAGLINE","Generate enrollment tokens for nodes and Kibana"],["TLDR","**Create token for new node**\n\n```elasticsearch-create-enrollment-token -s node```\n\n**Create token for Kibana**\n\n```elasticsearch-create-enrollment-token -s kibana```\n\n**Specify URL**\n\n```elasticsearch-create-enrollment-token -s node --url [https://localhost:9200]```"],["SYNOPSIS","**elasticsearch-create-enrollment-token** **-s** _scope_ [_options_]"],["PARAMETERS","**-s**, **--scope** _scope_\n> Scope of the generated token: `node` or `kibana`.\n\n**--url** _url_\n> Base URL (hostname and port) used to submit API requests. Defaults to the value derived from elasticsearch.yml.\n\n**-f**, **--force**\n> Force the command to run against an unhealthy cluster.\n\n**-E** _setting=value_\n> Configure the specified Elasticsearch setting for the duration of the command.\n\n**-h**, **--help**\n> Display help information."],["DESCRIPTION","**elasticsearch-create-enrollment-token** generates enrollment tokens used to add new Elasticsearch nodes to an existing cluster or to configure a Kibana instance to communicate with a security-enabled cluster.\n\nTokens are short-lived and expire 30 minutes after they are generated. The command must be run from the Elasticsearch installation directory (typically `bin/elasticsearch-create-enrollment-token`)."],["CAVEATS","Only works with clusters that were auto-configured for security (the default in Elasticsearch 8.0 and later). Generated tokens expire after 30 minutes. Requires a running, healthy node unless `--force` is given."],["SEE ALSO","[elasticsearch](/man/elasticsearch)(1), [elasticsearch-reset-password](/man/elasticsearch-reset-password)(1)"]],"e":["elasticsearch","elasticsearch-reset-password"],"x":"elasticsearch-create-enrollment-token -s node"},"evince":{"n":"evince","t":"GNOME document viewer for PDF and PostScript","s":[["TAGLINE","GNOME document viewer for PDF and PostScript"],["TLDR","**Open** a PDF file\n\n```evince [path/to/file.pdf]```\n\nOpen **multiple** documents\n\n```evince [file1.pdf] [file2.pdf]```\n\nOpen in **fullscreen** mode\n\n```evince [-f|--fullscreen] [path/to/file.pdf]```\n\nOpen in **presentation** mode\n\n```evince [-s|--presentation] [path/to/file.pdf]```\n\nOpen a specific **page** number\n\n```evince [-i|--page-index] [5] [path/to/file.pdf]```"],["SYNOPSIS","**evince** [_options_] [_files_]"],["PARAMETERS","**-f, --fullscreen**\n> Open in fullscreen mode\n\n**-s, --presentation**\n> Open in presentation mode\n\n**-i, --page-index** _page_\n> Open at specific page\n\n**-l, --page-label** _label_\n> Open at page with label\n\n**-w, --preview**\n> Run in preview mode"],["DESCRIPTION","**evince** is the GNOME document viewer supporting PDF, PostScript, DjVu, TIFF, and other formats. It provides annotation support, form filling, and presentation mode.\n\nIntegrates with GNOME desktop services for printing and file management."],["CAVEATS","GNOME desktop application. Some features require poppler library. Large documents may consume significant memory."],["INSTALL","```apt: sudo apt install evince```\n\n```dnf: sudo dnf install evince```\n\n```pacman: sudo pacman -S evince```\n\n```apk: sudo apk add evince```\n\n```zypper: sudo zypper install evince```\n\n```brew: brew install evince```\n\n```nix: nix profile install nixpkgs#evince```"],["SEE ALSO","[okular](/man/okular)(1), [zathura](/man/zathura)(1), [xdg-open](/man/xdg-open)(1)"],["RESOURCES","```[Source code](https://gitlab.gnome.org/GNOME/evince)```\n\n```[Homepage](https://apps.gnome.org/Evince/)```"]],"e":["okular","zathura","xdg-open"],"x":"evince [path/to/file.pdf]"},"evtest":{"n":"evtest","t":"Input device event monitor and query tool","s":[["TAGLINE","Input device event monitor and query tool"],["TLDR","**List** all detected input devices\n\n```sudo evtest```\n\nDisplay **events** from a specific device\n\n```sudo evtest /dev/input/event[number]```\n\n**Grab** device exclusively (prevent other apps from receiving events)\n\n```sudo evtest --grab /dev/input/event[number]```\n\n**Query** state of a specific key or button\n\n```sudo evtest --query /dev/input/event[number] EV_KEY [KEY_A]```"],["SYNOPSIS","**evtest** [**--grab**] _/dev/input/eventX_\n\n**evtest** **--query** _/dev/input/eventX_ _event_type_ _event_code_"],["PARAMETERS","**--grab**\n> Grab device exclusively using EVIOCGRAB, preventing other processes from receiving events.\n\n**--query** _device_ _type_ _code_\n> Query state of a specific key/button/switch. Type can be EV_KEY, EV_SW, EV_SND, EV_LED or a numeric value. Returns exit code 0 if unset, 10 if set."],["DESCRIPTION","**evtest** displays information from input device drivers using the Linux input subsystem. In capture mode, it shows all events supported by the device and then monitors it to display all generated events like key presses, mouse movements, and touch events.\n\nIn query mode, it performs a one-shot query of the state of a specific key, switch, sound, or LED value."],["CAVEATS","Requires root privileges to read from /dev/input/ devices. Grabbing a device prevents other applications from receiving events until evtest quits."],["INSTALL","```apt: sudo apt install evtest```\n\n```dnf: sudo dnf install evtest```\n\n```pacman: sudo pacman -S evtest```\n\n```apk: sudo apk add evtest```\n\n```zypper: sudo zypper install evtest```\n\n```nix: nix profile install nixpkgs#evtest```"],["SEE ALSO","[xinput](/man/xinput)(1), [libinput](/man/libinput)(1)"]],"e":["xinput","libinput"],"x":"sudo evtest"},"foo2zjs":{"n":"foo2zjs","t":"PBM to ZjStream converter for HP and Minolta printers","s":[["TAGLINE","PBM to ZjStream converter for HP and Minolta printers"],["TLDR","**Convert PBM to** ZjStream format\n\n```foo2zjs [options] < [input.pbm] > [output.prn]```\n\n**Set resolution**\n\n```foo2zjs -r [600] < [input.pbm] > [output.prn]```\n\n**Set paper size (1=letter, 9=A4, 5=legal)**\n\n```foo2zjs -p [9] < [input.pbm] > [output.prn]```\n\n**Color mode**\n\n```foo2zjs -c < [input.ppm] > [output.prn]```\n\n**Print multiple copies**\n\n```foo2zjs -n [3] < [input.pbm] > [output.prn]```"],["SYNOPSIS","**foo2zjs** [_options_] < _input_ > _output_"],["PARAMETERS","**-r** _DPI_\n> Resolution (300, 600, 1200).\n\n**-p** _CODE_\n> Paper size code: 1=letter, 5=legal, 7=executive, 9=A4, 11=A5, 13=B5. Default is 1.\n\n**-c**\n> Color mode (input must be PPM).\n\n**-m** _MEDIA_\n> Media type code.\n\n**-n** _COPIES_\n> Number of copies.\n\n**-d** _DUPLEX_\n> Duplex mode: 1=off, 2=long edge, 3=short edge.\n\n**-g** _XPIXxYPIX_\n> Page dimensions in pixels (default 10200x6600).\n\n**--help**\n> Display help information."],["DESCRIPTION","**foo2zjs** converts PBM/PPM images to ZjStream format for HP LaserJet and Minolta/QMS magicolor printers. It's the core tool of the foo2zjs printer driver suite.\n\nThe tool encodes raster data in the proprietary ZjStream protocol, handling page setup, compression, and printer commands. It works as a CUPS filter for supported printers.\n\nfoo2zjs enables Linux printing on numerous \"GDI printers\" that lack standard driver support."],["CAVEATS","Only for ZjStream-compatible printers. Model support varies. Check compatibility before use."],["HISTORY","foo2zjs was created by **Rick Richardson** to provide Linux drivers for \"Windows-only\" GDI printers. The project reverse-engineered proprietary protocols, enabling Linux support for many consumer laser printers."],["INSTALL","```apt: sudo apt install printer-driver-foo2zjs```\n\n```dnf: sudo dnf install foo2zjs```\n\n```nix: nix profile install nixpkgs#foo2zjs```"],["SEE ALSO","[foo2zjs-wrapper](/man/foo2zjs-wrapper)(1), [foo2hp](/man/foo2hp)(1), [foo2lava](/man/foo2lava)(1)"]],"e":["foo2zjs-wrapper","foo2hp","foo2lava"],"x":"foo2zjs [options] < [input.pbm] > [output.prn]"},"ganache-cli":{"n":"ganache-cli","t":"command-line Ethereum test blockchain","s":[["TAGLINE","command-line Ethereum test blockchain"],["TLDR","**Start Ethereum test blockchain**\n\n```ganache-cli```\n\n**Start on specific port**\n\n```ganache-cli -p [8545]```\n\n**Start with specific accounts**\n\n```ganache-cli -a [10]```\n\n**Start with mnemonic**\n\n```ganache-cli -m \"[mnemonic phrase]\"```\n\n**Start with deterministic addresses**\n\n```ganache-cli -d```\n\n**Fork from mainnet**\n\n```ganache-cli -f [https://mainnet.infura.io/v3/key]```\n\n**Set gas limit**\n\n```ganache-cli -l [8000000]```"],["SYNOPSIS","**ganache-cli** [_options_]"],["PARAMETERS","**-p**, **--port** _port_\n> Port number.\n\n**-a**, **--accounts** _num_\n> Number of accounts.\n\n**-m**, **--mnemonic** _phrase_\n> HD wallet mnemonic.\n\n**-d**, **--deterministic**\n> Deterministic addresses.\n\n**-f**, **--fork** _url_\n> Fork from network.\n\n**-l**, **--gasLimit** _limit_\n> Block gas limit.\n\n**-e**, **--defaultBalanceEther** _amount_\n> Default account balance.\n\n**-i**, **--networkId** _id_\n> Network ID."],["DESCRIPTION","**ganache-cli** (now part of Ganache) creates a local Ethereum blockchain for development and testing. It provides instant transaction mining and configurable test accounts for smart contract development.\n\nThis command-line version is now superseded by the **ganache** command, which offers the same functionality with additional features. ganache-cli was originally known as TestRPC before being renamed and integrated into the Ganache suite.\n\nThe tool simulates a full Ethereum client, making it ideal for testing smart contracts locally before deploying to testnets or mainnet. It integrates with development frameworks like Truffle and Hardhat."],["CAVEATS","ganache-cli is deprecated in favor of the **ganache** command. Not for production use. Blockchain state resets on restart unless persistence is configured. Behavior may differ from actual Ethereum mainnet."],["HISTORY","Originally released as **TestRPC**, it was renamed to **ganache-cli** when incorporated into the Truffle Suite around **2017**. The tool has since been replaced by the unified **ganache** command starting with Ganache 7."],["INSTALL","```aur: yay -S ganache-cli```"],["SEE ALSO","[geth](/man/geth)(1), [truffle](/man/truffle)(1)"],["RESOURCES","```[Source code](https://github.com/trufflesuite/ganache)```"]],"e":["geth","truffle"],"x":"ganache-cli"},"git-archive-file":{"n":"git-archive-file","t":"Export the current HEAD of the git repository to a zip archive","s":[["TAGLINE","Export the current HEAD of the git repository to a zip archive"],["TLDR","**Create archive of current branch**\n\n```git archive-file```"],["SYNOPSIS","**git** **archive-file**"],["DESCRIPTION","**git archive-file** is a git-extras utility that exports the current HEAD of the repository into a zip archive with a descriptive filename. The generated filename includes the repository name and branch name (e.g., \"project.main.zip\"), or on a detached HEAD such as a tag, uses the tag name (e.g., \"project.1.0.0.zip\").\n\nThe command takes no arguments or options. It always archives the current HEAD and writes the zip file to the current directory.\n\nAs of git-extras 6.4, the assumed default branch name changed from master to main."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-archive](/man/git-archive)(1), [git-extras](/man/git-extras)(1)"]],"e":["git-archive","git-extras"],"x":"git archive-file"},"git-symbolic-ref":{"n":"git-symbolic-ref","t":"Read and modify symbolic references like HEAD","s":[["TAGLINE","Read and modify symbolic references like HEAD"],["TLDR","**Show HEAD target**\n\n```git symbolic-ref HEAD```\n\n**Set HEAD to branch**\n\n```git symbolic-ref HEAD refs/heads/main```\n\n**Get short name**\n\n```git symbolic-ref --short HEAD```\n\n**Delete symbolic ref**\n\n```git symbolic-ref -d HEAD```"],["SYNOPSIS","**git symbolic-ref** [_options_] _name_ [_ref_]"],["PARAMETERS","_NAME_\n> Symbolic ref name (usually HEAD).\n\n_REF_\n> Target ref to set.\n\n**--short**\n> Show short name.\n\n**-d**, **--delete**\n> Delete symbolic ref.\n\n**-q**, **--quiet**\n> Quiet mode.\n\n**--help**\n> Display help information."],["DESCRIPTION","**git symbolic-ref** reads and modifies symbolic references. HEAD is the most common symbolic ref, pointing to the currently checked-out branch. This plumbing command shows which branch HEAD points to or changes it programmatically.\n\nWhen HEAD is detached (pointing directly to a commit rather than a branch), this command will report an error, distinguishing it from `git rev-parse`."],["CAVEATS","Plumbing command. HEAD is most common use. Detached HEAD is not a symbolic ref."],["HISTORY","git symbolic-ref is a core **Git** plumbing command for managing symbolic references, fundamental to branch tracking."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-checkout](/man/git-checkout)(1), [git-rev-parse](/man/git-rev-parse)(1)"],["RESOURCES","```[Source code](https://github.com/git/git)```\n\n```[Homepage](https://git-scm.com/)```\n\n```[Documentation](https://git-scm.com/docs/git-symbolic-ref)```"]],"e":["git-checkout","git-rev-parse"],"x":"git symbolic-ref HEAD"},"gita":{"n":"gita","t":"Manage multiple Git repositories side by side","s":[["TAGLINE","Manage multiple Git repositories side by side"],["TLDR","Display **status of all registered** repositories\n\n```gita ll```\n\n**Register repositories** to be tracked\n\n```gita add [path/to/repo1] [path/to/repo2]```\n\n**Recursively discover and add** all repos under a directory\n\n```gita add -a [path/to/parent]```\n\n**Run a git command** across all registered repos\n\n```gita fetch```\n\nRun an **arbitrary git command** across specific repos\n\n```gita super [repo1] [repo2] -c \"git [command]\"```\n\nRun a **shell command** across all repos\n\n```gita shell -c \"[command]\"```\n\n**List** all registered repository names\n\n```gita ls```\n\n**Remove** a repository from tracking\n\n```gita rm [repo_name]```"],["SYNOPSIS","**gita** [_subcommand_] [_options_] [_repos_]"],["PARAMETERS","**add** _path(s)_\n> Register repositories to track. Use **-a** to recursively discover repos under a directory, **-b** for bare repos.\n\n**rm** _repo(s)_\n> Unregister repositories from tracking.\n\n**ls**\n> List names of all registered repositories.\n\n**ll**\n> Display detailed status of all repos including branch, sync state, and modifications.\n\n**fetch**\n> Run git fetch across all registered repos.\n\n**pull**\n> Run git pull across all registered repos.\n\n**super** [_repos_] **-c** _\"git command\"_\n> Delegate any arbitrary git command to specified repos (or all if none specified).\n\n**shell** [_repos_] **-c** _\"command\"_\n> Run any shell command in each repo's directory.\n\n**freeze**\n> Export repository paths and URLs for backup or sharing.\n\n**clone** _url_\n> Clone a repository and register it.\n\n**group add** _repos_ **-n** _name_\n> Create a named group of repositories.\n\n**group ll**\n> List all groups and their contents.\n\n**group rm** _name_\n> Delete a named group.\n\n**context** _group_\n> Limit subsequent commands to a specific group. Use **none** to clear.\n\n**info**\n> Configure which information items are displayed in **ll** output.\n\n**color**\n> Manage color schemes for branch status display.\n\n**flags set** _repo_ _flags_\n> Set custom git flags for a specific repository."],["DESCRIPTION","**gita** is a command-line tool for managing multiple Git repositories simultaneously. It provides a unified view of repository statuses and allows batch execution of git commands across all tracked repos from any working directory, without needing to cd into each one individually.\n\nRepositories are registered with **gita add** and their paths are stored in **$XDG_CONFIG_HOME/gita/repos.csv**. Once registered, **gita ll** displays a color-coded summary of each repo's branch, sync state (ahead/behind remote), and working tree modifications. Git commands like **fetch** and **pull** can be delegated to all repos at once, and arbitrary git or shell commands can be run via **super** and **shell** subcommands.\n\nRepos can be organized into named **groups** and scoped with **context** to limit operations to a subset. Configuration files for groups, colors, display info, and custom commands are stored in **$XDG_CONFIG_HOME/gita/**."],["CAVEATS","Requires **Python 3.6+**. Batch operations run asynchronously across repos, but commands requiring user interaction (such as **log**, **difftool**, **mergetool**) run synchronously to avoid garbled output. By default only **fetch** and **pull** are allowed to run across all repos without specifying targets; other commands require explicit repo names unless custom configuration overrides this. On Windows, ANSI color output must be enabled manually."],["HISTORY","**gita** was created by **nosarthur** and first released in **January 2018**. Written in **Python**, it is distributed via PyPI and installable with pip, pipx, or uv. The name is a play on \"git\" with an added \"a\" suggesting management of multiple repos. The project is MIT-licensed and actively maintained."],["INSTALL","```nix: nix profile install nixpkgs#gita```"],["SEE ALSO","[git](/man/git)(1), [mr](/man/mr)(1), [ghq](/man/ghq)(1)"]],"e":["git","mr","ghq"],"x":"gita ll"},"gvfs-move":{"n":"gvfs-move","t":"moves files using GVFS","s":[["TAGLINE","moves files using GVFS"],["TLDR","**Move local file**\n\n```gvfs-move [source] [destination]```\n\n**Move to remote**\n\n```gvfs-move [local-file] smb://server/share/```\n\n**Move with progress**\n\n```gvfs-move -p [source] [destination]```\n\n**Interactive mode**\n\n```gvfs-move -i [source] [destination]```"],["SYNOPSIS","**gvfs-move** [_options_] _source_ _destination_"],["PARAMETERS","_SOURCE_\n> Source file or URI.\n\n_DESTINATION_\n> Destination file or URI.\n\n**-p**, **--progress**\n> Show progress.\n\n**-i**, **--interactive**\n> Prompt before overwriting files.\n\n**-b**, **--backup**\n> Backup existing destination files.\n\n**-T**, **--no-target-directory**\n> Don't move into destination even if it is a directory.\n\n**-h**, **--help**\n> Print a short help text and exit."],["DESCRIPTION","**gvfs-move** moves files using GVFS. It supports local files and remote URIs, handling moves across different backends transparently.\n\nThe tool provides atomic moves within the same filesystem and copy-then-delete for cross-filesystem moves. It integrates with GNOME's VFS."],["CAVEATS","Part of the gvfs-bin command-line tools, deprecated in gvfs 1.31 (2016) and removed entirely in gvfs 1.38 (2018) in favor of the unified **gio** command (`gio move`). Modern distributions no longer ship gvfs-move. Cross-filesystem moves require enough free space at the destination since they fall back to copy-then-delete."],["HISTORY","**gvfs-move** was part of GVFS's original gvfs-bin utilities, introduced for GNOME alongside GVFS in **2008** (GNOME 2.22). It has since been superseded by **gio move**."],["SEE ALSO","[gio](/man/gio)(1), [mv](/man/mv)(1), [gvfs-copy](/man/gvfs-copy)(1)"],["RESOURCES","```[Source code](https://gitlab.gnome.org/GNOME/gvfs)```\n\n```[Homepage](https://wiki.gnome.org/Projects/gvfs)```"]],"e":["gio","mv","gvfs-copy"],"x":"gvfs-move [source] [destination]"},"heretek":{"n":"heretek","t":"GDB TUI dashboard for remote and embedded debugging","s":[["TAGLINE","GDB TUI dashboard for remote and embedded debugging"],["TLDR","**Start heretek** with default GDB\n\n```heretek```\n\n**Connect to a remote target** via netcat\n\n```heretek -r [host:port]```\n\n**Use a custom GDB binary**\n\n```heretek --gdb-path [path/to/gdb]```\n\n**Run in 32-bit mode**\n\n```heretek --ptr-size 32```\n\n**Execute GDB commands from a file**\n\n```heretek -c [path/to/commands.gdb]```"],["SYNOPSIS","**heretek** [**--gdb-path** _path_] [**-r** _host:port_] [**--ptr-size** _size_] [**-c** _file_] [_options_]"],["PARAMETERS","**--gdb-path** _PATH_\n> Override gdb executable path\n\n**-r**, **--remote** _HOST:PORT_\n> Connect to a netcat session for remote debugging\n\n**--ptr-size** _SIZE_\n> Set pointer size mode (possible values: 32, 64, auto; default: auto)\n\n**-c**, **--cmds** _FILE_\n> Execute GDB commands line-by-line from a file (lines starting with # are ignored)\n\n**--log-path** _PATH_\n> Path to write log file (set RUST_LOG env variable to change log level)\n\n**-h**, **--help**\n> Print help\n\n**-V**, **--version**\n> Print version"],["DESCRIPTION","**heretek** is a GDB TUI dashboard designed to seamlessly connect to remote targets even without a functioning gdbserver. It ships as a single statically-linked **musl** binary with no Python dependencies, making it ideal for embedded and minimal environments where vendors may ship GDB without Python support.\n\nheretek operates on systems with only **gdb**, **nc** (netcat), and **mkfifo**, bypassing common issues with broken gdbserver binaries. The dashboard displays registers, disassembly, stack, and memory in an organized TUI layout."],["CAVEATS","Requires at minimum **gdb**, **nc**, and **mkfifo** on the target system. Some advanced GDB features that rely on Python extensions are not available. The TUI layout may require a sufficiently wide terminal."],["HISTORY","**heretek** was created by **wcampbell0x2a** and is written in **Rust**. It was built to address the pain points of debugging on embedded systems where traditional GDB frontends fail due to missing Python or broken gdbserver installations."],["INSTALL","```pacman: sudo pacman -S heretek```"],["SEE ALSO","[gdb](/man/gdb)(1), [nc](/man/nc)(1)"]],"e":["gdb","nc"],"x":"heretek"},"hyprland":{"n":"hyprland","t":"dynamic tiling Wayland compositor with smooth animations, eye-candy","s":[["TAGLINE","dynamic tiling Wayland compositor with smooth animations, eye-candy"],["TLDR","**Start Hyprland**\n\n```Hyprland```\n\n**Reload configuration**\n\n```hyprctl reload```\n\n**List windows**\n\n```hyprctl clients```\n\n**Dispatch keybind action**\n\n```hyprctl dispatch [workspace] [3]```\n\n**Get active window**\n\n```hyprctl activewindow```\n\n**Show monitors**\n\n```hyprctl monitors```"],["SYNOPSIS","**Hyprland** [_options_]\n\n**hyprctl** [_command_] [_args_]"],["PARAMETERS","**hyprctl dispatch** _action_ [_args_]\n> Execute dispatcher action.\n\n**hyprctl keyword** _option_ _value_\n> Set config option at runtime.\n\n**hyprctl reload**\n> Reload configuration.\n\n**hyprctl clients**\n> List windows.\n\n**hyprctl monitors**\n> List monitors.\n\n**hyprctl activewindow**\n> Show active window info.\n\n**hyprctl workspaces**\n> List workspaces.\n\n**hyprctl version**\n> Show version."],["DESCRIPTION","**Hyprland** is a dynamic tiling Wayland compositor with smooth animations, eye-candy effects, and extensive customization. It provides a modern desktop experience with features like window animations, blur, shadows, and workspace management.\n\nConfiguration is done through ~/.config/hypr/hyprland.conf with a custom syntax for bindings, decorations, and layout settings."],["CONFIGURATION EXAMPLE","```\nmonitor=,preferred,auto,1\nexec-once = waybar\nbind = SUPER, Return, exec, kitty\nbind = SUPER, Q, killactive\nbind = SUPER, 1, workspace, 1\nwindowrule = float, ^(pavucontrol)$\n```"],["CAVEATS","Wayland only. Requires compatible graphics drivers. Some X11 apps need XWayland. Configuration syntax differs from other WMs."],["HISTORY","Hyprland was created by **vaxerski** in **2022** as a feature-rich Wayland compositor. It quickly gained popularity for its aesthetics and performance, becoming a leading choice for Wayland tiling WM users."],["INSTALL","```pacman: sudo pacman -S hyprland```\n\n```apk: sudo apk add hyprland```\n\n```zypper: sudo zypper install hyprland```\n\n```nix: nix profile install nixpkgs#hyprland```"],["SEE ALSO","[sway](/man/sway)(1), [waybar](/man/waybar)(1)"]],"e":["sway","waybar"],"x":"Hyprland"},"insmod":{"n":"insmod","t":"simple program to insert a module into the Linux kernel","s":[["TAGLINE","simple program to insert a module into the Linux kernel"],["TLDR","**Insert** a kernel module\n\n```sudo insmod path/to/module.ko```\n\nInsert module with **parameters**\n\n```sudo insmod path/to/module.ko param=value```"],["SYNOPSIS","**insmod** [_filename_] [_module options_...]"],["DESCRIPTION","**insmod** is a simple program to insert a module into the Linux kernel. It requires the full path to the module file. Unlike modprobe, insmod does not handle dependencies automatically."],["PARAMETERS","_filename_\n> Path to the kernel module file (**.ko**). Use **-** to read the module from standard input.\n\n_module options_\n> Key=value parameters passed to the module on load.\n\n**-f**, **--force**\n> Disable kernel version and vermagic checks. Dangerous: can cause memory corruption or crashes.\n\n**--force-modversion**\n> Ignore module version symbol mismatches.\n\n**--force-vermagic**\n> Ignore vermagic string mismatches.\n\n**-s**, **--syslog**\n> Send error messages to syslog instead of stderr.\n\n**-v**, **--verbose**\n> Print detailed information about what insmod is doing.\n\n**-V**, **--version**\n> Show program version and exit.\n\n**-h**, **--help**\n> Show help and exit."],["CAVEATS","insmod is a low-level tool that does not resolve dependencies and requires the full path to the **.ko** file. Use **modprobe** instead for most cases, as it handles dependencies and uses module names rather than file paths. Detailed error diagnostics typically appear in **dmesg** rather than on stderr. Loading modules requires root privileges."],["HISTORY","**insmod** is part of **kmod**. It provides direct module insertion without the intelligence of modprobe, useful for testing or loading modules not in the standard module path."],["INSTALL","```apt: sudo apt install kmod```\n\n```dnf: sudo dnf install kmod```\n\n```pacman: sudo pacman -S kmod```\n\n```apk: sudo apk add kmod```\n\n```zypper: sudo zypper install kmod```\n\n```brew: brew install kmod```\n\n```nix: nix profile install nixpkgs#kmod```"],["SEE ALSO","[modprobe](/man/modprobe)(8), [rmmod](/man/rmmod)(8), [lsmod](/man/lsmod)(8), [modinfo](/man/modinfo)(8), [depmod](/man/depmod)(8), [kmod](/man/kmod)(8), [dmesg](/man/dmesg)(1)"]],"e":["modprobe","rmmod","lsmod","modinfo","depmod","kmod","dmesg"],"x":"sudo insmod path/to/module.ko"},"ipinfo":{"n":"ipinfo","t":"official command-line client for the IPinfo API","s":[["TAGLINE","official command-line client for the IPinfo API"],["TLDR","**Look up your own IP address**\n\n```ipinfo myip```\n\n**Look up details for a specific IP address**\n\n```ipinfo [8.8.8.8]```\n\n**Look up ASN information**\n\n```ipinfo [AS15169]```\n\n**Bulk lookup** multiple IPs or CIDR ranges\n\n```ipinfo bulk [1.1.1.0/30] [8.8.8.8]```\n\n**Extract a specific field** (city, country, org, hostname, etc.)\n\n```ipinfo [8.8.8.8] -f [city]```\n\n**Output in CSV format**\n\n```cat ips.txt | ipinfo -c```\n\n**Authenticate with an API token** for higher rate limits\n\n```ipinfo init [your_token]```\n\n**Summarize details for a list of IPs**\n\n```cat ips.txt | ipinfo summarize```"],["SYNOPSIS","**ipinfo** [_command_] [_IP_|_ASN_] [_options_]"],["PARAMETERS","**init** _token_\n> Save an API token for authenticated requests.\n\n**myip**\n> Look up details for your own IP address.\n\n**bulk** _ips_\n> Process multiple IPs or CIDR ranges.\n\n**summarize** _ips_\n> Produce a summary report for a list of IPs.\n\n**-f**, **--field** _FIELD_\n> Return only the specified field (e.g., ip, city, region, country, org, hostname).\n\n**-c**, **--csv**\n> Output results in CSV format.\n\n**--nocolor**\n> Disable colored output (or set the `NO_COLOR` environment variable).\n\n**--token** _TOKEN_\n> Pass an API token for this invocation.\n\n**completion**\n> Generate shell completions (bash, zsh, fish).\n\n**-h**, **--help**\n> Display help information.\n\n**-v**, **--version**\n> Display version information."],["DESCRIPTION","**ipinfo** is the official command-line client for the IPinfo.io API. It returns IP intelligence data including geographic location, ISP, organization, ASN, hostname, and abuse contact information.\n\nThe tool accepts IP addresses (IPv4/IPv6), ASNs, and CIDR ranges. Multiple inputs can be queried at once or piped in from another command. Output can be rendered as human-readable text, JSON, or CSV, and specific fields can be extracted for use in scripts.\n\nThe CLI ships with several helper binaries such as **grepip**, **prips**, **cidr2range**, **cidr2ip**, **range2cidr**, and **mmdb** for working with IP lists and MMDB databases."],["CAVEATS","Requires internet connectivity to reach ipinfo.io. Anonymous requests are heavily rate-limited; register for a free token via `ipinfo init` for higher quotas. Private, reserved, and multicast addresses cannot be geolocated. Geolocation accuracy varies by region and network type."],["HISTORY","Developed by **IPinfo.io**, a geolocation data provider founded in **2013**. The CLI is written in Go and wraps the same public API used by the web service."],["INSTALL","```brew: brew install ipinfo```\n\n```nix: nix profile install nixpkgs#ipinfo```"],["SEE ALSO","[curl](/man/curl)(1), [whois](/man/whois)(1), [dig](/man/dig)(1), [jq](/man/jq)(1)"]],"e":["curl","whois","dig","jq"],"x":"ipinfo myip"},"jj-commit":{"n":"jj-commit","t":"Update the description and create a new change on top in Jujutsu","s":[["TAGLINE","Update the description and create a new change on top in Jujutsu"],["TLDR","**Create a commit with a message**\n\n```jj commit -m \"[message]\"```\n\n**Commit interactively** choosing which changes to include\n\n```jj commit -i```\n\n**Open an editor** to write the commit description\n\n```jj commit```\n\n**Commit only specific files**\n\n```jj commit [path/to/file1] [path/to/file2]```\n\n**Commit with a specific diff editor tool**\n\n```jj commit --tool [meld]```\n\n**Set a message and still open the editor** to refine it\n\n```jj commit -m \"[draft message]\" --editor```"],["SYNOPSIS","**jj** **commit** [_options_] [_FILESETS_...]"],["PARAMETERS","_FILESETS_\n> Put these paths in the current commit.\n\n**-m**, **--message** _MESSAGE_\n> The change description to use (don't open editor).\n\n**-i**, **--interactive**\n> Interactively choose which changes to include in the current commit.\n\n**--tool** _NAME_\n> Specify diff editor to be used (implies --interactive).\n\n**--editor**\n> Open an editor to edit the change description. Forces an editor even when using --message."],["DESCRIPTION","**jj commit** updates the description of the current working-copy commit and creates a new empty change on top of it. It is equivalent to running **jj describe** followed by **jj new**, and has the alias **jj ci**.\n\nWhen invoked without path arguments or **--interactive**, all changes in the working copy are included. When paths or **--interactive** are used, only the selected changes are committed while the remaining changes are moved to the new working-copy commit above.\n\nUnlike git, Jujutsu automatically treats the working copy as a mutable commit, so this command finalizes the current changes by setting a description and advancing the working copy."],["INSTALL","```pacman: sudo pacman -S jujutsu```\n\n```apk: sudo apk add jujutsu```\n\n```zypper: sudo zypper install jujutsu```\n\n```brew: brew install jujutsu```\n\n```nix: nix profile install nixpkgs#jujutsu```"],["SEE ALSO","[jj](/man/jj)(1), [jj-describe](/man/jj-describe)(1), [jj-new](/man/jj-new)(1), [jj-status](/man/jj-status)(1), [jj-diff](/man/jj-diff)(1)"]],"e":["jj","jj-describe","jj-new","jj-status","jj-diff"],"x":"jj commit -m \"[message]\""},"lime":{"n":"lime","t":"open-source text editor inspired by Sublime Text","s":[["TAGLINE","open-source text editor inspired by Sublime Text"],["TLDR","**Start Lime text editor**\n\n```lime```\n\n**Open file**\n\n```lime [file.txt]```\n\n**Open multiple files**\n\n```lime [file1.txt] [file2.txt]```\n\n**Open file at specific line**\n\n```lime [file.txt]:[line_number]```"],["SYNOPSIS","**lime** [_options_] [_files_...]"],["PARAMETERS","**-h**, **--help**\n> Show help message.\n\n**-v**, **--version**\n> Show version.\n\n**--debug**\n> Enable debug mode."],["DESCRIPTION","**lime** (Lime Text) is an open-source text editor inspired by Sublime Text. It aims to provide a Sublime Text compatible editor with an open-source codebase.\n\nLime supports multiple cursors, split editing, command palette, and is extensible through Python plugins. The backend is written in Go."],["KEY FEATURES","```\n- Sublime Text compatibility\n- Multiple cursors\n- Split editing\n- Command palette\n- Python plugin system\n- Cross-platform\n```"],["CAVEATS","Development has slowed significantly. Not feature-complete compared to Sublime Text. Frontend implementations vary in maturity."],["HISTORY","Lime was started by **Fredrik Ehnbom** in **2013** as an attempt to create an open-source clone of Sublime Text with a Go backend."],["INSTALL","```aur: yay -S lime```"],["SEE ALSO","[vim](/man/vim)(1), [nano](/man/nano)(1), [code](/man/code)(1)"]],"e":["vim","nano","code"],"x":"lime"},"minio-client":{"n":"minio-client","t":"manages MinIO and S3 storage","s":[["TAGLINE","manages MinIO and S3 storage"],["TLDR","**Set alias**\n\n```mc alias set [myminio] [https://minio.example.com] [access_key] [secret_key]```\n\n**List buckets**\n\n```mc ls [myminio]```\n\n**Copy file**\n\n```mc cp [file.txt] [myminio/bucket/]```\n\n**Mirror directory**\n\n```mc mirror [local/] [myminio/bucket/]```\n\n**Remove file**\n\n```mc rm [myminio/bucket/file.txt]```\n\n**Make bucket**\n\n```mc mb [myminio/newbucket]```"],["SYNOPSIS","**mc** _command_ [_options_] [_args_]"],["PARAMETERS","_COMMAND_\n> MinIO client command.\n\n**alias**\n> Manage server aliases.\n\n**ls**\n> List objects.\n\n**cp**\n> Copy objects.\n\n**mirror**\n> Sync directories.\n\n**rm**\n> Remove objects.\n\n**--help**\n> Display help information."],["DESCRIPTION","**minio-client** (mc) manages MinIO and S3 storage. It provides file operations for object storage.\n\nThe tool works with MinIO servers and AWS S3. Supports multiple cloud providers."],["CAVEATS","Configure aliases first. S3 compatible. Different from Midnight Commander mc."],["HISTORY","MinIO Client was created by **MinIO** for managing object storage across cloud providers."],["INSTALL","```apt: sudo apt install minio-client```\n\n```pacman: sudo pacman -S minio-client```\n\n```zypper: sudo zypper install minio-client```\n\n```nix: nix profile install nixpkgs#minio-client```"],["SEE ALSO","[minio-server](/man/minio-server)(1), [aws](/man/aws)(1), [s3cmd](/man/s3cmd)(1)"]],"e":["minio-server","aws","s3cmd"],"x":"mc alias set [myminio] [https://minio.example.com] [access_key] [secret_key]"},"mongodump":{"n":"mongodump","t":"creates binary exports of MongoDB databases","s":[["TAGLINE","creates binary exports of MongoDB databases"],["TLDR","**Dump entire database**\n\n```mongodump --db [database]```\n\n**Dump specific collection**\n\n```mongodump --db [database] --collection [collection]```\n\n**Dump with authentication**\n\n```mongodump --uri \"mongodb://[user]:[pass]@[host]/[db]\"```\n\n**Dump to specific directory**\n\n```mongodump --out [/path/to/backup]```\n\n**Dump as archive file**\n\n```mongodump --archive=[backup.archive]```\n\n**Dump compressed**\n\n```mongodump --gzip --out [backup/]```\n\n**Dump with query filter**\n\n```mongodump --db [db] --collection [coll] --query '{\"status\": \"active\"}'```\n\n**Dump from remote host**\n\n```mongodump --host [hostname] --port [27017]```"],["SYNOPSIS","**mongodump** [_--uri uri_] [_--db database_] [_--collection coll_] [_--out dir_] [_--archive file_] [_options_]"],["PARAMETERS","**--uri** _URI_\n> MongoDB connection URI.\n\n**--host** _HOST_\n> Server hostname.\n\n**--port** _PORT_\n> Server port.\n\n**--db**, **-d** _DATABASE_\n> Database to dump.\n\n**--collection**, **-c** _COLL_\n> Collection to dump.\n\n**--out**, **-o** _DIR_\n> Output directory.\n\n**--archive**[=_FILE_]\n> Output as archive file.\n\n**--gzip**\n> Compress output.\n\n**--query**, **-q** _JSON_\n> Filter documents.\n\n**--queryFile** _FILE_\n> Query filter from file.\n\n**--username**, **-u** _USER_\n> Authentication username.\n\n**--password**, **-p** _PASS_\n> Authentication password.\n\n**--authenticationDatabase** _DB_\n> Authentication database.\n\n**--oplog**\n> Include oplog for point-in-time backup.\n\n**--dumpDbUsersAndRoles**\n> Include users and roles.\n\n**--numParallelCollections** _N_\n> Parallel collection dumps.\n\n**-j** _N_\n> Parallel document dumps."],["DESCRIPTION","**mongodump** creates binary exports of MongoDB databases. It reads data directly from MongoDB and writes BSON files, preserving document structure and types.\n\nOutput format is a directory structure with one folder per database containing BSON and metadata JSON files for each collection. The archive format combines everything into a single file.\n\nThe --oplog option captures operations during the dump, enabling point-in-time restore. This is essential for consistent backups of active databases.\n\nQuery filtering exports only matching documents. This enables partial backups, test data extraction, or archiving specific records.\n\nCompression (--gzip) significantly reduces backup size. Combined with archive format, it produces a single compressed file suitable for storage or transfer.\n\nParallelism options speed up large backups. Multiple collections can dump simultaneously, and documents within collections can be processed in parallel."],["CAVEATS","Not recommended for large production databases - use filesystem snapshots instead. Backup is not atomic without --oplog. Indexes are saved as metadata, rebuilt on restore. Requires read access to all databases being dumped."],["HISTORY","**mongodump** has been part of **MongoDB** since early versions, developed by MongoDB, Inc. (formerly 10gen). It provides the standard method for exporting MongoDB data, complemented by mongorestore for imports. It's part of the MongoDB Database Tools package."],["INSTALL","```apk: sudo apk add mongodb-tools```\n\n```nix: nix profile install nixpkgs#mongodb-tools```"],["SEE ALSO","[mongorestore](/man/mongorestore)(1), [mongoexport](/man/mongoexport)(1), [mongoimport](/man/mongoimport)(1), [mongo](/man/mongo)(1)"]],"e":["mongorestore","mongoexport","mongoimport","mongo"],"x":"mongodump --db [database]"},"monolith":{"n":"monolith","t":"saves complete webpages as single HTML files","s":[["TAGLINE","saves complete webpages as single HTML files"],["TLDR","**Save webpage as single HTML file**\n\n```monolith [https://example.com] -o [page.html]```\n\n**Include JavaScript**\n\n```monolith -j [https://example.com] -o [page.html]```\n\n**Exclude images**\n\n```monolith -i [https://example.com] -o [page.html]```\n\n**Isolate from network** (no external requests)\n\n```monolith -I [https://example.com] -o [page.html]```\n\n**Set custom user agent**\n\n```monolith -u \"[Mozilla/5.0...]\" [https://example.com] -o [page.html]```\n\n**Save from local file**\n\n```monolith [file:///path/to/page.html] -o [output.html]```\n\n**Set timeout**\n\n```monolith -t [30] [https://example.com] -o [page.html]```"],["SYNOPSIS","**monolith** [_-o output_] [_-jicfIe_] [_-t timeout_] [_-u agent_] _url_"],["PARAMETERS","**-o** _FILE_, **--output** _FILE_\n> Output file.\n\n**-j**, **--no-js**\n> Exclude JavaScript.\n\n**-c**, **--no-css**\n> Exclude CSS.\n\n**-i**, **--no-images**\n> Exclude images.\n\n**-f**, **--no-frames**\n> Exclude iframes.\n\n**-F**, **--no-fonts**\n> Exclude fonts.\n\n**-v**, **--no-video**\n> Exclude video.\n\n**-a**, **--no-audio**\n> Exclude audio.\n\n**-I**, **--isolate**\n> Disable network requests.\n\n**-e**, **--ignore-errors**\n> Continue on resource errors.\n\n**-t** _SEC_, **--timeout** _SEC_\n> Request timeout.\n\n**-u** _AGENT_, **--user-agent** _AGENT_\n> Custom User-Agent.\n\n**-b** _URL_, **--base-url** _URL_\n> Base URL for relative paths.\n\n**-d** _DOMAIN_, **--domain** _DOMAIN_\n> Limit to domain.\n\n**-k**, **--insecure**\n> Accept invalid certificates.\n\n**-s**, **--silent**\n> Suppress output."],["DESCRIPTION","**monolith** saves complete webpages as single HTML files. It embeds all assets - CSS, images, fonts, JavaScript - as data URLs within the HTML, creating a fully self-contained document.\n\nUnlike browser \"Save As\" which creates folders of assets, monolith produces one file that works offline. This is ideal for archiving, documentation, and sharing.\n\nThe isolation mode (-I) creates truly offline pages by removing external references. Combined with -j to remove JavaScript, it creates static snapshots without any network dependencies.\n\nAsset exclusion options reduce file size when full fidelity isn't needed. Excluding images or fonts significantly shrinks output. The tool handles CSS @import and relative URLs correctly.\n\nLocal files can be processed using file:// URLs. This enables converting existing HTML with external assets into self-contained documents.\n\nError handling with -e continues processing when individual resources fail to load, useful for pages with broken links or blocked resources."],["CAVEATS","Large pages with many assets produce large files. Base64 encoding increases size. Dynamic JavaScript content won't be captured. Some sites block automated access. Cookie-protected content not accessible. SPAs may not render completely."],["HISTORY","**monolith** was created by **Sunshine** (nicholaides) around **2019**. Written in Rust, it provides a modern alternative to wget/curl for webpage archiving. The single-file output format simplifies storage and sharing compared to traditional archival methods."],["INSTALL","```pacman: sudo pacman -S monolith```\n\n```apk: sudo apk add monolith```\n\n```brew: brew install monolith```\n\n```nix: nix profile install nixpkgs#monolith```"],["SEE ALSO","[wget](/man/wget)(1), [curl](/man/curl)(1), [httrack](/man/httrack)(1)"]],"e":["wget","curl","httrack"],"x":"monolith [https://example.com] -o [page.html]"},"needrestart":{"n":"needrestart","t":"checks which daemons need to be restarted after library upgrades","s":[["TAGLINE","checks which daemons need to be restarted after library upgrades"],["TLDR","**List** outdated processes\n\n```needrestart```\n\n**Interactively restart** services\n\n```sudo needrestart```\n\nList in **verbose** mode\n\n```needrestart -v```\n\nList in **quiet** mode\n\n```needrestart -q```\n\nCheck if **kernel** is outdated\n\n```needrestart -k```\n\nCheck if **CPU microcode** is outdated\n\n```needrestart -w```\n\nList in **batch** mode (non-interactive)\n\n```needrestart -b```\n\nUse specific **configuration file**\n\n```needrestart -c [path/to/config]```"],["SYNOPSIS","**needrestart** [**-v**|**-q**] [**-k**] [**-w**] [**-b**] [**-c** _config_]"],["PARAMETERS","**-v**\n> Verbose mode; show more details\n\n**-q**\n> Quiet mode; suppress output\n\n**-k**\n> Check if running kernel is outdated\n\n**-w**\n> Check if CPU microcode is outdated\n\n**-b**\n> Batch mode; non-interactive, suitable for scripts\n\n**-l**\n> List only; do not prompt for restart\n\n**-r _mode_**\n> Restart mode: i (interactive), a (automatic), l (list only)\n\n**-c _config_**\n> Use specified configuration file\n\n**-p**\n> Enable nagios plugin mode output\n\n**-f _frontend_**\n> Select frontend: debconf, stdio, readline"],["DESCRIPTION","**needrestart** checks which daemons need to be restarted after library upgrades. When shared libraries are updated, running processes may still use old versions in memory, potentially leaving security vulnerabilities unpatched.\n\nThe tool scans running processes for deleted library files (indicating updates) and identifies services that should be restarted. It can also check for outdated kernels and CPU microcode requiring a reboot."],["CAVEATS","Restarting services may cause brief outages. Kernel and microcode updates require a full reboot. Some processes may be difficult to identify or restart. Running with sudo enables interactive restart prompts."],["HISTORY","**needrestart** was developed by **Thomas Liske** for Debian-based systems to help administrators identify services needing restart after package updates. It integrates with apt to run automatically after upgrades."],["INSTALL","```dnf: sudo dnf install needrestart```"],["SEE ALSO","[systemctl](/man/systemctl)(1), [apt](/man/apt)(8), [checkrestart](/man/checkrestart)(1)"]],"e":["systemctl","apt","checkrestart"],"x":"needrestart"},"netsniff-ng":{"n":"netsniff-ng","t":"high-performance packet sniffer","s":[["TAGLINE","high-performance packet sniffer"],["TLDR","**Capture from an interface to a pcap file**\n\n```netsniff-ng --in [eth0] --out [capture.pcap]```\n\n**Replay a pcap file** onto the network\n\n```netsniff-ng --in [capture.pcap] --out [eth0]```\n\n**Apply a tcpdump/BPF filter** while capturing\n\n```netsniff-ng --in [eth0] --out [capture.pcap] \"[tcp port 80]\"```\n\n**Capture and print packets** to the console (no file)\n\n```netsniff-ng --in [eth0] --out - --verbose```\n\n**Rotate output** into a directory, one file per interval\n\n```netsniff-ng --in [eth0] --out [dir/] --interval [60sec]```\n\n**Set the kernel ring buffer size** for high-speed capture\n\n```netsniff-ng --in [eth0] --out [capture.pcap] --ring-size [64MiB]```"],["SYNOPSIS","**netsniff-ng** [_options_] [_filter-expression_]"],["PARAMETERS","**-i**, **-d**, **--in**, **--dev** _INPUT_\n> Input source: a network device, a pcap file, or **-** for stdin.\n\n**-o**, **--out** _OUTPUT_\n> Output sink: a network device, a pcap file, a directory (with --interval), or **-** for stdout.\n\n**-f**, **--filter** _EXPR_\n> Apply a low-level (BPF) or high-level (tcpdump-style) packet filter. A bare filter expression on the command line is also accepted.\n\n**--ring-size** _SIZE_\n> Set the mmap ring buffer size, e.g. **10MiB**, **1GiB**.\n\n**-F**, **--interval** _NUM_\n> When writing to a directory, start a new pcap file every NUM packets, or by time/size (e.g. **60sec**, **100MiB**).\n\n**-s**, **--silent**\n> Do not print captured packets to the console.\n\n**-V**, **--verbose**\n> Print/dump each captured packet in verbose form.\n\n**-T**, **--magic** _PCAP_MAGIC_\n> Set the pcap file format magic (link-layer/timestamp variant).\n\n**-b**, **--bind-cpu** _CPU_\n> Pin the capture process to the given CPU.\n\n**-H**, **--prio-high**\n> Run the process with high scheduling priority.\n\n**-V**, **--version**, **-h**, **--help**\n> Show version or help information."],["DESCRIPTION","**netsniff-ng** is a high-performance, zero-copy network analyzer, packet capture and replay tool. It uses the Linux kernel's **PACKET_MMAP** RX_RING/TX_RING interface to move packets between kernel and user space without copying, allowing capture and transmission close to line rate.\n\nIt can capture live traffic to a pcap file, replay a pcap back onto an interface, and rotate captures into per-interval files for long-running collection. Filtering accepts both raw BPF and tcpdump-style expressions."],["CAVEATS","Requires root (or **CAP_NET_RAW** / **CAP_NET_ADMIN**). It is part of the netsniff-ng toolkit, which also includes trafgen, mausezahn, ifpps, flowtop, and astraceroute. Some link types and timestamp formats depend on driver and kernel support."],["HISTORY","**netsniff-ng** was created by **Daniel Borkmann** in **2009** as a free, Linux-native, zero-copy packet analyzer, and grew into the broader netsniff-ng networking toolkit. It is licensed under the GPLv2."],["INSTALL","```apt: sudo apt install netsniff-ng```\n\n```dnf: sudo dnf install netsniff-ng```\n\n```pacman: sudo pacman -S netsniff-ng```\n\n```zypper: sudo zypper install netsniff-ng```\n\n```nix: nix profile install nixpkgs#netsniff-ng```"],["SEE ALSO","[tcpdump](/man/tcpdump)(1), [wireshark](/man/wireshark)(1), [tshark](/man/tshark)(1)"]],"e":["tcpdump","wireshark","tshark"],"x":"netsniff-ng --in [eth0] --out [capture.pcap]"},"numfmt":{"n":"numfmt","t":"converts numbers to and from human-readable representations","s":[["TAGLINE","converts numbers to and from human-readable representations"],["TLDR","**Convert bytes to human-readable**\n\n```numfmt --to=iec [1048576]```\n\n**Convert with IEC units** (Ki, Mi, Gi)\n\n```echo [1073741824] | numfmt --to=iec```\n\n**Convert with SI units** (K, M, G)\n\n```numfmt --to=si [1000000]```\n\n**Parse human-readable to number**\n\n```numfmt --from=iec [10M]```\n\n**Format specific field** in input\n\n```df -B1 | numfmt --header --field 2-4 --to=iec```\n\n**Add suffix**\n\n```numfmt --to=si --suffix=B [1000000]```\n\n**Set output padding**\n\n```numfmt --to=iec --padding=10 [1048576]```"],["SYNOPSIS","**numfmt** [_--from=UNIT_] [_--to=UNIT_] [_--field=N_] [_options_] [_numbers_]"],["PARAMETERS","**--from=** _UNIT_\n> Parse input units (auto, si, iec, iec-i).\n\n**--to=** _UNIT_\n> Convert to units (si, iec, iec-i).\n\n**--field=** _N_\n> Convert field N (default: 1).\n\n**--header**[=_N_]\n> Skip N header lines (default: 1).\n\n**--padding=** _N_\n> Pad output to N characters.\n\n**--suffix=** _STR_\n> Append suffix to output.\n\n**--format=** _FMT_\n> Printf-style format.\n\n**--round=** _METHOD_\n> Rounding: up, down, from-zero, towards-zero, nearest.\n\n**--grouping**\n> Use locale digit grouping (e.g., 1,000).\n\n**--delimiter=** _D_\n> Use D as field delimiter.\n\n**--invalid=** _MODE_\n> Handle invalid input: abort, fail, warn, ignore.\n\n**-d** _D_\n> Short for --delimiter.\n\n**--debug**\n> Print conversion warnings."],["DESCRIPTION","**numfmt** converts numbers to and from human-readable representations. It handles both SI units (powers of 1000: K, M, G) and IEC units (powers of 1024: Ki, Mi, Gi).\n\nInput parsing (--from) interprets suffixed numbers: 10K becomes 10000 (SI) or 10240 (IEC). Output conversion (--to) formats numbers with appropriate suffixes for readability.\n\nFor processing command output, --field selects which column to convert, and --header preserves header rows. This enables piping output from commands like df or ls through numfmt for human-readable conversion.\n\nThe --format option provides printf-style control: `%10.2f` for fixed-width decimal output. Combined with --grouping, numbers display with locale-appropriate thousands separators.\n\nRounding modes control how fractional values are handled. The default rounds to nearest, but ceiling (up) or floor (down) may be preferred for storage calculations."],["CAVEATS","SI and IEC units differ significantly at large values. The 'iec-i' format uses explicit \"Ki\" suffix while 'iec' uses just \"K\". Floating-point precision may affect very large numbers. Not all locales support grouping."],["HISTORY","**numfmt** is part of **GNU coreutils**, added to provide standardized number formatting across utilities. It addresses the inconsistency of human-readable output between different commands, providing a single tool for number conversion that respects both SI and IEC standards."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[printf](/man/printf)(1), [bc](/man/bc)(1), [units](/man/units)(1), [factor](/man/factor)(1)"]],"e":["printf","bc","units","factor"],"x":"numfmt --to=iec [1048576]"},"nxc-rdp":{"n":"nxc-rdp","t":"netExec RDP module","s":[["TAGLINE","netExec RDP module"],["TLDR","**Check RDP credentials**\n\n```nxc rdp [target] -u [user] -p [password]```\n\n**Screenshot on login**\n\n```nxc rdp [target] -u [user] -p [password] --screenshot```\n\n**Check NLA status**\n\n```nxc rdp [target] -u [user] -p [password] --nla```\n\n**Scan multiple hosts**\n\n```nxc rdp [targets.txt] -u [user] -p [password]```"],["SYNOPSIS","**nxc** **rdp** [_target_] [_options_]"],["PARAMETERS","_TARGET_\n> RDP server address.\n\n**-u** _USER_\n> Username.\n\n**-p** _PASSWORD_\n> Password.\n\n**--screenshot**\n> Capture screenshot on login.\n\n**--nla**\n> Check NLA status.\n\n**-H** _HASH_\n> NT hash for pass-the-hash.\n\n**--help**\n> Display help information."],["DESCRIPTION","**nxc rdp** is the NetExec RDP module. Tests Remote Desktop credentials.\n\nThe tool validates RDP access. Part of NetExec suite."],["CAVEATS","Security tool. Authorized use only. Part of NetExec."],["HISTORY","nxc rdp provides **RDP penetration testing** capabilities within NetExec."],["SEE ALSO","[nxc](/man/nxc)(1), [rdesktop](/man/rdesktop)(1), [xfreerdp](/man/xfreerdp)(1)"]],"e":["nxc","rdesktop","xfreerdp"],"x":"nxc rdp [target] -u [user] -p [password]"},"openssl-ts":{"n":"openssl-ts","t":"handles RFC 3161 Time Stamping Authority operations","s":[["TAGLINE","handles RFC 3161 Time Stamping Authority operations"],["TLDR","**Create timestamp request**\n\n```openssl ts -query -data [file] -out [request.tsq]```\n\n**Create timestamp response**\n\n```openssl ts -reply -queryfile [request.tsq] -signer [cert.pem] -out [response.tsr]```\n\n**Verify timestamp**\n\n```openssl ts -verify -data [file] -in [response.tsr] -CAfile [ca.pem]```\n\n**Print timestamp request**\n\n```openssl ts -query -in [request.tsq] -text```"],["SYNOPSIS","**openssl** **ts** _command_ [_options_]"],["PARAMETERS","**-query**\n> Create timestamp request.\n\n**-reply**\n> Create timestamp response.\n\n**-verify**\n> Verify timestamp response.\n\n**-data** _file_\n> Data file to timestamp.\n\n**-in** _file_\n> Input file.\n\n**-out** _file_\n> Output file.\n\n**-text**\n> Print in human-readable form.\n\n**-config** _FILE_\n> Specify a custom `openssl.cnf`.\n\n**-queryfile** _FILE_\n> Path to an existing TSQ when generating a reply (**-reply**) or during verification (**-verify**).\n\n**-digest** _HEX_\n> Provide a precomputed digest instead of hashing `-data`.\n\n**-no_nonce**\n> Do not include a nonce in the request.\n\n**-cert**\n> Request that the TSA include its certificate in the reply.\n\n**-signer** _FILE_\n> Signing certificate (PEM) used by **-reply**.\n\n**-inkey** _FILE_|_URI_\n> Private key matching `-signer`.\n\n**-chain** _FILE_\n> Extra certificates to include in the response chain.\n\n**-tspolicy** _OID_\n> Policy OID asserted by the TSA.\n\n**-untrusted** _FILES_\n> Untrusted intermediates for chain building during **-verify**.\n\n**-CAfile** _FILE_, **-CApath** _DIR_, **-CAstore** _URI_\n> Trust anchors used during **-verify**.\n\n**-attime** _TIMESTAMP_\n> Verify as if at a specific time.\n\n**-token_in**, **-token_out**\n> Read/write a bare PKCS#7 token rather than a full TimeStampResp."],["DESCRIPTION","**openssl ts** handles RFC 3161 Time Stamping Authority (TSA) operations. It creates timestamp requests (TSQ), generates responses (TSR) as a TSA server, and verifies existing TSRs. It is primarily used to prove that data existed in a specific form at a specific time — document notarization, code-signing countersignatures, and archival integrity.\n\nThe three operating modes are mutually exclusive:\n\n- **-query** — client-side request creation.\n- **-reply** — server-side response creation (requires a signer certificate and key).\n- **-verify** — client-side verification of a response against the original data (or digest) and a trust store."],["CAVEATS","Requires that the TSA certificate chains to a CA in the supplied trust store. Using **-no_nonce** makes the response replayable — avoid unless the protocol you are integrating with mandates it. In the OpenSSL 3 series this command is also exposed as `openssl-ts(1ssl)` in the manual."],["INSTALL","```apt: sudo apt install openssl```\n\n```dnf: sudo dnf install openssl```\n\n```pacman: sudo pacman -S openssl```\n\n```apk: sudo apk add openssl```\n\n```zypper: sudo zypper install openssl```\n\n```brew: brew install openssl```\n\n```nix: nix profile install nixpkgs#openssl```"],["SEE ALSO","[openssl](/man/openssl)(1), [gpg](/man/gpg)(1)"]],"e":["openssl","gpg"],"x":"openssl ts -query -data [file] -out [request.tsq]"},"pacremove":{"n":"pacremove","t":"convenience alias for **pactrans --remove** that removes packages","s":[["TAGLINE","convenience alias for **pactrans --remove** that removes packages"],["TLDR","This command is an alias of **pactrans --remove**\n\nView documentation for the original command\n\n```tldr pactrans```"],["SYNOPSIS","**pacremove** [_options_] _package(s)_"],["DESCRIPTION","**pacremove** is a convenience alias for **pactrans --remove** that removes packages from the system. It provides an alternative interface to package removal using the pactrans transaction system."],["SEE ALSO","[pactrans](/man/pactrans)(1), [pacman](/man/pacman)(8)"]],"e":["pactrans","pacman"],"x":"tldr pactrans"},"pamscale":{"n":"pamscale","t":"resizes PAM/PNM images using high-quality resampling","s":[["TAGLINE","resizes PAM/PNM images using high-quality resampling"],["TLDR","**Scale to specific size**\n\n```pamscale -width [800] -height [600] [input.pam] > [output.pam]```\n\n**Scale by factor**\n\n```pamscale [0.5] [input.pam] > [half.pam]```\n\n**Scale preserving aspect ratio**\n\n```pamscale -xyfit [800] [600] [input.pam] > [output.pam]```\n\n**Scale width only**\n\n```pamscale -xsize [800] [input.pam] > [output.pam]```\n\n**High quality scaling**\n\n```pamscale -filter=lanczos [0.5] [input.pam] > [output.pam]```"],["SYNOPSIS","**pamscale** [_options_] [_scale_] [_pamfile_]"],["PARAMETERS","**scale**\n> Scale factor (0.5 = half).\n\n**-width**, **-xsize** _n_\n> Output width.\n\n**-height**, **-ysize** _n_\n> Output height.\n\n**-xyfit** _w_ _h_\n> Fit within box, keep aspect.\n\n**-filter** _name_\n> Resampling filter.\n\n**-linear**\n> Linear interpolation."],["DESCRIPTION","**pamscale** resizes PAM/PNM images using high-quality resampling. It can scale by factor, to specific dimensions, or fit within a bounding box while preserving aspect ratio."],["FILTERS","```\npoint    - Nearest neighbor\nbox      - Box filter\ntriangle - Linear/bilinear\nlanczos  - High quality (default)\n```"],["CAVEATS","Upscaling may blur. Very small scales may lose detail. Large images need memory."],["HISTORY","pamscale is part of **Netpbm**, providing high-quality image scaling with various resampling algorithms."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pamenlarge](/man/pamenlarge)(1), [pamstretch](/man/pamstretch)(1), [pnmscale](/man/pnmscale)(1)"]],"e":["pamenlarge","pamstretch","pnmscale"],"x":"pamscale -width [800] -height [600] [input.pam] > [output.pam]"},"pio-debug":{"n":"pio-debug","t":"Start a debugging session for PlatformIO embedded projects","s":[["TAGLINE","Start a debugging session for PlatformIO embedded projects"],["TLDR","**Start debugging session**\n\n```pio debug```\n\n**Debug specific environment**\n\n```pio debug -e [uno]```\n\n**Debug with GDB interface**\n\n```pio debug --interface=gdb```\n\n**Debug with specific project directory**\n\n```pio debug -d [path/to/project]```"],["SYNOPSIS","**pio debug** [_options_]"],["PARAMETERS","**-e**, **--environment** _name_\n> Target environment from platformio.ini.\n\n**--interface** _type_\n> Debugger interface (gdb).\n\n**-d**, **--project-dir** _dir_\n> Path to PlatformIO project directory (default: current).\n\n**--upload-port** _port_\n> Upload port for the target board.\n\n**-v**, **--verbose**\n> Verbose output for troubleshooting.\n\n**--project-conf** _path_\n> Path to a specific platformio.ini file."],["DESCRIPTION","**pio debug** prepares a PlatformIO project for debugging or launches a debug server. It connects to on-chip debuggers (JTAG/SWD), enables setting breakpoints, stepping through code, and inspecting variables on embedded hardware.\n\nThe binary shortcut **piodebuggdb** is equivalent to `pio debug --interface=gdb`. Debug builds use the `debug_build_flags` from platformio.ini (typically `-O0 -g3 -ggdb3` for full symbol information)."],["INSTALL","```aur: yay -S pio```"],["SEE ALSO","[pio-run](/man/pio-run)(1), [pio-test](/man/pio-test)(1), [gdb](/man/gdb)(1)"]],"e":["pio-run","pio-test","gdb"],"x":"pio debug"},"poetry-init":{"n":"poetry-init","t":"Initialize a new pyproject.toml file","s":[["TAGLINE","Initialize a new pyproject.toml file"],["TLDR","**Initialize interactively** in the current directory\n\n```poetry init```\n\n**Non-interactive with defaults**\n\n```poetry init -n```\n\n**Specify name and description**\n\n```poetry init --name [myproject] --description \"[A cool project]\"```\n\n**Add dependencies during init**\n\n```poetry init --dependency [requests] --dev-dependency [pytest]```\n\n**Pin a Python version**\n\n```poetry init -n --python \"[^3.11]\"```"],["SYNOPSIS","**poetry init** [_options_]"],["PARAMETERS","**-n**, **--no-interaction**\n> Skip the interactive prompts; uses defaults for any value not provided.\n\n**--name** _name_\n> Package name (defaults to the current directory name).\n\n**--description** _text_\n> Package description.\n\n**--author** _author_\n> Author in **\"Name <email>\"** format. Repeatable.\n\n**--license** _spdx_\n> SPDX license identifier (e.g. **MIT**, **Apache-2.0**).\n\n**--python** _constraint_\n> Python version constraint (e.g. **^3.11**, **>=3.10,<4**).\n\n**--dependency** _name[@version]_\n> Add a runtime dependency. Repeatable.\n\n**--dev-dependency** _name[@version]_\n> Add a development dependency. Repeatable.\n\n**--directory** _path_\n> Run init in a different directory.\n\n**--help**\n> Display help."],["DESCRIPTION","**poetry init** creates a new **pyproject.toml** in the target directory describing the project's metadata and dependencies. The interactive flow walks through the project name, version, author, license, Python constraint, and any initial runtime/dev dependencies, validating package names against PyPI as you go.\n\nIt does not create a virtual environment, install dependencies, or write a lock file — run **poetry install** afterwards for that."],["CAVEATS","Poetry 2.0 (2025) writes pyproject.toml using the standard **[project]** table per PEP 621, while older versions use the legacy **[tool.poetry]** table. The exact layout depends on your installed Poetry version. Run **poetry --version** to check."],["HISTORY","**poetry** was created by **Sébastien Eustace** in **2018** as a dependency manager and packaging tool for Python projects. The **init** subcommand has been part of Poetry since the early releases."],["INSTALL","```dnf: sudo dnf install poetry```\n\n```apk: sudo apk add poetry```\n\n```brew: brew install poetry```\n\n```nix: nix profile install nixpkgs#poetry```"],["SEE ALSO","[poetry-install](/man/poetry-install)(1), [poetry-new](/man/poetry-new)(1), [poetry](/man/poetry)(1), [pip](/man/pip)(1)"]],"e":["poetry-install","poetry-new","poetry","pip"],"x":"poetry init"},"pygbag":{"n":"pygbag","t":"Package Pygame games for web browsers","s":[["TAGLINE","Package Pygame games for web browsers"],["TLDR","**Package game for web**\n\n```pygbag [path/to/game]```\n\n**Specify output directory**\n\n```pygbag --build [path/to/game]```\n\n**Run development server**\n\n```pygbag --dev [path/to/game]```\n\n**Custom archive name**\n\n```pygbag --archive [name] [path/to/game]```\n\n**Specify custom port for dev server**\n\n```pygbag --port [8000] [path/to/game]```\n\n**Specify template**\n\n```pygbag --template [template.html] [path/to/game]```"],["SYNOPSIS","**pygbag** [_--build_] [_--dev_] [_options_] _path_"],["PARAMETERS","**--build**\n> Build only, no server.\n\n**--dev**\n> Development mode.\n\n**--archive** _NAME_\n> Archive filename.\n\n**--template** _FILE_\n> Custom HTML template.\n\n**--port** _PORT_\n> Port for the development server (default: 8000).\n\n**--cdn** _URL_\n> CDN base URL.\n\n**--title** _TITLE_\n> Page title.\n\n**--ume_block** _SIZE_\n> Set the maximum cache block size."],["DESCRIPTION","**pygbag** packages Pygame applications for deployment in web browsers by compiling Python code to WebAssembly using Emscripten and Pyodide. The resulting build runs directly in modern browsers without plugins, making it straightforward to share Python games and multimedia applications online.\n\nThe tool includes a built-in development server for local testing before deployment. Games must use async Python patterns with **asyncio** to work within the browser's event loop model. pygbag handles asset bundling, HTML template generation, and the WebAssembly compilation pipeline, producing a self-contained web application from a standard Pygame project."],["CAVEATS","Not all Python libraries are supported in the WebAssembly environment; C extensions must be specifically compiled for Emscripten. The main game loop must use `asyncio` with `await asyncio.sleep(0)` for browser compatibility. The entry point must be named `main.py`. Resulting builds can be large due to the Python runtime being included."],["HISTORY","**pygbag** was created to bring Pygame games to web browsers using WebAssembly and Pyodide."],["SEE ALSO","[pygame](/man/pygame)(1), [python](/man/python)(1), [emcc](/man/emcc)(1)"]],"e":["pygame","python","emcc"],"x":"pygbag [path/to/game]"},"rawtoppm":{"n":"rawtoppm","t":"Convert raw RGB data to PPM format","s":[["TAGLINE","Convert raw RGB data to PPM format"],["TLDR","**Convert raw RGB to PPM**\n\n```rawtoppm [width] [height] [input.raw] > [output.ppm]```\n\n**Convert with interleaved format**\n\n```rawtoppm -interrow [width] [height] [input.raw] > [output.ppm]```"],["SYNOPSIS","**rawtoppm** [_options_] _width_ _height_ [_file_]"],["PARAMETERS","_width_\n> Image width in pixels.\n\n_height_\n> Image height in pixels.\n\n**-headerskip** _bytes_\n> Skip header bytes.\n\n**-interrow**\n> Row-interleaved RGB.\n\n**-interpixel**\n> Pixel-interleaved RGB."],["DESCRIPTION","**rawtoppm** converts raw RGB color data into Portable Pixmap (PPM) format by interpreting a byte stream as red, green, and blue pixel values arranged according to specified image dimensions. The tool handles different memory layouts for the color channels: the default expects pixel-interleaved data (RGBRGB...), while **-interrow** reads row-interleaved data where each color plane is stored as a complete row before the next.\n\nSince raw data contains no metadata, both width and height must be specified as arguments. The **-headerskip** option skips non-image data at the beginning of the file. It is part of the Netpbm image processing toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[rawtopgm](/man/rawtopgm)(1), [ppmtorgb3](/man/ppmtorgb3)(1)"]],"e":["rawtopgm","ppmtorgb3"],"x":"rawtoppm [width] [height] [input.raw] > [output.ppm]"},"reg":{"n":"reg","t":"Docker Registry v2 command-line client","s":[["TAGLINE","Docker Registry v2 command-line client"],["TLDR","**List repositories** on a registry\n\n```reg ls [registry.example.com]```\n\n**List tags** for an image\n\n```reg tags [registry.example.com/image]```\n\n**Show manifest** JSON\n\n```reg manifest [registry.example.com/image:tag]```\n\n**Show image digest**\n\n```reg digest [registry.example.com/image:tag]```\n\n**Delete** a reference\n\n```reg rm [registry.example.com/image@sha256:digest]```\n\n**Download a layer** to a file\n\n```reg layer -o [layer.tar] [registry.example.com/image@sha256:digest]```"],["SYNOPSIS","**reg** [*global-flags*] *command* [*args*]"],["DESCRIPTION","**reg** is a Docker Registry HTTP API v2 client for listing repositories and tags, fetching manifests and digests, deleting references, downloading layers, serving a simple registry UI, and optionally querying a Clair server for vulnerabilities. It can use Docker config credentials or explicit **-u**/**-p** flags.\n\nIt targets Open Source Distribution-style registries. **reg ls** does not work against Docker Hub's divergent API."],["PARAMETERS","**ls** *registry*\n\n> List repositories (can be slow on large registries).\n\n**tags** *repository*\n\n> List tags for a repository.\n\n**manifest** *ref*\n\n> Print image manifest JSON.\n\n**digest** *ref*\n\n> Print content digest.\n\n**layer** *ref*\n\n> Download a layer blob (**-o** for output path).\n\n**rm** *ref*\n\n> Delete a manifest reference (registry must allow deletes).\n\n**vulns** **--clair** *url* *ref*\n\n> Vulnerability report via Clair.\n\n**server**\n\n> Run static UI server for a registry.\n\n**version**\n\n> Show version.\n\n**-u**, **--username** *user*\n\n> Registry username.\n\n**-p**, **--password** *pass*\n\n> Registry password.\n\n**-k**, **--insecure**\n\n> Skip TLS certificate verification.\n\n**-f**, **--force-non-ssl**\n\n> Allow non-TLS registries.\n\n**-d**\n\n> Debug logging.\n\n**--timeout** *duration*\n\n> HTTP timeout (default 1m)."],["CAVEATS","Deleting images requires registry configuration to enable deletes. Hub.docker.com listing is unsupported. Treat credentials carefully; prefer Docker credential helpers when possible."],["INSTALL","```brew: brew install reg```\n\n```nix: nix profile install nixpkgs#reg```"],["SEE ALSO","[docker](/man/docker)(1), [skopeo](/man/skopeo)(1), [crane](/man/crane)(1)"],["RESOURCES","```[Source code](https://github.com/genuinetools/reg)```\n\n```[Homepage](https://r.j3ss.co)```"]],"e":["docker","skopeo","crane"],"x":"reg ls [registry.example.com]"},"run0":{"n":"run0","t":"Run commands as another user via systemd","s":[["TAGLINE","Run commands as another user via systemd"],["TLDR","Run command as **root**\n\n```run0 [command]```\n\nRun as **specific user**\n\n```run0 -u [username] [command]```\n\nRun as user and **group**\n\n```run0 -u [username] -g [group] [command]```"],["SYNOPSIS","**run0** [**-u**|**--user** _user_] [**-g**|**--group** _group_] [_command_]"],["PARAMETERS","**-u, --user _user_**\n> Run as specified user\n\n**-g, --group _group_**\n> Run with specified group\n\n**--no-ask-password**\n> Don't prompt for password\n\n**--machine _machine_**\n> Execute on container/VM\n\n**-D, --chdir _path_**\n> Change working directory before running\n\n**--setenv _NAME=VALUE_**\n> Set environment variable for the invoked process\n\n**--background _color_**\n> Change terminal background tint (set to empty to disable)\n\n**--nice _N_**\n> Run command with adjusted scheduling priority"],["DESCRIPTION","**run0** elevates privileges without being a SUID binary. Unlike sudo, it authenticates via polkit and spawns commands through a systemd service, providing better security isolation.\n\nThe tool is designed as a more secure alternative to sudo, avoiding the security concerns of SUID binaries while maintaining familiar privilege escalation semantics."],["CAVEATS","Requires systemd 256+. Authentication through polkit. Environment handling differs from sudo. Some sudo features not available."],["HISTORY","**run0** was introduced in **systemd 256** by Lennart Poettering as a secure alternative to sudo. It addresses SUID-related security concerns by using polkit for authentication and service-based command execution."],["INSTALL","```apt: sudo apt install systemd```\n\n```dnf: sudo dnf install systemd```\n\n```pacman: sudo pacman -S systemd```\n\n```zypper: sudo zypper install systemd```\n\n```brew: brew install systemd```\n\n```nix: nix profile install nixpkgs#systemd```"],["SEE ALSO","[sudo](/man/sudo)(8), [pkexec](/man/pkexec)(1), [doas](/man/doas)(1), [systemd-run](/man/systemd-run)(1)"]],"e":["sudo","pkexec","doas","systemd-run"],"x":"run0 [command]"},"sdiff":{"n":"sdiff","t":"Side-by-side file comparison and merge","s":[["TAGLINE","Side-by-side file comparison and merge"],["TLDR","**Compare files side by side**\n\n```sdiff [file1] [file2]```\n\n**Interactive merge**\n\n```sdiff -o [output] [file1] [file2]```\n\n**Set output width**\n\n```sdiff -w [120] [file1] [file2]```\n\n**Suppress common lines**\n\n```sdiff -s [file1] [file2]```\n\n**Ignore case differences**\n\n```sdiff -i [file1] [file2]```\n\n**Ignore whitespace changes**\n\n```sdiff -b [file1] [file2]```\n\n**Ignore all whitespace**\n\n```sdiff -W [file1] [file2]```\n\n**Tab-expand output**\n\n```sdiff -t [file1] [file2]```"],["SYNOPSIS","**sdiff** [_-o output_] [_-w width_] [_-sbBiW_] [_options_] _file1_ _file2_"],["PARAMETERS","**-o** _FILE_, **--output** _FILE_\n> Interactive merge to output file.\n\n**-w** _NUM_, **--width** _NUM_\n> Output width in columns.\n\n**-s**, **--suppress-common-lines**\n> Don't show identical lines.\n\n**-l**, **--left-column**\n> Show only left column for common lines.\n\n**-b**, **--ignore-space-change**\n> Ignore changes in whitespace amount.\n\n**-B**, **--ignore-blank-lines**\n> Ignore blank line changes.\n\n**-i**, **--ignore-case**\n> Case-insensitive comparison.\n\n**-W**, **--ignore-all-space**\n> Ignore all whitespace.\n\n**-t**, **--expand-tabs**\n> Expand tabs to spaces.\n\n**--strip-trailing-cr**\n> Strip trailing carriage returns.\n\n**-a**, **--text**\n> Treat all files as text.\n\n**-d**, **--minimal**\n> Find minimal set of changes.\n\n**-H**, **--speed-large-files**\n> Heuristics for large files."],["DESCRIPTION","**sdiff** compares two files side by side. It displays corresponding lines in parallel columns, marking differences with symbols.\n\nOutput symbols indicate relationship: blank (identical), | (different), < (only in left), > (only in right). This visual format makes differences immediately apparent.\n\nInteractive merge mode (-o) creates a combined file from the two inputs. At each difference, you choose: left (l), right (r), edit left (el), edit right (er), or both (b). This enables manual resolution of differences.\n\nWidth setting (-w) controls output columns. Narrow widths truncate lines; wider widths show more content. Terminal width is commonly used (e.g., -w 80 or -w $COLUMNS).\n\nSuppressing common lines (-s) shows only differences, useful for focusing on changes in files that are mostly identical.\n\nWhitespace and case options help compare files with formatting differences that aren't substantive."],["CAVEATS","Binary files display poorly. Very long lines may be truncated. Interactive mode requires terminal. Wide files need wide terminal or scrolling. Not as feature-rich as diff for scripting."],["HISTORY","**sdiff** is part of GNU diffutils, developed by the Free Software Foundation. Side-by-side diff originated in Unix research, providing visual comparison before graphical diff tools existed. The GNU version added interactive merge mode and various comparison options."],["INSTALL","```apt: sudo apt install diffutils```\n\n```dnf: sudo dnf install diffutils```\n\n```pacman: sudo pacman -S diffutils```\n\n```apk: sudo apk add diffutils```\n\n```zypper: sudo zypper install diffutils```\n\n```brew: brew install diffutils```\n\n```nix: nix profile install nixpkgs#diffutils```"],["SEE ALSO","[diff](/man/diff)(1), [diff3](/man/diff3)(1), [cmp](/man/cmp)(1), [vimdiff](/man/vimdiff)(1)"]],"e":["diff","diff3","cmp","vimdiff"],"x":"sdiff [file1] [file2]"},"sslstrip":{"n":"sslstrip","t":"","s":[["TLDR","Start **SSL stripping** on default port\n\n```sslstrip```\n\nListen on **specific port**\n\n```sslstrip --listen=8080```\n\nLog **all SSL** traffic\n\n```sslstrip --ssl --listen=8080```\n\nLog **all HTTP and SSL** traffic\n\n```sslstrip --listen=8080 --all```\n\nWrite logs to **file**\n\n```sslstrip --listen=8080 --write=output.log```\n\nDisplay **help**\n\n```sslstrip --help```"],["SYNOPSIS","**sslstrip** [_options_]"],["DESCRIPTION","**sslstrip** is a security testing tool that performs SSL stripping attacks by acting as a proxy between the victim and the server. It converts HTTPS links to HTTP, allowing interception of traffic that would otherwise be encrypted. This tool is used for authorized penetration testing and security research."],["PARAMETERS","**-l, --listen PORT**\n> Port to listen on (default: 10000)\n\n**-a, --all**\n> Log all SSL and HTTP traffic\n\n**-s, --ssl**\n> Log all SSL traffic to and from server\n\n**-w, --write FILE**\n> Specify log file path\n\n**-p, --post**\n> Log only HTTP POST requests (default)\n\n**-f, --favicon**\n> Substitute a lock favicon on secure requests\n\n**-k, --killsessions**\n> Kill sessions in progress\n\n**-h, --help**\n> Display help information"],["CAVEATS","This tool is intended for authorized security testing only. Unauthorized use against systems you do not own or have permission to test is illegal. Requires ARP spoofing or similar MITM setup to intercept traffic. Modern browsers with HSTS preloading may prevent this attack."],["HISTORY","**sslstrip** was created by Moxie Marlinspike and demonstrated at Black Hat DC 2009 to illustrate vulnerabilities in SSL/TLS implementation in web applications."],["INSTALL","```nix: nix profile install nixpkgs#sslstrip```"],["SEE ALSO","[arpspoof](/man/arpspoof)(8), [ettercap](/man/ettercap)(8), [mitmproxy](/man/mitmproxy)(1), [dsniff](/man/dsniff)(8), [bettercap](/man/bettercap)(1)"],["RESOURCES","```[Source code](https://github.com/moxie0/sslstrip)```"]],"e":["arpspoof","ettercap","mitmproxy","dsniff","bettercap"],"x":"sslstrip"},"syft":{"n":"syft","t":"Generate software bill of materials from images","s":[["TAGLINE","Generate software bill of materials from images"],["TLDR","**Generate SBOM for container image**\n\n```syft [alpine:latest]```\n\n**Generate SBOM in JSON format**\n\n```syft [alpine:latest] -o json```\n\n**Generate CycloneDX format SBOM**\n\n```syft [alpine:latest] -o cyclonedx-json```\n\n**Generate SPDX format SBOM**\n\n```syft [alpine:latest] -o spdx-json```\n\n**Scan a local directory**\n\n```syft dir:[/path/to/project]```\n\n**Scan a Docker archive**\n\n```syft docker-archive:[image.tar]```\n\n**Output multiple formats**\n\n```syft [image] -o spdx-json=[sbom.spdx.json] -o cyclonedx-json=[sbom.cdx.json]```"],["SYNOPSIS","**syft** [_source_] [_options_]\n\n**syft** _command_ [_options_]"],["PARAMETERS","**-o**, **--output** _format_\n> Output format: syft-table, syft-json, cyclonedx-json, cyclonedx-xml, spdx-json, spdx-tag-value, github-json.\n\n**-s**, **--scope** _scope_\n> Layer selection: squashed (default), all-layers, deep-squashed.\n\n**--platform** _platform_\n> Platform for container images (e.g., linux/arm64).\n\n**--source-name** _name_\n> Set name of target being analyzed.\n\n**--source-version** _version_\n> Set version of target being analyzed.\n\n**--file** _path_\n> Write output to file instead of stdout.\n\n**--from** _source_\n> Explicitly specify the source type (e.g., docker, registry, dir).\n\n**-t**, **--template** _file_\n> Path to Go template file for custom output.\n\n**-q**, **--quiet**\n> Suppress all logging output.\n\n**-v**, **--verbose**\n> Increase verbosity (-v = info, -vv = debug).\n\n**--select-catalogers** _list_\n> Add, remove, and filter catalogers.\n\n**--exclude** _glob_\n> Exclude paths from scanning (can be repeated).\n\n**-c**, **--config** _file_\n> Path to configuration file.\n\n**-h**, **--help**\n> Display help information.\n\n**--version**\n> Display version information."],["SOURCE TYPES","**docker:**_image_ - Use Docker daemon\n\n**podman:**_image_ - Use Podman daemon\n\n**registry:**_image_ - Pull from registry directly\n\n**docker-archive:**_file_ - Docker save tarball\n\n**oci-archive:**_file_ - OCI archive tarball\n\n**oci-dir:**_path_ - OCI layout directory\n\n**dir:**_path_ - Filesystem directory\n\n**file:**_path_ - Single file"],["DESCRIPTION","**syft** is a CLI tool for generating Software Bill of Materials (SBOM) from container images and filesystems. It identifies packages, libraries, and dependencies across multiple ecosystems.\n\nSupported ecosystems include container images (Docker, OCI, Podman, Singularity), programming languages (Java, Python, JavaScript, Ruby, Go, Rust, .NET), and Linux distributions (Alpine, Debian, RHEL, and more).\n\nOutput formats include industry standards like CycloneDX and SPDX for compliance requirements, as well as native formats for integration with Anchore tools. Generated SBOMs can be used with vulnerability scanners like Grype."],["CAVEATS","Accuracy depends on package metadata quality in images. Some dynamically linked or vendored dependencies may not be detected. Large images take significant time to scan. Registry authentication may be required for private images."],["HISTORY","**syft** was created by Anchore as an open-source SBOM generation tool. It addresses growing supply chain security requirements and software transparency regulations. The tool supports multiple SBOM standards to meet compliance needs across different industries."],["INSTALL","```pacman: sudo pacman -S syft```\n\n```apk: sudo apk add syft```\n\n```zypper: sudo zypper install syft```\n\n```brew: brew install syft```\n\n```nix: nix profile install nixpkgs#syft```"],["SEE ALSO","[grype](/man/grype)(1), [docker](/man/docker)(1), [trivy](/man/trivy)(1), [cosign](/man/cosign)(1)"]],"e":["grype","docker","trivy","cosign"],"x":"syft [alpine:latest]"},"sysbench":{"n":"sysbench","t":"Multi-threaded system benchmark tool","s":[["TAGLINE","Multi-threaded system benchmark tool"],["TLDR","**CPU benchmark**\n\n```sysbench cpu run```\n\n**CPU benchmark with threads**\n\n```sysbench cpu --threads=[4] run```\n\n**Memory benchmark**\n\n```sysbench memory run```\n\n**File I/O prepare**\n\n```sysbench fileio --file-total-size=[10G] prepare```\n\n**File I/O benchmark**\n\n```sysbench fileio --file-total-size=[10G] --file-test-mode=[rndrw] run```\n\n**MySQL benchmark**\n\n```sysbench oltp_read_write --mysql-host=[localhost] --mysql-user=[user] --mysql-password=[pass] --mysql-db=[test] run```\n\n**Cleanup test files**\n\n```sysbench fileio --file-total-size=[10G] cleanup```\n\n**CPU benchmark with custom prime limit**\n\n```sysbench cpu --cpu-max-prime=[20000] --threads=[4] run```"],["SYNOPSIS","**sysbench** _test_ [_options_] [**prepare**|**run**|**cleanup**]"],["PARAMETERS","**cpu**\n> CPU test.\n\n**memory**\n> Memory test.\n\n**fileio**\n> File I/O test.\n\n**oltp_read_write**\n> Database OLTP test.\n\n**oltp_read_only**\n> Read-only database test.\n\n**threads**\n> Thread scheduler performance test.\n\n**mutex**\n> Mutex contention performance test.\n\n**--threads** _N_\n> Number of threads (default: 1).\n\n**--time** _SECONDS_\n> Test duration (default: 10).\n\n**--events** _N_\n> Maximum number of events (default: 0, unlimited).\n\n**--file-total-size** _SIZE_\n> Total file size.\n\n**--file-test-mode** _MODE_\n> I/O mode (seqwr, seqrd, rndrw, etc.).\n\n**--mysql-host** _HOST_\n> MySQL host.\n\n**--mysql-user** _USER_\n> MySQL user.\n\n**--mysql-password** _PASS_\n> MySQL password.\n\n**--mysql-db** _DB_\n> MySQL database name.\n\n**--cpu-max-prime** _N_\n> Upper limit for prime number calculation in CPU test (default: 10000).\n\n**--memory-block-size** _SIZE_\n> Size of memory block for memory test (default: 1K).\n\n**--memory-total-size** _SIZE_\n> Total size of data to transfer in memory test (default: 100G).\n\n**--memory-oper** _OP_\n> Memory operation type: read or write (default: write).\n\n**--memory-access-mode** _MODE_\n> Memory access mode: seq or rnd (default: seq).\n\n**--file-num** _N_\n> Number of files to create (default: 128).\n\n**--file-extra-flags** _FLAGS_\n> Additional flags for opening files: sync, dsync, direct.\n\n**--percentile** _N_\n> Percentile to calculate in latency statistics (default: 95).\n\n**--report-interval** _SECONDS_\n> Periodically report intermediate statistics (0 = disabled).\n\n**prepare**\n> Prepare test data (required for fileio and database tests).\n\n**run**\n> Run the benchmark test.\n\n**cleanup**\n> Clean up test data created during prepare."],["DESCRIPTION","**sysbench** is a multi-threaded benchmarking tool for evaluating system performance across CPU, memory, file I/O, and database workloads. It provides consistent, reproducible measurements that allow meaningful comparisons between different hardware configurations, kernel versions, or tuning parameters.\n\nThe tool includes built-in test types for common benchmarks. CPU tests stress processors using prime number calculations to measure computational throughput. Memory tests measure bandwidth and latency across sequential and random access patterns. File I/O tests support multiple modes including sequential writes, sequential reads, and random read/write mixes, requiring a preparation step to create test files before running and a cleanup step afterward.\n\nDatabase benchmarks simulate OLTP (Online Transaction Processing) workloads against MySQL or PostgreSQL, executing realistic mixes of select, insert, update, and delete queries. All test types support multi-threaded execution to measure how performance scales with parallelism, revealing bottlenecks and contention points. Custom Lua scripts can define additional workloads beyond the built-in tests."],["CAVEATS","Results depend on test parameters. Synthetic benchmarks don't reflect all workloads. File tests need disk space. Database tests need setup."],["HISTORY","**sysbench** was created by **Alexey Kopytov** at MySQL AB around **2004**. It became a standard tool for MySQL benchmarking and general system performance testing."],["INSTALL","```apt: sudo apt install sysbench```\n\n```dnf: sudo dnf install sysbench```\n\n```pacman: sudo pacman -S sysbench```\n\n```apk: sudo apk add sysbench```\n\n```zypper: sudo zypper install sysbench```\n\n```brew: brew install sysbench```\n\n```nix: nix profile install nixpkgs#sysbench```"],["SEE ALSO","[fio](/man/fio)(1), [stress-ng](/man/stress-ng)(1), [pgbench](/man/pgbench)(1), [iperf](/man/iperf)(1)"]],"e":["fio","stress-ng","pgbench","iperf"],"x":"sysbench cpu run"},"systemctl-try-reload-or-restart":{"n":"systemctl-try-reload-or-restart","t":"Reload or restart running units only","s":[["TAGLINE","Reload or restart running units only"],["TLDR","**Reload or restart** unit (if running)\n\n```systemctl try-reload-or-restart [unit]```\n\nReload or restart **multiple** units\n\n```systemctl try-reload-or-restart [unit1 unit2 ...]```\n\nReload or restart by **pattern**\n\n```systemctl try-reload-or-restart '[pattern]'```"],["SYNOPSIS","**systemctl try-reload-or-restart** _PATTERN_..."],["DESCRIPTION","**systemctl try-reload-or-restart** attempts to reload configuration for running units if they support reloading, otherwise restarts them. Unlike `reload-or-restart`, this command only affects units that are currently active.\n\nIf a unit is not running, no action is taken. This is useful for applying configuration changes without starting stopped services."],["CAVEATS","Does nothing for inactive units. Reload only works if the unit has ExecReload defined. If reload isn't supported, the unit is restarted."],["ALIASES","This command has several aliases: `condreload`, `force-reload`, `reload-or-try-restart`."],["HISTORY","The **try-reload-or-restart** subcommand provides the safest way to apply configuration changes to services, avoiding unintended service starts."],["SEE ALSO","[systemctl-reload-or-restart](/man/systemctl-reload-or-restart)(1), [systemctl-reload](/man/systemctl-reload)(1), [systemctl-restart](/man/systemctl-restart)(1)"]],"e":["systemctl-reload-or-restart","systemctl-reload","systemctl-restart"],"x":"systemctl try-reload-or-restart [unit]"},"terraform-untaint":{"n":"terraform-untaint","t":"Remove the tainted status from a Terraform resource instance","s":[["TAGLINE","Remove the tainted status from a Terraform resource instance"],["TLDR","**Remove taint** from a resource so it will not be replaced\n\n```terraform untaint aws_instance.example```\n\n**Untaint even if missing** from state\n\n```terraform untaint -allow-missing module.foo.resource.bar```\n\n**Untaint without acquiring state lock**\n\n```terraform untaint -lock=false aws_instance.example```"],["SYNOPSIS","**terraform** **untaint** [_options_] _address_"],["DESCRIPTION","**terraform untaint** clears the tainted flag from a resource instance in the state file. The resource will be treated normally on the next apply (no forced replacement). It does not modify remote infrastructure—only state.\n\nLike `terraform taint`, this command is deprecated in favor of more explicit mechanisms such as `-replace` on apply."],["PARAMETERS","**-allow-missing**\n> Succeed even if the address does not exist in state.\n\n**-lock**=_true|false_\n> Control state locking (default true). Use `-lock=false` to disable.\n\n**-lock-timeout**=_duration_\n> How long to wait for a lock (e.g. `3s`).\n\n**-no-color**\n> Disable terminal color sequences in output."],["CAVEATS","Deprecated. Prefer `terraform apply -replace=...` when you need to force replacement. Untaint only removes the state marker; it does not repair remote objects."],["INSTALL","```pacman: sudo pacman -S terraform```\n\n```nix: nix profile install nixpkgs#terraform```"],["SEE ALSO","[terraform](/man/terraform)(1), [terraform-apply](/man/terraform-apply)(1), [terraform-taint](/man/terraform-taint)(1)"],["RESOURCES","```[Documentation](https://developer.hashicorp.com/terraform/cli/commands/untaint)```"]],"e":["terraform","terraform-apply","terraform-taint"],"x":"terraform untaint aws_instance.example"},"tfmtodit":{"n":"tfmtodit","t":"Convert TeX font metrics for groff","s":[["TAGLINE","Convert TeX font metrics for groff"],["TLDR","**Create a font description file** from a TeX font metrics file\n\n```tfmtodit [tfm_file] [map_file] [font]```\n\n**Create a special font** description file\n\n```tfmtodit -s [tfm_file] [map_file] [font]```\n\n**Use a gf file** for additional font metric information\n\n```tfmtodit -g [gf_file] [tfm_file] [map_file] [font]```\n\n**Specify a skew character** position\n\n```tfmtodit -k [skewchar] [tfm_file] [map_file] [font]```\n\n**Display the version** number\n\n```tfmtodit -v```"],["SYNOPSIS","**tfmtodit** [**-sv**] [**-g** _gf_file_] [**-k** _skewchar_] _tfm_file_ _map_file_ _font_"],["PARAMETERS","**-s**\n> Add the special directive to the font description file. Use when the font should be searched whenever a glyph is not found in the current font.\n\n**-g** _gf_file_\n> Use the gf file produced by Metafont containing special and numspecial commands to obtain additional font metric information.\n\n**-k** _skewchar_\n> Specify the skew character position. Any kerns whose second component is the skew character are ignored. May be given in decimal, octal (leading 0), or hexadecimal (leading 0x).\n\n**-v**\n> Print the version number and exit."],["DESCRIPTION","**tfmtodit** creates font description files for use with groff's DVI output device. It converts TeX Font Metrics (TFM) files into the format required by groff.\n\nThe _tfm_file_ is the TeX font metric file for the font. The _map_file_ assigns groff character identifiers to glyph indices in the font, consisting of lines in the form `i c1 ... cn` where i is the glyph position and c1 through cn are glyph identifiers. Output is written in groff_font(5) format to the specified _font_ file.\n\nFor math typesetting, groff requires additional font metric information not present in TFM files. This is because TeX uses separate math italic fonts while groff uses normal italic fonts for math. The additional information comes from the math_fit macro arguments in Metafont programs for Computer Modern fonts."],["CAVEATS","The tool is specifically designed for the DVI output device and may not work correctly with other groff output devices. Proper math typesetting requires supplementary font metric data from Metafont gf files via the **-g** option."],["HISTORY","**tfmtodit** is part of the **groff** (GNU troff) project, which began in **1990** as a free software replacement for the Unix troff typesetting system. The tool bridges the gap between TeX font metrics and groff's font description format, enabling use of TeX fonts with groff's DVI output."],["INSTALL","```apt: sudo apt install groff```\n\n```dnf: sudo dnf install groff```\n\n```pacman: sudo pacman -S groff```\n\n```apk: sudo apk add groff```\n\n```zypper: sudo zypper install groff```\n\n```brew: brew install groff```\n\n```nix: nix profile install nixpkgs#groff```"],["SEE ALSO","[groff](/man/groff)(1), [grodvi](/man/grodvi)(1), [groff_font](/man/groff_font)(5), [troff](/man/troff)(1)"]],"e":["groff","grodvi","groff_font","troff"],"x":"tfmtodit [tfm_file] [map_file] [font]"},"tuned":{"n":"tuned","t":"Dynamic system performance tuning daemon","s":[["TAGLINE","Dynamic system performance tuning daemon"],["TLDR","**Start the tuned daemon**\n\n```sudo systemctl start tuned```\n\n**Enable tuned at boot**\n\n```sudo systemctl enable tuned```\n\n**List available profiles**\n\n```tuned-adm list```\n\n**Show active profile**\n\n```tuned-adm active```\n\n**Apply a performance profile**\n\n```sudo tuned-adm profile [throughput-performance]```\n\n**Get recommended profile**\n\n```tuned-adm recommend```\n\n**Disable tuning**\n\n```sudo tuned-adm off```"],["SYNOPSIS","**tuned** [_options_]\n\n**tuned-adm** _command_ [_arguments_]"],["PROFILES","**balanced**: General purpose; balances performance and power.\n\n**throughput-performance**: Maximum throughput for servers.\n\n**latency-performance**: Minimize latency for real-time workloads.\n\n**powersave**: Reduce power consumption.\n\n**virtual-guest**: Optimized for virtual machine guests.\n\n**virtual-host**: Optimized for hypervisor hosts.\n\n**network-latency**: Low latency network tuning.\n\n**network-throughput**: High network throughput."],["TUNED-ADM COMMANDS","**list**: Show available profiles.\n\n**active**: Display currently active profile.\n\n**profile** _name_: Activate specified profile.\n\n**recommend**: Suggest optimal profile for system.\n\n**off**: Disable all tuning.\n\n**verify**: Check if current settings match profile."],["DESCRIPTION","**tuned** is a daemon that monitors system usage and dynamically adjusts system settings to optimize performance. It uses profiles that configure CPU governors, disk I/O schedulers, kernel parameters, and power management settings.\n\nThe daemon can operate in static mode (applying fixed settings from a profile) or dynamic mode (adjusting settings based on real-time system activity). Profiles are stored in /etc/tuned/ and /usr/lib/tuned/.\n\nManagement is done through tuned-adm, which communicates with the daemon to switch profiles and query status. Custom profiles can be created by extending existing ones or defining new tuning rules."],["CONFIGURATION","**/etc/tuned/tuned-main.conf**\n> Main daemon configuration controlling dynamic tuning, update interval, and logging.\n\n**/etc/tuned/**_profile_**/tuned.conf**\n> Custom profile definitions with kernel, disk, and CPU tuning parameters.\n\n**/usr/lib/tuned/**\n> System-provided default profiles directory."],["CAVEATS","Dynamic tuning is disabled by default on RHEL. Profile changes take effect immediately but don't persist reboots without enabling the service. Some settings may conflict with other system management tools."],["HISTORY","**tuned** was developed by Red Hat for enterprise Linux systems to simplify performance optimization. It evolved from manual kernel tuning practices into an automated system that applies best practices based on workload type, reducing the expertise required for system optimization."],["INSTALL","```dnf: sudo dnf install tuned```\n\n```pacman: sudo pacman -S tuned```\n\n```apk: sudo apk add tuned```\n\n```zypper: sudo zypper install tuned```\n\n```nix: nix profile install nixpkgs#tuned```"],["SEE ALSO","[tuned-adm](/man/tuned-adm)(8), [cpupower](/man/cpupower)(1), [sysctl](/man/sysctl)(8), [ionice](/man/ionice)(1)"]],"e":["tuned-adm","cpupower","sysctl","ionice"],"x":"sudo systemctl start tuned"},"update-desktop-database":{"n":"update-desktop-database","t":"Build MIME type cache for desktop applications","s":[["TAGLINE","Build MIME type cache for desktop applications"],["TLDR","**Update the MIME type cache** for the default directories\n\n```sudo update-desktop-database```\n\n**Update cache for a specific directory**\n\n```update-desktop-database [path/to/applications]```\n\n**Update in quiet mode** (suppress output)\n\n```update-desktop-database -q```\n\n**Update with verbose output**\n\n```update-desktop-database -v```\n\n**Update after installing a new .desktop file**\n\n```sudo update-desktop-database /usr/share/applications```"],["SYNOPSIS","**update-desktop-database** [**-q**|**--quiet**] [**-v**|**--verbose**] [_DIRECTORY_...]"],["PARAMETERS","**-q**, **--quiet**\n> Do not display any information about processing and updating progress.\n\n**-v**, **--verbose**\n> Display detailed information about processing and updating progress.\n\n**-h**, **--help**\n> Display help message and exit."],["DESCRIPTION","**update-desktop-database** builds a cache database of MIME types handled by desktop files. The cache contains a mapping between MIME types and the desktop files (applications) that can handle them.\n\nThis cache enables applications to quickly find which programs can open files of a specific MIME type without parsing every .desktop file on the system. The cache is stored in mimeinfo.cache within each applications directory.\n\nIf no directory is specified, the command processes desktop files in the standard XDG data directories ($XDG_DATA_DIRS/applications), typically /usr/share/applications and /usr/local/share/applications.\n\nThe command should be run after installing, removing, or modifying .desktop files to ensure the MIME type associations are current. Package managers typically run this automatically during software installation."],["CAVEATS","Invalid MIME types in desktop files are silently ignored. The cache only determines which applications can handle a MIME type; the preferred application order is controlled by external mechanisms like xdg-mime or desktop environment settings. Running without appropriate permissions for system directories will fail."],["HISTORY","**update-desktop-database** is part of the **desktop-file-utils** package from the **freedesktop.org** project. It was developed to support the Desktop Entry Specification, which standardizes how applications are described and launched in Linux desktop environments. The tool has been a standard component of Linux desktop systems since the early **2000s**."],["INSTALL","```apt: sudo apt install desktop-file-utils```\n\n```dnf: sudo dnf install desktop-file-utils```\n\n```pacman: sudo pacman -S desktop-file-utils```\n\n```apk: sudo apk add desktop-file-utils```\n\n```zypper: sudo zypper install desktop-file-utils```\n\n```brew: brew install desktop-file-utils```\n\n```nix: nix profile install nixpkgs#desktop-file-utils```"],["SEE ALSO","[update-mime-database](/man/update-mime-database)(1), [desktop-file-validate](/man/desktop-file-validate)(1), [xdg-mime](/man/xdg-mime)(1), [xdg-open](/man/xdg-open)(1)"]],"e":["update-mime-database","desktop-file-validate","xdg-mime","xdg-open"],"x":"sudo update-desktop-database"},"vnstatd":{"n":"vnstatd","t":"Network traffic statistics collection daemon","s":[["TAGLINE","Network traffic statistics collection daemon"],["TLDR","**Start the daemon**\n\n```sudo systemctl start vnstat```\n\n**Enable at boot**\n\n```sudo systemctl enable vnstat```\n\n**Run daemon in foreground**\n\n```vnstatd -n```\n\n**Run with debug output**\n\n```vnstatd -D```\n\n**Sync database and exit**\n\n```vnstatd --sync```"],["SYNOPSIS","**vnstatd** [_options_]"],["PARAMETERS","**-d**, **--daemon**\n> Fork to background as daemon.\n\n**-n**, **--nodaemon**\n> Stay in foreground.\n\n**-s**, **--sync**\n> Sync cached data to database and exit.\n\n**-D**, **--debug**\n> Enable debug output.\n\n**--config** _file_\n> Use specified config file.\n\n**-p**, **--pidfile** _file_\n> Use specified PID file.\n\n**--noadd**\n> Don't add new interfaces automatically.\n\n**-v**, **--version**\n> Display version."],["DESCRIPTION","**vnstatd** is the daemon component of vnStat that collects network traffic statistics. It reads interface counters from the kernel at configured intervals and stores data in a persistent database.\n\nThe daemon tracks multiple interfaces simultaneously, recording 5-minute, hourly, daily, monthly, and yearly statistics. It automatically detects new interfaces added to the database without requiring restart.\n\nResource usage is minimal since vnstatd reads kernel counters rather than capturing packets. Data persists across reboots, making it suitable for long-term bandwidth monitoring."],["CONFIGURATION","Config file: /etc/vnstat.conf\n\nKey settings include monitored interfaces, database location, update interval, and data retention periods."],["CAVEATS","Statistics begin from daemon start, not historically. Kernel counter resets (reboots, interface changes) may cause inaccuracies. Database corruption possible on unclean shutdown."],["HISTORY","**vnstatd** is part of vnStat, created by Teemu Toivola for lightweight network traffic monitoring. The project began in 2002 and has become a standard tool for bandwidth monitoring on Linux servers and embedded systems."],["INSTALL","```apt: sudo apt install vnstat```\n\n```dnf: sudo dnf install vnstat```\n\n```pacman: sudo pacman -S vnstat```\n\n```apk: sudo apk add vnstat```\n\n```zypper: sudo zypper install vnstat```\n\n```brew: brew install vnstat```\n\n```nix: nix profile install nixpkgs#vnstat```"],["SEE ALSO","[vnstat](/man/vnstat)(1), [vnstati](/man/vnstati)(1), [iftop](/man/iftop)(8), [nethogs](/man/nethogs)(8)"]],"e":["vnstat","vnstati","iftop","nethogs"],"x":"sudo systemctl start vnstat"},"wasm2c":{"n":"wasm2c","t":"Convert WebAssembly to portable C code","s":[["TAGLINE","Convert WebAssembly to portable C code"],["TLDR","**Convert to C**\n\n```wasm2c [input.wasm] -o [output.c]```\n\n**Generate header**\n\n```wasm2c [input.wasm] -o [output.c] --header=[output.h]```\n\n**Module name**\n\n```wasm2c [input.wasm] -o [output.c] --module-name=[mymod]```"],["SYNOPSIS","**wasm2c** [_-o output_] [_--header file_] [_options_] _input_"],["PARAMETERS","**-o** _FILE_\n> Output C file.\n\n**--header** _FILE_\n> Generate header.\n\n**--module-name** _NAME_\n> C module name.\n\n**--help**\n> Show help."],["DESCRIPTION","**wasm2c** is a tool from the WABT (WebAssembly Binary Toolkit) that translates WebAssembly binary modules into portable C source code. The generated C code can be compiled with any standard C compiler, enabling WebAssembly modules to run natively without a WebAssembly runtime.\n\nThe tool produces both a C source file containing the translated module logic and an optional header file with function declarations and type definitions. A custom module name can be specified to control the naming of generated symbols, which is useful when linking multiple translated modules together.\n\nThis approach is useful for embedding WebAssembly functionality in C/C++ projects, running WebAssembly on platforms without a runtime, or analyzing module behavior through the generated source code."],["CAVEATS","Generated code large. Slow compilation. Part of WABT."],["HISTORY","**wasm2c** is part of **WABT** (WebAssembly Binary Toolkit), converting WebAssembly to portable C code."],["INSTALL","```apt: sudo apt install wabt```\n\n```dnf: sudo dnf install wabt```\n\n```pacman: sudo pacman -S wabt```\n\n```brew: brew install wabt```\n\n```nix: nix profile install nixpkgs#wabt```"],["SEE ALSO","[wasm2wat](/man/wasm2wat)(1), [wat2wasm](/man/wat2wasm)(1), [wasmtime](/man/wasmtime)(1)"]],"e":["wasm2wat","wat2wasm","wasmtime"],"x":"wasm2c [input.wasm] -o [output.c]"},"wg":{"n":"wg","t":"WireGuard VPN configuration utility","s":[["TAGLINE","WireGuard VPN configuration utility"],["TLDR","**Check** status of currently active interfaces\n\n```sudo wg```\n\n**Generate** a new private key\n\n```wg genkey```\n\n**Generate** a public key from a private key\n\n```wg pubkey < [path/to/private_key] > [path/to/public_key]```\n\n**Generate** both public and private keys\n\n```wg genkey | tee [path/to/private_key] | wg pubkey > [path/to/public_key]```\n\n**Show** the current configuration of an interface\n\n```sudo wg showconf [wg0]```"],["SYNOPSIS","**wg** [_command_] [_arguments_]"],["PARAMETERS","**show**\n> Show current configuration and status\n\n**showconf _interface_**\n> Show configuration in config file format\n\n**genkey**\n> Generate a new private key\n\n**pubkey**\n> Generate public key from private key on stdin\n\n**genpsk**\n> Generate a preshared key\n\n**set _interface_ _options_**\n> Change configuration of an interface\n\n**setconf _interface_ _file_**\n> Apply configuration from file\n\n**addconf _interface_ _file_**\n> Add configuration from file"],["DESCRIPTION","**wg** is the configuration utility for WireGuard, a modern VPN tunnel. It manages WireGuard interfaces including key generation, peer configuration, and status monitoring.\n\nWireGuard uses state-of-the-art cryptography and is designed to be simpler, faster, and more secure than traditional VPN protocols like OpenVPN or IPsec."],["CAVEATS","Requires root privileges for most operations. Private keys should be kept secure with restricted file permissions. Interface must be created (e.g., with **ip link add**) before configuration. For easier setup, consider using **wg-quick**."],["INSTALL","```apt: sudo apt install wireguard-tools```\n\n```dnf: sudo dnf install wireguard-tools```\n\n```pacman: sudo pacman -S wireguard-tools```\n\n```apk: sudo apk add wireguard-tools-wg```\n\n```zypper: sudo zypper install wireguard-tools```\n\n```brew: brew install wireguard-tools```\n\n```nix: nix profile install nixpkgs#wireguard-tools```"],["SEE ALSO","[wg-quick](/man/wg-quick)(8), [ip](/man/ip)(8)"]],"e":["wg-quick","ip"],"x":"sudo wg"},"wkhtmltoimage":{"n":"wkhtmltoimage","t":"Convert HTML pages to images","s":[["TAGLINE","Convert HTML pages to images"],["TLDR","**Convert an HTML file** to PNG\n\n```wkhtmltoimage [input.html] [output.png]```\n\n**Convert a webpage** to image\n\n```wkhtmltoimage [https://example.com] [output.png]```\n\n**Set output dimensions**\n\n```wkhtmltoimage --width [1024] --height [768] [input.html] [output.png]```\n\n**Set output format** explicitly\n\n```wkhtmltoimage --format [jpeg] [input.html] [output.jpg]```\n\n**Set image quality** (0-100)\n\n```wkhtmltoimage --quality [85] [input.html] [output.jpg]```\n\n**Crop the output**\n\n```wkhtmltoimage --crop-x [10] --crop-y [10] --crop-w [800] --crop-h [600] [input.html] [output.png]```\n\n**Disable JavaScript**\n\n```wkhtmltoimage --disable-javascript [input.html] [output.png]```\n\n**Render with a zoom factor**\n\n```wkhtmltoimage --zoom [1.5] [input.html] [output.png]```\n\n**Wait for JavaScript** to finish\n\n```wkhtmltoimage --javascript-delay [2000] [input.html] [output.png]```"],["SYNOPSIS","**wkhtmltoimage** [_options_] _input_ _output_"],["PARAMETERS","**--width** _int_\n> Set screen width (used as guideline).\n\n**--height** _int_\n> Set screen height.\n\n**-f**, **--format** _format_\n> Output format: png, jpg, bmp, svg.\n\n**--quality** _int_\n> Output quality 0-100 (default 94, for JPEG).\n\n**--crop-x** _int_\n> X coordinate for cropping.\n\n**--crop-y** _int_\n> Y coordinate for cropping.\n\n**--crop-w** _int_\n> Width for cropping.\n\n**--crop-h** _int_\n> Height for cropping.\n\n**-n**, **--disable-javascript**\n> Disable JavaScript execution.\n\n**--javascript-delay** _msec_\n> Wait milliseconds for JavaScript to finish.\n\n**--user-style-sheet** _path_\n> Apply custom CSS stylesheet.\n\n**--cookie** _name_ _value_\n> Set a cookie (repeatable).\n\n**--custom-header** _name_ _value_\n> Set custom HTTP header (repeatable).\n\n**--zoom** _float_\n> Zoom factor for rendering (e.g., 2.0 for 2x).\n\n**--disable-smart-width**\n> Use strict width instead of guideline.\n\n**--encoding** _encoding_\n> Set the default text encoding for input.\n\n**-p**, **--proxy** _proxy_\n> Use a proxy.\n\n**--username** _username_\n> HTTP authentication username.\n\n**--password** _password_\n> HTTP authentication password.\n\n**--run-script** _js_\n> Run additional JavaScript after the page finishes loading (repeatable).\n\n**--load-error-handling** _handler_\n> How to handle pages that fail to load: abort, ignore, or skip (default: abort).\n\n**--allow** _path_\n> Allow files from the specified folder to be loaded (repeatable).\n\n**--no-images**\n> Do not load or print images.\n\n**--stop-slow-scripts**\n> Stop slow-running JavaScript.\n\n**--window-status** _string_\n> Wait until window.status equals this string before rendering.\n\n**--log-level** _level_\n> Set log level: none, error, warn, or info.\n\n**-q**, **--quiet**\n> Suppress output messages (equivalent to --log-level none).\n\n**-H**, **--extended-help**\n> Display comprehensive help documentation.\n\n**-V**, **--version**\n> Display version information.\n\n**-h**, **--help**\n> Display help message."],["DESCRIPTION","**wkhtmltoimage** converts HTML pages to images using the WebKit rendering engine. It can process local HTML files or fetch remote URLs, producing PNG, JPEG, BMP, or SVG output.\n\nThe tool runs headlessly without requiring a display server, making it suitable for server-side image generation. It supports JavaScript execution, custom stylesheets, cookies, and HTTP authentication.\n\nPart of the **wkhtmltopdf** suite, it shares the same rendering engine and many options with its PDF counterpart."],["CAVEATS","wkhtmltoimage is based on an older Qt WebKit version and may not render modern JavaScript frameworks correctly. For complex pages, consider alternatives like Puppeteer or Playwright. The project is in maintenance mode with limited updates."],["HISTORY","wkhtmltoimage was created by **Jakob Truelsen** as part of the **wkhtmltopdf** project, first released around **2008**. It leverages Qt's WebKit implementation for rendering. The project gained popularity for server-side HTML-to-PDF/image conversion. Development has slowed as the underlying Qt WebKit has been deprecated in favor of Qt WebEngine."],["SEE ALSO","[wkhtmltopdf](/man/wkhtmltopdf)(1), [convert](/man/convert)(1), [puppeteer](/man/puppeteer)(1), [playwright](/man/playwright)(1)"]],"e":["wkhtmltopdf","convert","puppeteer","playwright"],"x":"wkhtmltoimage [input.html] [output.png]"},"worm":{"n":"worm","t":"Classic terminal snake game","s":[["TAGLINE","Classic terminal snake game"],["TLDR","**Start the worm game**\n\n```worm```"],["SYNOPSIS","**worm**"],["IN-GAME CONTROLS","**Arrow keys** or **h j k l**\n> Move the worm (left, down, up, right)\n\n**q**\n> Quit the game"],["PREVIEW","```\n . . . . . . . . .\n . . ooooo . . . .\n . . . . o . . 6 .\n . . . . o . . . .\n . . . . . . . . .\n```"],["DESCRIPTION","**worm** is a classic terminal-based snake game from the BSD games collection. The player controls a growing worm that moves around the screen, eating numbers to grow longer and score points.\n\nNumbers appear randomly on screen, and the worm grows by the value of each number eaten. The goal is to eat as many numbers as possible without crashing into the walls or the worm's own body. Speed increases as the worm grows longer, making the game progressively more challenging.\n\nThe game displays in a simple ASCII terminal interface, making it playable on any text terminal. It is the original inspiration for the popular \"Snake\" game found on Nokia mobile phones."],["CAVEATS","Requires the **bsdgames** package to be installed. Terminal must support cursor positioning. Game speed may vary depending on terminal emulator performance."],["HISTORY","Worm is part of the **BSD games** collection, a set of classic text-based games originally distributed with BSD Unix systems in the 1970s and 1980s. These games were created during the early days of Unix when terminals were text-only, and they became a staple of Unix culture. The snake/worm game concept later became famous through Nokia's implementation on mobile phones in the late 1990s."],["INSTALL","```dnf: sudo dnf install bsd-games```\n\n```pacman: sudo pacman -S bsd-games```\n\n```apk: sudo apk add bsd-games```\n\n```zypper: sudo zypper install bsd-games```"],["SEE ALSO","[rogue](/man/rogue)(6), [nethack](/man/nethack)(6), [tetris-bsd](/man/tetris-bsd)(6)"]],"e":["rogue","nethack","tetris-bsd"],"x":"worm"},"xcodebuild":{"n":"xcodebuild","t":"Build Xcode projects from command line","s":[["TAGLINE","Build Xcode projects from command line"],["TLDR","**Build a project** with default settings\n\n```xcodebuild```\n\n**Build a specific scheme**\n\n```xcodebuild -scheme [SchemeName]```\n\n**Build for a specific SDK** and destination\n\n```xcodebuild -scheme [SchemeName] -sdk iphoneos -destination \"generic/platform=iOS\"```\n\n**Build a workspace**\n\n```xcodebuild -workspace [App.xcworkspace] -scheme [SchemeName]```\n\n**Create an archive**\n\n```xcodebuild archive -scheme [SchemeName] -archivePath [path/to/App.xcarchive]```\n\n**Export an archive** to IPA\n\n```xcodebuild -exportArchive -archivePath [App.xcarchive] -exportPath [output] -exportOptionsPlist [options.plist]```\n\n**Run tests**\n\n```xcodebuild test -scheme [SchemeName] -destination \"platform=iOS Simulator,name=iPhone 15\"```\n\n**List available schemes**\n\n```xcodebuild -list```\n\n**List available SDKs**\n\n```xcodebuild -showsdks```"],["SYNOPSIS","**xcodebuild** [**-project** _name_] [**-workspace** _name_] [**-scheme** _name_] [**-target** _name_] [**-sdk** _sdk_] [**-destination** _dest_] [_action_...]"],["PARAMETERS","**-project** _name_\n> Build the project specified by name.xcodeproj.\n\n**-workspace** _name_\n> Build the workspace specified by name.xcworkspace.\n\n**-scheme** _name_\n> Build the scheme with the specified name. Required for workspaces.\n\n**-target** _name_\n> Build the target with the specified name.\n\n**-sdk** _sdk_\n> Build with the specified SDK (iphoneos, iphonesimulator, macosx).\n\n**-destination** _destination_\n> Use the destination described by destination.\n\n**-configuration** _config_\n> Build configuration: Debug or Release.\n\n**-archivePath** _path_\n> Path for archive output.\n\n**-exportPath** _path_\n> Path for export output.\n\n**-exportOptionsPlist** _path_\n> Plist file specifying export options.\n\n**-list**\n> List all targets, schemes, and configurations.\n\n**-showsdks**\n> List all available SDKs.\n\n**-allowProvisioningUpdates**\n> Allow automatic provisioning profile management.\n\n**build**\n> Build the target (default action).\n\n**archive**\n> Create an archive for distribution.\n\n**test**\n> Run unit tests.\n\n**clean**\n> Remove build products."],["DESCRIPTION","**xcodebuild** is Apple's command-line tool for building Xcode projects and workspaces. It performs build, test, archive, and export operations, making it essential for CI/CD pipelines and automated builds of macOS, iOS, watchOS, and tvOS applications.\n\nThe tool works with projects (.xcodeproj) or workspaces (.xcworkspace). Workspaces, commonly used with CocoaPods or Swift Package Manager, require specifying a scheme. Schemes define which targets to build and with what configuration.\n\nBuilding for iOS distribution is typically a two-step process: first **archive** to create an .xcarchive, then **-exportArchive** with an export options plist to produce the final .ipa file. Code signing and provisioning can be managed automatically with **-allowProvisioningUpdates**."],["CAVEATS","Requires Xcode installation (not just Command Line Tools). Use `xcode-select -s` to switch developer directories if needed. Archive and export require valid code signing identities and provisioning profiles. Simulator destinations require the simulator to be installed."],["HISTORY","**xcodebuild** has been part of **Xcode** since its early versions, evolving alongside Apple's development tools. The tool has grown significantly more powerful, adding workspace support, destination specifiers for simulators and devices, and xcpretty-compatible output. It remains the foundation for iOS and macOS continuous integration systems."],["INSTALL","```nix: nix profile install nixpkgs#xcodebuild```"],["SEE ALSO","[xcrun](/man/xcrun)(1), [xcode-select](/man/xcode-select)(1), [codesign](/man/codesign)(1), [swift](/man/swift)(1)"]],"e":["xcrun","xcode-select","codesign","swift"],"x":"xcodebuild"}});
