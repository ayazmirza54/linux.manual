window.LCL_BUCKET(54,{"abroot":{"n":"abroot","t":"immutable root partition manager with A/B switching","s":[["TAGLINE","immutable root partition manager with A/B switching"],["TLDR","**Add** packages to the local image\n\n```sudo abroot pkg add [package]```\n\n**Remove** packages from the local image\n\n```sudo abroot pkg remove [package]```\n\n**List** packages in the local image\n\n```sudo abroot pkg list```\n\n**Apply** changes in the local image\n\n```sudo abroot pkg apply```\n\n**Rollback** your system to previous state\n\n```sudo abroot rollback```\n\nEdit or view **kernel parameters**\n\n```sudo abroot kargs [edit|show]```\n\nDisplay **status**\n\n```sudo abroot status```"],["SYNOPSIS","**abroot** [_pkg_] [_rollback_] [_kargs_] [_status_] [_command_]"],["DESCRIPTION","**abroot** is a utility providing full immutability and atomicity by transacting between two root partition states (A⟺B). Updates are performed using OCI images to ensure the system is always in a consistent state.\n\nThis approach allows safe system updates where changes can be rolled back if issues occur. The system boots from one root partition while changes are applied to the other."],["PARAMETERS","**pkg add PACKAGE**\n> Add packages to the local image; requires apply to take effect\n\n**pkg remove PACKAGE**\n> Remove packages from the local image; requires apply to take effect\n\n**pkg list**\n> List packages in the local image\n\n**pkg apply**\n> Apply pending package changes; requires reboot\n\n**upgrade**\n> Update the boot partition to the latest OCI image\n\n**rollback**\n> Rollback to the previous system state\n\n**kargs edit**\n> Edit kernel parameters\n\n**kargs show**\n> Display current kernel parameters\n\n**status**\n> Display current system status\n\n**-h, --help**\n> Display help information"],["CAVEATS","Package changes require running **pkg apply** followed by a system **reboot** before they take effect."],["HISTORY","Developed for **Vanilla OS**, a Linux distribution focused on providing an immutable and atomic desktop experience using A/B partition scheme."],["INSTALL","```aur: yay -S abroot```"],["SEE ALSO","[apt](/man/apt)(8), [dnf](/man/dnf)(8), [ostree](/man/ostree)(1)"],["RESOURCES","```[Source code](https://github.com/Vanilla-OS/ABRoot)```\n\n```[Homepage](https://vanillaos.org)```"]],"e":["apt","dnf","ostree"],"x":"sudo abroot pkg add [package]"},"atd":{"n":"atd","t":"Daemon that executes jobs scheduled with at","s":[["TAGLINE","Daemon that executes jobs scheduled with at"],["TLDR","**Start the at daemon**\n\n```atd```\n\n**Run in foreground** for debugging\n\n```atd -f```\n\n**Run with debug messages** to stderr\n\n```atd -d```\n\n**Set minimum interval** between batch jobs (seconds)\n\n```atd -b [120]```\n\n**Limit batch jobs** to when load average is below a value\n\n```atd -l [1.5]```\n\n**Process the queue once** and exit\n\n```atd -s```"],["SYNOPSIS","**atd** [**-d**] [**-f**] [**-s**] [**-l** _load_avg_] [**-b** _interval_]"],["PARAMETERS","**-d**\n> Debug mode; print errors to stderr instead of syslog (implies -f)\n\n**-f**\n> Run in foreground instead of daemonizing\n\n**-s**\n> Process the at/batch queue once and exit. Equivalent to the old atrun command.\n\n**-l** _load_avg_\n> Limit batch job execution to when system load is below this value (default: 0.8)\n\n**-b** _interval_\n> Minimum seconds between starting batch jobs (default: 60)"],["DESCRIPTION","**atd** is the daemon that executes jobs scheduled with the **at** command. It runs in the background, periodically checking **/var/spool/at/** for pending jobs and executing them at their scheduled times.\n\nJobs are submitted using **at** for time-specific scheduling or **batch** for load-based execution. The daemon runs jobs on behalf of the user who scheduled them, inheriting their environment and permissions.\n\nAccess control is managed through **/etc/at.allow** and **/etc/at.deny**. If at.allow exists, only listed users can schedule jobs. If only at.deny exists, all users except those listed can schedule jobs. If neither exists, only root can use at.\n\nRelated commands:\n- **at** - schedule a job for a specific time\n- **atq** - list pending jobs\n- **atrm** - remove a pending job\n- **batch** - schedule a job for when system load permits"],["CONFIGURATION","**/etc/at.allow**\n> If this file exists, only users listed in it can schedule jobs with at.\n\n**/etc/at.deny**\n> Users listed in this file are denied access to at. Ignored if at.allow exists."],["CAVEATS","The atd spool directory (**/var/spool/at/**) does not work correctly when mounted via NFS, even with no_root_squash. Jobs inherit the environment from when they were scheduled, not the current environment. The daemon must be running for scheduled jobs to execute."],["HISTORY","The **at** command and its daemon have origins in early Unix systems, dating back to **Version 7 Unix** in **1979**. The concept of deferred command execution was part of the original Unix job control philosophy. The modern implementation includes the **batch** command for load-sensitive scheduling, making it useful for resource-intensive tasks that should run during low-usage periods."],["INSTALL","```apt: sudo apt install at```\n\n```dnf: sudo dnf install at```\n\n```pacman: sudo pacman -S at```\n\n```apk: sudo apk add at```\n\n```zypper: sudo zypper install at```\n\n```nix: nix profile install nixpkgs#at```"],["SEE ALSO","[at](/man/at)(1), [atq](/man/atq)(1), [atrm](/man/atrm)(1), [batch](/man/batch)(1), [cron](/man/cron)(8), [crontab](/man/crontab)(1)"],["RESOURCES","```[Source code](https://salsa.debian.org/debian/at)```"]],"e":["at","atq","atrm","batch","cron","crontab"],"x":"atd"},"az-network":{"n":"az-network","t":"Manage Azure networking resources","s":[["TAGLINE","Manage Azure networking resources"],["TLDR","**Create a DNS zone**\n\n```az network dns zone create -g [resource-group] --name [example.com]```\n\n**Add an A record** to a DNS zone\n\n```az network dns record-set a add-record -g [resource-group] --zone-name [example.com] --record-set-name [www] --ipv4-address [1.2.3.4]```\n\n**Create an application security group**\n\n```az network asg create -g [resource-group] --name [myASG]```\n\n**Create an application gateway**\n\n```az network application-gateway create --name [myAppGateway] -g [resource-group] --sku [Standard_v2]```\n\n**List virtual networks** in a resource group\n\n```az network vnet list -g [resource-group]```\n\n**Create a virtual network**\n\n```az network vnet create -g [resource-group] --name [myVNet] --address-prefix [10.0.0.0/16]```\n\n**Create a network security group**\n\n```az network nsg create -g [resource-group] --name [myNSG]```"],["SYNOPSIS","**az network** _subcommand_ [_options_]"],["DESCRIPTION","**az network** manages Azure Network resources including virtual networks, DNS zones, load balancers, application gateways, VPN gateways, ExpressRoute circuits, and network security groups.\n\nAzure networking provides the foundation for cloud infrastructure, enabling secure connectivity between Azure resources, on-premises environments, and the internet."],["SUBCOMMANDS","**Virtual Networks**\n> vnet create, vnet delete, vnet list, vnet show, vnet subnet\n\n**DNS**\n> dns zone create, dns zone list, dns record-set a, dns record-set cname, dns record-set mx\n\n**Load Balancers**\n> lb create, lb delete, lb list, lb rule, lb probe, cross-region-lb\n\n**Application Gateway**\n> application-gateway create, application-gateway delete, application-gateway start, application-gateway stop\n\n**Security**\n> nsg create, nsg rule, asg create, ddos-protection\n\n**Connectivity**\n> express-route, vpn-gateway, bastion, nat gateway, public-ip"],["CAVEATS","Many network resources have dependencies; for example, deleting a virtual network requires removing all associated subnets and connected resources first. DNS record changes may take time to propagate globally. Application gateways incur charges even when stopped; delete unused gateways."],["HISTORY","Azure Virtual Network was one of the earliest Azure services, launching with Azure's general availability in **2010**. The networking capabilities have expanded significantly to include Azure DNS (2015), Application Gateway, ExpressRoute, and advanced security features."],["INSTALL","```dnf: sudo dnf install azure-cli```\n\n```pacman: sudo pacman -S azure-cli```\n\n```zypper: sudo zypper install azure-cli```\n\n```brew: brew install azure-cli```\n\n```nix: nix profile install nixpkgs#azure-cli```"],["SEE ALSO","[az](/man/az)(1), [az-vm](/man/az-vm)(1), [az-aks](/man/az-aks)(1)"]],"e":["az","az-vm","az-aks"],"x":"az network dns zone create -g [resource-group] --name [example.com]"},"basename":{"n":"basename","t":"Strip directory and suffix from filenames.","s":[["TAGLINE","Strip directory and suffix from filenames."],["TLDR","**Extract** filename from path\n\n```basename [/path/to/file.txt]```\n\n**Remove** suffix\n\n```basename [/path/to/file.txt] [.txt]```\n\n**Multiple** files\n\n```basename -a [/path/to/file1.txt] [/path/to/file2.txt]```\n\nRemove **any suffix**\n\n```basename -s [.txt] [/path/to/file.txt]```"],["SYNOPSIS","**basename** _path_ [_suffix_]"],["DESCRIPTION","**basename** removes directory components from a pathname, leaving only the final filename. It can optionally remove a trailing suffix, making it useful for extracting filenames in shell scripts.\n\nThe tool is part of GNU coreutils and commonly used in build scripts and file processing pipelines."],["PARAMETERS","**-a**, **--multiple**\n> Process multiple arguments\n\n**-s**, **--suffix=**_suffix_\n> Remove trailing suffix\n\n**-z**, **--zero**\n> Separate output with NUL instead of newline"],["BEHAVIOR","Given `/path/to/file.txt`:\n- **basename** returns `file.txt`\n- **basename** with suffix `.txt` returns `file`"],["CAVEATS","Only removes a single trailing suffix exactly matching the argument — `basename file.tar.gz .gz` yields `file.tar`, not `file`. The path does not need to exist on disk. The two-argument form (positional suffix) is the POSIX behavior; `-s` plus `-a` is the GNU extension that supports multiple inputs and a non-positional suffix. In Bash scripts the parameter-expansion forms `${filepath##*/}` and `${name%.txt}` are faster than spawning `basename`."],["HISTORY","**basename** has been part of Unix since the early days, included in POSIX standards, and is available in GNU coreutils since **1992**."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[dirname](/man/dirname)(1), [realpath](/man/realpath)(1), [readlink](/man/readlink)(1)"]],"e":["dirname","realpath","readlink"],"x":"basename [/path/to/file.txt]"},"bastet":{"n":"bastet","t":"Tetris clone that gives you the worst possible pieces","s":[["TAGLINE","Tetris clone that gives you the worst possible pieces"],["TLDR","**Start** Bastet Tetris game\n\n```bastet```"],["SYNOPSIS","**bastet**"],["PREVIEW","```\n    []\n    [][]\n      []\n\n  [][][]\n    [][]\n  [][][][]\n  [][][][]\n```"],["DESCRIPTION","**bastet** (Bastard Tetris) is a Tetris clone with a twist - it chooses the worst possible piece for your current situation. The game analyzes your board and deliberately gives you pieces that are hardest to place, making it significantly more challenging than standard Tetris.\n\nThe game provides a humorous and difficult take on the classic puzzle game."],["FEATURES","- \"Adversarial\" piece selection algorithm\n- Classic Tetris gameplay\n- High score tracking\n- Terminal-based interface\n- ncurses graphics"],["CONTROLS","- **Left/Right arrows** - Move piece\n- **Up arrow** - Rotate\n- **Down arrow** - Soft drop\n- **Space** - Hard drop\n- **p** - Pause\n- **q** - Quit"],["CAVEATS","Significantly harder than normal Tetris. Requires terminal with color support. Part of some game packages. May be frustrating by design."],["HISTORY","**bastet** was created by Federico Poloni around **2005** as a deliberately difficult variant of Tetris using an algorithm to select worst-case pieces."],["INSTALL","```dnf: sudo dnf install bastet```\n\n```aur: yay -S bastet```\n\n```brew: brew install bastet```\n\n```nix: nix profile install nixpkgs#bastet```"],["SEE ALSO","[vitetris](/man/vitetris)(6), [tetris](/man/tetris)(6), [nethack](/man/nethack)(6)"]],"e":["vitetris","tetris","nethack"],"x":"bastet"},"bloodhound-python":{"n":"bloodhound-python","t":"Collect Active Directory data for BloodHound analysis","s":[["TAGLINE","Collect Active Directory data for BloodHound analysis"],["TLDR","**Collect all Active Directory data**\n\n```bloodhound-python -d [domain.local] -u [username] -p [password] -ns [dc_ip] -c all```\n\n**Collect specific data** (comma-separated methods)\n\n```bloodhound-python -d [domain.local] -u [username] -p [password] -c [Group,LocalAdmin,Session]```\n\n**Use NTLM hash** for authentication\n\n```bloodhound-python -d [domain.local] -u [username] --hashes [LM:NT] -ns [dc_ip] -c all```\n\n**Output to zip file**\n\n```bloodhound-python -d [domain.local] -u [username] -p [password] -c all --zip```\n\n**Use Kerberos authentication**\n\n```bloodhound-python -d [domain.local] -u [username] -p [password] -k -c all```\n\n**Specify DNS server**\n\n```bloodhound-python -d [domain.local] -u [username] -p [password] -ns [dns_server] -c all```\n\n**Collect against BloodHound CE** (use the CE ingestor)\n\n```bloodhound-ce-python -d [domain.local] -u [username] -p [password] -ns [dc_ip] -c all --zip```"],["SYNOPSIS","**bloodhound-python** [_options_]"],["DESCRIPTION","**bloodhound-python** is a Python-based ingestor for BloodHound, an Active Directory security analysis tool. It collects information about AD objects (users, computers, groups) and their relationships, outputting JSON files for import into the BloodHound graph database.\n\nThe tool uses graph theory to identify attack paths in Active Directory environments that would be difficult to detect manually, helping both attackers and defenders understand domain security."],["PARAMETERS","**-d, --domain** _domain_\n> Target Active Directory domain\n\n**-u, --username** _user_\n> Username for authentication\n\n**-p, --password** _pass_\n> Password for authentication\n\n**--hashes** _LMHASH:NTHASH_\n> NTLM hashes for pass-the-hash authentication. The LM portion may be left blank\n\n**--auth-method** _auto|ntlm|kerberos_\n> Authentication method to use (default: auto)\n\n**-aesKey** _hexkey_\n> AES key for Kerberos authentication (128 or 256 bit)\n\n**-no-pass**\n> Do not prompt for a password (use with -k and a ccache, or -no-pass for null sessions)\n\n**-ns, --nameserver** _ip_\n> DNS server to query, usually a Domain Controller in the target domain\n\n**-dc** _host_\n> Override the Domain Controller hostname to query (default: detected via DNS)\n\n**-gc** _host_\n> Override the Global Catalog hostname to query\n\n**-c, --collectionmethod** _method_\n> Comma-separated collection methods: Default, Group, LocalAdmin, RDP, DCOM, PSRemote, Session, LoggedOn, Trusts, ACL, Container, ObjectProps, DCOnly, All\n\n**-k, --kerberos**\n> Use Kerberos authentication. Grabs credentials from the ccache file (KRB5CCNAME environment variable)\n\n**--zip**\n> Compress the JSON output into a single zip file\n\n**-o, --outputdir** _dir_\n> Output directory for JSON files\n\n**-w** _workers_\n> Number of computer enumeration workers (default: 10)\n\n**--dns-tcp**\n> Use TCP instead of UDP for DNS queries\n\n**--use-ldaps**\n> Use LDAP over TLS (port 636) for the connection\n\n**-v**\n> Enable verbose output"],["CAVEATS","Requires valid domain credentials. Some collection methods (like Session and LoggedOn) require local administrator rights on the target hosts. The **bloodhound-python** command (legacy ingestor) targets BloodHound 4.2 and 4.3, while the separate **bloodhound-ce-python** command produces output for BloodHound Community Edition. Do not mix legacy and CE collector output. Use responsibly and only on systems you are authorized to test."],["HISTORY","BloodHound was created by **@_wald0**, **@CptJesus**, and **@harmj0y** at **SpecterOps**, released in **2016**. The Python ingestor (bloodhound-python) was developed by **Dirk-jan Mollema** as a cross-platform alternative to the C# SharpHound collector."],["INSTALL","```nix: nix profile install nixpkgs#bloodhound```"],["SEE ALSO","[ldapsearch](/man/ldapsearch)(1), [impacket](/man/impacket)(1), [netexec](/man/netexec)(1), [crackmapexec](/man/crackmapexec)(1)"],["RESOURCES","```[Source code](https://github.com/dirkjanm/BloodHound.py)```\n\n```[Homepage](https://bloodhound.specterops.io/)```"]],"e":["ldapsearch","impacket","netexec","crackmapexec"],"x":"bloodhound-python -d [domain.local] -u [username] -p [password] -ns [dc_ip] -c all"},"cargo-bench":{"n":"cargo-bench","t":"Compile and run benchmarks for a Rust project","s":[["TAGLINE","Compile and run benchmarks for a Rust project"],["TLDR","**Run all benchmarks**\n\n```cargo bench```\n\n**Run benchmarks whose name contains a substring**\n\n```cargo bench [bench_name]```\n\n**Compile benchmarks without running**\n\n```cargo bench --no-run```\n\n**Run benchmarks across the whole workspace**\n\n```cargo bench --workspace```\n\n**Continue running on benchmark failure**\n\n```cargo bench --no-fail-fast```\n\n**Benchmark a specific package**\n\n```cargo bench -p [package]```\n\n**Benchmark a specific bench target**\n\n```cargo bench --bench [bench_target]```\n\n**Pass arguments through to the bench binary**\n\n```cargo bench -- --save-baseline [name]```"],["SYNOPSIS","**cargo bench** [_options_] [_benchname_] [-- _bench-options_]"],["DESCRIPTION","**cargo bench** compiles and executes benchmark targets of the current package. By default, it uses the `bench` profile (optimizations on, debug info minimal) and runs benchmarks serially.\n\nArguments after `--` are forwarded to the compiled benchmark binary, which is how Criterion and libtest options such as `--save-baseline` or `--test` are passed through."],["PARAMETERS","**--no-run**\n> Compile benchmarks but do not execute them.\n\n**--no-fail-fast**\n> Run every benchmark even if earlier ones fail.\n\n**--all-targets**\n> Benchmark all targets (equivalent to `--lib --bins --tests --benches --examples`).\n\n**--workspace**\n> Benchmark all packages in the workspace.\n\n**--exclude** _SPEC_\n> Exclude packages from a `--workspace` run.\n\n**-p**, **--package** _SPEC_\n> Benchmark only the specified package(s).\n\n**--lib**\n> Benchmark only the library target.\n\n**--bins**\n> Benchmark all binary targets.\n\n**--bin** _NAME_\n> Benchmark only the named binary.\n\n**--benches**\n> Benchmark all `bench = true` targets.\n\n**--bench** _NAME_\n> Benchmark only the named bench target.\n\n**--examples**\n> Benchmark all example targets.\n\n**--profile** _NAME_\n> Build with a specific profile (defaults to `bench`).\n\n**--target** _TRIPLE_\n> Build for the given target triple.\n\n**-j**, **--jobs** _N_\n> Number of parallel build jobs (affects compilation, not benchmark execution).\n\n**-F**, **--features** _FEATURES_\n> Space- or comma-separated list of features to activate.\n\n**--all-features**\n> Activate all features of every selected package.\n\n**--no-default-features**\n> Do not activate the `default` feature.\n\n**--manifest-path** _PATH_\n> Path to `Cargo.toml`.\n\n**--locked**\n> Require `Cargo.lock` to remain unchanged.\n\n**--offline**\n> Do not access the network."],["BENCHMARKING FRAMEWORKS","**libtest bench**\n> Built-in `#[bench]` harness, requires nightly Rust.\n\n**Criterion**\n> Popular stable harness with statistical analysis and baselines.\n\n**Iai**\n> Instruction-counting benchmarks using Cachegrind."],["CAVEATS","The built-in `#[bench]` attribute is unstable and requires nightly Rust; on stable, use a custom harness such as **Criterion** declared with `harness = false` in `Cargo.toml`. The `--jobs` flag controls build parallelism, not benchmark execution. Results can be noisy without CPU pinning and a quiet system."],["INSTALL","```apt: sudo apt install cargo```\n\n```dnf: sudo dnf install cargo```\n\n```apk: sudo apk add cargo```\n\n```zypper: sudo zypper install cargo```\n\n```nix: nix profile install nixpkgs#cargo```"],["SEE ALSO","[cargo](/man/cargo)(1), [cargo-test](/man/cargo-test)(1), [cargo-build](/man/cargo-build)(1)"]],"e":["cargo","cargo-test","cargo-build"],"x":"cargo bench"},"cargo-init":{"n":"cargo-init","t":"Create a new Rust package in an existing directory","s":[["TAGLINE","Create a new Rust package in an existing directory"],["TLDR","**Initialize in current directory**\n\n```cargo init```\n\n**Initialize binary project**\n\n```cargo init --bin```\n\n**Initialize library**\n\n```cargo init --lib```\n\n**Initialize without git**\n\n```cargo init --vcs none```\n\n**Initialize with specific name**\n\n```cargo init --name [myproject]```\n\n**Initialize with specific edition**\n\n```cargo init --edition [2021]```\n\n**Initialize in a specific directory**\n\n```cargo init [path/to/dir]```"],["SYNOPSIS","**cargo init** [_options_] [_path_]"],["DESCRIPTION","**cargo init** creates a new Cargo package in an existing directory. Generates Cargo.toml and src/ with either main.rs (binary) or lib.rs (library). Unlike cargo new, does not create a new directory."],["PARAMETERS","**--bin**\n> Create binary target with src/main.rs (default).\n\n**--lib**\n> Create library target with src/lib.rs.\n\n**--name** _name_\n> Set package name (defaults to directory name).\n\n**--edition** _year_\n> Rust edition (2015, 2018, 2021, 2024).\n\n**--vcs** _type_\n> Initialize version control (git, hg, pijul, fossil, none).\n\n**--registry** _name_\n> Registry for publishing.\n\n**--force**\n> Force overwrite of existing Cargo.toml or src directory.\n\n**-v**, **--verbose**\n> Verbose output.\n\n**-q**, **--quiet**\n> Suppress output."],["GENERATED FILES","```\n.\n├── Cargo.toml\n├── .gitignore (if using git)\n└── src\n    └── main.rs (or lib.rs)\n```"],["CAVEATS","Default edition is 2024 in recent Rust versions. Initializes git repository by default if not already in one."],["INSTALL","```apt: sudo apt install cargo```\n\n```dnf: sudo dnf install cargo```\n\n```apk: sudo apk add cargo```\n\n```zypper: sudo zypper install cargo```\n\n```nix: nix profile install nixpkgs#cargo```"],["SEE ALSO","[cargo](/man/cargo)(1), [cargo-new](/man/cargo-new)(1), [cargo-build](/man/cargo-build)(1)"]],"e":["cargo","cargo-new","cargo-build"],"x":"cargo init"},"chmod":{"n":"chmod","t":"change file access permissions","s":[["TAGLINE","change file access permissions"],["TLDR","**Make** file executable\n\n```chmod +x [script.sh]```\n\n**Set** specific permissions\n\n```chmod 755 [file]```\n\n**Remove** write permission\n\n```chmod -w [file]```\n\n**Recursive** change\n\n```chmod -R 755 [directory/]```\n\n**Set** permissions symbolically\n\n```chmod u=rwx,g=rx,o=r [file]```"],["SYNOPSIS","**chmod** [_options_] _mode_ _file_..."],["DESCRIPTION","**chmod** (change mode) modifies file access permissions. It controls read, write, and execute permissions for three classes of users: the file owner (user), the file's group, and all other users.\n\nPermissions can be specified in two ways: numeric (octal) mode using digits 0-7, or symbolic mode using letters (u/g/o/a, +/-/=, r/w/x). Numeric mode sets all permissions at once, while symbolic mode allows adding or removing individual permissions. The command is fundamental to Unix file security and access control."],["PARAMETERS","**-R**, **--recursive**\n> Change files and directories recursively\n\n**-v**, **--verbose**\n> Verbose output\n\n**-c**, **--changes**\n> Report only changes\n\n**--reference=**_file_\n> Use permissions from reference file"],["NUMERIC MODE","Octal digits (0-7):\n- **4** - Read (r)\n- **2** - Write (w)\n- **1** - Execute (x)\n\n**Common modes:**\n- **755** - rwxr-xr-x (owner full, others read/execute)\n- **644** - rw-r--r-- (owner read/write, others read)\n- **600** - rw------- (owner only)\n- **777** - rwxrwxrwx (all permissions, usually bad idea)"],["SYMBOLIC MODE","Format: **[ugoa][+-=][rwxXst]**\n\n**Who:**\n- **u** - User (owner)\n- **g** - Group\n- **o** - Others\n- **a** - All\n\n**Operation:**\n- **+** - Add permission\n- **-** - Remove permission\n- **=** - Set exact permission\n\n**Permissions:**\n- **r** - Read\n- **w** - Write\n- **x** - Execute\n- **X** - Execute only if directory\n- **s** - Setuid/setgid\n- **t** - Sticky bit"],["WORKFLOW","```bash"],["Make script executable","chmod +x script.sh"],["Set standard file permissions","chmod 644 file.txt"],["Set directory permissions","chmod 755 directory/"],["Recursive change","chmod -R 755 public_html/"],["Remove write for group and others","chmod go-w file.txt"],["Add execute for all","chmod a+x program"],["Set exact permissions","chmod u=rwx,g=rx,o= private_script\n```"],["SPECIAL MODES","**Setuid (4000):**\n> Run as file owner\n\n**Setgid (2000):**\n> Run as file group or inherit directory group\n\n**Sticky (1000):**\n> Only owner can delete (common for /tmp)\n\nExample: `chmod 4755 program` (setuid + rwxr-xr-x)"],["CAVEATS","Requires ownership or root privileges. 777 permissions are security risk. Recursive changes can break systems. Symbolic links not affected (use chmod -h on some systems). Execute on files vs directories different meaning."],["HISTORY","**chmod** has been part of Unix since the early **1970s**, implementing the Unix permission model designed by Dennis Ritchie and Ken Thompson."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[chown](/man/chown)(1), [chgrp](/man/chgrp)(1), [umask](/man/umask)(1), [ls](/man/ls)(1)"],["RESOURCES","```[Source code](https://github.com/coreutils/coreutils)```\n\n```[Documentation](https://www.gnu.org/software/coreutils/manual/html_node/chmod-invocation.html)```"]],"e":["chown","chgrp","umask","ls"],"x":"chmod +x [script.sh]"},"chsh":{"n":"chsh","t":"Change a user's login shell","s":[["TAGLINE","Change a user's login shell"],["TLDR","Set login shell **interactively**\n\n```chsh```\n\n**List** available shells\n\n```chsh -l```\n\nSet a **specific login shell** for current user\n\n```chsh -s [path/to/shell]```\n\nSet login shell for a **specific user**\n\n```sudo chsh -s [path/to/shell] [username]```"],["SYNOPSIS","**chsh** [_options_] [_username_]"],["DESCRIPTION","**chsh** changes a user's login shell, which is the program started when the user logs in via a terminal. The new shell must be listed in **/etc/shells** unless the change is made by root.\n\nRegular users can change their own shell; root can change any user's shell. The change takes effect on the next login. The user is authenticated before the change is made, so a password is required unless run as root.\n\nCommon use cases include switching between bash, zsh, fish, or other shells. Part of the **util-linux** package on Linux systems."],["PARAMETERS","**-s, --shell** _shell_\n> Specify the new login shell\n\n**-l, --list-shells**\n> List shells from /etc/shells\n\n**-h, --help**\n> Display help message"],["CONFIGURATION","**/etc/shells**\n> List of valid login shells. Only shells listed here can be set by non-root users."],["CAVEATS","The shell must exist as a binary and be listed in /etc/shells. Setting an invalid or non-existent shell can lock users out of their accounts (use **usermod -s /bin/bash** as root to fix). Changes take effect on next login. On some systems (e.g., macOS), chsh uses a different implementation with slightly different flags."],["INSTALL","```apt: sudo apt install passwd```\n\n```pacman: sudo pacman -S shadow```\n\n```apk: sudo apk add shadow```\n\n```zypper: sudo zypper install shadow```\n\n```nix: nix profile install nixpkgs#shadow```"],["SEE ALSO","[usermod](/man/usermod)(8), [passwd](/man/passwd)(1), [login](/man/login)(1), [bash](/man/bash)(1), [zsh](/man/zsh)(1), [fish](/man/fish)(1)"]],"e":["usermod","passwd","login","bash","zsh","fish"],"x":"chsh"},"cpufetch":{"n":"cpufetch","t":"CPU architecture fetching tool","s":[["TAGLINE","CPU architecture fetching tool"],["TLDR","**Display** CPU information with default settings\n\n```cpufetch```\n\n**Display** with a specific color scheme\n\n```cpufetch --color [amd]```\n\n**Use** custom RGB colors\n\n```cpufetch --color [239,90,45:210,200,200:0,0,0:100,200,45:0,200,200]```\n\n**Use** the retro style\n\n```cpufetch --style retro```\n\n**Show** full unabbreviated CPU name\n\n```cpufetch --full-cpu-name```\n\n**Force** small logo for narrow terminals\n\n```cpufetch --logo-short```"],["SYNOPSIS","**cpufetch** [_OPTION_]..."],["PARAMETERS","**-c**, **--color** _scheme_\n> Set the color scheme. Accepts predefined names (intel, amd, ibm, arm) or custom RGB values in the format R,G,B:R,G,B:R,G,B:R,G,B:R,G,B (first 3 for ASCII art, last 2 for text).\n\n**-s**, **--style** _style_\n> Set the style of the CPU logo: fancy (default), retro, or legacy (no color support).\n\n**-F**, **--full-cpu-name**\n> Show the full CPU name without abbreviation.\n\n**--logo-short**\n> Force display of the short version of the logo.\n\n**--logo-long**\n> Force display of the long version of the logo.\n\n**--logo-intel-old**\n> Use the old Intel logo.\n\n**--logo-intel-new**\n> Use the new Intel logo.\n\n**-d**, **--debug**\n> Print CPU model and CPUID levels for debugging.\n\n**-v**, **--verbose**\n> Print extra information about fetching process.\n\n**-r**, **--raw**\n> Print raw CPUID data for debugging.\n\n**-h**, **--help**\n> Print help and exit.\n\n**-V**, **--version**\n> Print version and exit."],["DESCRIPTION","**cpufetch** is a command-line utility written in C that displays detailed CPU architecture information in a visually appealing format, similar to how neofetch displays system information but focused specifically on the CPU. It renders ASCII art of the CPU manufacturer's logo alongside technical details including CPU name, microarchitecture, core and thread count, maximum frequency, semiconductor technology, cache sizes, AVX/FMA/SSE support, and theoretical peak performance.\n\nSupports x86_64, ARM, RISC-V, and PowerPC architectures across Linux, Windows, Android, macOS, and FreeBSD."],["CAVEATS","The peak performance calculation uses the maximum CPU frequency but does not account for AVX-specific frequency throttling. For accurate peak performance measurements, use **peakperf** instead. macOS ARM support is limited to Apple Silicon chips only. FreeBSD support is limited to x86_64/x86."],["HISTORY","**cpufetch** was created by **Dr-Noob** with the first commit in **March 2018**. Written in C under the GPL-2.0 license. The latest version is v1.07 (November 2025). The same author also created **gpufetch** for GPU information and **peakperf** for accurate CPU performance measurement."],["INSTALL","```apt: sudo apt install cpufetch```\n\n```dnf: sudo dnf install cpufetch```\n\n```pacman: sudo pacman -S cpufetch```\n\n```zypper: sudo zypper install cpufetch```\n\n```brew: brew install cpufetch```\n\n```nix: nix profile install nixpkgs#cpufetch```"],["SEE ALSO","[neofetch](/man/neofetch)(1), [fastfetch](/man/fastfetch)(1), [lscpu](/man/lscpu)(1), [screenfetch](/man/screenfetch)(1)"]],"e":["neofetch","fastfetch","lscpu","screenfetch"],"x":"cpufetch"},"cs-launch":{"n":"cs-launch","t":"run JVM applications from Maven coordinates","s":[["TAGLINE","run JVM applications from Maven coordinates"],["TLDR","**Launch an application** by Maven coordinates\n\n```cs launch [org.scalameta::metals:latest.stable]```\n\n**Launch with arguments** passed to the application\n\n```cs launch [com.lihaoyi::ammonite:2.5.9] -- [--help]```\n\n**Launch with a specific main** class\n\n```cs launch [org.example::app:1.0] -M [com.example.Main]```\n\n**Launch from custom repository**\n\n```cs launch -r [https://repo.example.com/maven] [org.example::app:1.0]```\n\n**Launch with extra JVM** options\n\n```cs launch [app:version] --java-opt [-Xmx2G]```\n\n**Launch and fork** to background\n\n```cs launch --fork [app:version]```"],["SYNOPSIS","**cs launch** [_options_] _coordinates_ [-- _app-args_]"],["PARAMETERS","_COORDINATES_\n> Maven coordinates (groupId::artifactId:version for Scala, groupId:artifactId:version for Java).\n\n**-M** _CLASS_, **--main-class** _CLASS_\n> Specify the main class to run.\n\n**-r** _URL_, **--repository** _URL_\n> Add a custom Maven repository.\n\n**--java-opt** _OPT_\n> JVM options passed to the launched application.\n\n**--fork**\n> Fork the process and return immediately.\n\n**--standalone**\n> Include all dependencies in classpath.\n\n**--**\n> Separator between cs options and application arguments.\n\n**-q**, **--quiet**\n> Suppress coursier output."],["DESCRIPTION","**cs launch** runs JVM applications directly from their Maven coordinates without prior installation. It resolves dependencies, downloads artifacts, and executes the application in a single command.\n\nThis enables trying applications without installing them, running specific versions for testing, or scripting application execution. Dependencies are cached, so subsequent launches of the same version are fast.\n\nThe command supports both Scala (using :: for cross-versioning) and Java artifacts. It can launch any artifact with a main class defined in its manifest or explicitly specified."],["CAVEATS","First launch downloads dependencies which takes time. Network connectivity required for new artifacts. Large applications with many dependencies consume significant cache space. Some applications may require specific JVM versions."],["HISTORY","cs launch is part of Coursier, created by Alexandre Archambault. The launch functionality enables the \"run without install\" workflow popularized by tools like npx, applied to the JVM ecosystem."],["INSTALL","```apt: sudo apt install csound```\n\n```pacman: sudo pacman -S csound```\n\n```zypper: sudo zypper install csound```\n\n```brew: brew install csound```\n\n```nix: nix profile install nixpkgs#csound```"],["SEE ALSO","[cs](/man/cs)(1), [cs-resolve](/man/cs-resolve)(1), [java](/man/java)(1), [scala](/man/scala)(1)"],["RESOURCES","```[Source code](https://github.com/coursier/coursier)```\n\n```[Documentation](https://get-coursier.io/docs/cli-launch)```"]],"e":["cs","cs-resolve","java","scala"],"x":"cs launch [org.scalameta::metals:latest.stable]"},"dh":{"n":"dh","t":"debhelper sequence executor for Debian packaging","s":[["TAGLINE","debhelper sequence executor for Debian packaging"],["TLDR","**Execute debhelper commands** sequentially\n\n```dh [sequence]```\n\n**Build package** with debhelper\n\n```dh build```\n\n**Clean build artifacts**\n\n```dh clean```\n\n**Run with verbose output**\n\n```dh [sequence] --verbose```\n\n**List all commands** in sequence\n\n```dh [sequence] --no-act```\n\n**Skip specific commands**\n\n```dh [sequence] --until [dh_auto_configure]```"],["SYNOPSIS","**dh** _sequence_ [_options_] [_debhelper-options_]"],["PARAMETERS","_SEQUENCE_\n> Build sequence: build, clean, install, binary, etc.\n\n**--verbose**\n> Enable verbose mode for all commands.\n\n**--no-act**, **-n**\n> Show what would be done without executing.\n\n**--with** _ADDON_\n> Use specified addon (e.g., python3, systemd).\n\n**--without** _ADDON_\n> Disable specified addon.\n\n**--until** _CMD_\n> Run until specified command.\n\n**--after** _CMD_\n> Start after specified command.\n\n**--help**\n> Display help information."],["DESCRIPTION","**dh** is the debhelper sequence executor that runs a series of dh_* commands in the correct order for building Debian packages. It simplifies debian/rules files by automatically determining which commands to run.\n\nThe tool reads debian/rules overrides to customize behavior. Override targets (override_dh_*) in debian/rules let maintainers modify specific steps. Addon support extends functionality for specific packaging scenarios.\n\ndh significantly reduces boilerplate in debian/rules, replacing explicit command lists with simple sequence invocations that automatically handle most packaging tasks."],["CAVEATS","Behavior depends on debhelper compatibility level (set in debian/compat or Build-Depends). Override syntax varies between compat levels. Some packages require explicit command sequences."],["HISTORY","dh was introduced in **debhelper v7** (2008) by **Joey Hess** to simplify Debian packaging. It enabled the \"dh short form\" rules files that dramatically reduced packaging complexity."],["SEE ALSO","[debhelper](/man/debhelper)(7), [dpkg-buildpackage](/man/dpkg-buildpackage)(1), [lintian](/man/lintian)(1)"]],"e":["debhelper","dpkg-buildpackage","lintian"],"x":"dh [sequence]"},"distrobox-export":{"n":"distrobox-export","t":"export container applications and binaries to host system","s":[["TAGLINE","export container applications and binaries to host system"],["TLDR","**Export an app** to the host\n\n```distrobox-export --app [mpv]```\n\n**Export an app** with extra launch flags\n\n```distrobox-export --app [atom] --extra-flags \"--foreground\"```\n\n**Export a binary** to ~/.local/bin\n\n```distrobox-export --bin [/usr/bin/vim] --export-path [~/.local/bin]```\n\n**List** apps already exported from this container\n\n```distrobox-export --list-apps```\n\n**Delete** an exported application\n\n```distrobox-export --app [atom] --delete```"],["SYNOPSIS","**distrobox-export** [_options_]"],["DESCRIPTION","**distrobox-export** exports applications or binaries from a Distrobox container to the host system. Exported apps appear in the host's application menu, and exported binaries become available on the host through a small wrapper script that runs them inside the container.\n\nRun this command from inside the container. Use either **--app** or **--bin**, not both together."],["PARAMETERS","**-a, --app** _app_\n> Export application by name or absolute path to a .desktop file (creates a desktop entry)\n\n**-b, --bin** _binary_\n> Export a binary by its absolute path inside the container\n\n**--list-apps**\n> List applications already exported from this container\n\n**--list-binaries**\n> List binaries already exported from this container\n\n**-ep, --export-path** _path_\n> Destination path for an exported binary (e.g. ~/.local/bin)\n\n**-el, --export-label** _label_\n> Label appended to the exported application name (defaults to \"(on _container_)\"; use \"none\" to disable)\n\n**-ef, --extra-flags** _flags_\n> Extra flags passed to the command when the exported item runs\n\n**-nf, --enter-flags** _flags_\n> Extra flags passed to the underlying `distrobox enter`\n\n**-S, --sudo**\n> Run the exported item as root inside the container\n\n**-d, --delete**\n> Un-export the specified application or binary"],["CAVEATS","Must be run from inside the container. Exported apps depend on the container existing (it is started automatically on launch). Deleting the container breaks exported applications and binaries."],["INSTALL","```dnf: sudo dnf install distrobox```\n\n```pacman: sudo pacman -S distrobox```\n\n```apk: sudo apk add distrobox```\n\n```zypper: sudo zypper install distrobox```\n\n```brew: brew install distrobox```\n\n```nix: nix profile install nixpkgs#distrobox```"],["SEE ALSO","[distrobox](/man/distrobox)(1), [distrobox-enter](/man/distrobox-enter)(1)"],["RESOURCES","```[Source code](https://github.com/89luca89/distrobox)```\n\n```[Homepage](https://distrobox.it/)```"]],"e":["distrobox","distrobox-enter"],"x":"distrobox-export --app [mpv]"},"elvish":{"n":"elvish","t":"Expressive shell with modern programming features","s":[["TAGLINE","Expressive shell with modern programming features"],["TLDR","**Start Elvish shell**\n\n```elvish```\n\n**Run Elvish script**\n\n```elvish [script.elv]```\n\n**Run command string**\n\n```elvish -c \"[echo hello]\"```\n\n**Start with specific** config\n\n```elvish --rc [config.elv]```"],["SYNOPSIS","**elvish** [_options_] [_script_] [_args_]"],["PARAMETERS","_SCRIPT_\n> Script file to execute.\n\n**-c** _CODE_\n> Execute command string.\n\n**--rc** _FILE_\n> Configuration file.\n\n**--norc**\n> Don't load rc file.\n\n**-i**\n> Force interactive mode even when input is not a terminal.\n\n**--compileonly**\n> Parse and compile the code but do not execute it, reporting any errors.\n\n**--json**\n> Print output of --compileonly as JSON.\n\n**--version**\n> Print the Elvish version and exit.\n\n**--help**\n> Display help information."],["DESCRIPTION","**Elvish** is an expressive programming language and a powerful shell for Unix-like systems. It combines shell features with a modern programming language including namespaces, lambdas, and structured data.\n\nThe shell offers pipelines that pass structured data (not just strings), built-in file manager (Ctrl-N), and comprehensive history with directory history (Ctrl-L). Its syntax is cleaner than traditional shells.\n\nElvish provides real-time syntax highlighting, intelligent completions, and a consistent programming model for both interactive use and scripting."],["CONFIGURATION","**~/.config/elvish/rc.elv** or **~/.elvish/rc.elv**\n> Main configuration file loaded on shell startup.\n\n**~/.config/elvish/lib/**\n> Directory for Elvish modules and libraries."],["CAVEATS","Not POSIX-compatible. Scripts not portable to bash/zsh. Smaller community than mainstream shells. Some traditional shell patterns don't work."],["HISTORY","Elvish was created by **Qi Xiao** starting around **2014** as a shell designed from scratch with modern language features rather than POSIX compatibility as the primary goal."],["INSTALL","```apt: sudo apt install elvish```\n\n```dnf: sudo dnf install elvish```\n\n```pacman: sudo pacman -S elvish```\n\n```apk: sudo apk add elvish```\n\n```brew: brew install elvish```\n\n```nix: nix profile install nixpkgs#elvish```"],["SEE ALSO","[bash](/man/bash)(1), [zsh](/man/zsh)(1), [fish](/man/fish)(1), [nushell](/man/nushell)(1)"],["RESOURCES","```[Source code](https://github.com/elves/elvish)```\n\n```[Homepage](https://elv.sh/)```\n\n```[Documentation](https://elv.sh/ref/)```"]],"e":["bash","zsh","fish","nushell"],"x":"elvish"},"epstopdf":{"n":"epstopdf","t":"Encapsulated PostScript to PDF converter","s":[["TAGLINE","Encapsulated PostScript to PDF converter"],["TLDR","**Convert EPS to PDF**\n\n```epstopdf [image.eps]```\n\n**Specify output file**\n\n```epstopdf --outfile=[output.pdf] [image.eps]```\n\n**Convert with Ghostscript options**\n\n```epstopdf --gsopt=[-dPDFSETTINGS=/prepress] [image.eps]```\n\n**Restricted mode** (safer)\n\n```epstopdf --restricted [image.eps]```\n\n**Embed fonts**\n\n```epstopdf --embed [image.eps]```\n\n**Read EPS from stdin, write PDF to stdout**\n\n```cat [image.eps] | epstopdf --filter > [out.pdf]```\n\n**Use HiResBoundingBox**\n\n```epstopdf --hires [image.eps]```"],["SYNOPSIS","**epstopdf** [_options_] _file.eps_"],["DESCRIPTION","**epstopdf** converts Encapsulated PostScript (EPS) files to PDF format. It uses Ghostscript for the conversion and is commonly used in TeX/LaTeX workflows.\n\nThe tool handles bounding box calculations, font embedding, and produces PDF output suitable for inclusion in documents."],["PARAMETERS","**-o**, **--outfile=** _file_\n> Output PDF file name.\n\n**--filter**\n> Read EPS from stdin and write PDF to stdout.\n\n**--restricted**\n> Restricted mode (no shell escapes or unsafe options).\n\n**--gsopt=** _option_\n> Pass a single option to Ghostscript (repeatable).\n\n**--gsopts=** _options_\n> Pass multiple space-separated options to Ghostscript.\n\n**--gscmd=** _command_\n> Ghostscript command to use.\n\n**--gs**, **--nogs**\n> Run Ghostscript (default) or output intermediate PostScript instead.\n\n**--hires**\n> Scan and use the HiResBoundingBox.\n\n**--exact**\n> Scan and use the ExactBoundingBox.\n\n**--embed**, **--noembed**\n> Control font embedding.\n\n**--compress**, **--nocompress**\n> Toggle output compression.\n\n**--gray**, **--nogray**\n> Convert output to grayscale.\n\n**--autorotate=** _MODE_\n> Set page rotation behavior (None, All, or PageByPage).\n\n**--pdfsettings=** _LEVEL_\n> Ghostscript PDF quality preset (e.g., screen, ebook, printer, prepress).\n\n**--res=** _DPI_\n> Set raster image resolution.\n\n**--safer**, **--nosafer**\n> Toggle Ghostscript safety restrictions.\n\n**--quiet**, **--noquiet**\n> Suppress or show output messages.\n\n**--debug**\n> Enable debug output.\n\n**--version**\n> Display version information.\n\n**--help**\n> Display help."],["CAVEATS","Requires Ghostscript installation. Bounding box must be valid in source EPS. Some PostScript features may not convert correctly. Output quality depends on source."],["HISTORY","**epstopdf** is part of the **TeX Live** distribution, essential for LaTeX users who need to include EPS graphics in PDF documents. It has been maintained by various TeX developers over the years, with **Karl Berry** and others contributing to its development."],["INSTALL","```apk: sudo apk add texlive-dvi```"],["SEE ALSO","[gs](/man/gs)(1), [ps2pdf](/man/ps2pdf)(1), [pdflatex](/man/pdflatex)(1), [convert](/man/convert)(1)"]],"e":["gs","ps2pdf","pdflatex","convert"],"x":"epstopdf [image.eps]"},"fastlane":{"n":"fastlane","t":"iOS and Android app automation tool","s":[["TAGLINE","iOS and Android app automation tool"],["TLDR","**Initialize Fastlane**\n\n```fastlane init```\n\n**Run a lane**\n\n```fastlane [lane_name]```\n\n**Build and test iOS app**\n\n```fastlane ios test```\n\n**Deploy to App Store**\n\n```fastlane release```\n\n**Deploy to TestFlight**\n\n```fastlane beta```\n\n**List available actions**\n\n```fastlane actions```\n\n**List available lanes**\n\n```fastlane lanes```"],["SYNOPSIS","**fastlane** [_options_] [_lane_]"],["SUBCOMMANDS","**init**\n> Initialize Fastlane.\n\n**lanes**\n> List all lanes.\n\n**actions**\n> List all actions.\n\n**env**\n> Print environment info.\n\n**new_action**\n> Create new action.\n\n**docs**\n> Generate documentation."],["PARAMETERS","**--verbose**\n> Enable verbose output.\n\n**--env** _environment_\n> Load environment-specific config."],["CONFIGURATION","**./fastlane/Fastfile**\n> Main configuration file defining lanes (workflows) for build, test, and deployment automation.\n\n**./fastlane/Appfile**\n> App-specific configuration including bundle identifiers and Apple ID."],["DESCRIPTION","**fastlane** automates iOS and Android development tasks including building, testing, code signing, and releasing apps to the App Store and Google Play.\n\nThe tool provides a Ruby-based DSL for defining workflows (called \"lanes\") that chain together actions. It handles complex tasks like certificate management, screenshot generation, beta distribution, and store submissions.\n\nfastlane integrates with tools like xcodebuild, Gradle, gym, match, and deliver to provide end-to-end automation for mobile development and release processes."],["INSTALL","```brew: brew install fastlane```\n\n```nix: nix profile install nixpkgs#fastlane```"],["SEE ALSO","[xcodebuild](/man/xcodebuild)(1), [gradle](/man/gradle)(1)"],["RESOURCES","```[Source code](https://github.com/fastlane/fastlane)```\n\n```[Homepage](https://fastlane.tools/)```"]],"e":["xcodebuild","gradle"],"x":"fastlane init"},"fbset":{"n":"fbset","t":"configure framebuffer device settings","s":[["TAGLINE","configure framebuffer device settings"],["TLDR","Show current **framebuffer settings**\n\n```sudo fbset [-i|--info]```\n\nSet a framebuffer **mode** from fb.modes\n\n```sudo fbset \"[800]x[600]-[60]\"```\n\nSet **arbitrary** framebuffer mode\n\n```sudo fbset [-g|--geometry] [TTY_horizontal] [TTY_vertical] [monitor_horizontal] [monitor_vertical] [color_depth]```"],["SYNOPSIS","**fbset** [_options_] [_mode_]"],["DESCRIPTION","**fbset** shows and modifies Linux framebuffer device settings. It can change resolution, color depth, timing, and other display parameters.\n\nUsed for configuring console display on systems using framebuffer."],["PARAMETERS","**-i, --info**\n> Show framebuffer information\n\n**-g, --geometry** _params_\n> Set geometry (xres, yres, vxres, vyres, depth)\n\n**-t, --timings** _params_\n> Set display timings\n\n**-fb** _device_\n> Use specified framebuffer device"],["CONFIGURATION","**/etc/fb.modes**\n> Defines predefined video modes with resolution, timing, and color depth settings."],["CAVEATS","Requires framebuffer support in kernel. May require root privileges. Incorrect settings can cause display issues."],["INSTALL","```apt: sudo apt install fbset```\n\n```pacman: sudo pacman -S fbset```\n\n```nix: nix profile install nixpkgs#fbset```"],["SEE ALSO","[fbi](/man/fbi)(1)"]],"e":["fbi"],"x":"sudo fbset [-i|--info]"},"gcloud-topic":{"n":"gcloud-topic","t":"displays supplementary help topics","s":[["TAGLINE","displays supplementary help topics"],["TLDR","**List available topics**\n\n```gcloud topic```\n\n**View specific topic**\n\n```gcloud topic [configurations]```\n\n**View auth topic**\n\n```gcloud topic [auth]```\n\n**View formats topic**\n\n```gcloud topic [formats]```"],["SYNOPSIS","**gcloud** **topic** [_topic_]"],["TOPICS","**configurations**\n> Named configurations.\n\n**auth**\n> Authentication information.\n\n**formats**\n> Output formats.\n\n**filters**\n> Filter expressions.\n\n**projections**\n> Resource projections.\n\n**escaping**\n> Escaping and quoting.\n\n**startup**\n> SDK startup."],["DESCRIPTION","**gcloud topic** displays supplementary help topics for gcloud commands. Topics provide detailed information about various gcloud concepts and features."],["SEE ALSO","[gcloud-help](/man/gcloud-help)(1), [gcloud](/man/gcloud)(1)"],["RESOURCES","```[Documentation](https://cloud.google.com/sdk/gcloud/reference/topic)```"]],"e":["gcloud-help","gcloud"],"x":"gcloud topic"},"git-filter-repo":{"n":"git-filter-repo","t":"Fast, safe tool for rewriting Git repository history","s":[["TAGLINE","Fast, safe tool for rewriting Git repository history"],["TLDR","**Remove file from history**\n\n```git filter-repo --path [file.txt] --invert-paths```\n\n**Rename paths**\n\n```git filter-repo --path-rename [old/path:new/path]```\n\n**Remove large files**\n\n```git filter-repo --strip-blobs-bigger-than [10M]```\n\n**Analyze repository**\n\n```git filter-repo --analyze```\n\n**Subdirectory to root**\n\n```git filter-repo --subdirectory-filter [dir/]```"],["SYNOPSIS","**git filter-repo** [_options_]"],["PARAMETERS","**--path** _PATH_\n> Only keep the given path (repeatable); combine with **--invert-paths** to remove it instead.\n\n**--path-glob** _GLOB_\n> Filter paths matching a glob pattern.\n\n**--path-regex** _REGEX_\n> Filter paths matching a regex.\n\n**--invert-paths**\n> Invert path selection, excluding rather than including matches.\n\n**--path-rename** _OLD:NEW_\n> Rename paths throughout history.\n\n**--replace-text** _FILE_\n> Replace text matching expressions listed in _FILE_ throughout history (for scrubbing secrets).\n\n**--strip-blobs-bigger-than** _SIZE_\n> Remove blobs larger than _SIZE_ (e.g. 10M).\n\n**--mailmap** _FILE_\n> Rewrite author/committer names and emails using a mailmap file.\n\n**--tag-rename** _OLD:NEW_\n> Rename tags matching a pattern.\n\n**--analyze**\n> Generate an analysis report instead of rewriting anything.\n\n**--subdirectory-filter** _DIR_\n> Make _DIR_ the new repository root, discarding everything else.\n\n**--dry-run**\n> Show what would change without rewriting the repo.\n\n**--partial**\n> Do a partial rewrite, leaving history mixed with unrewritten refs (skips some safety checks).\n\n**-f**, **--force**\n> Skip the fresh-clone safety check.\n\n**--help**\n> Display help information."],["DESCRIPTION","**git filter-repo** rewrites Git history with powerful filtering capabilities. It is the officially recommended replacement for the deprecated git filter-branch, offering significantly faster and safer history manipulation.\n\nThe tool can remove files, rename paths, strip sensitive data, change authors, and restructure repositories. It prevents common mistakes that git filter-branch was prone to and operates much faster on large repositories."],["CAVEATS","**History rewriting destroys old commits.** By default requires a fresh clone (no other work in progress) as a safety check; override with **--force** only when you understand the risk. Collaborators must reclone or hard-reset onto the new history. Cannot be undone."],["HISTORY","git filter-repo was created by **Elijah Newren** as the official replacement for git filter-branch, recommended by the git project itself since Git 2.24's release notes."],["INSTALL","```dnf: sudo dnf install git-filter-repo```\n\n```pacman: sudo pacman -S git-filter-repo```\n\n```apk: sudo apk add git-filter-repo```\n\n```zypper: sudo zypper install git-filter-repo```\n\n```brew: brew install git-filter-repo```\n\n```nix: nix profile install nixpkgs#git-filter-repo```"],["SEE ALSO","[git](/man/git)(1), [git-rebase](/man/git-rebase)(1), [git-filter-branch](/man/git-filter-branch)(1)"],["RESOURCES","```[Source code](https://github.com/newren/git-filter-repo)```\n\n```[Documentation](https://htmlpreview.github.io/?https://github.com/newren/git-filter-repo/blob/docs/html/git-filter-repo.html)```"]],"e":["git","git-rebase","git-filter-branch"],"x":"git filter-repo --path [file.txt] --invert-paths"},"goimports":{"n":"goimports","t":"Go import line management and formatting","s":[["TAGLINE","Go import line management and formatting"],["TLDR","**Format and fix imports in a file**\n\n```goimports [file.go]```\n\n**Fix imports and write to file**\n\n```goimports -w [file.go]```\n\n**Show diff of changes**\n\n```goimports -d [file.go]```\n\n**List files with import issues**\n\n```goimports -l [file.go]```\n\n**Separate local packages**\n\n```goimports -local [github.com/myorg] [file.go]```\n\n**Format all files in directory**\n\n```goimports -w .```"],["SYNOPSIS","**goimports** [_flags_] [_path_ ...]"],["PARAMETERS","**-d**\n> Display diffs instead of rewriting.\n\n**-l**\n> List files whose imports differ.\n\n**-w**\n> Write result to source file.\n\n**-local** _prefix_\n> Comma-separated prefixes for local packages.\n\n**-e**\n> Report all errors (not just the first 10 on different lines).\n\n**-format-only**\n> Do not fix imports; only format the code. In this mode goimports is effectively gofmt with grouped imports.\n\n**-v**\n> Verbose logging.\n\n**-srcdir** _dir_\n> Choose imports as if source code is from the specified directory."],["DESCRIPTION","**goimports** updates Go import lines by adding missing imports and removing unreferenced ones. It also formats code in the same style as gofmt, making it a drop-in replacement.\n\nImports are grouped with standard library first, then third-party packages, and sorted alphabetically within each group. The **-local** flag allows separating project-specific imports into their own group."],["INSTALL","```apt: sudo apt install golang-golang-x-tools```\n\n```brew: brew install goimports```"],["SEE ALSO","[gofmt](/man/gofmt)(1), [go-fmt](/man/go-fmt)(1), [gofumpt](/man/gofumpt)(1)"]],"e":["gofmt","go-fmt","gofumpt"],"x":"goimports [file.go]"},"gradle-clean":{"n":"gradle-clean","t":"Gradle build artifact removal task","s":[["TAGLINE","Gradle build artifact removal task"],["TLDR","**Clean build directory**\n\n```gradle clean```\n\n**Clean specific module**\n\n```gradle :module:clean```\n\n**Clean and build**\n\n```gradle clean build```"],["SYNOPSIS","**gradle clean** [_options_]"],["PARAMETERS","**clean**_TaskName_\n> Task rule that deletes only the outputs of a named task, e.g. `cleanJar` removes the JAR produced by the `jar` task.\n\n**--info**\n> Info logging.\n\n**--help**\n> Display help information."],["DESCRIPTION","**gradle clean** deletes the project build directory (the path set by `layout.buildDirectory`, by default `build/`) and everything in it. It removes compiled classes, packaged artifacts, and cached build output, ensuring a fresh build environment.\n\nThe task is provided by Gradle's base plugin, which is applied by the Java, Application, and most other language plugins. It is useful when builds become inconsistent or caches are corrupted, forcing a complete rebuild on the next invocation."],["CAVEATS","Removes all build output. Next build will be full rebuild. Doesn't clean dependencies."],["HISTORY","The clean task is a standard **Gradle** lifecycle task for build artifact removal."],["INSTALL","```pacman: sudo pacman -S gradle```\n\n```apk: sudo apk add gradle```\n\n```brew: brew install gradle```\n\n```nix: nix profile install nixpkgs#gradle```"],["SEE ALSO","[gradle](/man/gradle)(1), [gradle-build](/man/gradle-build)(1)"]],"e":["gradle","gradle-build"],"x":"gradle clean"},"idevice_id":{"n":"idevice_id","t":"lists connected iOS device identifiers","s":[["TAGLINE","lists connected iOS device identifiers"],["TLDR","**List connected devices**\n\n```idevice_id -l```\n\n**Show device UDID**\n\n```idevice_id```\n\n**List in simple mode**\n\n```idevice_id -l -n```\n\n**Debug output**\n\n```idevice_id -d -l```"],["SYNOPSIS","**idevice_id** [_options_]"],["PARAMETERS","**-l**, **--list**\n> List device UDIDs.\n\n**-n**, **--network**\n> Include network devices.\n\n**-d**, **--debug**\n> Debug output.\n\n**-u** _UDID_\n> Target device.\n\n**--help**\n> Display help information."],["DESCRIPTION","**idevice_id** lists connected iOS device identifiers. It detects iPhones, iPads, and iPods via USB or network.\n\nThe tool provides UDIDs needed for other libimobiledevice commands. It's the first step in iOS device management."],["CAVEATS","Part of libimobiledevice. Requires USB access. May need pairing."],["HISTORY","idevice_id is part of **libimobiledevice**, the open-source iOS device communication library."],["INSTALL","```apt: sudo apt install libimobiledevice-utils```\n\n```dnf: sudo dnf install libimobiledevice-utils```\n\n```apk: sudo apk add libimobiledevice-progs```"],["SEE ALSO","[idevicepair](/man/idevicepair)(1), [ideviceinfo](/man/ideviceinfo)(1), [idevicename](/man/idevicename)(1)"]],"e":["idevicepair","ideviceinfo","idevicename"],"x":"idevice_id -l"},"inz":{"n":"inz","t":"CLI for instancez, a single-binary Supabase alternative","s":[["TAGLINE","CLI for instancez, a single-binary Supabase alternative"],["TLDR","**Install** the inz CLI\n\n```curl -fsSL https://get.instancez.ai | sh```\n\n**Scaffold** a new project (creates instancez.yaml)\n\n```inz init```\n\n**Run the dev server** with embedded Postgres\n\n```inz dev --embedded-pg```\n\n**Run the dev server** against an existing database\n\n```inz dev```\n\n**Serve** in production mode\n\n```inz serve```\n\n**Deploy** instancez.yaml to instancez Cloud\n\n```inz cloud deploy```"],["SYNOPSIS","**inz** \\<command\\> [_options_]"],["DESCRIPTION","**inz** is the command-line interface for **instancez**, an LLM-friendly, single-binary backend intended as a drop-in replacement for many Supabase use cases. Schema, auth policies, storage buckets, and functions are declared in one **instancez.yaml** file. On change, instancez diffs the file against the live database and applies only the delta—including drops—without hand-written migration scripts.\n\nThe binary embeds a dashboard and speaks PostgREST-style REST, auth (password, magic link, OTP, anonymous, OAuth, TOTP MFA), SQL RPC, Node.js edge functions, and storage (local or S3) with row-level security. Existing **@supabase/supabase-js** clients can point at an instancez API URL and publishable key. **inz dev** provisions roles, applies the schema, and serves the API (default **http://localhost:8080**); with **--embedded-pg** no external Postgres install is required. Publishable and secret keys are written to **.development.env** on first run. Production secrets load from **.production.env** when present."],["PARAMETERS","**init**\n> Create a scaffold project including **instancez.yaml** and example tables/functions.\n\n**dev** [**--embedded-pg**]\n> Run the development API with live schema reload. **--embedded-pg** starts an embedded Postgres; otherwise set **INSTANCEZ_DATABASE_URL**.\n\n**serve**\n> Run the production server (self-host).\n\n**cloud deploy**\n> Push **instancez.yaml** to a managed instancez Cloud project."],["CAVEATS","Realtime/websockets are not supported yet. Storage image resize covers JPEG/PNG only (no WebP/AVIF). Phone/SMS auth is not available. Production still needs Postgres (or cloud hosting); the single binary is not a full multi-region Supabase stack. Building from source needs Go 1.25+ and Node 22+ to embed the dashboard."],["HISTORY","**instancez** is an Apache-2.0 project shipping a portable Go binary (**cmd/inz**) aimed at declarative, AI-friendly backend scaffolding with Supabase client compatibility."],["SEE ALSO","[supabase](/man/supabase)(1), [curl](/man/curl)(1), [docker](/man/docker)(1)"],["RESOURCES","```[Source code](https://github.com/instancez/instancez)```\n\n```[Homepage](https://instancez.ai)```\n\n```[Documentation](https://instancez.github.io)```"]],"e":["supabase","curl","docker"],"x":"curl -fsSL https://get.instancez.ai | sh"},"josm":{"n":"josm","t":"java OpenStreetMap Editor, a feature-rich desktop application for editing","s":[["TAGLINE","java OpenStreetMap Editor, a feature-rich desktop application for editing"],["TLDR","**Launch JOSM editor**\n\n```josm```\n\n**Open OSM file**\n\n```josm [file.osm]```\n\n**Download area by bounding box**\n\n```josm --download=[minlat],[minlon],[maxlat],[maxlon]```\n\n**Start in offline mode**\n\n```josm --offline=all```\n\n**Set window geometry**\n\n```josm --geometry=[800]x[600]+[100]+[100]```"],["SYNOPSIS","**josm** [_options_] [_file_...]"],["PARAMETERS","**--download=**_bounds_\n> Download bounding box (minlat,minlon,maxlat,maxlon).\n\n**--downloadgps=**_bounds_\n> Download GPS traces for area.\n\n**--offline=**_mode_\n> Start in offline mode (osm_api, josm_website, all).\n\n**--geometry=**_WxH+X+Y_\n> Set window size and position.\n\n**--language=**_lang_\n> Set interface language.\n\n**--selection=**_query_\n> Select objects matching search.\n\n**--version**\n> Display version information."],["DESCRIPTION","**josm** is the Java OpenStreetMap Editor, a feature-rich desktop application for editing OpenStreetMap data. It supports loading and saving OSM data files, GPS tracks, imagery layers, and provides advanced editing tools for mapping."],["INSTALL","```pacman: sudo pacman -S josm```\n\n```nix: nix profile install nixpkgs#josm```"],["SEE ALSO","[osmium](/man/osmium)(1), [osm2pgsql](/man/osm2pgsql)(1)"]],"e":["osmium","osm2pgsql"],"x":"josm"},"kt":{"n":"kt","t":"Kafka topic tool for produce, consume, and admin basics","s":[["TAGLINE","Kafka topic tool for produce, consume, and admin basics"],["TLDR","**Consume** from a topic\n\n```kt consume -topic [my-topic]```\n\n**Produce** messages\n\n```echo \"[hello]\" | kt produce -topic [my-topic]```\n\n**List** topics\n\n```kt topic -filter [my-]```\n\n**Print** message keys/values modes\n\n```kt consume -topic [t] -value-as-string```"],["SYNOPSIS","**kt** *command* [*options*]"],["DESCRIPTION","**kt** is a lightweight Kafka CLI for producing and consuming messages, listing topics, and simple admin tasks without the full JVM **kafka-console-*** tool surface. Configure brokers via flags or environment variables (**KT_BROKERS**, etc.). Install with **go install github.com/fgeller/kt/v14@latest** (module path may change with major versions)."],["PARAMETERS","**consume** **-topic** *name*\n\n> Read messages.\n\n**produce** **-topic** *name*\n\n> Write messages from stdin.\n\n**topic** / **group** / **admin** subcommands\n\n> Metadata and group operations—see **kt -h**.\n\n**-brokers** *list*\n\n> Bootstrap servers."],["CAVEATS","Authentication modes (SASL/SSL) require matching flags/config. Large clusters need careful filters. Confirm module version when installing with Go."],["INSTALL","```nix: nix profile install nixpkgs#kt```"],["SEE ALSO","[kcat](/man/kcat)(1), [kafkacat](/man/kafkacat)(1), [kafka-console-consumer](/man/kafka-console-consumer)(1)"],["RESOURCES","```[Source code](https://github.com/fgeller/kt)```"]],"e":["kcat","kafkacat","kafka-console-consumer"],"x":"kt consume -topic [my-topic]"},"loopsy":{"n":"loopsy","t":"LAN daemon for cross-machine command, file, and agent control","s":[["TAGLINE","LAN daemon for cross-machine command, file, and agent control"],["TLDR","**Initialize** config and generate an API key\n\n```loopsy init```\n\n**Start** the daemon\n\n```loopsy start```\n\n**Stop** the daemon\n\n```loopsy stop```\n\n**Show** daemon status\n\n```loopsy status```\n\n**Initiate pairing** with another machine on the LAN (shows 6-digit code)\n\n```loopsy pair```\n\n**Complete pairing** with a peer at a known address\n\n```loopsy pair [192.168.1.50]```\n\n**List** paired phones\n\n```loopsy phone list```\n\n**Issue a phone-pair token** with a custom TTL\n\n```loopsy mobile pair --ttl [seconds]```\n\n**Tail** the audit log\n\n```loopsy logs -f```\n\n**Run a daemon health check**\n\n```loopsy doctor```"],["SYNOPSIS","**loopsy** _command_ [_subcommand_] [_options_]"],["DESCRIPTION","**loopsy** is a small daemon and CLI that discovers other Loopsy daemons on the local network via **mDNS**, lets the user pair them once, and then exposes a focused set of capabilities between machines: run a command, open a long-lived PTY session, transfer files, browse a peer's filesystem, share key/value state, and exchange protocol-compliant messages with read receipts.\n\nThese capabilities are also exposed to AI coding agents (Claude Code, Codex CLI, Gemini CLI, Opencode, etc.) via **MCP**, so an agent running on one machine can drive another paired machine directly.\n\nPairing uses **ECDH (P-256)** with a 6-digit short authentication string that two users compare visually, similar to **Signal** safety numbers, to prevent man-in-the-middle attacks on the LAN."],["COMMANDS","**init**\n> Generate the local configuration and API key.\n\n**start** / **stop** / **status**\n> Lifecycle control for the local daemon.\n\n**enable** / **disable**\n> Install or remove the auto-start unit (launchd, systemd, or Task Scheduler).\n\n**doctor**\n> Run a health check across config, daemon, MCP integration, and peers.\n\n**logs** [**-f**]\n> Print the audit log; **-f** follows new entries.\n\n**key show** / **key generate**\n> Display or rotate the local API key.\n\n**pair** [_address_]\n> Without an address, initiate pairing on the current machine and print a 6-digit verification code. With an address, complete pairing against the named peer.\n\n**peers add** _address_\n> Manually register a peer without mDNS discovery.\n\n**phone list**\n> List paired phones for this device.\n\n**phone revoke** _id_\n> Revoke a paired phone server-side.\n\n**mobile pair** [**--ttl** _seconds_]\n> Issue a pair token for a phone, printing a QR code and a 4-digit verification code.\n\n**relay configure** _url_\n> Point the daemon at a different relay (for cross-network use)."],["OPTIONS","**--ttl** _seconds_\n> Time-to-live for an issued pair token.\n\n**--lan**\n> Expose the daemon on the local network (non-default).\n\n**-f**\n> Follow log output (with **logs**)."],["CONFIGURATION","The daemon stores its configuration and API key under the user's config directory (created by **loopsy init**). The auto-start unit installed by **loopsy enable** uses the platform's native mechanism: **launchd** on macOS, **systemd --user** on Linux, **Task Scheduler** on Windows."],["CAVEATS","Always compare the 6-digit code on **both** machines before confirming a pair — the short authentication string is what protects against an active attacker on the LAN. Exposing the daemon with **--lan** broadens its attack surface; only use trusted networks. Rotating the API key with **loopsy key generate** invalidates existing agent integrations."],["HISTORY","**loopsy** is developed by **leox255** as an open-source bridge between machines and AI coding agents over the local network and is distributed via **npm**. The design borrows pairing UX from **Signal** and **Magic Wormhole**."],["SEE ALSO","[ssh](/man/ssh)(1), [mosh](/man/mosh)(1)"]],"e":["ssh","mosh"],"x":"loopsy init"},"lzgrep":{"n":"lzgrep","t":"searches for patterns in lzip-compressed files","s":[["TAGLINE","searches for patterns in lzip-compressed files"],["TLDR","**Search in lzip-compressed file**\n\n```lzgrep [pattern] [file.lz]```\n\n**Case-insensitive search**\n\n```lzgrep -i [pattern] [file.lz]```\n\n**Show line numbers**\n\n```lzgrep -n [pattern] [file.lz]```\n\n**Search multiple files**\n\n```lzgrep [pattern] [*.lz]```\n\n**Extended regex**\n\n```lzegrep [pattern] [file.lz]```"],["SYNOPSIS","**lzgrep** [_options_] _pattern_ [_file_...]"],["PARAMETERS","**-i**\n> Case-insensitive search.\n\n**-n**\n> Show line numbers.\n\n**-l**\n> Show only filenames.\n\n**-c**\n> Show match counts.\n\n**-v**\n> Invert match.\n\nAll grep options are supported."],["DESCRIPTION","**lzgrep** searches for patterns in lzip-compressed files. Automatically decompresses before searching. Passes options through to grep. Use lzegrep for extended regex or lzfgrep for fixed strings."],["INSTALL","```dnf: sudo dnf install xz```\n\n```pacman: sudo pacman -S xz```\n\n```apk: sudo apk add xz```\n\n```zypper: sudo zypper install xz```\n\n```brew: brew install xz```\n\n```nix: nix profile install nixpkgs#xz```"],["SEE ALSO","[lzip](/man/lzip)(1), [grep](/man/grep)(1), [zgrep](/man/zgrep)(1)"]],"e":["lzip","grep","zgrep"],"x":"lzgrep [pattern] [file.lz]"},"mariadb-install-db":{"n":"mariadb-install-db","t":"initializes the MariaDB data directory and system tables","s":[["TAGLINE","initializes the MariaDB data directory and system tables"],["TLDR","**Initialize MariaDB data directory**\n\n```mariadb-install-db```\n\n**Initialize with specific user**\n\n```mariadb-install-db --user=[mysql]```\n\n**Initialize with custom data directory**\n\n```mariadb-install-db --datadir=[/var/lib/mysql]```\n\n**Initialize with specific base directory**\n\n```mariadb-install-db --basedir=[/usr]```"],["SYNOPSIS","**mariadb-install-db** [_options_]"],["PARAMETERS","**--user** _user_\n> User for database files.\n\n**--datadir** _dir_\n> Data directory path.\n\n**--basedir** _dir_\n> MariaDB base directory.\n\n**--defaults-file** _file_\n> Configuration file.\n\n**--auth-root-authentication-method** _method_\n> Root authentication method.\n\n**--skip-test-db**\n> Don't create test database."],["DESCRIPTION","**mariadb-install-db** initializes the MariaDB data directory and system tables. Required before first server start. Creates privilege tables and default databases. Replaces mysql_install_db in MariaDB."],["INSTALL","```apt: sudo apt install mariadb-server-core```\n\n```dnf: sudo dnf install mariadb```\n\n```pacman: sudo pacman -S mariadb```\n\n```apk: sudo apk add mariadb```\n\n```zypper: sudo zypper install mariadb```\n\n```brew: brew install mariadb```\n\n```nix: nix profile install nixpkgs#mariadb```"],["SEE ALSO","[mariadb](/man/mariadb)(1)"]],"e":["mariadb"],"x":"mariadb-install-db"},"mkisofs":{"n":"mkisofs","t":"pre-mastering utility that generates ISO9660, Joliet, and HFS hybrid","s":[["TAGLINE","pre-mastering utility that generates ISO9660, Joliet, and HFS hybrid"],["TLDR","Create an **ISO image** from a directory\n\n```mkisofs -o [filename.iso] [path/to/source_directory]```\n\nSet the **disc label** when creating an ISO\n\n```mkisofs -o [filename.iso] -V \"[label_name]\" [path/to/source_directory]```\n\nCreate ISO with **Rock Ridge extensions** for Unix compatibility\n\n```mkisofs -o [filename.iso] -R [path/to/source_directory]```\n\nCreate ISO with **Joliet extensions** for Windows compatibility\n\n```mkisofs -o [filename.iso] -J [path/to/source_directory]```\n\nCreate **bootable ISO** with El Torito boot image\n\n```mkisofs -o [filename.iso] -b [boot_image] [path/to/source_directory]```\n\n**Exclude files** matching a pattern\n\n```mkisofs -o [filename.iso] -m \"*.bak\" [path/to/source_directory]```"],["SYNOPSIS","**mkisofs** [**-o** _filename_] [**-V** _volid_] [**-R**] [**-J**] [**-b** _boot_image_] [**-m** _glob_] _pathspec_ [_pathspec_...]"],["PARAMETERS","**-o _filename_**\n> Specify output file location for the ISO image\n\n**-V _volid_**\n> Set volume identifier; maximum 32 characters\n\n**-R**\n> Generate Rock Ridge SUSP records for Unix file attributes\n\n**-r**\n> Like -R but normalizes ownership and permissions globally\n\n**-J**\n> Add Joliet directory records for Windows long filename support\n\n**-l**\n> Allow full 31-character filenames (not MS-DOS compatible)\n\n**-b _boot_image_**\n> Specify El Torito boot image path relative to the source directory.\n\n**-no-emul-boot**\n> Use no-emulation boot mode (boot image is not a floppy image).\n\n**-boot-info-table**\n> Insert boot information table at offset 8 in boot image\n\n**-m _glob_**\n> Exclude files matching shell wildcard pattern\n\n**-graft-points**\n> Allow custom directory grafting with path=source syntax\n\n**-hide _glob_**\n> Hide files from ISO9660 and Rock Ridge directory trees\n\n**-hide-joliet _glob_**\n> Hide files from Joliet tree only\n\n**-udf**\n> Generate a UDF filesystem in addition to ISO9660.\n\n**-quiet**\n> Minimize verbose output.\n\n**-print-size**\n> Calculate and display filesystem size in sectors."],["DESCRIPTION","**mkisofs** (also known as **genisoimage**) is a pre-mastering utility that generates ISO9660, Joliet, and HFS hybrid filesystem images. It creates binary images suitable for writing to CDs, DVDs, or other optical media.\n\nThe tool supports Rock Ridge extensions for Unix file attributes, Joliet extensions for Windows long filenames, El Torito for bootable media, and Apple HFS hybrid filesystems. It processes directory trees without directly communicating with disc writers."],["CAVEATS","Basic ISO9660 limits filenames to 8.3 format; use -R or -J for longer names. The -boot-info-table option modifies the source boot file directly; use a copy if needed. HFS and UDF support are partially experimental. Character encoding varies between platforms and may require explicit charset options."],["HISTORY","**mkisofs** was originally written by Eric Youngdale and later maintained as part of the **cdrtools** package by Joerg Schilling. The **genisoimage** fork was created as part of **cdrkit** for Debian and derivatives. The ISO9660 standard dates to 1988."],["INSTALL","```apt: sudo apt install genisoimage```\n\n```dnf: sudo dnf install genisoimage```\n\n```apk: sudo apk add xorriso```\n\n```zypper: sudo zypper install xorriso```\n\n```brew: brew install xorriso```\n\n```nix: nix profile install nixpkgs#xorriso```"],["SEE ALSO","[genisoimage](/man/genisoimage)(1), [wodim](/man/wodim)(1), [cdrecord](/man/cdrecord)(1), [isoinfo](/man/isoinfo)(8)"]],"e":["genisoimage","wodim","cdrecord","isoinfo"],"x":"mkisofs -o [filename.iso] [path/to/source_directory]"},"mp4box":{"n":"mp4box","t":"multimedia packager from the GPAC framework","s":[["TAGLINE","multimedia packager from the GPAC framework"],["TLDR","**Add audio track to video**\n\n```mp4box -add [audio.m4a] [video.mp4]```\n\n**Merge multiple MP4 files**\n\n```mp4box -cat [file1.mp4] -cat [file2.mp4] -new [output.mp4]```\n\n**Extract specific track**\n\n```mp4box -raw [1] [input.mp4]```\n\n**Add subtitles to video**\n\n```mp4box -add [subtitles.srt] [video.mp4]```\n\n**Show file information**\n\n```mp4box -info [file.mp4]```\n\n**Split file at intervals**\n\n```mp4box -split [60] [input.mp4]```\n\n**Fragment for DASH streaming**\n\n```mp4box -dash [4000] [input.mp4]```\n\n**Remove specific track**\n\n```mp4box -rem [2] [input.mp4]```"],["SYNOPSIS","**mp4box** [_-add file_] [_-cat file_] [_-info_] [_-dash duration_] [_options_] _file_"],["PARAMETERS","**-add** _file_[:_opts_]\n> Add media to file (import track).\n\n**-cat** _file_\n> Concatenate file.\n\n**-rem** _trackID_\n> Remove track by ID.\n\n**-raw** _trackID_\n> Extract raw track data.\n\n**-info** [_trackID_]\n> Display file or track information.\n\n**-new** _file_\n> Create new output file.\n\n**-out** _file_\n> Specify output file name.\n\n**-split** _seconds_\n> Split file at time intervals.\n\n**-split-size** _KB_\n> Split file by size.\n\n**-dash** _ms_\n> Create DASH segments with duration in ms.\n\n**-frag** _ms_\n> Fragment file with duration in ms.\n\n**-rap**\n> Split only at random access points (keyframes).\n\n**-flat**\n> Store in flat mode (non-interleaved).\n\n**-inter** _ms_\n> Interleave with duration in ms.\n\n**-tmp** _dir_\n> Use alternate temp directory.\n\n**-nosys**\n> Remove system track info.\n\n**-fps** _rate_\n> Force frame rate for raw imports.\n\n**-lang** _code_\n> Set track language."],["DESCRIPTION","**MP4Box** is a multimedia packager from the GPAC framework. It manipulates MP4/ISO base media files - adding, removing, and editing tracks without re-encoding.\n\nThe tool handles muxing (combining audio, video, subtitles into one container), demuxing (extracting tracks), and file manipulation (splitting, concatenating, fragmenting). It supports numerous formats for import including H.264, H.265, AAC, MP3, SRT, and WebVTT.\n\nDASH (Dynamic Adaptive Streaming over HTTP) support creates segmented files for adaptive streaming. This fragments content into small chunks with a manifest file, enabling players to switch quality based on bandwidth.\n\nFor streaming preparation, MP4Box can fragment files for HTTP progressive download, add hint tracks for RTSP streaming, and create initialization segments for live streaming workflows.\n\nMetadata editing covers MP4 tags, chapter markers, and track properties. The tool can also encrypt content with various DRM schemes."],["CAVEATS","Does not transcode - only repackages streams. Some operations modify files in place. Complex command syntax for advanced operations. DASH output requires proper web server configuration. Large files may need significant temporary storage. Some format combinations require specific codec compatibility."],["HISTORY","**MP4Box** is part of **GPAC**, an open-source multimedia framework started by **Jean Le Feuvre** at **ENST Paris** around **2000**. Originally focused on MPEG-4 research, GPAC expanded to support modern streaming formats. MP4Box became widely used for DASH content preparation and is relied upon by streaming services and content creators for media packaging."],["INSTALL","```brew: brew install mp4box```"],["SEE ALSO","[ffmpeg](/man/ffmpeg)(1), [mkvmerge](/man/mkvmerge)(1), [mediainfo](/man/mediainfo)(1), [handbrake](/man/handbrake)(1)"]],"e":["ffmpeg","mkvmerge","mediainfo","handbrake"],"x":"mp4box -add [audio.m4a] [video.mp4]"},"ngettext":{"n":"ngettext","t":"handles plural forms in translations","s":[["TAGLINE","handles plural forms in translations"],["TLDR","**Translate with plural form**\n\n```ngettext \"[singular]\" \"[plural]\" [count]```\n\n**Translate with domain**\n\n```ngettext -d [domain] \"[singular]\" \"[plural]\" [count]```\n\n**Use specific locale**\n\n```LANG=[de_DE] ngettext \"[1 file]\" \"[%d files]\" [5]```\n\n**Specify directory**\n\n```ngettext -d [domain] -e \"[singular]\" \"[plural]\" [count]```"],["SYNOPSIS","**ngettext** [_options_] _singular_ _plural_ _count_"],["PARAMETERS","_SINGULAR_\n> Singular form message.\n\n_PLURAL_\n> Plural form message.\n\n_COUNT_\n> Number for plural selection.\n\n**-d** _DOMAIN_\n> Message domain.\n\n**-e**\n> Enable escape sequences.\n\n**--help**\n> Display help information."],["DESCRIPTION","**ngettext** handles plural forms in translations. It's part of GNU gettext.\n\nThe tool selects correct plural form based on count. Language-specific plural rules."],["CAVEATS","Part of gettext. Shell utility. Locale-dependent output."],["HISTORY","ngettext is part of **GNU gettext**, providing plural form support for internationalized programs."],["INSTALL","```apt: sudo apt install gettext-base```\n\n```dnf: sudo dnf install gettext```\n\n```pacman: sudo pacman -S gettext```\n\n```apk: sudo apk add gettext```\n\n```brew: brew install gettext```\n\n```nix: nix profile install nixpkgs#gettext```"],["SEE ALSO","[gettext](/man/gettext)(1), [envsubst](/man/envsubst)(1), [msgfmt](/man/msgfmt)(1)"]],"e":["gettext","envsubst","msgfmt"],"x":"ngettext \"[singular]\" \"[plural]\" [count]"},"npm-home":{"n":"npm-home","t":"opens package homepage in browser","s":[["TAGLINE","opens package homepage in browser"],["TLDR","**Open package homepage**\n\n```npm home [package-name]```\n\n**Open current project homepage**\n\n```npm home```\n\n**Open multiple package homepages**\n\n```npm home [package1] [package2]```"],["SYNOPSIS","**npm** **home** [_packages_]"],["PARAMETERS","_PACKAGES_\n> Package names.\n\n**--help**\n> Display help information."],["DESCRIPTION","**npm home** opens package homepage in browser. Uses homepage field from package.json.\n\nThe command navigates to project website. Alias for npm docs."],["CAVEATS","Requires browser. Falls back to npm registry. Uses package.json homepage."],["HISTORY","npm home provides **quick access** to package homepages from the command line."],["INSTALL","```pacman: sudo pacman -S npm```\n\n```apk: sudo apk add npm```\n\n```brew: brew install npm```"],["SEE ALSO","[npm](/man/npm)(1), [npm-docs](/man/npm-docs)(1), [npm-repo](/man/npm-repo)(1)"]],"e":["npm","npm-docs","npm-repo"],"x":"npm home [package-name]"},"npm-outdated":{"n":"npm-outdated","t":"checks the registry for newer versions of installed packages","s":[["TAGLINE","checks the registry for newer versions of installed packages"],["TLDR","**Check outdated packages**\n\n```npm outdated```\n\n**Check global packages**\n\n```npm outdated -g```\n\n**Output as JSON**\n\n```npm outdated --json```\n\n**Check specific package**\n\n```npm outdated [package]```\n\n**Long format output**\n\n```npm outdated --long```\n\n**Include nested (transitive) dependencies**\n\n```npm outdated --all```\n\n**Parseable tab-separated output**\n\n```npm outdated --parseable```"],["SYNOPSIS","**npm outdated** [_options_] [_packages_...]"],["PARAMETERS","**-g**, **--global**\n> Check globally installed packages instead of the local project.\n\n**--json**\n> Output as JSON (machine-parseable).\n\n**-l**, **--long**\n> Extended output including package type (dependencies, devDependencies, peerDependencies, optionalDependencies) and homepage.\n\n**-p**, **--parseable**\n> Tab-separated output suitable for parsing.\n\n**--all**\n> Show all outdated packages, including meta-dependencies (transitive/nested), not just direct dependencies.\n\n**--omit** _type_\n> Exclude a dependency group (dev, optional, peer). May be set multiple times.\n\n**-w**, **--workspace** _name_\n> Run the command within the named workspace(s).\n\n**-ws**, **--workspaces**\n> Run across all configured workspaces.\n\n**--include-workspace-root**\n> Include the workspace root when running with --workspaces."],["DESCRIPTION","**npm outdated** checks the registry for newer versions of installed packages. It reports the currently installed version, the maximum version satisfying the semver range in package.json (**wanted**), and the latest version tagged in the registry (**latest**).\n\nColor coding in terminal output: **red** indicates an available update within your semver range (safe to run `npm update`); **yellow** indicates a newer version exists that exceeds your semver range (requires a manual bump)."],["OUTPUT COLUMNS","```\nPackage   Current  Wanted  Latest  Location            Depended by\nlodash    4.17.0   4.17.21 4.17.21 node_modules/lodash my-app\n\nCurrent     - Currently installed version\nWanted      - Max version satisfying semver range\nLatest      - Version tagged \"latest\" in the registry\nLocation    - Location in the dependency tree\nDepended by - Package depending on this one\nPackage type - (with --long) dependencies/devDependencies/peer/optional\n```"],["EXAMPLE","```bash\n$ npm outdated\nPackage  Current  Wanted  Latest\nexpress  4.17.1   4.18.2  4.18.2\nlodash   4.17.0   4.17.21 4.17.21\n```"],["CAVEATS","Only shows direct dependencies by default; pass **--all** to include transitive ones. The command only reports — it never installs or modifies anything. Use **npm update** to apply updates within semver ranges, or bump the version in package.json for major upgrades. Exits with a non-zero status when outdated packages are found, which can break CI pipelines."],["HISTORY","npm outdated was added to npm to help developers identify packages that need updating, complementing npm update."],["INSTALL","```pacman: sudo pacman -S npm```\n\n```apk: sudo apk add npm```\n\n```brew: brew install npm```"],["SEE ALSO","[npm](/man/npm)(1), [npm-update](/man/npm-update)(1), [npm-ls](/man/npm-ls)(1)"]],"e":["npm","npm-update","npm-ls"],"x":"npm outdated"},"patch":{"n":"patch","t":"applies changes described in a patch file to original files","s":[["TAGLINE","applies changes described in a patch file to original files"],["TLDR","**Apply a patch**\n\n```patch < [patchfile.patch]```\n\n**Apply patch with strip level**\n\n```patch -p1 < [patchfile.patch]```\n\n**Apply patch to specific file**\n\n```patch [file] < [patchfile.patch]```\n\n**Apply patch in dry-run** mode\n\n```patch --dry-run < [patchfile.patch]```\n\n**Reverse a patch**\n\n```patch -R < [patchfile.patch]```\n\n**Create backup** of original files\n\n```patch -b < [patchfile.patch]```\n\n**Apply patch** with different prefix\n\n```patch -d [/path/to/directory] -p1 < [patchfile.patch]```\n\n**Force apply** ignoring errors\n\n```patch -f < [patchfile.patch]```"],["SYNOPSIS","**patch** [_options_] [_original_] [_patchfile_]"],["DESCRIPTION","**patch** applies changes described in a patch file to original files. Patch files are typically created by diff and contain instructions for adding, modifying, or removing lines.\n\nThe tool reads the patch file and attempts to apply each hunk (section of changes) to the target file. If a hunk doesn't match exactly, patch tries to find a location where it can apply with fuzz (allowing some context lines to differ).\n\npatch is essential for software development, applying updates to source code, and managing code changes distributed as patches."],["PARAMETERS","**-p** _num_\n> Strip num leading path components.\n\n**-R**, **--reverse**\n> Reverse the patch.\n\n**-N**, **--forward**\n> Ignore already applied patches.\n\n**-b**, **--backup**\n> Create backup files.\n\n**-d** _dir_, **--directory=** _dir_\n> Change to directory first.\n\n**--dry-run**\n> Don't modify files, show what would happen.\n\n**-f**, **--force**\n> Assume user knows best, don't ask.\n\n**-i** _file_, **--input=** _file_\n> Read patch from file.\n\n**-o** _file_, **--output=** _file_\n> Output to file.\n\n**-F** _num_, **--fuzz=** _num_\n> Set max fuzz factor.\n\n**-l**, **--ignore-whitespace**\n> Ignore whitespace changes.\n\n**--verbose**\n> Output debugging information.\n\n**-V** _method_, **--version-control=** _method_\n> Backup versioning method (simple, numbered, existing).\n\n**-E**, **--remove-empty-files**\n> Remove empty files after patching."],["CAVEATS","Patches may fail if files have changed since the diff was created. Fuzz can cause patches to apply in wrong locations. Always use --dry-run first for unfamiliar patches. Reverse patches should be applied in reverse order."],["HISTORY","**patch** was written by **Larry Wall** in **1985** as part of his contributions to Unix tools. It became essential for distributing software changes before version control systems were widespread. The tool remains important for kernel development, where patches are the standard method for submitting changes."],["INSTALL","```apt: sudo apt install patch```\n\n```dnf: sudo dnf install patch```\n\n```pacman: sudo pacman -S patch```\n\n```apk: sudo apk add patch```\n\n```zypper: sudo zypper install patch```\n\n```nix: nix profile install nixpkgs#patch```"],["SEE ALSO","[diff](/man/diff)(1), [git-apply](/man/git-apply)(1), [quilt](/man/quilt)(1)"]],"e":["diff","git-apply","quilt"],"x":"patch < [patchfile.patch]"},"pct-clone":{"n":"pct-clone","t":"creates a copy of an existing Proxmox LXC container","s":[["TAGLINE","creates a copy of an existing Proxmox LXC container"],["TLDR","**Clone** a container (linked clone by default)\n\n```pct clone [template_id] [new_id]```\n\nClone with custom **hostname**\n\n```pct clone [template_id] [new_id] --hostname [name]```\n\nCreate a **full (independent) copy**\n\n```pct clone [template_id] [new_id] --full```\n\nClone to a **specific storage**\n\n```pct clone [template_id] [new_id] --full --storage [storage_name]```\n\nClone with a **descriptive note**\n\n```pct clone [template_id] [new_id] --description \"[note]\"```"],["SYNOPSIS","**pct clone** _vmid_ _newid_ [_OPTIONS_]"],["DESCRIPTION","**pct clone** creates a copy of an existing Proxmox LXC container. This is useful for creating multiple containers from a configured template container or for backup purposes."],["PARAMETERS","**vmid**\n> The numeric ID of the source container\n\n**newid**\n> The numeric ID for the cloned container\n\n**--hostname** _name_\n> Set a custom hostname for the clone\n\n**--full**\n> Create a full copy instead of linked clone\n\n**--target** _node_\n> Target node for the clone\n\n**--storage** _storage_\n> Storage for the clone's disks"],["CAVEATS","The source container should be stopped for a consistent clone. Linked clones share base storage with the original. Full clones are independent but require more disk space."],["HISTORY","**pct clone** is part of the **Proxmox VE** virtualization platform for managing LXC containers."],["SEE ALSO","[pct](/man/pct)(1), [pct-create](/man/pct-create)(1)"]],"e":["pct","pct-create"],"x":"pct clone [template_id] [new_id]"},"pdftk":{"n":"pdftk","t":"command-line tool for manipulating PDF documents","s":[["TAGLINE","command-line tool for manipulating PDF documents"],["TLDR","**Merge PDF files**\n\n```pdftk [file1.pdf] [file2.pdf] cat output [merged.pdf]```\n\n**Extract specific pages**\n\n```pdftk [input.pdf] cat [1-5] [10] output [pages.pdf]```\n\n**Rotate pages**\n\n```pdftk [input.pdf] cat [1-endeast] output [rotated.pdf]```\n\n**Split PDF into single pages**\n\n```pdftk [input.pdf] burst output [page_%02d.pdf]```\n\n**Decrypt PDF**\n\n```pdftk [encrypted.pdf] input_pw [password] output [decrypted.pdf]```\n\n**Add password protection**\n\n```pdftk [input.pdf] output [protected.pdf] owner_pw [pass] user_pw [pass]```\n\n**Fill PDF form**\n\n```pdftk [form.pdf] fill_form [data.fdf] output [filled.pdf]```\n\n**Get PDF info**\n\n```pdftk [input.pdf] dump_data```"],["SYNOPSIS","**pdftk** _input_ [_input_pw password_] [_operation_] [_output file_] [_encryption options_]"],["PARAMETERS","**cat**\n> Concatenate pages (merge, extract, reorder).\n\n**shuffle**\n> Interleave pages from multiple files.\n\n**burst**\n> Split into single-page PDFs.\n\n**rotate**\n> Rotate pages.\n\n**fill_form**\n> Fill form fields from FDF/XFDF.\n\n**dump_data**\n> Report PDF metadata.\n\n**dump_data_fields**\n> Report form field information.\n\n**update_info**\n> Update PDF metadata.\n\n**attach_files**\n> Attach files to PDF.\n\n**unpack_files**\n> Extract attachments.\n\n**background**\n> Apply PDF as background.\n\n**stamp**\n> Apply PDF as foreground stamp.\n\n**output**\n> Specify output file.\n\n**input_pw** _PASS_\n> Input file password.\n\n**owner_pw** _PASS_\n> Set owner password (full permissions).\n\n**user_pw** _PASS_\n> Set user password (limited permissions).\n\n**allow** _PERMS_\n> Set allowed permissions.\n\n**encrypt_40bit**\n> Use 40-bit encryption.\n\n**encrypt_128bit**\n> Use 128-bit encryption.\n\n**flatten**\n> Flatten form fields.\n\n**compress**\n> Compress output.\n\n**uncompress**\n> Decompress output."],["DESCRIPTION","**pdftk** (PDF Toolkit) is a command-line tool for manipulating PDF documents. It can merge, split, rotate, encrypt, decrypt, fill forms, and modify metadata without quality loss.\n\nPage selection uses flexible syntax: ranges (1-5), specific pages (3,7,10), reverse order (end-1), and rotation suffixes (north, south, east, west). Multiple input files can be combined with per-file page selections.\n\nForm handling fills interactive PDF forms from FDF (Forms Data Format) or XFDF data files. The dump_data_fields command extracts field information for preparing fill data. Flatten permanently merges form data into the document.\n\nEncryption options set owner and user passwords with permission controls. Owner password grants full access; user password can have restricted permissions (no printing, no copying, etc.).\n\nThe stamp and background operations overlay PDFs, useful for watermarks, letterheads, or combining content. Attachments can embed files within PDFs."],["CAVEATS","Some complex PDFs may not process correctly. Java version (pdftk-java) differs from original C++ version. Encryption is not always strong. Large files may be slow. Form filling works best with Acrobat-created forms. Some features deprecated in newer PDF standards."],["HISTORY","**pdftk** was originally written by **Sid Steward** in **2003** using iText library. It became the standard command-line PDF tool on Linux. After the original became unmaintained, **pdftk-java** emerged as a port to Java, maintaining compatibility while enabling continued development."],["INSTALL","```pacman: sudo pacman -S pdftk```\n\n```zypper: sudo zypper install pdftk```\n\n```nix: nix profile install nixpkgs#pdftk```"],["SEE ALSO","[qpdf](/man/qpdf)(1), [poppler-utils](/man/poppler-utils)(1), [ghostscript](/man/ghostscript)(1), [pdfunite](/man/pdfunite)(1)"]],"e":["qpdf","poppler-utils","ghostscript","pdfunite"],"x":"pdftk [file1.pdf] [file2.pdf] cat output [merged.pdf]"},"phive":{"n":"phive","t":"PHAR Installation and Verification","s":[["TAGLINE","PHAR Installation and Verification"],["TLDR","**Install a PHAR tool** from the default repository\n\n```phive install [phpunit]```\n\n**Pin to a specific version** with the `@` syntax\n\n```phive install [phpunit@^9.5]```\n\n**Install system-wide** by copying into a shared directory\n\n```phive install --copy --target [/usr/local/bin] [phpunit]```\n\n**Show installed tools and available updates**\n\n```phive status```\n\n**Update all installed tools** to the latest pinned version\n\n```phive update```\n\n**List which tools are outdated**\n\n```phive outdated```\n\n**Remove a tool**\n\n```phive remove [phpunit]```\n\n**Update Phive itself**\n\n```phive selfupdate```"],["SYNOPSIS","**phive** _command_ [_options_]"],["COMMANDS","**install** _TOOL_[**@**_VERSION_]\n> Install a PHAR tool, optionally pinning to a version constraint (e.g. **@^9.5**).\n\n**update** [_TOOL_...]\n> Update one or more installed tools to their latest compatible versions.\n\n**remove** _TOOL_\n> Uninstall a tool and delete its PHAR.\n\n**reset** [_TOOL_]\n> Re-download a tool without changing its version pin.\n\n**status**\n> List installed tools and any drift from the pinned version.\n\n**outdated**\n> Report tools that have newer versions available.\n\n**list**\n> List tools known to the configured repository.\n\n**purge**\n> Remove unused PHARs from the local cache.\n\n**default**\n> Show or change the default repository configuration.\n\n**selfupdate**\n> Update the Phive tool itself to the latest release."],["PARAMETERS","**--copy**\n> Copy the PHAR to the target location instead of symlinking it (needed for shared/system-wide installs).\n\n**--target** _DIR_\n> Install the PHAR into _DIR_. Use this together with **--copy** for a global install (there is no **--global** flag).\n\n**--force-accept-unsigned**\n> Accept PHARs that lack a valid GPG signature (not recommended).\n\n**--trust-gpg-keys** _KEYIDS_\n> Pre-trust specific GPG key IDs without prompting.\n\n**--prefer-offline**\n> Use cached metadata (useful with **update**).\n\n**--help**\n> Display help."],["DESCRIPTION","**phive** — the **PHAR Installation and Verification Environment** — manages command-line PHP tools distributed as PHAR archives. It downloads PHARs from a trusted repository, verifies their GPG signatures, and records pinned versions in a project-local **phive.xml** so they can be reproducibly reinstalled.\n\nTypical usage is project-local: **phive install phpunit** writes the tool into **tools/** in the current project. System-wide installs are performed by combining **--copy** and **--target** with a directory on **$PATH**."],["CAVEATS","Requires PHP with the **phar** extension. GPG is used to verify PHAR signatures; missing or untrusted keys will block installation unless explicitly overridden. Tools must be published to the Phive registry (or a custom repository) to be installable by name."],["HISTORY","Phive was created for **secure phar distribution** with signature verification."],["INSTALL","```brew: brew install phive```"],["SEE ALSO","[composer](/man/composer)(1), [php](/man/php)(1)"]],"e":["composer","php"],"x":"phive install [phpunit]"},"pkg-config":{"n":"pkg-config","t":"Query library compiler and linker flags","s":[["TAGLINE","Query library compiler and linker flags"],["TLDR","**Get compiler flags**\n\n```pkg-config --cflags [library]```\n\n**Get linker flags**\n\n```pkg-config --libs [library]```\n\n**Get all flags**\n\n```pkg-config --cflags --libs [library]```\n\n**Check if library exists**\n\n```pkg-config --exists [library] && echo \"Found\"```\n\n**Get library version**\n\n```pkg-config --modversion [library]```\n\n**List all packages**\n\n```pkg-config --list-all```"],["SYNOPSIS","**pkg-config** [_options_] [_packages_]"],["PARAMETERS","_PACKAGES_\n> Package names.\n\n**--cflags**\n> Compiler flags.\n\n**--libs**\n> Linker flags.\n\n**--exists**\n> Check existence.\n\n**--modversion**\n> Show version.\n\n**--list-all**\n> List packages.\n\n**--atleast-version** _VERSION_\n> Exit successfully if version is at least VERSION.\n\n**--exact-version** _VERSION_\n> Exit successfully if version matches exactly.\n\n**--max-version** _VERSION_\n> Exit successfully if version is at most VERSION.\n\n**--variable** _NAME_\n> Print the value of a variable defined in the .pc file.\n\n**--libs-only-L**\n> Print only -L/-R library search path flags.\n\n**--libs-only-l**\n> Print only -l library name flags.\n\n**--cflags-only-I**\n> Print only -I include path flags.\n\n**--static**\n> Output for static linking (include private dependencies).\n\n**--print-errors**\n> Show errors when package not found.\n\n**--silence-errors**\n> Suppress error output."],["DESCRIPTION","**pkg-config** retrieves compiler and linker flags needed to build software against installed libraries. It reads metadata from **.pc** files that libraries install, providing the correct include paths, library paths, and link flags.\n\nBuild systems like autotools, CMake, and Meson use pkg-config to locate libraries portably. The **--cflags** flag returns compiler flags (include paths), **--libs** returns linker flags, and **--modversion** shows the installed version. Version constraints can be checked with **--atleast-version**."],["CONFIGURATION","**PKG_CONFIG_PATH**\n> Colon-separated list of additional directories to search for .pc files.\n\n**PKG_CONFIG_LIBDIR**\n> Override the default .pc file search path entirely."],["CAVEATS","Requires .pc files. PKG_CONFIG_PATH for custom locations."],["HISTORY","pkg-config was created by **James Henstridge** around **2001** as part of the freedesktop.org project to simplify library compilation and linking. It is widely used in autotools, CMake, and Meson build systems."],["INSTALL","```apt: sudo apt install pkgconf```\n\n```dnf: sudo dnf install pkgconf```\n\n```pacman: sudo pacman -S pkgconf```\n\n```apk: sudo apk add pkgconf```\n\n```zypper: sudo zypper install pkgconf```\n\n```brew: brew install pkgconf```\n\n```nix: nix profile install nixpkgs#pkgconf```"],["SEE ALSO","[make](/man/make)(1), [gcc](/man/gcc)(1), [pkgconf](/man/pkgconf)(1)"]],"e":["make","gcc","pkgconf"],"x":"pkg-config --cflags [library]"},"pkgctl-diff":{"n":"pkgctl-diff","t":"Compare Arch package file differences","s":[["TAGLINE","Compare Arch package file differences"],["TLDR","Compare package files in **tar content list** mode (default)\n\n```pkgctl diff -l path/to/file```\n\nCompare package files using **diffoscope**\n\n```pkgctl diff -d path/to/file```\n\nCompare package **.PKGINFO** files\n\n```pkgctl diff -p path/to/file```\n\nCompare package **.BUILDINFO** files\n\n```pkgctl diff -b path/to/file```"],["SYNOPSIS","**pkgctl diff** [_options_] _file|pkgname_"],["PARAMETERS","**-l**, **--list**\n> Compare using tar content list mode (default)\n\n**-d**, **--diffoscope**\n> Compare using diffoscope for detailed diff\n\n**-p**, **--pkginfo**\n> Compare .PKGINFO metadata files\n\n**-b**, **--buildinfo**\n> Compare .BUILDINFO files"],["DESCRIPTION","**pkgctl diff** compares a locally built **.pkg.tar.zst** against the same package as currently published in the official Arch repositories (or against another local package file). It is the standard pre-release sanity check for Arch package maintainers, exposing accidental file moves, dropped binaries, soname bumps, or unintended **.PKGINFO**/**.BUILDINFO** drift.\n\nWithout flags it runs in **--list** mode, which compares the tar manifest. Use **--diffoscope** for byte-level differences (slow but exhaustive), **--pkginfo** for runtime metadata, or **--buildinfo** to verify reproducibility-relevant fields like compiler flags and installed build dependencies."],["CAVEATS","**--diffoscope** requires the **diffoscope** package and can be very slow on packages with large compiled artifacts. When passed a bare _pkgname_, **pkgctl diff** downloads the published package from the mirrors, so a working network and configured Pacman mirrorlist are required."],["HISTORY","Part of **pkgctl**, the Arch Linux package control tool for official repository maintainers."],["SEE ALSO","[pkgctl](/man/pkgctl)(1), [diffoscope](/man/diffoscope)(1)"]],"e":["pkgctl","diffoscope"],"x":"pkgctl diff -l path/to/file"},"podman-compose":{"n":"podman-compose","t":"Run multi-container applications with Podman","s":[["TAGLINE","Run multi-container applications with Podman"],["TLDR","**Start services**\n\n```podman-compose up```\n\n**Start in background**\n\n```podman-compose up -d```\n\n**Stop services**\n\n```podman-compose down```\n\n**View logs**\n\n```podman-compose logs```\n\n**Build images**\n\n```podman-compose build```\n\n**List containers**\n\n```podman-compose ps```\n\n**Execute command in service**\n\n```podman-compose exec [service] [command]```"],["SYNOPSIS","**podman-compose** [_options_] _command_ [_args_...]"],["SUBCOMMANDS","**up** [_service_...]\n> Create and start the project's containers, networks, and volumes. **--build** forces a rebuild, **--force-recreate** recreates running containers.\n\n**down** [**-v**] [**--remove-orphans**]\n> Stop and remove containers (and optionally volumes / orphan containers).\n\n**start** / **stop** / **restart** / **pause** / **unpause** [_service_...]\n> Lifecycle commands for already-created services.\n\n**build** [_service_...]\n> Build (or rebuild) service images.\n\n**pull** / **push** [_service_...]\n> Pull or push service images.\n\n**logs** [**-f**] [_service_...]\n> View / follow container logs.\n\n**ps**\n> List containers belonging to the project.\n\n**exec** [_service_] _command_\n> Run a command in a running service.\n\n**run** [_service_] _command_\n> Start a one-off container with the service's configuration.\n\n**config**\n> Render the merged Compose file as YAML and validate it.\n\n**top** / **kill** / **rm** / **port** / **events**\n> Mirror the equivalent **docker compose** subcommands."],["OPTIONS","**-f** _FILE_, **--file** _FILE_\n> Path to a Compose file. Repeatable for layering multiple files.\n\n**-p** _NAME_, **--project-name** _NAME_\n> Project name (default: parent directory). Used as a prefix for container, network, and volume names.\n\n**--profile** _NAME_\n> Activate a Compose profile.\n\n**--podman-args** _ARGS_\n> Pass extra arguments to every **podman** invocation.\n\n**--podman-run-args** _ARGS_, **--podman-build-args** _ARGS_, **--podman-pull-args** _ARGS_\n> Pass extra arguments to specific Podman subcommands.\n\n**-d**, **--detach**\n> Run in the background (for **up**, **run**).\n\n**--no-cache**\n> Disable build cache (for **build**).\n\n**--dry-run**\n> Print the Podman commands that would be executed without running them.\n\n**-v**, **--verbose**\n> Verbose output."],["DESCRIPTION","**podman-compose** is a thin Python re-implementation of **docker compose** that drives **podman** instead of the Docker daemon. It reads **docker-compose.yml** / **compose.yaml** / **podman-compose.yml** files (Compose specification 1.x and 3.x) and translates them into a series of `podman run`, `podman build`, and `podman network create` commands, so it works rootless and without a daemon.\n\nModern Podman (4.0+) also ships native `podman compose` (with a space), which delegates to either **podman-compose** or **docker compose** depending on what is available — both eventually call the same Podman API."],["CAVEATS","Coverage of the Compose spec is good but not 100% — some advanced features (for example `extends`, healthcheck-driven `depends_on.condition`, certain `deploy.*` keys) behave differently or are silently ignored. Compose-managed networks use podman's CNI/Netavark backends, so DNS-based service discovery requires the **dnsname** plugin (CNI) or Aardvark-DNS (Netavark, the default since Podman 4.0)."],["INSTALL","```dnf: sudo dnf install podman-compose```\n\n```pacman: sudo pacman -S podman-compose```\n\n```apk: sudo apk add podman-compose```\n\n```brew: brew install podman-compose```\n\n```nix: nix profile install nixpkgs#podman-compose```"],["SEE ALSO","[podman](/man/podman)(1), [docker-compose](/man/docker-compose)(1)"]],"e":["podman","docker-compose"],"x":"podman-compose up"},"ppmtopcx":{"n":"ppmtopcx","t":"Convert PPM images to PCX format","s":[["TAGLINE","Convert PPM images to PCX format"],["TLDR","**Convert PPM to PCX**\n\n```ppmtopcx [input.ppm] > [output.pcx]```\n\n**Convert with 256 colors**\n\n```ppmtopcx -256 [input.ppm] > [output.pcx]```\n\n**Convert with 24-bit color**\n\n```ppmtopcx -24bit [input.ppm] > [output.pcx]```"],["SYNOPSIS","**ppmtopcx** [_options_] [_file_]"],["PARAMETERS","**-256**\n> 256 color palette.\n\n**-24bit**\n> 24-bit true color.\n\n**-packed**\n> Packed pixel mode."],["DESCRIPTION","**ppmtopcx** converts PPM images to PCX format. PCX is an older bitmap format commonly used in DOS applications. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pcxtoppm](/man/pcxtoppm)(1), [ppmtobmp](/man/ppmtobmp)(1)"]],"e":["pcxtoppm","ppmtobmp"],"x":"ppmtopcx [input.ppm] > [output.pcx]"},"pppd-radius":{"n":"pppd-radius","t":"RADIUS authentication plugin for PPP daemon","s":[["TAGLINE","RADIUS authentication plugin for PPP daemon"],["TLDR","**Start PPP with RADIUS authentication**\n\n```pppd plugin radius.so```\n\n**Use a custom RADIUS configuration file**\n\n```pppd plugin radius.so radius-config-file [/etc/radiusclient/radiusclient.conf]```\n\n**Add a RADIUS Attribute-Value pair**\n\n```pppd plugin radius.so avpair \"[attribute=value]\"```"],["SYNOPSIS","**pppd** plugin radius.so [_options_]"],["PARAMETERS","**plugin** radius.so\n> Load RADIUS plugin.\n\n**radius-config-file** _filename_\n> Use specified file as radiusclient configuration file [default=/etc/radiusclient/radiusclient.conf].\n\n**avpair** _attribute=value_\n> Add an Attribute-Value pair to be passed to the RADIUS server on each request.\n\n**map-to-ifname**\n> Set RADIUS NAS-Port attribute to number equal to interface name (default).\n\n**map-to-ttyname**\n> Set RADIUS NAS-Port attribute value via libradiusclient library."],["DESCRIPTION","**pppd-radius** is a plugin for the PPP daemon that enables RADIUS (Remote Authentication Dial-In User Service) authentication. It permits pppd to perform PAP, CHAP, MS-CHAP, and MS-CHAPv2 authentication against a RADIUS server instead of the usual /etc/ppp/pap-secrets and /etc/ppp/chap-secrets files.\n\nThe plugin is built on the **radiusclient** library, which has its own configuration files (usually in /etc/radiusclient). This is commonly used by ISPs and enterprise networks to centrally manage dial-up, DSL, and VPN user authentication."],["CAVEATS","Requires a RADIUS server and the radiusclient library. The plugin (radius.so) must be installed. Configuration is primarily done through the radiusclient configuration file rather than pppd options directly."],["HISTORY","pppd-radius was created for **RADIUS authentication** integration with PPP."],["INSTALL","```apt: sudo apt install ppp```\n\n```dnf: sudo dnf install ppp```\n\n```pacman: sudo pacman -S ppp```\n\n```apk: sudo apk add ppp-daemon```\n\n```zypper: sudo zypper install ppp```\n\n```nix: nix profile install nixpkgs#ppp```"],["SEE ALSO","[pppd](/man/pppd)(8), [radiusd](/man/radiusd)(8)"]],"e":["pppd","radiusd"],"x":"pppd plugin radius.so"},"pueue-log":{"n":"pueue-log","t":"Display pueue task output logs","s":[["TAGLINE","Display pueue task output logs"],["TLDR","**Show task log**\n\n```pueue log [task_id]```\n\n**Show all task logs**\n\n```pueue log```\n\n**Show last N lines**\n\n```pueue log --lines [100] [task_id]```\n\n**Full output**\n\n```pueue log --full [task_id]```"],["SYNOPSIS","**pueue log** [_options_] [_task_ids_]"],["PARAMETERS","_TASK_IDS_\n> Tasks to show logs for.\n\n**--lines** _N_\n> Number of lines.\n\n**--full**\n> Show full output."],["DESCRIPTION","**pueue log** displays the captured stdout and stderr output of pueue tasks, both running and finished. By default it shows the last few lines; use **--full** to see complete output or **--lines** to control the number of lines displayed."],["CAVEATS","Output stored on disk. Part of pueue."],["HISTORY","pueue log provides **task output** viewing functionality."],["INSTALL","```pacman: sudo pacman -S pueue```\n\n```apk: sudo apk add pueue```\n\n```zypper: sudo zypper install pueue```\n\n```brew: brew install pueue```\n\n```nix: nix profile install nixpkgs#pueue```"],["SEE ALSO","[pueue](/man/pueue)(1), [pueue-follow](/man/pueue-follow)(1)"]],"e":["pueue","pueue-follow"],"x":"pueue log [task_id]"},"pwqcheck":{"n":"pwqcheck","t":"Validate password strength against policy","s":[["TAGLINE","Validate password strength against policy"],["TLDR","**Check password strength**\n\n```echo \"[password]\" | pwqcheck```\n\n**Check with old password**\n\n```pwqcheck -1 [old_password] [new_password]```\n\n**Check from stdin**\n\n```pwqcheck < [password_file]```\n\n**Custom config**\n\n```pwqcheck config=[/path/to/pwquality.conf]```"],["SYNOPSIS","**pwqcheck** [_options_] [_check_params_]"],["PARAMETERS","**-1**\n> Read old password, new password on stdin.\n\n**-2**\n> Read new password only.\n\n**config=**_file_\n> Use specified config file.\n\n**min=**_n_\n> Minimum password length.\n\n**max=**_n_\n> Maximum password length."],["DESCRIPTION","**pwqcheck** checks password quality according to configurable rules. It's part of passwdqc for enforcing password policies.\n\nReturns OK or an error message describing the weakness."],["EXAMPLES","```bash"],["Check single password","echo \"mypassword\" | pwqcheck"],["With old password (change validation)","echo -e \"oldpass\\nnewpass\" | pwqcheck -1"],["Custom policy","echo \"Test123!\" | pwqcheck min=8 max=40"],["In script","if echo \"$password\" | pwqcheck > /dev/null 2>&1; then\n    echo \"Password OK\"\nfi\n```"],["ERROR MESSAGES","```\nBad passphrase (too short)\nBad passphrase (not enough classes)\nBad passphrase (based on dictionary word)\nBad passphrase (too similar to old)\n```"],["CAVEATS","Part of passwdqc package. Different from pwquality/pam_pwquality. Check return code for validation."],["HISTORY","pwqcheck is part of **passwdqc** by **Solar Designer**, providing proactive password quality checking."],["INSTALL","```apt: sudo apt install passwdqc```\n\n```dnf: sudo dnf install passwdqc```\n\n```zypper: sudo zypper install passwdqc```\n\n```brew: brew install passwdqc```\n\n```nix: nix profile install nixpkgs#passwdqc```"],["SEE ALSO","[pwqgen](/man/pwqgen)(1), [passwd](/man/passwd)(1)"]],"e":["pwqgen","passwd"],"x":"echo \"[password]\" | pwqcheck"},"pytorch":{"n":"pytorch","t":"Deep learning framework with GPU acceleration","s":[["TAGLINE","Deep learning framework with GPU acceleration"],["TLDR","**Install PyTorch**\n\n```pip install torch torchvision```\n\n**Check CUDA availability**\n\n```python -c \"import torch; print(torch.cuda.is_available())\"```\n\n**Check version**\n\n```python -c \"import torch; print(torch.__version__)\"```\n\n**Run with specific GPU**\n\n```CUDA_VISIBLE_DEVICES=[0] python [script.py]```"],["SYNOPSIS","**pytorch** Python library for deep learning"],["DESCRIPTION","**PyTorch** is an open-source deep learning framework with dynamic computational graphs. It provides tensor computation with GPU acceleration and automatic differentiation for neural networks."],["BASIC EXAMPLE","```python\nimport torch\nimport torch.nn as nn"],["Create tensor","x = torch.tensor([[1., 2.], [3., 4.]])"],["GPU transfer","if torch.cuda.is_available():\n    x = x.cuda()"],["Simple model","model = nn.Sequential(\n    nn.Linear(10, 64),\n    nn.ReLU(),\n    nn.Linear(64, 1)\n)"],["Training loop","optimizer = torch.optim.Adam(model.parameters())\ncriterion = nn.MSELoss()\n\nfor epoch in range(100):\n    output = model(input)\n    loss = criterion(output, target)\n    optimizer.zero_grad()\n    loss.backward()\n    optimizer.step()\n```"],["ENVIRONMENT","```bash"],["Use specific GPU","CUDA_VISIBLE_DEVICES=0,1 python train.py"],["Check GPU memory","python -c \"import torch; print(torch.cuda.memory_summary())\"\n```"],["CONFIGURATION","**CUDA_VISIBLE_DEVICES**\n> Environment variable controlling which GPUs are visible to PyTorch, used to restrict training to specific devices.\n\n**TORCH_HOME**\n> Environment variable setting the directory for cached models and datasets (default ~/.cache/torch)."],["CAVEATS","GPU version requires CUDA. Large library (~2GB). Install from pytorch.org for CUDA version matching."],["HISTORY","PyTorch was developed by **Facebook AI Research** (FAIR), released in 2016 as a successor to Torch (Lua)."],["INSTALL","```brew: brew install pytorch```"],["SEE ALSO","[python](/man/python)(1), [nvidia-smi](/man/nvidia-smi)(1)"]],"e":["python","nvidia-smi"],"x":"pip install torch torchvision"},"rails-console":{"n":"rails-console","t":"Interactive Ruby console with Rails loaded","s":[["TAGLINE","Interactive Ruby console with Rails loaded"],["TLDR","**Start an interactive console** with the Rails application loaded\n\n```rails console```\n\n**Use the short alias**\n\n```rails c```\n\n**Start in sandbox mode** (all database changes rolled back on exit)\n\n```rails console --sandbox```\n\n**Start console in a specific environment**\n\n```rails console -e [production|test|development]```\n\n**Sandbox in a specific environment**\n\n```rails console -e staging --sandbox```"],["SYNOPSIS","**rails console** [**-e** _environment_] [**--sandbox**] [**--skip-executor**] [**-h**]\n\n**rails c** [_options_]"],["PARAMETERS","**-e**, **--environment** _ENV_\n> Specify the Rails environment (development, test, production). Defaults to development.\n\n**--sandbox**, **-s**\n> Rollback any database changes made during the session on exit.\n\n**--skip-executor**, **-w**\n> Do not wrap the console with the Rails Executor (skips query cache, reloading, and callbacks).\n\n**-h**, **--help**\n> Show help information."],["DESCRIPTION","**rails console** opens an interactive Ruby (IRB) session with the full Rails application environment loaded. This includes all models, configuration, and database connections, making it a powerful tool for exploring and debugging applications.\n\nThe console provides direct access to ActiveRecord models for querying and manipulating data, testing methods and business logic, and inspecting application state. It inherits the full context of the specified Rails environment.\n\nSandbox mode wraps the entire session in a database transaction that rolls back when you exit. This allows safe experimentation with data without permanent changes, useful for testing destructive operations or debugging production issues."],["CAVEATS","Must be run from within a Rails application directory. The console loads the entire application, so startup time increases with application size.\n\nIn production environments, be cautious with data modifications. Even without sandbox mode, the console provides full database access with user privileges.\n\nSandbox mode only protects database changes. File system modifications, external API calls, or other side effects are not rolled back."],["SEE ALSO","[rails](/man/rails)(1), [irb](/man/irb)(1), [rails-db](/man/rails-db)(1), [rails-server](/man/rails-server)(1), [ruby](/man/ruby)(1)"]],"e":["rails","irb","rails-db","rails-server","ruby"],"x":"rails console"},"rancid-cvs":{"n":"rancid-cvs","t":"Initialize version control for network device configs","s":[["TAGLINE","Initialize version control for network device configs"],["TLDR","**Initialize CVS repositories** for all defined RANCID groups\n\n```rancid-cvs```\n\n**Add a new group** to the CVS repository (after adding to rancid.conf)\n\n```rancid-cvs```"],["SYNOPSIS","**rancid-cvs**"],["DESCRIPTION","**rancid-cvs** initializes and maintains the version control repository structure for RANCID (Really Awesome New Cisco Config Differ). It creates the necessary directories and CVS/Subversion repositories for each device group defined in the RANCID configuration.\n\nRANCID is a network device configuration management tool that automatically backs up configurations from routers, switches, and firewalls (Cisco, Juniper, HP, etc.), tracks changes in version control, and sends email notifications when configurations change.\n\nThe command reads the LIST_OF_GROUPS variable from **/etc/rancid/rancid.conf** and creates the corresponding directory structure under the RANCID home directory (typically /var/lib/rancid). Each group gets its own subdirectory with CVS working directories.\n\nRun this command after initial RANCID installation and whenever new device groups are added to the configuration. Existing groups are not overwritten when the command is rerun."],["CONFIGURATION","**/etc/rancid/rancid.conf**\n> Main RANCID configuration defining LIST_OF_GROUPS, RCSSYS (cvs or svn), mail aliases, and working directory paths.\n\n**router.db**\n> Per-group device inventory file listing device hostnames, types, and status (up/down) for configuration collection.\n\n**RCSSYS**\n> Variable in rancid.conf selecting the version control backend (cvs or svn)."],["CAVEATS","Must be run as the **rancid** user, not root. The command expects proper permissions on the RANCID directories and CVS repository.\n\nRequires CVS or Subversion to be installed and configured. The repository type is determined by the RCSSYS variable in rancid.conf.\n\nThe command does not configure devices or credentials. Device definitions must be added separately to the **router.db** file in each group directory."],["HISTORY","RANCID was originally developed at **Shrubbery Networks** by Henry Kilmer and John Heasley. The name \"Really Awesome New Cisco Config Differ\" reflects its origins as a Cisco-focused tool, though it now supports many network device vendors. Development began in the late 1990s as organizations needed automated configuration backup and change tracking."],["INSTALL","```apt: sudo apt install rancid```\n\n```dnf: sudo dnf install rancid```\n\n```apk: sudo apk add rancid```\n\n```brew: brew install rancid```\n\n```nix: nix profile install nixpkgs#rancid```"],["SEE ALSO","[rancid-run](/man/rancid-run)(1), [cvs](/man/cvs)(1), [svn](/man/svn)(1), [clogin](/man/clogin)(1)"]],"e":["rancid-run","cvs","svn","clogin"],"x":"rancid-cvs"},"rarcrack":{"n":"rarcrack","t":"Brute-force password recovery for archives","s":[["TAGLINE","Brute-force password recovery for archives"],["TLDR","**Crack RAR password**\n\n```rarcrack [archive.rar]```\n\n**Crack ZIP password**\n\n```rarcrack [archive.zip] --type zip```\n\n**Crack 7z password**\n\n```rarcrack [archive.7z] --type 7z```\n\n**Set thread count**\n\n```rarcrack [archive.rar] --threads [4]```\n\n**Set character set**\n\n```rarcrack [archive.rar] --charset [abc123]```\n\n**Resume cracking**\n\n```rarcrack [archive.rar]```"],["SYNOPSIS","**rarcrack** _archive_ [_--type_ _format_] [_--threads_ _n_] [_--charset_ _chars_]"],["PARAMETERS","**--type** _FORMAT_\n> Archive type (rar, zip, 7z). Auto-detected from file extension if not specified.\n\n**--threads** _N_\n> Number of threads for parallel password testing.\n\n**--charset** _CHARS_\n> Characters to use in brute-force attempts. Narrowing this reduces search space significantly.\n\n**-h**, **--help**\n> Show help."],["DESCRIPTION","**rarcrack** recovers passwords from encrypted RAR, ZIP, and 7Z archives using brute-force testing of all possible character combinations. It systematically tries passwords of increasing length from a configurable character set, with the archive type either auto-detected from the file extension or explicitly specified.\n\nThe tool automatically saves progress to an XML status file alongside the archive, allowing interrupted sessions to resume from the last tested position without starting over. Multi-threading distributes password testing across available CPU cores for faster throughput. Narrowing the character set with **--charset** significantly reduces the search space when the password composition is partially known."],["CAVEATS","Brute-force is slow for long passwords. For authorized recovery only. Strong passwords may be impractical."],["HISTORY","**rarcrack** was created for password recovery from encrypted archives. It provides a simple brute-force tool for RAR, ZIP, and 7Z files."],["INSTALL","```apt: sudo apt install rarcrack```\n\n```pacman: sudo pacman -S rarcrack```\n\n```nix: nix profile install nixpkgs#rarcrack```"],["SEE ALSO","[john](/man/john)(1), [hashcat](/man/hashcat)(1), [zip2john](/man/zip2john)(1), [rar2john](/man/rar2john)(1)"]],"e":["john","hashcat","zip2john","rar2john"],"x":"rarcrack [archive.rar]"},"rmmod":{"n":"rmmod","t":"Remove loaded kernel modules","s":[["TAGLINE","Remove loaded kernel modules"],["TLDR","**Remove** a kernel module\n\n```sudo rmmod module_name```\n\nRemove with **verbose** output\n\n```sudo rmmod --verbose module_name```\n\nRemove and log to **syslog**\n\n```sudo rmmod --syslog module_name```\n\n**Force** removal\n\n```sudo rmmod --force module_name```"],["SYNOPSIS","**rmmod** [_-f_] [_-s_] [_-v_] _modulename_..."],["DESCRIPTION","**rmmod** is a simple program to remove a module from the Linux kernel. It removes only the specified module without handling dependent modules."],["PARAMETERS","**-f, --force**\n> Force removal even if module is in use (dangerous)\n\n**-s, --syslog**\n> Send errors to syslog instead of stderr\n\n**-v, --verbose**\n> Print verbose information\n\n**-V, --version**\n> Display version\n\n**-h, --help**\n> Display help"],["CAVEATS","A module cannot be removed if it is in use by another module or process. Use **modprobe -r** instead for most cases as it handles dependencies. The **--force** option is dangerous and can crash the system."],["HISTORY","**rmmod** is part of **kmod**. It provides direct module removal without the intelligence of modprobe -r."],["INSTALL","```apt: sudo apt install kmod```\n\n```dnf: sudo dnf install kmod```\n\n```pacman: sudo pacman -S kmod```\n\n```apk: sudo apk add kmod```\n\n```zypper: sudo zypper install kmod```\n\n```brew: brew install kmod```\n\n```nix: nix profile install nixpkgs#kmod```"],["SEE ALSO","[modprobe](/man/modprobe)(8), [insmod](/man/insmod)(8), [lsmod](/man/lsmod)(8)"]],"e":["modprobe","insmod","lsmod"],"x":"sudo rmmod module_name"},"rsyncy":{"n":"rsyncy","t":"Status and progress bar for rsync","s":[["TAGLINE","Status and progress bar for rsync"],["TLDR","**Sync files with a visual progress bar**\n\n```rsyncy -a [/source/] [/destination/]```\n\n**Remote sync with progress display**\n\n```rsyncy -avz [remote:/path/] [/local/]```\n\n**Sync with delete and compression**\n\n```rsyncy -avz --delete [/source/] [/destination/]```\n\n**Dry run to preview** changes\n\n```rsyncy -avn [/source/] [/destination/]```"],["SYNOPSIS","**rsyncy** [_rsync-options_] _source_ _destination_"],["PARAMETERS","All standard **rsync** options are supported and passed through to rsync. rsyncy adds the necessary **--info** flags automatically for progress display."],["DESCRIPTION","**rsyncy** wraps rsync to display a visual progress bar showing overall transfer progress, speed, elapsed time, and file counts. You run it with the same arguments as rsync and it adds the necessary **--info** flags automatically. Unlike rsync's built-in **--progress** flag which shows per-file progress, rsyncy shows overall progress across all files."],["HISTORY","**rsyncy** was created by **Christian Zangl** (laktak) and is written in **Go**."],["INSTALL","```brew: brew install rsyncy```\n\n```nix: nix profile install nixpkgs#rsyncy```"],["SEE ALSO","[rsync](/man/rsync)(1), [progress](/man/progress)(1), [pv](/man/pv)(1)"]],"e":["rsync","progress","pv"],"x":"rsyncy -a [/source/] [/destination/]"},"semtools":{"n":"semtools","t":"Semantic document search, parsing, and Q&A for the terminal","s":[["TAGLINE","Semantic document search, parsing, and Q&A for the terminal"],["TLDR","**Parse** PDFs to text/markdown paths\n\n```semtools parse [files...]```\n\n**Semantic search** in text files\n\n```semtools search \"[query]\" [files...] --max-distance [0.3]```\n\n**Ask** a question over documents\n\n```semtools ask \"[question]\" [files...]```\n\n**Use a workspace** for cached embeddings\n\n```semtools workspace use [name]```\n\n**Search with workspace** after export\n\n```export SEMTOOLS_WORKSPACE=[name]; semtools search \"[query]\" [dir]/*.txt```"],["SYNOPSIS","**semtools** *command* [*options*] [*args*]"],["DESCRIPTION","**semtools** is a Rust CLI for document-centric workflows: **parse** converts PDFs and office documents (default backend: LlamaParse API), **search** performs local semantic keyword search with embeddings, **ask** runs an LLM agent with search/read tools over a corpus, and **workspace** caches embeddings for large collections under **~/.semtools/workspaces/**.\n\nInstall via **npm i -g @llamaindex/semtools** or **cargo install semtools**. Designed for Unix pipelines (stdin/stdout and **xargs**)."],["PARAMETERS","**parse** [*OPTIONS*] *FILES*...\n\n> Parse documents. **-b**, **--backend** selects parser (default **llama-parse**). **-c** config path. **-v** verbose.\n\n**search** [*OPTIONS*] *QUERY* [*FILES*...]\n\n> Semantic search. **-n**, **--n-lines** context lines (default 3). **--top-k** result count. **-m**, **--max-distance** distance threshold. **-i** ignore case. **-j** JSON output. Files optional if using stdin.\n\n**ask** [*OPTIONS*] *QUERY* [*FILES*...]\n\n> Question-answering agent over files or stdin. Requires an LLM API key (OpenAI-compatible; see config).\n\n**workspace** **use**|**status**|**prune**\n\n> Manage embedding workspaces. **use** prints shell export instructions for **SEMTOOLS_WORKSPACE**."],["CAVEATS","**parse** needs a LlamaIndex Cloud API key by default. **ask** needs an LLM provider key. **search**/**workspace** are local but use embedding models (download/runtime cost). Do not send confidential documents to third-party parse/LLM APIs without review."],["INSTALL","```nix: nix profile install nixpkgs#semtools```"],["SEE ALSO","[rg](/man/rg)(1), [pdftotext](/man/pdftotext)(1), [jq](/man/jq)(1)"],["RESOURCES","```[Source code](https://github.com/run-llama/semtools)```"]],"e":["rg","pdftotext","jq"],"x":"semtools parse [files...]"},"sh5util":{"n":"sh5util","t":"Merge Slurm HDF5 profiling data","s":[["TAGLINE","Merge Slurm HDF5 profiling data"],["TLDR","**Merge** HDF5 files for a job\n\n```sh5util -j [job_id]```\n\nMerge HDF5 files for a specific **job step**\n\n```sh5util -j [job_id.step_id]```\n\n**Extract** data series from a merged job file\n\n```sh5util -j [job_id] -E -i [path/to/file.h5] -s [Energy|Filesystem|Network|Task]```\n\nExtract a **specific data item** from all nodes\n\n```sh5util -j [job_id] -I -s [series] -d [data_item]```\n\n**List** available data items in a series\n\n```sh5util -j [job_id] -I -s [series] -L```\n\nKeep node files after **merging**\n\n```sh5util -j [job_id] -S```"],["SYNOPSIS","**sh5util** [**-j** _job[.step]_] [**-E**|**-I**] [_OPTIONS_]"],["PARAMETERS","**-j, --jobs** _job[.step]_\n> Merge HDF5 files for the specified job or job step\n\n**-p, --profiledir** _dir_\n> Directory containing node-step HDF5 files\n\n**-o, --output** _path_\n> Output file path (default: ./job_$jobid.h5)\n\n**-S, --savefiles**\n> Keep node-step files after merging\n\n**--user** _user_\n> User who ran the profiled job\n\n**-E, --extract**\n> Extract data series to CSV format\n\n**-i, --input** _path_\n> Input merged HDF5 file for extraction\n\n**-N, --node** _nodename_\n> Extract data for specific node only\n\n**-l, --level** _level_\n> Data level: Node:Totals or Node:TimeSeries\n\n**-s, --series** _series_\n> Data series: Energy, Filesystem, Network, Task, or Task_#\n\n**-I, --item-extract**\n> Extract single data item from all samples\n\n**-d, --data** _item_\n> Specific data item name to extract\n\n**-L, --list**\n> List available data items in a series\n\n**-h, --help**\n> Display usage information"],["DESCRIPTION","**sh5util** merges HDF5 profiling files produced by Slurm's **acct_gather_profile** plugin across compute nodes into a single consolidated file for analysis. It supports three modes: merging node files, extracting data series to CSV, and extracting specific metrics from time series.\n\nThe tool works with Slurm job profiling data that tracks energy consumption, filesystem I/O, network activity, and task-level metrics. Output files can be analyzed with HDF5 tools or converted to CSV for use with standard data analysis applications."],["CAVEATS","Requires HDF5 profiling to be enabled in Slurm configuration. Node files must exist in the profile directory. Large jobs with many nodes and long runtimes can produce substantial HDF5 files. The **acct_gather_profile** plugin must be configured on the cluster."],["HISTORY","**sh5util** is part of the **Slurm** (Simple Linux Utility for Resource Management) workload manager, developed at **Lawrence Livermore National Laboratory**. Slurm was first released in **2002** and has become one of the most widely used HPC job schedulers. HDF5 profiling support was added to provide detailed job performance analysis capabilities."],["INSTALL","```apt: sudo apt install slurm-wlm-hdf5-plugin```"],["SEE ALSO","[sacct](/man/sacct)(1), [sstat](/man/sstat)(1), [srun](/man/srun)(1), [sbatch](/man/sbatch)(1)"]],"e":["sacct","sstat","srun","sbatch"],"x":"sh5util -j [job_id]"},"ukify":{"n":"ukify","t":"Build and sign Unified Kernel Images for UEFI","s":[["TAGLINE","Build and sign Unified Kernel Images for UEFI"],["TLDR","**Build a UKI** from a kernel and initrd with a kernel command line\n\n```ukify build --linux=[/boot/vmlinuz] --initrd=[/boot/initrd.img] --cmdline=[\"root=/dev/sda1 ro\"] --output=[uki.efi]```\n\n**Build and sign** the image for Secure Boot\n\n```ukify build --linux=[vmlinuz] --initrd=[initrd.img] --secureboot-private-key=[sb.key] --secureboot-certificate=[sb.crt]```\n\n**Generate** Secure Boot and PCR signing keys and certificates\n\n```ukify genkey --secureboot-private-key=[sb.key] --secureboot-certificate=[sb.crt]```\n\n**Inspect** the sections of an existing UKI\n\n```ukify inspect [/boot/EFI/Linux/uki.efi]```\n\n**Build from** a configuration file instead of long command lines\n\n```ukify build --config=[/etc/kernel/uki.conf]```"],["SYNOPSIS","**ukify** [_options_] **build**\n\n**ukify** [_options_] **genkey**\n\n**ukify** [_options_] **inspect** _file..._"],["DESCRIPTION","**ukify** combines a kernel, an initrd, a kernel command line, the **systemd-stub** UEFI boot stub, and other resources into a single **Unified Kernel Image** (UKI). A UKI is one PE/COFF EFI executable that the firmware can boot directly or that **systemd-boot** can list, removing the need for a separate boot loader configuration for the kernel and initrd.\n\nBecause everything lives in one file, the whole image (including the embedded command line) can be signed once for **Secure Boot**, and its contents can be measured into TPM **PCRs** for measured boot and TPM-based disk unlocking. ukify embeds the components as named PE sections (such as **.linux**, **.initrd**, **.cmdline**, **.osrel**, and **.uname**), optionally adds a boot splash and a devicetree, and writes the result to an .efi file.\n\nThe tool exposes three verbs: **build** assembles and optionally signs the image, **genkey** creates the Secure Boot and PCR signing key material, and **inspect** prints the sections of an existing UKI together with their sizes and SHA256 digests."],["PARAMETERS","**--linux=** _PATH_\n> Path to the kernel image embedded as the **.linux** section.\n\n**--initrd=** _PATH_\n> Path to an initrd; may be given multiple times to concatenate several.\n\n**--cmdline=** _TEXT_|_@PATH_\n> Kernel command line, given literally or read from a file.\n\n**--os-release=** _TEXT_|_@PATH_\n> Operating system identification; taken from the host **os-release** if omitted.\n\n**--output=** _FILENAME_\n> Destination path for the generated image.\n\n**--stub=** _PATH_\n> Path to the **systemd-stub** EFI stub to use.\n\n**--config=** _PATH_\n> Read build settings from a configuration file.\n\n**--secureboot-private-key=** _KEY_\n> Private key used to sign the resulting binary for Secure Boot.\n\n**--secureboot-certificate=** _CERT_\n> Certificate matching the Secure Boot private key.\n\n**--signtool=** _SIGNER_\n> Signing backend: **sbsign**, **pesign**, or **systemd-sbsign** (default sbsign).\n\n**--pcr-private-key=** _PATH_\n> Private key for signing the PCR policy; may be repeated.\n\n**--measure**\n> Pre-calculate and embed expected PCR values via **systemd-measure**."],["CAVEATS","A UKI bundles the command line into the signed image, so changing boot parameters means rebuilding (and re-signing) the image rather than editing a loader entry. Secure Boot signing requires keys enrolled in firmware, and the chosen **--signtool** (sbsign/pesign) must be installed. ukify is implemented in Python and depends on tools such as **objcopy** for assembling PE sections."],["HISTORY","**ukify** was introduced in **systemd v253** as an experimental helper for assembling Unified Kernel Images, and was expanded in later releases (v254 and onward) with additional verbs and signing options. It complements **kernel-install** and **systemd-boot** in systemd's UEFI boot toolchain."],["INSTALL","```apk: sudo apk add ukify```"],["SEE ALSO","[bootctl](/man/bootctl)(1), [kernel-install](/man/kernel-install)(8), [dracut](/man/dracut)(8), [objcopy](/man/objcopy)(1), [efibootmgr](/man/efibootmgr)(8)"]],"e":["bootctl","kernel-install","dracut","objcopy","efibootmgr"],"x":"ukify build --linux=[/boot/vmlinuz] --initrd=[/boot/initrd.img] --cmdline=[\"root=/dev/sda1 ro\"] --output=[uki.efi]"},"uvccapture":{"n":"uvccapture","t":"Capture still images from UVC webcams","s":[["TAGLINE","Capture still images from UVC webcams"],["TLDR","**Capture a single image** from webcam\n\n```uvccapture```\n\n**Capture to a specific file**\n\n```uvccapture -o [image.jpg]```\n\n**Capture at specific resolution**\n\n```uvccapture -x [1280] -y [720]```\n\n**Capture from a specific device**\n\n```uvccapture -d [/dev/video1]```\n\n**Set JPEG quality**\n\n```uvccapture -q [90]```\n\n**Take continuous shots** every N seconds\n\n```uvccapture -t [5] -o [image.jpg]```\n\n**Run command after each capture**\n\n```uvccapture -t [10] -c \"[scp image.jpg user@host:]\"```\n\n**Adjust brightness and contrast**\n\n```uvccapture -B [128] -C [64]```"],["SYNOPSIS","**uvccapture** [_options_]"],["PARAMETERS","**-o** _FILE_\n> Output filename (default: snap.jpg)\n\n**-d** _DEVICE_\n> Video device (default: /dev/video0)\n\n**-x** _WIDTH_\n> Image width (must be supported by device)\n\n**-y** _HEIGHT_\n> Image height (must be supported by device)\n\n**-q** _QUALITY_\n> JPEG quality 0-100 (default: 95)\n\n**-t** _SECONDS_\n> Continuous capture interval (0 for single shot)\n\n**-c** _COMMAND_\n> Command to run after each capture\n\n**-w**\n> Wait for command to finish before next capture\n\n**-s** _FRAMES_\n> Frames to skip after first capture\n\n**-r**\n> Use read() instead of mmap for capture\n\n**-m**\n> Toggle to YUYV capture mode\n\n**-v**\n> Verbose output\n\n**-B** _VALUE_\n> Set brightness\n\n**-C** _VALUE_\n> Set contrast\n\n**-S** _VALUE_\n> Set saturation\n\n**-G** _VALUE_\n> Set gain"],["DESCRIPTION","**uvccapture** captures still images from USB Video Class (UVC) compatible webcams. It outputs JPEG images and is designed for scripting, time-lapse photography, and automated image capture without a graphical interface.\n\nThe tool uses Video4Linux2 (V4L2) to access the camera. Resolution must be supported by the device; use **v4l2-ctl --list-formats-ext** to check available options. High resolutions (>960x720) automatically activate YUYV capture mode.\n\nFor continuous capture, use **-t** with an interval in seconds. The **-c** option runs a command after each capture, useful for uploading images or triggering other actions. Image processing beyond JPEG can be done with ImageMagick or similar tools."],["CAVEATS","User must have read/write permissions on the video device. Resolution must be natively supported by the webcam. Only JPEG output is supported; use ImageMagick for other formats. Some cameras may require warm-up frames (**-s**) for proper exposure. Depends on libjpeg library."],["HISTORY","uvccapture was developed for capturing images from UVC webcams on Linux systems. UVC (USB Video Class) is a standard for USB video devices that provides driver-free compatibility across operating systems. The tool was designed as a lightweight alternative to full webcam applications, focusing on command-line scripting and automation use cases."],["INSTALL","```apt: sudo apt install uvccapture```\n\n```nix: nix profile install nixpkgs#uvccapture```"],["SEE ALSO","[fswebcam](/man/fswebcam)(1), [v4l2-ctl](/man/v4l2-ctl)(1), [ffmpeg](/man/ffmpeg)(1), [streamer](/man/streamer)(1)"]],"e":["fswebcam","v4l2-ctl","ffmpeg","streamer"],"x":"uvccapture"},"vimdiff":{"n":"vimdiff","t":"Compare files side-by-side in Vim","s":[["TAGLINE","Compare files side-by-side in Vim"],["TLDR","**Compare two files** side by side\n\n```vimdiff [file1] [file2]```\n\n**Compare three files**\n\n```vimdiff [file1] [file2] [file3]```\n\n**Open files in read-only mode**\n\n```vimdiff -R [file1] [file2]```\n\n**Use horizontal split** instead of vertical\n\n```vimdiff -o [file1] [file2]```\n\n**Start with a clean environment** (no .vimrc)\n\n```vimdiff --clean [file1] [file2]```\n\n**Compare files with a specific command** executed at startup\n\n```vimdiff -c \"[command]\" [file1] [file2]```"],["SYNOPSIS","**vimdiff** [_options_] _file1_ _file2_ [_file3_ [_file4_]]"],["PARAMETERS","**-O**\n> Open windows vertically, side by side (default for two files).\n\n**-o**\n> Open windows horizontally, stacked.\n\n**-R**\n> Open all files in read-only mode.\n\n**-c** _command_\n> Execute command after loading the first file.\n\n**--clean**\n> Start without loading .vimrc or plugins.\n\n**-d**\n> Explicitly enable diff mode (automatic with vimdiff)."],["DESCRIPTION","**vimdiff** starts Vim in diff mode with two to eight files, each in its own window. Differences between files are highlighted, making it easy to compare and merge changes.\n\nThis is equivalent to running **vim -d file1 file2**. In diff mode, Vim automatically sets options for synchronized scrolling (**scrollbind**), difference highlighting, and folding of unchanged lines.\n\nWithin vimdiff, use **]c** and **[c** to jump between differences. Use **do** (diff obtain) to pull changes from another window, and **dp** (diff put) to push changes to another window. Use **:diffupdate** to refresh highlighting after edits."],["CAVEATS","vimdiff works best with two files; comparing more than four files can become difficult to read. Large files with many differences may have performance issues. The folding of unchanged lines can be adjusted with **:set diffopt**."],["HISTORY","Diff mode was added to **Vim** by Bram Moolenaar. The vimdiff wrapper script has been part of Vim since version 6.0, released in **2001**. The feature was inspired by similar functionality in other diff tools but integrated directly into Vim's powerful editing environment. Support for up to eight files was added in later versions."],["INSTALL","```apk: sudo apk add vimdiff```"],["SEE ALSO","[vim](/man/vim)(1), [diff](/man/diff)(1), [meld](/man/meld)(1), [kdiff3](/man/kdiff3)(1)"]],"e":["vim","diff","meld","kdiff3"],"x":"vimdiff [file1] [file2]"},"wg-cmd":{"n":"wg-cmd","t":"TUI for managing WireGuard configurations","s":[["TAGLINE","TUI for managing WireGuard configurations"],["TLDR","**Start the setup wizard for a new interface**\n\n```wg-cmd new```\n\n**Manage an existing WireGuard interface**\n\n```wg-cmd [wg0]```\n\n**Generate configuration without UI**\n\n```wg-cmd [wg0] make```"],["SYNOPSIS","**wg-cmd** [_command_] [_interface_]"],["DESCRIPTION","**wg-cmd** (WG Commander) provides a text-based user interface for managing WireGuard configuration files. It includes an initial setup wizard, peer management with QR code viewing, and automatic configuration of sysctl, systemd, and NAT rules. Requires root permissions and assumes Linux with iptables and systemd."],["HISTORY","**wg-cmd** was created by **Andrian Budantsov** (AndrianBdn) and is written in **Go**."],["INSTALL","```apt: sudo apt install wireguard-tools```\n\n```dnf: sudo dnf install wireguard-tools```\n\n```pacman: sudo pacman -S wireguard-tools```\n\n```apk: sudo apk add wireguard-tools-wg```\n\n```zypper: sudo zypper install wireguard-tools```\n\n```brew: brew install wireguard-tools```\n\n```nix: nix profile install nixpkgs#wireguard-tools```"],["SEE ALSO","[wg](/man/wg)(1), [vortix](/man/vortix)(1)"]],"e":["wg","vortix"],"x":"wg-cmd new"},"xml-p2x":{"n":"xml-p2x","t":"Convert PYX format back to XML","s":[["TAGLINE","Convert PYX format back to XML"],["TLDR","**Convert PYX to XML**\n\n```xml p2x [file.pyx]```"],["SYNOPSIS","**xml p2x** [_options_] [_file_]"],["PARAMETERS","_file_\n> Input PYX file."],["DESCRIPTION","**xml p2x** converts PYX format back to XML. Part of xmlstarlet toolkit. Alias for xml depyx command."],["INSTALL","```dnf: sudo dnf install xmlstarlet```\n\n```pacman: sudo pacman -S xmlstarlet```\n\n```apk: sudo apk add xmlstarlet```\n\n```zypper: sudo zypper install xmlstarlet```\n\n```brew: brew install xmlstarlet```\n\n```nix: nix profile install nixpkgs#xmlstarlet```"],["SEE ALSO","[xml-pyx](/man/xml-pyx)(1), [xml-depyx](/man/xml-depyx)(1)"]],"e":["xml-pyx","xml-depyx"],"x":"xml p2x [file.pyx]"},"yarn":{"n":"yarn","t":"Fast JavaScript dependency manager","s":[["TAGLINE","Fast JavaScript dependency manager"],["TLDR","**Install all dependencies** from package.json\n\n```yarn install```\n\n**Add a package** to dependencies\n\n```yarn add [package]```\n\n**Add a package** as a dev dependency\n\n```yarn add --dev [package]```\n\n**Remove a package**\n\n```yarn remove [package]```\n\n**Upgrade packages** to latest versions\n\n```yarn upgrade```\n\n**Run a script** defined in package.json\n\n```yarn run [script]```\n\n**Initialize a new project**\n\n```yarn init```\n\n**Interactively upgrade** packages\n\n```yarn upgrade-interactive```"],["SYNOPSIS","**yarn** [_command_] [_flags_]"],["PARAMETERS","**install**\n> Install all dependencies from package.json.\n\n**add** _package_\n> Add a package to project dependencies.\n\n**remove** _package_\n> Remove a package from the project.\n\n**upgrade** [_package_]\n> Upgrade packages to their latest version based on version range.\n\n**run** _script_\n> Execute a script defined in package.json.\n\n**init**\n> Initialize a new project, creating package.json.\n\n**publish**\n> Publish a package to a registry.\n\n**create** _starter-kit_\n> Create a new project from a starter kit.\n\n**config**\n> Manage Yarn configuration settings.\n\n**cache clean**\n> Clean the global cache.\n\n**--dev**, **-D**\n> Add package as a dev dependency.\n\n**--global**, **-g**\n> Install package globally.\n\n**--verbose**\n> Print verbose output for the command.\n\n**--cwd** _directory_\n> Specify a working directory.\n\n**--mutex** _type_\n> Ensure only one Yarn instance runs (file or network).\n\n**--help**\n> Display help for a command.\n\n**--version**\n> Display version information."],["DESCRIPTION","**Yarn** is a fast, reliable, and secure dependency manager for JavaScript. It caches every package it downloads, enabling offline installation of previously fetched packages. Yarn parallelizes operations for faster performance compared to npm.\n\nRunning **yarn** with no command executes **yarn install**. Running **yarn script** executes a user-defined script from package.json. The lockfile **yarn.lock** ensures consistent installations across machines.\n\nYarn 2+ (Berry) introduces Plug'n'Play for zero-install workflows and improved workspace support. Configuration can be set globally or per-project via **.yarnrc.yml**."],["CAVEATS","Two major versions exist: Yarn Classic (1.x) and Yarn Modern (2+/Berry) with different features and configurations. Yarn Modern uses Plug'n'Play by default, which may require project configuration adjustments. The global install location differs from npm."],["HISTORY","Yarn was developed by **Facebook** (now Meta) in collaboration with Exponent, Google, and Tilde, and released in **October 2016** as an alternative to npm. It addressed npm's performance and security issues at the time. Yarn 2 (Berry), a complete rewrite, was released in **January 2020** with features like Plug'n'Play and improved workspaces. Yarn is now a community-driven project."],["INSTALL","```pacman: sudo pacman -S yarn```\n\n```apk: sudo apk add yarn```\n\n```zypper: sudo zypper install yarn```\n\n```brew: brew install yarn```\n\n```nix: nix profile install nixpkgs#yarn```"],["SEE ALSO","[npm](/man/npm)(1), [pnpm](/man/pnpm)(1), [node](/man/node)(1), [npx](/man/npx)(1)"]],"e":["npm","pnpm","node","npx"],"x":"yarn install"},"zebra":{"n":"zebra","t":"Quagga routing manager daemon","s":[["TAGLINE","Quagga routing manager daemon"],["TLDR","**Start zebra daemon**\n\n```sudo zebra -d```\n\n**Start with specific config file**\n\n```sudo zebra -d -f [/etc/quagga/zebra.conf]```\n\n**Run in foreground**\n\n```sudo zebra -A [127.0.0.1]```\n\n**Start with VTY on specific port**\n\n```sudo zebra -d -P [2601]```\n\n**Connect to zebra shell**\n\n```telnet localhost 2601```"],["SYNOPSIS","**zebra** [_options_]"],["PARAMETERS","**-d**, **--daemon**\n> Run as daemon in background.\n\n**-f**, **--config_file** _file_\n> Use specified configuration file.\n\n**-P**, **--vty_port** _port_\n> VTY (telnet) port number.\n\n**-A**, **--vty_addr** _address_\n> VTY bind address.\n\n**-u**, **--user** _user_\n> Run as specified user.\n\n**-g**, **--group** _group_\n> Run as specified group.\n\n**-v**, **--version**\n> Display version.\n\n**-h**, **--help**\n> Display help."],["DESCRIPTION","**zebra** is the core routing manager daemon from Quagga (formerly GNU Zebra). It manages the kernel routing table and provides the Zserv API for protocol daemons like ospfd, bgpd, and ripd to communicate routing updates.\n\nZebra abstracts the kernel's routing interface, allowing protocol daemons to be portable across Unix variants. It receives routes from protocol daemons and installs them in the kernel FIB (Forwarding Information Base).\n\nConfiguration uses a Cisco-like CLI accessible via telnet or the vtysh unified shell. Routes can be viewed and static routes configured through this interface."],["CONFIGURATION","Config file: /etc/quagga/zebra.conf\n\nDefines interfaces, static routes, and access control. Protocol daemons have separate configuration files."],["CAVEATS","Quagga is largely superseded by FRRouting (FRR). Default VTY password should be changed. Requires root or CAP_NET_ADMIN capability."],["HISTORY","**zebra** originated from the GNU Zebra project by Kunihiro Ishiguro, discontinued in 2005. Quagga forked from it and maintained development until FRRouting forked from Quagga in 2017 for faster, more open development. The zebra daemon name persists in both projects."],["INSTALL","```apk: sudo apk add quagga```\n\n```brew: brew install zebra```"],["SEE ALSO","[ospfd](/man/ospfd)(8), [bgpd](/man/bgpd)(8), [ripd](/man/ripd)(8), [vtysh](/man/vtysh)(1)"]],"e":["ospfd","bgpd","ripd","vtysh"],"x":"sudo zebra -d"},"zrun":{"n":"zrun","t":"Run commands on compressed files by auto-decompressing arguments","s":[["TAGLINE","Run commands on compressed files by auto-decompressing arguments"],["TLDR","**Run command on compressed file**\n\n```zrun [command] [file.gz]```\n\n**Run diff on two compressed files**\n\n```zrun diff [file1.gz] [file2.gz]```\n\n**Run less on gzipped file**\n\n```zrun less [file.gz]```"],["SYNOPSIS","**zrun** _command_ [_arguments_]"],["DESCRIPTION","**zrun** transparently decompresses compressed file arguments to temporary files before passing them to the specified command. It detects which arguments are compressed files by their extension, decompresses only those to temporary files (not pipes), runs the command with the decompressed paths, then cleans up.\n\nThis is useful for running commands that don't natively support compressed input without manual decompression steps.\n\nThe following compression types are supported: **gz**, **bz2**, **Z**, **xz**, **lzma**, **lzo**.\n\nIf **zrun** is symlinked to a name beginning with **z** (e.g., **zprog**), executing the link is equivalent to running **zrun prog**."],["CAVEATS","Modifications to the uncompressed temporary file are not fed back into the input file, so using this as a way to make an editor support compressed files will not work.\n\nLarge compressed files require temporary disk space for decompression.\n\nThe command sees different (temporary) filenames than the originals, which may matter for some tools."],["INSTALL","```apt: sudo apt install moreutils```\n\n```dnf: sudo dnf install moreutils```\n\n```pacman: sudo pacman -S moreutils```\n\n```apk: sudo apk add moreutils```\n\n```zypper: sudo zypper install moreutils```\n\n```brew: brew install moreutils```\n\n```nix: nix profile install nixpkgs#moreutils```"],["SEE ALSO","[zcat](/man/zcat)(1), [zless](/man/zless)(1), [zgrep](/man/zgrep)(1), [gzip](/man/gzip)(1), [bzip2](/man/bzip2)(1), [xz](/man/xz)(1)"]],"e":["zcat","zless","zgrep","gzip","bzip2","xz"],"x":"zrun [command] [file.gz]"}});
