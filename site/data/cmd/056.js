window.LCL_BUCKET(56,{"addr2line":{"n":"addr2line","t":"Translate addresses to source file locations","s":[["TAGLINE","Translate addresses to source file locations"],["TLDR","Display the **filename and line number** from an instruction address\n\n```addr2line -e [path/to/executable] [address]```\n\nDisplay the **function name**, filename, and line number\n\n```addr2line -e [path/to/executable] -f [address]```\n\n**Demangle** the function name for C++ code\n\n```addr2line -e [path/to/executable] -f -C [address]```\n\n**Pretty-print** with inlined frames\n\n```addr2line -e [path/to/executable] -f -C -i -p [address]```\n\n**Read addresses from stdin** (e.g., from a stack trace)\n\n```cat [addresses.txt] | addr2line -e [path/to/executable] -f -C```"],["SYNOPSIS","**addr2line** [_-e executable_] [_-f_] [_-C_] [_-s_] [_-i_] [_address_...]"],["DESCRIPTION","**addr2line** translates addresses into file names and line numbers. Given an address in an executable or relocatable object, it uses the debugging information to figure out which file name and line number are associated with it.\n\nThis is particularly useful for interpreting crash dumps and debugging information from stack traces."],["PARAMETERS","**-e, --exe executable**\n> Specify the executable to examine\n\n**-f, --functions**\n> Display function names as well as file and line number information\n\n**-C, --demangle**\n> Decode (demangle) low-level symbol names into user-level names (useful for C++)\n\n**-s, --basenames**\n> Display only the base of each file name rather than the full path\n\n**-i, --inlines**\n> If the address belongs to an inlined function, also print the caller information\n\n**-p, --pretty-print**\n> Print output in a human-readable format\n\n**-a, --addresses**\n> Display the address before the function name, file, and line\n\n**-j, --section** _NAME_\n> Treat input addresses as offsets within the named section.\n\n**--target** _BFDNAME_\n> Specify the binary format of the object file (auto-detected by default).\n\n**-r, --no-recurse-limit**\n> Disable the demangler's recursion limit."],["CAVEATS","Requires the executable to be compiled with debugging information (e.g., **-g** flag for gcc). Stripped executables will not provide useful output."],["HISTORY","Part of **GNU Binutils**, a collection of binary tools for working with object files and executables."],["INSTALL","```apt: sudo apt install binutils```\n\n```dnf: sudo dnf install binutils```\n\n```pacman: sudo pacman -S binutils```\n\n```apk: sudo apk add binutils```\n\n```zypper: sudo zypper install binutils```\n\n```brew: brew install binutils```\n\n```nix: nix profile install nixpkgs#binutils```"],["SEE ALSO","[objdump](/man/objdump)(1), [nm](/man/nm)(1), [readelf](/man/readelf)(1)"]],"e":["objdump","nm","readelf"],"x":"addr2line -e [path/to/executable] [address]"},"ansible-playbook":{"n":"ansible-playbook","t":"Execute Ansible playbooks","s":[["TAGLINE","Execute Ansible playbooks"],["TLDR","**Run** a playbook\n\n```ansible-playbook [playbook.yml]```\n\nRun with **inventory file**\n\n```ansible-playbook -i [inventory.ini] [playbook.yml]```\n\nRun with **sudo**\n\n```ansible-playbook [playbook.yml] --become --ask-become-pass```\n\n**Check** mode (dry run)\n\n```ansible-playbook [playbook.yml] --check```\n\nRun specific **tags**\n\n```ansible-playbook [playbook.yml] --tags \"[deploy,config]\"```\n\nPass **extra variables**\n\n```ansible-playbook [playbook.yml] -e \"env=production version=1.2.3\"```"],["SYNOPSIS","**ansible-playbook** [_-i inventory_] [_-e vars_] [_--tags tags_] [_options_] _playbook.yml_"],["DESCRIPTION","**ansible-playbook** executes Ansible playbooks, which are YAML files defining automation tasks. Playbooks can provision servers, deploy applications, configure services, and orchestrate complex multi-tier deployments.\n\nUnlike ad-hoc ansible commands, playbooks define complete automation workflows with variables, conditionals, loops, and handlers."],["PARAMETERS","**-i** _inventory_, **--inventory** _inventory_\n> Inventory file/path or comma-separated host list\n\n**-e** _vars_, **--extra-vars** _vars_\n> Extra variables (key=value, YAML/JSON, or @file.yml)\n\n**-t** _tags_, **--tags** _tags_\n> Run only plays and tasks tagged with these values\n\n**--skip-tags** _tags_\n> Skip plays and tasks whose tags match these values\n\n**-b**, **--become**\n> Run with privilege escalation (become)\n\n**-K**, **--ask-become-pass**\n> Prompt for privilege escalation password\n\n**-C**, **--check**\n> Dry run without making changes\n\n**-D**, **--diff**\n> Show differences in changed files (works with --check)\n\n**-l** _hosts_, **--limit** _hosts_\n> Further limit selected hosts to an additional pattern\n\n**--list-tasks**\n> List all tasks that would be executed\n\n**--list-tags**\n> List all available tags\n\n**-f** _forks_, **--forks** _forks_\n> Number of parallel processes (default: 5)\n\n**--start-at-task** _task_\n> Start the playbook at the task matching this name\n\n**--step**\n> Confirm each task before running\n\n**--syntax-check**\n> Check the playbook syntax without executing it\n\n**--flush-cache**\n> Clear the fact cache for every host in inventory"],["CONFIGURATION","**/etc/ansible/ansible.cfg**\n> System-wide Ansible configuration, including default inventory, forks, and connection settings.\n\n**~/.ansible.cfg**\n> Per-user Ansible configuration overriding system defaults.\n\n**ansible.cfg**\n> Project-level configuration in the current directory, highest priority.\n\n**/etc/ansible/hosts**\n> Default inventory file used when no inventory is specified."],["CAVEATS","Playbook syntax errors fail fast; use --syntax-check first. Check mode may not work perfectly with all modules. Always test in non-production first."],["HISTORY","**ansible-playbook** has been the primary execution method for Ansible automation since the project's creation in **2012**, enabling infrastructure as code and declarative automation."],["INSTALL","```dnf: sudo dnf install ansible-core```\n\n```pacman: sudo pacman -S ansible-core```\n\n```apk: sudo apk add ansible-core```\n\n```zypper: sudo zypper install ansible-core```"],["SEE ALSO","[ansible](/man/ansible)(1), [ansible-vault](/man/ansible-vault)(1), [ansible-lint](/man/ansible-lint)(1), [ansible-galaxy](/man/ansible-galaxy)(1)"],["RESOURCES","```[Source code](https://github.com/ansible/ansible)```\n\n```[Documentation](https://docs.ansible.com/ansible/latest/cli/ansible-playbook.html)```"]],"e":["ansible","ansible-vault","ansible-lint","ansible-galaxy"],"x":"ansible-playbook [playbook.yml]"},"bashbug":{"n":"bashbug","t":"Report bugs in bash to the maintainers","s":[["TAGLINE","Report bugs in bash to the maintainers"],["TLDR","**Report** bash bug\n\n```bashbug```"],["SYNOPSIS","**bashbug** [_--help_] [_--version_] [_email-address_]"],["DESCRIPTION","**bashbug** is a script for reporting bugs in bash to the maintainers. It collects system information, bash version details, and opens an editor to compose a bug report, then emails it to the bash developers.\n\nBy default stable releases send the report to **bug-bash@gnu.org**; an optional _email-address_ argument overrides the recipient. The editor is chosen from **DEFEDITOR**, **EDITOR**, or **VISUAL**, falling back to common editors and finally **vi**."],["PARAMETERS","**--help**\n> Print usage information and exit.\n\n**--version**\n> Print the bashbug version and exit.\n\n_email-address_\n> Send the report to this address instead of the default."],["WORKFLOW","1. Run `bashbug`\n2. Editor opens with template\n3. Describe the bug\n4. Save and exit\n5. Email sent to bash maintainers"],["INFORMATION COLLECTED","- Bash version\n- Operating system\n- Machine architecture\n- Compiler used\n- Configuration options"],["CAVEATS","Requires working mail system configured. May not work on all systems. Consider reporting bugs via the project's issue tracker instead. Modern bug reporting often done through online platforms."],["HISTORY","**bashbug** has been included with bash distributions since the early versions to facilitate bug reporting to the development team."],["INSTALL","```apt: sudo apt install bash```\n\n```dnf: sudo dnf install bash```\n\n```pacman: sudo pacman -S bash```\n\n```apk: sudo apk add bash-dev```\n\n```zypper: sudo zypper install bash```\n\n```brew: brew install bash```\n\n```nix: nix profile install nixpkgs#bash```"],["SEE ALSO","[bash](/man/bash)(1), [reportbug](/man/reportbug)(1)"],["RESOURCES","```[Homepage](https://www.gnu.org/software/bash/)```\n\n```[Source code](https://git.savannah.gnu.org/cgit/bash.git)```"]],"e":["bash","reportbug"],"x":"bashbug"},"bc":{"n":"bc","t":"Arbitrary precision calculator language","s":[["TAGLINE","Arbitrary precision calculator language"],["TLDR","**Calculate** expression\n\n```echo [\"2+2\"] | bc```\n\n**Division** with decimals\n\n```echo [\"scale=2; 10/3\"] | bc```\n\n**Run** bc script\n\n```bc [script.bc]```\n\n**Math library**\n\n```bc -l```\n\n**Evaluate an expression** directly\n\n```bc -e \"[scale=2; 100/3]\"```\n\n**Interactive** calculator\n\n```bc```"],["SYNOPSIS","**bc** [_options_] [_file_...]"],["DESCRIPTION","**bc** is an arbitrary precision calculator language. It supports interactive calculation, scripting, and mathematical operations with user-definable precision for decimal calculations.\n\nThe tool has been a Unix standard for decades and is widely used in shell scripts for arithmetic."],["PARAMETERS","**-l**, **--mathlib**\n> Load math library (includes functions like s, c, a, l, e, and sets scale to 20)\n\n**-s**, **--standard**\n> POSIX standard mode; error on any non-POSIX extensions\n\n**-q**, **--quiet**\n> Don't print the normal GNU bc welcome banner\n\n**-i**, **--interactive**\n> Force interactive mode\n\n**-w**, **--warn**\n> Warn about POSIX non-compliance\n\n**-e** _expression_, **--expression** _expression_\n> Evaluate expression; multiple -e options are processed in order\n\n**-v**, **--version**\n> Print version number and exit"],["SPECIAL VARIABLES","**scale**\n> Number of decimal places (default: 0)\n\n**ibase**\n> Input base (default: 10)\n\n**obase**\n> Output base (default: 10)"],["OPERATORS","- **+, -, *, /** - Basic arithmetic\n- **%** - Modulo\n- **^** - Exponentiation\n- **++, --** - Increment/decrement\n- **==, !=, <, >** - Comparisons"],["FUNCTIONS","With -l flag (also sets scale to 20):\n- **s(x)** - Sine (x in radians)\n- **c(x)** - Cosine (x in radians)\n- **a(x)** - Arctangent (returns radians)\n- **l(x)** - Natural logarithm\n- **e(x)** - Exponential (e raised to x)\n- **sqrt(x)** - Square root (available without -l)"],["WORKFLOW","```bash"],["Simple math","echo \"2+2\" | bc"],["Floating point","echo \"scale=4; 22/7\" | bc"],["Hex to decimal","echo \"ibase=16; FF\" | bc"],["Square root","echo \"sqrt(2)\" | bc -l"],["Multiple operations","bc <<< \"\nscale=2\nx = 5\ny = 3\nx / y\n\"\n```"],["CAVEATS","Unusual syntax for programmers. Default integer division (set scale). Variables persist in interactive mode. Limited string handling. For modern scripts, consider awk or programming languages."],["HISTORY","**bc** was developed at Bell Labs for Unix in **1975** by Robert Morris and Lorinda Cherry, based on the earlier **dc** calculator. The GNU version, which is the most widely used today, was written by Philip A. Nelson."],["INSTALL","```apt: sudo apt install bc```\n\n```dnf: sudo dnf install bc```\n\n```pacman: sudo pacman -S bc```\n\n```apk: sudo apk add bc```\n\n```zypper: sudo zypper install bc```\n\n```brew: brew install bc```\n\n```nix: nix profile install nixpkgs#bc```"],["SEE ALSO","[dc](/man/dc)(1), [calc](/man/calc)(1), [awk](/man/awk)(1)"]],"e":["dc","calc","awk"],"x":"echo [\"2+2\"] | bc"},"bondai":{"n":"bondai","t":"AI agent framework with built-in tools","s":[["TAGLINE","AI agent framework with built-in tools"],["TLDR","**Install** with pip\n\n```pip install bondai```\n\n**Start an interactive chat session**\n\n```bondai```\n\n**Run BondAI in a Docker container** (recommended for code execution)\n\n```docker run -it --rm -v ./agent-volume:/agent-volume -w /agent-volume -e OPENAI_API_KEY=[key] krohling/bondai:latest bondai```\n\n**Set the required API key** before running\n\n```export OPENAI_API_KEY=[sk-XXXXXXXXXX]```"],["SYNOPSIS","**bondai** [_options_]"],["DESCRIPTION","**BondAI** is an open-source framework for building AI agent systems. The CLI provides an interactive chat interface where users can communicate with an AI agent that has access to a powerful set of built-in tools for search, file operations, code execution, and external integrations.\n\nThe agent handles complex implementation details including memory and context management, error handling, and vector/semantic search. It can perform multi-step tasks autonomously, using tools as needed to accomplish goals.\n\nBuilt-in tools include **DuckDuckGoSearchTool** and **GoogleSearchTool** for web search, **WebsiteQueryTool** for querying webpage content, **FileWriteTool** for file operations, **PythonREPLTool** for Python code execution, and **ShellTool** for shell access. Additional integrations support PostgreSQL databases, Gmail, Alpaca Markets for trading, and Bland AI for phone calls."],["TOOLS","**DuckDuckGoSearchTool**\n> Web search without API key requirement.\n\n**GoogleSearchTool**\n> Web search (requires Google API key).\n\n**WebsiteQueryTool**\n> Query and extract content from webpages.\n\n**FileWriteTool**\n> Create and write files.\n\n**PythonREPLTool**\n> Execute Python code (Docker recommended).\n\n**ShellTool**\n> Execute shell commands (Docker recommended)."],["CAVEATS","Requires an **OpenAI API key** set via the OPENAI_API_KEY environment variable (or Azure OpenAI credentials). Tools that execute arbitrary code (**PythonREPLTool**) or access your shell (**ShellTool**) can potentially damage your system. Running BondAI in a **Docker container** is highly recommended when using these tools. Mount a volume to share files between your system and the agent's working directory."],["HISTORY","BondAI was created by **Kevin Rohling** and released as an open-source project on GitHub. It emerged during the AI agent development wave of **2023-2024**, designed to simplify the creation of autonomous AI systems by handling common implementation challenges like memory management and tool integration. The project supports multiple LLM providers including OpenAI and Azure OpenAI."],["SEE ALSO","[python](/man/python)(1), [docker](/man/docker)(1), [claude](/man/claude)(1)"],["RESOURCES","```[Source code](https://github.com/krohling/bondai)```\n\n```[Homepage](https://bondai.dev)```"]],"e":["python","docker","claude"],"x":"pip install bondai"},"bun-list":{"n":"bun-list","t":"List installed packages in a Bun project","s":[["TAGLINE","List installed packages in a Bun project"],["TLDR","**List installed packages** (top-level)\n\n```bun pm ls```\n\n**List all packages** including nested dependencies\n\n```bun pm ls --all```"],["SYNOPSIS","**bun** **pm** **ls** [**--all**]"],["PARAMETERS","**--all**\n> Show the full dependency tree, including transitive dependencies."],["DESCRIPTION","**bun pm ls** (sometimes invoked as **bun list**) prints the packages installed in the current project's **node_modules** directory. By default it shows only the direct dependencies declared in **package.json**; pass **--all** to see the complete resolved dependency tree.\n\nThe output includes each package's resolved version and location and is intended for quick auditing of what is installed. For machine-readable output, inspect **bun.lockb** with **bun pm** subcommands."],["INSTALL","```pacman: sudo pacman -S bun```\n\n```brew: brew install bun```\n\n```nix: nix profile install nixpkgs#bun```"],["SEE ALSO","[bun](/man/bun)(1), [bun-pm-ls](/man/bun-pm-ls)(1), [bun-outdated](/man/bun-outdated)(1)"]],"e":["bun","bun-pm-ls","bun-outdated"],"x":"bun pm ls"},"chainctl":{"n":"chainctl","t":"manage Chainguard container images and resources","s":[["TAGLINE","manage Chainguard container images and resources"],["TLDR","**Authenticate with Chainguard**\n\n```chainctl auth login```\n\n**List available container images**\n\n```chainctl images list```\n\n**List image tags**\n\n```chainctl images tags list [image_name]```\n\n**Get image details**\n\n```chainctl images describe [image_name]```\n\n**List organizations**\n\n```chainctl iam organizations list```\n\n**Configure chainctl settings**\n\n```chainctl config set [key] [value]```"],["SYNOPSIS","**chainctl** [**--api** _url_] [**--config** _file_] [**-o** _format_] [**--log-level** _level_] _command_ [_subcommand_] [_arguments..._]"],["DESCRIPTION","**chainctl** (Chainguard Control) is the command-line interface for managing Chainguard resources, including security-hardened container images, identity management, and platform configurations.\n\nThe CLI follows the **context noun verb** style of interaction, allowing users to discover available images, analyze security metadata, manage organizations, and control access to Chainguard resources."],["PARAMETERS","**--api** _url_\n> Chainguard platform API URL (default: https://console-api.enforce.dev)\n\n**--config** _file_\n> Path to chainctl config file (or set CHAINCTL_CONFIG)\n\n**-o, --output** _format_\n> Output format: csv, json, table, tree, wide, markdown, yaml, or go-template\n\n**--log-level** _level_\n> Log level: debug, info (default: ERROR)\n\n**--force-color**\n> Force color output\n\n**Main commands:**\n\n**auth** - Authentication commands (login, logout, token)\n**images** - Manage container images (list, describe, tags)\n**iam** - Identity and access management\n**packages** - Interact with Chainguard packages\n**config** - Manage chainctl configuration"],["CONFIGURATION","**CHAINCTL_CONFIG**\n> Environment variable specifying an alternate configuration file location."],["CAVEATS","Requires a Chainguard account for authentication. Some commands require appropriate IAM permissions within your organization."],["HISTORY","**chainctl** was developed by **Chainguard** as part of their secure software supply chain platform. Chainguard focuses on providing minimal, hardened container images with reduced attack surfaces and continuous security updates."],["SEE ALSO","[docker](/man/docker)(1), [cosign](/man/cosign)(1)"],["RESOURCES","```[Documentation](https://edu.chainguard.dev/chainguard/chainctl/)```"]],"e":["docker","cosign"],"x":"chainctl auth login"},"chrt":{"n":"chrt","t":"Manipulate real-time scheduling attributes of processes","s":[["TAGLINE","Manipulate real-time scheduling attributes of processes"],["TLDR","Display **scheduling attributes** of a process\n\n```chrt -p PID```\n\nDisplay attributes of **all threads** of a process\n\n```chrt -a -p PID```\n\nShow **min/max** priority values\n\n```chrt -m```\n\nSet **scheduling priority** of a process\n\n```chrt -p priority PID```\n\nSet **FIFO** scheduling policy\n\n```chrt --fifo -p priority PID```\n\nSet **round-robin** scheduling policy\n\n```chrt --rr -p priority PID```\n\nRun command with **idle** scheduling\n\n```chrt --idle 0 command```"],["SYNOPSIS","**chrt** [_options_] _priority_ _command_ [_argument_...]\n\n**chrt** [_options_] **-p** [_priority_] _PID_"],["DESCRIPTION","**chrt** sets or retrieves the real-time scheduling attributes of an existing process, or runs a command with specified scheduling attributes. It supports various Linux scheduling policies including FIFO, round-robin, batch, idle, and deadline.\n\nReal-time scheduling policies (SCHED_FIFO, SCHED_RR) give processes priority over normal tasks, which is critical for time-sensitive applications like audio processing, industrial control, and high-frequency trading. Non-real-time policies (SCHED_BATCH, SCHED_IDLE) are useful for background workloads that should yield to interactive processes.\n\nThe tool is part of the **util-linux** package and operates via the **sched_setscheduler**(2) and **sched_setattr**(2) system calls."],["PARAMETERS","**-p, --pid**\n> Operate on an existing PID without launching a new task\n\n**-a, --all-tasks**\n> Set or retrieve scheduling attributes for all threads of a PID\n\n**-m, --max**\n> Show minimum and maximum valid priorities for each policy\n\n**-v, --verbose**\n> Show status information\n\n**-R, --reset-on-fork**\n> Children do not inherit privileged scheduling policies\n\n**-o, --other**\n> Set SCHED_OTHER policy (default Linux time-sharing)\n\n**-f, --fifo**\n> Set SCHED_FIFO policy (first in-first out)\n\n**-r, --rr**\n> Set SCHED_RR policy (round-robin, default when no policy given)\n\n**-b, --batch**\n> Set SCHED_BATCH policy for batch processing (priority must be 0)\n\n**-i, --idle**\n> Set SCHED_IDLE policy for very low priority tasks (priority must be 0)\n\n**-d, --deadline**\n> Set SCHED_DEADLINE policy for sporadic deadline scheduling (priority must be 0)"],["CAVEATS","Users require CAP_SYS_NICE capability to modify scheduling attributes. SCHED_BATCH, SCHED_IDLE, and SCHED_DEADLINE policies require priority 0. Only SCHED_FIFO, SCHED_OTHER, and SCHED_RR are part of POSIX 1003.1b."],["HISTORY","**chrt** is part of the **util-linux** package, providing real-time process scheduling control since Linux 2.6."],["INSTALL","```apt: sudo apt install util-linux```\n\n```dnf: sudo dnf install util-linux```\n\n```pacman: sudo pacman -S util-linux```\n\n```apk: sudo apk add util-linux-misc```\n\n```zypper: sudo zypper install util-linux```\n\n```brew: brew install util-linux```\n\n```nix: nix profile install nixpkgs#util-linux```"],["SEE ALSO","[nice](/man/nice)(1), [renice](/man/renice)(1), [taskset](/man/taskset)(1)"]],"e":["nice","renice","taskset"],"x":"chrt -p PID"},"circleci":{"n":"circleci","t":"CLI for CircleCI continuous integration platform","s":[["TAGLINE","CLI for CircleCI continuous integration platform"],["TLDR","**Configure CLI with API token**\n\n```circleci setup```\n\n**Validate config file**\n\n```circleci config validate```\n\n**Run job locally**\n\n```circleci local execute --job [build]```\n\n**Process config file**\n\n```circleci config process [.circleci/config.yml]```\n\n**Create new project**\n\n```circleci project create```\n\n**List orbs**\n\n```circleci orb list```\n\n**Follow a project**\n\n```circleci follow```\n\n**Open CircleCI in browser**\n\n```circleci open```"],["SYNOPSIS","**circleci** [_options_] _command_ [_arguments_]"],["DESCRIPTION","**circleci** is the official CLI for CircleCI, enabling local job execution, config validation, pipeline management, and interaction with CircleCI resources from the terminal.\n\nThe CLI is particularly useful for validating and processing configuration files before pushing changes, running jobs locally in Docker containers for faster development iteration, and managing orbs, contexts, and project settings. It integrates with the CircleCI API for pipeline and workflow management.\n\nLocal execution requires Docker and runs individual jobs in containers that mirror the cloud environment, though some features like caching and workflows are not available locally."],["COMMANDS","**setup**\n> Configure CLI with API token\n\n**config validate**\n> Validate config.yml syntax\n\n**config process**\n> Process and expand config\n\n**local execute**\n> Run jobs locally\n\n**project**\n> Manage projects\n\n**context**\n> Manage contexts and environment variables\n\n**orb**\n> Manage orbs\n\n**namespace**\n> Manage namespaces\n\n**pipeline**\n> Manage pipelines\n\n**version**\n> Show version"],["PARAMETERS","**--token** _token_\n> CircleCI API token\n\n**--host** _url_\n> CircleCI host URL\n\n**--debug**\n> Enable debug output\n\n**--skip-update-check**\n> Skip update check"],["LOCAL EXECUTION","```circleci local execute --job build -e VAR=value```\n\nNote: Workflows, caching, and some features are not supported locally."],["CONFIGURATION","**~/.circleci/cli.yml**\n> CLI configuration including API token and host URL.\n\n**.circleci/config.yml**\n> Project-level CircleCI pipeline configuration defining jobs, workflows, and orbs."],["CAVEATS","Local execution runs single jobs only, not workflows. Caching not supported locally. Some CircleCI features require cloud execution."],["INSTALL","```brew: brew install circleci```"],["SEE ALSO","[docker](/man/docker)(1), [git](/man/git)(1)"]],"e":["docker","git"],"x":"circleci setup"},"conda-install":{"n":"conda-install","t":"package installation manager","s":[["TAGLINE","package installation manager"],["TLDR","**Install a package**\n\n```conda install [package_name]```\n\n**Install specific version**\n\n```conda install [package_name]=[version]```\n\n**Install from specific channel**\n\n```conda install -c [conda-forge] [package_name]```\n\n**Install into specific environment**\n\n```conda install -n [env_name] [package_name]```\n\n**Install multiple packages**\n\n```conda install [package1] [package2] [package3]```\n\n**Install without confirmation**\n\n```conda install -y [package_name]```"],["SYNOPSIS","**conda** **install** [_options_] [_packages..._]"],["PARAMETERS","**-n**, **--name** _name_\n> Install into named environment.\n\n**-c**, **--channel** _channel_\n> Additional channel to search.\n\n**-y**, **--yes**\n> Don't ask for confirmation.\n\n**--freeze-installed**\n> Don't update already-installed packages.\n\n**--update-deps**\n> Update dependencies."],["CONFIGURATION","**~/.condarc**\n> User-level conda configuration for channels, package settings, and solver options.\n\n**/opt/conda/.condarc**\n> System-wide conda configuration."],["DESCRIPTION","**conda install** installs packages into a conda environment. It resolves dependencies and ensures compatibility between packages.\n\nPackages are searched in configured channels, with defaults searched first unless a specific channel is specified."],["INSTALL","```dnf: sudo dnf install conda```\n\n```nix: nix profile install nixpkgs#conda```"],["SEE ALSO","[conda](/man/conda)(1), [conda-remove](/man/conda-remove)(1), [conda-update](/man/conda-update)(1)"],["RESOURCES","```[Source code](https://github.com/conda/conda)```\n\n```[Documentation](https://docs.conda.io/projects/conda/en/stable/commands/install.html)```"]],"e":["conda","conda-remove","conda-update"],"x":"conda install [package_name]"},"coolify":{"n":"coolify","t":"self-hostable PaaS platform","s":[["TAGLINE","self-hostable PaaS platform"],["TLDR","**Install Coolify** on a server\n\n```curl -fsSL https://get.coolify.io | bash```\n\n**Start Coolify** services\n\n```coolify start```\n\n**Stop Coolify** services\n\n```coolify stop```\n\n**Restart Coolify** services\n\n```coolify restart```\n\n**Check Coolify status**\n\n```coolify status```\n\n**Update Coolify** to latest version\n\n```coolify upgrade```\n\n**View Coolify logs**\n\n```coolify logs```"],["SYNOPSIS","**coolify** _command_ [_options_]"],["PARAMETERS","**start**\n> Start all Coolify services and containers.\n\n**stop**\n> Stop all Coolify services.\n\n**restart**\n> Restart all Coolify services.\n\n**status**\n> Show current status of Coolify services.\n\n**upgrade**\n> Upgrade Coolify to the latest version.\n\n**logs**\n> Display logs from Coolify services.\n\n**backup**\n> Create a backup of Coolify configuration and data.\n\n**restore**\n> Restore Coolify from a backup.\n\n**--help**\n> Display help information."],["DESCRIPTION","**Coolify** is an open-source, self-hostable alternative to Heroku, Netlify, and Vercel. It provides a platform for deploying applications, databases, and services on your own infrastructure with minimal configuration.\n\nThe platform supports multiple deployment methods including Git-based deployments, Docker images, and docker-compose files. It handles SSL certificates automatically via Let's Encrypt, provides built-in monitoring, and supports popular databases like PostgreSQL, MySQL, MongoDB, and Redis.\n\nCoolify is designed for developers and teams who want the convenience of PaaS platforms while maintaining control over their infrastructure and data. It runs on Docker and can be installed on any Linux server with minimal requirements."],["CAVEATS","Requires Docker and Docker Compose installed on the host system. Initial installation needs root access. Resource requirements depend on the number of applications deployed. Some advanced features may require additional configuration."],["HISTORY","Coolify was created by Andras Bacsai as an open-source PaaS solution. Development began around **2021**, inspired by the need for self-hosted alternatives to commercial platforms. It has grown to support various application types and gained popularity in the self-hosting community."],["SEE ALSO","[docker](/man/docker)(1), [docker-compose](/man/docker-compose)(1), [dokku](/man/dokku)(1)"],["RESOURCES","```[Source code](https://github.com/coollabsio/coolify)```\n\n```[Homepage](https://coolify.io)```\n\n```[Documentation](https://coolify.io/docs)```"]],"e":["docker","docker-compose","dokku"],"x":"curl -fsSL https://get.coolify.io | bash"},"coredumpctl":{"n":"coredumpctl","t":"core dump retrieval and analysis","s":[["TAGLINE","core dump retrieval and analysis"],["TLDR","**List** all captured core dumps\n\n```coredumpctl```\n\nList core dumps for a **specific program**\n\n```coredumpctl list program```\n\nShow **information** about core dump with PID\n\n```coredumpctl info 1234```\n\n**Debug** using the last core dump\n\n```coredumpctl debug```\n\nDebug last core dump of a **specific program**\n\n```coredumpctl debug program```\n\n**Extract** core dump to file\n\n```coredumpctl -o /path/to/file dump program```\n\nDebug with custom **gdb arguments**\n\n```coredumpctl debug -A \"-ex bt\"```"],["SYNOPSIS","**coredumpctl** [_OPTIONS_] [_COMMAND_] [_MATCHES_...]"],["DESCRIPTION","**coredumpctl** is a systemd utility for managing core dumps - memory snapshots created when a program crashes. Instead of traditional core dump files scattered across the filesystem, systemd-coredump captures crashes and stores them in the journal with rich metadata including timestamps, process information, and system state.\n\nThe tool provides a unified interface for working with these captured core dumps. You can list all crashes, filter by program or time range, view detailed information about specific crashes, and launch a debugger to analyze the failure. Core dumps can also be extracted to files for offline analysis or sharing with developers.\n\nThis centralized approach to core dump management makes it much easier to track down intermittent crashes, debug production issues, and maintain system reliability. The integration with journalctl means crash data is preserved alongside system logs, providing full context for debugging. The tool requires systemd-coredump to be configured as the system's core dump handler, which is the default on most modern systemd-based distributions."],["COMMANDS","**list [MATCHES]**\n> List core dumps in journal (default command)\n\n**info [MATCHES]**\n> Show detailed information about core dumps\n\n**dump [MATCHES]**\n> Export core dump data to stdout or file\n\n**debug [MATCHES]**\n> Invoke debugger on a core dump"],["PARAMETERS","**-o, --output FILE**\n> Write dump output to file\n\n**-1**\n> Show only the most recent core dump\n\n**-S, --since TIME**\n> Filter by start time\n\n**-U, --until TIME**\n> Filter by end time\n\n**-r, --reverse**\n> Show newest entries first\n\n**-F, --field FIELD**\n> Print all values of specified field\n\n**-D, --directory DIR**\n> Use journal files from directory\n\n**--debugger DEBUGGER**\n> Use specific debugger (default: gdb)\n\n**-A, --debugger-arguments ARGS**\n> Pass arguments to debugger\n\n**-n, --lines NUM**\n> Number of journal lines to show\n\n**-q, --quiet**\n> Suppress informational messages\n\n**--json MODE**\n> Output as JSON (short, pretty, off)"],["MATCHES","Matches filter by: PID, executable name, path, or core dump timestamp."],["CONFIGURATION","**/etc/systemd/coredump.conf**\n> Configuration file controlling core dump storage, compression, and retention settings."],["CAVEATS","Core dumps may contain sensitive data. Ensure proper permissions on extracted files. Requires systemd-coredump to be configured as the core dump handler."],["HISTORY","**coredumpctl** is part of **systemd**, providing centralized core dump management through systemd-coredump and the journal."],["INSTALL","```apt: sudo apt install systemd-coredump```"],["SEE ALSO","[gdb](/man/gdb)(1), [journalctl](/man/journalctl)(1)"]],"e":["gdb","journalctl"],"x":"coredumpctl"},"courtside":{"n":"courtside","t":"Terminal UI for NBA games, scores, and standings","s":[["TAGLINE","Terminal UI for NBA games, scores, and standings"],["TLDR","**Launch** the NBA scoreboard TUI\n\n```courtside```\n\n**Install** from source\n\n```git clone https://github.com/NolanFogarty/courtside.git && cd courtside && go build -o courtside```\n\n**Install** with Go\n\n```go install github.com/NolanFogarty/courtside@latest```"],["SYNOPSIS","**courtside**"],["DESCRIPTION","**courtside** is a keyboard-driven terminal application for following NBA games. It pulls live and historical data from the NBA's public JSON endpoints through the **nba-sdk** library, so no API key or account is required.\n\nOn launch, the app opens on today's games. You can move through the schedule, open a game for box score and play-by-play details, jump to any date, filter the list, and view league standings. Games in progress refresh automatically about every 15 seconds.\n\nNavigation is entirely from the keyboard. The interface has three main views: the game list, detailed game view, and standings."],["GAME LIST KEYS","**↑**/**k**, **↓**/**j**\n> Move between games\n\n**Enter**\n> Open the selected game\n\n**←**/**h**, **→**/**l**\n> Previous or next day\n\n**d**\n> Jump to a specific date\n\n**s**\n> Open league standings\n\n**/**\n> Filter games\n\n**q**\n> Quit"],["GAME DETAIL KEYS","**↑**/**k**, **↓**/**j**\n> Scroll play-by-play\n\n**o**\n> Toggle expanded stats\n\n**q**, **Esc**\n> Return to the game list"],["CAVEATS","The NBA endpoints are unofficial and undocumented. They can change or rate-limit without notice.\n\nBecause the data source is public but unofficial, availability and field accuracy are not guaranteed for production use."],["SEE ALSO","[go](/man/go)(1)"]],"e":["go"],"x":"courtside"},"csvsort":{"n":"csvsort","t":"Type-aware CSV file sorter","s":[["TAGLINE","Type-aware CSV file sorter"],["TLDR","**Sort by column**\n\n```csvsort -c [column] [data.csv]```\n\n**Sort descending**\n\n```csvsort -r -c [column] [data.csv]```\n\n**Sort by multiple columns**\n\n```csvsort -c [col1,col2] [data.csv]```\n\n**Sort numerically**\n\n```csvsort -c [amount] [data.csv]```\n\n**Sort with no header**\n\n```csvsort -H -c [1] [data.csv]```\n\n**Read from stdin**\n\n```cat [data.csv] | csvsort -c [name]```"],["SYNOPSIS","**csvsort** [_options_] _file_"],["DESCRIPTION","**csvsort** sorts CSV files by one or more columns. Part of csvkit, it properly handles CSV quoting and data types, detecting numeric and date values for appropriate sorting.\n\nThe tool maintains CSV structure during sorting, preserving headers and quoted fields correctly."],["PARAMETERS","**-c** _columns_\n> Column(s) to sort by.\n\n**-r**, **--reverse**\n> Sort in descending order.\n\n**-H**, **--no-header-row**\n> Input has no header.\n\n**-d** _char_\n> Field delimiter.\n\n**-q** _char_\n> Quote character.\n\n**-e** _encoding_\n> Input encoding.\n\n**-y** _n_\n> Rows to sniff for type."],["CAVEATS","Requires loading entire file into memory. Part of csvkit, needs Python. Type detection may not always match expectations. Large files can be slow."],["HISTORY","**csvsort** is part of **csvkit**, created by **Christopher Groskopf** in **2011**. The tool provides type-aware sorting that the Unix sort command cannot easily achieve with CSV data."],["SEE ALSO","[csvcut](/man/csvcut)(1), [csvlook](/man/csvlook)(1), [sort](/man/sort)(1), [csvstat](/man/csvstat)(1)"],["RESOURCES","```[Source code](https://github.com/wireservice/csvkit)```\n\n```[Documentation](https://csvkit.readthedocs.io)```"]],"e":["csvcut","csvlook","sort","csvstat"],"x":"csvsort -c [column] [data.csv]"},"difftastic":{"n":"difftastic","t":"Structural diff tool with syntax awareness","s":[["TAGLINE","Structural diff tool with syntax awareness"],["TLDR","**Compare** two files with syntax-aware diffing\n\n```difft [file1] [file2]```\n\n**Compare** files or directories recursively\n\n```difft [dir1] [dir2]```\n\n**Show** changes in a specific file from git\n\n```difft --git [file]```\n\n**Use** as git difftool\n\n```git difftool --tool=difftastic```"],["SYNOPSIS","**difft** [_options_] [_path1_] [_path2_]"],["PARAMETERS","**--color** _MODE_\n> Color mode: auto, always, never\n\n**--display** _MODE_\n> Display mode: side-by-side, inline, or json\n\n**--syntax-highlight** _MODE_\n> Syntax highlighting: on, off\n\n**--check-only**\n> Exit with status 1 if files differ\n\n**--strip-cr**\n> Strip carriage returns before comparing\n\n**--context** _N_\n> Number of context lines to show\n\n**--tab-width** _N_\n> Tab width for display (default: 4)\n\n**--width** _N_\n> Maximum width of output display\n\n**--list-languages**\n> Print all supported languages\n\n**-h, --help**\n> Display help and exit\n\n**-V, --version**\n> Display version and exit"],["DESCRIPTION","**difftastic** is a structural diff tool that understands syntax. Unlike traditional line-based diff tools, it parses the code according to the programming language and shows changes based on the actual structure of the code.\n\nThe tool supports over 20 programming languages and provides more accurate diffs by recognizing when code has moved versus changed. It can be used standalone or integrated with git as a difftool."],["GIT INTEGRATION","Configure as git difftool:\n\n```git config --global diff.tool difftastic```\n```git config --global difftool.difftastic.cmd 'difft \"$LOCAL\" \"$REMOTE\"'```\n\nUse with git:\n\n```git difftool```"],["CAVEATS","May be slower than traditional diff tools for very large files. Requires language grammars which may not support all edge cases. Binary files are not supported. Very deeply nested structures may have display issues."],["HISTORY","**difftastic** was created by Wilfred Hughes to address the limitations of line-based diffing when working with code. It uses tree-sitter parsers to build syntax trees and compare them structurally. The project has gained significant popularity for providing clearer, more meaningful diffs."],["INSTALL","```dnf: sudo dnf install difftastic```\n\n```pacman: sudo pacman -S difftastic```\n\n```zypper: sudo zypper install difftastic```\n\n```brew: brew install difftastic```\n\n```nix: nix profile install nixpkgs#difftastic```"],["SEE ALSO","[diff](/man/diff)(1), [git](/man/git)(1), [delta](/man/delta)(1), [diffnav](/man/diffnav)(1)"]],"e":["diff","git","delta","diffnav"],"x":"difft [file1] [file2]"},"docker-network":{"n":"docker-network","t":"manage container networking and connectivity","s":[["TAGLINE","manage container networking and connectivity"],["TLDR","**List networks**\n\n```docker network ls```\n\n**Create a network**\n\n```docker network create [name]```\n\n**Create bridge network with subnet**\n\n```docker network create --driver bridge --subnet [192.168.1.0/24] [name]```\n\n**Connect container to network**\n\n```docker network connect [network] [container]```\n\n**Disconnect container**\n\n```docker network disconnect [network] [container]```\n\n**Inspect network**\n\n```docker network inspect [network]```\n\n**Remove network**\n\n```docker network rm [network]```"],["SYNOPSIS","**docker** **network** _command_ [_options_]"],["SUBCOMMANDS","**ls**\n> List networks.\n\n**create**\n> Create a network.\n\n**rm**\n> Remove networks.\n\n**inspect**\n> Display detailed information.\n\n**connect**\n> Connect container to network.\n\n**disconnect**\n> Disconnect container from network.\n\n**prune**\n> Remove unused networks."],["DESCRIPTION","**docker network** manages Docker networks. Networks enable communication between containers. Supports bridge, host, overlay, and macvlan drivers. Containers on the same user-defined network can communicate using container names as hostnames, providing service discovery. Custom networks isolate containers and provide better security than the default bridge network."],["INSTALL","```apt: sudo apt install docker-cli```\n\n```dnf: sudo dnf install docker-cli```\n\n```pacman: sudo pacman -S docker```\n\n```apk: sudo apk add docker-cli```\n\n```zypper: sudo zypper install docker```\n\n```brew: brew install docker```\n\n```nix: nix profile install nixpkgs#docker```"],["SEE ALSO","[docker-run](/man/docker-run)(1), [docker-compose](/man/docker-compose)(1)"],["RESOURCES","```[Source code](https://github.com/docker/cli)```\n\n```[Documentation](https://docs.docker.com/reference/cli/docker/)```"]],"e":["docker-run","docker-compose"],"x":"docker network ls"},"doctl-auth":{"n":"doctl-auth","t":"manage DigitalOcean CLI authentication contexts","s":[["TAGLINE","manage DigitalOcean CLI authentication contexts"],["TLDR","**Initialize authentication**\n\n```doctl auth init```\n\n**Initialize with token** directly\n\n```doctl auth init --access-token [token]```\n\n**List auth contexts**\n\n```doctl auth list```\n\n**Switch to context**\n\n```doctl auth switch --context [context_name]```\n\n**Remove auth context**\n\n```doctl auth remove --context [context_name]```"],["SYNOPSIS","**doctl auth** _command_ [_options_]"],["PARAMETERS","_COMMAND_\n> Operation: init, list, switch, remove.\n\n**init**\n> Initialize authentication with token.\n\n**list**\n> List authentication contexts.\n\n**switch** **--context** _NAME_\n> Switch active context.\n\n**remove** **--context** _NAME_\n> Remove context.\n\n**--access-token** _TOKEN_\n> API access token.\n\n**--context** _NAME_\n> Context name.\n\n**--help**\n> Display help information."],["DESCRIPTION","**doctl auth** manages authentication contexts for the DigitalOcean CLI. Contexts allow storing multiple API tokens for different accounts, teams, or environments.\n\nThe init command prompts for or accepts an API token, storing it securely. Multiple contexts can be configured and switched between, enabling management of different DigitalOcean accounts from a single CLI installation.\n\nAuthentication tokens are obtained from the DigitalOcean control panel and provide API access according to their scope."],["CAVEATS","Tokens are stored locally. Token scopes determine available actions. Expired or revoked tokens require re-authentication. Contexts are machine-specific."],["HISTORY","doctl auth is part of the **doctl** CLI developed by **DigitalOcean**. Multi-context support enables users and organizations to manage multiple accounts efficiently."],["INSTALL","```dnf: sudo dnf install doctl```\n\n```pacman: sudo pacman -S doctl```\n\n```apk: sudo apk add doctl```\n\n```brew: brew install doctl```\n\n```nix: nix profile install nixpkgs#doctl```"],["SEE ALSO","[doctl](/man/doctl)(1), [doctl-compute-droplet](/man/doctl-compute-droplet)(1)"],["RESOURCES","```[Source code](https://github.com/digitalocean/doctl)```\n\n```[Documentation](https://docs.digitalocean.com/reference/doctl/reference/auth/)```"]],"e":["doctl","doctl-compute-droplet"],"x":"doctl auth init"},"esearch":{"n":"esearch","t":"NCBI Entrez database search utility","s":[["TAGLINE","NCBI Entrez database search utility"],["TLDR","Search **pubmed** for a query\n\n```esearch -db pubmed -query \"[selective serotonin reuptake inhibitor]\"```\n\nSearch **protein** database using regex\n\n```esearch -db [protein] -query '[Escherichia*]'```\n\nSearch **nucleotide** database with field qualifiers\n\n```esearch -db nuccore -query \"[insulin [PROT] AND rodents [ORGN]]\"```\n\nDisplay **help**\n\n```esearch [-h|-help]```"],["SYNOPSIS","**esearch** [_options_]"],["DESCRIPTION","**esearch** is a command-line tool for performing searches in NCBI's Entrez databases using indexed field queries. It provides programmatic access to major biological databases including PubMed (biomedical literature), GenBank (nucleotide sequences), Protein (protein sequences), and many others.\n\nThe tool is part of the NCBI EDirect (Entrez Direct) utilities suite, designed for automated retrieval and processing of biological data. esearch typically serves as the first step in a pipeline, identifying records that match search criteria. Its output is usually piped to other EDirect commands like efetch (to retrieve full records) or elink (to find related data across databases). This enables powerful automation of biological database queries in research workflows."],["PARAMETERS","**-db** _database_\n> Database to search\n\n**-query** _string_\n> Search query with optional field qualifiers\n\n**-h, -help**\n> Display help"],["CAVEATS","Part of the edirect package from NCBI. Requires internet connection. Output is typically piped to elink or efetch for further processing."],["INSTALL","```apt: sudo apt install ncbi-entrez-direct```"],["SEE ALSO","[elink](/man/elink)(1), [einfo](/man/einfo)(1)"]],"e":["elink","einfo"],"x":"esearch -db pubmed -query \"[selective serotonin reuptake inhibitor]\""},"exenv":{"n":"exenv","t":"Elixir version manager","s":[["TAGLINE","Elixir version manager"],["TLDR","**List installed versions**\n\n```exenv versions```\n\n**List available versions**\n\n```exenv install --list```\n\n**Install a version**\n\n```exenv install [1.14.0]```\n\n**Set global version**\n\n```exenv global [1.14.0]```\n\n**Set local version**\n\n```exenv local [1.14.0]```\n\n**Show current version**\n\n```exenv version```\n\n**Rehash shims**\n\n```exenv rehash```"],["SYNOPSIS","**exenv** _command_ [_args_]"],["SUBCOMMANDS","**versions**\n> List installed versions.\n\n**version**\n> Show current version.\n\n**install**\n> Install a version.\n\n**uninstall**\n> Uninstall a version.\n\n**global**\n> Set global version.\n\n**local**\n> Set directory-local version.\n\n**shell**\n> Set shell-specific version.\n\n**rehash**\n> Rehash exenv shims.\n\n**which**\n> Show path to executable."],["DESCRIPTION","**exenv** manages multiple Elixir versions. Allows switching between versions per-project or globally. Similar to rbenv for Ruby."],["CONFIGURATION","**~/.exenv/version**\n> Global default Elixir version.\n\n**.exenv-version**\n> Directory-local Elixir version file, written by `exenv local`."],["SEE ALSO","[elixir](/man/elixir)(1), [mix](/man/mix)(1), [rbenv](/man/rbenv)(1)"],["RESOURCES","```[Source code](https://github.com/mururu/exenv)```"]],"e":["elixir","mix","rbenv"],"x":"exenv versions"},"git-commit":{"n":"git-commit","t":"Record changes to the repository","s":[["TAGLINE","Record changes to the repository"],["TLDR","**Commit staged changes**\n\n```git commit -m \"[message]\"```\n\n**Commit all tracked changes**\n\n```git commit -am \"[message]\"```\n\n**Amend last commit**\n\n```git commit --amend```\n\n**Commit with editor**\n\n```git commit```\n\n**Empty commit**\n\n```git commit --allow-empty -m \"[message]\"```\n\n**Commit with signoff**\n\n```git commit -s -m \"[message]\"```\n\n**Fixup commit**\n\n```git commit --fixup [commit]```\n\n**Squash commit**\n\n```git commit --squash [commit]```"],["SYNOPSIS","**git** **commit** [_options_] [_--_] [_files_...]"],["PARAMETERS","**-m**, **--message** _msg_\n> Commit message.\n\n**-a**, **--all**\n> Stage all modified files.\n\n**--amend**\n> Amend previous commit.\n\n**--no-edit**\n> Use previous message.\n\n**-s**, **--signoff**\n> Add Signed-off-by.\n\n**--allow-empty**\n> Allow empty commits.\n\n**--fixup** _commit_\n> Fixup for commit.\n\n**--squash** _commit_\n> Squash into commit.\n\n**-v**, **--verbose**\n> Show diff in editor.\n\n**--author** _author_\n> Override author.\n\n**--date** _date_\n> Override the author date.\n\n**-C**, **--reuse-message** _commit_\n> Reuse the message (and authorship) from an existing commit.\n\n**-c**, **--reedit-message** _commit_\n> Like --reuse-message but open the editor to edit the message.\n\n**-F**, **--file** _file_\n> Take the commit message from _file_.\n\n**-S**, **--gpg-sign** [_keyid_]\n> GPG-sign the commit.\n\n**-n**, **--no-verify**\n> Skip the pre-commit and commit-msg hooks.\n\n**-e**, **--edit**\n> Force opening the editor (with -m/-F).\n\n**-p**, **--patch**\n> Interactively choose hunks to stage and commit.\n\n**--dry-run**\n> Show what would be committed without creating a commit."],["DESCRIPTION","**git commit** records changes to the repository by creating a new commit object containing the currently staged changes, metadata, and a descriptive message. Each commit represents a snapshot in the repository's history, identified by a unique SHA-1 hash and linked to its parent commit(s).\n\nThe standard workflow involves staging changes with git add, then committing those staged changes. The -a flag shortcuts this by automatically staging all modified tracked files before committing. Commit messages can be provided inline with -m or by opening an editor where you can write multi-line messages with detailed explanations.\n\nThe --amend option modifies the most recent commit instead of creating a new one, useful for correcting mistakes or adding forgotten changes. This rewrites history, so amended commits should not be pushed to shared branches unless force-pushing is acceptable.\n\nAdvanced options support signing commits with GPG (--gpg-sign), adding metadata like co-authors or issue references, and creating specialized commits for rebase workflows. The --fixup and --squash options create commits meant to be combined with earlier commits during interactive rebasing, supporting cleanup of messy development history.\n\nEmpty commits (--allow-empty) are occasionally useful for triggering CI/CD pipelines or marking milestones without actual code changes. The --verbose flag shows the full diff in the editor, helping write accurate commit messages by reviewing what's being committed."],["CONFIGURATION","**~/.gitconfig [commit]**\n> Commit-related settings including default gpgSign, template, and cleanup behavior.\n\n**.git/hooks/commit-msg**\n> Hook script that validates or modifies commit messages before finalizing.\n\n**.git/hooks/pre-commit**\n> Hook script that runs checks before allowing a commit to proceed."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-add](/man/git-add)(1), [git-status](/man/git-status)(1)"]],"e":["git-add","git-status"],"x":"git commit -m \"[message]\""},"git-ls-tree":{"n":"git-ls-tree","t":"List tree object contents","s":[["TAGLINE","List tree object contents"],["TLDR","**List tree contents**\n\n```git ls-tree HEAD```\n\n**List recursively**\n\n```git ls-tree -r HEAD```\n\n**Show only names**\n\n```git ls-tree --name-only HEAD```\n\n**List specific directory**\n\n```git ls-tree HEAD [src/]```\n\n**Show sizes**\n\n```git ls-tree -l HEAD```"],["SYNOPSIS","**git ls-tree** [_options_] _tree-ish_ [_path_]"],["PARAMETERS","_TREE-ISH_\n> Tree or commit to list.\n\n_PATH_\n> Limit to path.\n\n**-r**\n> Recurse into subtrees.\n\n**-d**\n> Show only trees.\n\n**--name-only**\n> Show only names.\n\n**--name-status**\n> Show names and status.\n\n**-l**, **--long**\n> Show object sizes.\n\n**--abbrev** _N_\n> Abbreviate hashes.\n\n**--help**\n> Display help information."],["DESCRIPTION","**git ls-tree** lists the contents of a tree object, showing file names, modes, types, and object hashes for a specific commit's directory structure. It provides a snapshot of the repository's file layout at any given commit.\n\nThe command is useful for scripting and understanding how Git stores directory contents internally. Recursive mode (`-r`) shows all files across all subdirectories, while `--name-only` provides clean output suitable for piping to other commands."],["CAVEATS","Shows tree at specific commit. Plumbing command for scripts. Output format is machine-readable."],["HISTORY","git ls-tree is a core **Git** plumbing command for examining tree objects, part of git's low-level interface."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-ls-files](/man/git-ls-files)(1), [git-ls-remote](/man/git-ls-remote)(1), [git-show](/man/git-show)(1)"],["RESOURCES","```[Source code](https://github.com/git/git)```\n\n```[Homepage](https://git-scm.com/)```\n\n```[Documentation](https://git-scm.com/docs/git-ls-tree)```"]],"e":["git-ls-files","git-ls-remote","git-show"],"x":"git ls-tree HEAD"},"go-bug":{"n":"go-bug","t":"open Go issue tracker with system info","s":[["TAGLINE","open Go issue tracker with system info"],["TLDR","**Open bug report page**\n\n```go bug```"],["SYNOPSIS","**go bug**"],["DESCRIPTION","**go bug** opens the Go issue tracker in a web browser with pre-filled system information. It collects Go version, platform, and environment details to streamline bug reports.\n\nThe command simplifies reporting issues to the Go project by automatically gathering relevant diagnostic information and opening the browser to the GitHub issue form."],["CAVEATS","Opens browser. Information may include environment variables. Review before submitting."],["HISTORY","go bug was added to the **Go** toolchain to streamline bug reporting with automatic environment information collection."],["SEE ALSO","[go](/man/go)(1), [go-version](/man/go-version)(1)"],["RESOURCES","```[Documentation](https://pkg.go.dev/cmd/go#hdr-Start_a_bug_report)```\n\n```[Source code](https://github.com/golang/go)```"]],"e":["go","go-version"],"x":"go bug"},"gup":{"n":"gup","t":"Update binaries installed by \"go install\" in parallel","s":[["TAGLINE","Update binaries installed by \"go install\" in parallel"],["TLDR","**Update all binaries** installed under $GOPATH/bin in parallel\n\n```gup update```\n\n**List all installed** Go binaries with paths and versions\n\n```gup list```\n\n**Update all binaries** but exclude specific ones\n\n```gup update --exclude=[binary1,binary2]```\n\n**Preview updates** without making any changes\n\n```gup update --dry-run```\n\n**Check** if installed binaries are at their latest versions\n\n```gup check```\n\n**Remove** an installed Go binary\n\n```gup remove [binary_name]```\n\n**Export** current binary configuration to gup.json\n\n```gup export```\n\n**Reinstall** all binaries from a gup.json configuration file\n\n```gup import --input=[path/to/gup.json]```"],["SYNOPSIS","**gup** _subcommand_ [_options_]"],["PARAMETERS","**update**\n> Update all binaries under $GOPATH/bin to the latest version using goroutines.\n\n**list**\n> Display all installed binaries with their import paths and versions.\n\n**remove** _binary_\n> Delete the specified binary from $GOPATH/bin or $GOBIN.\n\n**check**\n> Verify whether each installed binary is at its latest version without updating.\n\n**export**\n> Write the current set of binaries to a gup.json configuration file.\n\n**import**\n> Install or restore binaries listed in a gup.json file.\n\n**man**\n> Generate man pages (Linux/macOS).\n\n**completion**\n> Generate shell completion scripts.\n\n**version**\n> Print version information.\n\n**-e**, **--exclude** _list_\n> Comma-separated list of binaries to skip during update.\n\n**--dry-run**\n> Show what would be done without performing any updates.\n\n**--notify**\n> Display a desktop notification when the operation finishes.\n\n**--main**\n> Use the @main module channel when updating.\n\n**--master**\n> Use the @master module channel when updating.\n\n**--latest**\n> Use the @latest module channel when updating.\n\n**--force**\n> Skip confirmation prompts (used with **remove**)."],["DESCRIPTION","**gup** updates binaries installed by **go install** to the latest version. It scans the directory pointed to by **GOBIN** (or **$GOPATH/bin**), inspects each binary's embedded module information, and rebuilds them in parallel using goroutines. This makes upgrading large collections of Go-based CLI tools significantly faster than running **go install** manually for each one.\n\nThe tool reads version metadata embedded in Go binaries since Go 1.17 and feeds the original module path back into **go install**. Binaries built without module information cannot be updated and are skipped with a warning.\n\nIn addition to updates, **gup** provides subcommands for listing, removing, checking, exporting, and re-importing binaries, making it useful for replicating a Go toolchain across machines."],["CAVEATS","Requires the **go** toolchain to be installed and available in **$PATH**. Only binaries built with module information (Go 1.17+ default) can be updated. Binaries installed via **GOPROXY=off** or from local paths cannot be reinstalled. Update channels (**@latest**, **@main**, **@master**) must be supported by the upstream module."],["CONFIGURATION","**gup.json**\n> Configuration file written by **gup export** and consumed by **gup import**. Stores each binary's module path and chosen update channel. Replaced the older **gup.conf** format in v1.0.0.\n\n**$GOPATH/bin** / **$GOBIN**\n> Target directory scanned for binaries. **$GOBIN** takes precedence when set."],["HISTORY","**gup** was created by **nao1215** and first released in **2022**. Written in Go, it was designed to make managing Go-installed CLI tools easier by updating them concurrently. Version **1.0.0** introduced the JSON configuration format and per-binary update channel selection."],["INSTALL","```brew: brew install gup```\n\n```nix: nix profile install nixpkgs#gup```"],["SEE ALSO","[go](/man/go)(1), [asdf](/man/asdf)(1)"]],"e":["go","asdf"],"x":"gup update"},"gv2gml":{"n":"gv2gml","t":"converts Graphviz DOT format to GML","s":[["TAGLINE","converts Graphviz DOT format to GML"],["TLDR","**Convert DOT to GML**\n\n```gv2gml [input.gv] -o [output.gml]```\n\n**Output to stdout**\n\n```gv2gml [input.dot]```\n\n**Process multiple files**\n\n```gv2gml [file1.gv] [file2.gv] -o [output.gml]```"],["SYNOPSIS","**gv2gml** [_-y_] [_-?_] [**-o** _outfile_] [_files_]"],["PARAMETERS","_FILES_\n> Input DOT/GV files. Reads stdin if none given.\n\n**-o** _outfile_\n> Write output to outfile instead of stdout.\n\n**-y**\n> Use yWorks.com GML attribute conventions instead of the plain GML specification.\n\n**-?**\n> Display usage information and exit."],["DESCRIPTION","**gv2gml** converts a graph specified in the DOT language to GML (Graph Modelling Language), an interchange format used by graph analysis and visualization tools such as yEd and Gephi.\n\nIt is the reverse-direction counterpart of **gml2gv**, both built from the same source and dispatching on the program name."],["CAVEATS","Does not support subgraphs or clusters. GML has no way to declare default node/edge attributes, so gv2gml repeats each attribute explicitly on every node and edge."],["HISTORY","gv2gml is part of the **Graphviz** project, created at **AT&T Labs** for graph format conversion."],["INSTALL","```apt: sudo apt install graphviz```\n\n```dnf: sudo dnf install graphviz```\n\n```pacman: sudo pacman -S graphviz```\n\n```apk: sudo apk add graphviz```\n\n```zypper: sudo zypper install graphviz```\n\n```brew: brew install graphviz```\n\n```nix: nix profile install nixpkgs#graphviz```"],["SEE ALSO","[dot](/man/dot)(1), [gml2gv](/man/gml2gv)(1), [graphml2gv](/man/graphml2gv)(1), [gxl2gv](/man/gxl2gv)(1)"],["RESOURCES","```[Homepage](https://graphviz.org/)```\n\n```[Source code](https://gitlab.com/graphviz/graphviz)```\n\n```[Documentation](https://graphviz.org/doc/info/command.html)```"]],"e":["dot","gml2gv","graphml2gv","gxl2gv"],"x":"gv2gml [input.gv] -o [output.gml]"},"hash":{"n":"hash","t":"shell built-in that manages the hash table of recently executed commands","s":[["TAGLINE","shell built-in that manages the hash table of recently executed commands"],["TLDR","**Show command hash table**\n\n```hash```\n\n**Clear hash table**\n\n```hash -r```\n\n**Add command to hash**\n\n```hash -p [/usr/local/bin/mycommand] [mycommand]```\n\n**Remove command from hash**\n\n```hash -d [command]```\n\n**Show path for command**\n\n```hash -t [command]```"],["SYNOPSIS","**hash** [_options_] [_name_...]"],["PARAMETERS","**-r**\n> Clear hash table.\n\n**-p** _path_ _name_\n> Add path for name.\n\n**-d** _name_\n> Delete name from hash.\n\n**-t** _name_\n> Print path for name.\n\n**-l**\n> List in reusable format."],["DESCRIPTION","**hash** is a shell built-in that manages the hash table of recently executed commands. The shell uses this table to remember the full paths of commands, avoiding repeated PATH searches.\n\nWhen a command is executed, the shell stores its path in the hash table. Subsequent invocations use the cached path, improving performance.\n\n```bash"],["View hash table","hash"],["Clear after installing new software","hash -r"],["Check where a command is hashed","hash -t python"],["List all hashed commands","hash -l\n```"],["CAVEATS","Shell built-in; behavior and available options vary by shell (bash, zsh, ksh each implement their own version). The hash table becomes stale if a hashed command's file is moved or removed. Run `hash -r` after installing software or modifying PATH. Only affects the current shell session, not child processes."],["HISTORY","**hash** has been part of Unix shells since the Bourne shell. POSIX only mandates the `-r` option; bash extends it with `-p`, `-d`, `-t`, and `-l` for finer control over the command path cache."],["SEE ALSO","[type](/man/type)(1), [which](/man/which)(1), [whence](/man/whence)(1), [command](/man/command)(1)"],["RESOURCES","```[Documentation](https://www.gnu.org/software/bash/manual/bash.html#Bourne-Shell-Builtins)```"]],"e":["type","which","whence","command"],"x":"hash"},"im-config":{"n":"im-config","t":"configures input method framework for Linux desktops","s":[["TAGLINE","configures input method framework for Linux desktops"],["TLDR","**Configure input method**\n\n```im-config```\n\n**List available methods**\n\n```im-config -l```\n\n**Set input method**\n\n```im-config -n [ibus]```\n\n**Show current method**\n\n```im-config -c```\n\n**Reset to auto**\n\n```im-config -a```"],["SYNOPSIS","**im-config** [_options_]"],["PARAMETERS","**-l**\n> List available methods.\n\n**-n** _METHOD_\n> Set input method.\n\n**-c**\n> Show current setting.\n\n**-a**\n> Auto-detect method.\n\n**-x**\n> X mode (GUI).\n\n**--help**\n> Display help information."],["DESCRIPTION","**im-config** configures input method framework for Linux desktops. It selects between IBus, Fcitx, SCIM, and other input systems.\n\nThe tool manages X startup scripts for input method activation. It's used for setting up multilingual input."],["CAVEATS","Debian/Ubuntu tool. Session restart needed. Desktop-specific behavior."],["HISTORY","im-config is a Debian tool for managing input method configuration."],["SEE ALSO","[ibus](/man/ibus)(1), [fcitx](/man/fcitx)(1), [im-launch](/man/im-launch)(1)"]],"e":["ibus","fcitx","im-launch"],"x":"im-config"},"impacket-smbclient":{"n":"impacket-smbclient","t":"generic SMB client for listing shares and files, uploading","s":[["TAGLINE","generic SMB client for listing shares and files, uploading"],["TLDR","**Connect to SMB server**\n\n```impacket-smbclient [domain]/[user]:[password]@[target]```\n\n**Connect with NTLM hash**\n\n```impacket-smbclient -hashes :[hash] [domain]/[user]@[target]```\n\n**Use Kerberos authentication**\n\n```impacket-smbclient -k [domain]/[user]@[target]```"],["SYNOPSIS","**impacket-smbclient** [_options_] _target_"],["PARAMETERS","**-hashes** _lm:nt_\n> Use NTLM hashes.\n\n**-k**\n> Use Kerberos authentication.\n\n**-dc-ip** _ip_\n> Domain controller IP.\n\n**-target-ip** _ip_\n> Target IP (if different from hostname)."],["DESCRIPTION","**impacket-smbclient** is a generic SMB client for listing shares and files, uploading, downloading, renaming, and managing directories on Windows systems. Part of the Impacket toolkit. Supports password, NTLM hash, and Kerberos authentication."],["INSTALL","```pacman: sudo pacman -S impacket```"],["SEE ALSO","[smbclient](/man/smbclient)(1), [impacket-psexec](/man/impacket-psexec)(1)"]],"e":["smbclient","impacket-psexec"],"x":"impacket-smbclient [domain]/[user]:[password]@[target]"},"lastb":{"n":"lastb","t":"same as last, but shows failed login attempts as recorded in /var/log/btmp","s":[["TAGLINE","same as last, but shows failed login attempts as recorded in /var/log/btmp"],["TLDR","List **failed login attempts**\n\n```sudo lastb```\n\nList failed logins **since** a date\n\n```sudo lastb -s [YYYY-MM-DD]```\n\nList failed logins **until** a date\n\n```sudo lastb -t [YYYY-MM-DD]```\n\nShow users who were **present** at a specific time\n\n```sudo lastb -p [YYYY-MM-DD HH:MM]```\n\nResolve IPs to **hostnames**\n\n```sudo lastb -d```\n\nLimit output to a specific **number of entries**\n\n```sudo lastb -n [10]```\n\nShow **full timestamps** including year and seconds\n\n```sudo lastb -F```\n\nRead from a **specific btmp file**\n\n```sudo lastb -f [/var/log/btmp.1]```"],["SYNOPSIS","**lastb** [_options_] [_username_...] [_tty_...]"],["PARAMETERS","**-s**, **--since** _TIME_\n> Display records since specified time\n\n**-t**, **--until** _TIME_\n> Display records until specified time\n\n**-p**, **--present** _TIME_\n> Display users who were present at specified time\n\n**-d**, **--dns**\n> Translate IP addresses to hostnames\n\n**-n**, **--limit** _NUM_\n> Limit output to NUM records\n\n**-f**, **--file** _FILE_\n> Read from specified file instead of /var/log/btmp\n\n**-F**, **--fulltimes**\n> Print full login and logout times and dates\n\n**-i**, **--ip**\n> Display IP addresses instead of hostnames\n\n**-R**, **--nohostname**\n> Suppress display of the hostname field\n\n**-w**, **--fullnames**\n> Display full user and domain names\n\n**-x**, **--system**\n> Display system shutdown entries and run level changes\n\n**--time-format** _FORMAT_\n> Output timestamp format: notime, short, full, or iso"],["DESCRIPTION","**lastb** is the same as last, but shows failed login attempts as recorded in /var/log/btmp. It is useful for security auditing to identify brute force attacks or unauthorized access attempts.\n\nThe output shows username attempted, terminal, source address, and timestamp of each failed login."],["CAVEATS","Requires root privileges to read /var/log/btmp. The btmp file can grow large under attack; log rotation is recommended. File may not exist if never written to."],["HISTORY","lastb provides security auditing for failed logins, complementing the last command. The btmp file format mirrors wtmp but records failures instead of successful logins."],["INSTALL","```apk: sudo apk add util-linux-login```"],["SEE ALSO","[last](/man/last)(1), [lastlog](/man/lastlog)(8), [faillog](/man/faillog)(8), [utmpdump](/man/utmpdump)(1), [login](/man/login)(1)"]],"e":["last","lastlog","faillog","utmpdump","login"],"x":"sudo lastb"},"lmms":{"n":"lmms","t":"digital audio workstation","s":[["TAGLINE","digital audio workstation"],["TLDR","**Start LMMS**\n\n```lmms```\n\n**Open project**\n\n```lmms [project.mmp]```\n\n**Render to audio**\n\n```lmms -r [project.mmp] -o [output.wav]```\n\n**Render to MP3**\n\n```lmms -r [project.mmp] -o [output.mp3] -f mp3```\n\n**Import MIDI**\n\n```lmms --import [file.mid]```\n\n**Headless render**\n\n```lmms -r [project.mmp] -o [output.wav] --loop```"],["SYNOPSIS","**lmms** [_options_] [_file_]"],["PARAMETERS","_FILE_\n> Project file to open.\n\n**-r** _FILE_\n> Render project to audio.\n\n**-o** _FILE_\n> Output file for rendering.\n\n**-f** _FORMAT_\n> Output format (wav, ogg, mp3).\n\n**--import** _FILE_\n> Import MIDI file.\n\n**--help**\n> Display help information."],["DESCRIPTION","**lmms** is a digital audio workstation. It creates music with virtual instruments, samples, and effects.\n\nThe tool provides piano roll, step sequencer, and automation. It supports VST and LADSPA plugins."],["CAVEATS","GUI-based primarily. Plugin support varies by platform. Resource intensive."],["HISTORY","LMMS (Linux MultiMedia Studio) was created to provide a free, cross-platform alternative to commercial DAWs like FL Studio."],["INSTALL","```apt: sudo apt install lmms```\n\n```dnf: sudo dnf install lmms```\n\n```pacman: sudo pacman -S lmms```\n\n```apk: sudo apk add lmms```\n\n```zypper: sudo zypper install lmms```\n\n```nix: nix profile install nixpkgs#lmms```"],["SEE ALSO","[ardour](/man/ardour)(1), [audacity](/man/audacity)(1)"]],"e":["ardour","audacity"],"x":"lmms"},"msgcat":{"n":"msgcat","t":"concatenates and merges gettext PO files","s":[["TAGLINE","concatenates and merges gettext PO files"],["TLDR","**Concatenate PO files**\n\n```msgcat [file1.po] [file2.po] -o [combined.po]```\n\n**Merge with unique messages**\n\n```msgcat --use-first [file1.po] [file2.po] -o [output.po]```\n\n**Sort messages**\n\n```msgcat --sort-output [input.po] -o [sorted.po]```\n\n**Remove duplicates**\n\n```msgcat --unique [input.po] -o [output.po]```"],["SYNOPSIS","**msgcat** [_options_] _inputfile_..."],["PARAMETERS","**-o** _file_\n> Output file.\n\n**--use-first**\n> Use first translation for duplicates.\n\n**--sort-output**\n> Sort messages alphabetically.\n\n**--unique**\n> Remove duplicate messages.\n\n**-t** _encoding_\n> Output encoding."],["DESCRIPTION","**msgcat** concatenates and merges gettext PO files. Combines multiple translation files, handles duplicates, and normalizes output. Part of GNU gettext for managing internationalization catalogs."],["INSTALL","```apt: sudo apt install gettext```\n\n```dnf: sudo dnf install gettext```\n\n```pacman: sudo pacman -S gettext```\n\n```apk: sudo apk add gettext```\n\n```brew: brew install gettext```\n\n```nix: nix profile install nixpkgs#gettext```"],["SEE ALSO","[msgattrib](/man/msgattrib)(1), [msgfmt](/man/msgfmt)(1), [msgunfmt](/man/msgunfmt)(1)"]],"e":["msgattrib","msgfmt","msgunfmt"],"x":"msgcat [file1.po] [file2.po] -o [combined.po]"},"multi-gitter":{"n":"multi-gitter","t":"Run scripted changes across many Git repositories","s":[["TAGLINE","Run scripted changes across many Git repositories"],["TLDR","**Run a script** and open PRs on matching repos\n\n```multi-gitter run [./script.sh] --org [my-org] --branch [chore/update] --pr-title \"[title]\"```\n\n**Merge** open multi-gitter PRs\n\n```multi-gitter merge --org [my-org]```\n\n**Status** of previous runs\n\n```multi-gitter status --org [my-org]```\n\n**Close** PRs\n\n```multi-gitter close --org [my-org]```"],["SYNOPSIS","**multi-gitter** *command* [*options*]"],["DESCRIPTION","**multi-gitter** clones many repositories (by org, user, or explicit list), runs a script in each, commits changes, pushes a branch, and opens pull requests. Ideal for fleet-wide dependency bumps, renames, or CI file updates across dozens of repos. Supports GitHub and GitLab with tokens."],["PARAMETERS","**run** *script*\n\n> Execute the script in each repo; create commits/PRs when files change.\n\n**merge** / **close** / **status** / **print**\n\n> Manage resulting pull requests.\n\n**--org** / **--user** / **--repo**\n\n> Repository selection.\n\n**--branch** / **--pr-title** / **--pr-body**\n\n> PR metadata.\n\n**--dry-run**\n\n> Preview without pushing.\n\nAuth via **GITHUB_TOKEN** or provider flags—see **multi-gitter --help**."],["CAVEATS","Scripts run with your credentials; audit them carefully. Rate limits and branch protection can block merges. Clone disk usage grows with org size."],["INSTALL","```brew: brew install multi-gitter```"],["SEE ALSO","[git](/man/git)(1), [gh](/man/gh)(1), [git-xargs](/man/git-xargs)(1)"],["RESOURCES","```[Source code](https://github.com/lindell/multi-gitter)```"]],"e":["git","gh","git-xargs"],"x":"multi-gitter run [./script.sh] --org [my-org] --branch [chore/update] --pr-title \"[title]\""},"ntpdate":{"n":"ntpdate","t":"sets the system date and time by querying NTP servers","s":[["TAGLINE","sets the system date and time by querying NTP servers"],["TLDR","**Query** a time server without changing the clock\n\n```ntpdate -q [host]```\n\nSet the clock from a server (**sync**)\n\n```sudo ntpdate [host]```\n\nUse an **unprivileged** source port (firewalls)\n\n```sudo ntpdate -u [host]```\n\n**Step** the clock immediately instead of slewing\n\n```sudo ntpdate -b [host]```"],["SYNOPSIS","**ntpdate** [_options_] _server_..."],["PARAMETERS","**-q**\n> Query only; don't set the clock\n\n**-u**\n> Use unprivileged port for outgoing packets (useful for firewalls)\n\n**-b**\n> Force time to be stepped (abrupt change) rather than slewed (gradual)\n\n**-s**\n> Log to syslog instead of stdout\n\n**-v**\n> Verbose output\n\n**-d**\n> Debug mode; don't change time, show what would happen\n\n**-t _timeout_**\n> Timeout for server response in seconds"],["DESCRIPTION","**ntpdate** sets the system date and time by querying NTP (Network Time Protocol) servers. It performs a one-time synchronization, unlike **ntpd** which runs continuously.\n\nBy default, if the time offset is small, ntpdate gradually adjusts (slews) the clock to avoid disrupting running programs. For large offsets, it steps the clock immediately. The **-b** flag forces stepping regardless of offset size.\n\nMultiple servers can be specified; ntpdate queries all and uses the best response."],["CAVEATS","**Deprecated** in favor of **ntpd -q** or **sntp**. Abrupt time changes (**-b**) can confuse applications expecting monotonic time. Requires root privileges to set the system clock. Not suitable for continuous synchronization; use ntpd or chronyd instead."],["HISTORY","ntpdate was part of the original NTP distribution but has been deprecated since NTP 4.2.8. Modern systems typically use **systemd-timesyncd**, **chrony**, or **ntpd** for time synchronization."],["INSTALL","```apt: sudo apt install ntpsec-ntpdate```"],["SEE ALSO","[ntpd](/man/ntpd)(8), [chronyc](/man/chronyc)(1), [timedatectl](/man/timedatectl)(1), [sntp](/man/sntp)(8)"]],"e":["ntpd","chronyc","timedatectl","sntp"],"x":"ntpdate -q [host]"},"obabel":{"n":"obabel","t":"open Babel command-line tool for converting between molecular file formats","s":[["TAGLINE","open Babel command-line tool for converting between molecular file formats"],["TLDR","Convert a **.mol** file to XYZ coordinates\n\n```obabel [path/to/file.mol] -O [path/to/output_file.xyz]```\n\nConvert a **SMILES string** to a 500x500 picture\n\n```obabel -:\"[SMILES]\" -O [path/to/output_file.png] -xp 500```\n\nConvert a file of SMILES string to separate **3D .mol** files\n\n```obabel [path/to/file.smi] -O [path/to/output_file.mol] --gen3D -m```\n\nRender **multiple inputs** into one picture\n\n```obabel [path/to/file1 path/to/file2 ...] -O [path/to/output_file.png]```"],["SYNOPSIS","**obabel** [_OPTIONS_] [**-i** _input-type_] _infile_ [**-o** _output-type_] **-O** _outfile_\n\n**obabel** **-:**\"_SMILES-string_\" [_OPTIONS_] **-O** _outfile_"],["PARAMETERS","**-i** _format_\n> Specify input format explicitly (e.g., **-imol**, **-ismi**)\n\n**-o** _format_\n> Specify output format explicitly (e.g., **-osmi**, **-opng**)\n\n**-O** _outfile_\n> Specify output file path\n\n**-:**\"_SMILES_\"\n> Use SMILES string as input instead of a file\n\n**-m**\n> Produce multiple output files (one per molecule)\n\n**-f** _#_\n> Start import at molecule number specified\n\n**-l** _#_\n> End import at molecule number specified\n\n**--gen3D**\n> Generate 3D coordinates for molecules\n\n**-e**\n> Continue with next object after error\n\n**-z**\n> Compress output with gzip\n\n**-H**\n> Display help; use **-H**_format_ for format-specific help\n\n**-V**\n> Display version number\n\n**-a**_option_\n> Input format-specific option\n\n**-x**_option_\n> Output format-specific option"],["DESCRIPTION","**obabel** is the Open Babel command-line tool for converting between molecular file formats used in chemistry and computational modeling. It supports over 100 chemical data formats including SDF, MOL, PDB, SMILES, CML, and image formats for visualization.\n\nBeyond simple conversion, obabel can filter molecules by properties, generate 3D coordinates, perform energy minimization using forcefields (MMFF94, UFF, GAFF), and manipulate molecular data. It handles batch processing with wildcard patterns and can split multi-molecule files into individual outputs."],["CAVEATS","Format detection relies on file extensions; use **-i** and **-o** flags explicitly when extensions are ambiguous. Some conversions may lose data if the target format lacks equivalent fields. 3D coordinate generation (**--gen3D**) can be slow for large molecules."],["HISTORY","Open Babel originated from the **OELib/OBabel** project started in **1998**. The **obabel** command replaced the older **babel** command around version 2.3 (2011), offering improved option handling following Unix conventions. Open Babel 3.0 was released in **2019** with enhanced stereochemistry support."],["INSTALL","```apt: sudo apt install openbabel```\n\n```dnf: sudo dnf install openbabel```\n\n```pacman: sudo pacman -S openbabel```\n\n```zypper: sudo zypper install openbabel```\n\n```nix: nix profile install nixpkgs#openbabel```"],["SEE ALSO","[rdkit](/man/rdkit)(1), [pymol](/man/pymol)(1)"]],"e":["rdkit","pymol"],"x":"obabel [path/to/file.mol] -O [path/to/output_file.xyz]"},"ocrmypdf":{"n":"ocrmypdf","t":"adds an OCR text layer to scanned PDFs, making them searchable and selectable","s":[["TAGLINE","adds an OCR text layer to scanned PDFs, making them searchable and selectable"],["TLDR","**Add OCR layer to PDF**\n\n```ocrmypdf [input.pdf] [output.pdf]```\n\n**OCR and correct skew**\n\n```ocrmypdf --deskew [input.pdf] [output.pdf]```\n\n**OCR and clean background**\n\n```ocrmypdf --clean [input.pdf] [output.pdf]```\n\n**Specify language**\n\n```ocrmypdf -l [deu] [input.pdf] [output.pdf]```\n\n**Multiple languages**\n\n```ocrmypdf -l [eng+fra] [input.pdf] [output.pdf]```\n\n**Force OCR even if text exists**\n\n```ocrmypdf --force-ocr [input.pdf] [output.pdf]```\n\n**Skip pages with text**\n\n```ocrmypdf --skip-text [input.pdf] [output.pdf]```\n\n**Optimize and reduce size**\n\n```ocrmypdf --optimize [3] [input.pdf] [output.pdf]```"],["SYNOPSIS","**ocrmypdf** [_-l lang_] [_--deskew_] [_--clean_] [_--force-ocr_] [_--optimize level_] [_options_] _input.pdf_ _output.pdf_"],["PARAMETERS","**-l** _LANG_, **--language** _LANG_\n> OCR language (Tesseract language codes).\n\n**--deskew**\n> Correct page skew before OCR.\n\n**--clean**\n> Clean page background before OCR.\n\n**--clean-final**\n> Clean and keep cleaned image in output.\n\n**--rotate-pages**\n> Rotate pages to correct orientation.\n\n**--remove-background**\n> Remove background from pages.\n\n**--force-ocr**\n> OCR all pages, replacing existing text.\n\n**--skip-text**\n> Skip pages that already have text.\n\n**--redo-ocr**\n> Redo OCR on pages with existing text.\n\n**--optimize** _LEVEL_\n> Optimize output (0=off, 1-3 increasing).\n\n**--output-type** _TYPE_\n> Output type: pdf, pdfa, pdfa-1, pdfa-2, pdfa-3.\n\n**--pdfa-image-compression** _TYPE_\n> Compression: jpeg, lossless.\n\n**-j** _NUM_, **--jobs** _NUM_\n> Number of parallel jobs.\n\n**--image-dpi** _DPI_\n> DPI for images without metadata.\n\n**-q**, **--quiet**\n> Suppress output.\n\n**-v**, **--verbose** [_LEVEL_]\n> Verbose output (0-2).\n\n**--sidecar** _FILE_\n> Write OCR text to sidecar file."],["DESCRIPTION","**ocrmypdf** adds an OCR text layer to scanned PDFs, making them searchable and selectable. It uses Tesseract OCR and outputs PDF/A for archival quality by default.\n\nThe tool preserves the original visual appearance while adding invisible text behind the scanned images. This means the file looks identical but text can be copied, searched, and indexed.\n\nImage preprocessing improves OCR accuracy: deskew corrects tilted scans, clean removes noise and artifacts, and rotate-pages fixes orientation. These can significantly improve results on poor-quality scans.\n\nMultiple languages can be combined (eng+fra+deu). Language packs must be installed for Tesseract. The tool detects existing text to avoid double-processing unless forced.\n\nOptimization levels reduce file size through image recompression. Level 3 uses aggressive JBIG2 compression suitable for archival. PDF/A output ensures long-term readability.\n\nParallel processing speeds up multi-page documents. Progress is shown by default. Sidecar output extracts just the text for external processing."],["CAVEATS","OCR accuracy depends on scan quality. Very low resolution or heavily compressed images may produce poor results. Language packs must be installed separately. Some complex layouts may not OCR well. PDF/A conversion may strip some features. Processing large PDFs requires significant memory."],["HISTORY","**ocrmypdf** was created by **James R. Barlow** starting around **2013**. It wraps Tesseract OCR with intelligent PDF handling, image preprocessing, and PDF/A output. The project addressed the need for a complete solution to make scanned PDFs searchable, automating what was previously a manual multi-step process."],["INSTALL","```dnf: sudo dnf install ocrmypdf```\n\n```apk: sudo apk add ocrmypdf```\n\n```brew: brew install ocrmypdf```\n\n```nix: nix profile install nixpkgs#ocrmypdf```"],["SEE ALSO","[tesseract](/man/tesseract)(1), [pdftk](/man/pdftk)(1), [pdfimages](/man/pdfimages)(1), [img2pdf](/man/img2pdf)(1)"]],"e":["tesseract","pdftk","pdfimages","img2pdf"],"x":"ocrmypdf [input.pdf] [output.pdf]"},"pacman":{"n":"pacman","t":"package manager for Arch Linux","s":[["TAGLINE","package manager for Arch Linux"],["TLDR","**Synchronize** and update all packages\n\n```sudo pacman -Syu```\n\n**Install** a package\n\n```sudo pacman -S package```\n\n**Remove** a package and its dependencies\n\n```sudo pacman -Rs package```\n\n**Search** the package database\n\n```pacman -Ss \"search_pattern\"```\n\nSearch for packages containing a specific **file**\n\n```pacman -F \"file_name\"```\n\nList **explicitly installed** packages\n\n```pacman -Qe```\n\nList **orphan** packages\n\n```pacman -Qtdq```\n\n**Empty** the package cache\n\n```sudo pacman -Scc```"],["SYNOPSIS","**pacman** _<operation>_ [_options_] [_targets_]"],["DESCRIPTION","**pacman** is the package manager for Arch Linux. It combines a simple binary package format with an easy-to-use build system, tracking installed packages with dependency support, package groups, and synchronization with remote repositories."],["OPERATIONS","**-S, --sync**\n> Synchronize packages from remote repositories\n\n**-R, --remove**\n> Remove packages from the system\n\n**-Q, --query**\n> Query the local package database\n\n**-U, --upgrade**\n> Upgrade or add packages from local files or URLs\n\n**-D, --database**\n> Operate on the package database\n\n**-F, --files**\n> Query the files database\n\n**-T, --deptest**\n> Check if dependencies are satisfied"],["COMMON OPTIONS","**-y, --refresh**\n> Download fresh package databases from servers\n\n**-u, --sysupgrade**\n> Upgrade all outdated packages\n\n**-s, --search**\n> Search for packages matching a pattern\n\n**-i, --info**\n> Display package information\n\n**-l, --list**\n> List files owned by a package\n\n**-c, --cascade**\n> Remove packages and all dependent packages\n\n**-n, --nosave**\n> Remove configuration files during removal\n\n**-w, --downloadonly**\n> Download packages without installing\n\n**--noconfirm**\n> Bypass all confirmation prompts\n\n**--needed**\n> Skip reinstalling up-to-date packages\n\n**-d, --nodeps**\n> Skip dependency checks\n\n**-q, --quiet**\n> Produce less output"],["CAVEATS","Always run **-Syu** instead of just **-Sy** before installing packages to avoid partial upgrades which can break the system. Configuration files are saved with .pacsave extension when removing packages unless **-n** is used."],["HISTORY","**pacman** was created by Judd Vinet for Arch Linux and first released in 2002. It is the front-end to libalpm (Arch Linux Package Management library), allowing alternative front-ends like pamac or octopi."],["INSTALL","```apt: sudo apt install pacman-package-manager```\n\n```dnf: sudo dnf install pacman```\n\n```pacman: sudo pacman -S pacman```\n\n```apk: sudo apk add pacman```\n\n```nix: nix profile install nixpkgs#pacman```"],["SEE ALSO","[pacman.conf](/man/pacman.conf)(5), [makepkg](/man/makepkg)(8), [yay](/man/yay)(8), [paru](/man/paru)(8)"]],"e":["pacman.conf","makepkg","yay","paru"],"x":"sudo pacman -Syu"},"pamcrater":{"n":"pamcrater","t":"generates synthetic terrain with impact craters, useful for creating planetary","s":[["TAGLINE","generates synthetic terrain with impact craters, useful for creating planetary"],["TLDR","**Generate terrain with craters**\n\n```pamcrater -width [512] -height [512] > [output.pam]```\n\n**Generate with specific crater count**\n\n```pamcrater -number [1000] -width [256] -height [256] > [output.pam]```"],["SYNOPSIS","**pamcrater** [_options_]"],["PARAMETERS","**-width** _n_\n> Output width in pixels.\n\n**-height** _n_\n> Output height in pixels.\n\n**-number** _n_\n> Number of craters to generate.\n\n**-randomseed** _n_\n> Random seed for reproducibility."],["DESCRIPTION","**pamcrater** generates synthetic terrain with impact craters, useful for creating planetary surface textures or heightmaps. Output is grayscale elevation data. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pgmcrater](/man/pgmcrater)(1)"]],"e":["pgmcrater"],"x":"pamcrater -width [512] -height [512] > [output.pam]"},"pct-resize":{"n":"pct-resize","t":"changes the size of a Proxmox LXC container's storage volume","s":[["TAGLINE","changes the size of a Proxmox LXC container's storage volume"],["TLDR","**Resize** to absolute size\n\n```pct resize 100 rootfs 20G```\n\n**Add** to existing size\n\n```pct resize 100 rootfs +10G```"],["SYNOPSIS","**pct resize** _vmid_ _disk_ _size_"],["DESCRIPTION","**pct resize** changes the size of a Proxmox LXC container's storage volume. The filesystem inside the container is automatically resized along with the underlying storage."],["PARAMETERS","**vmid**\n> The numeric ID of the container\n\n**disk**\n> The disk to resize (rootfs or mpN for mount points)\n\n**size**\n> New size (absolute like 20G) or relative (like +10G)"],["CAVEATS","Only size increases are supported for most storage types. Shrinking volumes may cause data loss and is not supported for all storage backends. The container can be running during resize."],["HISTORY","**pct resize** is part of the **Proxmox VE** virtualization platform for managing LXC containers."],["SEE ALSO","[pct](/man/pct)(1), [pct-set](/man/pct-set)(1)"]],"e":["pct","pct-set"],"x":"pct resize 100 rootfs 20G"},"podman-image-pull":{"n":"podman-image-pull","t":"Download container images from registries","s":[["TAGLINE","Download container images from registries"],["TLDR","**Pull an image** from Docker Hub\n\n```podman image pull [nginx]```\n\n**Pull a specific tag** of an image\n\n```podman image pull [nginx]:[1.25]```\n\n**Pull from a specific registry**\n\n```podman image pull [quay.io/podman/hello]```\n\n**Pull all tags** of an image\n\n```podman image pull --all-tags [alpine]```\n\n**Pull without verifying TLS** (insecure)\n\n```podman image pull --tls-verify=false [registry.local:5000/myimage]```\n\n**Pull with authentication**\n\n```podman image pull --creds [user]:[password] [registry.example.com/image]```\n\n**Pull and specify architecture**\n\n```podman image pull --arch [arm64] [nginx]```"],["SYNOPSIS","**podman image pull** [_options_] _image_"],["PARAMETERS","**--all-tags**\n> Pull all tagged images from the repository\n\n**--arch** _ARCH_\n> Override the architecture of the image to pull\n\n**--authfile** _path_\n> Path to authentication file\n\n**--creds** _[username[:password]]_\n> Credentials for registry authentication\n\n**--disable-content-trust**\n> Skip image verification (default: true)\n\n**--os** _OS_\n> Override the OS of the image to pull\n\n**--platform** _PLATFORM_\n> Specify platform (os/arch) to pull\n\n**--quiet**, **-q**\n> Suppress output information\n\n**--tls-verify**\n> Require HTTPS and verify certificates (default: true)\n\n**--variant** _VARIANT_\n> Override the variant of the image"],["DESCRIPTION","**podman image pull** downloads container images from registries to local storage. It supports multiple registries including Docker Hub, Quay.io, and private registries. Images are stored locally and can be used to create containers.\n\nBy default, Podman searches registries defined in registries.conf. Fully qualified image names (including registry) bypass registry search. The command supports pulling by tag, digest, or retrieving all tags at once."],["CAVEATS","Large images may take significant time and bandwidth to download. Pulling from insecure registries requires explicit --tls-verify=false flag. Authentication credentials on command line may be visible in process listings; use --authfile for better security."],["HISTORY","**Podman** was developed by **Red Hat** as a daemonless container engine, first released in **2018**. The image pull command mirrors Docker's functionality while adding rootless container support. Podman has become a standard container tool in RHEL/Fedora ecosystems."],["INSTALL","```apt: sudo apt install podman```\n\n```dnf: sudo dnf install podman```\n\n```pacman: sudo pacman -S podman```\n\n```apk: sudo apk add podman```\n\n```zypper: sudo zypper install podman```\n\n```brew: brew install podman```\n\n```nix: nix profile install nixpkgs#podman```"],["SEE ALSO","[podman](/man/podman)(1), [podman-pull](/man/podman-pull)(1), [podman-images](/man/podman-images)(1), [podman-run](/man/podman-run)(1), [docker-pull](/man/docker-pull)(1)"]],"e":["podman","podman-pull","podman-images","podman-run","docker-pull"],"x":"podman image pull [nginx]"},"polybar":{"n":"polybar","t":"Customizable status bar for X11 window managers","s":[["TAGLINE","Customizable status bar for X11 window managers"],["TLDR","**Start polybar with default bar**\n\n```polybar```\n\n**Start specific bar**\n\n```polybar [bar_name]```\n\n**Start with config file**\n\n```polybar -c [~/.config/polybar/config.ini] [bar_name]```\n\n**List available bars**\n\n```polybar -m```\n\n**Reload configuration**\n\n```polybar-msg cmd restart```\n\n**Send action to module**\n\n```polybar-msg action [module_name] [action]```\n\n**Check configuration**\n\n```polybar -c [config.ini] --check```"],["SYNOPSIS","**polybar** [_-c config_] [_-m_] [_--check_] [_bar_name_]"],["PARAMETERS","**-c**, **--config** _FILE_\n> Path to configuration file.\n\n**-m**, **--list-monitors**\n> List available monitors/outputs.\n\n**-r**, **--reload**\n> Reload when config changes.\n\n**-q**, **--quiet**\n> Suppress output.\n\n**-l**, **--log** _LEVEL_\n> Log level: error, warning, info, trace.\n\n**--check**\n> Verify configuration syntax.\n\n**-w**, **--print-wmname**\n> Print WM name and exit."],["MODULES","**internal/battery**\n> Battery status and capacity.\n\n**internal/network**\n> Network connection info.\n\n**internal/cpu**\n> CPU usage.\n\n**internal/memory**\n> Memory usage.\n\n**internal/date**\n> Date and time.\n\n**internal/volume** / **internal/pulseaudio**\n> Audio volume control.\n\n**internal/xworkspaces**\n> Workspace indicator.\n\n**internal/temperature**\n> System temperature.\n\n**custom/script**\n> Custom script output.\n\n**custom/ipc**\n> Inter-process communication."],["DESCRIPTION","**Polybar** is a fast, customizable status bar for X11 window managers. It displays system information, workspace indicators, and custom content through a modular configuration.\n\nConfiguration uses INI format with sections for bars and modules. Bars define appearance (colors, fonts, position) and which modules to include. Modules provide content - built-in for system info, custom for scripts.\n\nThe bar is divided into left, center, and right sections. Modules can display text, icons (via icon fonts), progress bars, and clickable areas. Formatting uses tags for colors, fonts, and spacing.\n\nCustom script modules execute external commands and display output. IPC modules enable real-time updates from external programs via polybar-msg.\n\nMultiple bars can run simultaneously on different monitors. The -m flag lists available outputs for multi-monitor configuration."],["CONFIGURATION","**~/.config/polybar/config.ini**\n> Main configuration file in INI format defining bars, modules, colors, and fonts.\n\n**~/.config/polybar/launch.sh**\n> Common launch script for starting polybar with window manager."],["CAVEATS","X11 only - no native Wayland support (use Waybar). Requires icon fonts for symbols. Complex configurations can be verbose. Some window managers need specific settings. Reloading loses some state. CPU usage increases with many script modules."],["HISTORY","**Polybar** was created by **Michael Carlberg** around **2016** as a rewrite of lemonbar with easier configuration. It aimed to provide a powerful status bar without requiring shell scripting knowledge. Written in C++, it became popular in the r/unixporn community for desktop customization."],["INSTALL","```apt: sudo apt install polybar```\n\n```dnf: sudo dnf install polybar```\n\n```pacman: sudo pacman -S polybar```\n\n```apk: sudo apk add polybar```\n\n```zypper: sudo zypper install polybar```\n\n```nix: nix profile install nixpkgs#polybar```"],["SEE ALSO","[polybar-msg](/man/polybar-msg)(1), [waybar](/man/waybar)(1), [i3status](/man/i3status)(1), [lemonbar](/man/lemonbar)(1), [dunst](/man/dunst)(1)"]],"e":["polybar-msg","waybar","i3status","lemonbar","dunst"],"x":"polybar"},"prename":{"n":"prename","t":"Rename files using Perl expressions","s":[["TAGLINE","Rename files using Perl expressions"],["TLDR","**Rename with Perl expression**\n\n```prename 's/old/new/' [*.txt]```\n\n**Dry run**\n\n```prename -n 's/pattern/replacement/' [files]```\n\n**Lowercase filenames**\n\n```prename 'y/A-Z/a-z/' [*]```\n\n**Remove spaces**\n\n```prename 's/ /_/g' [*]```\n\n**Add prefix**\n\n```prename 's/^/prefix_/' [*]```\n\n**Add suffix before extension**\n\n```prename 's/(\\.[^.]+)$/_suffix$1/' [*]```\n\n**Verbose output**\n\n```prename -v 's/old/new/' [files]```"],["SYNOPSIS","**prename** [_-n_] [_-v_] [_-f_] _expression_ _files_"],["PARAMETERS","**-n**, **--no-act**\n> Dry run, show changes.\n\n**-v**, **--verbose**\n> Show renames.\n\n**-f**, **--force**\n> Overwrite existing files.\n\n**-0**, **--null**\n> Null-separated input.\n\n**-e** _EXPR_\n> Expression to apply.\n\n**-E** _EXPR_\n> Like -e but without warnings."],["DESCRIPTION","**prename** (Perl rename) renames files using Perl expressions. It provides powerful pattern-based renaming.\n\nThe s/// substitution operator replaces text. Global flag g replaces all occurrences.\n\nTransliteration y/// transforms character sets. Useful for case changes.\n\nPerl code can use $_ for filename. Complex transformations are possible.\n\nDry run shows what would happen. Always test patterns before applying.\n\nMultiple expressions chain transformations. Each -e adds another operation."],["CAVEATS","Perl syntax required. May conflict with util-linux rename. Use full path if needed."],["HISTORY","**prename** was written by **Larry Wall** (creator of Perl) and is often distributed as **rename** with Perl. It's distinct from the simpler util-linux rename command."],["INSTALL","```dnf: sudo dnf install prename```"],["SEE ALSO","[rename](/man/rename)(1), [mv](/man/mv)(1), [mmv](/man/mmv)(1), [perl](/man/perl)(1)"]],"e":["rename","mv","mmv","perl"],"x":"prename 's/old/new/' [*.txt]"},"pueue-status":{"n":"pueue-status","t":"Display pueue task queue state","s":[["TAGLINE","Display pueue task queue state"],["TLDR","**Show queue status**\n\n```pueue status```\n\n**Show specific group**\n\n```pueue status -g [group]```\n\n**Show in JSON format**\n\n```pueue status --json```\n\n**Show full task details**\n\n```pueue status --full```"],["SYNOPSIS","**pueue** **status** [_options_]"],["PARAMETERS","**-g**, **--group** _name_\n> Show specific group.\n\n**--json**\n> Output as JSON.\n\n**--full**\n> Display full task information with expanded output."],["DESCRIPTION","**pueue status** displays the current state of all tasks in the queue. Shows queued, running, completed, and failed tasks with their task IDs, commands, status, and execution times."],["INSTALL","```pacman: sudo pacman -S pueue```\n\n```apk: sudo apk add pueue```\n\n```zypper: sudo zypper install pueue```\n\n```brew: brew install pueue```\n\n```nix: nix profile install nixpkgs#pueue```"],["SEE ALSO","[pueue](/man/pueue)(1), [pueue-add](/man/pueue-add)(1), [pueue-log](/man/pueue-log)(1), [pueue-clean](/man/pueue-clean)(1), [pueue-follow](/man/pueue-follow)(1)"]],"e":["pueue","pueue-add","pueue-log","pueue-clean","pueue-follow"],"x":"pueue status"},"python":{"n":"python","t":"Interpreted general-purpose programming language","s":[["TAGLINE","Interpreted general-purpose programming language"],["TLDR","**Run Python script**\n\n```python [script.py]```\n\n**Start interactive REPL**\n\n```python```\n\n**Execute code**\n\n```python -c \"[print('Hello')]\"```\n\n**Run module**\n\n```python -m [module]```\n\n**Start HTTP server**\n\n```python -m http.server [8080]```\n\n**Check syntax**\n\n```python -m py_compile [script.py]```"],["SYNOPSIS","**python** [_options_] [_script_] [_args_]"],["PARAMETERS","_SCRIPT_\n> Python script to run.\n\n_ARGS_\n> Script arguments.\n\n**-c** _CODE_\n> Execute code string.\n\n**-m** _MODULE_\n> Run library module.\n\n**-i**\n> Interactive after script.\n\n**-O**\n> Optimize bytecode.\n\n**-V**\n> Show version.\n\n**-u**\n> Unbuffered output."],["DESCRIPTION","**python** is the interpreter for the Python programming language, executing scripts or starting an interactive REPL session. It supports procedural, object-oriented, and functional programming styles, and is widely used for scripting, web development, data analysis, automation, and scientific computing.\n\nWhen invoked without arguments, it starts an interactive read-eval-print loop for experimentation. With a script file as argument, it compiles and executes the code. The **-m** flag runs installed modules as scripts, enabling built-in utilities like HTTP servers, JSON formatters, and package management through pip. On many systems, **python** may refer to Python 2; use **python3** explicitly for Python 3."],["CAVEATS","Python 2 vs 3 differences. Use python3 for Python 3."],["HISTORY","Python was created by **Guido van Rossum** in 1991."],["INSTALL","```dnf: sudo dnf install python3```\n\n```pacman: sudo pacman -S python```\n\n```apk: sudo apk add python3```\n\n```brew: brew install python3```\n\n```nix: nix profile install nixpkgs#python3```"],["SEE ALSO","[python3](/man/python3)(1), [pip](/man/pip)(1), [pydoc](/man/pydoc)(1)"]],"e":["python3","pip","pydoc"],"x":"python [script.py]"},"qm-sendkey":{"n":"qm-sendkey","t":"Send key events to a Proxmox VM","s":[["TAGLINE","Send key events to a Proxmox VM"],["TLDR","**Send** a key event to a virtual machine\n\n```qm sendkey [vmid] ctrl-alt-delete```\n\nSend a **single key** to a VM\n\n```qm sendkey [vmid] ret```\n\nSend a key event **ignoring locks** (root only)\n\n```qm sendkey --skiplock [vmid] [key]```"],["SYNOPSIS","**qm sendkey** [_options_] _vmid_ _key_"],["PARAMETERS","_vmid_\n> The unique VM ID (integer between 100 and 999999999).\n\n_key_\n> Key name in QEMU monitor encoding format (e.g. `ctrl-alt-delete`, `ret`, `esc`, `f1`, `meta_l-r`).\n\n**--skiplock** _boolean_\n> Ignore VM locks and force key send. Requires root privileges."],["DESCRIPTION","**qm sendkey** sends a QEMU monitor encoding key event to a running virtual machine. This is useful for sending keyboard input such as `ctrl-alt-delete` or function keys directly to the VM console without needing graphical access.\n\nKey names follow QEMU monitor encoding: alphanumeric characters are written literally, special keys use names like `ret`, `esc`, `tab`, `spc`, `f1`–`f12`, `up`, `down`, `left`, `right`, and modifier combinations use hyphens (e.g. `ctrl-alt-delete`, `meta_l-r`)."],["CAVEATS","Key encoding follows QEMU monitor format, not standard key names. The --skiplock option requires root privileges."],["HISTORY","Part of **Proxmox VE** QEMU/KVM management tools for virtual machine administration."],["INSTALL","```dnf: sudo dnf install qm```"],["SEE ALSO","[qm](/man/qm)(1), [qm-monitor](/man/qm-monitor)(1), [qm-start](/man/qm-start)(1), [qm-stop](/man/qm-stop)(1), [qm-terminal](/man/qm-terminal)(1)"]],"e":["qm","qm-monitor","qm-start","qm-stop","qm-terminal"],"x":"qm sendkey [vmid] ctrl-alt-delete"},"qm-start":{"n":"qm-start","t":"Start a Proxmox virtual machine","s":[["TAGLINE","Start a Proxmox virtual machine"],["TLDR","**Start** a VM\n\n```qm start 100```\n\nSpecify **machine** type\n\n```qm start 100 --machine q35```\n\nStart with **timeout**\n\n```qm start 100 --timeout 60```"],["SYNOPSIS","**qm start** [_OPTIONS_] _vmid_"],["DESCRIPTION","**qm start** starts a QEMU/KVM virtual machine in Proxmox VE. The VM must exist and not already be running. Various startup options can control the emulated machine type, timeout, and other parameters."],["PARAMETERS","**vmid**\n> The numeric ID of the virtual machine\n\n**--machine** _type_\n> Specify the QEMU machine type (e.g., q35, pc, pc-i440fx)\n\n**--timeout** _seconds_\n> Timeout in seconds for the start operation\n\n**--skiplock** _boolean_\n> Skip lock check (root only)\n\n**--stateuri** _uri_\n> URI to restore state from a migration or snapshot"],["CAVEATS","The VM must be in a stopped state to start. If the VM has pending configuration changes, they will be applied on start. Ensure sufficient resources (CPU, memory, storage) are available."],["HISTORY","**qm start** is part of the **Proxmox VE** virtualization platform for managing QEMU/KVM virtual machines."],["INSTALL","```dnf: sudo dnf install qm```"],["SEE ALSO","[qm](/man/qm)(1), [qm-stop](/man/qm-stop)(1), [qm-shutdown](/man/qm-shutdown)(1)"]],"e":["qm","qm-stop","qm-shutdown"],"x":"qm start 100"},"rigorix":{"n":"rigorix","t":"Deterministic coding-agent runtime that compiles intent into executable DAGs","s":[["TAGLINE","Deterministic coding-agent runtime that compiles intent into executable DAGs"],["TLDR","**Initialize a project**\n\n```rigorix init```\n\n**Generate and review a plan** before executing\n\n```rigorix plan \"Add a new API endpoint\"```\n\n**Run a task**\n\n```rigorix run \"Refactor the user module\"```\n\n**Show the dashboard / interactive TUI**\n\n```rigorix```"],["SYNOPSIS","**rigorix** [_options_] <command> [_args_...]\n\nCommon commands: init, plan, run, update"],["DESCRIPTION","Rigorix turns natural-language development tasks into deterministic, auditable Directed Acyclic Graphs (DAGs). Planning is separated from execution: an LLM generates a plan according to templates; Rigorix validates the plan, then executes it under policy, permission, and budget controls.\n\nKey benefits over open-ended agent loops:\n\n- Repeatable execution graphs\n- Explicit quality gates and policy enforcement\n- Full audit trail\n- Safe to run in CI/CD contexts\n\nIt supports Rust, TypeScript, Python and Go codebases (TypeScript is the most mature)."],["INSTALL","```bash\ncargo install --git https://github.com/arman-jalili/rigorix-oss rigorix-cli\n```\n\nOr build from source and place the rigorix binary on your PATH.\n\nSet your LLM API key, e.g.:\n\n```bash\nexport RIGORIX__LLM__API_KEY=\"sk-ant-...\""],["or: export ANTHROPIC_API_KEY=\"sk-ant-...\"","```"],["PARAMETERS","Most configuration lives in the project after `rigorix init`. Command line flags control planning vs. execution, provider selection, and non-interactive mode.\n\n**init**\n> Scaffold project config and templates.\n\n**plan** _task_\n> Classify intent, build a reviewable DAG, and optionally prompt to run it.\n\n**run** _task_\n> Plan and execute a task end to end."],["CAVEATS","Rigorix is intentionally less flexible than free-form chat agents. It excels at repeatable, governed workflows rather than exploratory conversation. An LLM API key is required for planning and generation steps."],["RESOURCES","```[Source code](https://github.com/arman-jalili/rigorix-oss)```"]],"e":[],"x":"rigorix init"},"sadc":{"n":"sadc","t":"System activity data collector backend","s":[["TAGLINE","System activity data collector backend"],["TLDR","Write **10 records** at one-second intervals to a binary file\n\n```sadc 1 10 [/tmp/datafile]```\n\nWrite data to the **default daily data file** (/var/log/sa/saDD)\n\n```sadc 1 10 -```\n\nCollect **all available statistics** including disks and interrupts\n\n```sadc -S ALL 1 10 [/tmp/datafile]```\n\nCollect data with **disk and partition statistics**\n\n```sadc -S XDISK 1 10 [/tmp/datafile]```\n\nAdd a **comment record** to the data file\n\n```sadc -C \"[system reboot]\" [/var/log/sa/sa01]```"],["SYNOPSIS","**sadc** [_-C comment_] [_-D_] [_-F_] [_-L_] [_-V_] [_-S {keyword,...}_] [_interval_] [_count_] [_outfile_]"],["PARAMETERS","**-C** _comment_\n> Write a dummy record containing the specified comment string when interval and count are not specified\n\n**-D**\n> Use saYYYYMMDD instead of saDD as the standard daily data file name\n\n**-F**\n> Force creation of outfile; truncate if it exists with an incompatible format\n\n**-L**\n> Try to get an exclusive lock on the outfile before writing or truncating\n\n**-S** _keyword_\n> Specify optional activities to collect: INT (interrupts), DISK (block devices), XDISK (partitions/filesystems), SNMP, IPV6, POWER, ALL, XALL\n\n**-V**\n> Print version number and exit"],["DESCRIPTION","**sadc** (System Activity Data Collector) is the backend data collector for the **sar** command, part of the sysstat package. It samples system performance data at specified intervals and writes it in binary format to an output file.\n\nThe collector gathers metrics including CPU utilization, memory usage, I/O statistics, network activity, and process information. By default, it collects most kernel data except interrupts and disk statistics, which require explicit **-S** flags.\n\nWhen outfile is set to **-**, sadc writes to the standard daily data file /var/log/sa/saDD. If count is omitted, sadc runs endlessly. The binary output is not human-readable and requires **sar** to interpret."],["CAVEATS","The **/proc** filesystem must be mounted for sadc to function. Available statistics depend on kernel version. sadc is typically invoked by the **sa1** script via cron rather than run directly. Output files from older sadc versions may be incompatible with newer releases."],["HISTORY","sadc is part of the **sysstat** package, originally written by Sebastien Godard. The sysstat tools evolved from earlier Unix system accounting utilities, with sadc providing the binary data collection layer that enables historical performance analysis through sar."],["SEE ALSO","[sar](/man/sar)(1), [iostat](/man/iostat)(1), [mpstat](/man/mpstat)(1), [vmstat](/man/vmstat)(8)"]],"e":["sar","iostat","mpstat","vmstat"],"x":"sadc 1 10 [/tmp/datafile]"},"sed":{"n":"sed","t":"Stream editor for text transformations","s":[["TAGLINE","Stream editor for text transformations"],["TLDR","**Substitute** all occurrences of \"apple\" with \"mango\" on all lines\n\n```[command] | sed 's/apple/mango/g'```\n\n**Replace in-place** in a file (overwriting original)\n\n```sed -i 's/apple/mango/g' [path/to/file]```\n\nRun **multiple substitutions** in one command\n\n```[command] | sed -e 's/apple/mango/g' -e 's/orange/lime/g'```\n\nUse a **custom delimiter** (useful when pattern contains slashes)\n\n```[command] | sed 's#////#____#g'```\n\n**Delete lines** 1 to 5 and backup original with .orig extension\n\n```sed -i.orig '1,5d' [path/to/file]```\n\n**Print only** the first line to stdout\n\n```[command] | sed -n '1p'```\n\n**Insert** a new line at the beginning of a file\n\n```sed -i '1i\\your new line text' [path/to/file]```\n\n**Delete blank lines** from a file\n\n```sed -i '/^[[:space:]]*$/d' [path/to/file]```"],["SYNOPSIS","**sed** [_options_] '_script_' [_input-file_...]"],["PARAMETERS","**-i**[_suffix_], **--in-place**[=_suffix_]\n> Edit files in place; optionally create backup with suffix\n\n**-e** _script_, **--expression=**_script_\n> Add script commands to execute\n\n**-f** _file_, **--file=**_file_\n> Read script from file\n\n**-n**, **--quiet**, **--silent**\n> Suppress automatic printing; only print when p command used\n\n**-r**, **-E**, **--regexp-extended**\n> Use extended regular expressions\n\n**-s**, **--separate**\n> Treat files as separate rather than single stream\n\n**-z**, **--null-data**\n> Separate lines by NUL characters\n\n**--sandbox**\n> Block input/output commands (e/w/r); use for untrusted scripts\n\n**--posix**\n> Disable all GNU extensions for POSIX compliance\n\n**--debug**\n> Annotate program execution"],["DESCRIPTION","**sed** (stream editor) is a powerful text processing tool that performs basic transformations on input streams (files or piped data). It reads input line by line, applies specified editing commands, and writes to standard output.\n\nCommon operations include search and replace (s///), deletion (d), insertion (i), and printing (p). sed uses regular expressions for pattern matching and supports both basic and extended regex syntax. Address ranges (line numbers or patterns) can target specific lines."],["CAVEATS","The **-i** option modifies files directly; always test with output to stdout first or use backup suffix. Behavior of **-i** without suffix varies between GNU sed and BSD sed. Regular expression syntax differs between basic (default) and extended (**-r**) modes."],["HISTORY","Created by Lee McMahon at **Bell Labs in 1973-1974** as part of Unix. Based on the ed editor's scripting capabilities but designed for non-interactive stream processing. GNU sed extended the original with features like in-place editing and extended regex support."],["INSTALL","```apt: sudo apt install sed```\n\n```dnf: sudo dnf install sed```\n\n```pacman: sudo pacman -S sed```\n\n```apk: sudo apk add sed```\n\n```zypper: sudo zypper install sed```"],["SEE ALSO","[awk](/man/awk)(1), [grep](/man/grep)(1), [tr](/man/tr)(1), [ed](/man/ed)(1), [perl](/man/perl)(1)"]],"e":["awk","grep","tr","ed","perl"],"x":"[command] | sed 's/apple/mango/g'"},"sfdk-compiledb":{"n":"sfdk-compiledb","t":"Generate compilation database for Sailfish projects","s":[["TAGLINE","Generate compilation database for Sailfish projects"],["TLDR","**Generate a compile_commands.json** for the current project\n\n```sfdk compiledb```\n\n**Generate compilation database** after a clean build\n\n```sfdk build && sfdk compiledb```"],["SYNOPSIS","**sfdk compiledb** [_options_]"],["DESCRIPTION","**sfdk compiledb** generates a **compile_commands.json** compilation database for IDE integration and tooling. This file is used by clang-based tools, language servers, and IDEs to understand build flags, include paths, and compiler invocations for each source file.\n\nThe command must be run after a successful build so that build logs are available to extract compilation commands from. It is part of the **Sailfish SDK** (sfdk) toolchain."],["CAVEATS","Requires a prior successful build to extract compilation commands. The generated database reflects the last build configuration only."],["SEE ALSO","[sfdk](/man/sfdk)(1), [sfdk-cmake](/man/sfdk-cmake)(1), [sfdk-qmake](/man/sfdk-qmake)(1), [sfdk-deploy](/man/sfdk-deploy)(1)"]],"e":["sfdk","sfdk-cmake","sfdk-qmake","sfdk-deploy"],"x":"sfdk compiledb"},"steamos-session-select":{"n":"steamos-session-select","t":"Switch SteamOS between gaming and desktop","s":[["TAGLINE","Switch SteamOS between gaming and desktop"],["TLDR","Switch to **desktop** mode\n\n```steamos-session-select plasma```\n\nSwitch to **gamemode**\n\n```steamos-session-select```\n\nSwitch to **Wayland** desktop\n\n```steamos-session-select plasma-wayland```\n\n**Persistent** Wayland desktop (boot default)\n\n```steamos-session-select plasma-wayland-persistent```\n\n**Persistent** X11 desktop (boot default)\n\n```steamos-session-select plasma-x11-persistent```"],["SYNOPSIS","**steamos-session-select** [_SESSION_]"],["SESSIONS","**plasma**\n> KDE Plasma desktop mode\n\n**plasma-wayland**\n> KDE Plasma desktop on Wayland\n\n**plasma-x11-persistent**\n> X11 desktop, set as boot default\n\n**plasma-wayland-persistent**\n> Wayland desktop, set as boot default\n\n(no argument)\n> Return to gamemode (removes persistent desktop selection)"],["DESCRIPTION","**steamos-session-select** switches between SteamOS sessions, allowing users to change from the gaming interface to the KDE Plasma desktop environment and back. Sessions with `-persistent` suffix make the device boot to that session by default.\n\nThis is the command-line equivalent of the session switching available in the Steam Deck's power menu."],["CAVEATS","Session changes take effect after the current session ends. Persistent sessions change the boot default until changed again. Gamemode is the default session on fresh installations."],["HISTORY","**steamos-session-select** is part of the **SteamOS** utilities developed by **Valve**. The dual-session design allows the Steam Deck to function as both a gaming console and a full Linux desktop."],["INSTALL","```aur: yay -S steamos-session-select```"],["SEE ALSO","[steamos-devmode](/man/steamos-devmode)(1)"]],"e":["steamos-devmode"],"x":"steamos-session-select plasma"},"systemctl-restart":{"n":"systemctl-restart","t":"Stop and restart systemd units","s":[["TAGLINE","Stop and restart systemd units"],["TLDR","**Restart** a unit\n\n```systemctl restart [unit]```\n\nRestart **multiple** units\n\n```systemctl restart [unit1] [unit2]```\n\nRestart a **user** unit\n\n```systemctl restart [unit] --user```"],["SYNOPSIS","**systemctl** [_OPTIONS_...] **restart** _PATTERN_..."],["PARAMETERS","**--user**\n> Restart user service manager units instead of system units\n\n**--no-block**\n> Do not wait for the operation to complete\n\n**--job-mode=**_MODE_\n> Specify how to deal with already queued jobs"],["DESCRIPTION","**systemctl restart** stops and then starts systemd units. This is useful when a service needs to be fully restarted to pick up configuration changes or recover from an error state.\n\nUnlike **systemctl reload**, restart fully stops the service process and starts a new one. This causes a brief interruption but ensures a clean restart. Can be used on stopped units, but **systemctl start** is safer to avoid accidentally restarting running services."],["CAVEATS","Requires root privileges for system units. Causes service interruption during restart. For configuration reloads without interruption, use **systemctl reload** if the service supports it."],["INSTALL","```apt: sudo apt install systemd```\n\n```dnf: sudo dnf install systemd```\n\n```pacman: sudo pacman -S systemd```\n\n```zypper: sudo zypper install systemd```\n\n```brew: brew install systemd```\n\n```nix: nix profile install nixpkgs#systemd```"],["SEE ALSO","[systemctl](/man/systemctl)(1), [systemctl-start](/man/systemctl-start)(1), [systemctl-reload](/man/systemctl-reload)(1)"]],"e":["systemctl","systemctl-start","systemctl-reload"],"x":"systemctl restart [unit]"},"systemctl-service-log-level":{"n":"systemctl-service-log-level","t":"Get or set per-service log level","s":[["TAGLINE","Get or set per-service log level"],["TLDR","Show service **log level**\n\n```systemctl service-log-level [service]```\n\n**Set** log level\n\n```systemctl service-log-level [service] [emerg|alert|crit|err|warning|notice|info|debug]```"],["SYNOPSIS","**systemctl service-log-level** _SERVICE_ [_LEVEL_]"],["LOG LEVELS","**emerg** (0) - System unusable\n\n**alert** (1) - Immediate action required\n\n**crit** (2) - Critical conditions\n\n**err** (3) - Error conditions\n\n**warning** (4) - Warning conditions\n\n**notice** (5) - Normal but significant\n\n**info** (6) - Informational\n\n**debug** (7) - Debug messages"],["DESCRIPTION","**systemctl service-log-level** gets or sets the runtime log level for a specific service via D-Bus. This allows adjusting service verbosity without restarting the service or editing configuration.\n\nThe service must support the standard D-Bus logging interface for this to work."],["CAVEATS","Only works with services that expose the D-Bus logging interface. Changes are temporary and reset when the service restarts. Not all services support this feature."],["HISTORY","The **service-log-level** subcommand provides per-service logging control, enabling debugging of specific services without affecting system-wide verbosity."],["SEE ALSO","[systemctl-log-level](/man/systemctl-log-level)(1), [systemctl-service-log-target](/man/systemctl-service-log-target)(1), [journalctl](/man/journalctl)(1)"]],"e":["systemctl-log-level","systemctl-service-log-target","journalctl"],"x":"systemctl service-log-level [service]"},"systemd-sysusers":{"n":"systemd-sysusers","t":"Create system users and groups","s":[["TAGLINE","Create system users and groups"],["TLDR","Create users from **all config files**\n\n```systemd-sysusers```\n\nCreate users from **specific file**\n\n```systemd-sysusers path/to/file.conf```\n\n**Dry-run** (show what would be done)\n\n```systemd-sysusers --dry-run```\n\nShow all **config contents**\n\n```systemd-sysusers --cat-config```\n\nSpecify **root directory**\n\n```systemd-sysusers --root=/mnt/system```"],["SYNOPSIS","**systemd-sysusers** [_OPTIONS_...] [_CONFIGFILE_...]"],["DESCRIPTION","**systemd-sysusers** creates system users and groups based on configuration files in /usr/lib/sysusers.d/ and /etc/sysusers.d/. It is typically invoked during system boot or package installation."],["PARAMETERS","**--dry-run**\n> Print what would be done without making changes\n\n**--cat-config**\n> Show the contents of all configuration files\n\n**--root=PATH**\n> Operate on an alternative filesystem root\n\n**--image=PATH**\n> Operate on a disk image\n\n**--no-pager**\n> Do not pipe output into a pager\n\n**-h, --help**\n> Show help message\n\n**--version**\n> Show version information"],["CAVEATS","Configuration files use a declarative format defining users and groups. Users created by systemd-sysusers are typically system accounts with no login shell. Existing users are not modified."],["HISTORY","**systemd-sysusers** is part of **systemd**, implementing declarative system user/group creation to ensure consistent user accounts across installations."],["INSTALL","```apt: sudo apt install systemd```\n\n```dnf: sudo dnf install systemd```\n\n```pacman: sudo pacman -S systemd```\n\n```zypper: sudo zypper install systemd```\n\n```brew: brew install systemd```\n\n```nix: nix profile install nixpkgs#systemd```"],["SEE ALSO","[systemd-tmpfiles](/man/systemd-tmpfiles)(8), [useradd](/man/useradd)(8)"]],"e":["systemd-tmpfiles","useradd"],"x":"systemd-sysusers"},"t4":{"n":"t4","t":"T4 text template processor for code generation","s":[["TAGLINE","T4 text template processor for code generation"],["TLDR","**Process** a T4 template and write output to a file\n\n```t4 [template.tt] -o [output.cs]```\n\n**Process** a template with a parameter\n\n```t4 [template.tt] -o [output.cs] -p:[Name]=\"[value]\"```\n\n**Process** a template from stdin and write to stdout\n\n```echo '[<#= DateTime.Now #>]' | t4 -o -```\n\n**Preprocess** a template into a runtime class\n\n```t4 -c [ClassName] [template.tt] -o [Template.cs]```\n\n**Process** a template with a typed parameter\n\n```echo '<#@ parameter name=\"[Date]\" type=\"System.DateTime\" #><#=$\"{[Date]:dddd}\"#>' | t4 -o - -p:[Date]=\"[2024/03/08]\"```"],["SYNOPSIS","**t4** [_options_] [_template_]"],["PARAMETERS","**-o** _file_\n> Output file path; use **-** for stdout\n\n**-p:**_name_**=**_value_\n> Pass a parameter value to the template (repeatable)\n\n**-c** _classname_\n> Preprocess template into a runtime template class\n\n**-r** _assembly_\n> Reference an assembly\n\n**-u** _namespace_\n> Import a namespace\n\n**-I** _directory_\n> Add an include search directory\n\n**--dp** _processor!class!assembly_\n> Add a custom directive processor\n\n**--help**\n> Show help message\n\n**--version**\n> Show version information"],["DESCRIPTION","**t4** is a command-line tool for processing T4 (Text Template Transformation Toolkit) templates, a general-purpose way to use C# to generate any kind of text or code files. It is part of **Mono.TextTemplating**, a modern open-source reimplementation of the Visual Studio T4 text templating engine.\n\nThe tool operates in two modes: direct template processing, which generates output files immediately, and preprocessing, which converts templates into runtime template classes that can be embedded in applications and executed at runtime.\n\nTemplates use C# code blocks delimited by **<#** and **#>** tags to emit dynamic content. Parameter directives support automatic type conversion from CLI string values to the type specified in the template. The tool can read templates from standard input and write output to standard output, enabling pipeline integration."],["CAVEATS","Requires the **.NET SDK** (6.0 or later) to be installed. Install the tool globally with **dotnet tool install -g dotnet-t4**. Different from the original Visual Studio T4 engine in some edge cases. Templates written for Visual Studio may need minor adjustments for full compatibility."],["HISTORY","T4 (Text Template Transformation Toolkit) originated as part of **Visual Studio** for code generation. The **Mono.TextTemplating** project reimplemented the engine as an open-source, cross-platform alternative and has since evolved to include many improvements over the original, including support for the latest .NET APIs and C# language features. The command-line tool was first released as a **.NET global tool** in **2020**."],["SEE ALSO","[dotnet](/man/dotnet)(1), [m4](/man/m4)(1), [envsubst](/man/envsubst)(1)"]],"e":["dotnet","m4","envsubst"],"x":"t4 [template.tt] -o [output.cs]"},"tlmgr-repository":{"n":"tlmgr-repository","t":"Manage TeX Live package repositories","s":[["TAGLINE","Manage TeX Live package repositories"],["TLDR","**List repositories**\n\n```tlmgr repository list```\n\n**Add repository**\n\n```tlmgr repository add [url] [tag]```\n\n**Remove repository**\n\n```tlmgr repository remove [url]```\n\n**Set the entire repository list** (replaces all existing)\n\n```tlmgr repository set [url#tag] [url#tag ...]```\n\n**Check verification status** of repositories\n\n```tlmgr repository status```"],["SYNOPSIS","**tlmgr repository** _action_ [_options_]"],["PARAMETERS","**list** [_path|url|tag_]\n> List configured repositories and their tags. If a path, URL, or tag is given, initialize a TL database from that source and list its packages.\n\n**list** **--with-platforms**\n> Additionally list available platforms for each package.\n\n**add** _path_ [_tag_]\n> Add a repository, optionally attaching a tag for convenient reference.\n\n**remove** _path|tag_\n> Remove a repository by full path/URL or by its tag.\n\n**set** _path_[**#**_tag_] [_path_[**#**_tag_] ...]\n> Replace the entire repository list with the specified entries. One repository must be tagged as **main**.\n\n**status**\n> Report verification status of loaded repositories (tag, URL, and verification status)."],["DESCRIPTION","**tlmgr repository** manages the list of TeX Live package repositories. It allows configuring CTAN mirrors and additional package sources. One repository must always be tagged as **main**; operations will fail without it. Part of TeX Live Manager."],["CAVEATS","The **set** action replaces all previously configured repositories. One repository must be tagged as **main** or operations will fail. Repository verification depends on a working GnuPG (gpg) binary being available."],["SEE ALSO","[tlmgr-update](/man/tlmgr-update)(1), [tlmgr](/man/tlmgr)(1), [tlmgr-conf](/man/tlmgr-conf)(1), [tlmgr-install](/man/tlmgr-install)(1)"]],"e":["tlmgr-update","tlmgr","tlmgr-conf","tlmgr-install"],"x":"tlmgr repository list"},"tlmgr-shell":{"n":"tlmgr-shell","t":"Interactive TeX Live Manager session","s":[["TAGLINE","Interactive TeX Live Manager session"],["TLDR","**Start an interactive tlmgr shell**\n\n```tlmgr shell```\n\n**Start a shell and run a command** (e.g., list updates)\n\n```tlmgr shell <<< \"update --list\"```\n\n**Pipe multiple commands into the shell**\n\n```printf \"info [package]\\nquit\\n\" | tlmgr shell```"],["SYNOPSIS","**tlmgr shell**"],["DESCRIPTION","**tlmgr shell** starts an interactive TeX Live Manager session where you can execute multiple tlmgr commands without restarting the program each time. This is faster for batch operations since the TeX Live package database is loaded only once.\n\nIn general, all tlmgr actions that can be given on the command line work as commands in the shell (e.g., `update --list`, `install`, `info`, `search`). Type `quit` or `exit` (or send EOF) to leave the shell. The `protocol` command prints the current protocol version. The shell also supports machine-readable output for scripting by external tools."],["SEE ALSO","[tlmgr](/man/tlmgr)(1), [tlmgr-install](/man/tlmgr-install)(1), [tlmgr-update](/man/tlmgr-update)(1)"]],"e":["tlmgr","tlmgr-install","tlmgr-update"],"x":"tlmgr shell"},"uname":{"n":"uname","t":"Print system and kernel information","s":[["TAGLINE","Print system and kernel information"],["TLDR","**Show all system information**\n\n```uname -a```\n\n**Show kernel name**\n\n```uname -s```\n\n**Show hostname**\n\n```uname -n```\n\n**Show kernel release version**\n\n```uname -r```\n\n**Show kernel version**\n\n```uname -v```\n\n**Show machine hardware name** (architecture)\n\n```uname -m```\n\n**Show operating system**\n\n```uname -o```"],["SYNOPSIS","**uname** [_options_]"],["PARAMETERS","**-a**, **--all**\n> Print all information\n\n**-s**, **--kernel-name**\n> Print kernel name (e.g., Linux)\n\n**-n**, **--nodename**\n> Print network hostname\n\n**-r**, **--kernel-release**\n> Print kernel release (e.g., 5.15.0-generic)\n\n**-v**, **--kernel-version**\n> Print kernel version\n\n**-m**, **--machine**\n> Print machine hardware name (e.g., x86_64, arm64)\n\n**-p**, **--processor**\n> Print processor type\n\n**-i**, **--hardware-platform**\n> Print hardware platform\n\n**-o**, **--operating-system**\n> Print operating system"],["DESCRIPTION","**uname** prints system information about the machine and operating system. It's commonly used in scripts to detect the platform and adjust behavior accordingly.\n\nThe most common uses are **uname -a** for all information and **uname -m** to determine the system architecture (useful for downloading correct binaries).\n\nDifferent Unix systems may support different options. The **-s**, **-n**, **-r**, **-v**, and **-m** options are most portable across Unix variants."],["CAVEATS","On some systems, **-p** and **-i** may return \"unknown\" if the information isn't available.\n\nThe output of **uname -m** varies by system: x86_64, amd64, arm64, aarch64 may represent similar architectures on different systems.\n\nFor detailed OS distribution information on Linux, check **/etc/os-release** or use **lsb_release**."],["HISTORY","**uname** originated in **PWB/UNIX** in the late 1970s. It is part of POSIX and GNU coreutils on Linux."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[hostname](/man/hostname)(1), [arch](/man/arch)(1), [hostnamectl](/man/hostnamectl)(1), [lsb_release](/man/lsb_release)(1)"]],"e":["hostname","arch","hostnamectl","lsb_release"],"x":"uname -a"},"wami":{"n":"wami","t":"Search and recommend software tools","s":[["TAGLINE","Search and recommend software tools"],["TLDR","Search **all** categories sorted\n\n```wami -a -S [asc|desc] -s [search_string]```\n\nSearch **GitHub** sorted\n\n```wami -a -S desc --github [search_string]```\n\nList **topics**\n\n```wami --list-topics [search_string]```\n\nSearch for **pentest** tools\n\n```wami -S desc -s pentest credential default```"],["SYNOPSIS","**wami** [_OPTIONS_] [_SEARCH_...]"],["PARAMETERS","**-a, --show-all**\n> Show expanded results\n\n**-S, --sort** _ORDER_\n> Sort results (asc or desc)\n\n**-s, --search-all**\n> Search all categories\n\n**--github**\n> Search GitHub\n\n**--list-topics**\n> List matching topics"],["DESCRIPTION","**wami** is an open-source tool that recommends suitable programs for tasks. It searches a curated database and GitHub to find tools matching specified criteria.\n\nThe tool is particularly useful for finding security and pentesting tools but works for general software discovery."],["CAVEATS","Requires internet connection for GitHub searches. Results depend on database coverage. Search quality varies by term specificity."],["SEE ALSO","[apt-cache](/man/apt-cache)(8)"]],"e":["apt-cache"],"x":"wami -a -S [asc|desc] -s [search_string]"},"xdg-terminal-exec":{"n":"xdg-terminal-exec","t":"Launch the user's preferred terminal emulator","s":[["TAGLINE","Launch the user's preferred terminal emulator"],["TLDR","**Open the default terminal** without running a command\n\n```xdg-terminal-exec```\n\n**Run a command** inside the preferred terminal\n\n```xdg-terminal-exec [command] [args...]```\n\n**Set a window title** for the spawned terminal\n\n```xdg-terminal-exec --title=[My Shell] [command]```\n\n**Set a working directory** before executing the command\n\n```xdg-terminal-exec --dir=[path/to/dir] [command]```\n\n**Keep the terminal open** after the command exits\n\n```xdg-terminal-exec --hold [command]```\n\n**Print the selected Desktop Entry ID** instead of launching it\n\n```xdg-terminal-exec --print-id```\n\n**Show the resolved command line** that would be executed\n\n```xdg-terminal-exec --print-cmd [command]```"],["SYNOPSIS","**xdg-terminal-exec** [_options_] [**--**] [_command_ [_arguments_ ...]]"],["PARAMETERS","**--app-id=**_app_id_\n> Set the Wayland app-id (or X11 class) of the spawned window.\n\n**--title=**_title_\n> Set the terminal window title.\n\n**--dir=**_workdir_\n> Change to _workdir_ before running the command.\n\n**--hold**\n> Keep the terminal window open after the command terminates.\n\n**--print-id**\n> Print the Desktop Entry ID that was selected.\n\n**--print-path**\n> Print the filesystem path of the selected Desktop Entry.\n\n**--print-content**\n> Print the full content of the selected Desktop Entry.\n\n**--print-cmd**[=_printf_seq_]\n> Print the final command line that would be executed instead of running it.\n\n**--print-delimiter=**_printf_seq_\n> Set the delimiter used between multiple print outputs.\n\n**--**\n> Treat all following arguments as the command to execute."],["DESCRIPTION","**xdg-terminal-exec** launches the user's preferred terminal emulator, optionally with a command to execute inside it. It is the reference implementation of the proposed XDG Default Terminal Execution Specification from freedesktop.org.\n\nCandidate terminals are discovered from Desktop Entries installed on the system that declare the **TerminalEmulator** category and provide an **X-TerminalArgExec** key. The user chooses which of those entries to prefer by listing Desktop Entry IDs in a configuration file.\n\nThis mirrors tools like **xdg-open**: instead of hard-coding a terminal in scripts, desktop launchers, or applications, callers invoke **xdg-terminal-exec** and the system resolves the user's choice at runtime."],["CONFIGURATION","Preferred terminals are listed in the following files, searched in order of XDG config/data priority:\n\n```\n${desktop}-xdg-terminals.list\nxdg-terminals.list\n```\n\nWhere _${desktop}_ is a lowercased item from **XDG_CURRENT_DESKTOP**. Each line is a Desktop Entry ID, optionally with an action suffix:\n\n```\nfoot.desktop\nkitty.desktop\norg.gnome.Terminal.desktop:new-window\n```\n\nLines starting with **#** are comments. Special directives include **/enable_cache**, **/disable_cache**, **/execarg_compat**, **/execarg_strict**, and **/execarg_default:entry.desktop:arg**."],["ENVIRONMENT","**XTE_CACHE_ENABLED**\n> Enable or disable caching of the resolved terminal.\n\n**XTE_DEBUG**, **DEBUG**\n> Print debug information to stderr.\n\n**XDG_CURRENT_DESKTOP**, **XDG_CONFIG_HOME**, **XDG_CONFIG_DIRS**, **XDG_DATA_DIRS**\n> Standard XDG variables used for desktop detection and config lookup."],["CAVEATS","The specification is still a **proposal** on the freedesktop.org **terminal-wg** tracker; backwards compatibility is maintained on a best-effort basis. Not every terminal emulator ships the **X-TerminalArgExec** key yet, so some installed terminals may be invisible to the selector until their Desktop Entry is patched."],["HISTORY","**xdg-terminal-exec** was created by **Vladimir Kudrya** (Vladimir-csp) as a shell-based reference implementation accompanying a merge request against the freedesktop.org terminal-wg specifications repository. It is packaged by several distributions (Debian, Arch, Fedora) as a dependency of modern desktop launchers that need a generic way to spawn terminals."],["INSTALL","```dnf: sudo dnf install xdg-terminal-exec```\n\n```zypper: sudo zypper install xdg-terminal-exec```\n\n```nix: nix profile install nixpkgs#xdg-terminal-exec```"],["SEE ALSO","[xdg-open](/man/xdg-open)(1), [xdg-mime](/man/xdg-mime)(1), [xdg-settings](/man/xdg-settings)(1)"]],"e":["xdg-open","xdg-mime","xdg-settings"],"x":"xdg-terminal-exec"},"yum-config-manager":{"n":"yum-config-manager","t":"Manage yum repository configuration","s":[["TAGLINE","Manage yum repository configuration"],["TLDR","**View** the current configuration\n\n```yum-config-manager```\n\n**Add a repository** from a URL or file\n\n```yum-config-manager --add-repo [url_or_path]```\n\n**Enable** one or more repositories\n\n```yum-config-manager --enable [repo_id]```\n\n**Disable** one or more repositories\n\n```yum-config-manager --disable [repo_id]```\n\n**Set** a configuration option and save it\n\n```yum-config-manager --setopt=[repo_id.option=value] --save```"],["SYNOPSIS","**yum-config-manager** [_options_] [_section_...]"],["PARAMETERS","**-h, --help**\n> Display help information.\n\n**--save**\n> Save the current options (useful with **--setopt**).\n\n**--enable**\n> Enable the specified repositories (automatically saves).\n\n**--disable**\n> Disable the specified repositories (automatically saves).\n\n**--add-repo=**_URL_|_FILE_\n> Add (and enable) a repository from the given .repo file or URL.\n\n**--setopt=**_option_=_value_\n> Set any config option in yum config or repo files. Use _repoid.option=value_ for repo-specific options; _repoid_ may contain wildcards."],["DESCRIPTION","**yum-config-manager** is a utility for managing the main **yum** configuration and its repositories. It can display current settings, toggle which repositories are enabled, add new repositories from URLs or .repo files, and adjust individual options without manually editing `/etc/yum.conf` or files under `/etc/yum.repos.d/`.\n\nOn modern Red Hat-based distributions (RHEL 8+, Fedora, CentOS Stream), **yum** has been replaced by **dnf**, and this command is typically provided as an alias or wrapper for `dnf config-manager`."],["CAVEATS","Must be run as root to modify configuration. On DNF-based systems, **yum-config-manager** is an alias for **dnf config-manager** and requires the `dnf-plugins-core` package."],["HISTORY","**yum-config-manager** is part of the **yum-utils** package (later **dnf-utils**) from the RPM software management project. It was widely used on RHEL, CentOS, and Fedora to manage repositories before the transition to DNF."],["SEE ALSO","[dnf](/man/dnf)(8), [yum](/man/yum)(8), [rpm](/man/rpm)(8), [createrepo](/man/createrepo)(8)"]],"e":["dnf","yum","rpm","createrepo"],"x":"yum-config-manager"},"zapier":{"n":"zapier","t":"Zapier Platform CLI for building integrations","s":[["TAGLINE","Zapier Platform CLI for building integrations"],["TLDR","**Authenticate with Zapier**\n\n```zapier login```\n\n**Initialize a new integration project**\n\n```zapier init [project-name]```\n\n**Push integration code to Zapier**\n\n```zapier push```\n\n**Run integration tests locally**\n\n```zapier test```\n\n**Validate the integration against Zapier's schema**\n\n```zapier validate```\n\n**Promote a version to public access**\n\n```zapier promote [1.0.0] [100]```\n\n**View logs for the integration**\n\n```zapier logs```\n\n**Invoke a trigger or action locally for testing**\n\n```zapier invoke [trigger] [trigger_key]```"],["SYNOPSIS","**zapier** _command_ [_options_]"],["PARAMETERS","**login**\n> Authenticate with Zapier.\n\n**logout**\n> Deauthenticate from Zapier.\n\n**init**\n> Create a new integration project.\n\n**scaffold**\n> Scaffold a new trigger, action, or search.\n\n**push**\n> Upload the current integration to Zapier.\n\n**test**\n> Run integration tests locally.\n\n**validate**\n> Check integration against Zapier's schema.\n\n**promote**\n> Promote a version to public access.\n\n**describe**\n> Describe the current integration.\n\n**logs**\n> View recent logs for the integration.\n\n**env:set**\n> Set environment variables for a version.\n\n**env:get**\n> Get environment variables for a version.\n\n**invoke**\n> Invoke a trigger, action, or search locally.\n\n**register**\n> Register a new integration on Zapier.\n\n**build**\n> Build a pushable zip from the current directory.\n\n**versions**\n> List the versions of the current integration."],["DESCRIPTION","**zapier** is the official command-line interface for developing integrations on the Zapier automation platform. It provides tools for creating, testing, validating, and deploying custom integrations that connect third-party applications using JavaScript.\n\nThe development workflow starts with **zapier init** to scaffold a new project, followed by defining triggers, actions, and searches in JavaScript (Node.js v22). The **test** command runs integration tests locally, **invoke** lets you test triggers and actions from the terminal, and **validate** checks the integration against Zapier's schema requirements before deployment.\n\nOnce ready, **zapier push** uploads the integration to Zapier's platform. The **promote** command makes a specific version publicly available. Environment variables for deployed versions are managed with the **env:set** and **env:get** commands."],["CAVEATS","Zapier account required. Node.js required (integrations run on Node.js v22). Installed via `npm install -g zapier-platform-cli`."],["HISTORY","The **zapier** CLI was created by **Zapier** as the developer tool for building integrations on their automation platform. It replaced the earlier visual builder approach, giving developers a code-first workflow using JavaScript and Node.js."],["SEE ALSO","[node](/man/node)(1), [npm](/man/npm)(1)"]],"e":["node","npm"],"x":"zapier login"}});
