window.LCL_BUCKET(57,{"ajson":{"n":"ajson","t":"Query JSON data using JSONPath expressions","s":[["TAGLINE","Query JSON data using JSONPath expressions"],["TLDR","**Query** a JSON file using JSONPath\n\n```ajson '$.store.book[*].author' [file.json]```\n\nQuery from **stdin**\n\n```echo '{\"name\":\"John\"}' | ajson '$.name'```\n\nFilter with **condition**\n\n```ajson '$.store.book[?(@.price < 10)]' [file.json]```\n\nRead **multiline** JSON (one JSON object per line)\n\n```ajson -m '$.name' [file.jsonl]```\n\n**Evaluate** an expression over JSON data\n\n```ajson 'avg($..price)' [file.json]```"],["SYNOPSIS","**ajson** [**-mq**] _jsonpath_ [_file_]"],["DESCRIPTION","**ajson** is a command-line tool written in Go for querying and evaluating JSON data using JSONPath expressions. It reads JSON from a file or stdin and supports filters, wildcards, recursive descent, and evaluation functions such as **avg**, **sum**, **length**, **first**, **last**, and math functions.\n\nJSONPath syntax is similar to XPath for XML, with **$** representing the root object, **.** for property access, **[]** for array indexing or filtering, and **..** for recursive descent."],["PARAMETERS","**jsonpath**\n> JSONPath expression or evaluation string to evaluate.\n\n**file**\n> Path to JSON file (reads stdin if omitted).\n\n**-m**, **--multiline**\n> Read input as multiline JSON, where each line is a separate valid JSON object.\n\n**-q**, **--quiet**\n> Do not print errors to stderr."],["CAVEATS","JSONPath implementations may vary in supported features. Complex nested queries may be slow on large files. Invalid JSONPath expressions produce errors rather than empty results."],["HISTORY","**ajson** implements the JSONPath query language proposed by Stefan Goessner in **2007**, providing command-line access to this popular JSON querying method."],["SEE ALSO","[jq](/man/jq)(1), [yq](/man/yq)(1), [fx](/man/fx)(1), [gron](/man/gron)(1)"]],"e":["jq","yq","fx","gron"],"x":"ajson '$.store.book[*].author' [file.json]"},"apachectl":{"n":"apachectl","t":"Control the Apache HTTP Server","s":[["TAGLINE","Control the Apache HTTP Server"],["TLDR","**Start** the server\n\n```sudo apachectl start```\n\n**Restart** the server\n\n```sudo apachectl restart```\n\n**Stop** the server\n\n```sudo apachectl stop```\n\nTest **configuration file** validity\n\n```apachectl configtest```\n\nCheck server **status** (requires lynx browser)\n\n```apachectl status```\n\n**Reload** configuration without dropping connections\n\n```sudo apachectl graceful```\n\nPrint full **Apache configuration**\n\n```apachectl -S```"],["SYNOPSIS","**apachectl** _command_\n\n**apachectl** [_httpd-argument_]"],["DESCRIPTION","**apachectl** is a front end to the Apache HyperText Transfer Protocol (HTTP) server designed to help administrators manage the Apache daemon. It can operate in SysV init mode with simple commands or pass arguments directly to httpd."],["PARAMETERS","**start**\n> Start the Apache daemon\n\n**stop**\n> Stop the Apache daemon\n\n**restart**\n> Restart Apache; starts if not running\n\n**graceful**\n> Graceful restart without terminating active connections\n\n**graceful-stop**\n> Graceful stop without aborting current connections\n\n**configtest**\n> Validate syntax in configuration files\n\n**status**\n> Display brief operational status (requires lynx and mod_status)\n\n**fullstatus**\n> Display comprehensive status report\n\n**-S**\n> Print full Apache configuration (parsed vhosts)\n\n**-t**\n> Test configuration syntax\n\n**-M**\n> List loaded modules (passed to httpd)\n\n**-V**\n> Show httpd compile-time settings and version\n\n**-l**\n> List statically compiled modules\n\n**-h**\n> Display help"],["CONFIGURATION","**/etc/httpd/conf/httpd.conf**\n> Main configuration file on RHEL/CentOS/Fedora systems.\n\n**/etc/apache2/apache2.conf**\n> Main configuration file on Debian/Ubuntu systems.\n\n**/etc/httpd/conf.d/**\n> Directory for additional configuration files on RHEL-based systems.\n\n**/etc/apache2/sites-available/**\n> Virtual host configuration files on Debian-based systems."],["CAVEATS","The **status** and **fullstatus** commands require the lynx text browser and mod_status to be enabled."],["HISTORY","Part of the **Apache HTTP Server** project, developed by the Apache Software Foundation since **1995**."],["INSTALL","```apt: sudo apt install apache2```\n\n```apk: sudo apk add apache2-ctl```\n\n```zypper: sudo zypper install apache2```\n\n```brew: brew install apache2```"],["SEE ALSO","[apache2ctl](/man/apache2ctl)(8), [httpd](/man/httpd)(8)"]],"e":["apache2ctl","httpd"],"x":"sudo apachectl start"},"apt-key":{"n":"apt-key","t":"Deprecated APT key management utility for package authentication","s":[["TAGLINE","Deprecated APT key management utility for package authentication"],["TLDR","**List** trusted keys\n\n```apt-key list```\n\n**Add** a key to the trusted keystore\n\n```apt-key add [public_key_file.asc]```\n\n**Delete** a key from the trusted keystore\n\n```apt-key del [key_id]```\n\nAdd a **remote key** to the trusted keystore\n\n```wget -qO - [https://host.tld/filename.key] | apt-key add -```\n\nAdd a key from **keyserver** with only key ID\n\n```apt-key adv --keyserver pgp.mit.edu --recv [KEYID]```\n\n**Export** all trusted keys\n\n```apt-key exportall```\n\n**Modern replacement**: download a key directly into trusted.gpg.d (no apt-key needed)\n\n```curl -fsSL [https://example.com/key.gpg] | sudo gpg --dearmor -o /etc/apt/trusted.gpg.d/[example].gpg```\n\n**Modern replacement**: use signed-by in sources.list\n\n```echo \"deb [signed-by=/usr/share/keyrings/[example].gpg] [https://repo.example.com] [stable] main\" | sudo tee /etc/apt/sources.list.d/[example].list```"],["SYNOPSIS","**apt-key** [_command_] [_arguments_]"],["DESCRIPTION","**apt-key** is a deprecated key management utility for the APT Package Manager on Debian and Ubuntu. It manages the list of keys used by apt to authenticate packages.\n\n**Note:** apt-key is deprecated since Debian 11 (Bullseye) and Ubuntu 22.04 (Jammy). The recommended approach is to place keyring files directly in /etc/apt/trusted.gpg.d/ (with .gpg or .asc extension) or in /usr/share/keyrings/ and reference them via the `signed-by` option in sources.list entries. Only `apt-key del` remains supported for use in maintainer scripts."],["PARAMETERS","**list**\n> List all trusted keys with fingerprints\n\n**add file**\n> Add a new key to the list of trusted keys\n\n**del keyid**\n> Remove a key from the list of trusted keys\n\n**adv**\n> Pass advanced options to gpg\n\n**update**\n> Update the local keyring with the archive keyring\n\n**net-update**\n> Update the local keyring, fetching missing keys from a keyserver\n\n**export keyid**\n> Output the key to standard output\n\n**exportall**\n> Output all trusted keys to standard output\n\n**finger**\n> List fingerprints of trusted keys (deprecated)\n\n**--keyring** _filename_\n> Operate on a specific keyring file (deprecated)"],["CONFIGURATION","**/etc/apt/trusted.gpg.d/**\n> Directory for trusted keyring files in GPG format.\n\n**/usr/share/keyrings/**\n> System-wide keyrings referenced via signed-by in sources.list."],["CAVEATS","**Deprecated** in favor of placing keyring files directly in /etc/apt/trusted.gpg.d/ or using signed-by in apt sources. Only apt-key del should be used in maintainer scripts."],["HISTORY","Part of the **APT** (Advanced Package Tool) suite. Deprecated starting with Debian 11 and Ubuntu 22.04 in favor of trusted.gpg.d directory."],["INSTALL","```apt: sudo apt install apt```\n\n```dnf: sudo dnf install apt```\n\n```pacman: sudo pacman -S apt```\n\n```apk: sudo apk add apt```\n\n```brew: brew install apt```\n\n```nix: nix profile install nixpkgs#apt```"],["SEE ALSO","[apt](/man/apt)(8), [apt-get](/man/apt-get)(8), [gpg](/man/gpg)(1), [apt-secure](/man/apt-secure)(8)"]],"e":["apt","apt-get","gpg","apt-secure"],"x":"apt-key list"},"arithmetic":{"n":"arithmetic","t":"Practice basic math with a quiz game","s":[["TAGLINE","Practice basic math with a quiz game"],["TLDR","Start an **arithmetic quiz**\n\n```arithmetic```\n\nSpecify one or more **operation symbols** to practice\n\n```arithmetic -o [+|-|x|/]```\n\nSpecify a **range** for problem numbers\n\n```arithmetic -r [7]```"],["SYNOPSIS","**arithmetic** [**-o** _+-x/_] [**-r** _range_]"],["DESCRIPTION","**arithmetic** is a simple educational program that quizzes users on basic arithmetic problems. It generates random math problems and tracks the user's accuracy, providing a playful way to practice mental math.\n\nBy default, problems use all four basic operations. The range option controls the magnitude of numbers used in problems."],["PARAMETERS","**-o** _operations_\n> Specify which operations to include: **+** (addition), **-** (subtraction), **x** (multiplication), **/** (division). Defaults to **+** and **-**.\n\n**-r** _range_\n> Largest operand used in problems (default: **10**). For addition and multiplication the operands lie between 0 and _range_; for subtraction and division both the answer and the second operand lie in that range, so results are never negative or fractional."],["CAVEATS","**arithmetic** asks problems repeatedly and weights operations you answer slowly or incorrectly, so they recur more often. Press **Ctrl-D** to end the session and print a summary of the number of correct answers and the time taken."],["HISTORY","**arithmetic** is one of the classic BSD games, originating in the Unix games collection and carried into **4.4BSD**. It ships in the **bsdgames** package on most Linux distributions as a simple educational utility."],["INSTALL","```dnf: sudo dnf install bsd-games```\n\n```pacman: sudo pacman -S bsd-games```\n\n```apk: sudo apk add bsd-games```\n\n```zypper: sudo zypper install bsd-games```"],["SEE ALSO","[bc](/man/bc)(1), [dc](/man/dc)(1)"],["RESOURCES","```[Source code](https://github.com/jsm28/bsd-games)```\n\n```[Homepage](https://www.polyomino.org.uk/computer/software/bsd-games/)```"]],"e":["bc","dc"],"x":"arithmetic"},"awesome":{"n":"awesome","t":"Highly configurable tiling window manager for X11","s":[["TAGLINE","Highly configurable tiling window manager for X11"],["TLDR","**Start awesome** window manager\n\n```awesome```\n\n**Start with a specific configuration** file\n\n```awesome -c [~/.config/awesome/rc.lua]```\n\n**Check configuration** for errors\n\n```awesome -k```\n\n**Check specific config file** for errors\n\n```awesome -k -c [path/to/rc.lua]```\n\n**Display version** information\n\n```awesome -v```\n\n**Replace a running window manager**\n\n```awesome --replace```"],["SYNOPSIS","**awesome** [**-v** | **--version**] [**-h** | **--help**] [**-c** | **--config** _FILE_] [**-k** | **--check**] [**-s** | **--search** _DIR_] [**-a** | **--no-argb**] [**-r** | **--replace**]"],["PARAMETERS","**-v**, **--version**\n> Display version information and exit\n\n**-h**, **--help**\n> Display help message and exit\n\n**-c** _FILE_, **--config** _FILE_\n> Use specified Lua configuration file instead of default\n\n**-k**, **--check**\n> Check configuration file syntax and exit\n\n**-s**, **--search** _DIR_\n> Add directory to Lua library search path\n\n**-a**, **--no-argb**\n> Disable ARGB visuals (transparency)\n\n**-r**, **--replace**\n> Replace an existing window manager"],["DESCRIPTION","**awesome** is a highly configurable, tiling window manager for X11. It manages windows in tiled, floating, and other layouts, maximizing screen space efficiency for keyboard-driven workflows.\n\nConfiguration is done through **~/.config/awesome/rc.lua** using the Lua programming language, allowing extensive customization of layouts, keybindings, widgets, and appearance. The default configuration provides a functional starting point with sensible defaults.\n\nKey concepts include tags (virtual desktops), layouts (tiling algorithms), and widgets (status bar components). The Mod4 key (typically Windows/Super key) is the default modifier for keybindings.\n\nDefault keybindings include:\n- **Mod4 + Enter**: Open terminal\n- **Mod4 + r**: Run prompt\n- **Mod4 + j/k**: Focus next/previous client\n- **Mod4 + Shift + c**: Close focused client\n- **Mod4 + Space**: Cycle layouts\n- **Mod4 + 1-9**: Switch to tag\n\nThe window manager includes a built-in menu, notification system, and status bar with system information."],["CONFIGURATION","**~/.config/awesome/rc.lua**\n> Main configuration file written in Lua. Controls layouts, keybindings, widgets, themes, and all window manager behavior."],["CAVEATS","Requires X11; does not support Wayland. Configuration errors in rc.lua will cause awesome to fall back to defaults. Some features require a compositor for transparency effects. The learning curve for Lua configuration can be steep for new users."],["HISTORY","**awesome** was created by **Julien Danjou** and first released in **2007**. It was designed as a successor to dwm with greater extensibility through Lua scripting. The name is a playful reference to being \"awesome\" rather than just \"good.\" The project continues active development with a focus on stability and customization."],["INSTALL","```apt: sudo apt install awesome```\n\n```dnf: sudo dnf install awesome```\n\n```pacman: sudo pacman -S awesome```\n\n```apk: sudo apk add awesome```\n\n```zypper: sudo zypper install awesome```\n\n```nix: nix profile install nixpkgs#awesome```"],["SEE ALSO","[i3](/man/i3)(1), [dwm](/man/dwm)(1), [bspwm](/man/bspwm)(1), [lua](/man/lua)(1)"]],"e":["i3","dwm","bspwm","lua"],"x":"awesome"},"aws-cloudformation":{"n":"aws-cloudformation","t":"Provision infrastructure from declarative templates.","s":[["TAGLINE","Provision infrastructure from declarative templates."],["TLDR","**Create a new stack** from a template file\n\n```aws cloudformation create-stack --stack-name [my-stack] --template-body file://[template.yaml]```\n\n**Update an existing stack** with a new template\n\n```aws cloudformation update-stack --stack-name [my-stack] --template-body file://[template.yaml]```\n\n**Delete a stack** and all its resources\n\n```aws cloudformation delete-stack --stack-name [my-stack]```\n\n**Describe stack status** and outputs\n\n```aws cloudformation describe-stacks --stack-name [my-stack]```\n\n**Validate a template** before deployment\n\n```aws cloudformation validate-template --template-body file://[template.yaml]```\n\n**Create a change set** to preview changes before applying\n\n```aws cloudformation create-change-set --stack-name [my-stack] --change-set-name [my-changes] --template-body file://[template.yaml]```\n\n**Deploy a template** (creates or updates stack with change set)\n\n```aws cloudformation deploy --template-file [template.yaml] --stack-name [my-stack] --capabilities CAPABILITY_IAM```\n\n**List all stacks** and their statuses\n\n```aws cloudformation list-stacks --stack-status-filter CREATE_COMPLETE UPDATE_COMPLETE```\n\n**Package a template** with local artifacts and upload to S3\n\n```aws cloudformation package --template-file [template.yaml] --s3-bucket [my-bucket] --output-template-file [packaged.yaml]```"],["SYNOPSIS","**aws cloudformation** _command_ [_options_]"],["DESCRIPTION","**aws cloudformation** is the AWS CLI interface for AWS CloudFormation, an infrastructure-as-code service that lets you model and provision AWS resources using templates. Templates define resources declaratively in YAML or JSON format, and CloudFormation handles provisioning and configuration.\n\nCloudFormation manages resources as **stacks**, creating, updating, and deleting all resources together while automatically handling dependencies. Change sets allow previewing modifications before applying them."],["COMMANDS","**create-stack**\n> Create a new stack from a template\n\n**update-stack**\n> Update an existing stack with changes\n\n**delete-stack**\n> Delete a stack and its resources\n\n**describe-stacks**\n> Get stack information, status, and outputs\n\n**validate-template**\n> Check template syntax and structure\n\n**create-change-set**\n> Create a preview of stack changes\n\n**execute-change-set**\n> Apply a change set to the stack\n\n**package**\n> Upload local artifacts and rewrite template references\n\n**deploy**\n> Deploy a template with automatic change set creation\n\n**list-stacks**\n> List stacks with optional status filtering\n\n**detect-stack-drift**\n> Check for configuration drift from template"],["CAVEATS","Stack deletions can fail if resources have deletion protection or dependencies outside CloudFormation. Rollback on failure is automatic but can leave stacks in UPDATE_ROLLBACK_FAILED state requiring manual intervention. Large templates may hit API limits and require nested stacks."],["HISTORY","AWS CloudFormation launched in **February 2011** as one of the first infrastructure-as-code services. It introduced change sets in **2016** for safer updates, drift detection in **2018**, and has continuously expanded resource type coverage. The service now supports over 700 AWS resource types."],["INSTALL","```dnf: sudo dnf install aws```\n\n```pacman: sudo pacman -S aws-cli```\n\n```apk: sudo apk add aws-cli```\n\n```zypper: sudo zypper install aws-cli```"],["SEE ALSO","[aws](/man/aws)(1), [aws-ec2](/man/aws-ec2)(1), [aws-s3](/man/aws-s3)(1), [aws-iam](/man/aws-iam)(1), [terraform](/man/terraform)(1)"]],"e":["aws","aws-ec2","aws-s3","aws-iam","terraform"],"x":"aws cloudformation create-stack --stack-name [my-stack] --template-body file://[template.yaml]"},"backlight_control":{"n":"backlight_control","t":"Adjust screen backlight brightness by percentage.","s":[["TAGLINE","Adjust screen backlight brightness by percentage."],["TLDR","**Increase** the backlight by a percentage\n\n```backlight_control +[5]```\n\n**Decrease** the backlight by a percentage\n\n```backlight_control -[5]```\n\n**Set** the backlight to a specific percentage\n\n```backlight_control [90]```\n\nDisplay **help**\n\n```backlight_control```"],["SYNOPSIS","**backlight_control** [**+**|**-**]_percentage_"],["DESCRIPTION","**backlight_control** adjusts the screen backlight brightness on Linux systems using percentage values. It provides a simple interface to the kernel's backlight subsystem.\n\nThe tool works with laptop displays and other devices that expose backlight control through **/sys/class/backlight/**."],["PARAMETERS","**+**_n_\n> Increase brightness by n percent\n\n**-**_n_\n> Decrease brightness by n percent\n\n_n_\n> Set brightness to n percent (0-100)"],["CAVEATS","Requires appropriate permissions to write to backlight control files. May require adding the user to a video or similar group. Only works with hardware that exposes backlight controls through the sysfs interface."],["SEE ALSO","[xbacklight](/man/xbacklight)(1), [brightnessctl](/man/brightnessctl)(1), [light](/man/light)(1)"],["RESOURCES","```[Source code](https://github.com/Hendrikto/backlight_control)```"]],"e":["xbacklight","brightnessctl","light"],"x":"backlight_control +[5]"},"bcomps":{"n":"bcomps","t":"Decompose graphs into biconnected components","s":[["TAGLINE","Decompose graphs into biconnected components"],["TLDR","**Decompose graph into biconnected components**\n\n```bcomps [input.gv] > [output.gv]```\n\n**Print number of blocks and cutvertices**\n\n```bcomps -v [input.gv]```\n\n**Print each component as separate graph**\n\n```bcomps -x [input.gv]```\n\n**Print block-cutvertex tree**\n\n```bcomps -t [input.gv]```\n\n**Write components to separate files**\n\n```bcomps -o [output.gv] [input.gv]```\n\n**Silent mode with statistics only**\n\n```bcomps -sv [input.gv]```"],["SYNOPSIS","**bcomps** [_-stvx?_] [_-o_ _outfile_] [_files_]"],["DESCRIPTION","**bcomps** decomposes graphs into their biconnected components, printing the components to standard output. A biconnected component is a maximal subgraph that remains connected after removal of any single vertex.\n\nThe tool is part of the Graphviz graph visualization suite and works with DOT language graph files."],["PARAMETERS","**-s**\n> Silent mode; no output graph printed (implies -v)\n\n**-t**\n> Print the underlying block-cutvertex tree\n\n**-v**\n> Print number of blocks and cutvertices\n\n**-x**\n> Print each biconnected component as separate root graph\n\n**-o** _outfile_\n> Write each root graph to different file derived from outfile\n\n**-?**\n> Display help information"],["OUTPUT FORMAT","By default, each input graph is printed with blocks as subgraphs. Subgraph names concatenate the input graph name, \"_bcc_\", and the block number.\n\nWhen both -o and -x are used, each block is written to a separate file. If _outfile_ does not have a suffix, the nth block of the ith graph is written to outfile_n_i (the 0th block of the 0th graph is written to _outfile_ itself). If _outfile_ has a suffix (e.g., base.sfx), the files follow the same pattern with .sfx appended. Block-cutvertex trees are written to outfile_n_T."],["RETURN VALUES","**0**\n> All input graphs are biconnected\n\n**non-zero**\n> Any graph has multiple blocks or an error occurred"],["CAVEATS","Input files must be in Graphviz DOT format. The tool identifies articulation points (cutvertices) that separate components. Large graphs may require significant processing time."],["INSTALL","```apt: sudo apt install graphviz```\n\n```dnf: sudo dnf install graphviz```\n\n```pacman: sudo pacman -S graphviz```\n\n```apk: sudo apk add graphviz```\n\n```zypper: sudo zypper install graphviz```\n\n```brew: brew install graphviz```\n\n```nix: nix profile install nixpkgs#graphviz```"],["SEE ALSO","[ccomps](/man/ccomps)(1), [gc](/man/gc)(1), [dot](/man/dot)(1), [gvpr](/man/gvpr)(1), [sccmap](/man/sccmap)(1), [tred](/man/tred)(1), [acyclic](/man/acyclic)(1)"]],"e":["ccomps","gc","dot","gvpr","sccmap","tred","acyclic"],"x":"bcomps [input.gv] > [output.gv]"},"blockdev":{"n":"blockdev","t":"Query and set block device parameters","s":[["TAGLINE","Query and set block device parameters"],["TLDR","**Report** all devices\n\n```sudo blockdev --report```\n\nReport **specific** device\n\n```sudo blockdev --report /dev/sda1```\n\nGet **size** in sectors\n\n```sudo blockdev --getsz /dev/sda1```\n\nSet **read-only**\n\n```sudo blockdev --setro /dev/sda1```\n\nSet **read-write**\n\n```sudo blockdev --setrw /dev/sda1```\n\n**Flush** buffers\n\n```sudo blockdev --flushbufs /dev/sda1```\n\nGet **physical sector size**\n\n```sudo blockdev --getpbsz /dev/sda1```\n\nSet **read-ahead** in 512-byte sectors\n\n```sudo blockdev --setra 128 /dev/sda1```\n\n**Reread** the partition table\n\n```sudo blockdev --rereadpt /dev/sda```"],["SYNOPSIS","**blockdev** [**-q**] [**-v**] _command_... _device_...\n\n**blockdev** **--report** [_device_...]"],["DESCRIPTION","**blockdev** queries and manipulates block device properties from the command line. It can report device sizes, set read-only/read-write modes, flush buffers, and configure performance parameters like read-ahead."],["PARAMETERS","**--report**\n> Print a report for specified devices or all devices\n\n**--getsz**\n> Get size in 512-byte sectors\n\n**--getsize64**\n> Print device size in bytes\n\n**--getss**\n> Print logical sector size in bytes, usually 512\n\n**--getpbsz**\n> Get physical block (sector) size\n\n**--getbsz**\n> Print the blocksize in bytes (the value the kernel uses internally, file-descriptor specific)\n\n**--getiomin**\n> Get minimum I/O size\n\n**--getioopt**\n> Get optimal I/O size\n\n**--getro**\n> Get read-only state. Prints 1 if read-only, 0 otherwise\n\n**--setro**\n> Set device to read-only\n\n**--setrw**\n> Set device to read-write\n\n**--flushbufs**\n> Flush buffers\n\n**--setra** _sectors_\n> Set read-ahead value in 512-byte sectors\n\n**--getra**\n> Get current read-ahead value\n\n**--rereadpt**\n> Reread the partition table\n\n**-q**\n> Be quiet\n\n**-v**\n> Be verbose"],["CAVEATS","Requires root privileges. Setting a device read-only while mounted may cause issues. Changes to read-ahead affect I/O performance and may not be beneficial for all workloads."],["HISTORY","**blockdev** is part of the **util-linux** package, providing low-level block device management on Linux systems."],["INSTALL","```apt: sudo apt install util-linux```\n\n```dnf: sudo dnf install util-linux```\n\n```pacman: sudo pacman -S util-linux```\n\n```apk: sudo apk add util-linux-misc```\n\n```zypper: sudo zypper install util-linux```\n\n```brew: brew install util-linux```\n\n```nix: nix profile install nixpkgs#util-linux```"],["SEE ALSO","[lsblk](/man/lsblk)(8), [blkid](/man/blkid)(8), [fdisk](/man/fdisk)(8), [hdparm](/man/hdparm)(8), [partprobe](/man/partprobe)(8)"],["RESOURCES","```[Source code](https://github.com/util-linux/util-linux)```\n\n```[Documentation](https://www.man7.org/linux/man-pages/man8/blockdev.8.html)```"]],"e":["lsblk","blkid","fdisk","hdparm","partprobe"],"x":"sudo blockdev --report"},"bun-c":{"n":"bun-c","t":"Run a shell command string through Bun's built-in shell","s":[["TAGLINE","Run a shell command string through Bun's built-in shell"],["TLDR","**Run a shell command**\n\n```bun -c \"[shell command]\"```\n\n**Run a pipeline**\n\n```bun -c \"cat [file.txt] | grep [pattern]\"```\n\n**Run with environment from .env file**\n\n```bun -c \"[command]\" --env-file [.env]```"],["SYNOPSIS","**bun** **-c** _command_ [_options_]"],["PARAMETERS","**-c** _command_\n> Execute the given command string using Bun's built-in shell.\n\n**--env-file** _file_\n> Load environment variables from the specified file before executing."],["DESCRIPTION","**bun -c** runs a shell command string, similar to **sh -c** but using Bun's built-in cross-platform shell implementation. It supports common shell features like pipes, redirects, and environment variable expansion. It can load environment variables from **.env** files before executing the command, making it useful for running scripts that depend on environment configuration without additional tooling."],["SEE ALSO","[bun](/man/bun)(1), [bun-run](/man/bun-run)(1), [bun-x](/man/bun-x)(1), [sh](/man/sh)(1)"]],"e":["bun","bun-run","bun-x","sh"],"x":"bun -c \"[shell command]\""},"cargo-vendor":{"n":"cargo-vendor","t":"Vendor all dependencies locally","s":[["TAGLINE","Vendor all dependencies locally"],["TLDR","**Vendor dependencies**\n\n```cargo vendor```\n\n**Vendor to specific directory**\n\n```cargo vendor [vendor/]```\n\n**Vendor and save config**\n\n```cargo vendor > .cargo/config.toml```\n\n**Vendor with versioned directories**\n\n```cargo vendor --versioned-dirs```\n\n**Vendor specific package**\n\n```cargo vendor -s [package/Cargo.toml]```"],["SYNOPSIS","**cargo vendor** [_options_] [_path_]"],["DESCRIPTION","**cargo vendor** downloads and copies all crates.io and git dependencies into a local directory (default: `vendor/`). It outputs the Cargo configuration needed to redirect dependency resolution to the vendored sources.\n\nThis is primarily used for offline builds, air-gapped environments, and reproducible build systems where network access during compilation is not available or not desired. The vendored sources are read-only; to modify a vendored crate, use the `[patch]` section in Cargo.toml instead of editing files directly in the vendor directory."],["PARAMETERS","**-s**, **--sync** _path_\n> Additional Cargo.toml to sync\n\n**--no-delete**\n> Don't delete existing vendor directory\n\n**--versioned-dirs**\n> Use versioned directory names\n\n**--respect-source-config**\n> Respect [source] config when vendoring\n\n**--manifest-path** _path_\n> Path to Cargo.toml\n\n**-v**, **--verbose**\n> Verbose output\n\n**-q**, **--quiet**\n> Suppress output"],["CONFIGURATION","**.cargo/config.toml**\n> Source replacement settings that redirect dependency resolution to the vendored directory. The output of `cargo vendor` provides the exact configuration to add."],["OFFLINE BUILDS","After vendoring:\n```\ncargo build --offline\n```"],["CAVEATS","Vendored sources are read-only. Use [patch] for modifications. Resolution may differ from online mode. Run cargo fetch first for complete dependency download."],["INSTALL","```apt: sudo apt install cargo```\n\n```dnf: sudo dnf install cargo```\n\n```apk: sudo apk add cargo```\n\n```zypper: sudo zypper install cargo```\n\n```nix: nix profile install nixpkgs#cargo```"],["SEE ALSO","[cargo](/man/cargo)(1), [cargo-fetch](/man/cargo-fetch)(1)"],["RESOURCES","```[Source code](https://github.com/rust-lang/cargo)```\n\n```[Documentation](https://doc.rust-lang.org/cargo/commands/cargo-vendor.html)```"]],"e":["cargo","cargo-fetch"],"x":"cargo vendor"},"comchan":{"n":"comchan","t":"Minimal serial monitor for the terminal","s":[["TAGLINE","Minimal serial monitor for the terminal"],["TLDR","**Connect** to a serial port at 9600 baud\n\n```comchan -p /dev/ttyUSB0 -r 9600```\n\n**Auto-detect** the serial port and connect at 115200 baud\n\n```comchan --auto -r 115200```\n\n**Connect** with real-time plotting enabled\n\n```comchan -p /dev/ttyACM0 -r 9600 --plot```\n\n**Enable** verbose output and logging\n\n```comchan -p /dev/ttyUSB0 -r 9600 -v -l```\n\n**Generate** a default configuration file\n\n```comchan --generate-config```"],["SYNOPSIS","**comchan** [_OPTIONS_]"],["PARAMETERS","**-p**, **--port** _PORT_\n> Specify the serial port (e.g., /dev/ttyUSB0, /dev/ttyACM0, COM3).\n\n**-r**, **--baud** _RATE_\n> Set the baud rate (e.g., 9600, 115200).\n\n**--auto**\n> Auto-detect the connected serial port.\n\n**-v**, **--verbose**\n> Show detailed connection info.\n\n**-l**, **--log**\n> Save output to a log file.\n\n**--plot**\n> Enable real-time terminal-based graphing of sensor data.\n\n**--generate-config**\n> Generate a default configuration file at ~/.config/comchan/comchan.toml.\n\n**--help**\n> Display help information."],["DESCRIPTION","**comchan** (Communication Channel) is a fast, minimal serial monitor written in Rust for communicating with serial-connected devices such as Arduino, ESP32, Teensy, and Raspberry Pi from the terminal. It supports reading and writing serial data, auto-detecting connected devices, real-time terminal-based plotting for sensor data with multiple sensor visualization, colorized log output, and cross-platform operation on Linux, macOS, and Windows."],["CONFIGURATION","Configuration stored at **~/.config/comchan/comchan.toml**. Supports **port** (or \"auto\"), **baud** (default: 9600), **data_bits**, **stop_bits**, **parity** (none/odd/even), **flow_control** (none/software/hardware), **timeout_ms**, **reset_delay_ms**, **verbose**, and **plot**. Command-line flags override config file values."],["CAVEATS","On Linux, users may need appropriate permissions to access serial ports (typically membership in the **dialout** or **uucp** group). The **--plot** feature expects numeric data from the serial device to graph correctly."],["HISTORY","**comchan** was created by **Vaishnav Sabari Girish** and is written in Rust under the MIT license. It is available via cargo, AUR, and Homebrew, and follows the Unix philosophy of doing one thing well."],["INSTALL","```aur: yay -S comchan```"],["SEE ALSO","[minicom](/man/minicom)(1), [picocom](/man/picocom)(1), [screen](/man/screen)(1), [tio](/man/tio)(1), [cu](/man/cu)(1)"]],"e":["minicom","picocom","screen","tio","cu"],"x":"comchan -p /dev/ttyUSB0 -r 9600"},"configure":{"n":"configure","t":"autotools build configuration script","s":[["TAGLINE","autotools build configuration script"],["TLDR","**Run with default options**\n\n```./configure```\n\n**Set installation prefix**\n\n```./configure --prefix=[/usr/local]```\n\n**Enable or disable feature**\n\n```./configure --enable-[feature] --disable-[other]```\n\n**With specific dependency path**\n\n```./configure --with-[lib]=[/path/to/lib]```\n\n**Show all options**\n\n```./configure --help```\n\n**Build for different host**\n\n```./configure --host=[x86_64-linux-gnu]```\n\n**Set compiler and flags**\n\n```CC=[gcc] CFLAGS=\"[-O2]\" ./configure```"],["SYNOPSIS","**./configure** [_options_] [_VAR=VALUE_...]"],["DESCRIPTION","**configure** is a shell script generated by GNU Autotools that prepares software for building on the current system. It detects system capabilities, checks for dependencies, and generates Makefiles tailored to the environment.\n\nThe script probes for compilers, libraries, headers, and system features, storing results in config.h and substituting values into Makefile.in templates. This enables portable software building across different Unix-like systems."],["PARAMETERS","**--prefix=** _path_\n> Installation root directory.\n\n**--exec-prefix=** _path_\n> Architecture-dependent files location.\n\n**--bindir=** _path_\n> User executables directory.\n\n**--libdir=** _path_\n> Libraries directory.\n\n**--includedir=** _path_\n> Header files directory.\n\n**--enable-** _feature_\n> Enable optional feature.\n\n**--disable-** _feature_\n> Disable feature.\n\n**--with-** _package_\n> Use external package.\n\n**--without-** _package_\n> Don't use package.\n\n**--host=** _triplet_\n> Cross-compile for target.\n\n**--build=** _triplet_\n> Build system type.\n\n**--help**\n> Show available options."],["ENVIRONMENT VARIABLES","**CC**: C compiler command\n**CXX**: C++ compiler command\n**CFLAGS**: C compiler flags\n**LDFLAGS**: Linker flags\n**PKG_CONFIG_PATH**: pkg-config search path"],["CAVEATS","Script must be regenerated if autoconf files change. Cross-compilation requires careful setup. Some packages have many options to navigate. Build may fail if dependencies are missing."],["HISTORY","**configure** scripts are generated by **GNU Autoconf**, created by **David MacKenzie** starting in **1991**. Autoconf addressed the challenge of portable software across diverse Unix systems. The configure/make/make install pattern became standard for Unix software distribution. While newer build systems exist, Autotools remains widely used."],["SEE ALSO","[make](/man/make)(1), [autoconf](/man/autoconf)(1), [automake](/man/automake)(1), [pkg-config](/man/pkg-config)(1)"],["RESOURCES","```[Source code](https://git.savannah.gnu.org/cgit/autoconf.git)```\n\n```[Homepage](https://www.gnu.org/software/autoconf/)```\n\n```[Documentation](https://www.gnu.org/software/autoconf/manual/)```"]],"e":["make","autoconf","automake","pkg-config"],"x":"./configure"},"docker-run":{"n":"docker-run","t":"create and start containers from images","s":[["TAGLINE","create and start containers from images"],["TLDR","**Run a container interactively**\n\n```docker run -it [image] [bash]```\n\n**Run container in background**\n\n```docker run -d [image]```\n\n**Run with port mapping**\n\n```docker run -p [8080:80] [image]```\n\n**Run with volume mount**\n\n```docker run -v [/host/path:/container/path] [image]```\n\n**Run with environment variables**\n\n```docker run -e [VAR=value] [image]```\n\n**Run with automatic removal on exit**\n\n```docker run --rm [image]```\n\n**Run with custom name**\n\n```docker run --name [container_name] [image]```\n\n**Run with memory limit**\n\n```docker run -m [512m] [image]```"],["SYNOPSIS","**docker run** [_options_] _image_ [_command_] [_args_]"],["PARAMETERS","**-d**, **--detach**\n> Run container in background.\n\n**-it**\n> Interactive with pseudo-TTY.\n\n**-p**, **--publish** _host:container_\n> Publish container port to host.\n\n**-v**, **--volume** _host:container_\n> Bind mount a volume.\n\n**-e**, **--env** _VAR=value_\n> Set environment variable.\n\n**--name** _name_\n> Assign container name.\n\n**--rm**\n> Remove container on exit.\n\n**-m**, **--memory** _limit_\n> Memory limit (e.g., 512m, 1g).\n\n**--cpus** _n_\n> Number of CPUs.\n\n**--network** _network_\n> Connect to network.\n\n**--restart** _policy_\n> Restart policy: no, always, unless-stopped, on-failure.\n\n**-w**, **--workdir** _dir_\n> Working directory inside container.\n\n**-u**, **--user** _user_\n> Username or UID.\n\n**--entrypoint** _cmd_\n> Override default entrypoint."],["DESCRIPTION","**docker run** creates and starts a new container from an image. It combines **docker create** and **docker start** into a single command, providing the primary way to launch containers.\n\nThe command pulls the image if not present locally, creates a container from it, and starts execution. Options control resource allocation, networking, storage, and runtime behavior.\n\nContainers are isolated but can connect to host resources through port mapping, volume mounts, and network configuration. The **-it** flags enable interactive terminal access for debugging and exploration."],["CAVEATS","Containers run as root by default; use **-u** for non-root execution. Ports below 1024 require root on the host. Volumes with relative paths are created as Docker volumes, not bind mounts. Container filesystem changes are lost unless volumes are used."],["HISTORY","docker run has been the fundamental Docker command since the initial release in **2013** by **Solomon Hykes** and dotCloud (later Docker, Inc.). The command's design draws from Unix philosophy and LXC container concepts. Options have expanded significantly to support enterprise features, orchestration, and security requirements."],["INSTALL","```apt: sudo apt install docker-cli```\n\n```dnf: sudo dnf install docker-cli```\n\n```pacman: sudo pacman -S docker```\n\n```apk: sudo apk add docker-cli```\n\n```zypper: sudo zypper install docker```\n\n```brew: brew install docker```\n\n```nix: nix profile install nixpkgs#docker```"],["SEE ALSO","[docker](/man/docker)(1), [docker-build](/man/docker-build)(1), [docker-ps](/man/docker-ps)(1), [docker-exec](/man/docker-exec)(1)"],["RESOURCES","```[Source code](https://github.com/docker/cli)```\n\n```[Documentation](https://docs.docker.com/reference/cli/docker/)```"]],"e":["docker","docker-build","docker-ps","docker-exec"],"x":"docker run -it [image] [bash]"},"dumpcap":{"n":"dumpcap","t":"network packet capture engine","s":[["TAGLINE","network packet capture engine"],["TLDR","**Capture on default interface**\n\n```dumpcap -i [eth0] -w [capture.pcapng]```\n\n**List available interfaces**\n\n```dumpcap -D```\n\n**Capture with ring buffer**\n\n```dumpcap -i [eth0] -b filesize:100000 -w [capture.pcapng]```\n\n**Capture with duration limit**\n\n```dumpcap -i [eth0] -a duration:60 -w [capture.pcapng]```\n\n**Capture without promiscuous mode** (it is on by default)\n\n```dumpcap -i [eth0] -p -w [capture.pcapng]```\n\n**Capture with a packet count limit**\n\n```dumpcap -i [eth0] -c [1000] -w [capture.pcapng]```\n\n**Apply a capture filter**\n\n```dumpcap -i [eth0] -f \"[tcp port 443]\" -w [capture.pcapng]```"],["SYNOPSIS","**dumpcap** [_options_]"],["PARAMETERS","**-i** _interface_\n> Interface to capture on. May be repeated to capture on several at once.\n\n**-w** _file_\n> Write packets to this file. Use **-** for standard output.\n\n**-D**\n> List the available interfaces and exit.\n\n**-L**\n> List the link-layer types supported by the interface and exit.\n\n**-c** _count_\n> Stop after this many packets.\n\n**-a** _condition_:_value_\n> Autostop condition: **duration**:_secs_, **filesize**:_kB_, **files**:_n_, or **packets**:_n_.\n\n**-b** _condition_:_value_\n> Ring buffer: rotate to a new file on **filesize**:_kB_, **duration**:_secs_, **interval**:_secs_, or **packets**:_n_, keeping at most **files**:_n_.\n\n**-f** _filter_\n> Capture filter in BPF syntax. Filtering in the kernel is far cheaper than capturing everything and discarding it later.\n\n**-p**\n> Do **not** put the interface into promiscuous mode. Promiscuous mode is the default, so this flag turns it off.\n\n**-s** _snaplen_\n> Snapshot length: bytes captured per packet. Useful for keeping headers while discarding payloads.\n\n**-P**\n> Write the output in pcap format instead of the default pcapng.\n\n**-q**\n> Quiet: do not print the running packet count."],["DESCRIPTION","**dumpcap** is a network traffic capture tool from the Wireshark project. It captures packets and writes them to files in pcapng or pcap format. Unlike Wireshark or tshark, dumpcap focuses solely on capture without protocol dissection.\n\nIts whole reason for existing is **privilege separation**. Capturing packets requires elevated privileges, while dissecting them means running an enormous body of parsing code over hostile input from the network, which is the last thing you want privileged. Wireshark and tshark therefore do not capture: they run dumpcap as a small, separate, privileged helper and read the result back unprivileged. Running dumpcap directly is simply using that helper on its own.\n\nBecause it does no dissection, it is also the fastest capture path in the suite and the right choice for long-running or high-rate captures where tshark would drop packets."],["RING BUFFER","Rotate to a new file every 100 MB, keeping the 10 most recent:\n\n```dumpcap -i [eth0] -b filesize:100000 -b files:10 -w [capture.pcapng]```\n\nThis is the standard way to capture continuously without filling the disk: the oldest file is discarded as each new one is opened. Note that **filesize** is in **kB**, so `filesize:100000` is roughly 100 MB, not 100 kB."],["CAVEATS","Capture needs root, or the `CAP_NET_RAW` and `CAP_NET_ADMIN` capabilities on the binary. Distributions usually handle this by installing dumpcap setuid-capable and restricting it to the `wireshark` group; adding yourself to that group is the supported way to capture without root, and is why running `wireshark` as root is both unnecessary and a bad idea.\n\nNote the inverted flag: **-p** *disables* promiscuous mode, which is on by default. Promiscuous mode also does far less than people expect on a switched network, where you will see your own traffic and broadcasts regardless, and need a mirror port or a tap to see anyone else's.\n\nUse **-f** rather than filtering later. A capture filter is compiled to BPF and runs in the kernel, so unwanted packets are never copied to userspace at all, whereas a display filter in Wireshark discards them only after they have been written to disk.\n\nCapture files grow startlingly fast on a busy link. A ring buffer, a snaplen, or both are the difference between a useful capture and a full disk."],["HISTORY","dumpcap is part of the **Wireshark** project, which **Gerald Combs** started in **1998** under the name **Ethereal**. It was split out of the main application after a long run of security advisories in the dissectors made the risk of a privileged monolith untenable: with the split, only the few hundred lines that actually touch the network run with elevated rights, while the millions of lines of protocol parsing do not."],["INSTALL","```apt: sudo apt install wireshark-common```\n\n```apk: sudo apk add wireshark-common```"],["SEE ALSO","[wireshark](/man/wireshark)(1), [tshark](/man/tshark)(1), [tcpdump](/man/tcpdump)(1), [editcap](/man/editcap)(1)"],["RESOURCES","```[Source code](https://gitlab.com/wireshark/wireshark)```\n\n```[Homepage](https://www.wireshark.org)```\n\n```[Documentation](https://www.wireshark.org/docs/man-pages/dumpcap.html)```"]],"e":["wireshark","tshark","tcpdump","editcap"],"x":"dumpcap -i [eth0] -w [capture.pcapng]"},"eix":{"n":"eix","t":"fast Gentoo package search utility","s":[["TAGLINE","fast Gentoo package search utility"],["TLDR","Search the Portage package cache by **name**\n\n```eix [query]```\n\nLimit results to packages that are **already installed**\n\n```eix --installed [query]```\n\nMatch against the package **description** text\n\n```eix --description \"[text]\"```\n\nFilter by **license** name\n\n```eix --license [license]```\n\nInvert a filter (e.g. packages **not** under a license)\n\n```eix --not --license [license]```"],["SYNOPSIS","**eix** [_options_] [_query_]"],["DESCRIPTION","**eix** searches local Gentoo packages quickly using a pre-built cache. It provides fast package lookups with detailed version and USE flag information.\n\nRun `eix-update` to update the local package cache before searching."],["PARAMETERS","**--installed**\n> Only show installed packages\n\n**--description** _text_\n> Search in package descriptions\n\n**--license** _license_\n> Search by license\n\n**--not**\n> Negate the following condition\n\n**-c, --compact**\n> Compact output format\n\n**-e, --exact**\n> Exact name match"],["CAVEATS","Gentoo Linux specific. Requires running eix-update to build cache. Cache must be updated after emerge --sync."],["SEE ALSO","[emerge](/man/emerge)(1), [equery](/man/equery)(1)"]],"e":["emerge","equery"],"x":"eix [query]"},"emulationstation":{"n":"emulationstation","t":"graphical front-end for retro gaming emulators","s":[["TAGLINE","graphical front-end for retro gaming emulators"],["TLDR","**Start EmulationStation**\n\n```emulationstation```\n\n**Start in windowed mode**\n\n```emulationstation --windowed```\n\n**Specify resolution**\n\n```emulationstation --resolution [1920] [1080]```\n\n**Force full UI reload**\n\n```emulationstation --force-full```\n\n**Show help**\n\n```emulationstation --help```"],["SYNOPSIS","**emulationstation** [_options_]"],["PARAMETERS","**--windowed**\n> Run in windowed mode.\n\n**--resolution** _width_ _height_\n> Set screen resolution.\n\n**--gamelist-only**\n> Skip systems without gamelists.\n\n**--ignore-gamelist**\n> Ignore gamelist.xml files.\n\n**--force-full**\n> Force full reload of UI.\n\n**--no-splash**\n> Do not show the splash screen on startup.\n\n**--no-exit**\n> Do not show the exit option in the menu.\n\n**--draw-framerate**\n> Display the framerate on screen.\n\n**--vsync** _1|0_\n> Turn VSync on or off (default: on).\n\n**--force-kiosk**\n> Force kiosk mode.\n\n**--force-kid**\n> Force kid UI mode.\n\n**--home** _path_\n> Specify a directory to use as the home path.\n\n**--debug**\n> Enable debug output and extra logging.\n\n**--help**, **-h**\n> Display help."],["DESCRIPTION","**EmulationStation** is a graphical front-end for organizing and launching emulated games. It provides a polished interface for browsing game collections, displaying metadata, and launching various emulators.\n\nThe application is commonly used on RetroPie, Batocera, and other retro gaming distributions. It supports themes, scraping game metadata and artwork, and controller navigation for a console-like experience."],["CONFIGURATION","```\n~/.emulationstation/\n├── es_systems.cfg     # System definitions\n├── es_settings.cfg    # Application settings\n├── gamelists/         # Per-system game metadata\n└── themes/            # Visual themes\n```"],["CAVEATS","Requires emulators to be configured separately. First-time setup needs es_systems.cfg. Theme compatibility varies. Scraping requires network access. Controller mapping may need configuration."],["HISTORY","EmulationStation was created by **Alec Lofquist** (Aloshi) in **2012**. It became the default front-end for RetroPie, the popular Raspberry Pi retro gaming distribution. The project has spawned forks like EmulationStation-DE for continued development."],["INSTALL","```aur: yay -S emulationstation```"],["SEE ALSO","[retroarch](/man/retroarch)(1), [mame](/man/mame)(1)"]],"e":["retroarch","mame"],"x":"emulationstation"},"flask":{"n":"flask","t":"Python web microframework CLI","s":[["TAGLINE","Python web microframework CLI"],["TLDR","**Run the development server**\n\n```flask run```\n\n**Run with debug mode** and auto-reload\n\n```flask run --debug```\n\n**Run on a specific host and port**\n\n```flask run --host [0.0.0.0] --port [8080]```\n\n**Start an interactive Python shell** with app context\n\n```flask shell```\n\n**List all routes** in the application\n\n```flask routes```\n\n**Initialize a database** (if using Flask-Migrate)\n\n```flask db init```\n\n**Run database migrations**\n\n```flask db upgrade```\n\n**Set the application module**\n\n```FLASK_APP=[app.py] flask run```"],["SYNOPSIS","**flask** [_--app module_] [_--debug_] _command_ [_options_]"],["PARAMETERS","**--app** _module_\n> Specify the Flask application module (or set FLASK_APP environment variable).\n\n**--debug** / **--no-debug**\n> Enable/disable debug mode with auto-reloader and debugger.\n\n**run** [_--host host_] [_--port port_]\n> Run the development server.\n\n**shell**\n> Open an interactive Python shell with application context.\n\n**routes** [_--sort endpoint|methods|rule_]\n> Show all registered URL rules.\n\n**--version**\n> Show Flask version.\n\n**--help**\n> Show help for a command."],["RUN OPTIONS","**--host** _address_\n> Network interface to bind to (default: 127.0.0.1).\n\n**--port** _port_\n> Port to listen on (default: 5000).\n\n**--reload** / **--no-reload**\n> Enable/disable auto-reload on code changes.\n\n**--debugger** / **--no-debugger**\n> Enable/disable the interactive debugger.\n\n**--cert** _path_\n> SSL certificate file for HTTPS.\n\n**--key** _path_\n> SSL key file for HTTPS."],["DESCRIPTION","**flask** is the command-line interface for Flask, a popular Python web microframework. It provides commands for running development servers, opening application shells, and executing custom commands defined by Flask extensions or the application.\n\nThe CLI discovers the Flask application through the **FLASK_APP** environment variable or **--app** option. It supports loading from modules (app), module:app patterns (myapp:create_app()), or factory functions.\n\nThe development server (flask run) includes an auto-reloader that restarts when code changes and an interactive debugger that appears in the browser on errors. These features should only be used in development, never in production.\n\nFlask extensions often add their own CLI commands. Flask-Migrate adds **flask db** commands for database migrations, Flask-Admin might add admin commands, etc. Applications can define custom commands using the **@app.cli.command()** decorator.\n\nThe shell command provides an interactive Python session with the application and its context pre-loaded, useful for testing and debugging."],["CAVEATS","The development server is not suitable for production use. Debug mode exposes sensitive information and should never be enabled in production. The server is single-threaded by default. Use a production WSGI server like Gunicorn or uWSGI for deployment."],["HISTORY","**Flask** was created by Armin Ronacher in **2010** as part of an April Fools' joke that became popular. The CLI was added in Flask 0.11 (**2016**), replacing the older flask-script extension and server startup method. Flask has become one of the most widely used Python web frameworks."],["INSTALL","```apk: sudo apk add py3-flask```"],["SEE ALSO","[gunicorn](/man/gunicorn)(1), [uvicorn](/man/uvicorn)(1), [python](/man/python)(1), [pip](/man/pip)(1), [django-admin](/man/django-admin)(1)"],["RESOURCES","```[Documentation](https://flask.palletsprojects.com/)```\n\n```[Source code](https://github.com/pallets/flask)```"]],"e":["gunicorn","uvicorn","python","pip","django-admin"],"x":"flask run"},"fossil-add":{"n":"fossil-add","t":"stage files for addition to the current Fossil check-out","s":[["TAGLINE","stage files for addition to the current Fossil check-out"],["TLDR","**Add a file** to the staging list\n\n```fossil add [file]```\n\n**Add every new file** in the check-out tree\n\n```fossil add .```\n\n**Show what would be added** without actually adding\n\n```fossil add --dry-run .```\n\n**Include dotfiles**\n\n```fossil add --dotfiles [path]```\n\n**Add files matching a pattern, ignoring others**\n\n```fossil add --ignore \"[*.log,build/*]\" [path]```\n\n**Undo pending adds** in the current check-out\n\n```fossil add --reset```\n\n**Add a file** whose name is reserved on Windows\n\n```fossil add --allow-reserved [file]```"],["SYNOPSIS","**fossil add** [_options_] _FILE_...\n\n**fossil add --reset** [**-v**] [**-n**]"],["PARAMETERS","**--case-sensitive** _BOOL_\n> Override the `case-sensitive` setting when matching file names.\n\n**--dotfiles**\n> Include files whose name begins with a dot (hidden files). Without this flag, fossil ignores them.\n\n**-f**, **--force**\n> Add files without prompting (useful when the file is outside the usual scope).\n\n**--ignore** _CSG_\n> Comma-separated glob patterns of unmanaged files to skip. Overrides the `ignore-glob` setting.\n\n**--clean** _CSG_\n> Comma-separated glob patterns of files to ignore for the `clean` operation. Overrides the `clean-glob` setting.\n\n**--reset**\n> Remove the ADDED status from files previously staged with `fossil add` (they remain on disk, just not tracked for the next commit).\n\n**--allow-reserved**\n> Permit filenames that are reserved on Windows (`CON`, `NUL`, `COM1`, …). Needed mainly on cross-platform repos.\n\n**-v**, **--verbose**\n> Report each file affected by `--reset`.\n\n**-n**, **--dry-run**\n> Show what would happen without touching the repository."],["DESCRIPTION","**fossil add** schedules one or more files (or directories, recursively) to be added to the repository on the next `fossil commit`. It does not write to the repository directly: staged additions live in the check-out metadata until committed.\n\nDotfiles and files matching the configured `ignore-glob` are skipped unless overridden. Directory arguments are walked recursively.\n\nUse `fossil addremove` to stage both new and missing files in one call, or `fossil add --reset` to undo pending additions."],["CAVEATS","`fossil add` only stages — nothing is visible to other clones until `fossil commit` runs. File names are case-sensitive by default on Unix and insensitive on Windows; mix platforms by setting `case-sensitive on` globally or per-repo. Symbolic links are added as symlinks only if `allow-symlinks on` is set."],["INSTALL","```apt: sudo apt install fossil```\n\n```dnf: sudo dnf install fossil```\n\n```pacman: sudo pacman -S fossil```\n\n```apk: sudo apk add fossil```\n\n```zypper: sudo zypper install fossil```\n\n```brew: brew install fossil```\n\n```nix: nix profile install nixpkgs#fossil```"],["SEE ALSO","[fossil-commit](/man/fossil-commit)(1), [fossil-rm](/man/fossil-rm)(1)"]],"e":["fossil-commit","fossil-rm"],"x":"fossil add [file]"},"freecad":{"n":"freecad","t":"open-source parametric 3D CAD modeler","s":[["TAGLINE","open-source parametric 3D CAD modeler"],["TLDR","**Start FreeCAD**\n\n```freecad```\n\n**Open file**\n\n```freecad [model.FCStd]```\n\n**Start in console mode** (no GUI)\n\n```freecad -c```\n\n**Run a Python script** in console mode\n\n```freecad -c [script.py]```\n\n**Import and export** a file to a different format\n\n```freecadcmd [model.FCStd] -o [output.step]```\n\n**Add an additional module path**\n\n```freecad -M [path/to/modules] [model.FCStd]```"],["SYNOPSIS","**freecad** [_options_] [_file_...]"],["PARAMETERS","_file_\n> File to open (.FCStd, .step, .iges, etc.).\n\n**-c**, **--console**\n> Start in console mode without GUI. Optionally followed by a Python script to execute.\n\n**-o** _file_\n> Output/export file (used with freecadcmd for format conversion).\n\n**-t**, **--run-test** _module_\n> Run the specified test module.\n\n**--write-log**\n> Write a log file to FreeCAD_Std.log in the user home directory.\n\n**-M** _path_\n> Add an additional module search path.\n\n**-u** _file_\n> Use the specified user configuration file.\n\n**-v**, **--version**\n> Show version information."],["CONFIGURATION","**~/.config/FreeCAD/user.cfg**\n> User preferences including workbench settings, appearance, units, and default paths.\n\n**~/.local/share/FreeCAD/Mod/**\n> User-installed workbenches and macros directory."],["DESCRIPTION","**FreeCAD** is an open-source parametric 3D CAD modeler. It supports multiple workbenches for different tasks: Part Design for mechanical parts, Sketcher for 2D drawings, Arch for architecture, and more.\n\nFreeCAD uses Python for scripting and macros, allowing automation and custom tools. It can import and export standard formats including STEP, IGES, STL, OBJ, and DXF."],["WORKBENCHES","- **Part Design**: Parametric solid modeling\n- **Sketcher**: 2D constraint-based sketching\n- **Draft**: 2D drawing tools\n- **Arch**: Architectural modeling\n- **FEM**: Finite element analysis\n- **Path**: CNC toolpath generation"],["CAVEATS","Large assemblies may be slow. Learning curve for parametric modeling. Some import/export formats have limitations. Stability varies by version."],["HISTORY","FreeCAD was started by **Jürgen Riegel** in **2002** and has been developed by a community of contributors. It became a significant open-source alternative to commercial CAD software, particularly for mechanical engineering and 3D printing."],["INSTALL","```pacman: sudo pacman -S freecad```\n\n```nix: nix profile install nixpkgs#freecad```"],["SEE ALSO","[openscad](/man/openscad)(1), [blender](/man/blender)(1), [meshlab](/man/meshlab)(1)"]],"e":["openscad","blender","meshlab"],"x":"freecad"},"git-lrc":{"n":"git-lrc","t":"AI-powered code review that runs on each git commit","s":[["TAGLINE","AI-powered code review that runs on each git commit"],["TLDR","**Set up and authenticate** the tool for the first time\n\n```lrc setup```\n\n**Run an AI review** on the currently staged changes\n\n```git add . && lrc review```\n\n**Commit changes** and trigger the automatic review\n\n```git commit -m \"[message]\"```\n\n**Skip the AI review** and take personal responsibility for the change\n\n```lrc review --vouch```\n\n**Bypass the review** entirely\n\n```lrc review --skip```\n\n**Manage AI connectors** or re-authenticate\n\n```lrc ui```\n\n**Enable or disable** the git hook integration\n\n```lrc hooks enable```"],["SYNOPSIS","**lrc** _command_ [_options_]\n\n**git lrc** _command_ [_options_]"],["DESCRIPTION","**git-lrc** (LiveReview Code) is an AI-powered code review tool that installs as a git pre-commit hook. After changes are staged, running **git commit** triggers an automatic AI review of the diff before the commit lands, catching bugs, security issues, and technical debt early. It can also be invoked manually as **lrc** or as the git subcommand **git lrc**.\n\nEach commit records the review outcome, for example **ran (iter:3, coverage:85%)** or **vouched**, so review history travels with the repository. The tool delegates the actual review to a configurable AI provider, defaulting to Gemini's free tier and also supporting OpenAI, Claude, DeepSeek, and OpenRouter endpoints.\n\ngit-lrc is written in Go by HexmosTech and is distributed under a source-available license."],["PARAMETERS","**setup**\n\n> Run the initial onboarding and authentication flow.\n\n**review**\n\n> Run an AI review on the currently staged changes.\n\n**review --vouch**\n\n> Skip the AI review and take personal responsibility for the change. The commit is logged as vouched.\n\n**review --skip**\n\n> Bypass the review entirely for this commit.\n\n**ui**\n\n> Open the connector management interface to configure AI providers or re-authenticate.\n\n**hooks enable** | **disable** | **status**\n\n> Enable, disable, or report the status of the git pre-commit integration."],["CAVEATS","The staged diff is sent to the configured cloud AI provider, so it is unsuitable for code that may not leave your machine. Installation goes through the vendor's **ipm** package manager rather than a system package repository. A free tier covers 30k lines of code per month, beyond which a paid plan is required."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-commit](/man/git-commit)(1), [claude](/man/claude)(1), [copilot](/man/copilot)(1), [git](/man/git)(1)"],["RESOURCES","```[Source code](https://github.com/HexmosTech/git-lrc)```\n\n```[Homepage](https://hexmos.com)```"]],"e":["git-commit","claude","copilot","git"],"x":"lrc setup"},"gurk":{"n":"gurk","t":"Signal Messenger client for the terminal","s":[["TAGLINE","Signal Messenger client for the terminal"],["TLDR","**Start gurk** and link to Signal account\n\n```gurk```\n\n**Start with a specific config file**\n\n```gurk --config [path/to/config.toml]```"],["SYNOPSIS","**gurk** [**--config** _path_]"],["DESCRIPTION","**gurk** is a Signal Messenger client that runs entirely in the terminal. It provides a TUI interface for sending and receiving encrypted messages through the Signal protocol, supporting both individual and group conversations.\n\nOn first launch, gurk displays a **QR code** that can be scanned with the Signal mobile app to link the terminal client as a secondary device. Once linked, messages sync between devices and conversations can be managed using keyboard shortcuts.\n\nThe client stores its data and configuration in a local directory, and supports customizable key bindings through a TOML configuration file."],["CAVEATS","Requires an existing Signal account on a mobile device for linking. Does not support creating new Signal accounts. Some Signal features like voice/video calls and stories are not available in the terminal client. Message history before linking is not synced."],["HISTORY","**gurk** (gurk-rs) was created by **boxdot** and is written in **Rust**. The name is German slang for cucumber. It is the only actively maintained Signal client for the terminal, providing privacy-focused messaging for headless systems and terminal-centric workflows."],["INSTALL","```pacman: sudo pacman -S gurk```\n\n```brew: brew install gurk```"],["SEE ALSO","[iamb](/man/iamb)(1), [weechat](/man/weechat)(1)"]],"e":["iamb","weechat"],"x":"gurk"},"hosts":{"n":"hosts","t":"Command-line editor for the system hosts file","s":[["TAGLINE","Command-line editor for the system hosts file"],["TLDR","**List** all hosts entries\n\n```hosts```\n\n**Search** entries matching a string or regex\n\n```hosts [localhost]```\n\n**Add** an entry\n\n```hosts add [127.0.0.1] [example.com]```\n\n**Remove** an entry\n\n```hosts remove [example.com]```\n\n**Block** a domain (IPv4 and IPv6)\n\n```hosts block [example.com]```\n\n**Unblock** a domain\n\n```hosts unblock [example.com]```\n\n**Disable** (comment out) an entry\n\n```hosts disable [example.com]```\n\n**Enable** a disabled entry\n\n```hosts enable [example.com]```\n\n**Create a backup**\n\n```hosts backups create```"],["SYNOPSIS","**hosts** [*command*] [*args*]"],["DESCRIPTION","**hosts** is a portable shell script for managing **/etc/hosts** (or the system hosts file): list and search entries, add/remove mappings, block/unblock domains, enable/disable lines by commenting, and maintain backups. It works with existing hosts files rather than replacing them.\n\nInstall via Homebrew (**xwmx/taps**), npm (**hosts.sh**), bpkg, **make install**, AUR, or by placing the script on **PATH**. Many operations need root privileges to write the system hosts file."],["PARAMETERS","*(no args)* / **list**\n\n> List hosts file entries.\n\n*pattern*\n\n> Search entries matching a string or regular expression.\n\n**add** *ip* *hostname*...\n\n> Add a mapping.\n\n**remove** *ip|host|regex*\n\n> Remove matching entries (prompts for confirmation).\n\n**block** / **unblock** *domain*\n\n> Add or remove blocking entries for a domain.\n\n**disable** / **enable** *entry*\n\n> Comment or uncomment entries.\n\n**backups** create|show|compare|...\n\n> Backup management for the hosts file.\n\n**completions install**\n\n> Install shell completions (often needs sudo)."],["CAVEATS","Editing the system hosts file usually requires **sudo**. Bad entries can break name resolution; keep backups. On some systems the hosts path differs; the script targets the platform default. Completions are optional when installing the script alone."],["INSTALL","```aur: yay -S hosts```"],["SEE ALSO","[hostess](/man/hostess)(1), [getent](/man/getent)(1), [dig](/man/dig)(1)"],["RESOURCES","```[Source code](https://github.com/xwmx/hosts)```"]],"e":["hostess","getent","dig"],"x":"hosts"},"httpie":{"n":"httpie","t":"user-friendly HTTP client for the command line","s":[["TAGLINE","user-friendly HTTP client for the command line"],["TLDR","**Simple GET request**\n\n```http [https://api.example.com/users]```\n\n**POST with JSON**\n\n```http POST [https://api.example.com/users] name=John age:=30```\n\n**POST form data**\n\n```http -f POST [url] name=John email=john@example.com```\n\n**Add custom header**\n\n```http [url] Authorization:\"Bearer [token]\"```\n\n**Download file**\n\n```http --download [https://example.com/file.zip]```\n\n**Authenticated request**\n\n```http -a [user:password] [url]```\n\n**Follow redirects**\n\n```http --follow [url]```\n\n**Show verbose request and response**\n\n```http --verbose [url]```\n\n**Show only response headers**\n\n```http --headers [url]```"],["SYNOPSIS","**http** [_options_] [_method_] _url_ [_items_...]"],["PARAMETERS","_method_\n> HTTP method (GET, POST, PUT, DELETE, etc.).\n\n**--json**, **-j**\n> JSON data (default).\n\n**--form**, **-f**\n> Form data.\n\n**--headers**, **-h**\n> Print only headers.\n\n**--body**, **-b**\n> Print only body.\n\n**--download**, **-d**\n> Download file.\n\n**--follow**, **-F**\n> Follow redirects.\n\n**--auth**, **-a** _user:pass_\n> Basic authentication.\n\n**--auth-type**, **-A** _type_\n> Auth type: basic, digest, bearer.\n\n**--verbose**, **-v**\n> Print entire HTTP exchange (request and response).\n\n**--print**, **-p** _what_\n> Selectively print: H (request headers), B (request body), h (response headers), b (response body), m (metadata).\n\n**--verify** _verify_\n> SSL certificate verification. Set to \"no\" to skip.\n\n**--timeout** _seconds_\n> Connection timeout in seconds (default: 30).\n\n**--proxy** _protocol:url_\n> Set proxy (e.g., http:http://proxy:8080).\n\n**--check-status**\n> Exit with error on HTTP 3xx/4xx/5xx status codes.\n\n**--output**, **-o** _file_\n> Save response body to file.\n\n**--session** _name_\n> Named session for persisting cookies/auth/headers.\n\n**--pretty** _mode_\n> Output formatting: all, colors, format, none.\n\n**--offline**\n> Build request offline without sending."],["ITEM TYPES","```\nkey=value       String data (request body)\nkey:=value      Raw JSON value (request body)\nkey==value      URL query parameter\nHeader:value    HTTP header\n@file           File upload (multipart)\nkey@file        File upload with field name\n```"],["DESCRIPTION","**HTTPie** is a user-friendly HTTP client for the command line. It provides intuitive syntax, colored output, and sensible defaults for API testing and debugging."],["CAVEATS","Requires Python. Syntax differs from curl. Large responses may be slow to colorize. The command is `http` (or `https`), not `httpie`."],["HISTORY","HTTPie was created by **Jakub Roztočil** in **2012** as a more user-friendly alternative to curl for interacting with HTTP APIs."],["INSTALL","```dnf: sudo dnf install httpie```\n\n```pacman: sudo pacman -S httpie```\n\n```apk: sudo apk add httpie```\n\n```zypper: sudo zypper install httpie```\n\n```brew: brew install httpie```\n\n```nix: nix profile install nixpkgs#httpie```"],["SEE ALSO","[curl](/man/curl)(1), [wget](/man/wget)(1), [xh](/man/xh)(1)"]],"e":["curl","wget","xh"],"x":"http [https://api.example.com/users]"},"i3-msg":{"n":"i3-msg","t":"sends commands to a running i3 window manager","s":[["TAGLINE","sends commands to a running i3 window manager"],["TLDR","**Switch workspace**\n\n```i3-msg workspace [3]```\n\n**Move window**\n\n```i3-msg move left```\n\n**Execute command**\n\n```i3-msg exec [firefox]```\n\n**Reload config**\n\n```i3-msg reload```\n\n**Get workspaces**\n\n```i3-msg -t get_workspaces```\n\n**Change layout**\n\n```i3-msg layout [tabbed]```"],["SYNOPSIS","**i3-msg** [_options_] _command_"],["PARAMETERS","_COMMAND_\n> i3 command to execute.\n\n**-t** _TYPE_\n> Message type (command, get_workspaces).\n\n**-s** _SOCKET_\n> Socket path.\n\n**-q**\n> Quiet mode.\n\n**--help**\n> Display help information."],["DESCRIPTION","**i3-msg** sends commands to a running i3 window manager. It provides scriptable control over windows, workspaces, and layouts.\n\nThe tool can execute any i3 command and query state information. It's essential for scripting and automation."],["CAVEATS","Requires running i3. Socket must be accessible. Commands must be valid i3 syntax."],["HISTORY","i3-msg is part of the **i3** window manager project for IPC communication."],["INSTALL","```apt: sudo apt install i3-wm```\n\n```pacman: sudo pacman -S i3-wm```\n\n```apk: sudo apk add i3wm```"],["SEE ALSO","[i3](/man/i3)(1), [swaymsg](/man/swaymsg)(1), [i3lock](/man/i3lock)(1)"]],"e":["i3","swaymsg","i3lock"],"x":"i3-msg workspace [3]"},"ipatool":{"n":"ipatool","t":"Search and download iOS App Store IPA packages from the command line","s":[["TAGLINE","Search and download iOS App Store IPA packages from the command line"],["TLDR","**Log in** to the App Store\n\n```ipatool auth login```\n\n**Show** current account info\n\n```ipatool auth info```\n\n**Search** for an app\n\n```ipatool search \"[term]\"```\n\n**Download** an app by bundle identifier\n\n```ipatool download -b [com.example.app]```\n\n**Purchase** an app (if required before download)\n\n```ipatool purchase -b [com.example.app]```\n\n**JSON output** for scripting\n\n```ipatool search \"[term]\" --format json```"],["SYNOPSIS","**ipatool** *command* [*options*]"],["DESCRIPTION","**ipatool** is a command-line client for the iOS (and tvOS) App Store. After authenticating with Apple ID credentials, you can search the store, purchase apps when needed, and download **.ipa** packages for devices or tooling workflows. It is commonly used for automation, archiving, and reverse-engineering research—not as a substitute for official App Store installation on a phone.\n\nGlobal options control output format and interactivity. Subcommands cover **auth**, **search**, **download**, **purchase**, and related operations; run **ipatool** *command* **--help** for full flags."],["PARAMETERS","**auth login** | **auth info** | **auth revoke**\n\n> Manage App Store session credentials.\n\n**search** *term*\n\n> Search iOS/tvOS apps. **-l**, **--limit** caps results; **--platform** selects **iphone**, **ipad**, or **appletv**.\n\n**download**\n\n> Download an IPA (typically **-b** / **--bundle-identifier**). See **--help** for version and output options.\n\n**purchase**\n\n> Purchase an app so it can be downloaded with the current account.\n\n**--format** *text*|*json*\n\n> Output format (default: text).\n\n**--non-interactive**\n\n> Do not prompt; fail if input would be required.\n\n**--verbose**\n\n> Verbose logging.\n\n**-h**, **--help**\n\n> Help for the command or subcommand."],["CAVEATS","Requires a valid Apple ID and compliance with Apple's terms. Some apps are region-locked, paid, or refuse bulk download. IPAs obtained this way are not sideloadable onto stock iOS without additional signing tooling. Do not use for piracy."],["INSTALL","```brew: brew install ipatool```\n\n```nix: nix profile install nixpkgs#ipatool```"],["SEE ALSO","[ideviceinstaller](/man/ideviceinstaller)(1), [ios-deploy](/man/ios-deploy)(1)"],["RESOURCES","```[Source code](https://github.com/majd/ipatool)```"]],"e":["ideviceinstaller","ios-deploy"],"x":"ipatool auth login"},"kubectl-describe":{"n":"kubectl-describe","t":"shows detailed resource information","s":[["TAGLINE","shows detailed resource information"],["TLDR","**Describe pod**\n\n```kubectl describe pod [pod-name]```\n\n**Describe node**\n\n```kubectl describe node [node-name]```\n\n**Describe deployment**\n\n```kubectl describe deployment [deployment-name]```\n\n**Describe with selector**\n\n```kubectl describe pods -l [app=myapp]```\n\n**Describe in namespace**\n\n```kubectl describe pod [pod-name] -n [namespace]```"],["SYNOPSIS","**kubectl describe** [_options_] _resource_ [_name_]"],["PARAMETERS","_RESOURCE_\n> Resource type (pod, node, deployment, etc.).\n\n_NAME_\n> Resource name (optional).\n\n**-l** _SELECTOR_\n> Label selector.\n\n**-n** _NAMESPACE_\n> Target namespace.\n\n**--all-namespaces**\n> Search all namespaces.\n\n**--help**\n> Display help information."],["DESCRIPTION","**kubectl describe** produces a detailed, human-readable summary of one or more Kubernetes resources. Unlike `kubectl get`, which returns structured data fields, describe aggregates information from multiple API calls to present a comprehensive view that includes resource metadata, spec, current status, conditions, allocated resources, mounted volumes, and associated events.\n\nThe command is particularly valuable for troubleshooting, as the Events section at the bottom of the output shows recent actions taken by controllers, schedulers, and the kubelet -- such as image pull failures, scheduling decisions, readiness probe results, and container restarts. For nodes, it displays capacity, allocatable resources, running pods, and system conditions.\n\nOutput from describe is formatted for human consumption and should not be parsed programmatically. For structured data suitable for scripting, use `kubectl get -o json` or `kubectl get -o yaml` instead."],["CAVEATS","Subcommand of kubectl. Output not for parsing. Use get -o json for structured data."],["HISTORY","kubectl describe provides detailed resource inspection for **Kubernetes** troubleshooting."],["INSTALL","```apt: sudo apt install kubectl```\n\n```pacman: sudo pacman -S kubectl```\n\n```apk: sudo apk add kubectl```\n\n```brew: brew install kubectl```\n\n```nix: nix profile install nixpkgs#kubectl```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [kubectl-get](/man/kubectl-get)(1), [kubectl-logs](/man/kubectl-logs)(1)"]],"e":["kubectl","kubectl-get","kubectl-logs"],"x":"kubectl describe pod [pod-name]"},"lddd":{"n":"lddd","t":"scans the system for packages with broken shared library links","s":[["TAGLINE","scans the system for packages with broken shared library links"],["TLDR","Find packages with **broken library links**\n\n```lddd```"],["SYNOPSIS","**lddd**"],["DESCRIPTION","**lddd** scans the system for packages with broken shared library links. It identifies binaries that depend on libraries that have been removed or updated, indicating packages that may need to be rebuilt.\n\nThis tool is specifically designed for Arch Linux systems and helps maintain system health after library updates."],["CAVEATS","Only available on Arch Linux. May take considerable time on systems with many packages. Requires root privileges for complete system scanning."],["SEE ALSO","[ldd](/man/ldd)(1), [pacman](/man/pacman)(8), [checkupdates](/man/checkupdates)(8)"]],"e":["ldd","pacman","checkupdates"],"x":"lddd"},"lerna":{"n":"lerna","t":"manages JavaScript monorepos","s":[["TAGLINE","manages JavaScript monorepos"],["TLDR","**Initialize monorepo**\n\n```lerna init```\n\n**Bootstrap packages**\n\n```lerna bootstrap```\n\n**Run script in all packages**\n\n```lerna run [build]```\n\n**Run in specific package**\n\n```lerna run [build] --scope=[package-name]```\n\n**Publish packages**\n\n```lerna publish```\n\n**List packages**\n\n```lerna list```\n\n**Version packages**\n\n```lerna version```"],["SYNOPSIS","**lerna** _command_ [_options_]"],["PARAMETERS","**init**\n> Initialize monorepo.\n\n**bootstrap**\n> Install dependencies.\n\n**run** _SCRIPT_\n> Run npm script.\n\n**publish**\n> Publish packages.\n\n**version**\n> Bump versions.\n\n**list**\n> List packages.\n\n**--scope** _PKG_\n> Target specific package.\n\n**--help**\n> Display help information."],["DESCRIPTION","**Lerna** manages JavaScript monorepos. It handles multiple packages in a single repository.\n\nThe tool coordinates versioning and publishing. It links local dependencies and runs scripts across packages."],["CAVEATS","Consider npm/yarn workspaces. Nx for advanced features. Node.js required."],["HISTORY","Lerna was created by **Sebastian McKenzie** (Babel author) for managing monorepos, now maintained by Nx."],["INSTALL","```brew: brew install lerna```\n\n```nix: nix profile install nixpkgs#lerna```"],["SEE ALSO","[npm](/man/npm)(1), [yarn](/man/yarn)(1), [nx](/man/nx)(1)"]],"e":["npm","yarn","nx"],"x":"lerna init"},"lf":{"n":"lf","t":"terminal file manager","s":[["TAGLINE","terminal file manager"],["TLDR","**Start file manager**\n\n```lf```\n\n**Open specific directory**\n\n```lf [/path/to/dir]```\n\n**Start with command**\n\n```lf -command \"[set hidden]\"```\n\n**Print selected files**\n\n```lf -print-selection```\n\n**Use specific config**\n\n```lf -config [config.lf]```"],["SYNOPSIS","**lf** [_options_] [_path_]"],["PARAMETERS","_PATH_\n> Starting directory.\n\n**-command** _CMD_\n> Execute command on start.\n\n**-config** _FILE_\n> Config file path.\n\n**-print-selection**\n> Print selection on exit.\n\n**--help**\n> Display help information."],["DESCRIPTION","**lf** is a terminal file manager. It provides fast navigation with vim-like keybindings.\n\nThe tool is written in Go for portability. It supports preview, bulk operations, and scripting."],["CAVEATS","Terminal-based. Vim keybindings. Configuration via lfrc."],["HISTORY","lf was created as a **ranger**-inspired file manager written in Go for better performance and simplicity."],["INSTALL","```apt: sudo apt install lf```\n\n```pacman: sudo pacman -S lf```\n\n```apk: sudo apk add lf```\n\n```zypper: sudo zypper install lf```\n\n```brew: brew install lf```\n\n```nix: nix profile install nixpkgs#lf```"],["SEE ALSO","[ranger](/man/ranger)(1), [nnn](/man/nnn)(1), [mc](/man/mc)(1)"]],"e":["ranger","nnn","mc"],"x":"lf"},"llvm-strings":{"n":"llvm-strings","t":"extracts printable strings from binary files","s":[["TAGLINE","extracts printable strings from binary files"],["TLDR","**Extract strings from binary**\n\n```llvm-strings [file]```\n\n**Set minimum string length**\n\n```llvm-strings -n [8] [file]```\n\n**Print file offset with strings**\n\n```llvm-strings -t x [file]```\n\n**Process all sections**\n\n```llvm-strings -a [file]```"],["SYNOPSIS","**llvm-strings** [_options_] [_file_...]"],["PARAMETERS","**-n** _length_\n> Minimum string length (default 4).\n\n**-t** _format_\n> Print offset (d=decimal, o=octal, x=hex).\n\n**-a**, **--all**\n> Scan entire file, not just data sections."],["DESCRIPTION","**llvm-strings** extracts printable strings from binary files. Drop-in replacement for GNU strings. Useful for analyzing executables, finding embedded text, and reverse engineering."],["INSTALL","```apt: sudo apt install llvm```\n\n```dnf: sudo dnf install llvm```\n\n```pacman: sudo pacman -S llvm```\n\n```apk: sudo apk add llvm```\n\n```zypper: sudo zypper install llvm```\n\n```brew: brew install llvm```\n\n```nix: nix profile install nixpkgs#llvm```"],["SEE ALSO","[strings](/man/strings)(1), [llvm-objdump](/man/llvm-objdump)(1)"]],"e":["strings","llvm-objdump"],"x":"llvm-strings [file]"},"lxc-start":{"n":"lxc-start","t":"starts a stopped Linux container","s":[["TAGLINE","starts a stopped Linux container"],["TLDR","Start **lxc service**\n\n```systemctl start lxc-net```\n\n**Start** container\n\n```sudo lxc-start [container_name]```\n\nStart in **foreground**\n\n```sudo lxc-start [container_name] --foreground```\n\nStart with **debug logging**\n\n```sudo lxc-start [container_name] -l DEBUG -o [path/to/logfile]```\n\nDisplay **help**\n\n```lxc-start --help```"],["SYNOPSIS","**lxc-start** **-n** _name_ [_options_]"],["DESCRIPTION","**lxc-start** starts a stopped Linux container. By default it runs in the background. Use foreground mode for debugging."],["PARAMETERS","**-n, --name NAME**\n> Container name\n\n**-F, --foreground**\n> Run in foreground\n\n**-d, --daemon**\n> Run as daemon (default)\n\n**-l, --logpriority LEVEL**\n> Log priority (DEBUG, INFO, etc.)\n\n**-o, --logfile FILE**\n> Log output file\n\n**-p, --pidfile FILE**\n> PID file for daemon mode\n\n**-?, --help**\n> Display help information"],["CAVEATS","Requires root privileges. Container must exist and be stopped. Use lxc-stop to stop foreground containers."],["INSTALL","```apt: sudo apt install lxc```\n\n```dnf: sudo dnf install lxc```\n\n```pacman: sudo pacman -S lxc```\n\n```apk: sudo apk add lxc```\n\n```zypper: sudo zypper install lxc```\n\n```brew: brew install lxc```\n\n```nix: nix profile install nixpkgs#lxc```"],["SEE ALSO","[lxc-stop](/man/lxc-stop)(1), [lxc-create](/man/lxc-create)(1), [lxc-attach](/man/lxc-attach)(1)"]],"e":["lxc-stop","lxc-create","lxc-attach"],"x":"systemctl start lxc-net"},"mimetype":{"n":"mimetype","t":"determines the MIME type of files using the shared-mime-info database","s":[["TAGLINE","determines the MIME type of files using the shared-mime-info database"],["TLDR","Print the **MIME type** of a given file\n\n```mimetype [path/to/file]```\n\nDisplay only the MIME type, **not the filename**\n\n```mimetype --brief [path/to/file]```\n\nDisplay a **description** of the MIME type\n\n```mimetype --describe [path/to/file]```\n\nDetermine the MIME type of **stdin**\n\n```[command] | mimetype --stdin```\n\nDisplay **debug information** about how the MIME type was determined\n\n```mimetype --debug [path/to/file]```\n\nDisplay **all possible MIME types** in confidence order\n\n```mimetype --all [path/to/file]```\n\nSpecify the **language code** of the output\n\n```mimetype --language [path/to/file]```"],["SYNOPSIS","**mimetype** [_options_] _files_..."],["PARAMETERS","**--brief, -b**\n> Output only the MIME type without the filename\n\n**--describe, -d**\n> Output a human-readable description of the MIME type\n\n**--stdin, -i**\n> Read data from standard input instead of a file\n\n**--debug, -D**\n> Show debugging information about type detection\n\n**--all, -a**\n> List all possible MIME types with confidence scores\n\n**--language _code_**\n> Set the output language using a 2-letter code"],["DESCRIPTION","**mimetype** determines the MIME type of files using the shared-mime-info database. It examines file contents (magic bytes) and filename patterns to identify the type, similar to the **file** command but outputting standard MIME types.\n\nThe tool is part of the File::MimeInfo Perl module and uses the freedesktop.org shared MIME database. It can detect thousands of file types including documents, images, audio, video, and application-specific formats.\n\nUnlike simple extension-based detection, mimetype examines actual file contents, making it more reliable for files with incorrect or missing extensions."],["CAVEATS","Requires the shared-mime-info database to be installed. Detection accuracy depends on the quality and completeness of magic patterns in the database. Some proprietary formats may not be recognized."],["INSTALL","```pacman: sudo pacman -S perl-file-mimeinfo```\n\n```apk: sudo apk add perl-file-mimeinfo```"],["SEE ALSO","[file](/man/file)(1), [xdg-mime](/man/xdg-mime)(1)"]],"e":["file","xdg-mime"],"x":"mimetype [path/to/file]"},"mlocate":{"n":"mlocate","t":"finds files by searching a pre-built database rather than scanning","s":[["TAGLINE","finds files by searching a pre-built database rather than scanning"],["TLDR","**Find files by name**\n\n```locate [pattern]```\n\n**Case-insensitive search**\n\n```locate -i [pattern]```\n\n**Update database**\n\n```sudo updatedb```\n\n**Show only existing files**\n\n```locate -e [pattern]```\n\n**Limit results**\n\n```locate -l [10] [pattern]```\n\n**Count matches**\n\n```locate -c [pattern]```"],["SYNOPSIS","**locate** [_options_] _pattern_..."],["PARAMETERS","**-i**, **--ignore-case**\n> Case-insensitive match.\n\n**-l** _n_, **--limit** _n_\n> Limit output to n entries.\n\n**-c**, **--count**\n> Print count only.\n\n**-e**, **--existing**\n> Only existing files.\n\n**-b**, **--basename**\n> Match basename only.\n\n**-r**, **--regexp**\n> Use regex pattern.\n\n**-d** _path_\n> Use specific database."],["DESCRIPTION","**mlocate** (merging locate) finds files by searching a pre-built database rather than scanning the filesystem. This makes searches extremely fast.\n\nThe database is typically updated daily by cron, or manually with updatedb."],["DATABASE","```\nDefault: /var/lib/mlocate/mlocate.db\nConfig:  /etc/updatedb.conf"],["Update database","sudo updatedb\n```"],["CAVEATS","Database may be outdated. New files won't appear until updatedb runs. Some paths excluded by default. Requires updatedb for fresh results."],["HISTORY","mlocate was created by **Miloslav Trmač** as an improvement over GNU locate, using a more efficient database format that tracks file changes."],["INSTALL","```nix: nix profile install nixpkgs#mlocate```"],["SEE ALSO","[updatedb](/man/updatedb)(8), [find](/man/find)(1), [fd](/man/fd)(1), [plocate](/man/plocate)(1)"]],"e":["updatedb","find","fd","plocate"],"x":"locate [pattern]"},"mnemo":{"n":"mnemo","t":"Local-first AI memory layer with a knowledge graph and CLI","s":[["TAGLINE","Local-first AI memory layer with a knowledge graph and CLI"],["TLDR","**Store** a memory chunk for later retrieval\n\n```mnemo ingest \"I prefer Neovim with dark mode\"```\n\n**Search** for relevant context to inject into a prompt\n\n```mnemo search \"what editor do I use?\"```\n\n**List** extracted entities\n\n```mnemo entities```\n\n**Show** one entity and its graph neighbors\n\n```mnemo entity <uuid> --neighbors```\n\n**Check** server and database health\n\n```mnemo health```\n\n**Wipe** all stored memory (with confirmation)\n\n```mnemo wipe --yes```"],["SYNOPSIS","**mnemo** _subcommand_ [_arguments_] [_options_]"],["DESCRIPTION","**mnemo** is the command-line client for a local AI memory service. It talks to **mnemo-api** (default `http://localhost:8080`), which ingests text, uses an OpenAI-compatible LLM to extract entities and relationships, stores them in SQLite, and maintains an in-memory **petgraph** knowledge graph for multi-hop retrieval.\n\nTypical workflow: run **mnemo-api** (or `docker compose up` from the project), ingest conversations or notes with **mnemo ingest**, then call **mnemo search** to obtain a ranked **context_prompt** string suitable for injection into any LLM application. Backends include **Ollama** (fully local), OpenAI, Anthropic, or any OpenAI-compatible API.\n\nThe server exposes REST endpoints (`/ingest`, `/retrieve`, `/entities`, `/chunks`, `/search`, `/stats`, `/wipe`). The CLI is a thin wrapper around those endpoints using blocking HTTP."],["PARAMETERS","**ingest** _text_\n> POST text to `/ingest` for entity extraction and graph storage.\n\n**search** _query_\n> Retrieve ranked memory context for a natural-language query.\n\n**entities**\n> List stored entities (paginated on the server).\n\n**entity** _uuid_ [**--neighbors**]\n> Show one entity; optional graph neighbor traversal.\n\n**chunks**\n> List raw memory chunks.\n\n**health**\n> Report API, database, and LLM connectivity.\n\n**stats**\n> Print entity, chunk, and graph counts plus uptime.\n\n**wipe** [**--yes**]\n> Delete all memory; skips the confirmation prompt when **--yes** is set.\n\n**--server** _url_\n> Use a non-default API base URL (default `http://localhost:8080`)."],["CONFIGURATION","The API reads environment variables (or `mnemo.example.toml` via **--config**):\n\n| Variable | Default | Purpose |\n|----------|---------|---------|\n| **MNEMO_DB_PATH** | `mnemo.db` | SQLite database file |\n| **MNEMO_PORT** | `8080` | API listen port |\n| **MNEMO_LLM_BASE_URL** | `http://localhost:11434/v1` | OpenAI-compatible LLM endpoint |\n| **MNEMO_LLM_MODEL** | `llama3` | Model for extraction |\n| **MNEMO_LLM_API_KEY** | `ollama` | API key (any value works for Ollama) |\n| **MNEMO_LLM_PROVIDER** | `ollama` | `ollama`, `openai`, `anthropic`, or `custom` |\n\nEnvironment variables override TOML values."],["CAVEATS","**mnemo** requires **mnemo-api** to be running. Entity extraction quality depends on the configured LLM. **mnemo wipe** is irreversible. Install the CLI with `cargo install --path crates/mnemo-cli` from the upstream repository."],["INSTALL","```aur: yay -S mnemo```"],["SEE ALSO","[ollama](/man/ollama)(1), [curl](/man/curl)(1), [docker](/man/docker)(1)"]],"e":["ollama","curl","docker"],"x":"mnemo ingest \"I prefer Neovim with dark mode\""},"molecule":{"n":"molecule","t":"testing framework for Ansible roles","s":[["TAGLINE","testing framework for Ansible roles"],["TLDR","**Initialize a test scenario** in the current role\n\n```molecule init scenario```\n\n**Run full test sequence**\n\n```molecule test```\n\n**Create test instances**\n\n```molecule create```\n\n**Run playbook against instances**\n\n```molecule converge```\n\n**Verify instance state**\n\n```molecule verify```\n\n**Login to instance**\n\n```molecule login```\n\n**Destroy test instances**\n\n```molecule destroy```"],["SYNOPSIS","**molecule** _command_ [_--scenario-name name_] [_options_]"],["PARAMETERS","**init role** _NAME_\n> Create new Ansible role with Molecule.\n\n**init scenario**\n> Add scenario to existing role.\n\n**test**\n> Run full test sequence.\n\n**create**\n> Create instances.\n\n**converge**\n> Run playbook.\n\n**verify**\n> Run verification tests.\n\n**destroy**\n> Destroy instances.\n\n**login** [_HOST_]\n> Login to instance shell.\n\n**list**\n> List instances and status.\n\n**lint**\n> Run linters.\n\n**cleanup**\n> Run cleanup playbook.\n\n**-s** _NAME_, **--scenario-name** _NAME_\n> Target scenario.\n\n**-d** _NAME_, **--driver-name** _NAME_\n> Driver: docker, podman, delegated, vagrant.\n\n**--all**\n> Run against all scenarios.\n\n**--parallel**\n> Run scenarios in parallel.\n\n**--destroy** _WHEN_\n> When to destroy: always, never, passing."],["DESCRIPTION","**Molecule** is a testing framework for Ansible roles. It creates test instances, runs playbooks against them, verifies results, and cleans up - enabling test-driven infrastructure development.\n\nA scenario defines how to test a role: which driver creates instances, which playbooks run, and which verifier checks results. Default scenarios use Docker but Podman, Vagrant, and cloud providers are supported.\n\nThe test sequence runs: lint, cleanup, destroy, dependency, syntax, create, prepare, converge, idempotence, verify, cleanup, destroy. Individual stages can run separately during development.\n\nVerification typically uses Ansible's assert module or Testinfra for Python-based tests. Tests check that the role achieved the desired state: files exist, services run, configurations are correct.\n\nIdempotence testing runs the playbook twice, failing if changes occur on the second run. This ensures roles are properly idempotent.\n\nCI integration works with GitHub Actions, GitLab CI, and other platforms. The test command returns appropriate exit codes for automation."],["CAVEATS","Docker/Podman required for containerized testing. Some roles need privileged containers. Windows testing is limited. Large test matrices can be slow. Cloud drivers incur costs. Systemd in containers needs special setup."],["HISTORY","**Molecule** was created by **Cisco** engineers and released around **2015** to improve Ansible role testing. Red Hat later adopted the project. It replaced ad-hoc testing approaches with a structured framework, becoming the de facto standard for Ansible role testing."],["INSTALL","```pacman: sudo pacman -S molecule```\n\n```zypper: sudo zypper install molecule```\n\n```brew: brew install molecule```\n\n```nix: nix profile install nixpkgs#molecule```"],["SEE ALSO","[ansible](/man/ansible)(1), [ansible-lint](/man/ansible-lint)(1), [docker](/man/docker)(1)"]],"e":["ansible","ansible-lint","docker"],"x":"molecule init scenario"},"monodis":{"n":"monodis","t":"disassembles","s":[["TAGLINE","disassembles"],["TLDR","**Disassemble .NET assembly**\n\n```monodis [assembly.dll]```\n\n**Show assembly metadata**\n\n```monodis --assembly [assembly.dll]```\n\n**Show type definitions**\n\n```monodis --typedef [assembly.dll]```\n\n**Output to file**\n\n```monodis [assembly.dll] > [output.il]```"],["SYNOPSIS","**monodis** [_options_] _assembly_"],["PARAMETERS","**--assembly**\n> Show assembly table.\n\n**--typedef**\n> Show type definitions.\n\n**--typeref**\n> Show type references.\n\n**--methoddef**\n> Show method definitions.\n\n**--method** _name_\n> Disassemble specific method.\n\n**--output** _file_\n> Output to file.\n\n**--mscorlib**\n> Reference mscorlib for disassembly."],["DESCRIPTION","**monodis** disassembles .NET assemblies (DLL/EXE) to CIL (Common Intermediate Language) text. Part of the Mono framework. Used for debugging, reverse engineering, and understanding .NET bytecode."],["INSTALL","```apt: sudo apt install mono-utils```\n\n```pacman: sudo pacman -S mono```\n\n```apk: sudo apk add mono```\n\n```brew: brew install mono```\n\n```nix: nix profile install nixpkgs#mono```"],["SEE ALSO","[monop](/man/monop)(1)"]],"e":["monop"],"x":"monodis [assembly.dll]"},"mpremote":{"n":"mpremote","t":"microPython remote control tool","s":[["TAGLINE","microPython remote control tool"],["TLDR","**Connect to device**\n\n```mpremote```\n\n**Run Python code**\n\n```mpremote exec \"[print('hello')]\"```\n\n**Copy file to device**\n\n```mpremote cp [local.py] :[remote.py]```\n\n**Copy file from device**\n\n```mpremote cp :[remote.py] [local.py]```\n\n**Run script on device**\n\n```mpremote run [script.py]```\n\n**Enter REPL**\n\n```mpremote repl```\n\n**List files on device**\n\n```mpremote ls```\n\n**Mount local directory**\n\n```mpremote mount [.]```"],["SYNOPSIS","**mpremote** [_command_] [_options_]"],["PARAMETERS","_COMMAND_\n> Operation to perform.\n\n**connect** _DEVICE_\n> Connect to specific device.\n\n**exec** _CODE_\n> Execute Python code.\n\n**cp** _SRC_ _DST_\n> Copy files (: prefix for device).\n\n**run** _FILE_\n> Run local script on device.\n\n**repl**\n> Enter interactive REPL.\n\n**--help**\n> Display help information."],["DESCRIPTION","**mpremote** is the MicroPython remote control tool. It manages MicroPython devices.\n\nThe tool copies files, runs code, and provides REPL access. Works over USB serial."],["CAVEATS","Requires MicroPython device. Serial connection needed. Python 3 required."],["HISTORY","mpremote was developed by the **MicroPython** project as the official device management tool."],["INSTALL","```zypper: sudo zypper install mpremote```\n\n```brew: brew install mpremote```\n\n```nix: nix profile install nixpkgs#mpremote```"],["SEE ALSO","[python](/man/python)(1)"]],"e":["python"],"x":"mpremote"},"msgconv":{"n":"msgconv","t":"converts PO files between character encodings","s":[["TAGLINE","converts PO files between character encodings"],["TLDR","**Convert PO file encoding**\n\n```msgconv -t [UTF-8] [input.po] -o [output.po]```\n\n**Convert to specific encoding**\n\n```msgconv --to-code=[ISO-8859-1] [file.po]```\n\n**Convert and output to stdout**\n\n```msgconv -t [UTF-8] [input.po]```\n\n**Force conversion**\n\n```msgconv --force [input.po] -o [output.po]```"],["SYNOPSIS","**msgconv** [_options_] [_inputfile_]"],["PARAMETERS","_INPUTFILE_\n> PO file to convert.\n\n**-t** _ENCODING_\n> Target encoding.\n\n**--to-code** _ENCODING_\n> Target character encoding.\n\n**-o** _FILE_\n> Output file.\n\n**--force**\n> Force conversion.\n\n**--help**\n> Display help information."],["DESCRIPTION","**msgconv** converts PO files between character encodings. It's part of GNU gettext.\n\nThe tool handles translation file encoding. Preserves message structure during conversion."],["CAVEATS","Part of gettext-tools. May lose data in incompatible conversions. UTF-8 recommended."],["HISTORY","msgconv is part of **GNU gettext**, providing internationalization tools since 1995."],["INSTALL","```apt: sudo apt install gettext```\n\n```dnf: sudo dnf install gettext```\n\n```pacman: sudo pacman -S gettext```\n\n```apk: sudo apk add gettext```\n\n```brew: brew install gettext```\n\n```nix: nix profile install nixpkgs#gettext```"],["SEE ALSO","[msgen](/man/msgen)(1), [msgfmt](/man/msgfmt)(1), [msgmerge](/man/msgmerge)(1)"]],"e":["msgen","msgfmt","msgmerge"],"x":"msgconv -t [UTF-8] [input.po] -o [output.po]"},"ng-config":{"n":"ng-config","t":"Retrieves or sets Angular CLI configuration values","s":[["TAGLINE","Retrieves or sets Angular CLI configuration values"],["TLDR","**Get a configuration value**\n\n```ng config [jsonPath]```\n\n**Set a configuration value**\n\n```ng config [jsonPath] [value]```\n\n**Get the schematic collections**\n\n```ng config cli.schematicCollections```\n\n**Set the default package manager**\n\n```ng config cli.packageManager [npm|yarn|pnpm]```\n\n**Get global configuration value**\n\n```ng config --global [jsonPath]```\n\n**Show all project configuration**\n\n```ng config```"],["SYNOPSIS","**ng config** [_jsonPath_] [_value_] [_options_]"],["PARAMETERS","_jsonPath_\n> JSON path to the configuration value using dot notation (e.g., **projects.myApp.architect.build**).\n\n_value_\n> New value to set at the specified JSON path. If omitted, the current value is printed.\n\n**--global**, **-g**\n> Access the global Angular CLI configuration instead of the project-level angular.json.\n\n**--help**\n> Display help information."],["DESCRIPTION","**ng config** reads and writes values in the Angular workspace configuration file (**angular.json**). Configuration values are accessed using JSON path dot notation. When only a path is provided, the current value is displayed. When a value is also provided, the configuration is updated.\n\nThis command is useful for scripting Angular workspace setup and for inspecting or modifying build configurations, default schematics, and CLI behavior."],["CAVEATS","Part of Angular CLI. The configuration file must be valid JSON. Invalid JSON paths will produce errors. Changes to angular.json affect all developers working on the project."],["HISTORY","**ng config** was introduced with **Angular CLI 6** in **2018** as part of the workspace configuration overhaul that replaced **.angular-cli.json** with **angular.json**."],["SEE ALSO","[ng](/man/ng)(1), [ng-new](/man/ng-new)(1), [ng-build](/man/ng-build)(1)"]],"e":["ng","ng-new","ng-build"],"x":"ng config [jsonPath]"},"ng-extract-i18n":{"n":"ng-extract-i18n","t":"extracts internationalization messages from Angular templates","s":[["TAGLINE","extracts internationalization messages from Angular templates"],["TLDR","**Extract messages**\n\n```ng extract-i18n```\n\n**Extract to specific format**\n\n```ng extract-i18n --format [xlf]```\n\n**Extract to output path**\n\n```ng extract-i18n --output-path [locale/]```\n\n**Extract for specific project**\n\n```ng extract-i18n --project [my-app]```"],["SYNOPSIS","**ng extract-i18n** [_options_]"],["PARAMETERS","**--format** _format_\n> Output format (xlf, xlf2, xmb, json, arb).\n\n**--output-path** _path_\n> Output directory.\n\n**--out-file** _file_\n> Output filename.\n\n**--project** _name_\n> Project to extract from.\n\n**--progress**\n> Show progress."],["DESCRIPTION","**ng extract-i18n** extracts internationalization messages from Angular templates. Generates translation source files for localization workflow. Supports XLIFF, XMB, and JSON formats."],["SEE ALSO","[ng](/man/ng)(1), [ng-build](/man/ng-build)(1)"]],"e":["ng","ng-build"],"x":"ng extract-i18n"},"nkf":{"n":"nkf","t":"converts Japanese text encodings","s":[["TAGLINE","converts Japanese text encodings"],["TLDR","**Convert to UTF-8**\n\n```nkf -w [input.txt] > [output.txt]```\n\n**Convert to Shift-JIS**\n\n```nkf -s [input.txt] > [output.txt]```\n\n**Convert to EUC-JP**\n\n```nkf -e [input.txt] > [output.txt]```\n\n**Auto detect and convert**\n\n```nkf -w --guess [input.txt]```\n\n**Convert in place**\n\n```nkf -w --overwrite [file.txt]```\n\n**Show input encoding**\n\n```nkf --guess [file.txt]```"],["SYNOPSIS","**nkf** [_options_] [_files_]"],["PARAMETERS","_FILES_\n> Files to convert.\n\n**-w**\n> Convert to UTF-8.\n\n**-s**\n> Convert to Shift-JIS.\n\n**-e**\n> Convert to EUC-JP.\n\n**--guess**\n> Detect encoding.\n\n**--overwrite**\n> Modify file in place.\n\n**--help**\n> Display help information."],["DESCRIPTION","**nkf** (Network Kanji Filter) converts Japanese text encodings. Handles UTF-8, Shift-JIS, EUC-JP.\n\nThe tool auto-detects encodings. Essential for Japanese text processing."],["CAVEATS","Japanese text focused. Auto-detection may fail. Backup before overwrite."],["HISTORY","nkf was created for **Japanese computing**, handling various encodings used in Japan."],["INSTALL","```apt: sudo apt install nkf```\n\n```dnf: sudo dnf install nkf```\n\n```zypper: sudo zypper install nkf```\n\n```brew: brew install nkf```\n\n```nix: nix profile install nixpkgs#nkf```"],["SEE ALSO","[iconv](/man/iconv)(1), [uconv](/man/uconv)(1), [recode](/man/recode)(1)"]],"e":["iconv","uconv","recode"],"x":"nkf -w [input.txt] > [output.txt]"},"npm-install":{"n":"npm-install","t":"installs packages and their dependencies","s":[["TAGLINE","installs packages and their dependencies"],["TLDR","**Install all dependencies**\n\n```npm install```\n\n**Install specific package**\n\n```npm install [package]```\n\n**Install as dev dependency**\n\n```npm install -D [package]```\n\n**Install specific version**\n\n```npm install [package]@[version]```\n\n**Install globally**\n\n```npm install -g [package]```\n\n**Install from git**\n\n```npm install [git+https://github.com/user/repo.git]```\n\n**Clean install (CI)**\n\n```npm ci```"],["SYNOPSIS","**npm install** [_options_] [_packages_...]"],["PARAMETERS","**-D**, **--save-dev**\n> Save as devDependency.\n\n**-g**, **--global**\n> Install globally.\n\n**-E**, **--save-exact**\n> Save exact version.\n\n**--no-save**\n> Don't update package.json.\n\n**--legacy-peer-deps**\n> Ignore peer dep conflicts.\n\n**--force**\n> Force fetch packages."],["DESCRIPTION","**npm install** (or npm i) installs packages and their dependencies. Without arguments, it installs everything in package.json. With package names, it adds them to the project."],["VERSION RANGES","```bash\nnpm install lodash         # Latest\nnpm install lodash@4.17.0  # Exact\nnpm install lodash@^4.0.0  # Compatible\nnpm install lodash@~4.17.0 # Patch updates\n```"],["CAVEATS","npm ci is faster for CI/CD. Peer deps may cause issues in npm 7+. Lock file should be committed."],["HISTORY","npm install has been the core command of npm since **Isaac Schlueter** created npm in **2010** for Node.js package management."],["INSTALL","```pacman: sudo pacman -S npm```\n\n```apk: sudo apk add npm```\n\n```brew: brew install npm```"],["SEE ALSO","[npm](/man/npm)(1), [npm-ci](/man/npm-ci)(1), [npm-uninstall](/man/npm-uninstall)(1), [yarn](/man/yarn)(1)"]],"e":["npm","npm-ci","npm-uninstall","yarn"],"x":"npm install"},"ntl":{"n":"ntl","t":"netlify CLI shorthand","s":[["TAGLINE","netlify CLI shorthand"],["TLDR","**Start development server**\n\n```ntl dev```\n\n**Deploy site**\n\n```ntl deploy```\n\n**Deploy to production**\n\n```ntl deploy --prod```\n\n**Open site in browser**\n\n```ntl open```\n\n**List sites**\n\n```ntl sites:list```"],["SYNOPSIS","**ntl** [_command_] [_options_]"],["PARAMETERS","**dev**\n> Start local development server.\n\n**deploy**\n> Deploy site to Netlify.\n\n**--prod**\n> Deploy to production.\n\n**open**\n> Open site in browser.\n\n**sites:list**\n> List all sites.\n\n**login**\n> Authenticate with Netlify.\n\n**--help**\n> Display help information."],["DESCRIPTION","**ntl** is the Netlify CLI shorthand. Manages deployments and development.\n\nThe tool provides Netlify platform access. Alias for netlify command."],["CAVEATS","Requires Netlify account. Same as netlify command. Authentication required."],["HISTORY","ntl is a **shorthand alias** for the Netlify CLI, introduced for convenience."],["INSTALL","```dnf: sudo dnf install ntl```\n\n```pacman: sudo pacman -S ntl```\n\n```brew: brew install ntl```\n\n```nix: nix profile install nixpkgs#ntl```"],["SEE ALSO","[netlify](/man/netlify)(1), [npm](/man/npm)(1), [vercel](/man/vercel)(1)"]],"e":["netlify","npm","vercel"],"x":"ntl dev"},"oathtool":{"n":"oathtool","t":"generates and validates one-time passwords for two-factor authentication","s":[["TAGLINE","generates and validates one-time passwords for two-factor authentication"],["TLDR","**Generate TOTP code from secret**\n\n```oathtool --totp --base32 [SECRET_KEY]```\n\n**Generate HOTP code** (counter-based)\n\n```oathtool --hotp --counter [1] --base32 [SECRET_KEY]```\n\n**Generate 8-digit code**\n\n```oathtool --totp --base32 --digits [8] [SECRET_KEY]```\n\n**Validate a TOTP code**\n\n```oathtool --totp --base32 [SECRET_KEY] [123456]```\n\n**Generate code with time window**\n\n```oathtool --totp --base32 --now \"[2024-01-15 12:00:00 UTC]\" [SECRET_KEY]```\n\n**Use SHA256 algorithm**\n\n```oathtool --totp=sha256 --base32 [SECRET_KEY]```\n\n**Show verbose output**\n\n```oathtool --totp --base32 -v [SECRET_KEY]```"],["SYNOPSIS","**oathtool** [_--totp_[=_alg_]|_--hotp_] [_--base32_] [_--digits num_] [_--counter num_] [_options_] _secret_ [_otp_]"],["PARAMETERS","**--totp**[=_ALG_]\n> Generate time-based OTP (sha1, sha256, sha512).\n\n**--hotp**\n> Generate counter-based OTP.\n\n**-b**, **--base32**\n> Secret is base32 encoded.\n\n**-d** _NUM_, **--digits** _NUM_\n> Number of digits (default: 6).\n\n**-c** _NUM_, **--counter** _NUM_\n> Counter value for HOTP.\n\n**-s** _SEC_, **--time-step-size** _SEC_\n> Time step in seconds (default: 30).\n\n**-S** _TIME_, **--start-time** _TIME_\n> Start time for TOTP (Unix timestamp or ISO).\n\n**-N** _TIME_, **--now** _TIME_\n> Use specified time instead of current.\n\n**-w** _NUM_, **--window** _NUM_\n> Window for validation.\n\n**-v**, **--verbose**\n> Show detailed output."],["DESCRIPTION","**oathtool** generates and validates one-time passwords for two-factor authentication. It supports both HOTP (RFC 4226, counter-based) and TOTP (RFC 6238, time-based) algorithms.\n\nTOTP is the standard used by authenticator apps (Google Authenticator, Authy, etc.). The secret key is typically provided as a base32 string during 2FA setup. Running oathtool with this secret generates the same codes as the authenticator app.\n\nFor validation, provide both the secret and the code to verify. The window option allows for clock skew by accepting codes from adjacent time periods.\n\nHOTP uses an incrementing counter instead of time. Each code generation should increment the counter. This mode is less common but useful for hardware tokens.\n\nDifferent services use different parameters - some require 8 digits or SHA256. The verbose flag shows the calculation details for debugging setup issues."],["CAVEATS","Secrets must be kept secure - anyone with the secret can generate codes. System time must be accurate for TOTP. No built-in secret storage - integrate with password managers. Command history may expose secrets - use environment variables or files for scripting."],["HISTORY","**oathtool** is part of the **OATH Toolkit** project, implementing the Initiative for Open Authentication (OATH) standards. It was developed to provide open-source HOTP and TOTP implementations. The toolkit also includes PAM modules for system authentication and a library for developers."],["INSTALL","```apt: sudo apt install oathtool```\n\n```dnf: sudo dnf install oathtool```\n\n```apk: sudo apk add oath-toolkit-oathtool```"],["SEE ALSO","[pass-otp](/man/pass-otp)(1), [2fa](/man/2fa)(1), [gpg](/man/gpg)(1), [openssl](/man/openssl)(1)"]],"e":["pass-otp","2fa","gpg","openssl"],"x":"oathtool --totp --base32 [SECRET_KEY]"},"opamui":{"n":"opamui","t":"TUI for browsing and searching OCaml opam packages","s":[["TAGLINE","TUI for browsing and searching OCaml opam packages"],["TLDR","**Browse opam packages** interactively\n\n```opamui```"],["SYNOPSIS","**opamui**"],["DESCRIPTION","**opamui** is a terminal user interface for browsing and searching OCaml opam packages. It launches a full-screen TUI listing packages from the configured opam switch and lets you filter by name, see which packages are installed, and inspect package metadata such as version, synopsis, dependencies, and homepage.\n\nIt does **not** install or modify packages — it is purely a read-only browser that wraps **opam**'s package listing and inspection commands."],["KEY BINDINGS","**↑** / **↓**\n> Move selection in the package list.\n\n**/**\n> Filter packages by substring.\n\n**Enter**\n> Open the metadata view for the selected package.\n\n**q**\n> Quit."],["CAVEATS","Requires **opam** to be installed and an opam switch to be initialized (**opam init**). The set of packages shown reflects the currently active switch; run **opam switch** to see another set."],["HISTORY","**opamui** was created by **Nicolas Lamirault** (nlamirault) and is written in OCaml. It targets developers who want to explore the opam ecosystem without leaving the terminal."],["SEE ALSO","[opam](/man/opam)(1)"]],"e":["opam"],"x":"opamui"},"openstack-help":{"n":"openstack-help","t":"displays help information for OpenStack CLI commands","s":[["TAGLINE","displays help information for OpenStack CLI commands"],["TLDR","**Show general help**\n\n```openstack help```\n\n**Show help for command**\n\n```openstack help [server create]```\n\n**List all commands**\n\n```openstack command list```"],["SYNOPSIS","**openstack help** [_command_]"],["PARAMETERS","_command_\n> Command to get help for."],["DESCRIPTION","**openstack help** displays help information for OpenStack CLI commands. Shows available subcommands, options, and usage examples. Part of OpenStack unified CLI."],["SEE ALSO","[openstack](/man/openstack)(1)"]],"e":["openstack"],"x":"openstack help"},"pacman-remove":{"n":"pacman-remove","t":"removes packages from an Arch Linux system","s":[["TAGLINE","removes packages from an Arch Linux system"],["TLDR","**Remove a package and its unneeded dependencies**\n\n```sudo pacman -Rs [package]```\n\n**Remove without saving config backup files**\n\n```sudo pacman -Rsn [package]```\n\n**Remove without prompting for confirmation**\n\n```sudo pacman -R --noconfirm [package]```\n\n**Remove orphan packages (unneeded dependencies)**\n\n```sudo pacman -Rsn $(pacman -Qdtq)```\n\n**Cascade remove a package and all packages that depend on it**\n\n```sudo pacman -Rc [package]```\n\n**Preview what would be removed without removing**\n\n```pacman -Rp [package]```"],["SYNOPSIS","**pacman -R** [_OPTIONS_] _PACKAGE_..."],["DESCRIPTION","**pacman -R** (remove) removes packages from an Arch Linux system. It can also remove dependencies, configuration files, and handle cascading removals of dependent packages."],["PARAMETERS","**-R**, **--remove**\n> Remove packages from the system.\n\n**-s**, **--recursive**\n> Remove specified targets and their dependencies not required by other packages and not explicitly installed. Pass twice (**-ss**) to also remove explicitly installed dependencies.\n\n**-n**, **--nosave**\n> Remove configuration backup files (prevents **.pacsave** files from being created).\n\n**-c**, **--cascade**\n> Remove all target packages, as well as all packages that depend on them. This operation is recursive.\n\n**-p**, **--print**\n> Print what would be removed without actually removing.\n\n**-u**, **--unneeded**\n> Remove targets that are not required by any other packages. Useful when removing a group without using **-c**.\n\n**-d**, **--nodeps**\n> Skip dependency version checks. Pass twice to skip all dependency checks.\n\n**--dbonly**\n> Remove the database entry only, leaving all files in place.\n\n**--noscriptlet**\n> Do not execute install scriptlet during removal.\n\n**--noconfirm**\n> Skip confirmation prompts."],["CAVEATS","Using **-c** (cascade) can remove many packages if removing a core dependency. Always use **-p** first to preview what would be removed. Orphan packages should be cleaned periodically with **pacman -Rsn $(pacman -Qdtq)**."],["HISTORY","**pacman** is the package manager for **Arch Linux** and its derivatives."],["INSTALL","```apt: sudo apt install pacman-package-manager```\n\n```dnf: sudo dnf install pacman```\n\n```pacman: sudo pacman -S pacman```\n\n```apk: sudo apk add pacman```\n\n```nix: nix profile install nixpkgs#pacman```"],["SEE ALSO","[pacman](/man/pacman)(8), [pacman-query](/man/pacman-query)(8), [pacman-sync](/man/pacman-sync)(8), [pacman-database](/man/pacman-database)(8), [pacman-upgrade](/man/pacman-upgrade)(8)"]],"e":["pacman","pacman-query","pacman-sync","pacman-database","pacman-upgrade"],"x":"sudo pacman -Rs [package]"},"pamrgbatopng":{"n":"pamrgbatopng","t":"converts PAM images with alpha channel to PNG format, preserving transparency","s":[["TAGLINE","converts PAM images with alpha channel to PNG format, preserving transparency"],["TLDR","**Convert RGBA PAM to PNG with transparency**\n\n```pamrgbatopng [input.pam] > [output.png]```"],["SYNOPSIS","**pamrgbatopng** [_file_]"],["DESCRIPTION","**pamrgbatopng** converts PAM images with alpha channel to PNG format, preserving transparency. Input must be RGBA PAM (4 channels). Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pngtopam](/man/pngtopam)(1), [pnmtopng](/man/pnmtopng)(1)"]],"e":["pngtopam","pnmtopng"],"x":"pamrgbatopng [input.pam] > [output.png]"},"percent":{"n":"percent","t":"**%** in shell context refers to job control, bringing background jobs to the foreground","s":[["TAGLINE","**%** in shell context refers to job control, bringing background jobs to the foreground"],["TLDR","**Bring most recent background job to foreground**\n\n```%```\n\n**Bring job number N to foreground**\n\n```%[N]```\n\n**Bring job starting with string to foreground**\n\n```%[string]```\n\n**Bring job containing string to foreground**\n\n```%?[string]```\n\n**Use modulo operator in arithmetic**\n\n```echo $((10 % 3))```"],["SYNOPSIS","**%** [_job_spec_]"],["JOB SPECIFICATIONS","**%** or **%+** or **%%**\n> Current (most recent) job\n\n**%-**\n> Previous job\n\n**%n**\n> Job number n\n\n**%string**\n> Job whose command starts with string\n\n**%?string**\n> Job whose command contains string"],["DESCRIPTION","**%** in shell context refers to job control, bringing background jobs to the foreground. It's shorthand for the **fg** command.\n\nWhen a command is suspended (Ctrl+Z) or run in background (&), it gets a job number. The **%** notation provides quick access to these jobs.\n\nIn arithmetic contexts **$(( ))**, **%** is the modulo operator, returning the remainder of integer division.\n\nIn prompt strings (PS1), **%** introduces escape sequences in zsh for dynamic prompt elements."],["CAVEATS","Job control requires an interactive shell. Not available in scripts by default.\n\nThe **%** interpretation is context-dependent: job control at command position, modulo in arithmetic, format specifier in strings.\n\nJob numbers reset when the shell restarts."],["SEE ALSO","[fg](/man/fg)(1), [bg](/man/bg)(1), [jobs](/man/jobs)(1), [kill](/man/kill)(1)"]],"e":["fg","bg","jobs","kill"],"x":"%"},"pgmmake":{"n":"pgmmake","t":"creates a solid color PGM grayscale image of specified dimensions","s":[["TAGLINE","creates a solid color PGM grayscale image of specified dimensions"],["TLDR","**Create solid gray image**\n\n```pgmmake [0.5] [100] [100] > [gray.pgm]```\n\n**Create white image**\n\n```pgmmake 1.0 [200] [150] > [white.pgm]```\n\n**Create black image**\n\n```pgmmake 0.0 [640] [480] > [black.pgm]```\n\n**Specify max value**\n\n```pgmmake -maxval [65535] [0.5] [100] [100] > [gray16.pgm]```"],["SYNOPSIS","**pgmmake** [_options_] _grayval_ _width_ _height_"],["PARAMETERS","**grayval**\n> Gray level (0.0 = black, 1.0 = white).\n\n**width**\n> Image width in pixels.\n\n**height**\n> Image height in pixels.\n\n**-maxval** _n_\n> Maximum gray value (default 255)."],["DESCRIPTION","**pgmmake** creates a solid color PGM grayscale image of specified dimensions. The gray value is a fraction where 0.0 is black and 1.0 is white."],["50% gray 100x100 image","pgmmake 0.5 100 100 > gray.pgm"],["Black background","pgmmake 0 1920 1080 > black.pgm"],["16-bit white image","pgmmake -maxval 65535 1.0 100 100 > white16.pgm"],["Create and convert to PNG","pgmmake 0.75 200 200 | pnmtopng > gray.png\n```"],["CAVEATS","Creates grayscale only. For color use ppmmake. Part of Netpbm toolkit."],["HISTORY","pgmmake is part of **Netpbm**, the image manipulation toolkit originally created by **Jef Poskanzer**."],["SEE ALSO","[ppmmake](/man/ppmmake)(1), [pbmmake](/man/pbmmake)(1), [pnmtopng](/man/pnmtopng)(1), [netpbm](/man/netpbm)(1)"],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"]],"e":["ppmmake","pbmmake","pnmtopng","netpbm"],"x":"pgmmake [0.5] [100] [100] > [gray.pgm]"},"pio-home":{"n":"pio-home","t":"launches the PlatformIO Home web interface","s":[["TAGLINE","launches the PlatformIO Home web interface"],["TLDR","**Open PlatformIO Home**\n\n```pio home```\n\n**Open on specific port**\n\n```pio home --port [8008]```\n\n**Open without launching browser**\n\n```pio home --no-open```"],["SYNOPSIS","**pio home** [_options_]"],["PARAMETERS","**--port** _port_\n> HTTP server port.\n\n**--host** _host_\n> HTTP server host.\n\n**--no-open**\n> Don't open browser.\n\n**--session-id** _id_\n> Session ID."],["DESCRIPTION","**pio home** launches the PlatformIO Home web interface. Provides graphical project management, library browser, board explorer, and platform updates. Runs local web server."],["INSTALL","```aur: yay -S pio```"],["SEE ALSO","[pio-init](/man/pio-init)(1), [pio-boards](/man/pio-boards)(1)"]],"e":["pio-init","pio-boards"],"x":"pio home"},"pixi-task":{"n":"pixi-task","t":"Define and manage Pixi project tasks","s":[["TAGLINE","Define and manage Pixi project tasks"],["TLDR","**Add task to project**\n\n```pixi task add [name] [command]```\n\n**List tasks**\n\n```pixi task list```\n\n**Remove task**\n\n```pixi task remove [name]```\n\n**Add task with alias**\n\n```pixi task alias [name] [existing_task]```"],["SYNOPSIS","**pixi** **task** _command_ [_options_]"],["PARAMETERS","**add** _name_ _command_\n> Add new task.\n\n**list**\n> List all tasks.\n\n**remove** _name_\n> Remove task.\n\n**alias** _name_ _target_\n> Create task alias.\n\n**--depends-on** _tasks_\n> Task dependencies."],["DESCRIPTION","**pixi task** manages project tasks. Tasks are defined commands that can be run within the project environment. Similar to npm scripts or Makefile targets. Supports dependencies between tasks."],["INSTALL","```pacman: sudo pacman -S pixi```\n\n```brew: brew install pixi```\n\n```nix: nix profile install nixpkgs#pixi```"],["SEE ALSO","[pixi](/man/pixi)(1)"]],"e":["pixi"],"x":"pixi task add [name] [command]"},"pkey.1s":{"n":"pkey.1s","t":"Process and convert public and private keys with OpenSSL","s":[["TAGLINE","Process and convert public and private keys with OpenSSL"],["TLDR","**Extract the public key from a private key**\n\n```openssl pkey -in [key.pem] -pubout -out [pub.pem]```\n\n**Convert a PEM-encoded key to DER**\n\n```openssl pkey -in [key.pem] -out [key.der] -outform DER```\n\n**Print key parameters without re-encoding the key**\n\n```openssl pkey -in [key.pem] -text -noout```\n\n**Remove the passphrase from an encrypted key**\n\n```openssl pkey -in [encrypted.pem] -out [decrypted.pem]```\n\n**Encrypt a private key using AES-256**\n\n```openssl pkey -in [key.pem] -out [enc.pem] -aes-256-cbc```\n\n**Read a public key and convert its format**\n\n```openssl pkey -pubin -in [pub.pem] -outform DER -out [pub.der]```"],["SYNOPSIS","**openssl** **pkey** [_options_]"],["PARAMETERS","**-in** _FILE_\n> Input key file (stdin if omitted).\n\n**-out** _FILE_\n> Output file (stdout if omitted).\n\n**-inform** _FORMAT_\n> Input format: **PEM**, **DER**, or **ENGINE**.\n\n**-outform** _FORMAT_\n> Output format: **PEM** or **DER**.\n\n**-passin** _arg_\n> Input passphrase source (e.g. **pass:**, **env:**, **file:**).\n\n**-passout** _arg_\n> Output passphrase source for encryption.\n\n**-pubin**\n> Input file is a public key.\n\n**-pubout**\n> Output a public key only.\n\n**-text**\n> Print key details in human-readable form.\n\n**-text_pub**\n> Print only the public portion in text form.\n\n**-noout**\n> Do not output the encoded key (useful with **-text**).\n\n**-traditional**\n> Write the private key using the legacy \"traditional\" format (e.g. PKCS#1 for RSA).\n\n**-_cipher_**\n> Any OpenSSL cipher (e.g. **-aes-256-cbc**, **-des-ede3**) encrypts the output key."],["DESCRIPTION","**openssl pkey** is a unified key-processing utility that works with public and private keys of any supported algorithm (RSA, DSA, EC, Ed25519, Ed448, X25519, X448). It replaces the older algorithm-specific commands **openssl rsa**, **openssl dsa**, and **openssl ec**.\n\nThe tool can extract public keys with **-pubout**, convert between PEM and DER encodings, display parameters with **-text**, and add or remove passphrase encryption. When a cipher option is provided, the resulting private key is written in encrypted PKCS#8 form."],["CAVEATS","Writing to an unencrypted private-key file removes the existing passphrase. Ensure filesystem permissions are restrictive (e.g. **chmod 600**). The **-traditional** flag is required by some tools that cannot read modern PKCS#8 encoded keys."],["HISTORY","**openssl pkey** was introduced alongside the generic **EVP_PKEY** interface to provide a single tool for all key algorithms, superseding the algorithm-specific **rsa**, **dsa**, and **ec** subcommands."],["SEE ALSO","[openssl](/man/openssl)(1), [openssl-genpkey](/man/openssl-genpkey)(1), [openssl-rsa](/man/openssl-rsa)(1)"]],"e":["openssl","openssl-genpkey","openssl-rsa"],"x":"openssl pkey -in [key.pem] -pubout -out [pub.pem]"},"pnpm":{"n":"pnpm","t":"Fast disk-efficient Node.js package manager","s":[["TAGLINE","Fast disk-efficient Node.js package manager"],["TLDR","**Install dependencies**\n\n```pnpm install```\n\n**Add a package**\n\n```pnpm add [package]```\n\n**Add dev dependency**\n\n```pnpm add -D [package]```\n\n**Run script**\n\n```pnpm run [script]```\n\n**Update packages**\n\n```pnpm update```\n\n**Remove package**\n\n```pnpm remove [package]```"],["SYNOPSIS","**pnpm** [_command_] [_options_]"],["PARAMETERS","**install**\n> Install all dependencies.\n\n**add** _PACKAGE_\n> Add a package.\n\n**-D**, **--save-dev**\n> Save as dev dependency.\n\n**remove** _PACKAGE_\n> Remove a package.\n\n**update**\n> Update packages.\n\n**run** _SCRIPT_\n> Run a script.\n\n**exec** _CMD_\n> Execute a command.\n\n**-r**, **--recursive**\n> Run in all workspaces."],["DESCRIPTION","**pnpm** is a fast, disk-efficient package manager for Node.js that uses a content-addressable storage system. Instead of copying packages into each project's node_modules, pnpm creates hard links from a single global store, dramatically reducing disk usage when multiple projects share the same dependencies.\n\nIt provides a strict node_modules structure that prevents accessing undeclared dependencies, improving project reliability. The CLI is largely compatible with npm, supporting **install**, **add**, **remove**, **update**, and **run** commands with similar syntax. Workspace support via **-r** (recursive) enables managing monorepo projects."],["CONFIGURATION","**.npmrc**\n> Project-level or user-level configuration file for registry URLs, authentication tokens, and pnpm-specific settings like `store-dir` and `strict-peer-dependencies`.\n\n**~/.local/share/pnpm/store/** (Linux), **~/Library/pnpm/store/** (macOS)\n> Content-addressable package store shared across all projects.\n\n**pnpm-workspace.yaml**\n> Defines workspace packages for monorepo setups."],["CAVEATS","Node.js required. Uses hard links for efficiency."],["HISTORY","pnpm was created as a **fast alternative** to npm with better disk usage."],["INSTALL","```dnf: sudo dnf install pnpm```\n\n```pacman: sudo pacman -S pnpm```\n\n```apk: sudo apk add pnpm```\n\n```zypper: sudo zypper install pnpm```\n\n```brew: brew install pnpm```\n\n```nix: nix profile install nixpkgs#pnpm```"],["SEE ALSO","[npm](/man/npm)(1), [yarn](/man/yarn)(1), [node](/man/node)(1)"]],"e":["npm","yarn","node"],"x":"pnpm install"},"poetry-run":{"n":"poetry-run","t":"Execute commands in virtual environment","s":[["TAGLINE","Execute commands in virtual environment"],["TLDR","**Run command in virtual environment**\n\n```poetry run [command]```\n\n**Run Python script**\n\n```poetry run python [script.py]```\n\n**Run installed tool**\n\n```poetry run pytest```\n\n**Run with arguments**\n\n```poetry run python -m [module] [args]```"],["SYNOPSIS","**poetry run** _command_ [_args_]"],["PARAMETERS","_COMMAND_\n> Command to execute.\n\n_ARGS_\n> Arguments to pass."],["DESCRIPTION","**poetry run** executes a command within the project's virtual environment without requiring manual activation. It automatically locates and uses the correct virtual environment for the current project.\n\nThis is the recommended way to run project scripts, test suites, and installed tools. For example, `poetry run pytest` runs tests using the project's dependencies, and `poetry run python script.py` executes a script with the project's Python interpreter. An alternative is **poetry shell** for an interactive session."],["CAVEATS","Creates venv if not exists. Alternative to poetry shell."],["HISTORY","poetry run provides **virtual environment** command execution."],["INSTALL","```dnf: sudo dnf install poetry```\n\n```apk: sudo apk add poetry```\n\n```brew: brew install poetry```\n\n```nix: nix profile install nixpkgs#poetry```"],["SEE ALSO","[poetry](/man/poetry)(1), [poetry-shell](/man/poetry-shell)(1)"]],"e":["poetry","poetry-shell"],"x":"poetry run [command]"},"pumount":{"n":"pumount","t":"Unmount removable devices without root","s":[["TAGLINE","Unmount removable devices without root"],["TLDR","**Unmount device**\n\n```pumount [/dev/sdb1]```\n\n**Unmount by mount point**\n\n```pumount [/media/usb]```\n\n**Unmount by label**\n\n```pumount [LABEL]```\n\n**Lazy unmount**\n\n```pumount --lazy [/dev/sdb1]```"],["SYNOPSIS","**pumount** [_options_] _device_|_directory_"],["PARAMETERS","**-l**, **--lazy**\n> Lazy unmount (detach filesystem now, clean up when no longer busy). Use with caution as it may cause data loss.\n\n**--luks-force**\n> Force LUKS close even if pmount did not open the device.\n\n**-d**, **--debug**\n> Enable verbose debug messages.\n\n**--version**\n> Print version number and exit."],["DESCRIPTION","**pumount** is the counterpart to pmount for unmounting removable devices without root privileges. It is a wrapper around the standard umount that permits normal users to unmount removable devices without a matching /etc/fstab entry. It handles encrypted dm-crypt/LUKS volumes and ensures proper cleanup. It also supports unmounting devices that have been brutally removed."],["CAVEATS","Only works with pmount-mounted devices. Doesn't handle fstab mounts. May need --lazy for busy devices."],["HISTORY","pumount is part of **pmount** by **Martin Pitt**, providing non-root unmounting for removable media."],["INSTALL","```apt: sudo apt install pmount```\n\n```dnf: sudo dnf install pmount```\n\n```nix: nix profile install nixpkgs#pmount```"],["SEE ALSO","[pmount](/man/pmount)(1), [umount](/man/umount)(8), [udisksctl](/man/udisksctl)(1)"]],"e":["pmount","umount","udisksctl"],"x":"pumount [/dev/sdb1]"},"recins":{"n":"recins","t":"Insert records into GNU recutils databases","s":[["TAGLINE","Insert records into GNU recutils databases"],["TLDR","**Insert record**\n\n```recins -f [Name] -v \"[John]\" -f [Email] -v \"[john@example.com]\" [file.rec]```\n\n**Insert with type**\n\n```recins -t [Contact] -f [Name] -v \"[Jane]\" [file.rec]```\n\n**Insert from template**\n\n```echo \"Name: John\" | recins [file.rec]```\n\n**Replace if exists**\n\n```recins -r -f [Name] -v \"[John]\" [file.rec]```"],["SYNOPSIS","**recins** [_options_] [_file_]"],["PARAMETERS","**-t**, **--type** _type_\n> Record type.\n\n**-f**, **--field** _name_\n> Field name.\n\n**-v**, **--value** _value_\n> Field value.\n\n**-r**, **--replace**\n> Replace existing.\n\n**-n**, **--name** _name_\n> Set record descriptor.\n\n**-S**, **--sort** _field_\n> Sort after insert."],["DESCRIPTION","**recins** adds new records to GNU recutils plain-text database files. Fields and values can be specified on the command line with repeated **-f** and **-v** flags, or records can be piped in from stdin in standard recfile format (key-value pairs separated by blank lines).\n\nThe **-t** flag assigns a record type, **-r** replaces an existing record instead of appending, and **-S** sorts records after insertion. The file is created automatically if it does not exist. Part of the GNU recutils toolkit."],["EXAMPLES","```bash"],["Insert single record","recins -f Name -v \"Alice\" -f Email -v \"alice@example.com\" contacts.rec"],["With record type","recins -t Book -f Title -v \"1984\" -f Author -v \"Orwell\" library.rec"],["Multiple fields","recins -t Product \\\n  -f Name -v \"Widget\" \\\n  -f Price -v \"9.99\" \\\n  -f Stock -v \"100\" \\\n  inventory.rec"],["From stdin","cat << EOF | recins contacts.rec\nName: Bob\nEmail: bob@example.com\nPhone: 555-1234\nEOF\n```"],["CAVEATS","Creates file if doesn't exist. Appends by default. Part of GNU recutils."],["HISTORY","recins is part of **GNU recutils** by **Jose E. Marchesi** for text-based database management."],["INSTALL","```dnf: sudo dnf install recutils```\n\n```apk: sudo apk add recutils```\n\n```brew: brew install recutils```\n\n```nix: nix profile install nixpkgs#recutils```"],["SEE ALSO","[recsel](/man/recsel)(1), [recdel](/man/recdel)(1), [recset](/man/recset)(1)"]],"e":["recsel","recdel","recset"],"x":"recins -f [Name] -v \"[John]\" -f [Email] -v \"[john@example.com]\" [file.rec]"},"rig":{"n":"rig","t":"Generate random identity data","s":[["TAGLINE","Generate random identity data"],["TLDR","Display a **random name** and address\n\n```rig```\n\nDisplay a **male** random identity\n\n```rig -m```\n\nDisplay a **female** random identity\n\n```rig -f```\n\nUse data files from a **specific directory**\n\n```rig -d path/to/directory```\n\nDisplay a **specific number** of identities\n\n```rig -c 5```"],["SYNOPSIS","**rig** [_options_]"],["PARAMETERS","**-m**\n> Generate male identities only\n\n**-f**\n> Generate female identities only\n\n**-d** _directory_\n> Use data files from specified directory\n\n**-c** _count_\n> Number of identities to generate"],["DESCRIPTION","**rig** generates random identities including first name, last name, street address, city, state, ZIP code, and area code. All components are geographically consistent for the same region.\n\nThis is useful for testing applications, generating sample data, or populating databases with realistic-looking but fake personal information."],["CAVEATS","Generated data is random and fictional. Data files determine available names and locations."],["HISTORY","Created as a utility for generating random test data with realistic-looking personal information."],["INSTALL","```apt: sudo apt install rig```\n\n```dnf: sudo dnf install rig```\n\n```brew: brew install rig```\n\n```nix: nix profile install nixpkgs#rig```"],["SEE ALSO","[pwgen](/man/pwgen)(1)"]],"e":["pwgen"],"x":"rig"},"rvm":{"n":"rvm","t":"Ruby version manager for multiple installations","s":[["TAGLINE","Ruby version manager for multiple installations"],["TLDR","**Install Ruby version**\n\n```rvm install [3.2.0]```\n\n**List installed versions**\n\n```rvm list```\n\n**Use specific version**\n\n```rvm use [3.2.0]```\n\n**Set default version**\n\n```rvm use [3.2.0] --default```\n\n**Create gemset**\n\n```rvm gemset create [myproject]```\n\n**Use version with gemset**\n\n```rvm use [3.2.0]@[myproject]```\n\n**List available versions**\n\n```rvm list known```"],["SYNOPSIS","**rvm** [_options_] _command_ [_args_...]"],["DESCRIPTION","**rvm** (Ruby Version Manager) manages multiple Ruby installations and gemsets. It handles Ruby installation, version switching, and isolated gem environments.\n\nThe tool modifies shell environment to switch between Ruby versions and provides gemsets for project-specific gem isolation."],["PARAMETERS","**install** _version_\n> Install Ruby version.\n\n**use** _version_\n> Switch to version.\n\n**list**\n> List installed versions.\n\n**list known**\n> List available versions.\n\n**gemset create** _name_\n> Create gemset.\n\n**gemset use** _name_\n> Use gemset.\n\n**gemset list**\n> List gemsets.\n\n**--default**\n> Set as default.\n\n**implode**\n> Remove RVM completely."],["CAVEATS","Modifies shell initialization. Can conflict with system Ruby. Gemsets add complexity. Some prefer rbenv's simpler approach."],["HISTORY","**RVM** was created by **Wayne E. Seguin** in **2009** to simplify Ruby version management. It became the dominant Ruby version manager before rbenv offered a simpler alternative. Now maintained by **Michal Papis**."],["INSTALL","```dnf: sudo dnf install rvm```"],["SEE ALSO","[ruby](/man/ruby)(1), [gem](/man/gem)(1), [rbenv](/man/rbenv)(1), [bundler](/man/bundler)(1)"]],"e":["ruby","gem","rbenv","bundler"],"x":"rvm install [3.2.0]"},"sl":{"n":"sl","t":"Steam locomotive animation for typo correction","s":[["TAGLINE","Steam locomotive animation for typo correction"],["TLDR","**Display a steam locomotive** animation\n\n```sl```\n\n**Show a smaller locomotive**\n\n```sl -l```\n\n**Make the locomotive fly**\n\n```sl -F```\n\n**Show an accident** (people crying for help)\n\n```sl -a```\n\n**Display the C51 locomotive** instead of D51\n\n```sl -c```\n\n**Allow interruption** with Ctrl+C\n\n```sl -e```\n\n**Combine options** for flying little locomotive with accident\n\n```sl -alF```"],["SYNOPSIS","**sl** [**-alFce**]"],["PARAMETERS","**-a**\n> An accident occurs; people appear crying for help\n\n**-l**\n> Display a smaller (little) locomotive\n\n**-F**\n> The locomotive flies across the screen\n\n**-c**\n> Show C51 locomotive instead of the default D51\n\n**-e**\n> Allow interrupt with Ctrl+C; normally you must wait"],["DESCRIPTION","**sl** (Steam Locomotive) displays an ASCII art animation of a steam locomotive crossing the terminal. It was created as a humorous punishment for users who accidentally type **sl** instead of **ls** (list directory contents).\n\nWhen executed, a detailed ASCII train animation scrolls across the screen from right to left. By default, the program ignores Ctrl+C, forcing users to watch the entire train pass—a gentle reminder to type more carefully.\n\nThe default locomotive is the **D51** (a Japanese steam locomotive). The **-c** option shows the alternative **C51** model. Options can be combined for effects like a flying miniature locomotive with passengers in distress."],["CAVEATS","By default, Ctrl+C is ignored, and you must wait for the animation to complete. Use **-e** to allow early termination. The animation may not display correctly in terminals with very small dimensions."],["HISTORY","**sl** was created by **Toyoda Masashi** in **1993** at the University of Tokyo. It was designed as a joke program to discourage the common typo of typing sl instead of ls. The program has become a beloved piece of Unix humor and is available in most Linux distribution repositories."],["INSTALL","```dnf: sudo dnf install sl```\n\n```pacman: sudo pacman -S sl```\n\n```apk: sudo apk add sl```\n\n```zypper: sudo zypper install sl```\n\n```brew: brew install sl```\n\n```nix: nix profile install nixpkgs#sl```"],["SEE ALSO","[ls](/man/ls)(1), [cowsay](/man/cowsay)(1), [fortune](/man/fortune)(1), [figlet](/man/figlet)(1)"]],"e":["ls","cowsay","fortune","figlet"],"x":"sl"},"solcjs":{"n":"solcjs","t":"JavaScript-based Solidity compiler","s":[["TAGLINE","JavaScript-based Solidity compiler"],["TLDR","**Compile contract**\n\n```solcjs [contract.sol]```\n\n**Output binary**\n\n```solcjs --bin [contract.sol]```\n\n**Output ABI**\n\n```solcjs --abi [contract.sol]```\n\n**Both binary and ABI**\n\n```solcjs --bin --abi [contract.sol]```\n\n**Custom output directory**\n\n```solcjs --bin -o [build] [contract.sol]```\n\n**Optimize code**\n\n```solcjs --bin --optimize [contract.sol]```"],["SYNOPSIS","**solcjs** [_--bin_] [_--abi_] [_--optimize_] [_-o dir_] _files_"],["PARAMETERS","**--bin**\n> Output bytecode.\n\n**--abi**\n> Output ABI JSON.\n\n**--optimize**\n> Enable optimizer.\n\n**-o** _DIR_\n> Output directory.\n\n**--base-path** _DIR_\n> Base path for imports.\n\n**--include-path** _DIR_\n> Include path."],["DESCRIPTION","**solcjs** is a JavaScript/WebAssembly implementation of the Solidity compiler, installable via npm. It compiles Solidity smart contracts into EVM bytecode and ABI (Application Binary Interface) definitions needed for deploying and interacting with contracts on Ethereum and compatible blockchains.\n\nThe compiler produces bytecode (**--bin**) for deployment and ABI JSON (**--abi**) that describes the contract's functions and events for client interaction. The optimizer reduces bytecode size and gas costs when enabled.\n\nsolcjs provides the same core compilation functionality as the native **solc** compiler but runs in Node.js environments, making it easy to integrate into JavaScript-based development workflows and build tools like Truffle and Hardhat. It is somewhat slower than the native compiler and may lack some advanced features."],["CAVEATS","Slower than native solc. JavaScript implementation. Some features missing."],["HISTORY","**solcjs** is a JavaScript/WebAssembly port of **solc**, the Solidity compiler for Ethereum smart contracts."],["SEE ALSO","[solc](/man/solc)(1), [truffle](/man/truffle)(1), [hardhat](/man/hardhat)(1)"]],"e":["solc","truffle","hardhat"],"x":"solcjs [contract.sol]"},"stegsnow":{"n":"stegsnow","t":"Conceal messages in text file whitespace","s":[["TAGLINE","Conceal messages in text file whitespace"],["TLDR","**Extract** message from file\n\n```stegsnow [path/to/file.txt]```\n\nExtract **compressed** and **password-protected** message\n\n```stegsnow -C -p [password] [path/to/file.txt]```\n\nShow **storage capacity**\n\n```stegsnow -S -l 72 [path/to/file.txt]```\n\n**Conceal** message in text file\n\n```stegsnow -m '[message]' [path/to/file.txt] [path/to/output.txt]```\n\nConceal from **message file** with compression\n\n```stegsnow -C -f [path/to/message.txt] [path/to/file.txt] [path/to/output.txt]```\n\nConceal with **compression and password**\n\n```stegsnow -C -p [password] -m '[message]' [path/to/file.txt] [path/to/output.txt]```"],["SYNOPSIS","**stegsnow** [**-CQS**] [**-p** _passwd_] [**-l** _line-len_] [**-f** _file_ | **-m** _message_] [_infile_ [_outfile_]]"],["PARAMETERS","**-C**\n> Enable compression (concealing) or decompression (extracting)\n\n**-Q**\n> Quiet mode; suppress statistics\n\n**-S**\n> Show approximate storage capacity\n\n**-p** _password_\n> Encrypt/decrypt with password using ICE cipher in cipher-feedback mode\n\n**-l** _line-length_\n> Maximum line length (default: 80)\n\n**-f** _file_\n> Conceal contents of specified file\n\n**-m** _message_\n> Conceal specified text string\n\n**-V, --version**\n> Display version information\n\n**-h, --help**\n> Display usage information"],["DESCRIPTION","**stegsnow** (also known as SNOW) conceals messages in text files by appending tabs and spaces at line endings. These whitespace characters are invisible when viewing the file, providing steganographic encoding. The tool encodes data using sequences of spaces and tabs, typically storing 3 bits per 8 columns.\n\nFeatures include built-in Huffman compression optimized for English text and encryption using the ICE algorithm in cipher-feedback mode. A marker tab indicates the data start position, allowing file headers to remain intact."],["CAVEATS","Storage capacity depends on line lengths in the cover file. Very short lines provide less hiding space. Non-text data should use external compression (gzip) before concealment. Whitespace-stripping tools or editors may corrupt hidden data."],["HISTORY","**SNOW** (Steganographic Nature Of Whitespace) was created by **Matthew Kwan** in **1996**. The program is one of the earliest and most well-known text-based steganography tools, using the simple but effective technique of encoding data in trailing whitespace that is invisible to casual observers."],["INSTALL","```apt: sudo apt install stegsnow```"],["SEE ALSO","[steghide](/man/steghide)(1), [outguess](/man/outguess)(1), [gzip](/man/gzip)(1)"]],"e":["steghide","outguess","gzip"],"x":"stegsnow [path/to/file.txt]"},"strigger":{"n":"strigger","t":"Manage Slurm cluster event triggers","s":[["TAGLINE","Manage Slurm cluster event triggers"],["TLDR","**Register** a trigger for system events\n\n```strigger --set --primary_slurmctld_failure -p [path/to/script]```\n\nRegister trigger for **job completion**\n\n```strigger --set -j [job_id] -f -p \"[path/to/script] [args]\"```\n\n**View** active triggers\n\n```strigger --get```\n\nView triggers for **specific job**\n\n```strigger --get -j [job_id]```\n\n**Clear** a trigger\n\n```strigger --clear [trigger_id]```\n\nRegister **permanent** trigger\n\n```strigger --set --down --flags=PERM -p [path/to/script]```"],["SYNOPSIS","**strigger** **--set** [_OPTIONS_...]\n**strigger** **--get** [_OPTIONS_...]\n**strigger** **--clear** [_OPTIONS_...]"],["PARAMETERS","**--set**\n> Register a new trigger\n\n**--get**\n> Display registered triggers\n\n**--clear**\n> Remove a trigger\n\n**-j, --jobid** _id_\n> Target specific job\n\n**--node** _name_\n> Target specific node\n\n**-p, --program** _path_\n> Script to execute when trigger fires\n\n**--offset** _seconds_\n> Timing adjustment (negative = before event)\n\n**--flags** _flags_\n> Trigger flags (PERM = permanent)\n\n**-u, --user** _name_\n> Filter by trigger creator\n\n**-v, --verbose**\n> Detailed output\n\n**-q, --quiet**\n> Suppress non-critical errors"],["EVENT TYPES","**Node events**: --down, --up, --drained, --draining, --idle, --fail\n**Job events**: --fini, --time\n**System events**: --primary_slurmctld_failure, --primary_slurmdbd_failure, --reconfig"],["DESCRIPTION","**strigger** manages event triggers in Slurm that automatically execute scripts when specific events occur. Triggers can respond to node state changes, job completions, system failures, and other cluster events. The daemon checks triggers approximately every 15 seconds.\n\nTriggered programs execute on the slurmctld node with a 5-minute timeout. By default, triggers are removed after execution unless marked permanent with **--flags=PERM**."],["CAVEATS","Only SlurmUser (typically root) can set triggers. Programs run on slurmctld node, not compute nodes. Triggers must be re-registered after execution unless permanent. Scripts must be executable and accessible from slurmctld host."],["HISTORY","**strigger** is part of **Slurm**, developed at **Lawrence Livermore National Laboratory** starting in **2002**. Event triggers allow automated responses to cluster events for monitoring, alerting, and workflow automation. Slurm is now maintained by **SchedMD**."],["INSTALL","```apt: sudo apt install slurm-client```"],["SEE ALSO","[scontrol](/man/scontrol)(1), [slurmctld](/man/slurmctld)(8)"]],"e":["scontrol","slurmctld"],"x":"strigger --set --primary_slurmctld_failure -p [path/to/script]"},"superseedr":{"n":"superseedr","t":"Modern BitTorrent client for the terminal","s":[["TAGLINE","Modern BitTorrent client for the terminal"],["TLDR","**Launch the TUI client**\n\n```superseedr```\n\n**Add a magnet link**\n\n```superseedr add \"[magnet:?xt=urn:btih:...]\"```\n\n**Add a torrent file**\n\n```superseedr add [/path/to/file.torrent]```\n\n**List configured torrents**\n\n```superseedr torrents```\n\n**List the files of a torrent** by info hash\n\n```superseedr files [info_hash]```\n\n**Gracefully stop the running client**\n\n```superseedr stop-client```"],["SYNOPSIS","**superseedr** [_command_] [_options_]"],["DESCRIPTION","**superseedr** is a modern BitTorrent client featuring a high-performance 60 FPS terminal UI, real-time swarm observability with bandwidth graphs and peer analytics, and Docker with Gluetun VPN integration. It supports BitTorrent v2, RSS feed tracking, and 40+ built-in themes.\n\nRun without arguments, **superseedr** opens the interactive terminal UI. While it is running you can add content by pasting a magnet link or torrent path, or by running the `add` subcommand from another terminal, which hands the input to the live instance. The remaining subcommands provide a scriptable interface for managing torrents and the shared/standalone configuration. Add **--json** to any subcommand to get structured output."],["COMMANDS","**add** [**--path** _PATH_] [**--validated**] _input_...\n> Add one or more torrent file paths or magnet links. **--path** persists an existing download path for the inputs; **--validated** treats data as already verified when rebuilding a catalog.\n\n**torrents**\n> List configured torrents.\n\n**info** _info_hash_|_path_\n> Show a single torrent by info hash or unique file path.\n\n**files** _info_hash_|_path_\n> List the files for a torrent, including relative and resolved full paths.\n\n**stop-client**\n> Request a graceful shutdown of the running client.\n\n**to-shared** _PATH_ / **to-standalone**\n> Convert between layered shared config (rooted at _PATH_) and local standalone config."],["HISTORY","**superseedr** was created by **Jagalite** and is written in **Rust**. It is distributed via `cargo install superseedr`, Homebrew, the AUR, and platform installers."],["INSTALL","```brew: brew install superseedr```"],["SEE ALSO","[transmission-cli](/man/transmission-cli)(1), [transmission-remote](/man/transmission-remote)(1), [rtorrent](/man/rtorrent)(1), [aria2](/man/aria2)(1), [deluge](/man/deluge)(1)"]],"e":["transmission-cli","transmission-remote","rtorrent","aria2","deluge"],"x":"superseedr"},"systemd-cgls":{"n":"systemd-cgls","t":"Recursively show control group contents in a tree","s":[["TAGLINE","Recursively show control group contents in a tree"],["TLDR","**Display the whole control group hierarchy**\n\n```systemd-cgls```\n\n**Display the cgroup tree for a specific controller**\n\n```systemd-cgls [cpu|memory|io]```\n\n**Display the control group hierarchy of specific units**\n\n```systemd-cgls -u [unit1] [unit2]```\n\n**Show all control groups including empty ones**\n\n```systemd-cgls --all```\n\n**Display without truncating process names**\n\n```systemd-cgls -l```"],["SYNOPSIS","**systemd-cgls** [_options_] [_cgroup_...]"],["PARAMETERS","**-u**, **--unit** _UNIT_\n> Show the cgroup subtree of the specified systemd unit(s).\n\n**-a**, **--all**\n> Show all control groups, including empty ones that contain no processes.\n\n**-l**, **--full**\n> Do not ellipsize (truncate) process tree members.\n\n**-k**\n> Include kernel threads in the output.\n\n**--no-pager**\n> Do not pipe output into a pager.\n\n**-M**, **--machine** _CONTAINER_\n> Show control group contents in the specified container.\n\n**-h**, **--help**\n> Display help information."],["DESCRIPTION","**systemd-cgls** recursively shows the contents of the Linux control group (cgroup) hierarchy in a tree format. It displays which processes belong to which control groups, organized by systemd slices, scopes, and services.\n\nIf arguments are specified, shows all member processes of the specified control groups plus their subgroups. If no argument is given and the current working directory is beneath **/sys/fs/cgroup/**, it shows the contents of that cgroup. Otherwise, the full systemd control group hierarchy is shown.\n\nThe tool provides a clear visual representation of the cgroup nesting and processes, making it useful for understanding how systemd organizes processes and for debugging resource management issues."],["CAVEATS","Requires cgroups (v1 or v2) to be mounted. Empty control groups are hidden by default unless **--all** is used. The tree structure reflects the systemd unit hierarchy. Part of the systemd suite."],["HISTORY","systemd-cgls was introduced as part of the **systemd** project by Lennart Poettering, providing a user-friendly view of the cgroup hierarchy managed by systemd."],["INSTALL","```apt: sudo apt install systemd```\n\n```dnf: sudo dnf install systemd```\n\n```pacman: sudo pacman -S systemd```\n\n```zypper: sudo zypper install systemd```\n\n```brew: brew install systemd```\n\n```nix: nix profile install nixpkgs#systemd```"],["SEE ALSO","[systemd-cgtop](/man/systemd-cgtop)(1), [systemctl](/man/systemctl)(1), [ps](/man/ps)(1), [cgroups](/man/cgroups)(7)"]],"e":["systemd-cgtop","systemctl","ps","cgroups"],"x":"systemd-cgls"},"tifftopnm":{"n":"tifftopnm","t":"Convert TIFF images to PNM format","s":[["TAGLINE","Convert TIFF images to PNM format"],["TLDR","**Convert TIFF to PNM**\n\n```tifftopnm [input.tiff] > [output.pnm]```\n\n**Convert specific page**\n\n```tifftopnm -page [2] [input.tiff] > [output.pnm]```\n\n**List pages in TIFF**\n\n```tifftopnm -headerdump [input.tiff]```"],["SYNOPSIS","**tifftopnm** [_options_] [_file_]"],["PARAMETERS","**-page** _n_\n> Extract specific page.\n\n**-headerdump**\n> Dump TIFF headers.\n\n**-alphaout** _file_\n> Write alpha channel to file.\n\n**-respectfillorder**\n> Respect fill order tag."],["DESCRIPTION","**tifftopnm** converts TIFF images to PNM format. Part of Netpbm toolkit. Supports multi-page TIFF files and alpha channel extraction."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pnmtotiff](/man/pnmtotiff)(1), [pngtopnm](/man/pngtopnm)(1)"]],"e":["pnmtotiff","pngtopnm"],"x":"tifftopnm [input.tiff] > [output.pnm]"},"tio":{"n":"tio","t":"Simple serial device terminal emulator","s":[["TAGLINE","Simple serial device terminal emulator"],["TLDR","**Connect to serial device**\n\n```tio [/dev/ttyUSB0]```\n\n**Connect with baud rate**\n\n```tio -b [115200] [/dev/ttyUSB0]```\n\n**Connect with settings**\n\n```tio -b [9600] -d [8] -s [1] -p [none] [/dev/ttyUSB0]```\n\n**Enable local echo**\n\n```tio -e [/dev/ttyUSB0]```\n\n**Log session to file**\n\n```tio -l [session.log] [/dev/ttyUSB0]```"],["SYNOPSIS","**tio** [_options_] _device_"],["PARAMETERS","**-b** _rate_\n> Baud rate (default 115200).\n\n**-d** _bits_\n> Data bits (5, 6, 7, 8).\n\n**-s** _bits_\n> Stop bits (1, 2).\n\n**-p** _parity_\n> Parity (none, odd, even).\n\n**-e**\n> Enable local echo.\n\n**-l** _file_\n> Log to file.\n\n**-t**\n> Enable timestamps.\n\n**-m** _map_\n> Map special characters."],["DESCRIPTION","**tio** is a simple serial device terminal emulator designed as a modern, user-friendly alternative to minicom and screen for serial communication. It connects to serial ports with sensible defaults (115200 baud, 8N1) and supports automatic reconnection when a device is plugged in or becomes available.\n\nThe tool provides essential serial communication features including configurable baud rates, data bits, stop bits, parity settings, local echo, session logging, and timestamps. The prefix key **Ctrl+t** provides access to in-session commands, with **Ctrl+t q** to quit."],["CAVEATS","Press **Ctrl+t q** to quit the terminal session."],["INSTALL","```apt: sudo apt install tio```\n\n```dnf: sudo dnf install tio```\n\n```apk: sudo apk add tio```\n\n```zypper: sudo zypper install tio```\n\n```brew: brew install tio```\n\n```nix: nix profile install nixpkgs#tio```"],["SEE ALSO","[minicom](/man/minicom)(1), [screen](/man/screen)(1), [picocom](/man/picocom)(1)"]],"e":["minicom","screen","picocom"],"x":"tio [/dev/ttyUSB0]"},"tuckr":{"n":"tuckr","t":"Symlink-based dotfile manager","s":[["TAGLINE","Symlink-based dotfile manager"],["TLDR","**Symlink all dotfiles into place**\n\n```tuckr add \\*```\n\n**Symlink specific groups**\n\n```tuckr add [neovim] [zsh]```\n\n**Symlink everything except some groups**\n\n```tuckr add \\* -e [neovim]```\n\n**Set up groups and run their hooks**\n\n```tuckr set [group]```\n\n**Remove deployed dotfiles**\n\n```tuckr rm [group]```\n\n**Show symlinking status**\n\n```tuckr status```\n\n**Copy existing files into a group**\n\n```tuckr push [group] [file]```"],["SYNOPSIS","**tuckr** _command_ [_group_...] [_options_]"],["PARAMETERS","**add** (alias **a**)\n> Deploy dotfiles for the given groups by creating symlinks.\n\n**rm**\n> Remove the deployed dotfiles from the system.\n\n**set**\n> Set up groups and run their hooks.\n\n**unset**\n> Remove groups and run their cleanup hooks.\n\n**status** (alias **s**)\n> Show the symlinking status of dotfiles.\n\n**push**\n> Copy existing files into a group.\n\n**pop**\n> Remove groups from the dotfiles directory.\n\n**encrypt** (alias **e**)\n> Encrypt files and store them under Secrets.\n\n**decrypt** (alias **d**)\n> Decrypt files.\n\n**groupis**\n> Report which group a file belongs to.\n\n**init**\n> Initialize the dotfile directory.\n\n**-e** _GROUP_\n> Exclude the given group when used with add."],["DESCRIPTION","**tuckr** is a dotfile manager that organizes configuration files into logical groups and deploys them as symlinks. Each group represents a set of related dotfiles (such as vim, zsh, or git), allowing selective deployment of configurations across different machines.\n\nThe add command creates symlinks from the dotfile repository to their expected locations in the home directory. Groups can be linked individually or all at once with the `\\*` wildcard, and the status command shows which groups are currently linked. The rm command cleanly unlinks deployed symlinks, while set additionally runs per-group setup hooks.\n\nThe tool uses a simple directory structure with Configs, Hooks, and Secrets directories, where each subdirectory under Configs represents a group that mirrors the home directory layout within it. Inspired by GNU Stow, it adds hooks and encrypted secrets without requiring symlink-aware tooling."],["CAVEATS","Rust implementation. Specific directory structure. Symlink-based."],["HISTORY","**Tuckr** was created as a simple dotfile manager using symlinks with group-based organization."],["INSTALL","```apk: sudo apk add tuckr```\n\n```brew: brew install tuckr```\n\n```nix: nix profile install nixpkgs#tuckr```"],["SEE ALSO","[stow](/man/stow)(1), [chezmoi](/man/chezmoi)(1), [yadm](/man/yadm)(1)"]],"e":["stow","chezmoi","yadm"],"x":"tuckr add \\*"},"tzconfig":{"n":"tzconfig","t":"Interactive system timezone configuration","s":[["TAGLINE","Interactive system timezone configuration"],["TLDR","**Configure timezone**\n\n```tzconfig```"],["SYNOPSIS","**tzconfig** [_options_]"],["PARAMETERS","**--help**\n> Show help."],["DESCRIPTION","**tzconfig** is an interactive Debian utility for configuring the system timezone. It presents a menu-based interface that guides users through selecting a geographic region and city to determine the correct timezone, then updates the system-wide timezone configuration by modifying **/etc/localtime**.\n\nThis tool has been deprecated on systemd-based systems in favor of **timedatectl**, which provides timezone management along with broader time and date configuration capabilities. On older Debian systems without systemd, **dpkg-reconfigure tzdata** is the preferred alternative."],["CAVEATS","Deprecated on many systems. Use timedatectl. Root required."],["HISTORY","**tzconfig** was the traditional Debian tool for timezone configuration, now replaced by **timedatectl** on systemd systems."],["SEE ALSO","[timedatectl](/man/timedatectl)(1), [dpkg-reconfigure](/man/dpkg-reconfigure)(1), [tzselect](/man/tzselect)(1)"]],"e":["timedatectl","dpkg-reconfigure","tzselect"],"x":"tzconfig"},"vboxmanage":{"n":"vboxmanage","t":"VirtualBox command-line management interface","s":[["TAGLINE","VirtualBox command-line management interface"],["TLDR","**List VMs**\n\n```vboxmanage list vms```\n\n**Start VM**\n\n```vboxmanage startvm [vmname]```\n\n**Start headless**\n\n```vboxmanage startvm [vmname] --type headless```\n\n**Stop VM**\n\n```vboxmanage controlvm [vmname] poweroff```\n\n**Create VM**\n\n```vboxmanage createvm --name [vmname] --register```\n\n**Snapshot**\n\n```vboxmanage snapshot [vmname] take [snapname]```"],["SYNOPSIS","**vboxmanage** _command_ [_options_]"],["PARAMETERS","**list**\n> List objects.\n\n**startvm**\n> Start VM.\n\n**controlvm**\n> Control running VM.\n\n**createvm**\n> Create VM.\n\n**modifyvm**\n> Modify settings.\n\n**snapshot**\n> Snapshot operations.\n\n**showvminfo**\n> Display VM configuration details.\n\n**clonevm**\n> Clone an existing VM.\n\n**storagectl**\n> Manage storage controllers.\n\n**storageattach**\n> Attach storage media to a VM.\n\n**sharedfolder**\n> Manage shared folders between host and guest.\n\n**guestcontrol**\n> Execute commands within guest OS.\n\n**--type** _TYPE_\n> Start type (gui, headless, separate).\n\n**-q**, **--nologo**\n> Suppress logo output for scripting.\n\n**--machinereadable**\n> Machine-parsable output format."],["DESCRIPTION","**vboxmanage** is the command-line interface for Oracle VirtualBox, providing complete control over virtual machine creation, configuration, and operation. It exposes every feature available in the graphical interface and many that are only accessible from the command line.\n\nThe tool uses subcommands for different operations: **createvm** and **modifyvm** for setup, **startvm** and **controlvm** for runtime management, **snapshot** for state saving, and **list** for querying registered objects. Most configuration changes via **modifyvm** require the VM to be powered off. VMs can be identified by name or UUID."],["CAVEATS","VirtualBox must be installed. Most **modifyvm** changes require the VM to be powered off. Guest Additions must be installed for advanced features like shared folders and guest control."],["SEE ALSO","[qemu](/man/qemu)(1), [virsh](/man/virsh)(1)"]],"e":["qemu","virsh"],"x":"vboxmanage list vms"},"vlc":{"n":"vlc","t":"Cross-platform multimedia player and streamer","s":[["TAGLINE","Cross-platform multimedia player and streamer"],["TLDR","**Play a media file**\n\n```vlc [file]```\n\n**Play multiple files**\n\n```vlc [file1] [file2] [file3]```\n\n**Play a URL/stream**\n\n```vlc [https://example.com/stream.m3u8]```\n\n**Play in fullscreen**\n\n```vlc --fullscreen [file]```\n\n**Play without GUI** (command line)\n\n```cvlc [file]```\n\n**Loop playback**\n\n```vlc --loop [file]```\n\n**Play at specific volume** (0-1024, 256 = 100%)\n\n```vlc --volume=[200] [file]```\n\n**Stream to HTTP**\n\n```vlc [file] --sout '#standard{access=http,mux=ts,dst=:8080}'```"],["SYNOPSIS","**vlc** [_options_] [_file_|_url_...]"],["PARAMETERS","**--fullscreen**, **-f**\n> Start in fullscreen mode\n\n**--loop**, **-L**\n> Loop playlist\n\n**--repeat**, **-R**\n> Repeat current item\n\n**--random**, **-Z**\n> Random playback\n\n**--volume** _N_\n> Set volume (0-1024, 256 = 100%)\n\n**--play-and-exit**\n> Exit after playing\n\n**--start-time** _N_\n> Start at N seconds\n\n**--stop-time** _N_\n> Stop at N seconds\n\n**--rate** _N_\n> Playback speed (1.0 = normal)\n\n**--no-video**\n> Audio only\n\n**--no-audio**\n> Video only\n\n**--sout** _chain_\n> Stream output chain\n\n**--intf** _interface_\n> Interface module (qt, ncurses, dummy)\n\n**-I dummy**, **--intf=dummy**\n> No interface (background)"],["VARIANTS","**vlc**: Full GUI version\n**cvlc**: Command-line only (no GUI)\n**nvlc**: NCurses text interface\n**rvlc**: Remote control interface\n**svlc**: Skinnable interface"],["DESCRIPTION","**VLC** is a free, open-source multimedia player supporting virtually all audio and video formats without external codecs. It can play local files, discs, streams, and capture devices.\n\nBeyond playback, VLC includes powerful streaming and transcoding capabilities. It can serve as a streaming server, convert between formats, and apply various filters and effects.\n\nThe command-line interface provides full access to VLC's features for scripting and automation. Use **cvlc** for headless operation without starting the GUI.\n\nVLC's media library and playlist features help organize large collections, while its network streaming supports protocols like HTTP, RTSP, and HLS."],["CAVEATS","The **--sout** streaming syntax is complex. Consult VLC documentation for stream output chain construction.\n\nHardware acceleration options vary by platform. Use **--avcodec-hw** to control hardware decoding.\n\nSome proprietary formats may have playback issues due to licensing. VLC uses open-source implementations that may not match commercial decoders exactly."],["HISTORY","VLC was created as a student project at **École Centrale Paris** in **1996** under the name VideoLAN Client. It became open source in **2001** and grew into one of the most popular media players, known for playing \"anything\" without codec packs."],["INSTALL","```apt: sudo apt install vlc-bin```\n\n```dnf: sudo dnf install vlc```\n\n```pacman: sudo pacman -S vlc```\n\n```apk: sudo apk add vlc```\n\n```zypper: sudo zypper install vlc```\n\n```nix: nix profile install nixpkgs#vlc-bin```"],["SEE ALSO","[mpv](/man/mpv)(1), [ffmpeg](/man/ffmpeg)(1), [mplayer](/man/mplayer)(1), [ffplay](/man/ffplay)(1)"]],"e":["mpv","ffmpeg","mplayer","ffplay"],"x":"vlc [file]"},"vtysh":{"n":"vtysh","t":"Integrated shell for FRRouting daemons","s":[["TAGLINE","Integrated shell for FRRouting daemons"],["TLDR","**Start the interactive shell**\n\n```sudo vtysh```\n\n**Execute a single command**\n\n```sudo vtysh -c \"[show ip route]\"```\n\n**Execute command and echo it** (for scripting)\n\n```sudo vtysh -E -c \"[show ip route]\"```\n\n**Connect to a specific daemon** only\n\n```sudo vtysh -d [bgpd]```\n\n**Execute boot startup configuration**\n\n```sudo vtysh -b```\n\n**Show running configuration**\n\n```sudo vtysh -c \"show running-config\"```"],["SYNOPSIS","**vtysh** [**-b**] [**-c** _command_] [**-d** _daemon_] [**-E**] [**-h**]"],["PARAMETERS","**-b**, **--boot**\n> Execute boot startup configuration from the integrated config file.\n\n**-c** _command_, **--command** _command_\n> Execute specified command and exit. Can be used multiple times for multiple commands.\n\n**-d** _daemon_, **--daemon** _daemon_\n> Connect to specific daemon only (zebra, bgpd, ospfd, ripd, isisd, etc.).\n\n**-E**, **--echo**\n> Echo the command and prompt before displaying results. Useful for scripting.\n\n**-C**, **--dryrun**\n> Check the configuration file for syntax validity without applying.\n\n**-n**, **--noerror**\n> Ignore errors for purposes of return codes (useful in scripts).\n\n**-u**, **--user**\n> Restrict to unprivileged mode, disallowing configuration commands.\n\n**-h**, **--help**\n> Display help message."],["DESCRIPTION","**vtysh** is an integrated command shell for FRRouting (FRR) and Quagga routing software. It provides a unified Cisco IOS-like CLI for configuring and monitoring all routing daemons (Zebra, BGP, OSPF, RIP, IS-IS, etc.) from a single interface.\n\nThe shell operates in hierarchical modes: User EXEC mode for basic commands, Privileged EXEC mode (via **enable**) for administrative commands, and Configuration mode (via **configure terminal**) for making changes. Each routing protocol has its own configuration sub-mode.\n\nvtysh connects to all running routing daemons through their control sockets, presenting their combined functionality. Commands like **show ip route**, **show bgp summary**, and **show running-config** aggregate information from all relevant daemons.\n\nConfiguration can be saved with **write memory** to the integrated config file (/etc/frr/frr.conf) or individual daemon configs."],["CAVEATS","Requires root privileges or membership in the frr/quagga group to access daemon control sockets. The integrated config file must be enabled for unified configuration. Some commands may behave differently than Cisco IOS despite similar syntax. Tab completion and context-sensitive help (?) are available in interactive mode."],["HISTORY","**vtysh** was developed as part of the **Quagga** routing suite, which forked from GNU Zebra in **2003**. When Quagga development stalled, the **FRRouting** project forked it in **2017** and continued active development. vtysh remains the primary CLI for FRR, providing a familiar interface for network engineers accustomed to commercial router operating systems."],["INSTALL","```apt: sudo apt install frr```\n\n```dnf: sudo dnf install frr```\n\n```apk: sudo apk add quagga```\n\n```zypper: sudo zypper install frr```\n\n```nix: nix profile install nixpkgs#frr```"],["SEE ALSO","[zebra](/man/zebra)(8), [bgpd](/man/bgpd)(8), [ospfd](/man/ospfd)(8), [ripd](/man/ripd)(8)"]],"e":["zebra","bgpd","ospfd","ripd"],"x":"sudo vtysh"},"weechat":{"n":"weechat","t":"Extensible terminal-based chat client","s":[["TAGLINE","Extensible terminal-based chat client"],["TLDR","**Start WeeChat**\n\n```weechat```\n\n**Connect to IRC server**\n\n```/connect [irc.libera.chat]```\n\n**Join channel**\n\n```/join [#channel]```\n\n**Send private message**\n\n```/msg [nickname] [message]```\n\n**Start with specific config directory**\n\n```weechat -d [~/.config/weechat]```\n\n**Run command on startup**\n\n```weechat -r \"/connect libera\"```\n\n**Set nickname**\n\n```/set irc.server.libera.nicks \"[mynick]\"```\n\n**Quit WeeChat**\n\n```/quit```"],["SYNOPSIS","**weechat** [_-d directory_] [_-r command_] [_-p_] [_options_]"],["PARAMETERS","**-a**, **--no-connect**\n> Disable auto-connect to servers.\n\n**-c**, **--colors**\n> Display colors in terminal.\n\n**-d**, **--dir** _DIR_\n> Set configuration directory.\n\n**-p**, **--no-plugin**\n> Don't load plugins.\n\n**-P**, **--plugins** _LIST_\n> Load only specified plugins.\n\n**-r**, **--run-command** _CMD_\n> Run commands after startup.\n\n**-s**, **--no-script**\n> Don't load scripts.\n\n**-t**, **--temp-dir**\n> Use temporary directory.\n\n**-u**, **--upgrade**\n> Upgrade from previous session.\n\n**-v**, **--version**\n> Show version.\n\n**-h**, **--help**\n> Show help."],["COMMANDS","**/connect** _SERVER_\n> Connect to server.\n\n**/disconnect**\n> Disconnect from server.\n\n**/join** _CHANNEL_\n> Join channel.\n\n**/part** [_CHANNEL_]\n> Leave channel.\n\n**/msg** _TARGET_ _MESSAGE_\n> Send message.\n\n**/query** _NICK_\n> Open private conversation.\n\n**/nick** _NEWNICK_\n> Change nickname.\n\n**/set** _OPTION_ [_VALUE_]\n> Set configuration option.\n\n**/save**\n> Save configuration.\n\n**/quit**\n> Exit WeeChat.\n\n**/help** [_COMMAND_]\n> Show help."],["DESCRIPTION","**WeeChat** (Wee Enhanced Environment for Chat) is an extensible terminal-based chat client. While primarily an IRC client, plugins extend it to Matrix, Slack, Discord, and other protocols.\n\nThe interface uses multiple buffers for channels, private messages, and server consoles. Navigation uses Alt+arrow keys or buffer numbers. Windows can split horizontally or vertically.\n\nConfiguration through /set covers everything: colors, keybindings, network settings, and plugin options. Changes apply immediately; /save persists them.\n\nScripts extend functionality in Python, Perl, Ruby, Lua, and other languages. Popular scripts add features: notifications, auto-join, URL handling, and protocol bridges. The scripts repository provides community contributions.\n\nRelay plugin enables remote access from other clients or mobile apps. This provides a persistent connection that mobile devices can disconnect/reconnect to.\n\nSecurity features include SASL authentication, SSL/TLS, and proxy support. Password handling integrates with system keyrings."],["CAVEATS","Learning curve for commands and configuration. Some features require plugins/scripts. Mobile use requires relay setup. Default colors may not suit all terminals. IRC-centric despite multi-protocol support."],["HISTORY","**WeeChat** was created by **Sébastien Helleu** (FlashCode) in **2003**. The name references \"wee\" (small) and evolved to mean \"extensible environment.\" Written in C for performance with extensive scripting support, it became a popular alternative to irssi for terminal chat."],["INSTALL","```apt: sudo apt install weechat-curses```\n\n```dnf: sudo dnf install weechat```\n\n```pacman: sudo pacman -S weechat```\n\n```apk: sudo apk add weechat```\n\n```zypper: sudo zypper install weechat```\n\n```brew: brew install weechat```\n\n```nix: nix profile install nixpkgs#weechat```"],["SEE ALSO","[irssi](/man/irssi)(1), [hexchat](/man/hexchat)(1), [matrix-commander](/man/matrix-commander)(1)"]],"e":["irssi","hexchat","matrix-commander"],"x":"weechat"}});
