window.LCL_BUCKET(58,{"Xwayland":{"n":"Xwayland","t":"X11 compatibility layer for Wayland","s":[["TAGLINE","X11 compatibility layer for Wayland"],["TLDR","**Start rootless** (typical usage by compositor)\n\n```Xwayland :0 -rootless```\n\n**Start rootful for testing**\n\n```Xwayland :1 -geometry [1920x1080]```\n\n**Start fullscreen rootful**\n\n```Xwayland :1 -fullscreen```\n\n**Start with decorations**\n\n```Xwayland :1 -decorate```\n\n**Force shared memory backend**\n\n```Xwayland :0 -rootless -shm```\n\n**Enable verbose output**\n\n```Xwayland :0 -rootless -verbose [2]```"],["SYNOPSIS","**Xwayland** [:_display_] [_options_]"],["PARAMETERS","**-rootless**\n> Run rootless, integrating X clients with Wayland desktop.\n\n**-fullscreen**\n> Run rootful window fullscreen.\n\n**-geometry** _WxH_\n> Set rootful window geometry.\n\n**-decorate**\n> Add decorations to rootful window.\n\n**-output** _name_\n> Output for fullscreen rootful.\n\n**-host-grab**\n> Disable host shortcuts, confine pointer (Ctrl+Shift to release).\n\n**-shm**\n> Force shared memory backend.\n\n**-glamor**\n> Force OpenGL rendering (not GL ES).\n\n**-hidpi**\n> Adjust to output scale in rootful mode.\n\n**-noTouchPointerEmulation**\n> Disable touch pointer emulation.\n\n**-nokeymap**\n> Ignore compositor keymap.\n\n**-listenfd** _fd_\n> Add listen socket (used by compositor).\n\n**-wm** _fd_\n> Window manager socket (used by compositor).\n\n**-verbose** _n_\n> Set verbosity level.\n\n**-version**\n> Display version."],["DESCRIPTION","**Xwayland** is an X server that runs X11 applications under Wayland compositors. It translates X11 protocol to Wayland, enabling legacy X applications to work in modern Wayland desktops.\n\nIn rootless mode (default), X windows integrate seamlessly with Wayland windows, managed by the compositor. In rootful mode, Xwayland runs in its own window for testing or isolation.\n\nXwayland is typically spawned automatically by the Wayland compositor (GNOME, KDE Plasma, Sway, etc.) when X11 applications are launched.\n\nInput, clipboard, and drag-and-drop are bridged between X11 and Wayland contexts."],["CAVEATS","Some X11-specific features may not work (screen capture, global hotkeys). Performance may be lower than native Wayland. Rootful mode is mainly for testing. HiDPI scaling requires compositor support."],["HISTORY","**Xwayland** was developed as part of the X.Org server project to enable X11 compatibility during the transition to Wayland. It became essential for running applications that haven't been ported to Wayland natively."],["INSTALL","```apt: sudo apt install xwayland```\n\n```apk: sudo apk add xwayland```\n\n```zypper: sudo zypper install xwayland```\n\n```nix: nix profile install nixpkgs#xwayland```"],["SEE ALSO","[Xorg](/man/Xorg)(1), [sway](/man/sway)(1)"]],"e":["Xorg","sway"],"x":"Xwayland :0 -rootless"},"aoe":{"n":"aoe","t":"Agent of Empires - session manager for parallel AI coding agents","s":[["TAGLINE","Agent of Empires - session manager for parallel AI coding agents"],["TLDR","**Launch the interactive TUI** in the current directory\n\n```aoe```\n\n**Create a session** running a specific agent CLI\n\n```aoe add --cmd [claude]```\n\n**Add a session on a worktree branch**\n\n```aoe add --cmd [claude] --branch [feature/login]```\n\n**Start the web dashboard** for browser/phone access\n\n```aoe serve```\n\n**Expose the dashboard publicly** over HTTPS with QR auth\n\n```aoe serve --tunnel```\n\n**View debug logs**\n\n```aoe logs```\n\n**List active sessions** (also available in the TUI)\n\n```aoe list```"],["SYNOPSIS","**aoe** [_command_] [_options_]"],["PARAMETERS","**add**\n> Create a new agent session.\n> **--cmd** _name_: agent CLI to launch (claude, opencode, gemini, codex, copilot, cursor, ...).\n> **--branch** _name_: create or attach to a git worktree on that branch.\n> **--sandbox**: run the session inside a Docker container with shared auth volumes.\n> **--name** _label_: friendly name shown in the TUI.\n\n**list**\n> Print all known sessions and their status.\n\n**attach** _name_\n> Attach the current terminal to an existing session (equivalent to tmux attach).\n\n**kill** _name_\n> Terminate a session and clean up its worktree.\n\n**serve**\n> Start the web dashboard.\n> **--port** _n_: HTTP port (default 8765).\n> **--tunnel**: expose dashboard over HTTPS with QR-based pairing for phones.\n\n**logs**\n> Tail the AoE daemon log in the best viewer available.\n\n**doctor**\n> Print environment diagnostics (tmux, docker, agent binaries detected).\n\n**--help**, **-h**\n> Print help and exit.\n\n**--version**\n> Print version and exit."],["DESCRIPTION","**aoe** (Agent of Empires) is a Rust terminal session manager that orchestrates **multiple AI coding agents in parallel**. Each session runs inside its own **tmux** window — and optionally inside a **Docker** sandbox — so agents can work on different branches of the same repository without stepping on each other. The default invocation opens a **TUI** that shows every session, its current status (running, waiting for input, idle), and a diff viewer for reviewing changes without leaving the terminal.\n\nOut of the box, aoe knows how to spawn **Claude Code**, **OpenCode**, **Gemini CLI**, **Codex CLI**, **Copilot CLI**, **Cursor CLI**, **Mistral Vibe**, **Pi.dev**, **Factory Droid**, **Hermes**, **Qwen Code**, and **Kiro CLI**. Sessions persist across SSH disconnects, terminal closes, and aoe restarts, because the underlying tmux server keeps them alive. A separate **web dashboard** (**aoe serve**) makes the same sessions accessible from a browser or, with **--tunnel**, from a phone over HTTPS."],["CONFIGURATION","**~/.config/aoe/**\n> User config: agent definitions, default branch naming, dashboard auth.\n\n**~/.local/share/aoe/**\n> Per-session state, logs, and worktree metadata.\n\n**tmux**\n> Required runtime dependency; every session is a tmux window. Use **Ctrl+b d** to detach back to the AoE TUI.\n\n**Docker**\n> Optional. Required for **--sandbox**; AoE bind-mounts agent auth directories so sandboxed sessions stay signed in."],["CAVEATS","Native Windows is **not supported** — use **WSL2** instead. **tmux** is required and must be on **PATH**. Sandboxed sessions share auth volumes with the host, so secrets are visible to agents inside the container; treat this like running the agent locally. The web dashboard is in **beta**; pin a version if you script against its API."],["HISTORY","**Agent of Empires** was created by **Nate Brake** at **Mozilla.ai** and released in **late 2025** to scratch the practical itch of running several coding agents at once across long-lived sessions on a laptop or remote server. It is written in **Rust**, distributed via **Homebrew**, **Nix**, **Cargo**, and an install script, and is licensed under **MIT**."],["INSTALL","```brew: brew install aoe```"],["SEE ALSO","[tmux](/man/tmux)(1), [claude](/man/claude)(1), [gemini](/man/gemini)(1), [git-worktree](/man/git-worktree)(1)"]],"e":["tmux","claude","gemini","git-worktree"],"x":"aoe"},"box":{"n":"box","t":"official CLI for interacting with Box cloud storage","s":[["TAGLINE","official CLI for interacting with Box cloud storage"],["TLDR","**Authenticate** (JWT, OAuth, or CCG, depending on config)\n\n```box login```\n\n**List items** in a folder\n\n```box folders:items [folder_id]```\n\n**Upload a file** to a folder\n\n```box files:upload [file.txt] --parent-id [folder_id]```\n\n**Download a file** by id\n\n```box files:download [file_id]```\n\n**Create a subfolder**\n\n```box folders:create [parent_id] \"[folder_name]\"```\n\n**Search** content\n\n```box search \"[query]\"```\n\n**Create a shared link** for a file\n\n```box shared-links:create [file_id] --type file```\n\n**Execute bulk operations** from CSV\n\n```box [command] --bulk-file-path [commands.csv]```\n\n**Act on behalf of another user** (admin only)\n\n```box users:get [user_id] --as-user [user_id]```\n\n**Output as JSON**\n\n```box files:get [file_id] --json```"],["SYNOPSIS","**box** _topic_**:**_command_ [_arguments_] [_options_]"],["DESCRIPTION","**box** (the **@box/cli** package) is the official Node.js command-line tool for the Box API. It wraps the Box Content API so that technical and non-technical users can perform file, folder, user, and admin operations without writing code.\n\nCommands are grouped by topic, e.g. `files:upload`, `folders:items`, `users:list`, `collaborations:add`. Most commands accept Box identifiers (IDs) rather than paths; `box folders:items 0` lists the root folder. The CLI supports CSV-driven bulk execution via `--bulk-file-path`, letting each row invoke one API call."],["SUBCOMMAND TOPICS","**files** — upload, download, get, update, delete, copy, move, rename, lock, unlock, versions\n**folders** — create, get, items, delete, update, copy, move, rename\n**search** — search by query across the enterprise\n**users** — list, get, create, update, delete, terminate-session\n**groups** — list, create, update, delete, memberships\n**shared-links** — create/get/update/delete on files, folders, and web links\n**collaborations** — add, list, update, remove\n**tasks**, **comments**, **web-links**, **metadata**, **legal-holds**, **retention-policies**, **events**, **webhooks**, **terms-of-service**\n**configure** — create, switch, import, or set environments (auth configs)"],["COMMON OPTIONS","**-h**, **--help**\n> Show help for the CLI or a specific command.\n\n**-q**, **--quiet**\n> Suppress non-error output to stderr.\n\n**-v**, **--verbose**\n> Verbose debug output.\n\n**--as-user** _USER_ID_\n> Make the request on behalf of the given user (admin with App User or enterprise auth).\n\n**--token** _TOKEN_\n> Use the given OAuth/JWT access token instead of the saved environment.\n\n**--json**\n> Output in JSON.\n\n**--csv**\n> Output in CSV.\n\n**--save** / **--save-to-file-path** _PATH_\n> Save command output to a file.\n\n**--fields** _LIST_\n> Comma-separated list of fields to include in the response.\n\n**--bulk-file-path** _FILE_\n> Read a CSV/JSON file of input rows and run the command once per row.\n\n**--no-color**\n> Disable colored output."],["CONFIGURATION","**~/.box**\n> Default directory for saved environments and application auth configs.\n\n**box configure:environments:add** _FILE_\n> Adds a new auth environment from a JWT configuration JSON file.\n\n**box configure:environments:switch** _NAME_\n> Switches the active environment."],["CAVEATS","Requires Node.js 18+ and a Box developer application (JWT, OAuth 2.0, or Client Credentials Grant) with the scopes needed for the target operations. Many admin commands require an enterprise access token. API rate limits apply."],["HISTORY","The **Box CLI** is maintained by **Box, Inc.** as the official open-source command-line interface for the Box Content API. It is distributed on npm as `@box/cli` (formerly `box-cli`). Repository: github.com/box/boxcli."],["SEE ALSO","[rclone](/man/rclone)(1), [gsutil](/man/gsutil)(1)"]],"e":["rclone","gsutil"],"x":"box login"},"brew-autoremove":{"n":"brew-autoremove","t":"Remove orphaned Homebrew dependencies","s":[["TAGLINE","Remove orphaned Homebrew dependencies"],["TLDR","**Remove unused dependencies**\n\n```brew autoremove```\n\n**Preview what would be removed**\n\n```brew autoremove --dry-run```"],["SYNOPSIS","**brew autoremove** [_options_]"],["DESCRIPTION","**brew autoremove** uninstalls formulae that were only installed as dependencies and are no longer needed by any installed formula. It cleans up orphaned dependencies left behind after uninstalling packages."],["PARAMETERS","**--dry-run, -n**\n> List what would be removed without removing"],["HOW IT WORKS","When you install a package like **git**, dependencies (gettext, pcre2) are installed automatically. When you uninstall **git**, those dependencies remain. Running **brew autoremove** removes these orphaned dependencies."],["EXAMPLE WORKFLOW","```\nbrew uninstall git          # Remove package\nbrew autoremove             # Clean up dependencies\nbrew cleanup                # Remove cached downloads\n```"],["PREVENTING REMOVAL","To prevent a formula from being removed by autoremove, reinstall it explicitly to mark it as \"installed on request\":\n\n```brew install formula```\n\nThis changes the formula's status from dependency to explicitly installed."],["RELATED COMMANDS","**brew leaves**\n> Show top-level packages (not dependencies)\n\n**brew deps --installed**\n> Show dependency tree\n\n**brew cleanup**\n> Remove cached downloads and old versions"],["CAVEATS","Only removes formulae installed as dependencies, not those explicitly installed. Safe to run regularly after uninstalling packages. Does not remove casks."],["SEE ALSO","[brew](/man/brew)(1), [brew-uninstall](/man/brew-uninstall)(1), [brew-cleanup](/man/brew-cleanup)(1), [brew-leaves](/man/brew-leaves)(1)"]],"e":["brew","brew-uninstall","brew-cleanup","brew-leaves"],"x":"brew autoremove"},"cacafire":{"n":"cacafire","t":"Display ASCII fire animation in the terminal","s":[["TAGLINE","Display ASCII fire animation in the terminal"],["TLDR","Display an **ASCII fire animation**\n\n```cacafire```"],["SYNOPSIS","**cacafire**"],["DESCRIPTION","**cacafire** displays an animated ASCII art fire in the terminal using the libcaca library. It renders a realistic-looking fire effect using colored text characters.\n\nPress **Space** to pause/resume, **Esc** to exit, or interrupt with Ctrl+C."],["INSTALL","```apt: sudo apt install caca-utils```\n\n```dnf: sudo dnf install caca-utils```\n\n```apk: sudo apk add libcaca-apps```\n\n```zypper: sudo zypper install caca-utils```"],["SEE ALSO","[cacaclock](/man/cacaclock)(1), [cacademo](/man/cacademo)(1), [cacaview](/man/cacaview)(1), [aafire](/man/aafire)(1)"]],"e":["cacaclock","cacademo","cacaview","aafire"],"x":"cacafire"},"clef":{"n":"clef","t":"standalone Ethereum account manager and transaction signer","s":[["TAGLINE","standalone Ethereum account manager and transaction signer"],["TLDR","**Initialize clef** for the first time with a master seed\n\n```clef init```\n\n**Start clef** on mainnet with default settings\n\n```clef```\n\n**Start clef on the Sepolia** testnet with a custom keystore\n\n```clef --chainid 11155111 --keystore [path/to/keystore]```\n\n**Start clef with a custom** configuration directory\n\n```clef --configdir [path/to/clef/config]```\n\n**Start clef with automatic** rule-based transaction approval\n\n```clef --rules [path/to/rules.js]```\n\n**Set a password** for a keystore account\n\n```clef setpw [0xaddress]```\n\n**Start clef in stdio mode** for external signing requests\n\n```clef --stdio-ui```\n\n**Create a new account**\n\n```clef newaccount```"],["SYNOPSIS","**clef** [_options_] [_command_]"],["SUBCOMMANDS","**init**\n> Initialize the signer, generating a master seed and creating necessary directories.\n\n**attest**\n> Attest that a JavaScript rules file is to be used (stores the sha256 hash).\n\n**setpw**\n> Store a credential for a keystore file.\n\n**delpw**\n> Remove a credential for a keystore file.\n\n**newaccount**\n> Create a new account.\n\n**gendoc**\n> Generate documentation about the JSON-RPC format."],["PARAMETERS","**--keystore** _DIR_\n> Directory for the keystore (default: $HOME/.ethereum/keystore).\n\n**--configdir** _DIR_\n> Directory for Clef configuration (default: $HOME/.clef).\n\n**--chainid** _ID_\n> Chain ID for transaction signing (default: 1). Common values: 1=mainnet, 11155111=sepolia.\n\n**--rules** _FILE_\n> Path to JavaScript rules file for automatic transaction approval.\n\n**--stdio-ui**\n> Use standard input/output for user interface (for integration with external tools).\n\n**--ipcpath** _PATH_\n> Filename for IPC socket/pipe.\n\n**--ipcdisable**\n> Disable the IPC-RPC server.\n\n**--http**\n> Enable HTTP-RPC server for remote signing requests.\n\n**--http.addr** _ADDR_\n> HTTP server listening address (default: localhost).\n\n**--http.port** _PORT_\n> HTTP server listening port (default: 8550).\n\n**--http.vhosts** _HOSTS_\n> Comma-separated list of virtual hostnames (default: localhost).\n\n**--signersecret** _FILE_\n> File containing the encrypted master seed.\n\n**--auditlog** _FILE_\n> File for audit logs (default: audit.log).\n\n**--loglevel** _LEVEL_\n> Log level to emit to the screen (default: 4).\n\n**--lightkdf**\n> Reduce key-derivation RAM and CPU usage at some expense of KDF strength.\n\n**--advanced**\n> Issue warnings instead of rejections for advanced operations.\n\n**--suppress-bootwarn**\n> Suppress the boot warning display.\n\n**--help**\n> Display help information."],["DESCRIPTION","**Clef** is a standalone Ethereum account management and signing tool from the go-ethereum project. It provides secure storage of private keys and transaction signing capabilities separate from the main Ethereum client.\n\nClef acts as a signing oracle that can approve or reject transaction signing requests. It supports hardware wallets, encrypted keystores, and can run in a detached mode where a JavaScript rules engine automatically processes signing requests based on predefined policies.\n\nThe tool is designed for enhanced security by isolating key management from network-facing components. It can serve multiple clients through IPC or HTTP interfaces, making it suitable for both individual use and as part of larger infrastructure deployments."],["CAVEATS","Clef requires careful backup of the master seed created during initialization. Lost seeds cannot be recovered. The HTTP interface should only be enabled with proper firewall rules in production environments. Rules-based automatic signing requires thorough security auditing."],["HISTORY","Clef was introduced by the **go-ethereum** (Geth) team in **2018** as part of an effort to improve Ethereum account security. It was designed to replace the integrated account management in Geth with a more secure, standalone signing solution that reduces attack surface."],["INSTALL","```apk: sudo apk add geth-tools```"],["SEE ALSO","[geth](/man/geth)(1)"]],"e":["geth"],"x":"clef init"},"comfyui":{"n":"comfyui","t":"node-based workflow system for Stable Diffusion","s":[["TAGLINE","node-based workflow system for Stable Diffusion"],["TLDR","**Start ComfyUI server**\n\n```python [main.py]```\n\n**Start on specific port**\n\n```python [main.py] --port [8188]```\n\n**Start with CPU only**\n\n```python [main.py] --cpu```\n\n**Start without auto-opening browser**\n\n```python [main.py] --disable-auto-launch```\n\n**Use specific GPU**\n\n```python [main.py] --cuda-device [0]```\n\n**Run with low VRAM mode**\n\n```python [main.py] --lowvram```\n\n**Execute a workflow via API**\n\n```curl -X POST http://localhost:8188/prompt -H \"Content-Type: application/json\" -d @[workflow.json]```"],["SYNOPSIS","**python** _main.py_ [_options_]\n\n**comfyui** [_options_]"],["PARAMETERS","**--port** _PORT_\n> Server port (default: 8188).\n\n**--listen** _ADDR_\n> Listen address (default: 127.0.0.1, use 0.0.0.0 for network).\n\n**--cpu**\n> Run on CPU only.\n\n**--cuda-device** _ID_\n> CUDA GPU device index.\n\n**--lowvram**\n> Low VRAM mode for GPUs with limited memory.\n\n**--dont-print-server**\n> Suppress server output.\n\n**--output-directory** _DIR_\n> Custom output directory.\n\n**--temp-directory** _DIR_\n> Custom temp directory.\n\n**--auto-launch**\n> Auto-open browser on start.\n\n**--disable-auto-launch**\n> Prevent auto-opening browser."],["DESCRIPTION","**ComfyUI** is a node-based workflow system for Stable Diffusion and Flux image generation. It provides both a visual graph editor (web UI) and a REST API for programmatic use.\n\nWorkflows are built by connecting nodes: model loaders, samplers, VAE decoders, prompt encoders, and more. Complex pipelines (img2img, inpainting, ControlNet, LoRA stacking) are constructed visually without code.\n\nThe API accepts workflow JSON, enabling headless batch generation and integration with scripts. Workflows created in the web UI can be exported and run via the API.\n\nAn extensive ecosystem of custom nodes adds capabilities: video generation, face restoration, upscaling, IP-Adapter, and more. Custom nodes are installed into the `custom_nodes/` directory.\n\nInstall via comfy-cli (`pip install comfy-cli && comfy install`) or clone the repository and install requirements. Models are placed in the `models/` directory tree."],["CAVEATS","Web UI requires a modern browser. GPU strongly recommended. Custom nodes may conflict. Model files are large (2-10+ GB each). Python 3.10+ required. Complex workflows can consume significant VRAM."],["HISTORY","**ComfyUI** was created by **comfyanonymous** in **2023** as a modular alternative to the Automatic1111 web UI. Its node-based design attracted power users who needed flexible, reproducible generation pipelines. It became one of the most popular Stable Diffusion interfaces, with a large community building custom nodes."],["INSTALL","```aur: yay -S comfyui```"],["SEE ALSO","[sd-cli](/man/sd-cli)(1), [mflux](/man/mflux)(1), [convert](/man/convert)(1)"]],"e":["sd-cli","mflux","convert"],"x":"python [main.py]"},"crane-cp":{"n":"crane-cp","t":"Alias for crane copy","s":[["TAGLINE","Alias for crane copy"],["TLDR","**Copy an image** between registries\n\n```crane cp [source_image] [destination_image]```\n\n**Copy with platform selection**\n\n```crane cp --platform linux/amd64 [source_image] [destination_image]```"],["SYNOPSIS","**crane** **cp** [_options_] _source_ _destination_"],["DESCRIPTION","**crane cp** is an alias for **crane copy**. It copies container images between registries without pulling to local storage, performing a server-side copy when possible."],["INSTALL","```pacman: sudo pacman -S crane```\n\n```apk: sudo apk add crane```\n\n```zypper: sudo zypper install crane```\n\n```brew: brew install crane```\n\n```nix: nix profile install nixpkgs#crane```"],["SEE ALSO","[crane](/man/crane)(1), [crane-copy](/man/crane-copy)(1), [crane-push](/man/crane-push)(1), [crane-pull](/man/crane-pull)(1)"]],"e":["crane","crane-copy","crane-push","crane-pull"],"x":"crane cp [source_image] [destination_image]"},"ctags":{"n":"ctags","t":"Source code tag index generator","s":[["TAGLINE","Source code tag index generator"],["TLDR","**Generate tags for current directory**\n\n```ctags *```\n\n**Generate tags recursively**\n\n```ctags -R```\n\n**Generate tags for specific files**\n\n```ctags [file1.c] [file2.c]```\n\n**Output to specific file**\n\n```ctags -f [tags] -R [src/]```\n\n**Append to existing tags file**\n\n```ctags -a [newfile.c]```\n\n**List supported languages**\n\n```ctags --list-languages```\n\n**List tag kinds for language**\n\n```ctags --list-kinds=[c]```"],["SYNOPSIS","**ctags** [_options_] [_file_...]"],["DESCRIPTION","**ctags** generates index files (tags) that map identifiers in source code to their definitions. This enables text editors and IDEs to quickly navigate to function definitions, class declarations, variables, and other programming constructs across large codebases.\n\nThe tool parses source code in dozens of programming languages, extracting symbols like function names, classes, methods, variables, macros, and type definitions. It creates a tags file (default name: **tags**) containing the symbol name, file location, and search pattern to find it.\n\nModern editors like Vim, Emacs, and many IDEs use ctags files for features like \"go to definition,\" symbol completion, and code navigation. Universal Ctags is the actively maintained fork and supports more languages and features than the original Exuberant Ctags."],["PARAMETERS","**-R**, **--recurse**\n> Recurse into directories\n\n**-f** _file_, **-o** _file_\n> Output tags to file\n\n**-a**, **--append**\n> Append to existing tags file\n\n**-L** _file_, **--file-list** _file_\n> Read file names from file\n\n**--languages** _list_\n> Restrict to languages\n\n**--language-force** _lang_\n> Force specific language\n\n**--exclude** _pattern_\n> Exclude files matching pattern\n\n**--extras** _flags_\n> Extra tag entries\n\n**--fields** _flags_\n> Fields to include\n\n**--kinds-\\<lang\\>** _kinds_\n> Tag kinds to include\n\n**--list-languages**\n> List supported languages\n\n**--list-kinds**[=_lang_]\n> List tag kinds\n\n**--list-maps**\n> List file extension mappings"],["CONFIGURATION","**~/.ctags**\n> User configuration file for tag generation options and language definitions.\n\n**~/.ctags.d/**\n> Directory containing modular configuration files for different languages and settings.\n\n**.ctags.d/**\n> Project-level configuration directory for repository-specific tag settings."],["TAG KINDS","**c**: classes\n\n**f**: functions\n\n**m**: class members\n\n**v**: variables\n\n**t**: typedefs\n\n**d**: macro definitions"],["IMPLEMENTATIONS","**Universal Ctags**: Actively maintained fork\n\n**Exuberant Ctags**: Original implementation (unmaintained)"],["CAVEATS","Output file named 'tags' by default. Use with Vim :tag command or Ctrl-]. Universal Ctags recommended over Exuberant Ctags. Large codebases may produce large tag files. Requires regeneration after code changes."],["HISTORY","**ctags** was originally developed as part of the BSD Unix project in the late 1970s. **Exuberant Ctags** by Darren Hiebert became the dominant implementation in 1996, adding support for many languages. **Universal Ctags** forked in 2014 and is now the actively maintained version with modern language support."],["INSTALL","```dnf: sudo dnf install ctags```\n\n```pacman: sudo pacman -S ctags```\n\n```apk: sudo apk add ctags```\n\n```zypper: sudo zypper install ctags```\n\n```brew: brew install ctags```\n\n```nix: nix profile install nixpkgs#ctags```"],["SEE ALSO","[etags](/man/etags)(1), [vim](/man/vim)(1), [cscope](/man/cscope)(1)"],["RESOURCES","```[Source code](https://github.com/universal-ctags/ctags)```\n\n```[Homepage](https://ctags.io)```\n\n```[Documentation](https://docs.ctags.io)```"]],"e":["etags","vim","cscope"],"x":"ctags *"},"cups":{"n":"cups","t":"Common UNIX Printing System","s":[["TAGLINE","Common UNIX Printing System"],["TLDR","**List available printers**\n\n```lpstat -p```\n\n**Print file to default printer**\n\n```lp [file.pdf]```\n\n**Print to specific printer**\n\n```lp -d [printer-name] [file.pdf]```\n\n**Print multiple copies**\n\n```lp -n [3] [file.pdf]```\n\n**Check print queue**\n\n```lpq```\n\n**Cancel print job**\n\n```cancel [job-id]```\n\n**Add printer**\n\n```lpadmin -p [printer-name] -E -v [ipp://printer/ipp/print] -m everywhere```"],["SYNOPSIS","**lp** [_options_] [_file_...]\n\n**lpr** [_options_] [_file_...]\n\n**lpstat** [_options_]"],["DESCRIPTION","**CUPS** (Common UNIX Printing System) manages printing on Linux. Uses IPP protocol for network printing. Provides lp, lpr, lpstat, and other commands. Web interface at http://localhost:631."],["PRINT COMMANDS","**lp**, **lpr**\n> Print files\n\n**lpstat**\n> Show printer status\n\n**lpq**\n> Show print queue\n\n**lprm**, **cancel**\n> Remove print jobs\n\n**lpadmin**\n> Administer printers\n\n**lpoptions**\n> Set printer options\n\n**lpinfo**\n> List drivers and devices"],["PRINT OPTIONS","**-d** _printer_\n> Destination printer\n\n**-n** _copies_\n> Number of copies\n\n**-o media=** _size_\n> Paper size (Letter, A4, etc.)\n\n**-o sides=** _mode_\n> two-sided-long-edge, two-sided-short-edge\n\n**-o number-up=** _N_\n> N pages per sheet (1,2,4,6,9,16)\n\n**-o job-hold-until=** _time_\n> Hold until time (indefinite, night, weekend)\n\n**-o job-priority=** _N_\n> Priority 1-100"],["WEB INTERFACE","Administration: http://localhost:631/admin\n\nRequires user in lpadmin group."],["CONFIGURATION","**/etc/cups/cupsd.conf**\n> Main CUPS daemon configuration file controlling server behavior, access control, and logging.\n\n**/etc/cups/printers.conf**\n> Printer definitions and settings (managed automatically by cupsd).\n\n**~/.cups/lpoptions**\n> User-specific default printer options and preferences."],["CAVEATS","Use lpstat -p -d to see printers and default. Configure via web interface for easiest setup. cups-browsed auto-discovers network printers."],["INSTALL","```dnf: sudo dnf install cups```\n\n```pacman: sudo pacman -S cups```\n\n```zypper: sudo zypper install cups```\n\n```brew: brew install cups```\n\n```nix: nix profile install nixpkgs#cups```"],["SEE ALSO","[lp](/man/lp)(1), [lpr](/man/lpr)(1), [lpstat](/man/lpstat)(1), [lpq](/man/lpq)(1), [lprm](/man/lprm)(1), [lpadmin](/man/lpadmin)(8), [lpoptions](/man/lpoptions)(1), [cupsctl](/man/cupsctl)(8)"]],"e":["lp","lpr","lpstat","lpq","lprm","lpadmin","lpoptions","cupsctl"],"x":"lpstat -p"},"dcraw":{"n":"dcraw","t":"universal RAW camera image decoder","s":[["TAGLINE","universal RAW camera image decoder"],["TLDR","**Convert RAW to PPM with default settings**\n\n```dcraw [image.raw]```\n\n**Convert to 16-bit linear TIFF**\n\n```dcraw -T -4 [image.raw]```\n\n**Extract embedded JPEG thumbnail**\n\n```dcraw -e [image.raw]```\n\n**Convert with automatic white balance**\n\n```dcraw -a [image.raw]```\n\n**Convert with camera white balance**\n\n```dcraw -w [image.raw]```\n\n**Output to stdout as PPM**\n\n```dcraw -c [image.raw] > [output.ppm]```\n\n**Half-size output** (faster, less noise)\n\n```dcraw -h [image.raw]```"],["SYNOPSIS","**dcraw** [_options_] _rawfile_..."],["PARAMETERS","**-c**\n> Write to stdout.\n\n**-e**\n> Extract embedded thumbnail.\n\n**-T**\n> Output TIFF instead of PPM.\n\n**-4**\n> Linear 16-bit, no gamma curve.\n\n**-6**\n> 16-bit output with gamma curve.\n\n**-w**\n> Use camera white balance.\n\n**-a**\n> Calculate white balance from entire image.\n\n**-W**\n> Don't automatically brighten image.\n\n**-h**\n> Half-size output (2x2 pixels averaged).\n\n**-q** _n_\n> Interpolation quality (0-3).\n\n**-o** _n_\n> Output colorspace (0=raw, 1=sRGB, 2=AdobeRGB, etc.).\n\n**-v**\n> Verbose output.\n\n**-i**\n> Identify file, don't decode."],["DESCRIPTION","**dcraw** is a universal RAW image decoder that converts proprietary camera RAW formats to standard PPM or TIFF images. It supports hundreds of camera models from all major manufacturers.\n\nThe tool performs demosaicing (converting Bayer pattern to full color), white balance, gamma correction, and color space conversion. Output can be 8-bit or 16-bit, linear or gamma-corrected, depending on intended use.\n\ndcraw is often used as a backend by other applications (GIMP, digiKam) or in batch processing pipelines. Its predictable, consistent behavior makes it valuable for automated processing where reproducibility matters."],["CAVEATS","Development has slowed (last major update 2018); some newer cameras may not be supported. For actively maintained alternatives, consider libraw or RawTherapee. Output quality may not match camera-specific software. Some RAW features (lens corrections, proprietary algorithms) are not replicated."],["HISTORY","dcraw was written by **Dave Coffin** starting in **1997**, initially to decode his own camera's RAW files. It grew through reverse-engineering efforts to support nearly every camera manufacturer's format. The software became foundational in open-source photography, enabling Linux RAW processing years before manufacturers provided support. Dave Coffin maintained it as a personal project for over 20 years."],["INSTALL","```apt: sudo apt install dcraw```\n\n```dnf: sudo dnf install dcraw```\n\n```pacman: sudo pacman -S dcraw```\n\n```zypper: sudo zypper install dcraw```\n\n```brew: brew install dcraw```\n\n```nix: nix profile install nixpkgs#dcraw```"],["SEE ALSO","[exiftool](/man/exiftool)(1)"]],"e":["exiftool"],"x":"dcraw [image.raw]"},"dijkstra":{"n":"dijkstra","t":"shortest path graph algorithm filter","s":[["TAGLINE","shortest path graph algorithm filter"],["TLDR","**Compute shortest path from a source node in a graph**\n\n```dijkstra -p [source_node] [input.dot]```\n\n**Output distance matrix for all pairs of nodes**\n\n```dijkstra -d [input.dot]```\n\n**Write processed graph to output file**\n\n```dijkstra [input.dot] -o [output.dot]```\n\n**Compute shortest paths and output to file**\n\n```dijkstra -p [source_node] -o [output.dot] [input.dot]```"],["SYNOPSIS","**dijkstra** [_options_] [_files_]"],["PARAMETERS","**-o** _file_\n> Output file.\n\n**-p** _node_\n> Set source node for shortest path computation.\n\n**-d**\n> Output distance matrix."],["DESCRIPTION","**dijkstra** is a specialized Graphviz filter implementing Dijkstra's shortest path algorithm for graph analysis. It reads graph descriptions in DOT format, computes shortest paths between nodes, and outputs modified graphs with calculated distances or path information.\n\nThe tool can calculate distance matrices showing the shortest path length between all pairs of nodes in a graph. It modifies graph attributes to reflect path calculations, which can then be used by other Graphviz layout tools for visualization.\n\nThis utility is part of the Graphviz suite and is primarily used for network analysis, routing problems, and graph algorithm research. It processes graphs with weighted edges and can output results in various formats compatible with other Graphviz tools."],["INSTALL","```apt: sudo apt install graphviz```\n\n```dnf: sudo dnf install graphviz```\n\n```pacman: sudo pacman -S graphviz```\n\n```apk: sudo apk add graphviz```\n\n```zypper: sudo zypper install graphviz```\n\n```brew: brew install graphviz```\n\n```nix: nix profile install nixpkgs#graphviz```"],["SEE ALSO","[dot](/man/dot)(1), [neato](/man/neato)(1), [sfdp](/man/sfdp)(1)"]],"e":["dot","neato","sfdp"],"x":"dijkstra -p [source_node] [input.dot]"},"dmtxwrite":{"n":"dmtxwrite","t":"Data Matrix barcode encoder","s":[["TAGLINE","Data Matrix barcode encoder"],["TLDR","**Encode text as Data Matrix barcode**\n\n```echo \"[text]\" | dmtxwrite -o [output.png]```\n\n**Create barcode from file contents**\n\n```dmtxwrite -o [output.png] < [data.txt]```\n\n**Set module (cell) size in pixels**\n\n```echo \"[text]\" | dmtxwrite -d [5] -o [output.png]```\n\n**Set margin size in pixels**\n\n```echo \"[text]\" | dmtxwrite -m [10] -o [output.png]```\n\n**Create SVG output**\n\n```echo \"[text]\" | dmtxwrite -o [output.svg]```\n\n**Use specific symbol size**\n\n```echo \"[text]\" | dmtxwrite -s s -o [output.png]```\n\n**Print codeword listing instead of barcode**\n\n```echo \"[text]\" | dmtxwrite -c```\n\n**Set image print resolution (DPI)**\n\n```echo \"[text]\" | dmtxwrite -R [300] -o [output.png]```"],["SYNOPSIS","**dmtxwrite** [_options_] [**-o** _output_]"],["PARAMETERS","**-o**, **--output** _file_\n> Output filename. Default is standard output.\n\n**-d**, **--module** _pixels_\n> Module (cell) size in pixels.\n\n**-m**, **--margin** _pixels_\n> Margin size in pixels.\n\n**-s**, **--symbol-size** _size_\n> Symbol size: s(quare auto), r(ectangle auto), or RxC (e.g., 24x24).\n\n**-e**, **--encoding** _scheme_\n> Encodation scheme: b(est), a(scii) [default], c(40), t(ext), x(12), e(difact), 8 (Base 256).\n\n**-R**, **--resolution** _dpi_\n> Image print resolution in DPI.\n\n**-f**, **--format** _format_\n> Output format: png [default], tif, gif, pdf, svg.\n\n**-c**, **--codewords**\n> Print codeword listing instead of creating barcode image.\n\n**-M**, **--mosaic**\n> Create non-standard Data Mosaic barcode.\n\n**-v**, **--verbose**\n> Verbose output.\n\n**-V**, **--version**\n> Print version information."],["DESCRIPTION","**dmtxwrite** generates Data Matrix barcode images from input text or binary data. Data Matrix barcodes are 2D square or rectangular patterns that encode data with built-in error correction.\n\nThe tool reads data from standard input and produces images in various formats. Symbol size is automatically chosen based on data length, or can be specified manually. Error correction is automatically included per the Data Matrix standard.\n\nData Matrix barcodes are widely used for small item marking (electronics, medical devices), logistics, and anywhere high data density is needed in a small space."],["SYMBOL SIZES","**s**: Automatic square selection\n**r**: Automatic rectangle selection\n**10x10** to **144x144**: Specific square sizes\n**8x18** to **16x48**: Specific rectangle sizes"],["CAVEATS","Data capacity depends on symbol size and encoding; larger symbols hold more data. Binary data encoding may require specific encoding schemes. Very small module sizes may not scan reliably. Some scanners have difficulty with rectangular Data Matrix codes."],["HISTORY","dmtxwrite is part of **libdmtx**, created by **Mike Laughton** in **2006**. The library provides open-source tools for Data Matrix barcode generation and reading. Data Matrix is an ISO/IEC 16022 standard, originally developed for industrial marking applications."],["INSTALL","```apt: sudo apt install dmtx-utils```\n\n```dnf: sudo dnf install dmtx-utils```\n\n```brew: brew install dmtx-utils```\n\n```nix: nix profile install nixpkgs#dmtx-utils```"],["SEE ALSO","[dmtxread](/man/dmtxread)(1), [qrencode](/man/qrencode)(1), [zint](/man/zint)(1)"]],"e":["dmtxread","qrencode","zint"],"x":"echo \"[text]\" | dmtxwrite -o [output.png]"},"docker-rename":{"n":"docker-rename","t":"change container name identifier","s":[["TAGLINE","change container name identifier"],["TLDR","**Rename a container**\n\n```docker rename [old_name] [new_name]```"],["SYNOPSIS","**docker** **rename** _container_ _new_name_"],["DESCRIPTION","**docker rename** renames an existing container. Works on both running and stopped containers. Useful for giving meaningful names to containers created without --name. Container names must be unique within the Docker host and follow naming conventions (alphanumeric characters, underscores, periods, and hyphens)."],["INSTALL","```apt: sudo apt install docker-cli```\n\n```dnf: sudo dnf install docker-cli```\n\n```pacman: sudo pacman -S docker```\n\n```apk: sudo apk add docker-cli```\n\n```zypper: sudo zypper install docker```\n\n```brew: brew install docker```\n\n```nix: nix profile install nixpkgs#docker```"],["SEE ALSO","[docker-container-rename](/man/docker-container-rename)(1), [docker-run](/man/docker-run)(1)"],["RESOURCES","```[Source code](https://github.com/docker/cli)```\n\n```[Documentation](https://docs.docker.com/reference/cli/docker/)```"]],"e":["docker-container-rename","docker-run"],"x":"docker rename [old_name] [new_name]"},"dwm":{"n":"dwm","t":"dynamic tiling window manager","s":[["TAGLINE","dynamic tiling window manager"],["TLDR","**Start dwm** (from xinitrc)\n\n```exec dwm```\n\n**Set dwm status bar** text\n\n```xsetroot -name \"[status text]\"```\n\n**Restart dwm** in place\n\n```kill -HUP $(pidof dwm)```"],["SYNOPSIS","**dwm**"],["DESCRIPTION","**dwm** (dynamic window manager) is a minimalist tiling window manager for X11. It manages windows in tiled, monocle, and floating layouts with keyboard-driven operation and minimal resource usage.\n\nWindows are organized into tags (similar to workspaces). The master/stack layout places the primary window prominently with others stacked alongside. Configuration is done by editing the C source code and recompiling.\n\ndwm is designed to be extremely small (under 2000 lines of code) and fast, following the suckless philosophy of simplicity and efficiency."],["DEFAULT KEYBINDINGS","**Mod1-Shift-Enter** - Open terminal\n**Mod1-p** - Launch dmenu\n**Mod1-j** / **Mod1-k** - Focus next/previous window\n**Mod1-h** / **Mod1-l** - Resize master area\n**Mod1-Enter** - Promote focused window to master\n**Mod1-t** - Tiled layout\n**Mod1-f** - Floating layout\n**Mod1-m** - Monocle layout\n**Mod1-Shift-c** - Close focused window\n**Mod1-Shift-q** - Quit dwm\n**Mod1-[1-9]** - Switch to tag 1-9\n**Mod1-Shift-[1-9]** - Move window to tag 1-9"],["CONFIGURATION","**config.h**\n> Source-level configuration file that must be edited and recompiled to change keybindings, colors, layouts, bar settings, rules, and behavior. Mod1 is Alt by default."],["CAVEATS","Configuration requires recompilation. Learning curve for keyboard shortcuts. No built-in configuration file. Limited mouse support compared to floating WMs."],["HISTORY","dwm was created by the **suckless.org** community, led by **Anselm R. Garbe**, released around **2006**. It established the template for many other tiling window managers and the suckless software philosophy."],["INSTALL","```apk: sudo apk add dwm```\n\n```brew: brew install dwm```\n\n```nix: nix profile install nixpkgs#dwm```"],["SEE ALSO","[dmenu](/man/dmenu)(1), [st](/man/st)(1), [i3](/man/i3)(1), [awesome](/man/awesome)(1)"]],"e":["dmenu","st","i3","awesome"],"x":"exec dwm"},"editor":{"n":"editor","t":"system default text editor","s":[["TAGLINE","system default text editor"],["TLDR","**Open the default editor**\n\n```editor [file.txt]```\n\n**Change default editor**\n\n```sudo update-alternatives --config editor```\n\n**Set editor via environment**\n\n```export EDITOR=vim```\n\n**Set visual editor**\n\n```export VISUAL=nano```"],["SYNOPSIS","**editor** [_file_]"],["DESCRIPTION","**editor** is a symbolic link to the system's default text editor. On Debian-based systems /usr/bin/editor is managed by the alternatives system and typically points to nano, vim, or another installed editor. It is a Debian convention rather than a program in its own right, and it does not exist on most non-Debian distributions.\n\nThe editor command provides a standard way for programs to invoke a text editor. Commands like crontab, visudo, and git commit use the EDITOR or VISUAL environment variables to determine which editor to launch, falling back to sensible-editor or /usr/bin/editor when neither is set."],["CONFIGURATION","```bash"],["Check current editor","update-alternatives --display editor"],["Set default (Debian/Ubuntu)","sudo update-alternatives --config editor"],["Set via environment","export EDITOR=vim\nexport VISUAL=\"code --wait\"\n```"],["EDITOR PRIORITY","Programs typically check in order:\n1. VISUAL (for graphical terminals)\n2. EDITOR (for any terminal)\n3. System default (editor, /usr/bin/editor)"],["CAVEATS","The editor command may not exist on all systems. Environment variables override system settings. Some programs ignore EDITOR and use their own configuration. Graphical editors need --wait or similar flag for proper integration."],["SEE ALSO","[vim](/man/vim)(1), [nano](/man/nano)(1), [emacs](/man/emacs)(1), [update-alternatives](/man/update-alternatives)(8), [sensible-editor](/man/sensible-editor)(1)"]],"e":["vim","nano","emacs","update-alternatives","sensible-editor"],"x":"editor [file.txt]"},"efivar":{"n":"efivar","t":"UEFI variable management utility","s":[["TAGLINE","UEFI variable management utility"],["TLDR","**List** all UEFI variables\n\n```efivar -l```\n\n**Print** contents of a variable\n\n```efivar -n [guid-name] -p```\n\n**Export** a variable to a file\n\n```efivar -n [guid-name] -e [path/to/output_file]```\n\n**List** known GUID names\n\n```efivar -L```"],["SYNOPSIS","**efivar** [_OPTION_...]"],["DESCRIPTION","**efivar** manages UEFI variables stored in NVRAM. It can list, read, write, and delete EFI variables. These variables contain boot configuration, secure boot settings, and other firmware data.\n\nUseful for debugging UEFI boot issues and inspecting firmware configuration."],["PARAMETERS","**-l, --list**\n> List current EFI variables\n\n**-n, --name** _guid-name_\n> Variable to manipulate, in the form 8be4df61-93ca-11d2-aa0d-00e098032b8c-BootOrder\n\n**-p, --print**\n> Print variable specified by --name\n\n**-d, --print-decimal**\n> Print variable in decimal format values specified by --name\n\n**-a, --append**\n> Append to variable specified by --name\n\n**-A, --attributes** _attributes_\n> Attributes to use on append\n\n**-w, --write**\n> Write to variable specified by --name\n\n**-f, --datafile** _file_\n> Load or save variable contents from file\n\n**-e, --export** _file_\n> Export variable to file\n\n**-i, --import** _file_\n> Import variable from file\n\n**-L, --list-guids**\n> Show internal GUID list\n\n**-D, --dmpstore**\n> Use DMPSTORE format when exporting"],["CAVEATS","Requires root privileges for write operations. Modifying EFI variables incorrectly can prevent system boot. Some variables are protected by Secure Boot."],["INSTALL","```apt: sudo apt install efivar```\n\n```dnf: sudo dnf install efivar```\n\n```pacman: sudo pacman -S efivar```\n\n```apk: sudo apk add efivar```\n\n```zypper: sudo zypper install efivar```\n\n```nix: nix profile install nixpkgs#efivar```"],["SEE ALSO","[efibootmgr](/man/efibootmgr)(8), [mokutil](/man/mokutil)(1)"]],"e":["efibootmgr","mokutil"],"x":"efivar -l"},"flux":{"n":"flux","t":"GitOps continuous delivery tool for Kubernetes","s":[["TAGLINE","GitOps continuous delivery tool for Kubernetes"],["TLDR","**Bootstrap Flux** on a Kubernetes cluster with GitHub\n\n```flux bootstrap github --owner=[username] --repository=[repo] --path=[clusters/my-cluster]```\n\n**Check prerequisites** for Flux installation\n\n```flux check --pre```\n\n**Check Flux components** status\n\n```flux check```\n\n**Get all Flux resources** status\n\n```flux get all```\n\n**Reconcile a Kustomization** immediately\n\n```flux reconcile kustomization [name]```\n\n**Suspend updates** for a Kustomization\n\n```flux suspend kustomization [name]```\n\n**Resume updates** for a Kustomization\n\n```flux resume kustomization [name]```\n\n**Export Flux resources** to YAML\n\n```flux export source git --all```\n\n**View Flux controller logs**\n\n```flux logs```"],["SYNOPSIS","**flux** [_COMMAND_] [_FLAGS_]"],["COMMANDS","**bootstrap** _PROVIDER_\n> Deploy Flux on a cluster the GitOps way (github, gitlab, bitbucket).\n\n**check**\n> Check prerequisites and component status.\n\n**install**\n> Install Flux components without GitOps bootstrap.\n\n**uninstall**\n> Uninstall Flux components from cluster.\n\n**get** _RESOURCE_\n> Get status of Flux resources (sources, kustomizations, helmreleases).\n\n**reconcile** _RESOURCE_ _NAME_\n> Trigger reconciliation of a resource.\n\n**suspend** _RESOURCE_ _NAME_\n> Suspend reconciliation of a resource.\n\n**resume** _RESOURCE_ _NAME_\n> Resume reconciliation of a resource.\n\n**export** _RESOURCE_\n> Export resources to YAML manifests.\n\n**create** _RESOURCE_\n> Create Flux resources.\n\n**delete** _RESOURCE_\n> Delete Flux resources.\n\n**logs**\n> Display logs from Flux controllers.\n\n**trace** _RESOURCE_ _NAME_\n> Trace an object through the Flux dependency graph.\n\n**diff** _RESOURCE_\n> Diff a local resource against the cluster state.\n\n**version**\n> Print the Flux CLI and controller versions."],["PARAMETERS","**--kubeconfig** _FILE_\n> Path to kubeconfig file.\n\n**-n**, **--namespace** _NAMESPACE_\n> Namespace for CLI operations (default: flux-system).\n\n**--context** _CONTEXT_\n> Kubernetes context to use.\n\n**--timeout** _DURATION_\n> Timeout for operations (default: 5m0s).\n\n**--verbose**\n> Print generated objects.\n\n**-h**, **--help**\n> Display help for command."],["DESCRIPTION","**Flux** is a GitOps continuous delivery tool for Kubernetes. It automatically synchronizes cluster state with configuration stored in Git repositories, ensuring that the live cluster matches the desired state defined in version control.\n\nFlux monitors Git repositories, Helm repositories, and OCI registries for changes. When updates are detected, it applies them to the cluster automatically. This enables declarative, auditable infrastructure management following GitOps principles.\n\nThe flux CLI bootstraps Flux components into a cluster and provides management commands for sources, kustomizations, and Helm releases. Flux uses Kubernetes custom resources to define what to sync and how."],["CAVEATS","Requires kubectl configured with cluster access. Bootstrap creates resources in the flux-system namespace by default. Git repository access requires appropriate credentials or SSH keys. Flux components run as controllers in the cluster and require sufficient RBAC permissions."],["HISTORY","Flux was originally created by Weaveworks in **2016** as one of the first GitOps tools for Kubernetes. **Flux v2** was released in **2020** as a complete rewrite using the GitOps Toolkit, providing a more modular and extensible architecture. Flux became a **CNCF Graduated project** in **2022**, reflecting its maturity and wide adoption in the cloud-native ecosystem."],["INSTALL","```apk: sudo apk add flux```\n\n```brew: brew install flux```\n\n```nix: nix profile install nixpkgs#flux```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [helm](/man/helm)(1), [kustomize](/man/kustomize)(1), [argocd](/man/argocd)(1)"]],"e":["kubectl","helm","kustomize","argocd"],"x":"flux bootstrap github --owner=[username] --repository=[repo] --path=[clusters/my-cluster]"},"git-abort":{"n":"git-abort","t":"abort any in-progress git operation (rebase, merge, cherry-pick, revert, am)","s":[["TAGLINE","abort any in-progress git operation (rebase, merge, cherry-pick, revert, am)"],["TLDR","**Abort whatever is in progress**\n\n```git abort```"],["SYNOPSIS","**git abort**"],["DESCRIPTION","**git abort** is a convenience command from the **git-extras** suite. It detects which git operation is in progress by inspecting the `.git` directory and runs the matching abort:\n\n- Rebase in progress (`rebase-merge/` or `rebase-apply/`) → `git rebase --abort`\n- Merge in progress (`MERGE_HEAD`) → `git merge --abort`\n- Cherry-pick in progress (`CHERRY_PICK_HEAD`) → `git cherry-pick --abort`\n- Revert in progress (`REVERT_HEAD`) → `git revert --abort`\n- `git am` in progress (`rebase-apply/applying`) → `git am --abort`\n\nIf nothing is in progress, it prints a message and exits non-zero."],["CAVEATS","Provided by **git-extras**, not a built-in git command. Install via your package manager (`apt install git-extras`, `brew install git-extras`, etc.) or from source. All aborts are destructive with respect to the operation that was running — uncommitted merge resolutions or rebase edits are discarded. Your working tree is reset to the state before the operation began."],["HISTORY","**git-extras** is a long-running project by **TJ Holowaychuk** that bundles dozens of git subcommands for everyday workflows. `git abort` was added to consolidate the operation-specific `--abort` flags under one memorable command."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-rebase](/man/git-rebase)(1), [git-merge](/man/git-merge)(1), [git-cherry-pick](/man/git-cherry-pick)(1), [git-am](/man/git-am)(1), [git-extras](/man/git-extras)(1)"]],"e":["git-rebase","git-merge","git-cherry-pick","git-am","git-extras"],"x":"git abort"},"git-commit-tree":{"n":"git-commit-tree","t":"Create a commit object from a tree","s":[["TAGLINE","Create a commit object from a tree"],["TLDR","**Create commit from tree**\n\n```echo \"[message]\" | git commit-tree [tree_hash]```\n\n**Create with parent**\n\n```echo \"[message]\" | git commit-tree [tree] -p [parent]```\n\n**Create with message file**\n\n```git commit-tree [tree] -F [message.txt]```\n\n**Create merge commit**\n\n```git commit-tree [tree] -p [parent1] -p [parent2] -m \"[message]\"```"],["SYNOPSIS","**git** **commit-tree** _tree_ [_options_]"],["PARAMETERS","**-p** _parent_\n> Parent commit.\n\n**-m** _message_\n> A paragraph in the commit log message. Can be given more than once; each becomes its own paragraph.\n\n**-F** _file_\n> Read the commit log message from the given file. Use - to read from stdin. Can be given more than once.\n\n**-S**[_keyid_]\n> GPG sign commit. The keyid is optional and defaults to the committer identity; if specified, it must be attached to the option without a space.\n\n**--no-gpg-sign**\n> Countermand a previous --gpg-sign option."],["DESCRIPTION","**git commit-tree** is a low-level plumbing command that creates a new commit object directly from a tree object hash. Unlike git commit (a porcelain command), this bypasses the index and working directory, operating directly on Git's internal object database.\n\nThis command is used internally by git commit but is also available for advanced scenarios like repository surgery, history reconstruction, or programmatic commit creation. It requires providing a tree hash (typically created by git write-tree or extracted from existing commits) and accepts optional parent commit hashes to establish lineage.\n\nMultiple -p options create merge commits with multiple parents. The commit message can be provided inline with -m, read from a file with -F, or piped to stdin. Author and committer information come from git config unless overridden with environment variables (GIT_AUTHOR_NAME, GIT_COMMITTER_DATE, etc.).\n\nThis command outputs the SHA-1 hash of the newly created commit object. To make the commit visible, you typically need to update a branch reference using git update-ref or git reset. Most users never need this command directly, but it's essential for understanding Git's internal architecture and for advanced repository manipulation."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-commit](/man/git-commit)(1), [git-write-tree](/man/git-write-tree)(1), [git-update-ref](/man/git-update-ref)(1)"]],"e":["git-commit","git-write-tree","git-update-ref"],"x":"echo \"[message]\" | git commit-tree [tree_hash]"},"git-scp":{"n":"git-scp","t":"Copy files to a remote Git repository via scp","s":[["TAGLINE","Copy files to a remote Git repository via scp"],["TLDR","**Copy a single file to a remote**\n\n```git scp [remote] [path/to/file]```\n\n**Copy multiple files to a remote**\n\n```git scp [remote] [file1] [file2]```\n\n**Copy a file to a specific branch on remote**\n\n```git scp [remote] [path/to/file] --branch [branch_name]```"],["SYNOPSIS","**git** **scp** _remote_ _files_..."],["DESCRIPTION","**git scp** copies files to a Git remote using scp. Part of **git-extras**, it uses the remote's URL from your Git configuration to determine the host and path, then transfers files via scp. To copy files from a remote, use **git rscp** instead."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-rscp](/man/git-rscp)(1), [scp](/man/scp)(1), [git-extras](/man/git-extras)(1)"]],"e":["git-rscp","scp","git-extras"],"x":"git scp [remote] [path/to/file]"},"gouldtoppm":{"n":"gouldtoppm","t":"Gould scanner file to PPM converter","s":[["TAGLINE","Gould scanner file to PPM converter"],["TLDR","**Convert Gould scanner file to PPM**\n\n```gouldtoppm [file.gould] > [output.ppm]```\n\n**Convert from stdin**\n\n```cat [file.gould] | gouldtoppm > [output.ppm]```"],["SYNOPSIS","**gouldtoppm** [_gouldfile_]"],["DESCRIPTION","**gouldtoppm** reads a file produced by a Gould scanner and converts it to PPM (Portable Pixmap) format. It is part of the Netpbm package of graphics manipulation tools.\n\nThe command reads from a file argument or standard input and writes PPM output to standard output."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[ppmtogif](/man/ppmtogif)(1), [pnmtopng](/man/pnmtopng)(1), [netpbm](/man/netpbm)(1)"],["RESOURCES","```[Homepage](https://netpbm.sourceforge.net/)```\n\n```[Documentation](https://netpbm.sourceforge.net/doc/gouldtoppm.html)```"]],"e":["ppmtogif","pnmtopng","netpbm"],"x":"gouldtoppm [file.gould] > [output.ppm]"},"gpg":{"n":"gpg","t":"GNU Privacy Guard encryption and signing tool","s":[["TAGLINE","GNU Privacy Guard encryption and signing tool"],["TLDR","**Generate a new key pair**\n\n```gpg --gen-key```\n\n**List all keys** in the public keyring\n\n```gpg --list-keys```\n\n**Encrypt a file** for a specific recipient\n\n```gpg -e -r [recipient_email] [file]```\n\n**Decrypt a file**\n\n```gpg -d [file.gpg]```\n\n**Sign a file** with your private key\n\n```gpg --sign [file]```\n\n**Verify a signature**\n\n```gpg --verify [file.sig]```\n\n**Export a public key** to a file\n\n```gpg --export -a \"[user_id]\" > [public.key]```\n\n**Import a public key**\n\n```gpg --import [public.key]```"],["SYNOPSIS","**gpg** [_--homedir dir_] [_--options file_] [_options_] _command_ [_args_]"],["PARAMETERS","**-e**, **--encrypt**\n> Encrypt data to one or more recipients\n\n**-d**, **--decrypt**\n> Decrypt data (default if input looks encrypted)\n\n**-s**, **--sign**\n> Make a signature\n\n**--verify**\n> Verify a signature\n\n**-c**, **--symmetric**\n> Encrypt with symmetric cipher using a passphrase\n\n**-r** _name_, **--recipient** _name_\n> Encrypt for the specified recipient\n\n**-u** _name_, **--local-user** _name_\n> Use specified key as the key to sign with\n\n**-a**, **--armor**\n> Create ASCII armored output\n\n**-o** _file_, **--output** _file_\n> Write output to specified file\n\n**--gen-key**\n> Generate a new key pair\n\n**--full-gen-key**\n> Generate a new key pair with dialog for all options\n\n**--list-keys**\n> List all keys from the public keyrings\n\n**--list-secret-keys**\n> List all keys from the secret keyrings\n\n**--import**\n> Import keys from a file\n\n**--export**\n> Export keys from the keyring\n\n**--delete-keys** _name_\n> Remove keys from the public keyring\n\n**--delete-secret-keys** _name_\n> Remove keys from the secret keyring\n\n**--fingerprint**\n> List keys and their fingerprints\n\n**--keyserver** _url_\n> Specify keyserver to use\n\n**--send-keys** _keyids_\n> Send keys to a keyserver\n\n**--recv-keys** _keyids_\n> Import keys from a keyserver\n\n**--batch**\n> Batch mode, never ask for input\n\n**--yes**\n> Assume yes on most questions\n\n**--passphrase** _string_\n> Use specified passphrase"],["DESCRIPTION","**gpg** (GNU Privacy Guard) is a complete and free implementation of the OpenPGP standard as defined by RFC 4880. It allows encrypting and signing data, features a versatile key management system, and provides access modules for public key directories.\n\nGPG supports both symmetric (password-based) and asymmetric (public/private key) encryption. Common use cases include encrypting sensitive files, signing email messages, verifying software package authenticity, and establishing secure communication channels."],["CONFIGURATION","**~/.gnupg/gpg.conf**\n> User configuration file for default options, keyserver settings, and algorithm preferences.\n\n**~/.gnupg/pubring.kbx**\n> Public keyring database storing imported and generated public keys.\n\n**~/.gnupg/trustdb.gpg**\n> Trust database for the web of trust model."],["CAVEATS","Key management is critical; losing your private key or forgetting the passphrase means permanent loss of access to encrypted data. The web of trust model requires careful key verification to prevent man-in-the-middle attacks. Default algorithms and key sizes should be reviewed periodically as cryptographic standards evolve."],["HISTORY","GPG was first released in **1997** by **Werner Koch** (version 0.0.0), with the first stable release (1.0.0) in **1999**, as a free software replacement for PGP (Pretty Good Privacy). It was developed as part of the GNU Project and has since become the de facto standard for email encryption on Unix-like systems. GPG 2.0 introduced a modular architecture in **2006**, and version 2.2 brought improved default algorithms and keyserver handling. The project continues to be actively maintained and is widely used by journalists, activists, and security-conscious users worldwide."],["INSTALL","```apt: sudo apt install gpg```\n\n```apk: sudo apk add gpg```\n\n```brew: brew install gpg```"],["SEE ALSO","[ssh](/man/ssh)(1), [openssl](/man/openssl)(1), [age](/man/age)(1), [sha256sum](/man/sha256sum)(1)"]],"e":["ssh","openssl","age","sha256sum"],"x":"gpg --gen-key"},"grohtml":{"n":"grohtml","t":"Groff HTML output driver","s":[["TAGLINE","Groff HTML output driver"],["TLDR","**Convert groff to HTML**\n\n```groff -Thtml [file.roff] > output.html```\n\n**Direct grohtml usage**\n\n```grohtml [file] > output.html```\n\n**Generate with images**\n\n```groff -Thtml -P-i [file.roff] > output.html```"],["SYNOPSIS","**grohtml** [_options_] [_file_]"],["PARAMETERS","_FILE_\n> Groff intermediate output.\n\n**-b**\n> Create background color.\n\n**-i**\n> Generate inline images.\n\n**-s** _SIZE_\n> Font base size.\n\n**-F** _DIR_\n> Font directory.\n\n**--help**\n> Display help information."],["DESCRIPTION","**grohtml** is a groff output driver that produces HTML. It converts groff documents to web-viewable format with optional image generation for elements that do not translate directly to HTML.\n\nThe driver handles text formatting and creates appropriate HTML tags. Complex formatting elements can be rendered as inline images for accurate representation."],["CAVEATS","Part of groff package. Complex formatting may not render perfectly. Image generation requires additional tools."],["HISTORY","grohtml was developed as part of **GNU groff** to enable web publishing of groff documents."],["SEE ALSO","[groff](/man/groff)(1), [grops](/man/grops)(1)"]],"e":["groff","grops"],"x":"groff -Thtml [file.roff] > output.html"},"grops":{"n":"grops","t":"Groff PostScript output driver","s":[["TAGLINE","Groff PostScript output driver"],["TLDR","**Convert groff to PostScript**\n\n```groff -Tps [file.roff] > output.ps```\n\n**Direct grops usage**\n\n```grops [file] > output.ps```\n\n**Specify paper size**\n\n```grops -p [a4] [file] > output.ps```\n\n**Landscape mode**\n\n```grops -l [file] > output.ps```"],["SYNOPSIS","**grops** [_options_] [_file_]"],["PARAMETERS","_FILE_\n> Groff intermediate output.\n\n**-c** _N_\n> Number of copies.\n\n**-F** _DIR_\n> Font directory.\n\n**-l**\n> Landscape orientation.\n\n**-p** _SIZE_\n> Paper size (letter, a4, legal).\n\n**-P** _PROLOGUE_\n> Use prologue file.\n\n**-b**\n> Broken flag for compatibility.\n\n**--help**\n> Display help information."],["DESCRIPTION","**grops** is a groff output driver that produces PostScript. It is the primary driver for high-quality printed output from groff, generating Level 2 PostScript with support for color, graphics, and embedded fonts.\n\nOutput can be printed directly or converted to PDF with tools like ps2pdf."],["CAVEATS","Part of groff package. PostScript output can be large. Requires PostScript viewer or printer."],["HISTORY","grops was developed as part of **GNU groff** by **James Clark** as the primary output driver for printed documents."],["INSTALL","```apt: sudo apt install groff-base```\n\n```dnf: sudo dnf install groff-base```\n\n```pacman: sudo pacman -S groff```\n\n```apk: sudo apk add groff```\n\n```zypper: sudo zypper install groff```\n\n```brew: brew install groff```\n\n```nix: nix profile install nixpkgs#groff```"],["SEE ALSO","[groff](/man/groff)(1), [ps2pdf](/man/ps2pdf)(1)"]],"e":["groff","ps2pdf"],"x":"groff -Tps [file.roff] > output.ps"},"gt":{"n":"gt","t":"configfs-based USB gadget management tool","s":[["TAGLINE","configfs-based USB gadget management tool"],["TLDR","**List available USB device controllers (UDCs)**\n\n```gt udc```\n\n**List existing gadgets**\n\n```gt gadget```\n\n**Create a new gadget**\n\n```gt create [gadget-name]```\n\n**Set gadget attributes** (vendor/product IDs)\n\n```gt set [gadget-name] idVendor=[0x1d6b] idProduct=[0x0104]```\n\n**Create a USB function** (e.g. Ethernet)\n\n```gt func create [gadget-name] [ecm] [usb0]```\n\n**Create a configuration and bind the function to it**\n\n```gt config create [gadget-name] [c] [1]```\n\n```gt config add [gadget-name] [c] [1] [ecm] [usb0]```\n\n**Enable a gadget on the first available UDC**\n\n```gt enable [gadget-name]```\n\n**Save the current gadget setup as a reusable template**\n\n```gt save [gadget-name] [template-name]```\n\n**Remove a gadget and all its configurations/functions**\n\n```gt rm -rf [gadget-name]```"],["SYNOPSIS","**gt** _command_ [_options_...]\n\n**gadgetctl** _command_ [_options_...]"],["PARAMETERS","_COMMAND_\n> Subcommand to execute (see below).\n\n**udc**\n> Show the list of available USB device controllers.\n\n**create** _gadget_ [_attr=val_...]\n> Create a gadget with the given name and optionally set its attributes. `-f`/`--force` overrides an existing gadget with the same name.\n\n**rm** _gadget_\n> Remove a gadget. `-f`/`--force` disables it first if enabled; `-r`/`--recursive` also removes its configurations and functions.\n\n**get** _gadget_ [_attr_]\n> Print the given attribute, or all attributes if none is specified.\n\n**set** _gadget_ _attr=val_...\n> Set one or more gadget attributes to new values.\n\n**enable** [_gadget_]\n> Enable a gadget on a UDC. If only one gadget/UDC exists it is chosen automatically.\n\n**disable** [_gadget_]\n> Disable a gadget. `-u`/`--udc` _udc_ disables whichever gadget is active on the given UDC.\n\n**gadget** [_name_]\n> List all gadgets, or show details for one. `-v`/`--verbose` also prints attributes; `-r`/`--recursive` also shows function/config details.\n\n**func create/rm/show** _gadget_ _type_ _instance_\n> Create, remove, or show a USB function (e.g. **ecm**, **acm**, **mass_storage**) bound to a gadget.\n\n**func list-types**\n> Print the list of function types supported by the running kernel.\n\n**config create/rm/show** _gadget_ _label_ _id_\n> Create, remove, or show a gadget configuration.\n\n**config add/del** _gadget_ _label_ _id_ _func-type_ _func-instance_\n> Bind or unbind a function to/from a configuration.\n\n**save** _gadget_ [_name_]\n> Store the gadget's current setup as a system template. `--file`, `--stdout`, and `--path` redirect where it is stored.\n\n**load** _name_ [_gadget_]\n> Create and enable a gadget from a saved template. `-o`/`--off` skips enabling it.\n\n**template** [_name_]\n> List templates, or show one template's configurations and functions.\n\n**settings set/get/append/detach** _variable_ [_value_]\n> Manage gt's own configuration variables (some of which hold lists)."],["DESCRIPTION","**gt** (gadget-tool) configures Linux USB gadgets through the kernel's **configfs** interface, replacing manual `mkdir`/`echo` manipulation of the configfs tree with a small set of subcommands. It builds gadgets out of **functions** (e.g. Ethernet, mass storage, serial, HID) grouped into one or more **configurations**, which are then enabled on a USB Device Controller (UDC).\n\n**gt** operates directly on configfs and therefore needs root privileges. The companion binary **gadgetctl** talks to a **gadgetd** daemon instead using the same command syntax, but gadgetd is considered obsolete."],["CAVEATS","Requires root and a kernel built with USB gadget/configfs support (`CONFIGFS_FS`, `USB_CONFIGFS`, and the relevant function drivers); which function types are available depends on the running kernel. Mainly used on embedded boards and devices with a USB device-mode controller, not on typical desktop hardware."],["INSTALL","```apt: sudo apt install genometools```\n\n```dnf: sudo dnf install gt```\n\n```apk: sudo apk add gadget-tool```\n\n```brew: brew install genometools```\n\n```nix: nix profile install nixpkgs#gt```"],["SEE ALSO","[mount](/man/mount)(8), [lsusb](/man/lsusb)(8), [modprobe](/man/modprobe)(8)"],["RESOURCES","```[Source code](https://github.com/linux-usb-gadgets/gt)```"]],"e":["mount","lsusb","modprobe"],"x":"gt udc"},"hardinfo":{"n":"hardinfo","t":"graphical system information and benchmark tool for Linux","s":[["TAGLINE","graphical system information and benchmark tool for Linux"],["TLDR","**Start the graphical interface**\n\n```hardinfo```\n\n**Generate a text report to stdout**\n\n```hardinfo -r```\n\n**Generate an HTML report**\n\n```hardinfo -r -f html > [report.html]```\n\n**List available modules**\n\n```hardinfo -l```"],["SYNOPSIS","**hardinfo** [_options_]"],["PARAMETERS","**-r**, **--generate-report**\n> Generate report to stdout instead of opening GUI\n\n**-f**, **--report-format** _FORMAT_\n> Report format: text, html, or shell\n\n**-m**, **--load-module** _MODULE_\n> Load specific module\n\n**-a**, **--autoload**\n> Automatically load all modules\n\n**-l**, **--list-modules**\n> List available modules"],["DESCRIPTION","**hardinfo** (Hardware Information) is a graphical system information and benchmark tool for Linux. It displays detailed information about hardware components, software configuration, and system resources.\n\nThe GUI organizes information into categories including:\n- **Computer** - Summary, operating system, kernel modules, boots\n- **Devices** - Processor, memory, PCI devices, USB devices, storage\n- **Network** - Interfaces, IP connections, routing table\n- **Benchmarks** - CPU, FPU, and memory performance tests\n\nReports can be generated for documentation or support purposes in text, HTML, or shell variable formats."],["CAVEATS","Some information requires root privileges. Benchmark results are relative and shouldn't be compared across different hardinfo versions. Hardware detection depends on kernel drivers being loaded."],["HISTORY","hardinfo was created by Leandro A. F. Pereira and first released in **2003**. It has been a popular system information tool on Linux desktops, providing functionality similar to Windows' System Information utility."],["INSTALL","```zypper: sudo zypper install hardinfo```"],["SEE ALSO","[lshw](/man/lshw)(1), [inxi](/man/inxi)(1), [hwinfo](/man/hwinfo)(8), [lscpu](/man/lscpu)(1)"]],"e":["lshw","inxi","hwinfo","lscpu"],"x":"hardinfo"},"hp-scan":{"n":"hp-scan","t":"scans documents from HP all-in-one devices","s":[["TAGLINE","scans documents from HP all-in-one devices"],["TLDR","**Scan document**\n\n```hp-scan```\n\n**Specify output file**\n\n```hp-scan -o [output.pdf]```\n\n**Scan to JPEG**\n\n```hp-scan -t jpeg -o [output.jpg]```\n\n**Specify resolution**\n\n```hp-scan -r [300]```\n\n**Use ADF**\n\n```hp-scan --adf```"],["SYNOPSIS","**hp-scan** [_options_]"],["PARAMETERS","**-o**, **--output** _FILE_\n> Output filename.\n\n**-t**, **--type** _TYPE_\n> Output format (pdf, jpeg, png).\n\n**-r**, **--resolution** _DPI_\n> Scan resolution.\n\n**--adf**\n> Use automatic document feeder.\n\n**-p** _PRINTER_\n> Printer/scanner name.\n\n**-d** _DEVICE_\n> Device URI.\n\n**--help**\n> Display help information."],["DESCRIPTION","**hp-scan** scans documents from HP all-in-one devices. It supports various output formats and resolutions.\n\nThe tool can use the flatbed or automatic document feeder. It provides a command-line alternative to the graphical scanning interface."],["CAVEATS","Part of HPLIP package. Some models need plugin. Requires scanner-capable device."],["HISTORY","hp-scan is part of **HPLIP** (HP Linux Imaging and Printing) for scanning on HP multifunction devices."],["INSTALL","```apt: sudo apt install hplip```\n\n```dnf: sudo dnf install hplip```\n\n```pacman: sudo pacman -S hplip```\n\n```zypper: sudo zypper install hplip```\n\n```nix: nix profile install nixpkgs#hplip```"],["SEE ALSO","[hp-setup](/man/hp-setup)(1), [scanimage](/man/scanimage)(1)"]],"e":["hp-setup","scanimage"],"x":"hp-scan"},"identify.im6":{"n":"identify.im6","t":"ImageMagick 6 version of identify","s":[["TAGLINE","ImageMagick 6 version of identify"],["TLDR","**Show image info**\n\n```identify.im6 [image.jpg]```\n\n**Verbose output**\n\n```identify.im6 -verbose [image.png]```\n\n**Custom format**\n\n```identify.im6 -format \"%wx%h\" [image]```\n\n**Quick ping mode**\n\n```identify.im6 -ping [image.jpg]```"],["SYNOPSIS","**identify.im6** [_options_] _files_"],["PARAMETERS","_FILES_\n> Image files to identify.\n\n**-verbose**\n> Detailed information.\n\n**-format** _STRING_\n> Custom format string.\n\n**-ping**\n> Quick identification.\n\n**--help**\n> Display help information."],["DESCRIPTION","**identify.im6** is the ImageMagick 6 version of identify. It describes image format, dimensions, and characteristics.\n\nThe tool provides the same functionality as identify but specifically from ImageMagick version 6. Both may coexist on systems."],["CAVEATS","ImageMagick 6 specific. May conflict with IM7. Legacy version."],["HISTORY","identify.im6 is part of **ImageMagick 6**, maintained separately from ImageMagick 7."],["SEE ALSO","[identify](/man/identify)(1), [mogrify.im6](/man/mogrify.im6)(1)"]],"e":["identify","mogrify.im6"],"x":"identify.im6 [image.jpg]"},"imgp":{"n":"imgp","t":"fast, command-line image resizer and rotator for JPEG and PNG files","s":[["TAGLINE","fast, command-line image resizer and rotator for JPEG and PNG files"],["TLDR","**Resize** images or directories to specific resolution\n\n```imgp -x [1366x1000] [path/to/directory] [path/to/file]```\n\n**Scale** an image by percentage and overwrite source\n\n```imgp -x [75] -w [path/to/file]```\n\n**Rotate** an image clockwise by 90 degrees\n\n```imgp -o [90] [path/to/file]```"],["SYNOPSIS","**imgp** [_options_] [_paths_]"],["PARAMETERS","**-x**, **--res** _RES_\n> Target resolution (WxH) or scale percentage\n\n**-o**, **--rotate** _DEGREES_\n> Rotate image clockwise (90, 180, 270)\n\n**-w**, **--overwrite**\n> Overwrite source images\n\n**-q**, **--quality** _VALUE_\n> JPEG quality (1-95, default 75)\n\n**-n**, **--enlarge**\n> Allow enlarging images\n\n**-e**, **--erase**\n> Remove EXIF metadata\n\n**-c**, **--convert**\n> Convert between PNG and JPEG"],["DESCRIPTION","**imgp** is a fast, command-line image resizer and rotator for JPEG and PNG files. It can process single files or entire directories, making batch operations efficient.\n\nThe tool preserves aspect ratio by default when resizing and can process images in parallel for improved performance. It maintains EXIF orientation metadata and supports both percentage-based scaling and absolute resolution targets."],["CAVEATS","Only supports JPEG and PNG formats. By default creates new files rather than overwriting. Very large batch operations may consume significant memory."],["HISTORY","imgp was created by Arun Prakash Jana and is part of a collection of efficient command-line utilities. It was designed to provide fast, scriptable image manipulation without the complexity of larger tools like ImageMagick."],["INSTALL","```dnf: sudo dnf install imgp```\n\n```zypper: sudo zypper install imgp```\n\n```brew: brew install imgp```\n\n```nix: nix profile install nixpkgs#imgp```"],["SEE ALSO","[convert](/man/convert)(1), [mogrify](/man/mogrify)(1), [jpegtran](/man/jpegtran)(1)"]],"e":["convert","mogrify","jpegtran"],"x":"imgp -x [1366x1000] [path/to/directory] [path/to/file]"},"jinfo":{"n":"jinfo","t":"prints and modifies Java VM configuration","s":[["TAGLINE","prints and modifies Java VM configuration"],["TLDR","**Show JVM flags**\n\n```jinfo [pid]```\n\n**Show system properties**\n\n```jinfo -sysprops [pid]```\n\n**Show specific flag**\n\n```jinfo -flag [HeapDumpOnOutOfMemoryError] [pid]```\n\n**Set flag dynamically**\n\n```jinfo -flag +[HeapDumpOnOutOfMemoryError] [pid]```\n\n**Unset flag**\n\n```jinfo -flag -[PrintGC] [pid]```\n\n**Print all flags**\n\n```jinfo -flags [pid]```"],["SYNOPSIS","**jinfo** [_options_] _pid_"],["PARAMETERS","_PID_\n> Target Java process ID.\n\n**-flags**\n> Print VM flags.\n\n**-sysprops**\n> Print system properties.\n\n**-flag** _NAME_\n> Print or modify specific flag.\n\n**+**_FLAG_\n> Enable boolean flag.\n\n**-**_FLAG_\n> Disable boolean flag.\n\n**--help**\n> Display help information."],["DESCRIPTION","**jinfo** prints and modifies Java VM configuration. It shows system properties and VM flags for running processes.\n\nThe tool can dynamically change manageable flags. Useful for tuning JVM behavior without restart."],["CAVEATS","Part of JDK. Not all flags modifiable. May require same user as target process."],["HISTORY","jinfo has been part of the **JDK** since Java 5, providing runtime configuration inspection and modification."],["SEE ALSO","[java](/man/java)(1), [jps](/man/jps)(1), [jstack](/man/jstack)(1)"]],"e":["java","jps","jstack"],"x":"jinfo [pid]"},"keepass2":{"n":"keepass2","t":"free, open-source password manager that stores passwords in an encrypted","s":[["TAGLINE","free, open-source password manager that stores passwords in an encrypted"],["TLDR","**Open KeePass**\n\n```keepass2```\n\n**Open specific database**\n\n```keepass2 [database.kdbx]```\n\n**Open database with keyfile**\n\n```keepass2 [database.kdbx] -keyfile:[keyfile.key]```"],["SYNOPSIS","**keepass2** [_options_] [_database_]"],["PARAMETERS","**-keyfile:**_file_\n> Use key file for authentication.\n\n**-pw:**_password_\n> Database password (insecure).\n\n**-minimize**\n> Start minimized.\n\n**-auto-type**\n> Perform auto-type."],["DESCRIPTION","**keepass2** is a free, open-source password manager that stores passwords in an encrypted database. Supports strong encryption, password generation, and browser integration. The database file can be secured with a master password and/or key file."],["CAVEATS","Avoid using -pw: on command line as it exposes the password in process lists and shell history."],["SEE ALSO","[keepassxc-cli](/man/keepassxc-cli)(1)"]],"e":["keepassxc-cli"],"x":"keepass2"},"latexindent":{"n":"latexindent","t":"formats LaTeX source code with consistent indentation","s":[["TAGLINE","formats LaTeX source code with consistent indentation"],["TLDR","**Format LaTeX file**\n\n```latexindent [document.tex]```\n\n**Format and save in place**\n\n```latexindent -w [document.tex]```\n\n**Format with local settings**\n\n```latexindent -l [document.tex]```\n\n**Specify output file**\n\n```latexindent [input.tex] -o [output.tex]```\n\n**Silence output**\n\n```latexindent -s [document.tex]```\n\n**Send backup and log files to a different directory**\n\n```latexindent -c [path/to/cruft/dir/] [document.tex]```\n\n**Overwrite only if content changed**\n\n```latexindent -wd [document.tex]```"],["SYNOPSIS","**latexindent** [_options_] _file_"],["PARAMETERS","**-w**, **--overwrite**\n> Overwrite original file (backup is created first).\n\n**-wd**, **--overwriteIfDifferent**\n> Overwrite original file only if the indented text differs.\n\n**-o** _file_\n> Output to specific file.\n\n**-l** [_file_]\n> Use local settings YAML file (default: localSettings.yaml).\n\n**-s**, **--silent**\n> Silent mode; suppress terminal output.\n\n**-c** _dir_\n> Send backup files and indent.log to the specified directory instead of the current directory.\n\n**-m**, **--modifylinebreaks**\n> Modify line breaks according to settings.\n\n**-g** _file_\n> Specify log file location.\n\n**--replacement**\n> Enable replacement mode for string/regex substitutions.\n\n**-y** _yaml_\n> Provide YAML settings inline (e.g., -y=\"defaultIndent: '  '\")."],["DESCRIPTION","**latexindent** formats LaTeX source code with consistent indentation. It handles environments, commands, and special constructs intelligently.\n\nThe tool is configured through YAML files, allowing customization of indent size, environments, and special cases."],["LOCAL SETTINGS","```yaml"],["localSettings.yaml","defaultIndent: \"  \"\nremoveTrailingWhitespace: 1\nnoAdditionalIndent:\n  document: 1\n```"],["CAVEATS","Perl-based; requires Perl and YAML::Tiny. May change semantics in edge cases. Backup files created by default. Complex documents may need custom rules."],["HISTORY","latexindent was written by **Chris Hughes** as a Perl script for formatting LaTeX code. It's included in TeX Live and is commonly used for maintaining consistent LaTeX codestyle."],["INSTALL","```pacman: sudo pacman -S texlive-binextra```\n\n```apk: sudo apk add texlive-binextra```\n\n```brew: brew install latexindent```"],["SEE ALSO","[latex](/man/latex)(1), [latexmk](/man/latexmk)(1), [prettier](/man/prettier)(1)"]],"e":["latex","latexmk","prettier"],"x":"latexindent [document.tex]"},"lex":{"n":"lex","t":"lexical analyzer generator that is traditionally aliased to **flex** on most","s":[["TAGLINE","lexical analyzer generator that is traditionally aliased to **flex** on most"],["TLDR","This command is an **alias** of flex\n\n```tldr flex```"],["SYNOPSIS","**lex** [_options_] [_file_]"],["DESCRIPTION","**lex** is a lexical analyzer generator that is traditionally aliased to **flex** (fast lexical analyzer generator) on most modern systems. It generates C code for tokenizing input based on regular expression patterns."],["INSTALL","```apt: sudo apt install flex```\n\n```dnf: sudo dnf install flex```\n\n```pacman: sudo pacman -S flex```\n\n```apk: sudo apk add flex```\n\n```zypper: sudo zypper install flex```\n\n```brew: brew install flex```\n\n```nix: nix profile install nixpkgs#flex```"],["SEE ALSO","[flex](/man/flex)(1), [yacc](/man/yacc)(1), [bison](/man/bison)(1)"]],"e":["flex","yacc","bison"],"x":"tldr flex"},"linux-boot-prober":{"n":"linux-boot-prober","t":"probes a partition for bootable Linux operating systems","s":[["TAGLINE","probes a partition for bootable Linux operating systems"],["TLDR","**Probe** partition for bootable systems\n\n```sudo linux-boot-prober /dev/sdXY```"],["SYNOPSIS","**linux-boot-prober** _partition_"],["DESCRIPTION","**linux-boot-prober** probes a partition for bootable Linux operating systems. It is part of os-prober and is used by bootloaders like GRUB to detect other installed Linux systems and add them to the boot menu."],["PARAMETERS","**partition**\n> Device path of partition to probe (e.g., /dev/sda1)"],["CAVEATS","Requires root privileges. Only detects Linux installations, not other operating systems. Use os-prober for broader detection."],["INSTALL","```apt: sudo apt install os-prober```\n\n```dnf: sudo dnf install os-prober```\n\n```pacman: sudo pacman -S os-prober```\n\n```apk: sudo apk add os-prober```\n\n```zypper: sudo zypper install os-prober```\n\n```nix: nix profile install nixpkgs#os-prober```"],["SEE ALSO","[grub-mkconfig](/man/grub-mkconfig)(8), [update-grub](/man/update-grub)(8)"]],"e":["grub-mkconfig","update-grub"],"x":"sudo linux-boot-prober /dev/sdXY"},"llvm-ar":{"n":"llvm-ar","t":"archiver for creating and manipulating static library archives","s":[["TAGLINE","archiver for creating and manipulating static library archives"],["TLDR","**Create archive from object files**\n\n```llvm-ar rcs [libname.a] [file1.o] [file2.o]```\n\n**List archive contents**\n\n```llvm-ar t [archive.a]```\n\n**Extract files from archive**\n\n```llvm-ar x [archive.a]```\n\n**Add files to archive**\n\n```llvm-ar r [archive.a] [file.o]```\n\n**Delete file from archive**\n\n```llvm-ar d [archive.a] [file.o]```"],["SYNOPSIS","**llvm-ar** [_options_] _operation_ _archive_ [_files_...]"],["PARAMETERS","**r**\n> Replace or add files to archive.\n\n**c**\n> Create archive without warning.\n\n**s**\n> Create archive index.\n\n**t**\n> List archive contents.\n\n**x**\n> Extract files from archive.\n\n**d**\n> Delete files from archive.\n\n**--format** _type_\n> Archive format (gnu, darwin, bsd)."],["DESCRIPTION","**llvm-ar** is the LLVM archiver for creating and manipulating static library archives. Drop-in replacement for GNU ar. Creates archives compatible with the system linker for static linking."],["INSTALL","```apt: sudo apt install llvm```\n\n```dnf: sudo dnf install llvm```\n\n```pacman: sudo pacman -S llvm```\n\n```apk: sudo apk add llvm```\n\n```zypper: sudo zypper install llvm```\n\n```brew: brew install llvm```\n\n```nix: nix profile install nixpkgs#llvm```"],["SEE ALSO","[ar](/man/ar)(1)"]],"e":["ar"],"x":"llvm-ar rcs [libname.a] [file1.o] [file2.o]"},"llvm-objdump":{"n":"llvm-objdump","t":"displays information about object files","s":[["TAGLINE","displays information about object files"],["TLDR","**Disassemble object file**\n\n```llvm-objdump -d [file.o]```\n\n**Show all headers**\n\n```llvm-objdump -x [file]```\n\n**Show section headers**\n\n```llvm-objdump -h [file]```\n\n**Disassemble with source**\n\n```llvm-objdump -dS [file]```\n\n**Show relocations**\n\n```llvm-objdump -r [file.o]```\n\n**Show symbol table**\n\n```llvm-objdump -t [file]```"],["SYNOPSIS","**llvm-objdump** [_options_] [_file_...]"],["PARAMETERS","**-d**, **--disassemble**\n> Disassemble text sections.\n\n**-D**, **--disassemble-all**\n> Disassemble all sections.\n\n**-S**, **--source**\n> Show source with disassembly.\n\n**-h**, **--headers**\n> Show section headers.\n\n**-x**, **--all-headers**\n> Show all headers.\n\n**-t**, **--syms**\n> Show symbol table.\n\n**-r**, **--reloc**\n> Show relocations.\n\n**--demangle**\n> Demangle symbol names."],["DESCRIPTION","**llvm-objdump** displays information about object files. Drop-in replacement for GNU objdump. Supports disassembly, header display, symbol tables, and relocation information for various binary formats."],["INSTALL","```apt: sudo apt install llvm```\n\n```dnf: sudo dnf install llvm```\n\n```pacman: sudo pacman -S llvm```\n\n```apk: sudo apk add llvm```\n\n```zypper: sudo zypper install llvm```\n\n```brew: brew install llvm```\n\n```nix: nix profile install nixpkgs#llvm```"],["SEE ALSO","[objdump](/man/objdump)(1), [llvm-nm](/man/llvm-nm)(1)"]],"e":["objdump","llvm-nm"],"x":"llvm-objdump -d [file.o]"},"logria":{"n":"logria","t":"CLI tool for real-time log aggregation and analysis","s":[["TAGLINE","CLI tool for real-time log aggregation and analysis"],["TLDR","**Open the setup screen** to configure a log stream interactively\n\n```logria```\n\n**Stream logs from a command**\n\n```logria -e \"[tail -f /var/log/syslog]\"```\n\n**Stream logs from a command** with history cache disabled\n\n```logria -e \"[command]\" -t```\n\n**Print current configuration paths**\n\n```logria -p```"],["SYNOPSIS","**logria** [_options_]"],["PARAMETERS","**-e**, **--exec** _COMMAND_\n> Execute a command and stream its output.\n\n**-t**, **--no-history-tape**\n> Disable command history disk cache.\n\n**-m**, **--mindless**\n> Disable variable polling rate based on incoming message rate.\n\n**-d**, **--docs**\n> Print documentation.\n\n**-p**, **--paths**\n> Print current configuration paths.\n\n**-h**, **--help**\n> Print help information.\n\n**-V**, **--version**\n> Print version information."],["DESCRIPTION","**logria** is a CLI tool for real-time log aggregation that allows you to filter, parse, and aggregate live data streams from shell commands. It supports regex filtering, user-defined parsing rules with aggregation methods, and session saving.\n\nYou can watch live logs from multiple processes and filter them for specific events, replace regex or filter patterns without killing the process or losing the stream history, and parse logs using custom rules."],["CAVEATS","Best suited for text-based log formats. Does not have a dedicated file input flag; use `-e \"tail -f file\"` to stream from files. Complex aggregation rules require manual configuration."],["HISTORY","**logria** was created by **ReagentX** and is written in **Rust** (with an earlier Python proof of concept)."],["INSTALL","```aur: yay -S logria```"],["SEE ALSO","[lnav](/man/lnav)(1), [tail](/man/tail)(1), [grep](/man/grep)(1), [multitail](/man/multitail)(1)"]],"e":["lnav","tail","grep","multitail"],"x":"logria"},"lxc-stop":{"n":"lxc-stop","t":"stops a running Linux container","s":[["TAGLINE","stops a running Linux container"],["TLDR","**Stop** container\n\n```sudo lxc-stop [container_name]```\n\nDisplay **help**\n\n```lxc-stop --help```"],["SYNOPSIS","**lxc-stop** **-n** _name_ [_options_]"],["DESCRIPTION","**lxc-stop** stops a running Linux container. It sends SIGPWR and SIGKILL signals to gracefully shut down the container's init process."],["PARAMETERS","**-n, --name NAME**\n> Container name\n\n**-k, --kill**\n> Kill container immediately (SIGKILL)\n\n**-W, --nowait**\n> Don't wait for shutdown\n\n**-t, --timeout SECONDS**\n> Shutdown timeout before kill\n\n**-r, --reboot**\n> Reboot instead of stop\n\n**-?, --help**\n> Display help information"],["CAVEATS","Requires root privileges. Graceful shutdown may take time. Use --kill for immediate termination."],["INSTALL","```apt: sudo apt install lxc```\n\n```dnf: sudo dnf install lxc```\n\n```pacman: sudo pacman -S lxc```\n\n```apk: sudo apk add lxc```\n\n```zypper: sudo zypper install lxc```\n\n```brew: brew install lxc```\n\n```nix: nix profile install nixpkgs#lxc```"],["SEE ALSO","[lxc-start](/man/lxc-start)(1), [lxc-destroy](/man/lxc-destroy)(1), [lxc-info](/man/lxc-info)(1)"]],"e":["lxc-start","lxc-destroy","lxc-info"],"x":"sudo lxc-stop [container_name]"},"mailsy":{"n":"mailsy","t":"creates temporary disposable emails","s":[["TAGLINE","creates temporary disposable emails"],["TLDR","**Generate temp email**\n\n```mailsy generate```\n\n**Check inbox**\n\n```mailsy check```\n\n**List messages**\n\n```mailsy list```\n\n**Read message**\n\n```mailsy read [message_id]```\n\n**Delete email**\n\n```mailsy delete```\n\n**Get current address**\n\n```mailsy me```"],["SYNOPSIS","**mailsy** _command_ [_options_]"],["PARAMETERS","_COMMAND_\n> Operation to perform.\n\n**generate**\n> Create new temp email.\n\n**check**\n> Check for new messages.\n\n**list**\n> List all messages.\n\n**read** _ID_\n> Read specific message.\n\n**delete**\n> Delete email address.\n\n**me**\n> Show current address.\n\n**--help**\n> Display help information."],["DESCRIPTION","**mailsy** creates temporary disposable emails. It generates random addresses for receiving mail.\n\nThe tool is useful for sign-ups and testing. Addresses are automatically deleted after a period."],["CAVEATS","Depends on external service. Emails expire. Not for production use."],["HISTORY","mailsy provides CLI access to **temporary email** services for disposable addresses."],["INSTALL","```brew: brew install mailsy```\n\n```nix: nix profile install nixpkgs#mailsy```"],["SEE ALSO","[tmpmail](/man/tmpmail)(1), [mail](/man/mail)(1)"]],"e":["tmpmail","mail"],"x":"mailsy generate"},"mate-screenshot":{"n":"mate-screenshot","t":"captures screenshots in the MATE desktop environment","s":[["TAGLINE","captures screenshots in the MATE desktop environment"],["TLDR","Create a **fullscreen** screenshot\n\n```mate-screenshot```\n\nCreate an **active window** screenshot\n\n```mate-screenshot --window```\n\nCreate a **specific area** screenshot\n\n```mate-screenshot --area```\n\nCreate a screenshot **interactively**\n\n```mate-screenshot --interactive```\n\nCreate a screenshot **without borders**\n\n```mate-screenshot --window --remove-border```\n\nCreate a screenshot with a **specific effect**\n\n```mate-screenshot --effect [shadow|border|none]```\n\nCreate a screenshot with a **specific delay** in seconds\n\n```mate-screenshot --delay [5]```\n\nCreate a screenshot and **copy to clipboard** instead of saving\n\n```mate-screenshot --clipboard```"],["SYNOPSIS","**mate-screenshot** [_options_]"],["PARAMETERS","**--window, -w**\n> Capture only the active window\n\n**--area, -a**\n> Select a region to capture\n\n**--interactive, -i**\n> Open a dialog for capture options before taking screenshot\n\n**--delay _seconds_, -d**\n> Wait specified seconds before capturing\n\n**--remove-border, -B**\n> Remove window border from window captures\n\n**--effect _type_, -e**\n> Add an effect: shadow, border, or none\n\n**--include-border, -b**\n> Include window border in capture (default)\n\n**--clipboard, -c**\n> Copy screenshot to clipboard instead of saving"],["DESCRIPTION","**mate-screenshot** captures screenshots in the MATE desktop environment. It can capture the entire screen, a single window, or a user-selected region.\n\nBy default, screenshots are saved to the Pictures directory with a timestamped filename. The interactive mode presents a dialog for choosing capture type, delay, and effects before taking the screenshot.\n\nEffects like drop shadows can be applied to window captures for a more polished appearance."],["CAVEATS","Area selection requires mouse interaction to draw the capture region. The delay option is useful for capturing menus or tooltips that appear on hover. Window captures may not work correctly with some compositors or window managers."],["INSTALL","```apt: sudo apt install mate-utils```\n\n```dnf: sudo dnf install mate-utils```\n\n```pacman: sudo pacman -S mate-utils```\n\n```apk: sudo apk add mate-utils```\n\n```zypper: sudo zypper install mate-screenshot```\n\n```nix: nix profile install nixpkgs#mate-utils```"],["SEE ALSO","[gnome-screenshot](/man/gnome-screenshot)(1), [scrot](/man/scrot)(1), [flameshot](/man/flameshot)(1), [maim](/man/maim)(1)"]],"e":["gnome-screenshot","scrot","flameshot","maim"],"x":"mate-screenshot"},"munpack":{"n":"munpack","t":"extracts files from MIME-encoded messages","s":[["TAGLINE","extracts files from MIME-encoded messages"],["TLDR","**Extract MIME attachments**\n\n```munpack [message.mime]```\n\n**Extract from stdin**\n\n```cat [email.eml] | munpack```\n\n**Extract to specific directory**\n\n```munpack -C [output_dir] [message.mime]```\n\n**Quiet mode**\n\n```munpack -q [message.mime]```\n\n**Force overwrite**\n\n```munpack -f [message.mime]```"],["SYNOPSIS","**munpack** [_options_] [_files_...]"],["PARAMETERS","**-f**\n> Force overwrite existing files.\n\n**-q**\n> Quiet mode.\n\n**-t**\n> Text mode (convert newlines).\n\n**-C** _dir_\n> Extract to directory."],["DESCRIPTION","**munpack** extracts files from MIME-encoded messages. It decodes base64 and quoted-printable content and saves attachments as separate files.\n\nmunpack is the companion to mpack and handles multipart MIME messages."],["EXAMPLE","```bash"],["Extract from email","munpack saved_email.eml"],["Extract from mailbox","formail -s munpack < mbox"],["Extract to specific folder","munpack -C ~/attachments email.mime\n```"],["SUPPORTED ENCODINGS","```\n- Base64\n- Quoted-printable\n- Uuencode (legacy)\n- BinHex (legacy)\n```"],["CAVEATS","Filenames may be sanitized. Nested MIME may need multiple passes. Some legacy encodings not fully supported."],["HISTORY","munpack was written by **John G. Myers** at Carnegie Mellon University as part of the MIME utilities for handling email attachments."],["INSTALL","```apt: sudo apt install mpack```\n\n```brew: brew install mpack```\n\n```nix: nix profile install nixpkgs#mpack```"],["SEE ALSO","[mpack](/man/mpack)(1), [uudecode](/man/uudecode)(1), [base64](/man/base64)(1), [ripmime](/man/ripmime)(1)"]],"e":["mpack","uudecode","base64","ripmime"],"x":"munpack [message.mime]"},"musescore":{"n":"musescore","t":"free music notation software","s":[["TAGLINE","free music notation software"],["TLDR","**Open MuseScore**\n\n```musescore```\n\n**Open score file**\n\n```musescore [score.mscz]```\n\n**Export to PDF**\n\n```musescore [score.mscz] -o [output.pdf]```\n\n**Export to audio**\n\n```musescore [score.mscz] -o [output.mp3]```\n\n**Batch convert files**\n\n```musescore -j [job.json]```\n\n**Run without GUI**\n\n```musescore --no-gui [score.mscz] -o [output.pdf]```"],["SYNOPSIS","**musescore** [_options_] [_files_]"],["PARAMETERS","_FILES_\n> Score files to open.\n\n**-o** _FILE_\n> Output file.\n\n**-j** _FILE_\n> Batch job file (JSON).\n\n**--no-gui**\n> Headless mode.\n\n**-S** _FILE_\n> Style file to use.\n\n**--help**\n> Display help information."],["DESCRIPTION","**musescore** is free music notation software. It creates, plays, and prints sheet music.\n\nThe tool supports many formats. MIDI, MusicXML, PDF export. Professional features."],["CAVEATS","GUI application. Soundfont required for playback. Resource intensive for large scores."],["HISTORY","MuseScore was first released in **2002** as open-source alternative to commercial notation software."],["INSTALL","```apt: sudo apt install musescore```\n\n```dnf: sudo dnf install musescore```\n\n```pacman: sudo pacman -S musescore```\n\n```zypper: sudo zypper install musescore```\n\n```nix: nix profile install nixpkgs#musescore```"],["SEE ALSO","[mscore](/man/mscore)(1), [lilypond](/man/lilypond)(1)"]],"e":["mscore","lilypond"],"x":"musescore"},"mvn-install":{"n":"mvn-install","t":"Build project and install artifact to local Maven repository","s":[["TAGLINE","Build project and install artifact to local Maven repository"],["TLDR","**Install to local repository**\n\n```mvn install```\n\n**Install skipping tests**\n\n```mvn install -DskipTests```\n\n**Clean and install** (remove target/ first)\n\n```mvn clean install```\n\n**Install with a specific profile**\n\n```mvn install -P [profile]```\n\n**Install in offline mode** (no network access)\n\n```mvn install -o```\n\n**Install with debug output**\n\n```mvn install -X```\n\n**Install with parallel builds** using multiple threads\n\n```mvn install -T [4]```"],["SYNOPSIS","**mvn install** [_options_]"],["PARAMETERS","**-DskipTests**\n> Skip test execution but still compile tests.\n\n**-Dmaven.test.skip=true**\n> Skip test compilation and execution entirely.\n\n**-P** _profile_\n> Activate a build profile defined in pom.xml.\n\n**-pl** _modules_\n> Build only specified modules (comma-separated).\n\n**-am**\n> Also build required upstream modules (use with -pl).\n\n**-o**\n> Work offline, use only locally cached dependencies.\n\n**-U**\n> Force update of snapshot dependencies from remote repositories.\n\n**-T** _threads_\n> Number of threads for parallel module builds (e.g., 4 or 1C for one thread per CPU core).\n\n**-X**\n> Enable debug output for troubleshooting.\n\n**-e**\n> Show full stack traces on errors.\n\n**-q**\n> Quiet output, only show errors.\n\n**-f** _file_\n> Use an alternative POM file."],["DESCRIPTION","**mvn install** is a Maven build lifecycle phase that builds the project and installs the resulting artifact (JAR, WAR, etc.) to the local Maven repository at ~/.m2/repository. This makes the artifact available as a dependency for other local projects.\n\nRunning `mvn install` executes all preceding lifecycle phases in order: validate, compile, test, package, verify, and finally install. Each phase runs its bound plugin goals.\n\nThe local repository acts as a cache for both locally built artifacts and dependencies downloaded from remote repositories. Installing an artifact locally is a prerequisite for other local projects to depend on it during development."],["CAVEATS","The install phase runs all tests by default; use -DskipTests to skip. The local repository (~/.m2/repository) can grow large over time. Snapshot versions are not automatically updated unless -U is used. Multi-module projects install all modules by default."],["INSTALL","```dnf: sudo dnf install maven```\n\n```pacman: sudo pacman -S maven```\n\n```apk: sudo apk add maven```\n\n```zypper: sudo zypper install maven```\n\n```brew: brew install maven```\n\n```nix: nix profile install nixpkgs#maven```"],["SEE ALSO","[mvn](/man/mvn)(1), [mvn-package](/man/mvn-package)(1), [mvn-deploy](/man/mvn-deploy)(1), [gradle](/man/gradle)(1)"]],"e":["mvn","mvn-package","mvn-deploy","gradle"],"x":"mvn install"},"mysql":{"n":"mysql","t":"command-line client","s":[["TAGLINE","command-line client"],["TLDR","**Connect to database**\n\n```mysql -u [username] -p [database]```\n\n**Connect to remote host**\n\n```mysql -h [hostname] -u [username] -p [database]```\n\n**Execute SQL command**\n\n```mysql -u [user] -p -e \"[SELECT * FROM table]\" [database]```\n\n**Execute SQL file**\n\n```mysql -u [user] -p [database] < [script.sql]```\n\n**Connect with specific port**\n\n```mysql -h [host] -P [3306] -u [user] -p```\n\n**Import database dump**\n\n```mysql -u [user] -p [database] < [dump.sql]```"],["SYNOPSIS","**mysql** [_options_] [_database_]"],["PARAMETERS","_DATABASE_\n> Database name.\n\n**-h** _HOST_\n> Server hostname.\n\n**-u** _USER_\n> Username.\n\n**-p** [_PASSWORD_]\n> Password (prompts if empty).\n\n**-P** _PORT_\n> Port number.\n\n**-e** _COMMAND_\n> Execute SQL and exit.\n\n**--help**\n> Display help information."],["DESCRIPTION","**mysql** is the MySQL command-line client. It connects to MySQL/MariaDB servers.\n\nThe tool executes SQL queries. Provides interactive and batch modes."],["CAVEATS","Requires server access. Password prompt recommended. Beware SQL injection in scripts."],["HISTORY","mysql was created as the standard CLI for **MySQL** database, maintained by Oracle and the community."],["INSTALL","```apt: sudo apt install mariadb-client-core```\n\n```dnf: sudo dnf install mysql```\n\n```apk: sudo apk add mariadb-client```\n\n```zypper: sudo zypper install mariadb-client```\n\n```brew: brew install mysql```"],["SEE ALSO","[mysqladmin](/man/mysqladmin)(1), [mysqldump](/man/mysqldump)(1), [mycli](/man/mycli)(1), [mariadb](/man/mariadb)(1), [psql](/man/psql)(1), [sqlite3](/man/sqlite3)(1)"]],"e":["mysqladmin","mysqldump","mycli","mariadb","psql","sqlite3"],"x":"mysql -u [username] -p [database]"},"namei":{"n":"namei","t":"follows a pathname until a terminal point is found, displaying each component","s":[["TAGLINE","follows a pathname until a terminal point is found, displaying each component"],["TLDR","**Resolve pathnames** following symbolic links\n\n```namei [path/to/a] [path/to/b]```\n\nDisplay in **long-listing format**\n\n```namei -l [path/to/file]```\n\nShow **mode bits** of each file type\n\n```namei -m [path/to/file]```\n\nShow **owner and group** of each file\n\n```namei -o [path/to/file]```\n\n**Don't follow** symbolic links\n\n```namei -n [path/to/file]```\n\nShow **all information** (long, modes, owners)\n\n```namei -lmo [path/to/file]```"],["SYNOPSIS","**namei** [**-l**] [**-m**] [**-o**] [**-n**] [**-x**] [**-v**] _pathname_..."],["PARAMETERS","**-l, --long**\n> Use long-listing format (same as -m -o -v)\n\n**-m, --modes**\n> Show mode bits of each file type in ls style\n\n**-o, --owners**\n> Show owner and group name of each file\n\n**-n, --nosymlinks**\n> Don't follow symbolic links\n\n**-x, --mountpoints**\n> Show mountpoint directories with D\n\n**-v, --vertical**\n> Vertical alignment of modes and owners\n\n**-Z, --context**\n> Show security context of each file\n\n**-h, --help**\n> Display help\n\n**-V, --version**\n> Display version"],["DESCRIPTION","**namei** follows a pathname until a terminal point is found (file, directory, device, etc.), displaying each component of the path. It is particularly useful for diagnosing \"too many levels of symbolic links\" errors.\n\nThe output shows each path component with its type indicator: d (directory), l (symbolic link), - (regular file), c (character device), b (block device), s (socket), p (FIFO/pipe)."],["OUTPUT SYMBOLS","**f:** - Pathname currently being resolved\n**d** - Directory\n**l** - Symbolic link\n**s** - Socket\n**b** - Block device\n**c** - Character device\n**p** - FIFO/named pipe\n**-** - Regular file\n**?** - Error of some kind\n**D** - Mountpoint (with -x)"],["CAVEATS","Symbolic link loops will cause errors. The command stops at the first unresolvable component. Permission denied errors occur if any path component is inaccessible."],["HISTORY","**namei** is part of **util-linux**, the standard Linux utility collection. It provides a diagnostic tool for understanding complex path structures and resolving symlink issues."],["INSTALL","```apt: sudo apt install util-linux```\n\n```dnf: sudo dnf install util-linux```\n\n```pacman: sudo pacman -S util-linux```\n\n```apk: sudo apk add util-linux-misc```\n\n```zypper: sudo zypper install util-linux```\n\n```brew: brew install util-linux```\n\n```nix: nix profile install nixpkgs#util-linux```"],["SEE ALSO","[ls](/man/ls)(1), [stat](/man/stat)(1), [readlink](/man/readlink)(1)"]],"e":["ls","stat","readlink"],"x":"namei [path/to/a] [path/to/b]"},"nitch":{"n":"nitch","t":"small, fast system fetch utility written in Nim","s":[["TAGLINE","small, fast system fetch utility written in Nim"],["TLDR","Display **system information**\n\n```nitch```\n\nDisplay **help**\n\n```nitch -h```\n\nDisplay **version**\n\n```nitch -v```"],["SYNOPSIS","**nitch** [**-h**|**--help**] [**-v**|**--version**]"],["PARAMETERS","**-h, --help**\n> Display help information\n\n**-v, --version**\n> Display version information"],["DESCRIPTION","**nitch** is a small, fast system fetch utility written in Nim. It displays system information including hostname, username, operating system, kernel version, uptime, shell, packages, and memory usage.\n\nThe tool is designed for speed and minimalism, with no configuration options. It focuses on displaying essential system information quickly with a clean, colorful output format."],["OUTPUT","Displays:\n- User and hostname\n- Operating system\n- Kernel version\n- Uptime\n- Shell\n- Package count\n- Memory usage (used/total)"],["CAVEATS","Package count accuracy depends on the package manager detected. Some information may not be available on all systems. No configuration or customization options."],["HISTORY","**nitch** was developed by **ssleert** as an alternative to heavier system fetch tools like neofetch and fastfetch. Written in Nim for high performance, it focuses on displaying only essential system information with minimal resource usage. It targets Linux systems and retrieves data directly from system files such as /proc and /etc/os-release."],["INSTALL","```nix: nix profile install nixpkgs#nitch```"],["SEE ALSO","[neofetch](/man/neofetch)(1), [fastfetch](/man/fastfetch)(1), [screenfetch](/man/screenfetch)(1), [pfetch](/man/pfetch)(1), [cpufetch](/man/cpufetch)(1)"]],"e":["neofetch","fastfetch","screenfetch","pfetch","cpufetch"],"x":"nitch"},"nm-tool":{"n":"nm-tool","t":"displays NetworkManager information","s":[["TAGLINE","displays NetworkManager information"],["TLDR","**Show network status**\n\n```nm-tool```\n\n**Show all information**\n\n```nm-tool --all```"],["SYNOPSIS","**nm-tool** [_options_]"],["PARAMETERS","**--all**\n> Show all details.\n\n**--help**\n> Display help information."],["DESCRIPTION","**nm-tool** displays NetworkManager information. Shows network connections and status.\n\nThe tool queries NetworkManager. Deprecated in favor of nmcli."],["CAVEATS","Deprecated. Use nmcli instead. Requires NetworkManager."],["HISTORY","nm-tool was the original **NetworkManager** CLI tool, now replaced by nmcli."],["SEE ALSO","[nmcli](/man/nmcli)(1), [nm-applet](/man/nm-applet)(1), [NetworkManager](/man/NetworkManager)(1)"]],"e":["nmcli","nm-applet","NetworkManager"],"x":"nm-tool"},"ods-control":{"n":"ods-control","t":"manages OpenDNSSEC components","s":[["TAGLINE","manages OpenDNSSEC components"],["TLDR","**Start ODS services**\n\n```ods-control start```\n\n**Stop ODS services**\n\n```ods-control stop```\n\n**Check status**\n\n```ods-control status```\n\n**Reload configuration**\n\n```ods-control reload```\n\n**Start enforcer only**\n\n```ods-control enforcer start```"],["SYNOPSIS","**ods-control** [_component_] _command_"],["PARAMETERS","**start**\n> Start services.\n\n**stop**\n> Stop services.\n\n**status**\n> Show status.\n\n**reload**\n> Reload configuration.\n\n**enforcer**\n> Control enforcer only.\n\n**signer**\n> Control signer only."],["DESCRIPTION","**ods-control** manages OpenDNSSEC components. OpenDNSSEC automates DNSSEC key management and zone signing.\n\nThe tool controls the enforcer (key management) and signer (zone signing) daemons."],["COMPONENTS","```\nenforcer - Key management daemon\nsigner   - Zone signing daemon\n```"],["TYPICAL WORKFLOW","```bash"],["Start OpenDNSSEC","ods-control start"],["Check status","ods-control status"],["After config change","ods-control reload\n```"],["CAVEATS","Requires configured KASP. Database must be initialized. Root privileges typically needed."],["HISTORY","OpenDNSSEC was developed to simplify DNSSEC deployment, providing automated key rollover and zone signing."],["SEE ALSO","[ods-enforcer](/man/ods-enforcer)(8)"]],"e":["ods-enforcer"],"x":"ods-control start"},"package-lock.json":{"n":"package-lock.json","t":"npm-generated lockfile pinning the exact installed dependency tree","s":[["TAGLINE","npm-generated lockfile pinning the exact installed dependency tree"],["TLDR","Note: **package-lock.json** is a generated file, not an executable command. The commands below operate on it.\n\n**Create or update** the lock file based on **package.json**\n\n```npm install```\n\n**Clean install from lock file** (fails if lock file is out of sync; used in CI)\n\n```npm ci```\n\n**Update a specific package** and rewrite the lock file\n\n```npm update [package]```\n\n**Audit** the exact versions in the lock file for known CVEs\n\n```npm audit```\n\n**Inspect changes** to the lock file in a diff\n\n```git diff package-lock.json```\n\n**Regenerate** the lock file from scratch\n\n```rm package-lock.json && npm install```"],["SYNOPSIS","**package-lock.json** - npm dependency lock file (placed in the project root)"],["DESCRIPTION","**package-lock.json** is automatically generated by **npm** to describe the exact tree of dependencies that was installed. It records every resolved version, the registry URL, and an integrity hash (SRI), so subsequent installs produce the same **node_modules** regardless of which semver range the manifest allows.\n\nThe file should be committed to version control. In CI, use **npm ci** instead of **npm install**: **npm ci** installs directly from the lock file without touching it, fails fast if it drifts from **package.json**, and is significantly faster."],["KEY FIELDS","```json\n{\n  \"name\": \"my-project\",\n  \"lockfileVersion\": 3,\n  \"packages\": {\n    \"\": { \"dependencies\": {...} },\n    \"node_modules/lodash\": {\n      \"version\": \"4.17.21\",\n      \"resolved\": \"https://registry.npmjs.org/...\",\n      \"integrity\": \"sha512-...\"\n    }\n  }\n}\n```"],["LOCK FILE VERSIONS","```\nv1 - npm 5-6\nv2 - npm 7+ (backwards compatible)\nv3 - npm 7+ (optimized)\n```"],["CAVEATS","Do not edit by hand. Always commit it to git. Merge conflicts are common and are best resolved by accepting one side and re-running **npm install**. If both **package-lock.json** and **npm-shrinkwrap.json** exist, the shrinkwrap file takes precedence. Unlike **npm-shrinkwrap.json**, **package-lock.json** is never published to the registry."],["HISTORY","Introduced in **npm 5** (2017) to address long-standing reproducibility issues. **lockfileVersion 2** shipped with **npm 7** (backwards compatible with v1), and **lockfileVersion 3** (npm 7+) dropped the legacy **dependencies** block for a smaller, packages-only format."],["SEE ALSO","[npm](/man/npm)(1), [npm-ci](/man/npm-ci)(1), [npm-audit](/man/npm-audit)(1), [npm-shrinkwrap](/man/npm-shrinkwrap)(1), [yarn](/man/yarn)(1)"]],"e":["npm","npm-ci","npm-audit","npm-shrinkwrap","yarn"],"x":"npm install"},"pct-exec":{"n":"pct-exec","t":"launches a command inside a specified Proxmox LXC container","s":[["TAGLINE","launches a command inside a specified Proxmox LXC container"],["TLDR","**Execute** command in container\n\n```pct exec 100 command```\n\nOpen **bash** shell\n\n```pct exec 100 bash```\n\nPass **arguments**\n\n```pct exec 100 -- command arguments```"],["SYNOPSIS","**pct exec** _vmid_ [--] _command_ [_arguments_...]"],["DESCRIPTION","**pct exec** launches a command inside a specified Proxmox LXC container. It provides direct command execution without needing to enter the container interactively."],["PARAMETERS","_VMID_\n> The numeric ID of the LXC container (100–999999999).\n\n_COMMAND_\n> The command and any arguments to execute inside the container.\n\n**--**\n> Separator that stops `pct` from interpreting subsequent flags, so they are passed through to the command."],["CAVEATS","The container must be running to execute commands. The command runs as **root** inside the container, regardless of the host user invoking `pct exec`. There is no TTY allocation by default — for interactive shells use **pct enter** instead. The command's exit status is returned to the caller, but pre-execution failures (container not running, etc.) yield Proxmox-specific error codes."],["HISTORY","**pct exec** is part of the **Proxmox VE** virtualization platform for managing LXC containers."],["SEE ALSO","[pct](/man/pct)(1), [pct-enter](/man/pct-enter)(1)"]],"e":["pct","pct-enter"],"x":"pct exec 100 command"},"pgmtofs":{"n":"pgmtofs","t":"converts PGM images to Usenix FaceSaver format, used for storing face images","s":[["TAGLINE","converts PGM images to Usenix FaceSaver format, used for storing face images"],["TLDR","**Convert PGM to Usenix FaceSaver format**\n\n```pgmtofs [input.pgm] > [output.fs]```"],["SYNOPSIS","**pgmtofs** [_file_]"],["DESCRIPTION","**pgmtofs** converts PGM images to Usenix FaceSaver format, used for storing face images for email and news systems. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[fstopgm](/man/fstopgm)(1), [pbmtoybm](/man/pbmtoybm)(1)"]],"e":["fstopgm","pbmtoybm"],"x":"pgmtofs [input.pgm] > [output.fs]"},"play":{"n":"play","t":"Play audio files with effects via SoX","s":[["TAGLINE","Play audio files with effects via SoX"],["TLDR","**Play audio file**\n\n```play [audio.mp3]```\n\n**Play with effects**\n\n```play [audio.wav] reverb```\n\n**Play at different speed**\n\n```play [audio.wav] speed 1.5```\n\n**Play with volume change**\n\n```play [audio.wav] vol 0.5```\n\n**Combine files**\n\n```play [file1.wav] [file2.wav]```"],["SYNOPSIS","**play** [_options_] _file_ [_effects_...]"],["PARAMETERS","_FILE_\n> Audio file to play.\n\n_EFFECTS_\n> Audio effects to apply.\n\n**-v** _VOLUME_\n> Volume level.\n\n**-r** _RATE_\n> Sample rate.\n\n**--help**\n> Display help."],["DESCRIPTION","**play** plays audio files from the command line with optional real-time effects processing. Part of the SoX (Sound eXchange) audio toolkit, it supports a wide range of formats including WAV, MP3, FLAC, OGG, and many others.\n\nAudio effects such as reverb, echo, speed change, volume adjustment, and equalization can be chained together on the command line. Multiple files can be concatenated for sequential playback."],["CAVEATS","Part of SoX. Format support varies. Effects available."],["HISTORY","play is part of **SoX** (Sound eXchange) audio processing toolkit."],["INSTALL","```apt: sudo apt install sox```\n\n```dnf: sudo dnf install sox```\n\n```pacman: sudo pacman -S sox```\n\n```apk: sudo apk add sox```\n\n```zypper: sudo zypper install sox```\n\n```brew: brew install sox```\n\n```nix: nix profile install nixpkgs#sox```"],["SEE ALSO","[sox](/man/sox)(1), [rec](/man/rec)(1), [soxi](/man/soxi)(1)"]],"e":["sox","rec","soxi"],"x":"play [audio.mp3]"},"ppmgamma":{"n":"ppmgamma","t":"Apply gamma correction to PPM images","s":[["TAGLINE","Apply gamma correction to PPM images"],["TLDR","**Apply gamma correction**\n\n```ppmgamma [2.2] [input.ppm] > [output.ppm]```\n\n**Lighten image**\n\n```ppmgamma [0.5] [input.ppm] > [output.ppm]```\n\n**Darken image**\n\n```ppmgamma [2.0] [input.ppm] > [output.ppm]```\n\n**Per-channel gamma**\n\n```ppmgamma [1.0] [1.2] [1.0] [input.ppm] > [output.ppm]```"],["SYNOPSIS","**ppmgamma** _gamma_ [_ppmfile_]\n\n**ppmgamma** _rgamma_ _ggamma_ _bgamma_ [_ppmfile_]"],["PARAMETERS","**gamma**\n> Gamma value for all channels.\n\n**rgamma**, **ggamma**, **bgamma**\n> Per-channel gamma values."],["DESCRIPTION","**ppmgamma** applies gamma correction to a PPM image. Gamma < 1 lightens the image, gamma > 1 darkens it. Each pixel value is raised to the power of 1/gamma.\n\nPart of Netpbm toolkit."],["EXAMPLES","```bash"],["Standard gamma correction","ppmgamma 2.2 image.ppm > corrected.ppm"],["Lighten dark image","ppmgamma 0.5 dark.ppm > lighter.ppm"],["Darken bright image","ppmgamma 2.0 bright.ppm > darker.ppm"],["Adjust blue channel only","ppmgamma 1.0 1.0 1.3 image.ppm > blue_adjusted.ppm"],["Reverse gamma","ppmgamma 0.45 linear.ppm > srgb.ppm\n```"],["GAMMA VALUES","```\n< 1.0  - Lighten midtones\n= 1.0  - No change\n> 1.0  - Darken midtones\n2.2    - Typical monitor gamma\n```"],["CAVEATS","Use pamgamma for more features. Values at 0 and max unchanged. Part of Netpbm."],["HISTORY","ppmgamma is part of **Netpbm** by **Jef Poskanzer**, implementing gamma correction for images."],["SEE ALSO","[pamgamma](/man/pamgamma)(1), [ppmbrighten](/man/ppmbrighten)(1), [pnmgamma](/man/pnmgamma)(1), [netpbm](/man/netpbm)(1)"]],"e":["pamgamma","ppmbrighten","pnmgamma","netpbm"],"x":"ppmgamma [2.2] [input.ppm] > [output.ppm]"},"ppmtowinicon":{"n":"ppmtowinicon","t":"Convert PPM to Windows icon format","s":[["TAGLINE","Convert PPM to Windows icon format"],["TLDR","**Convert PPM to Windows icon**\n\n```ppmtowinicon [input.ppm] > [output.ico]```\n\n**Include AND mask**\n\n```ppmtowinicon -andmask [mask.pbm] [input.ppm] > [output.ico]```"],["SYNOPSIS","**ppmtowinicon** [_options_] [_file_]"],["PARAMETERS","**-andmask** _file_\n> Transparency mask file."],["DESCRIPTION","**ppmtowinicon** converts PPM images to Windows ICO format. Supports transparency via AND mask. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[winicontoppm](/man/winicontoppm)(1), [pamtowinicon](/man/pamtowinicon)(1)"]],"e":["winicontoppm","pamtowinicon"],"x":"ppmtowinicon [input.ppm] > [output.ico]"},"procs":{"n":"procs","t":"Modern Rust-based process viewer","s":[["TAGLINE","Modern Rust-based process viewer"],["TLDR","**List all processes**\n\n```procs```\n\n**Search for process by name**\n\n```procs [nginx]```\n\n**Watch processes** (auto-refresh)\n\n```procs --watch```\n\n**Show tree view**\n\n```procs --tree```\n\n**Sort by CPU usage**\n\n```procs --sortd cpu```\n\n**Sort by memory usage**\n\n```procs --sortd mem```\n\n**Show specific columns**\n\n```procs --insert [read,write]```\n\n**Show processes by user**\n\n```procs --or \"user eq [username]\"```"],["SYNOPSIS","**procs** [_--tree_] [_--watch_] [_--sortd column_] [_--insert columns_] [_keyword_]"],["PARAMETERS","**--tree**, **-t**\n> Tree view showing parent-child relationships.\n\n**--watch**, **-w**\n> Watch mode with auto-refresh.\n\n**--watch-interval** _SEC_\n> Refresh interval in seconds.\n\n**--sortd** _COLUMN_\n> Sort descending by column.\n\n**--sorta** _COLUMN_\n> Sort ascending by column.\n\n**--insert** _COLUMNS_\n> Add columns to display.\n\n**--only** _COLUMNS_\n> Show only specified columns.\n\n**--or** _FILTER_\n> Filter with OR logic.\n\n**--and** _FILTER_\n> Filter with AND logic.\n\n**--color** _MODE_\n> Color mode: auto, always, disable.\n\n**--pager** _MODE_\n> Pager: auto, always, disable.\n\n**--config** _FILE_\n> Use specific config file.\n\n**--theme** _THEME_\n> Color theme.\n\n**--no-header**\n> Hide column headers."],["DESCRIPTION","**procs** is a modern replacement for ps, written in Rust with enhanced output formatting. It displays process information with syntax highlighting, making output more readable.\n\nThe default view shows more useful information than ps: command name, PID, user, CPU%, memory, and a truncated command line. Colors distinguish types of processes and highlight keywords.\n\nTree view (-t) shows process hierarchy with visual indentation. This makes it easy to see parent-child relationships without piping through pstree.\n\nWatch mode (-w) provides top-like real-time updates. Combined with sorting, it helps identify resource-heavy processes. The interval is configurable.\n\nSearch works by keyword matching against multiple fields: process name, command line, user. Filters provide more precise control using expressions like \"cpu gt 10\" or \"user eq root\".\n\nConfiguration file (~/.config/procs/config.toml) allows customizing columns, colors, and default options."],["CONFIGURATION","**~/.config/procs/config.toml**\n> User configuration file defining default columns, color themes, sort order, and display options."],["CAVEATS","Some ps options don't have direct equivalents. Filter syntax differs from ps. Config file format may change between versions. Large process lists can be slow to render. Terminal must support colors for best experience."],["HISTORY","**procs** was created by **dalance** around **2019** as part of the wave of Rust-based Unix tool rewrites. It aimed to improve on ps's dated output format while maintaining familiarity. The tool emphasizes visual clarity and modern terminal features."],["INSTALL","```apt: sudo apt install procs```\n\n```pacman: sudo pacman -S procs```\n\n```apk: sudo apk add procs```\n\n```zypper: sudo zypper install procs```\n\n```brew: brew install procs```\n\n```nix: nix profile install nixpkgs#procs```"],["SEE ALSO","[ps](/man/ps)(1), [top](/man/top)(1), [htop](/man/htop)(1), [pgrep](/man/pgrep)(1)"]],"e":["ps","top","htop","pgrep"],"x":"procs"},"rletopnm":{"n":"rletopnm","t":"Convert Utah RLE images to PNM format","s":[["TAGLINE","Convert Utah RLE images to PNM format"],["TLDR","**Convert Utah RLE to PNM**\n\n```rletopnm [input.rle] > [output.pnm]```\n\n**Verbose output**\n\n```rletopnm -verbose [input.rle] > [output.pnm]```"],["SYNOPSIS","**rletopnm** [_options_] [_file_]"],["PARAMETERS","**-verbose**\n> Print image information.\n\n**-headerdump**\n> Dump RLE header information."],["DESCRIPTION","**rletopnm** converts Utah RLE (Run Length Encoded) images to PNM format. Utah RLE is part of the Utah Raster Toolkit. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pnmtorle](/man/pnmtorle)(1), [sgitopnm](/man/sgitopnm)(1)"]],"e":["pnmtorle","sgitopnm"],"x":"rletopnm [input.rle] > [output.pnm]"},"script":{"n":"script","t":"Record terminal session to file","s":[["TAGLINE","Record terminal session to file"],["TLDR","**Record** a new session to a file named \"typescript\"\n\n```script```\n\n**Record** a session to a custom file path\n\n```script [path/to/session.out]```\n\n**Append** to an existing file\n\n```script -a [logfile.log]```\n\n**Record** timing information to a separate file\n\n```script -t 2> [path/to/timing_file]```\n\n**Write** out data as soon as it happens (flush immediately)\n\n```script -f [path/to/file]```\n\n**Execute** quietly without start and done messages\n\n```script -q [logfile.log]```\n\n**Stop** recording\n\n```exit```"],["SYNOPSIS","**script** [_options_] [_file_]"],["PARAMETERS","**-a, --append**\n> Append output to an existing file\n\n**-f, --flush**\n> Flush output after each write\n\n**-q, --quiet**\n> Be quiet (suppress start and done messages)\n\n**-t, --timing[=_file_]**\n> Output timing data for scriptreplay\n\n**-c, --command _command_**\n> Run command rather than interactive shell\n\n**-e, --return**\n> Return exit code of the child process\n\n**-O, --log-out _file_**\n> Log stdout to file (structured)"],["DESCRIPTION","**script** records all terminal output to a typescript file. It creates a forked shell and captures everything displayed on the terminal, including input and output, until the session ends with **exit** or **Ctrl-D**.\n\nThe recorded session can be replayed using **scriptreplay** when timing information is captured. This is useful for creating tutorials, documenting procedures, or auditing terminal sessions."],["CAVEATS","The typescript file contains control characters and escape sequences. Use **cat -v** or **less -R** to view. Large sessions can produce very large files. Part of the util-linux package."],["HISTORY","The **script** command first appeared in 3.0BSD. It has been a standard Unix utility for recording terminal sessions since the early 1980s."],["INSTALL","```apt: sudo apt install bsdutils```\n\n```apk: sudo apk add util-linux-misc```"],["SEE ALSO","[scriptreplay](/man/scriptreplay)(1), [scriptlive](/man/scriptlive)(1), [tee](/man/tee)(1), [ttyrec](/man/ttyrec)(1)"]],"e":["scriptreplay","scriptlive","tee","ttyrec"],"x":"script"},"seinfo":{"n":"seinfo","t":"Query and examine SELinux policy components","s":[["TAGLINE","Query and examine SELinux policy components"],["TLDR","**Display policy statistics** for the current system policy\n\n```seinfo```\n\n**List all types** in the policy\n\n```seinfo -t```\n\n**Show details** for a specific type with expanded information\n\n```seinfo -t [httpd_t] -x```\n\n**List all attributes** assigned to a type attribute\n\n```seinfo -a [exec_type] -x```\n\n**List all roles** in the policy\n\n```seinfo -r```\n\n**List all booleans** in the policy\n\n```seinfo -b```\n\n**Display constraints** for an object class\n\n```seinfo --constrain [file]```\n\n**Query a specific policy file** instead of system default\n\n```seinfo [options] [path/to/policy]```"],["SYNOPSIS","**seinfo** [_options_] [_expression_] [_policy_]"],["PARAMETERS","**-a** [_ATTR_]\n> Display type attributes or details for a named attribute.\n\n**-b** [_BOOL_]\n> Show booleans or specific boolean configuration.\n\n**-c** [_CLASS_]\n> List object classes or permissions for a specific class.\n\n**-r** [_ROLE_]\n> Query roles or role-specific information.\n\n**-t** [_TYPE_]\n> Examine types or their assigned attributes.\n\n**-u** [_USER_]\n> Display users or user statements.\n\n**--category** [_CAT_]\n> Show categories or associated sensitivities.\n\n**--common** [_COMMON_]\n> List common permission sets.\n\n**--constrain** [_CLASS_]\n> Display constraints and mlsconstraints for an object class.\n\n**--all**\n> Query all policy components.\n\n**-x**, **--expand**\n> Print additional details for each component matching the expression.\n\n**--flat**\n> Exclude formatting headers and indentation.\n\n**-v**, **--verbose**\n> Display extra informational content.\n\n**--debug**\n> Activate debugging output.\n\n**-h**, **--help**\n> Display help documentation.\n\n**--version**\n> Show version information."],["DESCRIPTION","**seinfo** is a SELinux policy query tool that allows users to examine the components of a SELinux policy. It can analyze both binary and source policies, providing detailed information about types, attributes, roles, users, booleans, object classes, and constraints.\n\nIf no policy file is provided, seinfo searches for the system default policy: first checking for a source policy, then for a binary policy matching the running kernel's preferred version, and finally for the highest version available. The tool supports loading policies in source format (policy.conf), binary format (e.g., policy.20), or modular format with loadable policy modules.\n\nseinfo is part of the SETools suite and is commonly used for policy analysis, debugging, and security auditing on SELinux-enabled systems."],["CAVEATS","Policy files must be readable by the user running seinfo. When analyzing modular policies, the first module listed must be a base module. Some query options may produce large outputs on complex policies; use **--flat** or pipe to pagers for better readability."],["HISTORY","**seinfo** is part of the SETools project, a collection of SELinux policy analysis tools originally developed by Tresys Technology. SETools was created to help administrators and security professionals understand and manage SELinux policies. The project has been maintained and updated alongside SELinux development, with versions evolving to support new policy features and formats."],["INSTALL","```apt: sudo apt install setools```\n\n```dnf: sudo dnf install setools```\n\n```nix: nix profile install nixpkgs#setools```"],["SEE ALSO","[sesearch](/man/sesearch)(1), [semanage](/man/semanage)(8), [getenforce](/man/getenforce)(1)"]],"e":["sesearch","semanage","getenforce"],"x":"seinfo"},"sngrep":{"n":"sngrep","t":"Interactive SIP traffic analyzer","s":[["TAGLINE","Interactive SIP traffic analyzer"],["TLDR","**Capture SIP traffic**\n\n```sngrep```\n\n**Capture on interface**\n\n```sngrep -d [eth0]```\n\n**Read from pcap**\n\n```sngrep -I [capture.pcap]```\n\n**Show only INVITE dialogs**\n\n```sngrep -c```\n\n**Capture to file**\n\n```sngrep -O [output.pcap]```\n\n**Filter by IP using BPF**\n\n```sngrep host [192.168.1.100]```\n\n**Capture without interface** (quiet mode)\n\n```sngrep -N -q -O [output.pcap]```"],["SYNOPSIS","**sngrep** [_-d device_] [_-I file_] [_-O file_] [_options_] [_filter_]"],["PARAMETERS","**-d** _DEVICE_\n> Capture device (or comma-separated list).\n\n**-I** _FILE_\n> Read packets from pcap file.\n\n**-O** _FILE_\n> Save captured packets to pcap file.\n\n**-c**\n> Only display dialogs starting with INVITE.\n\n**-l** _LIMIT_\n> Maximum number of dialogs to capture.\n\n**-N**\n> No interface mode (capture only).\n\n**-q**\n> Quiet mode (suppress output in no-interface mode).\n\n**-r**\n> Capture RTP packet payloads.\n\n**-R**\n> Rotate calls when capture limit is reached.\n\n**-k** _FILE_\n> RSA private key for TLS decryption.\n\n**-B** _SIZE_\n> Pcap buffer size in MB (default: 2).\n\n**-H** _URL_\n> Send captured packets to Homer sipcapture URL.\n\n**-f** _FILE_\n> Load specific configuration file.\n\n**-D**\n> Print active configuration and exit."],["DESCRIPTION","**sngrep** is a terminal-based tool for capturing and analyzing SIP (Session Initiation Protocol) traffic used in VoIP systems. It provides an interactive ncurses interface that displays active SIP dialogs, call flow diagrams with directional arrows, and full message content including headers and body.\n\nThe tool captures SIP packets in real time from network interfaces or reads previously captured pcap files for offline analysis. Filters narrow results by SIP method, source or destination address, and custom BPF expressions. Captured traffic can be saved to pcap format for later review or sharing with other analysis tools.\n\nCall flow visualization is the tool's standout feature, showing the sequence of SIP messages (INVITE, 200 OK, ACK, BYE) between endpoints with timing information. This makes it particularly effective for diagnosing VoIP call setup failures, registration problems, and codec negotiation issues."],["INTERFACE CONTROLS","**Arrow keys** - Navigate\n**Enter** - Show details\n**F2** - Save\n**F7** - Filter\n**F10/q** - Quit"],["CAVEATS","Requires root for capture. TLS traffic needs decryption. High-volume may miss packets."],["HISTORY","**sngrep** was created by **Irontec** for VoIP troubleshooting. It provides visual SIP analysis in the terminal."],["INSTALL","```apt: sudo apt install sngrep```\n\n```dnf: sudo dnf install sngrep```\n\n```apk: sudo apk add sngrep```\n\n```zypper: sudo zypper install sngrep```\n\n```brew: brew install sngrep```\n\n```nix: nix profile install nixpkgs#sngrep```"],["SEE ALSO","[tcpdump](/man/tcpdump)(1), [tshark](/man/tshark)(1), [wireshark](/man/wireshark)(1)"]],"e":["tcpdump","tshark","wireshark"],"x":"sngrep"},"solc":{"n":"solc","t":"Solidity smart contract compiler","s":[["TAGLINE","Solidity smart contract compiler"],["TLDR","**Compile a Solidity file** and output bytecode\n\n```solc --bin [contract.sol]```\n\n**Compile and output ABI**\n\n```solc --abi [contract.sol]```\n\n**Compile with optimization** enabled\n\n```solc --optimize --bin [contract.sol]```\n\n**Output to a directory**\n\n```solc -o [output_dir] --bin --abi [contract.sol]```\n\n**Compile with Standard JSON** input/output\n\n```solc --standard-json < [input.json]```\n\n**Show estimated gas** for functions\n\n```solc --gas [contract.sol]```\n\n**Output abstract syntax tree**\n\n```solc --ast-compact-json [contract.sol]```\n\n**Remap import paths**\n\n```solc [prefix]=[path] [contract.sol]```"],["SYNOPSIS","**solc** [_options_] [_input-files_]"],["PARAMETERS","**--bin**\n> Output binary (bytecode) of the contracts.\n\n**--abi**\n> Output ABI (Application Binary Interface) specification.\n\n**--optimize**\n> Enable bytecode optimizer.\n\n**--optimize-runs** _n_\n> Optimize for n contract invocations (default: 200).\n\n**-o**, **--output-dir** _path_\n> Output directory for compiled artifacts.\n\n**--standard-json**\n> Use Standard JSON input/output mode. Reads from stdin if no file given.\n\n**--ast-compact-json**\n> Output AST in compact JSON format.\n\n**--asm**\n> Output EVM assembly.\n\n**--gas**\n> Output estimated gas usage for functions.\n\n**--metadata**\n> Output contract metadata.\n\n**--userdoc**\n> Output user documentation (NatSpec).\n\n**--devdoc**\n> Output developer documentation (NatSpec).\n\n**--combined-json** _keys_\n> Output combined JSON with specified keys (abi, bin, metadata, etc.).\n\n**--base-path** _path_\n> Base path for imports.\n\n**--include-path** _path_\n> Additional path for imports.\n\n**--evm-version** _version_\n> Target EVM version (homestead, tangerineWhistle, spuriousDragon, byzantium, constantinople, petersburg, istanbul, berlin, london, paris, shanghai).\n\n**--via-ir**\n> Enable compilation via Yul IR.\n\n**--lsp**\n> Run as Language Server Protocol backend.\n\n**--version**\n> Display version information."],["DESCRIPTION","**solc** is the command-line compiler for Solidity, the primary programming language for Ethereum smart contracts. It compiles Solidity source code into EVM (Ethereum Virtual Machine) bytecode that can be deployed to Ethereum and compatible blockchains.\n\nThe compiler can produce various outputs including bytecode, ABI definitions, assembly, abstract syntax trees, gas estimates, and documentation. The optimizer can reduce bytecode size and gas costs. Standard JSON mode provides structured input/output for integration with build tools.\n\nImport paths can be remapped using the **context:prefix=path** syntax, allowing flexible project structures. The compiler also supports LSP mode for IDE integration."],["CAVEATS","Different Solidity versions may have incompatible syntax; use **solc-select** to manage multiple versions. The optimizer should be tested thoroughly as it can sometimes introduce subtle issues. Gas estimates are approximations and may differ from actual execution costs. EVM version must match the target blockchain's capabilities."],["HISTORY","**Solidity** was proposed by Gavin Wood in **2014** and developed by the Ethereum Foundation's Solidity team. The first release was in **2015** alongside Ethereum's launch. The language and compiler have evolved significantly, with major versions introducing features like ABIEncoderV2, custom errors, and user-defined value types. Development continues actively to improve safety, efficiency, and developer experience."],["INSTALL","```nix: nix profile install nixpkgs#solc```"],["SEE ALSO","[forge](/man/forge)(1), [hardhat](/man/hardhat)(1)"]],"e":["forge","hardhat"],"x":"solc --bin [contract.sol]"},"sqly":{"n":"sqly","t":"Execute SQL against CSV, TSV, JSON, and Excel files","s":[["TAGLINE","Execute SQL against CSV, TSV, JSON, and Excel files"],["TLDR","**Open an interactive SQL shell with a CSV file**\n\n```sqly [user.csv]```\n\n**Run a query directly**\n\n```sqly --sql \"[SELECT * FROM user WHERE age > 30]\" [user.csv]```\n\n**Output results as CSV**\n\n```sqly --sql \"[SELECT * FROM user LIMIT 2]\" --csv [user.csv]```"],["SYNOPSIS","**sqly** [**--sql** _query_] [**--csv**|**--tsv**] [_files_...]"],["DESCRIPTION","**sqly** imports CSV, TSV, LTSV, JSON, and Excel files into an in-memory SQLite3 database, letting you run SQL queries against them. It supports both an interactive shell mode with SQL completion and command history, and direct one-shot query execution. Output can be formatted as ASCII table, CSV, TSV, or LTSV."],["HISTORY","**sqly** was created by **nao1215** and is written in **Go**."],["SEE ALSO","[csvq](/man/csvq)(1), [qsv](/man/qsv)(1), [miller](/man/miller)(1)"]],"e":["csvq","qsv","miller"],"x":"sqly [user.csv]"},"steamcmd":{"n":"steamcmd","t":"Steam command-line dedicated server tool","s":[["TAGLINE","Steam command-line dedicated server tool"],["TLDR","**Start interactive mode**\n\n```steamcmd```\n\n**Login anonymously**\n\n```steamcmd +login anonymous```\n\n**Install game server**\n\n```steamcmd +login anonymous +app_update [740] +quit```\n\n**Install to directory**\n\n```steamcmd +force_install_dir [/path/to/server] +login anonymous +app_update [740] +quit```\n\n**Validate installation**\n\n```steamcmd +login anonymous +app_update [740] validate +quit```\n\n**Login with credentials**\n\n```steamcmd +login [username] [password]```"],["SYNOPSIS","**steamcmd** [_+command_]..."],["PARAMETERS","**+login** _USER_ [_PASS_]\n> Login to Steam.\n\n**+app_update** _APPID_\n> Install/update app.\n\n**+force_install_dir** _PATH_\n> Installation directory.\n\n**+quit**\n> Exit after commands.\n\n**+validate**\n> Verify files.\n\n**+app_info_print** _APPID_\n> Show app info.\n\n**+workshop_download_item** _APPID_ _ITEMID_\n> Download workshop item."],["DESCRIPTION","**steamcmd** is Valve's command-line Steam client designed for automated management of dedicated game servers. It supports downloading, installing, updating, and validating game server files without requiring the full Steam graphical client.\n\nCommands are prefixed with **+** and can be chained for batch operation. Anonymous login is sufficient for most dedicated server downloads, while some content requires authentication with a Steam account that owns the game. The **+app_update** command handles both initial installation and subsequent updates, with **validate** verifying file integrity and repairing corrupted installations."],["COMMON APP IDS","**740** - Counter-Strike: Global Offensive\n**232250** - Team Fortress 2\n**258550** - Rust\n**376030** - ARK"],["CAVEATS","Some apps require game ownership. Downloads can be large. SteamGuard may require codes."],["HISTORY","**SteamCMD** was released by **Valve** for headless server management. It enables automated game server deployment without the full Steam client."],["INSTALL","```nix: nix profile install nixpkgs#steamcmd```"],["SEE ALSO","[steam](/man/steam)(1), [screen](/man/screen)(1), [tmux](/man/tmux)(1)"]],"e":["steam","screen","tmux"],"x":"steamcmd"},"synctl":{"n":"synctl","t":"Matrix Synapse homeserver control interface","s":[["TAGLINE","Matrix Synapse homeserver control interface"],["TLDR","**Start Synapse server**\n\n```synctl start```\n\n**Stop Synapse server**\n\n```synctl stop```\n\n**Restart Synapse server**\n\n```synctl restart```\n\n**Start with specific config file**\n\n```synctl start [/path/to/homeserver.yaml]```\n\n**Start a specific worker**\n\n```synctl start -w [/path/to/worker.yaml]```\n\n**Restart all workers and main process**\n\n```synctl restart -a [/path/to/workers/]```"],["SYNOPSIS","**synctl** {start|stop|restart} [_configfile_] [_options_]"],["PARAMETERS","**start**\n> Start Synapse server or worker.\n\n**stop**\n> Stop Synapse server or worker.\n\n**restart**\n> Restart Synapse server or worker.\n\n_configfile_\n> Path to configuration file (default: homeserver.yaml).\n\n**-w**, **--worker** _file_\n> Operate on a single worker using specified config file.\n\n**-a**, **--all-processes** _dir_\n> Operate on all workers in directory and main process."],["DESCRIPTION","**synctl** is the control interface for Matrix Synapse, the reference homeserver implementation for the Matrix communication protocol. It manages starting, stopping, and restarting the Synapse server and its worker processes.\n\nFor single-instance deployments, synctl operates on the main homeserver process. For scaled deployments using workers, the -a option manages all worker processes defined in a configuration directory.\n\nConfiguration files use YAML format and define server settings, database connections, federation parameters, and resource limits."],["ENVIRONMENT","**SYNAPSE_CACHE_FACTOR**\n: Controls memory cache size. Default is 0.5. Decrease to reduce RAM usage or increase for better performance."],["CAVEATS","The configuration file must exist and be valid YAML. When using workers, each worker needs its own configuration file. Memory usage can be significant; tune SYNAPSE_CACHE_FACTOR accordingly. Requires Python 3 and dependencies installed."],["HISTORY","**Synapse** is developed by Element (formerly New Vector) as the reference implementation of the Matrix protocol. Matrix is an open standard for decentralized, real-time communication. Synapse was first released in 2014 and remains the most widely deployed Matrix homeserver."],["INSTALL","```dnf: sudo dnf install synapse```\n\n```pacman: sudo pacman -S synapse```\n\n```apk: sudo apk add synapse```\n\n```nix: nix profile install nixpkgs#synapse```"],["SEE ALSO","[register_new_matrix_user](/man/register_new_matrix_user)(1), [systemctl](/man/systemctl)(1)"]],"e":["register_new_matrix_user","systemctl"],"x":"synctl start"},"systemctl-reload-or-try-restart":{"n":"systemctl-reload-or-try-restart","t":"Alias for try-reload-or-restart","s":[["TAGLINE","Alias for try-reload-or-restart"],["TLDR","This command is an alias of [systemctl try-reload-or-restart](/man/systemctl-try-reload-or-restart)."],["SEE ALSO","[systemctl-try-reload-or-restart](/man/systemctl-try-reload-or-restart)(1), [systemctl](/man/systemctl)(1)"]],"e":["systemctl-try-reload-or-restart","systemctl"],"x":""},"tailscale-file":{"n":"tailscale-file","t":"Transfer files between Tailscale devices","s":[["TAGLINE","Transfer files between Tailscale devices"],["TLDR","**Send file to device**\n\n```tailscale file cp [file] [device]:```\n\n**Receive files**\n\n```tailscale file get [directory]```\n\n**List pending files**\n\n```tailscale file get --wait=false .```"],["SYNOPSIS","**tailscale** **file** _command_ [_options_]"],["PARAMETERS","**cp** _file_ _target_\n> Send file to device.\n\n**get** _directory_\n> Receive pending files.\n\n**--wait**\n> Wait for files."],["DESCRIPTION","**tailscale file** transfers files between devices on your Tailscale network using the Taildrop feature. Files are sent directly from one device to another over encrypted WireGuard connections, without passing through cloud storage or third-party servers.\n\nThe **cp** subcommand sends files to a target device, specified by its Tailscale hostname followed by a colon. The **get** subcommand receives pending files into a local directory. By default, the receiver waits for incoming transfers; the **--wait=false** flag lists pending files without blocking. Transfers work across platforms including Linux, macOS, Windows, iOS, and Android."],["INSTALL","```dnf: sudo dnf install tailscale```\n\n```pacman: sudo pacman -S tailscale```\n\n```apk: sudo apk add tailscale```\n\n```zypper: sudo zypper install tailscale```\n\n```brew: brew install tailscale```\n\n```nix: nix profile install nixpkgs#tailscale```"],["SEE ALSO","[tailscale](/man/tailscale)(1)"]],"e":["tailscale"],"x":"tailscale file cp [file] [device]:"},"tracexec":{"n":"tracexec","t":"Tracer for execve syscalls and pre-exec behavior","s":[["TAGLINE","Tracer for execve syscalls and pre-exec behavior"],["TLDR","**Trace all exec calls made by bash**\n\n```tracexec log -- bash```\n\n**Launch TUI mode to trace a build system**\n\n```tracexec tui -- make```\n\n**TUI mode with pseudo terminal**\n\n```tracexec tui -t -- bash```"],["SYNOPSIS","**tracexec** [_options_] _command_"],["DESCRIPTION","**tracexec** helps you figure out what and how programs get executed when you run a command. It is useful for debugging build systems, understanding shell scripts, and tracing what programs proprietary software runs. In TUI mode with a pseudo terminal, you can view exec event details and interact with processes."],["HISTORY","**tracexec** was created by **Levi Zim** (kxxt) and is written in **Rust**."],["INSTALL","```pacman: sudo pacman -S tracexec```\n\n```nix: nix profile install nixpkgs#tracexec```"],["SEE ALSO","[strace](/man/strace)(1), [ltrace](/man/ltrace)(1)"]],"e":["strace","ltrace"],"x":"tracexec log -- bash"},"ttyplot":{"n":"ttyplot","t":"Real-time terminal plotting from stdin","s":[["TAGLINE","Real-time terminal plotting from stdin"],["TLDR","**Plot** values from stdin\n\n```printf \"1\\n2\\n3\\n\" | ttyplot```\n\nSet **title and unit**\n\n```printf \"10\\n20\\n30\\n\" | ttyplot -t \"Title\" -u \"ms\"```\n\n**Continuously** plot random values\n\n```while true; do echo $RANDOM; sleep 1; done | ttyplot```\n\n**Plot ping** latency\n\n```ping 8.8.8.8 | sed -u 's/^.*time=//g; s/ ms//g' | ttyplot -t \"Ping\" -u ms```\n\n**Monitor CPU usage** in real time\n\n```while true; do grep 'cpu ' /proc/stat | awk '{u=$2+$4; t=$2+$4+$5; printf \"%.1f\\n\", u/t*100}'; sleep 1; done | ttyplot -t \"CPU %\" -u \"%\"```\n\nPlot with **two values** (dual plot)\n\n```command | ttyplot -2```"],["SYNOPSIS","**ttyplot** [_options_]"],["DESCRIPTION","**ttyplot** is a real-time terminal plotting utility that reads numeric values from stdin and displays them as a scrolling graph in the terminal. It is useful for visualizing streaming data like network latency, CPU usage, or sensor readings."],["PARAMETERS","**-t TITLE**\n> Set the plot title\n\n**-u UNIT**\n> Set the unit label for values\n\n**-s SCALE**\n> Set the vertical scale (max value)\n\n**-m MIN**\n> Set the minimum value\n\n**-2**\n> Enable dual/two value plotting mode\n\n**-c COLOR**\n> Set the plot color\n\n**-e**\n> Exit when stdin closes\n\n**-r**\n> Rate mode (values per second)\n\n**-w WIDTH**\n> Set the plot width in characters"],["CAVEATS","Input must be numeric values, one per line. Use sed or awk to extract numeric values from command output. The plot resizes automatically with terminal size changes."],["HISTORY","**ttyplot** was created as a simple way to visualize streaming numeric data directly in the terminal without requiring a graphical environment."],["INSTALL","```apt: sudo apt install ttyplot```\n\n```dnf: sudo dnf install ttyplot```\n\n```apk: sudo apk add ttyplot```\n\n```zypper: sudo zypper install ttyplot```\n\n```brew: brew install ttyplot```\n\n```nix: nix profile install nixpkgs#ttyplot```"],["SEE ALSO","[gnuplot](/man/gnuplot)(1), [termgraph](/man/termgraph)(1), [sed](/man/sed)(1), [awk](/man/awk)(1)"]],"e":["gnuplot","termgraph","sed","awk"],"x":"printf \"1\\n2\\n3\\n\" | ttyplot"},"typos":{"n":"typos","t":"Low false-positive source code spell checker","s":[["TAGLINE","Low false-positive source code spell checker"],["TLDR","**Check** the current directory for misspellings\n\n```typos```\n\n**Check a specific** file or directory\n\n```typos [path/to/file_or_dir]```\n\n**Automatically fix** detected typos in place\n\n```typos --write-changes```\n\n**Preview** suggested fixes as a unified diff\n\n```typos --diff```\n\n**Output results as JSON** for tooling integration\n\n```typos --format json```\n\n**Include hidden files** and ignored paths\n\n```typos --hidden --no-ignore```\n\n**Use a specific** English locale\n\n```typos --locale en-gb```\n\n**Check** using a custom configuration file\n\n```typos --config [path/to/_typos.toml]```"],["SYNOPSIS","**typos** [_OPTIONS_] [_PATH_...]"],["PARAMETERS","**-w**, **--write-changes**\n> Apply the suggested corrections to files instead of only reporting them.\n\n**--diff**\n> Print proposed changes as a unified diff without modifying files.\n\n**--format** _FORMAT_\n> Output format: _silent_, _brief_, _long_ (default), _json_.\n\n**--config** _FILE_\n> Path to a configuration file (typically _\\_typos.toml_).\n\n**--locale** _LOCALE_\n> Set the English dialect: _en_, _en-us_, _en-gb_, _en-ca_, _en-au_.\n\n**--exclude** _GLOB_\n> Skip paths matching the given gitignore-style pattern.\n\n**--force-exclude**\n> Force excluded paths to be skipped even when given explicitly on the command line.\n\n**--hidden**\n> Include hidden files and directories.\n\n**--no-ignore**\n> Do not respect any ignore files.\n\n**--no-ignore-vcs**\n> Do not respect ignore files inside version-control directories.\n\n**--no-ignore-dot**\n> Do not respect _.ignore_ files.\n\n**--no-ignore-global**\n> Do not respect global ignore files.\n\n**--no-ignore-parent**\n> Do not respect ignore files from parent directories.\n\n**--binary**\n> Also check binary files as if they were text.\n\n**--no-unicode**\n> Restrict identifiers to ASCII.\n\n**--type-list**\n> Print all known file types and their globs.\n\n**--type** _TYPE_\n> Only check files of the given type (e.g. _rust_, _py_).\n\n**--type-not** _TYPE_\n> Exclude files of the given type.\n\n**-V**, **--version**\n> Print the version and exit.\n\n**-h**, **--help**\n> Print help."],["DESCRIPTION","**typos** is a fast spell checker tailored for source code. It walks the file tree, respects _.gitignore_ rules by default, and flags misspelled words inside identifiers, comments, and strings while keeping false positives low enough to run in pull-request gates.\n\nDetection is driven by a curated dictionary of common code typos rather than a full natural-language dictionary, so it tolerates camelCase, snake_case, abbreviations, and domain-specific jargon. Findings can be printed, written back to disk with **--write-changes**, or emitted as JSON for editors and CI tooling.\n\nThe checker also understands per-language conventions through a file-type map, can be tuned for British or American English with **--locale**, and respects per-project overrides defined in **\\_typos.toml**, **typos.toml**, **Cargo.toml**, or **pyproject.toml**."],["CONFIGURATION","Project configuration lives in **\\_typos.toml** (or a **[tool.typos]** table inside **pyproject.toml** / **Cargo.toml**). Common sections:\n\n```\n[default]\nlocale = \"en-us\"\nextend-ignore-re = [\"(?Rm)^.*#\\\\s*spellchecker:disable-line$\"]\n\n[default.extend-words]\nmater = \"mater\"\n\n[default.extend-identifiers]\nHashMa = \"HashMap\"\n\n[type.rust]\nextend-glob = [\"*.rs\"]\ncheck-file = true\n```\n\nThe **[default.extend-words]** and **[default.extend-identifiers]** tables either remap typos to their correct spelling or mark a word/identifier as valid by mapping it to itself."],["CAVEATS","**typos** is not a general English spell checker: it only flags words from its built-in typo dictionary, so it will miss many real misspellings outside that list. Conversely, project-specific names, acronyms, and non-English words may still trip it up and require entries in **\\_typos.toml**. Writing changes with **--write-changes** modifies files in place: review with **--diff** first, especially when the dictionary is unfamiliar."],["HISTORY","**typos** was created by **Ed Page** (crate-ci) and released as an open-source project in **2019**, written in **Rust**. It was designed to be fast and accurate enough to run on every commit in large monorepos, drawing inspiration from earlier code-oriented spell checkers like **misspell** and **scspell** while focusing on a curated, low false-positive corpus."],["INSTALL","```pacman: sudo pacman -S typos```\n\n```apk: sudo apk add typos```\n\n```nix: nix profile install nixpkgs#typos```"],["SEE ALSO","[aspell](/man/aspell)(1), [hunspell](/man/hunspell)(1), [codespell](/man/codespell)(1)"]],"e":["aspell","hunspell","codespell"],"x":"typos"},"virt-install":{"n":"virt-install","t":"Create and install libvirt virtual machines","s":[["TAGLINE","Create and install libvirt virtual machines"],["TLDR","**Create** a VM with 1GB RAM, 12GB storage, and start Debian installation\n\n```virt-install -n [vm_name] --memory [1024] --vcpus [2] --disk path=[path/to/image.qcow2],size=[12] --cdrom [path/to/debian.iso] --osinfo [debian12]```\n\n**Create** a UEFI-based VM with Q35 chipset and 4GB RAM\n\n```virt-install -n [vm_name] --arch x86_64 --virt-type kvm --machine q35 --boot uefi --memory [4096] --vcpus [4] --disk path=[path/to/image.raw],size=[16] --cdrom [path/to/fedora.iso] --osinfo [fedora40]```\n\n**Import** an existing disk image as a VM (no installation)\n\n```virt-install -n [vm_name] --memory [2048] --vcpus [2] --disk path=[path/to/image.qcow2] --import --osinfo [detect=on]```\n\n**Create** a VM with specific CPU topology and host-model CPU\n\n```virt-install -n [vm_name] --cpu host-model,topology.sockets=1,topology.cores=4,topology.threads=2 --memory [16384] --disk path=[path/to/image.qcow2],size=[250] --cdrom [path/to/os.iso]```\n\n**Perform** an automated kickstart installation from a remote tree\n\n```virt-install -n [vm_name] --memory [2048] --disk path=[path/to/image.qcow2],size=[20] --location [https://download.fedoraproject.org/...] --extra-args \"inst.ks=[https://path/to/kickstart]\"```\n\n**Boot** a VM via PXE with a bridged network\n\n```virt-install -n [vm_name] --memory [2048] --disk path=[path/to/image.qcow2],size=[20] --network bridge=[br0] --pxe --osinfo [linux2022]```"],["SYNOPSIS","**virt-install** [_options_]"],["PARAMETERS","**-n**, **--name** _NAME_\n> Unique name for the new virtual machine.\n\n**--memory** _SIZE_\n> Guest memory in MiB (or use suboptions like memory=2048,currentMemory=1024).\n\n**--vcpus** _N_\n> Number of virtual CPUs; supports suboptions for sockets/cores/threads and hotplug limits.\n\n**--cpu** _MODEL_\n> CPU model and features (e.g. host-model, host-passthrough, topology.sockets=...).\n\n**--disk** _SPEC_\n> Storage specification (path=, size=, format=, bus=, cache=...). Use `--disk none` for diskless VMs.\n\n**-c**, **--cdrom** _PATH_\n> ISO file or CDROM device used as installation media.\n\n**-l**, **--location** _URL_|_PATH_\n> Installation tree location (HTTP/FTP/NFS URL or local directory) for network installs.\n\n**--pxe**\n> Boot from the network (PXE) for installation.\n\n**--import**\n> Skip OS installation and build the guest around an existing disk image.\n\n**--boot** _OPTIONS_\n> Post-install boot configuration (uefi, bios, boot order, loader paths).\n\n**--osinfo** _ID_\n> Optimize the guest for a specific OS (libosinfo short-id, e.g. fedora40, debian12, win11). Replaces legacy --os-variant.\n\n**--network** _SPEC_\n> Network interface (e.g. bridge=br0, network=default, type=direct). May be repeated.\n\n**--graphics** _TYPE_\n> Graphical display: vnc, spice, or none.\n\n**--virt-type** _TYPE_\n> Hypervisor type (kvm, qemu, xen).\n\n**--machine** _TYPE_\n> Machine type (q35, pc, etc.).\n\n**-x**, **--extra-args** _ARGS_\n> Extra kernel command-line arguments passed to --location installs.\n\n**--unattended**\n> Perform an automated installation using libosinfo install scripts.\n\n**--autoconsole** _TYPE_\n> Console auto-connect behavior (graphical, text, none). Replaces --noautoconsole.\n\n**--noreboot**\n> Do not reboot the guest automatically after the installer completes.\n\n**--wait** _MINUTES_\n> How long to wait for installation to complete (-1 = wait forever, 0 = don't wait).\n\n**--dry-run**\n> Validate options and show the generated XML without creating the VM."],["DESCRIPTION","**virt-install** creates new virtual machines using libvirt. It provisions VM configuration, storage, and initiates OS installation from various sources including ISO images, network locations, or PXE boot.\n\nThe tool supports advanced configurations including UEFI boot, CPU pinning, NUMA topology, and various device passthrough options. It integrates with QEMU/KVM, Xen, and other libvirt-supported hypervisors."],["CAVEATS","Requires libvirt daemon running and appropriate virtualization backend. KVM requires hardware virtualization support. UEFI boot requires OVMF firmware package. Network installations require connectivity to installation sources."],["INSTALL","```dnf: sudo dnf install virt-install```\n\n```pacman: sudo pacman -S virt-install```\n\n```apk: sudo apk add virt-install```\n\n```zypper: sudo zypper install virt-install```"],["SEE ALSO","[virt-clone](/man/virt-clone)(1), [virsh](/man/virsh)(1), [virt-manager](/man/virt-manager)(1), [qemu-img](/man/qemu-img)(1)"]],"e":["virt-clone","virsh","virt-manager","qemu-img"],"x":"virt-install -n [vm_name] --memory [1024] --vcpus [2] --disk path=[path/to/image.qcow2],size=[12] --cdrom [path/to/debian.iso] --osinfo [debian12]"}});
