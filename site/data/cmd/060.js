window.LCL_BUCKET(60,{"aa-logprof":{"n":"aa-logprof","t":"Update AppArmor profiles from log events interactively","s":[["TAGLINE","Update AppArmor profiles from log events interactively"],["TLDR","Interactively **review and update** profiles based on system logs\n\n```sudo aa-logprof```\n\nUse a **specific directory** for AppArmor profiles\n\n```sudo aa-logprof -d /path/to/profiles```\n\nUse a **specific log file** instead of the default\n\n```sudo aa-logprof -f /path/to/logfile```\n\nIgnore all log entries **before a mark**\n\n```sudo aa-logprof -m \"log_marker_text\"```"],["SYNOPSIS","**aa-logprof** [_-d /path/to/profiles_] [_-f /path/to/logfile_] [_-m mark_]"],["DESCRIPTION","**aa-logprof** is an interactive utility that scans AppArmor security logs and prompts users to review and update existing security profiles. When launched, it identifies new AppArmor events not covered by current profiles and suggests modifications.\n\nUpon exit, updated profiles are saved and reloaded if AppArmor is active. Interactive responses include: **(A)llow**, **(D)eny**, **(I)gnore**, **(N)ew**, **(G)lob last piece**, **(Q)uit**."],["PARAMETERS","**-d, --dir /path/to/profiles**\n> Specifies where to look for the AppArmor security profile set; defaults to /etc/apparmor.d\n\n**-f, --file /path/to/logfile**\n> Specifies the location of the logfile; defaults are read from /etc/apparmor/logprof.conf\n\n**-m, --logmark \"mark\"**\n> Filters out log entries preceding a specified mark; use quotes if mark contains spaces\n\n**-h, --help**\n> Display help information"],["CONFIGURATION","**/etc/apparmor/logprof.conf**\n> Controls default logfile location, repository settings, and behavior options for log-based profile updates."],["CAVEATS","Log analysis depends on audit daemon configuration. Ensure **auditd** or **klogd** is properly configured to capture AppArmor events."],["HISTORY","Part of the **AppArmor** utilities package for managing application security profiles on Linux systems."],["INSTALL","```apk: sudo apk add apparmor-utils```\n\n```zypper: sudo zypper install apparmor-utils```\n\n```nix: nix profile install nixpkgs#apparmor-utils```"],["SEE ALSO","[aa-genprof](/man/aa-genprof)(8), [aa-cleanprof](/man/aa-cleanprof)(8), [aa-enforce](/man/aa-enforce)(8), [aa-complain](/man/aa-complain)(8), [auditd](/man/auditd)(8), [apparmor](/man/apparmor)(7)"],["RESOURCES","```[Source code](https://gitlab.com/apparmor/apparmor)```\n\n```[Homepage](https://apparmor.net/)```\n\n```[Documentation](https://gitlab.com/apparmor/apparmor/-/wikis/home)```"]],"e":["aa-genprof","aa-cleanprof","aa-enforce","aa-complain","auditd","apparmor"],"x":"sudo aa-logprof"},"adb-install":{"n":"adb-install","t":"Android application installer","s":[["TAGLINE","Android application installer"],["TLDR","**Install** an APK\n\n```adb install [app.apk]```\n\n**Reinstall** existing application, keeping its data\n\n```adb install -r [app.apk]```\n\nAllow installation of a **test APK**\n\n```adb install -t [app.apk]```\n\nInstall allowing **version downgrade**\n\n```adb install -d [app.apk]```\n\nInstall **granting all permissions**\n\n```adb install -g [app.apk]```"],["SYNOPSIS","**adb install** [_-rtdgf_] [_options_] _file_"],["DESCRIPTION","**adb install** pushes an Android application package (APK) to a connected device and installs it. The APK is copied to the device's temporary storage, installed by the package manager, and then the temporary file is removed.\n\nThe command provides various options to control installation behavior, including replacing existing apps, downgrading versions, and automatically granting runtime permissions."],["PARAMETERS","**-r**\n> Reinstall an existing app, keeping its data\n\n**-t**\n> Allow installation of test APKs\n\n**-d**\n> Allow version code downgrade\n\n**-g**\n> Grant all runtime permissions listed in the manifest\n\n**-f**\n> Install package on the internal system memory\n\n**-i** _installer_package_name_\n> Specify the installer package name\n\n**--user** _user_id_\n> Install the package for a specific user (default: all users)\n\n**--install-location** _location_\n> Set install location: 0 (default), 1 (internal), 2 (external media)\n\n**--fastdeploy**\n> Update only the changed parts of an already installed package\n\n**--incremental**\n> Stream the APK in the background; launch before fully installed (requires APK Signature Scheme v4). Append **--wait** to block until done, or use **--no-incremental** to disable"],["CAVEATS","Installation may fail if the APK is signed with a different key than the existing installation (requires uninstall first). Some devices restrict installation from unknown sources. Downgrading an installed version requires the **-d** flag. To install an app split into multiple APKs, use **adb install-multiple** instead."],["HISTORY","**adb install** has been available since the initial Android SDK release in **2008**. Options have expanded over time to support split APKs, instant apps, and Android App Bundles."],["INSTALL","```apt: sudo apt install adb```\n\n```dnf: sudo dnf install android-tools```\n\n```pacman: sudo pacman -S android-tools```\n\n```apk: sudo apk add android-tools-adb```\n\n```zypper: sudo zypper install android-tools```\n\n```nix: nix profile install nixpkgs#android-tools```"],["SEE ALSO","[adb](/man/adb)(1), [adb-uninstall](/man/adb-uninstall)(1), [adb-shell-pm](/man/adb-shell-pm)(1), [aapt](/man/aapt)(1), [apksigner](/man/apksigner)(1)"],["RESOURCES","```[Source code](https://android.googlesource.com/platform/packages/modules/adb/)```\n\n```[Documentation](https://developer.android.com/tools/adb)```"]],"e":["adb","adb-uninstall","adb-shell-pm","aapt","apksigner"],"x":"adb install [app.apk]"},"aspell-import":{"n":"aspell-import","t":"Import old personal dictionaries into GNU Aspell","s":[["TAGLINE","Import old personal dictionaries into GNU Aspell"],["TLDR","**Import** old personal dictionaries (Ispell and Aspell) automatically\n\n```aspell-import```\n\n**Invoke explicitly** through Perl if /usr/bin/perl is missing\n\n```perl $(which aspell-import)```"],["SYNOPSIS","**aspell-import**"],["DESCRIPTION","**aspell-import** is a Perl script that searches for old personal dictionaries from **Ispell** and earlier versions of **Aspell**, then imports them into the current user's GNU Aspell personal word list. It is intended to be run once after installing Aspell or upgrading from Ispell.\n\nThe script scans the user's home directory and standard dictionary locations, converts each entry, and writes a header line so the result is a valid Aspell personal dictionary."],["PARAMETERS","This command takes no command-line options. Run it without arguments."],["CAVEATS","The script must be able to find a working Perl interpreter at `/usr/bin/perl`. If Perl is installed elsewhere, invoke the script with `perl /path/to/aspell-import`. Existing Aspell personal dictionaries may be overwritten, so back them up first. Ispell affix rules are not migrated, only word lists."],["HISTORY","**aspell-import** ships with **GNU Aspell** and was added so users could migrate accumulated personal word lists when moving from Ispell to Aspell."],["INSTALL","```apt: sudo apt install aspell```\n\n```dnf: sudo dnf install aspell```\n\n```pacman: sudo pacman -S aspell```\n\n```apk: sudo apk add aspell-utils```\n\n```zypper: sudo zypper install aspell```\n\n```brew: brew install aspell```\n\n```nix: nix profile install nixpkgs#aspell```"],["SEE ALSO","[aspell](/man/aspell)(1), [ispell](/man/ispell)(1)"]],"e":["aspell","ispell"],"x":"aspell-import"},"at":{"n":"at","t":"Schedule commands to execute at a specified time","s":[["TAGLINE","Schedule commands to execute at a specified time"],["TLDR","Create commands interactively and execute them in **5 minutes**\n\n```at now + 5 minutes```\n\nExecute commands from stdin at a **specific time**\n\n```echo \"command\" | at 1000```\n\nExecute commands from a **file** at a specific time\n\n```at -f [path/to/file] 9:30 PM Tue```\n\n**List** all queued jobs\n\n```at -l```\n\n**View** a specified job\n\n```at -c [job_number]```\n\n**Remove** a job from the queue\n\n```atrm [job_number]```"],["SYNOPSIS","**at** [_-q queue_] [_-f file_] [_-mMlv_] _timespec_\n\n**at** [_-q queue_] [_-f file_] [_-mMkv_] [_-t time_]\n\n**at** _-c_ _job_ ...\n\n**at** [_-rd_] _job_ ..."],["DESCRIPTION","**at** executes commands at a specified time. Commands are read from standard input or a file and executed later. Results are sent to the user's mail. The **atd** daemon must be running for jobs to execute."],["PARAMETERS","**-f file**\n> Read commands from file instead of standard input\n\n**-l**\n> List all queued jobs (alias for atq)\n\n**-c job**\n> Print (cat) the commands of the specified job to standard output\n\n**-m**\n> Send mail to the user when the job completes, even if there was no output\n\n**-M**\n> Never send mail to the user\n\n**-q queue**\n> Use the specified queue. A queue is a single letter (a-z, A-Z); queues with higher letters run with increased niceness. The \"=\" queue is reserved for currently running jobs.\n\n**-t time**\n> Run the job at the given time, in the format [[CC]YY]MMDDhhmm[.ss]\n\n**-r job**, **-d job**\n> Delete the specified job (alias for atrm)\n\n**-b**\n> Alias for batch\n\n**-v**\n> Show the time the job will be executed before reading it"],["CONFIGURATION","**/etc/at.allow**\n> If this file exists, only users listed in it can schedule jobs with at.\n\n**/etc/at.deny**\n> Users listed in this file are denied access to at. Ignored if at.allow exists."],["CAVEATS","Requires the **atd** daemon to be running. Time can be specified in various formats: HH:MM, midnight, noon, teatime (4pm), or relative times like \"now + 5 minutes\"."],["HISTORY","Part of the traditional Unix job scheduling system, available since early Unix in the **1970s**. The modern Linux **at** package was mostly written by Thomas Koenig and has since been maintained by various Debian contributors."],["INSTALL","```apt: sudo apt install at```\n\n```dnf: sudo dnf install at```\n\n```pacman: sudo pacman -S at```\n\n```apk: sudo apk add at```\n\n```zypper: sudo zypper install at```\n\n```nix: nix profile install nixpkgs#at```"],["SEE ALSO","[atq](/man/atq)(1), [atrm](/man/atrm)(1), [cron](/man/cron)(8), [batch](/man/batch)(1)"],["RESOURCES","```[Source code](https://salsa.debian.org/debian/at)```"]],"e":["atq","atrm","cron","batch"],"x":"at now + 5 minutes"},"autokey":{"n":"autokey","t":"Desktop text expansion and automation utility","s":[["TAGLINE","Desktop text expansion and automation utility"],["TLDR","**Start AutoKey** with GTK interface\n\n```autokey-gtk```\n\n**Start AutoKey** with Qt interface\n\n```autokey-qt```\n\n**Start with verbose logging**\n\n```autokey-gtk --verbose```\n\n**Show configuration** window on startup\n\n```autokey-gtk --configure```\n\n**Log mouse button events** too\n\n```autokey-gtk --mouse```"],["SYNOPSIS","**autokey-gtk** [_options_]\n\n**autokey-qt** [_options_]"],["PARAMETERS","**-c**, **--configure**\n> Show the configuration window on startup, even if not the first run.\n\n**-l**, **--verbose**\n> Enable verbose (debug) logging.\n\n**-m**, **--mouse**\n> Like --verbose but also logs mouse button events.\n\n**-V**, **--version**\n> Display version information and exit.\n\n**-h**, **--help**\n> Display help message and exit."],["DESCRIPTION","**AutoKey** is a desktop automation utility for Linux that provides text expansion and hotkey-triggered automation. It supports both simple phrase replacement and complex Python scripting for advanced automation tasks.\n\n**Phrases** are text snippets that expand when you type an abbreviation. For example, typing \"addr\" could expand to your full address. Phrases support macros for dynamic content like dates, clipboard contents, or command output.\n\n**Scripts** use Python 3 for advanced automation including mouse clicks, keyboard simulation, window manipulation, and system commands. Scripts are triggered by abbreviations or hotkeys just like phrases.\n\nAutoKey comes in two variants: **autokey-gtk** for GTK-based desktops (GNOME, XFCE, MATE) and **autokey-qt** for Qt-based desktops (KDE Plasma). Both share the same configuration and scripting capabilities."],["CONFIGURATION","**~/.config/autokey/**\n> Configuration directory containing phrases, scripts, and application settings."],["CAVEATS","Requires X11; Wayland is not currently supported. Needs a system tray or appindicator; GNOME Shell users may need the AppIndicator Support extension. Must be running in the background for automation to work. Phrase triggers must be unique to avoid conflicts."],["HISTORY","**AutoKey** was originally created by **Chris Olsen** around **2008** as a Linux equivalent to text expansion tools available on other platforms. The project has been community-maintained since 2018 under the autokey GitHub organization. It provides TextExpander-like functionality for Linux users who need text expansion and keyboard automation."],["INSTALL","```nix: nix profile install nixpkgs#autokey```"],["SEE ALSO","[xdotool](/man/xdotool)(1), [xclip](/man/xclip)(1), [espanso](/man/espanso)(1)"],["RESOURCES","```[Source code](https://github.com/autokey/autokey)```\n\n```[Documentation](https://autokey.github.io/)```"]],"e":["xdotool","xclip","espanso"],"x":"autokey-gtk"},"avo":{"n":"avo","t":"CLI for the Avo analytics tracking plan platform","s":[["TAGLINE","CLI for the Avo analytics tracking plan platform"],["TLDR","**Initialize** an Avo workspace in the current folder\n\n```avo init```\n\n**Pull** analytics wrappers from your Avo workspace\n\n```avo pull```\n\n**Pull** a specific source\n\n```avo pull [source_name]```\n\n**Check implementation status**\n\n```avo status```\n\n**Switch** to a different Avo branch\n\n```avo checkout [branch]```\n\n**Merge** the Avo main branch into your current branch\n\n```avo merge main```\n\n**Log into** the Avo platform\n\n```avo login```"],["SYNOPSIS","**avo** _command_ [_options_]"],["PARAMETERS","**init**\n> Initialize an Avo workspace in the current folder.\n\n**pull** [_source_]\n> Pull analytics wrappers from your Avo workspace.\n\n**checkout** _branch_\n> Switch to a different Avo branch.\n\n**source** _command_\n> Manage sources for the current project.\n\n**status** [_source_]\n> Show the status of the Avo implementation.\n\n**merge main**\n> Pull the Avo main branch into your current branch.\n\n**conflict**\n> Resolve git conflicts in Avo files.\n\n**edit**\n> Open the Avo workspace in the browser.\n\n**login**\n> Log into the Avo platform.\n\n**logout**\n> Log out from the Avo platform.\n\n**whoami**\n> Show the currently logged in username.\n\n**-v**, **--verbose**\n> Make output more verbose.\n\n**-f**, **--force**\n> Proceed with merge when incoming branch is open.\n\n**--version**\n> Show version number.\n\n**--help**\n> Show help."],["DESCRIPTION","**avo** is the command-line interface for the Avo analytics tracking plan platform. It lets you pull type-safe analytics wrappers (codegen) from your Avo workspace directly into your project, ensuring analytics implementations match your tracking plan.\n\nThe main workflow is running `avo pull` to fetch the latest generated analytics code based on your tracking plan defined at avo.app. On first run, it prompts you to select which Avo source to generate code for and where to save it. This configuration is stored in the **avo.json** file."],["CAVEATS","Requires Node.js >= 14.16. Must be authenticated via `avo login` before pulling. Configuration is stored in avo.json in the project root."],["SEE ALSO","[npm](/man/npm)(1), [node](/man/node)(1)"]],"e":["npm","node"],"x":"avo init"},"aws-acm":{"n":"aws-acm","t":"Provision and manage SSL/TLS certificates","s":[["TAGLINE","Provision and manage SSL/TLS certificates"],["TLDR","**Request** a new SSL/TLS certificate\n\n```aws acm request-certificate --domain-name [example.com] --validation-method DNS```\n\n**List** all certificates in your account\n\n```aws acm list-certificates```\n\n**Describe** a specific certificate in detail\n\n```aws acm describe-certificate --certificate-arn [arn]```\n\n**Import** a certificate into ACM\n\n```aws acm import-certificate --certificate [file://cert.pem] --private-key [file://key.pem]```\n\n**Export** a private certificate and its private key\n\n```aws acm export-certificate --certificate-arn [arn] --passphrase [file://pass.txt]```\n\n**Delete** a certificate\n\n```aws acm delete-certificate --certificate-arn [arn]```"],["SYNOPSIS","**aws acm** _command_ [_options_]"],["DESCRIPTION","**AWS Certificate Manager (ACM)** is a service for provisioning, managing, deploying, and renewing SSL/TLS certificates for AWS-based websites and applications. It simplifies certificate lifecycle management by handling complex tasks like certificate creation, validation, renewal, and deployment.\n\nACM provides **public certificates** at no additional charge for use with AWS services like **Elastic Load Balancing**, **Amazon CloudFront**, **Amazon API Gateway**, and other integrated AWS services. It also supports **private certificates** for internal resources using AWS Certificate Manager Private Certificate Authority (PCA).\n\nThe service automates certificate renewal for ACM-issued certificates, eliminating manual renewal processes and reducing the risk of certificate expiration. Certificates can be validated through **DNS validation** (recommended) or **email validation**.\n\nACM integrates seamlessly with AWS services, allowing automatic deployment and renewal of certificates without manual intervention. Certificates managed by ACM cannot be downloaded but can be exported if they were imported or are private certificates."],["AVAILABLE COMMANDS","**add-tags-to-certificate**\n> Add metadata tags to an ACM certificate for organization and billing\n\n**delete-certificate**\n> Permanently delete a certificate (must not be in use)\n\n**describe-certificate**\n> Retrieve detailed information about a certificate including status, domain names, and validation details\n\n**export-certificate**\n> Export a private certificate and its private key (requires passphrase)\n\n**get-account-configuration**\n> Retrieve account-level ACM configuration settings\n\n**get-certificate**\n> Retrieve certificate and certificate chain in PEM format\n\n**import-certificate**\n> Import a third-party certificate into ACM for management\n\n**list-certificates**\n> List all certificates with optional filtering by status\n\n**list-tags-for-certificate**\n> List all tags attached to a specific certificate\n\n**put-account-configuration**\n> Configure account-level ACM settings like expiration events\n\n**remove-tags-from-certificate**\n> Remove metadata tags from a certificate\n\n**renew-certificate**\n> Manually renew an eligible certificate\n\n**request-certificate**\n> Request a new public SSL/TLS certificate from ACM\n\n**resend-validation-email**\n> Resend domain validation email for email-based validation\n\n**revoke-certificate**\n> Revoke a private certificate issued through ACM-PCA\n\n**search-certificates**\n> Search and filter certificates by domain, key type, or other criteria\n\n**update-certificate-options**\n> Update certificate configuration like certificate transparency logging\n\n**wait**\n> Wait for certificate state changes (issued, validated)"],["CAVEATS","Certificates managed by ACM cannot be exported unless they were imported or are private certificates. Certificate deletion is permanent and cannot be undone. Certificates must not be associated with any AWS resources before deletion. DNS validation requires the ability to modify DNS records for the domain."],["HISTORY","**AWS Certificate Manager** was launched in **January 2016** to simplify SSL/TLS certificate management on AWS. Initially supporting only Elastic Load Balancing, it expanded to CloudFront, API Gateway, and other services. Private CA support was added in **April 2018**, and DNS validation became available to streamline the validation process."],["INSTALL","```dnf: sudo dnf install aws```\n\n```pacman: sudo pacman -S aws-cli```\n\n```apk: sudo apk add aws-cli```\n\n```zypper: sudo zypper install aws-cli```"],["SEE ALSO","[aws-acm-pca](/man/aws-acm-pca)(1), [aws-elbv2](/man/aws-elbv2)(1), [aws-cloudfront](/man/aws-cloudfront)(1), [aws-apigateway](/man/aws-apigateway)(1), [aws](/man/aws)(1)"],["RESOURCES","```[Source code](https://github.com/aws/aws-cli)```\n\n```[Documentation](https://docs.aws.amazon.com/cli/latest/reference/acm/)```"]],"e":["aws-acm-pca","aws-elbv2","aws-cloudfront","aws-apigateway","aws"],"x":"aws acm request-certificate --domain-name [example.com] --validation-method DNS"},"barotrauma":{"n":"barotrauma","t":"Run a dedicated multiplayer game server.","s":[["TAGLINE","Run a dedicated multiplayer game server."],["TLDR","**Start** the dedicated server in the foreground\n\n```[path/to]/DedicatedServer```\n\n**Run on a custom port** (configured in serversettings.xml beforehand)\n\n```[path/to]/DedicatedServer -port [27015]```\n\n**Install or update** the server via SteamCMD\n\n```steamcmd +login anonymous +app_update [1026340] validate +quit```"],["SYNOPSIS","**DedicatedServer** [_options_]"],["DESCRIPTION","**DedicatedServer** is the headless game server distributed by **Undertow Games** for **Barotrauma**, a 2D co-op submarine survival game set on Jupiter's moon Europa. The binary listens for incoming player connections, runs the simulation, and acts as the authoritative host for round state.\n\nServer behaviour is driven almost entirely by **serversettings.xml** in the game directory: player slot count, password, allowed mods, game mode (campaign, sandbox, mission, PvP), and karma settings. Workshop content downloaded by the server lives under **WorkshopMods/**. The server can also be controlled at runtime through the in-game console (commands prefixed with **/**)."],["CONFIGURATION","**serversettings.xml**\n> Main server configuration file. Controls game settings, player slots, password, allowed mods, and most runtime parameters.\n\n**config_player.xml**\n> Server-side player configuration (admin list, banned IPs are persisted alongside).\n\n**servermessages.txt**\n> Optional rotating message-of-the-day file shown to connecting players."],["CAVEATS","Requires the Barotrauma dedicated server files (Steam app ID **1026340**). Settings must be authored before launch; the server does not currently expose a rich CLI. Internet-facing deployments need UDP port forwarding for the configured port (default **27015**) and may require Steam authentication for clients to join."],["HISTORY","**Barotrauma** was released for early access in **2019** and reached version **1.0** in **2023**. The dedicated server binary has shipped alongside the game client throughout the game's life and is freely redistributable through Steam."],["SEE ALSO","[steamcmd](/man/steamcmd)(1)"]],"e":["steamcmd"],"x":"[path/to]/DedicatedServer"},"bulk_extractor":{"n":"bulk_extractor","t":"High-performance digital forensics data extraction tool","s":[["TAGLINE","High-performance digital forensics data extraction tool"],["TLDR","**Extract data from disk image**\n\n```bulk_extractor -o [output_dir] [image.dd]```\n\n**Scan with multiple threads**\n\n```bulk_extractor -o [output_dir] -j [8] [image.dd]```\n\n**Enable specific scanner**\n\n```bulk_extractor -o [output_dir] -e [exif] [image.dd]```\n\n**Disable specific scanner**\n\n```bulk_extractor -o [output_dir] -x [email] [image.dd]```\n\n**Scan specific byte range**\n\n```bulk_extractor -o [output_dir] -Y [0-1000000000] [image.dd]```\n\n**Recursively scan directory**\n\n```bulk_extractor -o [output_dir] -R [directory]```\n\n**Search for specific pattern**\n\n```bulk_extractor -o [output_dir] -f \"[pattern]\" [image.dd]```\n\n**List available scanners**\n\n```bulk_extractor -H```"],["SYNOPSIS","**bulk_extractor** [_options_] _image_"],["DESCRIPTION","**bulk_extractor** is a high-performance digital forensics tool that scans disk images, files, or directories and extracts structured information without parsing file system structures. It extracts email addresses, credit card numbers, URLs, EXIF metadata, and other artifacts directly from raw data streams.\n\nThe tool processes data in parallel across multiple CPU cores, making it significantly faster than traditional forensic tools. It operates on raw bytes rather than file system metadata, allowing it to find data in unallocated space, slack space, and within compressed or encoded content. Each scanner plugin generates a separate feature file in the output directory, making results easy to filter and analyze."],["PARAMETERS","**-o** _directory_\n> Output directory (required)\n\n**-e** _scanner_\n> Enable specific scanner\n\n**-x** _scanner_\n> Disable specific scanner\n\n**-j** _threads_\n> Number of threads to use\n\n**-G** _bytes_\n> Page size (default: 16777216)\n\n**-M** _depth_\n> Maximum recursion depth (default: 7)\n\n**-R**\n> Recursively scan directory\n\n**-f** _pattern_\n> Search for specific pattern\n\n**-F** _file_\n> Read patterns from file\n\n**-Y** _start-end_\n> Scan specific byte range\n\n**-z** _pagestart_\n> Start processing at a specific page number\n\n**-Z**\n> Wipe output directory before starting\n\n**-q**\n> Quiet mode (no status output)\n\n**-H**\n> List available scanners with info"],["OUTPUT","Creates **report.xml** with Digital Forensics XML metadata about the run. Individual feature files contain extracted data types (emails.txt, ccn.txt, urls.txt, etc.)."],["CAVEATS","Output directory must not exist or use -Z to wipe. Processing large images requires significant disk space for output. Some scanners may produce false positives requiring manual review."],["HISTORY","**bulk_extractor** was created by **Simson Garfinkel** at the Naval Postgraduate School, first released around **2011**. It is widely used in law enforcement and incident response for forensic data extraction."],["INSTALL","```zypper: sudo zypper install bulk_extractor```\n\n```brew: brew install bulk_extractor```\n\n```nix: nix profile install nixpkgs#bulk_extractor```"],["SEE ALSO","[foremost](/man/foremost)(1), [scalpel](/man/scalpel)(1), [strings](/man/strings)(1), [photorec](/man/photorec)(1)"]],"e":["foremost","scalpel","strings","photorec"],"x":"bulk_extractor -o [output_dir] [image.dd]"},"chronyc":{"n":"chronyc","t":"Command-line interface for the chrony NTP daemon","s":[["TAGLINE","Command-line interface for the chrony NTP daemon"],["TLDR","**Check synchronization status**\n\n```chronyc tracking```\n\n**List time sources**\n\n```chronyc sources```\n\n**Verbose source list**\n\n```chronyc sources -v```\n\n**Source statistics**\n\n```chronyc sourcestats```\n\n**Force time sync**\n\n```chronyc makestep```\n\n**Add NTP server**\n\n```chronyc add server [pool.ntp.org]```\n\n**Show number of online/offline sources**\n\n```chronyc activity```\n\n**Connect to a remote chrony daemon**\n\n```chronyc -h [remote-host]```\n\n**Interactive mode**\n\n```chronyc```"],["SYNOPSIS","**chronyc** [_options_] [_command_]"],["DESCRIPTION","**chronyc** is the command-line interface for chronyd, the NTP daemon. Used to monitor synchronization status, manage time sources, and configure the daemon. Changes are not persistent across restarts."],["COMMANDS","**tracking**\n> Display system clock status and offset\n\n**sources**\n> List NTP sources\n\n**sourcestats**\n> Show source statistics\n\n**makestep**\n> Force immediate clock correction\n\n**activity**\n> Show online/offline source count\n\n**add server** _host_\n> Add NTP server\n\n**delete** _address_\n> Remove NTP source\n\n**burst** _good_/_max_\n> Start rapid polling\n\n**offline** / **online**\n> Set sources offline/online\n\n**ntpdata**\n> Display NTP data for sources"],["PARAMETERS","**-n**\n> Don't resolve hostnames\n\n**-N**\n> Print original hostnames as specified in config, not reverse DNS\n\n**-c**\n> CSV output format\n\n**-m**\n> Allow multiple commands on one line\n\n**-4**\n> Resolve hostnames to IPv4 addresses only\n\n**-6**\n> Resolve hostnames to IPv6 addresses only\n\n**-h** _host_\n> Connect to remote chronyd\n\n**-p** _port_\n> Specify port for remote chronyd connection"],["CONFIGURATION","**/etc/chrony.conf**\n> Permanent configuration for chronyd. Runtime changes via chronyc are lost on restart."],["CAVEATS","Requires chronyd running. Runtime changes lost on restart. Sub-microsecond accuracy possible with hardware timestamping."],["INSTALL","```apt: sudo apt install chrony```\n\n```dnf: sudo dnf install chrony```\n\n```pacman: sudo pacman -S chrony```\n\n```apk: sudo apk add chrony```\n\n```zypper: sudo zypper install chrony```\n\n```brew: brew install chrony```\n\n```nix: nix profile install nixpkgs#chrony```"],["SEE ALSO","[chronyd](/man/chronyd)(8), [timedatectl](/man/timedatectl)(1), [ntpq](/man/ntpq)(1), [ntpd](/man/ntpd)(8), [hwclock](/man/hwclock)(8), [date](/man/date)(1)"]],"e":["chronyd","timedatectl","ntpq","ntpd","hwclock","date"],"x":"chronyc tracking"},"chvt":{"n":"chvt","t":"Switch the foreground virtual terminal","s":[["TAGLINE","Switch the foreground virtual terminal"],["TLDR","Switch the console to **TTY number N** (requires root)\n\n```sudo chvt [N]```"],["SYNOPSIS","**chvt** _N_"],["DESCRIPTION","**chvt** changes the foreground virtual terminal to terminal N. Virtual terminals (VTs) are the text consoles accessible via Ctrl+Alt+F1 through F7 on most Linux systems.\n\nThis is equivalent to pressing Ctrl+Alt+FN but can be done programmatically or when keyboard shortcuts are unavailable."],["PARAMETERS","_N_\n> The virtual terminal number to switch to (typically 1-7)"],["CAVEATS","Requires root privileges. The target VT must exist. On systems with graphical desktop, VT7 or VT1 is typically the X/Wayland session. Available VTs depend on system configuration."],["INSTALL","```apt: sudo apt install kbd```\n\n```dnf: sudo dnf install kbd```\n\n```pacman: sudo pacman -S kbd```\n\n```apk: sudo apk add kbd```\n\n```zypper: sudo zypper install kbd```\n\n```nix: nix profile install nixpkgs#kbd```"],["SEE ALSO","[fgconsole](/man/fgconsole)(1), [deallocvt](/man/deallocvt)(1)"]],"e":["fgconsole","deallocvt"],"x":"sudo chvt [N]"},"countryfetch":{"n":"countryfetch","t":"Neofetch-like country information fetcher","s":[["TAGLINE","Neofetch-like country information fetcher"],["TLDR","**Display** information about your current country (auto-detected via IP)\n\n```countryfetch```\n\n**Display** information about a country using its 2-letter code\n\n```countryfetch [us]```\n\n**Display** information about multiple countries by name\n\n```countryfetch [UnitedStates] [UnitedKingdom]```\n\n**List** all available country names and codes\n\n```countryfetch --list-countries```\n\n**Display** information about every country\n\n```countryfetch --all-countries```\n\n**Show** only specific fields for a country\n\n```countryfetch [us] --select flag population capital```"],["SYNOPSIS","**countryfetch** [_OPTIONS_] [_COUNTRY_...]"],["PARAMETERS","**COUNTRY**\n> One or more country names or 2-letter ISO codes (case-insensitive). If omitted, auto-detects via public IP geolocation.\n\n**--all-countries**\n> Print information about all countries.\n\n**--list-countries**\n> Print a list of all available country names and codes.\n\n**--select** _FIELD..._\n> Select which fields to show: area, flag, emoji, continent, population, tlds, languages, currencies, neighbours, establishment-date, iso-code, driving-side, capital, dialing-code, palette, color.\n\n**--version**\n> Print version.\n\n**--help**\n> Print help."],["DESCRIPTION","**countryfetch** displays country information in a terminal layout similar to neofetch. It renders an ASCII-art version of the country's flag alongside key facts such as population, area, capital, languages, currencies, neighboring countries, top-level domains, dialing codes, driving side, establishment date, and ISO codes. Output text is colorized using the brightest color extracted from the country's flag. All country data is embedded at compile time, so no runtime API calls are needed for country information."],["CAVEATS","Country detection relies on a public IP lookup, so it will not work offline and may reflect VPN location rather than physical location. Country data is baked into the binary at compile time, so updating data requires a new release. Requires Rust/Cargo 1.85+ to build from source."],["HISTORY","**countryfetch** was created by **Nik Revenco** and first released in **March 2025**. Written in Rust under the MIT/Apache-2.0 dual license. The latest version is v0.2.0. Available in the official Arch Linux repositories, Homebrew, and via Cargo. Inspired by the \"fetch\" family of tools (neofetch, fastfetch), applying the same terminal aesthetic to country data."],["INSTALL","```pacman: sudo pacman -S countryfetch```\n\n```nix: nix profile install nixpkgs#countryfetch```"],["SEE ALSO","[neofetch](/man/neofetch)(1), [fastfetch](/man/fastfetch)(1), [onefetch](/man/onefetch)(1), [cpufetch](/man/cpufetch)(1)"]],"e":["neofetch","fastfetch","onefetch","cpufetch"],"x":"countryfetch"},"cryptsetup":{"n":"cryptsetup","t":"dm-crypt and LUKS encrypted volume manager","s":[["TAGLINE","dm-crypt and LUKS encrypted volume manager"],["TLDR","Initialize **LUKS** volume\n\n```cryptsetup luksFormat /dev/sda1```\n\n**Open** LUKS volume\n\n```cryptsetup open /dev/sda1 mapping_name```\n\nShow mapping **status**\n\n```cryptsetup status mapping_name```\n\n**Close** mapping\n\n```cryptsetup close mapping_name```\n\n**Change** passphrase\n\n```cryptsetup luksChangeKey /dev/sda1```"],["SYNOPSIS","**cryptsetup** _action_ [_OPTIONS_] _device_ [_name_]"],["DESCRIPTION","**cryptsetup** manages dm-crypt and LUKS (Linux Unified Key Setup) encrypted volumes. It provides disk encryption for protecting data at rest, supporting multiple key slots, strong encryption algorithms, and header backup/restore."],["PARAMETERS","**luksFormat** _device_\n> Initialize a LUKS encrypted volume (destroys all data)\n\n**open** _device_ _name_\n> Open encrypted device and create mapping at /dev/mapper/name\n\n**close** _name_\n> Remove an existing mapping\n\n**status** _name_\n> Display information about an active mapping\n\n**luksChangeKey** _device_\n> Change the passphrase for a LUKS volume\n\n**luksAddKey** _device_\n> Add a new passphrase to a key slot\n\n**luksRemoveKey** _device_\n> Remove a passphrase from a key slot\n\n**luksDump** _device_\n> Display LUKS header information\n\n**luksHeaderBackup** _device_\n> Backup LUKS header to a file\n\n**isLuks** _device_\n> Check if device is a LUKS volume\n\n**benchmark**\n> Benchmark encryption algorithms\n\n**--type** _type_\n> Specify encryption type (luks, luks2, plain)\n\n**--cipher** _cipher_\n> Encryption cipher specification\n\n**--key-size** _bits_\n> Encryption key size in bits\n\n**--hash** _hash_\n> Hash algorithm for LUKS header"],["CONFIGURATION","**/etc/crypttab**\n> Defines encrypted volumes to unlock at boot."],["CAVEATS","Losing all passphrases means permanent data loss. Always backup LUKS headers. Formatting destroys all existing data. The encrypted device must be opened before it can be mounted."],["HISTORY","**cryptsetup** was created by **Jana Saout** (dm-crypt) and **Clemens Fruhwirth** (LUKS). LUKS was introduced in **2004** to provide a standard on-disk format for encrypted volumes. LUKS2 was added in 2017 with improved header flexibility and Argon2 key derivation."],["INSTALL","```apt: sudo apt install cryptsetup-bin```\n\n```dnf: sudo dnf install cryptsetup```\n\n```pacman: sudo pacman -S cryptsetup```\n\n```apk: sudo apk add cryptsetup```\n\n```zypper: sudo zypper install cryptsetup```\n\n```nix: nix profile install nixpkgs#cryptsetup```"],["SEE ALSO","[dmsetup](/man/dmsetup)(8), [losetup](/man/losetup)(8), [mkfs](/man/mkfs)(8)"]],"e":["dmsetup","losetup","mkfs"],"x":"cryptsetup luksFormat /dev/sda1"},"csc":{"n":"csc","t":"C# compiler from .NET SDK","s":[["TAGLINE","C# compiler from .NET SDK"],["TLDR","**Compile a C# file** to executable\n\n```csc [Program.cs]```\n\n**Compile multiple files**\n\n```csc [File1.cs] [File2.cs] [File3.cs]```\n\n**Specify output filename**\n\n```csc -out:[MyApp.exe] [Program.cs]```\n\n**Create a library** (DLL)\n\n```csc -target:library -out:[MyLib.dll] [Library.cs]```\n\n**Reference external assemblies**\n\n```csc -reference:[System.Data.dll] [Program.cs]```\n\n**Compile with optimizations**\n\n```csc -optimize [Program.cs]```\n\n**Enable all warnings**\n\n```csc -warn:4 [Program.cs]```"],["SYNOPSIS","**csc** [_options_] _source-files_"],["PARAMETERS","**-out:**_FILE_\n> Specify output file name.\n\n**-target:**_TYPE_\n> Output type: exe, winexe, library, module.\n\n**-reference:**_FILE_\n> Reference an assembly file (can be repeated).\n\n**-lib:**_PATH_\n> Additional directories for assembly references.\n\n**-optimize**[**+**|**-**]\n> Enable or disable optimizations.\n\n**-debug**[**+**|**-**]\n> Emit debugging information.\n\n**-warn:**_LEVEL_\n> Warning level (0-4).\n\n**-nowarn:**_WARNINGS_\n> Disable specific warnings.\n\n**-define:**_SYMBOLS_\n> Define conditional compilation symbols.\n\n**-doc:**_FILE_\n> Generate XML documentation file.\n\n**-?**, **-help**\n> Display help information."],["DESCRIPTION","**csc** is the C# compiler from the .NET SDK. It compiles C# source files into assemblies (executables or libraries) that run on the .NET runtime. The compiler supports all C# language features and produces MSIL (Microsoft Intermediate Language) code.\n\nModern C# development typically uses the **dotnet** CLI which invokes csc internally, but direct csc usage is valuable for understanding compilation, scripting builds, or working with legacy projects.\n\nThe compiler handles syntax checking, type verification, code generation, and optimization. It supports incremental compilation and can produce debug symbols for debugging with Visual Studio or other debuggers."],["CAVEATS","Modern .NET development prefers **dotnet build** over direct csc invocation. Assembly references must be explicitly specified unlike with project files. Cross-platform .NET Core/5+ uses Roslyn compiler accessed through dotnet CLI."],["HISTORY","csc has been the C# compiler since **C# 1.0 in 2002**. Originally part of .NET Framework, it was reimplemented in C# itself as the Roslyn compiler starting in **2014**. The modern csc is part of the .NET SDK and tracks the latest language version (C# 14 with .NET 10, and beyond)."],["INSTALL","```apt: sudo apt install chicken-bin```\n\n```dnf: sudo dnf install chicken```\n\n```pacman: sudo pacman -S chicken```\n\n```apk: sudo apk add chicken```\n\n```brew: brew install chicken```\n\n```nix: nix profile install nixpkgs#chicken```"],["SEE ALSO","[dotnet](/man/dotnet)(1), [msbuild](/man/msbuild)(1), [mono](/man/mono)(1), [mcs](/man/mcs)(1)"],["RESOURCES","```[Source code](https://github.com/dotnet/roslyn)```\n\n```[Documentation](https://learn.microsoft.com/en-us/dotnet/csharp/language-reference/compiler-options/)```"]],"e":["dotnet","msbuild","mono","mcs"],"x":"csc [Program.cs]"},"cuyo":{"n":"cuyo","t":"Tetris-like puzzle game with many surprises","s":[["TAGLINE","Tetris-like puzzle game with many surprises"],["TLDR","**Start** a new game\n\n```cuyo```\n\n**Start in fullscreen** mode\n\n```cuyo -f```\n\n**Set a custom** window resolution\n\n```cuyo -g [800x600]```\n\n**Enable debug** mode\n\n```cuyo -d```\n\n**Load a custom** level description file\n\n```cuyo [level.ld]```\n\n**Show the help** message\n\n```cuyo -h```"],["SYNOPSIS","**cuyo** [**-d**] [**-f**] [**-g** _width_**x**_height_] [**-h**|**-?**] [**--version** _versions_] [_ld-file_]"],["PARAMETERS","**-d**\n> Enable debug mode.\n\n**-f**\n> Run in fullscreen mode.\n\n**-g** _WxH_\n> Set the window size (for example **-g 1024x768**).\n\n**-h**\n> Print a short help message and exit with status 0.\n\n**-?**\n> Print a short help message and exit with status 1.\n\n**--version** _versions_\n> Activate special level versions using a comma-separated list of specifiers (difficulty, player count, eco, geek modes, etc.).\n\n_ld-file_\n> Path to a level description file to load and test custom levels."],["DESCRIPTION","**cuyo** is a Puyo Puyo / Tetris style puzzle game for one or two players. Pieces fall from the top of the playfield and the player must connect matching blobs; once enough identical blobs touch, they explode. Clearing them triggers chain reactions that destroy grass or other obstacles, which is usually the level's real goal.\n\nEach of the many built-in levels has its own rules, graphics and music, and the game ships with a level description language so players can create and load their own levels via an _ld-file_ argument. In two-player mode, one player's chain reactions drop garbage into the opponent's field."],["KEYBINDINGS","**a**, **Left Arrow**\n> Move piece left.\n\n**d**, **Right Arrow**\n> Move piece right.\n\n**w**, **Up Arrow**\n> Rotate piece.\n\n**s**, **Down Arrow**\n> Drop piece faster (hard drop).\n\n**Esc**\n> Pause the game / open menu."],["CAVEATS","Requires SDL for graphics and sound. Levels vary wildly in rules, objectives and visual style, so strategies learned in one level rarely transfer directly to the next. The project has been largely unmaintained for years but remains packaged by most major distributions."],["HISTORY","**cuyo** was written by **Mark Weyer** and first released around **2001** as a free software homage to Puyo Puyo, but it quickly diverged by introducing per-level rule changes and a level description language. It is packaged in Debian and Ubuntu under the **cuyo** package."],["INSTALL","```pacman: sudo pacman -S cuyo```\n\n```nix: nix profile install nixpkgs#cuyo```"],["SEE ALSO","[tetris](/man/tetris)(6), [bastet](/man/bastet)(6)"]],"e":["tetris","bastet"],"x":"cuyo"},"debootstrap":{"n":"debootstrap","t":"install a basic Debian system into a subdirectory","s":[["TAGLINE","install a basic Debian system into a subdirectory"],["TLDR","Create a **Debian stable** system\n\n```sudo debootstrap stable [path/to/debian-root]/ http://deb.debian.org/debian```\n\nCreate a **minimal** system\n\n```sudo debootstrap --variant=minbase stable [path/to/debian-root]/```\n\nCreate **Debian Unstable** with local mirror\n\n```sudo debootstrap sid [path/to/sid-root]/ file:///[path/to/mirror]/```\n\nSwitch to the **bootstrapped system**\n\n```sudo chroot [path/to/root]```\n\n**List** available releases\n\n```ls /usr/share/debootstrap/scripts/```"],["SYNOPSIS","**debootstrap** [_options_] _suite_ _target_ [_mirror_]"],["DESCRIPTION","**debootstrap** installs a basic Debian system into a subdirectory. It downloads packages from a mirror and installs them to create a minimal working system that can be used for chroot environments, containers, or new installations.\n\nThe resulting system is minimal but functional, suitable for further customization."],["PARAMETERS","**--variant** _variant_\n> minbase (minimal), buildd (build dependencies), fakechroot\n\n**--arch** _arch_\n> Target architecture (amd64, i386, arm64, etc.)\n\n**--include** _packages_\n> Additional packages to install\n\n**--exclude** _packages_\n> Packages to exclude\n\n**--foreign**\n> First stage only (for cross-architecture)"],["CAVEATS","Requires root privileges. The target directory must exist. For cross-architecture bootstrapping, qemu-user-static may be needed. After debootstrap, additional configuration (networking, fstab, etc.) is usually needed."],["INSTALL","```dnf: sudo dnf install debootstrap```\n\n```pacman: sudo pacman -S debootstrap```\n\n```apk: sudo apk add debootstrap```\n\n```zypper: sudo zypper install debootstrap```\n\n```nix: nix profile install nixpkgs#debootstrap```"],["SEE ALSO","[chroot](/man/chroot)(8), [schroot](/man/schroot)(1), [systemd-nspawn](/man/systemd-nspawn)(1)"],["RESOURCES","```[Source code](https://salsa.debian.org/installer-team/debootstrap)```\n\n```[Documentation](https://manpages.debian.org/testing/debootstrap/debootstrap.8)```"]],"e":["chroot","schroot","systemd-nspawn"],"x":"sudo debootstrap stable [path/to/debian-root]/ http://deb.debian.org/debian"},"diff3":{"n":"diff3","t":"three-way file comparison and merge","s":[["TAGLINE","three-way file comparison and merge"],["TLDR","**Compare** three files\n\n```diff3 [path/to/file1] [path/to/file2] [path/to/file3]```\n\nShow all changes **outlining conflicts**\n\n```diff3 -A [path/to/file1] [path/to/file2] [path/to/file3]```"],["SYNOPSIS","**diff3** [_options_] _myfile_ _oldfile_ _yourfile_"],["DESCRIPTION","**diff3** compares three files line by line, showing differences and conflicts. It's typically used for three-way merges where one file is the common ancestor and two files are modified versions.\n\nThe output format is suitable for merge conflict resolution."],["PARAMETERS","**-A, --show-all**\n> Show all changes, bracketing conflicts\n\n**-e, --ed**\n> Output ed script\n\n**-E, --show-overlap**\n> Like -e but bracket conflicts\n\n**-m, --merge**\n> Output the merged file directly (implies -A unless -e, -E, or -x is given)\n\n**-x, --overlap-only**\n> Output overlapping (conflicting) changes only\n\n**-3, --easy-only**\n> Output non-overlapping changes from the third file only\n\n**-T, --initial-tab**\n> Make tabs line up by prefixing a tab"],["CAVEATS","File order matters: mine, older, yours. Commonly used internally by version control systems. Part of GNU diffutils."],["INSTALL","```apt: sudo apt install diffutils```\n\n```dnf: sudo dnf install diffutils```\n\n```pacman: sudo pacman -S diffutils```\n\n```apk: sudo apk add diffutils```\n\n```zypper: sudo zypper install diffutils```\n\n```brew: brew install diffutils```\n\n```nix: nix profile install nixpkgs#diffutils```"],["SEE ALSO","[diff](/man/diff)(1), [merge](/man/merge)(1), [patch](/man/patch)(1)"],["RESOURCES","```[Homepage](https://www.gnu.org/software/diffutils/)```\n\n```[Documentation](https://www.gnu.org/software/diffutils/manual/html_node/Invoking-diff3.html)```"]],"e":["diff","merge","patch"],"x":"diff3 [path/to/file1] [path/to/file2] [path/to/file3]"},"dmraid":{"n":"dmraid","t":"firmware RAID metadata discovery and activation","s":[["TAGLINE","firmware RAID metadata discovery and activation"],["TLDR","**List all RAID sets**\n\n```dmraid -s```\n\n**Activate all RAID** sets\n\n```dmraid -ay```\n\n**Activate specific RAID** set\n\n```dmraid -ay [raidset_name]```\n\n**Deactivate all RAID** sets\n\n```dmraid -an```\n\n**Show detailed RAID** information\n\n```dmraid -s -s```\n\n**List block devices**\n\n```dmraid -b```\n\n**Remove inactive RAID** metadata\n\n```dmraid -E [/dev/sda]```"],["SYNOPSIS","**dmraid** [_options_] [_device_...]"],["PARAMETERS","**-s**\n> Display RAID sets (double for details).\n\n**-ay**\n> Activate RAID sets.\n\n**-an**\n> Deactivate RAID sets.\n\n**-b**\n> List block devices.\n\n**-r**\n> List RAID devices.\n\n**-E** _DEVICE_\n> Erase RAID metadata.\n\n**-n**\n> Dry run (show but don't execute).\n\n**-v**\n> Verbose output."],["DESCRIPTION","**dmraid** discovers and activates software RAID sets using the Linux device-mapper. It supports various hardware RAID metadata formats including those from Intel, NVIDIA, VIA, and other motherboard RAID controllers.\n\nThe tool reads RAID metadata from drives and creates device-mapper mappings that present the RAID array as a single block device. This enables Linux to work with \"fake RAID\" or \"firmware RAID\" commonly found on consumer motherboards.\n\ndmraid operates independently of the RAID controller's BIOS, allowing arrays to be used even without controller-specific drivers."],["CAVEATS","Only supports specific metadata formats. Some RAID levels may have limited support. Activating degraded arrays may risk data. Does not replace hardware RAID controllers or mdadm software RAID."],["HISTORY","dmraid was developed by **Heinz Mauelshagen** at **Red Hat** to support hardware RAID metadata formats in Linux, addressing the need for compatibility with consumer motherboard RAID implementations that use software RAID with proprietary metadata formats. It is now largely unmaintained; most distributions have moved Intel IMSM and DDF \"fake RAID\" support to **mdadm**, which reads the same metadata and is the recommended tool for new setups."],["INSTALL","```pacman: sudo pacman -S dmraid```\n\n```apk: sudo apk add dmraid```\n\n```zypper: sudo zypper install dmraid```\n\n```nix: nix profile install nixpkgs#dmraid```"],["SEE ALSO","[mdadm](/man/mdadm)(8), [dmsetup](/man/dmsetup)(8), [lvm](/man/lvm)(8)"]],"e":["mdadm","dmsetup","lvm"],"x":"dmraid -s"},"dpkg-reconfigure":{"n":"dpkg-reconfigure","t":"reconfigure installed Debian packages","s":[["TAGLINE","reconfigure installed Debian packages"],["TLDR","**Reconfigure** packages\n\n```dpkg-reconfigure [package1] [package2]```\n\nReconfigure **console font** setup\n\n```dpkg-reconfigure console-setup```\n\nReconfigure **timezone**\n\n```dpkg-reconfigure tzdata```"],["SYNOPSIS","**dpkg-reconfigure** [_options_] _packages_"],["DESCRIPTION","**dpkg-reconfigure** reconfigures already installed packages on Debian-based systems. It runs a package's post-installation configuration scripts again, presenting the same prompts shown during initial installation and allowing you to change previously configured settings.\n\nThe tool uses the debconf system to manage configuration questions and responses. It's particularly useful for system settings that are difficult to change manually, such as locales, keyboard layouts, timezones, and display manager configurations.\n\nDifferent frontends (dialog, readline, noninteractive) control how questions are presented, making it suitable for both interactive terminal sessions and automated scripts."],["PARAMETERS","**-f, --frontend** _frontend_\n> Use specified frontend (dialog, readline, noninteractive)\n\n**-p, --priority** _priority_\n> Priority of questions (low, medium, high, critical)\n\n**-a, --all**\n> Reconfigure all packages\n\n**--default-priority**\n> Use default priority"],["CAVEATS","Debian/Ubuntu specific. Not all packages support reconfiguration. Some reconfigurations require root privileges."],["INSTALL","```apt: sudo apt install dpkg```\n\n```dnf: sudo dnf install dpkg```\n\n```pacman: sudo pacman -S dpkg```\n\n```apk: sudo apk add dpkg```\n\n```zypper: sudo zypper install dpkg```\n\n```brew: brew install dpkg```\n\n```nix: nix profile install nixpkgs#dpkg```"],["SEE ALSO","[dpkg](/man/dpkg)(1)"]],"e":["dpkg"],"x":"dpkg-reconfigure [package1] [package2]"},"equery":{"n":"equery","t":"Portage package information query tool","s":[["TAGLINE","Portage package information query tool"],["TLDR","**List** all installed packages\n\n```equery list '*'```\n\nSearch for packages in Portage tree and **overlays**\n\n```equery list -po [package1] [package2]```\n\nList all packages that **depend on** a package\n\n```equery depends [package]```\n\nList all packages that a package **depends on**\n\n```equery depgraph [package]```\n\nList all **files** installed by a package\n\n```equery files --tree [package]```\n\nShow **USE flags** for a package\n\n```equery uses [package]```\n\nFind which package **owns** a file\n\n```equery belongs [/path/to/file]```"],["SYNOPSIS","**equery** _command_ [_options_] [_package_]"],["DESCRIPTION","**equery** is a powerful query tool for viewing information about Portage packages on Gentoo Linux. It provides detailed information about installed packages, their dependencies, owned files, USE flag settings, and more. The tool can search both the local installed package database and the Portage tree including overlays.\n\nAs part of the gentoolkit package, equery offers a more user-friendly interface than raw Portage queries, making it easier to investigate package relationships, troubleshoot installation issues, and understand the state of the system. It's an essential tool for Gentoo system administration and package management."],["PARAMETERS","**list** _pattern_\n> List packages matching pattern\n\n**depends** _package_\n> Show reverse dependencies\n\n**depgraph** _package_\n> Show dependency graph\n\n**files** _package_\n> List installed files\n\n**uses** _package_\n> Show USE flags for a package\n\n**belongs** _file_\n> Find which package owns a file\n\n**hasuse** _flag_\n> Find packages with a specific USE flag\n\n**size** _package_\n> Show disk space used by a package\n\n**-p**, **--portage-tree**\n> Include Portage tree\n\n**-o**, **--overlay-tree**\n> Include overlays\n\n**--tree**\n> Display as tree"],["CAVEATS","Gentoo Linux specific. Part of gentoolkit package. Only queries local database, not remote repositories."],["SEE ALSO","[emerge](/man/emerge)(1), [eix](/man/eix)(1)"]],"e":["emerge","eix"],"x":"equery list '*'"},"fastboot":{"n":"fastboot","t":"Android bootloader flashing and modification tool","s":[["TAGLINE","Android bootloader flashing and modification tool"],["TLDR","**List connected devices**\n\n```fastboot devices```\n\n**Flash partition**\n\n```fastboot flash [boot] [boot.img]```\n\n**Flash all partitions** from factory image\n\n```fastboot flashall```\n\n**Unlock bootloader**\n\n```fastboot flashing unlock```\n\n**Reboot device**\n\n```fastboot reboot```\n\n**Reboot to recovery**\n\n```fastboot reboot recovery```\n\n**Erase partition**\n\n```fastboot erase [cache]```\n\n**Boot temporary image**\n\n```fastboot boot [recovery.img]```"],["SYNOPSIS","**fastboot** [_options_] _command_ [_args_]"],["DESCRIPTION","**fastboot** is an Android tool for flashing firmware and modifying device partitions. It communicates with devices in fastboot/bootloader mode for low-level operations.\n\nThe tool enables installing custom ROMs, recovery images, and performing maintenance on Android devices. It's part of the Android SDK platform tools."],["PARAMETERS","**devices**\n> List connected devices.\n\n**flash** _partition_ _image_\n> Flash image to partition.\n\n**flashall**\n> Flash all partitions.\n\n**erase** _partition_\n> Erase partition.\n\n**reboot** [_target_]\n> Reboot (bootloader, recovery).\n\n**boot** _image_\n> Boot temporary image.\n\n**oem** _command_\n> OEM-specific commands.\n\n**getvar** _variable_\n> Get bootloader variable.\n\n**-s** _serial_\n> Target specific device.\n\n**update** _zipfile_\n> Flash all partitions from an update zip.\n\n**flashing unlock**\n> Unlock the bootloader (newer devices).\n\n**-w**\n> Wipe userdata."],["CAVEATS","Device must be in fastboot mode. Wrong images can brick device. Unlocking bootloader may void warranty. USB drivers needed on Windows."],["HISTORY","**fastboot** is part of Android SDK platform tools, developed by **Google**. It emerged with Android's development in the late **2000s** as a standard way to flash devices. The protocol allows manufacturers to customize behavior while maintaining compatibility."],["INSTALL","```apt: sudo apt install fastboot```\n\n```apk: sudo apk add android-tools-fastboot```"],["SEE ALSO","[adb](/man/adb)(1)"]],"e":["adb"],"x":"fastboot devices"},"fc":{"n":"fc","t":"list, edit, and re-execute shell commands","s":[["TAGLINE","list, edit, and re-execute shell commands"],["TLDR","**Edit and rerun** last command\n\n```fc```\n\n**List recent commands**\n\n```fc -l```\n\n**List commands without** numbers\n\n```fc -ln [10] [20]```\n\n**Rerun command** by number\n\n```fc -s [15]```\n\n**Edit specific command**\n\n```fc [20]```"],["SYNOPSIS","**fc** [_options_] [_first_] [_last_]"],["PARAMETERS","_FIRST_\n> First command (number or string).\n\n_LAST_\n> Last command in range.\n\n**-l**\n> List commands without editing.\n\n**-n**\n> Suppress command numbers in listing.\n\n**-r**\n> Reverse order of listing.\n\n**-s** [_OLD=NEW_]\n> Rerun command, optionally with substitution.\n\n**-e** _EDITOR_\n> Use specified editor."],["DESCRIPTION","**fc** is a shell builtin for listing, editing, and re-executing commands from history. It opens commands in an editor for modification before execution, enabling quick fixes to previous commands.\n\nThe command can list history entries, open single commands or ranges in an editor, and directly re-execute commands with optional text substitution. The default editor is taken from FCEDIT, EDITOR, or vi.\n\nfc is particularly useful for fixing typos in long commands or re-running complex pipelines with modifications."],["CAVEATS","Only works with interactive shell history. Editing spawns external editor. Substitution syntax is limited. History must be enabled."],["HISTORY","fc (fix command) is a **POSIX-specified** shell builtin present in Korn shell, Bash, and Zsh. It originated in the Korn shell as a way to edit and re-execute command history entries."],["INSTALL","```apt: sudo apt install f2c```\n\n```dnf: sudo dnf install f2c```\n\n```zypper: sudo zypper install f2c```\n\n```nix: nix profile install nixpkgs#f2c```"],["SEE ALSO","[history](/man/history)(1), [bash](/man/bash)(1), [zsh](/man/zsh)(1)"]],"e":["history","bash","zsh"],"x":"fc"},"gh-org":{"n":"gh-org","t":"Manage GitHub organization information from the command line","s":[["TAGLINE","Manage GitHub organization information from the command line"],["TLDR","**List organizations**\n\n```gh org list```\n\n**View organization**\n\n```gh org view [orgname]```\n\n**List organization members**\n\n```gh api orgs/[orgname]/members```"],["SYNOPSIS","**gh org** _command_ [_options_]"],["PARAMETERS","**list**\n> List organizations you belong to.\n\n**view** _ORGNAME_\n> View organization details.\n\n**--help**\n> Display help information."],["DESCRIPTION","**gh org** manages GitHub organization information from the command line. It provides access to organization membership, details, and settings for organizations you belong to.\n\nThe command enables viewing organization information without visiting github.com. For more complex organization management like team operations and member management, the **gh api** command provides full API access."],["CAVEATS","Available operations depend on your role in the organization. Some features require admin permissions. Full org management may need gh api."],["HISTORY","gh org was added to the **GitHub CLI** to provide basic organization commands, complementing the repository and user management features."],["INSTALL","```apt: sudo apt install gh```\n\n```dnf: sudo dnf install gh```\n\n```pacman: sudo pacman -S github-cli```\n\n```apk: sudo apk add github-cli```\n\n```zypper: sudo zypper install gh```\n\n```brew: brew install gh```\n\n```nix: nix profile install nixpkgs#gh```"],["SEE ALSO","[gh](/man/gh)(1), [gh-repo](/man/gh-repo)(1)"],["RESOURCES","```[Documentation](https://cli.github.com/manual/gh_org)```\n\n```[Source code](https://github.com/cli/cli)```"]],"e":["gh","gh-repo"],"x":"gh org list"},"git-log":{"n":"git-log","t":"Display commit history","s":[["TAGLINE","Display commit history"],["TLDR","**Show commit history**\n\n```git log```\n\n**Show compact log**\n\n```git log --oneline```\n\n**Show graph view**\n\n```git log --graph --oneline --all```\n\n**Show specific file history**\n\n```git log [file.txt]```\n\n**Show last N commits**\n\n```git log -n [5]```"],["SYNOPSIS","**git log** [_options_] [_revision_] [_--_] [_path_]"],["PARAMETERS","**--oneline**\n> Compact one-line format.\n\n**--graph**\n> Show branch graph.\n\n**--all**\n> Show all branches.\n\n**-n** _NUM_\n> Limit to N commits.\n\n**--author** _AUTHOR_\n> Filter by author.\n\n**--since** _DATE_\n> Show commits since date.\n\n**--stat**\n> Show changed files.\n\n**-p**, **--patch**\n> Show diffs.\n\n**--format** _FORMAT_\n> Custom format string.\n\n**--help**\n> Display help information."],["DESCRIPTION","**git log** shows the commit history in reverse chronological order. Each entry displays the commit hash, author, date, and message. Extensive formatting and filtering options enable customized output for any use case.\n\nThe command supports filtering by author, date range, file path, and message content. Graph mode visualizes branch structure with ASCII art. Custom format strings enable scripted log processing and report generation."],["CAVEATS","Large histories can be slow. Some options don't combine. Graph may need wide terminal."],["HISTORY","git log is a core **Git** command from initial release, providing the essential capability to view commit history."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-show](/man/git-show)(1), [git-blame](/man/git-blame)(1), [tig](/man/tig)(1)"],["RESOURCES","```[Source code](https://github.com/git/git)```\n\n```[Homepage](https://git-scm.com/)```\n\n```[Documentation](https://git-scm.com/docs/git-log)```"]],"e":["git-show","git-blame","tig"],"x":"git log"},"git-rebase":{"n":"git-rebase","t":"Reapply commits on new base","s":[["TAGLINE","Reapply commits on new base"],["TLDR","**Rebase onto branch**\n\n```git rebase [branch]```\n\n**Interactive rebase**\n\n```git rebase -i [commit]```\n\n**Rebase last N commits**\n\n```git rebase -i HEAD~[n]```\n\n**Continue after conflict**\n\n```git rebase --continue```\n\n**Abort rebase**\n\n```git rebase --abort```\n\n**Skip current commit**\n\n```git rebase --skip```\n\n**Rebase onto specific base**\n\n```git rebase --onto [newbase] [oldbase] [branch]```"],["SYNOPSIS","**git** **rebase** [_options_] [_upstream_] [_branch_]"],["PARAMETERS","**-i**, **--interactive**\n> Interactive mode.\n\n**--onto** _newbase_\n> Rebase onto different base.\n\n**--continue**\n> Continue after resolving.\n\n**--abort**\n> Cancel rebase.\n\n**--skip**\n> Skip current patch.\n\n**--autosquash**\n> Auto-apply fixup/squash.\n\n**--autostash**\n> Auto-stash changes.\n\n**-x** _cmd_\n> Run command after each commit.\n\n**-r**, **--rebase-merges**\n> Recreate merge commits instead of flattening history (replaces the deprecated `--preserve-merges`)."],["DESCRIPTION","**git rebase** reapplies commits on top of another base tip, producing a linear project history. It finds the common ancestor of the current branch and the upstream, then replays each commit from the current branch onto the upstream tip.\n\nInteractive mode (`-i`) allows reordering, squashing, editing, or dropping commits during the replay, making it a powerful tool for cleaning up commit history. The `--onto` option enables advanced workflows like moving a branch to an entirely new base. Autosquash automatically applies fixup! and squash! commits, supporting the iterative fixup workflow."],["CAVEATS","Rebasing rewrites history. Don't rebase commits that have been pushed to shared branches."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-merge](/man/git-merge)(1), [git-cherry-pick](/man/git-cherry-pick)(1), [git-reset](/man/git-reset)(1)"],["RESOURCES","```[Source code](https://github.com/git/git)```\n\n```[Homepage](https://git-scm.com/)```\n\n```[Documentation](https://git-scm.com/docs/git-rebase)```"]],"e":["git-merge","git-cherry-pick","git-reset"],"x":"git rebase [branch]"},"git-show-unmerged-branches":{"n":"git-show-unmerged-branches","t":"List branches not yet merged into the current branch","s":[["TAGLINE","List branches not yet merged into the current branch"],["TLDR","**Show all unmerged branches**\n\n```git show-unmerged-branches```\n\n**Show unmerged branches (equivalent native git)**\n\n```git branch --no-merged```"],["SYNOPSIS","**git** **show-unmerged-branches**"],["DESCRIPTION","**git show-unmerged-branches** lists all local branches that have not been merged into the current HEAD. It is part of the **git-extras** collection and is useful for identifying work-in-progress branches and outstanding feature work.\n\nInternally, it runs `git branch --no-merged` while filtering out the current branch and the default branch."],["CAVEATS","Requires **git-extras** to be installed. Only checks local branches, not remote tracking branches. Written by Paul Schreiber."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-branch](/man/git-branch)(1), [git-show-merged-branches](/man/git-show-merged-branches)(1), [git-extras](/man/git-extras)(1), [git-log](/man/git-log)(1)"]],"e":["git-branch","git-show-merged-branches","git-extras","git-log"],"x":"git show-unmerged-branches"},"git-stripspace":{"n":"git-stripspace","t":"Clean up whitespace in Git messages","s":[["TAGLINE","Clean up whitespace in Git messages"],["TLDR","**Strip trailing whitespace**\n\n```git stripspace < [file]```\n\n**Strip comments**\n\n```git stripspace -s < [file]```\n\n**Comment out lines**\n\n```git stripspace -c < [file]```"],["SYNOPSIS","**git** **stripspace** [_options_]"],["PARAMETERS","**-s**, **--strip-comments**\n> Strip comment lines.\n\n**-c**, **--comment-lines**\n> Comment out lines."],["DESCRIPTION","**git stripspace** filters text from stdin, stripping trailing whitespace and collapsing multiple consecutive blank lines. It is used internally by Git for commit message cleanup.\n\nThe command can also strip or add comment lines, making it useful for processing Git-formatted messages and templates."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-commit](/man/git-commit)(1)"],["RESOURCES","```[Source code](https://github.com/git/git)```\n\n```[Homepage](https://git-scm.com/)```\n\n```[Documentation](https://git-scm.com/docs/git-stripspace)```"]],"e":["git-commit"],"x":"git stripspace < [file]"},"gojq":{"n":"gojq","t":"Pure Go implementation of jq JSON processor","s":[["TAGLINE","Pure Go implementation of jq JSON processor"],["TLDR","**Pretty print JSON**\n\n```cat [file.json] | gojq .```\n\n**Extract a field**\n\n```cat [file.json] | gojq '.[field]'```\n\n**Filter array elements**\n\n```cat [file.json] | gojq '.[] | select(.active == true)'```\n\n**Map and transform**\n\n```cat [file.json] | gojq '[.[] | {name: .title, id: .uuid}]'```\n\n**Read from file directly**\n\n```gojq '.users[]' [file.json]```\n\n**Process YAML input**\n\n```gojq --yaml-input '.spec.containers[]' [deployment.yaml]```\n\n**Output as YAML**\n\n```gojq --yaml-output '.data' [file.json]```\n\n**Use variables**\n\n```gojq --arg name \"[value]\" '.items[] | select(.name == $name)' [file.json]```"],["SYNOPSIS","**gojq** [_options_] _filter_ [_file_...]"],["PARAMETERS","**-r**, **--raw-output**\n> Output strings without JSON quotes.\n\n**-c**, **--compact-output**\n> Compact output instead of pretty-printed.\n\n**-n**, **--null-input**\n> Don't read input; useful with --argjson.\n\n**-e**, **--exit-status**\n> Set exit code based on output.\n\n**-s**, **--slurp**\n> Read all inputs into array.\n\n**-S**, **--sort-keys**\n> Sort object keys in output.\n\n**-C**, **--color-output**\n> Force colored output.\n\n**-M**, **--monochrome-output**\n> Disable colored output.\n\n**--tab**\n> Use tabs for indentation.\n\n**--indent** _n_\n> Set indentation level.\n\n**--yaml-input**\n> Parse input as YAML.\n\n**--yaml-output**\n> Output as YAML instead of JSON.\n\n**--arg** _name_ _value_\n> Set variable to string value.\n\n**--argjson** _name_ _json_\n> Set variable to JSON value.\n\n**--slurpfile** _name_ _file_\n> Set variable to file contents as array.\n\n**--rawfile** _name_ _file_\n> Set variable to raw file contents.\n\n**-f**, **--from-file** _file_\n> Read filter from file.\n\n**-L** _path_\n> Add directory to module search path."],["DESCRIPTION","**gojq** is a pure Go implementation of jq, the JSON processor. It provides the same query language for filtering, transforming, and extracting data from JSON, with additional native YAML support.\n\nThe filter language uses **|** for pipelines, **.** for field access, **[]** for iteration, and **select()** for filtering. Complex transformations combine these with object construction, array slicing, and built-in functions.\n\nYAML input and output make gojq particularly useful for Kubernetes and other YAML-heavy workflows. Variables with **--arg** enable parameterized queries for scripting."],["CAVEATS","Some advanced jq features have minor behavioral differences. Performance may differ from C jq on very large files. Module paths are searched differently than jq. SQL-style operators are not implemented."],["HISTORY","gojq was created by **itchyny** and first released around **2019**. It was developed to provide jq functionality in a single Go binary without C dependencies. YAML support was added as a distinguishing feature. The project maintains close compatibility with jq while serving as a library for Go applications needing JSON transformation."],["INSTALL","```apt: sudo apt install gojq```\n\n```dnf: sudo dnf install gojq```\n\n```apk: sudo apk add gojq```\n\n```zypper: sudo zypper install gojq```\n\n```brew: brew install gojq```\n\n```nix: nix profile install nixpkgs#gojq```"],["SEE ALSO","[jq](/man/jq)(1), [yq](/man/yq)(1), [jless](/man/jless)(1), [fx](/man/fx)(1)"],["RESOURCES","```[Source code](https://github.com/itchyny/gojq)```"]],"e":["jq","yq","jless","fx"],"x":"cat [file.json] | gojq ."},"gresource":{"n":"gresource","t":"GNOME GResource bundle inspector","s":[["TAGLINE","GNOME GResource bundle inspector"],["TLDR","**List resources in file**\n\n```gresource list [file.gresource]```\n\n**Extract resource**\n\n```gresource extract [file.gresource] [/path/to/resource]```\n\n**Show resource sections**\n\n```gresource sections [file.gresource]```\n\n**Get resource details**\n\n```gresource details [file.gresource] [/path/to/resource]```"],["SYNOPSIS","**gresource** _command_ [_options_] _file_ [_path_]"],["PARAMETERS","_COMMAND_\n> Operation to perform (list, extract, sections, details).\n\n_FILE_\n> GResource file or ELF binary with embedded resources.\n\n_PATH_\n> Resource path within the bundle.\n\n**--section** _NAME_\n> Specify resource section.\n\n**--help**\n> Display help information."],["DESCRIPTION","**gresource** inspects and extracts resources from GResource bundles. GResource is GNOME's resource bundling system that embeds files like icons, UI definitions, and data into executables.\n\nThe tool can list all resources, extract individual files, and show detailed information. It works with both standalone .gresource files and ELF binaries with embedded resources."],["CAVEATS","Specific to GNOME/GLib applications. Resource paths are case-sensitive. Part of glib package."],["HISTORY","gresource was developed as part of **GLib** by the **GNOME** project to provide resource inspection and extraction capabilities."],["INSTALL","```apt: sudo apt install libglib2.0-bin```\n\n```apk: sudo apk add glib-dev```"],["SEE ALSO","[glib-compile-resources](/man/glib-compile-resources)(1)"]],"e":["glib-compile-resources"],"x":"gresource list [file.gresource]"},"jbang":{"n":"jbang","t":"runs Java programs as scripts without build tools","s":[["TAGLINE","runs Java programs as scripts without build tools"],["TLDR","**Run Java script**\n\n```jbang [script.java]```\n\n**Create new script**\n\n```jbang init [script.java]```\n\n**Run with dependencies**\n\n```jbang [script.java]```\n\n**Edit in IDE**\n\n```jbang edit [script.java]```\n\n**Install as command**\n\n```jbang app install [script.java]```\n\n**Run from URL**\n\n```jbang [https://example.com/script.java]```"],["SYNOPSIS","**jbang** [_options_] _command_ [_args_]"],["PARAMETERS","**init** _FILE_\n> Create new script.\n\n**run** _FILE_\n> Run script (default).\n\n**edit** _FILE_\n> Open in IDE.\n\n**app install** _FILE_\n> Install script as app.\n\n**--deps** _GAV_\n> Add Maven dependencies.\n\n**--java** _VERSION_\n> Use specific Java version.\n\n**--help**\n> Display help information."],["DESCRIPTION","**jbang** runs Java programs as scripts without build tools. It handles dependencies, compilation, and execution automatically.\n\nThe tool supports inline dependency declarations using comments. It enables shell-script style Java programming."],["CAVEATS","Requires Java. Dependency download on first run. Scripts use comment directives."],["HISTORY","jbang was created by **Max Rydahl Andersen** to make Java scripting as easy as Python or Bash scripts."],["INSTALL","```brew: brew install jbang```\n\n```nix: nix profile install nixpkgs#jbang```"],["SEE ALSO","[java](/man/java)(1), [javac](/man/javac)(1), [kotlin](/man/kotlin)(1)"]],"e":["java","javac","kotlin"],"x":"jbang [script.java]"},"kafka-topics":{"n":"kafka-topics","t":"manages Kafka topics","s":[["TAGLINE","manages Kafka topics"],["TLDR","**Create topic**\n\n```kafka-topics.sh --create --topic [name] --partitions [3] --replication-factor [1] --bootstrap-server [localhost:9092]```\n\n**List all topics**\n\n```kafka-topics.sh --list --bootstrap-server [localhost:9092]```\n\n**Describe topic**\n\n```kafka-topics.sh --describe --topic [name] --bootstrap-server [localhost:9092]```\n\n**Delete topic**\n\n```kafka-topics.sh --delete --topic [name] --bootstrap-server [localhost:9092]```\n\n**Alter partitions**\n\n```kafka-topics.sh --alter --topic [name] --partitions [6] --bootstrap-server [localhost:9092]```"],["SYNOPSIS","**kafka-topics.sh** [_options_]"],["PARAMETERS","**--create**\n> Create new topic.\n\n**--list**\n> List all topics.\n\n**--describe**\n> Show topic details.\n\n**--delete**\n> Delete topic.\n\n**--alter**\n> Modify topic configuration.\n\n**--topic** _name_\n> Topic name.\n\n**--partitions** _n_\n> Number of partitions.\n\n**--replication-factor** _n_\n> Replication factor.\n\n**--bootstrap-server** _servers_\n> Kafka broker addresses.\n\n**--config** _key=value_\n> Topic configuration.\n\n**--if-exists**\n> Only if topic exists.\n\n**--if-not-exists**\n> Only if topic doesn't exist."],["DESCRIPTION","**kafka-topics.sh** manages Kafka topics. Topics are the core abstraction for organizing messages in Kafka, with partitions for parallelism and replication for durability."],["TOPIC CONFIGURATIONS","```bash"],["Set retention","--config retention.ms=86400000"],["Set cleanup policy","--config cleanup.policy=compact"],["Set max message size","--config max.message.bytes=1048576\n```"],["CAVEATS","Partitions can only be increased, not decreased. Replication factor limited by broker count. Deletion may need to be enabled in config. ZooKeeper connection deprecated."],["INSTALL","```pacman: sudo pacman -S kafka```\n\n```brew: brew install kafka```"],["SEE ALSO","[kafka](/man/kafka)(1), [kafkacat](/man/kafkacat)(1)"]],"e":["kafka","kafkacat"],"x":"kafka-topics.sh --create --topic [name] --partitions [3] --replication-factor [1] --bootstrap-server [localhost:9092]"},"lebab":{"n":"lebab","t":"converts JavaScript code from ES5 to ES6/ES2015+","s":[["TAGLINE","converts JavaScript code from ES5 to ES6/ES2015+"],["TLDR","**Convert ES5 to ES6**\n\n```lebab [input.js] -o [output.js]```\n\n**Apply specific transforms**\n\n```lebab [input.js] -o [output.js] --transform arrow,let```\n\n**Convert in place**\n\n```lebab --replace [file.js] --transform arrow```\n\n**List available transforms**\n\n```lebab --help```"],["SYNOPSIS","**lebab** [_options_] _file_"],["PARAMETERS","**-o**, **--out-file** _file_\n> Output file path.\n\n**-t**, **--transform** _transforms_\n> Comma-separated list of transforms.\n\n**-r**, **--replace** _glob_\n> Transform files in place, matching a file or glob pattern."],["TRANSFORMS","Safe transforms apply with high confidence: **arrow**, **arrow-return**, **for-of**, **for-each**, **arg-rest**, **arg-spread**, **obj-method**, **obj-shorthand**, **no-strict**, **exponent**, **multi-var**.\n\nUnsafe transforms rely on heuristics and should be applied one at a time with careful review: **let**, **class**, **commonjs**, **template**, **default-param**, **destruct-param**, **includes**."],["DESCRIPTION","**lebab** converts JavaScript code from ES5 to ES6/ES2015+. It applies transformations like arrow functions, let/const, template strings, and destructuring. The reverse of Babel, it modernizes legacy JavaScript code.\n\nNo transform runs by default: at least one must be selected with **--transform**. The recommended workflow is to apply a single transform at a time and inspect the diff, since combining many transforms in one pass can produce broken code."],["INSTALL","```aur: yay -S lebab```"],["SEE ALSO","[babel](/man/babel)(1), [eslint](/man/eslint)(1)"]],"e":["babel","eslint"],"x":"lebab [input.js] -o [output.js]"},"ledger":{"n":"ledger","t":"plain text accounting tool","s":[["TAGLINE","plain text accounting tool"],["TLDR","**Show balance**\n\n```ledger -f [journal.ledger] balance```\n\n**Show register**\n\n```ledger -f [journal.ledger] register```\n\n**Balance for account**\n\n```ledger -f [journal.ledger] balance [Expenses]```\n\n**Monthly summary**\n\n```ledger -f [journal.ledger] register --monthly```\n\n**Print transactions**\n\n```ledger -f [journal.ledger] print```\n\n**Budget report**\n\n```ledger -f [journal.ledger] budget```"],["SYNOPSIS","**ledger** [_options_] _command_ [_args_]"],["PARAMETERS","**-f** _FILE_\n> Journal file.\n\n**balance**\n> Show account balances.\n\n**register**\n> Show transaction register.\n\n**print**\n> Print transactions.\n\n**--monthly**\n> Group by month.\n\n**--period** _PERIOD_\n> Time period filter.\n\n**--help**\n> Display help information."],["DESCRIPTION","**ledger** is a plain text accounting tool. It processes double-entry accounting journals.\n\nThe tool generates reports from simple text files. It supports multiple currencies and commodities."],["CAVEATS","Text file format specific. Learning curve. Powerful query language."],["HISTORY","Ledger was created by **John Wiegley** as a powerful plain text accounting system for command-line users."],["INSTALL","```apt: sudo apt install ledger```\n\n```dnf: sudo dnf install ledger```\n\n```pacman: sudo pacman -S ledger```\n\n```apk: sudo apk add ledger```\n\n```zypper: sudo zypper install ledger```\n\n```brew: brew install ledger```\n\n```nix: nix profile install nixpkgs#ledger```"],["SEE ALSO","[hledger](/man/hledger)(1)"]],"e":["hledger"],"x":"ledger -f [journal.ledger] balance"},"lvmpolld":{"n":"lvmpolld","t":"LVM poll daemon that supervises long-running LVM operations","s":[["TAGLINE","LVM poll daemon that supervises long-running LVM operations"],["TLDR","Start the daemon in the **foreground**\n\n```lvmpolld -f```\n\nStart in the foreground with **debug logging**\n\n```lvmpolld -f -l debug```\n\nSet the **idle shutdown timeout** (seconds)\n\n```lvmpolld -t [300]```\n\nUse a **custom socket** path\n\n```lvmpolld -s [/tmp/lvmpolld.socket]```\n\nUse a **custom PID file**\n\n```lvmpolld -p [/tmp/lvmpolld.pid]```\n\n**Dump the current state**\n\n```lvmpolld --dump```"],["SYNOPSIS","**lvmpolld** [_options_]"],["PARAMETERS","**-f**, **--foreground**\n> Run in the foreground instead of daemonizing\n\n**-l**, **--log** _level_\n> Set log level (debug, info, warn, error)\n\n**-t**, **--timeout** _seconds_\n> Set idle shutdown timeout; daemon exits after being idle this long\n\n**-s**, **--socket** _path_\n> Use a custom Unix socket path for communication\n\n**-p**, **--pidfile** _path_\n> Use a custom PID file location\n\n**--dump**\n> Dump the current state of the daemon and exit"],["DESCRIPTION","**lvmpolld** is the LVM poll daemon that supervises long-running LVM operations such as pvmove, lvconvert mirroring, and thin pool extension. It monitors these operations and updates their progress, allowing the initiating command to return while work continues in the background.\n\nWhen an LVM command starts a polled operation, lvmpolld tracks its completion status. The daemon starts automatically when needed and shuts down after a configurable idle period.\n\nThe daemon communicates via a Unix socket, typically at /run/lvm/lvmpolld.socket, and maintains state about active operations."],["CAVEATS","Usually started automatically by LVM commands; manual invocation is rarely needed. If the daemon dies during an active operation, restarting the original command will resume polling. The **--dump** option is useful for debugging active operations."],["INSTALL","```apt: sudo apt install lvm2```\n\n```dnf: sudo dnf install lvm2```\n\n```pacman: sudo pacman -S lvm2```\n\n```apk: sudo apk add lvm2-lockd```\n\n```zypper: sudo zypper install lvm2```\n\n```nix: nix profile install nixpkgs#lvm2```"],["SEE ALSO","[lvm](/man/lvm)(1), [pvmove](/man/pvmove)(1), [lvconvert](/man/lvconvert)(1), [lvmconfig](/man/lvmconfig)(1)"]],"e":["lvm","pvmove","lvconvert","lvmconfig"],"x":"lvmpolld -f"},"mkfs.minix":{"n":"mkfs.minix","t":"creates a Minix filesystem on a device, typically a hard disk partition","s":[["TAGLINE","creates a Minix filesystem on a device, typically a hard disk partition"],["TLDR","Create a **Minix filesystem** inside partition Y on device X\n\n```sudo mkfs.minix [/dev/sdXY]```\n\nCreate a Minix filesystem and **check for bad blocks**\n\n```sudo mkfs.minix -c [/dev/sdXY]```\n\nCreate a **Minix version 2** filesystem\n\n```sudo mkfs.minix -2 [/dev/sdXY]```\n\nCreate a **Minix version 3** filesystem\n\n```sudo mkfs.minix -3 [/dev/sdXY]```\n\nSpecify **maximum filename length**\n\n```sudo mkfs.minix -n [30] [/dev/sdXY]```\n\nCreate with specific **number of inodes**\n\n```sudo mkfs.minix -i [number] [/dev/sdXY]```"],["SYNOPSIS","**mkfs.minix** [**-c**] [**-n** _namelength_] [**-i** _inodes_] [**-l** _badblocks_] [**-1**|**-2**|**-3**] _device_ [_size-in-blocks_]"],["PARAMETERS","**-c, --check**\n> Check device for bad blocks before creating filesystem\n\n**-n, --namelength _length_**\n> Maximum filename length; 14 or 30 for versions 1-2, 60 for version 3; default is 30\n\n**-i, --inodes _number_**\n> Number of inodes to create for the filesystem\n\n**-l, --badblocks _filename_**\n> Read bad blocks list from file (one block number per line)\n\n**-1**\n> Create Minix version 1 filesystem; this is the original default and remains the default in many distributions.\n\n**-2**, **-v**\n> Create Minix version 2 filesystem (supports larger files).\n\n**-3**\n> Create Minix version 3 filesystem (supports filenames up to 60 characters).\n\n**--lock[=_mode_]**\n> Use exclusive BSD locking; mode can be yes, no, or nonblock\n\n**-h, --help**\n> Display help information\n\n**-V, --version**\n> Display version information"],["DESCRIPTION","**mkfs.minix** creates a Minix filesystem on a device, typically a hard disk partition. Minix is a simple, educational filesystem originally designed for the Minix operating system.\n\nThe optional size-in-blocks parameter specifies filesystem size; if omitted, size is determined automatically. Block counts must be greater than 10 and less than 65536. Version 1 is the default and most compatible; version 2 adds larger file support; version 3 extends filename length to 60 characters."],["CAVEATS","The Minix filesystem has significant limitations: maximum filesystem size of 64MB for version 1, filename restrictions, and no journaling. It is primarily used for educational purposes, boot floppies, or embedded systems requiring minimal overhead. Not recommended for production use with modern storage."],["HISTORY","The Minix filesystem was created by **Andrew S. Tanenbaum** for the **Minix operating system** in **1987**. It served as a teaching tool and directly influenced the development of the Linux kernel. Linus Torvalds initially developed Linux using Minix, and the early Linux filesystem was based on Minix. The mkfs.minix utility is part of **util-linux**."],["INSTALL","```apt: sudo apt install util-linux-extra```\n\n```apk: sudo apk add util-linux-misc```"],["SEE ALSO","[mkfs](/man/mkfs)(8), [fsck](/man/fsck)(8)"]],"e":["mkfs","fsck"],"x":"sudo mkfs.minix [/dev/sdXY]"},"natural-selection-2":{"n":"natural-selection-2","t":"dedicated server runs a headless game server for the asymmetric multiplayer","s":[["TAGLINE","dedicated server runs a headless game server for the asymmetric multiplayer"],["TLDR","Start a server with **default settings**\n\n```[path/to]/server_linux```\n\nGive a server a **custom name** that shows in the server browser\n\n```[path/to]/server_linux -name '[server_name]'```\n\nSpecify a **connection port** for the server\n\n```[path/to]/server_linux -port [27015]```\n\nSpecify **maximum player count**\n\n```[path/to]/server_linux -limit [2..24]```\n\nSpecify the **initial map** the server starts on\n\n```[path/to]/server_linux -map [ns2_summit]```\n\nLimit access to the server with a **password**\n\n```[path/to]/server_linux -password [password]```\n\nStart a server with **webui admin** interface\n\n```[path/to]/server_linux -webadmin -webport [8080]```"],["SYNOPSIS","**server_linux** [_options_]"],["PARAMETERS","**-name _name_**\n> Server name displayed in the browser\n\n**-port _port_**\n> UDP port for client connections (default: 27015)\n\n**-limit _n_**\n> Maximum players (2-24)\n\n**-map _mapname_**\n> Initial map to load\n\n**-password _pass_**\n> Server password for private games\n\n**-webadmin**\n> Enable web administration interface\n\n**-webport _port_**\n> Port for web admin interface\n\n**-config _file_**\n> Custom configuration file"],["DESCRIPTION","**Natural Selection 2** dedicated server runs a headless game server for the asymmetric multiplayer FPS/RTS hybrid. Players join as either Marines or Aliens, with commanders directing team strategy in real-time.\n\nThe Linux server binary (server_linux) is provided through SteamCMD or the game's dedicated server tools. It manages game state, player connections, and map rotation for online matches.\n\nServer configuration can be done through command-line arguments or configuration files in the server directory."],["CAVEATS","Requires sufficient system resources (CPU, RAM, network bandwidth) for smooth gameplay. Port forwarding may be needed for public servers. Server files must be obtained through Steam. Performance depends heavily on player count and map complexity."],["SEE ALSO","[steamcmd](/man/steamcmd)(1)"]],"e":["steamcmd"],"x":"[path/to]/server_linux"},"neo4j-admin":{"n":"neo4j-admin","t":"administers Neo4j databases","s":[["TAGLINE","administers Neo4j databases"],["TLDR","**Dump a database** to a file (database must be stopped)\n\n```neo4j-admin database dump --to-path=[/backups] [neo4j]```\n\n**Load a database** from a dump file\n\n```neo4j-admin database load --from-path=[/backups] [neo4j]```\n\n**Set the initial admin password**\n\n```neo4j-admin dbms set-initial-password [password]```\n\n**Check database consistency**\n\n```neo4j-admin database check [neo4j]```\n\n**Bulk import CSV data** into a new database\n\n```neo4j-admin database import full --nodes=[nodes.csv] --relationships=[rels.csv] [neo4j]```\n\n**Show memory recommendations** for heap and pagecache\n\n```neo4j-admin server memory-recommendation```"],["SYNOPSIS","**neo4j-admin** _command_ [_options_]"],["PARAMETERS","_COMMAND_\n> Administration command category: database, dbms, or server.\n\n**database dump**\n> Dump a database to a single-file archive for offline backup.\n\n**database load**\n> Load a database from a dump file.\n\n**database import full**\n> Bulk import data from CSV files into a new database.\n\n**database check**\n> Check database consistency and integrity.\n\n**dbms set-initial-password**\n> Set the initial admin password before first startup.\n\n**server memory-recommendation**\n> Print heuristic memory recommendations for heap and pagecache.\n\n**--additional-config** _file_\n> Provide additional configuration from a file.\n\n**--expand-commands**\n> Allow command expansion in config values.\n\n**--verbose**\n> Enable verbose output.\n\n**--help**\n> Display help information."],["DESCRIPTION","**neo4j-admin** is the administration tool for Neo4j graph databases. It handles backup and restore (dump/load), database consistency checking, bulk data import, memory configuration recommendations, and initial setup tasks.\n\nIn Neo4j 5+, commands are organized into categories: **database** (per-database operations like dump, load, import, check), **dbms** (system-wide operations like setting the initial password), and **server** (server configuration like memory recommendations). Some operations require the database to be stopped first."],["CAVEATS","Some operations (dump, load, import) require the database to be stopped first. Admin privileges are typically required. Command syntax changed significantly between Neo4j 4.x and 5.x; the examples here use Neo4j 5+ syntax."],["HISTORY","neo4j-admin has been part of the Neo4j distribution since its early releases. The command structure was significantly reorganized in **Neo4j 5** (2022), moving from flat commands (e.g., `dump`, `memrec`) to categorized subcommands (e.g., `database dump`, `server memory-recommendation`)."],["INSTALL","```apk: sudo apk add neo4j```\n\n```brew: brew install neo4j```\n\n```nix: nix profile install nixpkgs#neo4j```"],["SEE ALSO","[neo4j](/man/neo4j)(1), [cypher-shell](/man/cypher-shell)(1)"]],"e":["neo4j","cypher-shell"],"x":"neo4j-admin database dump --to-path=[/backups] [neo4j]"},"next":{"n":"next","t":"Next.js command-line interface","s":[["TAGLINE","Next.js command-line interface"],["TLDR","**Create a new Next.js app** (uses create-next-app)\n\n```npx create-next-app@latest [app-name]```\n\n**Start development server**\n\n```next dev```\n\n**Build for production**\n\n```next build```\n\n**Start production server**\n\n```next start```\n\n**Run linter**\n\n```next lint```\n\n**Print environment info** for bug reports\n\n```next info```\n\n**Enable or disable telemetry**\n\n```next telemetry --disable```"],["SYNOPSIS","**next** [_command_] [_options_]"],["PARAMETERS","**dev** [_directory_]\n> Start development server with Hot Module Reloading.\n\n**build** [_directory_]\n> Create an optimized production build.\n\n**start** [_directory_]\n> Start the production server (requires **build** first).\n\n**lint**\n> Run ESLint on the project.\n\n**info**\n> Print system, binary, and package versions (for bug reports).\n\n**telemetry** **--enable** | **--disable**\n> Enable or disable anonymous telemetry.\n\n**-p**, **--port** _port_\n> Port to listen on (default: 3000). Applies to **dev** and **start**.\n\n**-H**, **--hostname** _host_\n> Hostname to bind to (default: 0.0.0.0).\n\n**--turbopack**\n> Force enable Turbopack (default in recent versions).\n\n**--webpack**\n> Use Webpack instead of Turbopack.\n\n**--experimental-https**\n> Serve **dev** over HTTPS using a self-signed certificate.\n\n**-h**, **--help**\n> Display help information.\n\n**-v**, **--version**\n> Display Next.js version."],["DESCRIPTION","**next** is the Next.js CLI for developing, building, and serving React applications. It provides file-based routing, server-side rendering, static generation, and API routes out of the box.\n\nRunning **next** with no command is an alias for **next dev**. Configuration lives in **next.config.js** (or **.mjs**/**.ts**). Starting with Next 13 the default output directory is **.next**."],["CAVEATS","Requires Node.js. The standalone **next export** command was removed in Next.js 14 in favor of `output: 'export'` in **next.config.js**. Linting as part of **next build** is being phased out starting in Next.js 15.5."],["HISTORY","Next.js was created by **Vercel** (then ZEIT) and released in **2016** as a React framework for production applications. Later versions introduced App Router, React Server Components, and Turbopack."],["INSTALL","```apk: sudo apk add mmh```\n\n```nix: nix profile install nixpkgs#mmh```"],["SEE ALSO","[npm](/man/npm)(1), [vercel](/man/vercel)(1), [node](/man/node)(1)"]],"e":["npm","vercel","node"],"x":"npx create-next-app@latest [app-name]"},"nft":{"n":"nft","t":"command-line interface for nftables packet filtering","s":[["TAGLINE","command-line interface for nftables packet filtering"],["TLDR","**List current ruleset**\n\n```sudo nft list ruleset```\n\n**Add a table**\n\n```sudo nft add table inet [filter]```\n\n**Add a chain with a hook**\n\n```sudo nft add chain inet [filter] [input] \\{ type filter hook input priority 0 \\; policy accept \\; \\}```\n\n**Add a rule to accept specific ports**\n\n```sudo nft add rule inet [filter] [input] tcp dport \\{ ssh, http, https \\} accept```\n\n**Delete a rule by handle**\n\n```sudo nft delete rule inet [filter] [input] handle [3]```\n\n**Flush all rules**\n\n```sudo nft flush ruleset```\n\n**Save ruleset to file**\n\n```sudo nft list ruleset > /etc/nftables.conf```"],["SYNOPSIS","**nft** [_OPTIONS_] [_commands_]"],["DESCRIPTION","**nft** is the command-line interface for nftables, the modern Linux kernel packet filtering framework that replaces iptables, ip6tables, arptables, and ebtables with a unified syntax and improved performance.\n\nnftables uses a hierarchy of tables, chains, and rules. Address families include **inet** (IPv4+IPv6), **ip** (IPv4 only), **ip6** (IPv6 only), **bridge**, **arp**, and **netdev**. Rules can be loaded from files using `nft -f /etc/nftables.conf`."],["PARAMETERS","**list ruleset**\n> Display the current complete ruleset\n\n**add table** _family_ _name_\n> Create a new table\n\n**add chain** _family_ _table_ _chain_ _specification_\n> Create a new chain\n\n**add rule** _family_ _table_ _chain_ _statement_\n> Add a rule to a chain\n\n**delete rule** _family_ _table_ _chain_ **handle** _n_\n> Delete a rule by its handle\n\n**flush ruleset**\n> Remove all rules and tables\n\n**--handle**\n> Show rule handles for deletion\n\n**--numeric**\n> Show numeric output (no name resolution)"],["CAVEATS","Nftables syntax differs significantly from iptables. Tables must exist before chains, chains before rules. Changes are immediate but not persistent by default - save to /etc/nftables.conf for persistence."],["HISTORY","**nft** is part of the **nftables** project, the successor to iptables, providing unified packet classification framework in the Linux kernel."],["INSTALL","```apt: sudo apt install nftables```\n\n```dnf: sudo dnf install nftables```\n\n```pacman: sudo pacman -S nftables```\n\n```apk: sudo apk add nftables```\n\n```zypper: sudo zypper install nftables```\n\n```brew: brew install nftables```\n\n```nix: nix profile install nixpkgs#nftables```"],["SEE ALSO","[iptables](/man/iptables)(8), [firewall-cmd](/man/firewall-cmd)(1)"]],"e":["iptables","firewall-cmd"],"x":"sudo nft list ruleset"},"nh":{"n":"nh","t":"nix helper tool","s":[["TAGLINE","nix helper tool"],["TLDR","**Build NixOS configuration**\n\n```nh os switch```\n\n**Build home-manager**\n\n```nh home switch```\n\n**Build with flake**\n\n```nh os switch --flake [.]```\n\n**Dry run build**\n\n```nh os switch --dry```\n\n**Clean old generations**\n\n```nh clean all```\n\n**Search packages**\n\n```nh search [package]```"],["SYNOPSIS","**nh** [_command_] [_options_]"],["PARAMETERS","_COMMAND_\n> nh subcommand.\n\n**os** _ACTION_\n> NixOS operations.\n\n**home** _ACTION_\n> home-manager operations.\n\n**clean** _TARGET_\n> Clean generations.\n\n**search** _QUERY_\n> Search packages.\n\n**--flake** _PATH_\n> Use flake at path.\n\n**--help**\n> Display help information."],["DESCRIPTION","**nh** is a Nix helper tool. It simplifies NixOS and home-manager operations.\n\nThe tool wraps common Nix commands. Provides better output and UX."],["CAVEATS","Requires Nix/NixOS. Rust-based. Wrapper around nixos-rebuild."],["HISTORY","nh was created to provide a **better interface** for common NixOS and home-manager operations."],["INSTALL","```nix: nix profile install nixpkgs#nh```"],["SEE ALSO","[nix](/man/nix)(1), [nixos-rebuild](/man/nixos-rebuild)(1), [home-manager](/man/home-manager)(1)"]],"e":["nix","nixos-rebuild","home-manager"],"x":"nh os switch"},"nvrs":{"n":"nvrs","t":"Fast new version checker for software releases","s":[["TAGLINE","Fast new version checker for software releases"],["TLDR","**Check for new versions**\n\n```nvrs```\n\n**Check a specific package**\n\n```nvrs check [package_name]```"],["SYNOPSIS","**nvrs** [_command_] [_options_]"],["DESCRIPTION","**nvrs** is a CLI tool for tracking and updating software versions. It checks package updates across multiple sources including GitHub, GitLab, AUR, crates.io, and regex-defined URLs.\n\nBuilt for speed, it can monitor many packages in parallel and report which ones have newer versions available."],["CAVEATS","Requires configuration of package sources. API rate limits may apply for some sources like GitHub."],["HISTORY","**nvrs** is written in **Rust**."],["INSTALL","```nix: nix profile install nixpkgs#nvrs```"],["SEE ALSO","[nix](/man/nix)(1)"]],"e":["nix"],"x":"nvrs"},"omega-rpg":{"n":"omega-rpg","t":"complex roguelike RPG with city and dungeon exploration","s":[["TAGLINE","complex roguelike RPG with city and dungeon exploration"],["TLDR","**Start a game**\n\n```omega```"],["SYNOPSIS","**omega**"],["PREVIEW","```\n ########\n #......#   CITY\n #..@...# [Tavern]\n #......# [Guild]\n #...>..# [Temple]\n ########\n```"],["DESCRIPTION","**omega** is a complex roguelike RPG from the **1980s** featuring both dungeon crawling and city exploration. Unlike most roguelikes, the game has a persistent overworld with cities containing guilds, temples, shops, and other locations.\n\nThe game offers multiple paths to victory through combat, magic, or diplomacy. Characters can join guilds, worship deities, and take on quests. Movement uses hjklyubn keys."],["CAVEATS","Very complex with many keyboard commands. Permadeath applies. The learning curve is steep even for roguelike veterans."],["HISTORY","**omega** was written by **Laurence Brothers** starting in **1987**. It was one of the most ambitious roguelikes of its era, featuring a scope far beyond typical dungeon crawlers."],["SEE ALSO","[rogue](/man/rogue)(6), [nethack-console](/man/nethack-console)(6), [angband](/man/angband)(6)"]],"e":["rogue","nethack-console","angband"],"x":"omega"},"openscad":{"n":"openscad","t":"Script-based 3D CAD modeler for parametric design","s":[["TAGLINE","Script-based 3D CAD modeler for parametric design"],["TLDR","**Open file in GUI**\n\n```openscad [model.scad]```\n\n**Render to STL**\n\n```openscad -o [output.stl] [model.scad]```\n\n**Render to PNG**\n\n```openscad -o [output.png] [model.scad]```\n\n**Set parameter**\n\n```openscad -D \"[var=value]\" -o [output.stl] [model.scad]```\n\n**Preview mode image export**\n\n```openscad --preview -o [output.png] [model.scad]```\n\n**Export with camera angle and image size**\n\n```openscad -o [output.png] --camera [0,0,0,25,0,35,500] --imgsize [1920,1080] [model.scad]```\n\n**Auto-center and fit object in exported image**\n\n```openscad -o [output.png] --autocenter --viewall [model.scad]```"],["SYNOPSIS","**openscad** [_options_] [_file_]"],["PARAMETERS","_FILE_\n> OpenSCAD source file.\n\n**-o** _OUTPUT_\n> Output file.\n\n**-D** _ASSIGNMENT_\n> Set variable value.\n\n**--preview**\n> Preview render mode.\n\n**--render**\n> Full render mode (CGAL).\n\n**--camera** _params_\n> Camera parameters: translate_x,y,z,rot_x,y,z,dist or eye_x,y,z,center_x,y,z.\n\n**--imgsize** _x,y_\n> PNG image dimensions.\n\n**--projection** _type_\n> Projection type: ortho or perspective.\n\n**--autocenter**\n> Adjust camera to look at object's center.\n\n**--viewall**\n> Adjust camera to fit the entire object.\n\n**--colorscheme** _scheme_\n> Color scheme for rendering.\n\n**-v**, **--version**\n> Display version information.\n\n**-h**, **--help**\n> Display help information."],["DESCRIPTION","**openscad** is a script-based 3D CAD modeler for creating parametric models using Constructive Solid Geometry (CSG). Unlike interactive CAD tools, models are defined programmatically in a scripting language. It can export to STL, OFF, AMF, 3MF, DXF, SVG, and PNG formats."],["CAVEATS","Full CGAL rendering can be very slow for complex models. The -o output format is determined by the file extension. Preview mode is faster but less accurate than full render."],["HISTORY","**OpenSCAD** was created by Marius Kintel and Clifford Wolf, first released in **2010** as an open-source tool for script-based 3D CAD modeling using Constructive Solid Geometry."],["INSTALL","```apt: sudo apt install openscad```\n\n```dnf: sudo dnf install openscad```\n\n```pacman: sudo pacman -S openscad```\n\n```apk: sudo apk add openscad```\n\n```zypper: sudo zypper install openscad```\n\n```nix: nix profile install nixpkgs#openscad```"],["SEE ALSO","[freecad](/man/freecad)(1), [blender](/man/blender)(1), [meshlab](/man/meshlab)(1)"]],"e":["freecad","blender","meshlab"],"x":"openscad [model.scad]"},"p4":{"n":"p4","t":"perforce client","s":[["TAGLINE","perforce client"],["TLDR","**Sync workspace**\n\n```p4 sync```\n\n**Check out file**\n\n```p4 edit [file]```\n\n**Submit changes**\n\n```p4 submit```\n\n**View changes**\n\n```p4 changes```\n\n**Revert file**\n\n```p4 revert [file]```\n\n**Show file status**\n\n```p4 status```"],["SYNOPSIS","**p4** [_command_] [_options_]"],["PARAMETERS","**sync**\n> Sync workspace.\n\n**edit** _FILE_\n> Check out file.\n\n**submit**\n> Submit changelist.\n\n**changes**\n> List changelists.\n\n**revert** _FILE_\n> Revert changes.\n\n**status**\n> Show file status.\n\n**--help**\n> Display help information."],["DESCRIPTION","**p4** is the Perforce client. Manages version control with central server.\n\nThe tool provides enterprise version control. Handles large binary files."],["CAVEATS","Requires Perforce server. Commercial product. Enterprise focused."],["HISTORY","Perforce was created in **1995** as an enterprise **version control system**."],["INSTALL","```nix: nix profile install nixpkgs#p4```"],["SEE ALSO","[git](/man/git)(1), [svn](/man/svn)(1)"]],"e":["git","svn"],"x":"p4 sync"},"pacseek":{"n":"pacseek","t":"TUI for browsing and searching Arch Linux packages","s":[["TAGLINE","TUI for browsing and searching Arch Linux packages"],["TLDR","**Launch the package browser**\n\n```pacseek```"],["SYNOPSIS","**pacseek** [_options_]"],["DESCRIPTION","**pacseek** is a terminal user interface for browsing and searching Arch Linux package databases and the AUR. You can install or remove packages directly from the interface. It supports search by name or description, auto-suggest, sortable results, PKGBUILD display, upgrade detection, and a news feed."],["CAVEATS","Only works on Arch Linux and Arch-based distributions. Uses yay as the default AUR helper but is configurable for other helpers."],["HISTORY","**pacseek** was created by **moson-mo** and is written in **Go**."],["INSTALL","```aur: yay -S pacseek```"],["SEE ALSO","[pacman](/man/pacman)(8), [pacsea](/man/pacsea)(1), [paru](/man/paru)(1), [yay](/man/yay)(1)"]],"e":["pacman","pacsea","paru","yay"],"x":"pacseek"},"partprobe":{"n":"partprobe","t":"informs the operating system kernel of partition table changes","s":[["TAGLINE","informs the operating system kernel of partition table changes"],["TLDR","**Notify** kernel of partition changes\n\n```sudo partprobe```\n\nShow **summary** of changes\n\n```sudo partprobe -s```\n\n**Dry run** with summary\n\n```sudo partprobe -d -s```\n\n**Probe a specific device**\n\n```sudo partprobe /dev/[sda]```"],["SYNOPSIS","**partprobe** [_OPTIONS_] [_device_...]"],["DESCRIPTION","**partprobe** informs the operating system kernel of partition table changes. After modifying partitions using fdisk, parted, or similar tools, partprobe updates the kernel's partition table without requiring a reboot."],["PARAMETERS","**-d, --dry-run**\n> Don't actually inform the kernel, just print what would be done\n\n**-s, --summary**\n> Show a summary of devices and their partitions\n\n**-h, --help**\n> Show help message\n\n**-v, --version**\n> Show version"],["CAVEATS","Cannot update partitions that are in use or mounted. The kernel may refuse to update the partition table if any partition on the device is busy. In such cases, a reboot may be required."],["HISTORY","**partprobe** is part of the **parted** package, providing partition table synchronization with the kernel."],["INSTALL","```apt: sudo apt install parted```\n\n```dnf: sudo dnf install parted```\n\n```pacman: sudo pacman -S parted```\n\n```apk: sudo apk add parted```\n\n```zypper: sudo zypper install parted```\n\n```nix: nix profile install nixpkgs#parted```"],["SEE ALSO","[parted](/man/parted)(8), [fdisk](/man/fdisk)(8), [lsblk](/man/lsblk)(8)"]],"e":["parted","fdisk","lsblk"],"x":"sudo partprobe"},"pfetch":{"n":"pfetch","t":"displays system information with ASCII art","s":[["TAGLINE","displays system information with ASCII art"],["TLDR","**Show system info**\n\n```pfetch```\n\n**Show with custom ASCII art**\n\n```PF_ASCII=\"[arch]\" pfetch```\n\n**Show specific info fields**\n\n```PF_INFO=\"[ascii title os kernel uptime]\" pfetch```\n\n**Disable colors**\n\n```PF_COLOR=0 pfetch```\n\n**Custom separator**\n\n```PF_SEP=\": \" pfetch```"],["SYNOPSIS","**pfetch**"],["ENVIRONMENT","**PF_INFO**\n> Fields to display (space-separated).\n\n**PF_ASCII**\n> ASCII art to use (distro name or \"off\").\n\n**PF_COLOR**\n> Enable colors (1) or disable (0).\n\n**PF_SEP**\n> Separator between label and value.\n\n**PF_COL1**\n> Color for labels.\n\n**PF_COL2**\n> Color for info.\n\n**PF_COL3**\n> Color for title."],["AVAILABLE INFO FIELDS","**ascii**\n> Distro ASCII art.\n\n**title**\n> Username@hostname.\n\n**os**\n> Operating system.\n\n**host**\n> Machine model.\n\n**kernel**\n> Kernel version.\n\n**uptime**\n> System uptime.\n\n**pkgs**\n> Package count.\n\n**memory**\n> RAM usage.\n\n**shell**\n> Current shell.\n\n**editor**\n> Default editor.\n\n**wm**\n> Window manager.\n\n**palette**\n> Color palette."],["DESCRIPTION","**pfetch** displays system information with ASCII art. It's a minimal, fast alternative to neofetch and screenfetch.\n\nThe tool is a single POSIX shell script with no dependencies. It works on Linux, BSD, macOS, and other Unix-like systems.\n\nConfiguration uses environment variables. Set PF_INFO to control displayed fields. Set PF_ASCII to use different distro logos.\n\nOutput is colorful by default, suitable for screenshots and ricing. Colors can be disabled for logging or minimal environments.\n\nDetection covers many distributions, package managers, window managers, and desktop environments automatically.\n\nThe minimal design prioritizes speed over features. It starts instantly compared to heavier alternatives."],["CAVEATS","Fewer features than neofetch. Some obscure systems may not be detected. ASCII art limited compared to alternatives."],["HISTORY","**pfetch** was created by **Dylan Araps** around **2019** as a minimal system info tool. The same author created neofetch, with pfetch being a stripped-down alternative for those preferring simplicity."],["INSTALL","```nix: nix profile install nixpkgs#pfetch```"],["SEE ALSO","[neofetch](/man/neofetch)(1), [screenfetch](/man/screenfetch)(1), [fastfetch](/man/fastfetch)(1), [macchina](/man/macchina)(1)"]],"e":["neofetch","screenfetch","fastfetch","macchina"],"x":"pfetch"},"phpcpd":{"n":"phpcpd","t":"finds duplicate code in PHP projects","s":[["TAGLINE","finds duplicate code in PHP projects"],["TLDR","**Find duplicate code**\n\n```phpcpd [src/]```\n\n**Set minimum lines**\n\n```phpcpd --min-lines [10] [src/]```\n\n**Set minimum tokens**\n\n```phpcpd --min-tokens [50] [src/]```\n\n**Exclude directory**\n\n```phpcpd --exclude [vendor] [src/]```\n\n**Output to file**\n\n```phpcpd --log-pmd [report.xml] [src/]```\n\n**Check multiple directories**\n\n```phpcpd [src/] [lib/]```\n\n**Fuzzy matching**\n\n```phpcpd --fuzzy [src/]```"],["SYNOPSIS","**phpcpd** [_--min-lines n_] [_--min-tokens n_] [_--exclude dir_] [_options_] _directories_"],["PARAMETERS","**--min-lines** _N_\n> Minimum lines for duplication.\n\n**--min-tokens** _N_\n> Minimum tokens for duplication.\n\n**--exclude** _DIR_\n> Exclude directory.\n\n**--log-pmd** _FILE_\n> PMD-CPD XML format.\n\n**--fuzzy**\n> Fuzzy matching.\n\n**--suffix** _EXT_\n> File suffix to check.\n\n**-v**, **--verbose**\n> Verbose output.\n\n**--help**\n> Show help."],["DESCRIPTION","**phpcpd** (PHP Copy/Paste Detector) finds duplicate code in PHP projects. It identifies code that should be refactored.\n\nToken-based analysis finds similar code blocks. Renaming variables doesn't hide duplications.\n\nThresholds control sensitivity. Higher minimums find larger, more significant duplications.\n\nFuzzy matching finds near-duplicates. Code with minor differences is also detected.\n\nReports show file locations and duplicated content. Integration with CI tools enables automated checks."],["CAVEATS","Similar but intentionally different code may be flagged. Very large codebases may be slow. **Note:** phpcpd has been archived by its author and is no longer actively maintained. Consider alternative static analysis tools for new projects."],["HISTORY","**phpcpd** was created by **Sebastian Bergmann** as part of the PHP QA toolset. It helps maintain code quality by detecting copy-pasted code blocks."],["INSTALL","```dnf: sudo dnf install phpcpd```"],["SEE ALSO","[phpcs](/man/phpcs)(1), [phpcbf](/man/phpcbf)(1), [phpmd](/man/phpmd)(1), [phpstan](/man/phpstan)(1)"]],"e":["phpcs","phpcbf","phpmd","phpstan"],"x":"phpcpd [src/]"},"phploc":{"n":"phploc","t":"measures PHP project size and complexity","s":[["TAGLINE","measures PHP project size and complexity"],["TLDR","**Measure project size**\n\n```phploc [src/]```\n\n**Output as CSV**\n\n```phploc --log-csv [stats.csv] [src/]```\n\n**Exclude a directory**\n\n```phploc --exclude [vendor] [.]```\n\n**Count tests**\n\n```phploc --count-tests [src/]```\n\n**Analyze specific file suffixes**\n\n```phploc --suffix [php] --suffix [inc] [src/]```\n\n**Exclude multiple directories**\n\n```phploc --exclude [vendor] --exclude [tests] [src/]```"],["SYNOPSIS","**phploc** [_options_] _directories_..."],["PARAMETERS","_DIRECTORIES_\n> One or more directories to analyze.\n\n**--log-csv** _FILE_\n> Write results in CSV format to file.\n\n**--log-xml** _FILE_\n> Write results in XML format to file.\n\n**--exclude** _DIR_\n> Exclude directory from analysis. Can be specified multiple times.\n\n**--suffix** _SUFFIX_\n> File suffix to include (default: php). Can be specified multiple times.\n\n**--count-tests**\n> Include test-related metrics in the output.\n\n**--names** _NAMES_\n> Comma-separated list of file names to include.\n\n**--names-exclude** _NAMES_\n> Comma-separated list of file names to exclude.\n\n**--help**\n> Display help information.\n\n**--version**\n> Display version information."],["DESCRIPTION","**phploc** measures the size and complexity of a PHP project. It reports metrics including lines of code (LOC), comment lines, non-comment lines, number of classes, methods, functions, interfaces, traits, namespaces, constants, and cyclomatic complexity.\n\nThe tool scans directories recursively for PHP files and generates a summary of structural metrics. It is part of the PHP QA toolchain created by Sebastian Bergmann."],["CAVEATS","Phploc has been **archived** by its author and is no longer actively maintained. It analyzes directories recursively by default. Only files with the `.php` suffix are scanned unless changed with `--suffix`."],["HISTORY","PHPLOC was created by **Sebastian Bergmann**, also known for PHPUnit, as part of the PHP quality assurance toolkit."],["INSTALL","```aur: yay -S phploc```"],["SEE ALSO","[cloc](/man/cloc)(1), [phpmd](/man/phpmd)(1), [phpcs](/man/phpcs)(1), [sloccount](/man/sloccount)(1)"]],"e":["cloc","phpmd","phpcs","sloccount"],"x":"phploc [src/]"},"poetry-new":{"n":"poetry-new","t":"Create new Python project structure","s":[["TAGLINE","Create new Python project structure"],["TLDR","**Create new project**\n\n```poetry new [project-name]```\n\n**Create with specific source directory**\n\n```poetry new --src [project-name]```\n\n**Create with custom name**\n\n```poetry new --name [package-name] [directory]```"],["SYNOPSIS","**poetry new** [_options_] _path_"],["PARAMETERS","_PATH_\n> Project directory path.\n\n**--name** _NAME_\n> Package name.\n\n**--src**\n> Use src layout.\n\n**--readme** _FORMAT_\n> Readme format (md, rst)."],["DESCRIPTION","**poetry new** scaffolds a new Python project directory with a standard structure including **pyproject.toml**, a package directory, and a tests directory. This provides a ready-to-use project layout.\n\nThe **--src** flag uses the `src/` layout pattern where the package lives under a `src/` subdirectory. The **--name** option allows the package name to differ from the directory name. For adding Poetry to an existing project, use **poetry init** instead."],["CAVEATS","Creates new directory. Use poetry init for existing projects."],["HISTORY","poetry new provides **project scaffolding** for new Python packages."],["INSTALL","```dnf: sudo dnf install poetry```\n\n```apk: sudo apk add poetry```\n\n```brew: brew install poetry```\n\n```nix: nix profile install nixpkgs#poetry```"],["SEE ALSO","[poetry](/man/poetry)(1), [poetry-init](/man/poetry-init)(1)"]],"e":["poetry","poetry-init"],"x":"poetry new [project-name]"},"pueue-switch":{"n":"pueue-switch","t":"Swap queue positions of two pueue tasks","s":[["TAGLINE","Swap queue positions of two pueue tasks"],["TLDR","**Switch task positions in queue**\n\n```pueue switch [task_id1] [task_id2]```"],["SYNOPSIS","**pueue** **switch** _task_id_1_ _task_id_2_"],["DESCRIPTION","**pueue switch** swaps the queue positions of two tasks. Both tasks must be in queued or stashed state (not running or completed). Useful for reordering task execution priority without removing and re-adding tasks."],["INSTALL","```pacman: sudo pacman -S pueue```\n\n```apk: sudo apk add pueue```\n\n```zypper: sudo zypper install pueue```\n\n```brew: brew install pueue```\n\n```nix: nix profile install nixpkgs#pueue```"],["SEE ALSO","[pueue](/man/pueue)(1), [pueue-status](/man/pueue-status)(1), [pueue-enqueue](/man/pueue-enqueue)(1), [pueue-stash](/man/pueue-stash)(1)"]],"e":["pueue","pueue-status","pueue-enqueue","pueue-stash"],"x":"pueue switch [task_id1] [task_id2]"},"pulumi-env":{"n":"pulumi-env","t":"Manage Pulumi ESC environments and secrets","s":[["TAGLINE","Manage Pulumi ESC environments and secrets"],["TLDR","**List all environments**\n\n```pulumi env ls```\n\n**Open and evaluate an environment**\n\n```pulumi env open [org/env]```\n\n**Get a specific value from an environment**\n\n```pulumi env get [org/env] [key]```\n\n**Set a value in an environment**\n\n```pulumi env set [org/env] [key] [value]```\n\n**Initialize a new environment**\n\n```pulumi env init [org/env]```\n\n**Edit an environment definition**\n\n```pulumi env edit [org/env]```\n\n**Remove an environment**\n\n```pulumi env rm [org/env]```\n\n**Run a command with environment variables loaded**\n\n```pulumi env run [org/env] -- [command]```"],["SYNOPSIS","**pulumi** **env** _command_ [_options_]"],["SUBCOMMANDS","**ls**\n> List environments.\n\n**open** _name_\n> Open and evaluate an environment, displaying resolved values.\n\n**get** _name_ _key_\n> Get a specific value from an environment.\n\n**set** _name_ _key_ _value_\n> Set a value in an environment.\n\n**init** _name_\n> Create a new environment.\n\n**edit** _name_\n> Edit an environment definition in an editor.\n\n**rm** _name_\n> Remove an environment.\n\n**run** _name_ **--** _command_\n> Run a command with environment variables set.\n\n**version-tag** _name_\n> Manage environment version tags."],["DESCRIPTION","**pulumi env** manages Pulumi ESC (Environments, Secrets, and Configuration). ESC provides centralized management of configuration and secrets across stacks and applications. Environments can compose other environments and integrate with secret providers like AWS Secrets Manager, HashiCorp Vault, and others."],["INSTALL","```pacman: sudo pacman -S pulumi```\n\n```apk: sudo apk add pulumi```\n\n```brew: brew install pulumi```\n\n```nix: nix profile install nixpkgs#pulumi```"],["SEE ALSO","[pulumi](/man/pulumi)(1), [pulumi-config](/man/pulumi-config)(1), [pulumi-stack](/man/pulumi-stack)(1)"]],"e":["pulumi","pulumi-config","pulumi-stack"],"x":"pulumi env ls"},"pw-midi2record":{"n":"pw-midi2record","t":"Record MIDI 2 (UMP) events through PipeWire","s":[["TAGLINE","Record MIDI 2 (UMP) events through PipeWire"],["TLDR","**Record MIDI 2 input to a UMP file**\n\n```pw-midi2record [path/to/output.midi2]```\n\n**Record from a specific source node**\n\n```pw-midi2record --target [node_id] [path/to/output.midi2]```\n\n**Force the legacy MIDI 1 byte-stream format**\n\n```pw-midi2record --force-midi midi [path/to/output.midi2]```\n\n**Record with verbose output**\n\n```pw-midi2record -v [path/to/output.midi2]```\n\n**Pipe MIDI 2 capture to another program**\n\n```pw-midi2record - | [tool]```"],["SYNOPSIS","**pw-midi2record** [_options_] [_FILE_ | **-**]"],["PARAMETERS","**-h**, **--help**\n> Show help and exit.\n\n**--version**\n> Show version information.\n\n**-v**, **--verbose**\n> Enable verbose output.\n\n**-R**, **--remote** _NAME_\n> Connect to the named PipeWire daemon.\n\n**--target** _VALUE_\n> Source node serial or name. **0** disables linking.\n\n**--latency** _VALUE_\n> Set node latency, e.g. **100ms**, **2048** samples.\n\n**-P**, **--properties** _JSON_\n> Extra stream properties as a JSON object.\n\n**-M**, **--force-midi** _FORMAT_\n> Force MIDI format, **midi** (legacy bytes) or **ump** (Universal MIDI Packets, default).\n\n**-C**, **--monitor**\n> Capture monitor ports of output nodes (when recording)."],["DESCRIPTION","**pw-midi2record** captures MIDI 2 events from the PipeWire graph and writes them to a file. It is an alias of **pw-cat**(1) invoked with the **TYPE_MIDI2** data type, encoding events as **Universal MIDI Packets (UMP)** as defined by the MIDI 2.0 specification.\n\nUMP can carry both legacy MIDI 1 messages and MIDI 2 features (per-note controllers, 32-bit values, 16 groups of 16 channels). A MIDI 2 capable source — controller, software instrument, or upstream PipeWire node — must be connected for events to be recorded.\n\nWhen **-** is given as the file argument, MIDI data is written to standard output."],["CAVEATS","Records MIDI events only, not rendered audio; use **pw-record**(1) to capture the audio output of a synthesiser. Without a connected MIDI source the file will only contain headers. Files produced are UMP, not the legacy SMF (**.mid**) format."],["HISTORY","MIDI 2 support arrived in **PipeWire** alongside the broader pw-cat MIDI 2 (UMP) work, complementing the older **pw-midiplay**/**pw-midirecord** pair. PipeWire was created by **Wim Taymans** at Red Hat and first released in **2017**. The MIDI 2.0 specification was ratified by the MMA in **2020**."],["INSTALL","```apk: sudo apk add pipewire-tools```\n\n```zypper: sudo zypper install pipewire-tools```"],["SEE ALSO","[pw-cat](/man/pw-cat)(1), [pw-midi2play](/man/pw-midi2play)(1), [pw-midiplay](/man/pw-midiplay)(1), [pw-midirecord](/man/pw-midirecord)(1), [pw-mididump](/man/pw-mididump)(1), [pw-record](/man/pw-record)(1), [pipewire](/man/pipewire)(1)"]],"e":["pw-cat","pw-midi2play","pw-midiplay","pw-midirecord","pw-mididump","pw-record","pipewire"],"x":"pw-midi2record [path/to/output.midi2]"},"reader":{"n":"reader","t":"Terminal reader view for cleaner web page content","s":[["TAGLINE","Terminal reader view for cleaner web page content"],["TLDR","**Read a web page in the terminal**\n\n```reader [https://example.com/article]```\n\n**Read a local HTML file**\n\n```reader [path/to/file.html]```"],["SYNOPSIS","**reader** [_options_] _url_or_file_"],["DESCRIPTION","**reader** mimics browser readability or reader view functionality in the terminal. It parses web page content and displays it in an easy-to-read format, stripping away navigation, ads, and other distractions. It can render embedded images as colored block graphics using Sixel and integrates with vi and Neovim editors."],["HISTORY","**reader** was created by **mrusme** and is written in **Go**."],["INSTALL","```apk: sudo apk add reader```\n\n```nix: nix profile install nixpkgs#reader```"],["SEE ALSO","[lynx](/man/lynx)(1), [w3m](/man/w3m)(1), [curl](/man/curl)(1)"]],"e":["lynx","w3m","curl"],"x":"reader [https://example.com/article]"},"sha512sum":{"n":"sha512sum","t":"Compute and verify SHA-512 checksums","s":[["TAGLINE","Compute and verify SHA-512 checksums"],["TLDR","**Calculate the SHA512 checksum** of a file\n\n```sha512sum [path/to/file]```\n\n**Calculate checksums for multiple files**\n\n```sha512sum [file1] [file2] [file3]```\n\n**Calculate and save checksums** to a file\n\n```sha512sum [*.iso] > [checksums.sha512]```\n\n**Verify checksums** from a file\n\n```sha512sum -c [checksums.sha512]```\n\n**Verify silently** (only show failures)\n\n```sha512sum -c --quiet [checksums.sha512]```\n\n**Read from stdin**\n\n```echo -n \"[text]\" | sha512sum```"],["SYNOPSIS","**sha512sum** [_OPTION_]... [_FILE_]..."],["PARAMETERS","**-b**, **--binary**\n> Read in binary mode\n\n**-c**, **--check**\n> Read checksums from FILEs and verify them\n\n**-t**, **--text**\n> Read in text mode (default)\n\n**--tag**\n> Create a BSD-style checksum output\n\n**-z**, **--zero**\n> End each output line with NUL instead of newline\n\n**--ignore-missing**\n> Don't fail for missing files when checking\n\n**--quiet**\n> Don't print OK for each successfully verified file\n\n**--status**\n> Don't output anything; use exit status for result\n\n**--strict**\n> Exit non-zero for improperly formatted checksum lines\n\n**-w**, **--warn**\n> Warn about improperly formatted checksum lines"],["DESCRIPTION","**sha512sum** computes and verifies SHA512 (512-bit) message digests as described in FIPS-180-2. It generates a 128-character hexadecimal hash for file contents, providing strong integrity verification.\n\nWith no FILE argument or when FILE is **-**, sha512sum reads from standard input. Output format is: checksum, space, mode indicator (* for binary, space for text), and filename.\n\nSHA512 provides stronger security than SHA1 or MD5 and is suitable for cryptographic applications. There is no difference between binary and text mode on GNU systems."],["CAVEATS","SHA512 produces significantly longer hashes (128 hex characters) compared to SHA256 (64) or SHA1 (40). While more secure, verification may be slower for large numbers of files compared to weaker algorithms."],["HISTORY","sha512sum is part of **GNU coreutils**, written by Ulrich Drepper, Scott Miller, and David Madore. SHA-512 was published by NIST as part of the SHA-2 family in **2001**, designed to replace the aging SHA-1 algorithm."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils-sha512sum```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[sha256sum](/man/sha256sum)(1), [sha384sum](/man/sha384sum)(1), [sha224sum](/man/sha224sum)(1), [sha1sum](/man/sha1sum)(1), [md5sum](/man/md5sum)(1), [b2sum](/man/b2sum)(1), [shasum](/man/shasum)(1)"]],"e":["sha256sum","sha384sum","sha224sum","sha1sum","md5sum","b2sum","shasum"],"x":"sha512sum [path/to/file]"},"spectre-meltdown-checker":{"n":"spectre-meltdown-checker","t":"Detect CPU speculative execution vulnerabilities","s":[["TAGLINE","Detect CPU speculative execution vulnerabilities"],["TLDR","Check currently **running kernel**\n\n```sudo spectre-meltdown-checker```\n\nCheck with **explanations** of mitigations\n\n```sudo spectre-meltdown-checker --explain```\n\nCheck **specific variants**\n\n```sudo spectre-meltdown-checker --variant [1|2|3|3a|4|l1tf|msbds|taa]```\n\nOutput in **machine-readable format**\n\n```sudo spectre-meltdown-checker --batch [text|json|nrpe|prometheus|short]```\n\nCheck a **non-running kernel**\n\n```sudo spectre-meltdown-checker --kernel [path/to/kernel]```\n\nSkip **/sys interface**\n\n```sudo spectre-meltdown-checker --no-sysfs```\n\n**Update** microcode database\n\n```sudo spectre-meltdown-checker --update-fwdb```"],["SYNOPSIS","**spectre-meltdown-checker** [_OPTIONS_]"],["PARAMETERS","**--kernel** _file_\n> Specify kernel image file to check\n\n**--config** _file_\n> Specify kernel configuration file\n\n**--map** _file_\n> Specify System.map file\n\n**--live**\n> Force live mode when files are specified\n\n**--variant** _variant_\n> Check specific vulnerability variant (repeatable)\n\n**--cve** _cve_\n> Check specific CVE (repeatable)\n\n**--explain**\n> Provide human-readable mitigation explanations\n\n**--batch** _format_\n> Machine-readable output: text, short, json, nrpe, prometheus\n\n**--no-color**\n> Disable color output\n\n**-v, --verbose**\n> Increase verbosity (repeatable)\n\n**--paranoid**\n> Apply stricter mitigation criteria\n\n**--hw-only**\n> Check CPU information only\n\n**--no-sysfs**\n> Skip /sys interface\n\n**--sysfs-only**\n> Use only /sys interface\n\n**--update-fwdb**\n> Update local CPU microcode database"],["DESCRIPTION","**spectre-meltdown-checker** detects CPU vulnerabilities and verifies mitigations for Spectre, Meltdown, and related speculative execution attacks. It operates in live mode (analyzing the running kernel) or offline mode (inspecting kernel files).\n\nThe tool checks for various vulnerability variants including Spectre v1/v2, Meltdown (v3), SSB (v4), L1TF, MDS variants (MSBDS, MFBDS, MLPDS), TAA, and others. It reports vulnerability status and whether proper mitigations are in place."],["RETURN CODES","**0**: Not vulnerable\n**2**: Vulnerable\n**3**: Unknown status\n**255**: Error occurred"],["CAVEATS","Requires root privileges for full analysis. Results depend on kernel version, CPU microcode, and configuration. False positives/negatives possible on unusual configurations. Mitigations may impact performance. Keep the tool updated for new vulnerability checks."],["HISTORY","**spectre-meltdown-checker** was created by **Stephane Lesimple** in response to the Spectre and Meltdown vulnerabilities disclosed in January **2018**. These CPU vulnerabilities affected most modern processors and required coordinated disclosure with Intel, AMD, and ARM. The tool has been continuously updated to detect new speculative execution vulnerabilities."],["INSTALL","```dnf: sudo dnf install spectre-meltdown-checker```\n\n```zypper: sudo zypper install spectre-meltdown-checker```\n\n```nix: nix profile install nixpkgs#spectre-meltdown-checker```"],["SEE ALSO","[lscpu](/man/lscpu)(1), [dmesg](/man/dmesg)(1), [uname](/man/uname)(1)"]],"e":["lscpu","dmesg","uname"],"x":"sudo spectre-meltdown-checker"},"srun":{"n":"srun","t":"Run parallel jobs under Slurm","s":[["TAGLINE","Run parallel jobs under Slurm"],["TLDR","**Submit** a basic interactive job\n\n```srun --pty /bin/bash```\n\n**Submit** an interactive job with custom resources\n\n```srun --ntasks-per-node [num_cores] --mem-per-cpu [memory_MB] --pty /bin/bash```\n\n**Run** a job on a specific partition with a time limit\n\n```srun -p [partition_name] -t [HH:MM:SS] [command]```\n\n**Run** a job requesting GPUs\n\n```srun --gpus=[num_gpus] --pty /bin/bash```\n\n**Run** a parallel job with multiple tasks and CPUs per task\n\n```srun -n [num_tasks] -c [cpus_per_task] [command]```\n\n**Connect** to a worker node with a running job\n\n```srun --jobid [job_id] --pty /bin/bash```"],["SYNOPSIS","**srun** [_options_] _executable_ [_arguments_]"],["PARAMETERS","**--pty**\n> Execute in pseudo-terminal mode (for interactive use)\n\n**--jobid _id_**\n> Initiate a job step under an already allocated job\n\n**-n, --ntasks _number_**\n> Number of tasks to run\n\n**--ntasks-per-node _ntasks_**\n> Number of tasks to invoke on each node\n\n**-c, --cpus-per-task _ncpus_**\n> Number of CPUs allocated per task\n\n**--mem _size[units]_**\n> Memory required per node (default units: MB; supports K, M, G, T)\n\n**--mem-per-cpu _size[units]_**\n> Minimum memory required per usable allocated CPU\n\n**-p, --partition _name_**\n> Request specific partition for resource allocation\n\n**-N, --nodes _min[-max]_**\n> Number of nodes required (optionally a range)\n\n**-t, --time _time_**\n> Time limit for job (format: HH:MM:SS or days-HH:MM:SS)\n\n**-J, --job-name _name_**\n> Specify a name for the job\n\n**-G, --gpus _[type:]number_**\n> Total number of GPUs required for the job\n\n**--gres _list_**\n> Comma-delimited list of generic consumable resources (e.g., gpu:2)\n\n**-w, --nodelist _node_list_**\n> Request a specific list of hosts\n\n**-x, --exclude _host_list_**\n> Exclude specific hosts from the allocation\n\n**-o, --output _filename_**\n> Redirect standard output to specified file\n\n**-e, --error _filename_**\n> Redirect standard error to specified file\n\n**-D, --chdir _path_**\n> Change to specified directory before execution\n\n**--exclusive**\n> Job cannot share nodes with other running jobs\n\n**--export _{ALL|NONE|variables}_**\n> Control which environment variables are propagated to the job\n\n**--mpi _type_**\n> Identify the type of MPI to be used"],["DESCRIPTION","**srun** submits a job for execution or initiates job steps in real time. For interactive work, it creates an interactive session on compute nodes. It can also be used within batch scripts to launch parallel tasks.\n\nThe command handles resource allocation automatically, requesting nodes from the scheduler and launching the specified program across allocated resources."],["CAVEATS","Interactive jobs (**--pty**) may wait in queue for resources. Resource requests must fit within partition limits. Connection to existing jobs requires appropriate permissions."],["INSTALL","```apt: sudo apt install slurm-client```"],["SEE ALSO","[sbatch](/man/sbatch)(1), [squeue](/man/squeue)(1), [salloc](/man/salloc)(1), [scontrol](/man/scontrol)(1), [scancel](/man/scancel)(1), [sinfo](/man/sinfo)(1)"]],"e":["sbatch","squeue","salloc","scontrol","scancel","sinfo"],"x":"srun --pty /bin/bash"},"step":{"n":"step","t":"Certificate and key management CLI","s":[["TAGLINE","Certificate and key management CLI"],["TLDR","**Create CA**\n\n```step ca init```\n\n**Generate certificate**\n\n```step ca certificate [hostname] [cert.pem] [key.pem]```\n\n**Inspect certificate**\n\n```step certificate inspect [cert.pem]```\n\n**Create CSR**\n\n```step certificate create --csr [hostname] [csr.pem] [key.pem]```\n\n**Verify certificate**\n\n```step certificate verify [cert.pem] --roots [ca.pem]```\n\n**Get CA fingerprint**\n\n```step certificate fingerprint [ca.pem]```\n\n**Bootstrap CA client**\n\n```step ca bootstrap --ca-url [https://ca:9000] --fingerprint [fingerprint]```"],["SYNOPSIS","**step** _command_ [_subcommand_] [_options_]"],["PARAMETERS","**ca**\n> Certificate authority commands.\n\n**certificate**\n> Certificate operations.\n\n**crypto**\n> Cryptographic operations.\n\n**ssh**\n> SSH certificate commands.\n\n**oauth**\n> OAuth and OIDC authorization operations.\n\n**path**\n> Print the configured step path (default ~/.step).\n\n**context**\n> Manage certificate authority contexts.\n\n**completion**\n> Print shell completion scripts (bash, zsh, fish).\n\n**--ca-url** _URL_\n> CA server URL.\n\n**--fingerprint** _FP_\n> CA fingerprint."],["DESCRIPTION","**step** is a comprehensive command-line toolkit for working with certificates, keys, and cryptographic operations. It serves as both a standalone utility for inspecting and creating certificates and as the client interface for the step-ca private certificate authority server.\n\nThe tool provides subcommands for the full certificate lifecycle: creating certificate signing requests, requesting signed certificates from a CA, inspecting certificate details, verifying certificate chains, and managing SSH certificates. It supports modern cryptographic standards including ECDSA, EdDSA, and RSA keys, and can work with ACME protocol for automated certificate issuance.\n\nWhen paired with a step-ca server, step enables automated PKI workflows including bootstrapping client trust, requesting short-lived certificates, and managing provisioners for different authentication methods such as OIDC, JWK, and ACME. It also supports SSH certificate management as an alternative to traditional long-lived SSH keys."],["CONFIGURATION","**~/.step/**\n> Default step path containing CA configuration, certificates, and keys.\n\n**~/.step/config/defaults.json**\n> Default settings including CA URL, fingerprint, and provisioner.\n\n**STEPPATH**\n> Environment variable to override the default step configuration directory."],["CAVEATS","CA setup requires planning. Certificate lifetimes matter. Key security essential."],["HISTORY","**step** and **step-ca** were created by **Smallstep** for modern PKI. They simplify certificate management for developers and DevOps."],["INSTALL","```apt: sudo apt install step```\n\n```dnf: sudo dnf install step```\n\n```pacman: sudo pacman -S step```\n\n```apk: sudo apk add step```\n\n```zypper: sudo zypper install step```\n\n```brew: brew install step```"],["SEE ALSO","[step-ca](/man/step-ca)(1), [openssl](/man/openssl)(1), [cfssl](/man/cfssl)(1), [certbot](/man/certbot)(1)"]],"e":["step-ca","openssl","cfssl","certbot"],"x":"step ca init"},"systemctl-bind":{"n":"systemctl-bind","t":"Bind-mount paths into unit mount namespaces at runtime","s":[["TAGLINE","Bind-mount paths into unit mount namespaces at runtime"],["TLDR","**Bind-mount** path into unit\n\n```systemctl bind [unit] [/path/to/host]```\n\nBind to **different** location in unit\n\n```systemctl bind [unit] [/host/path] [/unit/path]```\n\nBind as **read-only**\n\n```systemctl bind [unit] [/path] --read-only```\n\n**Create** destination before binding\n\n```systemctl bind [unit] [/host/path] [/unit/path] --mkdir```"],["SYNOPSIS","**systemctl bind** _UNIT_ _PATH_ [_PATH_]"],["PARAMETERS","**--read-only**\n> Mount the path as read-only inside the unit\n\n**--mkdir**\n> Create the destination directory if it doesn't exist"],["DESCRIPTION","**systemctl bind** ephemerally bind-mounts a file or directory from the host into a running unit's mount namespace. This allows injecting files into sandboxed services without modifying their unit configuration.\n\nIf only one path is specified, the mount appears at the same location inside the unit. If two paths are given, the source is mounted at the destination inside the unit."],["CAVEATS","Requires the unit to have a separate mount namespace (PrivateMounts=yes or similar). Binds are ephemeral and don't persist across unit restarts. The unit must be running."],["HISTORY","The **bind** subcommand was added to **systemctl** for runtime injection of files into isolated services. It complements systemd's sandboxing features like PrivateMounts and ProtectSystem."],["SEE ALSO","[systemctl](/man/systemctl)(1), [mount](/man/mount)(8), [systemd-run](/man/systemd-run)(1)"]],"e":["systemctl","mount","systemd-run"],"x":"systemctl bind [unit] [/path/to/host]"},"systemd-mount":{"n":"systemd-mount","t":"Create transient mount units","s":[["TAGLINE","Create transient mount units"],["TLDR","**Mount** a device automatically at /run/media/system/LABEL\n\n```systemd-mount [path/to/device]```\n\n**Mount** a device at a specific location\n\n```systemd-mount [path/to/device] [path/to/mount_point]```\n\n**List** all known block devices with mountable filesystems\n\n```systemd-mount --list```\n\n**Create** an automount point\n\n```systemd-mount --automount yes [path/to/device]```\n\n**Unmount** one or more devices\n\n```systemd-mount -u [path/to/mount_point]```\n\n**Mount** with a specific filesystem type\n\n```systemd-mount -t [filesystem_type] [path/to/device] [path/to/mount_point]```\n\n**Mount** with additional options\n\n```systemd-mount -o [mount_options] [path/to/device] [path/to/mount_point]```"],["SYNOPSIS","**systemd-mount** [_options_] _what_ [_where_]"],["PARAMETERS","**--list**\n> List known block devices with mountable filesystems\n\n**--automount _bool_**\n> Create an automount point\n\n**-u, --umount**\n> Unmount the specified mount point or device\n\n**-t, --type _fstype_**\n> Specify filesystem type\n\n**-o, --options _opts_**\n> Mount options\n\n**--owner _user_**\n> Set the owner of the mount point\n\n**-A, --automount**\n> Create automount unit\n\n**--discover**\n> Use automatic device discovery"],["DESCRIPTION","**systemd-mount** creates and destroys transient mount or automount points using systemd's mount handling. It creates temporary mount units that are automatically cleaned up when unmounted.\n\nThe tool integrates with systemd's device and mount management, supporting features like automount (on-demand mounting) and proper tracking of mount states through systemd units."],["CAVEATS","Mount points created are transient and don't persist across reboots. For persistent mounts, use fstab or create proper systemd mount units. Part of the systemd suite."],["INSTALL","```apt: sudo apt install systemd```\n\n```dnf: sudo dnf install systemd```\n\n```pacman: sudo pacman -S systemd```\n\n```zypper: sudo zypper install systemd```\n\n```brew: brew install systemd```\n\n```nix: nix profile install nixpkgs#systemd```"],["SEE ALSO","[mount](/man/mount)(8), [udisksctl](/man/udisksctl)(1)"]],"e":["mount","udisksctl"],"x":"systemd-mount [path/to/device]"},"unp":{"n":"unp","t":"Shell frontend for uncompressing/unpacking tools","s":[["TAGLINE","Shell frontend for uncompressing/unpacking tools"],["TLDR","**Extract archive**\n\n```unp [archive.tar.gz]```\n\n**Extract multiple** archives\n\n```unp [file1.zip] [file2.tar.bz2]```\n\n**Extract into** a subdirectory named after the archive\n\n```unp -u [archive.zip]```\n\n**Simulate extraction** without writing files\n\n```unp -n [archive.tar.gz]```\n\n**Force extraction** even if checks fail or directory collision occurs\n\n```unp -f [archive.zip]```\n\n**Show supported** formats\n\n```unp -s```\n\n**Pass options** to the backend tool\n\n```unp [archive.tar.gz] -- [-C] [/tmp]```"],["SYNOPSIS","**unp** [_OPTION_]... [_FILE_]... [**--** _TOOL-OPTION_...]"],["PARAMETERS","**-f**\n> Continue even if program availability checks fail or directory collision occurs.\n\n**-u**\n> Extract into a subdirectory named after the archive. For Debian packages, extracts data.tar.gz and control.tar.gz separately.\n\n**-U**\n> Smart mode: acts like -u if archive contains multiple elements, but places a single file/directory element in the current directory.\n\n**-s**\n> Show the list of supported formats.\n\n**-n**, **--dry-run**\n> Display the calculated commands without running them.\n\n**-v**\n> Increase output verbosity.\n\n**--**\n> Pass subsequent arguments to the backend tool (e.g., tar options)."],["DESCRIPTION","**unp** is a shell frontend for various uncompressing and unpacking tools. It automatically detects the format of an archive and calls the appropriate decompression program. Instead of remembering different commands for tar, gzip, bzip2, zip, rar, 7z, and dozens of other formats, users can simply run unp on any archive file.\n\nBy default, unp extracts to the current directory. It can optionally create a subdirectory for the extracted contents to prevent cluttering the working directory. Arguments after **--** are passed to the underlying extraction tool."],["SUPPORTED FORMATS","tar, gz, bz2, xz, zip, rar, 7z, arj, lha, deb, rpm, cpio"],["CAVEATS","Requires the underlying extraction tools to be installed for each format. The **-n** dry-run output should be examined carefully, as malicious filenames in archives could be dangerous if pasted into a shell."],["HISTORY","**unp** was created as a universal unpacking script that automatically detects and extracts various archive formats."],["INSTALL","```pacman: sudo pacman -S unp```\n\n```brew: brew install unp```\n\n```nix: nix profile install nixpkgs#unp```"],["SEE ALSO","[unar](/man/unar)(1), [atool](/man/atool)(1), [tar](/man/tar)(1), [unzip](/man/unzip)(1)"]],"e":["unar","atool","tar","unzip"],"x":"unp [archive.tar.gz]"},"vapor":{"n":"vapor","t":"Server-side Swift web framework toolbox","s":[["TAGLINE","Server-side Swift web framework toolbox"],["TLDR","**Create new Vapor project**\n\n```vapor new [ProjectName]```\n\n**Create project with a custom template**\n\n```vapor new [ProjectName] --template [https://github.com/user/template.git]```\n\n**Create project with automatic \"no\" to all prompts**\n\n```vapor new [ProjectName] -n```\n\n**Create project in a specific output directory**\n\n```vapor new [ProjectName] --output [path/to/directory]```\n\n**Create project without initializing a Git repository**\n\n```vapor new [ProjectName] --no-git```"],["SYNOPSIS","**vapor** [**new**] [_options_] [_name_]"],["PARAMETERS","**-n**\n> Automatically answer \"no\" to all interactive questions.\n\n**--output** _path_\n> Specify a custom folder location for the new project.\n\n**--template** _url_\n> Use a custom template from a Git repository URL.\n\n**--branch** _name_\n> Specify a different Git branch for the template (default: main).\n\n**--no-git**\n> Skip Git repository initialization.\n\n**--no-commit**\n> Initialize Git but skip the initial commit.\n\n**--help**\n> Display help information."],["DESCRIPTION","**vapor** is the command-line toolbox for the Vapor web framework, a server-side Swift HTTP framework. The toolbox's primary purpose is creating new Vapor projects from templates. The **new** command is the default subcommand and can be omitted.\n\nProjects are created from templates that include routing, middleware, and optionally Fluent ORM for database operations. The toolbox supports dynamic project generation through manifest files (YAML or JSON) that define variables and conditional file inclusion based on user responses.\n\nOnce a project is created, you use Swift Package Manager directly (`swift build`, `swift run`) to build and run the application."],["CAVEATS","Requires Swift toolchain and Xcode Command Line Tools on macOS. Linux requires Swift installation. The toolbox requires macOS 15.0+ or a Linux distribution supported by Swift 6.1+. Database drivers (PostgreSQL, MySQL, SQLite) are added as Swift package dependencies."],["HISTORY","**Vapor** was created by Tanner Nelson in 2016 as one of the first major server-side Swift frameworks. It quickly became the most popular Swift web framework. Vapor 4, released in 2020, brought full async/await support and improved performance. The framework is maintained by the Vapor community and used in production by companies worldwide."],["INSTALL","```brew: brew install vapor```"],["SEE ALSO","[swift](/man/swift)(1), [swiftc](/man/swiftc)(1), [docker](/man/docker)(1)"]],"e":["swift","swiftc","docker"],"x":"vapor new [ProjectName]"},"viddy":{"n":"viddy","t":"Modern watch command with time machine feature","s":[["TAGLINE","Modern watch command with time machine feature"],["TLDR","**Watch a command with default interval**\n\n```viddy '[ls -la]'```\n\n**Watch with diff highlighting**\n\n```viddy -d '[df -h]'```\n\n**Watch with a custom interval**\n\n```viddy -n [5] '[command]'```"],["SYNOPSIS","**viddy** [_options_] _command_"],["PARAMETERS","**-n**, **--interval** _SECONDS_\n> Command execution interval (default: 2)\n\n**-d**, **--differences**\n> Highlight differences between updates\n\n**-t**, **--no-title**\n> Disable header display\n\n**-b**, **--bell**\n> Ring bell on command error\n\n**-p**, **--precise**\n> Precise timing mode\n\n**-s**, **--skip-empty-diffs**\n> Skip updates when output has not changed"],["DESCRIPTION","**viddy** is a modern replacement for the Unix **watch** command. It executes commands periodically and displays results with colored output and diff highlighting. Its standout feature is time machine mode, which lets you rewind through past command outputs. Press **Space** to toggle time machine mode, then use arrow keys to navigate history. Includes vim-like keybindings and a pager."],["HISTORY","**viddy** was created by **sachaos**. Originally written in Go, it was rewritten in **Rust** starting with version 1.0.0."],["INSTALL","```apk: sudo apk add viddy```\n\n```zypper: sudo zypper install viddy```\n\n```brew: brew install viddy```\n\n```nix: nix profile install nixpkgs#viddy```"],["SEE ALSO","[watch](/man/watch)(1), [hwatch](/man/hwatch)(1), [entr](/man/entr)(1)"]],"e":["watch","hwatch","entr"],"x":"viddy '[ls -la]'"},"wbinfo":{"n":"wbinfo","t":"Query Winbind for domain information","s":[["TAGLINE","Query Winbind for domain information"],["TLDR","**List domain users**\n\n```wbinfo -u```\n\n**List domain groups**\n\n```wbinfo -g```\n\n**Check winbind connection**\n\n```wbinfo -p```\n\n**Ping domain controller**\n\n```wbinfo -P```\n\n**Show domain info**\n\n```wbinfo -D [DOMAIN]```\n\n**List trusted domains**\n\n```wbinfo -m```\n\n**Convert username to SID**\n\n```wbinfo -n [DOMAIN/username]```\n\n**Convert SID to name**\n\n```wbinfo -s [S-1-5-21-...]```\n\n**Get user info by UID**\n\n```wbinfo --uid-info [1000]```\n\n**Check trust account**\n\n```wbinfo -t```"],["SYNOPSIS","**wbinfo** [_options_]"],["PARAMETERS","**-u**, **--domain-users**\n> List all domain users.\n\n**-g**, **--domain-groups**\n> List all domain groups.\n\n**-p**, **--ping**\n> Check if winbindd is running.\n\n**-P**, **--ping-dc**\n> Ping the domain controller.\n\n**-t**, **--check-secret**\n> Verify trust account.\n\n**-m**, **--trusted-domains**\n> List trusted domains.\n\n**-D**, **--domain-info** _domain_\n> Show domain information.\n\n**--all-domains**\n> List all domains.\n\n**--own-domain**\n> Show own domain.\n\n**-n**, **--name-to-sid** _name_\n> Convert name to SID.\n\n**-s**, **--sid-to-name** _SID_\n> Convert SID to name.\n\n**-U**, **--uid-to-sid** _UID_\n> Convert UNIX UID to SID.\n\n**-G**, **--gid-to-sid** _GID_\n> Convert UNIX GID to SID.\n\n**-r**, **--user-groups** _user_\n> Get user's group memberships.\n\n**--uid-info** _UID_\n> Get user info for UID.\n\n**--domain** _name_\n> Specify domain for operations.\n\n**-a** _user%pass_\n> Authenticate user (testing only)."],["DESCRIPTION","**wbinfo** queries information from the winbindd daemon, which provides Windows domain integration for Samba and Linux systems. It retrieves user, group, and domain information from Active Directory or NT domains.\n\nThe tool is essential for troubleshooting Winbind configurations and verifying domain connectivity. It can resolve between Windows SIDs and UNIX UIDs/GIDs using the configured ID mapping.\n\nwinbindd must be running and properly configured for wbinfo to function. The tool is commonly used to verify Samba domain membership."],["CAVEATS","Requires running winbindd daemon. Authentication via -a is for testing only; use ntlm_auth for applications. Some operations may take time on large domains. Exit status 0 indicates success, 1 indicates failure."],["HISTORY","**wbinfo** is part of the Samba suite, which provides Windows interoperability for Unix systems. Winbind enables Unix systems to appear as members of Windows domains, allowing domain users to authenticate on Unix systems."],["INSTALL","```apt: sudo apt install winbind```\n\n```dnf: sudo dnf install samba-winbind-clients```\n\n```apk: sudo apk add samba-winbind-clients```"],["SEE ALSO","[samba](/man/samba)(7), [net](/man/net)(8)"]],"e":["samba","net"],"x":"wbinfo -u"},"yadm-enter":{"n":"yadm-enter","t":"Run a sub-shell with yadm Git variables set","s":[["TAGLINE","Run a sub-shell with yadm Git variables set"],["TLDR","**Enter a sub-shell with yadm Git variables set**\n\n```yadm enter```\n\n**Run a specific command in the yadm context**\n\n```yadm enter [command]```\n\n**Run git log within the yadm worktree**\n\n```yadm enter git log --oneline```"],["SYNOPSIS","**yadm** **enter** [_command_]"],["DESCRIPTION","**yadm enter** opens a sub-shell with **GIT_DIR** and **GIT_WORK_TREE** environment variables set to the yadm repository. Exit the sub-shell the same way you leave your normal shell (usually with \"exit\"). This is useful for complex git operations on dotfiles, or for integrating with tools that use Git directly such as **tig**, **vim-fugitive**, or **git-cola**.\n\nOptionally, provide a command after \"enter\" and that command will be run with the yadm Git variables exposed in its environment, instead of invoking a sub-shell."],["INSTALL","```pacman: sudo pacman -S yadm```\n\n```apk: sudo apk add yadm```\n\n```zypper: sudo zypper install yadm```\n\n```brew: brew install yadm```\n\n```nix: nix profile install nixpkgs#yadm```"],["SEE ALSO","[yadm](/man/yadm)(1)"]],"e":["yadm"],"x":"yadm enter"},"zabbix_agent2":{"n":"zabbix_agent2","t":"Next-generation Zabbix monitoring agent","s":[["TAGLINE","Next-generation Zabbix monitoring agent"],["TLDR","**Start agent in foreground**\n\n```zabbix_agent2 -f```\n\n**Start with config file**\n\n```zabbix_agent2 -c [/etc/zabbix/zabbix_agent2.conf]```\n\n**Test a specific item key**\n\n```zabbix_agent2 -t [system.cpu.load]```\n\n**Print all supported items**\n\n```zabbix_agent2 -p```\n\n**Validate configuration file**\n\n```zabbix_agent2 -T```\n\n**Change log level at runtime**\n\n```zabbix_agent2 -R [log_level_increase]```\n\n**Reload user parameters at runtime**\n\n```zabbix_agent2 -R userparameter_reload```"],["SYNOPSIS","**zabbix_agent2** [_options_]"],["PARAMETERS","**-c**, **--config** _file_\n> Use alternate configuration file instead of the default one.\n\n**-f**\n> Run in foreground.\n\n**-p**, **--print**\n> Print known items and exit.\n\n**-t** _item-key_\n> Test a single item key and exit.\n\n**-T**, **--test-config**\n> Validate configuration file and exit.\n\n**-R**, **--runtime-control** _option_\n> Perform administrative functions. Options: log_level_increase, log_level_decrease, userparameter_reload, metrics, version, help.\n\n**-h**, **--help**\n> Display help and exit.\n\n**-V**, **--version**\n> Display version information and exit."],["DESCRIPTION","**zabbix_agent2** is the next-generation Zabbix monitoring agent written in Go. Collects system metrics and sends them to Zabbix server. Supports plugins and concurrent checks."],["INSTALL","```apt: sudo apt install zabbix-agent2```\n\n```pacman: sudo pacman -S zabbix-agent2```\n\n```apk: sudo apk add zabbix-agent2```\n\n```zypper: sudo zypper install zabbix-agent2```"],["SEE ALSO","[zabbix_agentd](/man/zabbix_agentd)(1), [zabbix_get](/man/zabbix_get)(1), [zabbix_proxy](/man/zabbix_proxy)(1), [zabbix_server](/man/zabbix_server)(1)"]],"e":["zabbix_agentd","zabbix_get","zabbix_proxy","zabbix_server"],"x":"zabbix_agent2 -f"}});
