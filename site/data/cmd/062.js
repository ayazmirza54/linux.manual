window.LCL_BUCKET(62,{"aapt":{"n":"aapt","t":"Android resource packaging tool","s":[["TAGLINE","Android resource packaging tool"],["TLDR","**List contents** of an APK file\n\n```aapt list [app.apk]```\n\n**Dump** the AndroidManifest.xml from an APK\n\n```aapt dump xmltree [app.apk] AndroidManifest.xml```\n\n**Extract package name** and version from an APK\n\n```aapt dump badging [app.apk]```\n\n**Package** resources into an APK\n\n```aapt package -f -M [AndroidManifest.xml] -S [res/] -I [android.jar] -F [output.apk]```"],["SYNOPSIS","**aapt** _command_ [_options_] [_file_]"],["DESCRIPTION","**aapt** (Android Asset Packaging Tool) is a build tool used in Android development to package application resources into APK files. It compiles resources, generates the R.java file containing resource identifiers, and can inspect existing APK files.\n\nThe tool handles resource compilation, XML processing, asset packaging, and APK creation. It reads AndroidManifest.xml, processes drawable, layout, and other resource files, and bundles them into the final application package. While largely superseded by aapt2 in modern Android development, aapt remains useful for APK inspection and legacy projects."],["PARAMETERS","**list**\n> List contents of a ZIP-compatible archive (APK)\n\n**dump**\n> Dump specific information from an APK: badging, permissions, resources, configurations, xmltree, xmlstrings\n\n**package**\n> Package resources and assets into an APK\n\n**-f**\n> Force overwrite of existing files\n\n**-M** _file_\n> Specify AndroidManifest.xml location\n\n**-S** _dir_\n> Specify resource directory\n\n**-I** _jar_\n> Add an existing package to base include set (usually android.jar)\n\n**-F** _file_\n> Specify output APK file\n\n**-v**\n> Verbose output"],["CAVEATS","**aapt** has been superseded by **aapt2** in Android Gradle Plugin 3.0+. The older tool lacks incremental compilation support and has slower performance on large projects. Resource processing differences may cause issues when mixing tools."],["HISTORY","**aapt** was introduced as part of the Android SDK in **2008** with the initial public release of Android. It served as the primary resource packaging tool until **2017** when Google introduced aapt2 with improved performance and incremental build support. The original aapt remains included in the SDK for compatibility."],["INSTALL","```apt: sudo apt install aapt```\n\n```nix: nix profile install nixpkgs#aapt```"],["SEE ALSO","[aapt2](/man/aapt2)(1), [apksigner](/man/apksigner)(1), [apktool](/man/apktool)(1), [zipalign](/man/zipalign)(1), [adb](/man/adb)(1)"],["RESOURCES","```[Source code](https://android.googlesource.com/platform/frameworks/base/+/refs/heads/main/tools/aapt)```\n\n```[Documentation](https://developer.android.com/tools/aapt2)```"]],"e":["aapt2","apksigner","apktool","zipalign","adb"],"x":"aapt list [app.apk]"},"alr":{"n":"alr","t":"Ada/SPARK source package manager.","s":[["TAGLINE","Ada/SPARK source package manager."],["TLDR","**Search** for a crate\n\n```alr search [package_name]```\n\n**Show** available versions\n\n```alr show [package_name]```\n\n**Get** a crate\n\n```alr get [package_name]```\n\n**Initialize** a new crate\n\n```alr init --bin [project_name]```\n\n**Build** current project\n\n```alr build```\n\n**Run** current project\n\n```alr run```"],["SYNOPSIS","**alr** _command_ [_options_] [_arguments_]"],["DESCRIPTION","**alr** is the Alire package manager for the Ada programming language. It provides crate (package) management similar to Cargo for Rust or npm for Node.js, handling dependencies, building, and publishing Ada projects.\n\nAlire connects to the community-maintained crate index, enabling easy discovery and integration of Ada libraries and tools."],["PARAMETERS","**search** _term_\n> Search for crates matching term\n\n**show** _crate_\n> Display information about a crate\n\n**get** _crate_\n> Download and set up a crate\n\n**init**\n> Initialize a new project\n\n**build**\n> Build the current project\n\n**run**\n> Build and run the current project\n\n**with** _crate_\n> Add a dependency to current project\n\n**publish**\n> Publish crate to the index\n\n**update**\n> Update dependencies to latest versions\n\n**toolchain**\n> Manage the GNAT compilers and build tools (gprbuild)\n\n**test**\n> Run the crate test suite in batch mode\n\n**settings**\n> View or modify global and local configuration\n\n**--help**\n> Show help for command"],["CAVEATS","Requires GNAT Ada compiler to be installed. Some crates may have platform-specific dependencies. The crate index is community-maintained and separate from commercial Ada tool vendors."],["HISTORY","**Alire** was developed by the Ada community, with the first stable release around **2020**. It modernizes Ada development with package management comparable to other contemporary languages."],["INSTALL","```apt: sudo apt install alire```\n\n```aur: yay -S alire```\n\n```nix: nix profile install nixpkgs#alire```"],["SEE ALSO","[alr-help](/man/alr-help)(1), [alr-version](/man/alr-version)(1), [gnatmake](/man/gnatmake)(1), [gprbuild](/man/gprbuild)(1), [cargo](/man/cargo)(1), [opam](/man/opam)(1)"],["RESOURCES","```[Source code](https://github.com/alire-project/alire)```\n\n```[Homepage](https://alire.ada.dev/)```\n\n```[Documentation](https://alire.ada.dev/docs/)```"]],"e":["alr-help","alr-version","gnatmake","gprbuild","cargo","opam"],"x":"alr search [package_name]"},"aria2":{"n":"aria2","t":"Multi-protocol multi-source download utility","s":[["TAGLINE","Multi-protocol multi-source download utility"],["TLDR","**Download** a file\n\n```aria2c [https://example.com/file.zip]```\n\nDownload with **multiple connections**\n\n```aria2c -x 16 -s 16 [https://example.com/file.zip]```\n\nDownload from **multiple mirror sources**\n\n```aria2c [http://mirror1.com/file] [http://mirror2.com/file]```\n\n**Resume** an interrupted download\n\n```aria2c -c [https://example.com/file.zip]```\n\nDownload a **torrent** file\n\n```aria2c [file.torrent]```\n\nDownload from a **magnet link**\n\n```aria2c \"[magnet:?xt=urn:btih:...]\"```\n\nDownload **URLs from a file** (one URL per line)\n\n```aria2c -i [urls.txt]```\n\n**Limit download speed**\n\n```aria2c --max-download-limit=[500K] [https://example.com/file.zip]```"],["SYNOPSIS","**aria2c** [_options_] _URL_|_TORRENT_|_METALINK_ ..."],["DESCRIPTION","**aria2** is a lightweight multi-protocol download utility supporting HTTP/HTTPS, FTP, SFTP, BitTorrent, and Metalink. It can download files using multiple connections and sources simultaneously for faster speeds.\n\nThe actual command is **aria2c** (the \"c\" stands for command-line). The tool is highly optimized, using minimal memory (typically 4-9 MiB) while supporting advanced features like segmented downloading, BitTorrent, and JSON-RPC remote control."],["PARAMETERS","**-x** _n_, **--max-connection-per-server** _n_\n> Maximum connections per server (default: 1, max: 16)\n\n**-s** _n_, **--split** _n_\n> Split download into n parts (default: 5)\n\n**-c**, **--continue**\n> Resume a partially downloaded file\n\n**-d** _dir_, **--dir** _dir_\n> Download directory\n\n**-o** _file_, **--out** _file_\n> Output filename\n\n**-j** _n_, **--max-concurrent-downloads** _n_\n> Maximum number of parallel downloads (default: 5)\n\n**-i** _file_, **--input-file** _file_\n> Download URLs listed in file\n\n**--max-download-limit** _speed_\n> Per-download speed limit (e.g., 500K, 2M)\n\n**--max-overall-download-limit** _speed_\n> Overall download speed limit\n\n**--enable-rpc**\n> Enable JSON-RPC/XML-RPC server for remote control\n\n**--seed-time** _minutes_\n> BitTorrent seeding time (0 to disable seeding)\n\n**--select-file** _indices_\n> Select specific files from a torrent (comma-separated indices)\n\n**--file-allocation** _method_\n> File allocation method: none, prealloc, trunc, or falloc"],["CONFIGURATION","**~/.aria2/aria2.conf**\n> Default configuration file for setting download options, connection limits, and protocol-specific settings. One option per line without the leading --."],["CAVEATS","Multiple connections may be limited or throttled by the server. BitTorrent requires proper port forwarding for best performance. The **-x** and **-s** flags should generally be used together for segmented downloads."],["HISTORY","**aria2** was developed by Tatsuhiro Tsujikawa, with initial release around **2006**. Written in C++, it has become a popular alternative to wget and curl for its multi-connection download capability."],["INSTALL","```dnf: sudo dnf install aria2```\n\n```pacman: sudo pacman -S aria2```\n\n```zypper: sudo zypper install aria2```\n\n```brew: brew install aria2```\n\n```nix: nix profile install nixpkgs#aria2```"],["SEE ALSO","[aria2c](/man/aria2c)(1), [curl](/man/curl)(1), [wget](/man/wget)(1), [transmission-cli](/man/transmission-cli)(1)"]],"e":["aria2c","curl","wget","transmission-cli"],"x":"aria2c [https://example.com/file.zip]"},"aurman":{"n":"aurman","t":"AUR helper with pacman-like interface","s":[["TAGLINE","AUR helper with pacman-like interface"],["TLDR","**Synchronize and update** all packages\n\n```aurman -Syu```\n\nUpdate all packages **without showing PKGBUILD** changes\n\n```aurman -Syu --noedit```\n\n**Install** a new package\n\n```aurman -S [package]```\n\nInstall a package **without prompting**\n\n```aurman -S --noedit --noconfirm [package]```\n\n**Search** for a keyword in repositories and AUR\n\n```aurman -Ss [keyword]```\n\n**Remove** a package and its dependencies\n\n```aurman --remove --recursive --nosave [package]```\n\n**Clear** the package cache\n\n```aurman -Sc```"],["SYNOPSIS","**aurman** [_options_] [_packages_]"],["DESCRIPTION","**aurman** is an AUR helper for Arch Linux that provides a pacman-like interface for installing packages from both official repositories and the Arch User Repository. It handles dependency resolution, PKGBUILD review, and package building, and was aimed at advanced users already familiar with pacman, makepkg, and the AUR.\n\nThe command follows pacman's option syntax, making it familiar to Arch Linux users. It combines repository and AUR package management into a single workflow. Note that aurman is no longer maintained and has been removed from the AUR; the entries below are kept for historical reference."],["PARAMETERS","**-S, --sync**\n> Synchronize packages\n\n**-y, --refresh**\n> Download fresh package databases\n\n**-u, --sysupgrade**\n> Upgrade installed packages\n\n**-s, --search**\n> Search for packages matching a pattern\n\n**-c, --clean**\n> Remove old packages from cache\n\n**--noedit**\n> Do not show PKGBUILD changes before building\n\n**--noconfirm**\n> Do not ask for confirmation\n\n**--remove**\n> Remove a package\n\n**--recursive**\n> Remove dependencies not required by other packages\n\n**--devel**\n> Also consider development packages (VCS sources such as -git) for upgrades\n\n**--pgp_fetch**\n> Automatically fetch unknown PGP keys needed to verify sources\n\n**--aur**\n> Restrict the operation to AUR packages\n\n**--repo**\n> Restrict the operation to official repository packages"],["CAVEATS","**aurman** is no longer maintained and has been removed from the AUR. Use a current AUR helper such as **yay** or **paru** instead. Always review PKGBUILDs when installing from the AUR, as they contain user-submitted code that runs with your privileges."],["HISTORY","**aurman** was a popular AUR helper known for its advanced dependency solver and interactive package selection. Development stopped and the package was eventually dropped from the AUR, leaving **yay** and **paru** as the commonly recommended replacements."],["INSTALL","```aur: yay -S aurman```"],["SEE ALSO","[pacman](/man/pacman)(8), [yay](/man/yay)(1), [paru](/man/paru)(1), [makepkg](/man/makepkg)(8)"],["RESOURCES","```[Source code](https://github.com/polygamma/aurman)```"]],"e":["pacman","yay","paru","makepkg"],"x":"aurman -Syu"},"avahi-resolve-host-name":{"n":"avahi-resolve-host-name","t":"Resolve hostnames to IP addresses via mDNS","s":[["TAGLINE","Resolve hostnames to IP addresses via mDNS"],["TLDR","This command is an alias of **avahi-resolve --name**.\n\n**Resolve** a hostname to an IP address\n\n```avahi-resolve-host-name [hostname.local]```\n\n**Resolve** to an IPv4 address only\n\n```avahi-resolve-host-name -4 [hostname.local]```\n\n**Resolve** to an IPv6 address only\n\n```avahi-resolve-host-name -6 [hostname.local]```"],["SYNOPSIS","**avahi-resolve-host-name** [_OPTIONS_] _hostname_ ..."],["DESCRIPTION","**avahi-resolve-host-name** resolves hostnames to IP addresses using mDNS/DNS-SD via the Avahi daemon. It is equivalent to running **avahi-resolve** with the **--name** flag."],["INSTALL","```apt: sudo apt install avahi-utils```\n\n```dnf: sudo dnf install avahi-tools```\n\n```apk: sudo apk add avahi-tools```\n\n```zypper: sudo zypper install avahi-utils```"],["SEE ALSO","[avahi-resolve](/man/avahi-resolve)(1), [avahi-resolve-address](/man/avahi-resolve-address)(1), [avahi-browse](/man/avahi-browse)(1), [avahi-daemon](/man/avahi-daemon)(8)"],["RESOURCES","```[Homepage](https://avahi.org/)```\n\n```[Source code](https://github.com/avahi/avahi)```"]],"e":["avahi-resolve","avahi-resolve-address","avahi-browse","avahi-daemon"],"x":"avahi-resolve-host-name [hostname.local]"},"aws-elb":{"n":"aws-elb","t":"Manage Classic Load Balancers","s":[["TAGLINE","Manage Classic Load Balancers"],["TLDR","**List all Classic Load Balancers**\n\n```aws elb describe-load-balancers```\n\n**Describe a specific load balancer**\n\n```aws elb describe-load-balancers --load-balancer-names [lb_name]```\n\n**Check the health** of registered instances\n\n```aws elb describe-instance-health --load-balancer-name [lb_name]```\n\n**Register an instance** with a load balancer\n\n```aws elb register-instances-with-load-balancer --load-balancer-name [lb_name] --instances [i-xxxxxxxx]```\n\n**Deregister an instance** from a load balancer\n\n```aws elb deregister-instances-from-load-balancer --load-balancer-name [lb_name] --instances [i-xxxxxxxx]```\n\n**Create a Classic Load Balancer**\n\n```aws elb create-load-balancer --load-balancer-name [lb_name] --listeners Protocol=HTTP,LoadBalancerPort=80,InstanceProtocol=HTTP,InstancePort=80 --subnets [subnet-id] --security-groups [sg-id]```\n\n**Delete a load balancer**\n\n```aws elb delete-load-balancer --load-balancer-name [lb_name]```"],["SYNOPSIS","**aws elb** _subcommand_ [_options_]"],["DESCRIPTION","**aws elb** is a subcommand of the AWS CLI that manages Classic Load Balancers (CLB), the original Elastic Load Balancing service. Classic Load Balancers distribute incoming traffic across multiple EC2 instances in one or more Availability Zones.\n\nCLB operates at both Layer 4 (TCP) and Layer 7 (HTTP/HTTPS), routing traffic based on application or network level information. It performs health checks on registered instances and only routes traffic to healthy targets.\n\nClassic Load Balancers support sticky sessions, SSL termination, and integration with Auto Scaling groups for dynamic instance management."],["PARAMETERS","**describe-load-balancers**\n> List load balancers and their configuration.\n\n**create-load-balancer**\n> Create a new Classic Load Balancer.\n\n**delete-load-balancer**\n> Remove a load balancer.\n\n**describe-instance-health**\n> Check health status of registered instances.\n\n**register-instances-with-load-balancer**\n> Add instances to a load balancer.\n\n**deregister-instances-from-load-balancer**\n> Remove instances from a load balancer.\n\n**configure-health-check**\n> Set health check parameters.\n\n**create-load-balancer-listeners**\n> Add listener configurations.\n\n**delete-load-balancer-listeners**\n> Remove listeners.\n\n**set-load-balancer-listener-ssl-certificate**\n> Update the SSL certificate for an HTTPS listener.\n\n**describe-load-balancer-attributes**\n> Retrieve attributes such as connection draining and access log settings.\n\n**modify-load-balancer-attributes**\n> Modify attributes such as connection draining and idle timeout.\n\n**describe-load-balancer-policies**\n> List policies associated with load balancers.\n\n**create-load-balancer-policy**\n> Create a new policy for a load balancer.\n\n**delete-load-balancer-policy**\n> Remove a policy from a load balancer.\n\n**create-app-cookie-stickiness-policy**\n> Enable application-controlled session stickiness.\n\n**create-lb-cookie-stickiness-policy**\n> Enable load balancer-controlled session stickiness.\n\n**attach-load-balancer-to-subnets**\n> Add subnets to a load balancer (VPC only).\n\n**detach-load-balancer-from-subnets**\n> Remove subnets from a load balancer.\n\n**enable-availability-zones-for-load-balancer**\n> Add Availability Zones to a load balancer (EC2-Classic).\n\n**disable-availability-zones-for-load-balancer**\n> Remove Availability Zones from a load balancer.\n\n**apply-security-groups-to-load-balancer**\n> Associate security groups with a VPC load balancer.\n\n**add-tags**\n> Add or overwrite tags on a load balancer.\n\n**remove-tags**\n> Remove tags from a load balancer.\n\n**describe-tags**\n> List tags on one or more load balancers.\n\n**describe-account-limits**\n> Show ELB service limits for the account.\n\n**wait**\n> Wait until a load balancer reaches a specified state.\n\n**--load-balancer-name** _name_\n> Name of the load balancer.\n\n**--load-balancer-names** _names_\n> List of load balancer names.\n\n**--instances** _ids_\n> EC2 instance IDs.\n\n**--listeners** _config_\n> Listener protocol and port mappings.\n\n**--subnets** _ids_\n> VPC subnets for the load balancer.\n\n**--security-groups** _ids_\n> Security groups to assign."],["CAVEATS","Classic Load Balancers are a previous generation service. AWS recommends using Application Load Balancers (ALB) for HTTP/HTTPS or Network Load Balancers (NLB) for TCP/UDP workloads. CLB does not support path-based routing, host-based routing, or WebSocket natively. New features are not being added to CLB."],["HISTORY","Elastic Load Balancing launched in **2009** as one of AWS's earliest services. The Classic Load Balancer was the only load balancer type until **August 2016** when Application Load Balancer was introduced. Network Load Balancer followed in **2017**. Classic Load Balancers remain supported but are considered legacy, with AWS encouraging migration to newer types."],["INSTALL","```dnf: sudo dnf install aws```\n\n```pacman: sudo pacman -S aws-cli```\n\n```apk: sudo apk add aws-cli```\n\n```zypper: sudo zypper install aws-cli```"],["SEE ALSO","[aws](/man/aws)(1), [aws-elbv2](/man/aws-elbv2)(1), [aws-ec2](/man/aws-ec2)(1), [aws-autoscaling](/man/aws-autoscaling)(1)"]],"e":["aws","aws-elbv2","aws-ec2","aws-autoscaling"],"x":"aws elb describe-load-balancers"},"az-functionapp":{"n":"az-functionapp","t":"Manage Azure Functions serverless apps","s":[["TAGLINE","Manage Azure Functions serverless apps"],["TLDR","**List all function apps**\n\n```az functionapp list```\n\n**Create a function app** (consumption plan)\n\n```az functionapp create --resource-group [rg_name] --consumption-plan-location [eastus] --runtime [node] --functions-version [4] --name [app_name] --storage-account [storage_name]```\n\n**Create a function app** on a Flex Consumption plan\n\n```az functionapp create --resource-group [rg_name] --flexconsumption-location [northeurope] --runtime [java] --name [app_name] --storage-account [storage_name]```\n\n**Create a function app** on an App Service plan\n\n```az functionapp create --resource-group [rg_name] --plan [plan_name] --runtime [python] --runtime-version [3.11] --functions-version [4] --name [app_name] --storage-account [storage_name]```\n\n**Show function app details**\n\n```az functionapp show --name [app_name] --resource-group [rg_name]```\n\n**Deploy code** from a zip file\n\n```az functionapp deployment source config-zip --name [app_name] --resource-group [rg_name] --src [function.zip]```\n\n**Restart a function app**\n\n```az functionapp restart --name [app_name] --resource-group [rg_name]```\n\n**View application settings**\n\n```az functionapp config appsettings list --name [app_name] --resource-group [rg_name]```\n\n**Set an application setting**\n\n```az functionapp config appsettings set --name [app_name] --resource-group [rg_name] --settings \"[KEY=value]\"```\n\n**List functions** in an app\n\n```az functionapp function list --name [app_name] --resource-group [rg_name]```"],["SYNOPSIS","**az functionapp** _subcommand_ [_options_]"],["DESCRIPTION","**az functionapp** is a subcommand of the Azure CLI that manages Azure Functions apps. Azure Functions is a serverless compute service that runs event-triggered code without managing infrastructure.\n\nFunction apps host one or more functions that share configuration, deployment, and scaling. They can run on consumption plans (pay per execution, auto-scale), premium plans (pre-warmed instances, VNet integration), or dedicated App Service plans.\n\nSupported runtimes include .NET, Node.js, Python, Java, PowerShell, and custom handlers. Functions can be triggered by HTTP requests, timers, queues, blobs, Event Grid, and other Azure services."],["PARAMETERS","**list**\n> List all function apps.\n\n**create**\n> Create a new function app.\n\n**delete**\n> Remove a function app.\n\n**show**\n> Get function app details.\n\n**restart**\n> Restart the function app.\n\n**start**\n> Start a stopped function app.\n\n**stop**\n> Stop a running function app.\n\n**deployment source config-zip**\n> Deploy from zip file.\n\n**config appsettings list**\n> List application settings.\n\n**config appsettings set**\n> Set application settings.\n\n**function list**\n> List functions in the app.\n\n**--name** _name_\n> Function app name (globally unique).\n\n**--resource-group** _rg_\n> Resource group name.\n\n**--consumption-plan-location** _location_\n> Region for consumption plan.\n\n**--plan** _plan_\n> App Service plan name.\n\n**--runtime** _runtime_\n> Functions runtime stack (e.g. node, python, dotnet, dotnet-isolated, java, powershell, custom). Run `az functionapp list-runtimes` for supported values.\n\n**--runtime-version** _version_\n> Version of the runtime stack. Run `az functionapp list-runtimes` to check compatibility.\n\n**--functions-version** _version_\n> Azure Functions runtime major version (currently 4).\n\n**--os-type** _{Linux, Windows}_\n> Operating system for the app.\n\n**--flexconsumption-location** _location_\n> Region for a Flex Consumption plan app.\n\n**--storage-account** _storage_, **-s**\n> Storage account name (or resource ID) for function state. Required."],["CAVEATS","Function app names must be globally unique. Consumption plan has cold start latency. Storage account is required for triggers and state. Some features like VNet integration require Premium or App Service plans. Linux consumption plan has limited region availability. Functions version 4 requires runtime-specific version compatibility."],["HISTORY","**Azure Functions** launched in **March 2016** as Microsoft's serverless compute platform, inspired by AWS Lambda. **Durable Functions** for stateful workflows was added in **2017**. **Premium plan** was introduced in **2019** for enterprise scenarios. **Functions 4.0** with .NET 6 support released in **November 2021**. The service has expanded to support containers, Kubernetes via KEDA, and Azure Arc for hybrid deployments."],["INSTALL","```dnf: sudo dnf install azure-cli```\n\n```pacman: sudo pacman -S azure-cli```\n\n```zypper: sudo zypper install azure-cli```\n\n```brew: brew install azure-cli```\n\n```nix: nix profile install nixpkgs#azure-cli```"],["SEE ALSO","[az](/man/az)(1), [az-appservice](/man/az-appservice)(1), [az-storage](/man/az-storage)(1), [func](/man/func)(1)"],["RESOURCES","```[Source code](https://github.com/Azure/azure-cli)```\n\n```[Documentation](https://learn.microsoft.com/en-us/cli/azure/functionapp)```"]],"e":["az","az-appservice","az-storage","func"],"x":"az functionapp list"},"az-storage-account":{"n":"az-storage-account","t":"Manage Azure Storage accounts","s":[["TAGLINE","Manage Azure Storage accounts"],["TLDR","**Create a storage account**\n\n```az storage account create -n [account-name] -g [resource-group] -l [westus] --sku [Standard_LRS]```\n\n**List all storage accounts**\n\n```az storage account list -o table```\n\n**Show storage account details**\n\n```az storage account show -n [account-name] -g [resource-group]```\n\n**Get the connection string**\n\n```az storage account show-connection-string -n [account-name] -g [resource-group]```\n\n**List storage account keys**\n\n```az storage account keys list -n [account-name] -g [resource-group]```\n\n**Generate an account-level SAS token**\n\n```az storage account generate-sas --account-name [account-name] --services [bfqt] --resource-types [sco] --permissions [racwdl] --expiry [2026-12-31T00:00Z]```\n\n**Check name availability**\n\n```az storage account check-name --name [proposed-name]```\n\n**Delete a storage account**\n\n```az storage account delete -n [account-name] -g [resource-group] --yes```"],["SYNOPSIS","**az storage account** _subcommand_ [_options_]"],["DESCRIPTION","**az storage account** manages Azure storage accounts, which provide a unique namespace for storing and accessing Azure Storage data objects. Storage accounts support blobs, files, queues, tables, and disks.\n\nDifferent account types and SKUs offer varying performance tiers, redundancy options, and access patterns. StorageV2 (general-purpose v2) accounts support all storage services and features."],["SUBCOMMANDS","**Account Lifecycle**\n> create, delete, list, show, update, check-name\n\n**Access**\n> keys list, keys renew, generate-sas, show-connection-string, revoke-delegation-keys\n\n**Network Security**\n> network-rule add, network-rule list, network-rule remove, private-endpoint-connection\n\n**Blob Service**\n> blob-service-properties show, blob-service-properties update, blob-inventory-policy\n\n**File Service**\n> file-service-properties show, file-service-properties update\n\n**Advanced**\n> management-policy create, encryption-scope create, failover, local-user"],["PARAMETERS","**-n, --name** _value_\n> Storage account name (globally unique, 3-24 chars, lowercase/numbers only)\n\n**-g, --resource-group** _value_\n> Name of the resource group\n\n**-l, --location** _value_\n> Azure region for the storage account\n\n**--sku** _value_\n> Storage SKU. Default **Standard_RAGRS**. Values include Standard_LRS, Standard_GRS, Standard_RAGRS, Standard_ZRS, Standard_GZRS, Standard_RAGZRS, Premium_LRS, Premium_ZRS.\n\n**--kind** _value_\n> Account kind. Default **StorageV2**. Values: StorageV2, Storage, BlobStorage, FileStorage, BlockBlobStorage.\n\n**--access-tier** _value_\n> Access tier for blob data: Hot, Cool, Cold, Premium.\n\n**--https-only** _value_\n> Require HTTPS for requests. Default **true**.\n\n**--min-tls-version** _value_\n> Minimum TLS version allowed: TLS1_0, TLS1_1, TLS1_2, TLS1_3.\n\n**--allow-blob-public-access** _value_\n> Allow anonymous public access to blobs. Default **false** for new accounts.\n\n**--hns**, **--enable-hierarchical-namespace** _value_\n> Enable hierarchical namespace (Azure Data Lake Storage Gen2)."],["CAVEATS","Storage account names must be globally unique across all Azure customers. Changing redundancy options (GRS to LRS) may require data migration. Deleting a storage account is irreversible and removes all contained data. Premium accounts have different pricing and performance characteristics."],["HISTORY","Azure Storage was one of the first Azure services, available since the platform's launch in **2010**. StorageV2 accounts became the recommended default in **2018**, unifying previously separate storage types and enabling new features like access tiers."],["INSTALL","```dnf: sudo dnf install azure-cli```\n\n```pacman: sudo pacman -S azure-cli```\n\n```zypper: sudo zypper install azure-cli```\n\n```brew: brew install azure-cli```\n\n```nix: nix profile install nixpkgs#azure-cli```"],["SEE ALSO","[az](/man/az)(1), [azure-cli](/man/azure-cli)(1), [az-storage](/man/az-storage)(1), [az-storage-blob](/man/az-storage-blob)(1), [az-storage-container](/man/az-storage-container)(1), [azcopy](/man/azcopy)(1)"],["RESOURCES","```[Documentation](https://learn.microsoft.com/en-us/cli/azure/storage/account)```\n\n```[Homepage](https://learn.microsoft.com/en-us/cli/azure/)```\n\n```[Source code](https://github.com/Azure/azure-cli)```"]],"e":["az","azure-cli","az-storage","az-storage-blob","az-storage-container","azcopy"],"x":"az storage account create -n [account-name] -g [resource-group] -l [westus] --sku [Standard_LRS]"},"az-storage":{"n":"az-storage","t":"Manage Azure Cloud Storage resources","s":[["TAGLINE","Manage Azure Cloud Storage resources"],["TLDR","**Copy a local file** to blob storage\n\n```az storage copy -s [/path/to/file.txt] -d https://[account].blob.core.windows.net/[container]/[blob]```\n\n**Download a blob** to local filesystem\n\n```az storage copy -s https://[account].blob.core.windows.net/[container]/[blob] -d [/path/to/file.txt]```\n\n**Upload a directory recursively**\n\n```az storage copy -s [/path/to/dir] -d https://[account].blob.core.windows.net/[container]/[path/] --recursive```\n\n**Copy between storage accounts**\n\n```az storage copy -s https://[src].blob.core.windows.net/[container]/[blob] -d https://[dest].blob.core.windows.net/[container]/[blob]```\n\n**Delete a blob**\n\n```az storage remove -c [container-name] -n [blob-name] --account-name [account]```\n\n**Delete a directory** recursively\n\n```az storage remove -c [container-name] -n [path/to/directory] --recursive --account-name [account]```\n\n**Copy from AWS S3** to Azure Blob\n\n```az storage copy -s https://s3.amazonaws.com/[bucket]/[object] -d https://[account].blob.core.windows.net/[container]/[blob]```"],["SYNOPSIS","**az storage** _subcommand_ [_options_]"],["DESCRIPTION","**az storage** manages Azure Cloud Storage resources including storage accounts, blob containers, file shares, queues, and tables. It provides commands for uploading, downloading, copying, and managing stored data.\n\nAzure Storage offers multiple storage types: Blob storage for unstructured data, File storage for SMB file shares, Queue storage for messaging, and Table storage for NoSQL data."],["SUBCOMMANDS","**Account Management**\n> account create, account delete, account list, account show, account keys list\n\n**Blob Storage**\n> blob upload, blob download, blob delete, blob list, blob copy\n\n**Containers**\n> container create, container delete, container list, container lease\n\n**File Shares**\n> share create, share delete, share list, file upload, file download\n\n**Copy Operations**\n> copy, remove\n\n**Data Lake Gen2**\n> fs create, fs file upload, fs directory create\n\n**Queue & Table**\n> queue create, queue list, message put, table create, entity insert"],["PARAMETERS","**--account-name** _value_\n> Storage account name\n\n**--account-key** _value_\n> Storage account access key\n\n**--connection-string** _value_\n> Storage account connection string\n\n**-c, --container** _value_\n> Blob container name\n\n**-s, --source** _value_\n> Source path (local or URL)\n\n**-d, --destination** _value_\n> Destination path (local or URL)\n\n**--recursive**\n> Process subdirectories recursively.\n\n**--auth-mode** _value_\n> Authentication mode: **key** (uses account key) or **login** (uses Azure AD credentials).\n\n**--sas-token** _value_\n> Shared access signature token used to authenticate the request."],["CAVEATS","Commands support multiple authentication methods: account key, SAS token, Azure AD login, or connection string. The **copy** and **remove** commands use AzCopy under the hood and support S3-to-Azure and Google Cloud Storage-to-Azure transfers. Wildcard patterns work differently than shell globbing; use **--include-pattern** and **--exclude-pattern** for filtering. Some subcommands require installing the **storage-preview** extension."],["HISTORY","**az storage** is part of the **Azure CLI**, Microsoft's cross-platform command-line tool first released in **2014** (originally as **xplat-cli**). The modern `az` interface rewritten in Python launched in **2017**. The **az storage copy** and **az storage remove** commands integrate **AzCopy v10**, replacing older upload/download flows with a faster, resumable transfer engine."],["INSTALL","```dnf: sudo dnf install azure-cli```\n\n```pacman: sudo pacman -S azure-cli```\n\n```zypper: sudo zypper install azure-cli```\n\n```brew: brew install azure-cli```\n\n```nix: nix profile install nixpkgs#azure-cli```"],["SEE ALSO","[az](/man/az)(1), [az-storage-account](/man/az-storage-account)(1), [az-storage-blob](/man/az-storage-blob)(1), [azcopy](/man/azcopy)(1)"]],"e":["az","az-storage-account","az-storage-blob","azcopy"],"x":"az storage copy -s [/path/to/file.txt] -d https://[account].blob.core.windows.net/[container]/[blob]"},"az-vm":{"n":"az-vm","t":"Manage Azure virtual machines","s":[["TAGLINE","Manage Azure virtual machines"],["TLDR","**Create** virtual machine\n\n```az vm create --resource-group [MyRG] --name [MyVM] --image [Ubuntu2204]```\n\n**List** VMs\n\n```az vm list```\n\n**Start** VM\n\n```az vm start --resource-group [MyRG] --name [MyVM]```\n\n**Stop** VM\n\n```az vm stop --resource-group [MyRG] --name [MyVM]```\n\n**Deallocate** VM (stop compute billing)\n\n```az vm deallocate --resource-group [MyRG] --name [MyVM]```\n\n**Delete** VM\n\n```az vm delete --resource-group [MyRG] --name [MyVM]```\n\n**Get** VM details\n\n```az vm show --resource-group [MyRG] --name [MyVM]```"],["SYNOPSIS","**az vm** _command_ [_options_]"],["DESCRIPTION","**az vm** manages Azure virtual machines. It provides commands for creating, starting, stopping, configuring, and deleting VMs, as well as managing disks, network interfaces, and extensions.\n\nThe command group enables full lifecycle management of Azure compute resources."],["PARAMETERS","**create**\n> Create new virtual machine\n\n**list**\n> List virtual machines\n\n**start**\n> Start VM\n\n**stop**\n> Stop VM (still incurs compute charges)\n\n**deallocate**\n> Deallocate VM (stops billing for compute)\n\n**restart**\n> Restart VM\n\n**delete**\n> Delete VM\n\n**show**\n> Get VM details\n\n**--resource-group**, **-g** _name_\n> Resource group name\n\n**--name**, **-n** _name_\n> VM name\n\n**--image**\n> OS image (Ubuntu2204, Win2022Datacenter, etc.)\n\n**--size**\n> VM size (Standard_B2s, Standard_D2s_v3, etc.)\n\n**--admin-username**\n> Admin username\n\n**--admin-password**\n> Admin password\n\n**--ssh-key-values**\n> SSH public key(s)"],["COMMON OPERATIONS","```bash"],["Create Ubuntu VM with SSH key","az vm create -g MyRG -n MyVM --image Ubuntu2204 --ssh-key-values ~/.ssh/id_rsa.pub"],["Get VM IP address","az vm show -g MyRG -n MyVM -d --query publicIps -o tsv"],["Resize VM","az vm resize -g MyRG -n MyVM --size Standard_D4s_v3"],["Run command on VM","az vm run-command invoke -g MyRG -n MyVM --command-id RunShellScript --scripts \"apt-get update\"\n```"],["CAVEATS","Creating VMs incurs costs. **az vm stop** does not deallocate -- use **az vm deallocate** to stop billing for compute. Some VM sizes are not available in all regions. Deleting a VM does not delete associated resources (disks, NICs) by default. The **UbuntuLTS** image alias was deprecated in 2023; use **Ubuntu2204** or similar instead."],["HISTORY","**az vm** has been part of Azure CLI since version 2.0 in **2017**, providing comprehensive VM management capabilities."],["INSTALL","```dnf: sudo dnf install azure-cli```\n\n```pacman: sudo pacman -S azure-cli```\n\n```zypper: sudo zypper install azure-cli```\n\n```brew: brew install azure-cli```\n\n```nix: nix profile install nixpkgs#azure-cli```"],["SEE ALSO","[az](/man/az)(1), [az-group](/man/az-group)(1), [az-network](/man/az-network)(1)"]],"e":["az","az-group","az-network"],"x":"az vm create --resource-group [MyRG] --name [MyVM] --image [Ubuntu2204]"},"banshee":{"n":"banshee","t":"Music player and media manager","s":[["TAGLINE","Music player and media manager"],["TLDR","**Start** Banshee music player\n\n```banshee```\n\n**Play** specific file\n\n```banshee [music.mp3]```\n\n**Import** media\n\n```banshee --import [/path/to/music]```"],["SYNOPSIS","**banshee** [_options_] [_files_]"],["DESCRIPTION","**banshee** is a cross-platform music player and media management application for Linux. It supports audio and video playback, library management, podcasts, Internet radio, and syncing with portable media devices.\n\nThe application uses Mono and GTK+ to provide a feature-rich media experience."],["PARAMETERS","**--import** _path_\n> Import media from directory\n\n**--play**\n> Start playback\n\n**--pause**\n> Pause playback\n\n**--next**\n> Skip to next track\n\n**--previous**\n> Previous track\n\n**--volume** _level_\n> Set volume (0-100)\n\n**--show-import-media**\n> Show import dialog"],["FEATURES","- Music library management\n- Smart playlists\n- Podcast subscriptions\n- Internet radio\n- iPod/Android sync\n- Album artwork\n- Last.fm scrobbling\n- Extensions support"],["CAVEATS","Development slowed significantly. Many distributions no longer package it. Mono dependency can be large. Some hardware sync features may not work. Consider alternatives like Rhythmbox or Clementine."],["HISTORY","**Banshee** was created by Aaron Bockover in **2005** and became GNOME's default music player for several years before being replaced by GNOME Music."],["SEE ALSO","[clementine](/man/clementine)(1), [audacious](/man/audacious)(1)"]],"e":["clementine","audacious"],"x":"banshee"},"beets":{"n":"beets","t":"Music library manager and metadata organizer","s":[["TAGLINE","Music library manager and metadata organizer"],["TLDR","**Import music into your library**\n\n```beet import [path/to/music]```\n\n**Import without auto-tagging**\n\n```beet import -A [path/to/music]```\n\n**List all albums**\n\n```beet ls -a```\n\n**Search for tracks**\n\n```beet ls [artist:beatles]```\n\n**Update library statistics**\n\n```beet stats```\n\n**Move files to match library organization**\n\n```beet move```\n\n**Modify metadata**\n\n```beet modify [query] [field=value]```\n\n**Fetch missing album art**\n\n```beet fetchart```"],["SYNOPSIS","**beet** _command_ [_options_] [_query_]"],["PARAMETERS","**import** [_path_]\n> Import music files into the library, tagging via MusicBrainz.\n\n**list** (**ls**) [_query_]\n> List tracks or albums matching query.\n\n**modify** _query_ _field=value_\n> Modify metadata for matching items.\n\n**move** (**mv**) [_query_]\n> Move (or copy with -c) files to match configured paths.\n\n**remove** (**rm**) _query_\n> Remove items from the library (add -d to delete the files too).\n\n**update** (**upd**) [_query_]\n> Update library from on-disk tag changes.\n\n**write** [_query_]\n> Write database metadata back into the file tags.\n\n**stats** [_query_]\n> Show library statistics.\n\n**fields**\n> List the available metadata fields for queries.\n\n**config** [**-pe**]\n> Show config (-p path, -e edit in $EDITOR).\n\n**-a**, **--album**\n> Operate on albums instead of individual tracks.\n\n**-f**, **--format** _template_\n> Custom output format string for list output.\n\n**-A**, **--noautotag**\n> Skip automatic MusicBrainz tagging during import.\n\n**-s**, **--singletons**\n> Import tracks as standalone singletons rather than albums.\n\n**-c**, **--copy** / **-C**, **--nocopy**\n> Copy files into the library directory (or leave them in place).\n\n**-t**, **--timid**\n> Prompt for confirmation on every import candidate."],["QUERY SYNTAX","**artist:name**: Match artist field\n**album:name**: Match album field\n**year:2020**: Match year\n**year:2018..2022**: Year range\n**bitrate:320..**: Minimum bitrate\n**path:/music/**: Match file path"],["DESCRIPTION","**beets** is a music library manager and organizer that automatically tags, organizes, and manages music collections. It queries online databases like MusicBrainz to identify albums and correct metadata.\n\nDuring import, beets identifies albums, fixes metadata, downloads album art, and moves files into a structured directory hierarchy. The query system allows powerful searches and bulk modifications across the library.\n\nThe plugin system extends functionality with features like fetching lyrics, managing play counts, converting formats, and integrating with music players. Configuration is stored in **~/.config/beets/config.yaml**."],["PLUGINS","**fetchart**: Download album artwork\n**lyrics**: Fetch song lyrics\n**lastgenre**: Get genres from Last.fm\n**convert**: Transcode to different formats\n**duplicates**: Find duplicate tracks\n**web**: Web interface for browsing"],["CONFIGURATION","**~/.config/beets/config.yaml**\n> Main configuration file for library paths, import behavior, plugins, and metadata preferences. Set the **BEETSDIR** environment variable to use a different directory (and library database). Run `beet config -p` to print the active path."],["CAVEATS","Initial import of large libraries can be time-consuming due to online lookups. Automatic matching may fail for obscure releases or poorly-tagged files. The database must be kept in sync with actual files; moving files outside beets can cause inconsistencies."],["HISTORY","Beets was created by **Adrian Sampson** starting in **2010** as a research project while pursuing a PhD in computer science. It grew from a personal tool into a comprehensive music management system. The project has been open source since inception and benefits from community-contributed plugins. It remains one of the most capable command-line music library managers available."],["INSTALL","```dnf: sudo dnf install beets```\n\n```pacman: sudo pacman -S beets```\n\n```zypper: sudo zypper install beets```\n\n```brew: brew install beets```\n\n```nix: nix profile install nixpkgs#beets```"],["SEE ALSO","[picard](/man/picard)(1), [kid3](/man/kid3)(1), [metaflac](/man/metaflac)(1), [mpd](/man/mpd)(1), [mpc](/man/mpc)(1), [ffmpeg](/man/ffmpeg)(1)"],["RESOURCES","```[Source code](https://github.com/beetbox/beets)```\n\n```[Documentation](https://beets.readthedocs.io/)```"]],"e":["picard","kid3","metaflac","mpd","mpc","ffmpeg"],"x":"beet import [path/to/music]"},"cvt":{"n":"cvt","t":"VESA CVT display modeline calculator","s":[["TAGLINE","VESA CVT display modeline calculator"],["TLDR","**Generate a modeline** for a resolution and refresh rate\n\n```cvt [1920] [1080] [60]```\n\n**Generate reduced blanking modeline** (for LCD displays)\n\n```cvt -r [1920] [1080] [60]```\n\n**Generate modeline with reduced blanking v2**\n\n```cvt -r2 [2560] [1440] [144]```\n\n**Generate verbose output** with more details\n\n```cvt -v [1920] [1200] [75]```"],["SYNOPSIS","**cvt** [_options_] _width_ _height_ [_refresh_]"],["PARAMETERS","_WIDTH_\n> Horizontal resolution in pixels.\n\n_HEIGHT_\n> Vertical resolution in pixels.\n\n_REFRESH_\n> Refresh rate in Hz (default: 60).\n\n**-r**, **--reduced**\n> Generate reduced blanking timings (CVT-RB).\n\n**-r2**, **--reduced-v2**\n> Generate reduced blanking v2 timings (CVT-RBv2).\n\n**-v**, **--verbose**\n> Enable verbose output.\n\n**-h**, **--help**\n> Display help information.\n\n**-V**, **--version**\n> Display version information."],["DESCRIPTION","**cvt** calculates VESA Coordinated Video Timing (CVT) modelines for X11 display configuration. It outputs modeline parameters suitable for use with xrandr or in xorg.conf files.\n\nCVT is the standard formula for calculating display timing parameters from resolution and refresh rate. The tool generates timing values including pixel clock, horizontal and vertical sync parameters, and blanking intervals.\n\nReduced blanking modes (**-r**, **-r2**) are designed for LCD displays and digital connections (HDMI, DisplayPort), using less bandwidth than traditional CRT-compatible timings. This enables higher resolutions and refresh rates over limited bandwidth connections."],["CAVEATS","Generated modelines may not be supported by all displays or graphics hardware. Reduced blanking is not compatible with CRT monitors. Custom timings can potentially damage displays if incorrect. Always verify timings are within display specifications."],["HISTORY","The CVT standard was developed by **VESA** (Video Electronics Standards Association) to provide a standardized method for calculating display timings. The cvt tool is part of the xorg-server utilities, implementing the CVT 1.1 and CVT-RB specifications."],["INSTALL","```apt: sudo apt install xcvt```\n\n```dnf: sudo dnf install libxcvt```\n\n```pacman: sudo pacman -S libxcvt```\n\n```apk: sudo apk add libxcvt```\n\n```zypper: sudo zypper install libxcvt```\n\n```brew: brew install libxcvt```\n\n```nix: nix profile install nixpkgs#libxcvt```"],["SEE ALSO","[gtf](/man/gtf)(1), [xrandr](/man/xrandr)(1)"]],"e":["gtf","xrandr"],"x":"cvt [1920] [1080] [60]"},"dc":{"n":"dc","t":"arbitrary-precision reverse Polish notation calculator","s":[["TAGLINE","arbitrary-precision reverse Polish notation calculator"],["TLDR","**Start interactive** calculator\n\n```dc```\n\n**Evaluate expression** directly\n\n```echo \"[5 3 + p]\" | dc```\n\n**Multiply two numbers**\n\n```echo \"[4 7 * p]\" | dc```\n\n**Calculate with decimal** precision\n\n```echo \"[10 k 22 7 / p]\" | dc```\n\n**Execute from file**\n\n```dc [script.dc]```\n\n**Store and recall** values\n\n```echo \"[5 sa la 3 + p]\" | dc```\n\n**Calculate factorial** of 5\n\n```echo \"[5 [d 1 - d 1 <f *] d sf x p]\" | dc```"],["SYNOPSIS","**dc** [_options_] [_file_...]"],["PARAMETERS","_FILE_\n> File containing dc commands to execute.\n\n**-e** _EXPR_, **--expression** _EXPR_\n> Evaluate expression from command line.\n\n**-f** _FILE_, **--file** _FILE_\n> Read commands from file.\n\n**-h**, **--help**\n> Display help information.\n\n**-V**, **--version**\n> Display version information."],["DESCRIPTION","**dc** is an arbitrary precision desk calculator using reverse Polish notation (RPN). Numbers and operators are entered sequentially, with operators acting on values in a stack. It supports arithmetic, comparisons, and programmable macros.\n\nBasic operations push numbers onto the stack and apply operators: 5 3 + pushes 5 and 3, then adds them. The p command prints the top of stack. The k command sets decimal precision for division and other operations.\n\ndc provides registers (a-z) for storing values, conditional execution, loops through macros, and string manipulation. Despite its terse syntax, it can implement complex algorithms including recursion."],["CAVEATS","RPN syntax can be unintuitive for users accustomed to infix notation. Error messages are minimal. Complex programs are difficult to read and debug. Some extended features vary between implementations."],["HISTORY","dc is one of the oldest Unix utilities, written by **Lorinda Cherry** and **Robert Morris** at Bell Labs around **1969-1971**. It predates the C programming language and was originally implemented in B. The bc calculator was later written as a preprocessor for dc."],["INSTALL","```apt: sudo apt install dc```\n\n```dnf: sudo dnf install bc```\n\n```pacman: sudo pacman -S bc```\n\n```apk: sudo apk add bc```\n\n```zypper: sudo zypper install bc```\n\n```brew: brew install bc```\n\n```nix: nix profile install nixpkgs#bc```"],["SEE ALSO","[bc](/man/bc)(1), [calc](/man/calc)(1), [expr](/man/expr)(1)"]],"e":["bc","calc","expr"],"x":"dc"},"dconf-reset":{"n":"dconf-reset","t":"reset dconf keys to default values","s":[["TAGLINE","reset dconf keys to default values"],["TLDR","**Reset** a specific key\n\n```dconf reset /[path/to/key]```\n\nReset an entire **directory**\n\n```dconf reset -f /[path/to/directory]/```"],["SYNOPSIS","**dconf reset** [_options_] _path_"],["DESCRIPTION","**dconf reset** removes keys from the dconf database, effectively resetting them to their default values. The defaults are typically defined by application GSettings schemas, and removing the user's stored value causes the application to fall back to these defaults.\n\nWhen resetting a single key, only that specific setting is removed. Using the -f flag with a directory path removes all keys under that directory tree, which is useful for completely resetting a configuration section or entire application settings.\n\nThis operation is immediate and cannot be undone through dconf itself. Consider using \"dconf dump\" before resetting to create a backup if you might need to restore the previous values. The reset operation is commonly used when troubleshooting application behavior or returning to a known-good configuration state."],["PARAMETERS","**-f**\n> Force reset of entire directory tree\n\n_path_\n> Key or directory to reset"],["CAVEATS","Resetting removes the stored value; the application will use its schema default. Use -f carefully as it resets all keys in a directory tree. Cannot be undone."],["INSTALL","```apt: sudo apt install dconf-cli```\n\n```dnf: sudo dnf install dconf```\n\n```pacman: sudo pacman -S dconf```\n\n```apk: sudo apk add dconf```\n\n```zypper: sudo zypper install dconf```\n\n```nix: nix profile install nixpkgs#dconf```"],["SEE ALSO","[dconf](/man/dconf)(1), [dconf-read](/man/dconf-read)(1), [dconf-write](/man/dconf-write)(1), [gsettings](/man/gsettings)(1)"]],"e":["dconf","dconf-read","dconf-write","gsettings"],"x":"dconf reset /[path/to/key]"},"djpeg":{"n":"djpeg","t":"JPEG decompressor with format conversion","s":[["TAGLINE","JPEG decompressor with format conversion"],["TLDR","**Decompress JPEG** to PPM\n\n```djpeg [image.jpg] > [output.ppm]```\n\n**Decompress to BMP** format\n\n```djpeg -bmp [image.jpg] > [output.bmp]```\n\n**Decompress to GIF** format\n\n```djpeg -gif [image.jpg] > [output.gif]```\n\n**Scale output** to fraction\n\n```djpeg -scale [1/2] [image.jpg] > [output.ppm]```\n\n**Output grayscale only**\n\n```djpeg -grayscale [image.jpg] > [output.ppm]```\n\n**Show verbose info**\n\n```djpeg -verbose [image.jpg] > [output.ppm]```"],["SYNOPSIS","**djpeg** [_options_] [_file_]"],["PARAMETERS","_FILE_\n> Input JPEG file (stdin if omitted).\n\n**-bmp**\n> Output BMP format.\n\n**-gif**\n> Output GIF format.\n\n**-ppm**\n> Output PPM format (default).\n\n**-targa**\n> Output Targa format.\n\n**-scale** _M/N_\n> Scale output by fraction.\n\n**-grayscale**\n> Output grayscale only.\n\n**-colors** _N_\n> Quantize to N colors.\n\n**-verbose**, **-debug**\n> Show debug information."],["DESCRIPTION","**djpeg** decompresses JPEG images to various uncompressed or lossless formats. It's part of the libjpeg/libjpeg-turbo package and provides command-line JPEG decoding with format conversion.\n\nThe tool reads JPEG files and outputs PPM, BMP, GIF, or Targa format. Scaling during decompression is efficient as it uses DCT coefficient scaling rather than full decompression followed by resizing.\n\ndjpeg is useful for batch processing, scripting, and converting JPEG images to formats suitable for further processing with other tools."],["CAVEATS","Output goes to stdout by default (redirect to file). Color quantization for GIF may lose quality. Not all JPEG features supported equally. Progressive JPEG processing requires buffering."],["HISTORY","djpeg is part of the **libjpeg** project created by the **Independent JPEG Group (IJG)** starting in **1991**. libjpeg-turbo, a high-performance fork, provides compatible djpeg with SIMD optimizations."],["INSTALL","```apt: sudo apt install libjpeg-turbo-progs```\n\n```dnf: sudo dnf install libjpeg-turbo-utils```\n\n```apk: sudo apk add libjpeg-turbo-utils```"],["SEE ALSO","[cjpeg](/man/cjpeg)(1), [jpegtran](/man/jpegtran)(1), [convert](/man/convert)(1)"],["RESOURCES","```[Source code](https://github.com/libjpeg-turbo/libjpeg-turbo)```\n\n```[Homepage](https://libjpeg-turbo.org/)```"]],"e":["cjpeg","jpegtran","convert"],"x":"djpeg [image.jpg] > [output.ppm]"},"dotnet-add-package":{"n":"dotnet-add-package","t":"Add or update a NuGet package reference in a project","s":[["TAGLINE","Add or update a NuGet package reference in a project"],["TLDR","**Add a NuGet package**\n\n```dotnet add package [package_name]```\n\n**Add specific version**\n\n```dotnet add package [package_name] --version [1.0.0]```\n\n**Add package to specific project**\n\n```dotnet add [project.csproj] package [package_name]```\n\n**Add prerelease version**\n\n```dotnet add package [package_name] --prerelease```\n\n**Add from specific source**\n\n```dotnet add package [package_name] --source [https://api.nuget.org/v3/index.json]```\n\n**Add package targeting a specific framework**\n\n```dotnet add package [package_name] --framework [net8.0]```"],["SYNOPSIS","**dotnet** **add** [_project_] **package** _package_name_ [_options_]"],["PARAMETERS","**-v**, **--version** _version_\n> Specific version to install.\n\n**--prerelease**\n> Allow prerelease packages to be installed.\n\n**-f**, **--framework** _framework_\n> Add a package reference only when targeting a specific framework.\n\n**-s**, **--source** _source_\n> NuGet package source URI to use during restore.\n\n**-n**, **--no-restore**\n> Add reference without performing a restore preview and compatibility check.\n\n**--package-directory** _dir_\n> Directory to restore packages to.\n\n**--interactive**\n> Allow the command to stop and wait for user input or action."],["DESCRIPTION","**dotnet add package** adds a NuGet package reference to a project file and runs a compatibility check. It modifies the .csproj or .fsproj file to include a PackageReference element, making the dependency trackable in version control.\n\nIf the package is already referenced, it updates the reference to the latest compatible version. The command runs an implicit **dotnet restore** after adding the reference unless **--no-restore** is specified. Projects using Central Package Management (CPM) will have the PackageVersion element updated in the Directory.Packages.props file."],["INSTALL","```dnf: sudo dnf install dotnet-host```\n\n```pacman: sudo pacman -S dotnet-host```\n\n```apk: sudo apk add dotnet-host```\n\n```brew: brew install dotnet```"],["SEE ALSO","[dotnet-restore](/man/dotnet-restore)(1), [dotnet-build](/man/dotnet-build)(1), [dotnet](/man/dotnet)(1)"]],"e":["dotnet-restore","dotnet-build","dotnet"],"x":"dotnet add package [package_name]"},"dtrx":{"n":"dtrx","t":"intelligent archive extraction tool","s":[["TAGLINE","intelligent archive extraction tool"],["TLDR","**Extract an archive** into its own directory\n\n```dtrx [archive.tar.gz]```\n\n**Extract multiple archives**\n\n```dtrx [*.tar.gz]```\n\n**List archive contents** without extracting\n\n```dtrx -l [archive.tar.gz]```\n\n**Extract into the current directory** instead of a dedicated one\n\n```dtrx -f [archive.tar.gz]```\n\n**Extract nested archives** too, recursively\n\n```dtrx -r [archive.tar.gz]```\n\n**Overwrite** an existing target directory instead of picking a new name\n\n```dtrx -o [archive.tar.gz]```\n\n**Extract the metadata** from a .deb or .gem\n\n```dtrx -m [package.deb]```\n\n**Never prompt**, taking conservative defaults\n\n```dtrx -n [archive.tar.gz]```"],["SYNOPSIS","**dtrx** [_options_] _archive_..."],["PARAMETERS","_ARCHIVE_\n> Archive file(s) to extract.\n\n**-r**, **--recursive**\n> Look inside the archive for further archives and extract those as well.\n\n**-f**, **--flat**\n> Extract everything into the **current** directory rather than a dedicated one.\n\n**-o**, **--overwrite**\n> Use the default directory name even if it already exists, overwriting it, instead of choosing an alternative name. This is **not** an output-directory option.\n\n**-l**, **-t**, **--list**, **--table**\n> List the contents without extracting.\n\n**-m**, **--metadata**\n> Extract the *metadata* from a `.deb` or `.gem` package rather than its contents.\n\n**--one**, **--one-entry** _MODE_\n> How to handle an archive containing a single file or directory: **inside** (wrap it in a directory named after the archive; the default), **rename** (rename it to match the archive), or **here** (extract it into the current directory as-is).\n\n**-n**, **--noninteractive**\n> Never prompt; use conservative defaults instead.\n\n**-q**, **--quiet**\n> Suppress warnings. Give it twice for silence.\n\n**-v**, **--verbose**\n> List the files extracted. Give it twice for debugging output.\n\n**--help** / **--version**\n> Display help, or version and copyright information."],["DESCRIPTION","**dtrx** stands for \"Do The Right Extraction\", and its purpose is to remove every decision you would otherwise have to make about an archive.\n\nIt identifies the format from the *contents* rather than the file name, so it handles tar in all its compressions, zip, rar, 7z, cpio, deb, rpm, gem, and more without being told which is which, and it invokes whichever underlying tool is appropriate.\n\nIts most valuable behaviour is preventing a **tarbomb**: an archive whose members are not wrapped in a top-level directory and which therefore scatters files across your current directory. dtrx always extracts into a single directory named after the archive, and if the archive already has one sensible top-level directory it uses that instead of nesting redundantly. Where a name would collide, dtrx picks a fresh one rather than clobbering what is there.\n\nIt also normalises permissions, so a directory that arrives unreadable or a file that arrives world-writable is corrected on extraction."],["CAVEATS","**-o does not take an output directory.** It means *overwrite*, and passing `dtrx -o mydir archive.tar.gz` will try to extract two things: the archive, and a file called `mydir`. To extract somewhere specific, change directory first. Likewise **-m** does not preserve ownership or permissions; it extracts the metadata section of a `.deb` or `.gem`.\n\ndtrx is a front end and does not implement any format itself, so it can only handle what is installed: without `unrar` or `p7zip` on the system, the corresponding archives simply fail.\n\nThe original project has been unmaintained for years, and its Python 2 codebase forced distributions either to drop it or to carry a Python 3 port, so the version available varies. **atool** covers much the same ground and is still maintained."],["HISTORY","dtrx was written by **Brett Smith** in **2006**, in a period when the tarbomb was a genuine hazard and every archive format wanted a different incantation, `tar xzf` here, `tar xjf` there, `unzip` for one thing and `unrar x` for another. The tool's premise, that the computer can obviously work this out for itself, was compelling enough that it became a fixture in distributions long after `tar` grew automatic decompression detection and made half the original problem go away."],["INSTALL","```apk: sudo apk add dtrx```\n\n```zypper: sudo zypper install dtrx```\n\n```brew: brew install dtrx```\n\n```nix: nix profile install nixpkgs#dtrx```"],["SEE ALSO","[tar](/man/tar)(1), [unzip](/man/unzip)(1), [7z](/man/7z)(1), [atool](/man/atool)(1), [unar](/man/unar)(1)"],["RESOURCES","```[Source code](https://github.com/dtrx-py/dtrx)```"]],"e":["tar","unzip","7z","atool","unar"],"x":"dtrx [archive.tar.gz]"},"fulcio":{"n":"fulcio","t":"certificate authority for keyless code signing","s":[["TAGLINE","certificate authority for keyless code signing"],["TLDR","**Start Fulcio server** with a config file\n\n```fulcio serve --config-path [config.yaml]```\n\n**Start server** with file-based CA\n\n```fulcio serve --ca fileca --fileca-cert [ca.pem] --fileca-key [ca-key.pem]```\n\n**Start server** on a specific host and port\n\n```fulcio serve --host [0.0.0.0] --port [8080]```"],["SYNOPSIS","**fulcio** _command_ [_options_]"],["PARAMETERS","**serve**\n> Start Fulcio server.\n\n**--ca** _type_\n> Certificate authority type: googleca, fileca, kmsca, tinkca, pkcs11ca, ephemeralca.\n\n**--config-path** _file_\n> Path to Fulcio config YAML (default: /etc/fulcio-config/config.yaml).\n\n**--host** _addr_\n> HTTP server listen address (default: 0.0.0.0).\n\n**--port** _num_\n> HTTP server port (default: 8080).\n\n**--grpc-port** _num_\n> gRPC server port (default: 8081).\n\n**--fileca-cert** _file_\n> Path to CA certificate (fileca only).\n\n**--fileca-key** _file_\n> Path to CA encrypted private key (fileca only).\n\n**--ct-log-url** _url_\n> Certificate transparency log URL.\n\n**version**\n> Show version."],["CONFIGURATION","**/etc/fulcio-config/config.yaml**\n> Server configuration including OIDC provider settings, certificate policies, and CA parameters."],["DESCRIPTION","**Fulcio** is a certificate authority for code signing, part of the Sigstore project. It issues short-lived certificates tied to OIDC identities (GitHub, Google, Microsoft), enabling keyless code signing.\n\nFulcio eliminates the need for developers to manage long-term signing keys. Certificates are logged in the transparency log (Rekor) for verification."],["WORKFLOW","```\n1. Developer authenticates via OIDC\n2. Fulcio issues short-lived certificate\n3. Developer signs artifact\n4. Signature logged in Rekor\n5. Verifier checks Rekor and certificate chain\n```"],["CAVEATS","Requires OIDC provider integration. Certificates are short-lived by design. Production requires proper CA management. Part of larger Sigstore ecosystem."],["HISTORY","Fulcio is part of **Sigstore**, initiated by **Google**, **Red Hat**, and **Purdue University** in **2021** to make code signing accessible. Named after a Roman consul, it provides the CA component of the keyless signing infrastructure."],["INSTALL","```pacman: sudo pacman -S fulcio```\n\n```nix: nix profile install nixpkgs#fulcio```"],["SEE ALSO","[cosign](/man/cosign)(1), [rekor-cli](/man/rekor-cli)(1)"]],"e":["cosign","rekor-cli"],"x":"fulcio serve --config-path [config.yaml]"},"fusermount":{"n":"fusermount","t":"FUSE filesystem mount manager","s":[["TAGLINE","FUSE filesystem mount manager"],["TLDR","**Unmount FUSE filesystem**\n\n```fusermount -u [mountpoint]```\n\n**Force unmount** (lazy)\n\n```fusermount -uz [mountpoint]```\n\n**Show version**\n\n```fusermount -V```\n\n**List all FUSE mounts**\n\n```mount -t fuse```\n\n**Unmount with options**\n\n```fusermount -u -o [option] [mountpoint]```"],["SYNOPSIS","**fusermount** [_options_] _mountpoint_"],["DESCRIPTION","**fusermount** is a utility for mounting and unmounting FUSE (Filesystem in Userspace) filesystems. It's primarily used for unmounting, as mounting is typically done by the filesystem program itself.\n\nThe tool enables non-root users to mount and unmount filesystems, subject to proper permissions and configuration."],["PARAMETERS","**-u**\n> Unmount filesystem.\n\n**-z**\n> Lazy unmount (like umount -l).\n\n**-q**\n> Quiet mode.\n\n**-o** _options_\n> Mount options.\n\n**-V**\n> Display version.\n\n**-h**\n> Display help."],["CONFIGURATION","**/etc/fuse.conf**\n> Controls system-wide FUSE settings including user_allow_other and mount_max."],["CAVEATS","Lazy unmount may leave filesystem in use. Regular users need proper permissions. Some options require root. Mount must be from fusermount or user_allow_other."],["HISTORY","**fusermount** is part of the **FUSE** (Filesystem in Userspace) project, originally created by **Miklos Szeredi** around **2001**. FUSE enables userspace programs to export filesystems to the kernel, revolutionizing filesystem development on Linux."],["INSTALL","```apt: sudo apt install fuse3```\n\n```dnf: sudo dnf install fuse3```\n\n```pacman: sudo pacman -S fuse3```\n\n```apk: sudo apk add fuse```\n\n```zypper: sudo zypper install fuse3```\n\n```nix: nix profile install nixpkgs#fuse3```"],["SEE ALSO","[mount](/man/mount)(1), [umount](/man/umount)(1), [sshfs](/man/sshfs)(1), [encfs](/man/encfs)(1)"],["RESOURCES","```[Source code](https://github.com/libfuse/libfuse)```"]],"e":["mount","umount","sshfs","encfs"],"x":"fusermount -u [mountpoint]"},"gcov":{"n":"gcov","t":"test coverage analysis program","s":[["TAGLINE","test coverage analysis program"],["TLDR","Generate a **coverage report** named file.cpp.gcov\n\n```gcov [path/to/file.cpp]```\n\nWrite individual execution counts for every **basic block**\n\n```gcov -a [path/to/file.cpp]```\n\nWrite **branch frequencies** to output and print summary as percentage\n\n```gcov -b [path/to/file.cpp]```\n\nWrite branch frequencies as **number of branches taken**\n\n```gcov -c [path/to/file.cpp]```\n\nDo not create a **gcov output file**\n\n```gcov -n [path/to/file.cpp]```\n\nWrite file level and **function level summaries**\n\n```gcov -f [path/to/file.cpp]```"],["SYNOPSIS","**gcov** [_options_] _source-files_"],["PARAMETERS","**-a**, **--all-blocks**\n> Write individual execution counts for every basic block\n\n**-b**, **--branch-probabilities**\n> Write branch frequencies to output file as percentages\n\n**-c**, **--branch-counts**\n> Write branch frequencies as counts rather than percentages\n\n**-f**, **--function-summaries**\n> Output summaries for each function in addition to file summary\n\n**-n**, **--no-output**\n> Do not create gcov output file\n\n**-l**, **--long-file-names**\n> Use long file names for output files\n\n**-p**, **--preserve-paths**\n> Preserve complete path information in output file names\n\n**-o**, **--object-directory** _DIR_\n> Search for object files in _DIR_\n\n**-s**, **--source-prefix** _PREFIX_\n> Remove source path prefix for shorter output names"],["DESCRIPTION","**gcov** is a test coverage program used with GCC to analyze which parts of a program have been executed during testing. It helps identify untested code paths, improving test suite completeness.\n\nTo use gcov, compile your program with **-fprofile-arcs -ftest-coverage** (or **--coverage**). After running the program, gcov reads the generated .gcno and .gcda files to produce annotated source listings showing execution counts for each line.\n\nOutput files (*.gcov) contain the original source code annotated with execution counts. Lines marked **#####** were never executed. Branch information helps identify untested conditional paths."],["CAVEATS","Requires programs to be compiled with special GCC flags. Only works with GCC-compiled code. Running the program multiple times accumulates coverage data unless .gcda files are deleted between runs. Inline functions may show misleading counts."],["HISTORY","gcov has been part of the GNU Compiler Collection (GCC) since the early 1990s. It evolved alongside GCC's profiling capabilities and remains a fundamental tool for C/C++ code coverage analysis in the open-source ecosystem."],["INSTALL","```apt: sudo apt install gcc```\n\n```dnf: sudo dnf install gcc```\n\n```pacman: sudo pacman -S gcc```\n\n```apk: sudo apk add gcc```\n\n```zypper: sudo zypper install gcc```\n\n```brew: brew install gcc```\n\n```nix: nix profile install nixpkgs#gcc```"],["SEE ALSO","[gcc](/man/gcc)(1), [lcov](/man/lcov)(1), [gprof](/man/gprof)(1)"],["RESOURCES","```[Documentation](https://gcc.gnu.org/onlinedocs/gcc/Gcov.html)```"]],"e":["gcc","lcov","gprof"],"x":"gcov [path/to/file.cpp]"},"genisoimage":{"n":"genisoimage","t":"ISO9660 filesystem image creator","s":[["TAGLINE","ISO9660 filesystem image creator"],["TLDR","Create an **ISO image** from the given source directory\n\n```genisoimage -o myimage.iso [path/to/source_directory]```\n\nCreate an ISO image with files **larger than 2 GiB**\n\n```genisoimage -allow-limited-size -o myimage.iso [path/to/source_directory]```\n\nCreate a **bootable ISO** with Rock Ridge and Joliet extensions\n\n```genisoimage -R -J -V \"[VOLUME_LABEL]\" -b [boot/isolinux.bin] -c [boot/boot.cat] -no-emul-boot -boot-load-size 4 -boot-info-table -o myimage.iso [path/to/source_directory]```"],["SYNOPSIS","**genisoimage** [_options_] _-o output.iso_ _pathspec_"],["PARAMETERS","**-o** _FILE_\n> Output ISO filename\n\n**-R**, **-rock**\n> Generate Rock Ridge extensions (Unix permissions/symlinks)\n\n**-J**, **-joliet**\n> Generate Joliet extensions (Windows long filenames)\n\n**-V** _ID_\n> Set volume ID (volume label)\n\n**-b** _FILE_\n> Set El Torito boot image for bootable CD/DVD\n\n**-c** _FILE_\n> Set El Torito boot catalog file\n\n**-allow-limited-size**\n> Allow files larger than 2GiB by reporting smaller apparent size\n\n**-udf**\n> Generate UDF filesystem (supports larger files)\n\n**-iso-level** _LEVEL_\n> Set ISO9660 conformance level (1-4)\n\n**-input-charset** _CHARSET_\n> Set input character set\n\n**-v**, **-verbose**\n> Enable verbose output"],["DESCRIPTION","**genisoimage** creates ISO9660 filesystem images suitable for CD/DVD burning or mounting as loop devices. It supports multiple filesystem extensions including Rock Ridge (Unix), Joliet (Windows), and HFS (Mac) for cross-platform compatibility.\n\nThe tool can create bootable images using El Torito specification, supporting both legacy BIOS and UEFI boot methods with appropriate configuration.\n\ngenisoimage is a fork of mkisofs from the cdrtools package, maintained separately in Debian and Ubuntu repositories."],["CAVEATS","Standard ISO9660 has a 4GiB file size limit. Use **-udf** or **-allow-limited-size** for larger files. Filename length and character restrictions vary by extension type. Boot image creation requires careful configuration."],["HISTORY","genisoimage is a fork of mkisofs created in **2006** due to licensing concerns with the original cdrtools package. It's maintained as part of the cdrkit project and is the default ISO creation tool on Debian-based systems."],["INSTALL","```apt: sudo apt install genisoimage```\n\n```dnf: sudo dnf install genisoimage```\n\n```apk: sudo apk add cdrkit```\n\n```nix: nix profile install nixpkgs#cdrkit```"],["SEE ALSO","[mkisofs](/man/mkisofs)(1), [wodim](/man/wodim)(1), [mount](/man/mount)(8)"]],"e":["mkisofs","wodim","mount"],"x":"genisoimage -o myimage.iso [path/to/source_directory]"},"git-merge-file":{"n":"git-merge-file","t":"Three-way file merging","s":[["TAGLINE","Three-way file merging"],["TLDR","**Three-way merge files**\n\n```git merge-file [current] [base] [other]```\n\n**Merge with diff3 style**\n\n```git merge-file --diff3 [current] [base] [other]```\n\n**Output to stdout**\n\n```git merge-file -p [current] [base] [other]```\n\n**Quiet mode**\n\n```git merge-file -q [current] [base] [other]```"],["SYNOPSIS","**git merge-file** [_options_] _current_ _base_ _other_"],["PARAMETERS","_CURRENT_\n> Current version file.\n\n_BASE_\n> Common ancestor file.\n\n_OTHER_\n> Other version to merge.\n\n**-p**, **--stdout**\n> Output to stdout.\n\n**--diff3**\n> Include base in conflicts.\n\n**-q**, **--quiet**\n> Suppress warnings.\n\n**--ours**\n> Resolve conflicts in favor of the \"current\" version.\n\n**--theirs**\n> Resolve conflicts in favor of the \"other\" version.\n\n**--union**\n> Take both sides of each conflict (append rather than mark).\n\n**-L** _label_\n> Provide a label for a file (pass up to three times for current/base/other).\n\n**--help**\n> Display help information."],["DESCRIPTION","**git merge-file** performs a three-way merge on individual files. Given the current version, common ancestor, and other version, it produces a merged result with conflict markers where automatic resolution is not possible.\n\nThe command operates on files outside of Git's normal merge workflow, making it useful for custom merge scenarios or merging files independently. The first file is modified in place with the merge result, and the exit code indicates whether conflicts occurred."],["CAVEATS","Plumbing command. Modifies first file in place. Exit code indicates conflicts."],["HISTORY","git merge-file is a core **Git** plumbing command implementing the three-way merge algorithm used internally by git merge."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-merge](/man/git-merge)(1), [diff3](/man/diff3)(1)"]],"e":["git-merge","diff3"],"x":"git merge-file [current] [base] [other]"},"git-rerere":{"n":"git-rerere","t":"Reuse recorded resolution of conflicted merges","s":[["TAGLINE","Reuse recorded resolution of conflicted merges"],["TLDR","**Enable rerere**\n\n```git config rerere.enabled true```\n\n**Show recorded resolutions**\n\n```git rerere status```\n\n**Show diff of resolution**\n\n```git rerere diff```\n\n**Clear recorded resolutions**\n\n```git rerere clear```\n\n**Forget resolution**\n\n```git rerere forget [file]```"],["SYNOPSIS","**git rerere** [_command_]"],["PARAMETERS","**status**\n> Show paths with recorded resolutions.\n\n**diff**\n> Show diff for current resolutions.\n\n**clear**\n> Clear recorded resolutions.\n\n**forget** _PATH_\n> Forget resolution for path.\n\n**gc**\n> Prune old recordings.\n\n**--help**\n> Display help information."],["DESCRIPTION","**git rerere** (reuse recorded resolution) remembers how you resolved merge conflicts and automatically reapplies those resolutions in future merges. This is especially valuable during repeated merges and rebases.\n\nWhen enabled, Git records conflict resolutions. On subsequent merges with identical conflicts, the previous resolution is applied automatically, eliminating repetitive conflict resolution work."],["CONFIGURATION","**rerere.enabled**\n> Set to `true` in git config to enable automatic recording of conflict resolutions."],["CAVEATS","Must be enabled in config. Records are local. May apply wrong resolution if contexts differ."],["HISTORY","git rerere was added to **Git** to address the pain of resolving the same merge conflicts repeatedly, particularly during long-running branches and rebases."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-merge](/man/git-merge)(1), [git-rebase](/man/git-rebase)(1)"],["RESOURCES","```[Source code](https://github.com/git/git)```\n\n```[Homepage](https://git-scm.com/)```\n\n```[Documentation](https://git-scm.com/docs/git-rerere)```"]],"e":["git-merge","git-rebase"],"x":"git config rerere.enabled true"},"gksudo":{"n":"gksudo","t":"Graphical frontend for sudo with GTK password dialog","s":[["TAGLINE","Graphical frontend for sudo with GTK password dialog"],["TLDR","Run a command as a **specific user**\n\n```gksudo -u [userid] [command]```\n\nRun the command while **preserving** the current environment\n\n```gksudo -u [userid] -k [command]```\n\nForce gksudo to use **su** to run the command\n\n```gksudo -u [userid] -w [command]```\n\nForce gksudo to use **sudo** to run the command\n\n```gksudo -u [userid] -S [command]```\n\nOutput **debug** info for the given command\n\n```gksudo -u [userid] -d [command]```\n\nAsk gksudo to **print the password** to stdout\n\n```gksudo -p [command]```"],["SYNOPSIS","**gksudo** [_options_] _command_"],["PARAMETERS","**-u**, **--user** _USER_\n> Run command as specified user (default: root)\n\n**-k**, **--preserve-env**\n> Preserve current environment variables, does not set `$HOME` or `$PATH`\n\n**-l**, **--login**\n> Make this a login shell (may cause problems with Xauthority)\n\n**-w**, **--su-mode**\n> Force use of su for authentication\n\n**-S**, **--sudo-mode**\n> Force use of sudo for authentication (default)\n\n**-d**, **--debug**\n> Output debug information\n\n**-g**, **--disable-grab**\n> Disable locking of the keyboard, mouse, and focus while asking for the password\n\n**-P**, **--prompt**\n> Ask the user before grabbing the keyboard and mouse\n\n**-m**, **--message** _MSG_\n> Custom message in password dialog\n\n**-D**, **--description** _DESC_|_FILE_\n> Description for the command being run, used in the default message\n\n**-p**, **--print-pass**\n> Print the password to stdout, like ssh-askpass"],["DESCRIPTION","**gksudo** is a graphical frontend for `sudo` that prompts for a password using a GTK dialog. It is essentially `gksu` configured to use sudo-mode by default, authenticating with the current user's password and sudo privileges.\n\nThis allows running graphical applications with elevated privileges while using sudo's configuration for permission control.\n\n> **DEPRECATED**: gksudo is no longer maintained and has security issues. Use **pkexec** from PolicyKit for graphical privilege escalation instead."],["CAVEATS","Deprecated and unmaintained. Running graphical applications as root can cause permission problems with user config files and X authority. Modern systems discourage running GUI applications as root."],["HISTORY","gksudo was created alongside gksu for Ubuntu and other distributions that preferred sudo over su for privilege escalation. Both were deprecated around **2012** in favor of PolicyKit's pkexec mechanism."],["SEE ALSO","[pkexec](/man/pkexec)(1), [sudo](/man/sudo)(8), [gksu](/man/gksu)(1)"],["RESOURCES","```[Debian manual page (archived)](https://manpages.debian.org/testing/gksu/gksu.1.en.html)```"]],"e":["pkexec","sudo","gksu"],"x":"gksudo -u [userid] [command]"},"gpg-zip":{"n":"gpg-zip","t":"Encrypt or sign files into a GPG archive (deprecated)","s":[["TAGLINE","Encrypt or sign files into a GPG archive (deprecated)"],["TLDR","**Encrypt directory with symmetric encryption**\n\n```gpg-zip --symmetric --output [archive.gpg] [directory]```\n\n**Encrypt for a recipient**\n\n```gpg-zip --encrypt --output [archive.gpg] --gpg-args \"-r [recipient]\" [directory]```\n\n**Decrypt an archive**\n\n```gpg-zip --decrypt [archive.gpg]```\n\n**List archive contents**\n\n```gpg-zip --list-archive [archive.gpg]```\n\n**Sign and encrypt**\n\n```gpg-zip --encrypt --sign --output [archive.gpg] [directory]```"],["SYNOPSIS","**gpg-zip** [_options_] [_files_]"],["PARAMETERS","**-e**, **--encrypt**\n> Encrypt data.\n\n**-d**, **--decrypt**\n> Decrypt data.\n\n**-c**, **--symmetric**\n> Encrypt with symmetric cipher using passphrase.\n\n**-s**, **--sign**\n> Sign data.\n\n**--list-archive**\n> List archive contents.\n\n**-o**, **--output** _file_\n> Output file.\n\n**--gpg-args** _args_\n> Pass options to gpg.\n\n**--tar** _command_\n> Use alternative tar command."],["DESCRIPTION","**gpg-zip** encrypts or signs files into an archive using GnuPG. It combines tar archiving with GPG encryption in a format compatible with PGP Zip, supporting both symmetric (passphrase) and asymmetric (public-key) encryption.\n\nThis tool is deprecated in favor of **gpgtar**, which provides the same functionality with better integration into the modern GnuPG suite."],["CAVEATS","**gpg-zip** is deprecated. Use **gpgtar** instead."],["INSTALL","```apt: sudo apt install gpg```\n\n```apk: sudo apk add gpg```\n\n```brew: brew install gpg```"],["SEE ALSO","[gpg](/man/gpg)(1), [tar](/man/tar)(1)"]],"e":["gpg","tar"],"x":"gpg-zip --symmetric --output [archive.gpg] [directory]"},"hugo-server":{"n":"hugo-server","t":"starts a local development server with live reload","s":[["TAGLINE","starts a local development server with live reload"],["TLDR","**Start development server**\n\n```hugo server```\n\n**Include draft content**\n\n```hugo server -D```\n\n**Start on a custom port**\n\n```hugo server -p [8080]```\n\n**Bind to all interfaces** (accessible from other devices on the network)\n\n```hugo server --bind 0.0.0.0```\n\n**Disable live reload**\n\n```hugo server --disableLiveReload```\n\n**Navigate to changes** automatically in the browser\n\n```hugo server --navigateToChanged```\n\n**Build and serve with a specific base URL**\n\n```hugo server --baseURL [http://example.com]```"],["SYNOPSIS","**hugo server** [_options_]"],["PARAMETERS","**-D**, **--buildDrafts**\n> Include draft content.\n\n**-E**, **--buildExpired**\n> Include expired content.\n\n**-F**, **--buildFuture**\n> Include content with future publish dates.\n\n**-p**, **--port** _PORT_\n> Server port (default: 1313).\n\n**--bind** _ADDR_\n> Listen address (default: 127.0.0.1).\n\n**-b**, **--baseURL** _URL_\n> Hostname and path to the root.\n\n**--disableLiveReload**\n> Disable automatic live reloading of browser.\n\n**--navigateToChanged**\n> Navigate to changed content file on live reload.\n\n**--poll** _DURATION_\n> Use polling with this period for file change detection (e.g., 700ms).\n\n**-w**, **--watch**\n> Watch for changes and rebuild (default: true).\n\n**--noHTTPCache**\n> Prevent HTTP caching.\n\n**--renderStaticToDisk**\n> Serve static files from disk and dynamic files from memory.\n\n**--help**\n> Display help information."],["DESCRIPTION","**hugo server** starts a local development server with live reload. It watches for file changes and automatically rebuilds the site, then signals connected browsers to refresh via injected JavaScript.\n\nThe server renders content to memory by default for speed. It defaults to **localhost:1313** and enables watching/live reload automatically."],["CAVEATS","Intended for development only, not production use. The **--bind 0.0.0.0** option exposes the server on all network interfaces. Memory usage can be significant with large sites since content is rendered to memory. Use **--poll** in environments where filesystem events are unreliable (e.g., Docker, network mounts)."],["HISTORY","hugo server is the built-in development server for **Hugo**, the open-source static site generator written in Go."],["INSTALL","```apt: sudo apt install hugo```\n\n```dnf: sudo dnf install hugo```\n\n```pacman: sudo pacman -S hugo```\n\n```apk: sudo apk add hugo```\n\n```zypper: sudo zypper install hugo```\n\n```brew: brew install hugo```\n\n```nix: nix profile install nixpkgs#hugo```"],["SEE ALSO","[hugo](/man/hugo)(1), [live-server](/man/live-server)(1)"]],"e":["hugo","live-server"],"x":"hugo server"},"i386":{"n":"i386","t":"run a program with the 32-bit x86 (i386) personality — alias of **setarch i386**","s":[["TAGLINE","run a program with the 32-bit x86 (i386) personality — alias of **setarch i386**"],["TLDR","This command is an alias of **setarch i386**. See:\n\n```tldr setarch```\n\n**Run a shell under the i386 personality**\n\n```i386```\n\n**Run a command under the i386 personality**\n\n```i386 [command] [args]```\n\n**Report an i386 uname** to a child process\n\n```i386 uname -m```\n\n**Disable address-space layout randomization** in the child\n\n```i386 -R [command]```"],["SYNOPSIS","**i386** [_options_] [_program_ [_arguments_]]"],["DESCRIPTION","**i386** is a convenience name for `setarch i386` — it tweaks the Linux personality of the child process so that `uname -m` reports `i386` and address-layout quirks match a 32-bit x86 system. It does **not** install 32-bit libraries or run non-native binaries; it only changes how the kernel reports architecture and optionally disables ASLR, READ_IMPLIES_EXEC, and similar flags.\n\nWithout arguments, `i386` starts `$SHELL` under the modified personality. Any flags accepted by `setarch` (`-R` disable ASLR, `-F` FDPIC_FUNCPTRS, `-Z` MMAP_PAGE_ZERO, `-L` ADDR_COMPAT_LAYOUT, `-X` READ_IMPLIES_EXEC, `-B` ADDR_NO_RANDOMIZE, etc.) work the same here."],["CAVEATS","Only changes the reported architecture and personality flags — it does **not** let 64-bit kernels run 32-bit binaries on their own; you still need 32-bit multiarch/multilib support. Some distributions no longer ship the `i386` symlink; use `setarch i386` directly if the shortcut is missing."],["INSTALL","```apt: sudo apt install util-linux```\n\n```dnf: sudo dnf install util-linux```\n\n```pacman: sudo pacman -S util-linux```\n\n```apk: sudo apk add util-linux-misc```\n\n```zypper: sudo zypper install util-linux```\n\n```brew: brew install util-linux```\n\n```nix: nix profile install nixpkgs#util-linux```"],["SEE ALSO","[setarch](/man/setarch)(8), [linux32](/man/linux32)(8), [uname](/man/uname)(1), [personality](/man/personality)(2)"]],"e":["setarch","linux32","uname","personality"],"x":"tldr setarch"},"ibmcloud-logout":{"n":"ibmcloud-logout","t":"ends the current IBM Cloud CLI session","s":[["TAGLINE","ends the current IBM Cloud CLI session"],["TLDR","**Logout from IBM Cloud**\n\n```ibmcloud logout```\n\n**Logout and clear cache**\n\n```ibmcloud logout -a```"],["SYNOPSIS","**ibmcloud logout** [_options_]"],["PARAMETERS","**-a**, **--all**\n> Clear all cached data.\n\n**--help**\n> Display help information."],["DESCRIPTION","**ibmcloud logout** ends the current IBM Cloud CLI session. It removes stored credentials and tokens.\n\nThe command clears authentication state locally. Use before switching accounts or for security."],["CAVEATS","Local credentials only. API keys remain valid. Re-login required after."],["HISTORY","ibmcloud logout is part of the **IBM Cloud** CLI authentication system."],["SEE ALSO","[ibmcloud](/man/ibmcloud)(1), [ibmcloud-login](/man/ibmcloud-login)(1)"]],"e":["ibmcloud","ibmcloud-login"],"x":"ibmcloud logout"},"idevicedate":{"n":"idevicedate","t":"displays or sets the date and time on iOS devices","s":[["TAGLINE","displays or sets the date and time on iOS devices"],["TLDR","**Show device date and time**\n\n```idevicedate```\n\n**Set date and time**\n\n```idevicedate -s \"[2024-01-15 10:30:00]\"```\n\n**Show in UTC**\n\n```idevicedate -u```\n\n**Target specific device**\n\n```idevicedate -u [device-udid]```"],["SYNOPSIS","**idevicedate** [_options_]"],["PARAMETERS","**-s** _datetime_\n> Set device date/time.\n\n**-u**\n> Show time in UTC.\n\n**-c**\n> Use current host time.\n\n**-n**\n> Connect over network."],["DESCRIPTION","**idevicedate** displays or sets the date and time on iOS devices. Part of the libimobiledevice suite. Can synchronize device time with host system."],["INSTALL","```apt: sudo apt install libimobiledevice-utils```\n\n```dnf: sudo dnf install libimobiledevice-utils```\n\n```apk: sudo apk add libimobiledevice-progs```"],["SEE ALSO","[ideviceinfo](/man/ideviceinfo)(1), [date](/man/date)(1)"]],"e":["ideviceinfo","date"],"x":"idevicedate"},"jfs_tune":{"n":"jfs_tune","t":"adjusts parameters on JFS filesystems","s":[["TAGLINE","adjusts parameters on JFS filesystems"],["TLDR","**Show filesystem info**\n\n```sudo jfs_tune [/dev/sda1]```\n\n**Set volume label**\n\n```sudo jfs_tune -L [label] [/dev/sda1]```\n\n**Change UUID**\n\n```sudo jfs_tune -U [random] [/dev/sda1]```\n\n**List filesystem options**\n\n```jfs_tune -l [/dev/sda1]```"],["SYNOPSIS","**jfs_tune** [_options_] _device_"],["PARAMETERS","_DEVICE_\n> JFS filesystem device.\n\n**-L** _LABEL_\n> Set volume label.\n\n**-U** _UUID_\n> Set or generate UUID.\n\n**-l**\n> List filesystem info.\n\n**-J** _OPTIONS_\n> External journal options.\n\n**--help**\n> Display help information."],["DESCRIPTION","**jfs_tune** adjusts parameters on JFS filesystems. It sets labels, UUIDs, and other filesystem metadata.\n\nThe tool works on unmounted filesystems. It's similar to tune2fs for ext filesystems."],["CAVEATS","JFS filesystems only. Unmount before use. Linux utility."],["HISTORY","jfs_tune is part of **jfsutils**, providing administration tools for the JFS filesystem ported from IBM's AIX."],["INSTALL","```apt: sudo apt install jfsutils```\n\n```dnf: sudo dnf install jfsutils```\n\n```pacman: sudo pacman -S jfsutils```\n\n```apk: sudo apk add jfsutils```\n\n```zypper: sudo zypper install jfsutils```\n\n```nix: nix profile install nixpkgs#jfsutils```"],["SEE ALSO","[tune2fs](/man/tune2fs)(8)"]],"e":["tune2fs"],"x":"sudo jfs_tune [/dev/sda1]"},"kage":{"n":"kage","t":"Mirror websites for offline, JavaScript-free browsing","s":[["TAGLINE","Mirror websites for offline, JavaScript-free browsing"],["TLDR","**Clone a website** into an offline-browsable folder\n\n```kage clone [https://example.com]```\n\n**Preview** a cloned folder in a local web server\n\n```kage serve [path/to/mirror]```\n\n**Pack a mirror** into a self-contained archive\n\n```kage pack [path/to/mirror]```\n\n**Open a packed** archive and serve it\n\n```kage open [archive.zim]```"],["SYNOPSIS","**kage** _command_ [_arguments_]"],["DESCRIPTION","**kage** clones websites into static, offline-browsable copies. It drives a headless Chrome browser to render each page, captures the final DOM after scripts have executed, strips out all JavaScript, and downloads the referenced CSS, images, and fonts to local paths. The result is a self-contained mirror that works completely offline with no network calls and no tracking.\n\nBecause pages are saved after rendering rather than as raw HTML, sites that build their content with JavaScript are captured as plain, static markup. The saved copy can be browsed directly, served locally for preview, or packed into a single portable archive."],["COMMANDS","**clone** _url_\n> Render a website with headless Chrome and save a static, JavaScript-free mirror to a local folder.\n\n**serve** [_dir_]\n> Start a local web server to preview a previously cloned folder.\n\n**pack** _mirror-dir_\n> Compress a mirror directory into a ZIM file or a self-contained binary.\n\n**open** _file_\n> Serve a packed archive (for example a .zim file) over a local web server."],["CAVEATS","Requires a headless Chrome or Chromium installation to render pages. Stripping JavaScript means interactive features that depend on client-side scripts will not work in the mirror. Sites behind authentication or aggressive bot protection may not clone cleanly."],["HISTORY","**kage** is written in **Go** and distributed under the MIT license. The name means \"shadow\" in Japanese, reflecting its purpose of producing a static shadow of a live website."],["SEE ALSO","[wget](/man/wget)(1), [httrack](/man/httrack)(1), [monolith](/man/monolith)(1), [curl](/man/curl)(1)"],["RESOURCES","```[Source code](https://github.com/tamnd/kage)```\n\n```[Homepage](https://kage.tamnd.com)```"]],"e":["wget","httrack","monolith","curl"],"x":"kage clone [https://example.com]"},"kotlinc":{"n":"kotlinc","t":"kotlin compiler","s":[["TAGLINE","kotlin compiler"],["TLDR","**Compile Kotlin file**\n\n```kotlinc [file.kt] -include-runtime -d [output.jar]```\n\n**Compile to class files**\n\n```kotlinc [file.kt] -d [output_dir]```\n\n**Compile multiple files**\n\n```kotlinc [file1.kt] [file2.kt] -d [output.jar]```\n\n**Add classpath**\n\n```kotlinc -cp [lib.jar] [file.kt] -d [output.jar]```\n\n**Compile with JVM target**\n\n```kotlinc -jvm-target [17] [file.kt] -d [output.jar]```"],["SYNOPSIS","**kotlinc** [_options_] _files_"],["PARAMETERS","_FILES_\n> Kotlin source files (.kt).\n\n**-d** _OUTPUT_\n> Output JAR or directory.\n\n**-include-runtime**\n> Include Kotlin runtime.\n\n**-cp** _PATH_\n> Classpath.\n\n**-jvm-target** _VERSION_\n> Target JVM version.\n\n**-no-stdlib**\n> Don't automatically include kotlin-stdlib.jar and kotlin-reflect.jar in the classpath.\n\n**-no-reflect**\n> Don't automatically include kotlin-reflect.jar in the classpath.\n\n**-jdk-home** _path_\n> Use a custom JDK home directory instead of the default JAVA_HOME.\n\n**-language-version** _version_\n> Compile against the specified Kotlin language version (e.g. `2.0`).\n\n**-script**\n> Evaluate a Kotlin script file (.kts).\n\n**-nowarn**\n> Suppress all compiler warnings.\n\n**-Werror**\n> Treat all warnings as compilation errors.\n\n**-verbose**\n> Enable verbose logging output with compilation details.\n\n**-version**\n> Display the compiler version.\n\n**-help**, **-h**\n> Display usage information."],["DESCRIPTION","**kotlinc** is the command-line Kotlin compiler that translates Kotlin source files (.kt) into JVM bytecode. It can produce either standalone JAR files with the Kotlin runtime bundled via the `-include-runtime` flag, or output class files to a directory for integration with existing build pipelines.\n\nThe compiler supports full interoperability with Java, allowing Kotlin code to call Java libraries and vice versa. It accepts a target JVM version through the `-jvm-target` option, supports adding external dependencies via the classpath, and can compile multiple source files together. For larger projects, build tools like Gradle or Maven are typically preferred, but kotlinc is useful for quick compilation tasks, learning, and scripting workflows."],["CAVEATS","Requires JVM. Slow startup. Consider Gradle for projects."],["HISTORY","kotlinc is the official compiler for **Kotlin**, created by **JetBrains** as a modern JVM language."],["SEE ALSO","[kotlin](/man/kotlin)(1), [javac](/man/javac)(1), [gradle](/man/gradle)(1)"]],"e":["kotlin","javac","gradle"],"x":"kotlinc [file.kt] -include-runtime -d [output.jar]"},"ktea":{"n":"ktea","t":"Apache Kafka TUI client","s":[["TAGLINE","Apache Kafka TUI client"],["TLDR","**Launch the Kafka TUI**\n\n```ktea```\n\n**Connect to a specific cluster**\n\n```ktea -c [cluster_name]```"],["SYNOPSIS","**ktea** [_options_]"],["DESCRIPTION","**ktea** is an Apache Kafka TUI client designed to simplify and accelerate interactions with Kafka clusters. It provides multi-cluster support, topic management, record consumption in text, JSON, and Avro formats, consumer group monitoring, and schema registry integration.\n\nTables can be navigated using vi-like key bindings (**j**/**k** for up/down, **d**/**u** for page down/up). The tool is useful for quickly checking topics, reading events, and managing Kafka resources without the overhead of GUI clients."],["CAVEATS","Requires access to a running Kafka cluster. Schema registry integration requires a compatible schema registry service."],["HISTORY","**ktea** was created by **Jonas Geiregat** (jonas-grgt) and is written in **Go**. It was designed for quick remote access to Kafka clusters from the terminal."],["INSTALL","```brew: brew install ktea```"],["SEE ALSO","[kplay](/man/kplay)(1), [kaskade](/man/kaskade)(1)"]],"e":["kplay","kaskade"],"x":"ktea"},"kube-linter":{"n":"kube-linter","t":"analyzes Kubernetes YAML files and Helm charts for security and best practices","s":[["TAGLINE","analyzes Kubernetes YAML files and Helm charts for security and best practices"],["TLDR","**Lint Kubernetes manifests**\n\n```kube-linter lint [path/to/manifests]```\n\n**Lint Helm chart**\n\n```kube-linter lint [path/to/chart]```\n\n**List available checks**\n\n```kube-linter checks list```\n\n**Lint with specific checks**\n\n```kube-linter lint --include [no-read-only-root-fs] [path]```\n\n**Exclude checks**\n\n```kube-linter lint --exclude [run-as-non-root] [path]```\n\n**Output as JSON**\n\n```kube-linter lint --format json [path]```"],["SYNOPSIS","**kube-linter** _command_ [_options_]"],["PARAMETERS","**lint** _path_\n> Lint manifests or charts.\n\n**checks list**\n> List available checks.\n\n**--include** _checks_\n> Only run specified checks.\n\n**--exclude** _checks_\n> Skip specified checks.\n\n**--config** _file_\n> Configuration file.\n\n**--format** _format_\n> Output format: plain, json, sarif.\n\n**--fail-on-invalid-resource**\n> Fail on invalid YAML."],["DESCRIPTION","**kube-linter** analyzes Kubernetes YAML files and Helm charts for security and best practices. It catches misconfigurations like missing security contexts, resource limits, and privilege escalation risks.\n\nThe tool is designed for CI/CD integration, helping catch issues before deployment."],["CHECKS EXAMPLES","```\nno-read-only-root-fs      Root filesystem not read-only\nrun-as-non-root           Container runs as root\nunset-cpu-requirements    CPU limits not set\nunset-memory-requirements Memory limits not set\nlatest-tag                Using 'latest' image tag\n```"],["CONFIGURATION","```yaml"],[".kube-linter.yaml","checks:\n  addAllBuiltIn: true\n  exclude:\n    - \"latest-tag\"\n```"],["CAVEATS","May produce false positives. Not all checks apply to all workloads. Custom checks require Go. Helm chart linting requires Helm."],["HISTORY","kube-linter was developed by **StackRox** (acquired by Red Hat) to improve Kubernetes security posture through static analysis."],["INSTALL","```pacman: sudo pacman -S kube-linter```\n\n```zypper: sudo zypper install kube-linter```\n\n```brew: brew install kube-linter```\n\n```nix: nix profile install nixpkgs#kube-linter```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [kubeaudit](/man/kubeaudit)(1), [helm](/man/helm)(1)"]],"e":["kubectl","kubeaudit","helm"],"x":"kube-linter lint [path/to/manifests]"},"kubectl-kustomize":{"n":"kubectl-kustomize","t":"Build a kustomization target from a directory or URL","s":[["TAGLINE","Build a kustomization target from a directory or URL"],["TLDR","**Build kustomization from current directory**\n\n```kubectl kustomize```\n\n**Build kustomization from directory**\n\n```kubectl kustomize [directory]```\n\n**Build and output to file**\n\n```kubectl kustomize [directory] -o [output.yaml]```\n\n**Build with Helm chart support**\n\n```kubectl kustomize --enable-helm [directory]```\n\n**Build from a remote URL**\n\n```kubectl kustomize [https://github.com/user/repo/config]```\n\n**Apply kustomization directly**\n\n```kubectl apply -k [directory]```"],["SYNOPSIS","**kubectl** **kustomize** [_directory_] [_options_]"],["PARAMETERS","**--enable-helm**\n> Enable Helm chart rendering.\n\n**--helm-command** _STRING_\n> Helm command path (default: \"helm\").\n\n**--load-restrictor** _STRING_\n> Control resource loading restrictions (default: LoadRestrictionsRootOnly). Set to LoadRestrictionsNone to allow loading from outside root.\n\n**--reorder** _STRING_\n> Reorder resources before output: \"legacy\" or \"none\" (default: \"legacy\").\n\n**-o**, **--output** _STRING_\n> Write output to this file path.\n\n**--enable-alpha-plugins**\n> Enable kustomize plugins."],["DESCRIPTION","**kubectl kustomize** builds a set of KRM (Kubernetes Resource Model) resources from a directory containing a kustomization.yaml file, or from a git repository URL with a path suffix. If the directory argument is omitted, the current directory is assumed. Processes patches, overlays, and resource modifications without templates. Output can be piped to kubectl apply."],["INSTALL","```apt: sudo apt install kubectl```\n\n```pacman: sudo pacman -S kubectl```\n\n```apk: sudo apk add kubectl```\n\n```brew: brew install kubectl```\n\n```nix: nix profile install nixpkgs#kubectl```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [kubectl-apply](/man/kubectl-apply)(1)"]],"e":["kubectl","kubectl-apply"],"x":"kubectl kustomize"},"lazyssh":{"n":"lazyssh","t":"Terminal-based interactive SSH manager","s":[["TAGLINE","Terminal-based interactive SSH manager"],["TLDR","**Launch the SSH manager UI**\n\n```lazyssh```"],["SYNOPSIS","**lazyssh**"],["KEYBINDINGS","**/**\n> Toggle search bar.\n\n**Up/Down** or **j/k**\n> Navigate servers.\n\n**Enter**\n> SSH into selected server.\n\n**c**\n> Copy SSH command to clipboard.\n\n**g**\n> Ping selected server.\n\n**r**\n> Refresh background data.\n\n**a**\n> Add server.\n\n**e**\n> Edit server.\n\n**t**\n> Edit tags.\n\n**d**\n> Delete server.\n\n**p**\n> Pin/Unpin server.\n\n**s**\n> Toggle sort field.\n\n**S**\n> Reverse sort order.\n\n**q**\n> Quit."],["DESCRIPTION","**lazyssh** is a keyboard-driven SSH manager that makes it easy to browse, connect to, and manage servers directly from the command line. It reads servers from your **~/.ssh/config** and displays them in a scrollable interface.\n\nFeatures include fuzzy search by alias, IP, or tags, adding or editing SSH config entries, pinning favorite servers, pinging servers to test connectivity, tagging for categorization, port forwarding configuration, and connection multiplexing. You can SSH into a selected server with a single keypress.\n\nAll SSH connections are executed through the system's native **ssh** binary. Private keys, passwords, and credentials are never stored, transmitted, or modified by lazyssh. A backup of your SSH config is created automatically before any changes."],["CAVEATS","Requires an existing **~/.ssh/config** file. Relies on the system's OpenSSH binary for connections."],["HISTORY","**lazyssh** was created by **Adembc** and is written in **Go** using tview and tcell. It was inspired by lazydocker and k9s."],["INSTALL","```brew: brew install lazyssh```\n\n```nix: nix profile install nixpkgs#lazyssh```"],["SEE ALSO","[ssh](/man/ssh)(1), [ssh-keygen](/man/ssh-keygen)(1)"]],"e":["ssh","ssh-keygen"],"x":"lazyssh"},"lnd":{"n":"lnd","t":"Lightning Network Daemon for Bitcoin","s":[["TAGLINE","Lightning Network Daemon for Bitcoin"],["TLDR","**Start the daemon**\n\n```lnd```\n\n**Start with a specific Bitcoin backend**\n\n```lnd --bitcoin.active --bitcoin.mainnet --bitcoind.rpcuser=[user] --bitcoind.rpcpass=[pass]```\n\n**Start on testnet**\n\n```lnd --bitcoin.active --bitcoin.testnet```\n\n**Specify a custom data directory**\n\n```lnd --lnddir=[~/.lnd]```"],["SYNOPSIS","**lnd** [_options_]"],["PARAMETERS","**--bitcoin.active**\n> Enable Bitcoin as the active chain.\n\n**--bitcoin.mainnet**\n> Use the Bitcoin main network.\n\n**--bitcoin.testnet**\n> Use the Bitcoin test network.\n\n**--bitcoind.rpcuser** _USER_\n> Username for bitcoind RPC authentication.\n\n**--bitcoind.rpcpass** _PASS_\n> Password for bitcoind RPC authentication.\n\n**--lnddir** _DIR_\n> Custom path to lnd's data directory.\n\n**--listen** _ADDR_\n> Add an interface/port to listen for peer connections.\n\n**--rpclisten** _ADDR_\n> Add an interface/port for the RPC server to listen on.\n\n**--restlisten** _ADDR_\n> Add an interface/port for the REST server to listen on.\n\n**--debuglevel** _LEVEL_\n> Logging level (trace, debug, info, warn, error, critical).\n\n**--help**, **-h**\n> Show help.\n\n**--version**, **-v**\n> Display version information."],["DESCRIPTION","**lnd** (Lightning Network Daemon) is a complete implementation of the **Lightning Network** protocol. It enables fast, low-cost Bitcoin transactions through payment channels, allowing near-instant payments without waiting for blockchain confirmations.\n\nThe daemon manages payment channels, routes payments across the network, and maintains a local channel graph. It exposes gRPC and REST APIs for interaction, typically used via the companion **lncli** command-line client."],["CAVEATS","Requires a Bitcoin full node backend (bitcoind, btcd, or neutrino). Channel management involves real funds on mainnet. Written in Go."],["INSTALL","```nix: nix profile install nixpkgs#lnd```"],["SEE ALSO","[electrum](/man/electrum)(1), [geth](/man/geth)(1)"]],"e":["electrum","geth"],"x":"lnd"},"loft":{"n":"loft","t":"manages virtual Kubernetes clusters and namespaces","s":[["TAGLINE","manages virtual Kubernetes clusters and namespaces"],["TLDR","**Start Loft**\n\n```loft start```\n\n**Login to Loft**\n\n```loft login [https://loft.example.com]```\n\n**Create virtual cluster**\n\n```loft create vcluster [name]```\n\n**List virtual clusters**\n\n```loft list vclusters```\n\n**Connect to space**\n\n```loft use space [name]```\n\n**Create space**\n\n```loft create space [name]```"],["SYNOPSIS","**loft** _command_ [_options_]"],["PARAMETERS","_COMMAND_\n> Loft operation.\n\n**start**\n> Start Loft server.\n\n**login** _URL_\n> Login to Loft instance.\n\n**create** _TYPE_ _NAME_\n> Create resource.\n\n**list** _TYPE_\n> List resources.\n\n**use** _TYPE_ _NAME_\n> Switch context.\n\n**--help**\n> Display help information."],["DESCRIPTION","**loft** manages virtual Kubernetes clusters and namespaces. It provides self-service Kubernetes for teams.\n\nThe tool enables multi-tenancy on shared clusters. It creates isolated virtual clusters quickly."],["CAVEATS","Requires Kubernetes cluster. Loft platform needed. Commercial product."],["HISTORY","Loft was created by **Loft Labs** to provide virtual cluster technology and self-service Kubernetes for developers."],["SEE ALSO","[kubectl](/man/kubectl)(1), [vcluster](/man/vcluster)(1), [helm](/man/helm)(1)"]],"e":["kubectl","vcluster","helm"],"x":"loft start"},"lstopo-no-graphics":{"n":"lstopo-no-graphics","t":"displays the hardware topology of the system in text format without requiring","s":[["TAGLINE","displays the hardware topology of the system in text format without requiring"],["TLDR","Display the machine **topology in tree format**\n\n```lstopo-no-graphics```\n\nDisplay only **physical cores** (ignore logical processors)\n\n```lstopo-no-graphics --only pu```\n\nDisplay the topology with **physical indexes**\n\n```lstopo-no-graphics -p```\n\nDisplay **help**\n\n```lstopo-no-graphics -h```"],["SYNOPSIS","**lstopo-no-graphics** [_options_]"],["PARAMETERS","**--only _type_**\n> Display only objects of the specified type (e.g., pu for processing units)\n\n**-p, --physical**\n> Display physical indexes instead of logical indexes\n\n**-l, --logical**\n> Display logical indexes (default)\n\n**--no-io**\n> Hide I/O devices from the output\n\n**--no-bridges**\n> Hide bridge devices from the output\n\n**-h, --help**\n> Display help information"],["DESCRIPTION","**lstopo-no-graphics** displays the hardware topology of the system in text format without requiring a graphical display. Part of the **hwloc** (Hardware Locality) package, it shows the hierarchical structure of CPUs, caches, memory, and I/O devices.\n\nThe output shows the system's NUMA nodes, packages (sockets), cores, and processing units (hardware threads) in a tree structure. This information is useful for understanding CPU architecture, cache sharing, and memory locality for performance optimization.\n\nUnlike **lstopo**, this variant works in console-only environments without X11 or graphical libraries."],["CAVEATS","Output detail depends on the kernel's exposure of hardware information. Some virtual machines or containers may show limited topology data. The hwloc library must be installed for this command to be available."],["HISTORY","The hwloc project was developed at Inria Bordeaux and the University of Tennessee, first released around 2009. It provides portable abstraction of hierarchical topology information across various operating systems and architectures."],["INSTALL","```apt: sudo apt install hwloc```\n\n```dnf: sudo dnf install hwloc```\n\n```pacman: sudo pacman -S hwloc```\n\n```apk: sudo apk add hwloc-tools```\n\n```zypper: sudo zypper install hwloc```\n\n```brew: brew install hwloc```\n\n```nix: nix profile install nixpkgs#hwloc```"],["SEE ALSO","[lstopo](/man/lstopo)(1), [hwloc-ls](/man/hwloc-ls)(1), [lscpu](/man/lscpu)(1), [numactl](/man/numactl)(8)"]],"e":["lstopo","hwloc-ls","lscpu","numactl"],"x":"lstopo-no-graphics"},"ltrace":{"n":"ltrace","t":"Intercepts and records dynamic library calls made by a process","s":[["TAGLINE","Intercepts and records dynamic library calls made by a process"],["TLDR","**Trace library calls of a program**\n\n```ltrace [path/to/program]```\n\n**Count library calls and print a summary on exit**\n\n```ltrace -c [path/to/program]```\n\n**Trace specific library functions**\n\n```ltrace -e malloc+free [path/to/program]```\n\n**Trace calls and also display system calls**\n\n```ltrace -S [path/to/program]```\n\n**Attach to a running process**\n\n```ltrace -p [pid]```\n\n**Write trace output to a file**\n\n```ltrace -o [path/to/file] [path/to/program]```\n\n**Trace child processes created by fork**\n\n```ltrace -f [path/to/program]```\n\n**Show time spent inside each library call**\n\n```ltrace -T [path/to/program]```"],["SYNOPSIS","**ltrace** [_options_] [_command_ [_arg ..._]]"],["DESCRIPTION","**ltrace** intercepts and records dynamic library calls made by a process and signals received by it. It is useful for debugging and understanding how programs interact with shared libraries."],["PARAMETERS","**-a**, **--align** _column_\n> Align return values in a specific column (default is 5/8 of screen width).\n\n**-A** _maxelts_\n> Maximum number of array elements to print before suppressing the rest with an ellipsis.\n\n**-b**, **--no-signals**\n> Disable printing of signals received by the traced process.\n\n**-c**\n> Count time and calls for each library call and print a summary on program exit.\n\n**-C**, **--demangle**\n> Decode (demangle) low-level C++ symbol names into user-level names.\n\n**-D**, **--debug** _mask_\n> Show debugging output of ltrace itself.\n\n**-e** _filter_\n> Qualifying expression to select which library calls to trace.\n\n**-f**\n> Trace child processes created by fork(2) or clone(2).\n\n**-F**, **--config** _pathlist_\n> Colon-separated list of paths for prototype library configuration files.\n\n**-i**\n> Print the instruction pointer at the time of the library call.\n\n**-l**, **--library** _library_pattern_\n> Display only calls to functions in libraries matching the pattern.\n\n**-L**\n> Don't display library calls (use with -x to trace only symbol entry points).\n\n**-n**, **--indent** _nr_\n> Indent trace output by nr spaces for each nested call level.\n\n**-o**, **--output** _filename_\n> Write trace output to a file instead of stderr.\n\n**-p** _pid_\n> Attach to the process with the given process ID.\n\n**-r**\n> Print a relative timestamp with each line of the trace.\n\n**-s** _strsize_\n> Maximum string size to print (default: 32).\n\n**-S**\n> Display system calls as well as library calls.\n\n**-t**\n> Prefix each line with the time of day.\n\n**-tt**\n> Time of day with microseconds.\n\n**-ttt**\n> Microseconds since the epoch.\n\n**-T**\n> Show the time spent inside each call.\n\n**-u** _username_\n> Run command with the userid and groupid of the specified user.\n\n**-w**, **--where** _nr_\n> Show backtrace of nr stack frames for each traced function.\n\n**-x** _filter_\n> Qualifying expression for tracing symbol table entry points.\n\n**-V**, **--version**\n> Show version number and exit.\n\n**-h**, **--help**\n> Display help and exit."],["CAVEATS","May significantly slow down traced programs. Not all library calls may be intercepted on all architectures. The **-w** option requires elfutils or libunwind support at compile time."],["INSTALL","```apt: sudo apt install ltrace```\n\n```dnf: sudo dnf install ltrace```\n\n```pacman: sudo pacman -S ltrace```\n\n```apk: sudo apk add ltrace```\n\n```zypper: sudo zypper install ltrace```\n\n```nix: nix profile install nixpkgs#ltrace```"],["SEE ALSO","[strace](/man/strace)(1), [ldd](/man/ldd)(1), [gdb](/man/gdb)(1), [nm](/man/nm)(1), [objdump](/man/objdump)(1)"]],"e":["strace","ldd","gdb","nm","objdump"],"x":"ltrace [path/to/program]"},"msedit":{"n":"msedit","t":"Microsoft terminal-based text editor","s":[["TAGLINE","Microsoft terminal-based text editor"],["TLDR","**Launch** the editor with an empty buffer\n\n```msedit```\n\n**Open** a file\n\n```msedit [path/to/file]```\n\n**Open** a file at a specific **line number**\n\n```msedit [path/to/file]:[line]```\n\n**Open** a file at a specific **line and column**\n\n```msedit [path/to/file]:[line]:[column]```\n\n**Open multiple files** in tabs\n\n```msedit [file1] [file2] [file3]```"],["SYNOPSIS","**msedit** [_OPTIONS_] [_FILE_[:_LINE_[:_COLUMN_]]]..."],["PARAMETERS","**-h**, **--help**\n> Print help information\n\n**-v**, **--version**\n> Print version information"],["DESCRIPTION","**msedit** is a lightweight, modeless, terminal-based text editor developed by **Microsoft** and written in **Rust**. It draws inspiration from the classic MS-DOS Editor but provides a modern interface with input controls similar to VS Code. Unlike modal editors like Vim, editing is immediate with no mode switching required. It features a menu bar, status bar, mouse support, UTF-8 handling, LF/CRLF conversion, and a localized UI supporting multiple languages. The binary is less than 250 KB.\n\nOn Linux the executable is named **msedit** to avoid conflicts with existing **edit** commands. On Windows it is simply **edit**."],["KEYBOARD SHORTCUTS","```\nCtrl+S       Save file\nCtrl+Shift+S Save As\nCtrl+O       Open file\nCtrl+F       Find\nCtrl+R       Find & Replace\nF3           Next search result\nCtrl+P       Switch between tabs\nCtrl+G       Go to line/column\nCtrl+Z       Undo\nCtrl+Y       Redo\nCtrl+C       Copy\nCtrl+V       Paste\nCtrl+Q       Quit\nCtrl+W       Close tab\nAlt+Z        Toggle word wrap\nAlt+F / F10  Focus menu bar\n```"],["CAVEATS","On Linux, the executable name varies between **edit** (from GitHub binary) and **msedit** (from distro packages) depending on installation method. Has an optional dependency on **ICU** for Search and Replace functionality. Requires a **Rust nightly toolchain** to build from source. As a relatively new tool (2025), it lacks the plugin ecosystem of established editors."],["HISTORY","Microsoft Edit was announced at **Build 2025** in May 2025 and simultaneously open-sourced under the **MIT license**. It was created because 64-bit versions of Windows lacked a default CLI text editor. The project has gained significant community interest with over 13,000 GitHub stars. It will ship pre-installed with future Windows 11 builds."],["INSTALL","```dnf: sudo dnf install msedit```\n\n```pacman: sudo pacman -S msedit```\n\n```zypper: sudo zypper install msedit```\n\n```brew: brew install msedit```\n\n```nix: nix profile install nixpkgs#msedit```"],["SEE ALSO","[nano](/man/nano)(1), [vim](/man/vim)(1), [emacs](/man/emacs)(1), [micro](/man/micro)(1)"]],"e":["nano","vim","emacs","micro"],"x":"msedit"},"ncc":{"n":"ncc","t":"compiles Node","s":[["TAGLINE","compiles Node"],["TLDR","**Compile Node.js project**\n\n```ncc build [src/index.js]```\n\n**Compile with output directory**\n\n```ncc build [src/index.js] -o [dist/]```\n\n**Compile with minification**\n\n```ncc build [src/index.js] -m```\n\n**Compile with source maps**\n\n```ncc build [src/index.js] -s```\n\n**Compile and watch**\n\n```ncc build [src/index.js] -w```\n\n**Run directly** (compile and execute)\n\n```ncc run [src/index.js]```\n\n**Compile TypeScript**\n\n```ncc build [src/index.ts]```"],["SYNOPSIS","**ncc** [_build_] [_run_] [_-o dir_] [_-m_] [_-s_] [_-w_] [_options_] _file_"],["PARAMETERS","**build** _FILE_\n> Compile to single file.\n\n**run** _FILE_\n> Compile and run.\n\n**-o** _DIR_\n> Output directory.\n\n**-m**, **--minify**\n> Minify output.\n\n**-s**, **--source-map**\n> Generate source maps.\n\n**-w**, **--watch**\n> Watch for changes.\n\n**-e**, **--external** _PKG_\n> Keep package external.\n\n**-a**, **--asset-builds**\n> Emit asset files separately.\n\n**--license** _FILE_\n> Output license file.\n\n**-q**, **--quiet**\n> Quiet mode.\n\n**-v**, **--version**\n> Show version."],["DESCRIPTION","**ncc** (Node.js Compiler Collection) compiles Node.js projects into a single file. It bundles code and dependencies, producing portable output that runs without node_modules.\n\nThe compiler handles CommonJS and ES modules, automatically resolving imports. Native modules and assets are extracted alongside the bundle.\n\nTypeScript and modern JavaScript features are transpiled automatically. The output targets Node.js, maintaining compatibility with Node's module system.\n\nExternals keep specific packages out of the bundle. This is useful for native modules that can't be bundled or for reducing size when dependencies are available at runtime.\n\nMinification reduces output size. Source maps enable debugging of bundled code by mapping back to original sources.\n\nWatch mode recompiles on file changes, useful during development."],["CAVEATS","Native modules need special handling. Dynamic requires may not resolve. Some packages have bundling issues. Large bundles can be slow to build."],["HISTORY","**ncc** was created by **Vercel** (formerly ZEIT) around **2018** to simplify Node.js deployment. It enables distributing Node.js applications as single files, similar to Go binaries, addressing node_modules complexity."],["INSTALL","```brew: brew install ncc```"],["SEE ALSO","[webpack](/man/webpack)(1), [esbuild](/man/esbuild)(1), [rollup](/man/rollup)(1), [pkg](/man/pkg)(1)"]],"e":["webpack","esbuild","rollup","pkg"],"x":"ncc build [src/index.js]"},"nemu":{"n":"nemu","t":"Ncurses-based TUI for QEMU virtual machine management","s":[["TAGLINE","Ncurses-based TUI for QEMU virtual machine management"],["TLDR","**Launch the VM manager**\n\n```nemu```"],["SYNOPSIS","**nemu** [_options_]"],["DESCRIPTION","**nemu** is an ncurses-based TUI for managing QEMU virtual machines from the terminal. It provides VM creation, configuration, status monitoring, CPU usage tracking, and connectivity via VNC or SPICE, all without requiring a graphical desktop environment.\n\nFeatures include support for qcow2 and raw disk image formats, configurable refresh timeouts, and VM preview using the Kitty graphics protocol in supported terminals."],["CAVEATS","Requires QEMU to be installed. Some features like VM preview require a terminal with Kitty graphics protocol support."],["HISTORY","**nemu** is developed by the **nemuTUI** project and is written in **C**."],["INSTALL","```apk: sudo apk add nemu```\n\n```brew: brew install nemu```\n\n```nix: nix profile install nixpkgs#nemu```"],["SEE ALSO","[qemu](/man/qemu)(1), [virsh](/man/virsh)(1), [virt-manager](/man/virt-manager)(1)"]],"e":["qemu","virsh","virt-manager"],"x":"nemu"},"nmtui-connect":{"n":"nmtui-connect","t":"activate network connections via TUI","s":[["TAGLINE","activate network connections via TUI"],["TLDR","This command is an alias of [nmtui connect](/man/nmtui)."],["INSTALL","```apt: sudo apt install network-manager```\n\n```apk: sudo apk add networkmanager-tui```"],["SEE ALSO","[nmtui](/man/nmtui)(1), [nmcli](/man/nmcli)(1)"]],"e":["nmtui","nmcli"],"x":""},"pbmtopgm":{"n":"pbmtopgm","t":"converts PBM bitmap to PGM grayscale by averaging pixel regions","s":[["TAGLINE","converts PBM bitmap to PGM grayscale by averaging pixel regions"],["TLDR","**Convert bitmap to grayscale**\n\n```pbmtopgm [width] [height] [input.pbm] > [output.pgm]```"],["SYNOPSIS","**pbmtopgm** _width_ _height_ [_file_]"],["PARAMETERS","_width_\n> Averaging width.\n\n_height_\n> Averaging height."],["DESCRIPTION","**pbmtopgm** converts PBM bitmap to PGM grayscale by averaging pixel regions. Anti-aliases bitmap edges. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pgmtopbm](/man/pgmtopbm)(1), [ppmtopgm](/man/ppmtopgm)(1)"]],"e":["pgmtopbm","ppmtopgm"],"x":"pbmtopgm [width] [height] [input.pbm] > [output.pgm]"},"pbmupc":{"n":"pbmupc","t":"Create UPC-A barcode PBM images","s":[["TAGLINE","Create UPC-A barcode PBM images"],["TLDR","**Create a UPC-A barcode image**\n\n```pbmupc [0] [12345] [67890] [5] > [barcode.pbm]```\n\n**Create barcode with style 1 (tall guards)**\n\n```pbmupc -s1 [0] [12345] [67890] [5] > [barcode.pbm]```\n\n**Create barcode with style 2 (short guards)**\n\n```pbmupc -s2 [0] [12345] [67890] [5] > [barcode.pbm]```"],["SYNOPSIS","**pbmupc** [**-s1** | **-s2**] _type_ _manufacturer_ _product_ _check_"],["PARAMETERS","_type_\n> Single digit indicating the number system type (typically 0 for standard UPC-A).\n\n_manufacturer_\n> Five-digit manufacturer code.\n\n_product_\n> Five-digit product code.\n\n_check_\n> Single check digit (modulo-10 checksum of the other 11 digits).\n\n**-s1**\n> Style 1 output with tall guard bars (default).\n\n**-s2**\n> Style 2 output with short guard bars."],["DESCRIPTION","**pbmupc** creates UPC-A (Universal Product Code) barcode images in PBM (Portable Bitmap) format. It takes the four components of a 12-digit UPC-A code as separate arguments: the number system type digit, a five-digit manufacturer code, a five-digit product code, and the check digit.\n\nThe output PBM image can be converted to other formats using Netpbm conversion tools. UPC-A barcodes are the standard retail barcode format used in the United States and Canada."],["CAVEATS","Only generates UPC-A format barcodes. The check digit is not validated or computed automatically; the user must supply the correct value. Part of the Netpbm toolkit."],["HISTORY","**pbmupc** is part of the **Netpbm** package, which originated from Jef Poskanzer's PBMplus toolkit in the late 1980s."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pbm](/man/pbm)(5), [netpbm](/man/netpbm)(1)"]],"e":["pbm","netpbm"],"x":"pbmupc [0] [12345] [67890] [5] > [barcode.pbm]"},"pdf2ps":{"n":"pdf2ps","t":"converts PDF files to PostScript","s":[["TAGLINE","converts PDF files to PostScript"],["TLDR","**Convert PDF to PostScript**\n\n```pdf2ps [input.pdf] [output.ps]```\n\n**Convert specific pages**\n\n```pdf2ps -f [1] -l [5] [input.pdf] [output.ps]```\n\n**Convert for a Level 2 PostScript printer**\n\n```pdf2ps -level2 [input.pdf] [output.ps]```\n\n**Specify paper size**\n\n```pdf2ps -paper [A4] [input.pdf] [output.ps]```\n\n**Pipe output directly to a printer**\n\n```pdf2ps [input.pdf] - | lpr```"],["SYNOPSIS","**pdf2ps** [_options_] _input.pdf_ [_output.ps_]"],["PARAMETERS","**-f** _num_\n> First page to convert (default: 1).\n\n**-l** _num_\n> Last page to convert (default: last page of document).\n\n**-level1**, **-level2**, **-level3**\n> Set PostScript language level for output. Level 1 is most compatible, Level 2 adds color and compression, Level 3 supports the latest features.\n\n**-paper** _size_\n> Set paper size (e.g., letter, A4, legal).\n\n**-r** _dpi_\n> Set resolution in dots per inch for rasterized content.\n\n**-q**\n> Quiet mode. Suppress routine Ghostscript messages."],["DESCRIPTION","**pdf2ps** converts PDF files to PostScript. PostScript is needed for certain printers and print workflows. The output quality and compatibility depend on the PostScript level selected."],["POSTSCRIPT LEVELS","```\nLevel 1 - Most compatible\nLevel 2 - Color, compression\nLevel 3 - Latest features\n```"],["EXAMPLE","```bash"],["Basic conversion","pdf2ps document.pdf document.ps"],["Print via PostScript","pdf2ps document.pdf - | lpr\n```"],["CAVEATS","Output files can be large. Level affects compatibility. Consider pdftops from Poppler for better output."],["HISTORY","pdf2ps is part of **Ghostscript**, the PostScript/PDF interpreter originally developed by **Artifex Software**."],["INSTALL","```apt: sudo apt install ghostscript```\n\n```dnf: sudo dnf install ghostscript```\n\n```pacman: sudo pacman -S ghostscript```\n\n```apk: sudo apk add ghostscript```\n\n```zypper: sudo zypper install ghostscript```\n\n```brew: brew install ghostscript```\n\n```nix: nix profile install nixpkgs#ghostscript```"],["SEE ALSO","[pdftops](/man/pdftops)(1), [ps2pdf](/man/ps2pdf)(1), [gs](/man/gs)(1), [lpr](/man/lpr)(1), [pdftotext](/man/pdftotext)(1), [enscript](/man/enscript)(1)"]],"e":["pdftops","ps2pdf","gs","lpr","pdftotext","enscript"],"x":"pdf2ps [input.pdf] [output.ps]"},"picom-trans":{"n":"picom-trans","t":"sets window transparency for the picom compositor","s":[["TAGLINE","sets window transparency for the picom compositor"],["TLDR","**Set window transparency**\n\n```picom-trans -w [window_id] [75]```\n\n**Set by clicking**\n\n```picom-trans -c [75]```\n\n**Set by window name**\n\n```picom-trans -n [firefox] [90]```\n\n**Reset transparency**\n\n```picom-trans -w [window_id] -d```\n\n**Toggle transparency**\n\n```picom-trans -c -t```\n\n**Set opacity (0-100)**\n\n```picom-trans -c [50]```"],["SYNOPSIS","**picom-trans** [_-w id_] [_-n name_] [_-c_] [_-t_] [_-d_] _opacity_"],["PARAMETERS","**-w** _ID_\n> Window ID.\n\n**-n** _NAME_\n> Window name/class.\n\n**-c**\n> Click to select window.\n\n**-t**\n> Toggle transparency.\n\n**-d**\n> Delete/reset transparency.\n\n**-o** _OPACITY_\n> Set opacity (0-100).\n\n**-s**\n> Select focused window."],["DESCRIPTION","**picom-trans** sets window transparency for the picom compositor. It adjusts individual window opacity.\n\nWindow selection by click enables interactive use. Click any window to modify its transparency.\n\nOpacity values range from 0 (invisible) to 100 (opaque). Values like 75 give subtle transparency.\n\nToggle mode switches between transparent and opaque. Useful for temporary visibility changes.\n\nResets remove custom transparency. Windows return to default compositor settings."],["CAVEATS","Requires picom compositor running. Some windows may not support transparency. GPU compositing needed."],["HISTORY","**picom-trans** is part of **picom**, a compositor forked from compton. It provides command-line control over window transparency for X11 desktops."],["INSTALL","```apt: sudo apt install picom```\n\n```dnf: sudo dnf install picom```\n\n```pacman: sudo pacman -S picom```\n\n```apk: sudo apk add picom```\n\n```zypper: sudo zypper install picom```\n\n```nix: nix profile install nixpkgs#picom```"],["SEE ALSO","[picom](/man/picom)(1), [xdotool](/man/xdotool)(1), [xwininfo](/man/xwininfo)(1)"]],"e":["picom","xdotool","xwininfo"],"x":"picom-trans -w [window_id] [75]"},"pomo":{"n":"pomo","t":"Simple, customizable Pomodoro timer for the terminal","s":[["TAGLINE","Simple, customizable Pomodoro timer for the terminal"],["TLDR","**Start a default 25-minute work session**\n\n```pomo```\n\n**Start a 30-minute work session**\n\n```pomo 30m```\n\n**Start a work session with custom break duration**\n\n```pomo 45m 15m```\n\n**View productivity statistics**\n\n```pomo stats```"],["SYNOPSIS","**pomo** [_duration_] [_break_duration_]\n\n**pomo** _command_"],["PARAMETERS","_duration_\n> Work session length (default: 25m). Accepts formats like 30m, 1h.\n\n_break_duration_\n> Break length after work session (default: 5m).\n\n**stats**\n> Show productivity statistics with weekly charts and heatmap."],["DESCRIPTION","**pomo** is a terminal-based Pomodoro timer with customizable work and break intervals, real-time progress visualization, ASCII art displays, and cross-platform desktop notifications. It tracks productivity statistics with weekly charts and a GitHub-style activity heatmap."],["HISTORY","**pomo** was created by **Bahaaio** and is written in **Go**."],["INSTALL","```aur: yay -S pomo```"]],"e":[],"x":"pomo"},"ptx":{"n":"ptx","t":"Produce permuted keyword-in-context index","s":[["TAGLINE","Produce permuted keyword-in-context index"],["TLDR","Generate index with **line references**\n\n```ptx -r [file]```\n\nGenerate with **auto references**\n\n```ptx -A [file]```\n\nSet output **width**\n\n```ptx -w [80] [file]```\n\nUse **word filter**\n\n```ptx -o [filter_file] [file]```\n\nUse **SYSV** traditional mode\n\n```ptx -G [file]```"],["SYNOPSIS","**ptx** [**-A**] [**-r**] [**-w** _width_] [**-o** _file_] [**-G**] [_input_file_]"],["PARAMETERS","**-A, --auto-reference**\n> Generate automatic index references\n\n**-r, --references**\n> Use first field as index reference\n\n**-w, --width _cols_**\n> Output line width in columns\n\n**-o, --only-file _file_**\n> Read list of words to index\n\n**-i, --ignore-file _file_**\n> Read list of words to ignore\n\n**-G, --traditional**\n> SYSV-compatible behavior"],["DESCRIPTION","**ptx** produces a permuted index (also known as KWIC - Key Word In Context) from text files. Each significant word appears as an index entry with surrounding context, allowing readers to locate topics by any word.\n\nThe output shows each word centered with its surrounding context, useful for creating concordances or keyword indexes for documentation."],["CAVEATS","Output format designed for old terminals. May need post-processing for modern use. Word filtering requires separate file. Large files produce extensive output."],["HISTORY","**ptx** is a traditional Unix text processing tool, part of GNU coreutils. Permuted indexes were important for printed documentation before full-text search became common."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[sort](/man/sort)(1), [grep](/man/grep)(1), [awk](/man/awk)(1)"]],"e":["sort","grep","awk"],"x":"ptx -r [file]"},"pueue-enqueue":{"n":"pueue-enqueue","t":"Move stashed tasks into the pueue queue","s":[["TAGLINE","Move stashed tasks into the pueue queue"],["TLDR","**Enqueue stashed task**\n\n```pueue enqueue [task_id]```\n\n**Enqueue multiple tasks**\n\n```pueue enqueue [id1] [id2] [id3]```\n\n**Enqueue with delay**\n\n```pueue enqueue --delay \"[30min]\" [task_id]```"],["SYNOPSIS","**pueue enqueue** [_options_] _task_ids_"],["PARAMETERS","_TASK_IDS_\n> Task IDs to enqueue.\n\n**--delay** _DURATION_\n> Delay before execution."],["DESCRIPTION","**pueue enqueue** moves stashed tasks into the active queue for execution. Tasks that were added with **--stashed** or moved to stash with **pueue stash** can be activated with this command. An optional **--delay** defers execution by a specified duration."],["CAVEATS","Tasks must be in the stashed state before they can be enqueued. Use **pueue stash** to stash running or queued tasks, or add tasks with **pueue add --stashed** to create them in the stashed state."],["INSTALL","```pacman: sudo pacman -S pueue```\n\n```apk: sudo apk add pueue```\n\n```zypper: sudo zypper install pueue```\n\n```brew: brew install pueue```\n\n```nix: nix profile install nixpkgs#pueue```"],["SEE ALSO","[pueue](/man/pueue)(1), [pueue-stash](/man/pueue-stash)(1), [pueue-add](/man/pueue-add)(1), [pueue-status](/man/pueue-status)(1)"]],"e":["pueue","pueue-stash","pueue-add","pueue-status"],"x":"pueue enqueue [task_id]"},"pw-container":{"n":"pw-container","t":"Run apps in isolated PipeWire security contexts","s":[["TAGLINE","Run apps in isolated PipeWire security contexts"],["TLDR","Create a **new security context** and print its socket address\n\n```pw-container```\n\nRun a program within a **new security context**\n\n```pw-container [command] [arg1] [arg2]```\n\nRun connecting to a **specific remote** PipeWire instance\n\n```pw-container -r [remote_name] [command]```\n\nRun with specific **security properties** using JSON\n\n```pw-container -P '{\"pipewire.sec.app-id\": \"[my-app]\"}' [command]```\n\nDisplay **help**\n\n```pw-container -h```"],["SYNOPSIS","**pw-container** [_options_] [_command_] [_arguments_...]"],["PARAMETERS","**-r**, **--remote** _name_\n> Connect to specific remote PipeWire instance\n\n**-P**, **--properties** _json_\n> Set context properties using JSON object\n\n**-h**, **--help**\n> Display help message"],["DESCRIPTION","**pw-container** creates a new security context using the PipeWire SecurityContext extension API. It sets up a temporary Unix socket as a server with the given properties, and clients created from this socket have the security properties attached to them.\n\nIf a command is specified, it is executed within the new security context. If no command is given, the socket address is printed to standard output. The process does not exit until terminated with a signal (exits 0 on SIGINT/SIGTERM).\n\nThis is useful for sandboxing applications or running programs with restricted PipeWire access."],["CAVEATS","Security contexts must be supported by the PipeWire configuration. Properties must be valid JSON format. The process stays running until signaled."],["HISTORY","Part of **PipeWire**, the modern multimedia framework for Linux. Provides application isolation capabilities for audio and video access control."],["INSTALL","```apt: sudo apt install pipewire-bin```\n\n```apk: sudo apk add pipewire-tools```\n\n```zypper: sudo zypper install pipewire-tools```"],["SEE ALSO","[pipewire](/man/pipewire)(1), [pw-cli](/man/pw-cli)(1), [pw-cat](/man/pw-cat)(1), [wpctl](/man/wpctl)(1)"]],"e":["pipewire","pw-cli","pw-cat","wpctl"],"x":"pw-container"},"pw-v4l2":{"n":"pw-v4l2","t":"Run V4L2 video apps on PipeWire","s":[["TAGLINE","Run V4L2 video apps on PipeWire"],["TLDR","Run a **v4l2 program** within PipeWire\n\n```pw-v4l2 program```\n\nDisplay **help**\n\n```pw-v4l2 -h```"],["SYNOPSIS","**pw-v4l2** [_options_] _program_ [_arguments_...]"],["PARAMETERS","**-h**\n> Display help message"],["DESCRIPTION","**pw-v4l2** runs V4L2 (Video4Linux2) programs within PipeWire. It provides a compatibility layer that allows applications using the V4L2 API to access camera devices managed by PipeWire.\n\nThis enables video applications to benefit from PipeWire's permission system and device sharing capabilities."],["CAVEATS","Not all V4L2 features may be fully supported. Some applications may require native PipeWire support for full functionality."],["HISTORY","Part of **PipeWire**, the modern multimedia framework for Linux. Extends PipeWire's device abstraction to video capture devices."],["INSTALL","```apt: sudo apt install pipewire-v4l2```\n\n```dnf: sudo dnf install pipewire-v4l2```\n\n```pacman: sudo pacman -S pipewire-v4l2```\n\n```apk: sudo apk add pipewire-tools```\n\n```zypper: sudo zypper install pipewire-tools```"],["SEE ALSO","[pipewire](/man/pipewire)(1), [pw-cli](/man/pw-cli)(1), [v4l2-ctl](/man/v4l2-ctl)(1)"]],"e":["pipewire","pw-cli","v4l2-ctl"],"x":"pw-v4l2 program"},"python2.7":{"n":"python2.7","t":"Legacy Python 2.7 interpreter","s":[["TAGLINE","Legacy Python 2.7 interpreter"],["TLDR","**Run Python 2.7 script**\n\n```python2.7 [script.py]```\n\n**Interactive mode**\n\n```python2.7```\n\n**Execute command**\n\n```python2.7 -c \"[print 'hello']\"```\n\n**Run module**\n\n```python2.7 -m [module]```"],["SYNOPSIS","**python2.7** [_options_] [_script_] [_args_]"],["PARAMETERS","**-c** _command_\n> Execute command.\n\n**-m** _module_\n> Run library module.\n\n**-i**\n> Interactive after script.\n\n**-u**\n> Unbuffered I/O.\n\n**-O**\n> Optimize bytecode.\n\n**-v**\n> Verbose imports.\n\n**-V**\n> Print version."],["DESCRIPTION","**python2.7** is the Python 2.7 interpreter. Python 2 reached end-of-life on January 1, 2020, and should only be used for legacy code maintenance.\n\nUse Python 3 for all new development."],["EXAMPLES","```bash"],["Run script","python2.7 legacy_script.py"],["Check version","python2.7 --version"],["One-liner (note print statement)","python2.7 -c \"print 'Hello, Python 2'\""],["Install pip for Python 2","python2.7 -m ensurepip\n```"],["KEY DIFFERENCES FROM PYTHON 3","```python"],["Python 2 syntax","print \"hello\"              # Statement\nraw_input(\"Name: \")        # User input\nunicode_string = u\"text\"   # Unicode prefix\n5 / 2 == 2                 # Integer division\n```"],["CAVEATS","Python 2 is EOL and receives no updates. Security vulnerabilities unfixed. Migrate to Python 3."],["HISTORY","Python 2.0 was released in 2000. Python 2.7 was the final 2.x release (2010), with support ending January 1, 2020."],["SEE ALSO","[python3](/man/python3)(1), [2to3](/man/2to3)(1), [pip](/man/pip)(1)"]],"e":["python3","2to3","pip"],"x":"python2.7 [script.py]"},"raspinfo":{"n":"raspinfo","t":"Display Raspberry Pi hardware and software info","s":[["TAGLINE","Display Raspberry Pi hardware and software info"],["TLDR","Display **system information**\n\n```raspinfo```\n\nDisplay system information **paged**\n\n```raspinfo | more```"],["SYNOPSIS","**raspinfo**"],["DESCRIPTION","**raspinfo** displays comprehensive system information for Raspberry Pi devices. It collects and presents hardware details, OS version, memory usage, CPU information, and other diagnostic data.\n\nThe output includes Raspberry Pi model identification, firmware versions, temperature readings, and configuration settings. It's useful for troubleshooting and system documentation."],["OUTPUT","Displays:\n- Raspberry Pi model and revision\n- OS and kernel version\n- CPU and memory information\n- Video configuration\n- Network interfaces\n- USB devices\n- Temperature and voltage\n- Clock frequencies and memory allocations"],["CAVEATS","Raspberry Pi specific tool, installed by default even on Lite images. Output format designed for support tickets. Some information requires root for full details."],["HISTORY","**raspinfo** is part of the Raspberry Pi utilities package, created by the Raspberry Pi Foundation to help users gather system information for troubleshooting and support purposes."],["SEE ALSO","[raspi-config](/man/raspi-config)(1), [vcgencmd](/man/vcgencmd)(1), [pinout](/man/pinout)(1)"]],"e":["raspi-config","vcgencmd","pinout"],"x":"raspinfo"},"scd":{"n":"scd","t":"Smart directory changer with learning","s":[["TAGLINE","Smart directory changer with learning"],["TLDR","**Change to a directory** matching a pattern\n\n```scd [pattern]```\n\n**Add directories** to the index\n\n```scd -a [path/to/directory]```\n\n**Recursively add directories** to the index\n\n```scd -ar [path/to/directory]```\n\n**Create a directory alias**\n\n```scd --alias=[name]```\n\n**Remove a directory alias**\n\n```scd --unalias=[name]```\n\n**Show verbose directory rankings**\n\n```scd -v [pattern]```"],["SYNOPSIS","**scd** [_options_] [_pattern_...]"],["PARAMETERS","**-a, --add**\n> Add specified directories to the directory index.\n\n**-r, --recursive**\n> Apply --add or --unindex recursively.\n\n**--alias**=_NAME_\n> Create alias for the current or specified directory, stored in ~/.scdalias.zsh.\n\n**--unalias**=_NAME_\n> Remove an alias definition.\n\n**--unindex**\n> Remove specified directories from the index.\n\n**-v, --verbose**\n> Display directory rank in the selection menu.\n\n**-h, --help**\n> Show help message."],["DESCRIPTION","**scd** is a Z shell script for changing to any directory with a few keystrokes. It keeps a history of visited directories, which serves as an index of known paths. The directory index is updated after every cd command and can also be filled manually with **scd -a**.\n\nTo switch to a directory, scd needs just a few fragments of the desired path to match against the index. A selection menu is displayed when there are several matches, with preference given to recently visited paths. Permanent directory aliases can be created for instant access.\n\nscd is available as an oh-my-zsh plugin and also works with bash through shell integration."],["CAVEATS","Primarily a zsh script; bash support requires separate setup. History-based learning needs time to build a useful index."],["INSTALL","```aur: yay -S scd```"],["SEE ALSO","[cd](/man/cd)(1), [z](/man/z)(1), [autojump](/man/autojump)(1), [fasd](/man/fasd)(1)"]],"e":["cd","z","autojump","fasd"],"x":"scd [pattern]"},"shnsplit":{"n":"shnsplit","t":"Split audio files using CUE sheets","s":[["TAGLINE","Split audio files using CUE sheets"],["TLDR","Split a **WAV file** using a CUE sheet\n\n```shnsplit -f [path/to/file.cue] [path/to/file.wav]```\n\nSplit and output as **FLAC**\n\n```shnsplit -f [path/to/file.cue] -o flac [path/to/file.flac]```\n\nSplit with **custom filename format**\n\n```shnsplit -f [path/to/file.cue] -t \"%n - %a - %t\" [path/to/file.wav]```\n\nSplit input file into **equal-length segments**\n\n```shnsplit -l [5:00] [path/to/file.wav]```\n\nSplit to a **specific directory**\n\n```shnsplit -f [path/to/file.cue] -d [output_directory] [path/to/file.wav]```\n\nExtract only **specific tracks** from a CUE sheet\n\n```shnsplit -f [path/to/file.cue] -x [1-3,5] [path/to/file.wav]```"],["SYNOPSIS","**shnsplit** [_OPTIONS_] [_file_]\n\n**shntool split** [_OPTIONS_] [_file_]"],["PARAMETERS","**-f** _file_\n> Read split points from CUE sheet or split point file\n\n**-o** _format_\n> Output format (wav, flac, ape, shn, wv)\n\n**-d** _dirname_\n> Output directory for split files\n\n**-t** _fmt_\n> Name files using CUE fields (%n=track, %a=album, %t=title, %p=performer)\n\n**-a** _str_\n> Prefix output filenames with str (default: split-track)\n\n**-z** _str_\n> Postfix output filenames with str\n\n**-c** _num_\n> Start counting from num when naming output files (default: 0)\n\n**-l** _len_\n> Split input file into segments of length len\n\n**-m** _str_\n> Character manipulation string for filenames from CUE sheets (alternating from/to pairs)\n\n**-n** _fmt_\n> Specify file count output format (default: %02d)\n\n**-x** _list_\n> Extract specific tracks only (e.g., \"2-6,9,11-13\")\n\n**-e** _len_\n> Add lead-in from previous track\n\n**-u** _len_\n> Add lead-out from next track\n\n**-i** _fmt_\n> Specify input file format decoder and/or arguments\n\n**-O** _val_\n> Overwrite existing files: ask, always, or never\n\n**-q**\n> Suppress non-critical output (quiet mode)\n\n**-w**\n> Suppress warnings\n\n**-D**\n> Print debugging information\n\n**-h**\n> Display help information"],["DESCRIPTION","**shnsplit** splits a single large audio file into multiple individual track files using split points defined in a CUE sheet. It is part of the **shntool** suite and supports various lossless audio formats including WAV, FLAC, APE, and Shorten.\n\nSplit points can be specified in several formats: bytes, m:ss (minutes:seconds), m:ss.ff (CD frames, 75 per second), or m:ss.nnn (milliseconds). The tool is commonly used to split CD rips, live recordings, or continuous mixes into individual tracks."],["CAVEATS","Requires appropriate encoders/decoders (flac, wavpack, mac) installed for non-WAV formats. Split accuracy depends entirely on CUE sheet correctness. Does not transfer metadata to output files; use **cuetag.sh** for tagging after splitting. Output files are created in current directory unless **-d** specifies otherwise."],["HISTORY","**shntool** and **shnsplit** were developed by **Jason Jordan** as part of the shntool audio processing suite. The project began in the early **2000s** to provide Unix/Linux users with tools for handling Shorten (.shn) files, which were popular for lossless audio distribution. The suite expanded to support multiple formats and remains a standard tool for audio file manipulation."],["INSTALL","```apt: sudo apt install shntool```\n\n```dnf: sudo dnf install shntool```\n\n```zypper: sudo zypper install shntool```\n\n```brew: brew install shntool```\n\n```nix: nix profile install nixpkgs#shntool```"],["SEE ALSO","[cuebreakpoints](/man/cuebreakpoints)(1), [cuetag](/man/cuetag)(1), [flac](/man/flac)(1), [sox](/man/sox)(1), [ffmpeg](/man/ffmpeg)(1)"]],"e":["cuebreakpoints","cuetag","flac","sox","ffmpeg"],"x":"shnsplit -f [path/to/file.cue] [path/to/file.wav]"},"skopeo":{"n":"skopeo","t":"Container image operations without runtime","s":[["TAGLINE","Container image operations without runtime"],["TLDR","**Copy image between registries**\n\n```skopeo copy docker://[source-image] docker://[dest-image]```\n\n**Inspect image**\n\n```skopeo inspect docker://[image:tag]```\n\n**List tags**\n\n```skopeo list-tags docker://[registry/image]```\n\n**Copy to local directory**\n\n```skopeo copy docker://[image] dir:[local-dir]```\n\n**Delete image**\n\n```skopeo delete docker://[image:tag]```\n\n**Sync repository**\n\n```skopeo sync --src docker --dest docker [source] [dest]```"],["SYNOPSIS","**skopeo** _command_ [_options_]"],["DESCRIPTION","**skopeo** performs operations on container images and registries without requiring a container runtime. It can copy, inspect, delete, and sign images across registries.\n\nThe tool is useful for CI/CD pipelines, mirror management, and image inspection without pulling full images."],["PARAMETERS","**copy** _src_ _dest_\n> Copy image.\n\n**inspect** _image_\n> Show image metadata.\n\n**list-tags** _repo_\n> List available tags.\n\n**delete** _image_\n> Delete image from registry.\n\n**sync** _args_\n> Synchronize repositories.\n\n**--src-creds** _user:pass_\n> Source credentials.\n\n**--dest-creds** _user:pass_\n> Destination credentials.\n\n**--authfile** _file_\n> Authentication file.\n\n**--override-os** _os_\n> Override OS.\n\n**--override-arch** _arch_\n> Override architecture."],["CAVEATS","Registry authentication may be needed. Some operations require write access. Large images take time. Delete may not reclaim space."],["HISTORY","**skopeo** was developed by **Red Hat** as part of the container tools project alongside podman and buildah. It enables daemonless container image operations, fitting the podman philosophy."],["INSTALL","```apt: sudo apt install skopeo```\n\n```dnf: sudo dnf install skopeo```\n\n```pacman: sudo pacman -S skopeo```\n\n```apk: sudo apk add skopeo```\n\n```zypper: sudo zypper install skopeo```\n\n```brew: brew install skopeo```\n\n```nix: nix profile install nixpkgs#skopeo```"],["SEE ALSO","[podman](/man/podman)(1), [docker](/man/docker)(1), [crane](/man/crane)(1), [buildah](/man/buildah)(1)"]],"e":["podman","docker","crane","buildah"],"x":"skopeo copy docker://[source-image] docker://[dest-image]"},"snownews":{"n":"snownews","t":"Lightweight terminal RSS feed reader","s":[["TAGLINE","Lightweight terminal RSS feed reader"],["TLDR","**Start Snownews** RSS reader\n\n```snownews```\n\n**Import feeds** from an OPML file\n\n```snownews --import [path/to/feeds.opml]```\n\n**Start with a custom URL file**\n\n```snownews --url [path/to/urls]```\n\n**Force update** of all feeds on startup\n\n```snownews --update```"],["SYNOPSIS","**snownews** [_options_]"],["PARAMETERS","**--import** _file_\n> Import feed subscriptions from an OPML file.\n\n**--url** _file_\n> Use a custom URL file for feed list.\n\n**--update**\n> Update all feeds on startup.\n\n**--charset** _charset_\n> Force a specific character set if auto-detection fails.\n\n**--cursor**\n> Always display the cursor on screen.\n\n**--help**\n> Display help information.\n\n**--version**\n> Display version information."],["KEYBINDINGS","**a**\n> Add a new feed URL.\n\n**D**\n> Delete the highlighted feed.\n\n**r**\n> Reload the highlighted feed.\n\n**R**\n> Reload all feeds.\n\n**T**\n> Force refresh ignoring cache.\n\n**n**, **Down**\n> Move to next item.\n\n**p**, **Up**\n> Move to previous item.\n\n**Enter**\n> Open/read the highlighted item.\n\n**o**\n> Open URL in browser.\n\n**q**\n> Quit or go back.\n\n**h**\n> Display context-sensitive help."],["DESCRIPTION","**Snownews** is a lightweight command-line RSS feed reader designed for text terminals. It supports RSS 0.91, 1.0 (RDF), and 2.0 formats. The interface is simple and keyboard-driven, with feeds displayed in a list format.\n\nSnownews maintains a local cache to minimize network traffic and supports HTTP proxies, HTTP authentication (basic and digest), and cookies. Feed URLs can be imported from OPML files for easy migration from other readers.\n\nBy default, URLs in feeds are opened with a text-mode browser like Lynx, but this can be configured to use any browser. Configuration files are stored in **~/.snownews/** including the URL list and settings."],["CONFIGURATION","**~/.snownews/urls**\n> List of subscribed feed URLs, one per line.\n\n**~/.snownews/browser**\n> Script or command used to open URLs from feed items."],["CAVEATS","Snownews is text-mode only and does not display images or complex formatting from feeds. The default browser is Lynx; configure the browser setting to use graphical browsers. RSS 1.0 feeds must comply with W3C RDF specification for proper parsing."],["HISTORY","**Snownews** was developed as a lightweight RSS reader for the command line. It was designed to integrate well with Unix tools and philosophy, supporting piping and filtering. The project is released under the GNU General Public License version 3 and supports multiple platforms including Linux, BSD, macOS, and Windows (via Cygwin)."],["INSTALL","```dnf: sudo dnf install snownews```\n\n```brew: brew install snownews```"],["SEE ALSO","[newsboat](/man/newsboat)(1), [lynx](/man/lynx)(1), [rss2email](/man/rss2email)(1)"]],"e":["newsboat","lynx","rss2email"],"x":"snownews"},"tldx":{"n":"tldx","t":"Fast domain name availability research tool","s":[["TAGLINE","Fast domain name availability research tool"],["TLDR","**Check domain availability across TLDs**\n\n```tldx [myapp] --tlds com,io,dev```\n\n**Show only available domains with prefixes/suffixes**\n\n```tldx [cloud] --prefixes get,my --suffixes hub,ly --available```\n\n**JSON output with max length filter**\n\n```tldx [startup] --format json --max-length 15```"],["SYNOPSIS","**tldx** _keywords_ [_flags_] [_options_]"],["PARAMETERS","**--tlds** _LIST_\n> Comma-separated list of TLDs to check.\n\n**--prefixes** _LIST_\n> Comma-separated prefixes to prepend.\n\n**--suffixes** _LIST_\n> Comma-separated suffixes to append.\n\n**--available**\n> Show only available domains.\n\n**--format** _FORMAT_\n> Output format: text, json, csv, or grouped.\n\n**--max-length** _N_\n> Maximum domain name length."],["DESCRIPTION","**tldx** generates domain name combinations from keywords, prefixes, and suffixes, then checks their availability via WHOIS lookups using concurrent goroutines. It supports multiple output formats and TLD presets for quick searches."],["HISTORY","**tldx** was created by **Brandon Young** (brandonyoungdev) and is written in **Go**."],["INSTALL","```brew: brew install tldx```\n\n```nix: nix profile install nixpkgs#tldx```"],["SEE ALSO","[whois](/man/whois)(1), [dog](/man/dog)(1), [dig](/man/dig)(1)"]],"e":["whois","dog","dig"],"x":"tldx [myapp] --tlds com,io,dev"},"torbrowser-launcher":{"n":"torbrowser-launcher","t":"Download and launch Tor Browser","s":[["TAGLINE","Download and launch Tor Browser"],["TLDR","**Launch Tor Browser**\n\n```torbrowser-launcher```\n\n**Open settings dialog**\n\n```torbrowser-launcher --settings```\n\n**Check for updates**\n\n```torbrowser-launcher```"],["SYNOPSIS","**torbrowser-launcher** [_options_]"],["PARAMETERS","**--settings**\n> Open the Tor Browser Launcher Settings dialog.\n\n**--help**\n> Display help information."],["DESCRIPTION","**torbrowser-launcher** downloads, installs, and launches the Tor Browser on Linux. On first run, it downloads the latest Tor Browser from the Tor Project, verifies the signature, and installs it in the user's home directory.\n\nSubsequent runs check for updates and launch the browser. The launcher handles downloading the correct version for your architecture and language, and preserves bookmarks and preferences across updates.\n\nThe settings dialog allows configuring the download mirror, whether to use the system Tor service for downloads, and other options.\n\nTor Browser enables anonymous web browsing by routing traffic through the Tor network, encrypting connections and hiding the user's IP address."],["CAVEATS","First launch requires internet connection to download Tor Browser. Downloads may take time depending on network speed. Some networks block Tor; use bridges in settings if needed. Signature verification requires GPG."],["HISTORY","**torbrowser-launcher** was created by Micah Lee to simplify Tor Browser installation on Linux. It addresses security concerns of manual installation by automating signature verification. The launcher is widely packaged in Linux distributions including Debian, Ubuntu, and Fedora."],["INSTALL","```dnf: sudo dnf install torbrowser-launcher```\n\n```pacman: sudo pacman -S torbrowser-launcher```\n\n```zypper: sudo zypper install torbrowser-launcher```"],["SEE ALSO","[tor](/man/tor)(1), [firefox](/man/firefox)(1), [gpg](/man/gpg)(1)"]],"e":["tor","firefox","gpg"],"x":"torbrowser-launcher"},"typioca":{"n":"typioca","t":"Cozy typing speed tester in the terminal","s":[["TAGLINE","Cozy typing speed tester in the terminal"],["TLDR","**Launch** the interactive typing test\n\n```typioca```\n\n**Start** an SSH server for remote typing tests\n\n```typioca serve```"],["SYNOPSIS","**typioca** [_command_]"],["DESCRIPTION","**typioca** is a minimal, terminal-based typing speed tester with an interactive TUI menu. It provides accurate Words Per Minute results and offers timer-based, word count, or sentence count tests with multiple word lists sourced from classical books. Test mode and settings are configured through the interactive menu, not CLI flags."],["HISTORY","**typioca** was created by **Lukas Bloznelis** (bloznelis) and is written in **Go**."],["INSTALL","```zypper: sudo zypper install typioca```\n\n```brew: brew install typioca```\n\n```nix: nix profile install nixpkgs#typioca```"],["SEE ALSO","[ttyper](/man/ttyper)(1), [typtea](/man/typtea)(1), [tukai](/man/tukai)(1), [smassh](/man/smassh)(1)"]],"e":["ttyper","typtea","tukai","smassh"],"x":"typioca"},"vgcfgrestore":{"n":"vgcfgrestore","t":"Restore LVM volume group metadata","s":[["TAGLINE","Restore LVM volume group metadata"],["TLDR","Restore from **last** backup\n\n```sudo vgcfgrestore [vg_name]```\n\nRestore from **file**\n\n```sudo vgcfgrestore -f [path/to/file] [vg_name]```\n\n**List** all backups\n\n```sudo vgcfgrestore -l [vg_name]```\n\nList **specific** backup file\n\n```sudo vgcfgrestore -l -f [path/to/file] [vg_name]```"],["SYNOPSIS","**vgcfgrestore** [_OPTIONS_] _VG_NAME_"],["PARAMETERS","**-f**, **--file** _FILE_\n> Read metadata backup from the named file.\n\n**-l**, **--list**\n> List metadata backup and archive files pertaining to the VG.\n\n**--force**\n> Force metadata restore even with thin pool LVs. Use with extreme caution.\n\n**-M**, **--metadatatype** lvm2\n> Specify the type of on-disk metadata to use.\n\n**-t**, **--test**\n> Run in test mode. Commands will not update metadata.\n\n**-d**, **--debug**\n> Set debug level. Repeat from 1 to 6 times to increase detail."],["DESCRIPTION","**vgcfgrestore** restores LVM volume group configuration from a text backup file produced by vgcfgbackup. This restores the volume group structure, not the actual user data.\n\nThe command is useful for recovering from metadata corruption or recreating volume group configurations after disk replacement."],["CAVEATS","Only restores metadata, not data. Requires root privileges. Physical volumes must exist for restore to succeed. Data may be lost if metadata is inconsistent with disk contents."],["INSTALL","```apt: sudo apt install lvm2```\n\n```dnf: sudo dnf install lvm2```\n\n```pacman: sudo pacman -S lvm2```\n\n```apk: sudo apk add lvm2```\n\n```zypper: sudo zypper install lvm2```\n\n```nix: nix profile install nixpkgs#lvm2```"],["SEE ALSO","[vgcfgbackup](/man/vgcfgbackup)(8), [vgdisplay](/man/vgdisplay)(8), [lvm](/man/lvm)(8)"]],"e":["vgcfgbackup","vgdisplay","lvm"],"x":"sudo vgcfgrestore [vg_name]"},"vgreduce":{"n":"vgreduce","t":"Remove physical volumes from volume groups","s":[["TAGLINE","Remove physical volumes from volume groups"],["TLDR","**Remove physical volume from volume group**\n\n```sudo vgreduce [vg_name] [/dev/sdb]```\n\n**Remove all empty physical volumes**\n\n```sudo vgreduce -a [vg_name]```\n\n**Remove missing physical volumes**\n\n```sudo vgreduce --removemissing [vg_name]```\n\n**Force remove missing PVs with partial LVs**\n\n```sudo vgreduce --removemissing --force [vg_name]```\n\n**Test removal without making changes**\n\n```sudo vgreduce -t [vg_name] [/dev/sdb]```"],["SYNOPSIS","**vgreduce** [_options_] _volume_group_ [_physical_volume_...]"],["PARAMETERS","**-a**, **--all**\n> Remove all empty physical volumes.\n\n**--removemissing**\n> Remove missing (failed) physical volumes.\n\n**--mirrorsonly**\n> Only remove missing PVs from mirror LVs.\n\n**-f**, **--force**\n> Force removal, removing partial LVs if needed.\n\n**-t**, **--test**\n> Test mode; don't make changes.\n\n**-v**, **--verbose**\n> Verbose output.\n\n**-y**, **--yes**\n> Answer yes to prompts."],["DESCRIPTION","**vgreduce** removes physical volumes from a volume group, reducing its capacity. The removed physical volumes can then be used in other volume groups or removed from the system entirely.\n\nBefore removal, ensure the physical volume contains no data by checking with pvdisplay. If data exists, use pvmove to migrate it to other physical volumes first. After vgreduce, use pvremove to clean LVM metadata from the disk.\n\nThe --removemissing option handles failed disks that are no longer present. Combined with --force, it removes any logical volumes that had data on the missing disk."],["CAVEATS","Cannot remove PV with active data without pvmove. Force removal destroys data on affected LVs. Test mode recommended before force operations. Removed PVs need pvremove for reuse."],["HISTORY","**vgreduce** is part of LVM2 (Logical Volume Manager), which evolved from the original LVM in Linux 2.4. LVM2 provides flexible disk management allowing dynamic resizing of storage. The vgreduce command enables non-destructive removal of storage from volume groups."],["INSTALL","```apt: sudo apt install lvm2```\n\n```dnf: sudo dnf install lvm2```\n\n```pacman: sudo pacman -S lvm2```\n\n```apk: sudo apk add lvm2```\n\n```zypper: sudo zypper install lvm2```\n\n```nix: nix profile install nixpkgs#lvm2```"],["SEE ALSO","[vgextend](/man/vgextend)(8), [vgdisplay](/man/vgdisplay)(8), [pvmove](/man/pvmove)(8), [pvremove](/man/pvremove)(8), [lvm](/man/lvm)(8)"]],"e":["vgextend","vgdisplay","pvmove","pvremove","lvm"],"x":"sudo vgreduce [vg_name] [/dev/sdb]"},"vmstat":{"n":"vmstat","t":"Report virtual memory and system statistics","s":[["TAGLINE","Report virtual memory and system statistics"],["TLDR","Display **virtual memory** statistics\n\n```vmstat```\n\nDisplay reports every **2 seconds**, 5 times\n\n```vmstat 2 5```\n\nDisplay **active/inactive** memory\n\n```vmstat -a```\n\nDisplay **disk** statistics\n\n```vmstat -d```\n\nDisplay **slab** information\n\n```vmstat -m```\n\nDisplay **event counters** and memory stats\n\n```vmstat -s```\n\nDisplay with **timestamp**\n\n```vmstat -t 1```"],["SYNOPSIS","**vmstat** [_options_] [_delay_ [_count_]]"],["DESCRIPTION","**vmstat** reports information about processes, memory, paging, block IO, traps, disks, and CPU activity. The first report shows averages since boot; subsequent reports show activity during the sampling period."],["PARAMETERS","**-a, --active**\n> Display active and inactive memory\n\n**-f, --forks**\n> Display number of forks since boot\n\n**-m, --slabs**\n> Display slab information\n\n**-s, --stats**\n> Display event counters and memory statistics\n\n**-d, --disk**\n> Report disk statistics\n\n**-D, --disk-sum**\n> Report summary disk statistics\n\n**-p, --partition DEVICE**\n> Detailed partition statistics\n\n**-n, --one-header**\n> Print header only once\n\n**-t, --timestamp**\n> Append timestamp to each line\n\n**-w, --wide**\n> Wide output mode\n\n**-y, --no-first**\n> Omit first report (boot statistics)\n\n**-S, --unit UNIT**\n> Switch output units (k, K, m, M)"],["OUTPUT FIELDS","**Procs**: r (runnable), b (blocked)\n**Memory**: swpd, free, buff, cache\n**Swap**: si (in), so (out)\n**IO**: bi (blocks in), bo (blocks out)\n**System**: in (interrupts), cs (context switches)\n**CPU**: us (user), sy (system), id (idle), wa (wait), st (stolen)"],["CAVEATS","The first report shows averages since boot. Use **-y** to omit it. Wide mode (**-w**) is recommended for systems with large amounts of memory."],["HISTORY","**vmstat** is part of **procps-ng**, providing virtual memory statistics since early Unix systems."],["INSTALL","```apt: sudo apt install procps```\n\n```dnf: sudo dnf install procps-ng```\n\n```pacman: sudo pacman -S procps-ng```\n\n```apk: sudo apk add procps-ng```\n\n```zypper: sudo zypper install procps```\n\n```brew: brew install procps```\n\n```nix: nix profile install nixpkgs#procps```"],["SEE ALSO","[free](/man/free)(1), [iostat](/man/iostat)(1), [mpstat](/man/mpstat)(1), [ps](/man/ps)(1), [top](/man/top)(1)"]],"e":["free","iostat","mpstat","ps","top"],"x":"vmstat"},"write-good":{"n":"write-good","t":"English prose linter for documentation","s":[["TAGLINE","English prose linter for documentation"],["TLDR","**Check a file** for prose issues\n\n```write-good [path/to/file.md]```\n\n**Check multiple files** using glob pattern\n\n```write-good [**/*.md]```\n\n**Check inline text**\n\n```write-good --text=\"[It should have been defined there.]\"```\n\n**Disable passive voice check**\n\n```write-good --no-passive [path/to/file.md]```\n\n**Disable adverb warnings**\n\n```write-good --no-adverb [path/to/file.md]```\n\n**Only check for specific issues**\n\n```write-good --passive --adverb [path/to/file.md]```\n\n**Parse text from stdin**\n\n```echo \"[The file was written by him.]\" | write-good```"],["SYNOPSIS","**write-good** [**--text**=_string_] [**--no-**_check_] [_file_|_glob_...]"],["PARAMETERS","**--text**=_string_\n> Analyze the provided text string instead of files.\n\n**--no-passive**\n> Disable passive voice detection.\n\n**--no-adverb**\n> Disable adverb warnings (really, extremely, etc.).\n\n**--no-illusion**\n> Disable lexical illusion detection (repeated words).\n\n**--no-so**\n> Disable warnings for sentences starting with \"so\".\n\n**--no-thereIs**\n> Disable warnings for \"there is/are\" at sentence start.\n\n**--no-weasel**\n> Disable weasel word detection.\n\n**--no-tooWordy**\n> Disable wordy phrase detection.\n\n**--no-cliches**\n> Disable cliche detection.\n\n**--yes-eprime**\n> Enable E-Prime checking, flagging forms of \"to be\" (disabled by default).\n\n**--parse**\n> Enable parse-friendly output with Unix exit codes.\n\n**--whitelist** _WORDS_\n> Comma-separated list of words to whitelist from suggestions."],["DESCRIPTION","**write-good** is a linter for English prose that helps identify common writing issues. It flags passive voice, unnecessary adverbs, weasel words, cliches, lexical illusions (repeated words), and wordy phrases.\n\nThe tool is designed for developers writing documentation, README files, and technical content. It can analyze individual files, multiple files via glob patterns, or inline text strings.\n\nEach suggestion includes the problematic text, its position in the file, and a reason explaining the issue. Checks can be selectively disabled using **--no-** flags, or enabled individually by specifying only the desired checks.\n\nwrite-good integrates with editors through plugins for Vim (via ALE), Sublime Text, VS Code, and others. It can also run in Docker containers for CI/CD pipelines."],["CAVEATS","The linter is intentionally \"naive\" and may flag false positives. Passive voice and adverbs are not always wrong; use judgment when reviewing suggestions. The tool focuses on common issues and is not a comprehensive grammar checker. Some technical writing may legitimately require passive construction."],["HISTORY","**write-good** was created by **Brian Ford** (btford) and first released around **2014**. It has become a popular tool in the JavaScript community for improving documentation quality and is used in CI pipelines for open-source projects."],["INSTALL","```brew: brew install write-good```\n\n```nix: nix profile install nixpkgs#write-good```"],["SEE ALSO","[vale](/man/vale)(1), [proselint](/man/proselint)(1), [grammarly](/man/grammarly)(1), [aspell](/man/aspell)(1)"]],"e":["vale","proselint","grammarly","aspell"],"x":"write-good [path/to/file.md]"},"yast2":{"n":"yast2","t":"SUSE system configuration tool","s":[["TAGLINE","SUSE system configuration tool"],["TLDR","**Launch YaST graphical interface**\n\n```sudo yast2```\n\n**Launch YaST text interface**\n\n```sudo yast2 --ncurses```\n\n**Open specific module**\n\n```sudo yast2 [sw_single]```\n\n**List available modules**\n\n```yast2 --list```\n\n**Install packages**\n\n```sudo yast2 --install [package_name]```\n\n**Remove packages**\n\n```sudo yast2 --remove [package_name]```\n\n**Configure network**\n\n```sudo yast2 lan```"],["SYNOPSIS","**yast2** [_options_] [_module_] [_module-options_]"],["PARAMETERS","**--ncurses**\n> Force text-based interface.\n\n**--qt**\n> Force Qt graphical interface.\n\n**--gtk**\n> Force GTK graphical interface.\n\n**--list**\n> List available modules.\n\n**--install** _pkg_\n> Install package.\n\n**--remove** _pkg_\n> Remove package.\n\n**--update** _pkg_\n> Update package.\n\n**--help**\n> Display help."],["COMMON MODULES","**sw_single**: Software management.\n\n**lan**: Network configuration.\n\n**users**: User management.\n\n**firewall**: Firewall configuration.\n\n**disk**: Partitioner.\n\n**bootloader**: Boot loader configuration.\n\n**printer**: Printer setup.\n\n**sound**: Sound card configuration."],["DESCRIPTION","**yast2** (Yet another Setup Tool 2) is the comprehensive system configuration tool for openSUSE and SUSE Linux Enterprise. It provides both graphical and text interfaces for system administration.\n\nYaST can configure hardware, networking, security, software packages, system services, and users. Modules handle specific tasks, accessible from the control center or directly via command line.\n\nThe tool supports AutoYaST for automated installations using XML control files, enabling unattended deployment of multiple systems."],["CAVEATS","SUSE-specific; not available on other distributions. Some modules require root privileges. Being deprecated in favor of Cockpit and Agama in newer SUSE versions."],["HISTORY","**YaST** was first released in April 1995 with SuSE Linux. YaST2 appeared in SuSE 6.3 (1999) as an installer, becoming the full system tool in 6.4. It was released under GPL in 2004. On openSUSE Leap 16 and SLES 16, YaST is being replaced by web-based tools."],["INSTALL","```zypper: sudo zypper install yast2```"],["SEE ALSO","[zypper](/man/zypper)(8), [systemctl](/man/systemctl)(1)"]],"e":["zypper","systemctl"],"x":"sudo yast2"},"zenith":{"n":"zenith","t":"Terminal system monitor with historical data","s":[["TAGLINE","Terminal system monitor with historical data"],["TLDR","**Start zenith**\n\n```zenith```\n\n**Refresh at custom interval**\n\n```zenith --refresh-rate [500]```\n\n**Start with specific database**\n\n```zenith --db [/path/to/zenith.db]```\n\n**Disable GPU monitoring**\n\n```zenith --disable-gpu```\n\n**Use specific graphics mode**\n\n```zenith --graphics-mode [plain]```"],["SYNOPSIS","**zenith** [_options_]"],["PARAMETERS","**-d**, **--db** _path_\n> History database path.\n\n**-r**, **--refresh-rate** _ms_\n> Refresh rate in milliseconds (default: 2000).\n\n**--disable-gpu**\n> Disable GPU monitoring.\n\n**--disable-history**\n> Disable history collection.\n\n**--graphics-mode** _mode_\n> Graphics mode: unicode (default), plain.\n\n**-p**, **--process** _mode_\n> Process display: cpu, mem, top.\n\n**--cpu-height** _rows_\n> CPU chart height.\n\n**--net-height** _rows_\n> Network chart height.\n\n**--disk-height** _rows_\n> Disk chart height.\n\n**-h**, **--help**\n> Display help information.\n\n**-V**, **--version**\n> Display version information."],["KEYBOARD COMMANDS","**q**\n> Quit.\n\n**Tab**\n> Switch focus between sections.\n\n**Up**/**Down** or **j**/**k**\n> Navigate process list.\n\n**/**\n> Filter processes.\n\n**+**/**-**\n> Zoom charts.\n\n**g**/**G**\n> Go to top/bottom of process list.\n\n**e**\n> Expand/collapse disk section.\n\n**p**\n> Pause updates.\n\n**c**\n> Toggle CPU view modes.\n\n**m**\n> Toggle memory view modes.\n\n**dd**\n> Send SIGTERM to selected process.\n\n**dk**\n> Send SIGKILL to selected process."],["DESCRIPTION","**zenith** is a terminal system monitor focusing on historical data visualization. It stores metrics in SQLite for reviewing past system state.\n\nThe interface displays CPU, memory, network, disk, and GPU metrics with sparkline charts. Process list shows current system activity with sorting and filtering.\n\nHistorical data stored in the SQLite database allows viewing past system behavior. Navigate time with zoom controls. Default location is **~/.zenith.db**.\n\nGPU monitoring supports NVIDIA (nvidia-smi) and AMD (amdgpu). Enable at compile time or check build features. Use **--disable-gpu** if unavailable.\n\nThe refresh rate controls how often metrics update. Lower values provide smoother animation but higher CPU usage."],["CAVEATS","GPU support depends on drivers and hardware. Database can grow over time. Some metrics may be platform-specific. Unicode graphics require compatible terminal."],["HISTORY","zenith was created by **Benjamin Vaisvil** and first released around **2019**. Written in Rust, it was designed to combine real-time monitoring with historical analysis. The project distinguishes itself from other monitors by storing and visualizing historical metrics."],["INSTALL","```pacman: sudo pacman -S zenith```\n\n```apk: sudo apk add zenith```\n\n```zypper: sudo zypper install zenith```\n\n```brew: brew install zenith```\n\n```nix: nix profile install nixpkgs#zenith```"],["SEE ALSO","[htop](/man/htop)(1), [top](/man/top)(1), [btm](/man/btm)(1), [gtop](/man/gtop)(1), [glances](/man/glances)(1)"]],"e":["htop","top","btm","gtop","glances"],"x":"zenith"}});
