window.LCL_BUCKET(63,{"ausyscall":{"n":"ausyscall","t":"map between Linux system-call names and numbers","s":[["TAGLINE","map between Linux system-call names and numbers"],["TLDR","**Look up the number of a syscall** by name\n\n```ausyscall [openat]```\n\n**Look up the name** of a syscall by number\n\n```ausyscall [257]```\n\n**Dump the whole syscall table** for the current architecture\n\n```ausyscall --dump```\n\n**Dump the syscall table** for a specific architecture\n\n```ausyscall [i386] --dump```\n\n**Look up a syscall** on a specific architecture\n\n```ausyscall [arm64] [connect]```\n\n**Require an exact match** (no substring)\n\n```ausyscall --exact [open]```"],["SYNOPSIS","**ausyscall** [_arch_] _name_|_number_ [**--exact**]\n\n**ausyscall** [_arch_] **--dump**"],["PARAMETERS","_ARCH_\n> Target architecture: `b32` / `b64` (biarch shortcuts) or an explicit name such as `i386`, `x86_64`, `aarch64`/`arm64`, `arm`, `ppc`, `ppc64le`, `s390x`, `riscv64`. Defaults to the running kernel's architecture.\n\n_NAME_\n> System-call name (e.g. `openat`, `execve`). A substring match is used unless `--exact` is given.\n\n_NUMBER_\n> Numeric system-call number.\n\n**--dump**\n> List every syscall name and number for the selected architecture.\n\n**--exact**\n> Require an exact name match instead of substring.\n\n**-h**, **--help**\n> Show help.\n\n**-V**, **--version**\n> Show version."],["DESCRIPTION","**ausyscall** is a small lookup utility from the **Linux audit** framework. It is the canonical place to translate syscall numbers seen in audit logs, `strace` output, `seccomp` filters, and BPF programs into their human-readable names (and vice-versa) for any supported architecture.\n\nSyscall numbers differ between architectures — `openat` is 257 on x86_64 but 295 on i386 — so always pass the correct architecture when inspecting logs produced on a different machine."],["CAVEATS","Part of the **audit** package (`audit-userspace`). The syscall table is baked in at build time; very new syscalls may be missing on older audit builds. On multi-arch kernels, use `b32`/`b64` shortcuts to pick the right ABI."],["HISTORY","**ausyscall** ships with the Linux Audit user-space tools originally developed by **Red Hat** (primarily **Steve Grubb**). It uses the syscall tables maintained in the audit source tree alongside `auparse`."],["INSTALL","```apt: sudo apt install auditd```\n\n```dnf: sudo dnf install audit```\n\n```pacman: sudo pacman -S audit```\n\n```apk: sudo apk add audit```\n\n```zypper: sudo zypper install audit```\n\n```nix: nix profile install nixpkgs#audit```"],["SEE ALSO","[auditctl](/man/auditctl)(8), [ausearch](/man/ausearch)(8), [aureport](/man/aureport)(8), [strace](/man/strace)(1)"]],"e":["auditctl","ausearch","aureport","strace"],"x":"ausyscall [openat]"},"autoconf":{"n":"autoconf","t":"Generate configure scripts from templates","s":[["TAGLINE","Generate configure scripts from templates"],["TLDR","**Generate** configure script\n\n```autoconf```\n\nGenerate with **specific template**\n\n```autoconf -o [configure] [configure.ac]```\n\n**Trace** a specific macro in configure.ac\n\n```autoconf --trace=[AC_CHECK_LIB]```\n\nShow **version**\n\n```autoconf --version```"],["SYNOPSIS","**autoconf** [_-o output_] [_input_]"],["DESCRIPTION","**autoconf** generates configure scripts from templates (configure.ac or configure.in). These scripts probe the system for available features, libraries, and tools, producing Makefiles tailored to the build environment.\n\nAutoconf is part of the GNU Build System (autotools), used by thousands of open source projects."],["PARAMETERS","**-o** _file_\n> Output file (default: configure)\n\n**-W** _category_\n> Warning category\n\n**-I** _dir_\n> Add directory to search path\n\n**-f**, **--force**\n> Force regeneration\n\n**-v**, **--verbose**\n> Verbose output\n\n**--trace=**_macro_\n> Trace macro calls in configure.ac\n\n**-B** _dir_\n> Prepend directory to search path"],["WORKFLOW","1. Write configure.ac\n2. Run **autoconf** to generate configure\n3. Distribute configure with source\n4. Users run ./configure to generate Makefile"],["CAVEATS","Requires M4 macro processor. Complex syntax based on M4. Generated configure scripts are large. Learning curve is steep."],["HISTORY","**Autoconf** was created by David MacKenzie in **1991** to address the portability problems of Unix software. It became the standard build system for GNU and many other projects."],["INSTALL","```dnf: sudo dnf install autoconf```\n\n```pacman: sudo pacman -S autoconf```\n\n```apk: sudo apk add autoconf```\n\n```zypper: sudo zypper install autoconf```\n\n```brew: brew install autoconf```\n\n```nix: nix profile install nixpkgs#autoconf```"],["SEE ALSO","[automake](/man/automake)(1), [autoreconf](/man/autoreconf)(1), [configure](/man/configure)(1), [m4](/man/m4)(1), [libtool](/man/libtool)(1)"]],"e":["automake","autoreconf","configure","m4","libtool"],"x":"autoconf"},"aws-cloudwatch":{"n":"aws-cloudwatch","t":"Monitor resources with metrics, alarms, and dashboards.","s":[["TAGLINE","Monitor resources with metrics, alarms, and dashboards."],["TLDR","**Put custom metric data** to CloudWatch\n\n```aws cloudwatch put-metric-data --namespace [MyApp] --metric-name [RequestCount] --value [1] --unit Count```\n\n**Get metric statistics** for an EC2 instance CPU\n\n```aws cloudwatch get-metric-statistics --namespace AWS/EC2 --metric-name CPUUtilization --dimensions Name=InstanceId,Value=[i-1234567890abcdef0] --start-time [2024-01-01T00:00:00Z] --end-time [2024-01-02T00:00:00Z] --period 3600 --statistics Average```\n\n**Create an alarm** for high CPU usage\n\n```aws cloudwatch put-metric-alarm --alarm-name [high-cpu] --metric-name CPUUtilization --namespace AWS/EC2 --statistic Average --period 300 --threshold 80 --comparison-operator GreaterThanThreshold --evaluation-periods 2 --alarm-actions [arn:aws:sns:us-east-1:123456789012:my-topic] --dimensions Name=InstanceId,Value=[i-1234567890abcdef0]```\n\n**List all alarms** in the account\n\n```aws cloudwatch describe-alarms```\n\n**List available metrics** for a namespace\n\n```aws cloudwatch list-metrics --namespace [AWS/EC2]```\n\n**Delete specific** alarms\n\n```aws cloudwatch delete-alarms --alarm-names [high-cpu] [low-disk]```\n\n**Disable actions** on an alarm\n\n```aws cloudwatch disable-alarm-actions --alarm-names [high-cpu]```\n\n**Enable actions** on an alarm\n\n```aws cloudwatch enable-alarm-actions --alarm-names [high-cpu]```"],["SYNOPSIS","**aws cloudwatch** _command_ [_options_]"],["DESCRIPTION","**aws cloudwatch** is the AWS CLI interface for Amazon CloudWatch, a monitoring and observability service for AWS resources and applications. CloudWatch collects metrics, logs, and events, providing visibility into resource utilization, application performance, and operational health.\n\nCloudWatch enables setting alarms that trigger notifications or automated actions based on metric thresholds. It supports both built-in AWS service metrics and custom application metrics."],["COMMANDS","**put-metric-data**\n> Publish custom metric data points\n\n**get-metric-data**\n> Retrieve metric values using queries\n\n**get-metric-statistics**\n> Get statistics for a specific metric\n\n**list-metrics**\n> List available metrics by namespace\n\n**put-metric-alarm**\n> Create or update a metric alarm\n\n**describe-alarms**\n> List configured alarms and their states\n\n**set-alarm-state**\n> Manually set alarm state for testing\n\n**put-dashboard**\n> Create or update a CloudWatch dashboard\n\n**delete-alarms**\n> Delete one or more alarms\n\n**enable-alarm-actions**\n> Enable actions for specified alarms\n\n**disable-alarm-actions**\n> Disable actions for specified alarms\n\n**describe-alarms-for-metric**\n> List alarms associated with a specific metric\n\n**list-tags-for-resource**\n> List tags for a CloudWatch resource\n\n**put-anomaly-detector**\n> Create an anomaly detection model for a metric"],["CAVEATS","Metric data older than 15 months is automatically deleted. High-resolution metrics (1-second granularity) incur additional costs. Custom metrics are charged per metric per month. Alarm evaluations may be delayed during service disruptions."],["HISTORY","Amazon CloudWatch launched in **May 2009** alongside EC2 monitoring. Custom metrics were added in **2011**, dashboards in **2015**, and anomaly detection in **2019**. The service has evolved to become the central monitoring hub for all AWS services and is now integrated with CloudWatch Logs and EventBridge."],["INSTALL","```dnf: sudo dnf install aws```\n\n```pacman: sudo pacman -S aws-cli```\n\n```apk: sudo apk add aws-cli```\n\n```zypper: sudo zypper install aws-cli```"],["SEE ALSO","[aws](/man/aws)(1), [aws-logs](/man/aws-logs)(1), [aws-events](/man/aws-events)(1), [aws-ec2](/man/aws-ec2)(1), [aws-sns](/man/aws-sns)(1)"]],"e":["aws","aws-logs","aws-events","aws-ec2","aws-sns"],"x":"aws cloudwatch put-metric-data --namespace [MyApp] --metric-name [RequestCount] --value [1] --unit Count"},"awsmux":{"n":"awsmux","t":"Run one AWS CLI command across many accounts in parallel","s":[["TAGLINE","Run one AWS CLI command across many accounts in parallel"],["TLDR","**Check** environment, aws CLI, and profile discovery\n\n```awsmux doctor```\n\n**List verified targets** (profiles/regions with STS identity)\n\n```awsmux targets --regions [us-east-1,us-west-2]```\n\n**Run a read-only command** across matching profiles\n\n```awsmux run --profiles '[prod-*]' --format jsonl -- ec2 describe-instances --query 'Reservations[].Instances[].InstanceId'```\n\n**Fan out** with concurrency and exclude patterns\n\n```awsmux run --profiles '[prod-*]' --exclude '[*-sandbox]' --concurrency [100] -- ec2 describe-vpcs```\n\n**Plan a mutating/destructive change** (does not execute)\n\n```awsmux plan -- ssm put-parameter --name [/app/flag] --value [on] --type String```\n\n**Approve a plan** (prints a one-time token)\n\n```awsmux approve [plan-01k...]```\n\n**Apply an approved plan** with the token\n\n```awsmux apply [plan-01k...] --approval-token [token]```\n\n**Serve MCP** for AI agents over stdio\n\n```awsmux mcp```"],["SYNOPSIS","**awsmux** _command_ [_options_] [**--** _aws-cli-args_...]"],["PARAMETERS","**doctor**\n\n> Verify aws CLI presence, shared config/credentials files, and profile discovery.\n\n**targets**\n\n> List STS-verified account/region targets selected by profile and region filters.\n\n**run**\n\n> Execute an aws CLI invocation across selected targets in parallel. Read-only operations run freely; mutating and destructive ops require confirmation, **--yes** (mutating only), or a plan.\n\n**plan**\n\n> Build an immutable plan for a mutating or destructive aws CLI operation without executing it.\n\n**approve** _PLAN_ID_\n\n> Approve a plan and print a one-time approval token (never stored).\n\n**apply** _PLAN_ID_ **--approval-token** _TOKEN_\n\n> Execute an approved plan. Token is bound to the plan’s SHA-256 hash; tampering refuses apply.\n\n**replay**\n\n> Re-run a past execution from **history**, re-selecting its targets.\n\n**history**\n\n> Show past runs and results.\n\n**mcp**\n\n> Serve the agent interface over stdio (Model Context Protocol). No extra credentials; uses the same AWS profiles as the shell.\n\nCommon selection and execution flags (on **run**, **plan**, **targets**, and related commands):\n\n**--profiles** _GLOB_\n\n> Include profiles matching shell-style globs (comma-separated).\n\n**--exclude** _GLOB_\n\n> Exclude matching profiles.\n\n**--regions** _LIST_\n\n> Comma-separated regions to fan out into.\n\n**--dedupe**\n\n> Collapse targets that resolve to the same account, principal, and region (runs STS preflight).\n\n**--concurrency** _N_\n\n> Parallel aws CLI workers (default **100**).\n\n**--timeout** _DURATION_\n\n> Per-target timeout (e.g. **30s**).\n\n**--format** _jsonl_|_table_\n\n> Result stream format.\n\n**--output-dir** _DIR_\n\n> Write one result file per target.\n\n**--interactive**\n\n> Checkbox picker for targets (**run** only).\n\n**--max-errors** _N_ / **--stop-on-access-denied**\n\n> Stop the fan-out when error thresholds are hit."],["DESCRIPTION","**awsmux** fans a single **aws** CLI command out across many accounts and regions in parallel. It discovers profiles from the standard shared config and credentials files (**~/.aws/config**, **~/.aws/credentials**, and **AWS_CONFIG_FILE** / **AWS_SHARED_CREDENTIALS_FILE**), supports SSO, static keys, and **credential_process** profiles unchanged, and always executes through the installed **aws** CLI. Every target is identity-checked with STS before work runs.\n\nOperations are classified into **read_only**, **mutating**, **destructive**, and **unknown** (treated as mutating). Read-only work (e.g. describe/list/get, **s3 ls**) runs freely. Mutating work needs **--yes**, interactive confirm, or an approved plan. Destructive work (delete/terminate/revoke, **s3 rm**, **s3 mv**, **s3 sync --delete**, etc.) never accepts **--yes**; it requires a typed confirm or the plan/approve/apply flow. Some verbs that look read-only are forced mutating (credential-minting STS calls, **s3 presign**, s3api get-object to a local outfile).\n\nExit codes are stable for automation: **0** all succeeded, **1** some failed, **2** selection/config error, **3** approval required or rejected, **4** stopped by threshold. **awsmux mcp** exposes the same safety model to AI agents over MCP; agents can list targets and run reads, but mutating work returns a plan the human must approve in a real terminal."],["CAVEATS","Requires a working **aws** CLI on **PATH** (or a well-known install path for MCP clients with a sparse GUI PATH). awsmux does not replace credential tools such as **aws-vault**; it runs through the profiles those tools manage. Destructive commands cannot be forced with **--yes**. Plan tokens bind to plan content: editing a plan file after approve invalidates apply. Default concurrency of 100 spawns many aws subprocesses — tune for rate limits and local resources."],["SEE ALSO","[aws](/man/aws)(1), [aws-vault](/man/aws-vault)(1), [go](/man/go)(1)"],["RESOURCES","```[Source code](https://github.com/0hardik1/awsmux)```\n\n```[Documentation](https://github.com/0hardik1/awsmux/blob/main/docs/ARCHITECTURE.md)```"]],"e":["aws","aws-vault","go"],"x":"awsmux doctor"},"bfgminer":{"n":"bfgminer","t":"modular multi-threaded Bitcoin miner","s":[["TAGLINE","modular multi-threaded Bitcoin miner"],["TLDR","**Mine Bitcoin** using a pool with username and password\n\n```bfgminer -o [stratum+tcp://pool:port] -u [username] -p [password]```\n\n**Mine using a specific device**\n\n```bfgminer -o [stratum+tcp://pool:port] -u [username] -p [password] -d [device]```\n\n**Mine with scrypt algorithm** (Litecoin, Dogecoin, etc.)\n\n```bfgminer --scrypt -o [stratum+tcp://pool:port] -u [username] -p [password]```\n\n**Set GPU intensity** for mining\n\n```bfgminer -o [stratum+tcp://pool:port] -u [username] -p [password] -I [10]```\n\n**Enable the API** on a specific port\n\n```bfgminer -o [stratum+tcp://pool:port] -u [username] -p [password] --api-listen --api-port [4028]```\n\n**Load configuration from file**\n\n```bfgminer -c [path/to/config.json]```"],["SYNOPSIS","**bfgminer** [_-DdEgIKklLmpPQqUSsTouvwOchV_] [_-o url_] [_-u user_] [_-p pass_]"],["PARAMETERS","**-o**, **--url** _URL_\n> URL for bitcoin JSON-RPC server (pool address)\n\n**-u**, **--user** _USERNAME_\n> Username for JSON-RPC server\n\n**-p**, **--pass** _PASSWORD_\n> Password for JSON-RPC server\n\n**-O**, **--userpass** _USER:PASS_\n> Username:password pair for server\n\n**-d**, **--device** _PATTERN_\n> Enable devices matching pattern (use -d? to list)\n\n**-I**, **--intensity** _VALUE_\n> Intensity of GPU scanning (-10 to 20, default: d for desktop interactivity)\n\n**--scrypt**\n> Use scrypt algorithm instead of SHA256d\n\n**-c**, **--config** _FILE_\n> Load a JSON-formatted configuration file\n\n**-l**, **--log** _INTERVAL_\n> Interval in seconds between log output\n\n**-L**, **--log-file** _FILE_\n> Append log output to this file\n\n**-T**, **--text-only**\n> Disable ncurses formatted screen output\n\n**-q**, **--quiet**\n> Disable logging output, display status and errors\n\n**--api-listen**\n> Enable API (default: disabled)\n\n**--api-port** _PORT_\n> Port for API (default: 4028)\n\n**--api-allow** _[G:]IP[/PREFIX]_\n> Allow API access only to specified addresses\n\n**--failover-only**\n> Do not leak work to backup pools in multi-pool setups\n\n**-U**, **--quota** _VALUE_\n> Load-balance quota for pool (e.g., 2:http://pool)\n\n**--benchmark**\n> Run in offline benchmark mode\n\n**--shares** _NUMBER_\n> Quit after mining specified number of shares\n\n**-V**, **--version**\n> Display version and exit\n\n**-h**, **--help**\n> Display help message and exit"],["DESCRIPTION","**bfgminer** is a modular multi-threaded miner written in C, designed for mining Bitcoin and other SHA256d or scrypt-based cryptocurrencies. It supports a wide range of hardware including ASICs, FPGAs, GPUs, and CPUs.\n\nThe miner connects to mining pools using the stratum protocol or JSON-RPC and submits proof-of-work solutions. It features automatic detection and configuration of supported mining devices, dynamic clocking, monitoring, and fan speed control for compatible hardware.\n\nBFGMiner provides a built-in API for remote monitoring and control, making it suitable for managing mining rigs. It supports multiple simultaneous pools with configurable failover and load-balancing strategies."],["CAVEATS","GPU mining for Bitcoin is no longer profitable due to the dominance of ASIC miners. BFGMiner's GPU support is primarily useful for scrypt-based coins or testing. The software requires appropriate drivers for your mining hardware. ASIC and FPGA devices may need specific firmware. Running mining software significantly increases power consumption and heat output."],["HISTORY","BFGMiner was forked from **CGMiner** in **2012** by Luke Dashjr to focus on FPGA and ASIC support while maintaining modularity. The name is a backronym for \"**B**asically a **F**reaking **G**ood Miner.\" It became popular for its extensive hardware support and remote API capabilities. Development continues on GitHub with support for modern mining hardware."],["SEE ALSO","[cgminer](/man/cgminer)(1), [xmrig](/man/xmrig)(1)"]],"e":["cgminer","xmrig"],"x":"bfgminer -o [stratum+tcp://pool:port] -u [username] -p [password]"},"cloc":{"n":"cloc","t":"source code line counter","s":[["TAGLINE","source code line counter"],["TLDR","**Count lines in directory**\n\n```cloc [directory]```\n\n**Count lines in file**\n\n```cloc [file.cpp]```\n\n**Count with file-by-file report**\n\n```cloc --by-file [directory]```\n\n**Count in archive**\n\n```cloc [archive.tar.gz]```\n\n**Compare two versions**\n\n```cloc --diff [old/] [new/]```\n\n**Output as CSV**\n\n```cloc --csv [directory]```\n\n**Output as JSON**\n\n```cloc --json [directory]```\n\n**Exclude directory**\n\n```cloc --exclude-dir=[test,vendor] [directory]```\n\n**List supported languages**\n\n```cloc --show-lang```"],["SYNOPSIS","**cloc** [_options_] _file|directory|archive_..."],["DESCRIPTION","**cloc** (Count Lines of Code) counts blank lines, comment lines, and physical lines of source code in over 200 programming languages. It analyzes individual files, entire directory trees, and compressed archives, producing summary reports broken down by language or by file.\n\nThe tool includes a diff mode that compares two codebases and reports added, removed, modified, and unchanged lines. Output can be generated in plain text, CSV, JSON, SQL, or XML format for integration with other tools and reporting systems.\n\nWritten in Perl with no external module dependencies, cloc is easy to install and runs on any platform with a Perl interpreter. It correctly handles multi-language files and uses language-specific comment detection rules for accurate counting. It can also operate on git repositories directly, comparing branches or commits."],["PARAMETERS","**--by-file**\n> Report results per source file\n\n**--by-file-by-lang**\n> Report per file and per language\n\n**--diff**\n> Compare two code bases\n\n**--csv**\n> Output in CSV format\n\n**--json**\n> Output in JSON format\n\n**--sql**=_file_\n> Output as SQL insert statements\n\n**--exclude-dir**=_list_\n> Comma-separated directories to exclude\n\n**--exclude-lang**=_list_\n> Comma-separated languages to exclude\n\n**--include-lang**=_list_\n> Only count specified languages\n\n**--include-ext**=_list_\n> Only count files with the given comma-separated extensions.\n\n**--max-file-size**=_MB_\n> Skip files larger than _MB_ megabytes (default: 100).\n\n**--show-lang**\n> List recognized languages and exit.\n\n**--git**\n> Forces inputs to be interpreted as git targets (commit hashes, branch names).\n\n**--vcs**=_VCS_\n> Use _VCS_ to obtain list of files to count (e.g., git, svn).\n\n**--xml**\n> Output in XML format.\n\n**--yaml**\n> Output in YAML format.\n\n**--quiet**\n> Suppress progress output.\n\n**--force-lang**=_LANG_\n> Process all files with the given language counter.\n\n**--config** _FILE_\n> Read command-line switches from _FILE_ instead of defaults."],["OUTPUT FIELDS","Language, files, blank lines, comment lines, code lines"],["CAVEATS","Does not analyze semantic code complexity. Some edge cases in multi-language files may be miscounted. Requires Perl but has no external module dependencies. Supports over 250 programming languages."],["HISTORY","**cloc** was created by **Al Danial**, first released on SourceForge and later moved to GitHub. It has become one of the most widely used open-source code counting tools."],["INSTALL","```dnf: sudo dnf install cloc```\n\n```pacman: sudo pacman -S cloc```\n\n```apk: sudo apk add cloc```\n\n```brew: brew install cloc```\n\n```nix: nix profile install nixpkgs#cloc```"],["SEE ALSO","[wc](/man/wc)(1), [sloccount](/man/sloccount)(1), [tokei](/man/tokei)(1)"]],"e":["wc","sloccount","tokei"],"x":"cloc [directory]"},"cloud-init":{"n":"cloud-init","t":"cloud instance initialization and configuration","s":[["TAGLINE","cloud instance initialization and configuration"],["TLDR","Display the **status** of the most recent cloud-init run\n\n```cloud-init status```\n\n**Wait** for cloud-init to finish and report status\n\n```cloud-init status --wait```\n\n**List** available top-level metadata keys\n\n```cloud-init query --list-keys```\n\n**Query** cached instance metadata\n\n```cloud-init query [dot_delimited_variable_path]```\n\n**Clean** logs and artifacts to allow cloud-init to rerun\n\n```cloud-init clean```\n\n**Analyze** cloud-init boot performance\n\n```cloud-init analyze show```\n\n**Validate** a cloud-config file\n\n```cloud-init schema --config-file [path/to/config.yaml]```"],["SYNOPSIS","**cloud-init** [_options_] _command_ [_arguments_]"],["DESCRIPTION","**cloud-init** is the industry standard for cloud instance initialization across all major cloud providers and Linux distributions. It handles early initialization of cloud instances including networking configuration, storage setup, SSH key injection, user account creation, and package installation.\n\nCloud-init reads configuration from various datasources including cloud provider metadata services, user-data scripts, and local configuration files. It runs during the boot process in multiple stages to ensure proper ordering of system configuration tasks.\n\nThe tool supports cloud-config YAML for declarative configuration, shell scripts for imperative setup, and can be extended with custom modules. It provides commands for querying instance metadata, validating configurations, and troubleshooting initialization issues."],["PARAMETERS","**init**\n> Initialize cloud-init and perform initial modules\n\n**modules**\n> Activate modules using a given configuration key\n\n**single**\n> Run a single module\n\n**status**\n> Report cloud-init status\n\n**query** _key_\n> Query instance metadata\n\n**clean**\n> Remove logs and artifacts to allow re-run on next boot\n\n**collect-logs**\n> Gather debugging information into a tarball\n\n**analyze**\n> Analyze cloud-init boot performance\n\n**schema**\n> Validate cloud-config files\n\n**features**\n> List defined features supported by this version"],["CONFIGURATION","**/etc/cloud/cloud.cfg**\n> Main configuration file controlling modules, datasources, and default settings.\n\n**/etc/cloud/cloud.cfg.d/*.cfg**\n> Drop-in configuration files merged with the main config."],["CAVEATS","Primarily useful in cloud/virtual environments with metadata services. Running clean allows cloud-init to run again on next boot, which may reconfigure the system. Some commands require root privileges."],["HISTORY","**cloud-init** was originally developed for Ubuntu by Canonical and has become the de facto standard for cloud instance initialization across most Linux distributions and cloud providers."],["INSTALL","```dnf: sudo dnf install cloud-init```\n\n```pacman: sudo pacman -S cloud-init```\n\n```apk: sudo apk add cloud-init```\n\n```zypper: sudo zypper install cloud-init```\n\n```nix: nix profile install nixpkgs#cloud-init```"],["SEE ALSO","[systemctl](/man/systemctl)(1)"]],"e":["systemctl"],"x":"cloud-init status"},"code2prompt":{"n":"code2prompt","t":"source code to LLM prompt converter","s":[["TAGLINE","source code to LLM prompt converter"],["TLDR","**Generate a prompt** from source code and copy to clipboard\n\n```code2prompt [path/to/project]```\n\n**Use a custom Handlebars template**\n\n```code2prompt [path] -t [template.hbs]```\n\n**Write output to a file**\n\n```code2prompt [path] --output-file [prompt.txt]```\n\n**Include specific file patterns**\n\n```code2prompt [path] --include \"[*.py,*.js]\"```\n\n**Exclude patterns**\n\n```code2prompt [path] --exclude \"[node_modules,*.log]\"```\n\n**Show token count**\n\n```code2prompt [path] --tokens```\n\n**Generate from staged git changes**\n\n```code2prompt [path] --diff```"],["SYNOPSIS","**code2prompt** [_options_] _path_"],["PARAMETERS","**-t**, **--template** _FILE_\n> Path to a custom Handlebars template file.\n\n**--include** _PATTERNS_\n> Comma-separated glob patterns of files to include.\n\n**--exclude** _PATTERNS_\n> Comma-separated glob patterns of files to exclude.\n\n**--exclude-from-tree**\n> Remove excluded files from the source tree display.\n\n**--output-file** _FILE_\n> Write the generated prompt to a file instead of the clipboard.\n\n**--tokens**\n> Display token count of the generated prompt.\n\n**--encoding** _NAME_\n> Tokenizer to use: cl100k (default), p50k, p50k_edit, r50k_base.\n\n**--json**\n> Output results in JSON format.\n\n**--diff**\n> Include the git diff of staged files.\n\n**--git-diff-branch** _BRANCH_\n> Include the diff between branches.\n\n**--git-log-branch** _BRANCH_\n> Include the git log for a branch.\n\n**--line-number**\n> Prefix source lines with line numbers.\n\n**--no-codeblock**\n> Do not wrap code in markdown fenced blocks."],["DESCRIPTION","**code2prompt** transforms a source tree into a structured prompt for Large Language Models (LLMs). It recursively traverses a codebase, respects `.gitignore`, and formats files along with a file-tree overview. The output is copied to the clipboard by default.\n\nHandlebars templates let you customize the generated prompt for specific workflows such as code review, documentation, refactoring, or PR description drafting. Token counting using tiktoken-compatible encodings helps keep prompts within model context limits.\n\nGit integrations (`--diff`, `--git-diff-branch`, `--git-log-branch`) make it easy to generate prompts focused on recent changes rather than the entire codebase."],["INSTALL","```brew: brew install code2prompt```\n\n```nix: nix profile install nixpkgs#code2prompt```"],["SEE ALSO","[tree](/man/tree)(1), [find](/man/find)(1), [git](/man/git)(1)"]],"e":["tree","find","git"],"x":"code2prompt [path/to/project]"},"desktui":{"n":"desktui","t":"Pixel-exact VNC client for the terminal (Kitty graphics)","s":[["TAGLINE","Pixel-exact VNC client for the terminal (Kitty graphics)"],["TLDR","**Connect** to a VNC display by host and display number\n\n```desktui [desk:1]```\n\n**Connect** with host and raw port\n\n```desktui [10.0.0.5::5900]```\n\n**Connect** and scale the desktop to fit the terminal\n\n```desktui [10.0.0.5::5900] --scale fit```\n\n**View-only** session (no input to the remote)\n\n```desktui [desk:1] --view-only```\n\n**Use a password file**\n\n```desktui [desk:1] --password-file [~/.vnc/passwd.txt]```\n\n**Print terminal graphics capabilities**\n\n```desktui --print-caps```\n\n**Draw a test pattern** without a VNC server\n\n```desktui --test-pattern```\n\n**Connect over an SSH tunnel** with env password\n\n```VNC_PASSWORD=[secret] desktui [localhost::5901]```"],["SYNOPSIS","**desktui** [_options_] [_target_]"],["PARAMETERS","_target_\n\n> VNC address in common client forms: **host**, **host:display**, **host::port**, IPv6, or display-style names such as **desk:1**.\n\n**--scale** _MODE_\n\n> Desktop fitting mode: **native** (ask the server to resize to the terminal; default when supported), **fit**, **integer**, or **1:1**. Switchable live with the local prefix then **m**.\n\n**--view-only**\n\n> Do not send keyboard or pointer input to the remote (toggle with prefix then **v**).\n\n**--no-clipboard**\n\n> Disable bidirectional clipboard sync (on by default).\n\n**--password-file** _FILE_\n\n> Read the VNC password from _FILE_ instead of prompting.\n\n**--quality** _N_\n\n> Request a Tight quality level (enables JPEG). Unset means lossless when the server allows it.\n\n**--compression** _N_\n\n> Compression hint for the server.\n\n**--fps** _N_\n\n> Cap frame rate (defaults aim at lossless ~60 fps when the link allows).\n\n**--prefix** _KEY_\n\n> Local command prefix (default **Ctrl+A**). Use when it collides with tmux or another multiplexer.\n\n**--print-caps**\n\n> Probe and print terminal graphics and input capabilities, then exit.\n\n**--test-pattern**\n\n> Exercise the pixel pipeline without connecting to a server.\n\n**--log-file** _FILE_\n\n> Write diagnostics to _FILE_ (stdout cannot be used for logs during a graphics session)."],["DESCRIPTION","**desktui** is a terminal VNC (RFB) client that renders the remote desktop as real pixels via the **Kitty graphics protocol** — one remote pixel per terminal pixel, not half-block ASCII art. It targets terminals that implement Kitty graphics (notably **Ghostty**, **kitty**, and **WezTerm**). Startup probes the terminal and refuses to start with a clear error if image graphics are unavailable.\n\nOn connect, desktui prefers **native** scaling: it sends **SetDesktopSize** so the remote desktop matches the terminal’s usable pixel area and nothing is resampled. Servers that refuse (common with **x11vnc**) fall back to **fit**, **integer**, or **1:1** with panning. Input is passed through to the remote; local commands live behind a prefix (default **Ctrl+A**): quit, refresh, rescale, pan, view-only, and stats.\n\nAuthentication supports none and classic VNC password (TigerVNC, x11vnc, TightVNC, QEMU, Kasm). Password comes from **--password-file**, **$VNC_PASSWORD**, or a prompt when the server asks. Apple Remote Desktop auth, RealVNC proprietary auth, and VeNCrypt/TLS-only servers are not supported. For untrusted networks, use an SSH tunnel: VNC password auth and the session that follows are not modern TLS."],["CAVEATS","Requires a terminal with the Kitty graphics protocol; ordinary terminals without image support cannot run desktui. Shared-memory frame transport (**t=s**) is much faster on a local terminal; over SSH you pay zlib+base64 cost and full-screen motion is capped near the compression path (~48 fps in project measurements). macOS Screen Sharing and RealVNC proprietary schemes will fail with an explicit message. Prefer SSH tunnels for any non-local VNC endpoint."],["SEE ALSO","[vncviewer](/man/vncviewer)(1), [tigervnc](/man/tigervnc)(1), [x11vnc](/man/x11vnc)(1), [vncserver](/man/vncserver)(1), [kitty](/man/kitty)(1), [ghostty](/man/ghostty)(1), [wezterm](/man/wezterm)(1)"],["RESOURCES","```[Source code](https://github.com/mishushakov/desktui)```"]],"e":["vncviewer","tigervnc","x11vnc","vncserver","kitty","ghostty","wezterm"],"x":"desktui [desk:1]"},"dillo":{"n":"dillo","t":"lightweight minimalist web browser","s":[["TAGLINE","lightweight minimalist web browser"],["TLDR","**Launch Dillo** browser\n\n```dillo```\n\n**Open a URL**\n\n```dillo [https://example.com]```\n\n**Open local HTML** file\n\n```dillo [file.html]```\n\n**Open in fullwindow mode** (hide address bar, menu, and buttons)\n\n```dillo -f [url]```\n\n**Open with specific window geometry**\n\n```dillo -g [800x600] [url]```\n\n**Open in local mode** (don't load remote images or follow redirections)\n\n```dillo -l [path/to/file.html]```"],["SYNOPSIS","**dillo** [_options_] [_URL_|_file_]"],["PARAMETERS","_URL_\n> Web address to open.\n\n_FILE_\n> Local HTML file to open.\n\n**-f**, **--fullwindow**\n> Start in fullwindow mode, hiding the address bar, navigation buttons, menu, and status bar.\n\n**-g** _WxH[+X+Y]_\n> Window geometry (width x height and optional position).\n\n**-l**, **--local**\n> Don't load remote images or stylesheets, or follow redirections.\n\n**-v**, **--version**\n> Display version information.\n\n**-h**, **--help**\n> Display help information."],["CONFIGURATION","**~/.dillo/dillorc**\n> Main configuration file for browser preferences, fonts, and behavior.\n\n**~/.dillo/keysrc**\n> Keyboard shortcut mappings.\n\n**~/.dillo/domainrc**\n> Domain-specific rules and settings."],["DESCRIPTION","**Dillo** is a fast, small, and resource-efficient web browser written in C using the FLTK toolkit. It prioritizes speed and privacy over feature completeness, making it suitable for older hardware or minimalist setups.\n\nThe browser supports HTML 4.01 and CSS 2.1 with limitations, focusing on core browsing functionality. It doesn't include JavaScript support, which improves performance and reduces security surface but limits compatibility with modern websites.\n\nDillo features tabbed browsing, bookmarks, and form handling. Its minimal resource usage makes it valuable for embedded systems, older computers, or users who prefer lightweight applications."],["CAVEATS","No JavaScript support limits modern website compatibility. CSS support is incomplete. No plugin system. HTTPS support may require additional libraries. Limited media playback capabilities."],["HISTORY","Dillo was started by **Jorge Arellano Cid** in **1999**. Development focused on creating a minimalist browser that could run on low-resource systems. After periods of dormancy, the project continues with community maintenance."],["INSTALL","```apt: sudo apt install dillo```\n\n```dnf: sudo dnf install dillo```\n\n```pacman: sudo pacman -S dillo```\n\n```apk: sudo apk add dillo```\n\n```brew: brew install dillo```\n\n```nix: nix profile install nixpkgs#dillo```"],["SEE ALSO","[firefox](/man/firefox)(1), [links](/man/links)(1), [lynx](/man/lynx)(1), [surf](/man/surf)(1)"]],"e":["firefox","links","lynx","surf"],"x":"dillo"},"docker-container-run":{"n":"docker-container-run","t":"create and start a new container","s":[["TAGLINE","create and start a new container"],["TLDR","**Run a container**\n\n```docker container run [image]```\n\n**Run interactively**\n\n```docker container run -it [image] /bin/bash```\n\n**Run in background**\n\n```docker container run -d [image]```\n\n**Run with port mapping**\n\n```docker container run -p [8080:80] [image]```\n\n**Run with volume mount**\n\n```docker container run -v [/host/path:/container/path] [image]```\n\n**Run with name**\n\n```docker container run --name [mycontainer] [image]```\n\n**Run and remove on exit**\n\n```docker container run --rm [image]```\n\n**Run with environment variable**\n\n```docker container run -e [VAR=value] [image]```"],["SYNOPSIS","**docker** **container** **run** [_options_] _image_ [_command_] [_args..._]"],["PARAMETERS","**-d**, **--detach**\n> Run in background.\n\n**-i**, **--interactive**\n> Keep STDIN open.\n\n**-t**, **--tty**\n> Allocate pseudo-TTY.\n\n**-p**, **--publish** _hostPort:containerPort_\n> Publish port.\n\n**-v**, **--volume** _src:dest_\n> Bind mount a volume.\n\n**-e**, **--env** _var=value_\n> Set environment variable.\n\n**--name** _name_\n> Assign container name.\n\n**--rm**\n> Remove container on exit.\n\n**--network** _network_\n> Connect to the specified network (bridge, host, none, or a user-defined network).\n\n**-w**, **--workdir** _dir_\n> Working directory inside the container.\n\n**-u**, **--user** _user_[_:group_]\n> Username/UID (and optionally group/GID) to run as.\n\n**--entrypoint** _cmd_\n> Override the image's default **ENTRYPOINT**.\n\n**--restart** _policy_\n> Restart policy: **no**, **on-failure**[**:**_N_], **always**, or **unless-stopped**.\n\n**--pull** _policy_\n> Pull policy before running: **always**, **missing** (default), or **never**.\n\n**-m**, **--memory** _bytes_\n> Hard memory limit (e.g. _512m_, _2g_).\n\n**--cpus** _n_\n> Number of CPUs the container may use (e.g. **1.5**).\n\n**--hostname** _name_\n> Hostname assigned inside the container.\n\n**--privileged**\n> Grant extended privileges to the container.\n\n**--read-only**\n> Mount the container's root filesystem read-only.\n\n**--platform** _os/arch_\n> Set the platform for the image (e.g. **linux/amd64**)."],["DESCRIPTION","**docker container run** creates and starts a new container from a specified image, combining the functionality of **docker container create** and **docker container start** into a single command. This is the most common way to launch containers in Docker.\n\nWhen executed, Docker pulls the image if not locally available, creates a container with the specified configuration, and starts it. The command supports extensive configuration options for networking, storage, resource limits, and runtime behavior. Using **--rm** ensures ephemeral containers are cleaned up automatically, while **-d** enables daemon mode for background services.\n\nThis command is equivalent to the legacy **docker run** command."],["INSTALL","```apt: sudo apt install docker-cli```\n\n```dnf: sudo dnf install docker-cli```\n\n```pacman: sudo pacman -S docker```\n\n```apk: sudo apk add docker-cli```\n\n```zypper: sudo zypper install docker```\n\n```brew: brew install docker```\n\n```nix: nix profile install nixpkgs#docker```"],["SEE ALSO","[docker-run](/man/docker-run)(1), [docker-container](/man/docker-container)(1)"]],"e":["docker-run","docker-container"],"x":"docker container run [image]"},"docx":{"n":"docx","t":"Read, edit, and comment on .docx files from the command line with full format fidelity","s":[["TAGLINE","Read, edit, and comment on .docx files from the command line with full format fidelity"],["TLDR","**Read a document** as Markdown with structural locators\n\n```docx read document.docx```\n\n**Create a new document** from Markdown\n\n```docx create report.docx --from content.md --title \"Q3 Report\"```\n\n**Replace text** while preserving formatting and tabs\n\n```docx replace invoice.docx \"Amount Due\" \"$1,250.00\"```\n\n**Find text and add a comment** at the match\n\n```docx comments add contract.docx --at \"$(docx find contract.docx 'liability' | head -1)\" --text \"Review this clause with legal.\"```\n\n**Turn on tracked changes** and make a redlined edit\n\n```docx track-changes contract.docx on\ndocx replace contract.docx \"reasonable efforts\" \"best efforts\" --track```\n\n**Render pages visually** for layout verification\n\n```docx render proposal.docx --out pages/```\n\n**Install standalone** (verified binary)\n\n```curl -fsSL https://raw.githubusercontent.com/kklimuk/docx-cli/main/install.sh | sh```"],["SYNOPSIS","**docx** _<command>_ [options]"],["DESCRIPTION","**docx** is a command-line tool for AI agents and humans to read, edit, comment on, and review Microsoft Word (.docx) documents without losing formatting or breaking files that Word can open.\n\nIt works by mutating the underlying OOXML directly instead of round-tripping through lossy models or re-serializing the document. Agents receive a stable locator system (e.g. `p3:5-20`, `t1:r0c2:p0`) plus an annotated Markdown view, allowing precise, safe edits.\n\nKey capabilities include:\n\n- Read as Markdown or lossless JSON AST\n- Create, insert, edit, delete, replace content\n- Add and manage comments, footnotes, endnotes, headers/footers, images, hyperlinks, tables\n- Full tracked-changes (redline) support with accept/reject\n- Style management and page geometry\n- Visual page rendering via Word or LibreOffice\n\nThe tool is designed so that `docx <command> --help` is always authoritative. It ships with an optional Agent Skill for Claude Code, Codex, and similar harnesses."],["LOCATORS","Locators address paragraphs (`pN`), tables (`tN`), sections (`sN`), cell paragraphs, character spans (`pN:S-E`), and entity IDs (`cN` for comments, `imgN`, etc.). Use `docx info locators` for the full grammar."],["COMMON COMMANDS","**create** FILE [--from PATH.md | --text \"...\"] [--title T] [--author A]\n\n> Create a new .docx. `--from` accepts the same Markdown dialect used by insert/edit.\n\n**read** FILE [--from LOC] [--to LOC] [--ast] [--comments] [--accepted|--current|--baseline]\n\n> Render body as Markdown (with locator annotations) or JSON AST. Supports tracked-change views.\n\n**edit** / **insert** / **delete** / **replace** FILE ...\n\n> Mutate content at locators or via batch JSONL. Replace keeps existing run formatting.\n\n**find** FILE QUERY [--regex] [--all]\n\n> Return locators for text or formatting matches (for feeding to --at).\n\n**track-changes** on|off|list|accept|reject FILE\n\n> Control and review revisions.\n\n**comments** / **images** / **tables** / **styles** / **sections** ...\n\n> Manage the corresponding document parts.\n\n**render** FILE [--out DIR]\n\n> Produce page images for visual verification of layout.\n\n**wc** / **outline** / **info**\n\n> Word counts, heading trees, and reference material (schema, locators)."],["CAVEATS","There is no undo inside the CLI — use git or copies. Edits that would corrupt the file (e.g. deleting a referenced relationship) are refused. Rendering requires Microsoft Word (macOS/Windows) or LibreOffice. Batch operations address the document snapshot at read time.\n\nThe Markdown dialect preserves most formatting but literal text channels (`--text-file`) exist for content that GFM would alter."],["SEE ALSO","[pandoc](/man/pandoc)(1), [libreoffice](/man/libreoffice)(1)"],["RESOURCES","```[Source code](https://github.com/kklimuk/docx-cli)```\n\n```[Homepage](https://kklimuk.github.io/docx-cli/)```\n\n```[Documentation](https://kklimuk.github.io/docx-cli/)```"]],"e":["pandoc","libreoffice"],"x":"docx read document.docx"},"dosfslabel":{"n":"dosfslabel","t":"display or set FAT filesystem labels","s":[["TAGLINE","display or set FAT filesystem labels"],["TLDR","**Display the label** of a FAT filesystem\n\n```dosfslabel [/dev/sdXN]```\n\n**Set a new label** on a FAT filesystem\n\n```dosfslabel [/dev/sdXN] \"[NEW_LABEL]\"```\n\n**Remove the filesystem label**\n\n```dosfslabel -r [/dev/sdXN]```\n\n**Display the volume ID** instead of label\n\n```dosfslabel -i [/dev/sdXN]```\n\n**Set a new volume ID** (hexadecimal)\n\n```dosfslabel -i [/dev/sdXN] [12345678]```\n\n**Use a specific codepage** for encoding\n\n```dosfslabel -c [437] [/dev/sdXN] \"[LABEL]\"```"],["SYNOPSIS","**dosfslabel** [_OPTIONS_] _DEVICE_ [_NEW_]"],["PARAMETERS","**-i**, **--volume-id**\n> Switch to volume ID mode instead of label mode.\n\n**-r**, **--reset**\n> Remove label in label mode, or generate a new random ID in volume ID mode.\n\n**-c** _PAGE_, **--codepage**=_PAGE_\n> Use DOS codepage PAGE to encode/decode label (default: 850).\n\n**-h**, **--help**\n> Display help message and exit.\n\n**-V**, **--version**\n> Show version number and exit."],["DESCRIPTION","**dosfslabel** displays or changes the volume label or volume ID on MS-DOS FAT filesystems (FAT12, FAT16, FAT32). It is a compatibility symlink to **fatlabel** from the dosfstools package.\n\nIf NEW is omitted, the current label or volume ID is printed to standard output. Labels cannot exceed 11 characters and should be uppercase for best compatibility. An empty string or whitespace-only label is not allowed. Volume IDs are specified as hexadecimal numbers (without 0x prefix) fitting in 32 bits.\n\nFor historical reasons, FAT labels are stored in two locations: the boot sector and as a special entry in the root directory. Modern Windows systems read labels from the root directory. Since version 4.2, dosfslabel reads from root directory but writes to both locations."],["CAVEATS","The filesystem should be unmounted before changing the label to avoid inconsistencies. Labels are limited to 11 characters. Using dosfslabel prior to version 3.0.16 is not recommended due to bugs. Some special characters may not be properly encoded without specifying the correct codepage."],["HISTORY","dosfslabel is part of the **dosfstools** package, which provides utilities for MS-DOS FAT filesystems on Linux. The package includes mkfs.fat for creating filesystems, fsck.fat for checking and repairing, and fatlabel for managing labels. The tools have been maintained since the early days of Linux to ensure compatibility with DOS/Windows filesystems."],["INSTALL","```apt: sudo apt install dosfstools```\n\n```dnf: sudo dnf install dosfstools```\n\n```pacman: sudo pacman -S dosfstools```\n\n```apk: sudo apk add dosfstools```\n\n```zypper: sudo zypper install dosfstools```\n\n```brew: brew install dosfstools```\n\n```nix: nix profile install nixpkgs#dosfstools```"],["SEE ALSO","[fatlabel](/man/fatlabel)(8), [mkfs.fat](/man/mkfs.fat)(8), [e2label](/man/e2label)(8)"]],"e":["fatlabel","mkfs.fat","e2label"],"x":"dosfslabel [/dev/sdXN]"},"dropuser":{"n":"dropuser","t":"PostgreSQL user removal utility","s":[["TAGLINE","PostgreSQL user removal utility"],["TLDR","**Drop a user**\n\n```dropuser [username]```\n\n**Drop with confirmation**\n\n```dropuser -i [username]```\n\n**Drop on remote** host\n\n```dropuser -h [hostname] [username]```\n\n**Drop as specific** admin user\n\n```dropuser -U [admin_user] [username]```\n\n**Drop if exists**\n\n```dropuser --if-exists [username]```"],["SYNOPSIS","**dropuser** [_options_] _username_"],["PARAMETERS","_USERNAME_\n> PostgreSQL role/user to drop.\n\n**-h**, **--host** _HOST_\n> Server hostname.\n\n**-p**, **--port** _PORT_\n> Server port.\n\n**-U**, **--username** _USER_\n> Connect as this user.\n\n**-i**, **--interactive**\n> Prompt for confirmation before dropping.\n\n**--if-exists**\n> Do not raise an error if the role does not exist.\n\n**-e**, **--echo**\n> Print the SQL command that is being sent to the server.\n\n**-w**, **--no-password**\n> Never prompt for a password. Useful in scripts, where the password must come from `~/.pgpass` or the environment.\n\n**-W**, **--password**\n> Force a password prompt.\n\n**--maintenance-db**=_dbname_\n> Database to connect to in order to issue the command. Defaults to `postgres`.\n\n**-V**, **--version**\n> Print the version and exit.\n\n**--help**\n> Display help information."],["DESCRIPTION","**dropuser** removes a PostgreSQL role. It is a thin wrapper around the `DROP ROLE` SQL statement, provided so that user management can be scripted from the shell without invoking `psql`.\n\nIn PostgreSQL, users and groups are the same thing: a **role**. `dropuser` and `createuser` are historical names kept for familiarity, and the distinction between a \"user\" and a \"role\" is only whether the role has the LOGIN attribute.\n\nTo run it you must be a superuser, or hold CREATEROLE and have been granted membership in the role you are dropping."],["CAVEATS","**A role cannot be dropped while it still owns anything.** PostgreSQL refuses with a dependency error listing the objects, and the fix is to reassign or remove them first:\n\n```REASSIGN OWNED BY [olduser] TO [newuser]```\n\n```DROP OWNED BY [olduser]```\n\n`DROP OWNED` also removes the role's privileges on objects it does not own, which `REASSIGN OWNED` leaves behind. Both must be run in **every database** where the role owns something, since ownership is per-database and `dropuser` connects to only one.\n\nDropping a role does not terminate its open sessions, and it does not touch objects in other databases that you have not cleaned up. The role also cannot be the one you are currently connected as."],["HISTORY","dropuser is one of PostgreSQL's original client wrapper scripts, dating from a time when they really were shell scripts around `psql`; they are C programs now, but the interface is unchanged. The name predates the unification of users and groups into roles in **PostgreSQL 8.1** (2005), which is why the tool is called `dropuser` while the SQL it emits says `DROP ROLE`."],["INSTALL","```apk: sudo apk add postgresql17-client```"],["SEE ALSO","[createuser](/man/createuser)(1), [psql](/man/psql)(1), [dropdb](/man/dropdb)(1), [createdb](/man/createdb)(1)"],["RESOURCES","```[Source code](https://git.postgresql.org/gitweb/?p=postgresql.git)```\n\n```[Homepage](https://www.postgresql.org)```\n\n```[Documentation](https://www.postgresql.org/docs/current/app-dropuser.html)```"]],"e":["createuser","psql","dropdb","createdb"],"x":"dropuser [username]"},"duc":{"n":"duc","t":"indexed disk usage calculator with visualization","s":[["TAGLINE","indexed disk usage calculator with visualization"],["TLDR","**Index a directory**\n\n```duc index [/path/to/directory]```\n\n**List directory sizes**\n\n```duc ls [/path]```\n\n**Interactive console UI**\n\n```duc ui [/path]```\n\n**Graphical sunburst view**\n\n```duc gui [/path]```\n\n**Show database info** and when each path was last indexed\n\n```duc info```\n\n**Generate a sunburst image**\n\n```duc graph -o [usage.png] [/path]```\n\n**Export to JSON**\n\n```duc json [/path]```"],["SYNOPSIS","**duc** _command_ [_options_] [_path_]"],["SUBCOMMANDS","**index** _path_\n> Scan the filesystem and write the results to the database. Everything else reads that database.\n\n**ls** [_path_]\n> List directory sizes, with an optional graph. **-R** recurses, **-g** draws a bar, **-d** limits depth.\n\n**ui** [_path_]\n> Interactive ncurses interface for browsing the index.\n\n**gui** [_path_]\n> X11 window with a clickable sunburst chart.\n\n**graph** [_path_]\n> Render a sunburst or treemap to a PNG, SVG, or PDF file.\n\n**xml** / **json**\n> Export the index for another tool to consume.\n\n**cgi**\n> Serve the index over HTTP as a CGI script.\n\n**info**\n> List the indexed paths in the database, with their sizes and the time each was indexed."],["PARAMETERS","**-d**, **--database** _FILE_\n> Database to use. Defaults to `~/.duc.db`, overridable with the `DUC_DATABASE` environment variable.\n\n**-x**, **--one-file-system**\n> Do not cross filesystem boundaries when indexing.\n\n**-e**, **--exclude** _PATTERN_\n> Skip files matching a pattern while indexing.\n\n**--check-hard-links**\n> Count a hard-linked file only once.\n\n**-p**, **--progress**\n> Show progress during a long index run.\n\n**-o**, **--output** _FILE_\n> Output file for **graph**.\n\n**-f**, **--format** _FORMAT_\n> Graph output format: `png`, `svg`, `pdf`, or `html`."],["DESCRIPTION","**duc** (Disk Usage Calculator) indexes filesystem usage into a database for fast repeated queries and provides multiple visualization interfaces. Unlike tools that scan filesystems on every invocation, duc performs an initial scan to build an index, then allows instant queries and visualizations from the cached data.\n\nThe workflow involves two phases: first, use **duc index** to scan directories and build the database; second, query the database using various interfaces (ls, ui, gui, graph). This approach is particularly effective for large filesystems where repeated full scans would be prohibitively slow.\n\nduc offers four main visualization modes: ls (command-line listing), ui (interactive ncurses interface), gui (graphical X11 window with sunburst charts), and graph (generate static images). The graphical modes provide intuitive visual representations of disk usage, making it easy to identify large directories at a glance.\n\nThe database stores complete size information including file counts and sizes at all directory levels. Multiple filesystem scans can live in a single database, and duc records when each was taken.\n\nThis design is what makes duc the right tool for **large** filesystems. Indexing a multi-terabyte fileserver takes a long time, but it only has to happen once, typically from a nightly cron job, after which any number of queries and visualisations are instant. `ncdu` and `du`, which rescan on every invocation, become impractical at that scale."],["CAVEATS","**The index is a snapshot, not a live view.** Everything duc reports reflects the filesystem as it was when `duc index` last ran, so a file deleted this morning still occupies space according to duc until the next index. This is the fundamental trade-off of the design, and the usual answer is a cron job.\n\nThere is no incremental indexing: a re-index rescans the tree from scratch.\n\nThe interfaces have separate dependencies, and distributions often split them across packages, so `duc ui` may be missing without ncurses support compiled in, and `duc gui` needs X11 and cairo. A duc built without them still indexes and lists happily, which makes the absence confusing when you first reach for the sunburst view."],["HISTORY","duc was written by **Ico Doornekamp** as a successor to his earlier `philesight`, and its whole premise is the separation of scanning from viewing. That distinction matters once a filesystem is large enough that walking it is measured in hours rather than seconds, at which point the interactive scanners everyone uses on a laptop stop being an option and an indexed database becomes the only workable approach."],["INSTALL","```apt: sudo apt install duc```\n\n```dnf: sudo dnf install duc```\n\n```zypper: sudo zypper install duc```\n\n```brew: brew install duc```\n\n```nix: nix profile install nixpkgs#duc```"],["SEE ALSO","[du](/man/du)(1), [ncdu](/man/ncdu)(1), [dua](/man/dua)(1), [dust](/man/dust)(1), [baobab](/man/baobab)(1)"],["RESOURCES","```[Source code](https://github.com/zevv/duc)```\n\n```[Homepage](https://duc.zevv.nl)```"]],"e":["du","ncdu","dua","dust","baobab"],"x":"duc index [/path/to/directory]"},"duff":{"n":"duff","t":"duplicate file finder","s":[["TAGLINE","duplicate file finder"],["TLDR","**Find duplicate files** in a directory\n\n```duff [directory]```\n\n**Find duplicates recursively**\n\n```duff -r [directory]```\n\n**Compare files from multiple directories**\n\n```duff -r [dir1] [dir2] [dir3]```\n\n**Byte-for-byte comparison**, not just digests\n\n```duff -rt [directory]```\n\n**Exclude empty files** (which are all identical to each other)\n\n```duff -rz [directory]```\n\n**Include hidden files** in a recursive search\n\n```duff -ra [directory]```\n\n**Print the excess copies only**, ready to pipe into removal\n\n```duff -re [directory]```\n\n**Use a stronger digest**\n\n```duff -r -d [sha256] [directory]```"],["SYNOPSIS","**duff** [_options_] [_file_...]"],["PARAMETERS","**-r**\n> Search the given directories recursively. Without it, duff only looks at the files named.\n\n**-a**\n> Include hidden files and directories in a recursive search.\n\n**-e**\n> Excess mode: print all but one file from each cluster, and omit the headers. This is the form to pipe into `xargs rm`.\n\n**-t**\n> Thorough mode: compare byte by byte when sizes match, instead of trusting the digest.\n\n**-d** _function_\n> Digest algorithm: `sha1` (the default), `sha256`, `sha384`, or `sha512`.\n\n**-z**\n> Do not report empty files as duplicates of one another.\n\n**-p**\n> Physical mode: treat hard links to the same file as distinct, rather than as duplicates.\n\n**-H** / **-L** / **-P**\n> Follow symbolic links given on the command line only; follow all symbolic links; or follow none. **-P** is the default, and each overrides the others.\n\n**-l** _limit_\n> Minimum file size at which duff samples rather than reading whole files. Defaults to zero. This is a **size threshold**, not a cap on the number of results.\n\n**-f** _format_\n> Custom cluster header, with escapes such as `%n` (file count), `%s` (size), `%d` (digest), `%i` (cluster index).\n\n**-q**\n> Quiet mode: suppress warnings and error messages.\n\n**-0**\n> When reading file names from stdin, expect them null-terminated rather than newline-separated."],["DESCRIPTION","**duff** (Duplicate File Finder) identifies duplicate files by comparing file sizes and contents. It groups files with identical content, making it useful for finding and removing redundant files to free disk space.\n\nThe tool first groups files by size, then compares content using checksums and byte-by-byte comparison when necessary. Output shows clusters of duplicate files separated by blank lines."],["OUTPUT FORMAT","```\nfile1.txt\nfile2.txt\n\nanother1.jpg\nanother2.jpg\nanother3.jpg\n```\n\nEach cluster contains files with identical content."],["CAVEATS","**duff does not recurse by default.** `duff mydir` inspects the directory entry itself and finds nothing; you almost always want **-r**. Hidden files are skipped in recursive mode too, unless **-a** is given, so a run that reports no duplicates may simply not have looked.\n\nWithout **-t**, duplicates are decided by **digest**, not by comparing the bytes. A SHA-1 collision is not something you will encounter by accident, but the guarantee is probabilistic rather than absolute, and **-t** upgrades it to certainty at the cost of speed. `-l` makes this weaker still: above the given size duff samples rather than hashing whole files, which is fast and occasionally wrong.\n\nduff never deletes anything, which is a feature. The intended pipeline is **-e**, which prints every copy except one per cluster:\n\n```duff -re [directory] | xargs -d '\\n' rm --```\n\nRead the output before running that. Empty files are all identical to one another and will be swept up unless **-z** is given, and by default hard links to the same inode are reported as duplicates even though deleting one frees nothing: **-p** stops that."],["HISTORY","duff was written by **Camilla Löwy** as a small, fast duplicate finder for Unix. Its approach is the classic one: group by size first, then by digest, and only fall back to comparing bytes when explicitly asked, which means the vast majority of candidate files are never fully read. Development has long been quiet, and **jdupes**, a maintained and considerably faster descendant of fdupes, is what most people reach for now."],["INSTALL","```apt: sudo apt install duff```\n\n```dnf: sudo dnf install duff```\n\n```brew: brew install duff```\n\n```nix: nix profile install nixpkgs#duff```"],["SEE ALSO","[fdupes](/man/fdupes)(1), [rdfind](/man/rdfind)(1), [jdupes](/man/jdupes)(1), [rmlint](/man/rmlint)(1), [find](/man/find)(1)"],["RESOURCES","```[Source code](https://github.com/elmindreda/duff)```"]],"e":["fdupes","rdfind","jdupes","rmlint","find"],"x":"duff [directory]"},"emulator":{"n":"emulator","t":"Android SDK emulator for testing apps","s":[["TAGLINE","Android SDK emulator for testing apps"],["TLDR","**Start Android emulator**\n\n```emulator -avd [avd_name]```\n\n**List available AVDs**\n\n```emulator -list-avds```\n\n**Start with wiped user data**\n\n```emulator -avd [avd_name] -wipe-data```\n\n**Start without snapshot**\n\n```emulator -avd [avd_name] -no-snapshot-load```\n\n**Start with GPU acceleration**\n\n```emulator -avd [avd_name] -gpu host```\n\n**Start in headless mode**\n\n```emulator -avd [avd_name] -no-window```"],["SYNOPSIS","**emulator** [_options_] **-avd** _name_"],["PARAMETERS","**-avd** _name_\n> Android Virtual Device name.\n\n**-list-avds**\n> List available AVDs.\n\n**-wipe-data**\n> Reset user data.\n\n**-no-snapshot-load**\n> Don't load snapshot.\n\n**-no-snapshot-save**\n> Don't save snapshot on exit.\n\n**-gpu** _mode_\n> GPU mode: auto, host, swiftshader, off.\n\n**-no-window**\n> Headless mode.\n\n**-no-audio**\n> Disable audio.\n\n**-memory** _size_\n> RAM size in MB.\n\n**-port** _port_\n> Console port number.\n\n**-dns-server** _servers_\n> DNS server addresses.\n\n**-http-proxy** _proxy_\n> HTTP proxy address.\n\n**-writable-system**\n> Make the system image writable for this session (needed for `adb remount`).\n\n**-snapshot** _name_\n> Boot from the named snapshot.\n\n**-netdelay** _delay_\n> Simulate network latency (gsm, edge, umts, none).\n\n**-netspeed** _speed_\n> Simulate network throughput (gsm, edge, umts, full).\n\n**-camera-back** _mode_\n> Back camera source: emulated, webcam0, none.\n\n**-verbose**\n> Print detailed startup and initialization messages."],["CONFIGURATION","**~/.android/avd/**\n> Directory containing Android Virtual Device configurations and data.\n\n**config.ini**\n> AVD-specific configuration file within each AVD directory."],["DESCRIPTION","**emulator** is the Android Emulator, part of the Android SDK. It runs Android Virtual Devices (AVDs) for app testing and development, simulating various Android devices with different API levels and configurations.\n\nThe emulator provides features like GPS simulation, network conditions, phone calls/SMS, camera, and accelerometer input. It integrates with Android Studio for debugging and testing."],["CAVEATS","Requires hardware virtualization (KVM on Linux, HAXM/Hypervisor on others). First boot is slow. x86 images run faster than ARM emulation. Requires significant RAM and CPU. Some hardware features cannot be fully emulated."],["HISTORY","The Android Emulator was introduced with the first Android SDK by **Google** in **2008**. It has evolved significantly, with QEMU-based architecture improving performance and accuracy. Modern versions use hardware acceleration and quick boot for faster startup."],["SEE ALSO","[adb](/man/adb)(1), [avdmanager](/man/avdmanager)(1), [sdkmanager](/man/sdkmanager)(1)"],["RESOURCES","```[Homepage](https://developer.android.com/studio/run/emulator)```\n\n```[Documentation](https://developer.android.com/studio/run/emulator-commandline)```"]],"e":["adb","avdmanager","sdkmanager"],"x":"emulator -avd [avd_name]"},"fc-cache":{"n":"fc-cache","t":"build fontconfig cache files","s":[["TAGLINE","build fontconfig cache files"],["TLDR","**Rebuild font cache** for all directories\n\n```fc-cache```\n\n**Force rebuild** with verbose output\n\n```fc-cache -fv```\n\n**Rebuild system-wide caches** only (requires root)\n\n```sudo fc-cache -fvs```\n\n**Rebuild cache** for a specific directory\n\n```fc-cache [path/to/fonts/directory]```\n\n**Erase all caches** and rebuild from scratch\n\n```fc-cache -r```\n\n**Check if cache files** are up to date (really verbose)\n\n```fc-cache -rv```"],["SYNOPSIS","**fc-cache** [**-EfrsvVh**] [**--error-on-no-fonts**] [**--force**] [**--really-force**] [**--system-only**] [**--verbose**] [**--version**] [**--help**] [_DIR_...]"],["PARAMETERS","**-f**, **--force**\n> Force re-generation of cache files, overriding timestamp checking.\n\n**-r**, **--really-force**\n> Erase all existing cache files and rescan.\n\n**-s**, **--system-only**\n> Only scan system-wide directories, omitting user home directory locations.\n\n**-v**, **--verbose**\n> Display status information while processing.\n\n**-E**, **--error-on-no-fonts**\n> Raise an error if there are no fonts in the specified directory.\n\n**-V**, **--version**\n> Display fontconfig version and exit.\n\n**-h**, **--help**\n> Display help message and exit."],["DESCRIPTION","**fc-cache** scans font directories and builds font information cache files for applications using fontconfig. These caches map font file names to font properties, enabling fast font lookup at application startup without scanning all font files.\n\nIf no directory arguments are given, fc-cache processes all directories in the current fontconfig configuration. Each directory is scanned for font files readable by FreeType, and cache files are created containing font properties and associated filenames.\n\nUser caches are stored in **~/.cache/fontconfig/** while system caches are stored in **/var/cache/fontconfig/**. Cache files must be regenerated when fonts are added or removed."],["CONFIGURATION","**~/.cache/fontconfig/**\n> User-specific font cache files.\n\n**/var/cache/fontconfig/**\n> System-wide font cache files.\n\n**/etc/fonts/fonts.conf**\n> Main fontconfig configuration specifying font directories.\n\n**~/.config/fontconfig/fonts.conf**\n> User-specific fontconfig configuration."],["CAVEATS","fc-cache must be run once per architecture to generate properly customized font information. When installing new fonts, running **fc-cache -fv** ensures the fonts become immediately available to applications. Some applications may need to be restarted to pick up new fonts even after cache regeneration."],["HISTORY","Fontconfig was initiated by Keith Packard in **2000** for improved font handling in X11, evolving from the older bitmap-focused X font system to support scalable fonts. The fc-cache utility became stable with Fontconfig 2.0 in **2002**. Fontconfig has become the standard font configuration system on Linux desktops."],["INSTALL","```apt: sudo apt install fontconfig```\n\n```dnf: sudo dnf install fontconfig```\n\n```pacman: sudo pacman -S fontconfig```\n\n```apk: sudo apk add fontconfig```\n\n```zypper: sudo zypper install fontconfig```\n\n```brew: brew install fontconfig```\n\n```nix: nix profile install nixpkgs#fontconfig```"],["SEE ALSO","[fc-list](/man/fc-list)(1), [fc-match](/man/fc-match)(1), [fc-query](/man/fc-query)(1), [fc-scan](/man/fc-scan)(1)"]],"e":["fc-list","fc-match","fc-query","fc-scan"],"x":"fc-cache"},"finger":{"n":"finger","t":"display user information and status","s":[["TAGLINE","display user information and status"],["TLDR","**Look up local** user\n\n```finger [username]```\n\n**Look up remote** user\n\n```finger [username]@[host]```\n\n**List all logged** in users\n\n```finger```\n\n**Long format output**\n\n```finger -l [username]```\n\n**Short format** output\n\n```finger -s [username]```"],["SYNOPSIS","**finger** [_options_] [_user_...] [_user@host_...]"],["PARAMETERS","_USER_\n> Username to look up.\n\n_USER@HOST_\n> Remote user lookup.\n\n**-l**\n> Long format output.\n\n**-s**\n> Short format output.\n\n**-m**\n> Match exact username only.\n\n**-p**\n> Omit .plan and .project files.\n\n**--help**\n> Display help information."],["DESCRIPTION","**finger** displays information about system users including name, terminal, login time, and contact information. It reads from system databases and user home directories.\n\nUsers can customize their finger output with .plan and .project files in their home directory. Remote finger queries contact the finger daemon on port 79.\n\nfinger was once common for finding contact information on multi-user systems and across the early internet."],["CAVEATS","Privacy concerns led to disabling on most systems. Remote finger is often blocked. Information disclosure can be a security risk."],["HISTORY","finger originated at **Stanford** in 1971 and became a standard Unix utility. Once ubiquitous for user lookup, it's now largely deprecated due to privacy and security concerns."],["INSTALL","```apt: sudo apt install finger```\n\n```dnf: sudo dnf install finger```"],["SEE ALSO","[who](/man/who)(1), [w](/man/w)(1), [pinky](/man/pinky)(1)"]],"e":["who","w","pinky"],"x":"finger [username]"},"funzip":{"n":"funzip","t":"extract the first member of a ZIP or gzip archive from a pipe","s":[["TAGLINE","extract the first member of a ZIP or gzip archive from a pipe"],["TLDR","**Extract first file from a ZIP archive via stdin**\n\n```cat [file.zip] | funzip > [output]```\n\n**Extract from a gzip file via stdin**\n\n```cat [file.gz] | funzip > [output]```\n\n**Download and extract in one step**\n\n```curl -s [url/file.zip] | funzip > [output]```\n\n**Extract from a file argument directly**\n\n```funzip [file.zip] > [output]```\n\n**Extract a password-protected ZIP archive**\n\n```funzip -[password] [file.zip] > [output]```\n\n**Extract a tar.zip archive using a secondary archiver**\n\n```cat [archive.tar.zip] | funzip | tar xf -```"],["SYNOPSIS","**funzip** [**-**_password_] [_input_[.zip|.gz]]"],["DESCRIPTION","**funzip** acts as a filter to extract the first member from a ZIP archive or a gzip file. Without a file argument, it reads from standard input and writes to standard output. When given a file argument, it reads from that file instead. It also handles gzip compressed files.\n\nThis is most useful for extracting single-file archives from pipes without creating intermediate files, or in combination with a secondary archiver like **tar**(1) for tape backups."],["PARAMETERS","**-**_password_\n> Decryption password for encrypted ZIP archives. Passing passwords on the command line is insecure as they may be visible via ps(1)."],["CAVEATS","Only extracts the first member from a ZIP archive. For multi-file archives, use **unzip** instead. When the first member is a directory, funzip simply creates it and exits."],["INSTALL","```apt: sudo apt install unzip```\n\n```dnf: sudo dnf install unzip```\n\n```pacman: sudo pacman -S unzip```\n\n```apk: sudo apk add unzip```\n\n```zypper: sudo zypper install unzip```\n\n```brew: brew install unzip```\n\n```nix: nix profile install nixpkgs#unzip```"],["SEE ALSO","[gzip](/man/gzip)(1), [unzip](/man/unzip)(1), [unzipsfx](/man/unzipsfx)(1), [zip](/man/zip)(1), [zipinfo](/man/zipinfo)(1)"]],"e":["gzip","unzip","unzipsfx","zip","zipinfo"],"x":"cat [file.zip] | funzip > [output]"},"gh-skyline":{"n":"gh-skyline","t":"Generate a 3D model of your GitHub contribution history","s":[["TAGLINE","Generate a 3D model of your GitHub contribution history"],["TLDR","**Generate skyline for the current user and year**\n\n```gh skyline```\n\n**Generate skyline for a specific user**\n\n```gh skyline --user [username]```\n\n**Generate skyline for a specific year**\n\n```gh skyline --year [2024]```\n\n**Generate skyline for a range of years**\n\n```gh skyline --year [2020]-[2024]```\n\n**Generate the full contribution history since joining GitHub**\n\n```gh skyline --full```\n\n**Show only the ASCII art preview without generating an STL file**\n\n```gh skyline --art-only```\n\n**Specify a custom output filename**\n\n```gh skyline --output [my-skyline.stl]```\n\n**Open the GitHub profile in a web browser**\n\n```gh skyline --web```"],["SYNOPSIS","**gh** **skyline** [_options_]"],["PARAMETERS","**-u**, **--user** _username_\n> GitHub username to generate the skyline for. Defaults to the authenticated user.\n\n**-y**, **--year** _year_\n> Year or range of years (e.g. 2024 or 2020-2024). Must be between 2008 and the current year.\n\n**-f**, **--full**\n> Generate the contribution graph from the user's join year to the current year.\n\n**-a**, **--art-only**\n> Show the ASCII art preview without generating an STL file.\n\n**-o**, **--output** _filename_\n> Output filename. Default is {username}-{year}-github-skyline.stl.\n\n**-w**, **--web**\n> Open the GitHub profile in a web browser.\n\n**-d**, **--debug**\n> Enable debug logging."],["DESCRIPTION","**gh skyline** is a GitHub CLI extension that generates a 3D STL model of a user's GitHub contribution history. It transforms the contribution graph into a skyline where each day's contributions appear as buildings of varying heights. The output is a 3D-printable STL file.\n\nAn ASCII art preview of the skyline is displayed in the terminal. The model can be generated for any GitHub user, for specific years, or for the full contribution history since the user joined GitHub."],["INSTALL","```nix: nix profile install nixpkgs#gh-skyline```"],["SEE ALSO","[gh](/man/gh)(1), [gh-extension](/man/gh-extension)(1)"]],"e":["gh","gh-extension"],"x":"gh skyline"},"git-blame":{"n":"git-blame","t":"Show what revision and author last modified each line","s":[["TAGLINE","Show what revision and author last modified each line"],["TLDR","**Blame a file**\n\n```git blame [file.txt]```\n\n**Blame specific lines**\n\n```git blame -L [10,20] [file.txt]```\n\n**Show email addresses**\n\n```git blame -e [file.txt]```\n\n**Ignore whitespace**\n\n```git blame -w [file.txt]```\n\n**Detect moved lines**\n\n```git blame -M [file.txt]```"],["SYNOPSIS","**git blame** [_options_] [_rev_] [_--_] _file_"],["PARAMETERS","_FILE_\n> File to annotate.\n\n**-L** _START_,_END_\n> Annotate specific line range.\n\n**-e**, **--show-email**\n> Show author email.\n\n**-w**\n> Ignore whitespace changes.\n\n**-M**\n> Detect moved lines within file.\n\n**-C**\n> Detect lines copied from other files.\n\n**-C**, **-C -C**\n> Detect lines copied from other files. Repeat to widen the search beyond the files the commit touched.\n\n**--since** _DATE_\n> Show only commits since date.\n\n**--ignore-rev** _REV_\n> Ignore a revision when assigning blame, e.g. a bulk reformatting commit.\n\n**--ignore-revs-file** _FILE_\n> Ignore every revision listed in a file. Often set repository-wide via **blame.ignoreRevsFile**.\n\n**--reverse** _START_.._END_\n> Walk history forwards, showing the last revision in which each line still existed.\n\n**--porcelain**, **--line-porcelain**\n> Stable, machine-readable output for scripts.\n\n**-l**\n> Show the full, unabbreviated commit hash.\n\n**-s**\n> Suppress the author name and timestamp.\n\n**-f**, **--show-name**\n> Show the filename of the originating commit.\n\n**-n**, **--show-number**\n> Show the line number in the originating commit.\n\n**--help**\n> Display help information."],["DESCRIPTION","**git blame** shows what revision and author last modified each line of a file. It annotates each line with commit information, helping identify when and why changes were made.\n\nThe command is essential for code archaeology, understanding code history, and finding who to ask about specific code. Options like **-M** and **-C** detect moved and copied code across files to show original authorship rather than the person who relocated it.\n\nLine ranges can be specified with **-L** to focus on specific sections, and **-w** ignores whitespace changes to find meaningful modifications. The output is invaluable for debugging, code review, and understanding the evolution of specific code sections."],["CAVEATS","Blame reports the commit that last **touched** a line, which is not necessarily the commit that introduced the behaviour you are investigating. Whitespace fixes, renames and code moves all claim lines they did not really author unless you pass **-w**, **-M** or **-C**.\n\nA repository-wide reformat buries real history behind one mechanical commit. Record such commits in a file and point **blame.ignoreRevsFile** at it, so **git blame** skips past them by default.\n\nAnnotating a large file over deep history is slow, because git reconstructs and diffs many revisions. Narrow the work with **-L** when only a few lines matter.\n\nThe default output format is meant for humans and is not stable across versions; parse **--porcelain** or **--line-porcelain** instead.\n\nBlame follows renames of the file by default. Rewriting history with **rebase** or **filter-branch** changes the commits blame points at, so links to blame output can go stale."],["HISTORY","**git blame** was added in **2006**, adopting the name used by CVS and Subversion for the same idea. **git annotate** exists alongside it as a compatibility spelling with a different output format. The **--ignore-rev** and **--ignore-revs-file** options arrived in git **2.23** (**2019**), in response to the problem of repository-wide reformatting commits."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-annotate](/man/git-annotate)(1), [git-log](/man/git-log)(1), [git-bisect](/man/git-bisect)(1), [git-show](/man/git-show)(1), [git-diff](/man/git-diff)(1)"],["RESOURCES","```[Source code](https://github.com/git/git)```\n\n```[Homepage](https://git-scm.com/)```\n\n```[Documentation](https://git-scm.com/docs/git-blame)```"]],"e":["git-annotate","git-log","git-bisect","git-show","git-diff"],"x":"git blame [file.txt]"},"git-commits-since":{"n":"git-commits-since","t":"List commits since a date","s":[["TAGLINE","List commits since a date"],["TLDR","**Show commits since yesterday**\n\n```git commits-since yesterday```\n\n**Show commits since last week** (default when no date is given)\n\n```git commits-since \"last week\"```\n\n**Show commits since a specific date**\n\n```git commits-since \"[2024-01-01]\"```\n\n**Show commits since a relative time**\n\n```git commits-since \"2 hours ago\"```"],["SYNOPSIS","**git** **commits-since** [_date_]"],["DESCRIPTION","**git commits-since** is a convenience command from git-extras that displays commits made since a specified date. When invoked without arguments, it defaults to showing commits since \"last week\".\n\nIt accepts natural language date expressions like \"yesterday\", \"last week\", \"2 hours ago\", \"3 o clock pm\", or specific dates like \"2024-01-01\". The command wraps git log with appropriate date filtering, providing more readable output than manually constructing git log --since commands.\n\nThis is particularly useful for stand-ups, weekly reviews, generating changelogs, or checking what changed since deploying a release."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-log](/man/git-log)(1), [git-extras](/man/git-extras)(1)"]],"e":["git-log","git-extras"],"x":"git commits-since yesterday"},"git-delete-merged-branches":{"n":"git-delete-merged-branches","t":"Remove all branches merged into current branch","s":[["TAGLINE","Remove all branches merged into current branch"],["TLDR","Remove local branches already **merged into the current branch**\n\n```git delete-merged-branches```"],["SYNOPSIS","**git** **delete-merged-branches**"],["DESCRIPTION","**git delete-merged-branches** deletes all branches that have been merged into the current branch, automating post-merge cleanup. Part of the git-extras toolkit, it identifies branches whose changes are fully incorporated into the current branch and removes them in batch.\n\nThe command uses Git's merge-base calculation to determine which branches are safe to delete, ensuring that only branches with no unique commits are removed. It is commonly run after release cycles or as part of repository maintenance routines to prevent branch sprawl."],["INSTALL","```brew: brew install git-delete-merged-branches```\n\n```nix: nix profile install nixpkgs#git-delete-merged-branches```"],["SEE ALSO","[git-branch](/man/git-branch)(1), [git-extras](/man/git-extras)(1)"],["RESOURCES","```[Source code](https://github.com/tj/git-extras)```"]],"e":["git-branch","git-extras"],"x":"git delete-merged-branches"},"git-merge-index":{"n":"git-merge-index","t":"Run merge program for unmerged files","s":[["TAGLINE","Run merge program for unmerged files"],["TLDR","**Run merge for unmerged files**\n\n```git merge-index [merge-program] -a```\n\n**Merge specific file**\n\n```git merge-index [merge-program] [file]```"],["SYNOPSIS","**git** **merge-index** [_options_] _merge-program_ _files_..."],["PARAMETERS","**-a**\n> Run on all unmerged entries.\n\n**-o**\n> Don't stop on error.\n\n**-q**\n> Quiet mode."],["DESCRIPTION","**git merge-index** runs a specified merge program for each file needing merging. This low-level plumbing command is part of Git's internal merge machinery, invoked during three-way merge operations to handle unmerged files in the index.\n\nThe command passes file information (including base, ours, and theirs versions with their object IDs) to the specified merge program. Most users never need to call this directly, as `git merge` handles it automatically. It is exposed for custom merge workflows and understanding Git internals."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-merge](/man/git-merge)(1), [git-mergetool](/man/git-mergetool)(1)"],["RESOURCES","```[Source code](https://github.com/git/git)```\n\n```[Homepage](https://git-scm.com/)```\n\n```[Documentation](https://git-scm.com/docs/git-merge-index)```"]],"e":["git-merge","git-mergetool"],"x":"git merge-index [merge-program] -a"},"go-test":{"n":"go-test","t":"Go package testing framework","s":[["TAGLINE","Go package testing framework"],["TLDR","**Run tests in current package**\n\n```go test```\n\n**Run tests with verbose output**\n\n```go test -v```\n\n**Run specific tests by pattern**\n\n```go test -run [TestName]```\n\n**Run benchmarks**\n\n```go test -bench .```\n\n**Run with race detector**\n\n```go test -race```\n\n**Run with coverage**\n\n```go test -cover```\n\n**Generate coverage profile**\n\n```go test -coverprofile=[coverage.out]```\n\n**Run tests in all packages**\n\n```go test ./...```"],["SYNOPSIS","**go** **test** [_build/test flags_] [_packages_] [_flags for test binary_]"],["PARAMETERS","**-v**\n> Verbose output.\n\n**-run** _regexp_\n> Run only tests matching pattern.\n\n**-bench** _regexp_\n> Run benchmarks matching pattern.\n\n**-cover**\n> Enable coverage analysis.\n\n**-coverprofile** _file_\n> Write coverage to file.\n\n**-race**\n> Enable race detector.\n\n**-count** _n_\n> Run each test n times.\n\n**-timeout** _d_\n> Test timeout (default 10m).\n\n**-short**\n> Tell tests to shorten long operations.\n\n**-parallel** _n_\n> Max parallel test execution.\n\n**-json**\n> Output results as JSON."],["DESCRIPTION","**go test** automates testing of Go packages. It compiles and runs test files (*_test.go) containing functions named Test*, Benchmark*, and Example*. Tests can run in parallel, include race detection, and generate coverage reports.\n\nThe command recompiles packages as needed and caches successful test results (cache is bypassed with **-count=1**). It runs **go vet** automatically on the package before testing, and supports subtests, table-driven tests, and fuzz testing for comprehensive validation."],["CAVEATS","Cached results are reused for unchanged tests unless **-count=1** is set. Only functions matching `Test*`, `Benchmark*`, `Example*`, and `Fuzz*` in `*_test.go` files are recognized."],["INSTALL","```apt: sudo apt install gccgo-go```\n\n```dnf: sudo dnf install gcc-go```\n\n```pacman: sudo pacman -S gcc-go```\n\n```apk: sudo apk add gcc-go```\n\n```zypper: sudo zypper install gcc-go```\n\n```brew: brew install go```\n\n```nix: nix profile install nixpkgs#go```"],["SEE ALSO","[go](/man/go)(1), [go-build](/man/go-build)(1)"],["RESOURCES","```[Documentation](https://pkg.go.dev/cmd/go#hdr-Test_packages)```"]],"e":["go","go-build"],"x":"go test"},"greed":{"n":"greed","t":"Terminal number-eating puzzle game","s":[["TAGLINE","Terminal number-eating puzzle game"],["TLDR","**Start a new game**\n\n```greed```\n\n**Move using arrow keys**\n\n```# Use arrow keys or hjkl to move```"],["SYNOPSIS","**greed** [_options_]"],["PARAMETERS","**-s**\n> Show high score list.\n\n**--help**\n> Display help information."],["PREVIEW","```\n 4 7 2 9 1 3 8 5\n 5 1 8 3 6 2 7 4\n 9 3 @ 7 4 8 1 6\n 2 6 5 1 9 4 3 7\n```"],["DESCRIPTION","**greed** is a terminal-based puzzle game where players move through a grid collecting numbers. Moving in any direction consumes that many squares, and the goal is to clear as many as possible.\n\nThe player uses arrow keys or vi-style hjkl keys to move. Strategy involves planning moves to avoid getting stuck. The game ends when no valid moves remain."],["CAVEATS","Text-based interface. Requires terminal with cursor support. Game state not saved."],["HISTORY","greed was originally created by **Matt Day** and is maintained by **Eric S. Raymond**. It is a standalone terminal game, not part of the BSD games collection."],["INSTALL","```nix: nix profile install nixpkgs#greed```"],["SEE ALSO","[worm](/man/worm)(1), [bsdgames](/man/bsdgames)(6)"]],"e":["worm","bsdgames"],"x":"greed"},"gri":{"n":"gri","t":"Git and Gerrit review interface for managing change requests","s":[["TAGLINE","Git and Gerrit review interface for managing change requests"],["TLDR","**List all pending reviews** across configured servers\n\n```gri```\n\n**List reviews** from a specific server\n\n```gri [server_name]```\n\n**Show reviews assigned to you**\n\n```gri --mine```\n\n**Show reviews with a specific status**\n\n```gri --status [open|merged|abandoned]```"],["SYNOPSIS","**gri** [_options_] [_server_]"],["DESCRIPTION","**gri** (Git Review Interface) is a command-line tool that lists git reviews from multiple servers in a way that makes it easier to identify which ones need your attention. It supports **Gerrit** and **GitHub** as backends, allowing developers to manage pull requests and code reviews across multiple repositories and servers from a single terminal interface.\n\ngri aggregates change requests and presents them in a unified view, helping developers track outstanding reviews, identify stale changes, and prioritize their code review workflow."],["CAVEATS","Requires Python 3 and appropriate authentication credentials configured for each server. Configuration is managed through a config file that specifies server URLs and authentication tokens. Performance depends on the number of servers and change requests being tracked."],["HISTORY","**gri** was developed as part of the **pycontribs** project to simplify the workflow of developers who review code across multiple Gerrit and GitHub instances. It is written in **Python** and released as open source software."],["INSTALL","```apt: sudo apt install gri```\n\n```zypper: sudo zypper install gri```"],["SEE ALSO","[git](/man/git)(1), [gh](/man/gh)(1)"]],"e":["git","gh"],"x":"gri"},"groupmod":{"n":"groupmod","t":"modify a group definition","s":[["TAGLINE","modify a group definition"],["TLDR","Change the **group name**\n\n```sudo groupmod -n [new_group] [group_name]```\n\nChange the **group ID**\n\n```sudo groupmod -g [new_id] [group_name]```"],["SYNOPSIS","**groupmod** [_options_] _group_"],["PARAMETERS","**-g**, **--gid** _GID_\n> Change group ID to _GID_\n\n**-n**, **--new-name** _NAME_\n> Change group name to _NAME_\n\n**-o**, **--non-unique**\n> Allow non-unique GID\n\n**-p**, **--password** _PASSWORD_\n> Set encrypted group password\n\n**-a**, **--append**\n> With **-U**, append the given users to the group's existing members instead of replacing the list\n\n**-U**, **--users** _USER_[,_USER_,...]\n> Comma-separated list of usernames to set as the group's members\n\n**-R**, **--root** _CHROOT_DIR_\n> Apply changes in chroot environment\n\n**-P**, **--prefix** _PREFIX_DIR_\n> Apply changes in prefix directory (uses config files there without chrooting)\n\n**-h**, **--help**\n> Display help message and exit"],["DESCRIPTION","**groupmod** modifies the attributes of an existing group on the system. It can change the group name, group ID (GID), or group password.\n\nWhen changing GID, files owned by the group are NOT automatically updated. You must manually find and update file ownership using commands like **find / -gid OLDGID -exec chgrp NEWGROUP {} \\;**.\n\nChanging a group name has no effect on file ownership since files reference groups by GID, not name."],["CAVEATS","Changing GID does not update file ownership automatically. Users must log out and back in for group changes to take effect. Cannot change a group to a name or GID that already exists (unless -o is used for GID)."],["HISTORY","groupmod is part of the shadow-utils package, which has been the standard for Unix group management across Linux distributions. It provides safe modification of the /etc/group and /etc/gshadow files."],["INSTALL","```apt: sudo apt install passwd```\n\n```pacman: sudo pacman -S shadow```\n\n```apk: sudo apk add shadow```\n\n```zypper: sudo zypper install shadow```\n\n```nix: nix profile install nixpkgs#shadow```"],["SEE ALSO","[groupadd](/man/groupadd)(8), [groupdel](/man/groupdel)(8), [gpasswd](/man/gpasswd)(1), [usermod](/man/usermod)(8)"]],"e":["groupadd","groupdel","gpasswd","usermod"],"x":"sudo groupmod -n [new_group] [group_name]"},"httprobe":{"n":"httprobe","t":"takes a list of domains from stdin and probes for working HTTP and HTTPS servers","s":[["TAGLINE","takes a list of domains from stdin and probes for working HTTP and HTTPS servers"],["TLDR","**Probe domains from stdin**\n\n```cat [subdomains.txt] | httprobe```\n\n**Probe with concurrency**\n\n```cat [subdomains.txt] | httprobe -c [50]```\n\n**Probe specific ports**\n\n```cat [subdomains.txt] | httprobe -p [http:8080] -p [https:8443]```\n\n**Skip default ports**\n\n```cat [subdomains.txt] | httprobe -s```\n\n**Set timeout**\n\n```cat [subdomains.txt] | httprobe -t [5000]```\n\n**Prefer HTTPS**\n\n```cat [subdomains.txt] | httprobe --prefer-https```"],["SYNOPSIS","**httprobe** [_options_]"],["PARAMETERS","**-c** _n_\n> Concurrency level (default 20).\n\n**-p** _proto:port_\n> Add port to probe (e.g., http:8080).\n\n**-s**\n> Skip default ports (80, 443).\n\n**-t** _ms_\n> Timeout in milliseconds (default 10000).\n\n**--prefer-https**\n> Only output HTTPS when both work."],["DESCRIPTION","**httprobe** takes a list of domains from stdin and probes for working HTTP and HTTPS servers. It outputs URLs of domains that respond to requests. Commonly used in bug bounty workflows after subdomain enumeration to filter down to live web servers before further testing."],["CAVEATS","Written in Go with minimal ongoing maintenance; **httpx** from ProjectDiscovery is a more actively developed alternative with additional probing and fingerprinting features."],["HISTORY","**httprobe** was written by **Tom Hudson** (\"tomnomnom\"), a security researcher known for a suite of small, composable Go tools (including **anew**, **waybackurls**, and **gf**) widely used in bug bounty and recon pipelines."],["INSTALL","```dnf: sudo dnf install httprobe```\n\n```nix: nix profile install nixpkgs#httprobe```"],["SEE ALSO","[httpx](/man/httpx)(1), [curl](/man/curl)(1), [subfinder](/man/subfinder)(1)"],["RESOURCES","```[Source code](https://github.com/tomnomnom/httprobe)```"]],"e":["httpx","curl","subfinder"],"x":"cat [subdomains.txt] | httprobe"},"ipaggcreate":{"n":"ipaggcreate","t":"creates IP address aggregates from network traffic dumps or pcap files","s":[["TAGLINE","creates IP address aggregates from network traffic dumps or pcap files"],["TLDR","**Aggregate IP addresses from a dump file**\n\n```ipaggcreate [input.dump] > [output.agg]```\n\n**Create aggregates from pcap file**\n\n```ipaggcreate -r [capture.pcap] > [output.agg]```\n\n**Aggregate by source IP address**\n\n```ipaggcreate --src [input.dump]```\n\n**Aggregate by destination IP address**\n\n```ipaggcreate --dst [input.dump]```\n\n**Aggregate by TCP/UDP flows**\n\n```ipaggcreate --flows -r [capture.pcap]```\n\n**Count bytes instead of packets**\n\n```ipaggcreate --bytes [input.dump]```"],["SYNOPSIS","**ipaggcreate** [_options_] [_files_]"],["PARAMETERS","**-r**, **--tcpdump** _FILE_\n> Read from one or more tcpdump/pcap files\n\n**-s**, **--src**\n> Label by IP source address\n\n**-d**, **--dst**\n> Label by IP destination address (default)\n\n**--flows**\n> Label by TCP or UDP flow\n\n**--address-pairs**\n> Label by address pair\n\n**--packets**\n> Count packets per label (default)\n\n**-B**, **--bytes**\n> Count bytes per label (IP and transport headers included, link headers excluded)\n\n**-o**, **--output** _FILE_\n> Write output to file instead of stdout\n\n**-f**, **--filter** _FILTER_\n> Include only packets matching a tcpdump filter expression\n\n**-b**, **--binary**\n> Write summary in binary format\n\n**-A**, **--anonymize**\n> Anonymize IP addresses in output\n\n**-t**, **--interval** _TIME_\n> Stop after recording aggregate data for specified duration\n\n**-q**, **--quiet**\n> Suppress progress bar output"],["DESCRIPTION","**ipaggcreate** reads IP packets from one or more data sources, maps each packet to a label (such as source address, destination address, or flow), and outputs an aggregate file reporting the number of packets or bytes observed per label.\n\nThis tool is part of the **ipsumdump** package and works in conjunction with **ipsumdump** and **ipaggmanip** for network traffic analysis and summarization."],["CAVEATS","Input format must be compatible with ipsumdump output or pcap format. Large capture files may require significant memory for aggregation. Output format is specific to the ipagg tool suite."],["HISTORY","Part of the **ipsumdump** package developed by Eddie Kohler at UCLA and later ICSI. The tool suite was created for network measurement research and released as open source software in the early **2000s**."],["SEE ALSO","[ipsumdump](/man/ipsumdump)(1), [ipaggmanip](/man/ipaggmanip)(1), [tcpdump](/man/tcpdump)(1)"]],"e":["ipsumdump","ipaggmanip","tcpdump"],"x":"ipaggcreate [input.dump] > [output.agg]"},"jf":{"n":"jf","t":"JFrog CLI for interacting with JFrog platform services","s":[["TAGLINE","JFrog CLI for interacting with JFrog platform services"],["TLDR","**Configure Artifactory**\n\n```jf config add```\n\n**Upload artifact**\n\n```jf rt u [file.jar] [repo/path/]```\n\n**Download artifact**\n\n```jf rt dl [repo/path/file.jar]```\n\n**Search artifacts**\n\n```jf rt s [repo/] --include-dirs```\n\n**Build publish**\n\n```jf rt bp [build-name] [build-number]```\n\n**Docker push**\n\n```jf docker push [image:tag] [repo]```\n\n**Run Xray scan** on a build\n\n```jf xr scan --watches \"[watch_name]\"```\n\n**Show server configuration**\n\n```jf config show```"],["SYNOPSIS","**jf** _command_ [_options_]"],["PARAMETERS","**config** _CMD_\n> Configure server connections.\n\n**rt** _CMD_\n> Artifactory commands.\n\n**docker** _CMD_\n> Docker registry commands.\n\n**npm** _CMD_\n> npm registry commands.\n\n**pip** _CMD_\n> Python registry commands.\n\n**xr** _CMD_\n> Xray security scanning commands.\n\n**go** _CMD_\n> Go registry commands.\n\n**--server-id** _ID_\n> Server ID from configured servers.\n\n**--help**\n> Display help information."],["DESCRIPTION","**jf** is the JFrog CLI for interacting with JFrog platform services including Artifactory, Xray, Distribution, and Pipelines. It manages artifact upload/download, build info collection, security scanning, and CI/CD integration.\n\nThe tool supports all major package types (Maven, npm, Docker, Go, Python, NuGet, etc.) and provides both interactive and automation-friendly interfaces. The `rt` subcommand prefix targets Artifactory operations specifically."],["CAVEATS","Alias for jfrog CLI. Requires server configuration. Enterprise features may need license."],["HISTORY","jf is the short alias for the **JFrog CLI**, introduced to simplify command-line usage of JFrog platform services."],["INSTALL","```nix: nix profile install nixpkgs#jf```"],["SEE ALSO","[jfrog](/man/jfrog)(1), [docker](/man/docker)(1), [npm](/man/npm)(1), [mvn](/man/mvn)(1)"]],"e":["jfrog","docker","npm","mvn"],"x":"jf config add"},"jj-evolog":{"n":"jj-evolog","t":"shows the evolution history of a change","s":[["TAGLINE","shows the evolution history of a change"],["TLDR","**Show evolution log**\n\n```jj evolog```\n\n**Evolog for specific revision(s)**\n\n```jj evolog -r [revset]```\n\n**Show with diff**\n\n```jj evolog -p```\n\n**Limit entries**\n\n```jj evolog -n [10]```\n\n**Flat list (no graph)**\n\n```jj evolog --no-graph```\n\n**Show oldest entries first**\n\n```jj evolog --reversed```"],["SYNOPSIS","**jj evolog** [_options_]"],["PARAMETERS","**-r**, **--revisions** _REVSETS_\n> Follow evolution of these revisions (default: _@_, the working-copy commit).\n\n**-n**, **--limit** _LIMIT_\n> Limit the number of revisions to show.\n\n**--reversed**\n> Show revisions in opposite order (older first).\n\n**-G**, **--no-graph**\n> Hide the ASCII graph and show a flat list of revisions.\n\n**-T**, **--template** _TEMPLATE_\n> Render output using a custom template expression.\n\n**-p**, **--patch**\n> Show a diff against the previous version of each change.\n\n**-s**, **--summary**\n> For each path, show only whether it was modified, added, or deleted.\n\n**--stat**\n> Show a histogram of the changes per file.\n\n**--git**\n> Format diffs in Git-compatible form.\n\n**--name-only**\n> Show only path names of changed files.\n\n**--help**\n> Display help information."],["DESCRIPTION","**jj evolog** shows the evolution history of a change. It displays how a change has been modified over time.\n\nThe command reveals rewriting, rebasing, and amendment history. It helps understand how changes evolved."],["CAVEATS","Subcommand of jj. Shows internal evolution. Unique to Jujutsu model."],["HISTORY","jj evolog is part of **Jujutsu**, leveraging its first-class support for change evolution tracking."],["SEE ALSO","[jj](/man/jj)(1), [jj-log](/man/jj-log)(1), [jj-show](/man/jj-show)(1)"]],"e":["jj","jj-log","jj-show"],"x":"jj evolog"},"kube-capacity":{"n":"kube-capacity","t":"provides a simple view of Kubernetes resource requests, limits, and utilization","s":[["TAGLINE","provides a simple view of Kubernetes resource requests, limits, and utilization"],["TLDR","**Show cluster capacity**\n\n```kube-capacity```\n\n**Show pod resource usage**\n\n```kube-capacity --pods```\n\n**Show utilization percentages**\n\n```kube-capacity --util```\n\n**Show specific namespace**\n\n```kube-capacity --namespace [default]```\n\n**Show containers detail**\n\n```kube-capacity --containers```"],["SYNOPSIS","**kube-capacity** [_options_]"],["PARAMETERS","**--pods**\n> Show pod-level resource usage.\n\n**--containers**\n> Show container-level details.\n\n**--util**\n> Show utilization percentages.\n\n**--namespace** _name_\n> Filter by namespace.\n\n**--node-labels** _selector_\n> Filter by node labels.\n\n**--sort** _field_\n> Sort output by field."],["DESCRIPTION","**kube-capacity** provides a simple view of Kubernetes resource requests, limits, and utilization. Displays CPU and memory allocation across nodes, pods, and containers. Helps identify over-provisioned or under-utilized resources."],["INSTALL","```nix: nix profile install nixpkgs#kube-capacity```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [kubectl-top](/man/kubectl-top)(1)"]],"e":["kubectl","kubectl-top"],"x":"kube-capacity"},"lcov":{"n":"lcov","t":"processes gcov code coverage data","s":[["TAGLINE","processes gcov code coverage data"],["TLDR","**Capture coverage data**\n\n```lcov --capture --directory [.] --output-file [coverage.info]```\n\n**Generate HTML report**\n\n```genhtml [coverage.info] --output-directory [coverage/]```\n\n**Remove patterns**\n\n```lcov --remove [coverage.info] '*/test/*' -o [filtered.info]```\n\n**Extract specific files**\n\n```lcov --extract [coverage.info] '*/src/*' -o [src.info]```\n\n**Combine traces**\n\n```lcov --add-tracefile [a.info] --add-tracefile [b.info] -o [combined.info]```\n\n**Zero counters**\n\n```lcov --zerocounters --directory [.]```"],["SYNOPSIS","**lcov** [_options_]"],["PARAMETERS","**--capture**\n> Capture coverage data.\n\n**--directory** _DIR_\n> Source directory.\n\n**--output-file** _FILE_\n> Output trace file.\n\n**--remove** _FILE_ _PATTERN_\n> Remove matching files.\n\n**--extract** _FILE_ _PATTERN_\n> Extract matching files.\n\n**--add-tracefile** _FILE_\n> Add trace file.\n\n**--help**\n> Display help information."],["DESCRIPTION","**lcov** processes gcov code coverage data. It collects and manipulates coverage information.\n\nThe tool generates coverage reports from gcc/gcov data. Use genhtml to create HTML reports."],["CAVEATS","Requires gcc/gcov. C/C++ focused. Part of LCOV package."],["HISTORY","lcov was created for the **Linux Test Project** to provide graphical code coverage analysis."],["INSTALL","```dnf: sudo dnf install lcov```\n\n```pacman: sudo pacman -S lcov```\n\n```apk: sudo apk add lcov```\n\n```zypper: sudo zypper install lcov```\n\n```brew: brew install lcov```\n\n```nix: nix profile install nixpkgs#lcov```"],["SEE ALSO","[gcov](/man/gcov)(1), [genhtml](/man/genhtml)(1)"]],"e":["gcov","genhtml"],"x":"lcov --capture --directory [.] --output-file [coverage.info]"},"lev_comp":{"n":"lev_comp","t":"calculates Levenshtein distance between strings","s":[["TAGLINE","calculates Levenshtein distance between strings"],["TLDR","**Compare two strings and show the distance**\n\n```lev_comp \"[string1]\" \"[string2]\"```\n\n**Compare strings with detailed distance output**\n\n```lev_comp -d \"[string1]\" \"[string2]\"```\n\n**Compare strings case-insensitively**\n\n```lev_comp -i \"[String1]\" \"[string2]\"```"],["SYNOPSIS","**lev_comp** [_options_] _string1_ _string2_"],["PARAMETERS","_STRING1_ _STRING2_\n> Strings to compare.\n\n**-d**\n> Show Levenshtein distance.\n\n**-i**\n> Case insensitive comparison.\n\n**--help**\n> Display help information."],["DESCRIPTION","**lev_comp** calculates the Levenshtein distance between two strings. The distance represents the minimum number of single-character edit operations (insertions, deletions, or substitutions) needed to transform one string into the other.\n\nA lower distance means the strings are more similar. A distance of zero means the strings are identical."],["CAVEATS","This is a niche utility and may not be available in standard package repositories. Only handles simple edit distance without support for weighted operations or transpositions."],["HISTORY","lev_comp implements **Levenshtein distance**, named after Vladimir Levenshtein who defined the metric in 1965."],["SEE ALSO","[diff](/man/diff)(1), [cmp](/man/cmp)(1)"]],"e":["diff","cmp"],"x":"lev_comp \"[string1]\" \"[string2]\""},"lure":{"n":"lure","t":"Community package manager for all Linux distros","s":[["TAGLINE","Community package manager for all Linux distros"],["TLDR","**Install** a package from LURE repos\n\n```lure install [package]```\n\n**Upgrade** all installed LURE packages\n\n```lure upgrade```\n\n**Show information** about a package\n\n```lure info [package]```\n\n**List** all packages in LURE repos\n\n```lure list```\n\n**List only installed** LURE packages\n\n```lure list --installed```\n\n**Add** a new repository\n\n```lure addrepo -n [name] -u [https://github.com/user/repo]```\n\n**Remove** a repository\n\n```lure removerepo -n [name]```\n\n**Refresh** all repositories\n\n```lure refresh```"],["SYNOPSIS","**lure** [_command_] [_options_]"],["PARAMETERS","**install**, **in**\n> Install a package from LURE repos. Packages not found in LURE are forwarded to the system package manager.\n\n**remove**, **rm**\n> Remove a package via the system package manager.\n\n**upgrade**, **up**\n> Upgrade installed LURE packages using rpmvercmp version comparison.\n\n**info**\n> Display package information from LURE repos. Supports wildcards (%).\n\n**list**, **ls**\n> List all packages in LURE repos. Accepts an optional pattern for filtering.\n\n**build**\n> Build a package from a lure.sh script in the current directory.\n\n**addrepo**, **ar**\n> Add a new repository. Requires **-n** (name) and **-u** (URL).\n\n**removerepo**, **rr**\n> Remove a repository and delete its contents. Requires **-n** (name).\n\n**refresh**, **ref**\n> Pull latest changes from all LURE repositories.\n\n**fix**\n> Delete and rebuild LURE's cache to resolve issues.\n\n**version**\n> Print the current LURE version.\n\n**-c**, **--clean**\n> Force a rebuild when installing or upgrading.\n\n**-I**, **--installed**\n> Show only installed packages when listing.\n\n**-s** _path_\n> Specify a custom lure.sh script path when building."],["DESCRIPTION","**LURE** (Linux User REpository) is a distro-agnostic package manager that brings AUR-like functionality to all Linux distributions. It reads **lure.sh** build scripts from git repositories, builds native packages (.deb, .rpm, .pkg.tar.zst, .apk), and installs them using the system's native package manager.\n\nLURE supports **apt**, **pacman**, **apk**, **dnf**, **yum**, and **zypper**. Packages are built from lure.sh scripts similar to Arch's PKGBUILD format. Repositories are standard git repos containing directories with lure.sh files that describe how to build each package."],["CONFIGURATION","LURE uses environment variables for targeting specific distributions and architectures:\n\n**LURE_DISTRO** — Target distribution (arch, alpine, opensuse, debian)\n**LURE_PKG_FORMAT** — Package format (archlinux, apk, rpm, deb)\n**LURE_ARM_VARIANT** — ARM variant (arm5, arm6, arm7)"],["CAVEATS","LURE is written in pure Go with zero runtime dependencies beyond a privilege elevation command (sudo, doas) and a supported package manager. Build scripts from community repositories are not officially vetted — always review lure.sh files before installing packages, similar to AUR PKGBUILD review practices."],["HISTORY","LURE was created to solve the problem of packaging software for multiple Linux distributions. Inspired by the **Arch User Repository** (AUR), it was designed to bring the same community-driven package building model to all major Linux distros. Written in **Go**, it was first released in **2022** and is actively maintained at **lure.sh**."],["INSTALL","```aur: yay -S lure```"],["SEE ALSO","[yay](/man/yay)(1), [pacman](/man/pacman)(8), [apt](/man/apt)(8), [dnf](/man/dnf)(8), [zypper](/man/zypper)(8)"]],"e":["yay","pacman","apt","dnf","zypper"],"x":"lure install [package]"},"mono":{"n":"mono","t":"open-source implementation of the ECMA Common Language Infrastructure","s":[["TAGLINE","open-source implementation of the ECMA Common Language Infrastructure"],["TLDR","**Run** a .NET assembly\n\n```mono [path/to/program.exe]```\n\nRun a .NET assembly in **debug mode**\n\n```mono --debug [path/to/program.exe]```\n\nRun with **specific runtime version**\n\n```mono --runtime=v4.0 [path/to/program.exe]```\n\n**Trace method calls** during execution\n\n```mono --trace [path/to/program.exe]```\n\nRun using the **interpreter** instead of JIT\n\n```mono --interpreter [path/to/program.exe]```\n\n**Pre-compile** assembly to native code (AOT)\n\n```mono --aot [path/to/assembly.dll]```"],["SYNOPSIS","**mono** [_options_] _file_ [_arguments_...]"],["PARAMETERS","**--debug[=_options_]**\n> Enable debugging mode; options include casts, mdb-optimizations, gdb\n\n**--aot[=_options_]**\n> Pre-compile CIL to native code; options: llvm, full, static, asmonly\n\n**--full-aot**\n> Run exclusively on pre-generated AOT code without runtime JIT\n\n**--interpreter**\n> Use interpreter instead of JIT compilation\n\n**--llvm**\n> Use LLVM backend for code generation when available\n\n**--gc=_engine_**\n> Select garbage collector: boehm or sgen\n\n**-O=_mode_**\n> Enable/disable optimizations (inline, sse2, tailc, abcrem, etc.)\n\n**--trace[=_expression_]**\n> Trace method invocations with optional filtering\n\n**--config _filename_**\n> Load custom configuration file\n\n**--runtime=_version_**\n> Override runtime version selection (e.g., v4.0)\n\n**--debugger-agent=_options_**\n> Configure remote debugging capabilities\n\n**--profile[=_profiler_]**\n> Enable profiling with specified profiler"],["DESCRIPTION","**Mono** is an open-source implementation of the ECMA Common Language Infrastructure (CLI) that executes .NET applications on Linux, macOS, and other platforms. It provides a runtime environment for C#, VB.NET, and other .NET languages.\n\nThe runtime loads assembly files (.exe or .dll) and compiles Common Intermediate Language (CIL) bytecode to native machine code using Just-in-Time (JIT) compilation. Ahead-of-Time (AOT) compilation is also supported for improved startup performance."],["CAVEATS","MONO_PATH environment variable should not be used by deployed applications as it breaks the assembly loader. AOT-generated code typically cannot be moved between computers due to CPU-specific optimizations. Some Windows-specific APIs may not be available or may behave differently."],["HISTORY","**Mono** was started by **Miguel de Icaza** and **Ximian** in **2001** as an open-source implementation of the .NET Framework. It was later sponsored by Novell, Xamarin, and Microsoft. The project enabled cross-platform .NET development before Microsoft's official .NET Core release."],["INSTALL","```apt: sudo apt install mono-runtime```\n\n```pacman: sudo pacman -S mono```\n\n```apk: sudo apk add mono```\n\n```brew: brew install mono```\n\n```nix: nix profile install nixpkgs#mono```"],["SEE ALSO","[mcs](/man/mcs)(1), [gacutil](/man/gacutil)(1), [dotnet](/man/dotnet)(1)"]],"e":["mcs","gacutil","dotnet"],"x":"mono [path/to/program.exe]"},"mq":{"n":"mq","t":"A jq-like command-line tool for querying and transforming Markdown files","s":[["TAGLINE","A jq-like command-line tool for querying and transforming Markdown files"],["TLDR","**Extract all headings** from a Markdown file\n\n```mq '.h' [path/to/file.md]```\n\n**Select a specific section** by heading text\n\n```mq 'select(\"Section Name\")' [path/to/file.md]```\n\n**Extract code blocks** of a specific language\n\n```mq '.code(\"python\")' [path/to/file.md]```\n\n**Convert Markdown to plain text**\n\n```mq 'to_text' [path/to/file.md]```\n\n**Output as HTML**\n\n```mq -F html '.h1' [path/to/file.md]```\n\n**Launch interactive REPL** mode\n\n```mq repl [path/to/file.md]```\n\n**Load query from a file**\n\n```mq -f [query.mq] [path/to/file.md]```\n\n**Aggregate multiple files** into a single array\n\n```mq -A '.h1' [*.md]```"],["SYNOPSIS","**mq** [_options_] [_query_] [_files..._]\n\n**mq** **repl** [_file_]\n\n**mq** **fmt** [_options_] [_file_]"],["PARAMETERS","**-A**, **--aggregate**\n> Combine all input files into a single array for processing.\n\n**-f** _file_, **--from-file** _file_\n> Load the query filter from a file instead of a command-line argument.\n\n**-I** _format_, **--input-format** _format_\n> Set input format: markdown, mdx, html, text, null, or raw.\n\n**-F** _format_, **--output-format** _format_\n> Set output format: markdown, html, text, json, or none.\n\n**-U**, **--update**\n> Update input files in-place with the query result.\n\n**-o** _file_, **--output** _file_\n> Write output to the specified file.\n\n**-C**, **--color-output**\n> Colorize Markdown output.\n\n**-S** _query_, **--separator** _query_\n> Query to insert between files when processing multiple inputs.\n\n**-L** _dir_, **--directory** _dir_\n> Search path for modules.\n\n**-M** _modules_, **--module-names** _modules_\n> Load specific modules (json, csv, yaml, toml, xml)."],["DESCRIPTION","**mq** is a command-line tool for processing and querying Markdown files, inspired by **jq** for JSON. It lets you slice, filter, map, and transform structured Markdown content directly from the terminal with built-in selectors (`.h1`-`.h6`, `.code`, `.link`, `.image`, `..` for recursive descent), functions (`select()`, `contains()`, `to_text()`, `to_link()`), and support for custom extensions.\n\nUseful for preparing structured prompts for language models, automating documentation updates, and analyzing content across multiple Markdown files. Extensible via custom subcommands placed in `~/.mq/bin/`."],["CAVEATS","Queries follow mq's own filter syntax similar to jq. Complex transformations may require chaining multiple filters with the pipe (`|`) operator."],["HISTORY","**mq** was created by **harehare** and is written in **Rust**."],["INSTALL","```brew: brew install mq```"],["SEE ALSO","[jq](/man/jq)(1), [glow](/man/glow)(1)"]],"e":["jq","glow"],"x":"mq '.h' [path/to/file.md]"},"ng-deploy":{"n":"ng-deploy","t":"Deploys an Angular application to a hosting provider","s":[["TAGLINE","Deploys an Angular application to a hosting provider"],["TLDR","**Deploy the default project**\n\n```ng deploy```\n\n**Deploy a specific project**\n\n```ng deploy [project-name]```\n\n**Deploy with a build configuration**\n\n```ng deploy --configuration production```\n\n**Deploy without building first**\n\n```ng deploy --no-build```"],["SYNOPSIS","**ng deploy** [_project_] [_options_]"],["PARAMETERS","_project_\n> The project to deploy. Defaults to the default project in the workspace.\n\n**--configuration** _name_\n> Build configuration to use for deployment.\n\n**--no-build**\n> Skip the build step before deploying.\n\n**--help**\n> Display help information."],["DESCRIPTION","**ng deploy** builds and deploys an Angular application to a remote hosting provider. The deploy command requires a deployment builder to be configured in the project's **angular.json** file, which is typically set up by adding a hosting provider package such as **@angular/fire**, **@azure/ng-deploy**, or **@netlify-builder/deploy**.\n\nThe command first runs the build process and then invokes the configured builder to upload the output to the target hosting environment."],["CONFIGURATION","A deploy builder must be added via **ng add** before using this command:\n\n```ng add @angular/fire```\n```ng add @netlify-builder/deploy```\n```ng add @azure/ng-deploy```\n\nThis registers the deploy target in **angular.json** under the project's architect section."],["CAVEATS","Requires a deploy builder package to be installed and configured. Without one, the command will fail with a \"no deploy target\" error. Authentication with the hosting provider is typically required. Part of Angular CLI."],["HISTORY","**ng deploy** was added to Angular CLI in **version 8.3** (**2019**), providing a standardized interface for deployment across different hosting platforms through pluggable builders."],["SEE ALSO","[ng](/man/ng)(1), [ng-build](/man/ng-build)(1), [firebase](/man/firebase)(1)"]],"e":["ng","ng-build","firebase"],"x":"ng deploy"},"nix-registry":{"n":"nix-registry","t":"manages the Nix flake registry","s":[["TAGLINE","manages the Nix flake registry"],["TLDR","**List registries**\n\n```nix registry list```\n\n**Pin flake to specific version**\n\n```nix registry pin [nixpkgs]```\n\n**Add registry entry**\n\n```nix registry add [myflake] [github:owner/repo]```\n\n**Remove registry entry**\n\n```nix registry remove [myflake]```"],["SYNOPSIS","**nix registry** _command_ [_options_]"],["PARAMETERS","**list**\n> List registry entries.\n\n**add** _name_ _flake_\n> Add flake alias.\n\n**remove** _name_\n> Remove alias.\n\n**pin** _name_\n> Pin flake to current version."],["DESCRIPTION","**nix registry** manages the Nix flake registry. The registry provides shorthand names for flake URLs. Enables pinning flakes to specific versions for reproducibility."],["INSTALL","```apt: sudo apt install nix-bin```\n\n```dnf: sudo dnf install nix```\n\n```pacman: sudo pacman -S nix```\n\n```apk: sudo apk add nix```\n\n```zypper: sudo zypper install nix```\n\n```nix: nix profile install nixpkgs#nix```"],["SEE ALSO","[nix](/man/nix)(1), [nix-flake](/man/nix-flake)(1)"]],"e":["nix","nix-flake"],"x":"nix registry list"},"noti":{"n":"noti","t":"triggers notifications when commands complete","s":[["TAGLINE","triggers notifications when commands complete"],["TLDR","**Notify when command completes**\n\n```noti [long-running-command]```\n\n**Notify with custom title**\n\n```noti -t \"[Build]\" [make]```\n\n**Notify with custom message**\n\n```noti -m \"[Done!]\" [command]```\n\n**Use specific service**\n\n```noti --slack [command]```\n\n**Pipe mode**\n\n```[command] | noti```\n\n**Banner notification**\n\n```noti -b [command]```\n\n**Watch a running process by PID**\n\n```noti -w [PID]```"],["SYNOPSIS","**noti** [_options_] [_command_]"],["PARAMETERS","_COMMAND_\n> Command to monitor.\n\n**-t** _TITLE_\n> Notification title.\n\n**-m** _MESSAGE_\n> Notification message.\n\n**-b**\n> Banner notification.\n\n**-s**\n> Speech notification (text-to-speech).\n\n**-w** _PID_\n> Watch a running process by PID and notify when it finishes.\n\n**--slack**\n> Notify via Slack.\n\n**--pushover**\n> Notify via Pushover.\n\n**--help**\n> Display help information."],["DESCRIPTION","**noti** triggers notifications when commands complete. Supports multiple services.\n\nThe tool alerts on completion. Works with Slack, Pushover, and native notifications."],["CAVEATS","Requires notification service. Cross-platform. Configuration for external services."],["HISTORY","noti was created to provide **command completion notifications** across multiple platforms and services."],["INSTALL","```brew: brew install noti```\n\n```nix: nix profile install nixpkgs#noti```"],["SEE ALSO","[notify-send](/man/notify-send)(1), [terminal-notifier](/man/terminal-notifier)(1)"]],"e":["notify-send","terminal-notifier"],"x":"noti [long-running-command]"},"npm-cache":{"n":"npm-cache","t":"manages the npm package cache","s":[["TAGLINE","manages the npm package cache"],["TLDR","**Clear npm cache**\n\n```npm cache clean --force```\n\n**Verify cache integrity**\n\n```npm cache verify```\n\n**Show cache location**\n\n```npm cache ls```"],["SYNOPSIS","**npm** **cache** _command_ [_options_]"],["PARAMETERS","**clean**\n> Delete cache folder contents.\n\n**verify**\n> Verify cache integrity.\n\n**ls**\n> List cache contents.\n\n**--force**\n> Required for clean command."],["DESCRIPTION","**npm cache** manages the npm package cache. The cache stores downloaded packages to speed up installations. Verify checks integrity and removes corrupt entries. Clean requires --force flag."],["INSTALL","```pacman: sudo pacman -S npm```\n\n```apk: sudo apk add npm```\n\n```brew: brew install npm```"],["SEE ALSO","[npm](/man/npm)(1), [npm-install](/man/npm-install)(1)"]],"e":["npm","npm-install"],"x":"npm cache clean --force"},"nvm.fish":{"n":"nvm.fish","t":"Node Version Manager for Fish shell","s":[["TAGLINE","Node Version Manager for Fish shell"],["TLDR","**Install** a specific Node.js **version**\n\n```nvm install [version]```\n\n**Install** the latest **LTS** version\n\n```nvm install lts```\n\n**Install** version from **.nvmrc** or **.node-version** file\n\n```nvm install```\n\n**Use** a specific installed version\n\n```nvm use [version]```\n\n**List installed** versions\n\n```nvm list```\n\n**List all available** remote versions\n\n```nvm list-remote```\n\n**Set default** version for new shells\n\n```set --universal nvm_default_version [version]```\n\n**Uninstall** a version\n\n```nvm uninstall [version]```"],["SYNOPSIS","**nvm** [_command_] [_version_]"],["PARAMETERS","**install** _VERSION_\n> Install a Node.js version. Accepts specific versions (v18.4.0), partial versions (v18), or aliases (latest, lts).\n\n**use** _VERSION_\n> Activate an installed Node.js version in the current environment.\n\n**list**\n> List installed Node.js versions, including system Node if present.\n\n**list-remote**\n> Show all available Node.js versions from the mirror.\n\n**current**\n> Show the currently active Node.js version.\n\n**uninstall** _VERSION_\n> Remove an installed Node.js version."],["CONFIGURATION","**$nvm_default_version**\n> Default Node.js version for new shell sessions. Set with `set --universal nvm_default_version VERSION`.\n\n**$nvm_default_packages**\n> Packages to auto-install with each new Node.js version. Set with `set --universal nvm_default_packages yarn np`.\n\n**$nvm_data**\n> Storage location for Node.js binaries. Defaults to `$XDG_DATA_HOME/nvm` (~/.local/share/nvm).\n\n**$nvm_mirror**\n> Node.js binary download mirror. Defaults to https://nodejs.org/dist."],["DESCRIPTION","**nvm.fish** is a pure Fish shell implementation of Node Version Manager, created by **jorgebucaran**. It provides a lightweight way to install, switch between, and manage multiple Node.js versions directly within the Fish shell.\n\nUnlike the original bash-based nvm, nvm.fish is written entirely in Fish with no external dependencies. It supports **.nvmrc** and **.node-version** files for automatic version switching per project, and is XDG Base Directory compliant.\n\nInstallation is typically done via **Fisher** (`fisher install jorgebucaran/nvm.fish`) or by manually copying the functions and completions."],["CAVEATS","Fish shell specific and not compatible with bash nvm or its configuration. The **nvm install** command activates the installed version only in the current environment; use **$nvm_default_version** to persist across sessions. Requires Fish 3.x or later."],["HISTORY","**nvm.fish** was created in **2016** by **jorgebucaran** as a native Fish shell implementation of Node Version Manager, inspired by the original **nvm.sh** by creationix and ljharb."],["SEE ALSO","[nvm](/man/nvm)(1), [fish](/man/fish)(1), [fnm](/man/fnm)(1), [node](/man/node)(1), [fisher](/man/fisher)(1)"]],"e":["nvm","fish","fnm","node","fisher"],"x":"nvm install [version]"},"openvpn3":{"n":"openvpn3","t":"next-generation OpenVPN client for Linux, providing a D-Bus based architecture","s":[["TAGLINE","next-generation OpenVPN client for Linux, providing a D-Bus based architecture"],["TLDR","**Start** a new VPN session\n\n```openvpn3 session-start -c [path/to/config.conf]```\n\n**List** established sessions\n\n```openvpn3 sessions-list```\n\n**Disconnect** the currently established session\n\n```openvpn3 session-manage -c [path/to/config.conf] -D```\n\n**Import** VPN configuration\n\n```openvpn3 config-import -c [path/to/config.conf]```\n\n**List** imported configurations\n\n```openvpn3 configs-list```"],["SYNOPSIS","**openvpn3** _command_ [_options_]"],["PARAMETERS","**session-start** **-c** _config_\n> Start a new VPN session using the specified configuration\n\n**session-manage** **-c** _config_ **-D**\n> Manage (disconnect) an active session\n\n**sessions-list**\n> List all active VPN sessions\n\n**config-import** **-c** _config_\n> Import a VPN configuration file into the configuration manager\n\n**configs-list**\n> List all imported configurations\n\n**config-manage** **-c** _config_\n> Manage imported configurations (rename, remove)\n\n**config-show** **-c** _config_\n> Display configuration details\n\n**log** **-c** _config_\n> Show session log output\n\n**-c**, **--config** _path_\n> Path to OpenVPN configuration file\n\n**-D**, **--disconnect**\n> Disconnect the session\n\n**-R**, **--restart**\n> Restart the session\n\n**-P**, **--pause**\n> Pause the session\n\n**--resume**\n> Resume a paused session"],["DESCRIPTION","**openvpn3** is the next-generation OpenVPN client for Linux, providing a D-Bus based architecture that allows non-root users to manage VPN connections. It separates the VPN configuration management from the session management, enabling better integration with desktop environments and system services.\n\nThe client supports importing and storing configurations persistently, managing multiple simultaneous VPN sessions, and provides granular access control through D-Bus policies. It is designed to integrate with NetworkManager and other Linux desktop components."],["CAVEATS","Requires the openvpn3-linux package and associated D-Bus services. Not all OpenVPN 2.x configuration directives are supported. Configuration import stores credentials in the configuration manager; review access policies for sensitive environments."],["HISTORY","Developed by **OpenVPN Inc.** as a modernized Linux client using the OpenVPN 3 Core library. Released to address limitations of the traditional OpenVPN 2.x client, particularly around privilege separation and desktop integration. The D-Bus architecture was introduced to allow unprivileged VPN management."],["INSTALL","```apt: sudo apt install openvpn3-client```\n\n```nix: nix profile install nixpkgs#openvpn3```"],["SEE ALSO","[openvpn](/man/openvpn)(8), [nmcli](/man/nmcli)(1), [networkctl](/man/networkctl)(1)"]],"e":["openvpn","nmcli","networkctl"],"x":"openvpn3 session-start -c [path/to/config.conf]"},"pdfinfo":{"n":"pdfinfo","t":"displays metadata and properties of PDF files","s":[["TAGLINE","displays metadata and properties of PDF files"],["TLDR","**Show PDF information**\n\n```pdfinfo [document.pdf]```\n\n**Show detailed metadata**\n\n```pdfinfo -meta [document.pdf]```\n\n**Show JavaScript info**\n\n```pdfinfo -js [document.pdf]```\n\n**Show structure info**\n\n```pdfinfo -struct [document.pdf]```\n\n**Show first page box dimensions**\n\n```pdfinfo -box [document.pdf]```\n\n**Show info from encrypted PDF**\n\n```pdfinfo -upw [password] [document.pdf]```"],["SYNOPSIS","**pdfinfo** [_-meta_] [_-box_] [_-js_] [_-struct_] [_options_] _file.pdf_"],["PARAMETERS","**-meta**\n> Show document metadata (XMP).\n\n**-box**\n> Show page box dimensions.\n\n**-js**\n> Show JavaScript.\n\n**-struct**\n> Show structure information.\n\n**-f** _PAGE_\n> First page for info.\n\n**-l** _PAGE_\n> Last page for info.\n\n**-enc** _ENCODING_\n> Text encoding.\n\n**-opw** _PASSWORD_\n> Owner password.\n\n**-upw** _PASSWORD_\n> User password.\n\n**-rawdates**\n> Show raw date strings.\n\n**-isodates**\n> Show ISO-8601 dates."],["OUTPUT FIELDS","**Title**\n> Document title.\n\n**Author**\n> Document author.\n\n**Creator**\n> Creating application.\n\n**Producer**\n> PDF producer.\n\n**CreationDate**\n> Creation date.\n\n**ModDate**\n> Modification date.\n\n**Pages**\n> Page count.\n\n**Page size**\n> Dimensions.\n\n**PDF version**\n> PDF specification version.\n\n**Encrypted**\n> Encryption status."],["DESCRIPTION","**pdfinfo** displays metadata and properties of PDF files. It extracts document information without viewing the actual content.\n\nBasic info includes title, author, creation date, page count, and dimensions. This helps identify and catalog PDF files.\n\nPage box information shows MediaBox, CropBox, and other box dimensions that affect printing and display.\n\nEncryption information reveals what permissions are set: printing, copying, modification. It also shows the encryption method.\n\nXMP metadata (-meta) contains extended information that applications embed. This may include copyright, keywords, and application-specific data.\n\nJavaScript detection (-js) reveals embedded scripts, which may be security concerns in untrusted PDFs."],["CAVEATS","Requires poppler-utils package. Some metadata may be inaccurate if PDF was poorly created. Encrypted PDFs need correct password."],["HISTORY","**pdfinfo** is part of **poppler-utils**, derived from the **Xpdf** project created by **Derek Noonburg**. These utilities became standard tools for PDF manipulation on Unix-like systems."],["INSTALL","```apt: sudo apt install poppler-utils```\n\n```dnf: sudo dnf install poppler-utils```\n\n```apk: sudo apk add poppler-utils```\n\n```nix: nix profile install nixpkgs#poppler-utils```"],["SEE ALSO","[pdffonts](/man/pdffonts)(1), [pdfimages](/man/pdfimages)(1), [pdftotext](/man/pdftotext)(1), [exiftool](/man/exiftool)(1)"]],"e":["pdffonts","pdfimages","pdftotext","exiftool"],"x":"pdfinfo [document.pdf]"},"pdfjoin":{"n":"pdfjoin","t":"joins PDF files","s":[["TAGLINE","joins PDF files"],["TLDR","**Join PDFs**\n\n```pdfjoin [file1.pdf] [file2.pdf]```\n\n**Join with output name**\n\n```pdfjoin -o [output.pdf] [file1.pdf] [file2.pdf]```\n\n**Join specific pages**\n\n```pdfjoin [file.pdf] [1-5] [file2.pdf] [2,4,6]```\n\n**Join all PDFs in directory**\n\n```pdfjoin [*.pdf]```\n\n**Rotated pages**\n\n```pdfjoin --rotateoversize false [file1.pdf] [file2.pdf]```"],["SYNOPSIS","**pdfjoin** [_-o output_] [_options_] _file1_ [_pages_] _file2_ [_pages_] ..."],["PARAMETERS","**-o** _FILE_\n> Output filename.\n\n**--rotateoversize** _BOOL_\n> Rotate oversized pages.\n\n**--paper** _SIZE_\n> Paper size (a4, letter).\n\n**--landscape**\n> Landscape orientation.\n\n**--tidy**\n> Remove temp files.\n\n**--**\n> End of options."],["DESCRIPTION","**pdfjoin** joins PDF files. It combines multiple PDFs into one.\n\nPart of pdfjam utilities. Uses LaTeX pdfpages package.\n\nPage selection supported. Specify ranges or individual pages.\n\nPreserves PDF quality. No recompression."],["CAVEATS","Requires LaTeX installation. Part of pdfjam package. Large files may be slow."],["HISTORY","**pdfjoin** is part of **pdfjam**, a collection of shell scripts for PDF manipulation using LaTeX."],["SEE ALSO","[pdfjam](/man/pdfjam)(1), [pdfunite](/man/pdfunite)(1), [gs](/man/gs)(1)"]],"e":["pdfjam","pdfunite","gs"],"x":"pdfjoin [file1.pdf] [file2.pdf]"},"pgmcrater":{"n":"pgmcrater","t":"generates synthetic terrain with impact craters as grayscale heightmap","s":[["TAGLINE","generates synthetic terrain with impact craters as grayscale heightmap"],["TLDR","**Generate crater terrain**\n\n```pgmcrater -number [5000] > [output.pgm]```\n\n**Specify dimensions**\n\n```pgmcrater -width [512] -height [512] -number [1000] > [output.pgm]```"],["SYNOPSIS","**pgmcrater** [_options_]"],["PARAMETERS","**-width** _n_, **-xsize** _n_\n> Output width in pixels (default 256).\n\n**-height** _n_, **-ysize** _n_\n> Output height in pixels (default 256).\n\n**-number** _n_\n> Number of craters to generate.\n\n**-gamma** _g_\n> Gamma correction value applied to the shaded relief output.\n\n**-randomseed** _n_\n> Seed for the random number generator; use a fixed value for reproducible output."],["DESCRIPTION","**pgmcrater** generates synthetic terrain populated with impact craters and outputs it as a grayscale PGM heightmap rendered via shaded relief. It is useful for creating planetary surface textures and procedural terrain.\n\nAs of Netpbm 10.27 **pgmcrater** is obsolete; it is now a thin wrapper that pipes **pamcrater** into **pamshadedrelief** and **pamtopnm**. Prefer those primitives for new workflows."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm-extras```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pamcrater](/man/pamcrater)(1), [pgmnoise](/man/pgmnoise)(1)"]],"e":["pamcrater","pgmnoise"],"x":"pgmcrater -number [5000] > [output.pgm]"},"pip-list":{"n":"pip-list","t":"List installed Python packages with versions","s":[["TAGLINE","List installed Python packages with versions"],["TLDR","**List installed packages**\n\n```pip list```\n\n**List outdated packages**\n\n```pip list --outdated```\n\n**List in JSON format**\n\n```pip list --format json```\n\n**List user packages only**\n\n```pip list --user```\n\n**List editable packages**\n\n```pip list --editable```\n\n**List packages not required by others**\n\n```pip list --not-required```\n\n**Exclude a package from output**\n\n```pip list --exclude [pip] --exclude [setuptools]```"],["SYNOPSIS","**pip** **list** [_options_]"],["PARAMETERS","**-o**, **--outdated**\n> Show only outdated packages.\n\n**-u**, **--uptodate**\n> Show only up-to-date packages.\n\n**-e**, **--editable**\n> Show only editable packages.\n\n**-l**, **--local**\n> Show only virtualenv packages.\n\n**--user**\n> Show only user packages.\n\n**--format** _format_\n> Output format (columns, freeze, json).\n\n**--not-required**\n> Show packages not required by others.\n\n**--exclude** _pkg_\n> Exclude specified package from output (can be repeated).\n\n**--path** _dir_\n> Restrict to packages in specified installation path.\n\n**--exclude-editable**\n> Exclude editable packages from output.\n\n**--include-editable**\n> Include editable packages in output."],["DESCRIPTION","**pip list** displays installed Python packages with their versions. Can filter by outdated, up-to-date, or editable packages. Supports multiple output formats including JSON for scripting."],["INSTALL","```apk: sudo apk add py3-pip```"],["SEE ALSO","[pip](/man/pip)(1), [pip-freeze](/man/pip-freeze)(1), [pip-show](/man/pip-show)(1)"]],"e":["pip","pip-freeze","pip-show"],"x":"pip list"},"plink":{"n":"plink","t":"PuTTY command-line SSH connection tool","s":[["TAGLINE","PuTTY command-line SSH connection tool"],["TLDR","**Connect to SSH server**\n\n```plink [user]@[host]```\n\n**Execute remote command**\n\n```plink [user]@[host] [command]```\n\n**Use specific port**\n\n```plink -P [port] [user]@[host]```\n\n**Use private key**\n\n```plink -i [key.ppk] [user]@[host]```\n\n**SSH tunnel**\n\n```plink -L [local_port]:[remote_host]:[remote_port] [user]@[host]```"],["SYNOPSIS","**plink** [_options_] [_user_@]_host_ [_command_]"],["PARAMETERS","_HOST_\n> Remote host (optionally prefixed with _user_@).\n\n**-ssh**, **-telnet**, **-rlogin**, **-raw**, **-serial**\n> Force use of a specific protocol.\n\n**-P** _PORT_\n> Connect to the specified port.\n\n**-l** _USER_\n> Login username.\n\n**-pw** _PASSWORD_\n> Login with the given password (insecure; prefer -pwfile).\n\n**-pwfile** _FILE_\n> Read password from file.\n\n**-i** _KEY_\n> Private key file for authentication (.ppk format).\n\n**-load** _SESSION_\n> Load settings from a saved PuTTY session.\n\n**-L** [_bindaddr_:]_port_:_host_:_hostport_\n> Forward local port to remote destination.\n\n**-R** [_bindaddr_:]_port_:_host_:_hostport_\n> Forward remote port to local destination.\n\n**-D** [_bindaddr_:]_port_\n> Dynamic SOCKS-based local port forwarding.\n\n**-N**\n> Don't start a shell or command (SSH-2 only).\n\n**-T**, **-t**\n> Disable/enable pseudo-terminal allocation.\n\n**-A**, **-a**\n> Enable/disable agent forwarding.\n\n**-X**, **-x**\n> Enable/disable X11 forwarding.\n\n**-m** _FILE_\n> Read remote command(s) from file.\n\n**-batch**\n> Disable all interactive prompts.\n\n**-v**\n> Verbose output.\n\n**--help**\n> Display help."],["DESCRIPTION","**plink** is the command-line SSH connection tool from the PuTTY suite. It provides non-interactive SSH access for executing remote commands, port forwarding, and scripted SSH operations.\n\nUnlike the interactive PuTTY terminal, plink is designed for automation and batch operations. It supports SSH key authentication using PuTTY's .ppk key format. The **-batch** flag disables all interactive prompts, making it suitable for scripts and cron jobs."],["CAVEATS","Part of PuTTY. Uses PPK key format. Windows/Unix versions."],["HISTORY","Plink is part of **PuTTY** for command-line SSH operations."],["INSTALL","```apt: sudo apt install putty-tools```\n\n```dnf: sudo dnf install putty```\n\n```pacman: sudo pacman -S putty```\n\n```apk: sudo apk add putty```\n\n```zypper: sudo zypper install putty```\n\n```brew: brew install putty```\n\n```nix: nix profile install nixpkgs#putty```"],["SEE ALSO","[ssh](/man/ssh)(1), [putty](/man/putty)(1), [pscp](/man/pscp)(1)"]],"e":["ssh","putty","pscp"],"x":"plink [user]@[host]"},"pnmnorm":{"n":"pnmnorm","t":"Normalize contrast in PNM images","s":[["TAGLINE","Normalize contrast in PNM images"],["TLDR","**Normalize image contrast**\n\n```pnmnorm [input.pnm] > [output.pnm]```\n\n**Specify brightness range**\n\n```pnmnorm -bpercent [5] -wpercent [5] [input.pnm] > [output.pnm]```\n\n**Preserve midpoint**\n\n```pnmnorm -midvalue [127] [input.pnm] > [output.pnm]```"],["SYNOPSIS","**pnmnorm** [_options_] [_file_]"],["PARAMETERS","_FILE_\n> Input PNM file.\n\n**-bpercent** _N_\n> Black point percentage.\n\n**-wpercent** _N_\n> White point percentage.\n\n**-bvalue** _N_\n> Black cutoff value.\n\n**-wvalue** _N_\n> White cutoff value.\n\n**-midvalue** _N_\n> Middle gray value to preserve."],["DESCRIPTION","**pnmnorm** normalizes the contrast of PNM images by stretching the histogram to use the full dynamic range. It finds the darkest and brightest pixel values and linearly maps them to the minimum and maximum possible values.\n\nThe **-bpercent** and **-wpercent** options control how aggressively the black and white points are clipped, ignoring a percentage of outlier pixels at each end. The **-midvalue** option preserves a specific middle gray value during the normalization. Part of the Netpbm toolkit."],["CAVEATS","May clip extreme values. Part of Netpbm suite."],["HISTORY","pnmnorm was created as part of **Netpbm** for automatic contrast enhancement."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pnmhisteq](/man/pnmhisteq)(1), [pnmgamma](/man/pnmgamma)(1), [ppmnorm](/man/ppmnorm)(1)"]],"e":["pnmhisteq","pnmgamma","ppmnorm"],"x":"pnmnorm [input.pnm] > [output.pnm]"},"pnmtotiffcmyk":{"n":"pnmtotiffcmyk","t":"Convert PNM to CMYK TIFF for printing","s":[["TAGLINE","Convert PNM to CMYK TIFF for printing"],["TLDR","**Convert PNM to CMYK TIFF**\n\n```pnmtotiffcmyk [input.pnm] > [output.tiff]```\n\n**Specify UCR percentage**\n\n```pnmtotiffcmyk -ucr [20] [input.pnm] > [output.tiff]```"],["SYNOPSIS","**pnmtotiffcmyk** [_options_] [_file_]"],["PARAMETERS","**-ucr** _percent_\n> Under color removal percentage.\n\n**-gcr** _percent_\n> Gray component replacement."],["DESCRIPTION","**pnmtotiffcmyk** converts PNM images to CMYK (Cyan, Magenta, Yellow, Key/Black) TIFF format for professional print production. It performs the RGB to CMYK color space conversion required by commercial printing processes.\n\nThe **-ucr** (Under Color Removal) and **-gcr** (Gray Component Replacement) options control how the black channel is generated from the CMY components, affecting ink usage and print quality. Part of the Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pamtotiff](/man/pamtotiff)(1), [pnmtotiff](/man/pnmtotiff)(1)"]],"e":["pamtotiff","pnmtotiff"],"x":"pnmtotiffcmyk [input.pnm] > [output.tiff]"},"ppmwheel":{"n":"ppmwheel","t":"Generate HSV color wheel image","s":[["TAGLINE","Generate HSV color wheel image"],["TLDR","**Generate color wheel**\n\n```ppmwheel [diameter] > [output.ppm]```\n\n**Create 200px color wheel**\n\n```ppmwheel 200 > [wheel.ppm]```"],["SYNOPSIS","**ppmwheel** _diameter_"],["PARAMETERS","_DIAMETER_\n> Wheel diameter in pixels."],["DESCRIPTION","**ppmwheel** generates a PPM image of an HSV color wheel with the specified diameter in pixels. The output is a square image with the color wheel centered. Hue varies around the circumference, saturation varies from center to edge, and value is constant at maximum.\n\nPart of the Netpbm suite of image manipulation tools."],["CAVEATS","Output is always a square PPM image. Pipe through other Netpbm tools or ImageMagick to convert to other formats."],["HISTORY","ppmwheel was created as part of **Netpbm** for color wheel generation."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[ppm](/man/ppm)(5), [ppmrainbow](/man/ppmrainbow)(1)"]],"e":["ppm","ppmrainbow"],"x":"ppmwheel [diameter] > [output.ppm]"},"pptpsetup":{"n":"pptpsetup","t":"Configure and manage PPTP VPN tunnels","s":[["TAGLINE","Configure and manage PPTP VPN tunnels"],["TLDR","**Create PPTP tunnel**\n\n```pptpsetup --create [tunnel] --server [host] --username [user] --password [pass]```\n\n**Delete tunnel**\n\n```pptpsetup --delete [tunnel]```\n\n**Create with encryption**\n\n```pptpsetup --create [tunnel] --server [host] --username [user] --password [pass] --encrypt```"],["SYNOPSIS","**pptpsetup** [_options_]"],["PARAMETERS","**--create** _NAME_\n> Create tunnel.\n\n**--delete** _NAME_\n> Delete tunnel.\n\n**--server** _HOST_\n> Server address.\n\n**--username** _USER_\n> Authentication username.\n\n**--password** _PASS_\n> Authentication password.\n\n**--encrypt**\n> Enable encryption."],["DESCRIPTION","**pptpsetup** is a helper script that simplifies creating and managing PPTP VPN tunnel configurations. It generates the necessary peer configuration files in **/etc/ppp/peers/** and stores authentication credentials in the appropriate secrets files.\n\nThe **--encrypt** option enables MPPE encryption for the tunnel. Tunnels can be started with **pon** and stopped with **poff** using the tunnel name created by pptpsetup."],["CAVEATS","PPTP is deprecated. Creates files in /etc/ppp/."],["HISTORY","pptpsetup provides **PPTP tunnel configuration** management."],["INSTALL","```apt: sudo apt install pptp-linux```"],["SEE ALSO","[pptp](/man/pptp)(8), [pppd](/man/pppd)(8), [pon](/man/pon)(1)"]],"e":["pptp","pppd","pon"],"x":"pptpsetup --create [tunnel] --server [host] --username [user] --password [pass]"},"pvscan":{"n":"pvscan","t":"Scan and discover LVM physical volumes","s":[["TAGLINE","Scan and discover LVM physical volumes"],["TLDR","**List** all physical volumes\n\n```sudo pvscan```\n\nShow **volume group** using a PV\n\n```sudo pvscan --cache --listvg [/dev/sdX]```\n\nShow **logical volumes** using a PV\n\n```sudo pvscan --cache --listlvs [/dev/sdX]```\n\nOutput in **JSON** format\n\n```sudo pvscan --reportformat json```"],["SYNOPSIS","**pvscan** [**--cache**] [**--listvg**] [**--listlvs**] [**--reportformat** _format_] [_device_...]"],["PARAMETERS","**--cache**\n> Update LVM metadata cache\n\n**--listvg**\n> List volume group of specified device\n\n**--listlvs**\n> List logical volumes on specified device\n\n**--reportformat _format_**\n> Output format (basic, json)\n\n**-v, --verbose**\n> Verbose output"],["DESCRIPTION","**pvscan** scans for LVM physical volumes and lists them. It can update the LVM metadata cache and report which volume groups and logical volumes are present on specific devices.\n\nThe command is useful for discovering physical volumes after adding new disks and for troubleshooting LVM configuration issues."],["CAVEATS","Requires root privileges. Results depend on device availability. Cache updates may be needed after hardware changes. Use with --cache for udev integration."],["HISTORY","**pvscan** is part of **LVM2** (Logical Volume Manager). It provides physical volume discovery functionality essential for dynamic LVM management."],["INSTALL","```apt: sudo apt install lvm2```\n\n```dnf: sudo dnf install lvm2```\n\n```pacman: sudo pacman -S lvm2```\n\n```apk: sudo apk add lvm2```\n\n```zypper: sudo zypper install lvm2```\n\n```nix: nix profile install nixpkgs#lvm2```"],["SEE ALSO","[pvs](/man/pvs)(8), [pvdisplay](/man/pvdisplay)(8), [vgscan](/man/vgscan)(8), [lvscan](/man/lvscan)(8)"]],"e":["pvs","pvdisplay","vgscan","lvscan"],"x":"sudo pvscan"},"pw-play":{"n":"pw-play","t":"Play audio files through PipeWire","s":[["TAGLINE","Play audio files through PipeWire"],["TLDR","**Play a WAV file**\n\n```pw-play [path/to/file.wav]```\n\n**Play a file at a specific volume**\n\n```pw-play --volume [0.8] [path/to/file.wav]```\n\n**Play with a custom sample rate**\n\n```pw-play --rate [48000] [path/to/file.wav]```\n\n**Play to a specific target node**\n\n```pw-play --target [node_name] [path/to/file.wav]```\n\n**Play with specific resampler quality**\n\n```pw-play -q [8] [path/to/file.wav]```"],["SYNOPSIS","**pw-play** [_options_] _file_"],["PARAMETERS","**--volume** _level_\n> Playback volume (1.0 = 100%).\n\n**--rate** _hz_\n> Sample rate in Hz.\n\n**--channels** _num_\n> Number of audio channels.\n\n**-q**, **--quality** _level_\n> Resampler quality (0-15, default 4).\n\n**--target** _target_\n> Target PipeWire node to connect to.\n\n**--channel-map** _map_\n> Channel map to use (e.g., FL,FR for stereo).\n\n**--format** _format_\n> Sample format (e.g., s16, s32, f32, f64).\n\n**--latency** _latency_\n> Latency in milliseconds."],["DESCRIPTION","**pw-play** is a convenience wrapper for **pw-cat --playback**, used to play audio files through PipeWire. It supports common audio formats including WAV, FLAC, and Ogg.\n\nAll options available to **pw-cat** in playback mode also apply to **pw-play**. See **pw-cat** for full documentation."],["INSTALL","```apt: sudo apt install pipewire-bin```\n\n```apk: sudo apk add pipewire-tools```\n\n```zypper: sudo zypper install pipewire-tools```"],["SEE ALSO","[pw-cat](/man/pw-cat)(1), [pw-record](/man/pw-record)(1), [pw-cli](/man/pw-cli)(1), [pipewire](/man/pipewire)(1)"]],"e":["pw-cat","pw-record","pw-cli","pipewire"],"x":"pw-play [path/to/file.wav]"},"rpm":{"n":"rpm","t":"RPM package manager for Red Hat systems","s":[["TAGLINE","RPM package manager for Red Hat systems"],["TLDR","**Query** package version\n\n```rpm -q [package]```\n\nList **all** matching packages\n\n```rpm -qa '[pattern*]'```\n\n**Forcibly install** package\n\n```rpm -U [package.rpm] --force```\n\nFind **file owner**\n\n```rpm -qf [/path/to/file]```\n\nList **package files**\n\n```rpm -ql [package]```\n\nShow package **scripts**\n\n```rpm -qp --scripts [package.rpm]```\n\n**Verify** packages\n\n```rpm -Va '[pattern*]'```\n\nShow **changelog**\n\n```rpm -q --changelog [package]```"],["SYNOPSIS","**rpm** [**-q**|**-i**|**-U**|**-e**|**-V**] [_options_] [_package_]"],["MODES","**-q, --query**\n> Query installed packages\n\n**-i, --install**\n> Install package\n\n**-U, --upgrade**\n> Upgrade or install package\n\n**-e, --erase**\n> Remove package\n\n**-V, --verify**\n> Verify package integrity"],["PARAMETERS","**-a, --all**\n> All packages\n\n**-f, --file _file_**\n> Query package owning file\n\n**-l, --list**\n> List files in package\n\n**-p, --package _file_**\n> Query package file\n\n**--force**\n> Force operation\n\n**--nodeps**\n> Ignore dependencies\n\n**--scripts**\n> Show scriptlets"],["DESCRIPTION","**rpm** is the RPM Package Manager for Red Hat-based Linux distributions. It installs, upgrades, removes, and queries software packages in RPM format.\n\nThe tool maintains a database of installed packages, handles dependencies, and can verify package integrity. It's the low-level package tool; dnf/yum provide higher-level functionality."],["CAVEATS","Does not resolve dependencies automatically (use dnf/yum). Package signatures should be verified. Force and nodeps options can break systems. Root required for install/remove."],["HISTORY","**RPM** was originally developed at Red Hat by Marc Ewing and Erik Troan. It became the standard package format for many Linux distributions including Fedora, RHEL, CentOS, and SUSE."],["INSTALL","```apt: sudo apt install rpm```\n\n```dnf: sudo dnf install rpm```\n\n```apk: sudo apk add rpm```\n\n```zypper: sudo zypper install rpm```\n\n```brew: brew install rpm```\n\n```nix: nix profile install nixpkgs#rpm```"],["SEE ALSO","[dnf](/man/dnf)(8), [yum](/man/yum)(8), [rpm2cpio](/man/rpm2cpio)(8), [rpmbuild](/man/rpmbuild)(8)"]],"e":["dnf","yum","rpm2cpio","rpmbuild"],"x":"rpm -q [package]"},"rtcwake":{"n":"rtcwake","t":"Schedule system wake from sleep using RTC alarm","s":[["TAGLINE","Schedule system wake from sleep using RTC alarm"],["TLDR","**Show** alarm status\n\n```sudo rtcwake -m show -v```\n\nSuspend to **RAM** and wake after 10 seconds\n\n```sudo rtcwake -m mem -s 10```\n\nSuspend to **disk** and wake in 15 minutes\n\n```sudo rtcwake -m disk --date +15min```\n\n**Freeze** and wake at specific time\n\n```sudo rtcwake -m freeze --date [YYYYMMDDhhmm]```\n\n**Disable** alarm\n\n```sudo rtcwake -m disable```\n\n**Dry run** without actually suspending\n\n```sudo rtcwake -m mem -s 600 -n```\n\n**List supported wake modes** for this kernel\n\n```rtcwake --list-modes```"],["SYNOPSIS","**rtcwake** [_options_] [**-d** _device_] [**-m** _mode_] {**-s** _seconds_ | **-t** _time_t_ | **--date** _timestamp_}"],["MODES","**standby**\n> ACPI S1 — minimal but real power savings. Default mode if **-m** is omitted.\n\n**mem**\n> Suspend to RAM (ACPI S3).\n\n**disk**\n> Suspend to disk / hibernate (ACPI S4).\n\n**freeze**\n> Suspend-to-idle (S0ix), kernel 3.9+.\n\n**off**\n> Power off the system (ACPI S5).\n\n**no**\n> Set the RTC alarm but do not suspend or shut down.\n\n**on**\n> Do not sleep; instead poll the RTC until the alarm fires (debugging).\n\n**show**\n> Display the currently set alarm.\n\n**disable**\n> Cancel a pending alarm."],["PARAMETERS","**-m**, **--mode** _mode_\n> Sleep / action mode (see **MODES**).\n\n**-s**, **--seconds** _sec_\n> Wake _sec_ seconds from now.\n\n**-t**, **--time** _time_t_\n> Wake at the absolute Unix epoch time _time_t_.\n\n**--date** _timestamp_\n> Wake at the given timestamp (e.g. `+5min`, `2026-04-30 18:00`, `tomorrow 03:00`).\n\n**-d**, **--device** _rtc_\n> Use the specified RTC device (default **/dev/rtc0**).\n\n**-l**, **--local**\n> Treat the hardware clock as local time.\n\n**-u**, **--utc**\n> Treat the hardware clock as UTC.\n\n**-a**, **--auto**\n> Read the clock interpretation from **/etc/adjtime** (default).\n\n**-A**, **--adjfile** _file_\n> Use _file_ instead of **/etc/adjtime**.\n\n**-n**, **--dry-run**\n> Test mode — set up the alarm but do not actually suspend or shut down.\n\n**--list-modes**\n> Print modes the running kernel supports.\n\n**-v**, **--verbose**\n> Verbose output.\n\n**-V**, **--version**\n> Print version and exit."],["DESCRIPTION","**rtcwake** enters a system sleep state and configures the RTC (Real-Time Clock) to wake the system at a specified time. It's useful for scheduled tasks, power saving, and automatic system wake.\n\nThe tool supports various sleep modes with different power/wake trade-offs. The RTC alarm persists even if the system loses power (battery backup)."],["CAVEATS","Requires root privileges. Not all hardware supports all modes. Time is relative to hardware clock (usually UTC). BIOS/UEFI must support RTC wake."],["HISTORY","**rtcwake** is part of **util-linux**, providing a standard interface to the Linux RTC wake functionality used for scheduled wake-ups and automated power management."],["INSTALL","```apt: sudo apt install util-linux```\n\n```dnf: sudo dnf install util-linux```\n\n```pacman: sudo pacman -S util-linux```\n\n```apk: sudo apk add util-linux-misc```\n\n```zypper: sudo zypper install util-linux```\n\n```brew: brew install util-linux```\n\n```nix: nix profile install nixpkgs#util-linux```"],["SEE ALSO","[systemctl](/man/systemctl)(1), [hwclock](/man/hwclock)(8), [pm-suspend](/man/pm-suspend)(8)"]],"e":["systemctl","hwclock","pm-suspend"],"x":"sudo rtcwake -m show -v"},"scan-build":{"n":"scan-build","t":"Clang static analyzer build integration tool","s":[["TAGLINE","Clang static analyzer build integration tool"],["TLDR","**Analyze build**\n\n```scan-build make```\n\n**Analyze with output directory**\n\n```scan-build -o [reports/] make```\n\n**Use specific checkers**\n\n```scan-build -enable-checker [alpha.security] make```\n\n**Open results in browser**\n\n```scan-build --view make```\n\n**With CMake**\n\n```scan-build cmake --build [build/]```\n\n**List available checkers**\n\n```scan-build --list-checkers```\n\n**Verbose output**\n\n```scan-build -v make```"],["SYNOPSIS","**scan-build** [_-o dir_] [_--view_] [_options_] _build-command_"],["PARAMETERS","**-o** _DIR_\n> Output directory.\n\n**--view**\n> Open results in browser.\n\n**-v**\n> Verbose output.\n\n**-enable-checker** _CHECKER_\n> Enable checker.\n\n**-disable-checker** _CHECKER_\n> Disable checker.\n\n**--list-checkers**\n> Show available checkers.\n\n**--status-bugs**\n> Exit non-zero if bugs found.\n\n**-plist**\n> Generate plist files."],["DESCRIPTION","**scan-build** wraps build commands to run Clang's static analyzer on each compiled source file. It intercepts compilation, analyzing C, C++, and Objective-C code for bugs including null pointer dereferences, memory leaks, use-after-free errors, and API misuse without executing the code.\n\nAnalysis results are presented as HTML reports with interactive path visualizations showing the exact sequence of events leading to each bug. Additional checker categories can be enabled with **-enable-checker** for deeper analysis including security vulnerabilities and experimental checks. The **--status-bugs** flag returns a non-zero exit code when bugs are found, useful for CI integration."],["CAVEATS","Increases build time significantly. False positives require tuning. C/C++ and Objective-C only."],["HISTORY","**scan-build** is part of the **Clang** project. It provides an accessible interface to Clang's static analysis capabilities."],["INSTALL","```apt: sudo apt install clang-tools```\n\n```apk: sudo apk add clang22-analyzer```\n\n```zypper: sudo zypper install clang-tools```\n\n```nix: nix profile install nixpkgs#clang-tools```"],["SEE ALSO","[clang](/man/clang)(1), [cppcheck](/man/cppcheck)(1), [make](/man/make)(1)"]],"e":["clang","cppcheck","make"],"x":"scan-build make"},"scrapy":{"n":"scrapy","t":"Python web scraping and crawling framework","s":[["TAGLINE","Python web scraping and crawling framework"],["TLDR","**Create new project**\n\n```scrapy startproject [project_name]```\n\n**Generate spider**\n\n```scrapy genspider [spider_name] [domain.com]```\n\n**Run spider**\n\n```scrapy crawl [spider_name]```\n\n**Run spider and save to file**\n\n```scrapy crawl [spider_name] -o [output.json]```\n\n**Interactive shell for testing**\n\n```scrapy shell \"[https://example.com]\"```\n\n**Check spider contracts**\n\n```scrapy check [spider_name]```\n\n**List available spiders**\n\n```scrapy list```\n\n**Fetch URL and show response**\n\n```scrapy fetch [https://example.com]```"],["SYNOPSIS","**scrapy** _command_ [_-o output_] [_-s setting=value_] [_options_] [_arguments_]"],["PARAMETERS","**startproject** _NAME_\n> Create new Scrapy project.\n\n**genspider** _NAME_ _DOMAIN_\n> Generate spider from template.\n\n**crawl** _SPIDER_\n> Run a spider.\n\n**shell** [_URL_]\n> Interactive shell for testing.\n\n**list**\n> List available spiders.\n\n**check** [_SPIDER_]\n> Run contract checks.\n\n**fetch** _URL_\n> Fetch URL and print.\n\n**view** _URL_\n> Open URL in browser.\n\n**parse** _URL_\n> Parse URL with spider.\n\n**runspider** _FILE_\n> Run spider from file.\n\n**-o** _FILE_, **--output** _FILE_\n> Append crawled items to a file. Format is inferred from the extension (json, jsonl, csv, xml).\n\n**-O** _FILE_, **--overwrite-output** _FILE_\n> Same as **-o** but overwrites any existing file.\n\n**-s** _NAME=VALUE_, **--set** _NAME=VALUE_\n> Override a setting (e.g. **-s LOG_LEVEL=INFO**).\n\n**-a** _NAME=VALUE_\n> Pass an argument to the spider (read via **self.<NAME>**).\n\n**-t** _FORMAT_, **--output-format** _FORMAT_\n> Explicitly set the output format when the filename does not indicate it.\n\n**--nolog**\n> Disable logging entirely.\n\n**--loglevel** _LEVEL_, **-L** _LEVEL_\n> Set log level: DEBUG, INFO, WARNING, ERROR, CRITICAL.\n\n**--logfile** _FILE_\n> Write log output to a file.\n\n**--profile** _FILE_\n> Write Python cProfile stats to file."],["DESCRIPTION","**Scrapy** is a Python framework for web scraping and crawling. It handles requests, parsing, and data extraction with built-in support for following links, handling cookies, and respecting robots.txt.\n\nProjects contain spiders - classes that define how to scrape sites. Spiders specify start URLs, parse response with CSS/XPath selectors, and yield items or further requests.\n\nThe shell command provides interactive testing. You can experiment with selectors on live pages before writing spider code. Response object methods match spider context.\n\nItems define the data structure being scraped. Item pipelines process scraped data: validation, cleaning, storage to databases or files. Multiple output formats are supported.\n\nMiddleware handles request/response processing: user agents, proxies, retries, cookies. Settings control behavior: concurrency, delays, download timeouts, and more.\n\nExtensions add functionality: stats collection, throttling, autothrottle, and custom callbacks."],["CAVEATS","JavaScript-rendered content requires Splash or Selenium integration. Some sites block scrapers via rate limiting or CAPTCHAs. Aggressive scraping may violate ToS. Robots.txt should be respected. Debug shell doesn't persist state."],["HISTORY","**Scrapy** was created by **Pablo Hoffman** and **Shane Evans** at Insophia around **2008**. It grew from internal tools to a general-purpose framework. The project became one of the most popular Python scraping tools, with commercial company Scrapinghub (now Zyte) providing support and services."],["INSTALL","```pacman: sudo pacman -S scrapy```\n\n```brew: brew install scrapy```"],["SEE ALSO","[curl](/man/curl)(1), [wget](/man/wget)(1), [beautifulsoup](/man/beautifulsoup)(1), [playwright](/man/playwright)(1)"]],"e":["curl","wget","beautifulsoup","playwright"],"x":"scrapy startproject [project_name]"},"semver":{"n":"semver","t":"Validate and manipulate semantic version strings","s":[["TAGLINE","Validate and manipulate semantic version strings"],["TLDR","**Check if version is valid**\n\n```semver [1.2.3]```\n\n**Increment major version**\n\n```semver -i major [1.2.3]```\n\n**Increment minor version**\n\n```semver -i minor [1.2.3]```\n\n**Increment patch version**\n\n```semver -i patch [1.2.3]```\n\n**Compare versions**\n\n```semver -r \"[>=1.0.0 <2.0.0]\" [1.5.0]```\n\n**Coerce loose version string**\n\n```semver -c [v1.2]```\n\n**Increment prerelease with identifier**\n\n```semver -i prerelease --preid [beta] [1.2.3]```\n\n**Sort versions**\n\n```semver [1.2.3] [1.0.0] [2.0.0]```"],["SYNOPSIS","**semver** [_options_] _version_ [_version_ ...]"],["PARAMETERS","**-i**, **--increment** _TYPE_\n> Increment version by specified level. Default level is patch.\n\n**-r**, **--range** _RANGE_\n> Print versions that match the specified range.\n\n**-c**, **--coerce**\n> Coerce a string into a valid semver if possible.\n\n**-l**, **--loose**\n> Interpret versions and ranges loosely.\n\n**-p**, **--include-prerelease**\n> Always include prerelease versions in range matching.\n\n**--preid** _IDENTIFIER_\n> Identifier for prerelease version increments.\n\n**-n** _BASE_\n> Base number (0 or 1) for the prerelease identifier.\n\n**--rtl**\n> Coerce version strings right to left.\n\n**--ltr**\n> Coerce version strings left to right (default)."],["INCREMENT TYPES","**major** - 1.2.3 -> 2.0.0\n**minor** - 1.2.3 -> 1.3.0\n**patch** - 1.2.3 -> 1.2.4\n**premajor** - 1.2.3 -> 2.0.0-0\n**preminor** - 1.2.3 -> 1.3.0-0\n**prepatch** - 1.2.3 -> 1.2.4-0\n**prerelease** - 1.2.3 -> 1.2.4-0"],["DESCRIPTION","**semver** validates, compares, and manipulates semantic version strings following the semver.org specification. It provides version incrementing (bumping major, minor, patch, or prerelease components), range matching using NPM-style range syntax, and version sorting.\n\nRange matching tests whether versions satisfy constraints like **>=1.0.0 <2.0.0**, **^1.2.3**, or **~1.2.3**, using the same range syntax as npm's package.json version specifiers. Coercion converts non-standard version strings like \"v1.2\" into valid semver format (1.2.0). Multiple versions passed as arguments are printed in sorted order."],["CAVEATS","Node.js package. Strict mode may reject valid-looking versions. Range syntax can be complex."],["HISTORY","**semver** is the Node.js implementation of semantic versioning. It's used by npm for package version resolution."],["INSTALL","```pacman: sudo pacman -S semver```\n\n```brew: brew install semver```\n\n```nix: nix profile install nixpkgs#semver```"],["SEE ALSO","[npm](/man/npm)(1), [node](/man/node)(1)"]],"e":["npm","node"],"x":"semver [1.2.3]"},"sfdisk":{"n":"sfdisk","t":"Scriptable partition table manipulator","s":[["TAGLINE","Scriptable partition table manipulator"],["TLDR","**Back up** the partition layout to a file\n\n```sudo sfdisk -d [/dev/sdX] > [path/to/file.dump]```\n\n**Restore** a partition layout from a backup\n\n```sudo sfdisk [/dev/sdX] < [path/to/file.dump]```\n\n**Set** the type of a partition\n\n```sudo sfdisk --part-type [/dev/sdX] [partition_number] [swap]```\n\n**Delete** a partition\n\n```sudo sfdisk --delete [/dev/sdX] [partition_number]```"],["SYNOPSIS","**sfdisk** [_options_] _device_"],["PARAMETERS","**-d, --dump**\n> Dump partition table in sfdisk-compatible format\n\n**-l, --list**\n> List partitions on a device\n\n**--delete**\n> Delete a partition\n\n**--part-type**\n> Set or change partition type\n\n**-n, --no-act**\n> Dry run; don't write changes\n\n**-b, --backup**\n> Backup partition table sectors before changes\n\n**-f, --force**\n> Disable all consistency checks\n\n**--verify**\n> Check partition table consistency\n\n**-J, --json**\n> Output in JSON format"],["DESCRIPTION","**sfdisk** is a scriptable partition table manipulator. Unlike interactive tools like **fdisk**, sfdisk is designed for non-interactive use, making it ideal for backup/restore operations and automated partitioning scripts.\n\nThe dump format is human-readable and can be edited before restoring. This makes it useful for cloning partition layouts between identical disks."],["CAVEATS","Requires root privileges. Modifying partition tables can cause data loss; always backup important data first. The partition layout must be compatible with the target disk's geometry. Part of the util-linux package."],["INSTALL","```apt: sudo apt install fdisk```\n\n```apk: sudo apk add sfdisk```"],["SEE ALSO","[fdisk](/man/fdisk)(8), [parted](/man/parted)(8), [gdisk](/man/gdisk)(8), [partprobe](/man/partprobe)(8)"]],"e":["fdisk","parted","gdisk","partprobe"],"x":"sudo sfdisk -d [/dev/sdX] > [path/to/file.dump]"},"sfdk-emulator":{"n":"sfdk-emulator","t":"Manage Sailfish OS emulator instances","s":[["TAGLINE","Manage Sailfish OS emulator instances"],["TLDR","**Start emulator**\n\n```sfdk emulator start```\n\n**Stop emulator**\n\n```sfdk emulator stop```\n\n**List emulators**\n\n```sfdk emulator list```\n\n**Show emulator** properties\n\n```sfdk emulator show```\n\n**Set emulator** CPU count\n\n```sfdk emulator set vm.cpuCount=[4]```\n\n**Set emulator** memory size\n\n```sfdk emulator set vm.memorySize=[2048]```"],["SYNOPSIS","**sfdk emulator** _command_ [_options_]"],["PARAMETERS","**start**\n> Start the emulator virtual machine.\n\n**stop**\n> Stop the emulator virtual machine.\n\n**list**\n> List available emulator instances.\n\n**show**\n> Display emulator virtual machine properties.\n\n**set** _property=value_\n> Set emulator virtual machine properties (e.g., vm.cpuCount, vm.memorySize).\n\n**status**\n> Show the current status of the emulator."],["DESCRIPTION","**sfdk emulator** manages Sailfish OS emulator instances for testing applications. It provides commands to start, stop, and configure emulator virtual machines. VM properties such as CPU count and memory size can be adjusted using the **set** subcommand. Part of the Sailfish SDK."],["SEE ALSO","[sfdk](/man/sfdk)(1), [sfdk-device](/man/sfdk-device)(1)"]],"e":["sfdk","sfdk-device"],"x":"sfdk emulator start"},"skaffold":{"n":"skaffold","t":"Automate Kubernetes development workflows","s":[["TAGLINE","Automate Kubernetes development workflows"],["TLDR","**Initialize skaffold**\n\n```skaffold init```\n\n**Development mode**\n\n```skaffold dev```\n\n**Build images**\n\n```skaffold build```\n\n**Deploy to cluster**\n\n```skaffold deploy```\n\n**Run once**\n\n```skaffold run```\n\n**Delete deployment**\n\n```skaffold delete```\n\n**Debug mode**\n\n```skaffold debug```\n\n**Render manifests**\n\n```skaffold render```"],["SYNOPSIS","**skaffold** _command_ [_-f config_] [_options_]"],["PARAMETERS","**init**\n> Create config.\n\n**dev**\n> Continuous development.\n\n**build**\n> Build images.\n\n**deploy**\n> Deploy to cluster.\n\n**run**\n> Build and deploy once.\n\n**delete**\n> Remove deployment.\n\n**debug**\n> Debug mode.\n\n**-f**, **--filename** _FILE_\n> Config file.\n\n**-p**, **--profile** _NAME_\n> Use profile.\n\n**-n**, **--namespace** _NS_\n> Kubernetes namespace.\n\n**--default-repo** _REPO_\n> Default image repository (overrides global config).\n\n**--kube-context** _CTX_\n> Deploy to specified Kubernetes context.\n\n**--tail**\n> Stream logs from deployed objects (default: true in dev mode).\n\n**--port-forward**\n> Port-forward exposed container ports within pods.\n\n**--trigger** _MODE_\n> Change detection trigger: polling, notify, or manual (default: notify)."],["COMMANDS","**verify**\n> Run verification tests against deployments.\n\n**test**\n> Run tests against built application images.\n\n**apply**\n> Apply hydrated manifests to a cluster.\n\n**diagnose**\n> Run a diagnostic on Skaffold configuration.\n\n**fix**\n> Update old configuration to a newer schema version."],["DESCRIPTION","**Skaffold** automates the Kubernetes development workflow by handling the build, push, and deploy cycle for containerized applications. The **dev** command watches source files for changes and automatically rebuilds images and redeploys to the cluster on each save, providing a rapid inner development loop.\n\nThe tool integrates with multiple build systems including Docker, Jib, and Cloud Native Buildpacks, and supports deployment via kubectl, Helm, and kustomize. Profiles allow customizing the pipeline for different environments such as development, staging, and production, each with different image registries, build configurations, and deployment targets.\n\nPort forwarding automatically exposes deployed services on localhost, and the **debug** command configures remote debugging for supported languages."],["CAVEATS","Requires Kubernetes cluster. Docker or alternative builder needed. Learning curve for configuration."],["HISTORY","**Skaffold** was created by **Google** for Kubernetes development. It simplifies the inner development loop for containerized applications."],["INSTALL","```pacman: sudo pacman -S skaffold```\n\n```zypper: sudo zypper install skaffold```\n\n```brew: brew install skaffold```\n\n```nix: nix profile install nixpkgs#skaffold```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [docker](/man/docker)(1), [helm](/man/helm)(1), [devspace](/man/devspace)(1)"]],"e":["kubectl","docker","helm","devspace"],"x":"skaffold init"},"sstat":{"n":"sstat","t":"","s":[["TLDR","Display status of a **running job**\n\n```sstat -j [job_id]```\n\nShow **specific fields** in parsable format\n\n```sstat -p -j [job_id] -o JobID,AveCPU,AveVMSize```\n\nList **available fields**\n\n```sstat -e```\n\nShow **all job steps**\n\n```sstat -a -j [job_id]```\n\nShow **PIDs** for each step\n\n```sstat -i -j [job_id]```"],["SYNOPSIS","**sstat** [_OPTIONS_...]"],["PARAMETERS","**-j, --jobs** _job[.step]_\n> Job ID or job.step to query (required)\n\n**-o, --format, --fields** _fields_\n> Comma-separated list of fields to display\n\n**-e, --helpformat**\n> List available format fields\n\n**-a, --allsteps**\n> Show all steps for specified job\n\n**-i, --pidformat**\n> Display PIDs for each job step\n\n**-n, --noheader**\n> Omit header from output\n\n**-p, --parsable**\n> Pipe-delimited with trailing pipe\n\n**-P, --parsable2**\n> Pipe-delimited without trailing pipe\n\n**--noconvert**\n> Keep original units (don't convert)\n\n**-v, --verbose**\n> Verbose output\n\n**-V, --version**\n> Print version"],["COMMON FIELDS","**JobID**: Job identifier\n**AveCPU**: Average CPU time\n**AveRSS**: Average resident set size\n**AveVMSize**: Average virtual memory\n**MaxRSS**: Maximum RSS\n**MaxVMSize**: Maximum VM size\n**NTasks**: Number of tasks\n**MinCPU**: Minimum CPU time"],["DESCRIPTION","**sstat** displays real-time status information for running Slurm jobs including CPU usage, memory consumption, and I/O statistics. It queries the jobacct_gather plugin for metrics on currently executing jobs and their steps.\n\nRoot users can view all running jobs; regular users can only see their own jobs. The command is useful for monitoring resource usage during job execution and diagnosing performance issues."],["CAVEATS","Only works on running jobs; use **sacct** for completed jobs. Requires the jobacct_gather plugin. Available metrics depend on the specific plugin (linux, cgroup). Non-root users have limited visibility."],["HISTORY","**sstat** is part of **Slurm**, developed at **Lawrence Livermore National Laboratory** starting in **2002**. Job accounting features help users and administrators monitor resource consumption in real-time. Slurm is now maintained by **SchedMD**."],["INSTALL","```apt: sudo apt install slurm-client```"],["SEE ALSO","[sacct](/man/sacct)(1), [squeue](/man/squeue)(1), [scontrol](/man/scontrol)(1), [srun](/man/srun)(1)"],["RESOURCES","```[Source code](https://github.com/SchedMD/slurm)```\n\n```[Homepage](https://slurm.schedmd.com/)```\n\n```[Documentation](https://slurm.schedmd.com/sstat.html)```"]],"e":["sacct","squeue","scontrol","srun"],"x":"sstat -j [job_id]"},"sudo":{"n":"sudo","t":"Execute commands with elevated privileges","s":[["TAGLINE","Execute commands with elevated privileges"],["TLDR","**Run a command as root**\n\n```sudo [command]```\n\n**Run a command as another user**\n\n```sudo -u [username] [command]```\n\n**Start a root shell**\n\n```sudo -i```\n\n**Start a shell as another user**\n\n```sudo -u [username] -s```\n\n**Run command with preserved environment**\n\n```sudo -E [command]```\n\n**Edit a file as root** (using sudoedit)\n\n```sudo -e [/etc/hosts]```\n\n**List allowed commands** for current user\n\n```sudo -l```\n\n**Invalidate cached credentials**\n\n```sudo -k```\n\n**Run command in background**\n\n```sudo -b [command]```"],["SYNOPSIS","**sudo** [_options_] [_command_]\n\n**sudo** -u _user_ [_command_]\n\n**sudo** -i | -s"],["PARAMETERS","**-u** _USER_, **--user** _USER_\n> Run command as specified user (default: root)\n\n**-g** _GROUP_, **--group** _GROUP_\n> Run command with specified primary group\n\n**-i**, **--login**\n> Run login shell as target user\n\n**-s**, **--shell**\n> Run shell (from $SHELL or user's default)\n\n**-e**, **--edit**\n> Edit files (sudoedit mode)\n\n**-E**, **--preserve-env**\n> Preserve user's environment variables\n\n**-H**, **--set-home**\n> Set HOME to target user's home directory\n\n**-b**, **--background**\n> Run command in background\n\n**-l**, **--list**\n> List allowed commands for user\n\n**-v**, **--validate**\n> Refresh credential cache without running command\n\n**-k**, **--reset-timestamp**\n> Invalidate cached credentials\n\n**-K**, **--remove-timestamp**\n> Remove cached credentials entirely\n\n**-n**, **--non-interactive**\n> Non-interactive mode; fail if password required\n\n**-A**, **--askpass**\n> Use askpass helper for password\n\n**-S**, **--stdin**\n> Read password from stdin\n\n**-p** _PROMPT_, **--prompt** _PROMPT_\n> Custom password prompt\n\n**-T** _TIMEOUT_\n> Command timeout in seconds\n\n**-V**, **--version**\n> Display version information"],["DESCRIPTION","**sudo** (superuser do) executes commands with elevated privileges, typically as root. It provides controlled access to privileged operations based on rules defined in **/etc/sudoers**, allowing fine-grained permission management.\n\nAfter successful authentication, sudo caches credentials for a grace period (typically 5 minutes), allowing subsequent commands without re-entering the password. Use **-k** to invalidate this cache.\n\nThe **sudoers** file controls who can run what commands on which hosts. Users can be granted full root access or restricted to specific commands. The **visudo** command should be used to edit sudoers safely."],["CONFIGURATION","**/etc/sudoers**\n> Main policy file defining which users can run which commands. Edit with visudo only.\n\n**/etc/sudoers.d/**\n> Drop-in directory for modular sudo policy files.\n\n**SUDO_EDITOR**\n> Environment variable specifying the editor for sudoedit."],["CAVEATS","Running commands as root is dangerous—mistakes can damage the system. The **-E** flag can expose sensitive environment variables. Some environment variables are filtered by default for security. Incorrect sudoers syntax can lock out administrative access; always use **visudo** to edit. Avoid using sudo in scripts that might be run by untrusted users."],["HISTORY","sudo was originally written by **Bob Coggeshall** and **Cliff Spencer** at SUNY Buffalo around **1980**. **Todd Miller** took over maintenance in **1994** and continues to lead development. The name is commonly understood as \"superuser do\" though it originally stood for \"substitute user do\" since it can run commands as any user. sudo has become the standard method for privilege escalation on Unix-like systems, replacing direct root login in most distributions."],["INSTALL","```apt: sudo apt install sudo```\n\n```dnf: sudo dnf install sudo```\n\n```pacman: sudo pacman -S sudo```\n\n```apk: sudo apk add doas-sudo-shim```\n\n```zypper: sudo zypper install sudo```\n\n```nix: nix profile install nixpkgs#sudo```"],["SEE ALSO","[su](/man/su)(1), [visudo](/man/visudo)(8), [doas](/man/doas)(1)"]],"e":["su","visudo","doas"],"x":"sudo [command]"},"systemctl-reload-or-restart":{"n":"systemctl-reload-or-restart","t":"Reload configuration or restart unit","s":[["TAGLINE","Reload configuration or restart unit"],["TLDR","**Reload or restart** unit\n\n```systemctl reload-or-restart [unit]```\n\nReload or restart with **pattern**\n\n```systemctl reload-or-restart [pattern]```\n\n**Async** (no wait)\n\n```systemctl reload-or-restart [unit] --no-block```\n\nFor **user** units\n\n```systemctl reload-or-restart [unit] --user```"],["SYNOPSIS","**systemctl reload-or-restart** [_OPTIONS_] _PATTERN_..."],["PARAMETERS","**--no-block**\n> Return immediately without waiting\n\n**--user**\n> Operate on user units"],["DESCRIPTION","**systemctl reload-or-restart** reloads one or more units if they support it. If not, it stops and then starts them instead. If the units are not running yet, they will be started.\n\nThis is useful for applying configuration changes when you don't know whether a service supports hot reloading. If the unit has `ExecReload=` defined, configuration is reloaded without interrupting the service. Otherwise, the unit is stopped and started again."],["CAVEATS","Reload only works if the unit has ExecReload defined. Restart causes brief service interruption. For units without reload support, this always restarts."],["HISTORY","The **reload-or-restart** subcommand provides a safe way to apply configuration changes without needing to know whether a service supports hot reloading."],["SEE ALSO","[systemctl-reload](/man/systemctl-reload)(1), [systemctl-restart](/man/systemctl-restart)(1), [systemctl-try-reload-or-restart](/man/systemctl-try-reload-or-restart)(1)"]],"e":["systemctl-reload","systemctl-restart","systemctl-try-reload-or-restart"],"x":"systemctl reload-or-restart [unit]"},"systemctl-try-restart":{"n":"systemctl-try-restart","t":"Restart units only if running","s":[["TAGLINE","Restart units only if running"],["TLDR","**Restart** unit if running\n\n```systemctl try-restart [unit]```\n\nRestart **multiple** units if running\n\n```systemctl try-restart [unit1 unit2 ...]```\n\nRestart by **pattern** if running\n\n```systemctl try-restart '[pattern]'```"],["SYNOPSIS","**systemctl try-restart** _PATTERN_..."],["DESCRIPTION","**systemctl try-restart** restarts one or more units only if they are currently running. If a unit is not active, no action is taken.\n\nThis is useful for restarting services after configuration changes without accidentally starting services that were intentionally stopped."],["CAVEATS","Does nothing for inactive units. Unlike `restart`, this won't start a stopped service. For units not currently running, the command succeeds without action."],["ALIASES","This command is also known as `condrestart`."],["HISTORY","The **try-restart** subcommand provides safe restart operations that respect the current service state, avoiding unintended service starts."],["SEE ALSO","[systemctl-restart](/man/systemctl-restart)(1), [systemctl-try-reload-or-restart](/man/systemctl-try-reload-or-restart)(1), [systemctl](/man/systemctl)(1)"]],"e":["systemctl-restart","systemctl-try-reload-or-restart","systemctl"],"x":"systemctl try-restart [unit]"},"tailscale-funnel":{"n":"tailscale-funnel","t":"Expose local services to internet","s":[["TAGLINE","Expose local services to internet"],["TLDR","**Expose a local service** on port 8080 to the internet\n\n```tailscale funnel [8080]```\n\n**Expose a local HTTPS proxy** target to the internet\n\n```tailscale funnel https+insecure://localhost:[8443]```\n\n**Expose on a specific port** (443, 8443, or 10000)\n\n```tailscale funnel --serve-port [8443] [3000]```\n\n**Turn off funnel**\n\n```tailscale funnel off```\n\n**Check funnel status**\n\n```tailscale funnel status```\n\n**Run in background** mode\n\n```tailscale funnel --bg [8080]```"],["SYNOPSIS","**tailscale** **funnel** [_options_] _target_"],["PARAMETERS","**--bg**\n> Run in background.\n\n**--serve-port** _port_\n> The public-facing port (443, 8443, or 10000; default 443).\n\n**--set-path** _path_\n> Mount the service at a specific URL path.\n\n**off**\n> Disable funnel.\n\n**status**\n> Show current funnel configuration.\n\n**reset**\n> Reset all funnel configuration."],["DESCRIPTION","**tailscale funnel** exposes local services to the public internet through your Tailscale node. Unlike **tailscale serve** which only makes services available within your tailnet, Funnel creates a publicly accessible HTTPS endpoint that anyone on the internet can reach.\n\nTailscale automatically provisions and manages TLS certificates for the endpoint, so traffic is encrypted without manual certificate setup. Services can be mapped to specific URL paths using **--set-path**, allowing multiple local services to be exposed under different routes. The **off** subcommand disables the funnel, and **status** shows the current configuration."],["CAVEATS","Exposes services to the public internet -- use with caution. Funnel must be enabled in the Tailscale admin console ACL policy. Only ports 443, 8443, and 10000 are supported for public-facing endpoints."],["INSTALL","```dnf: sudo dnf install tailscale```\n\n```pacman: sudo pacman -S tailscale```\n\n```apk: sudo apk add tailscale```\n\n```zypper: sudo zypper install tailscale```\n\n```brew: brew install tailscale```\n\n```nix: nix profile install nixpkgs#tailscale```"],["SEE ALSO","[tailscale](/man/tailscale)(1), [tailscale-up](/man/tailscale-up)(1), [ngrok](/man/ngrok)(1)"]],"e":["tailscale","tailscale-up","ngrok"],"x":"tailscale funnel [8080]"},"telegram-desktop":{"n":"telegram-desktop","t":"Telegram messaging desktop client","s":[["TAGLINE","Telegram messaging desktop client"],["TLDR","**Start Telegram**\n\n```telegram-desktop```\n\n**Open with URL**\n\n```telegram-desktop -- [tg://resolve?domain=username]```\n\n**Start minimized**\n\n```telegram-desktop -startintray```\n\n**With working directory**\n\n```telegram-desktop -workdir [~/.telegram]```\n\n**Debug mode**\n\n```telegram-desktop -debug```"],["SYNOPSIS","**telegram-desktop** [_-startintray_] [_-workdir dir_] [_options_]"],["PARAMETERS","**-startintray**\n> Start minimized to tray.\n\n**-workdir** _DIR_\n> Data directory.\n\n**-debug**\n> Debug mode.\n\n**-scale** _N_\n> UI scale factor.\n\n**--**\n> Open URL."],["DESCRIPTION","**telegram-desktop** is the official desktop client for the Telegram messaging platform. It provides access to all Telegram features including text messages, voice and video calls, media sharing, and channels from a native desktop application.\n\nThe client supports group chats with up to 200,000 members, secret chats with end-to-end encryption, file transfers up to 2GB per file, and seamless synchronization across all devices. It is built with Qt and runs on Linux, macOS, and Windows, with data stored in a configurable working directory."],["CAVEATS","Account required. Internet needed. Qt-based application."],["HISTORY","**Telegram Desktop** is the official desktop client for **Telegram**, created by the Durov brothers' team."],["INSTALL","```pacman: sudo pacman -S telegram-desktop```\n\n```zypper: sudo zypper install telegram-desktop```\n\n```nix: nix profile install nixpkgs#telegram-desktop```"],["SEE ALSO","[signal-desktop](/man/signal-desktop)(1), [discord](/man/discord)(1)"]],"e":["signal-desktop","discord"],"x":"telegram-desktop"},"tere":{"n":"tere","t":"Terminal file explorer for fast directory navigation","s":[["TAGLINE","Terminal file explorer for fast directory navigation"],["TLDR","**Navigate to a directory interactively**\n\n```cd \"$(tere)\"```\n\n**Use filter mode to show only matching items**\n\n```cd \"$(tere --filter-search)\"```\n\n**Auto-enter directories after a timeout (milliseconds)**\n\n```cd \"$(tere --autocd-timeout 500)\"```\n\n**Enable mouse navigation**\n\n```cd \"$(tere --mouse=on)\"```\n\n**Sort by modification date and use case-insensitive search**\n\n```cd \"$(tere --sort modified --ignore-case)\"```"],["SYNOPSIS","**tere** [_options_]"],["PARAMETERS","**-h**, **--help**\n> Display help information.\n\n**-V**, **--version**\n> Print version.\n\n**-f**, **--filter-search**\n> Show only items matching current search query.\n\n**-F**, **--no-filter-search**\n> Show all items while searching (default).\n\n**-S**, **--smart-case**\n> Case insensitive for lowercase, sensitive with uppercase (default).\n\n**-i**, **--ignore-case**\n> Always ignore case in searches.\n\n**-s**, **--case-sensitive**\n> Always enforce case sensitivity.\n\n**-g**, **--gap-search**\n> Allow fuzzy matches with gaps from start of name (default).\n\n**-G**, **--gap-search-anywhere**\n> Allow fuzzy matches with gaps anywhere in names.\n\n**-n**, **--normal-search**\n> Match consecutive characters from start of name.\n\n**-N**, **--normal-search-anywhere**\n> Match consecutive characters anywhere in name.\n\n**--files** _MODE_\n> File display mode: ignore (default), hide, or match.\n\n**--sort** _ORDER_\n> Sort order: name (default), created, or modified.\n\n**--autocd-timeout** _MS_\n> Auto-enter directory after MS milliseconds when single match. Use \"off\" to disable.\n\n**--mouse=on|off**\n> Enable or disable mouse navigation (default: off).\n\n**--history-file** _PATH_\n> Path to JSON history file.\n\n**--map** _KEY:ACTION_\n> Custom keyboard shortcut mapping."],["DESCRIPTION","**tere** provides a TUI for quickly navigating to a directory, then prints the selected path on exit. It is not a file manager -- it only browses folders. Navigation uses arrow keys, Enter, and type-to-search, designed for minimal keystrokes."],["HISTORY","**tere** was created by **Marton Gunyho** (mgunyho) and is written in **Rust**."],["INSTALL","```pacman: sudo pacman -S tere```\n\n```brew: brew install tere```\n\n```nix: nix profile install nixpkgs#tere```"],["SEE ALSO","[broot](/man/broot)(1), [zoxide](/man/zoxide)(1), [ranger](/man/ranger)(1), [fzf](/man/fzf)(1), [nnn](/man/nnn)(1)"]],"e":["broot","zoxide","ranger","fzf","nnn"],"x":"cd \"$(tere)\""},"testdisk":{"n":"testdisk","t":"Recover lost partitions and boot sectors","s":[["TAGLINE","Recover lost partitions and boot sectors"],["TLDR","**Start testdisk** interactively\n\n```sudo testdisk```\n\n**Analyze a specific disk**\n\n```sudo testdisk [/dev/sdX]```\n\n**Analyze a disk image** file\n\n```testdisk [path/to/image.dd]```\n\n**Create a log file** during recovery\n\n```sudo testdisk /log [/dev/sdX]```\n\n**List partitions** on a device\n\n```sudo testdisk /list [/dev/sdX]```\n\n**Dump raw sectors** to the log\n\n```sudo testdisk /dump [/dev/sdX]```\n\n**Show the version** and exit\n\n```testdisk /version```"],["SYNOPSIS","**testdisk** [_/log_] [_/debug_] [_/list_] [_/dump_] [_/version_] [_device|image_]"],["PARAMETERS","**/log**\n> Append all operations and analysis results to **testdisk.log** in the current directory.\n\n**/debug**\n> Enable debug mode with additional detailed output.\n\n**/list**\n> Display partition information for the specified device or image and exit.\n\n**/dump**\n> Dump raw sector contents into the log for post-mortem inspection.\n\n**/version**\n> Print the TestDisk version and exit.\n\n**device**\n> The disk device to analyze (e.g. **/dev/sda**, **/dev/nvme0n1**).\n\n**image**\n> A disk image file to analyze instead of a physical device."],["DESCRIPTION","**TestDisk** is a powerful open-source data recovery utility designed to recover lost partitions and repair boot sectors. It can restore partition tables, recover deleted partitions, rebuild boot sectors for FAT and NTFS, and fix corrupted file systems.\n\nTestDisk supports a wide range of file systems including FAT12/FAT16/FAT32, NTFS, exFAT, ext2/ext3/ext4, HFS/HFS+, JFS, ReiserFS, XFS, and many others. It works with various partition table types including DOS/MBR, GPT, Mac, and BSD disklabels.\n\nThe utility operates in an ncurses-based interactive menu, guiding users through the recovery process. TestDisk also includes a file recovery feature accessible through its Advanced menu, allowing recovery of deleted files from supported file systems."],["CAVEATS","Root privileges are required when working with physical disk devices. When recovering files, always save them to a different disk or partition to avoid overwriting recoverable data. TestDisk is primarily a partition recovery tool; for file-level recovery from damaged partitions, consider using its companion tool **photorec**."],["HISTORY","TestDisk was created by **Christophe Grenier** and initially developed as a partition repair tool for DOS, which explains its **/flag** command-line syntax. The project became part of **CGSecurity** and was open-sourced under the GPL. It is bundled with **PhotoRec**, a file carving tool for recovering files regardless of file system."],["INSTALL","```apt: sudo apt install testdisk```\n\n```dnf: sudo dnf install testdisk```\n\n```pacman: sudo pacman -S testdisk```\n\n```apk: sudo apk add testdisk```\n\n```zypper: sudo zypper install testdisk```\n\n```brew: brew install testdisk```\n\n```nix: nix profile install nixpkgs#testdisk```"],["SEE ALSO","[photorec](/man/photorec)(1), [fdisk](/man/fdisk)(8), [gdisk](/man/gdisk)(8), [fsck](/man/fsck)(8), [ddrescue](/man/ddrescue)(1)"]],"e":["photorec","fdisk","gdisk","fsck","ddrescue"],"x":"sudo testdisk"},"tuisky":{"n":"tuisky","t":"TUI client for the Bluesky social network","s":[["TAGLINE","TUI client for the Bluesky social network"],["TLDR","**Launch the Bluesky TUI client**\n\n```tuisky```\n\n**Specify a config file**\n\n```tuisky -c [path/to/config.toml]```\n\n**Set maximum number of columns**\n\n```tuisky -n [3]```\n\n**Show version**\n\n```tuisky -V```"],["SYNOPSIS","**tuisky** [_options_]"],["PARAMETERS","**-c**, **--config** _CONFIG_\n> Path to configuration file.\n\n**-n**, **--num-columns** _NUM_\n> Maximum number of columns to display (auto-determined by terminal width if not set).\n\n**-h**, **--help**\n> Print help.\n\n**-V**, **--version**\n> Print version."],["DESCRIPTION","**tuisky** is a terminal user interface client for Bluesky. It features multiple columns, multiple session management, pinned feed selection, auto-refresh, post creation with text and image embedding, notifications and chat, and configurable settings with auto-save and restore."],["HISTORY","**tuisky** was created by **sugyan** and is written in **Rust** using the ratatui TUI framework."],["INSTALL","```brew: brew install tuisky```\n\n```nix: nix profile install nixpkgs#tuisky```"],["SEE ALSO","[toot](/man/toot)(1), [servitor](/man/servitor)(1)"]],"e":["toot","servitor"],"x":"tuisky"},"ul":{"n":"ul","t":"Translate underline sequences for terminals","s":[["TAGLINE","Translate underline sequences for terminals"],["TLDR","**Display** file with underlines\n\n```ul [file.txt]```\n\nDisplay with **dash** underlines\n\n```ul -i [file.txt]```"],["SYNOPSIS","**ul** [_OPTIONS_] [_FILE_...]"],["PARAMETERS","**-i, --indicated**\n> Use dashes (-) for underlining\n\n**-t, --terminal** _TYPE_\n> Override terminal type"],["DESCRIPTION","**ul** translates underline sequences in text files to the appropriate terminal escape sequences. It processes files containing overstriking (backspace-based underlining) and converts them to display properly on modern terminals.\n\nThe tool reads files that use traditional underlining where each underlined character is followed by backspace and underscore."],["CAVEATS","Input must contain traditional overstrike underline sequences. Output depends on terminal capabilities. Modern text rarely uses this format."],["HISTORY","**ul** is part of **util-linux** and dates from early Unix when terminals used overstrike sequences for formatting effects."],["INSTALL","```apt: sudo apt install bsdextrautils```\n\n```apk: sudo apk add util-linux-misc```"],["SEE ALSO","[col](/man/col)(1), [colcrt](/man/colcrt)(1)"]],"e":["col","colcrt"],"x":"ul [file.txt]"},"unzstd":{"n":"unzstd","t":"Decompress Zstandard compressed files","s":[["TAGLINE","Decompress Zstandard compressed files"],["TLDR","**Decompress file**\n\n```unzstd [file.zst]```\n\n**Keep original**\n\n```unzstd -k [file.zst]```\n\n**To stdout**\n\n```unzstd -c [file.zst]```\n\n**Force overwrite**\n\n```unzstd -f [file.zst]```\n\n**Decompress multiple**\n\n```unzstd [file1.zst] [file2.zst]```"],["SYNOPSIS","**unzstd** [_-k_] [_-c_] [_-f_] [_options_] _files_"],["PARAMETERS","**-k**\n> Keep original.\n\n**-c**\n> Write to stdout.\n\n**-f**\n> Force overwrite.\n\n**-v**\n> Verbose mode.\n\n**-q**\n> Quiet mode."],["DESCRIPTION","**unzstd** decompresses files that were compressed with the Zstandard (zstd) algorithm. It is functionally equivalent to running **zstd --decompress** and is provided as a convenience command within the zstd package.\n\nZstandard offers very fast decompression speeds while achieving compression ratios comparable to zlib. By default, unzstd replaces the compressed .zst file with the decompressed output. The **-k** flag preserves the original, and **-c** writes to standard output for piping.\n\nDeveloped by Facebook (now Meta) and released in 2016, Zstandard has been adopted by the Linux kernel, package managers like pacman and apt, and many other tools as a modern replacement for gzip that provides both better compression and faster decompression."],["CAVEATS","Newer format. May not be everywhere. Part of zstd package."],["HISTORY","**unzstd** is part of **Zstandard** (zstd), a fast compression algorithm developed by Facebook."],["INSTALL","```apt: sudo apt install zstd```\n\n```dnf: sudo dnf install zstd```\n\n```pacman: sudo pacman -S zstd```\n\n```apk: sudo apk add zstd```\n\n```zypper: sudo zypper install zstd```\n\n```brew: brew install zstd```\n\n```nix: nix profile install nixpkgs#zstd```"],["SEE ALSO","[zstd](/man/zstd)(1), [gzip](/man/gzip)(1), [xz](/man/xz)(1)"]],"e":["zstd","gzip","xz"],"x":"unzstd [file.zst]"},"valkey-cli":{"n":"valkey-cli","t":"Command-line client for Valkey data store","s":[["TAGLINE","Command-line client for Valkey data store"],["TLDR","**Connect to server**\n\n```valkey-cli```\n\n**Connect to host**\n\n```valkey-cli -h [host] -p [6379]```\n\n**Execute command**\n\n```valkey-cli SET [key] [value]```\n\n**With password**\n\n```valkey-cli -a [password]```\n\n**Select database**\n\n```valkey-cli -n [2]```\n\n**Cluster mode**\n\n```valkey-cli -c```\n\n**Monitor live server latency**\n\n```valkey-cli --latency```\n\n**Scan keys matching a pattern**\n\n```valkey-cli --scan --pattern '[user:*]'```\n\n**Find large keys in the dataset**\n\n```valkey-cli --bigkeys```"],["SYNOPSIS","**valkey-cli** [_-h host_] [_-p port_] [_-a password_] [_options_] [_command_]"],["PARAMETERS","**-h** _HOST_\n> Server hostname (default: 127.0.0.1).\n\n**-p** _PORT_\n> Server port (default: 6379).\n\n**-s** _SOCKET_\n> Unix socket path (overrides host and port).\n\n**-u** _URI_\n> Connection URI (valkey://user:password@host:port/db).\n\n**-a** _PASSWORD_\n> Password for authentication.\n\n**--user** _USERNAME_\n> Username for ACL authentication.\n\n**--askpass**\n> Prompt for a password without echoing.\n\n**-n** _DB_\n> Database number to select on connect.\n\n**-c**\n> Enable cluster mode with automatic redirects.\n\n**-r** _N_\n> Run the command _N_ times (use **-1** for infinite).\n\n**-i** _SECONDS_\n> Interval between repetitions when using **-r**.\n\n**-x**\n> Read last argument from standard input.\n\n**--scan**\n> Iterate keys using SCAN.\n\n**--pattern** _PATTERN_\n> Filter keys by pattern (used with --scan/--bigkeys/--hotkeys).\n\n**--bigkeys**\n> Sample the keyspace and report the largest keys.\n\n**--memkeys**\n> Sample the keyspace and report the most memory-intensive keys.\n\n**--hotkeys**\n> Report the most frequently accessed keys (requires LFU policy).\n\n**--latency**\n> Measure server latency in real time.\n\n**--stat**\n> Display continuous server statistics.\n\n**--rdb** _FILE_\n> Transfer a remote RDB dump to a local file.\n\n**--pipe**\n> Transfer raw RESP protocol from stdin for mass import.\n\n**--tls**\n> Enable a TLS-encrypted connection.\n\n**--cluster** _CMD_\n> Run cluster management subcommands (create, check, reshard, rebalance, add-node, del-node)."],["DESCRIPTION","**valkey-cli** is the command-line interface for interacting with a Valkey server. It provides both an interactive REPL mode for exploring data and a non-interactive mode for scripting and automation. The client uses the same protocol as Redis, making it compatible with existing Redis workflows.\n\nCommands can be passed directly on the command line or piped from files and other programs. The client supports cluster mode for connecting to sharded deployments, database selection, authentication, and TLS connections."],["CAVEATS","Fork of Redis. Redis-cli compatible. Server must be running."],["HISTORY","**valkey-cli** is the CLI for **Valkey**, a Redis fork maintained by the Linux Foundation."],["INSTALL","```apt: sudo apt install valkey-tools```\n\n```apk: sudo apk add valkey-cli```"],["SEE ALSO","[redis-cli](/man/redis-cli)(1), [valkey-server](/man/valkey-server)(1)"]],"e":["redis-cli","valkey-server"],"x":"valkey-cli"},"virsh-help":{"n":"virsh-help","t":"Display virsh command help","s":[["TAGLINE","Display virsh command help"],["TLDR","**List all available commands** grouped by category\n\n```virsh help```\n\n**Show detailed help for a specific command**\n\n```virsh help [start]```\n\n**List all domain management commands**\n\n```virsh help domain```\n\n**List all network commands**\n\n```virsh help network```\n\n**List all storage pool commands**\n\n```virsh help pool```"],["SYNOPSIS","**virsh** **help** [_command_|_group_]"],["PARAMETERS","_command_\n> Show detailed help for a specific virsh command.\n\n**domain**\n> Domain (virtual machine) management commands.\n\n**interface**\n> Host interface commands.\n\n**network**\n> Virtual network commands.\n\n**nodedev**\n> Host device commands.\n\n**pool**\n> Storage pool commands.\n\n**volume**\n> Storage volume commands.\n\n**snapshot**\n> Domain snapshot commands.\n\n**monitor**\n> Connection and event monitoring commands."],["DESCRIPTION","**virsh help** displays help for virsh commands and command groups. When called without arguments, it lists all commands grouped by category with a keyword for each group. When given a command name, it shows detailed usage, options, and description for that command. When given a group keyword, it lists only the commands in that group."],["INSTALL","```apt: sudo apt install libvirt-clients```\n\n```dnf: sudo dnf install libvirt-client```\n\n```apk: sudo apk add libvirt-client```\n\n```zypper: sudo zypper install libvirt-client```"],["SEE ALSO","[virsh](/man/virsh)(1)"]],"e":["virsh"],"x":"virsh help"},"vitetris":{"n":"vitetris","t":"Terminal-based Tetris game","s":[["TAGLINE","Terminal-based Tetris game"],["TLDR","**Start the game**\n\n```vitetris```\n\n**Start without menu** (jump straight into game)\n\n```vitetris -nomenu```\n\n**Listen for network game** on a port\n\n```vitetris listen [34034]```\n\n**Connect to a network game**\n\n```vitetris connect [hostname:34034]```\n\n**Show high scores**\n\n```vitetris -hiscores```\n\n**Set player name** for network play\n\n```vitetris -name [name] listen [port]```"],["SYNOPSIS","**vitetris** [**-nomenu**] [**-hiscores**] [**listen** _port_] [**connect** _host:port_]"],["PARAMETERS","**-nomenu**\n> Skip the menu and start playing immediately.\n\n**-hiscores**\n> Print the high score list and exit.\n\n**-hiscores** _FILE_\n> Read and add high score entries from FILE.\n\n**listen** _port_\n> Listen for incoming network connections on the specified port.\n\n**connect** _host:port_\n> Connect to a remote player for network play. Host can be an IP address or hostname. Use **connect** _port_ to connect to localhost.\n\n**-help**\n> Print help and exit. Use **-help game** for game options and **-help term** for terminal options.\n\n**-js0** _device_, **-js1** _device_\n> Specify joystick device for player 1 or 2.\n\n**-name** _NAME_\n> Set player name for network play."],["PREVIEW","```\n    []\n    [][]\n      []\n\n  [][][]\n    [][]\n  [][][][]\n  [][][][]\n```"],["DESCRIPTION","**vitetris** is a terminal-based Tetris clone with gameplay similar to early Nintendo Tetris games. It features configurable controls, a high score table, two-player mode with garbage lines, network play, and joystick support on Linux.\n\nThe game runs entirely in the terminal using text-mode graphics, making it playable over SSH or on systems without a graphical environment. Controls are fully customizable through the in-game options menu.\n\nNetwork play allows two players to compete over the internet, with each player able to choose their own difficulty level and starting height. One player listens on a port while the other connects to that address.\n\nThe game has minimal dependencies, requiring only libc, and has been tested on Linux, BSD, and ported to Windows and DOS."],["CAVEATS","Terminal size should be at least 80x24 for proper display. Network play requires appropriate firewall configuration to allow connections on the chosen port. Some terminal emulators may have input lag affecting gameplay."],["HISTORY","**vitetris** was created by **Victor Geraldsson** and first released in the early **2000s**. It was designed to be a lightweight, terminal-based Tetris implementation with network play capabilities. The project continues to be maintained and is available in package repositories for major Linux distributions."],["INSTALL","```zypper: sudo zypper install vitetris```\n\n```brew: brew install vitetris```\n\n```nix: nix profile install nixpkgs#vitetris```"],["SEE ALSO","[bastet](/man/bastet)(6), [tint](/man/tint)(6), [ninvaders](/man/ninvaders)(6)"]],"e":["bastet","tint","ninvaders"],"x":"vitetris"}});
