window.LCL_BUCKET(64,{"acyclic":{"n":"acyclic","t":"make a directed graph acyclic","s":[["TAGLINE","make a directed graph acyclic"],["TLDR","**Output** a copy of the graph with enough edges reversed to make it acyclic\n\n```acyclic [graph.dot]```\n\n**Write** the acyclic result to a file\n\n```acyclic -o [output.dot] [graph.dot]```\n\n**Test** only whether the graph is acyclic (no output, check exit status)\n\n```acyclic -nv [graph.dot]```"],["SYNOPSIS","**acyclic** [_-nv?_] [**-o** _outfile_] [_file_]"],["DESCRIPTION","**acyclic** is a Graphviz filter that takes a directed graph as input and outputs a copy of the graph with sufficient edges reversed to make the graph acyclic. Each reversed edge inherits all of the attributes of the original edge. By default the program reads from stdin and writes to stdout; an optional file argument specifies where the input graph is stored.\n\nThis tool is useful for preprocessing graphs before layout algorithms that require acyclic input, or for verifying dependency graphs where cycles would indicate errors. The exit status reports whether the input was acyclic, contained a cycle, or was undirected."],["PARAMETERS","**-n**\n> Produce no output; the return value indicates whether the graph is acyclic.\n\n**-v**\n> Print information about whether the file is acyclic, has a cycle, or is undirected.\n\n**-o** _outfile_\n> Write output to the specified file instead of stdout.\n\n**-?**\n> Print usage information."],["RETURN CODES","**acyclic** returns **0** if the graph is acyclic, **1** if the graph has a cycle, **2** if the graph is undirected, and **255** if there are any errors."],["CAVEATS","Only works with directed graphs in DOT format. acyclic makes a graph acyclic by reversing edges, never by removing them. If the graph is strict and contains a cycle of length 2, the attributes of the reversed edge are lost. Some edge attributes are non-symmetric (referring to either the head or tail node) and are not switched when an edge is reversed."],["HISTORY","**acyclic** is part of the Graphviz project, developed at AT&T Labs Research starting in **1991**. The tool was included to support preprocessing of graphs for hierarchical layout algorithms like **dot**, which require acyclic input graphs."],["INSTALL","```apt: sudo apt install graphviz```\n\n```dnf: sudo dnf install graphviz```\n\n```pacman: sudo pacman -S graphviz```\n\n```apk: sudo apk add graphviz```\n\n```zypper: sudo zypper install graphviz```\n\n```brew: brew install graphviz```\n\n```nix: nix profile install nixpkgs#graphviz```"],["SEE ALSO","[dot](/man/dot)(1), [tred](/man/tred)(1), [gc](/man/gc)(1), [gvpr](/man/gvpr)(1), [gvcolor](/man/gvcolor)(1), [ccomps](/man/ccomps)(1), [sccmap](/man/sccmap)(1)"],["RESOURCES","```[Source code](https://gitlab.com/graphviz/graphviz)```\n\n```[Homepage](https://graphviz.org/)```\n\n```[Documentation](https://graphviz.org/docs/cli/acyclic/)```"]],"e":["dot","tred","gc","gvpr","gvcolor","ccomps","sccmap"],"x":"acyclic [graph.dot]"},"amadmin":{"n":"amadmin","t":"Administrative interface for Amanda backup management","s":[["TAGLINE","Administrative interface for Amanda backup management"],["TLDR","Dump the full internal **configuration** representation\n\n```amadmin [config_name] config```\n\nShow when each disk was last dumped and the next planned **level**\n\n```amadmin [config_name] info [hostname]```\n\nForce a **new full backup** of a host on the next run\n\n```amadmin [config_name] force [hostname]```\n\n**Remove** a host or disk from the configuration database\n\n```amadmin [config_name] delete [hostname]```\n\nShow how backups are **balanced** across run days\n\n```amadmin [config_name] balance```\n\nList the **disks** in the configuration\n\n```amadmin [config_name] disklist```"],["SYNOPSIS","**amadmin** _config_ _command_ [_args_]"],["DESCRIPTION","**amadmin** is the administrative interface for Amanda (Advanced Maryland Automatic Network Disk Archiver), an open-source backup solution. It performs interactive maintenance on a backup configuration: forcing full dumps, inspecting the backup database, and querying where past dumps were stored.\n\nThe first argument is always the configuration name, followed by a subcommand. **amadmin** operates on Amanda's own database and schedule; it does not run backups itself (use **amdump**) or label volumes (use **amlabel**)."],["PARAMETERS","**config**\n> Dump the full internal representation of the configuration in text format\n\n**info** [_host_ [_disk_]]\n> Show the current information Amanda has recorded for hosts and disks\n\n**disklist** [_host_ [_disk_]]\n> List the disklist entries Amanda will back up\n\n**force** _host_ [_disk_]\n> Force a full backup of the host or disk on the next run\n\n**unforce** _host_ [_disk_]\n> Undo a previous force request\n\n**delete** _host_ [_disk_]\n> Remove a host or disk from the Amanda database (does not change the disklist file)\n\n**balance** [**--days** _num_]\n> Show how full backups are balanced across the run cycle\n\n**find** [_host_ [_disk_]]\n> Show which volumes hold the dumps for the given hosts and disks"],["CONFIGURATION","**/etc/amanda/<config>/amanda.conf**\n> Main Amanda configuration file defining backup schedules, tape types, and holding disks.\n\n**/etc/amanda/<config>/disklist**\n> Lists hosts and disks to be backed up for a given configuration."],["CAVEATS","Requires Amanda to be properly installed and configured. Some operations require appropriate permissions. Changes affect the next scheduled backup run."],["HISTORY","**Amanda** was developed at the University of Maryland starting in **1991**. It became one of the most widely used open-source backup solutions for Unix systems, with amadmin providing its administrative interface."],["INSTALL","```apt: sudo apt install amanda-server```\n\n```dnf: sudo dnf install amanda-server```"],["SEE ALSO","[amcheck](/man/amcheck)(8), [amdump](/man/amdump)(8), [amrecover](/man/amrecover)(8)"],["RESOURCES","```[Source code](https://github.com/zmanda/amanda)```\n\n```[Homepage](https://www.amanda.org/)```\n\n```[Documentation](https://manpages.ubuntu.com/manpages/noble/man8/amadmin.8.html)```"]],"e":["amcheck","amdump","amrecover"],"x":"amadmin [config_name] config"},"amarok":{"n":"amarok","t":"Feature-rich KDE music player","s":[["TAGLINE","Feature-rich KDE music player"],["TLDR","**Start** Amarok\n\n```amarok```\n\nOpen with **specific file**\n\n```amarok [music.mp3]```\n\n**Enqueue** files to current playlist\n\n```amarok --enqueue [file1.mp3] [file2.mp3]```\n\n**Play/Pause** currently playing track\n\n```amarok --play-pause```\n\n**Skip** to next track\n\n```amarok --next```\n\nShow **previous** track\n\n```amarok --previous```"],["SYNOPSIS","**amarok** [_options_] [_files_|_urls_]"],["DESCRIPTION","**amarok** is a powerful music player for Linux/KDE with support for many audio formats, podcast management, and integration with online music services. It features a sophisticated collection manager, dynamic playlists, and extensive plugin support.\n\nThe player can scan your music library, fetch album art and lyrics, and provides scriptable interfaces for automation."],["PARAMETERS","**--play**\n> Start playback\n\n**--play-pause**\n> Toggle play/pause\n\n**--pause**\n> Pause playback\n\n**--stop**\n> Stop playback\n\n**--next**\n> Skip to next track\n\n**--previous**\n> Go to previous track\n\n**--enqueue** _files_\n> Add files to playlist without playing\n\n**--append** _files_\n> Append files to playlist\n\n**--load** _files_\n> Load and replace playlist\n\n**-s**, **--stop-after-current**\n> Stop after current track\n\n**--volume** _percent_\n> Set volume level\n\n**--engine** _engine_\n> Use specific audio engine"],["CONFIGURATION","**~/.config/amarokrc** (KF5/Qt5 builds), **~/.kde/share/config/amarokrc** (KDE4 builds)\n> Main configuration file storing player settings, collection paths, and plugin preferences."],["CAVEATS","Heavy on system resources compared to simpler players. Some features require KDE frameworks. Plugin compatibility varies with version."],["HISTORY","**Amarok** was first released in **2003** as a KDE music player. Version 2.0 in 2008 brought a major rewrite. It was one of the most feature-rich Linux music players, though development has slowed in recent years."],["INSTALL","```apt: sudo apt install amarok```\n\n```dnf: sudo dnf install amarok```\n\n```pacman: sudo pacman -S amarok```\n\n```zypper: sudo zypper install amarok```\n\n```nix: nix profile install nixpkgs#amarok```"],["SEE ALSO","[vlc](/man/vlc)(1), [clementine](/man/clementine)(1)"]],"e":["vlc","clementine"],"x":"amarok"},"anki":{"n":"anki","t":"Spaced repetition flashcard program","s":[["TAGLINE","Spaced repetition flashcard program"],["TLDR","**Start** Anki\n\n```anki```\n\nOpen with **specific profile**\n\n```anki -p \"[Profile Name]\"```\n\nImport a **deck**\n\n```anki [deck.apkg]```\n\nOpen in **base directory**\n\n```anki -b [~/.anki2]```"],["SYNOPSIS","**anki** [_-p profile_] [_-b basedir_] [_file_]"],["DESCRIPTION","**anki** is a spaced repetition flashcard program that helps memorize information efficiently. It uses an intelligent scheduling algorithm to show cards at optimal intervals, maximizing retention while minimizing study time.\n\nThe application supports text, images, audio, and video on cards, with extensive customization through card templates and add-ons. It syncs across devices through AnkiWeb."],["PARAMETERS","**-p** _profile_\n> Load specific user profile\n\n**-b** _directory_\n> Use alternate base directory\n\n**-l** _lang_\n> Interface language code\n\n**--syncserver** _url_\n> Custom sync server URL"],["CAVEATS","First startup may be slow while building database. Deck imports can be large. Sync requires AnkiWeb account. Some add-ons may not be compatible with latest versions."],["HISTORY","**Anki** was created by Damien Elmes and first released in **2006**. It's based on the SM2 spaced repetition algorithm developed by Piotr Wozniak. The name comes from the Japanese word for \"memorization.\""],["INSTALL","```pacman: sudo pacman -S anki```\n\n```apk: sudo apk add anki```\n\n```nix: nix profile install nixpkgs#anki```"],["SEE ALSO","[man](/man/man)(1)"]],"e":["man"],"x":"anki"},"arm2hpdl":{"n":"arm2hpdl","t":"wrap an ARM ELF printer firmware image with HP download header/trailer","s":[["TAGLINE","wrap an ARM ELF printer firmware image with HP download header/trailer"],["TLDR","**Convert an ARM firmware image** to an HP `.dl` download file\n\n```arm2hpdl [sihp1005.img] > [sihp1005.dl]```\n\n**Enable debug output**\n\n```arm2hpdl -D [1] [firmware.img] > [firmware.dl]```\n\n**Pipe the result straight to the printer device**\n\n```arm2hpdl [firmware.img] | lp -d [printer]```"],["SYNOPSIS","**arm2hpdl** [**-D** _level_] _arm-binary.img_ > _hpdl.dl_"],["DESCRIPTION","**arm2hpdl** reads an ARM ELF binary firmware image for an HP laser printer (typically files named `sihpXXXX.img` distributed by HP) and writes to stdout the same image framed with the HP download header and trailer expected by the printer's firmware-loader. If the input already carries an HP header the file is copied through unchanged.\n\nThe resulting `.dl` file can be sent to the printer over USB, parallel, or network as a normal print job to flash the firmware."],["PARAMETERS","_arm-binary.img_\n> Input ARM ELF firmware image.\n\n**-D** _level_\n> Set debug level (default `0`). Higher levels print parsing details to stderr."],["CAVEATS","Specific to HP laser printers driven by the **foo2zjs**/QPDL family of tools. Flashing incorrect or mismatched firmware can brick the printer — verify the model and revision before sending the `.dl` file. Output is binary; always redirect to a file (or pipe directly to the printer device), never to a terminal."],["HISTORY","**arm2hpdl** was written by **Rick Richardson** as part of the **foo2zjs** printer driver project, which reverse-engineered HP/Samsung QPDL and ZjStream protocols for Linux."],["INSTALL","```apt: sudo apt install printer-driver-foo2zjs```"],["SEE ALSO","[foo2zjs](/man/foo2zjs)(1), [foo2zjs-wrapper](/man/foo2zjs-wrapper)(1), [lp](/man/lp)(1), [lpr](/man/lpr)(1)"]],"e":["foo2zjs","foo2zjs-wrapper","lp","lpr"],"x":"arm2hpdl [sihp1005.img] > [sihp1005.dl]"},"arthas":{"n":"arthas","t":"Diagnose and troubleshoot Java applications in real time.","s":[["TAGLINE","Diagnose and troubleshoot Java applications in real time."],["TLDR","**Attach** to Java process\n\n```java -jar arthas-boot.jar [PID]```\n\nSelect process **interactively**\n\n```java -jar arthas-boot.jar```\n\nAttach with **specific port**\n\n```java -jar arthas-boot.jar --telnet-port [3658] --http-port [8563] [PID]```"],["SYNOPSIS","**arthas-boot.jar** [_--telnet-port port_] [_--http-port port_] [_pid_]"],["DESCRIPTION","**Arthas** is an Alibaba open-source Java diagnostic tool. It enables real-time monitoring and troubleshooting of Java applications without stopping them or modifying code.\n\nFeatures include method tracing, class decompilation, monitoring metrics, and OGNL expression evaluation on live JVMs."],["PARAMETERS","**pid**\n> Java process ID to attach\n\n**--telnet-port** _port_\n> Telnet console port\n\n**--http-port** _port_\n> HTTP API port\n\n**--target-ip** _ip_\n> IP to bind\n\n**--tunnel-server** _url_\n> Tunnel server URL"],["COMMON COMMANDS","**dashboard**\n> System overview\n\n**trace** _class_ _method_\n> Trace method execution\n\n**watch** _class_ _method_\n> Watch method input/output\n\n**jad** _class_\n> Decompile class\n\n**sc** _pattern_\n> Search loaded classes\n\n**thread**\n> Show thread info\n\n**monitor** _class_ _method_\n> Monitor method stats"],["CONFIGURATION","**~/.arthas/arthas.properties**\n> Default configuration for telnet port, HTTP port, target IP, and tunnel server settings."],["CAVEATS","Attaching to production JVMs should be done carefully. Some features may impact performance. Requires compatible JVM version."],["HISTORY","**Arthas** was developed by Alibaba and open-sourced in **2018**. It has become a popular Java diagnostic tool, especially in the Chinese developer community."],["INSTALL","```aur: yay -S arthas```"],["SEE ALSO","[jstack](/man/jstack)(1), [jmap](/man/jmap)(1)"]],"e":["jstack","jmap"],"x":"java -jar arthas-boot.jar [PID]"},"awless":{"n":"awless","t":"hierarchical AWS CLI with templates and local infrastructure graph","s":[["TAGLINE","hierarchical AWS CLI with templates and local infrastructure graph"],["TLDR","**List** EC2 instances\n\n```awless list instances```\n\n**SSH** to an instance (smart SSH)\n\n```awless ssh [instance-id-or-name]```\n\n**Run** a create template\n\n```awless run [template.awless]```\n\n**Sync** local graph of the account\n\n```awless sync```"],["SYNOPSIS","**awless** *command* [*args*]"],["DESCRIPTION","**awless** is an alternative AWS CLI focusing on a small hierarchical command set, infrastructure templating, local graph sync for offline exploration, and “smart SSH” into instances. It logs cloud-modifying actions for review/revert workflows. Install from GitHub releases or package managers."],["PARAMETERS","**list** *resources*\n\n> List AWS resources of a type.\n\n**create** / **delete** / **update**\n\n> Mutating operations (confirm carefully).\n\n**run** *template*\n\n> Execute an awless template.\n\n**ssh** *target*\n\n> Connect using discovered connection details.\n\n**sync**\n\n> Refresh the local infrastructure graph.\n\nAuth uses standard AWS credentials/regions."],["CAVEATS","Project activity may lag official **aws** CLI features—verify against current AWS APIs. Templates that create resources incur cost. Prefer least-privilege IAM credentials."],["INSTALL","```aur: yay -S awless```"],["SEE ALSO","[aws](/man/aws)(1), [saws](/man/saws)(1)"],["RESOURCES","```[Source code](https://github.com/wallix/awless)```"]],"e":["aws","saws"],"x":"awless list instances"},"az-tag":{"n":"az-tag","t":"Manage tags on Azure resources","s":[["TAGLINE","Manage tags on Azure resources"],["TLDR","**Set the entire tag set** on a resource (acts like init, replaces existing)\n\n```az tag create --resource-id [resource-id] --tags [env=prod team=dev]```\n\n**List tags** on a resource, resource group, or subscription\n\n```az tag list --resource-id [resource-id]```\n\n**Merge tags** into the existing set (add or update)\n\n```az tag update --resource-id [resource-id] --operation Merge --tags [newkey=newvalue]```\n\n**Replace all tags** on a resource\n\n```az tag update --resource-id [resource-id] --operation Replace --tags [key=value]```\n\n**Selectively delete tags** from a resource\n\n```az tag update --resource-id [resource-id] --operation Delete --tags [keytoremove=value]```\n\n**Create a predefined tag name** at the subscription level\n\n```az tag create --name [tagname]```\n\n**Add a predefined value** to a tag name\n\n```az tag add-value --name [tagname] --value [tagvalue]```\n\n**Delete a predefined tag name** from the subscription\n\n```az tag delete --name [tagname]```"],["SYNOPSIS","**az** **tag** _subcommand_ [_options_]"],["SUBCOMMANDS","**create**\n> Create (set) the entire set of tags on a resource, resource group, or subscription. Replaces any existing tags. With only --name, creates a predefined tag name on the subscription.\n\n**delete**\n> Delete the entire set of tags on a resource, or delete a predefined tag name from the subscription.\n\n**list**\n> List the entire set of tags on a resource, resource group, or subscription.\n\n**update**\n> Selectively update tags on a resource using Merge, Replace, or Delete.\n\n**add-value**\n> Create a predefined tag value for a predefined tag name.\n\n**remove-value**\n> Delete a predefined tag value for a predefined tag name."],["PARAMETERS","**--resource-id**\n> The resource identifier for the entity being tagged. A resource, resource group, or subscription may be tagged.\n\n**--tags**\n> Space-separated tags as key=value pairs.\n\n**--operation**\n> Update operation: Merge, Replace, or Delete.\n\n**--name** **-n**\n> Predefined tag name for subscription-level operations.\n\n**--value**\n> Tag value, used with add-value and remove-value.\n\n**--yes** **-y**\n> Do not prompt for confirmation (delete)."],["DESCRIPTION","**az tag** manages Azure resource tags, which are metadata key-value pairs used for organizing and categorizing resources. Tags help with cost management, automation, and governance.\n\nTags can be applied at subscription, resource group, or individual resource level via **--resource-id**. Resources do not inherit tags from their parent resource group. Separately, **--name** and **--value** manage predefined tag names and values at the subscription level (a reusable taxonomy) without attaching them to a resource."],["CAVEATS","Requires Azure CLI to be installed and authenticated. Maximum of 50 tags per resource. Tag names are case-insensitive but tag values are case-sensitive. Some resource types do not support tags."],["INSTALL","```dnf: sudo dnf install azure-cli```\n\n```pacman: sudo pacman -S azure-cli```\n\n```zypper: sudo zypper install azure-cli```\n\n```brew: brew install azure-cli```\n\n```nix: nix profile install nixpkgs#azure-cli```"],["SEE ALSO","[az](/man/az)(1), [az-resource](/man/az-resource)(1), [az-lock](/man/az-lock)(1), [az-provider](/man/az-provider)(1)"],["RESOURCES","```[Source code](https://github.com/Azure/azure-cli)```\n\n```[Documentation](https://learn.microsoft.com/en-us/cli/azure/tag)```\n\n```[Homepage](https://learn.microsoft.com/en-us/cli/azure/)```"]],"e":["az","az-resource","az-lock","az-provider"],"x":"az tag create --resource-id [resource-id] --tags [env=prod team=dev]"},"b4-prep":{"n":"b4-prep","t":"Prepare and manage a patch series for submission","s":[["TAGLINE","Prepare and manage a patch series for submission"],["TLDR","**Start a new tracked branch** for a series\n\n```b4 prep -n [topical-branch-name]```\n\n**Enroll the current branch** for b4 management\n\n```b4 prep -e [base-branch]```\n\n**Seed a new branch** from a previously posted series\n\n```b4 prep -F [message-id]```\n\n**Edit the cover letter** in your editor\n\n```b4 prep --edit-cover```\n\n**Auto-populate To and Cc** from maintainers and git history\n\n```b4 prep --auto-to-cc```\n\n**Run pre-submission checks** on the series\n\n```b4 prep --check```"],["SYNOPSIS","**b4** **prep** [_options_]"],["DESCRIPTION","**b4 prep** sets up and maintains a patch series as a dedicated, b4-tracked git branch. After creating or enrolling a branch, you add commits as usual and reorder or amend them with **git rebase -i**; b4 stores the cover letter and series metadata inside the branch so revisions stay versioned alongside the code.\n\nIt can build the recipient list automatically from MAINTAINERS files and commit history, validate the series against project conventions, and hand a clean series to **b4 send** for delivery. Each time you post, b4 records the revision so later submissions are numbered correctly."],["PARAMETERS","**-n** _NAME_, **--new** _NAME_\n> Create a new b4-tracked branch named _NAME_ for the series.\n\n**-f** _POINT_, **--fork-point** _POINT_\n> Use _POINT_ as the fork point when creating the new branch.\n\n**-e** _[BRANCH]_, **--enroll** _[BRANCH]_\n> Enroll an existing branch for b4 management, tracking against _BRANCH_.\n\n**-F** _MSGID_, **--from-thread** _MSGID_\n> Populate a new branch from a previously posted series.\n\n**--edit-cover**\n> Open the cover letter in your editor.\n\n**--show-revision**\n> Show the current tracked revision of the series.\n\n**--force-revision** _N_\n> Force the series to revision number _N_.\n\n**--auto-to-cc**\n> Collect To and Cc recipients from maintainers and git history.\n\n**--check**\n> Run b4 and project checks against the series.\n\n**--cleanup** _[BRANCH]_\n> Archive and remove a completed prep-tracked branch."],["INSTALL","```dnf: sudo dnf install b4```\n\n```pacman: sudo pacman -S b4```\n\n```apk: sudo apk add b4```\n\n```zypper: sudo zypper install b4```\n\n```brew: brew install b4```\n\n```nix: nix profile install nixpkgs#b4```"],["SEE ALSO","[b4](/man/b4)(1), [b4-send](/man/b4-send)(1), [git-rebase](/man/git-rebase)(1)"],["RESOURCES","```[Documentation](https://b4.docs.kernel.org/)```"]],"e":["b4","b4-send","git-rebase"],"x":"b4 prep -n [topical-branch-name]"},"bemenu":{"n":"bemenu","t":"Dynamic menu for Wayland and X11","s":[["TAGLINE","Dynamic menu for Wayland and X11"],["TLDR","**Display a menu** and output selected item\n\n```echo -e \"option1\\noption2\\noption3\" | bemenu```\n\n**Display a menu with a prompt**\n\n```echo -e \"firefox\\nchromium\\nvivaldi\" | bemenu -p \"Browser:\"```\n\n**Run selected command**\n\n```echo -e \"firefox\\nchromium\" | bemenu | xargs -r swaymsg exec --```\n\n**Use as a program launcher** (with dmenu_path)\n\n```dmenu_path | bemenu | sh```\n\n**Customize appearance**\n\n```bemenu --fn \"monospace 12\" --tb \"#285577\" --tf \"#ffffff\"```\n\n**List installed applications**\n\n```compgen -c | sort -u | bemenu```\n\n**Password prompt** (hidden input)\n\n```bemenu --password -p \"Password:\"```"],["SYNOPSIS","**bemenu** [_options_]\n\n**bemenu-run** [_options_]"],["DESCRIPTION","**bemenu** is a dynamic menu library and client program inspired by dmenu. It reads items from stdin, presents them in a menu, and outputs the selected item to stdout. It supports multiple backends including Wayland, X11, and curses.\n\nUnlike dmenu which only works on X11, bemenu provides native Wayland support, making it ideal for Sway and other Wayland compositors. The rendering is handled through backend libraries, allowing the same interface across different display servers.\n\n**bemenu-run** is a wrapper that lists executables from PATH and runs the selected one, functioning as a simple application launcher."],["PARAMETERS","**-p**, **--prompt** _text_\n> Set the prompt text.\n\n**-i**\n> Case-insensitive matching.\n\n**-l**, **--list** _lines_\n> Show items in a vertical list.\n\n**-b**, **--bottom**\n> Display at bottom of screen.\n\n**-m**, **--monitor** _index_\n> Show on specific monitor.\n\n**-H**, **--line-height** _height_\n> Set item height in pixels.\n\n**--password**\n> Hide input (password mode).\n\n**-w**, **--wrap**\n> Wrap cursor selection.\n\n**-f**, **--filter** _text_\n> Filter items for given text.\n\n**--fn** _font_\n> Set font (Pango format, e.g., \"monospace 12\").\n\n**--tb** _color_\n> Title bar background color.\n\n**--tf** _color_\n> Title bar foreground color.\n\n**--fb** _color_\n> Filter background color.\n\n**--ff** _color_\n> Filter foreground color.\n\n**--nb** _color_\n> Normal item background.\n\n**--nf** _color_\n> Normal item foreground.\n\n**--hb** _color_\n> Highlighted item background.\n\n**--hf** _color_\n> Highlighted item foreground."],["CAVEATS","Requires appropriate backend library for the display server (wayland, x11, or curses). Colors are specified in #RRGGBB format. Font specification follows Pango format. On Wayland, bemenu needs the layer-shell protocol support from the compositor."],["HISTORY","**bemenu** was created as a cross-platform replacement for **dmenu** that could work on Wayland compositors. It was developed as part of the effort to bring traditional X11 utilities to the Wayland ecosystem. The project provides library bindings allowing integration into other applications, following a more modular design than the original dmenu."],["INSTALL","```apt: sudo apt install bemenu```\n\n```dnf: sudo dnf install bemenu```\n\n```pacman: sudo pacman -S bemenu```\n\n```apk: sudo apk add bemenu```\n\n```zypper: sudo zypper install bemenu```\n\n```nix: nix profile install nixpkgs#bemenu```"],["SEE ALSO","[dmenu](/man/dmenu)(1), [rofi](/man/rofi)(1), [fzf](/man/fzf)(1), [wofi](/man/wofi)(1)"]],"e":["dmenu","rofi","fzf","wofi"],"x":"echo -e \"option1\\noption2\\noption3\" | bemenu"},"bindgen":{"n":"bindgen","t":"Generate Rust FFI bindings from C/C++ headers","s":[["TAGLINE","Generate Rust FFI bindings from C/C++ headers"],["TLDR","**Generate Rust bindings** from a C header\n\n```bindgen [input.h] -o [bindings.rs]```\n\n**Generate bindings with allowlist**\n\n```bindgen [input.h] --allowlist-function \"[regex]\" --allowlist-type \"[regex]\" -o [bindings.rs]```\n\n**Generate bindings with blocklist**\n\n```bindgen [input.h] --blocklist-type \"[type_name]\" -o [bindings.rs]```\n\n**Add include path** for headers\n\n```bindgen [input.h] -o [bindings.rs] -- -I[/path/to/includes]```\n\n**Generate bindings** for a C++ header\n\n```bindgen [input.hpp] --enable-cxx-namespaces -o [bindings.rs]```\n\n**Use specific clang arguments**\n\n```bindgen [input.h] -o [bindings.rs] -- -std=c11 -DFOO=1```\n\n**Generate with derive traits**\n\n```bindgen [input.h] --with-derive-default --with-derive-eq -o [bindings.rs]```"],["SYNOPSIS","**bindgen** [_options_] _header_ [**--**] [_clang-args_]"],["DESCRIPTION","**bindgen** automatically generates Rust FFI (Foreign Function Interface) bindings to C and C++ libraries. It parses C/C++ header files using libclang and produces Rust code that allows calling native functions from Rust.\n\nThe generated bindings include function declarations, struct definitions, enums, constants, and type aliases. bindgen handles complex types like function pointers, unions, and opaque types, mapping them to appropriate Rust equivalents.\n\nTypically used in build.rs scripts to generate bindings at compile time, ensuring bindings stay synchronized with library headers. It's a key tool in the Rust ecosystem for interfacing with existing C/C++ code."],["PARAMETERS","**-o** _file_\n> Write output to file instead of stdout.\n\n**--allowlist-function** _regex_\n> Only generate bindings for matching functions.\n\n**--allowlist-type** _regex_\n> Only generate bindings for matching types.\n\n**--blocklist-type** _regex_\n> Exclude matching types from bindings.\n\n**--blocklist-function** _regex_\n> Exclude matching functions.\n\n**--enable-cxx-namespaces**\n> Enable C++ namespace support.\n\n**--with-derive-default**\n> Add Default derive to structs.\n\n**--with-derive-eq**\n> Add Eq derive to applicable types.\n\n**--with-derive-hash**\n> Add Hash derive to applicable types.\n\n**--no-layout-tests**\n> Skip generating layout tests.\n\n**--generate** _items_\n> Specify what to generate (functions, types, vars, methods).\n\n**--opaque-type** _regex_\n> Treat matching types as opaque.\n\n**--wrap-static-fns**\n> Generate wrappers for static and static inline functions.\n\n**--rust-target** _version_\n> Minimum supported Rust version for the generated bindings.\n\n**--**\n> Separator for clang arguments passed to libclang."],["CAVEATS","Requires libclang to be installed. C++ support is more limited than C. Some C constructs like bitfields may need manual adjustment. Generated code may require the libc crate. Build-time generation requires libclang on all build machines. Complex macros are not translated."],["HISTORY","**bindgen** was originally created by **Jyun-Yan You** in **2013** and has been maintained by the Rust project community. It became an official rust-lang project and is now maintained under the **rust-lang/rust-bindgen** repository. The tool has evolved significantly to handle more complex C/C++ patterns and improve the quality of generated Rust code, becoming essential for Rust's interoperability story."],["INSTALL","```apt: sudo apt install bindgen```\n\n```pacman: sudo pacman -S rust-bindgen```\n\n```apk: sudo apk add rust-bindgen```\n\n```zypper: sudo zypper install rust-bindgen```\n\n```brew: brew install bindgen```\n\n```nix: nix profile install nixpkgs#rust-bindgen```"],["SEE ALSO","[cargo](/man/cargo)(1), [rustc](/man/rustc)(1), [cbindgen](/man/cbindgen)(1), [clang](/man/clang)(1)"]],"e":["cargo","rustc","cbindgen","clang"],"x":"bindgen [input.h] -o [bindings.rs]"},"blockout2":{"n":"blockout2","t":"3D falling blocks puzzle game","s":[["TAGLINE","3D falling blocks puzzle game"],["TLDR","**Launch BlockOut II**\n\n```blockout2```"],["SYNOPSIS","**blockout2**"],["DESCRIPTION","**BlockOut II** is a free, open-source 3D Tetris game and adaptation of the original BlockOut DOS game published by California Dreams in 1989. It uses OpenGL for rendering and features an online score database with replay functionality.\n\nThe game involves manipulating falling 3D blocks in a pit, rotating and positioning them to complete horizontal layers. Completed layers are removed, and the goal is to clear as many layers as possible before the pit fills up.\n\n**In-game controls:**\n- **Q, W, E, A, S, D** - Rotate block (also numpad)\n- **Arrow keys** - Move block horizontally\n- **Space** - Drop block\n- **P** - Pause game\n- **Esc** - Return to menu\n\nReplay files are stored in **~/.bl2/replay/** on Linux systems."],["CONFIGURATION","**~/.bl2/blockout.cfg**\n> Game settings including pit dimensions, block set, and display options."],["CAVEATS","Requires OpenGL support. The **BL2_HOME** environment variable may need to be set to the installation directory if running from a custom location."],["HISTORY","The original BlockOut was released in **1989** by California Dreams for DOS. BlockOut II is an open-source recreation that began development in the early 2000s, with version 2.5 adding 64-bit support and unifying the OpenGL renderer across platforms."],["INSTALL","```aur: yay -S blockout2```"],["SEE ALSO","[tetris-bsd](/man/tetris-bsd)(6)"]],"e":["tetris-bsd"],"x":"blockout2"},"bob":{"n":"bob","t":"Manage and switch between Neovim versions","s":[["TAGLINE","Manage and switch between Neovim versions"],["TLDR","**Install a Neovim version**\n\n```bob install [stable]```\n\n**Install nightly build**\n\n```bob install nightly```\n\n**Switch to a version**\n\n```bob use [stable]```\n\n**List installed versions**\n\n```bob list```\n\n**List available remote versions**\n\n```bob list-remote```\n\n**Uninstall a version**\n\n```bob uninstall [nightly]```\n\n**Run a specific version** without switching\n\n```bob run [stable] [file.txt]```\n\n**Sync version** from config file\n\n```bob sync```\n\n**Erase all bob data** and installations\n\n```bob erase```"],["SYNOPSIS","**bob** _command_ [_version_] [_arguments_]"],["DESCRIPTION","**bob** is a cross-platform Neovim version manager that allows easy switching between different Neovim versions from the command line. It can install stable releases, nightly builds, specific versions, or even build from commit hashes.\n\nThe tool manages multiple Neovim installations, automatically downloading and setting up the appropriate binaries for your platform."],["SUBCOMMANDS","**install**\n> Install a specific Neovim version\n\n**use**\n> Switch to a version (auto-installs if needed)\n\n**run**\n> Run a specific version with arguments\n\n**uninstall**\n> Remove an installed version\n\n**list**\n> Show installed and active versions\n\n**list-remote**\n> Show available versions for download\n\n**sync**\n> Install the version pinned in the config file\n\n**update**\n> Update an installed version (use **--all** to update every version)\n\n**rollback**\n> Roll back to a previously installed nightly build\n\n**erase**\n> Remove all bob data and installations\n\n**complete**\n> Generate a shell completion script"],["VERSION SPECIFIERS","**stable**\n> Latest stable release\n\n**nightly**\n> Latest nightly build\n\n**latest**\n> Most recent version\n\n**v0.9.0**\n> Specific version string\n\n**commit-hash**\n> Build from specific commit"],["PARAMETERS","Configuration is read from a JSON or TOML file under the platform config directory (on Linux **~/.config/bob/config.json** or **config.toml**). The **$BOB_CONFIG** environment variable overrides this location."],["CAVEATS","Requires internet access for downloading versions. GitHub API rate limits may apply; set **GITHUB_TOKEN** environment variable to increase limits. The erase command permanently removes all bob data including downloaded versions."],["HISTORY","Bob was created by **Mordechai Hadad** as a modern, cross-platform solution for managing multiple Neovim installations, similar to how nvm manages Node.js versions or pyenv manages Python versions."],["INSTALL","```pacman: sudo pacman -S bob```\n\n```brew: brew install bob```"],["SEE ALSO","[nvim](/man/nvim)(1), [nvm](/man/nvm)(1), [rustup](/man/rustup)(1)"],["RESOURCES","```[Source code](https://github.com/MordechaiHadad/bob)```"]],"e":["nvim","nvm","rustup"],"x":"bob install [stable]"},"calcure":{"n":"calcure","t":"TUI calendar and task manager","s":[["TAGLINE","TUI calendar and task manager"],["TLDR","**Launch** the calendar TUI\n\n```calcure```\n\n**Start** on a specific date\n\n```calcure --date [2024-01-15]```\n\n**Import** events from an iCal file\n\n```calcure --import [path/to/file.ics]```"],["SYNOPSIS","**calcure** [_options_]"],["PARAMETERS","**--date** _DATE_\n> Start on a specific date (YYYY-MM-DD).\n\n**--import** _FILE_\n> Import events from an iCal (.ics) file.\n\n**--config** _FILE_\n> Use a custom configuration file.\n\n**-p**, **--privacy**\n> Launch in privacy mode (hide event details)."],["DESCRIPTION","**calcure** is a terminal-based calendar and task manager with a modern TUI. It displays a monthly calendar view alongside task lists, supporting event creation, recurring events, and task management with priorities and deadlines.\n\nThe interface provides vim-like keybindings for navigation and supports color-coded categories, multiple calendars, and iCal import/export. Events and tasks are stored locally and can be synced through iCal-compatible services."],["CONFIGURATION","Configuration is stored in **~/.config/calcure/config.ini**. Options include color themes, keybindings, default views, calendar data paths, and privacy settings. The data directory defaults to **~/.config/calcure/**."],["CAVEATS","Does not provide built-in calendar sync — external tools are needed for CalDAV or Google Calendar integration. Recurring event support may not cover all iCal recurrence rules."],["HISTORY","**calcure** was created as a modern, visually appealing alternative to terminal calendar tools like **calcurse**. Written in **Python**, it focuses on a clean user interface with minimal dependencies."],["INSTALL","```nix: nix profile install nixpkgs#calcure```"],["SEE ALSO","[calcurse](/man/calcurse)(1), [cal](/man/cal)(1), [taskwarrior](/man/taskwarrior)(1)"]],"e":["calcurse","cal","taskwarrior"],"x":"calcure"},"chalie":{"n":"chalie","t":"Local personal AI companion with persistent memory","s":[["TAGLINE","Local personal AI companion with persistent memory"],["TLDR","**Install** with the official installer\n\n```curl -fsSL https://chalie.ai/install | bash```\n\n**Start** on the default port (31025)\n\n```chalie```\n\n**Start** on a custom port\n\n```chalie --port=[9000]```\n\n**Stop** the daemon\n\n```chalie stop```\n\n**Restart** the daemon\n\n```chalie restart```\n\n**Update** to the latest release\n\n```chalie update```\n\n**Check** whether it is running\n\n```chalie status```\n\n**Follow** the log file\n\n```chalie logs```"],["SYNOPSIS","**chalie** [**--port=**_N_] [**--host=**_H_] [_command_]"],["DESCRIPTION","**chalie** is an open-source personal AI that runs on your machine. It keeps working in the background with persistent memory (episodes, concepts, and abstractions with decay), goal tracking, and proactive research. Actions go through an Allow / Ask / Deny policy so it does not act without consent.\n\nIt is a single Python process (Flask + WebSocket) with SQLite storage (including vector and FTS search). Optional local voice uses ONNX STT/TTS. Features include vision (photos/screenshots), a driven browser, MCP clients and tools, email/calendar/contacts (IMAP, CalDAV, CardDAV), a natural-language scheduler, guarded shell and sandboxed code, and encrypted whole-instance backup. Models can be cloud providers (OpenAI, Anthropic, Gemini) or local via **ollama**. Default web UI: **http://localhost:31025**."],["PARAMETERS","**start**\n\n> Start the daemon in the background (default if no command is given). Writes PID to **~/.chalie/chalie.pid** and logs to **~/.chalie/chalie.log**.\n\n**stop**\n\n> Stop a running instance and remove the PID file.\n\n**restart**\n\n> Stop, then start again with the same port/host options.\n\n**update**\n\n> Stop, re-run the installer for the latest release, then start again.\n\n**status**\n\n> Print whether Chalie is running and its PID.\n\n**logs**\n\n> Follow **~/.chalie/chalie.log** when stdout is a terminal; otherwise print the last 50 lines.\n\n**version**, **-V**, **--version**\n\n> Print the installed version.\n\n**--port=**_N_, **--port** _N_\n\n> Listen port (default: **31025**).\n\n**--host=**_H_, **--host** _H_\n\n> Bind address (default: **0.0.0.0**). Use **127.0.0.1** to keep the UI local-only."],["CONFIGURATION","**~/.chalie/**\n\n> Install home: virtualenv (**venv/**), app source (**app/**), PID and log files.\n\n**~/.chalie/app/data/**\n\n> Persistent instance data (SQLite and related state). Not overwritten by upgrades.\n\n**CHALIE_HOME**\n\n> Override the install home directory (default **$HOME/.chalie**).\n\n**CHALIE_VENV**\n\n> Path to the Python virtualenv used by **run.sh** (set by the installer CLI).\n\nFirst boot opens an onboarding wizard to choose a model provider. Fully local setup: install **ollama** and pull a supported model, then point Chalie at it during setup."],["CAVEATS","Requires Python 3.11+. Beta software: sharp edges are expected. First start may download a large embedding model (~400 MB). Binding to **0.0.0.0** exposes the UI on the network — restrict with **--host=127.0.0.1** or a firewall if the machine is shared. Ensure **~/.local/bin** is on **PATH** so the **chalie** wrapper is found after install."],["HISTORY","**Chalie** is an open-source personal AI companion focused on long-running local agency and restrained action policies rather than single-session chat. It ships under the Apache 2.0 license."],["SEE ALSO","[ollama](/man/ollama)(1), [llm](/man/llm)(1), [aichat](/man/aichat)(1), [sgpt](/man/sgpt)(1)"],["RESOURCES","```[Source code](https://github.com/chalie-ai/chalie)```\n\n```[Homepage](https://chalie.ai)```\n\n```[Documentation](https://chalie.ai/docs)```"]],"e":["ollama","llm","aichat","sgpt"],"x":"curl -fsSL https://chalie.ai/install | bash"},"discord":{"n":"discord","t":"voice, video, and text communication platform","s":[["TAGLINE","voice, video, and text communication platform"],["TLDR","**Launch Discord**\n\n```discord```\n\n**Start minimized** to system tray\n\n```discord --start-minimized```\n\n**Ignore GPU blocklist**\n\n```discord --ignore-gpu-blocklist```\n\n**Disable hardware** acceleration\n\n```discord --disable-gpu```\n\n**Enable verbose logging**\n\n```discord --enable-logging```"],["SYNOPSIS","**discord** [_options_]"],["PARAMETERS","**--start-minimized**\n> Start minimized to system tray.\n\n**--ignore-gpu-blocklist**\n> Ignore GPU hardware blocklist.\n\n**--disable-gpu**\n> Disable hardware acceleration.\n\n**--enable-logging**\n> Enable verbose logging.\n\n**--no-sandbox**\n> Disable sandbox (not recommended).\n\n**--proxy-server** _URL_\n> Use specified proxy server.\n\n**--help**\n> Display help information."],["DESCRIPTION","**Discord** is a voice, video, and text communication platform popular among gaming communities and increasingly used for general collaboration. The Linux client provides access to servers, direct messages, voice channels, and screen sharing.\n\nThe Electron-based application supports all core Discord features including rich presence, push-to-talk, noise suppression, and server management. It integrates with the system tray for background operation.\n\nDiscord on Linux can be installed through official packages, Flatpak, Snap, or distribution repositories. Some features like game detection may have limited functionality compared to Windows."],["CAVEATS","Electron-based with higher resource usage. Screen sharing on Wayland may have limitations. Some game integrations unavailable on Linux. Custom Electron flags can be set in **~/.config/discord-flags.conf** (one flag per line)."],["HISTORY","Discord was founded by **Jason Citron** and released in **2015**. Originally targeting gamers, it expanded to general communities. The Linux client was added in **2017**, providing cross-platform support alongside Windows, macOS, iOS, and Android."],["INSTALL","```pacman: sudo pacman -S discord```\n\n```nix: nix profile install nixpkgs#discord```"],["SEE ALSO","[slack](/man/slack)(1), [mumble](/man/mumble)(1)"]],"e":["slack","mumble"],"x":"discord"},"dive":{"n":"dive","t":"explore Docker image contents and layer efficiency","s":[["TAGLINE","explore Docker image contents and layer efficiency"],["TLDR","**Analyze a Docker image**\n\n```dive [image:tag]```\n\n**Analyze with CI mode**\n\n```dive [image:tag] --ci```\n\n**Build and analyze** in one step\n\n```dive build -t [image:tag] [.]```\n\n**Analyze from tar archive**\n\n```dive --source [docker-archive] [image.tar]```\n\n**Set efficiency threshold** for CI\n\n```dive [image:tag] --ci --highestUserWastedPercent [0.1]```"],["SYNOPSIS","**dive** [_options_] _image_"],["PARAMETERS","_IMAGE_\n> Docker image name with optional tag.\n\n**--ci**\n> CI mode (no TUI, returns exit code).\n\n**--source** _SOURCE_\n> Image source: docker, podman, docker-archive.\n\n**build** [_args_]\n> Build image then analyze.\n\n**--highestUserWastedPercent** _N_\n> Max wasted space percentage for CI pass.\n\n**--lowestEfficiency** _N_\n> Minimum efficiency for CI pass.\n\n**--json**\n> Output results as JSON.\n\n**--help**\n> Display help information."],["DESCRIPTION","**dive** is a tool for exploring Docker image contents and layer efficiency. It provides an interactive TUI showing what changed in each layer, helping identify opportunities to reduce image size.\n\nThe tool displays a layer-by-layer breakdown of the image filesystem, highlighting added, removed, and modified files. The efficiency score indicates how well the image is optimized, with wasted space shown explicitly.\n\ndive integrates into CI pipelines to enforce image efficiency requirements. It can analyze images from Docker, Podman, or archive files, and can build images before analysis."],["CAVEATS","Large images may take time to analyze. Some layer details depend on image build history. Squashed images have limited layer information. Requires container runtime access for live images."],["HISTORY","dive was created by **Alex Goodman** and released in **2018**. It was developed to address the common problem of Docker image bloat by making layer contents visible and quantifying efficiency."],["INSTALL","```pacman: sudo pacman -S dive```\n\n```apk: sudo apk add dive```\n\n```zypper: sudo zypper install dive```\n\n```brew: brew install dive```\n\n```nix: nix profile install nixpkgs#dive```"],["SEE ALSO","[docker](/man/docker)(1), [podman](/man/podman)(1), [skopeo](/man/skopeo)(1)"],["RESOURCES","```[Source code](https://github.com/wagoodman/dive)```"]],"e":["docker","podman","skopeo"],"x":"dive [image:tag]"},"dotnet-restore":{"n":"dotnet-restore","t":"Restore NuGet package dependencies for a .NET project","s":[["TAGLINE","Restore NuGet package dependencies for a .NET project"],["TLDR","**Restore** dependencies for the project in the current directory\n\n```dotnet restore```\n\n**Restore a specific project**\n\n```dotnet restore [project.csproj]```\n\n**Restore from a specific NuGet source**\n\n```dotnet restore --source [https://api.nuget.org/v3/index.json]```\n\n**Restore without using HTTP cache**\n\n```dotnet restore --no-cache```\n\n**Restore for a specific runtime** (RID)\n\n```dotnet restore --runtime [linux-x64]```\n\n**Restore in lock-file mode** (fails if any package version drifts)\n\n```dotnet restore --locked-mode```\n\n**Restore with verbose logging**\n\n```dotnet restore --verbosity detailed```"],["SYNOPSIS","**dotnet** **restore** [_project_|_solution_] [_options_]"],["PARAMETERS","**-s**, **--source** _SOURCE_\n> NuGet package source to use during restore (overrides nuget.config).\n\n**--packages** _DIR_\n> Directory in which to install the restored packages (default: ~/.nuget/packages).\n\n**--no-cache**\n> Don't cache HTTP requests; always re-fetch from the source.\n\n**--no-dependencies**\n> Restore only the root project, ignoring project-to-project references.\n\n**--force**\n> Force all dependencies to be re-resolved even if a cached lock exists.\n\n**--locked-mode**\n> Don't allow updates to **packages.lock.json** — fail if it would change.\n\n**--use-lock-file**\n> Generate or update **packages.lock.json**.\n\n**--runtime** _RID_\n> Target a specific runtime identifier (e.g. **linux-x64**, **win-x86**).\n\n**--configfile** _FILE_\n> NuGet config file to use instead of the default chain.\n\n**--disable-parallel**\n> Disable parallel downloads.\n\n**-v**, **--verbosity** _LEVEL_\n> Verbosity: q[uiet], m[inimal], n[ormal], d[etailed], diag[nostic].\n\n**-?**, **-h**, **--help**\n> Display help."],["DESCRIPTION","**dotnet restore** downloads and installs all NuGet package dependencies declared in a project, solution, or **dotnet-tools.json**. It reads **PackageReference** entries from .csproj/.fsproj/.vbproj files, queries the configured NuGet sources, and writes restored assets to **obj/project.assets.json**.\n\nOther commands like **dotnet build** and **dotnet run** trigger an implicit restore by default, so explicit invocation is mainly useful in CI pipelines (where you want to cache the restore step), when troubleshooting package resolution, or when **--no-restore** is being used downstream."],["CAVEATS","Restore obeys the standard NuGet config chain: machine-wide, user, and per-directory **NuGet.config** files. **--source** replaces (not adds to) configured sources. Setting **--locked-mode** without an existing **packages.lock.json** fails immediately."],["INSTALL","```dnf: sudo dnf install dotnet-host```\n\n```pacman: sudo pacman -S dotnet-host```\n\n```apk: sudo apk add dotnet-host```\n\n```brew: brew install dotnet```"],["SEE ALSO","[dotnet-build](/man/dotnet-build)(1), [dotnet-add-package](/man/dotnet-add-package)(1), [dotnet](/man/dotnet)(1)"]],"e":["dotnet-build","dotnet-add-package","dotnet"],"x":"dotnet restore"},"exfatlabel":{"n":"exfatlabel","t":"get or set an exFAT filesystem label or serial number","s":[["TAGLINE","get or set an exFAT filesystem label or serial number"],["TLDR","**Show volume label**\n\n```exfatlabel [/dev/sda1]```\n\n**Set volume label**\n\n```exfatlabel [/dev/sda1] \"[NEW_LABEL]\"```\n\n**Show volume serial number**\n\n```exfatlabel -i [/dev/sda1]```\n\n**Set volume serial number**\n\n```exfatlabel -i [/dev/sda1] [0x12345678]```"],["SYNOPSIS","**exfatlabel** [_options_] _device_ [_label_]"],["PARAMETERS","**-i**, **--volume-serial**\n> Switch to volume serial mode to get or set the volume serial number.\n\n**-V**\n> Print version number and exit."],["DESCRIPTION","**exfatlabel** displays or sets the volume label or volume serial of an exFAT filesystem. If no label argument is given, displays the current label. Labels can be up to 15 characters (fewer if characters beyond Unicode BMP are used, since labels are stored internally as UTF-16). An empty label (`''`) removes the volume name."],["CAVEATS","The filesystem must be unmounted before changing the label or serial number. Zero is returned on success, non-zero on failure."],["INSTALL","```apt: sudo apt install exfatprogs```\n\n```dnf: sudo dnf install exfatprogs```\n\n```pacman: sudo pacman -S exfatprogs```\n\n```apk: sudo apk add exfatprogs```\n\n```zypper: sudo zypper install exfatprogs```\n\n```nix: nix profile install nixpkgs#exfatprogs```"],["SEE ALSO","[mkfs.exfat](/man/mkfs.exfat)(8), [fsck.exfat](/man/fsck.exfat)(8), [e2label](/man/e2label)(8)"]],"e":["mkfs.exfat","fsck.exfat","e2label"],"x":"exfatlabel [/dev/sda1]"},"exo-compute-sks":{"n":"exo-compute-sks","t":"Exoscale managed Kubernetes cluster management","s":[["TAGLINE","Exoscale managed Kubernetes cluster management"],["TLDR","**List SKS clusters**\n\n```exo compute sks list```\n\n**Create SKS cluster**\n\n```exo compute sks create [cluster_name] --zone [ch-gva-2]```\n\n**Get kubeconfig**\n\n```exo compute sks kubeconfig [cluster_name]```\n\n**Add nodepool**\n\n```exo compute sks nodepool add [cluster_name] [nodepool_name]```\n\n**Scale nodepool**\n\n```exo compute sks nodepool scale [cluster_name] [nodepool_name] [5]```\n\n**Delete cluster**\n\n```exo compute sks delete [cluster_name]```"],["SYNOPSIS","**exo compute sks** _action_ [_options_]"],["PARAMETERS","_ACTION_\n> Operation: list, create, delete, kubeconfig, nodepool.\n\n**create** _NAME_\n> Create new SKS cluster.\n\n**kubeconfig** _NAME_\n> Get kubeconfig for cluster.\n\n**nodepool add** _CLUSTER_ _POOL_\n> Add nodepool to cluster.\n\n**nodepool scale** _CLUSTER_ _POOL_ _SIZE_\n> Scale nodepool.\n\n**--zone** _ZONE_\n> Availability zone.\n\n**--help**\n> Display help information."],["DESCRIPTION","**exo compute sks** manages Exoscale SKS (Scalable Kubernetes Service) clusters. SKS provides managed Kubernetes with automatic control plane management.\n\nCommands handle cluster lifecycle, nodepool management, and kubeconfig retrieval. Nodepools allow different instance configurations within a cluster.\n\nSKS integrates with Exoscale's infrastructure for storage, networking, and load balancing."],["CAVEATS","Kubernetes clusters incur costs. Control plane is managed but workers are billable. Zone determines available features. kubeconfig provides full cluster access."],["HISTORY","SKS is Exoscale's managed Kubernetes offering, providing CNCF-certified Kubernetes clusters on Exoscale's European cloud infrastructure."],["INSTALL","```dnf: sudo dnf install exo```\n\n```pacman: sudo pacman -S exo```\n\n```apk: sudo apk add exoscale```\n\n```nix: nix profile install nixpkgs#exo```"],["SEE ALSO","[exo](/man/exo)(1), [exo-compute](/man/exo-compute)(1), [kubectl](/man/kubectl)(1)"],["RESOURCES","```[Source code](https://github.com/exoscale/cli)```\n\n```[Homepage](https://www.exoscale.com/)```"]],"e":["exo","exo-compute","kubectl"],"x":"exo compute sks list"},"exports":{"n":"exports","t":"NFS shared directory configuration and management","s":[["TAGLINE","NFS shared directory configuration and management"],["TLDR","**View current exports**\n\n```exportfs -v```\n\n**Reload exports configuration**\n\n```sudo exportfs -ra```\n\n**Export directory temporarily**\n\n```sudo exportfs -o [rw,sync] [client:/path]```\n\n**Unexport directory**\n\n```sudo exportfs -u [client:/path]```"],["SYNOPSIS","**/etc/exports** - NFS exports configuration\n\n**exportfs** [_options_] [_client:/path_]"],["PARAMETERS","**exportfs -r**\n> Re-export all directories.\n\n**exportfs -a**\n> Export all entries in /etc/exports.\n\n**exportfs -u** _client:/path_\n> Unexport directory.\n\n**exportfs -v**\n> Verbose; show current exports."],["EXPORTS FILE FORMAT","```\n/path      client(options)\n/home      192.168.1.0/24(rw,sync)\n/data      *(ro,async)\n/shared    @group(rw,no_root_squash)\n```"],["OPTIONS","**rw** / **ro**\n> Read-write or read-only access.\n\n**sync** / **async**\n> Synchronous or asynchronous writes.\n\n**no_root_squash**\n> Don't map root to anonymous user.\n\n**root_squash**\n> Map root to anonymous user (default).\n\n**all_squash**\n> Map all users to anonymous.\n\n**subtree_check** / **no_subtree_check**\n> Subtree checking.\n\n**secure** / **insecure**\n> Require ports < 1024."],["DESCRIPTION","**/etc/exports** defines which directories are shared via NFS (Network File System). Each line specifies a path and which clients can access it with what options.\n\nThe **exportfs** command manages the export table at runtime, reading from /etc/exports and updating the kernel's export table without restarting NFS services."],["CAVEATS","Changes to /etc/exports require `exportfs -ra` to take effect. Security relies on IP-based access control by default. Consider Kerberos for authenticated NFS. Permission mapping between client and server must be considered."],["SEE ALSO","[nfsd](/man/nfsd)(8)"]],"e":["nfsd"],"x":"exportfs -v"},"fisher":{"n":"fisher","t":"plugin manager for fish shell","s":[["TAGLINE","plugin manager for fish shell"],["TLDR","**Install plugin**\n\n```fisher install [jorgebucaran/nvm.fish]```\n\n**List installed plugins**\n\n```fisher list```\n\n**Update all plugins**\n\n```fisher update```\n\n**Remove plugin**\n\n```fisher remove [jorgebucaran/nvm.fish]```\n\n**Update specific plugin**\n\n```fisher update [jorgebucaran/nvm.fish]```"],["SYNOPSIS","**fisher** _command_ [_plugins_...]"],["PARAMETERS","_COMMAND_\n> Operation: install, remove, update, list.\n\n_PLUGINS_\n> Plugin names (github/repo format).\n\n**install** _PLUGIN_\n> Install one or more plugins.\n\n**remove** _PLUGIN_\n> Uninstall plugins.\n\n**update**\n> Update installed plugins.\n\n**list**\n> List installed plugins."],["CONFIGURATION","**~/.config/fish/fish_plugins**\n> List of installed plugins tracked for version control and bulk installation."],["DESCRIPTION","**fisher** is a plugin manager for the fish shell. It installs plugins from GitHub repositories, local directories, or URLs, extending fish with themes, functions, and completions.\n\nThe manager is minimal with no configuration file required. It stores plugin references in a fish_plugins file for version control and can bulk install from this file.\n\nfisher handles plugin dependencies and provides fast installation through parallel downloads."],["CAVEATS","Fish shell only. Some plugins may conflict. No automatic dependency resolution."],["HISTORY","fisher was created by **Jorge Bucaran** as a minimal, fast plugin manager for fish. It replaced the older Oh My Fish framework approach with a simpler, more direct installation method."],["INSTALL","```pacman: sudo pacman -S fisher```\n\n```apk: sudo apk add fisher```\n\n```brew: brew install fisher```"],["SEE ALSO","[fish](/man/fish)(1), [omf](/man/omf)(1)"],["RESOURCES","```[Source code](https://github.com/jorgebucaran/fisher)```"]],"e":["fish","omf"],"x":"fisher install [jorgebucaran/nvm.fish]"},"foo2hp":{"n":"foo2hp","t":"PBM to HP ZjStream converter","s":[["TAGLINE","PBM to HP ZjStream converter"],["TLDR","**Convert PBM to** HP format\n\n```foo2hp [options] < [input.pbm] > [output.prn]```\n\n**Set resolution**\n\n```foo2hp -r [600] < [input.pbm] > [output.prn]```\n\n**Set paper size**\n\n```foo2hp -p [letter] < [input.pbm] > [output.prn]```\n\n**Color mode**\n\n```foo2hp -c < [input.ppm] > [output.prn]```"],["SYNOPSIS","**foo2hp** [_options_] < _input_ > _output_"],["PARAMETERS","**-r** _DPI_\n> Resolution (300, 600, 1200).\n\n**-p** _PAPER_\n> Paper size (letter, a4, legal).\n\n**-c**\n> Color mode (requires PPM input).\n\n**-m** _MEDIA_\n> Media type.\n\n**-n** _COPIES_\n> Number of copies.\n\n**-d** _DUPLEX_\n> Duplex mode.\n\n**--help**\n> Display help information."],["DESCRIPTION","**foo2hp** converts PBM/PPM images to HP's proprietary ZjStream format for certain HP Color LaserJet printers. It enables printing on printers that lack native Linux drivers.\n\nThe tool handles raster-to-printer protocol conversion, managing page setup, color handling, and HP-specific encoding. It works as a CUPS filter backend.\n\nfoo2hp supports HP Color LaserJet 1600, 2600n, and similar models using ZjStream protocol."],["CAVEATS","Only for specific HP printer models. Quality depends on input resolution. May not support all printer features."],["HISTORY","foo2hp is part of **foo2zjs**, Rick Richardson's open source driver collection. It reverse-engineered HP's ZjStream protocol to provide Linux support for GDI-based HP printers."],["INSTALL","```apt: sudo apt install printer-driver-foo2zjs```\n\n```dnf: sudo dnf install foo2hp```"],["SEE ALSO","[foo2hp2600-wrapper](/man/foo2hp2600-wrapper)(1), [foo2zjs](/man/foo2zjs)(1)"],["RESOURCES","```[Source code](https://github.com/OpenPrinting/foo2zjs)```"]],"e":["foo2hp2600-wrapper","foo2zjs"],"x":"foo2hp [options] < [input.pbm] > [output.prn]"},"fzputtygen":{"n":"fzputtygen","t":"SSH key generator and format converter","s":[["TAGLINE","SSH key generator and format converter"],["TLDR","**Generate SSH key** pair\n\n```fzputtygen -t [rsa] -o [key.ppk]```\n\n**Convert OpenSSH to** PuTTY format\n\n```fzputtygen [key] -o [key.ppk]```\n\n**Export public key**\n\n```fzputtygen [key.ppk] -L```\n\n**Convert PuTTY to** OpenSSH\n\n```fzputtygen [key.ppk] -O private-openssh -o [key]```\n\n**Change passphrase**\n\n```fzputtygen [key.ppk] -P```"],["SYNOPSIS","**fzputtygen** [_options_] [_keyfile_]"],["PARAMETERS","_KEYFILE_\n> Key file to process.\n\n**-t** _TYPE_\n> Key type: rsa, dsa, ecdsa, ed25519.\n\n**-o** _FILE_\n> Output file.\n\n**-O** _FORMAT_\n> Output format.\n\n**-L**\n> Output public key.\n\n**-P**\n> Change passphrase.\n\n**--help**\n> Display help information."],["DESCRIPTION","**fzputtygen** is FileZilla's key generator and converter based on PuTTYgen. It creates SSH keys and converts between PuTTY (.ppk) and OpenSSH formats.\n\nThe tool generates RSA, DSA, ECDSA, and Ed25519 keys for SSH authentication. It handles format conversion needed when switching between PuTTY and OpenSSH clients.\n\nfzputtygen enables key management for FileZilla SFTP connections."],["CAVEATS","PPK format not universal. Passphrase protection recommended. Key type support varies."],["HISTORY","fzputtygen is adapted from **PuTTYgen** for use with FileZilla. It provides key generation and format conversion supporting FileZilla's SFTP functionality."],["INSTALL","```apt: sudo apt install filezilla```\n\n```dnf: sudo dnf install filezilla```\n\n```pacman: sudo pacman -S filezilla```\n\n```apk: sudo apk add filezilla```\n\n```zypper: sudo zypper install filezilla```\n\n```nix: nix profile install nixpkgs#filezilla```"],["SEE ALSO","[ssh-keygen](/man/ssh-keygen)(1), [filezilla](/man/filezilla)(1)"]],"e":["ssh-keygen","filezilla"],"x":"fzputtygen -t [rsa] -o [key.ppk]"},"git-squash":{"n":"git-squash","t":"Squash a branch into a single commit","s":[["TAGLINE","Squash a branch into a single commit"],["TLDR","**Squash branch into current** (leaves changes staged but uncommitted)\n\n```git squash [branch]```\n\n**Squash with message** (commits the result)\n\n```git squash [branch] \"[message]\"```\n\n**Squash the last N commits** on current branch\n\n```git squash HEAD~[5] \"[message]\"```\n\n**Squash with concatenated commit messages**\n\n```git squash --squash-msg [branch]```"],["SYNOPSIS","**git** **squash** [**--squash-msg**] _source-branch|commit-ref_ [_message_]"],["PARAMETERS","**--squash-msg**\n> Commit the squash result with the concatenated messages of all squashed commits. Cannot be used together with a commit message argument."],["DESCRIPTION","**git squash** merges all commits from a specified branch or commit range into the current branch as a single squashed commit. Part of git-extras, it is useful for cleaning up feature branch history before merging into the main branch. When a commit ref from the current branch is given, the range from that ref to HEAD is squashed. If no commit message is provided and **--squash-msg** is not used, changes are staged but not committed."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-merge](/man/git-merge)(1), [git-rebase](/man/git-rebase)(1), [git-extras](/man/git-extras)(1)"]],"e":["git-merge","git-rebase","git-extras"],"x":"git squash [branch]"},"gn":{"n":"gn","t":"Meta-build system that generates Ninja files","s":[["TAGLINE","Meta-build system that generates Ninja files"],["TLDR","**Generate** Ninja build files into an output directory\n\n```gn gen [out/Default]```\n\n**Generate** with build arguments inline\n\n```gn gen [out/Default] --args='is_debug=true target_cpu=\"x64\"'```\n\n**Open** the args editor for an output directory\n\n```gn args [out/Default]```\n\n**List** all available build arguments and their defaults\n\n```gn args [out/Default] --list```\n\n**Describe** a build target (deps, sources, configs)\n\n```gn desc [out/Default] [//base:base]```\n\n**List** every build target known to the build\n\n```gn ls [out/Default]```\n\n**Check** include rules without writing build files\n\n```gn check [out/Default]```\n\n**Format** a BUILD.gn file in place\n\n```gn format [path/to/BUILD.gn]```"],["SYNOPSIS","**gn** _command_ [_options_]... [_args_]..."],["PARAMETERS","**gen** _out_dir_ [**--args=**_string_]\n> Generate Ninja build files for _out_dir_. **--args** sets build arguments inline; arguments are stored in _out_dir_/args.gn.\n\n**args** _out_dir_ [**--list**[=_arg_]] [**--short**] [**--overrides-only**]\n> Open _out_dir_/args.gn in **$EDITOR**, or list arguments. **--list** shows defaults and help text; pass an argument name for a single entry.\n\n**desc** _out_dir_ _target_ [_what_]\n> Print information about _target_. _what_ may be **sources**, **deps**, **configs**, **public**, **defines**, **include_dirs**, **runtime_deps**, etc.\n\n**ls** _out_dir_ [_label_pattern_]\n> List matching targets. Pattern supports wildcards such as **//base/\\***.\n\n**refs** _out_dir_ _target_ [**--all**]\n> Show targets that depend on _target_.\n\n**path** _out_dir_ _target1_ _target2_\n> Show a dependency path between two targets.\n\n**check** _out_dir_ [_label_pattern_]\n> Run include-rule checking. Equivalent to **gn gen --check** without writing build files.\n\n**format** [**--dry-run**] [**--stdin**] _file.gn_\n> Reformat a GN file in canonical style.\n\n**clean** _out_dir_\n> Delete the contents of _out_dir_ except **args.gn**, then re-run **ninja** to repopulate.\n\n**help** [_command_]\n> Print general help, or detailed help for a single command, target type, or built-in.\n\n**-q**\n> Suppress informational output.\n\n**--root=**_path_\n> Override the source root (where the **.gn** file lives).\n\n**--dotfile=**_path_\n> Use a non-default **.gn** dotfile.\n\n**--script-executable=**_path_\n> Override the Python interpreter used by **exec_script**.\n\n**--time**\n> Print timing information for build steps.\n\n**--tracelog=**_file_\n> Write a Chrome-trace-format log of GN's execution."],["DESCRIPTION","**gn** is a meta-build system used by **Chromium**, **Fuchsia**, **V8**, **Skia**, **ANGLE**, **Dart**, and other large native projects. It reads **.gn** dotfiles and **BUILD.gn** files written in a small declarative language, then emits **Ninja** build files for fast incremental builds.\n\nTargets are declared in **BUILD.gn** files using rules such as **executable**, **static_library**, **shared_library**, **source_set**, **group**, and **action**. Targets reference other targets via **deps** (private) and **public_deps**, and pull in compile flags via **configs**, **public_configs**, **defines**, **include_dirs**, and **sources**. Properties can be appended or removed per-target with **+=** and **-=**.\n\nBuild configuration lives in **args.gn** inside each output directory, allowing many parallel build configurations (debug, release, cross-compile, instrumented) without re-checking out the source tree. Cross-compilation is configured through **target_os** and **target_cpu** arguments."],["COMMON ARGS","```\nis_debug          Debug vs. release build\nis_component_build  Build with shared libraries\ntarget_os         \"linux\", \"mac\", \"win\", \"android\", ...\ntarget_cpu        \"x64\", \"arm64\", \"x86\", \"arm\", ...\nsymbol_level      0=none, 1=minimal, 2=full\ntreat_warnings_as_errors\n```\n\nUse **gn args [out/Default] --list** to see every argument exposed by the project."],["CONFIGURATION","Each project has a **.gn** dotfile at the source root that points to the **BUILDCONFIG.gn** file and other defaults. Per-build-directory configuration is stored in **<out_dir>/args.gn** and edited with **gn args**. The **gn format** command enforces a single canonical style for **BUILD.gn** files; many projects run it as a presubmit check."],["CAVEATS","**gn** does not run the build itself - it only generates Ninja files. Run **ninja -C [out/Default]** afterwards. The **BUILD.gn** language is **not** Python: although it looks similar, it is intentionally restricted (no classes, no recursion, deterministic execution). Editing **args.gn** by hand is supported, but running **gn gen** afterwards is required to regenerate Ninja files.\n\nFor Chromium and Chromium-derived projects, **gn** is shipped via **depot_tools**; system-wide installs may lag behind the version expected by the source tree."],["HISTORY","**GN** (\"Generate Ninja\") was created by **Brett Wilson** at **Google** as a faster replacement for **GYP** in the **Chromium** build. It first appeared in the Chromium tree around **2014** and progressively replaced GYP through **2017**. GN is now a standalone project hosted at **gn.googlesource.com/gn** and is used as the primary build system for Chromium, Fuchsia, V8, and several other large open-source codebases."],["INSTALL","```apt: sudo apt install generate-ninja```\n\n```dnf: sudo dnf install gn```\n\n```pacman: sudo pacman -S gn```\n\n```apk: sudo apk add gn```\n\n```zypper: sudo zypper install gn```\n\n```nix: nix profile install nixpkgs#gn```"],["SEE ALSO","[ninja](/man/ninja)(1), [cmake](/man/cmake)(1), [bazel](/man/bazel)(1), [meson](/man/meson)(1)"]],"e":["ninja","cmake","bazel","meson"],"x":"gn gen [out/Default]"},"gow":{"n":"gow","t":"File watcher for Go commands","s":[["TAGLINE","File watcher for Go commands"],["TLDR","**Run with auto-reload**\n\n```gow run [.]```\n\n**Run specific file**\n\n```gow run [main.go]```\n\n**Run tests with watching**\n\n```gow test [./...]```\n\n**Clear the terminal on every restart**\n\n```gow -c run [.]```\n\n**Watch additional file extensions besides .go**\n\n```gow -e=go,mod,html run [.]```"],["SYNOPSIS","**gow** [_flags_] _command_ [_args_]"],["PARAMETERS","_COMMAND_\n> Any go subcommand to wrap and re-run (run, test, build, vet, ...).\n\n**-c**\n> Clear the terminal before each restart.\n\n**-e** _ext,ext,..._\n> File extensions to watch. Default: go.\n\n**-v**\n> Verbose output, printing what triggered each restart.\n\n**--help**\n> Display help information."],["DESCRIPTION","**gow** (\"go watch\") is invoked exactly like **go**, but additionally watches the Go source tree and reruns the given command whenever a watched file changes. It wraps go run, go test, go build, and other subcommands transparently, ignoring non-Go files by default.\n\nIt uses a recursive, event-driven watcher (no polling) so restarts are near-instant, clears leftover output between runs, and supports hotkeys such as **ctrl+r** to force a restart."],["CAVEATS","Development tool only; not intended for production process supervision. Watches recursively from the current directory by default."],["HISTORY","gow was created by **Nelo Mitranim** as a lightweight replacement for his earlier **gorun** tool, adding a better file watcher and hotkey support for Go development workflows."],["INSTALL","```nix: nix profile install nixpkgs#gow```"],["SEE ALSO","[go](/man/go)(1), [air](/man/air)(1), [goreload](/man/goreload)(1)"],["RESOURCES","```[Source code](https://github.com/mitranim/gow)```"]],"e":["go","air","goreload"],"x":"gow run [.]"},"grpconv":{"n":"grpconv","t":"convert to shadow group format","s":[["TAGLINE","convert to shadow group format"],["TLDR","**Convert to shadow groups**\n\n```sudo grpconv```\n\n**Check current status first**\n\n```ls -la /etc/gshadow```"],["SYNOPSIS","**grpconv** [_options_]"],["PARAMETERS","**-R** _DIR_\n> Chroot directory.\n\n**--help**\n> Display help information."],["DESCRIPTION","**grpconv** converts from traditional group format to shadow group format. It creates /etc/gshadow from /etc/group information.\n\nThe tool moves encrypted group passwords from /etc/group to /etc/gshadow, improving security by limiting access to password data."],["CAVEATS","Requires root access. Run grpck first to verify integrity. Irreversible without backup."],["HISTORY","grpconv was developed as part of the **shadow** password suite to improve group password security."],["INSTALL","```apt: sudo apt install passwd```\n\n```apk: sudo apk add shadow-conv```"],["SEE ALSO","[grpunconv](/man/grpunconv)(8), [grpck](/man/grpck)(8), [pwconv](/man/pwconv)(8)"],["RESOURCES","```[Source code](https://github.com/shadow-maint/shadow)```"]],"e":["grpunconv","grpck","pwconv"],"x":"sudo grpconv"},"gst-typefind-1.0":{"n":"gst-typefind-1.0","t":"Print media type of file","s":[["TAGLINE","Print media type of file"],["TLDR","**Determine the media type** of a file\n\n```gst-typefind-1.0 [path/to/file]```\n\n**Detect media types** of multiple files\n\n```gst-typefind-1.0 [path/to/file1] [path/to/file2]```\n\n**Use a custom plugin path** for type detection\n\n```gst-typefind-1.0 --gst-plugin-path=[path/to/plugins] [path/to/file]```"],["SYNOPSIS","**gst-typefind-1.0** [_options_] _files_"],["PARAMETERS","**--gst-plugin-path=**_PATH_\n> Add colon-separated directories to the GStreamer plugin search path.\n\n**--gst-plugin-spew**\n> Enable printout of errors while loading GStreamer plugins.\n\n**--gst-debug-level=**_LEVEL_\n> Set the debugging output threshold (0-9, default 0).\n\n**--gst-info-mask=**_FLAGS_\n> Set GStreamer info flags.\n\n**--gst-debug-mask=**_FLAGS_\n> Set GStreamer debugging flags.\n\n**--gst-mask=**_FLAGS_\n> Set both GStreamer info and debugging flags simultaneously.\n\n**--help**\n> Display help information."],["DESCRIPTION","**gst-typefind-1.0** is a command-line tool from the GStreamer multimedia framework that identifies the media type (MIME type) of files. It uses GStreamer's typefind subsystem, which examines file headers and content patterns through installed plugins to determine what kind of media a file contains.\n\nThe tool applies a probability-based detection system where plugins report confidence levels for their media type matches. It is specifically designed for multimedia files that GStreamer can handle, rather than being a general-purpose file type detector."],["CAVEATS","Detection accuracy depends entirely on which GStreamer plugins are installed. If the plugin for a particular media format is missing, the file may not be identified correctly or at all. This tool is not a replacement for the general-purpose **file** command, as it only recognizes media types supported by the installed GStreamer plugin ecosystem."],["HISTORY","**gst-typefind-1.0** is part of the **GStreamer** multimedia framework, originally created for the GNOME project. The **1.0** suffix was introduced with the GStreamer 1.0 release in **September 2012**, allowing parallel installation with the older 0.10 series. The typefind subsystem is a core part of GStreamer's architecture, enabling automatic media format detection across the framework."],["INSTALL","```apt: sudo apt install gstreamer1.0-tools```\n\n```apk: sudo apk add gstreamer-tools```"],["SEE ALSO","[gst-inspect-1.0](/man/gst-inspect-1.0)(1), [gst-launch-1.0](/man/gst-launch-1.0)(1), [file](/man/file)(1)"]],"e":["gst-inspect-1.0","gst-launch-1.0","file"],"x":"gst-typefind-1.0 [path/to/file]"},"guetzli":{"n":"guetzli","t":"Perceptual JPEG encoder that optimizes for visual quality at small file sizes","s":[["TAGLINE","Perceptual JPEG encoder that optimizes for visual quality at small file sizes"],["TLDR","**Re-encode a JPEG**\n\n```guetzli [input.jpg] [output.jpg]```\n\n**Encode a PNG to JPEG**\n\n```guetzli [input.png] [output.jpg]```\n\n**Set quality level (84-100)**\n\n```guetzli --quality [90] [input.jpg] [output.jpg]```\n\n**Maximum compression (lowest supported quality)**\n\n```guetzli --quality [84] [input.jpg] [output.jpg]```\n\n**Show progress trace**\n\n```guetzli --verbose [input.png] [output.jpg]```"],["SYNOPSIS","**guetzli** [**--quality** _Q_] [**--verbose**] _input_ _output.jpg_"],["PARAMETERS","_input_\n> Source image. PNG and JPEG inputs are supported.\n\n_output.jpg_\n> Destination JPEG file.\n\n**--quality** _Q_\n> Visual quality, expressed in libjpeg-equivalent units. Range 84-100, default 95. Values below 84 are not supported.\n\n**--verbose**\n> Print a trace of encoding attempts."],["DESCRIPTION","**Guetzli** is a JPEG encoder that targets the human visual system. It uses Butteraugli, a psychovisual model, to search the JPEG quantization space and produce baseline-compatible JPEG files that are typically 20-30% smaller than equivalent libjpeg output at the same perceived quality.\n\nOutput files are compatible with all standard JPEG decoders. The trade-off is encoding cost: guetzli is computationally and memory intensive."],["CAVEATS","Very slow: encoding a single megapixel can take a minute or more. Memory usage is roughly 300 MB per megapixel. Only quality values 84-100 are accepted. Best results come from non-degraded sources (PNG or high-quality JPEG); re-encoding heavily compressed JPEGs offers limited gains."],["HISTORY","**Guetzli** was developed by **Google Research** and released as open source in **March 2017** alongside the Butteraugli perceptual distance metric."],["INSTALL","```apt: sudo apt install guetzli```\n\n```pacman: sudo pacman -S guetzli```\n\n```apk: sudo apk add guetzli```\n\n```brew: brew install guetzli```\n\n```nix: nix profile install nixpkgs#guetzli```"],["SEE ALSO","[cjpeg](/man/cjpeg)(1), [jpegoptim](/man/jpegoptim)(1)"]],"e":["cjpeg","jpegoptim"],"x":"guetzli [input.jpg] [output.jpg]"},"hr":{"n":"hr","t":"prints horizontal rules in the terminal","s":[["TAGLINE","prints horizontal rules in the terminal"],["TLDR","**Print a horizontal rule using the default # character**\n\n```hr```\n\n**Print a horizontal rule with a specific character**\n\n```hr [=]```\n\n**Print multiple horizontal rules with different characters**\n\n```hr [#] [-]```\n\n**Print a horizontal rule with a multi-character pattern**\n\n```hr [-#-]```"],["SYNOPSIS","**hr** [_characters_ ...]"],["DESCRIPTION","**hr** prints horizontal rules spanning the terminal width. Each argument is used as a character or pattern to repeat across one line. If no argument is given, **#** is used by default. Multiple arguments produce multiple lines, one per argument.\n\nThe terminal width is determined by `tput cols`, falling back to the **COLUMNS** environment variable or 80 columns if neither is available."],["CAVEATS","Multiple implementations of **hr** exist with different features. The most common (LuRsT/hr) is a pure Bash script with no flags or options. Other implementations may support additional features like color or width control."],["INSTALL","```apk: sudo apk add hr```\n\n```brew: brew install hr```\n\n```nix: nix profile install nixpkgs#hr```"],["SEE ALSO","[echo](/man/echo)(1), [printf](/man/printf)(1), [tput](/man/tput)(1)"]],"e":["echo","printf","tput"],"x":"hr"},"hyprsunset":{"n":"hyprsunset","t":"blue light filter for Hyprland, the dynamic tiling Wayland compositor","s":[["TAGLINE","blue light filter for Hyprland, the dynamic tiling Wayland compositor"],["TLDR","**Start** the blue light filter service\n\n```hyprsunset```\n\nStart with specific **temperature** and **gamma**\n\n```hyprsunset -t [4000] -g [90]```\n\n**Adjust temperature** while running (via hyprctl)\n\n```hyprctl hyprsunset temperature [4000]```\n\n**Adjust gamma** while running (via hyprctl)\n\n```hyprctl hyprsunset gamma [90]```\n\n**Reset** temperature to default (6000K)\n\n```hyprctl hyprsunset reset temperature```\n\n**Reset** gamma to default (100%)\n\n```hyprctl hyprsunset reset gamma```"],["SYNOPSIS","**hyprsunset** [_options_]"],["PARAMETERS","**-t**, **--temperature** _KELVIN_\n> Set color temperature in Kelvin (lower is warmer, default: 6000K)\n\n**-g**, **--gamma** _PERCENT_\n> Set gamma percentage (default: 100%)\n\n**--identity**\n> Disable temperature adjustments; only gamma changes take effect"],["DESCRIPTION","**hyprsunset** is a blue light filter for Hyprland, the dynamic tiling Wayland compositor. It adjusts screen color temperature to reduce blue light emission, which can help reduce eye strain and improve sleep quality during evening computer use.\n\nOnce running, settings can be adjusted dynamically using hyprctl commands without restarting the service. This method is preferred over screen shaders as it will not be captured via recording or screenshots.\n\nLower temperature values (e.g., 3000K-4000K) produce warmer, more orange-tinted colors suitable for night use. The default 6000K represents standard daylight white."],["CAVEATS","Only works with Hyprland compositor. Requires Hyprland to be running. Changes affect all monitors. May interfere with color-accurate work like photo editing."],["HISTORY","hyprsunset is part of the Hyprland ecosystem, created by Vaxry. It provides functionality similar to redshift or gammastep but specifically designed for the Hyprland Wayland compositor."],["INSTALL","```pacman: sudo pacman -S hyprsunset```\n\n```zypper: sudo zypper install hyprsunset```\n\n```nix: nix profile install nixpkgs#hyprsunset```"],["SEE ALSO","[hyprctl](/man/hyprctl)(1), [redshift](/man/redshift)(1), [gammastep](/man/gammastep)(1)"]],"e":["hyprctl","redshift","gammastep"],"x":"hyprsunset"},"ipsumdump":{"n":"ipsumdump","t":"summarizes network traffic by extracting and displaying selected","s":[["TAGLINE","summarizes network traffic by extracting and displaying selected"],["TLDR","**Dump summary of packets** from a pcap file\n\n```ipsumdump -r [capture.pcap]```\n\n**Capture live traffic** and summarize\n\n```sudo ipsumdump -i [eth0]```\n\n**Extract source and destination IPs**\n\n```ipsumdump -r [capture.pcap] --src --dst```\n\n**Output timestamp, source, destination, and protocol**\n\n```ipsumdump -r [capture.pcap] --timestamp --src --dst --proto```\n\n**Filter packets** using BPF expression\n\n```ipsumdump -r [capture.pcap] --filter '[tcp port 80]' --src --dst```\n\n**Output in specific format** with custom fields\n\n```ipsumdump -r [capture.pcap] --src --sport --dst --dport --length```"],["SYNOPSIS","**ipsumdump** [_-r file_] [_-i interface_] [_--filter expression_] [_--output fields_] [_options_]"],["PARAMETERS","**-r** _file_\n> Read from pcap file\n\n**-i** _interface_\n> Capture live traffic from interface\n\n**--filter** _expression_\n> BPF filter expression\n\n**--src**\n> Output source IP address\n\n**--dst**\n> Output destination IP address\n\n**--sport**\n> Output source port\n\n**--dport**\n> Output destination port\n\n**--proto**\n> Output IP protocol\n\n**--timestamp**\n> Output packet timestamp\n\n**--length**\n> Output packet length\n\n**--payload**\n> Output payload data\n\n**--no-promiscuous**\n> Don't set interface to promiscuous mode\n\n**-o** _file_\n> Write output to file"],["DESCRIPTION","**ipsumdump** summarizes network traffic by extracting and displaying selected fields from packet headers. It reads from pcap files or captures live traffic, producing compact text output suitable for further processing.\n\nThe tool is designed for network analysis and measurement, producing ASCII output with one line per packet. It's particularly useful for creating datasets for analysis, generating aggregate statistics, or extracting specific packet fields."],["CAVEATS","Live capture requires root/administrator privileges. High traffic volumes may cause packet drops. Output format is text-based; for binary analysis, consider other tools. Part of a suite that includes ipaggcreate and ipaggmanip."],["HISTORY","**ipsumdump** was developed by Eddie Kohler at UCLA (later ICSI) for network measurement research. First released in the early **2000s**, it's built on the Click modular router framework and remains useful for network traffic analysis and research."],["INSTALL","```brew: brew install ipsumdump```"],["SEE ALSO","[tcpdump](/man/tcpdump)(1), [tshark](/man/tshark)(1), [ipaggcreate](/man/ipaggcreate)(1), [ipaggmanip](/man/ipaggmanip)(1)"]],"e":["tcpdump","tshark","ipaggcreate","ipaggmanip"],"x":"ipsumdump -r [capture.pcap]"},"kerberos":{"n":"kerberos","t":"network authentication protocol","s":[["TAGLINE","network authentication protocol"],["TLDR","**Get ticket**\n\n```kinit [principal]```\n\n**List tickets**\n\n```klist```\n\n**Destroy tickets**\n\n```kdestroy```\n\n**Change password**\n\n```kpasswd```\n\n**Forward ticket**\n\n```kinit -f [principal]```\n\n**Renew ticket**\n\n```kinit -R```"],["SYNOPSIS","Kerberos commands: **kinit**, **klist**, **kdestroy**, **kpasswd**"],["PARAMETERS","**kinit** _PRINCIPAL_\n> Get Kerberos ticket.\n\n**klist**\n> List cached tickets.\n\n**kdestroy**\n> Destroy tickets.\n\n**kpasswd**\n> Change Kerberos password.\n\n**-f**\n> Get forwardable ticket.\n\n**-R**\n> Renew existing ticket.\n\n**-l** _LIFETIME_\n> Ticket lifetime."],["DESCRIPTION","**Kerberos** is a network authentication protocol. Clients receive a time-limited Ticket-Granting Ticket (TGT) from a Key Distribution Center (KDC) after presenting credentials, then exchange the TGT for service tickets to access individual network services. All authentication exchanges are encrypted, and the user's password never traverses the network after the initial **kinit**.\n\nThe user-facing CLI is built around four small commands: **kinit** (request a TGT), **klist** (inspect the credential cache), **kdestroy** (clear cached tickets), and **kpasswd** (change the password held by the KDC). Service tickets are obtained transparently by Kerberos-aware applications (SSH, NFSv4, HTTP via SPNEGO, LDAP, SMB)."],["CONFIGURATION","**/etc/krb5.conf**\n> Client configuration: realms, KDC addresses, default principal, forwardable flag, encryption types.\n\n**/etc/krb5.keytab** (or **$KRB5_KTNAME**)\n> Service-side keytab containing long-term keys for daemon principals.\n\n**$KRB5CCNAME**\n> Path or backend for the credentials cache (e.g. **FILE:/tmp/krb5cc_$UID**, **KEYRING:persistent:$UID**, **KCM:**)."],["CAVEATS","Clocks between client, KDC, and target service must agree to within a few minutes (default skew: 5 minutes). DNS forward and reverse records must match the principal name; broken reverse DNS is the most common cause of `KRB_AP_ERR_BAD_INTEGRITY` and `Server not found in Kerberos database` errors."],["HISTORY","Kerberos was developed at **MIT** as part of Project Athena in the late 1980s. **Kerberos v5** (RFC 4120) is the current standard; v4 is obsolete and removed from modern Linux distributions. The two interoperable implementations in widespread use are **MIT Kerberos** and **Heimdal**."],["SEE ALSO","[kinit](/man/kinit)(1), [smbclient](/man/smbclient)(1), [ldapsearch](/man/ldapsearch)(1)"]],"e":["kinit","smbclient","ldapsearch"],"x":"kinit [principal]"},"kismet":{"n":"kismet","t":"Wireless network detector, sniffer, and intrusion detection system","s":[["TAGLINE","Wireless network detector, sniffer, and intrusion detection system"],["TLDR","**Start Kismet** with default settings\n\n```kismet```\n\n**Start Kismet** with a specific capture source\n\n```kismet -c [wlan0]```\n\n**Start with multiple capture sources**\n\n```kismet -c [wlan0] -c [wlan1]```\n\n**Run without the ncurses wrapper** (for scripting or service mode)\n\n```kismet --no-ncurses```\n\n**Specify an alternate configuration file**\n\n```kismet -f [path/to/kismet.conf]```\n\n**Disable all logging**\n\n```kismet -n```\n\n**Specify logging types**\n\n```kismet -T [kismet,pcapng]```\n\n**Use an alternate log directory**\n\n```kismet -p [path/to/logs]```\n\n**Enable debug mode** (for running under GDB)\n\n```kismet --debug```"],["SYNOPSIS","**kismet** [_options_]"],["PARAMETERS","**-c** _SOURCE_\n> Define a data source (e.g., interface name). May be specified multiple times; takes priority over config file sources.\n\n**-f** _FILE_\n> Use an alternate configuration file.\n\n**-n**, **--no-logging**\n> Disable all logging.\n\n**-T** _TYPES_, **--log-types** _TYPES_\n> Override the log types to generate (e.g., kismet, pcapng).\n\n**-t** _TITLE_, **--log-title** _TITLE_\n> Set the log title field used in log filenames.\n\n**-p** _DIR_, **--log-prefix** _DIR_\n> Directory to write logs to.\n\n**--homedir** _PATH_\n> Use an alternate home directory for settings and logs.\n\n**--confdir** _PATH_\n> Use a custom configuration directory.\n\n**--override** _FLAVOR_\n> Apply a named override configuration (e.g., wardrive).\n\n**--no-ncurses**\n> Disable the ncurses wrapper, producing plain text output. Useful for scripts and services.\n\n**--no-line-wrap**\n> Disable line wrapping in terminal messages.\n\n**--no-plugins**\n> Do not load plugins. Useful when debugging a crashing plugin.\n\n**--debug**\n> Enable debug mode: disables crash/backtrace handlers and the ncurses wrapper, for use under GDB.\n\n**--silent**\n> Do not print status messages to the console after startup.\n\n**--daemonize**\n> Fork to the background and run as a daemon.\n\n**-v**, **--version**\n> Print version information.\n\n**-h**, **--help**\n> Display help."],["DESCRIPTION","**kismet** is an 802.11 wireless network detector, sniffer, and intrusion detection system. It works with any wireless card that supports raw monitoring (rfmon) mode and can detect 802.11a/b/g/n/ac networks, as well as Bluetooth, Zigbee, and other radios via capture plugins.\n\nKismet operates passively by collecting packets without transmitting, making it difficult to detect. It identifies networks from beacon frames, decloaks hidden networks over time, and infers non-beaconing networks from data traffic.\n\nModern Kismet (2016+) runs as a single process exposing a web-based UI (default at **http://localhost:2501**) and a REST API. The older **kismet_server**/**kismet_client**/**gpsmap** split is no longer used. Kismet logs to its own SQLite-based **.kismet** format by default, and also supports pcap/pcapng for tools like Wireshark."],["CONFIGURATION","Configuration is primarily read from **kismet.conf** and override files (e.g., **kismet_logging.conf**, **kismet_httpd.conf**) in the system config directory (typically **/etc/kismet/** or **/usr/local/etc/**). Per-user settings live under **~/.kismet/**."],["CAVEATS","Requires a wireless card that supports monitor mode. Often requires root privileges (or CAP_NET_ADMIN/CAP_NET_RAW) to place interfaces in monitor mode. Capturing wireless traffic without authorization may violate laws in many jurisdictions. Channel hopping may miss traffic on other channels during rotation."],["HISTORY","Kismet was created by **Mike Kershaw** (dragorn) with the first version released in **2001**. It became one of the first widely used open-source wireless network detectors. The name comes from the Turkish word for \"fate\" or \"destiny\". The architecture was significantly rewritten between 2016 and 2018, merging the server/client/drone components into a single process with a web UI and REST API."],["INSTALL","```dnf: sudo dnf install kismet```\n\n```pacman: sudo pacman -S kismet```\n\n```zypper: sudo zypper install kismet```\n\n```nix: nix profile install nixpkgs#kismet```"],["SEE ALSO","[aircrack-ng](/man/aircrack-ng)(1), [wireshark](/man/wireshark)(1), [tcpdump](/man/tcpdump)(1)"]],"e":["aircrack-ng","wireshark","tcpdump"],"x":"kismet"},"kodi-send":{"n":"kodi-send","t":"sends commands to Kodi media center","s":[["TAGLINE","sends commands to Kodi media center"],["TLDR","**Send an action**\n\n```kodi-send --action=\"[PlayPause]\"```\n\n**Send to a specific host**\n\n```kodi-send --host=[192.168.1.100] --action=\"[Stop]\"```\n\n**Send a notification**\n\n```kodi-send --notification=\"[Backup complete]\"```\n\n**Send multiple actions as a macro with delays**\n\n```kodi-send --action=\"[Up]\" --delay=[500] --action=\"[Select]\"```\n\n**Send a key press event**\n\n```kodi-send --button=\"[KB_Return]\"```\n\n**Open a media URL**\n\n```kodi-send --action=\"PlayMedia([url])\"```"],["SYNOPSIS","**kodi-send** [_options_]"],["PARAMETERS","**-a**, **--action** _ACTION_\n> Action to send. Can be specified multiple times to create a macro.\n\n**--button** _BUTTON_\n> Send a key press event. Can be specified multiple times.\n\n**--host** _HOST_\n> Kodi host address (default localhost).\n\n**--port** _PORT_\n> Kodi EventClient port (default 9777).\n\n**--notification** _MESSAGE_\n> Send a notification to Kodi.\n\n**--log** _MESSAGE_\n> Send a log message to Kodi.\n\n**--loglevel** _LEVEL_\n> Log level when using --log (default LOGDEBUG).\n\n**--mouse** _X,Y_\n> Send mouse position to Kodi.\n\n**-d**, **--delay** _T_\n> Wait for T milliseconds. Can be used between actions to create macros.\n\n**--keymap** _KEYMAP_\n> Specify a custom keymap.\n\n**--help**\n> Display help information."],["DESCRIPTION","**kodi-send** is a command-line tool for remotely controlling a Kodi media center instance over the network. It communicates using the Kodi EventClient UDP protocol on port 9777 by default, sending action commands that simulate remote control button presses, navigation events, and media playback operations.\n\nThe tool supports a wide range of built-in Kodi actions including playback control (play, pause, stop, skip), volume adjustment, menu navigation (directional movement and selection), and media loading via URLs or file paths. Because it operates entirely from the command line, kodi-send is well suited for scripting automated media workflows, integrating Kodi with home automation systems, or building custom remote control interfaces."],["CAVEATS","Requires Kodi EventClient enabled. Network access needed. Part of kodi-eventclients."],["HISTORY","kodi-send is part of **Kodi** (formerly XBMC) event client tools for remote media center control."],["INSTALL","```apt: sudo apt install kodi-eventclients-kodi-send```\n\n```pacman: sudo pacman -S kodi```\n\n```apk: sudo apk add kodi```\n\n```nix: nix profile install nixpkgs#kodi```"],["SEE ALSO","[curl](/man/curl)(1)"]],"e":["curl"],"x":"kodi-send --action=\"[PlayPause]\""},"kscreen-doctor":{"n":"kscreen-doctor","t":"manipulates display settings on KDE Plasma","s":[["TAGLINE","manipulates display settings on KDE Plasma"],["TLDR","Show **display outputs**\n\n```kscreen-doctor --outputs```\n\n**Rotate** display to the right\n\n```kscreen-doctor output.1.rotation.right```\n\nSet display **scale** to 200%\n\n```kscreen-doctor output.HDMI-2.scale.2```\n\n**Enable** a display\n\n```kscreen-doctor output.HDMI-A-1.enable```\n\n**Disable** a display\n\n```kscreen-doctor output.DP-2.disable```\n\nSet **primary** display\n\n```kscreen-doctor output.HDMI-A-1.primary```\n\nSet display **resolution and refresh rate**\n\n```kscreen-doctor output.HDMI-A-1.mode.1920x1080@60```\n\nSet display **position**\n\n```kscreen-doctor output.DP-1.position.1920,0```"],["SYNOPSIS","**kscreen-doctor** [_options_] [_output.ID.property.value_...]"],["DESCRIPTION","**kscreen-doctor** is a command-line tool for manipulating display settings on KDE Plasma desktops. It can enable/disable outputs, set rotation, scaling, resolution, refresh rate, position, and primary display status using a simple dot-notation syntax. Multiple output changes can be specified in a single command invocation. Changes take effect immediately without requiring a restart."],["PARAMETERS","**-o, --outputs**\n> Show display output information\n\n**output.ID.enable**\n> Enable the specified output\n\n**output.ID.disable**\n> Disable the specified output\n\n**output.ID.primary**\n> Set as primary display\n\n**output.ID.rotation.VALUE**\n> Set rotation: none, left, right, inverted\n\n**output.ID.scale.VALUE**\n> Set scaling factor (e.g., 1, 1.5, 2)\n\n**output.ID.mode.WxH@RATE**\n> Set resolution and refresh rate\n\n**output.ID.position.X,Y**\n> Set display position"],["CAVEATS","Output IDs can be numeric (1, 2) or connector names (HDMI-A-1, DP-2). Use `--outputs` to find the correct identifiers. This tool is specific to KDE Plasma and requires the KScreen backend to be running. On X11, xrandr provides similar functionality; on Wayland-only desktops outside KDE, use compositor-specific tools."],["INSTALL","```apt: sudo apt install libkscreen-bin```\n\n```dnf: sudo dnf install libkscreen```\n\n```pacman: sudo pacman -S libkscreen```\n\n```apk: sudo apk add libkscreen```"],["SEE ALSO","[kscreen-console](/man/kscreen-console)(1), [xrandr](/man/xrandr)(1)"]],"e":["kscreen-console","xrandr"],"x":"kscreen-doctor --outputs"},"lbu":{"n":"lbu","t":"manages APK overlay files on diskless Alpine Linux systems","s":[["TAGLINE","manages APK overlay files on diskless Alpine Linux systems"],["TLDR","**Commit** changes to persistent storage\n\n```lbu commit```\n\n**List** files that would be saved\n\n```lbu status```\n\nDisplay **changes** in tracked files\n\n```lbu diff```\n\n**Include** a file or directory in overlay\n\n```lbu include [path/to/file_or_directory]```\n\n**Exclude** a file from overlay\n\n```lbu exclude [path/to/file_or_directory]```\n\nList **manually included/excluded** files\n\n```lbu include -l```\n\nList **backups** (previous overlays)\n\n```lbu list-backup```\n\n**Revert** to a backup overlay\n\n```lbu revert [overlay_filename.tar.gz]```"],["SYNOPSIS","**lbu** _command_ [_arguments_]"],["PARAMETERS","**ci**, **commit**\n> Save changes to persistent storage\n\n**st**, **status**\n> List files that would be committed\n\n**diff**\n> Show changes in tracked files\n\n**inc**, **include** _PATH_\n> Add file/directory to overlay\n\n**ex**, **exclude** _PATH_\n> Remove file/directory from overlay\n\n**lb**, **list-backup**\n> List available backups\n\n**revert** _FILE_\n> Restore a backup overlay"],["DESCRIPTION","**lbu** (Local Backup Utility) manages APK overlay files on diskless Alpine Linux systems. In diskless mode, the root filesystem runs from RAM, and lbu saves configuration changes to persistent storage.\n\nBy default, only /etc is included in the overlay. Other files can be manually included. Changes are not persistent until committed with lbu commit."],["CAVEATS","Specific to Alpine Linux diskless installations. Changes in RAM are lost on reboot if not committed. Overlay storage must be properly configured."],["HISTORY","lbu is part of Alpine Linux's approach to running diskless systems, allowing customizations to be preserved across reboots while keeping the base system read-only."],["INSTALL","```apk: sudo apk add alpine-conf```"],["SEE ALSO","[apk](/man/apk)(1), [setup-alpine](/man/setup-alpine)(8)"]],"e":["apk","setup-alpine"],"x":"lbu commit"},"makoctl-mode":{"n":"makoctl-mode","t":"manages notification modes in the mako notification daemon","s":[["TAGLINE","manages notification modes in the mako notification daemon"],["TLDR","Print **active** mako modes\n\n```makoctl mode```\n\n**Enable** a mode (e.g. do-not-disturb)\n\n```makoctl mode -a [do-not-disturb]```\n\n**Disable** a mode\n\n```makoctl mode -r [do-not-disturb]```\n\n**Toggle** a mode on or off\n\n```makoctl mode -t [do-not-disturb]```\n\nReplace the mode set with an explicit **list**\n\n```makoctl mode -s [mode1] [mode2]```"],["SYNOPSIS","**makoctl mode** [_options_] [_mode_...]"],["PARAMETERS","**-a _mode_**\n> Add a mode to the active set\n\n**-r _mode_**\n> Remove a mode from the active set\n\n**-t _mode_**\n> Toggle a mode (add if inactive, remove if active)\n\n**-s _modes_**\n> Set the exact list of active modes, replacing all current modes"],["DESCRIPTION","**makoctl mode** manages notification modes in the mako notification daemon. Modes allow changing notification behavior based on context—for example, enabling \"do-not-disturb\" to silence notifications during presentations or focus time.\n\nModes are defined in mako's configuration file with specific criteria and actions. When a mode is active, notifications matching that mode's criteria receive different treatment (hidden, grouped, styled differently, etc.).\n\nMultiple modes can be active simultaneously, and their effects are combined according to mako's configuration rules."],["CAVEATS","Mode names must be defined in mako's configuration file before they can be used. Without configuration, modes have no effect. Changes are immediate and persist until explicitly removed or mako restarts."],["INSTALL","```apt: sudo apt install mako-notifier```\n\n```dnf: sudo dnf install mako```\n\n```pacman: sudo pacman -S mako```\n\n```apk: sudo apk add mako```\n\n```zypper: sudo zypper install mako```\n\n```brew: brew install mako```\n\n```nix: nix profile install nixpkgs#mako```"],["SEE ALSO","[makoctl](/man/makoctl)(1), [makoctl-invoke](/man/makoctl-invoke)(1), [mako](/man/mako)(5)"]],"e":["makoctl","makoctl-invoke","mako"],"x":"makoctl mode"},"mcp-postgres":{"n":"mcp-postgres","t":"MCP server exposing PostgreSQL tools to AI assistants","s":[["TAGLINE","MCP server exposing PostgreSQL tools to AI assistants"],["TLDR","**Start in stdio mode** for Claude Desktop integration\n\n```mcp-postgres --database-url \"[postgres://user:pass@localhost:5432/dbname]\" --stdio```\n\n**Start a TCP server** on the default port\n\n```mcp-postgres --database-url \"[postgres://user:pass@localhost:5432/dbname]\"```\n\n**Start an HTTP/2 server**\n\n```mcp-postgres --database-url \"[postgres://user:pass@localhost:5432/dbname]\" --http-port [3001]```\n\n**Use restricted access mode** for production databases\n\n```mcp-postgres --database-url \"[postgres://user:pass@localhost:5432/dbname]\" --access-mode restricted```\n\n**Enable Prometheus metrics**\n\n```mcp-postgres --database-url \"[postgres://user:pass@localhost:5432/dbname]\" --enable-metrics --metrics-port [9090]```"],["SYNOPSIS","**mcp-postgres** **--database-url** _URL_ [_options_]"],["PARAMETERS","**-d**, **--database-url** _URL_\n> PostgreSQL connection string (required)\n\n**--stdio**\n> Run in stdio mode for Claude Desktop and compatible MCP clients\n\n**-H**, **--host** _host_\n> TCP server bind address (default: 127.0.0.1)\n\n**-p**, **--port** _port_\n> TCP server port (default: 3000)\n\n**--http-port** _port_\n> HTTP/2 server port (default: 3001)\n\n**--min-connections** _n_\n> Minimum connection pool size (default: 5)\n\n**--max-connections** _n_\n> Maximum connection pool size (default: 20)\n\n**--log-level** _level_\n> Log verbosity: trace, debug, info, warn, error (default: info)\n\n**--access-mode** _mode_\n> Security level: `unrestricted` (all SQL allowed) or `restricted` (safe queries only)\n\n**--enable-metrics**\n> Activate Prometheus metrics endpoint\n\n**--metrics-port** _port_\n> Prometheus metrics server port (default: 9090)\n\n**--tls-cert** _path_\n> PEM certificate file for HTTPS\n\n**--tls-key** _path_\n> PEM private key file for HTTPS\n\n**-V**, **--version**\n> Show version number"],["DESCRIPTION","**mcp-postgres** is a Model Context Protocol (MCP) server that connects AI assistants such as Claude to a PostgreSQL database. It exposes over 135 tools covering query execution, schema inspection, DDL operations, data management, monitoring, and security auditing.\n\nThe server supports three transport modes: **stdio** for direct integration with Claude Desktop and compatible MCP clients, **TCP** for network-accessible deployments on port 3000, and **HTTP/2** for web-based clients on port 3001. Connection pooling is managed automatically with configurable minimum and maximum pool sizes.\n\nInstall via Cargo with `cargo install mcp-postgres`, or on macOS via Homebrew with `brew tap corporatepiyush/mcp-postgres && brew install mcp-postgres`."],["CAVEATS","The `unrestricted` access mode permits all SQL operations including DDL statements and destructive queries. Use `restricted` mode when connecting to production databases. Ensure the PostgreSQL user has only the minimum required privileges regardless of the access mode setting."],["SEE ALSO","[psql](/man/psql)(1), [pg_dump](/man/pg_dump)(1)"],["RESOURCES","```[Source code](https://github.com/corporatepiyush/mcp-pg-rust)```"]],"e":["psql","pg_dump"],"x":"mcp-postgres --database-url \"[postgres://user:pass@localhost:5432/dbname]\" --stdio"},"mdatopbm":{"n":"mdatopbm","t":"converts MicroDesign Area format to PBM","s":[["TAGLINE","converts MicroDesign Area format to PBM"],["TLDR","**Convert** a MicroDesign area to PBM on stdout\n\n```mdatopbm [input.mda] > [output.pbm]```\n\n**Read from stdin** and write PBM on stdout\n\n```cat [input.mda] | mdatopbm > [output.pbm]```\n\n**Double-height** output (for non-square PCW pixels)\n\n```mdatopbm -d [input.mda] > [output.pbm]```\n\n**Invert** black/white in the output\n\n```mdatopbm -i [input.mda] > [output.pbm]```"],["SYNOPSIS","**mdatopbm** [**-d**] [**-i**] [_mdafile_]"],["PARAMETERS","**-d**, **-double**\n> Double the height of the image to compensate for the rectangular pixel aspect ratio of the Amstrad PCW screen.\n\n**-i**, **-invert**\n> Swap foreground and background, producing white-on-black output.\n\n_mdafile_\n> Input MicroDesign Area (.mda) file. Reads from standard input if omitted."],["DESCRIPTION","**mdatopbm** converts MicroDesign Area files to PBM. MicroDesign was a desktop publishing program for the Amstrad PCW range of computers, and its .mda files store bitmap clip art at the PCW's non-square pixel ratio. The **-d** option doubles the vertical resolution so the image appears at correct proportions on a square-pixel display.\n\nOutput is written to standard output. The tool is part of the **Netpbm** toolkit."],["CAVEATS","Only the MicroDesign Area (.mda) variant is supported; documents from MicroDesign Plus may need to be exported to .mda first. The format is monochrome (1 bpp)."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pbmtomda](/man/pbmtomda)(1), [pbmtolj](/man/pbmtolj)(1), [pbm](/man/pbm)(5)"]],"e":["pbmtomda","pbmtolj","pbm"],"x":"mdatopbm [input.mda] > [output.pbm]"},"meshlabserver":{"n":"meshlabserver","t":"Command-line interface for MeshLab 3D mesh processing","s":[["TAGLINE","Command-line interface for MeshLab 3D mesh processing"],["TLDR","**Apply filter script to mesh**\n\n```meshlabserver -i [input.obj] -o [output.obj] -s [script.mlx]```\n\n**Convert mesh format**\n\n```meshlabserver -i [input.stl] -o [output.ply]```\n\n**Apply script with log**\n\n```meshlabserver -i [input.obj] -o [output.obj] -s [script.mlx] -l [log.txt]```\n\n**Save output with vertex colors and normals**\n\n```meshlabserver -i [input.obj] -o [output.ply] -m vc vn```\n\n**Process a MeshLab project file**\n\n```meshlabserver -p [project.mlp] -s [script.mlx] -w [output_project.mlp]```\n\n**Dump a list of all available filter functions**\n\n```meshlabserver -d [filters.txt]```"],["SYNOPSIS","**meshlabserver** **-i** _input_ [**-o** _output_] [**-s** _script_] [**-l** _log_] [**-m** _options_]"],["PARAMETERS","**-i** _file_\n> Input mesh file to be loaded (OBJ, STL, PLY, OFF, etc.).\n\n**-o** _file_\n> Output mesh file. Format determined by extension.\n\n**-s** _file_\n> MeshLab filter script file (.mlx), created from MeshLab GUI via Filters > Show current filter script. Must be in XML format.\n\n**-l** _file_\n> Log file for filter processing output.\n\n**-p** _file_\n> MeshLab project file (.mlp) to be loaded.\n\n**-w** _file_\n> Output MeshLab project file (.mlp) to be saved.\n\n**-x**\n> Used with **-w**. Overwrite 3D model files in the input project instead of creating new output files with an _out suffix.\n\n**-d** _file_\n> Dump a list of all available filtering functions to a text file.\n\n**-m** _options_\n> Mesh attributes to save. Vertex: vc (color), vf (flags), vq (quality), vn (normals), vt (texcoord), vr (radius). Face: fc (color), ff (flags), fq (quality), fn (normals). Wedge: wc (color), wn (normals), wt (texcoord). Format: mp (polygonal), sa (ASCII)."],["DESCRIPTION","**meshlabserver** is the command-line (headless) interface for MeshLab. It allows batch processing of 3D meshes non-interactively using filter scripts exported from the MeshLab GUI. It supports mesh format conversion, project file processing, and automation of mesh processing workflows."],["CAVEATS","Deprecated since MeshLab 2022.02 and replaced by **PyMeshLab**, a Python library providing equivalent functionality. Filter scripts (.mlx) must be created from the MeshLab GUI. The output mesh attribute flag is **-m**, not **-om**."],["SEE ALSO","[meshlab](/man/meshlab)(1), [blender](/man/blender)(1)"]],"e":["meshlab","blender"],"x":"meshlabserver -i [input.obj] -o [output.obj] -s [script.mlx]"},"middleman":{"n":"middleman","t":"static site generator built with Ruby","s":[["TAGLINE","static site generator built with Ruby"],["TLDR","**Create new project**\n\n```middleman init [project_name]```\n\n**Start development server**\n\n```middleman server```\n\n**Start on specific port**\n\n```middleman server --port [4567]```\n\n**Build static site**\n\n```middleman build```\n\n**Build with clean output**\n\n```middleman build --clean```\n\n**Create new article** (blog)\n\n```middleman article \"[Article Title]\"```\n\n**Show version**\n\n```middleman version```"],["SYNOPSIS","**middleman** [_init_] [_server_] [_build_] [_article_] [_options_]"],["PARAMETERS","**init** _NAME_\n> Create new project.\n\n**server**, **s**\n> Start development server.\n\n**build**, **b**\n> Build static site.\n\n**article** _TITLE_\n> Create new blog article.\n\n**console**\n> Start interactive console.\n\n**--port** _PORT_\n> Server port (default 4567).\n\n**--bind** _HOST_\n> Server bind address.\n\n**--clean**\n> Remove orphaned files during build.\n\n**--verbose**\n> Verbose output.\n\n**--environment** _ENV_\n> Build environment (development, production).\n\n**--help**\n> Show help."],["DESCRIPTION","**Middleman** is a static site generator built with Ruby. It uses templates, layouts, and data files to produce static HTML, CSS, and JavaScript.\n\nThe development server provides live reloading. Changes to source files automatically refresh the browser. This speeds up the design and development workflow.\n\nTemplates support ERB, Haml, Slim, and other Ruby templating languages. Asset pipeline handles Sass, CoffeeScript, and JavaScript bundling.\n\nData files in YAML or JSON populate templates dynamically. This separates content from presentation, enabling data-driven pages.\n\nThe blog extension adds post creation, tagging, and pagination. Articles are written in Markdown with YAML frontmatter.\n\nBuild produces a static site in the build directory, ready for deployment to any web server or CDN."],["CAVEATS","Requires Ruby environment. Plugin dependencies can be complex. Build times increase with site size. Some plugins may conflict."],["HISTORY","**Middleman** was created by **Thomas Reynolds** starting around **2009**. It brought modern web development practices (asset pipeline, live reload) to static site generation. The project influenced later static site generators."],["SEE ALSO","[jekyll](/man/jekyll)(1), [hugo](/man/hugo)(1), [gatsby](/man/gatsby)(1), [bundle](/man/bundle)(1)"]],"e":["jekyll","hugo","gatsby","bundle"],"x":"middleman init [project_name]"},"moo":{"n":"moo","t":"Git versions files. moo versions the machine (isolated Linux microVMs per branch/commit)","s":[["TAGLINE","Git versions files. moo versions the machine (isolated Linux microVMs per branch/commit)"],["TLDR","**Create** a machine for the current branch or worktree\n\n```moo new feat/billing```\n\n**Run** a command inside the machine\n\n```moo run feat/billing -- npm run migrate```\n\n**Snapshot** the machine state tied to the current commit\n\n```moo save feat/billing```\n\n**Open** a host URL for a forwarded guest port\n\n```moo open feat/billing 3000```\n\n**Destroy** a machine\n\n```moo drop feat/billing --force```\n\n**Check** status of machines\n\n```moo ls```"],["SYNOPSIS","**moo** <verb> <name> [options]"],["DESCRIPTION","moo gives every git branch, worktree or agent attempt its own hardware-isolated Linux microVM containing databases, ports, packages and services. Machine state is saved per commit and restored on `git checkout`, enabling true time-travel for the full runtime.\n\nIt uses copy-on-write, boots quickly, and keeps storage small even with many machines. Primarily for macOS Apple Silicon hosts today (Linux host support planned). Working tree files are synced into the guest on demand (`new`/`run` from inside the repo).\n\nThe four core verbs are new, run, save, drop. Admin helpers include `ls`, `open`, and `doctor`."],["VERBS","**new** <name> [from <src>] [--detached]\n> Create or boot a machine. Prefers a snapshot for the current commit when present. `<src>` may be a git ref/SHA, snapshot ID, or another machine name.\n\n**run** <name> -- <cmd>...\n> Execute a command inside the machine (services persist between runs).\n\n**save** [<name>]\n> Snapshot current machine state, associated with the current git commit.\n\n**drop** <name> [--force] [--snapshots]\n> Destroy the live machine (snapshots remain unless `--snapshots`).\n\n**ls**\n> List machines, ports, and snapshots.\n\n**open** <name> [guest-port] [/path]\n> Print (and open) the host URL for a forwarded guest port.\n\n**doctor**\n> Run host readiness checks."],["CAVEATS","- Currently Apple Silicon macOS host + Linux (arm64) guests; alpha pre-1.0 (snapshot format may change).\n- `moo save` before `git checkout` to preserve unsaved runtime work — `new` prefers the snapshot for HEAD over the live overlay.\n- Network is fully isolated per machine; use `moo open` / `[network] ports` in `moo.toml` for host reachability.\n- Base images come from OCI registries without a Docker daemon; Docker is not required."],["INSTALL","```aur: yay -S moo```"],["SEE ALSO","[git](/man/git)(1), [docker](/man/docker)(1)"],["RESOURCES","```[Source code](https://github.com/heyito/moo)```\n\n```[Documentation](https://github.com/heyito/moo#readme)```"]],"e":["git","docker"],"x":"moo new feat/billing"},"ncal":{"n":"ncal","t":"displays a calendar in a vertical format where weeks run down the screen","s":[["TAGLINE","displays a calendar in a vertical format where weeks run down the screen"],["TLDR","This command is an alias of **cal** with vertical calendar layout.\n\nDisplay **current month** calendar\n\n```ncal```\n\nDisplay calendar for **specific month and year**\n\n```ncal [month] [year]```\n\nDisplay **entire year** calendar\n\n```ncal -y [year]```"],["SYNOPSIS","**ncal** [**-y**] [**-3**] [**-1**] [**-A** _months_] [**-B** _months_] [[_month_] _year_]"],["PARAMETERS","**-y**\n> Display entire year.\n\n**-3**\n> Display previous, current, and next month.\n\n**-1**\n> Display only current month.\n\n**-A** _months_\n> Display months after current.\n\n**-B** _months_\n> Display months before current.\n\n**-h**\n> Turn off highlighting of today.\n\n**-M**\n> Weeks start on Monday.\n\n**-S**\n> Weeks start on Sunday.\n\n**-b**\n> Use horizontal layout (like cal)."],["DESCRIPTION","**ncal** displays a calendar in a vertical format where weeks run down the screen. It is an alternative interface to the **cal** command, which displays calendars horizontally.\n\nThe vertical format is useful for terminals with limited width or when comparing dates across weeks. Both ncal and cal are typically provided by the same package on most systems."],["CAVEATS","Available on BSD-derived systems. Some Linux distributions may not include ncal or may alias it to cal. The **-b** flag switches to the traditional horizontal cal layout."],["INSTALL","```apt: sudo apt install ncal```"],["SEE ALSO","[cal](/man/cal)(1)"]],"e":["cal"],"x":"ncal"},"nft-monitor":{"n":"nft-monitor","t":"displays real-time changes to the nftables firewall configuration","s":[["TAGLINE","displays real-time changes to the nftables firewall configuration"],["TLDR","Monitor **all nftables events**\n\n```sudo nft monitor```\n\nMonitor only **rule updates** (add, delete, replace)\n\n```sudo nft monitor rules```\n\nMonitor **set and element** updates\n\n```sudo nft monitor sets```\n\nMonitor events and print **JSON output**\n\n```sudo nft -j monitor```"],["SYNOPSIS","**nft monitor** [_object_]"],["PARAMETERS","**rules**\n> Monitor only rule changes\n\n**sets**\n> Monitor only set and element changes\n\n**tables**\n> Monitor only table changes\n\n**chains**\n> Monitor only chain changes\n\n**-j, --json**\n> Output events in JSON format"],["DESCRIPTION","**nft monitor** displays real-time changes to the nftables firewall configuration. It subscribes to netlink notifications and shows when rules, sets, chains, or tables are added, modified, or deleted.\n\nEvents are displayed as they occur, showing the exact nftables commands that would recreate the change. This is useful for debugging firewall behavior, auditing changes, or understanding how other tools modify the firewall.\n\nWithout a filter argument, all nftables events are shown. Specifying an object type (rules, sets, etc.) filters the output to only those changes."],["CAVEATS","Requires root privileges to access netlink notifications. The monitor runs continuously until interrupted with **Ctrl+C**. High-frequency changes (like connection tracking updates) may produce substantial output. Only shows changes made while monitoring is active."],["INSTALL","```apt: sudo apt install nftables```\n\n```dnf: sudo dnf install nftables```\n\n```pacman: sudo pacman -S nftables```\n\n```apk: sudo apk add nftables```\n\n```zypper: sudo zypper install nftables```\n\n```brew: brew install nftables```\n\n```nix: nix profile install nixpkgs#nftables```"],["SEE ALSO","[nft](/man/nft)(8), [iptables](/man/iptables)(8), [conntrack](/man/conntrack)(8)"]],"e":["nft","iptables","conntrack"],"x":"sudo nft monitor"},"npx":{"n":"npx","t":"executes npm packages","s":[["TAGLINE","executes npm packages"],["TLDR","**Run package command**\n\n```npx [package-name]```\n\n**Run specific version**\n\n```npx [package]@[version]```\n\n**Run without installing**\n\n```npx --no-install [package]```\n\n**Run with arguments**\n\n```npx [create-react-app] [my-app]```\n\n**Run from GitHub**\n\n```npx github:[user]/[repo]```\n\n**Run local binary**\n\n```npx [./node_modules/.bin/command]```\n\n**Execute package command**\n\n```npx -p [package] -c \"[command]\"```"],["SYNOPSIS","**npx** [_options_] _package_ [_args_]"],["PARAMETERS","_PACKAGE_\n> Package to execute.\n\n_ARGS_\n> Arguments to pass.\n\n**--no-install**\n> Don't install missing.\n\n**-p** _PKG_\n> Package to install.\n\n**-c** _CMD_\n> Command to run.\n\n**-y**, **--yes**\n> Automatically confirm prompts.\n\n**--help**\n> Display help information."],["DESCRIPTION","**npx** executes npm package binaries. It first checks local **node_modules/.bin**, then globally installed packages, and finally downloads the package temporarily if not found.\n\nCommon uses include running project scaffolding tools (create-react-app, create-next-app), one-off utilities, and testing different package versions without installing them globally."],["CAVEATS","Downloads on first run. Caches packages. Part of npm 5.2+."],["HISTORY","npx was introduced with **npm 5.2** to simplify running npm packages without installation."],["INSTALL","```pacman: sudo pacman -S npm```\n\n```apk: sudo apk add npm```\n\n```brew: brew install npm```"],["SEE ALSO","[npm](/man/npm)(1), [npm-exec](/man/npm-exec)(1), [yarn](/man/yarn)(1)"]],"e":["npm","npm-exec","yarn"],"x":"npx [package-name]"},"nxc-winrm":{"n":"nxc-winrm","t":"netExec WinRM module","s":[["TAGLINE","netExec WinRM module"],["TLDR","**Check WinRM credentials**\n\n```nxc winrm [target] -u [user] -p [password]```\n\n**Execute command**\n\n```nxc winrm [target] -u [user] -p [password] -x \"[command]\"```\n\n**Execute PowerShell**\n\n```nxc winrm [target] -u [user] -p [password] -X \"[ps_command]\"```\n\n**Use hash authentication**\n\n```nxc winrm [target] -u [user] -H [hash]```"],["SYNOPSIS","**nxc** **winrm** [_target_] [_options_]"],["PARAMETERS","_TARGET_\n> WinRM server address.\n\n**-u** _USER_\n> Username.\n\n**-p** _PASSWORD_\n> Password.\n\n**-x** _COMMAND_\n> Execute cmd command.\n\n**-X** _COMMAND_\n> Execute PowerShell command.\n\n**-H** _HASH_\n> NT hash for pass-the-hash.\n\n**--help**\n> Display help information."],["DESCRIPTION","**nxc winrm** is the NetExec WinRM module. Tests Windows Remote Management.\n\nThe tool validates WinRM access and executes commands. Part of NetExec suite."],["CAVEATS","Security tool. Authorized use only. Part of NetExec."],["HISTORY","nxc winrm provides **WinRM penetration testing** capabilities within NetExec."],["SEE ALSO","[nxc](/man/nxc)(1), [evil-winrm](/man/evil-winrm)(1), [winrm](/man/winrm)(1)"]],"e":["nxc","evil-winrm","winrm"],"x":"nxc winrm [target] -u [user] -p [password]"},"openclaw":{"n":"openclaw","t":"open-source autonomous AI agent that runs locally or on private servers","s":[["TAGLINE","open-source autonomous AI agent that runs locally or on private servers"],["TLDR","**Run the first-time setup wizard**\n\n```openclaw onboard```\n\n**Start the gateway for persistent operation**\n\n```openclaw gateway```\n\n**Install the gateway as a system daemon**\n\n```openclaw gateway install```\n\n**Send a task to the agent**\n\n```openclaw agent --message \"[task]\"```\n\n**List and manage installed skills**\n\n```openclaw skills```\n\n**Check system health and connected services**\n\n```openclaw status```"],["SYNOPSIS","**openclaw** [_command_] [_options_]\n\n**openclaw** **gateway** [_options_]\n\n**openclaw** **agent** [_options_]"],["PARAMETERS","**onboard**\n> Run the interactive setup wizard to configure messaging platforms and install the gateway daemon.\n\n**gateway**\n> Start the Gateway server for persistent operation (default port: 18789).\n\n**gateway install**\n> Install the Gateway as a system daemon (launchd on macOS, systemd on Linux).\n\n**gateway start**\n> Start the installed Gateway daemon.\n\n**gateway stop**\n> Stop the running Gateway daemon.\n\n**gateway restart**\n> Restart the Gateway daemon.\n\n**agent --message** _MESSAGE_\n> Send a natural language task or instruction to the AI agent.\n\n**config get** _PATH_\n> Retrieve a configuration value.\n\n**config set** _PATH_ _VALUE_\n> Set a configuration value.\n\n**skills**\n> List and manage installed skills from the skill registry.\n\n**status**\n> Display system health, connected services, and active sessions.\n\n**doctor**\n> Run diagnostics to identify configuration or connectivity issues.\n\n**cron**\n> Manage scheduled recurring tasks.\n\n**sessions**\n> List active and past agent sessions.\n\n**--port** _PORT_\n> Port number for the gateway (default: 18789).\n\n**--verbose**\n> Enable verbose logging output.\n\n**--help**\n> Display help information.\n\n**--version**\n> Display version information."],["DESCRIPTION","**OpenClaw** is a free, open-source, MIT-licensed autonomous AI agent that runs locally or on private servers. Written in TypeScript and installed via npm, it uses large language models to execute tasks autonomously through messaging platforms. Supported channels include WhatsApp, Telegram, Signal, Slack, Discord, Google Chat, iMessage, Microsoft Teams, Matrix, and WebChat, enabling voice and text-based interaction from any device.\n\nThe agent operates through a Gateway architecture that provides persistent background operation. The Gateway listens on multiple ports for its control plane, messaging relay, and other services. Tasks are dispatched to the agent via messaging platforms or the CLI, and the agent can perform multi-step workflows, manage calendars, draft emails, send messages, and automate system commands.\n\nOpenClaw supports fully local operation using local LLM backends such as Ollama, allowing all reasoning and task execution to happen on-device without any data sent to external services. It can also connect to external AI APIs for enhanced capabilities. A skills framework with 50+ built-in skills and a community registry (ClaHub) provides extensible integrations for calendars, email, smart home systems, and more."],["CAVEATS","Messaging integrations require API tokens or credentials from their respective platforms. Running with local models requires sufficient computational resources for LLM inference. The onboarding wizard guides initial configuration of messaging channels and daemon installation. Ensure proper network security when exposing the Gateway to external networks."],["HISTORY","OpenClaw was created in November 2025 by **Peter Steinberger**, co-founder and former CEO of PSPDFKit. The project was originally named **Clawdbot** and was open source from the start. In January 2026, Anthropic threatened legal action over the trademark similarity between \"Clawd\" and \"Claude,\" prompting a rename to **Moltbot**. Just three days later, the project was renamed again to **OpenClaw** after cryptocurrency scammers hijacked abandoned social media accounts associated with the Moltbot name. The project grew rapidly, becoming one of the fastest-growing repositories in GitHub history and surpassing 200,000 stars within 84 days. In February 2026, Steinberger announced he was joining OpenAI to lead next-generation personal AI agents, and the OpenClaw Foundation was established to steward the open-source project."],["INSTALL","```dnf: sudo dnf install openclaw```\n\n```nix: nix profile install nixpkgs#openclaw```"],["SEE ALSO","[claude](/man/claude)(1), [ollama](/man/ollama)(1), [llm](/man/llm)(1)"]],"e":["claude","ollama","llm"],"x":"openclaw onboard"},"pamgamma":{"n":"pamgamma","t":"applies gamma correction to images","s":[["TAGLINE","applies gamma correction to images"],["TLDR","**Apply gamma correction**\n\n```pamgamma [2.2] [input.pam] > [output.pam]```\n\n**Apply inverse gamma**\n\n```pamgamma [0.45] [input.pam] > [output.pam]```\n\n**Convert sRGB to linear**\n\n```pamgamma -ungamma [input.pam] > [linear.pam]```\n\n**Convert linear to sRGB**\n\n```pamgamma -gamma [linear.pam] > [srgb.pam]```"],["SYNOPSIS","**pamgamma** [_options_] [_gamma_] [_pamfile_]"],["PARAMETERS","**gamma**\n> Gamma value (> 0).\n\n**-ungamma**\n> Remove gamma (to linear).\n\n**-gamma**\n> Apply gamma (to sRGB).\n\n**-bt709**\n> Use BT.709 transfer.\n\n**-srgb**\n> Use sRGB transfer."],["DESCRIPTION","**pamgamma** applies gamma correction to images. Gamma correction adjusts the brightness curve; values > 1 darken midtones, values < 1 brighten them.\n\nStandard display gamma is approximately 2.2; inverse is about 0.45."],["COMMON VALUES","```\n2.2   - Standard gamma\n0.45  - Inverse gamma (1/2.2)\n1.0   - No change\n```"],["EXAMPLE","```bash"],["Brighten dark image","pamgamma 0.7 dark.ppm > brighter.ppm"],["Standard gamma encode","pamgamma 2.2 linear.ppm > encoded.ppm\n```"],["CAVEATS","Input should match expected gamma. Clipping may occur at extremes."],["HISTORY","pamgamma is part of **Netpbm**, providing gamma correction as part of color space conversions."],["SEE ALSO","[pnmgamma](/man/pnmgamma)(1), [pamrecolor](/man/pamrecolor)(1), [netpbm](/man/netpbm)(1)"]],"e":["pnmgamma","pamrecolor","netpbm"],"x":"pamgamma [2.2] [input.pam] > [output.pam]"},"pbmpscale":{"n":"pbmpscale","t":"scales PBM bitmaps by an integer factor with anti-aliasing, producing","s":[["TAGLINE","scales PBM bitmaps by an integer factor with anti-aliasing, producing"],["TLDR","**Scale bitmap with anti-aliasing**\n\n```pbmpscale [4] [input.pbm] > [output.pgm]```"],["SYNOPSIS","**pbmpscale** _scale_ [_file_]"],["PARAMETERS","_scale_\n> Integer scale factor."],["DESCRIPTION","**pbmpscale** scales PBM bitmaps by an integer factor with anti-aliasing, producing grayscale output. Useful for smooth enlargement of 1-bit images. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pbmreduce](/man/pbmreduce)(1), [pamscale](/man/pamscale)(1)"]],"e":["pbmreduce","pamscale"],"x":"pbmpscale [4] [input.pbm] > [output.pgm]"},"pbmtobbnbg":{"n":"pbmtobbnbg","t":"Convert PBM bitmaps to BBN BitGraph terminal format","s":[["TAGLINE","Convert PBM bitmaps to BBN BitGraph terminal format"],["TLDR","**Convert a PBM file to BBN BitGraph format**\n\n```pbmtobbnbg [input.pbm] > [output.bg]```\n\n**Read from standard input**\n\n```cat [input.pbm] | pbmtobbnbg > [output.bg]```"],["SYNOPSIS","**pbmtobbnbg** [_pbmfile_]"],["DESCRIPTION","**pbmtobbnbg** reads a PBM image as input and produces BBN BitGraph graphics terminal format as output. If no file is specified, it reads from standard input.\n\nThe BitGraph was a graphics terminal manufactured by BBN (Bolt Beranek and Newman). This tool is part of the **Netpbm** toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pbmtoplot](/man/pbmtoplot)(1), [pbmtoascii](/man/pbmtoascii)(1), [pbm](/man/pbm)(1), [netpbm](/man/netpbm)(1)"]],"e":["pbmtoplot","pbmtoascii","pbm","netpbm"],"x":"pbmtobbnbg [input.pbm] > [output.bg]"},"piactl":{"n":"piactl","t":"controls Private Internet Access VPN from the command line","s":[["TAGLINE","controls Private Internet Access VPN from the command line"],["TLDR","**Connect to VPN**\n\n```piactl connect```\n\n**Disconnect VPN**\n\n```piactl disconnect```\n\n**Show connection status**\n\n```piactl get connectionstate```\n\n**Set region**\n\n```piactl set region [us-east]```\n\n**List regions**\n\n```piactl get regions```\n\n**Get current region**\n\n```piactl get region```\n\n**Enable kill switch**\n\n```piactl set killswitch on```\n\n**Show public IP**\n\n```piactl get vpnip```"],["SYNOPSIS","**piactl** _command_ [_args_]"],["PARAMETERS","**connect**\n> Connect to VPN.\n\n**disconnect**\n> Disconnect VPN.\n\n**get** _PROPERTY_\n> Get setting or status.\n\n**set** _PROPERTY_ _VALUE_\n> Set configuration.\n\n**login**\n> Authenticate.\n\n**logout**\n> Log out.\n\n**reset**\n> Reset to defaults."],["PROPERTIES","**connectionstate**\n> Connection status.\n\n**region**\n> VPN region.\n\n**regions**\n> Available regions.\n\n**vpnip**\n> VPN IP address.\n\n**publicip**\n> Current public IP.\n\n**killswitch**\n> Kill switch state.\n\n**protocol**\n> VPN protocol."],["DESCRIPTION","**piactl** controls Private Internet Access VPN from the command line. It manages connections and settings.\n\nConnection commands handle VPN state. Quick connect uses last region or auto-selects.\n\nRegion selection chooses exit location. List available regions to see options.\n\nKill switch blocks traffic when disconnected. This prevents IP leaks during connection issues.\n\nSettings configure protocol, port, and features. DNS and split tunneling are configurable."],["CAVEATS","Requires PIA daemon running. Subscription required. Some features need GUI configuration first."],["HISTORY","**piactl** is the command-line interface for **Private Internet Access** VPN service. It provides scriptable control over the PIA client for automation and headless systems."],["SEE ALSO","[openvpn](/man/openvpn)(1), [wg](/man/wg)(1), [nmcli](/man/nmcli)(1)"]],"e":["openvpn","wg","nmcli"],"x":"piactl connect"},"pkgdelta":{"n":"pkgdelta","t":"Create delta files between package versions","s":[["TAGLINE","Create delta files between package versions"],["TLDR","**Create delta package**\n\n```pkgdelta [old_package] [new_package]```\n\n**Apply delta**\n\n```xdelta3 -d -s [old_package] [delta] [new_package]```"],["SYNOPSIS","**pkgdelta** [_options_] _old_pkg_ _new_pkg_"],["PARAMETERS","_OLD_PKG_\n> Original package file.\n\n_NEW_PKG_\n> New package file.\n\n**-q**\n> Quiet mode."],["DESCRIPTION","**pkgdelta** creates binary delta files between two versions of an Arch Linux package. The resulting delta file contains only the differences, significantly reducing the download size needed for package upgrades compared to downloading the full new package.\n\nPart of the pacman package manager suite, it uses the xdelta3 algorithm to compute efficient binary diffs. Delta packages are applied with xdelta3 to reconstruct the new package from the old one and the delta file."],["CAVEATS","Arch Linux specific. Requires xdelta3. Space savings vary."],["HISTORY","pkgdelta was created for **bandwidth-efficient** Arch package updates."],["SEE ALSO","[pacman](/man/pacman)(8), [xdelta3](/man/xdelta3)(1)"]],"e":["pacman","xdelta3"],"x":"pkgdelta [old_package] [new_package]"},"pnmpaste":{"n":"pnmpaste","t":"Paste one PNM image onto another","s":[["TAGLINE","Paste one PNM image onto another"],["TLDR","**Paste image onto another**\n\n```pnmpaste [small.pnm] [x] [y] [background.pnm] > [output.pnm]```\n\n**Paste at top-left**\n\n```pnmpaste [overlay.pnm] 0 0 [base.pnm] > [output.pnm]```\n\n**Replace operation**\n\n```pnmpaste -replace [patch.pnm] [100] [50] [image.pnm] > [output.pnm]```\n\n**OR operation**\n\n```pnmpaste -or [mask.pbm] [x] [y] [image.pnm] > [output.pnm]```"],["SYNOPSIS","**pnmpaste** [_options_] _from_ _x_ _y_ _to_"],["PARAMETERS","**from**\n> Image to paste.\n\n**x**, **y**\n> Position in target image.\n\n**to**\n> Background image.\n\n**-replace**\n> Simple replacement (default).\n\n**-or**\n> Bitwise OR.\n\n**-and**\n> Bitwise AND.\n\n**-xor**\n> Bitwise XOR."],["DESCRIPTION","**pnmpaste** pastes one PNM image onto another at a specified position. Multiple paste operations (replace, or, and, xor) allow for different compositing effects.\n\nPart of Netpbm for image compositing."],["EXAMPLES","```bash"],["Paste logo at position 10,10","pnmpaste logo.ppm 10 10 background.ppm > result.ppm"],["Paste at bottom-right (compute position)","pnmpaste small.ppm 300 200 large.ppm > result.ppm"],["Combine with masks","pnmpaste -or mask.pbm 0 0 image.pbm > masked.pbm"],["Multiple pastes","pnmpaste img1.ppm 0 0 base.ppm | pnmpaste img2.ppm 100 0 - > result.ppm\n```"],["CAVEATS","Pasted image must fit within background. Use negative coordinates for offset. Part of Netpbm."],["HISTORY","pnmpaste is part of **Netpbm** by **Jef Poskanzer**, providing basic image compositing."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pnmcomp](/man/pnmcomp)(1), [pnmcut](/man/pnmcut)(1), [pnmcat](/man/pnmcat)(1), [netpbm](/man/netpbm)(1)"]],"e":["pnmcomp","pnmcut","pnmcat","netpbm"],"x":"pnmpaste [small.pnm] [x] [y] [background.pnm] > [output.pnm]"},"poetry-remove":{"n":"poetry-remove","t":"Remove dependencies from a Poetry project","s":[["TAGLINE","Remove dependencies from a Poetry project"],["TLDR","**Remove a dependency**\n\n```poetry remove [package]```\n\n**Remove dev dependency**\n\n```poetry remove --group dev [package]```\n\n**Remove multiple packages**\n\n```poetry remove [package1] [package2]```\n\n**Dry run**\n\n```poetry remove --dry-run [package]```"],["SYNOPSIS","**poetry remove** [_options_] _packages_"],["PARAMETERS","_PACKAGES_\n> Packages to remove.\n\n**-G**, **--group** _GROUP_\n> Remove from specific group.\n\n**-D**, **--dev**\n> Remove from dev dependencies (deprecated, use **-G dev** instead).\n\n**--dry-run**\n> Simulate removal without making changes.\n\n**--lock**\n> Only update the lock file, do not uninstall packages."],["DESCRIPTION","**poetry remove** removes one or more dependencies from the project, updating both **pyproject.toml** and the lock file. It also uninstalls the packages and any orphaned transitive dependencies from the virtual environment.\n\nUse **-G** to remove from a specific dependency group (e.g., dev, test). The **--dry-run** flag shows what would be removed without making changes."],["CAVEATS","Updates lock file. May remove dependent packages."],["HISTORY","poetry remove provides **dependency removal** from Poetry projects."],["INSTALL","```dnf: sudo dnf install poetry```\n\n```apk: sudo apk add poetry```\n\n```brew: brew install poetry```\n\n```nix: nix profile install nixpkgs#poetry```"],["SEE ALSO","[poetry](/man/poetry)(1), [poetry-add](/man/poetry-add)(1)"]],"e":["poetry","poetry-add"],"x":"poetry remove [package]"},"poetry-show":{"n":"poetry-show","t":"Display installed package information","s":[["TAGLINE","Display installed package information"],["TLDR","**List all installed packages**\n\n```poetry show```\n\n**Show details for a specific package**\n\n```poetry show [package]```\n\n**Show the full dependency tree**\n\n```poetry show --tree```\n\n**Show only top-level dependencies**\n\n```poetry show --top-level```\n\n**Show outdated packages with latest versions**\n\n```poetry show --outdated --latest```\n\n**Show why a package is required**\n\n```poetry show --why [package]```\n\n**Output package list as JSON**\n\n```poetry show --format json```"],["SYNOPSIS","**poetry show** [_options_] [_package_]"],["PARAMETERS","_PACKAGE_\n> Package to show details for.\n\n**--tree**\n> Show dependency tree.\n\n**--top-level**\n> Only show top-level dependencies.\n\n**--outdated**\n> Show outdated packages.\n\n**--latest**\n> Show latest available versions.\n\n**--why**\n> Show whether packages are a direct dependency or required by other packages.\n\n**-a**, **--all**\n> Show all packages including development dependencies.\n\n**-f**, **--format** _FORMAT_\n> Output format: text (default) or json. Cannot be combined with --tree."],["DESCRIPTION","**poetry show** displays information about installed packages in the project. Without arguments, it lists all installed packages with their versions and descriptions. When given a package name, it shows detailed information including dependencies and available versions.\n\nThe **--tree** flag displays the dependency tree showing how packages relate to each other. The **--outdated** flag highlights packages with newer versions available, and **--top-level** shows only direct project dependencies."],["CAVEATS","Requires valid lock file. Shows installed state."],["HISTORY","poetry show provides **dependency inspection** for Poetry projects."],["INSTALL","```dnf: sudo dnf install poetry```\n\n```apk: sudo apk add poetry```\n\n```brew: brew install poetry```\n\n```nix: nix profile install nixpkgs#poetry```"],["SEE ALSO","[poetry](/man/poetry)(1), [poetry-lock](/man/poetry-lock)(1)"]],"e":["poetry","poetry-lock"],"x":"poetry show"},"ppmshadow":{"n":"ppmshadow","t":"Add drop shadow effect to PPM images","s":[["TAGLINE","Add drop shadow effect to PPM images"],["TLDR","**Add a default drop shadow**\n\n```ppmshadow [input.ppm] > [output.ppm]```\n\n**Specify blur radius for shadow softness**\n\n```ppmshadow -b [8] [input.ppm] > [output.ppm]```\n\n**Specify shadow offset direction**\n\n```ppmshadow -x [5] -y [5] [input.ppm] > [output.ppm]```\n\n**Create a translucent shadow**\n\n```ppmshadow -t [input.ppm] > [output.ppm]```"],["SYNOPSIS","**ppmshadow** [_options_] [_file_]"],["PARAMETERS","_FILE_\n> Input PPM file. Reads from stdin if omitted.\n\n**-b** _N_\n> Blur radius controlling shadow softness (default: 11).\n\n**-x** _N_\n> Horizontal shadow offset in pixels (default: blur radius / 2).\n\n**-y** _N_\n> Vertical shadow offset in pixels (default: same as -x).\n\n**-t**\n> Create a translucent shadow instead of a solid black one."],["DESCRIPTION","**ppmshadow** adds a drop shadow effect to a PPM image, creating the appearance of the image floating above a background. The shadow is created by blurring a darkened copy of the image and placing it offset behind the original.\n\nThe blur radius controls shadow softness, while x and y offsets control the apparent light direction. The **-t** option creates a translucent shadow that allows the background to show through. Part of the Netpbm toolkit.\n\nThe input image must have a transparent (black) background area for the shadow to be visible. The output image is larger than the input by the shadow offset."],["CAVEATS","Requires the image to have a transparent (black) background around the subject for the shadow to appear correctly. Internally uses pnmconvol for blurring, which can be slow for large blur radii. Part of the Netpbm suite."],["HISTORY","ppmshadow was created by **John Walker** as part of the **Netpbm** toolkit for image shadow effect generation."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm-extras```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[ppm](/man/ppm)(5), [pnmcomp](/man/pnmcomp)(1), [netpbm](/man/netpbm)(1), [pnmconvol](/man/pnmconvol)(1)"]],"e":["ppm","pnmcomp","netpbm","pnmconvol"],"x":"ppmshadow [input.ppm] > [output.ppm]"},"ppmtospu":{"n":"ppmtospu","t":"Convert PPM to Atari Spectrum 512 format","s":[["TAGLINE","Convert PPM to Atari Spectrum 512 format"],["TLDR","**Convert PPM to Atari Spectrum 512**\n\n```ppmtospu [input.ppm] > [output.spu]```"],["SYNOPSIS","**ppmtospu** [_options_] [_file_]"],["PARAMETERS","**-d**\n> Double resolution."],["DESCRIPTION","**ppmtospu** converts PPM images to Atari Spectrum 512 format, which displays 512 colors on Atari ST. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[sputoppm](/man/sputoppm)(1), [ppmtoneo](/man/ppmtoneo)(1)"]],"e":["sputoppm","ppmtoneo"],"x":"ppmtospu [input.ppm] > [output.spu]"},"protontricks":{"n":"protontricks","t":"Run Winetricks for Proton Steam games","s":[["TAGLINE","Run Winetricks for Proton Steam games"],["TLDR","Run **GUI**\n\n```protontricks --gui```\n\nRun Winetricks for **specific game**\n\n```protontricks [appid] [winetricks_args]```\n\nRun **command** in game directory\n\n```protontricks -c \"[command]\" [appid]```\n\n**List** installed games\n\n```protontricks -l```\n\n**Search** for game by name\n\n```protontricks -s [game_name]```\n\n**Launch** executable in Proton environment\n\n```protontricks-launch --appid [appid] [path/to/file.exe]```"],["SYNOPSIS","**protontricks** [**--gui**] [**-l**|**--list**] [**-s** _name_] [**-c** _cmd_] [_appid_] [_args_...]"],["PARAMETERS","**--gui**\n> Launch graphical interface\n\n**-l, --list**\n> List installed Steam games\n\n**-s, --search _name_**\n> Search for game App ID by name\n\n**-c, --command _cmd_**\n> Run command in game's Wine prefix\n\n**--no-bwrap**\n> Disable bubblewrap sandboxing\n\n**-h, --help**\n> Display help information"],["DESCRIPTION","**protontricks** runs Winetricks commands for Proton-enabled Steam games. It allows installing Windows components, fonts, and libraries into game prefixes to fix compatibility issues.\n\nThe tool locates game prefixes automatically and sets up the correct Proton environment. It works with games installed through Steam using Proton compatibility layer."],["CAVEATS","Requires Steam and Proton. Game must have been run at least once to create prefix. Some winetricks verbs may not work with all Proton versions."],["HISTORY","**protontricks** was created by **Matoking** to bridge Winetricks and Proton, addressing the common need to install Windows dependencies in Proton game prefixes."],["INSTALL","```dnf: sudo dnf install protontricks```\n\n```pacman: sudo pacman -S protontricks```\n\n```zypper: sudo zypper install protontricks```\n\n```nix: nix profile install nixpkgs#protontricks```"],["SEE ALSO","[winetricks](/man/winetricks)(1), [wine](/man/wine)(1), [steam](/man/steam)(1)"]],"e":["winetricks","wine","steam"],"x":"protontricks --gui"},"prowler-m365":{"n":"prowler-m365","t":"Microsoft 365 tenant security assessment","s":[["TAGLINE","Microsoft 365 tenant security assessment"],["TLDR","**Run Microsoft 365 security assessment**\n\n```prowler m365```\n\n**Run specific checks**\n\n```prowler m365 --checks [conditional_access_policies]```\n\n**Run with compliance framework**\n\n```prowler m365 --compliance [cis_2.0_m365]```"],["SYNOPSIS","**prowler m365** [_options_]"],["PARAMETERS","**--checks** _checks_\n> Specific checks to run.\n\n**--services** _services_\n> Services to check.\n\n**--compliance** _framework_\n> Compliance framework.\n\n**-M**, **--output-modes** _format_\n> Output format.\n\n**-o**, **--output-directory** _dir_\n> Output directory."],["DESCRIPTION","**prowler m365** performs security assessment of Microsoft 365 tenants. Checks Entra ID, Exchange, SharePoint, and Teams configurations against security best practices."],["INSTALL","```brew: brew install prowler```\n\n```nix: nix profile install nixpkgs#prowler```"],["SEE ALSO","[prowler](/man/prowler)(1)"]],"e":["prowler"],"x":"prowler m365"},"pulumi-import":{"n":"pulumi-import","t":"Import existing cloud resources into Pulumi","s":[["TAGLINE","Import existing cloud resources into Pulumi"],["TLDR","**Import an existing resource by type, name, and cloud ID**\n\n```pulumi import [aws:s3/bucket:Bucket] [my-bucket] [my-bucket-id]```\n\n**Import from a JSON file**\n\n```pulumi import -f [resources.json]```\n\n**Import and write generated code to a file**\n\n```pulumi import [type] [name] [id] -o [code.ts]```\n\n**Import without deletion protection**\n\n```pulumi import --protect=false [type] [name] [id]```\n\n**Preview the import without performing it**\n\n```pulumi import --preview-only [type] [name] [id]```\n\n**Import and skip the confirmation prompt**\n\n```pulumi import -y [type] [name] [id]```\n\n**Import specific properties only**\n\n```pulumi import --properties [prop1,prop2] [type] [name] [id]```"],["SYNOPSIS","**pulumi** **import** [_options_] _type_ _name_ _id_"],["PARAMETERS","**-f**, **--file** _file_\n> Import resources from a JSON file containing resource definitions.\n\n**-o**, **--out** _file_\n> Write generated resource declaration code to the specified file.\n\n**--protect**\n> Import resources with deletion protection enabled (default: true).\n\n**--generate-code**\n> Generate resource declaration code for imported resources (default: true).\n\n**--parent** _name=urn_\n> Parent resource name and URN in the format `name=urn`.\n\n**--provider** _name=urn_\n> Provider resource name and URN in the format `name=urn`.\n\n**--properties** _names_\n> Comma-separated list of property names to use for the import.\n\n**--preview-only**\n> Only show a preview of the import without performing it.\n\n**--skip-preview**\n> Skip the preview calculation before performing the import.\n\n**--diff**\n> Display the operation as a rich diff showing the overall change.\n\n**-s**, **--stack** _name_\n> Target stack to operate on.\n\n**-p**, **--parallel** _n_\n> Allow N resource operations to run in parallel (default: 16).\n\n**-y**, **--yes**\n> Automatically approve the import after preview.\n\n**-m**, **--message** _msg_\n> Optional message for the update operation.\n\n**--from** _converter_\n> Invoke a converter to import resources (e.g., from Terraform state).\n\n**--suppress-outputs**\n> Suppress display of stack outputs (useful when they contain sensitive values).\n\n**-d**, **--debug**\n> Print detailed debugging output during the operation.\n\n**-j**, **--json**\n> Serialize output as JSON."],["DESCRIPTION","**pulumi import** imports existing cloud resources into Pulumi state and optionally generates the code needed to manage them. This is useful for adopting existing infrastructure under Pulumi management without recreating resources.\n\nThe command requires the resource type (in the format `provider:module/resource:Resource`), a logical name for the resource in Pulumi, and the cloud provider's resource ID. After import, the resource appears in the stack state and future `pulumi up` operations will manage it."],["INSTALL","```pacman: sudo pacman -S pulumi```\n\n```apk: sudo apk add pulumi```\n\n```brew: brew install pulumi```\n\n```nix: nix profile install nixpkgs#pulumi```"],["SEE ALSO","[pulumi](/man/pulumi)(1), [pulumi-up](/man/pulumi-up)(1), [pulumi-state](/man/pulumi-state)(1), [pulumi-preview](/man/pulumi-preview)(1), [pulumi-refresh](/man/pulumi-refresh)(1)"]],"e":["pulumi","pulumi-up","pulumi-state","pulumi-preview","pulumi-refresh"],"x":"pulumi import [aws:s3/bucket:Bucket] [my-bucket] [my-bucket-id]"},"qm-shutdown":{"n":"qm-shutdown","t":"Gracefully shutdown a Proxmox VM","s":[["TAGLINE","Gracefully shutdown a Proxmox VM"],["TLDR","**Shutdown** a VM\n\n```qm shutdown vm_id```\n\nShutdown with **timeout**\n\n```qm shutdown --timeout 10 vm_id```\n\n**Keep** storage active\n\n```qm shutdown --keepActive true vm_id```\n\n**Skip** lock check\n\n```qm shutdown --skiplock true vm_id```\n\n**Force** stop after shutdown\n\n```qm shutdown --forceStop true vm_id```"],["SYNOPSIS","**qm shutdown** [_OPTIONS_] _vmid_"],["DESCRIPTION","**qm shutdown** sends an ACPI shutdown signal to gracefully shut down a QEMU/KVM virtual machine in Proxmox VE. The guest operating system must respond to ACPI events for this to work properly."],["PARAMETERS","**vmid**\n> The numeric ID of the virtual machine\n\n**--timeout** _seconds_\n> Maximum time to wait for the VM to shut down\n\n**--keepActive** _boolean_\n> Do not deactivate storage volumes after shutdown\n\n**--skiplock** _boolean_\n> Skip lock check (root only)\n\n**--forceStop** _boolean_\n> Force stop the VM if it doesn't respond to shutdown"],["CAVEATS","The guest OS must have ACPI support enabled and respond to shutdown events. If the guest ignores the ACPI signal, use **--forceStop** or **qm stop** instead. Windows guests may require proper ACPI drivers."],["HISTORY","**qm shutdown** is part of the **Proxmox VE** virtualization platform for managing QEMU/KVM virtual machines."],["INSTALL","```dnf: sudo dnf install qm```"],["SEE ALSO","[qm](/man/qm)(1), [qm-start](/man/qm-start)(1), [qm-stop](/man/qm-stop)(1)"]],"e":["qm","qm-start","qm-stop"],"x":"qm shutdown vm_id"},"qm-suspend":{"n":"qm-suspend","t":"Suspend a Proxmox virtual machine","s":[["TAGLINE","Suspend a Proxmox virtual machine"],["TLDR","**Suspend** a virtual machine by ID\n\n```qm suspend 100```\n\nSuspend **skipping lock** check\n\n```qm suspend 100 --skiplock```\n\nSuspend skipping **storage lock** check\n\n```qm suspend 100 --skiplockstorage```"],["SYNOPSIS","**qm suspend** _vmid_ [_options_]"],["PARAMETERS","**--skiplock**\n> Skip the lock check when suspending\n\n**--skiplockstorage**\n> Skip the storage lock check when suspending"],["DESCRIPTION","**qm suspend** suspends a running virtual machine in Proxmox VE. The VM's state is preserved in memory, allowing it to be resumed later with qm resume.\n\nSuspension pauses VM execution without shutting down the guest OS."],["CAVEATS","Use --skiplock and --skiplockstorage with caution as they may lead to data corruption in certain situations. Suspended VMs still consume memory resources."],["HISTORY","Part of **Proxmox VE** QEMU/KVM management tools for virtual machine administration."],["INSTALL","```dnf: sudo dnf install qm```"],["SEE ALSO","[qm](/man/qm)(1), [qm-resume](/man/qm-resume)(1), [qm-shutdown](/man/qm-shutdown)(1)"]],"e":["qm","qm-resume","qm-shutdown"],"x":"qm suspend 100"},"qmmp":{"n":"qmmp","t":"Qt-based audio player with Winamp skins","s":[["TAGLINE","Qt-based audio player with Winamp skins"],["TLDR","**Open qmmp**\n\n```qmmp```\n\n**Play files**\n\n```qmmp [file1.mp3] [file2.ogg]```\n\n**Enqueue files**\n\n```qmmp --enqueue [files]```\n\n**Play and exit**\n\n```qmmp --play [file.mp3]```\n\n**Pause playback**\n\n```qmmp --pause```\n\n**Stop playback**\n\n```qmmp --stop```\n\n**Next track**\n\n```qmmp --next```"],["SYNOPSIS","**qmmp** [_options_] [_files_]"],["PARAMETERS","**--enqueue**\n> Add to queue.\n\n**--play**\n> Start playback.\n\n**--pause**\n> Pause playback.\n\n**--stop**\n> Stop playback.\n\n**--next**\n> Next track.\n\n**--previous**\n> Previous track.\n\n**--toggle-visibility**\n> Show/hide window.\n\n**--volume-up**\n> Increase volume.\n\n**--volume-down**\n> Decrease volume."],["DESCRIPTION","**Qmmp** is a Qt-based audio player. Its interface resembles Winamp/XMMS classic skins.\n\nMultiple audio formats are supported including MP3, FLAC, OGG, and APE. Plugin architecture extends capabilities.\n\nWinamp skins provide customization. Classic 2.x skins work directly.\n\nVisualization plugins show audio analysis. Spectrum analyzers and oscilloscopes included.\n\nQueue management handles playlists. Shuffle, repeat, and sorting options available."],["CAVEATS","Qt dependencies required. Some skins may not work. Plugin availability varies."],["HISTORY","**Qmmp** started as a Qt-based alternative to XMMS and Audacious. It preserves the classic Winamp interface style in a modern media player."],["INSTALL","```apt: sudo apt install qmmp```\n\n```dnf: sudo dnf install qmmp```\n\n```pacman: sudo pacman -S qmmp```\n\n```zypper: sudo zypper install qmmp```\n\n```brew: brew install qmmp```\n\n```nix: nix profile install nixpkgs#qmmp```"],["SEE ALSO","[audacious](/man/audacious)(1), [mpv](/man/mpv)(1), [vlc](/man/vlc)(1), [cmus](/man/cmus)(1)"]],"e":["audacious","mpv","vlc","cmus"],"x":"qmmp"},"rcrack":{"n":"rcrack","t":"Crack password hashes using rainbow tables","s":[["TAGLINE","Crack password hashes using rainbow tables"],["TLDR","**Crack a single hash** using rainbow tables\n\n```rcrack [path/to/tables] -h [5d41402abc4b2a76b9719d911017c592]```\n\n**Crack hashes from a file**\n\n```rcrack [path/to/tables] -l [hashes.txt]```\n\n**Crack LM hashes** from a pwdump file\n\n```rcrack [path/to/tables] -lm [pwdump.txt]```\n\n**Crack NTLM hashes** from a pwdump file\n\n```rcrack [path/to/tables] -ntlm [pwdump.txt]```\n\n**Use multiple table directories**\n\n```rcrack [tables1/] [tables2/] -h [hash]```"],["SYNOPSIS","**rcrack** _table_path_ [_table_path_...] -h _hash_\n\n**rcrack** _table_path_ -l _hash_file_\n\n**rcrack** _table_path_ -lm _pwdump_file_\n\n**rcrack** _table_path_ -ntlm _pwdump_file_"],["PARAMETERS","**-h** _HASH_\n> Crack a single hash\n\n**-l** _FILE_\n> Load hashes from file (one per line)\n\n**-lm** _FILE_\n> Load LM hashes from pwdump file\n\n**-ntlm** _FILE_\n> Load NTLM hashes from pwdump file\n\n**-t** _DIR_\n> Rainbow tables directory\n\n**-o** _FILE_\n> Write cracked passwords to output file\n\n**-p** _NUM_\n> Number of threads to use\n\n**-s**\n> Display cracking statistics\n\n**-u**\n> Display username with cracked password"],["SUPPORTED ALGORITHMS","**lm**\n> LAN Manager hash (up to 7 characters)\n\n**ntlm**\n> NT LAN Manager hash (up to 15 characters)\n\n**md5**\n> MD5 hash (up to 15 characters)\n\n**sha1**\n> SHA-1 hash (up to 20 characters)\n\n**sha256**\n> SHA-256 hash (up to 20 characters)"],["DESCRIPTION","**rcrack** is a rainbow table-based password cracker from the RainbowCrack project. It uses pre-computed tables to perform time-memory trade-off attacks, finding plaintext passwords from hash values much faster than brute-force methods.\n\nRainbow tables must be generated beforehand using **rtgen** and sorted with **rtsort**. The tables encode chains of hash computations that allow quick lookup of passwords matching a given hash. Table files have **.rt** or **.rtc** (compressed) extensions.\n\nThe tool is particularly effective against LM and NTLM hashes from Windows systems. LM hashes are split into two 7-character halves, making them especially vulnerable. NTLM is stronger but still susceptible to rainbow table attacks for common passwords."],["CAVEATS","Rainbow tables require significant storage space (gigabytes to terabytes) and must match the hash algorithm and character set of the target passwords. Salted hashes cannot be cracked with rainbow tables. Modern password storage uses salting and slow hash functions (bcrypt, scrypt) specifically to defeat rainbow table attacks. Only use on systems you own or have authorization to test."],["HISTORY","RainbowCrack was developed by **Zhu Shuanglei** (Philippe Oechslin's rainbow table concept) and first released around **2003**. The project demonstrated the practical danger of unsalted password hashes and contributed to improved password storage practices. The tool showed that LM hashes could be cracked in minutes, leading to Microsoft deprecating LM hash storage in Windows Vista."],["SEE ALSO","[ophcrack](/man/ophcrack)(1), [hashcat](/man/hashcat)(1), [john](/man/john)(1), [rtgen](/man/rtgen)(1)"]],"e":["ophcrack","hashcat","john","rtgen"],"x":"rcrack [path/to/tables] -h [5d41402abc4b2a76b9719d911017c592]"},"react-scripts":{"n":"react-scripts","t":"Create React App build and development scripts","s":[["TAGLINE","Create React App build and development scripts"],["TLDR","**Start development server**\n\n```react-scripts start```\n\n**Build for production**\n\n```react-scripts build```\n\n**Run tests**\n\n```react-scripts test```\n\n**Eject configuration**\n\n```react-scripts eject```"],["SYNOPSIS","**react-scripts** _command_ [_options_]"],["COMMANDS","**start**\n> Start development server.\n\n**build**\n> Create production build.\n\n**test**\n> Run test suite.\n\n**eject**\n> Expose configuration files."],["DESCRIPTION","**react-scripts** is the build tool for Create React App. It encapsulates webpack, Babel, ESLint, and other tools with zero configuration, handling development server, building, and testing."],["EXAMPLES","```bash"],["Start dev server (port 3000)","npm start"],["or","react-scripts start"],["Production build","npm run build"],["Run tests","npm test"],["Run tests with coverage","npm test -- --coverage"],["Eject (one-way operation)","npm run eject\n```"],["ENVIRONMENT VARIABLES","```bash\nPORT=3001 npm start              # Custom port\nBROWSER=none npm start           # Don't open browser\nGENERATE_SOURCEMAP=false npm build  # No sourcemaps\nCI=true npm test                 # CI mode\n```"],["PROXY SETUP","```json\n// package.json\n{\n  \"proxy\": \"http://localhost:5000\"\n}\n```"],["CAVEATS","Eject is irreversible. Limited customization without ejecting. Consider CRACO for config overrides."],["HISTORY","react-scripts was created by **Facebook** as part of Create React App (2016) to simplify React development setup."],["SEE ALSO","[npm](/man/npm)(1), [vite](/man/vite)(1)"]],"e":["npm","vite"],"x":"react-scripts start"},"resolvectl":{"n":"resolvectl","t":"Query and manage systemd-resolved DNS settings","s":[["TAGLINE","Query and manage systemd-resolved DNS settings"],["TLDR","Show **DNS settings**\n\n```resolvectl status```\n\n**Resolve** domain names\n\n```resolvectl query domain1 domain2```\n\n**Reverse lookup** IP address\n\n```resolvectl query 8.8.8.8```\n\n**Flush** DNS caches\n\n```resolvectl flush-caches```\n\nShow DNS **statistics**\n\n```resolvectl statistics```\n\nQuery **MX record** for domain\n\n```resolvectl --type MX query domain```\n\nQuery **SRV record**\n\n```resolvectl service _xmpp-server._tcp example.com```\n\nQuery **TLS key**\n\n```resolvectl tlsa tcp example.com:443```"],["SYNOPSIS","**resolvectl** [_OPTIONS_] _COMMAND_ [_NAME_...]"],["DESCRIPTION","**resolvectl** resolves domain names, IPv4 and IPv6 addresses, DNS resource records, and services. It introspects and reconfigures the DNS resolver through systemd-resolved."],["COMMANDS","**query HOSTNAME|ADDRESS**\n> Resolve hostnames or reverse lookup addresses\n\n**service [[NAME] TYPE] DOMAIN**\n> Resolve DNS-SD or SRV service\n\n**openpgp EMAIL**\n> Query OpenPGP keys\n\n**tlsa [FAMILY] DOMAIN[:PORT]**\n> Query TLS server certificates\n\n**status [LINK]**\n> Show DNS settings per interface\n\n**statistics**\n> Show resolver statistics\n\n**reset-statistics**\n> Reset resolver statistics\n\n**flush-caches**\n> Flush all local DNS caches\n\n**reset-server-features**\n> Reset learned server features\n\n**dns [LINK [SERVER...]]**\n> Get/set DNS servers per link\n\n**domain [LINK [DOMAIN...]]**\n> Get/set search domains per link\n\n**dnssec [LINK [MODE]]**\n> Get/set DNSSEC mode\n\n**nta [LINK [DOMAIN...]]**\n> Get/set DNSSEC negative trust anchors"],["PARAMETERS","**-4, --ipv4**\n> Query only IPv4 addresses\n\n**-6, --ipv6**\n> Query only IPv6 addresses\n\n**-t, --type TYPE**\n> Query specific DNS record type\n\n**--class CLASS**\n> Query specific DNS class\n\n**--legend yes|no**\n> Show/hide result legend\n\n**--cname yes|no**\n> Follow CNAME redirects\n\n**-p, --protocol**\n> Specify protocol (dns, llmnr, mdns)"],["CAVEATS","Requires **systemd-resolved** to be running and `/etc/resolv.conf` to point at its stub resolver (`127.0.0.53`) for host resolution to actually use it. Per-link settings (`dns`, `domain`, `dnssec`) apply only until the next time the interface is brought up unless persisted via a `systemd-networkd` .network file or NetworkManager. `flush-caches` drops cached answers but does not change configured upstream servers."],["HISTORY","**resolvectl** is part of **systemd**, providing DNS resolution through systemd-resolved. It replaces the older systemd-resolve command."],["INSTALL","```apt: sudo apt install systemd-resolved```\n\n```dnf: sudo dnf install systemd-resolved```\n\n```zypper: sudo zypper install systemd-resolved```"],["SEE ALSO","[dig](/man/dig)(1), [nslookup](/man/nslookup)(1), [resolved.conf](/man/resolved.conf)(5)"]],"e":["dig","nslookup","resolved.conf"],"x":"resolvectl status"},"scli":{"n":"scli","t":"Simple terminal user interface for Signal messenger","s":[["TAGLINE","Simple terminal user interface for Signal messenger"],["TLDR","**Launch the Signal TUI**\n\n```scli```\n\n**Start with conversation history saving**\n\n```scli -s```\n\n**Link scli with an existing Signal account**\n\n```scli link --name \"[my-laptop]\"```"],["SYNOPSIS","**scli** [_options_]"],["PARAMETERS","**-s**, **--save-history**\n> Enable conversation history saving.\n\n**--enable-notifications**\n> Turn on desktop notifications.\n\n**--color**\n> Colorize messages with standard or 256-color mode.\n\n**--wrap-at** _NUM_\n> Text wrapping width.\n\n**--editor-command** _CMD_\n> Custom editor command for composing messages."],["DESCRIPTION","**scli** is a TUI for Signal messenger that uses **signal-cli** as its backend and urwid for the interface. It features Vim-like navigation, emoji reactions, remote message deletion, desktop notifications with sender avatars, and external editor support for composing messages."],["CAVEATS","Requires **signal-cli** to be installed and registered. Cannot quote messages or add mentions due to signal-cli limitations. Voice calls and read receipts are unsupported. Data is stored unencrypted; full-disk encryption is recommended."],["HISTORY","**scli** was created by **isamert** and is written in **Python**."],["INSTALL","```nix: nix profile install nixpkgs#scli```"],["SEE ALSO","[toot](/man/toot)(1)"]],"e":["toot"],"x":"scli"},"semsearch":{"n":"semsearch","t":"Embedding-first search engine admin CLI for indie blogs","s":[["TAGLINE","Embedding-first search engine admin CLI for indie blogs"],["TLDR","**Sync** dependencies and start Postgres\n\n```uv sync```\n\n```docker compose up -d db```\n\nCopy env and set **EMBEDDING_API_KEY**, then init the schema\n\n```cp .env.example .env```\n\n```uv run semsearch init-db```\n\n**Add** a blog (feed is required; sitemap optional)\n\n```uv run semsearch site add https://[example.blog]/ --sitemap auto --feed auto```\n\n**List** configured sites\n\n```uv run semsearch site list```\n\n**Remove** a site\n\n```uv run semsearch site remove https://[example.blog]/```\n\nRun the **ingestion daemon** (poll, crawl, embed)\n\n```uv run semsearch daemon```\n\nShow **index status** and failed jobs\n\n```uv run semsearch status```"],["SYNOPSIS","**semsearch** _command_ [_options_]"],["PARAMETERS","**init-db**\n> Create database schema for the configured embedding model and dimension. Prints the model name, vector size, and redacted DSN when ready.\n\n**site add** _url_ [**--sitemap** auto|none|_url_] [**--feed** auto|_url_]\n> Register or update a feed-backed site. **--feed** is mandatory in practice (auto-discovery or explicit URL). **--sitemap** defaults to **auto**.\n\n**site list**\n> List configured sites.\n\n**site remove** _url_\n> Remove a site from the index configuration.\n\n**daemon**\n> Long-running process that polls feeds, fetches pages, chunks text, embeds, and writes to **pgvector**. Uses an advisory lock so only one daemon runs at a time.\n\n**status**\n> Report index statistics and list failed ingestion jobs."],["DESCRIPTION","**semsearch** is the Typer-based administration CLI for **Semsearch**, an embedding-focused indexing and search engine aimed at aggregating and promoting indie blogs. The stack pairs a **FastAPI** web frontend with **PostgreSQL + pgvector** storage. The CLI handles schema setup, site registry, background ingestion, and operational status; the HTTP app serves search (see **uvicorn semsearch.web.app:app**).\n\nConfiguration comes from environment / **.env** (see **.env.example**): database URL, embedding API endpoint and key, model name, and embedding dimension. Chunking and embedding settings are shared between CLI and web. Changing chunking algorithm, embedding dimension, or model requires a full re-index.\n\nTypical local layout: Postgres via **docker compose**, Python tools via **uv run semsearch …**. Production runs the same binary inside the app container (**docker compose exec app … semsearch …**)."],["CAVEATS","Project status is **proof-of-concept toward production**. Requires a modern Python (upstream pins **>=3.14**), a running Postgres with **pgvector**, and a working embedding API key before indexing. Feed URLs are effectively required when adding sites. Not a general-purpose desktop search tool—it is the operator CLI for this specific blog-search service."],["HISTORY","**Semsearch** is a small open-source project exploring agentic RAG-style embedding pipelines for independent blogs, with admin tooling under the **semsearch** console script."],["SEE ALSO","[curl](/man/curl)(1), [docker](/man/docker)(1), [uv](/man/uv)(1)"],["RESOURCES","```[Source code](https://github.com/yikerman/semantic-search)```\n\n```[Homepage](https://github.com/yikerman/semantic-search)```"]],"e":["curl","docker","uv"],"x":"uv sync"},"sou":{"n":"sou","t":"Explore files in container image layers interactively","s":[["TAGLINE","Explore files in container image layers interactively"],["TLDR","**Explore a local container image**\n\n```sou [nginx:latest]```\n\n**Explore a remote container image**\n\n```sou [ghcr.io/user/image:latest]```"],["SYNOPSIS","**sou** _image-name_"],["DESCRIPTION","**sou** is a TUI tool for inspecting Docker/OCI container image layers. It lets you browse the filesystem within each layer, preview file contents, view image manifests and configuration, and export files locally. The name derives from the Japanese word for \"layer.\""],["KEYBINDINGS","**Layer view**\n> Navigate with arrow keys or **hjkl**. Copy the layer ID with **yy** and filter layers with **/**.\n\n**File view**\n> Browse files, toggle hidden files with **.**, and export the selected file with **x**.\n\n**Content view**\n> Scroll through the file content using the arrow keys or **hjkl**."],["HISTORY","**sou** was created by **knqyf263** and is written in **Go**."],["INSTALL","```nix: nix profile install nixpkgs#sou```"],["SEE ALSO","[dive](/man/dive)(1), [docker](/man/docker)(1), [skopeo](/man/skopeo)(1), [crane](/man/crane)(1)"]],"e":["dive","docker","skopeo","crane"],"x":"sou [nginx:latest]"},"sudoedit":{"n":"sudoedit","t":"Safely edit files with elevated privileges","s":[["TAGLINE","Safely edit files with elevated privileges"],["TLDR","**Edit file as root**\n\n```sudoedit [/etc/hosts]```\n\n**Edit with specific editor**\n\n```SUDO_EDITOR=[vim] sudoedit [/etc/fstab]```\n\n**Edit multiple files**\n\n```sudoedit [/etc/file1] [/etc/file2]```\n\n**Edit as another user**\n\n```sudoedit -u [username] [file]```"],["SYNOPSIS","**sudoedit** [_-u user_] [_options_] _files_"],["PARAMETERS","**-u**, **--user** _USER_\n> Edit as user.\n\n**-g**, **--group** _GROUP_\n> Edit with group.\n\n**-H**\n> Set HOME.\n\n**-n**, **--non-interactive**\n> Non-interactive mode."],["ENVIRONMENT","**SUDO_EDITOR** - Preferred editor\n**VISUAL** - Visual editor\n**EDITOR** - Default editor"],["DESCRIPTION","**sudoedit** safely edits files with elevated privileges. It's equivalent to sudo -e.\n\nFile is copied to temp location. User edits with their own editor.\n\nAfter editing, changes copy back. Original file replaced atomically.\n\nUser's environment is preserved. Editor runs as the user, not root.\n\nSafer than running editor as root. Limits privilege escalation risks."],["CAVEATS","Editor plugins run as user. Some editors may not work. Temp files briefly exist."],["HISTORY","**sudoedit** is part of **sudo**, providing secure file editing. It prevents running arbitrary code with root privileges through editor extensions."],["INSTALL","```apt: sudo apt install sudo```\n\n```dnf: sudo dnf install sudo```\n\n```pacman: sudo pacman -S sudo```\n\n```apk: sudo apk add sudo```\n\n```zypper: sudo zypper install sudo```\n\n```nix: nix profile install nixpkgs#sudo```"],["SEE ALSO","[sudo](/man/sudo)(8), [visudo](/man/visudo)(8), [vi](/man/vi)(1)"]],"e":["sudo","visudo","vi"],"x":"sudoedit [/etc/hosts]"},"terraform-fmt":{"n":"terraform-fmt","t":"Format Terraform configuration files","s":[["TAGLINE","Format Terraform configuration files"],["TLDR","**Format current directory**\n\n```terraform fmt```\n\n**Format recursively**\n\n```terraform fmt -recursive```\n\n**Check formatting**\n\n```terraform fmt -check```\n\n**Show diff**\n\n```terraform fmt -diff```\n\n**Check formatting and show diff**\n\n```terraform fmt -check -diff```\n\n**Format a specific directory**\n\n```terraform fmt [path/to/directory]```"],["SYNOPSIS","**terraform** **fmt** [_options_] [_dir_]"],["PARAMETERS","**-recursive**\n> Process subdirectories.\n\n**-check**\n> Check if formatted (exit 0 if yes).\n\n**-diff**\n> Show formatting diff.\n\n**-write**\n> Write changes (default true).\n\n**-list**\n> List files with formatting differences (default true).\n\n**-no-color**\n> Disable color output in diff."],["DESCRIPTION","**terraform fmt** formats Terraform configuration files to canonical style. Ensures consistent formatting across files and teams. Run before commits or in CI."],["INSTALL","```pacman: sudo pacman -S terraform```\n\n```nix: nix profile install nixpkgs#terraform```"],["SEE ALSO","[terraform](/man/terraform)(1), [terraform-init](/man/terraform-init)(1), [terraform-plan](/man/terraform-plan)(1)"]],"e":["terraform","terraform-init","terraform-plan"],"x":"terraform fmt"},"ufw-enable":{"n":"ufw-enable","t":"Enable Uncomplicated Firewall and load rules on boot","s":[["TAGLINE","Enable Uncomplicated Firewall and load rules on boot"],["TLDR","**Reload** and enable ufw\n\n```sudo ufw enable```\n\n**Simulate** enabling without making changes\n\n```sudo ufw --dry-run enable```"],["SYNOPSIS","**ufw** [_--dry-run_] **enable**"],["PARAMETERS","**enable**\n> Enable the firewall, load configured rules, and activate them on boot\n\n**--dry-run**\n> Show what would change without applying it"],["DESCRIPTION","**ufw enable** turns on Uncomplicated Firewall: configured rules are loaded into netfilter and the service is set so the firewall starts on boot. Pair with `ufw allow`/`deny` (or application profiles) before enabling on remote hosts so you do not lock yourself out of SSH.\n\nThis is the counterpart to `ufw disable`. After enabling, `ufw status` reports the active policy and rules."],["CAVEATS","On remote systems, allow SSH (e.g. `ufw allow OpenSSH` or port 22) **before** enabling. Enabling replaces or layers with other host firewall configuration depending on distro defaults. Requires root or sudo."],["HISTORY","Part of **ufw** (Uncomplicated Firewall), the Ubuntu-originated frontend for iptables/nftables."],["INSTALL","```dnf: sudo dnf install ufw```\n\n```pacman: sudo pacman -S ufw```\n\n```apk: sudo apk add ufw```\n\n```zypper: sudo zypper install ufw```"],["SEE ALSO","[ufw](/man/ufw)(8), [ufw-disable](/man/ufw-disable)(8), [iptables](/man/iptables)(8)"],["RESOURCES","```[Source code](https://git.launchpad.net/ufw)```\n\n```[Documentation](https://help.ubuntu.com/community/UFW)```"]],"e":["ufw","ufw-disable","iptables"],"x":"sudo ufw enable"},"ugdb":{"n":"ugdb","t":"Alternative TUI for GDB debugger","s":[["TAGLINE","Alternative TUI for GDB debugger"],["TLDR","**Debug a program**\n\n```ugdb [./myprogram]```\n\n**Debug with arguments**\n\n```ugdb [./myprogram] -- [arg1] [arg2]```\n\n**Attach to a running process by PID**\n\n```ugdb -p [pid]```\n\n**Use a custom GDB executable**\n\n```ugdb --gdb [/path/to/gdb] [./myprogram]```\n\n**Read symbols from a separate file**\n\n```ugdb -s [symbols.elf] [./myprogram]```\n\n**Start as an interface for rr replay**\n\n```ugdb --rr [./myprogram]```\n\n**Skip all .gdbinit initialization files**\n\n```ugdb -n [./myprogram]```"],["SYNOPSIS","**ugdb** [_options_] [_program_] [-- _args_...]"],["PARAMETERS","**--gdb** _path_\n> Path to an alternative gdb binary (default: gdb).\n\n**-p**, **--pid** _pid_\n> Attach to a running process with the given PID.\n\n**-s**, **--symbols** _file_\n> Read debug symbols from the given file.\n\n**-d**, **--directory** _dir_\n> Add a directory to the source file search path.\n\n**-c**, **--core** _file_\n> Use a core dump file for examination.\n\n**-x**, **--command** _file_\n> Execute GDB commands from a file on startup.\n\n**-e**, **--initial-expression** _expr_\n> Pre-populate the expression table with an entry.\n\n**--layout** _string_\n> Define the initial pane layout (default: (1s-1c)|(1e-1t)). Pane letters: s=source, c=console, e=expressions, t=terminal.\n\n**-n**, **--nx**\n> Do not execute commands from any .gdbinit file.\n\n**--nh**\n> Do not execute commands from ~/.gdbinit.\n\n**-q**, **--quiet**\n> Suppress the introductory and copyright messages.\n\n**--rr**\n> Start ugdb as an interface for rr; trailing arguments are passed to rr replay.\n\n**--rr-path** _path_\n> Path to an alternative rr binary.\n\n**--log_dir** _dir_\n> Directory for the log file (default: /tmp)."],["DESCRIPTION","**ugdb** is a terminal user interface for GDB built on the unsegen library. It provides four interactive panes: a GDB console, a source code pager, an expression table for watching variables, and a virtual terminal for the debugged program's output. Pane layout is configurable via **--layout**.\n\nSupports vim-like keybindings and remote control via IPC. The **--rr** flag enables use with the Mozilla rr record-and-replay debugger as a drop-in interface."],["HISTORY","**ugdb** was created by **ftilde** and is written in **Rust**."],["INSTALL","```nix: nix profile install nixpkgs#ugdb```"],["SEE ALSO","[gdb](/man/gdb)(1), [lldb](/man/lldb)(1)"]],"e":["gdb","lldb"],"x":"ugdb [./myprogram]"},"vivaldi-stable":{"n":"vivaldi-stable","t":"Customizable Chromium-based web browser (stable channel)","s":[["TAGLINE","Customizable Chromium-based web browser (stable channel)"],["TLDR","**Open** Vivaldi\n\n```vivaldi-stable```\n\n**Open a specific URL**\n\n```vivaldi-stable [https://example.com]```\n\n**Open with a specific profile**\n\n```vivaldi-stable --profile-directory=[test]```\n\n**Open URL in a new tab**\n\n```vivaldi-stable --new-tab [https://example.com]```\n\n**Start in incognito (private) mode**\n\n```vivaldi-stable --incognito```\n\n**Disable GPU** (useful for troubleshooting)\n\n```vivaldi-stable --disable-gpu```"],["SYNOPSIS","**vivaldi-stable** [_options_] [_URL_]"],["DESCRIPTION","**vivaldi-stable** is the Linux package name for the stable release of the Vivaldi browser. Vivaldi is a highly customizable web browser built on Chromium that focuses on power users, offering features such as tab stacking, tiling, built-in email, notes, and extensive theming.\n\nSince Vivaldi is Chromium-based, it accepts most Chromium command-line switches. User-specific flags can also be placed in `~/.config/vivaldi-stable.conf` for persistent application."],["CAVEATS","Not fully open source: the UI layer is proprietary while the Chromium base is open source. Some Chromium features may behave differently due to Vivaldi modifications."],["INSTALL","```pacman: sudo pacman -S vivaldi```\n\n```nix: nix profile install nixpkgs#vivaldi```"],["SEE ALSO","[chromium](/man/chromium)(1), [google-chrome](/man/google-chrome)(1), [firefox](/man/firefox)(1), [brave](/man/brave)(1)"]],"e":["chromium","google-chrome","firefox","brave"],"x":"vivaldi-stable"},"vulkaninfo":{"n":"vulkaninfo","t":"Display Vulkan GPU and driver information","s":[["TAGLINE","Display Vulkan GPU and driver information"],["TLDR","**Show Vulkan information summary**\n\n```vulkaninfo --summary```\n\n**Display full Vulkan information** (verbose)\n\n```vulkaninfo```\n\n**Output to a file**\n\n```vulkaninfo -o [vulkan_info.txt]```\n\n**Generate JSON output**\n\n```vulkaninfo --json```\n\n**Generate HTML report**\n\n```vulkaninfo --html```\n\n**Show all available formats**\n\n```vulkaninfo --show-formats```\n\n**Target specific GPU** in multi-GPU system\n\n```vulkaninfo --json=[0]```"],["SYNOPSIS","**vulkaninfo** [**--summary**] [**--json**[=_gpu_]] [**--html**] [**--text**] [**-o** _file_] [**--show-formats**]"],["PARAMETERS","**--summary**\n> Display a brief summary of Vulkan information for all GPUs.\n\n**--text**\n> Produce text output to stdout. This is the default format.\n\n**--html**\n> Generate HTML report saved as vulkaninfo.html.\n\n**-j**, **--json**[=_gpu_]\n> Produce JSON output conforming to Vulkan Profiles schema. Optional GPU number targets specific device.\n\n**-o** _file_, **--output** _file_\n> Save output to the specified file.\n\n**--show-formats**\n> Display detailed information about supported image formats.\n\n**--show-all**\n> Display all available information.\n\n**-h**, **--help**\n> Display help message."],["DESCRIPTION","**vulkaninfo** displays detailed information about Vulkan API support on the system, including physical devices (GPUs), available extensions, layers, memory types, queue families, and supported formats.\n\nThe tool queries the Vulkan runtime to enumerate all Vulkan-capable devices and their capabilities. This includes device properties (name, type, driver version, API version), limits, supported features, memory heaps, and queue family properties.\n\nOutput can be extremely verbose on systems with multiple GPUs or extensive driver features. Use **--summary** for a quick overview or redirect output to a file for detailed analysis. The tool is essential for verifying Vulkan installation, debugging driver issues, and determining hardware capabilities for application development.\n\nvulkaninfo is part of the vulkan-tools package on most Linux distributions."],["CAVEATS","Requires working Vulkan drivers and runtime. Default output can be thousands of lines; use **--summary** for brief output. Some features require specific Vulkan extensions to be present. Multi-GPU systems show information for all devices unless filtered."],["HISTORY","**vulkaninfo** is developed by the **Khronos Group** as part of the Vulkan SDK tools, maintained by **LunarG**. Vulkan was announced in **2015** and released in **February 2016** as the successor to OpenGL for high-performance graphics. vulkaninfo has been part of the Vulkan SDK since its initial release, serving as the primary diagnostic tool for Vulkan installations."],["INSTALL","```apt: sudo apt install vulkan-tools```\n\n```dnf: sudo dnf install vulkan-tools```\n\n```pacman: sudo pacman -S vulkan-tools```\n\n```apk: sudo apk add vulkan-tools```\n\n```zypper: sudo zypper install vulkan-tools```\n\n```brew: brew install vulkan-tools```\n\n```nix: nix profile install nixpkgs#vulkan-tools```"],["SEE ALSO","[glxinfo](/man/glxinfo)(1), [nvidia-smi](/man/nvidia-smi)(1), [lspci](/man/lspci)(8), [clinfo](/man/clinfo)(1)"]],"e":["glxinfo","nvidia-smi","lspci","clinfo"],"x":"vulkaninfo --summary"},"wmctrl":{"n":"wmctrl","t":"Command-line window manager control","s":[["TAGLINE","Command-line window manager control"],["TLDR","**List** windows\n\n```wmctrl -l```\n\n**Activate** window by title\n\n```wmctrl -a [window_title]```\n\n**Move** window and focus\n\n```wmctrl -R [window_title]```\n\n**Switch** workspace\n\n```wmctrl -s [workspace_number]```\n\nToggle **fullscreen**\n\n```wmctrl -r [window_title] -b toggle,fullscreen```\n\nMove window to **workspace**\n\n```wmctrl -r [window_title] -t [workspace_number]```"],["SYNOPSIS","**wmctrl** [_OPTIONS_]"],["PARAMETERS","**-l**\n> List windows\n\n**-a** _TITLE_\n> Activate window matching title\n\n**-R** _TITLE_\n> Move window to current workspace and focus\n\n**-s** _N_\n> Switch to workspace N\n\n**-r** _TITLE_\n> Select window for action\n\n**-b** _ACTION_\n> Modify window state (toggle, add, remove)\n\n**-t** _N_\n> Move window to workspace N"],["DESCRIPTION","**wmctrl** is a command-line tool for interacting with an X Window Manager that supports the EWMH/NetWM specification. It allows listing, switching, and manipulating windows from scripts or the terminal.\n\nWindow titles can be partial matches. Multiple actions can be combined."],["CAVEATS","X11 only. Requires EWMH-compliant window manager. Some features depend on window manager support."],["INSTALL","```apt: sudo apt install wmctrl```\n\n```dnf: sudo dnf install wmctrl```\n\n```pacman: sudo pacman -S wmctrl```\n\n```zypper: sudo zypper install wmctrl```\n\n```brew: brew install wmctrl```\n\n```nix: nix profile install nixpkgs#wmctrl```"],["SEE ALSO","[xdotool](/man/xdotool)(1), [xprop](/man/xprop)(1)"]],"e":["xdotool","xprop"],"x":"wmctrl -l"},"xdp-filter":{"n":"xdp-filter","t":"XDP-based network packet filtering","s":[["TAGLINE","XDP-based network packet filtering"],["TLDR","**Load** filter on interface\n\n```sudo xdp-filter load -p allow -m skb [network_interface]```\n\n**Unload** filter\n\n```sudo xdp-filter unload [network_interface]```\n\n**Deny** destination port\n\n```sudo xdp-filter port [destination_port]```\n\nDeny **source IP**\n\n```sudo xdp-filter ip -m src [source_ip]```\n\nDeny **source MAC**\n\n```sudo xdp-filter ether -m src [mac_address]```\n\nShow **statistics**\n\n```sudo xdp-filter poll -i 10000```"],["SYNOPSIS","**xdp-filter** _COMMAND_ [_OPTIONS_]"],["PARAMETERS","**load** _INTERFACE_\n> Load filter on network interface\n\n**unload** _INTERFACE_\n> Unload filter from interface\n\n**port** _PORT_\n> Filter by port number\n\n**ip** _ADDRESS_\n> Filter by IP address\n\n**ether** _ADDRESS_\n> Filter by MAC address\n\n**poll**\n> Show statistics\n\n**-p, --policy** _POLICY_\n> Set default policy (allow/deny)\n\n**-m, --mode** _MODE_\n> Filter mode (src/dst for direction, skb/native for XDP mode)\n\n**-i, --interval** _MS_\n> Poll interval in milliseconds"],["DESCRIPTION","**xdp-filter** loads and manages eBPF XDP (eXpress Data Path) packet filters. It provides high-performance packet filtering at the network driver level, before packets reach the kernel networking stack.\n\nPart of the xdp-tools collection for managing XDP programs."],["CAVEATS","Requires root privileges. Not all network drivers support native XDP mode. Filters operate at layer 2/3, not application layer."],["INSTALL","```apt: sudo apt install xdp-tools```\n\n```dnf: sudo dnf install xdp-tools```\n\n```pacman: sudo pacman -S xdp-tools```\n\n```apk: sudo apk add xdp-tools```\n\n```zypper: sudo zypper install xdp-tools```\n\n```nix: nix profile install nixpkgs#xdp-tools```"],["SEE ALSO","[iptables](/man/iptables)(8), [nftables](/man/nftables)(8), [tc](/man/tc)(8)"]],"e":["iptables","nftables","tc"],"x":"sudo xdp-filter load -p allow -m skb [network_interface]"},"xpilot":{"n":"xpilot","t":"Multiplayer space combat game","s":[["TAGLINE","Multiplayer space combat game"],["TLDR","**Start the game** and connect to a server\n\n```xpilot```\n\n**Connect to a specific server**\n\n```xpilot -join [server_address]```\n\n**Set player name** for the session\n\n```xpilot -name \"[player_name]\"```\n\n**Connect with team selection**\n\n```xpilot -join [server] -team [team_number]```\n\n**Use a specific ship shape**\n\n```xpilot -shipShape \"[shape_string]\"```\n\n**List available servers** on the network\n\n```xpilot -list```\n\n**Start with specific display**\n\n```xpilot -display [hostname:0]```"],["SYNOPSIS","**xpilot** [_-display host:n_] [_-join server_] [_-name name_] [_-team n_] [_-list_] [_-port n_] [_-shipShape shape_] [_options_]"],["PARAMETERS","**-display** _host:n_\n> Connect to X server on specified display.\n\n**-join** _server_\n> Connect directly to the specified game server.\n\n**-name** _name_\n> Set the player name (default: login name).\n\n**-team** _n_\n> Join the specified team number.\n\n**-list**\n> Query and list available servers on the network.\n\n**-port** _n_\n> Use specified port number for server connection.\n\n**-shipShape** _shape_\n> Define custom ship appearance using shape string.\n\n**-geometry** _WxH+X+Y_\n> Set window geometry.\n\n**-fps** _n_\n> Target frames per second.\n\n**-keyTurnLeft** _key_\n> Bind key for turning left.\n\n**-keyTurnRight** _key_\n> Bind key for turning right.\n\n**-keyThrust** _key_\n> Bind key for thrust.\n\n**-keyFire** _key_\n> Bind key for firing.\n\n**-help**\n> Display help and available options."],["DESCRIPTION","**XPilot** is a multiplayer tactical space combat game for the X Window System. Players pilot spacecraft in a 2D arena, competing against others in various game modes including team battles, capture the flag, and survival matches.\n\nThe game features Newtonian physics with momentum and gravity, requiring players to master thrust and rotation for maneuvering. Ships can collect power-ups for weapons like missiles, lasers, and mines. The gameplay emphasizes skill in navigation and combat timing.\n\nPlayers connect to dedicated servers which host the game maps and manage the match. Multiple clients can connect to a single server for competitive play. The game supports both internet play and LAN sessions. Custom ship designs can be created using the ship shape specification language.\n\nXPilot has an active community with various forks and enhanced versions. Configuration can be customized through command-line options or the **.xpilotrc** configuration file in the user's home directory."],["CAVEATS","Requires an X11 display. Network latency significantly affects gameplay on internet servers. Some servers may run modified versions with different rules or physics. The original XPilot has been superseded by forks like XPilot-AI and XPilot NG with improved features."],["HISTORY","**XPilot** was created by Bjorn Stabell and Ken Ronny Schouten, with the first public release in **1991**. It became one of the earliest popular networked multiplayer games for Unix systems. The game gained a dedicated following throughout the 1990s with servers running worldwide. Various forks emerged including XPilot NG (Next Generation) which modernized the codebase and added features."],["INSTALL","```aur: yay -S xpilot```"],["SEE ALSO","[xpilot-ng](/man/xpilot-ng)(1), [xscreensaver](/man/xscreensaver)(1)"]],"e":["xpilot-ng","xscreensaver"],"x":"xpilot"},"yadm-transcrypt":{"n":"yadm-transcrypt","t":"Transparent encryption for yadm files","s":[["TAGLINE","Transparent encryption for yadm files"],["TLDR","**Initialize transcrypt**\n\n```yadm transcrypt -c aes-256-gcm```\n\n**Unlock files**\n\n```yadm transcrypt --display```\n\n**Configure transcrypt**\n\n```yadm transcrypt -p [password]```"],["SYNOPSIS","**yadm** **transcrypt** [_options_]"],["PARAMETERS","**-c** _cipher_\n> Encryption cipher.\n\n**-p** _password_\n> Set password.\n\n**--display**\n> Show configuration."],["DESCRIPTION","**yadm transcrypt** wraps transcrypt for transparent file encryption. Encrypts files on push using .gitattributes patterns. Alternative encryption method to yadm encrypt."],["SEE ALSO","[yadm](/man/yadm)(1), [transcrypt](/man/transcrypt)(1)"]],"e":["yadm","transcrypt"],"x":"yadm transcrypt -c aes-256-gcm"},"zabbix_get":{"n":"zabbix_get","t":"Retrieve data from Zabbix agents","s":[["TAGLINE","Retrieve data from Zabbix agents"],["TLDR","**Get CPU load from agent**\n\n```zabbix_get -s [192.168.1.10] -k \"system.cpu.load[all,avg1]\"```\n\n**Get data from specific port**\n\n```zabbix_get -s [host] -p [10050] -k \"[agent.version]\"```\n\n**Get disk free space**\n\n```zabbix_get -s [host] -k \"vfs.fs.size[/,free]\"```\n\n**Get with timeout**\n\n```zabbix_get -s [host] -t [60] -k \"[key]\"```\n\n**Get using TLS PSK**\n\n```zabbix_get -s [host] --tls-connect psk --tls-psk-identity [id] --tls-psk-file [psk.txt] -k \"[key]\"```"],["SYNOPSIS","**zabbix_get** -s _host_ -k _key_ [_options_]"],["PARAMETERS","**-s**, **--host** _host_\n> Host name or IP address of Zabbix agent.\n\n**-p**, **--port** _port_\n> Port number (default: 10050).\n\n**-k**, **--key** _key_\n> Item key to retrieve.\n\n**-I**, **--source-address** _ip_\n> Source IP address for connection.\n\n**-t**, **--timeout** _seconds_\n> Timeout (1-600, default: 30).\n\n**--tls-connect** _mode_\n> Connection type: unencrypted, psk, or cert.\n\n**--tls-psk-identity** _id_\n> PSK identity string.\n\n**--tls-psk-file** _file_\n> File containing pre-shared key.\n\n**-P**, **--protocol** _value_\n> Communication protocol: auto (default, JSON with plaintext fallback), json, or plaintext (for agents 6.4.x and older).\n\n**--tls-ca-file** _file_\n> Full path to file containing top-level CA certificates for peer certificate verification.\n\n**--tls-cert-file** _file_\n> Full path to file containing the client certificate.\n\n**--tls-key-file** _file_\n> Full path to file containing the private key.\n\n**-h**, **--help**\n> Display help information.\n\n**-V**, **--version**\n> Display version."],["DESCRIPTION","**zabbix_get** retrieves data from Zabbix agents for testing and troubleshooting. It performs passive checks, querying agents for specific item values using the same protocol as Zabbix server.\n\nThe tool helps verify agent configuration and connectivity before adding hosts to monitoring. The requesting host must be listed in the agent's Server configuration parameter.\n\nCommon use cases include testing new item keys, verifying agent responses, and debugging collection issues without accessing the Zabbix frontend."],["EXIT STATUS","Returns 0 on success, 1 on error or if value retrieval failed."],["CAVEATS","Requesting host must be in agent's Server parameter. Only works with Zabbix agents, not other monitoring protocols. TLS requires matching configuration on both ends."],["HISTORY","**zabbix_get** is part of the Zabbix monitoring system, created by Alexei Vladishev in 2001. Zabbix has grown into one of the most popular enterprise monitoring solutions, with zabbix_get remaining essential for agent troubleshooting."],["INSTALL","```apt: sudo apt install zabbix-proxy-mysql```\n\n```dnf: sudo dnf install zabbix-proxy-mysql```\n\n```apk: sudo apk add zabbix-utils```\n\n```zypper: sudo zypper install zabbix-proxy-mysql```"],["SEE ALSO","[zabbix_agentd](/man/zabbix_agentd)(8), [zabbix_server](/man/zabbix_server)(8), [zabbix_proxy](/man/zabbix_proxy)(8)"]],"e":["zabbix_agentd","zabbix_server","zabbix_proxy"],"x":"zabbix_get -s [192.168.1.10] -k \"system.cpu.load[all,avg1]\""},"zpool-events":{"n":"zpool-events","t":"Display ZFS pool event log","s":[["TAGLINE","Display ZFS pool event log"],["TLDR","**Show pool events**\n\n```zpool events```\n\n**Show verbose event details**\n\n```zpool events -v```\n\n**Show events for specific pool**\n\n```zpool events [poolname]```\n\n**Follow events in real-time**\n\n```zpool events -f```\n\n**Clear event log**\n\n```zpool events -c```\n\n**Show verbose and follow**\n\n```zpool events -vf```"],["SYNOPSIS","**zpool events** [_options_] [_pool_]"],["PARAMETERS","**-v**\n> Verbose output with full event payloads.\n\n**-f**\n> Follow mode; wait for new events.\n\n**-c**\n> Clear event log after displaying.\n\n**-H**\n> Scripted mode; no headers, tab-separated."],["EVENT CLASSES","**ereport.fs.zfs.checksum**: Checksum error detected.\n\n**ereport.fs.zfs.io**: I/O error on vdev.\n\n**ereport.fs.zfs.delay**: Slow I/O completion.\n\n**sysevent.fs.zfs.pool_import**: Pool imported.\n\n**sysevent.fs.zfs.pool_export**: Pool exported.\n\n**sysevent.fs.zfs.scrub_start**: Scrub started.\n\n**sysevent.fs.zfs.resilver_finish**: Resilver completed."],["DESCRIPTION","**zpool events** displays the internal event log maintained by ZFS since the kernel module was loaded. Events include errors, pool state changes, scrub operations, and device status updates.\n\nThe event log is stored in RAM with a configurable maximum size. Events are invaluable for diagnosing hardware problems, tracking pool operations, and understanding ZFS behavior.\n\nThe ZFS Event Daemon (zed) monitors these events in real-time and can trigger scripts for notifications or automated responses to events."],["CAVEATS","Events stored in RAM; lost on reboot or module unload. Log size limited by zfs_event_len_max parameter. Use zed for persistent event handling and alerting."],["HISTORY","The ZFS event system provides visibility into pool operations and errors that traditional filesystems lack. It enables proactive monitoring and automated responses through the zed daemon, making ZFS self-healing capabilities more accessible."],["INSTALL","```apt: sudo apt install zfs-fuse```\n\n```apk: sudo apk add zfs```\n\n```nix: nix profile install nixpkgs#zfs```"],["SEE ALSO","[zpool-status](/man/zpool-status)(8), [zpool](/man/zpool)(8), [zed](/man/zed)(8), [zfs](/man/zfs)(8)"]],"e":["zpool-status","zpool","zed","zfs"],"x":"zpool events"}});
