window.LCL_BUCKET(65,{"ansible-lint":{"n":"ansible-lint","t":"Check playbooks for best practices","s":[["TAGLINE","Check playbooks for best practices"],["TLDR","**Lint** a playbook\n\n```ansible-lint [playbook.yml]```\n\nLint **all files** in directory\n\n```ansible-lint```\n\nLint with **specific profile**\n\n```ansible-lint --profile [production] [playbook.yml]```\n\n**List** all rules\n\n```ansible-lint -L```\n\n**Skip** specific rules\n\n```ansible-lint --skip-list [yaml,risky-shell-pipe] [playbook.yml]```\n\nOutput in **JSON** format\n\n```ansible-lint -f json [playbook.yml]```"],["SYNOPSIS","**ansible-lint** [_--profile profile_] [_-x skip_] [_-f format_] [_files_]"],["DESCRIPTION","**ansible-lint** checks Ansible playbooks, roles, and collections for practices and behaviors that could potentially be improved. It identifies deprecated syntax, security concerns, style issues, and best practice violations.\n\nThe tool helps maintain consistent, high-quality Ansible code and catch common mistakes before deployment."],["PARAMETERS","**-L**, **--list-rules**\n> List all available rules\n\n**-T**, **--list-tags**\n> List all the tags and the rules they cover\n\n**-x** _rules_, **--skip-list** _rules_\n> Skip specific rules or tags\n\n**-w** _rules_, **--warn-list** _rules_\n> Treat the listed rules or tags as warnings only\n\n**--profile** _profile_\n> Select a rule profile: min, basic, moderate, safety, shared, or production\n\n**-f** _format_, **--format** _format_\n> Output format: brief, full, md, json (alias of codeclimate), codeclimate, quiet, pep8, sarif\n\n**--fix** [_rules_]\n> Auto-fix violations, optionally limited to specific rules or tags\n\n**-q**, **--quiet**\n> Quieter output (may be given twice)\n\n**--strict**\n> Return a non-zero exit code on warnings\n\n**--offline**\n> Disable installation of requirements and dependencies\n\n**-c** _file_, **--config-file** _file_\n> Use the specified configuration file"],["CONFIGURATION","**.ansible-lint**\n> Project-level configuration file for rule selection, skip lists, and profiles.\n\n**~/.config/ansible-lint/config.yml**\n> User-level default configuration."],["CAVEATS","Not all rules apply to all use cases; configure skip-list appropriately. The --fix option may not handle all issues. False positives may occur with complex variable usage."],["HISTORY","**ansible-lint** was created to help enforce best practices in Ansible projects. It became an official Ansible project and has been actively maintained with evolving rule sets."],["INSTALL","```pacman: sudo pacman -S ansible-lint```\n\n```apk: sudo apk add ansible-lint```\n\n```zypper: sudo zypper install ansible-lint```\n\n```brew: brew install ansible-lint```\n\n```nix: nix profile install nixpkgs#ansible-lint```"],["SEE ALSO","[ansible](/man/ansible)(1), [ansible-playbook](/man/ansible-playbook)(1), [yamllint](/man/yamllint)(1)"],["RESOURCES","```[Source code](https://github.com/ansible/ansible-lint)```\n\n```[Documentation](https://docs.ansible.com/projects/lint/)```"]],"e":["ansible","ansible-playbook","yamllint"],"x":"ansible-lint [playbook.yml]"},"apparmor_status":{"n":"apparmor_status","t":"Report the current AppArmor confinement state.","s":[["TAGLINE","Report the current AppArmor confinement state."],["TLDR","This command is an **alias** of aa-status\n\nView documentation for the original command\n\n```tldr aa-status```"],["SYNOPSIS","**apparmor_status**"],["DESCRIPTION","**apparmor_status** is an alias for **aa-status**, which reports various aspects of the current state of AppArmor confinement. See **aa-status** for full documentation."],["INSTALL","```apt: sudo apt install apparmor```\n\n```pacman: sudo pacman -S apparmor```\n\n```apk: sudo apk add apparmor-utils```\n\n```zypper: sudo zypper install apparmor-utils```\n\n```nix: nix profile install nixpkgs#apparmor-utils```"],["SEE ALSO","[aa-status](/man/aa-status)(8)"],["RESOURCES","```[Source code](https://gitlab.com/apparmor/apparmor)```\n\n```[Homepage](https://apparmor.net)```"]],"e":["aa-status"],"x":"tldr aa-status"},"az-logout":{"n":"az-logout","t":"Remove Azure CLI authentication credentials","s":[["TAGLINE","Remove Azure CLI authentication credentials"],["TLDR","**Logout** current user\n\n```az logout```\n\nLogout **specific user**\n\n```az logout --username [user@example.com]```"],["SYNOPSIS","**az logout** [_options_]"],["DESCRIPTION","**az logout** logs out to remove access to Azure subscriptions. It clears access tokens and refresh tokens for the account from the local credential cache.\n\nThe command is useful when switching between accounts or securing a shared system."],["PARAMETERS","**--username** _user_\n> Account user to log out. If missing, log out the current active account."],["WORKFLOW","```bash"],["Logout current account","az logout"],["Login with different account","az login\n```"],["CAVEATS","Does not revoke tokens on Azure side, only removes local cache. Service principal credentials in environment variables are not affected. Managed identity authentication persists."],["HISTORY","**az logout** has been available since Azure CLI 2.0 in **2017** as the companion to az login for credential management."],["INSTALL","```dnf: sudo dnf install azure-cli```\n\n```pacman: sudo pacman -S azure-cli```\n\n```zypper: sudo zypper install azure-cli```\n\n```brew: brew install azure-cli```\n\n```nix: nix profile install nixpkgs#azure-cli```"],["SEE ALSO","[az-login](/man/az-login)(1), [az-account](/man/az-account)(1), [az](/man/az)(1)"],["RESOURCES","```[Source code](https://github.com/Azure/azure-cli)```\n\n```[Documentation](https://learn.microsoft.com/en-us/cli/azure/reference-index#az-logout)```\n\n```[Homepage](https://learn.microsoft.com/en-us/cli/azure/)```"]],"e":["az-login","az-account","az"],"x":"az logout"},"bazel":{"n":"bazel","t":"Fast, scalable multi-language build system","s":[["TAGLINE","Fast, scalable multi-language build system"],["TLDR","**Build** target\n\n```bazel build [//path/to:target]```\n\n**Run** binary\n\n```bazel run [//path/to:binary]```\n\n**Test** targets\n\n```bazel test [//path/to:tests]```\n\n**Query** dependencies\n\n```bazel query \"deps([//path/to:target])\"```\n\n**Clean** build artifacts\n\n```bazel clean```\n\nBuild **all** targets recursively\n\n```bazel build //...```\n\n**Info** about build environment\n\n```bazel info```"],["SYNOPSIS","**bazel** _command_ [_options_] [_targets_]"],["DESCRIPTION","**bazel** is a fast, scalable build system that supports multi-language projects and massive codebases. Developed by Google based on their internal Blaze system, it provides reproducible builds, remote caching, and distributed execution.\n\nThe tool is designed for monorepos and projects requiring strict dependency management and incremental builds."],["PARAMETERS","**build** _target_\n> Build specified targets\n\n**run** _target_\n> Build and run executable target\n\n**test** _target_\n> Build and run tests\n\n**query** _expression_\n> Query build graph\n\n**cquery** _expression_\n> Query configured build graph (post-analysis)\n\n**aquery** _expression_\n> Query action graph\n\n**clean**\n> Remove build artifacts\n\n**fetch** _target_\n> Fetch external dependencies\n\n**coverage** _target_\n> Generate code coverage report for tests\n\n**version**\n> Print Bazel version\n\n**shutdown**\n> Stop the Bazel server\n\n**info**\n> Show build environment info\n\n**--jobs** _n_\n> Number of parallel jobs\n\n**--config** _name_\n> Use configuration from .bazelrc\n\n**--remote_cache** _url_\n> Remote cache URL\n\n**--disk_cache** _path_\n> Disk cache location\n\n**--compilation_mode** (_-c_) _mode_\n> Compilation mode: fastbuild, dbg, or opt\n\n**--keep_going** (_-k_)\n> Continue building after errors\n\n**--verbose_failures**\n> Show full command line for failed commands"],["TARGET SYNTAX","- **//path/to:target** - Specific target\n- **//path/to:all** - All targets in package\n- **//...** - All targets recursively\n- **@repo//path:target** - External repository"],["FEATURES","- Incremental builds\n- Remote caching\n- Distributed execution\n- Hermetic builds\n- Multi-language support (Java, C++, Go, Python, etc.)\n- Dependency analysis\n- Reproducible builds"],["BUILD FILES","BUILD or BUILD.bazel files define targets:\n```python\ncc_binary(\n    name = \"hello\",\n    srcs = [\"hello.cc\"],\n)\n```"],["CONFIGURATION","**~/.bazelrc**\n> User-level build options and configuration flags.\n\n**WORKSPACE** or **WORKSPACE.bazel** (or **MODULE.bazel** for Bzlmod)\n> Root file that defines external dependencies and the project boundary.\n\n**.bazelrc**\n> Project-level build options, config groups, and default flags."],["CAVEATS","Steep learning curve. Requires BUILD files throughout project. Initial setup complex. Can be overkill for small projects. Build times include analysis phase. JVM-based (high memory usage)."],["HISTORY","**Bazel** was released by Google in **2015** as an open-source version of their internal Blaze build system, designed to handle massive multi-language monorepos."],["INSTALL","```apt: sudo apt install bazel-bootstrap```\n\n```pacman: sudo pacman -S bazel```\n\n```brew: brew install bazel```\n\n```nix: nix profile install nixpkgs#bazel```"],["SEE ALSO","[make](/man/make)(1), [cmake](/man/cmake)(1), [ninja](/man/ninja)(1), [gradle](/man/gradle)(1), [buck](/man/buck)(1)"]],"e":["make","cmake","ninja","gradle","buck"],"x":"bazel build [//path/to:target]"},"bchunk":{"n":"bchunk","t":"Convert BIN/CUE CD images to ISO and audio tracks","s":[["TAGLINE","Convert BIN/CUE CD images to ISO and audio tracks"],["TLDR","**Convert** a binary CD image to ISO\n\n```bchunk [path/to/image.bin] [path/to/image.cue] [path/to/output]```\n\nConvert with **verbose mode**\n\n```bchunk -v [path/to/image.bin] [path/to/image.cue] [path/to/output]```\n\nOutput audio files in **WAV format**\n\n```bchunk -w [path/to/image.bin] [path/to/image.cue] [path/to/output]```"],["SYNOPSIS","**bchunk** [_options_] _image.bin_ _image.cue_ _output_"],["DESCRIPTION","**bchunk** converts CD images in BIN/CUE format to standard ISO 9660 images and audio tracks. It reads the CUE sheet to determine track layout and extracts each track to a separate file.\n\nData tracks are converted to ISO format, while audio tracks can be output as raw CDR audio or WAV files. The output files are named with the specified prefix and numbered sequentially."],["PARAMETERS","**-v**\n> Verbose mode; display detailed progress information\n\n**-w**\n> Output audio tracks as WAV files instead of raw CDR\n\n**-r**\n> Raw mode; do not convert data tracks to ISO format\n\n**-p**\n> Use PSX (PlayStation) mode for certain disc types\n\n**-s**\n> Swap byte order in audio tracks"],["CAVEATS","BIN files must match the layout described in the CUE sheet exactly. Multi-session discs may not convert correctly. Some protected or non-standard disc images may fail to convert."],["HISTORY","**bchunk** (BIN/CUE CHunK) was developed as a tool to convert proprietary CD image formats to standard formats usable by CD burning software and emulators."],["INSTALL","```apt: sudo apt install bchunk```\n\n```dnf: sudo dnf install bchunk```\n\n```pacman: sudo pacman -S bchunk```\n\n```zypper: sudo zypper install bchunk```\n\n```brew: brew install bchunk```\n\n```nix: nix profile install nixpkgs#bchunk```"],["SEE ALSO","[cdrdao](/man/cdrdao)(1), [wodim](/man/wodim)(1)"]],"e":["cdrdao","wodim"],"x":"bchunk [path/to/image.bin] [path/to/image.cue] [path/to/output]"},"bitcoind":{"n":"bitcoind","t":"Bitcoin Core full node daemon","s":[["TAGLINE","Bitcoin Core full node daemon"],["TLDR","**Start** Bitcoin daemon\n\n```bitcoind -daemon```\n\n**Start** with configuration\n\n```bitcoind -conf=[bitcoin.conf]```\n\n**Stop** daemon\n\n```bitcoin-cli stop```\n\n**Run** in foreground\n\n```bitcoind```"],["SYNOPSIS","**bitcoind** [_options_]"],["DESCRIPTION","**bitcoind** is the Bitcoin Core daemon. It maintains a full copy of the Bitcoin blockchain, validates transactions and blocks, and can serve as a wallet. The daemon provides RPC and REST interfaces for interaction.\n\nRunning a full node contributes to Bitcoin network decentralization and allows trustless validation of transactions."],["PARAMETERS","**-daemon**\n> Run as background daemon\n\n**-conf=**_file_\n> Configuration file path\n\n**-datadir=**_dir_\n> Data directory\n\n**-chain=**_chain_\n> Select chain: main, testnet4, signet, regtest (default: main)\n\n**-testnet4**\n> Use testnet4 network\n\n**-signet**\n> Use signet network\n\n**-regtest**\n> Regression test mode\n\n**-prune=**_n_\n> Enable pruning (keep only N MB)\n\n**-txindex**\n> Maintain full transaction index\n\n**-rpcuser=**_user_\n> RPC username\n\n**-rpcpassword=**_pass_\n> RPC password\n\n**-rpcport=**_port_\n> RPC port\n\n**-server**\n> Accept command line and JSON-RPC commands\n\n**-blocksonly**\n> Reduce bandwidth by not relaying transactions"],["CONFIGURATION","**~/.bitcoin/bitcoin.conf**\n> Main configuration file for daemon mode, RPC credentials, network selection, pruning, and indexing options."],["SYSTEM REQUIREMENTS","- **Disk**: ~600 GB+ (full blockchain)\n- **RAM**: 2 GB minimum, 4 GB recommended\n- **Network**: Unlimited or high cap\n- **Bandwidth**: ~20 GB/month upload"],["FEATURES","- Full blockchain validation\n- Wallet functionality\n- Mining support (via external miners)\n- RPC/REST APIs\n- Pruning mode (reduced storage)\n- Testnet/regtest support\n- ZMQ notifications"],["WORKFLOW","```bash"],["Start daemon","bitcoind -daemon"],["Check status","bitcoin-cli getblockchaininfo"],["Wait for sync (can take days)","bitcoin-cli getblockcount"],["Stop daemon","bitcoin-cli stop\n```"],["CAVEATS","Initial sync takes days/weeks. Large disk space required (unless pruned). High bandwidth usage. Keep wallet backups secure. RPC should not be exposed to internet. The -testnet flag (testnet3) was deprecated in v28 and removed in v30; use -testnet4 or -signet instead."],["HISTORY","**bitcoind** was created by Satoshi Nakamoto as part of the original Bitcoin software in **2009**, serving as the reference implementation of the Bitcoin protocol."],["INSTALL","```apk: sudo apk add bitcoin```\n\n```zypper: sudo zypper install bitcoind```\n\n```brew: brew install bitcoin```\n\n```nix: nix profile install nixpkgs#bitcoin```"],["SEE ALSO","[bitcoin-cli](/man/bitcoin-cli)(1), [bitcoin-qt](/man/bitcoin-qt)(1)"]],"e":["bitcoin-cli","bitcoin-qt"],"x":"bitcoind -daemon"},"bluetoothd":{"n":"bluetoothd","t":"Bluetooth system daemon","s":[["TAGLINE","Bluetooth system daemon"],["TLDR","**Start** the daemon\n\n```bluetoothd```\n\nStart the daemon, **logging to stdout**\n\n```bluetoothd -n```\n\nStart with a **specific configuration** file\n\n```bluetoothd -f [path/to/file]```\n\nStart with **verbose debug** output\n\n```bluetoothd -d```\n\nStart with debug output from **specific source files**\n\n```bluetoothd -d=[path/to/file1:path/to/file2]```"],["SYNOPSIS","**bluetoothd** [_options_]"],["DESCRIPTION","**bluetoothd** is the Bluetooth system daemon that manages Bluetooth devices, connections, and services. It handles device discovery, pairing, and connection management for all Bluetooth functionality on the system.\n\nThe daemon is typically started automatically by systemd. It reads configuration from **/etc/bluetooth/main.conf** and manages the Bluetooth adapter(s) and connected devices."],["PARAMETERS","**-n, --nodetach**\n> Run in the foreground; do not detach and become a daemon. Log goes to stdout.\n\n**-f, --configfile** _file_\n> Use alternate configuration file instead of /etc/bluetooth/main.conf.\n\n**-d, --debug** [_files_]\n> Enable debug output, optionally limited to a colon-separated list of source files.\n\n**-E, --experimental**\n> Enable experimental D-Bus interfaces and features.\n\n**-p, --plugin** _name_\n> Specify plugins to load\n\n**-P, --noplugin** _name_\n> Specify plugins to exclude"],["CONFIGURATION","**/etc/bluetooth/main.conf**\n> Main configuration file controlling device class, name, pairing timeout, discoverable mode, and adapter defaults."],["CAVEATS","Usually managed by systemd; manual invocation rarely needed. Requires root privileges. Configuration changes require daemon restart. Debug mode generates extensive output."],["HISTORY","**bluetoothd** is part of **BlueZ**, the official Linux Bluetooth protocol stack maintained since the early **2000s**. It replaced the older per-profile daemons of BlueZ 4 with a single plugin-based daemon in BlueZ 5 (**2012**), exposing its functionality over D-Bus."],["INSTALL","```apt: sudo apt install bluez```\n\n```dnf: sudo dnf install bluez```\n\n```pacman: sudo pacman -S bluez```\n\n```zypper: sudo zypper install bluez```\n\n```brew: brew install bluez```\n\n```nix: nix profile install nixpkgs#bluez```"],["SEE ALSO","[bluetoothctl](/man/bluetoothctl)(1), [hciconfig](/man/hciconfig)(1), [hcitool](/man/hcitool)(1)"],["RESOURCES","```[Homepage](http://www.bluez.org/)```\n\n```[Source code](https://github.com/bluez/bluez)```"]],"e":["bluetoothctl","hciconfig","hcitool"],"x":"bluetoothd"},"captoinfo":{"n":"captoinfo","t":"Convert termcap descriptions to terminfo format","s":[["TAGLINE","Convert termcap descriptions to terminfo format"],["TLDR","**Convert a termcap file** to terminfo format\n\n```captoinfo [path/to/termcap_file]```\n\n**Convert and compile** to terminfo database in one step\n\n```captoinfo [path/to/termcap_file] | tic```\n\n**Convert with verbose output** for debugging\n\n```captoinfo -v [path/to/termcap_file]```\n\n**Output fields one per line** instead of wrapped\n\n```captoinfo -1 [path/to/termcap_file]```\n\n**Convert with extended capabilities** support\n\n```captoinfo -x [path/to/termcap_file]```\n\n**Display version** information\n\n```captoinfo -V```"],["SYNOPSIS","**captoinfo** [**-v**_n_] [**-V**] [**-1**] [**-w** _width_] [**-x**] [_file_...]"],["PARAMETERS","**-v**_n_\n> Print verbose comments and debugging information to standard error. Higher values increase verbosity.\n\n**-V**\n> Display the version number of the ncurses library and exit.\n\n**-1**\n> Output fields one per line instead of wrapping multiple fields to a maximum width of 60 characters.\n\n**-w** _width_\n> Set the maximum output width for wrapped fields.\n\n**-x**\n> Enable parsing and emission of extended terminfo capabilities for modern terminal features."],["DESCRIPTION","**captoinfo** converts termcap terminal capability descriptions into the terminfo format. For each termcap entry found in the input file, an equivalent terminfo description is written to standard output. It also translates some obsolete, nonstandard capabilities into standard (SVr4/XSI Curses) terminfo capabilities.\n\nIf no files are specified, captoinfo reads from the file named in the **TERMCAP** environment variable and extracts only the entry for the terminal named in **TERM**. If TERMCAP is not set, it reads from /etc/termcap by default.\n\nThe utility is implemented as a link to **tic**(1) with the **-I** option implied. Other tic options like **-1**, **-f**, **-v**, **-w**, and **-x** can be used."],["CAVEATS","Some termcap capabilities may not have direct terminfo equivalents. Complex or non-standard termcap entries may require manual adjustment after conversion. The output is terminfo source format and must be compiled with **tic** to be used by applications."],["HISTORY","The termcap database was the original mechanism for terminal descriptions in Unix, widely used in the 1970s and early 1980s. Its limitations (fixed-size buffer, lack of strong typing) led to terminfo development in the mid-1980s. **captoinfo** was created as part of the ncurses project to facilitate migration from termcap to terminfo, allowing administrators to convert existing terminal definitions rather than rewriting them from scratch."],["INSTALL","```apt: sudo apt install ncurses-bin```\n\n```dnf: sudo dnf install ncurses```\n\n```pacman: sudo pacman -S ncurses```\n\n```apk: sudo apk add ncurses```\n\n```brew: brew install ncurses```\n\n```nix: nix profile install nixpkgs#ncurses```"],["SEE ALSO","[tic](/man/tic)(1), [infocmp](/man/infocmp)(1), [infotocap](/man/infotocap)(1), [tput](/man/tput)(1)"]],"e":["tic","infocmp","infotocap","tput"],"x":"captoinfo [path/to/termcap_file]"},"checkov":{"n":"checkov","t":"static analysis for infrastructure as code","s":[["TAGLINE","static analysis for infrastructure as code"],["TLDR","**Scan a directory for IaC misconfigurations**\n\n```checkov -d [path/to/directory]```\n\n**Scan a specific file**\n\n```checkov -f [path/to/file.tf]```\n\n**Scan with a specific framework**\n\n```checkov -d [path/to/directory] --framework [terraform|kubernetes|cloudformation]```\n\n**Run only specific checks**\n\n```checkov -d [path/to/directory] --check [CKV_AWS_1,CKV_AWS_2]```\n\n**Skip specific checks**\n\n```checkov -d [path/to/directory] --skip-check [CKV_AWS_1]```\n\n**Output results in JSON format**\n\n```checkov -d [path/to/directory] --output json```\n\n**Scan a Terraform plan file**\n\n```checkov -f [tfplan.json] --framework terraform_plan```"],["SYNOPSIS","**checkov** [**-d** _directory_] [**-f** _file_] [**--framework** _framework_] [**--check** _checks_] [**--skip-check** _checks_] [**--output** _format_] [_options..._]"],["DESCRIPTION","**Checkov** is a static code analysis tool for infrastructure as code (IaC) that detects security and compliance misconfigurations. It supports Terraform, CloudFormation, Kubernetes, Helm, Dockerfile, ARM templates, and other frameworks.\n\nThe tool includes over 750 built-in policies covering industry standards like CIS Benchmarks, PCI, and HIPAA. It uses graph-based scanning to analyze resource dependencies and detect complex configuration issues.\n\nCheckov also performs software composition analysis (SCA) and secrets detection using regex, keywords, and entropy-based detection."],["PARAMETERS","**-d, --directory** _path_\n> Directory to scan\n\n**-f, --file** _path_\n> Specific file to scan\n\n**--framework** _framework_\n> IaC framework: terraform, cloudformation, kubernetes, helm, dockerfile, etc.\n\n**--check** _ids_\n> Run only specific checks by ID\n\n**--skip-check** _ids_\n> Skip specific checks by ID\n\n**--output** _format_\n> Output format: cli, json, junitxml, sarif\n\n**--compact**\n> Show only failed checks\n\n**--list**\n> List all available checks"],["CONFIGURATION","**.checkov.yaml**\n> Project-level configuration for default frameworks, skip rules, and output settings."],["CAVEATS","Custom policies can be written in Python or YAML. For accurate Terraform scanning, consider scanning the plan output (**terraform plan -out=tfplan && terraform show -json tfplan > tfplan.json**) rather than just the source files."],["HISTORY","**Checkov** was originally developed by **Bridgecrew.io** and has since been acquired by **Palo Alto Networks** as part of their Prisma Cloud platform. It remains open source and actively maintained on GitHub."],["INSTALL","```brew: brew install checkov```\n\n```nix: nix profile install nixpkgs#checkov```"],["SEE ALSO","[terraform](/man/terraform)(1), [kubectl](/man/kubectl)(1), [tfsec](/man/tfsec)(1)"],["RESOURCES","```[Source code](https://github.com/bridgecrewio/checkov)```\n\n```[Documentation](https://www.checkov.io/)```"]],"e":["terraform","kubectl","tfsec"],"x":"checkov -d [path/to/directory]"},"cmdperf":{"n":"cmdperf","t":"Interactive command-line benchmarking tool","s":[["TAGLINE","Interactive command-line benchmarking tool"],["TLDR","**Benchmark** a single command with default 10 runs\n\n```cmdperf \"sleep 0.1\"```\n\n**Compare** two commands side-by-side\n\n```cmdperf \"grep 'ERROR' log.txt\" \"rg 'ERROR' log.txt\"```\n\n**Run** 100 iterations of a command\n\n```cmdperf -n 100 \"ls -la\"```\n\n**Simulate** 10 concurrent requests\n\n```cmdperf -c 10 \"curl -s https://example.com > /dev/null\"```\n\n**Run** for a fixed duration of 30 seconds\n\n```cmdperf -d 30s \"redis-cli PING\"```\n\n**Export** results to a markdown file\n\n```cmdperf --markdown [results.md] \"sleep 0.1\" \"sleep 0.2\"```"],["SYNOPSIS","**cmdperf** [_options_] _command_ [_command_...]"],["PARAMETERS","**-n**, **--runs=**_N_\n> Number of iterations to run (default: 10).\n\n**-c**, **--concurrency=**_N_\n> Number of parallel executions (default: 1).\n\n**-d**, **--duration=**_DURATION_\n> Run for a fixed duration instead of a fixed number of runs.\n\n**-r**, **--rate=**_RATE_\n> Target request rate in requests per second.\n\n**-t**, **--timeout=**_DURATION_\n> Per-command timeout (default: 1m).\n\n**-s**, **--shell=**_SHELL_\n> Shell to use for command execution (default: /bin/sh).\n\n**--shell-opt=**_OPT_\n> Shell option, repeatable (default: -c).\n\n**-N**, **--no-shell**\n> Execute command directly without wrapping in a shell.\n\n**--csv=**_FILE_\n> Export results to CSV file.\n\n**--markdown=**_FILE_\n> Export results to Markdown file.\n\n**--fail-on-error**\n> Exit with non-zero status if any command returns non-zero exit code.\n\n**--color-scheme=**_SCHEME_\n> Set color scheme (auto, catppuccin, tokyonight, nord, monokai, solarized, solarized-light, gruvbox, monochrome).\n\n**--list-color-schemes**\n> Display available color schemes.\n\n**--version**\n> Display version information."],["DESCRIPTION","**cmdperf** runs one or more shell commands repeatedly and gathers timing statistics, displaying them in a live terminal UI with a progress bar, ETA, and key metrics including mean execution time, standard deviation, and min/max ranges. It supports single command benchmarking, multi-command comparison, concurrent execution to simulate load, duration-based runs, and rate limiting for HTTP endpoint testing."],["CAVEATS","Designed for quick interactive benchmarking, not a replacement for rigorous statistical profiling tools. Default shell wrapping adds overhead; use **-N/--no-shell** for tighter measurements when shell features are not needed."],["HISTORY","**cmdperf** was created by **miklosn** and is written in Go under the MIT license. The latest release is v0.1.4 (January 2026). It fills a niche as a lightweight, interactive alternative to tools like **hyperfine**, with a focus on live TUI experience and concurrent execution simulation."],["SEE ALSO","[hyperfine](/man/hyperfine)(1), [time](/man/time)(1), [perf](/man/perf)(1)"]],"e":["hyperfine","time","perf"],"x":"cmdperf \"sleep 0.1\""},"conda-list":{"n":"conda-list","t":"installed packages viewer","s":[["TAGLINE","installed packages viewer"],["TLDR","**List packages** in current environment\n\n```conda list```\n\n**List packages** in specific environment\n\n```conda list -n [env_name]```\n\n**List packages matching pattern**\n\n```conda list [pattern]```\n\n**Show as revisions**\n\n```conda list --revisions```\n\n**Export to file**\n\n```conda list --export > [packages.txt]```"],["SYNOPSIS","**conda** **list** [_options_] [_regex_]"],["PARAMETERS","**-n**, **--name** _name_\n> List packages in named environment.\n\n**--export**\n> Output in format for conda create --file.\n\n**--revisions**\n> List revision history.\n\n**--json**\n> Output in JSON format."],["CONFIGURATION","**~/.condarc**\n> User-level conda configuration for channels, package settings, and solver options.\n\n**/opt/conda/.condarc**\n> System-wide conda configuration."],["DESCRIPTION","**conda list** displays packages installed in a conda environment. By default shows packages in the current active environment.\n\nUse regex patterns to filter the list."],["INSTALL","```dnf: sudo dnf install conda```\n\n```nix: nix profile install nixpkgs#conda```"],["SEE ALSO","[conda](/man/conda)(1), [conda-search](/man/conda-search)(1), [conda-info](/man/conda-info)(1)"],["RESOURCES","```[Source code](https://github.com/conda/conda)```\n\n```[Documentation](https://docs.conda.io/projects/conda/en/stable/commands/list.html)```"]],"e":["conda","conda-search","conda-info"],"x":"conda list"},"consolechars":{"n":"consolechars","t":"console font and character map loader","s":[["TAGLINE","console font and character map loader"],["TLDR","**Load a console font**\n\n```consolechars -f [font_name]```\n\n**Load a font from specific file**\n\n```consolechars -f [/usr/share/consolefonts/Lat2-Terminus16.psf.gz]```\n\n**Load a Unicode mapping**\n\n```consolechars -m [8859-1]```\n\n**Load font with specific height**\n\n```consolechars -H [16]```\n\n**Show current font information**\n\n```consolechars -i```\n\n**Reset to default font**\n\n```consolechars -d```"],["SYNOPSIS","**consolechars** [_options_]"],["PARAMETERS","**-f**, **--font** _font_\n> Load console font from file or by name.\n\n**-m**, **--acm** _map_\n> Load application charset map (ACM).\n\n**-u**, **--sfm** _map_\n> Load screen font map (SFM/Unicode map).\n\n**-H**, **--height** _n_\n> Force font height (8, 14, 16, etc.).\n\n**-d**, **--default**\n> Load default font.\n\n**-i**, **--info**\n> Display current font information.\n\n**-v**, **--verbose**\n> Verbose output.\n\n**-h**, **--help**\n> Display help."],["DESCRIPTION","**consolechars** loads fonts and character maps for the Linux virtual console (text-mode terminals). It allows changing the appearance of text on the console, supporting various character encodings and Unicode.\n\nConsole fonts are bitmap fonts in PSF (PC Screen Font) format stored in **/usr/share/consolefonts/** or **/usr/share/kbd/consolefonts/**. Character maps translate between application character sets and the screen font.\n\nThe tool is typically used in system initialization scripts to set console appearance before graphical environments load. It's particularly useful for non-Latin scripts and accessibility (larger fonts)."],["CAVEATS","Only affects Linux virtual consoles (tty1-tty6), not terminal emulators in graphical environments. Font changes don't persist across reboots without configuration. Available fonts depend on installed console-data or kbd packages. Maximum font size is limited by video mode."],["HISTORY","**consolechars** is part of the **console-tools** package, which was developed for Debian in the late **1990s** to manage Linux console configuration. It served as an alternative to the **kbd** package's **setfont** utility. While **setfont** from kbd has become more common on many distributions, consolechars remains available on Debian-based systems."],["SEE ALSO","[setfont](/man/setfont)(8), [showconsolefont](/man/showconsolefont)(1), [loadkeys](/man/loadkeys)(1)"]],"e":["setfont","showconsolefont","loadkeys"],"x":"consolechars -f [font_name]"},"cookiecutter":{"n":"cookiecutter","t":"project template generator","s":[["TAGLINE","project template generator"],["TLDR","**Create project from template**\n\n```cookiecutter [https://github.com/user/template]```\n\n**Create from local template**\n\n```cookiecutter [/path/to/template]```\n\n**Create without prompts** (use defaults)\n\n```cookiecutter --no-input [template]```\n\n**Override template variables**\n\n```cookiecutter [template] [project_name]=\"[MyProject]\"```\n\n**List installed templates**\n\n```cookiecutter --list-installed```\n\n**Replay last creation**\n\n```cookiecutter --replay [template]```\n\n**Output to specific directory**\n\n```cookiecutter -o [/output/path] [template]```"],["SYNOPSIS","**cookiecutter** [_options_] _template_"],["DESCRIPTION","**cookiecutter** is a command-line utility for creating projects from templates, eliminating the need to manually set up boilerplate code and project structure. It takes a template directory (local or from a Git repository) and prompts for configuration values, then generates a complete project with those values substituted throughout.\n\nTemplates use Jinja2 syntax for variable substitution in both filenames and file content. For example, a template might prompt for \"project_name\" and use it to create directories, populate setup files, and customize code comments. This ensures consistency across projects while allowing customization.\n\nThe tool has spawned a large ecosystem of community templates for frameworks like Django, Flask, React, and many others. cookiecutter is language-agnostic and can template any type of project. It supports advanced features like conditional file inclusion, pre/post-generation hooks, and replay files for reproducing previous configurations. The tool has become a standard in Python development and influenced similar tools in other ecosystems."],["PARAMETERS","**-o**, **--output-dir** _path_\n> Output directory for generated project.\n\n**--no-input**\n> Use default values without prompting.\n\n**-c**, **--checkout** _branch_\n> Git branch or tag to checkout.\n\n**-v**, **--verbose**\n> Print debug information.\n\n**--replay**\n> Use previously entered values.\n\n**--replay-file** _file_\n> Use values from specified file.\n\n**-f**, **--overwrite-if-exists**\n> Overwrite existing output directory.\n\n**-s**, **--skip-if-file-exists**\n> Skip files that already exist.\n\n**--list-installed**\n> List installed templates.\n\n**--config-file** _file_\n> User configuration file."],["CAVEATS","Templates must follow cookiecutter conventions. Git required for remote templates. Complex templates may have many prompts. Jinja2 errors in templates cause failures."],["HISTORY","**cookiecutter** was created by **Audrey Roy Greenfeld** in **2013**. It popularized the concept of project templating in the Python community and inspired similar tools in other languages. The tool has enabled thousands of community templates for various frameworks and project types."],["INSTALL","```brew: brew install cookiecutter```\n\n```nix: nix profile install nixpkgs#cookiecutter```"],["SEE ALSO","[copier](/man/copier)(1), [git](/man/git)(1)"]],"e":["copier","git"],"x":"cookiecutter [https://github.com/user/template]"},"detex":{"n":"detex","t":"strip LaTeX commands from documents","s":[["TAGLINE","strip LaTeX commands from documents"],["TLDR","**Remove LaTeX commands from a file**\n\n```detex [document.tex]```\n\n**Process and output to file**\n\n```detex [document.tex] > [output.txt]```\n\n**Process a single file** without following \\\\input and \\\\include\n\n```detex -n [document.tex]```\n\n**Force LaTeX mode** regardless of content\n\n```detex -l [document.tex]```\n\n**Replace math with placeholder words** to keep sentences readable\n\n```detex -r [document.tex]```\n\n**Output a word list** (one word per line, e.g. for counting)\n\n```detex -w [document.tex] | wc -l```"],["SYNOPSIS","**detex** [**-clnstw**] [**-e** _environment-list_] [_file_...]"],["PARAMETERS","**-c**\n> In LaTeX mode, echo the arguments of \\\\cite, \\\\ref, and \\\\pageref macros.\n\n**-e** _environment-list_\n> Comma-separated list of environments to ignore (replaces the defaults: array, eqnarray, equation, longtable, picture, tabular, verbatim).\n\n**-l**\n> Force LaTeX mode processing.\n\n**-n**\n> Do not follow \\\\input and \\\\include commands (single file processing).\n\n**-r**\n> Replace math with the placeholder words \"noun\" and \"verbs\" to keep sentences readable.\n\n**-s**\n> Replace control sequences with a space (old behavior; may break accented words).\n\n**-t**\n> Force plain TeX mode.\n\n**-w**\n> Output one word per line."],["DESCRIPTION","**detex** strips LaTeX and TeX commands from documents, leaving plain text. This is useful for spell checking, word counting, text analysis, and extracting readable content from LaTeX source files.\n\nThe tool handles standard LaTeX constructs: commands, environments, comments, and special characters. Math mode content is removed (use **-r** to substitute readable placeholders). By default detex follows \\\\input and \\\\include directives to process multi-file documents, using the TEXINPUTS environment variable to locate them; **-n** disables this.\n\nOutput is the document text without markup, suitable for piping to spell checkers, word processors, or text analysis tools."],["CAVEATS","Complex or non-standard LaTeX may not process correctly. Some packages define commands that detex doesn't recognize. Math removal may affect context in technical documents. The tool doesn't handle bibliographies or index entries specially."],["HISTORY","detex was originally written by **Daniel Trinkle** of the Computer Science Department at Purdue University. It has been a standard tool in TeX distributions since the **1980s** and is maintained today as **OpenDetex**. Despite its age, it remains useful for basic LaTeX-to-text conversion."],["INSTALL","```apt: sudo apt install texlive-binaries```\n\n```dnf: sudo dnf install texlive```\n\n```apk: sudo apk add texlive```\n\n```zypper: sudo zypper install texlive```\n\n```brew: brew install texlive```"],["SEE ALSO","[latex](/man/latex)(1), [aspell](/man/aspell)(1), [pandoc](/man/pandoc)(1), [tex](/man/tex)(1)"],["RESOURCES","```[Source code](https://github.com/pkubowicz/opendetex)```"]],"e":["latex","aspell","pandoc","tex"],"x":"detex [document.tex]"},"flite":{"n":"flite","t":"lightweight text-to-speech synthesis","s":[["TAGLINE","lightweight text-to-speech synthesis"],["TLDR","**Speak text**\n\n```flite -t \"[Hello world]\"```\n\n**Speak from file**\n\n```flite -f [text.txt]```\n\n**Save to audio file**\n\n```flite -t \"[Hello world]\" -o [output.wav]```\n\n**Use specific voice**\n\n```flite -voice [rms] -t \"[Hello world]\"```\n\n**List available voices**\n\n```flite -lv```\n\n**Speak input as phonemes**\n\n```flite -p \"[pau hh ax l ow w er l d]\"```\n\n**Read SSML input from file**\n\n```flite -ssml -f [input.ssml]```"],["SYNOPSIS","**flite** [_options_] [_text_]"],["PARAMETERS","_TEXT_\n> Text to speak. If it contains a space, it is treated as a literal text string rather than a filename.\n\n**-t** _TEXT_\n> Explicitly set input text string.\n\n**-f** _FILE_\n> Explicitly set input filename.\n\n**-o** _FILE_\n> Output audio to file (WAV format). If omitted or set to \"play\", audio is played on the default audio device. Set to \"none\" to discard output.\n\n**-p** _PHONES_\n> Synthesize input as phonemes.\n\n**-voice** _NAME_\n> Voice to use (name, filename, or URL).\n\n**-voicedir** _DIR_\n> Directory containing voice data.\n\n**-lv**\n> List available voices.\n\n**-ssml**\n> Read input text/file in SSML mode.\n\n**-b**\n> Benchmark mode.\n\n**-l**\n> Loop endlessly.\n\n**-s** _F=V_\n> Set feature to value (guesses type).\n\n**-v**\n> Verbose mode.\n\n**--version**\n> Display version number.\n\n**--help**\n> Display help information."],["DESCRIPTION","**flite** (Festival Lite) is a small, fast text-to-speech synthesis engine developed at Carnegie Mellon University. It converts text to audio using concatenative synthesis, producing speech from recorded fragments.\n\nThe engine is designed for embedded systems with limited resources, providing reasonable quality without large runtime requirements. Multiple voices are available with different characteristics.\n\nflite works offline without internet connectivity, making it suitable for accessibility applications and audio generation."],["CAVEATS","Limited voice naturalness compared to neural TTS. Few voice options. Output quality varies by text type."],["HISTORY","flite was developed at **Carnegie Mellon University** as a lightweight version of the Festival speech synthesis system. It's used in accessibility applications, embedded systems, and offline TTS scenarios."],["INSTALL","```apt: sudo apt install flite```\n\n```dnf: sudo dnf install flite```\n\n```pacman: sudo pacman -S flite```\n\n```apk: sudo apk add flite```\n\n```zypper: sudo zypper install flite```\n\n```nix: nix profile install nixpkgs#flite```"],["SEE ALSO","[espeak](/man/espeak)(1), [espeak-ng](/man/espeak-ng)(1), [festival](/man/festival)(1), [say](/man/say)(1), [spd-say](/man/spd-say)(1)"]],"e":["espeak","espeak-ng","festival","say","spd-say"],"x":"flite -t \"[Hello world]\""},"fstopgm":{"n":"fstopgm","t":"Usenix FaceSaver to PGM converter","s":[["TAGLINE","Usenix FaceSaver to PGM converter"],["TLDR","**Convert Usenix FaceSaver to PGM**\n\n```fstopgm < [input.fs] > [output.pgm]```"],["SYNOPSIS","**fstopgm** [_input_]"],["DESCRIPTION","**fstopgm** converts Usenix FaceSaver format images to PGM (Portable Graymap) format. The Usenix FaceSaver was an early format used for storing grayscale face images in online directory systems. This tool is part of the Netpbm image processing toolkit, which provides utilities for converting between numerous graphics formats.\n\nThe conversion reads from standard input and writes to standard output, making it suitable for pipeline operations with other Netpbm tools."],["CAVEATS","The Usenix FaceSaver format is obsolete and rarely encountered today. This tool is part of the Netpbm suite and may not be included in all Netpbm distributions. Input must be in valid FaceSaver format or the conversion will fail."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pgmtofs](/man/pgmtofs)(1), [netpbm](/man/netpbm)(1)"]],"e":["pgmtofs","netpbm"],"x":"fstopgm < [input.fs] > [output.pgm]"},"fwconsole":{"n":"fwconsole","t":"command-line interface for FreePBX administration","s":[["TAGLINE","command-line interface for FreePBX administration"],["TLDR","**Reload** FreePBX configurations\n\n```fwconsole reload```\n\n**Start** Asterisk and other commands needed by FreePBX\n\n```fwconsole start```\n\n**Stop** Asterisk and other commands needed by FreePBX\n\n```fwconsole stop```\n\nView and **update settings**\n\n```fwconsole setting [keyword] [new_value]```\n\nList available **backups**\n\n```fwconsole backup --list```\n\nList available **FreePBX commands**\n\n```fwconsole list```\n\nChange **ownership** of all FreePBX files to the apache user\n\n```fwconsole chown```"],["SYNOPSIS","**fwconsole** _command_ [_options_]"],["PARAMETERS","**reload**\n> Apply configuration changes without restarting Asterisk\n\n**start** [_module_]\n> Start FreePBX and Asterisk services (optionally a single module)\n\n**stop** [_module_]\n> Stop FreePBX and Asterisk services\n\n**restart**\n> Restart FreePBX and Asterisk services\n\n**setting** _keyword_ [_value_]\n> View or modify FreePBX settings\n\n**backup** [**--list** | **--file** _file_]\n> Manage or restore FreePBX backups\n\n**chown**\n> Fix file ownership for web server access\n\n**ma** (module admin)\n> Install, enable, disable, upgrade or remove FreePBX modules\n\n**sysadmin**\n> System administration tasks\n\n**list**\n> Display all available fwconsole commands\n\n**-h**, **--help**\n> Display help for a given command\n\n**-v**, **-vv**, **-vvv**\n> Increase verbosity level\n\n**-q**, **--quiet**\n> Suppress output"],["DESCRIPTION","**fwconsole** is the command-line interface for managing FreePBX, an open-source web-based GUI for managing Asterisk PBX systems. It provides administrative control over the FreePBX configuration, modules, and underlying Asterisk server.\n\nThe tool handles configuration reloads, service management, module administration, backups, and system maintenance tasks. It's essential for automation, scripting, and troubleshooting FreePBX installations.\n\nConfiguration changes made through the web interface typically require **fwconsole reload** to apply to the running Asterisk instance."],["CAVEATS","Most commands require root privileges or appropriate sudo access. Running reload during active calls may cause brief service interruption. The chown command is necessary after manual file modifications to maintain proper web server permissions."],["HISTORY","fwconsole is part of FreePBX, which began as a web GUI for Asterisk in **2004**. Originally known as AMP (Asterisk Management Portal), it was renamed to FreePBX in 2006. The fwconsole command-line tool was introduced as part of FreePBX's modernization efforts."],["SEE ALSO","[asterisk](/man/asterisk)(8), [fs_cli](/man/fs_cli)(1)"]],"e":["asterisk","fs_cli"],"x":"fwconsole reload"},"gcrane-completion":{"n":"gcrane-completion","t":"generates shell autocompletion scripts","s":[["TAGLINE","generates shell autocompletion scripts"],["TLDR","Generate the autocompletion script for your **shell**\n\n```gcrane completion [shell_name]```\n\n**Disable** completion descriptions\n\n```gcrane completion [shell_name] --no-descriptions```\n\nLoad completions in your **current shell session** (Bash/Zsh)\n\n```source <(gcrane completion bash)```\n\nLoad completions in your current shell session (**fish**)\n\n```gcrane completion fish | source```\n\nLoad completions for **every new Bash session**\n\n```gcrane completion bash > /etc/bash_completion.d/gcrane```\n\nLoad completions for **every new Zsh session**\n\n```gcrane completion zsh > \"${fpath[1]}/_gcrane\"```\n\nLoad completions for **every new fish session**\n\n```gcrane completion fish > ~/.config/fish/completions/gcrane.fish```\n\nDisplay **help**\n\n```gcrane completion [shell_name] -h```"],["SYNOPSIS","**gcrane completion** _shell_ [_options_]"],["PARAMETERS","**bash**\n> Generate Bash completion script\n\n**zsh**\n> Generate Zsh completion script\n\n**fish**\n> Generate fish completion script\n\n**powershell**\n> Generate PowerShell completion script\n\n**--no-descriptions**\n> Disable completion descriptions\n\n**-h**, **--help**\n> Display help for completion command"],["DESCRIPTION","**gcrane completion** generates shell autocompletion scripts for the gcrane command-line tool. gcrane is a tool for working with container registries, part of Google's go-containerregistry project.\n\nCompletions provide tab-completion for gcrane commands, flags, and arguments, improving command-line efficiency. The generated scripts must be sourced or installed according to your shell's completion system.\n\nFor persistent completions, install the output to the appropriate location for your shell (bash_completion.d for Bash, fpath for Zsh, or ~/.config/fish/completions for fish)."],["CAVEATS","Installation paths vary by system configuration. Zsh requires the completion directory to be in fpath before compinit is called. Some systems may require logging out and back in for changes to take effect."],["INSTALL","```zypper: sudo zypper install gcrane```\n\n```nix: nix profile install nixpkgs#gcrane```"],["SEE ALSO","[gcrane](/man/gcrane)(1), [gcrane-gc](/man/gcrane-gc)(1), [gcrane-ls](/man/gcrane-ls)(1), [crane](/man/crane)(1), [docker](/man/docker)(1), [podman](/man/podman)(1)"]],"e":["gcrane","gcrane-gc","gcrane-ls","crane","docker","podman"],"x":"gcrane completion [shell_name]"},"git-mailsplit":{"n":"git-mailsplit","t":"Split mbox into individual messages","s":[["TAGLINE","Split mbox into individual messages"],["TLDR","**Split mbox into messages**\n\n```git mailsplit -o [output-dir] [mbox]```\n\n**Split from stdin**\n\n```git mailsplit -o [output-dir]```"],["SYNOPSIS","**git mailsplit** [_options_] [_mbox_]"],["PARAMETERS","_MBOX_\n> Mailbox file to split.\n\n**-o** _DIR_\n> Output directory.\n\n**-b**\n> Include mbox \"From \" line.\n\n**-d** _DIGITS_\n> Number padding width.\n\n**-f** _NUM_\n> Start numbering from N.\n\n**--keep-cr**\n> Keep carriage returns.\n\n**--help**\n> Display help information."],["DESCRIPTION","**git mailsplit** splits an mbox file into individual email messages. Each email becomes a numbered file in the output directory, preparing patches for processing by `git am`.\n\nThe command is a plumbing tool used internally by `git am` to handle mailbox files containing multiple patches. It detects mbox format boundaries and separates each message into its own file."],["CAVEATS","Plumbing command. Usually called by git am. Output is individual email files."],["HISTORY","git mailsplit is part of **Git's** email patch workflow, breaking mailboxes into individual messages for processing."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-am](/man/git-am)(1), [git-mailinfo](/man/git-mailinfo)(1)"],["RESOURCES","```[Source code](https://github.com/git/git)```\n\n```[Homepage](https://git-scm.com/)```\n\n```[Documentation](https://git-scm.com/docs/git-mailsplit)```"]],"e":["git-am","git-mailinfo"],"x":"git mailsplit -o [output-dir] [mbox]"},"git-sed":{"n":"git-sed","t":"Find and replace across tracked files","s":[["TAGLINE","Find and replace across tracked files"],["TLDR","**Find and replace in tracked files**\n\n```git sed \"[old]\" \"[new]\"```\n\n**Replace with sed flags** (e.g. case-insensitive)\n\n```git sed -f [i] \"[old]\" \"[new]\"```\n\n**Limit to a path**\n\n```git sed \"[old]\" \"[new]\" -- [path/]```\n\n**Replace and commit** the result\n\n```git sed -c \"[old]\" \"[new]\"```"],["SYNOPSIS","**git** **sed** [_-c_] [_-f_ _flags_] _search_ _replacement_ [_flags_] [_--_ _pathspec_]"],["PARAMETERS","_SEARCH_\n> Pattern to find.\n\n_REPLACEMENT_\n> Text to substitute in.\n\n**-f**, **--flags** _flags_\n> sed substitution flags to append (e.g. `g`, `i`); may also be given as a trailing positional argument.\n\n**-c**, **--commit**\n> Commit the change afterward; fails if the working tree isn't already clean.\n\n**--** _pathspec_\n> Limit the replacement to matching files/paths."],["DESCRIPTION","**git sed** runs `git grep` to find matching files, then pipes them through `sed -i` to replace `search` with `replacement`, limiting changes to files tracked by Git so untracked or ignored files are never touched. Part of git-extras, it saves the separate `grep | xargs sed` pipeline for repository-wide renames."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[sed](/man/sed)(1), [git-grep](/man/git-grep)(1), [git-extras](/man/git-extras)(1)"],["RESOURCES","```[Source code](https://github.com/tj/git-extras)```\n\n```[Documentation](https://github.com/tj/git-extras/blob/master/Commands.md#git-sed)```"]],"e":["sed","git-grep","git-extras"],"x":"git sed \"[old]\" \"[new]\""},"gomoku":{"n":"gomoku","t":"Terminal-based Five in a Row board game","s":[["TAGLINE","Terminal-based Five in a Row board game"],["TLDR","**Start gomoku game**\n\n```gomoku```"],["SYNOPSIS","**gomoku** [_options_]"],["PREVIEW","```\n . . . . . . . .\n . . . X . . . .\n . . O X O . . .\n . . . X . . . .\n . . . O . . . .\n```"],["DESCRIPTION","**gomoku** is a terminal-based implementation of the Gomoku (Five in a Row) board game. Players take turns placing stones on a grid, trying to get five in a row horizontally, vertically, or diagonally.\n\nThe game is typically played against an AI opponent and is a strategy game originating from Japan."],["CAVEATS","Terminal game. Requires appropriate terminal. Various implementations exist."],["HISTORY","Gomoku is a traditional strategy board game. Terminal versions provide accessible gameplay."],["INSTALL","```dnf: sudo dnf install bsd-games```\n\n```pacman: sudo pacman -S bsd-games```\n\n```apk: sudo apk add bsd-games```\n\n```zypper: sudo zypper install bsd-games```"],["SEE ALSO","[gnugo](/man/gnugo)(1)"]],"e":["gnugo"],"x":"gomoku"},"grpcurl":{"n":"grpcurl","t":"command-line gRPC client like curl","s":[["TAGLINE","command-line gRPC client like curl"],["TLDR","**List services** on a gRPC server (with reflection)\n\n```grpcurl [localhost:50051] list```\n\n**Describe a service**\n\n```grpcurl [localhost:50051] describe [package.ServiceName]```\n\n**Call a method** with JSON input\n\n```grpcurl -d '{\"name\": \"[value]\"}' [localhost:50051] [package.Service/Method]```\n\n**Call using proto file** instead of reflection\n\n```grpcurl -proto [service.proto] -d '{\"id\": 1}' [localhost:50051] [package.Service/Method]```\n\n**Call with plaintext** (no TLS)\n\n```grpcurl -plaintext [localhost:50051] [package.Service/Method]```\n\n**Add metadata/headers**\n\n```grpcurl -H \"Authorization: Bearer [token]\" [localhost:50051] [package.Service/Method]```\n\n**Stream data from file**\n\n```grpcurl -d @ [localhost:50051] [package.Service/Method] < [data.json]```\n\n**List methods** of a service\n\n```grpcurl [localhost:50051] list [package.ServiceName]```"],["SYNOPSIS","**grpcurl** [_options_] _target_ [_method_] [_request_]"],["PARAMETERS","**-d** _data_\n> Request data as JSON string (use @ to read from stdin).\n\n**-proto** _file_\n> Proto file defining the service (alternative to reflection).\n\n**-protoset** _file_\n> Pre-compiled proto descriptor file.\n\n**-import-path** _path_\n> Path to search for proto imports.\n\n**-plaintext**\n> Use plaintext (no TLS) connection.\n\n**-insecure**\n> Skip TLS certificate verification.\n\n**-H** _header_\n> Add header as \"name: value\" (repeatable).\n\n**-rpc-header** _header_\n> Add gRPC metadata header.\n\n**-authority** _name_\n> Set :authority pseudo-header.\n\n**-connect-timeout** _seconds_\n> Connection timeout.\n\n**-max-time** _seconds_\n> Maximum time for the call.\n\n**-format** _fmt_\n> Output format: json (default) or text.\n\n**-v**\n> Verbose output (show headers).\n\n**list**\n> List services or methods.\n\n**describe**\n> Describe service, method, or message."],["DESCRIPTION","**grpcurl** is a command-line tool for interacting with gRPC servers, similar to how curl works for HTTP APIs. It can invoke RPC methods, inspect services, and help debug gRPC-based applications.\n\nThe tool can discover services in two ways: server reflection (where the server describes its own API) or by providing proto files. Reflection is convenient but must be enabled on the server. Proto files work with any server but require having the service definitions.\n\nFor unary calls, request data is provided as JSON via the -d flag. Streaming calls can read multiple messages from stdin. Response data is output as JSON by default, making it easy to parse with jq or other tools.\n\n**grpcurl** handles the complexity of gRPC's HTTP/2 transport, protobuf encoding, and TLS. The -plaintext flag is needed for non-TLS servers (common in development). For production servers with TLS, proper certificates may be required."],["CAVEATS","Server must have reflection enabled for discovery, or proto files must be provided. JSON representation of protobuf has edge cases (especially with bytes, enums, and 64-bit integers). Streaming calls have different interaction patterns. Some gRPC features like deadlines need explicit configuration."],["HISTORY","**grpcurl** was created by FullStory Inc. and released as open-source. It emerged from the need for a curl-equivalent for the gRPC ecosystem, which was gaining adoption for microservices communication. The tool is written in Go and uses the official gRPC-Go libraries."],["INSTALL","```dnf: sudo dnf install grpcurl```\n\n```brew: brew install grpcurl```\n\n```nix: nix profile install nixpkgs#grpcurl```"],["SEE ALSO","[curl](/man/curl)(1), [protoc](/man/protoc)(1)"]],"e":["curl","protoc"],"x":"grpcurl [localhost:50051] list"},"gtk-launch":{"n":"gtk-launch","t":"Launch an application using its desktop file","s":[["TAGLINE","Launch an application using its desktop file"],["TLDR","**Launch** an application\n\n```gtk-launch [Application_name]```\n\nDisplay **help**\n\n```gtk-launch -h```"],["SYNOPSIS","**gtk-launch** _application_ [_URI_...]"],["PARAMETERS","_application_\n> Desktop file name (without .desktop extension)\n\n_URI..._\n> Optional URIs/files to open with the application\n\n**-h**, **--help**\n> Display help message"],["DESCRIPTION","**gtk-launch** launches applications using their .desktop files from standard XDG locations (/usr/share/applications, ~/.local/share/applications, etc.). This is the programmatic equivalent of clicking an application icon.\n\nThe application parameter is the desktop file ID (filename without .desktop extension). For example, **gtk-launch firefox** launches the application defined in firefox.desktop.\n\nThis is useful for launching applications from scripts in a desktop-agnostic way, respecting the application's StartupNotify, exec parameters, and other desktop file settings."],["CAVEATS","Only works with applications that have .desktop files. Application names are case-sensitive and must match the desktop file name exactly (without extension). Requires GTK libraries to be installed."],["HISTORY","gtk-launch is part of GTK's command-line tools, providing a standard way to launch desktop applications. It supports the XDG Desktop Entry Specification for cross-desktop compatibility."],["INSTALL","```apt: sudo apt install libgtk-3-bin```\n\n```apk: sudo apk add gtk+3.0```"],["SEE ALSO","[xdg-open](/man/xdg-open)(1), [gio](/man/gio)(1), [desktop-file-validate](/man/desktop-file-validate)(1)"]],"e":["xdg-open","gio","desktop-file-validate"],"x":"gtk-launch [Application_name]"},"hydra":{"n":"hydra","t":"fast network login cracker supporting many protocols","s":[["TAGLINE","fast network login cracker supporting many protocols"],["TLDR","**Brute force SSH**\n\n```hydra -l [user] -P [passwords.txt] ssh://[target]```\n\n**HTTP form brute force**\n\n```hydra -l [admin] -P [pass.txt] [target] http-post-form \"/login:user=^USER^&pass=^PASS^:Invalid\"```\n\n**FTP brute force**\n\n```hydra -L [users.txt] -P [pass.txt] ftp://[target]```\n\n**Specify threads**\n\n```hydra -t [4] -l [user] -P [pass.txt] [target] [service]```\n\n**Verbose output**\n\n```hydra -v -l [user] -P [pass.txt] [target] ssh```"],["SYNOPSIS","**hydra** [_options_] _target_ _service_"],["PARAMETERS","_TARGET_\n> Target host.\n\n_SERVICE_\n> Service to attack (ssh, ftp, http-post-form).\n\n**-l** _LOGIN_\n> Single username.\n\n**-L** _FILE_\n> Load several usernames from a file.\n\n**-p** _PASS_\n> Single password.\n\n**-P** _FILE_\n> Load several passwords from a file.\n\n**-C** _FILE_\n> Colon-separated `login:pass` list, instead of **-L**/**-P**.\n\n**-e** _nsr_\n> Also try a null password (**n**), the login as password (**s**), and/or the reversed login (**r**).\n\n**-x** _MIN:MAX:CHARSET_\n> Generate passwords via brute force instead of a wordlist (e.g. `1:8:a` for lowercase letters, length 1-8).\n\n**-M** _FILE_\n> Attack a list of targets read from a file, one per line (`host[:port]`).\n\n**-o** _FILE_\n> Write found login/password pairs to a file instead of stdout.\n\n**-b** _FORMAT_\n> Output format for **-o**: `text` (default), `json`, or `jsonv1`.\n\n**-f** / **-F**\n> Exit as soon as a valid pair is found: per host (**-f**), or globally across all targets (**-F**, requires **-M**).\n\n**-t** _TASKS_\n> Parallel connections to run per target (default: 16).\n\n**-T** _TASKS_\n> Parallel connections overall when using **-M** (default: 64).\n\n**-s** _PORT_\n> Use a non-default port for the service.\n\n**-S**\n> Perform the connection over SSL/TLS.\n\n**-4** / **-6**\n> Use IPv4 (default) or IPv6 addresses.\n\n**-R**\n> Restore a previous aborted or crashed session from `hydra.restore`.\n\n**-v** / **-V**\n> Verbose mode / show the login+pass combination being tried for every attempt.\n\n**-h**\n> Display help information."],["DESCRIPTION","**Hydra** is a parallelized network login cracker used in authorized penetration testing and security auditing. It systematically attempts username and password combinations against remote authentication services, drawing credentials from user-supplied wordlists, single values, or a brute-force character-set generator (**-x**). Multiple connection threads run in parallel (configurable with **-t**), allowing it to test a large number of combinations efficiently.\n\nHydra supports over 50 protocols and services out of the box, including SSH, FTP, Telnet, HTTP/HTTPS (basic auth, form-based, and digest), SMB, RDP, MySQL, PostgreSQL, SMTP, IMAP, LDAP, VNC, and many more. For web applications, its `http-post-form` and `http-get-form` modules accept custom request templates with `^USER^`/`^PASS^` placeholder markers for injecting credentials. Results can be saved to a file (**-o**) for later analysis, and interrupted sessions can be resumed with **-R**."],["CAVEATS","For use only against systems you're authorized to test - unauthorized use is illegal in most jurisdictions. Aggressive attacks can trigger account lockouts, IPS/WAF bans, or degrade the target service. `-h` (not `--help`) prints the short option summary; `hydra -U <service>` prints module-specific options."],["HISTORY","Hydra was created in **2000** by **van Hauser** of **THC** (The Hacker's Choice), billed as the first parallelized network login cracker. Development continues on GitHub with contributions from David Maciejak and others."],["INSTALL","```apt: sudo apt install hydra```\n\n```dnf: sudo dnf install hydra```\n\n```pacman: sudo pacman -S hydra```\n\n```apk: sudo apk add hydra```\n\n```zypper: sudo zypper install hydra```\n\n```brew: brew install hydra```\n\n```nix: nix profile install nixpkgs#hydra```"],["SEE ALSO","[ncrack](/man/ncrack)(1), [medusa](/man/medusa)(1), [john](/man/john)(1)"],["RESOURCES","```[Source code](https://github.com/vanhauser-thc/thc-hydra)```"]],"e":["ncrack","medusa","john"],"x":"hydra -l [user] -P [passwords.txt] ssh://[target]"},"idn":{"n":"idn","t":"Encode and decode internationalized domain names","s":[["TAGLINE","Encode and decode internationalized domain names"],["TLDR","**Encode a Unicode domain to ACE/Punycode (default mode)**\n\n```echo \"münchen.de\" | idn```\n\n**Decode a Punycode domain back to Unicode**\n\n```echo \"xn--mnchen-3ya.de\" | idn --idna-to-unicode```\n\n**Apply Nameprep stringprep to input**\n\n```echo \"[string]\" | idn --stringprep```\n\n**Explicitly run IDNA ToASCII on an argument**\n\n```idn --idna-to-ascii [münchen.de]```\n\n**Encode or decode raw Punycode (no IDNA wrapping)**\n\n```echo \"[label]\" | idn --punycode-encode```\n\n**Quiet mode (suppress informational messages)**\n\n```idn --quiet [domain]```"],["SYNOPSIS","**idn** [_options_] [_strings_...]"],["PARAMETERS","_strings_\n> Domain names or labels to convert. Read from standard input if not provided.\n\n**-a**, **--idna-to-ascii**\n> Convert input to ACE according to IDNA. This is the default mode.\n\n**-u**, **--idna-to-unicode**\n> Convert input from ACE (Punycode) back to Unicode using IDNA.\n\n**-s**, **--stringprep**\n> Prepare the string according to the Nameprep profile.\n\n**-e**, **--punycode-encode**\n> Encode raw input with the Punycode algorithm, without IDNA pre/post processing.\n\n**-d**, **--punycode-decode**\n> Decode raw Punycode input, without IDNA pre/post processing.\n\n**-n**, **--nfkc**\n> Normalize input according to Unicode v3.2 NFKC.\n\n**-p** _profile_, **--profile**=_profile_\n> Use the named stringprep profile. Valid values: `Nameprep`, `iSCSI`, `Nodeprep`, `Resourceprep`, `trace`, `SASLprep`.\n\n**--allow-unassigned**\n> Toggle the IDNA `AllowUnassigned` flag (default off).\n\n**--usestd3asciirules**\n> Toggle the IDNA `UseSTD3ASCIIRules` flag (default off); forbids non-LDH characters.\n\n**--no-tld**\n> Skip TLD-specific validity checks (only affects `--idna-to-ascii` / `--idna-to-unicode`).\n\n**--quiet**\n> Silent operation.\n\n**--debug**\n> Print debugging information, including the detected character set.\n\n**-h**, **--help**\n> Print help and exit.\n\n**-V**, **--version**\n> Print version and exit."],["DESCRIPTION","**idn** converts internationalized domain names between Unicode and ASCII-Compatible Encoding (ACE / Punycode). It implements the IDNA (Internationalized Domain Names in Applications) standard, allowing domain names with non-ASCII characters to be represented in the DNS.\n\nThe encoding pipeline applies Nameprep stringprep (case folding, NFKC normalization, prohibited-character checks) before Punycode encoding. ACE-encoded labels use the **xn--** prefix. The tool reads strings from command-line arguments, or from standard input if none are supplied.\n\nInput is expected in the locale's preferred charset; override this by setting the **CHARSET** environment variable. To process a string starting with `-`, use `--` to mark the end of options (e.g. `idn --quiet -a -- -foo`)."],["CAVEATS","This tool implements **IDNA 2003** (RFC 3490). For the newer **IDNA 2008** standard (RFC 5891), use **idn2** from the libidn2 package. The two standards differ in handling of several characters (e.g. German eszett `ß`, Greek final sigma `ς`). Part of **GNU Libidn**."],["HISTORY","**idn** is part of **GNU Libidn**, written by **Simon Josefsson**. The IDNA standard was published as RFC 3490 in **2003** to enable internationalized domain names in the DNS."],["INSTALL","```apt: sudo apt install idn```\n\n```dnf: sudo dnf install libidn```\n\n```pacman: sudo pacman -S libidn```\n\n```apk: sudo apk add libidn```\n\n```brew: brew install libidn```\n\n```nix: nix profile install nixpkgs#libidn```"],["SEE ALSO","[host](/man/host)(1), [dig](/man/dig)(1)"]],"e":["host","dig"],"x":"echo \"münchen.de\" | idn"},"jadx":{"n":"jadx","t":"decompiles Android DEX bytecode to Java source","s":[["TAGLINE","decompiles Android DEX bytecode to Java source"],["TLDR","**Decompile APK**\n\n```jadx [app.apk]```\n\n**Decompile to specific directory**\n\n```jadx -d [output_dir] [app.apk]```\n\n**Open GUI**\n\n```jadx-gui [app.apk]```\n\n**Decompile DEX file**\n\n```jadx [classes.dex]```\n\n**Show decompilation info**\n\n```jadx --show-bad-code [app.apk]```\n\n**Export as Gradle project**\n\n```jadx -e [output_dir] [app.apk]```"],["SYNOPSIS","**jadx** [_options_] _file_"],["PARAMETERS","_FILE_\n> APK, DEX, or AAR file to decompile.\n\n**-d** _DIR_\n> Output directory.\n\n**-e** _DIR_\n> Export as Gradle project.\n\n**--show-bad-code**\n> Show decompilation errors in output.\n\n**-r**, **--no-res**\n> Skip resources decompilation.\n\n**-j** _N_\n> Thread count.\n\n**--help**\n> Display help information."],["DESCRIPTION","**jadx** decompiles Android DEX bytecode to Java source. It processes APK files directly including resources.\n\nThe tool provides both CLI and GUI interfaces. It produces readable Java code from compiled Android apps."],["CAVEATS","Decompilation not perfect. Obfuscated code harder. Legal considerations apply."],["HISTORY","jadx was created as an improvement over older Android decompilers, focusing on producing cleaner, more readable Java output."],["INSTALL","```pacman: sudo pacman -S jadx```\n\n```brew: brew install jadx```\n\n```nix: nix profile install nixpkgs#jadx```"],["SEE ALSO","[apktool](/man/apktool)(1), [dex2jar](/man/dex2jar)(1)"]],"e":["apktool","dex2jar"],"x":"jadx [app.apk]"},"jj-show":{"n":"jj-show","t":"Display details of a change","s":[["TAGLINE","Display details of a change"],["TLDR","**Show the current change** (working copy)\n\n```jj show```\n\n**Show a specific revision**\n\n```jj show [revset]```\n\n**Show a histogram of changes**\n\n```jj show --stat```\n\n**Show only a summary** of changed files\n\n```jj show -s```\n\n**Show a Git-format diff**\n\n```jj show --git```\n\n**Use a custom output template**\n\n```jj show -T \"[template]\"```"],["SYNOPSIS","**jj show** [_options_] [_revsets_...]"],["PARAMETERS","_REVSETS_\n> Revision(s) to display (default: @, the working-copy commit).\n\n**-T**, **--template** _TEMPLATE_\n> Render each revision using the given template.\n\n**-p**, **--patch**\n> Display the patch compared to the parent revision.\n\n**-s**, **--summary**\n> For each path, show only whether it was modified, added, or deleted.\n\n**--stat**\n> Show a histogram of the changes.\n\n**--types**\n> For each path, show only its type (F=file, L=symlink, C=conflict, G=Git submodule).\n\n**--name-only**\n> For each path, show only its path.\n\n**--git**\n> Show a Git-format diff.\n\n**--color-words**\n> Show word-level diff with changes indicated only by color.\n\n**--tool** _TOOL_\n> Generate diff via an external command.\n\n**--context** _N_\n> Number of lines of context to show.\n\n**-w**, **--ignore-all-space**\n> Ignore whitespace when comparing lines.\n\n**-b**, **--ignore-space-change**\n> Ignore changes in amount of whitespace when comparing lines."],["DESCRIPTION","**jj show** displays details of a change in a Jujutsu repository, including description, author, timestamps and the diff compared to its parent. Output can be customized with templates and a variety of diff-formatting options."],["CAVEATS","Subcommand of **jj**. The symbol `@` refers to the working-copy commit. Template syntax is specific to Jujutsu; see `jj help -k templates`."],["HISTORY","**jj show** is part of **Jujutsu** (jj), a Git-compatible DVCS, providing detailed inspection of individual changes."],["INSTALL","```pacman: sudo pacman -S jujutsu```\n\n```apk: sudo apk add jujutsu```\n\n```zypper: sudo zypper install jujutsu```\n\n```brew: brew install jujutsu```\n\n```nix: nix profile install nixpkgs#jujutsu```"],["SEE ALSO","[jj](/man/jj)(1), [jj-log](/man/jj-log)(1), [jj-diff](/man/jj-diff)(1)"]],"e":["jj","jj-log","jj-diff"],"x":"jj show"},"kastor":{"n":"kastor","t":"Terraform-style source-of-truth layer for AI agents using HCL specs","s":[["TAGLINE","Terraform-style source-of-truth layer for AI agents using HCL specs"],["TLDR","**Validate** an agent specification\n\n```kastor validate examples/weather```\n\n**Compile** the spec to runnable LangGraph code\n\n```kastor build examples/weather```\n\n**Plan** changes for hosted agents (Terraform-style)\n\n```kastor plan examples/weather```\n\n**Apply** the plan to create or update agents\n\n```kastor apply examples/weather```\n\n**Install** via Homebrew or Go\n\n```brew install kastor```"],["SYNOPSIS","**kastor** <command> [options] [path]"],["DESCRIPTION","Kastor provides a declarative, versionable source of truth for AI agents. You define agents, tools, prompts, models and deployment targets in HCL files (`.agent`, `.tool`, `.prompt`, `kastor.hcl`). Kastor validates the spec, can generate runnable framework code (currently LangGraph), and supports plan/apply/destroy operations against targets with state, diffs and drift detection.\n\nIt is not an agent runtime or executor; it manages the contract and reconciliation around agents."],["COMMANDS","**validate** [path]\n> Parse and validate references in a Kastor module.\n\n**build** [path]\n> Compile the module into runnable framework code (e.g. LangGraph project).\n\n**plan** [path]\n> Show a plan of changes against the target state.\n\n**apply** [path]\n> Apply changes to create/update agents and state.\n\n**destroy** [path]\n> Destroy agents managed by the module."],["PARAMETERS","Common options include paths to modules containing the declarative files. Many commands operate on a directory (the module root)."],["CAVEATS","- Early proof-of-concept; the CLI surface is stable but hosted provider support is limited.\n- Generated code is an output; the `.agent`/`.tool` files are the source of truth.\n- Requires appropriate credentials (e.g. OPENAI_API_KEY) when running generated agents."],["SEE ALSO","[terraform](/man/terraform)(1)"],["RESOURCES","```[Source code](https://github.com/weirdGuy/kastor)```\n\n```[Homepage](https://www.getkastor.dev)```\n\n```[Documentation](https://github.com/weirdGuy/kastor#readme)```"]],"e":["terraform"],"x":"kastor validate examples/weather"},"kde-builder":{"n":"kde-builder","t":"tool for building KDE software from source repositories","s":[["TAGLINE","tool for building KDE software from source repositories"],["TLDR","**Initialize** kde-builder\n\n```kde-builder --initial-setup```\n\n**Build** a KDE component and its dependencies\n\n```kde-builder [component_name]```\n\nBuild **without updating** or dependencies\n\n```kde-builder -SD [component_name]```\n\n**Refresh** build directories before compiling\n\n```kde-builder -r [component_name]```\n\n**Resume** compilation from a specific dependency\n\n```kde-builder -f [dependency_component] [component_name]```\n\n**Run** a built component\n\n```kde-builder --run [executable_name]```\n\nInstall **login session**\n\n```kde-builder --install-login-session-only```"],["SYNOPSIS","**kde-builder** [_options_] [_components_]"],["PARAMETERS","**--initial-setup**\n> Initialize kde-builder configuration\n\n**-S**, **--no-src**\n> Don't update source code\n\n**-D**, **--no-include-dependencies**\n> Don't build dependencies\n\n**-r**, **--refresh-build**\n> Clean and refresh build directories\n\n**-f**, **--resume-from** _COMPONENT_\n> Resume build from specified component\n\n**--run** _EXECUTABLE_\n> Run a built component\n\n**--install-login-session-only**\n> Install login session files\n\n**--no-stop-on-failure**\n> Continue building if a component fails"],["DESCRIPTION","**kde-builder** is a tool for building KDE software from source repositories. It handles dependency resolution, source updates, configuration, and compilation of KDE components.\n\nThe tool is a drop-in replacement for kdesrc-build with improved performance and usability. It can build individual components or entire desktop environments like Plasma."],["CAVEATS","Requires significant disk space and build time. Build dependencies must be installed separately. Configuration file customization may be needed for specific setups."],["HISTORY","kde-builder was developed as a modern replacement for kdesrc-build, providing easier setup and improved performance for building KDE software from source."],["SEE ALSO","[kdesrc-build](/man/kdesrc-build)(1), [cmake](/man/cmake)(1), [ninja](/man/ninja)(1)"]],"e":["kdesrc-build","cmake","ninja"],"x":"kde-builder --initial-setup"},"lazydocker":{"n":"lazydocker","t":"terminal UI for managing Docker containers, images, volumes, and networks","s":[["TAGLINE","terminal UI for managing Docker containers, images, volumes, and networks"],["TLDR","**Start lazydocker**\n\n```lazydocker```\n\n**Start with a specific Docker config**\n\n```lazydocker -f [/path/to/config.yml]```\n\n**Use a specific Docker Compose file**\n\n```lazydocker -f [docker-compose.yml]```\n\n**Start without live updates**\n\n```lazydocker --no-live```"],["SYNOPSIS","**lazydocker** [_-f config_] [_--no-live_] [_-v_]"],["PARAMETERS","**-f**, **--config** _file_\n> Path to a Docker Compose config file or lazydocker config file.\n\n**--no-live**\n> Disable live update of container stats.\n\n**-v**, **--version**\n> Print version information.\n\n**-d**, **--debug**\n> Enable debug logging.\n\n**-h**, **--help**\n> Display help."],["KEYBOARD SHORTCUTS","**Tab / Shift+Tab**\n> Switch between panels.\n\n**↑ / ↓** or **j / k**\n> Navigate list items.\n\n**Enter**\n> Focus on selected item.\n\n**d**\n> Delete/remove selected item.\n\n**s**\n> Stop container.\n\n**r**\n> Restart container.\n\n**a**\n> Attach to container.\n\n**m**\n> View container logs.\n\n**e**\n> Exec shell in container.\n\n**b**\n> View bulk commands.\n\n**[**\n> Previous tab.\n\n**]**\n> Next tab.\n\n**/**\n> Filter items.\n\n**x**\n> Open menu.\n\n**q** or **Ctrl+c**\n> Quit.\n\n**?**\n> Show help."],["DESCRIPTION","**lazydocker** is a terminal UI for managing Docker containers, images, volumes, and networks. It provides an interactive dashboard that displays real-time container stats, logs, and allows common operations without typing docker commands.\n\nThe interface is divided into panels: containers, images, volumes, and a main view area. Each panel lists resources with status indicators. Selecting an item shows details, stats, logs, or configuration in the main panel.\n\nFor containers, you can view live CPU/memory stats, stream logs, attach for interaction, or exec a shell. Bulk operations allow stopping, removing, or restarting multiple containers. Image management includes pulling, building, and pruning.\n\nConfiguration is stored in **~/.config/lazydocker/config.yml**, allowing customization of keybindings, colors, and Docker connection settings. Custom commands can be defined for frequently used operations.\n\nThe tool works with both Docker and Docker Compose projects, detecting compose files and allowing per-service operations."],["CAVEATS","Requires Docker daemon access (user must be in docker group or use sudo). Live stats increase CPU usage. Not all Docker features are exposed. Works best with modern terminal emulators. Large numbers of containers may affect performance."],["HISTORY","**lazydocker** was created by Jesse Duffield around **2019** as part of a series of \"lazy\" terminal UIs (including lazygit). Written in Go, it was inspired by the desire for a more visual way to manage Docker without leaving the terminal. The project has become popular among developers who prefer keyboard-driven interfaces."],["INSTALL","```pacman: sudo pacman -S lazydocker```\n\n```apk: sudo apk add lazydocker```\n\n```brew: brew install lazydocker```\n\n```nix: nix profile install nixpkgs#lazydocker```"],["SEE ALSO","[docker](/man/docker)(1), [docker-compose](/man/docker-compose)(1), [lazygit](/man/lazygit)(1), [ctop](/man/ctop)(1), [podman](/man/podman)(1), [dive](/man/dive)(1)"]],"e":["docker","docker-compose","lazygit","ctop","podman","dive"],"x":"lazydocker"},"lcl":{"n":"lcl","t":"native CLI for offline access to thousands of Linux command references","s":[["TAGLINE","native CLI for offline access to thousands of Linux command references"],["TLDR","**Start interactive mode** to browse commands, basics, and tips\n\n```lcl```\n\n**Show details** for a specific command\n\n```lcl [grep]```\n\n**List all available commands** in the library\n\n```lcl --list```\n\n**Show version** information\n\n```lcl --version```\n\n**Find commands** by piping list output to grep\n\n```lcl --list | grep [network]```"],["SYNOPSIS","**lcl** [_command_]\n\n**lcl** [**--list** | **-l**]\n\n**lcl** [**--version** | **-v**]\n\n**lcl** [**--help** | **-h**]"],["PARAMETERS","**command**\n> Name of a Linux command to display directly. If an exact match is found, shows the command details. If multiple partial matches exist, lists them for selection.\n\n**-l**, **--list**\n> List all available commands in the library. Useful for piping to other tools like grep.\n\n**-v**, **--version**\n> Display the current version of Linux Command Library.\n\n**-h**, **--help**\n> Show help information with usage examples."],["DESCRIPTION","**lcl** (Linux Command Library) is a native CLI application that provides offline access to over **6200 Linux command** man pages, **23+ basic categories**, and general terminal tips. It functions as an interactive terminal-based reference tool for Unix and Linux commands.\n\nThe application operates in two modes: **interactive mode** when run without arguments, presenting a navigable TUI (Terminal User Interface) with menus for commands, basics, and tips; and **direct mode** when given a command name argument, displaying that command's documentation immediately.\n\nIn interactive mode, the interface supports both arrow key navigation and vim-style keybindings (**j/k** for up/down). The search screen allows real-time filtering by typing characters. All content is bundled within the binary, requiring no internet connection."],["INTERACTIVE CONTROLS","**Arrow Up / k**\n> Move selection up\n\n**Arrow Down / j**\n> Move selection down\n\n**Enter**\n> Select current item\n\n**Escape / q**\n> Go back or exit\n\n**Page Up / Page Down**\n> Scroll by page\n\n**Home / End**\n> Jump to start or end of list\n\n**Type characters**\n> Filter commands in search screen"],["CAVEATS","The TUI requires a terminal that supports ANSI escape sequences. On Windows, raw mode input has limited support and falls back to line-based input. Some terminal emulators may not render the interface correctly if they lack proper escape sequence handling."],["HISTORY","Linux Command Library originated as an **Android app** released on the Google Play Store and F-Droid, later expanding to **iOS**, **desktop** (via Compose Multiplatform), and the **web**. The native CLI version was added in **2025** using **Kotlin/Native** with the Mordant library for terminal rendering, providing a lightweight offline reference tool that runs directly in the terminal without requiring a JVM. The project maintains command documentation sourced from official man pages and the tldr-pages community project."],["SEE ALSO","[man](/man/man)(1), [tldr](/man/tldr)(1), [cheat](/man/cheat)(1), [apropos](/man/apropos)(1)"]],"e":["man","tldr","cheat","apropos"],"x":"lcl"},"locate":{"n":"locate","t":"searches a prebuilt database to find files matching a pattern","s":[["TAGLINE","searches a prebuilt database to find files matching a pattern"],["TLDR","**Find** files by pattern\n\n```locate \"[pattern]\"```\n\nFind **case-insensitive**\n\n```locate -i \"[pattern]\"```\n\nFind by **exact filename** (basename only)\n\n```locate -b \"\\[filename]\"```\n\n**Limit** output to a specific number of results\n\n```locate -l [10] \"[pattern]\"```\n\n**Count** the number of matching entries\n\n```locate -c \"[pattern]\"```\n\n**Display** database statistics\n\n```locate -S```\n\n**Update** the database\n\n```sudo updatedb```"],["SYNOPSIS","**locate** [_options_] _pattern_..."],["DESCRIPTION","**locate** searches a prebuilt database to find files matching a pattern, making it much faster than find for simple filename searches. The database is typically updated daily by a cron job."],["PARAMETERS","**-i, --ignore-case**\n> Case-insensitive search\n\n**-l, --limit N**\n> Limit output to N entries\n\n**-c, --count**\n> Print count of matches\n\n**-e, --existing**\n> Only print existing files\n\n**-b, --basename**\n> Match only basename\n\n**-r, --regexp**\n> Use basic regex pattern\n\n**--regex**\n> Use extended regex pattern\n\n**-d, --database** _DBPATH_\n> Search in specified database instead of the default\n\n**-S, --statistics**\n> Print statistics about each database and exit\n\n**-w, --wholename**\n> Match against the whole path name (default behavior)"],["CAVEATS","Database is updated periodically, so recently created files may not be found. Run `updatedb` to refresh the database manually."],["INSTALL","```apk: sudo apk add findutils-locate```"],["SEE ALSO","[find](/man/find)(1), [updatedb](/man/updatedb)(1), [mlocate](/man/mlocate)(1), [plocate](/man/plocate)(1)"]],"e":["find","updatedb","mlocate","plocate"],"x":"locate \"[pattern]\""},"lumabri":{"n":"lumabri","t":"Peer-to-peer MoE model swarm for distributed LLM inference","s":[["TAGLINE","Peer-to-peer MoE model swarm for distributed LLM inference"],["TLDR","**Serve** a local model directory on the swarm (tracker + bytes + optional expert compute)\n\n```lumabri serve --model [/path/to/model]```\n\n**Chat** against a tracker, using colibri engine binaries from a local tree\n\n```lumabri chat --tracker [host:7300] --engines-dir [/path/to/colibri/c]```\n\nOpen the interactive front end with **saved swarm settings** (no flags)\n\n```lumabri```\n\nGenerate an **operator keypair** for a signed swarm\n\n```lumabri key --out [swarm]```\n\nServe and **join** an existing tracker while donating disk space\n\n```lumabri serve --model [./slice] --join [host:7300] --model-name [model] --donate [GB]```\n\nChat against a **local** model directory (no mirror, no second copy)\n\n```lumabri chat --local [/path/to/model] --engines-dir [/path/to/colibri/c]```"],["SYNOPSIS","**lumabri** [_subcommand_] [_options_]"],["DESCRIPTION","**lumabri** runs huge mixture-of-experts (MoE) language models across a swarm of peers using the [colibri](https://github.com/JustVugg/colibri) engine. Pure C, no runtime dependencies beyond a colibri build for the engine binaries. One machine can share a model; others chat without downloading the full weights up front. Bytes that inference actually touches are fetched from peers, verified, and kept in a local mirror under **~/.lumabri** so later prompts can run offline from cache.\n\n**serve** starts a tracker (index of who holds which files) and a maintainer that answers byte-range reads. With phase 2 enabled it can also run expert execution for peers. **chat** boots a colibri engine against a model path that may exist only on the swarm: an **LD_PRELOAD** shim fetches missing blocks, and optional expert offload sends activations to peers that hold those experts. With no arguments, **lumabri** prompts for swarm address and key once and remembers them in **~/.lumabri/config**.\n\nIntegrity modes include open swarms (per-MiB hashes, optional operator signatures, spot-check of expert results) and private swarms gated by **LUMABRI_TOKEN**. Design goals emphasize byte-identical output whether experts run locally or remotely, and a hardware floor that includes CPU-only machines."],["SUBCOMMANDS","**serve**\n\n> Share a model: run tracker/maintainer (and usually expert execution). Flags include **--model**, **--port**, **--join**, **--donate**, **--model-name**, **--key**, **--advertise**, **--cache** / **--exec-cache**, **--no-exec**.\n\n**chat**\n\n> Connect to a swarm or a local model. Flags include **--tracker**, **--engines-dir**, **--local**, **--role**, **--donate**, **--model-dir**. In-chat commands include **/swarm** and **/model**.\n\n**key**\n\n> Create an Ed25519 operator keypair (**--out** path) for signed swarms."],["ENVIRONMENT","**LUMABRI_TOKEN**\n\n> Shared invite secret for private swarms; required on every node when set.\n\n**LUMABRI_PUBKEY**\n\n> Operator public key (hex) for verifying signed model hashes out of band.\n\n**LUMABRI_REQUIRE_HASH**\n\n> When set to 1, refuse fetches without integrity data.\n\n**LUMABRI_VERIFY**\n\n> Spot-check fraction for phase-2 expert results on a second replica.\n\n**LUMABRI_PREFETCH**\n\n> Readahead block count for cold mirrors (default 2)."],["CAVEATS","Requires **Linux**, **gcc**, **GNU make**, and a matching **colibri** checkout for engine binaries (**make phase2-all ENGINE=...** for peer expert execution). First cold answers are network-bound while the working set is mirrored. Misconfigured **--engines-dir** or missing keys can cause huge downloads or unverified models; the interactive default path is intentionally safer than raw flags. Tracker/maintainer/executor ports (commonly **7300–7302**, plus offsets per extra model) must be reachable for multi-host swarms. Status is a working prototype: features such as speculative drafting and key rotation remain incomplete."],["SEE ALSO","[ollama](/man/ollama)(1), [llama.cpp](/man/llama-cli)(1)"],["RESOURCES","```[Source code](https://github.com/JustVugg/lumabri)```"]],"e":["ollama","llama.cpp"],"x":"lumabri serve --model [/path/to/model]"},"lwp-request":{"n":"lwp-request","t":"command-line HTTP client from the LWP suite","s":[["TAGLINE","command-line HTTP client from the LWP suite"],["TLDR","**GET request**\n\n```lwp-request [https://example.com/]```\n\n**POST request with data**\n\n```lwp-request -m POST -c 'application/json' -C '[{\"key\":\"value\"}]' [https://api.example.com/]```\n\n**HEAD request** (headers only)\n\n```lwp-request -m HEAD [https://example.com/]```\n\n**Show response headers and content**\n\n```lwp-request -USed [https://example.com/]```\n\n**Follow redirects**\n\n```lwp-request -f [https://example.com/redirect]```\n\n**Use specific method**\n\n```lwp-request -m [DELETE] [https://api.example.com/resource]```\n\n**Set custom header**\n\n```lwp-request -H 'Authorization: Bearer [token]' [https://api.example.com/]```\n\n**Save to file**\n\n```lwp-request [https://example.com/file] > [output]```"],["SYNOPSIS","**lwp-request** [_-m method_] [_-f_] [_-H header_] [_-c content-type_] [_-C content_] [_options_] _url_"],["PARAMETERS","**-m** _METHOD_\n> HTTP method (GET, POST, HEAD, PUT, DELETE).\n\n**-f**\n> Follow redirects.\n\n**-H** _HEADER_\n> Add custom header.\n\n**-c** _TYPE_\n> Content-Type for request body.\n\n**-C** _CONTENT_\n> Content/body data.\n\n**-b** _URI_\n> Base URI for relative URLs.\n\n**-o** _FORMAT_\n> Output format.\n\n**-d**\n> Print request content/body.\n\n**-e**\n> Print response headers.\n\n**-s**\n> Print response status.\n\n**-S**\n> Print request URL.\n\n**-U**\n> Print request headers.\n\n**-t** _SECONDS_\n> Timeout.\n\n**-a**\n> Use text mode for content."],["DESCRIPTION","**lwp-request** is a command-line HTTP client from the LWP (Library for WWW in Perl) suite. It sends HTTP requests and displays responses.\n\nThe tool is also available as GET, POST, and HEAD commands, which are symlinks with preset methods. These shortcuts simplify common operations.\n\nOutput options control what gets printed: request details (-U, -S, -d) and response details (-e, -s). Combined flags show the full transaction for debugging.\n\nAuthentication, cookies, and proxy settings come from environment variables or configuration files. It integrates with the Perl LWP ecosystem.\n\nFor simple testing and scripting, lwp-request provides HTTP access without additional dependencies beyond Perl's LWP modules."],["CAVEATS","Requires libwww-perl package. Less feature-rich than curl or httpie. Error handling is basic. Large file downloads may be slow."],["HISTORY","**lwp-request** is part of **libwww-perl**, created by **Gisle Aas** starting in the **1990s**. LWP became the standard HTTP library for Perl, and lwp-request provided command-line access to its functionality."],["INSTALL","```pacman: sudo pacman -S perl-libwww```\n\n```apk: sudo apk add perl-libwww```"],["SEE ALSO","[curl](/man/curl)(1), [wget](/man/wget)(1), [http](/man/http)(1), [httpie](/man/httpie)(1), [perl](/man/perl)(1)"]],"e":["curl","wget","http","httpie","perl"],"x":"lwp-request [https://example.com/]"},"maltego":{"n":"maltego","t":"OSINT and graphical link analysis tool","s":[["TAGLINE","OSINT and graphical link analysis tool"],["TLDR","**Start Maltego**\n\n```maltego```\n\n**Start community edition**\n\n```maltego ce```\n\n**Run headless with config**\n\n```maltego -i [config.mtgx]```"],["SYNOPSIS","**maltego** [_options_]"],["PARAMETERS","**ce**\n> Community edition mode.\n\n**-i** _file_\n> Import configuration.\n\n**--help**\n> Show help."],["DESCRIPTION","**Maltego** is an OSINT (Open Source Intelligence) and graphical link analysis tool. It visualizes relationships between entities like people, organizations, domains, IP addresses, and infrastructure.\n\nMaltego uses transforms to automatically gather data from various sources and display connections in a graph format."],["ENTITY TYPES","```\n- Infrastructure: Domains, IPs, DNS\n- Personal: Names, Email, Social\n- Network: AS Numbers, Netblocks\n- Documents: Files, URLs\n```"],["EDITIONS","```\nCommunity (CE) - Free, limited transforms\nClassic        - Commercial\nXL             - Enterprise scale\n```"],["CAVEATS","Requires Java. CE edition has limitations. Transform servers may have rate limits. Premium transforms require subscriptions."],["HISTORY","Maltego was developed by **Paterva** in South Africa, first released in **2008**. It became a standard tool in penetration testing and intelligence gathering."],["INSTALL","```nix: nix profile install nixpkgs#maltego```"],["SEE ALSO","[recon-ng](/man/recon-ng)(1)"]],"e":["recon-ng"],"x":"maltego"},"marimo":{"n":"marimo","t":"reactive Python notebook that stores notebooks as pure Python files","s":[["TAGLINE","reactive Python notebook that stores notebooks as pure Python files"],["TLDR","**Create new notebook**\n\n```marimo edit [notebook.py]```\n\n**Open existing notebook**\n\n```marimo edit [notebook.py]```\n\n**Run notebook as app**\n\n```marimo run [notebook.py]```\n\n**Run with custom port**\n\n```marimo run --port [8080] [notebook.py]```\n\n**Convert Jupyter notebook**\n\n```marimo convert [notebook.ipynb] > [notebook.py]```\n\n**Export to HTML**\n\n```marimo export html [notebook.py] > [output.html]```\n\n**New notebook with template**\n\n```marimo new```\n\n**Show version**\n\n```marimo --version```"],["SYNOPSIS","**marimo** [_edit_] [_run_] [_convert_] [_export_] [_options_] [_notebook.py_]"],["PARAMETERS","**edit** _FILE_\n> Edit notebook in browser.\n\n**run** _FILE_\n> Run notebook as interactive app.\n\n**convert** _FILE_\n> Convert from Jupyter to marimo.\n\n**export** _FORMAT_ _FILE_\n> Export notebook (html, md, script).\n\n**new**\n> Create new notebook from template.\n\n**--port** _PORT_\n> Server port.\n\n**--host** _HOST_\n> Server host.\n\n**--headless**\n> Don't open browser.\n\n**--watch**\n> Watch for file changes.\n\n**--sandbox**\n> Run in sandbox environment.\n\n**--version**\n> Show version.\n\n**--help**\n> Show help."],["DESCRIPTION","**marimo** is a reactive Python notebook that stores notebooks as pure Python files. Unlike Jupyter, cells automatically re-execute when their dependencies change.\n\nNotebooks are Python scripts, enabling version control with Git without JSON merge conflicts. Cells define functions and variables that form a dependency graph.\n\nThe reactive execution model ensures consistency. Changing a variable automatically updates all dependent cells. Hidden state and execution order issues from traditional notebooks are eliminated.\n\nRun mode deploys notebooks as interactive web applications. Users interact with widgets while the Python backend handles computation.\n\nConversion from Jupyter preserves code and markdown cells. The reactive structure may need adjustment for notebooks that relied on execution order."],["CAVEATS","Different mental model from Jupyter - linear execution not guaranteed. Some Jupyter magic commands not supported. Large notebooks may have performance implications from reactive updates."],["HISTORY","**marimo** was created by **Akshay Agrawal** and **Myles Scolnick** in **2023** at Stanford. It addresses reproducibility issues in traditional notebooks by making execution deterministic through reactive programming."],["INSTALL","```nix: nix profile install nixpkgs#marimo```"],["SEE ALSO","[jupyter](/man/jupyter)(1), [ipython](/man/ipython)(1), [streamlit](/man/streamlit)(1), [python](/man/python)(1)"]],"e":["jupyter","ipython","streamlit","python"],"x":"marimo edit [notebook.py]"},"memtester":{"n":"memtester","t":"userspace utility for stress-testing memory subsystems","s":[["TAGLINE","userspace utility for stress-testing memory subsystems"],["TLDR","**Test 1GB of memory**\n\n```sudo memtester [1G] [1]```\n\n**Test 512MB with 5 iterations**\n\n```sudo memtester [512M] [5]```\n\n**Test memory at a physical address**\n\n```sudo memtester -p [0x10000000] [256M] [1]```\n\n**Run infinite test** until interrupted\n\n```sudo memtester [1G] [0]```\n\n**Test 2GB of memory** for thorough checking\n\n```sudo memtester [2G] [3]```"],["SYNOPSIS","**memtester** [**-p** _PHYSADDR_] _MEMORY_ [_ITERATIONS_]"],["PARAMETERS","_MEMORY_\n> Amount of memory to test (e.g., 100K, 256M, 1G).\n\n_ITERATIONS_\n> Number of test loops. Use 0 for infinite testing.\n\n**-p** _PHYSADDR_\n> Test memory starting at physical address PHYSADDR."],["DESCRIPTION","**memtester** is a userspace utility for stress-testing memory subsystems. It allocates the specified amount of memory and runs a series of pattern tests to detect faults in RAM, including stuck address bits, coupling faults, and bit flips.\n\nThe tests include: stuck address, random value, compare XOR, compare SUB, compare MUL, compare DIV, compare OR, compare AND, sequential increment, solid bits, block sequential, checkerboard, bit spread, bit flip, walking ones, and walking zeroes.\n\nmemtester runs in userspace and tests virtual memory. For testing physical RAM at specific addresses (e.g., to confirm hardware faults), use the **-p** option with the physical address."],["CAVEATS","Root privileges are typically required to lock memory pages and prevent swapping. Testing cannot use more memory than is available; the kernel may kill the process if memory is exhausted. memtester tests memory from userspace and may not detect all hardware faults that kernel-level tools like memtest86+ would find."],["HISTORY","memtester was written by Charles Cazabon as a userspace alternative to boot-time memory testing tools. It allows memory testing without rebooting and can target specific memory regions, useful for diagnosing intermittent faults on running systems."],["INSTALL","```apt: sudo apt install memtester```\n\n```dnf: sudo dnf install memtester```\n\n```pacman: sudo pacman -S memtester```\n\n```apk: sudo apk add memtester```\n\n```brew: brew install memtester```\n\n```nix: nix profile install nixpkgs#memtester```"],["SEE ALSO","[free](/man/free)(1), [vmstat](/man/vmstat)(8), [stress](/man/stress)(1)"]],"e":["free","vmstat","stress"],"x":"sudo memtester [1G] [1]"},"minisign":{"n":"minisign","t":"creates and verifies signatures","s":[["TAGLINE","creates and verifies signatures"],["TLDR","**Generate key pair**\n\n```minisign -G```\n\n**Sign file**\n\n```minisign -Sm [file]```\n\n**Verify signature**\n\n```minisign -Vm [file] -p [pubkey.pub]```\n\n**Sign with comment**\n\n```minisign -Sm [file] -c \"[Signed by me]\"```\n\n**Verify with trusted key**\n\n```minisign -Vm [file] -P [public_key_string]```"],["SYNOPSIS","**minisign** [_options_]"],["PARAMETERS","**-G**\n> Generate a new key pair.\n\n**-S**\n> Sign a file.\n\n**-V**\n> Verify a signature.\n\n**-R**\n> Re-create a public key file from an existing secret key.\n\n**-m** _FILE_\n> File to sign or verify.\n\n**-o**\n> With **-S**, emit a pre-hashed signature (required for files >1 GiB).\n\n**-H**\n> With **-V**, require the signature to be a pre-hashed signature.\n\n**-q**\n> Quiet mode; suppress the \"Comment\" banner on successful verification.\n\n**-Q**\n> Pretty-print the public key and the signature comment.\n\n**-s** _FILE_\n> Secret key file (default `~/.minisign/minisign.key`).\n\n**-p** _FILE_\n> Public key file.\n\n**-P** _KEY_\n> Public key supplied on the command line (base64).\n\n**-x** _FILE_\n> Signature file path (default `<input>.minisig`).\n\n**-c** _COMMENT_\n> Untrusted comment stored in the signature.\n\n**-t** _COMMENT_\n> Trusted comment (signed, cannot be tampered with without invalidating the signature).\n\n**-f**\n> Force: overwrite existing files without prompting.\n\n**-W**\n> Do not encrypt the newly generated secret key.\n\n**-v**\n> Show the minisign version and exit.\n\n**--help**\n> Display help information."],["DESCRIPTION","**minisign** creates and verifies signatures. It's simpler than GPG with good security.\n\nThe tool uses Ed25519 signatures. Designed for signing software releases."],["CAVEATS","Not compatible with GPG. Signing only (no encryption). Simple by design."],["HISTORY","Minisign was created by **Frank Denis** as a dead-simple tool for signing files."],["INSTALL","```apt: sudo apt install minisign```\n\n```dnf: sudo dnf install minisign```\n\n```pacman: sudo pacman -S minisign```\n\n```apk: sudo apk add minisign```\n\n```zypper: sudo zypper install minisign```\n\n```brew: brew install minisign```\n\n```nix: nix profile install nixpkgs#minisign```"],["SEE ALSO","[gpg](/man/gpg)(1), [age](/man/age)(1), [ssh-keygen](/man/ssh-keygen)(1)"]],"e":["gpg","age","ssh-keygen"],"x":"minisign -G"},"mousecrack":{"n":"mousecrack","t":"Human-like mouse movement via deep learning","s":[["TAGLINE","Human-like mouse movement via deep learning"],["TLDR","**Install** the global CLI\n\n```npm i -g mousecrack```\n\n**Move** the pointer to screen coordinates\n\n```mousecrack move [x] [y]```\n\n**Generate** step points from one position to another\n\n```mousecrack steps [x1] [y1] [x2] [y2]```"],["SYNOPSIS","**mousecrack** **move** _x_ _y_\n\n**mousecrack** **steps** _x1_ _y1_ _x2_ _y2_"],["DESCRIPTION","**mousecrack** synthesizes organically varied, human-like mouse trajectories using a mixture density network trained as a multivariate time-series forecaster over position deltas and timing. It is available both as a Node.js SDK (**move**, **steps**) and as a small CLI intended for agents and automation.\n\nThe project is experimental (v0.x) and labeled educational. Output quality varies; it is not a guaranteed bypass of bot detection systems.\n\nOptional agent skills/plugins are published from the same repository for Claude Code and Codex."],["COMMANDS","**move** _x_ _y_\n\n> Drive the system pointer to absolute coordinates (_x_, _y_) along a generated path.\n\n**steps** _x1_ _y1_ _x2_ _y2_\n\n> Print a sequence of intermediate points (with timestamps) from (_x1_, _y1_) to (_x2_, _y2_) without necessarily actuating the pointer (SDK-oriented)."],["CAVEATS","Experimental software. GUI automation may require accessibility/input permissions depending on the OS. Misuse against third-party services may violate their terms of service."],["SEE ALSO","[xdotool](/man/xdotool)(1), [ydotool](/man/ydotool)(1), [cliclick](/man/cliclick)(1)"],["RESOURCES","```[Source code](https://github.com/puffinsoft/mousecrack)```"]],"e":["xdotool","ydotool","cliclick"],"x":"npm i -g mousecrack"},"mousetweaks":{"n":"mousetweaks","t":"provides mouse accessibility features for GNOME","s":[["TAGLINE","provides mouse accessibility features for GNOME"],["TLDR","**Start mousetweaks**\n\n```mousetweaks```\n\n**Enable dwell click**\n\n```mousetweaks --dwell```\n\n**Enable secondary click**\n\n```mousetweaks --ssc```\n\n**Set dwell time**\n\n```mousetweaks --dwell-time=[1.2]```\n\n**Disable animations**\n\n```mousetweaks --animate=false```"],["SYNOPSIS","**mousetweaks** [_options_]"],["PARAMETERS","**--dwell**\n> Enable dwell click.\n\n**--ssc**\n> Enable simulated secondary click.\n\n**--dwell-time** _SECS_\n> Dwell click delay.\n\n**--animate** _BOOL_\n> Enable/disable animations.\n\n**--help**\n> Display help information."],["DESCRIPTION","**mousetweaks** provides mouse accessibility features for GNOME. It enables dwell clicking and other aids.\n\nThe tool assists users with motor impairments. Provides automatic clicking based on pointer dwelling."],["CAVEATS","GNOME specific. Part of GNOME accessibility suite. Requires X11 or Wayland."],["HISTORY","mousetweaks was developed as part of **GNOME's accessibility** initiative to assist users with limited mobility."],["INSTALL","```apt: sudo apt install mousetweaks```\n\n```dnf: sudo dnf install mousetweaks```\n\n```pacman: sudo pacman -S mousetweaks```\n\n```zypper: sudo zypper install mousetweaks```\n\n```nix: nix profile install nixpkgs#mousetweaks```"]],"e":[],"x":"mousetweaks"},"mren":{"n":"mren","t":"renames files and directories on MS-DOS filesystems","s":[["TAGLINE","renames files and directories on MS-DOS filesystems"],["TLDR","**Rename file on MS-DOS disk**\n\n```mren [a:/oldname.txt] [a:/newname.txt]```\n\n**Rename directory**\n\n```mren [a:/olddir] [a:/newdir]```\n\n**Verbose output**\n\n```mren -v [a:/old] [a:/new]```"],["SYNOPSIS","**mren** [_options_] _source_ _target_"],["PARAMETERS","_SOURCE_\n> Original MS-DOS file/directory name.\n\n_TARGET_\n> New MS-DOS file/directory name.\n\n**-v**\n> Verbose mode.\n\n**-D** _conflictoption_\n> Specify what to do when a name collision occurs (autorename, overwrite, skip).\n\n**-o**\n> Overwrite existing files without prompting."],["DESCRIPTION","**mren** renames files and directories on MS-DOS filesystems. It's part of the mtools package.\n\nThe tool works directly on FAT disks. Does not require mounting the filesystem."],["CAVEATS","Part of mtools. MS-DOS naming conventions apply. 8.3 filename limits on older systems."],["HISTORY","mren is part of **mtools**, the MS-DOS filesystem utilities collection for Unix systems."],["INSTALL","```apt: sudo apt install mtools```\n\n```dnf: sudo dnf install mtools```\n\n```pacman: sudo pacman -S mtools```\n\n```apk: sudo apk add mtools```\n\n```zypper: sudo zypper install mtools```\n\n```brew: brew install mtools```\n\n```nix: nix profile install nixpkgs#mtools```"],["SEE ALSO","[mcopy](/man/mcopy)(1), [mmove](/man/mmove)(1), [mdel](/man/mdel)(1)"]],"e":["mcopy","mmove","mdel"],"x":"mren [a:/oldname.txt] [a:/newname.txt]"},"mysqldump":{"n":"mysqldump","t":"creates logical backups of MySQL databases","s":[["TAGLINE","creates logical backups of MySQL databases"],["TLDR","**Dump database**\n\n```mysqldump -u [user] -p [database] > [backup.sql]```\n\n**Dump specific tables**\n\n```mysqldump -u [user] -p [database] [table1] [table2] > [backup.sql]```\n\n**Dump all databases**\n\n```mysqldump -u [user] -p --all-databases > [all.sql]```\n\n**Dump with compression**\n\n```mysqldump -u [user] -p [database] | gzip > [backup.sql.gz]```\n\n**Dump structure only** (no data)\n\n```mysqldump -u [user] -p --no-data [database] > [schema.sql]```\n\n**Dump data only** (no structure)\n\n```mysqldump -u [user] -p --no-create-info [database] > [data.sql]```\n\n**Dump with routines and triggers**\n\n```mysqldump -u [user] -p --routines --triggers [database] > [backup.sql]```\n\n**Dump for replication**\n\n```mysqldump -u [user] -p --single-transaction --master-data [database] > [backup.sql]```"],["SYNOPSIS","**mysqldump** [_-u user_] [_-p_] [_--all-databases_] [_--single-transaction_] [_options_] [_database_] [_tables_]"],["PARAMETERS","**-u** _USER_, **--user** _USER_\n> MySQL username.\n\n**-p**[_PASSWORD_], **--password**[=_PASS_]\n> Prompt for or specify password.\n\n**-h** _HOST_, **--host** _HOST_\n> Server hostname.\n\n**-P** _PORT_, **--port** _PORT_\n> Server port.\n\n**--all-databases**, **-A**\n> Dump all databases.\n\n**--databases**, **-B**\n> Dump multiple named databases.\n\n**--no-data**, **-d**\n> Don't dump table data.\n\n**--no-create-info**, **-t**\n> Don't dump CREATE TABLE.\n\n**--single-transaction**\n> Consistent snapshot for InnoDB.\n\n**--routines**, **-R**\n> Include stored procedures/functions.\n\n**--triggers**\n> Include triggers (default on).\n\n**--events**, **-E**\n> Include events.\n\n**--add-drop-table**\n> Add DROP TABLE before CREATE.\n\n**--add-drop-database**\n> Add DROP DATABASE before CREATE.\n\n**--master-data**[=_VALUE_]\n> Include binary log position.\n\n**--quick**, **-q**\n> Don't buffer, write directly.\n\n**--lock-tables**, **-l**\n> Lock tables during dump."],["DESCRIPTION","**mysqldump** creates logical backups of MySQL databases. Output is SQL statements that can recreate the database structure and data when executed.\n\nThe --single-transaction option provides consistent backups for InnoDB tables without locking. It starts a transaction and dumps data at that point in time, allowing other operations to continue.\n\nFor MyISAM or mixed storage engines, --lock-tables prevents writes during dump. This ensures consistency but blocks writes.\n\nRoutines (stored procedures, functions) and events are not included by default. Use --routines and --events explicitly to back up these objects.\n\nThe --master-data option records binary log coordinates, essential for setting up replication slaves or point-in-time recovery.\n\nOutput is plain SQL text, easily compressed with gzip. For very large databases, consider mysqlpump (parallel) or physical backup tools like Percona XtraBackup."],["CAVEATS","Not suitable for very large databases - consider physical backups. Memory usage can be high without --quick. Restore time can be long. Character set issues possible. Binary data encoding in SQL. Locking may affect production."],["HISTORY","**mysqldump** has been part of MySQL since early versions, developed at MySQL AB and later Oracle. It remains the standard logical backup tool for MySQL and MariaDB. While physical backup tools are preferred for large databases, mysqldump remains popular for its simplicity and portability."],["INSTALL","```apt: sudo apt install mariadb-client```\n\n```apk: sudo apk add mariadb-client```\n\n```zypper: sudo zypper install mariadb-client```"],["SEE ALSO","[mysql](/man/mysql)(1), [pg_dump](/man/pg_dump)(1)"]],"e":["mysql","pg_dump"],"x":"mysqldump -u [user] -p [database] > [backup.sql]"},"netplan":{"n":"netplan","t":"network configuration utility for Ubuntu and other Linux distributions","s":[["TAGLINE","network configuration utility for Ubuntu and other Linux distributions"],["TLDR","**Apply** network configuration permanently\n\n```sudo netplan apply```\n\n**Generate** backend configuration files\n\n```sudo netplan generate```\n\nConfigure interface to use **DHCP**\n\n```sudo netplan set ethernets.[interface].dhcp4=true```\n\n**Try** configuration changes with timeout\n\n```sudo netplan try --timeout [120]```\n\nApply with **debug** output\n\n```sudo netplan --debug apply```\n\nDisplay **current status**\n\n```netplan status```\n\n**Get** current configuration value\n\n```netplan get [ethernets]```"],["SYNOPSIS","**netplan** [**--debug**] _command_ [_options_]"],["COMMANDS","**apply**\n> Apply network configuration and make persistent\n\n**generate**\n> Generate backend-specific configuration files\n\n**try**\n> Try configuration changes with automatic rollback\n\n**get**\n> Get current configuration values\n\n**set**\n> Set configuration values\n\n**status**\n> Display current network status\n\n**info**\n> Show available features\n\n**ip**\n> Retrieve IP information (like DHCP leases) from the system\n\n**rebind**\n> Rebind SR-IOV virtual functions to their driver"],["PARAMETERS","**--debug**\n> Enable debug output\n\n**--root-dir** _path_\n> Use alternate root directory.\n\n**--timeout** _seconds_\n> Timeout for try command before rollback [default: 120].\n\n**--config-file** _path_\n> Use a specific configuration file."],["CONFIGURATION","Configuration files are stored in **/etc/netplan/*.yaml**\n\nExample YAML:\n```\nnetwork:\n  version: 2\n  ethernets:\n    eth0:\n      dhcp4: true\n```"],["DESCRIPTION","**netplan** is a network configuration utility for Ubuntu and other Linux distributions. It uses YAML files to describe network interfaces and generates configuration for backend renderers like NetworkManager or systemd-networkd.\n\nThe abstraction layer allows the same configuration to work with different network management backends, simplifying network setup across environments."],["CAVEATS","YAML syntax errors prevent configuration from applying. The try command defaults to a 120-second timeout before automatic rollback if not confirmed. A backend must be installed (NetworkManager or systemd-networkd). Changes to configuration files require **apply** to take effect."],["HISTORY","**netplan** was developed by **Canonical** for Ubuntu, first appearing in Ubuntu 17.10. It was designed to provide a unified network configuration system that works across different network backends while being human-readable."],["INSTALL","```apt: sudo apt install netplan.io```\n\n```dnf: sudo dnf install netplan```\n\n```pacman: sudo pacman -S netplan```\n\n```nix: nix profile install nixpkgs#netplan```"],["SEE ALSO","[networkctl](/man/networkctl)(1), [nmcli](/man/nmcli)(1), [ip](/man/ip)(8)"]],"e":["networkctl","nmcli","ip"],"x":"sudo netplan apply"},"nmcli":{"n":"nmcli","t":"command-line client for NetworkManager","s":[["TAGLINE","command-line client for NetworkManager"],["TLDR","List all **NetworkManager connections**\n\n```nmcli connection```\n\nShow **device status**\n\n```nmcli device```\n\n**Connect** to a Wi-Fi network\n\n```nmcli device wifi connect ssid --ask```\n\nShow available **Wi-Fi networks**\n\n```nmcli device wifi```\n\nShow **password** for current Wi-Fi\n\n```nmcli device wifi show-password```\n\n**Activate** a connection\n\n```nmcli connection up connection_name```\n\n**Deactivate** a connection\n\n```nmcli connection down connection_name```"],["SYNOPSIS","**nmcli** [_OPTIONS_] {_general_|_networking_|_radio_|_connection_|_device_|_agent_|_monitor_} [_COMMAND_] [_ARGUMENTS_]"],["DESCRIPTION","**nmcli** is a command-line client for NetworkManager. It allows controlling NetworkManager and reporting network status, creating, editing, activating, and deactivating network connections, and managing network devices."],["OBJECTS","**general**\n> Manage NetworkManager status, hostname, permissions, and logging\n\n**networking**\n> Control overall networking state and connectivity\n\n**radio**\n> Manage Wi-Fi and WWAN radio switches\n\n**connection**\n> Manage network connection profiles\n\n**device**\n> Manage network interfaces\n\n**agent**\n> Run as a NetworkManager secret or polkit agent\n\n**monitor**\n> Observe NetworkManager activity in real-time"],["PARAMETERS","**-t, --terse**\n> Produce terse output suitable for scripts\n\n**-p, --pretty**\n> Produce human-readable formatted output\n\n**-m, --mode tabular|multiline**\n> Switch between output modes\n\n**-c, --colors auto|yes|no**\n> Control colored output\n\n**-f, --fields**\n> Specify which fields/columns to output\n\n**-e, --escape yes|no**\n> Escape column separators in values\n\n**-a, --ask**\n> Prompt for missing parameters\n\n**-s, --show-secrets**\n> Display passwords and secrets\n\n**-w, --wait seconds**\n> Set timeout for operations\n\n**--offline**\n> Work without NetworkManager daemon"],["COMMON CONNECTION COMMANDS","**nmcli connection show**: List all connections\n**nmcli connection up**: Activate a connection\n**nmcli connection down**: Deactivate a connection\n**nmcli connection add**: Create a new connection\n**nmcli connection modify**: Change connection settings\n**nmcli connection delete**: Remove a connection\n**nmcli connection import**: Import a VPN connection"],["CAVEATS","The **--ask** flag prompts for passwords interactively. Use **--show-secrets** carefully as it displays sensitive information. The **-t** option with **-f** is useful for scripting."],["HISTORY","**nmcli** is the command-line interface for **NetworkManager**, first released around 2004. It provides a way to manage network connections on systems without graphical interfaces or for automation."],["INSTALL","```apt: sudo apt install network-manager```\n\n```apk: sudo apk add networkmanager-cli```"],["SEE ALSO","[nmtui](/man/nmtui)(1), [NetworkManager](/man/NetworkManager)(8), [nm-settings](/man/nm-settings)(5)"]],"e":["nmtui","NetworkManager","nm-settings"],"x":"nmcli connection"},"otree":{"n":"otree","t":"interactive TUI tree viewer for structured data","s":[["TAGLINE","interactive TUI tree viewer for structured data"],["TLDR","**View a JSON file**\n\n```otree [path/to/file.json]```\n\n**View YAML from stdin**\n\n```cat [file.yaml] | otree```\n\n**View a TOML file**\n\n```otree [path/to/file.toml]```\n\n**View an XML file**\n\n```otree [path/to/file.xml]```"],["SYNOPSIS","**otree** [_options_] [_file_]"],["PARAMETERS","**--debug**\n> Write debug logs to a file.\n\n**-h**, **--help**\n> Display help message.\n\n**-V**, **--version**\n> Display version information."],["DESCRIPTION","**otree** is a command-line tool for viewing structured data objects (JSON, YAML, TOML, XML) in an interactive TUI tree widget. It was designed to handle deeply nested Kubernetes YAML files and similar complex configurations. It supports root-changing, scaling, and clipboard operations.\n\nNavigation keys and colors are configurable via **~/.config/otree.toml**."],["CAVEATS","Only supports structured data formats (JSON, YAML, TOML, XML). Plain text files cannot be displayed."],["HISTORY","**otree** was created by **fioncat** and is written in **Rust**."],["INSTALL","```apk: sudo apk add otree```\n\n```brew: brew install otree```\n\n```nix: nix profile install nixpkgs#otree```"],["SEE ALSO","[jq](/man/jq)(1), [yq](/man/yq)(1), [jnv](/man/jnv)(1), [fx](/man/fx)(1)"]],"e":["jq","yq","jnv","fx"],"x":"otree [path/to/file.json]"},"oxker":{"n":"oxker","t":"Terminal user interface for viewing and controlling Docker containers","s":[["TAGLINE","Terminal user interface for viewing and controlling Docker containers"],["TLDR","**Launch the Docker container viewer**\n\n```oxker```\n\n**Set the update interval** to 500 milliseconds\n\n```oxker -d 500```\n\n**Show raw logs** without ANSI formatting\n\n```oxker -r```\n\n**Connect to a custom Docker socket**\n\n```oxker --host [/path/to/docker.sock]```"],["SYNOPSIS","**oxker** [_options_]"],["PARAMETERS","**-d** _MILLISECONDS_\n> Set the update interval in milliseconds. Default: 1000.\n\n**-r**\n> Show raw logs without ANSI formatting.\n\n**-c**\n> Attempt to colorize logs.\n\n**-t**\n> Remove timestamps from log entries.\n\n**--host** _STRING_\n> Custom Docker socket connection path.\n\n**--timezone** _STRING_\n> Display logs in the specified timezone."],["DESCRIPTION","**oxker** is a terminal user interface for viewing and controlling Docker containers. It displays a container list, logs, and resource stats in a single screen. You can pause, start, stop, restart, and delete containers directly from the TUI."],["CAVEATS","Requires access to the Docker socket. Running inside Docker itself requires mounting the host's Docker socket as a volume."],["HISTORY","**oxker** was created by **mrjackwills** and is written in **Rust**."],["INSTALL","```brew: brew install oxker```\n\n```nix: nix profile install nixpkgs#oxker```"],["SEE ALSO","[docker](/man/docker)(1), [lazydocker](/man/lazydocker)(1), [ctop](/man/ctop)(1)"]],"e":["docker","lazydocker","ctop"],"x":"oxker"},"packer":{"n":"packer","t":"HashiCorp's tool for creating machine images across multiple platforms","s":[["TAGLINE","HashiCorp's tool for creating machine images across multiple platforms"],["TLDR","**Build image from template**\n\n```packer build [template.pkr.hcl]```\n\n**Validate template**\n\n```packer validate [template.pkr.hcl]```\n\n**Initialize plugins**\n\n```packer init [template.pkr.hcl]```\n\n**Format template files**\n\n```packer fmt [template.pkr.hcl]```\n\n**Build with variables**\n\n```packer build -var \"[key]=[value]\" [template.pkr.hcl]```\n\n**Build specific target only**\n\n```packer build -only [amazon-ebs.web-server] [template.pkr.hcl]```\n\n**Inspect template**\n\n```packer inspect [template.pkr.hcl]```\n\n**Enable debug mode**\n\n```packer build -debug [template.pkr.hcl]```"],["SYNOPSIS","**packer** _command_ [_-var key=value_] [_-only target_] [_-except target_] [_options_] _template_"],["PARAMETERS","**build**\n> Build image from template.\n\n**validate**\n> Check template syntax.\n\n**init**\n> Install required plugins.\n\n**fmt**\n> Format template files.\n\n**inspect**\n> Show template components.\n\n**console**\n> Interactive HCL console.\n\n**-var** _key=value_\n> Set variable value.\n\n**-var-file** _file_\n> Variable definitions file.\n\n**-only** _name_\n> Build only specified builders.\n\n**-except** _name_\n> Skip specified builders.\n\n**-force**\n> Force build, overwriting existing.\n\n**-on-error** _ACTION_\n> Action on error: cleanup, abort, ask, run-cleanup-provisioner.\n\n**-debug**\n> Debug mode with breakpoints.\n\n**-parallel-builds** _N_\n> Parallel build limit.\n\n**-timestamp-ui**\n> Add timestamps to UI output."],["DESCRIPTION","**Packer** is HashiCorp's tool for creating machine images across multiple platforms from a single source configuration. It automates image building for AWS, Azure, GCP, Docker, VMware, and many other platforms.\n\nTemplates define builders (where to create images), provisioners (how to configure them), and post-processors (what to do with finished images). Modern templates use HCL format (.pkr.hcl), though JSON is still supported.\n\nBuilders create the initial machine instance. Examples include amazon-ebs for AWS AMIs, azure-arm for Azure images, and docker for containers. Each builder handles platform-specific details.\n\nProvisioners run after the machine boots: shell scripts, Ansible, Chef, Puppet, or file uploads configure the image. Multiple provisioners run in sequence, building up the configuration.\n\nPost-processors handle finished images: upload to registries, compress, or create Vagrant boxes. Images can be distributed to multiple regions or clouds."],["CAVEATS","Platform credentials required for cloud builders. Build times vary significantly by platform. Large images require more time and storage. Debug mode pauses between steps. JSON templates are deprecated but still work. Plugin initialization required for non-bundled builders."],["HISTORY","**Packer** was released by **HashiCorp** in **2013**, created by Mitchell Hashimoto. It addressed the challenge of creating identical machine images across multiple platforms. The tool became essential in DevOps workflows for immutable infrastructure. In **2020**, HCL2 template format was introduced, aligning with Terraform syntax."],["INSTALL","```pacman: sudo pacman -S packer```\n\n```nix: nix profile install nixpkgs#packer```"],["SEE ALSO","[terraform](/man/terraform)(1), [vagrant](/man/vagrant)(1), [docker](/man/docker)(1), [ansible](/man/ansible)(1)"]],"e":["terraform","vagrant","docker","ansible"],"x":"packer build [template.pkr.hcl]"},"padsp":{"n":"padsp","t":"Wrap OSS applications for PulseAudio","s":[["TAGLINE","Wrap OSS applications for PulseAudio"],["TLDR","**Run an OSS application** with PulseAudio\n\n```padsp [oss_application]```\n\n**Connect to a specific** PulseAudio server\n\n```padsp -s [server] [application]```\n\n**Set the client** application name\n\n```padsp -n [name] [application]```\n\n**Disable /dev/dsp emulation**\n\n```padsp -D [application]```\n\n**Enable debug** output\n\n```padsp -d [application]```"],["SYNOPSIS","**padsp** [_options_] _program_ [_arguments_...]"],["PARAMETERS","_PROGRAM_\n> OSS application to run.\n\n**-s** _SERVER_\n> PulseAudio server to connect to.\n\n**-n** _NAME_\n> Client application name sent to the server.\n\n**-m** _NAME_\n> Stream name passed when creating a stream.\n\n**-M**\n> Disable /dev/mixer device emulation.\n\n**-S**\n> Disable /dev/sndstat device emulation.\n\n**-D**\n> Disable /dev/dsp device emulation.\n\n**-d**\n> Enable debug output.\n\n**-h**, **--help**\n> Display help information."],["DESCRIPTION","**padsp** (PulseAudio OSS Wrapper) starts a program using an LD_PRELOAD library that redirects OSS audio device access (/dev/dsp, /dev/mixer, /dev/sndstat) to PulseAudio. This provides a compatibility layer for legacy OSS applications so they play through the PulseAudio sound server."],["CAVEATS","Relies on **LD_PRELOAD**, so it only works with dynamically linked ELF binaries. Not every OSS feature is emulated; applications using mmap or exotic ioctls may not work. Part of the PulseAudio distribution."],["HISTORY","**padsp** was developed as part of the **PulseAudio** project to support legacy OSS applications after PulseAudio replaced OSS as the default sound system on most Linux distributions."],["INSTALL","```apt: sudo apt install pulseaudio-utils```\n\n```dnf: sudo dnf install pulseaudio-utils```\n\n```apk: sudo apk add pulseaudio-utils```\n\n```zypper: sudo zypper install pulseaudio-utils```"],["SEE ALSO","[pulseaudio](/man/pulseaudio)(1), [aoss](/man/aoss)(1), [pactl](/man/pactl)(1)"]],"e":["pulseaudio","aoss","pactl"],"x":"padsp [oss_application]"},"pam":{"n":"pam","t":"pluggable Authentication Modules","s":[["TAGLINE","pluggable Authentication Modules"],["TLDR","**Check PAM configuration**\n\n```pam-auth-update```\n\n**Test PAM authentication**\n\n```pamtester [service] [user] authenticate```\n\n**View PAM config**\n\n```cat /etc/pam.d/[service]```\n\n**Debug PAM**\n\n```pam-auth-update --package```"],["SYNOPSIS","**pam** modules configured in /etc/pam.d/"],["PARAMETERS","Configuration files in /etc/pam.d/:\n\n**auth**\n> Authentication token verification.\n\n**account**\n> Account validation.\n\n**password**\n> Password update.\n\n**session**\n> Session setup/teardown."],["DESCRIPTION","**PAM** is Pluggable Authentication Modules. Framework for system authentication.\n\nThe system provides flexible authentication. Configured per-service."],["CAVEATS","Critical system component. Misconfiguration can lock out users. Test carefully."],["HISTORY","PAM was developed by **Sun Microsystems** as a pluggable authentication framework."],["INSTALL","```dnf: sudo dnf install pam```\n\n```pacman: sudo pacman -S pam```\n\n```zypper: sudo zypper install pam```\n\n```nix: nix profile install nixpkgs#pam```"],["SEE ALSO","[pam.conf](/man/pam.conf)(5), [pam.d](/man/pam.d)(5), [login](/man/login)(1)"]],"e":["pam.conf","pam.d","login"],"x":"pam-auth-update"},"pamoil":{"n":"pamoil","t":"creates oil painting effect","s":[["TAGLINE","creates oil painting effect"],["TLDR","**Apply oil painting effect**\n\n```pamoil [input.pam] > [output.pam]```\n\n**Set smear size**\n\n```pamoil -n [3] [input.pam] > [output.pam]```"],["SYNOPSIS","**pamoil** [_options_] [_file_]"],["PARAMETERS","_FILE_\n> Input PAM/PNM file.\n\n**-n** _SIZE_\n> Neighborhood size (default 3)."],["DESCRIPTION","**pamoil** creates oil painting effect. Smears colors based on frequency.\n\nThe tool applies artistic filter. Part of Netpbm."],["CAVEATS","Part of Netpbm. Larger sizes slower. Artistic effect."],["HISTORY","pamoil is part of **Netpbm** for applying oil painting effects."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[ppmrelief](/man/ppmrelief)(1), [pgmedge](/man/pgmedge)(1), [netpbm](/man/netpbm)(1)"]],"e":["ppmrelief","pgmedge","netpbm"],"x":"pamoil [input.pam] > [output.pam]"},"pasuspender":{"n":"pasuspender","t":"temporarily suspends PulseAudio to allow another application exclusive access","s":[["TAGLINE","temporarily suspends PulseAudio to allow another application exclusive access"],["TLDR","Suspend PulseAudio while running **JACK**\n\n```pasuspender -- jackd --driver alsa --device hw:0```\n\nSuspend PulseAudio for **any command**\n\n```pasuspender -- [command]```"],["SYNOPSIS","**pasuspender** [**-s** _server_] **--** _command_ [_arguments_...]"],["PARAMETERS","**-s, --server _server_**\n> Connect to specified PulseAudio server\n\n**--**\n> Separator between pasuspender options and command\n\n**-h, --help**\n> Display help information\n\n**--version**\n> Display version information"],["DESCRIPTION","**pasuspender** temporarily suspends PulseAudio to allow another application exclusive access to audio hardware through ALSA. This is necessary for applications that cannot work with PulseAudio or require direct hardware access.\n\nWhen the specified command exits, PulseAudio automatically resumes normal operation. This is commonly used with professional audio applications like JACK that need low-latency direct hardware access."],["CAVEATS","Only works with PulseAudio (not PipeWire without compatibility layer). The command runs with exclusive audio access, blocking other audio applications. PulseAudio must be running for the command to work. Some applications may not release audio cleanly."],["HISTORY","**pasuspender** is part of the **PulseAudio** sound system developed by **Lennart Poettering** and the freedesktop.org community. It addresses the common need to temporarily bypass the sound server for applications requiring direct hardware access."],["INSTALL","```apt: sudo apt install pulseaudio-utils```\n\n```dnf: sudo dnf install pulseaudio-utils```\n\n```apk: sudo apk add pulseaudio-utils```\n\n```zypper: sudo zypper install pulseaudio-utils```"],["SEE ALSO","[pulseaudio](/man/pulseaudio)(1), [pactl](/man/pactl)(1), [jackd](/man/jackd)(1)"]],"e":["pulseaudio","pactl","jackd"],"x":"pasuspender -- jackd --driver alsa --device hw:0"},"pdfbook2":{"n":"pdfbook2","t":"creates printable booklets from PDF files by rearranging pages into printer","s":[["TAGLINE","creates printable booklets from PDF files by rearranging pages into printer"],["TLDR","Create a **booklet** named file-book.pdf with sane defaults\n\n```pdfbook2 [path/to/file.pdf]```\n\nCreate a booklet with **paper size** set to A4\n\n```pdfbook2 -p a4paper [path/to/file.pdf]```\n\nCreate a booklet with **inner margin** reduced to 50 pixels\n\n```pdfbook2 -p a4paper -i 50 [path/to/file.pdf]```\n\nOrganize large file with **print signatures** for binding\n\n```pdfbook2 -p a4paper --signature 24 [path/to/file.pdf]```"],["SYNOPSIS","**pdfbook2** [_options_] _file.pdf_"],["PARAMETERS","**-p**, **--paper** _size_\n> Set paper size (a4paper, letterpaper, etc.)\n\n**-s**, **--short-edge**\n> Use short-edge binding (default is long-edge)\n\n**-n**, **--no-crop**\n> Do not crop pages before reassembling\n\n**-o**, **--outer-margin** _pixels_\n> Set outer margin (default: 40)\n\n**-i**, **--inner-margin** _pixels_\n> Set inner margin (default: 150)\n\n**-t**, **--top-margin** _pixels_\n> Set top margin (default: 30)\n\n**-b**, **--bottom-margin** _pixels_\n> Set bottom margin (default: 30)\n\n**--signature** _num_\n> Number of pages per signature (must be divisible by 4)\n\n**--signature\\*** _num_\n> Same as --signature but allow incomplete last signature\n\n**-h**, **--help**\n> Display help message"],["DESCRIPTION","**pdfbook2** creates printable booklets from PDF files by rearranging pages into printer spreads. The output is designed for double-sided printing in landscape mode with long-edge flipping, resulting in a booklet that can be folded and bound.\n\nFor larger documents, the signature option divides the booklet into smaller sections (signatures) that can be nested and bound together. Each signature must contain a number of pages divisible by 4."],["CAVEATS","Print in **landscape mode** with **long-edge flip** for correct results. Signature values must be divisible by 4. Default margins may need adjustment depending on printer capabilities. Requires pdflatex and the pdfpages LaTeX package."],["HISTORY","Written as a Python wrapper around pdfpages and pdflatex to simplify booklet creation. Replaces manual LaTeX workflows for creating print-ready booklets from existing PDF documents. Common in the self-publishing and zine-making communities."],["INSTALL","```pacman: sudo pacman -S texlive-binextra```\n\n```apk: sudo apk add texlive-binextra```"],["SEE ALSO","[pdfjam](/man/pdfjam)(1), [pdfcrop](/man/pdfcrop)(1), [pdflatex](/man/pdflatex)(1)"]],"e":["pdfjam","pdfcrop","pdflatex"],"x":"pdfbook2 [path/to/file.pdf]"},"php-config":{"n":"php-config","t":"Display information about PHP's compile-time configuration","s":[["TAGLINE","Display information about PHP's compile-time configuration"],["TLDR","**Show the PHP installation prefix**\n\n```php-config --prefix```\n\n**Show the extension directory path**\n\n```php-config --extension-dir```\n\n**Show the header file include directory**\n\n```php-config --include-dir```\n\n**Show compiler include flags for building extensions**\n\n```php-config --includes```\n\n**Show the configure options PHP was built with**\n\n```php-config --configure-options```\n\n**Show the PHP version**\n\n```php-config --version```\n\n**Show the path to the PHP binary**\n\n```php-config --php-binary```\n\n**Show all available SAPI modules**\n\n```php-config --php-sapis```"],["SYNOPSIS","**php-config** [_options_]"],["PARAMETERS","**--prefix**\n> PHP installation prefix directory (e.g., /usr or /usr/local).\n\n**--includes**\n> Compiler -I flags for all include directories, used when building extensions.\n\n**--ldflags**\n> Linker flags PHP was compiled with.\n\n**--libs**\n> Extra libraries PHP was compiled with.\n\n**--extension-dir**\n> Default directory where extensions are installed and loaded from.\n\n**--include-dir**\n> Directory prefix where header files are installed.\n\n**--php-binary**\n> Full path to the PHP CLI or CGI binary.\n\n**--php-sapis**\n> List all available SAPI (Server API) modules (e.g., cli, fpm, apache2handler).\n\n**--configure-options**\n> The ./configure options used when PHP was built, useful for recreating the same build.\n\n**--version**\n> PHP version string.\n\n**--vernum**\n> PHP version as an integer (e.g., 80200 for 8.2.0), useful in scripts.\n\n**--ini-path**\n> Directory from which PHP reads its main INI configuration file.\n\n**--ini-dir**\n> Directory from which PHP scans for additional INI configuration files."],["DESCRIPTION","**php-config** is a shell script that provides information about PHP's compile-time configuration. It is essential for building PHP extensions with **phpize** and for writing Makefiles that need to link against PHP.\n\nTypical usage in extension development involves passing **--includes** and **--ldflags** to the compiler, and specifying the full path to php-config via **./configure --with-php-config=/path/to/php-config**."],["CAVEATS","Output varies by PHP installation and version. When multiple PHP versions are installed, each has its own php-config (e.g., php-config7.4, php-config8.2). Always use the path corresponding to the target PHP version when building extensions."],["INSTALL","```apk: sudo apk add php85-dev```"],["SEE ALSO","[phpize](/man/phpize)(1), [php](/man/php)(1), [pecl](/man/pecl)(1)"]],"e":["phpize","php","pecl"],"x":"php-config --prefix"},"pip-wheel":{"n":"pip-wheel","t":"Build wheel archives from Python packages","s":[["TAGLINE","Build wheel archives from Python packages"],["TLDR","**Build wheel from package**\n\n```pip wheel [package]```\n\n**Build wheels to directory**\n\n```pip wheel -w [wheeldir] [package]```\n\n**Build from requirements file**\n\n```pip wheel -r requirements.txt```\n\n**Build wheel from local project**\n\n```pip wheel .```"],["SYNOPSIS","**pip** **wheel** [_options_] _packages_..."],["PARAMETERS","**-w**, **--wheel-dir** _dir_\n> Build wheels to directory.\n\n**-r**, **--requirement** _file_\n> Build from requirements file.\n\n**--no-deps**\n> Don't build dependencies.\n\n**--no-build-isolation**\n> Disable build isolation.\n\n**--src** _dir_\n> Source directory for editable projects."],["DESCRIPTION","**pip wheel** builds wheel archives for packages and dependencies. Wheels are pre-built binary distributions that install faster than source distributions. Useful for creating local wheels for offline installation or deployment."],["INSTALL","```apk: sudo apk add py3-pip```"],["SEE ALSO","[pip](/man/pip)(1), [pip-download](/man/pip-download)(1), [pip-install](/man/pip-install)(1)"]],"e":["pip","pip-download","pip-install"],"x":"pip wheel [package]"},"pnm2ppa":{"n":"pnm2ppa","t":"Convert PNM images to HP PPA printer format","s":[["TAGLINE","Convert PNM images to HP PPA printer format"],["TLDR","**Convert a color PNM to PPA format**\n\n```pnm2ppa --eco -i [input.pnm] -o [output.ppa]```\n\n**Print a PNM file directly to the printer**\n\n```pnm2ppa -i [image.pnm] -o - | lpr```\n\n**Convert with a specific paper size**\n\n```pnm2ppa --paper [a4] -i [input.pnm] -o [output.ppa]```\n\n**Convert a black-and-white PNM**\n\n```pbm2ppa -i [input.pbm] -o [output.ppa]```"],["SYNOPSIS","**pnm2ppa** [_options_] [**-i** _infile_] [**-o** _outfile_]"],["PARAMETERS","**-i** _FILE_\n> Input PNM file (use - for stdin).\n\n**-o** _FILE_\n> Output PPA file (use - for stdout).\n\n**--paper** _SIZE_\n> Paper size (letter, a4, legal).\n\n**--dpi** _DPI_\n> Resolution (300 or 600).\n\n**--eco**\n> Economode: use less ink.\n\n**--bw**\n> Print in black and white only.\n\n**-v** _PRINTER_\n> Printer model (710, 720, 820, 1000).\n\n**--gamma** _VALUE_\n> Gamma correction value for color adjustment.\n\n**-f** _FILE_\n> Read configuration from file.\n\n**--help**\n> Display help."],["DESCRIPTION","**pnm2ppa** converts PNM (Portable Any Map) images to HP PPA (Printer Performance Architecture) format used by certain HP DeskJet inkjet printers (710, 720, 820, 1000 series). These printers lack standard PCL or PostScript support and require a host-based driver to render pages.\n\nThe tool generates the raw printer data stream, which can be sent directly to the printer device or piped through the print spooler. It is typically used in a GhostScript pipeline: a PostScript or PDF file is first rendered to PNM format by GhostScript, then converted to PPA by pnm2ppa. A companion utility **pbm2ppa** handles black-and-white PBM images. Paper size, resolution, and economy mode options control the output formatting."],["CAVEATS","Only works with specific HP DeskJet PPA printers. Considered legacy; most modern printers use standard drivers. Supports only 300 and 600 DPI resolutions."],["HISTORY","pnm2ppa was created for **HP PPA printers** lacking standard driver support."],["INSTALL","```apt: sudo apt install printer-driver-pnm2ppa```\n\n```dnf: sudo dnf install pnm2ppa```\n\n```zypper: sudo zypper install pnm2ppa```"],["SEE ALSO","[pnm](/man/pnm)(5), [pbmtoppa](/man/pbmtoppa)(1), [gs](/man/gs)(1), [lpr](/man/lpr)(1), [lpstat](/man/lpstat)(1)"]],"e":["pnm","pbmtoppa","gs","lpr","lpstat"],"x":"pnm2ppa --eco -i [input.pnm] -o [output.ppa]"},"popeye":{"n":"popeye","t":"Scan Kubernetes clusters for issues","s":[["TAGLINE","Scan Kubernetes clusters for issues"],["TLDR","**Scan Kubernetes cluster**\n\n```popeye```\n\n**Scan specific namespace**\n\n```popeye -n [namespace]```\n\n**Scan with specific context**\n\n```popeye --context [context]```\n\n**Output as JSON**\n\n```popeye -o json```\n\n**Save report to file**\n\n```popeye --save --out [dir]```"],["SYNOPSIS","**popeye** [_options_]"],["PARAMETERS","**-n**, **--namespace** _NS_\n> Scan specific namespace.\n\n**--context** _CTX_\n> Kubernetes context.\n\n**-o**, **--out** _FORMAT_\n> Output format (json, yaml, html).\n\n**--save**\n> Save report to file.\n\n**-A**, **--all-namespaces**\n> Scan all namespaces."],["DESCRIPTION","**popeye** scans a live Kubernetes cluster and reports potential issues, misconfigurations, and resource problems. It checks pods, services, deployments, and other resources against best practices and common pitfalls.\n\nResults are color-coded by severity (OK, info, warning, error). Use **-n** to scan a specific namespace, **-o** for machine-readable output formats (JSON, YAML, HTML), and **--save** to write reports to disk. All operations are read-only."],["CAVEATS","Requires cluster access. Read-only operations."],["HISTORY","Popeye was created as a **Kubernetes cluster sanitizer** and linter."],["INSTALL","```pacman: sudo pacman -S popeye```\n\n```zypper: sudo zypper install popeye```\n\n```brew: brew install popeye```\n\n```nix: nix profile install nixpkgs#popeye```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [kube-linter](/man/kube-linter)(1)"]],"e":["kubectl","kube-linter"],"x":"popeye"},"py-sql-cleaner":{"n":"py-sql-cleaner","t":"Format and extract SQL strings embedded in Python source files","s":[["TAGLINE","Format and extract SQL strings embedded in Python source files"],["TLDR","**List** embedded SQL blocks in a Python file\n\n```py-sql-cleaner list jobs/load_users.py```\n\n**Format** SQL in place using the generic dialect\n\n```py-sql-cleaner format jobs/load_users.py```\n\n**Preview** formatting changes without writing\n\n```py-sql-cleaner format jobs/load_users.py --dry-run```\n\n**Format** using a specific dialect (mysql, postgres, redshift)\n\n```py-sql-cleaner format jobs/load_users.py -d postgres```\n\n**Extract** SQL to standalone .sql files\n\n```py-sql-cleaner extract jobs/load_users.py --out-dir sql```\n\n**Check** SQL formatting in CI (non-zero exit on diff)\n\n```py-sql-cleaner check jobs/load_users.py```\n\n**List** supported SQL dialects\n\n```py-sql-cleaner dialects```"],["SYNOPSIS","**py-sql-cleaner** _command_ [_options_] _file_"],["DESCRIPTION","**py-sql-cleaner** is a Python CLI that finds triple-quoted SQL strings inside Python files and reformats or extracts them using **SQLGlot**. It is intended for ETL and data-engineering projects where SQL is embedded in `.py` files rather than kept in separate `.sql` files.\n\nThe tool is conservative by default. f-strings, Jinja templates and other runtime placeholders are detected but skipped instead of being rewritten, so formatting cannot silently change a query that depends on interpolation.\n\npy-sql-cleaner never connects to a database and never executes SQL. The supported dialects (`generic`, `mysql`, `postgres`, `redshift`) only select SQLGlot's parser and formatter mode, not full database validation.\n\nThe `check` subcommand exits non-zero when formatting would change a file, making it suitable as a pre-commit hook or CI gate next to `black`, `ruff` and similar formatters."],["PARAMETERS","**list** _file_\n> Show every embedded SQL block found in the file.\n\n**format** _file_\n> Reformat embedded SQL in place.\n\n**extract** _file_\n> Write each SQL block to a separate `.sql` file.\n\n**check** _file_\n> Exit non-zero if formatting would change the file.\n\n**dialects**\n> Print supported SQL dialects.\n\n**-d**, **--dialect** _NAME_\n> Select dialect: `generic`, `mysql`, `postgres`, `redshift`.\n\n**--dry-run**\n> Print the formatted result instead of writing it.\n\n**--out-dir** _DIR_\n> Output directory for `extract`.\n\n**--version**\n> Print the installed version."],["INSTALLATION","```pip install py-sql-cleaner```\n\nOr with pipx for isolated installation:\n\n```pipx install py-sql-cleaner```\n\nRun without installing:\n\n```uvx py-sql-cleaner --help```"],["CAVEATS","The project is an early MVP. f-strings and templating syntax are intentionally skipped to avoid breaking queries that build SQL at runtime. Dialect selection affects parsing only and does not guarantee a query will execute on the target database."],["SEE ALSO","[black](/man/black), [ruff](/man/ruff), [sqlfluff](/man/sqlfluff)"]],"e":["black","ruff","sqlfluff"],"x":"py-sql-cleaner list jobs/load_users.py"},"pyp":{"n":"pyp","t":"Easily run Python at the shell for data plumbing","s":[["TAGLINE","Easily run Python at the shell for data plumbing"],["TLDR","**Double each line** as an integer\n\n```seq 3 | pyp 'int(x)*2'```\n\n**Sum numbers** from stdin\n\n```seq 10 | pyp 'sum(map(int, lines))'```\n\n**Pretty-print JSON** lines\n\n```cat [data.jsonl] | pyp 'json.loads(x)'```\n\n**Run with automatic imports** explained\n\n```pyp -b '...'```"],["SYNOPSIS","**pyp** [*options*] *expression*"],["DESCRIPTION","**pyp** lets you use short Python expressions as shell filters. Each stdin line is available as **x** (and related helpers), and **lines** exposes all input for reductions. It automatically imports common modules when referenced, aiming to replace many **awk**/**sed** one-liners when Python is a better fit.\n\nInstall via **pip install pypyp** (package name **pypyp** on PyPI; command is **pyp**)."],["PARAMETERS","*expression*\n\n> Python expression or small script applied to input.\n\n**-b**, **--explain** / magic import related flags\n\n> Explain automatic imports or control behavior (see **pyp --help**).\n\n**-h**, **--help**\n\n> Full option list for your installed version."],["CAVEATS","Python startup cost makes it slower than **awk** for huge streams. Automatic imports can surprise; prefer explicit imports for production scripts. Not a replacement for complex multi-file programs."],["INSTALL","```brew: brew install pyp```\n\n```nix: nix profile install nixpkgs#pyp```"],["SEE ALSO","[python](/man/python)(1), [awk](/man/awk)(1), [jq](/man/jq)(1), [perl](/man/perl)(1)"],["RESOURCES","```[Source code](https://github.com/hauntsaninja/pyp)```"]],"e":["python","awk","jq","perl"],"x":"seq 3 | pyp 'int(x)*2'"},"qutebrowser":{"n":"qutebrowser","t":"Keyboard-driven web browser with vim keybindings","s":[["TAGLINE","Keyboard-driven web browser with vim keybindings"],["TLDR","**Start browser**\n\n```qutebrowser```\n\n**Open URL**\n\n```qutebrowser [https://example.com]```\n\n**Open in new window**\n\n```qutebrowser --target window [https://example.com]```\n\n**Start with temp basedir**\n\n```qutebrowser --temp-basedir```\n\n**Set config option**\n\n```qutebrowser --set [content.javascript.enabled] [false]```"],["SYNOPSIS","**qutebrowser** [_options_] [_urls_...]"],["DESCRIPTION","**qutebrowser** is a keyboard-driven web browser with vim-style keybindings. It uses QtWebEngine and focuses on minimalism and keyboard efficiency.\n\nThe browser is highly configurable through a Python config file and commands, appealing to users who prefer keyboard-centric workflows."],["PARAMETERS","**-t**, **--target** _type_\n> How to open URL (tab, tab-bg, window).\n\n**-T**, **--temp-basedir**\n> Use temporary directory.\n\n**-s** _opt_ _value_, **--set** _opt_ _value_\n> Set configuration option.\n\n**-r** _session_, **--restore** _session_\n> Restore session.\n\n**-B** _dir_, **--basedir** _dir_\n> Base directory.\n\n**--debug**\n> Debug mode."],["KEY BINDINGS","**o**: Open URL\n**O**: Open in new tab\n**d**: Close tab\n**j/k**: Scroll\n**h/l**: History\n**f**: Follow hints\n**yy**: Copy URL\n**/**: Search\n**:**: Command mode"],["CAVEATS","Some sites may not work (WebEngine limitations). Extension support limited. Learning curve for keybindings. Qt dependency large."],["HISTORY","**qutebrowser** was created by **Florian Bruhin** starting in **2013**. It fills the niche for keyboard-driven browsers like uzbl and vimperator, but with modern web engine support."],["INSTALL","```dnf: sudo dnf install qutebrowser```\n\n```pacman: sudo pacman -S qutebrowser```\n\n```apk: sudo apk add qutebrowser```\n\n```zypper: sudo zypper install qutebrowser```\n\n```nix: nix profile install nixpkgs#qutebrowser```"],["SEE ALSO","[firefox](/man/firefox)(1), [chromium](/man/chromium)(1), [surf](/man/surf)(1), [w3m](/man/w3m)(1)"]],"e":["firefox","chromium","surf","w3m"],"x":"qutebrowser"},"sensors-detect":{"n":"sensors-detect","t":"Detect hardware monitoring sensor chips","s":[["TAGLINE","Detect hardware monitoring sensor chips"],["TLDR","**Run interactively** to detect hardware monitoring chips\n\n```sudo sensors-detect```\n\n**Run in automatic mode** with default answers\n\n```sudo sensors-detect --auto```\n\n**Display I2C address statistics**\n\n```sudo sensors-detect --stat```"],["SYNOPSIS","**sensors-detect** [**--auto**] [**--stat**]"],["PARAMETERS","**--auto**\n> Run in automatic, non-interactive mode. Assumes default answers to all questions. Use with caution as this may lead to potentially dangerous hardware probes.\n\n**--stat**\n> Display I2C address statistics."],["DESCRIPTION","**sensors-detect** is an interactive program that scans your system for hardware monitoring chips (sensors) supported by libsensors and the lm_sensors tool suite. It systematically searches for:\n\n1. Sensors embedded in CPUs, south bridges, and memory controllers\n2. Sensors embedded in Super I/O chips\n3. Hardware monitoring chips accessed through ISA I/O ports\n4. Hardware monitoring chips reachable over SMBus or I2C buses\n\nAfter detection, sensors-detect recommends which kernel modules need to be loaded and can optionally add them to **/etc/modules** for automatic loading at boot. The detected sensors can then be read using the **sensors** command.\n\nThe program typically skips the last two detection steps if a Super I/O chip with complete hardware monitoring features is found, though users can request full detection if desired."],["CAVEATS","sensors-detect must access hardware directly during detection, which could potentially cause problems ranging from SMBus lockup to permanent hardware damage in rare cases. The authors have made detection as safe as possible, but it cannot be guaranteed safe for all systems. Avoid running on production systems without understanding the risks. The **--auto** mode may trigger potentially dangerous probes and should be used with caution."],["HISTORY","**sensors-detect** is part of the **lm_sensors** (Linux monitoring sensors) project, which was started in **1998** to provide tools and kernel drivers for hardware monitoring on Linux. The project was originally hosted on lm-sensors.org and later moved to GitHub. It has become the standard tool for detecting and configuring hardware sensors on Linux systems, supporting a wide range of motherboard chipsets, CPU thermal sensors, and dedicated monitoring chips."],["INSTALL","```apt: sudo apt install lm-sensors```\n\n```apk: sudo apk add lm-sensors-detect```\n\n```brew: brew install lm-sensors```"],["SEE ALSO","[sensors](/man/sensors)(1), [fancontrol](/man/fancontrol)(8), [modprobe](/man/modprobe)(8), [lshw](/man/lshw)(1)"]],"e":["sensors","fancontrol","modprobe","lshw"],"x":"sudo sensors-detect"},"smbcacls":{"n":"smbcacls","t":"Manage Windows ACLs on SMB file shares","s":[["TAGLINE","Manage Windows ACLs on SMB file shares"],["TLDR","**Display ACLs** for a file on SMB share\n\n```smbcacls //[server]/[share] [path/to/file] --user [domain\\\\username]%[password]```\n\n**Set** a new ACL\n\n```smbcacls //[server]/[share] [path/to/file] --user [username]%[password] \"ACL:[DACL]\"```\n\n**Reset** ACLs and set new ones\n\n```smbcacls //[server]/[share] [path/to/file] --user [username]%[password] \"RESET\" \"ACL:[DACL]\"```\n\n**Change owner** of file\n\n```smbcacls //[server]/[share] [path/to/file] --chown [domain\\\\user] --user [username]```\n\n**Recursive** ACL operation\n\n```smbcacls //[server]/[share] [path/to/dir] --recurse --user [username]```"],["SYNOPSIS","**smbcacls** _//server/share_ _filename_ [_OPTIONS_]"],["PARAMETERS","**-a, --add** _acl_\n> Add ACE entries without modifying existing ones\n\n**-M, --modify** _acl_\n> Modify permission masks for specified ACEs\n\n**-D, --delete** _acl_\n> Delete specified ACE entries\n\n**-S, --set** _acl_\n> Replace entire ACL with new entries\n\n**-C, --chown** _name_\n> Change file/directory owner\n\n**-G, --chgrp** _name_\n> Change group ownership\n\n**-I, --inherit** _allow|remove|copy_\n> Enable, remove, or copy inherited permissions\n\n**--recurse**\n> Apply operations recursively to directories\n\n**--numeric**\n> Display ACLs in numeric format\n\n**--sddl**\n> Use SDDL format for input/output\n\n**--save** _file_\n> Save DACLs to file for later restoration\n\n**--restore** _file_\n> Restore DACLs from saved file\n\n**-U, --user** _username_\n> SMB username (format: user%password or domain\\\\user%password)\n\n**-W, --workgroup** _domain_\n> Set SMB workgroup/domain\n\n**-d, --debuglevel** _level_\n> Debug verbosity (0-10)"],["DESCRIPTION","**smbcacls** manipulates Windows NT Access Control Lists (ACLs) on SMB/CIFS file shares. It allows viewing, modifying, and managing file permissions on remote Windows or Samba shares from the Linux command line.\n\nAn ACL consists of Access Control Entries (ACEs) that define permissions for specific users or groups. The tool supports standard Windows permission operations including ownership changes and inheritance management."],["CAVEATS","Requires appropriate permissions on the share to modify ACLs. ACL format follows Windows SDDL (Security Descriptor Definition Language) conventions. Some operations require administrative privileges on the target share. Kerberos authentication may be needed for domain environments."],["HISTORY","**smbcacls** is part of the **Samba** suite, originally developed by **Andrew Tridgell** starting in **1992**. Samba provides Windows interoperability for Unix/Linux systems. The smbcacls utility enables Unix administrators to manage Windows-style permissions without requiring a Windows workstation."],["INSTALL","```apt: sudo apt install smbclient```\n\n```dnf: sudo dnf install samba-client```\n\n```pacman: sudo pacman -S smbclient```\n\n```apk: sudo apk add samba-client```\n\n```zypper: sudo zypper install samba-client```"],["SEE ALSO","[smbclient](/man/smbclient)(1), [mount.cifs](/man/mount.cifs)(8), [getfacl](/man/getfacl)(1), [setfacl](/man/setfacl)(1)"]],"e":["smbclient","mount.cifs","getfacl","setfacl"],"x":"smbcacls //[server]/[share] [path/to/file] --user [domain\\\\username]%[password]"},"synoservice":{"n":"synoservice","t":"Synology NAS service management","s":[["TAGLINE","Synology NAS service management"],["TLDR","**List all services**\n\n```synoservicecfg --list```\n\n**Check service status**\n\n```synoservice --status [servicename]```\n\n**Start a service**\n\n```synoservicecfg --start [servicename]```\n\n**Stop a service**\n\n```synoservicecfg --stop [servicename]```\n\n**Force stop a service**\n\n```synoservicecfg --hard-stop [servicename]```\n\n**Restart a service**\n\n```synoservice --restart [servicename]```\n\n**Restart a package service**\n\n```synoservicectl --restart [pkgctl-WebStation]```"],["SYNOPSIS","**synoservice** {--status|--restart} [_service_]\n\n**synoservicecfg** {--start|--stop|--hard-start|--hard-stop|--list} [_service_]\n\n**synoservicectl** {--start|--stop|--restart|--status} [_service_]"],["PARAMETERS","**--status**\n> Display status of specified service.\n\n**--start**\n> Start the specified service.\n\n**--stop**\n> Stop the specified service.\n\n**--restart**\n> Restart the specified service.\n\n**--hard-start**\n> Force start the service.\n\n**--hard-stop**\n> Force stop the service.\n\n**--list**\n> List all available services."],["DESCRIPTION","**synoservice** and related commands (**synoservicecfg**, **synoservicectl**) are Synology NAS utilities for managing system and package services from the command line via SSH.\n\nThese commands control DSM (DiskStation Manager) services including web server, Samba file sharing, SSH, FTP, Docker, and third-party packages. They are located in /usr/syno/sbin/ and require root privileges.\n\nCommon services include DSM, nginx, samba, ssh-shell, nfsd, ftpd, crond, and package services prefixed with pkgctl- such as pkgctl-Docker and pkgctl-WebStation.\n\nIn DSM 7 and newer, the standard **systemctl** command can also be used for service management."],["CAVEATS","Requires SSH access and root privileges (use sudo -i). Commands may differ slightly between DSM versions. Package services use the pkgctl- prefix. Some critical services should not be stopped remotely. On DSM 7+, consider using systemctl as an alternative."],["HISTORY","**synoservice** utilities are Synology-proprietary tools developed for their DiskStation Manager operating system. They provide a consistent interface for managing services across different DSM versions and hardware platforms. DSM 7 introduced better systemd integration alongside these legacy tools."],["SEE ALSO","[systemctl](/man/systemctl)(1), [service](/man/service)(8)"]],"e":["systemctl","service"],"x":"synoservicecfg --list"},"topydo":{"n":"topydo","t":"Feature-rich todo.txt task manager","s":[["TAGLINE","Feature-rich todo.txt task manager"],["TLDR","**List tasks**\n\n```topydo ls```\n\n**Add task**\n\n```topydo add \"[task description]\"```\n\n**Complete task**\n\n```topydo do [number]```\n\n**Set priority**\n\n```topydo pri [number] [A]```\n\n**Postpone task**\n\n```topydo postpone [number] [1w]```\n\n**Interactive mode**\n\n```topydo prompt```"],["SYNOPSIS","**topydo** _command_ [_options_] [_args_]"],["PARAMETERS","**ls**\n> List tasks.\n\n**add**\n> Add task.\n\n**do**\n> Complete task.\n\n**pri**\n> Set priority.\n\n**postpone**\n> Delay task.\n\n**prompt**\n> Interactive mode."],["DESCRIPTION","**topydo** is a feature-rich command-line todo list manager that uses the todo.txt format while adding capabilities like recurring tasks, relative date handling, and task dependencies. It maintains full compatibility with other todo.txt tools and clients.\n\nThe interactive **prompt** mode provides a continuous session for managing tasks without restarting the tool. Tasks can be postponed with relative date expressions like \"1w\" for one week, and recurring tasks automatically create new instances when completed."],["CAVEATS","Python required. todo.txt format. Configuration recommended."],["HISTORY","**topydo** was created as a feature-rich, Python-based todo.txt client with recurring task support."],["INSTALL","```brew: brew install topydo```\n\n```nix: nix profile install nixpkgs#topydo```"],["SEE ALSO","[todo.sh](/man/todo.sh)(1), [task](/man/task)(1), [todoman](/man/todoman)(1)"]],"e":["todo.sh","task","todoman"],"x":"topydo ls"},"tsh":{"n":"tsh","t":"Teleport unified infrastructure access client","s":[["TAGLINE","Teleport unified infrastructure access client"],["TLDR","**Log in to Teleport cluster**\n\n```tsh login --proxy=[teleport.example.com] --user=[username]```\n\n**List available SSH servers**\n\n```tsh ls```\n\n**SSH to a server**\n\n```tsh ssh [user]@[hostname]```\n\n**List Kubernetes clusters**\n\n```tsh kube ls```\n\n**Connect to Kubernetes cluster**\n\n```tsh kube login [cluster-name]```\n\n**List available databases**\n\n```tsh db ls```\n\n**Connect to a database**\n\n```tsh db connect [database-name]```\n\n**List active sessions**\n\n```tsh sessions ls```\n\n**Log out from cluster**\n\n```tsh logout```"],["SYNOPSIS","**tsh** [_global-options_] _command_ [_command-options_] [_args_...]"],["COMMANDS","**login**\n> Authenticate to Teleport cluster.\n\n**logout**\n> End session and remove credentials.\n\n**status**\n> Show current login status.\n\n**ls**\n> List available SSH servers.\n\n**ssh** _[user@]host_\n> Start SSH session to a server.\n\n**scp** _src_ _dest_\n> Secure copy files.\n\n**join** _session-id_\n> Join an active session.\n\n**play** _session-id_\n> Replay a recorded session.\n\n**sessions ls**\n> List active sessions.\n\n**kube** ls|login|credentials\n> Manage Kubernetes access.\n\n**db** ls|login|connect|logout\n> Manage database access.\n\n**apps** ls|login|logout\n> Manage application access.\n\n**request** create|ls|show|review\n> Manage access requests.\n\n**config**\n> Generate SSH config for native SSH client.\n\n**version**\n> Show version information."],["PARAMETERS","**--proxy** _address_\n> Teleport proxy service address.\n\n**--user** _username_\n> Teleport username.\n\n**--login** _os-user_\n> Remote OS username for SSH.\n\n**--ttl** _duration_\n> Session credential TTL. Default: 12h.\n\n**--identity** _file_\n> Path to identity file.\n\n**--auth** _connector_\n> Authentication connector name.\n\n**--insecure**\n> Skip TLS certificate verification.\n\n**--debug**\n> Enable verbose debug logging.\n\n**--jumphost**, **-J** _host_\n> SSH jump host.\n\n**--cert-format** _format_\n> Certificate format: standard or kubernetes.\n\n**--skip-version-check**\n> Skip client/server version compatibility check."],["DESCRIPTION","**tsh** is the Teleport client CLI for accessing infrastructure protected by Teleport. It provides unified access to SSH servers, Kubernetes clusters, databases, and web applications with built-in audit logging and access controls.\n\nAuthentication via **login** stores credentials in **~/.tsh** with automatic expiration (default 12 hours). Certificates support short-lived access aligned with zero-trust principles.\n\nFor SSH access, use **ssh** command or generate native SSH config with **tsh config** for use with standard ssh clients. Session recording enables **play** for audit review.\n\nKubernetes access with **kube login** configures kubectl credentials. Database access via **db connect** provides authenticated connections to PostgreSQL, MySQL, MongoDB, and other databases.\n\nAccess requests enable just-in-time privilege escalation with approval workflows. Create requests with **request create** for resources requiring elevated permissions."],["CAVEATS","Requires Teleport cluster access configured by administrator. Some resources require role-based access or approval. Certificate TTL limits session duration. The --insecure flag should not be used in production."],["HISTORY","Teleport was created by **Gravitational** (now Teleport) in **2016** to provide secure access to infrastructure with audit logging and identity-based access controls. The project implements certificate-based authentication following BeyondCorp/zero-trust principles. Teleport open-sourced the core product and has since expanded to cover Kubernetes, databases, and applications."],["INSTALL","```aur: yay -S tsh```"],["SEE ALSO","[ssh](/man/ssh)(1), [kubectl](/man/kubectl)(1)"]],"e":["ssh","kubectl"],"x":"tsh login --proxy=[teleport.example.com] --user=[username]"},"uuidd":{"n":"uuidd","t":"UUID generation daemon service","s":[["TAGLINE","UUID generation daemon service"],["TLDR","**Run uuidd** as a daemon\n\n```uuidd -d```\n\n**Request** a random UUID from the running daemon\n\n```uuidd -r```\n\n**Request bulk** random UUIDs from the running daemon\n\n```uuidd -r -n [number_of_uuids]```\n\n**Request** a time-based UUID from the running daemon\n\n```uuidd -t```\n\n**Kill** a running uuidd daemon\n\n```uuidd -k```\n\n**Run** with a timeout, exiting after inactivity\n\n```uuidd -d -T [seconds]```"],["SYNOPSIS","**uuidd** [_OPTIONS_]"],["PARAMETERS","**-d, --debug**\n> Run in debugging mode (prevents daemonizing)\n\n**-F, --no-fork**\n> Do not daemonize using a double-fork\n\n**-k, --kill**\n> Kill a currently running uuidd daemon\n\n**-n, --uuids** _N_\n> Request bulk response of N UUIDs\n\n**-P, --no-pid**\n> Do not create a pid file\n\n**-p, --pid** _FILE_\n> Specify pathname for the pid file\n\n**-q, --quiet**\n> Suppress some failure messages\n\n**-r, --random**\n> Test by requesting a random-based UUID\n\n**-s, --socket** _PATH_\n> Use specified pathname for the unix-domain socket\n\n**-S, --socket-activation**\n> Expect socket from calling process (implies --no-fork and --no-pid)\n\n**-t, --time**\n> Test by requesting a time-based UUID\n\n**-T, --timeout** _N_\n> Exit after N seconds of inactivity\n\n**-C, --cont-clock** [_N_]\n> Activate continuous clock handling for time-based UUIDs; default offset is 2 hours"],["DESCRIPTION","**uuidd** is a daemon used by the UUID library to generate universally unique identifiers (UUIDs), especially time-based UUIDs, in a secure and guaranteed-unique fashion even when many threads on different CPUs request UUIDs simultaneously.\n\nThe daemon can generate both random (version 4) and time-based (version 1) UUIDs. Part of the **util-linux** package."],["CAVEATS","Time-based UUIDs may expose MAC address information. Running as a daemon requires appropriate permissions. The -r and -t flags are test/request operations that connect to a running daemon rather than generating UUIDs directly."],["INSTALL","```apt: sudo apt install uuid-runtime```\n\n```dnf: sudo dnf install uuidd```\n\n```zypper: sudo zypper install uuidd```"],["SEE ALSO","[uuid](/man/uuid)(1), [uuidgen](/man/uuidgen)(1), [uuidparse](/man/uuidparse)(1)"]],"e":["uuid","uuidgen","uuidparse"],"x":"uuidd -d"},"vagrant":{"n":"vagrant","t":"Manage portable virtual development environments","s":[["TAGLINE","Manage portable virtual development environments"],["TLDR","**Initialize a new Vagrantfile with a base box**\n\n```vagrant init [ubuntu/jammy64]```\n\n**Start and provision VM**\n\n```vagrant up```\n\n**SSH into a running VM**\n\n```vagrant ssh```\n\n**Stop a running VM**\n\n```vagrant halt```\n\n**Destroy VM and remove all resources**\n\n```vagrant destroy```\n\n**Show status of all VMs**\n\n```vagrant status```\n\n**Suspend VM** (save state to disk)\n\n```vagrant suspend```\n\n**Show all Vagrant VMs across the system**\n\n```vagrant global-status```"],["SYNOPSIS","**vagrant** _command_ [_options_]"],["PARAMETERS","**init** [_BOX_]\n> Initialize a new Vagrantfile, optionally with a specified base box.\n\n**up** [_NAME_]\n> Start and provision the VM. Optionally specify a machine name in multi-machine setups.\n\n**ssh** [_NAME_]\n> SSH into a running VM.\n\n**halt** [_NAME_]\n> Gracefully stop a running VM.\n\n**destroy** [_NAME_]\n> Remove a VM and all its resources.\n\n**reload** [_NAME_]\n> Restart a VM and reload Vagrantfile configuration.\n\n**suspend** [_NAME_]\n> Pause a running VM, saving its current state.\n\n**resume** [_NAME_]\n> Resume a previously suspended VM.\n\n**provision** [_NAME_]\n> Run configured provisioners on a running VM.\n\n**status** [_NAME_]\n> Show the status of VMs in the current environment.\n\n**global-status**\n> Show the status of all Vagrant VMs across the system.\n\n**snapshot push**\n> Save a snapshot of the current VM state.\n\n**snapshot pop**\n> Restore the most recent snapshot.\n\n**validate**\n> Check the Vagrantfile for syntax errors.\n\n**package**\n> Package a running VM into a reusable box.\n\n**plugin install** _NAME_\n> Install a Vagrant plugin.\n\n**box add** _BOX_\n> Download and add a box image.\n\n**box list**\n> List locally available boxes.\n\n**box remove** _BOX_\n> Remove a locally stored box.\n\n**--provider** _NAME_\n> Specify the provider (virtualbox, vmware, etc.).\n\n**-f**, **--force**\n> Force the operation without confirmation.\n\n**-h**, **--help**\n> Display help information for any command."],["DESCRIPTION","**vagrant** manages virtual development environments. It automates VM creation, configuration, and provisioning.\n\nVagrantfile defines the VM configuration. It specifies the base box, network settings, shared folders, and provisioning scripts.\n\nBoxes are base images. Many are available from Vagrant Cloud. Custom boxes can be created and shared.\n\nProviders handle virtualization. VirtualBox is default, with support for VMware, Hyper-V, Docker, and cloud providers.\n\nProvisioners configure VMs after boot. Shell scripts, Ansible, Puppet, and Chef are supported.\n\nMulti-machine setups define several VMs in one Vagrantfile. They can model complex environments like clusters."],["CAVEATS","Requires virtualization software. Large boxes need download time and disk space. Provider-specific features may vary."],["HISTORY","**Vagrant** was created by **Mitchell Hashimoto** in **2010** and later developed by **HashiCorp**. It revolutionized development environment management, enabling reproducible setups."],["INSTALL","```apt: sudo apt install vagrant```\n\n```dnf: sudo dnf install vagrant```\n\n```nix: nix profile install nixpkgs#vagrant```"],["SEE ALSO","[vboxmanage](/man/vboxmanage)(1), [docker](/man/docker)(1), [ansible](/man/ansible)(1), [packer](/man/packer)(1), [ssh](/man/ssh)(1)"]],"e":["vboxmanage","docker","ansible","packer","ssh"],"x":"vagrant init [ubuntu/jammy64]"},"vboxmanage-export":{"n":"vboxmanage-export","t":"Export VMs to OVF/OVA format","s":[["TAGLINE","Export VMs to OVF/OVA format"],["TLDR","**Export a VM to an OVA file**\n\n```vboxmanage export [vmname] -o [file.ova]```\n\n**Export a VM with a manifest file for integrity verification**\n\n```vboxmanage export [vmname] -o [file.ova] --manifest```\n\n**Export multiple VMs into a single appliance**\n\n```vboxmanage export [vm1] [vm2] -o [file.ova]```\n\n**Export using OVF 2.0 format**\n\n```vboxmanage export [vmname] -o [file.ovf] --ovf20```\n\n**Export with custom product and vendor metadata**\n\n```vboxmanage export [vmname] -o [file.ova] --vsys 0 --product [product_name] --vendor [vendor_name]```"],["SYNOPSIS","**vboxmanage** **export** _vmname_... **-o** _file_ [_options_]"],["PARAMETERS","**-o**, **--output** _file_\n> Output file. Use .ova for a single-file archive or .ovf for multiple files.\n\n**--legacy09**\n> Export in OVF 0.9 legacy format.\n\n**--ovf10**\n> Export in OVF 1.0 format (default).\n\n**--ovf20**\n> Export in OVF 2.0 format.\n\n**--manifest**\n> Include a manifest file with SHA digests for integrity verification.\n\n**--options** _option_[,...]\n> Comma-separated export options: manifest, iso, nomacs, nomacsbutnat.\n\n**--vsys** _n_\n> Specify virtual system number for per-VM settings (starting at 0).\n\n**--vmname** _name_\n> Override the VM name in the exported appliance.\n\n**--product** _name_\n> Set product name in appliance metadata.\n\n**--producturl** _url_\n> Set product URL in appliance metadata.\n\n**--vendor** _name_\n> Set vendor name in appliance metadata.\n\n**--vendorurl** _url_\n> Set vendor URL in appliance metadata.\n\n**--version** _string_\n> Set version string in appliance metadata.\n\n**--description** _text_\n> Set description in appliance metadata.\n\n**--eula** _text_\n> Embed end-user license agreement text.\n\n**--eulafile** _filename_\n> Embed EULA from a file."],["DESCRIPTION","**vboxmanage export** exports one or more virtual machines to the Open Virtualization Format (OVF/OVA). This creates portable appliance packages suitable for sharing, backup, or migration to other virtualization platforms. OVA is a single-file tar archive containing all appliance files; OVF produces a directory with separate descriptor, disk, and manifest files. Per-VM metadata options (product, vendor, description, etc.) require **--vsys** to specify which VM they apply to."],["SEE ALSO","[vboxmanage](/man/vboxmanage)(1), [vboxmanage-import](/man/vboxmanage-import)(1), [vboxmanage-clonevm](/man/vboxmanage-clonevm)(1)"]],"e":["vboxmanage","vboxmanage-import","vboxmanage-clonevm"],"x":"vboxmanage export [vmname] -o [file.ova]"},"wget2":{"n":"wget2","t":"Next-generation recursive metalink/file/website downloader","s":[["TAGLINE","Next-generation recursive metalink/file/website downloader"],["TLDR","**Download a file**\n\n```wget2 [https://example.com/file.zip]```\n\n**Download to a specific filename**\n\n```wget2 -O [output.zip] [url]```\n\n**Continue a partial download**\n\n```wget2 -c [url]```\n\n**Download recursively**\n\n```wget2 -r [https://example.com]```\n\n**Mirror a website**\n\n```wget2 -m [https://example.com]```\n\n**Download with chunked parallel transfer**\n\n```wget2 --chunk-size=[1M] [url]```\n\n**Limit download rate**\n\n```wget2 --limit-rate=[1M] [url]```\n\n**Download with specific compression support**\n\n```wget2 --compression=[gzip,br,zstd] [url]```"],["SYNOPSIS","**wget2** [_options_] [_urls_]"],["PARAMETERS","**-O** _FILE_\n> Save to specified output filename.\n\n**-c**, **--continue**\n> Resume a partially downloaded file.\n\n**-r**, **--recursive**\n> Recursive download.\n\n**-m**, **--mirror**\n> Mirror a website (shortcut for -r -N -l inf --no-remove-listing).\n\n**--chunk-size** _SIZE_\n> Download large files in multithreaded chunks of the given size.\n\n**--limit-rate** _RATE_\n> Limit bandwidth to the specified rate (e.g., 1M).\n\n**--http2**\n> Force HTTP/2 protocol.\n\n**--http2-request-window** _NUM_\n> Set max number of parallel streams per HTTP/2 connection (default: 30).\n\n**--compression** _TYPE_\n> Set accepted compression types (identity, gzip, deflate, br, zstd, lzip, etc.).\n\n**-q**, **--quiet**\n> Quiet mode, suppress output.\n\n**-P** _DIR_\n> Save files to specified directory prefix.\n\n**--no-clobber**\n> Do not overwrite existing files or truncate partial files.\n\n**-d**, **--debug**\n> Print debug output."],["DESCRIPTION","**wget2** is the next-generation version of GNU Wget, rebuilt to support modern protocols and parallel downloading. It adds native HTTP/2 support with multiplexed streams, enabling more efficient communication with servers that support the protocol.\n\nParallel downloading fetches multiple files simultaneously over reused connections, significantly speeding up batch downloads compared to the sequential approach of the original wget. The **--chunk-size** option splits large single-file downloads into parallel chunks. Recursive downloading and website mirroring are supported with the same familiar -r and -m flags.\n\nwget2 also supports Metalink files for mirror-aware downloads with automatic integrity checking, compression negotiation, and modern TLS. The tool maintains command-line compatibility with wget for basic operations while improving performance through its modernized network stack."],["CAVEATS","Not all original wget options are supported. Some behavioral differences exist compared to wget. The --chunk-size feature works best with servers that support HTTP range requests."],["HISTORY","**wget2** was developed by Tim Ruehsen as a modern rewrite of GNU Wget. Development began around 2012, with version 2.0.0 released in 2021. It adds HTTP/2, parallel downloading, and Metalink support while maintaining backward compatibility."],["INSTALL","```apt: sudo apt install wget2```\n\n```dnf: sudo dnf install wget2```\n\n```apk: sudo apk add wget2```\n\n```zypper: sudo zypper install wget2```\n\n```brew: brew install wget2```\n\n```nix: nix profile install nixpkgs#wget2```"],["SEE ALSO","[wget](/man/wget)(1), [curl](/man/curl)(1), [aria2c](/man/aria2c)(1)"]],"e":["wget","curl","aria2c"],"x":"wget2 [https://example.com/file.zip]"},"wikit":{"n":"wikit","t":"Wikipedia summaries from the terminal","s":[["TAGLINE","Wikipedia summaries from the terminal"],["TLDR","**Search Wikipedia**\n\n```wikit [topic]```\n\n**Multiple word query**\n\n```wikit \"[linux kernel]\"```\n\n**Specific language**\n\n```wikit -l [es] [topic]```\n\n**Open in browser**\n\n```wikit -b [topic]```\n\n**Show full article**\n\n```wikit -a [topic]```\n\n**Disable line breaks**\n\n```wikit -n [topic]```"],["SYNOPSIS","**wikit** [_-l lang_] [_-b_] [_-a_] [_options_] _query_"],["PARAMETERS","**-l**, **--lang** _LANG_\n> Wikipedia language.\n\n**-b**, **--browser**\n> Open in browser.\n\n**-a**, **--all**\n> Full article.\n\n**-n**, **--line**\n> No line breaks.\n\n**-d**, **--disambig**\n> Show disambiguation."],["DESCRIPTION","**wikit** is a command-line tool that fetches Wikipedia article summaries directly in the terminal. By default it displays the first paragraph of the matching article, providing a quick overview of any topic without leaving the command line.\n\nThe full article mode displays extended content beyond the opening summary. Language selection allows querying any of Wikipedia's language editions, and browser mode opens the complete article in a web browser for full reading. When a query matches multiple articles, disambiguation mode lists all possible meanings so you can refine your search."],["CAVEATS","Requires internet. Wikipedia API limits. Not all languages complete."],["HISTORY","**wikit** was created for quick Wikipedia lookups from the terminal. It provides instant access to encyclopedia knowledge."],["INSTALL","```nix: nix profile install nixpkgs#wikit```"],["SEE ALSO","[curl](/man/curl)(1), [w3m](/man/w3m)(1)"]],"e":["curl","w3m"],"x":"wikit [topic]"},"winetricks":{"n":"winetricks","t":"Install Windows runtime libraries for Wine","s":[["TAGLINE","Install Windows runtime libraries for Wine"],["TLDR","Start a **graphical setup** at the default Wine location\n\n```winetricks```\n\nSpecify a **custom Wine directory**\n\n```WINEPREFIX=[path/to/wine_directory] winetricks```\n\nInstall a Windows **DLL or component**\n\n```winetricks [package]```"],["SYNOPSIS","**winetricks** [_options_] [_verb_...]"],["PARAMETERS","_verb_\n> Package, font, or setting to install/configure\n\n**--gui**\n> Start graphical interface\n\n**--force**\n> Force installation even if already installed\n\n**--unattended**\n> Don't show prompts during installation\n\n**--no-isolate**\n> Don't install in isolated environment\n\n**--verify**\n> Verify installation after completion\n\n**list**\n> List available verbs\n\n**list-download**\n> List packages that can be downloaded\n\n**apps** / **benchmarks** / **dlls** / **fonts** / **games** / **settings**\n> List verbs in specific category\n\n**WINEPREFIX=**_path_\n> Environment variable to specify Wine prefix"],["DESCRIPTION","**winetricks** is a helper script to download and install various redistributable runtime libraries and settings needed to run Windows programs under Wine. It automates the installation of components like DirectX, Visual C++ runtimes, fonts, and .NET Framework.\n\nThe tool provides both a graphical interface and command-line operation. It can install multiple components at once and handles dependencies automatically. Each installation can be done in a specific Wine prefix."],["CAVEATS","Some components require accepting license agreements even in unattended mode. Downloaded files are cached in ~/.cache/winetricks. Some verbs require an internet connection to download components from Microsoft or other sources."],["HISTORY","Created by **Dan Kegel** and released in **2007** as a shell script to automate common Wine setup tasks. Became an essential companion tool for Wine users, reducing the complexity of configuring Windows applications. Now maintained by the Wine community."],["INSTALL","```dnf: sudo dnf install winetricks```\n\n```pacman: sudo pacman -S winetricks```\n\n```zypper: sudo zypper install winetricks```\n\n```brew: brew install winetricks```\n\n```nix: nix profile install nixpkgs#winetricks```"],["SEE ALSO","[wine](/man/wine)(1), [winecfg](/man/winecfg)(1)"]],"e":["wine","winecfg"],"x":"winetricks"},"wyzerc":{"n":"wyzerc","t":"Wyzer programming language compiler and runner","s":[["TAGLINE","Wyzer programming language compiler and runner"],["TLDR","**Build** the compiler from a source checkout (OCaml / Dune)\n\n```dune build```\n\n**Run** a Wyzer program (interpreter path)\n\n```wyzerc run [file.wyz]```\n\n**Build** a program to a native binary via LLVM + clang\n\n```wyzerc build [file.wyz]```\n\n**Project** a choreography to a specific role, then run\n\n```wyzerc run [file.wyz] --role [RoleName]```\n\n**Build** for a specific role (outputs _base_Role.ll_ and a linked binary)\n\n```wyzerc build [file.wyz] --role [RoleName]```"],["SYNOPSIS","**wyzerc** **run**|**build** _file.wyz_ [**--role** _RoleName_]"],["DESCRIPTION","**wyzerc** is the compiler front end for **Wyzer**, a statically typed, resource-oriented programming language with Perceus-style memory management and choreographic programming for distributed safety. Source files use the **.wyz** extension.\n\nInvoked as **wyzerc run** _file.wyz_, the tool parses and typechecks the program, applies Perceus transformation, and evaluates it in the built-in OCaml interpreter. **wyzerc build** takes the same front-end path, then generates LLVM IR (**_base_Role.ll_**) and invokes **clang** with the Wyzer runtime (**lib/wyzer_runtime.c**) to produce a native binary named **_base_Role_** (default role **Poly** when **--role** is omitted).\n\nOptional **--role** selects a choreographic projection so multi-party programs can be specialized per participant. The project is early research; several language and distributed features are still unfinished."],["PARAMETERS","**run** _file.wyz_\n> Typecheck, transform, and execute _file.wyz_ with the interpreter.\n\n**build** _file.wyz_\n> Typecheck, transform, emit LLVM IR, and compile with **clang -O3** plus the Wyzer C runtime.\n\n**--role** _RoleName_\n> Project the program to _RoleName_ before evaluation or codegen. Default role for build is **Poly**."],["CAVEATS","Requires an OCaml toolchain (Dune, Menhir, LLVM bindings, and related opam packages) to build **wyzerc** itself; **build** mode additionally needs **clang** and the in-tree **lib/wyzer_runtime.c**. There is no polished package-manager install path yet—typical workflow is clone, **opam** deps, and **dune build** / **dune exec wyzerc**. Older docs sometimes show **dune exec wyzerc -- file.wyz** without a **run**/**build** subcommand; current usage requires **run** or **build** as the first argument. Project status is experimental research, not production-ready."],["HISTORY","**Wyzer** is a research language exploring one ownership rule for memory, concurrency, and network protocols, combining Perceus reference counting with choreographic programming. The **wyzerc** binary is the public Dune executable name for the compiler in the upstream repository."],["SEE ALSO","[clang](/man/clang)(1), [dune](/man/dune)(1), [opam](/man/opam)(1)"],["RESOURCES","```[Source code](https://github.com/Wyzer-Lang/wyzer)```\n\n```[Documentation](https://github.com/Wyzer-Lang/wyzer/tree/master/docs)```"]],"e":["clang","dune","opam"],"x":"dune build"}});
