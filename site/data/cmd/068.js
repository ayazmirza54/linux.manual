window.LCL_BUCKET(68,{"2to3":{"n":"2to3","t":"automated Python 2 to Python 3 code converter","s":[["TAGLINE","automated Python 2 to Python 3 code converter"],["TLDR","**Show changes without applying**\n\n```2to3 [script.py]```\n\n**Convert file in place**\n\n```2to3 -w [script.py]```\n\n**Convert entire directory**\n\n```2to3 -w [directory/]```\n\n**Apply specific fixers only**\n\n```2to3 -f [print] -f [import] [script.py]```\n\n**List available fixers**\n\n```2to3 -l```\n\n**Convert in place without creating .bak files**\n\n```2to3 -w -n [script.py]```\n\n**Write converted files to a different directory**\n\n```2to3 -w -n -o [out_dir/] [script.py]```"],["SYNOPSIS","**2to3** [_options_] _file_|_directory_"],["PARAMETERS","**-w**, **--write**\n> Write changes to files\n\n**-n**, **--nobackups**\n> Don't create .bak backup files\n\n**-f** _fixer_, **--fix** _fixer_\n> Apply specific fixer\n\n**-x** _fixer_, **--nofix** _fixer_\n> Exclude specific fixer\n\n**-l**, **--list-fixes**\n> List available fixers\n\n**-p**, **--print-function**\n> Treat **print** as a function (skip the print fixer). Useful when the file already uses **from __future__ import print_function**.\n\n**-d**, **--doctests_only**\n> Only fix doctests inside the file.\n\n**-W**, **--write-unchanged-files**\n> Write output even for unchanged files (useful with **-o**).\n\n**--add-suffix** _SUFFIX_\n> Append this suffix to output filenames instead of overwriting.\n\n**-j** _n_, **--processes** _n_\n> Run in parallel with n processes\n\n**-o** _dir_, **--output-dir** _dir_\n> Write converted files to directory\n\n**-v**, **--verbose**\n> More verbose output"],["DESCRIPTION","**2to3** is an automated Python 2 to Python 3 code translator. It parses Python 2 source code and applies \"fixers\" that transform incompatible syntax and library calls to their Python 3 equivalents.\n\nCommon transformations include:\n- **print** statement to **print()** function\n- **unicode** to **str**, **str** to **bytes**\n- **raw_input()** to **input()**\n- Iterator methods (.iteritems() to .items())\n- **except Exception, e** to **except Exception as e**\n- Library renames (urllib, ConfigParser, etc.)\n\n2to3 handles most mechanical changes but may require manual review for semantic differences between Python 2 and 3."],["CAVEATS","2to3 makes syntactic changes but cannot handle all differences. Code behavior may still differ, especially around:\n- String/bytes handling\n- Integer division\n- Dictionary ordering (pre-Python 3.7)\n- Unicode text processing\n\nRunning **-w** without **-n** creates .bak files that may clutter version control.\n\nPython 2 reached end-of-life in January 2020. For major projects, consider modern porting tools like **python-modernize** or **futurize**."],["SEE ALSO","[python](/man/python)(1), [python3](/man/python3)(1), [pip](/man/pip)(1)"]],"e":["python","python3","pip"],"x":"2to3 [script.py]"},"MAKEDEV":{"n":"MAKEDEV","t":"creates device files in /dev","s":[["TAGLINE","creates device files in /dev"],["TLDR","**Create standard devices**\n\n```MAKEDEV std```\n\n**Create specific device**\n\n```MAKEDEV [sda]```\n\n**Create all devices**\n\n```MAKEDEV generic```\n\n**Create console devices**\n\n```MAKEDEV console```\n\n**Create tty devices**\n\n```MAKEDEV tty```"],["SYNOPSIS","**MAKEDEV** [_options_] _device_"],["PARAMETERS","_DEVICE_\n> Device name or group.\n\n**std**\n> Standard devices.\n\n**generic**\n> Generic device set.\n\n**console**\n> Console devices.\n\n**-d** _DIR_\n> Device directory.\n\n**--help**\n> Display help information."],["DESCRIPTION","**MAKEDEV** creates device files in /dev. It sets up device nodes with correct permissions and types.\n\nThe tool is largely obsolete with udev. Used in static /dev setups or initial ramdisks."],["CAVEATS","Obsolete with udev/devtmpfs. Requires root. Static device creation only."],["HISTORY","MAKEDEV was the traditional way to create device files before **udev** automated device management."],["SEE ALSO","[mknod](/man/mknod)(1), [udevadm](/man/udevadm)(8)"]],"e":["mknod","udevadm"],"x":"MAKEDEV std"},"act":{"n":"act","t":"local GitHub Actions runner","s":[["TAGLINE","local GitHub Actions runner"],["TLDR","**Run** the default workflow\n\n```act```\n\n**List** all available workflows\n\n```act -l```\n\nRun a **specific workflow**\n\n```act -W [.github/workflows/ci.yml]```\n\nRun a **specific job**\n\n```act -j [build]```\n\nRun with **secrets** from file\n\n```act --secret-file [.secrets]```\n\n**Dry run** to see what would be executed\n\n```act -n```\n\nRun with a specific **platform image**\n\n```act -P ubuntu-latest=catthehacker/ubuntu:act-latest```"],["SYNOPSIS","**act** [_-W workflow_] [_-j job_] [_-e event_] [_--secret-file file_] [_options_]"],["DESCRIPTION","**act** allows you to run GitHub Actions workflows locally using Docker containers. It parses workflow YAML files and executes jobs in containers that simulate the GitHub Actions environment, enabling rapid iteration and debugging without pushing to GitHub.\n\nThe tool supports most GitHub Actions features including matrix builds, secrets, environment variables, and artifacts. It downloads and caches action repositories automatically."],["PARAMETERS","**-W** _file_, **--workflows** _file_\n> Path to workflow file(s) (default: .github/workflows/)\n\n**-j** _job_, **--job** _job_\n> Run a specific job\n\n**-e** _file_, **--eventpath** _file_\n> Path to event JSON file\n\n**--secret-file** _file_\n> Load secrets from file (KEY=VALUE format)\n\n**-s** _secret_, **--secret** _secret_\n> Set a secret (KEY=VALUE)\n\n**--env-file** _file_\n> Load environment variables from file\n\n**-P** _platform_, **--platform** _platform_\n> Custom Docker image for platform (e.g., ubuntu-latest=catthehacker/ubuntu:act-latest)\n\n**--input** _input_\n> Set a workflow input (KEY=VALUE)\n\n**--input-file** _file_\n> Load workflow inputs from file\n\n**--matrix** _matrix_\n> Select specific matrix configuration to run\n\n**--action-offline-mode**\n> Don't pull images or fetch remote actions if already cached\n\n**-l**, **--list**\n> List available workflows and jobs\n\n**-n**, **--dryrun**\n> Show what would be run without executing\n\n**-r**, **--reuse**\n> Reuse containers between runs\n\n**-b**, **--bind**\n> Bind working directory instead of copying\n\n**--container-architecture** _arch_\n> Container architecture (amd64, arm64)"],["CONFIGURATION","**~/.actrc**\n> User-level default flags applied to every act invocation. Each line contains a single command-line flag (e.g., `-P ubuntu-latest=catthehacker/ubuntu:act-latest`).\n\n**.actrc**\n> Project-level defaults, same format as the user-level file. Overrides user-level settings when present."],["CAVEATS","Not all GitHub Actions features are supported; some hosted runner features like service containers have limitations. Large workflow images can be slow to download initially. Some actions may behave differently locally than on GitHub. Docker must be running."],["HISTORY","**act** was created by Casey Lee (nektos) and first released in **2019**. It was developed to address the slow feedback loop of GitHub Actions by enabling local execution. The project quickly gained popularity in the CI/CD community."],["INSTALL","```dnf: sudo dnf install act```\n\n```pacman: sudo pacman -S act```\n\n```apk: sudo apk add act```\n\n```zypper: sudo zypper install act```\n\n```brew: brew install act```\n\n```nix: nix profile install nixpkgs#act```"],["SEE ALSO","[docker](/man/docker)(1), [gh](/man/gh)(1), [make](/man/make)(1)"]],"e":["docker","gh","make"],"x":"act"},"adb-shell-pm":{"n":"adb-shell-pm","t":"Android package manager shell interface","s":[["TAGLINE","Android package manager shell interface"],["TLDR","**List** all installed packages\n\n```adb shell pm list packages```\n\nList only **system** packages\n\n```adb shell pm list packages -s```\n\nList only **third-party** packages\n\n```adb shell pm list packages -3```\n\n**Uninstall** a package\n\n```adb shell pm uninstall [com.example.app]```\n\n**Clear** app data and cache\n\n```adb shell pm clear [com.example.app]```\n\nGet **path** to APK\n\n```adb shell pm path [com.example.app]```"],["SYNOPSIS","**adb shell pm** _command_ [_options_]"],["DESCRIPTION","**pm** (Package Manager) is an Android shell command for managing installed applications. It provides functionality for listing, installing, uninstalling, and querying information about packages on the device.\n\nThis tool is more powerful than the standard adb install/uninstall commands, offering options to manage permissions, disable packages, and query detailed package information."],["PARAMETERS","**list packages** [_-f_] [_-d_] [_-e_] [_-s_] [_-3_] [_-i_] [_-u_] [_filter_]\n> List packages (-f: with APK path, -d: disabled, -e: enabled, -s: system, -3: third-party, -i: with installer, -u: include uninstalled)\n\n**path** _package_\n> Print path to the APK\n\n**install** [_-r_] [_-t_] [_-d_] _path_\n> Install package from device path\n\n**uninstall** [_-k_] _package_\n> Remove package (-k: keep data)\n\n**clear** _package_\n> Delete all data associated with package\n\n**enable** _package_\n> Enable a disabled package\n\n**disable** _package_\n> Disable a package\n\n**grant** _package_ _permission_\n> Grant a permission to a package\n\n**revoke** _package_ _permission_\n> Revoke a permission from a package\n\n**dump** _package_\n> Print package information"],["CAVEATS","Some operations require system or root privileges. Disabling system packages may cause instability. Package names are case-sensitive. Uninstalling system apps may require root."],["HISTORY","The package manager command has been part of Android since its initial release in **2008**, providing command-line access to the PackageManager system service."],["INSTALL","```apt: sudo apt install adb```\n\n```dnf: sudo dnf install android-tools```\n\n```pacman: sudo pacman -S android-tools```\n\n```apk: sudo apk add android-tools-adb```\n\n```zypper: sudo zypper install android-tools```\n\n```nix: nix profile install nixpkgs#android-tools```"],["SEE ALSO","[adb](/man/adb)(1), [adb-shell](/man/adb-shell)(1), [adb-shell-pm-list-packages](/man/adb-shell-pm-list-packages)(1), [adb-install](/man/adb-install)(1), [adb-uninstall](/man/adb-uninstall)(1)"],["RESOURCES","```[Source code](https://android.googlesource.com/platform/packages/modules/adb/)```\n\n```[Documentation](https://developer.android.com/tools/adb)```"]],"e":["adb","adb-shell","adb-shell-pm-list-packages","adb-install","adb-uninstall"],"x":"adb shell pm list packages"},"angular":{"n":"angular","t":"CLI for building Angular web applications","s":[["TAGLINE","CLI for building Angular web applications"],["TLDR","**Create a new Angular workspace** and application\n\n```ng new [project-name]```\n\n**Serve the application** with live reload\n\n```ng serve```\n\n**Serve on a specific port** and open browser\n\n```ng serve --port [4200] --open```\n\n**Generate a new component**\n\n```ng generate component [component-name]```\n\n**Generate a service**\n\n```ng generate service [service-name]```\n\n**Build the application** for production\n\n```ng build --configuration production```\n\n**Run unit tests**\n\n```ng test```\n\n**Run end-to-end tests**\n\n```ng e2e```"],["SYNOPSIS","**ng** _command_ [_options_]"],["PARAMETERS","**new** (alias: **n**)\n> Create a new Angular workspace and initial application\n\n**serve** (alias: **s**, **dev**)\n> Build and serve the application with live reload on file changes\n\n**generate** (alias: **g**)\n> Generate components, services, modules, pipes, directives, and other schematics\n\n**build** (alias: **b**)\n> Compile the application into the dist/ output directory\n\n**test** (alias: **t**)\n> Run unit tests using the configured test runner\n\n**e2e** (alias: **e**)\n> Build, serve, and run end-to-end tests\n\n**lint**\n> Run linting tools on the application code\n\n**add**\n> Add support for an external library (installs and configures packages)\n\n**update**\n> Update the workspace and its dependencies\n\n**deploy**\n> Invoke the deploy builder for the project\n\n**config**\n> Get or set configuration values in angular.json\n\n**cache**\n> Configure and manage the persistent disk cache\n\n**version** (alias: **v**)\n> Display Angular CLI version information\n\n**analytics**\n> Configure anonymous telemetry settings for the Angular CLI\n\n**completion**\n> Set up Angular CLI shell autocompletion\n\n**run**\n> Run an Architect target in the workspace (e.g. my-project:build)\n\n**extract-i18n**\n> Extract i18n messages from the source code\n\n**--help**\n> Display help for any command\n\n**--dry-run**\n> Preview changes without writing files\n\n**--verbose**\n> Enable verbose logging output\n\n**--force**\n> Force overwriting of existing files (generators)"],["DESCRIPTION","**ng** is the command-line interface for Angular, a platform for building web applications. It provides tools for creating projects, generating code, building, testing, and deploying Angular applications.\n\nThe CLI automates common development tasks through schematics - templates that generate boilerplate code for components, services, modules, and other Angular constructs. The **generate** command supports creating components, directives, pipes, services, classes, guards, interfaces, enums, and modules.\n\nProjects are configured through **angular.json**, which defines build targets, file paths, and environment-specific settings. The CLI supports multiple projects within a single workspace, enabling monorepo-style development.\n\nThe **serve** command provides a development server with hot module replacement, automatically rebuilding and refreshing the browser when source files change. Production builds use **build --configuration production** which enables optimizations like ahead-of-time compilation, tree shaking, and minification."],["CONFIGURATION","**angular.json**\n> Project workspace configuration defining build targets, file paths, and environment settings.\n\n**tsconfig.json**\n> TypeScript compiler configuration for the Angular project."],["CAVEATS","Requires Node.js and npm to be installed. Global installation via **npm install -g @angular/cli** provides the ng command system-wide, but projects can also use locally installed versions. Some commands like **deploy** require additional packages to be installed first using **ng add**."],["HISTORY","Angular CLI was introduced alongside **Angular 2** in **September 2016** to simplify project setup and development workflows. It replaced the manual configuration required for TypeScript compilation, module bundling, and build optimization. The CLI has evolved significantly, with major updates accompanying each Angular version, introducing features like differential loading, Ivy compilation, and standalone components."],["SEE ALSO","[npm](/man/npm)(1), [node](/man/node)(1), [webpack](/man/webpack)(1), [react-scripts](/man/react-scripts)(1), [vue](/man/vue)(1)"]],"e":["npm","node","webpack","react-scripts","vue"],"x":"ng new [project-name]"},"arch-wiki":{"n":"arch-wiki","t":"Search the Arch Linux Wiki from the terminal","s":[["TAGLINE","Search the Arch Linux Wiki from the terminal"],["TLDR","**Search the Arch Wiki**\n\n```arch-wiki [search_term]```\n\n**Search and open result in browser**\n\n```arch-wiki -o [search_term]```\n\n**Search in a specific language**\n\n```arch-wiki -l [de] [search_term]```\n\n**Show raw search results without formatting**\n\n```arch-wiki -r [search_term]```\n\n**Limit number of results**\n\n```arch-wiki -n [10] [search_term]```"],["SYNOPSIS","**arch-wiki** [_options_] _search_term_"],["PARAMETERS","**-o**, **--open**\n> Open the first result in the default web browser.\n\n**-l**, **--lang** _code_\n> Search in a specific language (e.g., de, es, fr, ja).\n\n**-n**, **--number** _count_\n> Limit the number of results returned.\n\n**-r**, **--raw**\n> Output raw results without color formatting.\n\n**-h**, **--help**\n> Display help information.\n\n**-v**, **--version**\n> Display version information."],["DESCRIPTION","**arch-wiki** is a command-line tool for searching the Arch Linux Wiki directly from the terminal. It queries the wiki's API and presents results with titles and URLs, enabling quick access to documentation without leaving the command line.\n\nThe Arch Wiki is renowned for its comprehensive, well-maintained documentation covering not just Arch Linux but general Linux topics. This tool provides rapid access to that knowledge base during system administration and troubleshooting tasks.\n\nResults are displayed with syntax highlighting by default, making them easy to scan. The **-o** flag opens the top result immediately, useful when you know exactly what you're looking for."],["CAVEATS","Requires an active internet connection. The tool queries the wiki API, so results depend on the wiki's search indexing. Some articles may have different names than expected; try alternative search terms if initial results are unhelpful."],["HISTORY","The **arch-wiki** tool was created to provide quick terminal access to the Arch Wiki, one of the most comprehensive Linux documentation resources. The Arch Wiki itself was established alongside Arch Linux in **2002** and has grown to include thousands of articles covering software configuration, troubleshooting, and best practices applicable across many Linux distributions."],["SEE ALSO","[man](/man/man)(1), [tldr](/man/tldr)(1)"]],"e":["man","tldr"],"x":"arch-wiki [search_term]"},"ausearch":{"n":"ausearch","t":"Search and query audit log events","s":[["TAGLINE","Search and query audit log events"],["TLDR","Search for **SELinux AVC** denials\n\n```sudo ausearch -m avc```\n\nSearch by **executable**\n\n```sudo ausearch -c httpd```\n\nSearch by **user**\n\n```sudo ausearch -ui 1000```\n\nSearch **recent** events\n\n```sudo ausearch -ts recent```\n\nSearch **failed logins**\n\n```sudo ausearch -m user_login -sv no```\n\nSearch by **file**\n\n```sudo ausearch -f path/to/file```\n\nOutput **raw** format\n\n```sudo ausearch -m avc --raw```"],["SYNOPSIS","**ausearch** [_OPTIONS_]"],["DESCRIPTION","**ausearch** queries the Linux audit log for events. It can search by message type, user, process, file, time range, and other criteria, making it essential for security analysis and incident investigation."],["PARAMETERS","**-m, --message** _type_\n> Search for specific message types (avc, user_login, etc.)\n\n**-c, --comm** _name_\n> Search by command/executable name\n\n**-ui, --uid** _uid_\n> Search by user ID\n\n**-f, --file** _path_\n> Search for events related to a specific file\n\n**-ts, --start** _time_\n> Start time for search (recent, today, or timestamp)\n\n**-te, --end** _time_\n> End time for search\n\n**-sv, --success** _yes|no_\n> Filter by success or failure\n\n**--raw**\n> Output in raw format for further processing\n\n**-i, --interpret**\n> Interpret numeric values (UIDs, syscalls, etc.)\n\n**-k, --key** _key_\n> Search for events matching the specified audit key\n\n**-p, --pid** _pid_\n> Search by process ID\n\n**-sc, --syscall** _syscall_\n> Search by system call name or number\n\n**-x, --executable** _path_\n> Search for events matching the executable path\n\n**--input-logs**\n> Read from specific audit log files"],["CONFIGURATION","**/etc/audit/auditd.conf**\n> Main audit daemon configuration, controls log file location and retention policy.\n\n**/etc/audit/audit.rules**\n> Defines which events are logged by the audit system. Determines what events are searchable."],["CAVEATS","Requires root privileges to access audit logs. The audit daemon must be running and logging events. Large audit logs may slow searches; use time filters to narrow results."],["HISTORY","**ausearch** is part of the **audit** package, providing search capabilities for the Linux Audit Framework."],["INSTALL","```apt: sudo apt install auditd```\n\n```dnf: sudo dnf install audit```\n\n```pacman: sudo pacman -S audit```\n\n```apk: sudo apk add audit```\n\n```zypper: sudo zypper install audit```\n\n```nix: nix profile install nixpkgs#audit```"],["SEE ALSO","[aureport](/man/aureport)(8), [auditctl](/man/auditctl)(8), [audit2why](/man/audit2why)(1)"]],"e":["aureport","auditctl","audit2why"],"x":"sudo ausearch -m avc"},"az-provider":{"n":"az-provider","t":"Manage Azure resource providers","s":[["TAGLINE","Manage Azure resource providers"],["TLDR","**List all resource providers**\n\n```az provider list```\n\n**List registered providers** only\n\n```az provider list --query \"[?registrationState=='Registered']\"```\n\n**Show details** of a specific provider\n\n```az provider show --namespace [Microsoft.Compute]```\n\n**Register a resource provider** and wait for it to finish\n\n```az provider register --namespace [Microsoft.ContainerService] --wait```\n\n**Unregister a resource provider**\n\n```az provider unregister --namespace [Microsoft.ContainerService]```\n\n**Show operations** for a specific provider\n\n```az provider operation show --namespace [Microsoft.Compute]```"],["SYNOPSIS","**az** **provider** _subcommand_ [_options_]"],["SUBCOMMANDS","**list**\n> List resource providers for a subscription.\n\n**show**\n> Get details about a resource provider.\n\n**register**\n> Register a resource provider.\n\n**unregister**\n> Unregister a resource provider.\n\n**operation list**\n> Get operations from all providers.\n\n**operation show**\n> Get an individual provider's operations.\n\n**permission list**\n> List permissions for a provider."],["PARAMETERS","**--namespace** **-n**\n> The resource provider namespace (e.g., Microsoft.Compute).\n\n**--wait**\n> Wait for the registration to finish. Default: false.\n\n**--management-group-id** **-m**\n> The management group id to register the provider against.\n\n**--consent-to-permissions** **-c**\n> Indicate whether authorization is consented (used by register). Default: false.\n\n**--accept-terms**\n> Accept terms when registering an RPaaS (Resource Provider as a Service) provider.\n\n**--expand**\n> Properties to include in list/show results, e.g. resourceTypes/aliases."],["DESCRIPTION","**az provider** manages Azure resource providers. Resource providers are services that supply Azure resources (VMs, storage accounts, databases, etc.). Before using a resource type, the provider must be registered in your subscription.\n\nCommon providers include Microsoft.Compute (VMs), Microsoft.Storage (storage accounts), Microsoft.Network (networking), and Microsoft.ContainerService (AKS)."],["CAVEATS","Requires Azure CLI to be installed and authenticated. Some providers auto-register when creating resources. Unregistering a provider does not delete existing resources but prevents creating new ones."],["INSTALL","```dnf: sudo dnf install azure-cli```\n\n```pacman: sudo pacman -S azure-cli```\n\n```zypper: sudo zypper install azure-cli```\n\n```brew: brew install azure-cli```\n\n```nix: nix profile install nixpkgs#azure-cli```"],["SEE ALSO","[az](/man/az)(1), [az-resource](/man/az-resource)(1), [az-lock](/man/az-lock)(1), [az-tag](/man/az-tag)(1)"],["RESOURCES","```[Source code](https://github.com/Azure/azure-cli)```\n\n```[Documentation](https://learn.microsoft.com/en-us/cli/azure/provider)```\n\n```[Homepage](https://learn.microsoft.com/en-us/cli/azure/)```"]],"e":["az","az-resource","az-lock","az-tag"],"x":"az provider list"},"boot":{"n":"boot","t":"Linux system startup process documentation","s":[["TAGLINE","Linux system startup process documentation"],["TLDR","**View the boot process documentation**\n\n```man 7 boot```\n\n**View systemd boot process documentation**\n\n```man 7 bootup```"],["SYNOPSIS","This is a documentation page describing the Linux boot sequence, not an executable command."],["DESCRIPTION","The **boot**(7) man page documents the Linux system startup process based on UNIX System V Release 4. The boot sequence consists of several phases:\n\n**Hardware Boot (BIOS/UEFI)**\n> POST (Power-On Self-Test) runs, hardware is initialized, and firmware locates the bootloader\n\n**OS Loader (GRUB/LILO)**\n> The bootloader locates and loads the kernel into memory\n\n**Kernel Startup**\n> The kernel initializes devices, starts the swapper process (kswapd), and mounts the root filesystem\n\n**Init Process**\n> The first userspace process (PID 1) starts. Modern systems use **systemd**, older systems use **SysVinit**\n\n**Boot Scripts**\n> System services are started according to the configured runlevel or systemd target"],["CAVEATS","This documentation primarily describes the traditional SysV init boot process. Most modern Linux distributions use **systemd**, which has a different boot architecture documented in **bootup**(7).\n\nThe exact boot sequence varies between distributions, hardware platforms, and init systems in use."],["HISTORY","The System V init process originated in **AT&T Unix System V** released in 1983. Linux distributions historically adopted this model before the transition to **systemd** beginning around 2010-2011. Systemd became the default init system in most major distributions by 2015."],["INSTALL","```nix: nix profile install nixpkgs#boot```"],["SEE ALSO","[systemctl](/man/systemctl)(1), [journalctl](/man/journalctl)(1), [init](/man/init)(1), [grub](/man/grub)(1)"]],"e":["systemctl","journalctl","init","grub"],"x":"man 7 boot"},"clojure":{"n":"clojure","t":"official Clojure language launcher","s":[["TAGLINE","official Clojure language launcher"],["TLDR","**Start a Clojure REPL**\n\n```clojure```\n\n**Run a Clojure script**\n\n```clojure -M [script.clj]```\n\n**Execute a specific function**\n\n```clojure -X [namespace/function]```\n\n**Run with additional dependencies**\n\n```clojure -Sdeps '{:deps {org.clojure/data.json {:mvn/version \"2.4.0\"}}}'```\n\n**Start a REPL with a specific alias** from deps.edn\n\n```clojure -A:[alias]```\n\n**Evaluate an expression**\n\n```clojure -M -e '(println \"Hello, World!\")'```\n\n**Build an uberjar** using tools.build\n\n```clojure -T:build uber```\n\n**Show classpath** without running\n\n```clojure -Spath```"],["SYNOPSIS","**clojure** [_dep-opts_] [_init-opts_] [_main-opts_] [_args_]"],["PARAMETERS","**-M** [_:aliases_]\n> Use concatenated aliases for running main entry point.\n\n**-X** [_:aliases_]\n> Execute a function with exec arguments.\n\n**-T** [_:aliases_]\n> Execute tool with exec arguments.\n\n**-A** [_:aliases_]\n> Use concatenated aliases for REPL.\n\n**-Sdeps** _EDN_\n> Additional dependencies in EDN format.\n\n**-Spath**\n> Print the classpath and exit.\n\n**-Stree**\n> Print the dependency tree.\n\n**-Sdescribe**\n> Print environment and command info.\n\n**-e**, **--eval** _STRING_\n> Evaluate the given expression.\n\n**-r**, **--repl**\n> Run a REPL.\n\n**-h**, **--help**\n> Display help information."],["DESCRIPTION","**clojure** is the official command-line launcher for the Clojure programming language. It manages dependencies through **deps.edn** configuration files and provides a streamlined workflow for REPL-driven development, script execution, and building applications.\n\nThe CLI integrates with Maven and Git repositories for dependency resolution, automatically downloading and caching libraries as needed. Aliases in deps.edn allow defining different configurations for development, testing, and production environments.\n\nClojure provides three main execution modes: **-M** for traditional main function execution, **-X** for executing functions with keyword arguments, and **-T** for tool invocation. The REPL supports interactive development with immediate feedback, a core practice in Clojure development."],["CONFIGURATION","**deps.edn**\n> Project-level dependency declarations, aliases, and path configurations.\n\n**~/.clojure/deps.edn**\n> User-level default dependencies and aliases applied to all projects."],["CAVEATS","Requires Java Runtime Environment (JRE) version 8 or higher. Initial dependency downloads can take time but are cached locally. The CLI requires a deps.edn file for project-specific configurations; global defaults apply without one."],["HISTORY","Clojure was created by Rich Hickey and released in **2007** as a modern Lisp for the JVM. The **clojure** CLI and deps.edn tooling were introduced in **2018** as an official alternative to Leiningen and Boot, providing a simpler, more composable approach to dependency management."],["INSTALL","```dnf: sudo dnf install clojure```\n\n```pacman: sudo pacman -S clojure```\n\n```apk: sudo apk add clojure```\n\n```zypper: sudo zypper install clojure```\n\n```brew: brew install clojure```\n\n```nix: nix profile install nixpkgs#clojure```"],["SEE ALSO","[lein](/man/lein)(1), [java](/man/java)(1), [clj](/man/clj)(1)"],["RESOURCES","```[Source code](https://github.com/clojure/brew-install)```\n\n```[Documentation](https://clojure.org/reference/deps_and_cli)```"]],"e":["lein","java","clj"],"x":"clojure"},"darkhttpd":{"n":"darkhttpd","t":"simple, fast, and secure static web server","s":[["TAGLINE","simple, fast, and secure static web server"],["TLDR","**Serve the current directory** on the default port\n\n```darkhttpd .```\n\n**Serve a specific directory** on a custom port\n\n```darkhttpd [/path/to/webroot] --port [8080]```\n\n**Bind to a specific address**\n\n```darkhttpd . --addr [127.0.0.1]```\n\n**Set a custom index file**\n\n```darkhttpd . --index [home.html]```\n\n**Disable directory listing**\n\n```darkhttpd . --no-listing```\n\n**Serve with logging** to stdout\n\n```darkhttpd . --log -```\n\n**Serve with virtual hosting**\n\n```darkhttpd . --forward [example.com] [http://backend:8000]```\n\n**Run as a daemon** (background)\n\n```darkhttpd . --daemon --pidfile [/var/run/darkhttpd.pid]```"],["SYNOPSIS","**darkhttpd** _docroot_ [_--port port_] [_--addr address_] [_--daemon_] [_--log file_] [_--index file_] [_options_]"],["PARAMETERS","**--port** _port_\n> Port to listen on (default: 80 when running as root, otherwise 8080).\n\n**--addr** _address_\n> Address to bind to (default: all interfaces).\n\n**--daemon**\n> Fork into background.\n\n**--pidfile** _file_\n> Write PID to file when daemonized.\n\n**--log** _file_\n> Log requests to file (- for stdout).\n\n**--index** _file_\n> Default index file to serve for directory requests (default: index.html).\n\n**--no-listing**\n> Disable directory listing (directory listing is enabled by default).\n\n**--timeout** _seconds_\n> Connection timeout (default: 60).\n\n**--uid** _uid_\n> Drop privileges to this user ID.\n\n**--gid** _gid_\n> Drop privileges to this group ID.\n\n**--chroot**\n> Chroot to docroot.\n\n**--maxconn** _n_\n> Limit number of simultaneous connections.\n\n**--forward** _host_ _url_\n> 301 redirect requests with matching Host header to the given URL.\n\n**--forward-all** _url_\n> 301 redirect all requests to the given URL (catch-all).\n\n**--forward-https** _host_ _url_\n> 301 redirect HTTP requests for host to HTTPS.\n\n**--no-keepalive**\n> Disable HTTP keep-alive.\n\n**--no-server-id**\n> Omit Server identification header from responses.\n\n**--mimetypes** _file_\n> Load MIME types from file.\n\n**--default-mimetype** _type_\n> Serve files with unknown extensions as this MIME type.\n\n**--hide-dotfiles**\n> Do not serve files or directories starting with a dot.\n\n**--single-file** _file_\n> Serve only the specified file rather than a directory.\n\n**--auth** _user:password_\n> Enable HTTP Basic authentication.\n\n**--syslog**\n> Use syslog for request logging instead of a file.\n\n**--header** _header_\n> Add a custom response header (repeatable)."],["DESCRIPTION","**darkhttpd** is a simple, fast, and secure static web server. Written in C with no dependencies, it compiles to a single small binary that can serve static files with minimal resource usage.\n\nThe server is designed for simplicity: it serves files from a document root directory with proper MIME types, handles HTTP/1.1 keep-alive connections, and supports security features like chroot and privilege dropping. There's no configuration file; all options are command-line arguments.\n\nDirectory listing is enabled by default and can be disabled with **--no-listing**. Custom index files can be specified with **--index** to override the default **index.html**. The server sends proper caching headers and handles range requests for resumable downloads.\n\nFor production use, darkhttpd supports daemonization, PID file creation, and logging. The chroot and uid/gid options provide security isolation. While not designed for high-traffic sites, it's excellent for local development, embedded systems, or serving static content where simplicity is valued."],["CAVEATS","Static files only; no CGI, PHP, or dynamic content support. No SSL/TLS; use a reverse proxy for HTTPS. No configuration file; all settings via command line. Limited to HTTP/1.1 features. Not suitable for high-concurrency production workloads."],["HISTORY","**darkhttpd** was created by Emil Mikulic in **2003** as a simple, portable HTTP server. The design philosophy emphasizes minimalism and security over features. The single-file C source compiles on virtually any Unix-like system without external dependencies, making it popular for embedded systems and minimal environments."],["INSTALL","```dnf: sudo dnf install darkhttpd```\n\n```pacman: sudo pacman -S darkhttpd```\n\n```apk: sudo apk add darkhttpd```\n\n```zypper: sudo zypper install darkhttpd```\n\n```brew: brew install darkhttpd```\n\n```nix: nix profile install nixpkgs#darkhttpd```"],["SEE ALSO","[nginx](/man/nginx)(8), [python3](/man/python3)(1), [caddy](/man/caddy)(1), [busybox](/man/busybox)(1)"]],"e":["nginx","python3","caddy","busybox"],"x":"darkhttpd ."},"ddgr":{"n":"ddgr","t":"DuckDuckGo search from the command line","s":[["TAGLINE","DuckDuckGo search from the command line"],["TLDR","**Search DuckDuckGo** from the terminal\n\n```ddgr [search terms]```\n\n**Open the first result** directly in browser (I'm Feeling Ducky)\n\n```ddgr -j [search terms]```\n\n**Search a specific website**\n\n```ddgr -w [amazon.com] [digital camera]```\n\n**Limit search to recent results** (d=day, w=week, m=month, y=year)\n\n```ddgr -t [w] [search terms]```\n\n**Set number of results** per page\n\n```ddgr -n [25] [search terms]```\n\n**Use a DuckDuckGo bang** shortcut\n\n```ddgr ![w] [wikipedia search]```\n\n**Output results as JSON**\n\n```ddgr --json [search terms]```\n\n**Search through a proxy**\n\n```ddgr --proxy [localhost:8118] [search terms]```"],["SYNOPSIS","**ddgr** [_OPTIONS_] [_KEYWORD_ [_KEYWORD_...]]"],["PARAMETERS","**-n**, **--num**=_N_\n> Show N results per page (0-25, default: 10).\n\n**-r**, **--reg**=_REG_\n> Region-specific search (e.g., us-en, uk-en, in-en).\n\n**-j**, **--ducky**\n> Open first result in browser; implies --noprompt.\n\n**-t**, **--time**=_SPAN_\n> Limit search by time: d (day), w (week), m (month), y (year).\n\n**-w**, **--site**=_SITE_\n> Search a specific website.\n\n**-x**, **--expand**\n> Show complete URLs instead of just domains.\n\n**-p**, **--proxy**=_URI_\n> Route traffic through HTTP proxy.\n\n**--unsafe**\n> Disable safe search.\n\n**--json**\n> Output results as JSON; implies --noprompt.\n\n**--np**, **--noprompt**\n> Search and exit without interactive prompt.\n\n**--colors**=_COLORS_\n> Set custom output color scheme.\n\n**-C**, **--nocolor**\n> Disable colored output.\n\n**-h**, **--help**\n> Display help and exit."],["OMNIPROMPT","**n**, **p**, **f**\n> Navigate to next, previous, or first page.\n\n_index_\n> Open result number in browser.\n\n**o** [_index_|_range_|**a**]\n> Open result(s) in browser.\n\n**d** _keywords_\n> Perform new search with same options.\n\n**c** _index_\n> Copy URL to clipboard.\n\n**x**\n> Toggle URL expansion.\n\n**q**, **^D**, double Enter\n> Exit ddgr."],["DESCRIPTION","**ddgr** is a command-line tool for searching DuckDuckGo from the terminal. It displays search results with title, URL, and text context. Results are paginated and indexed, allowing quick navigation and opening in a browser by number.\n\nThe tool supports DuckDuckGo bangs (shortcuts like !w for Wikipedia, !yt for YouTube) and keywords like filetype: and site:. Results can be limited by time period or region. DuckDuckGo's privacy-focused approach means no tracking, and ddgr works over the Tor network."],["ENVIRONMENT","**BROWSER**\n> Override the default web browser.\n\n**DDGR_COLORS**\n> Custom color scheme (six-letter string like BSD LSCOLORS).\n\n**HTTPS_PROXY**\n> HTTP proxy configuration."],["CONFIGURATION","**~/.config/ddgr/config**\n> Configuration file for setting default options like number of results, region, and proxy settings."],["CAVEATS","Requires Python 3. Results depend on DuckDuckGo's HTML interface which may change. Some features like instant answers are not available in the HTML version used by ddgr."],["HISTORY","ddgr was created by Arun Prakash Jana as a privacy-focused alternative to googler (a similar tool for Google search). The project emphasizes user privacy by leveraging DuckDuckGo's no-tracking policy and support for the Tor network."],["INSTALL","```dnf: sudo dnf install ddgr```\n\n```pacman: sudo pacman -S ddgr```\n\n```zypper: sudo zypper install ddgr```\n\n```brew: brew install ddgr```\n\n```nix: nix profile install nixpkgs#ddgr```"],["SEE ALSO","[googler](/man/googler)(1), [surfraw](/man/surfraw)(1), [w3m](/man/w3m)(1), [lynx](/man/lynx)(1)"],["RESOURCES","```[Source code](https://github.com/jarun/ddgr)```"]],"e":["googler","surfraw","w3m","lynx"],"x":"ddgr [search terms]"},"doctum":{"n":"doctum","t":"API documentation generator for PHP","s":[["TAGLINE","API documentation generator for PHP"],["TLDR","**Generate documentation**\n\n```doctum update [doctum.php]```\n\n**Parse and render** documentation\n\n```doctum render [doctum.php]```\n\n**Update and render** in one step\n\n```doctum update [doctum.php] --force```\n\n**Show verbose output**\n\n```doctum update -v [doctum.php]```"],["SYNOPSIS","**doctum** _command_ [_options_] _config-file_"],["PARAMETERS","_COMMAND_\n> Operation: update, render, parse.\n\n_CONFIG-FILE_\n> PHP configuration file.\n\n**update**\n> Parse source and render documentation.\n\n**render**\n> Render from cached parse data.\n\n**parse**\n> Parse source without rendering.\n\n**--force**\n> Force complete regeneration.\n\n**--only-version** _VERSION_\n> Restrict build to the specified version only.\n\n**--no-progress**\n> Disable the progress bar.\n\n**--ignore-parse-errors**\n> Ignore parse errors and exit with code 0.\n\n**-v**, **--verbose**\n> Verbose output.\n\n**--help**\n> Display help information."],["DESCRIPTION","**Doctum** is an API documentation generator for PHP projects. It parses PHP source code and generates browsable HTML documentation with class hierarchies, method signatures, and docblock comments.\n\nThe tool is a fork of Sami, providing continued maintenance and improvements. It supports modern PHP features including traits, namespaces, and type declarations. Configuration is done through PHP files allowing flexible customization.\n\nDoctum creates searchable documentation with cross-references between classes and methods, making it valuable for documenting libraries and frameworks."],["CONFIGURATION","**doctum.php**\n> Configuration file defining source directories, build paths, themes, and parsing options."],["CAVEATS","Requires PHP 8.1+. Large codebases may have slow initial parse. Configuration requires PHP knowledge. Some PHP edge cases may not parse correctly."],["HISTORY","Doctum is a fork of **Sami**, originally created by **Fabien Potencier** (creator of Symfony). When Sami was deprecated, Doctum emerged as the community-maintained continuation, providing API documentation for PHP projects."],["SEE ALSO","[phpdoc](/man/phpdoc)(1), [doxygen](/man/doxygen)(1), [php](/man/php)(1)"]],"e":["phpdoc","doxygen","php"],"x":"doctum update [doctum.php]"},"fast":{"n":"fast","t":"Netflix-powered internet speed test CLI","s":[["TAGLINE","Netflix-powered internet speed test CLI"],["TLDR","**Run speed test**\n\n```fast```\n\n**Run with upload test**\n\n```fast --upload```\n\n**Output on a single line** (script-friendly)\n\n```fast --single-line```\n\n**Show JSON output**\n\n```fast --json```\n\n**Include latency and server location**\n\n```fast --verbose```"],["SYNOPSIS","**fast** [_options_]"],["PARAMETERS","**-u**, **--upload**\n> Measure upload speed in addition to download speed.\n\n**--single-line**\n> Reduce spacing and print output on a single line.\n\n**--json**\n> Output results as JSON.\n\n**--verbose**\n> Include latency and server location information."],["DESCRIPTION","**fast** is a command-line speed testing tool powered by Netflix's Fast.com service. It measures download speed and optionally upload speed using Netflix's servers worldwide.\n\nThe tool provides a quick bandwidth measurement without ads or complexity. It's useful for testing internet connection quality and ISP performance."],["OUTPUT","```\n   $ fast\n\n     Ping: 12ms\n   Download: 95.2 Mbps\n   (Upload test with --upload)\n```"],["CAVEATS","Requires internet connection to Netflix's servers. Results may vary by location and time. Netflix servers may be throttled by some ISPs. For more detailed tests, use speedtest-cli."],["HISTORY","fast-cli was created as a command-line interface to Netflix's Fast.com, which Netflix launched in **2016** as a simple, ad-free speed test. The service uses Netflix's content delivery network."],["INSTALL","```nix: nix profile install nixpkgs#fast```"],["SEE ALSO","[speedtest-cli](/man/speedtest-cli)(1), [iperf](/man/iperf)(1), [curl](/man/curl)(1)"],["RESOURCES","```[Source code](https://github.com/sindresorhus/fast-cli)```"]],"e":["speedtest-cli","iperf","curl"],"x":"fast"},"fluidsynth":{"n":"fluidsynth","t":"A real-time software MIDI synthesizer using SoundFont 2","s":[["TAGLINE","A real-time software MIDI synthesizer using SoundFont 2"],["TLDR","**Play a MIDI file with a SoundFont**\n\n```fluidsynth [/usr/share/soundfonts/soundfont.sf2] [path/to/file.mid]```\n\n**Specify the audio driver**\n\n```fluidsynth -a [pipewire|pulseaudio|alsa|jack] [soundfont.sf2] [file.mid]```\n\n**Render a MIDI file to a WAV audio file (no real-time playback)**\n\n```fluidsynth -T wav -F [output.wav] [soundfont.sf2] [file.mid]```\n\n**Run as a MIDI server accepting external MIDI input**\n\n```fluidsynth -s [soundfont.sf2]```\n\n**Batch process multiple MIDI files without interactive shell**\n\n```fluidsynth -ni [soundfont.sf2] [file1.mid] [file2.mid]```\n\n**Set sample rate and master gain**\n\n```fluidsynth -r [44100] -g [1.0] [soundfont.sf2] [file.mid]```\n\n**Disable reverb and chorus effects**\n\n```fluidsynth -R 0 -C 0 [soundfont.sf2] [file.mid]```"],["SYNOPSIS","**fluidsynth** [_options_] [_soundfont_] [_midifile..._]"],["PARAMETERS","**-a**, **--audio-driver** _driver_\n> Audio output driver (pulseaudio, pipewire, alsa, jack, oss, etc.).\n\n**-m**, **--midi-driver** _driver_\n> MIDI input driver (alsa_seq, alsa_raw, jack, oss, etc.).\n\n**-F**, **--fast-render** _file_\n> Render to audio file as fast as possible (no real-time playback).\n\n**-T**, **--audio-file-type** _type_\n> File type for fast-render output (wav, raw, aiff, au, flac, oga).\n\n**-n**, **--no-midi-in**\n> Disable MIDI input.\n\n**-i**, **--no-shell**\n> Disable interactive shell.\n\n**-s**, **--server**\n> Run as background server process.\n\n**-r**, **--sample-rate** _rate_\n> Set audio sample rate (default: 44100).\n\n**-g**, **--gain** _value_\n> Set master gain (0.0-10.0, default: 0.2).\n\n**-L**, **--audio-channels** _num_\n> Number of audio channel pairs.\n\n**-R**, **--reverb** _0|1_\n> Enable or disable reverb effect.\n\n**-C**, **--chorus** _0|1_\n> Enable or disable chorus effect.\n\n**-o** _setting=value_\n> Set arbitrary synth setting (e.g., `synth.polyphony`).\n\n**-c**, **--audio-bufcount** _count_\n> Number of audio buffers.\n\n**-z**, **--audio-bufsize** _size_\n> Size of each audio buffer in frames.\n\n**-V**, **--version**\n> Display version information.\n\n**-h**, **--help**\n> Display help message."],["DESCRIPTION","**FluidSynth** is a real-time software MIDI synthesizer implementing the SoundFont 2 specifications. It synthesizes MIDI input or files into audio output using SoundFont instrument definitions.\n\nThe synthesizer can operate in multiple modes: playing MIDI files directly, running as a server for external MIDI input (keyboards, sequencers), or fast-rendering to audio files. An interactive shell allows real-time control of synthesis parameters.\n\nFluidSynth supports various audio backends (PulseAudio, PipeWire, ALSA, JACK) and MIDI backends (ALSA sequencer, JACK MIDI), making it suitable for both standalone use and integration into larger audio setups."],["CAVEATS","Requires at least one SoundFont file (**.sf2** or **.sf3**) to produce sound. Many distributions provide default SoundFonts in **/usr/share/soundfonts/**. Real-time synthesis requires adequate CPU resources; complex SoundFonts may cause audio dropouts on slower systems. The **-F** fast-render option disables real-time audio output."],["HISTORY","FluidSynth was created by **Peter Hanappe** in **2000** as an open-source software synthesizer. Originally named \"IIWU Synth,\" it was renamed to FluidSynth and has grown into a widely-used MIDI synthesizer for Linux, macOS, and Windows. The project continues active development, with version 2.x introducing significant improvements to the API and audio quality."],["INSTALL","```apt: sudo apt install fluidsynth```\n\n```dnf: sudo dnf install fluidsynth```\n\n```pacman: sudo pacman -S fluidsynth```\n\n```apk: sudo apk add fluidsynth```\n\n```zypper: sudo zypper install fluidsynth```\n\n```brew: brew install fluidsynth```\n\n```nix: nix profile install nixpkgs#fluidsynth```"],["SEE ALSO","[timidity](/man/timidity)(1), [aplaymidi](/man/aplaymidi)(1), [aconnect](/man/aconnect)(1), [amidi](/man/amidi)(1)"]],"e":["timidity","aplaymidi","aconnect","amidi"],"x":"fluidsynth [/usr/share/soundfonts/soundfont.sf2] [path/to/file.mid]"},"git-create-branch":{"n":"git-create-branch","t":"Create and optionally switch to new branch","s":[["TAGLINE","Create and optionally switch to new branch"],["TLDR","**Create and switch to branch**\n\n```git create-branch [branch_name]```\n\n**Create and switch to branch from a specific starting point**\n\n```git create-branch [name] [start_point]```\n\n**Create and push to remote**\n\n```git create-branch -r [name]```"],["SYNOPSIS","**git** **create-branch** [_options_] _name_ [_commit_]"],["PARAMETERS","**-r**, **--remote**\n> Setup a remote tracking branch as well. Creates the branch locally, pushes it to origin, and sets up tracking."],["DESCRIPTION","**git create-branch** is a convenience command from git-extras that simplifies branch creation workflows. It creates a new branch and switches to it. With the **-r** flag, it additionally pushes the branch to origin and sets up remote tracking, combining several manual steps into one command.\n\nWithout the -r flag, the command creates a local branch and switches to it, equivalent to **git checkout -b**. You can optionally specify a starting point (commit hash or branch name); otherwise, the branch starts from HEAD."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-branch](/man/git-branch)(1), [git-checkout](/man/git-checkout)(1), [git-switch](/man/git-switch)(1), [git-delete-branch](/man/git-delete-branch)(1), [git-extras](/man/git-extras)(1)"]],"e":["git-branch","git-checkout","git-switch","git-delete-branch","git-extras"],"x":"git create-branch [branch_name]"},"git-extras":{"n":"git-extras","t":"Collection of useful Git utilities and commands","s":[["TAGLINE","Collection of useful Git utilities and commands"],["TLDR","**Show git-extras version**\n\n```git extras --version```\n\n**Update git-extras** to the latest release\n\n```git extras update```\n\n**List all git-extras commands**\n\n```git extras --help```\n\n**Show a repository summary**\n\n```git summary```\n\n**Generate a changelog from tags**\n\n```git changelog```"],["SYNOPSIS","**git** **extras** [**--version**] [**--help**] [**update**]"],["PARAMETERS","**--version**\n> Print the installed git-extras version.\n\n**--help**\n> Print the list of extra subcommands provided by git-extras.\n\n**update**\n> Fetch and install the latest git-extras release from upstream."],["DESCRIPTION","**git extras** is a comprehensive collection of Git utility commands that extend Git's functionality beyond its core feature set. The package includes dozens of helper commands designed to streamline common Git workflows and provide shortcuts for repetitive tasks.\n\nNotable commands include git-summary for repository statistics, git-changelog for generating change logs, git-effort for tracking development activity, git-delete-branch for safe branch cleanup, git-ignore for template-based .gitignore creation, and git-undo for reverting recent changes.\n\nInstallation methods vary by platform, with packages available through Homebrew, apt, yum, and manual installation from source. The tools are implemented primarily as shell scripts, making them portable and easy to customize."],["INSTALL","```dnf: sudo dnf install git-extras```\n\n```brew: brew install git-extras```\n\n```nix: nix profile install nixpkgs#git-extras```"],["SEE ALSO","[git](/man/git)(1), [git-summary](/man/git-summary)(1), [git-changelog](/man/git-changelog)(1), [git-effort](/man/git-effort)(1), [git-ignore](/man/git-ignore)(1), [git-undo](/man/git-undo)(1)"]],"e":["git","git-summary","git-changelog","git-effort","git-ignore","git-undo"],"x":"git extras --version"},"iceauth":{"n":"iceauth","t":"manages ICE authorization entries","s":[["TAGLINE","manages ICE authorization entries"],["TLDR","**List entries**\n\n```iceauth list```\n\n**Extract to file**\n\n```iceauth extract [auth-file]```\n\n**Add entry**\n\n```iceauth add [protocol] [address] [auth-name] [auth-data]```\n\n**Merge from file**\n\n```iceauth merge [auth-file]```"],["SYNOPSIS","**iceauth** [_options_] [_command_]"],["PARAMETERS","_COMMAND_\n> Command (list, add, remove, extract, merge).\n\n**list**\n> List authorization entries.\n\n**add**\n> Add entry.\n\n**remove**\n> Remove entry.\n\n**extract**\n> Extract to file.\n\n**merge**\n> Merge from file.\n\n**--help**\n> Display help information."],["DESCRIPTION","**iceauth** manages ICE (Inter-Client Exchange) authorization entries. ICE is used for communication between X11 clients.\n\nThe tool handles authentication records for X session managers and similar applications. It's analogous to xauth for X display connections."],["CAVEATS","X11 specific. Used by session managers. Low-level utility."],["HISTORY","iceauth is part of **X.Org** for managing ICE protocol authentication, used in X session management."],["INSTALL","```apt: sudo apt install x11-xserver-utils```\n\n```dnf: sudo dnf install iceauth```\n\n```apk: sudo apk add iceauth```\n\n```zypper: sudo zypper install iceauth```\n\n```nix: nix profile install nixpkgs#iceauth```"],["SEE ALSO","[xauth](/man/xauth)(1), [Xorg](/man/Xorg)(1)"]],"e":["xauth","Xorg"],"x":"iceauth list"},"impacket-gettgt":{"n":"impacket-gettgt","t":"requests a Kerberos Ticket Granting Ticket from an Active Directory domain","s":[["TAGLINE","requests a Kerberos Ticket Granting Ticket from an Active Directory domain"],["TLDR","**Request a TGT** for a domain user with password\n\n```impacket-gettgt '[domain]/[user]:[password]'```\n\n**Request TGT** and save to a specific file\n\n```impacket-gettgt -dc-ip [192.168.1.100] '[domain]/[user]:[password]'```\n\n**Request TGT using NTLM hash**\n\n```impacket-gettgt -hashes ':[nthash]' '[domain]/[user]'```\n\n**Request TGT using AES key**\n\n```impacket-gettgt -aesKey '[aes256key]' '[domain]/[user]'```"],["SYNOPSIS","**impacket-gettgt** [_-h_] [_-hashes LMHASH:NTHASH_] [_-no-pass_] [_-k_] [_-aesKey KEY_] [_-dc-ip IP_] [_-service SPN_] _target_"],["PARAMETERS","**-hashes** _LMHASH:NTHASH_\n> Use NTLM hashes for authentication instead of password.\n\n**-no-pass**\n> Don't ask for password (useful together with **-k**).\n\n**-k**\n> Use Kerberos authentication; grab credentials from ccache file (KRB5CCNAME).\n\n**-aesKey** _KEY_\n> AES key to use for Kerberos authentication (128 or 256 bit).\n\n**-dc-ip** _IP_\n> IP address of the domain controller (KDC). If omitted, the domain part of the identity is used.\n\n**-service** _SPN_\n> Request a service ticket directly through an AS-REQ for the given SPN.\n\n**-principalType** _TYPE_\n> Principal name type (default NT_PRINCIPAL).\n\n**-debug**\n> Enable debug output."],["DESCRIPTION","**impacket-gettgt** requests a Kerberos Ticket Granting Ticket (TGT) from an Active Directory domain controller. The TGT is saved to a ccache file that can be used for subsequent Kerberos authentication with other tools.\n\nThis tool is useful for obtaining Kerberos tickets when you have valid credentials (password, hash, or AES key) and need to authenticate to Kerberos-enabled services. The resulting ccache file can be exported via the KRB5CCNAME environment variable."],["CAVEATS","Requires valid domain credentials in some form (password, NTLM hash, or AES key). The domain controller must be reachable and Kerberos ports (88/tcp) must be accessible. Time synchronization between client and KDC is critical for Kerberos to function."],["HISTORY","Part of the **Impacket** library by SecureAuth, implementing the Kerberos protocol for penetration testing. TGT retrieval is a fundamental operation in Kerberos-based attacks and authentication workflows."],["INSTALL","```pacman: sudo pacman -S impacket```"],["SEE ALSO","[impacket-getst](/man/impacket-getst)(1), [kinit](/man/kinit)(1), [impacket-secretsdump](/man/impacket-secretsdump)(1)"]],"e":["impacket-getst","kinit","impacket-secretsdump"],"x":"impacket-gettgt '[domain]/[user]:[password]'"},"intel_reg_read":{"n":"intel_reg_read","t":"reads hardware registers from Intel GPUs","s":[["TAGLINE","reads hardware registers from Intel GPUs"],["TLDR","**Read a specific register by address**\n\n```sudo intel_reg read [0x70000]```\n\n**Read multiple registers**\n\n```sudo intel_reg read [0x70000] [0x70004]```\n\n**Decode register bits into named fields**\n\n```sudo intel_reg read --decode [0x70000]```\n\n**Dump all known registers**\n\n```sudo intel_reg read --all```\n\n**Dump registers with decoding**\n\n```sudo intel_reg read --all --decode```"],["SYNOPSIS","**intel_reg** **read** [_options_] [_register_...]"],["PARAMETERS","_REGISTER_\n> Register address in hex (e.g., 0x70000).\n\n**--decode**\n> Decode register bits into named fields.\n\n**--all**\n> Dump all known registers.\n\n**--help**\n> Display help information."],["DESCRIPTION","**intel_reg read** is a subcommand of `intel_reg` that reads hardware registers from Intel GPUs. It provides direct access to GPU configuration registers for debugging and development.\n\nThe tool can decode register bits into meaningful named fields when used with the `--decode` flag."],["CAVEATS","Intel graphics only. Requires root. Can affect system stability. Part of intel-gpu-tools."],["HISTORY","intel_reg_read is part of **intel-gpu-tools**, created for Intel graphics driver development and debugging."],["SEE ALSO","[intel_gtt](/man/intel_gtt)(1)"]],"e":["intel_gtt"],"x":"sudo intel_reg read [0x70000]"},"intentrace":{"n":"intentrace","t":"Strace with intent for meaningful syscall tracing","s":[["TAGLINE","Strace with intent for meaningful syscall tracing"],["TLDR","**Trace a command**\n\n```intentrace [command]```\n\n**Trace a running process** by PID\n\n```intentrace -p [pid]```\n\n**Show only a summary table**\n\n```intentrace -c [command]```\n\n**Follow child processes** (forks)\n\n```intentrace -f [command]```\n\n**Only show failed syscalls**\n\n```intentrace -Z [command]```\n\n**Redirect output to a file**\n\n```intentrace -o [file] [command]```"],["SYNOPSIS","**intentrace** [_options_] [_command_ [_args_...]]"],["PARAMETERS","**-p** _pid_, **--attach** _pid_\n> Attach to a running process by PID.\n\n**-c**, **--summary-only**\n> Display only a summary table.\n\n**-C**, **--summary**\n> Show summary table in addition to normal output.\n\n**-f**, **--follow-forks**\n> Trace child processes when traced programs create them.\n\n**-Z**, **--failed-only**\n> Only show failed syscalls.\n\n**-o** _file_, **--output** _file_\n> Redirect output to a file.\n\n**-q**, **--mute-stdout**\n> Suppress traced program's standard output.\n\n**--trace**=_syscall1,syscall2_\n> Trace only specific syscalls."],["DESCRIPTION","**intentrace** is a syscall tracer that goes beyond raw syscall logging by consulting an extensive backlog of deduction heuristics to explain what each syscall is actually trying to accomplish. Because Linux syscalls often have dual usage that is obfuscated by libraries, seeing the intent behind a syscall is immensely useful when debugging crashing binaries.\n\nUnlike traditional strace output which shows raw syscall numbers and arguments, intentrace provides human-readable explanations of what each system call is doing in context."],["CAVEATS","Currently only supports **x86-64** Linux. Covers approximately 166 of the 380+ Linux syscalls, focusing on the most commonly used ones. The project is in beta; multi-threaded programs may not trace reliably."],["HISTORY","**intentrace** was created by **sectordistrict** and is written in **Rust**. It was designed as a more informative alternative to strace that provides context and meaning for each syscall rather than just raw data."],["INSTALL","```nix: nix profile install nixpkgs#intentrace```"],["SEE ALSO","[strace](/man/strace)(1), [ltrace](/man/ltrace)(1), [ptrace](/man/ptrace)(2)"]],"e":["strace","ltrace","ptrace"],"x":"intentrace [command]"},"jnv":{"n":"jnv","t":"interactive JSON viewer and jq filter editor","s":[["TAGLINE","interactive JSON viewer and jq filter editor"],["TLDR","**Open a JSON file** for interactive filtering\n\n```jnv [path/to/data.json]```\n\n**Filter JSON from stdin**\n\n```cat [path/to/data.json] | jnv```\n\n**Open with a default filter applied**\n\n```jnv --default-filter '.[0]' [path/to/data.json]```\n\n**Use a custom configuration file**\n\n```jnv -c [path/to/config.toml] [path/to/data.json]```\n\n**Write the filtered result to stdout on exit**\n\n```jnv --write-to-stdout [path/to/data.json]```"],["SYNOPSIS","**jnv** [_OPTIONS_] [_INPUT_]"],["PARAMETERS","_INPUT_\n> Path to a JSON file. If omitted or \"-\", reads from stdin.\n\n**-c**, **--config** _file_\n> Path to a TOML configuration file.\n\n**--default-filter** _filter_\n> Default jq filter to apply to input data.\n\n**--write-to-stdout**\n> Write the current JSON result to stdout when exiting.\n\n**-h**, **--help**\n> Print help information.\n\n**-V**, **--version**\n> Print version."],["DESCRIPTION","**jnv** is an interactive JSON viewer and **jq** filter editor. It provides a split-pane interface with a filter editor at the top and a syntax-highlighted JSON view below that updates in real time as you type your jq filter. Inspired by **jid** and **jiq**, jnv uses **jaq** internally to apply filters, eliminating the need to install jq separately.\n\nThe tool supports JSON files and JSON Lines input, configurable UI through TOML configuration, hint message toggling, and adjustable reactivity including debounce times."],["CAVEATS","Uses jaq (not jq) internally, so some advanced jq features or edge cases may behave differently. Large JSON files may impact responsiveness of the live preview. Supports JSON and JSON Lines input."],["HISTORY","**jnv** was created by **ynqa** and is written in **Rust**. It was designed to provide a more polished and self-contained interactive JSON exploration experience compared to earlier tools like jid and jiq."],["INSTALL","```pacman: sudo pacman -S jnv```\n\n```brew: brew install jnv```\n\n```nix: nix profile install nixpkgs#jnv```"],["SEE ALSO","[jq](/man/jq)(1), [jaq](/man/jaq)(1), [fx](/man/fx)(1), [jiq](/man/jiq)(1)"]],"e":["jq","jaq","fx","jiq"],"x":"jnv [path/to/data.json]"},"kiwi-ng":{"n":"kiwi-ng","t":"command-line tool for building Linux operating system images","s":[["TAGLINE","command-line tool for building Linux operating system images"],["TLDR","**Build image from description**\n\n```sudo kiwi-ng system build --description [path] --target-dir [output]```\n\n**Create image with custom repo**\n\n```sudo kiwi-ng system build --description [path] --set-repo [repo-url] --target-dir [output]```\n\n**List available image types**\n\n```kiwi-ng image list```\n\n**Prepare image root**\n\n```sudo kiwi-ng system prepare --description [path] --root [rootdir]```"],["SYNOPSIS","**kiwi-ng** [_options_] _command_ [_args_...]"],["PARAMETERS","**system build**\n> Build complete image from description.\n\n**system prepare**\n> Prepare image root filesystem.\n\n**--description** _path_\n> Path to image description.\n\n**--target-dir** _path_\n> Output directory for images.\n\n**--set-repo** _url_\n> Set repository URL.\n\n**--profile** _name_\n> Build specific profile."],["DESCRIPTION","**kiwi-ng** is a command-line tool for building Linux operating system images. Supports various output formats including ISOs, virtual machine images, containers, and cloud images. Uses XML-based descriptions to define image configuration."],["SEE ALSO","[mkisofs](/man/mkisofs)(1), [docker](/man/docker)(1)"]],"e":["mkisofs","docker"],"x":"sudo kiwi-ng system build --description [path] --target-dir [output]"},"kmod":{"n":"kmod","t":"central utility for managing Linux kernel modules","s":[["TAGLINE","central utility for managing Linux kernel modules"],["TLDR","**List** currently loaded kernel modules\n\n```kmod list```\n\nDisplay **static device nodes** information\n\n```kmod static-nodes```"],["SYNOPSIS","**kmod** [_options_] _command_"],["PARAMETERS","**list**\n> List currently loaded kernel modules (like lsmod)\n\n**static-nodes**\n> Display static device nodes provided by kernel modules"],["DESCRIPTION","**kmod** is the central utility for managing Linux kernel modules. It provides a unified interface that is typically accessed through symbolic links with more familiar names.\n\nThe following commands are symlinks to kmod:\n- **lsmod** - List loaded modules\n- **insmod** - Insert module\n- **rmmod** - Remove module\n- **modprobe** - Load module with dependencies\n- **modinfo** - Display module information\n- **depmod** - Generate module dependencies"],["CAVEATS","Direct kmod usage is rare; symlinked commands are more common. Requires appropriate privileges for loading/unloading modules. Module operations affect entire system."],["HISTORY","kmod replaced the older module-init-tools package starting around **2011**. It was developed by Lucas De Marchi to provide a more maintainable and efficient kernel module management system."],["INSTALL","```apt: sudo apt install kmod```\n\n```dnf: sudo dnf install kmod```\n\n```pacman: sudo pacman -S kmod```\n\n```apk: sudo apk add kmod```\n\n```zypper: sudo zypper install kmod```\n\n```brew: brew install kmod```\n\n```nix: nix profile install nixpkgs#kmod```"],["SEE ALSO","[lsmod](/man/lsmod)(8), [modprobe](/man/modprobe)(8), [insmod](/man/insmod)(8), [rmmod](/man/rmmod)(8), [modinfo](/man/modinfo)(8)"]],"e":["lsmod","modprobe","insmod","rmmod","modinfo"],"x":"kmod list"},"kubectl-cp":{"n":"kubectl-cp","t":"copies files and directories between local filesystem and containers in pods","s":[["TAGLINE","copies files and directories between local filesystem and containers in pods"],["TLDR","**Copy file to pod**\n\n```kubectl cp [localfile] [pod-name]:[/path/in/container]```\n\n**Copy file from pod**\n\n```kubectl cp [pod-name]:[/path/in/container] [localfile]```\n\n**Copy to specific container**\n\n```kubectl cp [localfile] [pod-name]:[path] -c [container]```\n\n**Copy entire directory**\n\n```kubectl cp [localdir] [pod-name]:[/remote/dir]```\n\n**Copy from pod in specific namespace**\n\n```kubectl cp [namespace]/[pod-name]:[/path/in/container] [localfile]```\n\n**Copy without preserving permissions**\n\n```kubectl cp [localfile] [pod-name]:[path] --no-preserve```"],["SYNOPSIS","**kubectl** **cp** _src_ _dest_ [_options_]"],["PARAMETERS","**-c**, **--container** _name_\n> Container name in multi-container pod.\n\n**-n**, **--namespace** _name_\n> Kubernetes namespace.\n\n**--no-preserve**\n> Do not preserve file ownership and permissions.\n\n**--retries** _int_\n> Number of retries on network errors (default: 0)."],["DESCRIPTION","**kubectl cp** copies files and directories between local filesystem and containers in pods. Uses tar internally, so tar must be present in the container. Supports copying in both directions."],["CAVEATS","Requires tar to be installed in the container. If tar is missing, the copy will fail. Large file transfers may be slow as data is streamed through the API server. Symlinks in copied directories are not followed. The namespace/pod format uses `/` as separator (e.g., `mynamespace/mypod:/path`)."],["INSTALL","```apt: sudo apt install kubectl```\n\n```pacman: sudo pacman -S kubectl```\n\n```apk: sudo apk add kubectl```\n\n```brew: brew install kubectl```\n\n```nix: nix profile install nixpkgs#kubectl```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [kubectl-exec](/man/kubectl-exec)(1)"]],"e":["kubectl","kubectl-exec"],"x":"kubectl cp [localfile] [pod-name]:[/path/in/container]"},"llvm-mc":{"n":"llvm-mc","t":"LLVM machine code playground","s":[["TAGLINE","LLVM machine code playground"],["TLDR","**Assemble to object file**\n\n```llvm-mc -filetype=obj [input.s] -o [output.o]```\n\n**Disassemble hex bytes** for x86_64\n\n```echo \"0x90\" | llvm-mc --disassemble -triple=x86_64```\n\n**Assemble for a specific target triple**\n\n```llvm-mc -triple=x86_64-linux-gnu [input.s]```\n\n**Show instruction encoding** alongside assembly\n\n```llvm-mc -show-encoding [input.s]```\n\n**Assemble with Intel syntax** instead of AT&T\n\n```llvm-mc -x86-asm-syntax=intel [input.s]```\n\n**Assemble for ARM target**\n\n```llvm-mc -triple=aarch64-linux-gnu -filetype=obj [input.s] -o [output.o]```"],["SYNOPSIS","**llvm-mc** [_options_] [_file_]"],["PARAMETERS","**-filetype**=_type_\n> Output file type: **asm** (assembly listing, default), **obj** (object file), or **null** (no output).\n\n**-triple**=_triple_\n> Target triple for assembly/disassembly (e.g., x86_64-linux-gnu, aarch64-linux-gnu).\n\n**--disassemble**, **-disassemble**\n> Disassemble hex-encoded input bytes into assembly.\n\n**-show-encoding**\n> Print instruction encoding as comments alongside assembly output.\n\n**-show-inst**\n> Show internal LLVM instruction representation in output.\n\n**-o** _file_\n> Output file name (default: stdout).\n\n**-x86-asm-syntax**=_syntax_\n> Assembly syntax for x86: **att** (default) or **intel**.\n\n**-mcpu**=_cpu_\n> Specify target CPU for instruction selection.\n\n**-mattr**=_attributes_\n> Target-specific attributes (e.g., +sse4.2,+avx).\n\n**-output-asm-variant**=_N_\n> Select assembly output variant (0 = AT&T, 1 = Intel for x86).\n\n**-g**\n> Generate DWARF debug information in object output."],["DESCRIPTION","**llvm-mc** is the LLVM machine code playground. It assembles assembly source into machine code and disassembles machine code bytes back into assembly for various target architectures. It is useful for testing assembly syntax, examining instruction encodings, verifying assembler behavior, and low-level debugging.\n\nInput for assembly mode is an assembly source file. Input for disassembly mode (**--disassemble**) is hex-encoded bytes, which can be piped from stdin. When no input file is given, stdin is read."],["INSTALL","```apt: sudo apt install llvm```\n\n```dnf: sudo dnf install llvm```\n\n```pacman: sudo pacman -S llvm```\n\n```apk: sudo apk add llvm```\n\n```zypper: sudo zypper install llvm```\n\n```brew: brew install llvm```\n\n```nix: nix profile install nixpkgs#llvm```"],["SEE ALSO","[llvm-as](/man/llvm-as)(1), [llvm-objdump](/man/llvm-objdump)(1), [as](/man/as)(1), [nasm](/man/nasm)(1), [objdump](/man/objdump)(1)"]],"e":["llvm-as","llvm-objdump","as","nasm","objdump"],"x":"llvm-mc -filetype=obj [input.s] -o [output.o]"},"maestral":{"n":"maestral","t":"lightweight Dropbox client for Linux and macOS","s":[["TAGLINE","lightweight Dropbox client for Linux and macOS"],["TLDR","**Link to Dropbox account**\n\n```maestral link```\n\n**Start syncing**\n\n```maestral start```\n\n**Stop syncing**\n\n```maestral stop```\n\n**Show sync status**\n\n```maestral status```\n\n**Pause syncing**\n\n```maestral pause```\n\n**Resume syncing**\n\n```maestral resume```\n\n**List sync activity**\n\n```maestral activity```\n\n**Exclude folder from sync**\n\n```maestral excluded add [/path/to/folder]```"],["SYNOPSIS","**maestral** [_start_] [_stop_] [_status_] [_link_] [_excluded_] [_options_] [_command_]"],["PARAMETERS","**start**\n> Start the sync daemon.\n\n**stop**\n> Stop the sync daemon.\n\n**restart**\n> Restart the sync daemon.\n\n**status**\n> Show current sync status.\n\n**pause**\n> Pause syncing.\n\n**resume**\n> Resume syncing.\n\n**link**\n> Link to Dropbox account.\n\n**unlink**\n> Unlink Dropbox account.\n\n**activity**\n> Show recent sync activity.\n\n**excluded add** _PATH_\n> Exclude folder from sync.\n\n**excluded remove** _PATH_\n> Include previously excluded folder.\n\n**excluded list**\n> List excluded folders.\n\n**ls** _PATH_\n> List folder contents.\n\n**notify** _LEVEL_\n> Set notification level.\n\n**autostart**\n> Configure automatic startup.\n\n**config**\n> Show configuration.\n\n**-c, --config-name** _NAME_\n> Configuration name for managing multiple Dropbox accounts."],["DESCRIPTION","**maestral** is a lightweight Dropbox client for Linux and macOS. It provides file synchronization without the heavy resource usage of the official client.\n\nThe daemon runs in the background, monitoring local changes and syncing with Dropbox. Selective sync excludes unwanted folders to save disk space.\n\nUnlike the official client, Maestral is open-source and uses fewer system resources. It supports the core sync functionality without extras like camera uploads or screenshots.\n\nMultiple Dropbox accounts can be configured using different configuration names. Each runs as a separate daemon with its own sync folder.\n\nThe status command shows sync progress, conflicts, and errors. Activity shows recent file changes. Notifications alert on sync events."],["CAVEATS","No GUI by default - CLI only (optional GUI available). Some Dropbox features not supported. Requires Python. Initial sync of large accounts takes time."],["HISTORY","**Maestral** was created by **Sam Schott** around **2018** as a lightweight alternative to the official Dropbox client for Linux. The name comes from the Mistral wind. It gained popularity among Linux users wanting Dropbox sync without Electron or heavy dependencies."],["INSTALL","```nix: nix profile install nixpkgs#maestral```"],["SEE ALSO","[rclone](/man/rclone)(1), [syncthing](/man/syncthing)(1)"]],"e":["rclone","syncthing"],"x":"maestral link"},"marzban-cli":{"n":"marzban-cli","t":"CLI for Marzban proxy panel administration","s":[["TAGLINE","CLI for Marzban proxy panel administration"],["TLDR","**List** all administrators\n\n```marzban cli admin list```\n\n**Create** a new administrator\n\n```marzban cli admin create -u [username]```\n\n**List** all users\n\n```marzban cli user list```\n\n**Transfer** ownership of a user to another administrator\n\n```marzban cli user set-owner -u [username] --owner [owner]```\n\nGenerate a **subscription config** for a user\n\n```marzban cli subscription get-config -u [username] -f [v2ray|clash]```\n\nDisplay a user's **subscription link**\n\n```marzban cli subscription get-link -u [username]```\n\nDisplay **help**\n\n```marzban cli --help```"],["SYNOPSIS","**marzban cli** [_OPTIONS_] _COMMAND_ [_ARGS_]..."],["PARAMETERS","**admin**\n> Manage panel administrators (`create`, `delete`, `list`, `update`, `import-from-env`)\n\n**user**\n> Manage users (`list`, `set-owner`)\n\n**subscription**\n> Subscription helpers (`get-config`, `get-link`)\n\n**completion**\n> Generate or install shell completion scripts\n\n**admin create** **-u** _username_\n> Create an admin; optional `--sudo`, `-tg`/`--telegram-id`, `-dc`/`--discord-webhook`\n\n**user list**\n> List users; filter with `-u`, `-s`/`--search`, `--status`, `--admin`/`--owner`\n\n**subscription get-config** **-u** _username_ **-f** {_v2ray_|_clash_}\n> Generate subscription config; optional `-o`/`--output` and `--base64`\n\n**subscription get-link** **-u** _username_\n> Print subscription URL (requires `XRAY_SUBSCRIPTION_URL_PREFIX`)"],["DESCRIPTION","**marzban cli** is the command-line interface for [Marzban](https://github.com/Gozargah/Marzban), an open-source proxy management panel built around Xray. It manages administrators, users, and subscription payloads without using the web UI.\n\nAdmin passwords can be supplied non-interactively via `MARZBAN_ADMIN_PASSWORD`. `admin import-from-env` creates a sudo admin from `SUDO_USERNAME`/`SUDO_PASSWORD` and attaches unowned users to it."],["CAVEATS","Requires a working Marzban installation and database access (typically run on the panel host). `subscription get-link` needs `XRAY_SUBSCRIPTION_URL_PREFIX` set correctly. `admin update` is interactive only."],["HISTORY","Part of **Marzban**, a Python-based multi-protocol proxy panel by Gozargah."],["SEE ALSO","[curl](/man/curl)(1), [docker](/man/docker)(1)"],["RESOURCES","```[Source code](https://github.com/Gozargah/Marzban)```\n\n```[Documentation](https://github.com/Gozargah/Marzban/blob/master/cli/README.md)```"]],"e":["curl","docker"],"x":"marzban cli admin list"},"mdk3":{"n":"mdk3","t":"wireless security testing tool","s":[["TAGLINE","wireless security testing tool"],["TLDR","**Beacon flood attack**\n\n```mdk3 [wlan0] b```\n\n**Authentication DoS**\n\n```mdk3 [wlan0] a -a [BSSID]```\n\n**Deauthentication attack**\n\n```mdk3 [wlan0] d```\n\n**Michael shutdown attack**\n\n```mdk3 [wlan0] m -t [BSSID]```\n\n**Probe request flood**\n\n```mdk3 [wlan0] p```"],["SYNOPSIS","**mdk3** _interface_ _mode_ [_options_]"],["PARAMETERS","_INTERFACE_\n> Wireless interface in monitor mode.\n\n_MODE_\n> Attack mode (b, a, d, m, p).\n\n**b**\n> Beacon flood.\n\n**a**\n> Authentication flood.\n\n**d**\n> Deauthentication.\n\n**-t** _BSSID_\n> Target BSSID.\n\n**--help**\n> Display help information."],["DESCRIPTION","**mdk3** is a wireless security testing tool. It tests 802.11 network resilience.\n\nThe tool performs various attack simulations for security auditing. Requires authorization."],["CAVEATS","Security tool. Legal use only with authorization. Requires monitor mode."],["HISTORY","mdk3 was created for **wireless security** testing and penetration testing of 802.11 networks."],["INSTALL","```apt: sudo apt install mdk3```"],["SEE ALSO","[mdk4](/man/mdk4)(1), [aircrack-ng](/man/aircrack-ng)(1), [aireplay-ng](/man/aireplay-ng)(1)"]],"e":["mdk4","aircrack-ng","aireplay-ng"],"x":"mdk3 [wlan0] b"},"microcodex":{"n":"microcodex","t":"Ultra-lightweight C++ coding agent for the terminal","s":[["TAGLINE","Ultra-lightweight C++ coding agent for the terminal"],["TLDR","**Install** the latest binary and sign in\n\n```curl -fsSL https://github.com/paoloanzn/microcodex/releases/latest/download/install.sh | sh```\n\n```microcodex login```\n\n**Start an interactive session** in the current directory\n\n```microcodex```\n\n**Run a one-shot prompt** without opening the full UI loop first\n\n```microcodex \"[Find the failing test, fix it, and run the relevant test suite]\"```\n\n**Sign in on a headless or remote machine** with device authorization\n\n```microcodex login --device-auth```"],["SYNOPSIS","**microcodex** [_prompt_]\n\n**microcodex** **login** [**--device-auth**]"],["PARAMETERS","**login**\n> Authenticate with a ChatGPT plan via OAuth. Opens a browser URL by default.\n\n**--device-auth**\n> Use device-code authentication for headless or remote machines: print a one-time code and verification URL instead of opening a browser.\n\n_prompt_\n> Optional one-shot task. When given, MicroCodex runs that request; without it, the interactive terminal UI starts."],["DESCRIPTION","**microcodex** is an ultra-lightweight coding agent written in **C++23** that runs locally in your terminal. It reimplements the core of OpenAI/Codex-style agent workflows in a small native binary: one-shot prompts, an interactive TUI, local coding tools (read/write/edit files, run shell commands, glob search), durable conversations, and automatic context compaction.\n\nAuthentication uses a ChatGPT plan through **microcodex login**. Credentials are stored under **$CODEX_HOME**, or **~/.codex** when **CODEX_HOME** is unset — the same home directory layout as OpenAI Codex. MicroCodex also discovers Codex filesystem skills from **$CODEX_HOME/skills** (or **~/.codex/skills**): each skill is a directory with a **SKILL.md** file whose YAML frontmatter includes **name** and **description**. Skill metadata is injected into agent instructions at session start; full skill content is loaded only when the task matches.\n\nLinux runtime needs **libcurl** and **OpenSSL**. Releases ship prebuilt binaries for Linux and macOS (x86_64 and arm64); the install script picks the matching build. Building from source requires a C++23 toolchain, **make**, and the matching development libraries."],["CAVEATS","MicroCodex is **not a sandbox**. Before launching the user's shell it applies a simple **lexical denylist** that blocks forced file removal (**rm -f** / **rm -rf**), **git reset --hard**, forced **git clean**, **git checkout --**, disk-formatting tools, and shutdown commands. That guard is not a shell parser and is not a complete security boundary: unrecognized destructive commands and all file operations run with the same permissions as the MicroCodex process.\n\nMCP support is not implemented yet. Text cannot currently be copied from the terminal while using the interactive UI. Requires a ChatGPT plan (or compatible OAuth path) via **microcodex login**."],["HISTORY","**MicroCodex** was published as an open-source, Apache-2.0-licensed C++ reimplementation of OpenAI Codex-style terminal coding agents, aiming for a sub-megabyte-class native binary with local tools and durable sessions rather than a large runtime stack."],["SEE ALSO","[codex](/man/codex)(1), [claude](/man/claude)(1), [cline](/man/cline)(1), [aider](/man/aider)(1), [opencode](/man/opencode)(1)"],["RESOURCES","```[Source code](https://github.com/paoloanzn/microcodex)```"]],"e":["codex","claude","cline","aider","opencode"],"x":"curl -fsSL https://github.com/paoloanzn/microcodex/releases/latest/download/install.sh | sh"},"mispipe":{"n":"mispipe","t":"pipes two commands and returns the exit status of the first command","s":[["TAGLINE","pipes two commands and returns the exit status of the first command"],["TLDR","**Run pipeline returning first command exit status**\n\n```mispipe \"[command1]\" \"[command2]\"```\n\n**Use in shell pipeline**\n\n```mispipe \"cat /nonexistent\" \"head -n 5\"; echo $?```"],["SYNOPSIS","**mispipe** _command1_ _command2_"],["PARAMETERS","_command1_\n> First command (source).\n\n_command2_\n> Second command (sink)."],["DESCRIPTION","**mispipe** pipes two commands but returns the exit status of the first command instead of the last. Unlike regular shell pipes that return the final command's exit status. Part of moreutils collection."],["INSTALL","```apt: sudo apt install moreutils```\n\n```dnf: sudo dnf install moreutils```\n\n```pacman: sudo pacman -S moreutils```\n\n```apk: sudo apk add moreutils```\n\n```zypper: sudo zypper install moreutils```\n\n```brew: brew install moreutils```\n\n```nix: nix profile install nixpkgs#moreutils```"],["SEE ALSO","[pee](/man/pee)(1), [sponge](/man/sponge)(1)"]],"e":["pee","sponge"],"x":"mispipe \"[command1]\" \"[command2]\""},"nc_openbsd":{"n":"nc_openbsd","t":"OpenBSD rewrite of netcat for TCP/UDP connections and port scanning","s":[["TAGLINE","OpenBSD rewrite of netcat for TCP/UDP connections and port scanning"],["TLDR","**Listen for incoming connections on a port**\n\n```nc.openbsd -l [8080]```\n\n**Connect to a host and port**\n\n```nc.openbsd [host] [port]```\n\n**Scan ports without sending data**\n\n```nc.openbsd -zv [host] [20-30]```\n\n**Keep listening after client disconnects**\n\n```nc.openbsd -k -l [port]```\n\n**Connect via a SOCKS5 proxy**\n\n```nc.openbsd -X 5 -x [proxy_host:proxy_port] [host] [port]```\n\n**Use UDP instead of TCP**\n\n```nc.openbsd -u [host] [port]```\n\n**Transfer a file** (receiver)\n\n```nc.openbsd -l [port] > [received_file]```\n\n**Send CRLF line endings** (useful for HTTP/SMTP)\n\n```nc.openbsd -C [host] [port]```"],["SYNOPSIS","**nc.openbsd** [**-46bCDdFhklNnrStUuvZz**] [**-I** _length_] [**-i** _interval_] [**-M** _ttl_] [**-m** _minttl_] [**-O** _length_] [**-P** _proxy_username_] [**-p** _source_port_] [**-q** _seconds_] [**-s** _sourceaddr_] [**-T** _keyword_] [**-V** _rtable_] [**-W** _recvlimit_] [**-w** _timeout_] [**-X** _proxy_protocol_] [**-x** _proxy_address_[_:port_]] [_destination_] [_port_]"],["PARAMETERS","_DESTINATION_\n> Target hostname or IP address.\n\n_PORT_\n> Port number or range (e.g., 20-30).\n\n**-4**\n> Use IPv4 addresses only.\n\n**-6**\n> Use IPv6 addresses only.\n\n**-C**\n> Send CRLF as line-ending (LF is translated to CR+LF).\n\n**-D**\n> Enable debugging on the socket.\n\n**-d**\n> Do not attempt to read from stdin.\n\n**-F**\n> Pass the first connected socket to stdout using sendmsg(2) and exit.\n\n**-i** _interval_\n> Sleep for interval seconds between lines of text sent and received.\n\n**-k**\n> Keep listening for another connection after the current one completes (requires -l).\n\n**-l**\n> Listen for an incoming connection rather than initiating one.\n\n**-M** _ttl_\n> Set the TTL / hop limit of outgoing packets.\n\n**-m** _minttl_\n> Drop incoming packets whose TTL / hop limit is under minttl.\n\n**-N**\n> Shut down the network socket after EOF on stdin.\n\n**-n**\n> Do not perform domain name resolution.\n\n**-P** _proxy_username_\n> Username to present to a proxy server requiring authentication.\n\n**-p** _source_port_\n> Specify the source port nc should use.\n\n**-q** _seconds_\n> After EOF on stdin, wait the specified number of seconds and then quit.\n\n**-r**\n> Choose source and/or destination ports randomly.\n\n**-S**\n> Enable RFC 2385 TCP MD5 signature option.\n\n**-s** _sourceaddr_\n> Set the source address to send packets from.\n\n**-T** _keyword_\n> Change the IPv4 TOS or IPv6 traffic class value.\n\n**-U**\n> Use Unix-domain sockets.\n\n**-u**\n> Use UDP instead of TCP.\n\n**-V** _rtable_\n> Set the routing table to be used.\n\n**-v**\n> Produce more verbose output.\n\n**-W** _recvlimit_\n> Terminate after receiving recvlimit packets from the network.\n\n**-w** _timeout_\n> Timeout connections that cannot be established or are idle after timeout seconds.\n\n**-X** _proxy_protocol_\n> Proxy protocol: 4 (SOCKS4), 5 (SOCKS5, default), or connect (HTTPS proxy).\n\n**-x** _proxy_address_[:_port_]\n> Connect to destination using the specified proxy.\n\n**-z**\n> Only scan for listening daemons, without sending any data."],["DESCRIPTION","**nc.openbsd** is the OpenBSD rewrite of netcat, a versatile networking utility for reading and writing data across TCP, UDP, and Unix-domain socket connections. It can open connections, send packets, listen on ports, perform port scanning, and handle both IPv4 and IPv6.\n\nCommon uses include simple TCP proxying, shell scripts that interact with network services, network daemon testing, and ad-hoc file transfers. The **-X** and **-x** flags provide built-in SOCKS and HTTPS proxy support, and the **-z** flag enables lightweight port scanning.\n\nUnlike some other netcat implementations, the OpenBSD version does not have **-c** or **-e** options for executing programs on connection, as a security measure."],["CAVEATS","OpenBSD variant of netcat, commonly the default on Debian/Ubuntu systems. Port scanning (**-z**) cannot be used with listen mode (**-l**). The **-k** flag only works with **-l**. No **-c** or **-e** option exists for executing commands, unlike some other netcat variants."],["HISTORY","nc.openbsd is the **OpenBSD** rewrite of the original netcat by Hobbit (1995). The OpenBSD version was developed to provide a cleaner, more secure implementation, removing features like arbitrary command execution (**-e**) and adding proxy support, TLS capabilities (on OpenBSD), and Unix-domain socket support."],["SEE ALSO","[nc](/man/nc)(1), [ncat](/man/ncat)(1), [socat](/man/socat)(1)"]],"e":["nc","ncat","socat"],"x":"nc.openbsd -l [8080]"},"nemo":{"n":"nemo","t":"default file manager for the Cinnamon desktop environment","s":[["TAGLINE","default file manager for the Cinnamon desktop environment"],["TLDR","**Open** the home directory\n\n```nemo```\n\nOpen specific directories in **separate windows**\n\n```nemo [path/to/directory1] [path/to/directory2]```\n\nOpen directories in **tabs**\n\n```nemo --tabs [path/to/directory1] [path/to/directory2]```\n\nOpen with specific **window size**\n\n```nemo --geometry=[600]x[400] [path/to/directory]```\n\n**Close all** windows\n\n```nemo --quit```"],["SYNOPSIS","**nemo** [**--tabs**] [**--geometry** _WxH_] [**--quit**] [_URI_...]"],["PARAMETERS","**--tabs**\n> Open directories in tabs instead of windows\n\n**--geometry _WxH_**\n> Set initial window size (width x height)\n\n**--new-window**\n> Always open a new window\n\n**--quit, -q**\n> Close all Nemo windows\n\n**--no-desktop**\n> Don't manage the desktop\n\n**--no-default-window**\n> Only create windows for explicitly specified URIs\n\n**--version**\n> Display version information\n\n**--help**\n> Display help information"],["DESCRIPTION","**Nemo** is the default file manager for the Cinnamon desktop environment. It is a fork of Nautilus (GNOME Files) with additional features and a more traditional interface.\n\nFeatures include dual-pane view, configurable toolbar, tree view in sidebar, and extensive plugin support. Nemo can manage the desktop icons and integrates with Cinnamon's system services."],["CAVEATS","Desktop management may conflict with other file managers. Some features require Cinnamon desktop integration. Extensions are installed separately from the main package."],["HISTORY","**Nemo** was forked from **Nautilus 3.4** by the **Linux Mint** team in 2012 after Nautilus removed features in version 3.6. It is named after Captain Nemo from Jules Verne's novels, continuing the nautical naming theme."],["INSTALL","```apt: sudo apt install nemo```\n\n```dnf: sudo dnf install nemo```\n\n```pacman: sudo pacman -S nemo```\n\n```apk: sudo apk add nemo```\n\n```zypper: sudo zypper install nemo```\n\n```nix: nix profile install nixpkgs#nemo```"],["SEE ALSO","[nautilus](/man/nautilus)(1), [thunar](/man/thunar)(1), [dolphin](/man/dolphin)(1), [caja](/man/caja)(1)"]],"e":["nautilus","thunar","dolphin","caja"],"x":"nemo"},"nologin":{"n":"nologin","t":"special shell that politely refuses login attempts","s":[["TAGLINE","special shell that politely refuses login attempts"],["TLDR","Set a user's **login shell to nologin** to prevent logging in\n\n```chsh -s nologin [user]```\n\n**Customize message** for users with the login shell of nologin\n\n```echo \"[declined_login_message]\" > /etc/nologin.txt```"],["SYNOPSIS","**nologin** [_options_]"],["PARAMETERS","**-c**, **--command** _command_\n> Ignored (accepted only for shell compatibility).\n\n**-h**, **--help**\n> Display help and exit.\n\n**-V**, **--version**\n> Display version information and exit.\n\nOther shell options (**-i**, **-l**, **--noprofile**, **--norc**, **--posix**, **--rcfile**, **-r**) are also accepted and ignored so that programs invoking a shell do not crash."],["DESCRIPTION","**nologin** displays a message that an account is not available and exits non-zero. It is designed as a replacement shell for accounts that should not be able to log in interactively.\n\nWhen set as a user's login shell, any attempt to log in as that user (via SSH, console, or **su**) is rejected. It is commonly used for system accounts (daemon, nobody, www-data) or to temporarily disable user accounts without deleting them.\n\nIf **/etc/nologin.txt** exists, nologin displays its contents instead of the default message. On Linux (util-linux) the attempt is also written to **syslog**.\n\nNote: per-account **nologin** (this tool) is distinct from the **pam_nologin** PAM module, which disables all non-root logins system-wide via **/etc/nologin** or **/var/run/nologin**."],["CAVEATS","Users with nologin as their shell can still run commands via **su -s /bin/bash username** if the caller has sufficient privileges. For complete account lockout, also lock the password with **passwd -l**. The path to nologin varies: **/usr/sbin/nologin** or **/sbin/nologin**."],["INSTALL","```apt: sudo apt install login```\n\n```apk: sudo apk add util-linux-login```"],["SEE ALSO","[chsh](/man/chsh)(1), [passwd](/man/passwd)(1), [usermod](/man/usermod)(8), [login](/man/login)(1)"]],"e":["chsh","passwd","usermod","login"],"x":"chsh -s nologin [user]"},"o":{"n":"o","t":"snappy, configuration-free terminal text editor and mini-IDE (orbiton)","s":[["TAGLINE","snappy, configuration-free terminal text editor and mini-IDE (orbiton)"],["TLDR","**Open a file**\n\n```o [file]```\n\n**Open at a specific line**\n\n```o [file]:[line]```\n\n**Open at a line and column**\n\n```o [file]:[line]:[column]```\n\n**Open the first file whose name matches a substring**\n\n```o -g [substring]```\n\n**Format a file** instead of editing it\n\n```o -F [file]```\n\n**Build the project** around the given file\n\n```o -b [file]```\n\n**Monitor a file** for changes (tail-like, read-only)\n\n```o -m [file]```\n\n**Paste the clipboard** contents into a new file\n\n```o -p [file]```\n\n**List all digraphs**\n\n```o -s```"],["SYNOPSIS","**o** [_options_] [_file_[:_line_[:_col_]]]"],["PARAMETERS","**-c**, **--copy**\n> Copy the file into the clipboard.\n\n**-p**, **--paste**\n> Paste the clipboard into the given file (combine with `-f` to overwrite).\n\n**-f**, **--force**\n> Ignore file locks when opening.\n\n**-m**, **--monitor**\n> Monitor the file for changes; open read-only.\n\n**-t**, **--list**\n> Print the file with syntax highlighting and exit.\n\n**-b**, **--build**\n> Build the file/project instead of editing.\n\n**-F**, **--format**\n> Format the file instead of editing.\n\n**-k**, **--create-dir**\n> Create parent directories as needed for new files.\n\n**-d**, **--debug**\n> Start in debug (gdb/dlv) mode.\n\n**-r**, **--release**\n> Build in release mode instead of debug when applicable.\n\n**-l**, **--last-command**\n> Print the last used build/format/export command and exit.\n\n**-e**, **--clear-locks**\n> Clear all file locks and close portals.\n\n**-n**, **--no-cache**\n> Do not write history, highscore, or last-command caches.\n\n**-i**, **--input-file** _FILE_\n> Use _FILE_ as stdin for programs launched with Ctrl-Space.\n\n**-a**, **--nano**\n> Emulate Pico/Nano keybindings.\n\n**-o**, **--ollama**\n> Enable Ollama AI features (model from `$OLLAMA_MODEL`).\n\n**-q**, **--quick-help**\n> Always show the quick-help pane at startup.\n\n**-z**, **--no-quick-help**\n> Never show the quick-help pane.\n\n**-x**, **--noapprox**\n> Disable approximate filename matching.\n\n**-g**, **--glob** _SUBSTRING_\n> Open the first filename matching the substring.\n\n**-s**, **--digraphs**\n> List all supported digraphs and exit.\n\n**-v**, **--version**\n> Print version information.\n\n**-h**, **--help**\n> Show help."],["DESCRIPTION","**o** (the executable name of **orbiton**) is a small, snappy, configuration-free text editor and mini-IDE for the terminal. It detects the language from the filename and provides syntax highlighting, rainbow parentheses, a macro recorder, cut/paste \"portals\" between sessions, jump-to-error from compiler output, LSP support, and simple gdb/dlv frontends.\n\nInvoking `o` with a file opens it; appending `:line` or `:line:col` jumps to that position. The editor has no config file by design: most behavior is inferred from file type and environment. A companion binary `ko` opens in light theme; `lo` reduces colors for low-bandwidth terminals."],["CAVEATS","Not related to the shell's built-in `o` alias (if any). Some distros ship orbiton under a different package name (e.g. `orbiton`, `o-editor`). Keybindings differ from vim/emacs; see the built-in quick-help (`Ctrl-o` then `?`)."],["HISTORY","**orbiton** was created by **Alexander F. Rødseth** (xyproto). The project is written in **Go** and is published on GitHub at `xyproto/orbiton` under the BSD 3-clause license."],["SEE ALSO","[vim](/man/vim)(1), [nano](/man/nano)(1), [micro](/man/micro)(1), [emacs](/man/emacs)(1)"]],"e":["vim","nano","micro","emacs"],"x":"o [file]"},"octocov":{"n":"octocov","t":"Toolkit for collecting code coverage and metrics","s":[["TAGLINE","Toolkit for collecting code coverage and metrics"],["TLDR","**Run octocov** using configuration in the current directory\n\n```octocov```\n\n**Generate a coverage badge**\n\n```octocov badge```\n\n**Compare two coverage reports**\n\n```octocov diff [report_a] [report_b]```\n\n**View file-level coverage** for a specific file\n\n```octocov view [file]```\n\n**List all files** tracked in the coverage report\n\n```octocov ls-files```\n\n**Generate a default configuration file**\n\n```octocov init```"],["SYNOPSIS","**octocov** [_command_] [_options_]"],["PARAMETERS","**init**\n> Generate a default `.octocov.yml` configuration file in the current directory.\n\n**ls-files**\n> List all source files recorded in the coverage report.\n\n**view**, **cat**\n> Display file-level coverage details for a given source file.\n\n**diff** [_report_a_] [_report_b_]\n> Compare two coverage reports and show the differences.\n\n**badge**\n> Generate a coverage badge image based on the current report."],["DESCRIPTION","**octocov** is a toolkit for collecting code metrics including code coverage, code-to-test ratio, and test execution time. It works both as a local CLI tool and as a CI tool integrated with GitHub Actions.\n\nFeatures include support for multiple coverage report formats (Go, Ruby, Python, Clover, LCOV, and more), badge generation, pull request commenting, and report storage in GitHub, S3, or BigQuery. It can aggregate reports from multiple repositories for centralized monitoring. Configuration is defined in `.octocov.yml`."],["CAVEATS","Coverage report format support depends on the language and test framework used. CI features such as pull request commenting require GitHub Actions configuration."],["HISTORY","**octocov** was created by **k1LoW** and is written in **Go**. It is available as a GitHub Action via `k1LoW/octocov-action`."],["SEE ALSO","[go-test](/man/go-test)(1)"]],"e":["go-test"],"x":"octocov"},"odps-func":{"n":"odps-func","t":"Manage MaxCompute user-defined functions from odpscmd","s":[["TAGLINE","Manage MaxCompute user-defined functions from odpscmd"],["TLDR","**List functions** in the current project\n\n```list functions;```\n\n**Describe a function**\n\n```desc function [name];```\n\n**Create a function** from an uploaded resource\n\n```create function [name] as '[package.ClassName]' using '[resource.jar]';```\n\n**Drop a function**\n\n```drop function [name];```\n\n**Show built-in functions**\n\n```show builtin functions;```"],["SYNOPSIS","Executed inside the **odpscmd** client as SQL-like statements:\n\n**list** **functions** [**-p** _project_]**;**\n**desc** **function** _name_**;**\n**create** **function** _name_ **as** '_class_' **using** '_resources_'**;**\n**drop** **function** _name_**;**"],["PARAMETERS","**list functions**\n> List user-defined functions (alias: **show functions**).\n\n**desc function** _name_\n> Show a function's owner, creation time, class, and resources.\n\n**create function** _name_ **as** '_class_' **using** '_resources_'\n> Register a new UDF. _class_ is the fully-qualified Java class or Python module; _resources_ is a comma-separated list of already-uploaded resources.\n\n**drop function** _name_\n> Remove a UDF (requires delete permission).\n\n**show builtin functions** [_name_]\n> List or describe MaxCompute built-in functions (client v0.43.0+).\n\n**-p** _project_\n> Operate on a specific project (list functions only)."],["DESCRIPTION","MaxCompute UDFs are managed through SQL-style statements issued from the **odpscmd** command-line client. The resource (jar, py file, archive) must be uploaded first with `add jar` / `add py` / `add archive` before a function can reference it.\n\nFunction names are unique per project. Users need write permission on the project to create functions and delete permission to drop them."],["CAVEATS","Requires a configured odpscmd client with project access. UDFs registered via odpscmd or MaxCompute Studio are not automatically surfaced in DataWorks. For Python UDFs, ensure the Python runtime is supported by the project."],["HISTORY","Part of the **Alibaba Cloud MaxCompute** (ODPS) CLI toolchain. UDF support has been a core MaxCompute feature since its first public release."],["SEE ALSO","[odps](/man/odps)(1), [odps-resource](/man/odps-resource)(1)"]],"e":["odps","odps-resource"],"x":"list functions;"},"pbmpage":{"n":"pbmpage","t":"generates printer calibration and test pages as PBM images","s":[["TAGLINE","generates printer calibration and test pages as PBM images"],["TLDR","**Generate printer test page**\n\n```pbmpage [1] > [output.pbm]```\n\n**Generate A4 test page**\n\n```pbmpage -a4 [1] > [output.pbm]```"],["SYNOPSIS","**pbmpage** [_options_] _pagenum_"],["PARAMETERS","_pagenum_\n> Test page number (1-3).\n\n**-a4**\n> Generate A4 size instead of US letter (default)."],["DESCRIPTION","**pbmpage** generates printer calibration and test pages as PBM images at 600 dpi resolution. Different page numbers produce different test patterns useful for calibrating margins, alignment, and print quality. The default output is for US letter paper; use **-a4** for A4 size. Part of the Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pbmtext](/man/pbmtext)(1), [pbmtolj](/man/pbmtolj)(1)"]],"e":["pbmtext","pbmtolj"],"x":"pbmpage [1] > [output.pbm]"},"pkgctl-auth":{"n":"pkgctl-auth","t":"Manage pkgctl service authentication","s":[["TAGLINE","Manage pkgctl service authentication"],["TLDR","Log in to Arch GitLab for **pkgctl** operations\n\n```pkgctl auth login```\n\nShow whether **credentials** are configured\n\n```pkgctl auth status```"],["SYNOPSIS","**pkgctl auth** _command_"],["PARAMETERS","**login**\n> Authenticate with the GitLab instance\n\n**status**\n> Display current authentication status"],["DESCRIPTION","**pkgctl auth** manages authentication for pkgctl with services like GitLab. It handles login credentials for package repository operations and source control integration.\n\nAuthentication is required for operations that interact with Arch Linux infrastructure, such as releasing packages and managing repository access."],["CAVEATS","Credentials are stored locally. Ensure proper permissions on credential files to prevent unauthorized access."],["HISTORY","Part of **pkgctl**, the Arch Linux package control tool for official repository maintainers."],["SEE ALSO","[pkgctl](/man/pkgctl)(1), [pkgctl-release](/man/pkgctl-release)(1)"]],"e":["pkgctl","pkgctl-release"],"x":"pkgctl auth login"},"postcss":{"n":"postcss","t":"Transform CSS with JavaScript plugins","s":[["TAGLINE","Transform CSS with JavaScript plugins"],["TLDR","**Process CSS file**\n\n```postcss [input.css] -o [output.css]```\n\n**Use config file**\n\n```postcss [input.css] -o [output.css] -c [postcss.config.js]```\n\n**Watch for changes**\n\n```postcss [input.css] -o [output.css] --watch```\n\n**Process directory**\n\n```postcss [src/] -d [dist/]```\n\n**Use specific plugins**\n\n```postcss [input.css] -o [output.css] -u [autoprefixer]```\n\n**Replace file in place**\n\n```postcss [style.css] --replace```\n\n**With source maps**\n\n```postcss [input.css] -o [output.css] --map```"],["SYNOPSIS","**postcss** [_-o output_] [_-d dir_] [_-c config_] [_-u plugins_] [_options_] _files_"],["PARAMETERS","**-o**, **--output** _FILE_\n> Output file.\n\n**-d**, **--dir** _DIR_\n> Output directory.\n\n**-c**, **--config** _FILE_\n> Config file path.\n\n**-u**, **--use** _PLUGINS_\n> Plugins to use.\n\n**-w**, **--watch**\n> Watch mode.\n\n**--map**\n> Generate source maps.\n\n**--replace**\n> Replace input files.\n\n**--no-map**\n> Disable source maps.\n\n**-v**, **--verbose**\n> Verbose output."],["DESCRIPTION","**postcss** is a tool for transforming CSS with JavaScript plugins. Rather than being a preprocessor itself, it provides a framework where plugins perform specific transformations on the CSS abstract syntax tree. Popular plugins include Autoprefixer for adding vendor prefixes, cssnano for minification, and postcss-preset-env for using future CSS syntax today.\n\nPlugins are chained together in a configuration file (postcss.config.js) or specified on the command line. Each plugin receives the parsed CSS, applies its transformation, and passes the result to the next plugin. This modular approach allows teams to compose exactly the processing pipeline they need.\n\nWatch mode reprocesses files automatically when changes are detected, integrating into development workflows. Source maps preserve the mapping between transformed and original CSS for debugging in browser developer tools."],["CONFIGURATION","**postcss.config.js** (or **.postcssrc**, **postcss.config.mjs**)\n> Plugin configuration file defining the processing pipeline. Lists plugins in order with their options. Searched upward from the input file directory.\n\n**package.json** `\"postcss\"` field\n> Alternative location for PostCSS configuration within the project manifest."],["CAVEATS","Requires plugins for functionality. Node.js environment. Config complexity can grow."],["HISTORY","**PostCSS** was created by **Andrey Sitnik** in **2013**. It pioneered CSS-as-AST transformation and hosts a rich plugin ecosystem."],["INSTALL","```nix: nix profile install nixpkgs#postcss```"],["SEE ALSO","[sass](/man/sass)(1), [less](/man/less)(1), [autoprefixer](/man/autoprefixer)(1), [cssnano](/man/cssnano)(1)"]],"e":["sass","less","autoprefixer","cssnano"],"x":"postcss [input.css] -o [output.css]"},"postman":{"n":"postman","t":"API development and testing platform","s":[["TAGLINE","API development and testing platform"],["TLDR","**Launch Postman**\n\n```postman```\n\n**Run collection from CLI**\n\n```newman run [collection.json]```\n\n**Run with environment**\n\n```newman run [collection.json] -e [environment.json]```\n\n**Export results**\n\n```newman run [collection.json] --reporters cli,json --reporter-json-export [results.json]```"],["SYNOPSIS","**postman** [_options_]\n\n**newman** run _collection_ [_options_]"],["PARAMETERS (newman)","**-e**, **--environment** _file_\n> Environment file.\n\n**-g**, **--globals** _file_\n> Global variables.\n\n**-d**, **--iteration-data** _file_\n> Data file for iterations.\n\n**-n** _count_\n> Iteration count.\n\n**--reporters** _list_\n> Output reporters.\n\n**--timeout** _ms_\n> Request timeout."],["DESCRIPTION","**Postman** is an API development platform providing a graphical interface for designing, testing, and documenting HTTP APIs. It organizes requests into collections that can include test scripts, environment variables, and pre-request hooks for building comprehensive API test suites.\n\n**Newman** is the command-line companion for running Postman collections outside the GUI. It enables automated API testing in CI/CD pipelines, supporting environment files, data-driven iterations with CSV or JSON data, and multiple output reporters for integration with test reporting systems."],["EXAMPLES","```bash"],["Launch GUI","postman"],["Run collection with Newman","newman run my-api-tests.json"],["With environment","newman run collection.json -e staging.json"],["Data-driven testing","newman run collection.json -d testdata.csv -n 10"],["Multiple reporters","newman run collection.json --reporters cli,htmlextra\n```"],["COLLECTION FORMAT","Postman collections (JSON) contain:\n- Requests with URLs, methods, headers\n- Tests (JavaScript assertions)\n- Variables and environments"],["CAVEATS","Postman GUI requires account for cloud features. Newman runs locally without account. Free tier has limitations."],["HISTORY","Postman was founded by **Abhinav Asthana** in 2014, evolving from a Chrome extension to a full API platform."],["INSTALL","```nix: nix profile install nixpkgs#postman```"],["SEE ALSO","[curl](/man/curl)(1), [httpie](/man/httpie)(1)"]],"e":["curl","httpie"],"x":"postman"},"pylint":{"n":"pylint","t":"Analyze Python code for errors and style","s":[["TAGLINE","Analyze Python code for errors and style"],["TLDR","**Check Python file**\n\n```pylint [file.py]```\n\n**Check with specific config**\n\n```pylint --rcfile=[.pylintrc] [file.py]```\n\n**Disable specific warnings**\n\n```pylint --disable=[C0114] [file.py]```\n\n**Generate config file**\n\n```pylint --generate-rcfile > [.pylintrc]```\n\n**Output as JSON**\n\n```pylint --output-format=json [file.py]```"],["SYNOPSIS","**pylint** [_options_] _modules_"],["PARAMETERS","_MODULES_\n> Python modules to check.\n\n**--rcfile** _FILE_\n> Configuration file.\n\n**--disable** _IDS_\n> Disable checks.\n\n**--enable** _IDS_\n> Enable checks.\n\n**--output-format** _FORMAT_\n> Output format.\n\n**--generate-rcfile**\n> Generate config."],["DESCRIPTION","**pylint** is a comprehensive static analysis tool that checks Python code for programming errors, coding standard violations, and code smells. It inspects modules without running them, reporting issues organized by category: conventions (C), refactoring suggestions (R), warnings (W), errors (E), and fatal problems (F), each with a numeric code for precise filtering.\n\nThe tool is highly configurable through **.pylintrc** files or **pyproject.toml** sections, allowing teams to enable, disable, or customize individual checks to match project standards. It also produces an overall code quality score out of 10, making it useful for tracking code health over time in CI pipelines. Plugins extend its analysis to frameworks like Django and SQLAlchemy."],["CONFIGURATION","**.pylintrc**\n> Project-level configuration file for enabling/disabling checks, setting thresholds, and defining coding standards.\n\n**pyproject.toml**\n> Project configuration with a `[tool.pylint]` section supporting the same options as .pylintrc.\n\n**setup.cfg**\n> Alternative configuration file with a `[pylint]` section for check settings.\n\n**~/.pylintrc**\n> User-level default configuration applied when no project config is found."],["CAVEATS","Can be slow on large codebases. Highly configurable."],["HISTORY","Pylint was created as a **comprehensive Python** code analysis tool."],["INSTALL","```dnf: sudo dnf install pylint```\n\n```apk: sudo apk add py3-pylint```\n\n```brew: brew install pylint```\n\n```nix: nix profile install nixpkgs#pylint```"],["SEE ALSO","[pyflakes](/man/pyflakes)(1), [flake8](/man/flake8)(1), [mypy](/man/mypy)(1), [ruff](/man/ruff)(1)"]],"e":["pyflakes","flake8","mypy","ruff"],"x":"pylint [file.py]"},"rustup-install":{"n":"rustup-install","t":"install a Rust toolchain","s":[["TAGLINE","install a Rust toolchain"],["TLDR","**Install** the stable toolchain\n\n```rustup install stable```\n\n**Install** the nightly toolchain\n\n```rustup install nightly```\n\n**Install a pinned version**\n\n```rustup install 1.78.0```\n\n**Install a nightly from a specific date**\n\n```rustup install nightly-2025-01-15```\n\n**Install with the minimal profile** (rustc, rust-std, cargo only)\n\n```rustup install stable --profile minimal```\n\n**Install and add components**\n\n```rustup install nightly --component rust-src --component rust-analyzer```\n\n**Install without checking for self-update**\n\n```rustup install stable --no-self-update```"],["SYNOPSIS","**rustup** **install** [_options_] _toolchain_..."],["DESCRIPTION","**rustup install** downloads and installs one or more Rust toolchains. It is an alias for **rustup toolchain install**.\n\nA toolchain is identified by a channel (**stable**, **beta**, **nightly**), an exact version (**1.78.0**), and optionally a date (**nightly-2025-01-15**) and host triple (**stable-x86_64-unknown-linux-gnu**). If the toolchain is already installed it is updated to the latest release on that channel."],["PARAMETERS","**--profile** _NAME_\n> Installation profile: **minimal**, **default**, or **complete**. Controls which components are installed.\n\n**-c**, **--component** _NAME_\n> Add a component to the toolchain (for example `rust-src`, `rust-analyzer`, `llvm-tools`, `miri`). May be repeated.\n\n**-t**, **--target** _TRIPLE_\n> Add a cross-compilation target. May be repeated.\n\n**--no-self-update**\n> Do not update rustup itself while installing the toolchain.\n\n**--force**\n> Reinstall the toolchain even if it appears up to date; on nightly, install even if some requested components are missing.\n\n**--force-non-host**\n> Allow installing a toolchain whose host triple does not match the running machine (introduced in rustup 1.28).\n\n**--allow-downgrade**\n> Allow choosing an older nightly if the most recent one is missing required components.\n\n**-h**, **--help**\n> Print help."],["CAVEATS","By default, rustup checks for and applies its own self-updates whenever a toolchain is installed; disable with **--no-self-update** in CI environments. Components and targets requested at install time must be available for the chosen channel/date or the install fails; with **--force** rustup will install the toolchain anyway, skipping the missing components."],["HISTORY","**rustup** is the official Rust toolchain installer, created by **Brian Anderson** and now maintained by the **Rust project**. **rustup install** has been the canonical way to add new toolchains since rustup's initial release."],["INSTALL","```apt: sudo apt install rustup```\n\n```dnf: sudo dnf install rustup```\n\n```pacman: sudo pacman -S rustup```\n\n```zypper: sudo zypper install rustup```\n\n```brew: brew install rustup```\n\n```nix: nix profile install nixpkgs#rustup```"],["SEE ALSO","[rustup](/man/rustup)(1), [rustup-toolchain](/man/rustup-toolchain)(1), [rustup-uninstall](/man/rustup-uninstall)(1)"]],"e":["rustup","rustup-toolchain","rustup-uninstall"],"x":"rustup install stable"},"salloc":{"n":"salloc","t":"Allocate Slurm cluster resources interactively","s":[["TAGLINE","Allocate Slurm cluster resources interactively"],["TLDR","Start an **interactive shell** on a cluster node\n\n```salloc```\n\n**Execute command** on a cluster node\n\n```salloc ls --all```\n\nAllocate nodes with **constraints**\n\n```salloc -C \"(amd|intel)&gpu\"```"],["SYNOPSIS","**salloc** [_options_] [_command_]"],["PARAMETERS","**-C**, **--constraint** _expr_\n> Node feature constraints"],["DESCRIPTION","**salloc** allocates resources from a Slurm cluster and starts an interactive shell or executes a specified command. It waits for resources to become available before starting.\n\nConstraints can specify required node features like CPU type, GPU availability, or memory configuration."],["CAVEATS","Resources are held until the shell exits or command completes. Allocation time depends on cluster load and requested resources."],["HISTORY","Part of **Slurm** workload manager, providing interactive resource allocation for HPC clusters."],["INSTALL","```apt: sudo apt install slurm-client```"],["SEE ALSO","[srun](/man/srun)(1), [sbatch](/man/sbatch)(1), [squeue](/man/squeue)(1)"]],"e":["srun","sbatch","squeue"],"x":"salloc"},"slabtop":{"n":"slabtop","t":"Display kernel slab cache information","s":[["TAGLINE","Display kernel slab cache information"],["TLDR","Start **slabtop**\n\n```sudo slabtop```\n\nSort by **cache size**\n\n```sudo slabtop -s c```\n\nSort by number of **objects**\n\n```sudo slabtop -s o```\n\nSort by **object size**\n\n```sudo slabtop -s s```\n\nDisplay once and **exit**\n\n```sudo slabtop -o```\n\nSet **refresh delay** to 5 seconds\n\n```sudo slabtop -d 5```"],["SYNOPSIS","**slabtop** [_-d delay_] [_-s sortcriteria_] [_-o_]"],["DESCRIPTION","**slabtop** displays detailed kernel slab cache information in real time. It shows a listing of the top caches sorted by one of the listed sort criteria, helping to monitor kernel memory allocation."],["PARAMETERS","**-d, --delay=N**\n> Refresh the display every N seconds (default: 3)\n\n**-s, --sort=S**\n> Arrange output by specified sort criteria\n\n**-o, --once**\n> Display output once then exit\n\n**-V, --version**\n> Show version and exit\n\n**-h, --help**\n> Show usage help and exit"],["SORT CRITERIA","**a**: Active objects (ACTIVE)\n**b**: Objects per slab (OBJ/SLAB)\n**c**: Cache size (CACHE SIZE)\n**l**: Number of slabs (SLABS)\n**o**: Number of objects (OBJS) - default\n**s**: Object size (OBJ SIZE)\n**u**: Cache utilization (USE)"],["INTERACTIVE COMMANDS","**Space**: Refresh display immediately\n**q**: Exit program\n**s**: Change sort column"],["CAVEATS","Requires root privileges to read kernel slab cache information. Sort criteria can be changed during runtime using the corresponding letter key."],["HISTORY","**slabtop** is part of the **procps-ng** package. It provides insight into the kernel's slab allocator, which manages memory for frequently used kernel objects."],["INSTALL","```apt: sudo apt install procps```\n\n```dnf: sudo dnf install procps-ng```\n\n```pacman: sudo pacman -S procps-ng```\n\n```apk: sudo apk add procps-ng```\n\n```zypper: sudo zypper install procps```\n\n```brew: brew install procps```\n\n```nix: nix profile install nixpkgs#procps```"],["SEE ALSO","[top](/man/top)(1), [vmstat](/man/vmstat)(8), [free](/man/free)(1), [ps](/man/ps)(1)"]],"e":["top","vmstat","free","ps"],"x":"sudo slabtop"},"ss-server":{"n":"ss-server","t":"","s":[["TLDR","**Start server** with inline configuration\n\n```ss-server -s [0.0.0.0] -p [8388] -k \"[password]\" -m [aes-256-gcm]```\n\n**Start server** with a configuration file\n\n```ss-server -c [/etc/shadowsocks/config.json]```\n\n**Start in verbose mode**\n\n```ss-server -c [config.json] -v```\n\n**Start with a specific user**\n\n```ss-server -c [config.json] -a [nobody]```\n\n**Run as daemon**\n\n```ss-server -c [config.json] -f [/var/run/ss-server.pid]```"],["SYNOPSIS","**ss-server** [**-c** _config_] [**-s** _server_] [**-p** _port_] [**-k** _password_] [**-m** _method_] [_options_]"],["PARAMETERS","**-c**, **--config** _file_\n> Path to JSON configuration file.\n\n**-s**, **--server** _address_\n> Server address to bind to.\n\n**-p**, **--server-port** _port_\n> Server port number.\n\n**-k**, **--password** _password_\n> Password for encryption.\n\n**-m**, **--encrypt-method** _method_\n> Encryption method (aes-256-gcm, chacha20-ietf-poly1305, etc.).\n\n**-t**, **--timeout** _seconds_\n> Socket timeout in seconds.\n\n**-a**, **--user** _user_\n> Run as specified user.\n\n**-f**, **--pid-file** _file_\n> PID file path for daemon mode.\n\n**-d**, **--dns** _server_\n> DNS server address.\n\n**--fast-open**\n> Enable TCP fast open.\n\n**--reuse-port**\n> Enable port reuse for multiple instances.\n\n**-u**, **--udp**\n> Enable UDP relay.\n\n**-v**, **--verbose**\n> Enable verbose logging.\n\n**-h**, **--help**\n> Display help information."],["DESCRIPTION","**ss-server** is the server component of Shadowsocks, a secure proxy protocol designed to protect network traffic. It listens for incoming connections from Shadowsocks clients and forwards traffic to destination servers.\n\nThe server encrypts all traffic between client and server using modern cryptographic algorithms. Recommended ciphers include **aes-256-gcm** and **chacha20-ietf-poly1305** (AEAD ciphers). Older stream ciphers are deprecated due to security vulnerabilities.\n\nConfiguration can be provided via command-line arguments or a JSON file. The JSON format supports additional options like multiple server ports and plugin configuration."],["CONFIGURATION","**/etc/shadowsocks-libev/config.json**\n> System-wide JSON configuration file specifying server address, port, password, encryption method, timeout, and optional plugin settings."],["CAVEATS","Avoid deprecated stream ciphers (rc4-md5, aes-cfb, etc.) as they lack authentication. Use AEAD ciphers for security. Running as root is not recommended; use **-a** to drop privileges. TCP fast open requires kernel support and may be blocked by some networks. Check local laws regarding proxy software usage."],["HISTORY","**Shadowsocks** was created by clowwindy in **2012** as a secure proxy protocol. The original Python implementation was followed by ports to various languages. The libev-based implementation (shadowsocks-libev) provides **ss-server** as a high-performance C implementation. The project has been widely adopted and continues development with community contributions despite the original repository's removal."],["INSTALL","```apt: sudo apt install shadowsocks-libev```\n\n```zypper: sudo zypper install shadowsocks-libev```\n\n```brew: brew install shadowsocks-libev```\n\n```nix: nix profile install nixpkgs#shadowsocks-libev```"],["SEE ALSO","[ss-local](/man/ss-local)(1)"]],"e":["ss-local"],"x":"ss-server -s [0.0.0.0] -p [8388] -k \"[password]\" -m [aes-256-gcm]"},"ssh-keyscan":{"n":"ssh-keyscan","t":"Gather SSH public host keys from servers","s":[["TAGLINE","Gather SSH public host keys from servers"],["TLDR","**Scan host for keys**\n\n```ssh-keyscan [hostname]```\n\n**Scan specific port**\n\n```ssh-keyscan -p [port] [hostname]```\n\n**Scan for specific key type**\n\n```ssh-keyscan -t ed25519 [hostname]```\n\n**Scan multiple hosts from file**\n\n```ssh-keyscan -f [hosts_file]```\n\n**Append to known_hosts with hashed hostnames**\n\n```ssh-keyscan -H [hostname] >> ~/.ssh/known_hosts```\n\n**Print keys as SSHFP DNS records**\n\n```ssh-keyscan -D [hostname]```"],["SYNOPSIS","**ssh-keyscan** [_options_] [_host_...]"],["PARAMETERS","**-4**\n> Force IPv4 addresses only.\n\n**-6**\n> Force IPv6 addresses only.\n\n**-c**\n> Request certificates from target hosts instead of plain keys.\n\n**-D**\n> Print keys found as SSHFP DNS records.\n\n**-f** _file_\n> Read hosts from file (use `-` for stdin).\n\n**-H**\n> Hash hostnames and addresses in output.\n\n**-p** _port_\n> Connect to specified port on the remote host.\n\n**-q**\n> Quiet mode; suppress comments in output.\n\n**-t** _type_\n> Key type to fetch: rsa, ed25519, ecdsa, ecdsa-sk, or ed25519-sk. Multiple types can be separated by commas.\n\n**-T** _timeout_\n> Connection timeout in seconds (default 5).\n\n**-v**\n> Verbose mode; print debugging messages."],["DESCRIPTION","**ssh-keyscan** is a utility for gathering SSH public host keys from remote servers. It connects to each specified host and retrieves the server's public keys, outputting them in a format suitable for appending to **~/.ssh/known_hosts** or **/etc/ssh/ssh_known_hosts** files.\n\nThe tool is primarily used for automating host key distribution in environments where manually accepting host keys during first connection is impractical. It can scan multiple hosts in parallel, read host lists from files, and filter by key type (RSA, Ed25519, ECDSA). The **-H** option hashes hostnames in the output for additional privacy."],["INSTALL","```apt: sudo apt install openssh-client```\n\n```apk: sudo apk add openssh-client-common```"],["SEE ALSO","[ssh](/man/ssh)(1), [ssh-keygen](/man/ssh-keygen)(1)"]],"e":["ssh","ssh-keygen"],"x":"ssh-keyscan [hostname]"},"ssh":{"n":"ssh","t":"","s":[["TLDR","**Connect to a remote server**\n\n```ssh [user]@[hostname]```\n\n**Connect on a specific port**\n\n```ssh -p [port] [user]@[hostname]```\n\n**Connect with a specific identity file** (private key)\n\n```ssh -i [~/.ssh/id_rsa] [user]@[hostname]```\n\n**Run a command on remote server**\n\n```ssh [user]@[hostname] \"[command]\"```\n\n**Create a local port forward** (tunnel)\n\n```ssh -L [local_port]:[target_host]:[target_port] [user]@[hostname]```\n\n**Create a remote port forward**\n\n```ssh -R [remote_port]:localhost:[local_port] [user]@[hostname]```\n\n**Create a SOCKS proxy**\n\n```ssh -D [port] [user]@[hostname]```\n\n**Enable X11 forwarding**\n\n```ssh -X [user]@[hostname]```\n\n**Copy SSH key to server** for passwordless login\n\n```ssh-copy-id [user]@[hostname]```"],["SYNOPSIS","**ssh** [**-p** _port_] [**-i** _identity_file_] [**-L** _forward_] [_user_**@**]_hostname_ [_command_]"],["PARAMETERS","**-p** _port_\n> Connect to specified port (default: 22)\n\n**-i** _identity_file_\n> Use specified private key file\n\n**-l** _login_name_\n> Specify username (alternative to user@host)\n\n**-L** [_bind_:]_port_:_host_:_port_\n> Local port forwarding\n\n**-R** [_bind_:]_port_:_host_:_port_\n> Remote port forwarding\n\n**-D** [_bind_:]_port_\n> Dynamic port forwarding (SOCKS proxy)\n\n**-N**\n> Do not execute remote command (for tunnels)\n\n**-f**\n> Go to background after authentication\n\n**-X**\n> Enable X11 forwarding\n\n**-Y**\n> Enable trusted X11 forwarding\n\n**-A**\n> Enable agent forwarding\n\n**-C**\n> Enable compression\n\n**-v**\n> Verbose mode (use -vvv for more detail)\n\n**-o** _option_\n> Set configuration option\n\n**-J** _jump_host_\n> Connect via jump host (ProxyJump)\n\n**-t**\n> Force pseudo-terminal allocation"],["DESCRIPTION","**ssh** (Secure Shell) provides secure encrypted communication between hosts over an insecure network. It's the primary tool for remote server administration and secure file transfer.\n\nAuthentication can use passwords or public key cryptography. Key-based authentication is more secure and convenient; generate keys with **ssh-keygen** and copy public keys to servers with **ssh-copy-id**.\n\nPort forwarding creates encrypted tunnels for other traffic. Local forwarding (**-L**) makes a remote service available locally. Remote forwarding (**-R**) exposes a local service on the remote host. Dynamic forwarding (**-D**) creates a SOCKS proxy.\n\nConfiguration in **~/.ssh/config** can define hosts with custom settings (port, user, identity file, proxy), making complex connections simple."],["CONFIGURATION","**~/.ssh/config**\n> Per-user configuration file defining host aliases, default ports, usernames, identity files, proxy settings, and other connection options.\n\n**~/.ssh/known_hosts**\n> Database of previously verified host keys, used to detect server impersonation.\n\n**/etc/ssh/ssh_config**\n> System-wide SSH client configuration applied to all users."],["CAVEATS","First connection to a new host shows a fingerprint warning. Verify the fingerprint through a trusted channel before accepting.\n\nAgent forwarding (**-A**) is convenient but risky on untrusted servers. Consider **ProxyJump** instead.\n\nKeep private keys secure with permissions 600. Protect keys with passphrases and use ssh-agent to avoid repeated passphrase entry.\n\nFirewall rules may block SSH. Port 22 is commonly filtered; consider running SSH on alternate ports for such networks."],["INSTALL","```apt: sudo apt install openssh-client```\n\n```apk: sudo apk add dropbear-ssh```"],["SEE ALSO","[scp](/man/scp)(1), [sftp](/man/sftp)(1), [ssh-keygen](/man/ssh-keygen)(1), [ssh-copy-id](/man/ssh-copy-id)(1), [ssh-agent](/man/ssh-agent)(1)"]],"e":["scp","sftp","ssh-keygen","ssh-copy-id","ssh-agent"],"x":"ssh [user]@[hostname]"},"sx":{"n":"sx","t":"Simple alternative to xinit and startx","s":[["TAGLINE","Simple alternative to xinit and startx"],["TLDR","**Start an X session** running the commands in _$XDG_CONFIG_HOME/sx/sxrc_\n\n```sx```\n\n**Start an X session** running a specific window manager directly\n\n```sx [dwm]```\n\n**Start an X session** with an explicit command line\n\n```sx [exec] [i3]```\n\n**Run** a one-off X program after starting the server\n\n```sx [xterm]```"],["SYNOPSIS","**sx** [_command_ [_args_...]]"],["DESCRIPTION","**sx** is a small POSIX shell script that starts an **Xorg** server, sets up an authority cookie, and then runs a user-supplied command (or _sxrc_) as the X session. It is intended as a minimal, transparent replacement for **xinit** and **startx**, exposing the same basic mechanics without their many optional behaviors.\n\nThe X server is started on the same TTY where **sx** is invoked, with the display number derived from the TTY number (so VT1 becomes _:1_). A fresh MIT-MAGIC-COOKIE-1 is generated from _/dev/urandom_ and registered with **xauth** before the server is launched with **-keeptty -noreset**.\n\nIf no arguments are passed, **sx** runs the executable file at _$XDG_CONFIG_HOME/sx/sxrc_ (defaulting to _~/.config/sx/sxrc_). Otherwise the supplied command and its arguments become the session leader: when it exits, **sx** tears down the server."],["CONFIGURATION","**$XDG_CONFIG_HOME/sx/sxrc**\n> Executable script run as the X session when **sx** is invoked without arguments. Must have the executable bit set. A typical _sxrc_ launches a window manager as the last (foreground) command, for example:\n\n```\n#!/bin/sh\nxrdb -merge ~/.Xresources &\nxsetroot -cursor_name left_ptr &\nexec dwm\n```\n\n**$XAUTHORITY**\n> Path to the X authority file. **sx** generates this file under _$XDG_DATA_HOME/sx/_ and exports it for child processes."],["PARAMETERS","**sx** does not parse flags of its own. Any arguments are treated as the session command and its arguments. Use **--** before the command if it begins with something that could be mistaken for an option."],["CAVEATS","The Xorg command line is **hardcoded**: server flags cannot be customized through **sx** itself. Only **Unix domain** sockets are configured; TCP listening is not enabled. The display number is tied to the TTY number, so starting **sx** on _tty1_ always produces display _:1_, which differs from **startx**, where _:0_ is the default. **sx** requires **Xorg**, **xauth**, and a readable _/dev/urandom_; error checking is intentionally minimal, leaving most reporting to the underlying tools."],["HISTORY","**sx** was written by **Earnest Wieczorek** (Earnestly) as a proof-of-concept response to the comment in **xinit(1)** urging \"site administrators\" to write nicer wrappers. First published on GitHub around **2015**, it has remained a tiny shell script (around 70 lines) and is packaged in several distributions, notably **Arch Linux**, as a lightweight way to launch an X session without **startx**."],["INSTALL","```apt: sudo apt install lrzsz```\n\n```dnf: sudo dnf install lrzsz```\n\n```pacman: sudo pacman -S lrzsz```\n\n```apk: sudo apk add sx```\n\n```brew: brew install lrzsz```\n\n```nix: nix profile install nixpkgs#lrzsz```"],["SEE ALSO","[xinit](/man/xinit)(1), [startx](/man/startx)(1), [Xorg](/man/Xorg)(1), [xauth](/man/xauth)(1)"]],"e":["xinit","startx","Xorg","xauth"],"x":"sx"},"sxhkd":{"n":"sxhkd","t":"Simple X11 hotkey daemon","s":[["TAGLINE","Simple X11 hotkey daemon"],["TLDR","**Start sxhkd** with default configuration\n\n```sxhkd &```\n\n**Start with a custom configuration** file\n\n```sxhkd -c [~/.config/sxhkd/custom_sxhkdrc] &```\n\n**Start with status output** to a FIFO\n\n```sxhkd -s [/tmp/sxhkd.fifo] &```\n\n**Redirect command output** to a file\n\n```sxhkd -r [/tmp/sxhkd.log] &```\n\n**Reload configuration** without restarting\n\n```pkill -USR1 sxhkd```\n\n**Toggle hotkey grabbing**\n\n```pkill -USR2 sxhkd```\n\n**Set chord chain timeout** to 3 seconds\n\n```sxhkd -t 3 &```"],["SYNOPSIS","**sxhkd** [**-hv**] [**-m** _count_] [**-t** _timeout_] [**-c** _config_file_] [**-r** _redir_file_] [**-s** _status_fifo_] [_config_file_...]"],["PARAMETERS","**-h**\n> Display help and exit\n\n**-v**\n> Display version and exit\n\n**-c** _config_file_\n> Read configuration from specified file instead of default\n\n**-r** _redir_file_\n> Redirect command output to specified file\n\n**-s** _status_fifo_\n> Write status information to specified FIFO\n\n**-t** _timeout_\n> Chord chain timeout in seconds\n\n**-m** _count_\n> Handle first count mapping notify events (for keyboard layout changes)"],["DESCRIPTION","**sxhkd** (Simple X Hotkey Daemon) listens for keyboard and mouse events on X11 and executes commands defined in its configuration file. It is window manager agnostic and commonly used with tiling window managers like **bspwm**.\n\nConfiguration is read from **~/.config/sxhkd/sxhkdrc** by default. Each binding consists of a hotkey line followed by a command line (indented with whitespace). Modifiers include **super**, **alt**, **ctrl**, **shift**, and **mod1-mod5**.\n\nKey features include chorded bindings (sequences like **super + a ; b**), key release events (prefix keysym with **@**), and pointer buttons (**button1-button24**). Sequences with **{a,b,c}** expand to multiple bindings.\n\nCommands execute via **$SXHKD_SHELL** or **$SHELL**. Send **SIGUSR1** to reload configuration and **SIGUSR2** to toggle hotkey grabbing."],["CONFIGURATION","**~/.config/sxhkd/sxhkdrc**\n> Default configuration file defining hotkey bindings and their associated commands."],["CAVEATS","sxhkd only works on X11; it does not support Wayland. Keysym names are determined by **xev**. Some key combinations may conflict with window managers or other applications grabbing the same keys."],["HISTORY","**sxhkd** was created by **Bastien Dejean** (baskerville), the same developer behind **bspwm**. It was first released around **2013** as a standalone hotkey daemon that could work with any window manager, addressing the need for a simple, scriptable keybinding system independent of the window manager."],["INSTALL","```apt: sudo apt install sxhkd```\n\n```dnf: sudo dnf install sxhkd```\n\n```pacman: sudo pacman -S sxhkd```\n\n```apk: sudo apk add sxhkd```\n\n```zypper: sudo zypper install sxhkd```\n\n```nix: nix profile install nixpkgs#sxhkd```"],["SEE ALSO","[xev](/man/xev)(1), [xdotool](/man/xdotool)(1), [bspwm](/man/bspwm)(1), [xbindkeys](/man/xbindkeys)(1)"]],"e":["xev","xdotool","bspwm","xbindkeys"],"x":"sxhkd &"},"systemctl-service-watchdogs":{"n":"systemctl-service-watchdogs","t":"Get or set the global service watchdog state","s":[["TAGLINE","Get or set the global service watchdog state"],["TLDR","Show current **watchdog** status\n\n```systemctl service-watchdogs```\n\n**Enable** service watchdogs\n\n```systemctl service-watchdogs yes```\n\n**Disable** service watchdogs\n\n```systemctl service-watchdogs no```"],["SYNOPSIS","**systemctl service-watchdogs** [_BOOL_]"],["PARAMETERS","**yes**\n> Enable the service watchdog logic globally.\n\n**no**\n> Disable the service watchdog logic globally."],["DESCRIPTION","**systemctl service-watchdogs** gets or sets the global state of service runtime watchdogs. When enabled, systemd monitors services with WatchdogSec= configured and sends SIGABRT to kill them if they fail to send a keep-alive notification within the timeout period.\n\nDisabling watchdogs globally stops systemd from killing and restarting unresponsive services, useful during debugging.\n\nWhen called without an argument, prints the current global watchdog state."],["CAVEATS","Disabling watchdogs may allow hung services to persist indefinitely. Changes are temporary and reset on reboot. Re-enabling watchdogs with `yes` does not resume monitoring for services whose watchdog already fired while disabled; those services must be restarted."],["HISTORY","The **service-watchdogs** subcommand provides a global kill switch for the watchdog mechanism, useful when debugging services that would otherwise be restarted before investigation completes."],["SEE ALSO","[systemctl](/man/systemctl)(1), [systemctl-status](/man/systemctl-status)(1), [systemctl-restart](/man/systemctl-restart)(1), [journalctl](/man/journalctl)(1)"]],"e":["systemctl","systemctl-status","systemctl-restart","journalctl"],"x":"systemctl service-watchdogs"},"tcpdump":{"n":"tcpdump","t":"Capture and analyze network packets","s":[["TAGLINE","Capture and analyze network packets"],["TLDR","**Capture packets** on default interface\n\n```tcpdump```\n\n**Capture on specific interface**\n\n```tcpdump -i [eth0]```\n\n**Capture only packets** to/from host\n\n```tcpdump host [192.168.1.1]```\n\n**Capture packets** on specific port\n\n```tcpdump port [80]```\n\n**Capture and save** to file\n\n```tcpdump -w [capture.pcap]```\n\n**Read packets** from file\n\n```tcpdump -r [capture.pcap]```\n\n**Capture with verbose output**\n\n```tcpdump -v```\n\n**Capture HTTP traffic**\n\n```tcpdump -A port [80]```\n\n**Capture packets** with specific protocol\n\n```tcpdump icmp```\n\n**Capture limited number** of packets\n\n```tcpdump -c [100]```\n\n**Don't resolve hostnames**\n\n```tcpdump -n```"],["SYNOPSIS","**tcpdump** [_options_] [_filter expression_]"],["DESCRIPTION","**tcpdump** is a packet analyzer that captures and displays network traffic. It uses libpcap to capture packets from network interfaces and can filter traffic using Berkeley Packet Filter (BPF) syntax.\n\nThe tool can capture packets in real-time, display their contents in various formats, and save them to files for later analysis. Output can show packet headers, full content, or hexadecimal dumps.\n\ntcpdump is essential for network troubleshooting, security analysis, and protocol debugging. It's the command-line counterpart to graphical tools like Wireshark."],["PARAMETERS","**-i** _interface_\n> Capture on specific interface.\n\n**-w** _file_\n> Write packets to file.\n\n**-r** _file_\n> Read packets from file.\n\n**-c** _count_\n> Capture only count packets.\n\n**-n**\n> Don't resolve hostnames.\n\n**-nn**\n> Don't resolve hostnames or ports.\n\n**-v**, **-vv**, **-vvv**\n> Verbose output levels.\n\n**-A**\n> Print packets in ASCII.\n\n**-X**\n> Print packets in hex and ASCII.\n\n**-s** _snaplen_\n> Capture snaplen bytes per packet (0=full).\n\n**-e**\n> Print link-layer header.\n\n**-q**\n> Quick output (less protocol info).\n\n**-D**, **--list-interfaces**\n> List available interfaces.\n\n**-t**\n> Don't print timestamp on each line.\n\n**-tt**\n> Print unformatted timestamp on each line.\n\n**-p**, **--no-promiscuous-mode**\n> Don't put the interface into promiscuous mode.\n\n**-F** _file_\n> Use file as input for the filter expression."],["FILTER EXPRESSIONS","**host** _ip_\n> Match packets to/from the given host.\n\n**net** _cidr_\n> Match packets within the given network (e.g., `net 10.0.0.0/8`).\n\n**port** _num_\n> Match packets with the given source or destination port.\n\n**portrange** _lo-hi_\n> Match packets within a port range.\n\n**src** / **dst**\n> Restrict the preceding primitive to source or destination.\n\n**tcp** / **udp** / **icmp**\n> Match packets of the given protocol.\n\n**and** / **or** / **not**\n> Combine primitives with boolean logic.\n\nExample: `tcpdump 'tcp port 80 and host 192.168.1.1'`"],["CAVEATS","Requires root privileges. Packet capture can impact performance on high-traffic networks. Full packet capture uses significant disk space. Some protocols are encrypted and contents cannot be viewed."],["HISTORY","**tcpdump** was originally written by **Van Jacobson**, **Craig Leres**, and **Steven McCanne** at the **Lawrence Berkeley National Laboratory** in **1988**. It became the foundation for network packet analysis on Unix systems. The libpcap library was extracted from tcpdump and is now used by many network analysis tools including Wireshark."],["INSTALL","```apt: sudo apt install tcpdump```\n\n```dnf: sudo dnf install tcpdump```\n\n```pacman: sudo pacman -S tcpdump```\n\n```apk: sudo apk add tcpdump```\n\n```zypper: sudo zypper install tcpdump```\n\n```brew: brew install tcpdump```\n\n```nix: nix profile install nixpkgs#tcpdump```"],["SEE ALSO","[wireshark](/man/wireshark)(1), [tshark](/man/tshark)(1), [ngrep](/man/ngrep)(1), [iftop](/man/iftop)(1)"]],"e":["wireshark","tshark","ngrep","iftop"],"x":"tcpdump"},"tfsec":{"n":"tfsec","t":"Terraform static security scanner","s":[["TAGLINE","Terraform static security scanner"],["TLDR","**Scan the current directory** for security issues\n\n```tfsec```\n\n**Scan a specific directory**\n\n```tfsec [path/to/terraform]```\n\n**Output results in JSON format**\n\n```tfsec --format json```\n\n**Exclude specific checks** from the scan\n\n```tfsec -e [check1,check2]```\n\n**Scan with a specific tfvars file**\n\n```tfsec --tfvars-file [terraform.tfvars]```\n\n**Only show high severity issues**\n\n```tfsec --minimum-severity HIGH```\n\n**Save results to a file**\n\n```tfsec --format json --out [results.json]```\n\n**Exclude directories** from the scan\n\n```tfsec --exclude-path [path/to/exclude]```"],["SYNOPSIS","**tfsec** [_options_] [_directory_]"],["PARAMETERS","**--format** _format_\n> Output format: **default**, **json**, **csv**, **checkstyle**, **sarif**, **junit**, **gif**, or **text**.\n\n**--out** _file_\n> Write results to a file instead of stdout.\n\n**-e**, **--exclude** _checks_\n> Comma-separated list of check IDs to exclude from the scan.\n\n**--exclude-path** _path_\n> Exclude specific directories from scanning.\n\n**--minimum-severity** _level_\n> Filter results by minimum severity: **LOW**, **MEDIUM**, **HIGH**, or **CRITICAL**.\n\n**--tfvars-file** _file_\n> Include variable values from a tfvars file in the scan.\n\n**--config-file** _file_\n> Use a custom configuration file (default: tfsec-config.json).\n\n**--no-colour**, **--no-color**\n> Disable colored output (useful for CI pipelines).\n\n**--soft-fail**\n> Exit with code 0 even if issues are found.\n\n**--include-ignored**\n> Include results that have been marked as ignored.\n\n**--include-passed**\n> Include checks that passed in the output.\n\n**--version**\n> Display version information and exit.\n\n**-h**, **--help**\n> Display help message and exit."],["DESCRIPTION","**tfsec** is a static analysis security scanner for Terraform code. It analyzes Terraform files to detect potential security misconfigurations, policy violations, and best practice issues before infrastructure is deployed.\n\nThe scanner runs locally and in CI/CD pipelines, providing developer-friendly output with documented checks. It supports multiple output formats for integration with various tools and platforms. Issues are categorized by severity to help prioritize remediation efforts.\n\ntfsec can be configured via command-line flags, configuration files, or environment variables (prefixed with **TFSEC_**). Inline comments can also be used to ignore specific warnings in Terraform files."],["CAVEATS","tfsec is being deprecated and merged into **Trivy**. For new projects, consider using **trivy config** instead. Existing tfsec users should plan migration to Trivy for continued updates and support."],["HISTORY","tfsec was created by **Liam Galvin** and released in **2019** as an open-source Terraform security scanner. It was later acquired by **Aqua Security** in **2021** and became part of their open-source security tooling portfolio. In **2023**, Aqua announced that tfsec would be merged into their comprehensive security scanner **Trivy**, with tfsec entering maintenance mode."],["INSTALL","```brew: brew install tfsec```\n\n```nix: nix profile install nixpkgs#tfsec```"],["SEE ALSO","[trivy](/man/trivy)(1), [terraform](/man/terraform)(1), [checkov](/man/checkov)(1), [terrascan](/man/terrascan)(1)"]],"e":["trivy","terraform","checkov","terrascan"],"x":"tfsec"},"thrift":{"n":"thrift","t":"Apache Thrift cross-language code generator","s":[["TAGLINE","Apache Thrift cross-language code generator"],["TLDR","**Generate code** for a specific language from a Thrift file\n\n```thrift --gen [language] [file.thrift]```\n\n**Generate code** in a specific output directory\n\n```thrift --gen [language] -o [output_directory] [file.thrift]```\n\n**Generate code** with language-specific options\n\n```thrift --gen [language]:key1=val1,key2=val2 [file.thrift]```\n\n**Generate code** including all referenced Thrift files\n\n```thrift -r --gen [language] [file.thrift]```\n\n**Add include search paths** for Thrift imports\n\n```thrift -I [include_dir] --gen [language] [file.thrift]```\n\n**List all available generators**\n\n```thrift --help```"],["SYNOPSIS","**thrift** [**-o** _dir_] [**-out** _dir_] [**-I** _dir_] [**-nowarn**] [**-strict**] [**-v**] [**-r**] [**-debug**] **--gen** _lang_[:_options_] _file.thrift_"],["PARAMETERS","**--gen** _lang_[:_key=val,..._]\n> Generate code with the specified generator. Language options can be passed as comma-separated key=value pairs.\n\n**-o** _dir_\n> Set the output directory for gen-* packages. Default is the current directory.\n\n**-out** _dir_\n> Set the output location directly without creating a gen-* folder.\n\n**-I** _dir_\n> Add a directory to search for include directives. Can be specified multiple times.\n\n**-r**, **--recurse**\n> Also generate code for included Thrift files recursively.\n\n**-nowarn**\n> Suppress all compiler warnings.\n\n**-strict**\n> Enable strict compiler warnings.\n\n**-v**, **--verbose**\n> Enable verbose output mode.\n\n**-debug**\n> Print parse debug trace to stdout.\n\n**-version**\n> Print the compiler version and exit."],["DESCRIPTION","**thrift** is the compiler for Apache Thrift, a framework for scalable cross-language services development. It combines a software stack with a code generation engine to build efficient and seamless services between languages like C++, Java, Python, PHP, Ruby, Erlang, Perl, Haskell, C#, JavaScript, Node.js, and more.\n\nThe compiler reads a Thrift Interface Definition Language (IDL) file containing struct and service definitions, then generates language-specific implementation code. This enables developers to define data types and service interfaces once and generate the boilerplate code for multiple programming languages.\n\nCommon generators include: **cpp**, **java**, **py**, **rb**, **php**, **js**, **go**, **rs** (Rust), **swift**, and many others. Run **thrift** without arguments to see all available generators for your installation."],["CAVEATS","Generated code structure and options vary significantly between language generators. Some generators may require additional runtime libraries to be installed. The **-nowarn** flag is generally discouraged as it can hide important compatibility issues."],["HISTORY","Apache Thrift was originally developed at **Facebook** in **2007** and was open-sourced in April **2008**. It became an Apache Incubator project in May 2008 and graduated to a top-level Apache project in **October 2010**. The framework was designed to enable efficient and reliable communication across programming languages at scale."],["INSTALL","```apt: sudo apt install thrift-compiler```\n\n```dnf: sudo dnf install thrift```\n\n```pacman: sudo pacman -S thrift```\n\n```apk: sudo apk add thrift```\n\n```zypper: sudo zypper install thrift```\n\n```brew: brew install thrift```\n\n```nix: nix profile install nixpkgs#thrift```"],["SEE ALSO","[protoc](/man/protoc)(1), [grpc](/man/grpc)(1)"]],"e":["protoc","grpc"],"x":"thrift --gen [language] [file.thrift]"},"timedatectl":{"n":"timedatectl","t":"Control system time and timezone","s":[["TAGLINE","Control system time and timezone"],["TLDR","Show current **time and date** settings\n\n```timedatectl```\n\nSet **local time** directly\n\n```timedatectl set-time \"2024-01-15 14:30:00\"```\n\nList available **timezones**\n\n```timedatectl list-timezones```\n\nSet system **timezone**\n\n```timedatectl set-timezone America/New_York```\n\nEnable **NTP synchronization**\n\n```timedatectl set-ntp on```\n\nSet hardware clock to **local time**\n\n```timedatectl set-local-rtc 1```"],["SYNOPSIS","**timedatectl** [_OPTIONS_] [_COMMAND_]"],["DESCRIPTION","**timedatectl** queries and changes the system clock and its settings. It controls the system time, timezone, and network time synchronization through systemd-timedated."],["COMMANDS","**status**\n> Show current time/date settings (default command)\n\n**show**\n> Show properties in machine-readable format\n\n**set-time TIME**\n> Set system time (YYYY-MM-DD HH:MM:SS format)\n\n**set-timezone ZONE**\n> Set system timezone\n\n**list-timezones**\n> List available timezones\n\n**set-local-rtc BOOL**\n> Control RTC in local time (0=UTC, 1=local)\n\n**set-ntp BOOL**\n> Enable or disable NTP synchronization\n\n**timesync-status**\n> Show status of systemd-timesyncd"],["PARAMETERS","**--no-pager**\n> Do not pipe output into a pager\n\n**--no-ask-password**\n> Do not prompt for password\n\n**-H, --host**\n> Execute operation on remote host\n\n**-M, --machine**\n> Execute operation on container\n\n**-p, --property**\n> Show specific property\n\n**--value**\n> Show only property values"],["CAVEATS","Setting time manually (set-time) disables NTP synchronization. RTC in local time (set-local-rtc 1) can cause issues with dual-boot systems during DST changes."],["HISTORY","**timedatectl** is part of **systemd**, introduced to provide a unified interface for time/date management replacing older tools like hwclock and tzselect."],["INSTALL","```apt: sudo apt install systemd```\n\n```dnf: sudo dnf install systemd```\n\n```pacman: sudo pacman -S systemd```\n\n```zypper: sudo zypper install systemd```\n\n```brew: brew install systemd```\n\n```nix: nix profile install nixpkgs#systemd```"],["SEE ALSO","[hwclock](/man/hwclock)(8), [date](/man/date)(1)"]],"e":["hwclock","date"],"x":"timedatectl"},"typst":{"n":"typst","t":"Modern typesetting system and compiler","s":[["TAGLINE","Modern typesetting system and compiler"],["TLDR","**Compile document to PDF**\n\n```typst compile [document.typ]```\n\n**Compile with specific output**\n\n```typst compile [document.typ] [output.pdf]```\n\n**Watch and recompile on changes**\n\n```typst watch [document.typ]```\n\n**Compile to PNG**\n\n```typst compile [document.typ] [output.png]```\n\n**Compile to SVG**\n\n```typst compile [document.typ] [output.svg]```\n\n**List available fonts**\n\n```typst fonts```\n\n**Compile with custom font path**\n\n```typst compile --font-path [/path/to/fonts] [document.typ]```\n\n**Initialize new project**\n\n```typst init```"],["SYNOPSIS","**typst** _command_ [_--font-path path_] [_options_] _input_ [_output_]"],["PARAMETERS","**compile** _INPUT_ [_OUTPUT_]\n> Compile document to output.\n\n**watch** _INPUT_\n> Watch for changes and recompile.\n\n**fonts**\n> List available fonts.\n\n**init** [_TEMPLATE_]\n> Initialize new project.\n\n**query** _INPUT_ _SELECTOR_\n> Query document metadata.\n\n**--root** _DIR_\n> Project root directory.\n\n**--font-path** _PATH_\n> Additional font search path.\n\n**-f**, **--format** _FORMAT_\n> Force output format (pdf, png, svg). Normally inferred from output extension.\n\n**--ppi** _NUM_\n> Pixels per inch for raster output (default 144).\n\n**--pages** _RANGE_\n> Comma-separated list of pages to export (e.g. 1-3,5).\n\n**--diagnostic-format** _FMT_\n> Diagnostic format: human, short.\n\n**--input** _KEY=VALUE_\n> Set input variable.\n\n**--open**\n> Open output after compile.\n\n**-h**, **--help**\n> Show help.\n\n**-V**, **--version**\n> Show version."],["DESCRIPTION","**Typst** is a modern typesetting system designed as an alternative to LaTeX. It combines the power of LaTeX with simpler syntax and faster compilation.\n\nDocuments use .typ files with markup similar to Markdown. Headers, lists, emphasis, and links use familiar syntax. Mathematical equations use a LaTeX-inspired but simpler notation.\n\nCompilation is incremental and extremely fast - documents compile in milliseconds. Watch mode provides near-instant preview updates during editing.\n\nStyling uses a functional approach. Set rules configure defaults; show rules transform elements. The language is Turing-complete, enabling complex documents with scripting.\n\nTemplates and packages extend functionality. Bibliography support uses BibTeX or Hayagriva formats. Multi-column layouts, figures, and tables work intuitively.\n\nOutput formats include PDF (default), PNG pages, and SVG. Font embedding ensures documents render correctly anywhere."],["CAVEATS","Newer ecosystem with fewer packages than LaTeX. Some advanced features still developing. Not all LaTeX packages have equivalents. PDF/A for archival not yet supported. Limited IDE integration compared to LaTeX."],["HISTORY","**Typst** was created by **Martin Haug** and **Laurenz Mädje** around **2019** as a master's thesis project. The goal was creating a typesetting system that's as powerful as LaTeX but more approachable. After development at Typst GmbH, it was open-sourced in **2023** and rapidly gained adoption."],["INSTALL","```pacman: sudo pacman -S typst```\n\n```apk: sudo apk add typst```\n\n```zypper: sudo zypper install typst```\n\n```brew: brew install typst```\n\n```nix: nix profile install nixpkgs#typst```"],["SEE ALSO","[pdflatex](/man/pdflatex)(1), [pandoc](/man/pandoc)(1), [groff](/man/groff)(1), [asciidoctor](/man/asciidoctor)(1)"]],"e":["pdflatex","pandoc","groff","asciidoctor"],"x":"typst compile [document.typ]"},"unflatten":{"n":"unflatten","t":"Improve Graphviz graph aspect ratio","s":[["TAGLINE","Improve Graphviz graph aspect ratio"],["TLDR","**Adjust graph layout** and write to output file\n\n```unflatten [input.dot] -o [output.dot]```\n\n**Stagger leaf edge lengths** between 1 and the specified limit\n\n```unflatten -l [3] [input.dot]```\n\n**Fan out nodes** with indegree and outdegree of 1 (requires -l)\n\n```unflatten -f -l [3] [input.dot]```\n\n**Form disconnected nodes** into chains of up to the specified length\n\n```unflatten -c [5] [input.dot]```\n\n**Pipe through dot** for improved layout\n\n```unflatten -l [4] [input.dot] | dot -Tpng -o [output.png]```"],["SYNOPSIS","**unflatten** [_-l limit_] [_-f_] [_-c len_] [_-o file_] _input_"],["PARAMETERS","**-l** _len_\n> Stagger the minimum length of leaf edges between 1 and _len_\n\n**-f**\n> Fan out nodes whose indegree and outdegree are both 1 (only works with -l)\n\n**-c** _len_\n> Form disconnected nodes into chains of up to _len_ nodes\n\n**-o** _file_\n> Write output to _file_ instead of stdout"],["DESCRIPTION","**unflatten** is a Graphviz preprocessor that improves the aspect ratio of directed graphs by breaking up long chains of nodes. It works by staggering the nodes in chains, converting some edges to increase the width of the graph relative to its height.\n\nThe tool is designed to be used as a preprocessing step before the **dot** layout engine. When a graph has many nodes arranged in long vertical chains, dot tends to produce very tall, narrow layouts. Running unflatten first adjusts the graph structure so that dot produces a more balanced, readable result.\n\nOptions control the maximum chain length before staggering occurs and whether leaf nodes should be fanned out. The tool reads DOT format input and produces modified DOT output that can be piped directly into dot or other Graphviz layout engines."],["CAVEATS","Designed as a preprocessing step before the **dot** layout engine. The **-f** option has no effect without **-l**. Graphviz must be installed."],["HISTORY","**unflatten** is part of **Graphviz**, used to improve the aspect ratio of graphs with long chains of nodes."],["INSTALL","```apt: sudo apt install graphviz```\n\n```dnf: sudo dnf install graphviz```\n\n```pacman: sudo pacman -S graphviz```\n\n```apk: sudo apk add graphviz```\n\n```zypper: sudo zypper install graphviz```\n\n```brew: brew install graphviz```\n\n```nix: nix profile install nixpkgs#graphviz```"],["SEE ALSO","[dot](/man/dot)(1), [neato](/man/neato)(1), [gvpr](/man/gvpr)(1)"]],"e":["dot","neato","gvpr"],"x":"unflatten [input.dot] -o [output.dot]"},"unxz":{"n":"unxz","t":"Decompress XZ compressed files","s":[["TAGLINE","Decompress XZ compressed files"],["TLDR","**Decompress file**\n\n```unxz [file.xz]```\n\n**Keep original**\n\n```unxz -k [file.xz]```\n\n**To stdout**\n\n```unxz -c [file.xz]```\n\n**Force overwrite**\n\n```unxz -f [file.xz]```\n\n**Decompress multiple**\n\n```unxz [file1.xz] [file2.xz]```"],["SYNOPSIS","**unxz** [_-k_] [_-c_] [_-f_] [_options_] _files_"],["PARAMETERS","**-k**\n> Keep original.\n\n**-c**\n> Write to stdout.\n\n**-f**\n> Force overwrite.\n\n**-v**\n> Verbose mode.\n\n**-q**\n> Quiet mode."],["DESCRIPTION","**unxz** decompresses files that were compressed with the XZ format, which uses the LZMA2 algorithm for high compression ratios. It is functionally equivalent to running **xz --decompress** and is provided as a convenience command within the xz-utils package.\n\nBy default, unxz replaces the compressed .xz file with the decompressed output, removing the original. The **-k** flag preserves the original compressed file, and **-c** writes decompressed data to standard output for piping to other commands.\n\nXZ is widely used in the Linux ecosystem for distributing source code tarballs, kernel sources, and package archives due to its excellent compression ratio. While decompression is fast, XZ compression is slower and more memory-intensive than alternatives like gzip or zstd."],["CAVEATS","Slow for very large files. Part of xz-utils. Single-threaded by default."],["HISTORY","**unxz** is part of **XZ Utils**, created by **Lasse Collin** and first released in **2009** as the successor to LZMA Utils. The XZ format has become the standard compression for Linux kernel sources and many source tarballs."],["INSTALL","```apt: sudo apt install xz-utils```\n\n```dnf: sudo dnf install xz```\n\n```pacman: sudo pacman -S xz```\n\n```apk: sudo apk add xz```\n\n```zypper: sudo zypper install xz```\n\n```brew: brew install xz```\n\n```nix: nix profile install nixpkgs#xz```"],["SEE ALSO","[xz](/man/xz)(1), [gzip](/man/gzip)(1), [bzip2](/man/bzip2)(1)"]],"e":["xz","gzip","bzip2"],"x":"unxz [file.xz]"},"wiremix":{"n":"wiremix","t":"Simple TUI audio mixer for PipeWire","s":[["TAGLINE","Simple TUI audio mixer for PipeWire"],["TLDR","**Launch the PipeWire audio mixer**\n\n```wiremix```"],["SYNOPSIS","**wiremix**"],["DESCRIPTION","**wiremix** is a terminal-based audio mixer for PipeWire that lets you adjust volumes, mute tracks, reroute audio between devices and applications, and configure ports and profiles. Its interface is a clone of ncpamixer, itself inspired by pavucontrol."],["HISTORY","**wiremix** was created by **Thomas Sowell** (tsowell) and is written in **Rust**."],["INSTALL","```dnf: sudo dnf install wiremix```\n\n```pacman: sudo pacman -S wiremix```\n\n```zypper: sudo zypper install wiremix```\n\n```nix: nix profile install nixpkgs#wiremix```"],["SEE ALSO","[pulsemixer](/man/pulsemixer)(1), [pavucontrol](/man/pavucontrol)(1)"]],"e":["pulsemixer","pavucontrol"],"x":"wiremix"},"xbindkeys":{"n":"xbindkeys","t":"Bind keys to commands in X11","s":[["TAGLINE","Bind keys to commands in X11"],["TLDR","**Start xbindkeys**\n\n```xbindkeys```\n\n**Generate default config file**\n\n```xbindkeys --defaults > ~/.xbindkeysrc```\n\n**Identify key codes interactively**\n\n```xbindkeys -k```\n\n**Identify multi-key combinations**\n\n```xbindkeys -mk```\n\n**Show current bindings**\n\n```xbindkeys -s```\n\n**Reload configuration**\n\n```killall -HUP xbindkeys```\n\n**Use specific config file**\n\n```xbindkeys -f [~/.config/xbindkeysrc]```"],["SYNOPSIS","**xbindkeys** [_options_]"],["PARAMETERS","**-d**, **--defaults**\n> Print default configuration to stdout.\n\n**-f**, **--file** _file_\n> Use specified configuration file.\n\n**-k**, **--key**\n> Identify a key (press key in window).\n\n**-mk**, **--multikey**\n> Identify multi-key combination.\n\n**-s**, **--show**\n> Show current key bindings.\n\n**-n**, **--nodaemon**\n> Don't run as daemon.\n\n**-v**, **--verbose**\n> Verbose mode.\n\n**-h**, **--help**\n> Display help.\n\n**-X**, **--display** _display_\n> Set X display to use.\n\n**-g**, **--geometry** _geometry_\n> Set size and position of the key identification window (used with -k or -mk).\n\n**-dg**, **--defaults-guile**\n> Print a default Guile configuration file to stdout.\n\n**-fg**, **--file-guile** _file_\n> Use an alternative Guile configuration file.\n\n**-V**, **--version**\n> Display version."],["CONFIGURATION","Config file syntax (~/.xbindkeysrc):\n```\n\"command\"\n    modifier + key"],["Example: Volume up","\"pactl set-sink-volume @DEFAULT_SINK@ +5%\"\n    XF86AudioRaiseVolume"],["Example: Screenshot","\"scrot\"\n    Print\n```"],["MODIFIERS","**Control**, **Shift**, **Mod1** (Alt), **Mod2** (NumLock), **Mod3** (CapsLock), **Mod4** (Super/Win), **Mod5** (Scroll Lock), **Release** (trigger on key release)"],["DESCRIPTION","**xbindkeys** binds keys or key combinations to shell commands in X11. It works independently of window managers, making it useful for global hotkeys including multimedia keys.\n\nUse `xbindkeys -k` to identify key names interactively. Press the desired key in the popup window to see its name and keycode.\n\nConfiguration is read from ~/.xbindkeysrc by default. The daemon monitors the config file and reloads automatically when modified. Send SIGHUP to force reload.\n\nFor complex setups, xbindkeys supports Guile Scheme configuration via ~/.xbindkeysrc.scm."],["CAVEATS","X11 only; won't work under Wayland. Some keys may be grabbed by other applications. Key names can be found in /usr/include/X11/keysymdef.h. Multimedia keys require proper kernel/X configuration."],["HISTORY","**xbindkeys** was created to provide a window-manager-independent way to bind keys to commands. It fills the gap for desktop environments and window managers that lack built-in hotkey configuration."],["INSTALL","```apt: sudo apt install xbindkeys```\n\n```dnf: sudo dnf install xbindkeys```\n\n```pacman: sudo pacman -S xbindkeys```\n\n```apk: sudo apk add xbindkeys```\n\n```zypper: sudo zypper install xbindkeys```\n\n```nix: nix profile install nixpkgs#xbindkeys```"],["SEE ALSO","[xev](/man/xev)(1), [xmodmap](/man/xmodmap)(1), [sxhkd](/man/sxhkd)(1), [xdotool](/man/xdotool)(1)"]],"e":["xev","xmodmap","sxhkd","xdotool"],"x":"xbindkeys"},"xzcmp":{"n":"xzcmp","t":"Compare xz compressed files","s":[["TAGLINE","Compare xz compressed files"],["TLDR","**Compare xz compressed files**\n\n```xzcmp [file1.xz] [file2.xz]```\n\n**Compare compressed with uncompressed**\n\n```xzcmp [file.xz] [file]```"],["SYNOPSIS","**xzcmp** [_options_] _file1_ [_file2_]"],["DESCRIPTION","**xzcmp** compares xz compressed files. Decompresses and compares files using cmp. If only one file given, compares with stdin or uncompressed version. Wrapper around cmp for xz files."],["INSTALL","```apt: sudo apt install xz-utils```\n\n```dnf: sudo dnf install xz```\n\n```pacman: sudo pacman -S xz```\n\n```apk: sudo apk add xz```\n\n```zypper: sudo zypper install xz```\n\n```brew: brew install xz```\n\n```nix: nix profile install nixpkgs#xz```"],["SEE ALSO","[xz](/man/xz)(1), [xzdiff](/man/xzdiff)(1), [cmp](/man/cmp)(1)"]],"e":["xz","xzdiff","cmp"],"x":"xzcmp [file1.xz] [file2.xz]"},"zdiff":{"n":"zdiff","t":"Compare gzip-compressed files line-by-line","s":[["TAGLINE","Compare gzip-compressed files line-by-line"],["TLDR","**Compare two compressed files**\n\n```zdiff [file1.gz] [file2.gz]```\n\n**Compare in unified format**\n\n```zdiff -u [file1.gz] [file2.gz]```\n\n**Compare in context format**\n\n```zdiff -c [file1.gz] [file2.gz]```\n\n**Compare side by side**\n\n```zdiff -y [file1.gz] [file2.gz]```\n\n**Compare a compressed file** against its uncompressed version\n\n```zdiff [file.gz]```"],["SYNOPSIS","**zdiff** [_diff_options_] _file1_ [_file2_]"],["PARAMETERS","All options are passed directly to **diff**(1). Common options include:\n\n**-u**\n> Unified output format.\n\n**-c**\n> Context output format.\n\n**-y**\n> Side-by-side output.\n\n**-q**\n> Report only whether files differ.\n\n**-i**\n> Ignore case differences.\n\n**-w**\n> Ignore all white space."],["DESCRIPTION","**zdiff** compares gzip-compressed files line-by-line without requiring manual decompression. It works like **diff** but transparently handles .gz files, decompressing them on the fly before comparing.\n\nIf only one file is specified, it is compared against the uncompressed contents of the corresponding .gz file. If two files are specified, their contents (uncompressed if necessary) are fed to diff.\n\nAll standard diff output formats are supported. Any options accepted by diff can be passed through to zdiff. The exit status from diff is preserved.\n\nThe command is part of the gzip package. For byte-by-byte comparison, use **zcmp** instead."],["CAVEATS","Only handles gzip-compressed files. Both files are fully decompressed in memory before comparison. For xz/lzma files, use xzdiff; for bzip2 files, use bzdiff."],["INSTALL","```apt: sudo apt install gzip```\n\n```dnf: sudo dnf install gzip```\n\n```pacman: sudo pacman -S gzip```\n\n```apk: sudo apk add gzip```\n\n```zypper: sudo zypper install gzip```\n\n```brew: brew install gzip```\n\n```nix: nix profile install nixpkgs#gzip```"],["SEE ALSO","[diff](/man/diff)(1), [zcmp](/man/zcmp)(1), [zcat](/man/zcat)(1), [gzip](/man/gzip)(1), [zmore](/man/zmore)(1)"]],"e":["diff","zcmp","zcat","gzip","zmore"],"x":"zdiff [file1.gz] [file2.gz]"}});
