window.LCL_BUCKET(69,{"aa-decode":{"n":"aa-decode","t":"Decode hex-encoded strings in AppArmor logs","s":[["TAGLINE","Decode hex-encoded strings in AppArmor logs"],["TLDR","**Decode** a single hex-encoded string\n\n```aa-decode [hexstring]```\n\nDecode hex-encoded paths in a **log file** via stdin\n\n```aa-decode < [/var/log/audit/audit.log]```\n\nPipe **kernel logs** through aa-decode\n\n```cat /var/log/kern.log | aa-decode```\n\nDecode the **current journal** AppArmor entries\n\n```journalctl -k | aa-decode```"],["SYNOPSIS","**aa-decode** [_option_] _HEX_STRING_"],["DESCRIPTION","**aa-decode** decodes hexadecimal-encoded strings found in AppArmor log output. It processes audit logs from standard input and converts any hex-encoded AppArmor entries to human-readable format on standard output.\n\nAppArmor encodes certain characters (like spaces and special characters) in hexadecimal format within log entries to prevent parsing issues. This utility converts those back to readable text."],["PARAMETERS","**--help**\n> Displays usage information"],["CAVEATS","The utility only decodes hexadecimal strings in the specific format used by AppArmor logs. Other hex-encoded data may not be decoded correctly."],["HISTORY","Part of the **AppArmor** utilities package for managing application security profiles on Linux systems."],["INSTALL","```apk: sudo apk add apparmor-utils```\n\n```zypper: sudo zypper install apparmor-utils```\n\n```nix: nix profile install nixpkgs#apparmor-utils```"],["SEE ALSO","[aa-logprof](/man/aa-logprof)(8), [aa-notify](/man/aa-notify)(8), [aa-status](/man/aa-status)(8), [apparmor](/man/apparmor)(7)"],["RESOURCES","```[Source code](https://gitlab.com/apparmor/apparmor)```\n\n```[Homepage](https://apparmor.net/)```\n\n```[Documentation](https://gitlab.com/apparmor/apparmor/-/wikis/home)```"]],"e":["aa-logprof","aa-notify","aa-status","apparmor"],"x":"aa-decode [hexstring]"},"appman":{"n":"appman","t":"AppImage and portable application manager for local installations","s":[["TAGLINE","AppImage and portable application manager for local installations"],["TLDR","**Search and install** a package by name\n\n```appman -i [package_name]```\n\n**Update all** installed applications\n\n```appman -u```\n\n**Remove** an installed application\n\n```appman -r [package_name]```\n\n**Search** available packages by keyword\n\n```appman -q [keyword]```\n\n**List all installed** applications\n\n```appman -f```\n\n**Show information** about an installed application\n\n```appman -a [package_name]```"],["SYNOPSIS","**appman** [_option_] [_package_name_]"],["PARAMETERS","**-i**, **install** _package_\n> Install one or more applications locally in the user home directory\n\n**-ia**, **install-appimage** _package_\n> Install AppImage packages specifically\n\n**-r**, **remove** _package_\n> Remove one or more installed applications\n\n**-u**, **update**\n> Update all installed applications and AppMan itself. Use **--apps** to update only programs\n\n**-l**, **list**\n> List all available applications in the database\n\n**-q**, **query** _keyword_\n> Search available applications by keyword\n\n**-f**, **files**\n> Show all locally installed applications\n\n**-a**, **about** _package_\n> View the description and source information for an application\n\n**-d**, **download** _package_\n> Download and read the installation script without installing\n\n**-b**, **backup** _package_\n> Create a snapshot of an installed application for rollback\n\n**-o**, **overwrite** _package_\n> Overwrite an existing installation with a fresh copy\n\n**-e**, **extra** _url_\n> Install applications from external GitHub repositories\n\n**-c**, **clean**\n> Remove temporary and cached files\n\n**-s**, **sync**\n> Update AppMan modules and core script\n\n**--rollback** _package_\n> Revert an application to a previous version\n\n**--sandbox** _package_\n> Enable Bubblewrap sandboxing for an AppImage via Aisap\n\n**--disable-sandbox** _package_\n> Remove sandbox isolation from an AppImage\n\n**--launcher** _package_\n> Integrate an application with desktop menus and launchers\n\n**--relocate**\n> Change the local installation directory for apps\n\n**-h**, **help**\n> Show all available options\n\n**-v**, **version**\n> Print version information"],["DESCRIPTION","**appman** is a rootless, portable package manager for AppImages and other portable Linux applications. It is a redistribution of the **AM Application Manager** that does not require root privileges, installing and managing all applications locally within the user's home directory.\n\nAppMan downloads installation scripts from the AM database and converts them for local use. Applications are stored in a user-chosen directory (e.g. **~/Applications**), with executable links placed in **~/.local/bin/** and desktop entries in **~/.local/share/applications/**. The database lists over 2500 AppImages and portable programs.\n\nAppMan supports the same commands as AM — simply replace **am** with **appman**. It handles installation, updates, removal, sandboxing, rollback, and desktop integration without ever requiring sudo access."],["CAVEATS","AppMan relies on installation scripts from a community-maintained database. These scripts download binaries from third-party sources and are not audited by a central authority — always review scripts with **-d** before installing. Sandboxing via Aisap requires Bubblewrap to be available on the system. Some applications may not update correctly if upstream changes their release format or download URLs."],["HISTORY","AppMan was created by **ivan-hc** as a companion to the **AM Application Manager**, first appearing on GitHub around **2022**. AM itself was inspired by the AUR model for Arch Linux, aiming to bring a similar community-driven package management experience to AppImages and portable apps across all GNU/Linux distributions. AppMan was introduced to provide the same functionality without requiring root privileges, making it suitable for restricted environments and multi-user systems."],["SEE ALSO","[flatpak](/man/flatpak)(1), [snap](/man/snap)(1), [wget](/man/wget)(1)"]],"e":["flatpak","snap","wget"],"x":"appman -i [package_name]"},"apptainer-exec":{"n":"apptainer-exec","t":"Execute a command within an Apptainer container.","s":[["TAGLINE","Execute a command within an Apptainer container."],["TLDR","**Execute a command** inside a container\n\n```apptainer exec [container.sif] [command]```\n\n**Run with bound directory** from host to container\n\n```apptainer exec --bind [/host/path]:[/container/path] [container.sif] [command]```\n\n**Execute with writable overlay**\n\n```apptainer exec --overlay [overlay.img] [container.sif] [command]```\n\n**Run with GPU support** (NVIDIA)\n\n```apptainer exec --nv [container.sif] [command]```\n\n**Execute from Docker Hub** image\n\n```apptainer exec docker://[image:tag] [command]```\n\n**Run with isolated network**\n\n```apptainer exec --net --network none [container.sif] [command]```\n\n**Execute with custom environment variable**\n\n```apptainer exec --env [VAR=value] [container.sif] [command]```\n\n**Run in a writable sandbox directory**\n\n```apptainer exec --writable [sandbox/] [command]```"],["SYNOPSIS","**apptainer exec** [_options_] _container_ _command_ [_args_]"],["PARAMETERS","**--bind**, **-B** _src[:dest[:opts]]_\n> Bind mount a path from the host into the container.\n\n**--overlay** _image_\n> Use an overlay filesystem for writable layer.\n\n**--nv**\n> Enable NVIDIA GPU support inside the container.\n\n**--rocm**\n> Enable AMD ROCm GPU support.\n\n**--contain**, **-c**\n> Use minimal /dev and empty other directories.\n\n**--containall**, **-C**\n> Full isolation: contain filesystems plus new PID and IPC namespaces and clean environment.\n\n**--cleanenv**, **-e**\n> Clean environment before running container.\n\n**--env** _VAR=value_\n> Set environment variable in container.\n\n**--env-file** _file_\n> Load environment variables from file.\n\n**--home** _path_\n> Set custom home directory.\n\n**--cwd** _path_\n> Set initial working directory in container. (**--pwd** is a deprecated synonym.)\n\n**--net**\n> Enable network namespace.\n\n**--network** _type_\n> Specify network type (none, bridge, etc.).\n\n**--fakeroot**\n> Run container with fake root privileges.\n\n**--writable-tmpfs**\n> Add writable tmpfs overlay.\n\n**--no-mount** _type_\n> Disable a specific mount point. Valid values: proc, sys, dev, devpts, home, tmp, hostfs, cwd.\n\n**--no-home**\n> Do not mount the user's home directory into the container.\n\n**--writable**, **-w**\n> Make the container filesystem read-write (requires a writable image or sandbox directory).\n\n**--scratch**, **-S** _dir_\n> Include a scratch directory within the container that is linked to a temporary directory on the host.\n\n**--workdir**, **-W** _path_\n> Working directory used for /tmp, /var/tmp, and $HOME overrides when using **--contain** or **--scratch**.\n\n**--ipc**, **-i**\n> Run container in a new IPC namespace.\n\n**--pid**, **-p**\n> Run container in a new PID namespace.\n\n**--userns**, **-u**\n> Run container in a new user namespace.\n\n**--uts**\n> Run container in a new UTS namespace.\n\n**--no-eval**\n> Do not shell-evaluate environment variables or OCI CMD/ENTRYPOINT.\n\n**--fusemount** _spec_\n> Mount a FUSE filesystem inside the container using the format: _type:fuse_command container_path_."],["DESCRIPTION","**apptainer exec** runs a specified command inside an Apptainer container. Unlike **apptainer shell** which provides an interactive session, exec runs a single command and returns its exit status, making it ideal for batch processing and scripts.\n\nThe container can be specified as a local SIF file, a library URI (library://), a Docker URI (docker://), or an OCI archive. The command and any arguments are executed within the container environment with the current user's identity preserved.\n\nBy default, Apptainer mounts the current directory, home directory, and standard system paths into the container. Additional paths can be bound using **--bind**. For HPC workloads, the **--nv** or **--rocm** flags enable GPU passthrough.\n\nThe command inherits the host's environment by default. Use **--cleanenv** for reproducibility or **--contain** for isolation. Exit status reflects the executed command's return code."],["CONFIGURATION","**/etc/apptainer/apptainer.conf**\n> Main configuration file controlling default bind paths, security options, GPU support, and namespace settings."],["CAVEATS","User namespaces must be enabled in the kernel for rootless operation. Some containers may require **--writable-tmpfs** or **--overlay** to function properly. NVIDIA GPU support requires the nvidia-container-cli and compatible drivers. Network namespace requires root or appropriate privileges unless user namespaces are configured."],["HISTORY","Apptainer is the continuation of the **Singularity** project after it joined the Linux Foundation in **2021**. The exec subcommand has been a core feature since Singularity's inception at Lawrence Berkeley National Laboratory in **2015**. Apptainer 1.0 was released in **2022**, maintaining full compatibility with Singularity container formats while adding new features."],["INSTALL","```dnf: sudo dnf install apptainer```\n\n```pacman: sudo pacman -S apptainer```\n\n```apk: sudo apk add apptainer```\n\n```zypper: sudo zypper install apptainer```\n\n```brew: brew install apptainer```\n\n```nix: nix profile install nixpkgs#apptainer```"],["SEE ALSO","[apptainer](/man/apptainer)(1), [apptainer-shell](/man/apptainer-shell)(1), [apptainer-run](/man/apptainer-run)(1), [apptainer-build](/man/apptainer-build)(1), [apptainer-pull](/man/apptainer-pull)(1), [apptainer-overlay](/man/apptainer-overlay)(1), [docker](/man/docker)(1)"]],"e":["apptainer","apptainer-shell","apptainer-run","apptainer-build","apptainer-pull","apptainer-overlay","docker"],"x":"apptainer exec [container.sif] [command]"},"asak":{"n":"asak","t":"Cross-platform audio recording/playback CLI tool with TUI","s":[["TAGLINE","Cross-platform audio recording/playback CLI tool with TUI"],["TLDR","**Record audio** to a WAV file\n\n```asak rec [output]```\n\n**Play back** a WAV file\n\n```asak play [input.wav]```\n\n**Play a file interactively** by selecting from WAV files in the current directory\n\n```asak play```\n\n**Record using a specific audio device** by index\n\n```asak rec -d [device_index] [output]```\n\n**Monitor audio input** through the output device in real time\n\n```asak monitor```\n\n**Monitor with a custom buffer size**\n\n```asak monitor -b [2048]```\n\n**List available audio devices**\n\n```asak list```"],["SYNOPSIS","**asak** [**-j**] _command_ [_options_]\n\n**asak rec** [**-d** _index_] [_output_]\n\n**asak play** [**-d** _index_] [_input_]\n\n**asak monitor** [**-b** _size_]\n\n**asak list**"],["PARAMETERS","**-j**, **--jack**\n> Use the JACK audio backend instead of the default. Only available on Linux, DragonFly BSD, FreeBSD, and NetBSD when built with the jack feature.\n\n**-d** _index_, **--device** _index_\n> The audio device index to use for recording or playback. Run **asak list** to see available devices and their indices.\n\n**-b** _size_, **--buffer_size** _size_\n> Buffer size for audio input monitoring. Defaults to 1024. Higher values increase latency.\n\n**-h**, **--help**\n> Display help information and exit.\n\n**-V**, **--version**\n> Display version information and exit."],["DESCRIPTION","**asak** (audio Swiss Army knife) is a cross-platform audio recording and playback CLI tool with a terminal user interface, written in Rust. It aims to provide functionality similar to SoX but with an interactive and visual experience.\n\nThe tool provides four main subcommands. **rec** records audio from an input device to a WAV file; if no filename is given, it prompts interactively and suggests a UTC-timestamped default name. **play** plays back a WAV file; if no file is specified, it searches the current directory for WAV files and presents an interactive selection menu. **monitor** routes audio from the input device to the output device in real time with a visual scope display, useful for quickly testing a microphone or speaker setup. **list** displays all available audio input and output devices with their indices.\n\nasak uses **cpal** as its audio backend and **ratatui** for its terminal UI, providing visual feedback such as level meters and playback animations. Audio resampling is handled via the **dasp** library when the source and target sample rates differ."],["CAVEATS","Only WAV format is supported for recording and playback at this time. The **monitor** subcommand requires the output device to match the system default settings. On Linux, the JACK audio backend requires **libjack-dev** (or equivalent) to be installed and the tool to be built with the **jack** feature flag. The project is under active development and some features like effects processing and live position control are not yet implemented."],["HISTORY","**asak** was created by **Qichao Lan** (chaosprint), a developer at the University of Oslo who specializes in Rust audio programming and is also the author of **Glicol**, a graph-oriented live coding language. The first commits appeared in **early 2024** and the project was publicly announced on Hacker News in **December 2024**. The tool reached version 0.3.6 by mid-2025. It is licensed under the MIT license."],["INSTALL","```aur: yay -S asak```\n\n```brew: brew install asak```\n\n```nix: nix profile install nixpkgs#asak```"],["SEE ALSO","[sox](/man/sox)(1), [arecord](/man/arecord)(1), [aplay](/man/aplay)(1), [play](/man/play)(1), [rec](/man/rec)(1), [ffmpeg](/man/ffmpeg)(1)"]],"e":["sox","arecord","aplay","play","rec","ffmpeg"],"x":"asak rec [output]"},"astroterm":{"n":"astroterm","t":"display real-time celestial maps in the terminal","s":[["TAGLINE","display real-time celestial maps in the terminal"],["TLDR","**Launch with color and constellations**\n\n```astroterm --color --constellations```\n\n**Set observer location by city name**\n\n```astroterm --city \"[Boston]\"```\n\n**Set observer location by latitude and longitude**\n\n```astroterm --latitude [42.36] --longitude [-71.06]```\n\n**Display a specific date and time** in UTC\n\n```astroterm --datetime [1969-07-16T13:32:00]```\n\n**Animate at high speed** with custom FPS\n\n```astroterm --color --constellations --speed [10000] --fps [64]```\n\n**Draw with azimuthal grid and unicode characters**\n\n```astroterm --grid --unicode --metadata```"],["SYNOPSIS","**astroterm** [_options_]"],["PARAMETERS","**-a**, **--latitude** _degrees_\n> Observer latitude [-90, 90] (default: 0.0).\n\n**-o**, **--longitude** _degrees_\n> Observer longitude [-180, 180] (default: 0.0).\n\n**-i**, **--city** _name_\n> Use latitude and longitude of the given city.\n\n**-d**, **--datetime** _yyyy-mm-ddThh:mm:ss_\n> Observation datetime in UTC (default: system time).\n\n**-t**, **--threshold** _float_\n> Only render stars brighter than this magnitude (default: 5.0).\n\n**-l**, **--label-thresh** _float_\n> Label stars brighter than this magnitude (default: 0.25).\n\n**-f**, **--fps** _int_\n> Frames per second (default: 24).\n\n**-s**, **--speed** _float_\n> Animation speed multiplier (default: 1.0).\n\n**-c**, **--color**\n> Enable terminal colors.\n\n**-C**, **--constellations**\n> Draw constellation stick figures.\n\n**-g**, **--grid**\n> Draw an azimuthal grid overlay.\n\n**-u**, **--unicode**\n> Use Unicode characters for rendering.\n\n**-m**, **--metadata**\n> Display metadata information.\n\n**-r**, **--aspect-ratio** _float_\n> Override the calculated terminal cell aspect ratio.\n\n**-q**, **--quit-on-any**\n> Quit on any keypress (default: quit on 'q' or ESC only).\n\n**-h**, **--help**\n> Print help message.\n\n**-v**, **--version**\n> Display version information."],["DESCRIPTION","**astroterm** is a terminal-based planetarium written in C that renders real-time positions of stars, planets, constellations, and other astronomical objects directly in the console. It computes celestial positions based on the observer's location and time, providing an interactive sky map without a graphical desktop environment. Press **q** or **ESC** to quit (or any key with **--quit-on-any**)."],["CAVEATS","Color output requires **--color** flag and a terminal with 256-color or truecolor support. Display quality depends on terminal size and font. Without **--latitude**/**--longitude** or **--city**, the observer defaults to 0,0 (Gulf of Guinea)."],["INSTALL","```dnf: sudo dnf install astroterm```\n\n```pacman: sudo pacman -S astroterm```\n\n```apk: sudo apk add astroterm```\n\n```zypper: sudo zypper install astroterm```\n\n```brew: brew install astroterm```\n\n```nix: nix profile install nixpkgs#astroterm```"],["SEE ALSO","[cal](/man/cal)(1), [date](/man/date)(1)"]],"e":["cal","date"],"x":"astroterm --color --constellations"},"auggie":{"n":"auggie","t":"AI-powered agentic coding CLI from Augment Code","s":[["TAGLINE","AI-powered agentic coding CLI from Augment Code"],["TLDR","**Start an interactive session** in the current directory\n\n```auggie```\n\n**Start with an initial prompt**\n\n```auggie \"[prompt]\"```\n\n**Run a one-off command** and print to stdout\n\n```auggie --print \"[prompt]\"```\n\n**Continue the most recent** conversation\n\n```auggie --continue```\n\n**Resume a specific session** by ID\n\n```auggie --resume [session_id]```\n\n**Use a specific model**\n\n```auggie --model [sonnet4.5]```\n\n**Pipe input** and run non-interactively\n\n```cat [file.txt] | auggie --print \"[analyze this code]\"```\n\n**Disable a specific tool** for the session\n\n```auggie --remove-tool [web-fetch]```"],["SYNOPSIS","**auggie** [_options_] [_prompt_]\n\n**auggie** **login** | **logout**\n\n**auggie** **models** **list**\n\n**auggie** **session** **list** | **continue** | **resume** [_id_] | **share**\n\n**auggie** **mcp** **add** | **add-json** | **list** | **remove**\n\n**auggie** **tools** **list** | **add** | **remove**\n\n**auggie** **rules** **list**\n\n**auggie** **command** [_command-name_] | **help**\n\n**auggie** **plugin** **marketplace** **add** | **list** | **update** | **remove**\n\n**auggie** **plugin** **list** | **enable** | **disable** | **reload**"],["PARAMETERS","**--print**, **-p**\n> Run the prompt once and print the result to stdout, then exit. Useful for CI/CD pipelines and scripted automation.\n\n**--quiet**\n> Return only the final output, suppressing intermediate messages and progress indicators.\n\n**--compact**\n> Output tool calls, results, and response as single lines.\n\n**--output-format** _FORMAT_\n> Output format for print mode (e.g., json).\n\n**--continue**, **-c**\n> Resume the most recent conversation session.\n\n**--resume** _ID_\n> Resume a specific session by ID or ID prefix.\n\n**-f**\n> Filter session list to the current workspace only.\n\n**--dont-save-session**\n> Skip saving conversation history for this session.\n\n**--delete-saved-sessions**\n> Remove all saved sessions.\n\n**--model** _NAME_\n> Select the model to use. Short names are supported (e.g., sonnet4.5).\n\n**--instruction** _TEXT_\n> Provide an initial instruction for interactive mode.\n\n**--instruction-file** _PATH_\n> Load the initial instruction from a file.\n\n**--workspace-root** _PATH_\n> Set the workspace root directory.\n\n**--rules** _PATH_\n> Append additional rules from a file.\n\n**--remove-tool** _NAME_\n> Disable a specific tool for the session. Can be repeated.\n\n**--permission** _SETTING_\n> Configure tool permissions at runtime.\n\n**--max-turns** _N_\n> Cap the number of agent iterations in print mode.\n\n**--enhance-prompt**\n> Improve prompts automatically before sending to the agent (non-interactive mode).\n\n**--image** _PATH_\n> Attach an image to the prompt.\n\n**--shell** _SHELL_\n> Set the shell to use for command execution.\n\n**--startup-script** _PATH_\n> Specify a shell startup script.\n\n**--mcp**\n> Run auggie as an MCP tool server.\n\n**--mcp-config** _PATH_\n> Load MCP server configuration from a JSON file or inline JSON string.\n\n**--mcp-auto-workspace**\n> Enable automatic workspace discovery in MCP mode.\n\n**-w** _PATH_\n> Specify a workspace directory to pre-index.\n\n**--log-file** _PATH_\n> Enable error logging to a file in MCP mode (default: /tmp/augment-log.txt).\n\n**--acp**\n> Enable Agent Client Protocol support for external editor integration.\n\n**--allow-indexing**\n> Allow codebase indexing in ACP mode.\n\n**--augment-session-json** _JSON_\n> Provide authentication session as JSON.\n\n**--help**, **-h**\n> Show help information.\n\n**--version**\n> Display the installed version."],["DESCRIPTION","**auggie** is a terminal-based AI coding agent built by Augment Code. It uses Augment's Context Engine for deep semantic understanding of codebases, going beyond simple text matching to comprehend project structure, dependencies, and relationships across large repositories.\n\nThe tool operates in two primary modes. Interactive mode provides a full TUI with real-time streaming, visual progress indicators, vim-style keybindings, and observable tool execution. Non-interactive mode (--print) enables scripted automation for CI/CD pipelines, code review workflows, and headless environments.\n\nauggie supports sub-agents for specialized tasks such as security audits, test writing, and data analysis, and can execute multiple agents in parallel. Sessions are persistent and resumable, allowing conversations to be continued across terminal sessions or shared with teammates via the /share command.\n\nThe CLI integrates with Model Context Protocol (MCP) servers for extended capabilities including GitHub, Linear, and Jira connectivity. It can also run as an MCP server itself, enabling integration with other tools. Multiple AI models are supported, and users can switch between them using the --model flag or the Option+M hotkey in the TUI.\n\nCustom slash commands can be defined as markdown files in .augment/commands/ or ~/.augment/commands/ directories, enabling reusable prompts for tasks like code review, bug fixing, and security analysis. User-specific rules in ~/.augment/rules/ and workspace rules customize agent behavior per project.\n\nThe plugin system allows extending functionality through marketplaces, and an agent skills framework loads specialized domain knowledge from SKILL.md files following the agentskills.io specification."],["CONFIGURATION","**~/.augment/settings.json**\n> Global user settings including MCP server configuration, tool permissions, model preferences, auto-update behavior, and default shell.\n\n**.augment/commands/**\n> Project-level custom slash commands as markdown files with frontmatter.\n\n**~/.augment/commands/**\n> User-level custom slash commands available across all projects.\n\n**~/.augment/rules/**\n> User-specific rules for customizing agent behavior globally.\n\n**.augment/.agents/**\n> Directory for skill and agent discovery using SKILL.md and AGENTS.md files."],["CAVEATS","Requires Node.js 22 or higher. Authentication via auggie login is mandatory before first use, and an active Augment subscription is required to use the tool. The CLI auto-updates by default; this can be disabled via the autoUpdate setting or the AUGMENT_DISABLE_AUTO_UPDATE environment variable. The software is proprietary and licensed under a custom license from Augment Computing, Inc."],["HISTORY","**auggie** was announced by **Augment Code** on **July 31, 2025** and launched in beta, with enterprise customers receiving immediate access and a phased rollout to teams and individuals. The GitHub repository was created in **September 2025**. By version **0.7.0**, the CLI dropped its beta label and reached general availability. Development has been rapid, with features like MCP server support, Agent Client Protocol integration, session sharing, parallel tool calls, plugin marketplaces, and agent skills added across frequent releases. The tool is part of Augment Code's broader platform that also includes VS Code and JetBrains IDE extensions."],["SEE ALSO","[claude](/man/claude)(1), [copilot](/man/copilot)(1), [aider](/man/aider)(1), [codex](/man/codex)(1), [opencode](/man/opencode)(1), [grok](/man/grok)(1)"]],"e":["claude","copilot","aider","codex","opencode","grok"],"x":"auggie"},"b2sum":{"n":"b2sum","t":"Compute and verify BLAKE2 cryptographic checksums.","s":[["TAGLINE","Compute and verify BLAKE2 cryptographic checksums."],["TLDR","**Calculate** BLAKE2 checksum\n\n```b2sum [file]```\n\n**Verify** checksums\n\n```b2sum -c [checksums.b2]```\n\nUse **specific length**\n\n```b2sum -l [256] [file]```\n\nCalculate for **multiple files**\n\n```b2sum [file1] [file2] [file3]```"],["SYNOPSIS","**b2sum** [_options_] [_file_...]"],["DESCRIPTION","**b2sum** computes and verifies BLAKE2b cryptographic checksums. BLAKE2 is a fast, secure hash function that outperforms MD5, SHA-1, and SHA-2 in software while providing at least as much security as SHA-3.\n\nThe tool is part of GNU coreutils and provides similar functionality to md5sum and sha256sum."],["PARAMETERS","**-c**, **--check**\n> Read checksums from the file(s) and verify them.\n\n**-l**, **--length** _bits_\n> Digest length in bits, a multiple of 8 (default: 512). Ignored when verifying.\n\n**-b**, **--binary**\n> Read in binary mode.\n\n**-t**, **--text**\n> Read in text mode (default).\n\n**--tag**\n> Create a BSD-style checksum line.\n\n**-z**, **--zero**\n> End each output line with NUL, not newline.\n\n**--ignore-missing**\n> When verifying, do not fail or report status for missing files.\n\n**--quiet**\n> When verifying, do not print OK for each successfully verified file.\n\n**--status**\n> When verifying, print nothing; signal result only via exit status.\n\n**-w**, **--warn**\n> When verifying, warn about improperly formatted checksum lines.\n\n**--strict**\n> When verifying, exit non-zero for any improperly formatted checksum line."],["FEATURES","- Fast performance\n- Configurable output length (multiple of 8 bits)\n- Verification mode\n- Same interface as md5sum and sha256sum"],["WORKFLOW","```bash"],["Create checksums","b2sum file1 file2 > checksums.b2"],["Verify","b2sum -c checksums.b2"],["Use 256-bit digest","b2sum -l 256 file\n```"],["CAVEATS","Not as widely supported as SHA-256. Different tools may use BLAKE2b vs BLAKE2s. Length parameter affects compatibility. Relatively new compared to SHA family."],["HISTORY","**b2sum** was added to GNU coreutils in version **8.26** (2016), providing BLAKE2 hashing support following the algorithm's publication in **2012**."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[sha256sum](/man/sha256sum)(1), [md5sum](/man/md5sum)(1), [b3sum](/man/b3sum)(1), [cksum](/man/cksum)(1)"],["RESOURCES","```[Source code](https://github.com/coreutils/coreutils)```\n\n```[Documentation](https://www.gnu.org/software/coreutils/manual/html_node/b2sum-invocation.html)```"]],"e":["sha256sum","md5sum","b3sum","cksum"],"x":"b2sum [file]"},"bgpgrep":{"n":"bgpgrep","t":"Filter and search BGP routing data in MRT format","s":[["TAGLINE","Filter and search BGP routing data in MRT format"],["TLDR","**Filter routes** matching a specific subnet from MRT dumps\n\n```bgpgrep [path/to/rib.mrt.gz] -subnet [192.0.2.0/24]```\n\n**Filter routes** by AS path pattern\n\n```bgpgrep [path/to/rib.mrt.gz] -aspath \"[64496 64497]\"```\n\n**List routes** from a specific peer\n\n```bgpgrep [path/to/rib.mrt.gz] -peer [198.51.100.1]```\n\n**Find routes** that lead to a specific address\n\n```bgpgrep [path/to/rib.mrt.gz] -supernet [8.8.8.8/32]```\n\n**Detect bogon ASNs** in routing data\n\n```bgpgrep [path/to/rib.mrt.gz] -bogon-asn```\n\n**Combine filters** with logical operators\n\n```bgpgrep [path/to/rib.mrt.gz] -bogon-asn -or -subnet [fullbogons.txt]```\n\n**Filter by timestamp** range\n\n```bgpgrep [path/to/updates.mrt.gz] -timestamp \"[>=2021-07-01]\" -and -timestamp \"[<2021-07-08]\"```"],["SYNOPSIS","**bgpgrep** [_file ..._] [_filter expressions_]"],["PARAMETERS","**-peer** _address|asn_\n> Match routes received from a specific peer, identified by IP address or AS number.\n\n**-aspath** _pattern_\n> Match routes whose AS path matches the given pattern. Supports AS number sequences and regular expression-like syntax.\n\n**-supernet** _prefix_\n> Match routes that are supernets of (or equal to) the specified prefix.\n\n**-subnet** _prefix|file_\n> Match routes that are subnets of the specified prefix, or match against a prefix list from a file.\n\n**-communities** _expression_\n> Match routes with specific BGP community values. Supports wildcard patterns.\n\n**-bogon-asn**\n> Match routes containing bogon (reserved/unallocated) AS numbers in the AS path.\n\n**-loops**\n> Detect AS path loops.\n\n**-timestamp** _condition_\n> Filter by timestamp using comparison operators (e.g., \">=2021-07-01\").\n\n**-and**\n> Logical AND between filter conditions (default when combining filters).\n\n**-or**\n> Logical OR between filter conditions.\n\n**-not**\n> Negate the following filter condition."],["DESCRIPTION","**bgpgrep** is part of the Micro BGP Suite, a set of shell tools for filtering and analyzing BGP routing data stored in MRT (Multi-threaded Routing Toolkit) format. It processes BGP table dumps and update files from route collectors like RIPE RIS and RouteViews.\n\nEach line of output is prefixed with a character indicating the message type: `=` for RIB snapshot, `+` for announcement, `-` for withdrawal, and `#` for BGP state change. The output uses a pipe-delimited format where the 9th field contains the peer address and ASN, enabling easy integration with standard command-line tools like `cut` and `awk`.\n\nThe tool supports filtering by prefix, AS path patterns, peer, communities, and timestamp. Multiple filters can be combined using boolean operators (`-and`, `-or`, `-not`) with parentheses for grouping. It handles both IPv4 and IPv6 prefixes and supports compressed MRT files (gzip, bzip2)."],["CAVEATS","Input must be in MRT format (common BGP dump format). Filter option names use single-dash long form (e.g., `-peer`, not `--peer`). AS path matching patterns use bgpgrep-specific syntax that differs from standard regular expressions."],["SEE ALSO","[traceroute](/man/traceroute)(1), [whois](/man/whois)(1)"]],"e":["traceroute","whois"],"x":"bgpgrep [path/to/rib.mrt.gz] -subnet [192.0.2.0/24]"},"binwalk":{"n":"binwalk","t":"Analyze and extract firmware images","s":[["TAGLINE","Analyze and extract firmware images"],["TLDR","**Scan** firmware for signatures\n\n```binwalk [firmware.bin]```\n\n**Extract** embedded files\n\n```binwalk -e [firmware.bin]```\n\n**Entropy** analysis\n\n```binwalk -E [firmware.bin]```\n\n**Recursive** extraction\n\n```binwalk -Me [firmware.bin]```\n\n**Carve** known and unknown contents to disk\n\n```binwalk -c [firmware.bin]```\n\n**List** supported signatures and extractors\n\n```binwalk -L```"],["SYNOPSIS","**binwalk** [_options_] _file_..."],["DESCRIPTION","**binwalk** is a firmware analysis tool for searching binary images for embedded files and executable code. It identifies file signatures, compressed data, filesystems, and other patterns commonly found in firmware images.\n\nThe tool is essential for reverse engineering, security research, and firmware modification.\n\nAs of version 3 (released 2024), binwalk was completely rewritten in Rust for greatly improved scanning speed, fewer false positives, and more built-in extractors. Several options from the older Python implementation (such as **--dd**, **--hexdump**, **--raw**, and **--disasm**) were removed or replaced."],["PARAMETERS","**-e**, **--extract**\n> Automatically extract known file types\n\n**-M**, **--matryoshka**\n> Recursively scan extracted files\n\n**-c**, **--carve**\n> Carve both known and unknown file contents to disk\n\n**-E**, **--entropy**\n> Generate an entropy graph (rendered with Plotly)\n\n**-p**, **--png**\n> Save the entropy graph as a PNG file\n\n**-a**, **--search-all**\n> Search for all signatures at all offsets\n\n**-d**, **--directory** _path_\n> Extract files and folders to a custom directory\n\n**-t**, **--threads** _count_\n> Manually specify the number of threads to use\n\n**-y**, **--include** _signatures_\n> Only scan for the given signatures\n\n**-x**, **--exclude** _signatures_\n> Do not scan for the given signatures\n\n**-l**, **--log** _file_\n> Log JSON results to a file ('-' for stdout)\n\n**-L**, **--list**\n> List supported signatures and extractors\n\n**-s**, **--stdin**\n> Read data from standard input\n\n**-q**, **--quiet**\n> Suppress normal stdout output\n\n**-v**, **--verbose**\n> During recursive extraction, display all results"],["FEATURES","- File signature identification\n- Embedded filesystem detection\n- Compression detection\n- Entropy visualization\n- Automatic extraction\n- Custom signature support\n- Plugin system"],["WORKFLOW","```bash"],["Scan firmware","binwalk firmware.bin"],["Extract all files","binwalk -e firmware.bin"],["Recursive extraction","binwalk -Me firmware.bin"],["Entropy analysis (finds compressed/encrypted regions)","binwalk -E firmware.bin"],["Extract into a custom directory","binwalk -e -d extracted/ firmware.bin\n```"],["COMMON FINDINGS","- **Filesystem images** (squashfs, cramfs, jffs2)\n- **Bootloaders** (U-Boot, LILO)\n- **Kernels** (Linux, VxWorks)\n- **Compressed data** (gzip, lzma, zlib)\n- **Certificates and keys**"],["CAVEATS","Extraction may not work for custom formats. Encrypted sections appear as high entropy. Some signatures produce false positives. Requires appropriate tools for extraction (sasquatch, jefferson, etc.). Large firmware images slow to process."],["HISTORY","**binwalk** was created by Craig Heffner around **2010** for reverse engineering and analyzing firmware images from embedded devices. It is now maintained by ReFirmLabs. Version 3, a full rewrite in Rust, debuted in 2024."],["INSTALL","```dnf: sudo dnf install binwalk```\n\n```pacman: sudo pacman -S binwalk```\n\n```zypper: sudo zypper install binwalk```\n\n```brew: brew install binwalk```\n\n```nix: nix profile install nixpkgs#binwalk```"],["SEE ALSO","[foremost](/man/foremost)(1), [strings](/man/strings)(1), [file](/man/file)(1), [dd](/man/dd)(1), [hexdump](/man/hexdump)(1)"],["RESOURCES","```[Source code](https://github.com/ReFirmLabs/binwalk)```\n\n```[Homepage](https://github.com/ReFirmLabs/binwalk)```"]],"e":["foremost","strings","file","dd","hexdump"],"x":"binwalk [firmware.bin]"},"bkill":{"n":"bkill","t":"Kill LSF batch jobs","s":[["TAGLINE","Kill LSF batch jobs"],["TLDR","**Kill a specific job**\n\n```bkill [job_id]```\n\n**Kill all your jobs**\n\n```bkill 0```\n\n**Kill a job by name**\n\n```bkill -J [job_name]```\n\n**Kill jobs in a specific queue**\n\n```bkill -q [queue_name] 0```\n\n**Force kill a job** (no cleanup)\n\n```bkill -r [job_id]```\n\n**Send a specific signal** to a job\n\n```bkill -s [SIGTERM] [job_id]```\n\n**Kill jobs matching a pattern**\n\n```bkill -J \"[pattern*]\" 0```\n\n**Kill job array elements**\n\n```bkill \"[job_id][1-10]\"```"],["SYNOPSIS","**bkill** [_options_] [_job_id_ | **0**]..."],["DESCRIPTION","**bkill** terminates LSF (Load Sharing Facility) batch jobs. It sends a signal to running jobs or removes pending jobs from the queue. Using job ID 0 targets all of your own jobs.\n\nFor running jobs, bkill first sends SIGINT, waits for a grace period, then sends SIGTERM, and finally SIGKILL if the job doesn't terminate. The -r option skips this sequence and forces immediate removal.\n\nbkill is part of IBM Spectrum LSF workload management system used in HPC environments."],["PARAMETERS","**-r**\n> Force removal: remove the job from LSF immediately, mark it EXIT, and release monitored resources. Cannot be combined with **-s**.\n\n**-s** _signal_\n> Send a specific signal by name or number instead of the default SIGINT/SIGTERM/SIGKILL sequence.\n\n**-J** _name_\n> Kill jobs matching name or name pattern.\n\n**-q** _queue_\n> Kill jobs in specific queue.\n\n**-m** _host_\n> Kill jobs on specific host or host group.\n\n**-g** _group_\n> Kill jobs in specific job group.\n\n**-u** _user_\n> Kill jobs for specific user or user group (admin only).\n\n**-app** _profile_\n> Kill jobs using specific application profile.\n\n**-sla** _class_\n> Kill jobs belonging to a specific service class.\n\n**-b**\n> Kill jobs quickly; pending jobs are removed immediately without waiting.\n\n**-l**\n> Display the signal names supported by bkill on this platform.\n\n**-V**\n> Display LSF version information.\n\n**0**\n> Target all of your jobs (combine with filters to scope by queue, host, etc.)."],["CAVEATS","Only administrators can kill other users' jobs. Force kill (-r) may leave orphan processes on execution hosts. Killing a job array master kills all elements. Pending jobs are removed immediately without signal sequence. Some jobs may not respond to signals and require force kill."],["HISTORY","**bkill** is part of **IBM Spectrum LSF**, originally developed by **Platform Computing** in the **early 1990s**. The command follows Unix conventions with signal handling while integrating with the LSF scheduler. Platform Computing was acquired by IBM in **2012**, continuing development of the LSF suite for enterprise HPC workload management."],["SEE ALSO","[bjobs](/man/bjobs)(1), [bsub](/man/bsub)(1)"]],"e":["bjobs","bsub"],"x":"bkill [job_id]"},"bw":{"n":"bw","t":"Bitwarden password manager CLI","s":[["TAGLINE","Bitwarden password manager CLI"],["TLDR","**Login** to Bitwarden\n\n```bw login```\n\n**Unlock** vault\n\n```bw unlock```\n\n**List** items\n\n```bw list items```\n\n**Get** password\n\n```bw get password [example.com]```\n\n**Create** item\n\n```bw create item```\n\n**Sync** vault\n\n```bw sync```"],["SYNOPSIS","**bw** _command_ [_options_]"],["DESCRIPTION","**bw** is the official command-line interface for Bitwarden, an open-source password manager. It enables accessing and managing passwords, notes, and credentials from the terminal or in scripts.\n\nThe tool provides full access to Bitwarden vaults for automation and integration purposes."],["PARAMETERS","**--session** _key_\n> Session key for authentication\n\n**--organizationid** _id_\n> Organization ID filter\n\n**--pretty**\n> Pretty-print JSON output\n\n**--raw**\n> Return raw value only"],["COMMON COMMANDS","**login** [_email_]\n> Log in to account\n\n**unlock**\n> Unlock vault (returns session key)\n\n**lock**\n> Lock vault\n\n**sync**\n> Sync vault with server\n\n**list** _object_\n> List items, folders, collections\n\n**get** _object_ _id_\n> Get specific item\n\n**create** _object_ _encodedJson_\n> Create new item\n\n**edit** _object_ _id_ _encodedJson_\n> Edit existing item\n\n**delete** _object_ _id_\n> Delete item"],["WORKFLOW","```bash"],["Login","bw login user@example.com"],["Unlock vault (save session key)","export BW_SESSION=\"$(bw unlock --raw)\""],["List all items","bw list items"],["Get password","bw get password github.com"],["Search items","bw list items --search \"google\""],["Get username","bw get username amazon.com"],["Generate password","bw generate -ulns --length 20"],["Create item","echo '{\"name\":\"My Login\",\"username\":\"user\",\"password\":\"pass\"}' | \\\n  bw encode | bw create item\n```"],["FEATURES","- Full vault access\n- Password generation\n- Secure note storage\n- Two-factor authentication\n- Organization support\n- Self-hosted server support\n- JSON output for scripting"],["CAVEATS","Session key needed for most operations. Store session key securely (environment variable). Output is JSON (requires parsing). CLI requires unlocking frequently. Self-hosted servers need configuration."],["HISTORY","**Bitwarden CLI** was released around **2018** as part of the Bitwarden password manager to enable terminal and scripting access."],["SEE ALSO","[pass](/man/pass)(1), [gopass](/man/gopass)(1), [op](/man/op)(1)"],["RESOURCES","```[Source code](https://github.com/bitwarden/clients)```\n\n```[Documentation](https://bitwarden.com/help/cli/)```"]],"e":["pass","gopass","op"],"x":"bw login"},"calligrawords":{"n":"calligrawords","t":"Word processor from Calligra suite","s":[["TAGLINE","Word processor from Calligra suite"],["TLDR","**Launch Calligra Words**\n\n```calligrawords```\n\n**Open a document file**\n\n```calligrawords [document.odt]```"],["SYNOPSIS","**calligrawords** [_options_] [_file_]"],["DESCRIPTION","**calligrawords** is a word processor from the Calligra office suite. It provides document creation and editing with support for styles, tables, images, and advanced formatting.\n\nThe application uses ODF (Open Document Format) and is comparable to Microsoft Word or LibreOffice Writer."],["INSTALL","```apt: sudo apt install calligrawords```\n\n```dnf: sudo dnf install calligra```\n\n```pacman: sudo pacman -S calligra```\n\n```apk: sudo apk add calligra```"],["SEE ALSO","[calligrasheets](/man/calligrasheets)(1), [calligrastage](/man/calligrastage)(1), [libreoffice](/man/libreoffice)(1)"],["RESOURCES","```[Source code](https://invent.kde.org/office/calligra)```\n\n```[Homepage](https://calligra.org/)```"]],"e":["calligrasheets","calligrastage","libreoffice"],"x":"calligrawords"},"chdman":{"n":"chdman","t":"MAME Compressed Hunks of Data manager","s":[["TAGLINE","MAME Compressed Hunks of Data manager"],["TLDR","**Display CHD file information**\n\n```chdman info -i [path/to/image.chd]```\n\n**Verify CHD file integrity**\n\n```chdman verify -i [path/to/image.chd]```\n\n**Create CHD from CD image (bin/cue)**\n\n```chdman createcd -i [path/to/image.cue] -o [path/to/output.chd]```\n\n**Create CHD from raw hard disk image**\n\n```chdman createhd -i [path/to/image.img] -o [path/to/output.chd]```\n\n**Extract CD image from CHD**\n\n```chdman extractcd -i [path/to/image.chd] -o [path/to/output.cue]```\n\n**Extract raw hard disk image from CHD**\n\n```chdman extracthd -i [path/to/image.chd] -o [path/to/output.img]```\n\n**Copy CHD with different compression**\n\n```chdman copy -i [path/to/input.chd] -o [path/to/output.chd] -c [lzma,zlib,huff,flac]```"],["SYNOPSIS","**chdman** _command_ [**-i** _input_] [**-o** _output_] [**-c** _compression_] [_options..._]"],["DESCRIPTION","**chdman** is the MAME Compressed Hunks of Data (CHD) manager. CHD is a lossless compression format designed for disk images used by MAME and other emulators, supporting hard disks, CD-ROMs, LaserDiscs, and GD-ROMs.\n\nThe format provides efficient compression while maintaining perfect data integrity, making it ideal for archiving game media. CHD supports delta compression against parent images to reduce storage for similar versions."],["PARAMETERS","**info**\n> Display CHD header information\n\n**verify**\n> Validate MD5/SHA1 checksums\n\n**createcd**\n> Create CHD from CD image (cue/bin, toc/bin, gdi)\n\n**createhd**\n> Create CHD from raw hard disk image\n\n**createraw**\n> Create CHD from raw data file\n\n**createld**\n> Create CHD from LaserDisc image\n\n**extractcd**\n> Extract CD image from CHD\n\n**extracthd**\n> Extract hard disk image from CHD\n\n**copy**\n> Copy CHD with optional recompression\n\n**-i** _file_\n> Input file\n\n**-o** _file_\n> Output file\n\n**-c** _algorithms_\n> Compression: none, or up to four comma-separated algorithms (default: lzma,zlib,huff,flac)\n\n**--outputparent** _file_\n> Create delta CHD storing only differences from parent"],["CAVEATS","Hunk size must be between 16 bytes and 1 MiB. CHD format is supported by emulators for 3DO, Dreamcast, PlayStation, Saturn, Mega CD, Neo-Geo CD, PC Engine CD, and other platforms."],["HISTORY","CHD was developed as part of the **MAME** (Multiple Arcade Machine Emulator) project to provide efficient, lossless compression for disk images while preserving exact data for accurate emulation."],["INSTALL","```apt: sudo apt install mame-tools```\n\n```dnf: sudo dnf install mame-tools```\n\n```pacman: sudo pacman -S mame-tools```\n\n```zypper: sudo zypper install mame-tools```\n\n```nix: nix profile install nixpkgs#mame-tools```"],["SEE ALSO","[mame](/man/mame)(6)"],["RESOURCES","```[Source code](https://github.com/mamedev/mame)```\n\n```[Documentation](https://docs.mamedev.org/tools/chdman.html)```"]],"e":["mame"],"x":"chdman info -i [path/to/image.chd]"},"codex":{"n":"codex","t":"OpenAI terminal-based coding agent","s":[["TAGLINE","OpenAI terminal-based coding agent"],["TLDR","**Launch interactive terminal UI**\n\n```codex```\n\n**Start with an initial prompt**\n\n```codex \"[fix the authentication bug]\"```\n\n**Run non-interactively and output results**\n\n```codex exec \"[refactor this function]\"```\n\n**Use a specific model**\n\n```codex --model [gpt-5.2-codex]```\n\n**Enable full auto mode for unattended work**\n\n```codex --full-auto```\n\n**Continue a previous session**\n\n```codex resume --last```\n\n**Attach an image for context**\n\n```codex -i [screenshot.png] \"[fix this UI bug]\"```"],["SYNOPSIS","**codex** [_options_] [_prompt_]"],["PARAMETERS","**--model**, **-m** _MODEL_\n> Override the default model (e.g., gpt-5.2-codex, codex-mini-latest)\n\n**--sandbox**, **-s** _POLICY_\n> Sandbox policy: read-only, workspace-write, or danger-full-access\n\n**--ask-for-approval**, **-a** _MODE_\n> Control approval timing: untrusted, on-failure, on-request, or never\n\n**--full-auto**\n> Enable low-friction mode with on-request approvals and workspace-write sandbox\n\n**--yolo**\n> Disable approvals and sandboxing (use only in hardened environments)\n\n**--cd**, **-C** _PATH_\n> Set working directory before processing\n\n**--add-dir** _PATH_\n> Grant additional directories write access\n\n**--image**, **-i** _PATH_\n> Attach image files to the initial prompt\n\n**--oss**\n> Use local open-source model via Ollama\n\n**--search**\n> Enable web search capability\n\n**--profile**, **-p** _NAME_\n> Load configuration profile from config.toml\n\n**--help**\n> Display help information"],["COMMANDS","**exec**, **e**\n> Run non-interactively; supports --json, --output-last-message\n\n**resume**\n> Continue a previous session by ID or use --last\n\n**fork**\n> Branch a previous session into a new thread\n\n**apply**, **a**\n> Apply latest diff from Codex Cloud task to local repository\n\n**cloud**\n> Browse or execute cloud tasks\n\n**login**\n> Authenticate via ChatGPT OAuth or API key\n\n**logout**\n> Remove stored credentials\n\n**mcp**\n> Manage Model Context Protocol servers (add, list, remove)\n\n**sandbox**\n> Run commands under macOS seatbelt or Linux Landlock sandboxing\n\n**completion**\n> Generate shell completions (bash, zsh, fish, powershell)"],["DESCRIPTION","**Codex** is OpenAI's official terminal-based coding agent. It launches into a full-screen terminal UI that can read your repository, suggest and implement code changes, and execute commands while you iterate conversationally.\n\nThe tool maintains security through OS-level sandboxing with three safety modes: **read-only** (consultative, no changes), **workspace-write** (default, asks before touching files outside workspace), and **danger-full-access** (unrestricted, use with caution). Model-generated commands run in isolated environments using macOS seatbelt or Linux Landlock.\n\nCodex integrates with ChatGPT Plus, Pro, Business, and Enterprise subscriptions, or can be used with API credits. It supports cloud tasks for offloading complex work and provides MCP integration for extended capabilities."],["CAVEATS","Requires authentication with a ChatGPT account or OpenAI API key. The **--yolo** flag bypasses all safety measures and should only be used in isolated environments. Network access is restricted by default; enable with **--search** or full-access sandbox mode."],["HISTORY","OpenAI originally launched Codex as a code autocompletion API in **August 2021**, which powered GitHub Copilot. The **Codex CLI** terminal agent was released on **April 16, 2025** under the Apache 2.0 license, with version 1.0.0 shipping in **May 2025**. Codex Cloud launched in **June 2025** for ChatGPT Plus users. The GPT-5.2-Codex model was released in **December 2025** with enhanced agentic coding capabilities."],["INSTALL","```nix: nix profile install nixpkgs#codex```"],["SEE ALSO","[claude](/man/claude)(1), [gemini](/man/gemini)(1), [opencode](/man/opencode)(1), [copilot](/man/copilot)(1)"]],"e":["claude","gemini","opencode","copilot"],"x":"codex"},"collectdmon":{"n":"collectdmon","t":"monitoring daemon wrapper for collectd with auto-restart","s":[["TAGLINE","monitoring daemon wrapper for collectd with auto-restart"],["TLDR","**Start collectd** as daemon\n\n```collectdmon```\n\n**Start with specific config**\n\n```collectdmon -c [/etc/collectd.conf]```\n\n**Start in foreground**\n\n```collectdmon -f```\n\n**Restart collectd on crash**\n\n```collectdmon -P [/var/run/collectd.pid]```"],["SYNOPSIS","**collectdmon** [_options_]"],["DESCRIPTION","**collectdmon** is a monitoring daemon wrapper for collectd. It monitors the collectd process and automatically restarts it if it terminates unexpectedly.\n\nThe daemon ensures continuous metric collection by maintaining collectd uptime. It handles signals appropriately, forwarding them to the child collectd process."],["PARAMETERS","**-c** _file_\n> Path to collectd configuration file.\n\n**-P** _file_\n> Path to PID file.\n\n**-f**\n> Run in foreground (don't daemonize).\n\n**-h**\n> Display help information."],["CONFIGURATION","**/etc/collectd.conf**\n> Configuration file passed to the monitored collectd process."],["CAVEATS","Requires collectd to be installed. Configuration issues in collectd will cause restart loops. PID file location needs proper permissions."],["HISTORY","**collectdmon** is part of the **collectd** project, a system statistics collection daemon created by **Florian Forster** in **2005**. The monitoring wrapper ensures reliability for production deployments where continuous metric collection is critical."],["INSTALL","```apt: sudo apt install collectd-core```\n\n```dnf: sudo dnf install collectd```\n\n```apk: sudo apk add collectd```\n\n```zypper: sudo zypper install collectd```\n\n```brew: brew install collectd```\n\n```nix: nix profile install nixpkgs#collectd```"],["SEE ALSO","[collectd](/man/collectd)(1)"]],"e":["collectd"],"x":"collectdmon"},"coreutils":{"n":"coreutils","t":"GNU core Unix utilities collection","s":[["TAGLINE","GNU core Unix utilities collection"],["TLDR","**List all commands** in GNU coreutils\n\n```coreutils --help```\n\n**Run a coreutils command** via the multicall binary\n\n```coreutils [ls] -la```\n\n**Check coreutils version**\n\n```coreutils --version```\n\n**Run cat through coreutils**\n\n```coreutils cat [file.txt]```\n\n**Run basename via coreutils**\n\n```coreutils basename [/path/to/file.txt]```"],["SYNOPSIS","**coreutils** [_command_] [_arguments_]"],["PARAMETERS","**--help**\n> List all available commands in the coreutils package.\n\n**--version**\n> Display the coreutils version information.\n\n_COMMAND_\n> Any coreutils command to execute (ls, cat, cp, mv, etc.)."],["DESCRIPTION","**GNU Coreutils** is the collection of basic file, shell, and text manipulation utilities that are expected to exist on every Unix-like operating system. The **coreutils** command itself is a multicall binary that can invoke any of the included utilities.\n\nThe package includes essential commands for file operations (cp, mv, rm, ls, mkdir), text processing (cat, head, tail, sort, uniq, wc), shell utilities (echo, printf, test, expr), and system information (uname, hostname, uptime, who).\n\nOn most Linux systems, these utilities are installed as individual binaries in /usr/bin or /bin. The multicall binary is useful in minimal environments like containers or embedded systems where a single binary provides all functionality, similar to BusyBox."],["INCLUDED COMMANDS","**File operations**: cp, mv, rm, ln, mkdir, rmdir, touch, chmod, chown, ls, dir, vdir\n\n**Text processing**: cat, head, tail, sort, uniq, wc, cut, paste, join, tr, fold\n\n**Shell utilities**: echo, printf, test, expr, true, false, yes, seq, sleep\n\n**System info**: uname, hostname, uptime, who, whoami, id, groups, date"],["CAVEATS","Behavior may differ between GNU coreutils and BSD/macOS implementations. Some options are GNU extensions not available on other Unix systems. The multicall binary may not be installed by default on all distributions."],["HISTORY","GNU Coreutils was formed in **2002** by merging three GNU packages: fileutils, shellutils, and textutils. These utilities were created as part of the GNU Project, started by Richard Stallman in **1983**, to provide free replacements for proprietary Unix tools."],["INSTALL","```apt: sudo apt install rust-coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[ls](/man/ls)(1), [cp](/man/cp)(1), [mv](/man/mv)(1), [cat](/man/cat)(1), [busybox](/man/busybox)(1)"],["RESOURCES","```[Source code](https://git.savannah.gnu.org/cgit/coreutils.git)```\n\n```[Homepage](https://www.gnu.org/software/coreutils/)```\n\n```[Documentation](https://www.gnu.org/software/coreutils/manual/)```"]],"e":["ls","cp","mv","cat","busybox"],"x":"coreutils --help"},"croc":{"n":"croc","t":"secure file transfer tool","s":[["TAGLINE","secure file transfer tool"],["TLDR","**Send file**\n\n```croc send [file]```\n\n**Send multiple files**\n\n```croc send [file1] [file2] [file3]```\n\n**Send directory**\n\n```croc send [directory/]```\n\n**Receive file**\n\n```croc [code-phrase]```\n\n**Send with custom code**\n\n```croc send --code [my-secret-code] [file]```\n\n**Use custom relay**\n\n```croc --relay [relay.example.com:9009] send [file]```\n\n**Run relay server**\n\n```croc relay```"],["SYNOPSIS","**croc** [_options_] send [_send_options_] _file_\n**croc** [_options_] _code-phrase_"],["DESCRIPTION","**croc** is a tool for securely transferring files between computers. It uses a relay server and PAKE (Password Authenticated Key Exchange) for end-to-end encryption, ensuring that transferred files cannot be read by the relay server or intercepted on the network.\n\nThe tool works across different networks without requiring port forwarding or firewall configuration. Users share a short code phrase, both parties connect to a relay server, and files are transferred through an encrypted channel. Local network transfers automatically use direct connections for faster speeds when possible.\n\nFeatures include resumable transfers (if interrupted, transfers can continue from where they left off), support for sending multiple files and entire directories, cross-platform compatibility (Linux, macOS, Windows), and the option to run your own relay server for complete control."],["COMMANDS","**send**\n> Send files or folders\n\n**relay**\n> Run a relay server"],["PARAMETERS","**--code** _phrase_\n> Use custom code phrase\n\n**--relay** _address_\n> Specify relay server\n\n**--yes**\n> Accept transfer without prompt\n\n**--out** _path_\n> Output directory for received files\n\n**--overwrite**\n> Overwrite existing files\n\n**--curve** _type_\n> Encryption curve (p256, p384, p521, siec)\n\n**--hash** _algorithm_\n> Hash algorithm (xxhash, imohash, md5)\n\n**--no-compress**\n> Disable compression\n\n**--no-local**\n> Disable local network transfer\n\n**--ports** _range_\n> Port range for local transfer"],["ENVIRONMENT","**CROC_RELAY**\n> Default relay server\n\n**CROC_PASS**\n> Default code phrase"],["HOW IT WORKS","1. Sender generates code phrase\n2. Both parties connect to relay\n3. PAKE establishes encrypted channel\n4. Files transfer through relay (or directly on LAN)"],["CAVEATS","Default relay is public (croc.schollz.com). For sensitive transfers, run your own relay. Code phrases should be shared securely."],["INSTALL","```pacman: sudo pacman -S croc```\n\n```apk: sudo apk add croc```\n\n```brew: brew install croc```\n\n```nix: nix profile install nixpkgs#croc```"],["SEE ALSO","[rsync](/man/rsync)(1), [scp](/man/scp)(1)"]],"e":["rsync","scp"],"x":"croc send [file]"},"csvsql":{"n":"csvsql","t":"SQL query executor and database importer for CSV","s":[["TAGLINE","SQL query executor and database importer for CSV"],["TLDR","**Execute SQL query** on a CSV file\n\n```csvsql --query \"[SELECT * FROM data WHERE id > 10]\" [data.csv]```\n\n**Generate CREATE TABLE** statement for a CSV\n\n```csvsql [data.csv]```\n\n**Import CSV into database**\n\n```csvsql --db [postgresql:///mydb] --insert [data.csv]```\n\n**Query multiple files** (tables named after filenames)\n\n```csvsql --query \"[SELECT * FROM file1 JOIN file2 ON file1.id = file2.id]\" [file1.csv] [file2.csv]```\n\n**Create table and insert** data into SQLite\n\n```csvsql --db [sqlite:///data.db] --insert --create-if-not-exists [data.csv]```\n\n**Query with aggregation**\n\n```csvsql --query \"[SELECT category, COUNT(*) FROM data GROUP BY category]\" [data.csv]```"],["SYNOPSIS","**csvsql** [_options_] _file_..."],["PARAMETERS","**--query** _SQL_\n> Execute SQL query on the CSV data.\n\n**--db** _CONNECTION_\n> Database connection string (SQLAlchemy format).\n\n**--insert**\n> Insert data into database (requires --db).\n\n**--create-if-not-exists**\n> Create table if it doesn't exist.\n\n**--tables** _NAMES_\n> Comma-separated table names (default: filenames).\n\n**--no-create**\n> Don't generate CREATE TABLE statement.\n\n**-d** _CHAR_, **--delimiter** _CHAR_\n> Field delimiter (default: comma).\n\n**-e** _ENCODING_, **--encoding** _ENCODING_\n> Input file encoding.\n\n**--no-inference**\n> Disable type inference."],["DESCRIPTION","**csvsql** is part of csvkit that enables SQL queries on CSV files or imports CSV data into databases. It creates an in-memory SQLite database for queries or connects to external databases for import operations.\n\nWithout **--query**, it outputs CREATE TABLE statements suitable for the data, useful for generating schema. With **--query**, it executes SQL against the CSV data, supporting joins, aggregations, and all SQL operations.\n\nFor database import, csvsql supports various databases through SQLAlchemy connection strings, including PostgreSQL, MySQL, SQLite, and others. It handles type inference to create appropriate column definitions."],["CAVEATS","In-memory queries load all data into RAM. Complex queries on large files may be slow. Type inference can misidentify columns. Database imports require appropriate drivers installed."],["HISTORY","csvsql is part of **csvkit**, created by Christopher Groskopf in **2011**. It bridges the gap between CSV files and databases, enabling SQL-powered data analysis without manual database setup."],["SEE ALSO","[csvkit](/man/csvkit)(1), [sqlite3](/man/sqlite3)(1)"]],"e":["csvkit","sqlite3"],"x":"csvsql --query \"[SELECT * FROM data WHERE id > 10]\" [data.csv]"},"datamash":{"n":"datamash","t":"command-line tool for basic numeric and statistical operations","s":[["TAGLINE","command-line tool for basic numeric and statistical operations"],["TLDR","Get **max, min, mean, median** of a column\n\n```seq 3 | datamash max 1 min 1 mean 1 median 1```\n\n**Group by** first column and sum the second (CSV input)\n\n```datamash -t, -g 1 sum 2 < [file.csv]```\n\nGet mean with **specific precision**\n\n```echo -e '1\\n2\\n3' | datamash -R [decimals] mean 1```\n\nGet mean **ignoring NA/NaN** values\n\n```echo -e '1\\n2\\nNa\\n3\\nNaN' | datamash --narm mean 1```"],["SYNOPSIS","**datamash** [_options_] _operation_ _column_ [_operation_ _column_...]"],["DESCRIPTION","**datamash** performs basic numeric, textual, and statistical operations on input data from the command line. It's designed for quick data analysis tasks that would otherwise require scripting or statistical software, supporting operations like sum, mean, median, standard deviation, variance, and more.\n\nInput is read from stdin or files, with columns separated by whitespace or a specified delimiter. The tool can group data by fields and compute aggregate statistics for each group, similar to SQL's GROUP BY functionality.\n\ndatamash is part of the GNU project and excels at one-liners for data exploration. It's commonly used in pipelines to analyze CSV files, log data, or any tabular text data. The tool can handle both numeric and textual operations, including counting unique values, string operations, and random sampling."],["PARAMETERS","**-R, --round** _digits_\n> Round numeric output to specified decimals\n\n**--narm**\n> Ignore NA and NaN values\n\n**-t** _char_\n> Use specified field separator\n\n**-g, --group** _fields_\n> Group by specified fields\n\n**-H, --headers**\n> First line is header (both input and output)\n\n**-s, --sort**\n> Sort the input before grouping (required if input is not already sorted by the group fields)\n\n**-W, --whitespace**\n> Use whitespace (one or more spaces/tabs) as field separator\n\n**--full**\n> Print entire input line before the operation results"],["OPERATIONS","**sum, min, max, mean, median**\n> Basic statistics\n\n**pstdev, sstdev**\n> Population/sample standard deviation\n\n**count, unique, collapse**\n> Counting and grouping\n\n**first, last, rand**\n> Selection operations"],["CAVEATS","The decimal separator follows the current locale (period in the C locale, comma in e.g. German locales). Grouping (-g) expects sorted input unless -s is given. Column numbering starts at 1."],["INSTALL","```apt: sudo apt install datamash```\n\n```dnf: sudo dnf install datamash```\n\n```pacman: sudo pacman -S datamash```\n\n```apk: sudo apk add datamash```\n\n```zypper: sudo zypper install datamash```\n\n```brew: brew install datamash```\n\n```nix: nix profile install nixpkgs#datamash```"],["SEE ALSO","[awk](/man/awk)(1), [cut](/man/cut)(1), [sort](/man/sort)(1)"],["RESOURCES","```[Homepage](https://www.gnu.org/software/datamash/)```\n\n```[Documentation](https://www.gnu.org/software/datamash/manual/)```"]],"e":["awk","cut","sort"],"x":"seq 3 | datamash max 1 min 1 mean 1 median 1"},"dbx":{"n":"dbx","t":"Databricks CLI extensions for workflow development","s":[["TAGLINE","Databricks CLI extensions for workflow development"],["TLDR","**Configure a new dbx project** in the current directory\n\n```dbx configure --profile [DEFAULT]```\n\n**Deploy a workflow** to artifact storage\n\n```dbx deploy [workflow_name]```\n\n**Launch a deployed workflow**\n\n```dbx launch [workflow_name]```\n\n**Execute a workflow** on an interactive cluster (without deploying)\n\n```dbx execute [workflow_name] --cluster-name=[cluster]```\n\n**Sync local files to DBFS** and watch for changes\n\n```dbx sync dbfs --source [path/to/directory] --dest [path/to/remote_directory]```"],["SYNOPSIS","**dbx** _command_ [_options_]"],["PARAMETERS","**configure**\n> Configure the project in the current directory.\n\n**init**\n> Generate a new project skeleton from a template.\n\n**deploy** _workflow_\n> Deploy a workflow definition and its artifacts to the Databricks workspace.\n\n**launch** _workflow_\n> Launch a previously deployed workflow as a job run.\n\n**execute** _workflow_\n> Run workflow code directly on an interactive (all-purpose) cluster.\n\n**sync**\n> Synchronize local files to DBFS or a repo, optionally watching for changes.\n\n**destroy**\n> Delete defined workflows and optionally their assets.\n\n**--profile** _PROFILE_\n> Databricks CLI connection profile to use.\n\n**--environment** _ENV_\n> Project environment defined in the deployment file (default: default)."],["DESCRIPTION","**dbx** (Databricks CLI eXtensions) is a Databricks Labs tool that extends the databricks CLI with functionality for rapid development and CI/CD of Databricks workflows. It manages deployment of jobs, workflows, and their artifacts across multiple environments and workspaces.\n\nProjects are described in a deployment file (deployment.yml) which defines workflows, cluster configurations, and dependencies. dbx versions and uploads artifacts, then creates or updates the corresponding Databricks jobs, making it suitable for automated pipelines.\n\nAuthentication reuses the profiles of the databricks CLI (~/.databrickscfg), so dbx works against any workspace configured there."],["CAVEATS","dbx has been retired by Databricks; Databricks Asset Bundles (the `databricks bundle` command in the new Databricks CLI) is the recommended replacement. Installed via pip (`pip install dbx`), it requires Python and a configured Databricks authentication profile."],["HISTORY","dbx was developed by Databricks Labs as a shortcut for \"Databricks CLI eXtensions\", filling the CI/CD gap before Databricks Asset Bundles became generally available. It was subsequently deprecated in favor of that first-party tooling."],["INSTALL","```nix: nix profile install nixpkgs#dbx```"],["SEE ALSO","[pip](/man/pip)(1), [python](/man/python)(1)"],["RESOURCES","```[Source code](https://github.com/databrickslabs/dbx)```\n\n```[Documentation](https://dbx.readthedocs.io)```"]],"e":["pip","python"],"x":"dbx configure --profile [DEFAULT]"},"dex2jar":{"n":"dex2jar","t":"Android DEX to JAR file converter","s":[["TAGLINE","Android DEX to JAR file converter"],["TLDR","**Convert DEX to JAR**\n\n```d2j-dex2jar [classes.dex]```\n\n**Convert APK to JAR**\n\n```d2j-dex2jar [app.apk]```\n\n**Specify output file**\n\n```d2j-dex2jar -o [output.jar] [classes.dex]```\n\n**Force overwrite**\n\n```d2j-dex2jar -f [classes.dex]```\n\n**Convert JAR to DEX**\n\n```d2j-jar2dex [file.jar]```"],["SYNOPSIS","**d2j-dex2jar** [_options_] _dex-file_"],["DESCRIPTION","**dex2jar** converts Android DEX (Dalvik Executable) files to Java JAR files. This enables analysis of Android applications using standard Java decompilers.\n\nThe tool is commonly used for Android reverse engineering, security analysis, and studying app behavior. It handles the differences between Dalvik bytecode and JVM bytecode."],["PARAMETERS","**-o** _file_\n> Output JAR file name.\n\n**-f**, **--force**\n> Overwrite existing output.\n\n**-e** _file_\n> Output exception details to file.\n\n**-n**, **--not-handle-exception**\n> Don't handle exceptions.\n\n**-nc**, **--no-code**\n> Don't convert code.\n\n**-d**, **--debug-info**\n> Include debug information.\n\n**-r**, **--reuse-reg**\n> Reuse registers."],["RELATED TOOLS","**d2j-jar2dex**: Convert JAR to DEX\n**d2j-apk-sign**: Sign APK files\n**d2j-asm-verify**: Verify ASM code\n**d2j-dex-recompute-checksum**: Fix DEX checksums"],["CAVEATS","Conversion may not be perfect. Obfuscated code remains obfuscated. Some Dalvik features have no JVM equivalent. Legal restrictions may apply to reverse engineering."],["HISTORY","**dex2jar** was created by **Bob Pan** (pxb1988) for Android reverse engineering. It became a standard tool in the Android security research community, often used alongside jd-gui for decompiling the resulting JAR files."],["INSTALL","```brew: brew install dex2jar```\n\n```nix: nix profile install nixpkgs#dex2jar```"],["SEE ALSO","[apktool](/man/apktool)(1), [jadx](/man/jadx)(1)"]],"e":["apktool","jadx"],"x":"d2j-dex2jar [classes.dex]"},"dmd":{"n":"dmd","t":"D programming language reference compiler","s":[["TAGLINE","D programming language reference compiler"],["TLDR","**Compile D source** file\n\n```dmd [file.d]```\n\n**Compile with output** name\n\n```dmd -of=[output] [file.d]```\n\n**Compile with optimizations**\n\n```dmd -O [file.d]```\n\n**Compile to object** file only\n\n```dmd -c [file.d]```\n\n**Include import paths**\n\n```dmd -I[/path/to/imports] [file.d]```\n\n**Enable all warnings**\n\n```dmd -w [file.d]```\n\n**Generate debug info**\n\n```dmd -g [file.d]```"],["SYNOPSIS","**dmd** [_options_] _file_..."],["PARAMETERS","_FILE_\n> D source file(s) to compile.\n\n**-of**=_FILE_\n> Output file name.\n\n**-O**\n> Enable optimizations.\n\n**-c**\n> Compile only (no link).\n\n**-I**_PATH_\n> Add import path.\n\n**-w**\n> Enable all warnings.\n\n**-g**\n> Generate debug information.\n\n**-release**\n> Compile for release (disable asserts).\n\n**-unittest**\n> Compile and run unit tests.\n\n**--help**\n> Display help information."],["CONFIGURATION","**/etc/dmd.conf** or **~/.dmd/dmd.conf**\n> Import paths, library paths, and default compiler flags."],["DESCRIPTION","**dmd** is the reference compiler for the D programming language. It compiles D source code to native machine code, supporting the full D language specification including templates, mixins, and compile-time function execution.\n\nD combines the power of low-level programming with modern language features like garbage collection, closures, and built-in unit testing. dmd provides fast compilation suitable for rapid development cycles.\n\nThe compiler includes a standard library (Phobos) with extensive functionality for common programming tasks, and supports interfacing with C and C++ code."],["CAVEATS","Reference implementation may have performance differences from alternative compilers (LDC, GDC). Some optimizations less aggressive than LLVM-based compilers. Platform support varies."],["HISTORY","dmd was created by **Walter Bright**, who began developing D in **1999** as a successor to C and C++. The first public release was in **2001**. D draws on experience from compiler development and aims to fix perceived issues in C++."],["INSTALL","```pacman: sudo pacman -S dmd```\n\n```apk: sudo apk add dmd```\n\n```zypper: sudo zypper install dmd```\n\n```brew: brew install dmd```\n\n```nix: nix profile install nixpkgs#dmd```"],["SEE ALSO","[ldc](/man/ldc)(1), [gdc](/man/gdc)(1)"]],"e":["ldc","gdc"],"x":"dmd [file.d]"},"dpigs":{"n":"dpigs","t":"show largest installed packages","s":[["TAGLINE","show largest installed packages"],["TLDR","**Show the 10 largest installed packages**\n\n```dpigs```\n\n**Show the N largest packages**\n\n```dpigs -n [20]```\n\n**Show in human-readable format**\n\n```dpigs -H```\n\n**Show largest source packages**\n\n```dpigs -S```"],["SYNOPSIS","**dpigs** [_options_]"],["DESCRIPTION","**dpigs** shows which installed packages occupy the most disk space on Debian-based systems. It queries the dpkg database to determine the installed size of each package and sorts them to identify space hogs.\n\nThe tool is particularly useful when disk space is limited and you need to identify candidates for removal. It can display either binary packages or aggregate source packages, making it easy to see which software projects consume the most space.\n\ndpigs is part of the debian-goodies package, a collection of useful utilities for Debian system administrators."],["PARAMETERS","**-n, --lines** _n_\n> Show n largest packages (default: 10)\n\n**-s, --status** _file_\n> Use alternate dpkg status file\n\n**-S, --source**\n> Show source packages instead\n\n**-H, --human-readable**\n> Human-readable sizes\n\n**-h, --help**\n> Display help"],["CAVEATS","Shows installed size, not download size. Debian/Ubuntu specific. Part of debian-goodies package."],["SEE ALSO","[dpkg-query](/man/dpkg-query)(1), [apt](/man/apt)(8), [du](/man/du)(1)"]],"e":["dpkg-query","apt","du"],"x":"dpigs"},"eselect-kernel":{"n":"eselect-kernel","t":"Kernel source symlink manager for Gentoo","s":[["TAGLINE","Kernel source symlink manager for Gentoo"],["TLDR","**List** available kernel source targets\n\n```eselect kernel list```\n\n**Set** the /usr/src/linux symlink by number\n\n```eselect kernel set [number]```\n\n**Set** the /usr/src/linux symlink by name\n\n```eselect kernel set [linux-6.1.0-gentoo]```\n\n**Show** current kernel symlink target\n\n```eselect kernel show```"],["SYNOPSIS","**eselect kernel** _action_ [_options_]"],["PARAMETERS","**list**\n> List available kernel sources with numbers, asterisk marks current selection\n\n**set** _target_\n> Set /usr/src/linux symlink to target (name or number from list)\n\n**show**\n> Display the current symlink target"],["DESCRIPTION","**eselect kernel** is a module of the eselect framework that manages the /usr/src/linux symlink on Gentoo systems. This symlink is critical as it determines which kernel source tree is used when building external kernel modules, configuring the kernel, or compiling software that needs kernel headers.\n\nWhen multiple kernel source versions are installed (common during kernel upgrades or testing), this tool provides an easy way to switch between them without manually recreating the symlink. This is particularly important for maintaining external modules like graphics drivers or virtualization software that must be compiled against the correct kernel sources matching the running or target kernel version."],["CAVEATS","Gentoo Linux specific. Requires kernel sources to be installed via emerge. Changes affect kernel module compilation paths. Does not affect the currently running kernel, only which source tree is pointed to by /usr/src/linux."],["SEE ALSO","[eselect](/man/eselect)(1), [emerge](/man/emerge)(1)"]],"e":["eselect","emerge"],"x":"eselect kernel list"},"eventlogadm":{"n":"eventlogadm","t":"Samba Windows event log manager","s":[["TAGLINE","Samba Windows event log manager"],["TLDR","**Create event log** database\n\n```eventlogadm -o addsource [Application] [/var/lib/samba/eventlog]```\n\n**Dump event log**\n\n```eventlogadm -o dump [Application]```\n\n**Write event** to log\n\n```eventlogadm -o write [Application] [message]```"],["SYNOPSIS","**eventlogadm** [_options_] _operation_ [_args_]"],["PARAMETERS","_OPERATION_\n> Operation: addsource, dump, write.\n\n**-o** _OP_\n> Operation to perform.\n\n**-s** _CONF_\n> Samba config file.\n\n**addsource** _NAME_ _PATH_\n> Create event log source.\n\n**dump** _NAME_\n> Dump log contents.\n\n**write** _NAME_ _MSG_\n> Write event."],["DESCRIPTION","**eventlogadm** is a Samba utility for managing Windows-compatible event logs on Unix systems. It allows creating, reading, and writing event log databases that Windows clients can access.\n\nThe tool works with Samba's implementation of the Windows Event Log service. Event logs are stored in TDB (Trivial Database) format and can be viewed from Windows Event Viewer.\n\neventlogadm is useful for integrating Unix logging with Windows-centric management tools."],["CONFIGURATION","**/etc/samba/smb.conf**\n> Samba server configuration that defines event log paths and settings."],["CAVEATS","Requires Samba configured. Event log format specific to Windows compatibility. Storage location depends on Samba config."],["HISTORY","eventlogadm is part of **Samba**, providing Windows event log compatibility for the Unix implementation of Windows networking protocols."],["INSTALL","```apt: sudo apt install samba```\n\n```dnf: sudo dnf install samba```\n\n```pacman: sudo pacman -S samba```\n\n```apk: sudo apk add samba-server```\n\n```zypper: sudo zypper install samba```\n\n```brew: brew install samba```\n\n```nix: nix profile install nixpkgs#samba```"],["SEE ALSO","[samba](/man/samba)(7)"]],"e":["samba"],"x":"eventlogadm -o addsource [Application] [/var/lib/samba/eventlog]"},"exec":{"n":"exec","t":"shell builtin to replace process with command","s":[["TAGLINE","shell builtin to replace process with command"],["TLDR","**Execute** a specific command, **replacing** the current shell\n\n```exec [command -with -flags]```\n\nExecute with **empty environment**\n\n```exec -c [command -with -flags]```\n\nExecute as **login shell**\n\n```exec -l [command -with -flags]```\n\nExecute with a **different name** as argv[0]\n\n```exec -a [name] [command -with -flags]```\n\n**Redirect** all shell **stdout** to a file\n\n```exec >[path/to/logfile]```\n\n**Redirect** both **stdout and stderr** to a file\n\n```exec >[path/to/logfile] 2>&1```\n\nOpen a file on **file descriptor** 3 for reading\n\n```exec 3<[path/to/file]```\n\n**Close** file descriptor 3\n\n```exec 3>&-```"],["SYNOPSIS","**exec** [_-cl_] [_-a name_] [_command_] [_arguments_] [_redirection ..._]"],["PARAMETERS","**-c**\n> Execute the command with a mostly empty environment, clearing inherited environment variables.\n\n**-l**\n> Place a dash at the beginning of argv[0], causing the command to behave as a login shell.\n\n**-a** _name_\n> Pass _name_ as argv[0] to the executed command instead of the actual command name."],["DESCRIPTION","**exec** replaces the current shell process with the specified command without creating a child process. When exec completes, the shell session ends because the shell process no longer exists.\n\nIf no command is specified but redirections are provided, the redirections take effect in the current shell. This allows redirecting stdin, stdout, or stderr for all subsequent commands, and opening or closing arbitrary file descriptors.\n\nIf the command cannot be executed, a non-interactive shell exits unless the **execfail** shell option is set. An interactive shell does not exit if exec fails."],["CAVEATS","Shell built-in command. The shell terminates after exec completes, so any commands after exec in a script will not run. File descriptor redirections without a command persist in the current shell. Behavior may vary slightly between bash, zsh, and other POSIX shells."],["INSTALL","```apk: sudo apk add execline```\n\n```brew: brew install execline```\n\n```nix: nix profile install nixpkgs#execline```"],["SEE ALSO","[bash](/man/bash)(1), [sh](/man/sh)(1), [env](/man/env)(1), [zsh](/man/zsh)(1)"]],"e":["bash","sh","env","zsh"],"x":"exec [command -with -flags]"},"file-roller":{"n":"file-roller","t":"GNOME graphical archive manager","s":[["TAGLINE","GNOME graphical archive manager"],["TLDR","**Open archive manager**\n\n```file-roller```\n\n**Open specific archive**\n\n```file-roller [archive.zip]```\n\n**Extract archive**\n\n```file-roller --extract-here [archive.tar.gz]```\n\n**Extract to directory**\n\n```file-roller --extract-to=[directory] [archive.zip]```\n\n**Create new archive**\n\n```file-roller --add [file1] [file2]```"],["SYNOPSIS","**file-roller** [_options_] [_archives_...]"],["PARAMETERS","_ARCHIVES_\n> Archive files to open.\n\n**--extract-here**\n> Extract to current directory.\n\n**--extract-to** _DIR_\n> Extract to specified directory.\n\n**--add** _FILES_\n> Create archive from files.\n\n**--add-to** _ARCHIVE_\n> Add files to existing archive.\n\n**-h**, **--help**\n> Display help information."],["DESCRIPTION","**file-roller** is the GNOME archive manager providing a graphical interface for creating, viewing, and extracting archives. It supports numerous formats through command-line tools.\n\nThe application integrates with the GNOME file manager for context menu archive operations. It handles zip, tar, gz, bz2, 7z, rar, and many other formats automatically.\n\nfile-roller provides drag-and-drop functionality and progress indication for archive operations."],["CAVEATS","Requires GNOME libraries. Format support depends on installed tools (unrar, p7zip, etc.). Command-line operation is basic."],["HISTORY","file-roller is part of the **GNOME desktop** project, providing archive management since GNOME 2. It serves as the default archive manager for GNOME and GNOME-based distributions."],["INSTALL","```apt: sudo apt install file-roller```\n\n```dnf: sudo dnf install file-roller```\n\n```pacman: sudo pacman -S file-roller```\n\n```apk: sudo apk add file-roller```\n\n```zypper: sudo zypper install file-roller```\n\n```brew: brew install file-roller```\n\n```nix: nix profile install nixpkgs#file-roller```"],["SEE ALSO","[tar](/man/tar)(1), [zip](/man/zip)(1), [7z](/man/7z)(1)"],["RESOURCES","```[Source code](https://gitlab.gnome.org/GNOME/file-roller)```"]],"e":["tar","zip","7z"],"x":"file-roller"},"gdalinfo":{"n":"gdalinfo","t":"displays raster dataset information","s":[["TAGLINE","displays raster dataset information"],["TLDR","**Show raster information**\n\n```gdalinfo [file.tif]```\n\n**Show statistics**\n\n```gdalinfo -stats [file.tif]```\n\n**Show in JSON** format\n\n```gdalinfo -json [file.tif]```\n\n**Check specific dataset**\n\n```gdalinfo [file.vrt]```\n\n**Show metadata**\n\n```gdalinfo -mm [file.tif]```"],["SYNOPSIS","**gdalinfo** [_options_] _datasetname_"],["PARAMETERS","_DATASETNAME_\n> Input raster file.\n\n**-json**\n> Output as JSON.\n\n**-stats**\n> Compute and display statistics.\n\n**-mm**\n> Compute min/max values.\n\n**-hist**\n> Compute histogram.\n\n**-nogcp**\n> Suppress GCP output.\n\n**-nomd**\n> Suppress metadata output.\n\n**--help**\n> Display help information."],["DESCRIPTION","**gdalinfo** displays information about raster datasets. It shows format, dimensions, coordinate system, bands, and metadata for geospatial raster files.\n\nThe tool reads header information and optionally computes statistics. It supports hundreds of raster formats through GDAL drivers.\n\ngdalinfo is the first step in understanding and validating geospatial raster data."],["CAVEATS","Statistics computation reads entire file. Some formats have limited metadata. Large files may be slow."],["HISTORY","gdalinfo is part of **GDAL** (Geospatial Data Abstraction Library), providing raster inspection capabilities across hundreds of formats."],["INSTALL","```apt: sudo apt install gdal-bin```\n\n```apk: sudo apk add gdal-tools```"],["SEE ALSO","[gdalwarp](/man/gdalwarp)(1), [gdal_translate](/man/gdal_translate)(1), [ogrinfo](/man/ogrinfo)(1)"],["RESOURCES","```[Documentation](https://gdal.org/programs/gdalinfo.html)```\n\n```[Source code](https://github.com/OSGeo/gdal)```"]],"e":["gdalwarp","gdal_translate","ogrinfo"],"x":"gdalinfo [file.tif]"},"gh-environment":{"n":"gh-environment","t":"manage deployment environments via gh-environments extension","s":[["TAGLINE","manage deployment environments via gh-environments extension"],["TLDR","**Install the gh-environments extension**\n\n```gh extension install katiem0/gh-environments```\n\n**List environments for a repository**\n\n```gh environments list [owner] --repo [repo]```\n\n**List environments across all repos for an owner**\n\n```gh environments list [owner]```\n\n**Create environments from a CSV file**\n\n```gh environments create [owner] --from-file [envs.csv]```\n\n**List environment secrets to CSV**\n\n```gh environments secrets list [owner] --repo [repo] -o [secrets.csv]```\n\n**List environment variables to CSV**\n\n```gh environments variables list [owner] --repo [repo] -o [vars.csv]```\n\n**Create environment secrets from a CSV file**\n\n```gh environments secrets create --from-file [secrets.csv]```"],["SYNOPSIS","**gh** **environments** _subcommand_ [_options_]"],["SUBCOMMANDS","**list**\n> Generate a CSV report of environments and metadata for a repository or owner.\n\n**create**\n> Create environments and metadata from a CSV file.\n\n**secrets list**\n> Generate a CSV report of environment secrets.\n\n**secrets create**\n> Create environment secrets from a CSV file.\n\n**variables list**\n> Generate a CSV report of environment variables.\n\n**variables create**\n> Create environment variables from a CSV file."],["PARAMETERS","**-t**, **--token** _token_\n> GitHub Personal Access Token (overrides `gh auth`).\n\n**--hostname** _host_\n> GitHub Enterprise Server hostname.\n\n**-o**, **--output-file** _file_\n> Output CSV filename for list commands.\n\n**-f**, **--from-file** _file_\n> Path to CSV file used by create commands.\n\n**-d**, **--debug**\n> Enable debug logging.\n\n**--help**\n> Show help information."],["DESCRIPTION","**gh-environments** is a GitHub CLI extension for managing repository deployment environments in bulk. It is not a built-in `gh` command; install it with `gh extension install katiem0/gh-environments`.\n\nEnvironments represent deployment targets (production, staging, etc.) with optional protection rules and environment-specific secrets and variables. The extension reads and writes CSV files, making it suitable for auditing or replicating environment configuration across many repositories.\n\nFor inspecting the **environment variables** that control the `gh` CLI itself, run `gh help environment`."],["CAVEATS","This is a third-party extension, not a core `gh` subcommand. Protection rules (required reviewers, wait timer, branch restrictions) are not fully manageable via this extension — use the web UI or the REST API for advanced configuration."],["INSTALL","```apt: sudo apt install gh```\n\n```dnf: sudo dnf install gh```\n\n```pacman: sudo pacman -S github-cli```\n\n```apk: sudo apk add github-cli```\n\n```zypper: sudo zypper install gh```\n\n```brew: brew install gh```\n\n```nix: nix profile install nixpkgs#gh```"],["SEE ALSO","[gh](/man/gh)(1), [gh-secret](/man/gh-secret)(1)"]],"e":["gh","gh-secret"],"x":"gh extension install katiem0/gh-environments"},"git-grep":{"n":"git-grep","t":"Search patterns in tracked files","s":[["TAGLINE","Search patterns in tracked files"],["TLDR","**Search for pattern**\n\n```git grep \"[pattern]\"```\n\n**Search with line numbers**\n\n```git grep -n \"[pattern]\"```\n\n**Case insensitive search**\n\n```git grep -i \"[pattern]\"```\n\n**Search in specific commit**\n\n```git grep \"[pattern]\" [commit]```\n\n**Show only filenames**\n\n```git grep -l \"[pattern]\"```"],["SYNOPSIS","**git grep** [_options_] _pattern_ [_revision_] [_--_] [_path_]"],["PARAMETERS","_PATTERN_\n> Search pattern (regex).\n\n_REVISION_\n> Commit/branch to search.\n\n**-n**, **--line-number**\n> Show line numbers.\n\n**-i**, **--ignore-case**\n> Case insensitive.\n\n**-l**, **--files-with-matches**\n> Show only filenames.\n\n**-c**, **--count**\n> Show match counts.\n\n**-w**, **--word-regexp**\n> Match whole words.\n\n**-v**, **--invert-match**\n> Select non-matching lines.\n\n**-A** _n_, **-B** _n_, **-C** _n_\n> Show _n_ lines of context after, before, or around each match.\n\n**-E**, **--extended-regexp**\n> Use extended (ERE) regex instead of the default basic (BRE) regex.\n\n**-F**, **--fixed-strings**\n> Treat pattern as literal strings, not regex.\n\n**--cached**\n> Search the index instead of the working tree.\n\n**--untracked**\n> Also search untracked files.\n\n**-e** _PATTERN_\n> Pattern argument; combine multiple **-e** with **--and**/**--or**/**--not**.\n\n**--help**\n> Display help information."],["DESCRIPTION","**git grep** searches tracked files for patterns, optimized specifically for Git repositories. Unlike regular grep, it ignores untracked files and can search any commit in the repository history.\n\nThe command is significantly faster on large repositories because it uses Git's index rather than scanning the filesystem directly. It supports the same regex syntax as grep and integrates seamlessly with Git's revision and path specifications."],["CAVEATS","Only searches tracked files. Regex syntax differs from grep. Binary files skipped by default."],["HISTORY","git grep is a core **Git** command providing optimized search that understands git's object model and history."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[grep](/man/grep)(1), [git-log](/man/git-log)(1), [rg](/man/rg)(1)"],["RESOURCES","```[Source code](https://github.com/git/git)```\n\n```[Homepage](https://git-scm.com/)```\n\n```[Documentation](https://git-scm.com/docs/git-grep)```"]],"e":["grep","git-log","rg"],"x":"git grep \"[pattern]\""},"git-write-tree":{"n":"git-write-tree","t":"Create a tree object from the current index","s":[["TAGLINE","Create a tree object from the current index"],["TLDR","**Write tree from index**\n\n```git write-tree```\n\n**Write with prefix**\n\n```git write-tree --prefix=[subdir/]```\n\n**Missing OK**\n\n```git write-tree --missing-ok```"],["SYNOPSIS","**git** **write-tree** [_options_]"],["PARAMETERS","**--prefix** _prefix_\n> Write subtree.\n\n**--missing-ok**\n> Allow missing objects."],["DESCRIPTION","**git write-tree** creates a tree object from the current index contents and prints the resulting tree object's SHA to standard output. It is a low-level plumbing command used internally by `git commit` to snapshot the staged file structure into a tree object in the Git object database.\n\nThe index must be in a fully merged state before running this command; typically `git update-index` is used first to sync the index with the working directory."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-commit-tree](/man/git-commit-tree)(1), [git-read-tree](/man/git-read-tree)(1), [git-update-index](/man/git-update-index)(1)"],["RESOURCES","```[Documentation](https://git-scm.com/docs/git-write-tree)```"]],"e":["git-commit-tree","git-read-tree","git-update-index"],"x":"git write-tree"},"goldeneye.py":{"n":"goldeneye.py","t":"HTTP stress testing tool","s":[["TAGLINE","HTTP stress testing tool"],["TLDR","**Test** a specific website\n\n```goldeneye.py [url]```\n\nTest with a custom **user-agent list** and **200 concurrent sockets**\n\n```goldeneye.py [url] -u [useragents.txt] -s 200```\n\nTest without **verifying SSL** certificate\n\n```goldeneye.py [url] -n```\n\nTest in **debug mode**\n\n```goldeneye.py [url] -d```\n\nDisplay **help**\n\n```goldeneye.py -h```"],["SYNOPSIS","**goldeneye.py** _url_ [_options_]"],["PARAMETERS","**-u**, **--useragents** _FILE_\n> File containing a list of user agents to rotate through\n\n**-s**, **--sockets** _NUM_\n> Number of concurrent sockets\n\n**-w**, **--workers** _NUM_\n> Number of workers\n\n**-n**, **--nosslcheck**\n> Do not verify SSL certificates\n\n**-d**, **--debug**\n> Enable debug output\n\n**-m**, **--method** _METHOD_\n> HTTP method: GET, POST, RANDOM\n\n**-h**, **--help**\n> Display help message"],["DESCRIPTION","**goldeneye.py** is an HTTP stress testing tool designed to test the resilience of web servers and applications against heavy load conditions. It generates concurrent HTTP connections using randomized headers and user agents to simulate traffic patterns and identify performance bottlenecks.\n\n> **WARNING**: This tool is intended ONLY for authorized security testing and load testing of systems you own or have explicit permission to test. Unauthorized use against systems you don't own is illegal and unethical."],["CAVEATS","Using this tool against systems without authorization is illegal in most jurisdictions. Only use for legitimate load testing, penetration testing with written permission, or security research on your own systems."],["HISTORY","**GoldenEye** was created by **Jan Seidl** (jseidl) as a proof-of-concept implementation of the Layer 7 DoS technique behind the older \"GoldenEye\" C tool, exploiting HTTP Keep-Alive and no-cache request headers to exhaust server connection pools."],["SEE ALSO","[ab](/man/ab)(1), [siege](/man/siege)(1), [wrk](/man/wrk)(1)"],["RESOURCES","```[Source code](https://github.com/jseidl/GoldenEye)```"]],"e":["ab","siege","wrk"],"x":"goldeneye.py [url]"},"golint":{"n":"golint","t":"Go source code style linter (deprecated)","s":[["TAGLINE","Go source code style linter (deprecated)"],["TLDR","**Lint current package**\n\n```golint .```\n\n**Lint specific file**\n\n```golint [file.go]```\n\n**Lint package recursively**\n\n```golint ./...```\n\n**Set minimum confidence**\n\n```golint -min_confidence [0.8] ./...```"],["SYNOPSIS","**golint** [_options_] [_packages_]"],["PARAMETERS","**-min_confidence** _n_\n> Minimum confidence for issues (0.0-1.0).\n\n**-set_exit_status**\n> Exit with non-zero status on issues.\n\n_packages_\n> Package paths to lint."],["DESCRIPTION","**golint** checks Go source code for style issues based on the guidelines from Effective Go and the Go Code Review Comments. It focuses on naming conventions, comments, and code structure.\n\nUnlike go vet which finds bugs, golint focuses on style and readability. Issues are suggestions, not errors. The tool is deprecated in favor of staticcheck or golangci-lint."],["CAVEATS","Deprecated in favor of staticcheck or golangci-lint. Suggestions are stylistic, not bugs. High false-positive rate for some projects. Not actively maintained."],["HISTORY","golint was created by the **Go team** at Google as a style checker. It was deprecated in **2021** in favor of more comprehensive linters like staticcheck. The project recommends using golangci-lint which includes multiple linters."],["INSTALL","```nix: nix profile install nixpkgs#golint```"],["SEE ALSO","[go](/man/go)(1), [gofmt](/man/gofmt)(1), [staticcheck](/man/staticcheck)(1), [golangci-lint](/man/golangci-lint)(1)"],["RESOURCES","```[Source code](https://github.com/golang/lint)```"]],"e":["go","gofmt","staticcheck","golangci-lint"],"x":"golint ."},"google-chrome-stable":{"n":"google-chrome-stable","t":"Google Chrome stable release browser","s":[["TAGLINE","Google Chrome stable release browser"],["TLDR","**Open a URL**\n\n```google-chrome-stable [https://example.com]```\n\n**Open in incognito mode**\n\n```google-chrome-stable --incognito [https://example.com]```\n\n**Open in headless mode** and print to PDF\n\n```google-chrome-stable --headless --print-to-pdf=[output.pdf] [https://example.com]```\n\n**Start with remote debugging** enabled\n\n```google-chrome-stable --remote-debugging-port=[9222]```\n\n**Disable all extensions**\n\n```google-chrome-stable --disable-extensions```\n\n**Open in kiosk mode** (fullscreen, no UI chrome)\n\n```google-chrome-stable --kiosk [https://example.com]```\n\n**Use a custom user data directory**\n\n```google-chrome-stable --user-data-dir=[/path/to/profile]```"],["SYNOPSIS","**google-chrome-stable** [_options_] [_URL_...]"],["PARAMETERS","**--incognito**\n> Open in private browsing mode.\n\n**--headless**\n> Run without a visible UI (for automation and testing).\n\n**--print-to-pdf=**_file_\n> Print page to PDF (use with --headless).\n\n**--remote-debugging-port=**_port_\n> Enable remote debugging on the specified port.\n\n**--disable-extensions**\n> Disable all browser extensions.\n\n**--no-sandbox**\n> Disable the sandbox (for testing environments).\n\n**--disable-gpu**\n> Disable GPU hardware acceleration.\n\n**--user-data-dir=**_dir_\n> Use a custom user data directory for the profile.\n\n**--proxy-server=**_proxy_\n> Route traffic through a specified proxy server.\n\n**--window-size=**_W,H_\n> Set initial window dimensions (e.g., 1024,768).\n\n**--app=**_URL_\n> Launch URL in application mode (minimal UI).\n\n**--kiosk**\n> Launch in kiosk mode (fullscreen, restricted UI).\n\n**--new-window**\n> Open URL in a new window instead of a new tab."],["DESCRIPTION","**google-chrome-stable** is the command-line launcher for the stable release of Google Chrome. It shares most functionality with the open-source Chromium browser but includes Google-specific features like automatic updates, proprietary codecs, and integration with Google services.\n\nCommon command-line options are identical to Chromium, including headless mode, remote debugging, user profile management, and various flags for development and testing. The command name varies by system: **google-chrome**, **google-chrome-stable**, or simply **chrome** may be available."],["INSTALL","```nix: nix profile install nixpkgs#google-chrome```"],["SEE ALSO","[chromium](/man/chromium)(1), [firefox](/man/firefox)(1)"]],"e":["chromium","firefox"],"x":"google-chrome-stable [https://example.com]"},"grap2graph":{"n":"grap2graph","t":"Grap graph to image converter","s":[["TAGLINE","Grap graph to image converter"],["TLDR","**Convert grap to image**\n\n```grap2graph [file.grap] > output.png```\n\n**Specify output format**\n\n```grap2graph -Tpng [file.grap] > output.png```\n\n**Create SVG output**\n\n```grap2graph -Tsvg [file.grap] > output.svg```\n\n**Process from stdin**\n\n```echo \".G1 ... .G2\" | grap2graph > output.png```"],["SYNOPSIS","**grap2graph** [_options_] [_file_]"],["PARAMETERS","_FILE_\n> Input file containing grap code.\n\n**-T** _FORMAT_\n> Output format (png, svg, pdf, etc.).\n\n**-resolution** _DPI_\n> Set image resolution.\n\n**--help**\n> Display help information."],["DESCRIPTION","**grap2graph** converts grap graphs to image formats. It is a wrapper that pipes grap output through groff and converts the result to various image formats like PNG, SVG, and PDF.\n\nThe tool is part of the groff suite and handles the intermediate processing steps automatically, producing standalone images from grap source."],["CAVEATS","Requires groff and grap. Image quality depends on resolution settings. Part of groff package."],["HISTORY","grap2graph was developed as part of the **GNU groff** project to facilitate graph image creation from grap source."],["INSTALL","```apt: sudo apt install groff```\n\n```dnf: sudo dnf install groff```\n\n```pacman: sudo pacman -S groff```\n\n```apk: sudo apk add groff```\n\n```zypper: sudo zypper install groff```\n\n```brew: brew install groff```\n\n```nix: nix profile install nixpkgs#groff```"],["SEE ALSO","[grap](/man/grap)(1), [groff](/man/groff)(1)"]],"e":["grap","groff"],"x":"grap2graph [file.grap] > output.png"},"graphml2gv":{"n":"graphml2gv","t":"GraphML to Graphviz DOT format converter","s":[["TAGLINE","GraphML to Graphviz DOT format converter"],["TLDR","**Convert GraphML to DOT**\n\n```graphml2gv [input.graphml] -o [output.gv]```\n\n**Output to stdout**\n\n```graphml2gv [input.graphml]```\n\n**Process multiple files**\n\n```graphml2gv [file1.graphml] [file2.graphml] -o [output.gv]```"],["SYNOPSIS","**graphml2gv** [_options_] [_files_]"],["PARAMETERS","_FILES_\n> GraphML input files. If omitted, reads from standard input.\n\n**-o** _FILE_\n> Write output to the specified file.\n\n**-g** _gname_\n> Use gname as the name of the generated graph. For multiple graphs, an integer is appended.\n\n**-v**\n> Enable verbose mode.\n\n**-?**\n> Print usage information and exit."],["DESCRIPTION","**graphml2gv** converts GraphML format graphs to Graphviz DOT format. It enables using GraphML data with Graphviz visualization tools.\n\nThe tool handles the translation between the XML-based GraphML format and the GV (formerly DOT) language. As both the graph and graphics models of GV and GraphML differ significantly, the conversion is approximate. GraphML features such as hyperedges or edges containing graphs have no GV equivalent. Currently, graphml2gv only supports basic graph topology."],["CAVEATS","Part of the Graphviz package. GraphML features like hyperedges and nested graphs are not supported. Requires valid GraphML input."],["HISTORY","graphml2gv is part of the **Graphviz** project, created at **AT&T Labs** to handle format conversions for graph visualization."],["INSTALL","```apt: sudo apt install graphviz```\n\n```dnf: sudo dnf install graphviz```\n\n```pacman: sudo pacman -S graphviz```\n\n```apk: sudo apk add graphviz```\n\n```zypper: sudo zypper install graphviz```\n\n```brew: brew install graphviz```\n\n```nix: nix profile install nixpkgs#graphviz```"],["SEE ALSO","[gv2gml](/man/gv2gml)(1), [gml2gv](/man/gml2gv)(1), [dot](/man/dot)(1), [neato](/man/neato)(1), [graphviz](/man/graphviz)(1)"]],"e":["gv2gml","gml2gv","dot","neato","graphviz"],"x":"graphml2gv [input.graphml] -o [output.gv]"},"group":{"n":"group","t":"System group database file","s":[["TAGLINE","System group database file"],["TLDR","**View group file**\n\n```cat /etc/group```\n\n**Find groups for user**\n\n```groups [username]```\n\n**Find group by name**\n\n```getent group [groupname]```\n\n**List group members**\n\n```getent group [groupname] | cut -d: -f4```"],["DESCRIPTION","**/etc/group** is the system file that defines groups and their members. Each line contains a group entry with four colon-separated fields: group name, password placeholder, numeric GID, and a comma-separated list of member usernames.\n\nGroups control file access permissions and are fundamental to Unix security. Users can belong to multiple groups, with one primary group set in /etc/passwd."],["FILE FORMAT","```\ngroupname:password:GID:members\n```\n\n**Fields:**\n- **groupname**: Group name\n- **password**: Usually 'x' (see /etc/gshadow)\n- **GID**: Numeric group ID\n- **members**: Comma-separated list of users"],["RELATED COMMANDS","```bash\ngroupadd groupname      # Create group\ngroupdel groupname      # Delete group\ngroupmod -n new old     # Rename group\ngpasswd -a user group   # Add user to group\ngpasswd -d user group   # Remove user from group\nnewgrp groupname        # Switch primary group\n```"],["CAVEATS","Direct editing not recommended; use groupadd/groupmod. Changes may require logout to take effect. GIDs below 1000 typically reserved for system groups."],["SEE ALSO","[groups](/man/groups)(1), [groupadd](/man/groupadd)(8), [groupmod](/man/groupmod)(8), [passwd](/man/passwd)(5)"],["RESOURCES","```[Source code](https://github.com/shadow-maint/shadow)```"]],"e":["groups","groupadd","groupmod","passwd"],"x":"cat /etc/group"},"http-server-upload":{"n":"http-server-upload","t":"Simple zero-configuration HTTP server for uploading files","s":[["TAGLINE","Simple zero-configuration HTTP server for uploading files"],["TLDR","**Start upload server on default port 8080**\n\n```http-server-upload```\n\n**Start on a specific port with a token**\n\n```http-server-upload --port [9090] --token [my-secret-token]```\n\n**Set upload directory and max file size**\n\n```http-server-upload --upload-dir [./uploads] --max-file-size [500]```\n\n**Enable automatic folder creation**\n\n```http-server-upload --enable-folder-creation```\n\n**Configure via environment variables**\n\n```PORT=9000 UPLOAD_DIR=~/uploads/ TOKEN=secret http-server-upload```"],["SYNOPSIS","**http-server-upload** [_options_] [_uploadRootPath_]"],["PARAMETERS","Configuration is done by command line arguments or environment variables. If both are used, arguments take higher priority.\n\n**--port** _PORT_ (env: PORT)\n> Server port. Default: 8080. If the port is in use, it is automatically increased until a free port is found.\n\n**--upload-dir** _DIR_ (env: UPLOAD_DIR)\n> Directory where uploaded files are stored. Default: current working directory.\n\n**--upload-tmp-dir** _DIR_ (env: UPLOAD_TMP_DIR)\n> Temporary directory for uploads before moving to final destination.\n\n**--token** _TOKEN_ (env: TOKEN)\n> Optional token required for uploads, providing basic access protection.\n\n**--max-file-size** _MB_ (env: MAX_FILE_SIZE)\n> Maximum allowed file size in megabytes. Default: 200.\n\n**--disable-auto-port** (env: DISABLE_AUTO_PORT)\n> Prevent automatic port increment if the port is already in use.\n\n**--enable-folder-creation** (env: ENABLE_FOLDER_CREATION)\n> Automatically create folders when uploading to a non-existent path.\n\n**--path-regexp** _REGEX_ (env: PATH_REGEXP)\n> Regular expression to validate upload paths.\n\n**--index-file** _FILE_ (env: INDEX_FILE)\n> Use a custom HTML file for the upload form.\n\n**-h**, **--help**\n> Display help text."],["DESCRIPTION","**http-server-upload** is a simple zero-configuration Node.js command-line HTTP server for uploading files. When running, visit http://localhost:8080/ to access the upload form. Files are uploaded to the current working directory by default.\n\nThe tool is useful for quick file transfers without setting up full file sharing. An optional token provides basic access protection."],["CAVEATS","Not intended for production use. Requires Node.js 14.18 or higher. If the desired port is already in use, the port is automatically incremented until a free port is found."],["INSTALL","```brew: brew install http-server```\n\n```nix: nix profile install nixpkgs#http-server```"],["SEE ALSO","[http-server](/man/http-server)(1), [updog](/man/updog)(1)"]],"e":["http-server","updog"],"x":"http-server-upload"},"httping":{"n":"httping","t":"measures the latency and throughput of a web server by sending HTTP requests","s":[["TAGLINE","measures the latency and throughput of a web server by sending HTTP requests"],["TLDR","**Ping a URL**\n\n```httping [https://example.com]```\n\n**Ping with count**\n\n```httping -c [5] [https://example.com]```\n\n**Use GET instead of HEAD**\n\n```httping -G [https://example.com]```\n\n**Use SSL/HTTPS**\n\n```httping -l [https://example.com]```\n\n**Show split latency timing**\n\n```httping -S [https://example.com]```\n\n**Use persistent connection**\n\n```httping -Q [https://example.com]```\n\n**Set timeout**\n\n```httping -t [5] [https://example.com]```"],["SYNOPSIS","**httping** [_options_] _url_"],["PARAMETERS","**-c** _count_\n> Number of pings.\n\n**-G**\n> Use GET request instead of HEAD.\n\n**-l**\n> Connect using SSL (for https).\n\n**-S**\n> Split latency into connect, send, receive, etc.\n\n**-Q**\n> Use persistent connection (Keep-Alive).\n\n**-t** _seconds_\n> Timeout per request.\n\n**-i** _seconds_\n> Interval between requests.\n\n**-q**\n> Quiet mode, only show summary.\n\n**-x** _host:port_\n> Probe through the given proxy server.\n\n**-m**\n> Machine-readable output, for use in scripts.\n\n**-b**\n> Show transfer speed in KB/s (used with **-G**).\n\n**-v**\n> Verbose mode, adds standard deviation and timestamps."],["DESCRIPTION","**httping** measures the latency and throughput of a web server by sending HTTP requests and timing the response. Unlike ICMP ping, it measures actual HTTP round-trip time including network latency and server processing. Use **-S** to see breakdown of connection phases."],["INSTALL","```apt: sudo apt install httping```\n\n```dnf: sudo dnf install httping```\n\n```pacman: sudo pacman -S httping```\n\n```brew: brew install httping```\n\n```nix: nix profile install nixpkgs#httping```"],["SEE ALSO","[ping](/man/ping)(1), [curl](/man/curl)(1)"],["RESOURCES","```[Source code](https://github.com/folkertvanheusden/HTTPing)```\n\n```[Homepage](https://www.vanheusden.com/httping/)```"]],"e":["ping","curl"],"x":"httping [https://example.com]"},"idea":{"n":"idea","t":"command-line launcher for IntelliJ IDEA","s":[["TAGLINE","command-line launcher for IntelliJ IDEA"],["TLDR","**Open project**\n\n```idea [project-directory]```\n\n**Open file**\n\n```idea [file.java]```\n\n**Open at line**\n\n```idea --line [42] [file.java]```\n\n**Compare files**\n\n```idea diff [file1] [file2]```\n\n**Merge files**\n\n```idea merge [local] [remote] [base] [output]```\n\n**Wait for IDE** (use as $EDITOR)\n\n```idea --wait [file]```\n\n**Format files** with project code style\n\n```idea format -r -s [code-style.xml] [src/]```\n\n**Run inspections headlessly**\n\n```idea inspect [project_dir] [inspection-profile.xml] [out_dir]```"],["SYNOPSIS","**idea** [_options_] [_files_]"],["PARAMETERS","_FILES_\n> Files or projects to open.\n\n**--line** _NUM_\n> Open the file with the cursor at the given line number.\n\n**--column** _NUM_\n> Open at a specific column (combine with **--line**).\n\n**diff** _file1_ _file2_\n> Open the diff viewer comparing two files.\n\n**merge** _local_ _remote_ _base_ _output_\n> Open the three-way merge tool.\n\n**format** [_options_] _files_\n> Apply project code-style formatting to one or more files non-interactively.\n\n**inspect** _project_ _profile_ _output_\n> Run code inspection on a project headlessly and write the report to _output_.\n\n**installPlugins** _id_...\n> Install plugins by ID from JetBrains Marketplace or a custom repository.\n\n**--wait**\n> Block until the opened file is closed (useful as **$EDITOR**).\n\n**nosplash**\n> Skip the splash screen at startup.\n\n**dontReopenProjects**\n> Show the welcome screen instead of reopening the previous projects.\n\n**disableNonBundledPlugins**\n> Launch with only bundled plugins; helpful for troubleshooting.\n\n**--help**\n> Display help information."],["DESCRIPTION","**idea** is the command-line launcher for IntelliJ IDEA. It opens files, projects, and invokes IDE features from the terminal.\n\nThe tool supports diff, merge, and project navigation. It integrates with git and other tools requiring an editor."],["CAVEATS","Requires IntelliJ IDEA installed. Path setup needed. Resource intensive."],["HISTORY","idea is the CLI launcher for **JetBrains IntelliJ IDEA**, a popular Java IDE."],["SEE ALSO","[code](/man/code)(1), [webstorm](/man/webstorm)(1), [vim](/man/vim)(1)"]],"e":["code","webstorm","vim"],"x":"idea [project-directory]"},"jj-tag-set":{"n":"jj-tag-set","t":"creates or updates a tag pointing to a revision","s":[["TAGLINE","creates or updates a tag pointing to a revision"],["TLDR","**Create tag on current revision**\n\n```jj tag set [tagname]```\n\n**Create tag on specific revision**\n\n```jj tag set [tagname] -r [revision]```"],["SYNOPSIS","**jj** **tag** **set** [_options_] _name_"],["PARAMETERS","**-r**, **--revision** _rev_\n> Revision to tag."],["DESCRIPTION","**jj tag set** creates or updates a tag pointing to a revision. Tags are lightweight references useful for marking release points or important commits. By default, tags the current working copy parent."],["INSTALL","```pacman: sudo pacman -S jujutsu```\n\n```apk: sudo apk add jujutsu```\n\n```zypper: sudo zypper install jujutsu```\n\n```brew: brew install jujutsu```\n\n```nix: nix profile install nixpkgs#jujutsu```"],["SEE ALSO","[jj](/man/jj)(1), [jj-tag](/man/jj-tag)(1), [jj-tag-delete](/man/jj-tag-delete)(1)"]],"e":["jj","jj-tag","jj-tag-delete"],"x":"jj tag set [tagname]"},"jobs":{"n":"jobs","t":"shell builtin that displays the status of jobs started in the current shell","s":[["TAGLINE","shell builtin that displays the status of jobs started in the current shell"],["TLDR","View **jobs** spawned by current shell\n\n```jobs```\n\nList jobs with **process IDs**\n\n```jobs -l```\n\nDisplay jobs with **changed status**\n\n```jobs -n```\n\nDisplay only **process IDs**\n\n```jobs -p```\n\nDisplay **running** processes\n\n```jobs -r```\n\nDisplay **stopped** processes\n\n```jobs -s```"],["SYNOPSIS","**jobs** [_options_] [_jobspec_]"],["PARAMETERS","**-l**\n> List process IDs in addition to normal information\n\n**-n**\n> Display only jobs with status changed since last notification\n\n**-p**\n> Display only process IDs\n\n**-r**\n> Display only running jobs\n\n**-s**\n> Display only stopped jobs\n\n**-x** _command_\n> Replace jobspec in command with process group ID and execute it"],["DESCRIPTION","**jobs** is a shell builtin that displays the status of jobs started in the current shell session. Jobs are processes started with & (background) or suspended with Ctrl+Z.\n\nEach job has a job number (shown in brackets) that can be used with fg, bg, and kill commands using the %n syntax. The + indicates the current job, and - indicates the previous job."],["CAVEATS","Shell builtin - behavior may vary between shells. Options -n, -r, -s are Bash-specific. Jobs are specific to the current shell session and not visible to other shells."],["HISTORY","Job control was introduced in the C shell (csh) in the late **1970s** and adopted by other shells including Bash. It allows users to manage multiple processes within a single terminal session."],["SEE ALSO","[fg](/man/fg)(1), [bg](/man/bg)(1), [disown](/man/disown)(1), [kill](/man/kill)(1)"]],"e":["fg","bg","disown","kill"],"x":"jobs"},"key.dns_resolver":{"n":"key.dns_resolver","t":"resolves DNS keys for the kernel keyring","s":[["TAGLINE","resolves DNS keys for the kernel keyring"],["TLDR","**Resolve DNS key**\n\n```key.dns_resolver [domain]```\n\n**Resolve DNSSEC key**\n\n```key.dns_resolver -t DNSKEY [domain]```"],["SYNOPSIS","**key.dns_resolver** [_options_] _domain_"],["PARAMETERS","_DOMAIN_\n> Domain to resolve key for.\n\n**-t** _TYPE_\n> Key type (DNSKEY, DS).\n\n**--help**\n> Display help information."],["DESCRIPTION","**key.dns_resolver** is a Linux kernel utility that resolves DNS security keys and stores them in the kernel keyring. It is primarily used for DNSSEC validation, fetching DNSKEY and DS records from DNS servers and making them available to kernel subsystems that need to verify the authenticity of DNS responses.\n\nThe tool operates as a helper program invoked by the kernel's key management facility (request-key) rather than being called directly by users in most cases. When the kernel needs a DNS key for authentication purposes, it triggers key.dns_resolver to perform the lookup and populate the keyring automatically. This mechanism supports DNS-based authentication of named entities (DANE) and other security protocols that rely on cryptographic keys published in DNS."],["CAVEATS","Kernel keyring integration. DNSSEC infrastructure required. Linux-specific."],["HISTORY","key.dns_resolver is part of the **Linux kernel** keyring utilities, supporting DNS-based key resolution for DNSSEC."],["INSTALL","```apt: sudo apt install keyutils```\n\n```dnf: sudo dnf install keyutils```\n\n```pacman: sudo pacman -S keyutils```\n\n```apk: sudo apk add keyutils```\n\n```zypper: sudo zypper install keyutils```\n\n```brew: brew install keyutils```\n\n```nix: nix profile install nixpkgs#keyutils```"],["SEE ALSO","[keyctl](/man/keyctl)(1), [dig](/man/dig)(1)"]],"e":["keyctl","dig"],"x":"key.dns_resolver [domain]"},"krita":{"n":"krita","t":"professional digital painting application","s":[["TAGLINE","professional digital painting application"],["TLDR","**Open Krita**\n\n```krita```\n\n**Open file**\n\n```krita [image.kra]```\n\n**Open multiple files**\n\n```krita [file1.png] [file2.kra]```\n\n**Export from command line**\n\n```krita --export --export-filename [output.png] [input.kra]```\n\n**Start with no splash**\n\n```krita --nosplash```"],["SYNOPSIS","**krita** [_options_] [_files_...]"],["PARAMETERS","_FILES_\n> Image files to open.\n\n**--export**\n> Export mode.\n\n**--export-filename** _FILE_\n> Export output file.\n\n**--nosplash**\n> Disable splash screen.\n\n**--new-image** _SPEC_\n> Create new image.\n\n**--help**\n> Display help information."],["DESCRIPTION","**Krita** is a professional digital painting application. It supports brushes, layers, filters, and animation.\n\nThe tool targets concept artists, illustrators, and texture artists. It has extensive brush customization."],["CAVEATS","Resource intensive. Large brush packs. KDE-based."],["HISTORY","Krita began as part of **KOffice** in 1999 and evolved into a leading open-source digital painting application."],["INSTALL","```apt: sudo apt install krita```\n\n```dnf: sudo dnf install krita```\n\n```pacman: sudo pacman -S krita```\n\n```apk: sudo apk add krita```\n\n```zypper: sudo zypper install krita```\n\n```nix: nix profile install nixpkgs#krita```"],["SEE ALSO","[gimp](/man/gimp)(1), [inkscape](/man/inkscape)(1), [blender](/man/blender)(1)"]],"e":["gimp","inkscape","blender"],"x":"krita"},"lpadmin":{"n":"lpadmin","t":"configures CUPS printers","s":[["TAGLINE","configures CUPS printers"],["TLDR","**Add printer**\n\n```lpadmin -p [printer] -v [device://uri] -m [driver.ppd]```\n\n**Set default printer**\n\n```lpadmin -d [printer]```\n\n**Delete printer**\n\n```lpadmin -x [printer]```\n\n**Enable printer**\n\n```lpadmin -p [printer] -E```\n\n**Set printer option**\n\n```lpadmin -p [printer] -o [option=value]```\n\n**Add network printer**\n\n```lpadmin -p [printer] -v ipp://[host]/printers/[name] -m everywhere```"],["SYNOPSIS","**lpadmin** [_options_]"],["PARAMETERS","**-p** _PRINTER_\n> Printer name.\n\n**-v** _URI_\n> Device URI.\n\n**-m** _MODEL_\n> Model/PPD file.\n\n**-d** _PRINTER_\n> Set default printer.\n\n**-x** _PRINTER_\n> Delete printer.\n\n**-E**\n> Enable printer.\n\n**-o** _OPTION_\n> Set option.\n\n**--help**\n> Display help information."],["DESCRIPTION","**lpadmin** configures CUPS printers. It adds, modifies, and removes printer definitions.\n\nThe tool manages printer queues, options, and drivers. Administrator access is required."],["CAVEATS","Requires root privileges. CUPS must be running. Driver availability varies."],["HISTORY","lpadmin is part of **CUPS** (Common Unix Printing System), providing printer administration on Linux and macOS."],["INSTALL","```apt: sudo apt install cups-client```\n\n```dnf: sudo dnf install cups-client```\n\n```apk: sudo apk add cups-client```\n\n```zypper: sudo zypper install cups-client```"],["SEE ALSO","[lp](/man/lp)(1), [lpinfo](/man/lpinfo)(8), [cupsd](/man/cupsd)(8)"]],"e":["lp","lpinfo","cupsd"],"x":"lpadmin -p [printer] -v [device://uri] -m [driver.ppd]"},"mk":{"n":"mk","t":"plan 9's make replacement","s":[["TAGLINE","plan 9's make replacement"],["TLDR","**Build default target**\n\n```mk```\n\n**Build specific target**\n\n```mk [target]```\n\n**Parallel build**\n\n```mk -j```\n\n**Use specific mkfile**\n\n```mk -f [mkfile.custom]```\n\n**Dry run**\n\n```mk -n```\n\n**Force rebuild**\n\n```mk -a```"],["SYNOPSIS","**mk** [_options_] [_target_]"],["PARAMETERS","_TARGET_\n> Target to build.\n\n**-f** _FILE_\n> Use specified mkfile.\n\n**-j**\n> Parallel execution.\n\n**-n**\n> Dry run.\n\n**-a**\n> Force rebuild all.\n\n**--help**\n> Display help information."],["DESCRIPTION","**mk** is Plan 9's make replacement. It provides similar functionality with cleaner syntax.\n\nThe tool reads mkfiles for build instructions. Simpler than traditional make."],["CAVEATS","Plan 9/9front tool. Different from GNU make. Simpler rules syntax."],["HISTORY","mk was created for **Plan 9** at Bell Labs as a cleaner replacement for make."],["INSTALL","```apk: sudo apk add bmk```\n\n```brew: brew install mk```"],["SEE ALSO","[make](/man/make)(1), [ninja](/man/ninja)(1), [rc](/man/rc)(1)"]],"e":["make","ninja","rc"],"x":"mk"},"mkreiserfs":{"n":"mkreiserfs","t":"creates ReiserFS filesystems","s":[["TAGLINE","creates ReiserFS filesystems"],["TLDR","**Create ReiserFS filesystem**\n\n```mkreiserfs [/dev/sdb1]```\n\n**Force creation**\n\n```mkreiserfs -f [/dev/sdb1]```\n\n**Set label**\n\n```mkreiserfs -l \"[label]\" [/dev/sdb1]```\n\n**Set UUID**\n\n```mkreiserfs -u [uuid] [/dev/sdb1]```\n\n**Quiet mode**\n\n```mkreiserfs -q [/dev/sdb1]```"],["SYNOPSIS","**mkreiserfs** [_options_] _device_"],["PARAMETERS","_DEVICE_\n> Block device to format.\n\n**-f**\n> Force creation.\n\n**-l** _LABEL_\n> Volume label.\n\n**-u** _UUID_\n> Set UUID.\n\n**-q**\n> Quiet mode.\n\n**--help**\n> Display help information."],["DESCRIPTION","**mkreiserfs** creates ReiserFS filesystems. ReiserFS is a journaling filesystem.\n\nThe tool formats partitions with ReiserFS. Known for handling small files efficiently."],["CAVEATS","ReiserFS is legacy. Use ext4 or btrfs instead. Destroys existing data."],["HISTORY","mkreiserfs was part of **ReiserFS** tools, a journaling filesystem created by Hans Reiser."],["INSTALL","```apt: sudo apt install reiserfsprogs```"],["SEE ALSO","[mkfs](/man/mkfs)(8)"]],"e":["mkfs"],"x":"mkreiserfs [/dev/sdb1]"},"nagios2":{"n":"nagios2","t":"IT infrastructure monitoring system","s":[["TAGLINE","IT infrastructure monitoring system"],["TLDR","**Start Nagios with a configuration file**\n\n```nagios [/etc/nagios/nagios.cfg]```\n\n**Verify configuration file for errors**\n\n```nagios -v [/etc/nagios/nagios.cfg]```\n\n**Run Nagios as a daemon in the background**\n\n```nagios -d [/etc/nagios/nagios.cfg]```\n\n**Show scheduling information**\n\n```nagios -s [/etc/nagios/nagios.cfg]```"],["SYNOPSIS","**nagios** [_options_] _config_file_"],["PARAMETERS","**-v**\n> Verify configuration file for errors without starting.\n\n**-d**\n> Run as a daemon (background process).\n\n**-s**\n> Show scheduling information.\n\n**-p**\n> Show projected/pending events."],["DESCRIPTION","**nagios** (version 2) is an IT infrastructure monitoring system. It monitors hosts, services, and network devices, providing alerting and reporting for system administrators. Nagios checks are performed by plugins, with results determining host/service states. Nagios 2 is legacy; consider upgrading to Nagios 4."],["CAVEATS","Nagios 2 is no longer maintained and lacks features and security updates available in Nagios 4. Migration to Nagios 4 or alternatives like Icinga is strongly recommended."],["SEE ALSO","[nagios4](/man/nagios4)(1), [nagios](/man/nagios)(1)"]],"e":["nagios4","nagios"],"x":"nagios [/etc/nagios/nagios.cfg]"},"ncdu":{"n":"ncdu","t":"interactive disk usage analyzer with an ncurses interface","s":[["TAGLINE","interactive disk usage analyzer with an ncurses interface"],["TLDR","**Analyze disk usage** of current directory\n\n```ncdu```\n\n**Analyze specific directory**\n\n```ncdu [/path/to/directory]```\n\n**Analyze and exclude pattern**\n\n```ncdu --exclude [*.log]```\n\n**Scan in quiet mode** (for slow disks)\n\n```ncdu -q [/path/to/directory]```\n\n**Export scan results** to file\n\n```ncdu -o [scan.json] [/path/to/directory]```\n\n**Load scan results** from file\n\n```ncdu -f [scan.json]```\n\n**Enable file deletion**\n\n```ncdu [/path] && ncdu```\n\n**Scan as root** (for full filesystem)\n\n```sudo ncdu /```"],["SYNOPSIS","**ncdu** [_options_] [_path_]"],["DESCRIPTION","**ncdu** (NCurses Disk Usage) is an interactive disk usage analyzer with an ncurses interface. It scans a directory tree, then presents the results sorted by size, allowing navigation through directories to find space-consuming files.\n\nThe interface shows directory contents sorted by size with graphical bars indicating relative sizes. Users can navigate using arrow keys, enter directories, and optionally delete files or directories directly.\n\nncdu is particularly useful for identifying large files on servers, cleaning up disk space, and understanding storage usage patterns."],["PARAMETERS","**-q**\n> Quiet mode, no scanning progress.\n\n**-x**\n> Stay on one filesystem.\n\n**--exclude** _pattern_\n> Exclude files matching pattern.\n\n**-X** _file_, **--exclude-from** _file_\n> Read exclude patterns from file.\n\n**-o** _file_\n> Export scan results to JSON file.\n\n**-f** _file_\n> Load scan results from file.\n\n**-e**\n> Enable shell extension (for exports).\n\n**-r**\n> Read-only mode (disable deletion).\n\n**--si**\n> Use base 10 (SI) units.\n\n**--color** _scheme_\n> Color scheme (dark, off)."],["KEYBOARD CONTROLS","**Up/Down**: Navigate\n**Enter**: Enter directory\n**d**: Delete selected item\n**n**: Sort by name\n**s**: Sort by size\n**C**: Sort by count\n**g**: Show percentage/graph\n**i**: Show item info\n**r**: Refresh/recalculate\n**q**: Quit"],["CAVEATS","Deletion is permanent and bypasses trash. Scanning large filesystems takes time. File counts may differ from other tools due to hard links. Running without root may skip inaccessible directories."],["HISTORY","**ncdu** was created by **Yoran Heling** in **2007** as an ncurses-based alternative to du for interactive disk usage analysis. It was designed to address the difficulty of identifying large files using traditional tools. ncdu became a staple system administration tool, and version 2.0 (rewritten in Zig) was released in **2022**."],["INSTALL","```apt: sudo apt install ncdu```\n\n```dnf: sudo dnf install ncdu```\n\n```pacman: sudo pacman -S ncdu```\n\n```apk: sudo apk add ncdu```\n\n```zypper: sudo zypper install ncdu```\n\n```brew: brew install ncdu```\n\n```nix: nix profile install nixpkgs#ncdu```"],["SEE ALSO","[du](/man/du)(1), [df](/man/df)(1), [dust](/man/dust)(1), [duf](/man/duf)(1)"]],"e":["du","df","dust","duf"],"x":"ncdu"},"netcat":{"n":"netcat","t":"versatile networking utility for reading and writing data across TCP and UDP","s":[["TAGLINE","versatile networking utility for reading and writing data across TCP and UDP"],["TLDR","**Connect to a host** on a specific port\n\n```nc [host] [port]```\n\n**Listen for incoming connections** on a port\n\n```nc -l [port]```\n\n**Listen and keep listening** after client disconnects\n\n```nc -lk [port]```\n\n**Scan ports** on a host\n\n```nc -zv [host] [20-30]```\n\n**Send a file** over the network\n\n```nc [host] [port] < [file.txt]```\n\n**Receive a file** on listening side\n\n```nc -l [port] > [received_file.txt]```\n\n**Connect via UDP** instead of TCP\n\n```nc -u [host] [port]```\n\n**Set a connection timeout**\n\n```nc -w [5] [host] [port]```\n\n**Use IPv6**\n\n```nc -6 [host] [port]```"],["SYNOPSIS","**nc** [_options_] [_hostname_] [_port_]\n\n**nc** -l [_options_] [_port_]"],["PARAMETERS","**-l**\n> Listen mode; wait for incoming connections\n\n**-k**\n> Keep listening after connection closes (with -l)\n\n**-n**\n> No DNS lookup; use numeric addresses only\n\n**-p** _PORT_\n> Specify source port\n\n**-s** _ADDR_\n> Specify source IP address\n\n**-u**\n> Use UDP instead of TCP\n\n**-v**\n> Verbose output\n\n**-w** _SECS_\n> Timeout for connections and idle time\n\n**-z**\n> Zero-I/O mode; scan without sending data\n\n**-4**\n> Force IPv4 only\n\n**-6**\n> Force IPv6 only\n\n**-d**\n> Do not read from stdin\n\n**-i** _SECS_\n> Delay between lines sent/received\n\n**-r**\n> Randomize port numbers\n\n**-t**\n> Enable telnet negotiation\n\n**-C**\n> Send CRLF line endings\n\n**-x** _PROXY:PORT_\n> Connect via proxy\n\n**-X** _PROTO_\n> Proxy protocol (4=SOCKS4, 5=SOCKS5, connect=HTTPS)\n\n**-U**\n> Use Unix domain sockets\n\n**-e** _CMD_\n> Execute command on connection (traditional nc only)"],["DESCRIPTION","**nc** (netcat) is a versatile networking utility for reading and writing data across TCP and UDP connections. Often called the \"network Swiss army knife,\" it can create almost any kind of network connection and is invaluable for debugging and testing.\n\nCommon uses include: port scanning, transferring files, creating simple chat servers, network debugging, and testing network services. In listen mode (**-l**), nc waits for incoming connections; otherwise it initiates connections to remote hosts.\n\nData is read from stdin and written to stdout, making it easy to pipe data through network connections. Multiple implementations exist (OpenBSD, GNU, ncat from Nmap), with varying feature sets. The **-e** option for command execution exists in some versions but is omitted from security-focused implementations."],["CAVEATS","Different netcat implementations have different options and capabilities. OpenBSD netcat omits the **-e** flag for security reasons. Some options like **-k** may not be available in all versions. Port scanning may be detected as hostile activity. Use **ncat** (from Nmap) for consistent cross-platform behavior."],["HISTORY","Netcat was originally written by a hacker known as **Hobbit** and released in **1995**. It quickly became an essential tool for network administrators and security professionals. The original version included powerful but potentially dangerous features like **-e** for command execution. OpenBSD rewrote netcat from scratch in **2001** with a focus on security, removing risky features. The Nmap project created **ncat** as a modern reimplementation with SSL support and additional features."],["INSTALL","```dnf: sudo dnf install netcat```\n\n```brew: brew install netcat```\n\n```nix: nix profile install nixpkgs#netcat```"],["SEE ALSO","[ncat](/man/ncat)(1), [socat](/man/socat)(1), [telnet](/man/telnet)(1), [ss](/man/ss)(8)"]],"e":["ncat","socat","telnet","ss"],"x":"nc [host] [port]"},"node":{"n":"node","t":"Node.js JavaScript runtime built on Chrome's V8 engine","s":[["TAGLINE","Node.js JavaScript runtime built on Chrome's V8 engine"],["TLDR","**Run** a JavaScript file\n\n```node [script.js]```\n\n**Start the REPL** (interactive shell)\n\n```node```\n\n**Evaluate** an expression and print the result\n\n```node -e \"[console.log('hello')]\"```\n\n**Pass arguments** to a script (available via `process.argv`)\n\n```node [script.js] [arg1] [arg2]```\n\n**Run** a script with the inspector enabled and break on first line\n\n```node --inspect-brk [script.js]```\n\n**Syntax-check** without running\n\n```node --check [script.js]```\n\n**Watch and restart** on file changes (Node.js 18+)\n\n```node --watch [script.js]```\n\n**Run TypeScript** with a custom loader\n\n```node --import [tsx] [script.ts]```\n\n**Print version**\n\n```node --version```"],["SYNOPSIS","**node** [_options_] [_v8-options_] [_script_ | _-_ | -e _string_] [_arguments_]"],["PARAMETERS","**-e**, **--eval** _CODE_\n> Evaluate _code_ as JavaScript without loading a file.\n\n**-p**, **--print** _CODE_\n> Like **-e** but prints the result to stdout.\n\n**-r**, **--require** _MODULE_\n> Preload _module_ before running the main script (CommonJS only).\n\n**--import** _MODULE_\n> Preload an ES module before running the main script.\n\n**-c**, **--check**\n> Parse the script and check for syntax errors without executing it.\n\n**-i**, **--interactive**\n> Force the REPL even if stdin is not a TTY.\n\n**--inspect**[=_host:port_]\n> Enable the V8 inspector on the given host/port (default 127.0.0.1:9229).\n\n**--inspect-brk**[=_host:port_]\n> Like **--inspect** but break before user code starts.\n\n**--watch**\n> Restart the process when watched files change. (Node.js 18+)\n\n**--watch-path** _PATH_\n> Additional path to watch when **--watch** is enabled.\n\n**--enable-source-maps**\n> Use source maps for stack traces.\n\n**--no-warnings**\n> Silence all process warnings (including deprecations).\n\n**--max-old-space-size** _MB_\n> Set the V8 old-generation heap size in megabytes.\n\n**--experimental-vm-modules**\n> Enable the experimental ES module API in the `vm` module.\n\n**-v**, **--version**\n> Print Node.js version.\n\n**-h**, **--help**\n> Print Node.js command-line help."],["CONFIGURATION","**NODE_OPTIONS**\n> Space-separated list of CLI options applied before any explicit ones (e.g. `NODE_OPTIONS=\"--enable-source-maps\"`).\n\n**NODE_PATH**\n> Colon-separated list of additional directories searched for CommonJS modules.\n\n**NODE_ENV**\n> Convention used by many frameworks; not interpreted by Node itself.\n\n**package.json**\n> The presence of `\"type\": \"module\"` switches `.js` files to ES modules; `\"main\"` and `\"exports\"` define entry points."],["DESCRIPTION","**node** is the Node.js JavaScript and (with `--experimental-wasi`) WebAssembly runtime built on the V8 engine. It executes scripts on the server, exposing an event-driven, non-blocking I/O model and a large standard library (`fs`, `http`, `crypto`, `stream`, `child_process`, etc.).\n\nNode supports both CommonJS (`require`) and ECMAScript modules (`import`). Module type is determined by the file extension (`.cjs`/`.mjs`) or by the nearest `package.json`'s `\"type\"` field. Built-in modules can be imported with the `node:` specifier (e.g. `import fs from 'node:fs/promises'`).\n\nThe interpreter ships with **npm** and **npx** for package management, a built-in test runner (`node --test`), worker threads for CPU-bound parallelism, and a built-in debugger and profiler accessible through `--inspect`."],["CAVEATS","The default event loop is single-threaded — CPU-heavy work blocks all I/O unless offloaded to **worker_threads** or a child process. Long-term-support (LTS) and current release lines have different feature sets; check `node --version` and the docs before relying on flags such as **--watch** or **--import**. Some long-standing flags (like `--loader`) have been replaced by `--import`/`--experimental-loader`."],["HISTORY","Node.js was created by **Ryan Dahl** in **2009**. It is now stewarded by the **OpenJS Foundation** and follows a release schedule of even-numbered LTS lines (e.g. 18, 20, 22) supported for ~30 months and odd-numbered current releases that bring newer V8 features."],["INSTALL","```apt: sudo apt install nodejs```\n\n```pacman: sudo pacman -S nodejs```\n\n```apk: sudo apk add nodejs```\n\n```brew: brew install nodejs```\n\n```nix: nix profile install nixpkgs#nodejs```"],["SEE ALSO","[npm](/man/npm)(1), [npx](/man/npx)(1), [deno](/man/deno)(1), [bun](/man/bun)(1)"]],"e":["npm","npx","deno","bun"],"x":"node [script.js]"},"npm-pack":{"n":"npm-pack","t":"creates a tarball of a package exactly as it would be published","s":[["TAGLINE","creates a tarball of a package exactly as it would be published"],["TLDR","**Pack current package**\n\n```npm pack```\n\n**Pack specific package**\n\n```npm pack [package]```\n\n**Pack without gzip**\n\n```npm pack --pack-destination [./dist]```\n\n**Dry run (list contents)**\n\n```npm pack --dry-run```\n\n**Output as JSON**\n\n```npm pack --json```"],["SYNOPSIS","**npm pack** [_options_] [_packages_...]"],["PARAMETERS","**--dry-run**\n> Show contents without packing.\n\n**--json**\n> Output as JSON.\n\n**--pack-destination** _dir_\n> Output directory.\n\n**--workspace** _ws_\n> Pack specific workspace."],["DESCRIPTION","**npm pack** creates a tarball of a package exactly as it would be published. This is useful for testing what will be included in a published package.\n\nThe tarball can be installed locally to test the package."],["WORKFLOW","```bash"],["Create tarball","npm pack"],["Test installation","npm install ./package-1.0.0.tgz"],["Check contents without creating file","npm pack --dry-run\n```"],["FILES INCLUDED","```\n- Files in package.json \"files\"\n- package.json\n- README\n- LICENSE\n- CHANGELOG\nExcluded by .npmignore\n```"],["CAVEATS","Different from npm publish. Respects .npmignore and files field. Useful for pre-publish testing."],["HISTORY","npm pack was added to allow developers to test package contents before publishing to the registry."],["INSTALL","```pacman: sudo pacman -S npm```\n\n```apk: sudo apk add npm```\n\n```brew: brew install npm```"],["SEE ALSO","[npm](/man/npm)(1), [npm-publish](/man/npm-publish)(1), [npm-install](/man/npm-install)(1)"]],"e":["npm","npm-publish","npm-install"],"x":"npm pack"},"ntp-ctl":{"n":"ntp-ctl","t":"controls OpenNTPD","s":[["TAGLINE","controls OpenNTPD"],["TLDR","**Show NTP status**\n\n```ntpctl -s status```\n\n**Show peers**\n\n```ntpctl -s peers```\n\n**Show all information**\n\n```ntpctl -s all```\n\n**Show sensors**\n\n```ntpctl -s sensors```"],["SYNOPSIS","**ntpctl** [_options_]"],["PARAMETERS","**-s** _TYPE_\n> Show information type.\n\n**status**\n> Display sync status.\n\n**peers**\n> Display configured peers.\n\n**sensors**\n> Display time sensors.\n\n**all**\n> Display all information.\n\n**--help**\n> Display help information."],["DESCRIPTION","**ntpctl** controls OpenNTPD. Queries time synchronization status.\n\nThe tool monitors NTP daemon state. Read-only control interface."],["CAVEATS","OpenBSD specific. Works with OpenNTPD. Read-only queries."],["HISTORY","ntpctl was developed as the **control interface** for OpenNTPD on OpenBSD systems."],["INSTALL","```apt: sudo apt install ntpd-rs```\n\n```dnf: sudo dnf install ntpd-rs```\n\n```pacman: sudo pacman -S ntpd-rs```\n\n```zypper: sudo zypper install ntpd-rs```\n\n```nix: nix profile install nixpkgs#ntpd-rs```"],["SEE ALSO","[ntpd](/man/ntpd)(1), [ntpq](/man/ntpq)(1), [chronyc](/man/chronyc)(1)"]],"e":["ntpd","ntpq","chronyc"],"x":"ntpctl -s status"},"nvidia-smi-mig":{"n":"nvidia-smi-mig","t":"manages Multi-Instance GPU configurations on supported NVIDIA GPUs","s":[["TAGLINE","manages Multi-Instance GPU configurations on supported NVIDIA GPUs"],["TLDR","**List MIG devices**\n\n```nvidia-smi mig -lgip```\n\n**Create GPU instance**\n\n```nvidia-smi mig -cgi [profile_id] -C```\n\n**Delete GPU instance**\n\n```nvidia-smi mig -dci -gi [instance_id]```\n\n**List GPU instances**\n\n```nvidia-smi mig -lgi```"],["SYNOPSIS","**nvidia-smi mig** [_options_]"],["PARAMETERS","**-lgip**\n> List GPU instance profiles.\n\n**-lcip**\n> List compute instance profiles.\n\n**-lgi**\n> List GPU instances.\n\n**-lci**\n> List compute instances.\n\n**-cgi** _profile_\n> Create GPU instance.\n\n**-cci** _profile_\n> Create compute instance.\n\n**-dgi**\n> Delete GPU instances.\n\n**-dci**\n> Delete compute instances.\n\n**-gi** _id_\n> GPU instance ID.\n\n**-C**\n> Create default compute instance."],["DESCRIPTION","**nvidia-smi mig** manages Multi-Instance GPU (MIG) configurations on supported NVIDIA GPUs. MIG partitions a single GPU into multiple isolated instances for sharing among multiple users or workloads."],["CAVEATS","Only supported on NVIDIA A100, A30, and H100 GPUs with MIG capability enabled."],["SEE ALSO","[nvidia-smi](/man/nvidia-smi)(1)"]],"e":["nvidia-smi"],"x":"nvidia-smi mig -lgip"},"nvim":{"n":"nvim","t":"neovim, a modern Vim fork","s":[["TAGLINE","neovim, a modern Vim fork"],["TLDR","**Open file**\n\n```nvim [file]```\n\n**Open multiple files**\n\n```nvim [file1] [file2]```\n\n**Open at line number**\n\n```nvim +[line] [file]```\n\n**Open in diff mode**\n\n```nvim -d [file1] [file2]```\n\n**Open read-only**\n\n```nvim -R [file]```\n\n**Execute command on startup**\n\n```nvim -c \"[command]\" [file]```"],["SYNOPSIS","**nvim** [_options_] [_file_...]"],["PARAMETERS","_FILE_\n> File(s) to edit.\n\n**+**_LINE_\n> Start at line number.\n\n**-c** _COMMAND_\n> Execute command after loading.\n\n**-d**\n> Diff mode.\n\n**-R**\n> Read-only mode.\n\n**-u** _FILE_\n> Use alternate config.\n\n**--headless**\n> Run without UI.\n\n**--help**\n> Display help information."],["DESCRIPTION","**nvim** is Neovim, a modern Vim fork. Extensible text editor with Lua support.\n\nThe editor provides improved architecture over Vim. Better plugin system and async support."],["CAVEATS","Learning curve from Vim differences. Lua config preferred. Plugin ecosystem differs."],["HISTORY","Neovim was forked from **Vim in 2014** to modernize the codebase with better architecture."],["INSTALL","```apt: sudo apt install neovim```\n\n```dnf: sudo dnf install neovim```\n\n```pacman: sudo pacman -S neovim```\n\n```apk: sudo apk add neovim```\n\n```zypper: sudo zypper install neovim```\n\n```brew: brew install neovim```\n\n```nix: nix profile install nixpkgs#neovim```"],["SEE ALSO","[vim](/man/vim)(1), [vi](/man/vi)(1), [emacs](/man/emacs)(1)"]],"e":["vim","vi","emacs"],"x":"nvim [file]"},"odps":{"n":"odps","t":"command-line client for Alibaba Cloud MaxCompute (ODPS)","s":[["TAGLINE","command-line client for Alibaba Cloud MaxCompute (ODPS)"],["TLDR","**Start the interactive client**\n\n```odps```\n\n**Execute a single SQL statement**\n\n```odps -e \"[SELECT * FROM table LIMIT 10;]\"```\n\n**Run a SQL script file**\n\n```odps -f [query.sql]```\n\n**Specify the target project** and endpoint\n\n```odps --project [my_project] --endpoint [https://service.cn-shanghai.maxcompute.aliyun.com/api]```\n\n**Set output format** to CSV\n\n```odps -e \"[SELECT * FROM table;]\" --format csv```"],["SYNOPSIS","**odps** [_options_]"],["PARAMETERS","**-e** _SQL_\n> Execute a single statement and exit. Multiple statements may be separated with semicolons.\n\n**-f** _FILE_\n> Execute the SQL script read from _FILE_.\n\n**--project** _NAME_\n> MaxCompute project to use for the session. Overrides the value in **odps_config.ini**.\n\n**--endpoint** _URL_\n> MaxCompute service endpoint URL. Required when not set in the configuration file.\n\n**--access-id** _ID_\n> Alibaba Cloud Access Key ID. Reads from **odps_config.ini** by default.\n\n**--access-key** _KEY_\n> Alibaba Cloud Access Key Secret.\n\n**--format** _FORMAT_\n> Output format: text (default), csv, json.\n\n**-r**, **--retry** _N_\n> Number of times to retry a failed network call.\n\n**--help**\n> Display usage information."],["DESCRIPTION","**odps** is the official command-line client for Alibaba Cloud's MaxCompute service (formerly Open Data Processing Service, abbreviated ODPS). It opens an interactive SQL prompt against a MaxCompute project where the user can run SQL queries, manage tables, partitions, resources, and functions, and submit MapReduce or Spark jobs.\n\nNon-interactive use is supported through **-e** for one-off statements and **-f** for SQL files, which is the common pattern in scheduled pipelines. The client reads endpoint URLs and credentials from **conf/odps_config.ini** so the same binary can target multiple regions by swapping configuration files."],["CAVEATS","Requires a valid Alibaba Cloud Access Key pair with permissions on the target MaxCompute project. Each statement consumes billable compute and storage resources. Endpoint URLs are region-specific; cross-region access is generally not permitted. Java 8 or later is required because the client is a Java JAR wrapped in a shell script."],["HISTORY","The MaxCompute service launched as **ODPS** within Alibaba's internal infrastructure and was opened to public cloud customers around **2014**. The client retained the **odps** name even after the service was rebranded as MaxCompute, so legacy scripts and documentation often refer to either name interchangeably."],["SEE ALSO","[odpscmd](/man/odpscmd)(1), [odps-tunnel](/man/odps-tunnel)(1)"]],"e":["odpscmd","odps-tunnel"],"x":"odps"},"openssl-x509":{"n":"openssl-x509","t":"multi-purpose certificate utility","s":[["TAGLINE","multi-purpose certificate utility"],["TLDR","**View certificate details**\n\n```openssl x509 -in [certificate.crt] -text -noout```\n\n**Get certificate fingerprint**\n\n```openssl x509 -in [certificate.crt] -fingerprint -sha256 -noout```\n\n**Convert PEM to DER**\n\n```openssl x509 -in [cert.pem] -outform DER -out [cert.der]```\n\n**Check expiration date**\n\n```openssl x509 -in [certificate.crt] -enddate -noout```\n\n**Extract public key**\n\n```openssl x509 -in [certificate.crt] -pubkey -noout```\n\n**Create self-signed from key**\n\n```openssl x509 -req -in [csr.pem] -signkey [key.pem] -out [cert.pem] -days [365]```"],["SYNOPSIS","**openssl x509** [_options_] [-in _file_] [-out _file_]"],["PARAMETERS","**-in** _file_\n> Input certificate.\n\n**-out** _file_\n> Output file.\n\n**-text**\n> Print text form.\n\n**-noout**\n> Don't output certificate.\n\n**-fingerprint**\n> Print fingerprint.\n\n**-subject**, **-issuer**\n> Print subject/issuer.\n\n**-dates**\n> Print validity dates.\n\n**-pubkey**\n> Output public key.\n\n**-req**\n> Input is CSR.\n\n**-signkey** _file_\n> Self-sign with key."],["DESCRIPTION","**openssl x509** is a multi-purpose certificate utility. It can display information, convert formats, sign certificates, and modify certificate properties.\n\nOne of the most commonly used OpenSSL commands for certificate management."],["COMMON QUERIES","```bash"],["Subject","openssl x509 -in cert.pem -subject -noout"],["Validity dates","openssl x509 -in cert.pem -dates -noout"],["Serial number","openssl x509 -in cert.pem -serial -noout\n```"],["CAVEATS","Different from openssl req which creates certificates. Use -noout with info commands to suppress PEM output."],["HISTORY","X.509 is the ITU-T standard for PKI, with OpenSSL providing comprehensive tooling since its early versions."],["INSTALL","```apt: sudo apt install openssl```\n\n```dnf: sudo dnf install openssl```\n\n```pacman: sudo pacman -S openssl```\n\n```apk: sudo apk add openssl```\n\n```zypper: sudo zypper install openssl```\n\n```brew: brew install openssl```\n\n```nix: nix profile install nixpkgs#openssl```"],["SEE ALSO","[openssl-req](/man/openssl-req)(1), [openssl-ca](/man/openssl-ca)(1), [openssl-verify](/man/openssl-verify)(1)"]],"e":["openssl-req","openssl-ca","openssl-verify"],"x":"openssl x509 -in [certificate.crt] -text -noout"},"pam_selinux":{"n":"pam_selinux","t":"PAM module to set the default SELinux security context","s":[["TAGLINE","PAM module to set the default SELinux security context"],["TLDR","**Set SELinux context** for a session\n\n```session required pam_selinux.so```\n\n**Close session** context before other session modules\n\n```session required pam_selinux.so close```\n\n**Open session** context after other session modules\n\n```session required pam_selinux.so open```\n\n**Prompt user to select** a security context role\n\n```session required pam_selinux.so select_context```\n\n**Obtain context from PAM environment** variables\n\n```session required pam_selinux.so env_params```\n\n**Enable verbose output** to inform the user when context is set\n\n```session required pam_selinux.so open verbose```"],["SYNOPSIS","**pam_selinux.so** [**open**] [**close**] [**restore**] [**nottys**] [**debug**] [**verbose**] [**select_context**] [**env_params**] [**use_current_range**]"],["PARAMETERS","**open**\n> Execute only the open_session portion of the module, which sets the execution and terminal security contexts.\n\n**close**\n> Execute only the close_session portion of the module, which restores previous security contexts.\n\n**restore**\n> In open_session, temporarily restore security contexts as they were before the previous call of the module. Useful when open and close cannot be placed around other session modules.\n\n**nottys**\n> Do not set the security context of the controlling terminal.\n\n**debug**\n> Turn on debug messages via syslog(3).\n\n**verbose**\n> Attempt to inform the user when the security context is set.\n\n**select_context**\n> Prompt the user to select a custom role for the security context. Mutually exclusive with **env_params**.\n\n**env_params**\n> Obtain a custom security context role from PAM environment variables (SELINUX_ROLE_REQUESTED, SELINUX_LEVEL_REQUESTED, SELINUX_USE_CURRENT_RANGE). Mutually exclusive with **select_context**.\n\n**use_current_range**\n> Use the current process MLS sensitivity level rather than the default."],["DESCRIPTION","**pam_selinux** is a PAM module that sets the default SELinux security context for authenticated user sessions. It operates in two phases: **open_session** sets up the execution and controlling terminal security contexts, and **close_session** restores the previously saved contexts when the session ends.\n\nIn a typical PAM configuration, this module is called twice -- once with **close** before other session modules, and once with **open** after them. This ensures that other modules run with the caller's context, while the user session receives the appropriate SELinux context."],["RETURN VALUES","**PAM_SUCCESS**\n> Security context was set successfully.\n\n**PAM_SESSION_ERR**\n> Unable to get or set a valid context.\n\n**PAM_USER_UNKNOWN**\n> The user is not known to the system.\n\n**PAM_BUF_ERR**\n> Memory allocation failure."],["CAVEATS","Only applicable on systems with SELinux enabled. This module provides only the **session** module type. The **select_context** and **env_params** options are mutually exclusive."],["SEE ALSO","[pam](/man/pam)(8), [pam.d](/man/pam.d)(5), [chcon](/man/chcon)(1), [semanage](/man/semanage)(8), [sestatus](/man/sestatus)(8), [getenforce](/man/getenforce)(8)"]],"e":["pam","pam.d","chcon","semanage","sestatus","getenforce"],"x":"session required pam_selinux.so"},"pamixer":{"n":"pamixer","t":"controls PulseAudio and PipeWire volume from the command line","s":[["TAGLINE","controls PulseAudio and PipeWire volume from the command line"],["TLDR","**Get current volume**\n\n```pamixer --get-volume```\n\n**Set volume**\n\n```pamixer --set-volume [50]```\n\n**Increase volume**\n\n```pamixer -i [5]```\n\n**Decrease volume**\n\n```pamixer -d [5]```\n\n**Mute audio**\n\n```pamixer -m```\n\n**Unmute audio**\n\n```pamixer -u```\n\n**Toggle mute**\n\n```pamixer -t```\n\n**Set volume for specific sink**\n\n```pamixer --sink [1] --set-volume [50]```"],["SYNOPSIS","**pamixer** [_--get-volume_] [_--set-volume n_] [_-i n_] [_-d n_] [_-m_] [_-u_] [_options_]"],["PARAMETERS","**--get-volume**\n> Print current volume.\n\n**--set-volume** _N_\n> Set volume percentage.\n\n**-i** _N_, **--increase** _N_\n> Increase volume by N%.\n\n**-d** _N_, **--decrease** _N_\n> Decrease volume by N%.\n\n**-m**, **--mute**\n> Mute.\n\n**-u**, **--unmute**\n> Unmute.\n\n**-t**, **--toggle-mute**\n> Toggle mute state.\n\n**--get-mute**\n> Print mute status.\n\n**--sink** _ID_\n> Select sink (output).\n\n**--source** _ID_\n> Select source (input).\n\n**--default-source**\n> Use default source.\n\n**--list-sinks**\n> List available sinks.\n\n**--list-sources**\n> List available sources.\n\n**--allow-boost**\n> Allow volume above 100%."],["DESCRIPTION","**pamixer** controls PulseAudio and PipeWire volume from the command line. It provides simple volume management for scripting and keybindings.\n\nVolume operations work as percentages. Increase and decrease adjust relative to current level. Set-volume applies an absolute percentage.\n\nMute control toggles audio without changing volume level. The toggle command provides single-key mute/unmute functionality.\n\nSink selection targets specific audio outputs when multiple exist. Default sink is used when not specified. Sources control microphone and input levels.\n\nAllow-boost permits volume above 100%, useful for quiet audio sources. This may cause distortion with already-loud content.\n\nThe tool works with both PulseAudio and PipeWire (through its PulseAudio compatibility layer)."],["CAVEATS","Requires PulseAudio or PipeWire. Volume above 100% may distort. Sink IDs may change across reboots."],["HISTORY","**pamixer** was created as a command-line alternative to graphical PulseAudio mixers. It enables volume control through scripts, keybindings, and status bars common in minimal window managers."],["INSTALL","```apt: sudo apt install pamixer```\n\n```dnf: sudo dnf install pamixer```\n\n```pacman: sudo pacman -S pamixer```\n\n```apk: sudo apk add pamixer```\n\n```zypper: sudo zypper install pamixer```\n\n```nix: nix profile install nixpkgs#pamixer```"],["SEE ALSO","[pactl](/man/pactl)(1), [pulsemixer](/man/pulsemixer)(1), [amixer](/man/amixer)(1), [wpctl](/man/wpctl)(1)"]],"e":["pactl","pulsemixer","amixer","wpctl"],"x":"pamixer --get-volume"},"pdfjam":{"n":"pdfjam","t":"manipulates PDF pages using LaTeX and pdfpages","s":[["TAGLINE","manipulates PDF pages using LaTeX and pdfpages"],["TLDR","**Combine PDFs**\n\n```pdfjam [file1.pdf] [file2.pdf] -o [output.pdf]```\n\n**Select pages**\n\n```pdfjam [input.pdf] [1,3,5-7] -o [output.pdf]```\n\n**Rotate pages**\n\n```pdfjam --angle [90] [input.pdf] -o [output.pdf]```\n\n**Two pages per sheet**\n\n```pdfjam --nup 2x1 [input.pdf] -o [output.pdf]```\n\n**Scale pages**\n\n```pdfjam --scale [0.8] [input.pdf] -o [output.pdf]```\n\n**Set paper size**\n\n```pdfjam --paper [a4paper] [input.pdf] -o [output.pdf]```\n\n**Landscape orientation**\n\n```pdfjam --landscape [input.pdf] -o [output.pdf]```"],["SYNOPSIS","**pdfjam** [_options_] _files_ [_page-ranges_] [_--outfile file_]"],["PARAMETERS","**-o**, **--outfile** _FILE_\n> Output filename.\n\n**--nup** _COLxROW_\n> Pages per sheet.\n\n**--angle** _DEGREES_\n> Rotation angle.\n\n**--scale** _FACTOR_\n> Scale factor.\n\n**--paper** _SIZE_\n> Paper size.\n\n**--landscape**\n> Landscape orientation.\n\n**--frame** _true|false_\n> Frame around pages.\n\n**--delta** _X Y_\n> Offset adjustment.\n\n**--offset** _X Y_\n> Page offset.\n\n**--suffix** _STRING_\n> Output filename suffix."],["DESCRIPTION","**pdfjam** manipulates PDF pages using LaTeX and pdfpages. It combines, rearranges, and transforms PDF documents.\n\nPage selection uses ranges like 1-5 or lists like 1,3,5. Negative numbers count from the end.\n\nN-up printing places multiple pages on one sheet. Common layouts are 2x1, 2x2, and 3x3.\n\nRotation handles landscape documents or corrects orientation. Any angle is supported.\n\nScaling adjusts page size. Combined with paper size, this enables fitting to different formats.\n\nOutput defaults to modified input name. Suffix option adds to original filename."],["CAVEATS","Requires TeX Live or similar LaTeX distribution. Complex PDFs may not process correctly. Some PDF features may be lost."],["HISTORY","**pdfjam** was created by **David Firth** as a shell wrapper around the LaTeX pdfpages package. It provides convenient command-line access to PDF manipulation."],["INSTALL","```pacman: sudo pacman -S texlive-binextra```\n\n```apk: sudo apk add texlive-binextra```"],["SEE ALSO","[pdfunite](/man/pdfunite)(1), [pdftk](/man/pdftk)(1), [pdfpages](/man/pdfpages)(1)"]],"e":["pdfunite","pdftk","pdfpages"],"x":"pdfjam [file1.pdf] [file2.pdf] -o [output.pdf]"},"pg_checksums":{"n":"pg_checksums","t":"enables, disables, or verifies data checksums","s":[["TAGLINE","enables, disables, or verifies data checksums"],["TLDR","**Enable checksums**\n\n```pg_checksums -e -D [data_dir]```\n\n**Disable checksums**\n\n```pg_checksums -d -D [data_dir]```\n\n**Verify checksums**\n\n```pg_checksums -c -D [data_dir]```"],["SYNOPSIS","**pg_checksums** [_options_] [**-D**] _datadir_"],["PARAMETERS","**-D**, **--pgdata** _dir_\n> Data directory.\n\n**-c**, **--check**\n> Verify checksums.\n\n**-e**, **--enable**\n> Enable checksums.\n\n**-d**, **--disable**\n> Disable checksums.\n\n**-v**, **--verbose**\n> Verbose output.\n\n**-P**, **--progress**\n> Show progress."],["DESCRIPTION","**pg_checksums** enables, disables, or verifies data checksums. Checksums detect data corruption. Server must be stopped before running. Enable for production databases."],["CAVEATS","Server must be stopped before enabling or disabling checksums."],["INSTALL","```dnf: sudo dnf install postgresql17```\n\n```apk: sudo apk add postgresql17```\n\n```zypper: sudo zypper install postgresql17```\n\n```nix: nix profile install nixpkgs#pg_checksums```"],["SEE ALSO","[pg_amcheck](/man/pg_amcheck)(1), [initdb](/man/initdb)(1)"]],"e":["pg_amcheck","initdb"],"x":"pg_checksums -e -D [data_dir]"},"phpmd":{"n":"phpmd","t":"PHP Mess Detector","s":[["TAGLINE","PHP Mess Detector"],["TLDR","**Check for issues**\n\n```phpmd [src/] text [cleancode,codesize]```\n\n**Generate HTML report**\n\n```phpmd [src/] html [rulesets] --reportfile [report.html]```\n\n**Use ruleset file**\n\n```phpmd [src/] text [phpmd.xml]```\n\n**Exclude directory**\n\n```phpmd [src/] text [rulesets] --exclude [vendor]```"],["SYNOPSIS","**phpmd** _source_ _format_ _rulesets_ [_options_]"],["PARAMETERS","_SOURCE_\n> Source files or directory.\n\n_FORMAT_\n> Output format (text, xml, html).\n\n_RULESETS_\n> Rules to apply.\n\n**--reportfile** _FILE_\n> Output file.\n\n**--exclude** _PATTERN_\n> Exclude pattern.\n\n**--help**\n> Display help."],["DESCRIPTION","**phpmd** is PHP Mess Detector. Finds potential problems in code.\n\nThe tool detects code smells. Complexity and design issues."],["CAVEATS","Configurable rules. Can produce false positives."],["HISTORY","PHPMD was created for **detecting PHP code issues** and maintainability problems."],["INSTALL","```brew: brew install phpmd```"],["SEE ALSO","[phpcs](/man/phpcs)(1), [phan](/man/phan)(1), [phpstan](/man/phpstan)(1)"]],"e":["phpcs","phan","phpstan"],"x":"phpmd [src/] text [cleancode,codesize]"},"promtool":{"n":"promtool","t":"Prometheus configuration and query utility","s":[["TAGLINE","Prometheus configuration and query utility"],["TLDR","**Check Prometheus config syntax**\n\n```promtool check config [prometheus.yml]```\n\n**Check alerting rules**\n\n```promtool check rules [rules.yml]```\n\n**Test alerting rules**\n\n```promtool test rules [test.yml]```\n\n**Query Prometheus metrics**\n\n```promtool query instant [http://localhost:9090] \"[up]\"```\n\n**Query range of metrics**\n\n```promtool query range [http://localhost:9090] \"[up]\" --start [1h]```\n\n**Validate metrics exposition**\n\n```curl -s [http://localhost:9100/metrics] | promtool check metrics```\n\n**Debug metrics parsing**\n\n```promtool debug metrics [http://localhost:9090]```"],["SYNOPSIS","**promtool** _command_ _subcommand_ [_options_] [_arguments_]"],["PARAMETERS","**check config** _FILE_\n> Validate Prometheus configuration file.\n\n**check rules** _FILES_\n> Validate alerting/recording rules.\n\n**check metrics**\n> Validate metrics format from stdin.\n\n**test rules** _FILES_\n> Unit test alerting rules.\n\n**query instant** _SERVER_ _QUERY_\n> Execute instant query.\n\n**query range** _SERVER_ _QUERY_\n> Execute range query.\n\n**query labels** _SERVER_ _LABEL_\n> Query label values.\n\n**query series** _SERVER_ _MATCH_\n> Query time series.\n\n**debug pprof** _TYPE_\n> Get debug profiling data.\n\n**debug metrics** _SERVER_\n> Debug metrics endpoint.\n\n**debug all** _SERVER_\n> Get all debug information.\n\n**tsdb** _subcommand_\n> TSDB database operations (`analyze`, `dump`, `create-blocks-from`, `list`, `bench write`).\n\n**check service-discovery** _FILE_ _JOB_\n> Validate service-discovery configuration.\n\n**check web-config** _FILE_\n> Validate Prometheus web / TLS configuration.\n\n**push metrics** _URL_ _FILE_\n> Push a file of Prometheus exposition to the given remote write endpoint.\n\n**--start** _TIME_\n> Query start time.\n\n**--end** _TIME_\n> Query end time.\n\n**--step** _DURATION_\n> Query step interval."],["DESCRIPTION","**promtool** is the command-line utility for Prometheus, the popular monitoring and alerting system. It validates configuration, tests rules, and queries metrics.\n\nConfiguration checking catches syntax errors and invalid settings before applying changes. This prevents Prometheus from failing to start due to configuration issues.\n\nRule validation ensures alerting and recording rules are syntactically correct. Combined with unit testing (test rules), you can verify alert behavior before deployment. Test files specify sample data and expected alert states.\n\nQuery commands execute PromQL queries against running Prometheus servers. Instant queries return current values; range queries return time series data. This enables scripting and automation around Prometheus data.\n\nThe tsdb subcommand provides direct TSDB (time series database) manipulation: analyzing, benchmarking, and maintaining the storage layer."],["CAVEATS","Requires Prometheus knowledge to use effectively. Query commands need running Prometheus server. Rule tests require careful test data construction. Some commands access remote servers. Output format varies by command."],["HISTORY","**promtool** is part of **Prometheus**, created at **SoundCloud** around **2012** by Matt T. Proud and Julius Volz. Prometheus was open-sourced in **2015** and joined CNCF in **2016**. The promtool utility evolved alongside Prometheus to provide operational tooling for configuration management and troubleshooting."],["INSTALL","```apt: sudo apt install promtool```\n\n```dnf: sudo dnf install prometheus```\n\n```pacman: sudo pacman -S prometheus```\n\n```apk: sudo apk add prometheus```\n\n```brew: brew install prometheus```\n\n```nix: nix profile install nixpkgs#prometheus```"],["SEE ALSO","[prometheus](/man/prometheus)(1), [alertmanager](/man/alertmanager)(1), [grafana](/man/grafana)(1)"]],"e":["prometheus","alertmanager","grafana"],"x":"promtool check config [prometheus.yml]"},"pulumi-org":{"n":"pulumi-org","t":"Manage Pulumi Cloud organizations","s":[["TAGLINE","Manage Pulumi Cloud organizations"],["TLDR","**Set default organization**\n\n```pulumi org set-default [org]```\n\n**Get default organization**\n\n```pulumi org get-default```\n\n**List organizations**\n\n```pulumi org search```"],["SYNOPSIS","**pulumi** **org** _command_ [_options_]"],["PARAMETERS","**set-default** _org_\n> Set default organization.\n\n**get-default**\n> Show default organization.\n\n**search**\n> List available organizations."],["DESCRIPTION","**pulumi org** manages Pulumi Cloud organizations. Organizations group users, stacks, and settings. Set default organization for new stacks and operations."],["INSTALL","```pacman: sudo pacman -S pulumi```\n\n```apk: sudo apk add pulumi```\n\n```brew: brew install pulumi```\n\n```nix: nix profile install nixpkgs#pulumi```"],["SEE ALSO","[pulumi](/man/pulumi)(1), [pulumi-whoami](/man/pulumi-whoami)(1)"]],"e":["pulumi","pulumi-whoami"],"x":"pulumi org set-default [org]"},"pw-profiler":{"n":"pw-profiler","t":"Analyze PipeWire performance and latency","s":[["TAGLINE","Analyze PipeWire performance and latency"],["TLDR","**Profile** the default instance, logging to profile.log\n\n```pw-profiler```\n\nChange the **log output file**\n\n```pw-profiler -o path/to/file.log```\n\nProfile a **remote** instance\n\n```pw-profiler -r remote_name```\n\nDisplay **help**\n\n```pw-profiler -h```"],["SYNOPSIS","**pw-profiler** [_options_]"],["PARAMETERS","**-o**, **--output** _file_\n> Specify output log file (default: profile.log)\n\n**-r**, **--remote** _name_\n> Profile a remote PipeWire instance\n\n**-h**, **--help**\n> Display help message"],["DESCRIPTION","**pw-profiler** profiles a local or remote PipeWire instance for performance analysis. It generates log files along with gnuplot data files and HTML visualization for analyzing PipeWire performance.\n\nThe profiler captures timing information, buffer usage, and processing statistics useful for diagnosing audio latency and performance issues."],["CAVEATS","Profiling adds overhead and should be used for diagnostic purposes only. Generated files can grow large during extended profiling sessions."],["HISTORY","Part of **PipeWire**, the modern multimedia framework for Linux. Provides performance analysis tools for optimizing audio and video pipelines."],["INSTALL","```apt: sudo apt install pipewire-bin```\n\n```apk: sudo apk add pipewire-tools```\n\n```zypper: sudo zypper install pipewire-tools```"],["SEE ALSO","[pipewire](/man/pipewire)(1), [pw-top](/man/pw-top)(1), [pw-cli](/man/pw-cli)(1), [pw-dump](/man/pw-dump)(1), [pw-mon](/man/pw-mon)(1)"]],"e":["pipewire","pw-top","pw-cli","pw-dump","pw-mon"],"x":"pw-profiler"},"qumulator":{"n":"qumulator","t":"Cloud quantum-circuit simulator client","s":[["TAGLINE","Cloud quantum-circuit simulator client"],["TLDR","**Run a 1000-qubit GHZ** demonstration against the public API\n\n```qumulator demo```\n\n**Run the** Willow-layout benchmark\n\n```qumulator demo --willow```\n\n**Run the holographic** wormhole demonstration\n\n```qumulator demo --wormhole```\n\n**Run the anyon braiding** demonstration\n\n```qumulator demo --anyon```\n\n**Show instructions** for obtaining a free API key\n\n```qumulator key```\n\n**Submit a QASM circuit** and print the result\n\n```qumulator run [circuit.qasm]```\n\n**Pass an API key** explicitly\n\n```qumulator run [circuit.qasm] --key [YOUR_KEY]```"],["SYNOPSIS","**qumulator** _command_ [_options_]"],["DESCRIPTION","**qumulator** is the command-line client for the **Qumulator** cloud API, which simulates quantum circuits, spin systems, photonic amplitudes, and molecular properties on standard classical hardware. The local CLI submits jobs via HTTP and prints the result; the heavy compute runs in the cloud (Google Cloud Run, 4 vCPU, 16 GB RAM by default).\n\nThe simulator targets up to about **1000 qubits** for structured circuits without requiring a GPU or quantum hardware. The CLI is bundled with the **qumulator-sdk** Python package, so installing the SDK exposes both the Python client and the **qumulator** binary."],["COMMANDS","**demo** [**--willow** | **--wormhole** | **--anyon**]\n> Run a built-in demonstration. Without flags, runs a 1000-qubit GHZ circuit. **--willow** runs a 105-qubit Willow-layout benchmark, **--wormhole** runs a holographic wormhole simulation, **--anyon** runs an anyon-braiding computation.\n\n**key**\n> Print instructions for obtaining a free API key.\n\n**run** _circuit.qasm_\n> Submit a **QASM** file to the API and print the result."],["OPTIONS","**--key** _KEY_\n> Pass the API key directly instead of relying on the environment."],["CONFIGURATION","**QUMULATOR_API_KEY**\n> Environment variable used by all subcommands when **--key** is not supplied. Obtain a key with **qumulator key**."],["CAVEATS","Because circuits are executed in the cloud, **circuit data leaves the local machine** — do not submit circuits whose structure you consider confidential. Network latency dominates short jobs, so iterating on tiny circuits will be much slower than running them in a local simulator."],["HISTORY","**Qumulator** is published as the **qumulator-sdk** Python package. It positions itself as a way to run very large but structured quantum simulations on commodity classical hardware via a hosted API, lowering the cost of experimenting with quantum algorithms compared with paid quantum hardware time."],["SEE ALSO","[python](/man/python)(1), [pip](/man/pip)(1)"]],"e":["python","pip"],"x":"qumulator demo"},"rcon":{"n":"rcon","t":"Source RCON client for the command line","s":[["TAGLINE","Source RCON client for the command line"],["TLDR","**Send a single command**\n\n```rcon -H [host] -p [port] -P [password] status```\n\n**Multi-word server command**\n\n```rcon -H [host] -p [port] -P [password] sm plugins list```\n\n**Commands from stdin**\n\n```rcon -H [host] -p [port] -P [password] <<EOF\nstatus\nsm plugins list\nEOF```\n\n**Use a saved server profile**\n\n```rcon -s [profile] status```\n\n**Alternate config file**\n\n```rcon -c [~/.rconrc] -s [profile] status```"],["SYNOPSIS","**rcon** **-H** *host* **-p** *port* **-P** *password* [*command*...]\n**rcon** **-s** *profile* [*command*...]"],["DESCRIPTION","**rcon** is a command-line Source RCON client. It sends commands to a game server that exposes the Source RCON protocol and prints the reply on stdout. Extra argv words after the options are joined into one server command. Without a command on the argv, lines are read from stdin (lines starting with **#** are comments), so it can run script files.\n\nBuild with CMake (needs **libbsd**, **check**, **glib-2.0**). Optional bash completion via **INSTALL_BASH_COMPLETION=ON**. Packages exist on several platforms (see repology)."],["PARAMETERS","**-H** *host*\n\n> Server hostname or IP.\n\n**-p** *port*\n\n> RCON port.\n\n**-P** *password*\n\n> RCON password.\n\n**-s** *profile*\n\n> Named server entry from the config file.\n\n**-c** *file*\n\n> Config file path (default **$HOME/.rconrc**).\n\n*command*...\n\n> Server command words (concatenated). Omit to read commands from stdin."],["CONFIGURATION","Default config: **$HOME/.rconrc**. Define server profiles (host, port, password) and select them with **-s**. Prefer restricting file permissions (**chmod 600**) because credentials are stored in plain text."],["CAVEATS","RCON is not encrypted; passwords travel in cleartext on the network. Use only on trusted networks or tunnels. Exit status is 0 on success, non-zero on failure. Not related to Redis or other protocols named \"RCON\" outside Source engine games."],["SEE ALSO","[mcrcon](/man/mcrcon)(1), [netcat](/man/netcat)(1)"],["RESOURCES","```[Source code](https://github.com/n0la/rcon)```"],["INSTALL","```apk: sudo apk add rcon```\n\n```nix: nix profile install nixpkgs#rcon```"]],"e":["mcrcon","netcat"],"x":"rcon -H [host] -p [port] -P [password] status"},"rdiff-backup":{"n":"rdiff-backup","t":"Reverse differential backup with file history","s":[["TAGLINE","Reverse differential backup with file history"],["TLDR","**Backup** a local directory to a local destination\n\n```rdiff-backup [source/] [backup/]```\n\n**Backup** a local directory to a **remote** host over SSH\n\n```rdiff-backup [source/] [user@host::backup/]```\n\n**Restore** the latest version from a backup\n\n```rdiff-backup -r now [backup/] [restore/]```\n\n**Restore** files from a **specific time** ago\n\n```rdiff-backup -r [3D] [backup/] [restore/]```\n\n**List** all backup increments and their dates\n\n```rdiff-backup --list-increments [backup/]```\n\n**Remove** backup increments **older** than a given period\n\n```rdiff-backup --remove-older-than [2W] [backup/]```\n\n**Verify** the integrity of a backup repository\n\n```rdiff-backup --verify [backup/]```\n\n**Backup** excluding specific patterns\n\n```rdiff-backup --exclude '[**/*.tmp]' [source/] [backup/]```"],["SYNOPSIS","**rdiff-backup** [_-r time_] [_options_] _source_ _destination_"],["PARAMETERS","**-r**, **--restore-as-of** _TIME_\n> Restore from time.\n\n**--list-increments**\n> Show backup history.\n\n**--remove-older-than** _TIME_\n> Delete old increments.\n\n**--verify**\n> Verify backup integrity.\n\n**--include** _PATTERN_\n> Include files.\n\n**--exclude** _PATTERN_\n> Exclude files.\n\n**-v**, **--verbosity** _N_\n> Verbosity level.\n\n**--force**\n> Force operation, even if destination is not empty or increments are corrupt.\n\n**--print-statistics**\n> Print file transfer statistics after backup."],["TIME FORMATS","**now** - Current\n**3D** - 3 days ago\n**2W** - 2 weeks ago\n**1M** - 1 month ago\n**2025-01-15** - Specific date\n**2025-01-15T14:30:00** - Specific date and time"],["DESCRIPTION","**rdiff-backup** creates incremental backups using reverse diffs. The destination mirrors current state while storing history.\n\nEach backup stores only differences from previous. Space-efficient for frequent backups.\n\nRestoration from any point in history is possible. Diffs are applied to reconstruct old states.\n\nRemote backup works over SSH. No special server needed, just rdiff-backup on both ends.\n\nRetention policies remove old increments. Balance history depth against storage."],["CAVEATS","Requires rdiff-backup installed on both local and remote machines for remote backups. The initial full backup can be large and time-consuming. Metadata changes (permissions, ownership) trigger incremental updates even if file contents are unchanged."],["HISTORY","**rdiff-backup** combines rsync-style mirroring with reverse-diff versioning. It provides space-efficient incremental backup while maintaining easy restoration."],["INSTALL","```apt: sudo apt install rdiff-backup```\n\n```dnf: sudo dnf install rdiff-backup```\n\n```apk: sudo apk add rdiff-backup```\n\n```zypper: sudo zypper install rdiff-backup```\n\n```brew: brew install rdiff-backup```\n\n```nix: nix profile install nixpkgs#rdiff-backup```"],["SEE ALSO","[rsync](/man/rsync)(1), [borgbackup](/man/borgbackup)(1), [duplicity](/man/duplicity)(1), [restic](/man/restic)(1), [tar](/man/tar)(1)"]],"e":["rsync","borgbackup","duplicity","restic","tar"],"x":"rdiff-backup [source/] [backup/]"},"rmux":{"n":"rmux","t":"Programmable, tmux-compatible terminal multiplexer","s":[["TAGLINE","Programmable, tmux-compatible terminal multiplexer"],["TLDR","**Create a new detached session** named **ci**\n\n```rmux new-session -d -s [ci]```\n\n**Send a command** to a session as if typed\n\n```rmux send-keys -t [ci] \"[printf 'ok\\n']\" Enter```\n\n**Capture the current pane** output\n\n```rmux capture-pane -p -t [ci]```\n\n**Attach** to an existing session\n\n```rmux attach -t [ci]```\n\n**List** all sessions\n\n```rmux list-sessions```\n\n**Wait for** a named signal to fire\n\n```rmux wait-for [ci-done]```\n\n**Kill** a session\n\n```rmux kill-session -t [ci]```"],["SYNOPSIS","**rmux** _command_ [_command-options_]\n\n**rmux** [**-t** _target_] [**-s** _name_] [**-d**] ..."],["PARAMETERS","**new-session**, **new**\n> Create a session. **-d** detaches it; **-s** _NAME_ names it; **-c** _DIR_ sets the working directory.\n\n**attach-session**, **attach**\n> Attach the current terminal to an existing session given by **-t** _TARGET_.\n\n**list-sessions**, **ls**\n> Print all known sessions with their IDs and status.\n\n**send-keys**\n> Send keystrokes or literal text to a target pane. Special keys (**Enter**, **C-c**, **Escape**, **Up**, ...) are recognized as separate arguments.\n\n**capture-pane**\n> Dump the visible buffer of a pane. **-p** prints to stdout; **-S** _LINE_ / **-E** _LINE_ select a history range.\n\n**wait-for**\n> Block until a named signal is fired with **rmux wait-for -S** _NAME_.\n\n**kill-session**, **kill-server**\n> Terminate a session or the whole server.\n\n**-t** _TARGET_\n> Target pane or session (e.g. **ci**, **ci:0.1**).\n\n**-s** _NAME_\n> Session name when creating.\n\n**-d**\n> Detach (do not attach the terminal to the new session).\n\n**-c** _DIR_\n> Set the working directory for a new session or window."],["DESCRIPTION","**rmux** is a Rust-implemented terminal multiplexer with a tmux-compatible command surface (all 90 commands implemented) and a typed SDK for driving sessions from code. It targets agent automation, headless CI workflows, and interactive use, running natively on Linux, macOS, and Windows (including Windows Named Pipes as a local transport).\n\nSessions are persistent: a process started inside a session keeps running after the controlling terminal disconnects, and can be reattached, inspected, or driven later. Each session is a tree of windows and panes addressable by name or ID, mirroring tmux semantics so existing tmux configurations and muscle memory mostly carry over.\n\nBeyond classic multiplexing, **rmux** exposes structured snapshots and terminal-native locators that let an SDK or CLI consumer treat a TUI as an addressable surface, sending keystrokes and reading back stable representations of the screen. This makes it usable as a backing engine for AI agents or test harnesses that need to drive interactive programs deterministically."],["CAVEATS","While the command surface aims for tmux parity, edge-case behaviors and configuration semantics may diverge; existing **.tmux.conf** files are not parsed. The project is young (v0.2.0 in mid-2026); APIs and on-disk session formats may still shift. Sessions are local to the **rmux** server process and are lost if the server is killed."],["HISTORY","**rmux** is developed by **Helvesec** (**github.com/helvesec/rmux**), with v0.2.0 released **18 May 2026**. It is distributed as cross-platform binaries via a shell script (macOS/Linux), a PowerShell installer (Windows), and as a Cargo crate, and ships alongside a typed Rust SDK."],["INSTALL","```brew: brew install rmux```\n\n```nix: nix profile install nixpkgs#rmux```"],["SEE ALSO","[tmux](/man/tmux)(1), [screen](/man/screen)(1), [zellij](/man/zellij)(1), [abduco](/man/abduco)(1)"]],"e":["tmux","screen","zellij","abduco"],"x":"rmux new-session -d -s [ci]"},"sublist3r":{"n":"sublist3r","t":"Subdomain enumeration using search engines","s":[["TAGLINE","Subdomain enumeration using search engines"],["TLDR","**Enumerate subdomains**\n\n```sublist3r -d [example.com]```\n\n**Save to file**\n\n```sublist3r -d [example.com] -o [subdomains.txt]```\n\n**Use specific engines**\n\n```sublist3r -d [example.com] -e [google,bing,virustotal]```\n\n**Set threads**\n\n```sublist3r -d [example.com] -t [10]```\n\n**Enable bruteforce**\n\n```sublist3r -d [example.com] -b```\n\n**Verbose output**\n\n```sublist3r -d [example.com] -v```\n\n**Show ports**\n\n```sublist3r -d [example.com] -p [80,443]```"],["SYNOPSIS","**sublist3r** [_-d domain_] [_-o file_] [_-e engines_] [_options_]"],["PARAMETERS","**-d**, **--domain** _DOMAIN_\n> Target domain.\n\n**-o**, **--output** _FILE_\n> Output file.\n\n**-e**, **--engines** _LIST_\n> Search engines.\n\n**-b**, **--bruteforce**\n> Enable bruteforce.\n\n**-t**, **--threads** _N_\n> Thread count.\n\n**-p**, **--ports** _PORTS_\n> Scan ports.\n\n**-v**, **--verbose**\n> Verbose output."],["DESCRIPTION","**sublist3r** is an OSINT reconnaissance tool that discovers subdomains of a target domain by querying multiple search engines and data sources. It aggregates results from Google, Bing, Yahoo, Baidu, Ask, Netcraft, VirusTotal, and other sources to build a comprehensive list of subdomains.\n\nBeyond passive search engine enumeration, sublist3r can perform active DNS bruteforce using common subdomain wordlists. It also includes port scanning capabilities to identify which discovered subdomains have live services running on specified ports, helping prioritize targets during security assessments.\n\nThe tool is designed for authorized penetration testing and bug bounty reconnaissance. Multi-threading support allows faster enumeration, and results can be saved to files for further processing by other security tools."],["CAVEATS","For authorized testing only. Rate limiting may apply. Some engines require API keys."],["HISTORY","**Sublist3r** was created by **Ahmed Aboul-Ela** for subdomain enumeration. It's widely used in penetration testing and bug bounty hunting."],["SEE ALSO","[amass](/man/amass)(1), [subfinder](/man/subfinder)(1), [dnsrecon](/man/dnsrecon)(1)"]],"e":["amass","subfinder","dnsrecon"],"x":"sublist3r -d [example.com]"},"sway":{"n":"sway","t":"i3-compatible tiling Wayland compositor","s":[["TAGLINE","i3-compatible tiling Wayland compositor"],["TLDR","**Start sway** from a TTY\n\n```sway```\n\n**Start with a custom configuration file**\n\n```sway --config [path/to/config]```\n\n**Validate the configuration file** without starting\n\n```sway --validate```\n\n**Start with verbose logging**\n\n```sway --verbose```\n\n**Start with debug logging**\n\n```sway --debug```\n\n**Get the IPC socket path**\n\n```sway --get-socketpath```"],["SYNOPSIS","**sway** [_options_] [_command_]"],["PARAMETERS","**-c**, **--config** _file_\n> Use an alternative configuration file instead of the default (~/.config/sway/config).\n\n**-C**, **--validate**\n> Check the configuration file for syntax errors without starting the compositor.\n\n**-d**, **--debug**\n> Enable full logging, including debug information.\n\n**-V**, **--verbose**\n> Enable more verbose logging.\n\n**--get-socketpath**\n> Get the IPC socket path, print it, and exit.\n\n**--unsupported-gpu**\n> Allow running on unsupported GPUs (required for NVIDIA with driver 495+).\n\n**-v**, **--version**\n> Show the version number and exit.\n\n**-h**, **--help**\n> Show help message and exit."],["DESCRIPTION","**sway** is a tiling Wayland compositor and a drop-in replacement for the i3 window manager for X11. It works with existing i3 configuration files and supports most i3 features while running natively on Wayland.\n\nSway can be launched directly from a TTY or via a Wayland-compatible display manager. The configuration file is typically located at **~/.config/sway/config**, with the default template at **/etc/sway/config**. Sway communicates with clients via an IPC socket specified in the **SWAYSOCK** environment variable.\n\nThe compositor uses wlroots as its Wayland library and supports features like multiple outputs, workspaces, floating and tiling layouts, keyboard and mouse bindings, and integration with status bars like waybar and swaybar."],["CONFIGURATION","**~/.config/sway/config**\n> Main user configuration file defining keybindings, outputs, inputs, window rules, and startup commands.\n\n**/etc/sway/config**\n> System-wide default configuration template.\n\n**SWAYSOCK**\n> Environment variable pointing to the IPC socket for communicating with the running Sway instance."],["CAVEATS","Proprietary NVIDIA drivers are not officially supported. For NVIDIA driver version 495 and later, sway may work with **--unsupported-gpu** flag after enabling kernel mode setting, but this is unsupported. Open-source drivers (Intel, AMD, nouveau) are recommended."],["HISTORY","Sway was created by **Drew DeVault** (SirCmpwn) and first released in **2016** as part of an effort to bring i3's workflow to Wayland. The name stands for \"SirCmpwn's Wayland compositor\". Sway reached version 1.0 stable in **March 2019**, marking feature parity with i3. It is built on the **wlroots** library, which Drew DeVault also created to provide a modular Wayland compositor library."],["INSTALL","```apt: sudo apt install sway```\n\n```dnf: sudo dnf install sway```\n\n```pacman: sudo pacman -S sway```\n\n```apk: sudo apk add sway```\n\n```zypper: sudo zypper install sway```\n\n```nix: nix profile install nixpkgs#sway```"],["SEE ALSO","[swaymsg](/man/swaymsg)(1), [swaybar](/man/swaybar)(5), [i3](/man/i3)(1), [waybar](/man/waybar)(5)"]],"e":["swaymsg","swaybar","i3","waybar"],"x":"sway"},"symbio":{"n":"symbio","t":"Local AI agent CLI that learns from corrections via LoRA (Apple Silicon / MLX)","s":[["TAGLINE","Local AI agent CLI that learns from corrections via LoRA (Apple Silicon / MLX)"],["TLDR","**Start** interactive chat (first run opens the setup wizard)\n\n```symbio```\n\n```symb```\n\n**Show or edit** configuration\n\n```symb config```\n\n```symb config get agent.temperature```\n\n```symb config set agent.temperature 0.7```\n\n**Train** a LoRA adapter from collected data\n\n```symb train```\n\n**Manage skills** and idle adapters\n\n```symb skill list```\n\n```symb skill new [name]```\n\n```symb archive --dry-run```\n\n**Telegram** gateway\n\n```symb gateway status```\n\n```symb gateway start```"],["SYNOPSIS","**symbio** [_subcommand_] [_options_]\n\n**symb** [_subcommand_] [_options_]"],["DESCRIPTION","**symbio** (short alias **symb**) is a local AI assistant that chats in the terminal (or via Telegram), keeps markdown notes, runs sandboxed tools, and turns your corrections into **LoRA** fine-tuning data so the model improves on-device.\n\nIt uses Apple's **MLX** / Metal stack: current releases target **Apple Silicon** (recommended ~16 GB unified memory). There is no cloud API requirement for the core loop. On first launch an interactive wizard sets names, model preset, and optional features (browser, web search, mixture-of-agents dispatch, Telegram).\n\nCorrections are detected automatically (phrases like \"No, …\" / \"Actually …\") and stored as mistake notes; when **learn.mistake_threshold** (default 5) is reached, a batch LoRA update runs and is checked against a **golden set** with automatic rollback on regression. Optional **MOA** mode delegates bounded tasks to smaller worker models. Skills start as markdown procedures and can grow dedicated adapters.\n\nInstall from a clone with **pip install -e .** or **pipx install .** so **symbio** / **symb** land on **$PATH**."],["PARAMETERS","**(no subcommand)** / **chat**\n> Start the interactive chat session.\n\n**config** [**show** | **get** _key_ | **set** _key_ _value_]\n> View or change **config.json** (bot tokens redacted in show output).\n\n**train**\n> Run LoRA fine-tuning (**lora.iters**) and reload the adapter.\n\n**skill list** | **skill new** _name_ | **skill rm** _role_\n> List, create, or remove skill notes/adapters.\n\n**archive** [**--dry-run**] [**--restore** **note**|**adapter** _name_]\n> Archive idle notes/adapters or restore one.\n\n**gateway status** | **gateway start** | **gateway stop**\n> Check or control the Telegram bot gateway.\n\n**setup**\n> Re-run the interactive setup wizard."],["CONFIGURATION","**config.json** (project / install directory)\n> Model name, agent limits, LoRA hyperparameters, learn thresholds, Telegram settings, dispatch (MOA), and tool groups. Prefer **symb config set** over hand-editing secrets.\n\n**SYMBIO_TELEGRAM_TOKEN**\n> Telegram bot token; overrides the value stored in config when set.\n\n**notes/**, **training_data/**, **adapters/**\n> Markdown memory, JSONL training corpora, and LoRA adapter weights (workers live under **workers/**_role_/)."],["CAVEATS","Inference and training currently require **Apple Silicon + MLX**; CUDA/llama.cpp backends are roadmap items, not production defaults. Sandbox for shell/Python is best-effort under your user privileges. Telegram and browser features need extra config and explicit approval for dangerous actions. Model downloads and LoRA training are resource-heavy."],["HISTORY","**Symbio** is an open-source local agent (Apache-2.0) focused on correction-driven LoRA personalization without a cloud subscription. Upstream: **github.com/huyedits/Symbio**."],["SEE ALSO","[ollama](/man/ollama)(1), [pipx](/man/pipx)(1), [python](/man/python)(1), [claude](/man/claude)(1)"],["RESOURCES","```[Source code](https://github.com/huyedits/Symbio)```\n\n```[Homepage](https://huggingface.co/spaces/HuyEdits/symbio-demo)```"]],"e":["ollama","pipx","python","claude"],"x":"symbio"},"systemctl-enable":{"n":"systemctl-enable","t":"Enable unit auto-start at boot","s":[["TAGLINE","Enable unit auto-start at boot"],["TLDR","Enable a unit so it **starts at boot**\n\n```systemctl enable [unit]```\n\nEnable and **start immediately**\n\n```systemctl enable --now [unit]```\n\nEnable a **user** unit for the current user session lifecycle\n\n```systemctl enable --user [unit]```"],["SYNOPSIS","**systemctl** [_OPTIONS_...] **enable** _UNIT_..."],["PARAMETERS","**--now**\n> Also start the unit after enabling\n\n**--user**\n> Enable user service manager units instead of system units\n\n**--no-reload**\n> Do not reload systemd configuration after enabling\n\n**--force**\n> Enable units even if they are masked\n\n**--runtime**\n> Enable temporarily until next reboot only"],["DESCRIPTION","**systemctl enable** creates symbolic links in systemd directories to enable units to start automatically at boot (for system units) or at login (for user units). This does not start the unit immediately; use **--now** to both enable and start.\n\nThe symlinks are created based on the [Install] section of the unit file, typically linking into target.wants directories. Enabling is idempotent; enabling an already-enabled unit has no effect."],["CAVEATS","Requires root privileges for system units. Enabling does not start the unit; combine with **--now** or use **systemctl start** separately. Masked units cannot be enabled without **--force**."],["INSTALL","```apt: sudo apt install systemd```\n\n```dnf: sudo dnf install systemd```\n\n```pacman: sudo pacman -S systemd```\n\n```zypper: sudo zypper install systemd```\n\n```brew: brew install systemd```\n\n```nix: nix profile install nixpkgs#systemd```"],["SEE ALSO","[systemctl](/man/systemctl)(1), [systemctl-disable](/man/systemctl-disable)(1), [systemctl-start](/man/systemctl-start)(1)"]],"e":["systemctl","systemctl-disable","systemctl-start"],"x":"systemctl enable [unit]"},"systemctl-preset":{"n":"systemctl-preset","t":"Reset unit enablement to defaults","s":[["TAGLINE","Reset unit enablement to defaults"],["TLDR","Reset to **preset** defaults\n\n```systemctl preset [unit1 unit2 ...]```\n\n**Enable** only mode\n\n```systemctl preset [unit] --preset-mode enable-only```\n\n**Disable** only mode\n\n```systemctl preset [unit] --preset-mode disable-only```\n\n**Quiet** mode\n\n```systemctl preset [unit] -q```"],["SYNOPSIS","**systemctl preset** [_OPTIONS_] _UNIT_..."],["PARAMETERS","**--preset-mode=** _MODE_\n> full (default), enable-only, or disable-only\n\n**-q, --quiet**\n> Suppress output, return only exit code\n\n**--user**\n> Operate on user units"],["DESCRIPTION","**systemctl preset** resets the enablement state of specified unit files to the defaults defined in preset policy files. Preset files in `/usr/lib/systemd/system-preset/` and `/etc/systemd/system-preset/` define which units should be enabled or disabled by default.\n\nThis is typically used after package installation to set the initial enablement state according to distribution policy."],["CAVEATS","Preset policies are distribution-specific. Running preset on a unit may change whether it starts at boot. Use `--preset-mode` to limit changes to only enabling or disabling."],["HISTORY","The **preset** subcommand implements policy-based enablement, allowing distributions to define default service states while letting administrators override them."],["SEE ALSO","[systemctl-preset-all](/man/systemctl-preset-all)(1), [systemctl-enable](/man/systemctl-enable)(1)"]],"e":["systemctl-preset-all","systemctl-enable"],"x":"systemctl preset [unit1 unit2 ...]"},"time":{"n":"time","t":"Measure command execution time","s":[["TAGLINE","Measure command execution time"],["TLDR","**Measure execution time of a command**\n\n```time [command]```\n\n**Measure time with verbose output** (GNU time)\n\n```/usr/bin/time -v [command]```\n\n**Output to a file** (GNU time)\n\n```/usr/bin/time -o [time.log] [command]```\n\n**Custom output format** (GNU time)\n\n```/usr/bin/time -f \"Real: %e User: %U Sys: %S\" [command]```\n\n**Append to output file** (GNU time)\n\n```/usr/bin/time -a -o [time.log] [command]```"],["SYNOPSIS","**time** [_command_ [_arguments_...]]\n\n**/usr/bin/time** [**-v**] [**-o** _file_] [**-f** _format_] _command_ [_arguments_...]"],["PARAMETERS (GNU TIME)","**-v**, **--verbose**\n> Display detailed resource usage\n\n**-o** _file_, **--output**=_file_\n> Write timing statistics to file\n\n**-a**, **--append**\n> Append to output file instead of overwriting\n\n**-f** _format_, **--format**=_format_\n> Use specified output format\n\n**-p**, **--portability**\n> Use POSIX output format"],["FORMAT SPECIFIERS (GNU TIME)","**%e**: Elapsed real time (seconds)\n**%E**: Elapsed real time (h:mm:ss)\n**%U**: User CPU time (seconds)\n**%S**: System CPU time (seconds)\n**%P**: Percentage of CPU ((U+S)/E)\n**%M**: Maximum resident set size (KB)\n**%x**: Exit status of command\n**%C**: Command and arguments"],["DESCRIPTION","**time** measures the execution time of a command, reporting real (wall clock), user (CPU time in user mode), and system (CPU time in kernel mode) times.\n\nThere are two versions: the shell builtin (bash, zsh) and GNU time (**/usr/bin/time**). The builtin is simpler; GNU time provides more detailed resource statistics and formatting options.\n\nReal time is the actual elapsed time. User time is CPU time executing user code. System time is CPU time in kernel calls. User + System may exceed Real on multi-core systems with parallel execution, or be less if the process waits for I/O."],["CAVEATS","The shell builtin **time** takes precedence. Use **/usr/bin/time** or **\\time** for the external command with full features.\n\nFor accurate benchmarking, run multiple iterations and consider system load. Use tools like **hyperfine** for statistical benchmarking.\n\nResource measurements (memory, I/O) are only available with GNU time, not the shell builtin."],["INSTALL","```apt: sudo apt install time```\n\n```dnf: sudo dnf install time```\n\n```pacman: sudo pacman -S time```\n\n```apk: sudo apk add time```\n\n```zypper: sudo zypper install time```\n\n```nix: nix profile install nixpkgs#time```"],["SEE ALSO","[timeout](/man/timeout)(1), [watch](/man/watch)(1), [hyperfine](/man/hyperfine)(1), [perf](/man/perf)(1)"]],"e":["timeout","watch","hyperfine","perf"],"x":"time [command]"},"tinboard":{"n":"tinboard","t":"Terminal-based client for Pinboard bookmarks","s":[["TAGLINE","Terminal-based client for Pinboard bookmarks"],["TLDR","**Launch the Pinboard TUI client**\n\n```tinboard```"],["SYNOPSIS","**tinboard** [_options_]"],["DESCRIPTION","**tinboard** is a TUI client for the Pinboard.in bookmarking service. It allows you to manage, search, and browse your Pinboard bookmarks entirely from the terminal."],["HISTORY","**tinboard** was created by **Dave Pearson** (davep) and is written in **Python** using the Textual framework."],["SEE ALSO","[buku](/man/buku)(1), [nb](/man/nb)(1), [surfraw](/man/surfraw)(1)"]],"e":["buku","nb","surfraw"],"x":"tinboard"},"ts-node":{"n":"ts-node","t":"TypeScript execution environment for Node.js","s":[["TAGLINE","TypeScript execution environment for Node.js"],["TLDR","**Run TypeScript file**\n\n```ts-node [script.ts]```\n\n**Start REPL**\n\n```ts-node```\n\n**Run with specific config**\n\n```ts-node -P [tsconfig.json] [script.ts]```\n\n**Transpile only** (skip type checking)\n\n```ts-node --transpile-only [script.ts]```\n\n**Run as ESM**\n\n```ts-node --esm [script.ts]```\n\n**Evaluate code**\n\n```ts-node -e \"console.log('Hello')\"```\n\n**Transpile with SWC** (fastest mode)\n\n```ts-node --swc [script.ts]```\n\n**Print evaluated expression**\n\n```ts-node -p \"1 + 1\"```"],["SYNOPSIS","**ts-node** [_-P config_] [_--transpile-only_] [_--swc_] [_--esm_] [_options_] [_file_] [_args_]"],["PARAMETERS","**-P** _FILE_\n> Path to tsconfig.json.\n\n**--transpile-only**, **-T**\n> Skip type checking.\n\n**--esm**\n> Use ESM loader.\n\n**-e** _CODE_\n> Evaluate code.\n\n**-p** _CODE_\n> Evaluate and print.\n\n**-r** _MODULE_\n> Require module.\n\n**--pretty**\n> Pretty-print errors.\n\n**--skip-project**\n> Skip loading tsconfig.json.\n\n**--swc**\n> Transpile with SWC for faster startup. Implies --transpileOnly.\n\n**--files**\n> Load files, include, and exclude from tsconfig.json on startup.\n\n**-C** _NAME_, **--compiler** _NAME_\n> TypeScript compiler to use (default: typescript).\n\n**-D** _CODES_, **--ignoreDiagnostics** _CODES_\n> Ignore TypeScript diagnostics by code.\n\n**--emit**\n> Write compiled output files to .ts-node directory.\n\n**-i**, **--interactive**\n> Force REPL even if stdin is not a terminal.\n\n**--skipIgnore**\n> Skip ignore checks, allowing compilation of files in node_modules.\n\n**-O** _JSON_, **--compilerOptions** _JSON_\n> Merge JSON compiler options with tsconfig.\n\n**--showConfig**\n> Print resolved tsconfig.json and exit."],["DESCRIPTION","**ts-node** is a TypeScript execution engine for Node.js that compiles and runs TypeScript files on-the-fly without a separate build step. By default it performs full type checking at runtime, reporting type errors before execution begins.\n\nThe **--transpile-only** mode skips type checking for significantly faster startup, which is useful during development when your editor already provides type feedback. The **--swc** flag uses the SWC transpiler (written in Rust) for an even greater speed improvement, and implies --transpileOnly. ESM mode (**--esm**) handles ES module imports and is needed when working with import/export syntax natively. The built-in REPL provides an interactive TypeScript environment for experimentation.\n\nConfiguration is loaded from tsconfig.json by default, and a custom config can be specified with **-P**. The tool integrates well with development workflows using ts-node-dev or nodemon for automatic restart on file changes."],["CAVEATS","Slower startup than precompiled JavaScript. Type checking adds overhead. Some advanced TypeScript features need configuration. Not recommended for production."],["HISTORY","**ts-node** was created around **2015** to enable direct TypeScript execution. It became essential for TypeScript development, enabling scripts, REPL, and development servers."],["INSTALL","```pacman: sudo pacman -S ts-node```"],["SEE ALSO","[tsc](/man/tsc)(1), [node](/man/node)(1), [npx](/man/npx)(1), [tsx](/man/tsx)(1)"]],"e":["tsc","node","npx","tsx"],"x":"ts-node [script.ts]"},"ubuntu-security-status":{"n":"ubuntu-security-status","t":"Show package security support status","s":[["TAGLINE","Show package security support status"],["TLDR","Display **unsupported** package count\n\n```ubuntu-security-status```\n\nList **unavailable** packages\n\n```ubuntu-security-status --unavailable```\n\nList **third-party** packages\n\n```ubuntu-security-status --thirdparty```"],["SYNOPSIS","**ubuntu-security-status** [_OPTIONS_]"],["PARAMETERS","**--unavailable**\n> List packages no longer available for download\n\n**--thirdparty**\n> List packages from third-party sources"],["DESCRIPTION","**ubuntu-security-status** displays information about security support status for installed Ubuntu packages. It identifies packages that may no longer receive security updates.\n\nThe tool helps administrators understand their system's security posture by identifying packages outside the official support period or from unofficial sources."],["CAVEATS","Ubuntu specific. Results depend on current Ubuntu release and support status. Third-party packages may have their own security policies."],["SEE ALSO","[apt](/man/apt)(8), [pro](/man/pro)(1)"]],"e":["apt","pro"],"x":"ubuntu-security-status"},"udisksd":{"n":"udisksd","t":"Storage device management D-Bus daemon","s":[["TAGLINE","Storage device management D-Bus daemon"],["TLDR","**Daemon is started automatically by D-Bus or systemd**\n\n```udisksd```\n\n**Run with debug output**\n\n```udisksd --debug```\n\n**Run without replacing existing instance**\n\n```udisksd --no-replace```\n\n**Run without LUKS support**\n\n```udisksd --no-cryptsetup```"],["SYNOPSIS","**udisksd** [_options_]"],["PARAMETERS","**--debug**\n> Enable debug output.\n\n**--no-replace**\n> Don't replace existing daemon instance.\n\n**--no-cryptsetup**\n> Disable LUKS cryptographic volume support.\n\n**--no-sigint**\n> Don't exit on SIGINT for debugging.\n\n**--force-load-modules**\n> Load kernel modules even if not needed.\n\n**-h**, **--help**\n> Display help.\n\n**--version**\n> Display version."],["DESCRIPTION","**udisksd** is the system daemon for udisks2, providing D-Bus interfaces for managing storage devices. It handles mounting, unmounting, formatting, partitioning, and LUKS encryption operations without requiring root privileges for each action.\n\nThe daemon monitors udev events to track device changes and exposes device information through the org.freedesktop.UDisks2 D-Bus interface. Desktop environments and applications use this interface for storage management.\n\nUsers should not start udisksd manually; it's automatically activated by D-Bus or systemd when applications access its interfaces. Access control uses polkit, allowing logged-in users to manage devices on their session's seat."],["CAVEATS","Requires polkit for access control. Automatic mounting depends on desktop environment integration. Debug mode produces verbose output. Some features require specific kernel modules."],["HISTORY","**udisksd** evolved from the original udisks project (DeviceKit-disks) created for modern Linux desktop storage management. The udisks2 rewrite improved the API and added features like LUKS support. It's now maintained as part of the storaged project and is a core component of most Linux desktop environments."],["SEE ALSO","[udisksctl](/man/udisksctl)(1), [polkit](/man/polkit)(8), [mount](/man/mount)(8), [udevadm](/man/udevadm)(8)"]],"e":["udisksctl","polkit","mount","udevadm"],"x":"udisksd"},"uv":{"n":"uv","t":"Extremely fast Python package and project manager","s":[["TAGLINE","Extremely fast Python package and project manager"],["TLDR","**Create a new Python project**\n\n```uv init [project_name]```\n\n**Add a dependency**\n\n```uv add [package]```\n\n**Remove a dependency**\n\n```uv remove [package]```\n\n**Sync dependencies** from pyproject.toml\n\n```uv sync```\n\n**Run a command** in the project environment\n\n```uv run [command]```\n\n**Install a Python version**\n\n```uv python install [3.12]```\n\n**Create a virtual environment**\n\n```uv venv```\n\n**Install a tool globally**\n\n```uv tool install [ruff]```"],["SYNOPSIS","**uv** _command_ [_options_] [_arguments_]"],["PARAMETERS","**init** [_name_]\n> Create a new Python project\n\n**add** _package_\n> Add a dependency to pyproject.toml\n\n**remove** _package_\n> Remove a dependency\n\n**sync**\n> Sync dependencies with lock file\n\n**lock**\n> Generate/update uv.lock file\n\n**run** _command_\n> Run command in project environment\n\n**python install** _version_\n> Install a Python version\n\n**python list**\n> List installed Python versions\n\n**venv** [_path_]\n> Create a virtual environment\n\n**pip install** _package_\n> Install package (pip-compatible interface)\n\n**tool install** _tool_\n> Install a CLI tool globally\n\n**tool run** _tool_\n> Run a tool without installing\n\n**--help**, **-h**\n> Show help\n\n**--version**, **-V**\n> Show version"],["DESCRIPTION","**uv** is an extremely fast Python package and project manager written in Rust. It replaces pip, pip-tools, pipx, poetry, pyenv, and virtualenv with a single unified tool.\n\nProjects are managed through **pyproject.toml** with dependencies locked in **uv.lock**. The **uv sync** command installs exact versions from the lock file, ensuring reproducible environments.\n\nUV manages Python versions directly—no need for pyenv. Use **uv python install** to download and manage multiple Python versions.\n\nThe tool provides a pip-compatible interface (**uv pip**) for drop-in replacement in existing workflows, while offering project-based workflows for new projects."],["CAVEATS","UV is relatively new (2024) and rapidly evolving. Some edge cases may differ from pip behavior.\n\nLock files should be committed to version control for reproducibility. Run **uv lock** after modifying dependencies.\n\nFor CI/CD, use **uv sync --frozen** to fail if lock file is outdated rather than regenerating it."],["HISTORY","UV was created by **Astral** (makers of Ruff) and released in **2024**. It achieved 10-100x faster performance than pip by leveraging Rust and parallel downloads. The tool quickly gained adoption as a modern Python toolchain replacement."],["INSTALL","```dnf: sudo dnf install uv```\n\n```pacman: sudo pacman -S uv```\n\n```apk: sudo apk add uv```\n\n```brew: brew install uv```\n\n```nix: nix profile install nixpkgs#uv```"],["SEE ALSO","[pip](/man/pip)(1), [poetry](/man/poetry)(1), [pipx](/man/pipx)(1), [python](/man/python)(1)"]],"e":["pip","poetry","pipx","python"],"x":"uv init [project_name]"},"xinput":{"n":"xinput","t":"Manage X11 input devices","s":[["TAGLINE","Manage X11 input devices"],["TLDR","**List** all input devices\n\n```xinput list```\n\n**Disable** an input device\n\n```xinput disable id```\n\n**Enable** an input device\n\n```xinput enable id```\n\n**List properties** of a device\n\n```xinput list-props id```\n\n**Set property** value for device\n\n```xinput set-prop id property value```\n\n**Detach** device from its master\n\n```xinput float id```\n\n**Reattach** device to a master\n\n```xinput reattach id master_id```\n\n**Test** input events from device\n\n```xinput test id```"],["SYNOPSIS","**xinput** [_COMMAND_] [_OPTIONS_] [_DEVICE_]"],["DESCRIPTION","**xinput** is a utility to list available input devices, query information about a device, and change input device settings in the X Window System. It provides control over keyboard, mouse, touchpad, and other input devices."],["PARAMETERS","**list**\n> List all input devices with their IDs\n\n**enable ID**\n> Enable the input device\n\n**disable ID**\n> Disable the input device\n\n**list-props ID**\n> List configurable properties of a device\n\n**set-prop ID PROPERTY VALUE**\n> Set property value (supports 8, 16, or 32-bit formats)\n\n**watch-props ID**\n> Monitor property changes in real-time\n\n**query-state ID**\n> Query the current state of a device\n\n**set-mode ID MODE**\n> Set device mode to ABSOLUTE or RELATIVE\n\n**set-button-map ID MAP**\n> Remap physical buttons to logical buttons\n\n**map-to-output ID OUTPUT**\n> Restrict device to specific display output\n\n**float ID**\n> Detach a slave device from its master\n\n**reattach ID MASTER_ID**\n> Reattach a slave device to a different master\n\n**create-master NAME**\n> Create a new master device pair\n\n**remove-master ID**\n> Remove a master device and its paired device\n\n**test ID**\n> Register for extended events and display data"],["CAVEATS","On Xwayland, xinput only affects X11 clients connected to that Xwayland instance, not Wayland-native applications. Configuration changes may not persist across sessions; use system configuration files for permanent changes."],["HISTORY","**xinput** is part of the X.Org input utilities, providing command-line management of XInput extension devices."],["INSTALL","```apt: sudo apt install xinput```\n\n```dnf: sudo dnf install xinput```\n\n```apk: sudo apk add xinput```\n\n```zypper: sudo zypper install xinput```\n\n```brew: brew install xinput```\n\n```nix: nix profile install nixpkgs#xinput```"],["SEE ALSO","[xrandr](/man/xrandr)(1), [xset](/man/xset)(1), [xmodmap](/man/xmodmap)(1), [setxkbmap](/man/setxkbmap)(1)"]],"e":["xrandr","xset","xmodmap","setxkbmap"],"x":"xinput list"},"xxd":{"n":"xxd","t":"Hexadecimal dump and reverse tool","s":[["TAGLINE","Hexadecimal dump and reverse tool"],["TLDR","**Create a hex dump** of a file\n\n```xxd [file]```\n\n**Create a plain hex dump** (no line numbers or ASCII)\n\n```xxd -p [file]```\n\n**Create a binary dump** instead of hex\n\n```xxd -b [file]```\n\n**Dump only first N bytes**\n\n```xxd -l [16] [file]```\n\n**Start at a specific offset**\n\n```xxd -s [0x100] [file]```\n\n**Output as C include** array\n\n```xxd -i [file]```\n\n**Convert hex dump back to binary**\n\n```xxd -r [hexdump.txt] [output.bin]```\n\n**Convert plain hex** to binary\n\n```echo \"[48656c6c6f]\" | xxd -r -p```"],["SYNOPSIS","**xxd** [_options_] [_infile_ [_outfile_]]"],["PARAMETERS","**-b**\n> Binary digit dump instead of hexadecimal\n\n**-c** _cols_\n> Format output with specified columns per line (default: 16)\n\n**-g** _bytes_\n> Number of bytes per group (default: 2)\n\n**-i**\n> Output as C include file (array definition)\n\n**-l** _len_\n> Stop after outputting len bytes\n\n**-p**, **-ps**, **-plain**\n> Plain hexdump style (continuous hex without addresses)\n\n**-r**, **-revert**\n> Reverse operation: convert hexdump to binary\n\n**-s** _seek_\n> Start at specified offset in file\n\n**-u**\n> Use uppercase hexadecimal letters\n\n**-e**\n> Little-endian dump (group bytes are reversed)"],["DESCRIPTION","**xxd** creates a hexadecimal dump of a file or standard input. The output shows the offset, hexadecimal values, and ASCII representation of the data. It can also reverse a hex dump back to the original binary.\n\nThe default output format shows 16 bytes per line with the file offset, hex values in groups of 2 bytes, and printable ASCII characters. The **-p** option produces a continuous hex string without formatting.\n\nThe **-i** option generates C source code with the data as an unsigned char array, useful for embedding binary data in programs.\n\nThe **-r** option reverses the process, converting a hex dump back to binary. Combined with **-p**, it can convert plain hex strings to binary data."],["VIM INTEGRATION","xxd integrates with vim for hex editing. In vim, use **:%!xxd** to convert buffer to hex dump, edit the hex values, then **:%!xxd -r** to convert back to binary."],["CAVEATS","When using **-r**, xxd is tolerant of missing or out-of-order lines if the output is seekable. Non-hex characters in the hex column are ignored during revert. The **-r** option requires properly formatted input matching xxd's output format (or plain hex with **-p**)."],["HISTORY","**xxd** was written by **Juergen Weigert** and is distributed with **vim**. It was created as a more versatile alternative to traditional hex dump utilities like **od**. The tool has been part of the vim distribution since the mid-1990s and is commonly available on Unix-like systems."],["INSTALL","```apt: sudo apt install xxd```\n\n```dnf: sudo dnf install xxd```\n\n```apk: sudo apk add xxd```\n\n```zypper: sudo zypper install xxd```\n\n```nix: nix profile install nixpkgs#xxd```"],["SEE ALSO","[od](/man/od)(1), [hexdump](/man/hexdump)(1), [hd](/man/hd)(1), [vim](/man/vim)(1)"]],"e":["od","hexdump","hd","vim"],"x":"xxd [file]"},"zapier-init":{"n":"zapier-init","t":"Initialize new Zapier integration project","s":[["TAGLINE","Initialize new Zapier integration project"],["TLDR","**Initialize new integration**\n\n```zapier init [path]```\n\n**Initialize with template**\n\n```zapier init [path] --template [minimal]```"],["SYNOPSIS","**zapier** **init** [_options_] _path_"],["PARAMETERS","**--template** _name_\n> Use specific template."],["DESCRIPTION","**zapier init** creates a new Zapier integration project. Scaffolds project structure with boilerplate code. Starting point for developing Zapier integrations with CLI."],["SEE ALSO","[zapier](/man/zapier)(1), [zapier-scaffold](/man/zapier-scaffold)(1)"]],"e":["zapier","zapier-scaffold"],"x":"zapier init [path]"}});
