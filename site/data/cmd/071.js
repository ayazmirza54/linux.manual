window.LCL_BUCKET(71,{"adb-start-server":{"n":"adb-start-server","t":"Start the Android Debug Bridge background server","s":[["TAGLINE","Start the Android Debug Bridge background server"],["TLDR","**Start** the adb server\n\n```adb start-server```\n\nStart server with **specific port**\n\n```ANDROID_ADB_SERVER_PORT=[5038] adb start-server```"],["SYNOPSIS","**adb start-server**"],["DESCRIPTION","**adb start-server** explicitly starts the adb server daemon if it's not already running. The server is a background process that manages communication between adb clients and connected Android devices.\n\nNormally, the server starts automatically when you run any adb command. This command is useful when you want to ensure the server is running before connecting devices or in scripts where you want explicit control over server startup."],["CAVEATS","The server typically starts on port 5037. Only one adb server can run at a time on a given port. If another instance is running, this command has no effect. The server may fail to start if the port is in use by another process."],["HISTORY","The adb server architecture has been part of the Android SDK since its initial release in **2008**. The client-server model allows multiple adb clients to share device connections efficiently."],["INSTALL","```apt: sudo apt install adb```\n\n```dnf: sudo dnf install android-tools```\n\n```pacman: sudo pacman -S android-tools```\n\n```apk: sudo apk add android-tools-adb```\n\n```zypper: sudo zypper install android-tools```\n\n```nix: nix profile install nixpkgs#android-tools```"],["SEE ALSO","[adb](/man/adb)(1), [adb-kill-server](/man/adb-kill-server)(1), [adb-devices](/man/adb-devices)(1), [adb-connect](/man/adb-connect)(1)"],["RESOURCES","```[Source code](https://android.googlesource.com/platform/packages/modules/adb/)```\n\n```[Documentation](https://developer.android.com/tools/adb)```"]],"e":["adb","adb-kill-server","adb-devices","adb-connect"],"x":"adb start-server"},"ansible-vault":{"n":"ansible-vault","t":"Encrypt and decrypt sensitive data","s":[["TAGLINE","Encrypt and decrypt sensitive data"],["TLDR","**Create** a new encrypted file\n\n```ansible-vault create [secrets.yml]```\n\n**Encrypt** an existing file\n\n```ansible-vault encrypt [file.yml]```\n\n**Decrypt** a file\n\n```ansible-vault decrypt [secrets.yml]```\n\n**Edit** an encrypted file\n\n```ansible-vault edit [secrets.yml]```\n\n**View** encrypted file contents\n\n```ansible-vault view [secrets.yml]```\n\n**Encrypt** a string\n\n```ansible-vault encrypt_string '[secret_value]' --name '[variable_name]'```\n\n**Rekey** (change password)\n\n```ansible-vault rekey [secrets.yml]```"],["SYNOPSIS","**ansible-vault** _command_ [_--vault-password-file file_] [_options_] _files_"],["DESCRIPTION","**ansible-vault** encrypts sensitive data like passwords, keys, and credentials within Ansible projects. It uses AES256 encryption to protect files while allowing them to be used directly in playbooks.\n\nEncrypted files can be edited, viewed, and used in playbooks without manual decryption; ansible-playbook handles decryption automatically when provided with the vault password."],["PARAMETERS","**create** _file_\n> Create new encrypted file\n\n**encrypt** _files_\n> Encrypt existing files\n\n**decrypt** _files_\n> Decrypt files\n\n**edit** _file_\n> Edit encrypted file in place\n\n**view** _file_\n> View encrypted file contents\n\n**encrypt_string** _string_\n> Encrypt a string for embedding in a playbook or vars file\n\n**rekey** _files_\n> Re-encrypt files with a new password\n\n**--vault-password-file** _file_\n> File containing the vault password\n\n**--vault-id** _label@source_\n> Vault identity to use (label plus password source)\n\n**-J**, **--ask-vault-pass**\n> Prompt for the vault password\n\n**-n** _name_, **--name** _name_\n> Variable name to assign (encrypt_string only)\n\n**--encrypt-vault-id** _id_\n> Vault id to use when encrypting (when several are provided)\n\n**--output** _file_\n> Write output to the named file instead of stdout\n\n**--new-vault-password-file** _file_\n> Password file holding the new password for rekey\n\n**--new-vault-id** _id_\n> Vault id holding the new password for rekey"],["CONFIGURATION","**/etc/ansible/ansible.cfg**\n> System-wide Ansible configuration, including vault password file path and vault identity settings.\n\n**~/.ansible.cfg**\n> Per-user Ansible configuration overriding system defaults.\n\n**ansible.cfg**\n> Project-level configuration in the current directory, highest priority."],["CAVEATS","Vault password must be available during playbook runs. Encrypted files should be committed to version control, not the password. Use vault-id for multiple passwords."],["HISTORY","**ansible-vault** was introduced in Ansible 1.5 (**2014**) to address the need for secure handling of sensitive data in automation. Multiple vault passwords support was added in Ansible 2.4."],["INSTALL","```dnf: sudo dnf install ansible-core```\n\n```pacman: sudo pacman -S ansible-core```\n\n```apk: sudo apk add ansible-core```\n\n```zypper: sudo zypper install ansible-core```"],["SEE ALSO","[ansible-playbook](/man/ansible-playbook)(1), [ansible](/man/ansible)(1), [gpg](/man/gpg)(1)"],["RESOURCES","```[Source code](https://github.com/ansible/ansible)```\n\n```[Documentation](https://docs.ansible.com/ansible/latest/cli/ansible-vault.html)```"]],"e":["ansible-playbook","ansible","gpg"],"x":"ansible-vault create [secrets.yml]"},"ansiweather":{"n":"ansiweather","t":"Display weather in the terminal with ANSI colors","s":[["TAGLINE","Display weather in the terminal with ANSI colors"],["TLDR","**Get** weather for a city\n\n```ansiweather -l [London,UK]```\n\nShow **forecast**\n\n```ansiweather -l [Paris,FR] -f [5]```\n\nUse **Celsius**\n\n```ansiweather -l [Tokyo,JP] -u metric```\n\nUse **Fahrenheit**\n\n```ansiweather -l [New York,US] -u imperial```\n\nShow **wind and humidity** data\n\n```ansiweather -l [Berlin,DE] -w true -h true```\n\nShow **five-day** forecast\n\n```ansiweather -l [Berlin,DE] -F```\n\nDisplay **UV index** information\n\n```ansiweather -l [Berlin,DE] -i true```"],["SYNOPSIS","**ansiweather** [_-l location_] [_-u units_] [_-f days_] [_options_]"],["DESCRIPTION","**ansiweather** is a shell script that displays current weather conditions in your terminal using ANSI colors and Unicode symbols. It fetches data from OpenWeatherMap API and formats it for terminal display.\n\nThe tool provides a quick, colorful weather overview without leaving the command line."],["PARAMETERS","**-l** _location_\n> Location (City,CountryCode format)\n\n**-u** _units_\n> Units: metric (Celsius) or imperial (Fahrenheit)\n\n**-f** _days_\n> Forecast days (1-5)\n\n**-F**\n> Toggle forecast mode for the next five days.\n\n**-a** _bool_\n> Toggle ANSI colors display (true/false).\n\n**-s** _bool_\n> Toggle Unicode symbols display (true/false).\n\n**-k** _key_\n> Specify OpenWeatherMap API key.\n\n**-d** _bool_\n> Toggle daylight data display: sunrise/sunset (true/false).\n\n**-w** _bool_\n> Toggle wind data display (true/false).\n\n**-p** _bool_\n> Toggle pressure data display (true/false).\n\n**-h** _bool_\n> Toggle humidity data display (true/false).\n\n**-i** _bool_\n> Toggle UV index display (true/false).\n\n**-v**\n> Display version information."],["CONFIGURATION","**~/.ansiweatherrc**\n> User configuration file for default location, units, API key, and display options."],["CAVEATS","Requires OpenWeatherMap API key for some features. Free API has rate limits. Location matching can be imprecise; use country codes for accuracy."],["HISTORY","**ansiweather** was created as a simple, colorful terminal weather tool, leveraging OpenWeatherMap's free API tier for weather data."],["INSTALL","```aur: yay -S ansiweather```\n\n```brew: brew install ansiweather```"],["SEE ALSO","[curl](/man/curl)(1), [wttr.in](/man/wttr.in)(1)"]],"e":["curl","wttr.in"],"x":"ansiweather -l [London,UK]"},"apparmor":{"n":"apparmor","t":"Mandatory Access Control framework for confining applications.","s":[["TAGLINE","Mandatory Access Control framework for confining applications."],["TLDR","**Display AppArmor status** and loaded profiles\n\n```sudo aa-status```\n\n**Set a profile** to enforce mode\n\n```sudo aa-enforce [/etc/apparmor.d/usr.bin.firefox]```\n\n**Set a profile** to complain mode (log only)\n\n```sudo aa-complain [/etc/apparmor.d/usr.bin.firefox]```\n\n**Disable a profile**\n\n```sudo aa-disable [/etc/apparmor.d/usr.bin.firefox]```\n\n**Reload all AppArmor profiles**\n\n```sudo systemctl reload apparmor```\n\n**Generate a new profile** interactively\n\n```sudo aa-genprof [/usr/bin/application]```\n\n**Update profiles** based on log events\n\n```sudo aa-logprof```"],["SYNOPSIS","**aa-status** [_options_]\n\n**aa-enforce** _profile_\n\n**aa-complain** _profile_\n\n**aa-disable** _profile_\n\n**aa-genprof** _executable_\n\n**aa-logprof**"],["PARAMETERS","**aa-status**\n> Display the current state of AppArmor, including loaded profiles and their modes\n\n**aa-enforce** _profile_\n> Set a profile to enforce mode (blocks policy violations)\n\n**aa-complain** _profile_\n> Set a profile to complain mode (logs violations without blocking)\n\n**aa-disable** _profile_\n> Disable a profile entirely\n\n**aa-genprof** _executable_\n> Generate a new profile for an application interactively\n\n**aa-logprof**\n> Update profiles based on events logged in complain mode\n\n**aa-unconfined**\n> List processes running without AppArmor confinement\n\n**--verbose**\n> Display detailed information (aa-status)\n\n**--enforced**\n> Show only enforced profile count (aa-status)\n\n**--complaining**\n> Show only complaining profile count (aa-status)"],["DESCRIPTION","**AppArmor** (Application Armor) is a Linux Security Module that provides Mandatory Access Control (MAC) for programs. It confines applications using per-program security profiles that restrict file access, network capabilities, and other system resources.\n\nProfiles operate in two modes: **enforce** mode blocks and logs violations, while **complain** mode only logs without blocking, useful for developing and testing new profiles.\n\nThe **aa-status** command displays loaded profiles and their modes. Use **aa-enforce** and **aa-complain** to switch profile modes. The **aa-genprof** tool generates new profiles by running an application and learning its behavior, while **aa-logprof** refines existing profiles based on logged events.\n\nProfiles are stored in **/etc/apparmor.d/** and loaded at boot. The AppArmor service manages profile loading via systemd on modern distributions."],["EXIT CODES","**aa-status** returns: **0** = AppArmor enabled with policy loaded, **1** = AppArmor not enabled, **2** = AppArmor enabled but no policy loaded, **3** = AppArmor control files not available, **4** = insufficient privileges."],["CONFIGURATION","**/etc/apparmor.d/**\n> Directory containing AppArmor security profiles, loaded at boot by the apparmor service.\n\n**/etc/apparmor.d/tunables/**\n> Shared variable definitions included by profiles.\n\n**/etc/apparmor.d/abstractions/**\n> Reusable rule fragments that profiles can include."],["CAVEATS","Most AppArmor commands require root privileges. Profiles in enforce mode can break application functionality if the policy is too restrictive. Test new profiles in complain mode first. The **apparmor-utils** package must be installed for profile management tools."],["HISTORY","AppArmor was developed by **Immunix Inc.** in the late **1990s** and acquired by **Novell** in **2005**. It became part of the mainline Linux kernel in version **2.6.36** (2010). Ubuntu has included AppArmor by default since version **7.10** (2007), and it is also available on Debian, SUSE, and other distributions as an alternative to SELinux."],["INSTALL","```pacman: sudo pacman -S apparmor```"],["SEE ALSO","[aa-status](/man/aa-status)(8), [aa-enforce](/man/aa-enforce)(8), [aa-complain](/man/aa-complain)(8), [aa-notify](/man/aa-notify)(8)"]],"e":["aa-status","aa-enforce","aa-complain","aa-notify"],"x":"sudo aa-status"},"apptainer-overlay":{"n":"apptainer-overlay","t":"Create and manage writable overlay filesystems for containers.","s":[["TAGLINE","Create and manage writable overlay filesystems for containers."],["TLDR","**Add a writable overlay** to an existing SIF image\n\n```apptainer overlay create -s [size] [path/to/image.sif]```\n\nCreate a **standalone EXT3** writable overlay image\n\n```apptainer overlay create -s [size] [path/to/overlay.img]```\n\nCreate a **sparse overlay** image\n\n```apptainer overlay create -s [size] -S [path/to/overlay.img]```\n\nCreate an overlay for use with **fakeroot**\n\n```apptainer overlay create -f -s [size] [path/to/overlay.img]```\n\nCreate an overlay with a **specific directory** in the layout\n\n```apptainer overlay create --create-dir [path/to/directory] [path/to/overlay.img]```"],["SYNOPSIS","**apptainer overlay create** [_options_] _image_"],["DESCRIPTION","**apptainer overlay** manages EXT3 writable overlay filesystems for Apptainer containers. Overlays allow persistent writes on top of immutable SIF container images without modifying the original image.\n\nOverlays can be embedded directly into a SIF file or created as standalone image files that are attached at runtime. This enables persistent storage of modifications, logs, and user data while maintaining container immutability."],["PARAMETERS","**-s, --size** _size_\n> Size of the EXT3 writable overlay in MiB (default: 64).\n\n**-S, --sparse**\n> Create a sparse file instead of allocating full disk space.\n\n**-f, --fakeroot**\n> Make the overlay layout usable by actions run with --fakeroot.\n\n**--create-dir** _path_\n> Directory to create as part of the overlay layout (can be repeated)."],["CAVEATS","Overlays require filesystem support for loop devices and EXT3. Sparse overlays save disk space but may cause performance issues with some workloads. Overlay size cannot be easily changed after creation."],["INSTALL","```dnf: sudo dnf install apptainer```\n\n```pacman: sudo pacman -S apptainer```\n\n```apk: sudo apk add apptainer```\n\n```zypper: sudo zypper install apptainer```\n\n```brew: brew install apptainer```\n\n```nix: nix profile install nixpkgs#apptainer```"],["SEE ALSO","[apptainer](/man/apptainer)(1), [apptainer-build](/man/apptainer-build)(1), [apptainer-run](/man/apptainer-run)(1), [apptainer-shell](/man/apptainer-shell)(1), [apptainer-exec](/man/apptainer-exec)(1)"],["RESOURCES","```[Source code](https://github.com/apptainer/apptainer)```\n\n```[Homepage](https://apptainer.org)```\n\n```[Documentation](https://apptainer.org/docs/)```"]],"e":["apptainer","apptainer-build","apptainer-run","apptainer-shell","apptainer-exec"],"x":"apptainer overlay create -s [size] [path/to/image.sif]"},"avrdude":{"n":"avrdude","t":"Program AVR microcontroller flash and fuses","s":[["TAGLINE","Program AVR microcontroller flash and fuses"],["TLDR","**Upload** firmware to Arduino\n\n```avrdude -p [atmega328p] -c [arduino] -P [/dev/ttyUSB0] -U flash:w:[firmware.hex]```\n\n**Read** flash memory\n\n```avrdude -p [atmega328p] -c [usbasp] -U flash:r:[backup.hex]:i```\n\nSet **fuse bits**\n\n```avrdude -p [atmega328p] -c [usbasp] -U lfuse:w:[0xE2]:m```\n\n**Verify** without writing\n\n```avrdude -p [atmega328p] -c [arduino] -P [/dev/ttyUSB0] -U flash:v:[firmware.hex]```"],["SYNOPSIS","**avrdude** -p _partno_ -c _programmer_ [_options_]"],["DESCRIPTION","**avrdude** (AVR Downloader Uploader) is a utility for programming Atmel AVR microcontrollers. It supports numerous programmers and can read/write flash, EEPROM, fuse bits, and lock bits.\n\nThe tool is essential for Arduino development and embedded systems work with AVR chips."],["PARAMETERS","**-p** _partno_\n> Specify AVR device (e.g., atmega328p, attiny85)\n\n**-c** _programmer_\n> Programmer type (arduino, usbasp, avrisp2)\n\n**-P** _port_\n> Communication port (/dev/ttyUSB0, COM3)\n\n**-U** _memtype:op:file:format_\n> Memory operation (flash:w:file.hex:i)\n\n**-b** _baudrate_\n> Override RS-232 baud rate for serial connections\n\n**-B** _bitclock_\n> JTAG/ISP bit clock period (microseconds or frequency)\n\n**-e**\n> Perform a chip erase, resetting flash and EEPROM to 0xFF\n\n**-D**\n> Disable the automatic chip erase before flash write\n\n**-V**\n> Skip automatic verification after writing memory\n\n**-v**\n> Verbose output (repeat for more detail)\n\n**-n**\n> Dry run, do not actually write to the device\n\n**-F**\n> Force operation, override signature and init checks\n\n**-t**\n> Enter interactive terminal mode for manual memory operations"],["MEMORY OPERATIONS","**flash**\n> Program memory\n\n**eeprom**\n> Data memory\n\n**lfuse**, **hfuse**, **efuse**\n> Configuration fuses\n\n**lock**\n> Lock bits\n\nOperations: **r** (read), **w** (write), **v** (verify)"],["CONFIGURATION","**/etc/avrdude.conf**\n> System-wide configuration defining programmer types, part definitions, and default settings.\n\n**~/.avrduderc**\n> Per-user configuration overrides. Can add custom programmer definitions or modify part settings."],["CAVEATS","Incorrect fuse settings can brick the device. Requires proper permissions for serial ports. Wrong programmer selection causes failures. Always backup fuses before modification. USB connection issues common on some platforms."],["HISTORY","**avrdude** was created by Brian Dean in **2003** as an open-source alternative to Atmel's proprietary programming tools. It has become the standard tool for AVR programming."],["INSTALL","```apt: sudo apt install avrdude```\n\n```dnf: sudo dnf install avrdude```\n\n```pacman: sudo pacman -S avrdude```\n\n```apk: sudo apk add avrdude```\n\n```zypper: sudo zypper install avrdude```\n\n```brew: brew install avrdude```\n\n```nix: nix profile install nixpkgs#avrdude```"],["SEE ALSO","[arduino-cli](/man/arduino-cli)(1), [avr-gcc](/man/avr-gcc)(1), [platformio](/man/platformio)(1), [minicom](/man/minicom)(1)"],["RESOURCES","```[Source code](https://github.com/avrdudes/avrdude)```\n\n```[Documentation](https://avrdudes.github.io/avrdude/)```"]],"e":["arduino-cli","avr-gcc","platformio","minicom"],"x":"avrdude -p [atmega328p] -c [arduino] -P [/dev/ttyUSB0] -U flash:w:[firmware.hex]"},"aws-ses":{"n":"aws-ses","t":"Send and manage email through AWS","s":[["TAGLINE","Send and manage email through AWS"],["TLDR","**Send an email** with subject and body\n\n```aws ses send-email --from [sender@example.com] --to [recipient@example.com] --subject \"[Subject]\" --text \"[Body text]\"```\n\n**Verify an email identity**\n\n```aws ses verify-email-identity --email-address [email@example.com]```\n\n**List verified identities**\n\n```aws ses list-identities```\n\n**Get sending quota** and limits\n\n```aws ses get-send-quota```\n\n**Get sending statistics**\n\n```aws ses get-send-statistics```\n\n**Create an email template**\n\n```aws ses create-template --template file://[template.json]```\n\n**Send a templated email**\n\n```aws ses send-templated-email --source [sender@example.com] --destination ToAddresses=[recipient@example.com] --template [template-name] --template-data '{\"name\":\"value\"}'```"],["SYNOPSIS","**aws ses** _subcommand_ [_options_]"],["DESCRIPTION","**aws ses** is the AWS CLI interface for Amazon Simple Email Service (SES), a cloud-based email sending service for transactional email, marketing messages, and high-volume email communications.\n\nSES provides email sending capabilities with features like deliverability tracking, bounce and complaint handling, email templates, and configuration sets for managing sending behavior."],["SUBCOMMANDS","**Email Sending**\n> send-email, send-raw-email, send-templated-email, send-bulk-templated-email, send-bounce, send-custom-verification-email\n\n**Identity Verification**\n> verify-email-identity, verify-domain-identity, verify-domain-dkim, delete-identity, list-identities, get-identity-verification-attributes\n\n**Templates**\n> create-template, get-template, update-template, delete-template, list-templates, test-render-template\n\n**Configuration Sets**\n> create-configuration-set, describe-configuration-set, delete-configuration-set, list-configuration-sets\n\n**Receipt Rules**\n> create-receipt-rule, describe-receipt-rule, update-receipt-rule, delete-receipt-rule, list-receipt-rule-sets\n\n**Statistics**\n> get-send-quota, get-send-statistics, get-account-sending-enabled"],["CAVEATS","New SES accounts start in sandbox mode, which limits sending to verified email addresses only. Request production access to send to any address. Email identities (addresses or domains) must be verified before sending. SES charges per 1,000 emails sent plus data transfer fees."],["HISTORY","Amazon SES launched in **January 2011** as AWS's email sending service, providing developers with a cost-effective way to send email at scale. The service has evolved to include receiving capabilities, templates, and advanced deliverability features."],["INSTALL","```dnf: sudo dnf install aws```\n\n```pacman: sudo pacman -S aws-cli```\n\n```apk: sudo apk add aws-cli```\n\n```zypper: sudo zypper install aws-cli```"],["SEE ALSO","[aws](/man/aws)(1), [sendmail](/man/sendmail)(1), [mail](/man/mail)(1)"],["RESOURCES","```[Source code](https://github.com/aws/aws-cli)```\n\n```[Homepage](https://aws.amazon.com/cli/)```\n\n```[Documentation](https://docs.aws.amazon.com/cli/latest/reference/ses/index.html)```"]],"e":["aws","sendmail","mail"],"x":"aws ses send-email --from [sender@example.com] --to [recipient@example.com] --subject \"[Subject]\" --text \"[Body text]\""},"bitwise":{"n":"bitwise","t":"Interactive multi-base calculator with bit manipulation","s":[["TAGLINE","Interactive multi-base calculator with bit manipulation"],["TLDR","**Interactive** mode\n\n```bitwise```\n\nConvert **decimal**\n\n```bitwise 12345```\n\nConvert **hexadecimal**\n\n```bitwise 0x563d```\n\n**Calculate** expression\n\n```bitwise \"0x123 + 0x20 - 30\"```"],["SYNOPSIS","**bitwise** [_OPTIONS_] [_value_|_expression_]"],["DESCRIPTION","**bitwise** is a multi-base interactive calculator supporting dynamic base conversion and bit manipulation. It displays values in decimal, hexadecimal, octal, and binary simultaneously."],["PARAMETERS","**-i, --interactive**\n> Force interactive mode (the default when no value or expression is given)\n\n**-w, --width** _b8|b16|b32|b64_\n> Set the bit width of the calculation\n\n**--no-color**\n> Disable colored output\n\n**-v, --version**\n> Print version information\n\n**-h, --help**\n> Show help message\n\nValues can be specified in different bases:\n> Decimal: plain numbers (123)\n> Hexadecimal: 0x prefix (0x7B)\n> Octal: 0 prefix (0173)\n> Binary: 0b prefix (0b1111011)\n\nSupports C-style arithmetic and bitwise operators (NOT, OR, AND, XOR, and shifts). In command-line mode the result is printed in all bases at once."],["CAVEATS","Interactive mode provides a visual bit toggle interface. Expressions with special shell characters need quoting. Useful for low-level programming and debugging."],["HISTORY","**bitwise** is an open-source (GPL-3.0) ncurses-based calculator written by **Ramon Fried** (mellowcandle). It targets low-level developers such as kernel and device-driver programmers who frequently work across number bases."],["INSTALL","```apt: sudo apt install bitwise```\n\n```dnf: sudo dnf install bitwise```\n\n```apk: sudo apk add bitwise```\n\n```zypper: sudo zypper install bitwise```\n\n```brew: brew install bitwise```\n\n```nix: nix profile install nixpkgs#bitwise```"],["SEE ALSO","[bc](/man/bc)(1), [calc](/man/calc)(1)"],["RESOURCES","```[Source code](https://github.com/mellowcandle/bitwise)```"]],"e":["bc","calc"],"x":"bitwise"},"borgbackup":{"n":"borgbackup","t":"Deduplicating backup with compression and encryption","s":[["TAGLINE","Deduplicating backup with compression and encryption"],["TLDR","**Initialize a new backup repository**\n\n```borg init --encryption=repokey [/path/to/repo]```\n\n**Create a backup**\n\n```borg create [/path/to/repo]::[archive_name] [/path/to/source]```\n\n**Create backup with compression and statistics**\n\n```borg create --stats --compression lz4 [/path/to/repo]::[archive_name] [/path/to/source]```\n\n**List archives in a repository**\n\n```borg list [/path/to/repo]```\n\n**List contents of an archive**\n\n```borg list [/path/to/repo]::[archive_name]```\n\n**Extract an archive**\n\n```borg extract [/path/to/repo]::[archive_name]```\n\n**Show repository info** including storage usage and encryption details\n\n```borg info [/path/to/repo]```\n\n**Delete old archives** keeping the last 7 daily and 4 weekly\n\n```borg prune --keep-daily=7 --keep-weekly=4 [/path/to/repo]```\n\n**Mount archive as filesystem**\n\n```borg mount [/path/to/repo]::[archive_name] [/mnt/point]```"],["SYNOPSIS","**borg** _command_ [_options_] [_repository_]"],["PARAMETERS","**init**\n> Initialize a new backup repository.\n\n**create**\n> Create a new backup archive.\n\n**list**\n> List repository contents or archive files.\n\n**extract**\n> Extract files from an archive.\n\n**delete**\n> Delete archives.\n\n**prune**\n> Delete archives according to retention policy.\n\n**mount** / **umount**\n> Mount/unmount archive as FUSE filesystem.\n\n**info**\n> Show archive or repository information.\n\n**check**\n> Verify repository consistency.\n\n**--encryption** _mode_\n> Encryption mode: none, repokey, keyfile.\n\n**--compression** _algo_\n> Compression: none, lz4, zstd, zlib, lzma.\n\n**--exclude** _pattern_\n> Exclude files matching pattern.\n\n**--progress**\n> Show progress during operations.\n\n**-v**, **--verbose**\n> Increase output verbosity.\n\n**--stats**\n> Print statistics about the created archive (file count, size, dedup ratio).\n\n**--list**\n> Output a list of files as they are processed during create or extract.\n\n**--dry-run**\n> Perform a trial run with no changes made (supported by create, prune, delete).\n\n**--remote-path** _path_\n> Set the path to the borg executable on the remote host."],["DESCRIPTION","**Borg** (BorgBackup) is a deduplicating backup program with compression and encryption. It efficiently stores multiple backups by identifying and reusing duplicate data blocks, dramatically reducing storage requirements for incremental backups.\n\nKey features include **client-side encryption** ensuring only you can read your data, **efficient deduplication** that stores each unique data chunk only once across all archives, and **compression** to further reduce storage needs.\n\nBorg can backup to local directories, remote hosts via SSH, or cloud storage through rclone. Archives can be mounted via FUSE for easy browsing and selective restoration. Retention policies automatically prune old archives."],["CAVEATS","The repository format is Borg-specific; archives cannot be read with other tools. The encryption key must be backed up separately - losing it means losing access to backups. Remote backups require SSH access and Borg installed on both sides. Deduplication happens across all archives in a repository, not across repositories."],["HISTORY","BorgBackup was forked from **Attic** in **2015** after Attic development stalled. The name \"Borg\" references Star Trek's Borg, known for assimilating and efficiently organizing. Development accelerated after the fork, adding features like authenticated encryption, improved compression, and macOS/Windows support. Borg has become one of the most popular open-source backup solutions."],["INSTALL","```apt: sudo apt install borgbackup```\n\n```dnf: sudo dnf install borgbackup```\n\n```zypper: sudo zypper install borgbackup```\n\n```brew: brew install borgbackup```\n\n```nix: nix profile install nixpkgs#borgbackup```"],["SEE ALSO","[restic](/man/restic)(1), [duplicity](/man/duplicity)(1), [rsync](/man/rsync)(1), [rclone](/man/rclone)(1), [rdiff-backup](/man/rdiff-backup)(1), [tar](/man/tar)(1)"]],"e":["restic","duplicity","rsync","rclone","rdiff-backup","tar"],"x":"borg init --encryption=repokey [/path/to/repo]"},"bun-publish":{"n":"bun-publish","t":"Publish packages to the npm registry","s":[["TAGLINE","Publish packages to the npm registry"],["TLDR","**Publish a package** to npm registry\n\n```bun publish```\n\n**Publish with a specific tag**\n\n```bun publish --tag [beta]```\n\n**Publish with public access** (scoped packages)\n\n```bun publish --access public```\n\n**Dry run** without actually publishing\n\n```bun publish --dry-run```"],["SYNOPSIS","**bun** **publish** [_options_]"],["PARAMETERS","**--tag** _tag_\n> Publish with a specific dist-tag (default: latest).\n\n**--access** _public|restricted_\n> Set access level for scoped packages.\n\n**--dry-run**\n> Show what would be published without actually publishing.\n\n**--otp** _code_\n> One-time password for 2FA."],["DESCRIPTION","**bun publish** publishes the current package to the npm registry. It reads package configuration from package.json and uploads the package tarball.\n\nBefore publishing, ensure you're logged in with **bun login** or have appropriate credentials configured."],["CAVEATS","Requires authentication to the npm registry. Scoped packages (@org/name) are private by default; use **--access public** for public scoped packages."],["SEE ALSO","[bun](/man/bun)(1), [npm-publish](/man/npm-publish)(1)"],["RESOURCES","```[Source code](https://github.com/oven-sh/bun)```\n\n```[Homepage](https://bun.sh)```\n\n```[Documentation](https://bun.sh/docs)```"]],"e":["bun","npm-publish"],"x":"bun publish"},"bun-sqlgen":{"n":"bun-sqlgen","t":"Type-safe SQL code generator for Bun","s":[["TAGLINE","Type-safe SQL code generator for Bun"],["TLDR","**Add** bun-sqlgen to a project\n\n```bun add @ilbertt/bun-sqlgen```\n\n**Generate** type definitions for tagged SQL queries found in a set of files\n\n```bun bun-sqlgen generate '[src/**/*.ts]' --migrations [db/migrations]```\n\n**Point** at the database used to validate queries via an environment variable\n\n```DATABASE_URL=[postgres://localhost/app] bun bun-sqlgen generate '[src/**/*.ts]' --migrations [db/migrations]```"],["SYNOPSIS","**bun bun-sqlgen generate** _GLOB_ **--migrations** _DIR_"],["DESCRIPTION","**bun-sqlgen** is a code generation tool for **Bun**'s native SQL client (**Bun.sql**, the **bun:sql** module). It lets you write raw SQL with full type safety and without an ORM.\n\nThe **generate** subcommand scans the TypeScript source files matching the supplied glob pattern, extracts the tagged SQL template literals it finds, and validates each query against a real **PostgreSQL** or **SQLite** database whose schema is defined by your migration files. It then writes a **src/queries.gen.d.ts** file containing precise parameter and result types for every query, so that mistakes in column names, types, or query shape become compile-time errors.\n\nBecause checking happens against a live database rather than a parsed model, the generated types stay accurate to the actual schema. No Docker or separate type-definition language is required."],["PARAMETERS","**generate** _GLOB_\n\n> Process the source files matching the quoted glob pattern (for example **'src/\\*\\*/\\*.ts'**), emitting type definitions for the SQL queries they contain. Quote the glob so the shell does not expand it.\n\n**--migrations** _DIR_\n\n> Directory holding the SQL migration files that act as the schema source of truth. The migrations are applied to derive the schema the queries are checked against."],["CONFIGURATION","**DATABASE_URL**\n\n> Connection string for the PostgreSQL or SQLite database used to validate queries during generation. Code generation runs against this real database instance."],["CAVEATS","Requires the **Bun** runtime and a reachable PostgreSQL or SQLite database at generation time; it is not a standalone binary. It targets only **Bun.sql**, so it is not useful with other database clients or runtimes."],["HISTORY","Created by **ilbertt** and first released in **2026** as part of the growing Bun ecosystem, offering a no-ORM alternative to schema-aware query builders. It is distributed on npm as **@ilbertt/bun-sqlgen** under the Unlicense."],["SEE ALSO","[bun](/man/bun)(1), [psql](/man/psql)(1), [sqlite3](/man/sqlite3)(1), [protoc](/man/protoc)(1)"],["RESOURCES","```[Source code](https://github.com/ilbertt/bun-sqlgen)```"]],"e":["bun","psql","sqlite3","protoc"],"x":"bun add @ilbertt/bun-sqlgen"},"bun-unlink":{"n":"bun-unlink","t":"Remove symbolic package links created by bun link","s":[["TAGLINE","Remove symbolic package links created by bun link"],["TLDR","**Unregister the current package** from the global link registry\n\n```bun unlink```\n\n**Unlink a specific package** from the current project\n\n```bun unlink [package-name]```"],["SYNOPSIS","**bun** **unlink** [_package-name_]"],["DESCRIPTION","**bun unlink** removes symbolic links created by **bun link**. When run without arguments in a package directory, it unregisters the package from the global link registry. When run with a package name, it removes the symlink from the current project's node_modules.\n\nThis is the reverse of the `bun link` workflow used during local package development."],["INSTALL","```pacman: sudo pacman -S bun```\n\n```brew: brew install bun```\n\n```nix: nix profile install nixpkgs#bun```"],["SEE ALSO","[bun](/man/bun)(1), [bun-link](/man/bun-link)(1), [bun-install](/man/bun-install)(1)"]],"e":["bun","bun-link","bun-install"],"x":"bun unlink"},"busctl":{"n":"busctl","t":"Introspect and monitor the D-Bus message bus","s":[["TAGLINE","Introspect and monitor the D-Bus message bus"],["TLDR","Show all **peers on the bus**\n\n```busctl list```\n\nShow all peers on the **user session bus**\n\n```busctl --user list```\n\nShow **process information** for a service or PID\n\n```busctl status [service|pid]```\n\n**Monitor** messages on the bus\n\n```busctl monitor [service1] [service2] ...```\n\n**Capture** bus traffic in pcap format\n\n```busctl capture [service] > [bus-traffic.pcap]```\n\nShow **object tree** of services\n\n```busctl tree [service1] [service2] ...```\n\n**Introspect** an object's interfaces\n\n```busctl introspect [service] [path/to/object]```\n\n**Get property** value from an object\n\n```busctl get-property [service] [path/to/object] [interface_name] [property_name]```\n\n**Call a method** on an object\n\n```busctl call [service] [path/to/object] [interface_name] [method_name]```"],["SYNOPSIS","**busctl** [_command_] [_options_] [_arguments_]"],["DESCRIPTION","**busctl** introspects and monitors the D-Bus message bus. D-Bus is an inter-process communication system used extensively in Linux for communication between system services and desktop applications.\n\nThe tool can list services, monitor message traffic, inspect object hierarchies, and invoke methods on D-Bus objects."],["SUBCOMMANDS","**list**\n> List all peers on the bus\n\n**status**\n> Show process info for a service or PID\n\n**monitor**\n> Watch message traffic\n\n**capture**\n> Capture bus traffic in pcap format\n\n**tree**\n> Show object hierarchy\n\n**introspect**\n> Examine object interfaces\n\n**get-property**\n> Read a property value\n\n**set-property**\n> Set a property value\n\n**call**\n> Invoke a method\n\n**emit**\n> Emit a signal\n\n**wait-signal**\n> Wait for a specific signal"],["PARAMETERS","**--system**\n> Connect to system bus (default).\n\n**--user**\n> Connect to user session bus.\n\n**--address** _ADDRESS_\n> Connect to the bus specified by ADDRESS.\n\n**-j**, **--json**=_MODE_\n> Output in JSON format. MODE is short, pretty, or off.\n\n**--verbose**\n> Show verbose output for call or get-property commands.\n\n**--expect-reply**=_BOOL_\n> Whether to wait for method call completion. Defaults to yes.\n\n**--auto-start**=_BOOL_\n> Whether method calls should auto-start the target service. Defaults to yes.\n\n**--list**\n> Show a flat list instead of a tree with the tree command.\n\n**--unique**\n> Show only unique bus names (e.g., :1.42).\n\n**--acquired**\n> Show only well-known bus names.\n\n**--activatable**\n> Show only activatable services not yet started.\n\n**--no-pager**\n> Disable pager.\n\n**--no-legend**\n> Do not print column headers and footers."],["INSTALL","```apt: sudo apt install elogind```\n\n```apk: sudo apk add busctl```\n\n```nix: nix profile install nixpkgs#elogind```"],["SEE ALSO","[dbus-send](/man/dbus-send)(1), [dbus-monitor](/man/dbus-monitor)(1), [gdbus](/man/gdbus)(1), [systemctl](/man/systemctl)(1)"]],"e":["dbus-send","dbus-monitor","gdbus","systemctl"],"x":"busctl list"},"cbatticon":{"n":"cbatticon","t":"lightweight battery indicator for the system tray","s":[["TAGLINE","lightweight battery indicator for the system tray"],["TLDR","Show the **battery icon** in the system tray\n\n```cbatticon```\n\nSet the **update interval** in seconds (default: 5)\n\n```cbatticon -u [10]```\n\nList available **icon types**\n\n```cbatticon -t```\n\nShow with a **specific icon type**\n\n```cbatticon -i [standard|notification|symbolic]```\n\nList available **power supplies**\n\n```cbatticon -p```\n\nShow icon for a **specific battery**\n\n```cbatticon [BAT0]```\n\nSet **critical level** command\n\n```cbatticon -r [5] -c [poweroff]```"],["SYNOPSIS","**cbatticon** [_options_] [_battery_]"],["DESCRIPTION","**cbatticon** is a lightweight battery indicator for the system tray. It displays battery status with configurable icons and can execute commands when battery reaches critical levels.\n\nThe tool is designed for minimal window managers and desktop environments that don't include battery monitoring."],["PARAMETERS","**-u**, **--update-interval** _seconds_\n> Update interval in seconds (default: 5).\n\n**-i**, **--icon-type** _type_\n> Icon style: standard, notification, or symbolic.\n\n**-t**, **--list-icon-types**\n> List available icon types.\n\n**-p**, **--list-power-supplies**\n> List available power supplies.\n\n**-r**, **--critical-level** _percent_\n> Set critical battery level percentage (default: 5%).\n\n**-c**, **--command-critical-level** _cmd_\n> Command to execute when critical level is reached.\n\n**-o**, **--command-low-level** _cmd_\n> Command to execute when low level is reached.\n\n**-l**, **--low-level** _percent_\n> Set low battery level percentage.\n\n**-x**, **--command-left-click** _cmd_\n> Command to execute when left clicking on the tray icon.\n\n**-d**, **--debug**\n> Display debug information."],["CAVEATS","Requires a system tray to display. May not work with Wayland compositors that lack tray support. Critical level commands run with user privileges."],["INSTALL","```apt: sudo apt install cbatticon```\n\n```pacman: sudo pacman -S cbatticon```\n\n```apk: sudo apk add cbatticon```\n\n```nix: nix profile install nixpkgs#cbatticon```"],["SEE ALSO","[upower](/man/upower)(1), [acpi](/man/acpi)(1)"]],"e":["upower","acpi"],"x":"cbatticon"},"cmd-wrapped":{"n":"cmd-wrapped","t":"shell history year-in-review summary generator","s":[["TAGLINE","shell history year-in-review summary generator"],["TLDR","**Generate your command-line wrapped summary** (current stats)\n\n```cmd-wrapped```\n\n**Show statistics for a specific year**\n\n```cmd-wrapped [2024]```\n\n**Analyze a specific shell's history**\n\n```cmd-wrapped -s [zsh|bash|fish|nu|atuin]```"],["SYNOPSIS","**cmd-wrapped** [**-s** _shell_] [_year_]"],["PARAMETERS","**-s** _shell_\n> Specify which shell history to analyze: zsh, bash, fish, nu (nushell), or atuin.\n\n_year_\n> Positional argument selecting a specific year (e.g., 2024). Without it, current statistics are shown."],["DESCRIPTION","**cmd-wrapped** is a command-line tool that generates a \"Spotify Wrapped\" style summary of your terminal command history. It analyzes your shell history to produce statistics about your most-used commands, busiest hours, and command patterns over time.\n\nThe tool reads the history of the detected (or specified) shell and presents colorful, formatted output showing top commands, total commands executed, unique commands used, and temporal usage patterns.\n\ncmd-wrapped supports Zsh, Bash, Fish, Nushell, and Atuin. Pass a year as a positional argument to scope the report to that year, or `-s` to choose which shell's history to analyze."],["CAVEATS","Accuracy depends on your shell's history configuration. Commands may be undercounted if history is limited in size, duplicates are ignored, or certain commands are excluded from history. Fish shell uses a different history format that requires specific parsing."],["HISTORY","cmd-wrapped was created as a fun end-of-year tool inspired by **Spotify Wrapped**, the popular annual music listening summary. It emerged from the developer community's interest in personal analytics and the trend of \"wrapped\" summaries across various platforms."],["INSTALL","```nix: nix profile install nixpkgs#cmd-wrapped```"],["SEE ALSO","[history](/man/history)(1), [bash](/man/bash)(1), [zsh](/man/zsh)(1), [fish](/man/fish)(1)"],["RESOURCES","```[Source code](https://github.com/YiNNx/cmd-wrapped)```"]],"e":["history","bash","zsh","fish"],"x":"cmd-wrapped"},"composite.im6":{"n":"composite.im6","t":"combine images using ImageMagick 6","s":[["TAGLINE","combine images using ImageMagick 6"],["TLDR","**Overlay one image** on top of another\n\n```composite.im6 [overlay.png] [background.png] [output.png]```\n\n**Place overlay at a specific position**\n\n```composite.im6 -geometry +[100]+[50] [overlay.png] [background.png] [output.png]```\n\n**Blend images** with transparency\n\n```composite.im6 -blend [50]% [image1.png] [image2.png] [output.png]```\n\n**Apply multiply** blend mode\n\n```composite.im6 -compose multiply [overlay.png] [background.png] [output.png]```\n\n**Dissolve overlay** into background\n\n```composite.im6 -dissolve [75]% [overlay.png] [background.png] [output.png]```\n\n**Center the overlay** on background\n\n```composite.im6 -gravity center [overlay.png] [background.png] [output.png]```"],["SYNOPSIS","**composite.im6** [_options_] _overlay_ _background_ [_mask_] _output_"],["PARAMETERS","**-compose** _METHOD_\n> Compositing operator: over, multiply, screen, overlay, darken, lighten, etc.\n\n**-geometry** _GEOMETRY_\n> Position offset for overlay (+X+Y format).\n\n**-gravity** _TYPE_\n> Anchor point: center, north, south, east, west, northeast, etc.\n\n**-blend** _GEOMETRY_\n> Blend percentages for source and destination.\n\n**-dissolve** _PERCENT_\n> Dissolve overlay into background at specified percentage.\n\n**-watermark** _BRIGHTNESS_\n> Apply overlay as watermark with given brightness.\n\n**-tile**\n> Tile the overlay image across the background.\n\n**-quality** _VALUE_\n> Output compression quality (0-100).\n\n**-stegano** _OFFSET_\n> Hide watermark within image using steganography.\n\n**-stereo** _+X+Y_\n> Create a stereo anaglyph from two images.\n\n**-alpha** _TYPE_\n> Activate, deactivate, or reset the alpha channel."],["DESCRIPTION","**composite.im6** is the ImageMagick version 6 command for combining images through compositing operations. It is functionally identical to **composite** but explicitly references the ImageMagick 6 installation, useful on systems where both ImageMagick 6 and 7 are installed.\n\nThe tool overlays one image on another using various blending modes and positioning options. It supports mask-based compositing, watermarking, and dozens of composition operators matching professional image editing capabilities.\n\nOn most systems, this command exists as a versioned alternative to ensure compatibility with scripts written for ImageMagick 6 when ImageMagick 7 is also present. The **im6** suffix indicates the legacy API version."],["CAVEATS","ImageMagick 6 is in maintenance mode; new features are added to ImageMagick 7. Some systems may only have one version installed. Scripts using **composite.im6** explicitly will fail on systems with only ImageMagick 7 installed."],["HISTORY","ImageMagick was created by John Cristy in **1990**. Version 6 was a long-standing stable release. When ImageMagick 7 introduced breaking changes in **2016**, versioned command names were added to allow both versions to coexist, with **.im6** suffix denoting version 6 commands."],["SEE ALSO","[composite](/man/composite)(1), [convert](/man/convert)(1), [mogrify.im6](/man/mogrify.im6)(1), [display.im6](/man/display.im6)(1), [magick](/man/magick)(1)"]],"e":["composite","convert","mogrify.im6","display.im6","magick"],"x":"composite.im6 [overlay.png] [background.png] [output.png]"},"conjure.im6":{"n":"conjure.im6","t":"ImageMagick MSL script interpreter","s":[["TAGLINE","ImageMagick MSL script interpreter"],["TLDR","**Execute a Magick Scripting Language** (MSL) script\n\n```conjure.im6 [script.msl]```\n\n**Execute with verbose output**\n\n```conjure.im6 -verbose [script.msl]```\n\n**Execute a script with custom defines**\n\n```conjure.im6 -define [key]=[value] [script.msl]```\n\n**Run in debug mode**\n\n```conjure.im6 -debug all [script.msl]```\n\n**Execute with specific resource limits**\n\n```conjure.im6 -limit memory [256MB] [script.msl]```"],["SYNOPSIS","**conjure.im6** [_options_] _script.msl_"],["PARAMETERS","**-debug** _EVENTS_\n> Enable debugging output for specified event types.\n\n**-define** _KEY=VALUE_\n> Set a configuration value accessible within the script.\n\n**-limit** _TYPE VALUE_\n> Set resource limits (memory, map, disk, file, thread, time).\n\n**-log** _FORMAT_\n> Log output format specification.\n\n**-monitor**\n> Monitor progress of image processing operations.\n\n**-quiet**\n> Suppress all warning messages.\n\n**-regard-warnings**\n> Treat warnings as errors.\n\n**-seed** _VALUE_\n> Seed a pseudo-random number generator.\n\n**-verbose**\n> Enable verbose output during execution.\n\n**-version**\n> Display version information."],["DESCRIPTION","**conjure.im6** is the ImageMagick version 6 interpreter for Magick Scripting Language (MSL). MSL is an XML-based scripting language for performing complex image processing operations programmatically.\n\nMSL scripts define image manipulation workflows using XML tags that correspond to ImageMagick operations. This allows creating reusable, parameterized image processing pipelines without writing shell scripts or using programming language bindings.\n\nMSL is particularly useful for batch processing and creating complex image manipulation templates that can be applied consistently across many images. It is primarily aimed at users who want custom image processing without needing a programming language or compiler."],["CAVEATS","MSL is a specialized language with limited adoption compared to using ImageMagick from shell scripts or programming languages. ImageMagick 6 is in maintenance mode; ImageMagick 7 uses different scripting approaches. The **.im6** suffix ensures version 6 is used on systems with both versions installed."],["HISTORY","conjure was part of ImageMagick's scripting capabilities, allowing XML-based batch image processing. ImageMagick was created by John Cristy in **1990**. The versioned **conjure.im6** command was introduced when ImageMagick 7 brought breaking changes in **2016**, allowing both versions to coexist."],["SEE ALSO","[composite.im6](/man/composite.im6)(1), [display.im6](/man/display.im6)(1), [mogrify.im6](/man/mogrify.im6)(1), [magick](/man/magick)(1), [convert](/man/convert)(1)"]],"e":["composite.im6","display.im6","mogrify.im6","magick","convert"],"x":"conjure.im6 [script.msl]"},"cpanm":{"n":"cpanm","t":"Zero-configuration CPAN installer","s":[["TAGLINE","Zero-configuration CPAN installer"],["TLDR","**Install a Perl module**\n\n```cpanm [Module::Name]```\n\n**Install module locally** (without root)\n\n```cpanm -l ~/perl5 [Module::Name]```\n\n**Install from cpanfile**\n\n```cpanm --installdeps .```\n\n**Install specific version**\n\n```cpanm [Module::Name]@[1.23]```\n\n**Install from Git repository**\n\n```cpanm [git://github.com/user/repo.git]```\n\n**Uninstall a module**\n\n```cpanm -U [Module::Name]```\n\n**Show what would be installed**\n\n```cpanm --info [Module::Name]```\n\n**Install quietly**\n\n```cpanm -q [Module::Name]```"],["SYNOPSIS","**cpanm** [_options_] _Module_..."],["PARAMETERS","**-l**, **--local-lib** _path_\n> Install modules to local directory.\n\n**-L**, **--local-lib-contained** _path_\n> Install to directory with contained dependencies.\n\n**-n**, **--notest**\n> Skip running tests.\n\n**-q**, **--quiet**\n> Quiet output.\n\n**-v**, **--verbose**\n> Verbose output.\n\n**-f**, **--force**\n> Force install even if tests fail.\n\n**-U**, **--uninstall**\n> Uninstall module.\n\n**--installdeps**\n> Install dependencies only.\n\n**--info**\n> Show module information without installing.\n\n**--look**\n> Download and unpack, then open shell.\n\n**--mirror** _url_\n> CPAN mirror URL.\n\n**--sudo**\n> Use sudo for installation."],["DESCRIPTION","**cpanm** (cpanminus) is a script to install Perl modules from CPAN (Comprehensive Perl Archive Network). It provides a simpler, faster alternative to the traditional CPAN shell with minimal dependencies and configuration.\n\nThe tool automatically resolves and installs dependencies, downloads modules from CPAN mirrors, runs tests, and installs to the appropriate location. It supports local::lib for user-local installations without root privileges.\n\ncpanm can install modules by name, from tarballs, from Git repositories, or from URLs. The **cpanfile** format allows declaring project dependencies that cpanm can install with **--installdeps**."],["CAVEATS","Installing globally requires root privileges unless using local::lib. Some modules have system dependencies (libraries, headers) that must be installed separately. Test failures may indicate incompatible system configurations. The --notest flag should be used cautiously as it may install broken modules."],["HISTORY","cpanminus was created by **Tatsuhiko Miyagawa** in **2010** as a zero-configuration CPAN installer. Frustrated with the complexity of CPAN.pm and CPANPLUS, he designed cpanm to \"just work\" with sensible defaults. It became the de facto standard for installing Perl modules, valued for its simplicity and speed."],["INSTALL","```apk: sudo apk add perl-app-cpanminus```\n\n```brew: brew install cpanm```"],["SEE ALSO","[cpan](/man/cpan)(1), [carton](/man/carton)(1), [perl](/man/perl)(1)"]],"e":["cpan","carton","perl"],"x":"cpanm [Module::Name]"},"crane-blob":{"n":"crane-blob","t":"retrieve blobs from container images","s":[["TAGLINE","retrieve blobs from container images"],["TLDR","**Get a blob** from an image\n\n```crane blob [image]@[digest]```\n\n**Save blob to file**\n\n```crane blob [image]@[sha256:abc...] > [blob.tar.gz]```"],["SYNOPSIS","**crane** **blob** [_options_] _image@digest_"],["DESCRIPTION","**crane blob** retrieves a specific blob from a container image by its digest. Blobs are typically layers or configuration objects.\n\nOutput is written to stdout; redirect to save to a file."],["INSTALL","```pacman: sudo pacman -S crane```\n\n```apk: sudo apk add crane```\n\n```zypper: sudo zypper install crane```\n\n```brew: brew install crane```\n\n```nix: nix profile install nixpkgs#crane```"],["SEE ALSO","[crane](/man/crane)(1), [crane-manifest](/man/crane-manifest)(1)"],["RESOURCES","```[Source code](https://github.com/google/go-containerregistry)```\n\n```[Documentation](https://github.com/google/go-containerregistry/blob/main/cmd/crane/doc/crane_blob.md)```"]],"e":["crane","crane-manifest"],"x":"crane blob [image]@[digest]"},"cryptsetup-luksformat":{"n":"cryptsetup-luksformat","t":"initialize LUKS encrypted partition","s":[["TAGLINE","initialize LUKS encrypted partition"],["TLDR","Initialize LUKS volume with **passphrase**\n\n```cryptsetup luksFormat [/dev/sdXY]```\n\nInitialize LUKS volume with **keyfile**\n\n```cryptsetup luksFormat [/dev/sdXY] [path/to/keyfile]```\n\nInitialize with passphrase and **set label**\n\n```cryptsetup luksFormat --label [label] [/dev/sdXY]```"],["SYNOPSIS","**cryptsetup luksFormat** [_options_] _device_ [_keyfile_]"],["DESCRIPTION","**cryptsetup luksFormat** initializes a LUKS (Linux Unified Key Setup) encrypted partition. It creates the LUKS header and sets up the initial key slot with either a passphrase or keyfile.\n\nLUKS provides standardized on-disk encryption with multiple key slots, secure key management, and compatibility across distributions."],["PARAMETERS","**--type** _type_\n> LUKS type: luks1 or luks2. Defaults to luks2.\n\n**-c**, **--cipher** _cipher_\n> Encryption cipher specification. Compiled-in default (typically aes-xts-plain64).\n\n**-s**, **--key-size** _bits_\n> Key size in bits, must be a multiple of 8. For XTS the volume key is split between two ciphers, so 512 yields AES-256.\n\n**-h**, **--hash** _hash_\n> Hash algorithm used in the LUKS key setup and volume key digest.\n\n**-i**, **--iter-time** _ms_\n> Milliseconds to spend on PBKDF passphrase processing. Higher values increase brute-force resistance.\n\n**--pbkdf** _algo_\n> PBKDF algorithm for LUKS2: pbkdf2, argon2i, or argon2id (the default).\n\n**-d**, **--key-file** _file_\n> Read the passphrase from a file. Use - to read from standard input.\n\n**-l**, **--keyfile-size** _bytes_\n> Read at most this many bytes from the key file.\n\n**-y**, **--verify-passphrase**\n> Ask for the passphrase twice and fail if the entries differ.\n\n**-q**, **--batch-mode**\n> Suppress all confirmation questions. Useful for scripting.\n\n**--use-urandom**, **--use-random**\n> Select the kernel RNG (/dev/urandom or /dev/random) used to generate the volume key.\n\n**--sector-size** _bytes_\n> Encryption sector size for LUKS2: a power of two between 512 and 4096.\n\n**--label** _label_\n> Set the LUKS2 device label.\n\n**--integrity** _algo_\n> Enable authenticated disk encryption with the given integrity algorithm (LUKS2 only)."],["CAVEATS","Destroys all data on the partition. Cannot be undone. Always backup data first. LUKS1 is more compatible; LUKS2 supports newer features. Requires root privileges."],["INSTALL","```apt: sudo apt install cryptsetup-bin```\n\n```dnf: sudo dnf install cryptsetup```\n\n```pacman: sudo pacman -S cryptsetup```\n\n```apk: sudo apk add cryptsetup```\n\n```zypper: sudo zypper install cryptsetup```\n\n```nix: nix profile install nixpkgs#cryptsetup```"],["SEE ALSO","[cryptsetup](/man/cryptsetup)(8), [cryptsetup-open](/man/cryptsetup-open)(8), [lsblk](/man/lsblk)(8)"]],"e":["cryptsetup","cryptsetup-open","lsblk"],"x":"cryptsetup luksFormat [/dev/sdXY]"},"dbus-send":{"n":"dbus-send","t":"command-line D-Bus method invocation and signaling","s":[["TAGLINE","command-line D-Bus method invocation and signaling"],["TLDR","**Send a method call** to a service\n\n```dbus-send --session --dest=[org.freedesktop.Notifications] [/org/freedesktop/Notifications] [org.freedesktop.Notifications.GetCapabilities]```\n\n**Send notification** via D-Bus\n\n```dbus-send --session --dest=org.freedesktop.Notifications /org/freedesktop/Notifications org.freedesktop.Notifications.Notify string:\"app\" uint32:0 string:\"\" string:\"[Title]\" string:\"[Body]\" array:string:\"\" dict:string:string:\"\" int32:5000```\n\n**List available services** on session bus\n\n```dbus-send --session --dest=org.freedesktop.DBus --print-reply /org/freedesktop/DBus org.freedesktop.DBus.ListNames```\n\n**Call system bus** method\n\n```dbus-send --system --print-reply --dest=[org.freedesktop.hostname1] [/org/freedesktop/hostname1] [org.freedesktop.DBus.Properties.GetAll] string:\"[org.freedesktop.hostname1]\"```\n\n**Get property value**\n\n```dbus-send --session --print-reply --dest=[destination] [object_path] org.freedesktop.DBus.Properties.Get string:\"[interface]\" string:\"[property]\"```"],["SYNOPSIS","**dbus-send** [_options_] **--dest=**_NAME_ _object-path_ _message_ [_args_...]"],["PARAMETERS","**--session**\n> Send to session bus.\n\n**--system**\n> Send to system bus.\n\n**--dest** _NAME_\n> Destination service name.\n\n**--print-reply**\n> Block for a reply and print the result in human-readable form.\n\n**--print-reply=literal**\n> Print reply with string values unescaped and unquoted.\n\n**--type** _TYPE_\n> Message type: method_call (default) or signal.\n\n**--reply-timeout=**_MSEC_\n> Timeout in milliseconds to wait for a reply (default: 25000).\n\n_OBJECT-PATH_\n> D-Bus object path (e.g., /org/freedesktop/DBus).\n\n_MESSAGE_\n> Method or signal name including interface (e.g., org.freedesktop.DBus.ListNames).\n\n**string:**, **int32:**, **uint32:**, **boolean:**, **double:**, **byte:**, **objpath:**\n> Typed arguments appended to the message.\n\n**--help**\n> Display help information."],["DESCRIPTION","**dbus-send** sends a message to a D-Bus message bus, either the system-wide bus or a session bus. It can invoke methods on D-Bus objects or emit signals, and optionally wait for and print replies.\n\nThe command is useful for scripting interactions with D-Bus services, debugging D-Bus interfaces, and testing service implementations. Arguments are type-annotated (string:, int32:, boolean:, etc.) to construct properly typed D-Bus messages.\n\nCommon use cases include sending desktop notifications, querying system properties, and controlling media players or other desktop services that expose D-Bus interfaces."],["CAVEATS","Complex method calls with nested types can be difficult to construct. System bus methods often require elevated privileges. Missing --print-reply means responses are ignored. Type annotations must match service expectations exactly."],["HISTORY","dbus-send is part of the **D-Bus** reference implementation, developed by **Red Hat** and freedesktop.org starting in **2003**. It provides command-line access to the D-Bus IPC system used extensively in Linux desktop environments."],["INSTALL","```apt: sudo apt install dbus-bin```\n\n```dnf: sudo dnf install dbus```\n\n```pacman: sudo pacman -S dbus```\n\n```apk: sudo apk add dbus```\n\n```brew: brew install dbus```\n\n```nix: nix profile install nixpkgs#dbus```"],["SEE ALSO","[dbus-daemon](/man/dbus-daemon)(1), [dbus-launch](/man/dbus-launch)(1), [dbus-monitor](/man/dbus-monitor)(1), [busctl](/man/busctl)(1), [gdbus](/man/gdbus)(1), [qdbus](/man/qdbus)(1)"]],"e":["dbus-daemon","dbus-launch","dbus-monitor","busctl","gdbus","qdbus"],"x":"dbus-send --session --dest=[org.freedesktop.Notifications] [/org/freedesktop/Notifications] [org.freedesktop.Notifications.GetCapabilities]"},"deepseek":{"n":"deepseek","t":"AI coding assistant using DeepSeek Coder models","s":[["TAGLINE","AI coding assistant using DeepSeek Coder models"],["TLDR","**Start interactive mode**\n\n```deepseek```\n\n**Run a single prompt**\n\n```deepseek chat \"[write a Python function for binary search]\"```\n\n**Start with a specific model**\n\n```deepseek --model [deepseek-coder:6.7b]```\n\n**Set up the local environment** (Ollama)\n\n```deepseek setup```\n\n**Force the CLI to use local mode**\n\n```deepseek --local```\n\n**Display help**\n\n```deepseek --help```"],["SYNOPSIS","**deepseek** [_command_] [_options_]"],["PARAMETERS","**chat** _prompt_\n> Send a single prompt and print the response.\n\n**setup**\n> Set up the local Ollama environment and models.\n\n**--model** _model_\n> Model to use (e.g., deepseek-coder:6.7b, deepseek-coder:1.3b).\n\n**--local**\n> Force the CLI to use local Ollama models instead of the cloud API.\n\n**--help**\n> Display help information."],["DESCRIPTION","**deepseek** is a command-line AI coding assistant that leverages DeepSeek Coder models to assist with code completion, generation, refactoring, debugging, and code review across many programming languages.\n\nIt can run in two modes. The recommended local mode uses **Ollama** to run DeepSeek Coder models on your own machine, keeping code private and avoiding API costs. Cloud mode uses the DeepSeek API and requires an API key.\n\nWithout arguments it launches an interactive session; the **chat** subcommand runs a single prompt and exits, which is convenient for scripting."],["CONFIGURATION","**DEEPSEEK_API_KEY** (environment variable)\n> API key used in cloud mode.\n\n**DEEPSEEK_USE_LOCAL** (environment variable)\n> Set to true/false to select local or cloud mode."],["CAVEATS","Local mode requires Ollama and a downloaded DeepSeek Coder model (sizes range from ~1GB to ~19GB). Cloud mode requires a DeepSeek API key and may incur costs. Installed via npm as the `run-deepseek-cli` package (requires Node.js 18+); this is a community project, not an official DeepSeek release."],["SEE ALSO","[ollama](/man/ollama)(1), [claude](/man/claude)(1)"],["RESOURCES","```[Source code](https://github.com/holasoymalva/deepseek-cli)```"]],"e":["ollama","claude"],"x":"deepseek"},"gdm-binary":{"n":"gdm-binary","t":"GNOME Display Manager daemon process","s":[["TAGLINE","GNOME Display Manager daemon process"],["TLDR","**Start GNOME Display Manager**\n\n```gdm-binary```\n\n**Start with debugging**\n\n```gdm-binary --debug```"],["SYNOPSIS","**gdm-binary** [_options_]"],["PARAMETERS","**--debug**\n> Enable debug output.\n\n**--nodaemon**\n> Don't become a daemon.\n\n**--preserve-ld-vars**\n> Preserve LD_* environment variables."],["DESCRIPTION","**gdm-binary** is the actual daemon executable for the GNOME Display Manager. It handles the low-level operations of managing graphical login screens, authenticating users, and launching desktop sessions. This binary is the core GDM process that runs with elevated privileges to manage display servers and user session creation.\n\nOn modern systems, gdm-binary is typically started and managed by systemd rather than being invoked directly by users or administrators. The gdm service wrapper handles process management, while gdm-binary performs the actual display manager functions.\n\nUsers rarely need to interact with gdm-binary directly, as system management commands like systemctl or the gdm wrapper script are the preferred interfaces for controlling the display manager."],["INSTALL","```dnf: sudo dnf install gdm```\n\n```pacman: sudo pacman -S gdm```\n\n```apk: sudo apk add gdm```\n\n```zypper: sudo zypper install gdm```\n\n```nix: nix profile install nixpkgs#gdm```"],["SEE ALSO","[gdm](/man/gdm)(8), [gdm-restart](/man/gdm-restart)(1)"],["RESOURCES","```[Source code](https://gitlab.gnome.org/GNOME/gdm)```"]],"e":["gdm","gdm-restart"],"x":"gdm-binary"},"gemtopbm":{"n":"gemtopbm","t":"convert GEM IMG format to PBM","s":[["TAGLINE","convert GEM IMG format to PBM"],["TLDR","**Convert GEM IMG to PBM**\n\n```gemtopbm [input.img] > [output.pbm]```"],["SYNOPSIS","**gemtopbm** [_input_]"],["DESCRIPTION","**gemtopbm** is a format conversion utility that converts GEM IMG format images to PBM (Portable Bitmap) format. GEM IMG was the native bitmap image format used by Digital Research's GEM (Graphics Environment Manager) operating system and desktop environment, popular in the 1980s and early 1990s, particularly on Atari ST computers.\n\nThe tool is part of the Netpbm suite of image conversion utilities, enabling integration of legacy GEM images into modern workflows. By converting to PBM, a simple portable format, GEM images can then be further processed or converted to contemporary formats using other Netpbm tools.\n\nThis converter is primarily useful for digital preservation, retro computing enthusiasts, and anyone working with historical computer graphics from the GEM era."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pbmtogem](/man/pbmtogem)(1), [gemtopnm](/man/gemtopnm)(1)"],["RESOURCES","```[Homepage](https://netpbm.sourceforge.net/)```\n\n```[Documentation](https://netpbm.sourceforge.net/doc/gemtopnm.html)```"]],"e":["pbmtogem","gemtopnm"],"x":"gemtopbm [input.img] > [output.pbm]"},"getconf":{"n":"getconf","t":"POSIX system configuration variable query tool","s":[["TAGLINE","POSIX system configuration variable query tool"],["TLDR","List **all** configuration values available\n\n```getconf -a```\n\nList the configuration values for a **specific directory**\n\n```getconf -a [path/to/directory]```\n\nCheck if the system is **32-bit or 64-bit**\n\n```getconf LONG_BIT```\n\nCheck how many **processes** the current user can run at once\n\n```getconf CHILD_MAX```\n\nList every configuration value and filter with **grep**\n\n```getconf -a | grep MAX```"],["SYNOPSIS","**getconf** [_-v spec_] _system_var_\n**getconf** [_-v spec_] _path_var_ _pathname_"],["PARAMETERS","**-a**\n> Display all configuration variables and their values\n\n**-v** _SPEC_\n> Use specification _SPEC_ for configuration values\n\n**LONG_BIT**\n> Number of bits in a long integer (32 or 64)\n\n**CHILD_MAX**\n> Maximum number of processes per user\n\n**OPEN_MAX**\n> Maximum number of files a process can have open\n\n**PAGE_SIZE**\n> System memory page size in bytes\n\n**PATH_MAX**\n> Maximum length of a file path\n\n**HOST_NAME_MAX**\n> Maximum length of a hostname\n\n**ARG_MAX**\n> Maximum length of arguments to the exec functions\n\n**_NPROCESSORS_CONF**\n> Number of processors configured\n\n**_NPROCESSORS_ONLN**\n> Number of processors currently online"],["DESCRIPTION","**getconf** retrieves system configuration variables defined by POSIX and system-specific extensions. These values represent system limits and capabilities that applications can query to adapt their behavior.\n\nVariables fall into two categories: system-wide values (like CHILD_MAX, PAGE_SIZE) and pathname-specific values (like PATH_MAX, NAME_MAX) that may vary by filesystem.\n\nThis is useful for shell scripts that need to query system limits without hardcoding values, ensuring portability across different systems."],["CAVEATS","Some values may be indeterminate (shown as \"undefined\"). Pathname-specific variables can return different values for different filesystems. Not all variables are supported on all systems."],["HISTORY","getconf is part of the POSIX standard and has been available on Unix-like systems since the early 1990s. It provides a standardized way to query the same configuration values that the C functions sysconf(), pathconf(), and confstr() return."],["INSTALL","```apt: sudo apt install libc-bin```\n\n```apk: sudo apk add musl-utils```\n\n```nix: nix profile install nixpkgs#getconf```"],["SEE ALSO","[sysctl](/man/sysctl)(8), [ulimit](/man/ulimit)(1), [nproc](/man/nproc)(1)"]],"e":["sysctl","ulimit","nproc"],"x":"getconf -a"},"git-delta":{"n":"git-delta","t":"List files that differ from another branch","s":[["TAGLINE","List files that differ from another branch"],["TLDR","**List files that differ from master** (added, copied, or modified by default)\n\n```git delta```\n\n**List files that differ from a specific branch**\n\n```git delta [branch]```\n\n**Show only modified and renamed files** compared to master\n\n```git delta master MR```\n\n**Show only deleted files** compared to a branch\n\n```git delta [branch] D```"],["SYNOPSIS","**git delta** [_branch_] [_filter_]"],["PARAMETERS","_branch_\n> Branch to compare against. Defaults to **master** if not specified.\n\n_filter_\n> Diff filter characters to select file status types. Uses the same filter letters as **git diff --diff-filter**: **A** (added), **C** (copied), **D** (deleted), **M** (modified), **R** (renamed). Multiple letters can be combined (e.g., **MR**). Defaults to **ACM** (added, copied, modified)."],["DESCRIPTION","**git delta** lists files that differ from a given branch, showing only file names without content differences. By default it compares against the **master** branch and shows files that have been added, copied, or modified.\n\nThe filter parameter allows narrowing results to specific change types using git's diff-filter notation. This is useful for code review preparation, deployment planning, or quickly assessing the scope of changes on a feature branch.\n\nPart of the **git-extras** suite. Not to be confused with the separate **delta** project (dandavison/delta), which is a syntax-highlighting pager for git diff output."],["CAVEATS","Requires the **git-extras** package to be installed. Only shows file names, not content differences. The default comparison branch is **master**, not **main** -- this may need adjustment for repositories using **main** as their default branch."],["INSTALL","```dnf: sudo dnf install git-delta```\n\n```pacman: sudo pacman -S git-delta```\n\n```zypper: sudo zypper install git-delta```\n\n```brew: brew install git-delta```"],["SEE ALSO","[git-diff](/man/git-diff)(1), [git-log](/man/git-log)(1), [git-branch](/man/git-branch)(1), [git-extras](/man/git-extras)(1), [delta](/man/delta)(1)"]],"e":["git-diff","git-log","git-branch","git-extras","delta"],"x":"git delta"},"git-merge":{"n":"git-merge","t":"Join development histories together","s":[["TAGLINE","Join development histories together"],["TLDR","**Merge branch into current**\n\n```git merge [branch-name]```\n\n**Merge with commit message**\n\n```git merge [branch] -m \"[message]\"```\n\n**Merge without fast-forward (preserve topology)**\n\n```git merge --no-ff [branch]```\n\n**Only allow fast-forward, otherwise fail**\n\n```git merge --ff-only [branch]```\n\n**Squash merge (single staged changeset, no merge commit)**\n\n```git merge --squash [branch]```\n\n**Favor our or their side on conflicts**\n\n```git merge -X ours [branch]```\n\n**Abort or continue an in-progress merge**\n\n```git merge --abort``` / ```git merge --continue```"],["SYNOPSIS","**git merge** [_options_] [_commit_...]\n\n**git merge** (--continue | --abort | --quit)"],["PARAMETERS","_COMMIT_\n> Branch or commit to merge into current branch.\n\n**--no-ff**\n> Always create a merge commit, even when fast-forward is possible.\n\n**--ff-only**\n> Refuse to merge unless the current HEAD can be fast-forwarded.\n\n**--squash**\n> Produce working tree/index as if a real merge happened, but do not make a commit.\n\n**--no-commit**\n> Perform the merge but stop before creating a commit (allows inspection).\n\n**-m** _MESSAGE_\n> Set the commit message for the merge commit.\n\n**-e**, **--edit**\n> Invoke the editor to refine the auto-generated commit message.\n\n**--abort**\n> Abort the current conflict resolution and reconstruct the pre-merge state.\n\n**--continue**\n> Continue the merge after conflicts have been resolved.\n\n**--quit**\n> Forget about the current merge in progress without restoring the pre-merge state.\n\n**-s** _STRATEGY_, **--strategy=**_STRATEGY_\n> Select merge strategy: `ort` (default), `resolve`, `octopus`, `ours`, or `subtree`.\n\n**-X** _OPTION_, **--strategy-option=**_OPTION_\n> Pass a strategy-specific option (e.g., `ours`, `theirs`, `ignore-all-space`, `find-renames`).\n\n**--autostash**\n> Automatically stash and restore local changes around the merge.\n\n**--allow-unrelated-histories**\n> Merge histories that do not share a common ancestor.\n\n**--verify-signatures**\n> Require a valid GPG signature on the side branch tip.\n\n**-S**[_KEYID_], **--gpg-sign**[=_KEYID_]\n> GPG-sign the resulting merge commit.\n\n**--log**[=_N_]\n> Include one-line descriptions of the merged commits in the merge commit message.\n\n**--help**\n> Display help information."],["DESCRIPTION","**git merge** incorporates changes from the named commits (typically branch tips) into the current branch. It is also invoked internally by **git pull** to integrate fetched changes.\n\nA fast-forward merge simply advances the current branch pointer to the merged commit without creating a new commit; `--no-ff` forces a merge commit to preserve branch topology, while `--ff-only` refuses the merge unless it is a fast-forward. Squash merges (`--squash`) collapse all incoming changes into a single staged changeset without recording a merge commit.\n\nWhen textual conflicts occur, conflict markers are written into affected files; resolve them, `git add` the files, then run `git merge --continue` (or `git commit`). The default strategy is **ort** (\"Ostensibly Recursive's Twin\"), which replaced the older **recursive** strategy in Git 2.33 and handles three-way merges with rename detection."],["CAVEATS","Conflicts require manual resolution before the merge can complete. Fast-forward merges discard branch topology unless `--no-ff` is used. Using `-X ours`/`-X theirs` silently discards one side's changes on conflict. The `--strategy=ours` option is very different from `-X ours` and keeps only the current branch's tree."],["HISTORY","**git merge** has been a core **Git** command since its earliest releases. The default strategy was renamed from **recursive** to **ort** in Git 2.33 (August 2021) for correctness and performance improvements. `--autostash` was added in Git 2.27, and `--quit` in Git 2.11."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-rebase](/man/git-rebase)(1), [git-cherry-pick](/man/git-cherry-pick)(1), [git-mergetool](/man/git-mergetool)(1), [git-pull](/man/git-pull)(1), [git-branch](/man/git-branch)(1)"]],"e":["git-rebase","git-cherry-pick","git-mergetool","git-pull","git-branch"],"x":"git merge [branch-name]"},"git-remote-fd":{"n":"git-remote-fd","t":"File descriptor transport helper for Git","s":[["TAGLINE","File descriptor transport helper for Git"],["TLDR","**Clone using file descriptors** for input and output\n\n```git clone \"fd::[fd-in],[fd-out]\"```\n\n**Fetch using pre-opened** file descriptor connections\n\n```git fetch \"fd::17,18\"```\n\n**Use a single file descriptor** for both input and output\n\n```git fetch \"fd::17\"```"],["SYNOPSIS","**git-remote-fd** _url_"],["PARAMETERS","_URL_\n> fd:: URL in the format fd::in[,out].\n\n**in**\n> File descriptor number for reading from the remote. If out is omitted, this descriptor is used for both directions.\n\n**out**\n> File descriptor number for writing to the remote."],["DESCRIPTION","**git-remote-fd** is a Git remote helper program that uses pre-opened file descriptors for communication instead of establishing its own connections. When a URL in the form `fd::in[,out]` is used, Git invokes this helper to transfer data over the specified file descriptors.\n\nIf only one file descriptor is provided, it is used for both reading and writing. If two are given separated by a comma, the first is used for reading from the remote and the second for writing.\n\nThis enables integration with connection brokers, process supervisors, or custom transport layers that manage connections externally. The file descriptors must be opened before invoking Git."],["CAVEATS","This is an advanced, low-level feature. File descriptors must be pre-opened by the calling process. Not suitable for everyday Git workflows; primarily useful for embedding Git in applications that manage their own connections."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-remote](/man/git-remote)(1), [git-remote-ext](/man/git-remote-ext)(1)"]],"e":["git-remote","git-remote-ext"],"x":"git clone \"fd::[fd-in],[fd-out]\""},"glab-alias":{"n":"glab-alias","t":"Manage command aliases for GitLab CLI","s":[["TAGLINE","Manage command aliases for GitLab CLI"],["TLDR","**List aliases**\n\n```glab alias list```\n\n**Set alias**\n\n```glab alias set [name] \"[command]\"```\n\n**Delete alias**\n\n```glab alias delete [name]```\n\n**Set an alias that expands through a shell**\n\n```glab alias set [name] --shell \"[command]\"```"],["SYNOPSIS","**glab** **alias** _command_ [_options_]"],["SUBCOMMANDS","**list**\n> List aliases.\n\n**set** _name_ _expansion_\n> Create an alias for a glab command, or for an external command with **--shell**.\n\n**delete** _name_\n> Remove an alias."],["PARAMETERS","**-s**, **--shell**\n> Interpret the alias expansion as a shell command instead of a glab command, allowing pipes (`|`) and redirects (`>`). Use `$1`, `$2`, ... to reference passed arguments and `$@` for all of them."],["DESCRIPTION","**glab alias** manages command aliases for the GitLab CLI. It allows creating shortcuts for frequently used glab commands, making common workflows faster to execute.\n\nAliases are stored in the glab configuration file and can reference any valid glab command with arguments, or, with **--shell**, an arbitrary shell command."],["SEE ALSO","[glab](/man/glab)(1)"],["RESOURCES","```[Documentation](https://docs.gitlab.com/cli/alias/)```\n\n```[Source code](https://gitlab.com/gitlab-org/cli)```"]],"e":["glab"],"x":"glab alias list"},"glab-auth":{"n":"glab-auth","t":"Manage GitLab CLI authentication","s":[["TAGLINE","Manage GitLab CLI authentication"],["TLDR","**Login to GitLab**\n\n```glab auth login```\n\n**Login by piping a token** from a file\n\n```glab auth login --stdin < [token.txt]```\n\n**Login to a self-hosted instance**\n\n```glab auth login --hostname [gitlab.example.com]```\n\n**Show auth status**\n\n```glab auth status```\n\n**Set up git credential helper**\n\n```glab auth git-credential```\n\n**Logout**\n\n```glab auth logout```"],["SYNOPSIS","**glab** **auth** _command_ [_options_]"],["SUBCOMMANDS","**login**\n> Authenticate with a GitLab instance, interactively or non-interactively.\n\n**logout**\n> Log out of a GitLab instance.\n\n**status**\n> Show authentication status for known hosts.\n\n**git-credential**\n> Implement a git-credential-helper for GitLab, usually invoked by git itself.\n\n**configure-docker**\n> Configure glab as a credential helper for Docker."],["PARAMETERS","**-h**, **--hostname** _host_\n> GitLab host to authenticate with (default: gitlab.com).\n\n**-t**, **--token** _token_\n> An authentication token, provided via prompt, `--stdin`, or `$GITLAB_TOKEN`.\n\n**--stdin**\n> Read the token from standard input rather than prompting.\n\n**--api-host** _host_\n> API host, if different from the web host.\n\n**--api-protocol** _http|https_\n> Protocol to use for API requests.\n\n**--git-protocol** _http|ssh_\n> Protocol to use for git operations.\n\n**--use-keyring**\n> Store credentials in the system keyring instead of the plain-text config file."],["DESCRIPTION","**glab auth** manages GitLab CLI authentication. It handles logging in to GitLab instances with personal access tokens or OAuth, and stores credentials for use by other glab commands.\n\nFor self-hosted GitLab instances, use the `--hostname` flag to specify the server address. Credentials are stored in the glab configuration file (`~/.config/glab-cli/config.yml` by default), or in the OS keyring with `--use-keyring`."],["INSTALL","```apt: sudo apt install glab```\n\n```dnf: sudo dnf install glab```\n\n```pacman: sudo pacman -S glab```\n\n```apk: sudo apk add glab```\n\n```zypper: sudo zypper install glab```\n\n```brew: brew install glab```\n\n```nix: nix profile install nixpkgs#glab```"],["SEE ALSO","[glab](/man/glab)(1)"],["RESOURCES","```[Documentation](https://docs.gitlab.com/cli/auth/)```\n\n```[Source code](https://gitlab.com/gitlab-org/cli)```"]],"e":["glab"],"x":"glab auth login"},"gnmic-subscribe":{"n":"gnmic-subscribe","t":"subscribe to gNMI streaming telemetry (full form)","s":[["TAGLINE","subscribe to gNMI streaming telemetry (full form)"],["TLDR","**Subscribe to a path**\n\n```gnmic -a [host:port] subscribe --path [/state/port]```\n\n**Subscribe with sample interval**\n\n```gnmic -a [host:port] subscribe --path [/state/port] --sample-interval [30s]```\n\n**Subscribe with on-change mode**\n\n```gnmic -a [host:port] subscribe --path [/state/port] --stream-mode on-change```\n\n**Subscribe with heartbeat interval**\n\n```gnmic -a [host:port] subscribe --path [/path] --stream-mode on-change --heartbeat-interval [1m]```\n\n**Subscribe to multiple paths**\n\n```gnmic -a [host:port] subscribe --path [/path1] --path [/path2]```\n\n**Subscribe with authentication**\n\n```gnmic -a [host:port] -u [user] -p [pass] subscribe --path [/path]```"],["SYNOPSIS","**gnmic** **subscribe** [_options_]"],["PARAMETERS","**--path** _path_\n> gNMI path to subscribe to.\n\n**--mode** _mode_\n> Subscription mode: STREAM, ONCE, POLL.\n\n**--stream-mode** _mode_\n> Stream mode: SAMPLE, ON_CHANGE, TARGET_DEFINED.\n\n**--sample-interval** _duration_\n> Sample interval for SAMPLE mode.\n\n**--heartbeat-interval** _duration_\n> Heartbeat interval for ON_CHANGE mode.\n\n**--suppress-redundant**\n> Suppress redundant updates; only send updates when values actually change.\n\n**--updates-only**\n> Only send updates, not the initial state snapshot.\n\n**--quiet**\n> Do not output to stdout.\n\n**--prefix** _PATH_\n> Common prefix applied to all **--path** values.\n\n**--qos** _N_\n> DSCP value for packet marking. Defaults to 20; set to 0 to disable.\n\n**--output** _NAME_\n> Named output(s), from the config file, to write subscription results to (e.g. a Kafka or Prometheus output) instead of stdout.\n\n**--name** _NAME_\n> Run one or more subscriptions predefined in the configuration file, instead of specifying **--path** on the command line."],["DESCRIPTION","**gnmic subscribe** (aliased **gnmic sub**) sends a gNMI Subscribe RPC to network devices for streaming telemetry. It supports three subscription modes: STREAM (continuous updates, the default), ONCE (immediate single response), and POLL (on-demand). Stream mode supports SAMPLE (periodic, the default) and ON_CHANGE (event-driven) delivery.\n\nResults are printed to stdout by default, or routed to a configured output (file, Kafka, NATS, Prometheus, InfluxDB, etc.) via **--output**."],["INSTALL","```brew: brew install gnmic```\n\n```nix: nix profile install nixpkgs#gnmic```"],["SEE ALSO","[gnmic](/man/gnmic)(1), [gnmic-get](/man/gnmic-get)(1), [gnmic-set](/man/gnmic-set)(1)"],["RESOURCES","```[Documentation](https://gnmic.openconfig.net/cmd/subscribe/)```\n\n```[Source code](https://github.com/openconfig/gnmic)```"]],"e":["gnmic","gnmic-get","gnmic-set"],"x":"gnmic -a [host:port] subscribe --path [/state/port]"},"hunt":{"n":"hunt","t":"multiplayer terminal game where players hunt each other in a maze","s":[["TAGLINE","multiplayer terminal game where players hunt each other in a maze"],["TLDR","**Start hunt game**\n\n```hunt```\n\n**Connect to server**\n\n```hunt -h [hostname]```\n\n**Use specific name**\n\n```hunt -n [playername]```\n\n**Connect to port**\n\n```hunt -p [port]```"],["SYNOPSIS","**hunt** [_options_]"],["PARAMETERS","**-h** _HOST_\n> Server hostname.\n\n**-n** _NAME_\n> Player name.\n\n**-p** _PORT_\n> Server port.\n\n**-t** _TEAM_\n> Team number.\n\n**-c**\n> Auto-connect.\n\n**--help**\n> Display help information."],["PREVIEW","```\n ######  ######\n #    #  # @  #\n # #  ####    #\n #        # X #\n ##############\n```"],["DESCRIPTION","**hunt** is a multiplayer terminal game where players hunt each other in a maze. Players shoot and dodge in text-based combat.\n\nThe game connects to a huntd server where multiple players compete. It features walls, bombs, and maze navigation."],["CAVEATS","Requires huntd server. Part of BSD games. Terminal-based graphics."],["HISTORY","hunt is a classic **BSD** network game from the 1980s, providing multiplayer action in terminals."],["INSTALL","```apt: sudo apt install hunt```\n\n```dnf: sudo dnf install hunt```\n\n```nix: nix profile install nixpkgs#hunt```"],["SEE ALSO","[huntd](/man/huntd)(6), [nethack](/man/nethack)(6), [bsdgames](/man/bsdgames)(6)"]],"e":["huntd","nethack","bsdgames"],"x":"hunt"},"intel_infoframes":{"n":"intel_infoframes","t":"view and change HDMI InfoFrames sent by the GPU","s":[["TAGLINE","view and change HDMI InfoFrames sent by the GPU"],["TLDR","**View HDMI infoframes**\n\n```sudo intel_infoframes```\n\n**Display help** with field descriptions\n\n```intel_infoframes --help```"],["SYNOPSIS","**intel_infoframes** [_options_]"],["PARAMETERS","**-h**, **--help**\n> Display help information and field descriptions."],["DESCRIPTION","**intel_infoframes** is a debugging tool that views and changes the HDMI InfoFrames sent by the GPU. InfoFrames carry metadata about video format, audio, and other display parameters.\n\nDescriptions of the InfoFrame fields can be found in the HDMI and CEA-861 specifications. Not all HDMI monitors respect the InfoFrames sent to them. In some cases (e.g., when changing modes) the Kernel will undo the changes made by this tool."],["CAVEATS","Intel graphics only (GEN 4 or newer). Requires active display. Part of intel-gpu-tools (igt-gpu-tools)."],["HISTORY","intel_infoframes is part of **intel-gpu-tools**, created for debugging digital display connections on Intel graphics."],["INSTALL","```apt: sudo apt install intel-gpu-tools```\n\n```pacman: sudo pacman -S intel-gpu-tools```\n\n```zypper: sudo zypper install intel-gpu-tools```\n\n```nix: nix profile install nixpkgs#intel-gpu-tools```"],["SEE ALSO","[intel_panel_fitter](/man/intel_panel_fitter)(1), [intel_reg_read](/man/intel_reg_read)(1), [xrandr](/man/xrandr)(1)"]],"e":["intel_panel_fitter","intel_reg_read","xrandr"],"x":"sudo intel_infoframes"},"jira-issue":{"n":"jira-issue","t":"manages Jira issues from the command line","s":[["TAGLINE","manages Jira issues from the command line"],["TLDR","**List issues**\n\n```jira issue list```\n\n**Create new issue**\n\n```jira issue create```\n\n**View issue details**\n\n```jira issue view [ISSUE-123]```\n\n**Move issue to status**\n\n```jira issue move [ISSUE-123] \"[Done]\"```\n\n**Assign issue**\n\n```jira issue assign [ISSUE-123] [username]```"],["SYNOPSIS","**jira** **issue** _subcommand_ [_options_]"],["PARAMETERS","**list**\n> List issues matching filters.\n\n**create**\n> Create a new issue interactively.\n\n**view** _key_\n> View issue details.\n\n**move** _key_ _status_\n> Transition issue to new status.\n\n**assign** _key_ _user_\n> Assign issue to user.\n\n**comment** _key_\n> Add comment to issue."],["DESCRIPTION","**jira issue** manages Jira issues from the command line. Part of jira-cli, it allows creating, viewing, updating, and transitioning issues without using the web interface. Supports filtering, JQL queries, and batch operations."],["SEE ALSO","[jira](/man/jira)(1), [jira-browse](/man/jira-browse)(1), [jira-sprint](/man/jira-sprint)(1)"]],"e":["jira","jira-browse","jira-sprint"],"x":"jira issue list"},"jj-log":{"n":"jj-log","t":"displays the revision history","s":[["TAGLINE","displays the revision history"],["TLDR","**Show log**\n\n```jj log```\n\n**Show with diff**\n\n```jj log -p```\n\n**Limit entries**\n\n```jj log -n [10]```\n\n**Filter revisions**\n\n```jj log -r \"[revset]\"```\n\n**Show all commits**\n\n```jj log -r \"all()\"```\n\n**Compact format**\n\n```jj log --template builtin_log_oneline```"],["SYNOPSIS","**jj log** [_options_]"],["PARAMETERS","**-r** _REVSET_\n> Revision set to show.\n\n**-p**, **--patch**\n> Show diff.\n\n**-n** _LIMIT_\n> Maximum entries.\n\n**--template** _TEMPLATE_\n> Output template.\n\n**--no-graph**\n> Disable graph display.\n\n**--help**\n> Display help information."],["DESCRIPTION","**jj log** displays the revision history. It shows a graph of changes with descriptions and metadata.\n\nThe command supports revsets for filtering. It visualizes branches and merges in the history graph."],["CAVEATS","Subcommand of jj. Revset syntax differs from Git. Shows working copy by default."],["HISTORY","jj log is part of **Jujutsu**, providing revision history viewing with its unique change-based model."],["INSTALL","```pacman: sudo pacman -S jujutsu```\n\n```apk: sudo apk add jujutsu```\n\n```zypper: sudo zypper install jujutsu```\n\n```brew: brew install jujutsu```\n\n```nix: nix profile install nixpkgs#jujutsu```"],["SEE ALSO","[jj](/man/jj)(1), [jj-show](/man/jj-show)(1), [jj-evolog](/man/jj-evolog)(1)"]],"e":["jj","jj-show","jj-evolog"],"x":"jj log"},"jj-new":{"n":"jj-new","t":"creates a new change in the repository","s":[["TAGLINE","creates a new change in the repository"],["TLDR","**Create new change**\n\n```jj new```\n\n**New change with description**\n\n```jj new -m \"[message]\"```\n\n**New change on specific parent**\n\n```jj new -r [rev]```\n\n**New merge change**\n\n```jj new [rev1] [rev2]```\n\n**Insert change before current**\n\n```jj new --insert-before```"],["SYNOPSIS","**jj new** [_options_] [_revisions_...]"],["PARAMETERS","_REVISIONS_\n> Parent revisions (creates merge if multiple).\n\n**-m** _MESSAGE_\n> Description for new change.\n\n**-r** _REV_\n> Parent revision.\n\n**--insert-before**\n> Insert before current change.\n\n**--insert-after**\n> Insert after specified.\n\n**--help**\n> Display help information."],["DESCRIPTION","**jj new** creates a new change in the repository. It starts a fresh working copy state on top of specified parents.\n\nThe command enables branching and merge point creation. Multiple parents create a merge change."],["CAVEATS","Subcommand of jj. Current change auto-commits. Different from Git branch."],["HISTORY","jj new is part of **Jujutsu**, central to its workflow of treating all changes as first-class revisions."],["INSTALL","```pacman: sudo pacman -S jujutsu```\n\n```apk: sudo apk add jujutsu```\n\n```zypper: sudo zypper install jujutsu```\n\n```brew: brew install jujutsu```\n\n```nix: nix profile install nixpkgs#jujutsu```"],["SEE ALSO","[jj](/man/jj)(1), [jj-describe](/man/jj-describe)(1), [jj-next](/man/jj-next)(1)"]],"e":["jj","jj-describe","jj-next"],"x":"jj new"},"kiterunner-scan":{"n":"kiterunner-scan","t":"performs API endpoint discovery","s":[["TAGLINE","performs API endpoint discovery"],["TLDR","**Scan URL**\n\n```kr scan [url] -w [wordlist]```\n\n**Scan with Assetnote wordlist**\n\n```kr scan [url] -A apiroutes-210228```\n\n**Scan with authentication**\n\n```kr scan [url] -w [list] -H \"Authorization: Bearer [token]\"```\n\n**Scan with concurrency**\n\n```kr scan [url] -w [list] -x [50]```\n\n**Filter status codes**\n\n```kr scan [url] -w [list] --fail-status-codes 404,400```"],["SYNOPSIS","**kr scan** [_options_] _target_"],["PARAMETERS","_TARGET_\n> URL to scan.\n\n**-w** _WORDLIST_\n> Custom wordlist file.\n\n**-A** _KB_\n> Assetnote knowledge base.\n\n**-H** _HEADER_\n> Add request header.\n\n**-x** _N_\n> Concurrent requests.\n\n**--fail-status-codes** _CODES_\n> Status codes to ignore.\n\n**--help**\n> Display help information."],["DESCRIPTION","**kr scan** is the primary scanning subcommand of kiterunner, performing context-aware API endpoint discovery against a target URL. Rather than simply appending wordlist entries as path suffixes, it constructs full API requests with appropriate HTTP methods, content types, and route parameters based on patterns in the supplied wordlist, which makes it significantly more effective at identifying real API routes.\n\nThe command supports Assetnote's pre-built knowledge base wordlists (via the -A flag) as well as custom wordlist files. You can control concurrency with -x, add authentication headers with -H, and filter out unwanted responses by status code. Results include the detected HTTP method, path, status code, and response size, providing a clear picture of the target's API surface."],["CAVEATS","Subcommand of kiterunner. Authorized testing only. Can generate high traffic."],["HISTORY","kr scan is the main scanning command in **kiterunner**, developed by Assetnote for API security testing."],["INSTALL","```nix: nix profile install nixpkgs#kiterunner```"],["SEE ALSO","[kiterunner](/man/kiterunner)(1), [kiterunner-kb](/man/kiterunner-kb)(1)"]],"e":["kiterunner","kiterunner-kb"],"x":"kr scan [url] -w [wordlist]"},"matlab":{"n":"matlab","t":"launches MATLAB, a computing environment for numerical computation","s":[["TAGLINE","launches MATLAB, a computing environment for numerical computation"],["TLDR","**Start MATLAB**\n\n```matlab```\n\n**Start without GUI** (command line)\n\n```matlab -nodesktop```\n\n**Run script and exit**\n\n```matlab -batch \"[script_name]\"```\n\n**Run with specific startup file**\n\n```matlab -r \"[startup_commands]\"```\n\n**Start without splash screen**\n\n```matlab -nosplash```\n\n**Run script with arguments**\n\n```matlab -batch \"[myscript('[arg1]', '[arg2]')]\"```\n\n**Start with reduced memory**\n\n```matlab -nojvm```\n\n**Show version**\n\n```matlab -help```"],["SYNOPSIS","**matlab** [_-nodesktop_] [_-batch command_] [_-r command_] [_-nosplash_] [_options_]"],["PARAMETERS","**-nodesktop**\n> Start without desktop GUI.\n\n**-nodisplay**\n> Start without display (no figures).\n\n**-nosplash**\n> Skip splash screen.\n\n**-nojvm**\n> Start without Java (limited features).\n\n**-batch** _COMMAND_\n> Run command/script and exit.\n\n**-r** _COMMAND_\n> Run command on startup.\n\n**-logfile** _FILE_\n> Copy output to log file.\n\n**-sd** _DIR_\n> Set startup directory.\n\n**-useStartupFolderPref**\n> Use preference for startup folder.\n\n**-singleCompThread**\n> Limit to single thread.\n\n**-help**\n> Show help and version."],["DESCRIPTION","**matlab** launches MATLAB, a computing environment for numerical computation, visualization, and programming. It's widely used in engineering, science, and finance.\n\nThe desktop provides an integrated environment with editor, command window, workspace, and figure windows. No-desktop mode offers command-line interaction for scripts and automation.\n\nBatch mode (-batch) executes commands and exits, suitable for automated workflows and scheduled jobs. It returns non-zero exit codes on errors, unlike -r which continues on errors.\n\nScripts (.m files) contain MATLAB code. Functions define reusable operations. The MATLAB path determines which files are accessible.\n\nWithout JVM (-nojvm), MATLAB uses less memory but loses desktop, figure windows, and some toolbox features. Useful for computation-only tasks on resource-limited systems."],["CAVEATS","Commercial license required. Large memory footprint. Startup time can be significant. Some toolboxes require additional licenses. Not all features work in nodesktop mode."],["HISTORY","**MATLAB** was created by **Cleve Moler** in the late **1970s** to provide students access to LINPACK and EISPACK without Fortran. **MathWorks** was founded in **1984** to commercialize it. MATLAB became the standard for numerical computing in academia and industry."],["INSTALL","```aur: yay -S matlab```"],["SEE ALSO","[octave](/man/octave)(1), [python](/man/python)(1), [julia](/man/julia)(1), [r](/man/r)(1)"]],"e":["octave","python","julia","r"],"x":"matlab"},"mcomp":{"n":"mcomp","t":"Compares two files using mtools","s":[["TAGLINE","Compares two files using mtools"],["TLDR","**Compare a file on a DOS disk with a local file**\n\n```mcomp [a:file.txt] [path/to/local_file.txt]```\n\n**Compare a file from a disk image with a local file**\n\n```mcomp -i [path/to/disk.img] [::/file.txt] [path/to/local_file.txt]```"],["SYNOPSIS","**mcomp** _msdos-file_ _local-file_"],["PARAMETERS","_msdos-file_\n> File on a DOS/floppy disk accessed via mtools.\n\n_local-file_\n> Local file to compare against."],["DESCRIPTION","**mcomp** compares two files byte by byte, where the first file resides on a DOS-formatted disk (such as a floppy) accessible through mtools. It is functionally equivalent to using mcopy to copy the file locally and then comparing with cmp.\n\nThis command is part of the **mtools** package, a collection of utilities for accessing MS-DOS disks from Unix without mounting them."],["CAVEATS","The first argument must be a file accessible via mtools (e.g., on a DOS-formatted disk or disk image). On modern systems, using mcopy followed by cmp provides the same functionality and is more commonly used."],["INSTALL","```apt: sudo apt install mtools```\n\n```dnf: sudo dnf install mtools```\n\n```pacman: sudo pacman -S mtools```\n\n```apk: sudo apk add mtools```\n\n```zypper: sudo zypper install mtools```\n\n```brew: brew install mtools```\n\n```nix: nix profile install nixpkgs#mtools```"],["SEE ALSO","[cmp](/man/cmp)(1), [mcopy](/man/mcopy)(1), [diff](/man/diff)(1)"]],"e":["cmp","mcopy","diff"],"x":"mcomp [a:file.txt] [path/to/local_file.txt]"},"mesg":{"n":"mesg","t":"controls whether other users can send messages to your terminal using","s":[["TAGLINE","controls whether other users can send messages to your terminal using"],["TLDR","Check **current status**\n\n```mesg```\n\n**Disallow** messages\n\n```mesg n```\n\n**Allow** messages\n\n```mesg y```\n\nEnable **verbose** mode\n\n```mesg --verbose```"],["SYNOPSIS","**mesg** [**y** | **n**]"],["DESCRIPTION","**mesg** controls whether other users can send messages to your terminal using the write command. Without arguments, it displays the current setting."],["PARAMETERS","**y**\n> Allow messages from other users\n\n**n**\n> Disallow messages from other users\n\n**-v, --verbose**\n> Print warning if not executed from terminal"],["OUTPUT","Without arguments shows:\n**is y**: Messages allowed\n**is n**: Messages disallowed"],["CAVEATS","Affects the current terminal only. Setting is not persistent across sessions. Some terminals may default to denying messages."],["INSTALL","```apk: sudo apk add util-linux-misc```"],["SEE ALSO","[write](/man/write)(1), [talk](/man/talk)(1), [wall](/man/wall)(1)"]],"e":["write","talk","wall"],"x":"mesg"},"mflux":{"n":"mflux","t":"Flux image generation tool built natively on Apple's MLX framework","s":[["TAGLINE","Flux image generation tool built natively on Apple's MLX framework"],["TLDR","**Generate an image from a text prompt**\n\n```mflux-generate --model [schnell] --prompt \"[a sunset over mountains]\"```\n\n**Generate with the higher-quality dev model and more steps**\n\n```mflux-generate --model [dev] --prompt \"[prompt]\" --steps [25] --seed [42]```\n\n**Specify image dimensions**\n\n```mflux-generate --model [schnell] --prompt \"[prompt]\" --width [1024] --height [768]```\n\n**Save to a specific output path**\n\n```mflux-generate --model [schnell] --prompt \"[prompt]\" --output [output.png]```\n\n**Use a quantized model for lower memory usage**\n\n```mflux-generate --model [schnell] --prompt \"[prompt]\" --quantize [8]```\n\n**Generate using image-to-image with an init image**\n\n```mflux-generate --model [dev] --prompt \"[prompt]\" --image-path [input.png] --image-strength [0.4]```\n\n**Apply LoRA adapter weights**\n\n```mflux-generate --model [dev] --prompt \"[prompt]\" --lora-paths [adapter.safetensors]```\n\n**Save a local quantized copy of model weights**\n\n```mflux-save --model [schnell] --quantize [8] --path [path/to/save]```"],["SYNOPSIS","**mflux-generate** **--model** _name_ **--prompt** _text_ [_options_]\n\n**mflux-save** **--model** _name_ **--path** _dir_ [_options_]"],["PARAMETERS","**--model**, **-m** _NAME_\n> Model to use (schnell, dev, or a HuggingFace repo/local path).\n\n**--prompt** _TEXT_\n> Text prompt for image generation. Use - to read from stdin.\n\n**--output** _FILE_\n> Output image path.\n\n**--width** _PX_\n> Image width in pixels.\n\n**--height** _PX_\n> Image height in pixels.\n\n**--steps** _N_\n> Number of inference steps.\n\n**--seed** _INT_\n> Random seed for reproducibility.\n\n**--quantize**, **-q** _BITS_\n> Quantization level (4 or 8 bit).\n\n**--guidance** _FLOAT_\n> Guidance scale.\n\n**--negative-prompt** _TEXT_\n> Text prompt for what the model should not generate.\n\n**--image-path** _FILE_\n> Path to an initial image for image-to-image generation.\n\n**--image-strength** _FLOAT_\n> How strongly the initial image influences output (default: 0.4, 0.0 = no influence).\n\n**--lora-paths** _FILE_...\n> Paths to one or more LoRA adapter weights.\n\n**--lora-scales** _FLOAT_...\n> Scales for each LoRA adapter.\n\n**--metadata**\n> Export a JSON file with generation metadata alongside the image.\n\n**--low-ram**\n> Reduce GPU memory usage by limiting MLX cache and releasing components after use.\n\n**--base-model** _NAME_\n> Specify architecture (schnell, dev) when loading from a local path."],["DESCRIPTION","**mflux** is a Flux image generation tool built natively on Apple's **MLX** framework, optimized for Apple Silicon (M1/M2/M3/M4). It generates images locally using Flux models without requiring a GPU server or cloud API. The package provides multiple CLI commands: **mflux-generate** for image generation, **mflux-save** for saving quantized model weights locally, and **mflux-info** for viewing image metadata.\n\nInstallation is via pip (`pip install mflux`). Model weights are downloaded from HuggingFace on first use and cached locally. Custom models can also be loaded from local paths or HuggingFace repositories.\n\n**schnell** is faster with fewer steps needed (2-4 steps). **dev** produces higher quality but requires more steps (20-25). Quantization (4-bit or 8-bit) reduces memory usage for machines with limited unified memory. The **--low-ram** flag further reduces memory by releasing components after use.\n\nLoRA adapters allow fine-tuned styles and concepts to be applied on top of base models. Image-to-image generation is supported via **--image-path** and **--image-strength**."],["CAVEATS","Apple Silicon only (M-series Macs). Requires Python 3.10+. Model downloads are several GB. Memory usage depends on model and quantization level. The **--low-ram** flag helps on memory-constrained systems but limits generation to single images."],["HISTORY","**mflux** was created by **Filip Strand** in **2024** to bring native Flux image generation to Apple Silicon using the MLX framework. It has since expanded to support multiple model architectures beyond Flux, including models loaded from HuggingFace repositories and local paths."],["SEE ALSO","[sd-cli](/man/sd-cli)(1), [comfyui](/man/comfyui)(1)"]],"e":["sd-cli","comfyui"],"x":"mflux-generate --model [schnell] --prompt \"[a sunset over mountains]\""},"npm-restart":{"n":"npm-restart","t":"runs stop, restart, and start scripts","s":[["TAGLINE","runs stop, restart, and start scripts"],["TLDR","**Restart application**\n\n```npm restart```"],["SYNOPSIS","**npm** **restart** [_args_...]"],["DESCRIPTION","**npm restart** runs the lifecycle hooks **prerestart**, **restart**, and **postrestart** in order. If no **restart** script is defined in **package.json**, it falls back to running **stop** followed by **start** (so the package's own **start** and **stop** scripts execute). This is the conventional way to bounce a long-running Node process described by **package.json scripts** without rewriting the command in your shell."],["CAVEATS","Unlike a process supervisor (pm2, systemd), **npm restart** does not background the new process or persist it across shell exits — it simply runs the **start** script in the foreground. For production process management use a real supervisor."],["INSTALL","```pacman: sudo pacman -S npm```\n\n```apk: sudo apk add npm```\n\n```brew: brew install npm```"],["SEE ALSO","[npm](/man/npm)(1), [npm-start](/man/npm-start)(1), [npm-stop](/man/npm-stop)(1)"]],"e":["npm","npm-start","npm-stop"],"x":"npm restart"},"omz":{"n":"omz","t":"Oh My Zsh management CLI","s":[["TAGLINE","Oh My Zsh management CLI"],["TLDR","**Update Oh My Zsh**\n\n```omz update```\n\n**List all available plugins**\n\n```omz plugin list```\n\n**Enable a plugin**\n\n```omz plugin enable [plugin_name]```\n\n**Disable a plugin**\n\n```omz plugin disable [plugin_name]```\n\n**List available themes**\n\n```omz theme list```\n\n**Set a theme**\n\n```omz theme set [theme_name]```\n\n**Show the current theme**\n\n```omz theme use [theme_name]```\n\n**Show changelog**\n\n```omz changelog```\n\n**Reload the Zsh configuration**\n\n```omz reload```"],["SYNOPSIS","**omz** _command_ [_subcommand_] [_args_]"],["PARAMETERS","**update**\n> Update Oh My Zsh to the latest version.\n\n**plugin list**\n> List all available plugins.\n\n**plugin enable** _NAME_\n> Enable a plugin by adding it to the plugins array in .zshrc.\n\n**plugin disable** _NAME_\n> Disable a plugin by removing it from the plugins array in .zshrc.\n\n**plugin info** _NAME_\n> Show information about a plugin.\n\n**theme list**\n> List all available themes.\n\n**theme set** _NAME_\n> Set theme in .zshrc.\n\n**theme use** _NAME_\n> Load a theme temporarily for the current session.\n\n**changelog**\n> Show recent changes and release notes.\n\n**reload**\n> Reload the current Zsh session with updated configuration.\n\n**--help**\n> Display help information."],["DESCRIPTION","**omz** is the command-line management utility for the Oh My Zsh framework. It provides commands to update the framework, manage plugins and themes, and reload configuration. Changes to plugins and themes are persisted by modifying the `.zshrc` file."],["CAVEATS","Requires Oh My Zsh to be installed. Only works with the Zsh shell. Plugin/theme changes modify ~/.zshrc directly. The `theme use` command only applies for the current session; use `theme set` for persistence."],["SEE ALSO","[zsh](/man/zsh)(1), [oh-my-zsh](/man/oh-my-zsh)(1)"]],"e":["zsh","oh-my-zsh"],"x":"omz update"},"packagist":{"n":"packagist","t":"main repository for PHP packages used by Composer","s":[["TAGLINE","main repository for PHP packages used by Composer"],["TLDR","**Search packages**\n\n```composer search [query]```\n\n**Show package info**\n\n```composer show [vendor/package]```\n\n**Require package**\n\n```composer require [vendor/package]```\n\n**Browse on web**\n\n```xdg-open https://packagist.org/packages/[vendor/package]```"],["SYNOPSIS","**Packagist** - PHP package repository"],["DESCRIPTION","**Packagist** is the main repository for PHP packages used by Composer. It aggregates PHP packages from GitHub, GitLab, and other sources, making them installable via Composer.\n\nPackagist is not a command but a service accessed through Composer and web interface."],["COMPOSER COMMANDS","```bash"],["Search packages","composer search monolog"],["Get package info","composer info monolog/monolog"],["Install package","composer require monolog/monolog"],["Update packages","composer update\n```"],["PUBLISHING","```bash"],["3. Submit URL at packagist.org","```"],["PACKAGE NAMING","```\nvendor/package\nmonolog/monolog\nsymfony/console\nlaravel/framework\n```"],["CAVEATS","Requires Composer installed. Packages need composer.json. Auto-updates from VCS repositories."],["HISTORY","Packagist was created by **Jordi Boggiano** and **Nils Adermann** in **2011** as the default repository for Composer, PHP's dependency manager."],["SEE ALSO","[composer](/man/composer)(1), [php](/man/php)(1), [phpunit](/man/phpunit)(1)"]],"e":["composer","php","phpunit"],"x":"composer search [query]"},"pacstall":{"n":"pacstall","t":"AUR-inspired package manager for Ubuntu and Debian-based distributions","s":[["TAGLINE","AUR-inspired package manager for Ubuntu and Debian-based distributions"],["TLDR","**Search** for a package\n\n```pacstall -S [query]```\n\n**Install** a package\n\n```pacstall -I [package]```\n\n**Remove** a package\n\n```pacstall -R [package]```\n\n**Add** a repository\n\n```pacstall -A [repository_url]```\n\n**Update** pacstall's scripts\n\n```pacstall -U```\n\n**Upgrade** all installed packages\n\n```pacstall -Up```\n\nDisplay **information** about an installed package\n\n```pacstall -Ci [package]```\n\n**List** all installed packages\n\n```pacstall -L```\n\nInstall a package without prompts, keeping build files\n\n```pacstall -P -K -I [package]```"],["SYNOPSIS","**pacstall** [_command_] [_options_] [_package_...]"],["PARAMETERS","**-I**, **--install** _package_\n> Install a package from a repository or a pacscript.\n\n**-R**, **--remove** _package_\n> Remove a package from the system.\n\n**-S**, **--search** _query_\n> Search for packages from all available repositories.\n\n**-Sd**, **--search-description** _query_\n> Search package names and descriptions.\n\n**-Si**, **--search-info** _package_\n> Display metadata (SRCINFO) of a remote package.\n\n**-Ci**, **--cache-info** _package_\n> Display metadata of an installed package.\n\n**-A**, **--add-repo** _url_\n> Add a repository to pacstall.\n\n**-Rr**, **--remove-repo** _repo_\n> Remove a repository from pacstall.\n\n**-U**, **--update**\n> Update pacstall and needed scripts.\n\n**-Up**, **--upgrade**\n> Upgrade packages that have a newer version.\n\n**-L**, **--list**\n> List installed packages.\n\n**-T**, **--tree** _package_\n> Display a tree graph of a package's dependencies.\n\n**-D**, **--download** _package_\n> Download pacscript to the current directory.\n\n**-Qa**, **--quality-assurance** _package#number_\n> Test a package from a pull request prior to merging.\n\n**-V**, **--version**\n> Display version information.\n\n**-P**, **--disable-prompts**\n> Disable prompts and accept all defaults.\n\n**-K**, **--keep**\n> Keep the build files after installation.\n\n**-B**, **--build-only**\n> Build the deb but do not install it.\n\n**-Q**, **--quiet**\n> Download package entries quietly.\n\n**-Nc**, **--nocheck**\n> Skip the check() function if present in the pacscript.\n\n**-Ns**, **--nosandbox**\n> Build the package without bwrap sandboxing."],["DESCRIPTION","**pacstall** is a package manager for Ubuntu and Debian-based distributions that brings AUR-like functionality by allowing users to install packages from community-maintained build scripts called pacscripts (similar to Arch's PKGBUILDs).\n\nThe tool maintains its own repositories of build scripts, separate from the system's apt repositories. Users can add custom repositories hosted on GitHub or GitLab, enabling community-driven package distribution outside official channels."],["CAVEATS","Packages are built from source, which can be time-consuming. Requires build dependencies to be installed. Not officially supported by Ubuntu/Debian; packages may conflict with system packages. Always review pacscripts before installation."],["HISTORY","Created to bring the AUR (Arch User Repository) experience to Ubuntu users. Developed as an alternative to PPAs and Snaps for users who prefer building packages from source with community-maintained scripts. The project aims to provide a simple way to install software not available in official repositories."],["SEE ALSO","[apt](/man/apt)(8), [dpkg](/man/dpkg)(1), [yay](/man/yay)(8), [makepkg](/man/makepkg)(8), [nala](/man/nala)(8)"]],"e":["apt","dpkg","yay","makepkg","nala"],"x":"pacstall -S [query]"},"pam_tally":{"n":"pam_tally","t":"counts login attempts","s":[["TAGLINE","counts login attempts"],["TLDR","**Enable login counting (deprecated)**\n\n```auth required pam_tally.so```\n\n**Lock after failures**\n\n```auth required pam_tally.so deny=5```\n\n**Reset tally**\n\n```pam_tally --user=[username] --reset```\n\n**View tally**\n\n```pam_tally --user=[username]```"],["SYNOPSIS","**pam_tally.so** [_options_]"],["PARAMETERS","**deny=**_N_\n> Lock after N failures.\n\n**unlock_time=**_SECONDS_\n> Auto unlock time.\n\n**per_user**\n> Per-user settings.\n\n**--user** _NAME_\n> Specify user.\n\n**--reset**\n> Reset counter."],["DESCRIPTION","**pam_tally** counts login attempts. Locks accounts on failures.\n\nThe module tracks failed logins. Deprecated, use pam_tally2."],["CAVEATS","Deprecated. Use pam_tally2 instead. Legacy systems."],["HISTORY","pam_tally provided **login attempt counting** before pam_tally2."],["SEE ALSO","[pam](/man/pam)(8), [pam_tally2](/man/pam_tally2)(8), [pam_faillock](/man/pam_faillock)(8)"]],"e":["pam","pam_tally2","pam_faillock"],"x":"auth required pam_tally.so"},"pbmtoptx":{"n":"pbmtoptx","t":"converts PBM bitmaps to Printronix printer graphics format","s":[["TAGLINE","converts PBM bitmaps to Printronix printer graphics format"],["TLDR","**Convert PBM to Printronix format**\n\n```pbmtoptx [input.pbm] > [output.ptx]```"],["SYNOPSIS","**pbmtoptx** [_file_]"],["DESCRIPTION","**pbmtoptx** converts PBM bitmaps to Printronix printer graphics format. Printronix made line printers with graphics capabilities. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pbmtoepson](/man/pbmtoepson)(1), [pbmtolj](/man/pbmtolj)(1)"]],"e":["pbmtoepson","pbmtolj"],"x":"pbmtoptx [input.pbm] > [output.ptx]"},"pdfattach":{"n":"pdfattach","t":"adds a new embedded file to an existing PDF file","s":[["TAGLINE","adds a new embedded file to an existing PDF file"],["TLDR","**Add** attachment to PDF\n\n```pdfattach input.pdf file_to_attach output.pdf```\n\n**Replace** existing attachment with same name\n\n```pdfattach -replace input.pdf file_to_attach output.pdf```\n\nDisplay **help**\n\n```pdfattach --help```\n\nDisplay **version**\n\n```pdfattach -v```"],["SYNOPSIS","**pdfattach** [_options_] _PDF-file_ _file-to-attach_ _output-PDF-file_"],["DESCRIPTION","**pdfattach** adds a new embedded file (attachment) to an existing PDF file. The attachment is stored inside the PDF and can be extracted using pdfdetach."],["PARAMETERS","**-replace**\n> Replace an existing attachment with the same name if it exists\n\n**-h, --help**\n> Display usage information\n\n**-v**\n> Display version information"],["CAVEATS","The original PDF file is not modified; a new output file is created with the attachment. To update a file in place, output to a temporary file and then rename it."],["HISTORY","**pdfattach** is part of **poppler-utils**, a set of command-line utilities based on the Poppler PDF rendering library for working with PDF files."],["INSTALL","```apt: sudo apt install poppler-utils```\n\n```dnf: sudo dnf install poppler-utils```\n\n```apk: sudo apk add poppler-utils```\n\n```nix: nix profile install nixpkgs#poppler-utils```"],["SEE ALSO","[pdfdetach](/man/pdfdetach)(1), [pdfinfo](/man/pdfinfo)(1), [pdfunite](/man/pdfunite)(1)"]],"e":["pdfdetach","pdfinfo","pdfunite"],"x":"pdfattach input.pdf file_to_attach output.pdf"},"pg_resetwal":{"n":"pg_resetwal","t":"resets write-ahead log and control information","s":[["TAGLINE","resets write-ahead log and control information"],["TLDR","**Reset WAL (dry run)**\n\n```pg_resetwal -n [data_dir]```\n\n**Reset WAL**\n\n```pg_resetwal [data_dir]```\n\n**Force reset**\n\n```pg_resetwal -f [data_dir]```"],["SYNOPSIS","**pg_resetwal** [_options_] [**-D**] _datadir_"],["PARAMETERS","**-D**, **--pgdata** _dir_\n> Data directory.\n\n**-n**, **--dry-run**\n> Show what would be done.\n\n**-f**, **--force**\n> Force reset.\n\n**-e**, **--epoch** _epoch_\n> Set next transaction ID epoch.\n\n**-x**, **--next-transaction-id** _xid_\n> Set next transaction ID.\n\n**-o**, **--next-oid** _oid_\n> Set next OID.\n\n**-m**, **--multixact-ids** _mxid,mxid_\n> Set next and oldest multitransaction ID.\n\n**-O**, **--multixact-offset** _mxoff_\n> Set next multitransaction offset.\n\n**-c**, **--commit-timestamp-ids** _xid,xid_\n> Set oldest and newest transaction IDs for commit timestamps.\n\n**-u**, **--oldest-transaction-id** _xid_\n> Set oldest unfrozen transaction ID.\n\n**-l**, **--next-wal-file** _walfile_\n> Force minimum WAL starting location.\n\n**--wal-segsize** _size_\n> Set WAL segment size in megabytes."],["DESCRIPTION","**pg_resetwal** resets write-ahead log and control information. Last resort recovery when WAL is corrupted. May cause data loss. Server must be stopped."],["CAVEATS","Use only as a last resort when WAL is corrupted or pg_controldata shows inconsistencies. May cause data loss and transaction integrity issues. The PostgreSQL server must be stopped before running."],["INSTALL","```dnf: sudo dnf install postgresql17```\n\n```apk: sudo apk add postgresql17```\n\n```zypper: sudo zypper install postgresql17```"],["SEE ALSO","[pg_controldata](/man/pg_controldata)(1), [pg_rewind](/man/pg_rewind)(1)"]],"e":["pg_controldata","pg_rewind"],"x":"pg_resetwal -n [data_dir]"},"pgm":{"n":"pgm","t":"grayscale image format in the Netpbm family","s":[["TAGLINE","grayscale image format in the Netpbm family"],["TLDR","**View PGM format info**\n\n```pamfile [image.pgm]```\n\n**Convert JPEG to PGM**\n\n```jpegtopnm [image.jpg] | ppmtopgm > [output.pgm]```\n\n**Create PGM from text**\n\n```echo \"P2 3 2 255 0 128 255 64 192 32\" > [output.pgm]```\n\n**Display PGM image**\n\n```display [image.pgm]```"],["SYNOPSIS","PGM format specification and related utilities"],["DESCRIPTION","**PGM** (Portable Graymap) is a grayscale image format in the Netpbm family. It stores images as grayscale values from 0 (black) to maxval (white).\n\nSimple ASCII or binary format, easy to read and write programmatically."],["FORMAT","```\nP2              # ASCII format (P5 for binary)\nwidth height\nmaxval          # Maximum gray value (usually 255)\npixel_values    # Space-separated (ASCII) or raw (binary)\n```"],["EXAMPLE FILE","```\nP2"],["comment","3 2\n255\n0   128  255\n64  192  32\n```"],["CONVERSIONS","```bash"],["JPEG to PGM","jpegtopnm photo.jpg | ppmtopgm > photo.pgm"],["PNG to PGM","pngtopam image.png | pamtopnm | ppmtopgm > image.pgm"],["PGM to PNG","pnmtopng image.pgm > image.png\n```"],["CAVEATS","ASCII format is larger but human-readable. Binary more efficient. Part of Netpbm toolkit."],["HISTORY","PGM was designed by **Jef Poskanzer** as part of the **Netpbm** (formerly Pbmplus) toolkit for image manipulation."],["SEE ALSO","[ppm](/man/ppm)(5), [pbm](/man/pbm)(5), [pnm](/man/pnm)(5), [netpbm](/man/netpbm)(1)"]],"e":["ppm","pbm","pnm","netpbm"],"x":"pamfile [image.pgm]"},"pio-project":{"n":"pio-project","t":"inspects PlatformIO project configuration and metadata","s":[["TAGLINE","inspects PlatformIO project configuration and metadata"],["TLDR","**Show project configuration**\n\n```pio project config```\n\n**Show project metadata**\n\n```pio project metadata -e [uno]```\n\n**Initialize project data**\n\n```pio project init```"],["SYNOPSIS","**pio project** _command_ [_options_]"],["PARAMETERS","**config**\n> Show project configuration.\n\n**metadata**\n> Show project metadata.\n\n**init**\n> Initialize project.\n\n**-e**, **--environment** _name_\n> Target environment.\n\n**-d**, **--project-dir** _dir_\n> Project directory.\n\n**--json-output**\n> Output in JSON format."],["DESCRIPTION","**pio project** inspects PlatformIO project configuration and metadata. Shows parsed platformio.ini settings, resolved dependencies, and build information."],["INSTALL","```aur: yay -S pio```"],["SEE ALSO","[pio-init](/man/pio-init)(1), [pio-run](/man/pio-run)(1)"]],"e":["pio-init","pio-run"],"x":"pio project config"},"plesk":{"n":"plesk","t":"Command-line interface for Plesk hosting panel","s":[["TAGLINE","Command-line interface for Plesk hosting panel"],["TLDR","**Show version**\n\n```plesk version```\n\n**List domains**\n\n```plesk bin domain --list```\n\n**Create backup**\n\n```plesk bin pleskbackup --domains-name [domain] --output-file [backup]```\n\n**Repair installation**\n\n```plesk repair all```\n\n**Check status**\n\n```plesk bin service --status```"],["SYNOPSIS","**plesk** [_command_] [_options_]"],["PARAMETERS","**bin** _COMMAND_\n> Run Plesk utility.\n\n**repair** _COMPONENT_\n> Repair component.\n\n**version**\n> Show version.\n\n**--help**\n> Display help."],["DESCRIPTION","**plesk** is the command-line interface for the Plesk web hosting control panel. It provides access to domain management, backup operations, service control, and system diagnostics without using the web interface.\n\nThe **bin** subcommand runs individual Plesk utilities for specific tasks like domain configuration, database management, and mail setup. The **repair** command diagnoses and fixes common installation issues."],["CAVEATS","Commercial software. Root access required. Server administration."],["HISTORY","Plesk CLI provides **command-line management** of Plesk hosting panel."],["SEE ALSO","[apache2ctl](/man/apache2ctl)(1), [nginx](/man/nginx)(1)"]],"e":["apache2ctl","nginx"],"x":"plesk version"},"pw-dsdplay":{"n":"pw-dsdplay","t":"Play DSD audio files through PipeWire","s":[["TAGLINE","Play DSD audio files through PipeWire"],["TLDR","**Play a DSF file**\n\n```pw-dsdplay [path/to/file.dsf]```\n\n**Play with a specific volume**\n\n```pw-dsdplay --volume [0.5] [path/to/file.dsf]```\n\n**Play to a specific target node**\n\n```pw-dsdplay --target [node_id] [path/to/file.dsf]```\n\n**Play with verbose output**\n\n```pw-dsdplay -v [path/to/file.dsf]```"],["SYNOPSIS","**pw-dsdplay** [_options_] [_FILE_ | **-**]"],["PARAMETERS","**--volume** _level_\n> Stream volume (default 1.0).\n\n**--rate** _hz_\n> Sample rate in Hz (default 48000).\n\n**--channels** _num_\n> Number of audio channels (default 2).\n\n**--channel-map** _map_\n> Channel layout (e.g. stereo, surround-51, or custom FL,FR mapping).\n\n**--format** _fmt_\n> Sample format: u8, s8, s16, s24, s32, f32, f64 (default s16).\n\n**--target** _value_\n> Target node: auto, 0, or a specific node ID.\n\n**--latency** _value_\n> Node latency (default 100ms).\n\n**-q**, **--quality** _level_\n> Resampler quality (0-15, default 4).\n\n**-R**, **--remote** _name_\n> Connect to a specific PipeWire instance.\n\n**-P**, **--properties** _json_\n> Extra stream properties as a JSON object.\n\n**-v**, **--verbose**\n> Enable verbose output.\n\n**-h**, **--help**\n> Display help information.\n\n**--version**\n> Show version details."],["DESCRIPTION","**pw-dsdplay** plays DSD (Direct Stream Digital) audio files in DSF format through a PipeWire server. It is a convenience wrapper for **pw-cat --playback --dsd**, making DSD data available to the PipeWire audio graph.\n\nDSD is a high-resolution audio format used in Super Audio CDs (SACDs) that encodes audio as a stream of single-bit values at very high sample rates (typically 2.8224 MHz or higher). Native DSD playback requires compatible hardware that supports DSD natively.\n\nWhen **-** is given as the file, data is read from stdin."],["CAVEATS","Requires native DSD-capable hardware to produce audible output. Without compatible hardware, no audio will be heard. DSF is the only supported file format for DSD playback."],["HISTORY","Part of **PipeWire**, the modern multimedia framework for Linux created by **Wim Taymans** at Red Hat. PipeWire was first released in **2017** and has become the default audio and video server in major Linux distributions, replacing both PulseAudio and JACK. The pw-cat family of tools provides simple command-line media playback and capture."],["INSTALL","```apt: sudo apt install pipewire-bin```\n\n```apk: sudo apk add pipewire-tools```\n\n```zypper: sudo zypper install pipewire-tools```"],["SEE ALSO","[pw-cat](/man/pw-cat)(1), [pw-play](/man/pw-play)(1), [pw-record](/man/pw-record)(1), [pipewire](/man/pipewire)(1)"]],"e":["pw-cat","pw-play","pw-record","pipewire"],"x":"pw-dsdplay [path/to/file.dsf]"},"rcsclean":{"n":"rcsclean","t":"Remove unchanged RCS working files","s":[["TAGLINE","Remove unchanged RCS working files"],["TLDR","**Remove unchanged working files**\n\n```rcsclean```\n\n**Remove unchanged working files** in a directory\n\n```rcsclean [path/to/directory]```\n\n**Remove unchanged working files** and unlock them\n\n```rcsclean -u```\n\n**Preview which files would be removed** (dry run)\n\n```rcsclean -n```\n\n**Remove a specific unchanged file**\n\n```rcsclean [filename]```"],["SYNOPSIS","**rcsclean** [_options_] [_file_...]"],["PARAMETERS","**-n**\n> Dry run; show what would be done without doing it\n\n**-q**\n> Quiet mode; suppress diagnostics\n\n**-u** [_rev_]\n> Unlock the revision if locked by the caller\n\n**-r** _rev_\n> Compare against specified revision instead of default\n\n**-T**\n> Preserve modification time of RCS file\n\n**-V**\n> Print version number"],["DESCRIPTION","**rcsclean** removes working files that have not been modified since checkout. It compares each working file against its corresponding RCS revision and deletes unchanged files, cleaning up the working directory.\n\nThis utility is useful for removing files that were checked out for reading but never modified, reducing clutter in the working directory. With **-u**, it also unlocks any locked revisions that the caller owns if the working file is unchanged.\n\nBy default, rcsclean operates on all RCS-controlled files in the current directory. Specify filenames to operate on specific files only."],["CAVEATS","Only removes files that are completely unchanged from their checked-out revision. Any modification, including whitespace changes, prevents deletion.\n\nThe **-u** option only unlocks revisions locked by the current user. It cannot unlock files locked by other users.\n\nUse **-n** (dry run) first to preview which files will be removed, especially when running on an entire directory."],["INSTALL","```apt: sudo apt install rcs```\n\n```dnf: sudo dnf install rcs```\n\n```pacman: sudo pacman -S rcs```\n\n```apk: sudo apk add rcs```\n\n```zypper: sudo zypper install rcs```\n\n```brew: brew install rcs```\n\n```nix: nix profile install nixpkgs#rcs```"],["SEE ALSO","[rcs](/man/rcs)(1), [ci](/man/ci)(1), [co](/man/co)(1), [rcsdiff](/man/rcsdiff)(1)"]],"e":["rcs","ci","co","rcsdiff"],"x":"rcsclean"},"remembrane":{"n":"remembrane","t":"Local-first SQLite memory store CLI for AI agents","s":[["TAGLINE","Local-first SQLite memory store CLI for AI agents"],["TLDR","**Install** via pip\n\n```pip install remembrane```\n\n**Store** a memory in a SQLite database\n\n```remembrane --db [agent.db] store \"[the user prefers dark mode]\" --importance 0.8```\n\n**Recall** memories matching a query\n\n```remembrane --db [agent.db] recall \"[what theme?]\"```\n\n**List** all stored memories\n\n```remembrane --db [agent.db] list```\n\nShow **stats** for the memory database\n\n```remembrane --db [agent.db] stats```\n\n**Export** memories as JSON\n\n```remembrane --db [agent.db] export > [backup.json]```\n\n**Snapshot** the current state, then **diff** later changes\n\n```remembrane --db [agent.db] snapshot [v1]```\n\n```remembrane --db [agent.db] diff [v1]```"],["SYNOPSIS","**remembrane** [**--db** _path_] _subcommand_ [_options_] [_args_]"],["DESCRIPTION","**remembrane** is a command-line interface for inspecting and managing a local-first agent memory store backed by a single SQLite file. The same package also exposes a Python API, framework adapters (LangChain, CrewAI), and an optional MCP server entry point (**remembrane-mcp**).\n\nMemories are stored with optional namespaces and importance scores. **recall** ranks candidates with a hybrid of vector similarity and BM25 keyword score, plus recency decay, importance, and usefulness learned from task-outcome feedback. Ranking is exact (brute-force over the store) rather than approximate nearest-neighbor; the default embedder is a pure-stdlib hash embedder so **pip install remembrane** needs no extra dependencies.\n\nEvery store, forget, and reinforce operation is journaled, so **snapshot**, **diff**, and **log** support time-travel over what the agent knew. **conflicts** surfaces contradictory memories for adjudication instead of silently picking one. **pack** selects an optimal (or near-optimal without numpy) memory set under a token budget. **merge** absorbs another memory database with near-duplicate deduplication."],["PARAMETERS","**--db** _path_\n> Path to the SQLite memory database (default: **remembrane.db**).\n\n**store** [_content_] [**--file** _path_|**-**] [**--namespace** _ns_] [**--importance** _f_]\n> Store a memory. Provide _content_ as an argument, or use **--file** (or **--file -** for stdin) for large payloads that hit OS argv limits.\n\n**recall** _query_ [**--namespace** _ns_] [**-k** _n_] [**--mode** hybrid|vector|keyword] [**--explain**]\n> Recall top memories for _query_. **--explain** prints the ranking breakdown per result.\n\n**list** [**--namespace** _ns_]\n> List stored memories (optionally filtered by namespace).\n\n**forget** _memory_id_\n> Delete one memory by id.\n\n**export** [**--namespace** _ns_]\n> Export memories as JSON to stdout.\n\n**stats**\n> Print memory counts overall and per namespace.\n\n**snapshot** _label_\n> Record a named point in the journal for later **diff** / reconstruction.\n\n**log** [**--namespace** _ns_] [**--limit** _n_]\n> Show newest-first history of operations (default limit 30).\n\n**diff** _a_ [_b_]\n> Show what changed between snapshot _a_ and snapshot _b_ (or now if _b_ is omitted).\n\n**conflicts** [_query_] [**--namespace** _ns_] [**--min-confidence** possible|likely]\n> Surface memories in tension (heuristic conflict detection).\n\n**feedback** _memory_id_ **--useful**|**--useless**\n> Record task-outcome feedback that adjusts usefulness ranking.\n\n**pack** _query_ [**--budget** _tokens_] [**--namespace** _ns_]\n> Select memories that fit a hard token budget for context packing.\n\n**merge** _source.db_ [**--dedupe-threshold** _f_]\n> Merge another memory database into the current one."],["CAVEATS","The CLI writes wherever **--db** points with the invoking user's permissions; it is a local tool, not a sandbox. The default embedder is lexical (n-gram hashes), not semantic—plug in optional embedders for true semantic recall, and do not mix embedders in one database. Conflict detection is heuristic: treat **likely** / **possible** hits as candidates for the agent or user to adjudicate. WAL mode keeps transient **-wal** / **-shm** sidecars next to the db file; avoid placing memory files on NFS/SMB. For MCP use, install **remembrane[mcp]** and run **remembrane-mcp** separately."],["HISTORY","**remembrane** is an open-source Python project for local agent memory without a vector-database dependency. Public releases on PyPI (0.4–0.5.x series) refined hybrid recall, conflict signals, usefulness feedback, and packing; the console scripts **remembrane** and **remembrane-mcp** ship with the package."],["SEE ALSO","[sqlite3](/man/sqlite3)(1), [pip](/man/pip)(1)"],["RESOURCES","```[Source code](https://github.com/satyasairay/remembrane)```\n\n```[Homepage](https://pypi.org/project/remembrane/)```"]],"e":["sqlite3","pip"],"x":"pip install remembrane"},"rev":{"n":"rev","t":"Reverse characters in each line","s":[["TAGLINE","Reverse characters in each line"],["TLDR","**Reverse** text typed into terminal (interactive mode)\n\n```rev```\n\n**Reverse** the text string \"hello\"\n\n```echo \"hello\" | rev```\n\n**Reverse** an entire file and print to stdout\n\n```rev [path/to/file]```\n\n**Use** '\\0' as a line separator (zero termination)\n\n```rev -0 [path/to/file]```"],["SYNOPSIS","**rev** [_options_] [_file_...]"],["PARAMETERS","**-0, --zero**\n> Use NUL character as line terminator instead of newline\n\n**-h, --help**\n> Display help information\n\n**-V, --version**\n> Display version information"],["DESCRIPTION","**rev** reverses the order of characters in each line of input. It reads from files specified on the command line or from standard input if no files are given. Each line is reversed independently.\n\nThe command is useful for text manipulation, creating palindrome effects, or processing data where character order needs to be inverted."],["CAVEATS","Only reverses characters within individual lines; does not reverse the order of lines themselves. Multi-byte characters may not be handled correctly in all locales. Part of the util-linux package."],["HISTORY","The **rev** command first appeared in Version 7 Unix and has been a standard utility since. It was originally created as a simple text transformation tool."],["INSTALL","```apt: sudo apt install util-linux```\n\n```dnf: sudo dnf install util-linux```\n\n```pacman: sudo pacman -S util-linux```\n\n```apk: sudo apk add util-linux-misc```\n\n```zypper: sudo zypper install util-linux```\n\n```brew: brew install util-linux```\n\n```nix: nix profile install nixpkgs#util-linux```"],["SEE ALSO","[tac](/man/tac)(1), [cat](/man/cat)(1), [cut](/man/cut)(1), [tr](/man/tr)(1)"]],"e":["tac","cat","cut","tr"],"x":"rev"},"roqet":{"n":"roqet","t":"Execute SPARQL and RDQL queries on RDF data","s":[["TAGLINE","Execute SPARQL and RDQL queries on RDF data"],["TLDR","**Execute SPARQL query**\n\n```roqet -i sparql \"[query]\" [data.rdf]```\n\n**Query file**\n\n```roqet -i sparql [query.rq] [data.rdf]```\n\n**Query with results**\n\n```roqet -r json -i sparql \"[SELECT * WHERE { ?s ?p ?o }]\" [data.rdf]```\n\n**Explain query**\n\n```roqet -e \"[query]\"```"],["SYNOPSIS","**roqet** [_options_] [_query_] [_data_]"],["PARAMETERS","**-i**, **--input** _format_\n> Query format (sparql, rdql).\n\n**-r**, **--results** _format_\n> Output format (xml, json, csv).\n\n**-e**, **--exec** _query_\n> Execute query string.\n\n**-D**, **--data** _uri_\n> Data source.\n\n**-d**, **--dump-query**\n> Show parsed query."],["DESCRIPTION","**roqet** is a SPARQL/RDQL query utility from the Redland RDF library. It executes queries against RDF data and outputs results in various formats.\n\nPart of Redland RDF libraries."],["EXAMPLES","```bash"],["Simple SPARQL query","roqet -i sparql \"SELECT * WHERE { ?s ?p ?o } LIMIT 10\" data.rdf"],["Query from file","roqet -i sparql query.rq data.ttl"],["JSON results","roqet -r json -i sparql \"SELECT * WHERE { ?s ?p ?o }\" data.rdf"],["Query remote endpoint","roqet -i sparql \"SELECT * WHERE { ?s ?p ?o } LIMIT 5\" \\\n  -D http://dbpedia.org/sparql"],["Explain query","roqet -d \"SELECT * WHERE { ?s ?p ?o }\"\n```"],["OUTPUT FORMATS","```\nxml    - SPARQL Results XML\njson   - SPARQL Results JSON\ncsv    - Comma-separated values\ntsv    - Tab-separated values\n```"],["CAVEATS","Part of Redland RDF libraries. SPARQL 1.0 support. Use rapper for parsing only."],["HISTORY","roqet is part of **Rasqal** RDF query library by **Dave Beckett** within the Redland framework."],["INSTALL","```apt: sudo apt install rasqal-utils```\n\n```dnf: sudo dnf install rasqal```\n\n```pacman: sudo pacman -S rasqal```\n\n```apk: sudo apk add rasqal```\n\n```zypper: sudo zypper install rasqal```\n\n```brew: brew install rasqal```"],["SEE ALSO","[rapper](/man/rapper)(1), [rdfproc](/man/rdfproc)(1), [redland](/man/redland)(3)"]],"e":["rapper","rdfproc","redland"],"x":"roqet -i sparql \"[query]\" [data.rdf]"},"rustup-toolchain":{"n":"rustup-toolchain","t":"Manage installed Rust toolchains","s":[["TAGLINE","Manage installed Rust toolchains"],["TLDR","**List toolchains**\n\n```rustup toolchain list```\n\n**Install toolchain**\n\n```rustup toolchain install stable```\n\n**Uninstall toolchain**\n\n```rustup toolchain uninstall nightly```\n\n**Link custom toolchain**\n\n```rustup toolchain link [name] [path]```"],["SYNOPSIS","**rustup** **toolchain** _command_ [_options_]"],["PARAMETERS","**list**\n> List installed toolchains.\n\n**install** _name_\n> Install toolchain.\n\n**uninstall** _name_\n> Remove toolchain.\n\n**link** _name_ _path_\n> Link custom toolchain.\n\n**--profile** _name_\n> Installation profile."],["DESCRIPTION","**rustup toolchain** manages Rust toolchains. Install, remove, and list toolchains. Supports stable, beta, nightly, and specific versions. Can link custom-built toolchains."],["INSTALL","```apt: sudo apt install rustup```\n\n```dnf: sudo dnf install rustup```\n\n```pacman: sudo pacman -S rustup```\n\n```zypper: sudo zypper install rustup```\n\n```brew: brew install rustup```\n\n```nix: nix profile install nixpkgs#rustup```"],["SEE ALSO","[rustup](/man/rustup)(1), [rustup-default](/man/rustup-default)(1)"]],"e":["rustup","rustup-default"],"x":"rustup toolchain list"},"shotcut":{"n":"shotcut","t":"Free cross-platform video editor","s":[["TAGLINE","Free cross-platform video editor"],["TLDR","**Open Shotcut**\n\n```shotcut```\n\n**Open project**\n\n```shotcut [project.mlt]```\n\n**Open video file**\n\n```shotcut [video.mp4]```\n\n**GPU processing**\n\n```shotcut --gpu```\n\n**Full screen**\n\n```shotcut --fullscreen```"],["SYNOPSIS","**shotcut** [_--gpu_] [_--fullscreen_] [_options_] [_file_]"],["PARAMETERS","**--gpu**\n> Use GPU processing.\n\n**--fullscreen**\n> Fill the screen with the Shotcut window.\n\n**--noupgrade**\n> Hide upgrade prompt and menu item.\n\n**--glaxnimate**\n> Run Glaxnimate instead of Shotcut.\n\n**--clear-recent**\n> Clear recent files on exit.\n\n**--appdata** _DIR_\n> Specify directory for app configuration and data.\n\n**-h**, **--help**\n> Display help.\n\n**-v**, **--version**\n> Display version information."],["DESCRIPTION","**Shotcut** is a free, open-source, cross-platform video editor that provides non-linear editing with a timeline-based workflow. It supports a wide range of audio and video formats natively through its FFmpeg backend, allowing files to be placed directly on the timeline without import or transcoding.\n\nThe editor includes a comprehensive set of video and audio filters for effects, color correction, transitions, and compositing. GPU acceleration via OpenGL is available for faster preview and rendering on supported hardware. Projects are saved in MLT XML format and can export to numerous output formats and codecs."],["CAVEATS","GPU processing varies by hardware and may not be available on all systems. Resource intensive for high-resolution or multi-track projects. Does not support command-line rendering; use **melt** for batch/scripted exports."],["HISTORY","**Shotcut** was created by **Dan Dennedy**, building on MLT framework to provide a free, open-source video editor."],["INSTALL","```apt: sudo apt install shotcut```\n\n```dnf: sudo dnf install shotcut```\n\n```pacman: sudo pacman -S shotcut```\n\n```apk: sudo apk add shotcut```\n\n```zypper: sudo zypper install shotcut```\n\n```nix: nix profile install nixpkgs#shotcut```"],["SEE ALSO","[kdenlive](/man/kdenlive)(1), [ffmpeg](/man/ffmpeg)(1), [melt](/man/melt)(1)"]],"e":["kdenlive","ffmpeg","melt"],"x":"shotcut"},"spa-json-dump":{"n":"spa-json-dump","t":"SPA JSON to standard JSON converter","s":[["TAGLINE","SPA JSON to standard JSON converter"],["TLDR","**Convert a PipeWire configuration file** to standard JSON\n\n```spa-json-dump /usr/share/pipewire/pipewire.conf```\n\n**Convert a SPA JSON file** and pretty-print with jq\n\n```spa-json-dump [path/to/file.conf] | jq .```\n\n**Read SPA JSON from stdin** and output as standard JSON\n\n```cat [path/to/file.conf] | spa-json-dump```"],["SYNOPSIS","**spa-json-dump** [_FILE_]"],["DESCRIPTION","**spa-json-dump** reads a SPA JSON file or standard input and outputs it as standard JSON. SPA JSON is a lightweight JSON variant used by PipeWire for its configuration files. The SPA JSON parser is intentionally relaxed and can handle a wide range of JSON-like syntax, including unquoted strings and comments, which are not valid in strict JSON. This tool bridges the gap by converting SPA JSON into standards-compliant JSON that can be processed by tools like **jq** or any JSON parser."],["CAVEATS","The tool has no options beyond the optional file argument. If the input is not valid SPA JSON, the output may be malformed or empty without a clear error message. Only useful for PipeWire SPA JSON files; standard JSON files will pass through unchanged."],["HISTORY","**spa-json-dump** was introduced as part of the **PipeWire** project, the modern audio and video server for Linux that replaced PulseAudio and JACK in many distributions. PipeWire uses its own SPA (Simple Plugin API) JSON format for configuration, and this tool was created to make those config files interoperable with standard JSON tooling."],["INSTALL","```apt: sudo apt install pipewire-bin```\n\n```apk: sudo apk add pipewire-spa-tools```\n\n```zypper: sudo zypper install pipewire-spa-tools```"],["SEE ALSO","[pipewire](/man/pipewire)(1), [jq](/man/jq)(1), [pw-dump](/man/pw-dump)(1)"]],"e":["pipewire","jq","pw-dump"],"x":"spa-json-dump /usr/share/pipewire/pipewire.conf"},"tauri":{"n":"tauri","t":"Build desktop apps with web technologies","s":[["TAGLINE","Build desktop apps with web technologies"],["TLDR","**Initialize a new Tauri project**\n\n```tauri init```\n\n**Start development server**\n\n```tauri dev```\n\n**Build production application**\n\n```tauri build```\n\n**Build with debug symbols**\n\n```tauri build --debug```\n\n**Build for specific target**\n\n```tauri build --target [x86_64-apple-darwin]```\n\n**Build specific bundle types**\n\n```tauri build --bundles [deb,appimage]```\n\n**Generate icons**\n\n```tauri icon [icon.png]```\n\n**Display version**\n\n```tauri --version```"],["SYNOPSIS","**tauri** [_command_] [_options_]\n\n**cargo tauri** [_command_] [_options_]"],["COMMANDS","**init**: Initialize a new Tauri project in existing directory.\n\n**dev**: Start development server with hot-reload.\n\n**build**: Build production application bundle.\n\n**icon**: Generate icons from source image.\n\n**info**: Show environment diagnostic information.\n\n**add**: Add a Tauri plugin to the project.\n\n**plugin**: Plugin development commands.\n\n**signer**: Signing utilities for updates.\n\n**completions**: Generate shell completions."],["PARAMETERS","**-v**, **--verbose**\n> Enable verbose logging.\n\n**-d**, **--debug**\n> Build with debug profile.\n\n**-t**, **--target** _target_\n> Target triple (e.g., x86_64-unknown-linux-gnu).\n\n**-f**, **--features** _features_\n> Comma-separated Rust features to enable.\n\n**-b**, **--bundles** _bundles_\n> Bundle types: deb, rpm, appimage, msi, app, dmg, updater.\n\n**-r**, **--runner** _runner_\n> Build tool to use (default: cargo).\n\n**-c**, **--config** _file_\n> Path to tauri.conf.json or config JSON string.\n\n**-h**, **--help**\n> Display help information."],["DESCRIPTION","**tauri** is the command-line interface for building Tauri applications. Tauri is a framework for building tiny, fast desktop applications with web technologies (HTML, CSS, JavaScript) and a Rust backend.\n\nThe CLI manages the complete application lifecycle: project initialization, development with hot-reload, production builds, and distribution packaging. It integrates with the frontend build process and handles platform-specific bundling.\n\nApplications can be built for Windows (MSI, NSIS), macOS (DMG, app bundle), and Linux (DEB, RPM, AppImage). The framework produces small binaries by using the system webview rather than bundling a browser engine.\n\nInstall via Cargo: `cargo install tauri-cli` or npm: `npm add -D @tauri-apps/cli`"],["CAVEATS","Requires Rust toolchain and platform-specific dependencies (WebKitGTK on Linux, Xcode on macOS, WebView2 on Windows). Cross-compilation has limitations. Universal macOS builds require both ARM and Intel targets installed."],["HISTORY","**Tauri** was created as a lightweight alternative to Electron, using system webviews instead of bundled Chromium. Version 1.0 was released in 2022, with version 2.0 adding mobile platform support. The project is maintained by the Tauri Program within the Commons Conservancy."],["SEE ALSO","[cargo](/man/cargo)(1), [npm](/man/npm)(1), [electron](/man/electron)(1)"]],"e":["cargo","npm","electron"],"x":"tauri init"},"trunk":{"n":"trunk","t":"Rust WebAssembly application bundler","s":[["TAGLINE","Rust WebAssembly application bundler"],["TLDR","**Build the project**\n\n```trunk build```\n\n**Build for release** with optimizations\n\n```trunk build --release```\n\n**Serve with hot reload**\n\n```trunk serve```\n\n**Serve on a specific port and address**\n\n```trunk serve --port [3000] --address [0.0.0.0]```\n\n**Serve with a backend proxy**\n\n```trunk serve --proxy-backend=http://localhost:[9000]/api/```\n\n**Watch for changes and rebuild**\n\n```trunk watch```\n\n**Clean build artifacts**\n\n```trunk clean```"],["SYNOPSIS","**trunk** _command_ [_--release_] [_options_]"],["PARAMETERS","**build**\n> Build the project, compiling Rust to WASM and bundling assets.\n\n**serve**\n> Start development server with hot reloading.\n\n**watch**\n> Watch for file changes and rebuild automatically.\n\n**clean**\n> Remove build artifacts (dist directory).\n\n**--release**\n> Enable release optimizations including WASM size reduction.\n\n**--public-url** _URL_\n> Public URL prefix for asset paths.\n\n**--port** _PORT_\n> Port for the dev server (default: 8080).\n\n**--address** _ADDR_\n> Address to bind the dev server to.\n\n**--proxy-backend** _URL_\n> Proxy requests to a backend server. The URI path of the URL determines the proxy path.\n\n**--proxy-rewrite** _URI_\n> Alternative URI on which to listen for proxy requests."],["DESCRIPTION","**Trunk** is a build tool for Rust WebAssembly applications that handles compilation, bundling, and serving. It compiles Rust code to WebAssembly, processes assets like CSS and JavaScript, and generates the HTML and JavaScript glue code needed to run WASM modules in the browser.\n\nThe development server provides hot reloading, automatically rebuilding and refreshing the browser when source files change. Release builds apply optimizations including WASM size reduction for production deployment.\n\nTrunk integrates with wasm-bindgen for JavaScript interop and works with frameworks like Yew, Leptos, and Seed. Configuration is managed through a Trunk.toml file in the project root."],["CONFIGURATION","**Trunk.toml**\n> Project configuration file specifying build options, proxy settings, and asset pipeline rules."],["CAVEATS","Rust/WASM specific. wasm-bindgen based. Trunk.toml config."],["HISTORY","**Trunk** was created for building and bundling Rust WebAssembly applications."],["INSTALL","```dnf: sudo dnf install trunk```\n\n```pacman: sudo pacman -S trunk```\n\n```apk: sudo apk add trunk```\n\n```brew: brew install trunk```\n\n```nix: nix profile install nixpkgs#trunk```"],["SEE ALSO","[wasm-pack](/man/wasm-pack)(1), [cargo](/man/cargo)(1)"]],"e":["wasm-pack","cargo"],"x":"trunk build"},"update-alternatives":{"n":"update-alternatives","t":"Manage default command symlinks for alternatives","s":[["TAGLINE","Manage default command symlinks for alternatives"],["TLDR","**Add** a symbolic link\n\n```sudo update-alternatives --install [path/to/symlink] [command_name] [path/to/binary] [priority]```\n\n**Configure** a symbolic link interactively\n\n```sudo update-alternatives --config [java]```\n\n**Remove** a symbolic link\n\n```sudo update-alternatives --remove [java] [/opt/java/jdk1.8.0_102/bin/java]```\n\n**Display** information about a specified command\n\n```update-alternatives --display [java]```\n\n**Display** all commands and their current selection\n\n```update-alternatives --get-selections```"],["SYNOPSIS","**update-alternatives** [_option_] _command_"],["PARAMETERS","**--install _link_ _name_ _path_ _priority_**\n> Add an alternative for name with specified priority\n\n**--config _name_**\n> Interactively configure alternatives for name\n\n**--remove _name_ _path_**\n> Remove a specific alternative\n\n**--display _name_**\n> Display information about alternatives for name\n\n**--get-selections**\n> List all configured alternatives\n\n**--set _name_ _path_**\n> Set the alternative non-interactively\n\n**--auto _name_**\n> Switch to automatic mode (highest priority wins)\n\n**--list _name_**\n> List all alternatives for a name"],["DESCRIPTION","**update-alternatives** maintains symbolic links that determine default commands when multiple versions of a program are installed. It manages a directory of links in **/etc/alternatives/** that point to actual program binaries.\n\nCommon uses include managing multiple Java versions, editors, or compilers. Higher priority numbers are preferred in automatic mode."],["CAVEATS","Requires root privileges to modify. Priority numbers determine automatic selection. Manual mode persists until explicitly changed. Debian/Ubuntu specific; Red Hat uses **alternatives** command."],["INSTALL","```apt: sudo apt install dpkg```\n\n```dnf: sudo dnf install dpkg```\n\n```pacman: sudo pacman -S dpkg```\n\n```apk: sudo apk add dpkg```\n\n```zypper: sudo zypper install dpkg```\n\n```brew: brew install dpkg```\n\n```nix: nix profile install nixpkgs#dpkg```"],["SEE ALSO","[alternatives](/man/alternatives)(8), [ln](/man/ln)(1)"]],"e":["alternatives","ln"],"x":"sudo update-alternatives --install [path/to/symlink] [command_name] [path/to/binary] [priority]"},"uptime":{"n":"uptime","t":"Show system uptime and load averages","s":[["TAGLINE","Show system uptime and load averages"],["TLDR","**Show system uptime** and load averages\n\n```uptime```\n\n**Show uptime in pretty format**\n\n```uptime -p```\n\n**Show when the system was booted**\n\n```uptime -s```"],["SYNOPSIS","**uptime** [_options_]"],["PARAMETERS","**-p**, **--pretty**\n> Show uptime in human-readable format\n\n**-s**, **--since**\n> Show date and time when system started (yyyy-mm-dd HH:MM:SS)\n\n**-h**, **--help**\n> Display help message\n\n**-V**, **--version**\n> Display version information"],["OUTPUT FORMAT","Default output shows:\n- Current time\n- System uptime (days, hours, minutes)\n- Number of logged-in users\n- Load averages for 1, 5, and 15 minutes\n\nExample: `14:30:05 up 7 days, 3:42, 2 users, load average: 0.15, 0.20, 0.18`"],["DESCRIPTION","**uptime** displays how long the system has been running, the number of users currently logged in, and the system load averages for the past 1, 5, and 15 minutes.\n\nLoad averages represent the average number of processes waiting for CPU time. On a single-CPU system, a load of 1.0 means the CPU is fully utilized. On a 4-CPU system, a load of 4.0 indicates full utilization. Values above the CPU count suggest processes are waiting for CPU time.\n\nThe command reads from **/proc/uptime** for system uptime and **/var/run/utmp** for user information. It is part of the procps-ng package."],["CAVEATS","Load averages include processes waiting for I/O, not just CPU-bound processes, which can make interpretation less straightforward. The user count reflects logged-in sessions, including multiple terminals for the same user. System time resets on reboot; for historical uptime tracking, use external monitoring tools."],["HISTORY","The uptime command originated in early Unix systems, providing a quick way to check system health. It became part of standard Unix utilities and is included in all Unix-like systems. The load average concept was introduced in BSD Unix. On Linux, uptime is part of the **procps** (later **procps-ng**) package that provides utilities for examining the **/proc** filesystem."],["INSTALL","```apt: sudo apt install procps```\n\n```dnf: sudo dnf install procps-ng```\n\n```pacman: sudo pacman -S procps-ng```\n\n```apk: sudo apk add procps-ng```\n\n```zypper: sudo zypper install procps```\n\n```brew: brew install procps```\n\n```nix: nix profile install nixpkgs#procps```"],["SEE ALSO","[w](/man/w)(1), [top](/man/top)(1), [who](/man/who)(1), [last](/man/last)(1)"]],"e":["w","top","who","last"],"x":"uptime"},"vpnc":{"n":"vpnc","t":"Cisco-compatible IPSec VPN client","s":[["TAGLINE","Cisco-compatible IPSec VPN client"],["TLDR","**Connect** with config file\n\n```sudo vpnc [config_file]```\n\n**Disconnect** VPN\n\n```sudo vpnc-disconnect```"],["SYNOPSIS","**vpnc** [_OPTIONS_] [_CONFIG_FILE_]"],["PARAMETERS","**--gateway** _HOST_\n> VPN gateway address\n\n**--id** _ID_\n> IPSec group ID\n\n**--secret** _SECRET_\n> IPSec group secret\n\n**--username** _USER_\n> Username for authentication"],["DESCRIPTION","**vpnc** is a VPN client for Cisco 3000 VPN Concentrator, IOS, and PIX devices. It establishes IPSec connections compatible with Cisco VPN hardware.\n\nConfiguration files typically contain gateway address, group ID, group secret, and user credentials."],["CAVEATS","Requires root privileges. Configuration files may contain sensitive credentials. Some features may not work with all Cisco VPN devices."],["INSTALL","```apt: sudo apt install vpnc```\n\n```dnf: sudo dnf install vpnc```\n\n```pacman: sudo pacman -S vpnc```\n\n```apk: sudo apk add vpnc```\n\n```zypper: sudo zypper install vpnc```\n\n```nix: nix profile install nixpkgs#vpnc```"],["SEE ALSO","[openconnect](/man/openconnect)(8), [openvpn](/man/openvpn)(8)"]],"e":["openconnect","openvpn"],"x":"sudo vpnc [config_file]"},"wajig":{"n":"wajig","t":"Simplified Debian package management wrapper","s":[["TAGLINE","Simplified Debian package management wrapper"],["TLDR","**Update** package list\n\n```wajig update```\n\n**Install** package\n\n```wajig install [package]```\n\n**Purge** package\n\n```wajig purge [package]```\n\n**Daily** upgrade\n\n```wajig daily-upgrade```\n\nShow package **sizes**\n\n```wajig sizes```\n\nList **versions**\n\n```wajig versions```\n\nList **upgradable**\n\n```wajig toupgrade```\n\nShow **dependents**\n\n```wajig dependents [package]```"],["SYNOPSIS","**wajig** _COMMAND_ [_ARGS_...]"],["COMMANDS","**update**\n> Update package lists\n\n**install**\n> Install or upgrade package\n\n**purge**\n> Remove package and configuration\n\n**daily-upgrade**\n> Update and dist-upgrade\n\n**sizes**\n> Display installed package sizes\n\n**versions**\n> List versions for all packages\n\n**toupgrade**\n> List upgradable packages\n\n**dependents**\n> Show packages depending on given package"],["DESCRIPTION","**wajig** is a simplified all-in-one-place system support tool for Debian-based systems. It wraps apt, dpkg, and other tools into a consistent interface with easy-to-remember commands.\n\nThe tool provides a unified interface for common package management tasks."],["CAVEATS","Debian-based systems only. Requires root for most operations. Wraps standard package tools."],["SEE ALSO","[apt](/man/apt)(8), [dpkg](/man/dpkg)(1), [aptitude](/man/aptitude)(8)"]],"e":["apt","dpkg","aptitude"],"x":"wajig update"},"xdg-user-dir":{"n":"xdg-user-dir","t":"Find an XDG user directory path","s":[["TAGLINE","Find an XDG user directory path"],["TLDR","Show **home** directory (default with no argument)\n\n```xdg-user-dir```\n\nShow the **Downloads** directory path\n\n```xdg-user-dir DOWNLOAD```\n\nShow the **Documents** directory path\n\n```xdg-user-dir DOCUMENTS```\n\nShow the **Desktop** directory path\n\n```xdg-user-dir DESKTOP```"],["SYNOPSIS","**xdg-user-dir** [_DIRECTORY_]"],["PARAMETERS","**DESKTOP**\n> Desktop directory\n\n**DOWNLOAD**\n> Downloads directory\n\n**DOCUMENTS**\n> Documents directory\n\n**MUSIC**\n> Music directory\n\n**PICTURES**\n> Pictures directory\n\n**VIDEOS**\n> Videos directory\n\n**TEMPLATES**\n> Templates directory\n\n**PUBLICSHARE**\n> Public share directory\n\n**--help**\n> Display help and exit\n\n**--version**\n> Show version information and exit"],["DESCRIPTION","**xdg-user-dir** looks up the current path for an XDG user directory as defined in the user-dirs.dirs configuration file. These are the standard directories for common content types like documents, music, and downloads.\n\nThe paths are configured in **~/.config/user-dirs.dirs** and can be customized per user. The **XDG_CONFIG_HOME** environment variable determines where the configuration file is located."],["CAVEATS","Returns home directory if the requested directory is not configured or if an unknown directory name is given. Directory paths may not exist on the filesystem."],["INSTALL","```apt: sudo apt install xdg-user-dirs```\n\n```dnf: sudo dnf install xdg-user-dirs```\n\n```pacman: sudo pacman -S xdg-user-dirs```\n\n```apk: sudo apk add xdg-user-dirs```\n\n```zypper: sudo zypper install xdg-user-dirs```\n\n```nix: nix profile install nixpkgs#xdg-user-dirs```"],["SEE ALSO","[xdg-user-dirs-update](/man/xdg-user-dirs-update)(1), [xdg-open](/man/xdg-open)(1), [xdg-mime](/man/xdg-mime)(1)"]],"e":["xdg-user-dirs-update","xdg-open","xdg-mime"],"x":"xdg-user-dir"},"xscreensaver-settings":{"n":"xscreensaver-settings","t":"Configure xscreensaver preferences","s":[["TAGLINE","Configure xscreensaver preferences"],["TLDR","**Open screensaver settings**\n\n```xscreensaver-settings```\n\n**Open settings with debug output**\n\n```xscreensaver-settings --debug```"],["SYNOPSIS","**xscreensaver-settings** [**--display** _host:display.screen_] [**--debug**]"],["PARAMETERS","**--display** _host:display.screen_\n> The X display to use for both the settings window and the controlled xscreensaver daemon\n\n**--debug**\n> Print diagnostics to stderr"],["DESCRIPTION","**xscreensaver-settings** is the graphical configuration tool for xscreensaver. It allows enabling/disabling individual screensavers, setting idle timeout, lock screen options, and display preferences.\n\nThe interface has two main tabs: Display Modes for selecting and previewing screensavers, and Advanced for timing, locking, and power management settings. Changes are saved to ~/.xscreensaver.\n\nKey settings include idle timeout before blanking, lock screen delay, cycle time between screensavers, and whether to blank all monitors or run different screensavers on each."],["SETTINGS","**Blank After**: Idle time before screen blanks.\n\n**Cycle After**: Time before switching screensavers.\n\n**Lock Screen After**: Grace period before password required.\n\n**Power Management**: Monitor power saving options.\n\n**Mode**: Disable, Blank Only, One Random, or Random."],["CAVEATS","Changes saved to ~/.xscreensaver file. GUI not available under Wayland (edit file manually). Requires xscreensaver daemon running. Previously called xscreensaver-demo."],["HISTORY","**xscreensaver-settings** (formerly xscreensaver-demo) is the configuration GUI for xscreensaver, created by Jamie Zawinski in 1992. Xscreensaver became the most popular X11 screensaver, with hundreds of display modules developed over the years."],["INSTALL","```apt: sudo apt install xscreensaver```\n\n```dnf: sudo dnf install xscreensaver```\n\n```pacman: sudo pacman -S xscreensaver```\n\n```apk: sudo apk add xscreensaver```\n\n```zypper: sudo zypper install xscreensaver```\n\n```nix: nix profile install nixpkgs#xscreensaver```"],["SEE ALSO","[xscreensaver](/man/xscreensaver)(1), [xscreensaver-command](/man/xscreensaver-command)(1)"]],"e":["xscreensaver","xscreensaver-command"],"x":"xscreensaver-settings"},"zint":{"n":"zint","t":"Multi-format barcode generator","s":[["TAGLINE","Multi-format barcode generator"],["TLDR","**Generate QR code**\n\n```zint -b 58 -d \"[data]\" -o [output.png]```\n\n**Generate Code 128 barcode**\n\n```zint -b 20 -d \"[data]\" -o [output.png]```\n\n**Generate EAN-13 barcode**\n\n```zint -b 13 -d \"[1234567890123]\"```\n\n**Set output format**\n\n```zint -b 58 -d \"[data]\" -o [output.svg]```\n\n**Scale barcode size**\n\n```zint -b 58 -d \"[data]\" --scale [3]```\n\n**Generate with human-readable text**\n\n```zint -b 20 -d \"[data]\" --notext=0```"],["SYNOPSIS","**zint** [_options_]"],["PARAMETERS","**-b** _type_\n> Barcode type (number or name)\n\n**-d** _data_\n> Data to encode\n\n**-o** _file_\n> Output filename\n\n**--scale** _factor_\n> Scale factor for output\n\n**--height** _value_\n> Barcode height (for 1D barcodes)\n\n**--fg** _color_\n> Foreground color (hex)\n\n**--bg** _color_\n> Background color (hex)\n\n**--notext**\n> Disable human-readable text\n\n**-i** _file_\n> Read data from file\n\n**--batch**\n> Process batch data from file\n\n**--dump**\n> Output hex dump instead of image\n\n**-e**\n> Display symbol information"],["BARCODE TYPES","Common types: **1** (Code 11), **13** (EAN-13), **20** (Code 128), **29** (UPC-A), **55** (PDF417), **58** (QR Code), **71** (Data Matrix), **92** (Aztec Code), **142** (MaxiCode)\n\nUse **zint -t** to list all supported types."],["DESCRIPTION","**zint** is a barcode generator supporting over 50 barcode types. It creates 1D barcodes (Code 128, EAN, UPC), 2D barcodes (QR Code, Data Matrix, PDF417), and specialized formats (postal codes, pharmaceutical).\n\nOutput formats include PNG, BMP, GIF, PCX, SVG, EPS, and EMF. The tool can also output raw hex dumps for debugging or custom rendering.\n\nzint includes both a command-line interface and a GUI (zint-qt). A library (libzint) is available for integration into applications."],["CAVEATS","Different barcode types have different data requirements. Some accept only numbers, others have length restrictions.\n\nQR codes and other 2D barcodes have error correction levels that trade capacity for redundancy.\n\nNot all barcode scanners support all symbologies. Verify compatibility with target readers."],["INSTALL","```apt: sudo apt install zint```\n\n```dnf: sudo dnf install zint```\n\n```pacman: sudo pacman -S zint```\n\n```apk: sudo apk add zint```\n\n```zypper: sudo zypper install zint```\n\n```brew: brew install zint```"],["SEE ALSO","[qrencode](/man/qrencode)(1), [barcode](/man/barcode)(1), [dmtxwrite](/man/dmtxwrite)(1)"]],"e":["qrencode","barcode","dmtxwrite"],"x":"zint -b 58 -d \"[data]\" -o [output.png]"},"zlib-flate":{"n":"zlib-flate","t":"Raw zlib compression and decompression utility","s":[["TAGLINE","Raw zlib compression and decompression utility"],["TLDR","**Compress data**\n\n```zlib-flate -compress < [input] > [output.zz]```\n\n**Decompress data**\n\n```zlib-flate -uncompress < [input.zz] > [output]```\n\n**Compress with specific level (1=fastest, 9=best)**\n\n```zlib-flate -compress=[9] < [input] > [output.zz]```\n\n**Decompress a raw zlib stream from a PDF**\n\n```zlib-flate -uncompress < [stream.bin] > [decoded.txt]```"],["SYNOPSIS","**zlib-flate** **-compress**[=_level_] | **-uncompress**"],["PARAMETERS","**-compress**\n> Compress data.\n\n**-uncompress**\n> Decompress data.\n\n**=**_LEVEL_\n> Compression level (1-9)."],["DESCRIPTION","**zlib-flate** performs raw zlib compression and decompression. It reads from standard input and writes to standard output, making it suitable for use in shell pipelines.\n\nThe tool uses raw zlib compression format, which distinguishes it from utilities like **gzip** that add wrapper headers. Compression levels from 1 (fastest) to 9 (best compression) can be specified with the **-compress=** option.\n\nzlib-flate is included as a utility in the **QPDF** package. It is primarily provided as a debugging tool for working with raw zlib streams such as those found inside PDF files. It should not be used as a general purpose compression tool; use **gzip** instead for that."],["CAVEATS","Raw zlib format only, no gzip or other wrapper headers. Part of the qpdf package. Not intended as a general purpose compression tool."],["HISTORY","**zlib-flate** is included with **QPDF**, providing raw zlib deflate compression and decompression."],["INSTALL","```apt: sudo apt install qpdf```\n\n```dnf: sudo dnf install qpdf```\n\n```pacman: sudo pacman -S qpdf```\n\n```apk: sudo apk add qpdf```\n\n```zypper: sudo zypper install qpdf```\n\n```brew: brew install qpdf```\n\n```nix: nix profile install nixpkgs#qpdf```"],["SEE ALSO","[qpdf](/man/qpdf)(1), [gzip](/man/gzip)(1), [zcat](/man/zcat)(1), [pigz](/man/pigz)(1)"]],"e":["qpdf","gzip","zcat","pigz"],"x":"zlib-flate -compress < [input] > [output.zz]"}});
