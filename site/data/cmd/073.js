window.LCL_BUCKET(73,{"ak":{"n":"ak","t":"Manage the authentik identity provider","s":[["TAGLINE","Manage the authentik identity provider"],["TLDR","**Start the server**\n\n```ak server```\n\n**Run database migrations**\n\n```ak migrate```\n\n**Create a superuser**\n\n```ak createsuperuser```\n\n**Export configuration**\n\n```ak export_blueprint```\n\n**Import a blueprint**\n\n```ak import_blueprint [blueprint.yaml]```\n\n**Generate a recovery link valid for a number of minutes**\n\n```ak create_recovery_key [minutes] [username]```\n\n**Show version**\n\n```ak version```\n\n**Run a worker process**\n\n```ak worker```"],["SYNOPSIS","**ak** _command_ [_options_]"],["COMMANDS","**server**\n> Start the authentik web server.\n\n**worker**\n> Start a background worker process.\n\n**migrate**\n> Apply database migrations.\n\n**createsuperuser**\n> Create an administrative user interactively.\n\n**create_recovery_key** _minutes_ _user_\n> Generate an account recovery link valid for the given number of minutes.\n\n**dump_config**\n> Print the full effective configuration as YAML.\n\n**export_blueprint**\n> Export current configuration as blueprint YAML.\n\n**import_blueprint** _file_\n> Import configuration from blueprint file.\n\n**version**\n> Display version information.\n\n**repair**\n> Run diagnostic and repair routines.\n\n**shell**\n> Open interactive Python shell with authentik context.\n\n**dbshell**\n> Open database shell."],["PARAMETERS","**--bind** _address_\n> Address for server to listen on.\n\n**--help**\n> Display help information."],["DESCRIPTION","**ak** is the command-line interface for authentik, an open-source identity provider. It manages the authentik server, workers, database operations, and configuration import/export.\n\nThe **server** command starts the web interface and API server. Background tasks like email sending and LDAP synchronization require a **worker** process running separately.\n\nBlueprints provide declarative configuration management. Export configurations with **export_blueprint** for version control or migration, and apply them with **import_blueprint**.\n\nFor account recovery when users are locked out, **create_recovery_key** generates a time-limited recovery URL. The first argument is the validity in minutes and the second is the username. This requires access to the server command line.\n\nauthentik supports various authentication protocols including SAML, OAuth2/OIDC, LDAP, SCIM, and proxy authentication for applications that don't support SSO natively."],["CONFIGURATION","**/etc/authentik/config.yml**\n> Main authentik configuration file for database, secret key, email, and logging settings.\n\n**blueprints/**\n> Directory containing declarative YAML blueprints for configuration-as-code."],["CAVEATS","Requires Python environment with authentik installed. Database must be configured and accessible. Most administrative tasks are performed through the web interface; CLI is primarily for server operations and emergency recovery."],["HISTORY","authentik was created by **Jens Langhammer** and first released in **2020** as a modern alternative to older identity providers. Built with Django and designed for containerized deployments, it provides comprehensive identity management features. The project gained popularity as a self-hosted alternative to commercial identity providers, offering features like application proxying, multi-factor authentication, and extensive protocol support."],["SEE ALSO","[authelia](/man/authelia)(1), [keycloak](/man/keycloak)(1), [vault](/man/vault)(1)"],["RESOURCES","```[Source code](https://github.com/goauthentik/authentik)```\n\n```[Homepage](https://goauthentik.io/)```\n\n```[Documentation](https://docs.goauthentik.io/)```"]],"e":["authelia","keycloak","vault"],"x":"ak server"},"alien":{"n":"alien","t":"Convert between Linux package formats.","s":[["TAGLINE","Convert between Linux package formats."],["TLDR","Convert a file to **Debian format** (.deb)\n\n```sudo alien -d [path/to/file]```\n\nConvert a file to **Red Hat format** (.rpm)\n\n```sudo alien -r [path/to/file]```\n\nConvert a file to **Slackware format** (.tgz)\n\n```sudo alien -t [path/to/file]```\n\nConvert to Debian format and **install** on the system\n\n```sudo alien -d -i [path/to/file]```\n\nConvert **keeping the original version** number\n\n```sudo alien -k [path/to/file]```"],["SYNOPSIS","**alien** [_--to-deb_] [_--to-rpm_] [_--to-tgz_] [_--to-slp_] [_options_] _file_ [...]"],["DESCRIPTION","**alien** is a program that converts between Red Hat rpm, Debian deb, Stampede slp, Slackware tgz, and Solaris pkg file formats. It enables users to convert packages from one Linux distribution format to another and install them."],["PARAMETERS","**-d, --to-deb**\n> Create Debian packages (default)\n\n**-r, --to-rpm**\n> Create RPM packages\n\n**-t, --to-tgz**\n> Create TGZ (Slackware) packages\n\n**--to-slp**\n> Create SLP (Stampede) packages\n\n**-p, --to-pkg**\n> Create Solaris pkg packages\n\n**-i, --install**\n> Install generated packages automatically\n\n**-g, --generate**\n> Create temporary directory for manual package building\n\n**-k, --keep-version**\n> Don't increment version numbers\n\n**-c, --scripts**\n> Convert installation/removal scripts\n\n**--fixperms**\n> Sanitize file permissions (Debian only)\n\n**-v, --verbose**\n> Display executed commands\n\n**-T, --test**\n> Test generated packages with lintian\n\n**--bump**=_N_\n> Increment version number by N instead of 1\n\n**-h, --help**\n> Display usage summary\n\n**--version**\n> Display alien version"],["CAVEATS","Converted packages may not work perfectly as distribution-specific dependencies and scripts may not translate correctly between formats."],["HISTORY","Created to help users install software from other Linux distributions when native packages are not available."],["INSTALL","```dnf: sudo dnf install alien```\n\n```apk: sudo apk add alien```"],["SEE ALSO","[dpkg](/man/dpkg)(1), [rpm](/man/rpm)(8), [debtap](/man/debtap)(1)"]],"e":["dpkg","rpm","debtap"],"x":"sudo alien -d [path/to/file]"},"alltray":{"n":"alltray","t":"Dock any application into the system tray.","s":[["TAGLINE","Dock any application into the system tray."],["TLDR","**Click-mode:** run alltray and click on a window to dock it\n\n```alltray```\n\n**Dock a specific application** to the system tray\n\n```alltray [application]```\n\n**Dock an application** with a custom icon\n\n```alltray -i [path/to/icon.png] [application]```\n\n**Dock an application** and keep it visible after start\n\n```alltray -s [application]```\n\n**Dock an application** visible on all workspaces\n\n```alltray --sticky [application]```\n\n**Create a keyboard shortcut** to toggle the window\n\n```alltray -k [Modifier:]Key [application]```"],["SYNOPSIS","**alltray** [_OPTIONS_] [_APPLICATION_]"],["PARAMETERS","**-h**, **--help**\n> Show summary of options.\n\n**-v**, **--version**\n> Show version of program.\n\n**-d**, **--debug**\n> Show debug messages.\n\n**-s**, **--show**\n> Do not hide window after start.\n\n**-i**, **--icon** _FILE_\n> Use a PNG image as the tray icon.\n\n**-l**, **--large_icons**\n> Allow icons larger than 24x24 pixels.\n\n**-st**, **--sticky**\n> Make the window visible on all workspaces.\n\n**-g**, **--geometry** _GEOMETRY_\n> Set initial window position (see man X for format).\n\n**-k**, **--key** _[Modifier:]Key_\n> Create a keyboard shortcut to toggle window visibility.\n\n**-nm**, **--nominimize**\n> Close button closes the application instead of minimizing to tray.\n\n**-conf**, **--configure**\n> Show KDE configuration dialog."],["DESCRIPTION","**AllTray** docks any application into the system tray, even applications that do not natively support tray icons. This allows you to minimize applications like terminals, email clients, or media players to the system tray instead of the taskbar.\n\nA key feature is that clicking the window's close button minimizes the application to the system tray rather than closing it. This behavior can be changed with the **--nominimize** option. AllTray works with GNOME, KDE, XFCE 4, Fluxbox, WindowMaker, and other window managers that support the system tray specification.\n\nWhen run without arguments, AllTray enters click-mode where you can click on any window to dock it. Press **c** to abort click-mode."],["CAVEATS","AllTray depends on the X Window System and standard system tray protocols. It may not work with Wayland-based desktop environments. Some applications with complex window hierarchies may not dock properly. The system tray must be present in your desktop environment for AllTray to function."],["HISTORY","AllTray was created by Jochen Baier in **2005** to provide a universal solution for docking applications to the system tray on Linux desktops. It became popular as a way to keep applications running in the background without cluttering the taskbar, especially before many applications added native tray support."],["INSTALL","```aur: yay -S alltray```"],["SEE ALSO","[kdocker](/man/kdocker)(1), [trayer](/man/trayer)(1), [stalonetray](/man/stalonetray)(1)"],["RESOURCES","```[Source code](https://github.com/mbt/AllTray)```"]],"e":["kdocker","trayer","stalonetray"],"x":"alltray"},"az-version":{"n":"az-version","t":"Display Azure CLI version information","s":[["TAGLINE","Display Azure CLI version information"],["TLDR","**Show** Azure CLI version\n\n```az version```\n\nShow version in **JSON** format\n\n```az version -o json```\n\nShow version in **table** format\n\n```az version --output table```"],["SYNOPSIS","**az version** [**-o**|**--output** _format_]"],["PARAMETERS","**-o**, **--output** _format_\n> Output format. Accepted values: **json**, **jsonc**, **none**, **table**, **tsv**, **yaml**, **yamlc**. Default: **json**."],["DESCRIPTION","**az version** displays version information for the Azure CLI and all installed extensions. It shows the core CLI version, Python version, and individual extension versions.\n\nThe command is useful for troubleshooting and verifying installations."],["OUTPUT","Information displayed:\n- Azure CLI core version\n- Python version and platform\n- Installed extensions and their versions"],["WORKFLOW","```bash"],["Show all version info","az version"],["JSON output for parsing","az version -o json"],["Check specific extension","az version -o json | jq '.extensions.\"some-extension\"'\n```"],["CAVEATS","Output format may vary between Azure CLI versions. Extension versions are listed separately from the core CLI version. The default output format is JSON."],["HISTORY","**az version** has been available since Azure CLI 2.0 in **2017** as a diagnostic command for version information."],["INSTALL","```dnf: sudo dnf install azure-cli```\n\n```pacman: sudo pacman -S azure-cli```\n\n```zypper: sudo zypper install azure-cli```\n\n```brew: brew install azure-cli```\n\n```nix: nix profile install nixpkgs#azure-cli```"],["SEE ALSO","[az](/man/az)(1), [az-upgrade](/man/az-upgrade)(1), [az-extension](/man/az-extension)(1), [az-login](/man/az-login)(1)"]],"e":["az","az-upgrade","az-extension","az-login"],"x":"az version"},"btmgmt":{"n":"btmgmt","t":"Low-level Bluetooth adapter management tool","s":[["TAGLINE","Low-level Bluetooth adapter management tool"],["TLDR","**Start interactive mode**\n\n```btmgmt```\n\n**List Bluetooth adapters**\n\n```btmgmt info```\n\n**Use specific adapter**\n\n```btmgmt -i [hci0] info```\n\n**Power on adapter**\n\n```btmgmt power on```\n\n**Power off adapter**\n\n```btmgmt power off```\n\n**Start discovery**\n\n```btmgmt find```\n\n**Enable monitor mode**\n\n```btmgmt -m```\n\n**Run command with timeout**\n\n```btmgmt -t [10] find```"],["SYNOPSIS","**btmgmt** [_options_] [_command_]"],["DESCRIPTION","**btmgmt** is an interactive Bluetooth management tool that issues commands to the kernel using the Bluetooth Management socket. It provides low-level control of Bluetooth adapter settings, discovery, and pairing functionality.\n\nThe tool is part of the BlueZ Bluetooth stack and uses the management interface available since Linux kernel 3.4."],["PARAMETERS","**-i, --index** _adapter_\n> Specify adapter index (e.g., hci0)\n\n**-m, --monitor**\n> Enable monitor output\n\n**-t, --timeout** _seconds_\n> Timeout for non-interactive mode\n\n**-v, --version**\n> Display version\n\n**--init-script** _file_\n> Run commands from init script\n\n**-h, --help**\n> Display help"],["COMMANDS","**info**\n> Show adapter information\n\n**power** _on|off_\n> Control adapter power state\n\n**find**\n> Start device discovery\n\n**pair** _address_\n> Pair with device\n\n**unpair** _address_\n> Remove pairing\n\n**disconnect** _address_\n> Disconnect device\n\n**con**\n> List connections"],["CAVEATS","Some commands require net-admin capability or root privileges. This is a low-level interface meant for bluetoothd; applications should use D-Bus APIs instead. Direct use may cause unexpected behavior with the Bluetooth daemon."],["INSTALL","```apt: sudo apt install bluez```\n\n```dnf: sudo dnf install bluez```\n\n```pacman: sudo pacman -S bluez```\n\n```apk: sudo apk add bluez-btmgmt```\n\n```zypper: sudo zypper install bluez```\n\n```brew: brew install bluez```\n\n```nix: nix profile install nixpkgs#bluez```"],["SEE ALSO","[bluetoothctl](/man/bluetoothctl)(1), [hciconfig](/man/hciconfig)(1), [hcitool](/man/hcitool)(1), [bluetoothd](/man/bluetoothd)(8)"],["RESOURCES","```[Source code](https://github.com/bluez/bluez)```"]],"e":["bluetoothctl","hciconfig","hcitool","bluetoothd"],"x":"btmgmt"},"cargo-generate-lockfile":{"n":"cargo-generate-lockfile","t":"Create or regenerate Cargo.lock for a Rust project","s":[["TAGLINE","Create or regenerate Cargo.lock for a Rust project"],["TLDR","**Generate lock file**\n\n```cargo generate-lockfile```\n\n**Regenerate with latest versions**\n\n```cargo generate-lockfile```\n\n**Generate with specific manifest**\n\n```cargo generate-lockfile --manifest-path [path/to/Cargo.toml]```\n\n**Generate with custom lockfile path**\n\n```cargo generate-lockfile --lockfile-path [/tmp/Cargo.lock]```"],["SYNOPSIS","**cargo generate-lockfile** [_options_]"],["DESCRIPTION","**cargo generate-lockfile** creates or rebuilds Cargo.lock for the current package or workspace. If lockfile exists, rebuilds with latest available versions of all packages."],["PARAMETERS","**--manifest-path** _path_\n> Path to Cargo.toml\n\n**--lockfile-path** _path_\n> Custom lockfile path (must end with Cargo.lock)\n\n**--locked**\n> Assert exact same dependencies as original lockfile\n\n**--offline**\n> Don't access network\n\n**--frozen**\n> Equivalent to --locked and --offline\n\n**-v**, **--verbose**\n> Verbose output (use twice for very verbose)\n\n**-q**, **--quiet**\n> Suppress log messages"],["BEHAVIOR","Creates new Cargo.lock if none exists. Rebuilds existing lockfile with newest compatible versions. Use cargo update for more control over version updates."],["CAVEATS","Ensures reproducible builds by locking dependency versions. Useful in CI pipelines for deterministic builds. Consider committing Cargo.lock for applications, not libraries."],["INSTALL","```apt: sudo apt install cargo```\n\n```dnf: sudo dnf install cargo```\n\n```apk: sudo apk add cargo```\n\n```zypper: sudo zypper install cargo```\n\n```nix: nix profile install nixpkgs#cargo```"],["SEE ALSO","[cargo](/man/cargo)(1), [cargo-update](/man/cargo-update)(1), [cargo-fetch](/man/cargo-fetch)(1)"],["RESOURCES","```[Source code](https://github.com/rust-lang/cargo)```\n\n```[Documentation](https://doc.rust-lang.org/cargo/commands/cargo-generate-lockfile.html)```"]],"e":["cargo","cargo-update","cargo-fetch"],"x":"cargo generate-lockfile"},"cargo-pkgid":{"n":"cargo-pkgid","t":"Print a fully qualified package specification","s":[["TAGLINE","Print a fully qualified package specification"],["TLDR","**Show package ID for current package**\n\n```cargo pkgid```\n\n**Show package ID for a dependency**\n\n```cargo pkgid [serde]```\n\n**Show package ID for a specific version of a dependency**\n\n```cargo pkgid [serde@1.0.0]```\n\n**Show package ID using a specific Cargo.toml**\n\n```cargo pkgid --manifest-path [path/to/Cargo.toml] [serde]```\n\n**Show package ID from a URL**\n\n```cargo pkgid [https://github.com/rust-lang/crates.io-index#serde]```"],["SYNOPSIS","**cargo pkgid** [_options_] [_spec_]"],["DESCRIPTION","**cargo pkgid** prints the fully qualified package ID specification for a package in the dependency graph. The output includes the source URL, package name, and version in a canonical format that can be used with other Cargo commands like `cargo update -p`.\n\nThis command is primarily useful when multiple versions of the same package exist in the dependency tree and disambiguation is needed. It accepts partial specifications such as just the name or name with version, and resolves them against the lockfile. The Cargo.lock file must exist and dependencies must be fetched before this command can be used."],["PARAMETERS","**-p**, **--package** _spec_\n> Package to query\n\n**--manifest-path** _path_\n> Path to Cargo.toml\n\n**--locked**\n> Assert that the exact same dependencies and versions are used as when the existing Cargo.lock was generated\n\n**--offline**\n> Prevent Cargo from accessing the network\n\n**--frozen**\n> Equivalent to specifying both --locked and --offline\n\n**--color** _when_\n> Control colored output: auto (default), always, or never\n\n**-v**, **--verbose**\n> Verbose output. Specify twice for very verbose output.\n\n**-q**, **--quiet**\n> Do not print cargo log messages"],["SPEC FORMATS","**name**\n> bitflags\n\n**name@version**\n> bitflags@1.0.4\n\n**url**\n> https://github.com/rust-lang/cargo\n\n**url#version**\n> https://github.com/rust-lang/cargo#0.33.0\n\n**url#name**\n> https://github.com/rust-lang/crates.io-index#bitflags\n\n**url#name@version**\n> https://github.com/rust-lang/cargo#crates-io@0.21.0"],["FULL SPEC FORMAT","```\nregistry+https://github.com/rust-lang/crates.io-index#regex@1.4.3\n```"],["CAVEATS","Errors if specification is ambiguous. Add version qualifier to disambiguate multiple versions. Requires lockfile and fetched dependencies."],["INSTALL","```apt: sudo apt install cargo```\n\n```dnf: sudo dnf install cargo```\n\n```apk: sudo apk add cargo```\n\n```zypper: sudo zypper install cargo```\n\n```nix: nix profile install nixpkgs#cargo```"],["SEE ALSO","[cargo](/man/cargo)(1), [cargo-metadata](/man/cargo-metadata)(1)"]],"e":["cargo","cargo-metadata"],"x":"cargo pkgid"},"cgminer":{"n":"cgminer","t":"multi-threaded cryptocurrency miner for ASIC devices","s":[["TAGLINE","multi-threaded cryptocurrency miner for ASIC devices"],["TLDR","**Mine Bitcoin** using a pool with username and password\n\n```cgminer -o [stratum+tcp://pool:port] -u [username] -p [password]```\n\n**Solo mine** to a local Bitcoin node\n\n```cgminer -o [http://localhost:8332] -u [rpcuser] -p [rpcpassword] --btc-address [your_address]```\n\n**Run in benchmark mode** without submitting shares\n\n```cgminer --benchmark```\n\n**Enable the API** on a specific port\n\n```cgminer -o [stratum+tcp://pool:port] -u [username] -p [password] --api-listen --api-port [4028]```\n\n**Load configuration from file**\n\n```cgminer -c [path/to/config.json]```\n\n**Mine with multiple pools** using load balancing\n\n```cgminer -o [pool1:port] -u [user1] -p [pass1] -o [pool2:port] -u [user2] -p [pass2] --load-balance```"],["SYNOPSIS","**cgminer** [_-DdlmpPqUTouOchnV_] [_-o url_] [_-u user_] [_-p pass_]"],["PARAMETERS","**-o**, **--url** _URL_\n> URL for bitcoin JSON-RPC server (pool or local node)\n\n**-u**, **--user** _USERNAME_\n> Username for JSON-RPC server\n\n**-p**, **--pass** _PASSWORD_\n> Password for JSON-RPC server\n\n**-O**, **--userpass** _USER:PASS_\n> Username:password pair for server\n\n**-c**, **--config** _FILE_\n> Load a JSON-formatted configuration file\n\n**--benchmark**\n> Run in benchmark mode producing no actual shares\n\n**--shares** _NUMBER_\n> Quit after mining the specified number of shares\n\n**--no-submit-stale**\n> Do not submit shares if they are detected as stale\n\n**--btc-address** _ADDRESS_\n> Bitcoin address for solo mining coinbase rewards\n\n**--balance**\n> Change pool strategy to even share balance\n\n**--load-balance**\n> Change pool strategy to quota based balance\n\n**--rotate** _MINUTES_\n> Rotate between pools every N minutes\n\n**--failover-only**\n> Do not leak work to backup pools when primary is active\n\n**--api-listen**\n> Enable the miner API (disabled by default)\n\n**--api-port** _PORT_\n> Port number for API (default: 4028)\n\n**--api-allow** _[G:]IP[/PREFIX]_\n> Allow API access only to the given addresses\n\n**-D**, **--debug**\n> Enable debug output\n\n**-l**, **--log** _INTERVAL_\n> Interval in seconds between log output (default: 5)\n\n**-q**, **--quiet**\n> Disable logging output, display status and errors only\n\n**-T**, **--text-only**\n> Disable ncurses formatted screen output\n\n**--temp-cutoff** _TEMP_\n> Temperature where devices will be disabled\n\n**--usb** _DEVICES_\n> USB device selection (e.g., 1:2,1:3 or BAS:1,BFL:1)\n\n**-h**, **--help**\n> Print help message and exit\n\n**-V**, **--version**\n> Display version and exit"],["DESCRIPTION","**cgminer** is a multi-threaded multi-pool miner for Bitcoin and other SHA256d cryptocurrencies. Originally supporting CPU, GPU, FPGA, and ASIC mining, modern versions focus exclusively on ASIC devices as GPU mining is no longer economically viable for Bitcoin.\n\nThe miner supports multiple pools with configurable failover, load-balancing, and rotation strategies. It connects using the stratum protocol or JSON-RPC and submits proof-of-work solutions. The built-in API allows remote monitoring and control of mining operations.\n\nCGMiner supports various ASIC hardware including Avalon, AntMiner, BitFury, Cointerra, Drillbit, HashFast, Icarus, and Klondike devices. On Linux, direct USB support requires no additional drivers, though udev rules may be needed for non-root access."],["CONFIGURATION","**cgminer.conf**\n> JSON configuration file with pool URLs, credentials, device settings, and mining parameters. Loaded with -c flag or auto-loaded from the working directory."],["CAVEATS","GPU mining support has been removed in recent versions as it is no longer profitable for Bitcoin. On Linux, USB device permissions may require adding the user to the **plugdev** group or installing udev rules from the cgminer distribution. High hash rates generate significant heat and power consumption. Pool URLs must use the correct protocol prefix (**http://** for solo mining, **stratum+tcp://** for pool mining)."],["HISTORY","CGMiner was created by **Con Kolivas** (ckolivas) and first released in **2011**. It began as a fork of the CPU miner **cpuminer** by Jeff Garzik. The name stands for **Con's GPU Miner**, though it later expanded to support FPGAs and ASICs. It became one of the most widely used Bitcoin mining programs during the GPU and early ASIC mining eras. BFGMiner was later forked from CGMiner in 2012 with a focus on FPGA/ASIC modularity."],["INSTALL","```nix: nix profile install nixpkgs#cgminer```"],["SEE ALSO","[bfgminer](/man/bfgminer)(1), [xmrig](/man/xmrig)(1)"]],"e":["bfgminer","xmrig"],"x":"cgminer -o [stratum+tcp://pool:port] -u [username] -p [password]"},"coderabbit":{"n":"coderabbit","t":"AI-powered code review tool for the terminal","s":[["TAGLINE","AI-powered code review tool for the terminal"],["TLDR","**Review** all changes interactively\n\n```coderabbit```\n\n**Review** all changes with plain text output\n\n```coderabbit --plain```\n\n**Review** only uncommitted changes\n\n```coderabbit --type uncommitted```\n\n**Compare** current branch against a base branch\n\n```coderabbit --base [develop]```\n\n**Provide** custom review instructions\n\n```coderabbit --config [./claude.md]```\n\n**Authenticate** with CodeRabbit\n\n```cr auth login```\n\n**Update** CLI to latest version\n\n```coderabbit update```"],["SYNOPSIS","**coderabbit** [_review_] [_options_]\n**cr** [_review_] [_options_]"],["PARAMETERS","**--plain**\n> Output detailed feedback in plain text format.\n\n**--prompt-only**\n> Show minimal output optimized for AI agents.\n\n**-t**, **--type** _type_\n> Review type: all, committed, uncommitted (default: all).\n\n**-c**, **--config** _files..._\n> Additional instruction files for the AI.\n\n**--base** _branch_\n> Base branch for comparison.\n\n**--base-commit** _commit_\n> Base commit hash on the current branch for comparison.\n\n**--cwd** _path_\n> Set working directory (must contain a Git repository).\n\n**--api-key** _key_\n> API key for usage-based access.\n\n**--self-hosted**\n> Enable self-hosted authentication mode.\n\n**--no-color**\n> Disable colored terminal output."],["SUBCOMMANDS","**review**\n> Run AI-driven code review (default when no subcommand given).\n\n**auth login**\n> Authenticate with CodeRabbit account.\n\n**auth logout**\n> Log out from CodeRabbit.\n\n**auth status**\n> Show current authentication status.\n\n**auth org**\n> Switch between organizations.\n\n**update**\n> Check for and install the latest CLI version."],["DESCRIPTION","**CodeRabbit CLI** brings AI-powered code reviews directly into the terminal. It analyzes code changes -- whether uncommitted, staged, committed, or across branches -- and provides line-by-line review comments identifying potential issues such as security vulnerabilities, performance problems, code smells, logical errors, and missed unit tests. **cr** is a short alias for **coderabbit**.\n\nThe tool operates in three output modes: interactive mode (default) with a browsable TUI, plain text mode (**--plain**) with detailed feedback for scripting, and prompt-only mode (**--prompt-only**) for integration with AI coding agents."],["CAVEATS","Requires an initialized Git repository in the working directory. Authentication is recommended for enhanced analysis but the tool can be used without it (with rate limits). Free tier has rate limits; Pro plan provides higher limits. Available on macOS and Linux natively; Windows is supported via WSL. The CLI tool is closed-source."],["HISTORY","**CodeRabbit** was founded in **2023** by **Harjot Gill**. The company initially launched as a GitHub/GitLab bot for reviewing pull requests. The **CLI tool was launched on September 16, 2025**, extending the platform to support pre-commit reviews directly in the terminal. CodeRabbit is headquartered in El Dorado Hills, California."],["INSTALL","```aur: yay -S coderabbit```"],["SEE ALSO","[gh](/man/gh)(1), [git](/man/git)(1), [semgrep](/man/semgrep)(1)"]],"e":["gh","git","semgrep"],"x":"coderabbit"},"diff-pdf":{"n":"diff-pdf","t":"visual PDF comparison tool","s":[["TAGLINE","visual PDF comparison tool"],["TLDR","**Compare two PDFs** visually\n\n```diff-pdf [file1.pdf] [file2.pdf]```\n\n**Output difference** to new PDF\n\n```diff-pdf --output-diff=[diff.pdf] [file1.pdf] [file2.pdf]```\n\n**Show visual comparison**\n\n```diff-pdf --view [file1.pdf] [file2.pdf]```\n\n**Mark differences** with a rectangle in the output\n\n```diff-pdf --mark-differences --output-diff=[diff.pdf] [file1.pdf] [file2.pdf]```\n\n**Set a per-channel color tolerance**\n\n```diff-pdf --channel-tolerance=[10] [file1.pdf] [file2.pdf]```"],["SYNOPSIS","**diff-pdf** [_options_] _file1.pdf_ _file2.pdf_"],["PARAMETERS","**--output-diff** _file_\n> Output the visual difference to a PDF file.\n\n**--view**\n> Open the comparison in an interactive viewer window.\n\n**-m**, **--mark-differences**\n> Additionally mark differences on left-side pages with a rectangle.\n\n**-g**, **--grayscale**\n> Render pages in grayscale so only differences appear in color.\n\n**--channel-tolerance** _N_\n> Allow up to N (0-255) difference per color channel before flagging a pixel.\n\n**--per-page-pixel-tolerance** _N_\n> Allow up to N differing pixels per page before reporting the page as different.\n\n**--dpi** _N_\n> Rasterization resolution in DPI (default: 300).\n\n**-s**, **--skip-identical**\n> In the output PDF, skip pages that are identical.\n\n**-v**, **--verbose**\n> Verbose output."],["DESCRIPTION","**diff-pdf** compares two PDF files visually, rendering each page and performing pixel-by-pixel comparison. Unlike text-based diff tools, it captures visual differences including formatting, fonts, images, and layout changes that wouldn't be visible in a text comparison.\n\nThe tool rasterizes each page from both PDFs and compares the resulting images. It can generate a difference PDF highlighting changes in color, or open an interactive viewer for side-by-side comparison. The --channel-tolerance and --per-page-pixel-tolerance options allow small rendering variations to be ignored.\n\nThis is particularly useful for comparing versions of documents where content may be identical but visual presentation differs, or for quality assurance workflows verifying that document generation produces consistent output. The tool returns exit code 0 if files are identical, 1 if different, making it suitable for automated testing."],["INSTALL","```apt: sudo apt install diff-pdf-wx```\n\n```dnf: sudo dnf install diff-pdf```\n\n```pacman: sudo pacman -S diff-pdf```\n\n```apk: sudo apk add diff-pdf```\n\n```zypper: sudo zypper install diff-pdf```\n\n```brew: brew install diff-pdf```\n\n```nix: nix profile install nixpkgs#diff-pdf```"],["SEE ALSO","[pdftk](/man/pdftk)(1), [diff](/man/diff)(1), [cpdf](/man/cpdf)(1)"],["RESOURCES","```[Source code](https://github.com/vslavik/diff-pdf)```\n\n```[Homepage](https://vslavik.github.io/diff-pdf/)```"]],"e":["pdftk","diff","cpdf"],"x":"diff-pdf [file1.pdf] [file2.pdf]"},"doggo":{"n":"doggo","t":"modern DNS client with encrypted protocol support","s":[["TAGLINE","modern DNS client with encrypted protocol support"],["TLDR","Query **A records** with the system resolver\n\n```doggo [example.com]```\n\nQuery a **specific record type** against a chosen resolver\n\n```doggo [example.com] [MX] @[9.9.9.9]```\n\nUse **DNS over HTTPS**\n\n```doggo [example.com] @https://[cloudflare-dns.com/dns-query]```\n\nUse **DNS over TLS**\n\n```doggo [example.com] @tls://[1.1.1.1]```\n\nQuery **every common record type** at once\n\n```doggo [example.com] --any```\n\nOutput **JSON** for scripting\n\n```doggo [example.com] --json```\n\n**Reverse lookup** an IP address, short form\n\n```doggo --reverse [8.8.8.8] --short```\n\nRun the query **from another country** via the Globalping network\n\n```doggo [example.com] --gp-from [Germany] --gp-limit [2]```"],["SYNOPSIS","**doggo** [_query options_] [_arguments_...]\n\nArguments may be given free-form: doggo recognises a hostname, a record type, a class, and an **@**_resolver_ in any order."],["PARAMETERS","**-q**, **--query**=_HOSTNAME_\n> Hostname to query the DNS records for.\n\n**-t**, **--type**=_TYPE_\n> Type of the DNS record (A, AAAA, MX, NS, TXT, ...).\n\n**-n**, **--nameserver**=_ADDR_\n> Address of a specific nameserver to send queries to.\n\n**-c**, **--class**=_CLASS_\n> Network class of the DNS record (IN, CH, HS).\n\n**-x**, **--reverse**\n> Reverse lookup of an IPv4 or IPv6 address; sets type PTR and class IN.\n\n**--any**\n> Query all supported record types (A, AAAA, CNAME, MX, NS, PTR, SOA, SRV, TXT, CAA).\n\n**-A**, **--authoritative**\n> Find the domain's zone via its SOA and query the delegated authoritative nameservers.\n\n**--strategy**=_STRATEGY_\n> How to pick among nameservers: **all**, **random**, **first**, or **internal**.\n\n**--ndots**=_INT_\n> ndots value; taken from `/etc/resolv.conf` for the system nameserver, otherwise 1.\n\n**--search**\n> Use the search list from `resolv.conf` (default true; **--search=false** disables it).\n\n**--timeout**=_DURATION_\n> Resolver timeout, e.g. `5s`, `400ms`, `1m`.\n\n**-4**, **--ipv4** / **-6**, **--ipv6**\n> Restrict queries to IPv4 or IPv6 only.\n\n**--tls-hostname**=_HOSTNAME_\n> Hostname used for certificate verification when the DoT nameserver is given as an IP.\n\n**--skip-hostname-verification**\n> Skip TLS hostname verification for DoT lookups.\n\n**--aa**, **--ad**, **--cd**, **--rd**, **--do**, **--z**\n> Set the Authoritative Answer, Authenticated Data, Checking Disabled, Recursion Desired (on by default), DNSSEC OK, and Z header flags.\n\n**--nsid**, **--cookie**, **--padding**, **--ede**\n> EDNS options: request the Name Server Identifier, a DNS Cookie, EDNS padding, or Extended DNS Errors.\n\n**--ecs**=_SUBNET_\n> Send an EDNS Client Subnet such as `192.0.2.0/24`, for geo-aware responses.\n\n**--bufsize**=_BYTES_\n> EDNS UDP buffer size (512-65535); setting it enables EDNS. Defaults to 1232 when EDNS is on.\n\n**-J**, **--json**\n> Format the output as JSON.\n\n**--short**\n> Short output: response section only.\n\n**--color**\n> Colourised output (default true; **--color=false** disables it).\n\n**--time**\n> Show how long the server took to respond.\n\n**--debug**\n> Enable debug logging.\n\n**--gp-from**=_LOCATION_, **--gp-limit**=_INT_\n> Run the query from Globalping probes in a given location, limiting the number of probes.\n\n**completions** [_bash_|_zsh_|_fish_]\n> Print the shell completion script for the given shell."],["TRANSPORT","The resolver is selected with a URL-style scheme after **@**. Plain UDP is used when no scheme is given.\n\n**@udp://1.1.1.1**\n> UDP query to port 53 (the default).\n\n**@tcp://1.1.1.1**\n> TCP query to port 53.\n\n**@tls://1.1.1.1**\n> DNS over TLS (DoT) to port 853.\n\n**@https://cloudflare-dns.com/dns-query**\n> DNS over HTTPS (DoH).\n\n**@quic://**\n> DNS over QUIC (DoQ).\n\n**@sdns://**\n> DNSCrypt or DoH via a DNS stamp."],["DESCRIPTION","**doggo** is a command-line DNS client written in Go, positioned as a friendlier `dig`. It prints a tabular, colourised answer by default and can emit JSON for scripting.\n\nBeyond plain UDP and TCP it speaks the modern encrypted transports: DNS over HTTPS, DNS over TLS, DNS over QUIC, and DNSCrypt. The resolver is chosen by prefixing it with a URL scheme, which keeps one flagless syntax for every protocol.\n\ndoggo reads `/etc/resolv.conf` for the system nameserver, ndots value, and search list, so bare hostnames resolve the way they would for any other program. Header flags, EDNS options, and DNSSEC can all be toggled from the command line, and **--gp-from** runs the same query from Globalping probes around the world to compare answers between regions."],["CAVEATS","Encrypted transports only work against resolvers that support them. **--short** hides the question, authority, and additional sections, which is convenient but drops information you often need when debugging. Colour is on by default, so pass **--color=false** when parsing output with anything other than **--json**. Globalping queries are executed by a third-party service rather than from your own machine."],["HISTORY","doggo was created by **Karan Sharma** and first released in **2020**. It was inspired by **dog**, the Rust DNS client, but written in Go so that it ships as a single static binary. It has since grown well past its inspiration with DNSCrypt, DNS over QUIC, Globalping integration, and a web frontend, and unlike dog it remains actively maintained."],["INSTALL","```pacman: sudo pacman -S doggo```\n\n```apk: sudo apk add doggo```\n\n```zypper: sudo zypper install doggo```\n\n```brew: brew install doggo```\n\n```nix: nix profile install nixpkgs#doggo```"],["SEE ALSO","[dig](/man/dig)(1), [dog](/man/dog)(1), [drill](/man/drill)(1), [nslookup](/man/nslookup)(1), [host](/man/host)(1)"],["RESOURCES","```[Source code](https://github.com/mr-karan/doggo)```\n\n```[Homepage](https://doggo.mrkaran.dev)```\n\n```[Documentation](https://doggo.mrkaran.dev/docs/)```"]],"e":["dig","dog","drill","nslookup","host"],"x":"doggo [example.com]"},"dos2unix":{"n":"dos2unix","t":"convert DOS/Windows line endings to Unix","s":[["TAGLINE","convert DOS/Windows line endings to Unix"],["TLDR","**Convert** file line endings\n\n```dos2unix [path/to/file]```\n\nCreate **copy** with Unix line endings\n\n```dos2unix -n [path/to/file] [path/to/new_file]```\n\nDisplay **file information**\n\n```dos2unix -i [path/to/file]```\n\nHandle **BOM** (keep/add/remove)\n\n```dos2unix --[keep-bom|add-bom|remove-bom] [path/to/file]```\n\nConvert **in place, preserving the timestamp**\n\n```dos2unix -k [path/to/file]```\n\nConvert **many files at once**\n\n```dos2unix [file1] [file2] [file3]```\n\nForce conversion of a file **detected as binary**\n\n```dos2unix -f [path/to/file]```"],["SYNOPSIS","**dos2unix** [_options_] [_file_...]\n\n**dos2unix** [_options_] **-n** _infile_ _outfile_ ..."],["DESCRIPTION","**dos2unix** converts text files from DOS/Windows format (CRLF line endings) to Unix format (LF line endings). DOS/Windows systems use carriage return and line feed (\\\\r\\\\n) to mark line endings, while Unix-like systems use only line feed (\\\\n).\n\nLine ending mismatches cause problems in scripts, compilation errors, or rendering issues in text editors. This tool resolves these issues by normalizing line endings to the Unix standard. It handles byte order marks (BOM) commonly found in UTF-8 files from Windows.\n\nThe tool operates in-place by default, modifying the original file, but can create new output files preserving the original. It intelligently detects file formats and can provide information about a file's current line ending format before conversion."],["PARAMETERS","**-o**, **--oldfile** _file_...\n> Old file mode: convert each file and overwrite it. This is the default.\n\n**-n**, **--newfile** _infile_ _outfile_...\n> New file mode: convert _infile_ and write the result to _outfile_, leaving the original untouched.\n\n**-i**[_FLAGS_], **--info**[=_FLAGS_] _file_...\n> Report each file's line-break counts, BOM, and text/binary status. No conversion is done.\n\n**-c**, **--convmode** _mode_\n> Conversion mode: **ascii** (the default), **7bit**, **iso**, or **mac**.\n\n**-k**, **--keepdate**\n> Give the output file the same timestamp as the input file.\n\n**-s**, **--safe**\n> Skip binary files. This is the default.\n\n**-f**, **--force**\n> Convert a file even if it looks binary.\n\n**-F**, **--follow-symlink**\n> Follow symbolic links and convert their targets.\n\n**-b**, **--keep-bom**\n> Keep a Byte Order Mark: if the input has one, write one to the output.\n\n**-m**, **--add-bom**\n> Write a Byte Order Mark (UTF-8 by default) to the output.\n\n**-r**, **--remove-bom**\n> Do not write a Byte Order Mark to the output.\n\n**-e**, **--add-eol**\n> Add a line break to the last line if it does not have one.\n\n**-l**, **--newline**\n> Also add an extra newline, turning each single line break into a double one.\n\n**-u**, **--keep-utf16**\n> Keep the input file's original UTF-16 encoding rather than converting to UTF-8.\n\n**-ul**, **--assume-utf16le** / **-ub**, **--assume-utf16be**\n> Assume the input is UTF-16 little-endian or big-endian when there is no BOM to say so.\n\n**-q**, **--quiet**\n> Quiet mode: suppress all warnings and messages.\n\n**-v**, **--verbose**\n> Report extra detail about Byte Order Marks and the conversion performed.\n\n**--allow-chown**\n> Permit a change of file ownership in old file mode.\n\n**-V**, **--version**\n> Display version information and exit."],["CAVEATS","The default is to modify files in place, so keep a copy or use **-n** if the original matters. In old file mode dos2unix writes a new file and renames it over the old one, so the target's ownership can change when running as another user; that is why **--allow-chown** must be given explicitly. Binary files are skipped by default (**-s**), and forcing conversion with **-f** on a real binary will corrupt it, so check with **-i** first when unsure. Note that the ASCII conversion mode only rewrites line breaks; it does not transcode character sets, and **iso** mode assumes a specific DOS code page that may not match your file."],["HISTORY","dos2unix has existed in one form or another since the late 1980s, when moving text between DOS and Unix was routine. The version shipped by every current Linux distribution is **Erwin Waterlander's** rewrite, begun in **2009**, which added Unicode and UTF-16 support, BOM handling, and the mac conversion mode to what had been a very small utility. It is distributed with a companion **unix2dos** that performs the reverse conversion, and the two are usually the same binary invoked under different names."],["INSTALL","```apt: sudo apt install dos2unix```\n\n```dnf: sudo dnf install dos2unix```\n\n```pacman: sudo pacman -S dos2unix```\n\n```apk: sudo apk add dos2unix```\n\n```zypper: sudo zypper install dos2unix```\n\n```brew: brew install dos2unix```\n\n```nix: nix profile install nixpkgs#dos2unix```"],["SEE ALSO","[unix2dos](/man/unix2dos)(1), [unix2mac](/man/unix2mac)(1), [file](/man/file)(1), [iconv](/man/iconv)(1), [tr](/man/tr)(1)"],["RESOURCES","```[Source code](https://sourceforge.net/projects/dos2unix/)```\n\n```[Homepage](https://dos2unix.sourceforge.io/)```"]],"e":["unix2dos","unix2mac","file","iconv","tr"],"x":"dos2unix [path/to/file]"},"dotnet-ef":{"n":"dotnet-ef","t":"Entity Framework Core database migration tools","s":[["TAGLINE","Entity Framework Core database migration tools"],["TLDR","**Add migration**\n\n```dotnet ef migrations add [MigrationName]```\n\n**Update database**\n\n```dotnet ef database update```\n\n**List migrations**\n\n```dotnet ef migrations list```\n\n**Remove last migration**\n\n```dotnet ef migrations remove```\n\n**Generate an idempotent SQL script** for a DBA to run\n\n```dotnet ef migrations script --idempotent -o [script.sql]```\n\n**Roll back to an earlier migration** (0 reverts everything)\n\n```dotnet ef database update [MigrationName]```\n\n**Scaffold a DbContext** from an existing database\n\n```dotnet ef dbcontext scaffold \"[connection_string]\" [Microsoft.EntityFrameworkCore.SqlServer] -o [Models]```\n\n**Check for model changes** not yet captured in a migration\n\n```dotnet ef migrations has-pending-model-changes```\n\n**Bundle migrations** into a standalone executable for deployment\n\n```dotnet ef migrations bundle --self-contained -r [linux-x64]```"],["SYNOPSIS","**dotnet ef** _area_ _command_ [_arguments_] [_options_]\n\nInstall it first, as a global tool, and add the design package to the project:\n\n```dotnet tool install --global dotnet-ef```\n\n```dotnet add package Microsoft.EntityFrameworkCore.Design```"],["COMMANDS","**migrations add** _NAME_\n> Add a new migration. **-o** sets the output directory (defaults to `Migrations`).\n\n**migrations list**\n> List available migrations. **--no-connect** avoids touching the database.\n\n**migrations remove**\n> Remove the last migration. **-f**, **--force** also reverts it in the database.\n\n**migrations script** [_FROM_] [_TO_]\n> Generate SQL from migrations. **-i**, **--idempotent** produces a script safe to run against a database at any migration; **--no-transactions** omits transaction statements.\n\n**migrations bundle**\n> Build a self-contained executable that applies the migrations, for use in a deployment pipeline.\n\n**migrations has-pending-model-changes**\n> Exit non-zero if the model has changed since the last migration. Added in EF Core 8.\n\n**database update** [_MIGRATION_]\n> Apply migrations up to _MIGRATION_, or to the latest if omitted. The special value **0** reverts every migration.\n\n**database drop**\n> Delete the database. **-f** skips the prompt, **--dry-run** only reports what would go.\n\n**dbcontext scaffold** _CONNECTION_ _PROVIDER_\n> Reverse-engineer a DbContext and entity types from an existing database.\n\n**dbcontext list** / **dbcontext info**\n> List the DbContext types in the project, or describe one.\n\n**dbcontext optimize**\n> Generate a compiled model, and optionally precompiled queries, to cut startup time.\n\n**dbcontext script**\n> Generate a SQL script from the model, bypassing migrations entirely."],["PARAMETERS","**-c**, **--context** _DBCONTEXT_\n> Which DbContext to use. Required when the project has more than one.\n\n**-p**, **--project** _PATH_\n> The *target* project, where files are written. Defaults to the current directory.\n\n**-s**, **--startup-project** _PATH_\n> The *startup* project, which the tools build and run to read configuration and connection strings. Defaults to the current directory.\n\n**--connection** _STRING_\n> Override the connection string from `AddDbContext` or `OnConfiguring`.\n\n**--framework** _TFM_ / **--configuration** _CONFIG_ / **--runtime** _RID_\n> Select the target framework, build configuration, and runtime when the project offers a choice.\n\n**--no-build**\n> Do not build the project first. Only safe when the build is already up to date.\n\n**-d**, **--data-annotations**\n> Configure the scaffolded model with attributes where possible instead of the fluent API.\n\n**-t**, **--table** _NAME_ / **--schema** _NAME_\n> Restrict scaffolding to specific tables or schemas. Repeat the option for each one.\n\n**--use-database-names**\n> Keep table and column names exactly as they are, rather than reshaping them to C# conventions.\n\n**--json** / **-v**, **--verbose**\n> Emit JSON output, or verbose diagnostics."],["DESCRIPTION","**dotnet ef** is the design-time tooling for Entity Framework Core. It is not part of the SDK: it installs separately as a .NET tool, and the project it operates on must reference `Microsoft.EntityFrameworkCore.Design`.\n\nIts central job is **migrations**. When the C# model changes, `migrations add` diffs the model against a snapshot of the previous one and writes a migration class describing the difference as `Up` and `Down` operations. `database update` then applies pending migrations, recording each in an `__EFMigrationsHistory` table so the same command is safe to re-run.\n\nThe tools work against two projects, which trips people up. The **target project** is where files are written, and the **startup project** is the one the tools actually build and execute, because EF has to run your application's configuration code to discover the DbContext and its connection string. They are usually the same, but they diverge when the model lives in a class library referenced by a web or console app.\n\n**dbcontext scaffold** goes the other direction, generating a DbContext and entity classes from a database that already exists. Every table needs a primary key for an entity type to be produced from it."],["CAVEATS","Migration scripts, not `database update`, are what belong in a production deployment. `--idempotent` scripts, or a `migrations bundle` executable, can be reviewed by a DBA and run without the SDK or your source code present on the server.\n\nScaffolding is a one-shot generator, not a sync: re-running it with **--force** overwrites the generated files and discards any edits you made, so treat the output as a starting point and keep customisations in partial classes or a separate configuration.\n\nThe tool version and the EF Core runtime version should match; a `dotnet-ef` older than the packages in the project will fail with confusing errors. `database drop` deletes the database outright. Finally, a migration's `Down` method is generated automatically and is often the weakest part of a migration: verify it before relying on a rollback, particularly for anything that drops a column."],["HISTORY","**Entity Framework Core** was released with .NET Core in **2016** as a ground-up rewrite of Entity Framework, cross-platform and considerably leaner. The CLI tooling has been reworked more than once: EF Core 1.x drove it through a `dotnet-ef` package referenced by each project, which proved awkward, and it was later reshaped into the global tool used today. `migrations bundle` and `has-pending-model-changes` are recent additions aimed squarely at CI/CD pipelines, where applying migrations from a developer's machine was never a good idea."],["INSTALL","```nix: nix profile install nixpkgs#dotnet-ef```"],["SEE ALSO","[dotnet](/man/dotnet)(1), [dotnet-build](/man/dotnet-build)(1), [dotnet-publish](/man/dotnet-publish)(1), [sqlcmd](/man/sqlcmd)(1)"],["RESOURCES","```[Source code](https://github.com/dotnet/efcore)```\n\n```[Documentation](https://learn.microsoft.com/en-us/ef/core/cli/dotnet)```"]],"e":["dotnet","dotnet-build","dotnet-publish","sqlcmd"],"x":"dotnet ef migrations add [MigrationName]"},"dracut":{"n":"dracut","t":"initramfs image generator","s":[["TAGLINE","initramfs image generator"],["TLDR","Generate initramfs for **current kernel**\n\n```dracut```\n\nGenerate and **overwrite** existing\n\n```dracut -f```\n\nGenerate for **specific kernel**\n\n```dracut --kver [kernel_version]```\n\n**List** available modules\n\n```dracut --list-modules```\n\n**Rebuild every** installed kernel's initramfs\n\n```dracut --regenerate-all -f```\n\nBuild a **generic** image that boots on any hardware\n\n```dracut -f --no-hostonly [/boot/initramfs-rescue.img]```\n\nShow which **kernel command line** the image expects\n\n```dracut --print-cmdline```\n\nAdd an extra **module and driver**\n\n```dracut -f --add [crypt] --add-drivers [nvme]```"],["SYNOPSIS","**dracut** [_options_] [_image_] [_kernel-version_]"],["DESCRIPTION","**dracut** builds an initramfs: the small root filesystem the kernel unpacks into memory and runs before the real root is available. Its job is to load whatever is needed to *find* and mount the real root, which may mean assembling an LVM volume group, unlocking a LUKS device, starting a RAID array, or bringing up the network for an NFS or iSCSI root.\n\nWhere older generators used hand-written scripts, dracut is event-driven: it populates the image with **udev** rules and systemd units, and the boot proceeds as devices appear rather than by polling in a fixed order. The image content comes from **dracut modules**, small shells of scripts and dependencies under `/usr/lib/dracut/modules.d/`, which are selected automatically from what the running system needs and can be forced in or out with **--add** and **--omit**.\n\nThe most consequential choice is **host-only** mode. With **-H** the image contains only the drivers and modules this specific machine needs to boot, which is small and fast; most distributions default to it. A **--no-hostonly** image includes far more and will boot on hardware other than the one that built it, which is what you want for a rescue image or for an image built inside a container or chroot."],["PARAMETERS","**-f**, **--force**\n> Overwrite an existing image.\n\n**--kver** _VERSION_\n> Build for a specific kernel version rather than the running one.\n\n**--regenerate-all**\n> Rebuild the initramfs for every installed kernel.\n\n**-a**, **--add** _MODULES_\n> Add dracut modules on top of the automatically selected set.\n\n**-o**, **--omit** _MODULES_\n> Leave dracut modules out.\n\n**--force-add** _MODULES_\n> Add modules even if they would otherwise be omitted.\n\n**-m**, **--modules** _MODULES_\n> Use exactly these dracut modules and nothing else.\n\n**--add-drivers** _MODULES_ / **--omit-drivers** _MODULES_\n> Add or exclude specific *kernel* modules (drivers).\n\n**-d**, **--drivers** _MODULES_\n> Include exactly these kernel modules.\n\n**-H**, **--hostonly** / **-N**, **--no-hostonly**\n> Build a host-specific image, or a generic one that boots on other hardware.\n\n**--list-modules** / **-M**, **--show-modules**\n> List the available dracut modules, or print the ones being included during a build.\n\n**--print-cmdline**\n> Print the kernel command line parameters this system's root setup requires.\n\n**--kernel-cmdline** _PARAMS_\n> Embed default kernel command line parameters in the image.\n\n**-I**, **--install** _FILES_\n> Install extra files into the image, resolving their library dependencies.\n\n**--include** _SRC_ _TARGET_\n> Copy a file or directory into the image at a given path.\n\n**--fstab**\n> Use `/etc/fstab` rather than `/proc/self/mountinfo` to work out the mounts.\n\n**--uefi**\n> Produce a UEFI executable combining kernel, initramfs, and command line.\n\n**--gzip** / **--xz** / **--zstd** / **--no-compress**\n> Choose the compression algorithm, or skip compression entirely.\n\n**--sysroot** _DIR_\n> Build against another root directory, for cross-building or installers.\n\n**-v**, **--verbose** / **-q**, **--quiet**\n> Raise or lower the verbosity."],["CONFIGURATION","**/etc/dracut.conf**\n> Main configuration file for dracut behavior and module selection.\n\n**/etc/dracut.conf.d/**\n> Directory for additional configuration files.\n\n**/usr/lib/dracut/dracut.conf.d/**\n> System-wide dracut configuration directory."],["CAVEATS","A bad initramfs makes the machine unbootable, and you will not find out until the next reboot. Always keep the previous kernel and its image installed, so the boot menu still offers a way in, and prefer `dracut -f` on a spare image name over overwriting the one you are currently running from.\n\n**Host-only images are not portable.** An image built with **-H** on one machine, in a container, or in a chroot may lack the drivers needed on the target and drop you into the emergency shell. Build with **--no-hostonly** whenever the image will boot hardware other than the machine that produced it.\n\n`--print-cmdline` is worth running before switching bootloader configuration: an image can only find the root filesystem if the kernel command line tells it where to look, and a missing `rd.luks.uuid` or `rd.lvm.lv` fails at exactly the point where debugging is hardest.\n\nHistorically dracut was the Fedora/RHEL/SUSE generator while Debian and Ubuntu used **initramfs-tools**, but Debian and Ubuntu now package dracut as an alternative, and it is no longer a Red Hat-only tool."],["HISTORY","dracut was started around **2008** by Harald Hoyer and others at Red Hat to replace `mkinitrd`, whose generated images were an unmaintainable tangle of distribution-specific shell script. The design goal was to keep as little logic as possible in the initramfs itself and let udev events drive the boot, so that one generator could serve every distribution instead of each rolling its own. Fedora, RHEL, and SUSE adopted it, and it became the de facto standard.\n\nIn **March 2024** development moved to **dracut-ng**, a coordinated fork involving the previous maintainer along with several distributions, after the original repository stalled. dracut-ng is where releases now come from, and distributions have followed it."],["INSTALL","```apt: sudo apt install dracut-core```\n\n```dnf: sudo dnf install dracut```\n\n```pacman: sudo pacman -S dracut```\n\n```apk: sudo apk add dracut-core```\n\n```zypper: sudo zypper install dracut```\n\n```nix: nix profile install nixpkgs#dracut```"],["SEE ALSO","[mkinitramfs](/man/mkinitramfs)(8), [lsinitrd](/man/lsinitrd)(1), [update-initramfs](/man/update-initramfs)(8), [mkinitcpio](/man/mkinitcpio)(8)"],["RESOURCES","```[Source code](https://github.com/dracut-ng/dracut-ng)```\n\n```[Homepage](https://dracut-ng.github.io/)```"]],"e":["mkinitramfs","lsinitrd","update-initramfs","mkinitcpio"],"x":"dracut"},"dstask":{"n":"dstask","t":"Git-powered terminal todo manager","s":[["TAGLINE","Git-powered terminal todo manager"],["TLDR","**Add** a new task\n\n```dstask add [task description]```\n\n**List** all tasks\n\n```dstask```\n\n**Mark** a task as done\n\n```dstask done [task_id]```\n\n**View** tasks by context/project\n\n```dstask context [context_name]```"],["SYNOPSIS","**dstask** [_command_] [_arguments_]"],["PARAMETERS","**-c, --context** _CONTEXT_\n> Set context filter\n\n**-p, --project** _PROJECT_\n> Set project filter\n\n**-P, --priority** _PRIORITY_\n> Set priority: low, medium, high, critical\n\n**-t, --tag** _TAG_\n> Add tag to task\n\n**-n, --note** _NOTE_\n> Add note to task\n\n**--due** _DATE_\n> Set due date (YYYY-MM-DD)\n\n**-v, --verbose**\n> Enable verbose output\n\n**-h, --help**\n> Display help and exit\n\n**--version**\n> Display version and exit"],["DESCRIPTION","**dstask** is a task manager that stores todos in a Git repository, allowing for version control, synchronization across devices, and offline access. It follows the Getting Things Done (GTD) methodology with support for contexts, projects, and priorities.\n\nTasks are stored as simple text files in a Git repository, making them portable and avoiding vendor lock-in. The tool provides a clean command-line interface for managing tasks without unnecessary complexity."],["COMMANDS","**add** _TASK_\n> Add a new task\n\n**edit** _ID_\n> Edit an existing task\n\n**done** _ID_\n> Mark task as completed\n\n**rm** _ID_\n> Remove a task\n\n**list**\n> List tasks (default command)\n\n**next**\n> Show next actionable task\n\n**context** _CONTEXT_\n> Switch to context\n\n**project** _PROJECT_\n> Switch to project view\n\n**sync**\n> Sync with remote Git repository\n\n**undo**\n> Undo last action"],["CAVEATS","Requires Git to be initialized and configured. Remote sync requires valid Git remote. Task IDs are internal references, not stable across operations. Complex recurring tasks not natively supported."],["HISTORY","**dstask** was created by Cody Chan as a task manager that embraces plain-text storage and Git for synchronization. It was inspired by todo.txt but adds Git integration and a more modern interface."],["INSTALL","```brew: brew install dstask```\n\n```nix: nix profile install nixpkgs#dstask```"],["SEE ALSO","[git](/man/git)(1), [task](/man/task)(1)"]],"e":["git","task"],"x":"dstask add [task description]"},"f3read":{"n":"f3read","t":"flash drive integrity verification tool","s":[["TAGLINE","flash drive integrity verification tool"],["TLDR","**Read and verify** data on a flash drive\n\n```f3read [/media/flash]```\n\n**Show progress** during verification\n\n```f3read --show-progress [/media/flash]```\n\n**Verify a specific range** of test files (e.g., files 5 through 10)\n\n```f3read --start-at [5] --end-at [10] [/media/flash]```\n\n**Start verification** from a specific file number\n\n```f3read --start-at [3] [/media/flash]```"],["SYNOPSIS","**f3read** [_options_] _directory_"],["PARAMETERS","_DIRECTORY_\n> Mount point of flash drive to verify.\n\n**--start-at** _N_\n> Start at file number N.\n\n**--end-at** _N_\n> End at file number N.\n\n**--show-progress**\n> Display progress during read.\n\n**--help**\n> Display help information."],["DESCRIPTION","**f3read** verifies data integrity on flash drives by reading files created by f3write. It's part of the F3 (Fight Flash Fraud) toolkit for detecting counterfeit flash storage.\n\nThe tool reads verification files and checks their content against expected patterns. Differences indicate either data corruption or fake capacity where writes appeared successful but data was lost.\n\nf3read works with f3write as a non-destructive test pair for validating flash drive integrity without data loss."],["CAVEATS","Requires files from f3write. Tests only the space used by test files. Slower than f3probe but non-destructive. Flash drive must be mounted."],["HISTORY","f3read is part of the **F3** (Fight Flash Fraud) project, providing verification of flash storage integrity. Combined with f3write, it offers a non-destructive alternative to f3probe for testing drive capacity."],["INSTALL","```apt: sudo apt install f3```\n\n```dnf: sudo dnf install f3```\n\n```apk: sudo apk add f3```\n\n```zypper: sudo zypper install f3```\n\n```brew: brew install f3```\n\n```nix: nix profile install nixpkgs#f3```"],["SEE ALSO","[f3write](/man/f3write)(1), [f3probe](/man/f3probe)(1), [f3fix](/man/f3fix)(1)"]],"e":["f3write","f3probe","f3fix"],"x":"f3read [/media/flash]"},"feroxbuster":{"n":"feroxbuster","t":"fast recursive web content discovery tool","s":[["TAGLINE","fast recursive web content discovery tool"],["TLDR","**Scan a URL** for directories and files\n\n```feroxbuster --url [https://example.com]```\n\n**Use a custom wordlist**\n\n```feroxbuster --url [https://example.com] --wordlist [path/to/wordlist.txt]```\n\n**Scan with specific extensions**\n\n```feroxbuster --url [https://example.com] --extensions [php,html,txt]```\n\n**Scan recursively** to a specific depth\n\n```feroxbuster --url [https://example.com] --depth [3]```\n\n**Scan with multiple threads**\n\n```feroxbuster --url [https://example.com] --threads [100]```\n\n**Filter out specific status codes**\n\n```feroxbuster --url [https://example.com] --filter-status [404,403]```\n\n**Output results to a file**\n\n```feroxbuster --url [https://example.com] --output [results.txt]```\n\n**Scan through a proxy**\n\n```feroxbuster --url [https://example.com] --proxy [http://127.0.0.1:8080]```"],["SYNOPSIS","**feroxbuster** [_-u url_] [_-w wordlist_] [_-x extensions_] [_-t threads_] [_-d depth_] [_-o output_] [_options_]"],["PARAMETERS","**-u**, **--url** _url_\n> Target URL to scan.\n\n**-w**, **--wordlist** _file_\n> Wordlist to use (default: built-in seclists).\n\n**-x**, **--extensions** _ext_\n> File extensions to check (comma-separated).\n\n**-t**, **--threads** _n_\n> Number of concurrent threads (default: 50).\n\n**-d**, **--depth** _n_\n> Maximum recursion depth (default: 4, 0 for infinite).\n\n**-o**, **--output** _file_\n> Output file for results.\n\n**-n**, **--no-recursion**\n> Disable recursive scanning.\n\n**-f**, **--add-slash**\n> Append / to each request.\n\n**-C**, **--filter-status** _codes_\n> Filter out specific HTTP status codes.\n\n**-S**, **--filter-size** _size_\n> Filter out responses of specific size.\n\n**-W**, **--filter-words** _count_\n> Filter out responses with specific word count.\n\n**-L**, **--filter-lines** _count_\n> Filter out responses with specific line count.\n\n**--proxy** _url_\n> Proxy to use for requests.\n\n**-H**, **--headers** _header_\n> Custom headers (can be used multiple times).\n\n**-k**, **--insecure**\n> Disable TLS certificate verification.\n\n**-q**, **--quiet**\n> Minimal output.\n\n**--json**\n> Output in JSON format."],["DESCRIPTION","**feroxbuster** is a fast, recursive content discovery tool written in Rust. It brute-forces directories and files on web servers by sending HTTP requests from a wordlist, identifying hidden resources through response analysis.\n\nSpeed is a key feature: Rust's performance combined with asynchronous requests enables scanning thousands of paths per second. The tool supports recursive scanning, automatically continuing into discovered directories up to a configurable depth.\n\nAdvanced filtering options reduce false positives by hiding responses based on status code, response size, word count, line count, or regex patterns. This helps when servers return generic pages for non-existent paths.\n\nThe interactive mode provides real-time statistics and allows pausing, resuming, adding new URLs, and adjusting settings during the scan. Results are color-coded by status code for quick interpretation.\n\nFeroxbuster integrates with tools like Burp Suite via proxy support and can save state to resume interrupted scans. Output formats include plain text, JSON, and formats compatible with other security tools."],["CAVEATS","Only use on systems you have permission to test. High thread counts may trigger rate limiting or IP bans. Large wordlists with recursion can generate enormous traffic. Some WAFs detect and block the tool's patterns."],["HISTORY","**feroxbuster** was created by Ben \"epi\" Risher, with the first release around **2020**. The name combines \"ferox\" (Latin for fierce/wild) with \"buster.\" Written in Rust for performance, it quickly gained popularity as a modern alternative to tools like dirb, gobuster, and dirsearch."],["INSTALL","```zypper: sudo zypper install feroxbuster```\n\n```brew: brew install feroxbuster```\n\n```nix: nix profile install nixpkgs#feroxbuster```"],["SEE ALSO","[gobuster](/man/gobuster)(1), [dirsearch](/man/dirsearch)(1), [ffuf](/man/ffuf)(1), [dirb](/man/dirb)(1)"],["RESOURCES","```[Source code](https://github.com/epi052/feroxbuster)```"]],"e":["gobuster","dirsearch","ffuf","dirb"],"x":"feroxbuster --url [https://example.com]"},"fontforge":{"n":"fontforge","t":"comprehensive font editor for creating and modifying fonts","s":[["TAGLINE","comprehensive font editor for creating and modifying fonts"],["TLDR","**Open FontForge GUI**\n\n```fontforge```\n\n**Open font file**\n\n```fontforge [font.ttf]```\n\n**Convert font format**\n\n```fontforge -c 'Open($1); Generate($2)' [input.otf] [output.ttf]```\n\n**Run script**\n\n```fontforge -script [convert.pe]```\n\n**Export to web** fonts\n\n```fontforge -c 'Open($1); Generate($2)' [font.ttf] [font.woff2]```"],["SYNOPSIS","**fontforge** [_options_] [_files_...]"],["PARAMETERS","_FILES_\n> Font files to open.\n\n**-c** _SCRIPT_\n> Execute script command.\n\n**-script** _FILE_\n> Run script file.\n\n**-lang** _LANGUAGE_\n> Scripting language (ff, py).\n\n**-nosplash**\n> Start without splash screen.\n\n**--help**\n> Display help information."],["DESCRIPTION","**fontforge** is a comprehensive font editor supporting OpenType, TrueType, and many other formats. It provides tools for creating, editing, and converting fonts.\n\nThe application offers glyph editing, metrics adjustment, kerning, hinting, and OpenType feature development. Scripting support enables batch operations and automated font processing.\n\nFontForge handles professional font development workflows from initial design to production-ready output."],["CAVEATS","Steep learning curve for font design. GUI can be complex. Some formats have limited support."],["HISTORY","FontForge (originally PfaEdit) was created by **George Williams** starting in 2000. It's the most capable open source font editor, used by type designers and font developers worldwide."],["INSTALL","```apt: sudo apt install fontforge```\n\n```dnf: sudo dnf install fontforge```\n\n```pacman: sudo pacman -S fontforge```\n\n```apk: sudo apk add fontforge```\n\n```zypper: sudo zypper install fontforge```\n\n```brew: brew install fontforge```\n\n```nix: nix profile install nixpkgs#fontforge```"],["SEE ALSO","[fc-query](/man/fc-query)(1)"]],"e":["fc-query"],"x":"fontforge"},"foo2qpdl":{"n":"foo2qpdl","t":"Convert Ghostscript pbmraw or bitcmyk format into a QPDL printer stream","s":[["TAGLINE","Convert Ghostscript pbmraw or bitcmyk format into a QPDL printer stream"],["TLDR","**Convert PBM to** QPDL format\n\n```foo2qpdl [options] < [input.pbm] > [output.prn]```\n\n**Set resolution**\n\n```foo2qpdl -r [600] < [input.pbm] > [output.prn]```\n\n**Set paper size to A4**\n\n```foo2qpdl -p 9 < [input.pbm] > [output.prn]```\n\n**Color mode**\n\n```foo2qpdl -c < [input.ppm] > [output.prn]```\n\n**Set number of copies**\n\n```foo2qpdl -n [3] < [input.pbm] > [output.prn]```\n\n**Enable duplex printing (long edge)**\n\n```foo2qpdl -d 2 < [input.pbm] > [output.prn]```\n\n**Specify printer model**\n\n```foo2qpdl -z [2] < [input.pbm] > [output.prn]```"],["SYNOPSIS","**foo2qpdl** [_options_] < _input_ > _output_"],["PARAMETERS","**-r** _xres_**x**_yres_\n> Set resolution in pixels per inch [1200x600].\n\n**-g** _xpix_**x**_ypix_\n> Set page dimensions in pixels [10200x6600].\n\n**-p** _paper_\n> Paper code: 0=letter, 1=legal, 2=A5, 3=B5, 6=folio, 7=quarto, 8=10x14, 9=A4, 11=executive, and various envelope codes.\n\n**-c**\n> Force color mode.\n\n**-m** _media_\n> Media type code: 0=plain, 1=thick, 2=thin, 3=bond, 4=color, 5=card, 6=labels, 7=envelope, 8=preprinted, 9=cotton, 19=recycled, 20=archive.\n\n**-n** _copies_\n> Number of copies [1].\n\n**-d** _duplex_\n> Duplex mode: 1=off, 2=long edge, 3=short edge.\n\n**-s** _source_\n> Source (InputSlot) code: 1=auto, 2=manual, 3=multi, 4=tray1.\n\n**-t**\n> Draft mode. Every other pixel is white.\n\n**-z** _model_\n> Printer model: 0=CLP-300/CLX-2160/CLX-3160, 1=CLP-600, 2=CLP-310/CLP-315/CLP-610/CLX-3175, 3=CLP-620.\n\n**-J** _filename_\n> Filename string to send to the printer."],["DESCRIPTION","**foo2qpdl** converts Ghostscript pbmraw, bitcmyk, or pksmraw output formats to monochrome or color QPDL streams. It supports Samsung CLP-300, CLX-2160, CLP-600, CLX-3160, CLP-610, CLP-620, CLP-360, CLP-365, and Xerox Phaser 6110 printers.\n\nThe tool handles the printer-specific encoding required by QPDL printers, managing color modes, compression, and page setup. It is typically invoked through the foo2qpdl-wrapper script which handles Ghostscript conversion automatically."],["CAVEATS","Only for QPDL-compatible printers. Model support varies. Some features may not be available on all printers."],["HISTORY","foo2qpdl is part of **foo2zjs**, providing support for Samsung printers using the QPDL protocol through reverse-engineered drivers."],["INSTALL","```apt: sudo apt install printer-driver-foo2zjs```\n\n```dnf: sudo dnf install foo2qpdl```"],["SEE ALSO","[foo2qpdl-wrapper](/man/foo2qpdl-wrapper)(1), [foo2zjs](/man/foo2zjs)(1)"]],"e":["foo2qpdl-wrapper","foo2zjs"],"x":"foo2qpdl [options] < [input.pbm] > [output.prn]"},"foo2xqx":{"n":"foo2xqx","t":"PBM to XQX converter for HP LaserJet printers","s":[["TAGLINE","PBM to XQX converter for HP LaserJet printers"],["TLDR","**Convert PBM to** XQX format\n\n```foo2xqx [options] < [input.pbm] > [output.prn]```\n\n**Set resolution**\n\n```foo2xqx -r [600] < [input.pbm] > [output.prn]```\n\n**Set paper size**\n\n```foo2xqx -p [letter] < [input.pbm] > [output.prn]```"],["SYNOPSIS","**foo2xqx** [_options_] < _input_ > _output_"],["PARAMETERS","**-r** _DPI_\n> Resolution (300, 600, 1200).\n\n**-p** _PAPER_\n> Paper size (letter, a4, legal).\n\n**-m** _MEDIA_\n> Media type.\n\n**-n** _COPIES_\n> Number of copies.\n\n**-d** _DUPLEX_\n> Duplex mode.\n\n**--help**\n> Display help information."],["DESCRIPTION","**foo2xqx** converts PBM images to XQX format for HP LaserJet and compatible printers. It encodes raster data for printers using HP's XQX protocol.\n\nThe tool handles printer-specific encoding and compression for monochrome printing. It functions as a CUPS filter backend for supported HP printers.\n\nfoo2xqx enables Linux printing on certain HP LaserJet models that use XQX."],["CAVEATS","Monochrome only. Limited to XQX-compatible printers. Model support varies."],["HISTORY","foo2xqx is part of **foo2zjs**, adding support for HP printers using the XQX protocol, reverse-engineered to provide Linux compatibility."],["INSTALL","```apt: sudo apt install printer-driver-foo2zjs```\n\n```dnf: sudo dnf install foo2xqx```"],["SEE ALSO","[foo2xqx-wrapper](/man/foo2xqx-wrapper)(1), [foo2zjs](/man/foo2zjs)(1)"],["RESOURCES","```[Source code](https://github.com/OpenPrinting/foo2zjs)```"]],"e":["foo2xqx-wrapper","foo2zjs"],"x":"foo2xqx [options] < [input.pbm] > [output.prn]"},"gh-pages":{"n":"gh-pages","t":"Publish static sites to GitHub Pages branch","s":[["TAGLINE","Publish static sites to GitHub Pages branch"],["TLDR","**Deploy dist folder to gh-pages**\n\n```npx gh-pages -d [dist]```\n\n**Deploy with custom message**\n\n```npx gh-pages -d [dist] -m \"[Deploy to GitHub Pages]\"```\n\n**Deploy to custom branch**\n\n```npx gh-pages -d [dist] -b [docs]```\n\n**Deploy specific files**\n\n```npx gh-pages -d [dist] -s \"[*.html,*.css]\"```\n\n**Deploy with custom remote**\n\n```npx gh-pages -d [dist] -o [upstream]```\n\n**Deploy with a custom domain (CNAME)**\n\n```npx gh-pages -d [dist] --cname [example.com]```"],["SYNOPSIS","**gh-pages** [_options_]"],["PARAMETERS","**-d**, **--dist** _dir_\n> Directory to deploy.\n\n**-s**, **--src** _pattern_\n> File pattern within dist.\n\n**-b**, **--branch** _branch_\n> Target branch (default: gh-pages).\n\n**-o**, **--remote** _remote_\n> Remote name (default: origin).\n\n**-m**, **--message** _msg_\n> Commit message.\n\n**-u**, **--user** _name_\n> Git user name.\n\n**-e**, **--email** _email_\n> Git user email.\n\n**-a**, **--add**\n> Add files, don't remove existing.\n\n**-t**, **--dotfiles**\n> Include dotfiles.\n\n**-r**, **--repo** _url_\n> Repository URL to push to (if not in a git repo or pushing elsewhere).\n\n**--cname** _domain_\n> Create a CNAME file with a custom domain name.\n\n**--nojekyll**\n> Add .nojekyll file to bypass Jekyll processing on GitHub Pages."],["DESCRIPTION","**gh-pages** is an npm package that publishes files to a GitHub Pages branch. It automates pushing built static sites to the gh-pages branch for hosting, creating clean commits of your distribution folder to the target branch.\n\nThe tool makes deployment a single command and is commonly used in CI/CD pipelines and as a package.json script."],["CONFIGURATION","**package.json**\n> Configure a deploy script with `\"deploy\": \"gh-pages -d dist\"` in the scripts section."],["CAVEATS","Requires git and npm. By default, existing files on the target branch are removed before deploying; use **-a** to preserve them. GitHub Pages repository settings must be configured to serve from the target branch. Use **--nojekyll** for sites that do not need Jekyll processing. Push credentials are required."],["HISTORY","gh-pages was created by **Tim Schaub** to simplify GitHub Pages deployments. It became a standard tool in the JavaScript ecosystem for static site deployment."],["INSTALL","```apt: sudo apt install gh```\n\n```dnf: sudo dnf install gh```\n\n```pacman: sudo pacman -S github-cli```\n\n```apk: sudo apk add github-cli```\n\n```zypper: sudo zypper install gh```\n\n```brew: brew install gh```\n\n```nix: nix profile install nixpkgs#gh```"],["SEE ALSO","[gh](/man/gh)(1), [git](/man/git)(1), [npm](/man/npm)(1)"]],"e":["gh","git","npm"],"x":"npx gh-pages -d [dist]"},"gh-pr-create":{"n":"gh-pr-create","t":"Create new pull requests on GitHub","s":[["TAGLINE","Create new pull requests on GitHub"],["TLDR","**Create PR interactively**\n\n```gh pr create```\n\n**Create with title and body**\n\n```gh pr create -t \"[title]\" -b \"[body]\"```\n\n**Create as draft**\n\n```gh pr create --draft -t \"[title]\"```\n\n**Create with reviewers**\n\n```gh pr create -t \"[title]\" -r [reviewer1],[reviewer2]```\n\n**Create to specific base branch**\n\n```gh pr create --base [main] --head [feature]```\n\n**Create and fill from commits**\n\n```gh pr create --fill```\n\n**Open in browser to create**\n\n```gh pr create -w```"],["SYNOPSIS","**gh** **pr** **create** [_options_]"],["PARAMETERS","**-t**, **--title** _title_\n> PR title.\n\n**-b**, **--body** _body_\n> PR body.\n\n**-d**, **--draft**\n> Create as draft.\n\n**-r**, **--reviewer** _users_\n> Request reviewers.\n\n**-a**, **--assignee** _users_\n> Assign users.\n\n**-l**, **--label** _labels_\n> Add labels.\n\n**--base** _branch_\n> Base branch.\n\n**--head** _branch_\n> Head branch.\n\n**--fill**\n> Fill from commits.\n\n**-w**, **--web**\n> Open in browser.\n\n**-R**, **--repo** _owner/repo_\n> Repository."],["DESCRIPTION","**gh pr create** creates pull requests on GitHub from the command line. It streamlines the PR creation workflow by detecting the current branch context and offering multiple input modes.\n\nWhen run without arguments, the command prompts interactively for title, body, and metadata. The --fill flag auto-populates the title from the first commit message and the body from all commit messages since branching. Draft PRs (--draft) allow pushing work-in-progress changes for early feedback without formal review requests."],["INSTALL","```apt: sudo apt install gh```\n\n```dnf: sudo dnf install gh```\n\n```pacman: sudo pacman -S github-cli```\n\n```apk: sudo apk add github-cli```\n\n```zypper: sudo zypper install gh```\n\n```brew: brew install gh```\n\n```nix: nix profile install nixpkgs#gh```"],["SEE ALSO","[gh-pr](/man/gh-pr)(1), [gh](/man/gh)(1)"]],"e":["gh-pr","gh"],"x":"gh pr create"},"gh-ssh-key":{"n":"gh-ssh-key","t":"Manage SSH keys for GitHub authentication","s":[["TAGLINE","Manage SSH keys for GitHub authentication"],["TLDR","**List SSH keys**\n\n```gh ssh-key list```\n\n**Add an SSH key**\n\n```gh ssh-key add [key.pub]```\n\n**Add with title**\n\n```gh ssh-key add [key.pub] -t \"[title]\"```\n\n**Delete an SSH key**\n\n```gh ssh-key delete [key_id]```"],["SYNOPSIS","**gh** **ssh-key** _command_ [_options_]"],["SUBCOMMANDS","**list**\n> List SSH keys.\n\n**add**\n> Add an SSH key.\n\n**delete**\n> Delete an SSH key."],["PARAMETERS","**-t**, **--title** _title_\n> Key title."],["DESCRIPTION","**gh ssh-key** manages SSH public keys associated with your GitHub account for secure Git authentication. SSH keys provide an alternative to HTTPS authentication, enabling passwordless git operations.\n\nThe command supports adding new SSH keys from public key files, typically generated with **ssh-keygen**. Keys can be labeled with titles for identification when managing multiple keys across different machines. Deletion removes keys from your GitHub account, immediately revoking access."],["INSTALL","```apt: sudo apt install gh```\n\n```dnf: sudo dnf install gh```\n\n```pacman: sudo pacman -S github-cli```\n\n```apk: sudo apk add github-cli```\n\n```zypper: sudo zypper install gh```\n\n```brew: brew install gh```\n\n```nix: nix profile install nixpkgs#gh```"],["SEE ALSO","[gh](/man/gh)(1), [gh-gpg-key](/man/gh-gpg-key)(1)"],["RESOURCES","```[Documentation](https://cli.github.com/manual/gh_ssh-key)```\n\n```[Source code](https://github.com/cli/cli)```"]],"e":["gh","gh-gpg-key"],"x":"gh ssh-key list"},"git-reflog":{"n":"git-reflog","t":"Record and recover reference movements","s":[["TAGLINE","Record and recover reference movements"],["TLDR","**Show reflog**\n\n```git reflog```\n\n**Show reflog for branch**\n\n```git reflog [branch]```\n\n**Show with timestamps**\n\n```git reflog --date=relative```\n\n**Expire old entries**\n\n```git reflog expire --expire=[90.days] --all```\n\n**Delete specific entry**\n\n```git reflog delete HEAD@{2}```"],["SYNOPSIS","**git reflog** [_command_] [_options_]"],["PARAMETERS","**show** _REF_\n> Show reflog for ref (default).\n\n**expire**\n> Prune old reflog entries.\n\n**delete**\n> Delete specific entries.\n\n**--all**\n> Process all refs.\n\n**--expire** _TIME_\n> Expire older than time.\n\n**--date** _FORMAT_\n> Date format.\n\n**-n** _NUM_\n> Limit entries shown.\n\n**--help**\n> Display help information."],["DESCRIPTION","**git reflog** records when refs (like HEAD and branch tips) are updated, providing a safety net for recovering lost commits. After accidental resets, rebases, or branch deletions, the reflog shows where refs previously pointed, allowing recovery of otherwise unreachable commits.\n\nEntries expire after a configurable period: reachable entries after 90 days (`gc.reflogExpire`), unreachable ones after 30 days (`gc.reflogExpireUnreachable`). The reflog is local only and is not shared between repositories."],["CAVEATS","Local only, not pushed. Entries expire (90 days reachable, 30 days unreachable, by default). Won't help after gc removes the underlying objects."],["HISTORY","git reflog is a core **Git** safety feature, providing local history of ref movements to enable recovery from destructive operations."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-log](/man/git-log)(1), [git-reset](/man/git-reset)(1), [git-gc](/man/git-gc)(1)"],["RESOURCES","```[Source code](https://github.com/git/git)```\n\n```[Homepage](https://git-scm.com/)```\n\n```[Documentation](https://git-scm.com/docs/git-reflog)```"]],"e":["git-log","git-reset","git-gc"],"x":"git reflog"},"gophish":{"n":"gophish","t":"Open-source phishing simulation framework","s":[["TAGLINE","Open-source phishing simulation framework"],["TLDR","**Start Gophish server**\n\n```./gophish```\n\n**Start with custom config**\n\n```./gophish -config [config.json]```\n\n**Access admin panel**\n\n```https://localhost:3333```"],["SYNOPSIS","**gophish** [_options_]"],["PARAMETERS","**-config** _file_\n> Configuration file path."],["DESCRIPTION","**Gophish** is an open-source phishing framework for security awareness training and penetration testing. It provides a web interface for creating and managing phishing campaigns, including email template editing, landing page cloning, and results tracking.\n\nThe platform is used by security teams to test organizational awareness and response to phishing attacks, with campaign management, user group handling, and API support for automation."],["CONFIGURATION","**config.json**\n> Main configuration file specifying admin server address, phishing server address, database path, and TLS settings."],["DEFAULT CREDENTIALS","```\nAdmin Panel: https://localhost:3333\nUsername: admin\nPassword: (shown on first run)\n```"],["CAVEATS","For authorized security testing only. Requires proper authorization before use. Email sending needs configured SMTP. May be flagged by security software. Store credentials securely."],["HISTORY","Gophish was created by **Jordan Wright** in **2015** as an open-source alternative to commercial phishing simulation platforms. It's widely used in security awareness training and penetration testing."],["INSTALL","```nix: nix profile install nixpkgs#gophish```"],["SEE ALSO","[setoolkit](/man/setoolkit)(1)"]],"e":["setoolkit"],"x":"./gophish"},"happy":{"n":"happy","t":"parser generator for Haskell, similar to yacc for C","s":[["TAGLINE","parser generator for Haskell, similar to yacc for C"],["TLDR","**Generate parser from grammar file**\n\n```happy [grammar.y] -o [Parser.hs]```\n\n**Generate with info file for debugging**\n\n```happy -i [grammar.y]```\n\n**Generate with GHC extensions for faster code**\n\n```happy -g [grammar.y]```\n\n**Generate array-based parser with GHC string encoding**\n\n```happy -ag [grammar.y]```\n\n**Generate parser with debugging output at runtime**\n\n```happy -d [grammar.y]```\n\n**Generate GLR parser**\n\n```happy -l [grammar.y]```"],["SYNOPSIS","**happy** [_options_] _file_"],["PARAMETERS","_FILE_\n> Grammar file (.y).\n\n**-o** _FILE_\n> Output file name. Defaults to FILE.hs.\n\n**-i** [_FILE_]\n> Generate info file with grammar details, parser states, and conflicts.\n\n**-g**, **--ghc**\n> Use GHC-specific extensions for faster parsers.\n\n**-a**, **--array**\n> Generate array-based parser (smaller but slower). Combined with -g, arrays are encoded as strings for faster performance.\n\n**-c**, **--coerce**\n> Use GHC's unsafeCoerce# for smaller, faster parsers. May crash at runtime if grammar has type errors.\n\n**-d**, **--debug**\n> Generate a parser that prints shift/reduce debugging info to stderr.\n\n**-l**, **--glr**\n> Generate a GLR parser instead of LALR(1).\n\n**-t** _DIR_, **--template=**_DIR_\n> Directory for template files.\n\n**-h**, **--help**\n> Display help information.\n\n**-v**, **--version**\n> Print version information."],["DESCRIPTION","**Happy** is a parser generator for Haskell, similar to yacc for C. It takes a grammar specification and produces a Haskell parser module.\n\nThe tool generates LALR parsers from BNF-like grammars and can also produce GLR parsers. It integrates with the Alex lexer generator for complete parsing solutions."],["CAVEATS","Haskell specific. Learning curve for grammar syntax. Debugging shift/reduce conflicts."],["HISTORY","Happy was created for **Haskell** as an equivalent to yacc/bison, widely used in Haskell compilers and tools like GHC."],["INSTALL","```apt: sudo apt install happy```\n\n```dnf: sudo dnf install happy```\n\n```pacman: sudo pacman -S happy```\n\n```apk: sudo apk add happy```\n\n```zypper: sudo zypper install happy```\n\n```nix: nix profile install nixpkgs#happy```"],["SEE ALSO","[alex](/man/alex)(1), [ghc](/man/ghc)(1), [yacc](/man/yacc)(1), [bison](/man/bison)(1)"]],"e":["alex","ghc","yacc","bison"],"x":"happy [grammar.y] -o [Parser.hs]"},"headscale":{"n":"headscale","t":"self-hosted implementation of the Tailscale control server","s":[["TAGLINE","self-hosted implementation of the Tailscale control server"],["TLDR","**Start the server**\n\n```headscale serve```\n\n**Create a user**\n\n```headscale users create [username]```\n\n**List users**\n\n```headscale users list```\n\n**Create auth key**\n\n```headscale authkeys create --user [username]```\n\n**List nodes**\n\n```headscale nodes list```\n\n**Register a node**\n\n```headscale nodes register --user [username] --key [nodekey:abc123]```\n\n**Delete a node**\n\n```headscale nodes delete --id [1]```\n\n**Enable routes for a node**\n\n```headscale routes enable --route [10.0.0.0/24]```"],["SYNOPSIS","**headscale** _command_ [_subcommand_] [_options_]"],["COMMANDS","**serve**\n> Start Headscale server.\n\n**users** create|list|rename|destroy\n> Manage users (namespaces).\n\n**nodes** list|register|delete|expire|rename|move|tag\n> Manage nodes.\n\n**authkeys** create|list|expire\n> Manage pre-authentication keys.\n\n**routes** list|enable|disable\n> Manage subnet routes.\n\n**preauthkeys** create|list|expire\n> Alias for authkeys.\n\n**apikeys** create|list|expire\n> Manage API keys.\n\n**debug**\n> Debug commands.\n\n**completion**\n> Generate shell completions."],["PARAMETERS","**--user**, **-u** _name_\n> User (namespace) name.\n\n**--key** _nodekey_\n> Node key for registration.\n\n**--id** _id_\n> Node or route ID.\n\n**--expiration** _duration_\n> Key expiration time.\n\n**--reusable**\n> Create reusable auth key.\n\n**--ephemeral**\n> Create ephemeral auth key.\n\n**--tags** _list_\n> ACL tags for auth key.\n\n**--output** _format_\n> Output format: human, json, yaml."],["DESCRIPTION","**headscale** is a self-hosted implementation of the Tailscale control server. It coordinates WireGuard connections between Tailscale clients.\n\n**serve** starts the coordination server. Clients connect to exchange keys and discover peers. Configuration is in **/etc/headscale/config.yaml**.\n\nUsers (namespaces) organize nodes. Each user has isolated nodes unless ACL policies allow cross-user communication. Create users before registering nodes.\n\n**authkeys** create pre-authentication keys for unattended registration. Clients use these instead of interactive login. Keys can be reusable or single-use.\n\nSubnet routes advertised by nodes require explicit enabling with **routes enable**. This controls which networks are accessible through the mesh.\n\nACL policies defined in config control traffic between nodes based on users, groups, and tags."],["CAVEATS","Requires TLS certificate for production. DERP servers needed for NAT traversal. Not all Tailscale features are supported. Tailscale clients need HEADSCALE_URL configuration."],["HISTORY","Headscale was created by **Juan Font** and first released in **2020**. It provides self-hosted alternative to Tailscale's coordination service while using the same WireGuard-based clients. The project grew from desire for self-hosted mesh VPN without vendor control. Headscale is popular for organizations wanting Tailscale compatibility with full infrastructure ownership."],["INSTALL","```dnf: sudo dnf install headscale```\n\n```pacman: sudo pacman -S headscale```\n\n```apk: sudo apk add headscale```\n\n```zypper: sudo zypper install headscale```\n\n```nix: nix profile install nixpkgs#headscale```"],["SEE ALSO","[tailscale](/man/tailscale)(1), [nebula](/man/nebula)(1)"]],"e":["tailscale","nebula"],"x":"headscale serve"},"hid2hci":{"n":"hid2hci","t":"switches Bluetooth USB dongles between HID and HCI modes","s":[["TAGLINE","switches Bluetooth USB dongles between HID and HCI modes"],["TLDR","**Switch device to HCI mode**\n\n```sudo hid2hci --mode=hci --method=csr --devpath=[/sys/devices/...]```\n\n**Switch Dell device to HCI mode**\n\n```sudo hid2hci --mode=hci --method=dell --devpath=[/sys/devices/...]```\n\n**Restore device to HID mode**\n\n```sudo hid2hci --mode=hid --method=csr --devpath=[/sys/devices/...]```"],["SYNOPSIS","**hid2hci** [_options_]"],["PARAMETERS","**--mode** _MODE_\n> Target mode to switch the device to: hid or hci.\n\n**--method** _METHOD_\n> Vendor-specific switching method (csr, csr2, logitech-hdi, dell).\n\n**--devpath** _PATH_\n> Device path in /sys.\n\n**--help**\n> Display help information."],["DESCRIPTION","**hid2hci** switches Bluetooth USB dongles between HID and HCI modes. Some devices boot in HID mode and need switching to function as Bluetooth controllers.\n\nThe tool is needed for certain USB Bluetooth adapters that start in keyboard/mouse mode. After switching, bluez can use the device."],["CAVEATS","Device-specific. Requires correct method. Part of BlueZ package."],["HISTORY","hid2hci was developed as part of **BlueZ** to handle USB Bluetooth adapters with dual-mode firmware."],["SEE ALSO","[bluetoothctl](/man/bluetoothctl)(1), [hciconfig](/man/hciconfig)(1)"]],"e":["bluetoothctl","hciconfig"],"x":"sudo hid2hci --mode=hci --method=csr --devpath=[/sys/devices/...]"},"hipstopgm":{"n":"hipstopgm","t":"converts a HIPS image file to PGM format","s":[["TAGLINE","converts a HIPS image file to PGM format"],["TLDR","**Convert HIPS file to PGM**\n\n```hipstopgm [file.hip] > [output.pgm]```\n\n**Convert from stdin**\n\n```cat [file.hip] | hipstopgm > [output.pgm]```"],["SYNOPSIS","**hipstopgm** [_hipsfile_]"],["DESCRIPTION","**hipstopgm** converts a HIPS (Human Information Processing Laboratory, NYU) image file to PGM (Portable Graymap) format. Part of the Netpbm package of graphics manipulation tools. If the HIPS file contains multiple frames, they are concatenated vertically in the output. The tool has no command-specific options beyond standard Netpbm options."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pgm](/man/pgm)(1), [pgmtoppm](/man/pgmtoppm)(1), [ppmtogif](/man/ppmtogif)(1), [netpbm](/man/netpbm)(1)"]],"e":["pgm","pgmtoppm","ppmtogif","netpbm"],"x":"hipstopgm [file.hip] > [output.pgm]"},"hledger-incomestatement":{"n":"hledger-incomestatement","t":"displays an income statement showing revenues and expenses for the report","s":[["TAGLINE","displays an income statement showing revenues and expenses for the report"],["TLDR","**Show income statement**\n\n```hledger incomestatement```\n\n**Show from specific file**\n\n```hledger incomestatement -f [journal.ledger]```\n\n**Show for specific period**\n\n```hledger incomestatement -p \"[this month]\"```\n\n**Show monthly breakdown**\n\n```hledger incomestatement --monthly```\n\n**Limit account depth**\n\n```hledger incomestatement --depth [2]```"],["SYNOPSIS","**hledger** **incomestatement** | **is** [_options_]"],["PARAMETERS","**-f**, **--file** _file_\n> Use specified journal file.\n\n**-p**, **--period** _expr_\n> Report period (e.g., \"this month\", \"2024\").\n\n**--monthly**\n> Show monthly columns.\n\n**--quarterly**\n> Show quarterly columns.\n\n**--yearly**\n> Show yearly columns.\n\n**--depth** _n_\n> Limit account depth.\n\n**--tree**\n> Show accounts as tree.\n\n**--flat**\n> Show accounts as flat list (default).\n\n**--average**\n> Show a row average column.\n\n**--row-total**\n> Show a row total column.\n\n**--no-total**\n> Omit the final total row.\n\n**--sort-amount**\n> Sort by amount instead of account name.\n\n**--output-format** _fmt_\n> Output format (txt, html, csv, tsv, json)."],["DESCRIPTION","**hledger incomestatement** (alias **is**) displays an income statement (profit & loss report) showing revenues and expenses for the report period, and their difference as net income. Part of hledger's standard financial reports alongside balance sheet and cash flow."],["INSTALL","```apt: sudo apt install hledger```\n\n```dnf: sudo dnf install hledger```\n\n```pacman: sudo pacman -S hledger```\n\n```apk: sudo apk add hledger```\n\n```zypper: sudo zypper install hledger```\n\n```brew: brew install hledger```\n\n```nix: nix profile install nixpkgs#hledger```"],["SEE ALSO","[hledger](/man/hledger)(1), [hledger-balancesheet](/man/hledger-balancesheet)(1)"]],"e":["hledger","hledger-balancesheet"],"x":"hledger incomestatement"},"html5validator":{"n":"html5validator","t":"command-line HTML5 validation tool using the Nu Html Checker","s":[["TAGLINE","command-line HTML5 validation tool using the Nu Html Checker"],["TLDR","**Validate an HTML file**\n\n```html5validator [file.html]```\n\n**Validate all HTML in directory**\n\n```html5validator --root [path/to/directory]```\n\n**Show warnings**\n\n```html5validator --show-warnings [file.html]```\n\n**Match custom file pattern**\n\n```html5validator --root [path] --match \"*.html *.php\"```\n\n**Exclude directories**\n\n```html5validator --root [path] --blacklist \"node_modules vendor\"```\n\n**Output as JSON**\n\n```html5validator --format json [file.html]```\n\n**Ignore specific errors**\n\n```html5validator --ignore-re 'Attribute \"ng-[a-z-]+\" not allowed' [file.html]```"],["SYNOPSIS","**html5validator** [_options_] [_files_]"],["PARAMETERS","**--root** _dir_\n> Start directory for file search.\n\n**--match** _pattern_\n> File patterns to match (default *.html).\n\n**--blacklist** _dirs_\n> Directory names to skip.\n\n**--show-warnings**\n> Show warnings as errors.\n\n**--format** _fmt_\n> Output format: gnu, xml, json, text.\n\n**--ignore-re** _regex_\n> Ignore errors matching regex.\n\n**--ignore** _message_\n> Ignore errors that exactly match the given message.\n\n**--also-check-css**\n> Also validate CSS files (matches *.html and *.css by default).\n\n**--config** _file_\n> Read options from a configuration file."],["DESCRIPTION","**html5validator** is a command-line HTML5 validation tool using the Nu Html Checker (v.Nu). Designed for static site generators and continuous integration. Returns exit code 0 for valid HTML. Requires Python 3.6+ and Java 8."],["CAVEATS","Requires a working **Java** runtime (OpenJDK 8+) on the **PATH**, since it shells out to the bundled **vnu.jar** to perform the actual validation."],["INSTALL","```nix: nix profile install nixpkgs#html5validator```"],["SEE ALSO","[tidy](/man/tidy)(1)"],["RESOURCES","```[Source code](https://github.com/svenkreiss/html5validator)```"]],"e":["tidy"],"x":"html5validator [file.html]"},"isd":{"n":"isd","t":"Interactive systemd unit manager TUI","s":[["TAGLINE","Interactive systemd unit manager TUI"],["TLDR","**Launch isd** to browse systemd units\n\n```isd```\n\n**Browse user units**\n\n```isd --user```"],["SYNOPSIS","**isd** [**--user**] [_options_]"],["DESCRIPTION","**isd** (interactive systemd) is a TUI for managing **systemd** units with fuzzy search, auto-refreshing previews, and smart sudo handling. It provides a fluid layout that adapts to terminal size, a command palette with keyboard shortcuts, and YAML configuration with auto-completion.\n\nThe tool offers features for both power users and newcomers, including the ability to switch between system and user units, pager support for viewing unit details, and input state caching for frequently used entries."],["CAVEATS","Requires **Python 3.11** or newer. Only works on Linux systems with systemd. Some operations require root privileges, which isd handles with smart sudo detection."],["HISTORY","**isd** was created by **kainctl** and is written in **Python**. It was designed as a more user-friendly alternative to the systemctl command for daily systemd management tasks."],["INSTALL","```pacman: sudo pacman -S isd```\n\n```nix: nix profile install nixpkgs#isd```"],["SEE ALSO","[systemctl](/man/systemctl)(1), [journalctl](/man/journalctl)(1)"]],"e":["systemctl","journalctl"],"x":"isd"},"jco":{"n":"jco","t":"JavaScript tooling for WebAssembly Components","s":[["TAGLINE","JavaScript tooling for WebAssembly Components"],["TLDR","**Transpile WASM component**\n\n```jco transpile [component.wasm] -o [output/]```\n\n**Create component from module**\n\n```jco componentize [module.wasm] -o [component.wasm]```\n\n**Inspect component**\n\n```jco print [component.wasm]```\n\n**Run component**\n\n```jco run [component.wasm]```\n\n**Generate types**\n\n```jco types [component.wasm] -o [types/]```"],["SYNOPSIS","**jco** _command_ [_options_] _file_"],["PARAMETERS","**transpile** _FILE_\n> Convert to JavaScript.\n\n**componentize** _FILE_\n> Create component from core module.\n\n**print** _FILE_\n> Print component info.\n\n**run** _FILE_\n> Execute component.\n\n**types** _FILE_\n> Generate TypeScript types.\n\n**-o** _DIR_\n> Output directory.\n\n**--help**\n> Display help information."],["DESCRIPTION","**jco** is JavaScript tooling for WebAssembly Components. It transpiles WASM components to JavaScript for browser/Node.js use.\n\nThe tool works with the component model standard. It generates JavaScript bindings and TypeScript definitions."],["CAVEATS","Experimental WASM standard. Bytecode Alliance project. Node.js tool."],["HISTORY","jco was created by the **Bytecode Alliance** as JavaScript tooling for the WebAssembly Component Model standard."],["SEE ALSO","[wasmtime](/man/wasmtime)(1)"]],"e":["wasmtime"],"x":"jco transpile [component.wasm] -o [output/]"},"jhipster":{"n":"jhipster","t":"development platform for generating modern web applications and microservices","s":[["TAGLINE","development platform for generating modern web applications and microservices"],["TLDR","**Generate new application**\n\n```jhipster```\n\n**Import JDL file to generate entities**\n\n```jhipster import-jdl [entities.jdl]```\n\n**Generate entity**\n\n```jhipster entity [EntityName]```\n\n**Upgrade JHipster**\n\n```jhipster upgrade```\n\n**Generate CI/CD configuration**\n\n```jhipster ci-cd```\n\n**Generate Docker Compose files**\n\n```jhipster docker-compose```\n\n**Show info**\n\n```jhipster info```"],["SYNOPSIS","**jhipster** [_command_] [_options_]"],["SUBCOMMANDS","**app**\n> Generate new application (default).\n\n**entity** _name_\n> Generate entity and CRUD.\n\n**import-jdl** _file_\n> Import JDL file for entities.\n\n**upgrade**\n> Upgrade JHipster version.\n\n**ci-cd**\n> Generate CI/CD configuration.\n\n**docker-compose**\n> Generate Docker Compose files.\n\n**kubernetes**\n> Generate Kubernetes configuration.\n\n**info**\n> Display environment info."],["DESCRIPTION","**JHipster** is a development platform for generating modern web applications and microservices. Creates production-ready Spring Boot backends with Angular, React, or Vue frontends. Includes authentication, monitoring, API documentation, and database configurations out of the box."],["INSTALL","```brew: brew install jhipster```"],["SEE ALSO","[npm](/man/npm)(1), [mvn](/man/mvn)(1), [spring](/man/spring)(1)"]],"e":["npm","mvn","spring"],"x":"jhipster"},"jpegtran":{"n":"jpegtran","t":"performs lossless transformations on JPEG images","s":[["TAGLINE","performs lossless transformations on JPEG images"],["TLDR","**Mirror** an image horizontally or vertically\n\n```jpegtran -flip [horizontal|vertical] [path/to/image.jpg] > [path/to/output.jpg]```\n\n**Rotate** an image 90, 180, or 270 degrees clockwise\n\n```jpegtran -rotate [90|180|270] [path/to/image.jpg] > [path/to/output.jpg]```\n\n**Transpose** across upper-left to lower-right axis\n\n```jpegtran -transpose [path/to/image.jpg] > [path/to/output.jpg]```\n\nConvert to **grayscale**\n\n```jpegtran -grayscale [path/to/image.jpg] > [path/to/output.jpg]```\n\n**Crop** to specified dimensions\n\n```jpegtran -crop [W]x[H]+[X]+[Y] [path/to/image.jpg] > [path/to/output.jpg]```\n\nCrop and save to **specific file**\n\n```jpegtran -crop [W]x[H] -outfile [path/to/output.jpg] [path/to/image.jpg]```"],["SYNOPSIS","**jpegtran** [_options_] [_file_]"],["PARAMETERS","**-flip** _horizontal|vertical_\n> Mirror the image\n\n**-rotate** _90|180|270_\n> Rotate clockwise by specified degrees\n\n**-transpose**\n> Transpose across main diagonal\n\n**-transverse**\n> Transpose across anti-diagonal\n\n**-grayscale**\n> Convert to grayscale\n\n**-crop** _WxH+X+Y_\n> Crop to width W, height H, at offset X,Y\n\n**-outfile** _FILE_\n> Write output to specified file\n\n**-optimize**\n> Optimize Huffman tables\n\n**-progressive**\n> Create progressive JPEG"],["DESCRIPTION","**jpegtran** performs lossless transformations on JPEG images. Unlike re-encoding, these operations work directly on the DCT coefficients, preserving image quality completely.\n\nSupported lossless operations include rotation (in 90-degree increments), flipping, transposition, grayscale conversion, and cropping (on MCU boundaries). The tool can also optimize or convert between baseline and progressive formats."],["CAVEATS","Crop dimensions may be adjusted to MCU (Minimum Coded Unit) boundaries. Some transformations may require -trim to discard partial edge blocks. Output goes to stdout by default; redirect or use -outfile."],["HISTORY","jpegtran is part of libjpeg/libjpeg-turbo, the reference JPEG library. The original libjpeg was developed by the Independent JPEG Group starting in **1991**."],["INSTALL","```apt: sudo apt install libjpeg-turbo-progs```\n\n```dnf: sudo dnf install libjpeg-turbo-utils```\n\n```apk: sudo apk add libjpeg-turbo-utils```"],["SEE ALSO","[cjpeg](/man/cjpeg)(1), [djpeg](/man/djpeg)(1), [convert](/man/convert)(1)"]],"e":["cjpeg","djpeg","convert"],"x":"jpegtran -flip [horizontal|vertical] [path/to/image.jpg] > [path/to/output.jpg]"},"klog":{"n":"klog","t":"Plain-text file format and CLI for time tracking","s":[["TAGLINE","Plain-text file format and CLI for time tracking"],["TLDR","**Start** a stopwatch entry for today\n\n```klog start```\n\n**Stop** the open range\n\n```klog stop```\n\n**Show today** summary\n\n```klog today```\n\n**Print** records from a file\n\n```klog print [times.klg]```\n\n**Create** a new day record\n\n```klog create```\n\n**Track** a finished duration entry\n\n```klog track [1h30m]```\n\n**Report** totals by period\n\n```klog report```\n\n**Total** time in files\n\n```klog total [*.klg]```\n\n**List tags**\n\n```klog tags```"],["SYNOPSIS","**klog** *command* [*options*] [*files*...]"],["DESCRIPTION","**klog** is both a plain-text time-tracking file format and a command-line tool to create, edit, and analyse those files. Records live in editable text (typically **.klg**), so they work with git, backups, and any editor.\n\nCommon workflow: **start** / **stop** for open time ranges, **track** for completed durations, **today** / **print** / **report** / **total** for summaries, and **edit** / **create** for manual changes. Bookmarks and config help point at default files.\n\nInstall from https://klog.jotaen.net (prebuilt binaries and packages). The CLI is MIT-licensed; the file format specification is public domain (CC0/OWFa). Tool and format version numbers are independent."],["PARAMETERS","**start** / **stop** / **pause** / **switch**\n\n> Control an open range (stopwatch-style tracking).\n\n**today**\n\n> Show or work with the current day record.\n\n**track** [*duration*]\n\n> Add a completed time entry.\n\n**print** / **report** / **total**\n\n> Display and aggregate time from klog files.\n\n**create** / **edit** / **goto**\n\n> Create or open records for editing.\n\n**tags** / **bookmarks** / **config** / **info** / **json** / **version**\n\n> Metadata, configuration, machine-readable output, and version.\n\nSee **klog** *command* **--help** and https://klog.jotaen.net for the full flag set and file format."],["CAVEATS","Learn the record syntax (date headers, ranges, tags) from the official docs before bulk edits. Filtering and multi-file behaviour depend on your version; check **--help**."],["INSTALL","```apt: sudo apt install klog```\n\n```dnf: sudo dnf install klog```\n\n```zypper: sudo zypper install klog```\n\n```brew: brew install klog```\n\n```nix: nix profile install nixpkgs#klog```"],["SEE ALSO","[timew](/man/timew)(1), [watson](/man/watson)(1), [ledger](/man/ledger)(1)"],["RESOURCES","```[Source code](https://github.com/jotaen/klog)```\n\n```[Homepage](https://klog.jotaen.net)```\n\n```[Documentation](https://klog.jotaen.net)```"]],"e":["timew","watson","ledger"],"x":"klog start"},"lvmdiskscan":{"n":"lvmdiskscan","t":"scans all SCSI, IDE, and other disks visible to the system and lists devices","s":[["TAGLINE","scans all SCSI, IDE, and other disks visible to the system and lists devices"],["TLDR","**Scan all devices** that may be used as physical volumes\n\n```lvmdiskscan```\n\nShow only **physical volumes** (PVs)\n\n```lvmdiskscan -l```\n\nIncrease **verbosity** (repeat for more detail)\n\n```lvmdiskscan -v```"],["SYNOPSIS","**lvmdiskscan** [_options_]"],["PARAMETERS","**-l, --lvmpartition**\n> Show only partitions that are LVM physical volumes\n\n**-v, --verbose**\n> Increase verbosity level; can be repeated for more detail"],["DESCRIPTION","**lvmdiskscan** scans all SCSI, IDE, and other disks visible to the system and lists devices that may be used as physical volumes by LVM. It reports each device's size and whether it is already an LVM physical volume.\n\nThe output shows partitions and whole disks, indicating which ones contain LVM metadata. This is useful for identifying available storage before creating new physical volumes."],["CAVEATS","This command is **deprecated**; use **pvs** for listing physical volumes and standard tools like **lsblk** for device enumeration. The scan can be slow on systems with many devices. Requires root privileges for complete device information."],["INSTALL","```apt: sudo apt install lvm2```\n\n```dnf: sudo dnf install lvm2```\n\n```pacman: sudo pacman -S lvm2```\n\n```apk: sudo apk add lvm2```\n\n```zypper: sudo zypper install lvm2```\n\n```nix: nix profile install nixpkgs#lvm2```"],["SEE ALSO","[pvs](/man/pvs)(8), [pvscan](/man/pvscan)(8), [lsblk](/man/lsblk)(8), [lvm](/man/lvm)(8)"]],"e":["pvs","pvscan","lsblk","lvm"],"x":"lvmdiskscan"},"makepasswd":{"n":"makepasswd","t":"generates random passwords suitable for user accounts","s":[["TAGLINE","generates random passwords suitable for user accounts"],["TLDR","**Generate random password**\n\n```makepasswd```\n\n**Generate multiple passwords**\n\n```makepasswd --count [5]```\n\n**Generate password of specific length**\n\n```makepasswd --chars [16]```\n\n**Generate with specific character set**\n\n```makepasswd --string '[A-Za-z0-9!@#]'```\n\n**Generate a crypted password** (for /etc/shadow, MD5 scheme)\n\n```makepasswd --crypt-md5```\n\n**Use a fixed random seed** for reproducible output\n\n```makepasswd --randomseed [42] --count [3]```\n\n**Specify minimum and maximum length**\n\n```makepasswd --minchars [8] --maxchars [12]```"],["SYNOPSIS","**makepasswd** [_--count n_] [_--chars n_] [_--string chars_] [_--crypt_] [_options_]"],["PARAMETERS","**--count** _N_\n> Generate N passwords.\n\n**--chars** _N_\n> Password length.\n\n**--minchars** _N_\n> Minimum length.\n\n**--maxchars** _N_\n> Maximum length.\n\n**--string** _CHARS_\n> Characters to use.\n\n**--crypt**\n> Output DES-crypted password.\n\n**--crypt-md5**\n> Output MD5-crypted password.\n\n**--clearfrom** _FILE_\n> Read cleartext from file.\n\n**--cryptsalt** _N_\n> Salt for crypt.\n\n**--randomseed** _N_\n> Seed the random generator with _N_ (use **0** for true randomness, a fixed value for reproducibility).\n\n**--rerandom** _N_\n> Re-seed the random generator every _N_ passwords.\n\n**--repeatpass** _N_\n> Emit each generated password _N_ times (useful with **--crypt** to pair cleartext with hash).\n\n**--nocrypt**\n> Skip cryptographic hashing and output the cleartext only.\n\n**--verbose**, **--noverbose**\n> Toggle explanatory output."],["DESCRIPTION","**makepasswd** generates random passwords suitable for user accounts. It uses /dev/urandom for randomness, producing cryptographically suitable output.\n\nBy default, it generates a single password of reasonable length using letters and digits. The string option customizes the character set for specific password policies.\n\nThe crypt options produce password hashes suitable for /etc/shadow. MD5 crypt produces hashes starting with $1$, compatible with modern systems. DES crypt is legacy and limited to 8 characters.\n\nBatch generation with count is useful for provisioning multiple accounts. Combined with proper length and character requirements, it meets most password policies.\n\nFor scripting, passwords output one per line. When combined with crypt, both cleartext and hash appear for reference."],["CAVEATS","DES crypt limited to 8 characters - use MD5. Random passwords hard to remember. Generated passwords should be changed by users. Some special characters may cause shell issues."],["HISTORY","**makepasswd** was written by **Rob Browning** for **Debian** in the **1990s**. It provided a simple tool for system administrators to generate passwords during account creation. The package remains in Debian and derived distributions."],["INSTALL","```dnf: sudo dnf install makepasswd```\n\n```apk: sudo apk add makepasswd```"],["SEE ALSO","[pwgen](/man/pwgen)(1), [apg](/man/apg)(1), [openssl](/man/openssl)(1), [passwd](/man/passwd)(1)"]],"e":["pwgen","apg","openssl","passwd"],"x":"makepasswd"},"nix-edit":{"n":"nix-edit","t":"opens the Nix expression for a package in your editor","s":[["TAGLINE","opens the Nix expression for a package in your editor"],["TLDR","**Edit package definition**\n\n```nix edit nixpkgs#[package]```\n\n**Edit with specific editor**\n\n```EDITOR=vim nix edit nixpkgs#[package]```"],["SYNOPSIS","**nix edit** _installable_ [_options_]"],["PARAMETERS","_installable_\n> Package to edit (e.g., nixpkgs#hello)."],["DESCRIPTION","**nix edit** opens the Nix expression for a package in your editor. Useful for inspecting or modifying package definitions. Uses $EDITOR environment variable."],["INSTALL","```apt: sudo apt install nix-bin```\n\n```dnf: sudo dnf install nix```\n\n```pacman: sudo pacman -S nix```\n\n```apk: sudo apk add nix```\n\n```zypper: sudo zypper install nix```\n\n```nix: nix profile install nixpkgs#nix```"],["SEE ALSO","[nix](/man/nix)(1), [nix-search](/man/nix-search)(1)"]],"e":["nix","nix-search"],"x":"nix edit nixpkgs#[package]"},"openssl-s_client":{"n":"openssl-s_client","t":"TLS/SSL client for testing and debugging connections","s":[["TAGLINE","TLS/SSL client for testing and debugging connections"],["TLDR","**Connect to HTTPS server**\n\n```openssl s_client -connect [example.com]:443```\n\n**Connect with SNI**\n\n```openssl s_client -connect [example.com]:443 -servername [example.com]```\n\n**Show certificate chain**\n\n```openssl s_client -connect [example.com]:443 -showcerts```\n\n**Connect to SMTP with STARTTLS**\n\n```openssl s_client -connect [mail.example.com]:587 -starttls smtp```\n\n**Verify certificate**\n\n```openssl s_client -connect [example.com]:443 -verify_return_error```\n\n**Test specific protocol**\n\n```openssl s_client -connect [example.com]:443 -tls1_3```"],["SYNOPSIS","**openssl s_client** [_options_] -connect _host:port_"],["PARAMETERS","**-connect** _host:port_\n> Server to connect to.\n\n**-servername** _name_\n> SNI hostname.\n\n**-showcerts**\n> Show certificate chain.\n\n**-starttls** _proto_\n> STARTTLS protocol.\n\n**-verify** _depth_\n> Verify depth.\n\n**-CAfile** _file_\n> CA certificates.\n\n**-tls1_2**, **-tls1_3**\n> Specific TLS version.\n\n**-cipher** _list_\n> Cipher list."],["DESCRIPTION","**openssl s_client** is a TLS/SSL client for testing and debugging connections. It establishes SSL/TLS connections and displays certificate and connection information.\n\nAfter connecting, you can type protocol commands (HTTP, SMTP, etc.)."],["EXAMPLE","```bash"],["Test HTTPS","openssl s_client -connect example.com:443 <<< \"GET / HTTP/1.1\\r\\nHost: example.com\\r\\n\\r\\n\"\n```"],["CAVEATS","Interactive by default; use <<< or EOF for scripting. Certificate verification may fail without CAfile."],["HISTORY","s_client has been part of OpenSSL since early versions, essential for SSL/TLS testing and debugging."],["INSTALL","```apt: sudo apt install openssl```\n\n```dnf: sudo dnf install openssl```\n\n```pacman: sudo pacman -S openssl```\n\n```apk: sudo apk add openssl```\n\n```zypper: sudo zypper install openssl```\n\n```brew: brew install openssl```\n\n```nix: nix profile install nixpkgs#openssl```"],["SEE ALSO","[openssl-verify](/man/openssl-verify)(1), [curl](/man/curl)(1)"]],"e":["openssl-verify","curl"],"x":"openssl s_client -connect [example.com]:443"},"pbmtogem":{"n":"pbmtogem","t":"Convert a PBM image to GEM .img format","s":[["TAGLINE","Convert a PBM image to GEM .img format"],["TLDR","**Convert a PBM image to GEM IMG format**\n\n```pbmtogem [input.pbm] > [output.img]```\n\n**Convert from stdin**\n\n```cat [input.pbm] | pbmtogem > [output.img]```"],["SYNOPSIS","**pbmtogem** [_pbmfile_]"],["DESCRIPTION","**pbmtogem** reads a PBM image as input and produces a compressed GEM .img file as output. GEM (Graphics Environment Manager) was a GUI system used on Atari ST computers and early IBM PCs.\n\nIf no input file is specified, the program reads from standard input. The output is always written to standard output. This tool does not support compression of repeated lines.\n\nPart of the **Netpbm** toolkit."],["CAVEATS","Does not support compression of repeated lines in the output. The GEM IMG format is largely obsolete and mainly of historical interest."],["HISTORY","pbmtogem was created by **David Beckemeyer** and **Jef Poskanzer** as part of the Netpbm suite."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[gemtopbm](/man/gemtopbm)(1), [gemtopnm](/man/gemtopnm)(1), [pbmtomacp](/man/pbmtomacp)(1)"]],"e":["gemtopbm","gemtopnm","pbmtomacp"],"x":"pbmtogem [input.pbm] > [output.img]"},"pct-stop":{"n":"pct-stop","t":"abruptly stops a Proxmox LXC container","s":[["TAGLINE","abruptly stops a Proxmox LXC container"],["TLDR","**Stop** a container immediately\n\n```pct stop 100```\n\nAbort active **shutdown** tasks before stopping\n\n```pct stop 100 --overrule-shutdown```"],["SYNOPSIS","**pct stop** _vmid_ [_OPTIONS_]"],["PARAMETERS","**vmid**\n> The numeric ID of the container (100–999999999)\n\n**--overrule-shutdown**\n> Try to abort active vzshutdown tasks before stopping (default off)\n\n**--skiplock**\n> Ignore locks; only root is allowed to use this option"],["DESCRIPTION","**pct stop** abruptly stops all processes running in a Proxmox VE LXC container. Unlike **pct shutdown**, which requests a clean power-off, this is a hard stop comparable to pulling the plug: processes do not get a normal chance to flush data or exit cleanly.\n\nUse **pct shutdown** when you want a graceful stop, and **pct stop** when the container is unresponsive or you need an immediate halt."],["CAVEATS","A hard stop can leave applications with unsaved data or inconsistent files. Prefer **pct shutdown** for normal operations. Locks from concurrent management tasks may block the command unless **--skiplock** is used carefully by root."],["HISTORY","**pct stop** is part of the **Proxmox VE** **pct** tool for managing LXC containers."],["SEE ALSO","[pct](/man/pct)(1), [pct-shutdown](/man/pct-shutdown)(1), [pct-start](/man/pct-start)(1), [pct-status](/man/pct-status)(1)"],["RESOURCES","```[Documentation](https://pve.proxmox.com/pve-docs/pct.1.html)```\n\n```[Homepage](https://www.proxmox.com)```"]],"e":["pct","pct-shutdown","pct-start","pct-status"],"x":"pct stop 100"},"pnmsplit":{"n":"pnmsplit","t":"Split multi-image PNM files into separate files","s":[["TAGLINE","Split multi-image PNM files into separate files"],["TLDR","**Split multi-image PNM file**\n\n```pnmsplit [input.pnm] [output%d.pnm]```\n\n**Split with custom naming**\n\n```pnmsplit [input.pnm] [frame_%03d.pnm]```"],["SYNOPSIS","**pnmsplit** [_options_] _input_ _output_"],["PARAMETERS","_INPUT_\n> Multi-image PNM file.\n\n_OUTPUT_\n> Output filename pattern with %d.\n\n**-padname** _N_\n> Zero-padding width for numbers."],["DESCRIPTION","**pnmsplit** splits a multi-image PNM stream into individual files. PNM files can contain multiple images concatenated together, and this tool extracts each one into a separate output file.\n\nThe output filename pattern must contain a **%d** format specifier (or variant like **%03d**) which is replaced with the image sequence number starting from 0. The **-padname** option controls zero-padding width for the numbering. This is useful for processing animation frames or batch image sequences. Part of the Netpbm toolkit."],["CAVEATS","Requires %d in output name. Part of Netpbm suite."],["HISTORY","pnmsplit was created as part of **Netpbm** for multi-image file handling."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pnmcat](/man/pnmcat)(1), [pnmindex](/man/pnmindex)(1), [pamcut](/man/pamcut)(1)"]],"e":["pnmcat","pnmindex","pamcut"],"x":"pnmsplit [input.pnm] [output%d.pnm]"},"ps2pdf":{"n":"ps2pdf","t":"Convert PostScript files to PDF format","s":[["TAGLINE","Convert PostScript files to PDF format"],["TLDR","**Convert PS to PDF**\n\n```ps2pdf [input.ps] [output.pdf]```\n\n**Convert with quality settings**\n\n```ps2pdf -dPDFSETTINGS=/prepress [input.ps] [output.pdf]```\n\n**Convert for screen viewing**\n\n```ps2pdf -dPDFSETTINGS=/screen [input.ps] [output.pdf]```\n\n**Convert EPS to PDF**\n\n```ps2pdf [input.eps] [output.pdf]```"],["SYNOPSIS","**ps2pdf** [_options_] _input.ps_ [_output.pdf_]"],["DESCRIPTION","**ps2pdf** converts PostScript files to PDF format using Ghostscript. It's a convenient wrapper around gs (Ghostscript) with PDF output options.\n\nThe tool is commonly used in document processing pipelines and print workflows."],["PARAMETERS","**-dPDFSETTINGS=** _setting_\n> Quality preset:\n> - /screen: Low resolution\n> - /ebook: Medium resolution\n> - /printer: High resolution\n> - /prepress: Maximum resolution\n\n**-dCompatibilityLevel=** _level_\n> PDF version (1.3, 1.4, 1.5, etc.).\n\n**-dEmbedAllFonts=** _bool_\n> Embed all fonts.\n\n**-dSubsetFonts=** _bool_\n> Subset embedded fonts.\n\n**-sPAPERSIZE=** _size_\n> Paper size (a4, letter, etc.)."],["CAVEATS","Output quality depends on input. Font embedding may increase size. Some PS features may not convert. Ghostscript must be installed."],["HISTORY","**ps2pdf** is part of **Ghostscript**, originally created by **L. Peter Deutsch** in **1988**. Ghostscript provides PostScript and PDF interpretation, making ps2pdf a standard conversion tool."],["INSTALL","```apt: sudo apt install ghostscript```\n\n```dnf: sudo dnf install ghostscript```\n\n```pacman: sudo pacman -S ghostscript```\n\n```apk: sudo apk add ghostscript```\n\n```zypper: sudo zypper install ghostscript```\n\n```brew: brew install ghostscript```\n\n```nix: nix profile install nixpkgs#ghostscript```"],["SEE ALSO","[gs](/man/gs)(1), [pdf2ps](/man/pdf2ps)(1), [epstopdf](/man/epstopdf)(1), [pdflatex](/man/pdflatex)(1)"]],"e":["gs","pdf2ps","epstopdf","pdflatex"],"x":"ps2pdf [input.ps] [output.pdf]"},"pulumi-plugin":{"n":"pulumi-plugin","t":"Manage Pulumi provider plugins","s":[["TAGLINE","Manage Pulumi provider plugins"],["TLDR","**List installed plugins**\n\n```pulumi plugin ls```\n\n**Install plugin**\n\n```pulumi plugin install resource [name] [version]```\n\n**Remove plugin**\n\n```pulumi plugin rm resource [name]```\n\n**Remove all unused plugins**\n\n```pulumi plugin rm --all```"],["SYNOPSIS","**pulumi** **plugin** _command_ [_options_]"],["PARAMETERS","**ls**\n> List installed plugins.\n\n**install** _kind_ _name_ _version_\n> Install plugin.\n\n**rm** _kind_ _name_\n> Remove plugin.\n\n**--all**\n> Remove all plugins.\n\n**--yes**\n> Skip confirmation."],["DESCRIPTION","**pulumi plugin** manages Pulumi plugins. Plugins provide resource providers for cloud platforms. Install, update, and remove plugins for different cloud providers and services."],["INSTALL","```pacman: sudo pacman -S pulumi```\n\n```apk: sudo apk add pulumi```\n\n```brew: brew install pulumi```\n\n```nix: nix profile install nixpkgs#pulumi```"],["SEE ALSO","[pulumi](/man/pulumi)(1), [pulumi-install](/man/pulumi-install)(1)"]],"e":["pulumi","pulumi-install"],"x":"pulumi plugin ls"},"pvesubscription":{"n":"pvesubscription","t":"Proxmox VE subscription key manager","s":[["TAGLINE","Proxmox VE subscription key manager"],["TLDR","**Show** current subscription info\n\n```pvesubscription get```\n\n**Set** a subscription key\n\n```pvesubscription set pveXc-xxxxxxxxxx```\n\n**Update** subscription info from the server\n\n```pvesubscription update```\n\nForce refresh even if local cache is valid\n\n```pvesubscription update --force```\n\n**Delete** the subscription key from this node\n\n```pvesubscription delete```"],["SYNOPSIS","**pvesubscription** _COMMAND_ [_ARGS_] [_OPTIONS_]"],["PARAMETERS","**get**\n> Read subscription info for this node\n\n**set** _key_\n> Set the Proxmox VE subscription key\n\n**update**\n> Refresh subscription info from the license server\n\n**--force**\n> With **update**: always contact the server even if the local cache is still valid\n\n**delete**\n> Remove the subscription key from this node\n\n**help**\n> Show help for pvesubscription or a subcommand"],["DESCRIPTION","**pvesubscription** manages the Proxmox VE subscription key on a node. A valid subscription enables access to the enterprise package repositories and official support channels. Without a subscription, nodes commonly use the free no-subscription repositories instead.\n\nThe tool can install or remove a key, display current status, and refresh validation data from Proxmox servers. Offline key handling is reserved for internal use; for offline environments Proxmox documents separate offline mirror tooling."],["CAVEATS","Subscription keys are node-specific and should not be shared across unrelated machines. Removing a key or running without a subscription does not remove Proxmox VE itself, but enterprise repo access and support entitlements change. Always verify repository configuration after changing subscription status."],["HISTORY","**pvesubscription** is part of the **Proxmox VE** administration CLI suite for managing commercial subscription licenses."],["SEE ALSO","[pvenode-config](/man/pvenode-config)(1), [pveversion](/man/pveversion)(1), [apt](/man/apt)(8)"],["RESOURCES","```[Documentation](https://pve.proxmox.com/pve-docs/pvesubscription.1.html)```\n\n```[Homepage](https://www.proxmox.com)```"]],"e":["pvenode-config","pveversion","apt"],"x":"pvesubscription get"},"pyflakes":{"n":"pyflakes","t":"Fast static error checker for Python","s":[["TAGLINE","Fast static error checker for Python"],["TLDR","**Check a Python file** for errors\n\n```pyflakes [file.py]```\n\n**Check multiple files**\n\n```pyflakes [file1.py] [file2.py]```\n\n**Check from stdin**\n\n```cat [file.py] | pyflakes```\n\n**Check a directory** recursively\n\n```pyflakes [path/to/directory]```"],["SYNOPSIS","**pyflakes** [_options_] [_files_or_directories_]"],["PARAMETERS","_FILES_\n> Python files or directories to check\n\n**--version**\n> Show program version and exit\n\n**-h**, **--help**\n> Show help message and exit"],["DESCRIPTION","**pyflakes** is a fast, lightweight static analysis tool that checks Python source files for common errors without executing them. It detects issues like undefined names, unused imports, redefined variables, and other logical mistakes that would cause runtime failures.\n\nUnlike **pylint** or **flake8**, pyflakes focuses exclusively on error detection and does not check code style or formatting. This narrow scope makes it significantly faster and produces no false positives for stylistic choices, making it well-suited for integration into editors and pre-commit hooks where speed matters."],["CAVEATS","Only checks for logical errors, not code style or formatting. Does not support configuration files for rule customization. Often used as a component within **flake8** rather than standalone."],["HISTORY","Pyflakes was created by Divmod for **fast static** analysis of Python code. It is now maintained by the PyCQA (Python Code Quality Authority) organization."],["INSTALL","```apk: sudo apk add py3-pyflakes```"],["SEE ALSO","[pylint](/man/pylint)(1), [flake8](/man/flake8)(1), [mypy](/man/mypy)(1), [ruff](/man/ruff)(1)"]],"e":["pylint","flake8","mypy","ruff"],"x":"pyflakes [file.py]"},"qsv":{"n":"qsv","t":"Blazing-fast data-wrangling toolkit for CSV and tabular data","s":[["TAGLINE","Blazing-fast data-wrangling toolkit for CSV and tabular data"],["TLDR","**Count rows in a CSV file**\n\n```qsv count [data.csv]```\n\n**Search for a pattern**\n\n```qsv search '[pattern]' [data.csv]```\n\n**Select specific columns**\n\n```qsv select [col1,col2] [data.csv]```\n\n**Run SQL queries on a CSV file**\n\n```qsv sqlp \"SELECT * FROM data WHERE value > 100\" [data.csv]```\n\n**Get summary statistics**\n\n```qsv stats [data.csv]```"],["SYNOPSIS","**qsv** _command_ [_options_] [_file_]"],["DESCRIPTION","**qsv** is a comprehensive command-line toolkit for querying, transforming, validating, and analyzing CSV and tabular data. It provides over 50 commands including sort, join, dedup, frequency, stats, search, validate, and an embedded Luau scripting DSL. It supports CSV, JSON, JSONL, Parquet, Arrow, Avro, and Excel formats with streaming, multithreaded processing for arbitrarily large files."],["HISTORY","**qsv** was created by **jqnatividad** (dathere) and is written in **Rust**."],["INSTALL","```pacman: sudo pacman -S qsv```\n\n```brew: brew install qsv```\n\n```nix: nix profile install nixpkgs#qsv```"],["SEE ALSO","[csvlens](/man/csvlens)(1), [xsv](/man/xsv)(1), [miller](/man/miller)(1), [cut](/man/cut)(1)"]],"e":["csvlens","xsv","miller","cut"],"x":"qsv count [data.csv]"},"rails-routes":{"n":"rails-routes","t":"Display URL routing table for Rails application","s":[["TAGLINE","Display URL routing table for Rails application"],["TLDR","**List all routes** in the application\n\n```rails routes```\n\n**Search for routes** matching a pattern\n\n```rails routes -g [pattern]```\n\n**Show routes for a specific controller**\n\n```rails routes -c [controller_name]```\n\n**Display routes in expanded format**\n\n```rails routes --expanded```"],["SYNOPSIS","**rails routes** [**-g** _pattern_] [**-c** _controller_] [**--expanded**]"],["PARAMETERS","**-g**, **--grep** _pattern_\n> Filter routes by pattern (matches against name, verb, path, or controller#action)\n\n**-c**, **--controller** _name_\n> Show routes for a specific controller only\n\n**--expanded**, **-E**\n> Print routes in expanded table format (one attribute per line)\n\n**-h**, **--help**\n> Show help information"],["DESCRIPTION","**rails routes** displays all routes defined in the Rails application. Routes map incoming URLs to controller actions and are defined in **config/routes.rb**.\n\nThe output shows the route name (used for URL helpers), HTTP verb, URL pattern, and the controller#action that handles the request. Named routes can be used with **_path** and **_url** suffixes in views and controllers.\n\nThe grep option is useful for large applications with many routes. It searches across all columns, making it easy to find routes by URL pattern, controller name, or route helper name."],["CAVEATS","Must be run from within a Rails application directory. The command loads the application environment, which may take time for large applications.\n\nRoutes are displayed in the order they are matched. The first matching route handles the request, so order matters when routes may overlap."],["SEE ALSO","[rails](/man/rails)(1), [rails-server](/man/rails-server)(1), [rake](/man/rake)(1)"]],"e":["rails","rails-server","rake"],"x":"rails routes"},"rpicam-raw":{"n":"rpicam-raw","t":"Capture raw sensor data from Raspberry Pi camera","s":[["TAGLINE","Capture raw sensor data from Raspberry Pi camera"],["TLDR","**Capture** raw video for specific duration (milliseconds)\n\n```rpicam-raw -t 2000 -o path/to/file.raw```\n\nCapture with **custom dimensions** and framerate\n\n```rpicam-raw -t 5000 --width 4056 --height 3040 -o path/to/file.raw --framerate 8```"],["SYNOPSIS","**rpicam-raw** [_options_]"],["PARAMETERS","**-t**, **--timeout** _ms_\n> Recording duration in milliseconds\n\n**-o**, **--output** _file_\n> Output file path\n\n**--width** _pixels_\n> Video width\n\n**--height** _pixels_\n> Video height\n\n**--framerate** _fps_\n> Frames per second"],["DESCRIPTION","**rpicam-raw** captures raw unprocessed video from a Raspberry Pi camera. Raw video preserves all sensor data without compression, suitable for post-processing.\n\nOutput files can be large due to the uncompressed nature of raw video."],["CAVEATS","Raw video files are very large. Ensure sufficient storage and fast media. Post-processing software required to view raw output."],["HISTORY","Part of **rpicam-apps**, the official Raspberry Pi camera application suite using libcamera."],["SEE ALSO","[rpicam-vid](/man/rpicam-vid)(1), [rpicam-still](/man/rpicam-still)(1)"]],"e":["rpicam-vid","rpicam-still"],"x":"rpicam-raw -t 2000 -o path/to/file.raw"},"safe-rm":{"n":"safe-rm","t":"Wrapper around rm that prevents deleting important files","s":[["TAGLINE","Wrapper around rm that prevents deleting important files"],["TLDR","**Delete a file** with protection against blacklisted paths\n\n```safe-rm [path/to/file]```\n\n**Recursively delete a directory** with protection checks\n\n```safe-rm -r [path/to/directory]```\n\n**Force delete files** while respecting protected paths\n\n```safe-rm -rf [path/to/directory]```\n\n**Delete multiple files** with verbose output\n\n```safe-rm -v [file1] [file2] [file3]```"],["SYNOPSIS","**safe-rm** [_rm options_] [_files_]"],["PARAMETERS","safe-rm accepts all standard **rm** options and passes them through to the real rm command after verifying paths are not protected.\n\n**-r**, **-R**, **--recursive**\n> Remove directories and their contents recursively\n\n**-f**, **--force**\n> Ignore nonexistent files and arguments, never prompt\n\n**-i**\n> Prompt before every removal\n\n**-v**, **--verbose**\n> Explain what is being done"],["DESCRIPTION","**safe-rm** is a wrapper around the rm command that prevents accidental deletion of important system files and directories. It checks arguments against a configurable blacklist before passing them to the real rm.\n\nWhen a user attempts to delete a protected path, safe-rm refuses the operation and displays a warning instead. This provides a safety net against catastrophic mistakes like **rm -rf /**.\n\nProtected paths are configured in **/etc/safe-rm.conf** for system-wide protection and **~/.safe-rm** for per-user settings. Each file contains one path per line. If both are empty, a default list of critical system paths is used.\n\nTo use safe-rm as the default rm, create a symlink in a directory that precedes /bin in your PATH:\n```ln -s /usr/bin/safe-rm /usr/local/bin/rm```"],["CONFIGURATION","**/etc/safe-rm.conf**\n> System-wide list of protected paths, one per line. Deletion of any listed path is blocked.\n\n**~/.safe-rm**\n> Per-user list of additional protected paths."],["CAVEATS","safe-rm cannot protect against all deletion patterns. While **rm -rf /usr/lib** is blocked if /usr/lib is protected, running **cd /usr/lib && rm -rf *** bypasses protection since the protected path itself is not an argument."],["HISTORY","safe-rm was created to prevent accidental deletion of critical system files. Version 1.0.0 released in **November 2020** was a complete rewrite in **Rust**, replacing the original implementation. The project is licensed under GPLv3."],["INSTALL","```apt: sudo apt install safe-rm```\n\n```brew: brew install safe-rm```\n\n```nix: nix profile install nixpkgs#safe-rm```"],["SEE ALSO","[rm](/man/rm)(1), [trash-cli](/man/trash-cli)(1), [shred](/man/shred)(1)"]],"e":["rm","trash-cli","shred"],"x":"safe-rm [path/to/file]"},"sg_inq":{"n":"sg_inq","t":"Query SCSI device inquiry data","s":[["TAGLINE","Query SCSI device inquiry data"],["TLDR","**Query SCSI device** information\n\n```sudo sg_inq [/dev/sda]```\n\n**Get device identification** (serial number, WWN)\n\n```sudo sg_inq --id [/dev/sda]```\n\n**Display VPD page** by page number\n\n```sudo sg_inq --vpd --page=[0x83] [/dev/sda]```\n\n**Output response in hexadecimal**\n\n```sudo sg_inq --hex [/dev/sda]```\n\n**Decode version descriptors**\n\n```sudo sg_inq --descriptors [/dev/sda]```\n\n**Query ATA device** directly\n\n```sudo sg_inq --ata [/dev/sda]```\n\n**Output in JSON format**\n\n```sudo sg_inq --json [/dev/sda]```\n\n**Decode INQUIRY data from file**\n\n```sg_inq --inhex=[path/to/file.hex]```"],["SYNOPSIS","**sg_inq** [_options_] _DEVICE_\n\n**sg_inq** **--inhex=**_FN_ [_options_]"],["PARAMETERS","**-a**, **--ata**\n> Treat device as ATA/ATAPI and use ATA IDENTIFY command instead of SCSI INQUIRY.\n\n**-B**, **--block=**_0|1_\n> Control blocking mode when opening device. 0 for non-blocking, 1 for blocking.\n\n**-c**, **--cmddt**\n> Set Command Support Data bit for opcode queries. Obsolete in SPC-2.\n\n**-d**, **--descriptors**\n> Decode version descriptors from the INQUIRY response.\n\n**-e**, **--vpd**\n> Enable Vital Product Data bit to fetch VPD pages.\n\n**-f**, **--force**\n> Skip sanity checks before accessing VPD pages.\n\n**-H**, **--hex**\n> Output response in hexadecimal format.\n\n**-i**, **--id**\n> Decode device identification VPD page (0x83).\n\n**-I**, **--inhex=**_FN_\n> Decode INQUIRY response from file instead of querying device.\n\n**-j**, **--json**[=_JO_]\n> Output in JSON format.\n\n**-l**, **--len=**_LEN_\n> Set allocation length for response buffer.\n\n**-o**, **--only**\n> Only output standard INQUIRY response, skip serial number VPD.\n\n**-p**, **--page=**_PG_\n> Specify VPD page number or abbreviation to fetch.\n\n**-q**, **--quiet**\n> Suppress decoding output and errors.\n\n**-r**, **--raw**\n> Output response in binary format.\n\n**-s**, **--vendor**\n> Display vendor-specific fields as ASCII.\n\n**-v**, **--verbose**\n> Increase verbosity level.\n\n**-V**, **--version**\n> Display version information."],["DESCRIPTION","**sg_inq** sends a SCSI INQUIRY command to a device and outputs the decoded response. All SCSI devices are required to respond to a standard INQUIRY command with at least 36 bytes containing vendor, product, and revision information.\n\nThe utility supports Vital Product Data (VPD) pages which provide additional information such as device serial number (page 0x80), device identification with WWN (page 0x83), and block device characteristics. The **--id** option specifically decodes the device identification page which is mandatory for SPC-3 compliant devices.\n\nBeyond SCSI, sg_inq can query ATA/ATAPI devices using the **--ata** option and NVMe devices where it sends an Identify controller NVMe Admin command. This makes it useful for querying various storage device types on Linux systems."],["CAVEATS","Requires root privileges to access raw device nodes. On Linux 2.4 kernels, only SCSI generic devices (/dev/sg*) can be used. Linux 2.6+ supports block devices directly. VPD page information is no longer being added to this utility; use **sg_vpd** or **sdparm** for newer VPD pages."],["HISTORY","**sg_inq** is part of the **sg3_utils** package, a collection of utilities for SCSI devices on Linux. The sg3_utils project was created to provide user-space tools for interacting with SCSI devices through the Linux SCSI generic (sg) driver. The package has evolved to support ATA devices via SAT (SCSI/ATA Translation) and more recently NVMe devices."],["INSTALL","```apt: sudo apt install sg3-utils```\n\n```dnf: sudo dnf install sg3_utils```\n\n```pacman: sudo pacman -S sg3_utils```\n\n```apk: sudo apk add sg3_utils```\n\n```zypper: sudo zypper install sg3_utils```\n\n```nix: nix profile install nixpkgs#sg3_utils```"],["SEE ALSO","[lsscsi](/man/lsscsi)(1), [smartctl](/man/smartctl)(8), [hdparm](/man/hdparm)(8)"]],"e":["lsscsi","smartctl","hdparm"],"x":"sudo sg_inq [/dev/sda]"},"sinfo":{"n":"sinfo","t":"Display Slurm cluster partition and node status","s":[["TAGLINE","Display Slurm cluster partition and node status"],["TLDR","**Show** a quick summary overview of the cluster\n\n```sinfo -s```\n\n**View** the detailed status of all partitions\n\n```sinfo```\n\n**View** the detailed status of a specific partition\n\n```sinfo -p [partition_name]```\n\n**View** information about idle nodes\n\n```sinfo -t idle```\n\n**Summarise** dead nodes\n\n```sinfo -d```\n\n**List** dead nodes and their reasons\n\n```sinfo -R```"],["SYNOPSIS","**sinfo** [_options_]"],["PARAMETERS","**-s, --summarize**\n> Display summary of partitions\n\n**-p, --partition _name_**\n> Show specific partition\n\n**-t, --states _states_**\n> Filter by node state (idle, allocated, down, etc.)\n\n**-d, --dead**\n> Show only non-responding nodes\n\n**-R, --list-reasons**\n> List reasons nodes are down or drained\n\n**-l, --long**\n> Long output format with more details\n\n**-N, --Node**\n> Display node-centric output\n\n**-o, --format _format_**\n> Customize output format"],["DESCRIPTION","**sinfo** reports the state of partitions and nodes managed by Slurm. It shows information about available computing resources including node counts, partition limits, and current states.\n\nCommon node states include: **idle** (available), **allocated** (in use), **down** (unavailable), **drain** (draining), and **mix** (some CPUs allocated)."],["CAVEATS","Information reflects the current Slurm scheduler state, which may have brief delays from actual node status. Complex clusters may require filtering to view relevant information."],["INSTALL","```apt: sudo apt install slurm-client```"],["SEE ALSO","[squeue](/man/squeue)(1), [sbatch](/man/sbatch)(1), [scontrol](/man/scontrol)(1), [srun](/man/srun)(1)"]],"e":["squeue","sbatch","scontrol","srun"],"x":"sinfo -s"},"sqfstar":{"n":"sqfstar","t":"Create squashfs filesystem from tar archives","s":[["TAGLINE","Create squashfs filesystem from tar archives"],["TLDR","Create squashfs from **tar archive**\n\n```sqfstar < [archive.tar] [filesystem.squashfs]```\n\nCreate with **specific compression**\n\n```zcat [archive.tar.gz] | sqfstar -comp [gzip|lzo|lz4|xz|zstd] [filesystem.squashfs]```\n\nCreate and **exclude files**\n\n```xzcat [archive.tar.xz] | sqfstar [filesystem.squashfs] [file1] [file2]```\n\nExclude by **pattern**\n\n```zstdcat [archive.tar.zst] | sqfstar [filesystem.squashfs] \"*.gz\"```\n\nExclude by **regex**\n\n```lz4cat [archive.tar.lz4] | sqfstar [filesystem.squashfs] -regex \"[pattern]\"```\n\nSet **block size**\n\n```cat [archive.tar] | sqfstar -b [131072] [filesystem.squashfs]```"],["SYNOPSIS","**sqfstar** [_OPTIONS_] _FILESYSTEM_ [_exclude_files_...]"],["PARAMETERS","**-comp** _compressor_\n> Compression algorithm: gzip, lzo, lz4, xz, zstd, lzma\n\n**-b** _block_size_\n> Block size in bytes (4K-1MB, default: 128K)\n\n**-noI**\n> Do not compress inode table\n\n**-noD**\n> Do not compress data blocks\n\n**-noF**\n> Do not compress fragment blocks\n\n**-no-duplicates**\n> Do not detect duplicate files\n\n**-all-root**\n> Make all files owned by root\n\n**-force-uid** _uid_\n> Set all file UIDs\n\n**-force-gid** _gid_\n> Set all file GIDs\n\n**-regex**\n> Treat exclude patterns as POSIX regex\n\n**-ignore-zeros**\n> Allow concatenated tar archives\n\n**-mkfs-time** _time_\n> Set filesystem creation time\n\n**-all-time** _time_\n> Set all inode timestamps\n\n**-processors** _num_\n> Number of processors to use\n\n**-mem** _size_\n> Set memory limit\n\n**-progress**\n> Show progress bar\n\n**-info**\n> Print files as they are added"],["DESCRIPTION","**sqfstar** creates a squashfs filesystem directly from a tar archive read from stdin. Squashfs is a highly compressed read-only filesystem supporting multiple compression algorithms with block sizes from 4K to 1MB.\n\nThe tool is useful for converting tar archives to squashfs without intermediate extraction. Compressed tar archives should be decompressed before piping (using zcat, xzcat, etc.). Files can be excluded by name, wildcard pattern, or regex."],["CAVEATS","Input must be tar format; compressed tars need external decompression. Squashfs is read-only after creation. Large archives require significant memory during creation. Block size affects compression ratio and random access performance."],["HISTORY","**sqfstar** is part of **squashfs-tools**, created by **Phillip Lougher**. The sqfstar utility was added to provide direct tar-to-squashfs conversion without intermediate files. Squashfs is widely used in Linux distributions for live systems, snap packages, and container images."],["INSTALL","```apt: sudo apt install squashfs-tools```\n\n```dnf: sudo dnf install squashfs-tools```\n\n```pacman: sudo pacman -S squashfs-tools```\n\n```apk: sudo apk add squashfs-tools```"],["SEE ALSO","[mksquashfs](/man/mksquashfs)(1), [unsquashfs](/man/unsquashfs)(1), [sqfscat](/man/sqfscat)(1), [tar](/man/tar)(1)"]],"e":["mksquashfs","unsquashfs","sqfscat","tar"],"x":"sqfstar < [archive.tar] [filesystem.squashfs]"},"tectonic":{"n":"tectonic","t":"Modernized self-contained LaTeX engine","s":[["TAGLINE","Modernized self-contained LaTeX engine"],["TLDR","**Compile LaTeX document**\n\n```tectonic [document.tex]```\n\n**Compile with output directory**\n\n```tectonic -o [output/] [document.tex]```\n\n**Compile with synctex**\n\n```tectonic --synctex [document.tex]```\n\n**Keep intermediate files**\n\n```tectonic --keep-intermediates [document.tex]```\n\n**Print mode** (no network)\n\n```tectonic --only-cached [document.tex]```\n\n**Watch mode** (V1 CLI)\n\n```tectonic --watch [document.tex]```\n\n**Use bundle**\n\n```tectonic -b [bundle.tar] [document.tex]```\n\n**Build document** using V2 CLI (with Tectonic.toml)\n\n```tectonic -X build```\n\n**Create new document** workspace\n\n```tectonic -X new [project_name]```\n\n**Compile freestanding document** (V2 CLI)\n\n```tectonic -X compile [document.tex]```"],["SYNOPSIS","**tectonic** [_-o dir_] [_--synctex_] [_--keep-intermediates_] [_options_] _file.tex_"],["PARAMETERS","**-o** _DIR_\n> Output directory.\n\n**--synctex**\n> Generate SyncTeX data.\n\n**--keep-intermediates**\n> Keep .aux, .log, etc.\n\n**--keep-logs**\n> Keep log files.\n\n**--only-cached**\n> Don't download packages.\n\n**--watch**, **-w**\n> Watch and recompile.\n\n**-b** _FILE_\n> Use bundle file.\n\n**--print**\n> Print mode (PDF to stdout).\n\n**--format** _FMT_\n> Output format.\n\n**--untrusted**\n> Disable shell-escape.\n\n**-c** _FILE_\n> Config file.\n\n**-h**, **--help**\n> Show help.\n\n**-X** _SUBCOMMAND_\n> Activate V2 CLI (build, compile, new, init, dump, show, watch)."],["DESCRIPTION","**tectonic** is a modernized, self-contained LaTeX engine that automatically downloads packages, runs the correct number of compilation passes, and produces reproducible output. Unlike traditional TeX distributions, it requires no separate TeX Live installation — missing packages are fetched from the network on demand.\n\nThe engine automatically handles the multiple compilation passes that LaTeX typically requires for resolving references, tables of contents, bibliographies, and cross-references. Watch mode (**--watch**) recompiles whenever source files change, providing live preview when paired with a PDF viewer.\n\nTectonic uses bundles — self-contained package collections — to ensure reproducible builds where the same input always produces identical output. The **--only-cached** flag enables offline compilation using previously downloaded packages.\n\nThe V2 CLI (activated with **-X**) provides a cargo-like interface anchored around a **Tectonic.toml** file. Use `tectonic -X build` for project builds and `tectonic -X compile` for freestanding documents. This approach makes Tectonic well-suited for CI/CD pipelines and collaborative writing projects under version control."],["CAVEATS","Initial compilation downloads packages. Network required for new packages. Some exotic packages may be missing. Different from traditional TeX workflow."],["HISTORY","**Tectonic** was created by **Peter Williams** starting around **2016**. It builds on the XeTeX engine with modern tooling, addressing pain points of traditional TeX distributions."],["INSTALL","```pacman: sudo pacman -S tectonic```\n\n```apk: sudo apk add tectonic```\n\n```brew: brew install tectonic```\n\n```nix: nix profile install nixpkgs#tectonic```"],["SEE ALSO","[pdflatex](/man/pdflatex)(1), [xelatex](/man/xelatex)(1), [lualatex](/man/lualatex)(1), [latexmk](/man/latexmk)(1)"]],"e":["pdflatex","xelatex","lualatex","latexmk"],"x":"tectonic [document.tex]"},"tlmgr-path":{"n":"tlmgr-path","t":"Manage TeX Live system PATH integration","s":[["TAGLINE","Manage TeX Live system PATH integration"],["TLDR","**Add TeX Live to PATH**\n\n```tlmgr path add```\n\n**Remove from PATH**\n\n```tlmgr path remove```"],["SYNOPSIS","**tlmgr path** _action_"],["PARAMETERS","**add**\n> Add TeX Live to system PATH.\n\n**remove**\n> Remove TeX Live from system PATH."],["DESCRIPTION","**tlmgr path** manages system PATH integration for TeX Live. Creates or removes symlinks in /usr/local/bin for TeX programs. Part of TeX Live Manager."],["CAVEATS","Requires root privileges. May conflict with system TeX packages."],["SEE ALSO","[tlmgr](/man/tlmgr)(1)"]],"e":["tlmgr"],"x":"tlmgr path add"},"tofu-fmt":{"n":"tofu-fmt","t":"Format OpenTofu configuration files","s":[["TAGLINE","Format OpenTofu configuration files"],["TLDR","**Format current directory**\n\n```tofu fmt```\n\n**Format recursively**\n\n```tofu fmt -recursive```\n\n**Check formatting (exit non-zero if not formatted)**\n\n```tofu fmt -check```\n\n**Show diff of formatting changes**\n\n```tofu fmt -diff```\n\n**Format from stdin and write to stdout**\n\n```cat main.tf | tofu fmt -```"],["SYNOPSIS","**tofu** **fmt** [_options_] [_target_]"],["DESCRIPTION","**tofu fmt** rewrites OpenTofu configuration files to a canonical format and style. It applies a subset of the HCL language style conventions, along with other minor adjustments for readability. OpenTofu is the open-source fork of Terraform maintained by the Linux Foundation.\n\nBy default, **tofu fmt** scans the current directory for configuration files. If you provide a target directory, it will scan that directory. If a single file is provided, only that file is processed. A hyphen (_-_) as target reads from standard input."],["PARAMETERS","**-list**=_false_\n> Don't list the files containing formatting inconsistencies.\n\n**-write**=_false_\n> Don't overwrite the input files (implied by **-check** or when reading from stdin).\n\n**-diff**\n> Display diffs of formatting changes.\n\n**-check**\n> Check if the input is formatted. Exit status is 0 if all input is properly formatted; non-zero otherwise.\n\n**-recursive**\n> Also process files in subdirectories. By default, only the given directory is processed."],["CAVEATS","The command modifies files in place unless **-check**, **-write=false**, or stdin input is used. Formatting changes from **tofu fmt** may not be stable across major versions; re-run after upgrades. Syntactically valid but unformatted files are changed; invalid files produce errors without modification."],["HISTORY","**OpenTofu** was forked from **Terraform** in **2023** after HashiCorp changed Terraform's license to the Business Source License. The **fmt** subcommand was inherited from Terraform and remains largely compatible with **terraform fmt**."],["INSTALL","```dnf: sudo dnf install opentofu```\n\n```pacman: sudo pacman -S opentofu```\n\n```apk: sudo apk add opentofu```\n\n```zypper: sudo zypper install opentofu```\n\n```brew: brew install opentofu```\n\n```nix: nix profile install nixpkgs#opentofu```"],["SEE ALSO","[tofu](/man/tofu)(1), [terraform-fmt](/man/terraform-fmt)(1)"]],"e":["tofu","terraform-fmt"],"x":"tofu fmt"},"toipe":{"n":"toipe","t":"Terminal-based typing speed test","s":[["TAGLINE","Terminal-based typing speed test"],["TLDR","**Start a typing test** with the default settings\n\n```toipe```\n\n**Choose a built-in wordlist**\n\n```toipe -w [top1000]```\n\n**Use a custom wordlist** file\n\n```toipe -f [path/to/words.txt]```\n\n**Set the number of words** per test\n\n```toipe -n [50]```\n\n**List built-in** wordlists\n\n```toipe --list```"],["SYNOPSIS","**toipe** [_-w wordlist_] [_-f file_] [_-n count_] [_options_]"],["PARAMETERS","**-w** _WORDLIST_\n> Built-in wordlist name (top250, top500, top1000, ...).\n\n**-f** _FILE_\n> Read words from a custom newline-separated file.\n\n**-n** _COUNT_\n> Number of words to include in the test.\n\n**-p**, **--punctuation**\n> Include punctuation in generated text.\n\n**--list**\n> List available built-in wordlists and exit.\n\n**--help**\n> Display help."],["DESCRIPTION","**toipe** is a terminal-based typing speed test written in Rust. It draws random words from a wordlist, presents them as a single paragraph in the terminal, and tracks per-character errors as you type. At the end of a run it prints words per minute (WPM), characters per minute (CPM), and accuracy.\n\nSeveral built-in wordlists ship with the binary, grouped by difficulty. Custom lists can be loaded with **-f**, allowing practice on programming keywords, foreign-language vocabulary, or domain-specific text."],["CAVEATS","Terminal-based only; requires a terminal that supports raw mode. Performance reporting assumes a one-second resolution clock. The shipped wordlists are English-only, but **-f** accepts any UTF-8 file. Wordlists with characters outside the typical 7-bit ASCII range may not render correctly on legacy terminals."],["HISTORY","**toipe** was created as a terminal-based typing test application written in Rust."],["INSTALL","```zypper: sudo zypper install toipe```\n\n```brew: brew install toipe```\n\n```nix: nix profile install nixpkgs#toipe```"],["SEE ALSO","[tt](/man/tt)(1), [typespeed](/man/typespeed)(1), [wpm](/man/wpm)(1)"]],"e":["tt","typespeed","wpm"],"x":"toipe"},"tsc":{"n":"tsc","t":"TypeScript to JavaScript compiler","s":[["TAGLINE","TypeScript to JavaScript compiler"],["TLDR","**Compile a TypeScript file** to JavaScript\n\n```tsc [file.ts]```\n\n**Compile using a tsconfig.json** configuration\n\n```tsc -p [tsconfig.json]```\n\n**Compile in watch mode** (recompile on changes)\n\n```tsc -w```\n\n**Compile with strict mode** enabled\n\n```tsc --strict [file.ts]```\n\n**Compile to a specific ECMAScript** target\n\n```tsc --target [ES2020] [file.ts]```\n\n**Output to a specific directory**\n\n```tsc --outDir [dist] [file.ts]```\n\n**Generate source maps**\n\n```tsc --sourceMap [file.ts]```\n\n**Initialize a new tsconfig.json**\n\n```tsc --init```"],["SYNOPSIS","**tsc** [_options_] [_file_...]"],["PARAMETERS","**-p**, **--project** _path_\n> Compile project from tsconfig.json at path\n\n**-w**, **--watch**\n> Watch input files and recompile on changes\n\n**-t**, **--target** _version_\n> ECMAScript target version (ES5, ES2015, ES2020, ES2022, ESNext)\n\n**--outDir** _directory_\n> Redirect output to specified directory\n\n**--outFile** _file_\n> Concatenate and emit output to single file\n\n**--strict**\n> Enable all strict type-checking options\n\n**--noEmit**\n> Do not emit outputs; type-check only\n\n**--sourceMap**\n> Generate corresponding .map source map files\n\n**--declaration**\n> Generate .d.ts declaration files\n\n**--module** _system_\n> Module system: commonjs, es2015, es2020, esnext, node16, nodenext\n\n**--moduleResolution** _strategy_\n> Module resolution strategy: node, nodenext, bundler\n\n**--incremental**\n> Enable incremental compilation for faster rebuilds\n\n**--skipLibCheck**\n> Skip type checking of declaration files\n\n**--esModuleInterop**\n> Enable interoperability between CommonJS and ES Modules\n\n**--resolveJsonModule**\n> Allow importing .json files\n\n**--jsx** _mode_\n> JSX handling: react, react-jsx, react-jsxdev, preserve\n\n**--lib** _libs_\n> Specify library files to include (e.g., ES2020, DOM, ES2020.Promise)\n\n**--noEmitOnError**\n> Do not emit outputs if any errors are reported\n\n**--init**\n> Initialize a tsconfig.json file\n\n**--listFiles**\n> Print names of files that are part of the compilation\n\n**--showConfig**\n> Print the final resolved configuration instead of compiling\n\n**-h**, **--help**\n> Show help\n\n**-v**, **--version**\n> Show version"],["DESCRIPTION","**tsc** is the TypeScript compiler that transforms TypeScript (.ts, .tsx) files into JavaScript (.js). Developed by Microsoft, TypeScript is a superset of JavaScript that adds optional static typing and modern language features.\n\nWhen run without arguments in a directory with **tsconfig.json**, tsc compiles the project according to that configuration. When files are specified on the command line, tsconfig.json is ignored unless **-p** is used.\n\nThe compiler performs type checking to catch errors during development, then emits valid JavaScript. The **--strict** flag enables comprehensive type checking including strict null checks and no implicit any.\n\nWatch mode (**-w**) monitors source files and recompiles automatically when changes are detected, enabling a fast development workflow."],["CAVEATS","tsc requires Node.js and is typically installed via npm (**npm install -g typescript**). Compilation options on the command line override tsconfig.json settings. When files are specified on the command line, **tsconfig.json** is ignored (use **-p** to use a config file). Large projects may have slow compile times; consider using **--incremental** or **--noEmit** for type-check-only workflows."],["HISTORY","**TypeScript** was developed by **Microsoft** under the lead of **Anders Hejlsberg** (creator of C# and Turbo Pascal). It was first publicly released in **October 2012**. The language was designed to address the challenges of large-scale JavaScript development by adding optional static types while maintaining full compatibility with existing JavaScript."],["INSTALL","```dnf: sudo dnf install typescript```\n\n```pacman: sudo pacman -S typescript```\n\n```apk: sudo apk add typescript```\n\n```zypper: sudo zypper install typescript```\n\n```brew: brew install typescript```\n\n```nix: nix profile install nixpkgs#typescript```"],["SEE ALSO","[node](/man/node)(1), [npm](/man/npm)(1), [ts-node](/man/ts-node)(1), [esbuild](/man/esbuild)(1), [swc](/man/swc)(1), [webpack](/man/webpack)(1), [babel](/man/babel)(1)"]],"e":["node","npm","ts-node","esbuild","swc","webpack","babel"],"x":"tsc [file.ts]"},"ubuntu-bug":{"n":"ubuntu-bug","t":"File Ubuntu bug reports via Apport","s":[["TAGLINE","File Ubuntu bug reports via Apport"],["TLDR","This command is an alias of **apport-bug**.\n\nView documentation for the original command\n\n```tldr apport-bug```"],["SEE ALSO","[apport-bug](/man/apport-bug)(1)"]],"e":["apport-bug"],"x":"tldr apport-bug"},"ubus":{"n":"ubus","t":"OpenWrt system message bus client","s":[["TAGLINE","OpenWrt system message bus client"],["TLDR","**List** available objects\n\n```ubus list```\n\nGet **system** information\n\n```ubus call system board```\n\n**Subscribe** to events\n\n```ubus subscribe [event_name]```\n\nDisplay **help**\n\n```ubus```"],["SYNOPSIS","**ubus** _COMMAND_ [_OPTIONS_]"],["COMMANDS","**list**\n> List available ubus objects\n\n**call** _OBJECT_ _METHOD_\n> Call a method on an object\n\n**subscribe** _EVENT_\n> Subscribe to event notifications\n\n**send** _EVENT_\n> Send an event"],["DESCRIPTION","**ubus** is the command-line interface for interacting with the OpenWrt ubusd message bus server. It allows communication between different system services and provides access to system information and configuration.\n\nThe ubus system enables IPC (inter-process communication) on OpenWrt routers and embedded systems, allowing scripts and applications to query and control system services."],["CAVEATS","OpenWrt specific. Requires running ubusd daemon. Available methods depend on installed services. Output is typically JSON formatted."],["HISTORY","**ubus** was developed as part of **OpenWrt's** system architecture to provide a unified IPC mechanism for the lightweight Linux distribution used on routers and embedded devices."],["INSTALL","```nix: nix profile install nixpkgs#ubus```"],["SEE ALSO","[uci](/man/uci)(1), [opkg](/man/opkg)(1)"]],"e":["uci","opkg"],"x":"ubus list"},"units":{"n":"units","t":"Convert between measurement units","s":[["TAGLINE","Convert between measurement units"],["TLDR","**Start interactive mode** for unit conversions\n\n```units```\n\n**Convert between units** directly\n\n```units \"[from_unit]\" \"[to_unit]\"```\n\n**Convert specific quantity**\n\n```units \"[100 kilometers]\" \"[miles]\"```\n\n**Show definition** of a unit\n\n```units \"[parsec]\"```\n\n**Use terse output** (for scripts)\n\n```units --terse \"[from_unit]\" \"[to_unit]\"```\n\n**Check units database** for errors\n\n```units --check```\n\n**Use a custom units file**\n\n```units -f [custom_units.dat]```\n\n**Display version and data file location**\n\n```units -V```"],["SYNOPSIS","**units** [_options_] [_from-unit_] [_to-unit_]"],["PARAMETERS","**-f** _filename_, **--file** _filename_\n> Use specified units database file.\n\n**-c**, **--check**\n> Check that all units reduce to primitive units.\n\n**-o** _format_, **--output-format** _format_\n> Specify numeric output format (printf-style).\n\n**-d** _ndigits_, **--digits** _ndigits_\n> Set number of significant digits in output.\n\n**-e**, **--exponential**\n> Use scientific (exponential) notation for output.\n\n**-t**, **--terse**\n> Print only the conversion factor (for parsing).\n\n**--compact**\n> Give compact output without operators.\n\n**-q**, **--quiet**\n> Suppress statistics and prompts.\n\n**-v**, **--verbose**\n> Show more detailed output.\n\n**--list-units**\n> List all known units and exit.\n\n**--conformable**\n> Show units conformable with the given expression.\n\n**-u** _system_, **--units** _system_\n> Use alternative unit system (e.g., si, cgs, natural).\n\n**--locale** _locale_\n> Force a specific locale.\n\n**--minus**\n> Interpret **-** as subtraction (default).\n\n**--product**\n> Interpret **-** as multiplication.\n\n**-V**, **--version**\n> Print version and data file location.\n\n**-h**, **--help**\n> Display help message."],["DESCRIPTION","**GNU Units** converts between different systems of measurement and performs unit arithmetic. It includes a comprehensive database of over 3000 units, including historical and esoteric measurements.\n\nIn interactive mode, enter the source unit at the \"You have:\" prompt and the target unit at \"You want:\". Units checks dimensional compatibility and reports conformability errors for incompatible conversions.\n\nThe program also functions as a scientific calculator with built-in functions (sin, cos, ln, exp, etc.). Complex expressions with units are supported."],["CAVEATS","Attempting to convert between dimensionally incompatible units (e.g., meters to seconds) produces a conformability error. The units database location varies by installation; use **-V** to find it. Custom unit definitions can be added to personal unit files."],["HISTORY","The original Unix units program was included in early versions of Unix. **GNU Units** was written by **Adrian Mariano** as a free implementation with an expanded unit database. It was first released in **1996** and is maintained as part of the GNU Project. The program includes units from ancient civilizations, historical measurements, and modern scientific standards."],["INSTALL","```apt: sudo apt install units```\n\n```dnf: sudo dnf install units```\n\n```apk: sudo apk add units```\n\n```zypper: sudo zypper install units```\n\n```nix: nix profile install nixpkgs#units```"],["SEE ALSO","[bc](/man/bc)(1), [dc](/man/dc)(1), [calc](/man/calc)(1)"]],"e":["bc","dc","calc"],"x":"units"},"update-ca-trust":{"n":"update-ca-trust","t":"Manage system CA certificate trust store","s":[["TAGLINE","Manage system CA certificate trust store"],["TLDR","**Update CA certificate trust store**\n\n```sudo update-ca-trust extract```\n\n**Update trust store (short form)**\n\n```sudo update-ca-trust```\n\n**Check current trust configuration**\n\n```update-ca-trust check```"],["SYNOPSIS","**update-ca-trust** [_command_]"],["COMMANDS","**extract**: Rebuild the consolidated CA certificate files.\n\n**check**: Verify trust configuration consistency."],["CERTIFICATE LOCATIONS","**Add trusted certificates:**\n> /etc/pki/ca-trust/source/anchors/\n\n**Blacklist certificates:**\n> /etc/pki/ca-trust/source/blacklist/\n\n**Output directory:**\n> /etc/pki/ca-trust/extracted/"],["DESCRIPTION","**update-ca-trust** manages the system's CA certificate trust store on Red Hat-based distributions. It consolidates certificates from multiple sources into unified files that applications use for SSL/TLS verification.\n\nCertificates placed in the anchors directory are added to the trust store. Certificates in the blacklist directory are explicitly distrusted. Both PEM and DER formats are supported.\n\nAfter adding or removing certificates, run update-ca-trust extract to rebuild the consolidated trust files. Applications reading from /etc/pki/ca-trust/extracted/ will then use the updated certificates."],["CAVEATS","Changes require running extract to take effect. Some applications cache certificates and need restart. Debian-based systems use update-ca-certificates instead. Incorrect certificates can break SSL connections."],["HISTORY","**update-ca-trust** is part of the ca-certificates package on Red Hat Enterprise Linux, Fedora, and CentOS. It replaced older certificate management methods to provide a unified approach to trust configuration that works with both OpenSSL and NSS libraries."],["SEE ALSO","[update-ca-certificates](/man/update-ca-certificates)(8), [openssl](/man/openssl)(1), [trust](/man/trust)(1), [p11-kit](/man/p11-kit)(8)"]],"e":["update-ca-certificates","openssl","trust","p11-kit"],"x":"sudo update-ca-trust extract"},"uvicorn":{"n":"uvicorn","t":"Lightning-fast Python ASGI web server","s":[["TAGLINE","Lightning-fast Python ASGI web server"],["TLDR","**Run ASGI application**\n\n```uvicorn [main:app]```\n\n**Run with auto-reload**\n\n```uvicorn [main:app] --reload```\n\n**Bind to specific host and port**\n\n```uvicorn [main:app] --host [0.0.0.0] --port [8000]```\n\n**Run with multiple workers**\n\n```uvicorn [main:app] --workers [4]```\n\n**Run with SSL**\n\n```uvicorn [main:app] --ssl-keyfile [key.pem] --ssl-certfile [cert.pem]```\n\n**Use specific loop** (uvloop for performance)\n\n```uvicorn [main:app] --loop uvloop```\n\n**Run with Unix socket**\n\n```uvicorn [main:app] --uds [/tmp/uvicorn.sock]```\n\n**Specify log level**\n\n```uvicorn [main:app] --log-level [debug]```"],["SYNOPSIS","**uvicorn** _app_ [_--host host_] [_--port port_] [_--reload_] [_--workers num_] [_options_]"],["PARAMETERS","**--host** _HOST_\n> Bind socket to host (default: 127.0.0.1).\n\n**--port** _PORT_\n> Bind socket to port (default: 8000).\n\n**--uds** _PATH_\n> Bind to Unix domain socket.\n\n**--fd** _NUM_\n> Bind to socket from file descriptor.\n\n**--reload**\n> Enable auto-reload on code changes.\n\n**--reload-dir** _PATH_\n> Directories to watch for reload.\n\n**--reload-include** _PATTERN_\n> Include patterns for reload.\n\n**--reload-exclude** _PATTERN_\n> Exclude patterns from reload.\n\n**--workers** _NUM_\n> Number of worker processes.\n\n**--loop** _IMPL_\n> Event loop: auto, uvloop, asyncio.\n\n**--http** _IMPL_\n> HTTP implementation: auto, h11, httptools.\n\n**--ws** _IMPL_\n> WebSocket implementation: auto, websockets, wsproto.\n\n**--interface** _TYPE_\n> Interface: auto, asgi3, asgi2, wsgi.\n\n**--log-level** _LEVEL_\n> Log level: critical, error, warning, info, debug, trace.\n\n**--access-log** / **--no-access-log**\n> Enable/disable access log.\n\n**--ssl-keyfile** _PATH_\n> SSL key file.\n\n**--ssl-certfile** _PATH_\n> SSL certificate file."],["DESCRIPTION","**uvicorn** is a lightning-fast ASGI server for Python. It serves async web frameworks like FastAPI, Starlette, and others that implement the ASGI specification.\n\nASGI (Asynchronous Server Gateway Interface) enables async/await handling of HTTP, WebSocket, and other protocols. Uvicorn implements the server side, connecting to async application code.\n\nDevelopment mode (--reload) watches files and restarts on changes. This enables rapid iteration without manual server restarts.\n\nProduction deployments use multiple workers (--workers) to utilize CPU cores. Each worker is a separate process handling requests independently. Process management handles worker crashes.\n\nPerformance comes from optional uvloop (libuv-based event loop) and httptools (HTTP parsing). These C-based libraries significantly outperform pure Python implementations.\n\nWebSocket support enables real-time bidirectional communication. The server handles connection upgrades and message routing to application code."],["CAVEATS","Workers share nothing - state must use external storage. Reload mode shouldn't be used in production. uvloop not available on Windows. SSL termination often better at reverse proxy. File uploads limited by memory."],["HISTORY","**uvicorn** was created by **Tom Christie** (creator of Django REST framework and Starlette) around **2017**. It implements the ASGI spec that enables async Python web frameworks. The name references UV (high-energy light) and the suffix -corn common in Python web servers."],["INSTALL","```pacman: sudo pacman -S uvicorn```\n\n```apk: sudo apk add uvicorn```\n\n```brew: brew install uvicorn```"],["SEE ALSO","[gunicorn](/man/gunicorn)(1), [hypercorn](/man/hypercorn)(1), [daphne](/man/daphne)(1)"]],"e":["gunicorn","hypercorn","daphne"],"x":"uvicorn [main:app]"},"vainfo":{"n":"vainfo","t":"Display VA-API video acceleration info","s":[["TAGLINE","Display VA-API video acceleration info"],["TLDR","**Show VA-API info** for the default device\n\n```vainfo```\n\n**Show info for a specific DRM device**\n\n```vainfo --display drm --device [/dev/dri/renderD128]```\n\n**Show VA-API info** via X11 display\n\n```vainfo --display x11```\n\n**Show VA-API info** via Wayland display\n\n```vainfo --display wayland```"],["SYNOPSIS","**vainfo** [_--display type_] [_--device path_] [_options_]"],["PARAMETERS","**--display** _TYPE_\n> Display backend type: drm, x11, or wayland.\n\n**--device** _PATH_\n> DRM device path (e.g., /dev/dri/renderD128). Only used with --display drm.\n\n**-a**, **--all**\n> Show all supported attributes for each profile/entrypoint pair.\n\n**--help**\n> Show help."],["DESCRIPTION","**vainfo** displays information about the Video Acceleration API (VA-API) support available on the system. It queries the GPU driver and reports which video encoding and decoding profiles are supported, such as H.264, HEVC, VP9, and AV1.\n\nThe output includes the VA-API driver name, version, and a list of supported profiles with their entry points (encoding, decoding, video processing). This information is essential for diagnosing hardware video acceleration issues and verifying that the correct VA-API driver is loaded for the GPU."],["CAVEATS","Requires VA-API drivers installed (e.g., intel-media-driver, mesa-va-drivers). Output is GPU and driver specific. Part of the libva-utils package."],["HISTORY","**vainfo** is part of **libva-utils**, providing information about VA-API (Video Acceleration API) support."],["INSTALL","```apt: sudo apt install vainfo```\n\n```dnf: sudo dnf install libva-utils```\n\n```pacman: sudo pacman -S libva-utils```\n\n```apk: sudo apk add libva-utils```\n\n```zypper: sudo zypper install libva-utils```\n\n```nix: nix profile install nixpkgs#libva-utils```"],["SEE ALSO","[vdpauinfo](/man/vdpauinfo)(1), [glxinfo](/man/glxinfo)(1), [vulkaninfo](/man/vulkaninfo)(1), [ffmpeg](/man/ffmpeg)(1)"]],"e":["vdpauinfo","glxinfo","vulkaninfo","ffmpeg"],"x":"vainfo"},"winpodx":{"n":"winpodx","t":"Run Windows apps on Linux as native windows","s":[["TAGLINE","Run Windows apps on Linux as native windows"],["TLDR","**Start the Windows pod** and wait until RDP is ready\n\n```winpodx pod start --wait```\n\n**List available Windows applications** detected inside the pod\n\n```winpodx app list```\n\n**Launch a registered application** (e.g. Microsoft Word)\n\n```winpodx app run [app_name]```\n\n**Open a local file** with a Windows application\n\n```winpodx app run [app_name] [path/to/file]```\n\n**Launch the full Windows desktop** as a single RDP window\n\n```winpodx app run desktop```\n\n**Open the Qt6 graphical interface**\n\n```winpodx gui```\n\n**Stop the pod** when finished\n\n```winpodx pod stop```\n\n**Run the interactive setup wizard**\n\n```winpodx setup```"],["SYNOPSIS","**winpodx** _domain_ _command_ [_arguments_] [**--flags**]"],["PARAMETERS","**app list**\n> Show every Windows application detected inside the pod.\n\n**app run** _name_ [_file_]\n> Launch the named application as a RemoteApp window. An optional file is opened with that application.\n\n**app install** _name_\n> Register a custom application in the desktop launcher menu.\n\n**app install-all**\n> Register every detected application as a desktop launcher entry.\n\n**app sessions**\n> List active RemoteApp sessions.\n\n**app kill** _name_\n> Terminate a running RemoteApp session.\n\n**app refresh**\n> Rescan the Windows guest for newly installed applications.\n\n**pod start** [**--wait**]\n> Start the Windows container. **--wait** blocks until RDP is reachable.\n\n**pod stop**\n> Stop the Windows container.\n\n**pod restart**\n> Restart the Windows container.\n\n**pod status**\n> Print container, RDP, and agent state.\n\n**pod wait-ready** [**--logs**]\n> Block until the first-boot Sysprep / OEM phase finishes. **--logs** streams progress.\n\n**pod apply-fixes**\n> Reapply Windows-side configuration tweaks (registry, services, defaults).\n\n**pod sync-password**\n> Synchronise the RDP password between **winpodx.toml** and the Windows guest.\n\n**pod multi-session** {_on_|_off_|_status_}\n> Toggle bundled rdprrap multi-session support.\n\n**power --suspend**\n> Pause the running container.\n\n**power --resume**\n> Resume a paused container.\n\n**rotate-password**\n> Generate and apply a new RDP password.\n\n**setup**\n> Run the interactive first-time configuration wizard.\n\n**check** [**--json**]\n> Run health probes (pod, RDP, agent, round-trip, disk) and print results.\n\n**info**\n> Print system diagnostics (versions, paths, dependencies).\n\n**cleanup**\n> Remove stale Office lock files in the guest.\n\n**timesync**\n> Force a time synchronisation in the Windows guest.\n\n**debloat**\n> Disable telemetry and unneeded services in the guest.\n\n**config show**\n> Print the current configuration.\n\n**config set** _key_ _value_\n> Update a configuration key (e.g. **rdp.scale 140**).\n\n**config import**\n> Import settings from an existing **winapps.conf**.\n\n**gui**\n> Launch the Qt6 main window.\n\n**tray**\n> Launch the system tray icon."],["DESCRIPTION","**winpodx** runs a Windows guest inside a Podman or Docker container and presents individual Windows applications as native Linux windows through **FreeRDP** RemoteApp. Each app gets its own pinnable, alt-tabbable window with the original icon and file association, without exposing the underlying Windows desktop.\n\nA small HTTP agent inside the guest receives bearer-authenticated commands from the host so app launches do not flash a PowerShell window. Application discovery, password rotation, multi-session, and health checks are all driven from the **winpodx** CLI; a Qt6 GUI provides the same actions plus an allowlisted in-app terminal.\n\n**winpodx** targets Linux only. The first run downloads a Windows ISO, runs Sysprep, and applies an OEM customisation pass; expect five to ten minutes before the pod is usable."],["CONFIGURATION","The configuration file lives at _~/.config/winpodx/winpodx.toml_ and is editable through **winpodx config set**:\n\n```\n[pod]\nbackend = \"podman\"        # or \"docker\"\ncpus = 4\nram = \"8G\"\nauto_start = true\nidle_timeout = \"30m\"\n\n[rdp]\nhost = \"127.0.0.1\"\nport = 3389\nscale = 100\ndpi = 96\nuser = \"winpodx\"\npassword_rotation_days = 7\n\n[agent]\nport = 8765\ntoken = \"<generated>\"\n```\n\nEnvironment variables read at start:\n\n```\nWINPODX_CONFIG       Override config file path\nWINPODX_LOG_LEVEL    debug | info | warn | error\n```"],["CAVEATS","Requires a working Podman or Docker installation, a usable KVM accelerator, and a recent **FreeRDP 3** build with RemoteApp support. The first boot performs a full Windows install plus Sysprep; do not interrupt **winpodx pod wait-ready**. Microsoft Windows itself remains subject to its own licensing terms and is **not redistributed** by winpodx; the user supplies a valid Windows installation. The guest agent listens on **localhost:8765** with a bearer token; do not expose this port to other hosts."],["HISTORY","**winpodx** is a Python 3.9+ project that pairs the **dockur/windows** container image with **FreeRDP** RemoteApp and a custom PowerShell HTTP guest agent. It targets the same use case as **WinApps** but ships container, agent, and GUI as a single tool rather than relying on a manually configured VM. Active development continues on the **kernalix7/winpodx** repository."],["INSTALL","```aur: yay -S winpodx```"],["SEE ALSO","[podman](/man/podman)(1), [docker](/man/docker)(1), [xfreerdp](/man/xfreerdp)(1), [virsh](/man/virsh)(1), [wine](/man/wine)(1)"]],"e":["podman","docker","xfreerdp","virsh","wine"],"x":"winpodx pod start --wait"},"wish":{"n":"wish","t":"Tcl/Tk graphical interpreter","s":[["TAGLINE","Tcl/Tk graphical interpreter"],["TLDR","**Start interactive Tk shell**\n\n```wish```\n\n**Run a Tk script**\n\n```wish [script.tcl]```\n\n**Run script with arguments**\n\n```wish [script.tcl] [arg1] [arg2]```\n\n**Execute Tk commands directly**\n\n```echo \"[button .b -text Hello; pack .b]\" | wish```"],["SYNOPSIS","**wish** [_options_] [_script_] [_args_...]"],["PARAMETERS","**-colormap** _new_\n> Use new private colormap.\n\n**-display** _display_\n> X display to use.\n\n**-geometry** _geometry_\n> Initial window geometry.\n\n**-name** _name_\n> Use name as application name.\n\n**-sync**\n> Execute X server requests synchronously.\n\n**-visual** _visual_\n> Visual type for main window.\n\n**-use** _id_\n> Embed in window with given id.\n\n**--**\n> End of options; remaining args go to script."],["DESCRIPTION","**wish** is a Tcl interpreter extended with Tk toolkit commands for creating graphical interfaces. It reads Tcl/Tk commands from files or standard input and executes them, creating GUI windows and widgets.\n\nWhen run without arguments, wish starts interactively with an empty main window and a \"%\" prompt for entering commands. Scripts can be made executable with #!/usr/bin/wish as the first line.\n\nTk provides widgets including buttons, labels, entries, listboxes, canvases, menus, and more. The geometry manager commands (pack, grid, place) control widget layout."],["INTERACTIVE","In interactive mode, wish displays \"%\" prompt. Commands are executed after complete statements. Prompts can be customized via tcl_prompt1 and tcl_prompt2 variables."],["CAVEATS","Requires a display server (X11 or Wayland with XWayland). Tk applications look dated without theming (ttk themed widgets improve this). Some distributions use versioned names like wish8.6. On macOS, Tk uses Aqua natively."],["HISTORY","**wish** was created by John Ousterhout alongside Tcl in the late 1980s. Tk was one of the first cross-platform GUI toolkits and influenced many later frameworks. Wish remains popular for quick GUI scripting and is used by tools like gitk."],["INSTALL","```apt: sudo apt install tk```\n\n```dnf: sudo dnf install tk```\n\n```pacman: sudo pacman -S tk```\n\n```apk: sudo apk add tk```\n\n```zypper: sudo zypper install tk```\n\n```nix: nix profile install nixpkgs#tk```"],["SEE ALSO","[gitk](/man/gitk)(1), [xdotool](/man/xdotool)(1)"]],"e":["gitk","xdotool"],"x":"wish"},"yadm-alt":{"n":"yadm-alt","t":"Process yadm alternate files","s":[["TAGLINE","Process yadm alternate files"],["TLDR","**Process alternate files**\n\n```yadm alt```"],["SYNOPSIS","**yadm** **alt**"],["DESCRIPTION","**yadm alt** processes alternate files based on system conditions. Creates symlinks for files with class, hostname, or OS-specific suffixes. Enables different configs per system from same repo."],["INSTALL","```pacman: sudo pacman -S yadm```\n\n```apk: sudo apk add yadm```\n\n```zypper: sudo zypper install yadm```\n\n```brew: brew install yadm```\n\n```nix: nix profile install nixpkgs#yadm```"],["SEE ALSO","[yadm](/man/yadm)(1), [yadm-config](/man/yadm-config)(1)"]],"e":["yadm","yadm-config"],"x":"yadm alt"}});
