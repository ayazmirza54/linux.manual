window.LCL_BUCKET(74,{"a2dissite":{"n":"a2dissite","t":"disable an Apache2 virtual host","s":[["TAGLINE","disable an Apache2 virtual host"],["TLDR","**Disable** a virtual host\n\n```sudo a2dissite [virtual_host]```\n\nDon't show **informative messages**\n\n```sudo a2dissite -q [virtual_host]```\n\nDisable a virtual host and **purge** all traces from the internal state database\n\n```sudo a2dissite -p [virtual_host]```"],["SYNOPSIS","**a2dissite** [_-q_|_--quiet_] [_-m_|_--maintmode_] [_-p_|_--purge_] [_site_]"],["DESCRIPTION","**a2dissite** is a Debian-specific utility that disables Apache2 virtual host configurations by removing symbolic links from **/etc/apache2/sites-enabled**. Site configuration files remain in **/etc/apache2/sites-available** and can be re-enabled later.\n\nThe tool silently succeeds if the specified site is already disabled. After disabling a site, Apache must be restarted or reloaded for changes to take effect."],["PARAMETERS","**-q, --quiet**\n> Don't show informative messages\n\n**-m, --maintmode**\n> Enables maintainer mode; the program invocation is effectuated automatically by a maintainer script\n\n**-p, --purge**\n> When disabling a site, purge all traces from the internal state database"],["CAVEATS","This utility is **Debian-specific** and not available on other Linux distributions. Site changes require an Apache restart or reload to take effect."],["HISTORY","Created as part of the Apache2 package for **Debian GNU/Linux**. Part of the a2ensite/a2dissite family of utilities for managing Apache virtual hosts."],["INSTALL","```apt: sudo apt install apache2```\n\n```zypper: sudo zypper install apache2```\n\n```brew: brew install apache2```"],["SEE ALSO","[a2ensite](/man/a2ensite)(8), [a2enmod](/man/a2enmod)(8), [a2dismod](/man/a2dismod)(8), [a2enconf](/man/a2enconf)(8), [a2disconf](/man/a2disconf)(8), [apache2ctl](/man/apache2ctl)(8)"],["RESOURCES","```[Homepage](https://httpd.apache.org/)```\n\n```[Documentation](https://manpages.debian.org/a2dissite)```"]],"e":["a2ensite","a2enmod","a2dismod","a2enconf","a2disconf","apache2ctl"],"x":"sudo a2dissite [virtual_host]"},"arch":{"n":"arch","t":"Print the machine hardware architecture","s":[["TAGLINE","Print the machine hardware architecture"],["TLDR","**Print** machine architecture\n\n```arch```"],["SYNOPSIS","**arch**"],["DESCRIPTION","**arch** prints the machine hardware architecture. On Linux this is the GNU coreutils command, equivalent to **uname -m**. There are no options other than **--help** and **--version**.\n\nCommon outputs include:\n- **x86_64**: 64-bit Intel/AMD\n- **aarch64** or **arm64**: 64-bit ARM\n- **i686** or **i386**: 32-bit Intel\n- **armv7l**: 32-bit ARM\n- **ppc64le**: 64-bit PowerPC little-endian\n- **riscv64**: 64-bit RISC-V"],["CAVEATS","The reported value depends on the kernel personality, not the silicon — running under **setarch** or in a 32-bit chroot can make a 64-bit machine report **i686**. On macOS the system **arch** binary is unrelated; it _switches_ a process to a different architecture instead of printing one."],["HISTORY","**arch** has been available on Unix systems since BSD. On Linux, it's part of coreutils and provides a simple wrapper around uname."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add uutils-coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[uname](/man/uname)(1), [setarch](/man/setarch)(8), [hostnamectl](/man/hostnamectl)(1)"]],"e":["uname","setarch","hostnamectl"],"x":"arch"},"asan":{"n":"asan","t":"Detect memory errors at runtime via compiler instrumentation.","s":[["TAGLINE","Detect memory errors at runtime via compiler instrumentation."],["TLDR","**Compile a C program with AddressSanitizer**\n\n```gcc -fsanitize=address -g [program.c] -o [program]```\n\n**Compile a C++ program with ASan**\n\n```g++ -fsanitize=address -g [program.cpp] -o [program]```\n\n**Compile with ASan using Clang**\n\n```clang -fsanitize=address -g [program.c] -o [program]```\n\n**Run with custom ASan options**\n\n```ASAN_OPTIONS=detect_leaks=1:verbosity=1 ./[program]```\n\n**Compile with ASan and UBSan combined**\n\n```gcc -fsanitize=address,undefined -g [program.c] -o [program]```\n\n**Compile for debugging with frame pointers**\n\n```gcc -fsanitize=address -g -fno-omit-frame-pointer [program.c] -o [program]```"],["SYNOPSIS","**gcc** **-fsanitize=address** [_options_] _source_files_"],["PARAMETERS","**-fsanitize=address**\n> Enable AddressSanitizer instrumentation.\n\n**-g**\n> Include debug information for meaningful stack traces.\n\n**-fno-omit-frame-pointer**\n> Preserve frame pointers for better stack traces.\n\n**-O1**\n> Compile with at least -O1 for reasonable performance and readable stack traces.\n\n**-fno-optimize-sibling-calls**\n> Disable tail-call elimination for complete, accurate stack traces.\n\n**-fsanitize-recover=address**\n> Continue execution after detecting errors (required for ASAN_OPTIONS=halt_on_error=0).\n\n**-static-libasan**\n> Link the ASan runtime statically (GCC; Clang uses **-static-libsan**). GCC defaults to a shared runtime, Clang to a static one."],["ENVIRONMENT VARIABLES","**ASAN_OPTIONS**\n> Runtime options for AddressSanitizer (colon-separated key=value pairs).\n\n**detect_leaks=1**\n> Enable memory leak detection (default on x86_64 Linux).\n\n**detect_stack_use_after_return=1**\n> Detect use of stack memory after the function returns (default on Linux).\n\n**halt_on_error=0**\n> Continue after a recoverable error (only effective when compiled with -fsanitize-recover=address; default is to halt).\n\n**abort_on_error=1**\n> Call abort() instead of _exit() on error, producing a core dump.\n\n**log_path=**_path_\n> Write reports to _path_.pid instead of stderr.\n\n**verbosity=1**\n> Increase output verbosity.\n\n**symbolize=1**\n> Enable symbolization of stack traces.\n\n**print_stats=1**\n> Print memory usage statistics at exit.\n\n**ASAN_SYMBOLIZER_PATH**\n> Path to llvm-symbolizer used to resolve addresses to source locations."],["DESCRIPTION","**AddressSanitizer (ASan)** is a fast memory error detector built into GCC and Clang. It detects buffer overflows, use-after-free, use-after-return, memory leaks, and other memory corruption bugs at runtime with relatively low overhead.\n\nWhen enabled via **-fsanitize=address**, the compiler instruments memory accesses and allocations. The instrumented program maintains shadow memory that tracks the state of each byte. Memory errors are detected immediately when they occur, with detailed reports including stack traces for both the error location and the allocation/deallocation points.\n\nASan typically adds 2x slowdown and 3x memory overhead, making it suitable for testing and development but not production use. Compile with **-g** for source locations in reports."],["CAVEATS","ASan cannot be combined with other sanitizers except UBSan. The program must be recompiled; ASan cannot analyze existing binaries. False positives are rare but possible with custom allocators. Memory overhead may cause out-of-memory conditions on memory-constrained systems. Not all platforms support all ASan features."],["HISTORY","AddressSanitizer was developed at **Google** and first released with **LLVM 3.1** in **2012**, later added to GCC 4.8 in **2013**. It was designed by Konstantin Serebryany and others as a faster alternative to Valgrind for memory error detection. ASan has since found thousands of bugs in major software projects including Chrome, Firefox, and the Linux kernel."],["SEE ALSO","[valgrind](/man/valgrind)(1), [gcc](/man/gcc)(1), [clang](/man/clang)(1)"],["RESOURCES","```[Source code](https://github.com/google/sanitizers)```\n\n```[Documentation](https://clang.llvm.org/docs/AddressSanitizer.html)```"]],"e":["valgrind","gcc","clang"],"x":"gcc -fsanitize=address -g [program.c] -o [program]"},"aticonfig":{"n":"aticonfig","t":"Configure AMD/ATI proprietary graphics drivers","s":[["TAGLINE","Configure AMD/ATI proprietary graphics drivers"],["TLDR","**Display current GPU configuration**\n\n```aticonfig --query-monitor```\n\n**Set up initial X configuration**\n\n```sudo aticonfig --initial```\n\n**Enable dual monitors in extended mode**\n\n```sudo aticonfig --initial=dual-head --screen-layout=right```\n\n**Set resolution for display**\n\n```aticonfig --resolution=0,1920x1080```\n\n**Enable or disable Vsync**\n\n```aticonfig --set-pcs-str=\"DDX,EnableVSync,1\"```\n\n**Show GPU temperature and fan speed**\n\n```aticonfig --odgt```\n\n**List all displays**\n\n```aticonfig --list-adapters```\n\n**Set GPU clock speeds** (overclocking)\n\n```aticonfig --odsc=950,1200```"],["SYNOPSIS","**aticonfig** [_options_]"],["PARAMETERS","**--initial**\n> Generate a basic X configuration for the AMD GPU.\n\n**--initial=dual-head**\n> Configure for dual monitor setup.\n\n**--query-monitor**\n> Display connected monitors and their status.\n\n**--list-adapters**\n> List all detected graphics adapters.\n\n**--resolution** _adapter,resolution_\n> Set screen resolution for an adapter.\n\n**--screen-layout** _position_\n> Set layout for dual monitors (left, right, above, below).\n\n**--odgt**\n> Display GPU temperature (OverDrive Get Temperature).\n\n**--odgc**\n> Display GPU clock information.\n\n**--odsc** _core,memory_\n> Set GPU core and memory clock speeds.\n\n**--od-enable**\n> Enable OverDrive (overclocking features).\n\n**--set-pcs-str** _path,value_\n> Set a persistent configuration string.\n\n**--lsa**\n> List adapters with detailed information."],["DESCRIPTION","**aticonfig** is a configuration utility for AMD/ATI proprietary graphics drivers (Catalyst/fglrx). It provides command-line control over display settings, GPU parameters, and X.org configuration for AMD Radeon graphics cards.\n\nThe tool generates and modifies X configuration files, manages multi-monitor setups, and provides access to OverDrive features for monitoring temperatures and adjusting clock speeds. It serves as the CLI counterpart to the AMD Catalyst Control Center graphical tool.\n\nConfiguration changes for X.org typically require restart of the X server to take effect. OverDrive settings for clocks and fan control can be applied at runtime."],["CONFIGURATION","**/etc/X11/xorg.conf**\n> X.org server configuration file generated and modified by aticonfig for display settings, multi-monitor layout, and GPU parameters."],["CAVEATS","**aticonfig** is only for the legacy proprietary fglrx/Catalyst driver, which is discontinued and incompatible with modern Linux kernels (4.2+) and X.org versions. Modern AMD GPUs should use the open-source amdgpu driver with radeon-profile or corectrl for similar functionality. The tool is documented here for legacy systems only."],["HISTORY","The **aticonfig** utility was part of AMD's proprietary Catalyst driver package for Linux, supporting Radeon HD series and earlier GPUs. AMD discontinued the Catalyst driver in **2015**, pivoting to the open-source AMDGPU driver for newer hardware. The tool remains relevant only for users maintaining older systems with legacy hardware that cannot use the open-source stack."],["SEE ALSO","[xrandr](/man/xrandr)(1), [nvidia-settings](/man/nvidia-settings)(1)"]],"e":["xrandr","nvidia-settings"],"x":"aticonfig --query-monitor"},"banksia":{"n":"banksia","t":"CLI for accountable multi-agent AI teams with a local web console","s":[["TAGLINE","CLI for accountable multi-agent AI teams with a local web console"],["TLDR","**Install** with pipx and initialize\n\n```pipx install banksia```\n\n```banksia init```\n\n**Start** the local controller and open the console\n\n```banksia serve```\n\n**Show** status (also the default with no subcommand)\n\n```banksia```\n\n```banksia status```\n\n**Configure** a Task provider\n\n```banksia setup```\n\n```banksia providers list```\n\n**Optional PostgreSQL** install path\n\n```pipx install \"banksia[postgres]\"```\n\n```banksia init --database-url [postgresql+asyncpg://…]```"],["SYNOPSIS","**banksia** [**--debug**] [**-V**|**--version**] [_subcommand_] [_options_]"],["DESCRIPTION","**banksia** is a command-line application and local runtime for building and running **accountable AI teams** on complex work. Workflows define a responsibility tree (not a fixed script). Managers can replan, parallelize, or iterate; progress and the lead's final Result are durable controller state, with deliverables kept as ordinary workspace files.\n\nThe package ships a visual **Console** (browser UI). After **banksia init** and **banksia serve**, open **http://127.0.0.1:18125/** (default). Initialization can configure a Task provider and an optional conversational **Operator** that uses the same controller operations as the UI. Default storage is **SQLite**; PostgreSQL is optional via **banksia[postgres]**.\n\nManaged providers include Codex and Claude. Requires **Python 3.12+**; Linux and macOS are supported (native Windows is not; WSL2 uses the Linux path). Install with **pipx install banksia**."],["PARAMETERS","**(no subcommand)** / **status**\n> Print controller status (default when no subcommand is given).\n\n**init** [**--data-dir** _dir_] [**--database-url** _url_] [**--workspace** _dir_] [**--host** _addr_] [**--port** _n_] [**--log-level** _level_] [**--force**] [**--skip-db-upgrade**] [**--non-interactive**] …\n> Initialize local controller state; optionally configure Task provider and Operator.\n\n**serve**\n> Start the local API/controller process that serves the Console and runtime.\n\n**setup**\n> Guided provider setup for Task work.\n\n**providers** **list**|**status**|**check**|**configure**|**identity**|**set-default** …\n> Inspect and configure LLM/providers.\n\n**operator** **setup**|**status**|**disable** …\n> Manage the conversational Operator agent.\n\n**config** **show**|**path** …\n> Show configuration or its path.\n\n**workflow** **import**|**export** …\n> Import or export Workflow definitions.\n\n**task** **start** …\n> Start a Task/run from the CLI.\n\n**--debug**\n> Include a traceback when a command fails.\n\n**-V**, **--version**\n> Print package version."],["CONFIGURATION","**Local data directory** (set during **init** / **--data-dir**)\n> Controller state, SQLite database by default, and runtime files.\n\n**--database-url**\n> SQLAlchemy URL for PostgreSQL (e.g. **postgresql+asyncpg://…**). Requires the **postgres** extra.\n\n**--host** / **--port**\n> Bind address for the local API (default loopback and project default port **18125**).\n\n**Default workspace**\n> Directory visible to Task members for files and managed actions."],["CAVEATS","Intended for a trusted local machine; the controller binds to loopback by default. Task members in a run share one provider-visible workspace. Console assets under **console/** use a Sustainable Use License (not pure MIT). Not a multi-tenant hosted service. Provider CLIs/SDKs and API credentials must be available for real runs."],["HISTORY","**Banksia** is an open-source accountable multi-agent runtime (MIT for the core; Console derived material under Sustainable Use License). Upstream: **github.com/ringlochid/banksia**. Distributed on PyPI as **banksia**."],["SEE ALSO","[pipx](/man/pipx)(1), [claude](/man/claude)(1), [codex](/man/codex)(1), [uvicorn](/man/uvicorn)(1)"],["RESOURCES","```[Source code](https://github.com/ringlochid/banksia)```\n\n```[Documentation](https://github.com/ringlochid/banksia/tree/main/docs)```"]],"e":["pipx","claude","codex","uvicorn"],"x":"pipx install banksia"},"br":{"n":"br","t":"Shell function for interactive directory navigation","s":[["TAGLINE","Shell function for interactive directory navigation"],["TLDR","Start **interactive** navigation\n\n```br```\n\nShow file **sizes**\n\n```br -s```\n\nShow **hidden** files\n\n```br -h```\n\nShow **only directories**\n\n```br -f```\n\n**Install** or reinstall the br shell function\n\n```broot --install```"],["SYNOPSIS","**br** [_options_] [_path_]"],["DESCRIPTION","**br** is a shell function that wraps **broot**, providing interactive directory tree navigation with the ability to change the shell's working directory. It is the recommended way to use broot for shell integration. Supported shells are bash, zsh, fish, and nushell.\n\nOn first run, broot offers to register the **br** function in your shell config. You can also install it manually with **broot --install**, or print the function with **broot --print-shell-function** _shell_."],["PARAMETERS","See **broot** for all available options. Common options:\n\n**-s, --sizes**\n> Display file and directory sizes\n\n**-h, --hidden**\n> Show hidden files and directories\n\n**-f, --only-folders**\n> Show only directories\n\n**-p, --permissions**\n> Display file permissions"],["CAVEATS","The br function must be installed using \"broot --install\" for proper shell integration. Unlike calling broot directly, br allows changing the shell's current directory."],["HISTORY","**br** is the shell function component of **broot**, installed to enable directory navigation and shell integration features."],["INSTALL","```apt: sudo apt install bottlerocket```"],["SEE ALSO","[broot](/man/broot)(1), [tree](/man/tree)(1), [ranger](/man/ranger)(1), [nnn](/man/nnn)(1), [yazi](/man/yazi)(1)"],["RESOURCES","```[Source code](https://github.com/Canop/broot)```\n\n```[Homepage](https://dystroy.org/broot/)```"]],"e":["broot","tree","ranger","nnn","yazi"],"x":"br"},"cargo-metadata":{"n":"cargo-metadata","t":"Output workspace and dependency metadata as JSON","s":[["TAGLINE","Output workspace and dependency metadata as JSON"],["TLDR","**Output project metadata**\n\n```cargo metadata```\n\n**Output with format version**\n\n```cargo metadata --format-version 1```\n\n**Without dependencies**\n\n```cargo metadata --no-deps```\n\n**Filter to specific features**\n\n```cargo metadata --features [feature1,feature2]```\n\n**Pretty print JSON**\n\n```cargo metadata --format-version 1 | jq .```"],["SYNOPSIS","**cargo metadata** [_options_]"],["DESCRIPTION","**cargo metadata** outputs JSON to stdout containing information about workspace members, resolved dependencies, and build configuration. Useful for tooling and IDE integration."],["PARAMETERS","**--format-version** _version_\n> Schema version (use 1 for stability)\n\n**--no-deps**\n> Skip dependency resolution (faster, workspace only)\n\n**--features** _features_\n> Enable specified features\n\n**--all-features**\n> Enable all features\n\n**--no-default-features**\n> Disable default features\n\n**--filter-platform** _triple_\n> Filter to specified platform\n\n**--manifest-path** _path_\n> Path to Cargo.toml\n\n**-v**, **--verbose**\n> Verbose output\n\n**-q**, **--quiet**\n> Suppress log messages"],["OUTPUT FIELDS","**packages**\n> Array of all packages and dependencies\n\n**workspace_members**\n> Package IDs of workspace members\n\n**workspace_root**\n> Absolute path to workspace root\n\n**target_directory**\n> Path to build output directory\n\n**resolve**\n> Dependency resolution graph\n\n**version**\n> Schema version number"],["CAVEATS","Output format may change between Cargo versions. Always use --format-version for scripts. The cargo_metadata crate provides Rust parsing."],["INSTALL","```apt: sudo apt install cargo```\n\n```dnf: sudo dnf install cargo```\n\n```apk: sudo apk add cargo```\n\n```zypper: sudo zypper install cargo```\n\n```nix: nix profile install nixpkgs#cargo```"],["SEE ALSO","[cargo](/man/cargo)(1), [cargo-locate-project](/man/cargo-locate-project)(1)"],["RESOURCES","```[Source code](https://github.com/rust-lang/cargo)```\n\n```[Documentation](https://doc.rust-lang.org/cargo/commands/cargo-metadata.html)```"]],"e":["cargo","cargo-locate-project"],"x":"cargo metadata"},"chisel":{"n":"chisel","t":"TCP/UDP tunnel over HTTP secured via SSH","s":[["TAGLINE","TCP/UDP tunnel over HTTP secured via SSH"],["TLDR","**Start server**\n\n```chisel server --port [8080]```\n\n**Start server with authentication**\n\n```chisel server --authfile [users.json]```\n\n**Connect client with port forward**\n\n```chisel client [server:8080] [local:3000:remote:80]```\n\n**Reverse tunnel**\n\n```chisel client [server:8080] R:[remote:8001:local:80]```\n\n**SOCKS5 proxy**\n\n```chisel client [server:8080] socks```\n\n**Connect via HTTPS**\n\n```chisel client https://[server] [8080:localhost:80]```"],["SYNOPSIS","**chisel** server [_options_]\n**chisel** client [_options_] _server_ _remote_..."],["DESCRIPTION","**chisel** is a fast TCP/UDP tunnel transported over HTTP and secured via SSH. A single executable contains both client and server functionality. It uses WebSockets to multiplex connections, allowing multiple tunnels through a single HTTP connection.\n\nThe tool is useful for traversing firewalls, accessing services behind NAT, and creating reverse tunnels. It supports forward and reverse port forwarding as well as SOCKS5 proxy mode. Authentication can be configured with username/password pairs or an authentication file. Written in Go."],["SERVER OPTIONS","**-p**, **--port** _PORT_\n> Listening port (default **8080**).\n\n**--host** _HOST_\n> Listening interface (default all).\n\n**--authfile** _FILE_\n> Path to a JSON file mapping **user:pass** credentials to allowed remotes.\n\n**--auth** _USER:PASS_\n> Single inline credential pair.\n\n**--reverse**\n> Allow clients to request reverse tunnels (**R:**).\n\n**--socks5**\n> Allow clients to request a SOCKS5 endpoint via the special **socks** remote.\n\n**--backend** _URL_\n> Forward non-chisel HTTP requests hitting the server to this backend URL (useful to co-host chisel with a web server).\n\n**--keepalive** _DURATION_\n> WebSocket keepalive interval (default **25s**).\n\n**--key**, **--keygen**, **--keyfile**\n> Manage the server's persistent SSH host key.\n\n**--tls-key**, **--tls-cert**, **--tls-ca**, **--tls-domain**\n> Enable native TLS termination and client-cert verification."],["CLIENT OPTIONS","**--auth** _USER:PASS_\n> Authenticate to the server.\n\n**--fingerprint** _HASH_\n> Pin the expected server SSH key fingerprint.\n\n**--proxy** _URL_\n> Dial through an upstream HTTP **CONNECT** or SOCKS5 proxy.\n\n**--header** _NAME: VAL_\n> Extra HTTP header to add to the WebSocket handshake (repeatable).\n\n**--hostname** _HOST_\n> Override the HTTP **Host** header.\n\n**--sni** _NAME_\n> Override the TLS **ServerName** used during the handshake.\n\n**--keepalive** _DURATION_\n> WebSocket keepalive interval.\n\n**--max-retry-count** _N_, **--max-retry-interval** _DURATION_\n> Cap reconnection attempts and backoff.\n\n**--tls-ca** _FILE_, **--tls-skip-verify**, **--tls-key** _FILE_, **--tls-cert** _FILE_\n> Control TLS verification and client-certificate authentication."],["REMOTE SYNTAX","```\n[<local-host>:]<local-port>[:<remote-host>:<remote-port>][/udp]\n```\n\nDefaults: local-host = **0.0.0.0**, remote-host = **0.0.0.0**, protocol = **tcp**. Prefix with **R:** for a reverse tunnel. Special values: **socks** (SOCKS5 endpoint, requires **--socks5** on the server) and **stdio:host:port** (stdio-attached tunnel)."],["CAVEATS","Useful for bypassing firewalls. WebSocket support varies by PaaS provider. Install: curl https://i.jpillora.com/chisel! | bash"],["INSTALL","```brew: brew install chisel```\n\n```nix: nix profile install nixpkgs#chisel```"],["SEE ALSO","[ssh](/man/ssh)(1), [socat](/man/socat)(1), [ngrok](/man/ngrok)(1)"]],"e":["ssh","socat","ngrok"],"x":"chisel server --port [8080]"},"chruby":{"n":"chruby","t":"Lightweight Ruby version switcher","s":[["TAGLINE","Lightweight Ruby version switcher"],["TLDR","**List available Ruby versions**\n\n```chruby```\n\n**Switch to specific Ruby**\n\n```chruby ruby-3.2.0```\n\n**Switch to system Ruby**\n\n```chruby system```\n\n**Show current Ruby**\n\n```chruby | grep '\\*'```"],["SYNOPSIS","**chruby** [_ruby_version_ | system]"],["DESCRIPTION","**chruby** switches between multiple Ruby versions by modifying PATH and related environment variables. Lightweight alternative to RVM and rbenv. Just 90 lines of shell script.\n\nWorks with rubies installed by ruby-install or built from source."],["SETUP","Add to ~/.bashrc or ~/.zshrc:\n\n```bash\nsource /usr/local/share/chruby/chruby.sh\n```\n\nFor auto-switching:\n```bash\nsource /usr/local/share/chruby/auto.sh\n```"],["AUTO-SWITCHING","With auto.sh loaded, chruby reads .ruby-version files:\n\n```bash\necho \"ruby-3.2.0\" > .ruby-version\n```\n\nChecks current and parent directories automatically."],["DEFAULT VERSION","Set in ~/.bashrc or ~/.zshrc:\n\n```bash\nchruby ruby-3.2.0\n```"],["ENVIRONMENT","**RUBIES**\n> Array of Ruby installation paths\n\n**RUBY_ROOT**\n> Current Ruby installation path\n\n**GEM_HOME** / **GEM_PATH**\n> Gem directories"],["CAVEATS","Only switches versions - use ruby-install to install rubies. Does not override cd command (uses pre-prompt hook)."],["INSTALL","```brew: brew install chruby```\n\n```nix: nix profile install nixpkgs#chruby```"],["SEE ALSO","[rbenv](/man/rbenv)(1), [rvm](/man/rvm)(1)"]],"e":["rbenv","rvm"],"x":"chruby"},"crane-manifest":{"n":"crane-manifest","t":"retrieve container image manifest","s":[["TAGLINE","retrieve container image manifest"],["TLDR","**Get image manifest**\n\n```crane manifest [image:tag]```\n\n**Get manifest for specific platform**\n\n```crane manifest --platform [linux/amd64] [image:tag]```\n\n**Pretty print manifest**\n\n```crane manifest [image] | jq .```"],["SYNOPSIS","**crane** **manifest** [_options_] _image_"],["PARAMETERS","**--platform** _platform_\n> Specifies the platform in the form os/arch[/variant][:osversion] (e.g. linux/amd64). Default: all.\n\n**--insecure**\n> Allow image references to be fetched without TLS.\n\n**-v**, **--verbose**\n> Enable debug logs."],["DESCRIPTION","**crane manifest** retrieves the manifest for a container image. The manifest describes the image's layers, configuration, and platform information.\n\nOutput is in JSON format (OCI or Docker manifest schema)."],["INSTALL","```pacman: sudo pacman -S crane```\n\n```apk: sudo apk add crane```\n\n```zypper: sudo zypper install crane```\n\n```brew: brew install crane```\n\n```nix: nix profile install nixpkgs#crane```"],["SEE ALSO","[crane](/man/crane)(1), [crane-config](/man/crane-config)(1), [crane-digest](/man/crane-digest)(1)"]],"e":["crane","crane-config","crane-digest"],"x":"crane manifest [image:tag]"},"eog":{"n":"eog","t":"Official image viewer for the GNOME desktop","s":[["TAGLINE","Official image viewer for the GNOME desktop"],["TLDR","**Open** an image file\n\n```eog [path/to/image.png]```\n\nOpen **multiple** images\n\n```eog [image1.jpg] [image2.png]```\n\nOpen images in **fullscreen** mode\n\n```eog --fullscreen [path/to/image.jpg]```\n\nStart a **slideshow**\n\n```eog --slide-show [path/to/directory]```\n\nOpen a single image **without** image gallery\n\n```eog --disable-gallery [path/to/image.jpg]```\n\nOpen an image in a **new** instance\n\n```eog --new-instance [path/to/image.jpg]```"],["SYNOPSIS","**eog** [_options_] [_files..._]"],["DESCRIPTION","**eog** (Eye of GNOME) is the official image viewer for the GNOME desktop environment. It provides a fast, lightweight interface for displaying images in many formats including JPEG, PNG, GIF, SVG, TIFF, BMP, and more. The viewer supports slideshows, fullscreen viewing, zoom, rotation, and basic image operations.\n\nThe application integrates with GNOME desktop services and file managers, supporting drag-and-drop, thumbnail generation, and EXIF metadata display. While designed for GNOME, eog can run on other desktop environments, though some features may require GNOME libraries."],["PARAMETERS","**-f**, **--fullscreen**\n> Open in fullscreen mode.\n\n**-s**, **--slide-show**\n> Open in slideshow mode.\n\n**-g**, **--disable-gallery**\n> Disable the image gallery panel.\n\n**-n**, **--new-instance**\n> Open in a new instance rather than reusing an existing one.\n\n**-w**, **--single-window**\n> Force all images to be opened in a single window.\n\n**--display=**_DISPLAY_\n> X display to use.\n\n**--version**\n> Display version information.\n\n**-?**, **--help**\n> Show help message."],["CONFIGURATION","**~/.config/eog/**\n> User configuration directory for preferences managed through GNOME.\n\n**dconf/gsettings**\n> Settings accessible via `gsettings` for the `org.gnome.eog` schema controlling UI, plugins, and behavior."],["CAVEATS","Designed for GNOME desktop; some features may not work outside GNOME. Large images may consume significant memory. As of GNOME 43, eog has been superseded in many distributions by **Loupe** (the new default GNOME image viewer written in Rust)."],["INSTALL","```apt: sudo apt install eog```\n\n```dnf: sudo dnf install eog```\n\n```pacman: sudo pacman -S eog```\n\n```apk: sudo apk add eog```\n\n```zypper: sudo zypper install eog```\n\n```nix: nix profile install nixpkgs#eog```"],["SEE ALSO","[feh](/man/feh)(1), [gpicview](/man/gpicview)(1), [gimp](/man/gimp)(1)"]],"e":["feh","gpicview","gimp"],"x":"eog [path/to/image.png]"},"gcal":{"n":"gcal","t":"advanced calendar with holiday calculations","s":[["TAGLINE","advanced calendar with holiday calculations"],["TLDR","**Show current month**\n\n```gcal```\n\n**Show specific month**\n\n```gcal [3] [2024]```\n\n**Show entire year**\n\n```gcal [2024]```\n\n**Show with week numbers**\n\n```gcal -K```\n\n**Show holidays**\n\n```gcal -q [US]```\n\n**Show 3 months centered on current**\n\n```gcal .```"],["SYNOPSIS","**gcal** [_options_] [_date_]"],["PARAMETERS","**-K**\n> Show week numbers.\n\n**-q** _country_\n> Show country holidays.\n\n**-b** _number_\n> Months before current.\n\n**-a** _number_\n> Months after current.\n\n**-s** _day_\n> Start of week (0=Sunday).\n\n**--today**\n> Highlight today."],["DESCRIPTION","**gcal** is a feature-rich calendar program that goes far beyond simple date display. Unlike the standard cal utility, gcal supports extensive customization including week numbering, configurable week start days, and multi-month displays.\n\nOne of gcal's most powerful features is its holiday calculation capability, which can display holidays for various countries and religious traditions. The program also supports astronomical data, eternal holiday lists, and various international calendar systems.\n\ngcal is highly scriptable and can be used for complex date calculations, making it valuable for both interactive use and automation tasks that require sophisticated calendar operations."],["INSTALL","```apt: sudo apt install gcal```\n\n```dnf: sudo dnf install gcal```\n\n```zypper: sudo zypper install gcal```\n\n```nix: nix profile install nixpkgs#gcal```"],["SEE ALSO","[cal](/man/cal)(1), [ncal](/man/ncal)(1)"],["RESOURCES","```[Homepage](https://www.gnu.org/software/gcal/)```\n\n```[Documentation](https://www.gnu.org/software/gcal/manual/)```"]],"e":["cal","ncal"],"x":"gcal"},"gcrane-gc":{"n":"gcrane-gc","t":"garbage collects untagged images","s":[["TAGLINE","garbage collects untagged images"],["TLDR","**Garbage collect untagged images**\n\n```gcrane gc [gcr.io/project/repo]```\n\n**Dry run**\n\n```gcrane gc --dry-run [gcr.io/project/repo]```\n\n**Garbage collect with grace period**\n\n```gcrane gc --grace [24h] [gcr.io/project/repo]```"],["SYNOPSIS","**gcrane** **gc** [_options_] _repository_"],["PARAMETERS","**--dry-run**\n> Show what would be deleted.\n\n**--grace** _duration_\n> Keep images newer than duration."],["DESCRIPTION","**gcrane gc** identifies untagged container images in Google Container Registry or Artifact Registry that are candidates for garbage collection. These orphaned images typically result from retagging or deleting tags, leaving unreferenced manifests that consume storage.\n\nThe command lists image digests that have no associated tags. It does not delete images directly; output should be piped to gcrane delete for actual removal. The grace period option protects recently created images from accidental deletion. Dry run mode shows what would be deleted without making changes, allowing safe preview before cleanup operations."],["INSTALL","```zypper: sudo zypper install gcrane```\n\n```nix: nix profile install nixpkgs#gcrane```"],["SEE ALSO","[gcrane](/man/gcrane)(1), [gcrane-ls](/man/gcrane-ls)(1)"],["RESOURCES","```[Source code](https://github.com/google/go-containerregistry)```"]],"e":["gcrane","gcrane-ls"],"x":"gcrane gc [gcr.io/project/repo]"},"git-commit-graph":{"n":"git-commit-graph","t":"Write and verify commit graph files","s":[["TAGLINE","Write and verify commit graph files"],["TLDR","**Write commit graph**\n\n```git commit-graph write```\n\n**Write with reachable commits**\n\n```git commit-graph write --reachable```\n\n**Verify commit graph**\n\n```git commit-graph verify```"],["SYNOPSIS","**git** **commit-graph** _command_ [_options_]"],["SUBCOMMANDS","**write**\n> Write commit graph file.\n\n**verify**\n> Verify commit graph."],["PARAMETERS","**--reachable**\n> Include reachable commits.\n\n**--stdin-commits**\n> Read commits from stdin.\n\n**--stdin-packs**\n> Scan pack indexes listed on stdin for commits.\n\n**--append**\n> Add to existing graph.\n\n**--split**[=_STRATEGY_]\n> Use incremental graph.\n\n**--changed-paths**\n> Compute Bloom filters for changed paths, speeding up history queries like **git log -- path**.\n\n**--object-dir** _DIR_\n> Object directory used to store the graph.\n\n**--shallow**\n> With **verify**, only check the tip file of a split commit graph."],["DESCRIPTION","**git commit-graph** manages commit-graph files, a performance optimization feature that stores commit metadata in a format optimized for fast graph traversal operations. These files significantly accelerate common operations like git log, git merge-base, and reachability checks in repositories with deep history.\n\nThe commit graph stores a pre-computed representation of the commit DAG (directed acyclic graph), including commit parent relationships, generation numbers, and tree object IDs. This avoids repeatedly parsing commit objects during graph walks, providing dramatic performance improvements for large repositories.\n\nThe write subcommand generates or updates the commit graph, either for all reachable commits (--reachable) or for commits provided via stdin (--stdin-commits). Incremental graphs (--split) allow updating the graph efficiently as new commits are added without rewriting the entire file.\n\nModern Git versions include commit-graph maintenance as part of git gc and git maintenance, so manual invocation is rarely necessary. The verify subcommand checks graph integrity, ensuring the data structure remains consistent with repository state."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-gc](/man/git-gc)(1), [git-maintenance](/man/git-maintenance)(1), [git-log](/man/git-log)(1)"],["RESOURCES","```[Source code](https://github.com/git/git)```\n\n```[Homepage](https://git-scm.com/)```\n\n```[Documentation](https://git-scm.com/docs/git-commit-graph)```"]],"e":["git-gc","git-maintenance","git-log"],"x":"git commit-graph write"},"git-lfs-transfer":{"n":"git-lfs-transfer","t":"Low-level LFS transfer protocol","s":[["TAGLINE","Low-level LFS transfer protocol"],["TLDR","**Transfer LFS objects**\n\n```git lfs transfer```"],["SYNOPSIS","**git lfs transfer** [_options_]"],["PARAMETERS","**--help**\n> Display help information."],["DESCRIPTION","**git lfs transfer** is a low-level Git LFS protocol helper that implements the **pure-SSH transfer protocol** introduced in Git LFS 3.0. When a Git LFS server is reached over **git+ssh://**, the LFS client invokes **git lfs transfer** on the remote shell to negotiate batch requests, exchange OIDs, and stream object content over the SSH channel — bypassing the HTTP-based \"ssh-as-auth\" flow used by older LFS deployments.\n\nDirect invocation is rare; users normally interact with the protocol indirectly through **git lfs push**, **git lfs pull**, **git lfs fetch**, or any operation that materialises LFS objects."],["CAVEATS","Internal helper — invoked by Git LFS over an SSH connection, not by users directly. Requires both client and server to support the pure-SSH transfer protocol (Git LFS ≥ 3.0 on both sides)."],["HISTORY","git lfs transfer is part of **Git LFS**, implementing the transfer protocol for large file synchronization."],["INSTALL","```nix: nix profile install nixpkgs#git-lfs-transfer```"],["SEE ALSO","[git-lfs](/man/git-lfs)(1), [git](/man/git)(1)"]],"e":["git-lfs","git"],"x":"git lfs transfer"},"grip":{"n":"grip","t":"GitHub-flavored Markdown preview server","s":[["TAGLINE","GitHub-flavored Markdown preview server"],["TLDR","**Preview README in current directory**\n\n```grip```\n\n**Preview and open in browser**\n\n```grip -b```\n\n**Preview specific file**\n\n```grip [README.md]```\n\n**Preview on specific port**\n\n```grip [file.md] [8080]```\n\n**Export to HTML file**\n\n```grip --export [file.md]```\n\n**Export to a specific HTML file**\n\n```grip [README.md] --export [output.html]```\n\n**Preview from stdin**\n\n```cat [file.md] | grip -```"],["SYNOPSIS","**grip** [_options_] [_path_] [_address_]"],["PARAMETERS","**-b**, **--browser**\n> Open in browser after starting.\n\n**--export**\n> Export to HTML file instead of serving.\n\n**--offline**\n> Render without GitHub API.\n\n**--norefresh**\n> Disable auto-refresh on file changes.\n\n**--quiet**\n> Suppress terminal output.\n\n**--user** _user_\n> GitHub username for API auth.\n\n**--pass** _pass_\n> GitHub password or personal access token.\n\n**--wide**\n> Render wide page layout.\n\n**--user-content**\n> Render as user-generated content (like comments or issues).\n\n**--context** _repo_\n> Repository context for relative URL linking (e.g., user/repo).\n\n**--title** _title_\n> Custom page title for the rendered output.\n\n**--no-inline**\n> Do not inline CSS styles in exported HTML.\n\n**--api-url** _url_\n> Use a custom GitHub API URL (for GitHub Enterprise)."],["DESCRIPTION","**grip** (GitHub Readme Instant Preview) is a command-line server that renders GitHub-flavored Markdown using GitHub's API. It displays how your README will appear on GitHub with accurate styling, and changes to files are reflected instantly without page refresh.\n\nUse **--offline** to avoid API rate limits for basic rendering."],["CAVEATS","Without authentication, GitHub API rate limits may apply. Use **--user** and **--pass** with a personal access token for higher limits."],["INSTALL","```dnf: sudo dnf install grip```\n\n```brew: brew install grip```\n\n```nix: nix profile install nixpkgs#grip```"],["SEE ALSO","[markdown](/man/markdown)(1), [pandoc](/man/pandoc)(1), [glow](/man/glow)(1)"]],"e":["markdown","pandoc","glow"],"x":"grip"},"grub-file":{"n":"grub-file","t":"check boot image file type","s":[["TAGLINE","check boot image file type"],["TLDR","Check if a file is an **ARM EFI** image\n\n```grub-file --is-arm-efi [path/to/file]```\n\nCheck if a file is an **i386 EFI** image\n\n```grub-file --is-i386-efi [path/to/file]```\n\nCheck if a file is an **x86_64 EFI** image\n\n```grub-file --is-x86_64-efi [path/to/file]```\n\nCheck if a file is an **ARM Linux** kernel\n\n```grub-file --is-arm-linux [path/to/file]```\n\nCheck if a file is an **x86 Linux** kernel (32 or 64-bit)\n\n```grub-file --is-x86-linux [path/to/file]```\n\nCheck if a file is an **x86 Multiboot2** kernel\n\n```grub-file --is-x86-multiboot2 [path/to/file]```\n\nCheck if a file is an **x86_64 XNU** (macOS) kernel\n\n```grub-file --is-x86_64-xnu [path/to/file]```"],["SYNOPSIS","**grub-file** _--is-TYPE_ _file_"],["PARAMETERS","**--is-arm-efi**\n> Check for ARM EFI image\n\n**--is-arm64-efi**\n> Check for ARM64 EFI image\n\n**--is-i386-efi**\n> Check for i386 EFI image\n\n**--is-x86_64-efi**\n> Check for x86_64 EFI image\n\n**--is-arm-linux**\n> Check for ARM Linux kernel\n\n**--is-arm64-linux**\n> Check for ARM64 Linux kernel\n\n**--is-x86-linux**\n> Check for x86 Linux kernel (covers both 32 and 64-bit)\n\n**--is-x86-linux32**\n> Check for x86 Linux kernel using the 32-bit-only boot protocol\n\n**--is-x86_64-xnu**\n> Check for macOS (XNU) x86_64 kernel\n\n**--is-x86-multiboot**\n> Check for Multiboot image\n\n**--is-x86-multiboot2**\n> Check for Multiboot2 image"],["DESCRIPTION","**grub-file** checks whether a file is a bootable image of a specific type. It examines file headers and signatures to identify kernel images, EFI applications, and other bootable formats.\n\nThe command returns exit code 0 (success) if the file matches the specified type, or 1 (failure) if it does not. This is useful in scripts for validating boot images or determining the correct boot method."],["CAVEATS","Only checks file type signatures; doesn't verify if the image is actually bootable or valid. Some specialized or custom kernels may not be recognized correctly."],["HISTORY","grub-file is part of GRUB 2, providing a utility for image type detection. This is useful during installation and configuration to ensure the correct bootloader setup for the target architecture."],["INSTALL","```apt: sudo apt install grub-common```\n\n```pacman: sudo pacman -S grub```\n\n```apk: sudo apk add grub```"],["SEE ALSO","[grub-install](/man/grub-install)(8), [grub-probe](/man/grub-probe)(8), [file](/man/file)(1)"],["RESOURCES","```[Homepage](https://www.gnu.org/software/grub/)```\n\n```[Source code](https://git.savannah.gnu.org/cgit/grub.git)```\n\n```[Documentation](https://www.gnu.org/software/grub/manual/grub/grub.html)```"]],"e":["grub-install","grub-probe","file"],"x":"grub-file --is-arm-efi [path/to/file]"},"grv":{"n":"grv","t":"Terminal interface for viewing Git repositories","s":[["TAGLINE","Terminal interface for viewing Git repositories"],["TLDR","**Open the current repository** in grv\n\n```grv```\n\n**Open a specific repository**\n\n```grv -repoFilePath [path/to/repo]```\n\n**Open with a specific log level**\n\n```grv -logLevel [NONE|PANIC|FATAL|ERROR|WARN|INFO|DEBUG|TRACE]```\n\n**Open in read-only mode**\n\n```grv -readOnly```"],["SYNOPSIS","**grv** [**-repoFilePath** _path_] [**-workTreeFilePath** _path_] [**-logFile** _path_] [**-logLevel** _level_] [**-readOnly**] [**-version**]"],["DESCRIPTION","**GRV** (Git Repository Viewer) is a terminal-based interface for viewing Git repositories. It allows refs, commits, and diffs to be viewed, searched, and filtered using **Vi/Vim-like key bindings**. The interface provides multiple views including a ref view for branches and tags, a commit view for browsing history, and a diff view for inspecting changes.\n\nGRV reads the repository directly using libgit2 rather than shelling out to git commands, resulting in fast navigation even in large repositories. The behaviour and style can be customised through a **.grvrc** configuration file."],["PARAMETERS","**-repoFilePath** _path_\n> Repository file path (default: \".\")\n\n**-workTreeFilePath** _path_\n> Work tree file path\n\n**-logFile** _path_\n> Log file path (default: \"grv.log\")\n\n**-logLevel** _level_\n> Logging level: NONE, PANIC, FATAL, ERROR, WARN, INFO, DEBUG, TRACE (default: \"NONE\")\n\n**-readOnly**\n> Run grv in read-only mode\n\n**-version**\n> Print version information"],["KEY BINDINGS","**j/k** — Move down/up\n**Enter** — Select item / open view\n**q** — Close view / quit\n**/** — Search\n**n/N** — Next/previous search match\n**Tab** — Switch between views\n**1-5** — Jump to specific view"],["CAVEATS","GRV is no longer actively maintained (last release 2019). It reads git repository data directly using libgit2, so very large repositories may use significant memory. Read-only; does not support write operations like committing or pushing. Consider **lazygit** or **gitui** as actively maintained alternatives."],["HISTORY","**GRV** was created by **Robert Burke** and written in **Go**. It was designed as a lightweight alternative to graphical Git clients, providing a fast terminal-based workflow for inspecting repository history and diffs."],["INSTALL","```aur: yay -S grv```"],["SEE ALSO","[git](/man/git)(1), [tig](/man/tig)(1), [gitui](/man/gitui)(1), [lazygit](/man/lazygit)(1)"]],"e":["git","tig","gitui","lazygit"],"x":"grv"},"hashcat":{"n":"hashcat","t":"high-performance password recovery tool that uses CPU, GPU, and other hardware","s":[["TAGLINE","high-performance password recovery tool that uses CPU, GPU, and other hardware"],["TLDR","**Run a dictionary attack** on MD5 hashes\n\n```hashcat -m 0 -a 0 [hashes.txt] [wordlist.txt]```\n\n**Crack SHA-256 hashes** with a wordlist and output to file\n\n```hashcat -m 1400 -a 0 -o [cracked.txt] [hashes.txt] [wordlist.txt]```\n\n**Brute-force attack** with a mask (8 lowercase characters)\n\n```hashcat -m 0 -a 3 [hashes.txt] '?l?l?l?l?l?l?l?l'```\n\n**Run benchmark** to test system performance\n\n```hashcat -b```\n\n**Show already cracked passwords** from potfile\n\n```hashcat -m 0 --show [hashes.txt]```\n\n**Crack bcrypt hashes** using GPU\n\n```hashcat -m 3200 -a 0 -D 2 [hashes.txt] [wordlist.txt]```\n\n**Resume a previous session**\n\n```hashcat --restore```\n\n**Crack NTLM hashes** with rules\n\n```hashcat -m 1000 -a 0 -r [rules/best64.rule] [hashes.txt] [wordlist.txt]```"],["SYNOPSIS","**hashcat** [_options_] _hashfile_ [_dictionary_|_mask_]\n\n**hashcat** -m _hashtype_ -a _attackmode_ [_options_] _hashfile_ _wordlist_"],["PARAMETERS","**-m**, **--hash-type** _NUM_\n> Hash type (0=MD5, 1000=NTLM, 1400=SHA256, 1800=SHA512crypt, 3200=bcrypt)\n\n**-a**, **--attack-mode** _NUM_\n> Attack mode (0=dictionary, 1=combinator, 3=brute-force, 6=hybrid, 7=hybrid)\n\n**-o**, **--outfile** _FILE_\n> Output file for recovered passwords\n\n**-r**, **--rules-file** _FILE_\n> Rule file for word mangling\n\n**-D**, **--opencl-device-types** _NUM_\n> Device types (1=CPU, 2=GPU, 3=FPGA/DSP/Co-Processor)\n\n**-w**, **--workload-profile** _NUM_\n> Workload profile (1=low, 2=default, 3=high, 4=nightmare)\n\n**-b**, **--benchmark**\n> Run benchmark on all hash types\n\n**--show**\n> Show cracked passwords from potfile\n\n**--remove**\n> Remove hashes from file once cracked\n\n**--restore**\n> Restore previous session\n\n**--session** _NAME_\n> Name session for restore capability\n\n**--status**\n> Enable automatic status updates\n\n**--runtime** _SECS_\n> Abort after specified seconds\n\n**-1**, **-2**, **-3**, **-4**\n> Define custom character sets for masks\n\n**-i**, **--increment**\n> Enable mask increment mode\n\n**--increment-min** _NUM_\n> Start mask length for increment\n\n**--increment-max** _NUM_\n> Stop mask length for increment\n\n**-h**, **--help**\n> Show help message\n\n**-V**, **--version**\n> Show version"],["MASK CHARSETS","**?l**\n> Lowercase letters (a-z)\n\n**?u**\n> Uppercase letters (A-Z)\n\n**?d**\n> Digits (0-9)\n\n**?s**\n> Special characters\n\n**?a**\n> All printable characters (?l?u?d?s)\n\n**?b**\n> Binary (0x00-0xff)"],["DESCRIPTION","**hashcat** is a high-performance password recovery tool that uses CPU, GPU, and other hardware accelerators to crack hashed passwords. It supports over 300 hash types including MD5, SHA-family, bcrypt, WPA, and many application-specific formats.\n\nThe tool offers multiple attack modes: dictionary attacks use wordlists, brute-force uses masks to generate candidates, combinator joins words from multiple dictionaries, and hybrid modes combine approaches. Rules can transform dictionary words to match common password patterns.\n\nHashcat maintains a potfile storing cracked hashes to avoid redundant work. Sessions can be paused and restored. The tool automatically optimizes for available hardware and supports distributed cracking across multiple machines."],["CAVEATS","Password cracking is CPU/GPU intensive and generates significant heat. Only use on systems you own or have explicit authorization to test. Large wordlists and complex attacks may run for days or weeks. GPU drivers must be properly installed for hardware acceleration. Some hash types (bcrypt, scrypt) are intentionally slow to crack."],["HISTORY","Hashcat was created by **Jens Steube** (atom) with the first version released in **2009**. Originally closed-source, it became open-source in **December 2015**. The project evolved from CPU-only (hashcat-legacy) to GPU-accelerated (oclHashcat) and eventually unified as hashcat supporting multiple platforms. It became the world's fastest password recovery tool, pioneering techniques like rule-based attacks and optimized GPU kernels."],["INSTALL","```apt: sudo apt install hashcat```\n\n```dnf: sudo dnf install hashcat```\n\n```pacman: sudo pacman -S hashcat```\n\n```zypper: sudo zypper install hashcat```\n\n```brew: brew install hashcat```\n\n```nix: nix profile install nixpkgs#hashcat```"],["SEE ALSO","[john](/man/john)(1), [hydra](/man/hydra)(1), [ophcrack](/man/ophcrack)(1), [aircrack-ng](/man/aircrack-ng)(1)"]],"e":["john","hydra","ophcrack","aircrack-ng"],"x":"hashcat -m 0 -a 0 [hashes.txt] [wordlist.txt]"},"hyperfine":{"n":"hyperfine","t":"command-line benchmarking tool that measures command execution time","s":[["TAGLINE","command-line benchmarking tool that measures command execution time"],["TLDR","**Benchmark a command**\n\n```hyperfine '[sleep 0.3]'```\n\n**Compare multiple commands**\n\n```hyperfine '[command1]' '[command2]' '[command3]'```\n\n**Run warmup iterations** before timing\n\n```hyperfine --warmup [3] '[command]'```\n\n**Set minimum number of runs**\n\n```hyperfine --min-runs [20] '[command]'```\n\n**Run setup command** before each benchmark\n\n```hyperfine --prepare '[make clean]' '[make]'```\n\n**Export results to JSON**\n\n```hyperfine '[command]' --export-json [results.json]```\n\n**Benchmark with parameter range**\n\n```hyperfine -P threads 1 8 '[./program --threads {threads}]'```\n\n**Ignore command failures**\n\n```hyperfine --ignore-failure '[command]'```"],["SYNOPSIS","**hyperfine** [_options_] _command_ [_command ..._]"],["PARAMETERS","**-w**, **--warmup** _n_\n> Run n warmup iterations before timing.\n\n**-m**, **--min-runs** _n_\n> Minimum number of runs (default: 10).\n\n**-M**, **--max-runs** _n_\n> Maximum number of runs.\n\n**-r**, **--runs** _n_\n> Exact number of runs.\n\n**-p**, **--prepare** _cmd_\n> Command to run before each timing run.\n\n**-c**, **--cleanup** _cmd_\n> Command to run after each timing run.\n\n**-s**, **--setup** _cmd_\n> Command to run once before all benchmarks.\n\n**-P**, **--parameter-scan** _var start end_\n> Run benchmark for parameter range.\n\n**-L**, **--parameter-list** _var_ _vals_\n> Run benchmark for comma-separated values.\n\n**-S**, **--shell** _shell_\n> Shell to use (default: system default).\n\n**-N**\n> No shell, run command directly.\n\n**--ignore-failure**\n> Continue on non-zero exit codes.\n\n**--export-json** _file_\n> Export to JSON.\n\n**--export-csv** _file_\n> Export to CSV.\n\n**--export-markdown** _file_\n> Export to Markdown.\n\n**--show-output**\n> Show command output.\n\n**--style** _type_\n> Output style: auto, full, basic, nocolor, color, none."],["DESCRIPTION","**hyperfine** is a command-line benchmarking tool that measures command execution time with statistical analysis. It provides accurate measurements by running commands multiple times and calculating mean, standard deviation, min, max, and relative comparisons.\n\nThe tool automatically determines the optimal number of runs based on variance, ensuring statistically meaningful results. Warmup runs help account for cache effects and JIT compilation in interpreted languages.\n\nWhen comparing multiple commands, hyperfine shows relative speedup/slowdown ratios. Color-coded output highlights the fastest command. This makes A/B testing of optimizations straightforward.\n\nParameter scanning enables benchmarking across value ranges without writing wrapper scripts. For example, testing thread counts from 1-16 with a single command. Results can be exported to JSON, CSV, or Markdown for further analysis or documentation.\n\nThe prepare option enables clean-state benchmarks (e.g., clearing caches or rebuilding). The shell option allows testing shell-specific features or running with minimal shell overhead using -N."],["CAVEATS","System load affects results - close other applications for accurate measurements. Warmup is important for JIT-compiled or cached operations. Very fast commands (< 5ms) may have significant measurement overhead. Statistical outliers can affect mean; check min/max values."],["HISTORY","**hyperfine** was created by David Peter (sharkdp) and released around **2018**. Written in Rust, it was designed as a more user-friendly and statistically rigorous alternative to shell-based benchmarking with **time**. It's part of a collection of modern CLI tools (including fd, bat, etc.) that improve on traditional Unix utilities."],["INSTALL","```apt: sudo apt install hyperfine```\n\n```dnf: sudo dnf install hyperfine```\n\n```pacman: sudo pacman -S hyperfine```\n\n```apk: sudo apk add hyperfine```\n\n```zypper: sudo zypper install hyperfine```\n\n```brew: brew install hyperfine```\n\n```nix: nix profile install nixpkgs#hyperfine```"],["SEE ALSO","[time](/man/time)(1), [perf](/man/perf)(1), [pv](/man/pv)(1)"]],"e":["time","perf","pv"],"x":"hyperfine '[sleep 0.3]'"},"i2cdump":{"n":"i2cdump","t":"reads and displays all registers of an I2C device connected to a specified bus","s":[["TAGLINE","reads and displays all registers of an I2C device connected to a specified bus"],["TLDR","**Dump** all registers of an I2C device\n\n```i2cdump [i2cbus] [device_address]```\n\nDump without **confirmation** prompt\n\n```i2cdump -y [i2cbus] [device_address]```\n\nDump using a **specific mode**\n\n```i2cdump [i2cbus] [device_address] [b|w|c|s|i]```\n\nDump a **range** of registers\n\n```i2cdump -r [start]-[end] [i2cbus] [device_address]```"],["SYNOPSIS","**i2cdump** [_options_] _i2cbus_ _device_address_ [_mode_]"],["PARAMETERS","**-y**\n> Disable interactive confirmation (dangerous on some hardware)\n\n**-r** _START-END_\n> Limit dump to registers in specified range\n\n**b**\n> Byte mode (default for most devices)\n\n**w**\n> Word mode (16-bit values)\n\n**c**\n> Consecutive byte mode\n\n**s**\n> SMBus block mode\n\n**i**\n> I2C block mode"],["DESCRIPTION","**i2cdump** reads and displays all registers of an I2C device connected to a specified bus. It outputs a hexadecimal table showing register addresses and their contents, useful for debugging I2C hardware.\n\nAll addresses should be specified in hexadecimal notation. The i2cbus number can be determined using i2cdetect -l, and device addresses can be found with i2cdetect."],["CAVEATS","Accessing I2C devices can cause hardware issues on some systems. Always use the -y flag with caution. Reading from some registers may have side effects. Requires appropriate permissions (usually root or i2c group membership)."],["HISTORY","i2cdump is part of the i2c-tools package, providing userspace access to I2C buses. The I2C bus protocol was developed by Philips Semiconductor (now NXP) in **1982** for inter-chip communication."],["INSTALL","```apt: sudo apt install i2c-tools```\n\n```dnf: sudo dnf install i2c-tools```\n\n```pacman: sudo pacman -S i2c-tools```\n\n```apk: sudo apk add i2c-tools```\n\n```zypper: sudo zypper install i2c-tools```\n\n```brew: brew install i2c-tools```\n\n```nix: nix profile install nixpkgs#i2c-tools```"],["SEE ALSO","[i2cdetect](/man/i2cdetect)(8), [i2cget](/man/i2cget)(8), [i2cset](/man/i2cset)(8)"]],"e":["i2cdetect","i2cget","i2cset"],"x":"i2cdump [i2cbus] [device_address]"},"idmap_rfc2307":{"n":"idmap_rfc2307","t":"samba ID mapping backend using RFC 2307 LDAP schema","s":[["TAGLINE","samba ID mapping backend using RFC 2307 LDAP schema"],["TLDR","**Configure in smb.conf**\n\n```idmap config DOMAIN : backend = rfc2307```\n\n**Set range**\n\n```idmap config DOMAIN : range = 10000-99999```"],["SYNOPSIS","**idmap_rfc2307** is a Samba configuration module"],["PARAMETERS","**backend**\n> Set to rfc2307.\n\n**range**\n> UID/GID range.\n\n**ldap_server**\n> LDAP server mode.\n\n**bind_path_user**\n> User search base.\n\n**bind_path_group**\n> Group search base."],["DESCRIPTION","**idmap_rfc2307** is a Samba ID mapping backend using RFC 2307 LDAP schema. It maps Windows SIDs to Unix UIDs/GIDs stored in LDAP.\n\nThe backend reads uidNumber and gidNumber attributes from Active Directory or LDAP. It provides consistent ID mapping across systems."],["CAVEATS","Samba configuration module. Requires LDAP with RFC 2307 schema. AD integration needed."],["HISTORY","idmap_rfc2307 is part of **Samba** for enterprise Unix/Windows ID mapping."]],"e":[],"x":"idmap config DOMAIN : backend = rfc2307"},"koji-cancel":{"n":"koji-cancel","t":"cancels one or more active tasks running on the Koji build system","s":[["TAGLINE","cancels one or more active tasks running on the Koji build system"],["TLDR","Cancel a **task by ID**\n\n```koji cancel [task_id]```\n\nCancel **multiple tasks**\n\n```koji cancel [task_id1] [task_id2]```\n\nCancel a **build** by NVR or build ID\n\n```koji cancel [package-1.0-1.fc38]```\n\nCancel **entire task tree** (task and all descendants)\n\n```koji cancel --full [task_id]```\n\n**Force** cancellation on non-owned tasks (admin only)\n\n```koji cancel --force [task_id]```"],["SYNOPSIS","**koji cancel** [_options_] _task_id_ [_task_id_...]"],["DESCRIPTION","**koji cancel** cancels one or more active tasks running on the Koji build system. Tasks must be in a cancelable state (pending or running) for this command to succeed."],["PARAMETERS","**task_id**\n> Numeric task ID, or build NVR / build ID (can specify multiple).\n\n**--justone**\n> Cancel only the task itself, not its descendant subtasks.\n\n**--full**\n> Cancel the task and all of its descendants (full task tree).\n\n**--force**\n> Allow an admin to cancel a task owned by another user.\n\n**-h, --help**\n> Display help information."],["CAVEATS","Only tasks owned by the user or tasks the user has permission to cancel can be stopped. Completed tasks cannot be canceled."],["SEE ALSO","[koji](/man/koji)(1), [koji-build](/man/koji-build)(1)"]],"e":["koji","koji-build"],"x":"koji cancel [task_id]"},"kubectl-logs":{"n":"kubectl-logs","t":"prints container logs from a pod","s":[["TAGLINE","prints container logs from a pod"],["TLDR","**View pod logs**\n\n```kubectl logs [pod-name]```\n\n**Follow logs in real-time**\n\n```kubectl logs -f [pod-name]```\n\n**View specific container logs**\n\n```kubectl logs [pod-name] -c [container]```\n\n**View previous container logs**\n\n```kubectl logs [pod-name] --previous```\n\n**Show last N lines**\n\n```kubectl logs [pod-name] --tail=[100]```\n\n**View logs from last hour**\n\n```kubectl logs [pod-name] --since=[1h]```"],["SYNOPSIS","**kubectl** **logs** [_options_] _pod-name_"],["PARAMETERS","**-f**, **--follow**\n> Stream logs continuously.\n\n**-c**, **--container** _name_\n> Container name.\n\n**--previous**\n> Print previous container logs.\n\n**--tail** _lines_\n> Number of lines from end.\n\n**--since** _duration_\n> Show logs newer than duration.\n\n**--timestamps**\n> Include timestamps."],["DESCRIPTION","**kubectl logs** retrieves and displays the standard output and standard error streams from containers running inside Kubernetes pods. It is one of the primary tools for debugging application behavior, diagnosing crashes, and monitoring runtime output in a cluster environment.\n\nThe command supports real-time log streaming with the -f flag, similar to `tail -f`, and can target a specific container within a multi-container pod using the -c option. You can also view logs from a previous container instance (useful for inspecting crash loops via --previous), limit output to recent entries with --tail, or filter by time window using --since and --since-time. When used with label selectors, it can aggregate logs across multiple pods simultaneously."],["INSTALL","```apt: sudo apt install kubectl```\n\n```pacman: sudo pacman -S kubectl```\n\n```apk: sudo apk add kubectl```\n\n```brew: brew install kubectl```\n\n```nix: nix profile install nixpkgs#kubectl```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [kubectl-exec](/man/kubectl-exec)(1)"]],"e":["kubectl","kubectl-exec"],"x":"kubectl logs [pod-name]"},"linode-cli-tickets":{"n":"linode-cli-tickets","t":"manages Linode support tickets","s":[["TAGLINE","manages Linode support tickets"],["TLDR","**List tickets**\n\n```linode-cli tickets list```\n\n**View ticket**\n\n```linode-cli tickets view [ticket_id]```\n\n**Create ticket**\n\n```linode-cli tickets create --summary \"[Issue summary]\" --description \"[Details]\"```\n\n**Reply to ticket**\n\n```linode-cli tickets reply [ticket_id] --description \"[Reply text]\"```"],["SYNOPSIS","**linode-cli tickets** _command_ [_options_]"],["PARAMETERS","**list**\n> List support tickets.\n\n**view** _id_\n> View ticket details.\n\n**create**\n> Create support ticket.\n\n**reply** _id_\n> Reply to ticket.\n\n**--summary** _text_\n> Ticket summary.\n\n**--description** _text_\n> Ticket description."],["DESCRIPTION","**linode-cli tickets** manages Linode support tickets. Create, view, and reply to support requests. Part of Linode CLI for cloud infrastructure management."],["INSTALL","```brew: brew install linode-cli```\n\n```nix: nix profile install nixpkgs#linode-cli```"],["SEE ALSO","[linode-cli](/man/linode-cli)(1)"]],"e":["linode-cli"],"x":"linode-cli tickets list"},"lxc":{"n":"lxc","t":"manages Linux containers using the LXD REST API","s":[["TAGLINE","manages Linux containers using the LXD REST API"],["TLDR","**List** containers\n\n```lxc list [match_string]```\n\nList **images**\n\n```lxc image list [remote:][match_string]```\n\n**Create** container from image\n\n```lxc init [remote:]image [container]```\n\n**Start** container\n\n```lxc start [remote:]container```\n\n**Stop** container\n\n```lxc stop [remote:]container```\n\nShow container **info**\n\n```lxc info [remote:]container```\n\nTake **snapshot**\n\n```lxc snapshot [remote:]container [snapshot_name]```\n\n**Execute** command in container\n\n```lxc exec [remote:]container [command]```\n\n**Launch** container (create + start)\n\n```lxc launch [remote:]image [container]```"],["SYNOPSIS","**lxc** _command_ [_options_]"],["DESCRIPTION","**lxc** manages Linux containers using the LXD REST API. It provides a user-friendly interface for creating, managing, and interacting with system containers and virtual machines."],["PARAMETERS","**list**\n> List containers\n\n**image list**\n> List available images\n\n**init**\n> Create container from image\n\n**start**\n> Start container\n\n**stop**\n> Stop container\n\n**info**\n> Show container details\n\n**snapshot**\n> Create container snapshot\n\n**exec**\n> Execute command in container\n\n**launch**\n> Create and start a container (combines init + start)\n\n**delete**\n> Delete container\n\n**copy**\n> Copy container\n\n**move**\n> Move container between servers\n\n**file** push|pull\n> Transfer files to/from container\n\n**config**\n> Manage container configuration\n\n**profile**\n> Manage container profiles\n\n**network**\n> Manage networks\n\n**storage**\n> Manage storage pools and volumes\n\n**remote**\n> Manage remote servers\n\n**--debug**\n> Show all debug messages\n\n**--force-local**\n> Force using the local unix socket\n\n**-h**, **--help**\n> Print help\n\n**--project** _PROJECT_\n> Override the source project\n\n**-q**, **--quiet**\n> Do not show progress information\n\n**-v**, **--verbose**\n> Show all information messages"],["CAVEATS","Requires LXD daemon running (lxd init to configure). Container names can be prefixed with remote server names for cluster operations. Manages both system containers and virtual machines. Note that lxc (LXD client) is distinct from the older lxc-* tools (liblxc)."],["HISTORY","**lxc** is the command-line client for LXD, originally developed by **Canonical** starting in **2014** as a next-generation container manager. LXD provides system containers (similar to lightweight VMs) and virtual machines, managed through a REST API that the lxc client communicates with."],["INSTALL","```apt: sudo apt install lxd-client```\n\n```dnf: sudo dnf install lxc```\n\n```pacman: sudo pacman -S lxc```\n\n```zypper: sudo zypper install lxc```\n\n```brew: brew install lxc```\n\n```nix: nix profile install nixpkgs#lxc```"],["SEE ALSO","[lxc-attach](/man/lxc-attach)(1), [lxc-start](/man/lxc-start)(1), [docker](/man/docker)(1), [podman](/man/podman)(1)"]],"e":["lxc-attach","lxc-start","docker","podman"],"x":"lxc list [match_string]"},"matrix-commander":{"n":"matrix-commander","t":"CLI client for the Matrix communication protocol","s":[["TAGLINE","CLI client for the Matrix communication protocol"],["TLDR","**Login and create session**\n\n```matrix-commander --login password```\n\n**Send text message**\n\n```matrix-commander -m \"[message]\" -r \"[!roomid:server]\"```\n\n**Send file**\n\n```matrix-commander -f [file.pdf] -r \"[!roomid:server]\"```\n\n**Send image**\n\n```matrix-commander -i [image.jpg] -r \"[!roomid:server]\"```\n\n**Listen for messages**\n\n```matrix-commander --listen once```\n\n**List rooms**\n\n```matrix-commander --rooms```"],["SYNOPSIS","**matrix-commander** [_options_]"],["PARAMETERS","**--login** _method_\n> Login (password, sso).\n\n**-m**, **--message** _text_\n> Send text message.\n\n**-r**, **--room** _id_\n> Target room ID.\n\n**-f**, **--file** _file_\n> Send file.\n\n**-i**, **--image** _file_\n> Send image.\n\n**--listen** _mode_\n> Listen for messages.\n\n**--rooms**\n> List joined rooms.\n\n**--verify**\n> Verify devices.\n\n**--logout**\n> End session."],["DESCRIPTION","**matrix-commander** is a CLI client for the Matrix communication protocol. It enables sending messages, files, and images from scripts and automation, and can listen for incoming messages.\n\nIdeal for bots, notifications, and scripted Matrix interactions."],["CONFIGURATION","```\n~/.config/matrix-commander/\n├── credentials.json\n└── store/\n```"],["CAVEATS","Requires Python 3. Session must be created first. Encryption requires verification. Room IDs needed for messaging."],["HISTORY","matrix-commander was created to provide a simple command-line interface for Matrix, enabling shell scripting and automation with the Matrix protocol."],["INSTALL","```nix: nix profile install nixpkgs#matrix-commander```"],["SEE ALSO","[gomuks](/man/gomuks)(1), [weechat](/man/weechat)(1)"]],"e":["gomuks","weechat"],"x":"matrix-commander --login password"},"mmd":{"n":"mmd","t":"creates directories on MS-DOS filesystems","s":[["TAGLINE","creates directories on MS-DOS filesystems"],["TLDR","**Create directory on MS-DOS**\n\n```mmd [a:dirname]```\n\n**Create multiple directories**\n\n```mmd [a:dir1] [a:dir2]```\n\n**Create nested path**\n\n```mmd [a:path/to/dir]```\n\n**Verbose output**\n\n```mmd -v [a:dirname]```"],["SYNOPSIS","**mmd** [_options_] _directories_"],["PARAMETERS","_DIRECTORIES_\n> Directories to create on MS-DOS disk.\n\n**-v**\n> Verbose output.\n\n**--help**\n> Display help information."],["DESCRIPTION","**mmd** creates directories on MS-DOS filesystems. It's part of mtools.\n\nThe tool creates directories on FAT filesystems without mounting."],["CAVEATS","Part of mtools. FAT filesystem only. Like mkdir for DOS."],["HISTORY","mmd is part of **mtools**, providing mkdir functionality for MS-DOS filesystems on Unix."],["INSTALL","```apt: sudo apt install mtools```\n\n```dnf: sudo dnf install mtools```\n\n```pacman: sudo pacman -S mtools```\n\n```apk: sudo apk add mtools```\n\n```zypper: sudo zypper install mtools```\n\n```brew: brew install mtools```\n\n```nix: nix profile install nixpkgs#mtools```"],["SEE ALSO","[mcopy](/man/mcopy)(1), [mdel](/man/mdel)(1)"]],"e":["mcopy","mdel"],"x":"mmd [a:dirname]"},"mn":{"n":"mn","t":"micronaut CLI for scaffolding applications and generating code","s":[["TAGLINE","micronaut CLI for scaffolding applications and generating code"],["TLDR","**Create a new application**\n\n```mn create-app [com.example.myapp]```\n\n**Create with specific features**\n\n```mn create-app [com.example.myapp] --features [postgres,hibernate-jpa]```\n\n**Create a function**\n\n```mn create-function [com.example.myfunction]```\n\n**Create a CLI application**\n\n```mn create-cli-app [com.example.mycli]```\n\n**Generate a controller**\n\n```mn create-controller [User]```\n\n**Generate a service bean**\n\n```mn create-bean [UserService]```\n\n**List available features**\n\n```mn feature-diff --features```\n\n**Start interactive mode**\n\n```mn```"],["SYNOPSIS","**mn** _command_ [_options_] [_args_...]"],["COMMANDS","**create-app** _name_\n> Create new Micronaut application.\n\n**create-function** _name_\n> Create serverless function project.\n\n**create-cli-app** _name_\n> Create CLI application.\n\n**create-grpc-app** _name_\n> Create gRPC application.\n\n**create-messaging-app** _name_\n> Create messaging application.\n\n**create-controller** _name_\n> Generate controller class.\n\n**create-bean** _name_\n> Generate service bean.\n\n**create-client** _name_\n> Generate HTTP client.\n\n**create-job** _name_\n> Generate scheduled job.\n\n**create-test** _name_\n> Generate test class.\n\n**feature-diff**\n> Compare feature differences."],["PARAMETERS","**--features** _list_\n> Comma-separated feature list.\n\n**--build** _tool_\n> Build tool: gradle (default), gradle-kotlin, maven.\n\n**--lang** _language_\n> Language: java (default), kotlin, groovy.\n\n**--jdk** _version_\n> Target JDK version.\n\n**--test** _framework_\n> Test framework: junit, spock, kotest.\n\n**--inplace**\n> Create in current directory.\n\n**-i**, **--interactive**\n> Interactive mode."],["FEATURES","**data-jdbc**, **data-jpa**, **hibernate-jpa**\n> Database access options.\n\n**postgres**, **mysql**, **h2**\n> Database drivers.\n\n**security-jwt**, **security-oauth2**\n> Security features.\n\n**kafka**, **rabbitmq**, **nats**\n> Messaging integration.\n\n**graalvm**\n> Native image support.\n\n**openapi**\n> OpenAPI/Swagger documentation.\n\n**graphql**\n> GraphQL support."],["DESCRIPTION","**mn** is the Micronaut CLI for scaffolding applications and generating code. Micronaut is a JVM framework for building cloud-native microservices.\n\n**create-app** generates a project with build files, application class, and configuration. Features add dependencies and configuration automatically.\n\nMicronaut uses compile-time dependency injection—no reflection at runtime. This enables fast startup and low memory, especially with GraalVM native images.\n\nCode generators (**create-controller**, **create-bean**, etc.) add classes following Micronaut conventions. Generated code includes annotations for injection, HTTP routing, and testing.\n\nInteractive mode provides tab completion for commands and features. Use **feature-diff** to see what a feature adds to the project."],["CAVEATS","Compile-time DI requires annotation processors configured correctly. Some features may conflict. Native image requires GraalVM and additional configuration. Generator creates files in conventional locations."],["HISTORY","Micronaut was created by **Graeme Rocher** (creator of Grails) at **Object Computing, Inc.** and released in **2018**. It was designed to address startup time and memory issues in traditional frameworks like Spring. Micronaut pioneered compile-time DI in the Java ecosystem. Version 4.0 (2023) brought modular architecture. The framework is popular for serverless and Kubernetes deployments."],["INSTALL","```apt: sudo apt install mininet```\n\n```dnf: sudo dnf install mininet```\n\n```zypper: sudo zypper install mininet```\n\n```nix: nix profile install nixpkgs#mininet```"],["SEE ALSO","[gradle](/man/gradle)(1), [maven](/man/maven)(1), [quarkus](/man/quarkus)(1)"]],"e":["gradle","maven","quarkus"],"x":"mn create-app [com.example.myapp]"},"monop":{"n":"monop","t":"Mono Class Outline Viewer","s":[["TAGLINE","Mono Class Outline Viewer"],["TLDR","**Show the outline of a type**\n\n```monop [System.String]```\n\n**Show a type from a specific assembly**\n\n```monop -r:[path/to/assembly.dll] [TypeName]```\n\n**List all types in an assembly**\n\n```monop -r:[path/to/assembly.dll] --list```\n\n**Show only members declared in the type** (exclude inherited)\n\n```monop --only-declared [System.Console]```\n\n**Search for a type by partial name**\n\n```monop -s [PartialName]```\n\n**Show private members**\n\n```monop -a [System.String]```"],["SYNOPSIS","**monop** [_options_] _typename_"],["PARAMETERS","**-r:**_assembly_\n> Reference a specific assembly file.\n\n**--only-declared**\n> Only show members declared in the specified type (exclude inherited members).\n\n**--filter-obsolete**\n> Do not show obsolete types and members.\n\n**--refs**\n> Print a list of referenced assemblies for the given assembly.\n\n**--list**\n> List all types in the assembly.\n\n**-a**\n> Show private members.\n\n**-s**\n> Search for a type by partial name across all known assemblies."],["DESCRIPTION","**monop** is a command-line class outline viewer for the Mono framework. It displays the outline of a .NET type, showing class signatures, methods, properties, fields, events, and constructors. It can inspect types from the standard library or from specific assemblies, making it useful for quick API reference without needing a decompiler or IDE."],["INSTALL","```pacman: sudo pacman -S mono```\n\n```apk: sudo apk add mono```\n\n```brew: brew install mono```\n\n```nix: nix profile install nixpkgs#mono```"],["SEE ALSO","[monodis](/man/monodis)(1), [mono](/man/mono)(1), [dotnet](/man/dotnet)(1)"]],"e":["monodis","mono","dotnet"],"x":"monop [System.String]"},"nfsd":{"n":"nfsd","t":"handles NFS requests from clients","s":[["TAGLINE","handles NFS requests from clients"],["TLDR","**Start NFS server**\n\n```sudo systemctl start nfs-server```\n\n**Enable at boot**\n\n```sudo systemctl enable nfs-server```\n\n**Start with a specific number of threads**\n\n```sudo rpc.nfsd [16]```\n\n**Disable NFS version 3**\n\n```sudo rpc.nfsd -N 3```\n\n**Check NFS server status**\n\n```sudo systemctl status nfs-server```\n\n**Start with specific port and RDMA support**\n\n```sudo rpc.nfsd -p [2049] -r```"],["SYNOPSIS","**rpc.nfsd** [_options_] [_nprocs_]"],["PARAMETERS","**nprocs**\n> Number of server threads (default: 8).\n\n**-d**, **--debug**\n> Debug mode.\n\n**-H**, **--host** _addr_\n> Specify a particular hostname or address to accept NFS requests on. By default, all known network addresses are used.\n\n**-p**, **--port** _port_\n> Specify a different port to listen on for NFS requests (default: 2049).\n\n**-r**, **--rdma** [_port_]\n> Enable RDMA transport on the specified port (default: 20049).\n\n**-N**, **--no-nfs-version** _ver_\n> Disable a specific NFS version (e.g., 3, 4, 4.1, 4.2).\n\n**-V**, **--nfs-version** _ver_\n> Enable a specific NFS version.\n\n**-s**, **--syslog**\n> Log error messages to syslog instead of stderr.\n\n**-t**, **--tcp**\n> Listen on a TCP socket (default).\n\n**-T**, **--no-tcp**\n> Disable TCP connections from clients.\n\n**-u**, **--udp**\n> Listen on a UDP socket.\n\n**-U**, **--no-udp**\n> Disable UDP connections from clients (default).\n\n**-L** _seconds_, **--lease-time** _seconds_\n> Set the NFSv4 lease time (10–3600 seconds).\n\n**-G** _seconds_, **--grace-time** _seconds_\n> Set the NFSv4/NLM grace period for reclaiming state.\n\n**-S** _scope_, **--scope** _scope_\n> Set the NFSv4.1+ server scope identifier."],["DESCRIPTION","**nfsd** (NFS server daemon) handles NFS requests from clients. It implements the NFS protocol to share filesystems over the network.\n\nThe number of threads determines how many concurrent requests can be handled."],["CONFIGURATION","```\n/etc/exports              - Exported filesystems\n/etc/nfs.conf            - NFS configuration\n/etc/default/nfs-kernel-server  # Debian\n```"],["EXPORTS FORMAT","```"],["/etc/exports","/home       192.168.1.0/24(rw,sync,no_root_squash)\n/data       *(ro,sync)\n```"],["CAVEATS","Requires rpcbind (except for NFSv4-only configurations). Firewall must allow NFS ports. NFSv4 uses a single port (2049). Security is managed through /etc/exports and optionally Kerberos authentication. The default of 8 threads should be tuned based on workload."],["HISTORY","NFS was developed at **Sun Microsystems** by a team led by **Russel Sandberg** in **1984**, becoming a foundational Unix network protocol."],["SEE ALSO","[showmount](/man/showmount)(8), [rpcbind](/man/rpcbind)(8), [nfsstat](/man/nfsstat)(8)"]],"e":["showmount","rpcbind","nfsstat"],"x":"sudo systemctl start nfs-server"},"nm-settings":{"n":"nm-settings","t":"describes the properties available for NetworkManager connections","s":[["TAGLINE","describes the properties available for NetworkManager connections"],["TLDR","**View connection settings**\n\n```nmcli connection show [connection_name]```\n\n**Set IPv4 address**\n\n```nmcli connection modify [conn] ipv4.addresses \"[192.168.1.10/24]\"```\n\n**Set DNS servers**\n\n```nmcli connection modify [conn] ipv4.dns \"[8.8.8.8 8.8.4.4]\"```\n\n**Set gateway**\n\n```nmcli connection modify [conn] ipv4.gateway \"[192.168.1.1]\"```\n\n**Set to static IP**\n\n```nmcli connection modify [conn] ipv4.method manual```"],["SYNOPSIS","**nm-settings** - NetworkManager connection settings reference"],["PARAMETERS","**connection.id**\n> Connection name.\n\n**connection.type**\n> Connection type.\n\n**ipv4.method**\n> auto, manual, disabled.\n\n**ipv4.addresses**\n> IP addresses.\n\n**ipv4.dns**\n> DNS servers.\n\n**802-11-wireless.ssid**\n> WiFi network name."],["DESCRIPTION","**nm-settings** describes the properties available for NetworkManager connections. These settings are configured via nmcli, nm-connection-editor, or directly in keyfiles.\n\nSettings are organized by category (connection, ipv4, ipv6, wifi, etc.)."],["COMMON SETTINGS","```\nconnection.autoconnect=yes\nipv4.method=auto|manual\nipv4.addresses=192.168.1.10/24\nipv4.gateway=192.168.1.1\nipv4.dns=8.8.8.8\n802-11-wireless.ssid=MyNetwork\n```"],["KEYFILE FORMAT","```ini"],["/etc/NetworkManager/system-connections/MyConn.nmconnection","[connection]\nid=MyConn\ntype=ethernet\n\n[ipv4]\nmethod=manual\naddresses=192.168.1.10/24\ngateway=192.168.1.1\ndns=8.8.8.8;\n```"],["CAVEATS","Setting names vary by connection type. Some settings require specific types. Keyfile format differs from D-Bus names."],["SEE ALSO","[nmcli](/man/nmcli)(1), [nm-connection-editor](/man/nm-connection-editor)(1), [NetworkManager.conf](/man/NetworkManager.conf)(5)"]],"e":["nmcli","nm-connection-editor","NetworkManager.conf"],"x":"nmcli connection show [connection_name]"},"npm-help":{"n":"npm-help","t":"displays documentation for npm commands","s":[["TAGLINE","displays documentation for npm commands"],["TLDR","**Get help on a command**\n\n```npm help [install]```\n\n**Search help topics by keyword**\n\n```npm help-search [dependency]```\n\n**Open help in the browser**\n\n```npm help [command] --viewer browser```\n\n**Read the npm overview**\n\n```npm help npm```\n\n**Open the help index**\n\n```npm help help```"],["SYNOPSIS","**npm** **help** [_topic_]"],["PARAMETERS","_TOPIC_\n> npm command name or general topic. Multiple words form a phrase passed to **man**'s search if no exact page is found.\n\n**--viewer** _TYPE_\n> Help viewer to use: _man_ (default on Unix), _browser_, or _woman_ (Emacs). On Windows the default is _browser_.\n\n**--help**\n> Display help for the **help** command itself."],["DESCRIPTION","**npm help** displays the npm manual page for a topic. By default it shells out to **man**(1) on Unix, but with **--viewer browser** it opens the rendered HTML on docs.npmjs.com.\n\nIf no exact match exists, **npm help** falls back to **npm help-search**, which performs a keyword search across all npm man pages and prints relevance-ranked excerpts."],["CAVEATS","The _man_ viewer requires a working **man** installation. The default viewer can be changed permanently with **npm config set viewer browser**."],["HISTORY","npm help provides access to **npm's documentation** system from the command line."],["INSTALL","```pacman: sudo pacman -S npm```\n\n```apk: sudo apk add npm```\n\n```brew: brew install npm```"],["SEE ALSO","[npm](/man/npm)(1), [npm-docs](/man/npm-docs)(1)"]],"e":["npm","npm-docs"],"x":"npm help [install]"},"npm-run":{"n":"npm-run","t":"executes scripts defined in package","s":[["TAGLINE","executes scripts defined in package"],["TLDR","**Run script from package.json**\n\n```npm run [script-name]```\n\n**List available scripts**\n\n```npm run```\n\n**Run with arguments**\n\n```npm run [script] -- [--flag]```\n\n**Run silently**\n\n```npm run [script] --silent```\n\n**Run if exists**\n\n```npm run [script] --if-present```"],["SYNOPSIS","**npm** **run** [_script_] [-- _args_]"],["PARAMETERS","_SCRIPT_\n> Script name from package.json.\n\n_ARGS_\n> Arguments to pass to script.\n\n**--silent**\n> Suppress output.\n\n**--if-present**\n> Don't fail if missing.\n\n**--help**\n> Display help information."],["DESCRIPTION","**npm run** executes scripts defined in package.json. Core workflow command.\n\nThe command runs scripts section. Passes additional arguments after --."],["CAVEATS","Scripts in package.json. Use -- for script args. Alias: npm run-script."],["HISTORY","npm run is the **primary mechanism** for executing project-defined scripts."],["INSTALL","```pacman: sudo pacman -S npm```\n\n```apk: sudo apk add npm```\n\n```brew: brew install npm```"],["SEE ALSO","[npm](/man/npm)(1), [npm-start](/man/npm-start)(1), [npm-test](/man/npm-test)(1)"]],"e":["npm","npm-start","npm-test"],"x":"npm run [script-name]"},"octave":{"n":"octave","t":"GNU Octave, a numerical computing language","s":[["TAGLINE","GNU Octave, a numerical computing language"],["TLDR","**Start an interactive session** (with GUI)\n\n```octave```\n\n**Start an interactive CLI session** without GUI\n\n```octave --no-gui```\n\n**Run a script file**\n\n```octave [path/to/script.m]```\n\n**Evaluate an expression and exit**\n\n```octave --eval \"[diag([1,2,3])]\"```\n\n**Run a script quietly** (suppress startup messages)\n\n```octave --quiet [path/to/script.m]```\n\n**Run in batch mode** (non-interactive, for scripts)\n\n```octave --no-gui --silent [path/to/script.m]```"],["SYNOPSIS","**octave** [_options_] [_file_]"],["PARAMETERS","_FILE_\n> Script file (.m) to execute.\n\n**--eval** _CODE_\n> Evaluate CODE and exit (unless --persist is also given).\n\n**--gui**\n> Start the graphical user interface.\n\n**--no-gui**\n> Use the command-line interface instead of the GUI.\n\n**-q**, **--quiet**, **--silent**\n> Suppress the startup message and version info.\n\n**-i**, **--interactive**\n> Force interactive mode (useful when running via pipes or remote shells).\n\n**--no-init-file**\n> Don't read the ~/.octaverc or .octaverc initialization files.\n\n**--persist**\n> Stay interactive after --eval or running a script file.\n\n**--path** _dir_\n> Add directory to the head of the function search path.\n\n**--jit-compiler**\n> Enable the JIT compiler for accelerating loops.\n\n**--help**\n> Display help information.\n\n**--version**\n> Display version information."],["DESCRIPTION","**GNU Octave** is a high-level interpreted language primarily intended for numerical computations. It provides a convenient command-line interface and GUI for solving linear and nonlinear problems numerically, and is largely compatible with MATLAB.\n\nOctave features extensive tools for matrix operations, solving systems of equations, numerical integration, statistics, optimization, and 2D/3D plotting. It can be extended with packages from Octave Forge and supports calling C, C++, and Fortran code."],["CAVEATS","MATLAB compatibility is extensive but not complete; some MATLAB toolbox functions are unavailable or behave differently. Large matrix operations can consume significant memory. The JIT compiler is experimental in some versions."],["HISTORY","GNU Octave was created by **John W. Eaton** beginning in **1988** as a companion to a chemical reactor design textbook. It was first released as a standalone application in **1993** and is developed as a GNU project."],["INSTALL","```apt: sudo apt install octave```\n\n```dnf: sudo dnf install octave```\n\n```pacman: sudo pacman -S octave```\n\n```apk: sudo apk add octave```\n\n```zypper: sudo zypper install octave```\n\n```brew: brew install octave```\n\n```nix: nix profile install nixpkgs#octave```"],["SEE ALSO","[matlab](/man/matlab)(1), [gnuplot](/man/gnuplot)(1), [python](/man/python)(1)"]],"e":["matlab","gnuplot","python"],"x":"octave"},"pnmflip":{"n":"pnmflip","t":"Flip and rotate PNM images","s":[["TAGLINE","Flip and rotate PNM images"],["TLDR","**Flip horizontally**\n\n```pnmflip -lr [input.pnm] > [output.pnm]```\n\n**Flip vertically**\n\n```pnmflip -tb [input.pnm] > [output.pnm]```\n\n**Rotate 90 clockwise**\n\n```pnmflip -cw [input.pnm] > [output.pnm]```\n\n**Rotate 90 counter-clockwise**\n\n```pnmflip -ccw [input.pnm] > [output.pnm]```\n\n**Rotate 180**\n\n```pnmflip -r180 [input.pnm] > [output.pnm]```"],["SYNOPSIS","**pnmflip** [_options_] [_file_]"],["PARAMETERS","**-lr**\n> Flip left-right.\n\n**-tb**\n> Flip top-bottom.\n\n**-cw**\n> Rotate 90 degrees clockwise.\n\n**-ccw**\n> Rotate 90 degrees counter-clockwise.\n\n**-r180**\n> Rotate 180 degrees.\n\n**-transpose**\n> Transpose rows and columns."],["DESCRIPTION","**pnmflip** flips and rotates PNM images. Part of Netpbm toolkit for image manipulation. Supports all basic geometric transformations."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm-extras```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pnmrotate](/man/pnmrotate)(1), [pamflip](/man/pamflip)(1)"]],"e":["pnmrotate","pamflip"],"x":"pnmflip -lr [input.pnm] > [output.pnm]"},"regex":{"n":"regex","t":"Regular expression syntax reference","s":[["TAGLINE","Regular expression syntax reference"],["TLDR","This is a reference page for regular expression syntax, not a standalone command."],["SYNOPSIS","**regex** — regular expression pattern matching syntax"],["DESCRIPTION","**Regular expressions** (regex) are patterns used to match character combinations in strings. They are supported by many commands including **grep**, **sed**, **awk**, **perl**, and programming languages."],["BASIC REGEX (BRE)","**.**: Match any single character\n**\\***: Match zero or more of preceding element\n**^**: Match start of line\n**$**: Match end of line\n**[abc]**: Match any character in brackets\n**[^abc]**: Match any character not in brackets\n**[a-z]**: Match character range\n**\\\\**: Escape special character"],["EXTENDED REGEX (ERE)","**+**: Match one or more of preceding element\n**?**: Match zero or one of preceding element\n**|**: Alternation (OR)\n**(...)**: Grouping\n**{n}**: Match exactly n times\n**{n,}**: Match n or more times\n**{n,m}**: Match between n and m times"],["CHARACTER CLASSES","**\\d**: Digit (PCRE) — equivalent to [0-9]\n**\\w**: Word character — [a-zA-Z0-9_]\n**\\s**: Whitespace — [ \\t\\n\\r\\f]\n**\\D**, **\\W**, **\\S**: Negated versions"],["POSIX CLASSES","**[[:alpha:]]**: Alphabetic characters\n**[[:digit:]]**: Digits\n**[[:alnum:]]**: Alphanumeric\n**[[:space:]]**: Whitespace\n**[[:upper:]]**: Uppercase letters\n**[[:lower:]]**: Lowercase letters\n**[[:punct:]]**: Punctuation characters\n**[[:print:]]**: Printable characters (including space)\n**[[:blank:]]**: Space and tab"],["ANCHORS","**^**: Start of line/string\n**$**: End of line/string\n**\\b**: Word boundary (PCRE)\n**\\B**: Non-word boundary (PCRE)"],["CAVEATS","Different tools support different regex flavors: BRE (basic), ERE (extended), PCRE (Perl-compatible). Use appropriate flags: **grep -E** for ERE, **grep -P** for PCRE.\n\nEscape sequences and metacharacters vary between flavors. Test patterns with your specific tool.\n\nGreedy vs. non-greedy: **\\*** and **+** are greedy by default. Use **\\*?** and **+?** for non-greedy matching (PCRE)."],["SEE ALSO","[grep](/man/grep)(1), [sed](/man/sed)(1), [awk](/man/awk)(1), [perlre](/man/perlre)(1)"]],"e":["grep","sed","awk","perlre"],"x":""},"salt-call":{"n":"salt-call","t":"Run Salt modules locally on a minion","s":[["TAGLINE","Run Salt modules locally on a minion"],["TLDR","**Run state locally**\n\n```salt-call state.apply```\n\n**Run specific state**\n\n```salt-call state.apply [apache]```\n\n**Execute module function**\n\n```salt-call cmd.run 'uname -a'```\n\n**Run in local mode**\n\n```salt-call --local grains.items```\n\n**Test mode** (dry run without making changes)\n\n```salt-call state.apply test=True```\n\n**Show output in JSON format**\n\n```salt-call --output json grains.items```\n\n**Run with debug logging**\n\n```salt-call --log-level=debug state.apply```"],["SYNOPSIS","**salt-call** [_options_] _function_ [_arguments_...]"],["PARAMETERS","**--local**\n> Run without connecting to a Salt master. Uses local file client.\n\n**--grains**\n> Return minion grain data.\n\n**--pillar** _data_\n> Set pillar data as a dictionary (e.g., '{\"key\": \"value\"}').\n\n**--output** _format_\n> Output format: json, yaml, nested, raw, highstate, quiet, etc.\n\n**--state-output** _mode_\n> State output verbosity: full, terse, mixed, changes.\n\n**--log-level** _level_\n> Log level: all, garbage, trace, debug, info, warning, error, critical, quiet.\n\n**--id** _MINION_ID_\n> Specify the minion ID (overrides the configured minion ID).\n\n**--retcode-passthrough**\n> Exit with the Salt command return code, useful for scripting.\n\n**--no-color**\n> Disable colored output."],["DESCRIPTION","**salt-call** runs Salt execution modules and states directly on a minion without going through the Salt master. This is useful for testing, debugging, and masterless Salt configurations where the minion applies states independently.\n\nWhen used with **--local**, it operates completely independently of a master, making it ideal for standalone configuration management or testing states before deploying them across infrastructure."],["INSTALL","```apk: sudo apk add salt-lts-minion```"],["SEE ALSO","[salt](/man/salt)(1), [salt-run](/man/salt-run)(1), [salt-master](/man/salt-master)(1), [salt-minion](/man/salt-minion)(1), [salt-key](/man/salt-key)(1)"]],"e":["salt","salt-run","salt-master","salt-minion","salt-key"],"x":"salt-call state.apply"},"sass":{"n":"sass","t":"Compile Sass and SCSS stylesheets to CSS","s":[["TAGLINE","Compile Sass and SCSS stylesheets to CSS"],["TLDR","**Compile SCSS to CSS**\n\n```sass [input.scss] [output.css]```\n\n**Watch for changes**\n\n```sass --watch [input.scss]:[output.css]```\n\n**Watch directory**\n\n```sass --watch [src/scss]:[dist/css]```\n\n**Compile without source maps**\n\n```sass --no-source-map [input.scss] [output.css]```\n\n**Compile compressed** (minified)\n\n```sass --style=compressed [input.scss] [output.css]```\n\n**Compile from stdin**\n\n```sass --stdin --style=compressed```\n\n**Use specific load path**\n\n```sass --load-path=[node_modules] [input.scss] [output.css]```"],["SYNOPSIS","**sass** [_options_] [_input.scss_] [_output.css_]"],["PARAMETERS","**--watch**\n> Watch for changes and recompile.\n\n**--style** _STYLE_\n> Output style: expanded, compressed.\n\n**--source-map**\n> Generate source map.\n\n**--no-source-map**\n> Disable source maps.\n\n**--load-path** _PATH_\n> Path to find imports.\n\n**--charset**\n> Emit @charset for non-ASCII.\n\n**--no-charset**\n> Don't emit @charset.\n\n**--error-css**\n> Emit error in CSS output.\n\n**--update**\n> Only compile changed files.\n\n**--stop-on-error**\n> Stop after first error.\n\n**--color** / **--no-color**\n> Color output control.\n\n**--quiet**\n> Suppress warnings.\n\n**--trace**\n> Show full stack trace on error.\n\n**--stdin**\n> Read from stdin.\n\n**--indented**\n> Parse stdin as indented syntax."],["DESCRIPTION","**Sass** (Syntactically Awesome Style Sheets) is a CSS preprocessor adding variables, nesting, mixins, and other features. It compiles to standard CSS for browser use.\n\nTwo syntaxes exist: SCSS (Sassy CSS, .scss) uses CSS-like braces and semicolons; Sass (.sass) uses indentation. SCSS is more popular for its CSS compatibility.\n\nVariables ($color: #333) enable reusable values. Nesting reflects HTML structure in styles. Mixins (@mixin, @include) share reusable style patterns. Functions perform calculations and transformations.\n\nImports (@use, @import) compose styles from multiple files. The @use rule provides namespaced access and better encapsulation than legacy @import.\n\nWatch mode monitors files for changes, automatically recompiling. This enables rapid development with live reload tools. Source maps connect compiled CSS to original Sass for debugging.\n\nOutput styles control formatting: expanded for development, compressed for production."],["CAVEATS","Node Sass deprecated in favor of Dart Sass (sass package). @import is being phased out for @use. Large projects can have slow compilation. Source maps add file size. Some CSS hacks may not compile correctly."],["HISTORY","**Sass** was created by **Hampton Catlin** and **Natalie Weizenbaum** around **2006**. Originally Ruby-based, it pioneered CSS preprocessing. LibSass (C/C++) improved performance. **Dart Sass** became the primary implementation in **2018**, with the sass npm package replacing node-sass. The SCSS syntax, introduced in **2010**, became the dominant style."],["INSTALL","```apk: sudo apk add dart-sass-js```\n\n```nix: nix profile install nixpkgs#sass```"],["SEE ALSO","[less](/man/less)(1), [postcss](/man/postcss)(1), [stylus](/man/stylus)(1), [tailwindcss](/man/tailwindcss)(1)"]],"e":["less","postcss","stylus","tailwindcss"],"x":"sass [input.scss] [output.css]"},"slurp":{"n":"slurp","t":"Wayland region selection tool","s":[["TAGLINE","Wayland region selection tool"],["TLDR","**Select a region** and print it to stdout\n\n```slurp```\n\nSelect a region while **displaying dimensions**\n\n```slurp -d```\n\nSelect a **single point** instead of a region\n\n```slurp -p```\n\nSelect an **output** and print its name\n\n```slurp -o -f '%o'```\n\nSelect a region and take a **borderless screenshot** with grim\n\n```grim -g \"$(slurp -w 0)\"```\n\nSelect a region and **record video** with wf-recorder\n\n```wf-recorder -g \"$(slurp -w 0)\"```"],["SYNOPSIS","**slurp** [_options_]"],["PARAMETERS","**-d**\n> Display dimensions of selection\n\n**-p**\n> Select a single point instead of region\n\n**-o**\n> Select an entire output (display)\n\n**-r**\n> Require selection to be inside a single output\n\n**-b** _color_\n> Set selection box background color\n\n**-c** _color_\n> Set selection box border color\n\n**-s** _color_\n> Set selection color\n\n**-w** _width_\n> Set selection border width (0 for no border)\n\n**-f** _format_\n> Output format string (%x, %y, %w, %h, %o)\n\n**-a** _ratio_\n> Force aspect ratio (e.g., 16:9)"],["DESCRIPTION","**slurp** allows users to select a region on a Wayland compositor interactively. It outputs the selected region coordinates in a format that can be used by other tools like **grim** for screenshots or **wf-recorder** for screen recording.\n\nThe tool renders a translucent overlay and lets users click and drag to select a rectangular region. It's designed to be combined with other Wayland-native tools through shell command substitution."],["CAVEATS","Wayland only; requires a compositor that supports the layer-shell protocol. Does not capture images itself; use with **grim** for screenshots. Color values use #RRGGBBAA format."],["HISTORY","Written by **Simon Ser** (emersion) as part of the sway/wlroots ecosystem for Wayland. Provides the region selection functionality that **scrot -s** provides on X11, adapted for Wayland's security model."],["INSTALL","```apt: sudo apt install slurp```\n\n```dnf: sudo dnf install slurp```\n\n```pacman: sudo pacman -S slurp```\n\n```apk: sudo apk add slurp```\n\n```zypper: sudo zypper install slurp```\n\n```nix: nix profile install nixpkgs#slurp```"],["SEE ALSO","[grim](/man/grim)(1), [wf-recorder](/man/wf-recorder)(1), [scrot](/man/scrot)(1)"]],"e":["grim","wf-recorder","scrot"],"x":"slurp"},"starship":{"n":"starship","t":"Cross-shell customizable prompt in Rust","s":[["TAGLINE","Cross-shell customizable prompt in Rust"],["TLDR","**Initialize for bash**\n\n```eval \"$(starship init bash)\"```\n\n**Initialize for zsh**\n\n```eval \"$(starship init zsh)\"```\n\n**Initialize for fish**\n\n```starship init fish | source```\n\n**Initialize for PowerShell**\n\n```Invoke-Expression (&starship init powershell)```\n\n**Print current configuration**\n\n```starship print-config```\n\n**Generate preset config**\n\n```starship preset [pastel-powerline] > ~/.config/starship.toml```\n\n**List available presets**\n\n```starship preset --list```\n\n**Explain active prompt segments**\n\n```starship explain```\n\n**Show prompt rendering times**\n\n```starship timings```"],["SYNOPSIS","**starship** [_init shell_] [_prompt_] [_preset_] [_options_]"],["PARAMETERS","**init** _SHELL_\n> Generate shell init script.\n\n**prompt**\n> Print prompt.\n\n**preset** _NAME_\n> Print preset configuration.\n\n**print-config**\n> Print config.\n\n**explain**\n> Explain prompt segments.\n\n**timings**\n> Time prompt rendering.\n\n**completions** _SHELL_\n> Generate completions.\n\n**bug-report**\n> Generate bug report.\n\n**--help**\n> Show help.\n\n**--version**\n> Show version."],["DESCRIPTION","**starship** is a minimal, fast, cross-shell prompt. Written in Rust, it provides consistent appearance and features across bash, zsh, fish, PowerShell, and more.\n\nConfiguration in ~/.config/starship.toml customizes modules. Each module shows specific information: git status, language versions, cloud context, etc.\n\nModules appear contextually. Python version shows only in Python projects. Git info shows only in repositories. This keeps prompts clean.\n\nPresets provide ready-made configurations. They range from minimal to information-rich, with various color schemes.\n\nSpeed is a priority. The prompt renders quickly even with many modules enabled. Timings command helps identify slow modules.\n\nThe prompt is cross-platform, working on Linux, macOS, and Windows. Configuration is portable across systems."],["CONFIGURATION","**~/.config/starship.toml**\n> Main configuration file defining enabled modules, display format, colors, and per-module settings.\n\n**STARSHIP_CONFIG**\n> Environment variable to specify an alternate configuration file path."],["CAVEATS","Requires Nerd Font for icons. Configuration syntax may differ from shell prompts. Some modules need external commands."],["HISTORY","**Starship** was created by **Matan Kushner** around **2019**. It gained popularity as a fast, configurable alternative to framework-specific prompts like Oh My Zsh themes."],["INSTALL","```apt: sudo apt install starship```\n\n```pacman: sudo pacman -S starship```\n\n```apk: sudo apk add starship```\n\n```zypper: sudo zypper install starship```\n\n```brew: brew install starship```\n\n```nix: nix profile install nixpkgs#starship```"],["SEE ALSO","[bash](/man/bash)(1), [zsh](/man/zsh)(1), [fish](/man/fish)(1), [powerline](/man/powerline)(1)"]],"e":["bash","zsh","fish","powerline"],"x":"eval \"$(starship init bash)\""},"storagectl":{"n":"storagectl","t":"Enumerate and mount storage volumes provided by systemd storage providers","s":[["TAGLINE","Enumerate and mount storage volumes provided by systemd storage providers"],["TLDR","**List all available storage volumes**\n\n```storagectl volumes```\n\n**List storage providers**\n\n```storagectl providers```\n\n**List volume templates**\n\n```storagectl templates```\n\n**List volumes matching a glob**\n\n```storagectl volumes '*foo*'```\n\n**Mount a directory volume** using the storage mount helper (as root)\n\n```mount -t storage fs:myvol /mnt/myvol```"],["SYNOPSIS","**storagectl** [OPTIONS...] {COMMAND} [NAME...]"],["DESCRIPTION","**storagectl** is a command-line tool to inspect storage providers and the storage volumes they expose. It is part of the systemd project.\n\nA storage provider implements the `io.systemd.StorageProvider` Varlink interface and registers an AF_UNIX socket under `/run/systemd/io.systemd.StorageProvider/` (system) or the user equivalent.\n\nsystemd ships two providers:\n\n- `systemd-storage-block@.service` — exposes the system's block devices.\n- `systemd-storage-fs@.service` — exposes regular files and directories from a backing filesystem (supports on-the-fly creation via templates).\n\nstoragectl can also act as a mount helper (`mount -t storage ...` or `mount -t storage.FSTYPE ...`) to acquire and mount volumes at arbitrary locations. The source is specified as `PROVIDER:VOLUME`."],["COMMANDS","**volumes** [_GLOB_]\n> List volumes (default command). Optional glob filters volume names.\n\n**templates** [_GLOB_]\n> List supported volume templates (for providers that support dynamic creation).\n\n**providers**\n> List known storage providers and whether their sockets are reachable."],["OPTIONS","**--system**\n> Operate on system providers (default).\n\n**--user**\n> Operate on per-user providers.\n\n**--json**=_MODE_\n> Output as JSON (\"short\", \"pretty\", or \"off\").\n\n**--no-pager**, **--no-legend**, **--no-ask-password**\n> Control output and authentication behavior."],["CAVEATS","This tool is relatively new (added in systemd 261). Storage providers are an experimental/low-level interface primarily for advanced use cases and other systemd components (e.g. machinectl)."],["SEE ALSO","[systemctl](/man/systemctl)(1), [mount](/man/mount)(8), [machinectl](/man/machinectl)(1)"],["RESOURCES","```[Source code](https://github.com/systemd/systemd)```\n\n```[Documentation](https://www.freedesktop.org/software/systemd/man/latest/storagectl.html)```"]],"e":["systemctl","mount","machinectl"],"x":"storagectl volumes"},"svgcleaner":{"n":"svgcleaner","t":"Lossless SVG file optimizer","s":[["TAGLINE","Lossless SVG file optimizer"],["TLDR","**Clean SVG**\n\n```svgcleaner [input.svg] [output.svg]```\n\n**Maximum compression**\n\n```svgcleaner --preset=maximum [input.svg] [output.svg]```\n\n**Preserve specific attributes**\n\n```svgcleaner --keep-named-ids [input.svg] [output.svg]```\n\n**Indent output**\n\n```svgcleaner --indent [2] [input.svg] [output.svg]```\n\n**Quiet mode**\n\n```svgcleaner -q [input.svg] [output.svg]```"],["SYNOPSIS","**svgcleaner** [_--preset preset_] [_options_] _input_ _output_"],["PARAMETERS","**--preset** _NAME_\n> Optimization preset.\n\n**--keep-named-ids**\n> Preserve IDs.\n\n**--indent** _N_\n> Indentation spaces.\n\n**-q**\n> Quiet mode.\n\n**--multipass**\n> Multiple optimization passes."],["DESCRIPTION","**svgcleaner** performs lossless optimization of SVG files, reducing file size without altering their visual appearance. It removes unnecessary metadata, editor artifacts, unused definitions, and redundant attributes that bloat SVG files exported from graphic editors.\n\nThe tool simplifies path data, optimizes geometry, merges compatible elements, and removes empty groups and containers. Multiple optimization passes can be applied for maximum size reduction. Optimization presets allow choosing between conservative and aggressive cleaning levels.\n\nWritten in Rust for performance, svgcleaner is particularly effective at cleaning SVGs produced by editors like Inkscape, Adobe Illustrator, and Sketch, which often embed substantial editor-specific metadata and redundant styling information."],["CAVEATS","May break complex SVGs. Test output. Rust written."],["HISTORY","**svgcleaner** was created as a lossless SVG optimizer, reducing file size without affecting visual appearance."],["INSTALL","```pacman: sudo pacman -S svgcleaner```\n\n```zypper: sudo zypper install svgcleaner```\n\n```nix: nix profile install nixpkgs#svgcleaner```"],["SEE ALSO","[svgo](/man/svgo)(1), [inkscape](/man/inkscape)(1), [scour](/man/scour)(1)"]],"e":["svgo","inkscape","scour"],"x":"svgcleaner [input.svg] [output.svg]"},"svnadmin":{"n":"svnadmin","t":"Subversion repository administration tool","s":[["TAGLINE","Subversion repository administration tool"],["TLDR","**Create a new repository**\n\n```svnadmin create [/path/to/repo]```\n\n**Dump repository to file**\n\n```svnadmin dump [/path/to/repo] > [backup.dump]```\n\n**Load dump into repository**\n\n```svnadmin load [/path/to/repo] < [backup.dump]```\n\n**Verify repository integrity**\n\n```svnadmin verify [/path/to/repo]```\n\n**List outstanding transactions**\n\n```svnadmin lstxns [/path/to/repo]```\n\n**Remove a dead transaction**\n\n```svnadmin rmtxns [/path/to/repo] [txn-id]```\n\n**Upgrade repository format**\n\n```svnadmin upgrade [/path/to/repo]```\n\n**Show repository info**\n\n```svnadmin info [/path/to/repo]```"],["SYNOPSIS","**svnadmin** _subcommand_ _repos-path_ [_options_] [_args_]"],["PARAMETERS","**--revision** _rev_, **-r** _rev_\n> Specify revision or revision range.\n\n**--incremental**\n> Dump only changes since previous revision.\n\n**--deltas**\n> Use deltas in dump output for smaller files.\n\n**--fs-type** _type_\n> Filesystem type for new repos: fsfs or bdb.\n\n**--force-uuid**\n> Set repository UUID from dump stream.\n\n**--ignore-uuid**\n> Ignore UUID in dump stream.\n\n**--quiet**, **-q**\n> Suppress normal output.\n\n**--wait**\n> Wait for repository lock instead of failing.\n\n**--config-dir** _dir_\n> Use alternate config directory.\n\n**--pre-1.4-compatible**\n> Create repository compatible with Subversion 1.4 or older.\n\n**--pre-1.6-compatible**\n> Create repository compatible with Subversion 1.6 or older.\n\n**--compatible-version** _ver_\n> Create repository compatible with specified Subversion version.\n\n**--bypass-hooks**\n> Skip hook scripts during load.\n\n**--bypass-prop-validation**\n> Skip property validation during load."],["SUBCOMMANDS","**create**: Create new empty repository.\n\n**dump**: Output repository contents in portable format.\n\n**load**: Load dump file into repository.\n\n**verify**: Check repository for corruption.\n\n**recover**: Recover repository from crash.\n\n**upgrade**: Upgrade repository to latest format.\n\n**hotcopy**: Make backup copy of repository.\n\n**lstxns**: List outstanding transactions.\n\n**rmtxns**: Remove specified transactions.\n\n**info**: Display repository information.\n\n**setlog**: Set log message for a revision.\n\n**setrevprop**: Set revision property.\n\n**pack**: Optimize repository storage.\n\n**freeze**: Freeze repository for backup."],["DESCRIPTION","**svnadmin** is the administrative tool for Subversion repositories. It provides commands for creating, maintaining, and backing up repositories stored on the local filesystem.\n\nThe tool can create repositories with either FSFS (default) or Berkeley DB backends. It supports full and incremental dumps for backup and migration, with optional delta compression for smaller dump files.\n\nRepository maintenance includes integrity verification, crash recovery, format upgrades, and transaction cleanup. The hotcopy command creates consistent backups without stopping repository access.\n\nUnlike the svn client, svnadmin operates only on local repository paths and cannot access repositories over the network."],["CAVEATS","Operates only on local repositories, not URLs. Dump files can be very large for full exports. Berkeley DB backend is deprecated in favor of FSFS. Hook bypass options should be used carefully. Repository locks prevent concurrent administrative operations."],["HISTORY","**svnadmin** has been part of Subversion since its initial release in 2000. Subversion was created by CollabNet as a replacement for CVS. The tool evolved alongside repository format changes, adding support for FSFS (Subversion 1.1), repository packing (1.6), and various performance improvements."],["INSTALL","```apt: sudo apt install subversion```\n\n```dnf: sudo dnf install subversion```\n\n```pacman: sudo pacman -S subversion```\n\n```apk: sudo apk add subversion```\n\n```zypper: sudo zypper install subversion```\n\n```brew: brew install subversion```\n\n```nix: nix profile install nixpkgs#subversion```"],["SEE ALSO","[svn](/man/svn)(1), [svnserve](/man/svnserve)(8), [git](/man/git)(1)"]],"e":["svn","svnserve","git"],"x":"svnadmin create [/path/to/repo]"},"systemctl-condreload":{"n":"systemctl-condreload","t":"Alias for try-reload-or-restart","s":[["TAGLINE","Alias for try-reload-or-restart"],["TLDR","This command is an alias of [systemctl try-reload-or-restart](/man/systemctl-try-reload-or-restart)."],["SEE ALSO","[systemctl-try-reload-or-restart](/man/systemctl-try-reload-or-restart)(1), [systemctl](/man/systemctl)(1)"]],"e":["systemctl-try-reload-or-restart","systemctl"],"x":""},"systemd-nspawn":{"n":"systemd-nspawn","t":"Lightweight namespace container","s":[["TAGLINE","Lightweight namespace container"],["TLDR","**Run** a command in a container\n\n```systemd-nspawn -D [path/to/container_root]```\n\n**Run** a full Linux OS in a container (boot)\n\n```systemd-nspawn -b -D [path/to/container_root]```\n\n**Run** the command as PID 2 using a stub init process\n\n```systemd-nspawn -D [path/to/container_root] -a```\n\n**Specify** the machine name and hostname\n\n```systemd-nspawn -M [container_name] --hostname [hostname] -D [path/to/container_root]```"],["SYNOPSIS","**systemd-nspawn** [_options_] [_command_] [_arguments_]"],["PARAMETERS","**-D, --directory _path_**\n> Root directory of the container\n\n**-b, --boot**\n> Boot the container's init system\n\n**-M, --machine _name_**\n> Set the machine name\n\n**-a, --as-pid2**\n> Run command as PID 2 with stub init\n\n**--hostname _name_**\n> Set the container hostname\n\n**-u, --user _user_**\n> Run as specified user\n\n**--bind _src:dest_**\n> Bind mount host path into container\n\n**-n, --network-veth**\n> Create a virtual Ethernet link\n\n**--private-network**\n> Disconnect from host network"],["DESCRIPTION","**systemd-nspawn** spawns a command or full operating system in a lightweight container. It uses Linux namespaces and cgroups for isolation but is simpler than full container solutions like Docker.\n\nThe tool is useful for testing, building packages, and running isolated services. With **--boot**, it can run a complete init system, making it suitable for system containers."],["CAVEATS","Requires root privileges. The container filesystem must contain a complete OS or at least the required binaries. Network isolation requires additional configuration. Part of the systemd suite."],["INSTALL","```apt: sudo apt install systemd-container```\n\n```dnf: sudo dnf install systemd-container```\n\n```zypper: sudo zypper install systemd-container```"],["SEE ALSO","[machinectl](/man/machinectl)(1), [systemd-detect-virt](/man/systemd-detect-virt)(1), [systemd-dissect](/man/systemd-dissect)(1), [chroot](/man/chroot)(1)"]],"e":["machinectl","systemd-detect-virt","systemd-dissect","chroot"],"x":"systemd-nspawn -D [path/to/container_root]"},"top":{"n":"top","t":"Real-time system process monitor","s":[["TAGLINE","Real-time system process monitor"],["TLDR","Start **top**\n\n```top```\n\nDo not show any **idle or zombie** processes\n\n```top -i```\n\nShow only processes owned by a **specific user**\n\n```top -u [username]```\n\n**Sort** processes by a field\n\n```top -o %MEM```\n\nShow individual **threads** of a process\n\n```top -Hp [process_id]```\n\nMonitor **specific PIDs**\n\n```top -p [1234],[5678]```\n\nRun in **batch mode** for scripting (single snapshot)\n\n```top -b -n 1```\n\nSet **update interval** to 5 seconds\n\n```top -d 5```\n\nShow **full command lines** instead of program names\n\n```top -c```"],["SYNOPSIS","**top** [_-b_] [_-c_] [_-d delay_] [_-H_] [_-i_] [_-n iterations_] [_-o field_] [_-p pid_] [_-S_] [_-u user_] [_-w width_]"],["DESCRIPTION","**top** provides a dynamic real-time view of a running system. It displays system summary information and a list of processes or threads currently being managed by the Linux kernel. The display is updated periodically and can be customized interactively."],["PARAMETERS","**-b**, **--batch**\n> Run in batch mode (non-interactive), useful for piping output.\n\n**-c**, **--cmdline-toggle**\n> Toggle between command line and program name display.\n\n**-d** _SECS_, **--delay=SECS**\n> Set the delay between screen updates (default: 3.0 seconds).\n\n**-e**, **--scale-task-mem**\n> Scale task memory values (k/m/g/t/p).\n\n**-E**, **--scale-summary-mem**\n> Scale summary memory values (k/m/g/t/p/e).\n\n**-H**, **--threads-show**\n> Display individual threads instead of processes.\n\n**-i**, **--idle-toggle**\n> Hide idle and zombie processes.\n\n**-n** _NUMBER_, **--iterations=NUMBER**\n> Set maximum number of iterations before exit.\n\n**-o** _FIELD_, **--sort-override=FIELD**\n> Override the default sort field. Prefix with + or - for ascending/descending.\n\n**-p** _PIDLIST_, **--pid=PIDLIST**\n> Monitor only specified process IDs (comma-separated).\n\n**-S**, **--accum-time-toggle**\n> Toggle cumulative time mode, showing CPU time used by each process and its dead children.\n\n**-s**, **--secure-mode**\n> Start top in secure mode, disabling potentially dangerous interactive commands.\n\n**-u** _USER_, **--filter-only-euser=USER**\n> Show only processes for specified effective user.\n\n**-U** _USER_, **--filter-any-user=USER**\n> Show processes for specified user (real, effective, saved, filesystem).\n\n**-w** _WIDTH_, **--width=WIDTH**\n> Override screen width in batch mode (minimum 80 characters)."],["INTERACTIVE COMMANDS","**h or ?**: Display help screen\n**q**: Quit top\n**d or s**: Change delay between updates\n**k**: Kill a process (prompts for PID and signal)\n**r**: Renice a process (prompts for PID and nice value)\n**f**: Manage displayed fields\n**o**: Add filter expression\n**c**: Toggle command line/program name\n**V**: Toggle forest view mode\n**1**: Toggle single/separate CPU states\n**m**: Cycle memory display modes\n**t**: Cycle CPU states display modes\n**R**: Reverse sort order\n**P**: Sort by CPU usage\n**M**: Sort by memory usage\n**T**: Sort by cumulative time\n**< or >**: Move sort column left/right\n**W**: Write configuration file"],["PROCESS STATUS CODES","**D**: Uninterruptible sleep\n**I**: Idle kernel thread\n**R**: Running\n**S**: Sleeping (interruptible)\n**T**: Stopped by job control signal\n**t**: Stopped by debugger\n**Z**: Zombie"],["CAVEATS","The configuration file is saved to ~/.config/procps/toprc. Running in batch mode (-b) is recommended for scripting as it disables interactive features and produces parseable output. The default delay between updates is 3.0 seconds."],["HISTORY","**top** was originally written by Roger Binns and has been part of the **procps** (now **procps-ng**) package. It has been a standard system monitoring tool on Unix-like systems since the 1980s."],["INSTALL","```apt: sudo apt install procps```\n\n```dnf: sudo dnf install procps-ng```\n\n```pacman: sudo pacman -S procps-ng```\n\n```apk: sudo apk add procps-ng```\n\n```zypper: sudo zypper install procps```\n\n```brew: brew install procps```\n\n```nix: nix profile install nixpkgs#procps```"],["SEE ALSO","[htop](/man/htop)(1), [atop](/man/atop)(1), [ps](/man/ps)(1), [free](/man/free)(1), [uptime](/man/uptime)(1), [vmstat](/man/vmstat)(1), [kill](/man/kill)(1)"]],"e":["htop","atop","ps","free","uptime","vmstat","kill"],"x":"top"},"traceroute6":{"n":"traceroute6","t":"Trace IPv6 packet route to host","s":[["TAGLINE","Trace IPv6 packet route to host"],["TLDR","**Trace the route to an IPv6 host**\n\n```traceroute6 [host]```\n\n**Trace with numeric output only** (no DNS resolution)\n\n```traceroute6 -n [host]```\n\n**Set maximum hop limit**\n\n```traceroute6 -m [60] [host]```\n\n**Start from a specific hop**\n\n```traceroute6 -f [5] [host]```\n\n**Use ICMP ECHO instead of UDP**\n\n```traceroute6 -I [host]```\n\n**Set the number of probes per hop**\n\n```traceroute6 -q [5] [host]```\n\n**Specify source address**\n\n```traceroute6 -s [source_address] [host]```"],["SYNOPSIS","**traceroute6** [_-dIlnNrvU_] [_-f firsthop_] [_-g gateway_] [_-m hoplimit_] [_-p port_] [_-q probes_] [_-s src_] [_-w waittime_] _target_ [_datalen_]"],["PARAMETERS","**-n**\n> Do not resolve addresses to hostnames\n\n**-m** _hoplimit_\n> Maximum hop limit (default: 30, max: 255)\n\n**-f** _firsthop_\n> Start probes at specified hop (skip earlier hops)\n\n**-I**\n> Use ICMPv6 ECHO instead of UDP datagrams\n\n**-N**\n> Use packets with no upper layer header\n\n**-p** _port_\n> Set destination UDP port\n\n**-q** _probes_\n> Number of probes per hop (default: 3)\n\n**-s** _src_\n> Use specified source address\n\n**-w** _waittime_\n> Wait time in seconds for responses\n\n**-g** _gateway_\n> Specify intermediate gateway (uses routing header)"],["DESCRIPTION","**traceroute6** traces the route IPv6 packets take to reach a destination host. It exploits the IPv6 hop limit field by sending probes with increasing limits, eliciting ICMPv6 TIME_EXCEEDED responses from each router along the path.\n\nEach line of output shows the hop number, gateway address (and hostname unless **-n** is used), and round-trip times for each probe. The trace continues until the destination responds or the maximum hop limit is reached.\n\nThe command is equivalent to **traceroute -6** and sends UDP datagrams by default. Use **-I** for ICMPv6 ECHO probes instead."],["CAVEATS","Requires CAP_NET_RAW capability or root privileges on Linux. Some routers may not respond to probes, appearing as * * * in output. Firewalls may block traceroute probes. Response times can vary significantly between probes."],["HISTORY","traceroute was originally written by **Van Jacobson** in **1988**. The IPv6 version was ported from NRL's IPv6 distribution in **1996** and later ported to Linux by Pedro Roque."],["SEE ALSO","[traceroute](/man/traceroute)(1), [ping6](/man/ping6)(8), [mtr](/man/mtr)(8), [tracepath](/man/tracepath)(8)"]],"e":["traceroute","ping6","mtr","tracepath"],"x":"traceroute6 [host]"},"tred":{"n":"tred","t":"Compute transitive reduction of directed graphs","s":[["TAGLINE","Compute transitive reduction of directed graphs"],["TLDR","**Compute transitive reduction** of a graph\n\n```tred [input.dot] > [output.dot]```\n\n**Process from stdin**\n\n```cat [graph.dot] | tred > [reduced.dot]```\n\n**Show verbose output** with removed edges info\n\n```tred -v [input.dot]```\n\n**Reduce graph before rendering** with dot\n\n```tred [graph.dot] | dot -Tpng -o [output.png]```"],["SYNOPSIS","**tred** [**-v**] [**-?**] [_files_]"],["PARAMETERS","**-v**\n> Verbose output. Print information about removed edges to stderr.\n\n**-?**\n> Print usage information and exit."],["DESCRIPTION","**tred** computes the transitive reduction of directed graphs. It removes edges that are implied by transitivity, simplifying the graph while preserving reachability relationships.\n\nIn a transitive reduction, if there exists a path from node A to node C through node B (A→B→C), the direct edge A→C is removed as redundant. This is particularly useful for simplifying dependency graphs, inheritance hierarchies, and other directed graphs where implied relationships clutter visualization.\n\nThe tool reads graphs in DOT format from input files or standard input, and outputs the reduced graph to standard output. Nodes and subgraphs are preserved; only redundant edges are removed. Undirected graphs are silently ignored.\n\ntred is commonly used as a preprocessor to **dot** to reduce visual clutter in dense graph layouts."],["CAVEATS","If a graph contains cycles, its transitive reduction is not uniquely defined. In such cases, tred emits a warning. The current implementation could be faster using bitmap data structures internally."],["HISTORY","**tred** is part of the **Graphviz** project, originally developed at **AT&T Labs Research** in the **1990s**. It was created by **Stephen C. North** and **Emden R. Gansner** as part of the graph visualization toolkit. Graphviz was open-sourced in **2000** and has since become the standard tool for graph layout and visualization."],["INSTALL","```apt: sudo apt install graphviz```\n\n```dnf: sudo dnf install graphviz```\n\n```pacman: sudo pacman -S graphviz```\n\n```apk: sudo apk add graphviz```\n\n```zypper: sudo zypper install graphviz```\n\n```brew: brew install graphviz```\n\n```nix: nix profile install nixpkgs#graphviz```"],["SEE ALSO","[dot](/man/dot)(1), [acyclic](/man/acyclic)(1), [gc](/man/gc)(1), [gvpr](/man/gvpr)(1), [ccomps](/man/ccomps)(1), [sccmap](/man/sccmap)(1)"]],"e":["dot","acyclic","gc","gvpr","ccomps","sccmap"],"x":"tred [input.dot] > [output.dot]"},"twm":{"n":"twm","t":"Classic X11 Tab Window Manager","s":[["TAGLINE","Classic X11 Tab Window Manager"],["TLDR","**Start window manager**\n\n```twm```\n\n**With display**\n\n```twm -display [localhost:0]```\n\n**Verbose mode**\n\n```twm -v```\n\n**Single screen**\n\n```twm -s```"],["SYNOPSIS","**twm** [_-display dpy_] [_-v_] [_-s_] [_options_]"],["PARAMETERS","**-display** _DPY_\n> X display to use.\n\n**-v**\n> Verbose mode.\n\n**-s**\n> Single screen.\n\n**-f** _FILE_\n> Config file."],["DESCRIPTION","**twm** (Tab Window Manager) is a minimal window manager for the X Window System. It provides basic window management functionality including title bars, borders, window resizing, and iconification using a small memory footprint.\n\nThe window manager is highly configurable through the **~/.twmrc** configuration file, which defines key bindings, menus, title bar buttons, and window appearance. It supports virtual screens for organizing workspaces.\n\nAs part of the standard X.Org distribution, twm serves as a fallback window manager when no other is configured. Its minimalism makes it suitable for resource-constrained environments and as a base for understanding X11 window management."],["CONFIGURATION","**~/.twmrc**\n> Main configuration file defining key bindings, menus, title bar buttons, colors, and window behavior."],["CAVEATS","Very minimal. No modern features. Legacy X11."],["HISTORY","**twm** (Tab Window Manager) was the default window manager for X11, created by Tom LaStrange in 1987."],["INSTALL","```apt: sudo apt install twm```\n\n```apk: sudo apk add twm```\n\n```zypper: sudo zypper install twm```\n\n```brew: brew install twm```\n\n```nix: nix profile install nixpkgs#twm```"],["SEE ALSO","[fvwm](/man/fvwm)(1), [i3](/man/i3)(1), [openbox](/man/openbox)(1)"]],"e":["fvwm","i3","openbox"],"x":"twm"},"unison":{"n":"unison","t":"Bidirectional file synchronization tool","s":[["TAGLINE","Bidirectional file synchronization tool"],["TLDR","**Sync directories**\n\n```unison [dir1] [dir2]```\n\n**Sync with SSH**\n\n```unison [local/path] ssh://[user@host]/[path]```\n\n**Batch mode**\n\n```unison -batch [dir1] [dir2]```\n\n**Use profile**\n\n```unison [profilename]```\n\n**Show differences**\n\n```unison -diff [dir1] [dir2]```"],["SYNOPSIS","**unison** [_-batch_] [_profile_] _root1_ _root2_"],["PARAMETERS","**-batch**\n> Non-interactive.\n\n**-auto**\n> Accept defaults.\n\n**-diff**\n> Show differences.\n\n**-force** _ROOT_\n> Force from root.\n\n**-ignore** _PATTERN_\n> Ignore pattern.\n\n**-path** _PATH_\n> Sync specific path."],["DESCRIPTION","**unison** is a file synchronization tool that propagates changes in both directions between two directory replicas. Unlike one-way sync tools like rsync, unison detects modifications on both sides and reconciles them, making it suitable for keeping directories in sync across multiple machines.\n\nWhen conflicts arise from both replicas being modified, unison detects the conflict and presents options for resolution rather than silently overwriting changes. It can synchronize over SSH for remote directories or work with local paths. The tool tracks file state between synchronizations, so it only transfers files that have actually changed.\n\nUnison supports profiles that save synchronization configurations for repeated use, including path filters, ignore patterns, and connection settings. It runs on Unix, macOS, and Windows, and requires the same version of unison on both sides of a synchronization."],["CAVEATS","Both sides must run the same version of unison; version mismatches cause failures. Conflicts require manual attention. Initial synchronization can be slow for large directory trees."],["HISTORY","**Unison** was developed by **Benjamin Pierce** at the University of Pennsylvania, first released around **2001**. It is written in OCaml and based on research in file synchronization theory."],["INSTALL","```dnf: sudo dnf install unison```\n\n```pacman: sudo pacman -S unison```\n\n```apk: sudo apk add unison```\n\n```zypper: sudo zypper install unison```\n\n```brew: brew install unison```\n\n```nix: nix profile install nixpkgs#unison```"],["SEE ALSO","[rsync](/man/rsync)(1), [syncthing](/man/syncthing)(1), [rclone](/man/rclone)(1)"]],"e":["rsync","syncthing","rclone"],"x":"unison [dir1] [dir2]"},"virt-customize":{"n":"virt-customize","t":"Customize VM disk images offline","s":[["TAGLINE","Customize VM disk images offline"],["TLDR","**Set root password**\n\n```virt-customize -a [image.qcow2] --root-password password:[newpass]```\n\n**Install packages**\n\n```virt-customize -a [image.qcow2] --install [nginx,vim]```\n\n**Upload file to image**\n\n```virt-customize -a [image.qcow2] --upload [local.conf]:[/etc/app.conf]```\n\n**Run shell script**\n\n```virt-customize -a [image.qcow2] --run [setup.sh]```\n\n**Run command**\n\n```virt-customize -a [image.qcow2] --run-command \"[systemctl enable nginx]\"```\n\n**Inject SSH key**\n\n```virt-customize -a [image.qcow2] --ssh-inject [root]:file:[~/.ssh/id_rsa.pub]```\n\n**Set timezone**\n\n```virt-customize -a [image.qcow2] --timezone [America/New_York]```\n\n**Set hostname**\n\n```virt-customize -a [image.qcow2] --hostname [myserver]```"],["SYNOPSIS","**virt-customize** **-a** _image_ [_options_]"],["PARAMETERS","**-a**, **--add** _file_\n> Disk image file to customize.\n\n**--root-password** _method:arg_\n> Set root password.\n\n**--password** _user:method:arg_\n> Set user password.\n\n**--install** _packages_\n> Install packages (comma-separated).\n\n**--uninstall** _packages_\n> Remove packages.\n\n**--update**\n> Update all packages.\n\n**--upload** _local:remote_\n> Upload file to image.\n\n**--copy-in** _local:remotedir_\n> Copy files/dirs into image.\n\n**--run** _script_\n> Run shell script in image.\n\n**--run-command** _cmd_\n> Run single command.\n\n**--ssh-inject** _user:file:keyfile_\n> Inject SSH public key.\n\n**--timezone** _tz_\n> Set timezone.\n\n**--hostname** _name_\n> Set hostname.\n\n**--edit** _file:expr_\n> Edit file with Perl expression.\n\n**--delete** _path_\n> Delete file or directory.\n\n**--commands-from-file** _file_\n> Read commands from file.\n\n**-v**, **--verbose**\n> Enable verbose output."],["DESCRIPTION","**virt-customize** modifies virtual machine disk images by installing packages, editing configuration, uploading files, and running scripts. It is part of the libguestfs tools suite.\n\nThe tool works by mounting the disk image using libguestfs and performing operations in a secure, isolated environment. The VM must be shut down before customization.\n\nCustomization scripts run inside a small appliance chrooted into the guest filesystem. Limited network access is available for package downloads.\n\nThis tool is useful for preparing cloud images, creating golden images, and automating VM provisioning."],["CAVEATS","VM must be shut down. Don't run as root unless necessary. Snapshot disk before modifying production images. Some operations require specific guest OS support. SELinux relabeling may be needed."],["HISTORY","**virt-customize** is part of libguestfs, created by Red Hat for virtual machine image manipulation. The toolset provides safe, scriptable access to VM disk images without requiring the guest to be running."],["INSTALL","```apt: sudo apt install guestfs-tools```\n\n```dnf: sudo dnf install guestfs-tools```\n\n```pacman: sudo pacman -S guestfs-tools```\n\n```zypper: sudo zypper install guestfs-tools```\n\n```nix: nix profile install nixpkgs#guestfs-tools```"],["SEE ALSO","[virt-sysprep](/man/virt-sysprep)(1)"]],"e":["virt-sysprep"],"x":"virt-customize -a [image.qcow2] --root-password password:[newpass]"},"virtualenvwrapper":{"n":"virtualenvwrapper","t":"Convenience wrappers for virtualenv","s":[["TAGLINE","Convenience wrappers for virtualenv"],["TLDR","**Create virtual environment**\n\n```mkvirtualenv [envname]```\n\n**Create with specific Python version**\n\n```mkvirtualenv -p [python3.11] [envname]```\n\n**Create with packages installed**\n\n```mkvirtualenv -i [package] [envname]```\n\n**Create from requirements file**\n\n```mkvirtualenv -r [requirements.txt] [envname]```\n\n**Activate environment**\n\n```workon [envname]```\n\n**List environments**\n\n```workon```\n\n**Deactivate**\n\n```deactivate```\n\n**Remove environment**\n\n```rmvirtualenv [envname]```\n\n**Copy an environment**\n\n```cpvirtualenv [source] [dest]```\n\n**Change directory to project**\n\n```cdproject```\n\n**Run command in all environments**\n\n```allvirtualenv [command]```"],["SYNOPSIS","**mkvirtualenv** _envname_\n\n**workon** [_envname_]\n\n**rmvirtualenv** _envname_"],["COMMANDS","**mkvirtualenv** [_-a project_path_] [_-i package_] [_-r requirements_file_] _envname_\n> Create environment. Options -a, -i, -r are handled by virtualenvwrapper; all other options are passed to virtualenv.\n\n**workon** [_envname_]\n> Activate environment, or list all if no name given.\n\n**deactivate**\n> Exit current environment.\n\n**rmvirtualenv** _envname_\n> Delete environment.\n\n**cpvirtualenv** _source_ _dest_\n> Duplicate an existing environment.\n\n**cdproject**\n> Change to associated project directory.\n\n**setvirtualenvproject** [_virtualenv_path_ _project_path_]\n> Associate project directory with environment.\n\n**allvirtualenv** _command_\n> Run a command across all environments.\n\n**lsvirtualenv** [_-b_] [_-l_]\n> List all environments (-b brief, -l long)."],["DESCRIPTION","**virtualenvwrapper** is a set of shell extensions that enhance the standard virtualenv tool for managing Python virtual environments. It provides convenient wrapper commands for creating, activating, switching between, and deleting environments, all from a centralized location rather than scattered across project directories.\n\nThe core workflow revolves around the `workon` command for listing and switching environments, `mkvirtualenv` for creation, and `rmvirtualenv` for deletion. All environments are stored in a single configurable directory (typically `~/.virtualenvs`), making them easy to find and manage regardless of where project code resides.\n\nThe tool also supports project directory association, allowing automatic directory changes when activating an environment, and provides hook scripts that run at key lifecycle events such as environment creation, activation, and deactivation for custom automation."],["CAVEATS","Requires shell configuration: source the virtualenvwrapper.sh script in your shell profile. All environments are stored in a single directory (default: ~/.virtualenvs), configured by the WORKON_HOME environment variable."],["HISTORY","**virtualenvwrapper** was created by **Doug Hellmann** to make managing Python virtual environments easier."],["INSTALL","```brew: brew install virtualenvwrapper```"],["SEE ALSO","[virtualenv](/man/virtualenv)(1), [venv](/man/venv)(1), [pip](/man/pip)(1)"]],"e":["virtualenv","venv","pip"],"x":"mkvirtualenv [envname]"},"zdump":{"n":"zdump","t":"Display timezone information","s":[["TAGLINE","Display timezone information"],["TLDR","Print **timezone** info\n\n```zdump [timezone]```"],["SYNOPSIS","**zdump** [_OPTIONS_] [_TIMEZONE..._]"],["PARAMETERS","**-v**\n> Verbose output with transition times\n\n**-V**\n> More verbose output\n\n**-c** _YEAR_\n> Limit transitions to specified year range\n\n**--help**\n> Display help"],["DESCRIPTION","**zdump** prints timezone information including current time and UTC offset. It reads timezone data from the system timezone database.\n\nWith -v flag, it shows all timezone transitions (daylight saving time changes)."],["CAVEATS","Timezone names must match system database entries. Output format varies by system version."],["INSTALL","```apt: sudo apt install libc-bin```\n\n```apk: sudo apk add tzdata-utils```"],["SEE ALSO","[zic](/man/zic)(8), [tzselect](/man/tzselect)(8), [date](/man/date)(1)"]],"e":["zic","tzselect","date"],"x":"zdump [timezone]"},"zerotier-one":{"n":"zerotier-one","t":"ZeroTier virtual networking service daemon","s":[["TAGLINE","ZeroTier virtual networking service daemon"],["TLDR","**Start service in foreground**\n\n```sudo zerotier-one```\n\n**Start with custom home directory**\n\n```sudo zerotier-one [/var/lib/zerotier-one]```\n\n**Run in background (daemon mode)**\n\n```sudo zerotier-one -d```"],["SYNOPSIS","**zerotier-one** [**-d**] [_home_directory_]"],["PARAMETERS","**-d**\n> Run as daemon in background\n\n_home_directory_\n> ZeroTier home directory (default: /var/lib/zerotier-one)"],["DESCRIPTION","**zerotier-one** is the ZeroTier network virtualization service daemon. It creates virtual network interfaces and manages encrypted peer-to-peer connections to form software-defined networks.\n\nWhen running, the service:\n- Maintains connections to ZeroTier root servers\n- Establishes peer-to-peer tunnels with other nodes\n- Creates virtual network interfaces (zt*) for joined networks\n- Provides a local API for zerotier-cli\n- Handles NAT traversal and connection optimization\n\nThe home directory contains identity files, network configurations, and the authentication token for the local API.\n\nOn systemd systems, zerotier-one is typically managed as a systemd service (zerotier-one.service) rather than run directly."],["CAVEATS","Requires root privileges to create network interfaces and bind to privileged ports.\n\nThe service binds to UDP port 9993 by default for peer communication.\n\nIdentity files in the home directory (identity.secret, identity.public) should be backed up. Losing them means generating a new identity with a different address.\n\nFirewall rules must allow UDP traffic for peer connections. Blocked UDP can force traffic through relays, reducing performance."],["INSTALL","```pacman: sudo pacman -S zerotier-one```"],["SEE ALSO","[zerotier-cli](/man/zerotier-cli)(1), [zerotier-idtool](/man/zerotier-idtool)(1), [systemctl](/man/systemctl)(1)"]],"e":["zerotier-cli","zerotier-idtool","systemctl"],"x":"sudo zerotier-one"},"zipsplit":{"n":"zipsplit","t":"Split ZIP archive into smaller parts","s":[["TAGLINE","Split ZIP archive into smaller parts"],["TLDR","**Split** archive\n\n```zipsplit [path/to/archive.zip]```\n\nSplit with **size** limit\n\n```zipsplit -n [size] [path/to/archive.zip]```\n\n**Pause** between parts\n\n```zipsplit -p -n [size] [path/to/archive.zip]```\n\nOutput to **directory**\n\n```zipsplit -b [path/to/output_directory] -n [size] [path/to/archive.zip]```"],["SYNOPSIS","**zipsplit** [_OPTIONS_] _ZIPFILE_"],["PARAMETERS","**-n** _SIZE_\n> Maximum bytes per split archive\n\n**-p**\n> Pause between creating each part\n\n**-b** _PATH_\n> Output directory for split archives\n\n**-t**\n> Report how many files would be created without actually creating them\n\n**-r** _ROOM_\n> Leave room for extra bytes on the first disk (default: 0)\n\n**-s**\n> Do a sequential split even if it takes more zip files\n\n**-i**\n> Create an index file (zipsplit.idx) and count its size against the first zip file\n\n**-h**\n> Show a short help message\n\n**-v**\n> Show version information\n\n**-L**\n> Show software license"],["DESCRIPTION","**zipsplit** splits a ZIP archive into smaller ZIP archives. The default size limit is 36000 bytes if not specified with **-n**.\n\nOutput files are named with numeric suffixes based on the original filename."],["CAVEATS","Individual files that exceed the size limit cannot be split across archives. All parts are needed to extract the complete archive contents."],["INSTALL","```apt: sudo apt install zip```\n\n```dnf: sudo dnf install zip```\n\n```pacman: sudo pacman -S zip```\n\n```apk: sudo apk add zip```\n\n```zypper: sudo zypper install zip```\n\n```brew: brew install zip```\n\n```nix: nix profile install nixpkgs#zip```"],["SEE ALSO","[zip](/man/zip)(1), [unzip](/man/unzip)(1), [split](/man/split)(1), [zipcloak](/man/zipcloak)(1), [zipnote](/man/zipnote)(1)"]],"e":["zip","unzip","split","zipcloak","zipnote"],"x":"zipsplit [path/to/archive.zip]"}});
