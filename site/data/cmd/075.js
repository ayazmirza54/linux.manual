window.LCL_BUCKET(75,{"airdecloak-ng":{"n":"airdecloak-ng","t":"Remove WEP cloaking from wireless capture files","s":[["TAGLINE","Remove WEP cloaking from wireless capture files"],["TLDR","**Remove cloaking** from a capture file filtered by BSSID\n\n```airdecloak-ng -i [capture.cap] --bssid [00:11:22:33:44:55]```\n\n**Filter by ESSID** (network name)\n\n```airdecloak-ng -i [capture.cap] --ssid [MyNetwork]```\n\n**Specify output** file for cleaned packets\n\n```airdecloak-ng -i [capture.cap] -o [output.pcap] --bssid [00:11:22:33:44:55]```\n\n**Treat null packets** as potentially cloaked\n\n```airdecloak-ng -i [capture.cap] --bssid [00:11:22:33:44:55] --null-packets```"],["SYNOPSIS","**airdecloak-ng** -i _input_ [--bssid _bssid_ | --ssid _essid_] [_options_]"],["DESCRIPTION","**airdecloak-ng** filters out WEP cloaking techniques from wireless capture files. Some access points implement cloaking by injecting fake or corrupted packets to confuse WEP cracking tools.\n\nThis tool identifies and removes these cloaking packets, producing a cleaner capture file that can be more effectively analyzed or cracked."],["PARAMETERS","**-i** _file_\n> Input capture file (pcap format).\n\n**-o** _file_\n> Output file for valid (cleaned) packets. Default: _<src>_-filtered.pcap.\n\n**-c** _file_\n> Output file for cloaked packets. Default: _<src>_-cloaked.pcap.\n\n**-u** _file_\n> Output file for unknown/ignored packets. Default: invalid_status.pcap.\n\n**--bssid** _mac_\n> Filter by access point MAC address.\n\n**--ssid** _essid_\n> Filter by network ESSID.\n\n**--filters** _list_\n> Apply filtering methods in order (signal, duplicate_sn, duplicate_sn_ap, duplicate_sn_client, consecutive_sn, duplicate_iv, signal_dup_consec_sn).\n\n**--null-packets**\n> Treat null packets as potentially cloaked.\n\n**--disable-base-filter**\n> Skip base filtering logic.\n\n**--drop-frag**\n> Remove fragmented packets."],["CAVEATS","Only useful for captures from access points using WEP cloaking. Modern networks using WPA/WPA2 don't use these cloaking techniques. May remove legitimate packets if cloaking detection is too aggressive."],["HISTORY","**airdecloak-ng** was added to the aircrack-ng suite to counter specific WEP protection mechanisms that some access point vendors implemented in the late **2000s**."],["INSTALL","```apt: sudo apt install aircrack-ng```\n\n```dnf: sudo dnf install aircrack-ng```\n\n```pacman: sudo pacman -S aircrack-ng```\n\n```apk: sudo apk add aircrack-ng```\n\n```zypper: sudo zypper install aircrack-ng```\n\n```brew: brew install aircrack-ng```\n\n```nix: nix profile install nixpkgs#aircrack-ng```"],["SEE ALSO","[aircrack-ng](/man/aircrack-ng)(1), [airodump-ng](/man/airodump-ng)(1)"]],"e":["aircrack-ng","airodump-ng"],"x":"airdecloak-ng -i [capture.cap] --bssid [00:11:22:33:44:55]"},"az-redis":{"n":"az-redis","t":"Manage Azure Cache for Redis instances","s":[["TAGLINE","Manage Azure Cache for Redis instances"],["TLDR","**Create a Redis cache**\n\n```az redis create --name [MyCache] --resource-group [MyResourceGroup] --location [eastus] --sku [Basic] --vm-size [c0]```\n\n**List all Redis caches** in a resource group\n\n```az redis list --resource-group [MyResourceGroup]```\n\n**Show details** of a Redis cache\n\n```az redis show --name [MyCache] --resource-group [MyResourceGroup]```\n\n**List access keys**\n\n```az redis list-keys --name [MyCache] --resource-group [MyResourceGroup]```\n\n**Regenerate access keys**\n\n```az redis regenerate-keys --name [MyCache] --resource-group [MyResourceGroup] --key-type [Primary]```\n\n**Update cache settings**\n\n```az redis update --name [MyCache] --resource-group [MyResourceGroup] --set redisConfiguration.maxmemory-policy=allkeys-lru```\n\n**Force reboot** specified Redis node(s)\n\n```az redis force-reboot --name [MyCache] --resource-group [MyResourceGroup] --reboot-type [AllNodes]```\n\n**Flush all keys** in a Redis cache\n\n```az redis flush --name [MyCache] --resource-group [MyResourceGroup] --yes```\n\n**Delete a Redis cache**\n\n```az redis delete --name [MyCache] --resource-group [MyResourceGroup] --yes```"],["SYNOPSIS","**az** **redis** _subcommand_ [_options_]"],["SUBCOMMANDS","**create**\n> Create a Redis cache.\n\n**delete**\n> Delete a Redis cache.\n\n**list**\n> List Redis caches.\n\n**show**\n> Get details of a Redis cache.\n\n**update**\n> Update a Redis cache.\n\n**list-keys**\n> Retrieve access keys.\n\n**regenerate-keys**\n> Regenerate access keys.\n\n**flush**\n> Delete all keys in a Redis cache.\n\n**force-reboot**\n> Reboot specified Redis node(s).\n\n**import**\n> Import data into a Redis cache.\n\n**export**\n> Export data from a Redis cache.\n\n**firewall-rules**\n> Manage firewall rules.\n\n**patch-schedule**\n> Manage patch schedules.\n\n**server-link**\n> Manage Redis server links (requires Premium SKU).\n\n**identity**\n> Manage identity assigned to Azure Cache for Redis.\n\n**access-policy**\n> Manage access policies for Redis cache.\n\n**access-policy-assignment**\n> Manage access policy assignments (Redis users)."],["PARAMETERS","**--name** **-n**\n> Name of the Redis cache.\n\n**--resource-group** **-g**\n> Name of resource group.\n\n**--sku**\n> SKU tier: Basic, Standard, or Premium.\n\n**--vm-size**\n> Size of the Redis cache to deploy. Basic and Standard sizes start with c (c0-c6). Premium sizes start with p (p1-p5).\n\n**--location** **-l**\n> Location of the cache.\n\n**--minimum-tls-version**\n> Specifies the TLS version required by clients to connect to cache (1.0, 1.1, 1.2).\n\n**--redis-version**\n> Redis version in the form 'major[.minor]' or 'latest'. Default value is 'latest'.\n\n**--yes** **-y**\n> Do not prompt for confirmation (used with delete and flush)."],["DESCRIPTION","**az redis** manages Azure Cache for Redis, a fully managed in-memory data store based on Redis. It provides low-latency, high-throughput caching for applications.\n\nAvailable SKUs are Basic (single node), Standard (replicated), and Premium (clustering, persistence, virtual network support)."],["CAVEATS","Requires Azure CLI to be installed and authenticated. Cache creation can take several minutes. Premium features like clustering and geo-replication require Premium SKU."],["INSTALL","```dnf: sudo dnf install azure-cli```\n\n```pacman: sudo pacman -S azure-cli```\n\n```zypper: sudo zypper install azure-cli```\n\n```brew: brew install azure-cli```\n\n```nix: nix profile install nixpkgs#azure-cli```"],["SEE ALSO","[az](/man/az)(1), [redis-cli](/man/redis-cli)(1)"]],"e":["az","redis-cli"],"x":"az redis create --name [MyCache] --resource-group [MyResourceGroup] --location [eastus] --sku [Basic] --vm-size [c0]"},"bedtools":{"n":"bedtools","t":"Analyze and manipulate genomic interval data","s":[["TAGLINE","Analyze and manipulate genomic interval data"],["TLDR","**Intersect** genomic intervals\n\n```bedtools intersect -a [file1.bed] -b [file2.bed]```\n\n**Merge** overlapping intervals\n\n```bedtools merge -i [sorted.bed]```\n\n**Sort** BED file\n\n```bedtools sort -i [unsorted.bed]```\n\n**Get coverage**\n\n```bedtools coverage -a [regions.bed] -b [reads.bam]```\n\n**Subtract** intervals\n\n```bedtools subtract -a [all.bed] -b [exclude.bed]```\n\n**Find closest** features\n\n```bedtools closest -a [query.bed] -b [reference.bed]```\n\n**Extract FASTA sequences** for intervals\n\n```bedtools getfasta -fi [genome.fa] -bed [regions.bed]```"],["SYNOPSIS","**bedtools** _command_ [_options_]"],["DESCRIPTION","**bedtools** is a suite of utilities for analyzing genomic intervals in BED, BAM, VCF, and GFF formats. It provides fast operations on genome annotations including intersection, merging, counting, and statistical analysis.\n\nThe tool is essential for genomics research and comparative analysis of genomic features."],["COMMANDS","**intersect**\n> Find overlapping intervals\n\n**merge**\n> Combine overlapping intervals\n\n**coverage**\n> Compute coverage statistics\n\n**subtract**\n> Remove overlapping intervals\n\n**closest**\n> Find nearest intervals\n\n**window**\n> Find nearby intervals within window\n\n**sort**\n> Sort BED/GFF/VCF files\n\n**genomecov**\n> Compute genome-wide coverage\n\n**getfasta**\n> Extract sequences from FASTA\n\n**bamtobed**\n> Convert BAM to BED\n\n**slop**\n> Extend intervals by a specified number of bases\n\n**flank**\n> Create flanking intervals for each feature\n\n**complement**\n> Return intervals not covered by features"],["COMMON PARAMETERS","**-a** _file_\n> First input file\n\n**-b** _file_\n> Second input file\n\n**-wa**\n> Write original A entry\n\n**-wb**\n> Write original B entry\n\n**-f** _fraction_\n> Minimum overlap fraction\n\n**-v**\n> Report A entries without B overlap\n\n**-s**\n> Require same strand\n\n**-S**\n> Require opposite strand\n\n**-sorted**\n> Use sorted algorithm for large files (lower memory)\n\n**-i** _file_\n> Input file (used by merge, sort, etc.)"],["WORKFLOW","```bash"],["Find genes overlapping SNPs","bedtools intersect -a genes.bed -b snps.bed"],["Merge nearby features","bedtools merge -i features.bed -d 100"],["Count reads in regions","bedtools coverage -a regions.bed -b reads.bam"],["Extract promoter regions","bedtools flank -i genes.bed -g genome.txt -l 2000 -r 0\n```"],["CAVEATS","Some operations require sorted input. Large files consume significant memory. Coordinate systems (0-based vs 1-based) can cause off-by-one errors. Performance depends on file size and operation."],["HISTORY","**bedtools** was created by Aaron Quinlan and Ira Hall, first released in **2010** as a comprehensive toolkit for genomic interval analysis."],["INSTALL","```apt: sudo apt install bedtools```\n\n```aur: yay -S bedtools```\n\n```brew: brew install bedtools```\n\n```nix: nix profile install nixpkgs#bedtools```"],["SEE ALSO","[samtools](/man/samtools)(1), [vcftools](/man/vcftools)(1), [bcftools](/man/bcftools)(1)"]],"e":["samtools","vcftools","bcftools"],"x":"bedtools intersect -a [file1.bed] -b [file2.bed]"},"bgpctl":{"n":"bgpctl","t":"Control and query the OpenBGPD routing daemon","s":[["TAGLINE","Control and query the OpenBGPD routing daemon"],["TLDR","**Show BGP summary** (neighbor status)\n\n```bgpctl show summary```\n\n**Show all BGP neighbors**\n\n```bgpctl show neighbor```\n\n**Show RIB** (Routing Information Base)\n\n```bgpctl show rib```\n\n**Show routes from a specific neighbor**\n\n```bgpctl show rib neighbor [192.168.1.1]```\n\n**Show routes for a specific prefix**\n\n```bgpctl show rib [10.0.0.0/8]```\n\n**Soft reset a neighbor** session\n\n```bgpctl neighbor [192.168.1.1] refresh```\n\n**Clear a neighbor** session\n\n```bgpctl neighbor [192.168.1.1] clear```\n\n**Show BGP network statements**\n\n```bgpctl show network```"],["SYNOPSIS","**bgpctl** [**-n**] [**-s** _socket_] _command_ [_arguments_]"],["DESCRIPTION","**bgpctl** is the control program for OpenBGPD, an implementation of the Border Gateway Protocol version 4. It communicates with the running bgpd daemon to display routing information and control BGP sessions.\n\nThe command provides visibility into the BGP Routing Information Base (RIB), neighbor session states, and allows administrative actions like resetting sessions or injecting routes. It connects to bgpd via a Unix socket.\n\nbgpctl is part of the OpenBGPD project, which originated in OpenBSD and has been ported to other operating systems."],["PARAMETERS","**show summary**\n> Display neighbor status overview.\n\n**show neighbor** [_peer_]\n> Show detailed neighbor information.\n\n**show rib** [_options_]\n> Display routing information base.\n\n**show network**\n> Show configured network statements.\n\n**show fib** [_options_]\n> Show forwarding information base.\n\n**neighbor** _peer_ **up**\n> Bring up a neighbor session.\n\n**neighbor** _peer_ **down**\n> Bring down a neighbor session.\n\n**neighbor** _peer_ **clear**\n> Clear and reset a session.\n\n**neighbor** _peer_ **refresh**\n> Request route refresh.\n\n**network add** _prefix_\n> Add a network to advertise.\n\n**network delete** _prefix_\n> Remove a network advertisement.\n\n**-n**\n> Show IP addresses instead of names.\n\n**-s** _socket_\n> Connect to alternate control socket."],["CAVEATS","Requires appropriate permissions to access bgpd control socket. Some commands may disrupt BGP sessions and cause routing changes. The clear command tears down the TCP session while refresh performs a soft reset. Route changes may take time to propagate through the network."],["HISTORY","**bgpctl** is part of **OpenBGPD**, which was developed by Henning Brauer and Claudio Jeker for **OpenBSD** starting in **2003**. It was designed as a clean, secure implementation of BGP after concerns about other implementations. The portable version for Linux and other systems has been available since **2016**. OpenBGPD is known for its simplicity and security focus."],["INSTALL","```apt: sudo apt install openbgpd```\n\n```dnf: sudo dnf install openbgpd```\n\n```apk: sudo apk add openbgpd```\n\n```nix: nix profile install nixpkgs#openbgpd```"],["SEE ALSO","[bgpd](/man/bgpd)(8), [vtysh](/man/vtysh)(1)"]],"e":["bgpd","vtysh"],"x":"bgpctl show summary"},"btm":{"n":"btm","t":"Cross-platform terminal system monitor","s":[["TAGLINE","Cross-platform terminal system monitor"],["TLDR","**Start** resource monitor\n\n```btm```\n\n**Basic** mode (less features)\n\n```btm -b```\n\n**Specific** refresh rate\n\n```btm -r [2000]```"],["SYNOPSIS","**btm** [_options_]"],["DESCRIPTION","**btm** (bottom) is a cross-platform graphical process/system monitor for the terminal. It provides information about CPU, memory, disk, network, and processes with a customizable interface.\n\nThe tool offers an alternative to htop and top with modern features and aesthetics."],["PARAMETERS","**-b**, **--basic**\n> Basic mode (disable graphs)\n\n**-r**, **--rate** _ms_\n> Refresh rate in milliseconds (default: 1000)\n\n**-a**, **--hide_avg_cpu**\n> Hide average CPU usage\n\n**-m**, **--hide_time**\n> Hide time graph\n\n**-t**, **--default_time_value** _ms_\n> Default time range\n\n**-c**, **--config** _path_\n> Configuration file"],["FEATURES","- CPU usage per core\n- Memory and swap monitoring\n- Disk I/O and usage\n- Network I/O\n- Process list with sorting\n- Process tree view\n- Battery monitoring\n- Temperature sensors\n- Customizable widgets\n- Mouse support"],["KEYBINDINGS","- **q/Ctrl+C** - Quit\n- **/** - Filter\n- **dd** - Kill process\n- **e** - Expand widget\n- **+/-** - Zoom time graph\n- **Tab** - Switch widgets\n- **?** - Help"],["CONFIGURATION","**~/.config/bottom/bottom.toml**\n> Main configuration file for flags, colors, layout, and widget settings."],["CAVEATS","Requires modern terminal with Unicode support. Some features platform-specific. Higher resource usage than minimal tools. Configuration can be complex."],["HISTORY","**bottom** was created by Clement Tsang around **2019** as a modern, customizable system monitor written in Rust."],["INSTALL","```apt: sudo apt install btm```\n\n```pacman: sudo pacman -S bottom```\n\n```apk: sudo apk add bottom```\n\n```zypper: sudo zypper install bottom```\n\n```brew: brew install bottom```\n\n```nix: nix profile install nixpkgs#bottom```"],["SEE ALSO","[htop](/man/htop)(1), [btop](/man/btop)(1), [top](/man/top)(1)"],["RESOURCES","```[Source code](https://github.com/ClementTsang/bottom)```\n\n```[Documentation](https://clementtsang.github.io/bottom/)```"]],"e":["htop","btop","top"],"x":"btm"},"btrfs-inspect-internal":{"n":"btrfs-inspect-internal","t":"Debug and inspect btrfs filesystem internals","s":[["TAGLINE","Debug and inspect btrfs filesystem internals"],["TLDR","Print **superblock** information\n\n```sudo btrfs inspect-internal dump-super [path/to/partition]```\n\nPrint **all superblock copies**\n\n```sudo btrfs inspect-internal dump-super -a [path/to/partition]```\n\nPrint filesystem **metadata** information\n\n```sudo btrfs inspect-internal dump-tree [path/to/partition]```\n\nPrint list of files in **inode** number\n\n```sudo btrfs inspect-internal inode-resolve [inode_number] [path/to/btrfs_mount]```\n\nPrint list of files at a **logical address**\n\n```sudo btrfs inspect-internal logical-resolve [logical_address] [path/to/btrfs_mount]```\n\nPrint **tree stats** for a filesystem\n\n```sudo btrfs inspect-internal tree-stats [path/to/partition]```\n\nCalculate **minimum device size** for shrinking\n\n```sudo btrfs inspect-internal min-dev-size [path/to/btrfs_mount]```"],["SYNOPSIS","**btrfs inspect-internal** _subcommand_ [_options_] _device_|_path_"],["DESCRIPTION","**btrfs inspect-internal** provides low-level access to btrfs filesystem internal structures. It is primarily used for debugging, forensics, and advanced troubleshooting.\n\nThe command can dump superblock data, metadata trees, and resolve inodes to file paths or logical addresses to physical locations."],["SUBCOMMANDS","**dump-super**\n> Print superblock information\n\n**dump-tree**\n> Print metadata tree structure\n\n**inode-resolve**\n> Find file paths from inode number\n\n**logical-resolve**\n> Find files at logical address\n\n**tree-stats**\n> Print tree statistics\n\n**min-dev-size**\n> Calculate minimum device size for shrinking"],["PARAMETERS","**-a, --all**\n> Dump all superblock copies\n\n**-f**\n> Force output even with warnings"],["CAVEATS","Output is technical and intended for developers or advanced users. Some commands require unmounted filesystem while others need mounted. Incorrect use won't damage data but output interpretation requires btrfs internals knowledge."],["SEE ALSO","[btrfs](/man/btrfs)(8), [btrfs-check](/man/btrfs-check)(8), [btrfs-rescue](/man/btrfs-rescue)(8), [btrfs-restore](/man/btrfs-restore)(8)"]],"e":["btrfs","btrfs-check","btrfs-rescue","btrfs-restore"],"x":"sudo btrfs inspect-internal dump-super [path/to/partition]"},"byzanz-record":{"n":"byzanz-record","t":"Record screen sessions as animated GIFs or videos","s":[["TAGLINE","Record screen sessions as animated GIFs or videos"],["TLDR","**Record** the screen to a file (default 10 seconds)\n\n```byzanz-record [path/to/file.gif]```\n\nShow **verbose** information during recording\n\n```byzanz-record -v [path/to/file.gif]```\n\nRecord for a **specific duration**\n\n```byzanz-record -d 60 [path/to/file.gif]```\n\n**Delay** before starting recording\n\n```byzanz-record --delay 10 [path/to/file.gif]```"],["SYNOPSIS","**byzanz-record** [_options_] _file_"],["DESCRIPTION","**byzanz-record** captures screen recordings and saves them in various formats including GIF, WebM, OGV, and FLV. It is designed for creating short screencasts and animated GIFs for documentation or demonstrations.\n\nBy default, recording lasts 10 seconds. The tool supports recording specific windows or screen regions."],["PARAMETERS","**-d, --duration** _seconds_\n> Recording duration in seconds\n\n**--delay** _seconds_\n> Delay before starting recording\n\n**-v, --verbose**\n> Show information during and after recording\n\n**-x, -y** _pixels_\n> X and Y position of recording area\n\n**-w, --width** _pixels_\n> Width of recording area\n\n**-h, --height** _pixels_\n> Height of recording area\n\n**-c, --cursor**\n> Record cursor movements\n\n**-a, --audio**\n> Record audio along with video (when supported by output format).\n\n**--exec** _command_\n> Run the specified command and record until it exits (overrides duration).\n\n**--display** _DISPLAY_\n> Specify the X display to record."],["OUTPUT FORMATS","- **.gif** - Animated GIF\n- **.webm** - WebM video\n- **.ogv/.ogg** - Ogg Theora video\n- **.flv** - Flash video\n- **.byzanz** - Native format"],["CAVEATS","GIF output can produce large files for long recordings. Works only with X11, not Wayland. Recording high-resolution screens may be resource-intensive."],["INSTALL","```apt: sudo apt install byzanz```\n\n```nix: nix profile install nixpkgs#byzanz```"],["SEE ALSO","[peek](/man/peek)(1), [ffmpeg](/man/ffmpeg)(1), [recordmydesktop](/man/recordmydesktop)(1)"]],"e":["peek","ffmpeg","recordmydesktop"],"x":"byzanz-record [path/to/file.gif]"},"caprover":{"n":"caprover","t":"Deploy and manage applications on CapRover","s":[["TAGLINE","Deploy and manage applications on CapRover"],["TLDR","**Login to CapRover machine**\n\n```caprover login```\n\n**Deploy application**\n\n```caprover deploy```\n\n**Deploy with specific options**\n\n```caprover deploy -n [machine-name] -a [app-name] -b [branch]```\n\n**Deploy Docker image**\n\n```caprover deploy -i [image-name]```\n\n**Deploy from tar file**\n\n```caprover deploy -t [app.tar]```\n\n**Set up a new CapRover server**\n\n```caprover serversetup```\n\n**List logged-in machines**\n\n```caprover list```\n\n**Logout from machine**\n\n```caprover logout```"],["SYNOPSIS","**caprover** _command_ [_options_]"],["DESCRIPTION","**caprover** is the CLI for CapRover, a modern automated app deployment and web server manager. Use it to perform initial server setup and deploy applications via captain-definition files, Dockerfiles, tar files, or Docker images."],["COMMANDS","**serversetup**, **setup**\n> Perform initial server setup (domain, SSL, password)\n\n**login**\n> Authenticate with a CapRover machine\n\n**logout**\n> Remove authentication for a machine\n\n**deploy**\n> Deploy application to CapRover\n\n**list**, **ls**\n> List all logged-in machines\n\n**api**\n> Call generic API on CapRover machine"],["PARAMETERS","**-n**, **--caproverName** _name_\n> Target CapRover machine name\n\n**-a**, **--caproverApp** _app_\n> Application name to deploy\n\n**-b**, **--branch** _branch_\n> Git branch to deploy\n\n**-t**, **--tarFile** _file_\n> Path to tar file for deployment\n\n**-i**, **--imageName** _image_\n> Docker image to deploy\n\n**-d**, **--default**\n> Use previously entered values\n\n**-c**, **--configFile** _file_\n> Deployment configuration file path\n\n**-u**, **--caproverUrl** _url_\n> CapRover machine URL\n\n**-p**, **--caproverPassword** _password_\n> CapRover password\n\n**--appToken** _token_\n> App-level authentication token\n\n**-V**, **--version**\n> Output version number\n\n**-h**, **--help**\n> Display help information"],["CI/CD INTEGRATION","For automation, provide options directly:\n\n```caprover deploy -n machine -a app -b main```\n\nRequires prior login to the machine."],["CAVEATS","Requires Node.js and npm. Uncommitted and git-ignored files are not included when deploying branches. Install via npm install -g caprover."],["INSTALL","```aur: yay -S caprover```"],["SEE ALSO","[docker](/man/docker)(1), [kubectl](/man/kubectl)(1), [heroku](/man/heroku)(1)"]],"e":["docker","kubectl","heroku"],"x":"caprover login"},"cargo-report":{"n":"cargo-report","t":"Display build reports and diagnostics","s":[["TAGLINE","Display build reports and diagnostics"],["TLDR","**Show future incompatibility report**\n\n```cargo report future-incompatibilities --id [1]```\n\n**Show report after build**\n\n```cargo build --future-incompat-report```"],["SYNOPSIS","**cargo report** _type_ [_options_]"],["DESCRIPTION","**cargo report** generates diagnostic reports about the project. Currently its primary subcommand is `future-incompatibilities`, which displays warnings about code patterns in dependencies that will be rejected in future Rust versions.\n\nWhen Cargo detects future-incompatible warnings during a build, it stores a report and displays a notification with instructions to view the full details. These warnings indicate patterns that may exhibit undefined behavior, are scheduled for removal, or lack well-defined semantics. Reviewing these reports helps maintainers proactively update their dependencies before breaking changes land."],["SUBCOMMANDS","**future-incompatibilities**\n> Display future incompatibility report"],["PARAMETERS","**--id** _id_\n> Report ID from previous build\n\n**--package** _spec_\n> Package to report on\n\n**-v**, **--verbose**\n> Verbose output\n\n**-q**, **--quiet**\n> Suppress output"],["FUTURE INCOMPATIBILITIES","Cargo checks dependencies for patterns that:\n- May exhibit undefined behavior\n- Are scheduled for removal from Rust\n- Do not have well-defined semantics\n\nWhen detected, a warning appears after build with instructions to view the full report."],["CONFIGURATION","**.cargo/config.toml**\n> Controls report frequency via `[future-incompat-report]` section (e.g., `frequency = \"always\"`)."],["RESOLVING ISSUES","1. Update dependencies to versions with fixes\n2. Work with dependency maintainers\n3. Consider alternatives if unmaintained"],["CAVEATS","Report is not a lint and doesn't interact with -Dwarnings. These warnings don't fail builds but indicate future breakage."],["INSTALL","```apt: sudo apt install cargo```\n\n```dnf: sudo dnf install cargo```\n\n```apk: sudo apk add cargo```\n\n```zypper: sudo zypper install cargo```\n\n```nix: nix profile install nixpkgs#cargo```"],["SEE ALSO","[cargo](/man/cargo)(1), [cargo-build](/man/cargo-build)(1)"],["RESOURCES","```[Source code](https://github.com/rust-lang/cargo)```\n\n```[Documentation](https://doc.rust-lang.org/cargo/commands/cargo-report.html)```"]],"e":["cargo","cargo-build"],"x":"cargo report future-incompatibilities --id [1]"},"ccat":{"n":"ccat","t":"colorizing cat with syntax highlighting","s":[["TAGLINE","colorizing cat with syntax highlighting"],["TLDR","**Display file with syntax highlighting**\n\n```ccat [file.py]```\n\n**Display multiple files**\n\n```ccat [file1.go] [file2.js]```\n\n**Output as HTML**\n\n```ccat --html [file.rb]```\n\n**Dark background colors**\n\n```ccat --bg=dark [file.c]```\n\n**Show color palette**\n\n```ccat --palette```"],["SYNOPSIS","**ccat** [_options_] _file_..."],["DESCRIPTION","**ccat** is a drop-in replacement for cat that adds syntax highlighting to file contents displayed in the terminal. It automatically detects the programming language from the file extension and applies color coding to keywords, strings, comments, and other syntax elements.\n\nThe tool supports a wide range of languages including JavaScript, Python, Go, Ruby, C, C++, Java, and JSON. It ships as a single binary with no external dependencies, providing native execution speed. Output can be rendered for light or dark terminal backgrounds, and HTML output is available for embedding highlighted code in web pages."],["PARAMETERS","**--html**\n> Output as HTML\n\n**--bg**=_color_\n> Background color: light, dark\n\n**-G** _key=color_\n> Set custom color codes\n\n**--palette**\n> Display color palette\n\n**-C**, **--color**=_mode_\n> Color mode: always, never, auto"],["SUPPORTED LANGUAGES","- JavaScript / TypeScript\n- Python\n- Go\n- Ruby\n- C / C++\n- Java\n- JSON\n- And more via Pygments"],["ALIASING","Replace cat with ccat:\n\n```bash\nalias cat='ccat'\n```\n\nAdd to ~/.bashrc for permanent use."],["CAVEATS","Written in Go (owenthereal version) or Python (welbornprod version). Requires terminal with color support."],["INSTALL","```apt: sudo apt install ccrypt```\n\n```brew: brew install ccrypt```\n\n```nix: nix profile install nixpkgs#ccrypt```"],["SEE ALSO","[cat](/man/cat)(1), [bat](/man/bat)(1), [highlight](/man/highlight)(1)"],["RESOURCES","```[Source code](https://github.com/owenthereal/ccat)```"]],"e":["cat","bat","highlight"],"x":"ccat [file.py]"},"csvtool":{"n":"csvtool","t":"Fast CSV manipulation utility","s":[["TAGLINE","Fast CSV manipulation utility"],["TLDR","**Extract specific columns** from a CSV\n\n```csvtool col [1,3,5] [data.csv]```\n\n**Extract rows by range**\n\n```csvtool head [10] [data.csv]```\n\n**Transpose CSV** (rows become columns)\n\n```csvtool transpose [data.csv]```\n\n**Join columns** from multiple files\n\n```csvtool paste [file1.csv] [file2.csv]```\n\n**Replace values** in a column\n\n```csvtool replace [column] [old] [new] [data.csv]```\n\n**Custom delimiter** input\n\n```csvtool -t \"[;]\" col [1,2] [data.csv]```"],["SYNOPSIS","**csvtool** [_options_] _command_ [_arguments_] _file_"],["PARAMETERS","**col** _COLUMNS_\n> Extract specified columns (1-indexed, comma-separated).\n\n**head** _N_\n> Output first N rows.\n\n**drop** _N_\n> Drop first N rows.\n\n**transpose**\n> Transpose rows and columns.\n\n**paste**\n> Paste files side by side.\n\n**replace** _COL_ _OLD_ _NEW_\n> Replace values in a column.\n\n**join** _COL_\n> Join files on a column.\n\n**call** _COMMAND_\n> Run command for each row.\n\n**-t** _CHAR_\n> Input delimiter (default: comma).\n\n**-u** _CHAR_\n> Output delimiter.\n\n**-o** _FILE_\n> Output file."],["DESCRIPTION","**csvtool** is a fast CSV manipulation utility written in OCaml. It provides various operations for transforming, filtering, and analyzing CSV data from the command line.\n\nThe tool is designed for speed and handles large files efficiently. It supports common operations like column extraction, row filtering, transposition, and file joining. The **call** command enables running external commands for each row.\n\ncsvtool uses a streaming approach where possible, allowing it to process files larger than available memory for many operations. It properly handles CSV quoting and escaping."],["CAVEATS","Column numbers are 1-indexed, not 0-indexed. Some operations require loading the entire file into memory. Quoting rules may differ from other CSV tools. Error messages can be cryptic."],["HISTORY","csvtool ships with the **ocaml-csv** library, a CSV parser for OCaml originally written by Richard Jones and now maintained by Christophe Troestler. The command-line tool exposes the library's parsing for quick shell-based CSV transformations."],["INSTALL","```apt: sudo apt install csvtool```\n\n```nix: nix profile install nixpkgs#csvtool```"],["SEE ALSO","[csvkit](/man/csvkit)(1), [cut](/man/cut)(1), [awk](/man/awk)(1), [miller](/man/miller)(1)"],["RESOURCES","```[Source code](https://github.com/Chris00/ocaml-csv)```"]],"e":["csvkit","cut","awk","miller"],"x":"csvtool col [1,3,5] [data.csv]"},"detox":{"n":"detox","t":"clean up problematic filenames","s":[["TAGLINE","clean up problematic filenames"],["TLDR","**Clean filenames** in current directory\n\n```detox *```\n\n**Clean filenames recursively**\n\n```detox -r [directory]```\n\n**Preview changes** without renaming\n\n```detox -n [files]```\n\n**Show verbose output**\n\n```detox -v [files]```\n\n**Use a specific sequence** of filters\n\n```detox -s [iso8859_1] [files]```\n\n**List available sequences**\n\n```detox -L```"],["SYNOPSIS","**detox** [_options_] _file-or-directory_..."],["PARAMETERS","_FILE_\n> Files or directories to process.\n\n**-r**, **--recurse**\n> Process directories recursively.\n\n**-n**, **--dry-run**\n> Show what would be renamed without changes.\n\n**-v**, **--verbose**\n> Verbose output showing renames.\n\n**-s** _SEQUENCE_\n> Cleaning sequence to use (default runs the safe and wipeup filters).\n\n**-L**\n> List the currently available sequences.\n\n**--special**\n> Work on links and special files too.\n\n**-f** _FILE_\n> Use specified configuration file instead of the defaults.\n\n**--help**\n> Display help information."],["DESCRIPTION","**detox** cleans up filenames by replacing problematic characters with safe alternatives. It handles spaces, special characters, Unicode characters, and other elements that can cause issues in shell scripts or cross-platform file sharing.\n\nThe tool applies configurable sequences of transformations including replacing spaces with underscores, removing or transliterating non-ASCII characters, and fixing case issues. Multiple built-in sequences handle different cleaning scenarios.\n\ndetox is valuable for batch-processing files from Windows systems, music libraries with complex names, or any source with inconsistent naming conventions."],["CONFIGURATION","**/etc/detoxrc** or **~/.detoxrc**\n> Configuration file defining cleaning sequences and filename filters."],["CAVEATS","May create duplicate filenames requiring manual resolution. Irreversible without backups. Unicode transliteration may lose information. Some sequences are aggressive and may over-simplify names."],["HISTORY","detox was written by **Doug Harple** to address the common problem of managing files with problematic names in Unix environments. It provides a systematic approach to filename sanitization."],["INSTALL","```apt: sudo apt install detox```\n\n```dnf: sudo dnf install detox```\n\n```pacman: sudo pacman -S detox```\n\n```brew: brew install detox```\n\n```nix: nix profile install nixpkgs#detox```"],["SEE ALSO","[rename](/man/rename)(1), [mv](/man/mv)(1), [convmv](/man/convmv)(1)"],["RESOURCES","```[Source code](https://github.com/dharple/detox)```"]],"e":["rename","mv","convmv"],"x":"detox *"},"dmesg":{"n":"dmesg","t":"kernel ring buffer message viewer","s":[["TAGLINE","kernel ring buffer message viewer"],["TLDR","Show **kernel messages**\n\n```sudo dmesg```\n\nShow kernel **error messages**\n\n```sudo dmesg -l err```\n\n**Follow** kernel messages in real-time\n\n```sudo dmesg -w```\n\nShow messages with **human-readable timestamps**\n\n```sudo dmesg -T```\n\nShow messages in **human-readable** form\n\n```sudo dmesg -H```\n\n**Colorize** output\n\n```sudo dmesg -L```\n\nFilter messages by **facility**\n\n```sudo dmesg -f kern```"],["SYNOPSIS","**dmesg** [_-l level_] [_-f facility_] [_-T_] [_-H_] [_-w_] [_-L_]"],["DESCRIPTION","**dmesg** prints and controls the kernel ring buffer, which contains messages from the kernel including hardware detection, driver loading, and system events. It is essential for debugging hardware and driver issues."],["PARAMETERS","**-l, --level level**\n> Restrict output to specified log levels (emerg, alert, crit, err, warn, notice, info, debug)\n\n**-f, --facility facility**\n> Restrict output to specified facilities (kern, user, mail, daemon, etc.)\n\n**-T, --ctime**\n> Print human-readable timestamps\n\n**-H, --human**\n> Enable human-readable output with colors and timestamps\n\n**-w, --follow**\n> Wait for new messages (like tail -f)\n\n**-L, --color**\n> Colorize output\n\n**-c, --read-clear**\n> Clear the ring buffer after printing\n\n**-C, --clear**\n> Clear the ring buffer without printing\n\n**-n, --console-level level**\n> Set level of messages printed to console\n\n**-k, --kernel**\n> Print kernel messages only\n\n**-u, --userspace**\n> Print userspace messages only\n\n**--time-format** _format_\n> Timestamp format: ctime, reltime, delta, iso, raw\n\n**--since** _time_\n> Display records since the specified time\n\n**--until** _time_\n> Display records until the specified time\n\n**-J, --json**\n> Output in JSON format\n\n**-t, --notime**\n> Do not print kernel timestamps\n\n**-r, --raw**\n> Print raw message buffer with log-level prefixes\n\n**-x, --decode**\n> Decode facility and level to human-readable prefixes"],["CAVEATS","Requires root privileges on most systems. The **-w** (follow) option requires kernel 3.5.0+ (for /dev/kmsg). The **-T** and **-H** options are util-linux userspace features and do not require a specific kernel version."],["HISTORY","Part of **util-linux** package. The kernel ring buffer has been part of Linux since early versions, providing essential diagnostic information."],["INSTALL","```apt: sudo apt install util-linux```\n\n```dnf: sudo dnf install util-linux```\n\n```pacman: sudo pacman -S util-linux```\n\n```apk: sudo apk add dmesg```\n\n```zypper: sudo zypper install util-linux```\n\n```brew: brew install util-linux```\n\n```nix: nix profile install nixpkgs#util-linux```"],["SEE ALSO","[journalctl](/man/journalctl)(1), [tail](/man/tail)(1)"]],"e":["journalctl","tail"],"x":"sudo dmesg"},"do-release-upgrade":{"n":"do-release-upgrade","t":"Upgrade the operating system to the latest release","s":[["TAGLINE","Upgrade the operating system to the latest release"],["TLDR","**Upgrade to the latest release**\n\n```sudo do-release-upgrade```\n\n**Upgrade to the development release**\n\n```sudo do-release-upgrade -d```\n\n**Upgrade using the proposed upgrader**\n\n```sudo do-release-upgrade -p```\n\n**Check if an upgrade is available without performing it**\n\n```sudo do-release-upgrade -c```\n\n**Run upgrade in server mode with text frontend**\n\n```sudo do-release-upgrade -m server -f DistUpgradeViewText```\n\n**Test upgrade in a sandbox without modifying the system**\n\n```sudo do-release-upgrade -s```"],["SYNOPSIS","**do-release-upgrade** [_options_]"],["PARAMETERS","**-h**, **--help**\n> Show help message and exit.\n\n**-d**, **--devel-release**\n> If using the latest supported release, upgrade to the development release.\n\n**-p**, **--proposed**\n> Try upgrading using the upgrader from ubuntu-proposed.\n\n**-c**, **--check-dist-upgrade-only**\n> Check if a new release is available and exit without upgrading.\n\n**-m** _MODE_, **--mode** _MODE_\n> Run in a special upgrade mode. Supported modes are \"desktop\" and \"server\".\n\n**-f** _FRONTEND_, **--frontend** _FRONTEND_\n> Run the specified frontend (DistUpgradeViewGtk3, DistUpgradeViewText, DistUpgradeViewNonInteractive).\n\n**-s**, **--sandbox**\n> Test upgrade with a sandbox aufs overlay."],["DESCRIPTION","**do-release-upgrade** upgrades Ubuntu to a new release version. It handles the complex process of updating sources, upgrading packages, and resolving conflicts. It is the recommended command for upgrading servers and systems without a graphical environment, or when upgrading over a remote connection.\n\nBy default, upgrades are only offered to the next LTS release (for LTS users) or the next release (for non-LTS users). The **-d** flag is needed to upgrade to a development release."],["CAVEATS","Ubuntu-specific. Backup important data before upgrading. May take significant time. Third-party repositories are disabled during upgrade and may cause issues. Run from a terminal multiplexer (screen or tmux) when upgrading over SSH to avoid disconnection problems."],["HISTORY","**do-release-upgrade** is part of the **ubuntu-release-upgrader** package, the standard tool for performing Ubuntu release upgrades since at least Ubuntu 8.04 (Hardy Heron). It replaced manual sources.list editing as the recommended upgrade method."],["SEE ALSO","[apt](/man/apt)(8), [apt-get](/man/apt-get)(8), [update-manager](/man/update-manager)(8), [screen](/man/screen)(1)"]],"e":["apt","apt-get","update-manager","screen"],"x":"sudo do-release-upgrade"},"docker-start":{"n":"docker-start","t":"start stopped containers","s":[["TAGLINE","start stopped containers"],["TLDR","**Start a stopped container**\n\n```docker start [container]```\n\n**Start multiple containers**\n\n```docker start [container1] [container2]```\n\n**Start with attached output**\n\n```docker start -a [container]```\n\n**Start interactively**\n\n```docker start -ai [container]```"],["SYNOPSIS","**docker** **start** [_options_] _container_ [_container..._]"],["PARAMETERS","**-a**, **--attach**\n> Attach STDOUT/STDERR and forward signals.\n\n**-i**, **--interactive**\n> Attach container's STDIN.\n\n**--detach-keys** _string_\n> Override the key sequence for detaching a container.\n\n**--checkpoint** _string_\n> Restore from this checkpoint.\n\n**--checkpoint-dir** _string_\n> Use a custom checkpoint storage directory."],["DESCRIPTION","**docker start** starts one or more stopped containers. Containers retain their configuration from when they were created or last run."],["INSTALL","```apt: sudo apt install docker-cli```\n\n```dnf: sudo dnf install docker-cli```\n\n```pacman: sudo pacman -S docker```\n\n```apk: sudo apk add docker-cli```\n\n```zypper: sudo zypper install docker```\n\n```brew: brew install docker```\n\n```nix: nix profile install nixpkgs#docker```"],["SEE ALSO","[docker-container-start](/man/docker-container-start)(1), [docker-run](/man/docker-run)(1)"]],"e":["docker-container-start","docker-run"],"x":"docker start [container]"},"dotenv-diff":{"n":"dotenv-diff","t":"Scan and validate environment variable usage across a codebase","s":[["TAGLINE","Scan and validate environment variable usage across a codebase"],["TLDR","**Scan the codebase** for missing, unused, duplicated and misused environment variables\n\n```dotenv-diff```\n\n**Compare** the runtime .env file against .env.example\n\n```dotenv-diff --compare```\n\n**Compare specific files**, including their values\n\n```dotenv-diff --compare --env [.env.production] --example [.env.example] --check-values```\n\n**Automatically fix** issues such as missing keys and duplicates\n\n```dotenv-diff --fix```\n\n**Explain a single variable**: where it is defined, where it is used and its status\n\n```dotenv-diff --explain [DATABASE_URL]```\n\n**Generate a default configuration file**\n\n```dotenv-diff --init```\n\n**Record current warnings as a baseline** when adopting the tool in an existing project\n\n```dotenv-diff --baseline```\n\n**Run in CI**, treating warnings as errors\n\n```dotenv-diff --ci --strict```"],["SYNOPSIS","**dotenv-diff** [**--compare**] [**--env** _FILE_] [**--example** _FILE_] [_options_]"],["DESCRIPTION","**dotenv-diff** is a Node.js command-line tool that scans a JavaScript/TypeScript codebase for environment variable references and validates them before they cause runtime errors. It detects variables that are used in code but missing from the .env file, variables that are defined but never used, duplicate keys, empty values, and accidental secrets in tracked files.\n\nIn comparison mode (**--compare**) it diffs the runtime **.env** file against a reference file such as **.env.example**, reporting missing keys, extra keys, empty values, duplicates, and whether the .env file is listed in .gitignore. The **--matrix** mode shows key presence across multiple environment files side by side.\n\nThe tool has first-class support for SvelteKit, Next.js and Nuxt, warning about framework-specific misuse such as exposing server-only variables to the client. It exits with status **0** when no errors are found and **1** on errors (or warnings in strict mode), which makes it suitable for git hooks and CI/CD pipelines.\n\nIt is distributed via npm and can be run without installation using ```npx dotenv-diff```."],["OPTIONS","**--compare**\n\n> Compare the runtime .env file against a reference file instead of scanning the codebase.\n\n**--env** _FILE_\n\n> Primary environment file to check (default: .env).\n\n**--example** _FILE_\n\n> Reference file for comparisons (default: .env.example).\n\n**--check-values**\n\n> Compare values as well as keys between the two files.\n\n**--only** _LIST_\n\n> Restrict comparison to specific issues: missing, extra, empty, duplicate, gitignore.\n\n**--matrix** [_FILES_...]\n\n> Side-by-side comparison of multiple env files showing where each key is present.\n\n**--fix**\n\n> Automatically correct issues such as missing keys and duplicates.\n\n**--ignore** _KEYS_\n\n> Exclude specific keys from checks (comma-separated).\n\n**--ignore-regex** _PATTERNS_\n\n> Exclude keys matching regular expression patterns.\n\n**--include-files** _PATTERNS_\n\n> Extend the default scan patterns with additional files, useful in monorepos.\n\n**--exclude-files** _PATTERNS_\n\n> Skip specified files beyond the defaults.\n\n**--explain** _KEY_\n\n> Show where a variable is defined, where it is used and its overall status.\n\n**--list-all**\n\n> List all unique environment variables found in the scan.\n\n**--strict**\n\n> Treat warnings as errors and exit with a failure code.\n\n**--no-secrets**\n\n> Skip the built-in secret detection.\n\n**--baseline**\n\n> Record current warnings into a baseline file so existing issues are suppressed in later runs.\n\n**--init**\n\n> Generate a default configuration file.\n\n**--json**\n\n> Output results as JSON.\n\n**--ci**\n\n> Non-interactive, read-only mode for CI/CD pipelines.\n\n**-y**, **--yes**\n\n> Auto-accept all prompts."],["CONFIGURATION","**dotenv-diff.config.json**\n\n> Optional configuration file in the project root. Accepts all CLI flags in camelCase form (e.g. ```allowDuplicates```, ```ignoreRegex```). Command-line arguments override file settings.\n\nInline ignore comments (```// dotenv-diff-ignore```) suppress warnings for a single line of code, and an expiration date can be attached to a variable in the .env file with ```# @expire YYYY-MM-DD``` to get warnings before credentials go stale."],["CAVEATS","Requires Node.js and is aimed at JavaScript/TypeScript projects; codebase scanning looks for references like ```process.env.KEY``` and framework-specific patterns, so variables read in other languages are not detected. Several checks (secret detection, unused-variable reporting, uppercase key enforcement, expiration warnings) are enabled by default and may need to be disabled with their ```--no-``` counterparts in unconventional setups."],["HISTORY","dotenv-diff is an MIT-licensed open-source project by GitHub user Chrilleweb, first published on npm in **July 2025**. It started as a simple .env vs .env.example diff tool and grew into a codebase-wide environment variable scanner with framework awareness, secret detection and CI integration; version 3.0.0 was released in **July 2026**."],["SEE ALSO","[dotenvx](/man/dotenvx)(1), [direnv](/man/direnv)(1), [env](/man/env)(1), [envsubst](/man/envsubst)(1), [trufflehog](/man/trufflehog)(1)"],["RESOURCES","```[Source code](https://github.com/Chrilleweb/dotenv-diff)```\n\n```[Documentation](https://dotenv-diff-docs.vercel.app)```"]],"e":["dotenvx","direnv","env","envsubst","trufflehog"],"x":"dotenv-diff"},"dotnet-test":{"n":"dotnet-test","t":"test runner for .NET projects","s":[["TAGLINE","test runner for .NET projects"],["TLDR","**Run tests**\n\n```dotnet test```\n\n**Run tests in specific project**\n\n```dotnet test [tests/Tests.csproj]```\n\n**Run with filter**\n\n```dotnet test --filter \"[FullyQualifiedName~UnitTests]\"```\n\n**Run specific test**\n\n```dotnet test --filter \"Name=[TestMethodName]\"```\n\n**Run with verbosity**\n\n```dotnet test --verbosity [detailed]```\n\n**Run without build**\n\n```dotnet test --no-build```\n\n**Generate coverage report**\n\n```dotnet test --collect:\"XPlat Code Coverage\"```"],["SYNOPSIS","**dotnet** **test** [_project_] [_options_]"],["PARAMETERS","**--filter** _expression_\n> Filter tests by expression.\n\n**--no-build**\n> Don't build before testing.\n\n**--no-restore**\n> Don't restore before testing.\n\n**-c**, **--configuration** _CONFIG_\n> Build configuration (e.g., Debug, Release).\n\n**-v**, **--verbosity** _level_\n> Verbosity (quiet, minimal, normal, detailed, diagnostic).\n\n**--logger** _logger_\n> Test logger.\n\n**--collect** _data_\n> Enable data collector.\n\n**--blame**\n> Run tests in blame mode.\n\n**-r**, **--results-directory** _dir_\n> Results directory."],["DESCRIPTION","**dotnet test** executes unit tests in .NET projects using the configured test framework adapter. It supports popular testing frameworks including MSTest, NUnit, and xUnit through their respective NuGet packages.\n\nThe command discovers test methods through framework-specific attributes, executes them, and reports results including pass/fail status, execution time, and detailed failure information. It integrates with code coverage tools and CI/CD pipelines through various loggers and output formats."],["INSTALL","```dnf: sudo dnf install dotnet-host```\n\n```pacman: sudo pacman -S dotnet-host```\n\n```apk: sudo apk add dotnet-host```\n\n```brew: brew install dotnet```"],["SEE ALSO","[dotnet](/man/dotnet)(1), [dotnet-build](/man/dotnet-build)(1), [dotnet-run](/man/dotnet-run)(1)"]],"e":["dotnet","dotnet-build","dotnet-run"],"x":"dotnet test"},"dte":{"n":"dte","t":"Small configurable terminal text editor","s":[["TAGLINE","Small configurable terminal text editor"],["TLDR","**Open** a file for editing\n\n```dte [filename]```\n\n**Open** multiple files\n\n```dte [file1] [file2]```\n\n**Open** file at specific line\n\n```dte +[line_number] [filename]```\n\n**Open** file in read-only mode\n\n```dte -R [filename]```"],["SYNOPSIS","**dte** [_options_] [_files_]"],["PARAMETERS","**+**_N_\n> Start at line N\n\n**-R, --read-only**\n> Open in read-only mode\n\n**-c, --config** _FILE_\n> Use specified configuration file\n\n**-h, --help**\n> Display help and exit\n\n**-V, --version**\n> Display version and exit"],["DESCRIPTION","**dte** is a small, fast, and configurable terminal text editor designed to be lightweight yet powerful. It features syntax highlighting for many languages, multiple buffers, split windows, and a clean command interface similar to vi/vim but with different key bindings.\n\nThe editor is written in C with minimal dependencies, making it suitable for resource-constrained environments. It supports macros, regular expression search, and customizable key bindings through configuration files."],["KEYBINDINGS","**Ctrl+S**\n> Save file\n\n**Ctrl+Q**\n> Quit\n\n**Ctrl+F**\n> Search\n\n**Ctrl+G**\n> Go to line\n\n**Ctrl+W**\n> Close buffer\n\n**Ctrl+N**\n> New file\n\n**Ctrl+O**\n> Open file\n\n**Ctrl+Z**\n> Suspend editor\n\n**Tab**\n> Indent/Autocomplete\n\n**Shift+Tab**\n> Unindent"],["CONFIGURATION","Configuration file location:\n- ~/.dterc\n- ~/.config/dte/dterc\n\nExample configuration:\n\n```\nset auto-indent true\nset tab-width 4\nset show-line-numbers true\n```"],["CAVEATS","Different key bindings than vi/vim may require adjustment period. Some advanced features require compilation with specific options. Terminal compatibility varies. No built-in scripting language like Vimscript or Emacs Lisp."],["HISTORY","**dte** was created by Craig Barnes as a lightweight alternative to larger editors. It aims to provide essential editing features in a small footprint while remaining easy to configure and extend."],["INSTALL","```apt: sudo apt install dte```\n\n```nix: nix profile install nixpkgs#dte```"],["SEE ALSO","[vim](/man/vim)(1), [nano](/man/nano)(1), [micro](/man/micro)(1), [kakoune](/man/kakoune)(1)"]],"e":["vim","nano","micro","kakoune"],"x":"dte [filename]"},"dvc-dag":{"n":"dvc-dag","t":"visualize DVC pipeline dependencies","s":[["TAGLINE","visualize DVC pipeline dependencies"],["TLDR","**Show pipeline DAG**\n\n```dvc dag```\n\n**Output as dot format**\n\n```dvc dag --dot```\n\n**Show specific stage**\n\n```dvc dag [stage_name]```\n\n**Show full graph**\n\n```dvc dag --full```"],["SYNOPSIS","**dvc** **dag** [_options_] [_target_]"],["PARAMETERS","**--dot**\n> Output in Graphviz DOT format.\n\n**--full**\n> Show full graph including deps.\n\n**-o**, **--out** _file_\n> Output to file."],["DESCRIPTION","**dvc dag** generates a visual representation of your DVC pipeline's structure as a directed acyclic graph (DAG). It shows how pipeline stages depend on each other and the flow of data through your machine learning workflow.\n\nEach node in the graph represents a pipeline stage (data processing, training, evaluation, etc.), and edges show dependencies between stages. This visualization helps understand pipeline structure, identify bottlenecks, and debug execution order issues.\n\nThe default output is ASCII art suitable for terminal viewing. The --dot option exports to Graphviz DOT format, which can be rendered to images using tools like Graphviz or online viewers. This is useful for documentation and sharing pipeline architecture with team members."],["INSTALL","```brew: brew install dvc```\n\n```nix: nix profile install nixpkgs#dvc```"]],"e":[],"x":"dvc dag"},"exch":{"n":"exch","t":"utility to swap two file paths","s":[["TAGLINE","utility to swap two file paths"],["TLDR","**Swap** filepaths of two files\n\n```exch [path/to/file1] [path/to/file2]```"],["SYNOPSIS","**exch** _file1_ _file2_"],["PARAMETERS","_file1_\n> First file to swap\n\n_file2_\n> Second file to swap"],["DESCRIPTION","**exch** atomically exchanges two paths. After the operation, file1 refers to what was at file2's path and vice versa. It is a thin wrapper around the **RENAME_EXCHANGE** flag of the **renameat2**(2) system call.\n\nPart of the **util-linux** package. Useful for quickly swapping configuration files, symlinks, or backups without an intermediate temporary name."],["CAVEATS","Both files must exist. The exchange is atomic (no window where either name is missing), unlike a three-step mv dance. Requires **renameat2**(2) with RENAME_EXCHANGE, so it is Linux-only and both paths must reside on the same filesystem. Added in util-linux 2.41."],["INSTALL","```apt: sudo apt install util-linux-extra```\n\n```apk: sudo apk add util-linux-misc```"],["SEE ALSO","[mv](/man/mv)(1), [cp](/man/cp)(1), [rename](/man/rename)(1)"],["RESOURCES","```[Source code](https://github.com/util-linux/util-linux)```\n\n```[Documentation](https://man7.org/linux/man-pages/man1/exch.1.html)```"]],"e":["mv","cp","rename"],"x":"exch [path/to/file1] [path/to/file2]"},"extundelete":{"n":"extundelete","t":"ext3/ext4 file recovery utility","s":[["TAGLINE","ext3/ext4 file recovery utility"],["TLDR","**Restore all** deleted files from partition\n\n```sudo extundelete [/dev/sdXN] --restore-all```\n\nRestore a **specific file**\n\n```extundelete [/dev/sdXN] --restore-file [path/to/file]```\n\nRestore a **specific directory**\n\n```extundelete [/dev/sdXN] --restore-directory [path/to/directory]```\n\nRestore files deleted **after** a date\n\n```extundelete [/dev/sdXN] --restore-all --after [1577840400]```"],["SYNOPSIS","**extundelete** _device_ [_options_]"],["DESCRIPTION","**extundelete** recovers deleted files from ext3 and ext4 partitions by parsing the filesystem journal. It can restore individual files, directories, or all recoverable files.\n\nWorks best when run immediately after deletion before journal wraps."],["PARAMETERS","**--restore-all**\n> Restore all recoverable files\n\n**--restore-file** _path_\n> Restore specific file\n\n**--restore-directory** _path_\n> Restore specific directory\n\n**--after** _time_\n> Only restore files deleted after Unix timestamp\n\n**--before** _time_\n> Only restore files deleted before timestamp"],["CAVEATS","Only works with ext3/ext4 filesystems. Unmount partition before recovery for best results. Paths are relative to filesystem root (no leading /). Recovery success depends on journal state."],["INSTALL","```apt: sudo apt install extundelete```\n\n```dnf: sudo dnf install extundelete```\n\n```zypper: sudo zypper install extundelete```\n\n```nix: nix profile install nixpkgs#extundelete```"],["SEE ALSO","[testdisk](/man/testdisk)(8), [photorec](/man/photorec)(8), [debugfs](/man/debugfs)(8)"],["RESOURCES","```[Homepage](http://extundelete.sourceforge.net/)```"]],"e":["testdisk","photorec","debugfs"],"x":"sudo extundelete [/dev/sdXN] --restore-all"},"fetchmail":{"n":"fetchmail","t":"retrieve mail from remote servers","s":[["TAGLINE","retrieve mail from remote servers"],["TLDR","**Fetch mail from server**\n\n```fetchmail```\n\n**Fetch from specific server**\n\n```fetchmail [mail.example.com]```\n\n**Keep messages on server**\n\n```fetchmail -k```\n\n**Check for mail only**\n\n```fetchmail -c```\n\n**Verbose output**\n\n```fetchmail -v```\n\n**Run as daemon**\n\n```fetchmail -d [300]```\n\n**Use specific config file**\n\n```fetchmail -f [~/.fetchmailrc]```"],["SYNOPSIS","**fetchmail** [_options_] [_servers_...]"],["DESCRIPTION","**fetchmail** retrieves mail from remote servers and forwards it to the local mail system. It supports POP3, IMAP, ETRN, and ODMR protocols with SSL/TLS encryption.\n\nThe tool is configured through ~/.fetchmailrc or command-line options. It can run as a daemon, periodically checking for new mail, and integrates with local MTAs."],["PARAMETERS","**-c**, **--check**\n> Check for mail only, don't fetch.\n\n**-k**, **--keep**\n> Keep messages on server.\n\n**-a**, **--all**\n> Fetch all messages (including seen).\n\n**-d** _seconds_\n> Daemon mode with poll interval.\n\n**-f** _file_\n> Use alternate config file.\n\n**-u** _user_\n> Remote username.\n\n**-p** _proto_, **--proto** _proto_\n> Protocol (auto, pop3, imap, etrn, odmr).\n\n**--ssl**\n> Use implicit SSL/TLS (connect directly over encrypted channel).\n\n**--sslproto** _value_\n> SSL/TLS protocol version and STARTTLS control. Recommended: tls1.2+.\n\n**-v**, **--verbose**\n> Verbose output.\n\n**-s**, **--silent**\n> Suppress progress messages.\n\n**-q**, **--quit**\n> Kill running daemon.\n\n**-l** _maxbytes_, **--limit** _maxbytes_\n> Skip messages larger than maxbytes.\n\n**-r** _folder_, **--folder** _folder_\n> Retrieve from non-default mail folder (IMAP only).\n\n**--idle**\n> Use IMAP IDLE for push notification of new mail."],["CONFIGURATION","**~/.fetchmailrc**\n> Main configuration file containing server settings, authentication, and delivery options."],["CAVEATS","Passwords in config file need protection. SSL verification recommended. Large mailboxes may be slow. Local MTA must accept forwarded mail."],["HISTORY","**fetchmail** was originally written by **Carl Harris** as popclient in **1993**. **Eric S. Raymond** took over development in **1996**, renaming it fetchmail and adding IMAP support. It became a standard tool for pulling mail from remote servers."],["INSTALL","```apt: sudo apt install fetchmail```\n\n```dnf: sudo dnf install fetchmail```\n\n```pacman: sudo pacman -S fetchmail```\n\n```apk: sudo apk add fetchmail```\n\n```zypper: sudo zypper install fetchmail```\n\n```brew: brew install fetchmail```\n\n```nix: nix profile install nixpkgs#fetchmail```"],["SEE ALSO","[procmail](/man/procmail)(1), [mutt](/man/mutt)(1), [sendmail](/man/sendmail)(1), [mail](/man/mail)(1)"]],"e":["procmail","mutt","sendmail","mail"],"x":"fetchmail"},"gemtopnm":{"n":"gemtopnm","t":"GEM IMG to PNM converter","s":[["TAGLINE","GEM IMG to PNM converter"],["TLDR","**Convert GEM IMG to PNM**\n\n```gemtopnm [input.img] > [output.pnm]```"],["SYNOPSIS","**gemtopnm** [_input_]"],["DESCRIPTION","**gemtopnm** converts GEM IMG format images to PNM (Portable Any Map) format. Handles both monochrome and color GEM images."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[gemtopbm](/man/gemtopbm)(1)"]],"e":["gemtopbm"],"x":"gemtopnm [input.img] > [output.pnm]"},"gh-pr-checks":{"n":"gh-pr-checks","t":"Display CI check status on pull requests","s":[["TAGLINE","Display CI check status on pull requests"],["TLDR","**View checks for the current branch's PR**\n\n```gh pr checks```\n\n**View checks for a specific PR**\n\n```gh pr checks [pr-number]```\n\n**Watch checks until they finish**\n\n```gh pr checks --watch```\n\n**Watch checks and exit on first failure**\n\n```gh pr checks --watch --fail-fast```\n\n**Show only required checks**\n\n```gh pr checks --required```\n\n**Output check results as JSON**\n\n```gh pr checks --json [name,state,bucket]```\n\n**Open check details in a web browser**\n\n```gh pr checks --web```"],["SYNOPSIS","**gh pr checks** [_number_ | _url_ | _branch_] [_options_]"],["PARAMETERS","_number_ | _url_ | _branch_\n> Pull request number, URL, or branch name (defaults to current branch).\n\n**--watch**\n> Watch checks until they finish.\n\n**-i**, **--interval** _SECONDS_\n> Refresh interval in seconds when watching (default 10).\n\n**--fail-fast**\n> Exit watch mode on first check failure.\n\n**--required**\n> Only show checks that are required.\n\n**-w**, **--web**\n> Open the web browser to show details about checks.\n\n**--json** _fields_\n> Output JSON with the specified fields. Available fields: bucket, completedAt, description, event, link, name, startedAt, state, workflow.\n\n**-q**, **--jq** _expression_\n> Filter JSON output using a jq expression.\n\n**-t**, **--template** _string_\n> Format JSON output using a Go template.\n\n**-R**, **--repo** _OWNER/REPO_\n> Select another repository using the [HOST/]OWNER/REPO format."],["DESCRIPTION","**gh pr checks** displays the status of CI checks and status checks on a pull request. It shows each check's name, status (pending, passing, failing), and conclusion.\n\nThe watch mode enables waiting for checks to complete, useful in scripts that need to proceed only after successful CI runs. The command integrates with GitHub Actions and third-party CI services. When using **--json**, the **bucket** field categorizes the state into pass, fail, pending, skipping, or cancel."],["CAVEATS","Only shows checks reported to GitHub. Some CI systems may have delays in reporting. The **--fail-fast** flag only works in combination with **--watch**. Watch mode is subject to API rate limits."],["INSTALL","```apt: sudo apt install gh```\n\n```dnf: sudo dnf install gh```\n\n```pacman: sudo pacman -S github-cli```\n\n```apk: sudo apk add github-cli```\n\n```zypper: sudo zypper install gh```\n\n```brew: brew install gh```\n\n```nix: nix profile install nixpkgs#gh```"],["SEE ALSO","[gh-pr](/man/gh-pr)(1), [gh-pr-merge](/man/gh-pr-merge)(1), [gh-run](/man/gh-run)(1), [gh-pr-view](/man/gh-pr-view)(1)"]],"e":["gh-pr","gh-pr-merge","gh-run","gh-pr-view"],"x":"gh pr checks"},"gh-repo-create":{"n":"gh-repo-create","t":"Create new GitHub repositories","s":[["TAGLINE","Create new GitHub repositories"],["TLDR","**Create repo interactively**\n\n```gh repo create```\n\n**Create from current directory**\n\n```gh repo create [name] --source=.```\n\n**Create empty repo**\n\n```gh repo create [name] --public```\n\n**Create private repo**\n\n```gh repo create [name] --private```\n\n**Create from template**\n\n```gh repo create [name] --template [owner/template]```\n\n**Clone after creating**\n\n```gh repo create [name] --clone```\n\n**Create with description**\n\n```gh repo create [name] -d \"[description]\"```"],["SYNOPSIS","**gh** **repo** **create** [_name_] [_options_]"],["PARAMETERS","**--public**\n> Make public.\n\n**--private**\n> Make private.\n\n**--internal**\n> Make internal.\n\n**--source** _dir_\n> Create from local directory.\n\n**--template** _repo_\n> Create from template.\n\n**--clone**\n> Clone after creating.\n\n**-d**, **--description** _text_\n> Description.\n\n**--homepage** _url_\n> Homepage URL.\n\n**--gitignore** _template_\n> Add .gitignore.\n\n**--license** _license_\n> Add license."],["DESCRIPTION","**gh repo create** creates GitHub repositories from the command line with support for multiple creation workflows. It can create empty repositories, initialize from the current directory, or instantiate from repository templates.\n\nWhen run interactively without arguments, the command guides you through repository creation with prompts for visibility, description, and initialization options. The --source flag publishes an existing local git repository to GitHub. Template repositories (--template) provide starter configurations for common project types."],["INSTALL","```apt: sudo apt install gh```\n\n```dnf: sudo dnf install gh```\n\n```pacman: sudo pacman -S github-cli```\n\n```apk: sudo apk add github-cli```\n\n```zypper: sudo zypper install gh```\n\n```brew: brew install gh```\n\n```nix: nix profile install nixpkgs#gh```"],["SEE ALSO","[gh-repo](/man/gh-repo)(1)"]],"e":["gh-repo"],"x":"gh repo create"},"gops":{"n":"gops","t":"Go process diagnostic tool","s":[["TAGLINE","Go process diagnostic tool"],["TLDR","**List Go processes**\n\n```gops```\n\n**Show process details**\n\n```gops [pid]```\n\n**Dump stack trace**\n\n```gops stack [pid]```\n\n**Memory statistics**\n\n```gops memstats [pid]```\n\n**CPU profile**\n\n```gops pprof-cpu [pid]```"],["SYNOPSIS","**gops** [_command_] [_pid_]"],["PARAMETERS","_PID_\n> Process ID.\n\n**stack** _PID_\n> Print stack trace.\n\n**memstats** _PID_\n> Show memory stats.\n\n**pprof-cpu** _PID_\n> CPU profile.\n\n**pprof-heap** _PID_\n> Heap profile.\n\n**gc** _PID_\n> Trigger garbage collection.\n\n**--help**\n> Display help information."],["DESCRIPTION","**gops** lists and diagnoses Go processes running on the system. It identifies Go programs and provides debugging commands including stack traces, memory statistics, and CPU/heap profiling.\n\nFor full diagnostic capabilities, the gops agent must be imported in target programs. Basic process identification works without the agent."],["CAVEATS","Full features need agent imported. Works with compatible Go versions. Some info from /proc."],["HISTORY","gops was created by **Jaana Dogan** at Google to provide a diagnostic tool for Go processes."],["INSTALL","```dnf: sudo dnf install gops```\n\n```apk: sudo apk add gops```\n\n```zypper: sudo zypper install gops```\n\n```brew: brew install gops```\n\n```nix: nix profile install nixpkgs#gops```"],["SEE ALSO","[go-tool](/man/go-tool)(1), [pprof](/man/pprof)(1)"],["RESOURCES","```[Source code](https://github.com/google/gops)```"]],"e":["go-tool","pprof"],"x":"gops"},"gotip":{"n":"gotip","t":"Daily builds of Go from development tree","s":[["TAGLINE","Daily builds of Go from development tree"],["TLDR","**Run** Go development version\n\n```gotip [go-commands]```\n\n**Install** latest development version\n\n```gotip download```\n\n**Build** specific CL\n\n```gotip download [CL-number]```"],["SYNOPSIS","**gotip** [_options_] _command_"],["PARAMETERS","**download** [_CL_]\n> Fetch and build the current Go development tree into **$HOME/sdk/gotip**. When a Gerrit change-list number is supplied, that CL is applied on top before building.\n\nAny other argument is passed through unchanged to the **go** tool inside the installed gotip SDK, so **gotip build**, **gotip test**, **gotip env**, etc. all Just Work."],["DESCRIPTION","**gotip** provides access to daily builds of Go from the development tree. It allows developers to test upcoming Go features and bug fixes before they are released in stable versions.\n\nUnlike the numbered `go1.XX` download wrappers, **gotip download** builds the toolchain from source at the latest **master** commit of **golang/go**, so a working Go installation is required to bootstrap it. To refresh the installation, delete **~/sdk/gotip/.unpacked-success** (or the whole directory) and re-run **gotip download**."],["USAGE","Install latest development version:\n\n```gotip download```\n\nUse gotip like regular Go:\n\n```gotip run main.go```\n```gotip test ./...```\n```gotip build```"],["CAVEATS","Development versions may be unstable. Not recommended for production use. Requires internet connection to download. Binaries are large downloads."],["HISTORY","**gotip** was created by the Go team to make testing development versions easier, replacing earlier workarounds for accessing pre-release Go versions."],["INSTALL","```nix: nix profile install nixpkgs#gotip```"],["SEE ALSO","[go](/man/go)(1)"]],"e":["go"],"x":"gotip [go-commands]"},"grodvi":{"n":"grodvi","t":"Groff DVI output driver","s":[["TAGLINE","Groff DVI output driver"],["TLDR","**Convert groff to DVI**\n\n```groff -Tdvi [file.roff] > output.dvi```\n\n**Direct grodvi usage**\n\n```grodvi [file] > output.dvi```\n\n**Specify font path**\n\n```grodvi -F [fontdir] [file]```"],["SYNOPSIS","**grodvi** [_options_] [_file_]"],["PARAMETERS","_FILE_\n> Groff intermediate output file.\n\n**-d**\n> Enable debug mode.\n\n**-F** _DIR_\n> Font directory.\n\n**-l**\n> Landscape mode.\n\n**-p** _SIZE_\n> Paper size.\n\n**--help**\n> Display help information."],["DESCRIPTION","**grodvi** is a groff output driver that produces DVI (DeVice Independent) format, compatible with TeX ecosystem tools like dvips and xdvi.\n\nThe driver converts groff intermediate output to DVI format, allowing integration with TeX-based workflows and standard groff font features."],["CAVEATS","Part of groff package. DVI format has limitations compared to PDF. Requires TeX fonts for best results."],["HISTORY","grodvi was developed as part of **GNU groff** to provide DVI output capability, bridging groff and TeX ecosystems."],["INSTALL","```apt: sudo apt install groff```\n\n```dnf: sudo dnf install groff```\n\n```pacman: sudo pacman -S groff```\n\n```apk: sudo apk add groff```\n\n```zypper: sudo zypper install groff```\n\n```brew: brew install groff```\n\n```nix: nix profile install nixpkgs#groff```"],["SEE ALSO","[groff](/man/groff)(1), [grops](/man/grops)(1), [dvips](/man/dvips)(1)"],["RESOURCES","```[Source code](https://git.savannah.gnu.org/cgit/groff.git)```\n\n```[Homepage](https://www.gnu.org/software/groff/)```"]],"e":["groff","grops","dvips"],"x":"groff -Tdvi [file.roff] > output.dvi"},"grub-probe":{"n":"grub-probe","t":"probe device and filesystem information for GRUB","s":[["TAGLINE","probe device and filesystem information for GRUB"],["TLDR","Get GRUB **filesystem module** for a path\n\n```sudo grub-probe -t fs /boot/grub```\n\nGet the **system device** containing a path\n\n```sudo grub-probe -t device /boot/grub```\n\nGet the **underlying disk** for a system device\n\n```sudo grub-probe -t disk /dev/sdX -d```\n\nGet **filesystem UUID**\n\n```sudo grub-probe -t fs_uuid /boot/grub```\n\nGet **filesystem label**\n\n```sudo grub-probe -t fs_label /boot/grub```\n\nGet **MBR partition type** code\n\n```sudo grub-probe -t msdos_parttype /dev/sdX```\n\nGet the **partition map type**\n\n```sudo grub-probe -t partmap /boot/grub```\n\nProbe using a **custom device map**\n\n```sudo grub-probe -t fs /boot/grub -m [path/to/custom_device.map]```"],["SYNOPSIS","**grub-probe** [_options_] **-t** _target_ _path_|_device_"],["PARAMETERS","**-t**, **--target** _TARGET_\n> Information to probe: fs, fs_uuid, fs_label, device, disk, drive, partmap, partuuid, abstraction, cryptodisk_uuid, msdos_parttype, gpt_parttype, hints_string, zero_check (default: fs)\n\n**-d**, **--device**\n> Treat argument as a system device, not a path\n\n**-m**, **--device-map** _FILE_\n> Use custom device map file (default: /boot/grub/device.map)\n\n**-0**\n> Separate items in output using ASCII NUL characters\n\n**-v**, **--verbose**\n> Enable verbose output (pass twice for debug printing)"],["DESCRIPTION","**grub-probe** extracts device and filesystem information in GRUB's naming format. It is primarily used internally by GRUB tools but is useful for debugging boot configuration.\n\nTarget types include **fs** (filesystem module), **device** (system device path), **disk** (system disk path), **drive** (GRUB drive name like hd0), **fs_uuid** (filesystem UUID), **fs_label** (filesystem label), **partmap** (partition map type), **partuuid** (partition UUID), **abstraction** (abstraction module like lvm), **cryptodisk_uuid**, **msdos_parttype**, **gpt_parttype**, and various boot hint targets.\n\nThis tool bridges Linux device names to GRUB's naming conventions, which is essential for generating correct boot configurations."],["CAVEATS","Requires root privileges for most operations. Device mapping can change between boots, especially with removable media. GRUB drive naming may differ from Linux device names."],["HISTORY","grub-probe is part of GRUB 2's toolset, providing the device detection logic used by grub-install and grub-mkconfig. It replaced simpler detection methods used in GRUB Legacy."],["INSTALL","```apt: sudo apt install grub-common```\n\n```pacman: sudo pacman -S grub```\n\n```apk: sudo apk add grub```"],["SEE ALSO","[grub-install](/man/grub-install)(8), [grub-mkconfig](/man/grub-mkconfig)(8), [grub-mkdevicemap](/man/grub-mkdevicemap)(8), [blkid](/man/blkid)(8)"]],"e":["grub-install","grub-mkconfig","grub-mkdevicemap","blkid"],"x":"sudo grub-probe -t fs /boot/grub"},"history":{"n":"history","t":"bash builtin command that displays or manipulates the command history list","s":[["TAGLINE","bash builtin command that displays or manipulates the command history list"],["TLDR","**Display command history** with line numbers\n\n```history```\n\n**Show the last N commands**\n\n```history [20]```\n\n**Clear the entire history**\n\n```history -c```\n\n**Delete a specific history entry** by line number\n\n```history -d [42]```\n\n**Append current session history** to the history file\n\n```history -a```\n\n**Read history file** and append to current list\n\n```history -r```\n\n**Write current history** to the history file\n\n```history -w```\n\n**Execute a command** from history by number\n\n```![42]```\n\n**Execute the previous command**\n\n```!!```"],["SYNOPSIS","**history** [_n_]\n\n**history** -c\n\n**history** -d _offset_\n\n**history** [-anrw] [_filename_]\n\n**history** -p _arg_ [_arg_ ...]\n\n**history** -s _arg_ [_arg_ ...]"],["PARAMETERS","**-c**\n> Clear the history list\n\n**-d** _OFFSET_\n> Delete the history entry at position offset\n\n**-d** _START-END_\n> Delete the range of history entries between start and end\n\n**-a**\n> Append new history lines to the history file\n\n**-n**\n> Read history lines not already read from the file to current list\n\n**-r**\n> Read the history file and append contents to the history list\n\n**-w**\n> Write current history to the history file\n\n**-p**\n> Perform history substitution on args and display without storing\n\n**-s** _ARG_\n> Add args to the end of the history list as a single entry"],["ENVIRONMENT","**HISTFILE**\n> File where history is saved (default: ~/.bash_history)\n\n**HISTSIZE**\n> Number of commands kept in memory for current session\n\n**HISTFILESIZE**\n> Number of commands kept in the history file\n\n**HISTTIMEFORMAT**\n> Format string for timestamps (strftime format)\n\n**HISTCONTROL**\n> Controls what is saved (ignorespace, ignoredups, erasedups)\n\n**HISTIGNORE**\n> Colon-separated patterns of commands to ignore"],["DESCRIPTION","**history** is a bash builtin command that displays or manipulates the command history list. The shell maintains a record of commands entered during a session, allowing quick access to previous commands.\n\nWithout arguments, history displays the numbered list of commands. With an integer argument **n**, it shows the last n entries. The history is loaded from **HISTFILE** at shell startup and can be written back with **-w** or automatically on exit.\n\nHistory expansion allows re-executing previous commands: **!!** repeats the last command, **!n** executes command number n, **!string** executes the most recent command starting with string, and **!?string** matches any command containing string. Use **Ctrl+r** for interactive reverse search."],["CAVEATS","Commands starting with a space are not saved if **HISTCONTROL** contains ignorespace. The history is per-shell and not shared between concurrent sessions unless explicitly synchronized with **-a** and **-r**. History files can contain sensitive information like passwords accidentally typed on the command line. Use **HISTIGNORE** to exclude sensitive commands."],["HISTORY","Command history originated in the **C shell** (csh) developed by Bill Joy in the late 1970s at Berkeley. The bash implementation builds on the **GNU Readline** library, which provides history manipulation and editing capabilities. The history command and expansion syntax became a standard feature in Unix shells, allowing users to efficiently reuse and modify previous commands."],["SEE ALSO","[bash](/man/bash)(1), [fc](/man/fc)(1), [readline](/man/readline)(3)"]],"e":["bash","fc","readline"],"x":"history"},"impacket-sniff":{"n":"impacket-sniff","t":"basic packet capture tool using Impacket's raw socket capabilities","s":[["TAGLINE","basic packet capture tool using Impacket's raw socket capabilities"],["TLDR","**Capture network packets** on an interface\n\n```sudo impacket-sniff [eth0]```\n\n**Sniff with a filter expression**\n\n```sudo impacket-sniff -filter '[tcp port 80]' [eth0]```"],["SYNOPSIS","**impacket-sniff** [_-h_] [_-filter FILTER_] _interface_"],["PARAMETERS","**-filter** _FILTER_\n> BPF filter expression to limit captured packets"],["DESCRIPTION","**impacket-sniff** is a basic packet capture tool using Impacket's raw socket capabilities. It captures network traffic on the specified interface and displays packet information.\n\nThe tool provides a simple way to observe network traffic using Impacket's protocol parsing, which can be useful for debugging or understanding network communications during security assessments."],["CAVEATS","Requires root/administrator privileges for raw socket access. For production packet capture needs, tools like **tcpdump** or **Wireshark** offer more features. The interface must be in a state that allows promiscuous mode."],["HISTORY","Part of the **Impacket** library by SecureAuth. Included as a demonstration of Impacket's packet capture and parsing capabilities using Python."],["INSTALL","```pacman: sudo pacman -S impacket```"],["SEE ALSO","[tcpdump](/man/tcpdump)(1), [wireshark](/man/wireshark)(1), [impacket-sniffer](/man/impacket-sniffer)(1), [tshark](/man/tshark)(1)"]],"e":["tcpdump","wireshark","impacket-sniffer","tshark"],"x":"sudo impacket-sniff [eth0]"},"impacket-ticketconverter":{"n":"impacket-ticketconverter","t":"Convert Kerberos tickets between ccache and kirbi formats","s":[["TAGLINE","Convert Kerberos tickets between ccache and kirbi formats"],["TLDR","**Convert a kirbi ticket to ccache** format\n\n```impacket-ticketConverter [ticket.kirbi] [ticket.ccache]```\n\n**Convert a ccache ticket to kirbi** format\n\n```impacket-ticketConverter [ticket.ccache] [ticket.kirbi]```\n\n**Convert a base64-encoded ticket**\n\n```impacket-ticketConverter -b [encoded_ticket.txt] [ticket.ccache]```"],["SYNOPSIS","**impacket-ticketConverter** [**-h**] [**-b**] _input_file_ _output_file_"],["PARAMETERS","_input_file_\n> Source ticket file in either kirbi (KRB-CRED) or ccache format. The format is detected automatically.\n\n_output_file_\n> Destination filename for the converted ticket.\n\n**-b, --base64**\n> Decode the input ticket from base64 encoding before conversion.\n\n**-h, --help**\n> Show help message and exit."],["DESCRIPTION","**impacket-ticketConverter** converts Kerberos authentication tickets between two common formats: **ccache** (used by UNIX-based tools like Impacket) and **kirbi** / KRB-CRED (used by Windows tools like Mimikatz). The tool automatically detects the input format by examining the first byte of the file and converts to the opposite format.\n\nThis is useful when working across platforms during authorized security assessments, where tickets obtained on Windows need to be used with Linux-based tools or vice versa."],["CAVEATS","Part of the **Impacket** toolkit. The tool only converts between ccache and kirbi formats; it does not create, modify, or validate tickets. Requires Python and the Impacket library to be installed. Only for use in authorized security testing contexts."],["HISTORY","**Impacket** was originally developed by **SecureAuth** and is now maintained by **Fortra's Core Security**. It is a collection of Python classes for working with network protocols, designed to assist security professionals with penetration testing and security research."],["INSTALL","```pacman: sudo pacman -S impacket```"],["SEE ALSO","[impacket](/man/impacket)(1), [kinit](/man/kinit)(1)"]],"e":["impacket","kinit"],"x":"impacket-ticketConverter [ticket.kirbi] [ticket.ccache]"},"j":{"n":"j","t":"autojump shell function for quick directory navigation","s":[["TAGLINE","autojump shell function for quick directory navigation"],["TLDR","**Jump to directory**\n\n```j [pattern]```\n\n**Jump to child directory**\n\n```jc [pattern]```\n\n**Open directory in file manager**\n\n```jo [pattern]```\n\n**Show matching directories**\n\n```j -s [pattern]```\n\n**Add directory to database**\n\n```j -a [/path/to/dir]```"],["SYNOPSIS","**j** [_options_] [_pattern_]"],["PARAMETERS","_PATTERN_\n> Directory name pattern to match.\n\n**-s**\n> Show matching directories.\n\n**-a** _PATH_\n> Add path to database.\n\n**-i**\n> Case-insensitive match.\n\n**--help**\n> Display help information."],["DESCRIPTION","**j** is the autojump shell function for quick directory navigation. It learns from your cd habits and jumps to frequently visited directories.\n\nThe tool maintains a weighted database of directories. Partial names match against this database for fast navigation."],["CAVEATS","Alias for autojump. Requires shell integration. Database builds over time."],["HISTORY","j is the default alias for **autojump**, created by **William Ting** to speed up directory navigation in the shell."],["INSTALL","```nix: nix profile install nixpkgs#j```"],["SEE ALSO","[autojump](/man/autojump)(1), [z](/man/z)(1), [cd](/man/cd)(1), [fasd](/man/fasd)(1)"]],"e":["autojump","z","cd","fasd"],"x":"j [pattern]"},"jid":{"n":"jid","t":"interactive JSON query tool","s":[["TAGLINE","interactive JSON query tool"],["TLDR","**Interactively explore JSON file**\n\n```jid < [file.json]```\n\n**Explore JSON from command output**\n\n```curl -s [https://api.example.com/data] | jid```\n\n**Explore with initial query**\n\n```jid -q \".[0].name\" < [file.json]```\n\n**Output with monochrome**\n\n```jid -M < [file.json]```\n\n**Pretty print output**\n\n```jid -p < [file.json]```"],["SYNOPSIS","**jid** [_options_]"],["PARAMETERS","**-q** _query_\n> Initial query string to start with.\n\n**-M**\n> Monochrome output (no colors).\n\n**-p**\n> Pretty print the output JSON.\n\n**-h**\n> Display help information.\n\n**-version**\n> Display version information."],["KEYBOARD COMMANDS","**Tab**\n> Trigger autocomplete suggestions.\n\n**Ctrl+w**\n> Delete word backward.\n\n**Ctrl+u**\n> Clear entire query line.\n\n**Ctrl+f** / **Right**\n> Move cursor forward.\n\n**Ctrl+b** / **Left**\n> Move cursor backward.\n\n**Ctrl+a** / **Home**\n> Jump to start of line.\n\n**Ctrl+e** / **End**\n> Jump to end of line.\n\n**Ctrl+n** / **Down**\n> Next suggestion.\n\n**Ctrl+p** / **Up**\n> Previous suggestion.\n\n**Enter**\n> Accept and output result.\n\n**Ctrl+c**\n> Exit without output."],["QUERY SYNTAX","**.**\n> Access object property.\n\n**.foo**\n> Access property \"foo\".\n\n**[0]**\n> Access array index 0.\n\n**.[0:5]**\n> Array slice from index 0 to 5.\n\n**.[]**\n> Iterate all array elements.\n\n**|**\n> Pipe output to next filter."],["DESCRIPTION","**jid** (JSON Incremental Digger) is an interactive JSON query tool. It displays JSON data and provides real-time filtering with autocomplete as you type queries.\n\nUnlike jq which runs queries non-interactively, jid shows immediate results as you build your query. Tab completion suggests available keys and indices, making it easy to explore unfamiliar JSON structures.\n\nThe query syntax follows jq conventions. Type **.** to access object keys, **[n]** for array indices, and **.[]** to iterate arrays. Autocomplete shows available options at each level.\n\nWhen you press Enter, jid outputs the current filtered result to stdout, which can be piped to other commands. This makes it useful for interactively building jq queries."],["CAVEATS","Reads entire JSON into memory, so very large files may be slow. Query syntax is a subset of jq; some advanced jq features are not supported. Input must be valid JSON."],["HISTORY","jid was created by **simeji** and first released around **2016**. Written in Go, it was designed to make JSON exploration interactive rather than requiring query construction upfront. The project provides a bridge between exploring unknown JSON structures and building jq queries for automation."],["INSTALL","```apt: sudo apt install jid```\n\n```dnf: sudo dnf install jid```\n\n```brew: brew install jid```\n\n```nix: nix profile install nixpkgs#jid```"],["SEE ALSO","[jq](/man/jq)(1), [gojq](/man/gojq)(1), [jless](/man/jless)(1), [fx](/man/fx)(1)"]],"e":["jq","gojq","jless","fx"],"x":"jid < [file.json]"},"keybase":{"n":"keybase","t":"command-line client for Keybase, providing end-to-end encrypted chat, file","s":[["TAGLINE","command-line client for Keybase, providing end-to-end encrypted chat, file"],["TLDR","**Login to Keybase**\n\n```keybase login```\n\n**Check status**\n\n```keybase status```\n\n**Search for users**\n\n```keybase search [username]```\n\n**Encrypt file for user**\n\n```keybase encrypt -i [file] -o [file.enc] [username]```\n\n**Decrypt file**\n\n```keybase decrypt -i [file.enc] -o [file]```\n\n**Sign a message**\n\n```keybase sign -m \"[message]\"```\n\n**Verify signature**\n\n```keybase verify -i [file.sig]```\n\n**Start a chat**\n\n```keybase chat send [username] \"[message]\"```"],["SYNOPSIS","**keybase** [_command_] [_options_]"],["PARAMETERS","**login**\n> Log in to Keybase.\n\n**signup**\n> Create new account.\n\n**status**\n> Show account status.\n\n**encrypt**\n> Encrypt data for users.\n\n**decrypt**\n> Decrypt data.\n\n**sign**\n> Sign message or file.\n\n**verify**\n> Verify signature.\n\n**chat**\n> Keybase chat operations.\n\n**fs**\n> Keybase filesystem operations.\n\n**git**\n> Keybase git operations.\n\n**pgp**\n> Manage PGP keys.\n\n**log send**\n> Send logs for debugging."],["DESCRIPTION","**keybase** is a command-line client for Keybase, providing end-to-end encrypted chat, file sharing, and identity verification. Uses public-key cryptography to verify identities across social networks. Includes encrypted filesystem (KBFS) and encrypted git repositories."],["INSTALL","```nix: nix profile install nixpkgs#keybase```"],["SEE ALSO","[gpg](/man/gpg)(1), [ssh](/man/ssh)(1)"]],"e":["gpg","ssh"],"x":"keybase login"},"killall":{"n":"killall","t":"sends a signal to all processes running the specified command","s":[["TAGLINE","sends a signal to all processes running the specified command"],["TLDR","**Terminate all processes** with a specific name\n\n```killall [process_name]```\n\n**Force kill processes** (SIGKILL)\n\n```killall -9 [process_name]```\n\n**Send a specific signal**\n\n```killall -s [SIGTERM] [process_name]```\n\n**Kill only processes owned by you**\n\n```killall -u $(whoami) [process_name]```\n\n**Interactive mode** (confirm before killing)\n\n```killall -i [process_name]```\n\n**Kill processes matching regex**\n\n```killall -r \"[pattern]\"```\n\n**Verbose mode** — report if signal was successfully sent\n\n```killall -v [process_name]```\n\n**Wait for processes to terminate**\n\n```killall -w [process_name]```\n\n**List all known signal names**\n\n```killall -l```\n\n**Kill processes older than** a specified duration\n\n```killall -o 1h [process_name]```"],["SYNOPSIS","**killall** [_-signal_] [_-eiIqrvw_] [_-u user_] [_-g group_] [_-o time_] [_-y time_] [_name ..._]"],["PARAMETERS","**-e**, **--exact**\n> Require exact name match for very long names. By default, killall truncates names to 15 characters.\n\n**-I**, **--ignore-case**\n> Case-insensitive matching.\n\n**-i**, **--interactive**\n> Ask for confirmation before killing.\n\n**-l**, **--list**\n> List known signal names.\n\n**-q**, **--quiet**\n> Don't complain if no processes were killed.\n\n**-r**, **--regexp**\n> Interpret names as extended regular expressions.\n\n**-s**, **--signal** _signal_\n> Send specified signal (name or number).\n\n**-u**, **--user** _user_\n> Kill only processes owned by user.\n\n**-g**, **--process-group**\n> Kill the process group instead of process.\n\n**-v**, **--verbose**\n> Report if signal was successfully sent.\n\n**-w**, **--wait**\n> Wait for killed processes to terminate.\n\n**-o**, **--older-than** _time_\n> Kill processes older than specified time.\n\n**-y**, **--younger-than** _time_\n> Kill processes younger than specified time.\n\n**-n**, **--ns** _PID_\n> Match against the PID namespace of the given PID.\n\n**-V**, **--version**\n> Display version information.\n\n**-Z**, **--context** _pattern_\n> Kill only processes with matching SELinux context (SELinux only)."],["COMMON SIGNALS","**SIGTERM (15)**\n> Graceful termination (default).\n\n**SIGKILL (9)**\n> Force kill (cannot be caught).\n\n**SIGHUP (1)**\n> Hangup (often triggers reload).\n\n**SIGINT (2)**\n> Interrupt (like Ctrl+C).\n\n**SIGSTOP (19)**\n> Pause process.\n\n**SIGCONT (18)**\n> Resume paused process."],["DESCRIPTION","**killall** sends a signal to all processes running the specified command. Unlike **kill** which requires process IDs, killall works with process names, making it convenient for terminating multiple instances of a program.\n\nBy default, killall sends SIGTERM (signal 15), which requests graceful termination. Processes can catch this signal and clean up before exiting. Use SIGKILL (-9) when processes don't respond to SIGTERM, but be aware this prevents cleanup.\n\nName matching compares against the command name (visible in ps). The -r flag enables regex matching for flexible patterns. With -i, you're prompted before each process is killed.\n\nThe -w flag makes killall wait until all processes are actually terminated before returning. This is useful in scripts where you need to ensure processes are stopped before proceeding.\n\nTime-based filtering (-o, -y) allows killing only processes that have been running longer or shorter than specified duration (e.g., \"1h\" for one hour, \"30m\" for 30 minutes, \"1d\" for one day)."],["CAVEATS","Behavior differs between Linux (psmisc) and BSD/macOS versions. On macOS, syntax and options differ significantly. Be careful with common names - killall java might kill more than intended. Root can kill any process; users can only kill their own."],["HISTORY","**killall** originated in BSD Unix. The Linux version is part of the **psmisc** package, maintained separately and with different features than the BSD version. The command has been a standard Unix utility for decades, though the implementation differences between systems require attention for portable scripts."],["INSTALL","```apt: sudo apt install psmisc```\n\n```dnf: sudo dnf install psmisc```\n\n```pacman: sudo pacman -S psmisc```\n\n```apk: sudo apk add psmisc```\n\n```zypper: sudo zypper install psmisc```\n\n```nix: nix profile install nixpkgs#psmisc```"],["SEE ALSO","[kill](/man/kill)(1), [pkill](/man/pkill)(1), [pgrep](/man/pgrep)(1), [ps](/man/ps)(1), [pidof](/man/pidof)(1)"]],"e":["kill","pkill","pgrep","ps","pidof"],"x":"killall [process_name]"},"kwallet-query":{"n":"kwallet-query","t":"command-line tool for reading and writing to KDE Wallet","s":[["TAGLINE","command-line tool for reading and writing to KDE Wallet"],["TLDR","List **all entries** in the default folder\n\n```kwallet-query kdewallet --list-entries```\n\nList entries in a **specific folder**\n\n```kwallet-query kdewallet --list-entries --folder [folder_name]```\n\nList **all folders** in a wallet\n\n```kwallet-query kdewallet --list-entries --folder \"\"```\n\n**Read a password** entry from a folder\n\n```kwallet-query kdewallet --folder [folder] --read-password [entry]```\n\n**Write a password** to a folder (reads value from stdin)\n\n```kwallet-query kdewallet --folder [folder] --write-password [entry] < [secret.txt]```\n\nDisplay **help**\n\n```kwallet-query --help```"],["SYNOPSIS","**kwallet-query** _wallet_ [_options_]"],["DESCRIPTION","**kwallet-query** is a command-line tool for reading and writing to KDE Wallet. It allows scripts and command-line users to access stored passwords and secrets without the graphical interface."],["PARAMETERS","**wallet**\n> Wallet name to query (typically \"kdewallet\")\n\n**-l, --list-entries**\n> List entries in the specified folder\n\n**-f, --folder FOLDER**\n> Specify folder to query (empty string lists all folders)\n\n**-r, --read-password ENTRY**\n> Read password for specified entry\n\n**-w, --write-password ENTRY**\n> Write password for specified entry\n\n**-h, --help**\n> Display help information"],["CAVEATS","Wallet must be unlocked before querying. Sensitive data will be displayed in plaintext when reading entries. The wallet daemon **kwalletd** must be running, otherwise queries will fail to connect."],["HISTORY","**KWallet** is the credential storage subsystem of **KDE**, introduced in the early 2000s as KDE's equivalent to GNOME Keyring. **kwallet-query** was added to provide a non-interactive, scriptable interface to wallet contents for scripts and headless setups, complementing the graphical **kwalletmanager**."],["INSTALL","```apt: sudo apt install kwallet6```\n\n```apk: sudo apk add kwallet-bin```"],["SEE ALSO","[secret-tool](/man/secret-tool)(1)"]],"e":["secret-tool"],"x":"kwallet-query kdewallet --list-entries"},"librewolf":{"n":"librewolf","t":"privacy-focused Firefox fork with enhanced security defaults","s":[["TAGLINE","privacy-focused Firefox fork with enhanced security defaults"],["TLDR","**Start LibreWolf**\n\n```librewolf```\n\n**Open specific URL**\n\n```librewolf [https://example.com]```\n\n**Open in private browsing mode**\n\n```librewolf --private-window```\n\n**Open URL in a new window**\n\n```librewolf --new-window [url]```\n\n**Open URL in a new tab**\n\n```librewolf --new-tab [url]```\n\n**Use specific profile**\n\n```librewolf -P [profile-name]```\n\n**Start profile manager**\n\n```librewolf --ProfileManager```\n\n**Start in safe mode with add-ons disabled**\n\n```librewolf --safe-mode```"],["SYNOPSIS","**librewolf** [_options_] [_url_...]"],["PARAMETERS","**--private-window**\n> Open private browsing window.\n\n**--new-window** _url_\n> Open URL in new window.\n\n**--new-tab** _url_\n> Open URL in new tab.\n\n**-P** _profile_\n> Use specified profile.\n\n**--ProfileManager**\n> Open profile manager.\n\n**--safe-mode**\n> Start with add-ons disabled.\n\n**--headless**\n> Run without GUI.\n\n**--search** _term_\n> Search using default search engine.\n\n**--screenshot** _file_\n> Take screenshot of a URL (use with --headless)."],["DESCRIPTION","**librewolf** is a privacy-focused Firefox fork with enhanced security defaults. Removes telemetry, includes uBlock Origin, and applies privacy-hardening configurations. Maintains Firefox compatibility while prioritizing user privacy. Since it is based on Firefox, it accepts all standard Firefox command-line options."],["INSTALL","```apk: sudo apk add librewolf```\n\n```nix: nix profile install nixpkgs#librewolf```"],["SEE ALSO","[firefox](/man/firefox)(1)"]],"e":["firefox"],"x":"librewolf"},"license-generator":{"n":"license-generator","t":"Generate LICENSE files from the command line","s":[["TAGLINE","Generate LICENSE files from the command line"],["TLDR","**Generate MIT license**\n\n```license-generator MIT```\n\n**List supported licenses**\n\n```license-generator -l```\n\n**Set author and year**\n\n```license-generator MIT --author [name] --year [2024]```\n\n**Write to stdout**\n\n```license-generator MIT --output /dev/stdout```\n\n**Multiple licenses** (LICENSE-MIT and LICENSE-APACHE)\n\n```license-generator MIT Apache```"],["SYNOPSIS","**license-generator** [*options*] *LICENSE_TYPE*..."],["DESCRIPTION","**license-generator** creates standard open-source **LICENSE** files from templates. Install with **cargo install license-generator**.\n\nSupported types include AGPL, Apache, CC-BY, CC-BY-NC, CC-BY-NC-SA, CC-BY-SA, CC0, GPL, LGPL, MIT, MPL, and Unlicense (see **-l** for the full list of your version)."],["PARAMETERS","*LICENSE_TYPE*\n\n> One or more license keys (for example **MIT**, **Apache**). Multiple types produce **LICENSE-*** files.\n\n**-l**, **--list**\n\n> List available licenses.\n\n**--author** *name*\n\n> Author string (default: git user name and email when available).\n\n**--project** *name*\n\n> Project name (required by some licenses).\n\n**--year** *year*\n\n> Copyright year.\n\n**--output** *path*\n\n> Output path (default **./LICENSE**)."],["CAVEATS","Generated text is a starting point; confirm the license matches your project's intent and any dual-licensing needs. Default author detection depends on git config."],["INSTALL","```nix: nix profile install nixpkgs#license-generator```"],["SEE ALSO","[cargo-license](/man/cargo-license)(1), [licensecheck](/man/licensecheck)(1)"],["RESOURCES","```[Source code](https://github.com/azu/license-generator)```"]],"e":["cargo-license","licensecheck"],"x":"license-generator MIT"},"mapscrn":{"n":"mapscrn","t":"loads output character translation maps","s":[["TAGLINE","loads output character translation maps"],["TLDR","**Load a screen mapping table**\n\n```mapscrn [mapfile]```\n\n**Load from the standard map directory** (resolved under /usr/{share,lib}/kbd/consoletrans)\n\n```mapscrn [trivial]```\n\n**Save the current mapping table** to a file before loading a new one\n\n```mapscrn -o [old-map.bin] [new-map]```\n\n**Reset to the trivial (identity) map**\n\n```mapscrn trivial```\n\n**Apply the map on a specific console**\n\n```mapscrn -C /dev/tty3 [mapfile]```"],["SYNOPSIS","**mapscrn** [_options_] _mapfile_"],["PARAMETERS","_MAPFILE_\n> Screen mapping table to load. Either an absolute path or a name resolved under the kbd consoletrans directory (e.g. **/usr/share/kbd/consoletrans/**). Files may be 256-byte binary, 512-byte (Unicode) binary, or a textual mapping.\n\n**-o** _FILE_, **--output**=_FILE_\n> Save the previous mapping table to _FILE_ before loading the new one.\n\n**-C** _DEV_, **--console**=_DEV_\n> Apply the change to the named console device (default: current TTY).\n\n**-v**, **--verbose**\n> Verbose output.\n\n**-V**, **--version**\n> Print version and exit.\n\n**-h**, **--help**\n> Print usage and exit."],["DESCRIPTION","**mapscrn** loads a screen output character mapping table for the Linux text console. The table tells the kernel how to translate the bytes written to **/dev/tty**_n_ into glyph indices in the currently loaded console font. Together with **loadkeys** (input mapping) and **setfont** (glyphs), it controls the legacy 8-bit console pipeline.\n\nTo return to the identity mapping, load the **trivial** table (e.g. **mapscrn trivial**); there is no dedicated \"clear\" flag."],["CAVEATS","The functionality of **mapscrn** is now built into **setfont** — modern kbd installations keep **mapscrn** only for backwards compatibility. The command applies only to Linux text VTs (not to terminal emulators or framebuffer terminals such as **fbterm**). On UTF-8 consoles it is rarely needed; load a Unicode map (**-u** to **setfont**) instead. Requires sufficient privileges to write to the console."],["HISTORY","**mapscrn** is part of the **kbd** package, the standard Linux console keyboard and character utilities maintained by Alexey Gladkov and others."],["INSTALL","```apt: sudo apt install kbd```\n\n```dnf: sudo dnf install kbd```\n\n```pacman: sudo pacman -S kbd```\n\n```apk: sudo apk add kbd```\n\n```zypper: sudo zypper install kbd```\n\n```nix: nix profile install nixpkgs#kbd```"],["SEE ALSO","[setfont](/man/setfont)(8), [loadkeys](/man/loadkeys)(1), [showconsolefont](/man/showconsolefont)(8)"]],"e":["setfont","loadkeys","showconsolefont"],"x":"mapscrn [mapfile]"},"mdv":{"n":"mdv","t":"Styled terminal Markdown viewer","s":[["TAGLINE","Styled terminal Markdown viewer"],["TLDR","**View a Markdown file** with default theme\n\n```mdv [path/to/file.md]```\n\n**Pipe Markdown** from stdin\n\n```echo \"# Hello\" | mdv -```\n\n**Monitor a file** and redisplay on change\n\n```mdv -m [path/to/file.md]```\n\n**Render with a specific theme** (e.g. 729.8953 for a darker style)\n\n```mdv -t [729.8953] [path/to/file.md]```\n\n**Preview all available themes**\n\n```mdv -t all [path/to/file.md]```\n\n**Render without ANSI color** (plain text)\n\n```mdv -A [path/to/file.md]```\n\n**Fix the column width** to 80\n\n```mdv -c [80] [path/to/file.md]```\n\n**Render as HTML** instead of ANSI\n\n```mdv -H [path/to/file.md]```"],["SYNOPSIS","**mdv** [_options_] _MDFILE_"],["PARAMETERS","**-A**\n> Strip all ANSI escape codes; useful for piping to files or paging tools that don't interpret color.\n\n**-C** _mode_\n> Source code highlighting mode (_all_, _code_, _doc_, _mod_).\n\n**-H**\n> Output the rendered document as HTML.\n\n**-L**\n> Shortcut for **-u i** (inline link style).\n\n**-M** _dir_\n> Monitor _dir_ for Markdown file changes.\n\n**-T** _theme_\n> Color theme for code blocks (accepts _random_ or _all_).\n\n**-X** _lexer_\n> Default Pygments lexer name (default: _python_).\n\n**-b** _n_\n> Tab length (default: _4_).\n\n**-c** _cols_\n> Fix column width to _cols_ characters.\n\n**-f** _from_\n> Start rendering at substring _from_; supports _HEADING:N_ to limit to _N_ lines after a heading.\n\n**-h**\n> Show help.\n\n**-i**\n> Display the selected theme's metadata along with the rendering.\n\n**-l**\n> Experimental light-background mode.\n\n**-m**\n> Monitor the input file and redisplay when it changes.\n\n**-n** _NRS_\n> Enable header numbering (e.g. _-3_, _1-_, _1-5_).\n\n**-t** _theme_\n> Markdown color theme. Accepts a numeric ID (e.g. _729.8953_), _random_, or _all_.\n\n**-u** _style_\n> Link rendering style: _it_ (inline table), _h_ (hide), _i_ (inline).\n\n**-x**\n> Disable automatic lexer guessing for code blocks."],["CONFIGURATION","Defaults are read from **~/.mdv** (YAML) for CLI arguments, and **~/.mdv.py** (Python) is evaluated if present for dynamic overrides.\n\nEnvironment variables:\n\n**MDV_THEME**\n> Default Markdown theme ID.\n\n**MDV_CODE_THEME**\n> Default Pygments theme for code blocks."],["DESCRIPTION","**mdv** is a Python tool that renders Markdown documents directly in a 256-color terminal. It parses headings, lists, tables, block quotes, and fenced code blocks and emits styled ANSI output with syntax highlighting driven by Pygments. A large collection of color themes is bundled and can be previewed with **-t all**.\n\nBeyond one-shot rendering, **mdv** can watch a file or directory with **-m**/**-M** and refresh the view whenever the source changes, which is useful when editing Markdown in a second window. When called as a Python library, the same rendering is available as **mdv.main()** so it can be embedded in other programs (for example, to pretty-print CLI help text)."],["CAVEATS","Requires a **256-color terminal**, and most themes assume a **dark background** — use **-l** for light backgrounds. Rendering uses unicode box characters, so a font that supports them is recommended. The project is written in Python; on recent distributions install it via **pip install mdv** or **brew install mdv**."],["HISTORY","Written by **axiros** and first released in **2015** as _terminal_markdown_viewer_. The project is published under the BSD license and continues to receive sporadic updates; version _1.7.5_ was released in **September 2023**. A lighter-weight sibling, **mdvl**, was later created by the same author for minimal environments."],["INSTALL","```brew: brew install mdv```"],["SEE ALSO","[glow](/man/glow)(1), [bat](/man/bat)(1), [pygmentize](/man/pygmentize)(1), [pandoc](/man/pandoc)(1)"]],"e":["glow","bat","pygmentize","pandoc"],"x":"mdv [path/to/file.md]"},"minikube-start":{"n":"minikube-start","t":"starts a local Kubernetes cluster","s":[["TAGLINE","starts a local Kubernetes cluster"],["TLDR","**Start minikube cluster**\n\n```minikube start```\n\n**Start with specific driver**\n\n```minikube start --driver=[docker]```\n\n**Start with specific Kubernetes version**\n\n```minikube start --kubernetes-version=[v1.28.0]```\n\n**Start with more resources**\n\n```minikube start --cpus=[4] --memory=[8192]```\n\n**Start with specific profile**\n\n```minikube start -p [profile-name]```"],["SYNOPSIS","**minikube** **start** [_options_]"],["PARAMETERS","**--driver** _name_\n> VM driver (docker, virtualbox, kvm2).\n\n**--kubernetes-version** _version_\n> Kubernetes version.\n\n**--cpus** _count_\n> Number of CPUs.\n\n**--memory** _mb_\n> Memory in MB.\n\n**-p**, **--profile** _name_\n> Profile name.\n\n**--nodes** _count_\n> Number of nodes."],["DESCRIPTION","**minikube start** starts a local Kubernetes cluster. Creates a VM or container running Kubernetes. Default driver depends on platform. Supports multiple profiles for different clusters."],["INSTALL","```pacman: sudo pacman -S minikube```\n\n```zypper: sudo zypper install minikube```\n\n```brew: brew install minikube```\n\n```nix: nix profile install nixpkgs#minikube```"],["SEE ALSO","[minikube](/man/minikube)(1), [kubectl](/man/kubectl)(1)"]],"e":["minikube","kubectl"],"x":"minikube start"},"networkctl":{"n":"networkctl","t":"queries and controls the state of network links as seen by systemd-networkd","s":[["TAGLINE","queries and controls the state of network links as seen by systemd-networkd"],["TLDR","List all network **links** with status\n\n```networkctl list```\n\nShow overall **network status**\n\n```networkctl status```\n\n**Bring up** network devices\n\n```networkctl up eth0```\n\n**Bring down** network devices\n\n```networkctl down eth0```\n\n**Renew** DHCP leases\n\n```networkctl renew eth0```\n\n**Reload** network configuration files\n\n```networkctl reload```\n\n**Reconfigure** network interfaces\n\n```networkctl reconfigure eth0```"],["SYNOPSIS","**networkctl** [_OPTIONS_] _COMMAND_ [_LINK_...]"],["DESCRIPTION","**networkctl** queries and controls the state of network links as seen by systemd-networkd. It provides administrative control over network interfaces managed by the systemd networking daemon."],["COMMANDS","**list [PATTERN]**\n> Display existing links with status; supports pattern matching\n\n**status [LINK]**\n> Show detailed link information including type, state, DNS, and IPs\n\n**lldp [LINK]**\n> Display discovered LLDP neighbor information\n\n**label**\n> Show numerical address labels for address selection\n\n**delete LINK**\n> Remove virtual network devices\n\n**up LINK**\n> Activate network devices\n\n**down LINK**\n> Deactivate network devices\n\n**renew LINK**\n> Refresh dynamic configurations (DHCP addresses)\n\n**forcerenew LINK**\n> Trigger DHCP reconfiguration on connected clients\n\n**reconfigure LINK**\n> Reapply network interface settings\n\n**reload**\n> Reload .netdev and .network configuration files\n\n**edit FILE**\n> Modify network configuration files\n\n**cat FILE**\n> Display network configuration file contents"],["PARAMETERS","**-a, --all**\n> Show all links with status\n\n**-s, --stats**\n> Include link statistics\n\n**-l, --full**\n> Disable output truncation\n\n**-n, --lines=NUM**\n> Control journal output lines (default: 10)\n\n**--json=MODE**\n> Format output as JSON (short/pretty/off)\n\n**--drop-in=NAME**\n> Edit drop-in configuration files\n\n**--no-reload**\n> Skip service reload after editing"],["CAVEATS","This tool manages interfaces through systemd-networkd, not NetworkManager. Configuration files are stored in /etc/systemd/network/. Changes made with **reload** require **reconfigure** to take effect on running interfaces."],["HISTORY","Part of **systemd**, networkctl was introduced to provide command-line management of systemd-networkd network configuration, offering an alternative to NetworkManager for server and embedded systems."],["INSTALL","```apt: sudo apt install systemd```\n\n```dnf: sudo dnf install systemd```\n\n```pacman: sudo pacman -S systemd```\n\n```zypper: sudo zypper install systemd```\n\n```brew: brew install systemd```\n\n```nix: nix profile install nixpkgs#systemd```"],["SEE ALSO","[ip](/man/ip)(8)"]],"e":["ip"],"x":"networkctl list"},"nix-build.2":{"n":"nix-build.2","t":"builds Nix derivations","s":[["TAGLINE","builds Nix derivations"],["TLDR","**Build default.nix**\n\n```nix-build```\n\n**Build specific attribute**\n\n```nix-build -A [package]```\n\n**Build from nixpkgs**\n\n```nix-build '<nixpkgs>' -A [hello]```\n\n**Build without result link**\n\n```nix-build --no-out-link```"],["SYNOPSIS","**nix-build** [_options_] [_path_]"],["PARAMETERS","_PATH_\n> Nix expression path.\n\n**-A** _ATTR_\n> Attribute to build.\n\n**--no-out-link**\n> Skip result symlink.\n\n**--help**\n> Display help information."],["DESCRIPTION","**nix-build** builds Nix derivations. This is an alternate documentation version.\n\nThe tool evaluates Nix expressions. Produces store paths and result links."],["CAVEATS","Part of Nix toolset. Legacy interface. Use nix build for flakes."],["HISTORY","nix-build is a core **Nix** command for building derivations from expressions."],["INSTALL","```apt: sudo apt install nix-bin```\n\n```dnf: sudo dnf install nix```\n\n```pacman: sudo pacman -S nix```\n\n```apk: sudo apk add nix```\n\n```zypper: sudo zypper install nix```\n\n```nix: nix profile install nixpkgs#nix```"],["SEE ALSO","[nix-build](/man/nix-build)(1), [nix](/man/nix)(1), [nix-shell](/man/nix-shell)(1)"]],"e":["nix-build","nix","nix-shell"],"x":"nix-build"},"nmcli-radio":{"n":"nmcli-radio","t":"controls the software radio switches for wireless interfaces managed","s":[["TAGLINE","controls the software radio switches for wireless interfaces managed"],["TLDR","Show **status of Wi-Fi**\n\n```nmcli radio wifi```\n\nTurn **Wi-Fi on or off**\n\n```nmcli radio wifi [on|off]```\n\nShow **status of WWAN** (mobile broadband)\n\n```nmcli radio wwan```\n\nTurn **WWAN on or off**\n\n```nmcli radio wwan [on|off]```\n\nShow **status of both** switches\n\n```nmcli radio```\n\nTurn **both switches** on or off\n\n```nmcli radio all [on|off]```"],["SYNOPSIS","**nmcli radio** [_switch_] [_on_|_off_]"],["PARAMETERS","**wifi, w**\n> Control or show Wi-Fi radio status\n\n**wwan, ww**\n> Control or show WWAN (mobile broadband) radio status\n\n**all, a**\n> Control all radio switches simultaneously\n\n**on**\n> Enable the specified radio\n\n**off**\n> Disable the specified radio"],["DESCRIPTION","**nmcli radio** controls the software radio switches for wireless interfaces managed by NetworkManager. This is equivalent to airplane mode functionality, allowing Wi-Fi and mobile broadband radios to be enabled or disabled independently.\n\nDisabling a radio switch turns off the corresponding hardware transmitter, preventing any wireless communication. This is different from just disconnecting—the radio itself is turned off.\n\nWithout an on/off argument, the command displays the current state of the specified radio(s)."],["CAVEATS","Software radio control depends on hardware and driver support. Some laptops have physical radio switches that override software control. Enabling the radio doesn't automatically connect to networks; it just makes the hardware available for connections."],["SEE ALSO","[nmcli](/man/nmcli)(1), [nmcli-networking](/man/nmcli-networking)(1), [rfkill](/man/rfkill)(8), [NetworkManager](/man/NetworkManager)(8)"]],"e":["nmcli","nmcli-networking","rfkill","NetworkManager"],"x":"nmcli radio wifi"},"npm-check":{"n":"npm-check","t":"checks for outdated and unused packages","s":[["TAGLINE","checks for outdated and unused packages"],["TLDR","**Check for updates**\n\n```npm-check```\n\n**Interactive update**\n\n```npm-check -u```\n\n**Check globally installed**\n\n```npm-check -g```\n\n**Skip unused check**\n\n```npm-check --skip-unused```\n\n**Update all**\n\n```npm-check -y```\n\n**Check production only**\n\n```npm-check --production```"],["SYNOPSIS","**npm-check** [_options_]"],["PARAMETERS","**-u**\n> Interactive update mode.\n\n**-g**\n> Check global packages.\n\n**--skip-unused**\n> Skip unused package check.\n\n**-y**\n> Update all without prompt.\n\n**--production**\n> Production deps only.\n\n**--help**\n> Display help information."],["DESCRIPTION","**npm-check** checks for outdated and unused packages. Provides interactive updates.\n\nThe tool shows update availability. Identifies unused dependencies."],["CAVEATS","Third-party tool. Install with npm install -g. Interactive TUI."],["HISTORY","npm-check was created to provide **interactive package updates** and unused dependency detection."],["INSTALL","```nix: nix profile install nixpkgs#npm-check```"],["SEE ALSO","[npm](/man/npm)(1), [npm-outdated](/man/npm-outdated)(1), [ncu](/man/ncu)(1)"]],"e":["npm","npm-outdated","ncu"],"x":"npm-check"},"npm-sbom":{"n":"npm-sbom","t":"Generate a Software Bill of Materials for the project","s":[["TAGLINE","Generate a Software Bill of Materials for the project"],["TLDR","**Generate SBOM** in default format\n\n```npm sbom```\n\n**Generate in SPDX** format\n\n```npm sbom --sbom-format spdx```\n\n**Generate in CycloneDX** format\n\n```npm sbom --sbom-format cyclonedx```\n\n**Output to file**\n\n```npm sbom --sbom-format cyclonedx > [sbom.json]```\n\n**Omit dev dependencies**\n\n```npm sbom --omit dev```\n\n**Use only package-lock.json** (ignore node_modules)\n\n```npm sbom --package-lock-only```"],["SYNOPSIS","**npm** **sbom** [_options_]"],["PARAMETERS","**--sbom-format** _FORMAT_\n> Output format: spdx or cyclonedx (required).\n\n**--sbom-type** _TYPE_\n> Package type: library (default), application, or framework.\n\n**--omit** _TYPE_\n> Omit dependency type (dev, optional, peer).\n\n**--package-lock-only**\n> Use package-lock.json only, ignoring node_modules.\n\n**--workspace** _name_\n> Run in the context of a specific workspace.\n\n**--workspaces**\n> Run for all configured workspaces."],["DESCRIPTION","**npm sbom** generates a Software Bill of Materials (SBOM) listing all dependencies of the current project. It supports the **SPDX** and **CycloneDX** formats, which are industry standards for documenting software supply chain components. The output includes package names, versions, licenses, and dependency relationships."],["CAVEATS","Requires npm 9.5.0 or later. A package-lock.json or node_modules must exist. The --sbom-format option is required for usable output."],["INSTALL","```pacman: sudo pacman -S npm```\n\n```apk: sudo apk add npm```\n\n```brew: brew install npm```"],["SEE ALSO","[npm](/man/npm)(1), [npm-audit](/man/npm-audit)(1), [npm-ls](/man/npm-ls)(1)"]],"e":["npm","npm-audit","npm-ls"],"x":"npm sbom"},"ophcrack":{"n":"ophcrack","t":"windows password cracker that uses rainbow tables to recover passwords from LM","s":[["TAGLINE","windows password cracker that uses rainbow tables to recover passwords from LM"],["TLDR","**Crack passwords from a pwdump file** using rainbow tables\n\n```ophcrack -g -d [path/to/tables] -f [hashes.txt]```\n\n**Crack with specific tables** and multiple threads\n\n```ophcrack -g -d [path/to/tables] -t [xp_free_fast] -f [hashes.txt] -n [4]```\n\n**Crack and save results** to a file\n\n```ophcrack -g -d [path/to/tables] -f [hashes.txt] -o [cracked.txt]```\n\n**Enable bruteforce** alongside rainbow tables\n\n```ophcrack -g -B -d [path/to/tables] -f [hashes.txt]```\n\n**Run in quiet mode** with logging\n\n```ophcrack -g -q -d [path/to/tables] -f [hashes.txt] -l [log.txt]```\n\n**Launch GUI mode**\n\n```ophcrack```"],["SYNOPSIS","**ophcrack** [_options_]\n\n**ophcrack** -g -d _tables_dir_ -f _hash_file_"],["PARAMETERS","**-g**\n> Disable GUI, run in command-line mode\n\n**-d** _DIR_\n> Specify rainbow tables base directory\n\n**-t** _TABLES_\n> Specify tables to use (table_name,start,end:...)\n\n**-f** _FILE_\n> Load hashes from pwdump or session file\n\n**-o** _FILE_\n> Write output in pwdump format\n\n**-l** _FILE_\n> Log all output to file\n\n**-n** _NUM_\n> Number of threads to use\n\n**-a**\n> Disable audit mode (default)\n\n**-A**\n> Enable audit mode\n\n**-b**\n> Disable bruteforce\n\n**-B**\n> Enable bruteforce (default)\n\n**-c** _FILE_\n> Specify configuration file\n\n**-e**\n> Do not display empty passwords\n\n**-i**\n> Hide usernames in output\n\n**-I**\n> Show usernames (default)\n\n**-p** _NUM_\n> Preload level (0=none, 1=index, 2=index+end, 3=all)\n\n**-q**\n> Quiet mode\n\n**-r**\n> Start cracking immediately (GUI only)\n\n**-s**\n> Disable session auto-saving\n\n**-S** _FILE_\n> Session file for auto-saving progress\n\n**-u**\n> Display statistics\n\n**-D**\n> Display debugging information\n\n**-h**\n> Show help"],["DESCRIPTION","**ophcrack** is a Windows password cracker that uses rainbow tables to recover passwords from LM and NTLM hashes. It implements a time-memory trade-off technique, pre-computing hash chains that dramatically speed up the cracking process.\n\nThe tool can crack Windows NT, 2000, XP, Vista, 7, 8, and 10 passwords. Free rainbow tables are available for common password patterns, with paid tables offering higher success rates. It can import hashes from pwdump files, SAM/SYSTEM registry files, or by dumping directly from local/remote Windows systems.\n\nOphcrack provides both GUI and command-line interfaces. The GUI includes real-time graphs analyzing password strength distribution. A LiveCD version allows booting directly on a Windows machine to extract and crack passwords without installing software."],["CAVEATS","Rainbow tables require significant disk space (hundreds of MB to tens of GB). LM hashes are limited to passwords up to 14 characters and are case-insensitive. NTLM hashes are harder to crack. Success depends on having appropriate tables for the password character set. Only use on systems you own or have authorization to test."],["HISTORY","Ophcrack was developed by **Philippe Oechslin** at EPFL (Swiss Federal Institute of Technology) and first released in **2004**. Oechslin invented the rainbow table technique as an improvement over earlier time-memory trade-off methods. The tool demonstrated the weakness of LM hash storage, contributing to Microsoft's decision to deprecate LM hashes in Windows Vista and later versions."],["INSTALL","```apt: sudo apt install ophcrack```\n\n```dnf: sudo dnf install ophcrack```\n\n```brew: brew install ophcrack```\n\n```nix: nix profile install nixpkgs#ophcrack```"],["SEE ALSO","[hashcat](/man/hashcat)(1), [john](/man/john)(1), [pwdump](/man/pwdump)(1), [rcrack](/man/rcrack)(1)"]],"e":["hashcat","john","pwdump","rcrack"],"x":"ophcrack -g -d [path/to/tables] -f [hashes.txt]"},"otti":{"n":"otti","t":"TUI for generating one-time passwords","s":[["TAGLINE","TUI for generating one-time passwords"],["TLDR","**Launch the OTP generator**\n\n```otti```"],["SYNOPSIS","**otti** [_options_]"],["DESCRIPTION","**otti** is a terminal user interface for generating one-time passwords (OTPs). It provides a quick and reliable way to generate TOTP codes for two-factor authentication directly from the terminal, useful for users who frequently authenticate across multiple platforms."],["CAVEATS","OTP secrets must be manually configured. Time synchronization is required for accurate TOTP generation."],["HISTORY","**otti** was created by **dnaka91** and is written in **Rust**. It supports macOS, Linux, BSD, and Windows."],["INSTALL","```aur: yay -S otti```"],["SEE ALSO","[pass-otp](/man/pass-otp)(1), [oathtool](/man/oathtool)(1)"]],"e":["pass-otp","oathtool"],"x":"otti"},"perlivp":{"n":"perlivp","t":"verifies Perl installation","s":[["TAGLINE","verifies Perl installation"],["TLDR","**Run installation verification**\n\n```perlivp```\n\n**Run with verbose output**\n\n```perlivp -v```\n\n**Print a description of each test before running it**\n\n```perlivp -p```\n\n**Run with both preface and verbose output**\n\n```perlivp -p -v```"],["SYNOPSIS","**perlivp** [_options_]"],["PARAMETERS","**-p**\n> Print a description of each test before performing it.\n\n**-v**\n> Give more detailed information about each test after it has been performed.\n\n**-h**\n> Print a brief help message."],["DESCRIPTION","**perlivp** (Perl Installation Verification Procedure) tests whether Perl and its libraries have been correctly installed. It is set up at Perl source code build time and should be run after `make install`.\n\nA successful verification produces output like `ok 1`, `ok 2`, etc."],["CAVEATS","Part of Perl. Basic tests only. Run after installation."],["HISTORY","perlivp is part of **Perl core** for installation verification."],["INSTALL","```apt: sudo apt install perl```\n\n```dnf: sudo dnf install perl```\n\n```pacman: sudo pacman -S perl```\n\n```apk: sudo apk add perl-dev```\n\n```zypper: sudo zypper install perl```\n\n```brew: brew install perl```\n\n```nix: nix profile install nixpkgs#perl```"],["SEE ALSO","[perl](/man/perl)(1), [perldiag](/man/perldiag)(1)"]],"e":["perl","perldiag"],"x":"perlivp"},"pio-remote":{"n":"pio-remote","t":"enables remote development with PlatformIO","s":[["TAGLINE","enables remote development with PlatformIO"],["TLDR","**Start remote agent**\n\n```pio remote agent start```\n\n**List remote devices**\n\n```pio remote device list```\n\n**Build and upload remotely**\n\n```pio remote run --target upload```\n\n**Open remote serial monitor**\n\n```pio remote device monitor```"],["SYNOPSIS","**pio remote** _command_ [_options_]"],["PARAMETERS","**agent** _action_\n> Manage remote agent (start, list).\n\n**device** _action_\n> Remote device operations.\n\n**run**\n> Run remote build/upload.\n\n**test**\n> Run remote tests.\n\n**-a**, **--agent** _name_\n> Target specific agent."],["DESCRIPTION","**pio remote** enables remote development with PlatformIO. Run builds and uploads on remote machines with connected hardware. Requires PlatformIO account and running remote agent."],["INSTALL","```aur: yay -S pio```"],["SEE ALSO","[pio-run](/man/pio-run)(1), [pio-device](/man/pio-device)(1)"]],"e":["pio-run","pio-device"],"x":"pio remote agent start"},"pkcs7.1s":{"n":"pkcs7.1s","t":"Process PKCS#7 certificate chain structures","s":[["TAGLINE","Process PKCS#7 certificate chain structures"],["TLDR","**View PKCS#7 structure**\n\n```openssl pkcs7 -in [file.p7b] -print_certs```\n\n**Convert to PEM**\n\n```openssl pkcs7 -in [file.p7b] -out [certs.pem] -print_certs```\n\n**View structure info**\n\n```openssl pkcs7 -in [file.p7b] -text -noout```\n\n**Convert DER to PEM**\n\n```openssl pkcs7 -in [file.der] -inform DER -out [file.pem]```"],["SYNOPSIS","**openssl** **pkcs7** [_options_]"],["PARAMETERS","**-in** _FILE_\n> Input file.\n\n**-out** _FILE_\n> Output file.\n\n**-print_certs**\n> Print certificates.\n\n**-text**\n> Print text form.\n\n**-inform** _FORMAT_\n> Input format (PEM, DER).\n\n**-noout**\n> No output."],["DESCRIPTION","**openssl pkcs7** processes PKCS#7 structures (.p7b files), which contain certificate chains without private keys. This format is commonly used to distribute CA certificate bundles and intermediate certificates.\n\nThe **-print_certs** option extracts individual PEM certificates from the chain. Format conversion between PEM and DER encodings is supported with **-inform** and **-outform**. The **-text** option displays the structure in human-readable form."],["CAVEATS","Part of OpenSSL. Certificate chains only. No private keys."],["HISTORY","PKCS#7 support in OpenSSL handles **certificate chain** structures."],["SEE ALSO","[openssl](/man/openssl)(1), [openssl-pkcs12](/man/openssl-pkcs12)(1)"]],"e":["openssl","openssl-pkcs12"],"x":"openssl pkcs7 -in [file.p7b] -print_certs"},"pofilter":{"n":"pofilter","t":"Filter PO files for translation errors","s":[["TAGLINE","Filter PO files for translation errors"],["TLDR","**Check for common errors**\n\n```pofilter -t [check] [input.po] [output.po]```\n\n**List available checks**\n\n```pofilter --list```\n\n**Check accelerators**\n\n```pofilter -t accelerators [input.po] [output.po]```\n\n**Check printf format**\n\n```pofilter -t printf [input.po] [output.po]```\n\n**Multiple checks**\n\n```pofilter -t escapes -t variables [input.po] [output.po]```"],["SYNOPSIS","**pofilter** [_options_] _input_ _output_"],["PARAMETERS","**-t**, **--test** _test_\n> Test to run.\n\n**-l**, **--language** _lang_\n> Target language.\n\n**--excludefilter** _filter_\n> Exclude specific test.\n\n**--list**\n> List available tests.\n\n**-i**, **--input** _path_\n> Input file or directory.\n\n**-o**, **--output** _path_\n> Output file or directory."],["DESCRIPTION","**pofilter** filters PO files to find translation errors. It checks for common issues like missing variables, inconsistent punctuation, and encoding problems.\n\nPart of the Translate Toolkit."],["EXAMPLES","```bash"],["List all available checks","pofilter --list"],["Check for variables","pofilter -t variables translations.po errors.po"],["Check escapes and printf","pofilter -t escapes -t printf input.po output.po"],["Exclude specific check","pofilter --excludefilter=blank input.po output.po"],["Process directory","pofilter -t all locale/ errors/\n```"],["COMMON CHECKS","```\nvariables   - Check variable placeholders\nprintf      - Check printf format strings\naccelerators - Check keyboard accelerators\nescapes     - Check escape sequences\nnewlines    - Check newline consistency\n```"],["CAVEATS","Output contains only problematic entries. Part of Translate Toolkit."],["HISTORY","pofilter is part of the **Translate Toolkit** developed by **Translate House** for translation quality assurance."],["SEE ALSO","[msgfmt](/man/msgfmt)(1), [pocount](/man/pocount)(1)"]],"e":["msgfmt","pocount"],"x":"pofilter -t [check] [input.po] [output.po]"},"pppoe-relay":{"n":"pppoe-relay","t":"Relay PPPoE traffic between interfaces","s":[["TAGLINE","Relay PPPoE traffic between interfaces"],["TLDR","**Start PPPoE relay**\n\n```pppoe-relay -S [eth0] -C [eth1]```\n\n**Relay with multiple interfaces**\n\n```pppoe-relay -S [eth0] -C [eth1] -C [eth2]```\n\n**Run in foreground**\n\n```pppoe-relay -n -S [eth0] -C [eth1]```"],["SYNOPSIS","**pppoe-relay** [_options_]"],["PARAMETERS","**-S** _INTERFACE_\n> Server-side interface.\n\n**-C** _INTERFACE_\n> Client-side interface.\n\n**-n**\n> Don't fork to background.\n\n**-i**\n> Ignore PADI if no AC."],["DESCRIPTION","**pppoe-relay** forwards PPPoE discovery and session packets between network interfaces, acting as a relay agent between PPPoE clients and access concentrators on different network segments. This allows clients on one subnet to connect to a PPPoE server on another.\n\nThe relay listens on both server-side and client-side interfaces, transparently forwarding PPPoE control and data traffic. Multiple client interfaces can be specified to serve several network segments through a single access concentrator."],["CAVEATS","Requires multiple interfaces. Specialized use case."],["HISTORY","pppoe-relay provides **PPPoE packet relaying** between network segments."],["INSTALL","```apt: sudo apt install pppoe```\n\n```dnf: sudo dnf install rp-pppoe```\n\n```pacman: sudo pacman -S rp-pppoe```\n\n```apk: sudo apk add rp-pppoe```\n\n```zypper: sudo zypper install rp-pppoe```\n\n```nix: nix profile install nixpkgs#rp-pppoe```"],["SEE ALSO","[pppoe](/man/pppoe)(8), [pppoe-server](/man/pppoe-server)(8)"]],"e":["pppoe","pppoe-server"],"x":"pppoe-relay -S [eth0] -C [eth1]"},"ptargrep":{"n":"ptargrep","t":"Apply pattern matching to the contents of files in a tar archive","s":[["TAGLINE","Apply pattern matching to the contents of files in a tar archive"],["TLDR","**Search for a pattern and extract matching files**\n\n```ptargrep \"[pattern]\" [archive.tar.gz]```\n\n**List matching file paths without extracting**\n\n```ptargrep --list-only \"[pattern]\" [archive.tar]```\n\n**Case-insensitive search**\n\n```ptargrep -i \"[pattern]\" [archive.tar.gz]```\n\n**Extract matching files using basename only (ignore directory paths)**\n\n```ptargrep --basename \"[pattern]\" [archive.tar]```\n\n**Search multiple archives with verbose output**\n\n```ptargrep -v \"[pattern]\" [archive1.tar.gz] [archive2.tar.bz2]```"],["SYNOPSIS","**ptargrep** [_options_] _pattern_ _tar-file_ ..."],["PARAMETERS","**-b**, **--basename**\n> When extracting matching files, ignore the directory path from the archive and write to the current directory using only the basename. Files with identical names will overwrite each other.\n\n**-i**, **--ignore-case**\n> Case-insensitive pattern matching.\n\n**-l**, **--list-only**\n> Print the pathname of each matching file to stdout instead of extracting.\n\n**-v**, **--verbose**\n> Log debugging info to stderr.\n\n**-?**, **--help**\n> Display help documentation."],["DESCRIPTION","**ptargrep** allows you to apply pattern matching to the contents of files in a tar archive. The pattern is used as a Perl regular expression.\n\nThe default behavior is to extract each matching file from the archive. Use **--list-only** to only print pathnames of matching files instead.\n\nCompressed archives (gzip, bzip2, xz) are handled automatically based on the file extension.\n\nMultiple tar archive filenames can be specified and they will each be processed in turn."],["CAVEATS","Part of the Perl distribution (Archive::Tar module). Large archives may be slow to process. Memory usage can be high with large files since file contents are read into memory."],["HISTORY","**ptargrep** is distributed with Perl's **Archive::Tar** module. It provides grep-like functionality for archived content."],["INSTALL","```apt: sudo apt install perl```\n\n```dnf: sudo dnf install perl```\n\n```pacman: sudo pacman -S perl```\n\n```apk: sudo apk add perl-utils```\n\n```zypper: sudo zypper install perl```\n\n```brew: brew install perl```\n\n```nix: nix profile install nixpkgs#perl```"],["SEE ALSO","[zgrep](/man/zgrep)(1), [tar](/man/tar)(1), [grep](/man/grep)(1), [perl](/man/perl)(1), [zipgrep](/man/zipgrep)(1)"]],"e":["zgrep","tar","grep","perl","zipgrep"],"x":"ptargrep \"[pattern]\" [archive.tar.gz]"},"pueue-help":{"n":"pueue-help","t":"Display pueue command help information","s":[["TAGLINE","Display pueue command help information"],["TLDR","**Show general help**\n\n```pueue help```\n\n**Show help for command**\n\n```pueue help [command]```\n\n**Show add command help**\n\n```pueue help add```"],["SYNOPSIS","**pueue help** [_command_]"],["PARAMETERS","_COMMAND_\n> Command to get help for."],["DESCRIPTION","**pueue help** displays usage information for pueue commands. Called without arguments, it lists all available subcommands and global options. With a specific command name, it shows detailed help for that subcommand including all its flags and parameters."],["CAVEATS","Also available via --help flag."],["HISTORY","pueue help provides **built-in documentation** for pueue commands."],["INSTALL","```pacman: sudo pacman -S pueue```\n\n```apk: sudo apk add pueue```\n\n```zypper: sudo zypper install pueue```\n\n```brew: brew install pueue```\n\n```nix: nix profile install nixpkgs#pueue```"],["SEE ALSO","[pueue](/man/pueue)(1)"]],"e":["pueue"],"x":"pueue help"},"pulumi-policy":{"n":"pulumi-policy","t":"Manage infrastructure policy packs","s":[["TAGLINE","Manage infrastructure policy packs"],["TLDR","**List policy packs**\n\n```pulumi policy ls```\n\n**Create new policy pack**\n\n```pulumi policy new [template]```\n\n**Publish policy pack**\n\n```pulumi policy publish [org]```\n\n**Enable policy pack**\n\n```pulumi policy enable [org/pack] [version]```\n\n**Disable policy pack**\n\n```pulumi policy disable [org/pack]```\n\n**Remove policy pack**\n\n```pulumi policy rm [org/pack] [version]```\n\n**Validate policy pack configuration**\n\n```pulumi policy validate-config [org/pack] [version] --config [policy-config.json]```"],["SYNOPSIS","**pulumi** **policy** _command_ [_options_]"],["PARAMETERS","**ls**\n> List policy packs.\n\n**new** _template_\n> Create policy pack.\n\n**publish** _org_\n> Publish policy pack.\n\n**enable** _pack_ _version_\n> Enable policy pack.\n\n**disable** _pack_\n> Disable policy pack.\n\n**rm** _pack_ _version_\n> Remove a policy pack from the organization. Must be disabled from all Policy Groups first.\n\n**group**\n> Manage policy groups.\n\n**validate-config** _pack_ _version_\n> Validate a policy pack configuration against the configuration schema."],["DESCRIPTION","**pulumi policy** manages Policy as Code (CrossGuard). Policies enforce compliance rules on infrastructure. Create, publish, and enable policy packs to validate deployments."],["INSTALL","```pacman: sudo pacman -S pulumi```\n\n```apk: sudo apk add pulumi```\n\n```brew: brew install pulumi```\n\n```nix: nix profile install nixpkgs#pulumi```"],["SEE ALSO","[pulumi](/man/pulumi)(1), [pulumi-up](/man/pulumi-up)(1), [pulumi-preview](/man/pulumi-preview)(1), [pulumi-stack](/man/pulumi-stack)(1)"]],"e":["pulumi","pulumi-up","pulumi-preview","pulumi-stack"],"x":"pulumi policy ls"},"pulumi":{"n":"pulumi","t":"Infrastructure as code with general-purpose languages","s":[["TAGLINE","Infrastructure as code with general-purpose languages"],["TLDR","**Create new project**\n\n```pulumi new [template]```\n\n**Preview changes**\n\n```pulumi preview```\n\n**Deploy stack**\n\n```pulumi up```\n\n**Destroy resources**\n\n```pulumi destroy```\n\n**List stacks**\n\n```pulumi stack ls```\n\n**Select stack**\n\n```pulumi stack select [stack-name]```\n\n**Show stack outputs**\n\n```pulumi stack output```"],["SYNOPSIS","**pulumi** [_options_] _command_ [_args_]"],["DESCRIPTION","**pulumi** is an infrastructure as code tool supporting multiple programming languages. It manages cloud resources using TypeScript, Python, Go, C#, Java, and YAML.\n\nThe tool provisions resources across AWS, Azure, GCP, Kubernetes, and many other providers using familiar programming constructs."],["PARAMETERS","**new**\n> Create new project.\n\n**up**\n> Deploy changes.\n\n**preview**\n> Preview changes.\n\n**destroy**\n> Delete resources.\n\n**stack**\n> Manage stacks.\n\n**config**\n> Manage configuration.\n\n**logs**\n> View logs.\n\n**-s** _stack_\n> Target stack.\n\n**-y**, **--yes**\n> Skip confirmations.\n\n**--diff**\n> Show detailed diff."],["CONFIGURATION","**Pulumi.yaml**\n> Project definition file specifying the project name, runtime language, and description.\n\n**Pulumi.<stack>.yaml**\n> Per-stack configuration file containing config values and encrypted secrets.\n\n**PULUMI_ACCESS_TOKEN**\n> Environment variable for authenticating with the Pulumi Cloud backend without interactive login."],["CAVEATS","Requires Pulumi account or self-hosted backend. State management important. Provider plugins downloaded on first use. Learning curve for programming approach."],["HISTORY","**Pulumi** was founded by **Joe Duffy** and **Eric Rudder** in **2017**. It brought general-purpose programming languages to infrastructure management, differentiating from domain-specific languages like Terraform's HCL."],["INSTALL","```pacman: sudo pacman -S pulumi```\n\n```apk: sudo apk add pulumi```\n\n```brew: brew install pulumi```\n\n```nix: nix profile install nixpkgs#pulumi```"],["SEE ALSO","[terraform](/man/terraform)(1), [aws](/man/aws)(1), [kubectl](/man/kubectl)(1), [cdktf](/man/cdktf)(1)"]],"e":["terraform","aws","kubectl","cdktf"],"x":"pulumi new [template]"},"pyenv-virtualenv":{"n":"pyenv-virtualenv","t":"Manage Python virtual environments with pyenv","s":[["TAGLINE","Manage Python virtual environments with pyenv"],["TLDR","**Create virtualenv**\n\n```pyenv virtualenv [3.11.0] [myproject]```\n\n**List virtualenvs**\n\n```pyenv virtualenvs```\n\n**Activate virtualenv**\n\n```pyenv activate [myproject]```\n\n**Deactivate virtualenv**\n\n```pyenv deactivate```\n\n**Delete virtualenv**\n\n```pyenv virtualenv-delete [myproject]```\n\n**Set local virtualenv**\n\n```pyenv local [myproject]```\n\n**Create with current version**\n\n```pyenv virtualenv [myproject]```"],["SYNOPSIS","**pyenv virtualenv** [_version_] _name_\n\n**pyenv activate** _name_\n\n**pyenv deactivate**"],["PARAMETERS","**virtualenv** _VERSION_ _NAME_\n> Create new virtualenv.\n\n**virtualenvs**\n> List virtualenvs.\n\n**activate** _NAME_\n> Activate virtualenv.\n\n**deactivate**\n> Deactivate current.\n\n**virtualenv-delete** _NAME_\n> Delete virtualenv."],["DESCRIPTION","**pyenv-virtualenv** is a pyenv plugin that manages Python virtual environments alongside pyenv's version management. It creates isolated environments tied to specific Python versions, each with its own independent set of installed packages, and integrates them seamlessly into pyenv's version-switching workflow.\n\nVirtual environments created with this plugin appear as regular pyenv versions and can be activated using **pyenv activate** or auto-activated by setting a local version with **pyenv local**. When a **.python-version** file references a virtualenv name, entering that directory automatically activates the environment and leaving it deactivates, providing a frictionless per-project workflow."],["CAVEATS","Requires pyenv installed. May need shell integration configured. Slower than venv in some cases."],["HISTORY","**pyenv-virtualenv** was created as a pyenv plugin by the pyenv community. It brings virtualenv management into the pyenv workflow."],["INSTALL","```brew: brew install pyenv-virtualenv```"],["SEE ALSO","[pyenv](/man/pyenv)(1), [virtualenv](/man/virtualenv)(1), [python](/man/python)(1), [pip](/man/pip)(1)"]],"e":["pyenv","virtualenv","python","pip"],"x":"pyenv virtualenv [3.11.0] [myproject]"},"qm-rescan":{"n":"qm-rescan","t":"Rescan Proxmox VM storage volumes","s":[["TAGLINE","Rescan Proxmox VM storage volumes"],["TLDR","**Rescan all storages** and update disk sizes for all VMs\n\n```qm rescan```\n\n**Rescan storage for a specific VM**\n\n```qm rescan --vmid [100]```\n\n**Perform a dry run** without writing changes\n\n```qm rescan --dryrun 1```\n\nThis command is an alias of **qm disk rescan**\n\nView documentation for the original command\n\n```tldr qm disk```"],["SYNOPSIS","**qm rescan** [_options_]"],["PARAMETERS","**--dryrun** _boolean_\n> Execute a test run without writing changes to VM configurations (default: **0**).\n\n**--vmid** _integer_\n> The unique ID of the VM (100-999999999). If omitted, all VMs are rescanned."],["DESCRIPTION","**qm rescan** is an alias for **qm disk rescan** that rescans all storages and updates disk sizes and unused disk images for virtual machines. When run without **--vmid**, it rescans storage for all VMs on the node."],["INSTALL","```dnf: sudo dnf install qm```"],["SEE ALSO","[qm](/man/qm)(1), [qm-disk](/man/qm-disk)(1), [pvesm](/man/pvesm)(1)"]],"e":["qm","qm-disk","pvesm"],"x":"qm rescan"},"rabbitmqctl-users":{"n":"rabbitmqctl-users","t":"Manage RabbitMQ users and permissions","s":[["TAGLINE","Manage RabbitMQ users and permissions"],["TLDR","**List users**\n\n```rabbitmqctl list_users```\n\n**Add user**\n\n```rabbitmqctl add_user [username] [password]```\n\n**Delete user**\n\n```rabbitmqctl delete_user [username]```\n\n**Change user password**\n\n```rabbitmqctl change_password [username] [new_password]```\n\n**Set user tags**\n\n```rabbitmqctl set_user_tags [username] [administrator]```\n\n**Set permissions** for a user on a vhost\n\n```rabbitmqctl set_permissions -p [vhost] [username] \".*\" \".*\" \".*\"```\n\n**List permissions** of a specific user across all vhosts\n\n```rabbitmqctl list_user_permissions [username]```\n\n**Verify** a user's credentials\n\n```rabbitmqctl authenticate_user [username] [password]```"],["SYNOPSIS","**rabbitmqctl** _user_command_ [_options_]"],["PARAMETERS","**list_users**\n> List all users.\n\n**add_user** _user_ _pass_\n> Create user.\n\n**delete_user** _user_\n> Remove user.\n\n**change_password** _user_ _pass_\n> Change password.\n\n**clear_password** _user_\n> Remove password for a user, preventing password-based login.\n\n**authenticate_user** _user_ _pass_\n> Verify whether credentials match a user's stored authentication.\n\n**set_user_tags** _user_ _tags ..._\n> Set role tags (administrator, monitoring, management, policymaker). Replaces all existing tags.\n\n**set_permissions** [**-p** _vhost_] _user_ _conf_ _write_ _read_\n> Set vhost permissions using regex patterns for configure, write, and read operations.\n\n**clear_permissions** [**-p** _vhost_] _user_\n> Revoke all permissions for a user on a vhost.\n\n**list_permissions** [**-p** _vhost_]\n> List all users with access to a vhost and their permissions.\n\n**list_user_permissions** _user_\n> List all vhosts a user has access to and their permissions.\n\n**-p** _vhost_\n> Target virtual host (default: \"/\")."],["DESCRIPTION","**rabbitmqctl** user commands manage RabbitMQ users and permissions. Create users, set passwords, assign roles, and configure vhost access. Part of RabbitMQ server management. These commands only manage users in the internal RabbitMQ database; users from alternative authentication backends (such as LDAP) cannot be managed with these commands."],["INSTALL","```dnf: sudo dnf install rabbitmq-server```\n\n```apk: sudo apk add rabbitmq-server```\n\n```zypper: sudo zypper install rabbitmq-server```\n\n```nix: nix profile install nixpkgs#rabbitmq-server```"],["SEE ALSO","[rabbitmqctl](/man/rabbitmqctl)(1), [rabbitmqctl-vhosts](/man/rabbitmqctl-vhosts)(1)"]],"e":["rabbitmqctl","rabbitmqctl-vhosts"],"x":"rabbitmqctl list_users"},"reflac":{"n":"reflac","t":"Re-encode FLAC audio files","s":[["TAGLINE","Re-encode FLAC audio files"],["TLDR","**Re-encode FLAC files**\n\n```reflac [path/to/music]```\n\n**Maximum compression**\n\n```reflac -8 [path/to/music]```\n\n**Verify after encoding**\n\n```reflac --verify [path/to/music]```\n\n**Preserve file times**\n\n```reflac --preserve-modtime [path/to/music]```\n\n**Dry run**\n\n```reflac --dry-run [path/to/music]```\n\n**Recursive processing**\n\n```reflac -r [path/to/directory]```"],["SYNOPSIS","**reflac** [_-0..-8_] [_--verify_] [_options_] _path_"],["PARAMETERS","**-0** to **-8**\n> Compression level.\n\n**--verify**\n> Verify after encode.\n\n**--preserve-modtime**\n> Keep file times.\n\n**--dry-run**\n> Show what would happen.\n\n**-r**, **--recursive**\n> Process subdirectories.\n\n**-j** _N_\n> Parallel jobs."],["DESCRIPTION","**reflac** is a batch tool for re-encoding FLAC audio files with updated compression settings. It recursively processes directories of FLAC files, applying the specified compression level while preserving all metadata tags, ensuring no audio data is lost during the process.\n\nThe tool supports parallel processing via the **-j** option, significantly speeding up operations on large music libraries. Compression levels range from **-0** (fastest) to **-8** (smallest file size), matching the standard FLAC encoder options. The **--verify** flag enables integrity checking by decoding each re-encoded file and comparing it against the original audio data.\n\nThis is particularly useful when upgrading a FLAC collection encoded with older or suboptimal compression settings to take advantage of improved encoding in newer FLAC versions, reducing storage requirements without any quality loss."],["CAVEATS","Requires FLAC encoder. CPU intensive at high levels. No quality improvement possible."],["HISTORY","**reflac** was created to batch re-encode FLAC files with modern compression settings while preserving all metadata."],["INSTALL","```aur: yay -S reflac```"],["SEE ALSO","[flac](/man/flac)(1), [metaflac](/man/metaflac)(1), [sox](/man/sox)(1)"]],"e":["flac","metaflac","sox"],"x":"reflac [path/to/music]"},"relic":{"n":"relic","t":"End-to-end encrypted secret manager for developers","s":[["TAGLINE","End-to-end encrypted secret manager for developers"],["TLDR","**Authenticate** with Relic via browser\n\n```relic login```\n\n**Initialize** Relic for the current project\n\n```relic init```\n\n**Open the interactive TUI** to manage secrets\n\n```relic```\n\n**Run a command** with secrets injected as environment variables\n\n```relic run -e [production] -- [npm run deploy]```\n\n**Run a command** with secrets from a specific folder and scope\n\n```relic run -e [staging] -f [api] -s [server] -- [node server.js]```\n\n**List all projects** with their environments and folders\n\n```relic projects```\n\n**Show the currently authenticated user**\n\n```relic whoami```\n\n**Disable anonymous telemetry**\n\n```relic telemetry disable```"],["SYNOPSIS","**relic** [_command_] [_options_]"],["PARAMETERS","**login**\n> Authenticate with Relic via browser using device authorization flow.\n\n**logout**\n> Clear stored authentication session, cached keys, and password.\n\n**init**\n> Initialize Relic for the current project by selecting a project and writing a relic.toml config file.\n\n**projects**\n> List all owned and shared projects with environments and folders in a tree view.\n\n**whoami**\n> Show the currently authenticated user's name, email, and plan.\n\n**mcp**\n> Start the Relic MCP server for AI assistant integration.\n\n**telemetry** _status|enable|disable_\n> Manage anonymous usage data collection.\n\n**run** **-e** _name_ [**-f** _folder_] [**-s** _scope_] [**-p** _id_] **--** _command..._\n> Run a command with decrypted secrets injected as environment variables.\n\n**-e**, **--environment** _name_\n> Environment name to load secrets from (required).\n\n**-f**, **--folder** _name_\n> Folder within the environment (optional).\n\n**-s**, **--scope** _scope_\n> Scope filter: **client**, **server**, or **shared** (optional).\n\n**-p**, **--project** _id_\n> Project ID. Defaults to relic.toml or **RELIC_PROJECT_ID** env var.\n\n**--version**\n> Show version information.\n\n**--help**\n> Show available commands and options."],["DESCRIPTION","**relic** is a CLI-first secret management layer that encrypts secrets on-device before transmitting them to the server. The server only stores ciphertext, ensuring zero-knowledge security.\n\nWhen running **relic run**, the CLI fetches encrypted secrets from the server, decrypts them locally using **AES-256** and **Argon2id**, and injects them into the target process through a Rust-based runner that clears memory after use. No secrets are written to disk during injection.\n\nRelic supports team collaboration where each member's secrets are encrypted with their own keys. It also works in CI/CD pipelines using API keys set via the **RELIC_API_KEY** and **RELIC_PASSWORD** environment variables.\n\nThe interactive TUI (invoked with bare **relic**) provides a visual interface for creating, editing, and organizing secrets across projects, environments, and folders. An MCP server mode enables integration with AI coding assistants."],["CONFIGURATION","Project configuration is stored in **relic.toml** in the project root, containing the project ID:\n\n```\nproject_id = \"your-project-id\"\n```\n\nA **.relic/** directory is created alongside relic.toml for local caching (cache.db). Add **.relic/** to .gitignore.\n\n**Environment variables:**\n\n**RELIC_API_KEY** — API key for CI/CD authentication (bypasses interactive login)\n**RELIC_PASSWORD** — Password for decryption in API key mode\n**RELIC_PROJECT_ID** — Override project ID without relic.toml"],["CAVEATS","The interactive TUI is not available in standalone binary installations; use CLI commands instead. Secrets require an active internet connection to fetch from the server, though results are cached locally for faster subsequent runs. The Rust-based runner is platform-specific and requires prebuilt binaries for each target architecture."],["HISTORY","Relic was created by **Can Vardar** at **Cupola** as an open-source, CLI-first alternative to cloud secret managers. It is written in **TypeScript** (CLI) with a **Rust** runtime component for secure secret injection. The project is licensed under the **BSL 1.1** (Business Source License) and hosted on GitHub."],["INSTALL","```nix: nix profile install nixpkgs#relic```"],["SEE ALSO","[doppler](/man/doppler)(1), [vault](/man/vault)(1), [sops](/man/sops)(1), [age](/man/age)(1), [gpg](/man/gpg)(1)"]],"e":["doppler","vault","sops","age","gpg"],"x":"relic login"},"repren":{"n":"repren","t":"Rename files and replace text with patterns","s":[["TAGLINE","Rename files and replace text with patterns"],["TLDR","**Rename with pattern**\n\n```repren --rename --from \"[old]\" --to \"[new]\" [files]```\n\n**Replace in files**\n\n```repren --from \"[search]\" --to \"[replace]\" [files]```\n\n**Using patterns file**\n\n```repren --patterns [patterns.txt] [files]```\n\n**Preview changes**\n\n```repren --dry-run --from \"[old]\" --to \"[new]\" [files]```\n\n**Case preserving**\n\n```repren --preserve-case --from \"[foo]\" --to \"[bar]\" [files]```\n\n**Literal strings**\n\n```repren --literal --from \"[text]\" --to \"[replacement]\" [files]```"],["SYNOPSIS","**repren** [_--rename_] [_--from pattern_] [_--to pattern_] [_options_] _files_"],["PARAMETERS","**--rename**\n> Rename files/directories.\n\n**--from** _PATTERN_\n> Search pattern.\n\n**--to** _PATTERN_\n> Replacement pattern.\n\n**--patterns** _FILE_\n> Patterns from file.\n\n**--dry-run**\n> Preview only.\n\n**--preserve-case**\n> Match case style.\n\n**--literal**\n> Literal strings.\n\n**-r**, **--recursive**\n> Recursive operation."],["DESCRIPTION","**repren** is a versatile batch tool for renaming files and performing search-and-replace operations across multiple files using regular expressions or literal patterns. It can apply several replacement rules simultaneously from a patterns file, making it efficient for large-scale refactoring tasks like renaming variables, updating imports, or reorganizing file structures.\n\nA key feature is case-preserving replacement, which automatically maintains the capitalization style of the original text. For example, replacing \"foo\" with \"bar\" will also convert \"Foo\" to \"Bar\" and \"FOO\" to \"BAR\". The **--dry-run** flag shows all planned changes without modifying any files, allowing safe verification before committing to batch operations.\n\nThe tool supports both file content replacement and file/directory renaming with the **--rename** flag. Patterns can use full Python regular expressions with capture groups, or literal strings with **--literal** for straightforward substitutions."],["CAVEATS","Python required. Complex patterns need care. Backup important files."],["HISTORY","**repren** was created as a versatile renaming and replacement tool supporting complex batch operations with pattern files."],["INSTALL","```brew: brew install repren```\n\n```nix: nix profile install nixpkgs#repren```"],["SEE ALSO","[rename](/man/rename)(1), [sed](/man/sed)(1), [perl](/man/perl)(1)"]],"e":["rename","sed","perl"],"x":"repren --rename --from \"[old]\" --to \"[new]\" [files]"},"rofi":{"n":"rofi","t":"Application launcher and window switcher","s":[["TAGLINE","Application launcher and window switcher"],["TLDR","**Show application launcher**\n\n```rofi -show drun```\n\n**Show command launcher**\n\n```rofi -show run```\n\n**Switch between open windows**\n\n```rofi -show window```\n\n**Use as dmenu replacement**\n\n```printf \"Choice1\\nChoice2\\nChoice3\" | rofi -dmenu```\n\n**SSH to a host**\n\n```rofi -show ssh```\n\n**Use a specific theme**\n\n```rofi -show drun -theme [solarized]```"],["SYNOPSIS","**rofi** [**-show** _mode_] [**-dmenu**] [**-theme** _theme_]"],["MODES","**drun**\n> Desktop application launcher\n\n**run**\n> Command launcher\n\n**window**\n> Window switcher\n\n**ssh**\n> SSH connection manager\n\n**keys**\n> Keybinding viewer\n\n**combi**\n> Combined modes"],["PARAMETERS","**-show _mode_**\n> Show specified mode\n\n**-dmenu**\n> Run as dmenu replacement\n\n**-theme _theme_**\n> Use specified theme\n\n**-modi _modes_**\n> Enable specified modes\n\n**-matching _method_**\n> Match method (fuzzy, regex, glob, normal)\n\n**-i**\n> Case insensitive matching"],["DESCRIPTION","**rofi** is a versatile application launcher and window switcher. It provides a searchable interface for launching programs, switching windows, running commands, and more.\n\nThe tool is highly customizable with themes and supports scripting via the dmenu mode. It's popular in tiling window manager setups as a replacement for dmenu."],["CONFIGURATION","**~/.config/rofi/config.rasi**\n> Main configuration file in rasi format, controlling modes, keybindings, matching behavior, and default settings.\n\n**~/.config/rofi/themes/**\n> Directory for custom theme files that control appearance, colors, fonts, and layout.\n\n**~/.local/share/rofi/themes/**\n> Additional theme search path for user-installed themes."],["CAVEATS","Requires X11 by default; for Wayland, use the **rofi-wayland** fork. Themes use the rasi format (not CSS). Custom scripts can extend rofi with new modes via the `-modi` option. The dmenu mode reads from stdin and writes the selected entry to stdout."],["HISTORY","**rofi** was created by **Dave Davenport** as a more feature-rich alternative to dmenu. It has become a standard component in many Linux desktop configurations."],["INSTALL","```apt: sudo apt install rofi```\n\n```dnf: sudo dnf install rofi```\n\n```pacman: sudo pacman -S rofi```\n\n```apk: sudo apk add rofi-wayland```\n\n```zypper: sudo zypper install rofi```\n\n```brew: brew install rofi```\n\n```nix: nix profile install nixpkgs#rofi```"],["SEE ALSO","[dmenu](/man/dmenu)(1), [wofi](/man/wofi)(1), [fuzzel](/man/fuzzel)(1)"]],"e":["dmenu","wofi","fuzzel"],"x":"rofi -show drun"},"ropper":{"n":"ropper","t":"Search for ROP and JOP gadgets in binaries","s":[["TAGLINE","Search for ROP and JOP gadgets in binaries"],["TLDR","**Find gadgets**\n\n```ropper -f [binary]```\n\n**Search for gadget**\n\n```ropper -f [binary] --search \"[pop rdi]\"```\n\n**Find string**\n\n```ropper -f [binary] --string \"[/bin/sh]\"```\n\n**Interactive mode**\n\n```ropper```\n\n**Generate ropchain**\n\n```ropper -f [binary] --chain [execve]```\n\n**Show all gadgets**\n\n```ropper -f [binary] --all```\n\n**Set architecture**\n\n```ropper -f [binary] -a [x86_64]```"],["SYNOPSIS","**ropper** [_-f file_] [_--search pattern_] [_--chain type_] [_options_]"],["PARAMETERS","**-f**, **--file** _FILE_\n> Binary file.\n\n**--search** _PATTERN_\n> Search gadgets.\n\n**--string** _STR_\n> Find string.\n\n**--chain** _TYPE_\n> Generate ROP chain.\n\n**-a**, **--arch** _ARCH_\n> Architecture.\n\n**--all**\n> All gadgets.\n\n**--type** _TYPE_\n> Gadget type (rop, jop).\n\n**-I** _OFFSET_\n> Image base.\n\n**--depth** _N_\n> Max instructions."],["DESCRIPTION","**ropper** finds gadgets for ROP/JOP/COP exploits. It supports multiple architectures and binary formats.\n\nInteractive shell provides exploration environment. Commands search and analyze gadgets.\n\nChain generation builds exploit payloads. execve, mprotect, and virtualprotect chains available.\n\nSemantic search finds gadgets by function. Specify what you need, not exact instructions.\n\nMultiple file support analyzes libraries together. Build chains across binaries."],["CAVEATS","For authorized security research only. Complex tool requiring exploit knowledge. Results need verification."],["HISTORY","**Ropper** was created by **Sascha Schirra** as a comprehensive gadget finder. It provides both CLI and interactive interfaces for ROP chain development."],["INSTALL","```pacman: sudo pacman -S ropper```"],["SEE ALSO","[pwntools](/man/pwntools)(1), [radare2](/man/radare2)(1)"]],"e":["pwntools","radare2"],"x":"ropper -f [binary]"},"run-kit":{"n":"run-kit","t":"Remote browser console for tmux sessions","s":[["TAGLINE","Remote browser console for tmux sessions"],["TLDR","**Start** the dashboard daemon (default port 3000)\n\n```run-kit daemon start```\n\n**Open** the dashboard in a browser\n\n```open http://localhost:3000```\n\n**Spawn** an agent workspace in a git worktree (must be inside tmux)\n\n```run-kit riff```\n\n**Spawn** three parallel workspaces\n\n```run-kit riff -N 3```\n\n**Check** dependencies when something fails\n\n```run-kit doctor```\n\n**Send** a Web Push notification to subscribed devices\n\n```run-kit notify \"deploy finished\" --title \"CI\"```"],["SYNOPSIS","**run-kit** \\<command\\> [_options_]"],["DESCRIPTION","**run-kit** (short alias **rk**) is a remote, phone-first console for **tmux**. It exposes every session and pane as a live terminal in a browser sidebar—no database; state is derived from tmux and the filesystem. The same dashboard works on a phone over Tailscale HTTPS.\n\nTwo halves compose independently:\n\n* **run-kit riff** — creates a git worktree (via sibling tool **wt**), opens a tmux window, and launches panes (default: a coding agent; any **--cmd** is allowed).\n* **run-kit serve** / **run-kit daemon** — HTTP dashboard (default **127.0.0.1:3000**) that watches tmux. The daemon runs in a dedicated tmux server (**rk-daemon**) so restarts do not kill user sessions.\n\nOptional **run-kit agent-setup** installs harness hooks so panes report agent busy/waiting/idle. Boards pin multiple panes side-by-side; **run-kit notify** fans Web Push to subscribed browsers. Shell completion: **eval \"$(run-kit shell-init zsh)\"**.\n\nInstall via the shll toolkit installer or Homebrew (**sahil87/tap/run-kit**). **rk** is a full interchangeable alias of **run-kit**."],["PARAMETERS","**riff** [_preset_] [**--skill** _slash_] [**--cmd** _command_] [**-N** _count_] [**--layout** _layout_] [**--** _wt-flags_]\n> Spawn worktree + tmux window + pane(s). Must run inside an existing tmux session. **--skill** and **--cmd** are repeatable (pane array). **-N** spawns parallel workspaces.\n\n**serve**\n> Start the HTTP server in the foreground. **RK_HOST** (default **127.0.0.1**), **RK_PORT** (default **3000**).\n\n**daemon start|stop|restart|status**\n> Manage the background dashboard daemon.\n\n**doctor**\n> Check runtime dependencies (tmux, **wt**, launcher, port).\n\n**agent-setup** [**--uninstall**]\n> Install or remove agent-harness hooks for pane state reporting.\n\n**notify** _message_ [**--title** _title_]\n> Send a Web Push notification (fail-silent if the server is down).\n\n**status**\n> Summarize tmux sessions.\n\n**url**\n> Print the configured server URL.\n\n**update**\n> Upgrade via Homebrew and restart the daemon.\n\n**shell-init** \\<shell\\>\n> Emit eval-safe completion for zsh, bash, fish, or powershell.\n\n**help**\n> Help for any subcommand."],["CAVEATS","**riff** requires **$TMUX**, **wt** on **PATH**, and a configured agent/launcher. Agent status dots need a one-time **agent-setup** and new agent sessions after install. Web Push needs a secure context (HTTPS or localhost). Binding **RK_HOST=0.0.0.0** exposes the terminal dashboard on the network—restrict access (VPN/Tailscale, firewall)."],["INSTALL","```brew: brew install run-kit```"],["SEE ALSO","[tmux](/man/tmux)(1), [git](/man/git)(1), [git-worktree](/man/git-worktree)(1), [tailscale](/man/tailscale)(1)"],["RESOURCES","```[Source code](https://github.com/sahil87/run-kit)```\n\n```[Homepage](https://shll.ai/run-kit)```\n\n```[Documentation](https://shll.ai/tools/run-kit/commands/)```"]],"e":["tmux","git","git-worktree","tailscale"],"x":"run-kit daemon start"},"scheme":{"n":"scheme","t":"Scheme programming language interpreter","s":[["TAGLINE","Scheme programming language interpreter"],["TLDR","**Start REPL**\n\n```scheme```\n\n**Run file**\n\n```scheme [file.scm]```\n\n**Evaluate expression**\n\n```scheme -e \"[(+ 1 2)]\"```\n\n**Load file then REPL**\n\n```scheme -l [file.scm]```\n\n**Quiet mode**\n\n```scheme -q```"],["SYNOPSIS","**scheme** [_-e expr_] [_-l file_] [_options_] [_file_]"],["PARAMETERS","**-e** _EXPR_\n> Evaluate expression.\n\n**-l** _FILE_\n> Load file.\n\n**-q**\n> Quiet startup.\n\n**--help**\n> Show help.\n\n**--version**\n> Show version."],["DESCRIPTION","**scheme** is an interpreter for the Scheme programming language, a minimalist dialect of Lisp emphasizing functional programming, lexical scoping, and first-class procedures. It provides both an interactive REPL for experimentation and the ability to run Scheme source files directly.\n\nScheme's design follows the R5RS and R7RS language standards, providing a small but powerful core with features like proper tail recursion, continuations, and hygienic macros. The interactive REPL allows evaluating expressions immediately, making it well-suited for learning, prototyping, and exploring algorithms.\n\nThe **scheme** command may refer to different implementations depending on the system, including MIT/GNU Scheme, Chez Scheme, or GNU Guile. Each implementation offers its own extensions beyond the standard, so behavior and available libraries may vary."],["CAVEATS","Implementation varies. Name may differ (guile, mit-scheme). Portability concerns."],["HISTORY","**Scheme** was designed by **Guy Steele** and **Gerald Sussman** in 1975. Various implementations provide the scheme command."],["SEE ALSO","[guile](/man/guile)(1), [racket](/man/racket)(1), [clisp](/man/clisp)(1)"]],"e":["guile","racket","clisp"],"x":"scheme"},"secret-tool":{"n":"secret-tool","t":"Store and retrieve secrets via desktop keyring","s":[["TAGLINE","Store and retrieve secrets via desktop keyring"],["TLDR","**Store** a secret (reads value from stdin) with a label and attribute pair\n\n```secret-tool store --label=\"[My password]\" [attribute] [value]```\n\n**Look up** a stored secret by attribute\n\n```secret-tool lookup [attribute] [value]```\n\n**Search** for secrets matching attributes (metadata only)\n\n```secret-tool search [attribute] [value]```\n\n**Search and unlock** with all matching secrets shown\n\n```secret-tool search --all --unlock [attribute] [value]```\n\n**Clear** all secrets matching attributes\n\n```secret-tool clear [attribute] [value]```"],["SYNOPSIS","**secret-tool store** **--label**=_label_ [_attribute_ _value_]...\n\n**secret-tool lookup** [_attribute_ _value_]...\n\n**secret-tool search** [**--all**] [**--unlock**] [_attribute_ _value_]...\n\n**secret-tool clear** [_attribute_ _value_]..."],["COMMANDS","**store**\n> Store a secret. The password is read from stdin. Requires **--label** and one or more attribute/value pairs to identify the entry.\n\n**lookup**\n> Print the password of a secret matching the given attribute/value pairs.\n\n**search**\n> List secrets matching the attribute/value pairs. By default shows only the first match without unlocking.\n\n**clear**\n> Remove all secrets that match the given attribute/value pairs."],["PARAMETERS","**--label** _label_\n> Human-readable label for the secret (required for **store**).\n\n**--all**\n> Show all matching secrets, not just the first (search only).\n\n**--unlock**\n> Unlock locked collections during the search."],["DESCRIPTION","**secret-tool** stores and retrieves passwords using the Freedesktop Secret Service API. It communicates with running keyring daemons such as gnome-keyring or KWallet over D-Bus.\n\nSecrets are not addressed by name but by arbitrary attribute/value pairs (for example `service mybackup user alice`). The same pairs used to store a secret must be supplied to look it up or remove it."],["CAVEATS","Requires a running Secret Service provider (gnome-keyring-daemon or kwalletd). Stored secrets are encrypted on disk but accessible to any application running as the user. Passwords are read from stdin during **store** to avoid leaking them via process listings."],["HISTORY","Part of **libsecret**, the GNOME library implementing the Freedesktop Secret Service specification. **secret-tool** was added as a convenience CLI so the keyring could be scripted without writing D-Bus code."],["INSTALL","```apt: sudo apt install libsecret-tools```\n\n```dnf: sudo dnf install libsecret```\n\n```pacman: sudo pacman -S libsecret```\n\n```apk: sudo apk add libsecret```\n\n```zypper: sudo zypper install secret-tool```\n\n```brew: brew install libsecret```\n\n```nix: nix profile install nixpkgs#libsecret```"],["SEE ALSO","[gnome-keyring](/man/gnome-keyring)(1), [pass](/man/pass)(1)"]],"e":["gnome-keyring","pass"],"x":"secret-tool store --label=\"[My password]\" [attribute] [value]"},"setfiles":{"n":"setfiles","t":"Set SELinux file security contexts from policy","s":[["TAGLINE","Set SELinux file security contexts from policy"],["TLDR","Set file contexts according to the **default policy**\n\n```sudo setfiles /etc/selinux/targeted/contexts/files/file_contexts [path/to/directory]```\n\nSet file contexts **recursively** and show changes\n\n```sudo setfiles -v /etc/selinux/targeted/contexts/files/file_contexts [path/to/directory]```\n\n**Preview** changes without modifying contexts\n\n```sudo setfiles -n /etc/selinux/targeted/contexts/files/file_contexts [path/to/directory]```\n\n**Force** complete context replacement\n\n```sudo setfiles -F /etc/selinux/targeted/contexts/files/file_contexts [path/to/directory]```\n\nUse **multiple threads** for faster processing\n\n```sudo setfiles -T 4 /etc/selinux/targeted/contexts/files/file_contexts [path/to/directory]```\n\n**Exclude** a directory from relabeling\n\n```sudo setfiles -e [path/to/exclude] /etc/selinux/targeted/contexts/files/file_contexts [path/to/directory]```"],["SYNOPSIS","**setfiles** [**-cdlmnpsvFIUW**] [**-e** _dir_] [**-r** _rootpath_] [**-T** _nthreads_] _spec_file_ _pathname_..."],["PARAMETERS","**-c** _policy_\n> Validate contexts against the specified binary policy file\n\n**-d**\n> Show which specification matched each file (debug)\n\n**-e** _directory_\n> Exclude directory from labeling (can be repeated)\n\n**-f** _infilename_\n> Read list of files to process from a file\n\n**-F**\n> Force complete context replacement including user, role, and range\n\n**-n**\n> Dry run; show what would be changed without modifying\n\n**-p**\n> Show progress as percentage or 1024-block increments\n\n**-r** _rootpath_\n> Use alternate root path for context matching\n\n**-s**\n> Log changes to syslog instead of stdout\n\n**-v**\n> Verbose; show files whose labels are changed\n\n**-T** _nthreads_\n> Number of threads to use (0 = number of CPU cores)\n\n**-I**\n> Ignore stored directory digests; force label checking\n\n**-D**\n> Set or update directory SHA1 digests in extended attributes\n\n**-W**\n> Display warnings about entries that have no matching specification\n\n**-l**\n> Log changes to the file named by the SETFILES_PROGRESS environment variable at 10-second intervals"],["DESCRIPTION","**setfiles** initializes and corrects SELinux security context labels on filesystems based on pattern-to-context mappings in a specification file. It is the primary tool for relabeling files after SELinux policy changes or system installation.\n\nThe specification file (typically **/etc/selinux/targeted/contexts/files/file_contexts**) contains regular expressions matched against file paths to determine appropriate security contexts. Unlike **restorecon**, setfiles requires an explicit specification file argument."],["CAVEATS","Requires SELinux to be enabled; on non-SELinux systems, the command has no effect. Relabeling large filesystems can be time-consuming. Using **-F** forces all context components to change, which may disrupt running services. The **-n** dry-run option should be used first on production systems."],["HISTORY","**setfiles** is part of the SELinux **policycoreutils** package, developed alongside SELinux at the **NSA** (National Security Agency) starting in **2000**. SELinux was merged into the Linux kernel mainline in **2003** with version **2.6**. The tool has evolved to support multi-threading and digest-based optimization."],["INSTALL","```apt: sudo apt install policycoreutils```\n\n```dnf: sudo dnf install policycoreutils```\n\n```zypper: sudo zypper install policycoreutils```\n\n```nix: nix profile install nixpkgs#policycoreutils```"],["SEE ALSO","[restorecon](/man/restorecon)(8), [fixfiles](/man/fixfiles)(8), [semanage](/man/semanage)(8), [semanage-fcontext](/man/semanage-fcontext)(8), [chcon](/man/chcon)(1), [getenforce](/man/getenforce)(8), [sestatus](/man/sestatus)(8)"]],"e":["restorecon","fixfiles","semanage","semanage-fcontext","chcon","getenforce","sestatus"],"x":"sudo setfiles /etc/selinux/targeted/contexts/files/file_contexts [path/to/directory]"},"sponge":{"n":"sponge","t":"Soak up stdin before writing to file","s":[["TAGLINE","Soak up stdin before writing to file"],["TLDR","**Overwrite file with pipeline output**\n\n```cat [file.txt] | sort | sponge [file.txt]```\n\n**Filter and save in place**\n\n```grep \"[pattern]\" [file.txt] | sponge [file.txt]```\n\n**Transform and overwrite**\n\n```sed 's/old/new/g' [file.txt] | sponge [file.txt]```\n\n**Append instead of overwrite**\n\n```echo \"new line\" | sponge -a [file.txt]```"],["SYNOPSIS","**sponge** [_options_] _file_"],["DESCRIPTION","**sponge** reads standard input and writes to a file. Unlike shell redirection, it reads all input before opening the output file, allowing safe in-place modifications.\n\nThe tool is part of moreutils and solves the problem of using a file as both input and output in a pipeline."],["PARAMETERS","**-a**\n> Append to file instead of overwriting."],["EXAMPLES","Without sponge (fails):\n```\nsort file.txt > file.txt  # Results in empty file!\n```\n\nWith sponge (works):\n```\nsort file.txt | sponge file.txt\n```"],["CAVEATS","Loads entire input into memory. Very large files may exhaust memory. Part of moreutils package. Creates a temp file internally and renames it atomically when possible. If no file is specified, sponge outputs to stdout."],["HISTORY","**sponge** is part of **moreutils**, a collection of Unix tools created by **Joey Hess**. It addresses a common shell scripting pitfall where redirection to the input file truncates it before reading."],["INSTALL","```apt: sudo apt install moreutils```\n\n```dnf: sudo dnf install moreutils```\n\n```pacman: sudo pacman -S moreutils```\n\n```apk: sudo apk add moreutils```\n\n```zypper: sudo zypper install moreutils```\n\n```brew: brew install moreutils```\n\n```nix: nix profile install nixpkgs#moreutils```"],["SEE ALSO","[tee](/man/tee)(1), [cat](/man/cat)(1), [moreutils](/man/moreutils)(7)"]],"e":["tee","cat","moreutils"],"x":"cat [file.txt] | sort | sponge [file.txt]"},"ssm":{"n":"ssm","t":"Simple SSH manager for organizing server connections","s":[["TAGLINE","Simple SSH manager for organizing server connections"],["TLDR","**List all SSH configurations**\n\n```ssm list```\n\n**Add a new SSH configuration**\n\n```ssm add```\n\n**Edit an existing configuration**\n\n```ssm edit [myserver]```\n\n**Print an SSH configuration**\n\n```ssm print [myserver]```"],["SYNOPSIS","**ssm** [_global options_] _command_ [_command options_]"],["PARAMETERS","**list**, **ls**\n> List available SSH configurations.\n\n**add**\n> Add a new SSH configuration interactively.\n\n**edit**, **e**\n> Edit an existing SSH configuration.\n\n**print**\n> Print an SSH configuration to stdout.\n\n**help**, **h**\n> Show a list of commands or help for a command.\n\n**--help**, **-h**\n> Show help.\n\n**--version**, **-v**\n> Print the version."],["DESCRIPTION","**ssm** (Simple SSH Manager) is a CLI tool for managing SSH configurations across multiple servers. It organizes SSH configs into modular files within a config.d directory structure, allowing you to list, add, edit, and print SSH configurations."],["HISTORY","**ssm** was created by **Elliot** (elliot40404) and is written in **Go**."],["SEE ALSO","[ssh](/man/ssh)(1), [sshs](/man/sshs)(1), [sshclick](/man/sshclick)(1)"]],"e":["ssh","sshs","sshclick"],"x":"ssm list"},"trace-cmd-report":{"n":"trace-cmd-report","t":"Display recorded kernel trace data","s":[["TAGLINE","Display recorded kernel trace data"],["TLDR","Display **recorded** trace\n\n```sudo trace-cmd report```\n\nDisplay trace for specific **CPU**\n\n```sudo trace-cmd report --cpu [cpu_number]```"],["SYNOPSIS","**trace-cmd report** [_OPTIONS_] [_FILE_]"],["PARAMETERS","**--cpu** _NUMBER_\n> Display trace for specific CPU only\n\n**-i** _FILE_\n> Read trace from specified file"],["DESCRIPTION","**trace-cmd report** displays recorded trace events from a trace.dat file (or specified file). It parses the binary trace data and presents it in human-readable format.\n\nThe output shows timestamps, CPU numbers, process information, and trace events in chronological order. Filtering options allow focusing on specific CPUs or event types."],["CAVEATS","Requires matching kernel version for proper symbol resolution. Large trace files may take time to process. Some events require debug information for full interpretation."],["INSTALL","```apt: sudo apt install trace-cmd```\n\n```dnf: sudo dnf install trace-cmd```\n\n```pacman: sudo pacman -S trace-cmd```\n\n```zypper: sudo zypper install trace-cmd```\n\n```nix: nix profile install nixpkgs#trace-cmd```"],["SEE ALSO","[trace-cmd](/man/trace-cmd)(1), [trace-cmd-list](/man/trace-cmd-list)(1), [trace-cmd-record](/man/trace-cmd-record)(1)"]],"e":["trace-cmd","trace-cmd-list","trace-cmd-record"],"x":"sudo trace-cmd report"},"xdelta":{"n":"xdelta","t":"Binary delta compression and patching","s":[["TAGLINE","Binary delta compression and patching"],["TLDR","**Create delta**\n\n```xdelta delta [old_file] [new_file] [patch.xdelta]```\n\n**Apply patch**\n\n```xdelta patch [patch.xdelta] [old_file] [new_file]```\n\n**Create with compression**\n\n```xdelta -9 delta [old_file] [new_file] [patch.xdelta]```\n\n**Show patch info**\n\n```xdelta info [patch.xdelta]```\n\n**Test patch**\n\n```xdelta test [patch.xdelta] [old_file]```"],["SYNOPSIS","**xdelta** _command_ [_options_] [_files_]"],["PARAMETERS","**delta**\n> Create delta file.\n\n**patch**\n> Apply delta file.\n\n**info**\n> Show patch info.\n\n**test**\n> Verify patch.\n\n**-0** to **-9**\n> Compression level.\n\n**-f**, **--force**\n> Force overwrite.\n\n**-s** _SOURCE_\n> Source file."],["DESCRIPTION","**xdelta** creates and applies binary delta patches between two versions of a file. The delta command compares an old file with a new file and produces a compact patch file containing only the differences, which is typically much smaller than distributing the full new file.\n\nThe patch command reconstructs the new file by applying the delta to the original file. Configurable compression levels trade processing time for smaller patch sizes. The tool is commonly used for software update distribution, ROM patching, and efficient file synchronization where only changes need to be transmitted."],["CAVEATS","Old file must match exactly. Large files need memory. Version compatibility matters."],["HISTORY","**xdelta** was created for efficient binary differencing. Version 1 evolved into xdelta3 with VCDIFF standard support."],["INSTALL","```apt: sudo apt install xdelta```\n\n```dnf: sudo dnf install xdelta```\n\n```brew: brew install xdelta```\n\n```nix: nix profile install nixpkgs#xdelta```"],["SEE ALSO","[xdelta3](/man/xdelta3)(1), [bsdiff](/man/bsdiff)(1), [rdiff](/man/rdiff)(1)"]],"e":["xdelta3","bsdiff","rdiff"],"x":"xdelta delta [old_file] [new_file] [patch.xdelta]"},"xfr":{"n":"xfr","t":"Modern iperf3 alternative with live TUI and QUIC","s":[["TAGLINE","Modern iperf3 alternative with live TUI and QUIC"],["TLDR","**Start a test server**\n\n```xfr server```\n\n**Run a client test against a server**\n\n```xfr client -c [192.168.1.1]```\n\n**Test with QUIC transport**\n\n```xfr client -c [host] --quic```"],["SYNOPSIS","**xfr** _server_|_client_ [_options_]"],["DESCRIPTION","**xfr** is a network performance testing tool with a live TUI featuring real-time throughput graphs, per-stream stats, and a multi-client server dashboard. It supports TCP, UDP, and QUIC protocols with configurable bitrate pacing and multiple output formats (plain text, JSON, CSV)."],["HISTORY","**xfr** was created by **lance0** and is written in **Rust**."],["INSTALL","```zypper: sudo zypper install xfr```\n\n```nix: nix profile install nixpkgs#xfr```"],["SEE ALSO","[iperf3](/man/iperf3)(1), [nuttcp](/man/nuttcp)(1)"]],"e":["iperf3","nuttcp"],"x":"xfr server"},"ytmdl":{"n":"ytmdl","t":"Download music with automatic metadata tagging","s":[["TAGLINE","Download music with automatic metadata tagging"],["TLDR","**Download song with metadata**\n\n```ytmdl [song name]```\n\n**Download from YouTube URL**\n\n```ytmdl [https://youtube.com/watch?v=VIDEO_ID]```\n\n**Specify output directory**\n\n```ytmdl -o [~/Music] [song name]```\n\n**Skip confirmation prompts**\n\n```ytmdl --skip-meta [song name]```\n\n**Download as MP3**\n\n```ytmdl --format mp3 [song name]```"],["SYNOPSIS","**ytmdl** [_options_] [_song_|_URL_]"],["PARAMETERS","**-o**, **--output-dir** _DIR_\n> Set output directory for downloads\n\n**--format** _FORMAT_\n> Audio format (mp3, m4a, opus, etc.)\n\n**--skip-meta**\n> Skip quering metadata and downloading default one\n\n**-q**, **--quiet**\n> Suppress output\n\n**--proxy** _URL_\n> Use proxy for downloads\n\n**--level** _LEVEL_\n> Log level (DEBUG, INFO, WARNING, ERROR)\n\n**--disable-metaadd**\n> Disable adding metadata to the song\n\n**--trim**, **-t**\n> Trim silence from start and end"],["DESCRIPTION","**ytmdl** is a tool to download music from YouTube and automatically add metadata (artist, album, cover art, lyrics) from sources like iTunes, Spotify, Deezer, and Gaana. It searches for songs, downloads them as audio, and enriches them with proper tags.\n\nThe tool can accept either a song name (which it searches on YouTube) or a direct YouTube URL. It presents metadata options from various sources and lets you choose the best match.\n\nConfiguration is stored in **~/.config/ytmdl/config** for setting default behaviors, output directories, and preferred metadata sources."],["CAVEATS","Metadata accuracy depends on search results. Obscure or misspelled songs may get incorrect metadata.\n\nRequires **ffmpeg** for audio conversion and format handling.\n\nSome metadata sources may have regional restrictions or rate limits.\n\nDownloaded content may be subject to copyright. Respect content creators' rights."],["INSTALL","```nix: nix profile install nixpkgs#ytmdl```"],["SEE ALSO","[yt-dlp](/man/yt-dlp)(1), [ffmpeg](/man/ffmpeg)(1), [youtube-dl](/man/youtube-dl)(1), [spotdl](/man/spotdl)(1)"]],"e":["yt-dlp","ffmpeg","youtube-dl","spotdl"],"x":"ytmdl [song name]"},"zcmp":{"n":"zcmp","t":"Compare gzip-compressed files byte-by-byte","s":[["TAGLINE","Compare gzip-compressed files byte-by-byte"],["TLDR","**Compare compressed files**\n\n```zcmp [file1.gz] [file2.gz]```\n\n**Show differences**\n\n```zcmp -l [file1.gz] [file2.gz]```\n\n**Silent mode**\n\n```zcmp -s [file1.gz] [file2.gz]```"],["SYNOPSIS","**zcmp** [_-l_] [_-s_] _file1_ _file2_"],["PARAMETERS","**-b**, **--print-bytes**\n> Print differing bytes (as octal values).\n\n**-l**, **--verbose**\n> Output byte numbers and differing byte values for all differences.\n\n**-s**, **--quiet**, **--silent**\n> Suppress all output; communicate result only via exit status.\n\n**-i** _SKIP_, **--ignore-initial=**_SKIP_\n> Skip the first _SKIP_ bytes of each (decompressed) input.\n\n**-n** _LIMIT_, **--bytes=**_LIMIT_\n> Compare at most _LIMIT_ bytes."],["DESCRIPTION","**zcmp** compares gzip-compressed files byte-by-byte without requiring manual decompression. It invokes **cmp** on uncompressed contents and passes all supplied options directly to it.\n\nBy default, zcmp reports only the first difference found. The **-l** flag shows all differing byte positions and values. Silent mode (**-s**) suppresses all output and communicates only through exit codes: 0 if files are identical, 1 if they differ, 2 on error.\n\nThe command is part of the gzip package and acts as a convenience wrapper. For line-by-line comparison of compressed files, use **zdiff** instead."],["CAVEATS","Gzip format. For .gz files. Part of gzip package."],["HISTORY","**zcmp** is part of the gzip package, providing cmp-like functionality for compressed files."],["INSTALL","```apt: sudo apt install gzip```\n\n```dnf: sudo dnf install gzip```\n\n```pacman: sudo pacman -S gzip```\n\n```apk: sudo apk add gzip```\n\n```zypper: sudo zypper install gzip```\n\n```brew: brew install gzip```\n\n```nix: nix profile install nixpkgs#gzip```"],["SEE ALSO","[cmp](/man/cmp)(1), [zdiff](/man/zdiff)(1), [zcat](/man/zcat)(1)"]],"e":["cmp","zdiff","zcat"],"x":"zcmp [file1.gz] [file2.gz]"},"zipcloak":{"n":"zipcloak","t":"Encrypt or decrypt ZIP archives","s":[["TAGLINE","Encrypt or decrypt ZIP archives"],["TLDR","**Encrypt ZIP file**\n\n```zipcloak [archive.zip]```\n\n**Decrypt ZIP file**\n\n```zipcloak -d [archive.zip]```\n\n**Encrypt with output**\n\n```zipcloak -O [encrypted.zip] [archive.zip]```\n\n**Quiet mode**\n\n```zipcloak -q [archive.zip]```"],["SYNOPSIS","**zipcloak** [_-d_] [_-O output_] [_options_] _zipfile_"],["PARAMETERS","**-d**, **--decrypt**\n> Decrypt archive.\n\n**-O** _FILE_\n> Output to new file.\n\n**-q**, **--quiet**\n> Quiet mode.\n\n**-b** _PATH_\n> Temp file path.\n\n**-h**, **--help**\n> Show help.\n\n**-v**, **--version**\n> Show version."],["DESCRIPTION","**zipcloak** encrypts or decrypts ZIP archives by adding or removing password protection. When encrypting, it prompts for a password and applies encryption to all files within the archive. When decrypting with the **-d** flag, it removes encryption after verifying the original password.\n\nBy default, zipcloak modifies the archive in place, replacing the original file. The **-O** option allows writing the encrypted or decrypted result to a new file, preserving the original archive unchanged.\n\nzipcloak uses standard ZIP encryption (PKZIP), which is widely compatible with most ZIP tools and operating systems. It is part of the Info-ZIP suite of utilities."],["CAVEATS","Standard ZIP encryption (PKZIP/ZipCrypto) is considered weak by modern standards and can be broken with known-plaintext attacks. For stronger encryption, consider using **7z** with AES-256. The **-O** option should be used when you want to preserve the original unencrypted archive. Encryption applies to file contents only; filenames and metadata remain visible."],["HISTORY","**zipcloak** is part of the **Info-ZIP** suite of utilities, originally developed in the early 1990s as free, portable implementations of the ZIP archive format tools. It provides a convenient way to add or remove password protection from existing ZIP archives without needing to recreate them."],["INSTALL","```apt: sudo apt install zip```\n\n```dnf: sudo dnf install zip```\n\n```pacman: sudo pacman -S zip```\n\n```apk: sudo apk add zip```\n\n```zypper: sudo zypper install zip```\n\n```brew: brew install zip```\n\n```nix: nix profile install nixpkgs#zip```"],["SEE ALSO","[zip](/man/zip)(1), [unzip](/man/unzip)(1), [7z](/man/7z)(1), [zipinfo](/man/zipinfo)(1), [zipnote](/man/zipnote)(1)"]],"e":["zip","unzip","7z","zipinfo","zipnote"],"x":"zipcloak [archive.zip]"},"zizmor":{"n":"zizmor","t":"Static analysis tool for GitHub Actions security","s":[["TAGLINE","Static analysis tool for GitHub Actions security"],["TLDR","**Audit all GitHub Actions in current directory**\n\n```zizmor .```\n\n**Output in SARIF format**\n\n```zizmor --format sarif [.github/workflows/]```\n\n**Run fully offline**\n\n```zizmor --offline .```"],["SYNOPSIS","**zizmor** [_options_] [_path_]"],["DESCRIPTION","**zizmor** audits GitHub Actions YAML files for security issues including template injection vulnerabilities, mutable tag pinning, and known advisory usage. It has approximately 24 audit rules, generates SARIF output for GitHub code scanning integration, and supports offline mode by default."],["HISTORY","**zizmor** was created by **William Woodruff** (Trail of Bits) and is written in **Rust**."],["INSTALL","```pacman: sudo pacman -S zizmor```\n\n```apk: sudo apk add zizmor```\n\n```zypper: sudo zypper install zizmor```\n\n```brew: brew install zizmor```\n\n```nix: nix profile install nixpkgs#zizmor```"],["SEE ALSO","[semgrep](/man/semgrep)(1)"]],"e":["semgrep"],"x":"zizmor ."},"zsh":{"n":"zsh","t":"Extended Bourne shell with advanced features","s":[["TAGLINE","Extended Bourne shell with advanced features"],["TLDR","**Start an interactive zsh shell**\n\n```zsh```\n\n**Execute a script**\n\n```zsh [script.zsh]```\n\n**Execute a command**\n\n```zsh -c \"[command]\"```\n\n**Start without loading config files**\n\n```zsh -f```\n\n**Start a login shell**\n\n```zsh -l```\n\n**Check script syntax** without executing\n\n```zsh -n [script.zsh]```"],["SYNOPSIS","**zsh** [**-cefinslvx**] [_script_ [_args_...]]"],["PARAMETERS","**-c** _string_\n> Execute commands from string\n\n**-f**\n> Don't source startup files\n\n**-i**\n> Force interactive mode\n\n**-l**\n> Login shell\n\n**-n**\n> No execute; check syntax only\n\n**-s**\n> Read commands from stdin\n\n**-v**\n> Verbose; print input as read\n\n**-x**\n> Xtrace; print commands as executed"],["DESCRIPTION","**zsh** (Z Shell) is an extended Bourne shell with many improvements, combining features from bash, ksh, and tcsh. It offers powerful completion, theming, and scripting capabilities.\n\nZsh is highly configurable through **~/.zshrc** for interactive settings and **~/.zshenv** for all shells. Framework projects like Oh My Zsh and Prezto provide pre-configured themes and plugins.\n\nKey features include advanced tab completion (programmable and context-aware), spelling correction, shared history across sessions, powerful globbing (recursive patterns, qualifiers), and extensive customization options.\n\nZsh is POSIX-compatible when needed but extends well beyond POSIX with associative arrays, floating-point math, and advanced parameter expansion."],["KEY FEATURES","**Completion**: Context-aware, programmable, menu selection\n**Globbing**: **\\*\\*/** recursive, **(.)** qualifiers for filtering\n**History**: Shared, searchable, substring matching\n**Prompts**: Highly customizable with themes\n**Aliases**: Global aliases, suffix aliases\n**Arrays**: Indexed and associative arrays"],["CAVEATS","Zsh syntax differs from bash in some areas (array indexing starts at 1 by default, different parameter expansion syntax). Scripts may need adjustment.\n\nHeavy customization can slow shell startup. Profile with **zprof** and lazy-load plugins for faster interactive use.\n\nThe default completion system needs initialization with **compinit**. Most frameworks handle this automatically."],["HISTORY","Zsh was created by **Paul Falstad** at Princeton University in **1990**, named after Yale professor Zhong Shao (zsh = Zhong SHell). It has grown into one of the most feature-rich shells available and became the default shell in macOS Catalina (2019)."],["INSTALL","```apt: sudo apt install zsh```\n\n```dnf: sudo dnf install zsh```\n\n```pacman: sudo pacman -S zsh```\n\n```apk: sudo apk add zsh```\n\n```zypper: sudo zypper install zsh```\n\n```brew: brew install zsh```\n\n```nix: nix profile install nixpkgs#zsh```"],["SEE ALSO","[bash](/man/bash)(1), [sh](/man/sh)(1), [fish](/man/fish)(1), [ksh](/man/ksh)(1)"]],"e":["bash","sh","fish","ksh"],"x":"zsh"}});
