window.LCL_BUCKET(76,{"adb-shell":{"n":"adb-shell","t":"Run a remote shell on an Android device","s":[["TAGLINE","Run a remote shell on an Android device"],["TLDR","**Open** an interactive shell on the device\n\n```adb shell```\n\n**Run** a single command\n\n```adb shell [ls /sdcard]```\n\nRun command as **root** (if available)\n\n```adb shell su -c \"[command]\"```\n\n**List** files with details\n\n```adb shell ls -la [/system/]```"],["SYNOPSIS","**adb shell** [_-e escape_] [_-n_] [_-T|-t_] [_-x_] [_command_]"],["DESCRIPTION","**adb shell** provides shell access to a connected Android device. Without arguments, it opens an interactive shell session. With arguments, it executes the specified command and returns the output.\n\nThe shell is a limited Unix environment with access to Android-specific commands like **am** (activity manager), **pm** (package manager), **dumpsys**, and **content**. Available commands depend on the device and whether root access is available."],["PARAMETERS","**-e** _char_\n> Set escape character (default: ~)\n\n**-n**\n> Don't read from stdin\n\n**-T**\n> Disable PTY allocation\n\n**-t**\n> Force PTY allocation\n\n**-x**\n> Disable remote exit codes"],["CAVEATS","Shell commands run as the \"shell\" user with limited permissions. Root access requires an unlocked bootloader or rooted device. Not all standard Unix commands are available. The shell environment may vary between Android versions and manufacturers."],["HISTORY","**adb shell** has been part of the Android SDK since its initial release in **2008**. The shell has evolved from a basic Toolbox implementation to the more capable Toybox on modern Android versions."],["INSTALL","```apt: sudo apt install adb```\n\n```dnf: sudo dnf install android-tools```\n\n```pacman: sudo pacman -S android-tools```\n\n```apk: sudo apk add android-tools-adb```\n\n```zypper: sudo zypper install android-tools```\n\n```nix: nix profile install nixpkgs#android-tools```"],["SEE ALSO","[adb](/man/adb)(1), [adb-shell-pm](/man/adb-shell-pm)(1), [adb-shell-pm-list-packages](/man/adb-shell-pm-list-packages)(1), [adb-install](/man/adb-install)(1), [toybox](/man/toybox)(1)"],["RESOURCES","```[Source code](https://android.googlesource.com/platform/packages/modules/adb/)```\n\n```[Documentation](https://developer.android.com/tools/adb)```"]],"e":["adb","adb-shell-pm","adb-shell-pm-list-packages","adb-install","toybox"],"x":"adb shell"},"apt-clone":{"n":"apt-clone","t":"Backup and restore installed package states","s":[["TAGLINE","Backup and restore installed package states"],["TLDR","**Clone** the package state of the current system into a directory\n\n```sudo apt-clone clone [path/to/directory]```\n\nClone with packages not available in **repositories**\n\n```sudo apt-clone clone --with-dpkg-repack [path/to/directory]```\n\n**Restore** the package state from a clone file\n\n```sudo apt-clone restore [path/to/apt-clone-state-hostname.tar.gz]```\n\nRestore into a **specific directory** (debootstrap)\n\n```sudo apt-clone restore [path/to/clone.tar.gz] --destination [path/to/directory]```\n\nShow **information** about a clone file\n\n```apt-clone info [path/to/apt-clone-state-hostname.tar.gz]```\n\nRestore and **upgrade** to a new distribution release\n\n```sudo apt-clone restore-new-distro [path/to/clone.tar.gz] [new_distro_codename]```"],["SYNOPSIS","**apt-clone** _command_ [_options_]"],["DESCRIPTION","**apt-clone** creates and restores backups of the installed package state on Debian-based systems. It captures information about installed packages, their versions, and repository sources, enabling system migration or recovery.\n\nClone files are compressed tar archives containing package lists and APT source configurations. This allows replicating a system's software configuration on another machine or after a fresh installation."],["SUBCOMMANDS","**clone** _destination_\n> Create a clone file of the current package state into the specified destination directory.\n\n**restore** _source_ [**--destination** _path_]\n> Restore packages from a clone file. Optionally debootstrap into a specific directory.\n\n**restore-new-distro** _source_ _new_distro_codename_\n> Restore a clone file while attempting to upgrade packages to a new distribution release.\n\n**info** _source_\n> Display information about a clone file."],["PARAMETERS","**--source** _path_\n> Specify an alternative source directory for cloning (default is /).\n\n**--destination** _path_\n> Specify the destination directory for restore (debootstraps the clone into this directory).\n\n**--with-dpkg-repack**\n> Include package files for packages no longer available in repositories.\n\n**--with-dpkg-status**\n> Include the full dpkg status file.\n\n**-h**, **--help**\n> Show help message and exit.\n\n**--debug**\n> Enable debug output."],["CAVEATS","Restoring a clone on a different release version may fail due to package incompatibilities; use **restore-new-distro** for cross-release restores. The clone and restore operations require root privileges. An active internet connection is needed to download packages during restore. Packages not available in configured repositories will be skipped unless **--with-dpkg-repack** was used during cloning."],["SEE ALSO","[apt](/man/apt)(8), [dpkg](/man/dpkg)(1), [apt-get](/man/apt-get)(8), [apt-mark](/man/apt-mark)(8)"]],"e":["apt","dpkg","apt-get","apt-mark"],"x":"sudo apt-clone clone [path/to/directory]"},"aspell":{"n":"aspell","t":"Interactive spell checker","s":[["TAGLINE","Interactive spell checker"],["TLDR","**Check** a file\n\n```aspell check [file.txt]```\n\nCheck with **specific language**\n\n```aspell --lang=[es] check [file.txt]```\n\n**List** available dictionaries\n\n```aspell dicts```\n\nCheck from **stdin**\n\n```echo \"speling errror\" | aspell list```\n\n**Check a TeX/LaTeX file** (ignoring TeX commands)\n\n```aspell --mode=tex check [file.tex]```\n\nCreate **personal dictionary**\n\n```aspell --lang=[en] create master [./custom.rws] < [wordlist.txt]```"],["SYNOPSIS","**aspell** [_options_] _mode_ [_file_]"],["DESCRIPTION","**aspell** is a spell checker designed to replace ispell. It can check individual files, work as a pipe filter, or provide a C library interface for other programs.\n\nThe tool supports multiple languages, personal dictionaries, and has better suggestion algorithms than older spell checkers."],["PARAMETERS","**check** _file_\n> Interactively check file\n\n**list**\n> List misspelled words from stdin\n\n**pipe**\n> Output misspelled words with position info (used by editors)\n\n**-c** _file_\n> Same as check (GNU ispell compatibility)\n\n**--lang=**_code_\n> Language code (en, es, fr, etc.)\n\n**--mode=**_mode_\n> Parsing mode (tex, html, markdown, etc.)\n\n**--encoding=**_enc_\n> File encoding\n\n**--add-extra-dicts=**_list_\n> Additional dictionaries\n\n**--personal=**_file_\n> Personal dictionary file\n\n**--ignore=**_n_\n> Ignore words ≤ n characters\n\n**--ignore-case**\n> Case insensitive checking\n\n**dicts**\n> List available dictionaries\n\n**dump** _mode_\n> Dump dictionary information"],["CONFIGURATION","**/etc/aspell.conf**\n> System-wide configuration for default language, dictionary paths, and options.\n\n**~/.aspell.conf**\n> Per-user configuration overriding system defaults."],["CAVEATS","Language dictionaries must be installed separately. Personal dictionary format differs from ispell. Some applications use hunspell instead."],["HISTORY","**aspell** was created by Kevin Atkinson and first released in **2001** as an improvement over ispell, featuring better suggestion algorithms and Unicode support."],["INSTALL","```apt: sudo apt install aspell```\n\n```dnf: sudo dnf install aspell```\n\n```pacman: sudo pacman -S aspell```\n\n```apk: sudo apk add aspell```\n\n```zypper: sudo zypper install aspell```\n\n```brew: brew install aspell```\n\n```nix: nix profile install nixpkgs#aspell```"],["SEE ALSO","[ispell](/man/ispell)(1), [hunspell](/man/hunspell)(1), [spell](/man/spell)(1)"]],"e":["ispell","hunspell","spell"],"x":"aspell check [file.txt]"},"az-login":{"n":"az-login","t":"Authenticate with Azure","s":[["TAGLINE","Authenticate with Azure"],["TLDR","**Login** interactively\n\n```az login```\n\nLogin with **service principal** using a client secret\n\n```az login --service-principal -u [app-id] -p [client-secret] --tenant [tenant-id]```\n\nLogin with **service principal** using a certificate\n\n```az login --service-principal -u [app-id] --certificate [/path/to/cert.pem] --tenant [tenant-id]```\n\nLogin with **managed identity**\n\n```az login --identity```\n\nLogin with **device code**\n\n```az login --use-device-code```\n\nLogin to **specific tenant**\n\n```az login --tenant [tenant-id]```"],["SYNOPSIS","**az login** [_options_]"],["DESCRIPTION","**az login** authenticates the Azure CLI with your Azure account. It supports multiple authentication methods including interactive browser login, service principals, managed identities, and device code flow.\n\nBy default it logs in with a user account: it uses Web Account Manager (WAM) on Windows and browser-based login on Linux and macOS, falling back to device code flow when no browser is available. The command stores authentication tokens locally for subsequent Azure CLI commands."],["PARAMETERS","**--service-principal**\n> Login as service principal\n\n**-u**, **--username** _name_\n> User name or service principal client ID\n\n**-p**, **--password** _password_\n> User password or service principal secret. Prompts if not given. No longer accepts a certificate; use --certificate instead\n\n**--certificate** _path_\n> PEM file with the key and public certificate for a service principal\n\n**-t**, **--tenant** _id_\n> Microsoft Entra tenant ID or domain. Required for a service principal\n\n**--identity**, **-i**\n> Login using a managed identity\n\n**--client-id** _id_\n> Client ID of a user-assigned managed identity (with --identity)\n\n**--federated-token** _token_\n> Federated token for OIDC token exchange (e.g. GitHub Actions, Workload Identity)\n\n**--use-device-code**\n> Use device code flow (for systems without a browser)\n\n**--allow-no-subscriptions**\n> Allow login to tenants without subscriptions, useful for tenant-level commands like az ad\n\n**--skip-subscription-discovery**\n> Skip subscription discovery during login. Requires --tenant\n\n**--scope** _scope_\n> OAuth scope used in the authorize request"],["AUTHENTICATION METHODS","**Interactive** (default)\n> Opens browser for authentication\n\n**Service Principal**\n> Automated authentication for applications\n\n**Managed Identity**\n> For Azure resources (VMs, App Service, etc.)\n\n**Device Code**\n> For systems without browser access"],["WORKFLOW","```bash"],["Interactive login","az login"],["Select subscription","az account set --subscription \"My Subscription\""],["Verify login","az account show\n```"],["CAVEATS","Tokens stored in ~/.azure directory. Interactive login requires browser. Service principal credentials should be secured. Managed identity only works on Azure resources. Multiple logins create multiple contexts."],["HISTORY","**az login** has been part of Azure CLI since version 2.0 in **2017**, supporting various authentication methods as Azure's identity platform evolved."],["INSTALL","```dnf: sudo dnf install azure-cli```\n\n```pacman: sudo pacman -S azure-cli```\n\n```zypper: sudo zypper install azure-cli```\n\n```brew: brew install azure-cli```\n\n```nix: nix profile install nixpkgs#azure-cli```"],["SEE ALSO","[az](/man/az)(1), [az-logout](/man/az-logout)(1), [az-account](/man/az-account)(1)"],["RESOURCES","```[Source code](https://github.com/Azure/azure-cli)```\n\n```[Documentation](https://learn.microsoft.com/en-us/cli/azure/reference-index)```"]],"e":["az","az-logout","az-account"],"x":"az login"},"btop":{"n":"btop","t":"Terminal-based resource monitor with GPU support","s":[["TAGLINE","Terminal-based resource monitor with GPU support"],["TLDR","**Start** resource monitor\n\n```btop```\n\nStart with a specific **update rate** in milliseconds\n\n```btop -u [500]```\n\nStart with a **process filter**\n\n```btop -f [process_name]```\n\nStart with a **preset** layout (0-9)\n\n```btop -p [0]```\n\nStart in **TTY mode** (16 colors, ANSI graph symbols)\n\n```btop -t```"],["SYNOPSIS","**btop** [_options_]"],["PARAMETERS","**-p**, **--preset** _0-9_\n> Start with a preset layout\n\n**-u**, **--update** _ms_\n> Set initial update rate in milliseconds\n\n**-f**, **--filter** _filter_\n> Set initial process filter\n\n**-t**, **--tty**\n> Force tty mode\n\n**-l**, **--low-color**\n> 256 colors only (no true color)\n\n**-c**, **--config** _file_\n> Path to config file\n\n**--force-utf**\n> Force start even if no UTF-8 locale was detected\n\n**--themes-dir** _dir_\n> Path to custom themes directory\n\n**--no-tty**\n> Force disable tty mode\n\n**--default-config**\n> Print default config to standard output\n\n**-d**, **--debug**\n> Start in debug mode with additional logs\n\n**-V**, **--version**\n> Show version\n\n**-h**, **--help**\n> Show help"],["DESCRIPTION","**btop++** (btop) is a resource monitor written in C++ showing CPU, memory, disks, network, and processes with a visually polished terminal UI. It's the successor to bashtop and bpytop, offering better performance and more features.\n\nThe tool provides comprehensive system monitoring with GPU support, mouse interaction, and extensive customization."],["FEATURES","- CPU usage per core with graphs\n- Memory and swap monitoring\n- Disk I/O and usage\n- Network bandwidth\n- Process management\n- GPU monitoring (NVIDIA, AMD)\n- Mouse support\n- Vim-like keybindings\n- Customizable themes\n- Process filtering and tree view\n- Battery monitoring"],["KEYBINDINGS","- **q** - Quit\n- **ESC/m** - Menu\n- **f** - Filter processes\n- **t** - Tree view\n- **k** - Kill process\n- **+/-** - Adjust update interval\n- **Arrow keys** - Navigate\n- **Mouse** - Click to interact"],["INTERFACE BOXES","**CPU**\n> Per-core usage, temperature, frequency\n\n**Memory**\n> RAM, swap, cache with graphs\n\n**Disks**\n> I/O activity, space usage\n\n**Network**\n> Bandwidth by interface\n\n**Processes**\n> Detailed sortable list"],["CONFIGURATION","**~/.config/btop/btop.conf**\n> Main configuration file for settings like update interval, theme, and layout.\n\n**~/.config/btop/themes/**\n> Directory for custom color themes."],["CAVEATS","Requires modern terminal with true color support. GPU monitoring needs appropriate drivers. Higher resource usage than minimal tools like top. Some features platform-specific."],["HISTORY","**btop++** was created by **Jakob P. Liljenberg** (aristocratos) in **2021** as a C++ rewrite of bpytop, providing significantly better performance and additional features."],["INSTALL","```apt: sudo apt install btop```\n\n```dnf: sudo dnf install btop```\n\n```pacman: sudo pacman -S btop```\n\n```apk: sudo apk add btop```\n\n```zypper: sudo zypper install btop```\n\n```brew: brew install btop```\n\n```nix: nix profile install nixpkgs#btop```"],["SEE ALSO","[htop](/man/htop)(1), [bpytop](/man/bpytop)(1), [top](/man/top)(1)"]],"e":["htop","bpytop","top"],"x":"btop"},"bun-audit":{"n":"bun-audit","t":"Scan dependencies for known security vulnerabilities","s":[["TAGLINE","Scan dependencies for known security vulnerabilities"],["TLDR","**Audit dependencies** for known security vulnerabilities\n\n```bun audit```\n\n**Only report high and critical** vulnerabilities\n\n```bun audit --audit-level=[high]```\n\n**Audit only production dependencies**, skipping devDependencies\n\n```bun audit --prod```\n\n**Ignore one or more vulnerabilities** by CVE ID (repeat flag)\n\n```bun audit --ignore [CVE-2023-12345] --ignore [CVE-2023-67890]```\n\n**Output raw JSON** from the registry (for CI parsing)\n\n```bun audit --json```"],["SYNOPSIS","**bun** **audit** [_options_]"],["PARAMETERS","**--audit-level**=_level_\n> Only report vulnerabilities of this severity or higher. Values: **low**, **moderate**, **high**, **critical**.\n\n**--prod**\n> Audit only production dependencies, ignoring **devDependencies**.\n\n**--ignore** _CVE_\n> Ignore a specific CVE. Can be repeated to ignore multiple advisories.\n\n**--json**\n> Print the raw JSON response from the npm registry instead of the formatted report."],["DESCRIPTION","**bun audit** sends the list of installed packages and versions (resolved from **bun.lock**) to the npm registry and prints a report of any vulnerabilities returned. The registry cross-references the GitHub Advisory Database, so coverage matches what **npm audit** would report.\n\nThe command categorizes findings by severity (**low**, **moderate**, **high**, **critical**) and covers direct dependencies, **devDependencies**, **bundledDependencies**, and **optionalDependencies**."],["EXIT STATUS","**0**\n> No vulnerabilities were found.\n\n**1**\n> One or more vulnerabilities were reported."],["CAVEATS","Only catches known, documented advisories. Does not detect zero-day exploits, malware, misconfigurations, or supply-chain attacks not yet reported. A clean audit does not guarantee security. Requires **bun.lock** to be present, so run **bun install** first. Unlike **npm audit fix**, **bun audit** currently has no automatic fix mode - you must bump versions manually."],["HISTORY","**bun audit** was introduced in **Bun v1.2.15** (mid 2025), providing **npm audit**-compatible security scanning for projects using the Bun package manager."],["SEE ALSO","[bun](/man/bun)(1), [bun-install](/man/bun-install)(1), [npm-audit](/man/npm-audit)(1), [snyk](/man/snyk)(1)"]],"e":["bun","bun-install","npm-audit","snyk"],"x":"bun audit"},"chage":{"n":"chage","t":"change user password expiry information","s":[["TAGLINE","change user password expiry information"],["TLDR","List **password information** for user\n\n```chage -l username```\n\nSet password to **expire in 10 days**\n\n```sudo chage -M 10 username```\n\n**Disable** password expiration\n\n```sudo chage -M -1 username```\n\nSet **account expiration** date\n\n```sudo chage -E 2025-12-31 username```\n\nForce user to **change password** on next login\n\n```sudo chage -d 0 username```\n\n**Re-enable** an expired account\n\n```sudo chage -E -1 username```"],["SYNOPSIS","**chage** [_options_] _LOGIN_"],["DESCRIPTION","**chage** changes user password expiry information stored in /etc/shadow. It controls the number of days between required password changes, the date of the last password change, and account expiration dates.\n\nSystem administrators use chage to enforce password policies, such as requiring users to change passwords at regular intervals, setting grace periods after expiration, and disabling accounts on a specific date. The -l option allows any user to view their own aging information without root privileges.\n\nSetting the last password change date to 0 with -d forces an immediate password change on the user's next login, which is commonly used when creating new accounts or after a security incident."],["PARAMETERS","**-d, --lastday LAST_DAY**\n> Set the date of last password change (YYYY-MM-DD or days since epoch)\n\n**-E, --expiredate EXPIRE_DATE**\n> Set account expiration date; use -1 to remove expiration\n\n**-i, --iso8601**\n> Output dates in ISO 8601 format\n\n**-I, --inactive DAYS**\n> Days of inactivity after password expiration before account lockout\n\n**-l, --list**\n> Display account aging information\n\n**-m, --mindays MIN_DAYS**\n> Minimum days between password changes\n\n**-M, --maxdays MAX_DAYS**\n> Maximum days before password change required\n\n**-W, --warndays WARN_DAYS**\n> Days before expiration to warn user"],["CONFIGURATION","**/etc/shadow**\n> Password aging data for each user account.\n\n**/etc/login.defs**\n> System-wide defaults for password aging policies."],["CAVEATS","Root privileges are required except when using **-l** to list your own information. Setting **-d 0** forces an immediate password change on next login."],["HISTORY","**chage** is part of the **shadow-utils** package. It manages password aging as defined in the /etc/shadow file."],["INSTALL","```apt: sudo apt install passwd```\n\n```pacman: sudo pacman -S shadow```\n\n```apk: sudo apk add shadow```\n\n```zypper: sudo zypper install shadow```\n\n```nix: nix profile install nixpkgs#shadow```"],["SEE ALSO","[passwd](/man/passwd)(1), [usermod](/man/usermod)(8), [chpasswd](/man/chpasswd)(8)"]],"e":["passwd","usermod","chpasswd"],"x":"chage -l username"},"checksec":{"n":"checksec","t":"audit security features in ELF binaries","s":[["TAGLINE","audit security features in ELF binaries"],["TLDR","**Check security properties of a binary**\n\n```checksec --file=[path/to/binary]```\n\n**Check security properties with JSON output**\n\n```checksec --file=[path/to/binary] --output=json```\n\n**Check the running kernel's security features**\n\n```checksec --kernel```\n\n**Check security properties of a running process**\n\n```checksec --proc [pid]```\n\n**Scan all binaries in a directory**\n\n```checksec --dir [path/to/directory]```\n\n**Check all running processes**\n\n```checksec --proc-all```"],["SYNOPSIS","**checksec** [**--file** _binary_] [**--dir** _directory_] [**--proc** _pid_] [**--kernel**] [**--output** _format_]"],["DESCRIPTION","**checksec** audits security features enabled in compiled Linux executables (ELF files), the kernel, or running processes. It helps security researchers and administrators assess the exploitability posture of binaries by checking for common mitigations.\n\n**Security properties checked:**\n\n**RELRO** (Relocation Read-Only) - Protects GOT/PLT from being overwritten. Full RELRO marks these as read-only after dynamic linking.\n\n**Stack Canary** - Random value placed before return addresses to detect buffer overflows.\n\n**NX** (No eXecute) - Marks memory regions as non-executable, preventing code execution from stack/heap.\n\n**PIE** (Position Independent Executable) - Enables ASLR by allowing random base address loading.\n\n**FORTIFY** - Compile-time buffer overflow checks for common functions."],["PARAMETERS","**--file** _binary_\n> Check security properties of specified binary\n\n**--dir** _directory_\n> Scan all binaries in directory\n\n**--proc** _pid_\n> Check running process by PID\n\n**--proc-all**\n> Check all running processes\n\n**--kernel**\n> Check kernel security features\n\n**--output** _format_\n> Output format: cli, csv, json, xml\n\n**--fortify-file** _binary_\n> Detailed fortify analysis"],["CAVEATS","Color-coded output indicates security status: green for enabled protections, red for missing. Red doesn't always indicate a vulnerability—distribution vendors may make intentional tradeoffs when compiling binaries."],["HISTORY","Originally written by **Tobias Klein** at trapkit.de. The tool has been actively maintained and expanded, moving from Bash to Golang, with the current version maintained at github.com/slimm609/checksec."],["INSTALL","```dnf: sudo dnf install checksec```\n\n```pacman: sudo pacman -S checksec```\n\n```apk: sudo apk add checksec-rs```\n\n```zypper: sudo zypper install checksec```\n\n```brew: brew install checksec```\n\n```nix: nix profile install nixpkgs#checksec```"],["SEE ALSO","[readelf](/man/readelf)(1), [objdump](/man/objdump)(1), [file](/man/file)(1)"],["RESOURCES","```[Source code](https://github.com/slimm609/checksec)```"]],"e":["readelf","objdump","file"],"x":"checksec --file=[path/to/binary]"},"conda-export":{"n":"conda-export","t":"environment specification exporter","s":[["TAGLINE","environment specification exporter"],["TLDR","**Export current environment**\n\n```conda export > [environment.yml]```\n\n**Export without builds**\n\n```conda export --no-builds > [environment.yml]```\n\n**Export from history** only\n\n```conda export --from-history > [environment.yml]```"],["SYNOPSIS","**conda** **export** [_options_]"],["PARAMETERS","**-n**, **--name** _name_\n> Name of environment to export. Defaults to the active environment.\n\n**-p**, **--prefix** _path_\n> Full path to the environment to export (alternative to **--name**).\n\n**-f**, **--file** _file_\n> Write output to a file. Format is auto-detected from the filename. Defaults to standard output.\n\n**--format** _format_\n> Output format: yaml (default), json, explicit, or requirements.\n\n**--no-builds**\n> Exclude build strings from the dependency list, making the file more portable.\n\n**--from-history**\n> Only include packages explicitly requested, omitting transitive dependencies.\n\n**-c**, **--channel** _channel_\n> Additional channel to include in the export.\n\n**--override-channels**\n> Do not include channels from .condarc.\n\n**--ignore-channels**\n> Do not prepend channel names to package specs.\n\n**--json**\n> Report output as JSON, suitable for programmatic use."],["CONFIGURATION","**~/.condarc**\n> User-level conda configuration for channels, package settings, and solver options.\n\n**/opt/conda/.condarc**\n> System-wide conda configuration."],["DESCRIPTION","**conda export** outputs the current environment specification in YAML format by default. This can be used to recreate the environment on another system with **conda env create -f environment.yml**.\n\n**conda export** is the newer subcommand and supports multiple output formats via **--format**. The traditional **conda env export** remains supported and produces equivalent YAML.\n\nUse **--from-history** for more portable exports that omit transitive dependencies and platform-specific build strings."],["INSTALL","```dnf: sudo dnf install conda```\n\n```nix: nix profile install nixpkgs#conda```"],["SEE ALSO","[conda](/man/conda)(1), [conda-env](/man/conda-env)(1), [conda-list](/man/conda-list)(1)"]],"e":["conda","conda-env","conda-list"],"x":"conda export > [environment.yml]"},"containerd":{"n":"containerd","t":"industry-standard container runtime daemon","s":[["TAGLINE","industry-standard container runtime daemon"],["TLDR","**Start containerd** daemon\n\n```sudo containerd```\n\n**Start with a specific configuration** file\n\n```sudo containerd --config [/etc/containerd/config.toml]```\n\n**Start with debug logging**\n\n```sudo containerd --log-level debug```\n\n**Generate default configuration**\n\n```containerd config default > [/etc/containerd/config.toml]```\n\n**Check containerd version**\n\n```containerd --version```\n\n**Start with a specific root directory**\n\n```sudo containerd --root [/var/lib/containerd]```"],["SYNOPSIS","**containerd** [_options_]"],["PARAMETERS","**--config** _FILE_\n> Path to configuration file (default: /etc/containerd/config.toml).\n\n**--root** _DIR_\n> Root directory for containerd data.\n\n**--state** _DIR_\n> State directory for containerd.\n\n**--address** _SOCKET_\n> Address for containerd's gRPC socket.\n\n**--log-level** _LEVEL_\n> Logging level: trace, debug, info, warn, error, fatal, panic.\n\n**config default**\n> Output the default configuration to stdout.\n\n**config dump**\n> Output the current configuration.\n\n**--version**\n> Display version information.\n\n**--help**\n> Display help information."],["CONFIGURATION","**/etc/containerd/config.toml**\n> Main configuration file for containerd daemon settings, runtime options, and plugin configurations."],["DESCRIPTION","**containerd** is an industry-standard container runtime that manages the complete container lifecycle on a host system. It handles image transfer and storage, container execution and supervision, low-level storage, and network attachments.\n\nAs a daemon, containerd provides a gRPC API for higher-level tools like Docker, Kubernetes, and nerdctl to interact with. It supports OCI (Open Container Initiative) images and runtime specifications, ensuring compatibility across the container ecosystem.\n\ncontainerd is designed to be embedded into larger systems rather than used directly by end users. Kubernetes uses containerd as its container runtime through the CRI (Container Runtime Interface), and Docker uses it as its core runtime engine."],["CAVEATS","containerd is typically managed as a system service rather than run directly. Direct interaction requires tools like **ctr** or **nerdctl**. Proper configuration is essential for security, especially regarding runtime options and namespace isolation. Root privileges are required for most operations."],["HISTORY","containerd was originally developed as part of Docker and was spun out as an independent project in **December 2016**. It was donated to the Cloud Native Computing Foundation (CNCF) in **2017** and graduated as a CNCF project in **2019**, becoming the standard container runtime for Kubernetes."],["INSTALL","```apt: sudo apt install containerd```\n\n```dnf: sudo dnf install containerd```\n\n```pacman: sudo pacman -S containerd```\n\n```apk: sudo apk add containerd```\n\n```zypper: sudo zypper install containerd```\n\n```brew: brew install containerd```\n\n```nix: nix profile install nixpkgs#containerd```"],["SEE ALSO","[ctr](/man/ctr)(1), [nerdctl](/man/nerdctl)(1), [docker](/man/docker)(1), [runc](/man/runc)(1)"],["RESOURCES","```[Source code](https://github.com/containerd/containerd)```\n\n```[Homepage](https://containerd.io)```\n\n```[Documentation](https://containerd.io/docs/)```"]],"e":["ctr","nerdctl","docker","runc"],"x":"sudo containerd"},"conv":{"n":"conv","t":"unit conversion terminal utility","s":[["TAGLINE","unit conversion terminal utility"],["TLDR","**Convert a value** by appending its unit abbreviation\n\n```conv [100f]```\n\n**Convert metres to other distance units**\n\n```conv [50m]```\n\n**Convert weight** in kilograms\n\n```conv [75kg]```\n\n**Convert data transfer speed** in megabits per second\n\n```conv [100mbps]```\n\n**Display help** and list all supported unit abbreviations\n\n```conv --help```"],["SYNOPSIS","**conv** _value__unit_\n\n**conv** **-h**|**--help**\n\n**conv** **-v**|**--version**"],["PARAMETERS","**-h**, **--help**\n> Display help information and list all supported unit abbreviations.\n\n**-v**, **--version**\n> Display version information."],["DESCRIPTION","**conv** is a lightweight, self-contained terminal utility for converting everyday units of measurement. A value and its unit abbreviation are written together as a single argument (e.g. `100f` for 100 degrees Fahrenheit), and conv prints the equivalent values in all other units of the same category.\n\nSupported categories and their abbreviations:\n\n- **Temperature:** c (Celsius), f (Fahrenheit)\n- **Distance:** cm (centimetres), ft (feet), in (inches), km (kilometres), m (metres), mi (miles), nm (nautical miles), yd (yards)\n- **Weight:** ct (carats), g (grams), kg (kilograms), oz (ounces), lb (pounds), st (stone)\n- **Volume:** cum (cubic metres), l (litres), guk (imperial gallons), gus (US gallons)\n- **Speed:** kmh (km/h), kn (knots), mph (miles per hour), mps (metres per second)\n- **Data rate:** bps, kbps, mbps (bits per second family); bs, kbs, mbs (bytes per second family)\n- **Power:** hp (horsepower), w (watts)\n- **Other:** bbl (petroleum barrels)"],["CAVEATS","The value and unit abbreviation must be written as a single argument with no space between them (e.g. `conv 100f`, not `conv 100 f`). Only the unit categories listed above are supported; currency and arbitrary unit expressions are not handled. The project was archived in 2019 and is no longer actively maintained."],["HISTORY","**conv** was written by Ben Garrett and implemented in Go. Precompiled binaries were provided for Linux, macOS, Windows, and ARM. The repository at github.com/bengarrett/conv was archived in September 2019."],["INSTALL","```apt: sudo apt install gmtsar-core```"],["SEE ALSO","[units](/man/units)(1), [bc](/man/bc)(1), [qalc](/man/qalc)(1)"]],"e":["units","bc","qalc"],"x":"conv [100f]"},"depsguard":{"n":"depsguard","t":"Harden npm, pnpm, yarn, bun and uv configurations against supply-chain risk","s":[["TAGLINE","Harden npm, pnpm, yarn, bun and uv configurations against supply-chain risk"],["TLDR","**Launch** the interactive TUI to scan and apply fixes\n\n```depsguard```\n\n**Scan only** and print a read-only report\n\n```depsguard scan```\n\n**Skip recursive file discovery** and check only local configs\n\n```depsguard --no-search```\n\n**Restore** a previous backup created by depsguard\n\n```depsguard restore```\n\n**Display** help\n\n```depsguard --help```"],["SYNOPSIS","**depsguard** [_scan_|_restore_] [_options_]"],["DESCRIPTION","**depsguard** is a Rust CLI that audits JavaScript and Python package manager configuration files for supply-chain hardening settings and applies missing protections after explicit user approval.\n\nIt inspects user-level files such as `~/.npmrc`, `~/.yarnrc.yml`, `~/.bunfig.toml` and `uv.toml`, and repository-level files including `package.json`, lockfiles, Renovate config and Dependabot config. It checks for settings like minimum release age (delaying installation of newly published versions), blocking install scripts, restricting exotic transitive dependencies, trust policies, and strict build requirements.\n\nIn its default interactive mode depsguard presents detected issues in a TUI, lets the user select which fixes to apply, and writes timestamped backups under `~/.depsguard/backups/` before modifying any file. The `restore` subcommand replays a chosen backup.\n\nThe tool itself never installs packages and ships with zero third-party crate dependencies."],["PARAMETERS","**scan**\n> Read-only report. Does not modify any files.\n\n**restore**\n> Recover configuration from a timestamped backup.\n\n**--no-search**\n> Check only local config files in the current directory. Skip recursive discovery.\n\n**--help**\n> Show CLI documentation."],["CONFIGURATION","Backups are written to `~/.depsguard/backups/` before each apply operation."],["CAVEATS","Some hardening options require recent package manager versions (for example npm 11.10 or newer for certain flags). A VT-capable terminal is recommended for proper TUI rendering. Building from source requires Rust 1.74 or newer."],["INSTALL","```brew: brew install depsguard```"],["SEE ALSO","[npm](/man/npm)(1), [pnpm](/man/pnpm)(1), [yarn](/man/yarn)(1), [bun](/man/bun)(1), [uv](/man/uv)(1)"]],"e":["npm","pnpm","yarn","bun","uv"],"x":"depsguard"},"dlv":{"n":"dlv","t":"Go programming language debugger","s":[["TAGLINE","Go programming language debugger"],["TLDR","**Debug an executable**\n\n```dlv exec [./binary]```\n\n**Debug with arguments**\n\n```dlv exec [./binary] -- [arg1] [arg2]```\n\n**Debug a Go package**\n\n```dlv debug [package]```\n\n**Attach to process**\n\n```dlv attach [pid]```\n\n**Debug a test**\n\n```dlv test [package]```\n\n**Start headless server**\n\n```dlv debug --headless --listen=:[2345]```\n\n**Connect to remote**\n\n```dlv connect [localhost:2345]```"],["SYNOPSIS","**dlv** _command_ [_options_] [_args_]"],["PARAMETERS","_COMMAND_\n> Operation: debug, exec, test, attach, connect, etc.\n\n**debug** [_PACKAGE_]\n> Build and debug Go package.\n\n**exec** _BINARY_\n> Debug prebuilt binary.\n\n**test** [_PACKAGE_]\n> Debug tests.\n\n**attach** _PID_\n> Attach to running process.\n\n**connect** _ADDR_\n> Connect to headless server.\n\n**--headless**\n> Run in headless mode.\n\n**--listen** _ADDR_\n> Listen address for headless mode.\n\n**--help**\n> Display help information."],["DESCRIPTION","**dlv** (Delve) is a debugger for the Go programming language. It provides source-level debugging with support for goroutines, breakpoints, variable inspection, and expression evaluation.\n\nDelve understands Go's runtime, correctly handling goroutine scheduling, garbage collection, and other Go-specific features. It supports both local debugging and remote debugging through its headless mode.\n\nThe debugger integrates with editors and IDEs via the Debug Adapter Protocol (DAP), enabling graphical debugging in VS Code, GoLand, and other editors."],["CAVEATS","Requires Go binaries with debug info (default). Some optimizations may affect debugging. Attaching to processes needs appropriate permissions. Core dumps require compatible Go version."],["HISTORY","Delve was created by **Derek Parker** and released in **2014**. It was developed to provide Go developers with a debugger that understands Go's concurrency model and runtime, replacing GDB for most Go debugging tasks."],["INSTALL","```apt: sudo apt install delve```\n\n```dnf: sudo dnf install delve```\n\n```pacman: sudo pacman -S delve```\n\n```apk: sudo apk add delve```\n\n```zypper: sudo zypper install delve```\n\n```brew: brew install delve```\n\n```nix: nix profile install nixpkgs#delve```"],["SEE ALSO","[go](/man/go)(1), [gdb](/man/gdb)(1)"],["RESOURCES","```[Source code](https://github.com/go-delve/delve)```"]],"e":["go","gdb"],"x":"dlv exec [./binary]"},"docker-container-commit":{"n":"docker-container-commit","t":"create image from container state","s":[["TAGLINE","create image from container state"],["TLDR","**Create image from container**\n\n```docker container commit [container] [image:tag]```\n\n**Commit with message**\n\n```docker container commit -m \"[message]\" [container] [image:tag]```"],["SYNOPSIS","**docker** **container** **commit** [_options_] _container_ [_repository[:tag]_]"],["DESCRIPTION","**docker container commit** creates a new image from a container's changes. This is an alias for **docker commit**."],["INSTALL","```apt: sudo apt install docker-cli```\n\n```dnf: sudo dnf install docker-cli```\n\n```pacman: sudo pacman -S docker```\n\n```apk: sudo apk add docker-cli```\n\n```zypper: sudo zypper install docker```\n\n```brew: brew install docker```\n\n```nix: nix profile install nixpkgs#docker```"],["SEE ALSO","[docker-commit](/man/docker-commit)(1), [docker-container](/man/docker-container)(1)"],["RESOURCES","```[Source code](https://github.com/docker/cli)```\n\n```[Documentation](https://docs.docker.com/reference/cli/docker/)```"]],"e":["docker-commit","docker-container"],"x":"docker container commit [container] [image:tag]"},"dunstify":{"n":"dunstify","t":"desktop notification sender for dunst","s":[["TAGLINE","desktop notification sender for dunst"],["TLDR","**Send a notification**\n\n```dunstify \"[message]\"```\n\n**Notification with title**\n\n```dunstify \"[title]\" \"[body]\"```\n\n**Set urgency level**\n\n```dunstify -u [critical] \"[message]\"```\n\n**Set timeout**\n\n```dunstify -t [5000] \"[message]\"```\n\n**Add action button**\n\n```dunstify -A \"[action,label]\" \"[message]\"```\n\n**Replace notification**\n\n```dunstify -r [id] \"[updated message]\"```\n\n**Set icon**\n\n```dunstify -i [icon_name] \"[message]\"```\n\n**Print the notification ID** so it can be replaced later\n\n```dunstify -p \"[message]\"```\n\n**Close a notification** by ID\n\n```dunstify -C [id]```"],["SYNOPSIS","**dunstify** [_options_] _summary_ [_body_]"],["PARAMETERS","**-u**, **--urgency** _level_\n> Urgency (low, normal, critical).\n\n**-t**, **--timeout** _ms_\n> Timeout in milliseconds.\n\n**-i**, **--icon** _icon_\n> Icon name or path.\n\n**-r**, **--replace** _id_\n> Replace notification with ID.\n\n**-A**, **--action** _action,label_\n> Add action button.\n\n**-C**, **--close** _id_\n> Close notification by ID.\n\n**-h**, **--hints** _TYPE:NAME:VALUE_\n> Set a hint, e.g. `int:value:50` for a progress bar.\n\n**-p**, **--printid**\n> Print the notification ID (use it later with **-r** or **-C**).\n\n**-a**, **--appname** _name_\n> Name of the sending application.\n\n**-I**, **--raw_icon** _path_\n> Send the icon as raw image data instead of a name or path.\n\n**-b**, **--block**\n> Block until the notification is closed; exit code reports how it was closed.\n\n**-c**, **--capabilities**\n> Print the notification server capabilities and exit.\n\n**-s**, **--serverinfo**\n> Print notification server information and exit.\n\n**-v**, **--version**\n> Print version and exit."],["DESCRIPTION","**dunstify** is a notification client specifically designed for dunst, offering extended features beyond the standard notify-send utility. It implements the freedesktop.org notification specification with dunst-specific enhancements.\n\nThe tool's key advantage is notification replacement: by storing the notification ID from a previous call (via --printid), subsequent notifications can update the existing one rather than creating duplicates. This is essential for progress indicators, status updates, and volume/brightness notifications.\n\nAdditional features include action buttons that can trigger commands when clicked, custom hints for advanced styling, and the ability to close notifications programmatically. The tool integrates seamlessly with dunst's configuration for consistent appearance and behavior."],["INSTALL","```apt: sudo apt install dunst```\n\n```dnf: sudo dnf install dunst```\n\n```pacman: sudo pacman -S dunst```\n\n```apk: sudo apk add dunstify```\n\n```zypper: sudo zypper install dunst```\n\n```nix: nix profile install nixpkgs#dunst```"],["SEE ALSO","[dunst](/man/dunst)(1), [dunstctl](/man/dunstctl)(1), [notify-send](/man/notify-send)(1)"],["RESOURCES","```[Source code](https://github.com/dunst-project/dunst)```\n\n```[Homepage](https://dunst-project.org/)```\n\n```[Documentation](https://dunst-project.org/documentation/)```"]],"e":["dunst","dunstctl","notify-send"],"x":"dunstify \"[message]\""},"enum4linux":{"n":"enum4linux","t":"enumerate information from Windows and Samba systems","s":[["TAGLINE","enumerate information from Windows and Samba systems"],["TLDR","Enumerate using **all** methods\n\n```enum4linux -a [remote_host]```\n\nEnumerate using given **credentials**\n\n```enum4linux -u [user_name] -p [password] [remote_host]```\n\nList **usernames** from a host\n\n```enum4linux -U [remote_host]```\n\nList **shares**\n\n```enum4linux -S [remote_host]```\n\nGet **OS** information\n\n```enum4linux -o [remote_host]```"],["SYNOPSIS","**enum4linux** [_options_] _target_"],["DESCRIPTION","**enum4linux** enumerates information from Windows and Samba systems using SMB. It extracts user lists, share information, group membership, password policies, and operating system details.\n\nA Perl wrapper around tools like smbclient, rpcclient, and nmblookup for penetration testing."],["PARAMETERS","**-a**\n> Do all simple enumeration\n\n**-U**\n> Get user list\n\n**-S**\n> Get share list\n\n**-o**\n> Get OS information\n\n**-u** _user_\n> Username for authentication\n\n**-p** _password_\n> Password for authentication\n\n**-G**\n> Get group information\n\n**-P**\n> Get password policy\n\n**-r**\n> Enumerate users via RID cycling (works even when RestrictAnonymous blocks direct listing)\n\n**-M**\n> Get machine list\n\n**-d**\n> Be detailed, applies to -U and -S\n\n**-w** _workgroup_\n> Specify the workgroup or domain manually"],["CAVEATS","Security testing tool. Use only on systems you have permission to test. Some enumeration requires valid credentials or a null session, which modern Windows blocks by default. May trigger security alerts. The original Perl enum4linux is effectively unmaintained; **enum4linux-ng**, a Python rewrite, is the actively developed successor and produces cleaner, machine-readable output."],["INSTALL","```nix: nix profile install nixpkgs#enum4linux```"],["SEE ALSO","[smbclient](/man/smbclient)(1), [rpcclient](/man/rpcclient)(1), [nmap](/man/nmap)(1), [nmblookup](/man/nmblookup)(1)"],["RESOURCES","```[Source code](https://github.com/CiscoCXSecurity/enum4linux)```"]],"e":["smbclient","rpcclient","nmap","nmblookup"],"x":"enum4linux -a [remote_host]"},"fdroid":{"n":"fdroid","t":"F-Droid repository management tool","s":[["TAGLINE","F-Droid repository management tool"],["TLDR","**Update repository index**\n\n```fdroid update```\n\n**Build app from** source\n\n```fdroid build [app.id]```\n\n**Initialize repository**\n\n```fdroid init```\n\n**Publish signed apps**\n\n```fdroid publish```\n\n**Check metadata for issues**\n\n```fdroid lint [app.id]```\n\n**Check for app updates**\n\n```fdroid checkupdates [app.id]```"],["SYNOPSIS","**fdroid** _command_ [_options_]"],["PARAMETERS","_COMMAND_\n> Operation: update, build, init, publish, lint, etc.\n\n**update**\n> Update the repository index.\n\n**build** _APP_\n> Build application from source.\n\n**init**\n> Initialize new repository.\n\n**publish**\n> Publish signed apps.\n\n**lint** _APP_\n> Check metadata for issues.\n\n**checkupdates** _APP_\n> Check for available upstream updates.\n\n**scanner** _APP_\n> Scan APKs for known non-free libraries.\n\n**rewritemeta** _APP_\n> Reformat and normalize metadata files.\n\n**deploy**\n> Deploy the repository to a server.\n\n**import**\n> Import a new app from source repository.\n\n**readmeta**\n> Read and validate metadata.\n\n**-v**, **--verbose**\n> More verbose output.\n\n**--help**\n> Display help information."],["DESCRIPTION","**fdroid** is the server-side tool for managing F-Droid repositories. It builds Android apps from source, maintains repository metadata, and generates the index files that F-Droid clients use.\n\nThe tool handles the entire app publication workflow: building APKs from source, signing them, generating metadata, and publishing to repositories. It enforces F-Droid's commitment to open source by building from source code.\n\nfdroid is used by the official F-Droid repository and third-party repositories hosting open source Android applications."],["CONFIGURATION","**config.yml**\n> Repository configuration including repo name, description, and keystore settings.\n\n**metadata/**\n> Directory containing per-app metadata files describing build recipes and app information."],["CAVEATS","Requires Android SDK. Building apps needs significant resources. Repository setup is complex. Signing requires key management."],["HISTORY","fdroid is the server component of **F-Droid**, the open source Android app repository founded in 2010. It enables anyone to host their own repository of free software Android applications."],["SEE ALSO","[fdroidcl](/man/fdroidcl)(1), [apktool](/man/apktool)(1), [adb](/man/adb)(1)"]],"e":["fdroidcl","apktool","adb"],"x":"fdroid update"},"fincore":{"n":"fincore","t":"page cache inspector for files","s":[["TAGLINE","page cache inspector for files"],["TLDR","Display **cache details** for a file\n\n```fincore [path/to/file]```\n\nDisplay **all** possible data columns\n\n```fincore --output-all [path/to/file]```\n\nDisplay **help**\n\n```fincore [-h|--help]```"],["SYNOPSIS","**fincore** [_options_] _files_"],["DESCRIPTION","**fincore** displays how much of a file is currently cached in memory by the kernel's page cache. It shows the number of pages and bytes cached.\n\nUseful for understanding memory usage and cache behavior."],["PARAMETERS","**-n**, **--noheadings**\n> Do not print a header line.\n\n**-b**, **--bytes**\n> Print sizes in bytes (not human-readable units).\n\n**-c**, **--total**\n> Print a grand total of results.\n\n**-o**, **--output** _list_\n> Specify output columns (use _+list_ to extend defaults).\n\n**--output-all**\n> Show all available columns.\n\n**-r**, **--raw**\n> Produce output in raw format.\n\n**-J**, **--json**\n> Use JSON output format.\n\n**-R**, **--recursive**\n> Recurse into directories.\n\n**-h**, **--help**\n> Display help.\n\n**-V**, **--version**\n> Print version."],["CAVEATS","Part of util-linux. Shows point-in-time snapshot of cache state. Cache contents change dynamically based on system activity."],["INSTALL","```apt: sudo apt install util-linux-extra```\n\n```apk: sudo apk add util-linux-misc```"],["SEE ALSO","[fadvise](/man/fadvise)(1), [vmtouch](/man/vmtouch)(1), [free](/man/free)(1)"]],"e":["fadvise","vmtouch","free"],"x":"fincore [path/to/file]"},"flit":{"n":"flit","t":"simple Python packaging and publishing","s":[["TAGLINE","simple Python packaging and publishing"],["TLDR","**Initialize new project**\n\n```flit init```\n\n**Build package**\n\n```flit build```\n\n**Install in development mode**\n\n```flit install --symlink```\n\n**Publish to PyPI**\n\n```flit publish```\n\n**Publish to TestPyPI**\n\n```flit publish --repository testpypi```\n\n**Install only dependencies** (not the package itself)\n\n```flit install --only-deps```\n\n**Check project configuration**\n\n```flit check```"],["SYNOPSIS","**flit** [_options_] _command_ [_args_]"],["PARAMETERS","**init**\n> Create pyproject.toml interactively.\n\n**build**\n> Build wheel and sdist.\n\n**install**\n> Install locally.\n\n**install --symlink**\n> Symlink module into site-packages for development.\n\n**install --pth-file**\n> Use .pth file for development (works on Windows).\n\n**install --deps** _DEPS_\n> Which dependencies to install: all, production, develop, or none (default: all).\n\n**install --only-deps**\n> Install dependencies only, not the package itself.\n\n**install --python** _PATH_\n> Install for a different Python interpreter.\n\n**publish**\n> Upload to PyPI.\n\n**publish --repository** _name_\n> Target repository (pypi, testpypi).\n\n**check**\n> Validate project configuration."],["DESCRIPTION","**flit** is a simple Python packaging tool that uses pyproject.toml for configuration. It handles building wheels and source distributions and publishing to PyPI with minimal setup.\n\nFlit is designed for pure Python packages with simple needs. It reads package metadata from the module's docstring and __version__ attribute, requiring minimal configuration."],["PYPROJECT.TOML EXAMPLE","```toml\n[build-system]\nrequires = [\"flit_core\"]\nbuild-backend = \"flit_core.buildapi\"\n\n[project]\nname = \"mypackage\"\nauthors = [{name = \"Me\"}]\ndynamic = [\"version\", \"description\"]\n\n[project.scripts]\nmycli = \"mypackage:main\"\n```"],["CAVEATS","Best for pure Python packages without complex build requirements. No support for compiled extensions. Requires module docstring for description. Publishing needs PyPI credentials."],["HISTORY","flit was created by **Thomas Kluyver** as a simpler alternative to setuptools for pure Python packages. It helped drive the adoption of pyproject.toml and was an early implementation of PEP 517/518 build standards."],["INSTALL","```dnf: sudo dnf install flit```\n\n```apk: sudo apk add py3-flit```\n\n```brew: brew install flit```"],["SEE ALSO","[pip](/man/pip)(1), [poetry](/man/poetry)(1), [hatch](/man/hatch)(1), [twine](/man/twine)(1)"]],"e":["pip","poetry","hatch","twine"],"x":"flit init"},"fnm":{"n":"fnm","t":"fast Node.js version manager written in Rust","s":[["TAGLINE","fast Node.js version manager written in Rust"],["TLDR","**Install a specific Node.js version**\n\n```fnm install [18.17.0]```\n\n**Install the latest LTS release**\n\n```fnm install --lts```\n\n**Switch the current shell to a version**\n\n```fnm use [18]```\n\n**Set the default global version**\n\n```fnm default [18]```\n\n**List installed versions**\n\n```fnm list```\n\n**List versions available for download**\n\n```fnm list-remote```\n\n**Enable automatic switching on cd and evaluate shell init**\n\n```eval \"$(fnm env --use-on-cd)\"```"],["SYNOPSIS","**fnm** _command_ [_options_] [_arguments_]"],["PARAMETERS","**install** [_VERSION_] [**--lts**]\n> Download and install a Node.js version. Accepts full versions (18.17.0), major versions (18), `latest`, or `--lts` for the newest LTS.\n\n**use** _VERSION_ [**--install-if-missing**]\n> Switch the current shell to _VERSION_. With `--install-if-missing`, installs the version first if it is not present.\n\n**default** _VERSION_\n> Set the default global Node.js version used in new shells.\n\n**current**\n> Print the active Node.js version.\n\n**list**, **ls**\n> List all versions installed locally.\n\n**list-remote**, **ls-remote**\n> List all versions available from the distribution mirror.\n\n**uninstall** _VERSION_\n> Remove an installed Node.js version.\n\n**alias** _VERSION_ _NAME_\n> Create a named alias for a version.\n\n**unalias** _NAME_\n> Remove a named alias.\n\n**exec** **--using=**_VERSION_ _CMD_\n> Run _CMD_ with a specific Node.js version without switching the shell.\n\n**env** [**--shell**=_SHELL_] [**--use-on-cd**]\n> Print shell configuration to be eval'd; enables fnm in the current shell and optionally auto-switches on directory change.\n\n**completions** **--shell**=_SHELL_\n> Print shell completion scripts for bash, zsh, fish, or PowerShell.\n\n**--node-dist-mirror** _URL_\n> Use an alternative Node.js distribution mirror.\n\n**--fnm-dir** _PATH_\n> Override the fnm installation directory (default `~/.fnm`).\n\n**--log-level** _LEVEL_\n> Control verbosity (quiet, error, info).\n\n**--version**, **-V**\n> Display version information.\n\n**--help**, **-h**\n> Display help information."],["DESCRIPTION","**fnm** (Fast Node Manager) is a Node.js version manager written in Rust. It provides quick installation and switching between Node.js versions with minimal overhead.\n\nThe tool reads .node-version or .nvmrc files for automatic version switching. Shell integration enables per-directory version selection without manual commands.\n\nfnm emphasizes speed with Rust implementation and cross-platform support including Windows."],["CONFIGURATION","**.node-version**\n> Specifies Node.js version for the current directory.\n\n**.nvmrc**\n> Alternative version file format compatible with nvm."],["CAVEATS","Shell integration via `eval \"$(fnm env --use-on-cd)\"` is required for `fnm use`, automatic version switching on `cd`, and keeping PATH in sync. fnm is not a drop-in replacement for **nvm**: it does not source bash profile or provide all nvm subcommands. Installed Node versions are stored under `$FNM_DIR` (default `~/.fnm`); the active version is symlinked into a per-shell directory, so different shells can have different active versions."],["HISTORY","fnm was created as a fast alternative to **nvm**, the original Node Version Manager. Written in Rust, it dramatically improves installation and switching speed over shell-based alternatives."],["INSTALL","```pacman: sudo pacman -S fnm```\n\n```zypper: sudo zypper install fnm```\n\n```brew: brew install fnm```\n\n```nix: nix profile install nixpkgs#fnm```"],["SEE ALSO","[nvm](/man/nvm)(1), [node](/man/node)(1), [volta](/man/volta)(1)"]],"e":["nvm","node","volta"],"x":"fnm install [18.17.0]"},"fuck":{"n":"fuck","t":"command correction suggester","s":[["TAGLINE","command correction suggester"],["TLDR","**Fix previous command**\n\n```fuck```\n\n**Confirm before running**\n\n```fuck --yeah```\n\n**Use specific correction**\n\n```fuck --yes [number]```\n\n**Show available fixes**\n\n```fuck --help```"],["SYNOPSIS","**fuck** [_options_]"],["PARAMETERS","**--yeah**, **--yes**\n> Confirm fix immediately.\n\n**--help**\n> Display help information."],["CONFIGURATION","**~/.config/thefuck/settings.py**\n> Configuration for rules, excluded commands, priority, and aliases."],["DESCRIPTION","**fuck** is the command from The Fuck application that corrects previous console commands. It analyzes the previous failed command and suggests corrections.\n\nWhen you mistype a command or forget sudo, running fuck proposes the corrected command. It learns from your shell history and supports many common mistake patterns.\n\nfuck integrates with shells through aliases or key bindings for quick access."],["CAVEATS","Alias setup required. May not catch all errors. Some corrections may not be appropriate. Use with caution."],["HISTORY","fuck is part of **The Fuck** project created by Vladimir Iakovlev. The application provides an amusing yet practical way to correct command-line mistakes through Python-based rule matching."],["INSTALL","```aur: yay -S fuck```"],["SEE ALSO","[bash](/man/bash)(1)"]],"e":["bash"],"x":"fuck"},"getkeycodes":{"n":"getkeycodes","t":"Print the kernel scancode-to-keycode mapping table","s":[["TAGLINE","Print the kernel scancode-to-keycode mapping table"],["TLDR","**Display keyboard scancodes**\n\n```getkeycodes```"],["SYNOPSIS","**getkeycodes**"],["PARAMETERS","**-V**, **--version**\n> Print version number.\n\n**-h**, **--help**\n> Print usage message."],["DESCRIPTION","**getkeycodes** displays the kernel scancode-to-keycode mapping table. It shows how raw keyboard scancodes are translated to key codes used by applications.\n\nThe output shows scancode ranges and their corresponding keycodes. This mapping is used by the Linux input system to translate hardware key presses.\n\ngetkeycodes is useful for debugging keyboard issues and understanding key mapping."],["CAVEATS","Requires access to the Linux console (will not work in X11 or Wayland terminal emulators). Output format is dense with hexadecimal scan codes and decimal key codes. Shows kernel-level mapping only, not X11 or desktop keymaps."],["HISTORY","getkeycodes is part of the **kbd** package, providing keyboard utilities for Linux console management."],["INSTALL","```apt: sudo apt install kbd```\n\n```dnf: sudo dnf install kbd```\n\n```pacman: sudo pacman -S kbd```\n\n```apk: sudo apk add kbd```\n\n```zypper: sudo zypper install kbd```\n\n```nix: nix profile install nixpkgs#kbd```"],["SEE ALSO","[showkey](/man/showkey)(1), [dumpkeys](/man/dumpkeys)(1), [loadkeys](/man/loadkeys)(1)"]],"e":["showkey","dumpkeys","loadkeys"],"x":"getkeycodes"},"go-fix":{"n":"go-fix","t":"update Go code for newer API versions","s":[["TAGLINE","update Go code for newer API versions"],["TLDR","**Fix all packages in the current module**\n\n```go fix ./...```\n\n**Fix a specific package**\n\n```go fix [package]```\n\n**Preview changes as a unified diff without modifying files**\n\n```go fix -diff [package]```\n\n**Output fixes as JSON instead of applying them**\n\n```go fix -json [package]```\n\n**Use a custom fix tool**\n\n```go fix -fixtool=[prog] [package]```"],["SYNOPSIS","**go fix** [_-diff_] [_-json_] [_-fixtool=prog_] [_packages_]"],["PARAMETERS","_PACKAGES_\n> Packages to fix. Use ./... for all packages in the current module.\n\n**-diff**\n> Print changes as a unified diff instead of applying them. Useful for CI pipelines.\n\n**-json**\n> Emit fixes as JSON output instead of applying them.\n\n**-fixtool** _prog_\n> Select a different analysis tool with alternative or additional fixers. The default is go tool fix.\n\n**-v**\n> Verbose output.\n\n**-n**\n> Print the commands that would be executed but do not run them.\n\n**-x**\n> Print the commands as they are executed."],["DESCRIPTION","**go fix** updates Go code for newer Go versions. It applies transformations for API changes and deprecated patterns, helping maintain code across Go releases.\n\nThe tool runs specific fixers (analyzers) that handle known changes between Go versions. As of Go 1.24, go fix uses the same analysis framework as go vet and supports selecting specific analyzers by name.\n\nAvailable analyzers include fixes for: replacing interface{} with any, using min/max builtins, modernizing string operations, updating fmt.Appendf patterns, range-over-int, and more.\n\nFiles are modified in place by default, so version control is recommended before running."],["CAVEATS","Modifies files in place unless -diff is used. Use version control. May need manual review. Limited to known fixes. Each run analyzes only one build configuration; run with different GOARCH/GOOS values for multi-platform projects."],["HISTORY","go fix is part of the **Go** toolchain. Originally introduced for handling breaking API changes during Go 1.x development, it was significantly enhanced in **Go 1.24** to use the analysis framework, gaining support for custom analyzers and modernization fixes."],["SEE ALSO","[go](/man/go)(1), [go-vet](/man/go-vet)(1), [go-fmt](/man/go-fmt)(1)"]],"e":["go","go-vet","go-fmt"],"x":"go fix ./..."},"gsa":{"n":"gsa","t":"Analyze size breakdown of Go compiled binaries","s":[["TAGLINE","Analyze size breakdown of Go compiled binaries"],["TLDR","**Text report** for a binary\n\n```gsa [path/to/binary]```\n\n**Interactive terminal UI**\n\n```gsa --tui [path/to/binary]```\n\n**Web UI** on local server\n\n```gsa --web [path/to/binary]```\n\n**JSON output**\n\n```gsa -f json [path/to/binary]```\n\n**Compare two binaries**\n\n```gsa --diff [old-binary] [new-binary]```"],["SYNOPSIS","**gsa** [*options*] *binary*..."],["DESCRIPTION","**gsa** (**go-size-analyzer**) analyses the size of Go-compiled binaries. It supports ELF, Mach-O, PE, and experimental WebAssembly, with breakdowns by packages and sections. Output formats include text, JSON, HTML, and SVG. Interactive exploration is available via **--tui** and **--web**.\n\nInstall from GitHub releases, Homebrew (**brew install go-size-analyzer**), Scoop, or:\n\n```\nGOEXPERIMENT=jsonv2 go install github.com/Zxilly/go-size-analyzer/cmd/gsa@latest\n```\n\nBuilding from source needs a recent Go toolchain with the **jsonv2** experiment enabled (as documented upstream)."],["PARAMETERS","*binary*\n\n> Path to a compiled Go binary (or multiple for supported modes).\n\n**--tui**\n\n> Open the terminal UI explorer.\n\n**--web**\n\n> Start a web UI (default port documented in **--help**, commonly 8080).\n\n**-f**, **--format** text|json|html|svg\n\n> Output format for non-interactive runs.\n\n**--diff**\n\n> Compare binaries (json/text).\n\nSee **gsa --help** for verbose flags, hide options, and server bind addresses."],["CAVEATS","Best results on Go binaries with symbol/package information; stripped binaries yield coarser reports. WebAssembly analysis is experimental. Browser/WASM analysis is much slower than native. Source builds need specific Go version features."],["INSTALL","```apt: sudo apt install gwenhywfar-tools```\n\n```dnf: sudo dnf install gwenhywfar```\n\n```pacman: sudo pacman -S gwenhywfar```\n\n```apk: sudo apk add gwenhywfar```\n\n```zypper: sudo zypper install gwenhywfar-tools```\n\n```brew: brew install gwenhywfar```\n\n```nix: nix profile install nixpkgs#gwenhywfar```"],["SEE ALSO","[go](/man/go)(1), [nm](/man/nm)(1), [size](/man/size)(1), [binsider](/man/binsider)(1)"],["RESOURCES","```[Source code](https://github.com/Zxilly/go-size-analyzer)```\n\n```[Homepage](https://gsa.zxilly.dev)```"]],"e":["go","nm","size","binsider"],"x":"gsa [path/to/binary]"},"hurl":{"n":"hurl","t":"Run and test HTTP requests with plain text","s":[["TAGLINE","Run and test HTTP requests with plain text"],["TLDR","**Run requests from a Hurl file**\n\n```hurl [path/to/file.hurl]```\n\n**Run in test mode** with progress reporting\n\n```hurl --test [path/to/file.hurl]```\n\n**Run with verbose output**\n\n```hurl --verbose [path/to/file.hurl]```\n\n**Run with a variable**\n\n```hurl --variable [name]=[value] [path/to/file.hurl]```\n\n**Run multiple files in parallel**\n\n```hurl --parallel [path/to/file1.hurl] [path/to/file2.hurl]```\n\n**Output results as JSON**\n\n```hurl --json [path/to/file.hurl]```\n\n**Run with retry on failure**\n\n```hurl --retry [3] [path/to/file.hurl]```"],["SYNOPSIS","**hurl** [_options_] [_FILE ..._]"],["PARAMETERS","**-u, --user** _user:password_\n> Add basic authentication credentials\n\n**-H, --header** _header_\n> Add a custom HTTP header\n\n**-L, --location**\n> Follow HTTP redirects\n\n**--test**\n> Activate test mode with progress reporting\n\n**--json**\n> Output results in JSON format\n\n**--parallel**\n> Run files concurrently\n\n**--retry** _num_\n> Number of retry attempts on failure\n\n**--delay** _ms_\n> Pause before each request\n\n**--variable** _name=value_\n> Define a template variable\n\n**--variables-file** _file_\n> Load variables from a file\n\n**--secret** _name=value_\n> Define a redacted variable (hidden from logs)\n\n**-o, --output** _file_\n> Write response body to file\n\n**-k, --insecure**\n> Allow insecure SSL connections\n\n**--verbose**\n> Enable detailed logging\n\n**--very-verbose**\n> Enable extra-detailed logging including request/response bodies\n\n**--continue-on-error**\n> Proceed despite assertion failures\n\n**--from-entry** _num_\n> Start execution at a specific entry\n\n**--to-entry** _num_\n> Stop execution at a specific entry"],["DESCRIPTION","**hurl** is a command-line tool that runs HTTP requests defined in simple plain text **.hurl** files. It can chain multiple requests, capture values from responses, and evaluate assertions on headers, status codes, and body content using XPath, JSONPath, and regex queries.\n\nHurl supports REST, SOAP, GraphQL, and any XML/JSON-based API. It handles authentication, cookies, form data, multipart uploads, and binary bodies. Built in **Rust** and powered by **libcurl**, it is a single binary with no runtime dependencies.\n\nHurl is designed for both fetching data and testing HTTP sessions, with built-in support for assertions on response status, headers, body content, duration, and SSL certificates. Multiple report formats (text, JUnit, TAP, HTML) make it easy to integrate into CI/CD pipelines."],["CAVEATS","Hurl files use a custom format that is not compatible with curl command syntax despite the shared libcurl backend. The plain text format requires learning Hurl's specific syntax for assertions and captures. Error messages for malformed .hurl files can be cryptic."],["HISTORY","**hurl** was created by **Orange-OpenSource** (the French telecommunications company) and first released on GitHub in **August 2020**. Written in Rust with libcurl as its HTTP engine, it has grown to over 18,000 GitHub stars and is actively maintained."],["INSTALL","```pacman: sudo pacman -S hurl```\n\n```zypper: sudo zypper install hurl```\n\n```brew: brew install hurl```\n\n```nix: nix profile install nixpkgs#hurl```"],["SEE ALSO","[curl](/man/curl)(1), [wget](/man/wget)(1), [httpie](/man/httpie)(1)"]],"e":["curl","wget","httpie"],"x":"hurl [path/to/file.hurl]"},"id3v2":{"n":"id3v2","t":"command-line tool for viewing and editing ID3v2 tags in MP3 files","s":[["TAGLINE","command-line tool for viewing and editing ID3v2 tags in MP3 files"],["TLDR","**List** all available genres\n\n```id3v2 -L```\n\n**List** all tags of specific files\n\n```id3v2 -l [path/to/file.mp3]```\n\n**Delete** id3v2 tags from files\n\n```id3v2 --delete-v2 [path/to/file.mp3]```\n\n**Delete** id3v1 tags from files\n\n```id3v2 --delete-v1 [path/to/file.mp3]```\n\nDisplay **help**\n\n```id3v2 -h```"],["SYNOPSIS","**id3v2** [_options_] [_files_]"],["PARAMETERS","**-L**, **--list-genres**\n> List all available genre codes\n\n**-l**, **--list**\n> List all tags in the specified files\n\n**--delete-v2**\n> Delete all id3v2 tags\n\n**--delete-v1**\n> Delete all id3v1 tags\n\n**-a**, **--artist** _ARTIST_\n> Set the artist tag\n\n**-A**, **--album** _ALBUM_\n> Set the album tag\n\n**-t**, **--song** _TITLE_\n> Set the song title tag\n\n**-g**, **--genre** _GENRE_\n> Set the genre (number or name)\n\n**-y**, **--year** _YEAR_\n> Set the year tag\n\n**-T**, **--track** _NUM/TOTAL_\n> Set track number"],["DESCRIPTION","**id3v2** is a command-line tool for viewing and editing ID3v2 tags in MP3 files. It also handles ID3v1 tags and can convert between the two versions.\n\nID3 tags store metadata like artist, album, title, year, and genre within MP3 files. The id3v2 format supports extended information including album art, lyrics, and custom frames."],["CAVEATS","Some poorly-encoded files may have corrupted tags. ID3v2 tags can be at the beginning or end of the file. Editing tags doesn't re-encode the audio. Some players may cache tag information."],["HISTORY","ID3v2 was developed as an extension to the original ID3v1 tag format, which was limited to 30 characters per field. ID3v2 was introduced in **1998** and supports Unicode, embedded images, and virtually unlimited field lengths."],["INSTALL","```apt: sudo apt install id3v2```\n\n```dnf: sudo dnf install id3v2```\n\n```pacman: sudo pacman -S id3v2```\n\n```zypper: sudo zypper install id3v2```\n\n```brew: brew install id3v2```\n\n```nix: nix profile install nixpkgs#id3v2```"],["SEE ALSO","[mid3v2](/man/mid3v2)(1), [ffmpeg](/man/ffmpeg)(1)"]],"e":["mid3v2","ffmpeg"],"x":"id3v2 -L"},"inkmake":{"n":"inkmake","t":"automates Inkscape exports for batch processing","s":[["TAGLINE","automates Inkscape exports for batch processing"],["TLDR","**Export all layers**\n\n```inkmake [input.svg]```\n\n**Export specific size**\n\n```inkmake -w [1024] [input.svg]```\n\n**Export to directory**\n\n```inkmake -o [output/] [input.svg]```\n\n**Export specific format**\n\n```inkmake -f [png|pdf|svg] [input.svg]```\n\n**Export specific layers**\n\n```inkmake -l \"[layer1,layer2]\" [input.svg]```"],["SYNOPSIS","**inkmake** [_options_] _file_"],["PARAMETERS","**-w** _WIDTH_\n> Output width in pixels.\n\n**-h** _HEIGHT_\n> Output height in pixels.\n\n**-o** _DIR_\n> Output directory.\n\n**-f** _FORMAT_\n> Output format (png, pdf, svg).\n\n**-l** _LAYERS_\n> Comma-separated layer names.\n\n**-d** _DPI_\n> Output resolution.\n\n**--help**\n> Display help information."],["DESCRIPTION","**inkmake** automates Inkscape exports for batch processing. It exports layers or objects from SVG files to various formats.\n\nThe tool is useful for generating icons, assets, or multiple sizes from a single source. It wraps Inkscape's export capabilities."],["CAVEATS","Requires Inkscape installed. SVG layer naming important. Wrapper script."],["HISTORY","inkmake was created to simplify batch exporting from **Inkscape** SVG files for asset generation workflows."],["SEE ALSO","[inkscape](/man/inkscape)(1), [convert](/man/convert)(1), [rsvg-convert](/man/rsvg-convert)(1)"]],"e":["inkscape","convert","rsvg-convert"],"x":"inkmake [input.svg]"},"ioping":{"n":"ioping","t":"monitors disk I/O latency in real time, similar to how ping shows network","s":[["TAGLINE","monitors disk I/O latency in real time, similar to how ping shows network"],["TLDR","**Measure disk latency**\n\n```ioping [path]```\n\n**Run specific number of requests**\n\n```ioping -c [10] [path]```\n\n**Measure with specific size**\n\n```ioping -s [1M] [path]```\n\n**Measure disk seek rate**\n\n```ioping -R [/dev/sda]```\n\n**Measure sequential speed**\n\n```ioping -RL [/dev/sda]```\n\n**Use direct I/O**\n\n```ioping -D [path]```\n\n**Quiet mode with statistics**\n\n```ioping -q -c [100] [path]```"],["SYNOPSIS","**ioping** [_options_] _path_|_device_"],["PARAMETERS","**-c** _count_\n> Stop after count requests.\n\n**-s** _size_\n> Request size (default 4k).\n\n**-R**\n> Disk seek rate test.\n\n**-L**\n> Sequential operations (default random).\n\n**-D**\n> Use direct I/O (O_DIRECT).\n\n**-W**\n> Use writes (dangerous for devices).\n\n**-C**\n> Use cached I/O.\n\n**-A**\n> Use asynchronous I/O.\n\n**-q**\n> Quiet mode, show only summary.\n\n**-w** _seconds_\n> Stop after specified time."],["DESCRIPTION","**ioping** monitors disk I/O latency in real time, similar to how ping shows network latency. Generates I/O patterns and measures response time. SSDs should show sub-millisecond latency; HDDs typically show a few milliseconds. High latency spikes may indicate problems."],["INSTALL","```apt: sudo apt install ioping```\n\n```dnf: sudo dnf install ioping```\n\n```pacman: sudo pacman -S ioping```\n\n```apk: sudo apk add ioping```\n\n```zypper: sudo zypper install ioping```\n\n```brew: brew install ioping```\n\n```nix: nix profile install nixpkgs#ioping```"],["SEE ALSO","[fio](/man/fio)(1), [iostat](/man/iostat)(1), [ping](/man/ping)(1)"]],"e":["fio","iostat","ping"],"x":"ioping [path]"},"ipscan":{"n":"ipscan","t":"fast, cross-platform network scanner that scans IP addresses and ports","s":[["TAGLINE","fast, cross-platform network scanner that scans IP addresses and ports"],["TLDR","**Launch Angry IP Scanner** GUI\n\n```ipscan```\n\n**Scan a single IP address** from command line\n\n```ipscan -s -f:txt -o [output.txt] [192.168.1.1]```\n\n**Scan an IP range** and save results\n\n```ipscan -s -f:txt -o [output.txt] [192.168.1.1] [192.168.1.254]```\n\n**Scan using a file** with IP addresses\n\n```ipscan -s -f:csv -o [output.csv] -ff:[iplist.txt]```\n\n**Scan and export in XML format**\n\n```ipscan -s -f:xml -o [output.xml] [192.168.1.0/24]```\n\n**Scan with specific ports and quit after completion**\n\n```ipscan -s -q -f:txt -o [output.txt] -p [80,443,8080] [192.168.1.1] [192.168.1.254]```"],["SYNOPSIS","**ipscan** [_-s_] [_-f:format_] [_-o filename_] [_-ff:filename_] [_start-ip_] [_end-ip_]"],["PARAMETERS","**-s**\n> Run in silent (command-line) mode without GUI\n\n**-f:format**\n> Output format: txt, csv, xml, ip-port\n\n**-o** _filename_\n> Output filename for scan results\n\n**-ff:** _filename_\n> Read IP addresses from file\n\n**-q**\n> Quit after scanning (with -s).\n\n**-a**\n> Append to output file instead of overwriting.\n\n**-p** _ports_\n> Specify ports to scan.\n\n**-d** _ms_\n> Delay in milliseconds between each request."],["DESCRIPTION","**ipscan** (Angry IP Scanner) is a fast, cross-platform network scanner that scans IP addresses and ports. It uses multiple threads for speed and can detect live hosts, resolve hostnames, scan ports, and retrieve NetBIOS information.\n\nThe tool can run as a GUI application for interactive use or in command-line mode for scripting and automation. It supports multiple output formats for integration with other tools."],["CAVEATS","High scan rates may trigger IDS/IPS alerts or violate network policies. Some features require administrative privileges. Port scanning without authorization may be illegal in certain jurisdictions. The GUI requires Java to be installed."],["HISTORY","**Angry IP Scanner** was created by Anton Keks and first released in **2001**. Originally written for Windows, it was rewritten in Java around **2007** to become cross-platform. It remains one of the most popular open-source network scanners with millions of downloads."],["INSTALL","```nix: nix profile install nixpkgs#ipscan```"],["SEE ALSO","[nmap](/man/nmap)(1), [masscan](/man/masscan)(1), [ping](/man/ping)(1), [netcat](/man/netcat)(1)"]],"e":["nmap","masscan","ping","netcat"],"x":"ipscan"},"kdesrc-build":{"n":"kdesrc-build","t":"tool for building KDE software from source repositories","s":[["TAGLINE","tool for building KDE software from source repositories"],["TLDR","**Initialize** kdesrc-build\n\n```kdesrc-build --initial-setup```\n\n**Build** a KDE component and its dependencies\n\n```kdesrc-build [component_name]```\n\nBuild **without updating** or dependencies\n\n```kdesrc-build --no-src --no-include-dependencies [component_name]```\n\n**Refresh** build directories before compiling\n\n```kdesrc-build --refresh-build [component_name]```\n\n**Resume** from a specific dependency\n\n```kdesrc-build --resume-from [dependency_component] [component_name]```\n\n**Run** a built component\n\n```kdesrc-build --run --exec [executable_name] [component_name]```\n\nBuild **all** configured components\n\n```kdesrc-build```"],["SYNOPSIS","**kdesrc-build** [_options_] [_components_]"],["PARAMETERS","**--initial-setup**\n> Initialize configuration\n\n**--no-src**\n> Don't update source code\n\n**--no-include-dependencies**\n> Don't build dependencies\n\n**--refresh-build**\n> Clean build directories before building\n\n**--resume-from** _COMPONENT_\n> Resume from specified component\n\n**--run** **--exec** _NAME_\n> Run built executable\n\n**--no-stop-on-failure**\n> Continue building if component fails"],["DESCRIPTION","**kdesrc-build** is a tool for building KDE software from source repositories. It automates downloading, configuring, and compiling KDE components with proper dependency handling.\n\nThe tool manages a local checkout of KDE source code and can build individual components or entire desktop environments. Configuration is stored in ~/.config/kdesrc-buildrc."],["CAVEATS","Requires significant disk space and time. Build dependencies must be installed. Consider using kde-builder as a modern replacement with improved performance."],["HISTORY","kdesrc-build has been the standard tool for KDE developers to build KDE software from source for many years. It's being gradually replaced by kde-builder."],["SEE ALSO","[kde-builder](/man/kde-builder)(1), [cmake](/man/cmake)(1), [make](/man/make)(1)"]],"e":["kde-builder","cmake","make"],"x":"kdesrc-build --initial-setup"},"kubectl-plugin":{"n":"kubectl-plugin","t":"Manage kubectl plugins","s":[["TAGLINE","Manage kubectl plugins"],["TLDR","**List all available plugins with full paths**\n\n```kubectl plugin list```\n\n**List only plugin binary names without paths**\n\n```kubectl plugin list --name-only```"],["SYNOPSIS","**kubectl plugin** list [**--name-only**] [_options_]"],["PARAMETERS","**list**\n> List all available plugin executables on the user's PATH.\n\n**--name-only**\n> Show only the binary name of each plugin, rather than its full path.\n\n**-h**, **--help**\n> Display help information."],["DESCRIPTION","**kubectl plugin** provides utilities for interacting with plugins. It discovers plugin executables in PATH that begin with the **kubectl-** prefix. For example, a binary named **kubectl-foo** becomes available as **kubectl foo**.\n\nRunning **kubectl plugin list** traverses all directories in PATH and displays matching executables in the order they appear. A warning is shown for any files beginning with **kubectl-** that are not executable, and for valid plugin files that shadow each other (duplicate names in different PATH directories).\n\nPlugins can be installed manually by placing executables in PATH, or managed through **krew**, the kubectl plugin manager. Krew provides a curated index of community plugins and handles installation, upgrades, and removal."],["CAVEATS","Plugins must be executable files with the **kubectl-** prefix. Only the **list** subcommand is available; there are no built-in install or remove commands (use krew for that). If multiple plugins share the same name in different PATH directories, only the first one is used and a warning is displayed."],["HISTORY","kubectl plugin support was added to **Kubernetes** to enable extensible command-line functionality, following the pattern of git's plugin system."],["INSTALL","```apt: sudo apt install kubectl```\n\n```pacman: sudo pacman -S kubectl```\n\n```apk: sudo apk add kubectl```\n\n```brew: brew install kubectl```\n\n```nix: nix profile install nixpkgs#kubectl```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [krew](/man/krew)(1)"]],"e":["kubectl","krew"],"x":"kubectl plugin list"},"limits.conf":{"n":"limits.conf","t":"PAM configuration file that sets resource limits for users and groups","s":[["TAGLINE","PAM configuration file that sets resource limits for users and groups"],["TLDR","**Set maximum open files for user**\n\n```[username] hard nofile [65535]```\n\n**Set memory limit**\n\n```[username] hard as [4194304]```\n\n**Set for all users**\n\n```* soft nproc [1024]```\n\n**Set for group**\n\n```@[groupname] hard maxlogins [10]```"],["SYNOPSIS","**/etc/security/limits.conf**"],["PARAMETERS","**hard**\n> Hard limit (maximum value).\n\n**soft**\n> Soft limit (default value).\n\n**nofile**\n> Maximum open files.\n\n**nproc**\n> Maximum processes.\n\n**as**\n> Address space limit.\n\n**maxlogins**\n> Maximum logins.\n\n**memlock**\n> Maximum locked memory.\n\n**stack**\n> Maximum stack size."],["DESCRIPTION","**limits.conf** is a PAM configuration file that sets resource limits for users and groups. Limits are enforced when users log in through PAM-enabled services.\n\nThe format is: domain type item value. Domain can be username, @groupname, or * for all users."],["EXAMPLE CONFIG","```"],["/etc/security/limits.conf","* soft nofile 4096\n* hard nofile 65535\n@developers soft nproc 2048\nroot hard nproc unlimited\n```"],["CAVEATS","Requires pam_limits module. Changes apply at next login. Systemd services may need LimitNOFILE in unit files. Check with ulimit -a."],["SEE ALSO","[ulimit](/man/ulimit)(1), [pam](/man/pam)(8), [pam_limits](/man/pam_limits)(8), [sysctl](/man/sysctl)(8)"]],"e":["ulimit","pam","pam_limits","sysctl"],"x":"[username] hard nofile [65535]"},"lstr":{"n":"lstr","t":"Fast minimalist directory tree viewer","s":[["TAGLINE","Fast minimalist directory tree viewer"],["TLDR","**Display directory tree**\n\n```lstr```\n\n**Display tree for a specific path**\n\n```lstr [path/to/directory]```\n\n**Launch interactive TUI mode**\n\n```lstr interactive```\n\n**Show tree with Git status** and respect .gitignore\n\n```lstr -G -g```\n\n**Show sizes, permissions and icons**\n\n```lstr -s -p --icons```\n\n**Limit recursion depth**\n\n```lstr -L [2]```\n\n**Sort by size, directories first**\n\n```lstr --sort size --dirs-first```"],["SYNOPSIS","**lstr** [_options_] [_path_]\n\n**lstr interactive** [_options_] [_path_]"],["PARAMETERS","**-a**, **--all**\n> List all files and directories, including hidden ones.\n\n**-d**, **--dirs-only**\n> Show directories only.\n\n**-g**, **--gitignore**\n> Respect `.gitignore` and other standard ignore files.\n\n**-G**, **--git-status**\n> Show Git status indicators for files and directories.\n\n**--icons**\n> Display file-specific icons (requires a Nerd Font).\n\n**--hyperlinks**\n> Render paths as clickable hyperlinks in supported terminals.\n\n**-L**, **--level** _DEPTH_\n> Limit the depth of the tree.\n\n**-p**, **--permissions**\n> Show file permissions (Unix-like systems).\n\n**-s**, **--size**\n> Show file sizes.\n\n**--sort** _TYPE_\n> Sort by **name**, **size**, **modified**, or **extension**.\n\n**--dirs-first**\n> List directories before files.\n\n**--case-sensitive**\n> Use case-sensitive sorting.\n\n**--natural-sort**\n> Use version-aware (\"natural\") sort ordering.\n\n**-r**, **--reverse**\n> Reverse the sort order.\n\n**--dotfiles-first**\n> Sort hidden files and folders first.\n\n**--color** _WHEN_\n> Colorize output (**always**, **auto**, **never**).\n\n**--expand-level** _LEVEL_\n> Initial expansion depth (interactive mode only)."],["DESCRIPTION","**lstr** is a fast, minimalist directory tree viewer written in Rust. It scans directories in parallel and brings modern features to the classic **tree** command: `.gitignore` support, Git status indicators, Nerd Font icons, clickable hyperlinks, and multiple sort modes.\n\nThe **interactive** subcommand launches a keyboard-driven TUI (built with ratatui) for browsing the tree. Non-interactive output is designed to pipe cleanly into tools like **fzf**."],["CAVEATS","Icon display requires a Nerd Font installed in the terminal."],["HISTORY","**lstr** was created by **bgreenwell** and is written in **Rust**."],["INSTALL","```brew: brew install lstr```\n\n```nix: nix profile install nixpkgs#lstr```"],["SEE ALSO","[tree](/man/tree)(1), [ls](/man/ls)(1), [eza](/man/eza)(1)"]],"e":["tree","ls","eza"],"x":"lstr"},"luigi":{"n":"luigi","t":"python workflow engine for building complex pipelines of batch jobs","s":[["TAGLINE","python workflow engine for building complex pipelines of batch jobs"],["TLDR","**Run task**\n\n```luigi --module [mymodule] [MyTask]```\n\n**Run with parameters**\n\n```luigi --module [mymodule] [MyTask] --[param]=[value]```\n\n**Run local scheduler**\n\n```luigid```\n\n**Run with workers**\n\n```luigi --module [mymodule] [MyTask] --workers [4]```\n\n**Run with local scheduler** (no central scheduler needed)\n\n```luigi --module [mymodule] [MyTask] --local-scheduler```"],["SYNOPSIS","**luigi** [_options_] _task_ [_task-params_]"],["PARAMETERS","**--module** _name_\n> Python module containing tasks.\n\n**--workers** _n_\n> Number of workers.\n\n**--local-scheduler**\n> Use local instead of central scheduler.\n\n**--scheduler-host** _host_\n> Central scheduler hostname.\n\n**--scheduler-port** _port_\n> Central scheduler port (default 8082).\n\n**--log-level** _level_\n> Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL).\n\n**--help**\n> Display available options and task parameters."],["DESCRIPTION","**Luigi** is a Python workflow engine for building complex pipelines of batch jobs. It handles dependency resolution, workflow management, visualization, and failure handling.\n\nTasks define dependencies via requires(), and Luigi ensures tasks run in correct order."],["TASK EXAMPLE","```python\nimport luigi\n\nclass MyTask(luigi.Task):\n    param = luigi.Parameter()\n\n    def requires(self):\n        return UpstreamTask()\n\n    def output(self):\n        return luigi.LocalTarget('output.txt')\n\n    def run(self):\n        with self.output().open('w') as f:\n            f.write(self.param)\n```"],["CAVEATS","Central scheduler recommended for production. No built-in triggering (use cron). Targets define task completion. Python 3 required. Parameter names with underscores must use hyphens on the command line (e.g., --my-parameter for my_parameter)."],["HISTORY","Luigi was developed at **Spotify** and open-sourced in **2012** for managing complex data pipelines and machine learning workflows."],["INSTALL","```nix: nix profile install nixpkgs#luigi```"],["SEE ALSO","[airflow](/man/airflow)(1), [prefect](/man/prefect)(1)"]],"e":["airflow","prefect"],"x":"luigi --module [mymodule] [MyTask]"},"lvmsar":{"n":"lvmsar","t":"Deprecated LVM1 system activity reporter for logical volume I/O statistics","s":[["TAGLINE","Deprecated LVM1 system activity reporter for logical volume I/O statistics"],["TLDR","Run the **legacy LVM1** statistics reporter (obsolete on modern LVM2)\n\n```lvmsar```\n\nReport device-mapper **I/O statistics**\n\n```dmstats report [/dev/mapper/device]```\n\n**List** configured statistics regions\n\n```dmstats list [/dev/mapper/device]```"],["SYNOPSIS","**lvmsar**"],["DESCRIPTION","**lvmsar** was the LVM system activity reporter for displaying I/O statistics on logical volumes. It processed data collected by **lvmsadc** and presented performance metrics for analysis.\n\nThis command is **not supported under LVM2**. Modern systems should use **dmstats** instead, which provides comprehensive device-mapper statistics including read/write operations, throughput, and latency histograms."],["CAVEATS","**Deprecated**: This command only works on legacy LVM1 systems. For LVM2, use **dmstats report** to view I/O statistics on device-mapper devices."],["INSTALL","```apt: sudo apt install lvm2```\n\n```dnf: sudo dnf install lvm2```\n\n```pacman: sudo pacman -S lvm2```\n\n```apk: sudo apk add lvm2```\n\n```zypper: sudo zypper install lvm2```\n\n```nix: nix profile install nixpkgs#lvm2```"],["SEE ALSO","[lvmsadc](/man/lvmsadc)(8), [sar](/man/sar)(1), [lvm](/man/lvm)(8)"]],"e":["lvmsadc","sar","lvm"],"x":"lvmsar"},"ly":{"n":"ly","t":"Lightweight TUI display manager for Linux and BSD","s":[["TAGLINE","Lightweight TUI display manager for Linux and BSD"],["TLDR","**Start the display manager**\n\n```ly```\n\n**Start on a specific tty**\n\n```ly -c [tty2]```"],["SYNOPSIS","**ly** [_options_]"],["PARAMETERS","**-c**, **--config-tty** _TTY_\n> Set the tty to operate on."],["DESCRIPTION","**ly** is a lightweight TUI display manager for Linux and BSD that stays in the framebuffer console as an interactive text-based login screen. It supports both X11 and Wayland sessions and uses PAM for authentication. It does not require systemd to run.\n\nUsers navigate with arrow keys to change fields and select desktop environments, enter credentials, and launch sessions. The interface is minimal by design, avoiding the overhead of graphical display managers."],["CONFIGURATION","**/etc/ly/config.ini**\n> Configuration file with default values for customizing the login screen appearance and behavior, including animation, colors, and session paths."],["CAVEATS","Runs in the framebuffer console -- does not launch a graphical environment for the login screen itself. Some terminal features may vary depending on the framebuffer driver. Typically managed as a systemd service (**ly.service**) or via an init system."],["HISTORY","**ly** is developed by the **fairyglade** project and is written in **Zig**. It was designed as a minimal alternative to graphical display managers like GDM and SDDM."],["INSTALL","```dnf: sudo dnf install ly```\n\n```pacman: sudo pacman -S ly```\n\n```brew: brew install ly```\n\n```nix: nix profile install nixpkgs#ly```"],["SEE ALSO","[lemurs](/man/lemurs)(1), [gdm](/man/gdm)(1), [startx](/man/startx)(1), [xinit](/man/xinit)(1)"]],"e":["lemurs","gdm","startx","xinit"],"x":"ly"},"makoctl-invoke":{"n":"makoctl-invoke","t":"triggers actions on notifications displayed by the mako notification daemon","s":[["TAGLINE","triggers actions on notifications displayed by the mako notification daemon"],["TLDR","Invoke the **default action** on the most recent notification\n\n```makoctl invoke```\n\nInvoke a **specific action** on a notification\n\n```makoctl invoke -n [notification_id] [action_name]```"],["SYNOPSIS","**makoctl invoke** [_-n id_] [_action_]"],["PARAMETERS","**-n _id_**\n> Specify the notification ID to act upon\n\n**_action_**\n> The action name to invoke (uses default action if not specified)"],["DESCRIPTION","**makoctl invoke** triggers actions on notifications displayed by the mako notification daemon. Notifications can define multiple actions (like \"Reply\", \"Mark as Read\", \"Open\"), and this command invokes them programmatically.\n\nWithout arguments, it invokes the default action on the most recent notification—typically equivalent to clicking the notification. With **-n**, a specific notification can be targeted by its ID.\n\nThis is useful for keyboard-driven workflows where mouse interaction with notifications is undesirable, or for scripting notification responses."],["CAVEATS","The notification must still be visible for its actions to be invokable. Notification IDs can be obtained from **makoctl list**. Actions depend on what the sending application defined; not all notifications have actions beyond dismissal."],["INSTALL","```apt: sudo apt install mako-notifier```\n\n```dnf: sudo dnf install mako```\n\n```pacman: sudo pacman -S mako```\n\n```apk: sudo apk add mako```\n\n```zypper: sudo zypper install mako```\n\n```brew: brew install mako```\n\n```nix: nix profile install nixpkgs#mako```"],["SEE ALSO","[makoctl](/man/makoctl)(1), [makoctl-mode](/man/makoctl-mode)(1), [mako](/man/mako)(1)"]],"e":["makoctl","makoctl-mode","mako"],"x":"makoctl invoke"},"mosquitto_sub":{"n":"mosquitto_sub","t":"subscribes to MQTT topics and prints received messages","s":[["TAGLINE","subscribes to MQTT topics and prints received messages"],["TLDR","**Subscribe to topic**\n\n```mosquitto_sub -t [sensors/temperature]```\n\n**Subscribe to all topics**\n\n```mosquitto_sub -t '#'```\n\n**Subscribe with wildcard**\n\n```mosquitto_sub -t '[sensors/+/temperature]'```\n\n**Connect to remote broker**\n\n```mosquitto_sub -h [broker.example.com] -t [topic]```\n\n**Subscribe with authentication**\n\n```mosquitto_sub -h [broker] -u [username] -P [password] -t [topic]```\n\n**Subscribe with TLS**\n\n```mosquitto_sub -h [broker] --cafile [ca.crt] -t [topic]```\n\n**Show verbose output**\n\n```mosquitto_sub -v -t '[sensors/#]'```\n\n**Read single message and exit**\n\n```mosquitto_sub -t [topic] -C 1```"],["SYNOPSIS","**mosquitto_sub** [_-h host_] [_-p port_] [_-t topic_] [_-u user_] [_-P pass_] [_options_]"],["PARAMETERS","**-t** _TOPIC_\n> Topic to subscribe to (can repeat).\n\n**-h** _HOST_\n> Broker hostname.\n\n**-p** _PORT_\n> Broker port (default 1883).\n\n**-u** _USER_\n> Username.\n\n**-P** _PASSWORD_\n> Password.\n\n**-i** _ID_\n> Client ID.\n\n**-v**\n> Verbose (print topic with message).\n\n**-C** _COUNT_\n> Exit after receiving count messages.\n\n**-q** _QOS_\n> Quality of service (0, 1, 2).\n\n**--cafile** _FILE_\n> CA certificate for TLS.\n\n**--cert** _FILE_\n> Client certificate.\n\n**--key** _FILE_\n> Client private key.\n\n**-F** _FORMAT_\n> Output format string.\n\n**-N**\n> Don't append newline to messages.\n\n**--retained-only**\n> Only show retained messages."],["DESCRIPTION","**mosquitto_sub** subscribes to MQTT topics and prints received messages. MQTT is a lightweight publish/subscribe messaging protocol for IoT and telemetry.\n\nTopics use hierarchical naming with / separators. Wildcards expand subscriptions: + matches one level, # matches all remaining levels. For example, sensors/+/temperature matches any sensor's temperature.\n\nQuality of Service levels control delivery guarantees: QoS 0 is fire-and-forget, QoS 1 ensures at-least-once delivery, QoS 2 guarantees exactly-once delivery.\n\nRetained messages are stored by the broker and sent immediately on subscription. This provides last-known values to new subscribers.\n\nTLS encryption protects communication. Client certificates enable mutual authentication. The broker must be configured to accept TLS connections."],["CAVEATS","Subscribing to # on busy brokers produces high volume. QoS 2 has significant overhead. Passwords on command line visible in process list."],["HISTORY","**mosquitto_sub** is part of the **Eclipse Mosquitto** project, an MQTT broker and client suite. Mosquitto was created by **Roger Light** starting around **2009** and joined the **Eclipse Foundation** in **2014**."],["INSTALL","```apt: sudo apt install mosquitto-clients```\n\n```apk: sudo apk add mosquitto-clients```\n\n```zypper: sudo zypper install mosquitto-clients```"],["SEE ALSO","[mosquitto_pub](/man/mosquitto_pub)(1), [mosquitto](/man/mosquitto)(8), [mosquitto_passwd](/man/mosquitto_passwd)(1)"]],"e":["mosquitto_pub","mosquitto","mosquitto_passwd"],"x":"mosquitto_sub -t [sensors/temperature]"},"muffin-theme-viewer":{"n":"muffin-theme-viewer","t":"previews Muffin window manager themes","s":[["TAGLINE","previews Muffin window manager themes"],["TLDR","**Preview a theme by name (case-sensitive)**\n\n```muffin-theme-viewer [theme-name]```\n\n**Preview the default theme (Atlanta)**\n\n```muffin-theme-viewer```"],["SYNOPSIS","**muffin-theme-viewer** [_theme_]"],["PARAMETERS","_THEME_\n> Theme name or path to preview.\n\n**--help**\n> Display help information."],["DESCRIPTION","**muffin-theme-viewer** previews Muffin window manager themes by rendering window decorations in a test window. It can be used to measure the performance of a window frame option and to preview its appearance. The theme name is case-sensitive, and defaults to Atlanta if none is specified.\n\nUseful for theme development, testing, and selection within the Cinnamon desktop environment."],["CAVEATS","Cinnamon/Muffin specific. Requires a running display server. Only previews Metacity-compatible window decoration themes."],["HISTORY","muffin-theme-viewer is part of **Muffin**, a fork of GNOME's Mutter window manager adapted for the Cinnamon desktop environment maintained by the Linux Mint project."],["SEE ALSO","[muffin](/man/muffin)(1), [muffin-window-demo](/man/muffin-window-demo)(1), [cinnamon](/man/cinnamon)(1)"]],"e":["muffin","muffin-window-demo","cinnamon"],"x":"muffin-theme-viewer [theme-name]"},"nix-shell":{"n":"nix-shell","t":"creates temporary development environments","s":[["TAGLINE","creates temporary development environments"],["TLDR","**Enter shell with package**\n\n```nix-shell -p [hello]```\n\n**Enter shell from default.nix**\n\n```nix-shell```\n\n**Enter shell from file**\n\n```nix-shell [shell.nix]```\n\n**Run command in shell**\n\n```nix-shell -p [jq] --run \"[jq --version]\"```\n\n**Pure shell**\n\n```nix-shell --pure -p [python3]```\n\n**Multiple packages**\n\n```nix-shell -p [python3] [nodejs] [git]```"],["SYNOPSIS","**nix-shell** [_options_] [_path_]"],["PARAMETERS","_PATH_\n> Nix expression file.\n\n**-p** _PACKAGES_\n> Packages to include.\n\n**--run** _CMD_\n> Command to execute.\n\n**--pure**\n> Clear environment.\n\n**-A** _ATTR_\n> Use specific attribute.\n\n**-I** _PATH_\n> Add path to Nix expression search path (e.g., `-I nixpkgs=/path/to/nixpkgs`).\n\n**--command** _CMD_\n> Run a command then stay in the shell (unlike `--run`, which exits after).\n\n**--keep** _VAR_\n> Keep an environment variable when using `--pure`.\n\n**--packages**\n> Long form of `-p`.\n\n**-i** _INTERPRETER_\n> Use as a shebang interpreter (see `#!nix-shell` scripts).\n\n**--help**\n> Display help information."],["DESCRIPTION","**nix-shell** creates temporary development environments. Provides packages without installing.\n\nThe tool sets up shell with dependencies. Exits cleanly without system changes."],["CAVEATS","Legacy command. Consider nix develop. Per-invocation environment."],["HISTORY","nix-shell is a core **Nix** command for creating temporary development shells."],["INSTALL","```apt: sudo apt install nix-bin```\n\n```dnf: sudo dnf install nix```\n\n```pacman: sudo pacman -S nix```\n\n```apk: sudo apk add nix```\n\n```zypper: sudo zypper install nix```\n\n```nix: nix profile install nixpkgs#nix```"],["SEE ALSO","[nix-develop](/man/nix-develop)(1), [nix](/man/nix)(1), [direnv](/man/direnv)(1)"]],"e":["nix-develop","nix","direnv"],"x":"nix-shell -p [hello]"},"nsxiv":{"n":"nsxiv","t":"lightweight, keyboard-driven image viewer for X11","s":[["TAGLINE","lightweight, keyboard-driven image viewer for X11"],["TLDR","**Open images**\n\n```nsxiv [path/to/file1 path/to/file2 ...]```\n\nOpen images from **directories** in image mode\n\n```nsxiv [path/to/directory1 path/to/directory2 ...]```\n\nSearch directories **recursively** for images to view\n\n```nsxiv -r [path/to/directory1 path/to/directory2 ...]```\n\n**Quit** nsxiv\n\n```q```\n\nSwitch to **thumbnail mode** or open selected image\n\n```Enter```\n\n**Next/previous** image\n\n```n / p```"],["SYNOPSIS","**nsxiv** [_options_] _files_|_directories_..."],["PARAMETERS","**-r, --recursive**\n> Search directories recursively for images\n\n**-t, --thumbnail**\n> Start in thumbnail mode\n\n**-f, --fullscreen**\n> Start in fullscreen mode\n\n**-s _mode_**\n> Scale mode: fit, fill, width, height\n\n**-g _geometry_**\n> Window geometry (WxH+X+Y)\n\n**-b**\n> Hide info bar\n\n**-q**\n> Quiet mode; suppress warnings\n\n**-o**\n> Output selected files to stdout on exit"],["DESCRIPTION","**nsxiv** (Neo Simple X Image Viewer) is a lightweight, keyboard-driven image viewer for X11. It's a fork of the original sxiv with additional features and active maintenance.\n\nThe viewer has two modes: **image mode** for viewing individual images with zoom and pan, and **thumbnail mode** for browsing multiple images. Navigation uses vim-like keybindings: **h/j/k/l** or arrow keys, **n/p** for next/previous.\n\nImages can be marked for batch operations, and the selection can be output to stdout for use in scripts. External commands can be configured through shell scripts."],["CAVEATS","X11 only; does not work in Wayland without XWayland. Supports common image formats through imlib2 (JPEG, PNG, GIF, etc.). Animated GIF support is limited. Custom keybindings require recompilation."],["HISTORY","nsxiv is a community fork of sxiv (simple X image viewer) created after the original project became unmaintained. It continues active development with new features while maintaining the minimalist philosophy."],["INSTALL","```apt: sudo apt install nsxiv```\n\n```pacman: sudo pacman -S nsxiv```\n\n```apk: sudo apk add nsxiv```\n\n```nix: nix profile install nixpkgs#nsxiv```"],["SEE ALSO","[sxiv](/man/sxiv)(1), [feh](/man/feh)(1), [imv](/man/imv)(1), [viewnior](/man/viewnior)(1)"]],"e":["sxiv","feh","imv","viewnior"],"x":"nsxiv [path/to/file1 path/to/file2 ...]"},"orc-tools":{"n":"orc-tools","t":"collection of utilities for working with Apache ORC files","s":[["TAGLINE","collection of utilities for working with Apache ORC files"],["TLDR","**Show ORC file metadata**\n\n```orc-metadata [file.orc]```\n\n**Show file contents**\n\n```orc-contents [file.orc]```\n\n**Get file statistics**\n\n```orc-statistics [file.orc]```\n\n**Convert CSV to ORC**\n\n```orc-tools convert [data.csv] -o [output.orc]```\n\n**Scan ORC file**\n\n```orc-scan [file.orc]```\n\n**Merge ORC files**\n\n```orc-tools merge [file1.orc] [file2.orc] -o [merged.orc]```"],["SYNOPSIS","**orc-tools** _command_ [_options_] _files_..."],["PARAMETERS","**metadata**\n> Show file metadata.\n\n**contents**\n> Display contents.\n\n**statistics**\n> Show statistics.\n\n**convert**\n> Convert to ORC.\n\n**scan**\n> Scan and validate.\n\n**merge**\n> Merge files.\n\n**-o** _file_\n> Output file."],["DESCRIPTION","**orc-tools** is a collection of utilities for working with Apache ORC (Optimized Row Columnar) files. ORC is a columnar storage format optimized for Hadoop workloads.\n\nThe tools allow inspection, conversion, and manipulation of ORC files."],["ORC FEATURES","```\n- Columnar storage\n- Compression (ZLIB, Snappy, LZO)\n- Predicate pushdown\n- Type evolution\n- ACID support\n```"],["CAVEATS","Java required. Large files may need memory tuning. Part of Apache ORC project."],["HISTORY","Apache ORC was created at **Hortonworks** for Hive, later becoming a top-level Apache project for efficient columnar storage."],["INSTALL","```brew: brew install orc-tools```"],["SEE ALSO","[parquet-tools](/man/parquet-tools)(1), [hive](/man/hive)(1), [spark](/man/spark)(1)"]],"e":["parquet-tools","hive","spark"],"x":"orc-metadata [file.orc]"},"pactl":{"n":"pactl","t":"controls a running PulseAudio sound server from the command line","s":[["TAGLINE","controls a running PulseAudio sound server from the command line"],["TLDR","**List all audio sinks** (output devices)\n\n```pactl list sinks short```\n\n**Set the default audio output**\n\n```pactl set-default-sink [sink_name]```\n\n**Set volume** to a percentage\n\n```pactl set-sink-volume @DEFAULT_SINK@ [50%]```\n\n**Increase volume** by 10%\n\n```pactl set-sink-volume @DEFAULT_SINK@ +[10%]```\n\n**Mute/unmute** the default sink\n\n```pactl set-sink-mute @DEFAULT_SINK@ toggle```\n\n**List all audio sources** (input devices)\n\n```pactl list sources short```\n\n**Move an application** to a different output\n\n```pactl move-sink-input [stream_index] [sink_name]```\n\n**Load a PulseAudio module**\n\n```pactl load-module [module-null-sink]```"],["SYNOPSIS","**pactl** [_options_] _command_ [_args_]\n\n**pactl** set-sink-volume _SINK_ _VOLUME_\n\n**pactl** list [short] [_TYPE_]"],["PARAMETERS","**-s** _SERVER_, **--server** _SERVER_\n> Connect to specified PulseAudio server\n\n**-n** _NAME_, **--client-name** _NAME_\n> Client name for connection\n\n**-f** _FORMAT_, **--format** _FORMAT_\n> Output format (text or json)\n\n**-v**, **--verbose**\n> Enable verbose output\n\n**--version**\n> Show version information\n\n**-h**, **--help**\n> Show help message"],["COMMANDS","**stat**\n> Show memory usage statistics\n\n**info**\n> Display server information\n\n**list** [short] [_TYPE_]\n> List objects (modules, sinks, sources, sink-inputs, source-outputs, clients, samples, cards)\n\n**exit**\n> Terminate PulseAudio server\n\n**set-default-sink** _NAME|INDEX_\n> Set default audio output\n\n**set-default-source** _NAME|INDEX_\n> Set default audio input\n\n**set-sink-volume** _SINK_ _VOLUME_\n> Set sink volume (percentage, dB, or absolute)\n\n**set-source-volume** _SOURCE_ _VOLUME_\n> Set source volume\n\n**set-sink-mute** _SINK_ _0|1|toggle_\n> Mute/unmute sink\n\n**set-source-mute** _SOURCE_ _0|1|toggle_\n> Mute/unmute source\n\n**move-sink-input** _INDEX_ _SINK_\n> Move playback stream to different sink\n\n**move-source-output** _INDEX_ _SOURCE_\n> Move recording stream to different source\n\n**load-module** _NAME_ [_ARGS_]\n> Load a PulseAudio module\n\n**unload-module** _INDEX|NAME_\n> Unload a module\n\n**set-card-profile** _CARD_ _PROFILE_\n> Change sound card profile\n\n**subscribe**\n> Listen for PulseAudio events"],["DESCRIPTION","**pactl** controls a running PulseAudio sound server from the command line. It provides a scriptable interface for managing audio devices, streams, volumes, and modules.\n\nVolume can be specified as percentages (50%), decibels (0dB), or absolute values. Prefix with + or - for relative adjustments. Special names **@DEFAULT_SINK@**, **@DEFAULT_SOURCE@**, and **@DEFAULT_MONITOR@** refer to default devices.\n\nThe **list** command shows detailed information about audio objects. Use **short** for condensed output suitable for scripting. Stream indices from **list sink-inputs** can be used to move applications between outputs with **move-sink-input**."],["CAVEATS","Requires a running PulseAudio daemon. On systems using PipeWire with PulseAudio compatibility, pactl commands work but interact with PipeWire instead. The **pacmd** command is deprecated in favor of pactl. Volume values above 100% are allowed but may cause distortion."],["HISTORY","pactl is part of **PulseAudio**, a sound server for Linux and other Unix-like systems created by **Lennart Poettering**. PulseAudio was first released in **2004** and became the default sound system in most Linux distributions by 2009. The pactl utility provides command-line access to PulseAudio's D-Bus interface. In recent years, **PipeWire** has begun replacing PulseAudio while maintaining compatibility with pactl commands."],["INSTALL","```apt: sudo apt install pulseaudio-utils```\n\n```dnf: sudo dnf install pulseaudio-utils```\n\n```apk: sudo apk add pulseaudio-utils```\n\n```zypper: sudo zypper install pulseaudio-utils```"],["SEE ALSO","[pulseaudio](/man/pulseaudio)(1), [pacmd](/man/pacmd)(1), [pavucontrol](/man/pavucontrol)(1), [amixer](/man/amixer)(1)"]],"e":["pulseaudio","pacmd","pavucontrol","amixer"],"x":"pactl list sinks short"},"parted.interactive":{"n":"parted.interactive","t":"partition manipulation program that supports both MBR and GPT partition tables","s":[["TAGLINE","partition manipulation program that supports both MBR and GPT partition tables"],["TLDR","Start **interactive mode** with the specified disk selected\n\n```sudo parted /dev/sdX```\n\nShow **partition information** in interactive mode\n\n```print```\n\n**Select** a disk in interactive mode\n\n```select /dev/sdX```\n\nCreate a **16 GB partition** with filesystem (GPT partition table)\n\n```mkpart partition_name ext4 0% 16G```\n\nCreate a partition with filesystem (**MBR** partition table)\n\n```mkpart primary ext4 0% 16G```\n\n**Resize** a partition in interactive mode\n\n```resizepart /dev/sdX1 end_position```\n\n**Remove** a partition in interactive mode\n\n```rm /dev/sdX1```\n\nDisplay **help**\n\n```?```"],["SYNOPSIS","**parted** [_options_] [_device_]"],["PARAMETERS","**print**\n> Display partition table information\n\n**select** _device_\n> Select a disk to work with\n\n**mkpart** _name_ _fs-type_ _start_ _end_\n> Create a new partition\n\n**resizepart** _partition_ _end_\n> Resize a partition\n\n**rm** _partition_\n> Remove a partition\n\n**?**\n> Display help for interactive commands"],["DESCRIPTION","**parted** is a partition manipulation program that supports both MBR and GPT partition tables. In interactive mode, it provides a command-line interface for viewing and modifying disk partitions.\n\nThe interactive mode allows real-time partition management with immediate feedback on changes. It supports various filesystem types including ext2, ext3, ext4, btrfs, fat16, fat32, ntfs, and xfs."],["CAVEATS","Partition changes can result in data loss. Always backup important data before modifying partitions. Some operations require the partition to be unmounted first."],["HISTORY","Part of **GNU Parted**, developed as a free partition editor supporting multiple partition table types and filesystems."],["SEE ALSO","[parted](/man/parted)(8), [partprobe](/man/partprobe)(8), [fdisk](/man/fdisk)(8), [gdisk](/man/gdisk)(8)"]],"e":["parted","partprobe","fdisk","gdisk"],"x":"sudo parted /dev/sdX"},"perl":{"n":"perl","t":"The Perl 5 language interpreter","s":[["TAGLINE","The Perl 5 language interpreter"],["TLDR","**Run a Perl script**\n\n```perl [script.pl]```\n\n**Execute a one-liner**\n\n```perl -e 'print \"Hello\\n\"'```\n\n**In-place edit** a file with substitution\n\n```perl -i -pe 's/old/new/g' [file]```\n\n**In-place edit with backup** (original saved as .bak)\n\n```perl -i.bak -pe 's/old/new/g' [file]```\n\n**Print specific columns** with autosplit\n\n```perl -ane 'print \"$F[0]\\n\"' [file]```\n\n**Check syntax** without running\n\n```perl -c [script.pl]```\n\n**Process input line by line** (like sed/awk)\n\n```perl -ne 'print if /[pattern]/' [file]```\n\n**Use a module** from the command line\n\n```perl -MJSON -e 'print encode_json({key => \"value\"})'```"],["SYNOPSIS","**perl** [_options_] [_program_] [_arguments_]"],["PARAMETERS","_PROGRAM_\n> Perl script file to execute.\n\n**-e** _CODE_\n> Execute the given code as a one-liner. Multiple -e flags are allowed.\n\n**-E** _CODE_\n> Like -e but enables all optional features (say, state, etc.).\n\n**-n**\n> Wrap code in a while(<>) loop, reading input line by line without printing.\n\n**-p**\n> Like -n but also prints $_ after each iteration.\n\n**-i**[_EXT_]\n> In-place edit files. If extension given, creates backup with that suffix.\n\n**-a**\n> Turn on autosplit mode (used with -n or -p). Splits each line into @F.\n\n**-F** _PATTERN_\n> Specify the split pattern for autosplit mode (default: whitespace).\n\n**-l**\n> Enable automatic line-ending processing. Strips newlines on input, adds on output.\n\n**-0**[_OCTAL_]\n> Specify input record separator as octal value. -0777 slurps entire files.\n\n**-w**\n> Enable warnings (prefer `use warnings;` in scripts).\n\n**-W**\n> Enable all warnings unconditionally.\n\n**-c**\n> Syntax check only; do not execute the program.\n\n**-M**_MODULE_\n> Load a module before executing (equivalent to `use MODULE`).\n\n**-T**\n> Enable taint mode for security. Untrusted data cannot affect the system.\n\n**-S**\n> Use PATH to find the script. Emulates #! on platforms that don't support it.\n\n**-d**\n> Run the program under the debugger.\n\n**-v**\n> Print version and configuration summary."],["DESCRIPTION","**perl** is the Perl 5 language interpreter. Perl is a general-purpose programming language originally developed for text manipulation, combining features from C, sed, awk, and shell scripting.\n\nThe language excels at text processing with powerful built-in regular expression support. It is widely used for system administration, web development, network programming, and data processing. The Comprehensive Perl Archive Network (CPAN) provides a vast repository of reusable modules."],["CAVEATS","Modern Perl scripts should use `use strict;` and `use warnings;` for safer code. The -i flag without an extension modifies files in place with no backup. CPAN modules can be installed with the cpan command."],["HISTORY","Perl was created by **Larry Wall** in **1987** as a practical language for text manipulation and report generation. Perl 5, released in 1994, introduced major features including references, objects, and modules."],["INSTALL","```apt: sudo apt install perl-base```\n\n```dnf: sudo dnf install perl-base```\n\n```pacman: sudo pacman -S perl```\n\n```apk: sudo apk add perl```\n\n```zypper: sudo zypper install perl-base```\n\n```brew: brew install perl```\n\n```nix: nix profile install nixpkgs#perl```"],["SEE ALSO","[python](/man/python)(1), [ruby](/man/ruby)(1), [awk](/man/awk)(1), [sed](/man/sed)(1), [grep](/man/grep)(1), [cpan](/man/cpan)(1), [perldoc](/man/perldoc)(1)"]],"e":["python","ruby","awk","sed","grep","cpan","perldoc"],"x":"perl [script.pl]"},"phpquery":{"n":"phpquery","t":"queries PHP configuration on Debian-based systems","s":[["TAGLINE","queries PHP configuration on Debian-based systems"],["TLDR","List available **PHP versions**\n\n```sudo phpquery -V```\n\nList available **SAPIs** for a version\n\n```sudo phpquery -v [7.4] -S```\n\nList **enabled extensions** for version and SAPI\n\n```sudo phpquery -v [7.4] -s [cli] -M```\n\n**Check** if extension is enabled\n\n```sudo phpquery -v [7.4] -s [apache2] -m [json]```"],["SYNOPSIS","**phpquery** [**-v** _version_] [**-s** _sapi_] [**-V**|**-S**|**-M**|**-m** _module_]"],["PARAMETERS","**-v _version_**\n> PHP version to query\n\n**-s _sapi_**\n> SAPI to query\n\n**-V**\n> List available PHP versions\n\n**-S**\n> List available SAPIs\n\n**-M**\n> List enabled modules\n\n**-m _module_**\n> Check if specific module is enabled\n\n**-q**\n> Quiet mode, exit status only"],["DESCRIPTION","**phpquery** queries PHP configuration on Debian-based systems. It reports installed PHP versions, available SAPIs (Server API), and enabled extensions. This is useful for scripts and automation that need to detect PHP configuration.\n\nThe tool reads configuration from /etc/php/ directory structure and reports the state of symbolic links that control module activation."],["CAVEATS","Debian/Ubuntu specific tool. May require root for some queries. Only reports extensions managed through phpenmod/phpdismod system."],["HISTORY","**phpquery** is part of the **php-defaults** package maintained by the Debian PHP team. It complements phpenmod and phpdismod by providing query capabilities for PHP configuration management."],["SEE ALSO","[phpenmod](/man/phpenmod)(8), [phpdismod](/man/phpdismod)(8), [php](/man/php)(1)"]],"e":["phpenmod","phpdismod","php"],"x":"sudo phpquery -V"},"pnmalias":{"n":"pnmalias","t":"Anti-alias edges in PNM images","s":[["TAGLINE","Anti-alias edges in PNM images"],["TLDR","**Anti-alias image edges**\n\n```pnmalias [input.pnm] > [output.pnm]```\n\n**Specify background color**\n\n```pnmalias -bgcolor [white] [input.pnm] > [output.pnm]```"],["SYNOPSIS","**pnmalias** [_options_] [_file_]"],["PARAMETERS","**-bgcolor** _color_\n> Background color for anti-aliasing.\n\n**-fgcolor** _color_\n> Foreground color."],["DESCRIPTION","**pnmalias** anti-aliases edges in PNM images by smoothing transitions between foreground and background colors. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pnmsmooth](/man/pnmsmooth)(1), [ppmquant](/man/ppmquant)(1)"]],"e":["pnmsmooth","ppmquant"],"x":"pnmalias [input.pnm] > [output.pnm]"},"pulumi-stack-history":{"n":"pulumi-stack-history","t":"View stack deployment history","s":[["TAGLINE","View stack deployment history"],["TLDR","**Show stack deployment history**\n\n```pulumi stack history```\n\n**Show in JSON format**\n\n```pulumi stack history --json```\n\n**Show specific number of entries**\n\n```pulumi stack history --page-size [n]```\n\n**Show history for specific stack**\n\n```pulumi stack history -s [stack]```"],["SYNOPSIS","**pulumi** **stack** **history** [_options_]"],["PARAMETERS","**-s**, **--stack** _name_\n> Target stack.\n\n**--json**\n> Output as JSON.\n\n**--page-size** _n_\n> Number of entries to show.\n\n**--page** _n_\n> Page number."],["DESCRIPTION","**pulumi stack history** displays deployment history for a stack. Shows updates, previews, and operations with timestamps, results, and resource changes."],["INSTALL","```pacman: sudo pacman -S pulumi```\n\n```apk: sudo apk add pulumi```\n\n```brew: brew install pulumi```\n\n```nix: nix profile install nixpkgs#pulumi```"],["SEE ALSO","[pulumi](/man/pulumi)(1), [pulumi-stack](/man/pulumi-stack)(1)"]],"e":["pulumi","pulumi-stack"],"x":"pulumi stack history"},"pw-midi2play":{"n":"pw-midi2play","t":"Play MIDI 2 (UMP) files through PipeWire","s":[["TAGLINE","Play MIDI 2 (UMP) files through PipeWire"],["TLDR","**Play a MIDI 2 (UMP) file**\n\n```pw-midi2play [path/to/file.midi2]```\n\n**Play to a specific PipeWire target node**\n\n```pw-midi2play --target [node_id] [path/to/file.midi2]```\n\n**Force the legacy MIDI 1 byte-stream format instead of UMP**\n\n```pw-midi2play --force-midi midi [path/to/file.midi2]```\n\n**Play with verbose output**\n\n```pw-midi2play -v [path/to/file.midi2]```\n\n**Read MIDI 2 data from stdin**\n\n```cat [file.midi2] | pw-midi2play -```"],["SYNOPSIS","**pw-midi2play** [_options_] [_FILE_ | **-**]"],["PARAMETERS","**-h**, **--help**\n> Show help and exit.\n\n**--version**\n> Show version information.\n\n**-v**, **--verbose**\n> Enable verbose output.\n\n**-R**, **--remote** _NAME_\n> Connect to the named PipeWire daemon.\n\n**--target** _VALUE_\n> Target node serial or name. **0** disables linking.\n\n**--latency** _VALUE_\n> Set node latency, e.g. **100ms**, **2048** samples.\n\n**-P**, **--properties** _JSON_\n> Extra stream properties as a JSON object.\n\n**-M**, **--force-midi** _FORMAT_\n> Force MIDI format, **midi** (legacy bytes) or **ump** (Universal MIDI Packets, default)."],["DESCRIPTION","**pw-midi2play** sends MIDI 2 events into the PipeWire graph for playback. It is an alias of **pw-cat**(1) invoked with the **TYPE_MIDI2** data type, which expects **Universal MIDI Packet (UMP)** containers as defined by the MIDI 2.0 specification.\n\nUMP packets carry both legacy MIDI 1 messages and MIDI 2 features such as 32-bit-per-channel resolution, per-note controllers, and 16 groups of 16 channels. The tool itself does not synthesise audio: events are routed through the PipeWire graph to a MIDI 2 capable synthesiser or hardware device that renders sound.\n\nWhen **-** is given as the file argument, MIDI data is read from standard input."],["CAVEATS","Requires the destination synthesiser, plugin, or hardware to understand MIDI 2 / UMP. Without a downstream renderer the playback is silent. Standard **.mid** (SMF) files use MIDI 1 byte streams and are typically played with **pw-midiplay**(1) instead, or with **--force-midi midi**."],["HISTORY","MIDI 2 support arrived in **PipeWire** alongside the broader pw-cat MIDI 2 (UMP) work, complementing the older **pw-midiplay**/**pw-midirecord** pair. PipeWire was created by **Wim Taymans** at Red Hat and first released in **2017** and is now the default audio server in major Linux distributions. The MIDI 2.0 specification was ratified by the MMA in **2020**."],["INSTALL","```apk: sudo apk add pipewire-tools```\n\n```zypper: sudo zypper install pipewire-tools```"],["SEE ALSO","[pw-cat](/man/pw-cat)(1), [pw-midi2record](/man/pw-midi2record)(1), [pw-midiplay](/man/pw-midiplay)(1), [pw-midirecord](/man/pw-midirecord)(1), [pw-mididump](/man/pw-mididump)(1), [pipewire](/man/pipewire)(1)"]],"e":["pw-cat","pw-midi2record","pw-midiplay","pw-midirecord","pw-mididump","pipewire"],"x":"pw-midi2play [path/to/file.midi2]"},"pygame":{"n":"pygame","t":"Python game development library","s":[["TAGLINE","Python game development library"],["TLDR","**Install Pygame**\n\n```pip install pygame```\n\n**Run Pygame example**\n\n```python -m pygame.examples.aliens```\n\n**Check installation**\n\n```python -c \"import pygame; print(pygame.ver)\"```"],["SYNOPSIS","**pygame** Python library for game development"],["DESCRIPTION","**Pygame** is a Python library for game development built on SDL. It provides modules for graphics, sound, input handling, and game logic, making it ideal for 2D games and multimedia applications."],["BASIC EXAMPLE","```python\nimport pygame\n\npygame.init()\nscreen = pygame.display.set_mode((800, 600))\npygame.display.set_caption(\"My Game\")\n\nrunning = True\nwhile running:\n    for event in pygame.event.get():\n        if event.type == pygame.QUIT:\n            running = False\n\n    screen.fill((0, 0, 0))\n    pygame.draw.circle(screen, (255, 0, 0), (400, 300), 50)\n    pygame.display.flip()\n\npygame.quit()\n```"],["KEY MODULES","```python\npygame.display  # Window management\npygame.draw     # Shape drawing\npygame.image    # Load/save images\npygame.mixer    # Sound playback\npygame.font     # Text rendering\npygame.sprite   # Sprite groups\npygame.time     # Timing/FPS control\npygame.key      # Keyboard input\npygame.mouse    # Mouse input\n```"],["EXAMPLES","```bash"],["List all examples","python -m pygame.examples"],["Run specific example","python -m pygame.examples.chimp\npython -m pygame.examples.aliens\n```"],["CAVEATS","2D focused. For 3D, consider PyOpenGL or Panda3D. SDL-based, good cross-platform support."],["HISTORY","Pygame was created by **Pete Shinners** in 2000 as a Python wrapper for SDL, enabling easy game development."],["SEE ALSO","[python](/man/python)(1)"]],"e":["python"],"x":"pip install pygame"},"pytest":{"n":"pytest","t":"Python testing framework with fixtures and plugins","s":[["TAGLINE","Python testing framework with fixtures and plugins"],["TLDR","**Run all tests**\n\n```pytest```\n\n**Run specific test file**\n\n```pytest [test_file.py]```\n\n**Run specific test function**\n\n```pytest [test_file.py]::[test_function]```\n\n**Run tests matching keyword**\n\n```pytest -k \"[keyword]\"```\n\n**Run with verbose output**\n\n```pytest -v```\n\n**Stop on first failure**\n\n```pytest -x```\n\n**Show print statements**\n\n```pytest -s```\n\n**Run tests in parallel** (requires pytest-xdist)\n\n```pytest -n [4]```\n\n**Run tests matching a marker**\n\n```pytest -m \"[slow]\"```"],["SYNOPSIS","**pytest** [_-v_] [_-x_] [_-k expression_] [_-m marker_] [_--cov package_] [_options_] [_paths_]"],["PARAMETERS","**-v**, **--verbose**\n> Increase verbosity.\n\n**-q**, **--quiet**\n> Decrease verbosity.\n\n**-x**, **--exitfirst**\n> Stop on first failure.\n\n**-s**, **--capture=no**\n> Disable output capture (show print).\n\n**-k** _EXPRESSION_\n> Run tests matching keyword expression.\n\n**-m** _MARKER_\n> Run tests with specific marker.\n\n**--collect-only**\n> List tests without running.\n\n**--lf**, **--last-failed**\n> Run only last failed tests.\n\n**--ff**, **--failed-first**\n> Run failed tests first.\n\n**-n** _NUM_\n> Parallel workers (pytest-xdist).\n\n**--cov** _PACKAGE_\n> Measure coverage (pytest-cov).\n\n**--cov-report** _TYPE_\n> Coverage report format.\n\n**--pdb**\n> Drop into debugger on failure.\n\n**--tb** _STYLE_\n> Traceback style: short, long, line, native, no.\n\n**--maxfail** _NUM_\n> Stop after N failures.\n\n**--ignore** _PATH_\n> Ignore path during collection.\n\n**--durations** _NUM_\n> Show N slowest tests."],["DESCRIPTION","**pytest** is Python's most popular testing framework. It discovers and runs tests automatically, with powerful fixtures, parameterization, and plugin ecosystem.\n\nTest discovery finds files matching test_*.py or *_test.py, and functions/methods starting with test_. Classes starting with Test are also collected. This convention-based approach minimizes configuration.\n\nFixtures provide reusable test dependencies. Defined with @pytest.fixture, they can set up databases, create objects, or configure environments. Fixtures can be scoped (function, class, module, session) for efficient resource management.\n\nParameterization runs tests with multiple inputs using @pytest.mark.parametrize. Markers (@pytest.mark.X) tag tests for selective running or custom behavior.\n\nThe plugin ecosystem extends functionality: pytest-cov for coverage, pytest-xdist for parallel execution, pytest-mock for mocking, and hundreds more. Plugins install via pip and activate automatically.\n\nAssertion introspection provides detailed failure messages without special assertion methods. Standard Python assert statements work with rich comparison displays."],["CONFIGURATION","**pytest.ini**\n> Primary configuration file for test discovery paths, markers, command-line defaults, and plugin settings.\n\n**pyproject.toml**\n> Project configuration with a `[tool.pytest.ini_options]` section supporting the same options as pytest.ini.\n\n**setup.cfg**\n> Alternative configuration file with a `[tool:pytest]` section for test settings.\n\n**conftest.py**\n> Per-directory fixture and plugin file automatically loaded by pytest, used to define shared fixtures, hooks, and test configuration."],["CAVEATS","Large test suites need pytest-xdist for reasonable runtime. Fixture complexity can obscure test logic. Plugin conflicts occasionally occur. Coverage may miss some code patterns. Parameterized test output can be verbose."],["HISTORY","**pytest** was created by **Holger Krekel** around **2004** as py.test, evolving from the py library. It grew as an alternative to unittest, emphasizing simplicity and minimal boilerplate. The project became pytest around **2016** with the 3.0 release. It's now the de facto standard for Python testing."],["INSTALL","```apk: sudo apk add py3-pytest```\n\n```brew: brew install pytest```"],["SEE ALSO","[python](/man/python)(1), [coverage](/man/coverage)(1), [tox](/man/tox)(1), [mypy](/man/mypy)(1)"]],"e":["python","coverage","tox","mypy"],"x":"pytest"},"qm-rollback":{"n":"qm-rollback","t":"Rollback a Proxmox VM to a snapshot","s":[["TAGLINE","Rollback a Proxmox VM to a snapshot"],["TLDR","**Rollback** VM to snapshot\n\n```qm rollback vm_id snap_name```"],["SYNOPSIS","**qm rollback** _vmid_ _snapname_"],["DESCRIPTION","**qm rollback** reverts a QEMU/KVM virtual machine in Proxmox VE to a previous snapshot state. All changes made after the snapshot was taken will be discarded."],["PARAMETERS","**vmid**\n> The numeric ID of the virtual machine\n\n**snapname**\n> The name of the snapshot to rollback to"],["CAVEATS","The VM must be stopped before rollback. All data and configuration changes made after the snapshot was created will be lost. This operation cannot be undone."],["HISTORY","**qm rollback** is part of the **Proxmox VE** virtualization platform for managing QEMU/KVM virtual machine snapshots."],["INSTALL","```dnf: sudo dnf install qm```"],["SEE ALSO","[qm](/man/qm)(1), [qm-snapshot](/man/qm-snapshot)(1), [qm-delsnapshot](/man/qm-delsnapshot)(1)"]],"e":["qm","qm-snapshot","qm-delsnapshot"],"x":"qm rollback vm_id snap_name"},"rabbitmq-queues":{"n":"rabbitmq-queues","t":"Inspect and manage RabbitMQ queues","s":[["TAGLINE","Inspect and manage RabbitMQ queues"],["TLDR","**List queues**\n\n```rabbitmq-queues list```\n\n**Show queue info**\n\n```rabbitmq-queues info [queue_name]```\n\n**Check if queues are quorum**\n\n```rabbitmq-queues check_if_node_is_quorum_critical```\n\n**Rebalance queue leaders**\n\n```rabbitmq-queues rebalance all```"],["SYNOPSIS","**rabbitmq-queues** _command_ [_options_]"],["PARAMETERS","**list**\n> List all queues.\n\n**info** _queue_\n> Show queue information.\n\n**rebalance** _type_\n> Rebalance queue leaders (all, quorum, classic).\n\n**check_if_node_is_quorum_critical**\n> Check quorum queue health.\n\n**grow** _node_ _strategy_\n> Add queue members.\n\n**shrink** _node_\n> Remove queue members."],["DESCRIPTION","**rabbitmq-queues** manages RabbitMQ queues. Inspect queue status, rebalance load, and manage quorum queue membership. Part of RabbitMQ CLI tools."],["INSTALL","```dnf: sudo dnf install rabbitmq-server```\n\n```apk: sudo apk add rabbitmq-server```\n\n```zypper: sudo zypper install rabbitmq-server```\n\n```nix: nix profile install nixpkgs#rabbitmq-server```"],["SEE ALSO","[rabbitmqctl](/man/rabbitmqctl)(1), [rabbitmq-streams](/man/rabbitmq-streams)(1)"]],"e":["rabbitmqctl","rabbitmq-streams"],"x":"rabbitmq-queues list"},"rtorrent":{"n":"rtorrent","t":"Terminal-based BitTorrent client","s":[["TAGLINE","Terminal-based BitTorrent client"],["TLDR","**Start rtorrent** and load a torrent file or magnet link\n\n```rtorrent [path/to/file.torrent]```\n\n**Start a download** (within rtorrent)\n\n```Ctrl+s```\n\n**Stop a download** (within rtorrent)\n\n```Ctrl+d```\n\n**View details** about a selected torrent\n\n```Right Arrow```\n\n**Close** rtorrent safely\n\n```Ctrl+q```\n\n**Set a download directory** on startup\n\n```rtorrent -d [path/to/download/directory]```"],["SYNOPSIS","**rtorrent** [_options_] [_torrent_|_magnet_]"],["PARAMETERS","**-d** _directory_\n> Set the default download directory.\n\n**-n**\n> Do not load the ~/.rtorrent.rc configuration file on startup.\n\n**-o** _key=value_\n> Set a configuration option directly from the command line.\n\n**-s** _directory_\n> Session directory for saving torrent state between restarts."],["DESCRIPTION","**rtorrent** is a text-based BitTorrent client that runs in the terminal. It provides torrent downloading with a curses-based interface for monitoring and managing downloads.\n\nUse keyboard shortcuts to control downloads: Ctrl+s to start, Ctrl+d to stop or remove (press twice to delete with data), and Ctrl+q to quit safely. Press the right arrow key to view detailed information about a selected torrent, and left arrow to return to the main view."],["CONFIGURATION","**~/.rtorrent.rc**\n> Main configuration file controlling download directories, connection limits, port ranges, DHT settings, and throttle rates.\n\n**/etc/rtorrent/rtorrent.rc**\n> System-wide configuration file used as fallback when no user config exists."],["CAVEATS","Configuration via ~/.rtorrent.rc file. Requires proper port forwarding for optimal speeds. Support for magnet links requires DHT enabled."],["HISTORY","Written in C++ as a high-performance console BitTorrent client. Popular for headless servers and scripted environments."],["INSTALL","```apt: sudo apt install rtorrent```\n\n```dnf: sudo dnf install rtorrent```\n\n```pacman: sudo pacman -S rtorrent```\n\n```apk: sudo apk add rtorrent```\n\n```zypper: sudo zypper install rtorrent```\n\n```brew: brew install rtorrent```\n\n```nix: nix profile install nixpkgs#rtorrent```"],["SEE ALSO","[transmission-cli](/man/transmission-cli)(1), [aria2c](/man/aria2c)(1), [deluge](/man/deluge)(1)"]],"e":["transmission-cli","aria2c","deluge"],"x":"rtorrent [path/to/file.torrent]"},"ruff-check":{"n":"ruff-check","t":"Run Ruff linter on Python code","s":[["TAGLINE","Run Ruff linter on Python code"],["TLDR","**Check current directory**\n\n```ruff check```\n\n**Check specific files**\n\n```ruff check [file.py]```\n\n**Check and fix**\n\n```ruff check --fix```\n\n**Check with specific rules**\n\n```ruff check --select [E,F]```\n\n**Watch for changes**\n\n```ruff check --watch```"],["SYNOPSIS","**ruff** **check** [_options_] [_files_...]"],["PARAMETERS","**--fix**\n> Auto-fix violations.\n\n**--select** _rules_\n> Enable specific rules.\n\n**--ignore** _rules_\n> Ignore specific rules.\n\n**--watch**\n> Watch for file changes.\n\n**--config** _file_\n> Configuration file.\n\n**--output-format** _format_\n> Output format (text, json, github)."],["DESCRIPTION","**ruff check** is the linting subcommand of Ruff, an extremely fast Python linter written in Rust. It analyzes Python source files for style violations, potential bugs, and code quality issues, implementing rule sets from Flake8, isort, pyupgrade, and many other established Python tools in a single unified interface.\n\nThe **--fix** flag automatically corrects fixable violations in place, while **--select** and **--ignore** control which rule categories are active. Watch mode re-checks files on every save, providing real-time feedback during development. Output formats include plain text, JSON, and GitHub Actions annotations for CI integration."],["INSTALL","```dnf: sudo dnf install ruff```\n\n```pacman: sudo pacman -S ruff```\n\n```apk: sudo apk add ruff```\n\n```brew: brew install ruff```\n\n```nix: nix profile install nixpkgs#ruff```"],["SEE ALSO","[ruff](/man/ruff)(1), [ruff-format](/man/ruff-format)(1), [flake8](/man/flake8)(1)"]],"e":["ruff","ruff-format","flake8"],"x":"ruff check"},"sherlock":{"n":"sherlock","t":"Find usernames across social networks","s":[["TAGLINE","Find usernames across social networks"],["TLDR","Search for a username and **save results** to file\n\n```sherlock [username] --output [path/to/file]```\n\nSearch **multiple usernames** saving to directory\n\n```sherlock [username1] [username2] --folderoutput [path/to/directory]```\n\nSearch using **Tor network**\n\n```sherlock --tor [username]```\n\nUse **unique Tor circuit** per request\n\n```sherlock --unique-tor [username]```\n\nSearch using a **proxy**\n\n```sherlock [username] --proxy [proxy_url]```\n\nSearch and **open results** in browser\n\n```sherlock [username] --browse```\n\nSearch **specific sites** only\n\n```sherlock [username] --site github --site twitter```"],["SYNOPSIS","**sherlock** [_OPTIONS_] _username_ [_username_...]"],["PARAMETERS","**--output, -o** _file_\n> Save results to specified file\n\n**--folderoutput, -fo** _directory_\n> Save results for multiple users to directory\n\n**--csv**\n> Output results in CSV format\n\n**--xlsx**\n> Output results in Excel format\n\n**--site** _site_name_\n> Limit search to specific site(s) (can be repeated)\n\n**--tor, -t**\n> Route requests through Tor network\n\n**--unique-tor, -u**\n> Use new Tor circuit for each request\n\n**--proxy, -p** _url_\n> Route through specified proxy (e.g., socks5://127.0.0.1:1080)\n\n**--timeout** _seconds_\n> Request timeout in seconds (default: 60)\n\n**--print-all**\n> Show all results including sites where username not found\n\n**--print-found**\n> Show only sites where username was found\n\n**--browse, -b**\n> Open found URLs in default browser\n\n**--nsfw**\n> Include NSFW sites in search\n\n**--json, -j** _file_\n> Load custom site data from JSON file\n\n**--verbose, -v, -d, --debug**\n> Show extra debugging output\n\n**--version**\n> Display version information"],["DESCRIPTION","**Sherlock** is an OSINT (Open Source Intelligence) tool that searches for usernames across 400+ social networks and websites simultaneously. It helps locate online presence and connected accounts by querying each platform's user lookup functionality.\n\nResults are saved to text files named after each searched username. The tool supports various output formats including CSV and Excel for further analysis. Network routing through Tor or proxies provides anonymity during searches."],["CAVEATS","Rate limiting by target sites may cause false negatives. Some sites require authentication or have regional restrictions. Results should be verified manually as username availability does not confirm identity. Excessive use may trigger IP blocks from target platforms. Tor routing significantly increases search time."],["HISTORY","**Sherlock** was created by **Siddharth Dushantha** and first released in **2019**. The project quickly gained popularity in the security and OSINT community for its ease of use and extensive site coverage. Named after the famous detective, it is maintained as an open-source project on GitHub with active community contributions adding new site support."],["INSTALL","```brew: brew install sherlock```\n\n```nix: nix profile install nixpkgs#sherlock```"],["SEE ALSO","[tor](/man/tor)(1), [curl](/man/curl)(1), [wget](/man/wget)(1)"]],"e":["tor","curl","wget"],"x":"sherlock [username] --output [path/to/file]"},"snmpset":{"n":"snmpset","t":"Modify SNMP object values on agents","s":[["TAGLINE","Modify SNMP object values on agents"],["TLDR","**Set an integer value** using SNMPv2c\n\n```snmpset -v 2c -c [community] [host] [OID] i [value]```\n\n**Set a string value**\n\n```snmpset -v 2c -c [community] [host] [OID] s \"[string_value]\"```\n\n**Set multiple values** in one request\n\n```snmpset -v 2c -c [community] [host] [OID1] i [value1] [OID2] s \"[value2]\"```\n\n**Set using SNMPv3** with authentication\n\n```snmpset -v 3 -u [username] -l authNoPriv -a MD5 -A [password] [host] [OID] i [value]```\n\n**Set using SNMPv3** with authentication and encryption\n\n```snmpset -v 3 -u [username] -l authPriv -a SHA -A [authpass] -x AES -X [privpass] [host] [OID] i [value]```"],["SYNOPSIS","**snmpset** [_options_] _agent_ _OID_ _type_ _value_ [_OID_ _type_ _value_...]"],["PARAMETERS","**-v** _version_\n> SNMP version: **1**, **2c**, or **3**.\n\n**-c** _community_\n> Community string for SNMPv1/v2c authentication.\n\n**-u** _username_\n> SNMPv3 security name (username).\n\n**-l** _level_\n> SNMPv3 security level: **noAuthNoPriv**, **authNoPriv**, or **authPriv**.\n\n**-a** _protocol_\n> SNMPv3 authentication protocol: **MD5** or **SHA**.\n\n**-A** _passphrase_\n> SNMPv3 authentication passphrase.\n\n**-x** _protocol_\n> SNMPv3 privacy protocol: **DES** or **AES**.\n\n**-X** _passphrase_\n> SNMPv3 privacy passphrase.\n\n**-t** _timeout_\n> Timeout in seconds before retransmitting.\n\n**-r** _retries_\n> Number of retries before giving up."],["VALUE TYPES","**i**\n> Integer\n\n**u**\n> Unsigned integer\n\n**s**\n> String\n\n**x**\n> Hexadecimal string\n\n**d**\n> Decimal string\n\n**n**\n> Null\n\n**o**\n> Object identifier\n\n**t**\n> Timeticks\n\n**a**\n> IP address\n\n**b**\n> Bits"],["DESCRIPTION","**snmpset** communicates with a network entity using SNMP SET requests to modify management information on the device. Each variable to set requires three arguments: the OID, the data type, and the value.\n\nThe tool supports SNMPv1, SNMPv2c, and SNMPv3. Write access typically requires a different community string than read-only access, and many devices restrict which OIDs can be modified. SNMPv3 with authentication and encryption is recommended for security.\n\nCommon use cases include changing device configurations, enabling/disabling interfaces, modifying system contact or location information, and triggering actions on network equipment."],["CAVEATS","Modifying SNMP values can affect device operation and network stability. Ensure you have proper authorization and understand the impact before using snmpset. Write community strings should be kept confidential. Many OIDs are read-only and cannot be modified. Incorrect type specifications will cause the request to fail."],["HISTORY","**snmpset** is part of the **Net-SNMP** project, an open-source suite of SNMP tools and libraries. Net-SNMP evolved from the CMU SNMP implementation and later UCD-SNMP. It provides comprehensive SNMP v1, v2c, and v3 support and is the standard SNMP toolkit on most Unix-like systems."],["INSTALL","```apt: sudo apt install snmp```\n\n```apk: sudo apk add net-snmp-tools```"],["SEE ALSO","[snmpget](/man/snmpget)(1), [snmpwalk](/man/snmpwalk)(1), [snmptranslate](/man/snmptranslate)(1)"]],"e":["snmpget","snmpwalk","snmptranslate"],"x":"snmpset -v 2c -c [community] [host] [OID] i [value]"},"speed-test":{"n":"speed-test","t":"Measure internet connection speed","s":[["TAGLINE","Measure internet connection speed"],["TLDR","**Run speed test**\n\n```speed-test```\n\n**JSON output**\n\n```speed-test --json```\n\n**Output in bytes**\n\n```speed-test --bytes```\n\n**Verbose output**\n\n```speed-test --verbose```"],["SYNOPSIS","**speed-test** [_--json_] [_--bytes_] [_--verbose_]"],["PARAMETERS","**--json**\n> JSON output format.\n\n**--bytes**\n> Show in bytes/second.\n\n**--verbose**\n> Detailed output.\n\n**--version**\n> Show version.\n\n**--help**\n> Show help."],["DESCRIPTION","**speed-test** measures internet connection performance by testing download speed, upload speed, and ping latency against speedtest.net servers. It automatically selects the nearest server to provide representative results.\n\nThe tool displays results in megabits per second (Mbps) by default, with an option to show bytes per second instead. JSON output mode is available for integration with monitoring scripts and dashboards.\n\nResults reflect the connection speed between your device and the selected test server, which may differ from speeds to other destinations due to network routing, congestion, and ISP peering arrangements."],["OUTPUT","Results show:\n- Ping (latency in ms)\n- Download speed (Mbps)\n- Upload speed (Mbps)\n- Server location"],["CAVEATS","Results vary by server. Network conditions affect measurements. ISP throttling possible."],["HISTORY","**speed-test** is an npm package for command-line speed testing. It uses the same methodology as speedtest.net."],["INSTALL","```aur: yay -S speed-test```"],["SEE ALSO","[speedtest-cli](/man/speedtest-cli)(1), [fast](/man/fast)(1), [iperf](/man/iperf)(1)"]],"e":["speedtest-cli","fast","iperf"],"x":"speed-test"},"ss":{"n":"ss","t":"Display socket statistics and connections","s":[["TAGLINE","Display socket statistics and connections"],["TLDR","Show all **TCP/UDP/RAW/UNIX sockets**\n\n```ss -a -t```\n\nShow all **listening** sockets\n\n```ss -l```\n\nShow all TCP sockets with **process** information\n\n```ss -tp```\n\nShow all TCP sockets connected to local **HTTPS port**\n\n```ss -t src :443```\n\nShow all TCP sockets **listening** on port 8080\n\n```ss -lt src :8080```\n\nShow **established** SSH connections\n\n```ss -o state established '( dport = :ssh or sport = :ssh )'```\n\n**Kill** a socket connection\n\n```ss -K dst 192.168.1.1 dport = 22```\n\nPrint **summary** statistics\n\n```ss -s```"],["SYNOPSIS","**ss** [_options_] [_FILTER_]"],["DESCRIPTION","**ss** is used to dump socket statistics. It displays information similar to netstat but can show more TCP and state information than other tools. It retrieves socket information directly from kernel space, making it faster than netstat."],["PARAMETERS","**-h, --help**\n> Show summary of options\n\n**-a, --all**\n> Display both listening and non-listening sockets\n\n**-l, --listening**\n> Display only listening sockets\n\n**-n, --numeric**\n> Do not resolve service names; show numeric ports\n\n**-r, --resolve**\n> Attempt to resolve numeric address/ports\n\n**-p, --processes**\n> Show process using socket\n\n**-t, --tcp**\n> Display TCP sockets\n\n**-u, --udp**\n> Display UDP sockets\n\n**-x, --unix**\n> Display Unix domain sockets\n\n**-w, --raw**\n> Display raw sockets\n\n**-4, --ipv4**\n> Display IPv4 sockets only\n\n**-6, --ipv6**\n> Display IPv6 sockets only\n\n**-o, --options**\n> Show timer information\n\n**-e, --extended**\n> Show detailed socket information (UID, inode, cookie)\n\n**-m, --memory**\n> Show socket memory usage\n\n**-i, --info**\n> Show internal TCP information\n\n**-K, --kill**\n> Forcibly close sockets\n\n**-s, --summary**\n> Print summary statistics\n\n**-E, --events**\n> Continually display sockets as they are destroyed\n\n**-N, --net=NSNAME**\n> Switch to specified network namespace"],["CAVEATS","State filtering supports TCP states (established, syn-sent, syn-recv, fin-wait-1, fin-wait-2, time-wait, closed, close-wait, last-ack, listening, closing) and meta-states (all, connected, synchronized, bucket). The **-K** option requires appropriate privileges."],["HISTORY","**ss** is part of the **iproute2** package and was developed as a modern replacement for **netstat**. It provides faster performance by reading directly from kernel netlink sockets rather than parsing /proc files."],["INSTALL","```apt: sudo apt install iproute2```\n\n```pacman: sudo pacman -S iproute2```\n\n```apk: sudo apk add iproute2-ss```\n\n```zypper: sudo zypper install iproute2```\n\n```brew: brew install iproute2```\n\n```nix: nix profile install nixpkgs#iproute2```"],["SEE ALSO","[ip](/man/ip)(8), [netstat](/man/netstat)(8), [lsof](/man/lsof)(8)"]],"e":["ip","netstat","lsof"],"x":"ss -a -t"},"surf":{"n":"surf","t":"Minimalist suckless WebKit web browser","s":[["TAGLINE","Minimalist suckless WebKit web browser"],["TLDR","**Open a URL**\n\n```surf [https://example.com]```\n\n**Embed into tabbed** for tab support\n\n```tabbed surf -e```\n\n**Open with specific window geometry**\n\n```surf -g [800x600] [https://example.com]```\n\n**Open in fullscreen mode**\n\n```surf -F [https://example.com]```\n\n**Disable JavaScript**\n\n```surf -s [https://example.com]```\n\n**Disable images and cookies**\n\n```surf -i -k [https://example.com]```\n\n**Embed surf into another window** by window ID\n\n```surf -e [window_id] [https://example.com]```\n\n**Print the X window ID** to stdout (useful for scripting)\n\n```surf -x [https://example.com]```"],["SYNOPSIS","**surf** [_options_] [_URI_]"],["PARAMETERS","**-b**\n> Disable scrollbars.\n\n**-c** _file_\n> Specify cookie file.\n\n**-d**\n> Disable disk cache.\n\n**-e** _winid_\n> Embed into window with given ID.\n\n**-f**\n> Start in windowed mode (not fullscreen).\n\n**-F**\n> Start in fullscreen mode.\n\n**-g** _geometry_\n> Set window geometry (WIDTHxHEIGHT).\n\n**-i**\n> Disable images.\n\n**-I**\n> Enable images (default).\n\n**-k**\n> Disable cookies.\n\n**-K**\n> Enable cookies (default).\n\n**-m**\n> Disable applying styles from the specified file.\n\n**-M**\n> Enable applying styles from the specified file.\n\n**-n**\n> Disable the Web Inspector (Developer Tools).\n\n**-N**\n> Enable the Web Inspector (Developer Tools).\n\n**-p**\n> Disable plugins.\n\n**-P**\n> Enable plugins (default).\n\n**-r** _file_\n> Specify script file.\n\n**-s**\n> Disable JavaScript.\n\n**-S**\n> Enable JavaScript (default).\n\n**-t** _file_\n> Specify style file.\n\n**-v**\n> Display version information.\n\n**-x**\n> Print X window ID to stdout."],["DESCRIPTION","**surf** is a minimalist web browser from the suckless project. It is based on WebKit/GTK+ and follows the suckless philosophy of simplicity, clarity, and frugality.\n\nThe browser has no graphical user interface elements such as toolbars or URL bars. Navigation is performed entirely through keyboard shortcuts and external tools like **dmenu**. Use Ctrl+g to enter a new URL, Ctrl+y to copy the current URL, and Ctrl+p to navigate to a URL from the clipboard.\n\nConfiguration is done by editing the config.h source file and recompiling, following the suckless approach. For tabbed browsing, use the companion **tabbed** utility with the included surf.sh script.\n\nThe source code is intentionally kept under 2000 lines for readability and auditability."],["CAVEATS","Requires GTK+ and WebKit libraries. No built-in tab support; use tabbed for tabs. Configuration requires editing source code and recompiling. Install dmenu for URL bar functionality. Limited features compared to mainstream browsers."],["HISTORY","**surf** was created as part of the suckless project, which produces software that emphasizes minimalism and clarity. It embodies the Unix philosophy of doing one thing well: displaying web pages. The browser provides a lightweight alternative for users who prefer keyboard-driven interfaces and minimal resource usage."],["INSTALL","```apt: sudo apt install surf```\n\n```dnf: sudo dnf install surf```\n\n```apk: sudo apk add sxmo-surf```\n\n```nix: nix profile install nixpkgs#surf```"],["SEE ALSO","[dmenu](/man/dmenu)(1), [st](/man/st)(1), [dwm](/man/dwm)(1), [firefox](/man/firefox)(1), [chromium](/man/chromium)(1), [xprop](/man/xprop)(1)"]],"e":["dmenu","st","dwm","firefox","chromium","xprop"],"x":"surf [https://example.com]"},"systemctl-rescue":{"n":"systemctl-rescue","t":"Enter single-user rescue mode","s":[["TAGLINE","Enter single-user rescue mode"],["TLDR","Switch the system into **rescue** target (single-user-like)\n\n```systemctl rescue```\n\nRequest rescue mode **without waiting** for completion\n\n```systemctl --no-block rescue```"],["SYNOPSIS","**systemctl rescue** [_OPTIONS_]"],["PARAMETERS","**--no-block**\n> Return immediately without waiting"],["DESCRIPTION","**systemctl rescue** transitions the system to rescue mode (`rescue.target`), a single-user environment for system maintenance. Most services are stopped, but more are available than in emergency mode.\n\nRescue mode provides a shell with basic system services, networking may be available, and all filesystems are typically mounted read-write."],["CAVEATS","Requires root privileges. Most user services and network services will be stopped. Use rescue mode for system maintenance tasks like filesystem repairs or package recovery."],["HISTORY","The **rescue** subcommand provides the systemd equivalent of traditional single-user mode (runlevel 1), offering a minimal but functional recovery environment."],["SEE ALSO","[systemctl-emergency](/man/systemctl-emergency)(1), [systemctl-default](/man/systemctl-default)(1), [systemctl](/man/systemctl)(1)"]],"e":["systemctl-emergency","systemctl-default","systemctl"],"x":"systemctl rescue"},"systemd-run":{"n":"systemd-run","t":"Run commands in transient units","s":[["TAGLINE","Run commands in transient units"],["TLDR","**Start** a transient service\n\n```sudo systemd-run [command] [arguments]```\n\n**Start** a transient service under the current user\n\n```systemd-run --user [command] [arguments]```\n\n**Start** a transient service with a custom name and description\n\n```sudo systemd-run -u [name] --description [string] [command]```\n\n**Start** a service that remains after exit with custom environment\n\n```sudo systemd-run -r --set-env=[name]=[value] [command]```\n\n**Start** a transient timer that runs periodically\n\n```sudo systemd-run --on-calendar=[calendar_event] [command]```\n\n**Run** interactively with terminal access\n\n```systemd-run -r --pty [command]```\n\n**Set** resource limits and wait for completion\n\n```systemd-run -p MemoryMax=[bytes] -p CPUQuota=[percentage]% --wait [command]```\n\n**Use** in a shell pipeline\n\n```[command1] | systemd-run -P [command2] | [command3]```"],["SYNOPSIS","**systemd-run** [_options_] [_command_] [_arguments_]"],["PARAMETERS","**--user**\n> Run under user service manager\n\n**-u, --unit _name_**\n> Unit name for the transient service\n\n**--description _text_**\n> Description for the unit\n\n**-r, --remain-after-exit**\n> Keep unit after process exits\n\n**--set-env _name=value_**\n> Set environment variables\n\n**--on-calendar _spec_**\n> Create a timer unit\n\n**--pty**\n> Allocate a pseudo-TTY\n\n**-p, --property _name=value_**\n> Set unit properties\n\n**--wait**\n> Wait for service completion\n\n**-P, --pipe**\n> Enable pipe mode for use in pipelines.\n\n**-t, --pty**\n> Run interactively with a PTY attached (alias variant).\n\n**-q, --quiet**\n> Suppress informational messages.\n\n**--scope**\n> Create a transient .scope unit instead of a .service unit.\n\n**--slice** _NAME_\n> Add the unit into the specified slice.\n\n**--working-directory** _DIR_\n> Set the working directory for the invoked command.\n\n**--uid** _USER_, **--gid** _GROUP_\n> Run the command as the given user/group.\n\n**--nice** _N_\n> Set scheduling priority for the invoked command."],["DESCRIPTION","**systemd-run** runs programs in transient scope or service units managed by systemd. This allows applying resource controls, creating timers, and proper service management for ad-hoc commands.\n\nThe tool is useful for running commands with specific resource limits (CPU, memory), creating one-shot timers, and ensuring proper cleanup and logging through systemd."],["CAVEATS","Timer syntax follows systemd.time format. Resource properties require appropriate cgroup controllers. Some options require root privileges. Part of the systemd suite."],["INSTALL","```apt: sudo apt install systemd```\n\n```dnf: sudo dnf install systemd```\n\n```pacman: sudo pacman -S systemd```\n\n```zypper: sudo zypper install systemd```\n\n```brew: brew install systemd```\n\n```nix: nix profile install nixpkgs#systemd```"],["SEE ALSO","[systemctl](/man/systemctl)(1), [systemd-analyze](/man/systemd-analyze)(1), [systemd-cgls](/man/systemd-cgls)(1), [systemd-inhibit](/man/systemd-inhibit)(1)"]],"e":["systemctl","systemd-analyze","systemd-cgls","systemd-inhibit"],"x":"sudo systemd-run [command] [arguments]"},"tcprewrite":{"n":"tcprewrite","t":"Edit pcap packet headers","s":[["TAGLINE","Edit pcap packet headers"],["TLDR","**Rewrite source and destination MAC addresses**\n\n```tcprewrite --enet-smac=[00:11:22:33:44:55] --enet-dmac=[66:77:88:99:aa:bb] -i [input.pcap] -o [output.pcap]```\n\n**Rewrite IP addresses**\n\n```tcprewrite --srcipmap=[192.168.1.0/24:10.0.0.0/24] --dstipmap=[192.168.2.0/24:10.0.1.0/24] -i [input.pcap] -o [output.pcap]```\n\n**Rewrite port numbers**\n\n```tcprewrite --portmap=[80:8080,443:8443] -i [input.pcap] -o [output.pcap]```\n\n**Randomize IP addresses**\n\n```tcprewrite --seed=[42] --pnat=[192.168.0.0/16:10.0.0.0/8] -i [input.pcap] -o [output.pcap]```\n\n**Fix checksums after editing**\n\n```tcprewrite --fixcsum -i [input.pcap] -o [output.pcap]```\n\n**Change MTU and truncate**\n\n```tcprewrite --mtu=[1400] --mtu-trunc -i [input.pcap] -o [output.pcap]```"],["SYNOPSIS","**tcprewrite** [_options_] **-i** _input.pcap_ **-o** _output.pcap_"],["PARAMETERS","**-i**, **--infile** _file_\n> Input pcap file to read.\n\n**-o**, **--outfile** _file_\n> Output pcap file to write.\n\n**-c**, **--cachefile** _file_\n> tcpprep cache file for client/server classification.\n\n**--enet-smac** _mac_\n> Rewrite source Ethernet MAC address.\n\n**--enet-dmac** _mac_\n> Rewrite destination Ethernet MAC address.\n\n**--srcipmap** _cidr:cidr_\n> Rewrite source IP addresses.\n\n**--dstipmap** _cidr:cidr_\n> Rewrite destination IP addresses.\n\n**--pnat** _cidr:cidr_\n> Pseudo-NAT: rewrite IPs with randomization.\n\n**--portmap** _port:port_\n> Rewrite TCP/UDP port numbers.\n\n**--seed** _n_\n> Randomization seed for pseudo-NAT.\n\n**--fixcsum**\n> Recalculate TCP/UDP/IP checksums.\n\n**--mtu** _bytes_\n> Set maximum packet size.\n\n**--mtu-trunc**\n> Truncate packets exceeding MTU.\n\n**-C**\n> Recalculate checksums."],["DESCRIPTION","**tcprewrite** is a pcap file editor that rewrites packet headers at Layers 2, 3, and 4. It can modify Ethernet MAC addresses, IP addresses, and TCP/UDP port numbers in captured network traffic.\n\nThe tool is part of the Tcpreplay suite and is used to prepare captured traffic for replay in different network environments. After rewriting, packets can be replayed using **tcpreplay** or **tcpreplay-edit**.\n\nCommon use cases include anonymizing captures before sharing, adapting captures to different network configurations, and preparing traffic for security testing in lab environments.\n\nRewriting operations that change packet sizes or content require checksum recalculation using the --fixcsum or -C options."],["CAVEATS","Packets captured with \"any\" interface (cooked mode) cannot be properly edited. Some rewrites may require a tcpprep cache file for client/server classification. Large pcap files may require significant processing time. Does not handle application-layer protocol data (e.g., embedded IPs in HTTP)."],["HISTORY","**tcprewrite** is part of the Tcpreplay suite, originally developed by Aaron Turner in 2001. The project is now maintained by AppNeta and distributed under the GPLv3 license. It has become a standard tool for network testing and security analysis."],["INSTALL","```apt: sudo apt install tcpreplay```\n\n```dnf: sudo dnf install tcpreplay```\n\n```pacman: sudo pacman -S tcpreplay```\n\n```apk: sudo apk add tcpreplay```\n\n```zypper: sudo zypper install tcpreplay```\n\n```brew: brew install tcpreplay```\n\n```nix: nix profile install nixpkgs#tcpreplay```"],["SEE ALSO","[tcpreplay](/man/tcpreplay)(1), [tcpdump](/man/tcpdump)(1), [wireshark](/man/wireshark)(1)"]],"e":["tcpreplay","tcpdump","wireshark"],"x":"tcprewrite --enet-smac=[00:11:22:33:44:55] --enet-dmac=[66:77:88:99:aa:bb] -i [input.pcap] -o [output.pcap]"},"texdoc":{"n":"texdoc","t":"View TeX/LaTeX package documentation from TeX Live","s":[["TAGLINE","View TeX/LaTeX package documentation from TeX Live"],["TLDR","**Open the documentation** for a package\n\n```texdoc [package-name]```\n\n**List matching docs** without opening anything\n\n```texdoc -l [package-name]```\n\n**Full-text search** across documentation\n\n```texdoc -s [query]```\n\n**Open in a specific PDF viewer**\n\n```texdoc --viewer=[evince] [package-name]```\n\n**Show file paths** instead of opening\n\n```texdoc -f [package-name]```\n\n**Interactively pick** between matches\n\n```texdoc -I [package-name]```\n\n**Open multiple packages** at once\n\n```texdoc [package1] [package2] [package3]```"],["SYNOPSIS","**texdoc** [_options_] _name_..."],["PARAMETERS","**-l**, **--list**\n> Show numbered list of matches; prompt to choose one.\n\n**-s**, **--search**\n> Full-text search across the documentation tree.\n\n**-I**, **--interact**\n> Force interactive mode (opposite of **--nointeract**).\n\n**-M**, **--machine**\n> Machine-readable output: one line per match (file path, score, name).\n\n**-f**, **--files**\n> Print matching file paths instead of opening them.\n\n**-w**, **--view**\n> View documentation (default action).\n\n**-m**, **--mixed**\n> Combine name search and full-text search (useful when names overlap with content).\n\n**--viewer** _CMD_\n> Override the configured PDF viewer.\n\n**-D**, **--debug**\n> Print debug output to stderr.\n\n**-q**, **--quiet**\n> Suppress informational messages.\n\n**-v**, **--verbose**\n> Increase verbosity.\n\n**-V**, **--version**\n> Show texdoc version."],["DESCRIPTION","**texdoc** finds and displays documentation for TeX, LaTeX, ConTeXt, and related packages installed in a TeX Live distribution. It searches **TEXMF** trees for files associated with a package — usually a PDF manual but sometimes README, .txt, or .html — and opens the best match in a configured viewer.\n\nWhen multiple matches exist, **-l** prompts for a selection and **-s** broadens the search to the documentation contents (using the texdoc-fulltext index). The viewer is selected from configuration (**~/.texlive*/texmf-config/texdoc/texdoc.cnf**) or from the **PDFVIEWER** / **BROWSER** environment variables."],["CONFIGURATION","**texdoc.cnf**\n> Per-user configuration controlling viewer selection, mode aliases, and language preferences. Searched in **TEXMFCONFIG**, **TEXMFVAR**, **TEXMFHOME**, etc.\n\n**TEXDOCS**\n> kpathsea variable controlling where texdoc looks for documentation files."],["CAVEATS","Requires a TeX Live (or MiKTeX) installation with documentation packages installed; minimal \"scheme-small\" installs ship without docs. Not all packages provide documentation in PDF form. Full-text search depends on the **texdoc-fulltext** package being installed."],["HISTORY","**texdoc** is part of TeX Live, written in Lua and bundled with **luatex**. Earlier shell-script versions were superseded by the Lua implementation in **TeX Live 2009**."],["INSTALL","```pacman: sudo pacman -S texlive-binextra```\n\n```apk: sudo apk add texlive-binextra```"],["SEE ALSO","[tlmgr](/man/tlmgr)(1), [latex](/man/latex)(1), [pdflatex](/man/pdflatex)(1)"]],"e":["tlmgr","latex","pdflatex"],"x":"texdoc [package-name]"},"tip":{"n":"tip","t":"Connect to a remote system over a serial line","s":[["TAGLINE","Connect to a remote system over a serial line"],["TLDR","**Connect** to a system defined in /etc/remote\n\n```tip [system_name]```\n\n**Connect** at a specific baud rate\n\n```tip -[115200] [system_name]```\n\n**Connect** directly to a serial device\n\n```tip [/dev/ttyUSB0]```\n\n**Connect** with verbose output\n\n```tip -v [system_name]```\n\n**Connect** with tilde escape sequences disabled\n\n```tip -n [system_name]```"],["SYNOPSIS","**tip** [**-v**] [**-**_speed_] {_system_ | _phone-number_ | _device_}"],["DESCRIPTION","**tip** establishes a full-duplex terminal connection to a remote host over a serial line, modem, or directly attached serial device. It is one of the classic Unix ways to talk to a serial console, a microcontroller, a router's management port, or a dial-up system.\n\nConnection targets are usually named entries in the **/etc/remote** database, which records the device, baud rate, and other settings for each system. A target may also be given as a bare device path or, with a modem, a phone number to dial.\n\nWhile connected, **tip** is mostly transparent: keystrokes are sent to the remote side and remote output is shown locally. Commands to the local **tip** process are entered through tilde (**~**) escape sequences typed at the start of a line, for example **~.** to disconnect, **~p** to send a file, or **~C** to fork a local shell."],["PARAMETERS","**-v**\n\n> Verbose mode: echo commands from a script file as they run.\n\n**-**_speed_\n\n> Set the connection baud rate (for example **-115200** or **-9600**), overriding the value from /etc/remote.\n\n**-n**\n\n> Disable interpretation of tilde escape sequences for the session."],["ESCAPE SEQUENCES","**~.**\n\n> Drop the connection and exit.\n\n**~c** _dir_\n\n> Change the local working directory.\n\n**~!**\n\n> Fork an interactive shell on the local host.\n\n**~p** _from_ [_to_]\n\n> Send (put) a file to the remote host.\n\n**~t** _from_ [_to_]\n\n> Receive (take) a file from the remote host.\n\n**~#**\n\n> Send a BREAK to the remote system."],["CAVEATS","**tip** is a BSD utility and is not installed by default on most Linux distributions, where **cu**, **minicom**, or **picocom** are more common. The exact set of supported escape sequences and options varies between implementations; consult the local manual page."],["HISTORY","**tip** originated in Berkeley Unix (BSD) alongside the **cu** (call Unix) utility and the UUCP suite. It remains part of the base system on the BSDs and is one of the oldest tools for serial terminal access."],["SEE ALSO","[cu](/man/cu)(1), [minicom](/man/minicom)(1), [picocom](/man/picocom)(1), [screen](/man/screen)(1), [stty](/man/stty)(1)"],["RESOURCES","```[Source code](https://github.com/freebsd/freebsd-src/tree/main/usr.bin/tip)```"]],"e":["cu","minicom","picocom","screen","stty"],"x":"tip [system_name]"},"usbip":{"n":"usbip","t":"Share USB devices over IP networks","s":[["TAGLINE","Share USB devices over IP networks"],["TLDR","List **local** devices\n\n```usbip list -l```\n\n**Bind** device on server\n\n```sudo usbip bind -b [bus_id]```\n\nLoad **kernel module** on client\n\n```sudo modprobe vhci-hcd```\n\n**Attach** to remote device\n\n```sudo usbip attach -r [ip_address] -b [bus_id]```\n\nList **attached** devices\n\n```usbip port```\n\n**Detach** from device\n\n```sudo usbip detach -p [port]```\n\n**Unbind** device\n\n```usbip unbind -b [bus_id]```"],["SYNOPSIS","**usbip** _COMMAND_ [_OPTIONS_]"],["COMMANDS","**list**\n> List USB devices\n\n**bind**\n> Bind device for remote sharing\n\n**unbind**\n> Unbind device from sharing\n\n**attach**\n> Attach to remote USB device\n\n**detach**\n> Detach from remote device\n\n**port**\n> Show attached remote devices"],["PARAMETERS","**-l, --local**\n> List local devices\n\n**-r, --remote** _IP_\n> Remote server address\n\n**-b, --busid** _ID_\n> Device bus ID\n\n**-p, --port** _PORT_\n> Port number for detach"],["DESCRIPTION","**usbip** enables sharing USB devices over IP networks. Devices can be exported from a server and attached to clients as if they were locally connected.\n\nThe server runs the usbipd daemon and binds devices for sharing. Clients load the vhci-hcd kernel module and attach to remote devices."],["CAVEATS","Requires kernel modules on both server and client. Network latency affects device performance. Not suitable for high-bandwidth devices. Security depends on network configuration."],["INSTALL","```apt: sudo apt install usbip```\n\n```dnf: sudo dnf install usbip```\n\n```pacman: sudo pacman -S usbip```\n\n```apk: sudo apk add linux-tools-usbip```\n\n```zypper: sudo zypper install usbip```"],["SEE ALSO","[lsusb](/man/lsusb)(8), [modprobe](/man/modprobe)(8)"]],"e":["lsusb","modprobe"],"x":"usbip list -l"},"vectorize-pixelart":{"n":"vectorize-pixelart","t":"Convert pixel art to SVG vectors","s":[["TAGLINE","Convert pixel art to SVG vectors"],["TLDR","**Convert pixel art**\n\n```vectorize-pixelart [input.png] [output.svg]```\n\n**With scale**\n\n```vectorize-pixelart -s [4] [input.png] [output.svg]```\n\n**Simplify curves**\n\n```vectorize-pixelart --simplify [input.png] [output.svg]```"],["SYNOPSIS","**vectorize-pixelart** [_-s scale_] [_options_] _input_ _output_"],["PARAMETERS","**-s** _SCALE_\n> Output scale.\n\n**--simplify**\n> Simplify paths.\n\n**--help**\n> Show help."],["DESCRIPTION","**vectorize-pixelart** converts pixel art images into scalable vector graphics (SVG). Unlike general-purpose image tracers, it is specifically designed for pixel art, preserving the sharp edges and clean geometric shapes that define the style.\n\nThe resulting SVG files can be scaled to any resolution without blurring or aliasing artifacts. This makes the tool useful for preparing pixel art assets for high-resolution displays, print media, or web use where crisp rendering at multiple sizes is needed."],["CAVEATS","Pixel art only. Not for photos. Rust implementation."],["HISTORY","**vectorize-pixelart** was created for converting pixel art images to scalable vector graphics."],["SEE ALSO","[potrace](/man/potrace)(1), [inkscape](/man/inkscape)(1), [convert](/man/convert)(1)"]],"e":["potrace","inkscape","convert"],"x":"vectorize-pixelart [input.png] [output.svg]"},"wpexec":{"n":"wpexec","t":"run a WirePlumber Lua script outside of the daemon","s":[["TAGLINE","run a WirePlumber Lua script outside of the daemon"],["TLDR","**Run a Lua** script\n\n```wpexec [path/to/script.lua]```\n\n**Run a script with arguments**\n\n```wpexec [path/to/script.lua] [arg1] [arg2]```\n\n**Show help**\n\n```wpexec --help```"],["SYNOPSIS","**wpexec** [_OPTIONS_] _SCRIPT_ [_ARGUMENTS_]"],["PARAMETERS","_SCRIPT_\n> Path to the Lua script to execute against the running WirePlumber daemon.\n\n_ARGUMENTS_\n> Positional arguments passed to the script and accessible via the **arg** Lua table.\n\n**-h**, **--help**\n> Display help information and exit."],["DESCRIPTION","**wpexec** loads and runs a single WirePlumber Lua script in an isolated context. Unlike scripts that live in the **/usr/share/wireplumber/scripts/** tree and are managed by the daemon, **wpexec** spins up its own connection to the running WirePlumber session, executes the script, and exits when the script finishes.\n\nThe full WirePlumber Lua API is available, including the **wp.Core**, **wp.ObjectManager**, **wp.Node**, and **wp.Link** classes, so the tool is well suited to ad-hoc PipeWire queries, one-off graph manipulations, and testing snippets that may later be promoted into a managed script."],["CAVEATS","Requires both **WirePlumber** and **PipeWire** to be running. Scripts must be written against the version of the WirePlumber Lua API installed on the system; API breaks are documented in the WirePlumber release notes. Errors raised inside a script propagate to the exit status of **wpexec** but generally do not affect the long-running WirePlumber daemon."],["HISTORY","**wpexec** ships with **WirePlumber**, the session and policy manager for PipeWire developed under the FreeDesktop.org umbrella. It exists alongside the **wpctl** control utility to give power users and packagers a way to drive PipeWire from Lua without modifying the daemon's own configuration."],["INSTALL","```apt: sudo apt install wireplumber```\n\n```dnf: sudo dnf install wireplumber```\n\n```pacman: sudo pacman -S wireplumber```\n\n```apk: sudo apk add wireplumber```\n\n```zypper: sudo zypper install wireplumber```\n\n```brew: brew install wireplumber```\n\n```nix: nix profile install nixpkgs#wireplumber```"],["SEE ALSO","[wpctl](/man/wpctl)(1), [wireplumber](/man/wireplumber)(1), [pw-cli](/man/pw-cli)(1), [pw-dump](/man/pw-dump)(1)"]],"e":["wpctl","wireplumber","pw-cli","pw-dump"],"x":"wpexec [path/to/script.lua]"},"zenity":{"n":"zenity","t":"Display GTK dialogs from shell scripts","s":[["TAGLINE","Display GTK dialogs from shell scripts"],["TLDR","**Display** the default question dialog\n\n```zenity --question```\n\n**Display** an info dialog with a message\n\n```zenity --info --text \"[message]\"```\n\n**Display** a name/password form with custom separator\n\n```zenity --forms --add-entry \"[name_label]\" --add-password \"[password_label]\" --separator \";\"```\n\n**Display** a file selection for directories only\n\n```zenity --file-selection --directory```\n\n**Display** a progress bar that updates\n\n```(echo \"#1\"; sleep 1; echo \"50\"; echo \"#2\"; sleep 1; echo \"100\") | zenity --progress```"],["SYNOPSIS","**zenity** [_options_]"],["PARAMETERS","**--question**\n> Display a question dialog with Yes/No buttons\n\n**--info**\n> Display an information dialog\n\n**--warning**\n> Display a warning dialog\n\n**--error**\n> Display an error dialog\n\n**--file-selection**\n> Display a file selection dialog\n\n**--forms**\n> Display a forms dialog\n\n**--progress**\n> Display a progress dialog\n\n**--text _text_**\n> Set dialog text\n\n**--add-entry _label_**\n> Add a text entry field (forms)\n\n**--add-password _label_**\n> Add a password field (forms)\n\n**--directory**\n> Allow only directory selection\n\n**--separator _char_**\n> Set output separator"],["DESCRIPTION","**zenity** displays GTK+ dialogs from command-line scripts. It provides various dialog types including questions, information, file selection, progress bars, and forms.\n\nReturn values indicate user action: 0 for OK/Yes, 1 for Cancel/No, 5 for timeout, and -1 for errors. Form data is returned on stdout."],["CAVEATS","Requires a graphical environment (X11 or Wayland with XWayland). Dialog appearance depends on GTK theme. For KDE environments, consider **kdialog** as an alternative."],["INSTALL","```apt: sudo apt install zenity```\n\n```dnf: sudo dnf install zenity```\n\n```pacman: sudo pacman -S zenity```\n\n```apk: sudo apk add zenity```\n\n```zypper: sudo zypper install zenity```\n\n```brew: brew install zenity```\n\n```nix: nix profile install nixpkgs#zenity```"],["SEE ALSO","[kdialog](/man/kdialog)(1), [dialog](/man/dialog)(1), [whiptail](/man/whiptail)(1)"]],"e":["kdialog","dialog","whiptail"],"x":"zenity --question"}});
