window.LCL_BUCKET(78,{"acccheck":{"n":"acccheck","t":"Windows SMB password dictionary attack tool","s":[["TAGLINE","Windows SMB password dictionary attack tool"],["TLDR","**Check** for blank passwords on a target\n\n```acccheck -t [192.168.1.1]```\n\nUse a **password list** against a single user\n\n```acccheck -t [192.168.1.1] -u [administrator] -P [passwords.txt]```\n\nUse **username and password lists**\n\n```acccheck -t [192.168.1.1] -U [users.txt] -P [passwords.txt]```\n\nAttack **multiple targets** from a file\n\n```acccheck -T [targets.txt] -U [users.txt] -P [passwords.txt]```\n\nCheck with **verbose** output\n\n```acccheck -t [192.168.1.1] -U [users.txt] -P [passwords.txt] -v```"],["SYNOPSIS","**acccheck** [_-t target_ | _-T targetfile_] [_-u user_ | _-U userfile_] [_-p pass_ | _-P passfile_] [_-v_]"],["DESCRIPTION","**acccheck** is a password dictionary attack tool designed specifically for Windows SMB authentication. It attempts to connect to the Windows IPC$ and ADMIN$ shares using provided credentials to identify valid username/password combinations.\n\nThe tool is useful during penetration testing to identify weak or default passwords on Windows systems. It can test single credentials or iterate through wordlists for both usernames and passwords."],["PARAMETERS","**-t** _target_\n> Single target IP address.\n\n**-T** _file_\n> File containing list of target IPs.\n\n**-u** _username_\n> Single username to test.\n\n**-U** _file_\n> File containing list of usernames.\n\n**-p** _password_\n> Single password to test.\n\n**-P** _file_\n> File containing list of passwords.\n\n**-v**\n> Verbose mode (show all attempts)."],["CAVEATS","Intended for authorized security testing only. Unauthorized access attempts are illegal. **acccheck** is a Perl wrapper around **smbclient**, so it depends on a working Samba client installation. Successful login pairs are appended to a file named **cracked** in the current working directory. Account lockout policies may trigger after multiple failed attempts, and the tool relies on SMB connectivity (ports 139/445)."],["HISTORY","**acccheck** was written by Faisal Dean (Faiz) and released as part of various penetration testing tool collections. It became part of Kali Linux and other security-focused distributions for Windows network security assessments."],["SEE ALSO","[smbclient](/man/smbclient)(1), [hydra](/man/hydra)(1), [medusa](/man/medusa)(1), [nmap](/man/nmap)(1), [enum4linux](/man/enum4linux)(1), [crackmapexec](/man/crackmapexec)(1)"]],"e":["smbclient","hydra","medusa","nmap","enum4linux","crackmapexec"],"x":"acccheck -t [192.168.1.1]"},"amidi":{"n":"amidi","t":"Send and receive raw MIDI data","s":[["TAGLINE","Send and receive raw MIDI data"],["TLDR","**List** MIDI hardware ports\n\n```amidi -l```\n\n**Send** a MIDI file to a port\n\n```amidi -p [hw:1,0,0] -s [file.syx]```\n\n**Receive** MIDI data to file\n\n```amidi -p [hw:1,0,0] -r [output.syx]```\n\n**Dump** incoming MIDI messages\n\n```amidi -p [hw:1,0,0] -d```\n\nSend **system exclusive** data\n\n```amidi -p [hw:1,0,0] -S \"F0 43 10 4C 00 00 7E 00 F7\"```"],["SYNOPSIS","**amidi** [_-l_] [_-p port_] [_-s file_] [_-r file_] [_-S hex_] [_-d_] [_options_]"],["DESCRIPTION","**amidi** is an ALSA utility for sending and receiving raw MIDI data. It works at the byte level, making it suitable for sending system exclusive (SysEx) messages, firmware updates to MIDI devices, or capturing MIDI dumps.\n\nUnlike higher-level MIDI sequencers, amidi doesn't interpret MIDI timing or song structure; it transfers raw data to and from hardware ports."],["PARAMETERS","**-l**, **--list-devices**\n> List available hardware MIDI ports\n\n**-L**, **--list-rawmidis**\n> List all RawMIDI definitions (useful when debugging configuration files)\n\n**-p** _port_, **--port=**_port_\n> RawMIDI port to use (e.g., hw:1,0,0). Defaults to port 0 on card 0\n\n**-s** _file_, **--send=**_file_\n> Send the raw MIDI commands in file to the port (e.g., a .syx file)\n\n**-r** _file_, **--receive=**_file_\n> Receive data from the port and write it to file\n\n**-S** _hex_, **--send-hex=**_hex_\n> Send the bytes given as hexadecimal numbers to the port\n\n**-d**, **--dump**\n> Print received data as hexadecimal bytes\n\n**-t** _seconds_, **--timeout=**_seconds_\n> Stop receiving when no data arrives for the given time\n\n**-a**, **--active-sensing**\n> Do not ignore Active Sensing bytes (FEh)\n\n**-c**, **--clock**\n> Do not ignore Clock bytes (F8h)\n\n**-i** _ms_, **--sysex-interval=**_ms_\n> Add a delay between each SysEx message sent, useful for firmware updates"],["CAVEATS","Works only with hardware MIDI ports, not virtual/software ports. SysEx transfers can be large and slow. Some devices require specific timing between messages."],["HISTORY","**amidi** is part of the ALSA utilities, providing low-level MIDI access since ALSA replaced OSS in the Linux kernel in the early **2000s**."],["INSTALL","```apt: sudo apt install alsa-utils```\n\n```dnf: sudo dnf install alsa-utils```\n\n```pacman: sudo pacman -S alsa-utils```\n\n```apk: sudo apk add alsa-utils```\n\n```zypper: sudo zypper install alsa-utils```\n\n```nix: nix profile install nixpkgs#alsa-utils```"],["SEE ALSO","[aconnect](/man/aconnect)(1), [aplaymidi](/man/aplaymidi)(1), [arecordmidi](/man/arecordmidi)(1)"],["RESOURCES","```[Source code](https://github.com/alsa-project/alsa-utils)```\n\n```[Homepage](https://www.alsa-project.org)```"]],"e":["aconnect","aplaymidi","arecordmidi"],"x":"amidi -l"},"authelia":{"n":"authelia","t":"Authentication and authorization server with multi-factor support","s":[["TAGLINE","Authentication and authorization server with multi-factor support"],["TLDR","**Validate configuration file**\n\n```authelia validate-config --config [config.yml]```\n\n**Generate password hash**\n\n```authelia crypto hash generate argon2```\n\n**Generate random secret**\n\n```authelia crypto rand --length [64]```\n\n**Generate RSA keypair**\n\n```authelia crypto pair rsa generate```\n\n**Generate TOTP secret**\n\n```authelia crypto totp generate --issuer [MyApp]```\n\n**Hash a password interactively**\n\n```authelia crypto hash generate argon2 --password```\n\n**Show version**\n\n```authelia --version```\n\n**Run the server**\n\n```authelia --config [/etc/authelia/config.yml]```"],["SYNOPSIS","**authelia** [_global-options_] _command_ [_options_]"],["COMMANDS","**crypto** hash|pair|rand|certificate|totp\n> Cryptographic utilities for generating secrets and hashes.\n\n**validate-config**\n> Validate configuration file syntax and values.\n\n**storage** migrate|schema-info|user|encryption\n> Database storage management and migrations.\n\n**access-control** check-policy\n> Test access control rules against requests.\n\n**build-info**\n> Display build information."],["CRYPTO SUBCOMMANDS","**crypto hash generate** argon2|pbkdf2|sha2crypt|bcrypt\n> Generate password hash using specified algorithm.\n\n**crypto pair** rsa|ecdsa|ed25519\n> Generate cryptographic key pairs.\n\n**crypto rand**\n> Generate random bytes or strings.\n\n**crypto certificate** rsa|ecdsa|ed25519\n> Generate self-signed certificates.\n\n**crypto totp generate**\n> Generate TOTP secret and QR code."],["PARAMETERS","**--config**, **-c** _path_\n> Path to configuration file(s).\n\n**--password**\n> Read password interactively.\n\n**--length** _n_\n> Length for random generation.\n\n**--issuer** _name_\n> TOTP issuer name.\n\n**--algorithm** _algo_\n> Hashing or encryption algorithm.\n\n**--help**, **-h**\n> Display help information.\n\n**--version**\n> Display version information."],["DESCRIPTION","**authelia** is the CLI for Authelia, an open-source authentication and authorization server. It provides multi-factor authentication and single sign-on for applications behind a reverse proxy.\n\nThe **crypto** commands generate secrets, password hashes, certificates, and TOTP configurations needed for Authelia setup. Use **crypto hash generate argon2** to create password hashes for the user database.\n\nConfiguration validation with **validate-config** checks YAML syntax and setting values before deployment. This catches common configuration errors.\n\nThe **storage** commands manage the user database, including migrations between versions and encryption key rotation. Authelia stores user credentials, TOTP secrets, and session data.\n\nAuthelia integrates with reverse proxies like Nginx, Traefik, and HAProxy to protect web applications with authentication portals and access policies."],["CONFIGURATION","**/etc/authelia/configuration.yml**\n> Main configuration file defining authentication backends, session settings, access control rules, and storage options.\n\n**/etc/authelia/users_database.yml**\n> File-based user database when using the file authentication backend. Contains usernames, password hashes, and group memberships."],["CAVEATS","Authelia runs as a service behind a reverse proxy; the CLI is for configuration and utilities. Requires proper reverse proxy configuration for authentication flow. User database and session storage must be configured. Some features require Redis for session storage in HA deployments."],["HISTORY","Authelia was created by **Clement Michaud** in **2016** as a simple authentication portal. The project evolved to support multiple second-factor methods (TOTP, WebAuthn, Duo) and advanced access control. Version 4 brought major architectural improvements and became widely adopted for self-hosted authentication. The project emphasizes security, privacy, and ease of deployment with container-first design."],["INSTALL","```aur: yay -S authelia```\n\n```nix: nix profile install nixpkgs#authelia```"],["SEE ALSO","[keycloak](/man/keycloak)(1), [nginx](/man/nginx)(1), [traefik](/man/traefik)(1)"]],"e":["keycloak","nginx","traefik"],"x":"authelia validate-config --config [config.yml]"},"az-resource":{"n":"az-resource","t":"Manage Azure resources generically across all types","s":[["TAGLINE","Manage Azure resources generically across all types"],["TLDR","**List all resources** in the subscription\n\n```az resource list```\n\n**List resources** in a resource group\n\n```az resource list --resource-group [rg_name]```\n\n**Show a resource** by ID\n\n```az resource show --ids [/subscriptions/.../resourceGroups/.../providers/.../name]```\n\n**Show a resource** by name and type\n\n```az resource show --resource-group [rg_name] --resource-type [Microsoft.Web/sites] --name [resource_name]```\n\n**Delete a resource**\n\n```az resource delete --ids [resource_id]```\n\n**Move resources** to another resource group\n\n```az resource move --destination-group [dest_rg] --ids [resource_id1] [resource_id2]```\n\n**Tag a resource**\n\n```az resource tag --tags [env=prod] [team=backend] --ids [resource_id]```\n\n**Update a resource** property\n\n```az resource update --ids [resource_id] --set properties.someProperty=value```"],["SYNOPSIS","**az resource** _subcommand_ [_options_]"],["DESCRIPTION","**az resource** is a subcommand of the Azure CLI that manages Azure resources generically across all resource types. It provides a unified interface for operations that apply to any resource, regardless of the specific service.\n\nResources in Azure are identifiable entities like virtual machines, storage accounts, or databases. Each has a unique resource ID following the pattern /subscriptions/{sub}/resourceGroups/{rg}/providers/{provider}/{type}/{name}.\n\nThis command is useful for scripting cross-resource operations, bulk management, and working with resource types that may not have dedicated CLI command groups."],["PARAMETERS","**list**\n> List resources with optional filters.\n\n**show**\n> Display resource details.\n\n**create**\n> Create a resource from properties.\n\n**delete**\n> Remove a resource.\n\n**update**\n> Update a resource by PUT request.\n\n**patch**\n> Update a resource by PATCH request.\n\n**move**\n> Move resources between groups or subscriptions.\n\n**tag**\n> Tag a resource.\n\n**invoke-action**\n> Invoke an action on a resource.\n\n**wait**\n> Wait until a condition on a resource is met (provisioning, deletion, etc.).\n\n**--ids** _ids_\n> One or more resource IDs.\n\n**--resource-group** _rg_\n> Resource group name.\n\n**--resource-type** _type_\n> Resource type (e.g., Microsoft.Compute/virtualMachines).\n\n**--name** _name_\n> Resource name.\n\n**--namespace** _namespace_\n> Provider namespace.\n\n**--api-version** _version_\n> API version for the resource type.\n\n**--set** _property=value_\n> Update a property on the resource (e.g., properties.someProperty=value).\n\n**--add** _list property=value_\n> Add an object to a list of objects (e.g., --add tags.newKey=value).\n\n**--remove** _property/index_\n> Remove a property or an element from a list.\n\n**--tags** _tags_\n> Space-separated tags (key=value).\n\n**--query-string** _query_\n> Filter resources with OData query."],["CAVEATS","Some resources cannot be moved between resource groups or subscriptions. Deleting a resource may leave orphaned dependent resources. API version compatibility varies by resource type. Generic commands may lack service-specific validations available in dedicated command groups. Resource moves may take extended time and have service-specific limitations."],["HISTORY","The generic resource commands have been part of the Azure CLI since **Azure CLI 2.0** launched in **2017**. They provide ARM (Azure Resource Manager) API access for any resource type. As Azure has grown, dedicated command groups for services have expanded, but the generic resource commands remain valuable for automation, bulk operations, and accessing new resource types before dedicated commands exist."],["INSTALL","```dnf: sudo dnf install azure-cli```\n\n```pacman: sudo pacman -S azure-cli```\n\n```zypper: sudo zypper install azure-cli```\n\n```brew: brew install azure-cli```\n\n```nix: nix profile install nixpkgs#azure-cli```"],["SEE ALSO","[az](/man/az)(1), [az-group](/man/az-group)(1), [az-deployment](/man/az-deployment)(1), [az-provider](/man/az-provider)(1), [az-tag](/man/az-tag)(1)"],["RESOURCES","```[Source code](https://github.com/Azure/azure-cli)```\n\n```[Documentation](https://learn.microsoft.com/en-us/cli/azure/resource)```\n\n```[Homepage](https://learn.microsoft.com/en-us/cli/azure/)```"]],"e":["az","az-group","az-deployment","az-provider","az-tag"],"x":"az resource list"},"bibiman":{"n":"bibiman","t":"TUI for managing BibLaTeX databases with Vim-like keybindings","s":[["TAGLINE","TUI for managing BibLaTeX databases with Vim-like keybindings"],["TLDR","Browse entries in a **bibliography file**\n\n```bibiman [path/to/references.bib]```\n\nOpen **multiple bibliography files** simultaneously\n\n```bibiman [file1.bib] [file2.bib]```\n\nRecursively load all **.bib files** from a directory\n\n```bibiman [path/to/directory]```\n\nUse a **custom configuration** file\n\n```bibiman -c [path/to/bibiman.toml] [path/to/file.bib]```\n\nLoad **only CLI-specified files**, ignoring the config\n\n```bibiman -C [path/to/file.bib]```\n\nEnable colors optimized for **light terminal** backgrounds\n\n```bibiman --light-terminal [path/to/file.bib]```\n\n**Set a custom PDF directory** for matching PDFs to entries\n\n```bibiman --pdf-path [path/to/pdfs] [path/to/file.bib]```\n\n**Format citekeys** in a bibliography file\n\n```bibiman format-citekeys [path/to/file.bib]```"],["SYNOPSIS","**bibiman** [**-h**|**--help**] [**-v**|**--version**] [**-c** _path_] [**-l** _path_] [**--pdf-path** _path_] [**--light-terminal**] [**-C**] [_file_|_directory_]...\n\n**bibiman** **format-citekeys** [_options_] _file_"],["PARAMETERS","**-h**, **--help**\n> Display help information and exit\n\n**-v**, **--version**\n> Print the installed version and exit\n\n**-c**, **--config-file** _path_\n> Specify a custom configuration file location\n\n**-l**, **--log-file** _path_\n> Specify a custom log file location\n\n**--pdf-path** _path_\n> Set the directory path for PDF file discovery during the session\n\n**--light-terminal**\n> Enable color scheme optimized for light terminal backgrounds\n\n**-C**, **--cli-only**\n> Load only files specified on the command line, ignoring any files defined in the configuration\n\n**format-citekeys**\n> Subcommand to reformat citekeys in a bibliography file using pattern syntax"],["DESCRIPTION","**bibiman** is a terminal user interface for managing BibLaTeX bibliographic databases as part of a terminal-based scientific workflow. Written in Rust, it provides fast, keyboard-driven interaction with .bib files.\n\nThe interface uses Vim-like keybindings for navigation: **j**/**k** or arrow keys to move between entries, **Ctrl-d**/**Ctrl-u** to jump by five entries, **g**/**G** to jump to the first or last entry, and **h**/**l** to select columns. Pressing **?** opens a help popup with the full keybinding reference.\n\nEntries can be searched with **/** or **Ctrl-f** for fuzzy search across all fields, or **Alt-/** to search a specific field. Fuzzy matching is provided by the nucleo-matcher engine. Special prefixes modify matching behavior: **^** anchors to the beginning, **$** anchors to the end, and **'** forces literal matching. Pressing **TAB** switches between entry and keyword views, allowing filtering by keywords.\n\nPressing **e** opens the current entry in a terminal editor at the correct line. The editor is determined by the config file, then the **VISUAL** environment variable, then **EDITOR**, falling back to **vi**. Pressing **y** copies the citekey to the system clipboard, **o** opens an associated PDF, URL, or DOI link, and **n** creates or opens a note file linked to the entry. Entries can be added via DOI lookup with **a**.\n\nSorting is available by pressing **s** to cycle through author, title, and year columns, or **S** to restore the original file position order. A custom column can display additional metadata such as journal title, publisher, institution, series, or publication type.\n\nMultiple .bib files can be loaded in a single session by passing several file arguments or a directory path. When a directory is given, bibiman recursively searches for all .bib files within it. PDF files are matched to entries either through the BibTeX **file** field or by matching the citekey against filenames in the configured PDF directory.\n\nThe **format-citekeys** subcommand provides batch citekey reformatting via the command line using a configurable pattern syntax, without launching the TUI."],["CONFIGURATION","The configuration file is located at **$XDG_CONFIG_HOME/bibiman/bibiman.toml** (typically ~/.config/bibiman/bibiman.toml).\n\n```[general]\nbibfiles = [\"/path/to/references.bib\"]\neditor = { command = \"vim\", args = [] }\npdf_opener = \"xdg-open\"\nurl_opener = \"xdg-open\"\npdf_path = \"/path/to/pdfs\"\nnote_path = \"/path/to/notes\"\nnote_extensions = [\"md\", \"txt\"]\ncustom_column = \"pubtype\"\nclipboard = \"wayland\"```\n\nThe **editor** field accepts either a simple string or a table with **command** and **args** keys. The **custom_column** field controls what extra metadata appears in the entry list and can be set to **journaltitle**, **organization**, **institution**, **series**, **publisher**, or **pubtype**. The **clipboard** field selects the clipboard provider: **wayland**, **x-clip**, **x-sel**, **pasteboard**, **tmux**, or **win32-yank**.\n\nColors are configured in a **[colors]** section using ANSI color names, 256-color indices, or hex codes for elements such as **main_text_color**, **highlight_text_color**, **entry_color**, **keyword_color**, and **popup_bg_color**.\n\nCitekey formatting rules are defined in a **[citekey_formatter]** section with options for **fields**, **ascii_only**, and **case**."],["CAVEATS","Citekeys cannot contain the special characters **/ | # * \\ \" ' ; !** as these interfere with note file creation and other operations. The editor must support the **+line** argument syntax for jumping to a specific line. Windows support for opening PDF files and URLs is untested. LaTeX macros are hidden in the display for readability but are preserved in the underlying .bib file."],["HISTORY","**bibiman** was created by **lukeflo** and first published to crates.io as version 0.7.0 in **November 2024**. The first official Codeberg release (v0.15.0) followed in **October 2025**. The project is written in Rust without AI-generated code, licensed under GPL-3.0-or-later, and hosted on Codeberg. As of early 2026, development remains active with version 0.19.3 released in January 2026 and over 376 commits in the repository."],["SEE ALSO","[biber](/man/biber)(1), [bibtex](/man/bibtex)(1), [papis](/man/papis)(1), [latex](/man/latex)(1)"],["INSTALL","```nix: nix profile install nixpkgs#bibiman```"]],"e":["biber","bibtex","papis","latex"],"x":"bibiman [path/to/references.bib]"},"btrace":{"n":"btrace","t":"Dynamic tracing tool for Java applications","s":[["TAGLINE","Dynamic tracing tool for Java applications"],["TLDR","**Trace method calls** in a running Java application\n\n```btrace [pid] [script.java]```\n\n**Trace with classpath**\n\n```btrace -cp [/path/to/classes] [pid] [script.java]```\n\n**Trace with unsafe mode** (allows more operations)\n\n```btrace -u [pid] [script.java]```\n\n**Compile a BTrace script**\n\n```btracec [script.java]```\n\n**Run with debug output**\n\n```btrace -v [pid] [script.java]```"],["SYNOPSIS","**btrace** [_options_] _pid_ _script_\n\n**btracec** [_options_] _script_"],["DESCRIPTION","**BTrace** is a safe, dynamic tracing tool for the Java platform. It allows attaching tracing scripts to running Java applications without restarting them. BTrace scripts are written in Java with annotations defining trace points.\n\nBTrace works by instrumenting bytecode of the target application. Scripts can intercept method entries, exits, exceptions, and other events. The tracing is designed to be safe, preventing scripts from modifying application state or causing deadlocks.\n\nCommon use cases include performance profiling, debugging production issues, monitoring method call patterns, and tracking memory allocations without application restarts."],["PARAMETERS","**pid**\n> Process ID of target Java application.\n\n**script**\n> BTrace script file (.java).\n\n**-v**\n> Verbose mode.\n\n**-u**\n> Unsafe mode (enables more operations).\n\n**-d** _path_\n> Dump instrumented classes to path.\n\n**-pd** _path_\n> Probe descriptor search path.\n\n**-cp** _classpath_\n> Classpath for script compilation.\n\n**-I** _path_\n> Preprocessor include path.\n\n**-p** _port_\n> Agent server port.\n\n**-statsd** _host:port_\n> Send metrics to StatsD server."],["CAVEATS","Target JVM must have similar or compatible Java version. Some operations are restricted in safe mode to prevent application harm. Cannot trace native methods. May have performance overhead on high-frequency methods. Requires write access to target application's process."],["HISTORY","**BTrace** was created by **A. Sundararajan** at Sun Microsystems around **2007-2008** as a DTrace-inspired tool for Java. After Oracle's acquisition of Sun, the project became open source and community-maintained. It has been used extensively for production debugging and performance analysis of Java applications, filling a gap for dynamic instrumentation in the JVM ecosystem."],["INSTALL","```apt: sudo apt install blktrace```\n\n```dnf: sudo dnf install blktrace```\n\n```zypper: sudo zypper install blktrace```\n\n```nix: nix profile install nixpkgs#blktrace```"],["SEE ALSO","[jstack](/man/jstack)(1), [jmap](/man/jmap)(1), [java](/man/java)(1)"]],"e":["jstack","jmap","java"],"x":"btrace [pid] [script.java]"},"cargo-test":{"n":"cargo-test","t":"Run unit and integration tests","s":[["TAGLINE","Run unit and integration tests"],["TLDR","**Run all tests**\n\n```cargo test```\n\n**Run specific test**\n\n```cargo test [test_name]```\n\n**Run tests in module**\n\n```cargo test [module_name]::**```\n\n**Run ignored tests**\n\n```cargo test -- --ignored```\n\n**Show output from passing tests**\n\n```cargo test -- --show-output```\n\n**Run documentation tests only**\n\n```cargo test --doc```\n\n**Run tests without stopping on failure**\n\n```cargo test -- --no-fail-fast```\n\n**Compile without running**\n\n```cargo test --no-run```"],["SYNOPSIS","**cargo test** [_options_] [_testname_] [-- _test-options_]"],["DESCRIPTION","**cargo test** compiles and runs the test suite for a Rust project. It discovers unit tests (functions annotated with `#[test]` in `src/`), integration tests (files in `tests/`), documentation tests (code blocks in doc comments), and benchmark tests. A test name filter can be provided to run only matching tests.\n\nArguments after `--` are passed directly to the test harness rather than to Cargo. This allows controlling test execution behavior such as thread count, output capture, and whether ignored tests should run. Tests run in parallel by default; use `--test-threads=1` for sequential execution when tests have shared state."],["PARAMETERS","**--no-run**\n> Compile without running\n\n**--no-fail-fast**\n> Run all tests regardless of failures\n\n**--doc**\n> Run documentation tests only\n\n**--lib**\n> Test library only\n\n**--bins**\n> Test all binaries\n\n**--tests**\n> Test all test targets\n\n**--benches**\n> Test all benchmarks\n\n**--all-targets**\n> Test all targets\n\n**-p**, **--package** _spec_\n> Test specified packages\n\n**--workspace**\n> Test all workspace members\n\n**-j**, **--jobs** _n_\n> Parallel build jobs\n\n**--release**\n> Test with release profile\n\n**--features** _features_\n> Enable features\n\n**--all-features**\n> Enable all features"],["TEST BINARY OPTIONS","**-- --ignored**\n> Run ignored tests\n\n**-- --show-output**\n> Show stdout from passing tests\n\n**-- --test-threads** _n_\n> Number of parallel test threads\n\n**-- --nocapture**\n> Don't capture test output"],["CAVEATS","Tests run in parallel by default. Use --test-threads=1 for sequential execution."],["INSTALL","```apt: sudo apt install cargo```\n\n```dnf: sudo dnf install cargo```\n\n```apk: sudo apk add cargo```\n\n```zypper: sudo zypper install cargo```\n\n```nix: nix profile install nixpkgs#cargo```"],["SEE ALSO","[cargo](/man/cargo)(1), [cargo-bench](/man/cargo-bench)(1)"],["RESOURCES","```[Source code](https://github.com/rust-lang/cargo)```\n\n```[Documentation](https://doc.rust-lang.org/cargo/commands/cargo-test.html)```"]],"e":["cargo","cargo-bench"],"x":"cargo test"},"clx":{"n":"clx","t":"ahead-of-time Lua compiler to native executables","s":[["TAGLINE","ahead-of-time Lua compiler to native executables"],["TLDR","**Compile** a Lua script to a native executable\n\n```clx [file.lua]```\n\nCompile with **custom output** name\n\n```clx [file.lua] --output [name]```\n\nOptimize for **speed** or **size**\n\n```clx [file.lua] --fast\nclx [file.lua] --size```\n\nEmit **C++ source** only (do not compile)\n\n```clx --cpp [file.lua]```\n\nProduce an **object file** or **static module**\n\n```clx --object [file.lua]\nclx --static [file.lua]```\n\n**Minimal** runtime (base + package modules only)\n\n```clx [file.lua] --minimal```"],["SYNOPSIS","**clx** [_options_] _file.lua_"],["DESCRIPTION","**clx** is a cross-platform ahead-of-time (AOT) Lua compiler and runtime that generates standalone native executables through a modern C++ toolchain (Clang, GCC, or MSVC). It targets Lua 5.5 compatibility and aims for fast startup, deployable binaries, and predictable performance without a bytecode interpreter.\n\nUnknown flags are forwarded to the backend C++ compiler (for example **-O2**). Size-oriented builds can produce compact executables; **--minimal** links only base and package modules."],["PARAMETERS","**--output** _name_\n> Output executable name\n\n**--object** / **--static** / **--cpp**\n> Emit object file, static library, or C++ source instead of a full executable\n\n**--fast** / **--size** / **--debug**\n> Optimize for speed, size (default orientation), or debug builds\n\n**--minimal**\n> Link only base and package modules\n\n**--version** / **--help**\n> Version or usage"],["CAVEATS","Dynamic loading (**load**, **dofile**, **loadfile**, **string.dump**) and full **debug** support are limited in a pure AOT model. The traditional Lua C API is not supported; native modules use the clx C++ API. The compiler used to build clx is fixed at build time for ABI compatibility with generated code."],["SEE ALSO","[lua](/man/lua)(1), [luajit](/man/luajit)(1)"],["RESOURCES","```[Source code](https://github.com/samyeyo/clx)```\n\n```[Homepage](https://samyeyo.github.io/clx)```\n\n```[Documentation](https://github.com/samyeyo/clx/blob/main/doc/index.md)```"]],"e":["lua","luajit"],"x":"clx [file.lua]"},"cowbuilder":{"n":"cowbuilder","t":"Copy-on-write Debian package builder","s":[["TAGLINE","Copy-on-write Debian package builder"],["TLDR","**Create a new cowbuilder environment**\n\n```sudo cowbuilder --create --distribution [bookworm]```\n\n**Update the cowbuilder environment**\n\n```sudo cowbuilder --update```\n\n**Build a package**\n\n```sudo cowbuilder --build [package.dsc]```\n\n**Login to the cowbuilder environment**\n\n```sudo cowbuilder --login```\n\n**Build for specific architecture**\n\n```sudo cowbuilder --build --architecture [i386] [package.dsc]```\n\n**Use specific base.cow location**\n\n```sudo cowbuilder --basepath [/var/cache/pbuilder/base-bookworm.cow] --build [package.dsc]```"],["SYNOPSIS","**cowbuilder** **--create** | **--update** | **--build** | **--login** [_options_]"],["PARAMETERS","**--create**\n> Create a new base.cow environment.\n\n**--update**\n> Update packages in the cowbuilder environment.\n\n**--build** _dsc_\n> Build a package from .dsc file.\n\n**--login**\n> Start a shell in the chroot environment.\n\n**--distribution** _dist_\n> Debian/Ubuntu distribution (bookworm, jammy, etc.).\n\n**--architecture** _arch_\n> Target architecture (amd64, i386, arm64).\n\n**--basepath** _path_\n> Path to base.cow directory.\n\n**--buildresult** _path_\n> Directory for build results.\n\n**--mirror** _url_\n> Package mirror URL.\n\n**--save-after-login**\n> Save changes made during login session."],["DESCRIPTION","**cowbuilder** is a wrapper around pbuilder that uses copy-on-write (COW) semantics for faster Debian package builds. Instead of creating a new chroot tarball for each build, it uses hard links to create a working copy, significantly reducing setup time.\n\nThe tool creates clean, reproducible build environments for Debian packages. Each build starts from a pristine base system, ensuring packages build correctly without hidden dependencies on the developer's system.\n\ncowbuilder is commonly used for verifying package builds before upload to Debian archives, testing packages across different distributions, and CI/CD pipelines for Debian packaging."],["CAVEATS","Requires root privileges for chroot operations. The base.cow directory must be on a filesystem supporting hard links (not across filesystems). First build after update may be slower due to cache warming. COW benefits are lost if base.cow is on different filesystem than build directory."],["HISTORY","cowbuilder was created as an enhancement to **pbuilder** (Personal Builder) by using COW filesystem techniques. Pbuilder itself was created by **Junichi Uekawa** in the early **2000s** for building Debian packages in clean environments. Cowbuilder addressed pbuilder's main performance bottleneck: extracting the base tarball for each build."],["INSTALL","```apt: sudo apt install cowbuilder```"],["SEE ALSO","[pbuilder](/man/pbuilder)(8), [sbuild](/man/sbuild)(1), [debuild](/man/debuild)(1), [dpkg-buildpackage](/man/dpkg-buildpackage)(1)"],["RESOURCES","```[Source code](https://salsa.debian.org/debian/cowdancer)```"]],"e":["pbuilder","sbuild","debuild","dpkg-buildpackage"],"x":"sudo cowbuilder --create --distribution [bookworm]"},"csound":{"n":"csound","t":"sound synthesis and audio programming language","s":[["TAGLINE","sound synthesis and audio programming language"],["TLDR","**Render orchestra and score** to audio\n\n```csound [orchestra.orc] [score.sco]```\n\n**Render unified csd file**\n\n```csound [file.csd]```\n\n**Output to WAV file**\n\n```csound -o [output.wav] [file.csd]```\n\n**Real-time audio output**\n\n```csound -odac [file.csd]```\n\n**Set sample rate**\n\n```csound -r [44100] [file.csd]```\n\n**Set control rate**\n\n```csound -k [4410] [file.csd]```"],["SYNOPSIS","**csound** [_options_] _files_..."],["DESCRIPTION","**csound** is a sound synthesis program and audio programming language. It generates audio from text-based scores and orchestras, enabling precise control over sound generation and processing.\n\nThe system uses two main components: an orchestra defining instruments with synthesis algorithms, and a score specifying when and how to play them. Modern usage typically combines these in CSD (unified) files."],["PARAMETERS","**-o** _file_\n> Output audio file (use dac for realtime).\n\n**-i** _file_\n> Input audio file (use adc for realtime).\n\n**-r** _rate_\n> Sample rate in Hz.\n\n**-k** _rate_\n> Control rate in Hz.\n\n**-b** _size_\n> Software buffer size.\n\n**-B** _size_\n> Hardware buffer size.\n\n**-d**\n> Suppress displays.\n\n**-m** _level_\n> Message level (0-231).\n\n**-W**\n> Create WAV format output.\n\n**-A**\n> Create AIFF format output.\n\n**--midi-device=** _dev_\n> MIDI input device."],["CAVEATS","Steep learning curve for synthesis programming. Real-time performance depends on buffer settings. Orchestra errors can be cryptic. Requires audio system configuration."],["HISTORY","**Csound** was developed by **Barry Vercoe** at **MIT** in **1986**, based on earlier Music N languages dating to **1957**. It descends from Max Mathews' MUSIC series at Bell Labs. Csound remains actively developed, used in academic research, electronic music, and multimedia installations."],["INSTALL","```apt: sudo apt install csound```\n\n```pacman: sudo pacman -S csound```\n\n```zypper: sudo zypper install csound```\n\n```brew: brew install csound```\n\n```nix: nix profile install nixpkgs#csound```"],["SEE ALSO","[sox](/man/sox)(1), [supercollider](/man/supercollider)(1), [ffmpeg](/man/ffmpeg)(1)"]],"e":["sox","supercollider","ffmpeg"],"x":"csound [orchestra.orc] [score.sco]"},"dcgmi":{"n":"dcgmi","t":"NVIDIA data center GPU management interface","s":[["TAGLINE","NVIDIA data center GPU management interface"],["TLDR","**Show GPU discovery** information\n\n```dcgmi discovery -l```\n\n**Display GPU health** status\n\n```dcgmi health -g [group_id]```\n\n**Run diagnostics** on GPUs\n\n```dcgmi diag -r [1]```\n\n**Show real-time stats**\n\n```dcgmi dmon```\n\n**Create a GPU group**\n\n```dcgmi group -c [group_name]```\n\n**Add GPU to group**\n\n```dcgmi group -g [group_id] -a [gpu_id]```\n\n**Show GPU topology**\n\n```dcgmi topo -g [group_id]```"],["SYNOPSIS","**dcgmi** _subsystem_ [_options_]"],["PARAMETERS","_SUBSYSTEM_\n> Management subsystem: discovery, health, diag, dmon, group, topo, etc.\n\n**discovery** **-l**\n> List discovered GPUs.\n\n**health** **-g** _GROUP_\n> Check health of GPU group.\n\n**diag** **-r** _LEVEL_\n> Run diagnostics (level 1-4).\n\n**dmon**\n> Real-time monitoring dashboard.\n\n**group** **-c** _NAME_\n> Create named GPU group.\n\n**topo** **-g** _GROUP_\n> Show interconnect topology.\n\n**fieldgroup** **-c** _NAME_\n> Create a named field group for metric collection.\n\n**modules** **-l**\n> List available DCGM modules and their status.\n\n**policy** **-g** _GROUP_\n> View or set GPU policy conditions.\n\n**stats** **-j** _JOB_ID_\n> Display job-level GPU statistics.\n\n**--host** _HOST_:_PORT_\n> Connect to a remote DCGM host daemon (default: localhost:5555).\n\n**--help**\n> Display help information."],["DESCRIPTION","**dcgmi** is the command-line interface for NVIDIA's Data Center GPU Manager (DCGM). It provides monitoring, management, and diagnostic capabilities for NVIDIA GPUs in data center and HPC environments.\n\nThe tool enables administrators to monitor GPU health, run diagnostics, track performance metrics, and manage GPU groups for policy enforcement. It integrates with job schedulers and cluster management systems for automated GPU management.\n\nDCGM tracks hundreds of GPU metrics including temperature, power, memory usage, and error counts. The diagnostic subsystem can detect hardware issues before they cause failures, supporting proactive maintenance."],["CAVEATS","Requires NVIDIA DCGM service running on the host. Only works with supported NVIDIA data center GPUs. Some diagnostics require GPUs to be idle. Elevated privileges needed for certain operations."],["HISTORY","DCGM was developed by **NVIDIA** and released around **2016** for enterprise GPU deployments. dcgmi provides CLI access to DCGM functionality, complementing the API and GUI interfaces for data center GPU fleet management."],["SEE ALSO","[nvidia-smi](/man/nvidia-smi)(1), [nvtop](/man/nvtop)(1), [gpustat](/man/gpustat)(1)"]],"e":["nvidia-smi","nvtop","gpustat"],"x":"dcgmi discovery -l"},"dia":{"n":"dia","t":"diagram and flowchart creation program","s":[["TAGLINE","diagram and flowchart creation program"],["TLDR","**Launch Dia** diagram editor\n\n```dia```\n\n**Open a diagram file**\n\n```dia [diagram.dia]```\n\n**Export diagram** to image\n\n```dia -e [output.png] [diagram.dia]```\n\n**Export to specific** format\n\n```dia -t [svg] -e [output.svg] [diagram.dia]```\n\n**Export without GUI**\n\n```dia --export=[output.png] --filter=[png] [diagram.dia]```\n\n**List available export** formats\n\n```dia --list-filters```"],["SYNOPSIS","**dia** [_options_] [_file_...]"],["PARAMETERS","_FILE_\n> Diagram file(s) to open.\n\n**-e**, **--export** _FILE_\n> Export to specified file.\n\n**-t**, **--filter** _FORMAT_\n> Export format: png, svg, eps, pdf, etc.\n\n**--size** _WxH_\n> Size for bitmap export.\n\n**--list-filters**\n> List available export filters.\n\n**--nosplash**\n> Start without splash screen.\n\n**--help**\n> Display help information."],["DESCRIPTION","**Dia** is a diagram creation program similar to Microsoft Visio. It supports various diagram types including flowcharts, network diagrams, UML diagrams, circuit diagrams, and more through customizable shape libraries.\n\nThe application features a GTK-based interface with tools for drawing, connecting, and formatting diagram elements. Diagrams are saved in XML format (optionally compressed) and can be exported to numerous image and vector formats.\n\nDia's extensibility through custom shapes and Python scripting makes it adaptable to specialized diagramming needs. It's widely used for technical documentation and software design."],["CAVEATS","Native file format differs from other diagramming tools. Some export formats may lose features. Complex diagrams can be slow to render. Font rendering varies across platforms."],["HISTORY","Dia was created by **Alexander Larsson** and first released in **1998**. It was developed as a free software alternative to commercial diagramming tools, becoming part of the GNOME project's application suite."],["INSTALL","```apt: sudo apt install dia```\n\n```dnf: sudo dnf install dia```\n\n```zypper: sudo zypper install dia```\n\n```nix: nix profile install nixpkgs#dia```"],["SEE ALSO","[inkscape](/man/inkscape)(1), [graphviz](/man/graphviz)(1), [drawio](/man/drawio)(1)"],["RESOURCES","```[Source code](https://gitlab.gnome.org/GNOME/dia)```\n\n```[Homepage](https://wiki.gnome.org/Apps/Dia)```"]],"e":["inkscape","graphviz","drawio"],"x":"dia"},"dnsdomainname":{"n":"dnsdomainname","t":"display DNS domain name","s":[["TAGLINE","display DNS domain name"],["TLDR","Print the **DNS domain** portion of the FQDN\n\n```dnsdomainname```"],["SYNOPSIS","**dnsdomainname**"],["DESCRIPTION","**dnsdomainname** displays the DNS domain name portion of the system's fully qualified domain name (FQDN). It uses gethostname() to retrieve the hostname, then getaddrinfo() to resolve it to the canonical name via DNS lookup.\n\nThe domain name is extracted as everything after the first dot in the FQDN. For example, if the FQDN is \"server.example.com\", dnsdomainname returns \"example.com\". This differs from tools like domainname which display the NIS/YP domain. On most distributions dnsdomainname is a link to the same **hostname** multi-call binary (equivalent to `hostname --domain`). The command requires proper DNS configuration and name resolution to work correctly. If the system hostname doesn't include a domain component or DNS resolution fails, it returns an empty result."],["CAVEATS","Returns empty if the system hostname doesn't include a domain name or DNS resolution fails. Requires proper DNS configuration."],["INSTALL","```apt: sudo apt install hostname```\n\n```dnf: sudo dnf install hostname```\n\n```pacman: sudo pacman -S net-tools```\n\n```apk: sudo apk add net-tools```\n\n```zypper: sudo zypper install hostname```\n\n```brew: brew install net-tools```\n\n```nix: nix profile install nixpkgs#hostname```"],["SEE ALSO","[hostname](/man/hostname)(1), [domainname](/man/domainname)(1), [hostnamectl](/man/hostnamectl)(1)"]],"e":["hostname","domainname","hostnamectl"],"x":"dnsdomainname"},"dpkg-deb":{"n":"dpkg-deb","t":"Debian archive manipulation tool","s":[["TAGLINE","Debian archive manipulation tool"],["TLDR","Display package **information**\n\n```dpkg-deb -I [path/to/file.deb]```\n\nDisplay package **name and version**\n\n```dpkg-deb -W [path/to/file.deb]```\n\n**List** package contents\n\n```dpkg-deb -c [path/to/file.deb]```\n\n**Extract** contents to directory\n\n```dpkg-deb -x [path/to/file.deb] [path/to/directory]```\n\n**Extract** contents and control information\n\n```dpkg-deb --raw-extract [path/to/file.deb] [path/to/directory]```\n\n**Build** package from directory\n\n```dpkg-deb -b [path/to/directory]```\n\n**Show a specific control file** field\n\n```dpkg-deb -f [path/to/file.deb] [field_name]```"],["SYNOPSIS","**dpkg-deb** _action_ [_options_] _archive_|_directory_"],["DESCRIPTION","**dpkg-deb** packs, unpacks, and provides information about Debian archives (.deb files). It's the low-level tool for working directly with the .deb file format without involving the package database.\n\nThe tool allows you to inspect package contents and metadata before installation, extract files from packages without installing them, or build new .deb archives from properly structured directory trees. Unlike dpkg itself, dpkg-deb operates purely on archive files and doesn't modify the system's package database.\n\nCommon use cases include examining unfamiliar packages for security auditing, extracting specific files from packages, and building custom .deb packages as part of a development workflow."],["PARAMETERS","**-I, --info** _archive_\n> Show package information\n\n**-W, --show** _archive_\n> Show package name and version\n\n**-c, --contents** _archive_\n> List archive contents\n\n**-x, --extract** _archive_ _dir_\n> Extract files to directory\n\n**-X, --vextract** _archive_ _dir_\n> Extract and list files\n\n**-e, --control** _archive_ [_dir_]\n> Extract control information files\n\n**-R, --raw-extract** _archive_ _dir_\n> Extract everything (data and control info)\n\n**-f, --field** _archive_ [_field_]\n> Show control file field values\n\n**-b, --build** _dir_ [_archive_]\n> Build package from directory"],["CAVEATS","Lower-level than dpkg; use dpkg for installation. Building packages requires proper debian/ directory structure. Part of dpkg package."],["INSTALL","```apt: sudo apt install dpkg```\n\n```dnf: sudo dnf install dpkg```\n\n```pacman: sudo pacman -S dpkg```\n\n```apk: sudo apk add dpkg```\n\n```zypper: sudo zypper install dpkg```\n\n```brew: brew install dpkg```\n\n```nix: nix profile install nixpkgs#dpkg```"],["SEE ALSO","[dpkg](/man/dpkg)(1), [dpkg-query](/man/dpkg-query)(1), [apt](/man/apt)(1), [ar](/man/ar)(1)"]],"e":["dpkg","dpkg-query","apt","ar"],"x":"dpkg-deb -I [path/to/file.deb]"},"e2fsck":{"n":"e2fsck","t":"check and repair ext filesystems","s":[["TAGLINE","check and repair ext filesystems"],["TLDR","**Check** filesystem for errors\n\n```sudo e2fsck /dev/sdXN```\n\nCheck and **auto-repair** (preen)\n\n```sudo e2fsck -p /dev/sdXN```\n\nCheck for **bad blocks** using read-only test\n\n```sudo e2fsck -c /dev/sdXN```\n\n**Force** check even if clean\n\n```sudo e2fsck -f /dev/sdXN```\n\nExhaustive **bad block** test\n\n```sudo e2fsck -fccky /dev/sdXN```\n\n**Read-only** check without making changes\n\n```sudo e2fsck -n /dev/sdXN```\n\n**Verbose** output\n\n```sudo e2fsck -v /dev/sdXN```\n\nInteractively repair, answering **yes to all** questions\n\n```sudo e2fsck -y /dev/sdXN```"],["SYNOPSIS","**e2fsck** [_-panyrcdfvtDFV_] [_-b superblock_] [_-B blocksize_] _device_"],["DESCRIPTION","**e2fsck** checks and optionally repairs Linux ext2, ext3, or ext4 filesystems. The filesystem should be unmounted before running this command, or mounted read-only at minimum."],["PARAMETERS","**-a**\n> Automatically repair without prompting (same as -p)\n\n**-b SUPERBLOCK**\n> Use alternative superblock\n\n**-B BLOCKSIZE**\n> Specify block size when searching for superblock\n\n**-c**\n> Check for bad blocks using read-only test\n\n**-C FD**\n> Write completion information to file descriptor\n\n**-d**\n> Print debugging output\n\n**-D**\n> Optimize directories\n\n**-f**\n> Force check even if filesystem seems clean\n\n**-F**\n> Flush buffer cache before checking\n\n**-k**\n> Keep existing bad block list\n\n**-n**\n> Open read-only, answer \"no\" to all questions\n\n**-p**\n> Preen mode: automatically repair safe problems\n\n**-t**\n> Print timing statistics\n\n**-v**\n> Verbose mode\n\n**-y**\n> Answer \"yes\" to all questions"],["CONFIGURATION","**/etc/e2fsck.conf**\n> Configuration file controlling default behavior, problem handling, and optimization settings."],["CAVEATS","Never run e2fsck on a mounted filesystem unless it's read-only. The **-y** option can cause data loss if used carelessly. Always backup before repairing."],["HISTORY","**e2fsck** is part of **e2fsprogs**, providing filesystem checking for ext2/3/4 filesystems since the early 1990s."],["INSTALL","```apt: sudo apt install e2fsprogs```\n\n```dnf: sudo dnf install e2fsprogs```\n\n```pacman: sudo pacman -S e2fsprogs```\n\n```apk: sudo apk add e2fsprogs```\n\n```zypper: sudo zypper install e2fsprogs```\n\n```brew: brew install e2fsprogs```\n\n```nix: nix profile install nixpkgs#e2fsprogs```"],["SEE ALSO","[fsck](/man/fsck)(8), [tune2fs](/man/tune2fs)(8), [dumpe2fs](/man/dumpe2fs)(8), [mke2fs](/man/mke2fs)(8), [badblocks](/man/badblocks)(8), [debugfs](/man/debugfs)(8)"]],"e":["fsck","tune2fs","dumpe2fs","mke2fs","badblocks","debugfs"],"x":"sudo e2fsck /dev/sdXN"},"ecl":{"n":"ecl","t":"Embeddable Common Lisp interpreter","s":[["TAGLINE","Embeddable Common Lisp interpreter"],["TLDR","**Start ECL REPL**\n\n```ecl```\n\n**Load and execute a file**\n\n```ecl -load [file.lisp]```\n\n**Evaluate expression**\n\n```ecl -eval \"(print \\\"Hello\\\")\"```\n\n**Compile file to shared library**\n\n```ecl -compile [file.lisp]```\n\n**Run script and exit**\n\n```ecl -shell [script.lisp]```\n\n**Start without initialization**\n\n```ecl -norc```"],["SYNOPSIS","**ecl** [_options_] [_args_]"],["PARAMETERS","**-load** _file_\n> Load Lisp file.\n\n**-eval** _expr_\n> Evaluate expression.\n\n**-compile** _file_\n> Translate file to C and compile to a shared library (.fas).\n\n**-o** _ofile_\n> Name the compiled shared library output file.\n\n**-c** _cfile_\n> Name the intermediary C file and keep it after compilation.\n\n**-h** _hfile_\n> Name the intermediary C header file and keep it after compilation.\n\n**-data** [_datafile_]\n> Dump compiler data into datafile.\n\n**-s**\n> Produce a linkable object file instead of a shared library.\n\n**-shell** _script_\n> Run as script, then exit.\n\n**-norc**\n> Don't load init file (~/.eclrc).\n\n**-dir** _directory_\n> Use directory as system directory.\n\n**-q**\n> Reduce compiler output verbosity."],["CONFIGURATION","**~/.eclrc**\n> Initialization file loaded on startup containing Lisp expressions for customization."],["DESCRIPTION","**ECL** (Embeddable Common Lisp) is an implementation of Common Lisp designed to be embedded in other applications. It compiles Lisp to C, enabling integration with C libraries and native code generation.\n\nECL supports the full ANSI Common Lisp standard with extensions for threading, FFI (Foreign Function Interface), and embedding. It can produce standalone executables and shared libraries."],["REPL COMMANDS","```\n(quit)           ; Exit ECL\n(load \"file\")    ; Load Lisp file\n(compile-file \"f\"); Compile to object\n(require :asdf)  ; Load ASDF\n```"],["CAVEATS","Compiling to C adds complexity. Some libraries may need adaptation for ECL. Debugging compiled code is harder than interpreted. Threading behavior may differ from other implementations."],["HISTORY","ECL was originally developed at **IRCAM** (Paris) in the **1980s** as **KCL** (Kyoto Common Lisp). It was forked and developed as **ECL** by **Giuseppe Attardi** and later maintained by the community. The focus on embeddability distinguishes it from other Common Lisp implementations."],["INSTALL","```apt: sudo apt install ecl```\n\n```pacman: sudo pacman -S ecl```\n\n```apk: sudo apk add ecl```\n\n```zypper: sudo zypper install ecl```\n\n```brew: brew install ecl```\n\n```nix: nix profile install nixpkgs#ecl```"],["SEE ALSO","[sbcl](/man/sbcl)(1), [clisp](/man/clisp)(1)"]],"e":["sbcl","clisp"],"x":"ecl"},"encfs":{"n":"encfs","t":"user-space encrypted filesystem using FUSE","s":[["TAGLINE","user-space encrypted filesystem using FUSE"],["TLDR","**Create/mount encrypted directory**\n\n```encfs [~/.encrypted] [~/private]```\n\n**Create with standard mode**\n\n```encfs -s [~/.encrypted] [~/private]```\n\n**Unmount encrypted directory**\n\n```fusermount -u [~/private]```\n\n**Change password**\n\n```encfsctl passwd [~/.encrypted]```\n\n**Show volume info**\n\n```encfsctl info [~/.encrypted]```\n\n**Idle timeout** unmount\n\n```encfs -i [10] [~/.encrypted] [~/private]```"],["SYNOPSIS","**encfs** [_options_] _encdir_ _mountpoint_"],["DESCRIPTION","**encfs** provides encrypted filesystem in user-space using FUSE. It encrypts files individually, storing them in a directory with encrypted names and contents.\n\nThe tool is useful for encrypting cloud-synced folders or sensitive directories. Each file is encrypted separately, allowing efficient sync of changed files."],["PARAMETERS","**-s**\n> Single-threaded mode.\n\n**-f**\n> Run in foreground.\n\n**-v**\n> Verbose mode.\n\n**-i** _minutes_\n> Idle timeout before unmount.\n\n**-o** _options_\n> FUSE mount options.\n\n**--standard**\n> Use standard (safer) settings.\n\n**--paranoid**\n> Use paranoid settings.\n\n**--reverse**\n> Reverse mode for backups.\n\n**--extpass** _cmd_\n> External password program."],["CONFIGURATION","**.encfs6.xml**\n> Configuration file stored in the encrypted directory containing encryption parameters and settings."],["CAVEATS","Leaks file sizes and directory structure. Not resistant to watermarking attacks. Consider newer alternatives like gocryptfs. FUSE adds overhead. Passphrase recovery not possible."],["HISTORY","**encfs** was created by **Valient Gough** in **2003**. It pioneered user-space encrypted filesystems on Linux. While popular for years, security audits revealed weaknesses, leading to recommendations for alternatives like gocryptfs for new deployments."],["INSTALL","```apt: sudo apt install encfs```\n\n```pacman: sudo pacman -S encfs```\n\n```apk: sudo apk add encfs```\n\n```zypper: sudo zypper install encfs```\n\n```brew: brew install encfs```"],["SEE ALSO","[gocryptfs](/man/gocryptfs)(1), [cryptsetup](/man/cryptsetup)(1), [fusermount](/man/fusermount)(1), [ecryptfs](/man/ecryptfs)(1)"],["RESOURCES","```[Source code](https://github.com/vgough/encfs)```"]],"e":["gocryptfs","cryptsetup","fusermount","ecryptfs"],"x":"encfs [~/.encrypted] [~/private]"},"envdiff":{"n":"envdiff","t":"Environment snapshot and diff tool","s":[["TAGLINE","Environment snapshot and diff tool"],["TLDR","**Capture** current environment state\n\n```envdiff capture```\n\n**Compare** current environment to snapshot\n\n```envdiff diff [snapshot.json]```\n\n**List** all captured snapshots\n\n```envdiff list```\n\n**Compare** two snapshots\n\n```envdiff diff [snapshot1.json] [snapshot2.json]```"],["SYNOPSIS","**envdiff** [_command_] [_options_] [_snapshots_]"],["PARAMETERS","**capture** [_NAME_]\n> Capture current environment state\n\n**diff** [_SNAPSHOT1_] [_SNAPSHOT2_]\n> Compare environments (defaults to current if only one specified)\n\n**list**\n> List all saved snapshots\n\n**delete** _NAME_\n> Delete a snapshot\n\n**--format** _FORMAT_\n> Output format: text, json, yaml (default: text)\n\n**--include** _VARS_\n> Comma-separated list of variables to include\n\n**--exclude** _VARS_\n> Comma-separated list of variables to exclude\n\n**--sensitive**\n> Include sensitive variables (masked by default)\n\n**-v, --version**\n> Display version and exit\n\n**-h, --help**\n> Display help and exit"],["DESCRIPTION","**envdiff** is a tool for capturing and comparing environment variable states. It allows users to snapshot their environment, compare different states, and track changes over time.\n\nThe tool is useful for debugging environment-related issues, verifying configuration consistency across different systems, and documenting environment setups."],["CAVEATS","Sensitive data is masked by default but may still be captured. Large environment variable sets can create large snapshot files. System-specific variables may differ between platforms."],["HISTORY","**envdiff** was created to help developers troubleshoot environment-related issues by providing a way to track and compare environment states over time."],["INSTALL","```aur: yay -S envdiff```"],["SEE ALSO","[env](/man/env)(1), [printenv](/man/printenv)(1), [export](/man/export)(1)"]],"e":["env","printenv","export"],"x":"envdiff capture"},"exim":{"n":"exim","t":"flexible Mail Transfer Agent","s":[["TAGLINE","flexible Mail Transfer Agent"],["TLDR","**Start mail queue processing**\n\n```exim -q```\n\n**Send test message**\n\n```echo \"Test\" | exim [user@example.com]```\n\n**Show mail queue**\n\n```exim -bp```\n\n**Display queue count**\n\n```exim -bpc```\n\n**Force delivery of message**\n\n```exim -M [message_id]```\n\n**Remove message from queue**\n\n```exim -Mrm [message_id]```\n\n**Test address routing**\n\n```exim -bt [user@example.com]```\n\n**Check configuration**\n\n```exim -bV```"],["SYNOPSIS","**exim** [_options_] [_addresses_...]"],["PARAMETERS","**-q** [_time_]\n> Run queue, optionally repeatedly.\n\n**-bp**\n> List messages in queue.\n\n**-bpc**\n> Count messages in queue.\n\n**-bt** _address_\n> Test address routing.\n\n**-bV**\n> Display version and config.\n\n**-M** _id_\n> Force delivery of message.\n\n**-Mrm** _id_\n> Remove message from queue.\n\n**-Mvh** _id_\n> View message headers.\n\n**-Mvb** _id_\n> View message body.\n\n**-d**\n> Enable debug mode.\n\n**-C** _file_\n> Use alternate configuration."],["DESCRIPTION","**exim** is a Mail Transfer Agent (MTA) that handles sending, receiving, and routing email on Unix systems. It's known for flexibility in configuration and filtering capabilities.\n\nThe tool supports ACLs, content scanning, TLS encryption, and various authentication mechanisms. It can function as both an incoming and outgoing mail server."],["CONFIGURATION","**/etc/exim4/exim4.conf**\n> Main configuration file controlling routing, ACLs, authentication, and transport settings.\n\n**/etc/exim4/passwd**\n> Authentication credentials for SMTP clients."],["CAVEATS","Complex configuration syntax. Requires careful security setup. Port 25 often blocked by ISPs. Log monitoring essential for production."],["HISTORY","**Exim** was originally written by **Philip Hazel** at the **University of Cambridge** starting in **1995**. The name stands for \"EXperimental Internet Mailer.\" It became the default MTA for Debian and is widely used on Unix servers."],["INSTALL","```apt: sudo apt install exim4-daemon-heavy```\n\n```dnf: sudo dnf install exim```\n\n```pacman: sudo pacman -S exim```\n\n```apk: sudo apk add exim```\n\n```zypper: sudo zypper install exim```\n\n```brew: brew install exim```\n\n```nix: nix profile install nixpkgs#exim```"],["SEE ALSO","[sendmail](/man/sendmail)(1), [postfix](/man/postfix)(1), [mailq](/man/mailq)(1), [mutt](/man/mutt)(1)"],["RESOURCES","```[Source code](https://github.com/Exim/exim)```\n\n```[Homepage](https://www.exim.org/)```"]],"e":["sendmail","postfix","mailq","mutt"],"x":"exim -q"},"fcitx":{"n":"fcitx","t":"input method framework","s":[["TAGLINE","input method framework"],["TLDR","**Start fcitx**\n\n```fcitx```\n\n**Start in daemon** mode\n\n```fcitx -d```\n\n**Restart fcitx**\n\n```fcitx -r```\n\n**Replace existing instance**\n\n```fcitx --replace```\n\n**Show version**\n\n```fcitx -v```"],["SYNOPSIS","**fcitx** [_options_]"],["PARAMETERS","**-d**, **--daemon**\n> Run as daemon (background).\n\n**-r**, **--replace**\n> Replace existing instance.\n\n**-D**, **--disable**\n> Disable addon loading.\n\n**-s**, **--enable**\n> Enable specific addon.\n\n**-v**, **--version**\n> Display version information.\n\n**--help**\n> Display help information."],["DESCRIPTION","**fcitx** (Flexible Context-aware Input Tool with eXtension) is an input method framework for entering non-Latin characters. It supports Chinese, Japanese, Korean, and many other languages through various input method engines.\n\nThe framework provides a modular architecture with addons for different input methods like Pinyin, Wubi, Hangul, and Anthy. It integrates with desktop environments through GTK and Qt input modules.\n\nfcitx handles input method switching, candidate selection, and provides a configurable user interface for text entry."],["CONFIGURATION","**~/.config/fcitx/**\n> User configuration files for input methods, hotkeys, and addons.\n\n**/usr/share/fcitx/**\n> System-wide input method data and addon files."],["CAVEATS","Requires environment variables set correctly. May conflict with other input frameworks. Desktop integration depends on toolkit support."],["HISTORY","fcitx was created by **Yuking** in 2002 as a Chinese input method for Linux. It evolved into a general-purpose input method framework, with **fcitx5** being the modern rewrite with improved architecture."],["INSTALL","```apt: sudo apt install fcitx-bin```\n\n```dnf: sudo dnf install fcitx```"],["SEE ALSO","[fcitx5](/man/fcitx5)(1), [ibus](/man/ibus)(1)"]],"e":["fcitx5","ibus"],"x":"fcitx"},"ftp":{"n":"ftp","t":"File Transfer Protocol client","s":[["TAGLINE","File Transfer Protocol client"],["TLDR","**Connect to server**\n\n```ftp [ftp.example.com]```\n\n**Connect with username**\n\n```ftp [username]@[ftp.example.com]```\n\n**Non-interactive mode**\n\n```ftp -n [ftp.example.com]```\n\n**Download file**\n\n```ftp> get [remote_file] [local_file]```\n\n**Upload file**\n\n```ftp> put [local_file] [remote_file]```"],["SYNOPSIS","**ftp** [_options_] [_host_]"],["PARAMETERS","_HOST_\n> FTP server hostname or IP.\n\n**-n**\n> No auto-login.\n\n**-i**\n> No interactive prompts for multiple transfers.\n\n**-v**\n> Verbose mode.\n\n**-p**\n> Passive mode.\n\n**get** _FILE_\n> Download file.\n\n**put** _FILE_\n> Upload file.\n\n**mget** _FILES_\n> Download multiple files.\n\n**--help**\n> Display help information."],["CONFIGURATION","**~/.netrc**\n> Stores FTP credentials for auto-login (format: machine host login user password pass)."],["DESCRIPTION","**ftp** is the standard File Transfer Protocol client for transferring files between local and remote systems. It provides an interactive command interface for file operations.\n\nThe client supports navigation, file listing, binary/ASCII transfer modes, and batch transfers. Passive mode helps with firewalls. The protocol transmits credentials in cleartext.\n\nftp remains useful for accessing legacy systems and public FTP archives."],["CAVEATS","**Insecure** - passwords sent in cleartext. Use SFTP or FTPS for sensitive data. Firewall issues with active mode."],["HISTORY","ftp is one of the oldest Internet protocols, standardized in **RFC 959** (1985). While largely replaced by secure alternatives, it remains widely deployed for anonymous file archives."],["INSTALL","```dnf: sudo dnf install ftp```\n\n```apk: sudo apk add inetutils-ftp```\n\n```zypper: sudo zypper install ftp```"],["SEE ALSO","[sftp](/man/sftp)(1), [lftp](/man/lftp)(1), [curl](/man/curl)(1)"],["RESOURCES","```[Documentation](https://manpages.debian.org/ftp)```"]],"e":["sftp","lftp","curl"],"x":"ftp [ftp.example.com]"},"gallery-dl":{"n":"gallery-dl","t":"image gallery downloader for multiple sites","s":[["TAGLINE","image gallery downloader for multiple sites"],["TLDR","**Download from URL**\n\n```gallery-dl [https://example.com/gallery]```\n\n**Set output directory**\n\n```gallery-dl -d [/path/to/downloads] [url]```\n\n**List supported sites**\n\n```gallery-dl --list-extractors```\n\n**Download with metadata**\n\n```gallery-dl --write-metadata [url]```\n\n**Authentication**\n\n```gallery-dl -u [username] -p [password] [url]```"],["SYNOPSIS","**gallery-dl** [_options_] _urls_..."],["PARAMETERS","_URLS_\n> Gallery or image URLs.\n\n**-d** _PATH_, **--dest** _PATH_\n> Destination directory.\n\n**-u** _USER_, **--username** _USER_\n> Site username.\n\n**-p** _PASS_, **--password** _PASS_\n> Site password.\n\n**--write-metadata**\n> Save metadata as JSON.\n\n**--list-extractors**\n> List supported sites.\n\n**-o** _KEY=VALUE_\n> Set config option.\n\n**--help**\n> Display help information."],["CONFIGURATION","**~/.config/gallery-dl/config.json**\n> Main configuration file for output paths, filename patterns, authentication, and site-specific options."],["DESCRIPTION","**gallery-dl** downloads image galleries from websites. It supports hundreds of image hosting sites, social media, and art communities through site-specific extractors.\n\nThe tool handles pagination, authentication, rate limiting, and metadata extraction automatically. Downloads can be organized with customizable directory and filename patterns.\n\ngallery-dl is commonly used for archiving content from image boards, artist portfolios, and social media."],["CAVEATS","Site support may break with updates. Respect site terms of service. Large downloads may be throttled."],["HISTORY","gallery-dl was created as a counterpart to youtube-dl for images. It provides comprehensive image downloading with support for hundreds of sites through Python-based extractors."],["INSTALL","```apk: sudo apk add gallery-dl```\n\n```brew: brew install gallery-dl```\n\n```nix: nix profile install nixpkgs#gallery-dl```"],["SEE ALSO","[youtube-dl](/man/youtube-dl)(1), [wget](/man/wget)(1), [curl](/man/curl)(1)"],["RESOURCES","```[Source code](https://github.com/mikf/gallery-dl)```"]],"e":["youtube-dl","wget","curl"],"x":"gallery-dl [https://example.com/gallery]"},"gcloud-feedback":{"n":"gcloud-feedback","t":"open a feedback form for the Google Cloud CLI","s":[["TAGLINE","open a feedback form for the Google Cloud CLI"],["TLDR","**Open the feedback form** in the browser\n\n```gcloud feedback```\n\n**Attach a log file** to the feedback\n\n```gcloud feedback --log-file [path/to/gcloud.log]```"],["SYNOPSIS","**gcloud feedback** [**--log-file=**_LOG_FILE_] [_GCLOUD_WIDE_FLAGS_...]"],["PARAMETERS","**--log-file** _FILE_\n> Path to a `gcloud` log file to attach (typically from `~/.config/gcloud/logs/`). When provided, gcloud reads the log and pastes a link to it into the new-issue URL along with the trace info so the CLI team can reproduce the problem.\n\n**gcloud wide flags**\n> All global flags apply: `--help`, `--quiet`, `--verbosity`, `--configuration`, `--user-output-enabled`, `--log-http`, etc."],["DESCRIPTION","**gcloud feedback** opens the official Google Cloud CLI issue tracker in your default browser, pre-filled with system information (gcloud version, platform, Python version) so reports don't start empty. It is the recommended way to report bugs or request features for the CLI itself; for feedback on specific Google Cloud products, use their in-console \"Send feedback\" widgets.\n\nWith `--log-file`, gcloud opens the log, strips sensitive headers, and makes it easy to share a recent failure trace with the filed report."],["CAVEATS","Requires a browser — on headless machines the URL is printed to stdout instead. Logs may contain resource names and project IDs; review before pasting into a public issue. For security vulnerabilities, use the private security contact, not the public issue tracker."],["SEE ALSO","[gcloud](/man/gcloud)(1), [gcloud-help](/man/gcloud-help)(1), [gcloud-version](/man/gcloud-version)(1)"]],"e":["gcloud","gcloud-help","gcloud-version"],"x":"gcloud feedback"},"gh-config":{"n":"gh-config","t":"manage GitHub CLI settings and preferences","s":[["TAGLINE","manage GitHub CLI settings and preferences"],["TLDR","**Get config value**\n\n```gh config get [key]```\n\n**Set config value**\n\n```gh config set [key] [value]```\n\n**List all configuration**\n\n```gh config list```\n\n**Set default editor**\n\n```gh config set editor [vim]```\n\n**Set default browser**\n\n```gh config set browser [firefox]```"],["SYNOPSIS","**gh config** _command_ [_options_]"],["PARAMETERS","**get** _KEY_\n> Get a configuration value.\n\n**set** _KEY_ _VALUE_\n> Set a configuration value.\n\n**list**\n> List all configuration settings.\n\n**--host** _HOSTNAME_\n> Apply to specific GitHub host.\n\n**--help**\n> Display help information."],["DESCRIPTION","**gh config** manages configuration for the GitHub CLI. It controls behavior settings like preferred editor, browser, default prompts, and protocol preferences.\n\nConfiguration is stored per-host, allowing different settings for github.com and GitHub Enterprise instances. Settings can be environment-specific or global.\n\nCommon settings include git_protocol (https/ssh), editor, browser, and pager."],["CONFIGURATION","**~/.config/gh/config.yml**\n> Main configuration file storing user preferences, default settings, and per-host configurations."],["CAVEATS","Config stored in ~/.config/gh/config.yml. Environment variables may override settings. Some settings require reauthentication to take effect."],["HISTORY","gh config is part of the **GitHub CLI**, providing user customization following XDG Base Directory conventions on Unix systems."],["INSTALL","```apt: sudo apt install gh```\n\n```dnf: sudo dnf install gh```\n\n```pacman: sudo pacman -S github-cli```\n\n```apk: sudo apk add github-cli```\n\n```zypper: sudo zypper install gh```\n\n```brew: brew install gh```\n\n```nix: nix profile install nixpkgs#gh```"],["SEE ALSO","[gh](/man/gh)(1), [gh-auth](/man/gh-auth)(1)"],["RESOURCES","```[Documentation](https://cli.github.com/manual/gh_config)```\n\n```[Source code](https://github.com/cli/cli)```"]],"e":["gh","gh-auth"],"x":"gh config get [key]"},"github-label-sync":{"n":"github-label-sync","t":"Synchronize GitHub repository labels from a config file","s":[["TAGLINE","Synchronize GitHub repository labels from a config file"],["TLDR","**Sync labels to a repository**\n\n```github-label-sync --labels [labels.json] [owner/repo]```\n\n**Preview changes without applying them**\n\n```github-label-sync --dry-run --labels [labels.json] [owner/repo]```\n\n**Keep labels not listed in the config instead of deleting them**\n\n```github-label-sync --allow-added-labels --labels [labels.json] [owner/repo]```\n\n**Use a specific access token**\n\n```github-label-sync --access-token [token] --labels [labels.json] [owner/repo]```\n\n**Use a labels file hosted at a URL**\n\n```github-label-sync --labels [https://example.com/labels.json] [owner/repo]```"],["SYNOPSIS","**github-label-sync** [_options_] _owner/repo_"],["PARAMETERS","**-l**, **--labels** _path-or-url_\n> Path or URL to the JSON labels configuration file.\n\n**-d**, **--dry-run**\n> Calculate the required changes without applying them.\n\n**-a**, **--allow-added-labels**\n> Don't delete labels found on the repo that aren't listed in the config.\n\n**-A**, **--access-token** _token_\n> GitHub personal access token. Falls back to the `GITHUB_ACCESS_TOKEN` environment variable if omitted."],["DESCRIPTION","**github-label-sync** synchronizes GitHub labels with a JSON configuration file. It ensures consistent labeling across repositories by defining labels declaratively, adding missing labels, updating changed ones, and (by default) removing labels not present in the configuration file.\n\nThe labels file is a JSON array of label objects, each with `name`, `color`, and optionally `description` and `aliases` (previous names to rename from, preserving existing issue/PR associations)."],["SEE ALSO","[gh-label](/man/gh-label)(1)"],["RESOURCES","```[Source code](https://github.com/Financial-Times/github-label-sync)```"]],"e":["gh-label"],"x":"github-label-sync --labels [labels.json] [owner/repo]"},"hub-branch":{"n":"hub-branch","t":"runs git's own branch command unmodified through hub","s":[["TAGLINE","runs git's own branch command unmodified through hub"],["TLDR","**List local and remote branches**\n\n```hub branch -a```\n\n**Create a new branch**\n\n```hub branch [branch-name]```\n\n**Delete a branch**\n\n```hub branch -d [branch-name]```\n\n**Force delete branch**\n\n```hub branch -D [branch-name]```\n\n**Rename current branch**\n\n```hub branch -m [new-name]```"],["SYNOPSIS","**hub** **branch** [_options_] [_branch_]"],["PARAMETERS","**-a**, **--all**\n> List both local and remote branches.\n\n**-r**, **--remotes**\n> List remote branches only.\n\n**-d**\n> Delete branch (only if merged).\n\n**-D**\n> Force delete branch.\n\n**-m**\n> Rename branch.\n\n**-v**, **--verbose**\n> Show more info."],["DESCRIPTION","**hub** is a command-line wrapper for git that adds GitHub-specific behavior to certain git subcommands (**clone**, **fetch**, **remote**, **checkout**, **merge**, and a few others) while transparently passing every other subcommand straight through to the real git binary. **branch** is not one of the commands hub extends, so `hub branch` behaves exactly like `git branch`: it lists, creates, renames, and deletes branches with no added GitHub integration.\n\nIt is included here because, when hub is aliased as `git` (see `hub alias`), any git subcommand - including **branch** - can be invoked through it."],["CAVEATS","Hub is no longer actively maintained and has been superseded by the official GitHub CLI (**gh**). For actual GitHub-aware branch operations (e.g. checking out a pull request as a branch), see **hub checkout** or **gh pr checkout** instead."],["INSTALL","```apt: sudo apt install hub```\n\n```pacman: sudo pacman -S hub```\n\n```zypper: sudo zypper install hub```\n\n```brew: brew install hub```\n\n```nix: nix profile install nixpkgs#hub```"],["SEE ALSO","[hub](/man/hub)(1), [git-branch](/man/git-branch)(1), [gh](/man/gh)(1)"],["RESOURCES","```[Source code](https://github.com/mislav/hub)```"]],"e":["hub","git-branch","gh"],"x":"hub branch -a"},"imagemagick":{"n":"imagemagick","t":"suite of tools for image manipulation","s":[["TAGLINE","suite of tools for image manipulation"],["TLDR","**Convert image format** (v7 syntax)\n\n```magick [input.png] [output.jpg]```\n\n**Resize image**\n\n```magick [input.jpg] -resize [800x600] [output.jpg]```\n\n**Create thumbnail**\n\n```magick [input.jpg] -thumbnail [150x150^] -gravity center -extent [150x150] [thumb.jpg]```\n\n**Add text watermark**\n\n```magick [input.jpg] -gravity south -annotate +0+10 \"[Copyright]\" [output.jpg]```\n\n**Combine images horizontally**\n\n```magick [a.jpg] [b.jpg] +append [combined.jpg]```\n\n**Create GIF from images**\n\n```magick -delay [100] [*.png] [animation.gif]```\n\n**Identify image info**\n\n```magick identify [image.jpg]```\n\n**Batch convert all PNGs to JPEGs** in the current directory\n\n```magick mogrify -format jpg [*.png]```"],["SYNOPSIS","**magick** [_input_] [_operations_] _output_\n\n**magick identify** [_options_] _image_\n\n**magick mogrify** [_options_] _images_"],["PARAMETERS","**-resize** _geometry_\n> Resize image.\n\n**-crop** _geometry_\n> Crop image.\n\n**-rotate** _degrees_\n> Rotate image.\n\n**-quality** _value_\n> Compression quality (JPEG: 1-100, PNG: 0-9 for zlib compression level).\n\n**-gravity** _type_\n> Anchor point for operations.\n\n**-annotate** _geometry_ _text_\n> Add text annotation.\n\n**-blur** _radius_\n> Apply blur.\n\n**-sharpen** _radius_\n> Sharpen image.\n\n**-colorspace** _type_\n> Convert colorspace.\n\n**-density** _value_\n> Set resolution (DPI).\n\n**-strip**\n> Remove all metadata and profiles from image.\n\n**-format** _type_\n> Set the output image format."],["DESCRIPTION","**ImageMagick** is a suite of tools for image manipulation. It can convert, resize, crop, rotate, combine, and apply effects to images in over 200 formats.\n\nKey tools: **magick** (transform, replaces convert in v7), **identify** (info), **mogrify** (in-place edit), **composite** (combine), **montage** (collage). In v7, all subcommands are invoked via `magick` (e.g., `magick identify`, `magick mogrify`)."],["CAVEATS","Memory-intensive for large images. The default security policy (policy.xml) may restrict some operations and file formats. In v7, the `convert` command is deprecated in favor of `magick`; legacy v6 syntax still works but emits warnings."],["HISTORY","ImageMagick was created by **John Cristy** in **1987** at DuPont. It's been continuously developed and remains the most widely used command-line image processing toolkit."],["INSTALL","```pacman: sudo pacman -S imagemagick```\n\n```brew: brew install imagemagick```\n\n```nix: nix profile install nixpkgs#imagemagick```"],["SEE ALSO","[magick](/man/magick)(1), [convert](/man/convert)(1), [identify](/man/identify)(1), [mogrify](/man/mogrify)(1), [composite](/man/composite)(1), [montage](/man/montage)(1), [ffmpeg](/man/ffmpeg)(1)"]],"e":["magick","convert","identify","mogrify","composite","montage","ffmpeg"],"x":"magick [input.png] [output.jpg]"},"in":{"n":"in","t":"Shell keyword separating the loop variable from the list in for and select loops","s":[["TAGLINE","Shell keyword separating the loop variable from the list in for and select loops"],["TLDR","**Iterate over a list of words**\n\n```for fruit in apple banana cherry; do echo \"$fruit\"; done```\n\n**Iterate over a glob**\n\n```for f in *.txt; do echo \"$f\"; done```\n\n**Iterate over command substitution**\n\n```for user in $(cat users.txt); do echo \"$user\"; done```\n\n**Iterate over a brace-expansion range**\n\n```for i in {1..10}; do echo \"$i\"; done```\n\n**Interactive menu with select**\n\n```select opt in start stop quit; do echo \"$opt\"; done```"],["SYNOPSIS","**for** _name_ **in** [_words_ ...]**;** **do** _commands_**;** **done**\n\n**select** _name_ **in** [_words_ ...]**;** **do** _commands_**;** **done**"],["DESCRIPTION","**in** is a reserved word of the POSIX shell grammar, used as a delimiter between the loop variable and the word list in **for** and **select** compound commands and in **case** statements. It is not a standalone program and cannot be invoked directly; shells such as bash, zsh, dash, and ksh parse it as part of the surrounding control structure.\n\nInside a **for** loop, the variable named before **in** is assigned successively to each word produced by the list after **in** (which may be literal words, glob expansions, command substitutions, or parameter expansions). When the word list is omitted entirely, the loop iterates over the positional parameters \"$@\"."],["CAVEATS","**in** is a keyword, not an executable; `which in` will typically return nothing. It has no flags or options of its own. Behavior depends on the surrounding shell construct and on POSIX quoting/expansion rules."],["HISTORY","**in** comes from the Bourne shell and has been part of the POSIX shell specification since its inception. It is inherited by all POSIX-compatible shells including bash, ksh, zsh, and dash, and by the C shell family in similar form."],["INSTALL","```aur: yay -S in```"],["SEE ALSO","[for](/man/for)(1), [while](/man/while)(1), [do](/man/do)(1), [done](/man/done)(1), [read](/man/read)(1)"]],"e":["for","while","do","done","read"],"x":"for fruit in apple banana cherry; do echo \"$fruit\"; done"},"kfp":{"n":"kfp","t":"Kubeflow Pipelines CLI for managing ML workflows on Kubernetes","s":[["TAGLINE","Kubeflow Pipelines CLI for managing ML workflows on Kubernetes"],["TLDR","**Create a pipeline run from a compiled pipeline**\n\n```kfp run create -e [experiment] -r [run-name] -f [pipeline.yaml] --endpoint [http://localhost:8080]```\n\n**List pipelines**\n\n```kfp pipeline list --endpoint [http://localhost:8080]```\n\n**Upload a pipeline**\n\n```kfp pipeline create -p [pipeline-name] [pipeline.yaml]```\n\n**Create an experiment**\n\n```kfp experiment create -n [experiment-name] --endpoint [http://localhost:8080]```\n\n**Compile a pipeline from Python**\n\n```kfp dsl compile --py [pipeline.py] --output [pipeline.yaml]```\n\n**Diagnose KFP installation**\n\n```kfp diagnose_me```"],["SYNOPSIS","**kfp** _command_ [_options_]"],["PARAMETERS","**run** create|list|get|archive|unarchive|delete\n> Manage pipeline runs.\n\n**recurring-run** create|list|get|enable|disable|delete\n> Manage scheduled recurring runs.\n\n**pipeline** create|create-version|list|list-versions|get|delete\n> Manage pipelines.\n\n**experiment** create|list|get|delete|archive|unarchive\n> Manage experiments.\n\n**dsl** compile\n> Compile a Python pipeline definition to YAML.\n\n**component** build\n> Build a containerized component from a Python function.\n\n**diagnose_me**\n> Run environment diagnostics (GCP-focused).\n\n**-e**, **--experiment** _name_\n> Experiment name or ID.\n\n**-r**, **--run-name** _name_\n> Name for the run.\n\n**-f**, **--package-file** _file_\n> Compiled pipeline file (YAML).\n\n**-p**, **--pipeline-name** _name_\n> Pipeline name.\n\n**--endpoint** _url_\n> KFP API endpoint URL."],["DESCRIPTION","**kfp** is the CLI for **Kubeflow Pipelines**, which orchestrates ML workflows as directed acyclic graphs (DAGs) on Kubernetes. Pipelines define reusable components with inputs, outputs, and dependencies.\n\nThe CLI manages the full pipeline lifecycle: compiling Python pipeline definitions to YAML, uploading pipelines, creating experiments, and submitting runs. It connects to a running KFP backend via the `--endpoint` flag.\n\nKubeflow itself is installed using Kustomize manifests via `kubectl apply -k` from the **kubeflow/manifests** repository. The older **kfctl** deployment tool is deprecated and archived."],["CAVEATS","Resource-intensive; requires a Kubernetes cluster with sufficient capacity. The `diagnose_me` command is GCP-focused. The old `kfp run submit` syntax still works as a deprecated alias for `kfp run create`. Similarly, `kfp pipeline upload` is aliased to `kfp pipeline create`."],["HISTORY","Kubeflow was started at **Google** in **2017** as a way to run TensorFlow on Kubernetes. Open-sourced in **2018**, it expanded to support the full ML lifecycle. Version 1.0 released in **2020**. The project is a **CNCF incubating project**. The kfp v2 SDK brought a redesigned Python API and CLI with `dsl compile` and simplified component authoring."],["SEE ALSO","[kubectl](/man/kubectl)(1), [mlflow](/man/mlflow)(1)"]],"e":["kubectl","mlflow"],"x":"kfp run create -e [experiment] -r [run-name] -f [pipeline.yaml] --endpoint [http://localhost:8080]"},"kiterunner-brute":{"n":"kiterunner-brute","t":"performs traditional wordlist-based bruteforce against API endpoints","s":[["TAGLINE","performs traditional wordlist-based bruteforce against API endpoints"],["TLDR","**Bruteforce API endpoints**\n\n```kr brute [https://api.example.com] -w [wordlist.txt]```\n\n**Bruteforce with specific methods**\n\n```kr brute [url] -w [wordlist.txt] -X GET,POST```\n\n**Bruteforce with headers**\n\n```kr brute [url] -w [wordlist.txt] -H \"Authorization: Bearer [token]\"```"],["SYNOPSIS","**kr** **brute** [_options_] _target_"],["PARAMETERS","**-w** _wordlist_\n> Wordlist file for bruteforcing.\n\n**-X** _methods_\n> HTTP methods to use.\n\n**-H** _header_\n> Add custom header.\n\n**-t** _threads_\n> Number of concurrent threads.\n\n**-o** _file_\n> Output file."],["DESCRIPTION","**kr brute** performs traditional wordlist-based bruteforce against API endpoints. Part of Kiterunner, an API endpoint discovery tool by Assetnote. Tests various HTTP methods and paths to discover hidden endpoints."],["INSTALL","```nix: nix profile install nixpkgs#kiterunner```"],["SEE ALSO","[kiterunner-wordlist](/man/kiterunner-wordlist)(1), [gobuster](/man/gobuster)(1)"]],"e":["kiterunner-wordlist","gobuster"],"x":"kr brute [https://api.example.com] -w [wordlist.txt]"},"lci":{"n":"lci","t":"interpreter for LOLCODE, an esoteric programming language inspired by lolcat","s":[["TAGLINE","interpreter for LOLCODE, an esoteric programming language inspired by lolcat"],["TLDR","**Run** a LOLCODE file\n\n```lci [path/to/file.lol]```\n\nDisplay **help**\n\n```lci -h```\n\nDisplay **version**\n\n```lci -v```"],["SYNOPSIS","**lci** [_options_] _file_"],["PARAMETERS","**-h**, **--help**\n> Display help information\n\n**-v**, **--version**\n> Display version information"],["DESCRIPTION","**lci** is an interpreter for LOLCODE, an esoteric programming language inspired by lolcat memes and internet humor. The language uses lolspeak-style syntax for programming constructs.\n\nExample LOLCODE:\n```\nHAI 1.2\n  VISIBLE \"HAI WORLD!\"\nKTHXBYE\n```"],["CAVEATS","LOLCODE is a joke language not intended for serious development. The specification is informal and implementations may vary. Primarily useful for entertainment and educational purposes."],["HISTORY","LOLCODE was created by Adam Lindsay in **2007** at Lancaster University as a humorous programming language. The lci interpreter was developed by Justin Meza to provide a C-based implementation."],["INSTALL","```brew: brew install lci```\n\n```nix: nix profile install nixpkgs#lci```"],["SEE ALSO","[python](/man/python)(1), [perl](/man/perl)(1), [ruby](/man/ruby)(1)"]],"e":["python","perl","ruby"],"x":"lci [path/to/file.lol]"},"leaftoppm":{"n":"leaftoppm","t":"Convert Interleaf image format to PPM","s":[["TAGLINE","Convert Interleaf image format to PPM"],["TLDR","**Convert an Interleaf image to PPM**\n\n```leaftoppm [input.lf] > [output.ppm]```\n\n**Read from stdin and convert to PPM**\n\n```cat [input.lf] | leaftoppm > [output.ppm]```"],["SYNOPSIS","**leaftoppm** [_leaffile_]"],["DESCRIPTION","**leaftoppm** reads an Interleaf image file as input and produces a PPM (Portable Pixmap) image as output. If no input file is specified, input is read from standard input. Output is written to standard output.\n\nInterleaf was a technical publishing software company popular in the 1980s and 1990s, acquired by BroadVision around 2000. This program is part of the Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[ppmtoleaf](/man/ppmtoleaf)(1), [tifftopnm](/man/tifftopnm)(1), [bmptoppm](/man/bmptoppm)(1)"]],"e":["ppmtoleaf","tifftopnm","bmptoppm"],"x":"leaftoppm [input.lf] > [output.ppm]"},"linode-cli":{"n":"linode-cli","t":"manages Linode cloud infrastructure","s":[["TAGLINE","manages Linode cloud infrastructure"],["TLDR","**List Linodes**\n\n```linode-cli linodes list```\n\n**Create Linode**\n\n```linode-cli linodes create --type [g6-nanode-1] --region [us-east] --image [linode/ubuntu22.04]```\n\n**View Linode**\n\n```linode-cli linodes view [linode_id]```\n\n**Reboot Linode**\n\n```linode-cli linodes reboot [linode_id]```\n\n**Delete Linode**\n\n```linode-cli linodes delete [linode_id]```\n\n**Configure CLI**\n\n```linode-cli configure```"],["SYNOPSIS","**linode-cli** _command_ [_subcommand_] [_options_]"],["PARAMETERS","_COMMAND_\n> Resource type (linodes, volumes, domains, etc.).\n\n**linodes**\n> Manage Linode instances.\n\n**volumes**\n> Manage block storage volumes.\n\n**domains**\n> Manage DNS domains.\n\n**configure**\n> Set up authentication.\n\n**--help**\n> Display help information."],["DESCRIPTION","**linode-cli** manages Linode cloud infrastructure. It provides access to all Linode API features.\n\nThe tool controls virtual machines, storage, networking, and DNS from the command line."],["CAVEATS","Requires API token. Operations may incur charges. Token stored in ~/.config/linode-cli."],["HISTORY","linode-cli is the official **Linode** command-line interface for managing their cloud infrastructure services."],["INSTALL","```brew: brew install linode-cli```\n\n```nix: nix profile install nixpkgs#linode-cli```"],["SEE ALSO","[linode-cli-linodes](/man/linode-cli-linodes)(1), [linode-cli-volumes](/man/linode-cli-volumes)(1), [aws](/man/aws)(1)"]],"e":["linode-cli-linodes","linode-cli-volumes","aws"],"x":"linode-cli linodes list"},"llamafile":{"n":"llamafile","t":"Single-file executable for portable LLM inference","s":[["TAGLINE","Single-file executable for portable LLM inference"],["TLDR","**Run a llamafile (launches chat in terminal and server on port 8080)**\n\n```./[model].llamafile```\n\n**Run in server-only mode**\n\n```./[model].llamafile --server```\n\n**Run in CLI mode with a prompt**\n\n```./[model].llamafile --cli -p \"[prompt]\"```\n\n**Run interactive chat mode**\n\n```./[model].llamafile --chat```\n\n**Load external model weights**\n\n```llamafile -m [path/to/model.gguf]```\n\n**Set context size and number of threads**\n\n```./[model].llamafile -c [8192] -t [8] -p \"[prompt]\"```\n\n**Run server on a specific host and port**\n\n```./[model].llamafile --server --host [0.0.0.0] --port [8080]```\n\n**Offload layers to GPU and set temperature**\n\n```./[model].llamafile -ngl [999] --temp [0.7] -p \"[prompt]\"```"],["SYNOPSIS","**llamafile** [_options_]"],["PARAMETERS","**-m** _model_\n> Path to model weights file (if not embedded in the llamafile).\n\n**-p** _prompt_\n> Input prompt text.\n\n**--cli**\n> Run in CLI mode, answering a single prompt.\n\n**--chat**\n> Run interactive chat mode with slash commands.\n\n**--server**\n> Start HTTP server mode with web UI.\n\n**-c** _size_\n> Context window size in tokens.\n\n**-t** _threads_\n> Number of threads to use for computation.\n\n**-n** _count_\n> Maximum number of tokens to generate.\n\n**-ngl** _n_\n> Number of layers to offload to GPU.\n\n**--host** _addr_\n> Server listening address (default: 127.0.0.1).\n\n**--port** _port_\n> Server port (default: 8080).\n\n**--temp** _value_\n> Sampling temperature (higher = more random).\n\n**--top-k** _n_\n> Top-k sampling (default: 40).\n\n**--top-p** _value_\n> Top-p nucleus sampling (default: 0.95).\n\n**--seed** _n_\n> Random seed for reproducible output.\n\n**--grammar** _grammar_\n> Apply BNF grammar to constrain output format.\n\n**--mmproj** _file_\n> Multimodal projection model weights for vision models.\n\n**--image** _file_\n> Image file input for multimodal models.\n\n**--jinja**\n> Enable Jinja template support for chat templates.\n\n**-e**\n> Enable prompt evaluation."],["DESCRIPTION","**llamafile** is a single-file executable that bundles llama.cpp with a model for portable LLM inference. The same file runs on Linux, macOS, Windows, FreeBSD, NetBSD, and OpenBSD without installation, built on Cosmopolitan Libc.\n\nBy default, llamafile launches both a terminal chatbot and an HTTP server with a web UI on port 8080. It can also run in dedicated CLI, chat, or server modes."],["CAVEATS","File sizes can be large (several GB). Requires chmod +x on Unix systems. Apple Silicon may require code signing. Models are memory-mapped for efficiency."],["HISTORY","llamafile was created by **Justine Tunney** at **Mozilla** in **2023**, combining Cosmopolitan Libc's universal binary format with llama.cpp."],["INSTALL","```aur: yay -S llamafile```"],["SEE ALSO","[llama.cpp](/man/llama.cpp)(1), [ollama](/man/ollama)(1)"]],"e":["llama.cpp","ollama"],"x":"./[model].llamafile"},"lsfd":{"n":"lsfd","t":"lists information about files opened by running processes on Linux","s":[["TAGLINE","lists information about files opened by running processes on Linux"],["TLDR","List **all open files**\n\n```lsfd```\n\nList files by **process ID**\n\n```lsfd -Q 'PID == [pid]'```\n\nFind process with **file open**\n\n```lsfd -Q \"NAME == '[path/to/file]'\"```\n\nList files for **specific PID** (efficient)\n\n```lsfd -p [pid]```\n\nList **IPv4 sockets**\n\n```lsfd -i4```\n\nList **IPv6 sockets**\n\n```lsfd -i6```\n\nList with **specific columns**\n\n```lsfd -o COMMAND,PID,TYPE,NAME```"],["SYNOPSIS","**lsfd** [_options_]"],["DESCRIPTION","**lsfd** lists information about file descriptors opened by running processes on Linux. It is a modern, Linux-specific replacement for lsof with better performance and filtering capabilities using a SQL-like query language. Part of util-linux."],["PARAMETERS","**-p, --pid** _PIDS_\n> Collect information only for specified processes (more efficient than -Q).\n\n**-Q, --filter** _EXPR_\n> Filter output with expression (e.g. 'PID == 1234', \"NAME == '/path'\").\n\n**-l, --threads**\n> List in threads level.\n\n**-i4**\n> Show IPv4 sockets only.\n\n**-i6**\n> Show IPv6 sockets only.\n\n**-o, --output** _COLUMNS_\n> Specify output columns (use -H to list available columns).\n\n**-r, --raw**\n> Use raw output format.\n\n**-J, --json**\n> Output in JSON format.\n\n**-n, --noheadings**\n> Suppress column headers.\n\n**-u, --notruncate**\n> Do not truncate text in columns.\n\n**-H, --list-columns**\n> List available output columns.\n\n**--summary**[=_WHEN_]\n> Print summary information (only, append, or never)."],["CAVEATS","Part of util-linux package. Requires Linux 5.12+ for full functionality. Filter syntax differs from lsof. Using **-p** is more efficient than **-Q** for filtering by PID. Column names may change in future releases."],["INSTALL","```apt: sudo apt install util-linux-extra```\n\n```apk: sudo apk add util-linux-misc```"],["SEE ALSO","[lsof](/man/lsof)(8), [fuser](/man/fuser)(1), [ss](/man/ss)(8)"]],"e":["lsof","fuser","ss"],"x":"lsfd"},"lsix":{"n":"lsix","t":"displays images in the terminal","s":[["TAGLINE","displays images in the terminal"],["TLDR","**Display images**\n\n```lsix [*.jpg]```\n\n**Display all images in directory**\n\n```lsix```\n\n**Display specific images**\n\n```lsix [image1.png] [image2.png]```\n\n**Set thumbnail size**\n\n```LSIX_SIZE=[200x200] lsix```\n\n**Display with border**\n\n```lsix -b [*.png]```"],["SYNOPSIS","**lsix** [_options_] [_files_]"],["PARAMETERS","_FILES_\n> Image files to display.\n\n**-b**\n> Show border around images.\n\n**LSIX_SIZE**\n> Environment variable for size."],["DESCRIPTION","**lsix** displays images in the terminal. It requires a terminal with sixel graphics support.\n\nThe tool creates thumbnails in the terminal. It works with terminals like xterm, mlterm, and foot."],["CAVEATS","Requires sixel-capable terminal. ImageMagick needed. Not all terminals supported."],["HISTORY","lsix was created to display images directly in terminals that support **sixel graphics** format."],["INSTALL","```pacman: sudo pacman -S lsix```\n\n```brew: brew install lsix```\n\n```nix: nix profile install nixpkgs#lsix```"],["SEE ALSO","[img2sixel](/man/img2sixel)(1), [viu](/man/viu)(1), [chafa](/man/chafa)(1)"]],"e":["img2sixel","viu","chafa"],"x":"lsix [*.jpg]"},"lvim":{"n":"lvim","t":"pre-configured Neovim distribution with IDE-like features","s":[["TAGLINE","pre-configured Neovim distribution with IDE-like features"],["TLDR","**Start LunarVim**\n\n```lvim```\n\n**Open a file** for editing\n\n```lvim [file.py]```\n\n**Open file at specific line**\n\n```lvim +[10] [file.py]```\n\n**Update LunarVim** from command line\n\n```lvim +LvimUpdate +q```\n\n**Open multiple files** in tabs\n\n```lvim -p [file1.py] [file2.py]```\n\n**Start in diff mode**\n\n```lvim -d [file1.py] [file2.py]```\n\n**Run headless** for scripting\n\n```lvim --headless +'lua print(\"hello\")' +q```"],["SYNOPSIS","**lvim** [_options_] [_file_...]"],["PARAMETERS","**+**[_num_]\n> Position cursor on specified line number.\n\n**+/**_pattern_\n> Position cursor on first line matching pattern.\n\n**-c** _command_\n> Execute command after loading file.\n\n**-o**[_N_]\n> Open N windows split horizontally.\n\n**-O**[_N_]\n> Open N windows split vertically.\n\n**-p**[_N_]\n> Open N tab pages.\n\n**-d**\n> Start in diff mode.\n\n**-R**\n> Readonly mode.\n\n**--headless**\n> Start without UI for scripting.\n\n**--clean**\n> Start with minimal configuration.\n\n**-u** _config_\n> Use alternate config file.\n\n**-h**, **--help**\n> Display help information.\n\n**-v**, **--version**\n> Show version information."],["EDITOR COMMANDS","**:LvimUpdate**\n> Update LunarVim to latest version.\n\n**:LvimSyncCorePlugins**\n> Refresh core plugins.\n\n**:LvimCacheReset**\n> Clear cached data.\n\n**:LvimInfo**\n> Display LunarVim information.\n\n**:LvimDocs**\n> Open documentation."],["DESCRIPTION","**lvim** (LunarVim) is a pre-configured Neovim distribution that provides an IDE-like experience out of the box. It includes curated plugins, keybindings, and sensible defaults while remaining fully customizable.\n\nLunarVim includes built-in support for LSP (Language Server Protocol), providing intelligent code completion, diagnostics, and refactoring. Treesitter provides advanced syntax highlighting and code navigation. The which-key plugin displays available keybindings contextually.\n\nConfiguration is done via **~/.config/lvim/config.lua** using Lua. The **lvim** global object exposes settings for plugins, keybindings, and editor options. Custom plugins can be added through the lvim.plugins table.\n\nSince LunarVim is built on Neovim, all Neovim command-line options are supported. The editor inherits Vim's modal editing paradigm with Normal, Insert, Visual, and Command-line modes."],["CAVEATS","Requires Neovim 0.9.0 or higher, along with git, make, pip, npm, node, cargo, and ripgrep. Initial installation downloads and compiles multiple plugins, requiring internet connectivity. Updates may occasionally require manual intervention if breaking changes occur."],["HISTORY","LunarVim was created in **2021** by **Christian Chiarulli** as a way to provide a batteries-included Neovim experience. It gained popularity as a more approachable alternative to building a custom Neovim configuration from scratch. Note: the project has been **archived** and is no longer actively maintained as of **2024**."],["SEE ALSO","[nvim](/man/nvim)(1), [vim](/man/vim)(1)"]],"e":["nvim","vim"],"x":"lvim"},"mariadb-binlog":{"n":"mariadb-binlog","t":"Read and replay MariaDB binary log files","s":[["TAGLINE","Read and replay MariaDB binary log files"],["TLDR","**Display events** from a binary log file\n\n```mariadb-binlog /var/lib/mysql/mariadb-bin.000003```\n\n**Filter events** for one database\n\n```mariadb-binlog -d mydb /var/lib/mysql/mariadb-bin.000003```\n\n**Show events** between two timestamps\n\n```mariadb-binlog --start-datetime='2026-01-01 00:00:00' --stop-datetime='2026-02-01 00:00:00' /var/lib/mysql/mariadb-bin.000003```\n\n**Show events** between byte positions\n\n```mariadb-binlog -j 100 --stop-position 200 /var/lib/mysql/mariadb-bin.000003```\n\n**Replay** log output into MariaDB\n\n```mariadb-binlog mariadb-bin.000001 | mariadb -u root -p```"],["SYNOPSIS","**mariadb-binlog** [_options_] _log_file_ ..."],["PARAMETERS","**-d** _db_name_, **--database** _db_name_\n> Output only events that modify tables in _db_name_\n\n**-j** _N_, **--start-position** _N_\n> Start at the first event at or after byte position _N_\n\n**--stop-position** _N_\n> Stop at the first event at or after byte position _N_\n\n**--start-datetime** _datetime_\n> Start at the first event at or after _datetime_\n\n**--stop-datetime** _datetime_\n> Stop at the first event at or after _datetime_\n\n**-h** _host_, **--host** _host_\n> Read the binary log from a remote MariaDB server (with **--read-from-remote-server**)\n\n**-R**, **--read-from-remote-server**\n> Fetch logs from a server instead of local files\n\n**-v**, **--verbose**\n> Decode row events as commented SQL statements\n\n**-H**, **--hexdump**\n> Include a hex dump of each event in the output\n\n**-s**, **--short-form**\n> Print only SQL statements, without extra metadata\n\n**-r** _file_, **--result-file** _file_\n> Write output to _file_ instead of stdout\n\n**--help**, **-?**\n> Show help and exit\n\n**--version**, **-V**\n> Show version and exit"],["DESCRIPTION","**mariadb-binlog** is a MariaDB client utility for reading binary log files. The server writes these logs in a compact binary format; **mariadb-binlog** converts the events into human-readable SQL or metadata so DBAs can audit changes, debug replication, and perform point-in-time recovery.\n\nEach event is preceded by a header comment with the byte position, timestamp, server ID, and execution time. For statement-based logging, the output shows the original SQL. For row-based logging, use **--verbose** to reconstruct the affected rows as SQL comments.\n\nThe utility can read local log files or stream logs from a remote server. Its output can be piped into **mariadb** to replay statements, edited first for selective recovery, or saved to a text file for later inspection.\n\nRelay logs on replication replicas use the same format, so **mariadb-binlog** can inspect them as well."],["CAVEATS","Replaying multiple log files through separate **mariadb** connections can break statements that rely on temporary tables created in an earlier file. Pipe all required logs through a single **mariadb** process instead.\n\nWhen using **--database**, filtering behavior differs between statement-based and row-based logging. With row-based logs, only events that modify tables in the selected database are shown.\n\nRemote log reading requires a running server and appropriate privileges."],["INSTALL","```apt: sudo apt install mariadb-client```\n\n```dnf: sudo dnf install mariadb```\n\n```pacman: sudo pacman -S mariadb```\n\n```apk: sudo apk add mariadb```\n\n```zypper: sudo zypper install mariadb-client```\n\n```brew: brew install mariadb```\n\n```nix: nix profile install nixpkgs#mariadb```"],["SEE ALSO","[mysqlbinlog](/man/mysqlbinlog)(1), [mariadb](/man/mariadb)(1)"]],"e":["mysqlbinlog","mariadb"],"x":"mariadb-binlog /var/lib/mysql/mariadb-bin.000003"},"mdbtools":{"n":"mdbtools","t":"read Microsoft Access (.mdb, .accdb) databases on Unix","s":[["TAGLINE","read Microsoft Access (.mdb, .accdb) databases on Unix"],["TLDR","**List the tables** in a database\n\n```mdb-tables [database.mdb]```\n\n**Export a table** to CSV on stdout\n\n```mdb-export [database.mdb] [tablename]```\n\n**Dump the entire schema** as SQL DDL\n\n```mdb-schema [database.mdb]```\n\n**Count rows** in a table\n\n```mdb-count [database.mdb] [tablename]```\n\n**Open an interactive SQL** prompt\n\n```mdb-sql [database.mdb]```\n\n**Dump every table** to a directory of CSVs\n\n```for t in $(mdb-tables -1 [database.mdb]); do mdb-export [database.mdb] \"$t\" > \"$t.csv\"; done```\n\n**Convert to a SQLite database**\n\n```mdb-schema [database.mdb] sqlite | sqlite3 [out.db] && for t in $(mdb-tables -1 [database.mdb]); do mdb-export -I sqlite [database.mdb] \"$t\" | sqlite3 [out.db]; done```"],["SYNOPSIS","**mdb-tables** [_-1_] _database_\n\n**mdb-schema** [_options_] _database_ [_backend_]\n\n**mdb-export** [_options_] _database_ _tablename_\n\n**mdb-sql** [_options_] _database_\n\n**mdb-count** _database_ _tablename_"],["PARAMETERS","_DATABASE_\n> Microsoft Access database file (.mdb for Jet/Access 97-2003, .accdb for Access 2007+).\n\n**mdb-tables** [**-1**]\n> List tables; **-1** prints one name per line for shell iteration.\n\n**mdb-schema** _backend_\n> Dump SQL CREATE statements; backend selects dialect (access, sybase, oracle, sqlite, postgres, mysql).\n\n**mdb-export** [**-I** _backend_] [**-d** _delim_] [**-q** _quote_]\n> Export a single table. **-I** emits INSERT statements for the given backend; otherwise CSV.\n\n**mdb-sql**\n> Open an interactive SQL prompt supporting a subset of SQL92.\n\n**mdb-count**\n> Print the number of rows in a table."],["DESCRIPTION","**mdbtools** is a suite of utilities for reading Microsoft Access databases on Unix-like systems without requiring the Microsoft Jet or ACE engine. Each tool is a separate binary that operates on a single **.mdb** or **.accdb** file: **mdb-tables** lists tables, **mdb-schema** prints DDL, **mdb-export** dumps rows, **mdb-sql** runs queries, **mdb-count** counts rows, **mdb-prop** lists object properties, and **mdb-ver** shows the Access version stored in the file.\n\nTypical workflows are migration (export every table to CSV or to another RDBMS using **-I** _backend_) and inspection (running SELECTs from **mdb-sql**). The suite also ships a libmdb library that GUI tools and the **mdb-export** ODBC driver use under the hood."],["CAVEATS","**Read-only**: mdbtools cannot create or modify Access databases. Some advanced features (encrypted files, certain index types, complex linked tables, attachment columns, multi-value fields) are partially or not supported. Older releases struggled with .accdb files; **mdbtools 1.0** (2021) added robust .accdb support, so always prefer recent versions."],["HISTORY","**mdbtools** was started by **Brian Bruns** in **2000** and stalled until **Evan Miller** revived the project in **2014**. The **1.0** release in **2021** brought up-to-date .accdb support, a packaged libmdb API, and an actively maintained ODBC driver."],["INSTALL","```dnf: sudo dnf install mdbtools```\n\n```zypper: sudo zypper install mdbtools```\n\n```brew: brew install mdbtools```\n\n```nix: nix profile install nixpkgs#mdbtools```"],["SEE ALSO","[mdb-sql](/man/mdb-sql)(1), [sqlite3](/man/sqlite3)(1), [csvtool](/man/csvtool)(1)"]],"e":["mdb-sql","sqlite3","csvtool"],"x":"mdb-tables [database.mdb]"},"moonshine":{"n":"moonshine","t":"Headless game streaming server for Moonlight clients","s":[["TAGLINE","Headless game streaming server for Moonlight clients"],["TLDR","**Install** from the Arch User Repository\n\n```yay -S moonshine```\n\n**Enable** the user service (start now and on boot)\n\n```sudo systemctl enable --now moonshine@$USER```\n\n**Allow** the service to run when you are not logged in\n\n```sudo loginctl enable-linger $USER```\n\n**Run** the server directly with a config file (builds from source)\n\n```moonshine [/path/to/config.toml]```\n\n**Pair** a Moonlight client by submitting its PIN\n\n```curl -X POST \"http://localhost:47989/submit-pin\" -d \"uniqueid=0123456789ABCDEF&pin=[PIN]\"```"],["SYNOPSIS","**moonshine** _config.toml_"],["DESCRIPTION","**moonshine** is a Linux-only headless streaming host compatible with [Moonlight](https://moonlight-stream.org/) clients. It streams games (or any application) from your PC to phones, tablets, and other devices, and sends keyboard, mouse, and controller input back to the host.\n\nUnlike desktop-sharing hosts, each stream runs in its own isolated Wayland compositor session, separate from your local desktop. The host remains usable while streaming, and no physical monitor or HDMI dummy plug is required.\n\nVideo is hardware-encoded (H.264, H.265, and experimental AV1) via Vulkan on NVIDIA RTX, AMD RDNA2+, or Intel Arc GPUs. Audio uses low-latency Opus (stereo or 5.1/7.1). HDR (10-bit) is supported when the game and client allow it. Clients need Moonlight v6.0.0 or newer. systemd is required to launch and manage application processes."],["PARAMETERS","_config.toml_\n\n> Path to the TOML configuration file. Created automatically with defaults if it does not exist. The AUR package defaults to **$XDG_CONFIG_HOME/moonshine/config.toml**.\n\n**-V**, **--version**\n\n> Print version and exit."],["CONFIGURATION","**$XDG_CONFIG_HOME/moonshine/config.toml** (typically **~/.config/moonshine/config.toml**)\n\n> Main configuration. Define applications and optional scanners.\n\nExample application entry:\n\n```\n[[application]]\ntitle = \"Steam\"\nboxart = \"/path/to/steam.png\"\ncommand = [\"/usr/bin/steam\", \"steam://open/bigpicture\"]\npre_command = [\n    [\"/usr/bin/systemctl\", \"stop\", \"conflicting.service\"],\n]\npost_command = [\n    [\"/usr/bin/nvidia-smi\", \"pstate\", \"performance\"],\n]\n```\n\nSteam library scanner (auto-discovers installed games):\n\n```\n[[application_scanner]]\ntype = \"steam\"\nlibrary = \"$HOME/.local/share/Steam\"\ncommand = [\"/usr/bin/steam\", \"-bigpicture\", \"steam://rungameid/{game_id}\"]\n```\n\nDesktop scanner (from .desktop files):\n\n```\n[[application_scanner]]\ntype = \"desktop\"\ndirectories = [\n  \"$HOME/.local/share/applications\",\n  \"/usr/share/applications\",\n]\ninclude_terminal = false\nresolve_icons = true\n```\n\n**MOONSHINE_LOG**\n\n> Environment filter for tracing (default level: **error**). Passed to the Rust tracing EnvFilter.\n\nFirst-time pairing: connect with Moonlight, then open **http://localhost:47989/pin** on the host (or use the curl example above) to submit the PIN."],["CAVEATS","Linux and systemd only. Requires a GPU with Vulkan video encoding. AV1 encoding is experimental and can grow frame sizes over time on some NVIDIA drivers; prefer H.264 or H.265 until fixed. Not safe on public networks: the GameStream protocol is not fully encrypted at the application layer — use a VPN (**tailscale**, **wg**/WireGuard) for remote access and never expose ports to the internet."],["HISTORY","**Moonshine** is a Rust reimplementation of a Moonlight-compatible host focused on isolated headless sessions on Linux. It builds on ideas from Sunshine (GameStream host API), Inputtino (input devices), and magic-mirror (Vulkan/Wayland compositor streaming)."],["INSTALL","```aur: yay -S moonshine```"],["SEE ALSO","[systemctl](/man/systemctl)(1), [loginctl](/man/loginctl)(1), [steam](/man/steam)(1), [tailscale](/man/tailscale)(1), [wg](/man/wg)(8), [yay](/man/yay)(8)"],["RESOURCES","```[Source code](https://github.com/hgaiser/moonshine)```\n\n```[Homepage](https://github.com/hgaiser/moonshine)```\n\n```[Documentation](https://github.com/hgaiser/moonshine#readme)```"]],"e":["systemctl","loginctl","steam","tailscale","wg","yay"],"x":"yay -S moonshine"},"mvn":{"n":"mvn","t":"Apache Maven build automation and dependency management tool","s":[["TAGLINE","Apache Maven build automation and dependency management tool"],["TLDR","**Build project**\n\n```mvn package```\n\n**Clean and build, install to local repository**\n\n```mvn clean install```\n\n**Run tests**\n\n```mvn test```\n\n**Skip tests during build**\n\n```mvn install -DskipTests```\n\n**Generate a new project from archetype**\n\n```mvn archetype:generate```\n\n**Deploy artifact to remote repository**\n\n```mvn deploy```\n\n**Show dependency tree**\n\n```mvn dependency:tree```\n\n**Check for dependency updates**\n\n```mvn versions:display-dependency-updates```\n\n**Run with a specific profile**\n\n```mvn clean install -P [profile-name]```\n\n**Build offline without downloading dependencies**\n\n```mvn package -o```"],["SYNOPSIS","**mvn** [_options_] [_phase(s)_] [_goal(s)_]"],["PARAMETERS","_PHASES_\n> Build lifecycle phases to execute.\n\n**clean**\n> Remove target directory with compiled output.\n\n**validate**\n> Validate project is correct and all information is available.\n\n**compile**\n> Compile project sources.\n\n**test**\n> Run unit tests.\n\n**package**\n> Package compiled code (JAR, WAR, etc.).\n\n**verify**\n> Run integration tests and checks.\n\n**install**\n> Install package to local repository (~/.m2/repository).\n\n**deploy**\n> Deploy package to remote repository.\n\n**-D** _PROPERTY=VALUE_\n> Set a system property (e.g., -DskipTests, -Dmaven.test.skip=true).\n\n**-P** _PROFILE_\n> Activate a build profile defined in pom.xml.\n\n**-pl** _MODULES_, **--projects** _MODULES_\n> Build specific modules in a multi-module project.\n\n**-am**, **--also-make**\n> Build required dependent modules when using -pl.\n\n**-f** _FILE_, **--file** _FILE_\n> Use an alternative POM file.\n\n**-o**, **--offline**\n> Work offline without downloading dependencies.\n\n**-U**, **--update-snapshots**\n> Force update of snapshot dependencies.\n\n**-T** _THREADS_\n> Thread count for parallel builds (e.g., -T 4, -T 1C for 1 per CPU core).\n\n**-X**, **--debug**\n> Produce debug output.\n\n**-q**, **--quiet**\n> Quiet output, only show errors.\n\n**-e**, **--errors**\n> Produce execution error messages.\n\n**-B**, **--batch-mode**\n> Non-interactive batch mode (recommended in CI environments).\n\n**-N**, **--non-recursive**\n> Do not recurse into sub-projects (build reactor root only).\n\n**-s** _FILE_, **--settings** _FILE_\n> Use alternate user settings file.\n\n**-gs** _FILE_, **--global-settings** _FILE_\n> Use alternate global settings file.\n\n**--help**\n> Display help information."],["DESCRIPTION","**mvn** is the command-line interface for Apache Maven, a build automation and dependency management tool primarily used for Java projects. It uses a Project Object Model (pom.xml) to describe the project, its dependencies, plugins, and build lifecycle.\n\nMaven follows a convention-over-configuration approach with a standard project structure (src/main/java, src/test/java, etc.). The build lifecycle consists of ordered phases: validate, compile, test, package, verify, install, deploy. Specifying a phase executes all preceding phases.\n\nDependencies are resolved from remote repositories (Maven Central by default) and cached in the local repository at ~/.m2/repository. Maven supports multi-module projects, build profiles for environment-specific configurations, and an extensive plugin ecosystem."],["CAVEATS","Requires a JDK installation and a pom.xml file. First builds download many dependencies and may take longer. The **-DskipTests** flag skips test execution but still compiles tests; use **-Dmaven.test.skip=true** to skip compilation of tests entirely. The local repository (~/.m2/repository) can grow large over time."],["HISTORY","Maven was created by **Jason van Zyl** at the Apache Software Foundation. Maven 1.0 was released in **2004**, and Maven 2.0 followed in **2005** with a complete rewrite. Maven 3.0 was released in **2010** with improved performance and backward compatibility. It remains one of the most widely used Java build tools alongside Gradle."],["INSTALL","```dnf: sudo dnf install maven```\n\n```pacman: sudo pacman -S maven```\n\n```apk: sudo apk add maven```\n\n```zypper: sudo zypper install maven```\n\n```brew: brew install maven```\n\n```nix: nix profile install nixpkgs#maven```"],["SEE ALSO","[gradle](/man/gradle)(1), [ant](/man/ant)(1), [java](/man/java)(1), [javac](/man/javac)(1)"]],"e":["gradle","ant","java","javac"],"x":"mvn package"},"nh-search":{"n":"nh-search","t":"searches Nix packages interactively","s":[["TAGLINE","searches Nix packages interactively"],["TLDR","**Search packages**\n\n```nh search [query]```\n\n**Search with JSON output**\n\n```nh search --json [query]```"],["SYNOPSIS","**nh search** [_options_] _query_"],["PARAMETERS","_query_\n> Search query.\n\n**--json**\n> Output in JSON format.\n\n**--channel** _channel_\n> Nixpkgs channel."],["DESCRIPTION","**nh search** searches Nix packages interactively. Provides fast fuzzy search with preview. Part of nh (nix helper) toolkit."],["INSTALL","```nix: nix profile install nixpkgs#nh```"],["SEE ALSO","[nh](/man/nh)(1), [nix-search](/man/nix-search)(1)"]],"e":["nh","nix-search"],"x":"nh search [query]"},"nmcli-monitor":{"n":"nmcli-monitor","t":"watches for and displays NetworkManager events in real-time","s":[["TAGLINE","watches for and displays NetworkManager events in real-time"],["TLDR","Start **monitoring** NetworkManager changes\n\n```nmcli monitor```"],["SYNOPSIS","**nmcli monitor**"],["DESCRIPTION","**nmcli monitor** watches for and displays NetworkManager events in real-time. It shows changes to network connections, device states, and connectivity status as they occur.\n\nEvents displayed include connection activations and deactivations, device state changes (connecting, connected, disconnected), IP address assignments, and overall connectivity state changes.\n\nThe command runs continuously until interrupted with **Ctrl+C**, making it useful for debugging network issues or scripting responses to network events."],["CAVEATS","Runs in the foreground and blocks the terminal. Only shows events occurring after the monitor starts; historical events are not displayed. For parsing in scripts, consider using nmcli's **-t** (terse) option for machine-readable output."],["INSTALL","```apt: sudo apt install network-manager```\n\n```apk: sudo apk add networkmanager-cli```"],["SEE ALSO","[nmcli](/man/nmcli)(1), [nmcli-general](/man/nmcli-general)(1), [nmcli-connection](/man/nmcli-connection)(1), [nmcli-device](/man/nmcli-device)(1)"]],"e":["nmcli","nmcli-general","nmcli-connection","nmcli-device"],"x":"nmcli monitor"},"nnn":{"n":"nnn","t":"terminal file manager","s":[["TAGLINE","terminal file manager"],["TLDR","**Start file manager**\n\n```nnn```\n\n**Start in directory**\n\n```nnn [/path/to/dir]```\n\n**Show hidden files**\n\n```nnn -H```\n\n**Detail mode**\n\n```nnn -d```\n\n**Don't use color**\n\n```nnn -c```\n\n**Open with editor**\n\n```nnn -e```\n\n**Start with bookmarks**\n\n```nnn -b [bookmark-file]```"],["SYNOPSIS","**nnn** [_options_] [_directory_]"],["PARAMETERS","_DIRECTORY_\n> Starting directory.\n\n**-H**\n> Show hidden files.\n\n**-d**\n> Detail mode.\n\n**-c**\n> No color.\n\n**-e**\n> Open text files in editor.\n\n**-b** _FILE_\n> Bookmark file.\n\n**--help**\n> Display help information."],["DESCRIPTION","**nnn** is a terminal file manager. Fast, minimal, and keyboard-driven.\n\nThe tool navigates directories. Supports plugins and file operations."],["CAVEATS","Keyboard-centric. Vim-like bindings. Plugins extend functionality."],["HISTORY","nnn was created as a **blazing fast** terminal file manager with low resource usage."],["INSTALL","```apt: sudo apt install nnn```\n\n```dnf: sudo dnf install nnn```\n\n```pacman: sudo pacman -S nnn```\n\n```apk: sudo apk add nnn```\n\n```zypper: sudo zypper install nnn```\n\n```brew: brew install nnn```\n\n```nix: nix profile install nixpkgs#nnn```"],["SEE ALSO","[ranger](/man/ranger)(1), [mc](/man/mc)(1), [lf](/man/lf)(1)"]],"e":["ranger","mc","lf"],"x":"nnn"},"partclone":{"n":"partclone","t":"creates and restores partition images while skipping empty blocks","s":[["TAGLINE","creates and restores partition images while skipping empty blocks"],["TLDR","**Clone** partition to image file\n\n```sudo partclone.ext4 -c -s /dev/sdXY -o [path/to/backup.img]```\n\n**Restore** partition from image\n\n```sudo partclone.ext4 -r -s [path/to/backup.img] -o /dev/sdXY```\n\nClone **btrfs** partition\n\n```sudo partclone.btrfs -c -s /dev/sdXY -o [path/to/backup.img]```\n\n**Device to device** clone\n\n```sudo partclone.ext4 -b -s /dev/sdX1 -o /dev/sdY1```\n\nClone with **rescue mode** (continue after read errors)\n\n```sudo partclone.ext4 -c -R -s /dev/sdXY -o [path/to/backup.img]```\n\nClone with **ncurses** interface\n\n```sudo partclone.ext4 -c -N -s /dev/sdXY -o [path/to/backup.img]```"],["SYNOPSIS","**partclone.**_fstype_ [**-c**|**-r**|**-b**] [**-s** _source_] [**-o** _output_] [_options_]"],["COMMANDS","**partclone.ext4**\n> Clone ext4 filesystems\n\n**partclone.btrfs**\n> Clone btrfs filesystems\n\n**partclone.ntfs**\n> Clone NTFS filesystems\n\n**partclone.fat32**\n> Clone FAT32 filesystems\n\n**partclone.xfs**\n> Clone XFS filesystems\n\n**partclone.dd**\n> Raw block copy (any filesystem)"],["PARAMETERS","**-c**, **--clone**\n> Clone partition to image file.\n\n**-r**, **--restore**\n> Restore partition from image file.\n\n**-b**, **--dev-to-dev**\n> Device to device clone.\n\n**-s** _source_, **--source** _source_\n> Source device or image file.\n\n**-o** _output_, **--output** _output_\n> Output device or image file.\n\n**-L** _file_, **--logfile** _file_\n> Write log to file.\n\n**-C**, **--no-check**\n> Skip filesystem check before clone.\n\n**-R**, **--rescue**\n> Continue after disk read errors.\n\n**-N**, **--ncurses**\n> Use Ncurses text user interface.\n\n**-O** _file_, **--overwrite** _file_\n> Output file, overwriting if it exists.\n\n**-n**, **--note** _MSG_\n> Add a note to the image (up to 128 words)."],["DESCRIPTION","**partclone** creates and restores partition images while skipping empty blocks, resulting in smaller backup files and faster operations compared to raw disk imaging. Each filesystem type has its own binary that understands the filesystem structure.\n\nThe tool reads only used blocks from the source filesystem, making backups significantly smaller than the partition size. This is especially beneficial for partially filled partitions."],["CAVEATS","Source partition should be unmounted during cloning. Filesystem-specific binary must match the partition type. Restored partitions may need fsck. Image files are not directly mountable."],["HISTORY","**partclone** was developed as an efficient alternative to **dd** for partition backup. By understanding filesystem metadata, it achieves compression without needing external tools, making it popular for system backup and deployment scenarios."],["INSTALL","```dnf: sudo dnf install partclone```\n\n```pacman: sudo pacman -S partclone```\n\n```zypper: sudo zypper install partclone```\n\n```nix: nix profile install nixpkgs#partclone```"],["SEE ALSO","[dd](/man/dd)(1), [clonezilla](/man/clonezilla)(1), [fsarchiver](/man/fsarchiver)(1)"]],"e":["dd","clonezilla","fsarchiver"],"x":"sudo partclone.ext4 -c -s /dev/sdXY -o [path/to/backup.img]"},"pbuilder":{"n":"pbuilder","t":"builds Debian packages in chroot","s":[["TAGLINE","builds Debian packages in chroot"],["TLDR","**Create base tarball**\n\n```pbuilder create```\n\n**Build package**\n\n```pbuilder build [package.dsc]```\n\n**Update base tarball**\n\n```pbuilder update```\n\n**Login to chroot**\n\n```pbuilder login```\n\n**Build with distribution**\n\n```pbuilder build --distribution [sid] [package.dsc]```"],["SYNOPSIS","**pbuilder** [_command_] [_options_]"],["PARAMETERS","**create**\n> Create base tarball.\n\n**build** _DSC_\n> Build package from .dsc.\n\n**update**\n> Update base tarball.\n\n**login**\n> Enter chroot shell.\n\n**--distribution** _DIST_\n> Target distribution.\n\n**--basetgz** _FILE_\n> Base tarball path."],["DESCRIPTION","**pbuilder** builds Debian packages in chroot. Clean build environment.\n\nThe tool ensures reproducible builds. Isolated from host system."],["CAVEATS","Root required. Debian/Ubuntu specific. Network access configurable."],["HISTORY","pbuilder was created for **clean Debian package** building in isolated chroots."],["INSTALL","```dnf: sudo dnf install pbuilder```\n\n```zypper: sudo zypper install pbuilder```"],["SEE ALSO","[dpkg-buildpackage](/man/dpkg-buildpackage)(1), [sbuild](/man/sbuild)(1), [cowbuilder](/man/cowbuilder)(1)"]],"e":["dpkg-buildpackage","sbuild","cowbuilder"],"x":"pbuilder create"},"phantasia":{"n":"phantasia","t":"multiplayer terminal fantasy RPG game","s":[["TAGLINE","multiplayer terminal fantasy RPG game"],["TLDR","**Start a game**\n\n```phantasia```\n\n**Start in setup/wizard mode**\n\n```phantasia -s```"],["SYNOPSIS","**phantasia** [**-s**] [**-m**] [**-a**] [**-p**]"],["PARAMETERS","**-s**\n> Setup/wizard mode.\n\n**-m**\n> Manage the game (admin).\n\n**-a**\n> All players listing.\n\n**-p**\n> Purge old characters."],["PREVIEW","```\n Phantasia -- Level 3\n HP: 45/60  Str: 12\n Gold: 350  Gems: 2\n You encounter a Troll!\n (f)ight or (r)un?\n```"],["DESCRIPTION","**phantasia** is a multi-player fantasy RPG game from the **bsdgames** collection. Players create characters, fight monsters, gain experience and gold, and compete with other players on the same system for ranking.\n\nThe game features character progression, equipment, magic, and encounters with both monsters and other players."],["CAVEATS","Part of the bsdgames package. Multi-player features require shared file access on the same system."],["HISTORY","**phantasia** was written by **Edward Straker** and has been part of BSD games distributions since the **1980s**. It is one of the earliest multiplayer RPGs on Unix."],["SEE ALSO","[rogue](/man/rogue)(6), [nethack](/man/nethack)(6), [moria](/man/moria)(6)"]],"e":["rogue","nethack","moria"],"x":"phantasia"},"pic":{"n":"pic","t":"troff preprocessor for drawing diagrams","s":[["TAGLINE","troff preprocessor for drawing diagrams"],["TLDR","**Process pic to troff**\n\n```pic [diagram.pic] | troff```\n\n**Generate output**\n\n```pic [diagram.pic] > [output.troff]```\n\n**Safe mode**\n\n```pic -S [diagram.pic]```\n\n**Check syntax only**\n\n```pic -n [diagram.pic]```\n\n**Show version**\n\n```pic --version```"],["SYNOPSIS","**pic** [_-S_] [_-n_] [_-C_] [_options_] [_files_]"],["PARAMETERS","**-S**\n> Safe mode (no shell commands).\n\n**-n**\n> Suppress output, check syntax.\n\n**-C**\n> Compatible mode.\n\n**-D**\n> Define macro.\n\n**-T** _DEVICE_\n> Output device.\n\n**-z**\n> Draw invisible lines and boxes as zero-size objects (GNU extension).\n\n**--help**\n> Display usage information.\n\n**--version**\n> Print version information."],["DESCRIPTION","**pic** is a troff preprocessor for drawing diagrams. It converts textual descriptions into troff drawing commands.\n\nDiagrams use geometric primitives: boxes, circles, lines, arrows. Positioning is relative or absolute.\n\nObjects connect with lines and arrows. Labels and text annotate elements.\n\nThe language supports macros and variables. Complex diagrams build from reusable components.\n\nOutput integrates with groff/troff document processing. Pic diagrams embed in technical documents."],["EXAMPLE","```\n.PS\nbox \"Start\"\narrow\ncircle \"Process\"\narrow\nbox \"End\"\n.PE\n```"],["CAVEATS","Limited compared to modern diagramming tools. Troff output only. Steep learning curve."],["HISTORY","**pic** was created by **Brian Kernighan** at **Bell Labs** in the **1980s**. It was part of the troff/groff document preparation system and influenced later diagramming languages."],["INSTALL","```apt: sudo apt install groff-base```\n\n```dnf: sudo dnf install groff-base```\n\n```pacman: sudo pacman -S groff```\n\n```apk: sudo apk add groff```\n\n```zypper: sudo zypper install groff```\n\n```brew: brew install groff```\n\n```nix: nix profile install nixpkgs#groff```"],["SEE ALSO","[groff](/man/groff)(1), [troff](/man/troff)(1), [tbl](/man/tbl)(1), [eqn](/man/eqn)(1)"]],"e":["groff","troff","tbl","eqn"],"x":"pic [diagram.pic] | troff"},"pxelinux-options":{"n":"pxelinux-options","t":"Reference for PXELINUX-specific DHCP options","s":[["TAGLINE","Reference for PXELINUX-specific DHCP options"],["TLDR","**View the pxelinux-options man page**\n\n```man pxelinux-options```"],["SYNOPSIS","This is a documentation reference, not an executable command."],["PARAMETERS","**DHCP Option 208** (pxelinux.magic)\n> Magic number identifying PXELINUX-aware DHCP configuration. Must be set to F1:00:74:7E.\n\n**DHCP Option 209** (pxelinux.configfile)\n> Specifies the PXELINUX configuration file path relative to the TFTP server prefix.\n\n**DHCP Option 210** (pxelinux.pathprefix)\n> Path prefix prepended to all file references in the PXELINUX configuration file.\n\n**DHCP Option 211** (pxelinux.reboottime)\n> Reboot time in seconds, encoded as a 32-bit unsigned integer. Controls how long PXELINUX waits before rebooting on failure."],["DESCRIPTION","**pxelinux-options** is a man page documenting the DHCP options specific to **PXELINUX**, the network (PXE) booting variant of the SYSLINUX bootloader. These options are passed via DHCP to PXELINUX clients to configure their boot behavior.\n\nThe options use DHCP option numbers 208-211 (site-specific option space) and allow the DHCP server to specify the configuration file, path prefix, and reboot timeout for PXE booting clients. These options must be configured on the DHCP server (e.g., ISC dhcpd or dnsmasq) to be passed to booting clients."],["CAVEATS","This is a documentation reference, not an executable. The DHCP options 208-211 are in the site-local option space and may conflict with other uses of these option numbers. Option 208 (pxelinux.magic) must be set correctly for PXELINUX to recognize the other options."],["HISTORY","pxelinux-options is part of the **SYSLINUX** project, created by **H. Peter Anvin**. PXELINUX enables Linux systems to boot over the network using the PXE (Preboot Execution Environment) standard."],["INSTALL","```apt: sudo apt install syslinux-utils```\n\n```dnf: sudo dnf install syslinux```\n\n```pacman: sudo pacman -S syslinux```\n\n```apk: sudo apk add syslinux```\n\n```zypper: sudo zypper install syslinux```\n\n```nix: nix profile install nixpkgs#syslinux```"],["SEE ALSO","[syslinux](/man/syslinux)(1), [pxelinux](/man/pxelinux)(1), [tftp](/man/tftp)(1), [dhcpd](/man/dhcpd)(1), [dnsmasq](/man/dnsmasq)(1)"]],"e":["syslinux","pxelinux","tftp","dhcpd","dnsmasq"],"x":"man pxelinux-options"},"qm-disk":{"n":"qm-disk","t":"Manage Proxmox VM disk images","s":[["TAGLINE","Manage Proxmox VM disk images"],["TLDR","Add **10 gigabytes** to a virtual disk\n\n```qm disk resize [100] scsi0 +10G```\n\n**Move** a virtual disk to different storage\n\n```qm disk move [100] scsi0 [local-lvm]```\n\nMove and **delete** the original copy\n\n```qm disk move [100] scsi0 [local-lvm] --delete```\n\n**Import** a disk image with specific format\n\n```qm disk import [100] [/path/to/disk.vmdk] [local-lvm] --format qcow2```\n\n**Rescan** all storages and update disk sizes\n\n```qm disk rescan```\n\nPerform a **dry-run** rescan\n\n```qm disk rescan --dryrun```\n\nRescan for a **specific VM**\n\n```qm disk rescan --vmid [100]```"],["SYNOPSIS","**qm disk** _command_ [_options_]"],["PARAMETERS","**resize** _vmid_ _disk_ _size_\n> Resize a disk (use +nG to add space). Shrinking is not supported.\n\n**move** _vmid_ _disk_ _storage_\n> Move disk to different storage or different VM.\n\n**import** _vmid_ _source_ _storage_\n> Import external disk image as unused disk.\n\n**rescan** [_options_]\n> Rescan storages for disk changes.\n\n**--format** _format_\n> Target disk image format (qcow2, raw, vmdk).\n\n**--delete**\n> Delete source after move.\n\n**--dryrun**\n> Preview changes without applying.\n\n**--vmid** _id_\n> Target specific VM for rescan.\n\n**--disk** _disk_id_\n> Assign specific disk ID (e.g. scsi0, sata1) on import."],["DESCRIPTION","**qm disk** manages disk images for Proxmox VE virtual machines. It handles resizing, moving between storage backends, importing external disk images, and rescanning storage for changes.\n\nThis is essential for storage management and migration tasks."],["CAVEATS","Resize operations cannot shrink disks. Import and move operations may take time for large disks. Ensure sufficient space on destination storage."],["HISTORY","Part of **Proxmox VE** QEMU/KVM management tools for virtual machine administration."],["INSTALL","```dnf: sudo dnf install qm```"],["SEE ALSO","[qm](/man/qm)(1), [qm-create](/man/qm-create)(1), [pvesm](/man/pvesm)(1)"]],"e":["qm","qm-create","pvesm"],"x":"qm disk resize [100] scsi0 +10G"},"qm-showcmd":{"n":"qm-showcmd","t":"Show QEMU command line for a Proxmox VM","s":[["TAGLINE","Show QEMU command line for a Proxmox VM"],["TLDR","Show **command-line** used to start a VM\n\n```qm showcmd 100```\n\nShow with **enhanced readability** (one option per line)\n\n```qm showcmd 100 --pretty 1```\n\nShow command-line from a **specific snapshot**\n\n```qm showcmd 100 --snapshot snapshot_name```"],["SYNOPSIS","**qm showcmd** _vmid_ [_options_]"],["PARAMETERS","**--pretty** _boolean_\n> Put each option on a new line for readability. Default is 0.\n\n**--snapshot** _string_\n> Fetch configuration from a given snapshot"],["DESCRIPTION","**qm showcmd** displays the command-line that would be used to start a virtual machine. This is useful for debugging VM configuration and understanding how QEMU is invoked.\n\nThe output shows all QEMU options and arguments based on the VM's current configuration or a specific snapshot."],["CAVEATS","Debug information only; the actual command may vary at runtime. Configuration changes require VM restart to take effect."],["HISTORY","Part of **Proxmox VE** QEMU/KVM management tools for debugging VM configuration."],["INSTALL","```dnf: sudo dnf install qm```"],["SEE ALSO","[qm](/man/qm)(1), [qm-config](/man/qm-config)(1)"]],"e":["qm","qm-config"],"x":"qm showcmd 100"},"rcs":{"n":"rcs","t":"Revision Control System for file versioning","s":[["TAGLINE","Revision Control System for file versioning"],["TLDR","**Initialize RCS for a file** (check in initial version)\n\n```ci [filename]```\n\n**Check out a file for editing** (locked)\n\n```co -l [filename]```\n\n**Check out a read-only copy**\n\n```co [filename]```\n\n**Check in changes** with a log message\n\n```ci -m \"[message]\" [filename]```\n\n**View revision history**\n\n```rlog [filename]```\n\n**Show differences** between working file and latest revision\n\n```rcsdiff [filename]```\n\n**Check out a specific revision**\n\n```co -r[1.2] [filename]```"],["SYNOPSIS","**rcs** [_options_] _file_...\n\n**ci** [_options_] _file_...\n\n**co** [_options_] _file_..."],["PARAMETERS","**ci** (check-in)\n> Store a new revision in the RCS file\n\n**co** (check-out)\n> Retrieve a revision from the RCS file\n\n**rcs**\n> Change RCS file attributes\n\n**rcsdiff**\n> Compare RCS revisions\n\n**rcsmerge**\n> Merge RCS revisions\n\n**rlog**\n> Print revision log messages\n\n**-l**\n> Lock the revision for exclusive editing (with co)\n\n**-u**\n> Unlock after check-in (with ci)\n\n**-r**_rev_\n> Specify revision number\n\n**-m**_msg_\n> Specify log message\n\n**-q**\n> Quiet mode, suppress diagnostics\n\n**-d**_date_\n> Select the latest revision on or before the given date (with co)\n\n**-w**_login_\n> Select the latest revision checked in by the given user (with co)\n\n**-k**_sub_\n> Set keyword substitution mode (e.g., -kv, -kvl, -ko)"],["DESCRIPTION","**RCS** (Revision Control System) manages multiple revisions of files, providing version control for individual files. It automates storing, retrieving, logging, and merging revisions, making it useful for source code, documentation, and configuration files.\n\nThe workflow centers on **ci** (check-in) and **co** (check-out). Check-in stores a new revision and removes the working file by default. Check-out retrieves a revision; use **-l** to lock the file for exclusive editing.\n\nRCS stores revisions efficiently using reverse deltas—the latest revision is stored complete, while older revisions are stored as differences. This makes retrieving the current version fast.\n\nRevisions are numbered hierarchically (1.1, 1.2, 1.3, etc.). The first number is the release, the second is the level. Branches create additional number fields (1.2.1.1)."],["CAVEATS","RCS operates on individual files, not directories or projects. For multi-file version control, modern tools like Git are more appropriate.\n\nThe default check-in behavior removes the working file. Use **ci -u** to keep an unlocked copy or **ci -l** to keep a locked copy for continued editing.\n\nRCS files (ending in **,v**) are stored in the same directory or in an **RCS/** subdirectory. Ensure proper permissions for collaborative use."],["HISTORY","RCS was developed by **Walter F. Tichy** at Purdue University, first released in **1982**. It was one of the earliest version control systems and influenced later tools like CVS (which added network support) and eventually Git. RCS is part of the GNU project and continues to be available on most Unix-like systems."],["INSTALL","```apt: sudo apt install rcs```\n\n```dnf: sudo dnf install rcs```\n\n```pacman: sudo pacman -S rcs```\n\n```apk: sudo apk add rcs```\n\n```zypper: sudo zypper install rcs```\n\n```brew: brew install rcs```\n\n```nix: nix profile install nixpkgs#rcs```"],["SEE ALSO","[cvs](/man/cvs)(1), [git](/man/git)(1), [svn](/man/svn)(1), [diff](/man/diff)(1)"]],"e":["cvs","git","svn","diff"],"x":"ci [filename]"},"regedit":{"n":"regedit","t":"Windows registry editor via Wine","s":[["TAGLINE","Windows registry editor via Wine"],["TLDR","**Open the graphical registry editor**\n\n```regedit```\n\n**Import a registry file**\n\n```regedit [file.reg]```\n\n**Silently import a registry file** (no confirmation dialog)\n\n```regedit /S [file.reg]```\n\n**Export a registry key** to a file\n\n```regedit /E [output.reg] [HKEY_CURRENT_USER\\Software\\App]```\n\n**Delete a registry key** (Wine-specific)\n\n```regedit /D [HKEY_CURRENT_USER\\Software\\App]```"],["SYNOPSIS","**regedit** [_/S_] [_file.reg_]\n**regedit** **/E** _file_ _key_\n**regedit** **/D** _key_"],["PARAMETERS","**/E** _FILE_ _KEY_\n> Export the specified registry key and all subkeys to a .reg file.\n\n**/S**\n> Silent mode; suppress confirmation dialogs when importing .reg files.\n\n**/D** _KEY_\n> Delete registry key and all its subkeys (Wine extension, not in Windows regedit).\n\n**/C** _FILE_\n> Create a new registry from the specified .reg file (replaces entire registry).\n\n**/V**\n> Open in advanced mode."],["DESCRIPTION","**regedit** is the Windows Registry Editor, available on Linux through the Wine compatibility layer. It provides a graphical interface for browsing, searching, and modifying the hierarchical Windows registry database that stores configuration settings for Windows applications running under Wine.\n\nThe tool supports importing and exporting registry files in the standard .reg format, enabling batch configuration changes and backup of registry sections. The **/S** flag allows silent imports for scripting, while **/E** exports specific registry keys and their subtrees. These operations are essential for configuring Windows applications that rely on registry settings to function correctly under Wine.\n\nOn Linux, regedit manages Wine's emulated registry stored in the user's Wine prefix directory (typically **~/.wine**). The registry is split into files: **system.reg** (HKEY_LOCAL_MACHINE), **user.reg** (HKEY_CURRENT_USER), and **userdef.reg** (HKEY_USERS\\\\.Default). Changes made through regedit affect how Windows applications behave within the Wine environment."],["CAVEATS","Requires Wine to be installed on Linux. The graphical interface requires a display server (X11 or Wayland via XWayland). The **/D** flag for deleting keys is a Wine extension not available in Windows regedit. Use **/C** with extreme caution as it replaces the entire registry. For scripting, the Wine-specific **wine reg** command may be more appropriate."],["HISTORY","**regedit** is Microsoft's registry editor, included in Windows since Windows 3.1. Wine provides a compatible implementation that operates on Wine's own registry files, enabling configuration of Windows applications running on Linux and macOS."],["INSTALL","```dnf: sudo dnf install wine```\n\n```pacman: sudo pacman -S wine```\n\n```apk: sudo apk add wine```\n\n```zypper: sudo zypper install wine```\n\n```nix: nix profile install nixpkgs#wine```"],["SEE ALSO","[wine](/man/wine)(1), [winecfg](/man/winecfg)(1), [winetricks](/man/winetricks)(1)"]],"e":["wine","winecfg","winetricks"],"x":"regedit"},"rename":{"n":"rename","t":"Rename multiple files","s":[["TAGLINE","Rename multiple files"],["TLDR","**rename** can refer to multiple commands with the same name\n\nView documentation for the **Perl version**\n\n```tldr perl-rename```\n\nView documentation for the **util-linux version**\n\n```tldr rename.util```"],["SYNOPSIS","**rename** [_options_] _expression_ _replacement_ _files_..."],["DESCRIPTION","**rename** is a command for batch renaming files. Two major implementations exist:\n\nThe **Perl rename** (also called prename or rename.ul) uses Perl regular expressions for powerful pattern matching. It is common on Debian-based systems.\n\nThe **util-linux rename** uses simple string substitution. It replaces the first occurrence of a string in each filename. Common on Red Hat-based systems.\n\nCheck which version is installed with **rename --version** or **file $(which rename)**."],["INSTALL","```apk: sudo apk add util-linux-misc```\n\n```brew: brew install rename```\n\n```nix: nix profile install nixpkgs#rename```"],["SEE ALSO","[perl-rename](/man/perl-rename)(1), [rename.util](/man/rename.util)(1), [mv](/man/mv)(1), [mmv](/man/mmv)(1)"]],"e":["perl-rename","rename.util","mv","mmv"],"x":"tldr perl-rename"},"roll":{"n":"roll","t":"Simulate dice rolls with RPG notation","s":[["TAGLINE","Simulate dice rolls with RPG notation"],["TLDR","**Roll dice**\n\n```roll [2d6]```\n\n**Roll with modifier**\n\n```roll [1d20+5]```\n\n**Roll multiple times**\n\n```roll [3x4d6]```\n\n**Verbose output showing each die**\n\n```roll -v [2d6]```\n\n**Sum only output**\n\n```roll -s [2d6]```\n\n**Roll percentile**\n\n```roll [1d100]```\n\n**Roll with negative modifier**\n\n```roll [1d20-2]```\n\n**Multiple dice expressions**\n\n```roll [1d20] [2d6+3]```"],["SYNOPSIS","**roll** [_-v_] _dice-expression_ ..."],["PARAMETERS","**-v**\n> Verbose, show each die.\n\n**-s**\n> Sum only output.\n\n_NdS_\n> N dice with S sides.\n\n_NdS+M_\n> Add modifier M.\n\n_TxNdS_\n> Roll T times."],["DESCRIPTION","**roll** is a command-line dice roller that parses standard RPG dice notation in the **NdS** format, where N is the number of dice and S is the number of sides. It supports modifiers (**+** or **-**) for adding or subtracting fixed values, and the **TxNdS** format for rolling the same expression multiple times.\n\nMultiple dice expressions can be specified on a single command line. The **-v** flag shows individual die results in addition to the total sum, while **-s** outputs only the final sum. Common RPG dice like d4, d6, d8, d10, d12, d20, and d100 (percentile) are all supported through the flexible notation."],["CAVEATS","Random source varies. Not cryptographically secure. Fun utility only."],["HISTORY","**roll** is a simple command-line dice roller implementing standard RPG dice notation for tabletop gaming."],["INSTALL","```brew: brew install roll```"],["SEE ALSO","[rolldice](/man/rolldice)(6), [shuf](/man/shuf)(1)"]],"e":["rolldice","shuf"],"x":"roll [2d6]"},"rtmpdump":{"n":"rtmpdump","t":"Download RTMP streaming media","s":[["TAGLINE","Download RTMP streaming media"],["TLDR","**Download RTMP stream** to a file\n\n```rtmpdump -r \"[rtmp://server/path]\" -o [output.flv]```\n\n**Download with separate playpath**\n\n```rtmpdump -r \"[rtmp://server/app]\" -y \"[playpath]\" -o [output.flv]```\n\n**Record a live stream**\n\n```rtmpdump -r \"[rtmp://server/live]\" -v -o [output.flv]```\n\n**Download with SWF verification**\n\n```rtmpdump -r \"[rtmp://server/path]\" -W \"[http://example.com/player.swf]\" -o [output.flv]```\n\n**Resume an interrupted download**\n\n```rtmpdump -r \"[rtmp://server/path]\" -o [output.flv] --resume```\n\n**Download with progress indicators**\n\n```rtmpdump -r \"[rtmp://server/path]\" -o [output.flv] --hashes```"],["SYNOPSIS","**rtmpdump** **-r** _url_ [**-o** _file_] [_options_]"],["PARAMETERS","**-r** _URL_, **--rtmp** _URL_\n> RTMP URL of the stream to download.\n\n**-o** _FILE_, **--flv** _FILE_\n> Output file name. If not specified, streams to stdout.\n\n**-y** _PATH_, **--playpath** _PATH_\n> Override the playpath parsed from the RTMP URL.\n\n**-a** _APP_, **--app** _APP_\n> Name of application to connect to on the RTMP server.\n\n**-v**, **--live**\n> Specify that the media is a live stream. No resuming or seeking is possible.\n\n**-W** _URL_, **--swfVfy** _URL_\n> URL of the SWF player. Hash and size are computed automatically.\n\n**-s** _URL_, **--swfUrl** _URL_\n> URL of the SWF player for the media. Required by some servers for verification.\n\n**--resume**\n> Resume an incomplete download.\n\n**-T** _TOKEN_, **--token** _TOKEN_\n> Key for SecureToken response.\n\n**--timeout** _SECS_\n> Timeout the session after SECS seconds without receiving data.\n\n**-n** _HOST_, **--host** _HOST_\n> Override the hostname in the RTMP URL.\n\n**-c** _PORT_, **--port** _PORT_\n> Override the port number in the RTMP URL.\n\n**--hashes**\n> Display a hash mark for each 1% of progress.\n\n**-q**, **--quiet**\n> Suppress all command output.\n\n**-V**, **--verbose**\n> Verbose output.\n\n**-z**, **--debug**\n> Debug level output with hex dumps of all packet data."],["DESCRIPTION","**rtmpdump** downloads media streams using the Real-Time Messaging Protocol (RTMP), which was originally developed by Adobe for Flash-based streaming. It connects to RTMP servers and saves the stream data to a local file, supporting both live broadcasts and on-demand video content.\n\nThe tool handles the full RTMP handshake and protocol negotiation, including SWF (Shockwave Flash) verification when servers require it. RTMP URLs consist of a server address, application path, and playpath, which may need to be specified separately. For live streams, the **-v** flag enables live mode where recording continues until the stream ends or the user interrupts.\n\nInterrupted downloads can be resumed with the **--resume** flag, which is particularly useful for long recordings over unreliable connections. The tool outputs FLV (Flash Video) format by default, which can be converted to other formats using tools like ffmpeg."],["CAVEATS","RTMP is declining in use as modern streaming protocols like HLS and DASH have replaced it. Some servers require SWF verification or token authentication. Output is always in FLV format."],["HISTORY","**rtmpdump** was created as an open-source RTMP client. It enabled downloading Adobe Flash video streams before modern streaming protocols became common."],["INSTALL","```apt: sudo apt install rtmpdump```\n\n```dnf: sudo dnf install rtmpdump```\n\n```pacman: sudo pacman -S rtmpdump```\n\n```apk: sudo apk add rtmpdump```\n\n```brew: brew install rtmpdump```\n\n```nix: nix profile install nixpkgs#rtmpdump```"],["SEE ALSO","[ffmpeg](/man/ffmpeg)(1), [streamlink](/man/streamlink)(1), [youtube-dl](/man/youtube-dl)(1), [curl](/man/curl)(1)"]],"e":["ffmpeg","streamlink","youtube-dl","curl"],"x":"rtmpdump -r \"[rtmp://server/path]\" -o [output.flv]"},"rustup-check":{"n":"rustup-check","t":"Check for Rust toolchain updates","s":[["TAGLINE","Check for Rust toolchain updates"],["TLDR","**Check** for available updates to installed toolchains and rustup itself\n\n```rustup check```"],["SYNOPSIS","**rustup** **check**"],["DESCRIPTION","**rustup check** queries the official Rust repositories and displays a comparison of installed and available versions for all Rust toolchains managed by rustup. It shows whether updates are available for each installed toolchain (stable, beta, nightly) and for rustup itself, without downloading or installing anything. This makes it useful for quickly determining if a **rustup update** is needed.\n\nThe command requires an internet connection to check for the latest versions."],["INSTALL","```apt: sudo apt install rustup```\n\n```dnf: sudo dnf install rustup```\n\n```pacman: sudo pacman -S rustup```\n\n```zypper: sudo zypper install rustup```\n\n```brew: brew install rustup```\n\n```nix: nix profile install nixpkgs#rustup```"],["SEE ALSO","[rustup](/man/rustup)(1), [rustup-update](/man/rustup-update)(1), [rustup-show](/man/rustup-show)(1), [rustup-toolchain](/man/rustup-toolchain)(1), [rustc](/man/rustc)(1), [cargo](/man/cargo)(1)"]],"e":["rustup","rustup-update","rustup-show","rustup-toolchain","rustc","cargo"],"x":"rustup check"},"saws":{"n":"saws","t":"Supercharged interactive AWS CLI with fuzzy completion","s":[["TAGLINE","Supercharged interactive AWS CLI with fuzzy completion"],["TLDR","**Start** the SAWS shell\n\n```saws```\n\n**Then run AWS commands** with completion (inside SAWS)\n\n```aws [ec2] [describe-instances]```\n\n**List S3** with bucket completion\n\n```aws s3 ls s3://```"],["SYNOPSIS","**saws**"],["DESCRIPTION","**saws** is a productivity layer around the official AWS CLI. You launch an interactive session that supports the same **aws** *command* *subcommand* structure, but adds fuzzy auto-completion of commands, subcommands, options, and live AWS resources (S3 buckets, EC2 instance ids/tags, and more), plus syntax highlighting, fish-style suggestions, shortcuts, history, and contextual help.\n\nIt requires a working AWS CLI installation and standard AWS credentials / profiles. Configuration and theme live in **~/.sawsrc** and related files."],["PARAMETERS","**saws** is primarily interactive. Once running, type normal AWS CLI commands:\n\n**aws** *service* *operation* [*options*]\n\n> Same surface as **aws**, with enhanced completion.\n\nShortcuts and toolbar options are documented upstream. **F-keys** / toolbar toggles control features such as fuzzy matching and docs shortcuts (see project README)."],["CAVEATS","Project activity may lag current AWS CLI feature velocity—verify compatibility with your AWS CLI v1/v2 install. Resource completion needs network permission to enumerate account resources. Not a separate API client: failures often originate from **aws** itself or IAM permissions."],["SEE ALSO","[aws](/man/aws)(1), [aws-shell](/man/aws-shell)(1)"],["RESOURCES","```[Source code](https://github.com/donnemartin/saws)```\n\n```[Documentation](https://saws.readthedocs.org/)```"]],"e":["aws","aws-shell"],"x":"saws"},"sfdk-qmltypes":{"n":"sfdk-qmltypes","t":"Generate QML type information","s":[["TAGLINE","Generate QML type information"],["TLDR","**Generate QML type information**\n\n```sfdk qmltypes```"],["SYNOPSIS","**sfdk qmltypes** [_options_]"],["DESCRIPTION","**sfdk qmltypes** generates QML type information for IDE integration and type checking. Part of Sailfish SDK."],["SEE ALSO","[sfdk](/man/sfdk)(1), [sfdk-qmake](/man/sfdk-qmake)(1)"]],"e":["sfdk","sfdk-qmake"],"x":"sfdk qmltypes"},"snipt":{"n":"snipt","t":"Text snippet expansion tool for your terminal","s":[["TAGLINE","Text snippet expansion tool for your terminal"],["TLDR","**Start the background daemon**\n\n```snipt start```\n\n**Add a new snippet**\n\n```snipt add --shortcut [email] --snippet \"[user@example.com]\"```\n\n**Display all saved snippets**\n\n```snipt list```"],["SYNOPSIS","**snipt** _command_ [_options_]"],["DESCRIPTION","**snipt** is a system-wide text snippet expansion tool that runs as a background daemon monitoring keyboard input. You type a prefix character followed by a shortcut and snipt automatically expands it into predefined text. It supports clipboard integration, script execution with the **!** prefix, parameterized snippets, and text transformations."],["HISTORY","**snipt** was created by the **snipt** organization and is written in **Rust**."],["INSTALL","```nix: nix profile install nixpkgs#snipt```"],["SEE ALSO","[espanso](/man/espanso)(1), [pet](/man/pet)(1), [nap](/man/nap)(1)"]],"e":["espanso","pet","nap"],"x":"snipt start"},"sshs":{"n":"sshs","t":"Interactive TUI for browsing and connecting to SSH hosts","s":[["TAGLINE","Interactive TUI for browsing and connecting to SSH hosts"],["TLDR","**Launch the interactive SSH host selector**\n\n```sshs```\n\n**Use a custom SSH config file**\n\n```sshs --config [~/.ssh/custom_config]```"],["SYNOPSIS","**sshs** [**--config** _path_]"],["DESCRIPTION","**sshs** is a terminal user interface for SSH that reads your ~/.ssh/config file to list and connect to hosts. It provides a fuzzy-searchable interactive interface to quickly select and connect to configured SSH destinations."],["HISTORY","**sshs** was created by **quantumsheep** and is written in **Rust**."],["INSTALL","```pacman: sudo pacman -S sshs```\n\n```brew: brew install sshs```\n\n```nix: nix profile install nixpkgs#sshs```"],["SEE ALSO","[ssh](/man/ssh)(1), [sshclick](/man/sshclick)(1), [ssh-list](/man/ssh-list)(1)"]],"e":["ssh","sshclick","ssh-list"],"x":"sshs"},"symfony":{"n":"symfony","t":"Symfony PHP framework development CLI","s":[["TAGLINE","Symfony PHP framework development CLI"],["TLDR","**Create new project**\n\n```symfony new [project_name]```\n\n**Create web app**\n\n```symfony new [project_name] --webapp```\n\n**Start development server**\n\n```symfony server:start```\n\n**Stop server**\n\n```symfony server:stop```\n\n**Show server status**\n\n```symfony server:status```\n\n**Open in browser**\n\n```symfony open:local```\n\n**Run console command**\n\n```symfony console [command]```\n\n**Check security**\n\n```symfony check:security```"],["SYNOPSIS","**symfony** _command_ [_options_] [_args_]"],["PARAMETERS","**new** _NAME_\n> Create project.\n\n**server:start**\n> Start local server.\n\n**server:stop**\n> Stop server.\n\n**server:status**\n> Show status of the local server.\n\n**server:log**\n> Show logs from the local web server and PHP-FPM.\n\n**console** _CMD_\n> Run Symfony console (wrapped bin/console).\n\n**open:local**\n> Open local project URL in browser.\n\n**check:security**\n> Check for known vulnerabilities in composer.lock dependencies.\n\n**local:php:list**\n> List installed PHP versions.\n\n**--webapp**\n> Scaffold full web app template (Twig, Doctrine, security, etc.).\n\n**--version** _VER_\n> Install a specific Symfony version (e.g. lts, 6.4, 7.1).\n\n**-d**, **--daemon**\n> Run the local server in the background.\n\n**--allow-http**\n> Disable HTTPS on the local server."],["DESCRIPTION","**symfony** is the official command-line tool for managing Symfony PHP framework projects. It provides project scaffolding, a built-in local development server with PHP support, and wrappers for common Symfony console commands.\n\nProject creation offers templates ranging from minimal microservice skeletons to full web applications with pre-configured bundles. The local development server includes automatic HTTPS via locally-trusted certificates, PHP version management, and Docker service integration for databases and other dependencies.\n\nThe console wrapper executes Symfony console commands (cache clearing, database migrations, route listing) with automatic environment detection. Security checking scans project dependencies against known vulnerability databases and recommends updates."],["CAVEATS","Requires PHP. Full features need Docker. Symfony account for cloud features."],["HISTORY","The **Symfony CLI** was created by **SensioLabs** for Symfony framework development. It consolidates project management and local development."],["SEE ALSO","[php](/man/php)(1), [composer](/man/composer)(1)"]],"e":["php","composer"],"x":"symfony new [project_name]"},"terraform":{"n":"terraform","t":"Infrastructure as code provisioning tool","s":[["TAGLINE","Infrastructure as code provisioning tool"],["TLDR","**Initialize a Terraform working directory**\n\n```terraform init```\n\n**Preview infrastructure changes**\n\n```terraform plan```\n\n**Apply infrastructure changes**\n\n```terraform apply```\n\n**Destroy managed infrastructure**\n\n```terraform destroy```\n\n**Format configuration files**\n\n```terraform fmt```\n\n**Validate configuration syntax**\n\n```terraform validate```\n\n**Show current state**\n\n```terraform show```\n\n**List resources in state**\n\n```terraform state list```\n\n**Import existing infrastructure**\n\n```terraform import [resource_type.name] [id]```"],["SYNOPSIS","**terraform** _command_ [_options_] [_args_]"],["PARAMETERS","**init**\n> Initialize working directory, download providers\n\n**plan**\n> Create execution plan showing proposed changes\n\n**apply**\n> Apply changes to reach desired state\n\n**destroy**\n> Destroy all managed infrastructure\n\n**fmt**\n> Format configuration files to canonical style\n\n**validate**\n> Check configuration syntax and consistency\n\n**show**\n> Display current state or saved plan\n\n**output**\n> Read output values from state\n\n**state**\n> Advanced state management commands\n\n**import**\n> Import existing infrastructure into state\n\n**workspace**\n> Manage workspaces\n\n**-auto-approve**\n> Skip interactive approval (apply/destroy)\n\n**-var** _'key=value'_\n> Set input variable\n\n**-var-file** _file_\n> Load variables from file\n\n**-target** _resource_\n> Target specific resource\n\n**-parallelism** _n_\n> Limit concurrent operations"],["DESCRIPTION","**terraform** is an infrastructure as code tool for building, changing, and versioning cloud infrastructure safely and efficiently. It uses declarative configuration files to describe desired infrastructure state.\n\nConfiguration is written in HCL (HashiCorp Configuration Language) defining resources like servers, networks, and databases. Terraform communicates with cloud providers (AWS, Azure, GCP, etc.) via plugins called providers.\n\nThe workflow is: **init** (setup), **plan** (preview), **apply** (execute). State is tracked in **terraform.tfstate**, enabling Terraform to know what exists and what needs changing.\n\nWorkspaces allow managing multiple environments (dev, staging, prod) from the same configuration with different state files."],["CONFIGURATION","**terraform.tfvars**\n> Default variable values file, automatically loaded when present in the working directory\n\n**\\*.auto.tfvars**\n> Additional variable files automatically loaded alphabetically\n\n**~/.terraformrc** or **%APPDATA%/terraform.rc**\n> CLI configuration file for provider installation methods, credentials, and plugin cache\n\n**TF_VAR_name**\n> Environment variables prefixed with TF_VAR_ set input variable values\n\n**TF_CLI_CONFIG_FILE**\n> Override the default CLI configuration file location"],["CAVEATS","State files contain sensitive information (passwords, keys) and should be stored securely. Use remote backends (S3, Terraform Cloud) for team collaboration and state locking.\n\n**terraform destroy** is irreversible. Always run **plan** first and verify the destruction plan carefully.\n\nProvider versions should be pinned in configuration to ensure reproducibility. Unexpected provider updates can cause issues.\n\nChanges made outside Terraform (manual console changes) cause state drift. Use **terraform refresh** or **import** to reconcile."],["HISTORY","Terraform was created by **HashiCorp** and first released in **2014** by Mitchell Hashimoto. It pioneered the \"infrastructure as code\" approach for multi-cloud provisioning and became the de facto standard for declarative infrastructure management."],["INSTALL","```pacman: sudo pacman -S terraform```\n\n```nix: nix profile install nixpkgs#terraform```"],["SEE ALSO","[aws](/man/aws)(1), [ansible](/man/ansible)(1), [pulumi](/man/pulumi)(1), [tofu](/man/tofu)(1)"]],"e":["aws","ansible","pulumi","tofu"],"x":"terraform init"},"tmt-try":{"n":"tmt-try","t":"Interactive tmt test experimentation","s":[["TAGLINE","Interactive tmt test experimentation"],["TLDR","**Experiment** with default provision\n\n```tmt try```\n\nRun test in **current** directory\n\n```cd [path/to/test_directory] && tmt try```\n\nTry against a specific **image**\n\n```tmt try [fedora]```\n\nSelect image and **provision method**\n\n```tmt try [fedora@container]```\n\nRun only tests matching a **regexp**\n\n```tmt try -t [feature]```\n\nJust provision and **ask** what to do next\n\n```tmt try -a```\n\n**Log in** to the guest without running tests\n\n```tmt try -l```\n\nConnect to a **running guest** by IP or FQDN\n\n```tmt try [192.168.12.23@connect]```"],["SYNOPSIS","**tmt try** [_OPTIONS_] [_IMAGE_[@_HOW_]]..."],["PARAMETERS","**-t**, **--test** _REGEXP_\n> Run tests matching the given regular expression. By default all tests under the current working directory are executed. Can be given multiple times.\n\n**-p**, **--plan** _REGEXP_\n> Use the matching plan. By default a `/user/plan*` plan from the user config is used, otherwise a default plan.\n\n**-l**, **--login**\n> Log into the guest only, do not run any tests.\n\n**-a**, **--ask**\n> Just provision the guest and ask what to do next.\n\n**--epel**\n> Enable the EPEL repository on the guest.\n\n**--fips**\n> Enable FIPS mode on the guest.\n\n**--install** _PACKAGE_\n> Install the given package on the guest. Can be given multiple times.\n\n**--arch** _ARCH_\n> Specify the guest CPU architecture.\n\n**--help**\n> Display help information."],["DESCRIPTION","**tmt try** provides an interactive session to run tests or experiment with guests. It provisions a guest, runs the tests found in the current working directory, and then presents a menu of what to do next. If no tests are detected, it logs into the guest so you can start experimenting.\n\nSpecify the guest with an image name, for example `tmt try fedora`. Append `@HOW` to choose the provision method per guest, such as `fedora@container` or `centos-stream-9@virtual`. Use `@connect` with an FQDN or IP address to attach to an already running guest, for example `192.168.12.23@connect`."],["CAVEATS","If no tests are found in the current directory, **tmt try** simply logs into the guest. Image availability depends on the provisioning backend. Interactive mode requires terminal access, and some images may require authentication or special configuration."],["INSTALL","```dnf: sudo dnf install tmt```\n\n```brew: brew install tmt```"],["SEE ALSO","[tmt](/man/tmt)(1), [tmt-run](/man/tmt-run)(1), [podman](/man/podman)(1)"]],"e":["tmt","tmt-run","podman"],"x":"tmt try"},"uxterm":{"n":"uxterm","t":"Launch xterm with Unicode UTF-8 support","s":[["TAGLINE","Launch xterm with Unicode UTF-8 support"],["TLDR","**Start Unicode terminal**\n\n```uxterm```\n\n**Start with specific geometry**\n\n```uxterm -geometry [80x24]```\n\n**Start with specific font (ISO 10646-1 encoded)**\n\n```uxterm -fn [fixed]```\n\n**Start with custom title**\n\n```uxterm -T \"[My Terminal]\"```\n\n**Start and run command**\n\n```uxterm -e [bash]```"],["SYNOPSIS","**uxterm** [_xterm-options_]"],["PARAMETERS","All xterm options are passed through. Common options include:\n\n**-geometry** _WxH+X+Y_\n> Set window size and position.\n\n**-fn** _font_\n> Set terminal font.\n\n**-fb** _font_\n> Set bold font.\n\n**-fg** _color_\n> Set foreground color.\n\n**-bg** _color_\n> Set background color.\n\n**-T** _title_\n> Set window title.\n\n**-e** _command_\n> Run command in terminal.\n\n**-hold**\n> Keep window open after command exits."],["DESCRIPTION","**uxterm** is a wrapper script that launches xterm with Unicode (UTF-8) support enabled. It sets the UXTerm resource class and enables UTF-8 mode, making it suitable for displaying international characters and symbols.\n\nThe wrapper uses the system locale to determine character encoding. If no locale is set, it defaults to en_US. The locale utility verifies system support for the selected locale before starting.\n\nAll command-line arguments pass directly to xterm. The **-class** and **-u8** options should not be specified as they are set by the wrapper. Font configuration uses ISO 10646-1 encoded fonts."],["CAVEATS","Requires locale with UTF-8 support. Needs appropriate Unicode fonts installed (xfonts-base on Debian). May produce unexpected results if the current locale does not support UTF-8 character encoding. X11 only."],["HISTORY","**uxterm** was created as part of xterm to simplify running the terminal with proper Unicode support. It emerged as Unicode became standard and users needed reliable UTF-8 display without manually configuring xterm's complex options."],["INSTALL","```apt: sudo apt install xterm```\n\n```dnf: sudo dnf install xterm```\n\n```pacman: sudo pacman -S xterm```\n\n```apk: sudo apk add xterm```\n\n```zypper: sudo zypper install xterm```\n\n```brew: brew install xterm```\n\n```nix: nix profile install nixpkgs#xterm```"],["SEE ALSO","[xterm](/man/xterm)(1), [locale](/man/locale)(1)"]],"e":["xterm","locale"],"x":"uxterm"},"vagrant-init":{"n":"vagrant-init","t":"Create a new Vagrantfile configuration","s":[["TAGLINE","Create a new Vagrantfile configuration"],["TLDR","**Initialize with default box**\n\n```vagrant init```\n\n**Initialize with specific box**\n\n```vagrant init [box_name]```\n\n**Initialize with minimal Vagrantfile**\n\n```vagrant init -m [box_name]```\n\n**Initialize with box URL**\n\n```vagrant init [name] [url]```"],["SYNOPSIS","**vagrant** **init** [_options_] [_name_] [_url_]"],["PARAMETERS","**-m**, **--minimal**\n> Create minimal Vagrantfile.\n\n**-f**, **--force**\n> Overwrite existing Vagrantfile.\n\n**--box-version** _version_\n> Specify box version.\n\n**-o**, **--output** _file_\n> Output file path."],["DESCRIPTION","**vagrant init** creates a new Vagrantfile in the current directory. The Vagrantfile defines VM configuration including box, networking, and provisioning. Start of every Vagrant project."],["INSTALL","```apt: sudo apt install vagrant```\n\n```dnf: sudo dnf install vagrant```\n\n```nix: nix profile install nixpkgs#vagrant```"],["SEE ALSO","[vagrant](/man/vagrant)(1), [vagrant-box](/man/vagrant-box)(1)"]],"e":["vagrant","vagrant-box"],"x":"vagrant init"},"varnishhist":{"n":"varnishhist","t":"Live Varnish request latency histogram","s":[["TAGLINE","Live Varnish request latency histogram"],["TLDR","**Display request histogram**\n\n```varnishhist```\n\n**Connect to specific instance**\n\n```varnishhist -n [instance_name]```\n\n**Read from log file**\n\n```varnishhist -r [/var/log/varnish.log]```\n\n**Set update delay**\n\n```varnishhist -d [2]```\n\n**Filter by pattern**\n\n```varnishhist -q \"[RespStatus == 200]\"```\n\n**Exclude pattern**\n\n```varnishhist -x \"[BogoHeader]\"```"],["SYNOPSIS","**varnishhist** [_options_]"],["PARAMETERS","**-n** _name_\n> Specify varnishd instance name.\n\n**-r** _file_\n> Read log from file instead of shared memory.\n\n**-d** _delay_\n> Delay between updates in seconds (default: 1).\n\n**-q** _query_\n> Filter log entries with VSL query.\n\n**-x** _regex_\n> Exclude entries matching regular expression.\n\n**-T** _seconds_\n> Transaction timeout (default: 120).\n\n**-h**\n> Display help.\n\n**-V**\n> Display version."],["DISPLAY","The histogram shows distribution of request processing times. The horizontal axis uses a logarithmic scale. The vertical scale and number of requests (N) appear in the top left corner.\n\n**|**: Cache hit.\n\n**#**: Cache miss."],["DESCRIPTION","**varnishhist** reads Varnish shared memory logs and displays a live histogram of request processing times. It visualizes the distribution of cache hits and misses, helping identify performance patterns.\n\nThe display updates continuously, showing how request latencies distribute across logarithmic time buckets. Cache hits (|) and misses (#) use different characters, making it easy to compare their performance characteristics.\n\nUseful for real-time monitoring of Varnish cache performance and identifying latency issues. Filter options allow focusing on specific request types or excluding noise."],["CAVEATS","Requires access to Varnish shared memory (typically needs appropriate group membership). Large log volumes may affect system resources. Historical analysis requires logged data."],["HISTORY","**varnishhist** was developed by Poul-Henning Kamp as part of the Varnish Cache project. Varnish was created for Verdens Gang (VG.no) in 2006 and has become one of the most popular HTTP accelerators, with varnishhist providing essential visual performance monitoring."],["INSTALL","```apt: sudo apt install varnish```\n\n```dnf: sudo dnf install varnish```\n\n```apk: sudo apk add varnish```\n\n```brew: brew install varnish```\n\n```nix: nix profile install nixpkgs#varnish```"],["SEE ALSO","[varnishd](/man/varnishd)(1), [varnishstat](/man/varnishstat)(1), [varnishlog](/man/varnishlog)(1)"]],"e":["varnishd","varnishstat","varnishlog"],"x":"varnishhist"},"vd":{"n":"vd","t":"Terminal-based data exploration spreadsheet","s":[["TAGLINE","Terminal-based data exploration spreadsheet"],["TLDR","**Open a CSV file**\n\n```vd [file.csv]```\n\n**Open a JSON file**\n\n```vd [file.json]```\n\n**Open multiple files**\n\n```vd [file1.csv] [file2.xlsx]```\n\n**Open from URL**\n\n```vd [https://example.com/data.csv]```\n\n**Pipe data from stdin**\n\n```cat [file.csv] | vd```\n\n**Open SQLite database**\n\n```vd [database.db]```\n\n**Specify filetype explicitly**\n\n```vd -f json [file.txt]```\n\n**Open with read-only mode**\n\n```vd --readonly [file.csv]```"],["SYNOPSIS","**vd** [_options_] [_file_...]"],["PARAMETERS","**-f** _filetype_\n> Force input file type.\n\n**-p** _cmdlog_\n> Replay a saved command log file within the interface.\n\n**-b**, **--batch**\n> Run in batch mode (no interface): replay cmdlog and exit.\n\n**-o** _file_\n> Save the final sheet to file on exit (used with -b).\n\n**--readonly**\n> Disable saving/modifying.\n\n**-d** _delimiter_\n> Field delimiter for text files.\n\n**-c** _cmdlog_\n> Execute cmdlog file.\n\n**--diff** _file_\n> Show diff between sources.\n\n**-h**, **--help**\n> Display help information.\n\n**-v**, **--version**\n> Display version information."],["KEYBOARD COMMANDS","**h**/**j**/**k**/**l** or arrows\n> Navigate cells.\n\n**gg**/**G**\n> Go to first/last row.\n\n**gh**/**gl**\n> Go to first/last column.\n\n**/**\n> Search in current column.\n\n**|**\n> Select rows matching regex.\n\n**,**\n> Select rows where current column equals current cell.\n\n**s**/**t**/**u**\n> Select/toggle/unselect current row.\n\n**gs**/**gt**/**gu**\n> Select/toggle/unselect all rows.\n\n**[**/**]**\n> Sort by current column ascending/descending.\n\n**Enter**\n> Open row as a new sheet (dive into nested data).\n\n**F**\n> Frequency/pivot table for current column.\n\n**I**\n> Describe sheet (statistics for all columns).\n\n**+**\n> Add aggregator to column.\n\n**S**\n> Open the Sheets sheet to switch between open sheets.\n\n**Ctrl+s**\n> Save current sheet.\n\n**q**\n> Quit current sheet."],["DESCRIPTION","**vd** (VisiData) is a terminal-based spreadsheet and data exploration tool. It opens tabular data from many formats including CSV, JSON, Excel, SQLite, and remote URLs.\n\nNavigation uses vim-style keys. Sheets stack like buffers; **q** closes current sheet, returning to previous. The sheets sheet (**S**) shows all open sheets.\n\nData analysis features include frequency tables (**F**), column statistics (**I**), and aggregations (**+**). Group rows with **g** prefix commands. Create pivot tables and perform joins between sheets.\n\nSelection (**s**/**t**/**u**) marks rows for operations. Use **|** to select by regex or **,** for exact match. **\"** opens selected rows as new sheet.\n\nChanges can be saved back to files with **Ctrl+s**. Command logs enable reproducible transformations and batch processing with **-b**."],["CAVEATS","Large files may use significant memory. Some formats require additional Python packages. Editing support varies by file type. Complex operations have learning curve."],["HISTORY","VisiData was created by **Saul Pwanson** and first released in **2016**. It was designed as a universal tool for exploring tabular data in the terminal, inspired by sc but supporting many formats. The project emphasizes keyboard efficiency and exploratory data analysis. VisiData has grown to support dozens of data formats and is widely used for data journalism and analysis workflows."],["SEE ALSO","[sc-im](/man/sc-im)(1), [csvkit](/man/csvkit)(1), [miller](/man/miller)(1), [xsv](/man/xsv)(1)"]],"e":["sc-im","csvkit","miller","xsv"],"x":"vd [file.csv]"},"xbmtopbm":{"n":"xbmtopbm","t":"Convert X11 or X10 bitmap to PBM format","s":[["TAGLINE","Convert X11 or X10 bitmap to PBM format"],["TLDR","**Convert XBM to PBM**\n\n```xbmtopbm [input.xbm] > [output.pbm]```"],["SYNOPSIS","**xbmtopbm** [_file_]"],["DESCRIPTION","**xbmtopbm** reads an X11 or X10 bitmap (.xbm) as input and produces a PBM image as output. X bitmap files are C source code that define image data as arrays; this tool extracts and converts the image data. Part of the Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pbmtoxbm](/man/pbmtoxbm)(1), [xpmtoppm](/man/xpmtoppm)(1)"]],"e":["pbmtoxbm","xpmtoppm"],"x":"xbmtopbm [input.xbm] > [output.pbm]"},"zipnote":{"n":"zipnote","t":"Manage ZIP file comments","s":[["TAGLINE","Manage ZIP file comments"],["TLDR","**Show file comments**\n\n```zipnote [archive.zip]```\n\n**Edit comments**\n\n```zipnote [archive.zip] > [notes.txt] && vim [notes.txt] && zipnote -w [archive.zip] < [notes.txt]```\n\n**Write comments from file**\n\n```zipnote -w [archive.zip] < [notes.txt]```"],["SYNOPSIS","**zipnote** [_-w_] [_options_] _zipfile_"],["PARAMETERS","**-w**\n> Write mode.\n\n**-b** _PATH_\n> Temp file path.\n\n**-h**\n> Show help.\n\n**-v**\n> Show version."],["DESCRIPTION","**zipnote** reads and writes comments stored in ZIP archives. ZIP files support per-file comments and an overall archive comment, and zipnote provides a way to view and modify these metadata fields from the command line.\n\nIn read mode (the default), zipnote outputs a structured listing of all filenames and their associated comments. The output uses a specific format with **@** markers separating entries, which can be redirected to a file for editing.\n\nIn write mode (**-w**), zipnote reads the modified structured format from standard input and updates the archive's comments accordingly. This workflow allows batch editing of comments across all files in an archive. zipnote is part of the Info-ZIP suite of utilities."],["FORMAT","```\n@ filename\ncomment text here\n@ (comment above this line)\n@ filename2\n```"],["CAVEATS","Part of Info-ZIP. Specific format required. Edit carefully."],["HISTORY","**zipnote** is part of **Info-ZIP** utilities. It enables comment management in ZIP files."],["INSTALL","```apt: sudo apt install zip```\n\n```dnf: sudo dnf install zip```\n\n```pacman: sudo pacman -S zip```\n\n```apk: sudo apk add zip```\n\n```zypper: sudo zypper install zip```\n\n```brew: brew install zip```\n\n```nix: nix profile install nixpkgs#zip```"],["SEE ALSO","[zip](/man/zip)(1), [unzip](/man/unzip)(1), [zipinfo](/man/zipinfo)(1)"]],"e":["zip","unzip","zipinfo"],"x":"zipnote [archive.zip]"}});
