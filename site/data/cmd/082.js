window.LCL_BUCKET(82,{"adb-reverse":{"n":"adb-reverse","t":"Set up reverse port forwarding from device to host","s":[["TAGLINE","Set up reverse port forwarding from device to host"],["TLDR","**Reverse forward** device port to local port\n\n```adb reverse tcp:[8080] tcp:[8080]```\n\n**List** all reverse forwards\n\n```adb reverse --list```\n\n**Remove** a specific reverse forward\n\n```adb reverse --remove tcp:[8080]```\n\nRemove **all** reverse forwards\n\n```adb reverse --remove-all```"],["SYNOPSIS","**adb reverse** [_--no-rebind_] _remote_ _local_\n\n**adb reverse** --list | --remove _remote_ | --remove-all"],["DESCRIPTION","**adb reverse** sets up reverse port forwarding from the Android device to your computer. This allows apps on the device to connect to services running on your development machine as if they were running locally on the device.\n\nThis is commonly used for connecting React Native apps to the Metro bundler, or allowing device apps to access local development servers."],["PARAMETERS","**tcp:**_port_\n> TCP port number\n\n**localabstract:**_name_\n> Unix domain socket in abstract namespace\n\n**--no-rebind**\n> Fail if remote port is already forwarded\n\n**--list**\n> List all active reverse forwards\n\n**--remove** _remote_\n> Remove specific reverse forward rule\n\n**--remove-all**\n> Remove all reverse forward rules"],["CAVEATS","Reverse forwarding is available on Android 5.0+ only. The feature requires the adb server to maintain the connection. Reverse forwards are lost when the device disconnects or adb server restarts."],["HISTORY","**adb reverse** was added in Android 5.0 Lollipop (**2014**) to support development workflows where device apps need to connect to services on the development machine."],["INSTALL","```apt: sudo apt install adb```\n\n```dnf: sudo dnf install android-tools```\n\n```pacman: sudo pacman -S android-tools```\n\n```apk: sudo apk add android-tools-adb```\n\n```zypper: sudo zypper install android-tools```\n\n```nix: nix profile install nixpkgs#android-tools```"],["SEE ALSO","[adb](/man/adb)(1), [adb-forward](/man/adb-forward)(1), [adb-shell](/man/adb-shell)(1), [scrcpy](/man/scrcpy)(1)"],["RESOURCES","```[Source code](https://android.googlesource.com/platform/packages/modules/adb/)```\n\n```[Documentation](https://developer.android.com/tools/adb)```"]],"e":["adb","adb-forward","adb-shell","scrcpy"],"x":"adb reverse tcp:[8080] tcp:[8080]"},"adb":{"n":"adb","t":"Android Debug Bridge client-server tool","s":[["TAGLINE","Android Debug Bridge client-server tool"],["TLDR","**List** connected devices\n\n```adb devices```\n\n**Install** an APK\n\n```adb install [app.apk]```\n\n**Copy file** from device to computer\n\n```adb pull [/sdcard/file.txt] [./local/]```\n\n**Copy file** from computer to device\n\n```adb push [local_file] [/sdcard/]```\n\nOpen a **shell** on the device\n\n```adb shell```\n\n**Pair** with device for wireless debugging (Android 11+)\n\n```adb pair [host]:[port]```\n\n**Connect** wirelessly to a device\n\n```adb connect [host]:[port]```"],["SYNOPSIS","**adb** [_-d|-e|-s serial_] _command_ [_options_]"],["DESCRIPTION","**adb** (Android Debug Bridge) is a versatile command-line tool for communicating with Android devices. It provides access to a Unix shell for running commands, installing/uninstalling apps, transferring files, and debugging applications.\n\nThe tool operates through a client-server architecture where the adb server runs on your computer and communicates with the adb daemon (adbd) on connected devices. Connections can be made via USB or wirelessly over TCP/IP."],["PARAMETERS","**-d**\n> Direct command to the only connected USB device\n\n**-e**\n> Direct command to the only running emulator\n\n**-s** _serial_\n> Direct command to device with specified serial number\n\n**-t** _transport_id_\n> Direct command to device with given transport ID\n\n**devices**\n> List all connected devices and their states\n\n**install** [**-r**] [**-g**] _apk_\n> Install an APK file. -r reinstalls keeping data, -g grants all runtime permissions.\n\n**uninstall** [**-k**] _package_\n> Remove an application by package name. -k keeps data and cache directories.\n\n**pair** _host_:_port_ [_pairing_code_]\n> Pair with a device for wireless debugging (Android 11+).\n\n**connect** _host_:_port_\n> Connect to a device over TCP/IP.\n\n**push** _local_ _remote_\n> Copy file from computer to device\n\n**pull** _remote_ _local_\n> Copy file from device to computer\n\n**shell**\n> Open interactive shell or run command on device\n\n**logcat**\n> View device log output\n\n**forward** _local_ _remote_\n> Set up port forwarding\n\n**reverse** _remote_ _local_\n> Set up reverse port forwarding\n\n**reboot**\n> Reboot the device (bootloader, recovery options available)\n\n**start-server**\n> Start the adb server\n\n**kill-server**\n> Stop the adb server\n\n**sideload** _otapackage_\n> Sideload an OTA update package in recovery mode\n\n**bugreport** [_path_]\n> Generate a bug report and save to path"],["CONFIGURATION","**~/.android/adb_usb.ini**\n> List of USB vendor IDs for device detection, one per line.\n\n**~/.android/adbkey**\n> RSA private key used for device authentication. The corresponding public key (adbkey.pub) is pushed to authorized devices.\n\n**ANDROID_ADB_SERVER_PORT**\n> Environment variable to override the default adb server port (5037).\n\n**ADB_VENDOR_KEYS**\n> Environment variable specifying colon-separated paths to additional RSA key files for device authorization."],["CAVEATS","USB debugging must be enabled on the device. First connection requires authorizing the computer on the device. Some commands require root access. Wireless debugging requires initial USB setup or pairing (Android 11+)."],["HISTORY","**adb** was developed by Google as part of the Android SDK, first released with Android 1.0 in **2008**. It has evolved to support features like wireless debugging, scoped storage access, and improved security through RSA key authentication."],["INSTALL","```apt: sudo apt install adb```\n\n```dnf: sudo dnf install android-tools```\n\n```pacman: sudo pacman -S android-tools```\n\n```apk: sudo apk add android-tools-adb```\n\n```zypper: sudo zypper install android-tools```\n\n```nix: nix profile install nixpkgs#android-tools```"],["SEE ALSO","[fastboot](/man/fastboot)(1), [aapt](/man/aapt)(1), [logcat](/man/logcat)(1), [scrcpy](/man/scrcpy)(1)"]],"e":["fastboot","aapt","logcat","scrcpy"],"x":"adb devices"},"add-computer.py":{"n":"add-computer.py","t":"Add computer accounts to Active Directory domains","s":[["TAGLINE","Add computer accounts to Active Directory domains"],["TLDR","**Add** a computer account to Active Directory\n\n```add-computer.py [domain]/[user]:[password] -computer-name [hostname]```\n\nAdd with **specific attributes**\n\n```add-computer.py [domain]/[user]:[password] -computer-name [hostname] -computer-pass [password]```\n\nUse **Kerberos** authentication\n\n```add-computer.py -k [domain]/[user] -computer-name [hostname]```"],["SYNOPSIS","**add-computer.py** [_-dc-ip ip_] _domain_/_user_:_password_ -computer-name _name_ [_options_]"],["DESCRIPTION","**add-computer.py** is an Impacket tool for adding computer accounts to Active Directory domains. It exploits the default AD configuration that allows authenticated users to add up to 10 computer accounts to the domain.\n\nThis is commonly used in penetration testing for resource-based constrained delegation attacks, as controlling a computer account provides additional attack vectors within an AD environment."],["PARAMETERS","**-dc-ip** _ip_\n> Domain controller IP address\n\n**-computer-name** _name_\n> Name for the new computer account\n\n**-computer-pass** _password_\n> Password for the computer account (auto-generated if not specified)\n\n**-method** _method_\n> Method to add computer: SAMR or LDAPS (default SAMR).\n\n**-dc-host** _hostname_\n> Domain controller hostname (FQDN). If omitted, derived from the domain argument.\n\n**-baseDN** _dn_\n> Distinguished name base in LDAP. If omitted, derived from the domain.\n\n**-domain-netbios** _name_\n> Domain NetBIOS name when the forest handles multiple domains.\n\n**-no-add**\n> Only change an existing computer account password without adding a new one.\n\n**-k**\n> Use Kerberos authentication (requires KRB5CCNAME ccache).\n\n**-no-pass**\n> Don't prompt for password (use with -k).\n\n**-hashes** _LMHASH:NTHASH_\n> Use NTLM hash for pass-the-hash authentication.\n\n**-aesKey** _key_\n> AES128 or AES256 hex key for Kerberos pass-the-key authentication.\n\n**-debug**\n> Enable verbose debug output."],["CAVEATS","This tool is intended for authorized penetration testing only. By default, AD allows users to create up to 10 computer accounts (ms-DS-MachineAccountQuota). This limit may be modified by administrators."],["HISTORY","**add-computer.py** is part of the Impacket library, developed by SecureAuth (formerly Core Security). Impacket has been actively developed since the early **2000s** as a collection of Python classes for network protocols."],["SEE ALSO","[impacket](/man/impacket)(1), [adcli](/man/adcli)(8), [addcomputer.py](/man/addcomputer.py)(1)"]],"e":["impacket","adcli","addcomputer.py"],"x":"add-computer.py [domain]/[user]:[password] -computer-name [hostname]"},"airolib-ng":{"n":"airolib-ng","t":"Pre-compute WPA/WPA2 PMK databases for faster cracking","s":[["TAGLINE","Pre-compute WPA/WPA2 PMK databases for faster cracking"],["TLDR","**Create** a new database\n\n```airolib-ng [database.db] --init```\n\n**Import** a wordlist\n\n```airolib-ng [database.db] --import passwd [wordlist.txt]```\n\n**Import** an ESSID\n\n```airolib-ng [database.db] --import essid [essid_file.txt]```\n\n**Batch process** (pre-compute PMKs)\n\n```airolib-ng [database.db] --batch```\n\n**Verify** database integrity\n\n```airolib-ng [database.db] --verify```\n\nShow database **statistics**\n\n```airolib-ng [database.db] --stats```"],["SYNOPSIS","**airolib-ng** _database_ _operation_ [_options_]"],["DESCRIPTION","**airolib-ng** manages databases of pre-computed Pairwise Master Keys (PMKs) for WPA/WPA2 cracking. Computing PMKs is the slow part of WPA cracking; pre-computing them for common ESSIDs and passwords dramatically speeds up attacks.\n\nThe tool creates SQLite databases that aircrack-ng can use directly, trading storage space for cracking speed."],["PARAMETERS","**--init**\n> Initialize a new database\n\n**--import passwd** _file_\n> Import passwords from wordlist\n\n**--import essid** _file_\n> Import ESSIDs from file\n\n**--import cowpatty** _file_\n> Import cowpatty hash file\n\n**--batch**\n> Compute all missing PMKs\n\n**--batch --essid** _name_\n> Compute PMKs for specific ESSID\n\n**--verify** [_all_]\n> Verify database integrity\n\n**--stats**\n> Show database statistics\n\n**--clean** [_all_]\n> Remove computed PMKs\n\n**--sql** _query_\n> Execute SQL query"],["CAVEATS","Pre-computation only helps for known ESSIDs. Database files can become very large. PMKs are ESSID-specific; generic wordlist attacks don't benefit. Computing PMKs is CPU-intensive."],["HISTORY","**airolib-ng** was added to the aircrack-ng suite to enable practical attacks against WPA/WPA2 networks by pre-computing the expensive PBKDF2 operations."],["INSTALL","```apt: sudo apt install aircrack-ng```\n\n```dnf: sudo dnf install aircrack-ng```\n\n```pacman: sudo pacman -S aircrack-ng```\n\n```apk: sudo apk add aircrack-ng```\n\n```zypper: sudo zypper install aircrack-ng```\n\n```brew: brew install aircrack-ng```\n\n```nix: nix profile install nixpkgs#aircrack-ng```"],["SEE ALSO","[aircrack-ng](/man/aircrack-ng)(1), [airodump-ng](/man/airodump-ng)(1), [genpmk](/man/genpmk)(1), [cowpatty](/man/cowpatty)(1)"],["RESOURCES","```[Source code](https://github.com/aircrack-ng/aircrack-ng)```\n\n```[Homepage](https://www.aircrack-ng.org/)```\n\n```[Documentation](https://www.aircrack-ng.org/documentation.html)```"]],"e":["aircrack-ng","airodump-ng","genpmk","cowpatty"],"x":"airolib-ng [database.db] --init"},"alex":{"n":"alex","t":"Catch insensitive, inconsiderate writing in text.","s":[["TAGLINE","Catch insensitive, inconsiderate writing in text."],["TLDR","**Check** a file for insensitive language\n\n```alex [file.md]```\n\nCheck **multiple files**\n\n```alex [file1.md] [file2.md]```\n\nCheck from **stdin**\n\n```echo \"This is crazy\" | alex --stdin```\n\nCheck with **custom config**\n\n```alex --config [.alexrc] [file.md]```\n\nOutput as **JSON**\n\n```alex --reporter json [file.md]```"],["SYNOPSIS","**alex** [_options_] [_files_...]"],["DESCRIPTION","**alex** catches insensitive, inconsiderate writing in text. It helps you find gender-favoring, polarizing, race-related, religion-inconsiderate, or other unequal phrasing in text.\n\nThe tool is designed for documentation, README files, and other written content, helping teams maintain inclusive language in their projects."],["PARAMETERS","**--stdin**\n> Read from standard input\n\n**--text**\n> Treat input as plain text (not markdown)\n\n**--html**\n> Search for and treat input as HTML (.html, .htm) files\n\n**--mdx**\n> Search for and treat input as MDX (.mdx) files\n\n**--config** _file_\n> Configuration file path\n\n**--reporter** _type_\n> Output format: json, compact, or default\n\n**--quiet**\n> Only report errors, suppressing files without problems\n\n**--why**\n> Show source of every message (which rule triggered it)\n\n**--diff**\n> Ignore unchanged lines (useful in CI to only report on the diff)\n\n**-v**, **--version**\n> Display version\n\n**-h**, **--help**\n> Display help text"],["CONFIGURATION","**.alexrc**\n> JSON or YAML configuration file in the project root. Defines allowed terms, custom rules, and language preferences.\n\n**.alexignore**\n> Lists files and directories to exclude from checking, using gitignore-style patterns."],["CAVEATS","May produce false positives; review suggestions in context. Technical terms or proper nouns may be flagged incorrectly. Configuration allows allowlisting specific terms."],["HISTORY","**alex** was created by Titus Wormer and released in **2015** as part of the unified.js ecosystem. It was designed to bring automated checking for inclusive language to the development workflow."],["INSTALL","```brew: brew install alexjs```"],["SEE ALSO","[write-good](/man/write-good)(1), [proselint](/man/proselint)(1), [vale](/man/vale)(1)"],["RESOURCES","```[Source code](https://github.com/get-alex/alex)```\n\n```[Homepage](https://alexjs.com/)```"]],"e":["write-good","proselint","vale"],"x":"alex [file.md]"},"ali":{"n":"ali","t":"HTTP load testing tool with real-time TUI charts","s":[["TAGLINE","HTTP load testing tool with real-time TUI charts"],["TLDR","**Run a basic load test** against a URL\n\n```ali [http://host.xz]```\n\n**Set custom request rate and duration**\n\n```ali --rate=[500] --duration=[5m] [http://host.xz]```\n\n**Send a POST request** with a body file\n\n```ali --method=POST --body-file=[path/to/payload.json] [http://host.xz]```\n\n**Add custom headers** to the request\n\n```ali -H \"Authorization: Bearer [token]\" [http://host.xz]```\n\n**Export results** to a directory for later analysis\n\n```ali --export-to [path/to/results/] [http://host.xz]```\n\n**Load test with TLS verification disabled**\n\n```ali --insecure [https://host.xz]```\n\n**Run at maximum speed** with no rate limit\n\n```ali --rate=0 --duration=[30s] [http://host.xz]```"],["SYNOPSIS","**ali** [_options_] _target_"],["PARAMETERS","**-r**, **--rate** _n_\n> Requests per second; 0 for max speed (default: 50)\n\n**-d**, **--duration** _duration_\n> Duration of the attack; 0s for infinite (default: 10s)\n\n**-w**, **--workers** _n_\n> Initial number of workers (default: 10)\n\n**-m**, **--method** _method_\n> HTTP method (default: GET)\n\n**-t**, **--timeout** _duration_\n> Request timeout (default: 30s)\n\n**-b**, **--body** _string_\n> Request body as a string\n\n**-B**, **--body-file** _file_\n> Path to file for request body\n\n**-H**, **--header** _header_\n> Custom header (repeatable)\n\n**-c**, **--connections** _n_\n> Max idle connections per host (default: 10000)\n\n**--export-to** _dir_\n> Directory to export results\n\n**--insecure**\n> Skip TLS certificate verification\n\n**--no-http2**\n> Disable HTTP/2\n\n**-K**, **--no-keepalive**\n> Disable keep-alive connections\n\n**--resolvers** _addrs_\n> Custom DNS resolver addresses"],["DESCRIPTION","**ali** is a terminal-based HTTP load testing tool that generates HTTP load and plots the results in real-time directly in the terminal. It combines the load generation capabilities of tools like vegeta with live terminal charts showing latency, percentiles (p50, p90, p95, p99), throughput, and bytes in/out. The interactive TUI supports mouse-based zooming into charts for detailed analysis.\n\nWritten in Go, ali supports HTTP/2 by default, custom TLS certificates, configurable concurrency, and result exporting for downstream analysis."],["CAVEATS","The terminal UI requires a terminal that supports standard escape sequences. Running with very high request rates may be limited by system resources and network capacity. Setting duration to 0 runs the attack indefinitely until manually stopped."],["HISTORY","**ali** was created by **Ryo Nakao** (nakabonne), a Go developer based in Tokyo, Japan. The tool was inspired by **vegeta** (HTTP load testing) and **jplot** (terminal plotting), combining both into a single real-time tool. It is open-source under the MIT license."],["INSTALL","```pacman: sudo pacman -S ali```\n\n```apk: sudo apk add ali```\n\n```brew: brew install ali```\n\n```nix: nix profile install nixpkgs#ali```"],["SEE ALSO","[ab](/man/ab)(1), [wrk](/man/wrk)(1), [curl](/man/curl)(1), [hey](/man/hey)(1), [siege](/man/siege)(1), [vegeta](/man/vegeta)(1), [k6](/man/k6)(1)"]],"e":["ab","wrk","curl","hey","siege","vegeta","k6"],"x":"ali [http://host.xz]"},"blackfire":{"n":"blackfire","t":"Profile PHP application performance","s":[["TAGLINE","Profile PHP application performance"],["TLDR","**Profile a PHP script**\n\n```blackfire run php [script.php]```\n\n**Profile with arguments**\n\n```blackfire run php [script.php] --arg1 [value]```\n\n**Profile an HTTP request** using curl\n\n```blackfire curl [https://example.com/page]```\n\n**Profile to a specific environment**\n\n```blackfire --env=[ENV_UUID] run php [script.php]```\n\n**Get JSON output** of profile results\n\n```blackfire --json run php [script.php]```\n\n**Profile with quiet output** (no URL displayed)\n\n```blackfire -q run php [script.php]```\n\n**Profile a Symfony console command**\n\n```blackfire run php bin/console [command:name]```"],["SYNOPSIS","**blackfire** [_options_] _subcommand_ [_arguments_]"],["DESCRIPTION","**blackfire** is the CLI tool for the Blackfire profiler, a performance management solution for PHP applications. It profiles PHP scripts, HTTP requests, and web applications to identify performance bottlenecks.\n\nThe tool integrates with Blackfire.io cloud services to provide detailed flame graphs, call graphs, and performance recommendations. Profiles can be compared to track performance changes over time."],["SUBCOMMANDS","**run**\n> Profile a PHP CLI script or command\n\n**curl**\n> Profile an HTTP request (uses system curl)\n\n**upload**\n> Upload a profile to Blackfire.io\n\n**client:config**\n> Configure client credentials\n\n**agent:config**\n> Configure agent settings"],["PARAMETERS","**--env** _uuid_\n> Target environment UUID for the profile\n\n**--json**\n> Output profile results in JSON format\n\n**-q, --quiet**\n> Suppress output (do not display profile URL)\n\n**--samples** _n_\n> Number of samples to collect\n\n**--title** _text_\n> Set a title for the profile\n\n**--external-id** _id_\n> Set an external identifier for the profile\n\n**--ignore-exit-status**\n> Ignore non-zero exit status from profiled command"],["CONFIGURATION","**/etc/blackfire/agent**\n> Blackfire agent configuration including server credentials and socket settings.\n\n**~/.blackfire.ini**\n> Client-side configuration with client ID and token for authentication."],["CAVEATS","Requires a Blackfire account and properly configured credentials (BLACKFIRE_CLIENT_ID and BLACKFIRE_CLIENT_TOKEN). The Blackfire PHP extension must be installed for profiling to work. The **curl** subcommand requires curl to be installed on the system."],["HISTORY","Blackfire was created by **SensioLabs** (the company behind Symfony) and launched in **2014** as a commercial profiling solution. It evolved from the open-source xhprof profiler and provides continuous performance monitoring for PHP applications."],["INSTALL","```nix: nix profile install nixpkgs#blackfire```"],["SEE ALSO","[php](/man/php)(1), [curl](/man/curl)(1)"]],"e":["php","curl"],"x":"blackfire run php [script.php]"},"blkdiscard":{"n":"blkdiscard","t":"Discard sectors on a storage device","s":[["TAGLINE","Discard sectors on a storage device"],["TLDR","**Discard** all sectors\n\n```blkdiscard /dev/device```\n\n**Secure** discard\n\n```blkdiscard -s /dev/device```\n\nDiscard first **100 MiB**\n\n```blkdiscard -l 100M /dev/device```\n\nDiscard in **512 MiB** steps (avoids long uninterruptible operations)\n\n```blkdiscard -p 512M /dev/device```"],["SYNOPSIS","**blkdiscard** [_OPTIONS_] _device_"],["DESCRIPTION","**blkdiscard** sends TRIM/DISCARD commands to a block device to mark sectors as unused. Unlike **fstrim**(8), it operates directly on the block device rather than a mounted filesystem. This is particularly useful for SSDs and thinly-provisioned storage. All data in the discarded region will be lost."],["PARAMETERS","**-o, --offset** _offset_\n> Byte offset into the device to start discarding. Must be aligned to the device sector size. Default is 0.\n\n**-l, --length** _size_\n> Number of bytes to discard from the offset. Must be aligned to the device sector size.\n\n**-p, --step** _size_\n> Discard this many bytes per iteration instead of all at once. Useful for avoiding long uninterruptible operations on large devices.\n\n**-s, --secure**\n> Perform a secure discard. This tells the device to also erase any copies of the discarded data (e.g. in garbage collection). Requires device support.\n\n**-z, --zeroout**\n> Zero-fill the specified region rather than discarding it.\n\n**-f, --force**\n> Disable exclusive-open check, allowing operation on mounted devices. Use with caution.\n\n**-q, --quiet**\n> Suppress warning messages.\n\n**-v, --verbose**\n> Print aligned offset and length values and progress information."],["CAVEATS","This operation destroys data and is irreversible. All data in the discarded region is lost. Not all devices support discard operations; on unsupported devices, blkdiscard exits with status 2 (since util-linux 2.39). Secure discard requires explicit hardware support and may not provide stronger guarantees than regular discard on all devices."],["HISTORY","**blkdiscard** is part of the **util-linux** package, providing SSD optimization and secure erase functionality."],["INSTALL","```apt: sudo apt install util-linux```\n\n```dnf: sudo dnf install util-linux```\n\n```pacman: sudo pacman -S util-linux```\n\n```apk: sudo apk add util-linux-misc```\n\n```zypper: sudo zypper install util-linux```\n\n```brew: brew install util-linux```\n\n```nix: nix profile install nixpkgs#util-linux```"],["SEE ALSO","[fstrim](/man/fstrim)(8), [hdparm](/man/hdparm)(8), [wipefs](/man/wipefs)(8), [blkid](/man/blkid)(8)"]],"e":["fstrim","hdparm","wipefs","blkid"],"x":"blkdiscard /dev/device"},"blkparse":{"n":"blkparse","t":"Parse and display block I/O trace data","s":[["TAGLINE","Parse and display block I/O trace data"],["TLDR","**Parse and display blktrace output** for a device\n\n```blkparse [sda]```\n\n**Parse multiple devices**\n\n```blkparse [sda] [sdb]```\n\n**Parse from stdin** (live mode with blktrace)\n\n```blktrace -d [/dev/sda] -o - | blkparse -i -```\n\n**Output to a file**\n\n```blkparse [sda] -o [output.txt]```\n\n**Show specific output fields**\n\n```blkparse -f \"%D %a %S %n\\n\" [sda]```\n\n**Parse with quiet output** (summary only)\n\n```blkparse -q [sda]```"],["SYNOPSIS","**blkparse** [_options_] [_device_...]"],["DESCRIPTION","**blkparse** produces formatted output from event streams recorded by **blktrace**. It combines event streams from multiple CPUs and devices, converting machine-readable trace data into human-readable format.\n\nThe tool is essential for analyzing block I/O patterns, debugging storage performance issues, and understanding how the Linux I/O scheduler processes requests."],["PARAMETERS","**-i** _file_\n> Base name for input files (default device.blktrace.cpu); use \"-\" for live stdin\n\n**-o** _file_\n> Output file (default: stdout)\n\n**-D** _dir_\n> Prepend this directory to the input file names\n\n**-d** _file_\n> Dump a merged binary event stream to _file_\n\n**-f** _format_\n> Custom output format string for all events\n\n**-F** _typ,format_\n> Output format string for a specific event type\n\n**-q**\n> Quiet mode; only print summary at end\n\n**-s**\n> Show per-program (per-process) statistics\n\n**-t**\n> Track I/O timing; display time deltas per I/O\n\n**-w** _span_\n> Limit output to a time window; _end_ or _start:end_ in nanoseconds\n\n**-a** _mask_\n> Add _mask_ to the current trace action filter\n\n**-A** _hexmask_\n> Set the trace action filter to _hexmask_\n\n**-v**\n> More verbose output"],["TRACE ACTIONS","**Q** - Queued: Intent to queue I/O noted\n**I** - Inserted: Request sent to I/O scheduler\n**D** - Issued: Request sent to driver\n**C** - Complete: Request completed\n**M** - Back merge: Request merged with existing\n**F** - Front merge: Request merged at front\n**P** - Plug: Queue plugged\n**U** - Unplug: Queue unplugged"],["CAVEATS","Requires trace data from **blktrace** to function. Trace files can become very large for busy systems. Running blktrace requires root privileges. Output can be overwhelming; use format options to limit fields."],["HISTORY","blkparse was written by **Jens Axboe**, **Alan D. Brunelle**, and **Nathan Scott** as part of the blktrace toolkit. It was developed to provide detailed I/O tracing capabilities for Linux storage subsystem analysis and debugging."],["INSTALL","```apt: sudo apt install blktrace```\n\n```dnf: sudo dnf install blktrace```\n\n```zypper: sudo zypper install blktrace```\n\n```nix: nix profile install nixpkgs#blktrace```"],["SEE ALSO","[blktrace](/man/blktrace)(8), [btrace](/man/btrace)(8), [btt](/man/btt)(1), [iostat](/man/iostat)(1)"],["RESOURCES","```[Source code](https://git.kernel.org/pub/scm/linux/kernel/git/axboe/blktrace.git)```"]],"e":["blktrace","btrace","btt","iostat"],"x":"blkparse [sda]"},"bmptoppm":{"n":"bmptoppm","t":"Convert BMP images to PPM format","s":[["TAGLINE","Convert BMP images to PPM format"],["TLDR","**Convert a BMP image** to PPM format\n\n```bmptoppm [image.bmp] > [output.ppm]```\n\n**Convert from stdin**\n\n```cat [image.bmp] | bmptoppm > [output.ppm]```\n\n**Convert BMP to JPEG** via pipe\n\n```bmptoppm [image.bmp] | pnmtojpeg > [output.jpg]```"],["SYNOPSIS","**bmptoppm** [_bmpfile_]"],["DESCRIPTION","**bmptoppm** reads a Microsoft Windows or OS/2 BMP file as input and produces a PPM (Portable PixMap) color image as output. This is part of the Netpbm package.\n\nThis command was replaced in Netpbm 9.25 (**March 2002**) by **bmptopnm**, which automatically determines the appropriate output format (PBM, PGM, or PPM) based on the input image characteristics. **bmptoppm** is retained only for backward compatibility and takes no options of its own.\n\nInput can be from a file or standard input; output is to standard output."],["CAVEATS","For grayscale BMP images, consider using **bmptopnm** instead, which will produce more appropriate PGM output. This command always outputs PPM regardless of the input color depth."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[bmptopnm](/man/bmptopnm)(1), [ppmtobmp](/man/ppmtobmp)(1), [pnmtojpeg](/man/pnmtojpeg)(1), [netpbm](/man/netpbm)(1)"],["RESOURCES","```[Homepage](https://netpbm.sourceforge.net/)```\n\n```[Documentation](https://netpbm.sourceforge.net/doc/bmptoppm.html)```"]],"e":["bmptopnm","ppmtobmp","pnmtojpeg","netpbm"],"x":"bmptoppm [image.bmp] > [output.ppm]"},"boundary":{"n":"boundary","t":"Secure identity-based remote access to infrastructure.","s":[["TAGLINE","Secure identity-based remote access to infrastructure."],["TLDR","**Authenticate to Boundary**\n\n```boundary authenticate```\n\n**Connect to a target**\n\n```boundary connect -target-id [ttcp_1234567890]```\n\n**Connect using SSH helper**\n\n```boundary connect ssh -target-id [ttcp_1234567890]```\n\n**List available targets**\n\n```boundary targets list -recursive```\n\n**List active sessions**\n\n```boundary sessions list -recursive```\n\n**Read target details**\n\n```boundary targets read -id [ttcp_1234567890]```\n\n**Cancel a session**\n\n```boundary sessions cancel -id [s_1234567890]```\n\n**Start a local proxy**\n\n```boundary connect -target-id [ttcp_1234567890] -listen-port [2222]```"],["SYNOPSIS","**boundary** _command_ [_options_]"],["COMMANDS","**authenticate**\n> Authenticate to a Boundary controller.\n\n**connect** [_helper_]\n> Establish connection to a target. Helpers: ssh, postgres, rdp, http, kube.\n\n**targets** list|read\n> List or inspect targets.\n\n**sessions** list|read|cancel\n> Manage active sessions.\n\n**hosts** list|read\n> View hosts in host catalogs.\n\n**host-catalogs** list|read\n> View host catalogs.\n\n**scopes** list|read\n> View organizational scopes.\n\n**accounts** list|read|create|update|delete\n> Manage accounts.\n\n**auth-methods** list|read\n> View authentication methods.\n\n**roles** list|read\n> View roles and permissions.\n\n**groups** list|read\n> View groups.\n\n**users** list|read\n> View users.\n\n**logout**\n> Clear stored credentials.\n\n**version**\n> Show version information."],["PARAMETERS","**-target-id** _id_\n> Target ID to connect to.\n\n**-target-name** _name_\n> Target name (requires scope).\n\n**-target-scope-id** _id_\n> Scope containing target.\n\n**-target-scope-name** _name_\n> Scope name containing target.\n\n**-listen-port** _port_\n> Local port for proxy connection.\n\n**-addr** _address_\n> Boundary controller address.\n\n**-token** _token_\n> Authentication token.\n\n**-token-name** _name_\n> Token name for storage.\n\n**-scope-id** _id_\n> Scope ID for operations.\n\n**-recursive**\n> List resources recursively across scopes.\n\n**-format** _format_\n> Output format: table, json.\n\n**-keyring-type** _type_\n> Keyring type for credential storage."],["DESCRIPTION","**boundary** is the CLI for HashiCorp Boundary, a tool for secure remote access to infrastructure. It provides identity-based access to hosts and services without exposing networks or managing credentials directly.\n\nThe **connect** command establishes sessions to targets. Protocol-specific helpers (ssh, postgres, rdp, http, kube) automatically configure client tools with proper credentials. For SSH, it spawns an ssh process with injected credentials; for databases, it provides connection strings.\n\nAuthentication methods include password, OIDC, and LDAP. Credentials are stored in the system keyring or specified location. Use **authenticate** to log in and **logout** to clear credentials.\n\nBoundary uses a hierarchical scope model with global, organization, and project scopes. Use **-recursive** to list resources across all accessible scopes.\n\nSessions maintain the connection state between client and target. Active sessions can be listed and canceled. Session recordings enable audit and compliance when configured."],["CAVEATS","Requires access to a Boundary controller. Targets must be configured by administrators with proper permissions. Connect helpers require their respective client tools installed (ssh, psql, etc.). Some features require Boundary Enterprise or HCP Boundary."],["HISTORY","Boundary was announced by **HashiCorp** in **October 2020** as part of their security product line alongside Vault. It addresses modern identity-based access patterns, replacing traditional VPNs and bastion hosts. Version 0.1 was released in **October 2020**. The project reached 1.0 GA status in **2022**. Boundary integrates with Vault for credential brokering and injection, providing just-in-time access to infrastructure."],["INSTALL","```nix: nix profile install nixpkgs#boundary```"],["SEE ALSO","[vault](/man/vault)(1), [ssh](/man/ssh)(1), [tsh](/man/tsh)(1), [kubectl](/man/kubectl)(1)"],["RESOURCES","```[Source code](https://github.com/hashicorp/boundary)```\n\n```[Homepage](https://developer.hashicorp.com/boundary)```\n\n```[Documentation](https://developer.hashicorp.com/boundary/docs)```"]],"e":["vault","ssh","tsh","kubectl"],"x":"boundary authenticate"},"check-dfsg-status":{"n":"check-dfsg-status","t":"Report non-free packages on Debian systems","s":[["TAGLINE","Report non-free packages on Debian systems"],["TLDR","List **non-free** and contrib packages\n\n```check-dfsg-status```\n\nOutput only **package names**\n\n```check-dfsg-status -s```\n\nShow **explanations** for non-free classification\n\n```check-dfsg-status -e```"],["SYNOPSIS","**check-dfsg-status** [_options_]"],["DESCRIPTION","**check-dfsg-status** reports installed packages from the non-free and contrib sections on Debian-based systems. DFSG stands for Debian Free Software Guidelines, which define what software qualifies as free in the Debian project.\n\nThe tool was formerly known as **vrms** (Virtual Richard M. Stallman) and helps users identify proprietary or non-free software on their system."],["PARAMETERS","**-s**, **--sparse**\n> Output only package names without descriptions\n\n**-e**, **--explain**\n> Explain why packages are classified as non-free"],["CAVEATS","Only checks packages from official Debian repositories. Manually installed software, Flatpaks, Snaps, or third-party repository packages are not analyzed. Classification depends on Debian's section metadata in package control files."],["HISTORY","The original **vrms** was created as a playful tool named after Richard Stallman, founder of the Free Software Foundation. It was renamed to **check-dfsg-status** for a more neutral and descriptive name."],["SEE ALSO","[apt](/man/apt)(8), [dpkg](/man/dpkg)(1), [vrms](/man/vrms)(1)"]],"e":["apt","dpkg","vrms"],"x":"check-dfsg-status"},"co":{"n":"co","t":"RCS revision checkout","s":[["TAGLINE","RCS revision checkout"],["TLDR","**Check out the latest revision** of a file (read-only)\n\n```co [file.txt]```\n\n**Check out a file for editing** (with lock)\n\n```co -l [file.txt]```\n\n**Check out a specific revision**\n\n```co -r[1.2] [file.txt]```\n\n**Check out and unlock** a file\n\n```co -u [file.txt]```\n\n**Check out a revision by date**\n\n```co -d\"[2024-01-15]\" [file.txt]```\n\n**Check out quietly** without printing diagnostics\n\n```co -q [file.txt]```\n\n**Force checkout** even if working file exists\n\n```co -f [file.txt]```"],["SYNOPSIS","**co** [_options_] _file_..."],["PARAMETERS","**-l**\n> Lock the checked-out revision to prevent others from editing.\n\n**-u**\n> Unlock the revision; check out without locking.\n\n**-r**_REV_\n> Check out specific revision REV.\n\n**-d**_DATE_\n> Check out the latest revision at or before DATE.\n\n**-f**\n> Force overwrite of the working file if it exists.\n\n**-q**\n> Quiet mode; suppress diagnostic output.\n\n**-p**\n> Print the revision to stdout instead of creating a working file.\n\n**-j**_JOINLIST_\n> Generate new revision by merging differences between revisions.\n\n**-k**_SUBST_\n> Set keyword substitution mode (kv, kvl, k, o, b, v).\n\n**-s**_STATE_\n> Check out the latest revision with the given state.\n\n**-w**_LOGIN_\n> Check out the latest revision checked in by LOGIN.\n\n**-T**\n> Set the working file's modification time to the revision's check-in time.\n\n**-V**\n> Print RCS version number."],["DESCRIPTION","**co** is the checkout command for RCS (Revision Control System), a legacy version control system for managing file revisions. It retrieves revisions from RCS files and places them in the working directory for viewing or editing.\n\nBy default, co retrieves the latest revision in read-only mode. The **-l** flag locks the revision for exclusive editing, preventing others from checking out the same revision for modification. After editing, the file is checked back in with **ci**.\n\nRCS stores revision history in special files (typically ending in **,v**) and co reconstructs working files from this delta-based storage. While largely superseded by distributed version control systems, RCS remains useful for simple single-file versioning."],["CAVEATS","RCS operates on individual files rather than repositories. Locks are advisory and depend on user cooperation. The working file's timestamp is set to the revision's check-in time by default. Checking out an already-locked file requires the lock owner to release it or use **rcs -u** to break the lock."],["HISTORY","RCS was created by Walter Tichy at Purdue University and first released in **1982**. It pioneered the concept of delta storage for version control. While CVS and later systems built upon RCS concepts, the standalone co/ci workflow remains available on most Unix-like systems."],["INSTALL","```apt: sudo apt install rcs```\n\n```dnf: sudo dnf install rcs```\n\n```pacman: sudo pacman -S rcs```\n\n```apk: sudo apk add rcs```\n\n```zypper: sudo zypper install rcs```\n\n```brew: brew install rcs```\n\n```nix: nix profile install nixpkgs#rcs```"],["SEE ALSO","[ci](/man/ci)(1), [rcs](/man/rcs)(1), [rlog](/man/rlog)(1), [rcsdiff](/man/rcsdiff)(1), [git](/man/git)(1)"]],"e":["ci","rcs","rlog","rcsdiff","git"],"x":"co [file.txt]"},"compton":{"n":"compton","t":"standalone compositor for X11 window systems","s":[["TAGLINE","standalone compositor for X11 window systems"],["TLDR","**Start compton** with default settings\n\n```compton```\n\n**Start with a configuration file**\n\n```compton --config [~/.config/compton.conf]```\n\n**Enable shadows** on windows\n\n```compton -c```\n\n**Enable fading** effects for windows\n\n```compton -f```\n\n**Set shadow opacity**\n\n```compton -c -o [0.5]```\n\n**Disable shadows on dock** and panel windows\n\n```compton -c -C```\n\n**Run in background** as daemon\n\n```compton -b```\n\n**Enable vsync** to prevent tearing\n\n```compton --vsync opengl```"],["SYNOPSIS","**compton** [_options_]"],["PARAMETERS","**-c**, **--shadow**\n> Enable client-side shadows on windows.\n\n**-C**, **--no-dock-shadow**\n> Disable shadows on dock/panel windows.\n\n**-f**, **--fading**\n> Fade windows in/out when opening/closing.\n\n**-o** _OPACITY_\n> Shadow opacity (0.0 to 1.0).\n\n**-r** _RADIUS_\n> Shadow blur radius in pixels.\n\n**-l** _OFFSET_\n> Left offset for shadows in pixels.\n\n**-t** _OFFSET_\n> Top offset for shadows in pixels.\n\n**--vsync** _METHOD_\n> VSync method: none, drm, opengl, opengl-oml, opengl-swc.\n\n**-b**, **--daemon**\n> Run as a background daemon.\n\n**--config** _FILE_\n> Read configuration from the specified file.\n\n**-i** _OPACITY_\n> Inactive window opacity (0.0 to 1.0).\n\n**-e** _OPACITY_\n> Opacity of window titlebars and borders.\n\n**--backend** _BACKEND_\n> Rendering backend: xrender, glx."],["CONFIGURATION","**~/.config/compton.conf**\n> User configuration file for compton settings.\n\n**/etc/xdg/compton.conf**\n> System-wide default configuration."],["DESCRIPTION","**compton** is a standalone compositor for X11, providing visual effects like window shadows, transparency, fading animations, and vsync to prevent screen tearing. It works with any window manager that supports compositing.\n\nThe compositor operates by intercepting window rendering and applying effects before displaying the final image. It supports both XRender and OpenGL backends, with GLX generally providing better performance. Configuration can be done via command-line options or a configuration file.\n\nCompton is commonly used with lightweight window managers like i3, openbox, and bspwm to add visual polish without the overhead of a full desktop environment's compositor. It can also help with screen tearing issues on systems without built-in compositing."],["CAVEATS","Compton development has ceased; **picom** is the actively maintained fork and recommended for new installations. OpenGL backend may have compatibility issues with some graphics drivers. Compositing adds GPU overhead that may affect performance on older hardware."],["HISTORY","Compton was forked from **xcompmgr** in **2011** by Christopher Jeffrey (chjj) to add features and fix bugs. It became popular in the Linux ricing community for its configurability. Development stalled around **2017**, leading to the creation of **picom** as an actively maintained fork."],["INSTALL","```apt: sudo apt install compton```\n\n```dnf: sudo dnf install picom```\n\n```pacman: sudo pacman -S picom```\n\n```apk: sudo apk add picom```\n\n```zypper: sudo zypper install picom```\n\n```nix: nix profile install nixpkgs#picom```"],["SEE ALSO","[picom](/man/picom)(1), [xcompmgr](/man/xcompmgr)(1), [i3](/man/i3)(1)"],["RESOURCES","```[Source code](https://github.com/chjj/compton)```"]],"e":["picom","xcompmgr","i3"],"x":"compton"},"cpufreq-set":{"n":"cpufreq-set","t":"modify CPU frequency scaling settings","s":[["TAGLINE","modify CPU frequency scaling settings"],["TLDR","**Set the frequency governor** for a specific CPU\n\n```sudo cpufreq-set -c [1] -g [performance]```\n\n**Set the minimum frequency** allowed\n\n```sudo cpufreq-set -c [1] -d [800MHz]```\n\n**Set the maximum frequency** allowed\n\n```sudo cpufreq-set -c [1] -u [2.4GHz]```\n\n**Pin to a specific frequency** (requires userspace governor)\n\n```sudo cpufreq-set -c [1] -f [2000MHz]```\n\n**Apply settings to all CPUs sharing the policy**\n\n```sudo cpufreq-set -r -g [powersave]```"],["SYNOPSIS","**cpufreq-set** [_options_]"],["PARAMETERS","**-c**, **--cpu** _cpu_\n> CPU number to operate on. Defaults to CPU 0 when omitted.\n\n**-d**, **--min** _freq_\n> Set the minimum frequency the governor may select.\n\n**-u**, **--max** _freq_\n> Set the maximum frequency the governor may select.\n\n**-g**, **--governor** _governor_\n> Select the scaling governor (e.g. `performance`, `powersave`, `ondemand`, `conservative`, `schedutil`, `userspace`).\n\n**-f**, **--freq** _freq_\n> Pin the CPU to a specific frequency. Requires the `userspace` governor and cannot be combined with `-d`, `-u`, or `-g`.\n\n**-r**, **--related**\n> Also apply the change to every CPU hardware-related to the selected one.\n\n**-h**, **--help**\n> Show help."],["DESCRIPTION","**cpufreq-set** modifies the Linux kernel cpufreq policy for a CPU, adjusting the scaling governor, minimum and maximum frequencies, or pinning a fixed frequency. It is part of the **cpufrequtils** package.\n\nFrequency values accept the suffixes `Hz`, `kHz` (default if no unit given), `MHz`, `GHz`, and `THz` appended directly to the number (for example `2400MHz` or `2.4GHz`). Valid values for the current CPU can be discovered with `cpufreq-info -l`."],["CAVEATS","Requires root privileges (or write access to `/sys/devices/system/cpu/cpu*/cpufreq/`). Pinning a fixed frequency only works when the `userspace` governor is active and loaded. Available governors depend on kernel configuration. Settings are not persisted across reboots; use a systemd unit or the `cpufrequtils` service to restore them at boot. On modern kernels using the `intel_pstate` driver, only the `performance` and `powersave` governors may be available."],["HISTORY","**cpufreq-set** ships with **cpufrequtils**, originally written by **Dominik Brodowski**. The newer **cpupower** suite (from the kernel tree) largely supersedes cpufrequtils on recent distributions."],["INSTALL","```apk: sudo apk add cpufrequtils```\n\n```nix: nix profile install nixpkgs#cpufrequtils```"],["SEE ALSO","[cpufreq-info](/man/cpufreq-info)(1), [cpupower](/man/cpupower)(1)"]],"e":["cpufreq-info","cpupower"],"x":"sudo cpufreq-set -c [1] -g [performance]"},"crane-auth":{"n":"crane-auth","t":"manage container registry authentication","s":[["TAGLINE","manage container registry authentication"],["TLDR","**Login to a registry**\n\n```crane auth login [registry] -u [username] -p [password]```\n\n**Get authentication token**\n\n```crane auth token [image]```\n\n**Logout from registry**\n\n```crane auth logout [registry]```"],["SYNOPSIS","**crane** **auth** _subcommand_ [_options_]"],["SUBCOMMANDS","**login**\n> Authenticate with a registry.\n\n**logout**\n> Remove credentials for a registry.\n\n**token**\n> Get authentication token for an image.\n\n**get**\n> Get credentials for a registry."],["PARAMETERS","**-u**, **--username** _user_\n> Username for authentication.\n\n**-p**, **--password** _pass_\n> Password for authentication.\n\n**--password-stdin**\n> Read password from stdin."],["DESCRIPTION","**crane auth** manages authentication for container registries. It stores credentials and retrieves authentication tokens for registry operations."],["INSTALL","```pacman: sudo pacman -S crane```\n\n```apk: sudo apk add crane```\n\n```zypper: sudo zypper install crane```\n\n```brew: brew install crane```\n\n```nix: nix profile install nixpkgs#crane```"],["SEE ALSO","[crane](/man/crane)(1), [docker-login](/man/docker-login)(1)"],["RESOURCES","```[Source code](https://github.com/google/go-containerregistry)```\n\n```[Documentation](https://github.com/google/go-containerregistry/blob/main/cmd/crane/doc/crane.md)```"]],"e":["crane","docker-login"],"x":"crane auth login [registry] -u [username] -p [password]"},"disown":{"n":"disown","t":"remove jobs from shell job table","s":[["TAGLINE","remove jobs from shell job table"],["TLDR","**Disown the most recent** background job\n\n```disown```\n\n**Disown a specific job** by job spec\n\n```disown %[1]```\n\n**Disown all background** jobs\n\n```disown -a```\n\n**Disown running jobs** only\n\n```disown -r```\n\n**Disown and suppress** SIGHUP\n\n```disown -h %[1]```"],["SYNOPSIS","**disown** [_options_] [_jobspec_...]"],["PARAMETERS","_JOBSPEC_\n> Job specification (e.g., %1, %2) to disown.\n\n**-a**\n> Disown all jobs.\n\n**-r**\n> Disown only running jobs.\n\n**-h**\n> Mark jobs so they don't receive SIGHUP on shell exit."],["DESCRIPTION","**disown** is a shell builtin that removes jobs from the shell's job table, preventing them from receiving the SIGHUP signal when the shell terminates. This allows background processes to continue running after logout.\n\nWithout arguments, disown affects the current job. Job specifications (like %1) can target specific jobs. The -h option keeps jobs in the table but marks them to not receive SIGHUP, useful when you want to monitor jobs but prevent termination.\n\ndisown is commonly used when a long-running process was started without nohup and needs to survive shell exit. It works with both bash and zsh shells."],["CAVEATS","Process must already be backgrounded before disown. Output may still go to terminal if not redirected. Cannot re-own a disowned job. Shell-specific implementation varies."],["HISTORY","disown is a shell builtin found in **bash**, **zsh**, and **ksh**. It complements the nohup command by providing a way to detach already-running jobs from the shell session."],["SEE ALSO","[nohup](/man/nohup)(1), [bg](/man/bg)(1), [fg](/man/fg)(1), [jobs](/man/jobs)(1)"],["RESOURCES","```[Documentation](https://www.gnu.org/software/bash/manual/html_node/Job-Control-Builtins.html)```"]],"e":["nohup","bg","fg","jobs"],"x":"disown"},"eim":{"n":"eim","t":"ESP-IDF Installation Manager","s":[["TAGLINE","ESP-IDF Installation Manager"],["TLDR","**Install a specific ESP-IDF version**\n\n```eim install -i [v5.3.2]```\n\n**Install interactively** (with prompts)\n\n```eim install -i [v5.3.2] -n false```\n\n**List installed ESP-IDF versions**\n\n```eim list```\n\n**Select a version as active** for IDE use\n\n```eim select [version]```\n\n**Rename an installed version**\n\n```eim rename [old_name] [new_name]```\n\n**Remove a specific ESP-IDF version**\n\n```eim remove [version]```"],["SYNOPSIS","**eim** _command_ [_options_]"],["PARAMETERS","_COMMAND_\n> Operation: install, list, select, rename, remove, run, purge.\n\n**install** [**-i** _version_]\n> Install an ESP-IDF version. Runs non-interactively (headless) by default.\n\n**list**\n> List installed ESP-IDF versions.\n\n**select** _version_\n> Set the active ESP-IDF version (updates eim_idf.json for IDE integration).\n\n**rename** _old_ _new_\n> Rename an installed ESP-IDF version.\n\n**remove** _version_\n> Remove a specific ESP-IDF version.\n\n**run** _version_ _command_\n> Run a command in the context of a specific ESP-IDF version.\n\n**purge**\n> Remove all ESP-IDF installations.\n\n**-n** _bool_\n> Set interactive mode (true/false, default: true for headless install).\n\n**--help**\n> Display help information."],["DESCRIPTION","**eim** (ESP-IDF Installation Manager) is a cross-platform tool by Espressif for managing ESP-IDF (Espressif IoT Development Framework) installations. It handles downloading, installing, and switching between multiple ESP-IDF versions for ESP32 development.\n\nThe tool supports both interactive and non-interactive (headless) installation modes, making it suitable for both manual setup and CI/CD automation. It integrates with IDEs through the eim_idf.json configuration file."],["CAVEATS","Requires build dependencies for the ESP-IDF toolchain. Installation may take significant time and disk space. The **purge** command removes all installed versions irreversibly."],["HISTORY","**eim** was developed by **Espressif Systems** as a streamlined installer for the ESP-IDF development framework, replacing manual setup procedures."],["SEE ALSO","[asdf](/man/asdf)(1)"]],"e":["asdf"],"x":"eim install -i [v5.3.2]"},"expect":{"n":"expect","t":"automate interactive applications with scripted responses","s":[["TAGLINE","automate interactive applications with scripted responses"],["TLDR","**Execute** an expect script from file\n\n```expect [path/to/file]```\n\nExecute a **specified** expect script\n\n```expect -c \"[commands]\"```\n\nEnter **interactive** REPL\n\n```expect -i```"],["SYNOPSIS","**expect** [_options_] [_script_] [_arguments_]"],["DESCRIPTION","**expect** automates interactive applications by scripting responses to expected prompts. It spawns processes and responds to their output based on pattern matching.\n\nThe tool uses Tcl scripting language with expect-specific commands like spawn, expect, and send. Scripts can handle multiple response patterns, timeouts, and conditional logic. This enables automation of programs that require interactive input like passwords, confirmations, or menu selections.\n\nCommonly used for automating SSH logins, FTP sessions, database setup scripts, and other interactive programs. While powerful, modern alternatives like SSH keys, API tokens, and configuration management tools are often preferred for security."],["PARAMETERS","**-c** _commands_\n> Execute the given Tcl/Expect commands before the script runs.\n\n**-i**\n> Interactive mode (REPL). Also implied when stdin is a terminal.\n\n**-f** _FILE_\n> Read the command script from FILE.\n\n**-b** _FILE_\n> Read the script line-by-line (buffered mode) — useful for very long scripts.\n\n**-d**\n> Enable diagnostic output (command tracing).\n\n**-D** [_N_]\n> Enter the interactive debugger (on first prompt or after N expect calls).\n\n**-v**\n> Print Expect version and exit.\n\n**--** \n> End of options; following arguments are passed to the script in `$argv`."],["CAVEATS","Uses Tcl scripting language. Sending passwords in scripts has security implications. Consider SSH keys or other authentication methods when possible."],["INSTALL","```apt: sudo apt install expect```\n\n```dnf: sudo dnf install expect```\n\n```pacman: sudo pacman -S expect```\n\n```apk: sudo apk add expect```\n\n```zypper: sudo zypper install expect```\n\n```brew: brew install expect```\n\n```nix: nix profile install nixpkgs#expect```"],["SEE ALSO","[autoexpect](/man/autoexpect)(1), [tcl](/man/tcl)(1), [ssh](/man/ssh)(1)"]],"e":["autoexpect","tcl","ssh"],"x":"expect [path/to/file]"},"fc-scan":{"n":"fc-scan","t":"scan font files and directories","s":[["TAGLINE","scan font files and directories"],["TLDR","**Scan directory for** fonts\n\n```fc-scan [/usr/share/fonts]```\n\n**Scan single font**\n\n```fc-scan [font.ttf]```\n\n**Output specific format**\n\n```fc-scan --format '%{family}: %{file}\\n' [directory]```"],["SYNOPSIS","**fc-scan** [_options_] _files_..."],["PARAMETERS","_FILES_\n> Font files or directories to scan. Directories are scanned recursively.\n\n**-f**, **--format** _FORMAT_\n> Output format string.\n\n**-V**, **--version**\n> Show the program version and exit.\n\n**-h**, **--help**\n> Display help information."],["DESCRIPTION","**fc-scan** scans font files and directories, displaying font information in fontconfig format. Unlike fc-query which examines single files, fc-scan can process directories and multiple files.\n\nThe tool outputs font patterns that fontconfig uses for font matching. Custom format strings extract specific properties. When given a directory, it scans the entire tree recursively.\n\nfc-scan is useful for inventorying fonts, finding duplicates, or generating font lists for documentation."],["CAVEATS","Does not update system font cache. Large directories may take time. Format string syntax can be complex."],["HISTORY","fc-scan is part of **fontconfig**, providing batch font scanning capabilities. It complements fc-query for examining fonts without affecting the system font configuration."],["INSTALL","```apt: sudo apt install fontconfig```\n\n```dnf: sudo dnf install fontconfig```\n\n```pacman: sudo pacman -S fontconfig```\n\n```apk: sudo apk add fontconfig```\n\n```zypper: sudo zypper install fontconfig```\n\n```brew: brew install fontconfig```\n\n```nix: nix profile install nixpkgs#fontconfig```"],["SEE ALSO","[fc-query](/man/fc-query)(1), [fc-list](/man/fc-list)(1), [fc-cache](/man/fc-cache)(1)"],["RESOURCES","```[Source code](https://gitlab.freedesktop.org/fontconfig/fontconfig)```\n\n```[Homepage](https://www.freedesktop.org/wiki/Software/fontconfig/)```"]],"e":["fc-query","fc-list","fc-cache"],"x":"fc-scan [/usr/share/fonts]"},"find2perl":{"n":"find2perl","t":"Translate find command lines to equivalent Perl code","s":[["TAGLINE","Translate find command lines to equivalent Perl code"],["TLDR","**Convert a find command to a Perl script**\n\n```find2perl [directory] -name \"[*.txt]\"```\n\n**Generate a script with -exec translation**\n\n```find2perl [.] -type f -exec cat {} \\;```\n\n**Translate a complex find expression and save to a file**\n\n```find2perl [/var] -mtime +[30] -name \"[*.log]\" > [cleanup.pl]```\n\n**Run the generated Perl script directly**\n\n```find2perl [.] -name \"[*.bak]\" | perl```"],["SYNOPSIS","**find2perl** [_paths_] [_predicates_]"],["PARAMETERS","_PATHS_\n> Starting directories to search.\n\n_PREDICATES_\n> find-style expressions translated to Perl equivalents.\n\n**-name** _PATTERN_\n> Match filename against shell glob pattern.\n\n**-type** _TYPE_\n> File type: f (regular file), d (directory), l (symbolic link).\n\n**-mtime** _N_\n> Modification time in days (+N older, -N newer, N exactly).\n\n**-size** _N_\n> File size.\n\n**-exec** _CMD_\n> Execute command on matching files.\n\n**-print**\n> Print matching paths (default action).\n\n**-depth**\n> Process directory contents before the directory itself."],["DESCRIPTION","**find2perl** translates find command expressions into equivalent Perl scripts using the File::Find module. The generated Perl code typically runs faster than the original find command and can be modified to add custom processing beyond what find supports.\n\nThe tool helps users migrate from shell-based find commands to Perl for complex file processing tasks. The output is a complete Perl script that can be piped directly to perl or saved and modified."],["CAVEATS","Not all find predicates are supported. The generated code may need manual cleanup for edge cases. find2perl was **removed from Perl core in version 5.22.0** (2015) and deprecated since Perl 5.20.0. It is available separately from CPAN as the **App::find2perl** distribution."],["HISTORY","find2perl was included with **Perl** as a utility for translating find commands. It was removed from the Perl core distribution in **Perl 5.22.0** and must now be installed separately from CPAN."],["INSTALL","```apk: sudo apk add perl-app-find2perl```"],["SEE ALSO","[find](/man/find)(1), [perl](/man/perl)(1)"]],"e":["find","perl"],"x":"find2perl [directory] -name \"[*.txt]\""},"flux-create":{"n":"flux-create","t":"Create or update Flux custom resources from the command line","s":[["TAGLINE","Create or update Flux custom resources from the command line"],["TLDR","**Create a Git source**\n\n```flux create source git my-repo --url https://github.com/org/repo --branch main```\n\n**Create a Kustomization** to sync a path from a source\n\n```flux create kustomization my-app --source my-repo --path ./k8s```\n\n**Create a HelmRelease**\n\n```flux create helmrelease my-release --chart mychart --source HelmRepository/myrepo```\n\n**Export YAML** to stdout instead of applying to the cluster\n\n```flux create source git my-repo --url https://github.com/org/repo --branch main --export```\n\n**Create a Git authentication secret**\n\n```flux create secret git my-git-secret --url https://github.com/org/repo --username user --password pass```"],["SYNOPSIS","**flux** **create** _kind_ _name_ [_options_]"],["DESCRIPTION","**flux create** generates Flux custom resources (GitRepository, Kustomization, HelmRelease, HelmRepository, Secret, etc.) without writing YAML by hand.\n\nBy default the resource is applied to the cluster. Pass **--export** to print the resource as YAML on stdout instead (for committing to Git or piping to other tools).\n\nIt supports the major Flux resource kinds and many common configuration options."],["COMMON KINDS","**source git**, **source helm**, **source oci**\n> Create source repositories.\n\n**kustomization**\n> Define a Kustomize sync.\n\n**helmrelease**\n> Deploy a Helm chart.\n\n**secret git**, **secret helm**\n> Create credentials.\n\n**image**, **receiver**, **alert**, **tenant**\n> Image automation, notification receivers, alerts, and multi-tenancy helpers."],["PARAMETERS","**--export**\n> Export the resource in YAML format to stdout instead of applying it to the cluster.\n\n**--interval** _duration_\n> Source sync interval (default `1m0s`).\n\n**--label** _key=value_\n> Set labels on the resource (repeatable / comma-separated).\n\n**-n**, **--namespace** _ns_\n> Namespace scope (default `flux-system`)."],["INSTALL","```apk: sudo apk add flux```\n\n```brew: brew install flux```\n\n```nix: nix profile install nixpkgs#flux```"],["SEE ALSO","[flux](/man/flux)(1), [flux-bootstrap](/man/flux-bootstrap)(1), [flux-build](/man/flux-build)(1), [flux-check](/man/flux-check)(1)"],["RESOURCES","```[Source code](https://github.com/fluxcd/flux2)```\n\n```[Documentation](https://fluxcd.io/flux/cmd/flux_create/)```"]],"e":["flux","flux-bootstrap","flux-build","flux-check"],"x":"flux create source git my-repo --url https://github.com/org/repo --branch main"},"getrandom":{"n":"getrandom","t":"kernel random number generator interface","s":[["TAGLINE","kernel random number generator interface"],["TLDR","**Get random bytes (shell)**\n\n```head -c [16] /dev/urandom | xxd```\n\n**Get random bytes (C function)**\n\n```getrandom(buffer, length, flags)```"],["SYNOPSIS","**#include <sys/random.h>**\n\n**ssize_t getrandom(void** \\*_buf_, **size_t** _buflen_, **unsigned int** _flags_**);**"],["PARAMETERS","_buf_\n> Buffer to receive random bytes.\n\n_buflen_\n> Number of bytes to read.\n\n_flags_\n> GRND_RANDOM (use /dev/random) or GRND_NONBLOCK."],["DESCRIPTION","**getrandom()** is a Linux system call that fills a buffer with random bytes from the kernel's random number generator. It's the recommended interface for obtaining random data in programs.\n\nUnlike reading from /dev/urandom, getrandom() blocks during early boot until the entropy pool is initialized, ensuring strong randomness."],["FLAGS","```\n0             - Default, blocks until entropy available\nGRND_RANDOM   - Use /dev/random pool (may block)\nGRND_NONBLOCK - Don't block, return error instead\n```"],["SHELL ALTERNATIVES","```bash"],["Read random bytes","dd if=/dev/urandom bs=16 count=1"],["Generate random hex","openssl rand -hex 16"],["Generate random base64","openssl rand -base64 16\n```"],["CAVEATS","System call, not a command. Available since Linux 3.17. On older systems, read from /dev/urandom. May block at early boot."],["HISTORY","The getrandom() system call was added to Linux kernel **3.17** in **2014** by Theodore Ts'o to address issues with /dev/urandom returning weak random data before the entropy pool was initialized."],["SEE ALSO","[random](/man/random)(4), [openssl-rand](/man/openssl-rand)(1)"]],"e":["random","openssl-rand"],"x":"head -c [16] /dev/urandom | xxd"},"gh-auth":{"n":"gh-auth","t":"manage GitHub CLI authentication and credentials","s":[["TAGLINE","manage GitHub CLI authentication and credentials"],["TLDR","**Login to GitHub**\n\n```gh auth login```\n\n**Login with specific host**\n\n```gh auth login --hostname [github.example.com]```\n\n**Check authentication status**\n\n```gh auth status```\n\n**Logout from GitHub**\n\n```gh auth logout```\n\n**Refresh authentication**\n\n```gh auth refresh```"],["SYNOPSIS","**gh auth** _command_ [_options_]"],["PARAMETERS","**login**\n> Authenticate with a GitHub host.\n\n**logout**\n> Log out of a GitHub host.\n\n**status**\n> View authentication status.\n\n**refresh**\n> Refresh stored credentials.\n\n**token**\n> Print authentication token.\n\n**--hostname** _HOST_\n> GitHub hostname (for Enterprise).\n\n**--web**\n> Open browser for authentication.\n\n**--with-token**\n> Read token from stdin.\n\n**--help**\n> Display help information."],["DESCRIPTION","**gh auth** manages authentication for the GitHub CLI. It handles OAuth device flow, personal access tokens, and SSH key authentication across multiple GitHub hosts.\n\nThe command stores credentials securely in the system keychain when available, falling back to encrypted file storage. It supports github.com and GitHub Enterprise Server instances simultaneously.\n\ngh auth provides the foundation for all authenticated gh operations."],["CONFIGURATION","**~/.config/gh/hosts.yml**\n> Stores authentication tokens and host-specific settings for each GitHub instance."],["CAVEATS","Token scopes affect available commands. Enterprise instances may require SSO authentication. Credentials stored in keychain or config file."],["HISTORY","gh auth is part of the **GitHub CLI**, providing secure authentication workflows designed for both interactive and scripted use cases."],["INSTALL","```apt: sudo apt install gh```\n\n```dnf: sudo dnf install gh```\n\n```pacman: sudo pacman -S github-cli```\n\n```apk: sudo apk add github-cli```\n\n```zypper: sudo zypper install gh```\n\n```brew: brew install gh```\n\n```nix: nix profile install nixpkgs#gh```"],["SEE ALSO","[gh](/man/gh)(1), [gh-config](/man/gh-config)(1)"],["RESOURCES","```[Documentation](https://cli.github.com/manual/gh_auth)```\n\n```[Source code](https://github.com/cli/cli)```"]],"e":["gh","gh-config"],"x":"gh auth login"},"git-reset-file":{"n":"git-reset-file","t":"Reset a single file to a specific revision","s":[["TAGLINE","Reset a single file to a specific revision"],["TLDR","**Reset file to HEAD**\n\n```git reset-file [file]```\n\n**Reset file to a specific commit**\n\n```git reset-file [file] [commit]```"],["SYNOPSIS","**git** **reset-file** _file_ [_commit_]"],["DESCRIPTION","**git reset-file** resets a single tracked file to its state at HEAD, or at a given commit if one is provided. Part of git-extras, it unstages the file and checks out its content from that revision, without touching the rest of the working tree or index."],["CAVEATS","The file argument comes first, followed by the optional commit; passing them in the wrong order silently fails since the second argument is treated as a commit-ish. The file must currently exist in the working tree."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-reset](/man/git-reset)(1), [git-checkout](/man/git-checkout)(1), [git-extras](/man/git-extras)(1)"],["RESOURCES","```[Source code](https://github.com/tj/git-extras)```\n\n```[Documentation](https://github.com/tj/git-extras/blob/master/Commands.md#git-reset-file)```"]],"e":["git-reset","git-checkout","git-extras"],"x":"git reset-file [file]"},"gomuks":{"n":"gomuks","t":"Terminal Matrix client","s":[["TAGLINE","Terminal Matrix client"],["TLDR","**Connect** to Matrix homeserver\n\n```gomuks```\n\n**Connect** with specific user\n\n```gomuks -u [@user:example.com]```\n\n**Connect** to specific server\n\n```gomuks -s [https://matrix.example.com]```"],["SYNOPSIS","**gomuks** [_options_]"],["PARAMETERS","**-u, --user** _MXID_\n> Matrix user ID (@user:server)\n\n**-s, --server** _URL_\n> Homeserver URL\n\n**-c, --config** _FILE_\n> Configuration file path\n\n**-d, --debug**\n> Enable debug logging\n\n**--version**\n> Display version and exit\n\n**-h, --help**\n> Display help and exit"],["DESCRIPTION","**gomuks** is a terminal-based Matrix client. It provides a text-based interface for the Matrix protocol, supporting end-to-end encryption, rooms, direct messages, and file sharing.\n\nThe client is designed for users who prefer terminal-based workflows and need Matrix access without a GUI."],["KEYBINDINGS","**↑/↓**\n> Navigate messages\n\n**Tab**\n> Switch between rooms\n\n**Ctrl+N**\n> Next room with unread messages\n\n**Ctrl+P**\n> Previous room\n\n**/**\n> Search messages\n\n**: or /**\n> Command mode\n\n**q**\n> Quit\n\n**?**\n> Show help"],["COMMANDS","**/join** _#room:server_\n> Join a room\n\n**/part**\n> Leave current room\n\n**/nick** _name_\n> Change display name\n\n**/me** _action_\n> Send action message\n\n**/react** _emoji_\n> React to message"],["CAVEATS","End-to-end encryption requires device verification. Some Matrix features may not be supported. Media display is limited in terminal. Requires Matrix account."],["HISTORY","**gomuks** was created as a terminal Matrix client for users preferring CLI-based communication tools."],["INSTALL","```apt: sudo apt install gomuks```\n\n```pacman: sudo pacman -S gomuks```\n\n```apk: sudo apk add gomuks```\n\n```zypper: sudo zypper install gomuks```\n\n```nix: nix profile install nixpkgs#gomuks```"],["SEE ALSO","[element](https://element.io/), [fractal](https://gitlab.gnome.org/GNOME/fractal), [matrix-spec](https://spec.matrix.org/)"]],"e":[],"x":"gomuks"},"hp-query":{"n":"hp-query","t":"Looks up static HP printer model information from HPLIP's models database","s":[["TAGLINE","Looks up static HP printer model information from HPLIP's models database"],["TLDR","**Query a single key** for a printer by device URI\n\n```hp-query -d [device-uri] -k [key]```\n\n**Query all keys** for a normalized model name (as used in models.dat)\n\n```hp-query -m [model-name] -a```\n\n**Query a key** using the raw model name from the device's MDL: field\n\n```hp-query -r [model-name] -k [key]```\n\n**Query all keys** with a custom separator\n\n```hp-query -m [model-name] -a -s [tab]```\n\n**Suppress the trailing line feed** on output (useful when scripting)\n\n```hp-query -m [model-name] -k [key] -x```"],["SYNOPSIS","**hp-query** [_MODE_] [_OPTIONS_]"],["PARAMETERS","**-d** _device-uri_, **--device**=_device-uri_\n> Specify the model by CUPS device URI.\n\n**-m** _model_, **--model**=_model_\n> Specify the normalized model name, as used in HPLIP's models.dat.\n\n**-r** _model_, **--raw**=_model_\n> Specify the raw model name, taken from the **MDL:** field of the device's IEEE-1284 device ID.\n\n**-k** _key_, **--key**=_key_\n> Key to query (a models.dat field name).\n\n**-a**, **--all**\n> Query all keys instead of a single one.\n\n**-s** _sep_, **--sep**=_sep_\n> Separator between values when used with **-a**: a literal character, or one of tab, newline, cr, lf, crlf.\n\n**-x**\n> Suppress the trailing linefeed on output.\n\n**-n**, **--non-interactive**\n> Run without interactive prompts.\n\n**-l** _level_, **--logging**=_level_\n> Set logging verbosity: none, info (default), error, warn, or debug.\n\n**-g**\n> Run with debug-level logging (equivalent to **-l debug**).\n\n**-h**, **--help**\n> Display help and exit."],["DESCRIPTION","**hp-query** looks up static model information for an HP printer from HPLIP's internal models database (models.dat), given a device URI, a normalized HPLIP model name, or a raw **MDL:** string taken from the printer's IEEE-1284 device ID.\n\nIt is designed to be called from other programs and installer scripts rather than run interactively, returning one field (**-k**) or all fields (**-a**) about a model: supported data types, required plugins, back-end capabilities, and similar metadata used elsewhere in HPLIP."],["CAVEATS","Part of the HPLIP package. This queries HPLIP's local models database, not the printer directly, so results depend entirely on whether that model is present in models.dat; very new or unlisted models return little or nothing. Resolving a model from **-d** _device-uri_ still requires the device to be reachable long enough to read its device ID."],["HISTORY","**hp-query** is one of the model-lookup utilities in **HPLIP** (HP Linux Imaging and Printing), HP's open-source printer/scanner driver suite for Linux."],["INSTALL","```apt: sudo apt install hplip```\n\n```dnf: sudo dnf install hplip```\n\n```pacman: sudo pacman -S hplip```\n\n```zypper: sudo zypper install hplip```\n\n```nix: nix profile install nixpkgs#hplip```"],["SEE ALSO","[hp-info](/man/hp-info)(1), [hp-setup](/man/hp-setup)(1), [hp-check](/man/hp-check)(1)"],["RESOURCES","```[Source code](https://sourceforge.net/projects/hplip/)```\n\n```[Homepage](https://developers.hp.com/hp-linux-imaging-and-printing)```\n\n```[Documentation](https://developers.hp.com/hp-linux-imaging-and-printing/tech_docs/man_pages/index)```"]],"e":["hp-info","hp-setup","hp-check"],"x":"hp-query -d [device-uri] -k [key]"},"inspect-cert-chain":{"n":"inspect-cert-chain","t":"Inspect and debug TLS certificate chains without OpenSSL","s":[["TAGLINE","Inspect and debug TLS certificate chains without OpenSSL"],["TLDR","**Inspect certificate chain** of a remote host\n\n```inspect-cert-chain [example.com]```\n\n**Inspect with interactive TUI**\n\n```inspect-cert-chain -i [example.com]```\n\n**Inspect a local PEM file**\n\n```inspect-cert-chain --file [path/to/cert.pem]```\n\n**Inspect a specific port**\n\n```inspect-cert-chain [example.com:8443]```"],["SYNOPSIS","**inspect-cert-chain** [**-i**] [**--file** _path_] _host_[_:port_]"],["DESCRIPTION","**inspect-cert-chain** is a CLI and TUI tool for probing and reading TLS certificate chains without relying on **OpenSSL**. It fetches the full chain from a remote server with SNI support, or parses PEM/DER input from local files, and prints concise OpenSSL-style text for every certificate.\n\nThe tool decodes subject, issuer, serial number, validity windows, algorithms, key sizes, X.509 extensions, SCT lists, CRL distribution points, and key-usage bits. The interactive TUI mode (**-i**) lists certificates on the left and shows scrollable detail on the right with Vim-like navigation and mouse support."],["CAVEATS","Does not perform full certificate validation or revocation checking. TUI mode requires a terminal with sufficient width for the split-pane layout."],["HISTORY","**inspect-cert-chain** was designed for sysadmins, pentesters, and developers who need quick certificate inspection without installing or depending on OpenSSL."],["SEE ALSO","[openssl](/man/openssl)(1), [curl](/man/curl)(1)"]],"e":["openssl","curl"],"x":"inspect-cert-chain [example.com]"},"ionic":{"n":"ionic","t":"CLI manages Ionic Framework projects","s":[["TAGLINE","CLI manages Ionic Framework projects"],["TLDR","**Create new app**\n\n```ionic start [myapp] [blank|tabs|sidemenu]```\n\n**Serve locally**\n\n```ionic serve```\n\n**Build for production**\n\n```ionic build --prod```\n\n**Add platform**\n\n```ionic capacitor add [ios|android]```\n\n**Run on device**\n\n```ionic capacitor run [ios|android]```\n\n**Generate component**\n\n```ionic generate component [name]```"],["SYNOPSIS","**ionic** _command_ [_options_]"],["PARAMETERS","**start** _NAME_ _TEMPLATE_\n> Create new project.\n\n**serve**\n> Start development server.\n\n**build**\n> Build web assets.\n\n**capacitor** _COMMAND_\n> Capacitor integration commands.\n\n**generate** _TYPE_ _NAME_\n> Generate components, pages, services.\n\n**--prod**\n> Production build.\n\n**--help**\n> Display help information."],["DESCRIPTION","**Ionic** CLI manages Ionic Framework projects. It creates hybrid mobile apps using web technologies.\n\nThe tool integrates with Capacitor or Cordova for native functionality. It provides development server, build tools, and code generation."],["CAVEATS","Requires Node.js. Native builds need platform SDKs. Framework updates may require migration."],["HISTORY","Ionic was created by **Drifty Co.** in 2013 as a framework for building hybrid mobile applications with web technologies."],["SEE ALSO","[capacitor](/man/capacitor)(1), [cordova](/man/cordova)(1), [npm](/man/npm)(1), [angular](/man/ng)(1)"]],"e":["capacitor","cordova","npm","angular"],"x":"ionic start [myapp] [blank|tabs|sidemenu]"},"ipkg":{"n":"ipkg","t":"lightweight package manager for embedded Linux systems","s":[["TAGLINE","lightweight package manager for embedded Linux systems"],["TLDR","**Update package list**\n\n```ipkg update```\n\n**Install package**\n\n```ipkg install [package]```\n\n**Remove package**\n\n```ipkg remove [package]```\n\n**List installed packages**\n\n```ipkg list_installed```\n\n**Search packages**\n\n```ipkg list | grep [pattern]```\n\n**Upgrade all packages**\n\n```ipkg upgrade```"],["SYNOPSIS","**ipkg** _command_ [_options_] [_package_...]"],["PARAMETERS","**update**\n> Update package list.\n\n**install** _package_\n> Install package.\n\n**remove** _package_\n> Remove package.\n\n**upgrade**\n> Upgrade all packages.\n\n**list**\n> List available packages.\n\n**list_installed**\n> List installed packages.\n\n**info** _package_\n> Show package information.\n\n**status** _package_\n> Show package status.\n\n**download** _package_\n> Download without installing."],["DESCRIPTION","**ipkg** (Itsy Package Management System) is a lightweight package manager for embedded Linux systems. It was designed for devices with limited storage and memory, using a format similar to Debian's dpkg.\n\nipkg was commonly used on OpenWrt, NSLU2, and other embedded devices before being succeeded by opkg."],["CAVEATS","Deprecated in favor of opkg. Limited to embedded systems. Package availability depends on feed configuration. No dependency resolution in some versions."],["HISTORY","ipkg was developed for the NSLU2 Linux community and used by OpenWrt before version 8.09. It was succeeded by **opkg**, which is a fork with improved features and active maintenance."],["INSTALL","```aur: yay -S ipkg```"],["SEE ALSO","[opkg](/man/opkg)(1), [apt](/man/apt)(8), [dpkg](/man/dpkg)(1)"]],"e":["opkg","apt","dpkg"],"x":"ipkg update"},"java":{"n":"java","t":"launches Java applications","s":[["TAGLINE","launches Java applications"],["TLDR","**Run class file**\n\n```java [ClassName]```\n\n**Run JAR file**\n\n```java -jar [application.jar]```\n\n**Run with classpath**\n\n```java -cp [path/to/classes] [ClassName]```\n\n**Set heap size**\n\n```java -Xmx[512m] -jar [app.jar]```\n\n**Run with system property**\n\n```java -D[property=value] [ClassName]```\n\n**Show version**\n\n```java -version```\n\n**Run single source file**\n\n```java [Source.java]```"],["SYNOPSIS","**java** [_options_] _class_ [_args_...]\n**java** [_options_] **-jar** _jarfile_ [_args_...]"],["PARAMETERS","_CLASS_\n> Main class to execute.\n\n**-jar** _FILE_\n> Execute JAR file.\n\n**-cp** _PATH_\n> Class path for dependencies.\n\n**-Xmx** _SIZE_\n> Maximum heap size.\n\n**-Xms** _SIZE_\n> Initial heap size.\n\n**-D** _PROP=VAL_\n> Set system property.\n\n**-Xss** _SIZE_\n> Thread stack size.\n\n**-ea**, **--enableassertions**\n> Enable assertions.\n\n**--enable-preview**\n> Enable preview language features.\n\n**-verbose** _:class|:gc|:jni_\n> Enable verbose output.\n\n**-version**\n> Show version information.\n\n**--help**\n> Display help information."],["DESCRIPTION","**java** launches Java applications by loading and executing compiled bytecode in the Java Virtual Machine (JVM). It supports class files, JAR archives, and since Java 11, single source-file programs.\n\nThe JVM handles memory management (garbage collection), threading, and platform abstraction. Memory is configured via **-Xms** (initial heap), **-Xmx** (max heap), and **-Xss** (stack size). System properties (**-D**) configure application behavior at runtime."],["CAVEATS","Requires JRE/JDK. Version compatibility matters. Memory settings may need tuning."],["HISTORY","Java was created by **James Gosling** at **Sun Microsystems** in 1995. It's now maintained by **Oracle** and the OpenJDK community."],["INSTALL","```apk: sudo apk add java-common```\n\n```brew: brew install java```"],["SEE ALSO","[javac](/man/javac)(1), [jar](/man/jar)(1), [jps](/man/jps)(1), [jstack](/man/jstack)(1)"]],"e":["javac","jar","jps","jstack"],"x":"java [ClassName]"},"jmap":{"n":"jmap","t":"prints memory-related statistics for Java processes","s":[["TAGLINE","prints memory-related statistics for Java processes"],["TLDR","**Print class loader statistics**\n\n```jmap -clstats [pid]```\n\n**Dump heap to file**\n\n```jmap -dump:format=b,file=[heap.hprof] [pid]```\n\n**Dump live objects only**\n\n```jmap -dump:live,format=b,file=[heap.hprof] [pid]```\n\n**Histogram of objects**\n\n```jmap -histo [pid]```\n\n**Live objects histogram**\n\n```jmap -histo:live [pid]```"],["SYNOPSIS","**jmap** [_options_] _pid_"],["PARAMETERS","_PID_\n> Target Java process ID.\n\n**-clstats** _pid_\n> Print class loader statistics of the Java heap.\n\n**-finalizerinfo** _pid_\n> Print information on objects awaiting finalization.\n\n**-histo**[**:live**]\n> Print histogram of the Java object heap. With :live, counts only live objects.\n\n**-dump:**_options_\n> Dump the Java heap. Sub-options:\n\n**:live**\n> Only live objects.\n\n**:format=b**\n> Binary hprof format.\n\n**:file=**_FILE_\n> Output file path."],["DESCRIPTION","**jmap** prints memory-related statistics for Java processes. It generates heap dumps, object histograms, and class loader statistics.\n\nThe tool is essential for memory analysis and leak detection. Heap dumps can be analyzed with tools like Eclipse MAT or VisualVM."],["CAVEATS","Part of JDK. This command is experimental and unsupported; it may not be available in future JDK releases. May pause the application during heap dump. Large heaps create large dump files."],["HISTORY","jmap has been part of the **JDK** since Java 5, providing memory analysis capabilities for running Java processes."],["SEE ALSO","[jhat](/man/jhat)(1), [jstack](/man/jstack)(1), [jinfo](/man/jinfo)(1)"]],"e":["jhat","jstack","jinfo"],"x":"jmap -clstats [pid]"},"kakoune":{"n":"kakoune","t":"Modal text editor with multiple selections as a core concept","s":[["TAGLINE","Modal text editor with multiple selections as a core concept"],["TLDR","**Open a file**\n\n```kak [path/to/file]```\n\n**Open multiple files**\n\n```kak [file1] [file2]```\n\n**Open at a specific line**\n\n```kak +[line] [path/to/file]```\n\n**Start in a named session**\n\n```kak -s [session_name] [path/to/file]```\n\n**Connect to an existing session**\n\n```kak -c [session_name]```\n\n**Run in filter mode** (apply commands to stdin)\n\n```echo \"[text]\" | kak -f '[commands]'```"],["SYNOPSIS","**kak** [**-s** _session_] [**-c** _session_] [**-f** _commands_] [**+**_line_] [_file_...]"],["PARAMETERS","**-s** _SESSION_\n> Start a named session\n\n**-c** _SESSION_\n> Connect to an existing session\n\n**-f** _COMMANDS_\n> Run commands on stdin (filter mode)\n\n**-e** _COMMANDS_\n> Execute commands after startup\n\n**-n**\n> Don't load kakrc configuration\n\n**-l**\n> List existing sessions\n\n**-d**\n> Run as daemon (no UI)"],["DESCRIPTION","**Kakoune** (invoked as **kak**) is a modal text editor inspired by Vim but designed around **multiple selections** as its central editing primitive. Instead of operating on a single cursor, most operations in Kakoune work on one or more selections simultaneously, enabling powerful text manipulation with fewer keystrokes.\n\nKakoune follows the **selection → action** model (as opposed to Vim's action → motion), meaning you first select text (using regex, text objects, or other methods), see what's selected, and then apply an operation. The editor supports client-server architecture for collaborative editing, built-in syntax highlighting, and extensive extensibility through its scripting language."],["CAVEATS","The selection-first paradigm differs from Vim's muscle memory and requires adjustment. Plugin ecosystem is smaller than Vim's. Configuration uses its own scripting language rather than a mainstream language."],["HISTORY","**Kakoune** was created by **Maxime Coste** (mawww) and is written in **C++**. Development began around **2011** as an experiment to improve upon Vim's editing model. The multiple-selections approach inspired later editors including **Helix**."],["INSTALL","```dnf: sudo dnf install kakoune```\n\n```pacman: sudo pacman -S kakoune```\n\n```zypper: sudo zypper install kakoune```\n\n```brew: brew install kakoune```\n\n```nix: nix profile install nixpkgs#kakoune```"],["SEE ALSO","[vim](/man/vim)(1), [helix](/man/helix)(1), [neovim](/man/neovim)(1)"]],"e":["vim","helix","neovim"],"x":"kak [path/to/file]"},"kerl":{"n":"kerl","t":"manages multiple Erlang/OTP installations","s":[["TAGLINE","manages multiple Erlang/OTP installations"],["TLDR","**List available releases**\n\n```kerl list releases```\n\n**Build Erlang version**\n\n```kerl build [26.0] [26.0-build]```\n\n**Install built version**\n\n```kerl install [26.0-build] [~/.kerl/26.0]```\n\n**Activate installation**\n\n```source ~/.kerl/26.0/activate```\n\n**List installations**\n\n```kerl list installations```\n\n**Show status of builds and installations**\n\n```kerl status```\n\n**Delete build**\n\n```kerl delete build [26.0-build]```\n\n**Clean build artifacts**\n\n```kerl cleanup [26.0-build]```"],["SYNOPSIS","**kerl** _command_ [_args_]"],["PARAMETERS","**list releases**\n> Show available versions.\n\n**list builds**\n> Show completed builds.\n\n**list installations**\n> Show installed versions.\n\n**build** _VSN_ _NAME_\n> Build Erlang version.\n\n**install** _BUILD_ _PATH_\n> Install build to path.\n\n**delete** _TYPE_ _NAME_\n> Delete build or installation.\n\n**update releases**\n> Fetch up-to-date list of available releases.\n\n**build-install** _VSN_ _NAME_ _PATH_\n> Combine build and install in one step.\n\n**status**\n> Print available builds and installations.\n\n**path** [_NAME_]\n> Print path of active or named installation.\n\n**active**\n> Print the currently active installation.\n\n**cleanup** _NAME_\n> Remove compilation artifacts for a build.\n\n**deploy** _HOST_ _DIR_\n> Deploy installation to remote host via SSH."],["DESCRIPTION","**kerl** manages multiple Erlang/OTP installations. It builds and installs different versions side by side.\n\nThe tool handles compilation from source. Activation scripts set up the environment for each version."],["CAVEATS","Builds from source, so build dependencies (gcc, make, ncurses-dev, libssl-dev, etc.) must be installed. Shell script tool — configure via **$HOME/.kerlrc**. Set **KERL_INCLUDE_RELEASE_CANDIDATES=yes** to include release candidates."],["INSTALL","```brew: brew install kerl```"],["SEE ALSO","[erl](/man/erl)(1), [asdf](/man/asdf)(1)"]],"e":["erl","asdf"],"x":"kerl list releases"},"kerneloops":{"n":"kerneloops","t":"collects and submits kernel crash information","s":[["TAGLINE","collects and submits kernel crash information"],["TLDR","**Start kerneloops daemon**\n\n```sudo kerneloops```\n\n**Run in debug mode** without daemonizing\n\n```kerneloops --debug```\n\n**Parse a specific file** for kernel oops messages\n\n```kerneloops --file [/var/log/messages]```\n\n**Run in foreground** without becoming a daemon\n\n```kerneloops --nodaemon```"],["SYNOPSIS","**kerneloops** [_options_]"],["PARAMETERS","**--debug**\n> Enable debug mode with additional diagnostic output.\n\n**--file** _filename_\n> Parse the specified file as if it were /var/log/messages.\n\n**--nodaemon**\n> Do not daemonize; run in the foreground."],["DESCRIPTION","**kerneloops** collects kernel crash information (oops messages) and submits the extracted signatures to the oops.kernel.org website for statistical analysis and presentation to Linux kernel developers.\n\nThe daemon monitors /var/log/messages for kernel oops data and automatically submits it. Configuration is stored in /etc/kerneloops.conf."],["CAVEATS","Submitted oops data may contain kernel state information. Requires network connectivity to submit reports to oops.kernel.org. The daemon typically runs as root."],["HISTORY","kerneloops was created by **Intel** to collect kernel crash data and help identify bugs in the Linux kernel."],["SEE ALSO","[dmesg](/man/dmesg)(1), [journalctl](/man/journalctl)(1)"]],"e":["dmesg","journalctl"],"x":"sudo kerneloops"},"koji":{"n":"koji","t":"command-line client for Koji, the build system used by Fedora, CentOS","s":[["TAGLINE","command-line client for Koji, the build system used by Fedora, CentOS"],["TLDR","Execute a **subcommand**\n\n```koji [subcommand]```\n\nTest **connectivity** with kojihub\n\n```koji moshimoshi```\n\nDisplay **global help**\n\n```koji -h```\n\nDisplay **all commands**\n\n```koji help```\n\nDisplay help for a **specific subcommand**\n\n```koji [subcommand] -h```\n\nDisplay **version**\n\n```koji version```"],["SYNOPSIS","**koji** [_options_] _subcommand_ [_arguments_]"],["PARAMETERS","**moshimoshi**\n> Test connectivity with the koji hub\n\n**help**\n> Display all available commands\n\n**build**\n> Submit a build task\n\n**tag-build**\n> Tag a build\n\n**download-build**\n> Download build artifacts\n\n**buildinfo**\n> Display build information\n\n**call**\n> Execute an arbitrary XML-RPC call\n\n**version**\n> Display version information"],["DESCRIPTION","**koji** is the command-line client for Koji, the build system used by Fedora, CentOS, and other RPM-based distributions. It interfaces with the Koji hub to submit builds, manage tags, and download packages.\n\nKoji handles the entire build process including package building, testing, and distribution. Developers use it to submit source packages for compilation across multiple architectures."],["CAVEATS","Requires proper authentication configuration. Some operations require specific permissions. Network access to the Koji hub is required."],["HISTORY","Koji was developed by Red Hat and released in **2007** as the build system for Fedora. The name \"Koji\" comes from the Japanese word for yeast or mold used in brewing, reflecting its role in \"brewing\" packages."],["INSTALL","```dnf: sudo dnf install koji```\n\n```brew: brew install koji```\n\n```nix: nix profile install nixpkgs#koji```"],["SEE ALSO","[mock](/man/mock)(1), [rpmbuild](/man/rpmbuild)(8)"]],"e":["mock","rpmbuild"],"x":"koji [subcommand]"},"lft":{"n":"lft","t":"layer Four Traceroute","s":[["TAGLINE","layer Four Traceroute"],["TLDR","**Traceroute to host**\n\n```lft [hostname]```\n\n**Use specific port**\n\n```lft -d [80] [hostname]```\n\n**TCP mode**\n\n```lft -T [hostname]```\n\n**UDP mode**\n\n```lft -U [hostname]```\n\n**Show ASN info**\n\n```lft -A [hostname]```\n\n**Set max TTL**\n\n```lft -m [30] [hostname]```"],["SYNOPSIS","**lft** [_options_] _host_"],["PARAMETERS","_HOST_\n> Target hostname or IP.\n\n**-d** _PORT_\n> Destination port.\n\n**-T**\n> TCP mode.\n\n**-U**\n> UDP mode.\n\n**-A**\n> Show AS numbers.\n\n**-m** _TTL_\n> Maximum TTL.\n\n**-n**\n> No DNS resolution.\n\n**--help**\n> Display help information."],["DESCRIPTION","**lft** is Layer Four Traceroute. It traces routes using TCP, UDP, or ICMP at layer 4.\n\nThe tool bypasses many firewalls that block ICMP. It shows AS numbers for network analysis."],["CAVEATS","May need root. Firewall-friendly. Network diagnostic tool."],["HISTORY","LFT was created by **Victor Oppleman** as a firewall-friendly alternative to traditional traceroute."],["INSTALL","```apt: sudo apt install lft```\n\n```brew: brew install lft```"],["SEE ALSO","[traceroute](/man/traceroute)(1), [mtr](/man/mtr)(1), [tcptraceroute](/man/tcptraceroute)(1)"]],"e":["traceroute","mtr","tcptraceroute"],"x":"lft [hostname]"},"llvd":{"n":"llvd","t":"downloads videos from LinkedIn Learning","s":[["TAGLINE","downloads videos from LinkedIn Learning"],["TLDR","**Download LinkedIn video**\n\n```llvd [video_url]```\n\n**Specify output file**\n\n```llvd -o [output.mp4] [video_url]```\n\n**Download with quality**\n\n```llvd -q [720] [video_url]```\n\n**List available formats**\n\n```llvd -F [video_url]```\n\n**Download subtitles**\n\n```llvd --write-subs [video_url]```"],["SYNOPSIS","**llvd** [_options_] _url_"],["PARAMETERS","_URL_\n> LinkedIn Learning video URL.\n\n**-o** _FILE_\n> Output filename.\n\n**-q** _QUALITY_\n> Video quality (360, 480, 720, 1080).\n\n**-F**\n> List available formats.\n\n**--write-subs**\n> Download subtitles.\n\n**--help**\n> Display help information."],["DESCRIPTION","**llvd** downloads videos from LinkedIn Learning. It supports various quality levels and subtitle extraction.\n\nThe tool requires LinkedIn credentials for authenticated access to learning content."],["CAVEATS","Requires LinkedIn account. Terms of service considerations. Authentication needed."],["HISTORY","llvd was created to enable offline access to LinkedIn Learning video content for authorized users."],["SEE ALSO","[youtube-dl](/man/youtube-dl)(1), [yt-dlp](/man/yt-dlp)(1)"]],"e":["youtube-dl","yt-dlp"],"x":"llvd [video_url]"},"ls":{"n":"ls","t":"lists directory contents","s":[["TAGLINE","lists directory contents"],["TLDR","**List files**\n\n```ls```\n\n**List with details**\n\n```ls -l```\n\n**List all including hidden**\n\n```ls -la```\n\n**Human readable sizes**\n\n```ls -lh```\n\n**Sort by time**\n\n```ls -lt```\n\n**Sort by size**\n\n```ls -lS```\n\n**Recursive listing**\n\n```ls -R```"],["SYNOPSIS","**ls** [_options_] [_files_]"],["PARAMETERS","_FILES_\n> Files or directories to list.\n\n**-l**\n> Long listing format.\n\n**-a**\n> Include hidden files.\n\n**-h**\n> Human readable sizes.\n\n**-t**\n> Sort by modification time.\n\n**-S**\n> Sort by size.\n\n**-R**\n> Recursive listing.\n\n**-r**\n> Reverse sort order.\n\n**-1**\n> One file per line.\n\n**-d**\n> List directories themselves, not contents.\n\n**--color** _WHEN_\n> Colorize output (auto, always, never).\n\n**-i**\n> Print inode number.\n\n**-F**, **--classify**\n> Append indicator (/ for dirs, * for executables)."],["DESCRIPTION","**ls** lists directory contents. Without arguments, it lists the current directory. With **-l**, it shows permissions, owner, size, and modification time for each entry.\n\nEntries are sorted alphabetically by default. Sorting can be changed with **-t** (time), **-S** (size), or **-r** (reverse). The **--color** option highlights files by type (directories, executables, symlinks)."],["CAVEATS","Output varies by implementation. Colors need terminal support. Hidden files start with dot."],["HISTORY","ls dates back to the original **Unix** system at AT&T Bell Labs in 1971, remaining a fundamental command."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[dir](/man/dir)(1), [exa](/man/exa)(1), [lsd](/man/lsd)(1), [tree](/man/tree)(1)"]],"e":["dir","exa","lsd","tree"],"x":"ls"},"mitmproxy":{"n":"mitmproxy","t":"interactive man-in-the-middle proxy for HTTP and HTTPS traffic","s":[["TAGLINE","interactive man-in-the-middle proxy for HTTP and HTTPS traffic"],["TLDR","**Start mitmproxy** on the default port (8080)\n\n```mitmproxy```\n\n**Start on a specific port**\n\n```mitmproxy -p [8888]```\n\n**Start in reverse proxy mode**\n\n```mitmproxy -m reverse:https://[example.com]```\n\n**Start in transparent proxy mode**\n\n```mitmproxy -m transparent```\n\n**Load a Python script** for traffic manipulation\n\n```mitmproxy -s [path/to/script.py]```\n\n**Read flows from a file**\n\n```mitmproxy -r [flows.mitm]```\n\n**Ignore specific hosts**\n\n```mitmproxy --ignore-hosts '^example\\.com$'```\n\n**Start web interface** instead of console\n\n```mitmweb```"],["SYNOPSIS","**mitmproxy** [_options_]\n\n**mitmdump** [_options_]\n\n**mitmweb** [_options_]"],["PARAMETERS","**-p** _PORT_, **--listen-port** _PORT_\n> Proxy service port (default: 8080)\n\n**-m** _MODE_, **--mode** _MODE_\n> Proxy mode: regular, transparent, socks5, reverse:SPEC, upstream:SPEC\n\n**-T**, **--transparent**\n> Enable transparent proxy mode\n\n**--socks**\n> Enable SOCKS5 proxy mode\n\n**-R** _URL_, **--reverse** _URL_\n> Reverse proxy to upstream server (http[s]://host[:port])\n\n**-s** _SCRIPT_\n> Path to Python script for flow manipulation\n\n**-r** _FILE_\n> Read flows from file\n\n**-w** _FILE_\n> Write flows to file\n\n**--ignore-hosts** _REGEX_\n> Regex matching hosts to ignore (not intercepted)\n\n**--allow-hosts** _REGEX_\n> Regex matching hosts to intercept\n\n**--anticache**\n> Strip cache headers to prevent 304 responses\n\n**--insecure**\n> Do not verify upstream SSL/TLS certificates\n\n**--cert** _SPEC_\n> Add SSL certificate (path or directory)\n\n**--cadir** _DIR_\n> Location of CA files (default: ~/.mitmproxy)\n\n**--no-http2**\n> Disable HTTP/2 support\n\n**-q**, **--quiet**\n> Quiet mode\n\n**-n**, **--no-server**\n> Don't start a proxy server\n\n**--options**\n> Dump all options as YAML\n\n**-h**, **--help**\n> Show help message"],["DESCRIPTION","**mitmproxy** is an interactive man-in-the-middle proxy for HTTP and HTTPS traffic. It allows inspection, modification, and replay of web traffic through a console interface. The suite includes three tools: **mitmproxy** (console UI), **mitmweb** (web UI), and **mitmdump** (command-line only).\n\nThe proxy works by intercepting connections and presenting its own certificate to clients. Install the mitmproxy CA certificate on clients to avoid SSL warnings. Traffic flows can be filtered, modified with Python scripts, or saved for later analysis.\n\nProxy modes include regular (explicit proxy), transparent (network-level interception), reverse (forwarding to upstream), and SOCKS5. Python addons can modify requests and responses, implement custom logic, or integrate with other tools."],["CAVEATS","Intercepting HTTPS requires clients to trust the mitmproxy CA certificate, which can be found in **~/.mitmproxy**. Transparent mode requires additional system configuration (iptables on Linux). Some applications implement certificate pinning and will refuse connections through mitmproxy. Intercepting traffic without authorization may violate laws."],["HISTORY","mitmproxy was created by **Aldo Cortesi** and first released in **2010**. Written in **Python**, it became a standard tool for web application security testing and debugging. The project introduced innovative features like the web interface (mitmweb) and a powerful scripting API. It gained widespread adoption among security researchers, developers, and QA engineers for analyzing HTTP traffic."],["INSTALL","```pacman: sudo pacman -S mitmproxy```\n\n```apk: sudo apk add mitmproxy```\n\n```nix: nix profile install nixpkgs#mitmproxy```"],["SEE ALSO","[burpsuite](/man/burpsuite)(1), [wireshark](/man/wireshark)(1), [tcpdump](/man/tcpdump)(1), [charles](/man/charles)(1)"]],"e":["burpsuite","wireshark","tcpdump","charles"],"x":"mitmproxy"},"mlabel":{"n":"mlabel","t":"adds, displays, or removes volume labels on MS-DOS FAT and VFAT filesystems","s":[["TAGLINE","adds, displays, or removes volume labels on MS-DOS FAT and VFAT filesystems"],["TLDR","**Set a volume label** on a FAT/VFAT filesystem\n\n```mlabel -i [/dev/sda] ::\"[new_label]\"```\n\n**Show current volume label**\n\n```mlabel -s -i [/dev/sda] ::```\n\n**Clear** existing volume label\n\n```mlabel -c -i [/dev/sda] ::```\n\nSet a **new random serial number**\n\n```mlabel -n -i [/dev/sda] ::```\n\nSet a **specific serial number** (8 hex digits)\n\n```mlabel -N [12345678] -i [/dev/sda] ::```"],["SYNOPSIS","**mlabel** [**-v**] [**-c**] [**-s**] [**-n**] [**-N** _serial_] _drive:_[_new_label_]"],["PARAMETERS","**-c**\n> Clear existing label without prompting\n\n**-s**\n> Show existing label without prompting\n\n**-n**\n> Assign new random serial number to disk\n\n**-N _serial_**\n> Set specific serial number; 8-digit hexadecimal, no spaces\n\n**-v**\n> Verbose mode; display new label if changed\n\n**-i _device_**\n> Specify device directly instead of using drive letter"],["DESCRIPTION","**mlabel** adds, displays, or removes volume labels on MS-DOS FAT and VFAT filesystems. When invoked without options, it displays the current label and prompts for a new one.\n\nVolume labels are limited to 11 single-byte characters. The tool validates and adjusts labels to ensure MS-DOS compatibility. An empty label input (pressing return at prompt) deletes the existing label.\n\nmlabel is part of the **mtools** package, which provides utilities for accessing MS-DOS filesystems from Unix/Linux without mounting."],["CAVEATS","Labels are limited to 11 characters and must be valid MS-DOS format. Invalid characters are automatically corrected. The serial number is stored in the boot sector and used by Windows for volume identification."],["HISTORY","**mlabel** is part of **mtools**, originally developed by Emmet P. Gray in the late 1980s. Mtools provides MS-DOS file system utilities for Unix systems, allowing access to FAT filesystems without mounting."],["INSTALL","```apt: sudo apt install mtools```\n\n```dnf: sudo dnf install mtools```\n\n```pacman: sudo pacman -S mtools```\n\n```apk: sudo apk add mtools```\n\n```zypper: sudo zypper install mtools```\n\n```brew: brew install mtools```\n\n```nix: nix profile install nixpkgs#mtools```"],["SEE ALSO","[fatlabel](/man/fatlabel)(8)"]],"e":["fatlabel"],"x":"mlabel -i [/dev/sda] ::\"[new_label]\""},"msfconsole":{"n":"msfconsole","t":"metasploit Framework console","s":[["TAGLINE","metasploit Framework console"],["TLDR","**Start Metasploit console**\n\n```msfconsole```\n\n**Execute resource script**\n\n```msfconsole -r [script.rc]```\n\n**Execute single command**\n\n```msfconsole -x \"[use exploit/multi/handler; run]\"```\n\n**Quiet mode**\n\n```msfconsole -q```\n\n**Connect to database**\n\n```msfconsole -d```\n\n**Show version**\n\n```msfconsole -v```"],["SYNOPSIS","**msfconsole** [_options_]"],["PARAMETERS","**-r** _FILE_\n> Execute resource script.\n\n**-x** _CMD_\n> Execute command string.\n\n**-q**\n> Quiet mode, no banner.\n\n**-d**\n> Connect to database.\n\n**-v**\n> Show version.\n\n**--help**\n> Display help information."],["DESCRIPTION","**msfconsole** is the Metasploit Framework console. It provides access to exploits and security tools.\n\nThe tool is the primary Metasploit interface. Used for penetration testing and security research."],["CAVEATS","Authorized testing only. Requires proper setup. Resource intensive."],["HISTORY","msfconsole is part of **Metasploit Framework**, created by H.D. Moore in 2003 for penetration testing."],["SEE ALSO","[msfvenom](/man/msfvenom)(1), [nmap](/man/nmap)(1), [msfpc](/man/msfpc)(1)"]],"e":["msfvenom","nmap","msfpc"],"x":"msfconsole"},"n":{"n":"n","t":"node","s":[["TAGLINE","node"],["TLDR","**Install latest Node.js**\n\n```n latest```\n\n**Install LTS version**\n\n```n lts```\n\n**Install specific version**\n\n```n [18.16.0]```\n\n**List installed versions**\n\n```n ls```\n\n**Switch to installed version**\n\n```n```\n\n**Remove version**\n\n```n rm [18.16.0]```\n\n**Run specific version**\n\n```n run [18.16.0] [script.js]```\n\n**Purge all versions**\n\n```n prune```"],["SYNOPSIS","**n** [_command_] [_version_]"],["PARAMETERS","_VERSION_\n> Node.js version number.\n\n**latest**\n> Install latest version.\n\n**lts**\n> Install LTS version.\n\n**ls**\n> List installed versions.\n\n**rm** _VERSION_\n> Remove version.\n\n**run** _VERSION_\n> Run with specific version.\n\n**prune**\n> Remove old versions.\n\n**--help**\n> Display help information."],["DESCRIPTION","**n** is a Node.js version manager. It installs and switches between Node.js versions.\n\nThe tool manages multiple installations. Simple interface for version control."],["CAVEATS","Requires sudo for global install. Simpler than nvm. Bash script based."],["HISTORY","n was created by **TJ Holowaychuk** as a simpler alternative to nvm for Node.js version management."],["INSTALL","```brew: brew install n```"],["SEE ALSO","[nvm](/man/nvm)(1), [node](/man/node)(1), [npm](/man/npm)(1)"]],"e":["nvm","node","npm"],"x":"n latest"},"neutron":{"n":"neutron","t":"openStack networking service","s":[["TAGLINE","openStack networking service"],["TLDR","**List networks**\n\n```openstack network list```\n\n**Create network**\n\n```openstack network create [network_name]```\n\n**Create subnet**\n\n```openstack subnet create --network [network] --subnet-range [192.168.1.0/24] [subnet_name]```\n\n**Create router**\n\n```openstack router create [router_name]```\n\n**List ports**\n\n```openstack port list```\n\n**Show network details**\n\n```openstack network show [network_name]```"],["SYNOPSIS","**openstack network** _command_ [_options_]"],["PARAMETERS","**list**\n> List networks.\n\n**create**\n> Create network.\n\n**delete**\n> Delete network.\n\n**show**\n> Show network details.\n\n**set**\n> Modify network."],["DESCRIPTION","**Neutron** is the OpenStack networking service (now accessed via unified openstack client). It provides network connectivity as a service, managing networks, subnets, routers, and ports."],["NETWORK TYPES","```\nflat       - Untagged network\nvlan       - VLAN segmentation\nvxlan      - Virtual overlay\ngre        - GRE tunnels\ngeneve     - Geneve encapsulation\n```"],["COMPONENTS","```\nneutron-server      - API server\nneutron-l2-agent    - L2 connectivity\nneutron-l3-agent    - L3 routing\nneutron-dhcp-agent  - DHCP service\n```"],["CAVEATS","Requires OpenStack credentials. Plugin determines capabilities. Complex multi-tenant networking."],["HISTORY","Neutron was originally called Quantum, renamed in **2013**. It's part of OpenStack's core services for software-defined networking."],["INSTALL","```aur: yay -S neutron```"],["SEE ALSO","[openstack](/man/openstack)(1), [nova](/man/nova)(1)"]],"e":["openstack","nova"],"x":"openstack network list"},"nix-env":{"n":"nix-env","t":"manages Nix user environments","s":[["TAGLINE","manages Nix user environments"],["TLDR","**Install package**\n\n```nix-env -iA nixpkgs.[package]```\n\n**Uninstall package**\n\n```nix-env -e [package]```\n\n**List installed packages**\n\n```nix-env -q```\n\n**Search packages**\n\n```nix-env -qaP [pattern]```\n\n**Upgrade all packages**\n\n```nix-env -u```\n\n**Rollback to previous generation**\n\n```nix-env --rollback```\n\n**List generations**\n\n```nix-env --list-generations```"],["SYNOPSIS","**nix-env** [_options_] _operation_"],["PARAMETERS","**-i**, **--install**\n> Install packages.\n\n**-e**, **--uninstall**\n> Remove packages.\n\n**-u**, **--upgrade**\n> Upgrade packages.\n\n**-q**, **--query**\n> Query packages.\n\n**-A**, **--attr**\n> Install by attribute.\n\n**--rollback**\n> Rollback generation.\n\n**-p** _profile_\n> Use profile."],["DESCRIPTION","**nix-env** manages Nix user environments. It installs, upgrades, and removes packages in user profiles, with the ability to rollback to previous states.\n\nEach operation creates a new generation, preserving previous states."],["PROFILES","```\n~/.nix-profile        - Default profile\n/nix/var/nix/profiles - System profiles\n```"],["CAVEATS","Consider using declarative config (home-manager) instead. Generations use disk space. -A for attribute paths is faster."],["HISTORY","nix-env is part of **Nix**, a purely functional package manager created by **Eelco Dolstra** in his 2006 PhD thesis at Utrecht University."],["INSTALL","```apt: sudo apt install nix-bin```\n\n```dnf: sudo dnf install nix```\n\n```pacman: sudo pacman -S nix```\n\n```apk: sudo apk add nix```\n\n```zypper: sudo zypper install nix```\n\n```nix: nix profile install nixpkgs#nix```"],["SEE ALSO","[nix](/man/nix)(1), [nix-shell](/man/nix-shell)(1), [nix-store](/man/nix-store)(1), [home-manager](/man/home-manager)(1)"]],"e":["nix","nix-shell","nix-store","home-manager"],"x":"nix-env -iA nixpkgs.[package]"},"npm-search":{"n":"npm-search","t":"searches the npm registry for packages matching the given terms","s":[["TAGLINE","searches the npm registry for packages matching the given terms"],["TLDR","**Search for packages**\n\n```npm search [query]```\n\n**Search with multiple terms**\n\n```npm search [react] [router]```\n\n**Output as JSON**\n\n```npm search --json [query]```\n\n**Limit results**\n\n```npm search --searchlimit=[20] [query]```\n\n**Long format output**\n\n```npm search --long [query]```"],["SYNOPSIS","**npm search** [_options_] [_terms_...]"],["PARAMETERS","**--json**\n> JSON output.\n\n**--long**\n> Extended information.\n\n**--searchlimit** _n_\n> Limit results.\n\n**--searchopts** _opts_\n> Search options.\n\n**--searchexclude** _terms_\n> Exclude terms.\n\n**--registry** _url_\n> Search specific registry."],["DESCRIPTION","**npm search** searches the npm registry for packages matching the given terms. It searches package names, descriptions, and keywords.\n\nResults include package name, description, author, date, version, and keywords."],["EXAMPLE OUTPUT","```\nNAME         DESCRIPTION                    AUTHOR    DATE\nlodash       Lodash modular utilities       jdalton   2023-01\nexpress      Fast web framework             dougwilson 2022-10\n```"],["ALTERNATIVES","```bash"],["Use npm-search-cli","npms query\n```"],["CAVEATS","Registry search can be slow. Results may differ from website. Consider web interface for complex searches."],["HISTORY","npm search has been part of npm since early versions, providing command-line access to the npm registry search functionality."],["INSTALL","```pacman: sudo pacman -S npm```\n\n```apk: sudo apk add npm```\n\n```brew: brew install npm```"],["SEE ALSO","[npm](/man/npm)(1), [npm-view](/man/npm-view)(1)"]],"e":["npm","npm-view"],"x":"npm search [query]"},"opensc-tool":{"n":"opensc-tool","t":"diagnostic tool for smart cards","s":[["TAGLINE","diagnostic tool for smart cards"],["TLDR","**List readers**\n\n```opensc-tool --list-readers```\n\n**Show card info**\n\n```opensc-tool --info```\n\n**Show ATR (Answer To Reset)**\n\n```opensc-tool --atr```\n\n**List card files**\n\n```opensc-tool --list-files```\n\n**Send APDU command**\n\n```opensc-tool --send-apdu [00:A4:04:00:00]```\n\n**Verbose output**\n\n```opensc-tool -v --info```"],["SYNOPSIS","**opensc-tool** [_options_]"],["PARAMETERS","**-l**, **--list-readers**\n> List all configured readers.\n\n**-i**, **--info**\n> Print information about OpenSC (version and enabled components).\n\n**-a**, **--atr**\n> Print the Answer To Reset (ATR) of the card in hex.\n\n**-s** _apdu_, **--send-apdu** _apdu_\n> Send an arbitrary APDU command to the card.\n\n**-f**, **--list-files**\n> Recursively list all files stored on the card.\n\n**--list-algorithms**\n> List algorithms supported by the card.\n\n**-r** _reader_, **--reader** _reader_\n> Use a specific reader (by number or ATR).\n\n**-n**, **--name**\n> Print the name of the inserted card driver.\n\n**--serial**\n> Print the card serial number in hex.\n\n**-D**, **--list-drivers**\n> List all installed card drivers.\n\n**-c** _driver_, **--card-driver** _driver_\n> Use the specified card driver (use '?' to list).\n\n**--reset**\n> Reset the card (cold or warm).\n\n**-G** _section:name:key_, **--get-conf-entry**\n> Retrieve an opensc.conf configuration value.\n\n**-S** _section:name:key:value_, **--set-conf-entry**\n> Set an opensc.conf configuration value.\n\n**-w**, **--wait**\n> Wait for a card to be inserted.\n\n**-v**, **--verbose**\n> Verbose output (may be specified multiple times for debug).\n\n**--version**\n> Display OpenSC package version."],["DESCRIPTION","**opensc-tool** is a diagnostic tool for smart cards. It can query card readers, retrieve card information, and send raw APDU commands for testing and debugging.\n\nPart of the OpenSC project for smart card support on Unix systems."],["EXAMPLE","```bash"],["Check for card","opensc-tool --list-readers"],["Get card info","opensc-tool --info"],["Card type: PIV-II","```"],["CAVEATS","Requires PC/SC daemon (pcscd). Card must be inserted. Some commands require PIN."],["HISTORY","OpenSC was started to provide open-source smart card support on Unix systems, supporting PKCS#11 and cryptographic operations."],["INSTALL","```apt: sudo apt install opensc```\n\n```dnf: sudo dnf install opensc```\n\n```pacman: sudo pacman -S opensc```\n\n```apk: sudo apk add opensc```\n\n```zypper: sudo zypper install opensc```\n\n```brew: brew install opensc```\n\n```nix: nix profile install nixpkgs#opensc```"],["SEE ALSO","[pkcs11-tool](/man/pkcs11-tool)(1)"]],"e":["pkcs11-tool"],"x":"opensc-tool --list-readers"},"openssl-pkcs12":{"n":"openssl-pkcs12","t":"creates and parses PKCS#12 files","s":[["TAGLINE","creates and parses PKCS#12 files"],["TLDR","**Create PKCS12 from cert and key**\n\n```openssl pkcs12 -export -out [certificate.p12] -inkey [private.key] -in [certificate.crt]```\n\n**Include CA chain**\n\n```openssl pkcs12 -export -out [cert.p12] -inkey [key.pem] -in [cert.pem] -certfile [ca-chain.pem]```\n\n**Extract certificate**\n\n```openssl pkcs12 -in [certificate.p12] -clcerts -nokeys -out [certificate.crt]```\n\n**Extract private key**\n\n```openssl pkcs12 -in [certificate.p12] -nocerts -out [private.key]```\n\n**Extract all to PEM**\n\n```openssl pkcs12 -in [certificate.p12] -out [all.pem] -nodes```"],["SYNOPSIS","**openssl pkcs12** [_options_]"],["PARAMETERS","**-export**\n> Create PKCS12 file.\n\n**-in** _file_\n> Input file.\n\n**-out** _file_\n> Output file.\n\n**-inkey** _file_\n> Private key file.\n\n**-certfile** _file_\n> Additional certificates.\n\n**-nokeys**\n> Don't output keys.\n\n**-nocerts**\n> Don't output certs.\n\n**-nodes**\n> Don't encrypt output.\n\n**-name** _name_\n> Friendly name.\n\n**-password** _arg_\n> Input/output PKCS#12 password source (e.g., `pass:secret`, `file:path`, `env:VAR`, `stdin`).\n\n**-passin** _arg_, **-passout** _arg_\n> Separate input / output password sources.\n\n**-legacy**\n> Use legacy algorithms for compatibility with older software (OpenSSL 3+).\n\n**-clcerts**\n> Output client certificates only (exclude CAs).\n\n**-info**\n> Print information about the PKCS#12 file's structure."],["DESCRIPTION","**openssl pkcs12** creates and parses PKCS#12 files (.p12, .pfx). These files bundle private keys, certificates, and CA chains into a single encrypted file.\n\nCommonly used for importing/exporting certificates in browsers and applications."],["COMMON USES","```\n- Export from browser/keychain\n- Import to Java keystore\n- Certificate backup\n- Cross-platform transfer\n```"],["CAVEATS","PKCS12 passwords can be brute-forced. Use strong passwords. Legacy format uses weak encryption by default."],["HISTORY","PKCS#12 is a standard from RSA Laboratories, widely supported for portable certificate storage."],["INSTALL","```apt: sudo apt install openssl```\n\n```dnf: sudo dnf install openssl```\n\n```pacman: sudo pacman -S openssl```\n\n```apk: sudo apk add openssl```\n\n```zypper: sudo zypper install openssl```\n\n```brew: brew install openssl```\n\n```nix: nix profile install nixpkgs#openssl```"],["SEE ALSO","[openssl-x509](/man/openssl-x509)(1), [openssl-rsa](/man/openssl-rsa)(1), [keytool](/man/keytool)(1)"]],"e":["openssl-x509","openssl-rsa","keytool"],"x":"openssl pkcs12 -export -out [certificate.p12] -inkey [private.key] -in [certificate.crt]"},"osintui":{"n":"osintui","t":"Open source intelligence TUI for Shodan, Censys, and VirusTotal","s":[["TAGLINE","Open source intelligence TUI for Shodan, Censys, and VirusTotal"],["TLDR","**Launch the OSINT interface**\n\n```osintui```"],["SYNOPSIS","**osintui** [_options_]"],["DESCRIPTION","**osintui** is a terminal user interface for querying open source intelligence services including **Shodan**, **Censys**, and **VirusTotal**. It displays search results in a navigable TUI, allowing you to browse and analyze OSINT data without leaving the terminal.\n\nAPI tokens are configured in **~/.osintui/config/config.toml**, which is created automatically on first run."],["CONFIGURATION","**~/.osintui/config/config.toml**\n> TOML configuration file containing API tokens for each service."],["CAVEATS","Requires valid API keys for each service you want to use. Free API tiers have rate limits."],["HISTORY","**osintui** was created by **wssheldon** and is written in **Rust**. Its architecture is modeled after spotify-tui."],["SEE ALSO","[nmap](/man/nmap)(1), [theHarvester](/man/theHarvester)(1)"]],"e":["nmap","theHarvester"],"x":"osintui"},"paccheck":{"n":"paccheck","t":"verifies the integrity and consistency of installed packages on Arch-based","s":[["TAGLINE","verifies the integrity and consistency of installed packages on Arch-based"],["TLDR","**List and check** all installed packages\n\n```paccheck```\n\n**Check** specific packages\n\n```paccheck [package1] [package2]```\n\nOnly display messages if a **problem is found**\n\n```paccheck --quiet```\n\nCheck that all **package dependencies** are satisfied\n\n```paccheck --depends```\n\nDisplay **help**\n\n```paccheck --help```"],["SYNOPSIS","**paccheck** [_options_] [_packages_...]"],["PARAMETERS","**--quiet**\n> Only display output if a problem is found\n\n**--depends**\n> Check that all package dependencies are satisfied\n\n**--opt-depends**\n> Check optional dependencies\n\n**--files**\n> Check that package files exist\n\n**--file-properties**\n> Check file properties against package database\n\n**--md5sum**\n> Check file MD5 checksums\n\n**--sha256sum**\n> Check file SHA256 checksums\n\n**--require-mtree**\n> Treat missing MTREE data as an error\n\n**--db-files**\n> Include database files in checks\n\n**--backup**\n> Include backup files in checks\n\n**--noextract**\n> Include NoExtract files in checks\n\n**--noupgrade**\n> Include NoUpgrade files in checks\n\n**--help**\n> Display help information\n\n**--version**\n> Display version information"],["DESCRIPTION","**paccheck** verifies the integrity and consistency of installed packages on Arch-based systems. It can check dependency relationships, verify that package files exist on disk, and validate file checksums against the package database records.\n\nThe tool is useful for detecting system corruption, identifying manually modified configuration files, finding missing files, and ensuring all package dependencies are properly satisfied. It reads package metadata from the local pacman database."],["CAVEATS","Checksum verification requires MTREE data to be present in packages (not all packages include this). Some modified files are expected (configuration files edited by user). Running with all verification options can be slow on systems with many packages."],["HISTORY","Part of **pacutils**, a collection of utilities for pacman developed by Andrew Gregory. Provides functionality similar to **rpm -V** for RPM-based systems, allowing administrators to verify package integrity on Arch Linux."],["SEE ALSO","[pacman](/man/pacman)(8), [pacconf](/man/pacconf)(8), [pacsift](/man/pacsift)(8)"]],"e":["pacman","pacconf","pacsift"],"x":"paccheck"},"pacman-files":{"n":"pacman-files","t":"queries the files database to find which packages own specific files","s":[["TAGLINE","queries the files database to find which packages own specific files"],["TLDR","**Update** files database\n\n```sudo pacman -Fy```\n\n**Find** package owning file\n\n```pacman -F filename```\n\nFind using **regex**\n\n```pacman -Fx 'regex'```\n\nList package **names** only\n\n```pacman -Fq filename```\n\n**List** files in package\n\n```pacman -Fl package```"],["SYNOPSIS","**pacman -F** [_OPTIONS_] [_FILE_...]"],["DESCRIPTION","**pacman -F** (files) queries the files database to find which packages own specific files or to list files contained in packages. This works even for packages that are not installed."],["PARAMETERS","**-F, --files**\n> Query the files database\n\n**-y, --refresh**\n> Download fresh files database from the server\n\n**-l, --list**\n> List files owned by the specified package\n\n**-x, --regex**\n> Interpret the search pattern as a regular expression\n\n**-q, --quiet**\n> Show only package names without repository"],["CAVEATS","The files database must be updated separately from the sync database using **-Fy**. This database can be large and updates may take time. For finding files from installed packages, use **pacman -Qo** instead."],["HISTORY","**pacman** is the package manager for **Arch Linux** and its derivatives. The files database feature allows searching for files across all available packages."],["INSTALL","```apt: sudo apt install pacman-package-manager```\n\n```dnf: sudo dnf install pacman```\n\n```pacman: sudo pacman -S pacman```\n\n```apk: sudo apk add pacman```\n\n```nix: nix profile install nixpkgs#pacman```"],["SEE ALSO","[pacman](/man/pacman)(8), [pacman-query](/man/pacman-query)(8), [pkgfile](/man/pkgfile)(1)"]],"e":["pacman","pacman-query","pkgfile"],"x":"sudo pacman -Fy"},"pam_unix":{"n":"pam_unix","t":"provides standard Unix authentication","s":[["TAGLINE","provides standard Unix authentication"],["TLDR","**Standard Unix authentication**\n\n```auth required pam_unix.so```\n\n**Password management**\n\n```password required pam_unix.so sha512 shadow```\n\n**Session management**\n\n```session required pam_unix.so```\n\n**Null password check**\n\n```auth required pam_unix.so nullok```"],["SYNOPSIS","**pam_unix.so** [_options_]"],["PARAMETERS","**nullok**\n> Allow empty passwords.\n\n**sha512**\n> Use SHA-512 hashing.\n\n**shadow**\n> Use shadow passwords.\n\n**remember=**_N_\n> Remember N passwords.\n\n**try_first_pass**\n> Use previous password."],["DESCRIPTION","**pam_unix** provides standard Unix authentication. Uses /etc/passwd and shadow.\n\nThe module handles basic auth. Core PAM component."],["CAVEATS","Core PAM module. Configuration critical. Hash algorithm choice."],["HISTORY","pam_unix provides **traditional Unix authentication** via passwd/shadow."],["SEE ALSO","[pam](/man/pam)(8), [passwd](/man/passwd)(5)"]],"e":["pam","passwd"],"x":"auth required pam_unix.so"},"patool":{"n":"patool","t":"portable archive file manager that provides a unified interface for numerous","s":[["TAGLINE","portable archive file manager that provides a unified interface for numerous"],["TLDR","**Extract** an archive\n\n```patool extract [path/to/archive]```\n\n**Create** an archive\n\n```patool create [path/to/archive] [files...]```\n\n**List** contents of archive\n\n```patool list [path/to/archive]```\n\n**Compare** two archives\n\n```patool diff [archive1] [archive2]```\n\n**Search** for string in archive\n\n```patool search [pattern] [path/to/archive]```\n\n**Test** archive integrity\n\n```patool test [path/to/archive]```"],["SYNOPSIS","**patool** [**extract**|**create**|**list**|**diff**|**search**|**test**|**repack**] [_options_] _archive_ [_files_...]"],["COMMANDS","**extract**\n> Extract files from archive\n\n**create**\n> Create new archive from files\n\n**list**\n> List archive contents\n\n**diff**\n> Compare contents of two archives\n\n**search**\n> Search for pattern in archive contents\n\n**test**\n> Test archive integrity\n\n**repack**\n> Convert archive to different format"],["PARAMETERS","**-v, --verbose**\n> Verbose output\n\n**-n, --non-interactive**\n> Non-interactive mode\n\n**--outdir _dir_**\n> Extract to specified directory"],["DESCRIPTION","**patool** is a portable archive file manager that provides a unified interface for numerous archive formats. It automatically detects archive types and uses the appropriate backend tool for each format.\n\nSupported formats include zip, tar, gzip, bzip2, xz, 7z, rar, lzma, and many others. The tool abstracts away format-specific commands, allowing consistent operations across all archive types."],["CAVEATS","Requires backend tools for each archive format (unzip, tar, 7z, etc.). Some formats have limited support depending on available tools. Creating certain formats may require specific utilities."],["HISTORY","**patool** was created by **Bastian Kleineidam** to simplify archive handling. Written in Python, it serves as a wrapper around various archiving tools, providing a consistent interface regardless of the underlying format."],["INSTALL","```dnf: sudo dnf install patool```\n\n```zypper: sudo zypper install patool```\n\n```nix: nix profile install nixpkgs#patool```"],["SEE ALSO","[tar](/man/tar)(1), [unzip](/man/unzip)(1), [7z](/man/7z)(1), [unrar](/man/unrar)(1)"]],"e":["tar","unzip","7z","unrar"],"x":"patool extract [path/to/archive]"},"pdftops":{"n":"pdftops","t":"converts PDF to PostScript format","s":[["TAGLINE","converts PDF to PostScript format"],["TLDR","**Convert PDF to PostScript**\n\n```pdftops [input.pdf] [output.ps]```\n\n**Convert specific pages**\n\n```pdftops -f [1] -l [10] [input.pdf] [output.ps]```\n\n**Level 2 PostScript output**\n\n```pdftops -level2 [input.pdf] [output.ps]```\n\n**Optimize for printing**\n\n```pdftops -optimizecolorspace [input.pdf] [output.ps]```"],["SYNOPSIS","**pdftops** [_options_] _PDF-file_ [_PS-file_]"],["PARAMETERS","**-f** _num_\n> First page.\n\n**-l** _num_\n> Last page.\n\n**-level1**, **-level2**, **-level3**\n> PostScript level.\n\n**-eps**\n> Generate EPS.\n\n**-optimizecolorspace**\n> Optimize colors.\n\n**-paper** _size_\n> Paper size.\n\n**-r** _dpi_\n> Resolution for images."],["DESCRIPTION","**pdftops** converts PDF to PostScript format. Part of Poppler utilities, it produces higher quality output than pdf2ps from Ghostscript for most documents."],["Basic conversion","pdftops document.pdf document.ps"],["Create EPS from first page","pdftops -eps -f 1 -l 1 figure.pdf figure.eps"],["Print to PostScript printer","pdftops document.pdf - | lpr -P ps_printer\n```"],["POSTSCRIPT LEVELS","```\nLevel 1 - Maximum compatibility\nLevel 2 - Color, compression\nLevel 3 - Smooth shading, masks\n```"],["CAVEATS","Output can be large. EPS mode only for single pages. Part of poppler-utils package."],["HISTORY","pdftops is part of **Poppler**, the PDF rendering library forked from **Xpdf** by **Derek Noonburg**."],["SEE ALSO","[pdf2ps](/man/pdf2ps)(1), [pdftotext](/man/pdftotext)(1), [ps2pdf](/man/ps2pdf)(1), [poppler-utils](/man/poppler-utils)(1)"],["INSTALL","```apt: sudo apt install poppler-utils```\n\n```dnf: sudo dnf install poppler-utils```\n\n```apk: sudo apk add poppler-utils```\n\n```nix: nix profile install nixpkgs#poppler-utils```"]],"e":["pdf2ps","pdftotext","ps2pdf","poppler-utils"],"x":"pdftops [input.pdf] [output.ps]"},"pdu":{"n":"pdu","t":"Highly parallelized, fast directory tree disk usage analyzer","s":[["TAGLINE","Highly parallelized, fast directory tree disk usage analyzer"],["TLDR","**Show disk usage of the current directory**\n\n```pdu```\n\n**Show disk usage of a specific directory**\n\n```pdu [path/to/directory]```\n\n**Compare multiple directories**\n\n```pdu [dir1] [dir2]```\n\n**Show apparent sizes instead of block sizes**\n\n```pdu --quantity=apparent-size [path/to/directory]```\n\n**Show all entries regardless of size**\n\n```pdu --min-ratio=0 [path/to/directory]```\n\n**Output as JSON**\n\n```pdu --json-output [path/to/directory]```\n\n**Visualize a previously saved JSON file**\n\n```pdu --json-input < [disk-usage.json]```\n\n**Show disk usage using binary units (1K = 1024 B)**\n\n```pdu --bytes-format=binary [path/to/directory]```\n\n**Deduplicate hardlinks when calculating sizes**\n\n```pdu --deduplicate-hardlinks [path/to/directory]```"],["SYNOPSIS","**pdu** [_options_] [_files_...]"],["DESCRIPTION","**pdu** (Parallel Disk Usage) is a highly parallelized, fast directory tree analyzer that renders graphical bar charts of disk usage in the terminal. It serves as an alternative to dust and dutree, with support for JSON output and comparison of multiple directories.\n\nBy default, **pdu** measures block size, sorts entries by size (largest first), limits display to 10 levels deep, hides entries smaller than 1% of the total, and displays sizes in metric units (1K = 1000 B)."],["OPTIONS","**--bytes-format**, **-b** _FORMAT_\n> How to display byte values. Possible values: **plain** (raw byte count), **metric** (1K = 1000 B, default), **binary** (1K = 1024 B).\n\n**--quantity**, **-q** _MEASURE_\n> What to measure. Possible values: **block-size** (default), **apparent-size**, **block-count**.\n\n**--max-depth**, **-d** _N_\n> Maximum depth of the tree to display. Use **inf** for unlimited. Default: **10**.\n\n**--min-ratio**, **-m** _RATIO_\n> Minimum size proportion (0–1) required for an entry to appear. Default: **0.01** (1%).\n\n**--total-width**, **-w** _N_\n> Width in characters of the entire visualization.\n\n**--top-down**\n> Print the tree top-down instead of bottom-up.\n\n**--align-right**\n> Set the root of the bars to the right.\n\n**--no-sort**\n> Do not sort branches; preserve filesystem order.\n\n**--deduplicate-hardlinks**, **-H**\n> Detect hardlinks and subtract their sizes from parent nodes to avoid double-counting.\n\n**--progress**, **-p**\n> Report scanning progress. Has a performance cost.\n\n**--silent-errors**, **-s**\n> Suppress filesystem error messages on stderr.\n\n**--threads** _N_\n> Number of threads to use. Accepts a positive integer, **auto** (default), or **max**.\n\n**--json-output**\n> Print disk usage data as JSON instead of rendering a chart.\n\n**--json-input**\n> Read previously exported JSON from stdin and render it as a chart.\n\n**-h**, **--help**\n> Print help information.\n\n**-V**, **--version**\n> Print version information."],["HISTORY","**pdu** was created by **Hoang Van Khai** (KSXGitHub) and is written in **Rust**."],["SEE ALSO","[du](/man/du)(1), [dust](/man/dust)(1), [ncdu](/man/ncdu)(1), [gdu](/man/gdu)(1)"]],"e":["du","dust","ncdu","gdu"],"x":"pdu"},"perf":{"n":"perf","t":"official Linux kernel profiling tool","s":[["TAGLINE","official Linux kernel profiling tool"],["TLDR","Display **performance stats** for a command\n\n```perf stat [command]```\n\nDisplay **real-time** system-wide profile\n\n```sudo perf top```\n\n**Record** profile to perf.data\n\n```sudo perf record [command]```\n\nRecord profile of **existing process**\n\n```sudo perf record -p [pid]```\n\n**Display** recorded profile\n\n```sudo perf report```\n\n**Record** with call graph\n\n```sudo perf record -g [command]```\n\nList available **events**\n\n```perf list```"],["SYNOPSIS","**perf** [**stat**|**record**|**report**|**top**|**list**] [_options_] [_command_]"],["COMMANDS","**stat**\n> Run command and gather performance counter statistics\n\n**record**\n> Run command and record profile into perf.data\n\n**report**\n> Read perf.data and display profile\n\n**top**\n> System-wide real-time profiling\n\n**list**\n> List available performance events\n\n**annotate**\n> Read perf.data and display annotated code\n\n**script**\n> Read perf.data and output trace in scripted format\n\n**bench**\n> Run built-in microbenchmarks for scheduler, memory, etc.\n\n**trace**\n> Trace system calls (similar to strace)\n\n**sched**\n> Analyze scheduler latencies and behavior\n\n**diff**\n> Compare two perf.data files"],["PARAMETERS","**-p, --pid _pid_**\n> Profile existing process by PID\n\n**-e, --event _event_**\n> Select specific events to measure\n\n**-g**, **--call-graph**\n> Enable call-graph (stack chain/backtrace) recording\n\n**-a**, **--all-cpus**\n> System-wide collection from all CPUs\n\n**-F _freq_**, **--freq _freq_**\n> Profile at specified frequency (samples per second)\n\n**-o _file_**, **--output _file_**\n> Output file name\n\n**-c _count_**, **--count _count_**\n> Collect a sample after every _count_ events"],["DESCRIPTION","**perf** is the official Linux kernel profiling tool. It provides access to hardware performance counters, tracepoints, and software performance events. It's essential for identifying performance bottlenecks in applications and the kernel.\n\nThe tool can measure CPU cycles, cache misses, branch mispredictions, and many other low-level events. It supports both sampling-based profiling and event counting."],["CAVEATS","Requires root or kernel.perf_event_paranoid sysctl adjustment for many operations. Hardware counter availability varies by CPU. Profile data can be large. Some features require specific kernel configuration."],["HISTORY","**perf** was developed as part of the Linux kernel, primarily by **Ingo Molnar** and **Thomas Gleixner**. It replaced the older OProfile tool and became the standard Linux profiling interface, integrated directly into the kernel source tree."],["INSTALL","```apt: sudo apt install linux-perf```\n\n```dnf: sudo dnf install perf```\n\n```pacman: sudo pacman -S perf```\n\n```apk: sudo apk add perf```\n\n```zypper: sudo zypper install perf```\n\n```nix: nix profile install nixpkgs#perf```"],["SEE ALSO","[strace](/man/strace)(1), [ltrace](/man/ltrace)(1), [valgrind](/man/valgrind)(1), [gprof](/man/gprof)(1)"]],"e":["strace","ltrace","valgrind","gprof"],"x":"perf stat [command]"},"pip-cache":{"n":"pip-cache","t":"Inspect and manage pip's wheel cache","s":[["TAGLINE","Inspect and manage pip's wheel cache"],["TLDR","**Show cache directory**\n\n```pip cache dir```\n\n**List cached packages**\n\n```pip cache list```\n\n**Show cache info**\n\n```pip cache info```\n\n**Remove specific package from cache**\n\n```pip cache remove [package]```\n\n**Clear all cache**\n\n```pip cache purge```"],["SYNOPSIS","**pip** **cache** _command_ [_options_]"],["PARAMETERS","**dir**\n> Print the cache directory path.\n\n**info**\n> Show cache size and location.\n\n**list** [_pattern_]\n> List wheels currently in the cache, optionally filtered by glob _pattern_ or package name.\n\n**remove** _pattern_\n> Remove cached wheels matching _pattern_ (glob or package name).\n\n**purge**\n> Remove every wheel and HTTP response from the cache.\n\n**--format** _FORMAT_\n> Output format for **list**: **human** (default) or **abspath** (full file paths). Also configurable via **PIP_FORMAT**."],["DESCRIPTION","**pip cache** manages pip's HTTP-response cache and built-wheel cache. Pip stores downloaded and locally built wheels under a per-user cache directory (e.g. **~/.cache/pip** on Linux, **~/Library/Caches/pip** on macOS, **%LocalAppData%\\pip\\Cache** on Windows) so that subsequent installs of the same version can skip the network round-trip and the build step.\n\nTo bypass the cache for a single install, use **pip install --no-cache-dir** _pkg_; to override the cache location, set the **PIP_CACHE_DIR** environment variable or pass **pip --cache-dir** _DIR_."],["CAVEATS","**pip cache list** and **remove** only operate on the wheel cache, not the HTTP response cache. **purge** clears both. The HTTP cache uses ETag/Last-Modified validation, so safely removable at any time."],["INSTALL","```apk: sudo apk add py3-pip```"],["SEE ALSO","[pip](/man/pip)(1), [pip-install](/man/pip-install)(1)"]],"e":["pip","pip-install"],"x":"pip cache dir"},"plantuml":{"n":"plantuml","t":"Generate UML diagrams from text descriptions","s":[["TAGLINE","Generate UML diagrams from text descriptions"],["TLDR","**Generate diagram**\n\n```plantuml [diagram.puml]```\n\n**Output as PNG**\n\n```plantuml -tpng [diagram.puml]```\n\n**Output as SVG**\n\n```plantuml -tsvg [diagram.puml]```\n\n**Output to directory**\n\n```plantuml -o [output_dir] [diagram.puml]```\n\n**Read from stdin**\n\n```echo \"@startuml\\nA -> B\\n@enduml\" | plantuml -pipe > [diagram.png]```\n\n**Watch for changes**\n\n```plantuml -gui```"],["SYNOPSIS","**plantuml** [_options_] _files_..."],["DESCRIPTION","**plantuml** generates UML diagrams from text descriptions. It supports sequence diagrams, class diagrams, activity diagrams, and many other types.\n\nThe tool uses a simple text-based syntax that can be version-controlled and integrated into documentation workflows."],["PARAMETERS","**-t** _format_\n> Output format (png, svg, eps, pdf, txt).\n\n**-o** _dir_\n> Output directory.\n\n**-charset** _encoding_\n> Input file encoding.\n\n**-pipe**\n> Use stdin/stdout.\n\n**-gui**\n> Launch GUI.\n\n**-config** _file_\n> Include config file.\n\n**-checkversion**\n> Check for updates.\n\n**-verbose**\n> Verbose output."],["DIAGRAM TYPES","Sequence, Class, Activity, Use Case, Component, State, Object, Deployment, Timing, Wireframe, Gantt, Mind Map"],["CAVEATS","Requires Java. Graphviz needed for some diagrams. Large diagrams may be slow. Complex layouts need tuning."],["HISTORY","**PlantUML** was created by **Arnaud Roques** starting in **2009**. It brought text-based diagramming to UML, integrating with wikis, documentation tools, and IDEs."],["INSTALL","```dnf: sudo dnf install plantuml```\n\n```pacman: sudo pacman -S plantuml```\n\n```apk: sudo apk add plantuml```\n\n```zypper: sudo zypper install plantuml```\n\n```brew: brew install plantuml```\n\n```nix: nix profile install nixpkgs#plantuml```"],["SEE ALSO","[graphviz](/man/graphviz)(1), [mermaid](/man/mermaid)(1), [ditaa](/man/ditaa)(1), [drawio](/man/drawio)(1)"]],"e":["graphviz","mermaid","ditaa","drawio"],"x":"plantuml [diagram.puml]"},"pnpx":{"n":"pnpx","t":"Execute packages without installing them","s":[["TAGLINE","Execute packages without installing them"],["TLDR","**Run package without installing**\n\n```pnpx [package] [args]```\n\n**Run specific version**\n\n```pnpx [package]@[version] [args]```\n\n**Run with package name different from command**\n\n```pnpx --package [package] [command]```\n\n**Install multiple packages** and run a command\n\n```pnpx --package [pkg1] --package [pkg2] [command]```"],["SYNOPSIS","**pnpx** [_options_] _package_ [_args_]"],["PARAMETERS","_PACKAGE_\n> Package to execute.\n\n**--package** _PKG_\n> Package to install (if different from command). Can be specified multiple times."],["DESCRIPTION","**pnpx** executes npm packages without permanently installing them, similar to npx. It downloads the package to a temporary location, runs the specified command, and cleans up afterward. This is a deprecated alias for **pnpm dlx**; prefer using **pnpm dlx** directly.\n\nUse **--package** to specify a package name when the executable name differs from the package name. The **@version** suffix allows running a specific package version. Packages executed by pnpx are allowed to run postinstall scripts by default."],["CAVEATS","**pnpx** is deprecated in favor of **pnpm dlx**. Requires pnpm installed. The command runs inside a shell (/bin/sh on Unix, cmd.exe on Windows)."],["INSTALL","```dnf: sudo dnf install pnpm```\n\n```pacman: sudo pacman -S pnpm```\n\n```apk: sudo apk add pnpm```\n\n```zypper: sudo zypper install pnpm```\n\n```brew: brew install pnpm```\n\n```nix: nix profile install nixpkgs#pnpm```"],["SEE ALSO","[pnpm](/man/pnpm)(1), [npx](/man/npx)(1), [yarn-dlx](/man/yarn-dlx)(1)"]],"e":["pnpm","npx","yarn-dlx"],"x":"pnpx [package] [args]"},"prowler-aws":{"n":"prowler-aws","t":"AWS cloud security assessment","s":[["TAGLINE","AWS cloud security assessment"],["TLDR","**Run AWS security assessment**\n\n```prowler aws```\n\n**Run specific checks**\n\n```prowler aws -c [iam_password_policy] [s3_bucket_public_access]```\n\n**Run checks for specific services**\n\n```prowler aws -s [s3] [iam]```\n\n**Run with compliance framework**\n\n```prowler aws --compliance [cis_2.0_aws]```\n\n**Filter by severity**\n\n```prowler aws --severity [critical] [high]```\n\n**Output to file in JSON format**\n\n```prowler aws -M json-ocsf -o [results/]```\n\n**List all available checks**\n\n```prowler aws -l```"],["SYNOPSIS","**prowler aws** [_options_]"],["PARAMETERS","**-c**, **--checks** _checks_\n> Specific checks to run.\n\n**-s**, **--services** _services_\n> Services to check (iam, s3, ec2, etc.).\n\n**-e**, **--excluded-checks** _checks_\n> Exclude specific checks from execution.\n\n**--excluded-services** _services_\n> Exclude specific services from scanning.\n\n**--compliance** _framework_\n> Compliance framework (cis, gdpr, hipaa, etc.).\n\n**--severity** _levels_\n> Filter checks by severity (critical, high, medium, low, informational).\n\n**-M**, **--output-modes** _format_\n> Output format (csv, json-asff, json-ocsf, html).\n\n**-o**, **--output-directory** _dir_\n> Output directory.\n\n**-l**, **--list-checks**\n> List all available checks.\n\n**--list-services**\n> List all available services.\n\n**-p**, **--profile** _name_\n> AWS profile.\n\n**-f**, **--filter-region** _region_\n> AWS region(s) to scan.\n\n**--security-hub**\n> Send findings to AWS Security Hub."],["DESCRIPTION","**prowler aws** performs security assessment of AWS accounts. It checks configuration against best practices and compliance frameworks such as CIS, NIST 800, PCI-DSS, GDPR, HIPAA, and others. Identifies misconfigurations, vulnerabilities, and compliance gaps. Results can be output in multiple formats and optionally sent to AWS Security Hub."],["INSTALL","```brew: brew install prowler```\n\n```nix: nix profile install nixpkgs#prowler```"],["SEE ALSO","[prowler](/man/prowler)(1), [aws](/man/aws)(1)"]],"e":["prowler","aws"],"x":"prowler aws"},"pwunconv":{"n":"pwunconv","t":"Disable shadow password storage","s":[["TAGLINE","Disable shadow password storage"],["TLDR","**Revert shadow passwords**\n\n```pwunconv```\n\n**With specific root**\n\n```pwunconv -R [/]```"],["SYNOPSIS","**pwunconv** [_options_]"],["PARAMETERS","**-R** _ROOT_\n> Chroot directory."],["DESCRIPTION","**pwunconv** reverses the shadow password setup by moving password hashes from **/etc/shadow** back into **/etc/passwd** and removing the shadow file. This is the inverse of **pwconv** and effectively disables shadow password protection on the system.\n\nThe command is part of **shadow-utils** and is rarely used in practice since shadow passwords are a fundamental security feature on modern Linux systems. Removing shadow protection makes password hashes readable by all users, significantly weakening system security."],["CAVEATS","Security risk. Rarely used. Requires root."],["HISTORY","pwunconv is part of **shadow-utils** for reverting shadow passwords."],["INSTALL","```apt: sudo apt install passwd```\n\n```apk: sudo apk add shadow-conv```"],["SEE ALSO","[pwconv](/man/pwconv)(1), [grpunconv](/man/grpunconv)(1), [passwd](/man/passwd)(1)"]],"e":["pwconv","grpunconv","passwd"],"x":"pwunconv"},"redshift":{"n":"redshift","t":"Adjust screen color temperature for eye comfort","s":[["TAGLINE","Adjust screen color temperature for eye comfort"],["TLDR","**Start redshift** with automatic location detection\n\n```redshift```\n\n**Set color temperature manually** (day:night)\n\n```redshift -O [3500]```\n\n**Set day and night temperatures**\n\n```redshift -t [5500]:[3500]```\n\n**Set location manually** (latitude:longitude)\n\n```redshift -l [40.7]:[74.0]```\n\n**Reset screen to normal color** temperature\n\n```redshift -x```\n\n**Run in one-shot mode** (set and exit)\n\n```redshift -o```\n\n**Print current color temperature**\n\n```redshift -p```"],["SYNOPSIS","**redshift** [**-l** _lat_:_lon_] [**-t** _day_:_night_] [**-b** _day_:_night_] [**-m** _method_] [_options_]"],["PARAMETERS","**-l** _lat_:_lon_\n> Set location (latitude:longitude) for sunrise/sunset calculation\n\n**-t** _day_:_night_\n> Set color temperature in Kelvin (default: 5500:3500)\n\n**-b** _day_:_night_\n> Set screen brightness (0.1-1.0)\n\n**-O** _temp_\n> Set specific color temperature immediately\n\n**-o**\n> One-shot mode; set temperature and exit\n\n**-x**\n> Reset color temperature to default\n\n**-p**\n> Print current settings and parameters\n\n**-m** _method_\n> Adjustment method (randr, vidmode, drm, wayland)\n\n**-g** _r_:_g_:_b_\n> Set gamma correction\n\n**-r**\n> Disable temperature transitions\n\n**-v**\n> Verbose output\n\n**-c** _file_\n> Load configuration from file"],["DESCRIPTION","**redshift** adjusts the color temperature of the screen based on time of day, reducing blue light exposure in the evening. This can help reduce eye strain and improve sleep quality by matching screen color to ambient lighting.\n\nDuring the day, the screen maintains normal color temperature (around 5500-6500K). As the sun sets, redshift gradually transitions to warmer colors (3000-4000K), reducing blue light that can interfere with circadian rhythms.\n\nLocation can be determined automatically using geolocation services (geoclue) or specified manually. The program runs continuously, adjusting temperature throughout the day based on calculated sunrise and sunset times.\n\nDifferent backends support different display systems: **randr** for X11, **wayland** for Wayland compositors, and **drm** for console/framebuffer."],["CONFIGURATION","**~/.config/redshift/redshift.conf**\n> User configuration file specifying default location, temperature ranges, brightness, adjustment method, and transition settings."],["CAVEATS","Automatic location detection requires geoclue and appropriate permissions. If detection fails, specify location manually with **-l**.\n\nSome applications (photo/video editing, color-critical work) may require accurate colors. Use **redshift -x** to temporarily disable adjustments or exclude specific times.\n\nWayland support depends on compositor support. Some Wayland compositors implement their own night light features.\n\nRunning multiple instances may cause conflicts. Use a single instance or integrate with your desktop environment's night light settings."],["INSTALL","```apt: sudo apt install redshift```\n\n```dnf: sudo dnf install redshift```\n\n```pacman: sudo pacman -S redshift```\n\n```apk: sudo apk add redshift```\n\n```zypper: sudo zypper install redshift```\n\n```brew: brew install redshift```\n\n```nix: nix profile install nixpkgs#redshift```"],["SEE ALSO","[xrandr](/man/xrandr)(1), [xgamma](/man/xgamma)(1), [gammastep](/man/gammastep)(1)"]],"e":["xrandr","xgamma","gammastep"],"x":"redshift"},"regname":{"n":"regname","t":"Regular expression based file rename tool","s":[["TAGLINE","Regular expression based file rename tool"],["TLDR","**Rename files using a regex pattern**\n\n```regname [pattern] [replacement] [files]```"],["SYNOPSIS","**regname** [_options_] _pattern_ _replacement_ [_files_...]"],["DESCRIPTION","**regname** is a flexible regular expression based file rename tool that supports both a graphical and a command-line interface. It uses regex patterns with capture group substitutions for powerful batch file renaming operations."],["HISTORY","**regname** was created by **mobzystems** and is written in **C#**."],["INSTALL","```nix: nix profile install nixpkgs#regname```"],["SEE ALSO","[rename](/man/rename)(1), [mmv](/man/mmv)(1), [nomino](/man/nomino)(1)"]],"e":["rename","mmv","nomino"],"x":"regname [pattern] [replacement] [files]"},"rustup-which":{"n":"rustup-which","t":"Show path to Rust toolchain binaries","s":[["TAGLINE","Show path to Rust toolchain binaries"],["TLDR","**Find cargo path**\n\n```rustup which cargo```\n\n**Find rustc path**\n\n```rustup which rustc```\n\n**Find tool in specific toolchain**\n\n```rustup which --toolchain nightly rustfmt```"],["SYNOPSIS","**rustup** **which** [_options_] _command_"],["PARAMETERS","**--toolchain** _name_\n> Search in specific toolchain."],["DESCRIPTION","**rustup which** displays the path to Rust tools. Shows which binary would be executed for a given command. Useful for debugging toolchain issues."],["INSTALL","```apt: sudo apt install rustup```\n\n```dnf: sudo dnf install rustup```\n\n```pacman: sudo pacman -S rustup```\n\n```zypper: sudo zypper install rustup```\n\n```brew: brew install rustup```\n\n```nix: nix profile install nixpkgs#rustup```"],["SEE ALSO","[rustup](/man/rustup)(1), [which](/man/which)(1)"]],"e":["rustup","which"],"x":"rustup which cargo"},"sc_ttlexp":{"n":"sc_ttlexp","t":"Analyze TTL-expired responses from scamper","s":[["TAGLINE","Analyze TTL-expired responses from scamper"],["TLDR","**Analyze TTL-expired responses**\n\n```sc_ttlexp [input.warts]```"],["SYNOPSIS","**sc_ttlexp** [_options_] _file_"],["DESCRIPTION","**sc_ttlexp** analyzes TTL-expired ICMP responses from scamper measurements. Used for studying router behavior and path characteristics. Part of scamper toolkit."],["INSTALL","```apt: sudo apt install scamper```\n\n```zypper: sudo zypper install scamper```\n\n```brew: brew install scamper```"],["SEE ALSO","[scamper](/man/scamper)(1), [sc_analysis_dump](/man/sc_analysis_dump)(1)"]],"e":["scamper","sc_analysis_dump"],"x":"sc_ttlexp [input.warts]"},"service":{"n":"service","t":"Manage SysV init services","s":[["TAGLINE","Manage SysV init services"],["TLDR","List the **name and status** of all services\n\n```service --status-all```\n\n**Start/Stop/Restart/Reload** a service\n\n```service [service_name] [start|stop|restart|reload]```\n\nDo a **full restart** (runs script twice with stop and start)\n\n```service [service_name] --full-restart```\n\nShow the **current status** of a service\n\n```service [service_name] status```"],["SYNOPSIS","**service** _script_ _command_ [_options_]\n\n**service** **--status-all**"],["PARAMETERS","**--status-all**\n> List status of all services\n\n_script_\n> Service name (script in /etc/init.d/)\n\n**start**\n> Start the service\n\n**stop**\n> Stop the service\n\n**restart**\n> Stop and start the service\n\n**reload**\n> Reload service configuration\n\n**status**\n> Show service status\n\n**--full-restart**\n> Perform full stop then start sequence"],["DESCRIPTION","**service** is a wrapper for init scripts in /etc/init.d/, providing a consistent interface to manage SysV init services. It automatically handles environment sanitization and path normalization for init script execution.\n\nOn systemd systems, service typically redirects commands to systemctl for backward compatibility. It remains useful for managing traditional init scripts and provides a familiar interface for administrators."],["CAVEATS","On systemd systems, **systemctl** is preferred. Not all init scripts implement all commands (reload may not be available). The service command requires root privileges for most operations."],["HISTORY","Introduced in **Debian** and adopted by other distributions as a standard interface for SysV init scripts. With the transition to systemd, service became a compatibility wrapper that translates commands to systemctl where appropriate."],["INSTALL","```apt: sudo apt install runit-init```\n\n```apk: sudo apk add openrc```"],["SEE ALSO","[systemctl](/man/systemctl)(1), [chkconfig](/man/chkconfig)(8), [update-rc.d](/man/update-rc.d)(8)"]],"e":["systemctl","chkconfig","update-rc.d"],"x":"service --status-all"},"sport":{"n":"sport","t":"SlackBuilds package management tool","s":[["TAGLINE","SlackBuilds package management tool"],["TLDR","**Update** the slackbuilds tree via rsync\n\n```sudo sport rsync```\n\n**Search** for a package by name\n\n```sport search \"[keyword]\"```\n\n**Check** if a package is installed\n\n```sport check [package]```\n\nDisplay **README and info** files\n\n```sport cat [package]```\n\n**Install** a package\n\n```sudo sport install [package]```\n\nInstall packages from a **list file**\n\n```sudo sport install $(< [path/to/list])```"],["SYNOPSIS","**sport** _command_ [_arguments_]"],["COMMANDS","**rsync**\n> Update the local SlackBuilds tree from slackbuilds.org\n\n**search** _keyword_\n> Search for packages by name\n\n**check** _package_\n> Check if a package is installed\n\n**cat** _package_\n> Display README and .info files for a package\n\n**install** _package_\n> Build and install a SlackBuild package"],["DESCRIPTION","**sport** is a SlackBuilds package management tool for Slackware Linux. It provides a simple interface to search, view information about, and install packages from the SlackBuilds.org repository.\n\nThe tool maintains a local copy of the SlackBuilds tree in **/usr/ports**, synced via rsync. Before first use, the tree must be initialized. Dependencies must be resolved manually before installation."],["INITIAL SETUP","To set up sport for first use:\n\n```\nsudo mkdir -p /usr/ports\nsudo rsync -av rsync://slackbuilds.org/slackbuilds/$(awk '{print $2}' /etc/slackware-version)/ /usr/ports/\n```"],["CAVEATS","Dependencies are not automatically resolved; check .info files for requirements. Builds compile from source, requiring development tools. The SlackBuilds tree must be kept updated with **sport rsync**. Specific to Slackware-based distributions."],["HISTORY","**sport** was created for Slackware Linux to simplify working with SlackBuilds.org, the community repository of build scripts for Slackware. SlackBuilds.org was established in **2006** to provide a standardized way of building third-party software for Slackware."],["SEE ALSO","[sbopkg](/man/sbopkg)(8), [slackpkg](/man/slackpkg)(8), [slapt-src](/man/slapt-src)(8)"]],"e":["sbopkg","slackpkg","slapt-src"],"x":"sudo sport rsync"},"squall":{"n":"squall","t":"TUI SQLite viewer and editor","s":[["TAGLINE","TUI SQLite viewer and editor"],["TLDR","**Open a database file**\n\n```squall -f [path/to/database.sqlite]```\n\n**Launch the TUI without a file**\n\n```squall```"],["SYNOPSIS","**squall** [**-f** _database_path_]"],["DESCRIPTION","**squall** is a terminal-based SQLite database viewer and editor. It displays tables and query results in panes with syntax highlighting, supports browsing tables, executing SQL queries, and editing records directly. It has a minimal resource footprint."],["HISTORY","**squall** was created by **Mike Driscoll** (driscollis) and is written in **Python** using the Textual framework."],["SEE ALSO","[litecli](/man/litecli)(1), [sqlite3](/man/sqlite3)(1), [sqlit](/man/sqlit)(1)"]],"e":["litecli","sqlite3","sqlit"],"x":"squall -f [path/to/database.sqlite]"},"swaplabel":{"n":"swaplabel","t":"Display or change swap area label and UUID","s":[["TAGLINE","Display or change swap area label and UUID"],["TLDR","**Display** the current label and UUID of a swap area\n\n```swaplabel [path/to/device]```\n\n**Set** the label of a swap area\n\n```swaplabel -L [new_label] [path/to/device]```\n\n**Set** the UUID of a swap area\n\n```swaplabel -U [new_uuid] [path/to/device]```"],["SYNOPSIS","**swaplabel** [_options_] _device_"],["PARAMETERS","**-L, --label _label_**\n> Set the swap area label\n\n**-U, --uuid _uuid_**\n> Set the swap area UUID (use **uuidgen** to create)\n\n**-h, --help**\n> Display help information\n\n**-V, --version**\n> Display version information"],["DESCRIPTION","**swaplabel** prints or changes the label or UUID of a Linux swap area. The device can be a partition or a regular file configured as swap space.\n\nLabels and UUIDs are useful for identifying swap areas in **/etc/fstab** instead of using device paths, which may change between boots. Using UUIDs is particularly recommended for reliable identification."],["CAVEATS","The swap area must be inactive (not in use) when changing label or UUID. Requires root privileges. After changing UUID, update **/etc/fstab** if it references the old UUID. Part of the util-linux package."],["INSTALL","```apt: sudo apt install util-linux```\n\n```dnf: sudo dnf install util-linux```\n\n```pacman: sudo pacman -S util-linux```\n\n```apk: sudo apk add util-linux-misc```\n\n```zypper: sudo zypper install util-linux```\n\n```brew: brew install util-linux```\n\n```nix: nix profile install nixpkgs#util-linux```"],["SEE ALSO","[mkswap](/man/mkswap)(8), [swapon](/man/swapon)(8), [swapoff](/man/swapoff)(8), [uuidgen](/man/uuidgen)(1)"]],"e":["mkswap","swapon","swapoff","uuidgen"],"x":"swaplabel [path/to/device]"},"tldrl":{"n":"tldrl","t":"Rust-based TLDR pages client","s":[["TAGLINE","Rust-based TLDR pages client"],["TLDR","**Show page**\n\n```tldrl [command]```\n\n**Update cache**\n\n```tldrl --update```\n\n**List all pages**\n\n```tldrl --list```\n\n**Show platform-specific**\n\n```tldrl --platform [linux] [command]```\n\n**Search pages**\n\n```tldrl --search \"[keyword]\"```"],["SYNOPSIS","**tldrl** [_--update_] [_--platform os_] [_options_] [_command_]"],["PARAMETERS","**--update**\n> Update cache.\n\n**--list**\n> List pages.\n\n**--platform** _OS_\n> Platform filter.\n\n**--search** _TERM_\n> Search pages.\n\n**--help**\n> Show help."],["DESCRIPTION","**tldrl** is a fast, Rust-based client for accessing TLDR pages, the community-maintained collection of simplified command-line documentation focused on practical examples. It caches pages locally for offline access and quick lookups.\n\nThe client supports platform-specific pages for Linux, macOS, and Windows, and provides search functionality to find relevant commands by keyword. The cache can be updated with **--update** to fetch the latest pages from the TLDR repository."],["CAVEATS","Internet for updates. Cache needs refresh. Not official tldr client."],["HISTORY","**tldrl** is a Rust implementation of a TLDR client for accessing community-maintained simplified man pages."],["SEE ALSO","[tldr](/man/tldr)(1), [man](/man/man)(1), [cheat](/man/cheat)(1)"]],"e":["tldr","man","cheat"],"x":"tldrl [command]"},"trash-list":{"n":"trash-list","t":"List files in the trash","s":[["TAGLINE","List files in the trash"],["TLDR","This command is documented with **trash**\n\nView documentation for **trash-list**\n\n```tldr trash```"],["SYNOPSIS","**trash-list**"],["DESCRIPTION","**trash-list** is part of the trash-cli suite. See **trash** for full documentation."],["INSTALL","```dnf: sudo dnf install trash-cli```\n\n```pacman: sudo pacman -S trash-cli```\n\n```apk: sudo apk add trash-cli```\n\n```zypper: sudo zypper install trash-cli```\n\n```brew: brew install trash-cli```\n\n```nix: nix profile install nixpkgs#trash-cli```"],["SEE ALSO","[trash](/man/trash)(1), [trash-empty](/man/trash-empty)(1), [trash-restore](/man/trash-restore)(1)"]],"e":["trash","trash-empty","trash-restore"],"x":"tldr trash"},"tufw":{"n":"tufw","t":"Terminal UI for UFW firewall management","s":[["TAGLINE","Terminal UI for UFW firewall management"],["TLDR","**Launch the firewall management TUI**\n\n```sudo tufw```"],["SYNOPSIS","**sudo tufw**"],["DESCRIPTION","**tufw** wraps the Uncomplicated Firewall (UFW) in a terminal-based interface with dropdown menus and form-based rule editing. Every action translates into regular UFW commands behind the scenes. It works perfectly over SSH connections."],["CAVEATS","Must be run as root."],["HISTORY","**tufw** was created by **Thomas Pellegatta** (peltho) and is written in **Go**."],["INSTALL","```aur: yay -S tufw```"],["SEE ALSO","[ufw](/man/ufw)(8), [iptables](/man/iptables)(8), [firewalld](/man/firewalld)(1)"]],"e":["ufw","iptables","firewalld"],"x":"sudo tufw"},"viberaven":{"n":"viberaven","t":"Production context CLI for AI-built applications","s":[["TAGLINE","Production context CLI for AI-built applications"],["TLDR","**Open the local Studio cockpit**\n\n```viberaven```\n\n**Install agent guidance** into project config files\n\n```viberaven init --agents all```\n\n**Check agent integration health**\n\n```viberaven doctor --agents```\n\n**Serve MCP resources** on stdio for connected agents\n\n```viberaven --mcp```"],["SYNOPSIS","**viberaven** [_options_]\n\n**viberaven** _subcommand_ [_arguments_]"],["DESCRIPTION","**viberaven** is the command-line entry point for VibeRaven, a local-first toolkit that gives AI coding agents production context before they edit a repository. It installs bounded rules and skills into files such as **AGENTS.md**, **CLAUDE.md**, and **.cursor/rules**, maintains production memory under **.viberaven/**, and launches a Studio UI for release diffs, provider boundaries, deployment evidence, and agent chat.\n\nThe CLI is distributed on npm and is typically run with **npx -y viberaven**. It focuses on the loop **route → ask → evidence → fix → verify → remember**, routing uncertain tasks to specialized skills for architecture planning, regression analysis, production context, and go-live checks. MCP mode exposes structured project resources so agents can fetch release and provider state without guessing from repo files alone."],["PARAMETERS","**init**\n> Install VibeRaven agent rules and context files into the current project.\n\n**--agents** _LIST_\n> Target specific agent environments (for example `all`, or a comma-separated subset).\n\n**--dry-run**\n> Preview **init** changes without writing files.\n\n**doctor**\n> Validate installed agent guidance and integration health.\n\n**--mcp**\n> Run the Model Context Protocol server on standard input/output.\n\n**validate-npm-package** _PACKAGE_\n> Check an npm package before adding it as a dependency."],["CONFIGURATION","VibeRaven writes project-local state under **.viberaven/**, including **production-context.md**, **agent-context.md**, and optional **prp.json** for machine-readable production readiness data. Installed rules are wrapped in **** … **** markers so they can be updated or removed cleanly."],["CAVEATS","The open-source CLI and Studio are local-first and do not require login for basic use, but provider-specific proof (Supabase, Vercel, billing, auth) still depends on credentials and dashboards outside the repository. VibeRaven augments agent behavior; it does not replace tests, CI, or manual production verification."],["HISTORY","VibeRaven was created by **Ohad** (ohad6k) in 2026 as mission-control tooling for apps built primarily by AI agents, with the public repository serving as the agent discovery and installation surface."],["SEE ALSO","[claude](/man/claude)(1), [codex](/man/codex)(1), [npx](/man/npx)(1), [git](/man/git)(1)"],["RESOURCES","```[Source code](https://github.com/ohad6k/VibeRaven)```\n\n```[Homepage](https://viberaven.dev)```"]],"e":["claude","codex","npx","git"],"x":"viberaven"},"vipe":{"n":"vipe","t":"Insert text editor into pipelines","s":[["TAGLINE","Insert text editor into pipelines"],["TLDR","**Edit pipe content**\n\n```echo \"text\" | vipe```\n\n**Edit command output**\n\n```cat [file.txt] | vipe | [next_command]```\n\n**Edit with specific editor**\n\n```EDITOR=[vim] cat [file.txt] | vipe```\n\n**Transform data interactively**\n\n```echo '{\"key\": \"value\"}' | vipe | jq .```"],["SYNOPSIS","**vipe** < _stdin_ > _stdout_"],["ENVIRONMENT","**EDITOR** - Editor to use\n**VISUAL** - Visual editor"],["DESCRIPTION","**vipe** inserts a text editor into a Unix pipeline. It reads standard input into a temporary file, opens it in the editor specified by the EDITOR or VISUAL environment variable, and writes the edited content to standard output when the editor exits.\n\nThis allows interactive modification of data flowing through a pipeline, which is useful for manually fixing input before it reaches the next command, debugging pipeline data, or performing one-off transformations that are easier to do in an editor than with sed or awk."],["EXAMPLE","```\n$ seq 5 | vipe | sort -r"],["Output continues through sort","```"],["CAVEATS","Part of moreutils package. Breaks non-interactive pipelines. Editor must be terminal-based."],["HISTORY","**vipe** is part of **moreutils** by **Joey Hess**. It enables interactive editing within Unix pipelines."],["INSTALL","```apt: sudo apt install moreutils```\n\n```dnf: sudo dnf install moreutils```\n\n```pacman: sudo pacman -S moreutils```\n\n```apk: sudo apk add moreutils```\n\n```zypper: sudo zypper install moreutils```\n\n```brew: brew install moreutils```\n\n```nix: nix profile install nixpkgs#moreutils```"],["SEE ALSO","[vidir](/man/vidir)(1), [sponge](/man/sponge)(1), [tee](/man/tee)(1)"]],"e":["vidir","sponge","tee"],"x":"echo \"text\" | vipe"},"wal":{"n":"wal","t":"Generate color schemes from images","s":[["TAGLINE","Generate color schemes from images"],["TLDR","**Generate colors from image**\n\n```wal -i [image.png]```\n\n**Light theme**\n\n```wal -i [image.png] -l```\n\n**Skip wallpaper setting**\n\n```wal -i [image.png] -n```\n\n**Use theme**\n\n```wal --theme [theme-name]```\n\n**List themes**\n\n```wal --theme```\n\n**Restore colors**\n\n```wal -R```"],["SYNOPSIS","**wal** [_-a alpha_] [_-b background_] [_-i image_] [_-l_] [_-n_] [_--theme name_] [_options_]"],["PARAMETERS","**-i** _IMAGE_\n> Input image or directory of images.\n\n**-l**\n> Generate light colorscheme.\n\n**-n**\n> Skip setting the wallpaper.\n\n**--theme** _NAME_\n> Use a theme file (use without argument to list themes).\n\n**-R**\n> Restore previous colorscheme.\n\n**-c**\n> Delete all cached colorschemes.\n\n**-a** _ALPHA_\n> Set terminal background transparency (URxvt only).\n\n**-b** _COLOR_\n> Custom background color to use.\n\n**--backend** _BACKEND_\n> Color backend to use (use without argument to list backends).\n\n**--saturate** _0.0-1.0_\n> Set color saturation level.\n\n**-o** _SCRIPT_\n> External script to run after wal finishes.\n\n**-q**\n> Quiet mode, suppress output.\n\n**-s**\n> Skip changing colors in terminals.\n\n**-t**\n> Skip changing colors in TTY."],["DESCRIPTION","**wal** (pywal) generates color schemes by extracting dominant colors from wallpaper images and applies them to the terminal emulator, shell, and other applications. It creates a cohesive visual theme across the desktop environment based on a single source image.\n\nThe extracted color palette is applied to terminal colors in real time and can be used to generate configuration files for other programs through a template system. This allows applications like i3, polybar, rofi, and many others to automatically adopt the same color scheme.\n\nGenerated themes are cached for quick switching, and previous color schemes can be restored on login. Both light and dark color variants are supported, and the wallpaper-setting step can be skipped when only the colors are needed."],["CAVEATS","Python required. Terminal support varies. X11/Wayland differences."],["HISTORY","**pywal** was created to generate and apply color schemes from images to terminals and applications."],["INSTALL","```apk: sudo apk add py3-pywal```"],["SEE ALSO","[feh](/man/feh)(1), [wpgtk](/man/wpgtk)(1), [flavours](/man/flavours)(1)"]],"e":["feh","wpgtk","flavours"],"x":"wal -i [image.png]"},"watson":{"n":"watson","t":"Command-line time tracking tool","s":[["TAGLINE","Command-line time tracking tool"],["TLDR","**Start tracking**\n\n```watson start [project] +[tag]```\n\n**Stop tracking**\n\n```watson stop```\n\n**Show current status**\n\n```watson status```\n\n**List today's entries**\n\n```watson log -d```\n\n**List this week**\n\n```watson log -w```\n\n**Show report**\n\n```watson report```\n\n**Edit last entry**\n\n```watson edit```\n\n**Cancel current tracking**\n\n```watson cancel```"],["SYNOPSIS","**watson** _command_ [_options_] [_args_]"],["PARAMETERS","**start** _PROJECT_ [_+TAGS_]\n> Start tracking.\n\n**stop**\n> Stop tracking.\n\n**status**\n> Current frame.\n\n**log** [_-d_|_-w_|_-m_]\n> Show entries.\n\n**report** [_-d_|_-w_|_-m_]\n> Generate report.\n\n**edit**\n> Edit entry.\n\n**cancel**\n> Cancel current.\n\n**add**\n> Add past entry.\n\n**remove**\n> Delete entry."],["DESCRIPTION","**watson** is a command-line time tracking tool that records work sessions on named projects with optional tags for categorization. Starting a timer associates the current time with a project and tags, and stopping it saves a completed time frame with the calculated duration.\n\nThe log command displays tracked entries chronologically, with filters for viewing today's work, the current week, or a specific date range. Reports aggregate time by project, showing total hours spent on each project and its tags over a given period.\n\nPast entries can be added retroactively, existing entries can be edited to correct mistakes, and the cancel command discards the currently running timer without saving it."],["CAVEATS","Single-user tool. No sync built-in. SQLite backend."],["HISTORY","**Watson** was created for simple CLI time tracking. Named after Sherlock Holmes' companion, it helps track where time goes."],["INSTALL","```zypper: sudo zypper install watson```\n\n```brew: brew install watson```\n\n```nix: nix profile install nixpkgs#watson```"],["SEE ALSO","[timew](/man/timew)(1), [clockify](/man/clockify)(1)"]],"e":["timew","clockify"],"x":"watson start [project] +[tag]"},"xpilot-ng":{"n":"xpilot-ng","t":"Next generation multiplayer space combat","s":[["TAGLINE","Next generation multiplayer space combat"],["TLDR","**Start game client**\n\n```xpilot-ng-x11```\n\n**Start SDL/OpenGL client**\n\n```xpilot-ng-sdl```\n\n**Connect to specific server**\n\n```xpilot-ng-x11 -join [server.example.com]```\n\n**Start local server**\n\n```xpilot-ng-server```\n\n**Start server with map**\n\n```xpilot-ng-server -map [/path/to/map.xp2]```\n\n**List public servers**\n\n```xpilot-ng-x11 -list```"],["SYNOPSIS","**xpilot-ng-x11** [_options_]\n\n**xpilot-ng-server** [_options_]"],["CLIENT PARAMETERS","**-join** _server_\n> Connect to specified server.\n\n**-name** _name_\n> Player name.\n\n**-team** _num_\n> Join specified team.\n\n**-list**\n> List available servers.\n\n**-port** _num_\n> Server port (default: 15345)."],["SERVER PARAMETERS","**-map** _file_\n> Load specified map file.\n\n**-port** _num_\n> Listen port (default: 15345).\n\n**-contactPort** _num_\n> Meta server contact port.\n\n**-password** _pass_\n> Server password."],["DESCRIPTION","**xpilot-ng** (XPilot Next Generation) is a multiplayer space combat game where players pilot ships, collect power-ups, and compete in various game modes including deathmatch, capture the flag, and racing.\n\nThe NG version features polygon-based maps, OpenGL graphics via SDL client, and improved performance over original XPilot. Players connect to servers hosting games; a central metaserver lists available public servers.\n\nServer configuration in /etc/xpilot-ng/defaults.txt controls extensive gameplay options including gravity, weapons, scoring, and physics. Client settings go in ~/.xpilotrc."],["CAVEATS","Requires network connectivity for multiplayer. Public servers vary in availability. X11 client needs X server; SDL client needs OpenGL support."],["HISTORY","**XPilot** was created in 1991 by Bjørn Stabell and Ken Schouten at the University of Tromsø. XPilot NG branched in 2000, adding polygon maps and OpenGL rendering. One of the oldest continuously played online multiplayer games."],["INSTALL","```dnf: sudo dnf install xpilot-ng```\n\n```nix: nix profile install nixpkgs#xpilot-ng```"],["SEE ALSO","[xpilot](/man/xpilot)(6)"]],"e":["xpilot"],"x":"xpilot-ng-x11"}});
