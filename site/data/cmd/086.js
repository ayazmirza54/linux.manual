window.LCL_BUCKET(86,{"adb-kill-server":{"n":"adb-kill-server","t":"Terminate the running adb server process","s":[["TAGLINE","Terminate the running adb server process"],["TLDR","**Stop** the adb server\n\n```adb kill-server```\n\nKill server and **restart** immediately\n\n```adb kill-server && adb start-server```"],["SYNOPSIS","**adb kill-server**"],["DESCRIPTION","**adb kill-server** terminates the running adb server process on your computer. The adb server is a background process that manages communication between adb clients and connected devices.\n\nKilling the server is useful when troubleshooting connection issues, freeing up USB resources, or when the server becomes unresponsive. The server automatically restarts when you run any adb command."],["CAVEATS","All active adb connections are terminated when the server is killed. Any port forwards, reverse forwards, and wireless connections will be lost. Running adb commands after this will automatically start a new server."],["HISTORY","The client-server architecture of adb has been part of the Android SDK since its initial release in **2008**. The kill-server command provides a way to reset the server when troubleshooting issues."],["INSTALL","```apt: sudo apt install adb```\n\n```dnf: sudo dnf install android-tools```\n\n```pacman: sudo pacman -S android-tools```\n\n```apk: sudo apk add android-tools-adb```\n\n```zypper: sudo zypper install android-tools```\n\n```nix: nix profile install nixpkgs#android-tools```"],["SEE ALSO","[adb](/man/adb)(1), [adb-start-server](/man/adb-start-server)(1), [adb-devices](/man/adb-devices)(1), [adb-disconnect](/man/adb-disconnect)(1)"],["RESOURCES","```[Source code](https://android.googlesource.com/platform/packages/modules/adb/)```\n\n```[Documentation](https://developer.android.com/tools/adb)```"]],"e":["adb","adb-start-server","adb-devices","adb-disconnect"],"x":"adb kill-server"},"agetty":{"n":"agetty","t":"Open a terminal and prompt for login","s":[["TAGLINE","Open a terminal and prompt for login"],["TLDR","Connect stdin to a **port** with an optional baud rate\n\n```agetty [tty] [115200]```\n\nSet a **timeout** for the login\n\n```agetty -t [timeout_in_seconds] -```\n\nAssume the tty is **8-bit clean** and override the TERM variable\n\n```agetty -8 - [term_var]```\n\nSkip the login and invoke **another login program**\n\n```agetty -n -l [login_program] [tty]```\n\nDo not display the **pre-login issue** file\n\n```agetty -i -```"],["SYNOPSIS","**agetty** [_options_] _port_ [_baud_rate_...] [_term_]"],["DESCRIPTION","**agetty** opens a tty port, prompts for a login name, and invokes the /bin/login command. It is normally invoked by **init**(8). The command provides features useful for hardwired and dial-in connections, including automatic parity detection, baud rate deduction from modem messages, and optional hardware flow control."],["PARAMETERS","**-8, --8bits**\n> Disable parity detection for 8-bit clean terminals\n\n**-a, --autologin username**\n> Automatic login without prompting for username\n\n**-h, --flow-control**\n> Enable RTS/CTS hardware flow control\n\n**-i, --noissue**\n> Skip displaying /etc/issue\n\n**-J, --noclear**\n> Don't clear screen before login prompt\n\n**-l, --login-program program**\n> Use alternative login executable\n\n**-L, --local-line**\n> Control carrier detect requirement\n\n**-m, --extract-baud**\n> Extract baud rate from modem status messages\n\n**-n, --skip-login**\n> Don't prompt for login name\n\n**-r, --chroot directory**\n> Change the root directory\n\n**-H, --host hostname**\n> Write a specific fake host into the utmp file\n\n**-t, --timeout seconds**\n> Terminate if no input within timeout"],["CONFIGURATION","**/etc/issue**\n> Pre-login message displayed before the login prompt. Supports escape codes for system information.\n\n**/etc/os-release**\n> Operating system identification data used by agetty for \\S escape in /etc/issue."],["CAVEATS","The baud rate is the speed of data transfer between a terminal and a device over a serial connection. Default is 9600 for serial terminals."],["HISTORY","Part of **util-linux** package. Alternative to the traditional **getty** command with additional features for modern Linux systems."],["INSTALL","```apt: sudo apt install util-linux```\n\n```dnf: sudo dnf install util-linux```\n\n```pacman: sudo pacman -S util-linux```\n\n```apk: sudo apk add agetty```\n\n```zypper: sudo zypper install util-linux```\n\n```brew: brew install util-linux```\n\n```nix: nix profile install nixpkgs#util-linux```"],["SEE ALSO","[login](/man/login)(1), [init](/man/init)(8), [getty](/man/getty)(8)"],["RESOURCES","```[Source code](https://github.com/util-linux/util-linux)```\n\n```[Documentation](https://man7.org/linux/man-pages/man8/agetty.8.html)```"]],"e":["login","init","getty"],"x":"agetty [tty] [115200]"},"aichat":{"n":"aichat","t":"Chat with AI models from the terminal","s":[["TAGLINE","Chat with AI models from the terminal"],["TLDR","**Ask a question**\n\n```aichat \"[explain quicksort]\"```\n\n**Pipe input for analysis**\n\n```cat [file.py] | aichat \"[review this code]\"```\n\n**Use a specific model**\n\n```aichat --model [claude:claude-sonnet-4-20250514] \"[question]\"```\n\n**Execute shell commands from natural language**\n\n```aichat -e \"[list large files in current directory]\"```\n\n**Start interactive chat session**\n\n```aichat```\n\n**Use a role/persona**\n\n```aichat --role [shell] \"[find duplicate files]\"```\n\n**Process a file**\n\n```aichat --file [document.pdf] \"[summarize this]\"```"],["SYNOPSIS","**aichat** [_options_] [_prompt_]"],["PARAMETERS","**-m**, **--model** _MODEL_\n> Model to use (provider:model format).\n\n**-r**, **--role** _ROLE_\n> Use a predefined role/persona.\n\n**-e**, **--execute**\n> Execute mode: translate natural language to shell commands.\n\n**--file** _FILE_\n> Include file in the conversation.\n\n**-S**, **--no-stream**\n> Disable streaming output.\n\n**--list-models**\n> List available models.\n\n**--list-roles**\n> List available roles.\n\n**--info**\n> Show current configuration."],["DESCRIPTION","**aichat** is an all-in-one AI CLI tool supporting chat, command execution, and RAG (Retrieval-Augmented Generation). It works with 20+ AI providers including OpenAI, Claude, Gemini, Ollama, Azure, and many more.\n\nIn chat mode, it provides a REPL with conversation history, multi-line input, and syntax highlighting. Execute mode translates natural language descriptions into shell commands and optionally runs them. RAG mode indexes documents for question-answering over local files.\n\nRoles define reusable personas and system prompts. Built-in roles include %code%, %shell%, %explain-shell%, and %functions%. Custom roles are defined as individual files in the roles configuration directory.\n\nThe tool supports function calling, allowing AI models to invoke defined tools. Sessions persist conversations across invocations. Multiple providers and models can be configured simultaneously.\n\nInstall via `cargo install aichat`, Homebrew, or download binaries."],["CONFIGURATION","**~/.config/aichat/config.yaml**\n> Main configuration file for model settings, API keys, roles, and default parameters.\n\n**~/.config/aichat/roles/**\n> Directory of custom role definitions, each as an individual file."],["CAVEATS","API keys required for cloud providers. Configuration file needed for multi-provider setup. RAG indexing requires additional setup. Token usage and costs vary by provider and model."],["HISTORY","**aichat** was created by **sigoden** in **2023** as a unified CLI for interacting with multiple AI providers. It grew from a simple chat tool into a comprehensive AI terminal toolkit with execute mode, RAG, and agent capabilities."],["INSTALL","```pacman: sudo pacman -S aichat```\n\n```brew: brew install aichat```\n\n```nix: nix profile install nixpkgs#aichat```"],["SEE ALSO","[mods](/man/mods)(1), [smartcat](/man/smartcat)(1), [ollama](/man/ollama)(1), [llm](/man/llm)(1)"]],"e":["mods","smartcat","ollama","llm"],"x":"aichat \"[explain quicksort]\""},"airbase-ng":{"n":"airbase-ng","t":"Create software-based wireless access points for penetration testing","s":[["TAGLINE","Create software-based wireless access points for penetration testing"],["TLDR","**Create** a fake access point\n\n```sudo airbase-ng -e \"[FakeSSID]\" -c [6] [wlan0mon]```\n\nCreate AP with **WPA1/TKIP**\n\n```sudo airbase-ng -e \"[SSID]\" -c [6] -z 2 [wlan0mon]```\n\nCreate AP with **WPA2/CCMP**\n\n```sudo airbase-ng -e \"[SSID]\" -c [6] -Z 4 [wlan0mon]```\n\nRespond to **all probes**\n\n```sudo airbase-ng -e \"[SSID]\" -P -c [6] [wlan0mon]```"],["SYNOPSIS","**airbase-ng** [_-e essid_] [_-c channel_] [_-a bssid_] [_options_] _interface_"],["DESCRIPTION","**airbase-ng** is an aircrack-ng suite tool that creates software access points for penetration testing. It can simulate access points, capture WPA handshakes, perform MITM attacks, and test client security.\n\nThe tool creates virtual interfaces (at0) that can be used to route traffic, enabling network analysis of connected clients."],["PARAMETERS","**-e** _essid_\n> Access point ESSID (network name)\n\n**-c** _channel_\n> Operating channel\n\n**-a** _bssid_\n> Set the BSSID (MAC address) of the access point\n\n**-W** _0|1_\n> Set the WEP flag advertised in beacons (0 = off, 1 = on; default 0)\n\n**-z** _type_\n> Set WPA1 cipher: 1 = WEP40, 2 = TKIP, 4 = CCMP, 5 = WEP104\n\n**-Z** _type_\n> Same as **-z** but for WPA2\n\n**-P**\n> Respond to all probe requests, including those for other ESSIDs\n\n**-C** _seconds_\n> Enable beaconing of probed ESSID values (used with **-P**), at this interval\n\n**-v**\n> Verbose output\n\n**-F** _prefix_\n> Write all sent and received frames to a pcap file with this prefix\n\n**-I** _interval_\n> Set the beacon interval in milliseconds\n\n**--caffe-latte**\n> Caffe-Latte attack mode\n\n**--cfrag**\n> PRGA attack using fragmentation"],["CAVEATS","For authorized security testing only. Requires interface in monitor mode. Creating fake access points may violate local laws if used without permission. Clients may connect and expose traffic."],["HISTORY","**airbase-ng** is part of the aircrack-ng suite, which evolved from aircrack in the mid-**2000s**. It was developed to test wireless security by simulating various access point scenarios."],["INSTALL","```apt: sudo apt install aircrack-ng```\n\n```dnf: sudo dnf install aircrack-ng```\n\n```pacman: sudo pacman -S aircrack-ng```\n\n```apk: sudo apk add aircrack-ng```\n\n```zypper: sudo zypper install aircrack-ng```\n\n```brew: brew install aircrack-ng```\n\n```nix: nix profile install nixpkgs#aircrack-ng```"],["SEE ALSO","[aircrack-ng](/man/aircrack-ng)(1), [airmon-ng](/man/airmon-ng)(1), [aireplay-ng](/man/aireplay-ng)(1), [airodump-ng](/man/airodump-ng)(1), [airtun-ng](/man/airtun-ng)(1), [airdecap-ng](/man/airdecap-ng)(1)"],["RESOURCES","```[Source code](https://github.com/aircrack-ng/aircrack-ng)```\n\n```[Homepage](https://www.aircrack-ng.org/)```\n\n```[Documentation](https://www.aircrack-ng.org/documentation.html)```"]],"e":["aircrack-ng","airmon-ng","aireplay-ng","airodump-ng","airtun-ng","airdecap-ng"],"x":"sudo airbase-ng -e \"[FakeSSID]\" -c [6] [wlan0mon]"},"apt":{"n":"apt","t":"High-level package management interface","s":[["TAGLINE","High-level package management interface"],["TLDR","**Update** the list of available packages\n\n```sudo apt update```\n\n**Search** packages by name or description\n\n```apt search [package]```\n\n**Show** detailed information about a package\n\n```apt show [package]```\n\n**Install** a package or update it to the latest version\n\n```sudo apt install [package]```\n\n**Remove** a package\n\n```sudo apt remove [package]```\n\n**Upgrade** all installed packages to their latest versions\n\n```sudo apt upgrade```\n\nList all **installed** packages\n\n```apt list --installed```\n\nList packages that **can be upgraded**\n\n```apt list --upgradable```\n\n**Remove** unused dependencies\n\n```sudo apt autoremove```"],["SYNOPSIS","**apt** [_options_] _command_ [_arguments_]"],["DESCRIPTION","**apt** is the high-level command-line interface for the package management system on Debian-based distributions. It is intended as a user-friendly alternative to **apt-get** for interactive use with improved defaults and progress display."],["PARAMETERS","**update**\n> Download package information from configured sources\n\n**upgrade**\n> Install available upgrades without removing packages\n\n**full-upgrade**\n> Perform upgrades, removing packages if necessary\n\n**install package**\n> Install or upgrade specified packages; use _package=version_ to pin a version or _package/codename_ to select a release\n\n**reinstall package**\n> Reinstall packages already at the newest version\n\n**remove package**\n> Remove specified packages\n\n**purge package**\n> Remove packages and their configuration files\n\n**autoremove**\n> Remove packages that were automatically installed but are no longer needed\n\n**search pattern**\n> Search for packages matching a pattern\n\n**show package**\n> Display package information\n\n**list**\n> List packages matching criteria (use **--installed**, **--upgradable**, or **--all-versions**)\n\n**satisfy string**\n> Satisfy dependency strings (as used in Build-Depends)\n\n**edit-sources**\n> Edit sources.list files\n\n**-y, --yes**\n> Assume yes to prompts\n\n**-q, --quiet**\n> Quiet output\n\n**-t, --target-release release**\n> Prefer packages from the given release (e.g. a backports suite)\n\n**--no-install-recommends**\n> Don't install recommended packages"],["CONFIGURATION","**/etc/apt/sources.list**\n> Main list of package repositories.\n\n**/etc/apt/sources.list.d/**\n> Directory for additional repository list files.\n\n**/etc/apt/apt.conf**\n> Main APT configuration file.\n\n**/etc/apt/apt.conf.d/**\n> Directory for additional APT configuration fragments.\n\n**/etc/apt/preferences**\n> Package pinning preferences.\n\n**/etc/apt/preferences.d/**\n> Directory for additional pinning preference files."],["CAVEATS","Intended for interactive use; for scripts, **apt-get** is recommended for more stable output formatting."],["HISTORY","**apt** was introduced in **Debian 8 (Jessie)** and **Ubuntu 14.04** as a unified frontend combining features from apt-get, apt-cache, and apt-config."],["INSTALL","```apt: sudo apt install apt```\n\n```dnf: sudo dnf install apt```\n\n```pacman: sudo pacman -S apt```\n\n```apk: sudo apk add apt```\n\n```brew: brew install apt```\n\n```nix: nix profile install nixpkgs#apt```"],["SEE ALSO","[apt-get](/man/apt-get)(8), [apt-cache](/man/apt-cache)(8), [dpkg](/man/dpkg)(1), [aptitude](/man/aptitude)(8)"],["RESOURCES","```[Source code](https://salsa.debian.org/apt-team/apt)```\n\n```[Documentation](https://manpages.debian.org/apt)```"]],"e":["apt-get","apt-cache","dpkg","aptitude"],"x":"sudo apt update"},"ascii-xfr":{"n":"ascii-xfr","t":"ASCII file transfer over serial connections","s":[["TAGLINE","ASCII file transfer over serial connections"],["TLDR","**Send** a file over the serial line\n\n```ascii-xfr -s [path/to/file]```\n\n**Receive** a file from the serial line\n\n```ascii-xfr -r [path/to/file]```\n\n**Send** a file with a **line delay** of 100 milliseconds\n\n```ascii-xfr -s -l 100 [path/to/file]```\n\n**Send** a file with a **character delay** of 10 milliseconds\n\n```ascii-xfr -s -c 10 [path/to/file]```\n\n**Send** a file and transmit an **end-of-file** character when done\n\n```ascii-xfr -s -e [path/to/file]```\n\n**Receive** a file with **verbose** output\n\n```ascii-xfr -r -v [path/to/file]```"],["SYNOPSIS","**ascii-xfr** **-s**|**-r** [**-ednv**] [**-l** _linedelay_] [**-c** _characterdelay_] _filename_"],["PARAMETERS","**-s**\n> Send a file\n\n**-r**\n> Receive a file\n\n**-e**\n> Send the End-Of-File character (Control-Z) when uploading has finished\n\n**-d**\n> Use Control-D instead of Control-Z as the EOF character\n\n**-n**\n> Do not translate CR/LF; skip automatic CRLF conversion\n\n**-v**\n> Verbose mode; display transfer statistics on stderr\n\n**-l** _milliseconds_\n> Line delay; wait this many milliseconds after each line when transmitting\n\n**-c** _milliseconds_\n> Character delay; wait this many milliseconds after each character when transmitting"],["DESCRIPTION","**ascii-xfr** is a file transfer utility that sends or receives files over serial connections using plain ASCII line-by-line transfer. It is part of the **minicom** package and is designed as a last-resort transfer method when the remote system does not support proper file transfer protocols like ZMODEM, XMODEM, or Kermit.\n\nDuring sending, end-of-line characters are transmitted as **CRLF**. During receiving, **CR** characters are stripped from incoming data. The tool reads from stdin when receiving and writes to stdout when sending, so it requires I/O redirection to the serial device, typically provided by minicom or a similar terminal emulator."],["CAVEATS","There is no error detection or correction. Data corruption during transfer goes undetected, making it unsuitable for binary files or unreliable links. The tool is designed for text file transfer only; binary files will be corrupted by the CRLF translation unless **-n** is used. The man page itself recommends it should only be used if the remote system does not support anything else."],["HISTORY","**ascii-xfr** was written by **Miquel van Smoorenburg** and **Jukka Lahtinen**, the authors of **minicom**. Minicom originated in the early 1990s as a free, text-based replacement for the DOS program Telix and became the de facto standard serial terminal emulator on Linux. ascii-xfr was created as a companion utility for the simplest possible file transfer scenario."],["INSTALL","```apt: sudo apt install minicom```\n\n```dnf: sudo dnf install minicom```\n\n```pacman: sudo pacman -S minicom```\n\n```apk: sudo apk add minicom```\n\n```zypper: sudo zypper install minicom```\n\n```brew: brew install minicom```\n\n```nix: nix profile install nixpkgs#minicom```"],["SEE ALSO","[minicom](/man/minicom)(1)"]],"e":["minicom"],"x":"ascii-xfr -s [path/to/file]"},"asterisk-cli":{"n":"asterisk-cli","t":"Interactive console for managing the Asterisk PBX server","s":[["TAGLINE","Interactive console for managing the Asterisk PBX server"],["TLDR","**Connect to the running Asterisk** console\n\n```asterisk -r```\n\n**Connect with verbose output**\n\n```asterisk -rvvv```\n\n**Execute a CLI command** from shell\n\n```asterisk -rx \"sip show peers\"```\n\n**Reload the dialplan**\n\n```asterisk -rx \"dialplan reload\"```\n\n**Show active calls**\n\n```asterisk -rx \"core show channels\"```\n\n**Show SIP registrations**\n\n```asterisk -rx \"sip show registry\"```\n\n**Gracefully shutdown** Asterisk\n\n```asterisk -rx \"core stop gracefully\"```"],["SYNOPSIS","**asterisk** **-r** [**-v**...]\n\n**asterisk** **-rx** \"_command_\""],["PARAMETERS","**-r**\n> Connect to a running Asterisk instance via remote console\n\n**-R**\n> Same as -r, but attempt reconnection if disconnected\n\n**-v**\n> Increase verbosity (can be repeated: -vvv for more detail)\n\n**-x** _command_\n> Execute a CLI command and exit\n\n**-c**\n> Start Asterisk in console mode (foreground)\n\n**-g**\n> Dump core on crash (for debugging)\n\n**-n**\n> Disable ANSI color output"],["CLI COMMANDS","**core show help** [_command_]\n> Display help for CLI commands\n\n**core show channels**\n> List active channels and calls\n\n**core show applications**\n> List available dialplan applications\n\n**dialplan reload**\n> Reload the dialplan configuration\n\n**sip show peers**\n> Display configured SIP peers\n\n**sip show registry**\n> Show SIP registration status\n\n**pjsip show endpoints**\n> Display PJSIP endpoints (modern SIP stack)\n\n**module reload** _module_\n> Reload a specific module\n\n**logger reload**\n> Reload logging configuration\n\n**core stop gracefully**\n> Stop Asterisk after calls complete\n\n**core stop now**\n> Stop Asterisk immediately\n\n**database show** [_family_]\n> Display AstDB database contents"],["DESCRIPTION","**Asterisk CLI** is the command-line interface for managing the Asterisk PBX (Private Branch Exchange) server. It provides real-time monitoring, configuration reloading, and administrative control over the telephony system.\n\nConnect to a running Asterisk instance with **asterisk -r**. For scripts and automation, use **asterisk -rx \"command\"** to execute commands non-interactively. Tab completion is available for commands within the interactive console.\n\nThe CLI provides access to all Asterisk subsystems including SIP/PJSIP peers, active channels, dialplan, voicemail, queues, and the internal database. Verbosity levels (-v flags) control the amount of real-time event output displayed.\n\nConfiguration changes typically require module reloads rather than full restarts. Use **dialplan reload** for extensions.conf changes or **module reload** for specific modules."],["CONFIGURATION","**/etc/asterisk/asterisk.conf**\n> Main Asterisk configuration file controlling global settings, module paths, and logging.\n\n**/etc/asterisk/extensions.conf**\n> Dialplan configuration defining call routing rules and IVR logic.\n\n**/etc/asterisk/sip.conf**\n> Legacy SIP peer and trunk configuration.\n\n**/etc/asterisk/pjsip.conf**\n> Modern PJSIP endpoint and transport configuration."],["CAVEATS","The Asterisk daemon must be running before connecting with **-r**. Some commands require specific modules to be loaded. The legacy **chan_sip** module uses **sip** commands while modern deployments use **pjsip** commands. Heavy verbosity can impact performance on busy systems."],["HISTORY","**Asterisk** was created by **Mark Spencer** at Digium (now Sangoma) and first released in **1999**. It pioneered software-based PBX systems and VoIP telephony. The CLI has evolved alongside Asterisk, with the introduction of PJSIP in Asterisk 12 (2013) providing a modern SIP stack alongside the legacy chan_sip module."],["INSTALL","```dnf: sudo dnf install asterisk```\n\n```aur: yay -S asterisk```\n\n```apk: sudo apk add asterisk```\n\n```nix: nix profile install nixpkgs#asterisk```"],["SEE ALSO","[asterisk](/man/asterisk)(8)"]],"e":["asterisk"],"x":"asterisk -r"},"b2-tools":{"n":"b2-tools","t":"Official Backblaze B2 cloud storage command-line tool","s":[["TAGLINE","Official Backblaze B2 cloud storage command-line tool"],["TLDR","**Authorize account**\n\n```b2 account authorize [applicationKeyId] [applicationKey]```\n\n**Upload a file to a bucket**\n\n```b2 file upload [bucketName] [localFile] [remoteName]```\n\n**Download a file from a bucket**\n\n```b2 file download [b2://bucketName/fileName] [localPath]```\n\n**List files in a bucket**\n\n```b2 ls [bucketName]```\n\n**Create a new bucket**\n\n```b2 bucket create [bucketName] [allPrivate]```\n\n**Sync a local directory to B2**\n\n```b2 sync [localDir] b2://[bucketName]/[prefix]```"],["SYNOPSIS","**b2** _command_ [_options_]"],["PARAMETERS","**account authorize** _keyID_ _key_\n> Authorize with B2 using application key.\n\n**file upload** _bucket_ _local_ _remote_\n> Upload a file to a bucket.\n\n**file download** _uri_ _local_\n> Download a file from B2.\n\n**ls** _bucket_\n> List files in a bucket.\n\n**bucket create** _name_ _type_\n> Create a bucket (allPrivate or allPublic).\n\n**bucket delete** _name_\n> Delete a bucket.\n\n**sync** _source_ _destination_\n> Synchronize files between local and B2 or between B2 buckets.\n\n**file hide** _bucket_ _file_\n> Hide a file (soft delete)."],["DESCRIPTION","**b2-tools** (invoked as **b2**) is the official command-line tool for Backblaze B2 Cloud Storage. It provides file upload, download, sync, and bucket management operations for B2's S3-compatible object storage.\n\nThe sync command efficiently mirrors local directories to B2, uploading only changed files. B2 provides affordable cloud storage with free egress through Cloudflare's bandwidth alliance."],["CAVEATS","Requires application key authorization before use. Large file uploads are automatically split into parts. The sync command compares by SHA1 hash, so the first sync of large directories can be slow. B2-native URIs use the `b2://` prefix."],["HISTORY","**b2-tools** is the official CLI developed by **Backblaze** for their B2 Cloud Storage service. It was open-sourced on GitHub and is installable via pip as `b2`."],["INSTALL","```brew: brew install b2-tools```"],["SEE ALSO","[b2](/man/b2)(1), [rclone](/man/rclone)(1), [duplicity](/man/duplicity)(1)"]],"e":["b2","rclone","duplicity"],"x":"b2 account authorize [applicationKeyId] [applicationKey]"},"bloodhound":{"n":"bloodhound","t":"Visualize Active Directory attack paths","s":[["TAGLINE","Visualize Active Directory attack paths"],["TLDR","**Start the BloodHound GUI** (legacy Electron app)\n\n```bloodhound```\n\n**Start without the Chromium sandbox** (common workaround on modern Linux)\n\n```bloodhound --no-sandbox```\n\n**Disable GPU acceleration** if the window renders incorrectly\n\n```bloodhound --no-sandbox --disable-gpu```\n\nAfter launch, log in to the Neo4j database and use the GUI \"Upload Data\" button to import the JSON or zip produced by a collector."],["SYNOPSIS","**bloodhound** [_electron-options_]"],["DESCRIPTION","**BloodHound** is a security tool that uses graph theory to reveal hidden relationships and attack paths within Active Directory and Azure environments. It visualizes complex trust relationships, group memberships, and permissions that attackers could exploit to compromise domain administrators.\n\nThe tool consists of two parts: collectors (SharpHound for Windows, bloodhound-python for Linux) that enumerate directory objects, and the BloodHound application that imports this data into a Neo4j graph database for analysis.\n\nThe **bloodhound** command described here launches the legacy Electron desktop GUI. It is a graphical client and takes no BloodHound-specific command-line flags: databases and credentials are configured in the login screen, and collected data is imported through the GUI. The legacy edition is deprecated. The actively maintained successor, BloodHound Community Edition (BHCE), ships as a web application deployed with Docker Compose (or the bloodhound-cli helper) rather than as a standalone command.\n\nSecurity teams use BloodHound to identify and remediate dangerous configurations, while penetration testers use it to find privilege escalation paths. Built-in queries find common attack paths like \"Shortest Path to Domain Admin\" or \"Kerberoastable Users.\""],["CAVEATS","BloodHound is a powerful security tool that requires proper authorization before use. Collecting data from Active Directory may trigger security alerts. The tool shows theoretical attack paths; actual exploitation requires additional steps. The legacy GUI requires a running Neo4j instance, configured at the login screen. On modern Linux desktops the Electron app often needs **--no-sandbox** to start. Large environments may require significant memory for analysis. The legacy edition is no longer maintained; new deployments should use BloodHound Community Edition."],["HISTORY","BloodHound was created by **Andy Robbins**, **Rohan Vazarkar**, and **Will Schroeder** at SpecterOps and first presented at **DEF CON 24** in **2016**. It revolutionized Active Directory security assessments by visualizing complex relationships that were previously difficult to analyze manually. The tool is open source and has become an industry standard for AD security assessment."],["INSTALL","```nix: nix profile install nixpkgs#bloodhound```"],["SEE ALSO","[bloodhound-python](/man/bloodhound-python)(1), [neo4j](/man/neo4j)(1), [impacket](/man/impacket)(1), [ldapsearch](/man/ldapsearch)(1)"],["RESOURCES","```[Source code](https://github.com/SpecterOps/BloodHound)```\n\n```[Homepage](https://bloodhound.specterops.io/)```"]],"e":["bloodhound-python","neo4j","impacket","ldapsearch"],"x":"bloodhound"},"bosh":{"n":"bosh","t":"Manage distributed system deployments and lifecycle operations.","s":[["TAGLINE","Manage distributed system deployments and lifecycle operations."],["TLDR","**Deploy** release\n\n```bosh -d [deployment] deploy [manifest.yml]```\n\n**List** deployments\n\n```bosh deployments```\n\n**SSH** into instance\n\n```bosh -d [deployment] ssh [instance]```\n\n**View** logs\n\n```bosh -d [deployment] logs [instance]```\n\n**Update** cloud config\n\n```bosh update-cloud-config [cloud-config.yml]```"],["SYNOPSIS","**bosh** [_options_] _command_"],["DESCRIPTION","**bosh** is the command-line interface for BOSH, a tool for release engineering, deployment, and lifecycle management of distributed systems. It's commonly used for deploying Cloud Foundry and other complex distributed applications.\n\nThe tool manages infrastructure provisioning, software deployment, and operational concerns across multiple cloud providers."],["PARAMETERS","**-d**, **--deployment** _name_\n> Deployment name\n\n**-e**, **--environment** _alias_\n> Environment alias\n\n**-n**, **--non-interactive**\n> Non-interactive mode\n\n**--json**\n> JSON output\n\n**--column** _header_\n> Filter output columns"],["COMMON COMMANDS","**deployments**\n> List all deployments\n\n**deploy** _manifest_\n> Create or update deployment\n\n**delete-deployment**\n> Delete deployment\n\n**ssh** _instance_\n> SSH into VM\n\n**logs** _instance_\n> Fetch logs\n\n**vms**\n> List VMs\n\n**tasks**\n> List tasks\n\n**cloud-config**\n> Show cloud configuration"],["WORKFLOW","```bash"],["Target environment","bosh alias-env prod -e 10.0.0.6 --ca-cert cert.pem"],["Log in","bosh -e prod login"],["Upload stemcell","bosh -e prod upload-stemcell stemcell.tgz"],["Upload release","bosh -e prod upload-release release.tgz"],["Deploy","bosh -e prod -d myapp deploy manifest.yml"],["Check VMs","bosh -e prod -d myapp vms"],["SSH","bosh -e prod -d myapp ssh instance/0\n```"],["FEATURES","- Multi-cloud support (AWS, GCP, Azure, vSphere)\n- Automated health monitoring\n- Rolling updates\n- Resurrection (automatic VM recovery)\n- Snapshots and backups\n- Configuration management\n- Network isolation"],["CAVEATS","Steep learning curve. Complex configuration. Requires dedicated infrastructure. Best suited for large deployments. Manifest syntax can be intricate. Debugging difficult without experience."],["HISTORY","**BOSH** was created by VMware around **2010** for Cloud Foundry deployment, becoming an open-source project for managing complex distributed systems."],["INSTALL","```apt: sudo apt install bosh```\n\n```zypper: sudo zypper install bosh```"],["SEE ALSO","[cf](/man/cf)(1), [terraform](/man/terraform)(1), [kubectl](/man/kubectl)(1)"],["RESOURCES","```[Source code](https://github.com/cloudfoundry/bosh-cli)```\n\n```[Homepage](https://bosh.io)```\n\n```[Documentation](https://bosh.io/docs/cli-v2/)```"]],"e":["cf","terraform","kubectl"],"x":"bosh -d [deployment] deploy [manifest.yml]"},"caffeinate":{"n":"caffeinate","t":"create a macOS power-management assertion to keep the system awake","s":[["TAGLINE","create a macOS power-management assertion to keep the system awake"],["TLDR","**Prevent idle sleep** indefinitely (press Ctrl-C to stop)\n\n```caffeinate```\n\n**Prevent the display from sleeping**\n\n```caffeinate -d```\n\n**Prevent idle sleep for a duration** (in seconds)\n\n```caffeinate -t [3600]```\n\n**Prevent sleep while a command runs**\n\n```caffeinate -i [make]```\n\n**Combine flags** — keep display and disk awake for 1 hour\n\n```caffeinate -dim -t [3600]```\n\n**Hold the assertion until a process** (by PID) exits\n\n```caffeinate -w [pid]```\n\n**Prevent system sleep** (stronger, only effective on AC power)\n\n```caffeinate -s```"],["SYNOPSIS","**caffeinate** [**-disum**] [**-t** _timeout_] [**-w** _pid_] [_utility_ [_arguments_]]"],["PARAMETERS","**-d**\n> Prevent the display from sleeping.\n\n**-i**\n> Prevent the system from idle sleeping. (default when no assertion flag is given)\n\n**-m**\n> Prevent the disk from idle sleeping.\n\n**-s**\n> Prevent the system from sleeping. Only effective when plugged into AC power on a laptop.\n\n**-u**\n> Declare that the user is active. The assertion is released automatically after 5 seconds (or sooner if cancelled) — useful to bump activity without holding it indefinitely.\n\n**-t** _timeout_\n> Release the assertion after _timeout_ seconds. Without this flag the assertion holds until caffeinate is terminated or the wrapped utility exits.\n\n**-w** _pid_\n> Hold the assertion until the process with the given PID exits.\n\n_utility_ [_arguments_]\n> If a utility is given, caffeinate runs it and holds the assertion until it exits. `caffeinate -i make` is the idiomatic pattern."],["DESCRIPTION","**caffeinate** creates one or more **IOKit power-management assertions** to suppress different kinds of sleep. It is the supported way to prevent macOS from entering display, idle, disk, or system sleep for the duration of a task.\n\nWhen invoked with a utility, caffeinate forks and runs the utility with the assertion(s) held until the utility exits. Without a utility, it runs until it receives a signal (e.g. Ctrl-C) or until `-t` expires. Multiple flags may be combined; each adds its own assertion."],["CAVEATS","macOS only (shipped in `/usr/bin/caffeinate` since OS X 10.8 Mountain Lion). On battery, `-s` may be ignored — `-i` is the reliable choice for laptops. `-u` gives only a brief nudge; use `-d -i` for longer guarantees. Creating an assertion does not override closing the laptop lid (clamshell) — for that, use `pmset`.\n\nFor Linux, the equivalent is `systemd-inhibit` or `caffeine` (GNOME applet). For X11, `xset s off -dpms` controls screensaver and DPMS."],["HISTORY","**caffeinate** was introduced in **Mac OS X 10.8 (Mountain Lion, 2012)** by Apple as the supported CLI replacement for ad-hoc scripts that poked mouse events to keep the Mac awake. It wraps the IOKit `IOPMAssertion` API."],["INSTALL","```aur: yay -S caffeinate```"],["SEE ALSO","[caffeine](/man/caffeine)(1), [xset](/man/xset)(1), [systemd-inhibit](/man/systemd-inhibit)(1)"]],"e":["caffeine","xset","systemd-inhibit"],"x":"caffeinate"},"cavezofphear":{"n":"cavezofphear","t":"ASCII Boulder Dash clone","s":[["TAGLINE","ASCII Boulder Dash clone"],["TLDR","**Start a game**\n\n```phear```"],["SYNOPSIS","**phear** [_options_]"],["PREVIEW","```\n ###########\n # @ o . . #\n # . # o . #\n # . . . * #\n # . o # . #\n ###########\n```"],["DESCRIPTION","**cavezofphear** (phear) is an ASCII Boulder Dash clone. The player navigates through cave levels, collecting diamonds while avoiding falling boulders and enemies. Pushing boulders and timing movements are key to completing each level.\n\nUse arrow keys to move. Collect all diamonds to open the exit and advance to the next level."],["CAVEATS","Requires ncurses support. The command name is **phear**, not cavezofphear."],["HISTORY","**cavezofphear** is inspired by **Boulder Dash**, a classic computer game created by **Peter Liepa** and **Chris Gray** in **1984**."],["SEE ALSO","[robots](/man/robots)(6), [greed](/man/greed)(6)"]],"e":["robots","greed"],"x":"phear"},"cdparanoia":{"n":"cdparanoia","t":"CD audio ripper with error correction","s":[["TAGLINE","CD audio ripper with error correction"],["TLDR","**Rip entire CD to WAV files**\n\n```cdparanoia -B```\n\n**Rip specific track**\n\n```cdparanoia [5] [track05.wav]```\n\n**Rip track range**\n\n```cdparanoia [1-5]```\n\n**Query CD table of contents**\n\n```cdparanoia -Q```\n\n**Output raw PCM data**\n\n```cdparanoia -r [1] [track.raw]```\n\n**Verbose output**\n\n```cdparanoia -v [1]```"],["SYNOPSIS","**cdparanoia** [_options_] _span_ [_outfile_]"],["DESCRIPTION","**cdparanoia** is a CD digital audio extraction tool with advanced error correction and jitter handling. It reads audio tracks from CDDA-capable drives and performs verification, synchronization, and scratch reconstruction to produce bit-perfect rips whenever possible.\n\nUnlike simple CD rippers, cdparanoia uses multiple read passes and cross-correlates data to detect and correct errors caused by scratches, smudges, or drive imperfections. The progress display shows smiley faces indicating read quality, from normal operation to scratch detection and correction failures.\n\nOutput formats include WAV (default), AIFF, AIFF-C, and raw 16-bit PCM. Batch mode (`-B`) splits output into one file per track automatically."],["PARAMETERS","**-B**, **--batch**\n> Batch mode, split output at track boundaries\n\n**-Q**, **--query**\n> Query and print CD table of contents\n\n**-d** _device_, **--force-cdrom-device** _device_\n> Specify CD-ROM device\n\n**-v**, **--verbose**\n> Verbose output for debugging\n\n**-q**, **--quiet**\n> Quiet operation\n\n**-w**, **--output-wav**\n> Output WAV format (default)\n\n**-a**, **--output-aiff**\n> Output AIFF format\n\n**-r**, **--output-raw**\n> Output raw 16-bit PCM\n\n**-R**, **--output-raw-big-endian**\n> Output raw big-endian PCM\n\n**-c**, **--output-aifc**\n> Output AIFF-C format\n\n**-f**, **--force-cdrom-little-endian**\n> Force treating the drive as little endian.\n\n**-F**, **--force-cdrom-big-endian**\n> Force treating the drive as big endian.\n\n**-g** _device_, **--force-generic-device** _device_\n> Force use of the old generic SCSI (sg) interface with the specified device.\n\n**-S** _speed_, **--force-read-speed** _speed_\n> Set the read speed of the CD drive (where supported).\n\n**-A**, **--analyze-drive**\n> Run and log a complete analysis of drive caching, timing and reading behavior.\n\n**-z**, **--never-skip**\n> Never skip failed reads, retry indefinitely.\n\n**-Z**, **--disable-paranoia**\n> Disable all data verification and correction features.\n\n**-X**, **--abort-on-skip**\n> If a read is skipped due to imperfect data, abort reading the track."],["SPAN FORMAT","**N**: Track N\n\n**N-M**: Tracks N through M\n\n**N-**: Track N to end\n\n**-M**: Beginning to track M"],["PROGRESS SYMBOLS","**:-)** Normal operation, low jitter\n\n**:-|** Considerable jitter\n\n**:-(** Scratch detected\n\n**;-(** Gave up correction\n\n**:^D** Finished"],["CAVEATS","Requires CDDA-capable drive. Works best with Accurate Stream drives that don't cache audio. Scratched discs may cause long read times."],["INSTALL","```apt: sudo apt install cdparanoia```\n\n```dnf: sudo dnf install cdparanoia```\n\n```pacman: sudo pacman -S cdparanoia```\n\n```apk: sudo apk add cdparanoia```\n\n```zypper: sudo zypper install cdparanoia```\n\n```nix: nix profile install nixpkgs#cdparanoia```"],["SEE ALSO","[abcde](/man/abcde)(1), [lame](/man/lame)(1), [flac](/man/flac)(1), [sox](/man/sox)(1)"]],"e":["abcde","lame","flac","sox"],"x":"cdparanoia -B"},"chcat":{"n":"chcat","t":"change SELinux security categories","s":[["TAGLINE","change SELinux security categories"],["TLDR","**List** all available categories\n\n```sudo chcat -L```\n\n**Add** a category to a file\n\n```sudo chcat +[CategoryName] [path/to/file]```\n\n**Remove** a category from a file\n\n```sudo chcat -- -[CategoryName] [path/to/file]```\n\n**Set** specific categories for a file\n\n```sudo chcat [CategoryName1,CategoryName2,...] [path/to/file]```\n\n**Display** categories of a file\n\n```ls -Z [path/to/file]```\n\n**Remove all** categories from a file\n\n```sudo chcat -d [path/to/file]```"],["SYNOPSIS","**chcat** [_options_] _category_ _file_..."],["DESCRIPTION","**chcat** changes SELinux security categories for files. Categories are part of Multi-Category Security (MCS), providing additional access control beyond traditional SELinux types.\n\nMCS categories allow compartmentalization of data, where users or processes can only access files with matching categories."],["PARAMETERS","**-L, --list**\n> List all available categories\n\n**-d, --delete**\n> Remove all categories from file\n\n**+**_category_\n> Add a category\n\n**-**_category_\n> Remove a category (use -- before)"],["CAVEATS","Requires SELinux to be enabled with MCS policy. Category names must be defined in the policy. Changes affect access control immediately."],["SEE ALSO","[chcon](/man/chcon)(1), [semanage](/man/semanage)(8), [ls](/man/ls)(1)"],["RESOURCES","```[Source code](https://github.com/SELinuxProject/selinux)```"]],"e":["chcon","semanage","ls"],"x":"sudo chcat -L"},"code-server":{"n":"code-server","t":"VS Code in the browser on a remote server","s":[["TAGLINE","VS Code in the browser on a remote server"],["TLDR","**Start code-server**\n\n```code-server```\n\n**Start on specific port**\n\n```code-server --bind-addr [0.0.0.0:8080]```\n\n**Start with specific directory**\n\n```code-server [/path/to/project]```\n\n**Disable authentication**\n\n```code-server --auth none```\n\n**Use specific config**\n\n```code-server --config [~/.config/code-server/config.yaml]```\n\n**Show password**\n\n```cat ~/.config/code-server/config.yaml```"],["SYNOPSIS","**code-server** [_options_] [_path_]"],["DESCRIPTION","**code-server** runs Visual Studio Code on a remote server, making it accessible through any web browser. This enables development from Chromebooks, tablets, and other devices that cannot run VS Code natively, while maintaining a consistent development environment with full access to server resources.\n\nThe service provides nearly identical functionality to desktop VS Code, including extension support, terminal access, and file system integration. It's particularly useful for remote development, cloud workspaces, and teams needing standardized environments. All compute and storage occurs on the server, with only the interface rendered in the browser.\n\nAuthentication can be password-based or disabled for trusted networks. TLS certificates enable secure HTTPS connections. The tool is developed by Coder and runs as a long-lived service, typically behind a reverse proxy in production deployments."],["PARAMETERS","**--bind-addr** _host:port_\n> Address to bind (default: 127.0.0.1:8080)\n\n**--auth** _type_\n> Authentication: password, none\n\n**--password** _pass_\n> Set password (or use PASSWORD env)\n\n**--cert** _file_\n> TLS certificate file\n\n**--cert-key** _file_\n> TLS key file\n\n**--config** _file_\n> Configuration file path\n\n**--user-data-dir** _dir_\n> User data directory\n\n**--extensions-dir** _dir_\n> Extensions directory\n\n**--disable-telemetry**\n> Disable telemetry"],["CONFIGURATION","~/.config/code-server/config.yaml:\n```yaml\nbind-addr: 127.0.0.1:8080\nauth: password\npassword: yourpassword\ncert: false\n```"],["REQUIREMENTS","- 1 GB RAM minimum\n- 2 vCPUs\n- WebSockets enabled"],["INSTALLATION","```\ncurl -fsSL https://code-server.dev/install.sh | sh\n```"],["CAVEATS","Default password in config file. Use TLS in production. Some extensions may not work in browser."],["INSTALL","```brew: brew install code-server```\n\n```nix: nix profile install nixpkgs#code-server```"],["SEE ALSO","[code](/man/code)(1), [vim](/man/vim)(1)"],["RESOURCES","```[Source code](https://github.com/coder/code-server)```\n\n```[Documentation](https://coder.com/docs/code-server)```"]],"e":["code","vim"],"x":"code-server"},"compactdiff":{"n":"compactdiff","t":"Version, replay, and audit AI agent session transcripts","s":[["TAGLINE","Version, replay, and audit AI agent session transcripts"],["TLDR","**Watch** Claude Code and Codex sessions and snapshot changes into a local vault\n\n```compactdiff watch```\n\n**Snapshot** one transcript file into the vault now\n\n```compactdiff snapshot [~/.claude/projects/proj/session.jsonl]```\n\n**List** vaulted sessions and their version history\n\n```compactdiff list```\n\n**Diff** two snapshot SHAs to see what compaction removed\n\n```compactdiff diff [shaA] [shaB]```\n\n**Export** a self-contained HTML replay of a transcript or vault SHA\n\n```compactdiff export [file|sha] -o [replay.html]```\n\n**Branch** a transcript after N conversational messages to a truncated JSONL file\n\n```compactdiff branch [file|sha] --at [12] -o [fork.jsonl]```\n\n**Use a custom vault directory** (default **~/.compactdiff**)\n\n```compactdiff --vault [path/to/vault] list```"],["SYNOPSIS","**compactdiff** [**--vault** _DIR_] [**--no-redact** **--i-understand-sensitive-data**] _command_ [_args_]"],["PARAMETERS","**--vault** _DIR_\n> Vault directory for content-addressed snapshots (default **~/.compactdiff**).\n\n**--no-redact**\n> Store and export raw transcript data without redaction. Unsafe; must be paired with **--i-understand-sensitive-data**. Both global flags go before the subcommand.\n\n**--i-understand-sensitive-data**\n> Required acknowledgement when **--no-redact** is set. Without it, the CLI exits with an error.\n\n**snapshot** _file_...\n> Snapshot one or more transcript files into the vault now. Prints short SHA, NEW/unchanged status, session id, and line count per file.\n\n**watch**\n> Poll default Claude Code and Codex session globs and auto-snapshot every change into the vault. **--pattern** _GLOB_ replaces the default globs with one custom glob. **--interval** _SECONDS_ sets the poll period (default **5**). Stop with Ctrl-C.\n\n**list**\n> List every vaulted session and its version chain (short SHA, timestamp, line count).\n\n**diff** _sha_old_ _sha_new_\n> Compare two vaulted snapshots (full SHA or unique prefix). Prints a verdict (**append-only** or **history-rewritten**) with kept/added/removed counts and previews of removed and added lines.\n\n**export** _source_ **-o** _file_\n> Export a transcript path or vaulted SHA to a single self-contained HTML replay timeline (user / assistant / thinking / tool events). No server or external assets.\n\n**branch** _source_ **--at** _N_ **-o** _file_\n> Write an inspectable truncated prefix of a transcript (path or vault SHA) after the first _N_ conversational messages. Codex developer bootstrap records are preserved but do not consume branch numbers. Does not claim direct resume in Claude/Codex."],["DESCRIPTION","**compactdiff** is a local-first CLI that versions, audits, and replays AI coding-agent session transcripts. Agents such as Claude Code and Codex persist sessions as JSONL and may rewrite them in place during compaction, so history disappears and there is no built-in way to share a readable session. compactdiff snapshots observed states into a content-addressed vault under **~/.compactdiff**, so compaction cannot destroy earlier versions.\n\n**watch** polls Claude Code projects (**~/.claude/projects/**) and Codex sessions (**~/.codex/sessions/**) by default; **snapshot** records specific files on demand. **list** shows each session's version chain. **diff** proves what a rewrite removed with occurrence-aware ordered line counts (including duplicates and blank lines), keyed on line uuid when present. **export** writes a dark HTML timeline you can open offline or send as one file. **branch** cuts a truncated prefix for inspection or caller-managed seeding.\n\nRequires **Python 3.9+** and uses only the standard library at runtime. Install from a checkout with **python3 -m pip install -e .** (or install the **compactdiff** package when published). Redaction of common secret fields, tokens, emails, phones, and home-directory usernames is on by default for **snapshot**, **watch**, **export**, and **branch**."],["CAVEATS","Agent transcripts can hold credentials, private prompts, source code, and personal data. Default redaction is defense-in-depth, not a guarantee—inspect every replay before sharing and keep the vault private. Raw mode (**--no-redact**) may store secrets. Session IDs and prose may remain sensitive after pattern redaction. The watcher polls (default 5s), not filesystem events. Branch files are truncated prefixes for inspection; they do not automate resume in Claude Code or Codex. Cursor and other non-JSONL agent stores are out of scope in v1."],["HISTORY","**compactdiff** is a pure-stdlib Python tool (MIT) for local agent-session vaults, audit diffs, HTML replay, and branching. Version **0.1.0** focuses on Claude Code JSONL, Codex-native sessions, and optional Nerve Codex runner logs, with redaction enabled by default."],["SEE ALSO","[diff](/man/diff)(1), [claude](/man/claude)(1), [codex](/man/codex)(1), [claude-hibernate](/man/claude-hibernate)(1), [agent-vault](/man/agent-vault)(1)"],["RESOURCES","```[Source code](https://github.com/mkmkkkkk/compactdiff)```\n\n```[Documentation](https://github.com/mkmkkkkk/compactdiff#readme)```"]],"e":["diff","claude","codex","claude-hibernate","agent-vault"],"x":"compactdiff watch"},"dexdump":{"n":"dexdump","t":"Android DEX file analysis and disassembly tool","s":[["TAGLINE","Android DEX file analysis and disassembly tool"],["TLDR","**Dump a DEX file**\n\n```dexdump [classes.dex]```\n\n**Dump with disassembly**\n\n```dexdump -d [classes.dex]```\n\n**Dump file header** summary\n\n```dexdump -f [classes.dex]```\n\n**Dump exported items** only\n\n```dexdump -e -d [classes.dex]```\n\n**Output to file**\n\n```dexdump [classes.dex] -o [output.txt]```"],["SYNOPSIS","**dexdump** [_options_] _dexfile_"],["PARAMETERS","**-d**\n> Disassemble code sections.\n\n**-f**\n> Display summary information from file header.\n\n**-h**\n> Display file header details.\n\n**-e**\n> Display exported items only.\n\n**-a**\n> Display annotations.\n\n**-c**\n> Verify checksum and exit.\n\n**-g**\n> Dump CFG for methods (Graphviz dot format).\n\n**-l** _layout_\n> Output layout: plain, xml.\n\n**-o** _file_\n> Output to file instead of stdout."],["DESCRIPTION","**dexdump** displays detailed information about Android DEX (Dalvik Executable) files. It can dump class definitions, method signatures, field information, and disassemble bytecode into human-readable format.\n\nThe tool is part of the Android SDK build tools and provides insight into the structure of compiled Android applications. It can output information in plain text or XML format for further processing.\n\nDEX files contain compiled Java bytecode for the Dalvik VM or Android Runtime (ART). dexdump helps developers and security researchers understand app internals, debug compilation issues, and perform reverse engineering analysis."],["INSTALL","```apt: sudo apt install dexdump```"],["SEE ALSO","[apktool](/man/apktool)(1), [jadx](/man/jadx)(1), [dex2jar](/man/dex2jar)(1)"],["RESOURCES","```[Source code](https://android.googlesource.com/platform/art/+/refs/heads/main/dexdump/)```"]],"e":["apktool","jadx","dex2jar"],"x":"dexdump [classes.dex]"},"docker-container-cp":{"n":"docker-container-cp","t":"copy files between container and host","s":[["TAGLINE","copy files between container and host"],["TLDR","**Copy file from container to host**\n\n```docker container cp [container]:[/path/in/container] [/local/path]```\n\n**Copy file from host to container**\n\n```docker container cp [/local/file] [container]:[/path/in/container]```\n\n**Copy directory**\n\n```docker container cp [container]:[/src/dir] [/dest/dir]```"],["SYNOPSIS","**docker** **container** **cp** [_options_] _src_ _dest_"],["PARAMETERS","**-a**, **--archive**\n> Archive mode (preserve permissions).\n\n**-L**, **--follow-link**\n> Follow symbolic links."],["DESCRIPTION","**docker container cp** copies files between containers and the local filesystem. Works with running or stopped containers."],["INSTALL","```apt: sudo apt install docker-cli```\n\n```dnf: sudo dnf install docker-cli```\n\n```pacman: sudo pacman -S docker```\n\n```apk: sudo apk add docker-cli```\n\n```zypper: sudo zypper install docker```\n\n```brew: brew install docker```\n\n```nix: nix profile install nixpkgs#docker```"],["SEE ALSO","[docker-cp](/man/docker-cp)(1), [docker-container](/man/docker-container)(1)"],["RESOURCES","```[Source code](https://github.com/docker/cli)```\n\n```[Documentation](https://docs.docker.com/reference/cli/docker/)```"]],"e":["docker-cp","docker-container"],"x":"docker container cp [container]:[/path/in/container] [/local/path]"},"docker-service":{"n":"docker-service","t":"manage containerized services in Docker Swarm","s":[["TAGLINE","manage containerized services in Docker Swarm"],["TLDR","**Create a service**\n\n```docker service create --name [name] [image]```\n\n**List services**\n\n```docker service ls```\n\n**Inspect a service**\n\n```docker service inspect [service]```\n\n**Scale a service**\n\n```docker service scale [service]=[replicas]```\n\n**Update service image**\n\n```docker service update --image [image]:[tag] [service]```\n\n**View service logs**\n\n```docker service logs [service]```\n\n**Remove a service**\n\n```docker service rm [service]```"],["SYNOPSIS","**docker** **service** _command_ [_options_]"],["SUBCOMMANDS","**create**\n> Create a new service.\n\n**ls**\n> List services.\n\n**inspect**\n> Display detailed information.\n\n**update**\n> Update a service.\n\n**scale**\n> Scale services.\n\n**logs**\n> Fetch service logs.\n\n**ps**\n> List tasks of a service.\n\n**rm**\n> Remove services.\n\n**rollback**\n> Revert a service to its previous specification."],["COMMON OPTIONS","**--replicas** _n_ (create/scale)\n> Number of replicated tasks to run.\n\n**--mode** _mode_ (create)\n> **replicated** (default) or **global** (one task per node).\n\n**--publish** _published:target_ (create)\n> Publish a port externally (e.g. `8080:80`).\n\n**--network** _name_ (create)\n> Attach the service to a swarm overlay network.\n\n**--env**, **-e** _KEY=VAL_ (create/update)\n> Set environment variables.\n\n**--constraint** _expr_ (create)\n> Placement constraints (e.g. `node.role==worker`).\n\n**--update-parallelism** _n_ / **--update-delay** _dur_ (create/update)\n> Rolling-update concurrency and delay between batches."],["DESCRIPTION","**docker service** manages Swarm services. A service is a declaration of how a set of containers should run across a swarm: image, replica count, networks, mounts, secrets, resource limits, and update strategy. The swarm orchestrator schedules tasks (container instances) on cluster nodes and maintains the desired state, replacing failed tasks and routing traffic via the routing mesh.\n\nService commands are only available on a Docker host participating in a swarm; initialize one with **docker swarm init** before using them. Updates are applied as rolling updates by default, with **rollback** available to revert to the previous service spec."],["CAVEATS","Requires swarm mode (**docker swarm init** / **join**). For single-host workloads use **docker run** or **docker compose** instead. Rolling updates with **--update-parallelism 0** will replace all tasks at once and can cause downtime."],["INSTALL","```apt: sudo apt install docker-cli```\n\n```dnf: sudo dnf install docker-cli```\n\n```pacman: sudo pacman -S docker```\n\n```apk: sudo apk add docker-cli```\n\n```zypper: sudo zypper install docker```\n\n```brew: brew install docker```\n\n```nix: nix profile install nixpkgs#docker```"],["SEE ALSO","[docker-swarm](/man/docker-swarm)(1), [docker-node](/man/docker-node)(1), [docker](/man/docker)(1), [docker-compose](/man/docker-compose)(1)"]],"e":["docker-swarm","docker-node","docker","docker-compose"],"x":"docker service create --name [name] [image]"},"dropbearconvert":{"n":"dropbearconvert","t":"convert SSH keys between Dropbear and OpenSSH formats","s":[["TAGLINE","convert SSH keys between Dropbear and OpenSSH formats"],["TLDR","Convert **OpenSSH to Dropbear** format\n\n```dropbearconvert openssh dropbear ~/.ssh/id_rsa output_key```\n\nConvert **Dropbear to OpenSSH** format\n\n```dropbearconvert dropbear openssh input_key ~/.ssh/id_rsa```"],["SYNOPSIS","**dropbearconvert** _input_type_ _output_type_ _input_file_ _output_file_"],["DESCRIPTION","**dropbearconvert** converts private keys between Dropbear and OpenSSH formats. Dropbear and OpenSSH use different private key formats, and this utility bridges the two implementations."],["PARAMETERS","**input_type**\n> Format of the input key: \"dropbear\" or \"openssh\"\n\n**output_type**\n> Format of the output key: \"dropbear\" or \"openssh\"\n\n**input_file**\n> Path to the input private key file\n\n**output_file**\n> Path for the converted output key file"],["CAVEATS","**Passphrase-protected keys cannot be converted.** dropbearconvert has no way to prompt for a passphrase, so an encrypted OpenSSH key must be decrypted first, for example with `ssh-keygen -p -N \"\" -f key`, and the resulting plaintext key handled carefully. Note also that Dropbear's own format has no encrypted variant at all: converting *to* Dropbear always yields an unprotected private key on disk.\n\nOnly private keys are converted. Public keys need no conversion, since both implementations use the same one-line `authorized_keys` format.\n\nThe tool overwrites the output file without asking, and converting the wrong direction silently produces a file the other end cannot read, so keep a copy of the original."],["HISTORY","**dropbearconvert** exists because Dropbear predates OpenSSH's current key format and chose its own compact encoding, suited to devices where parsing PEM was an unwelcome cost. That decision left the two implementations mutually unreadable, and this small converter is the bridge. It ships with Dropbear itself, and on space-constrained firmware it is often the first binary left out, which is why key conversion is usually done on a workstation and the result copied across."],["INSTALL","```apt: sudo apt install dropbear-bin```\n\n```apk: sudo apk add dropbear-convert```"],["SEE ALSO","[dropbear](/man/dropbear)(8), [dropbearkey](/man/dropbearkey)(1), [dbclient](/man/dbclient)(1), [ssh-keygen](/man/ssh-keygen)(1)"],["RESOURCES","```[Source code](https://github.com/mkj/dropbear)```\n\n```[Homepage](https://matt.ucc.asn.au/dropbear/dropbear.html)```"]],"e":["dropbear","dropbearkey","dbclient","ssh-keygen"],"x":"dropbearconvert openssh dropbear ~/.ssh/id_rsa output_key"},"ds4-eval":{"n":"ds4-eval","t":"Quality evaluation harness for DwarfStar model outputs","s":[["TAGLINE","Quality evaluation harness for DwarfStar model outputs"],["TLDR","**Run the first N built-in evaluation questions**\n\n```ds4-eval --questions [10] --ctx [100000]```\n\n**Disable thinking and cap generation length**\n\n```ds4-eval --plain --nothink --tokens [512]```\n\n**Write a full evaluation trace**\n\n```ds4-eval --questions [10] --trace [eval.trace]```\n\n**Regrade a previous trace** without reloading the model\n\n```ds4-eval --regrade-trace [eval.trace]```\n\n**CPU diagnostic pass** on a single question\n\n```ds4-eval --cpu --questions [1] --tokens [32]```\n\n**Run selected case numbers** in order\n\n```ds4-eval --case-sequence [1,3,5] --ctx [100000]```"],["SYNOPSIS","**ds4-eval** [_options_]"],["PARAMETERS","**-m**, **--model** _FILE_\n> Path to the GGUF model. Default: **ds4flash.gguf**.\n\n**-c**, **--ctx** _N_\n> Allocated context length in tokens.\n\n**-n**, **--tokens** _N_\n> Maximum generated tokens per question.\n\n**--questions** _N_\n> Run only the first _N_ embedded evaluation questions.\n\n**--case-sequence** _LIST_\n> Comma-separated 1-based case numbers to run in that order.\n\n**--trace** _FILE_\n> Write a detailed evaluation trace.\n\n**--regrade-trace** _FILE_\n> Regrade a prior trace without loading the model again.\n\n**--plain**\n> Disable the split-screen ANSI UI.\n\n**--think** / **--think-max** / **--nothink**\n> Control thinking/reasoning mode for answers.\n\n**--metal** | **--cuda** | **--rocm** | **--cpu**\n> Select the inference backend explicitly.\n\n**--power** _N_\n> GPU duty-cycle target from 1 to 100.\n\n**--pause-ms** _N_\n> Pause after each result in the TTY UI. Default: **350**.\n\n**--self-test-extractors**\n> Run answer-extractor self-tests and exit."],["DESCRIPTION","**ds4-eval** is the quality-evaluation tool for **DwarfStar** (**ds4**). It runs a built-in set of questions against the loaded GGUF, extracts answers, and grades them so contributors can catch regressions when changing kernels, quantizations, or sampling defaults.\n\nUnlike **ds4-bench** (speed), **ds4-eval** focuses on correctness and answer quality. Traces can be saved and regraded later with **--regrade-trace**, which is useful when iterating on grading logic without paying the full inference cost again. A plain mode drops the ANSI UI for log-friendly CI or remote sessions."],["CAVEATS","Requires a DwarfStar-compatible GGUF for meaningful scores. Embedded cases and extractors are project-specific; they are not a general LLM leaderboard. CPU mode is slow and intended for diagnostics. Thinking modes and context size strongly affect both quality and runtime."],["HISTORY","**ds4-eval** ships with **DwarfStar** by **Salvatore Sanfilippo** (**antirez**) and contributors as part of the project's emphasis on official-vector validation and regression testing alongside speed benchmarks."],["SEE ALSO","[ds4-bench](/man/ds4-bench)(1), [ds4-server](/man/ds4-server)(1), [ds4-agent](/man/ds4-agent)(1), [llama-cli](/man/llama-cli)(1)"],["RESOURCES","```[Source code](https://github.com/antirez/ds4)```\n\n```[Documentation](https://github.com/antirez/ds4/blob/main/docs/ENGINE_REFERENCE.md)```"]],"e":["ds4-bench","ds4-server","ds4-agent","llama-cli"],"x":"ds4-eval --questions [10] --ctx [100000]"},"esptool.py":{"n":"esptool.py","t":"ESP8266 and ESP32 series flashing tool","s":[["TAGLINE","ESP8266 and ESP32 series flashing tool"],["TLDR","**Flash firmware to** ESP board\n\n```esptool.py --port [/dev/ttyUSB0] write_flash 0x0 [firmware.bin]```\n\n**Read chip info** and MAC address\n\n```esptool.py --port [/dev/ttyUSB0] chip_id```\n\n**Erase entire** flash\n\n```esptool.py --port [/dev/ttyUSB0] erase_flash```\n\n**Read flash to** file\n\n```esptool.py --port [/dev/ttyUSB0] read_flash 0x0 0x100000 [backup.bin]```\n\n**Identify the SPI** flash chip\n\n```esptool.py --port [/dev/ttyUSB0] flash_id```\n\n**Set baud rate** for faster flashing\n\n```esptool.py --port [/dev/ttyUSB0] --baud [921600] write_flash 0x0 [firmware.bin]```\n\n**Read the MAC** address\n\n```esptool.py --port [/dev/ttyUSB0] read_mac```"],["SYNOPSIS","**esptool.py** [_options_] _command_ [_args_]"],["PARAMETERS","_COMMAND_\n> Operation: write_flash, read_flash, erase_flash, chip_id, flash_id, read_mac, etc.\n\n**--port** _PORT_\n> Serial port device (e.g., /dev/ttyUSB0, COM3).\n\n**--baud** _RATE_\n> Serial baud rate (default: 115200).\n\n**--chip** _CHIP_\n> Target chip type (auto, esp8266, esp32, esp32s2, esp32s3, esp32c3, etc.).\n\n**--before** _ACTION_\n> Action before connecting: default_reset, no_reset, no_reset_no_sync.\n\n**--after** _ACTION_\n> Action after operation: hard_reset (default), soft_reset, no_reset, no_reset_stub.\n\n**--no-stub**\n> Disable stub loader; communicate directly with ROM bootloader.\n\n**write_flash** _ADDR_ _FILE_ [_ADDR_ _FILE_ ...]\n> Write one or more binaries to flash at specified addresses.\n\n**read_flash** _ADDR_ _SIZE_ _FILE_\n> Read flash contents to file.\n\n**erase_flash**\n> Erase entire flash.\n\n**erase_region** _ADDR_ _SIZE_\n> Erase a specific flash region (must be sector-aligned to 4096 bytes).\n\n**chip_id**\n> Read chip ID.\n\n**flash_id**\n> Read SPI flash manufacturer and device ID.\n\n**read_mac**\n> Display the MAC address burned into the chip.\n\n**image_info** _FILE_\n> Display metadata about a binary image file.\n\n**merge_bin** _-o_ _OUTPUT_ _ADDR_ _FILE_ [_ADDR_ _FILE_ ...]\n> Merge multiple binaries into a single flashable file.\n\n**--help**\n> Display help information."],["DESCRIPTION","**esptool.py** is the official flashing tool for Espressif ESP8266, ESP32, ESP32-S2, ESP32-S3, ESP32-C3, and other chips in the ESP series. It communicates over serial to program firmware, read/write flash, and manage the boot process.\n\nThe tool handles bootloader communication, flash layout, and various chip-specific requirements. It supports multiple flash sizes, frequencies, and modes (qio, qout, dio, dout). By default, data is compressed during transfer for better performance.\n\nesptool.py is essential for ESP development, used by Arduino IDE, PlatformIO, and ESP-IDF for firmware deployment."],["CAVEATS","Requires serial driver for USB adapter (e.g., CP210x, CH340). Boot mode may need manual GPIO0 control on some boards. Flash addresses are chip-specific. Higher baud rates increase speed but may reduce reliability. The erase_region address and size must be aligned to 4096-byte sector boundaries."],["HISTORY","esptool.py was developed by **Espressif Systems** as the official tool for their ESP8266 and ESP32 series of WiFi/Bluetooth microcontrollers, widely used in IoT projects. It is open-source and available via PyPI (`pip install esptool`)."],["SEE ALSO","[platformio](/man/platformio)(1), [arduino-cli](/man/arduino-cli)(1)"]],"e":["platformio","arduino-cli"],"x":"esptool.py --port [/dev/ttyUSB0] write_flash 0x0 [firmware.bin]"},"fallocate":{"n":"fallocate","t":"fast file space allocation utility","s":[["TAGLINE","fast file space allocation utility"],["TLDR","**Allocate** 700 MiB file\n\n```fallocate -l 700M path/to/file```\n\n**Shrink** file by 200 MiB\n\n```fallocate -c -l 200M path/to/file```\n\nShrink at **offset**\n\n```fallocate -c -o 100M -l 20M path/to/file```"],["SYNOPSIS","**fallocate** [_OPTIONS_] _filename_"],["DESCRIPTION","**fallocate** preallocates or deallocates space to a file. Unlike writing zeros, fallocate uses filesystem-specific operations to quickly allocate space without initializing the data, making it very fast for creating large files."],["PARAMETERS","**-l, --length** _size_\n> Length of the range to allocate or deallocate\n\n**-o, --offset** _offset_\n> Offset of the range from the beginning of the file\n\n**-c, --collapse-range**\n> Remove a range from the file without leaving a hole\n\n**-d, --dig-holes**\n> Detect and convert zero blocks to holes\n\n**-p, --punch-hole**\n> Deallocate space (create a hole) in the file\n\n**-z, --zero-range**\n> Zero a range but keep space allocated\n\n**-n, --keep-size**\n> Maintain apparent file size when extending allocation"],["CAVEATS","Not all filesystems support all operations. The collapse operation requires filesystem support (ext4 4.2+, XFS 4.15+). Space allocation without zeroing means the file may contain previous disk data."],["HISTORY","**fallocate** is a Linux-specific utility that uses the fallocate() system call introduced in kernel 2.6.23."],["INSTALL","```apt: sudo apt install util-linux```\n\n```dnf: sudo dnf install util-linux```\n\n```pacman: sudo pacman -S util-linux```\n\n```apk: sudo apk add util-linux-misc```\n\n```zypper: sudo zypper install util-linux```\n\n```brew: brew install util-linux```\n\n```nix: nix profile install nixpkgs#util-linux```"],["SEE ALSO","[truncate](/man/truncate)(1), [dd](/man/dd)(1), [fstrim](/man/fstrim)(8)"],["RESOURCES","```[Source code](https://github.com/util-linux/util-linux)```\n\n```[Documentation](https://man7.org/linux/man-pages/man1/fallocate.1.html)```"]],"e":["truncate","dd","fstrim"],"x":"fallocate -l 700M path/to/file"},"flatpak-builder":{"n":"flatpak-builder","t":"build tool for Flatpak applications","s":[["TAGLINE","build tool for Flatpak applications"],["TLDR","**Build** a Flatpak and export to a new repository\n\n```flatpak-builder [path/to/build_directory] [path/to/manifest.json]```\n\nBuild a Flatpak and export to a **specified repository**\n\n```flatpak-builder --repo [repository_name] [path/to/build_directory] [path/to/manifest.json]```\n\nBuild a Flatpak and **install it locally**\n\n```flatpak-builder --install [path/to/build_directory] [path/to/manifest.json]```\n\nBuild and **sign** a Flatpak with GPG\n\n```flatpak-builder --gpg-sign [key_id] --repo [repository_name] [path/to/build_directory] [path/to/manifest.json]```\n\n**Run a shell** inside the application sandbox without installing\n\n```flatpak-builder --run [path/to/build_directory] [path/to/manifest.json] sh```\n\nShow **dependencies** required by the manifest\n\n```flatpak-builder --show-deps [path/to/manifest.json]```\n\nBuild with **ccache** enabled for faster rebuilds\n\n```flatpak-builder --ccache [path/to/build_directory] [path/to/manifest.json]```"],["SYNOPSIS","**flatpak-builder** [_options_] _directory_ _manifest_\n\n**flatpak-builder** **--run** [_options_] _directory_ _manifest_ _command_\n\n**flatpak-builder** **--show-deps** [_options_] _manifest_"],["PARAMETERS","**--repo** _name_\n> Export the build to the specified repository\n\n**--install**\n> Install the built application locally for the current user\n\n**--gpg-sign** _key_id_\n> Sign the commit with the specified GPG key\n\n**--arch** _arch_\n> Specify target architecture\n\n**--ccache**\n> Enable compiler caching for faster rebuilds\n\n**--disable-cache**\n> Disable build cache, forcing full rebuild\n\n**--force-clean**\n> Erase previous build directory contents\n\n**--run** _command_\n> Run a command inside the build sandbox\n\n**--build-shell** _module_\n> Open interactive shell at specified module for debugging\n\n**--stop-at** _module_\n> Stop build after completing specified module\n\n**--show-deps**\n> Display dependencies required by the manifest\n\n**--show-manifest**\n> Display the processed manifest"],["DESCRIPTION","**Flatpak-builder** is a build automation tool that constructs Flatpak applications from manifest files. It wraps the core **flatpak build** command, providing a streamlined workflow for compiling applications and their dependencies within the Flatpak sandbox.\n\nThe tool processes JSON or YAML manifests that define application metadata, runtime requirements, and module build instructions. It follows the standard **./configure && make && make install** pattern automatically and caches intermediate results for efficient incremental builds.\n\nBuild stages include: downloading sources, initializing the application directory, building and installing each module, cleanup, and finalization. Results are cached per-module, allowing quick rebuilds when only specific components change."],["CAVEATS","Requires a valid Flatpak runtime and SDK to be installed. Large applications with many dependencies can take significant time and disk space on first build. The **--run** option provides a sandboxed environment that may not have full system access. Manifests must correctly specify all dependencies or builds will fail."],["HISTORY","Flatpak-builder was developed alongside **Flatpak** by Alexander Larsson at Red Hat, with initial releases around **2015-2016**. It emerged from the xdg-app project and became the standard tool for building Flatpak applications from source, enabling reproducible builds across different Linux distributions."],["INSTALL","```apt: sudo apt install flatpak-builder```\n\n```dnf: sudo dnf install flatpak-builder```\n\n```pacman: sudo pacman -S flatpak-builder```\n\n```apk: sudo apk add flatpak-builder```\n\n```zypper: sudo zypper install flatpak-builder```\n\n```nix: nix profile install nixpkgs#flatpak-builder```"],["SEE ALSO","[flatpak](/man/flatpak)(1), [flatpak-install](/man/flatpak-install)(1)"]],"e":["flatpak","flatpak-install"],"x":"flatpak-builder [path/to/build_directory] [path/to/manifest.json]"},"gcloud-iam":{"n":"gcloud-iam","t":"Identity and Access Management for Google Cloud","s":[["TAGLINE","Identity and Access Management for Google Cloud"],["TLDR","**List service accounts in the current project**\n\n```gcloud iam service-accounts list```\n\n**Create a service account**\n\n```gcloud iam service-accounts create [name] --display-name=\"[description]\"```\n\n**Create a key for a service account**\n\n```gcloud iam service-accounts keys create [key.json] --iam-account=[email]```\n\n**List keys for a service account**\n\n```gcloud iam service-accounts keys list --iam-account=[email]```\n\n**List all predefined roles**\n\n```gcloud iam roles list```\n\n**Describe a specific role and its permissions**\n\n```gcloud iam roles describe [roles/editor]```\n\n**List grantable roles for a resource**\n\n```gcloud iam list-grantable-roles [//cloudresourcemanager.googleapis.com/projects/my-project]```"],["SYNOPSIS","**gcloud iam** _group_ _command_ [_options_]"],["PARAMETERS","**service-accounts** _CMD_\n> Manage service accounts (create, delete, describe, list, update, add/get/set-iam-policy-binding, keys).\n\n**roles** _CMD_\n> Manage IAM roles (create, delete, describe, list, update, copy, undelete).\n\n**policies** _CMD_\n> Manage IAM deny policies.\n\n**workload-identity-pools** _CMD_\n> Manage workload identity pools for external identity federation.\n\n**list-grantable-roles** _RESOURCE_\n> List grantable roles for a given resource.\n\n**--iam-account** _EMAIL_\n> Service account email (used with service-accounts keys subcommands).\n\n**--help**\n> Display help information."],["DESCRIPTION","**gcloud iam** provides command-line access to Google Cloud's Identity and Access Management system, which controls who (identity) has what access (role) to which resources.\n\nService accounts represent non-human identities for applications and services. Keys can be created for service accounts to enable authentication from external environments. Roles bundle related permissions into assignable units. IAM policies bind roles to principals (users, groups, or service accounts) on specific resources.\n\nNote that adding role bindings to projects is done via **gcloud projects add-iam-policy-binding**, not through the **gcloud iam** command group directly."],["CAVEATS","IAM changes may take time to propagate. Service account keys are security-sensitive credentials and should be rotated regularly. Role bindings are additive; removing access requires explicit policy modification."],["HISTORY","gcloud iam is part of the **Google Cloud SDK** for managing IAM, Google Cloud's identity and access control system."],["SEE ALSO","[gcloud](/man/gcloud)(1), [gcloud-auth](/man/gcloud-auth)(1), [gcloud-config](/man/gcloud-config)(1), [gcloud-projects](/man/gcloud-projects)(1)"]],"e":["gcloud","gcloud-auth","gcloud-config","gcloud-projects"],"x":"gcloud iam service-accounts list"},"go":{"n":"go","t":"Go programming language toolchain","s":[["TAGLINE","Go programming language toolchain"],["TLDR","**Run a program**\n\n```go run [main.go]```\n\n**Build an executable**\n\n```go build```\n\n**Run tests**\n\n```go test```\n\n**Download dependencies**\n\n```go mod download```\n\n**Format code**\n\n```go fmt ./...```"],["SYNOPSIS","**go** _command_ [_arguments_]"],["PARAMETERS","**build**\n> Compile packages and dependencies.\n\n**run**\n> Compile and run program.\n\n**test**\n> Run tests.\n\n**mod**\n> Module maintenance.\n\n**get**\n> Download and install packages.\n\n**fmt**\n> Format source code.\n\n**vet**\n> Report likely mistakes.\n\n**doc**\n> Show documentation.\n\n**install**\n> Compile and install packages.\n\n**generate**\n> Run code generators.\n\n**clean**\n> Remove build cache and binaries.\n\n**env**\n> Print Go environment variables.\n\n**--help**\n> Display help information."],["DESCRIPTION","**go** is the tool for managing Go source code. It provides commands for building, testing, running, and managing Go programs and modules.\n\nThe tool handles compilation, dependency management, testing, and code quality. Go modules provide versioned dependency management. The toolchain is self-contained and produces statically linked binaries by default."],["CONFIGURATION","**go.mod**\n> Module definition file specifying module path, Go version, and dependencies.\n\n**go.sum**\n> Cryptographic checksums of module dependencies for verification.\n\n**$GOPATH**\n> Workspace directory for Go source and binaries (default: ~/go)."],["CAVEATS","Requires GOPATH or modules. Cross-compilation built-in. CGO may need additional setup."],["HISTORY","Go was created at **Google** by **Robert Griesemer, Rob Pike, and Ken Thompson** in **2007**, released publicly in **2009**."],["INSTALL","```apt: sudo apt install gccgo-go```\n\n```dnf: sudo dnf install gcc-go```\n\n```pacman: sudo pacman -S gcc-go```\n\n```apk: sudo apk add gcc-go```\n\n```zypper: sudo zypper install gcc-go```\n\n```brew: brew install go```\n\n```nix: nix profile install nixpkgs#go```"],["SEE ALSO","[go-build](/man/go-build)(1), [go-run](/man/go-run)(1), [go-mod](/man/go-mod)(1)"]],"e":["go-build","go-run","go-mod"],"x":"go run [main.go]"},"gpustat":{"n":"gpustat","t":"Concise NVIDIA GPU status monitor","s":[["TAGLINE","Concise NVIDIA GPU status monitor"],["TLDR","**Show GPU status**\n\n```gpustat```\n\n**Show with process info**\n\n```gpustat -p```\n\n**Continuous monitoring**\n\n```gpustat -i [1]```\n\n**Show full command names**\n\n```gpustat -cp```\n\n**JSON output**\n\n```gpustat --json```\n\n**Show process usernames**\n\n```gpustat -u```"],["SYNOPSIS","**gpustat** [_options_]"],["PARAMETERS","**-p**, **--show-pid**\n> Show process PIDs.\n\n**-u**, **--show-user**\n> Show process usernames.\n\n**-c**, **--show-cmd**\n> Show process commands.\n\n**-f**, **--show-full-cmd**\n> Show full command line and CPU stats of the running process.\n\n**-F**, **--show-fan-speed**\n> Show GPU fan speed.\n\n**-P**, **--show-power**\n> Show GPU power usage/draw and limit.\n\n**-e**, **--show-codec**\n> Show encoder/decoder utilization.\n\n**-i**, **--interval**, **--watch** _seconds_\n> Run in watch mode, refreshing every _seconds_.\n\n**--json**\n> Output in JSON format.\n\n**--no-color**\n> Disable color output.\n\n**--no-header**\n> Hide header.\n\n**--no-processes**\n> Omit running-process information (memory, user, etc.).\n\n**-a**, **--show-all**\n> Show all information.\n\n**--id** _id_\n> Show specific GPU only.\n\n**-v**, **--version**\n> Display version information."],["DESCRIPTION","**gpustat** displays GPU status in a concise format, similar to nvidia-smi but more readable. It shows GPU utilization, memory usage, temperature, and running processes at a glance.\n\nThe tool is designed for quick monitoring of NVIDIA GPUs during machine learning training or other GPU-intensive tasks."],["CAVEATS","Requires NVIDIA GPU and drivers. Uses nvidia-smi internally. Not suitable for non-NVIDIA GPUs. Process info needs appropriate permissions."],["HISTORY","**gpustat** was created by **Jongwook Choi** as a simple GPU monitoring tool for machine learning practitioners. It provides a cleaner alternative to nvidia-smi's verbose output, focusing on essential metrics."],["INSTALL","```nix: nix profile install nixpkgs#gpustat```"],["SEE ALSO","[nvidia-smi](/man/nvidia-smi)(1), [nvtop](/man/nvtop)(1), [htop](/man/htop)(1), [watch](/man/watch)(1)"],["RESOURCES","```[Source code](https://github.com/wookayin/gpustat)```"]],"e":["nvidia-smi","nvtop","htop","watch"],"x":"gpustat"},"gst-launch-1.0":{"n":"gst-launch-1.0","t":"build and run GStreamer multimedia pipelines","s":[["TAGLINE","build and run GStreamer multimedia pipelines"],["TLDR","**Play a video file**\n\n```gst-launch-1.0 filesrc location=[video.mp4] ! decodebin ! autovideosink```\n\n**Display test video pattern**\n\n```gst-launch-1.0 videotestsrc ! autovideosink```\n\n**Play audio file**\n\n```gst-launch-1.0 filesrc location=[audio.mp3] ! decodebin ! autoaudiosink```\n\n**Display webcam**\n\n```gst-launch-1.0 v4l2src ! autovideosink```\n\n**Stream from RTSP**\n\n```gst-launch-1.0 rtspsrc location=[rtsp://url] ! decodebin ! autovideosink```\n\n**Verbose output**\n\n```gst-launch-1.0 -v videotestsrc ! autovideosink```\n\n**Send EOS on interrupt**\n\n```gst-launch-1.0 -e filesrc location=[video.mp4] ! decodebin ! autovideosink```"],["SYNOPSIS","**gst-launch-1.0** [_options_] _pipeline-description_"],["PARAMETERS","**-v**, **--verbose**\n> Verbose output, prints properties and tags as the pipeline runs.\n\n**-q**, **--quiet**\n> Suppress status output (position, progress).\n\n**-e**, **--eos-on-shutdown**\n> Send EOS on interrupt for clean shutdown, instead of stopping abruptly.\n\n**-m**, **--messages**\n> Output messages posted on the pipeline's bus.\n\n**-t**, **--tags**\n> Output tags (metadata) found in the stream.\n\n**-f**, **--no-fault**\n> Do not install a fault handler (segfault backtrace helper).\n\n**--gst-debug=**_STRING_\n> Set debug levels for specific categories (e.g., **GST_SCHEDULING:5**).\n\n**--gst-debug-level=**_N_\n> Set global debug level (0-9).\n\n**--gst-plugin-path=**_PATH_\n> Add extra directories to scan for plugins."],["DESCRIPTION","**gst-launch-1.0** builds and runs GStreamer multimedia pipelines from the command line. Elements are linked with **!** (exclamation mark), and properties are set with **name=value** syntax.\n\nCaps filters constrain formats between elements (e.g., **video/x-raw,width=640,height=480**). Use **decodebin** or **playbin** for automatic codec selection."],["CAVEATS","Intended for testing and prototyping pipelines, not as a robust playback application. Press Ctrl+C to stop; without **-e** the pipeline stops immediately rather than draining buffered data."],["INSTALL","```apt: sudo apt install gstreamer1.0-tools```\n\n```apk: sudo apk add gstreamer-tools```"],["SEE ALSO","[gst-inspect-1.0](/man/gst-inspect-1.0)(1), [gst-discoverer-1.0](/man/gst-discoverer-1.0)(1), [ffmpeg](/man/ffmpeg)(1)"],["RESOURCES","```[Homepage](https://gstreamer.freedesktop.org/)```\n\n```[Source code](https://gitlab.freedesktop.org/gstreamer/gstreamer)```\n\n```[Documentation](https://gstreamer.freedesktop.org/documentation/tools/gst-launch.html)```"]],"e":["gst-inspect-1.0","gst-discoverer-1.0","ffmpeg"],"x":"gst-launch-1.0 filesrc location=[video.mp4] ! decodebin ! autovideosink"},"haiti":{"n":"haiti","t":"Hash type identifier for the command line","s":[["TAGLINE","Hash type identifier for the command line"],["TLDR","**Identify the type** of a hash\n\n```haiti [hash_string]```\n\n**Identify hash type** with extended algorithms including salted variants\n\n```haiti -e [hash_string]```\n\n**Identify hash from stdin**\n\n```echo \"[hash_string]\" | haiti -```\n\n**Show short output** without hashcat/john references\n\n```haiti --short [hash_string]```\n\n**Disable colorized output**\n\n```haiti --no-color [hash_string]```"],["SYNOPSIS","**haiti** [**-e**|**--extended**] [**--short**] [**--no-color**] _hash_"],["PARAMETERS","**-e**, **--extended**\n> List all possible hash algorithms including ones using salt\n\n**--short**\n> Display short format without hashcat and John the Ripper references\n\n**--no-color**\n> Disable colorized output\n\n**--hashcat-only**\n> Show only hashcat references\n\n**--john-only**\n> Show only John the Ripper references\n\n**-h**, **--help**\n> Print help\n\n**-V**, **--version**\n> Print version"],["DESCRIPTION","**HAITI** (HAsh IdenTifIer) is a command-line tool and library for identifying the type of a given hash. It detects over **675 hash types** including modern algorithms like SHA3, Keccak, Blake2, and many application-specific formats. For each identified hash type, it provides the corresponding **Hashcat** mode number and **John the Ripper** format name, making it easy to proceed with hash cracking.\n\nHAITI analyzes the hash length, character set, and format to narrow down possible algorithms, presenting results ranked by likelihood."],["CAVEATS","Hash identification is probabilistic — many hash types produce output of identical length and character set, so HAITI typically returns multiple possible matches. It cannot definitively determine the exact algorithm without additional context. Requires Ruby runtime for installation."],["HISTORY","**HAITI** was created by **Alexandre ZANNI** (noraj) and is written in **Ruby**. It was developed as a modern replacement for older hash identification tools, with a focus on supporting the latest algorithms and providing direct references to cracking tools. It is available in **Kali Linux** and other security-focused distributions."],["INSTALL","```brew: brew install haiti```\n\n```nix: nix profile install nixpkgs#haiti```"],["SEE ALSO","[hashcat](/man/hashcat)(1), [john](/man/john)(1)"]],"e":["hashcat","john"],"x":"haiti [hash_string]"},"hub-delete":{"n":"hub-delete","t":"deletes a GitHub repository","s":[["TAGLINE","deletes a GitHub repository"],["TLDR","**Delete a repository under your user**\n\n```hub delete [name]```\n\n**Delete an organization repository**\n\n```hub delete [organization]/[name]```\n\n**Skip the confirmation prompt**\n\n```hub delete -y [user]/[name]```"],["SYNOPSIS","**hub delete** [**-y**] [_organization_/]_name_"],["PARAMETERS","[_ORGANIZATION_/]_NAME_\n> The name of the repository on GitHub, optionally prefixed by an organization.\n\n**-y**, **--yes**\n> Skip the confirmation prompt and immediately delete the repository.\n\n**--help**\n> Display help information."],["DESCRIPTION","**hub delete** deletes a GitHub repository. The repository name is required and may be prefixed with an organization for repositories not owned by the current user.\n\nThe command requires interactive confirmation unless **-y** is provided. It permanently removes the repository including all issues, pull requests, and wiki data."],["CAVEATS","Destructive and irreversible. Requires authentication and the **delete_repo** scope on the GitHub token. Owner or admin permissions on the target repository are required."],["HISTORY","hub delete is part of **hub**, GitHub's original CLI for repository management."],["INSTALL","```apt: sudo apt install hub```\n\n```pacman: sudo pacman -S hub```\n\n```zypper: sudo zypper install hub```\n\n```brew: brew install hub```\n\n```nix: nix profile install nixpkgs#hub```"],["SEE ALSO","[hub](/man/hub)(1), [gh](/man/gh)(1), [hub-create](/man/hub-create)(1)"]],"e":["hub","gh","hub-create"],"x":"hub delete [name]"},"hyprshot":{"n":"hyprshot","t":"screenshot utility designed specifically for the Hyprland Wayland compositor","s":[["TAGLINE","screenshot utility designed specifically for the Hyprland Wayland compositor"],["TLDR","Select and screenshot a **region**\n\n```hyprshot -m region```\n\nSelect and screenshot a **specific window**\n\n```hyprshot -m window```\n\nSelect and screenshot a **specific output**\n\n```hyprshot -m output```\n\nScreenshot the **active window**\n\n```hyprshot -m active -m window```\n\n**Freeze** the screen and screenshot selected region\n\n```hyprshot -z -m region```\n\nScreenshot to a specific **output directory**\n\n```hyprshot -o [path/to/directory] -m window```\n\nScreenshot to **clipboard only**\n\n```hyprshot --clipboard -m output```"],["SYNOPSIS","**hyprshot** [_options_] **-m** _mode_"],["PARAMETERS","**-m**, **--mode** _MODE_\n> Screenshot mode: region, window, output, active\n\n**-o**, **--output-folder** _DIR_\n> Save screenshots to specified directory\n\n**-f**, **--filename** _NAME_\n> Set output filename\n\n**--clipboard**\n> Copy to clipboard only (don't save to file)\n\n**-z**, **--freeze**\n> Freeze screen while selecting\n\n**-s**, **--silent**\n> Don't send notification after screenshot\n\n**-r**, **--raw**\n> Output raw image data to stdout\n\n**-d**, **--delay** _SECONDS_\n> Wait before taking screenshot"],["DESCRIPTION","**hyprshot** is a screenshot utility designed specifically for the Hyprland Wayland compositor. It provides interactive selection for regions, windows, and outputs with clipboard integration.\n\nModes:\n- **region** - Select a rectangular area\n- **window** - Select a specific window\n- **output** - Select a monitor/output\n- **active** - Combined with window/output for current focus\n\nScreenshots are saved to **~/Pictures/Screenshots** by default. The tool uses slurp for region selection and grim for capture."],["CAVEATS","Requires Hyprland, grim, slurp, and wl-clipboard. The freeze option may not work on all systems. Window selection depends on Hyprland's client list."],["HISTORY","hyprshot was created by Gustash as a convenient wrapper around screenshot tools for Hyprland users. It provides a streamlined interface compared to manually combining grim and slurp."],["INSTALL","```pacman: sudo pacman -S hyprshot```\n\n```zypper: sudo zypper install hyprshot```\n\n```nix: nix profile install nixpkgs#hyprshot```"],["SEE ALSO","[hyprctl](/man/hyprctl)(1), [grim](/man/grim)(1), [slurp](/man/slurp)(1), [wl-copy](/man/wl-copy)(1)"]],"e":["hyprctl","grim","slurp","wl-copy"],"x":"hyprshot -m region"},"ignite":{"n":"ignite","t":"CLI for React Native project scaffolding and code generation","s":[["TAGLINE","CLI for React Native project scaffolding and code generation"],["TLDR","**Create a new React Native app**\n\n```npx ignite-cli new [MyApp]```\n\n**Create a new app** with Expo workflow\n\n```npx ignite-cli new [MyApp] --workflow=expo```\n\n**Generate a component**\n\n```npx ignite-cli generate component [Header]```\n\n**Generate a screen**\n\n```npx ignite-cli generate screen [Login]```\n\n**Generate a model**\n\n```npx ignite-cli generate model [User]```\n\n**Check environment** for issues\n\n```npx ignite-cli doctor```\n\n**Remove the demo code** from a generated project\n\n```npx ignite-cli remove-demo```"],["SYNOPSIS","**ignite** _command_ [_options_]"],["PARAMETERS","**new** _name_\n> Scaffold a new React Native app with the Ignite boilerplate.\n\n**generate** _type_ _name_\n> Generate a component, screen, model, or navigator from templates.\n\n**doctor**\n> Check the local development environment for common issues.\n\n**remove-demo**\n> Remove the demo/example code from a project generated with Ignite.\n\n**--workflow** _type_\n> Workflow to use when creating a new app: **expo** (default) or **cng** (Continuous Native Generation).\n\n**--yes**, **-y**\n> Accept all default options when creating a new app (non-interactive).\n\n**--help**\n> Display help information.\n\n**--version**\n> Display the CLI version."],["DESCRIPTION","**Ignite** is a CLI for React Native development created by **Infinite Red**. It provides an opinionated project scaffolding with industry best practices, including MobX-State-Tree for state management, TypeScript, React Navigation, and a testing setup with Jest.\n\nThe **generate** command creates boilerplate files from templates for common patterns: components, screens, models, and navigators. Generated code follows the project's established conventions and directory structure."],["CAVEATS","React Native and Node.js are required. The generated boilerplate is opinionated and follows Infinite Red's recommended architecture. Ignite v9+ defaults to Expo-based workflows."],["HISTORY","Ignite was created by **Infinite Red** as a React Native boilerplate and CLI tool. It has evolved through multiple major versions, shifting from multiple boilerplate options to a single, maintained boilerplate (Ignite Bowser, then Ignite v9+) that focuses on Expo."],["INSTALL","```brew: brew install ignite```"],["SEE ALSO","[npx](/man/npx)(1), [react-native](/man/react-native)(1), [expo](/man/expo)(1), [npm](/man/npm)(1)"]],"e":["npx","react-native","expo","npm"],"x":"npx ignite-cli new [MyApp]"},"immich-cli":{"n":"immich-cli","t":"command-line interface for Immich photo server","s":[["TAGLINE","command-line interface for Immich photo server"],["TLDR","**Upload photos**\n\n```immich upload [/path/to/photos]```\n\n**Upload with album**\n\n```immich upload --album [AlbumName] [/path/to/photos]```\n\n**Login to server**\n\n```immich login [https://server] [api-key]```\n\n**Check server info**\n\n```immich server-info```\n\n**Dry run upload**\n\n```immich upload --dry-run [/path/to/photos]```"],["SYNOPSIS","**immich** _command_ [_options_]"],["PARAMETERS","_COMMAND_\n> Subcommand to run.\n\n**upload**\n> Upload files.\n\n**login**\n> Authenticate with server.\n\n**server-info**\n> Show server information.\n\n**--album** _NAME_\n> Target album.\n\n**--dry-run**\n> Simulate upload.\n\n**--help**\n> Display help information."],["DESCRIPTION","**immich-cli** is the command-line interface for Immich photo server. It enables bulk uploads and server management.\n\nThe tool authenticates with API keys and uploads photos/videos. It supports album assignment and duplicate detection."],["CAVEATS","Requires Immich server. API key authentication. Node.js based."],["HISTORY","immich-cli is the official CLI for **Immich** photo management server."],["INSTALL","```brew: brew install immich-cli```\n\n```nix: nix profile install nixpkgs#immich-cli```"],["SEE ALSO","[immich](/man/immich)(1), [immich-go](/man/immich-go)(1), [rclone](/man/rclone)(1)"]],"e":["immich","immich-go","rclone"],"x":"immich upload [/path/to/photos]"},"impacket-sambapipe":{"n":"impacket-sambapipe","t":"connects to named pipes on remote Windows systems via SMB","s":[["TAGLINE","connects to named pipes on remote Windows systems via SMB"],["TLDR","**Access named pipe** on a remote SMB share\n\n```impacket-sambapipe '[domain]/[user]:[password]@[192.168.1.100]' '[pipename]'```\n\n**Connect to pipe using NTLM hash**\n\n```impacket-sambapipe -hashes ':[nthash]' '[domain]/[user]@[192.168.1.100]' '[pipename]'```\n\n**Access pipe via Kerberos authentication**\n\n```impacket-sambapipe -k -no-pass '[domain]/[user]@[target]' '[pipename]'```"],["SYNOPSIS","**impacket-sambapipe** [_-h_] [_-hashes LMHASH:NTHASH_] [_-no-pass_] [_-k_] [_-aesKey KEY_] [_-dc-ip IP_] _target_ _pipe_"],["PARAMETERS","**-hashes** _LMHASH:NTHASH_\n> Use NTLM hashes for authentication instead of password\n\n**-no-pass**\n> Don't ask for password (useful with -k)\n\n**-k**\n> Use Kerberos authentication from ccache file\n\n**-aesKey** _KEY_\n> AES key to use for Kerberos authentication\n\n**-dc-ip** _IP_\n> IP address of the domain controller (for Kerberos)"],["DESCRIPTION","**impacket-sambapipe** connects to named pipes on remote Windows systems via SMB. Named pipes are used for inter-process communication in Windows and many services expose functionality through pipes.\n\nThis tool is useful for interacting with services that communicate through named pipes, testing pipe accessibility, or as part of more complex attack chains involving pipe communication."],["CAVEATS","Requires appropriate permissions to access the named pipe. Some pipes require administrative access. The pipe must exist and be accessible on the target system."],["HISTORY","Part of the **Impacket** library by SecureAuth. Named pipe access is fundamental to many Windows protocols and services, making this tool useful for various testing scenarios."],["INSTALL","```pacman: sudo pacman -S impacket```"],["SEE ALSO","[impacket-smbclient](/man/impacket-smbclient)(1), [smbclient](/man/smbclient)(1), [rpcclient](/man/rpcclient)(1)"]],"e":["impacket-smbclient","smbclient","rpcclient"],"x":"impacket-sambapipe '[domain]/[user]:[password]@[192.168.1.100]' '[pipename]'"},"jira-cli":{"n":"jira-cli","t":"Feature-rich interactive Jira command line tool","s":[["TAGLINE","Feature-rich interactive Jira command line tool"],["TLDR","**Initialize configuration**\n\n```jira init```\n\n**Create an issue**\n\n```jira issue create```\n\n**List issues** assigned to you\n\n```jira issue list -a$(jira me)```\n\n**View an issue**\n\n```jira issue view [ISSUE-123]```\n\n**Move an issue** to a different status\n\n```jira issue move [ISSUE-123] \"[In Progress]\"```\n\n**Open an issue** in the browser\n\n```jira open [ISSUE-123]```\n\n**List sprints**\n\n```jira sprint list```\n\n**Add a comment**\n\n```jira issue comment add [ISSUE-123]```"],["SYNOPSIS","**jira** _command_ [_options_] [_arguments_]"],["DESCRIPTION","**jira-cli** is an interactive command-line tool for **Atlassian Jira** that helps you manage issues, sprints, and projects without leaving the terminal. It supports issue creation, cloning, linking, transitions, comments, sprint management, and an interactive issue search with TUI selection.\n\nThe tool uses Jira's REST API and supports both Jira Cloud and Jira Server. Configuration is stored locally with API token authentication."],["PARAMETERS","**init**\n> Configure Jira server and authentication\n\n**issue create**\n> Create a new issue interactively\n\n**issue list**\n> List issues with filters\n\n**issue view** _KEY_\n> View issue details\n\n**issue move** _KEY_ _STATUS_\n> Transition an issue\n\n**issue comment add** _KEY_\n> Add a comment\n\n**sprint list**\n> List sprints\n\n**open** _KEY_\n> Open issue in browser\n\n**me**\n> Print current user"],["CAVEATS","Requires a Jira API token for authentication. Some features may not be available on older Jira Server versions. Rate limiting may apply for heavy usage."],["HISTORY","**jira-cli** was created by **Ankit Pokhrel** and is written in **Go**. It was designed as the missing command-line tool for Atlassian Jira, providing a fast and interactive terminal workflow."],["INSTALL","```zypper: sudo zypper install jira-cli```\n\n```brew: brew install jira-cli```"],["SEE ALSO","[gh](/man/gh)(1)"]],"e":["gh"],"x":"jira init"},"kahlan":{"n":"kahlan","t":"BDD testing framework for PHP","s":[["TAGLINE","BDD testing framework for PHP"],["TLDR","**Run tests**\n\n```kahlan```\n\n**Run specific spec**\n\n```kahlan --spec=[spec/MySpec.php]```\n\n**Filter by pattern**\n\n```kahlan --pattern=\"[*Spec.php]\"```\n\n**Generate coverage**\n\n```kahlan --coverage=[4]```\n\n**Output as reporter**\n\n```kahlan --reporter=[dot|bar|verbose]```\n\n**Run with config**\n\n```kahlan --config=[kahlan-config.php]```"],["SYNOPSIS","**kahlan** [_options_]"],["PARAMETERS","**--spec** _PATH_\n> Specific spec file/directory.\n\n**--pattern** _GLOB_\n> File pattern.\n\n**--coverage** _LEVEL_\n> Coverage level (1-4).\n\n**--reporter** _NAME_\n> Output reporter.\n\n**--config** _FILE_\n> Configuration file.\n\n**--help**\n> Display help information."],["DESCRIPTION","**Kahlan** is a BDD testing framework for PHP. It uses a describe-it syntax similar to Jasmine/RSpec.\n\nThe tool supports mocking, stubbing, and code coverage. It focuses on behavior-driven development."],["CAVEATS","PHP-based. Different syntax from PHPUnit. Requires Composer."],["HISTORY","Kahlan was created as a PHP testing framework with JavaScript-like BDD syntax and powerful mocking."],["SEE ALSO","[phpunit](/man/phpunit)(1), [phpspec](/man/phpspec)(1), [pest](/man/pest)(1)"]],"e":["phpunit","phpspec","pest"],"x":"kahlan"},"mpd":{"n":"mpd","t":"server-side music player","s":[["TAGLINE","server-side music player"],["TLDR","**Start MPD**\n\n```mpd```\n\n**Start with config file**\n\n```mpd [~/.config/mpd/mpd.conf]```\n\n**Kill running MPD**\n\n```mpd --kill```\n\n**Run in foreground**\n\n```mpd --no-daemon```\n\n**Show version**\n\n```mpd --version```\n\n**Verbose output**\n\n```mpd --verbose```"],["SYNOPSIS","**mpd** [_options_] [_config_file_]"],["PARAMETERS","_CONFIG_FILE_\n> Path to configuration file.\n\n**--kill**\n> Kill running MPD instance.\n\n**--no-daemon**\n> Run in foreground.\n\n**--verbose**\n> Enable verbose output.\n\n**--version**\n> Show version information.\n\n**--help**\n> Display help information."],["DESCRIPTION","**mpd** (Music Player Daemon) is a server-side music player. It plays audio and manages playlists.\n\nThe tool runs as a daemon. Clients like mpc or ncmpcpp connect to control playback."],["CAVEATS","Server-side only. Requires client to control. Configuration needed for audio output."],["HISTORY","MPD was created as a **flexible music server** allowing multiple client interfaces and remote control."],["INSTALL","```apt: sudo apt install mpd```\n\n```dnf: sudo dnf install mpd```\n\n```pacman: sudo pacman -S mpd```\n\n```apk: sudo apk add mpd```\n\n```zypper: sudo zypper install mpd```\n\n```brew: brew install mpd```\n\n```nix: nix profile install nixpkgs#mpd```"],["SEE ALSO","[mpc](/man/mpc)(1), [ncmpcpp](/man/ncmpcpp)(1), [pulseaudio](/man/pulseaudio)(1)"]],"e":["mpc","ncmpcpp","pulseaudio"],"x":"mpd"},"mpijavac":{"n":"mpijavac","t":"compiles MPI Java programs","s":[["TAGLINE","compiles MPI Java programs"],["TLDR","**Compile MPI Java program**\n\n```mpijavac [MPIProgram.java]```\n\n**Compile to specific directory**\n\n```mpijavac -d [classes/] [MPIProgram.java]```\n\n**Compile with classpath**\n\n```mpijavac -cp [lib/*] [MPIProgram.java]```"],["SYNOPSIS","**mpijavac** [_options_] _source_files_..."],["PARAMETERS","**-d** _dir_\n> Output directory for class files.\n\n**-cp** _path_\n> Classpath for compilation.\n\n**-sourcepath** _path_\n> Source file path.\n\n**--showme**\n> Show underlying javac command."],["DESCRIPTION","**mpijavac** compiles MPI Java programs. Wrapper around javac with MPI Java bindings. Used with OpenMPI's Java interface for parallel computing in Java."],["INSTALL","```apt: sudo apt install openmpi-bin```"],["SEE ALSO","[mpirun](/man/mpirun)(1), [javac](/man/javac)(1), [mpicc](/man/mpicc)(1)"]],"e":["mpirun","javac","mpicc"],"x":"mpijavac [MPIProgram.java]"},"mvn-package":{"n":"mvn-package","t":"builds the project and creates a distributable package","s":[["TAGLINE","builds the project and creates a distributable package"],["TLDR","**Build package**\n\n```mvn package```\n\n**Build skipping tests**\n\n```mvn package -DskipTests```\n\n**Build with profile**\n\n```mvn package -P [production]```\n\n**Build clean package**\n\n```mvn clean package```"],["SYNOPSIS","**mvn package** [_options_]"],["PARAMETERS","**-DskipTests**\n> Skip test execution.\n\n**-Dmaven.test.skip**\n> Skip test compilation and execution.\n\n**-P** _profile_\n> Activate profile.\n\n**-o**\n> Work offline.\n\n**-T** _threads_\n> Parallel builds."],["DESCRIPTION","**mvn package** builds the project and creates a distributable package (JAR, WAR, etc.). Executes compile, test, and package phases. Output goes to target/ directory."],["INSTALL","```dnf: sudo dnf install maven```\n\n```pacman: sudo pacman -S maven```\n\n```apk: sudo apk add maven```\n\n```zypper: sudo zypper install maven```\n\n```brew: brew install maven```\n\n```nix: nix profile install nixpkgs#maven```"],["SEE ALSO","[mvn](/man/mvn)(1), [mvn-install](/man/mvn-install)(1), [mvn-compile](/man/mvn-compile)(1)"]],"e":["mvn","mvn-install","mvn-compile"],"x":"mvn package"},"naabu":{"n":"naabu","t":"fast port scanner written in Go","s":[["TAGLINE","fast port scanner written in Go"],["TLDR","**Scan target for open ports**\n\n```naabu -host [example.com]```\n\n**Scan specific ports**\n\n```naabu -host [example.com] -p [80,443,8080]```\n\n**Scan port range**\n\n```naabu -host [example.com] -p [1-1000]```\n\n**Scan top ports**\n\n```naabu -host [example.com] -top-ports [100]```\n\n**Scan from file**\n\n```naabu -list [targets.txt]```\n\n**Output to file**\n\n```naabu -host [example.com] -o [results.txt]```\n\n**Scan with rate limit**\n\n```naabu -host [example.com] -rate [1000]```\n\n**JSON output**\n\n```naabu -host [example.com] -json```"],["SYNOPSIS","**naabu** [_-host target_] [_-p ports_] [_-top-ports n_] [_-o file_] [_options_]"],["PARAMETERS","**-host** _TARGET_\n> Target to scan.\n\n**-list** _FILE_\n> File with targets.\n\n**-p** _PORTS_\n> Ports to scan.\n\n**-top-ports** _N_\n> Scan top N ports.\n\n**-exclude-ports** _PORTS_\n> Ports to exclude.\n\n**-o** _FILE_\n> Output file.\n\n**-json**\n> JSON output.\n\n**-rate** _N_\n> Packets per second.\n\n**-c** _N_\n> Concurrent hosts.\n\n**-timeout** _MS_\n> Timeout in milliseconds.\n\n**-retries** _N_\n> Retry count.\n\n**-verify**\n> Verify open ports.\n\n**-nmap**\n> Run nmap on discovered ports.\n\n**-silent**\n> Silent mode.\n\n**-v**\n> Verbose output."],["DESCRIPTION","**naabu** is a fast port scanner written in Go. It uses SYN scanning for speed while maintaining accuracy through verification.\n\nThe scanner optimizes for speed with configurable rate limiting. It can scan thousands of hosts quickly while respecting network constraints.\n\nTop ports mode scans the most commonly open ports, focusing resources on likely targets. Custom port lists target specific services.\n\nNmap integration hands off discovered ports for service detection. This combines naabu's speed with nmap's detailed fingerprinting.\n\nOutput formats include plain text, JSON, and nmap-compatible results. Integration with other security tools is straightforward.\n\nThe tool is designed for security professionals performing authorized assessments."],["CAVEATS","SYN scanning requires root/admin privileges. Aggressive scanning may trigger IDS/IPS. Only use on authorized targets. Rate limiting important on production networks."],["HISTORY","**naabu** was developed by **ProjectDiscovery** as part of their security tooling suite. It complements their other tools like nuclei, httpx, and subfinder for comprehensive security testing workflows."],["INSTALL","```brew: brew install naabu```\n\n```nix: nix profile install nixpkgs#naabu```"],["SEE ALSO","[nmap](/man/nmap)(1), [masscan](/man/masscan)(1), [rustscan](/man/rustscan)(1), [zmap](/man/zmap)(1)"]],"e":["nmap","masscan","rustscan","zmap"],"x":"naabu -host [example.com]"},"nix-flake":{"n":"nix-flake","t":"manages Nix flakes","s":[["TAGLINE","manages Nix flakes"],["TLDR","**Show flake info**\n\n```nix flake show```\n\n**Update flake inputs**\n\n```nix flake update```\n\n**Lock flake inputs**\n\n```nix flake lock```\n\n**Initialize new flake**\n\n```nix flake init```\n\n**Check flake**\n\n```nix flake check```\n\n**Show flake metadata**\n\n```nix flake metadata```\n\n**Clone flake**\n\n```nix flake clone [github:owner/repo]```"],["SYNOPSIS","**nix** **flake** [_command_] [_options_]"],["PARAMETERS","_COMMAND_\n> Flake subcommand.\n\n**show**\n> Show flake outputs.\n\n**update**\n> Update inputs.\n\n**lock**\n> Lock inputs.\n\n**init**\n> Initialize flake.\n\n**check**\n> Validate flake.\n\n**--help**\n> Display help information."],["DESCRIPTION","**nix flake** manages Nix flakes. Flakes provide reproducible, hermetic packages.\n\nThe tool handles flake.nix files. Manages inputs and outputs."],["CAVEATS","Requires experimental features. flake.nix required. Lock file generated."],["HISTORY","Nix flakes were introduced in **Nix 2.4** to provide reproducible and composable package definitions."],["INSTALL","```apt: sudo apt install nix-bin```\n\n```dnf: sudo dnf install nix```\n\n```pacman: sudo pacman -S nix```\n\n```apk: sudo apk add nix```\n\n```zypper: sudo zypper install nix```\n\n```nix: nix profile install nixpkgs#nix```"],["SEE ALSO","[nix](/man/nix)(1), [nix-build](/man/nix-build)(1), [nix-develop](/man/nix-develop)(1)"]],"e":["nix","nix-build","nix-develop"],"x":"nix flake show"},"numactl":{"n":"numactl","t":"Run processes with a specific NUMA scheduling or memory placement policy","s":[["TAGLINE","Run processes with a specific NUMA scheduling or memory placement policy"],["TLDR","Run command on **node 0** with memory on nodes 0 and 1\n\n```numactl --cpunodebind=0 --membind=0,1 -- command```\n\nRun command on **specific CPUs**\n\n```numactl --physcpubind=0-4,8-12 -- command```\n\nRun with memory **interleaved** across all nodes\n\n```numactl --interleave=all -- command```\n\nShow **current NUMA policy**\n\n```numactl --show```\n\nShow **hardware NUMA configuration**\n\n```numactl --hardware```\n\nPrefer memory allocation on a **specific node**\n\n```numactl --preferred=0 -- command```"],["SYNOPSIS","**numactl** [_--interleave=nodes_] [_--membind=nodes_] [_--cpunodebind=nodes_] [_--physcpubind=cpus_] [_--_] _command_ [_arguments_]"],["DESCRIPTION","**numactl** runs processes with a specific NUMA (Non-Uniform Memory Access) scheduling or memory placement policy. The policy is set for the command and inherited by all its children. It can also manage policies for shared memory segments."],["PARAMETERS","**-a, --all**\n> Disable cpuset awareness for broader CPU/node access\n\n**-i, --interleave=nodes**\n> Round-robin memory allocation across specified nodes\n\n**-m, --membind=nodes**\n> Restrict memory allocation to specified nodes only\n\n**-N, --cpunodebind=nodes**\n> Limit CPU execution to processors on specified nodes\n\n**-C, --physcpubind=cpus**\n> Bind process to specific physical CPUs\n\n**-l, --localalloc**\n> Prefer allocation on current node with fallback\n\n**-p, --preferred=node**\n> Prefer single node for allocation with fallback\n\n**-P, --preferred-many=nodes**\n> Prefer multiple nodes based on proximity\n\n**-b, --balancing**\n> Enable kernel NUMA balancing\n\n**-s, --show**\n> Display current NUMA policy settings\n\n**-H, --hardware**\n> Show available nodes and CPUs\n\n**--huge**\n> Use huge pages for SYSV shared memory\n\n**--shm, --shmid**\n> Specify shared memory segment\n\n**--touch**\n> Enforce policy immediately rather than on access\n\n**--strict**\n> Error if pages already faulted with conflicting policy\n\n**-V, --version**\n> Print version and exit"],["CAVEATS","NUMA policies only make sense on multi-node systems. Node numbering starts at 0. The special value \"all\" can be used to specify all nodes."],["HISTORY","**numactl** is part of the **numactl** package for Linux NUMA systems. It provides user-space control over NUMA memory and CPU policies that complement kernel automatic NUMA balancing."],["INSTALL","```apt: sudo apt install numactl```\n\n```dnf: sudo dnf install numactl```\n\n```pacman: sudo pacman -S numactl```\n\n```apk: sudo apk add numactl-tools```\n\n```zypper: sudo zypper install numactl```\n\n```brew: brew install numactl```\n\n```nix: nix profile install nixpkgs#numactl```"],["SEE ALSO","[migratepages](/man/migratepages)(8), [numastat](/man/numastat)(8), [mbind](/man/mbind)(2)"]],"e":["migratepages","numastat","mbind"],"x":"numactl --cpunodebind=0 --membind=0,1 -- command"},"od":{"n":"od","t":"Dump files in octal and other formats","s":[["TAGLINE","Dump files in octal and other formats"],["TLDR","**Dump file in octal** (default)\n\n```od [file]```\n\n**Dump in hexadecimal** (two-byte units)\n\n```od -x [file]```\n\n**Dump as ASCII characters** and backslash escapes\n\n```od -c [file]```\n\n**Dump with hex addresses** and single-byte hex output\n\n```od -A x -t x1 [file]```\n\n**Skip bytes from start**\n\n```od -j [bytes] [file]```\n\n**Limit output** to a specific number of bytes\n\n```od -N [bytes] [file]```\n\n**Show all data** without duplicate suppression\n\n```od -v [file]```\n\n**Dump as named characters** (ignoring high-order bit)\n\n```od -a [file]```"],["SYNOPSIS","**od** [_options_] [_file_...]"],["PARAMETERS","_FILE_\n> File(s) to dump. With no FILE, or when FILE is -, read standard input.\n\n**-A** _RADIX_, **--address-radix**=_RADIX_\n> Address radix: d (decimal), o (octal), x (hex), n (none)\n\n**-t** _TYPE_, **--format**=_TYPE_\n> Output type (a, c, d, f, o, u, x with optional size suffix)\n\n**-a**\n> Named characters, same as **-t a** (ignores high-order bit).\n\n**-b**\n> Octal bytes, same as **-t o1**.\n\n**-c**\n> Printable characters or backslash escapes, same as **-t c**.\n\n**-d**\n> Unsigned decimal, two-byte units, same as **-t u2**.\n\n**-f**\n> Floating point, same as **-t fF**.\n\n**-i**\n> Decimal int (sizeof(int)), same as **-t dI**.\n\n**-l**\n> Decimal long, same as **-t dL**.\n\n**-o**\n> Octal, two-byte units, same as **-t o2**.\n\n**-s**\n> Decimal, two-byte units, same as **-t d2**.\n\n**-x**\n> Hexadecimal, two-byte units, same as **-t x2**.\n\n**-j** _BYTES_, **--skip-bytes**=_BYTES_\n> Skip BYTES input bytes first\n\n**-N** _BYTES_, **--read-bytes**=_BYTES_\n> Limit dump to BYTES input bytes\n\n**-v**, **--output-duplicates**\n> Do not use * to mark line suppression\n\n**-w**[_BYTES_], **--width**[=_BYTES_]\n> Output BYTES bytes per output line (default 32)\n\n**-S** _BYTES_, **--strings**[=_BYTES_]\n> Show only NUL-terminated strings of at least BYTES printable characters\n\n**--endian**={big|little}\n> Swap input bytes according to the specified byte order"],["DESCRIPTION","**od** (octal dump) writes an unambiguous representation of its input to standard output. By default it displays data in octal, with each line showing an offset and the data values. The tool is useful for inspecting binary files, examining non-printable characters, and debugging data formats.\n\nDuplicate lines in the output are replaced by a single **\\*** character unless **-v** is used."],["CAVEATS","Default output is octal, which is less intuitive than hex for most modern use cases. BYTES arguments may be followed by size suffixes: b (512), KB (1000), K (1024), MB, M, etc. Part of GNU coreutils."],["HISTORY","od (octal dump) originated in **early Unix** (Version 1, 1971) for viewing file contents in octal representation."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[hexdump](/man/hexdump)(1), [xxd](/man/xxd)(1), [hd](/man/hd)(1)"]],"e":["hexdump","xxd","hd"],"x":"od [file]"},"opencode-import":{"n":"opencode-import","t":"Import OpenCode session data","s":[["TAGLINE","Import OpenCode session data"],["TLDR","**Import** a session from a JSON file\n\n```opencode import [path/to/session.json]```"],["SYNOPSIS","**opencode import** _file_"],["DESCRIPTION","**opencode import** imports session data into OpenCode from a JSON file previously created with **opencode export**. This allows restoring or sharing conversation sessions between different OpenCode installations or users."],["INSTALL","```pacman: sudo pacman -S opencode```\n\n```brew: brew install opencode```\n\n```nix: nix profile install nixpkgs#opencode```"],["SEE ALSO","[opencode](/man/opencode)(1), [opencode-export](/man/opencode-export)(1), [opencode-session](/man/opencode-session)(1)"]],"e":["opencode","opencode-export","opencode-session"],"x":"opencode import [path/to/session.json]"},"pacinstall":{"n":"pacinstall","t":"alias for **pactrans --install**, providing a convenient shorthand","s":[["TAGLINE","alias for **pactrans --install**, providing a convenient shorthand"],["TLDR","This command is an **alias** of **pactrans --install**\n\n```tldr pactrans```"],["SYNOPSIS","**pacinstall** [_options_] _package_..."],["DESCRIPTION","**pacinstall** is an alias for **pactrans --install**, providing a convenient shorthand for installing packages using the pactrans transaction manager from pacutils."],["SEE ALSO","[pactrans](/man/pactrans)(8), [pacman](/man/pacman)(8)"]],"e":["pactrans","pacman"],"x":"tldr pactrans"},"pnmtoplainpnm":{"n":"pnmtoplainpnm","t":"Convert PNM to plain ASCII format (obsolete)","s":[["TAGLINE","Convert PNM to plain ASCII format (obsolete)"],["TLDR","**Convert a PNM image to plain ASCII format**\n\n```pnmtoplainpnm [input.pnm] > [output.pnm]```\n\n**Read from stdin and write plain format to a file**\n\n```cat [input.pnm] | pnmtoplainpnm > [output.pnm]```"],["SYNOPSIS","**pnmtoplainpnm** [_file_]"],["DESCRIPTION","**pnmtoplainpnm** reads a PNM image as input and writes an equivalent image in plain (ASCII) PNM format. Plain format is human-readable but significantly larger than the binary (raw) format. Part of the Netpbm toolkit.\n\n**pnmtoplainpnm** was obsoleted in Netpbm 10.23 (July 2004) by **pamtopnm**. It is retained for backward compatibility; all it does is invoke `pamtopnm -plain`. The preferred modern approach is to use the **-plain** common option on any Netpbm program."],["HISTORY","**pnmtoplainpnm** was new in Netpbm 8.2 (March 2000) as a renaming of **pnmnoraw**, which was new in Pbmplus in November 1989. It was obsoleted by **pamtopnm** in Netpbm 10.23 (July 2004)."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pamtopnm](/man/pamtopnm)(1), [pnmtopnm](/man/pnmtopnm)(1), [pnm](/man/pnm)(1)"]],"e":["pamtopnm","pnmtopnm","pnm"],"x":"pnmtoplainpnm [input.pnm] > [output.pnm]"},"pssh":{"n":"pssh","t":"Run commands on multiple hosts in parallel","s":[["TAGLINE","Run commands on multiple hosts in parallel"],["TLDR","**Run command on multiple hosts**\n\n```pssh -h [hosts.txt] -i \"[uptime]\"```\n\n**Run with inline hosts**\n\n```pssh -H \"[user@host1] [user@host2]\" -i \"[command]\"```\n\n**Run with parallel limit**\n\n```pssh -h [hosts.txt] -p [10] -i \"[command]\"```\n\n**Run with timeout**\n\n```pssh -h [hosts.txt] -t [30] -i \"[command]\"```\n\n**Copy file to hosts**\n\n```pscp -h [hosts.txt] [local_file] [/remote/path]```\n\n**Copy file from hosts**\n\n```pslurp -h [hosts.txt] [/remote/file] [local_dir]```\n\n**Run interactively**\n\n```pssh -h [hosts.txt] -i -A \"[sudo command]\"```"],["SYNOPSIS","**pssh** [_-h hostfile_] [_-H hosts_] [_-p parallel_] [_-t timeout_] [_-i_] [_options_] _command_"],["PARAMETERS","**-h** _FILE_\n> File with hosts (one per line).\n\n**-H** _HOSTS_\n> Space-separated hosts.\n\n**-l** _USER_\n> Default username.\n\n**-p** _NUM_\n> Max parallel connections.\n\n**-t** _SECONDS_\n> Timeout per host.\n\n**-i**, **--inline**\n> Display output inline.\n\n**-o** _DIR_\n> Output directory.\n\n**-e** _DIR_\n> Error output directory.\n\n**-A**\n> Prompt for password.\n\n**-O** _OPTS_\n> SSH options.\n\n**-x** _ARGS_\n> Extra SSH arguments.\n\n**-v**\n> Verbose mode."],["DESCRIPTION","**pssh** (Parallel SSH) runs commands on multiple hosts simultaneously. It parallelizes SSH connections for faster execution across large server fleets.\n\nHost files list target machines, one per line. Format supports user@host:port syntax. Comments start with #.\n\nParallel limit controls concurrent connections. Too many simultaneous connections may overwhelm networks or hosts. Start conservatively.\n\nOutput modes collect results differently. Inline (-i) shows output as it arrives. Directory mode (-o) saves per-host output files.\n\nRelated tools include pscp for parallel file copy to hosts, pslurp for parallel file retrieval, and prsync for parallel rsync.\n\nTimeout prevents hanging on unresponsive hosts. Failed hosts are reported separately from successful ones."],["CAVEATS","Requires SSH key auth for non-interactive use. Password prompt (-A) is interactive. Large parallel counts may cause issues. Hostnames must resolve."],["HISTORY","**pssh** was created by **Brent N. Chun** at Intel Research around **2007**. It addressed the need for efficient multi-host command execution in cluster and cloud environments."],["INSTALL","```dnf: sudo dnf install pssh```\n\n```apk: sudo apk add pssh```\n\n```zypper: sudo zypper install pssh```\n\n```brew: brew install pssh```\n\n```nix: nix profile install nixpkgs#pssh```"],["SEE ALSO","[ssh](/man/ssh)(1), [pdsh](/man/pdsh)(1), [ansible](/man/ansible)(1), [clusterssh](/man/clusterssh)(1)"]],"e":["ssh","pdsh","ansible","clusterssh"],"x":"pssh -h [hosts.txt] -i \"[uptime]\""},"puppet":{"n":"puppet","t":"Declarative configuration management tool","s":[["TAGLINE","Declarative configuration management tool"],["TLDR","**Apply manifest locally**\n\n```puppet apply [manifest.pp]```\n\n**Check manifest syntax**\n\n```puppet parser validate [manifest.pp]```\n\n**Run puppet agent in test mode**\n\n```puppet agent --test```\n\n**Run puppet agent in dry-run mode**\n\n```puppet agent --test --noop```\n\n**List available modules**\n\n```puppet module list```\n\n**Install a module from Puppet Forge**\n\n```puppet module install [author-module]```\n\n**Inspect a resource on the system**\n\n```puppet resource [user] [root]```\n\n**View or set a configuration value**\n\n```puppet config print [server]```"],["SYNOPSIS","**puppet** _subcommand_ [_options_]"],["PARAMETERS","**apply**\n> Compile and apply a Puppet manifest locally.\n\n**agent**\n> Request a catalog from a Puppet server and enforce it.\n\n**parser**\n> Validate Puppet manifest syntax.\n\n**module**\n> Install, list, upgrade, and manage Puppet modules.\n\n**resource**\n> Inspect and manipulate resources on the system.\n\n**config**\n> View and change Puppet configuration settings.\n\n**ssl**\n> Manage SSL keys and certificates.\n\n**help**\n> Display help for subcommands.\n\n**--test**\n> Run the agent once in the foreground with verbose output.\n\n**--noop**\n> Simulate changes without applying them (dry run).\n\n**--verbose**\n> Enable verbose logging.\n\n**--debug**\n> Enable full debug logging.\n\n**--environment** _env_\n> Select a specific Puppet environment."],["DESCRIPTION","**puppet** is a configuration management tool that automates the provisioning and management of infrastructure using a declarative language. Administrators describe the desired state of systems through manifests written in Puppet's DSL, and the tool ensures machines converge to that state by installing packages, managing files, configuring services, and handling users.\n\nPuppet operates in two modes: a client-server architecture where agents pull catalogs from a Puppet server, and a standalone mode using **puppet apply** for local manifest execution. The resource abstraction layer handles platform differences automatically, so the same manifest can manage packages across Debian, Red Hat, and other distributions without modification.\n\nThe ecosystem includes a module system for sharing reusable configuration code through Puppet Forge, Facter for gathering system facts, and Hiera for separating data from code. Puppet supports environments for testing changes before production rollout and provides detailed reporting on configuration drift and enforcement actions."],["CONFIGURATION","**/etc/puppetlabs/puppet/puppet.conf**\n> Main configuration file controlling server address, environment, run interval, certificate settings, and module paths.\n\n**/etc/puppetlabs/code/environments/production/manifests/**\n> Default location for site manifests that define node configurations in the production environment.\n\n**/etc/puppetlabs/code/environments/production/modules/**\n> Module directory containing reusable Puppet modules with classes, defined types, and templates.\n\n**FACTERLIB**\n> Environment variable specifying additional directories to search for custom Facter facts."],["CAVEATS","Requires Puppet installation. Master-agent or standalone. The **--test** flag combines --onetime, --verbose, --no-daemonize, and a non-zero exit code on failures."],["HISTORY","Puppet was created by **Puppet Labs** (now Perforce) for IT automation."],["INSTALL","```dnf: sudo dnf install puppet```\n\n```pacman: sudo pacman -S puppet```"],["SEE ALSO","[puppet-apply](/man/puppet-apply)(1), [puppet-agent](/man/puppet-agent)(1), [ansible](/man/ansible)(1), [chef](/man/chef)(1)"]],"e":["puppet-apply","puppet-agent","ansible","chef"],"x":"puppet apply [manifest.pp]"},"pvck":{"n":"pvck","t":"Check and repair LVM physical volume metadata","s":[["TAGLINE","Check and repair LVM physical volume metadata"],["TLDR","Print LVM **on-disk headers and structures**\n\n```sudo pvck /dev/sdX1 --dump headers```\n\nPrint the current **VG metadata** text\n\n```sudo pvck /dev/sdX1 --dump metadata```\n\nList all **metadata versions** found in the metadata area\n\n```sudo pvck /dev/sdX1 --dump metadata_all```\n\n**Search** for metadata when headers may be damaged\n\n```sudo pvck /dev/sdX1 --dump metadata_search -f path/to/metadata.txt```\n\nSelect the **second metadata area** when printing\n\n```sudo pvck /dev/sdX1 --dump metadata --settings \"mda_num=2\"```\n\n**Repair** headers and metadata using a metadata file\n\n```sudo pvck /dev/sdX1 --repair -f path/to/metadata_file```\n\nRepair only **PV header and label**\n\n```sudo pvck /dev/sdX1 --repairtype pv_header```"],["SYNOPSIS","**pvck** [_options_] _PhysicalVolume_"],["PARAMETERS","**--dump** _type_\n> Dump specified type: headers, metadata, metadata_all, metadata_search.\n\n**--repair**\n> Repair LVM metadata on the physical volume.\n\n**--repairtype** _type_\n> Specify repair type: pv_header, metadata, label_header.\n\n**-f**, **--file** _file_\n> Input/output file for metadata operations.\n\n**--settings** _settings_\n> Additional settings (e.g., mda_num=2).\n\n**--labelsector** _sector_\n> Sector number to read the PV label from (0-3, default: 1).\n\n**-d**, **--debug**\n> Enable debug output.\n\n**-v**, **--verbose**\n> Enable verbose output.\n\n**-y**, **--yes**\n> Answer yes to all prompts automatically.\n\n**-t**, **--test**\n> Run in test mode without making changes."],["DESCRIPTION","**pvck** checks and repairs LVM metadata on physical volumes. It can dump on-disk headers and structures (label_header, pv_header, mda_header), examine metadata areas, search for metadata in damaged volumes, and perform repairs using backup metadata files.\n\nThe **--dump** option reads and displays metadata without modifying anything. The **--repair** option writes corrected headers and metadata using a provided backup file. A PV may have up to two metadata areas (front and back), selectable with `--settings \"mda_num=2\"`.\n\nThis tool is essential for LVM disaster recovery when physical volume metadata becomes corrupted."],["CAVEATS","Repair operations can be destructive. Always backup metadata before attempting repairs. Use with extreme caution on production systems."],["HISTORY","Part of **LVM2** (Logical Volume Manager 2), the standard volume management system for Linux."],["INSTALL","```apt: sudo apt install lvm2```\n\n```dnf: sudo dnf install lvm2```\n\n```pacman: sudo pacman -S lvm2```\n\n```apk: sudo apk add lvm2```\n\n```zypper: sudo zypper install lvm2```\n\n```nix: nix profile install nixpkgs#lvm2```"],["SEE ALSO","[pvcreate](/man/pvcreate)(8), [pvdisplay](/man/pvdisplay)(8), [vgcfgrestore](/man/vgcfgrestore)(8), [lvm](/man/lvm)(8)"]],"e":["pvcreate","pvdisplay","vgcfgrestore","lvm"],"x":"sudo pvck /dev/sdX1 --dump headers"},"py3clean":{"n":"py3clean","t":"Remove Python 3 compiled bytecode files","s":[["TAGLINE","Remove Python 3 compiled bytecode files"],["TLDR","**Clean Python 3 bytecode**\n\n```py3clean [directory]```\n\n**Clean entire system**\n\n```sudo py3clean -p [package]```\n\n**Dry run**\n\n```py3clean -n [directory]```\n\n**Verbose output**\n\n```py3clean -v [directory]```"],["SYNOPSIS","**py3clean** [_options_] _directory_|**-p** _package_"],["PARAMETERS","**-p**, **--package** _package_\n> Clean specific Debian package.\n\n**-v**, **--verbose**\n> Verbose output.\n\n**-n**, **--dry-run**\n> Show what would be deleted.\n\n**-q**, **--quiet**\n> Quiet mode."],["DESCRIPTION","**py3clean** removes Python 3 compiled bytecode files (.pyc) and __pycache__ directories. Part of Debian's python3-minimal package for system cleanup."],["EXAMPLES","```bash"],["Clean project directory","py3clean /path/to/project"],["Clean installed package","sudo py3clean -p python3-requests"],["Dry run first","py3clean -n /usr/lib/python3"],["Verbose cleaning","py3clean -v ~/myproject"],["Clean current directory","py3clean .\n```"],["WHAT IT REMOVES","```\n*.pyc files\n__pycache__/ directories\n```"],["CAVEATS","Debian/Ubuntu specific tool. Alternative: find . -name '*.pyc' -delete. Bytecode regenerates on next import."],["HISTORY","py3clean is part of Debian's **dh-python** package for Python 3 packaging and cleanup."],["INSTALL","```apt: sudo apt install python3-minimal```"],["SEE ALSO","[pyclean](/man/pyclean)(1), [python3](/man/python3)(1)"]],"e":["pyclean","python3"],"x":"py3clean [directory]"},"qm-create":{"n":"qm-create","t":"Create a Proxmox virtual machine","s":[["TAGLINE","Create a Proxmox virtual machine"],["TLDR","Create a VM with **default settings** (512MiB memory, 1 CPU)\n\n```qm create 100```\n\nCreate with **name** and **start** automatically\n\n```qm create 100 --name vm_name --start```\n\nCreate with specific **memory** and **CPUs**\n\n```qm create 100 --memory 8192 --cores 4```\n\nSpecify the **OS type**\n\n```qm create 100 --ostype win10```\n\n**Replace** an existing machine from archive\n\n```qm create 100 --archive path/to/backup.tar --force 1```\n\nSpecify **install media**\n\n```qm create 100 --cdrom local:iso/install.iso```\n\nCreate with **network bridge**\n\n```qm create 100 --net0 virtio,bridge=vmbr0```"],["SYNOPSIS","**qm create** _vmid_ [_options_]"],["PARAMETERS","**--name** _name_\n> Set VM name\n\n**--memory** _mb_\n> RAM size in megabytes\n\n**--cores** _count_\n> Number of CPU cores\n\n**--ostype** _type_\n> Operating system type (l24, l26, win10, etc.)\n\n**--cdrom** _volume_\n> ISO image for installation\n\n**--net0** _config_\n> Network adapter configuration\n\n**--start**\n> Start VM after creation\n\n**--archive** _file_\n> Restore from backup archive\n\n**--force** _1|0_\n> Force overwrite existing VM"],["DESCRIPTION","**qm create** creates or restores a virtual machine on QEMU/KVM in Proxmox VE. It supports extensive configuration options for CPU, memory, storage, and networking.\n\nVMs can be created from scratch or restored from backup archives."],["CAVEATS","VM IDs must be unique across the cluster. Some options require additional storage or network configuration."],["HISTORY","Part of **Proxmox VE** QEMU/KVM management tools for virtual machine administration."],["INSTALL","```dnf: sudo dnf install qm```"],["SEE ALSO","[qm](/man/qm)(1), [qm-clone](/man/qm-clone)(1), [qm-destroy](/man/qm-destroy)(1)"]],"e":["qm","qm-clone","qm-destroy"],"x":"qm create 100"},"rails":{"n":"rails","t":"Ruby on Rails web application framework CLI","s":[["TAGLINE","Ruby on Rails web application framework CLI"],["TLDR","**Create a new Rails application**\n\n```rails new [app_name]```\n\n**Create a new app with PostgreSQL** database\n\n```rails new [app_name] --database=postgresql```\n\n**Start the development server** on port 3000\n\n```rails server```\n\n**Open an interactive console** with the application loaded\n\n```rails console```\n\n**Generate a model** with attributes\n\n```rails generate model [ModelName] [field:type] [field:type]```\n\n**Generate a controller** with actions\n\n```rails generate controller [ControllerName] [action1] [action2]```\n\n**Run database migrations**\n\n```rails db:migrate```\n\n**List all routes** in the application\n\n```rails routes```"],["SYNOPSIS","**rails** _command_ [_options_] [_arguments_]"],["PARAMETERS","**new** _app_name_\n> Create a new Rails application with standard directory structure\n\n**server**, **s**\n> Start the Puma web server (default port 3000)\n\n**console**, **c**\n> Open an interactive IRB session with the application context\n\n**generate**, **g**\n> Run code generators for models, controllers, migrations, etc.\n\n**destroy**, **d**\n> Remove files created by a generator\n\n**db:migrate**\n> Run pending database migrations\n\n**db:create**\n> Create the database\n\n**db:seed**\n> Load seed data from db/seeds.rb\n\n**db:setup**\n> Create database, load schema, and seed data\n\n**routes**\n> Display all defined routes\n\n**test**\n> Run the test suite\n\n**--help**, **-h**\n> Display help for any command\n\n**--version**\n> Show the Rails version\n\n**-e**, **--environment** _ENV_\n> Specify environment (development, test, production)\n\n**-p**, **--port** _PORT_\n> Set server port (default 3000)"],["DESCRIPTION","**rails** is the command-line interface for the Ruby on Rails web application framework. It provides tools for creating, developing, and managing Rails applications through a unified interface.\n\nThe command operates in two contexts: outside an application (primarily **rails new** for creating projects) and inside an application directory (all other commands). Within a project, commands are typically invoked via **bin/rails** to ensure the correct bundled version is used.\n\nRails emphasizes convention over configuration, and its CLI reflects this by providing generators that create boilerplate code following Rails conventions. The framework integrates database management, testing, asset compilation, and server operation into cohesive command-line workflows."],["CONFIGURATION","**config/database.yml**\n> Database connection settings for each environment (development, test, production), specifying adapter, host, credentials, and pool size.\n\n**config/routes.rb**\n> Application URL routing definitions mapping HTTP methods and paths to controller actions.\n\n**config/application.rb**\n> Main application configuration including framework defaults, middleware, and autoload paths.\n\n**config/environments/**\n> Per-environment settings files (development.rb, test.rb, production.rb) controlling caching, logging, and asset behavior.\n\n**RAILS_ENV**\n> Environment variable selecting the active environment (defaults to development)."],["CAVEATS","Most commands must be run from within a Rails application directory. The **rails new** command is the exception and creates that directory structure.\n\nGenerated code should be reviewed before use. Generators create standard patterns that may need customization for specific requirements.\n\nDatabase commands require proper database configuration in **config/database.yml** and the appropriate database adapter gem."],["HISTORY","Rails was created by **David Heinemeier Hansson** and extracted from the Basecamp project management tool in **2004**. The first public release was version 1.0 in December **2005**. The framework popularized the MVC pattern for web development and introduced conventions like RESTful routing that influenced many subsequent frameworks."],["SEE ALSO","[ruby](/man/ruby)(1), [bundle](/man/bundle)(1), [rake](/man/rake)(1), [gem](/man/gem)(1)"]],"e":["ruby","bundle","rake","gem"],"x":"rails new [app_name]"},"rbac-lookup":{"n":"rbac-lookup","t":"Look up Kubernetes RBAC roles for users","s":[["TAGLINE","Look up Kubernetes RBAC roles for users"],["TLDR","**Find roles for user**\n\n```rbac-lookup [user@example.com]```\n\n**Find roles for service account**\n\n```rbac-lookup [service-account-name] --kind serviceaccount```\n\n**Find roles for group**\n\n```rbac-lookup [group-name] --kind group```\n\n**Output wide format**\n\n```rbac-lookup [subject] -o wide```\n\n**Search in namespace**\n\n```rbac-lookup [subject] -n [namespace]```\n\n**All bindings for subject**\n\n```rbac-lookup [subject] --all-namespaces```"],["SYNOPSIS","**rbac-lookup** [_--kind type_] [_-n namespace_] [_-o format_] [_options_] _subject_"],["PARAMETERS","**--kind** _TYPE_\n> Subject kind (user, group, serviceaccount).\n\n**-n**, **--namespace** _NS_\n> Namespace to search.\n\n**-A**, **--all-namespaces**\n> All namespaces.\n\n**-o**, **--output** _FORMAT_\n> Output format.\n\n**--gke**\n> GKE-specific mode.\n\n**-k**, **--kubeconfig** _FILE_\n> Kubeconfig file."],["DESCRIPTION","**rbac-lookup** queries a Kubernetes cluster to find all RBAC role bindings associated with a given subject, answering the question \"what permissions does this user, group, or service account have?\" It searches both ClusterRoleBindings and namespace-scoped RoleBindings to provide a complete picture of a subject's access across the cluster.\n\nThe tool supports lookups by user identity, group membership, and service account name via the **--kind** flag. Wide output format (**-o wide**) displays detailed binding information including the namespace, role type, and source binding for each permission grant. This makes it particularly useful for security audits and troubleshooting access issues in clusters with complex RBAC configurations."],["CONFIGURATION","**~/.kube/config**\n> Default kubeconfig file specifying cluster connections, authentication credentials, and context selection. Override with **-k** flag."],["CAVEATS","Requires cluster read access. Large clusters may be slow. Aggregated roles not expanded."],["HISTORY","**rbac-lookup** was created by **FairwindsOps** for Kubernetes RBAC auditing. It simplifies understanding of complex role binding relationships."],["SEE ALSO","[kubectl](/man/kubectl)(1), [rbac-tool](/man/rbac-tool)(1), [kubeaudit](/man/kubeaudit)(1)"]],"e":["kubectl","rbac-tool","kubeaudit"],"x":"rbac-lookup [user@example.com]"},"ripsecrets":{"n":"ripsecrets","t":"Fast local scanner to stop secrets entering git commits","s":[["TAGLINE","Fast local scanner to stop secrets entering git commits"],["TLDR","**Scan** the current directory for secrets\n\n```ripsecrets```\n\n**Scan specific files** (e.g. staged changes)\n\n```ripsecrets --strict-ignore $(git diff --cached --name-only --diff-filter=ACM)```\n\n**Install** as a git pre-commit hook\n\n```ripsecrets --install-pre-commit```"],["SYNOPSIS","**ripsecrets** [*options*] [*path*...]"],["DESCRIPTION","**ripsecrets** searches source files for high-entropy strings and known secret patterns to prevent accidental commits. It is designed for pre-commit speed: local-only (no cloud verification), low false-positive rate relative to naive regex tools, and a single static binary."],["PARAMETERS","*path*...\n\n> Files or directories to scan (default: recursive from cwd).\n\n**--install-pre-commit**\n\n> Install a git pre-commit hook that runs ripsecrets.\n\n**--strict-ignore**\n\n> Honor ignore rules strictly when scanning explicit file lists (typical with **git diff --cached**).\n\nAdditional flags control allowlists and output—see **ripsecrets --help**."],["CAVEATS","Local pattern matching cannot prove a string is a live credential; it also cannot catch every secret type. Use with commit hooks and complementary scanners for defense in depth. Never commit real secrets “just for testing.”"],["INSTALL","```brew: brew install ripsecrets```\n\n```nix: nix profile install nixpkgs#ripsecrets```"],["SEE ALSO","[git](/man/git)(1), [gitleaks](/man/gitleaks)(1), [trufflehog](/man/trufflehog)(1)"],["RESOURCES","```[Source code](https://github.com/sirwart/ripsecrets)```"]],"e":["git","gitleaks","trufflehog"],"x":"ripsecrets"},"rspamd":{"n":"rspamd","t":"Advanced spam filtering daemon","s":[["TAGLINE","Advanced spam filtering daemon"],["TLDR","**Start Rspamd**\n\n```rspamd```\n\n**Start with config file**\n\n```rspamd -c [/etc/rspamd/rspamd.conf]```\n\n**Test mode**\n\n```rspamd -t```\n\n**Foreground mode**\n\n```rspamd -f```\n\n**Start via systemd**\n\n```sudo systemctl start rspamd```"],["SYNOPSIS","**rspamd** [_options_]"],["PARAMETERS","**-c**, **--config** _file_\n> Configuration file.\n\n**-f**, **--no-fork**\n> Run in foreground.\n\n**-t**, **--test-config**\n> Test configuration.\n\n**-u**, **--user** _user_\n> Run as user.\n\n**-g**, **--group** _group_\n> Run as group."],["DESCRIPTION","**Rspamd** is a fast, free spam filtering system written in C. It uses machine learning, neural networks, and various rules to identify spam with high accuracy and low resource usage."],["EXAMPLES","```bash"],["Start daemon","rspamd"],["Test configuration","rspamd -t"],["Via systemctl","sudo systemctl start rspamd\nsudo systemctl enable rspamd"],["Check message","rspamc < message.eml"],["Check message with details","rspamc -v < message.eml"],["http://localhost:11334/","```"],["CONFIGURATION","**/etc/rspamd/rspamd.conf**\n> Main configuration file defining workers, modules, and global settings.\n\n**/etc/rspamd/local.d/**\n> Directory for local configuration overrides that merge with default settings. Preferred location for custom rules.\n\n**/etc/rspamd/override.d/**\n> Directory for full configuration overrides that completely replace default settings for specified modules."],["PORTS","```\n11333 - Worker (milter)\n11334 - Web interface\n11335 - Controller\n```"],["CAVEATS","Configure milter integration with MTA. Train Bayes for best results. Web UI password in worker-controller.inc."],["HISTORY","Rspamd was created by **Vsevolod Stakhov** starting in 2008 as a fast, extensible spam filtering system."],["INSTALL","```apt: sudo apt install rspamd```\n\n```pacman: sudo pacman -S rspamd```\n\n```apk: sudo apk add rspamd```\n\n```zypper: sudo zypper install rspamd```\n\n```nix: nix profile install nixpkgs#rspamd```"],["SEE ALSO","[rspamadm](/man/rspamadm)(1), [rspamc](/man/rspamc)(1)"]],"e":["rspamadm","rspamc"],"x":"rspamd"},"shc":{"n":"shc","t":"Compile shell scripts to binary executables","s":[["TAGLINE","Compile shell scripts to binary executables"],["TLDR","**Compile shell script**\n\n```shc -f [script.sh]```\n\n**Set expiration date**\n\n```shc -e [31/12/2026] -f [script.sh]```\n\n**Set expiration message**\n\n```shc -m \"[Script expired]\" -f [script.sh]```\n\n**Custom output name**\n\n```shc -o [binary] -f [script.sh]```\n\n**Relaxed security**\n\n```shc -r -f [script.sh]```\n\n**Hardened untraceable** binary\n\n```shc -U -H -f [script.sh]```\n\n**Verbose output**\n\n```shc -v -f [script.sh]```"],["SYNOPSIS","**shc** [**-f** _script_] [**-o** _output_] [**-e** _date_] [**-m** _message_] [**-r**] [**-U**] [**-H**] [**-v**]"],["PARAMETERS","**-f** _SCRIPT_\n> Input shell script to compile\n\n**-o** _FILE_\n> Output binary filename\n\n**-e** _DATE_\n> Expiration date in dd/mm/yyyy format\n\n**-m** _MESSAGE_\n> Message to display upon expiration (default: \"Please contact your provider\")\n\n**-r**\n> Relaxed security, allow redistributable binary across systems\n\n**-v**\n> Verbose compilation output\n\n**-U**\n> Make binary untraceable (blocks truss, strace, ltrace, etc.)\n\n**-H**\n> Hardening: extra protection against dumping, code injection, and ptrace\n\n**-S**\n> Switch on setuid for root callable programs\n\n**-B**\n> Compile for BusyBox\n\n**-D**\n> Switch on debug exec calls\n\n**-i** _inline_option_\n> Inline option for the shell interpreter\n\n**-x** _command_\n> Exec command, as a printf format"],["DESCRIPTION","**shc** converts shell scripts into compiled binary executables by encrypting the script content and wrapping it in auto-generated C code. The resulting C source is compiled with the system's C compiler to produce a standalone binary that decrypts and executes the original script at runtime.\n\nThe tool provides basic source code obfuscation rather than true security, since the script can be recovered from the binary with sufficient effort. Optional features include expiration dates that make the binary refuse to run after a specified date, and strict mode that binds the binary to the current host to prevent redistribution. Relaxed mode (**-r**) allows the binary to run on different systems.\n\nAn intermediate **.x.c** file containing the generated C source is produced alongside the binary and can be inspected, modified, or compiled manually with different options."],["CAVEATS","Not true compilation - script is recoverable. Security is obfuscation, not encryption. Requires C compiler."],["HISTORY","**shc** (Shell Compiler) was created by **Francisco Javier Rosales Garcia**. It's used for basic shell script protection and distribution."],["INSTALL","```dnf: sudo dnf install shc```\n\n```brew: brew install shc```\n\n```nix: nix profile install nixpkgs#shc```"],["SEE ALSO","[bash](/man/bash)(1), [gcc](/man/gcc)(1)"]],"e":["bash","gcc"],"x":"shc -f [script.sh]"},"simctl":{"n":"simctl","t":"Manage Apple iOS simulators","s":[["TAGLINE","Manage Apple iOS simulators"],["TLDR","**List all simulators** and runtimes\n\n```xcrun simctl list```\n\n**Boot a simulator** by name or UUID\n\n```xcrun simctl boot \"[iPhone 15 Pro]\"```\n\n**Shutdown a simulator**\n\n```xcrun simctl shutdown [device_uuid|booted]```\n\n**Install an app** on a booted simulator\n\n```xcrun simctl install booted [path/to/app.app]```\n\n**Launch an app** on the simulator\n\n```xcrun simctl launch booted [com.example.bundleid]```\n\n**Take a screenshot** of the simulator\n\n```xcrun simctl io booted screenshot [output.png]```\n\n**Send a push notification**\n\n```xcrun simctl push booted [com.example.bundleid] [notification.apns]```\n\n**Grant a privacy permission** to an app\n\n```xcrun simctl privacy booted grant [location] [com.example.bundleid]```"],["SYNOPSIS","**xcrun simctl** _subcommand_ [_arguments_]"],["SUBCOMMANDS","**list** [_devices_|_devicetypes_|_runtimes_|_pairs_]\n> List available devices, device types, runtimes, or device pairs.\n\n**create** _name_ _devicetype_ _runtime_\n> Create a new simulator with the specified name, device type, and runtime.\n\n**delete** _device_\n> Delete a simulator device.\n\n**boot** _device_\n> Boot a simulator device.\n\n**shutdown** _device_|**all**\n> Shutdown a simulator or all simulators.\n\n**erase** _device_|**all**\n> Erase a simulator's contents and settings.\n\n**install** _device_ _app_path_\n> Install an application bundle on a simulator.\n\n**uninstall** _device_ _bundle_id_\n> Uninstall an application by bundle identifier.\n\n**launch** _device_ _bundle_id_ [_args_]\n> Launch an application on a simulator.\n\n**terminate** _device_ _bundle_id_\n> Terminate a running application.\n\n**io** _device_ _operation_\n> Perform I/O operations: screenshot, recordVideo.\n\n**push** _device_ _bundle_id_ _json_file_\n> Send a push notification to a running application.\n\n**privacy** _device_ _action_ _service_ _bundle_id_\n> Manage privacy permissions (grant, revoke, reset).\n\n**addmedia** _device_ _paths_\n> Add photos or videos to the simulator's photo library.\n\n**openurl** _device_ _url_\n> Open a URL in the simulator.\n\n**runtime** **add**|**delete** _path_\n> Manage simulator runtime disk images.\n\n**help** [_subcommand_]\n> Display help for simctl or a specific subcommand."],["DESCRIPTION","**simctl** is Apple's command-line tool for managing and interacting with iOS, watchOS, and tvOS simulators on macOS. It's accessed through **xcrun simctl** to ensure the correct version matching your active Xcode installation is used.\n\nThe tool provides comprehensive control over simulator lifecycle (create, boot, shutdown, delete), application management (install, launch, uninstall), and testing features (push notifications, privacy permissions, screenshots, video recording). It's particularly useful for automated testing, CI/CD pipelines, and development workflows.\n\nThe special identifier **booted** can be used to target the currently running simulator instead of specifying a UUID."],["CAVEATS","Only available on macOS with Xcode installed. Requires the Xcode Command Line Tools. Some features like privacy management require Xcode 11.4 or later. Simulator runtimes consume significant disk space. The tool path is **/Applications/Xcode.app/Contents/Developer/usr/bin/simctl** but should be invoked via **xcrun** to ensure version compatibility."],["HISTORY","**simctl** was introduced by Apple as part of Xcode to replace older simulator management tools. It has evolved alongside iOS development, gaining features for push notification testing, privacy permission management, and improved runtime handling. The tool has become essential for iOS CI/CD workflows and automated testing, comparable to Android's **adb** command."],["SEE ALSO","[xcrun](/man/xcrun)(1), [xcodebuild](/man/xcodebuild)(1)"]],"e":["xcrun","xcodebuild"],"x":"xcrun simctl list"},"sot":{"n":"sot","t":"System observation tool with live graphs","s":[["TAGLINE","System observation tool with live graphs"],["TLDR","**Launch the main monitoring interface**\n\n```sot```\n\n**Interactive process viewer with port detection**\n\n```sot ps```\n\n**Run disk performance benchmarks**\n\n```sot bench```"],["SYNOPSIS","**sot** [_command_] [_options_]"],["DESCRIPTION","**sot** is a cross-platform system monitoring tool inspired by **top** that provides real-time monitoring of CPU, memory, disk I/O, and network activity with live graphs. It includes an interactive process viewer with port detection, disk benchmarking (sequential throughput, random IOPS, latency), system info display with ASCII logos, and a system cleanup utility."],["HISTORY","**sot** was created by **Kumar Anirudha** (anistark) and is written in **Python**."],["INSTALL","```aur: yay -S sot```"],["SEE ALSO","[top](/man/top)(1), [htop](/man/htop)(1), [glances](/man/glances)(1)"]],"e":["top","htop","glances"],"x":"sot"},"soundscope":{"n":"soundscope","t":"TUI audio analyzer with real-time visualization","s":[["TAGLINE","TUI audio analyzer with real-time visualization"],["TLDR","**Launch with microphone input**\n\n```soundscope```\n\n**Analyze an audio file**\n\n```soundscope [path/to/audio.mp3]```"],["SYNOPSIS","**soundscope** [_path/to/audio_]"],["DESCRIPTION","**soundscope** is a cross-platform CLI tool for analyzing audio files and live microphone input in real-time. It displays waveforms, FFT frequency spectrum, and LUFS loudness metering directly in the terminal. It supports custom themes and hoverable frequency/amplitude details."],["HISTORY","**soundscope** was created by **bananaofhappiness** and is written in **Rust**."],["INSTALL","```pacman: sudo pacman -S soundscope```"],["SEE ALSO","[cava](/man/cava)(1), [sox](/man/sox)(1)"]],"e":["cava","sox"],"x":"soundscope"},"srftopam":{"n":"srftopam","t":"Convert Garmin SRF vehicle images to PAM","s":[["TAGLINE","Convert Garmin SRF vehicle images to PAM"],["TLDR","**Convert Garmin SRF to PAM**\n\n```srftopam [input.srf] > [output.pam]```"],["SYNOPSIS","**srftopam** [_file_]"],["DESCRIPTION","**srftopam** converts images in the Garmin SRF (Sun Raster Format variant used for vehicle icons) to PAM (Portable Arbitrary Map) format. The SRF format is used for custom vehicle icons displayed on Garmin GPS navigation devices. This tool is part of the Netpbm image processing toolkit, which provides converters between many image formats."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pamtosrf](/man/pamtosrf)(1), [pngtopam](/man/pngtopam)(1)"]],"e":["pamtosrf","pngtopam"],"x":"srftopam [input.srf] > [output.pam]"},"sunbeam":{"n":"sunbeam","t":"General purpose command-line launcher","s":[["TAGLINE","General purpose command-line launcher"],["TLDR","**Launch the root view**\n\n```sunbeam```\n\n**Run an extension** by alias or path\n\n```sunbeam run [extension] [command]```\n\n**Install an extension** from a URL or local path\n\n```sunbeam extension install [origin]```\n\n**List installed extensions**\n\n```sunbeam extension list```\n\n**Validate a script** against the Sunbeam schema\n\n```sunbeam validate [manifest.json]```\n\n**Open a file or URL** in the system handler\n\n```sunbeam open [target]```"],["SYNOPSIS","**sunbeam** [_command_] [_flags_]"],["PARAMETERS","**run** _extension_ [_command_]\n> Execute a command exposed by an installed extension.\n\n**extension install** _origin_\n> Install an extension from a Git URL or local directory.\n\n**extension list**\n> Show installed extensions.\n\n**extension remove** _alias_\n> Uninstall an extension.\n\n**validate** _file_\n> Check that a manifest or page conforms to the Sunbeam JSON schema.\n\n**open** _target_\n> Open a URL or file with the default handler.\n\n**--help**\n> Show usage for sunbeam or any subcommand."],["DESCRIPTION","**sunbeam** lets you define UIs composed of a succession of views from simple scripts written in any language. It is a hybrid between application launchers like Raycast and fuzzy-finders like fzf, rendering interactive lists, details, and forms from JSON emitted by your scripts.\n\nExtensions can be written in shell, Python, TypeScript, or any language that can print JSON to stdout. Sunbeam ships integrations for GitHub, Nix, Google Search, Hacker News, and Pipe Boards, and embeds cleanly inside tmux or zellij popups."],["HISTORY","**sunbeam** was created by **pomdtr** and is written in **Go**. It is distributed under the MIT license."],["SEE ALSO","[fzf](/man/fzf)(1), [rofi](/man/rofi)(1), [television](/man/television)(1)"]],"e":["fzf","rofi","television"],"x":"sunbeam"},"tlmgr-check":{"n":"tlmgr-check","t":"Verify TeX Live installation integrity","s":[["TAGLINE","Verify TeX Live installation integrity"],["TLDR","**Check all aspects of the installation**\n\n```tlmgr check all```\n\n**Check that all files listed in the database are present**\n\n```tlmgr check files```\n\n**Check for missing dependencies**\n\n```tlmgr check depends```\n\n**Check for conflicting run files between packages**\n\n```tlmgr check runfiles```\n\n**Check ls-R file consistency**\n\n```tlmgr check lsr```\n\n**Run checks with verbose output**\n\n```tlmgr -v check all```"],["SYNOPSIS","**tlmgr check** [_what_] [_options_]"],["PARAMETERS","**files**\n> Check that all files listed in the local TLPDB are actually present on disk.\n\n**depends**\n> List packages that are dependencies of an installed collection but are not themselves installed, and packages not contained in any collection.\n\n**executes**\n> Check that files referred to by execute directives in the TeX Live Database are present.\n\n**runfiles**\n> List filenames that occur more than once in the runfiles sections, except for known duplicates.\n\n**lsr**\n> Check consistency of the ls-R files. Useful after defining new trees or changing TEXMF variables.\n\n**all**\n> Run all of the above checks."],["DESCRIPTION","**tlmgr check** verifies the consistency and integrity of a TeX Live installation. If no problems are found, there is no output. Use `tlmgr -v check` for verbose output showing what is being checked. This action does not require write permissions and works the same in user mode and normal mode."],["SEE ALSO","[tlmgr](/man/tlmgr)(1), [tlmgr-install](/man/tlmgr-install)(1), [tlmgr-update](/man/tlmgr-update)(1)"]],"e":["tlmgr","tlmgr-install","tlmgr-update"],"x":"tlmgr check all"},"toybox":{"n":"toybox","t":"Multipurpose Unix command-line toolkit","s":[["TAGLINE","Multipurpose Unix command-line toolkit"],["TLDR","**List** all commands\n\n```toybox```\n\nRun **command** explicitly\n\n```toybox [command] [arguments]```\n\n**List** files\n\n```toybox ls```\n\n**Remove** a file\n\n```toybox rm [path/to/file]```\n\nDisplay **help** for command\n\n```toybox [command] --help```\n\nShow **version**\n\n```toybox --version```"],["SYNOPSIS","**toybox** [_COMMAND_ [_ARGS_...]]"],["PARAMETERS","**--help**\n> Display help for a command.\n\n**--version**\n> Display version information.\n\n**--long**\n> List commands with their suggested installation paths.\n\n**--install** _dir_\n> Create symlinks in _dir_ for each built-in command."],["DESCRIPTION","**toybox** is a multipurpose tool that combines many standard Unix utilities into a single executable. It provides lightweight implementations of common commands like ls, rm, cp, and many others.\n\nToybox is commonly used in Android and embedded Linux systems where minimizing binary size is important. Running toybox without arguments lists all available built-in commands."],["CAVEATS","Some commands may have fewer features than full GNU implementations. Command availability depends on build configuration. Primarily designed for embedded systems and Android."],["HISTORY","**toybox** was created by **Rob Landley** as a BSD-licensed alternative to BusyBox. It became the standard command-line toolbox in Android, replacing the older Toolbox implementation."],["INSTALL","```apt: sudo apt install toybox```\n\n```nix: nix profile install nixpkgs#toybox```"],["SEE ALSO","[busybox](/man/busybox)(1)"]],"e":["busybox"],"x":"toybox"},"tura":{"n":"tura","t":"Open-source agent runtime harness with TUI and CLI","s":[["TAGLINE","Open-source agent runtime harness with TUI and CLI"],["TLDR","**Open** the interactive terminal UI\n\n```tura```\n\n**Start** the TUI with an initial prompt\n\n```tura \"[Inspect this repository]\"```\n\n**Run** a non-interactive gateway-backed prompt\n\n```tura run \"[Fix the failing test and verify it]\"```\n\n**Run** a one-shot prompt via the direct Rust CLI\n\n```tura exec \"[Summarize the architecture]\"```\n\n**List** configured LLM providers\n\n```tura provider list```\n\n**List** sessions\n\n```tura session list --json```\n\n**Resume** the latest session with a follow-up\n\n```tura resume --last \"[Continue and verify the fix]\"```"],["SYNOPSIS","**tura** [_global_options_] [_prompt_ | _command_ [_args_...]]"],["PARAMETERS","**--gateway-url** _URL_\n> Use an explicit gateway instead of auto-starting or discovering one.\n\n**--cwd** _PATH_\n> Workspace directory sent to the gateway. Defaults to the current directory.\n\n**--initial-session** _ID_\n> Open the TUI on a specific session. Also read from **TURA_TUI_INITIAL_SESSION_ID**.\n\n**--json**\n> Request JSON output where the selected command supports it.\n\n**--verbose**\n> Print gateway request diagnostics to stderr.\n\n**--plain** / **--rich**\n> Force plain/safe terminal rendering or rich rendering.\n\n**--lang** **en**|**zh-CN**, **--language** **en**|**zh-CN**\n> Set CLI display language.\n\nWith no subcommand, **tura** opens the interactive TUI. If the first argument is not a known command, remaining text is treated as the initial TUI prompt."],["COMMANDS","**run** [_options_] _\"prompt\"_\n> Gateway-backed non-interactive prompt: create or reuse a session, stream until the turn completes, print the result. Options include **-m**/**--model**, **-a**/**--agent**, **--session**, **--output** text|json|ndjson, **--timeout**, **--stream**/**--no-stream**, **--bash**/**--zsh**/**--shel**.\n\n**exec** [_options_] _\"prompt\"_\n> Direct Rust CLI one-shot runner (**tura_exec**). Reads prompt from stdin if omitted. Options include **-C**/**--cwd**, **-m**/**--model**, **-a**/**--agent**, **--json**, **--quiet**, **--sandbox**, **--goal**, **--bash**/**--zsh**/**--shll**.\n\n**bash** | **zsh** | **shel** _\"prompt\"_\n> Aliases for **run** with the **command_run** shell surface forced.\n\n**session** **list**|**show**|**update**|**abort** ...\n> Inspect and modify gateway sessions. **list** supports **--all** and **--json**.\n\n**resume** [_SESSION_ID_ | **--last**] [_prompt_...]\n> Show a session, or append a follow-up prompt.\n\n**config** **get**|**set**|**model-tier** ...\n> Read or patch workspace session config and model tiers.\n\n**provider** **list**|**status**|**login**|**set-auth**|**logout** ...\n> List providers and manage local authentication (OAuth or API key). Credentials are not bundled; configure a provider before prompt commands.\n\n**agent** **list**|**show**|**create**|**update**|**delete**|**model** ...\n> Work with the gateway agent registry (default agent id often **balanced**).\n\n**persona** **list**|**show**|**create**|**update**|**delete** ...\n> Work with the gateway persona registry.\n\n**project** **current**|**list**|**create**|**default** ...\n> Inspect or create gateway workspaces.\n\n**file** **list**|**read**|**open**|**reveal** ...\n> Operate on files in the selected workspace via the gateway.\n\n**command** **list**|**run** ...\n> List or execute registered gateway commands (not arbitrary shell).\n\n**inspect** **status**|**path**|**sessions**|**messages** ...\n> Diagnostics for gateway and session state.\n\n**completion** **bash**|**zsh**|**fish**\n> Print shell completion snippets.\n\n**gateway** _METHOD_ _PATH_ [**-d** _JSON_]\n> Send one raw HTTP request through the CLI gateway client.\n\n**register-cli** | **unregister-cli** | **doctor-cli-path**\n> npm package-entry helpers to register or check the release binary on **PATH**.\n\nRelated binaries: **tura_gateway** (local HTTP/SSE gateway), **tura_gui** (desktop client), **tura_exec**, **tura_router**, **tura_session_db** (runtime plumbing)."],["DESCRIPTION","**tura** is an open-source agent runtime harness designed to complete long-horizon coding tasks with fewer model turns and tokens than typical tool-calling loops. Instead of exposing many fine-grained tools per step, it emphasizes a macro **command_run** tool so related shell, patch, build, and test actions can run in one structured turn, plus runtime context management tied to task state.\n\nThe primary entry point is the **tura** command: with no arguments it launches a terminal UI; **tura run** and **tura exec** provide non-interactive paths for scripts and CI-style prompts. A local gateway process coordinates sessions, agents, and providers. Install via npm (**tura-ai**) or from source with the project's install scripts; on first launch, configure an LLM provider and model before sending prompts.\n\nPublished benchmarks compare Tura agent configurations against other coding agents on long-horizon tasks; results and methodology are documented upstream. License is AGPL-3.0-or-later."],["CAVEATS","Requires a configured LLM provider; installation alone does not supply credentials. Behavior and available models depend on which providers you authenticate. Some subcommands need a running or auto-started gateway. Companion binaries (**tura_gateway**, **tura_gui**, router/session DB tools) are for service and desktop use, not everyday prompt entry. Spelling of shell surfaces differs slightly between TypeScript (**shel**) and Rust (**shll**) frontends."],["HISTORY","Developed by **Tura-AI** as an open-source agent harness with TUI, CLI, and optional GUI, emphasizing macro command execution and context control. Distributed on npm as **tura-ai** and on GitHub as **Tura-AI/tura**."],["SEE ALSO","[codex](/man/codex)(1), [aider](/man/aider)(1), [opencode](/man/opencode)(1), [npm](/man/npm)(1)"],["RESOURCES","```[Source code](https://github.com/Tura-AI/tura)```\n\n```[Homepage](https://turaai.net/)```\n\n```[Documentation](https://github.com/Tura-AI/tura/blob/main/docs/SUMMARY.md)```"]],"e":["codex","aider","opencode","npm"],"x":"tura"},"uv-tool":{"n":"uv-tool","t":"Install and run Python CLI tools","s":[["TAGLINE","Install and run Python CLI tools"],["TLDR","**Run a tool without installing**\n\n```uv tool run [tool]```\n\n**Run a tool from a specific package**\n\n```uv tool run --from [package] [command]```\n\n**Install a tool globally**\n\n```uv tool install [tool]```\n\n**Install a tool with extra dependencies**\n\n```uv tool install [tool] --with [extra_package]```\n\n**List installed tools**\n\n```uv tool list```\n\n**Upgrade a specific tool**\n\n```uv tool upgrade [tool]```\n\n**Upgrade all installed tools**\n\n```uv tool upgrade --all```\n\n**Uninstall a tool**\n\n```uv tool uninstall [tool]```"],["SYNOPSIS","**uv** **tool** _command_ [_options_]"],["PARAMETERS","**run** _tool_\n> Run a tool in a temporary isolated environment. Alias: **uvx**.\n\n**install** _tool_\n> Install a tool globally in an isolated virtual environment.\n\n**uninstall** _tool_\n> Remove an installed tool and its environment.\n\n**list**\n> List installed tools and their executables.\n\n**upgrade** _tool_\n> Upgrade an installed tool.\n\n**upgrade** **--all**\n> Upgrade all installed tools.\n\n**dir**\n> Show the tools installation directory.\n\n**update-shell**\n> Add the tool executable directory to the PATH in shell configuration files.\n\n**--from** _package_\n> Specify the package to install or run the command from.\n\n**--with** _package_\n> Include additional packages in the tool environment.\n\n**--python** _version_\n> Specify the Python interpreter or version to use."],["DESCRIPTION","**uv tool** manages Python CLI tools in isolated virtual environments. Each tool gets its own environment with dependencies, keeping the system clean. Similar to **pipx** but significantly faster due to uv's Rust-based resolver and installer.\n\nTools can be run temporarily with **uv tool run** (aliased as **uvx**), which creates a disposable environment, or installed persistently with **uv tool install** for permanent PATH availability. The **--from** option allows running a specific command from a named package, and **--with** adds extra dependencies to the tool environment. Tool upgrades respect version constraints set during installation."],["INSTALL","```dnf: sudo dnf install uv```\n\n```pacman: sudo pacman -S uv```\n\n```apk: sudo apk add uv```\n\n```brew: brew install uv```\n\n```nix: nix profile install nixpkgs#uv```"],["SEE ALSO","[uv](/man/uv)(1), [uv-run](/man/uv-run)(1), [uv-pip](/man/uv-pip)(1), [uvx](/man/uvx)(1), [pipx](/man/pipx)(1)"]],"e":["uv","uv-run","uv-pip","uvx","pipx"],"x":"uv tool run [tool]"},"vboxmanage-clonevm":{"n":"vboxmanage-clonevm","t":"Clone VirtualBox virtual machines","s":[["TAGLINE","Clone VirtualBox virtual machines"],["TLDR","**Clone VM**\n\n```vboxmanage clonevm [vmname] --name [newname]```\n\n**Clone with full clone**\n\n```vboxmanage clonevm [vmname] --mode all --name [newname]```\n\n**Clone and register**\n\n```vboxmanage clonevm [vmname] --name [newname] --register```"],["SYNOPSIS","**vboxmanage** **clonevm** _vmname_ [_options_]"],["PARAMETERS","**--name** _name_\n> Name for cloned VM.\n\n**--mode** _mode_\n> Clone mode (machine, machinechildren, all).\n\n**--register**\n> Register clone with VirtualBox.\n\n**--basefolder** _path_\n> Clone to specified folder.\n\n**--snapshot** _name_\n> Clone from snapshot."],["DESCRIPTION","**vboxmanage clonevm** creates a copy of an existing virtual machine. Supports full clones (independent) and linked clones (sharing base disk). Useful for creating test environments from templates."],["SEE ALSO","[vboxmanage](/man/vboxmanage)(1), [vboxmanage-createvm](/man/vboxmanage-createvm)(1)"]],"e":["vboxmanage","vboxmanage-createvm"],"x":"vboxmanage clonevm [vmname] --name [newname]"},"vite":{"n":"vite","t":"Next-generation frontend build tool","s":[["TAGLINE","Next-generation frontend build tool"],["TLDR","**Start the development server**\n\n```vite```\n\n**Start dev server on a specific port**\n\n```vite --port [3000]```\n\n**Build for production**\n\n```vite build```\n\n**Preview production build** locally\n\n```vite preview```\n\n**Use a specific config file**\n\n```vite --config [vite.config.js]```\n\n**Build with a specific base path**\n\n```vite build --base [/my/public/path/]```\n\n**Force dependency pre-bundling**\n\n```vite --force```\n\n**Build in specific mode**\n\n```vite build --mode [production]```"],["SYNOPSIS","**vite** [_command_] [_options_]"],["PARAMETERS","**dev**, **serve**\n> Start development server (default command).\n\n**build**\n> Build for production.\n\n**preview**\n> Locally preview production build.\n\n**optimize**\n> Pre-bundle dependencies.\n\n**-c**, **--config** _file_\n> Use specified config file.\n\n**--base** _path_\n> Public base path for the build.\n\n**--mode** _mode_\n> Set env mode (development, production).\n\n**--port** _port_\n> Port number for dev server.\n\n**--host** _host_\n> Hostname to listen on.\n\n**--open** [_path_]\n> Open browser on server start.\n\n**--force**\n> Force dependency pre-bundling.\n\n**--clearScreen**\n> Clear terminal screen on start.\n\n**-l**, **--logLevel** _level_\n> Log level: info, warn, error, silent.\n\n**-d**, **--debug** [_filter_]\n> Enable debug mode with optional filter.\n\n**-h**, **--help**\n> Display help message.\n\n**-v**, **--version**\n> Display version."],["DESCRIPTION","**Vite** is a next-generation frontend build tool that provides an extremely fast development experience. It leverages native ES modules in the browser during development, eliminating the bundling step for faster hot module replacement (HMR).\n\nFor production, Vite bundles code using Rollup, producing optimized static assets. It supports TypeScript, JSX, CSS preprocessing, and various frameworks (Vue, React, Svelte) out of the box.\n\nConfiguration is typically stored in **vite.config.js** or **vite.config.ts**. Vite requires Node.js 20.19+ or 22.12+."],["CAVEATS","The preview server is not designed for production use; deploy the built files with a proper static file server. Some Node.js APIs are not available in the browser during development. Legacy browser support requires the @vitejs/plugin-legacy plugin."],["HISTORY","Vite was created by **Evan You** (creator of Vue.js) and first released in **April 2020**. The name means \"fast\" in French. Vite 2.0, released in **February 2021**, was a major rewrite that made it framework-agnostic. It has rapidly gained adoption as an alternative to webpack, influencing the broader JavaScript tooling ecosystem."],["INSTALL","```apt: sudo apt install vite```\n\n```brew: brew install vite```\n\n```nix: nix profile install nixpkgs#vite```"],["SEE ALSO","[npm](/man/npm)(1), [webpack](/man/webpack)(1), [rollup](/man/rollup)(1), [esbuild](/man/esbuild)(1)"]],"e":["npm","webpack","rollup","esbuild"],"x":"vite"},"wikiman":{"n":"wikiman","t":"Offline documentation search engine","s":[["TAGLINE","Offline documentation search engine"],["TLDR","**Search** documentation across all sources\n\n```wikiman [query]```\n\n**Search** only specific sources (comma-separated)\n\n```wikiman -s [man,arch] [query]```\n\n**List** available sources\n\n```wikiman -S```\n\n**Search** with quick search mode\n\n```wikiman -q [query]```\n\n**Search** using AND operator (all keywords must match)\n\n```wikiman -a [query]```\n\n**Print** raw output without interactive selection\n\n```wikiman -R [query]```\n\n**Open** results in HTML viewer instead of pager\n\n```wikiman -H [w3m] [query]```"],["SYNOPSIS","**wikiman** [_options_] [_keyword..._]"],["PARAMETERS","**-s** _SOURCES_\n> Comma-separated list of sources to search (default: all available).\n\n**-l** _LANGUAGES_\n> Comma-separated search languages (default: en).\n\n**-f** _FINDER_\n> Fuzzy finder to use (default: fzf).\n\n**-H** _VIEWER_\n> HTML viewer for wiki pages (default: w3m).\n\n**-q**\n> Enable quick search mode.\n\n**-a**\n> Enable AND operator mode (all keywords must match).\n\n**-p**\n> Disable quick result preview.\n\n**-k**\n> Keep wikiman open after viewing a result.\n\n**-c**\n> Show source column in results.\n\n**-R**\n> Print raw output without interactive selection.\n\n**-S**\n> List available sources and exit.\n\n**-W** _SHELL_\n> Print widget code for the specified shell (bash, zsh, fish) and exit.\n\n**-v**\n> Print version and exit.\n\n**-h**\n> Display help and exit."],["SOURCES","**man** - Man pages\n**arch** - Arch Wiki\n**gentoo** - Gentoo Wiki\n**tldr** - TLDR pages\n**fbsd** - FreeBSD documentation\n**devdocs** - DevDocs"],["DESCRIPTION","**wikiman** is an offline documentation search engine that aggregates multiple sources including man pages, Arch Wiki, Gentoo Wiki, TLDR pages, FreeBSD documentation, and DevDocs into a single searchable interface. Queries are matched across all configured sources simultaneously, with results ranked by relevance using fuzzy matching.\n\nSource filtering allows narrowing searches to specific documentation types, such as only wiki articles or only man pages. Results can be viewed in a terminal pager or opened in a browser with full HTML formatting preserved for wiki content.\n\nAll documentation is stored locally after an initial sync, enabling fast searches without network access. If no keywords are provided, all pages are shown."],["CONFIGURATION","**~/.config/wikiman/wikiman.conf**\n> User configuration file. Falls back to /etc/wikiman.conf if not present. Respects XDG_CONFIG_HOME.\n\nShell key binding (default Ctrl+F) can be enabled by sourcing widget files from /usr/share/wikiman/widgets/ for bash, zsh, or fish."],["CAVEATS","Initial source sync requires network access and may download significant data depending on which sources are enabled. Requires fzf or another fuzzy finder for interactive selection."],["INSTALL","```pacman: sudo pacman -S wikiman```\n\n```nix: nix profile install nixpkgs#wikiman```"],["SEE ALSO","[man](/man/man)(1), [tldr](/man/tldr)(1), [apropos](/man/apropos)(1), [fzf](/man/fzf)(1), [w3m](/man/w3m)(1)"]],"e":["man","tldr","apropos","fzf","w3m"],"x":"wikiman [query]"},"wpa_passphrase":{"n":"wpa_passphrase","t":"Generate WPA pre-shared key from passphrase","s":[["TAGLINE","Generate WPA pre-shared key from passphrase"],["TLDR","**Compute** WPA-PSK key reading passphrase from stdin\n\n```wpa_passphrase [SSID]```\n\n**Compute** WPA-PSK key with passphrase as argument\n\n```wpa_passphrase [SSID] [passphrase]```\n\n**Generate and append** to wpa_supplicant configuration\n\n```wpa_passphrase [SSID] [passphrase] >> /etc/wpa_supplicant/wpa_supplicant.conf```"],["SYNOPSIS","**wpa_passphrase** _ssid_ [_passphrase_]"],["PARAMETERS","**ssid**\n> The network name (SSID) to generate the key for\n\n**passphrase**\n> The WiFi password, must be 8 to 63 printable ASCII characters (read from stdin if omitted)"],["DESCRIPTION","**wpa_passphrase** generates a WPA-PSK (Pre-Shared Key) configuration from an ASCII passphrase for a given network SSID. The output is in wpa_supplicant.conf format and can be appended to the configuration file.\n\nThe tool pre-computes the PSK using PBKDF2, which is faster than having wpa_supplicant compute it each time. The output includes both the plain text passphrase (commented) and the computed PSK."],["CAVEATS","Providing the passphrase on the command line exposes it in process listings and shell history. For security, prefer reading from stdin. The passphrase must be between 8 and 63 characters. The SSID is case-sensitive and must match exactly. Part of the wpa_supplicant package."],["INSTALL","```apt: sudo apt install wpasupplicant```\n\n```dnf: sudo dnf install wpa_supplicant```\n\n```pacman: sudo pacman -S wpa_supplicant```\n\n```apk: sudo apk add wpa_supplicant```\n\n```zypper: sudo zypper install wpa_supplicant```\n\n```nix: nix profile install nixpkgs#wpa_supplicant```"],["SEE ALSO","[wpa_supplicant](/man/wpa_supplicant)(8), [wpa_cli](/man/wpa_cli)(8)"]],"e":["wpa_supplicant","wpa_cli"],"x":"wpa_passphrase [SSID]"},"xauth":{"n":"xauth","t":"X11 authorization credential management","s":[["TAGLINE","X11 authorization credential management"],["TLDR","**List all authorization entries**\n\n```xauth list```\n\n**Display authority file info**\n\n```xauth info```\n\n**Add an authorization entry**\n\n```xauth add [display_name] [protocol_name] [hex_key]```\n\n**Generate a new authorization entry via the server**\n\n```xauth generate [display_name] [protocol_name]```\n\n**Remove an authorization entry**\n\n```xauth remove [display_name]```\n\n**Extract entry for the current display to a file**\n\n```xauth extract [path/to/file] $DISPLAY```\n\n**Merge entries from a file**\n\n```xauth merge [path/to/file]```\n\n**Use a specific authority file**\n\n```xauth -f [path/to/file] list```"],["SYNOPSIS","**xauth** [_options_] [_command_ [_args_]]"],["COMMANDS","**add** _DISPLAY_ _PROTOCOL_ _HEXKEY_\n> Add an authorization entry for the indicated display.\n\n**generate** _DISPLAY_ _PROTOCOL_ [_options_]\n> Contact the server to generate and add an authorization entry.\n\n**remove** _DISPLAY_\n> Remove matching authorization entries.\n\n**list** [_DISPLAY_]\n> List authorization entries (all if no display specified).\n\n**nlist** [_DISPLAY_]\n> List entries in numeric format.\n\n**extract** _FILE_ _DISPLAY_\n> Write authorization entries to a file.\n\n**nextract** _FILE_ _DISPLAY_\n> Extract entries in numeric format for non-binary transmission.\n\n**merge** _FILE_...\n> Read and merge authorization entries from files.\n\n**nmerge** _FILE_...\n> Merge entries in numeric format.\n\n**source** _FILE_\n> Execute xauth commands from a script file.\n\n**info**\n> Display information about the authority file.\n\n**exit**\n> Save changes and exit.\n\n**quit**\n> Exit without saving changes.\n\n**help**\n> List available commands."],["PARAMETERS","**-f** _FILE_\n> Use the specified authority file instead of ~/.Xauthority.\n\n**-v**\n> Verbose mode. Print status messages for operations.\n\n**-q**\n> Quiet mode. Suppress unsolicited status messages.\n\n**-i**\n> Ignore authority file locks.\n\n**-b**\n> Attempt to break stale authority file locks.\n\n**-n**\n> Do not resolve hostnames; display stored addresses directly.\n\n**-V**\n> Display the xauth version number."],["DESCRIPTION","**xauth** edits and displays the authorization information used for connecting to X servers. It manages the ~/.Xauthority file (or the file specified by the XAUTHORITY environment variable) which contains credentials for X11 authentication.\n\nThe tool is commonly used to extract authorization records from one machine and merge them on another for remote display access, X11 forwarding over SSH, and managing X security."],["CAVEATS","Authority files contain sensitive credentials and should have restrictive permissions (readable only by the owner). X11 forwarding with SSH requires proper xauth setup. The program does not contact the X server except when the `generate` command is used."],["INSTALL","```apt: sudo apt install xauth```\n\n```apk: sudo apk add xauth```\n\n```zypper: sudo zypper install xauth```\n\n```brew: brew install xauth```\n\n```nix: nix profile install nixpkgs#xauth```"],["SEE ALSO","[xhost](/man/xhost)(1), [ssh](/man/ssh)(1), [xdpyinfo](/man/xdpyinfo)(1)"]],"e":["xhost","ssh","xdpyinfo"],"x":"xauth list"},"xe":{"n":"xe","t":"Simple xargs and apply replacement","s":[["TAGLINE","Simple xargs and apply replacement"],["TLDR","**Run command for each line of stdin**\n\n```cat [list.txt] | xe [command]```\n\n**Use {} as placeholder for the argument**\n\n```cat [list.txt] | xe [command] {}```\n\n**Parallel execution with N jobs**\n\n```cat [urls.txt] | xe -j [4] curl {}```\n\n**Pass arguments directly after --**\n\n```xe -a -- mv {} {}.bak -- [file1] [file2]```\n\n**Null-separated input (for filenames with spaces)**\n\n```find . -print0 | xe -0 echo {}```\n\n**Pass up to N arguments per command**\n\n```xe -N [10] rm -- [*.txt]```\n\n**Dry-run: print commands without executing**\n\n```cat list.txt | xe -n [command] {}```"],["SYNOPSIS","**xe** [_-0FLnqvR_] [_-I replace-arg_] [_-N maxargs_] [_-j maxjobs_] [_-f argfile_] [_-s shellscript_] [_-a_] [_command_ [_args...]]"],["DESCRIPTION","**xe** is a tool for constructing command lines from file listings or arguments, combining the best features of **xargs**(1) and **apply**(1). By default it runs the given command once per input line, substituting **{}** with the argument, making common use cases simpler than **xargs**.\n\nUnlike xargs, xe has sane defaults: it is equivalent to `xargs -d'\\n' -I{} -n1 -r`. Arguments can be read from stdin (default), from a file with **-f**, or directly from the command line after **-a**.\n\n**xe** supports parallel execution with **-j**, make-style percent rule matching with **-p**, and can invoke a shell script per argument with **-s**."],["PARAMETERS","**-0**\n> Input arguments are separated by NUL bytes instead of newlines. Useful with `find -print0`.\n\n**-a**\n> Take arguments from the command line (after the command, separated by **--**) instead of stdin.\n\n**-A** _argsep_\n> Use a custom argument separator instead of **--** (implies **-a**).\n\n**-f** _argfile_\n> Read arguments from _argfile_ instead of stdin.\n\n**-F**\n> Fatal: stop and exit when a command execution fails.\n\n**-I** _replace-arg_\n> Set the replacement string (default: **{}**).\n\n**-j** _maxjobs_\n> Run up to _maxjobs_ processes concurrently.\n\n**-L**\n> Line-buffered output so lines from concurrent jobs do not interleave.\n\n**-N** _maxargs_\n> Pass up to _maxargs_ arguments to each command invocation (default: **1**).\n\n**-n**\n> Dry-run: print the commands that would be executed without running them.\n\n**-p**\n> Make-style percent rule matching for pattern-dispatched commands.\n\n**-q**\n> Quiet: redirect stdout/stderr of commands to /dev/null.\n\n**-R**\n> Return exit status 122 when no arguments are supplied (instead of 0).\n\n**-s** _shellscript_\n> Execute _shellscript_ via the shell; arguments are available as $1, $2, etc.\n\n**-v**\n> Verbose: print commands to stderr before running them."],["CAVEATS","**xe** is distinct from the XenServer **xe** CLI and from xargs despite similar goals. If xargs compatibility is required, flags and behavior may differ (for example xe uses **{}** as placeholder by default and always runs one command per argument unless **-N** is given)."],["HISTORY","**xe** was written by **Leah Neukirchen** as a modernized replacement for **xargs**(1) and **apply**(1) with saner defaults and a cleaner interface. It is distributed as a small, portable C program."],["INSTALL","```apt: sudo apt install xe```\n\n```brew: brew install xe```\n\n```nix: nix profile install nixpkgs#xe```"],["SEE ALSO","[xargs](/man/xargs)(1), [parallel](/man/parallel)(1), [find](/man/find)(1)"]],"e":["xargs","parallel","find"],"x":"cat [list.txt] | xe [command]"},"xzdiff":{"n":"xzdiff","t":"Show differences in xz compressed files","s":[["TAGLINE","Show differences in xz compressed files"],["TLDR","**Show differences in xz files**\n\n```xzdiff [file1.xz] [file2.xz]```\n\n**Compare compressed with original**\n\n```xzdiff [file.xz] [file]```"],["SYNOPSIS","**xzdiff** [_diff_options_] _file1_ [_file2_]"],["DESCRIPTION","**xzdiff** shows differences between xz compressed files. Decompresses and runs diff on the contents. Supports all diff options. Wrapper around diff for xz files."],["INSTALL","```apt: sudo apt install xz-utils```\n\n```dnf: sudo dnf install xz```\n\n```pacman: sudo pacman -S xz```\n\n```apk: sudo apk add xz```\n\n```zypper: sudo zypper install xz```\n\n```brew: brew install xz```\n\n```nix: nix profile install nixpkgs#xz```"],["SEE ALSO","[xz](/man/xz)(1), [xzcmp](/man/xzcmp)(1), [diff](/man/diff)(1)"]],"e":["xz","xzcmp","diff"],"x":"xzdiff [file1.xz] [file2.xz]"},"yelp":{"n":"yelp","t":"GNOME desktop help browser","s":[["TAGLINE","GNOME desktop help browser"],["TLDR","**Open help browser**\n\n```yelp```\n\n**Open specific help document**\n\n```yelp [help:gnome-terminal]```\n\n**Open man page**\n\n```yelp [man:ls]```\n\n**Open info page**\n\n```yelp [info:bash]```\n\n**Open local file**\n\n```yelp [file:///usr/share/doc/package/manual.html]```\n\n**Open Mallard documentation**\n\n```yelp [ghelp:gedit]```"],["SYNOPSIS","**yelp** [_options_] [_uri_]"],["PARAMETERS","**--help**\n> Display help options.\n\n**--version**\n> Display version."],["URI SCHEMES","**help:**: GNOME help documentation.\n\n**ghelp:**: Legacy GNOME help (DocBook).\n\n**man:**: System manual pages.\n\n**info:**: GNU info pages.\n\n**file://**: Local HTML files."],["DESCRIPTION","**yelp** is the GNOME help browser, providing a unified interface for viewing various documentation formats. It supports Mallard, DocBook, man pages, info pages, and HTML documentation.\n\nThe application integrates with GNOME applications; pressing F1 in most GNOME apps launches yelp with the relevant help. It features interactive search, bookmarks, and navigation history.\n\nYelp uses XSLT to render XML documentation as HTML. The freedesktop.org help system specification ensures consistent help access across GNOME applications."],["CAVEATS","GNOME-centric; other desktops may have different help systems. Some documentation requires specific packages installed. Search indexes may need rebuilding after updates."],["HISTORY","**yelp** was created as the standard help browser for GNOME. The name was suggested by Daniel Lundin. It has evolved alongside GNOME, supporting the transition from DocBook to Mallard as the primary documentation format."],["INSTALL","```apt: sudo apt install yelp```\n\n```dnf: sudo dnf install yelp```\n\n```pacman: sudo pacman -S yelp```\n\n```apk: sudo apk add yelp```\n\n```zypper: sudo zypper install yelp```\n\n```nix: nix profile install nixpkgs#yelp```"],["SEE ALSO","[man](/man/man)(1), [info](/man/info)(1)"]],"e":["man","info"],"x":"yelp"}});
