window.LCL_BUCKET(89,{"acme.sh-dns":{"n":"acme.sh-dns","t":"DNS-based SSL certificate validation","s":[["TAGLINE","DNS-based SSL certificate validation"],["TLDR","**Issue** certificate using Cloudflare DNS\n\n```export CF_Token=\"[token]\" && acme.sh --issue -d [example.com] --dns dns_cf```\n\nIssue using **AWS Route53**\n\n```export AWS_ACCESS_KEY_ID=\"[key]\" && export AWS_SECRET_ACCESS_KEY=\"[secret]\" && acme.sh --issue -d [example.com] --dns dns_aws```\n\nIssue using **DigitalOcean** DNS\n\n```export DO_API_KEY=\"[token]\" && acme.sh --issue -d [example.com] --dns dns_dgon```\n\nIssue **wildcard** certificate with DNS\n\n```acme.sh --issue -d [example.com] -d [*.example.com] --dns dns_cf```"],["SYNOPSIS","**acme.sh** --issue -d _domain_ --dns _dns_provider_ [_options_]"],["DESCRIPTION","**acme.sh** DNS mode enables automated certificate issuance using DNS-01 validation. This method proves domain ownership by creating a specific TXT record in the domain's DNS zone. It's required for wildcard certificates and useful when HTTP validation isn't possible.\n\nThe tool supports over 100 DNS providers through API integration. Credentials are typically provided via environment variables, and acme.sh handles creating and removing the validation records automatically."],["PARAMETERS","**--dns** _provider_\n> DNS provider plugin name (dns_cf, dns_aws, dns_gd, dns_ali, etc.)\n\n**--dnssleep** _seconds_\n> Seconds to wait for DNS propagation (default: automatic)\n\n**--dns-alias** _domain_\n> Use alias domain for DNS validation (CNAME delegation)\n\n**-d** _domain_\n> Domain name (repeat for wildcards: -d example.com -d *.example.com)"],["COMMON DNS PROVIDERS","**dns_cf**\n> Cloudflare (CF_Token or CF_Key + CF_Email)\n\n**dns_aws**\n> AWS Route53 (AWS_ACCESS_KEY_ID + AWS_SECRET_ACCESS_KEY)\n\n**dns_gd**\n> GoDaddy (GD_Key + GD_Secret)\n\n**dns_dgon**\n> DigitalOcean (DO_API_KEY)\n\n**dns_ali**\n> Aliyun/Alibaba Cloud (Ali_Key + Ali_Secret)\n\n**dns_gcloud**\n> Google Cloud DNS (CLOUDSDK_ACTIVE_CONFIG_NAME)\n\n**dns_linode**\n> Linode (LINODE_V4_API_KEY)\n\n**dns_ovh**\n> OVH (OVH_AK + OVH_AS + OVH_CK)"],["CONFIGURATION","**~/.acme.sh/account.conf**\n> Stores DNS provider API credentials (tokens, keys, secrets) after first use. These are reused automatically during certificate renewal."],["CAVEATS","API credentials are stored in account.conf for automatic renewal. DNS propagation can take time; increase --dnssleep if validation fails. Some providers have rate limits on API calls. API token permissions should be limited to DNS management only."],["HISTORY","DNS validation support was added to **acme.sh** in **2016** following the ACME protocol specification. The number of supported DNS providers has grown substantially, with community contributions adding new providers regularly."],["INSTALL","```pacman: sudo pacman -S acme.sh```\n\n```apk: sudo apk add acme.sh```\n\n```brew: brew install acme.sh```"],["SEE ALSO","[acme.sh](/man/acme.sh)(1), [certbot](/man/certbot)(1), [dig](/man/dig)(1), [nslookup](/man/nslookup)(1)"],["RESOURCES","```[Source code](https://github.com/acmesh-official/acme.sh)```\n\n```[Documentation](https://github.com/acmesh-official/acme.sh/wiki/dnsapi)```"]],"e":["acme.sh","certbot","dig","nslookup"],"x":"export CF_Token=\"[token]\" && acme.sh --issue -d [example.com] --dns dns_cf"},"archinstall":{"n":"archinstall","t":"Guided Arch Linux installer with interactive menus","s":[["TAGLINE","Guided Arch Linux installer with interactive menus"],["TLDR","Start the **interactive installer**\n\n```archinstall```\n\nRun a specific **installation profile**\n\n```archinstall --script [minimal]```\n\nRun with a **configuration file**\n\n```archinstall --config [/path/to/config.json]```\n\nRun in **silent mode** with predefined config\n\n```archinstall --config [/path/to/config.json] --silent```"],["SYNOPSIS","**archinstall** [_options_]"],["DESCRIPTION","**archinstall** is a guided Arch Linux installer that provides an interactive menu-driven installation experience. It simplifies the Arch Linux installation process while still providing flexibility and customization options."],["PARAMETERS","**--config** _path_\n> Load configuration from a JSON file.\n\n**--creds** _path_\n> Load credentials (passwords, keys) from a file.\n\n**--disk-layout** _path_\n> Load disk layout configuration from a file.\n\n**--script** _name_\n> Run a specific installation profile (e.g., minimal, desktop).\n\n**--silent**\n> Run without user prompts (requires --config).\n\n**--dry-run**\n> Test the installation without making changes."],["CAVEATS","Requires network connectivity. Should only be run from the Arch Linux live installation environment. Creates partitions and formats disks; data loss may occur."],["HISTORY","Developed by the **Arch Linux** community and officially included in the installation ISO since **2021** as an alternative to the traditional manual installation process."],["INSTALL","```pacman: sudo pacman -S archinstall```"],["SEE ALSO","[pacman](/man/pacman)(8), [arch-chroot](/man/arch-chroot)(8), [pacstrap](/man/pacstrap)(8), [genfstab](/man/genfstab)(8)"]],"e":["pacman","arch-chroot","pacstrap","genfstab"],"x":"archinstall"},"audacity":{"n":"audacity","t":"Open-source graphical audio editor for recording and editing sounds.","s":[["TAGLINE","Open-source graphical audio editor for recording and editing sounds."],["TLDR","**Launch Audacity** graphical interface\n\n```audacity```\n\n**Open an audio file** for editing\n\n```audacity [path/to/audio.wav]```\n\n**Open multiple audio files**\n\n```audacity [file1.wav] [file2.mp3] [file3.ogg]```\n\n**Display version** information\n\n```audacity -version```\n\n**Display help** for command line options\n\n```audacity -help```\n\n**Set block size** for disk writes\n\n```audacity -blocksize [4096] [path/to/audio.wav]```"],["SYNOPSIS","**audacity** [**-help**]\n\n**audacity** [**-version**]\n\n**audacity** [**-blocksize** _nnn_] [_AUDIO-FILE_] ..."],["PARAMETERS","**-help**\n> Display a brief list of command line options.\n\n**-version**\n> Display the Audacity version number.\n\n**-blocksize** _nnn_\n> Set the Audacity block size for writing files to disk to nnn bytes.\n\n**-test**\n> Run self-diagnostic tests (only present in development builds)."],["DESCRIPTION","**Audacity** is a free, open-source graphical audio editor for recording and editing sounds. It supports multiple audio formats including uncompressed formats (WAV, AIFF, AU) and compressed formats (MP3, Ogg Vorbis, FLAC) through external libraries.\n\nAudacity provides multi-track editing, recording from various sources, effects processing, and format conversion. It supports LADSPA and Nyquist plugins for extended functionality. The editor is disk-based, meaning audio data is stored in a temporary directory during editing rather than entirely in memory.\n\nAudacity is primarily an interactive graphical application. For batch processing or command-line audio manipulation, tools like sox or ecasound are more suitable."],["FILES","**~/.audacity-data/audacity.cfg**\n> Per-user configuration file (Linux). On newer versions, may be at **~/.config/audacity/audacity.cfg**.\n\n**/var/tmp/audacity-_user_/**\n> Default location of the temporary directory. Should be on a fast local disk with ample free space."],["ENVIRONMENT","**AUDACITY_PATH**\n> Directories searched before standard locations for plugins and configuration files.\n\n**LADSPA_PATH**\n> Additional directories searched for LADSPA plugins."],["CAVEATS","Audacity has limited command line functionality and is not designed for batch processing. The temporary directory requires significant disk space for large projects. Some audio formats (MP3, FFmpeg formats) require additional libraries to be installed."],["HISTORY","Audacity was started in **1999** by Dominic Mazzoni and Roger Dannenberg at Carnegie Mellon University. It became one of the most popular open-source audio editors, available across Linux, macOS, and Windows. In **2021**, the project was acquired by Muse Group, which led to community concerns about telemetry and privacy. The project continues development with regular releases."],["INSTALL","```apt: sudo apt install audacity```\n\n```dnf: sudo dnf install audacity```\n\n```pacman: sudo pacman -S audacity```\n\n```apk: sudo apk add audacity```\n\n```zypper: sudo zypper install audacity```\n\n```nix: nix profile install nixpkgs#audacity```"],["SEE ALSO","[sox](/man/sox)(1), [ardour](/man/ardour)(1), [lame](/man/lame)(1)"]],"e":["sox","ardour","lame"],"x":"audacity"},"aur-sync":{"n":"aur-sync","t":"Download, build, and add AUR packages to a local pacman repository","s":[["TAGLINE","Download, build, and add AUR packages to a local pacman repository"],["TLDR","**Download and build** packages from the AUR\n\n```aur sync [package1] [package2] ...```\n\n**Upgrade** all AUR packages in the local repository\n\n```aur sync -u```\n\n**Build in a clean chroot**\n\n```aur sync -c [package]```\n\n**Sync without viewing** PKGBUILD and without confirmation\n\n```aur sync --noview --noconfirm [package]```\n\nUpgrade but **ignore** specific packages\n\n```aur sync -u --ignore [package1] --ignore [package2]```\n\n**Rebuild** a package regardless of version\n\n```aur sync -f [package]```"],["SYNOPSIS","**aur sync** [_options_] _package_..."],["DESCRIPTION","**aur sync** is the primary command in **aurutils** for downloading, building, and adding AUR packages to a local pacman repository. It resolves AUR dependencies, fetches PKGBUILDs, builds packages, and updates the local repository database.\n\nBy default, it opens PKGBUILDs for review using the file manager specified by **AUR_PAGER** (or **vifm**) before building. This behavior can be disabled with **--noview** for automated workflows."],["PARAMETERS","**-u**, **--upgrades**\n> Check for and build upgrades to installed AUR packages.\n\n**-c**, **--chroot**\n> Build packages in a clean chroot using aur-chroot.\n\n**-f**, **--force**\n> Force rebuild regardless of version.\n\n**-n**, **--noconfirm**\n> Do not prompt for confirmation.\n\n**--noview**\n> Skip PKGBUILD review.\n\n**--ignore** _package_\n> Skip a package during upgrades (repeatable).\n\n**-d**, **--database** _name_\n> Use specified local repository database.\n\n**--no-ver**\n> Disable version checking (rebuild all).\n\n**--no-ver-argv**\n> Disable version checking for command-line arguments only.\n\n**--provides**\n> Consider provides when resolving dependencies.\n\n**-S**, **--sign**\n> Sign built packages with GPG.\n\n**-k**, **--keep-going**\n> Continue building remaining packages if one fails.\n\n**--makepkg-args** _args_\n> Pass additional arguments to makepkg."],["CAVEATS","Requires a properly configured local pacman repository and **devtools** for chroot builds. PKGBUILD review is critical for security; use **--noview** cautiously since AUR packages are user-submitted and not officially vetted. The **--ignore** flag takes one package per flag (not comma-separated)."],["SEE ALSO","[aur](/man/aur)(1), [pacman](/man/pacman)(8), [makepkg](/man/makepkg)(8)"]],"e":["aur","pacman","makepkg"],"x":"aur sync [package1] [package2] ..."},"aws-kendra":{"n":"aws-kendra","t":"Manage ML-powered enterprise search indexes and data sources.","s":[["TAGLINE","Manage ML-powered enterprise search indexes and data sources."],["TLDR","**Create a new Kendra index**\n\n```aws kendra create-index --name [my-index] --role-arn [arn:aws:iam::account:role/kendra-role]```\n\n**List all indexes** in the account\n\n```aws kendra list-indices```\n\n**Submit a search query**\n\n```aws kendra query --index-id [index-id] --query-text \"[search terms]\"```\n\n**Add documents** to an index\n\n```aws kendra batch-put-document --index-id [index-id] --documents file://[documents.json]```\n\n**Create a data source** connector\n\n```aws kendra create-data-source --index-id [index-id] --name [s3-source] --type S3 --configuration file://[s3-config.json] --role-arn [arn:aws:iam::account:role/kendra-role]```\n\n**Start a data source sync**\n\n```aws kendra start-data-source-sync-job --index-id [index-id] --id [data-source-id]```\n\n**Create a FAQ** for direct answers\n\n```aws kendra create-faq --index-id [index-id] --name [my-faq] --s3-path Bucket=[bucket],Key=[faq.csv] --role-arn [arn:aws:iam::account:role/kendra-role]```"],["SYNOPSIS","**aws kendra** _command_ [_options_]"],["DESCRIPTION","**aws kendra** is the AWS CLI interface for Amazon Kendra, an intelligent enterprise search service powered by machine learning. Kendra provides natural language search capabilities across documents, FAQs, and other content sources.\n\nKendra indexes documents from various data sources including S3, SharePoint, Salesforce, ServiceNow, and databases. It understands natural language queries and returns relevant passages with highlighted answers, supporting semantic search beyond keyword matching."],["COMMANDS","**create-index**\n> Create a new search index\n\n**delete-index**\n> Delete an index\n\n**list-indices**\n> List all indexes\n\n**query**\n> Submit a search query\n\n**batch-put-document**\n> Add documents to an index\n\n**batch-delete-document**\n> Remove documents from an index\n\n**create-data-source**\n> Create a connector to external content\n\n**start-data-source-sync-job**\n> Trigger document synchronization\n\n**create-faq**\n> Add FAQ content for direct answers\n\n**get-query-suggestions**\n> Get autocomplete suggestions"],["CAVEATS","Index creation takes 15-30 minutes. Document ingestion costs apply per document scanned. Query capacity units (QCUs) determine concurrent query limits. Large document processing can be slow; use data sources for bulk content. Enterprise edition required for advanced features."],["HISTORY","Amazon Kendra launched in **December 2019** as an ML-powered enterprise search service. Query suggestions were added in **2021**, and featured results for promoted content in **2022**. The service has expanded data source connectors to support over 14 content repositories."],["INSTALL","```dnf: sudo dnf install aws```\n\n```pacman: sudo pacman -S aws-cli```\n\n```apk: sudo apk add aws-cli```\n\n```zypper: sudo zypper install aws-cli```"],["SEE ALSO","[aws](/man/aws)(1), [aws-opensearch](/man/aws-opensearch)(1)"],["RESOURCES","```[Source code](https://github.com/aws/aws-cli)```\n\n```[Homepage](https://aws.amazon.com/cli/)```\n\n```[Documentation](https://docs.aws.amazon.com/cli/latest/reference/kendra/index.html)```"]],"e":["aws","aws-opensearch"],"x":"aws kendra create-index --name [my-index] --role-arn [arn:aws:iam::account:role/kendra-role]"},"bdfr":{"n":"bdfr","t":"Bulk download submissions and media from Reddit","s":[["TAGLINE","Bulk download submissions and media from Reddit"],["TLDR","**Download posts from a subreddit**\n\n```bdfr download [./output] --subreddit [Python] -L [10]```\n\n**Download a user's submitted posts**\n\n```bdfr download [./output] --user [username] --submitted -L [100]```\n\n**Download your saved posts** (requires authentication)\n\n```bdfr download [./output] --user me --saved --authenticate -L [25]```\n\n**Archive submission data** with comments\n\n```bdfr archive [./output] --user [username] --submitted --all-comments```\n\n**Clone posts and data** (download + archive)\n\n```bdfr clone [./output] --subreddit [Python] -L [10]```\n\n**Download from multiple subreddits**\n\n```bdfr download [./output] --subreddit \"[Python, programming, linux]\" -L [10]```\n\n**Search and download** matching posts\n\n```bdfr download [./output] --subreddit [Python] --search \"[machine learning]\" -L [20]```"],["SYNOPSIS","**bdfr** _mode_ _output_path_ [_options_]"],["DESCRIPTION","**bdfr** (Bulk Downloader for Reddit) is a tool to download submissions, media, and data from Reddit. It can archive posts for research, backup saved content, or download media from subreddits.\n\nThe tool has three modes: **download** (downloads linked media like images and videos), **archive** (saves submission metadata, text, and comments in JSON/YAML/XML), and **clone** (performs both operations efficiently)."],["SUBCOMMANDS","**download**\n> Download media linked in Reddit submissions\n\n**archive**\n> Archive submission data, metadata, and comments\n\n**clone**\n> Perform both download and archive operations"],["PARAMETERS","**-s, --subreddit** _name_\n> Target subreddit(s); repeatable and comma-separated for multiple\n\n**-u, --user** _name_\n> Target Reddit username; use \"me\" for authenticated user (repeatable)\n\n**-m, --multireddit** _name_\n> Target a user's multireddit (requires --user; repeatable)\n\n**-l, --link** _url_\n> Download a single submission by URL or ID (repeatable)\n\n**-L, --limit** _n_\n> Maximum number of submissions per source (default: roughly 1000, the Reddit API ceiling)\n\n**--submitted**\n> Download user's submitted posts\n\n**--saved**\n> Download user's saved posts (requires --authenticate)\n\n**--upvoted**\n> Download user's upvoted posts (requires --authenticate)\n\n**--authenticate**\n> Use OAuth2 authentication for private content\n\n**--search** _query_\n> Search query to filter submissions\n\n**-S, --sort** _type_\n> Sort order: hot, top, new, controversial, rising, relevance\n\n**-t, --time** _period_\n> Time filter: all, hour, day, week, month, year\n\n**--all-comments**\n> Archive all of a user's comments (archive mode, with --user)\n\n**-f, --format** _type_\n> Archive output format: json (default), xml, yaml\n\n**--file-scheme** _format_\n> Custom filename template (placeholders: {POSTID}, {TITLE}, {REDDITOR}, {SUBREDDIT}, {DATE}, {FLAIR}, {UPVOTES})\n\n**--folder-scheme** _format_\n> Custom folder template using the same placeholders\n\n**--skip** _ext_\n> Skip files with the given extension (download mode; repeatable)\n\n**--no-dupes**\n> Skip files already downloaded, detected by MD5 hash (download mode)\n\n**-v, --verbose**\n> Increase output verbosity (repeatable)"],["CONFIGURATION","**~/.config/bdfr/default_config.cfg**\n> Main configuration file (Linux) holding client_id, client_secret, scopes, and default options. On macOS it lives under ~/Library/Application Support/bdfr."],["CAVEATS","Requires Python 3.9 or above. Authentication is required for accessing saved posts, upvoted posts, and private multireddits. Reddit API rate limits apply. Some media hosts may block automated downloads."],["HISTORY","BDFR was created as a modernized fork of earlier Reddit downloading tools, developed by **Serene-Arc** starting around **2020**. It replaced the original Bulk Downloader for Reddit with improved architecture and maintained compatibility with Reddit's evolving API."],["SEE ALSO","[gallery-dl](/man/gallery-dl)(1), [yt-dlp](/man/yt-dlp)(1), [youtube-dl](/man/youtube-dl)(1), [wget](/man/wget)(1)"],["RESOURCES","```[Source code](https://github.com/Serene-Arc/bulk-downloader-for-reddit)```\n\n```[Documentation](https://github.com/Serene-Arc/bulk-downloader-for-reddit/blob/master/README.md)```"]],"e":["gallery-dl","yt-dlp","youtube-dl","wget"],"x":"bdfr download [./output] --subreddit [Python] -L [10]"},"bun-pm-cache":{"n":"bun-pm-cache","t":"Manage Bun's global package cache","s":[["TAGLINE","Manage Bun's global package cache"],["TLDR","**Print** the path to Bun's global module cache\n\n```bun pm cache```\n\n**Clear** the entire global module cache\n\n```bun pm cache rm```"],["SYNOPSIS","**bun pm cache** [**rm**]"],["DESCRIPTION","**bun pm cache** manages Bun's global module cache directory where all packages downloaded from npm registries are stored. Running without arguments prints the absolute path to the cache directory. The **rm** subcommand deletes the entire cache contents.\n\nThe default cache location is **~/.bun/install/cache**, where packages are stored in subdirectories named **\\${name}@\\${version}**. When **bun install** runs, it checks this global cache first and uses cached copies via hardlink, clonefile, or copy instead of fetching from the network.\n\nThe cache location can be overridden via the **BUN_INSTALL_CACHE_DIR** environment variable or the **[install.cache]** section in **bunfig.toml**."],["CONFIGURATION","Cache settings in **bunfig.toml**\n\n```\n[install.cache]\ndir = \"~/.bun/install/cache\"\ndisable = false\ndisableManifest = false\n```\n\n**dir** sets a custom cache directory. **disable** prevents loading from global cache. **disableManifest** forces resolving latest versions from the registry."],["CAVEATS","There is no selective cache clearing; **bun pm cache rm** is all-or-nothing and removes the entire global cache. To remove a specific package, manually delete its directory under **~/.bun/install/cache/\\<package\\>@\\<version\\>**. The command historically required being run inside a directory containing a **package.json**, even though it operates on the global cache."],["INSTALL","```pacman: sudo pacman -S bun```\n\n```brew: brew install bun```\n\n```nix: nix profile install nixpkgs#bun```"],["SEE ALSO","[bun](/man/bun)(1), [bun-pm-trust](/man/bun-pm-trust)(1), [npm-cache](/man/npm-cache)(1)"]],"e":["bun","bun-pm-trust","npm-cache"],"x":"bun pm cache"},"c99":{"n":"c99","t":"POSIX-standard C99 compiler interface","s":[["TAGLINE","POSIX-standard C99 compiler interface"],["TLDR","**Compile a C source file to an executable**\n\n```c99 [file.c] -o [output]```\n\n**Compile without linking (produce object file)**\n\n```c99 -c [file.c]```\n\n**Compile with a library**\n\n```c99 [file.c] -l[library] -o [output]```\n\n**Define a preprocessor macro**\n\n```c99 -D [NAME=value] [file.c] -o [output]```\n\n**Add include directory for headers**\n\n```c99 -I [path/to/includes] [file.c] -o [output]```\n\n**Compile with optimization**\n\n```c99 -O [file.c] -o [output]```"],["SYNOPSIS","**c99** [**-c**] [**-D** _name_[=_value_]...] [**-E**] [**-g**] [**-I** _directory_...] [**-L** _directory_...] [**-o** _outfile_] [**-O** _optlevel_] [**-s**] [**-U** _name_...] _operand..._"],["DESCRIPTION","**c99** is the POSIX-standard interface to the C compilation system for compiling C99-conformant source code. It compiles C source files and links them to produce an executable file, or can produce object files for later linking.\n\nThe command accepts source files (**.c**), object files (**.o**), and library archives (**.a**) as operands. Without the **-o** option, the executable is written to **a.out**."],["PARAMETERS","**-c**\n> Compile only; do not link. Produces object files (.o)\n\n**-D** _name_[=_value_]\n> Define preprocessor macro\n\n**-E**\n> Preprocess only; do not compile\n\n**-g**\n> Include debugging information\n\n**-I** _directory_\n> Add directory to header search path\n\n**-L** _directory_\n> Add directory to library search path\n\n**-l** _library_\n> Link with specified library\n\n**-o** _outfile_\n> Write output to specified file\n\n**-O** _optlevel_\n> Enable optimization (0, 1, 2, 3)\n\n**-s**\n> Strip symbols from output\n\n**-U** _name_\n> Undefine preprocessor macro"],["CAVEATS","The **c99** command may not be available on all systems; it is required only on systems providing the POSIX C-Language Development option. On Linux, it typically invokes **gcc** or **clang** with appropriate flags for C99 compliance."],["HISTORY","The **c99** utility was introduced in **IEEE Std 1003.1-2001** (POSIX.1-2001), based on the earlier **c89** utility from ISO POSIX-2:1993. It reflects updates to support the **ISO/IEC 9899:1999** C standard (C99)."],["INSTALL","```dnf: sudo dnf install gcc```\n\n```pacman: sudo pacman -S gcc```\n\n```apk: sudo apk add gcc```\n\n```zypper: sudo zypper install gcc```\n\n```brew: brew install gcc```\n\n```nix: nix profile install nixpkgs#gcc```"],["SEE ALSO","[gcc](/man/gcc)(1), [clang](/man/clang)(1), [make](/man/make)(1), [ld](/man/ld)(1)"],["RESOURCES","```[Documentation](https://pubs.opengroup.org/onlinepubs/9699919799/utilities/c99.html)```"]],"e":["gcc","clang","make","ld"],"x":"c99 [file.c] -o [output]"},"cargo-verify-project":{"n":"cargo-verify-project","t":"Check correctness of a Cargo.toml manifest","s":[["TAGLINE","Check correctness of a Cargo.toml manifest"],["TLDR","**Verify current project manifest**\n\n```cargo verify-project```\n\n**Verify specific manifest**\n\n```cargo verify-project --manifest-path [path/to/Cargo.toml]```\n\n**Verify with locked dependencies**\n\n```cargo verify-project --locked```\n\n**Verify offline** (no network access)\n\n```cargo verify-project --frozen```"],["SYNOPSIS","**cargo verify-project** [_options_]"],["DESCRIPTION","**cargo verify-project** parses and validates the local Cargo.toml manifest file. It outputs a JSON object indicating whether the manifest is syntactically correct and contains all required fields, along with error details on failure.\n\nThis command is useful in CI/CD pipelines and pre-commit hooks for catching manifest errors early. It checks syntax correctness, valid dependency specifications, proper metadata formatting, and required fields. It does not verify that dependencies actually exist on a registry, only that the manifest itself is well-formed. The exit status is 0 for valid manifests and 1 for invalid ones."],["PARAMETERS","**--manifest-path** _path_\n> Path to Cargo.toml file.\n\n**--locked**\n> Assert that Cargo.lock is up-to-date.\n\n**--frozen**\n> Assert that Cargo.lock is up-to-date and prevent network access.\n\n**--offline**\n> Run without accessing the network.\n\n**-v**, **--verbose**\n> Use verbose output.\n\n**-q**, **--quiet**\n> Suppress cargo output messages.\n\n**--config** _KEY=VALUE_\n> Override a Cargo configuration value.\n\n**-C** _path_\n> Change to directory before executing."],["OUTPUT","**Success**\n```json\n{\"success\":\"true\"}\n```\n\n**Failure**\n```json\n{\"invalid\":\"error message\"}\n```"],["EXIT STATUS","**0**\n> Manifest is valid.\n\n**1**\n> Manifest is invalid."],["CAVEATS","Does not verify that dependencies actually exist on a registry, only that the manifest syntax is well-formed. Useful for CI/CD pipelines and pre-commit hooks."],["INSTALL","```apt: sudo apt install cargo```\n\n```dnf: sudo dnf install cargo```\n\n```apk: sudo apk add cargo```\n\n```zypper: sudo zypper install cargo```\n\n```nix: nix profile install nixpkgs#cargo```"],["SEE ALSO","[cargo](/man/cargo)(1), [cargo-check](/man/cargo-check)(1)"]],"e":["cargo","cargo-check"],"x":"cargo verify-project"},"cinnamon-launcher":{"n":"cinnamon-launcher","t":"Internal session and application launcher for Cinnamon","s":[["TAGLINE","Internal session and application launcher for Cinnamon"],["TLDR","**Start or restart a Cinnamon session**\n\n```cinnamon-launcher```"],["SYNOPSIS","**cinnamon-launcher**"],["DESCRIPTION","**cinnamon-launcher** is an internal utility for the Cinnamon desktop environment that facilitates starting or restarting Cinnamon sessions and launching applications.\n\nThe command processes **.desktop** files and handles application launching for the Cinnamon Shell, managing environment variables, working directories, and ensuring applications start correctly within the graphical session.\n\nIt is deeply integrated with Cinnamon components including the application menu, panel launchers, and the Nemo file manager, providing a consistent interface for starting processes across the desktop environment."],["CAVEATS","This utility is primarily for internal use by the Cinnamon desktop environment and is not typically invoked directly by users from the command line."],["INSTALL","```apt: sudo apt install cinnamon```\n\n```dnf: sudo dnf install cinnamon```\n\n```pacman: sudo pacman -S cinnamon```\n\n```zypper: sudo zypper install cinnamon```\n\n```nix: nix profile install nixpkgs#cinnamon```"],["SEE ALSO","[cinnamon](/man/cinnamon)(1)"]],"e":["cinnamon"],"x":"cinnamon-launcher"},"conda-env":{"n":"conda-env","t":"Manage conda environments","s":[["TAGLINE","Manage conda environments"],["TLDR","**List all environments**\n\n```conda env list```\n\n**Export the current environment to a YAML file**\n\n```conda env export > [environment.yml]```\n\n**Export only explicitly installed packages**\n\n```conda env export --from-history > [environment.yml]```\n\n**Create an environment from a YAML file**\n\n```conda env create -f [environment.yml]```\n\n**Create an environment with a specific name from a file**\n\n```conda env create -n [env_name] -f [environment.yml]```\n\n**Remove an environment**\n\n```conda env remove --name [env_name]```\n\n**Update an environment from a YAML file**\n\n```conda env update -f [environment.yml]```"],["SYNOPSIS","**conda** **env** _subcommand_ [_options_]"],["PARAMETERS","**list**\n> List all conda environments.\n\n**create**\n> Create an environment from a YAML specification file.\n\n**export**\n> Export an environment definition to YAML.\n\n**remove**\n> Remove an environment entirely.\n\n**update**\n> Update an existing environment from a YAML file.\n\n**config**\n> Configure environment-specific variables.\n\n**-n** _NAME_, **--name** _NAME_\n> Name of the environment to operate on.\n\n**-f** _FILE_, **--file** _FILE_\n> Path to the environment YAML file.\n\n**--from-history**\n> (export) Only include packages explicitly installed by the user.\n\n**--no-builds**\n> (export) Remove build specification from exported dependencies."],["DESCRIPTION","**conda env** manages conda environments including creation from specification files, export, and removal. The **environment.yml** format allows reproducible environment definitions that can include conda packages, pip packages, and channel specifications.\n\nUse **export --from-history** to create cross-platform environment files that only list explicitly requested packages rather than all resolved dependencies."],["CAVEATS","The **--from-history** flag only works with structured formats (YAML, JSON), not with explicit or requirements formats. Environment files may not be portable across platforms unless **--from-history** or **--no-builds** is used during export."],["INSTALL","```dnf: sudo dnf install conda```\n\n```nix: nix profile install nixpkgs#conda```"],["SEE ALSO","[conda](/man/conda)(1), [conda-create](/man/conda-create)(1), [conda-activate](/man/conda-activate)(1), [conda-install](/man/conda-install)(1), [conda-list](/man/conda-list)(1)"]],"e":["conda","conda-create","conda-activate","conda-install","conda-list"],"x":"conda env list"},"cradle-elastic":{"n":"cradle-elastic","t":"manage Elasticsearch within Cradle framework","s":[["TAGLINE","manage Elasticsearch within Cradle framework"],["TLDR","**Start Elasticsearch** with Cradle\n\n```cradle elastic start```\n\n**Stop Elasticsearch**\n\n```cradle elastic stop```\n\n**Check status** of Elasticsearch\n\n```cradle elastic status```\n\n**Flush** Elasticsearch indices\n\n```cradle elastic flush```\n\n**Map** Elasticsearch schema\n\n```cradle elastic map```"],["SYNOPSIS","**cradle** **elastic** _command_ [_options_]"],["PARAMETERS","**start**\n> Start the Elasticsearch service for the Cradle development environment.\n\n**stop**\n> Stop the running Elasticsearch service.\n\n**status**\n> Display the current status of the Elasticsearch service.\n\n**flush**\n> Flush all Elasticsearch indices, clearing cached data.\n\n**map**\n> Apply schema mappings to Elasticsearch indices."],["DESCRIPTION","**cradle elastic** manages Elasticsearch instances within the Cradle PHP framework. It provides commands to start, stop, and check the status of Elasticsearch services for development environments.\n\nThis tool simplifies local Elasticsearch management by handling service lifecycle operations through the Cradle CLI. It's particularly useful for developers who need Elasticsearch running locally for search functionality testing without manually managing the service.\n\nPart of the Cradle framework's integrated service management tools that allow developers to control dependencies directly from the framework CLI."],["SEE ALSO","[cradle](/man/cradle)(1), [cradle-install](/man/cradle-install)(1), [cradle-sql](/man/cradle-sql)(1), [elasticsearch](/man/elasticsearch)(1)"]],"e":["cradle","cradle-install","cradle-sql","elasticsearch"],"x":"cradle elastic start"},"csv-diff":{"n":"csv-diff","t":"compare CSV files and show differences","s":[["TAGLINE","compare CSV files and show differences"],["TLDR","**Diff two CSV files**\n\n```csv-diff [old.csv] [new.csv]```\n\n**Diff with specific key column**\n\n```csv-diff [old.csv] [new.csv] --key [id]```\n\n**Output as JSON**\n\n```csv-diff [old.csv] [new.csv] --json```\n\n**Show only changes**\n\n```csv-diff [old.csv] [new.csv] --show-unchanged```"],["SYNOPSIS","**csv-diff** [_options_] _old.csv_ _new.csv_"],["PARAMETERS","**--key** _column_\n> Column to use as unique identifier.\n\n**--json**\n> Output in JSON format.\n\n**--show-unchanged**\n> Include unchanged rows in output.\n\n**--singular** _name_\n> Singular noun for rows.\n\n**--plural** _name_\n> Plural noun for rows."],["DESCRIPTION","**csv-diff** compares two CSV, TSV, or JSON files and shows added, removed, and changed rows. It's useful for tracking changes in data exports, database dumps, or any tabular data.\n\nA **--key** column must be specified so rows can be matched across files. Output is human-readable by default, and machine-readable JSON is available via **--json**.\n\nThe tool is a standalone Python package by **Simon Willison**, installable via `pip install csv-diff`. It is distinct from csvkit."],["CAVEATS","Rows without a stable unique key cannot be meaningfully compared; choose a column guaranteed to be present on both sides. Large files are loaded fully into memory."],["SEE ALSO","[csvkit](/man/csvkit)(1), [diff](/man/diff)(1), [jq](/man/jq)(1)"]],"e":["csvkit","diff","jq"],"x":"csv-diff [old.csv] [new.csv]"},"csvlens":{"n":"csvlens","t":"CSV file viewer with search and navigation","s":[["TAGLINE","CSV file viewer with search and navigation"],["TLDR","**View** a CSV file with column alignment and navigation\n\n```csvlens [path/to/file.csv]```\n\n**Search** for a specific pattern in the CSV\n\n```csvlens [path/to/file.csv] -f [pattern]```\n\n**View** with a specific delimiter instead of comma\n\n```csvlens [path/to/file.csv] -d '[delimiter]'```\n\n**View** only specific columns\n\n```csvlens [path/to/file.csv] -c [1,3,5]```"],["SYNOPSIS","**csvlens** [_options_] [_file_]"],["PARAMETERS","**-f, --filter** _PATTERN_\n> Filter rows matching the pattern\n\n**-d, --delimiter** _CHAR_\n> Specify delimiter character (default: comma)\n\n**-c, --columns** _LIST_\n> Show only specified columns (comma-separated indices)\n\n**-H, --no-header**\n> Treat the first row as data, not headers\n\n**-n, --line-number**\n> Show line numbers\n\n**--tab**\n> Use tab as delimiter\n\n**--semi**\n> Use semicolon as delimiter\n\n**-h, --help**\n> Display help and exit\n\n**-V, --version**\n> Display version and exit"],["DESCRIPTION","**csvlens** is a command-line CSV file viewer designed for efficiently browsing and searching through CSV data. It provides column-aligned display, interactive search filtering, and keyboard navigation similar to less or vim.\n\nThe tool automatically detects and aligns columns for easy reading, supports various delimiters beyond commas, and allows filtering data without modifying the original file. It's particularly useful for inspecting large CSV files where traditional editors may struggle with performance."],["KEYBINDINGS","**↑/↓** or **k/j**\n> Navigate up/down through rows\n\n**←/→** or **h/l**\n> Scroll horizontally\n\n**/**\n> Search/filter mode\n\n**n/N**\n> Next/previous search result\n\n**g/G**\n> Go to first/last row\n\n**q**\n> Quit"],["CAVEATS","Very large CSV files may still experience performance limitations depending on available memory. Unicode and special character handling depends on terminal capabilities. Some complex CSV formats with embedded newlines may not render correctly."],["HISTORY","**csvlens** was developed to address the need for a fast, lightweight CSV viewer in the terminal. It draws inspiration from traditional Unix tools like less and grep while adding CSV-specific formatting and navigation features."],["INSTALL","```pacman: sudo pacman -S csvlens```\n\n```brew: brew install csvlens```\n\n```nix: nix profile install nixpkgs#csvlens```"],["SEE ALSO","[cat](/man/cat)(1), [less](/man/less)(1), [grep](/man/grep)(1), [awk](/man/awk)(1)"]],"e":["cat","less","grep","awk"],"x":"csvlens [path/to/file.csv]"},"delta":{"n":"delta","t":"syntax-highlighting pager for diffs","s":[["TAGLINE","syntax-highlighting pager for diffs"],["TLDR","**View git diff** with delta\n\n```git diff | delta```\n\n**Compare two files**\n\n```delta [file1.txt] [file2.txt]```\n\n**Use side-by-side** view\n\n```delta -s [file1.txt] [file2.txt]```\n\n**Show line numbers**\n\n```delta -n [file1.txt] [file2.txt]```\n\n**Use specific syntax** theme\n\n```delta --syntax-theme [Dracula] [file1.txt] [file2.txt]```\n\n**View git log** with delta formatting\n\n```git log -p | delta```\n\n**Show diff with navigate** mode (use n/N to jump between hunks)\n\n```git diff | delta --navigate```\n\n**Configure as git pager**\n\n```git config --global core.pager delta```\n\n**List available syntax** themes\n\n```delta --list-syntax-themes```"],["SYNOPSIS","**delta** [_options_] [_file1_] [_file2_]"],["PARAMETERS","_FILE1_ _FILE2_\n> Files to compare (reads from stdin if not provided).\n\n**-s**, **--side-by-side**\n> Side-by-side display mode.\n\n**-n**, **--line-numbers**\n> Show line numbers.\n\n**--syntax-theme** _THEME_\n> Syntax highlighting theme.\n\n**--word-diff-regex** _REGEX_\n> Highlight word-level changes using regex to define words.\n\n**--hyperlinks**\n> Render commit hashes, file paths, and line numbers as terminal hyperlinks.\n\n**--color-only**\n> Do not alter formatting; only add syntax highlighting.\n\n**--diff-stat-align-width** _N_\n> Width allocated for file paths in a diff stat section.\n\n**--features** _NAMES_\n> Activate named feature sets defined in gitconfig.\n\n**--list-syntax-themes**\n> List available syntax-highlighting themes.\n\n**--paging** _auto|always|never_\n> Control pager usage.\n\n**--true-color** _auto|always|never_\n> Control 24-bit RGB color output.\n\n**--navigate**\n> Enable navigation with n/N keys.\n\n**--dark**/**--light**\n> Optimize for dark/light terminal.\n\n**--diff-so-fancy**\n> Emulate diff-so-fancy output.\n\n**--raw**\n> Do not alter the input in any way; only highlight syntax.\n\n**--width** _N_\n> The width of underline/overline decorations and side-by-side mode.\n\n**--tabs** _N_\n> Number of spaces to replace tab characters with (default: 4).\n\n**--help**\n> Display help information."],["DESCRIPTION","**delta** is a syntax-highlighting pager for git, diff, and grep output. It provides beautiful, readable diffs with features like side-by-side view, line numbering, and word-level change highlighting.\n\ndelta is designed as a git pager replacement, integrating seamlessly with git diff, git show, git log, and git blame. It uses the same syntax highlighting engine as bat, supporting hundreds of languages.\n\nFeatures include navigation between hunks, customizable themes, hyperlinks to line numbers, and special handling of merge conflicts. It can also process unified diff output from other tools."],["CONFIGURATION","**~/.gitconfig**\n> Configure delta as the default git pager with core.pager and delta.* options."],["CAVEATS","Requires terminal with true color support for best results. Large diffs may be slow to render. Some themes may not work well with all terminal color schemes."],["HISTORY","delta was created by **Dan Davison** and released in **2019**. It was inspired by diff-so-fancy but implemented in Rust for better performance and with additional features like syntax highlighting."],["INSTALL","```apt: sudo apt install git-delta```\n\n```dnf: sudo dnf install git-delta```\n\n```pacman: sudo pacman -S git-delta```\n\n```apk: sudo apk add delta```\n\n```zypper: sudo zypper install git-delta```\n\n```brew: brew install git-delta```\n\n```nix: nix profile install nixpkgs#delta```"],["SEE ALSO","[diff](/man/diff)(1), [git](/man/git)(1), [bat](/man/bat)(1), [diff-so-fancy](/man/diff-so-fancy)(1), [less](/man/less)(1), [colordiff](/man/colordiff)(1)"]],"e":["diff","git","bat","diff-so-fancy","less","colordiff"],"x":"git diff | delta"},"distrobox-create":{"n":"distrobox-create","t":"create Distrobox containers from Linux distribution images","s":[["TAGLINE","create Distrobox containers from Linux distribution images"],["TLDR","Create a container using an **Ubuntu image**\n\n```distrobox-create --name [container_name] --image [ubuntu:latest]```\n\n**Clone** an existing container\n\n```distrobox-create --clone [source_container] --name [new_container]```\n\nCreate non-interactively, **pulling the image** and installing extra packages\n\n```distrobox-create -Y -n [dev] -i [fedora:latest] -ap \"[git vim]\"```\n\n**Show compatible images**\n\n```distrobox-create --compatibility```"],["SYNOPSIS","**distrobox-create** [_options_] [_name_]"],["DESCRIPTION","**distrobox-create** creates a Distrobox container from any Linux distribution image. The container integrates tightly with the host, sharing the user's home directory, external storage, USB devices, graphics (X11/Wayland), and audio."],["PARAMETERS","**-i, --image** _image_\n> Container image to use\n\n**-n, --name** _name_\n> Container name\n\n**-c, --clone** _container_\n> Clone existing container\n\n**-H, --home** _path_\n> Custom home directory\n\n**-p, --pull**\n> Pull the image even if it exists locally (implies --yes)\n\n**-Y, --yes**\n> Non-interactive; pull images without asking\n\n**-ap, --additional-packages** _packages_\n> Additional packages to install during initial setup\n\n**--volume** _spec_\n> Additional volume to mount into the container\n\n**-I, --init**\n> Use an init system (like systemd) inside the container\n\n**--nvidia**\n> Integrate the host's NVIDIA drivers into the container\n\n**-r, --root**\n> Create a rootful container (launch the container manager as root)\n\n**-C, --compatibility**\n> Show a list of compatible images"],["CAVEATS","Requires Podman or Docker. Images must be compatible Linux distributions. Home directory sharing is bidirectional. First entry may take time for initialization."],["INSTALL","```dnf: sudo dnf install distrobox```\n\n```pacman: sudo pacman -S distrobox```\n\n```apk: sudo apk add distrobox```\n\n```zypper: sudo zypper install distrobox```\n\n```brew: brew install distrobox```\n\n```nix: nix profile install nixpkgs#distrobox```"],["SEE ALSO","[distrobox](/man/distrobox)(1), [distrobox-enter](/man/distrobox-enter)(1), [podman](/man/podman)(1)"],["RESOURCES","```[Source code](https://github.com/89luca89/distrobox)```\n\n```[Homepage](https://distrobox.it)```"]],"e":["distrobox","distrobox-enter","podman"],"x":"distrobox-create --name [container_name] --image [ubuntu:latest]"},"dnsx":{"n":"dnsx","t":"fast multi-purpose DNS toolkit","s":[["TAGLINE","fast multi-purpose DNS toolkit"],["TLDR","**Resolve A records** for domains\n\n```cat [domains.txt] | dnsx -a```\n\n**Resolve multiple** record types\n\n```cat [domains.txt] | dnsx -a -aaaa -mx -ns```\n\n**Resolve with** custom resolver\n\n```cat [domains.txt] | dnsx -r [8.8.8.8]```\n\n**Output in JSON** format\n\n```cat [domains.txt] | dnsx -json```\n\n**Brute force subdomains**\n\n```dnsx -d [domain.com] -w [wordlist.txt]```\n\n**Reverse DNS** lookups\n\n```cat [ips.txt] | dnsx -ptr```\n\n**Rate limit requests**\n\n```cat [domains.txt] | dnsx -rate-limit [100]```"],["SYNOPSIS","**dnsx** [_options_]"],["PARAMETERS","**-a**\n> Query A records.\n\n**-aaaa**\n> Query AAAA records.\n\n**-mx**\n> Query MX records.\n\n**-ns**\n> Query NS records.\n\n**-ptr**\n> Query PTR records.\n\n**-cname**\n> Query CNAME records.\n\n**-d** _DOMAIN_\n> Target domain for subdomain brute force.\n\n**-w** _FILE_\n> Wordlist for brute force.\n\n**-r** _RESOLVER_\n> Custom DNS resolver.\n\n**-json**\n> Output in JSON format.\n\n**-rate-limit** _N_\n> Max queries per second.\n\n**-help**\n> Display help information."],["DESCRIPTION","**dnsx** is a fast, multi-purpose DNS toolkit designed for security reconnaissance. It runs DNS queries efficiently, supporting multiple record types, custom resolvers, and high-performance parallel execution.\n\nThe tool excels at bulk DNS operations including resolution, reverse lookups, and subdomain brute forcing. It accepts input from stdin, making it easy to integrate into reconnaissance pipelines with other tools.\n\ndnsx supports wildcard filtering, retries, and various output formats. Its focus on performance makes it suitable for large-scale DNS enumeration tasks."],["CAVEATS","High query rates may trigger rate limiting or blocks. Only perform on authorized targets. Large wordlists consume time and may be detected. DNS responses may be cached or manipulated."],["HISTORY","dnsx was developed by **ProjectDiscovery**, creators of other security tools like nuclei and httpx. It was released as part of their open-source security toolkit to provide fast, reliable DNS operations for reconnaissance workflows."],["INSTALL","```dnf: sudo dnf install dnsx```\n\n```brew: brew install dnsx```\n\n```nix: nix profile install nixpkgs#dnsx```"],["SEE ALSO","[dig](/man/dig)(1), [massdns](/man/massdns)(1)"]],"e":["dig","massdns"],"x":"cat [domains.txt] | dnsx -a"},"docker-commit":{"n":"docker-commit","t":"create image from container changes","s":[["TAGLINE","create image from container changes"],["TLDR","**Create image from container**\n\n```docker commit [container_id] [image_name:tag]```\n\n**Commit with author info**\n\n```docker commit --author \"[name]\" [container_id] [image:tag]```\n\n**Commit with message**\n\n```docker commit -m \"[Added config files]\" [container_id] [image:tag]```\n\n**Commit with changed CMD**\n\n```docker commit --change \"CMD ['/app/start.sh']\" [container_id] [image:tag]```"],["SYNOPSIS","**docker** **commit** [_options_] _container_ [_repository[:tag]_]"],["PARAMETERS","**-a**, **--author** _string_\n> Author of the image.\n\n**-c**, **--change** _list_\n> Apply Dockerfile instructions to the image.\n\n**-m**, **--message** _string_\n> Commit message.\n\n**-p**, **--pause**\n> Pause container during commit (default true)."],["DESCRIPTION","**docker commit** creates a new image from a container's changes. This captures the current state of a container's filesystem as a new image layer by examining all modifications made since the container was created.\n\nWhile useful for debugging, experimentation, or saving a working state, this approach is not recommended for production workflows. Dockerfiles provide better reproducibility, version control, and documentation of the build process. Committed images lack the transparent layer history that Dockerfiles provide, making them harder to maintain and audit."],["CAVEATS","Does not include data in volumes mounted into the container. The resulting image lacks meaningful build history and is not reproducible from source. By default the container is paused for the duration of the commit (**--pause=true**); pass **--pause=false** to keep it running, at the risk of capturing an inconsistent filesystem snapshot if writes are in flight."],["HISTORY","**docker commit** has been in the Docker CLI since the project's first public release in **2013**. It predates Dockerfiles and was the original way to build images; modern workflows prefer **docker build** for reproducibility and **docker commit** for ad-hoc debug snapshots only."],["INSTALL","```apt: sudo apt install docker-cli```\n\n```dnf: sudo dnf install docker-cli```\n\n```pacman: sudo pacman -S docker```\n\n```apk: sudo apk add docker-cli```\n\n```zypper: sudo zypper install docker```\n\n```brew: brew install docker```\n\n```nix: nix profile install nixpkgs#docker```"],["SEE ALSO","[docker](/man/docker)(1), [docker-build](/man/docker-build)(1), [docker-save](/man/docker-save)(1), [docker-diff](/man/docker-diff)(1)"]],"e":["docker","docker-build","docker-save","docker-diff"],"x":"docker commit [container_id] [image_name:tag]"},"ecpg":{"n":"ecpg","t":"PostgreSQL embedded SQL preprocessor","s":[["TAGLINE","PostgreSQL embedded SQL preprocessor"],["TLDR","**Preprocess embedded SQL file**\n\n```ecpg [file.pgc]```\n\n**Specify output file**\n\n```ecpg -o [output.c] [input.pgc]```\n\n**Include search path**\n\n```ecpg -I [/include/path] [file.pgc]```\n\n**Enable compatibility mode**\n\n```ecpg -C [INFORMIX] [file.pgc]```\n\n**Process a header file** containing embedded SQL declarations\n\n```ecpg -h [header.pgh]```\n\n**Use question marks** as parameter placeholders (Informix style)\n\n```ecpg -r [questionmarks] [file.pgc]```"],["SYNOPSIS","**ecpg** [_options_] _file_..."],["PARAMETERS","**-o** _file_\n> Write output to file.\n\n**-I** _directory_\n> Include search path.\n\n**-c**\n> Automatically generate C code from SQL code, currently for EXEC SQL TYPE declarations.\n\n**-C** _mode_\n> Compatibility mode: INFORMIX, INFORMIX_SE or ORACLE.\n\n**-D** _symbol_[**=**_value_]\n> Define a C preprocessor symbol.\n\n**-h**\n> Process a header file. Implies **-c** and writes a .h file rather than a .c file.\n\n**-i**\n> Parse system include files as well.\n\n**-r** _option_\n> Set a run-time behavior: no_indicator, prepare, questionmarks.\n\n**-t**\n> Turn on autocommit of transactions.\n\n**-v**\n> Print version, include path and other build information.\n\n**--version**\n> Print version and exit."],["DESCRIPTION","**ecpg** is the PostgreSQL embedded SQL preprocessor. It converts C programs with embedded SQL statements into regular C code that calls the ECPG runtime library, which in turn talks to the server through libpq.\n\nThe tool reads .pgc files containing EXEC SQL statements embedded in C code and generates standard C files. By default the output for `prog.pgc` is written to `prog.c`. The generated code must be compiled and linked against **libecpg** (for example with `gcc prog.c -lecpg`), and the include path from `pg_config --includedir` is usually needed."],["CAVEATS","The generated C file links against libecpg, not libpq directly, so the PostgreSQL client development package must be installed. Compatibility modes change SQL parsing and null handling to match Informix or Oracle, and code written for one mode is generally not portable to another."],["INSTALL","```apt: sudo apt install libecpg-dev```\n\n```apk: sudo apk add libecpg-dev```"],["SEE ALSO","[psql](/man/psql)(1), [pg_config](/man/pg_config)(1), [postgres](/man/postgres)(1)"],["RESOURCES","```[Source code](https://github.com/postgres/postgres)```\n\n```[Homepage](https://www.postgresql.org/)```\n\n```[Documentation](https://www.postgresql.org/docs/current/app-ecpg.html)```"]],"e":["psql","pg_config","postgres"],"x":"ecpg [file.pgc]"},"eglinfo":{"n":"eglinfo","t":"EGL graphics library information display","s":[["TAGLINE","EGL graphics library information display"],["TLDR","Display **full** platform EGL information\n\n```eglinfo```\n\nDisplay **brief** platform information\n\n```eglinfo -B```\n\nDisplay information for a **specific platform**\n\n```eglinfo -p [x11|wayland|gbm|surfaceless]```\n\nDisplay **help**\n\n```eglinfo -h```"],["SYNOPSIS","**eglinfo** [_options_]"],["DESCRIPTION","**eglinfo** displays information about EGL (Embedded-System Graphics Library) implementations on the system. It shows available displays, configurations, extensions, and rendering APIs.\n\nUseful for debugging graphics issues and verifying GPU driver configuration."],["PARAMETERS","**-B**\n> Brief output, showing only essential information.\n\n**-s**\n> Print one extension per line.\n\n**-v**\n> Print configuration details in verbose form.\n\n**-l**\n> Show OpenGL limits (requires EGL 1.2+).\n\n**-a** _api_\n> Display information for a specific API (gl, glcore, gles).\n\n**-p** _platform_\n> Display information for a specific platform (x11, wayland, gbm, surfaceless, android).\n\n**-h**\n> Display help information."],["CAVEATS","Requires a working EGL implementation. Output depends on graphics drivers installed. May not work in headless environments without display."],["INSTALL","```apt: sudo apt install mesa-utils```\n\n```pacman: sudo pacman -S mesa-utils```\n\n```apk: sudo apk add mesa-utils```"],["SEE ALSO","[glxinfo](/man/glxinfo)(1), [vulkaninfo](/man/vulkaninfo)(1)"]],"e":["glxinfo","vulkaninfo"],"x":"eglinfo"},"erb":{"n":"erb","t":"Embedded Ruby template processor","s":[["TAGLINE","Embedded Ruby template processor"],["TLDR","**Process ERB template**\n\n```erb [template.erb]```\n\n**Process with Ruby variables**\n\n```erb -r [./vars.rb] [template.erb]```\n\n**Output to file**\n\n```erb [template.erb] > [output.html]```\n\n**Trim mode for cleaner output**\n\n```erb -T - [template.erb]```\n\n**Print result without newline**\n\n```erb -P [template.erb]```"],["SYNOPSIS","**erb** [_options_] [_file_...]"],["PARAMETERS","**-r** _library_\n> Require Ruby library before processing.\n\n**-T** _mode_\n> Trim mode: 0, 1, 2, or -.\n\n**-P**\n> Don't print newline after output.\n\n**-e**\n> Ignore RUBYOPT environment variable.\n\n**-x**\n> Print Ruby script instead of executing.\n\n**-n**\n> Print line numbers in output.\n\n**-v**\n> Verbose mode.\n\n**--version**\n> Show version."],["DESCRIPTION","**erb** processes ERB (Embedded Ruby) templates, allowing Ruby code to be embedded within text files. ERB is commonly used in Rails views and configuration file generation.\n\nTemplates use special tags: `<%= expr %>` outputs the expression result, `<% code %>` executes code without output, and `<%# comment %>` is for comments."],["TEMPLATE SYNTAX","```erb\n<h1><%= @title %></h1>\n<ul>\n<% @items.each do |item| %>\n  <li><%= item %></li>\n<% end %>\n</ul>\n<%# This is a comment %>\n```"],["TRIM MODES","```\n-T 0  - No trimming\n-T 1  - Trim lines ending with %>\n-T 2  - Trim lines containing only <% %>\n-T -  - Trim newlines after -%>\n```"],["CAVEATS","Embedded Ruby executes with full permissions. Input should be trusted. Complex logic belongs in code, not templates. Whitespace handling may need trim mode adjustment."],["HISTORY","ERB was created by **Masatoshi Seki** and included in Ruby's standard library. It became popular with Ruby on Rails as the default view template engine, though Rails now often uses alternatives like Slim or Haml."],["INSTALL","```apt: sudo apt install ruby```\n\n```dnf: sudo dnf install ruby```\n\n```pacman: sudo pacman -S ruby```\n\n```apk: sudo apk add ruby```\n\n```zypper: sudo zypper install ruby```\n\n```brew: brew install ruby```\n\n```nix: nix profile install nixpkgs#ruby```"],["SEE ALSO","[ruby](/man/ruby)(1), [irb](/man/irb)(1), [haml](/man/haml)(1)"],["RESOURCES","```[Source code](https://github.com/ruby/erb)```\n\n```[Documentation](https://docs.ruby-lang.org/en/master/ERB.html)```"]],"e":["ruby","irb","haml"],"x":"erb [template.erb]"},"fbi":{"n":"fbi","t":"framebuffer image viewer","s":[["TAGLINE","framebuffer image viewer"],["TLDR","**Display** an image\n\n```sudo fbi [path/to/file]```\n\nDisplay **multiple** images\n\n```sudo fbi [path/to/file1] [path/to/file2]```\n\nStart **slideshow** with custom delay\n\n```sudo fbi [-t|--timeout] [delay] [path/to/files/*]```\n\n**Scale** image to fit screen\n\n```sudo fbi [-a|--autozoom] [path/to/file]```\n\nDisplay **help**\n\n```fbi [-h|--help]```"],["SYNOPSIS","**fbi** [_options_] _files_"],["DESCRIPTION","**fbi** (framebuffer imageviewer) displays images directly on the Linux framebuffer console without requiring X11 or Wayland. It supports various image formats including JPEG, PNG, GIF, and TIFF.\n\nUseful for viewing images on headless servers or minimal systems."],["PARAMETERS","**-t**, **--timeout** _seconds_\n> Slideshow delay between images\n\n**-a**, **--autozoom**\n> Scale to fit screen\n\n**-h**, **--help**\n> Display help\n\n**-1**, **--once**\n> Don't loop slideshow\n\n**-u**, **--random**\n> Randomize slideshow order"],["CAVEATS","Requires framebuffer support. Usually needs root or console access. Part of fbida package. Not for use in X11/Wayland sessions."],["INSTALL","```apt: sudo apt install fbi```\n\n```apk: sudo apk add fbida-fbi```"],["SEE ALSO","[fim](/man/fim)(1), [feh](/man/feh)(1), [fbset](/man/fbset)(1)"]],"e":["fim","feh","fbset"],"x":"sudo fbi [path/to/file]"},"firefox":{"n":"firefox","t":"Mozilla's open source web browser","s":[["TAGLINE","Mozilla's open source web browser"],["TLDR","**Open Firefox**\n\n```firefox```\n\n**Open specific URL**\n\n```firefox [https://example.com]```\n\n**New window**\n\n```firefox --new-window [url]```\n\n**Private browsing**\n\n```firefox --private-window```\n\n**Use specific profile**\n\n```firefox -P [profilename]```"],["SYNOPSIS","**firefox** [_options_] [_urls_...]"],["PARAMETERS","_URLS_\n> URLs to open.\n\n**--new-window** _URL_\n> Open URL in new window.\n\n**--new-tab** _URL_\n> Open URL in new tab.\n\n**--private-window**\n> Start private browsing.\n\n**-P** _PROFILE_\n> Use named profile.\n\n**--ProfileManager**\n> Open profile manager.\n\n**--safe-mode**\n> Start with extensions disabled.\n\n**--headless**\n> Run without GUI.\n\n**--screenshot** _FILE_\n> Capture page to file.\n\n**--help**\n> Display help information."],["CONFIGURATION","**~/.mozilla/firefox/profiles.ini**\n> Profile configuration and location mappings.\n\n**~/.mozilla/firefox/[profile]/prefs.js**\n> User preferences for a specific profile.\n\n**~/.mozilla/firefox/[profile]/user.js**\n> Custom user overrides for preferences."],["DESCRIPTION","**firefox** is Mozilla's open source web browser. The command-line interface launches the browser with various options for windows, profiles, and automated tasks.\n\nThe browser supports multiple profiles for separating work and personal browsing. Headless mode enables automated screenshots and testing without a visible window.\n\nFirefox provides extensive privacy features and extension support through its add-ons ecosystem."],["CAVEATS","Profile options require no running instance. Headless mode has limited functionality. Some options vary by platform."],["HISTORY","Firefox emerged from the **Mozilla** project in 2002 as Phoenix, renamed Firefox in 2004. It became a major browser challenging Internet Explorer's dominance, pioneering features like tabbed browsing and extensions."],["INSTALL","```apt: sudo apt install firefox-esr```\n\n```dnf: sudo dnf install firefox```\n\n```pacman: sudo pacman -S firefox```\n\n```apk: sudo apk add firefox```\n\n```zypper: sudo zypper install firefox-esr```\n\n```nix: nix profile install nixpkgs#firefox-esr```"],["SEE ALSO","[chromium](/man/chromium)(1), [google-chrome](/man/google-chrome)(1)"],["RESOURCES","```[Homepage](https://www.mozilla.org/firefox/)```"]],"e":["chromium","google-chrome"],"x":"firefox"},"flamegraph":{"n":"flamegraph","t":"visualization for profiled stack traces","s":[["TAGLINE","visualization for profiled stack traces"],["TLDR","**Generate flame graph from perf data**\n\n```perf script | stackcollapse-perf.pl | flamegraph.pl > [flame.svg]```\n\n**From dtrace output**\n\n```stackcollapse.pl [out.dtrace] | flamegraph.pl > [flame.svg]```\n\n**Custom title**\n\n```flamegraph.pl --title \"[My App Profile]\" [stacks.txt] > [flame.svg]```\n\n**Inverted (icicle) graph**\n\n```flamegraph.pl --inverted [stacks.txt] > [flame.svg]```\n\n**Set width**\n\n```flamegraph.pl --width [1200] [stacks.txt] > [flame.svg]```"],["SYNOPSIS","**flamegraph.pl** [_options_] [_input_]"],["PARAMETERS","**--title** _text_\n> Title for the graph.\n\n**--subtitle** _text_\n> Second level title (optional).\n\n**--width** _pixels_\n> SVG width (default 1200).\n\n**--height** _pixels_\n> Frame height (default 16).\n\n**--minwidth** _pixels_\n> Omit functions narrower than this (default 0.1 pixels).\n\n**--fontsize** _num_\n> Font size (default 12).\n\n**--fonttype** _font_\n> Font type (default \"Verdana\").\n\n**--countname** _text_\n> Count type label (default \"samples\").\n\n**--nametype** _text_\n> Name type label (default \"Function:\").\n\n**--inverted**\n> Generate icicle graph (top-down).\n\n**--reverse**\n> Reverse stack order.\n\n**--colors** _palette_\n> Color palette: hot (default), mem, io, wakeup, chain, java, js, perl, red, green, blue, aqua, yellow, purple, orange.\n\n**--bgcolors** _color_\n> Background gradient: yellow (default), blue, green, grey, or flat \"#rrggbb\".\n\n**--hash**\n> Color by function name hash.\n\n**--cp**\n> Use consistent palette (palette.map).\n\n**--flamechart**\n> Time-ordered flame chart (sort by time, do not merge stacks).\n\n**--negate**\n> Switch differential hues (blue/red)."],["DESCRIPTION","**FlameGraph** is a visualization tool for profiled stack traces, created by Brendan Gregg. It generates interactive SVG graphs where the x-axis shows the stack population and the y-axis shows stack depth.\n\nFlame graphs quickly reveal which code paths consume the most resources. They work with any profiler output that can be converted to the collapsed stack format."],["WORKFLOW","```bash"],["1. Collect profile (Linux perf)","perf record -g ./myapp"],["2. Convert to text stacks","perf script > out.perf"],["3. Collapse stacks","stackcollapse-perf.pl out.perf > stacks.txt"],["4. Generate flame graph","flamegraph.pl stacks.txt > flame.svg\n```"],["CAVEATS","Requires Perl. Input must be in collapsed stack format (use stackcollapse-*.pl scripts to convert). Large profiles may produce complex graphs. Colors are randomized unless --hash or --cp is used. Interactive features (zoom, search) require an SVG-capable browser."],["HISTORY","Flame graphs were invented by **Brendan Gregg** in **2011** while at Joyent for analyzing performance issues. The visualization technique has become standard for performance profiling across languages and platforms."],["INSTALL","```dnf: sudo dnf install flamegraph```\n\n```pacman: sudo pacman -S cargo-flamegraph```\n\n```apk: sudo apk add cargo-flamegraph```\n\n```brew: brew install cargo-flamegraph```\n\n```nix: nix profile install nixpkgs#cargo-flamegraph```"],["SEE ALSO","[perf](/man/perf)(1), [bpftrace](/man/bpftrace)(8), [strace](/man/strace)(1), [gprof](/man/gprof)(1), [valgrind](/man/valgrind)(1)"]],"e":["perf","bpftrace","strace","gprof","valgrind"],"x":"perf script | stackcollapse-perf.pl | flamegraph.pl > [flame.svg]"},"gcloud-container":{"n":"gcloud-container","t":"Google Kubernetes Engine cluster management","s":[["TAGLINE","Google Kubernetes Engine cluster management"],["TLDR","**Create GKE cluster**\n\n```gcloud container clusters create [name] --zone=[zone]```\n\n**List clusters**\n\n```gcloud container clusters list```\n\n**Get credentials**\n\n```gcloud container clusters get-credentials [name] --zone=[zone]```\n\n**Delete cluster**\n\n```gcloud container clusters delete [name] --zone=[zone]```\n\n**Resize node pool**\n\n```gcloud container clusters resize [name] --node-pool=[pool] --num-nodes=[3]```"],["SYNOPSIS","**gcloud container** _resource_ _command_ [_options_]"],["PARAMETERS","_RESOURCE_\n> Resource type: clusters, node-pools, images.\n\n**clusters** _CMD_\n> GKE cluster management.\n\n**node-pools** _CMD_\n> Node pool operations.\n\n**images** _CMD_\n> Container image operations.\n\n**get-credentials** _CLUSTER_\n> Configure kubectl for cluster.\n\n**--zone** _ZONE_\n> Cluster zone.\n\n**--help**\n> Display help information."],["DESCRIPTION","**gcloud container** provides comprehensive management for Google Kubernetes Engine (GKE), Google's managed Kubernetes service. It handles the complete lifecycle of Kubernetes clusters without requiring manual setup of master nodes or control plane components.\n\nThe clusters subcommand creates and manages GKE clusters with customizable node configurations, network settings, and Kubernetes versions. Node pools allow heterogeneous clusters with different machine types or configurations for different workload types. Scaling operations can adjust cluster capacity by modifying node pool sizes.\n\nA critical feature is get-credentials, which configures kubectl by writing cluster authentication details to your kubeconfig file. This seamlessly connects the standard Kubernetes CLI tools to your GKE clusters. The command integrates with Google Container Registry for private image storage and supports advanced features like cluster autoscaling, node auto-repair, and automatic Kubernetes version upgrades."],["CAVEATS","Clusters incur costs. Creation takes time. Kubectl configuration required for access."],["HISTORY","gcloud container is part of the **Google Cloud SDK** for managing GKE, Google's managed Kubernetes service for running containerized applications."],["SEE ALSO","[gcloud](/man/gcloud)(1), [kubectl](/man/kubectl)(1), [gcloud-compute](/man/gcloud-compute)(1)"],["RESOURCES","```[Documentation](https://cloud.google.com/sdk/gcloud/reference/container)```"]],"e":["gcloud","kubectl","gcloud-compute"],"x":"gcloud container clusters create [name] --zone=[zone]"},"git-diff-tree":{"n":"git-diff-tree","t":"Compare content of tree objects","s":[["TAGLINE","Compare content of tree objects"],["TLDR","**Compare two trees**\n\n```git diff-tree [tree1] [tree2]```\n\n**Show changes in commit**\n\n```git diff-tree -p [commit]```\n\n**Show changed files**\n\n```git diff-tree --name-only [commit]```\n\n**Raw output**\n\n```git diff-tree --raw [commit]```"],["SYNOPSIS","**git** **diff-tree** [_options_] _tree-ish_ [_tree-ish_] [_files_...]"],["PARAMETERS","**-r**\n> Recurse into subtrees.\n\n**-p**, **--patch**\n> Generate patch.\n\n**--raw**\n> Raw diff format.\n\n**--name-only**\n> Show file names only.\n\n**--name-status**\n> Show status and names.\n\n**-s**\n> Suppress diff output.\n\n**--stdin**\n> Read commits from stdin, one per line; each is compared with its parent(s).\n\n**--root**\n> Include the initial commit as a big-bang diff against the empty tree (only meaningful with **--stdin** or a single _tree-ish_).\n\n**-c**, **--cc**\n> Show a combined diff for merge commits instead of only listing changed files."],["DESCRIPTION","**git diff-tree** compares the content and mode of blobs found via two tree objects, providing low-level access to Git's internal tree comparison mechanism. This plumbing command forms the foundation for examining differences between commits, branches, or any tree-like structures in Git's object database.\n\nThe command accepts one or two tree-ish arguments and outputs the differences in various formats. It can process commits via stdin, enabling efficient batch processing of history. While rarely needed in daily Git use, it underpins many higher-level operations and is essential for scripts and custom diff tools."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-diff](/man/git-diff)(1), [git-diff-index](/man/git-diff-index)(1)"]],"e":["git-diff","git-diff-index"],"x":"git diff-tree [tree1] [tree2]"},"git-rscp":{"n":"git-rscp","t":"Copy files from a remote repository's working directory via rsync","s":[["TAGLINE","Copy files from a remote repository's working directory via rsync"],["TLDR","**Copy specific files from a remote** into the current directory\n\n```git rscp [remote] [file]```\n\n**Copy a remote directory**\n\n```git rscp [remote] [directory]```"],["SYNOPSIS","**git** **rscp** _remote_ _files_..."],["DESCRIPTION","**git rscp** is the reverse direction of git-extras' `git scp`: it copies the given files or directories from a remote's working directory to the current working directory using rsync. It is the same script as `git-scp`, just invoked under a different name, which switches it into \"pull\" mode instead of scp's default \"push and stage\" mode.\n\nThe destination path is derived from the named remote's configured URL, so the remote must already exist in `git remote`."],["CAVEATS","Part of git-extras package; requires `rsync` and, for SSH remotes, `ssh` to be installed. Unlike `git scp`, files copied this way are not automatically staged."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-scp](/man/git-scp)(1), [rsync](/man/rsync)(1), [git-extras](/man/git-extras)(1)"],["RESOURCES","```[Source code](https://github.com/tj/git-extras)```\n\n```[Documentation](https://github.com/tj/git-extras/blob/master/Commands.md#git-scp)```"]],"e":["git-scp","rsync","git-extras"],"x":"git rscp [remote] [file]"},"git-secrets":{"n":"git-secrets","t":"Prevent committing secrets and credentials to Git","s":[["TAGLINE","Prevent committing secrets and credentials to Git"],["TLDR","**Install hooks in repository**\n\n```git secrets --install```\n\n**Add AWS patterns**\n\n```git secrets --register-aws```\n\n**Add custom pattern**\n\n```git secrets --add '[pattern]'```\n\n**Scan repository**\n\n```git secrets --scan```\n\n**Scan specific file**\n\n```git secrets --scan [file.txt]```\n\n**List patterns**\n\n```git secrets --list```"],["SYNOPSIS","**git secrets** _command_ [_options_]"],["PARAMETERS","**--install**\n> Install hooks in current repo.\n\n**--register-aws**\n> Add AWS secret patterns.\n\n**--add** _pattern_\n> Add forbidden pattern.\n\n**--add-allowed** _pattern_\n> Add allowed pattern (exception).\n\n**--scan**\n> Scan repository for secrets.\n\n**--scan-history**\n> Scan entire commit history.\n\n**--list**\n> List registered patterns.\n\n**--add-provider** _cmd_\n> Add secret provider command."],["DESCRIPTION","**git-secrets** prevents committing secrets and credentials to Git repositories. It installs pre-commit hooks that scan staged changes against configurable patterns, blocking commits that match known secret formats.\n\nCreated by AWS Labs, it includes built-in patterns for AWS credentials and supports custom patterns for other types of secrets."],["CONFIGURATION","**~/.git-templates/git-secrets/**\n> Global git template directory for automatic hook installation in new repositories."],["CAVEATS","Must be installed per-repo or via templates. Only catches patterns, not all secrets. History scanning is slow on large repos. Consider tools like gitleaks for comprehensive scanning."],["HISTORY","git-secrets was created by **AWS Labs** to prevent AWS credential leaks. It's one of several tools addressing the widespread problem of secrets accidentally committed to version control."],["INSTALL","```dnf: sudo dnf install git-secrets```\n\n```brew: brew install git-secrets```\n\n```nix: nix profile install nixpkgs#git-secrets```"],["SEE ALSO","[git](/man/git)(1), [gitleaks](/man/gitleaks)(1), [trufflehog](/man/trufflehog)(1), [detect-secrets](/man/detect-secrets)(1)"],["RESOURCES","```[Source code](https://github.com/awslabs/git-secrets)```"]],"e":["git","gitleaks","trufflehog","detect-secrets"],"x":"git secrets --install"},"gnome-extensions":{"n":"gnome-extensions","t":"GNOME Shell extension manager","s":[["TAGLINE","GNOME Shell extension manager"],["TLDR","**List** all the installed extensions\n\n```gnome-extensions list```\n\nDisplay **information** about a specific extension\n\n```gnome-extensions info \"[extension_id]\"```\n\n**Enable** a specific extension\n\n```gnome-extensions enable \"[extension_id]\"```\n\n**Disable** a specific extension\n\n```gnome-extensions disable \"[extension_id]\"```\n\n**Uninstall** a specific extension\n\n```gnome-extensions uninstall \"[extension_id]\"```\n\nDisplay **help** for a specific subcommand\n\n```gnome-extensions help [subcommand]```\n\nDisplay **version**\n\n```gnome-extensions version```"],["SYNOPSIS","**gnome-extensions** _command_ [_options_]"],["PARAMETERS","**list**\n> List installed extensions (--user for user-only, --enabled for enabled-only)\n\n**info** _UUID_\n> Show information about an extension\n\n**enable** _UUID_\n> Enable an extension\n\n**disable** _UUID_\n> Disable an extension\n\n**uninstall** _UUID_\n> Remove an extension\n\n**create**\n> Create a new extension from template\n\n**pack**\n> Package extension for distribution\n\n**install** _BUNDLE_\n> Install extension from bundle file\n\n**prefs** _UUID_\n> Open extension's preferences window"],["DESCRIPTION","**gnome-extensions** is the command-line tool for managing GNOME Shell extensions. Extensions modify GNOME Shell's behavior and appearance, adding features like system monitors, window tiling, or custom themes.\n\nExtensions are identified by UUIDs like **dash-to-dock@micxgx.gmail.com**. They are installed in **~/.local/share/gnome-shell/extensions/** (user) or **/usr/share/gnome-shell/extensions/** (system-wide)."],["CAVEATS","Extensions can cause GNOME Shell instability or crashes. Extensions must be updated for each new GNOME Shell version. Disabling a problematic extension may require logging out or using a TTY."],["HISTORY","gnome-extensions was introduced in GNOME 3.34 (**2019**) as a modern replacement for the older gnome-shell-extension-tool. GNOME Shell extensions themselves have been available since GNOME 3.0 in **2011**."],["INSTALL","```apt: sudo apt install gnome-shell```\n\n```dnf: sudo dnf install gnome-shell```\n\n```pacman: sudo pacman -S gnome-shell```\n\n```apk: sudo apk add gnome-shell```\n\n```zypper: sudo zypper install gnome-shell```\n\n```nix: nix profile install nixpkgs#gnome-shell```"],["SEE ALSO","[dconf](/man/dconf)(1)"]],"e":["dconf"],"x":"gnome-extensions list"},"hostid":{"n":"hostid","t":"displays the current host's unique identifier as a 32-bit hexadecimal value","s":[["TAGLINE","displays the current host's unique identifier as a 32-bit hexadecimal value"],["TLDR","**Display the host ID**\n\n```hostid```\n\n**Show version**\n\n```hostid --version```\n\n**Show help**\n\n```hostid --help```"],["SYNOPSIS","**hostid** [_option_]"],["PARAMETERS","**--help**\n> Display help and exit.\n\n**--version**\n> Display version and exit."],["DESCRIPTION","**hostid** displays the current host's unique identifier as a 32-bit hexadecimal value. If **/etc/hostid** exists, the value stored there is printed; otherwise it is derived from the machine's hostname and IP address via the **gethostid**(3) function. The identifier was historically used by software licensing schemes and is largely a legacy feature on modern Linux systems."],["CAVEATS","The value is **not guaranteed to be unique** across machines, particularly when derived from a hostname rather than a fixed **/etc/hostid** file. It has no security value and should not be relied on as a hardware fingerprint."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[hostname](/man/hostname)(1), [uname](/man/uname)(1)"],["RESOURCES","```[Source code](https://git.savannah.gnu.org/cgit/coreutils.git)```\n\n```[Documentation](https://www.gnu.org/software/coreutils/manual/coreutils.html)```"]],"e":["hostname","uname"],"x":"hostid"},"icloudpd":{"n":"icloudpd","t":"Download photos and videos from iCloud Photos","s":[["TAGLINE","Download photos and videos from iCloud Photos"],["TLDR","**Download** to a directory\n\n```icloudpd --directory [~/Photos] --username [apple_id]```\n\n**Recent** photos only\n\n```icloudpd --directory [~/Photos] --username [user] --recent [100]```\n\n**List** without downloading\n\n```icloudpd --directory [~/Photos] --username [user] --only-print-filenames```"],["SYNOPSIS","**icloudpd** [*options*]"],["DESCRIPTION","**icloudpd** (iCloud Photos Downloader) authenticates to Apple iCloud and downloads the photo library to a local folder, with options for recent items, album filters, and skip-existing behavior. Install via PyPI or Docker images from **icloud-photos-downloader/icloud_photos_downloader**."],["PARAMETERS","**--directory** *path*\n\n> Destination folder.\n\n**--username** *apple_id*\n\n> Apple ID email.\n\n**--recent** *n*\n\n> Limit to *n* most recent assets.\n\n**--until-found** *n*\n\n> Stop after *n* consecutive existing files.\n\nMFA/2FA prompts appear interactively when required. See **icloudpd --help** for thread counts and live-photo handling."],["CAVEATS","Apple authentication and MFA flows change; keep the tool updated. Respect account security—prefer app-specific handling where documented. Large libraries need disk space and time."],["INSTALL","```brew: brew install icloudpd```\n\n```nix: nix profile install nixpkgs#icloudpd```"],["SEE ALSO","[rclone](/man/rclone)(1), [gphotos-uploader-cli](/man/gphotos-uploader-cli)(1)"],["RESOURCES","```[Source code](https://github.com/icloud-photos-downloader/icloud_photos_downloader)```"]],"e":["rclone","gphotos-uploader-cli"],"x":"icloudpd --directory [~/Photos] --username [apple_id]"},"kind":{"n":"kind","t":"runs Kubernetes clusters in Docker containers","s":[["TAGLINE","runs Kubernetes clusters in Docker containers"],["TLDR","**Create cluster**\n\n```kind create cluster```\n\n**Create named cluster**\n\n```kind create cluster --name [my-cluster]```\n\n**Create with config**\n\n```kind create cluster --config [kind-config.yaml]```\n\n**List clusters**\n\n```kind get clusters```\n\n**Delete cluster**\n\n```kind delete cluster --name [my-cluster]```\n\n**Load image to cluster**\n\n```kind load docker-image [myimage:tag] --name [my-cluster]```"],["SYNOPSIS","**kind** _command_ [_options_]"],["PARAMETERS","**create cluster**\n> Create a new cluster. Takes flags such as --name, --config, --image, --kubeconfig, --wait, --retain.\n\n**delete cluster**\n> Delete a cluster by name.\n\n**delete clusters**\n> Delete all kind clusters.\n\n**get clusters**\n> List currently running kind clusters.\n\n**get nodes**\n> List the Docker containers acting as nodes for a given cluster.\n\n**get kubeconfig**\n> Print the kubeconfig for a cluster to stdout.\n\n**export kubeconfig** **--name** _NAME_\n> Merge the cluster's credentials into the active kubeconfig file.\n\n**export logs** _DIR_\n> Export logs from all nodes of the cluster to _DIR_.\n\n**load docker-image** _IMAGE_\n> Sideload a local Docker image into the cluster nodes.\n\n**load image-archive** _FILE_\n> Sideload an image archive (docker save output) into the cluster nodes.\n\n**build node-image**\n> Build a kind node image from Kubernetes source or a release tarball.\n\n**version**\n> Print the kind version.\n\n**--name** _NAME_\n> Cluster name (default **kind**).\n\n**--config** _FILE_\n> Path to a kind configuration YAML file.\n\n**--image** _IMAGE_\n> Node image to use (overrides the default kindest/node image).\n\n**--kubeconfig** _FILE_\n> Write the cluster kubeconfig to _FILE_ instead of the default.\n\n**--wait** _DURATION_\n> Wait up to _DURATION_ for control plane readiness.\n\n**--retain**\n> Keep nodes when cluster creation fails (useful for debugging).\n\n**--help**\n> Display help information."],["DESCRIPTION","**kind** (Kubernetes IN Docker) creates and manages local Kubernetes clusters using Docker containers as nodes. Each cluster node runs as a separate Docker container with systemd, kubelet, and containerd inside it, providing a lightweight alternative to virtual machine-based solutions like minikube for local development and CI/CD testing.\n\nThe tool supports multi-node clusters including multiple control plane nodes for testing high-availability configurations, and accepts YAML configuration files to customize node counts, port mappings, mounted volumes, and Kubernetes feature gates. Local Docker images can be loaded directly into the cluster without pushing to a registry, which accelerates the inner development loop.\n\nkind was created by the Kubernetes SIG Testing team and is used extensively in the Kubernetes project's own CI pipelines. It is optimized for fast cluster creation and teardown, making it well-suited for automated testing workflows, but is not intended for production use."],["CAVEATS","Requires Docker. Development focused. Not for production."],["HISTORY","kind (Kubernetes IN Docker) was created by the **Kubernetes** SIG Testing as a tool for testing Kubernetes itself."],["INSTALL","```apt: sudo apt install kind```\n\n```dnf: sudo dnf install kind```\n\n```pacman: sudo pacman -S kind```\n\n```apk: sudo apk add kind```\n\n```zypper: sudo zypper install kind```\n\n```brew: brew install kind```\n\n```nix: nix profile install nixpkgs#kind```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [k3d](/man/k3d)(1), [minikube](/man/minikube)(1), [docker](/man/docker)(1)"]],"e":["kubectl","k3d","minikube","docker"],"x":"kind create cluster"},"kubectl-attach":{"n":"kubectl-attach","t":"attaches to a running container in a pod","s":[["TAGLINE","attaches to a running container in a pod"],["TLDR","**Attach to running pod**\n\n```kubectl attach [pod-name]```\n\n**Attach to specific container in pod**\n\n```kubectl attach [pod-name] -c [container-name]```\n\n**Interactive attach with TTY**\n\n```kubectl attach [pod-name] -c [container-name] -i -t```\n\n**Attach to first pod of a replica set**\n\n```kubectl attach rs/[nginx]```\n\n**Attach within a namespace**\n\n```kubectl attach [pod-name] -n [namespace]```"],["SYNOPSIS","**kubectl attach** (_POD_ | _TYPE/NAME_) [**-c** _CONTAINER_] [_options_]"],["PARAMETERS","**-c**, **--container** _name_\n> Container name. If omitted, uses the `kubectl.kubernetes.io/default-container` annotation or the first container in the pod.\n\n**-i**, **--stdin**\n> Pass stdin to the container.\n\n**-t**, **--tty**\n> Stdin is a TTY (allocate a terminal).\n\n**-q**, **--quiet**\n> Only print output from the remote session.\n\n**--pod-running-timeout** _duration_\n> Time to wait until at least one pod is running (default: 1m0s).\n\n**-n**, **--namespace** _name_\n> Namespace scope for the request.\n\n**--context** _name_\n> Kubeconfig context to use.\n\n**--kubeconfig** _path_\n> Path to the kubeconfig file."],["DESCRIPTION","**kubectl attach** connects your terminal to the stdin/stdout/stderr streams of a process already running inside a container. Unlike **kubectl exec**, which spawns a new process in the container, **attach** hooks into the container's PID 1 (the main command defined in the image or pod spec), so you see the same output as the container's log stream and can send input to that process when it was started with stdin open.\n\nUse **-i -t** together to get a full interactive shell experience when the container was started with an interactive command (e.g., a pod created with `kubectl run -it`). To attach to a specific container in a multi-container pod, pass **-c**. Detach with the escape sequence **Ctrl-P Ctrl-Q** to leave the process running."],["CAVEATS","The target container must have been started with stdin open (**stdin: true** in the pod spec or **-i** on `kubectl run`) for input to be delivered. Attaching does not restart the process; closing the connection improperly with Ctrl-C may signal the process. Only one attach session per container stream is reliable."],["INSTALL","```apt: sudo apt install kubectl```\n\n```pacman: sudo pacman -S kubectl```\n\n```apk: sudo apk add kubectl```\n\n```brew: brew install kubectl```\n\n```nix: nix profile install nixpkgs#kubectl```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [kubectl-exec](/man/kubectl-exec)(1), [kubectl-logs](/man/kubectl-logs)(1), [kubectl-run](/man/kubectl-run)(1)"]],"e":["kubectl","kubectl-exec","kubectl-logs","kubectl-run"],"x":"kubectl attach [pod-name]"},"kubectl-certificate":{"n":"kubectl-certificate","t":"Manage certificate signing requests in Kubernetes","s":[["TAGLINE","Manage certificate signing requests in Kubernetes"],["TLDR","**Approve a certificate signing request**\n\n```kubectl certificate approve [csr-name]```\n\n**Deny a certificate signing request**\n\n```kubectl certificate deny [csr-name]```\n\n**Approve all pending certificate signing requests**\n\n```kubectl get csr -o name | xargs kubectl certificate approve```\n\n**Approve a CSR with a specific reason**\n\n```kubectl certificate deny [csr-name] --reason=\"[Expired certificate]\"```\n\n**List certificate signing requests**\n\n```kubectl get csr```"],["SYNOPSIS","**kubectl** **certificate** _subcommand_ [_options_]"],["PARAMETERS","**approve** _name_\n> Approve a certificate signing request.\n\n**deny** _name_\n> Deny a certificate signing request.\n\n**--reason** _string_\n> Reason for approving or denying the CSR, stored in the status condition.\n\n**--force**\n> Update the CSR even if it is already approved or denied.\n\n**--allow-missing-template-keys**\n> If true, ignore errors in templates when a field or map key is missing. Default true.\n\n**-o, --output** _format_\n> Output format. One of: json, yaml, name, go-template, go-template-file, template, templatefile, jsonpath, jsonpath-as-json, jsonpath-file."],["DESCRIPTION","**kubectl certificate** manages certificate signing requests (CSRs) in Kubernetes. Used to approve or deny CSRs for kubelet client certificates and other TLS certificates. Part of the cluster's PKI management. When a new node joins a cluster, it generates a CSR that must be approved before the node can communicate with the API server."],["INSTALL","```apt: sudo apt install kubectl```\n\n```pacman: sudo pacman -S kubectl```\n\n```apk: sudo apk add kubectl```\n\n```brew: brew install kubectl```\n\n```nix: nix profile install nixpkgs#kubectl```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [kubectl-get](/man/kubectl-get)(1), [kubectl-auth](/man/kubectl-auth)(1), [kubectl-config](/man/kubectl-config)(1)"]],"e":["kubectl","kubectl-get","kubectl-auth","kubectl-config"],"x":"kubectl certificate approve [csr-name]"},"lsusb":{"n":"lsusb","t":"Display information about USB buses and connected devices","s":[["TAGLINE","Display information about USB buses and connected devices"],["TLDR","List all **USB devices**\n\n```lsusb```\n\nList USB hierarchy as **tree**\n\n```lsusb -t```\n\nList **verbose** information\n\n```sudo lsusb -v```\n\nShow details for **specific device**\n\n```sudo lsusb -v -s 001:002```\n\nFilter by **vendor:product** ID\n\n```lsusb -d 1234:5678```"],["SYNOPSIS","**lsusb** [_options_]"],["DESCRIPTION","**lsusb** displays information about USB buses in the system and devices connected to them. It uses udev's hardware database to provide human-readable vendor and product names."],["PARAMETERS","**-v, --verbose**\n> Display detailed device information including configuration and class descriptors\n\n**-s [[bus]:][devnum]**\n> Show only devices on specified bus and/or device number (decimal)\n\n**-d [vendor]:[product]**\n> Show only devices with specified vendor/product ID (hexadecimal)\n\n**-D** _device_\n> Display information about a specific device file (e.g., /dev/bus/usb/001/002)\n\n**-t, --tree**\n> Display USB device hierarchy as tree\n\n**-V, --version**\n> Display version information"],["CAVEATS","Verbose output (**-v**) and some device details require root privileges. Device numbers are in decimal format, while vendor/product IDs are in hexadecimal."],["HISTORY","**lsusb** is part of the **usbutils** package, providing command-line USB device information since the early days of USB support in Linux."],["INSTALL","```apt: sudo apt install usbutils```\n\n```dnf: sudo dnf install usbutils```\n\n```pacman: sudo pacman -S usbutils```\n\n```apk: sudo apk add usbutils```\n\n```zypper: sudo zypper install usbutils```\n\n```brew: brew install usbutils```\n\n```nix: nix profile install nixpkgs#usbutils```"],["SEE ALSO","[lspci](/man/lspci)(8), [udevadm](/man/udevadm)(8)"]],"e":["lspci","udevadm"],"x":"lsusb"},"lvmdevices":{"n":"lvmdevices","t":"manages the LVM devices file that controls which block devices LVM can","s":[["TAGLINE","manages the LVM devices file that controls which block devices LVM can"],["TLDR","**List devices** recorded in the devices file\n\n```sudo lvmdevices```\n\n**Add a device** to the devices file\n\n```sudo lvmdevices --adddev [/dev/sdXN]```\n\n**Remove a device** from the devices file\n\n```sudo lvmdevices --deldev [/dev/sdXN]```\n\n**Add a physical volume** by its PVID\n\n```sudo lvmdevices --addpvid [PVID]```\n\n**Remove a physical volume** by its PVID\n\n```sudo lvmdevices --delpvid [PVID]```\n\n**Update** the devices file after device names change\n\n```sudo lvmdevices --update```\n\n**Check** the devices file for problems\n\n```sudo lvmdevices --check```\n\nDisplay **version**\n\n```lvmdevices --version```"],["SYNOPSIS","**lvmdevices** [_options_]"],["PARAMETERS","**--adddev _device_**\n> Add a device to the LVM devices file by path\n\n**--deldev _device_**\n> Remove a device from the devices file by path\n\n**--addpvid _PVID_**\n> Add a physical volume by its unique PVID\n\n**--delpvid _PVID_**\n> Remove a physical volume by its PVID\n\n**--update**\n> Update the devices file to reflect changed device names\n\n**--check**\n> Verify the devices file and report problems\n\n**--version**\n> Display version information"],["DESCRIPTION","**lvmdevices** manages the LVM devices file that controls which block devices LVM can use as physical volumes. Located at **/etc/lvm/devices/system.devices**, this file restricts LVM to scanning only listed devices, improving performance and preventing accidental use of unintended disks.\n\nWhen devices are added or removed, or when device names change (e.g., after hardware changes), lvmdevices can update the devices file accordingly. The **--check** option validates that listed devices exist and are accessible.\n\nThe devices file uses stable device identifiers (WWID, serial numbers) rather than potentially changing paths like /dev/sdX, ensuring consistent behavior across reboots."],["CAVEATS","Requires root privileges. If the devices file is enabled but a device isn't listed, LVM will ignore that device even if it contains valid PV metadata. Use **--update** after hardware changes to ensure device paths are current."],["INSTALL","```dnf: sudo dnf install lvm2```\n\n```pacman: sudo pacman -S lvm2```\n\n```apk: sudo apk add lvm2```\n\n```zypper: sudo zypper install lvm2```\n\n```nix: nix profile install nixpkgs#lvm2```"],["SEE ALSO","[lvm](/man/lvm)(8), [lvmconfig](/man/lvmconfig)(8), [pvcreate](/man/pvcreate)(8), [pvs](/man/pvs)(8), [vgchange](/man/vgchange)(8)"]],"e":["lvm","lvmconfig","pvcreate","pvs","vgchange"],"x":"sudo lvmdevices"},"magento":{"n":"magento","t":"command-line interface for Magento/Adobe Commerce e-commerce platform","s":[["TAGLINE","command-line interface for Magento/Adobe Commerce e-commerce platform"],["TLDR","**List available commands**\n\n```magento list```\n\n**Enable maintenance mode**\n\n```magento maintenance:enable```\n\n**Disable maintenance mode**\n\n```magento maintenance:disable```\n\n**Clear cache**\n\n```magento cache:clean```\n\n**Flush cache**\n\n```magento cache:flush```\n\n**Reindex all**\n\n```magento indexer:reindex```\n\n**Compile dependency injection**\n\n```magento setup:di:compile```\n\n**Deploy static content**\n\n```magento setup:static-content:deploy```"],["SYNOPSIS","**magento** [_command_] [_--help_] [_options_] [_arguments_]"],["PARAMETERS","**cache:clean**\n> Clean cache types.\n\n**cache:flush**\n> Flush cache storage.\n\n**cache:status**\n> Show cache status.\n\n**cache:enable** _TYPE_\n> Enable cache types.\n\n**cache:disable** _TYPE_\n> Disable cache types.\n\n**indexer:reindex**\n> Reindex all indexers.\n\n**indexer:status**\n> Show indexer status.\n\n**maintenance:enable**\n> Enable maintenance mode.\n\n**maintenance:disable**\n> Disable maintenance mode.\n\n**setup:upgrade**\n> Upgrade database schema.\n\n**setup:di:compile**\n> Compile dependency injection.\n\n**setup:static-content:deploy**\n> Deploy static view files.\n\n**module:enable** _MODULE_\n> Enable module.\n\n**module:disable** _MODULE_\n> Disable module.\n\n**deploy:mode:set** _MODE_\n> Set application mode (developer, production)."],["DESCRIPTION","**magento** is the command-line interface for Magento/Adobe Commerce e-commerce platform. It manages store operations, deployments, and maintenance tasks.\n\nCache management is critical for performance. Clean removes specific cached data while flush clears all storage. Different cache types (config, layout, block_html, collections, etc.) can be targeted individually.\n\nThe deployment process involves dependency injection compilation, static content deployment, and database upgrades. These steps are required after code changes or module installations.\n\nIndexers keep derived data synchronized with source data. Reindexing is needed after catalog changes, price updates, or inventory modifications.\n\nMaintenance mode shows a service unavailable page to customers while allowing administrators to work. IP exceptions can be configured."],["CAVEATS","Commands must run from Magento root directory. File permissions critical - run as web server user. Static content deployment takes time on large catalogs. Memory limits may need increasing for large stores."],["HISTORY","**Magento** was founded in **2008** by **Varien** and later acquired by **eBay** in **2011**, then spun off as an independent company. **Adobe** acquired Magento in **2018**. The CLI was introduced with Magento 2 in **2015**, replacing the earlier admin-based configuration approach."],["SEE ALSO","[composer](/man/composer)(1), [php](/man/php)(1), [mysql](/man/mysql)(1), [nginx](/man/nginx)(8)"]],"e":["composer","php","mysql","nginx"],"x":"magento list"},"merge":{"n":"merge","t":"performs three-way file merge","s":[["TAGLINE","performs three-way file merge"],["TLDR","**Three-way merge**\n\n```merge [file] [base] [other]```\n\n**Output to different file**\n\n```merge -p [file] [base] [other] > [merged]```\n\n**Quiet mode**\n\n```merge -q [file] [base] [other]```\n\n**Show conflict markers** using diff3 style\n\n```merge -A [file] [base] [other]```\n\n**Merge with custom labels** for conflict markers\n\n```merge -L [mine] -L [base] -L [theirs] [file] [base] [other]```"],["SYNOPSIS","**merge** [_options_] _file1_ _file2_ _file3_"],["PARAMETERS","_FILE1_\n> File to receive changes.\n\n_FILE2_\n> Common ancestor file.\n\n_FILE3_\n> File with changes.\n\n**-p**\n> Print to stdout.\n\n**-q**\n> Quiet mode.\n\n**-A**\n> Output conflicts using diff3 -A style (most verbose).\n\n**-E**\n> Output conflicts using diff3 -E style (default).\n\n**-e**\n> Like -E, but does not warn about conflicts.\n\n**-L** _LABEL_\n> Use label instead of filename in conflict markers. May be given up to three times.\n\n**-V**\n> Print RCS version number."],["DESCRIPTION","**merge** performs three-way file merge. It incorporates changes from _file3_ (relative to _file2_) into _file1_. The common ancestor _file2_ is used to determine which changes were made in each version.\n\nThe tool is part of the RCS (Revision Control System) suite. When conflicts occur, merge writes conflict markers into _file1_ for manual resolution. The exit status is 0 for no conflicts, 1 if conflicts were found, or 2 for trouble."],["CAVEATS","Part of RCS. Conflicts need manual resolution. Exit code indicates conflicts."],["HISTORY","merge is part of **RCS** (Revision Control System), providing three-way file merging capability."],["INSTALL","```apt: sudo apt install rcs```\n\n```dnf: sudo dnf install rcs```\n\n```pacman: sudo pacman -S rcs```\n\n```apk: sudo apk add rcs```\n\n```zypper: sudo zypper install rcs```\n\n```brew: brew install rcs```\n\n```nix: nix profile install nixpkgs#rcs```"],["SEE ALSO","[diff3](/man/diff3)(1), [diff](/man/diff)(1), [patch](/man/patch)(1), [rcs](/man/rcs)(1), [sdiff](/man/sdiff)(1), [git-merge](/man/git-merge)(1)"]],"e":["diff3","diff","patch","rcs","sdiff","git-merge"],"x":"merge [file] [base] [other]"},"mirrord":{"n":"mirrord","t":"Run local code in the context of a Kubernetes cluster","s":[["TAGLINE","Run local code in the context of a Kubernetes cluster"],["TLDR","**Run a local binary** as if it were a specific pod\n\n```mirrord exec --target pod/[pod_name] -- [command] [arguments]```\n\n**Target a deployment** in a specific namespace\n\n```mirrord exec --target deployment/[deployment_name] --target-namespace [namespace] -- [command]```\n\n**Run with a configuration file**\n\n```mirrord exec --config-file [path/to/mirrord.json] -- [command]```\n\n**Forward a remote port** to a locally reachable host\n\n```mirrord port-forward --target pod/[pod_name] --port-mapping [local_port]:[remote_port]```\n\n**Dump incoming traffic** of a target's port\n\n```mirrord dump --target pod/[pod_name] --ports [8080]```\n\n**Generate a configuration** interactively\n\n```mirrord wizard```\n\n**Check setup and connectivity**\n\n```mirrord diagnose```"],["SYNOPSIS","**mirrord** _command_ [_options_] [**--** _binary_ [_args_]]"],["DESCRIPTION","**mirrord** lets a process running on your own machine behave as if it were running inside a remote Kubernetes cluster. It connects the local process to a chosen target (a pod or a deployment) and transparently relays that target's network traffic, environment variables, DNS resolution, and file system, so the local code observes the same context as the real workload without being built into an image, pushed, and deployed.\n\nIt works by spawning a temporary agent pod on the same node as the target. Incoming traffic for the target can be **mirrored** (a copy is sent to the local process while the cluster keeps serving) or **stolen** (traffic is redirected to the local process instead). Outgoing traffic, DNS, and file reads are tunnelled through the agent so they resolve against the cluster. When the local process exits, the agent is cleaned up and the cluster returns to its previous state.\n\nmirrord is most often used through its **exec** subcommand, which wraps an arbitrary command, but it also ships editor integrations for VS Code and IntelliJ that drive the same engine. It is typically installed from a shell with the upstream install script (`curl -fsSL https://raw.githubusercontent.com/metalbear-co/mirrord/main/scripts/install.sh | bash`) or via Homebrew."],["COMMANDS","**exec**\n\n> Run a local binary in the context of a target. Everything after **--** is the command and its arguments.\n\n**port-forward**\n\n> Forward one or more cluster ports to a host reachable from the local machine.\n\n**dump**\n\n> Capture and print incoming traffic seen by a target's port, without running a local binary.\n\n**container**\n\n> Run a container locally in the cluster's context, mirroring what **exec** does for a plain binary.\n\n**operator**\n\n> Manage the optional mirrord Operator, which enables team features such as queue splitting and shared targets.\n\n**wizard**\n\n> Interactively build a mirrord configuration file.\n\n**diagnose**\n\n> Run connectivity and latency checks against the cluster to verify the setup."],["PARAMETERS","These apply to **exec** (and most other subcommands that take a target).\n\n**-t**, **--target** _path_\n\n> Target to impersonate, for example `pod/my-pod`, `deployment/my-deploy`, or `pod/my-pod/container/sidecar`.\n\n**-n**, **--target-namespace** _namespace_\n\n> Namespace of the target. Defaults to the current kubectl context namespace.\n\n**-f**, **--config-file** _file_\n\n> Load options from a JSON, TOML, or YAML configuration file instead of flags.\n\n**-a**, **--agent-namespace** _namespace_\n\n> Namespace in which the temporary mirrord agent pod is created.\n\n**--steal**\n\n> Redirect (steal) the target's incoming traffic to the local process instead of mirroring a copy of it.\n\n**--fs-mode** _mode_\n\n> Control how file system access is handled (for example read from the remote, the local machine, or both).\n\n**-h**, **--help**\n\n> Show help for mirrord or for a specific subcommand."],["CAVEATS","mirrord needs access to a Kubernetes cluster and permission to create the temporary agent pod, so the same care that applies to `kubectl exec` applies here. Stealing traffic with **--steal** diverts real requests away from the cluster workload, which can disrupt a shared environment; mirroring is safer for that reason. File and network behaviour can differ subtly from a true in-cluster deployment, so it complements rather than replaces staging tests."],["HISTORY","mirrord is developed by **MetalBear** and released under the **MIT** license. It is written largely in **Rust** and grew from the idea of removing the slow build-push-deploy loop from cloud-native development, letting developers debug against a live cluster from their local machine. Adopters cited by the project include monday.com, SurveyMonkey, and Cadence."],["INSTALL","```nix: nix profile install nixpkgs#mirrord```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [docker](/man/docker)(1), [skaffold](/man/skaffold)(1), [helm](/man/helm)(1)"],["RESOURCES","```[Source code](https://github.com/metalbear-co/mirrord)```\n\n```[Homepage](https://metalbear.com/mirrord)```\n\n```[Documentation](https://metalbear.com/mirrord/docs)```"]],"e":["kubectl","docker","skaffold","helm"],"x":"mirrord exec --target pod/[pod_name] -- [command] [arguments]"},"mvn-dependency":{"n":"mvn-dependency","t":"goals manage Maven dependencies","s":[["TAGLINE","goals manage Maven dependencies"],["TLDR","**Show dependency tree**\n\n```mvn dependency:tree```\n\n**Analyze dependencies**\n\n```mvn dependency:analyze```\n\n**List dependencies**\n\n```mvn dependency:list```\n\n**Copy dependencies**\n\n```mvn dependency:copy-dependencies```\n\n**Resolve dependencies**\n\n```mvn dependency:resolve```\n\n**Verbose tree** showing conflict resolution\n\n```mvn dependency:tree -Dverbose```\n\n**Filter tree by scope**\n\n```mvn dependency:tree -Dscope=[compile]```\n\n**Purge local copies** and re-resolve\n\n```mvn dependency:purge-local-repository```"],["SYNOPSIS","**mvn** **dependency:**_goal_ [_options_]"],["PARAMETERS","_GOAL_\n> Dependency plugin goal.\n\n**tree**\n> Display dependency tree.\n\n**analyze**\n> Analyze used/unused deps.\n\n**list**\n> List all dependencies.\n\n**copy-dependencies**\n> Copy deps to target.\n\n**resolve**\n> Resolve and download all dependencies to the local repository.\n\n**purge-local-repository**\n> Remove project dependencies from the local repository and optionally re-resolve them.\n\n**-Dscope** _SCOPE_\n> Filter by dependency scope (compile, runtime, test, provided, system).\n\n**-Dincludes** _PATTERN_\n> Filter output to matching artifacts (groupId:artifactId:type:version pattern, supports wildcards).\n\n**-DoutputFile** _FILE_\n> Write output to a file instead of stdout (supported by tree, list, resolve)."],["DESCRIPTION","The **maven-dependency-plugin** provides goals for analyzing, listing, copying, and resolving Maven project dependencies. The **tree** goal is the most commonly used, displaying the full transitive dependency tree with conflict resolution details when **-Dverbose** is enabled. The **analyze** goal identifies dependencies that are declared but unused, or used but not explicitly declared."],["CAVEATS","Requires a valid **pom.xml**. Network access is needed for initial resolution. The **analyze** goal cannot detect dependencies used only via reflection."],["HISTORY","The Maven Dependency Plugin is part of **Apache Maven**, providing dependency analysis since early Maven versions."],["INSTALL","```dnf: sudo dnf install maven```\n\n```pacman: sudo pacman -S maven```\n\n```apk: sudo apk add maven```\n\n```zypper: sudo zypper install maven```\n\n```brew: brew install maven```\n\n```nix: nix profile install nixpkgs#maven```"],["SEE ALSO","[mvn](/man/mvn)(1), [mvn-deploy](/man/mvn-deploy)(1), [gradle](/man/gradle)(1)"]],"e":["mvn","mvn-deploy","gradle"],"x":"mvn dependency:tree"},"mycli":{"n":"mycli","t":"MySQL client with autocompletion and syntax highlighting","s":[["TAGLINE","MySQL client with autocompletion and syntax highlighting"],["TLDR","**Connect to database**\n\n```mycli -u [username] [database]```\n\n**Connect to remote host**\n\n```mycli -h [hostname] -u [username] [database]```\n\n**Connect with password prompt**\n\n```mycli -u [username] -p [database]```\n\n**Execute SQL file**\n\n```mycli -u [user] [database] < [script.sql]```\n\n**Connect via socket**\n\n```mycli -S [/var/run/mysqld/mysqld.sock] -u [user]```\n\n**Use specific port**\n\n```mycli -h [host] -P [3306] -u [user] [database]```\n\n**Execute a query and exit**\n\n```mycli -u [user] -e \"SELECT * FROM [table]\" [database]```\n\n**Output results as CSV**\n\n```mycli -u [user] -e \"SELECT * FROM [table]\" --csv [database]```"],["SYNOPSIS","**mycli** [_options_] [_database_]"],["PARAMETERS","_DATABASE_\n> Database name to connect.\n\n**-h** _HOST_\n> MySQL server hostname.\n\n**-u** _USER_\n> Username.\n\n**-p** [_PASSWORD_]\n> Password (prompt if no value).\n\n**-P** _PORT_\n> Port number.\n\n**-S** _SOCKET_\n> Unix socket path.\n\n**-D** _DATABASE_\n> Database name to connect to.\n\n**-e** _COMMAND_\n> Execute command and quit.\n\n**-R** _PROMPT_\n> Customize the prompt format.\n\n**--csv**\n> Output results in CSV format (batch mode).\n\n**--table**\n> Output results in table format (batch mode).\n\n**--auto-vertical-output**\n> Automatically switch to vertical output when result is wider than terminal.\n\n**-d** _DSN_\n> Use DSN configured in the [alias_dsn] section of myclirc file.\n\n**-l** _FILE_\n> Log every query and its results to a file.\n\n**-v**, **--verbose**\n> Verbose output.\n\n**--myclirc** _FILE_\n> Path to configuration file (default: ~/.myclirc).\n\n**--defaults-file** _FILE_\n> Only read MySQL options from the given file.\n\n**--warn** / **--no-warn**\n> Warn before running a destructive query.\n\n**--ssh-host** _HOST_\n> Host name to connect to ssh server.\n\n**--ssh-port** _PORT_\n> Port to connect to ssh server.\n\n**--ssh-user** _USER_\n> User name to connect to ssh server.\n\n**--ssh-key-filename** _FILE_\n> Private key filename for the ssh connection.\n\n**--ssl-ca** _PATH_\n> CA file in PEM format.\n\n**--ssl-cert** _PATH_\n> X509 cert in PEM format.\n\n**--ssl-key** _PATH_\n> X509 key in PEM format.\n\n**-V**, **--version**\n> Display version information.\n\n**--help**\n> Display help information."],["DESCRIPTION","**mycli** is a command-line client for MySQL, MariaDB, and Percona with smart autocompletion and syntax highlighting. It provides context-aware completion for SQL keywords, table names, column names, and functions as you type."],["CAVEATS","Python-based; requires a running MySQL/MariaDB/Percona server to connect to. Configuration is stored in ~/.myclirc."],["HISTORY","mycli was created by **Amjith Ramanujam** as a user-friendly MySQL CLI with autocompletion, inspired by **pgcli**."],["INSTALL","```dnf: sudo dnf install mycli```\n\n```apk: sudo apk add mycli```\n\n```brew: brew install mycli```\n\n```nix: nix profile install nixpkgs#mycli```"],["SEE ALSO","[mysql](/man/mysql)(1), [mariadb](/man/mariadb)(1), [mysqldump](/man/mysqldump)(1), [pgcli](/man/pgcli)(1), [litecli](/man/litecli)(1)"]],"e":["mysql","mariadb","mysqldump","pgcli","litecli"],"x":"mycli -u [username] [database]"},"nimble":{"n":"nimble","t":"nim package manager","s":[["TAGLINE","nim package manager"],["TLDR","**Install package**\n\n```nimble install [package]```\n\n**Build project**\n\n```nimble build```\n\n**Run project**\n\n```nimble run```\n\n**Initialize project**\n\n```nimble init```\n\n**Search packages**\n\n```nimble search [query]```\n\n**List installed packages**\n\n```nimble list --installed```\n\n**Update packages**\n\n```nimble refresh```\n\n**Run tests**\n\n```nimble test```"],["SYNOPSIS","**nimble** [_command_] [_options_]"],["PARAMETERS","_COMMAND_\n> Nimble command.\n\n**install** _PKG_\n> Install package.\n\n**build**\n> Build current project.\n\n**run**\n> Build and run.\n\n**init**\n> Initialize new project.\n\n**search** _QUERY_\n> Search packages.\n\n**test**\n> Run tests.\n\n**--help**\n> Display help information."],["DESCRIPTION","**nimble** is the Nim package manager. It installs dependencies and manages projects.\n\nThe tool handles package installation. Supports local and remote packages."],["CAVEATS","Requires Nim. Package registry based. nimble.directory for packages."],["HISTORY","Nimble was created as the **official package manager** for the Nim programming language."],["INSTALL","```apk: sudo apk add nimble```\n\n```nix: nix profile install nixpkgs#nimble```"],["SEE ALSO","[nim](/man/nim)(1), [npm](/man/npm)(1), [cargo](/man/cargo)(1)"]],"e":["nim","npm","cargo"],"x":"nimble install [package]"},"nmcli-agent":{"n":"nmcli-agent","t":"runs nmcli as an agent daemon that responds to NetworkManager's","s":[["TAGLINE","runs nmcli as an agent daemon that responds to NetworkManager's"],["TLDR","Register nmcli as a **secret agent** and listen for secret requests\n\n```nmcli agent secret```\n\nRegister nmcli as a **polkit agent** and listen for authorization requests\n\n```nmcli agent polkit```\n\nRegister nmcli as **both secret and polkit agent**\n\n```nmcli agent all```"],["SYNOPSIS","**nmcli agent** _command_"],["PARAMETERS","**secret, s**\n> Run as a NetworkManager secret agent to provide passwords/credentials\n\n**polkit, p**\n> Run as a polkit agent to handle authorization requests\n\n**all, a**\n> Run as both secret and polkit agent"],["DESCRIPTION","**nmcli agent** runs nmcli as an agent daemon that responds to NetworkManager's requests for secrets (passwords, keys) or polkit authorization requests. This is useful in environments without a desktop secret agent or polkit agent.\n\nAs a **secret agent**, nmcli can provide Wi-Fi passwords, VPN credentials, and other secrets when NetworkManager needs them to establish connections. It prompts the user in the terminal for required credentials.\n\nAs a **polkit agent**, nmcli handles authorization prompts for privileged NetworkManager operations, allowing command-line authentication without a graphical polkit agent."],["CAVEATS","The agent runs in the foreground and must remain running to handle requests. Only one secret agent or polkit agent of each type can be registered at a time. Primarily useful for headless systems or terminal-only environments."],["INSTALL","```apt: sudo apt install network-manager```\n\n```apk: sudo apk add networkmanager-cli```"],["SEE ALSO","[nmcli](/man/nmcli)(1), [nmcli-general](/man/nmcli-general)(1), [NetworkManager](/man/NetworkManager)(8), [polkit](/man/polkit)(8)"]],"e":["nmcli","nmcli-general","NetworkManager","polkit"],"x":"nmcli agent secret"},"ods-server":{"n":"ods-server","t":"openDNSSEC signer daemon","s":[["TAGLINE","openDNSSEC signer daemon"],["TLDR","**Start ODS server**\n\n```ods-server```\n\n**Start on specific port**\n\n```ods-server -p [port]```\n\n**Start with configuration**\n\n```ods-server -c [config.conf]```\n\n**Run in foreground**\n\n```ods-server -f```"],["SYNOPSIS","**ods-server** [_options_]"],["PARAMETERS","**-p** _PORT_\n> Listen port.\n\n**-c** _FILE_\n> Configuration file.\n\n**-f**\n> Run in foreground.\n\n**-d**\n> Debug mode.\n\n**--help**\n> Display help information."],["DESCRIPTION","**ods-server** is the OpenDNSSEC signer daemon. Signs DNS zones with DNSSEC.\n\nThe daemon handles zone signing operations. Part of OpenDNSSEC suite."],["CAVEATS","Part of OpenDNSSEC. Requires proper configuration. Works with KASP policies."],["HISTORY","ods-server was developed as part of **OpenDNSSEC** for automated DNSSEC signing."],["SEE ALSO","[ods-control](/man/ods-control)(1), [ods-enforcer](/man/ods-enforcer)(1), [named](/man/named)(1)"]],"e":["ods-control","ods-enforcer","named"],"x":"ods-server"},"omp":{"n":"omp","t":"AI coding agent for the terminal (Oh My Pi)","s":[["TAGLINE","AI coding agent for the terminal (Oh My Pi)"],["TLDR","**Start** the agent\n\n```omp```\n\n**Install** (Linux/macOS script)\n\n```curl -fsSL https://omp.sh/install | sh```\n\n**Generate shell completions** (zsh)\n\n```eval \"$(omp completions zsh)\"```\n\n**Generate shell completions** (bash)\n\n```eval \"$(omp completions bash)\"```"],["SYNOPSIS","**omp** [*options*] [*prompt*|*subcommand*]"],["DESCRIPTION","**omp** is the CLI for **oh-my-pi**, an open AI coding agent focused on terminal workflows: hash-anchored edits, tool harness, LSP, browser tools, subagents, and multi-provider model support. Install via **omp.sh**, Homebrew (**can1357/tap/omp**), or **bun install -g @oh-my-pi/pi-coding-agent**.\n\n**omp completions** emits completion scripts for bash, zsh, and fish from live command metadata so completions stay aligned with flags and models."],["PARAMETERS","**omp**\n\n> Start interactive agent session (default).\n\n**completions** *shell*\n\n> Print completion script for **bash**, **zsh**, or **fish**.\n\nOther subcommands and flags (**--model**, session resume, planning modes, etc.) evolve quickly—use **omp --help** for the installed version."],["CAVEATS","Requires API keys or local model endpoints depending on configuration. Agent tools can modify files and run shell commands—use in trusted directories. Unrelated tools also use the letters \"omp\" (e.g. some prompt themes); ensure the binary on PATH is oh-my-pi's **omp**."],["INSTALL","```aur: yay -S omp```"],["SEE ALSO","[gemini](/man/gemini)(1), [claude](/man/claude)(1), [aider](/man/aider)(1)"],["RESOURCES","```[Source code](https://github.com/can1357/oh-my-pi)```\n\n```[Homepage](https://omp.sh)```"]],"e":["gemini","claude","aider"],"x":"omp"},"openstack-port":{"n":"openstack-port","t":"manages network ports in OpenStack Neutron","s":[["TAGLINE","manages network ports in OpenStack Neutron"],["TLDR","**List ports**\n\n```openstack port list```\n\n**Create port**\n\n```openstack port create --network [network] [port_name]```\n\n**Show port details**\n\n```openstack port show [port_name]```\n\n**Delete port**\n\n```openstack port delete [port_name]```"],["SYNOPSIS","**openstack port** _command_ [_options_]"],["PARAMETERS","**list**\n> List ports.\n\n**create** _name_\n> Create port.\n\n**delete** _name_\n> Delete port.\n\n**show** _name_\n> Show port details.\n\n**--network** _network_\n> Network for port.\n\n**--fixed-ip** _ip_\n> Fixed IP address.\n\n**--security-group** _group_\n> Security group.\n\n**--mac-address** _mac_\n> MAC address."],["DESCRIPTION","**openstack port** manages network ports in OpenStack Neutron. Ports represent virtual network interfaces attached to instances. Part of OpenStack unified CLI."],["SEE ALSO","[openstack-network](/man/openstack-network)(1), [openstack-server](/man/openstack-server)(1)"]],"e":["openstack-network","openstack-server"],"x":"openstack port list"},"pandoc":{"n":"pandoc","t":"converts document formats","s":[["TAGLINE","converts document formats"],["TLDR","**Convert Markdown to HTML**\n\n```pandoc [input.md] -o [output.html]```\n\n**Convert to PDF**\n\n```pandoc [input.md] -o [output.pdf]```\n\n**Convert to Word**\n\n```pandoc [input.md] -o [output.docx]```\n\n**Specify input format**\n\n```pandoc -f [latex] -t [html] [input.tex] -o [output.html]```\n\n**With table of contents**\n\n```pandoc [input.md] --toc -o [output.html]```\n\n**Use template**\n\n```pandoc [input.md] --template=[template.html] -o [output.html]```"],["SYNOPSIS","**pandoc** [_options_] [_input_...]"],["PARAMETERS","_INPUT_\n> Input file(s).\n\n**-o** _FILE_\n> Output file.\n\n**-f** _FORMAT_\n> Input format.\n\n**-t** _FORMAT_\n> Output format.\n\n**--toc**\n> Include table of contents.\n\n**--template** _FILE_\n> Use template.\n\n**--help**\n> Display help information."],["DESCRIPTION","**pandoc** converts document formats. Universal document converter.\n\nThe tool handles many formats. Markdown, LaTeX, HTML, DOCX, PDF, and more."],["CAVEATS","PDF output requires LaTeX or wkhtmltopdf. Format support varies."],["HISTORY","Pandoc was created by **John MacFarlane** as a universal document converter."],["INSTALL","```apt: sudo apt install pandoc```\n\n```dnf: sudo dnf install pandoc-cli```\n\n```pacman: sudo pacman -S pandoc-cli```\n\n```apk: sudo apk add pandoc-cli```\n\n```zypper: sudo zypper install pandoc-cli```\n\n```brew: brew install pandoc```\n\n```nix: nix profile install nixpkgs#pandoc```"],["SEE ALSO","[markdown](/man/markdown)(1), [latex](/man/latex)(1), [groff](/man/groff)(1)"]],"e":["markdown","latex","groff"],"x":"pandoc [input.md] -o [output.html]"},"pdfbook":{"n":"pdfbook","t":"Put pages of PDF files into 2-up signatures for booklet printing","s":[["TAGLINE","Put pages of PDF files into 2-up signatures for booklet printing"],["TLDR","**Create a booklet from a PDF file**\n\n```pdfbook [input.pdf]```\n\n**Specify output file**\n\n```pdfbook -o [booklet.pdf] [input.pdf]```\n\n**Create booklet for A4 paper**\n\n```pdfbook --paper a4paper [input.pdf]```\n\n**Create booklet with a 16-page signature**\n\n```pdfbook --signature [16] [input.pdf]```\n\n**Create booklet for short-edge double-sided printing**\n\n```pdfbook --short-edge [input.pdf]```"],["SYNOPSIS","**pdfbook** [**--short-edge**] [_options_] _file_..."],["PARAMETERS","**-o** _file_\n> Output file name. Defaults to input filename with \"-book\" suffix.\n\n**--paper** _size_\n> Paper size (e.g. a4paper, letterpaper, legalpaper).\n\n**--signature** _num_\n> Pages per signature (must be a multiple of 4, default 4).\n\n**--signature*** _num_\n> Pages per signature for right-edge binding.\n\n**--short-edge**\n> Format for short-edge double-sided printing. Must be the first argument. Requires the LaTeX package everyshi.\n\n**--booklet** _bool_\n> Enable or disable booklet reordering (default true)."],["DESCRIPTION","**pdfbook** is a simple wrapper for **pdfjam** that arranges PDF pages into 2-up signatures suitable for printing as a booklet. Pages are reordered so that when printed double-sided and folded, they form a properly ordered booklet.\n\nThe default output is suitable for long-edge binding. Use **--short-edge** for short-edge binding. All options not listed above are passed through to **pdfjam**."],["CAVEATS","Requires LaTeX with the **pdfpages** package installed. Page count should be a multiple of 4 for best results. Output file defaults to the input filename with a \"-book\" suffix. The **--short-edge** option requires the **everyshi** LaTeX package."],["HISTORY","pdfbook is part of **pdfjam**, a collection of shell scripts using the **pdfpages** LaTeX package, created by **David Firth**. It has been largely superseded by **pdfbook2**, which provides additional features like margin control."],["SEE ALSO","[pdfjam](/man/pdfjam)(1), [pdfnup](/man/pdfnup)(1), [pdflatex](/man/pdflatex)(1), [pdfbook2](/man/pdfbook2)(1)"]],"e":["pdfjam","pdfnup","pdflatex","pdfbook2"],"x":"pdfbook [input.pdf]"},"pip-search":{"n":"pip-search","t":"Search PyPI for packages (deprecated)","s":[["TAGLINE","Search PyPI for packages (deprecated)"],["TLDR","**Search for packages**\n\n```pip search [query]```"],["SYNOPSIS","**pip** **search** _query_"],["DESCRIPTION","**pip search** searched for packages on PyPI by name or description. This command has been disabled on PyPI since late 2020 due to excessive resource usage from automated queries."],["CAVEATS","This command no longer works. PyPI disabled the XML-RPC search endpoint. Search for packages directly at pypi.org instead."],["INSTALL","```apk: sudo apk add py3-pip```"],["SEE ALSO","[pip](/man/pip)(1), [pip-index](/man/pip-index)(1)"]],"e":["pip","pip-index"],"x":"pip search [query]"},"pnmindex":{"n":"pnmindex","t":"Create thumbnail contact sheets from images","s":[["TAGLINE","Create thumbnail contact sheets from images"],["TLDR","**Create image index**\n\n```pnmindex [image1.pnm] [image2.pnm] [image3.pnm] > [index.pnm]```\n\n**Specify size**\n\n```pnmindex -size [100] [*.pnm] > [index.pnm]```\n\n**With a title**\n\n```pnmindex -title [\"My Gallery\"] [*.pnm] > [index.pnm]```\n\n**Multiple columns**\n\n```pnmindex -across [4] [*.pnm] > [index.pnm]```"],["SYNOPSIS","**pnmindex** [_options_] _pnmfiles_..."],["PARAMETERS","**-size** _n_\n> Maximum thumbnail size in pixels. Each image is scaled to fit within an N x N box while preserving aspect ratio. Default: 100.\n\n**-across** _n_\n> Number of thumbnails per row. Default: 6.\n\n**-colors** _n_\n> Maximum number of colors in the output image. Default: 256.\n\n**-title** _title_\n> Add a title string at the top of the image.\n\n**-black**\n> Use black background with white labels instead of the default white background with black labels.\n\n**-noquant**\n> Disable color quantization. By default, colors are reduced for PPM input.\n\n**-quant**\n> Enable color quantization (default for PPM input).\n\n**-quiet**\n> Suppress informational messages."],["DESCRIPTION","**pnmindex** creates a visual index (contact sheet) from multiple PNM images. Each image is scaled to a thumbnail and arranged in a grid.\n\nUseful for creating image previews and galleries."],["EXAMPLES","```bash"],["Create index of all PPM files","pnmindex *.ppm > index.ppm"],["Thumbnails 150 pixels, 5 across","pnmindex -size 150 -across 5 photos/*.ppm > gallery.ppm"],["With title on black background","pnmindex -title \"Photos\" -black images/*.pnm > contact.ppm"],["Convert to JPEG","pnmindex *.pnm | pnmtojpeg > index.jpg\n```"],["CAVEATS","Large numbers of images can produce huge output. Consider pamundice for splitting back."],["HISTORY","pnmindex is part of **Netpbm**, providing contact sheet generation since early Pbmplus versions."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pnmcat](/man/pnmcat)(1), [pnmscale](/man/pnmscale)(1), [pnmtile](/man/pnmtile)(1), [pnmmontage](/man/pnmmontage)(1), [pnmtojpeg](/man/pnmtojpeg)(1), [pamundice](/man/pamundice)(1), [netpbm](/man/netpbm)(1)"]],"e":["pnmcat","pnmscale","pnmtile","pnmmontage","pnmtojpeg","pamundice","netpbm"],"x":"pnmindex [image1.pnm] [image2.pnm] [image3.pnm] > [index.pnm]"},"pnmtopalm":{"n":"pnmtopalm","t":"Convert PNM images to Palm image format","s":[["TAGLINE","Convert PNM images to Palm image format"],["TLDR","**Convert PNM to Palm image**\n\n```pnmtopalm [input.pnm] > [output.palm]```\n\n**Specify color depth**\n\n```pnmtopalm -depth [8] [input.pnm] > [output.palm]```\n\n**Use a custom colormap**\n\n```pnmtopalm -colormap [input.pnm] > [output.palm]```\n\n**Mark a color as transparent**\n\n```pnmtopalm -transparent [rgb:ff/ff/ff] [input.pnm] > [output.palm]```\n\n**Apply scanline compression**\n\n```pnmtopalm -scanline_compression [input.pnm] > [output.palm]```"],["SYNOPSIS","**pnmtopalm** [_options_] [_pnmfile_]"],["PARAMETERS","**-depth** _n_\n> Output bits per pixel: 1, 2, 4, 8, or 16. Grayscale inputs are limited to 4 bits maximum.\n\n**-maxdepth** _n_\n> Create the minimal-depth output that does not exceed _n_ bits. Setting 16 forces direct-color output.\n\n**-colormap**\n> Build a custom colormap and embed it in the output. Less efficient than the default Palm colormap.\n\n**-transparent** _colorspec_\n> Mark one color as fully transparent (Palm OS 3.5+).\n\n**-density** _n_\n> Palm Bitmap density. Valid values: 72, 108, 144, 216, or 288.\n\n**-offset**\n> Set the `nextDepthOffset` field and pad to 4-byte boundaries, allowing multiple image renditions to be concatenated.\n\n**-withdummy**\n> Insert a dummy image header after the image so subsequent high-density renditions stay readable by older interpreters. Requires **-offset**.\n\n**-scanline_compression**\n> Use Palm scanline compression (Palm OS 2.0+).\n\n**-rle_compression**\n> Use Palm RLE compression (Palm OS 3.5+).\n\n**-packbits_compression**\n> Use Palm packbits compression (Palm OS 4.0+). Invalid with 16-bit direct color.\n\n**-verbose**\n> Print the format of the output file."],["DESCRIPTION","**pnmtopalm** converts a PNM image to the Palm Pilot bitmap format. It is part of the Netpbm toolkit. The inverse conversion is performed by **palmtopnm**."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[palmtopnm](/man/palmtopnm)(1), [pnmtopng](/man/pnmtopng)(1)"]],"e":["palmtopnm","pnmtopng"],"x":"pnmtopalm [input.pnm] > [output.palm]"},"poetry-list":{"n":"poetry-list","t":"List all available Poetry commands","s":[["TAGLINE","List all available Poetry commands"],["TLDR","**List all available commands**\n\n```poetry list```\n\n**List commands with namespace**\n\n```poetry list [namespace]```"],["SYNOPSIS","**poetry** **list** [_namespace_]"],["DESCRIPTION","**poetry list** displays all available Poetry commands. Can optionally filter by namespace to show only commands in a specific category. Useful for discovering available subcommands."],["INSTALL","```dnf: sudo dnf install poetry```\n\n```apk: sudo apk add poetry```\n\n```brew: brew install poetry```\n\n```nix: nix profile install nixpkgs#poetry```"],["SEE ALSO","[poetry](/man/poetry)(1), [poetry-help](/man/poetry-help)(1), [poetry-about](/man/poetry-about)(1)"]],"e":["poetry","poetry-help","poetry-about"],"x":"poetry list"},"pokemon-colorscripts":{"n":"pokemon-colorscripts","t":"Display Pokemon ANSI art in terminal","s":[["TAGLINE","Display Pokemon ANSI art in terminal"],["TLDR","**Display random Pokemon**\n\n```pokemon-colorscripts -r```\n\n**Display specific Pokemon**\n\n```pokemon-colorscripts -n [pikachu]```\n\n**Display with name**\n\n```pokemon-colorscripts -r --no-title```\n\n**Shiny variant**\n\n```pokemon-colorscripts -n [charizard] -s```\n\n**List all Pokemon**\n\n```pokemon-colorscripts -l```"],["SYNOPSIS","**pokemon-colorscripts** [_options_]"],["PARAMETERS","**-n**, **--name** _name_\n> Display specific Pokemon.\n\n**-r**, **--random**\n> Display random Pokemon.\n\n**-l**, **--list**\n> List all Pokemon.\n\n**-s**, **--shiny**\n> Show shiny variant.\n\n**--no-title**\n> Hide Pokemon name.\n\n**-f**, **--form** _form_\n> Specific form."],["DESCRIPTION","**pokemon-colorscripts** displays Pokemon as ASCII/ANSI art in the terminal. It can show random Pokemon, specific ones by name, and shiny variants.\n\nPopular for shell startup decoration."],["EXAMPLES","```bash"],["Random Pokemon","pokemon-colorscripts -r"],["Specific Pokemon","pokemon-colorscripts -n pikachu"],["Shiny Mewtwo","pokemon-colorscripts -n mewtwo -s"],["Random without name","pokemon-colorscripts -r --no-title"],["In .bashrc or .zshrc","pokemon-colorscripts -r\n```"],["SHELL INTEGRATION","```bash"],["Add to ~/.bashrc or ~/.zshrc","pokemon-colorscripts -r"],["Or with specific Pokemon","pokemon-colorscripts -n eevee\n```"],["CAVEATS","Requires Unicode/ANSI terminal support. Color accuracy depends on terminal. Generation 1-8 coverage varies."],["HISTORY","pokemon-colorscripts was created by **phoney badger** as a fun terminal decoration tool, inspired by ASCII art traditions."],["INSTALL","```nix: nix profile install nixpkgs#pokemon-colorscripts```"],["SEE ALSO","[neofetch](/man/neofetch)(1), [cowsay](/man/cowsay)(1), [lolcat](/man/lolcat)(1)"]],"e":["neofetch","cowsay","lolcat"],"x":"pokemon-colorscripts -r"},"ppmtopgm":{"n":"ppmtopgm","t":"Convert color PPM images to grayscale","s":[["TAGLINE","Convert color PPM images to grayscale"],["TLDR","**Convert color to grayscale**\n\n```ppmtopgm [input.ppm] > [output.pgm]```"],["SYNOPSIS","**ppmtopgm** [_file_]"],["DESCRIPTION","**ppmtopgm** converts PPM color images to PGM grayscale. Uses standard luminance formula to compute gray values. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pgmtoppm](/man/pgmtoppm)(1)"]],"e":["pgmtoppm"],"x":"ppmtopgm [input.ppm] > [output.pgm]"},"radeontool":{"n":"radeontool","t":"Control ATI Radeon hardware settings","s":[["TAGLINE","Control ATI Radeon hardware settings"],["TLDR","**Turn off the laptop backlight**\n\n```radeontool light off```\n\n**Turn on the laptop backlight**\n\n```radeontool light on```\n\n**Disable external video output** (DAC)\n\n```radeontool dac off```\n\n**Enable external video output** (DAC)\n\n```radeontool dac on```\n\n**Display a sampling of register values**\n\n```radeontool regs```\n\n**Show registers matching a pattern**\n\n```radeontool regmatch [pattern]```\n\n**Set registers matching a pattern** to a specific value\n\n```radeontool regset [pattern] [value]```"],["SYNOPSIS","**radeontool** [_options_] [_command_]"],["PARAMETERS","**light** [**on**|**off**]\n> Control the laptop panel backlight power state\n\n**dac** [**on**|**off**]\n> Control external video output (VGA/DVI) power state\n\n**stretch** [**on**|**off**|**vert**|**horiz**|**auto**|**manual**]\n> Control LCD stretching when running non-native resolutions\n\n**regs**\n> Display a sampling of important register values\n\n**regmatch** _pattern_\n> Show registers whose names match the wildcard pattern\n\n**regset** _pattern_ _value_\n> Set registers matching the wildcard pattern to value\n\n**--debug**\n> Enable verbose debugging output.\n\n**--skip=**_n_\n> Skip the first n Radeon cards and operate on card n+1.\n\n**--instance=**_n_\n> Use a specific Radeon card instance by number."],["DESCRIPTION","**radeontool** is a utility for controlling hardware features on ATI Radeon-based graphics cards, primarily used on laptops. It provides direct access to backlight control, external display output, and low-level register manipulation.\n\nThe tool communicates directly with the graphics hardware through memory-mapped I/O, allowing control over features not exposed through standard display drivers. Common uses include scripted backlight control for power management and debugging display issues."],["CAVEATS","**Root privileges required** for most operations due to direct hardware access via /dev/mem.\n\n**Register manipulation is dangerous**: Using regset incorrectly can cause system instability or permanent hardware damage. Only modify registers if you understand the consequences.\n\n**Not all features work on all cards**: Compatibility varies between Radeon generations. Test commands carefully on your specific hardware."],["HISTORY","radeontool was created by **David Airlie** as part of the Linux open-source graphics effort. It emerged in the early 2000s when ATI Radeon cards required userspace tools to control features not yet supported by the kernel drivers."],["INSTALL","```apt: sudo apt install radeontool```\n\n```dnf: sudo dnf install radeontool```\n\n```pacman: sudo pacman -S radeontool```"],["SEE ALSO","[xrandr](/man/xrandr)(1), [xbacklight](/man/xbacklight)(1), [aticonfig](/man/aticonfig)(1), [lspci](/man/lspci)(1)"]],"e":["xrandr","xbacklight","aticonfig","lspci"],"x":"radeontool light off"},"readlink":{"n":"readlink","t":"Resolve symbolic links and canonical paths","s":[["TAGLINE","Resolve symbolic links and canonical paths"],["TLDR","**Print the target of a symbolic link**\n\n```readlink [symlink]```\n\n**Get the absolute path** (resolve all symlinks)\n\n```readlink -f [path]```\n\n**Get the absolute path** (all but last component must exist)\n\n```readlink -e [path]```\n\n**Get the canonical path** (without requiring existence)\n\n```readlink -m [path]```\n\n**Print each resolved path on a new line**\n\n```readlink -f [path1] [path2]```"],["SYNOPSIS","**readlink** [_option_]... _file_..."],["PARAMETERS","**-f**, **--canonicalize**\n> Canonicalize by following every symlink recursively; all components must exist\n\n**-e**, **--canonicalize-existing**\n> Canonicalize, but all components must exist\n\n**-m**, **--canonicalize-missing**\n> Canonicalize without requiring components to exist\n\n**-n**, **--no-newline**\n> Do not output trailing newline\n\n**-q**, **--quiet**\n> Suppress error messages\n\n**-s**, **--silent**\n> Suppress error messages (same as -q)\n\n**-v**, **--verbose**\n> Report error messages\n\n**-z**, **--zero**\n> End each output line with NUL, not newline\n\n**--help**\n> Display help and exit\n\n**--version**\n> Output version information and exit"],["DESCRIPTION","**readlink** prints the value of a symbolic link or the canonical path of a file. Without options, it displays the immediate target of a symlink (one level only).\n\nThe canonicalize options (**-f**, **-e**, **-m**) resolve the complete path by following all symbolic links, resolving references to /./ and /../, and returning an absolute pathname. The difference is how they handle non-existent components:\n- **-f**: All components except the last must exist\n- **-e**: All components must exist (strictest)\n- **-m**: No existence requirements (most permissive)\n\nThis is commonly used in scripts to find the real location of files or to get absolute paths from relative ones."],["CAVEATS","Without canonicalize options, readlink only shows the immediate symlink target. For chained symlinks, use **-f** to resolve the final target.\n\nOn macOS, readlink behaves differently and may lack GNU options. Use **greadlink** from coreutils or **realpath** for portable canonicalization.\n\nFor scripts needing portability, consider **realpath** which is POSIX-specified, though not universally available."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[ln](/man/ln)(1), [realpath](/man/realpath)(1), [stat](/man/stat)(1), [ls](/man/ls)(1)"]],"e":["ln","realpath","stat","ls"],"x":"readlink [symlink]"},"rkdeveloptool":{"n":"rkdeveloptool","t":"Flash firmware to Rockchip devices via USB","s":[["TAGLINE","Flash firmware to Rockchip devices via USB"],["TLDR","**List devices**\n\n```rkdeveloptool ld```\n\n**Download boot**\n\n```rkdeveloptool db [loader.bin]```\n\n**Write image**\n\n```rkdeveloptool wl [0] [image.img]```\n\n**Read flash info**\n\n```rkdeveloptool rfi```\n\n**Reboot device**\n\n```rkdeveloptool rd```\n\n**Read partition**\n\n```rkdeveloptool rl [0] [size] [output.img]```"],["SYNOPSIS","**rkdeveloptool** _command_ [_options_]"],["PARAMETERS","**ld**\n> List devices.\n\n**db** _FILE_\n> Download boot loader.\n\n**wl** _OFFSET FILE_\n> Write image at offset.\n\n**rl** _OFFSET SIZE FILE_\n> Read to file.\n\n**rfi**\n> Read flash info.\n\n**rd**\n> Reboot device.\n\n**ef**\n> Erase entire flash.\n\n**ul** _FILE_\n> Upgrade firmware from a packed update image.\n\n**rid**\n> Read chip ID."],["DESCRIPTION","**rkdeveloptool** is a Linux utility for flashing firmware to Rockchip ARM SoC devices (RK3399, RK3328, RK3288, and others) via USB. It communicates with the device's bootrom when the device is placed in maskrom mode, a special recovery state entered by shorting specific pins or holding a button during power-on.\n\nThe flashing process typically involves two steps: first loading a boot loader with the **db** command, then writing the system image at the appropriate offset with **wl**. The tool can also read back flash contents, query flash chip information, and erase the entire storage. It serves as an open-source Linux alternative to Rockchip's Windows-based flashing tools.\n\nRoot or appropriate USB permissions are required to communicate with the device. The **ld** command verifies that the device is detected and in the correct mode before proceeding with write operations."],["CAVEATS","Root required. Device must be in maskrom. Wrong images can brick."],["HISTORY","**rkdeveloptool** is developed by **Rockchip** for flashing firmware to their ARM SoC devices via USB."],["INSTALL","```apt: sudo apt install rkdeveloptool```\n\n```dnf: sudo dnf install rkdeveloptool```\n\n```apk: sudo apk add rkdeveloptool```\n\n```zypper: sudo zypper install rkdeveloptool```\n\n```nix: nix profile install nixpkgs#rkdeveloptool```"],["SEE ALSO","[dd](/man/dd)(1), [fastboot](/man/fastboot)(1)"]],"e":["dd","fastboot"],"x":"rkdeveloptool ld"},"runsvchdir":{"n":"runsvchdir","t":"Change the runit service directory","s":[["TAGLINE","Change the runit service directory"],["TLDR","**Change service directory**\n\n```runsvchdir [multi-user]```\n\n**Switch to rescue mode**\n\n```runsvchdir [single]```\n\n**View current runlevel**\n\n```cat /etc/runit/runsvdir/current```"],["SYNOPSIS","**runsvchdir** _name_"],["DESCRIPTION","**runsvchdir** switches which service directory runsvdir supervises. It changes the system's effective runlevel.\n\nRunlevels are directories in /etc/runit/runsvdir/. Each contains symlinks to services.\n\nThe switch is graceful. Services not in the new level are stopped, new ones started.\n\nCommon levels include default, single, and multi-user. Custom levels can be created.\n\nThe current symlink tracks active level. /etc/runit/runsvdir/current points to active."],["RUNLEVELS","**default** - Normal multi-user\n**single** - Single user/rescue\n**multi-user** - Network services\n**custom** - User-defined"],["CAVEATS","Requires root. Services must exist in target level. Switch may take time for service stops."],["HISTORY","**runsvchdir** is part of **runit**, providing runlevel-like switching. It allows dynamic reconfiguration of which services are supervised."],["INSTALL","```apt: sudo apt install runit```\n\n```apk: sudo apk add runit```\n\n```brew: brew install runit```\n\n```nix: nix profile install nixpkgs#runit```"],["SEE ALSO","[runsvdir](/man/runsvdir)(8), [runit](/man/runit)(8), [sv](/man/sv)(8)"]],"e":["runsvdir","runit","sv"],"x":"runsvchdir [multi-user]"},"simutrans":{"n":"simutrans","t":"Open-source transportation simulation game","s":[["TAGLINE","Open-source transportation simulation game"],["TLDR","**Start Simutrans** normally\n\n```simutrans```\n\n**Start in fullscreen mode**\n\n```simutrans -fullscreen```\n\n**Set a specific screen size**\n\n```simutrans -screensize [1920] [1080]```\n\n**Load a specific pakset**\n\n```simutrans -objects [pak128/]```\n\n**Load a saved game**\n\n```simutrans -load [savegame.sve]```\n\n**Start in freeplay mode** (no bankruptcy)\n\n```simutrans -freeplay```\n\n**Start without sound** (useful for sound driver issues)\n\n```simutrans -nosound -nomidi```\n\n**Start a multiplayer server**\n\n```simutrans -server [13353]```"],["SYNOPSIS","**simutrans** [_options_]"],["PARAMETERS","**-screensize** _WIDTH_ _HEIGHT_\n> Set screen size to WIDTHxHEIGHT pixels. Widths not a multiple of 16 may cause issues.\n\n**-fullscreen**\n> Run in fullscreen mode.\n\n**-fps** _n_\n> Refresh the display n times per second.\n\n**-nosound**\n> Disable sound effects.\n\n**-nomidi**\n> Disable music. Also helps prevent crashes from buggy sound drivers on 64-bit Linux.\n\n**-freeplay**\n> Enable freeplay mode where you cannot go bankrupt.\n\n**-timeline** _0|1_\n> 0 = all vehicles available from start, 1 = realistic timeline.\n\n**-startyear** _year_\n> Set the starting year of the game.\n\n**-load** _savefile_\n> Load a saved game directly without showing the initial menu.\n\n**-pause**\n> Start the loaded game in paused state.\n\n**-lang** _code_\n> Set language using ISO codes (e.g., en, de, fr).\n\n**-objects** _folder/_\n> Load a specific pakset from the given folder. The trailing slash is mandatory.\n\n**-singleuser**\n> Don't search for files in home directory; use only the data directory.\n\n**-use_workdir**\n> Use the current working directory for Simutrans environment.\n\n**-log** _1_\n> Write all log output to \"simu.log\" file.\n\n**-debug** _1-4_\n> Set debug output detail level (1-4).\n\n**-server** [_port_]\n> Start as a multiplayer server on the specified port (default: 13353).\n\n**-server_name** _\"name\"_\n> Set the server name for multiplayer."],["DESCRIPTION","**Simutrans** is an open-source transportation simulation game where players build and manage transportation networks including trains, buses, trucks, ships, and aircraft. Players create routes between cities, industries, and tourist attractions to earn money while competing with AI companies.\n\nThe game supports extensive modding through \"paksets\" which define vehicles, buildings, and game rules. Popular paksets include pak64, pak128, and pak192.comic, each offering different visual styles and gameplay complexity.\n\nConfiguration files are stored in **~/.simutrans** on Linux. The game can be played in single-player, freeplay mode, or as a multiplayer server allowing multiple players to collaborate or compete on the same map."],["CONFIGURATION","**~/.simutrans/**\n> User data directory containing saved games, settings, screenshots, and installed paksets.\n\n**~/.simutrans/simuconf.tab**\n> Main configuration file for display, network, and gameplay settings."],["CAVEATS","Some command-line options like **-async** and **-net** are only available on Linux/X-Windows. Screen widths not divisible by 16 may cause display problems. The **-nomidi** option is recommended on systems with sound driver issues, particularly 64-bit Linux. Paksets must be downloaded separately from the base game."],["HISTORY","**Simutrans** was created by Hansjörg Malthaner and first released in **1999**. The name is a portmanteau of \"simulation\" and \"transport.\" After Malthaner ceased active development, the project continued as open source with an international community of developers. The game has been compared to Transport Tycoon but evolved with unique features and extensive modding support. Development continues actively with regular releases."],["INSTALL","```pacman: sudo pacman -S simutrans```\n\n```zypper: sudo zypper install simutrans```\n\n```brew: brew install simutrans```\n\n```nix: nix profile install nixpkgs#simutrans```"],["SEE ALSO","[openttd](/man/openttd)(6)"]],"e":["openttd"],"x":"simutrans"},"sslscan":{"n":"sslscan","t":"Test SSL/TLS server configuration and ciphers","s":[["TAGLINE","Test SSL/TLS server configuration and ciphers"],["TLDR","**Scan a server for SSL/TLS configuration**\n\n```sslscan [example.com]```\n\n**Scan specific port**\n\n```sslscan [example.com:8443]```\n\n**Show only supported ciphers**\n\n```sslscan --no-ciphersuites [example.com]```\n\n**Show certificate details**\n\n```sslscan --show-certificate [example.com]```\n\n**No color output**\n\n```sslscan --no-colour [example.com]```\n\n**Use STARTTLS**\n\n```sslscan --starttls-smtp [mail.example.com:25]```\n\n**XML output**\n\n```sslscan --xml=[output.xml] [example.com]```\n\n**Check specific protocol**\n\n```sslscan --tlsall [example.com]```"],["SYNOPSIS","**sslscan** [_--show-certificate_] [_--starttls-protocol_] [_--xml file_] [_options_] _host_[:_port_]"],["PARAMETERS","**--show-certificate**\n> Show certificate details.\n\n**--no-ciphersuites**\n> Skip cipher listing.\n\n**--no-colour**\n> Disable colors.\n\n**--xml** _FILE_\n> XML output.\n\n**--starttls-smtp**\n> STARTTLS for SMTP.\n\n**--starttls-imap**\n> STARTTLS for IMAP.\n\n**--starttls-pop3**\n> STARTTLS for POP3.\n\n**--starttls-ftp**\n> STARTTLS for FTP.\n\n**--ssl2**, **--ssl3**, **--tls10**, **--tls11**, **--tls12**, **--tls13**\n> Test specific versions.\n\n**--tlsall**\n> Test all TLS versions.\n\n**--no-fallback**\n> No version fallback.\n\n**--sni** _HOST_\n> SNI hostname.\n\n**--bugs**\n> Enable SSL bug workarounds."],["DESCRIPTION","**sslscan** tests SSL/TLS services for configuration issues. It identifies supported ciphers, protocols, and certificate problems.\n\nThe scan shows which TLS versions are enabled. Legacy protocols (SSLv2, SSLv3, TLS 1.0, TLS 1.1) are security concerns.\n\nCipher testing reveals weak algorithms. Export-grade, NULL, and RC4 ciphers are flagged. Color coding highlights problems.\n\nCertificate inspection shows expiry, chain, and trust issues. It identifies self-signed, expired, or incorrectly configured certificates.\n\nSTARTTLS support enables testing mail servers and other services that upgrade connections to TLS.\n\nResults help remediate server configurations. Compare against security standards and best practices."],["CAVEATS","Requires network access to target. Some servers rate-limit scans. Results depend on client capabilities. Cannot test client certs."],["HISTORY","**sslscan** was originally written by **Ian Ventura-Whiting** around **2007**. It was later forked and actively maintained by **rbsec** to support modern TLS features and protocols."],["INSTALL","```apt: sudo apt install sslscan```\n\n```dnf: sudo dnf install sslscan```\n\n```pacman: sudo pacman -S sslscan```\n\n```apk: sudo apk add sslscan```\n\n```zypper: sudo zypper install sslscan```\n\n```brew: brew install sslscan```\n\n```nix: nix profile install nixpkgs#sslscan```"],["SEE ALSO","[nmap](/man/nmap)(1), [openssl](/man/openssl)(1), [testssl](/man/testssl)(1), [sslyze](/man/sslyze)(1)"]],"e":["nmap","openssl","testssl","sslyze"],"x":"sslscan [example.com]"},"systemctl-list-automounts":{"n":"systemctl-list-automounts","t":"List automount units and states","s":[["TAGLINE","List automount units and states"],["TLDR","List **automount** units\n\n```systemctl list-automounts```\n\nList **all** including inactive\n\n```systemctl list-automounts -a```\n\nFilter by **state**\n\n```systemctl list-automounts --state [active|inactive|failed]```\n\nFilter by **pattern**\n\n```systemctl list-automounts [pattern]```"],["SYNOPSIS","**systemctl list-automounts** [_OPTIONS_] [_PATTERN_...]"],["PARAMETERS","**-a, --all**\n> Include inactive automount units\n\n**--state=** _STATE_\n> Filter by state (active, inactive, failed, etc.)\n\n**--no-legend**\n> Suppress header and footer\n\n**--no-pager**\n> Disable pager output"],["DESCRIPTION","**systemctl list-automounts** displays automount units currently loaded in memory, showing mount paths and corresponding unit names. Automount units trigger on-demand mounting when the mount point is accessed.\n\nThe output shows the path being watched, the automount unit name, and its current state."],["CAVEATS","Only shows automount units, not regular mount units. Inactive automounts are hidden by default. For all mount information, use `systemctl list-units --type=mount`."],["HISTORY","The **list-automounts** subcommand provides a focused view of on-demand mounting configuration, separating automount units from regular mounts for easier administration."],["SEE ALSO","[systemctl-list-units](/man/systemctl-list-units)(1), [systemctl](/man/systemctl)(1), [mount](/man/mount)(8)"]],"e":["systemctl-list-units","systemctl","mount"],"x":"systemctl list-automounts"},"systemctl-stop":{"n":"systemctl-stop","t":"Stop systemd units","s":[["TAGLINE","Stop systemd units"],["TLDR","**Stop** a running system unit\n\n```systemctl stop [unit]```\n\nStop a unit managed by the **user** instance\n\n```systemctl --user stop [unit]```\n\nStop while **suppressing** certain warnings\n\n```systemctl stop --no-warn [unit]```"],["SYNOPSIS","**systemctl** [_OPTIONS_...] **stop** _PATTERN_..."],["PARAMETERS","**--user**\n> Stop user service manager units instead of system units\n\n**--no-block**\n> Do not wait for the operation to complete\n\n**--no-warn**\n> Suppress warning about stopping running units\n\n**--job-mode=**_MODE_\n> Specify how to deal with already queued jobs"],["DESCRIPTION","**systemctl stop** deactivates systemd units, stopping services, unmounting filesystems, or deactivating other unit types. The command queues a stop job for the specified unit.\n\nFor services, this sends a stop command (usually SIGTERM followed by SIGKILL after timeout). The unit transitions to the inactive state. Dependent units are not automatically stopped unless they cannot function without the stopped unit."],["CAVEATS","Requires root privileges for system units. Stopping a unit does not disable it; it may restart on next boot if enabled. Some services may take time to stop gracefully."],["INSTALL","```apt: sudo apt install systemd```\n\n```dnf: sudo dnf install systemd```\n\n```pacman: sudo pacman -S systemd```\n\n```zypper: sudo zypper install systemd```\n\n```brew: brew install systemd```\n\n```nix: nix profile install nixpkgs#systemd```"],["SEE ALSO","[systemctl](/man/systemctl)(1), [systemctl-start](/man/systemctl-start)(1), [systemctl-restart](/man/systemctl-restart)(1)"]],"e":["systemctl","systemctl-start","systemctl-restart"],"x":"systemctl stop [unit]"},"systemctl-thaw":{"n":"systemctl-thaw","t":"Resume frozen unit processes","s":[["TAGLINE","Resume frozen unit processes"],["TLDR","**Thaw** (resume) a unit\n\n```systemctl thaw [unit]```\n\nThaw **multiple** units\n\n```systemctl thaw [unit1 unit2 ...]```\n\nThaw **all** frozen units\n\n```systemctl thaw '*'```"],["SYNOPSIS","**systemctl thaw** _PATTERN_..."],["DESCRIPTION","**systemctl thaw** resumes one or more units that were previously frozen with `systemctl freeze`. Frozen units have all their processes suspended via the kernel freezer cgroup controller.\n\nThawing allows the unit's processes to continue execution from where they were paused."],["CAVEATS","Only affects units that were frozen. Thawing a non-frozen unit has no effect. Requires the cgroup freezer controller to be available."],["HISTORY","The **thaw** subcommand complements the freeze functionality, enabling controlled process suspension and resumption for debugging or resource management."],["SEE ALSO","[systemctl-freeze](/man/systemctl-freeze)(1), [systemctl](/man/systemctl)(1)"]],"e":["systemctl-freeze","systemctl"],"x":"systemctl thaw [unit]"},"tmpmail":{"n":"tmpmail","t":"Temporary disposable email from terminal","s":[["TAGLINE","Temporary disposable email from terminal"],["TLDR","**Generate address**\n\n```tmpmail -g```\n\n**Check inbox**\n\n```tmpmail```\n\n**Read email by ID**\n\n```tmpmail [email-id]```\n\n**Copy address**\n\n```tmpmail -c```\n\n**Different domain**\n\n```tmpmail -g [domain]```\n\n**Recent emails**\n\n```tmpmail -r```"],["SYNOPSIS","**tmpmail** [_-g_] [_-c_] [_-r_] [_options_] [_id_]"],["PARAMETERS","**-g**\n> Generate address.\n\n**-c**\n> Copy to clipboard.\n\n**-r**\n> Recent emails.\n\n**-b** _BROWSER_\n> Open email in the specified browser instead of plain text in the terminal.\n\n**-t**, **--text**\n> Force text mode rendering (do not open in a browser).\n\n**-h**, **--help**\n> Show help."],["DESCRIPTION","**tmpmail** generates temporary, disposable email addresses and lets you check their inbox directly from the terminal. It is useful for signing up for services without exposing your real email address to potential spam.\n\nThe tool creates addresses on supported temporary email domains and retrieves incoming messages for viewing in the terminal or a browser. The current address can be copied to the clipboard with **-c** for quick pasting into registration forms."],["CAVEATS","External service dependent. May have limits. Not for sensitive use."],["HISTORY","**tmpmail** was created for generating temporary email addresses and checking them from the command line."],["INSTALL","```brew: brew install tmpmail```\n\n```nix: nix profile install nixpkgs#tmpmail```"],["SEE ALSO","[mail](/man/mail)(1), [mutt](/man/mutt)(1), [curl](/man/curl)(1)"]],"e":["mail","mutt","curl"],"x":"tmpmail -g"},"unbound-host":{"n":"unbound-host","t":"DNS lookup utility using Unbound resolver","s":[["TAGLINE","DNS lookup utility using Unbound resolver"],["TLDR","**Look up a hostname**\n\n```unbound-host [example.com]```\n\n**Look up with DNSSEC validation status**\n\n```unbound-host -v [example.com]```\n\n**Query specific record type**\n\n```unbound-host -t [MX] [example.com]```\n\n**Enable DNSSEC validation using the default root anchor**\n\n```unbound-host -D [example.com]```\n\n**Use system resolvers from resolv.conf**\n\n```unbound-host -r [example.com]```\n\n**Use custom config file**\n\n```unbound-host -C [/etc/unbound/unbound.conf] [example.com]```\n\n**Reverse lookup of an IP address**\n\n```unbound-host [93.184.216.34]```\n\n**Force IPv4 only**\n\n```unbound-host -4 [example.com]```"],["SYNOPSIS","**unbound-host** [**-C** _configfile_] [**-vdhr46D**] [**-c** _class_] [**-t** _type_] [**-y** _key_] [**-f** _keyfile_] [**-F** _namedkeyfile_] _hostname_"],["PARAMETERS","**-v**\n> Enable verbose output showing DNSSEC validation status on every line (secure, insecure, or bogus).\n\n**-d**\n> Enable debug output to stderr. Repeat the flag (-d -d) for increased verbosity including full packet details.\n\n**-t** _type_\n> Query specific record type (A, AAAA, MX, TXT, NS, SOA, etc.). Defaults to A, AAAA, and MX for forward lookups, or PTR for reverse lookups.\n\n**-c** _class_\n> Query specific DNS class. Defaults to IN (internet). Other values: CH (chaos), HS (hesiod).\n\n**-r**\n> Read /etc/resolv.conf and use the forward DNS servers listed there. Note: this may break DNSSEC validation if those servers do not support it.\n\n**-f** _keyfile_\n> Read DS or DNSKEY trust anchor records from a file in zone file format (one record per line). Used to supply trust anchors for DNSSEC validation.\n\n**-F** _namedkeyfile_\n> Read trust anchor keys from a BIND-style named.conf file. Only trusted-key {} entries are read.\n\n**-y** _key_\n> Specify a single trust anchor directly on the command line in DS or DNSKEY record format.\n\n**-D**\n> Enable DNSSEC validation using the root anchor from the default location (/usr/share/dns/root.key or /etc/trusted-key.key depending on the system).\n\n**-C** _file_\n> Use the specified unbound.conf configuration file to configure the resolver.\n\n**-4**\n> Use IPv4 only for sending packets.\n\n**-6**\n> Use IPv6 only for sending packets.\n\n**-h**\n> Display version and help information."],["DESCRIPTION","**unbound-host** performs DNS lookups using the Unbound resolver library (libunbound). It provides DNSSEC validation capabilities, reporting whether responses are secure (cryptographically validated), insecure (no DNSSEC chain of trust for the domain), or bogus (validation failed, possible tampering).\n\nIf _hostname_ is an IPv4 or IPv6 address, a reverse lookup (PTR record) is performed automatically.\n\nBy default, the tool reads no configuration file whatsoever and attempts to reach internet root servers directly. The **-r** option uses resolvers from /etc/resolv.conf, and **-C** loads a full unbound configuration.\n\nDNSSEC validation requires trust anchors. Use **-D** for automatic root anchor loading, or supply anchors manually with **-y**, **-f**, or **-F**."],["EXIT STATUS","**0**\n> Success (though the queried data may not exist).\n\n**1**\n> A fatal error occurred during the lookup."],["CAVEATS","Direct root queries may be slow or blocked by firewalls. Using **-r** with non-DNSSEC-capable servers breaks validation. Trust anchors must be current for DNSSEC to work correctly."],["HISTORY","**unbound-host** is part of the Unbound project by NLnet Labs, providing command-line access to Unbound's validating resolver capabilities. It was designed as a DNSSEC-aware alternative to traditional host and dig commands."],["INSTALL","```apt: sudo apt install unbound-host```\n\n```dnf: sudo dnf install unbound```\n\n```pacman: sudo pacman -S unbound```\n\n```apk: sudo apk add unbound```\n\n```zypper: sudo zypper install unbound```\n\n```brew: brew install unbound```\n\n```nix: nix profile install nixpkgs#unbound```"],["SEE ALSO","[unbound](/man/unbound)(8), [unbound-control](/man/unbound-control)(8), [dig](/man/dig)(1), [host](/man/host)(1), [drill](/man/drill)(1), [nslookup](/man/nslookup)(1)"]],"e":["unbound","unbound-control","dig","host","drill","nslookup"],"x":"unbound-host [example.com]"},"unhash":{"n":"unhash","t":"Remove entries from zsh's internal hash tables","s":[["TAGLINE","Remove entries from zsh's internal hash tables"],["TLDR","**Remove a command** from the command hash table\n\n```unhash [ls]```\n\n**Remove an alias** (equivalent to unalias)\n\n```unhash -a [ll]```\n\nRemove a **shell function**\n\n```unhash -f [my_function]```\n\nRemove a **named directory** (hash -d entry)\n\n```unhash -d [projects]```\n\nRemove a **suffix alias**\n\n```unhash -s [md]```\n\nRemove entries by **pattern**\n\n```unhash -m \"[tmp_*]\"```"],["SYNOPSIS","**unhash** [**-adfms**] _name_ ..."],["DESCRIPTION","**unhash** is a Zsh built-in that removes a named entry from one of the shell's internal hash tables. By default it operates on the command hash table, which caches the full path of recently executed commands. Options select an alternate table (aliases, functions, named directories, or suffix aliases) and the **-m** flag treats the remaining arguments as patterns matching multiple entries at once.\n\nThe related built-ins **unalias**, **unfunction**, and **unsetopt** are thin wrappers that call **unhash** with the appropriate flag preset. Using **unhash** directly is mainly useful inside scripts and functions where the table being modified is determined dynamically."],["PARAMETERS","**-a**\n> Remove regular or global aliases. Equivalent to **unalias**.\n\n**-s**\n> Remove suffix aliases.\n\n**-f**\n> Remove shell functions. Equivalent to **unfunction**.\n\n**-d**\n> Remove named directory entries created with **hash -d**.\n\n**-m**\n> Treat each _name_ as a pattern; every matching entry in the selected table is removed. Patterns should be quoted to prevent filename expansion."],["CAVEATS","Available only in **Zsh**; Bash uses **hash -d** to remove a single entry and has no direct equivalent. Removing a name from the command hash table forces Zsh to re-search **$PATH** the next time the command is invoked, which is the usual reason for clearing stale entries after installing new binaries. Global aliases must be quoted when removed because they would otherwise be expanded before reaching **unhash**."],["HISTORY","**unhash** has been part of **Zsh** since its early releases by **Paul Falstad** in **1990**. It mirrors the design of the **hash** built-in but operates in reverse and was generalized to cover multiple internal tables as Zsh grew aliases, named directories, and suffix aliases."],["SEE ALSO","[hash](/man/hash)(1), [unalias](/man/unalias)(1), [unset](/man/unset)(1), [zsh](/man/zsh)(1)"]],"e":["hash","unalias","unset","zsh"],"x":"unhash [ls]"},"vhs":{"n":"vhs","t":"Record terminal sessions as GIFs","s":[["TAGLINE","Record terminal sessions as GIFs"],["TLDR","**Record terminal**\n\n```vhs [script.tape]```\n\n**Create new tape**\n\n```vhs new [script.tape]```\n\n**Validate tape**\n\n```vhs validate [script.tape]```\n\n**Output to specific format**\n\n```vhs [script.tape] --output [demo.gif]```\n\n**Record with theme**\n\n```vhs [script.tape] --theme [Dracula]```"],["SYNOPSIS","**vhs** [_command_] [_options_] [_file.tape_]"],["PARAMETERS","**new** _FILE_\n> Create tape file.\n\n**validate** _FILE_\n> Check tape syntax.\n\n**--output** _FILE_\n> Output file.\n\n**--theme** _NAME_\n> Terminal theme.\n\n**--shell** _SHELL_\n> Shell to use."],["TAPE COMMANDS","**Type** _TEXT_ - Type text\n**Enter** - Press enter\n**Sleep** _DURATION_ - Wait\n**Set** _OPTION_ _VALUE_ - Configure\n**Output** _FILE_ - Set output\n**Hide** / **Show** - Control visibility"],["DESCRIPTION","**vhs** creates terminal recordings as GIFs, WebM, or MP4 files from declarative script files called tapes. Tape files define a sequence of actions such as typing text, pressing keys, and waiting, producing reproducible recordings that can be regenerated consistently.\n\nThe tool renders a virtual terminal and captures frames, so recordings look clean regardless of the actual terminal environment. Themes, font sizes, and window dimensions are configurable within the tape file. This makes VHS ideal for creating documentation demos, README GIFs, and tutorial content."],["EXAMPLE TAPE","```\nOutput demo.gif\nSet FontSize 20\nType \"echo Hello\"\nEnter\nSleep 2s\n```"],["CAVEATS","Requires ffmpeg for encoding. Large recordings take time. Complex interactions need scripting."],["HISTORY","**VHS** was created by **Charm** for terminal recording. It provides reproducible, high-quality terminal GIFs."],["INSTALL","```dnf: sudo dnf install vhs```\n\n```pacman: sudo pacman -S vhs```\n\n```apk: sudo apk add vhs```\n\n```zypper: sudo zypper install vhs```\n\n```brew: brew install vhs```\n\n```nix: nix profile install nixpkgs#vhs```"],["SEE ALSO","[asciinema](/man/asciinema)(1), [ttyrec](/man/ttyrec)(1), [terminalizer](/man/terminalizer)(1)"]],"e":["asciinema","ttyrec","terminalizer"],"x":"vhs [script.tape]"},"wizu":{"n":"wizu","t":"Fast interactive directory tree viewer","s":[["TAGLINE","Fast interactive directory tree viewer"],["TLDR","**Display current directory tree**\n\n```wisu```\n\n**Interactive mode with file icons**\n\n```wisu -i --icons```\n\n**Sort by size descending with gitignore support**\n\n```wisu -g --sort size --reverse```"],["SYNOPSIS","**wisu** [_path_] [_options_]"],["PARAMETERS","_PATH_\n> Root directory to display (defaults to the current directory).\n\n**-i**, **--interactive**\n> Launch the interactive TUI browser.\n\n**--icons**\n> Render file-type emoji icons next to entries.\n\n**-g**, **--gitignore**\n> Respect `.gitignore` files when walking.\n\n**--sort** _KEY_\n> Sort entries by `name`, `size`, `mtime`, etc.\n\n**--reverse**\n> Reverse the sort order.\n\n**--export** _FORMAT_\n> Export the tree to `csv`, `xml`, or `json`."],["DESCRIPTION","**wizu** (binary name **wisu**) is a directory tree viewer offering both a classic tree-like display and a fully interactive TUI. Features include dual display modes, search, theme-aware coloring via LS_COLORS, emoji file icons, gitignore support, multiple sort modes, time-based filtering, and export to CSV, XML, and JSON formats."],["HISTORY","**wizu** was created by **Andrea Frolli** (sh1zen) and is written in **Rust**."],["SEE ALSO","[tree](/man/tree)(1), [eza](/man/eza)(1), [broot](/man/broot)(1)"]],"e":["tree","eza","broot"],"x":"wisu"},"wpgtk":{"n":"wpgtk","t":"Wallpaper-based colorscheme manager","s":[["TAGLINE","Wallpaper-based colorscheme manager"],["TLDR","**Add wallpaper to collection**\n\n```wpg -a [/path/to/image.jpg]```\n\n**Set wallpaper and apply colorscheme**\n\n```wpg -s [wallpaper.jpg]```\n\n**List added wallpapers**\n\n```wpg -l```\n\n**Show current wallpaper**\n\n```wpg -c```\n\n**Shuffle wallpaper's colorscheme**\n\n```wpg -z [wallpaper.jpg]```\n\n**Import JSON colorscheme**\n\n```wpg -i [wallpaper.jpg] [colorscheme.json]```\n\n**Apply colorscheme to terminal**\n\n```wpg -t```\n\n**Set wallpaper without changing colorscheme**\n\n```wpg -ns [wallpaper.jpg]```\n\n**Adjust saturation of colorscheme**\n\n```wpg --sat [wallpaper.jpg] [0.1]```\n\n**Launch GUI**\n\n```wpgtk```"],["SYNOPSIS","**wpg** [_options_] [_wallpaper_]\n\n**wpgtk**"],["PARAMETERS","**-a** _image_\n> Add wallpaper to collection.\n\n**-s** _wallpaper_ [_fallback_]\n> Set wallpaper and colorscheme.\n\n**-l**\n> List added wallpapers.\n\n**-c**\n> Print current wallpaper name.\n\n**-d** _wallpaper_\n> Delete wallpaper from collection.\n\n**-t**\n> Apply colorscheme to terminal.\n\n**-z** _wallpaper_\n> Shuffle/regenerate colorscheme.\n\n**-n**\n> Do not set wallpaper when applying colorscheme (use with -s).\n\n**-i** _wallpaper_ _json_\n> Import colorscheme from JSON file.\n\n**-A** _wallpaper_\n> Auto-adjust colorscheme for distinct foreground and background.\n\n**--auto** _wallpaper_\n> Auto-generate foreground versions of the first 8 colors.\n\n**--sat** _wallpaper_ _value_\n> Adjust saturation of all colors (negative values subtract).\n\n**--brt** _wallpaper_ _value_\n> Adjust brightness of all colors (negative values subtract).\n\n**--theme** [_theme_]\n> Set a preset theme. Without arguments, list available themes.\n\n**--pywal** _theme_\n> Select and set a pywal theme.\n\n**-h**\n> Display help."],["DESCRIPTION","**wpgtk** is a colorscheme, wallpaper, and template manager for Unix-like systems. It uses pywal as its backend to extract colors from wallpapers and apply them system-wide through configurable templates.\n\nThe tool generates coordinated colorschemes from wallpaper images, applying them to terminals, GTK themes, window managers, and other applications. Templates define how colors are applied to each application's configuration files. Both a CLI (wpg) and GUI (wpgtk) interface are provided.\n\nColorschemes are saved with their wallpapers, allowing instant theme switching. Colors can be manually edited, imported from JSON (compatible with terminal.sexy), or auto-adjusted for better contrast."],["CAVEATS","Requires pywal as backend. Template files need manual setup for each application. Some applications require restart to apply new colors. GTK theme changes may need session restart."],["HISTORY","**wpgtk** was created by deviantfero to provide a user-friendly interface on top of pywal. While pywal handles color extraction and basic application, wpgtk adds wallpaper management, color editing, template management, and a graphical interface for users who prefer visual configuration."],["INSTALL","```nix: nix profile install nixpkgs#wpgtk```"],["SEE ALSO","[wal](/man/wal)(1), [feh](/man/feh)(1), [nitrogen](/man/nitrogen)(1)"]],"e":["wal","feh","nitrogen"],"x":"wpg -a [/path/to/image.jpg]"},"xed":{"n":"xed","t":"Cinnamon desktop text editor","s":[["TAGLINE","Cinnamon desktop text editor"],["TLDR","**Start** editor\n\n```xed```\n\n**Open** files\n\n```xed [path/to/file1 path/to/file2 ...]```\n\nOpen with **encoding**\n\n```xed --encoding [WINDOWS-1252] [path/to/file]```\n\nList **encodings**\n\n```xed --list-encodings```\n\nGo to **line** number\n\n```xed +[10] [path/to/file]```"],["SYNOPSIS","**xed** [_OPTIONS_] [_FILE..._]"],["PARAMETERS","**--encoding** _ENCODING_\n> Open files using specific character encoding\n\n**--list-encodings**\n> Print all supported encodings\n\n**+**_LINE_\n> Go to specified line number\n\n**--new-window**\n> Open in new window\n\n**--new-document**\n> Open new document"],["DESCRIPTION","**xed** is the default text editor for the Cinnamon desktop environment. It is based on gedit and provides syntax highlighting, tabs, spell checking, and plugin support.\n\nThe editor supports multiple character encodings and can convert between them."],["CAVEATS","Designed for Cinnamon desktop. May require GTK libraries on other environments. Plugins require Python."],["INSTALL","```dnf: sudo dnf install xed```\n\n```pacman: sudo pacman -S xed```\n\n```zypper: sudo zypper install xed```\n\n```nix: nix profile install nixpkgs#xed```"],["SEE ALSO","[gedit](/man/gedit)(1), [pluma](/man/pluma)(1), [nano](/man/nano)(1)"]],"e":["gedit","pluma","nano"],"x":"xed"},"zopflipng":{"n":"zopflipng","t":"Optimize PNG files with Zopfli compression","s":[["TAGLINE","Optimize PNG files with Zopfli compression"],["TLDR","**Optimize PNG**\n\n```zopflipng [input.png] [output.png]```\n\n**In-place optimization**\n\n```zopflipng -y [file.png] [file.png]```\n\n**Lossy optimization**\n\n```zopflipng --lossy_transparent [input.png] [output.png]```\n\n**Maximum compression**\n\n```zopflipng -m [input.png] [output.png]```\n\n**Keep metadata**\n\n```zopflipng --keepchunks=iCCP,sRGB [input.png] [output.png]```\n\n**Quick mode**\n\n```zopflipng -q [input.png] [output.png]```\n\n**Set specific number of iterations**\n\n```zopflipng --iterations=[50] [input.png] [output.png]```"],["SYNOPSIS","**zopflipng** [_-m_] [_-q_] [_--lossy_transparent_] [_options_] _input_ _output_"],["PARAMETERS","**-m**\n> Compress more: use more iterations (slower but better compression).\n\n**-q**\n> Quick mode, less thorough compression.\n\n**-y**\n> Allow overwriting the output file.\n\n**--iterations** _N_\n> Number of iterations for compression (default: 15 for small files, 5 for large files).\n\n**--lossy_transparent**\n> Lossy optimization of fully transparent pixels for smaller file size.\n\n**--keepchunks** _CHUNKS_\n> Comma-separated list of metadata chunk names to preserve (e.g., iCCP,sRGB).\n\n**--keeptime**\n> Keep the original modification time of the input file.\n\n**--filters** _TYPES_\n> Filter strategies to try: 0-4, m (minimum sum), e (entropy), p (predefined), b (brute force).\n\n**--prefix** _PREFIX_\n> Add prefix to output filenames (may contain a directory path)."],["DESCRIPTION","**zopflipng** optimizes PNG files using the Zopfli compression algorithm, which produces deflate-compatible output that works everywhere standard PNG files do. It typically achieves 3-8% smaller files compared to standard PNG optimization tools.\n\nThe tool strips metadata chunks by default for maximum size reduction, though specific chunks can be preserved with the **--keepchunks** option. A lossy transparency mode (**--lossy_transparent**) further reduces file size by optimizing invisible fully-transparent pixels without any visible quality change.\n\nzopflipng is intentionally slow, trading compression time for smaller output. It is designed for final-stage optimization in build pipelines rather than interactive use. The **-m** flag enables additional iterations for even better compression at the cost of more processing time."],["CAVEATS","Very slow for large images due to exhaustive compression search. CPU intensive. Best suited for final-stage optimization in build pipelines rather than interactive use. Strips metadata chunks by default."],["HISTORY","**Zopfli** was created by **Google** for maximum deflate compression. zopflipng applies it to PNG images."],["INSTALL","```apt: sudo apt install zopfli```\n\n```dnf: sudo dnf install zopfli```\n\n```pacman: sudo pacman -S zopfli```\n\n```apk: sudo apk add zopfli```\n\n```zypper: sudo zypper install zopfli```\n\n```brew: brew install zopfli```\n\n```nix: nix profile install nixpkgs#zopfli```"],["SEE ALSO","[optipng](/man/optipng)(1), [pngcrush](/man/pngcrush)(1), [pngquant](/man/pngquant)(1)"]],"e":["optipng","pngcrush","pngquant"],"x":"zopflipng [input.png] [output.png]"}});
