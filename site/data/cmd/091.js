window.LCL_BUCKET(91,{"a2ping":{"n":"a2ping","t":"Convert between PostScript, EPS, and PDF formats","s":[["TAGLINE","Convert between PostScript, EPS, and PDF formats"],["TLDR","**Convert** a PostScript file to PDF\n\n```a2ping [input.ps] [output.pdf]```\n\n**Convert** an EPS file to PDF with tight bounding box\n\n```a2ping --bboxfrom=compute-gs [input.eps] [output.pdf]```\n\nConvert to **specific output format**\n\n```a2ping --outformat=[pdf|eps|ps] [input.ps] [output]```"],["SYNOPSIS","**a2ping** [_options_] _inputfile_ [_outputfile_]"],["DESCRIPTION","**a2ping** is a Perl script that converts between various page description formats including PostScript (PS), Encapsulated PostScript (EPS), PDF, and related formats. It acts as a smart wrapper around Ghostscript and other tools, automatically detecting input formats and applying appropriate conversions.\n\nThe tool is particularly useful for converting EPS graphics with proper bounding box handling, making it ideal for preparing figures for LaTeX documents. It can compute accurate bounding boxes, handle font embedding, and optimize output for different use cases."],["PARAMETERS","**--outformat=**_format_\n> Output format: pdf, eps, eps2, eps3, ps, ps2, ps3\n\n**--bboxfrom=**_method_\n> Bounding box source: adsc (from DSC comments), compute-gs (compute using Ghostscript)\n\n**--resolution=**_dpi_\n> Set output resolution in dots per inch\n\n**--compress=**_method_\n> Compression method: none, zip, best\n\n**--gsextra=**_options_\n> Pass extra options to Ghostscript\n\n**--help**\n> Display help information"],["CAVEATS","Requires Ghostscript to be installed for most conversions. Some complex PostScript features may not convert perfectly. The tool is primarily designed for single-page documents; multi-page handling may vary."],["HISTORY","**a2ping** was written by Peter Szabo and first released in **2003**. It was developed to address common difficulties in converting EPS files to PDF with correct bounding boxes, particularly for users preparing scientific documents with LaTeX. The tool became part of TeX Live distributions."],["INSTALL","```pacman: sudo pacman -S texlive-binextra```\n\n```apk: sudo apk add texlive-binextra```"],["SEE ALSO","[gs](/man/gs)(1), [ps2pdf](/man/ps2pdf)(1), [epstopdf](/man/epstopdf)(1), [convert](/man/convert)(1)"],["RESOURCES","```[Source code](https://github.com/pts/a2ping)```\n\n```[Documentation](https://ctan.org/pkg/a2ping)```"]],"e":["gs","ps2pdf","epstopdf","convert"],"x":"a2ping [input.ps] [output.pdf]"},"aa-notify":{"n":"aa-notify","t":"Display and monitor AppArmor denial notifications","s":[["TAGLINE","Display and monitor AppArmor denial notifications"],["TLDR","**Display summary** of AppArmor denials since last login\n\n```aa-notify --since-last```\n\n**Show summary** for the last N days\n\n```aa-notify --since-days [7]```\n\n**Poll logs** and display desktop notifications\n\n```aa-notify --poll --user [username] --display $DISPLAY```\n\n**Search a specific file** for AppArmor messages\n\n```aa-notify --file [/var/log/audit/audit.log]```\n\n**Show verbose messages** with summaries\n\n```aa-notify --since-days [3] --verbose```\n\n**Wait before displaying notifications** (useful at startup)\n\n```aa-notify --poll --wait [10]```"],["SYNOPSIS","**aa-notify** [_options_]"],["PARAMETERS","**-p**, **--poll**\n> Poll AppArmor logs continuously and display desktop notifications for DENIED messages\n\n**-f** _FILE_, **--file=**_FILE_\n> Search FILE for AppArmor messages instead of the default log locations\n\n**-l**, **--since-last**\n> Show summary of messages since last login\n\n**-s** _NUM_, **--since-days=**_NUM_\n> Show summary of messages for the last NUM days\n\n**-u** _USER_, **--user=**_USER_\n> Drop privileges to USER when running privileged; required with --poll for desktop notifications\n\n**-w** _NUM_, **--wait=**_NUM_\n> Wait NUM seconds before displaying notifications (useful for autostart)\n\n**--display** _$DISPLAY_\n> Set the DISPLAY environment variable for desktop notifications\n\n**-v**, **--verbose**\n> Show full messages along with summaries\n\n**-h**, **--help**\n> Display usage information"],["DESCRIPTION","**aa-notify** displays information about logged AppArmor security events, specifically DENIED messages that occur when applications attempt actions blocked by their AppArmor profiles.\n\nThe tool operates in two modes: summary mode shows a report of past denials from a specified time period, while poll mode continuously monitors logs and delivers desktop notifications in real-time. This makes it useful for both auditing historical security events and receiving immediate alerts about policy violations.\n\nAppArmor messages are read from the systemd journal, **/var/log/syslog**, **/var/log/kern.log**, or **/var/log/audit/audit.log** when auditd is installed. The tool requires appropriate privileges to read these logs, typically running under sudo."],["CAVEATS","Desktop notifications require a running display server and proper DISPLAY environment variable configuration. When using sudo, the DISPLAY variable may be reset, requiring explicit **--display** specification. For systems with auditd, the log file path must be explicitly set with **--file /var/log/audit/audit.log**."],["HISTORY","**aa-notify** is part of the AppArmor utilities suite. AppArmor originated at Immunix in the late **1990s** and was acquired by Novell in **2005**. It became part of the mainline Linux kernel in version **2.6.36** released in **2010**. The notification daemon was added to help administrators monitor security events in desktop environments."],["INSTALL","```apk: sudo apk add apparmor-utils```\n\n```zypper: sudo zypper install apparmor-utils```\n\n```nix: nix profile install nixpkgs#apparmor-utils```"],["SEE ALSO","[aa-logprof](/man/aa-logprof)(8), [aa-decode](/man/aa-decode)(8), [aa-status](/man/aa-status)(8), [aa-complain](/man/aa-complain)(8), [journalctl](/man/journalctl)(1), [apparmor](/man/apparmor)(7)"],["RESOURCES","```[Source code](https://gitlab.com/apparmor/apparmor)```\n\n```[Homepage](https://apparmor.net/)```\n\n```[Documentation](https://gitlab.com/apparmor/apparmor/-/wikis/home)```"]],"e":["aa-logprof","aa-decode","aa-status","aa-complain","journalctl","apparmor"],"x":"aa-notify --since-last"},"apt-ftparchive":{"n":"apt-ftparchive","t":"Generate index files for APT repositories.","s":[["TAGLINE","Generate index files for APT repositories."],["TLDR","**Generate** Packages file\n\n```apt-ftparchive packages [pool/] > [Packages]```\n\nGenerate **Sources** file\n\n```apt-ftparchive sources [pool/] > [Sources]```\n\nGenerate **Release** file\n\n```apt-ftparchive release [dists/stable] > [Release]```\n\nGenerate with **configuration file**\n\n```apt-ftparchive generate [config.conf]```"],["SYNOPSIS","**apt-ftparchive** [_options_] _command_ [_arguments_]"],["DESCRIPTION","**apt-ftparchive** generates index files for APT repositories. It creates Packages, Sources, Contents, and Release files from a directory of .deb and .dsc files.\n\nThis tool is essential for creating and maintaining local APT repositories or mirrors."],["PARAMETERS","**packages** _dir_\n> Generate Packages file from directory\n\n**sources** _dir_\n> Generate Sources file\n\n**contents** _dir_\n> Generate Contents file\n\n**release** _dir_\n> Generate Release file\n\n**generate** _config_\n> Generate files per configuration\n\n**clean** _config_\n> Clean up database files\n\n**-c** _file_\n> Configuration file\n\n**-d** _db_\n> Database directory\n\n**--md5**, **--sha1**, **--sha256**, **--sha512**\n> Hash algorithms to generate"],["CAVEATS","Release files should be signed with gpg for secure repositories. Large repositories may take significant time to index. Database caching improves regeneration speed."],["HISTORY","**apt-ftparchive** is part of APT, designed for repository maintainers to create the index files that APT clients use to find and download packages."],["INSTALL","```apt: sudo apt install apt-utils```\n\n```dnf: sudo dnf install apt-utils```\n\n```pacman: sudo pacman -S apt```\n\n```apk: sudo apk add apt```\n\n```brew: brew install apt```\n\n```nix: nix profile install nixpkgs#apt```"],["SEE ALSO","[apt](/man/apt)(8)"]],"e":["apt"],"x":"apt-ftparchive packages [pool/] > [Packages]"},"asciitopgm":{"n":"asciitopgm","t":"Convert ASCII art to PGM grayscale image","s":[["TAGLINE","Convert ASCII art to PGM grayscale image"],["TLDR","**Convert** ASCII art to PGM (note: height comes before width)\n\n```asciitopgm [height] [width] < [ascii.txt] > [output.pgm]```\n\n**Brighten** the output with a divisor\n\n```asciitopgm -divisor=[2] [height] [width] [ascii.txt] > [output.pgm]```"],["SYNOPSIS","**asciitopgm** [_-divisor=integer_] _height_ _width_ [_asciifile_]"],["DESCRIPTION","**asciitopgm** converts ASCII art text to PGM (Portable Graymap) image format. Each character becomes one pixel; a large character such as a capital M produces a dark pixel, while a small one such as a period produces a light pixel.\n\nThis is useful for converting legacy ASCII artwork to image formats or for creating images from text patterns. Output values range from 0 to a maximum of 127."],["PARAMETERS","**height**\n> Output image height in pixels (number of rows)\n\n**width**\n> Output image width in pixels (number of columns)\n\n**asciifile**\n> Input ASCII file (reads from stdin if omitted)\n\n**-divisor** _integer_\n> Divide each character's blackness value, brightening the output. Default is 1; larger positive values produce brighter images."],["CAVEATS","Character-to-grayscale mapping may not produce optimal results for all ASCII art styles. Output dimensions must be specified manually."],["HISTORY","**asciitopgm** is part of the Netpbm image processing toolkit, which evolved from PBMPLUS in the late **1980s**."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pgmtopbm](/man/pgmtopbm)(1), [pnmtopng](/man/pnmtopng)(1), [img2txt](/man/img2txt)(1)"],["RESOURCES","```[Homepage](https://netpbm.sourceforge.net/)```\n\n```[Documentation](https://netpbm.sourceforge.net/doc/asciitopgm.html)```"]],"e":["pgmtopbm","pnmtopng","img2txt"],"x":"asciitopgm [height] [width] < [ascii.txt] > [output.pgm]"},"aws-opensearch":{"n":"aws-opensearch","t":"Manage OpenSearch Service domains for search and analytics.","s":[["TAGLINE","Manage OpenSearch Service domains for search and analytics."],["TLDR","**List all OpenSearch domains**\n\n```aws opensearch list-domain-names```\n\n**Describe a domain**\n\n```aws opensearch describe-domain --domain-name [domain_name]```\n\n**Get domain configuration**\n\n```aws opensearch describe-domain-config --domain-name [domain_name]```\n\n**Create a new domain**\n\n```aws opensearch create-domain --domain-name [domain_name] --engine-version OpenSearch_2.11 --cluster-config InstanceType=t3.small.search,InstanceCount=1```\n\n**Update domain configuration**\n\n```aws opensearch update-domain-config --domain-name [domain_name] --cluster-config InstanceType=r6g.large.search```\n\n**Delete a domain**\n\n```aws opensearch delete-domain --domain-name [domain_name]```\n\n**List available engine versions**\n\n```aws opensearch list-versions```\n\n**Get upgrade history**\n\n```aws opensearch get-upgrade-history --domain-name [domain_name]```"],["SYNOPSIS","**aws opensearch** _subcommand_ [_options_]"],["DESCRIPTION","**aws opensearch** is a subcommand of the AWS CLI that manages Amazon OpenSearch Service, a managed search and analytics engine. It is the successor to Amazon Elasticsearch Service and is compatible with OpenSearch and legacy Elasticsearch APIs.\n\nOpenSearch domains are clusters that run the OpenSearch engine. Each domain has a cluster configuration (instance types, counts), storage settings (EBS volumes), access policies, and optional features like fine-grained access control and encryption.\n\nThe service supports use cases including log analytics, full-text search, application monitoring, and SIEM. OpenSearch Dashboards provides visualization capabilities."],["PARAMETERS","**list-domain-names**\n> List all domains in the account.\n\n**describe-domain**\n> Get domain status and configuration.\n\n**describe-domain-config**\n> Get detailed configuration settings.\n\n**create-domain**\n> Create a new OpenSearch domain.\n\n**update-domain-config**\n> Modify domain settings.\n\n**delete-domain**\n> Remove a domain.\n\n**list-versions**\n> List available OpenSearch and Elasticsearch versions.\n\n**upgrade-domain**\n> Initiate a version upgrade.\n\n**get-upgrade-status**\n> Check upgrade progress.\n\n**--domain-name** _name_\n> Name of the domain.\n\n**--engine-version** _version_\n> OpenSearch or Elasticsearch version.\n\n**--cluster-config** _config_\n> Instance type, count, and zone awareness.\n\n**--ebs-options** _options_\n> EBS volume configuration.\n\n**--access-policies** _json_\n> Resource-based access policy.\n\n**--vpc-options** _options_\n> VPC configuration for private domains.\n\n**--encryption-at-rest-options** _options_\n> Enable encryption at rest."],["CAVEATS","Domain names must be lowercase and unique within a region. Configuration changes can take 15-30 minutes to apply. Domains in a VPC cannot be made public later. Version upgrades may require blue/green deployment. Fine-grained access control requires HTTPS and node-to-node encryption. Deleting a domain permanently removes all data."],["HISTORY","**Amazon Elasticsearch Service** launched in **October 2015** as a managed Elasticsearch offering. Following the Elasticsearch license change, AWS forked the project and rebranded the service as **Amazon OpenSearch Service** in **September 2021**, based on the open-source OpenSearch project. The service has added features like UltraWarm storage tiers, serverless collections, and vector search for AI applications."],["INSTALL","```dnf: sudo dnf install aws```\n\n```pacman: sudo pacman -S aws-cli```\n\n```apk: sudo apk add aws-cli```\n\n```zypper: sudo zypper install aws-cli```"],["SEE ALSO","[aws](/man/aws)(1), [curl](/man/curl)(1), [aws-logs](/man/aws-logs)(1), [aws-firehose](/man/aws-firehose)(1)"],["RESOURCES","```[Source code](https://github.com/aws/aws-cli)```\n\n```[Homepage](https://aws.amazon.com/cli/)```\n\n```[Documentation](https://docs.aws.amazon.com/cli/latest/reference/opensearch/index.html)```"]],"e":["aws","curl","aws-logs","aws-firehose"],"x":"aws opensearch list-domain-names"},"bluebuild":{"n":"bluebuild","t":"Build custom immutable Linux images","s":[["TAGLINE","Build custom immutable Linux images"],["TLDR","**Build a custom image** from a recipe file\n\n```bluebuild build [recipe.yml]```\n\n**Build and push** to a container registry\n\n```bluebuild build --push [recipe.yml]```\n\n**Generate a Containerfile** from the recipe\n\n```bluebuild generate [recipe.yml]```\n\n**Start a new BlueBuild project** in a new directory\n\n```bluebuild new [project-name]```\n\n**Initialize a BlueBuild project** in the current empty directory\n\n```bluebuild init```\n\n**Validate a recipe file** for errors\n\n```bluebuild validate [recipe.yml]```\n\n**Build locally and rebase** the running system onto the image\n\n```bluebuild switch [recipe.yml]```\n\n**Upgrade the running system** to the latest BlueBuild image\n\n```bluebuild upgrade```"],["SYNOPSIS","**bluebuild** [_command_] [_options_] [_recipe_]"],["PARAMETERS","**build** _recipe_\n> Template out the files and build the image with Docker, Podman, or Buildah.\n\n**generate** _recipe_\n> Generate the Containerfile used to build the recipe and print or write it out.\n\n**new** _name_\n> Create a new BlueBuild project in a new directory, based on the official template.\n\n**init**\n> Set up a new BlueBuild project in the current empty directory.\n\n**validate** _recipe_\n> Check a recipe and its modules for errors before building.\n\n**switch** _recipe_\n> Build the image locally, export it as an oci-archive tarball, and rebase or upgrade the running system onto it. Runs as a normal user, asking for sudo only when needed.\n\n**rebase** _recipe_\n> Build locally and rebase the current rpm-ostree system onto the resulting image.\n\n**upgrade** _recipe_\n> Build locally and upgrade the current rpm-ostree system to the resulting image.\n\n**generate-iso** _recipe_\n> Generate an offline ISO installation image from the recipe.\n\n**completions** _shell_\n> Print shell completions for bash, zsh, fish, and others to stdout.\n\n**upgrade** (no recipe)\n> Upgrade the bluebuild CLI itself to the latest version.\n\n**--push**\n> Push the built image to the configured container registry (build command).\n\n**--registry** _url_\n> Override the container registry to push to (build command).\n\n**--rechunk**\n> Rechunk the image layers for more efficient updates (build command).\n\n**-v**, **--verbose**\n> Enable verbose output.\n\n**--help**\n> Display help information."],["DESCRIPTION","**BlueBuild** is a tool for creating custom immutable Linux images based on Universal Blue and Fedora Atomic. It uses YAML recipe files to declaratively define image customizations including packages, scripts, and system configurations.\n\nThe tool simplifies the process of building custom rpm-ostree images that can be deployed on Fedora Silverblue, Kinoite, or other atomic Fedora variants. Recipes can specify base images, add/remove packages, include custom scripts, and configure system settings.\n\nBlueBuild integrates with GitHub Actions for automated CI/CD builds via a reusable GitHub Action; the **generate** command produces the Containerfile that the build uses. Images are typically hosted on GitHub Container Registry (ghcr.io).\n\nUsers can switch their existing Fedora Atomic system onto a locally built image with **bluebuild switch**, or onto a published image with **rebase** and **upgrade**. The project provides a library of reusable modules for common customizations like adding Flatpak repositories, configuring fonts, or setting up development tools."],["CAVEATS","Requires Podman or Docker for local builds. GitHub Actions integration requires repository write access and container registry credentials. Recipe changes may require complete image rebuilds. Images built locally may differ from CI-built images due to architecture or environment differences."],["HISTORY","**BlueBuild** emerged from the Universal Blue community in **2023** as a way to simplify custom image creation for Fedora Atomic desktops. It was inspired by the growing interest in immutable Linux distributions and the need for user-friendly customization tools. The project provides a higher-level abstraction over raw Containerfile/Dockerfile builds."],["SEE ALSO","[podman](/man/podman)(1), [rpm-ostree](/man/rpm-ostree)(1), [docker](/man/docker)(1), [buildah](/man/buildah)(1)"],["RESOURCES","```[Source code](https://github.com/blue-build/cli)```\n\n```[Homepage](https://blue-build.org/)```\n\n```[Documentation](https://blue-build.org/how-to/setup/)```"]],"e":["podman","rpm-ostree","docker","buildah"],"x":"bluebuild build [recipe.yml]"},"btrfs-send":{"n":"btrfs-send","t":"Generate a stream of btrfs subvolume changes for backup and replication","s":[["TAGLINE","Generate a stream of btrfs subvolume changes for backup and replication"],["TLDR","**Send a snapshot** to stdout\n\n```btrfs send [/path/to/snapshot]```\n\n**Send to a file**\n\n```btrfs send [/path/to/snapshot] -f [backup.send]```\n\n**Send incremental** (only differences from parent)\n\n```btrfs send -p [/path/to/parent] [/path/to/snapshot]```\n\n**Send with multiple clone sources**\n\n```btrfs send -c [/path/to/clone1] -c [/path/to/clone2] [/path/to/snapshot]```\n\n**Send and receive** in one pipeline\n\n```btrfs send [/path/to/snapshot] | btrfs receive [/path/to/destination]```\n\n**Send incremental over SSH**\n\n```btrfs send -p [/snapshots/old] [/snapshots/new] | ssh [user@host] btrfs receive [/backup]```\n\n**Quiet mode** (no progress)\n\n```btrfs send -q [/path/to/snapshot]```"],["SYNOPSIS","**btrfs send** [**-v**] [**-p** _parent_] [**-c** _clone-src_] [**-f** _outfile_] _subvolume_"],["DESCRIPTION","**btrfs send** generates a stream of instructions representing a btrfs subvolume or snapshot. This stream can be received by **btrfs receive** to recreate the subvolume on another filesystem, enabling backup and replication workflows.\n\nThe send operation works on read-only snapshots and produces a binary stream containing file data, metadata, and structural information. When a parent snapshot is specified, only the differences (incremental send) are transmitted, dramatically reducing transfer size for regular backups.\n\nThe stream format is forward-compatible and can be piped directly, saved to a file, or transferred over network connections like SSH."],["PARAMETERS","**-f** _file_\n> Write stream to file instead of stdout.\n\n**-p** _parent_\n> Parent subvolume for incremental send.\n\n**-c** _clone-src_\n> Clone source (additional reference for deduplication).\n\n**-v**\n> Verbose mode.\n\n**-q**\n> Quiet mode (no progress).\n\n**--no-data**\n> Send without file data (metadata only).\n\n**-e**\n> End stream after subvolume data.\n\n**--proto** _N_\n> Use send stream protocol version N."],["CAVEATS","Source subvolume must be read-only. Parent snapshot must exist on receive side for incremental restores. Stream is not human-readable. Interrupting send/receive may leave incomplete subvolumes. File permissions and ownership require appropriate privileges to restore. Compressed send streams require protocol version 2+."],["HISTORY","**btrfs send/receive** was introduced in Linux kernel **3.6** (released **September 2012**) as part of btrfs development. It was designed to enable efficient snapshot-based backup and replication, similar to ZFS send/receive. The feature has been enhanced over time with compressed transfers (protocol v2) and improved performance for large filesystems."],["INSTALL","```apt: sudo apt install btrfs-progs```\n\n```dnf: sudo dnf install btrfs-progs```\n\n```pacman: sudo pacman -S btrfs-progs```\n\n```apk: sudo apk add btrfs-progs```\n\n```brew: brew install btrfs-progs```\n\n```nix: nix profile install nixpkgs#btrfs-progs```"],["SEE ALSO","[btrfs-subvolume](/man/btrfs-subvolume)(8), [btrfs](/man/btrfs)(8), [rsync](/man/rsync)(1)"]],"e":["btrfs-subvolume","btrfs","rsync"],"x":"btrfs send [/path/to/snapshot]"},"build":{"n":"build","t":"Build Python packages for distribution","s":[["TAGLINE","Build Python packages for distribution"],["TLDR","**Build a Python package**\n\n```python -m build```\n\n**Build only source distribution**\n\n```python -m build --sdist```\n\n**Build only wheel**\n\n```python -m build --wheel```\n\n**Build with specific output directory**\n\n```python -m build --outdir [dist/]```\n\n**Build with verbose output**\n\n```python -m build -v```\n\n**Build without isolation** (use current environment)\n\n```python -m build --no-isolation```"],["SYNOPSIS","**python -m build** [_options_] [_source_directory_]"],["PARAMETERS","**--sdist**\n> Build a source distribution only.\n\n**--wheel**\n> Build a wheel only.\n\n**--outdir** _directory_\n> Output directory for built packages (default: dist/).\n\n**--no-isolation**\n> Build without creating isolated environment.\n\n**--skip-dependency-check**\n> Skip dependency verification.\n\n**--config-setting** _key=value_\n> Pass settings to the build backend.\n\n**-v**, **--verbose**\n> Increase output verbosity.\n\n**-C** _setting_\n> Short form of --config-setting."],["DESCRIPTION","**build** is the standard Python package builder that creates source distributions (sdist) and wheels from Python projects. It is the recommended way to build packages for distribution on PyPI, replacing direct calls to **setup.py sdist bdist_wheel**.\n\nBy default, build creates an isolated virtual environment with only build dependencies installed, ensuring reproducible builds. It reads configuration from **pyproject.toml** and delegates to the configured build backend (setuptools, flit, hatch, etc.).\n\nThe output is placed in a **dist/** directory containing a **.tar.gz** source distribution and a **.whl** wheel file. These can be uploaded to PyPI with **twine** or installed directly with **pip**."],["CAVEATS","Requires a **pyproject.toml** file with build system configuration. The isolated build environment downloads dependencies on each build; use **--no-isolation** for faster repeated builds during development. Some legacy packages without proper pyproject.toml may fail to build."],["HISTORY","The **build** package was created by the Python Packaging Authority (PyPA) as part of modernizing Python packaging. It was first released in **2020** to provide a simple, standardized way to build packages according to PEP 517 and PEP 518 specifications. It replaced the older practice of running **python setup.py** commands directly, supporting any PEP 517 compliant build backend."],["INSTALL","```zypper: sudo zypper install build```"],["SEE ALSO","[pip](/man/pip)(1), [twine](/man/twine)(1), [flit](/man/flit)(1), [hatch](/man/hatch)(1)"]],"e":["pip","twine","flit","hatch"],"x":"python -m build"},"castero":{"n":"castero","t":"TUI podcast client for the terminal","s":[["TAGLINE","TUI podcast client for the terminal"],["TLDR","**Launch** the podcast client interactively\n\n```castero```\n\n**Import** podcast subscriptions from an OPML file\n\n```castero --import [path/to/subscriptions.opml]```\n\n**Export** current subscriptions to an OPML file\n\n```castero --export [path/to/feeds.opml]```\n\n**Show version** information\n\n```castero --version```"],["SYNOPSIS","**castero** [**-h**] [**-V**] [**--import** _PATH_] [**--export** _PATH_]"],["PARAMETERS","**-h**, **--help**\n> Show help message and exit.\n\n**-V**, **--version**\n> Display version information and exit.\n\n**--import** _PATH_\n> Path to an OPML file of feeds to import; adds subscriptions and exits.\n\n**--export** _PATH_\n> Path to save current feeds as an OPML file; exports and exits."],["DESCRIPTION","**castero** is a terminal-based podcast client that lets users subscribe to RSS podcast feeds, browse episodes, download them for offline playback, and play them directly from the terminal. It uses a curses-based TUI with multiple switchable layouts. Playback is delegated to either **VLC** or **mpv** as an external media player backend.\n\nKey capabilities include adding, deleting, and reloading podcast feeds, queuing episodes for playback, adjusting volume and playback speed, seeking within episodes, marking episodes as played or unplayed, and filtering or searching within feeds. Subscriptions can be imported and exported via OPML for portability between podcast clients."],["KEY BINDINGS","```\nh    Help              a    Add feed\nq    Exit              d    Delete feed\nr    Reload all        R    Reload selected\nEnter  Play episode    Space  Add to queue\nc    Clear queue       n    Next in queue\np/k  Pause/play        f/l  Seek forward\nb/j  Seek backward     =/−  Volume up/down\n]/[  Speed up/down     m    Mark played\ns    Save offline      x    Delete download\n/    Filter/search     1-5  Switch layout\n```"],["CONFIGURATION","Configuration stored at **~/.config/castero/castero.conf** (created on first run). User data and downloads stored at **~/.local/share/castero/**.\n\nNotable options include **player** (mpv or vlc), **default_layout** (1-5), **default_playback_speed**, **default_volume** (0-100), **seek_distance_forward/backward** in seconds, **reload_on_start**, **custom_download_dir**, and proxy settings (**proxy_http**, **proxy_https**). All keybindings and colors are customizable."],["CAVEATS","Requires an external media player (**VLC** or **mpv**) to be installed for audio playback. Python 3.5+ and SQLite3 are required. The project has not seen new releases since 2022 and may not be actively maintained."],["HISTORY","**castero** was created by **Jake Robertson** and first released in **April 2018**. Major milestones include episode downloading (v0.2.0, 2018), mpv backend support (v0.4.0, 2019), SQLite database and played/unplayed tracking (v0.5.0, 2019), and OPML import/export (v0.6.0, 2019). The latest release is v0.9.5."],["INSTALL","```nix: nix profile install nixpkgs#castero```"],["SEE ALSO","[mpv](/man/mpv)(1), [vlc](/man/vlc)(1), [newsboat](/man/newsboat)(1), [cmus](/man/cmus)(1)"]],"e":["mpv","vlc","newsboat","cmus"],"x":"castero"},"csplit":{"n":"csplit","t":"split files by context patterns","s":[["TAGLINE","split files by context patterns"],["TLDR","Split a file starting second part at **line 10**\n\n```csplit [path/to/file] 10```\n\nSplit a file into **three parts** at lines 7 and 23\n\n```csplit [path/to/file] 7 23```\n\nStart a new part at **every 5th line**\n\n```csplit [path/to/file] 5 {*}```\n\nSplit every 5th line, **ignoring division errors**\n\n```csplit -k [path/to/file] 5 {*}```\n\nUse a **custom prefix** for output files\n\n```csplit [path/to/file] 5 -f [prefix]```\n\nSplit above first line matching a **regex**\n\n```csplit [path/to/file] /[regex]/```"],["SYNOPSIS","**csplit** [_options_] _file_ _pattern_..."],["DESCRIPTION","**csplit** splits a file into pieces based on context (line numbers or patterns). It generates files named xx00, xx01, etc. by default.\n\nUnlike split which divides by size, csplit divides by content structure, making it useful for splitting log files, configuration files, or documents at specific boundaries."],["PARAMETERS","**-f, --prefix** _prefix_\n> Use specified prefix instead of 'xx'\n\n**-k, --keep-files**\n> Don't remove output files on error\n\n**-n, --digits** _digits_\n> Number of digits in output filename\n\n**-s, --quiet**\n> Don't print byte counts\n\n**{n}**\n> Repeat previous pattern n times\n\n**{*}**\n> Repeat previous pattern as many times as possible"],["CAVEATS","Part of GNU coreutils. Without -k, output files are deleted if an error occurs. Pattern repetition with {*} will fail if the pattern doesn't divide the file evenly (use -k to keep partial output)."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[split](/man/split)(1), [cut](/man/cut)(1), [head](/man/head)(1)"],["RESOURCES","```[Source code](https://git.savannah.gnu.org/cgit/coreutils.git)```\n\n```[Homepage](https://www.gnu.org/software/coreutils/)```\n\n```[Documentation](https://www.gnu.org/software/coreutils/manual/html_node/csplit-invocation.html)```"]],"e":["split","cut","head"],"x":"csplit [path/to/file] 10"},"docker-container":{"n":"docker-container","t":"Docker container management commands","s":[["TAGLINE","Docker container management commands"],["TLDR","**List running containers**\n\n```docker container ls```\n\n**List all containers including stopped ones**\n\n```docker container ls -a```\n\n**Create and run a container interactively**\n\n```docker container run -it [image] [command]```\n\n**Run a container in the background**\n\n```docker container run -d -p [host_port]:[container_port] --name [name] [image]```\n\n**Start a stopped container**\n\n```docker container start [container]```\n\n**Stop a running container**\n\n```docker container stop [container]```\n\n**Remove a stopped container**\n\n```docker container rm [container]```\n\n**Execute a command in a running container**\n\n```docker container exec -it [container] [command]```\n\n**View container logs**\n\n```docker container logs -f [container]```"],["SYNOPSIS","**docker** **container** _command_ [_options_]"],["SUBCOMMANDS","**ls**\n> List containers.\n\n**run**\n> Create and run a container.\n\n**start**\n> Start stopped containers.\n\n**stop**\n> Stop running containers.\n\n**rm**\n> Remove containers.\n\n**exec**\n> Execute command in container.\n\n**logs**\n> View container logs.\n\n**inspect**\n> Display detailed information.\n\n**cp**\n> Copy files between container and host.\n\n**stats**\n> Display resource usage statistics.\n\n**restart**\n> Restart one or more containers.\n\n**rename**\n> Rename a container.\n\n**prune**\n> Remove all stopped containers.\n\n**top**\n> Display running processes of a container.\n\n**attach**\n> Attach to a running container's STDIN, STDOUT, and STDERR.\n\n**wait**\n> Block until containers stop, then print exit codes."],["DESCRIPTION","**docker container** manages Docker containers. It provides commands for the full container lifecycle: creation, execution, monitoring, and removal.\n\nThis is the modern syntax; most subcommands also work as direct docker commands (e.g., **docker ps** = **docker container ls**)."],["INSTALL","```apt: sudo apt install docker-cli```\n\n```dnf: sudo dnf install docker-cli```\n\n```pacman: sudo pacman -S docker```\n\n```apk: sudo apk add docker-cli```\n\n```zypper: sudo zypper install docker```\n\n```brew: brew install docker```\n\n```nix: nix profile install nixpkgs#docker```"],["SEE ALSO","[docker](/man/docker)(1), [docker-run](/man/docker-run)(1), [docker-image](/man/docker-image)(1)"]],"e":["docker","docker-run","docker-image"],"x":"docker container ls"},"dokku":{"n":"dokku","t":"self-hosted Platform-as-a-Service powered by Docker","s":[["TAGLINE","self-hosted Platform-as-a-Service powered by Docker"],["TLDR","**Create an app**\n\n```dokku apps:create [app_name]```\n\n**Deploy via git push**\n\n```git push dokku main```\n\n**List all apps**\n\n```dokku apps:list```\n\n**View app logs**\n\n```dokku logs [app_name]```\n\n**Set environment variable**\n\n```dokku config:set [app_name] [KEY]=[value]```\n\n**Link database**\n\n```dokku postgres:link [db_name] [app_name]```\n\n**Scale app processes**\n\n```dokku ps:scale [app_name] web=[2]```\n\n**Add domain**\n\n```dokku domains:add [app_name] [example.com]```\n\n**Enable HTTPS** with a Let's Encrypt certificate\n\n```dokku letsencrypt:enable [app_name]```\n\n**Install a plugin** (as root)\n\n```sudo dokku plugin:install [https://github.com/dokku/dokku-postgres.git]```"],["SYNOPSIS","**dokku** _plugin_:_command_ [_app_] [_arguments_]\n\nMost commands follow a `namespace:verb` naming scheme, where the namespace is the plugin that provides them."],["COMMANDS","**apps:create** _app_ / **apps:list** / **apps:destroy** _app_\n> Create, list, and destroy applications.\n\n**apps:rename** _app_ _new-name_\n> Rename an application and rebuild it.\n\n**config:set** _app_ _KEY=VALUE_ / **config:unset** _app_ _KEY_ / **config:show** _app_\n> Manage environment variables. Setting or unsetting restarts the app unless **--no-restart** is passed.\n\n**ps:scale** _app_ _proc_=_n_\n> Scale a process type from the Procfile to _n_ containers.\n\n**ps:restart** _app_ / **ps:rebuild** _app_ / **ps:stop** _app_\n> Restart the running containers, rebuild the app from source, or stop it.\n\n**logs** _app_\n> Show application logs. **-t** follows them, **-n** _N_ limits the number of lines.\n\n**domains:add** _app_ _domain_ / **domains:remove** _app_ _domain_ / **domains:report** _app_\n> Manage the virtual hosts routed to the app.\n\n**letsencrypt:enable** _app_\n> Request and install a Let's Encrypt certificate (requires the `dokku-letsencrypt` plugin).\n\n**proxy:ports-add** _app_ _scheme_:_host-port_:_container-port_\n> Map an external port onto a container port.\n\n**run** _app_ _command_\n> Run a one-off command in a new container from the app's image.\n\n**enter** _app_ [_process_]\n> Open a shell inside a running container.\n\n**postgres:create** _service_ / **postgres:link** _service_ _app_\n> Create a database service and expose its credentials to an app. Equivalents exist for `mysql`, `redis`, `mongo`, and others, each from its own plugin.\n\n**plugin:install** _git-url_ / **plugin:list**\n> Install and list plugins. Must be run as root.\n\n**version**\n> Print the installed Dokku version."],["DESCRIPTION","**Dokku** is a self-hosted Platform-as-a-Service (PaaS) that implements a Heroku-like workflow on a single machine. Adding the server as a git remote and pushing to it is the whole deploy: Dokku detects the application type, builds an image, and cuts traffic over to the new containers.\n\nApplications are built either with **Herokuish** buildpacks, a **Dockerfile**, or Cloud Native Buildpacks, and are then run as Docker containers behind an nginx proxy that Dokku configures for you. A `Procfile` defines the process types, which **ps:scale** turns into container counts.\n\nNearly everything beyond the core is a plugin: `dokku-postgres`, `dokku-mysql`, `dokku-redis`, and friends provide backing services that are linked into an app as environment variables, while `dokku-letsencrypt` automates TLS certificates. This keeps the base install small while covering most of what Heroku offers."],["CAVEATS","Dokku targets a single host; there is no built-in clustering or zero-downtime failover across machines, and scaling means more containers on the same box. It expects a fresh server, since it takes ownership of nginx and Docker configuration. Plugin commands must be installed as **root** even though app commands run as the `dokku` user. Zero-downtime deploys depend on correctly configured `CHECKS`, otherwise a push briefly drops requests, and builds can be memory-hungry enough to fail on the smallest VPS sizes without swap."],["HISTORY","Dokku was created by **Jeff Lindsay** in **2013** as a \"docker-powered mini-Heroku\" written in a few hundred lines of shell. It quickly outgrew that, and maintenance passed to **Jose Diaz-Gonzalez**, who still leads the project. It remains one of the most widely used self-hosted PaaS options, and its git-push workflow is deliberately close to Heroku's so that apps can move between the two."],["INSTALL","```aur: yay -S dokku```"],["SEE ALSO","[docker](/man/docker)(1), [git](/man/git)(1), [heroku](/man/heroku)(1), [caprover](/man/caprover)(1)"],["RESOURCES","```[Source code](https://github.com/dokku/dokku)```\n\n```[Homepage](https://dokku.com)```\n\n```[Documentation](https://dokku.com/docs/)```"]],"e":["docker","git","heroku","caprover"],"x":"dokku apps:create [app_name]"},"dolt":{"n":"dolt","t":"SQL database with Git-like version control","s":[["TAGLINE","SQL database with Git-like version control"],["TLDR","**Initialize a repository**\n\n```dolt init```\n\n**Clone a repository**\n\n```dolt clone [owner/repo]```\n\n**Check status**\n\n```dolt status```\n\n**Add changes**\n\n```dolt add [table_name]```\n\n**Commit changes**\n\n```dolt commit -m \"[message]\"```\n\n**Run SQL query**\n\n```dolt sql -q \"[SELECT * FROM table]\"```\n\n**Start SQL server**\n\n```dolt sql-server```\n\n**Push to remote**\n\n```dolt push origin main```\n\n**Diff two commits** or branches\n\n```dolt diff [main] [feature_branch]```\n\n**Import a CSV** into a table\n\n```dolt table import -c --pk=[id] [table_name] [data.csv]```"],["SYNOPSIS","**dolt** _command_ [_arguments_] [_flags_]"],["COMMANDS","**init** / **clone** _owner_/_repo_\n> Create a new database in the current directory, or clone one from a remote such as DoltHub.\n\n**status** / **diff** / **log** / **blame**\n> Inspect the working set, compare commits or branches, browse history, and see which commit last changed a row.\n\n**add** _table_ / **reset** / **commit** **-m** _msg_\n> Stage, unstage, and commit table changes.\n\n**branch** / **checkout** / **merge** / **tag**\n> Manage branches, switch between them, join histories, and mark releases.\n\n**remote** / **fetch** / **pull** / **push**\n> Manage remotes and exchange commits with them.\n\n**sql** [**-q** _query_]\n> Open the SQL shell or run a single query.\n\n**sql-server**\n> Start a MySQL-compatible server so ordinary clients and ORMs can connect.\n\n**table import** / **table export**\n> Load a CSV, JSON, or Parquet file into a table, or dump one out.\n\n**dump**\n> Export the whole database to SQL, CSV, JSON, or Parquet.\n\n**gc**\n> Reclaim disk space from unreachable data.\n\n**version**\n> Print the installed Dolt version."],["DESCRIPTION","**Dolt** is a SQL database with Git-like version control. It is a real MySQL-compatible relational database, and every one of Git's core operations, `clone`, `branch`, `diff`, `merge`, `commit`, `push`, `pull`, works on the data and the schema inside it.\n\nThe version-control surface is available two ways. The CLI mirrors Git command for command, which is how most people first use it. Everything is also reachable from SQL: `AS OF` queries read a table at any commit or branch, system tables such as `dolt_log`, `dolt_diff_<table>`, and `dolt_status` expose history, and stored procedures like `CALL DOLT_COMMIT()` and `CALL DOLT_MERGE()` let an application version its own data without shelling out.\n\nBecause merges happen cell by cell rather than line by line, two branches that touch different columns of the same row merge cleanly, and genuine conflicts are surfaced as rows in the `dolt_conflicts` tables to be resolved with SQL. **DoltHub** and **DoltLab** provide hosted and self-hosted remotes, in the same relationship GitHub has to Git.\n\nTypical uses are data versioning and auditing, collaborative curation of shared datasets, reproducible pipelines that can be rolled back to any past state, and test fixtures that can be branched per test run."],["CONFIGURATION","**~/.dolt/config_global.json**\n> Global configuration for user name, email, and default remotes.\n\n**.dolt/config.json**\n> Repository-specific configuration for remotes and branch settings."],["CAVEATS","Keeping every historical version is not free: a Dolt database is larger and generally slower than the equivalent MySQL instance, and heavy write workloads need periodic **dolt gc**. MySQL compatibility is high but not complete, so an application that leans on exotic functions or storage-engine behaviour may need adjustment. Data written through `dolt sql` or `sql-server` lands in the working set and is not history until it is committed, which surprises people who expect an ordinary database. Conflicts are not resolved with text markers but through the `dolt_conflicts` system tables."],["HISTORY","Dolt was created by **Liquidata**, now DoltHub, and released in **2019**. Its storage engine descends from **Noms**, an earlier content-addressed, versioned database from Attic Labs, which is what makes cheap branching and structural diffing possible. Early versions had their own query dialect; the pivot to MySQL compatibility, built on the **go-mysql-server** engine that DoltHub adopted and now maintains, is what turned Dolt from an interesting data-sharing tool into a database you can point an existing application at."],["INSTALL","```pacman: sudo pacman -S dolt```\n\n```brew: brew install dolt```\n\n```nix: nix profile install nixpkgs#dolt```"],["SEE ALSO","[git](/man/git)(1), [mysql](/man/mysql)(1), [sqlite3](/man/sqlite3)(1), [dolt-sql](/man/dolt-sql)(1), [dolt-merge](/man/dolt-merge)(1), [dolt-clone](/man/dolt-clone)(1)"],["RESOURCES","```[Source code](https://github.com/dolthub/dolt)```\n\n```[Homepage](https://www.dolthub.com)```\n\n```[Documentation](https://www.dolthub.com/docs/)```"]],"e":["git","mysql","sqlite3","dolt-sql","dolt-merge","dolt-clone"],"x":"dolt init"},"dyff":{"n":"dyff","t":"YAML/JSON diff tool for configuration files","s":[["TAGLINE","YAML/JSON diff tool for configuration files"],["TLDR","**Compare** two YAML/JSON files\n\n```dyff between [file1.yaml] [file2.yaml]```\n\n**Compare** Kubernetes manifests\n\n```dyff between -k [manifest1.yaml] [manifest2.yaml]```\n\n**Show** differences with specific output format\n\n```dyff between -o json [file1] [file2]```\n\n**Compare** directories recursively\n\n```dyff between -r [dir1] [dir2]```"],["SYNOPSIS","**dyff** [_command_] [_options_] [_files_]"],["PARAMETERS","**between** _FILE1_ _FILE2_\n> Compare two files\n\n**-k, --kubernetes**\n> Compare Kubernetes manifests (ignore metadata timestamps)\n\n**-r, --recursive**\n> Compare directories recursively\n\n**-o, --output** _FORMAT_\n> Output format: human, json, yaml, tap (default: human)\n\n**--chroot** _PATH_\n> Chroot both inputs to a specific path\n\n**--ignore-order-changes**\n> Ignore changes in list order\n\n**--ignore-value-changes** _PATH_\n> Ignore value changes at specific path\n\n**--no-table-style**\n> Disable table output styling\n\n**-v, --version**\n> Display version and exit\n\n**-h, --help**\n> Display help and exit"],["DESCRIPTION","**dyff** is a diff tool specifically designed for YAML and JSON configuration files. Unlike traditional line-based diff tools, it understands the structure of YAML/JSON documents and shows meaningful differences at the semantic level.\n\nThe tool is particularly useful for comparing Kubernetes manifests, configuration files, and structured data where the order of keys doesn't matter but their values do. It can ignore certain types of changes (like metadata timestamps) and provides various output formats."],["COMMANDS","**between** _FILE1_ _FILE2_\n> Compare two files and show differences\n\n**json** _FILE_\n> Parse and validate JSON file\n\n**yaml** _FILE_\n> Parse and validate YAML file\n\n**version**\n> Display version information"],["CAVEATS","Complex nested structures may produce verbose output. Large files can be slow to compare. Order-sensitive comparisons may miss reorderings unless specifically enabled. Some special YAML features may not be fully supported."],["HISTORY","**dyff** was created by the Homeport team to address the need for semantic diffing of Kubernetes and configuration files. It provides more meaningful output than traditional diff tools when working with structured configuration data."],["INSTALL","```zypper: sudo zypper install dyff```\n\n```brew: brew install dyff```\n\n```nix: nix profile install nixpkgs#dyff```"],["SEE ALSO","[diff](/man/diff)(1), [kubectl](/man/kubectl)(1), [yq](/man/yq)(1), [dyff](/man/dyff)(1)"]],"e":["diff","kubectl","yq","dyff"],"x":"dyff between [file1.yaml] [file2.yaml]"},"easytag":{"n":"easytag","t":"graphical audio file metadata editor","s":[["TAGLINE","graphical audio file metadata editor"],["TLDR","**Launch EasyTAG**\n\n```easytag```\n\n**Open with specific directory**\n\n```easytag [/path/to/music]```"],["SYNOPSIS","**easytag** [_options_] [_directory_]"],["PARAMETERS","_directory_\n> Music directory to open.\n\n**--help**\n> Display help information.\n\n**--version**\n> Show version."],["CONFIGURATION","**~/.config/easytag/easytag.conf**\n> Application settings including default directory, tag format preferences, and UI options."],["DESCRIPTION","**EasyTAG** is a graphical application for viewing and editing audio file metadata (tags). It supports MP3, MP2, MP4/AAC, FLAC, Ogg Vorbis, Ogg Speex, Ogg Opus, MusePack, Monkey's Audio, and WavPack files.\n\nThe application provides batch tagging, filename to tag conversion, tag to filename renaming, and CDDB lookup for automatic metadata retrieval. It's useful for organizing music libraries and fixing incorrect or missing tags."],["FEATURES","- Edit ID3v1, ID3v2, Vorbis, APE, and other tag formats\n- Batch tagging of multiple files\n- Auto-fill from filename patterns\n- Rename files based on tags\n- CDDB/freedb lookup\n- Cover art embedding"],["CAVEATS","GUI application; no command-line tag editing. Some tag formats may have limited support. Large collections may take time to scan. Character encoding issues possible with older tags."],["HISTORY","EasyTAG was started by **Jérôme Couderc** in **2000** and has been maintained by the GNOME community. It evolved from earlier command-line tag editors to provide a user-friendly graphical interface for music metadata management."],["INSTALL","```apt: sudo apt install easytag```\n\n```dnf: sudo dnf install easytag```\n\n```pacman: sudo pacman -S easytag```\n\n```apk: sudo apk add easytag```\n\n```zypper: sudo zypper install easytag```\n\n```nix: nix profile install nixpkgs#easytag```"],["SEE ALSO","[id3v2](/man/id3v2)(1), [kid3](/man/kid3)(1), [picard](/man/picard)(1)"]],"e":["id3v2","kid3","picard"],"x":"easytag"},"gh-cache":{"n":"gh-cache","t":"manage GitHub Actions workflow caches","s":[["TAGLINE","manage GitHub Actions workflow caches"],["TLDR","**List caches** for the current repository\n\n```gh cache list```\n\n**List caches** sorted by size\n\n```gh cache list --sort size_in_bytes --order desc```\n\n**Filter caches** by key prefix\n\n```gh cache list --key [key_prefix]```\n\n**Delete a cache** by ID or key\n\n```gh cache delete [cache_id_or_key]```\n\n**Delete all caches** for a specific ref\n\n```gh cache delete --all --ref refs/heads/[branch]```\n\n**Delete all caches** in the repository\n\n```gh cache delete --all```"],["SYNOPSIS","**gh** **cache** _subcommand_ [_options_]"],["SUBCOMMANDS","**list**\n> List caches for the current repository.\n\n**delete**\n> Delete one or more caches by ID, key, or all at once."],["PARAMETERS","**-k**, **--key** _prefix_\n> Filter by cache key prefix (list), or match by key (delete).\n\n**-L**, **--limit** _N_\n> Maximum number of caches to fetch (default: 30). (list only)\n\n**-S**, **--sort** _field_\n> Sort by field: created_at, last_accessed_at, size_in_bytes (default: last_accessed_at). (list only)\n\n**-O**, **--order** _direction_\n> Order of results: asc or desc (default: desc). (list only)\n\n**-r**, **--ref** _ref_\n> Filter or delete by ref, e.g. refs/heads/main or refs/pull/123/merge.\n\n**-a**, **--all**\n> Delete all caches. May be combined with --ref to limit scope.\n\n**--succeed-on-no-caches**\n> Return exit code 0 if no caches are found (used with --all).\n\n**--json** _fields_\n> Output JSON with specified fields. (list only)\n\n**-q**, **--jq** _expression_\n> Filter JSON output using a jq expression. (list only)\n\n**-t**, **--template** _format_\n> Format JSON output using a Go template. (list only)\n\n**-R**, **--repo** _owner/repo_\n> Target a specific repository."],["DESCRIPTION","**gh cache** manages GitHub Actions caches for a repository. Caches store dependencies and build artifacts to speed up workflow execution by reusing data between runs.\n\nThe cache system uses key-based storage, where workflows save and restore cached data using unique keys. Cache entries have size limits and are automatically evicted based on usage policies. Managing caches manually helps troubleshoot workflow issues and reclaim storage.\n\n`gh cache list` can also be invoked as `gh cache ls`."],["INSTALL","```apt: sudo apt install gh```\n\n```dnf: sudo dnf install gh```\n\n```pacman: sudo pacman -S github-cli```\n\n```apk: sudo apk add github-cli```\n\n```zypper: sudo zypper install gh```\n\n```brew: brew install gh```\n\n```nix: nix profile install nixpkgs#gh```"],["SEE ALSO","[gh](/man/gh)(1), [gh-run](/man/gh-run)(1), [gh-secret](/man/gh-secret)(1)"]],"e":["gh","gh-run","gh-secret"],"x":"gh cache list"},"haveged":{"n":"haveged","t":"random number generator daemon that feeds the Linux kernel's entropy pool","s":[["TAGLINE","random number generator daemon that feeds the Linux kernel's entropy pool"],["TLDR","**Start** haveged daemon\n\n```sudo haveged```\n\nRun haveged in **foreground**\n\n```sudo haveged -F```\n\nSet **file path** for output\n\n```sudo haveged -f [path/to/file]```\n\nSet **run level** for daemon\n\n```sudo haveged -r [runlevel]```\n\nSet collection **buffer size** in KiB words\n\n```sudo haveged -b [buffersizeinKW]```\n\nSet **cache size** in KiB words\n\n```sudo haveged -d [cachesizeinKW]```\n\nSet **number of bytes** to write to output file\n\n```sudo haveged -n [byteamount]```"],["SYNOPSIS","**haveged** [_options_]"],["PARAMETERS","**-F**, **--Foreground**\n> Run in foreground (don't daemonize)\n\n**-f**, **--file** _PATH_\n> Write random data to file instead of /dev/random\n\n**-r**, **--run** _LEVEL_\n> Set run level (0=run as daemon, 1=display results)\n\n**-b**, **--buffer** _SIZE_\n> Collection buffer size in KiB words\n\n**-d**, **--data** _SIZE_\n> Cache size in KiB words\n\n**-n**, **--number** _BYTES_\n> Number of bytes to write to output\n\n**-w**, **--write** _BYTES_\n> Write _BYTES_ to /dev/random at a time\n\n**-v**, **--verbose** _LEVEL_\n> Verbosity level"],["DESCRIPTION","**haveged** is a random number generator daemon that feeds the Linux kernel's entropy pool (/dev/random). It uses the HAVEGE (HArdware Volatile Entropy Gathering and Expansion) algorithm, which harvests entropy from CPU timing variations.\n\nThe daemon is particularly useful for:\n- Headless servers lacking traditional entropy sources (keyboard, mouse)\n- Virtual machines with limited hardware entropy\n- Systems requiring high-throughput random number generation\n- Preventing /dev/random blocking during cryptographic operations\n\nhaveged monitors the kernel entropy pool and automatically feeds it when levels drop."],["CAVEATS","Some security researchers debate HAVEGE's entropy quality on virtualized or modern CPUs with consistent timing. Consider using hardware RNG (rngd) when available. On modern kernels, /dev/random no longer blocks, reducing the need for haveged."],["HISTORY","haveged was developed by Gary Wuertz based on the HAVEGE algorithm created by André Seznec and Nicolas Sendrier. It became popular for addressing entropy starvation on Linux servers and VMs."],["INSTALL","```apt: sudo apt install haveged```\n\n```dnf: sudo dnf install haveged```\n\n```pacman: sudo pacman -S haveged```\n\n```apk: sudo apk add haveged```\n\n```zypper: sudo zypper install haveged```\n\n```nix: nix profile install nixpkgs#haveged```"],["SEE ALSO","[random](/man/random)(4), [getrandom](/man/getrandom)(2)"]],"e":["random","getrandom"],"x":"sudo haveged"},"ikaros":{"n":"ikaros","t":"driver management tool for Vanilla OS","s":[["TAGLINE","driver management tool for Vanilla OS"],["TLDR","**Interactively install** drivers for a device\n\n```ikaros install [device]```\n\n**Automatically install** recommended drivers\n\n```ikaros auto-install [device]```\n\n**List** all devices\n\n```ikaros list-devices```"],["SYNOPSIS","**ikaros** _command_ [_arguments_]"],["PARAMETERS","**install** _DEVICE_\n> Interactively install drivers for the specified device\n\n**auto-install** _DEVICE_\n> Automatically install recommended drivers for the device\n\n**list-devices**\n> List all detected devices that may need drivers"],["DESCRIPTION","**ikaros** is the driver management tool for Vanilla OS. It detects hardware devices and helps install appropriate drivers, simplifying the often complex process of finding and installing device drivers on Linux.\n\nThe tool can either guide users through interactive driver selection or automatically install recommended drivers based on detected hardware."],["CAVEATS","Specific to Vanilla OS and its package management system. Device support depends on available drivers in the repository. Some proprietary drivers may require additional configuration."],["HISTORY","Ikaros was developed as part of the Vanilla OS project, an immutable Linux distribution that launched in **2022**. The tool provides a user-friendly way to manage drivers in the Vanilla OS environment."],["SEE ALSO","[apx](/man/apx)(1), [ubuntu-drivers](/man/ubuntu-drivers)(1)"]],"e":["apx","ubuntu-drivers"],"x":"ikaros install [device]"},"jabref":{"n":"jabref","t":"open-source bibliography reference manager","s":[["TAGLINE","open-source bibliography reference manager"],["TLDR","**Start JabRef**\n\n```jabref```\n\n**Open bibliography file**\n\n```jabref [library.bib]```\n\n**Import from file**\n\n```jabref -i [refs.ris]```\n\n**Export to a file**\n\n```jabref -i [library.bib] -o [output.html]```\n\n**Fetch entries from an online fetcher**\n\n```jabref --fetch=[arxiv]:[query]```\n\n**Run headless without the GUI**\n\n```jabref --nogui -i [library.bib] -o [output.html]```"],["SYNOPSIS","**jabref** [_options_] [_file_...]"],["PARAMETERS","_file_\n> BibTeX file to open.\n\n**-i**, **--import** _file_[,_format_]\n> Import or load a file with optional format specification.\n\n**-o**, **--output** _file_[,_format_]\n> Export to a file with optional format.\n\n**-m**, **--exportMatches** _[field]searchTerm,outputFile_\n> Save database entries matching a search query.\n\n**-f**, **--fetch** _fetcher_:_query_\n> Query a web fetcher (e.g., arxiv, doi, pubmed) and import results.\n\n**-a**, **--aux** _infile,outfile_ _base-file_\n> Extract a sub-database from a LaTeX .aux file.\n\n**-g**, **--generateCitationKeys**\n> Regenerate all citation keys in the opened library.\n\n**-b**, **--blank**\n> Do not open any files at startup.\n\n**-n**, **--nogui**\n> Suppress the GUI and exit after processing commands.\n\n**-v**, **--version**\n> Display the version number.\n\n**--debug**\n> Enable debug-level logging.\n\n**-h**, **--help**\n> Show help and list supported import/export formats."],["DESCRIPTION","**JabRef** is an open-source bibliography reference manager. It uses BibTeX/BibLaTeX format natively and integrates with LaTeX editors like TeXstudio, Emacs, and VS Code.\n\nJabRef can import from many formats (RIS, EndNote, PubMed), fetch references from online databases, and manage PDF attachments. It supports grouping, searching, and automatic key generation."],["FEATURES","- BibTeX/BibLaTeX native format\n- Web search (DOI, arXiv, PubMed)\n- PDF and file management\n- Duplicate detection\n- Custom entry types\n- Shared databases"],["CAVEATS","Requires Java runtime. Large libraries may be slow. Some web fetchers have rate limits."],["HISTORY","JabRef was started by **Morten O. Alver** in **2003** as an open-source alternative to commercial reference managers. It's maintained by a community of developers and is popular in academic circles."],["INSTALL","```nix: nix profile install nixpkgs#jabref```"],["SEE ALSO","[bibtex](/man/bibtex)(1), [zotero](/man/zotero)(1), [mendeley](/man/mendeley)(1)"]],"e":["bibtex","zotero","mendeley"],"x":"jabref"},"jira-navigate":{"n":"jira-navigate","t":"Open various Jira views in your web browser","s":[["TAGLINE","Open various Jira views in your web browser"],["TLDR","**Navigate to the project page**\n\n```jira navigate --project [PROJECT_KEY]```\n\n**Navigate to the active sprint board**\n\n```jira navigate --board```\n\n**Navigate to the backlog view**\n\n```jira navigate --backlog```"],["SYNOPSIS","**jira** **navigate** [_options_]"],["PARAMETERS","**--project** _key_\n> Navigate to the project summary page for the given project key.\n\n**--board**\n> Open the active sprint board for the current project.\n\n**--backlog**\n> Open the backlog view for the current project."],["DESCRIPTION","**jira navigate** opens various Jira views in your default web browser. Part of **jira-cli** (by Ankit Pokhrel), it provides quick access to boards, backlogs, and project pages without manually constructing URLs. The project defaults to the one configured in your jira-cli settings."],["SEE ALSO","[jira](/man/jira)(1), [jira-open](/man/jira-open)(1), [jira-sprint](/man/jira-sprint)(1)"]],"e":["jira","jira-open","jira-sprint"],"x":"jira navigate --project [PROJECT_KEY]"},"joe":{"n":"joe","t":"joe's Own Editor, a terminal text editor","s":[["TAGLINE","joe's Own Editor, a terminal text editor"],["TLDR","**Open file**\n\n```joe [file.txt]```\n\n**Open at line number**\n\n```joe +[100] [file.txt]```\n\n**Read-only mode**\n\n```joe -rdonly [file.txt]```\n\n**Enable syntax highlighting**\n\n```joe -syntax [c] [file.c]```\n\n**Disable word wrap**\n\n```joe -wordwrap [file.txt]```"],["SYNOPSIS","**joe** [_options_] [_file_...]"],["PARAMETERS","_FILE_\n> Files to edit.\n\n**+**_LINE_\n> Go to line number.\n\n**-rdonly**\n> Read-only mode.\n\n**-syntax** _NAME_\n> Syntax highlighting mode.\n\n**-wordwrap**\n> Enable word wrap.\n\n**-help**\n> Display help information."],["DESCRIPTION","**joe** is Joe's Own Editor, a terminal text editor. It supports multiple editing modes including WordStar and Emacs.\n\nThe editor features syntax highlighting, macros, and split windows. Key bindings are configurable."],["CAVEATS","WordStar bindings by default. Configuration via joerc. Multiple personalities (jmacs, jstar)."],["HISTORY","joe was created by **Joseph H. Allen** in 1991 as a modeless text editor with familiar WordStar key bindings."],["INSTALL","```apt: sudo apt install joe```\n\n```dnf: sudo dnf install joe```\n\n```pacman: sudo pacman -S joe```\n\n```apk: sudo apk add joe```\n\n```zypper: sudo zypper install joe```\n\n```brew: brew install joe```\n\n```nix: nix profile install nixpkgs#joe```"],["SEE ALSO","[nano](/man/nano)(1), [vim](/man/vim)(1), [emacs](/man/emacs)(1)"]],"e":["nano","vim","emacs"],"x":"joe [file.txt]"},"keepassxc-cli":{"n":"keepassxc-cli","t":"command-line interface for KeePassXC password manager","s":[["TAGLINE","command-line interface for KeePassXC password manager"],["TLDR","**List entries in database**\n\n```keepassxc-cli ls [database.kdbx]```\n\n**Show entry details**\n\n```keepassxc-cli show [database.kdbx] [entry]```\n\n**Copy password to clipboard**\n\n```keepassxc-cli clip [database.kdbx] [entry]```\n\n**Add new entry**\n\n```keepassxc-cli add [database.kdbx] [entry]```\n\n**Generate password**\n\n```keepassxc-cli generate -L [20]```\n\n**Create new database**\n\n```keepassxc-cli db-create [database.kdbx]```\n\n**Search entries**\n\n```keepassxc-cli search [database.kdbx] [query]```"],["SYNOPSIS","**keepassxc-cli** _command_ [_options_]"],["PARAMETERS","## General Options\n\n**-k**, **--key-file** _path_\n> Specify path to a key file for unlocking the database.\n\n**--no-password**\n> Deactivate the password key for the database.\n\n**-q**, **--quiet**\n> Silence password prompt and other secondary outputs.\n\n## Commands\n\n**ls** _database_ [_group_]\n> List entries in database or group. Use **-R** for recursive listing.\n\n**show** _database_ _entry_\n> Show entry details. Use **-a** to select specific attributes.\n\n**clip** _database_ _entry_ [_timeout_]\n> Copy password to clipboard. Clears after timeout (default 10s).\n\n**add** _database_ _entry_\n> Add new entry. Use **-g** to generate a password, **-u** to set username.\n\n**edit** _database_ _entry_\n> Edit existing entry.\n\n**rm** _database_ _entry_\n> Remove entry.\n\n**generate**\n> Generate random password. Use **-L** to set length (default 16).\n\n**db-create** _database_\n> Create new database.\n\n**search** _database_ _term_\n> Search entries.\n\n**analyze** _database_\n> Analyze password strength.\n\n**merge** _database1_ _database2_\n> Merge two databases. The first database is overwritten with the result.\n\n**import** _database_ _xml_\n> Import contents of an XML export into a new database.\n\n**export** _database_\n> Export database content to stdout (xml or csv format).\n\n**open** _database_\n> Open database in interactive shell mode.\n\n**attachment-export** _database_ _entry_ _name_ _export-file_\n> Export an attachment to a file.\n\n**attachment-import** _database_ _entry_ _name_ _import-file_\n> Import a file as an attachment into an entry."],["DESCRIPTION","**keepassxc-cli** is the command-line interface for KeePassXC password manager. It allows querying and modifying KeePass database entries (.kdbx format) from the terminal. Supports password generation, clipboard operations, and database management."],["CAVEATS","Avoid using --password on shared systems. Close the GUI before making CLI edits to prevent database corruption."],["INSTALL","```apt: sudo apt install keepassxc-full```\n\n```dnf: sudo dnf install keepassxc```\n\n```pacman: sudo pacman -S keepassxc```\n\n```apk: sudo apk add keepassxc```\n\n```zypper: sudo zypper install keepassxc```\n\n```nix: nix profile install nixpkgs#keepassxc```"],["SEE ALSO","[pass](/man/pass)(1)"]],"e":["pass"],"x":"keepassxc-cli ls [database.kdbx]"},"koji-buildinfo":{"n":"koji-buildinfo","t":"displays information about builds in the Koji build system","s":[["TAGLINE","displays information about builds in the Koji build system"],["TLDR","Print **basic information** about builds\n\n```koji buildinfo [BuildID_or_NVR]```\n\nPrint information with **changelog**\n\n```koji buildinfo [BuildID_or_NVR] --changelog```\n\nDisplay **help**\n\n```koji buildinfo -h```"],["SYNOPSIS","**koji buildinfo** [_options_] _build_..."],["PARAMETERS","_BuildID_or_NVR_\n> Build ID number or Name-Version-Release string\n\n**--changelog**\n> Include changelog information in output\n\n**-h**, **--help**\n> Display help message"],["DESCRIPTION","**koji buildinfo** displays information about builds in the Koji build system. It shows details like build state, owner, tags, source, and timestamps.\n\nBuilds can be specified by their numeric ID or by NVR (Name-Version-Release) format like \"python-3.9.1-1.fc34\"."],["CAVEATS","Requires network access to the Koji hub. Some builds may have restricted visibility based on access controls."],["HISTORY","koji buildinfo is part of the Koji build system used by Fedora, CentOS, and other RPM-based distributions for package building and management."],["INSTALL","```dnf: sudo dnf install koji```\n\n```brew: brew install koji```\n\n```nix: nix profile install nixpkgs#koji```"],["SEE ALSO","[koji](/man/koji)(1), [koji-download-build](/man/koji-download-build)(1), [koji-tag-build](/man/koji-tag-build)(1)"]],"e":["koji","koji-download-build","koji-tag-build"],"x":"koji buildinfo [BuildID_or_NVR]"},"leon":{"n":"leon","t":"Open-source personal AI assistant that runs on your own server","s":[["TAGLINE","Open-source personal AI assistant that runs on your own server"],["TLDR","**Install the Leon CLI globally**\n\n```npm install --global @leon-ai/cli```\n\n**Create a new Leon instance**\n\n```leon create birth```\n\n**Create an instance from the develop branch**\n\n```leon create birth --develop```\n\n**Start Leon**\n\n```leon start```\n\n**Check that the setup is correct**\n\n```leon check```\n\n**View instance information**\n\n```leon info```"],["SYNOPSIS","**leon** _command_ [_options_]"],["PARAMETERS","**create birth**\n> Initialize and set up a new Leon instance with all required dependencies.\n\n**start**\n> Start the Leon server and enable interaction.\n\n**check**\n> Verify that the setup completed successfully.\n\n**info**\n> View details about a Leon instance.\n\n**delete**\n> Remove a Leon instance.\n\n**update**\n> Update Leon to the latest version.\n\n**run**\n> Execute npm scripts within a Leon instance.\n\n**help**\n> Display available commands.\n\n**--help**\n> Display help information.\n\n**--version**\n> Display version information."],["DESCRIPTION","**Leon** is an open-source personal AI assistant that runs on your own server. It provides a skills-based architecture where each capability is a modular skill that can be installed, configured, and extended independently.\n\nLeon supports voice interaction and text-based input through its web interface. It runs on Node.js with a Python runtime for skill execution, making it cross-platform across Linux, macOS, and Windows.\n\nThe assistant is designed for self-hosting, keeping all data and processing on your own infrastructure. Skills cover categories such as productivity, utilities, and information retrieval. Developers can create custom skills to extend Leon's capabilities. The CLI companion helps with setup and management of Leon instances."],["CAVEATS","Requires Node.js and Python installed on the host system. Voice interaction depends on a microphone and supported speech recognition setup. Some skills may require additional API keys for external service integrations. The initial setup downloads several dependencies and may take time on slower connections."],["HISTORY","Leon was created by **Louis Grenard** and first released in **2019**. The project is hosted at **getleon.ai** and has accumulated over 15,000 GitHub stars. It was designed as a privacy-focused alternative to cloud-based assistants like Alexa and Google Assistant, emphasizing self-hosting and open-source extensibility. Development continues with an active community contributing skills and improvements."],["INSTALL","```apt: sudo apt install gatb-core```"],["SEE ALSO","[npm](/man/npm)(1), [node](/man/node)(1)"]],"e":["npm","node"],"x":"npm install --global @leon-ai/cli"},"llvm-config":{"n":"llvm-config","t":"provides configuration information for building against LLVM","s":[["TAGLINE","provides configuration information for building against LLVM"],["TLDR","**Show LLVM version**\n\n```llvm-config --version```\n\n**Show compiler flags**\n\n```llvm-config --cxxflags```\n\n**Show linker flags**\n\n```llvm-config --ldflags```\n\n**Show library names**\n\n```llvm-config --libs```\n\n**Show flags for specific components**\n\n```llvm-config --cxxflags --ldflags --libs core```\n\n**Show installation prefix**\n\n```llvm-config --prefix```"],["SYNOPSIS","**llvm-config** [_options_] [_components_...]"],["PARAMETERS","**--version**\n> Print LLVM version.\n\n**--prefix**\n> Print installation prefix.\n\n**--cxxflags**\n> Print C++ compiler flags.\n\n**--ldflags**\n> Print linker flags.\n\n**--libs**\n> Print library names.\n\n**--system-libs**\n> Print system library names.\n\n**--components**\n> Print available components.\n\n**--targets-built**\n> Print available targets."],["DESCRIPTION","**llvm-config** provides configuration information for building against LLVM. Outputs compiler flags, linker flags, and library names needed to compile and link programs using LLVM libraries."],["INSTALL","```apt: sudo apt install llvm```\n\n```dnf: sudo dnf install llvm```\n\n```pacman: sudo pacman -S llvm```\n\n```apk: sudo apk add llvm-dev```\n\n```zypper: sudo zypper install llvm```\n\n```brew: brew install llvm```\n\n```nix: nix profile install nixpkgs#llvm```"],["SEE ALSO","[clang](/man/clang)(1), [llc](/man/llc)(1)"]],"e":["clang","llc"],"x":"llvm-config --version"},"llvm-dis":{"n":"llvm-dis","t":"LLVM bitcode disassembler","s":[["TAGLINE","LLVM bitcode disassembler"],["TLDR","**Disassemble bitcode to LLVM IR**\n\n```llvm-dis [file.bc]```\n\n**Output to specific file**\n\n```llvm-dis [file.bc] -o [output.ll]```\n\n**Disassemble from stdin to stdout**\n\n```llvm-dis -o - < [file.bc]```\n\n**Disassemble and show timing for each pass**\n\n```llvm-dis --time-passes [file.bc]```\n\n**Show version**\n\n```llvm-dis --version```"],["SYNOPSIS","**llvm-dis** [_options_] [_file_]"],["PARAMETERS","**-o** _file_\n> Output file name. If omitted, input filename with .ll extension is used.\n\n**-f**, **--force**\n> Enable binary output on terminals and overwrite existing output files.\n\n**--show-annotations**\n> Show annotations in output.\n\n**--disable-output**\n> Discard output (useful with --time-passes for benchmarking).\n\n**--time-passes**\n> Time each pass during disassembly.\n\n**--help**\n> Display available options.\n\n**--version**\n> Display LLVM version."],["DESCRIPTION","**llvm-dis** is the LLVM disassembler. It converts LLVM bitcode (.bc) files to human-readable LLVM assembly language (.ll) format. If no filename is given or the filename is -, it reads from stdin. Useful for inspecting compiled code and debugging LLVM optimization passes."],["INSTALL","```apt: sudo apt install llvm```\n\n```dnf: sudo dnf install llvm```\n\n```pacman: sudo pacman -S llvm```\n\n```apk: sudo apk add llvm```\n\n```zypper: sudo zypper install llvm```\n\n```brew: brew install llvm```\n\n```nix: nix profile install nixpkgs#llvm```"],["SEE ALSO","[llvm-as](/man/llvm-as)(1), [llc](/man/llc)(1), [opt](/man/opt)(1), [llvm-bcanalyzer](/man/llvm-bcanalyzer)(1), [llvm-objdump](/man/llvm-objdump)(1), [clang](/man/clang)(1)"]],"e":["llvm-as","llc","opt","llvm-bcanalyzer","llvm-objdump","clang"],"x":"llvm-dis [file.bc]"},"lxc-checkconfig":{"n":"lxc-checkconfig","t":"checks whether the current kernel has the required features enabled for LXC","s":[["TAGLINE","checks whether the current kernel has the required features enabled for LXC"],["TLDR","Check **current kernel** support\n\n```lxc-checkconfig```\n\nCheck **specific kernel** config\n\n```CONFIG=/path/to/config lxc-checkconfig```"],["SYNOPSIS","**lxc-checkconfig**"],["DESCRIPTION","**lxc-checkconfig** checks whether the current kernel has the required features enabled for LXC container support. It examines namespaces, cgroups, and other kernel capabilities."],["CHECKS PERFORMED","**Namespaces**: User, PID, UTS, IPC, Network\n**Cgroups**: v1/v2, controllers\n**Misc**: Seccomp, checkpoint/restore"],["CAVEATS","Set CONFIG environment variable to check a different kernel configuration file. Helps diagnose why containers might not work on a system."],["INSTALL","```apt: sudo apt install lxc```\n\n```dnf: sudo dnf install lxc```\n\n```pacman: sudo pacman -S lxc```\n\n```apk: sudo apk add lxc```\n\n```zypper: sudo zypper install lxc```\n\n```brew: brew install lxc```\n\n```nix: nix profile install nixpkgs#lxc```"],["SEE ALSO","[lxc-create](/man/lxc-create)(1), [lxc-start](/man/lxc-start)(1)"]],"e":["lxc-create","lxc-start"],"x":"lxc-checkconfig"},"lxc-info":{"n":"lxc-info","t":"displays information about a Linux container including its state, PID, IP","s":[["TAGLINE","displays information about a Linux container including its state, PID, IP"],["TLDR","Get container **info**\n\n```sudo lxc-info [container_name]```\n\nDisplay **help**\n\n```lxc-info --help```"],["SYNOPSIS","**lxc-info** **-n** _name_ [_options_]"],["DESCRIPTION","**lxc-info** displays information about a Linux container including its state, PID, IP addresses, CPU and memory usage."],["PARAMETERS","**-n, --name NAME**\n> Container name\n\n**-s, --state**\n> Show state only\n\n**-p, --pid**\n> Show PID only\n\n**-i, --ips**\n> Show IP addresses only\n\n**-c, --config KEY**\n> Show specific config value\n\n**-S, --stats**\n> Show usage statistics\n\n**-?, --help**\n> Display help information"],["OUTPUT","**Name**: Container name\n**State**: RUNNING, STOPPED, FROZEN\n**PID**: Container init PID\n**IP**: IP addresses\n**CPU use**: CPU usage\n**Memory use**: Memory usage"],["INSTALL","```apt: sudo apt install lxc```\n\n```dnf: sudo dnf install lxc```\n\n```pacman: sudo pacman -S lxc```\n\n```apk: sudo apk add lxc```\n\n```zypper: sudo zypper install lxc```\n\n```brew: brew install lxc```\n\n```nix: nix profile install nixpkgs#lxc```"],["SEE ALSO","[lxc-ls](/man/lxc-ls)(1), [lxc-start](/man/lxc-start)(1), [lxc-attach](/man/lxc-attach)(1)"]],"e":["lxc-ls","lxc-start","lxc-attach"],"x":"sudo lxc-info [container_name]"},"lxc-network":{"n":"lxc-network","t":"manages networks for LXD containers","s":[["TAGLINE","manages networks for LXD containers"],["TLDR","List **all networks**\n\n```lxc network list```\n\nShow network **configuration**\n\n```lxc network show [network_name]```\n\n**Attach** container to network\n\n```lxc network attach [network_name] [container_name]```\n\n**Create** new network\n\n```lxc network create [network_name]```\n\nSet **bridge interface**\n\n```lxc network set [network_name] bridge.external_interfaces eth0```\n\n**Disable NAT**\n\n```lxc network set [network_name] ipv4.nat false```"],["SYNOPSIS","**lxc network** _command_ [_options_]"],["DESCRIPTION","**lxc network** manages networks for LXD containers. It can create, configure, and attach networks to containers, supporting bridges, VLANs, and various network configurations."],["SUBCOMMANDS","**list**\n> List available networks\n\n**show NAME**\n> Show network configuration\n\n**create NAME**\n> Create new managed network\n\n**delete NAME**\n> Delete network\n\n**attach NAME CONTAINER**\n> Attach container to network\n\n**detach NAME CONTAINER**\n> Detach container from network\n\n**set NAME KEY VALUE**\n> Set network configuration"],["SEE ALSO","[lxc](/man/lxc)(1), [lxc-start](/man/lxc-start)(1)"]],"e":["lxc","lxc-start"],"x":"lxc network list"},"lzdiff":{"n":"lzdiff","t":"compares two lzip-compressed files and shows differences","s":[["TAGLINE","compares two lzip-compressed files and shows differences"],["TLDR","**Compare two lzip-compressed files**\n\n```lzdiff [file1.lz] [file2.lz]```\n\n**Show unified diff**\n\n```lzdiff -u [file1.lz] [file2.lz]```\n\n**Show context diff**\n\n```lzdiff -c [file1.lz] [file2.lz]```"],["SYNOPSIS","**lzdiff** [_options_] _file1_ _file2_"],["PARAMETERS","**-u**\n> Unified diff format.\n\n**-c**\n> Context diff format.\n\nAll diff options are supported."],["DESCRIPTION","**lzdiff** compares two lzip-compressed files and shows differences. Automatically decompresses before comparing. Equivalent to running diff on decompressed contents. Passes options through to diff."],["INSTALL","```dnf: sudo dnf install xz```\n\n```pacman: sudo pacman -S xz```\n\n```apk: sudo apk add xz```\n\n```zypper: sudo zypper install xz```\n\n```brew: brew install xz```\n\n```nix: nix profile install nixpkgs#xz```"],["SEE ALSO","[lzip](/man/lzip)(1), [lzcmp](/man/lzcmp)(1), [diff](/man/diff)(1)"]],"e":["lzip","lzcmp","diff"],"x":"lzdiff [file1.lz] [file2.lz]"},"mach":{"n":"mach","t":"Compiler and build tool for the Mach systems language","s":[["TAGLINE","Compiler and build tool for the Mach systems language"],["TLDR","**Scaffold a new** project\n\n```mach init [project_name]```\n\n**Build** the project\n\n```mach build```\n\n**Build and run** the produced binary\n\n```mach run```\n\n**Build and run** the project's tests\n\n```mach test```\n\n**Build an optimized** release binary\n\n```mach build --release```\n\n**Manage git-backed** dependencies\n\n```mach dep [subcommand]```"],["SYNOPSIS","**mach** _command_ [_options_]"],["DESCRIPTION","**mach** is the command-line driver for the Mach programming language, a statically typed, compiled systems language with manual memory management and no hidden control flow. A single self-contained binary handles the whole workflow: it compiles and links projects, runs tests, vendors dependencies, generates documentation, and cross-compiles to other targets.\n\nThe compiler is self-hosted (written in Mach) and does not depend on LLVM. Build-related subcommands share a common configuration parser, so global flags such as **--target**, **--profile**, and **--release** apply consistently across building, running, testing, and documenting."],["COMMANDS","**build**\n> Compile the project to object files and, for a binary target, a linked executable.\n\n**run**\n> Build the project, then execute the produced binary.\n\n**test**\n> Build the test binary and run the collected tests.\n\n**dep**\n> Manage git-backed dependencies (clone, lock, vendor).\n\n**init**\n> Scaffold a new project.\n\n**doc**\n> Generate Markdown reference documentation from source doc-comments.\n\n**info**\n> Print the compiler version, build host, and registered target capabilities.\n\n**help**\n> Display usage information for a command."],["PARAMETERS","**--release**, **-O0**, **-O1**, **-O2**\n> Select the optimization level for the build.\n\n**--target** _TRIPLE_\n> Cross-compile for the given target.\n\n**--profile** _NAME_\n> Select a named build profile.\n\n**--bin** _NAME_, **--lib**\n> Choose the binary or library artifact to act on.\n\n**-o** _PATH_\n> Write output to _PATH_.\n\n**--emit-asm**, **--emit-ir**\n> Emit assembly or intermediate representation instead of (or alongside) the normal output.\n\n**-L** _PATH_, **-l** _NAME_\n> Add a library search path or link against a named library.\n\n**--verbose**, **--quiet**\n> Increase or decrease the amount of output."],["CAVEATS","Mach is a young language under active development; the standard library is intentionally minimal (\"batteries not included\") and the syntax and tooling may change between releases. Memory is managed manually with explicit allocator passing."],["HISTORY","**mach** ships as part of the Mach language project, which provides a self-hosted compiler with no LLVM dependency. It is installed as a single binary, typically via the install script at machlang.org."],["SEE ALSO","[zig](/man/zig)(1), [rustc](/man/rustc)(1), [cargo](/man/cargo)(1), [clang](/man/clang)(1), [tcc](/man/tcc)(1)"],["RESOURCES","```[Source code](https://github.com/octalide/mach)```\n\n```[Homepage](https://machlang.org)```"]],"e":["zig","rustc","cargo","clang","tcc"],"x":"mach init [project_name]"},"maigret":{"n":"maigret","t":"OSINT tool for collecting information about usernames across social","s":[["TAGLINE","OSINT tool for collecting information about usernames across social"],["TLDR","**Search username across sites**\n\n```maigret [username]```\n\n**Search with specific sites**\n\n```maigret [username] --site [twitter] --site [github]```\n\n**Output to file**\n\n```maigret [username] -o [report.txt]```\n\n**Generate HTML report**\n\n```maigret [username] --html```"],["SYNOPSIS","**maigret** [_options_] _username_"],["PARAMETERS","**-o** _file_\n> Output results to file.\n\n**--html**\n> Generate HTML report.\n\n**--pdf**\n> Generate PDF report.\n\n**--site** _name_\n> Check specific site only.\n\n**--timeout** _seconds_\n> Request timeout.\n\n**--tor**\n> Use Tor network.\n\n**--proxy** _url_\n> Use HTTP proxy.\n\n**-a**, **--all-sites**\n> Check all available sites."],["DESCRIPTION","**maigret** is an OSINT tool for collecting information about usernames across social networks and websites. Searches hundreds of platforms to find accounts associated with a username. Generates detailed reports for investigations."],["CAVEATS","For authorized security research and OSINT investigations only. Respect privacy and applicable laws."],["INSTALL","```brew: brew install maigret```\n\n```nix: nix profile install nixpkgs#maigret```"],["SEE ALSO","[sherlock](/man/sherlock)(1)"]],"e":["sherlock"],"x":"maigret [username]"},"mbw":{"n":"mbw","t":"Memory bandwidth benchmark tool","s":[["TAGLINE","Memory bandwidth benchmark tool"],["TLDR","Run **3 iterations** of all tests with 512 MiB arrays\n\n```mbw -n 3 512```\n\nRun tests with 512 MiB, **quiet mode** suppressing averages\n\n```mbw -n 3 -q -a 512```\n\nRun **memcpy test** 3 times with 512 MiB, only display statistics\n\n```mbw -n 3 -q -t0 512```\n\nRun **mcblock test** 10 times with **1024 byte blocks** using 8192 MiB arrays\n\n```mbw -n 10 -q -t2 -b 1024 8192```\n\nRun dumb test with 2048 MiB, **run forever**\n\n```mbw -n 0 -t1 -q 2048```"],["SYNOPSIS","**mbw** [_options_] _size_in_MiB_"],["PARAMETERS","**-n _count_**\n> Number of test iterations (0 for infinite)\n\n**-q**\n> Quiet mode; show only statistics, suppress per-iteration output\n\n**-a**\n> Suppress printing averages\n\n**-t _type_**\n> Test type: 0=memcpy, 1=dumb (byte-by-byte), 2=mcblock (block copy)\n\n**-b _bytes_**\n> Block size in bytes for mcblock test (-t2)\n\n**-h**\n> Show quick help"],["DESCRIPTION","**mbw** (Memory Bandwidth) is a benchmark tool that measures memory copy performance by allocating two memory regions and timing how fast data can be copied between them.\n\nThree test methods are available: **memcpy** uses the C library's optimized memcpy function, **dumb** copies byte-by-byte to measure raw memory access speed, and **mcblock** copies in user-specified block sizes to analyze cache behavior.\n\nResults are reported in MB/s, helping identify memory subsystem performance, compare different systems, or detect issues with memory configuration."],["CAVEATS","Results can vary significantly based on system load, CPU frequency scaling, and memory access patterns. For consistent benchmarks, ensure the system is idle and consider disabling CPU frequency scaling. The dumb test is intentionally slow and useful for measuring worst-case memory latency."],["HISTORY","mbw was written by Andras Horvath as a simple tool to test memory bandwidth, particularly useful for comparing RAM performance across different systems or configurations."],["INSTALL","```apt: sudo apt install mbw```\n\n```brew: brew install mbw```"],["SEE ALSO","[memtester](/man/memtester)(1), [stress-ng](/man/stress-ng)(1), [sysbench](/man/sysbench)(1)"]],"e":["memtester","stress-ng","sysbench"],"x":"mbw -n 3 512"},"mfterm":{"n":"mfterm","t":"interactive MIFARE terminal","s":[["TAGLINE","interactive MIFARE terminal"],["TLDR","**Start interactive terminal**\n\n```mfterm```\n\n**Load dump file**\n\n```mfterm -l [dump.mfd]```\n\n**Load keys**\n\n```mfterm -k [keys.txt]```\n\n**Read card**\n\n```mfterm -r```\n\n**Write to card**\n\n```mfterm -w [dump.mfd]```"],["SYNOPSIS","**mfterm** [_options_]"],["PARAMETERS","**-l** _FILE_\n> Load dump file.\n\n**-k** _FILE_\n> Load key file.\n\n**-r**\n> Read card.\n\n**-w** _FILE_\n> Write dump to card.\n\n**--help**\n> Display help information."],["DESCRIPTION","**mfterm** is an interactive MIFARE terminal. It reads, writes, and manipulates MIFARE Classic cards.\n\nThe tool provides commands for sector operations, key management, and data analysis."],["CAVEATS","Security research tool. Requires NFC reader. Legal use only."],["HISTORY","mfterm provides interactive **MIFARE Classic** card manipulation for security research purposes."],["INSTALL","```brew: brew install mfterm```"],["SEE ALSO","[mfoc](/man/mfoc)(1)"]],"e":["mfoc"],"x":"mfterm"},"mindwalk":{"n":"mindwalk","t":"replay coding-agent sessions on a 3D map of your codebase","s":[["TAGLINE","replay coding-agent sessions on a 3D map of your codebase"],["TLDR","**Install** and launch (scans Claude Code and Codex session dirs)\n\n```mindwalk```\n\n**Serve** the UI on a fixed port without opening a browser\n\n```mindwalk serve --port [8765] --no-open```\n\n**Open** one session file\n\n```mindwalk open [path/to/session.jsonl]```\n\n**Build** a repository citymap JSON\n\n```mindwalk build [path/to/repo] -o [out.json]```\n\n**Normalize** a session into a trace\n\n```mindwalk trace [session] -o [out.json]```\n\n**Evaluate** a session with a local agent CLI as judge\n\n```mindwalk analyze [session] --judge [claude|codex]```"],["SYNOPSIS","**mindwalk** [_command_] [_options_] [_args_]"],["DESCRIPTION","**mindwalk** visualizes how a coding agent explored a repository. It reads local Claude Code and Codex session logs, builds a deterministic layout of the repo (a \"citymap\"), and plays file reads, edits, and searches back as light on a 3D map in the browser.\n\nWith no arguments it scans default session directories, serves a local UI on a random port, and opens a browser. Viewing is fully local; optional **mindwalk analyze** (or the Evaluate panel) can send a session summary to your own **claude** or **codex** CLI for judging."],["PARAMETERS","**serve**\n> Start the local server (**--port**, **--no-open**, **--claude-dir**, **--codex-dir**)\n\n**open** _session.jsonl_\n> Open a specific session (**--no-open** to skip the browser)\n\n**build** _repo_\n> Write citymap JSON (**-o** output path)\n\n**trace** _session_\n> Write normalized trace JSON\n\n**analyze** _session_\n> LLM judge evaluation (**--judge**, **--model**)"],["CAVEATS","Session evaluation is opt-in and uses your local agent CLI, which may send a summary of that session to the model behind your account. Reports cache under **~/.mindwalk/reports**."],["SEE ALSO","[claude](/man/claude)(1)"],["RESOURCES","```[Source code](https://github.com/cosmtrek/mindwalk)```"]],"e":["claude"],"x":"mindwalk"},"mitmweb":{"n":"mitmweb","t":"Man-in-the-middle proxy with a web interface","s":[["TAGLINE","Man-in-the-middle proxy with a web interface"],["TLDR","**Start mitmweb with default settings**\n\n```mitmweb```\n\n**Specify proxy and web interface ports**\n\n```mitmweb --listen-port [8080] --web-port [8081]```\n\n**Save intercepted flows to a file**\n\n```mitmweb -w [flows.mitm]```\n\n**Load previously saved flows**\n\n```mitmweb -r [flows.mitm]```\n\n**Run in upstream proxy mode**\n\n```mitmweb --mode upstream:[http://proxy:8080]```\n\n**Run in transparent proxy mode**\n\n```mitmweb --mode transparent```\n\n**Run in reverse proxy mode**\n\n```mitmweb --mode reverse:[https://target.example.com]```\n\n**Disable auto-opening the browser**\n\n```mitmweb --no-web-open-browser```"],["SYNOPSIS","**mitmweb** [_options_]"],["PARAMETERS","**--web-port** _PORT_\n> Port for the web interface (default: 8081).\n\n**--web-open-browser**\n> Automatically open a browser on startup (default: true).\n\n**--no-web-open-browser**\n> Do not automatically open a browser.\n\n**--listen-port** _PORT_\n> Proxy listen port (default: 8080).\n\n**-w** _FILE_\n> Write flows to file.\n\n**-r** _FILE_\n> Read flows from file.\n\n**--mode** _MODE_\n> Proxy mode: regular, transparent, socks5, reverse:SPEC, upstream:SPEC, wireguard.\n\n**--set** _OPTION=VALUE_\n> Set an arbitrary option (e.g., --set ignore_hosts=example.com).\n\n**-s** _SCRIPT_\n> Run a Python addon script.\n\n**--ssl-insecure**\n> Do not verify upstream server SSL/TLS certificates.\n\n**--help**\n> Display help information."],["DESCRIPTION","**mitmweb** is the web-based interface for mitmproxy. It intercepts, inspects, modifies, and replays HTTP/HTTPS traffic through a browser-based UI. The web interface provides flow listing, detail views, and interactive request/response editing.\n\nBy default, mitmweb starts a proxy on port 8080 and a web interface on port 8081. It supports the same proxy modes as mitmproxy: regular, transparent, SOCKS5, reverse proxy, upstream proxy, and WireGuard.\n\nClients must be configured to use the proxy, and for HTTPS interception, the mitmproxy CA certificate must be installed on the client. Visit http://mitm.it through the proxy to download certificates."],["CAVEATS","HTTPS interception requires installing the mitmproxy CA certificate on the client device. Transparent mode requires OS-level traffic redirection (e.g., iptables). The web interface should not be exposed to untrusted networks without authentication."],["HISTORY","**mitmweb** is part of the **mitmproxy** project, created by Aldo Cortesi. The mitmproxy project started in **2010** and mitmweb was added to provide a browser-based alternative to the console-based mitmproxy interface."],["INSTALL","```pacman: sudo pacman -S mitmproxy```\n\n```apk: sudo apk add mitmproxy```\n\n```nix: nix profile install nixpkgs#mitmproxy```"],["SEE ALSO","[mitmproxy](/man/mitmproxy)(1), [mitmdump](/man/mitmdump)(1), [wireshark](/man/wireshark)(1), [tshark](/man/tshark)(1)"]],"e":["mitmproxy","mitmdump","wireshark","tshark"],"x":"mitmweb"},"nano":{"n":"nano","t":"simple text editor","s":[["TAGLINE","simple text editor"],["TLDR","**Open file**\n\n```nano [file.txt]```\n\n**Open at specific line**\n\n```nano +[10] [file.txt]```\n\n**Open read-only**\n\n```nano -v [file.txt]```\n\n**Enable line numbers**\n\n```nano -l [file.txt]```\n\n**Disable line wrapping**\n\n```nano -w [file.txt]```\n\n**Open with backup**\n\n```nano -B [file.txt]```\n\n**Set tab size**\n\n```nano -T [4] [file.txt]```"],["SYNOPSIS","**nano** [_options_] [_file_]"],["PARAMETERS","_FILE_\n> File to edit.\n\n**+** _LINE_\n> Start at line number.\n\n**-v**\n> View mode (read-only).\n\n**-l**\n> Show line numbers.\n\n**-w**\n> Don't wrap long lines.\n\n**-B**\n> Create backup files.\n\n**-T** _NUM_\n> Tab size.\n\n**--help**\n> Display help information."],["DESCRIPTION","**nano** is a simple text editor. It provides an easy-to-use terminal editor.\n\nThe tool shows shortcuts at bottom. Beginner-friendly alternative to vi/vim."],["CAVEATS","Less powerful than vim/emacs. Ctrl-based shortcuts. Config in ~/.nanorc."],["HISTORY","nano was created as a **free replacement** for the Pico editor, part of the GNU project since 2001."],["INSTALL","```apt: sudo apt install nano```\n\n```dnf: sudo dnf install nano```\n\n```pacman: sudo pacman -S nano```\n\n```apk: sudo apk add nano```\n\n```zypper: sudo zypper install nano```\n\n```brew: brew install nano```\n\n```nix: nix profile install nixpkgs#nano```"],["SEE ALSO","[vim](/man/vim)(1), [emacs](/man/emacs)(1), [pico](/man/pico)(1)"]],"e":["vim","emacs","pico"],"x":"nano [file.txt]"},"newman":{"n":"newman","t":"Run Postman collections from the command line","s":[["TAGLINE","Run Postman collections from the command line"],["TLDR","**Run Postman collection**\n\n```newman run [collection.json]```\n\n**Run with environment**\n\n```newman run [collection.json] -e [environment.json]```\n\n**Run specific folder**\n\n```newman run [collection.json] --folder \"[folder name]\"```\n\n**Export results**\n\n```newman run [collection.json] -r [html] --reporter-html-export [report.html]```\n\n**Run with iterations**\n\n```newman run [collection.json] -n [10]```\n\n**Run with data file**\n\n```newman run [collection.json] -d [data.csv]```\n\n**Set timeout**\n\n```newman run [collection.json] --timeout-request [5000]```"],["SYNOPSIS","**newman** **run** _collection_ [_options_]"],["PARAMETERS","_COLLECTION_\n> Postman collection file path or URL.\n\n**-e** _FILE_\n> Specify an environment file.\n\n**-g** _FILE_\n> Specify a globals file.\n\n**--folder** _NAME_\n> Run only a specific folder from the collection.\n\n**-r** _REPORTERS_\n> Comma-separated reporter types (cli, json, junit, html).\n\n**-n** _COUNT_\n> Number of iterations to run.\n\n**-d** _FILE_\n> Data file (CSV or JSON) for iteration variables.\n\n**--global-var** _KEY=VALUE_\n> Set a global variable via the command line (repeatable).\n\n**--env-var** _KEY=VALUE_\n> Set an environment variable via the command line (repeatable).\n\n**--bail**\n> Stop the run on the first test failure.\n\n**--timeout** _MS_\n> Time in milliseconds to wait for the entire collection run to complete.\n\n**--timeout-request** _MS_\n> Time in milliseconds to wait for each request.\n\n**-x**, **--suppress-exit-code**\n> Override the default exit code for the run.\n\n**--color** _VALUE_\n> Control color output: on, off, or auto (default).\n\n**--help**\n> Display help information."],["DESCRIPTION","**newman** is the command-line companion for Postman that runs exported collections, enabling automated API testing. It executes all requests in a collection sequentially, running pre-request scripts and tests, and reports results.\n\nNewman integrates with CI/CD pipelines to automate API testing. It supports data-driven testing through CSV/JSON files, multiple reporters for output formatting, and environment/global variable management."],["CAVEATS","Requires Node.js. Collections must be exported from Postman in JSON format. The HTML reporter requires an additional npm package (newman-reporter-html). Collection v1 format is deprecated; use v2 or later."],["HISTORY","Newman was created by **Postman** to enable command-line execution of API collections for testing automation."],["INSTALL","```brew: brew install newman```\n\n```nix: nix profile install nixpkgs#newman```"],["SEE ALSO","[curl](/man/curl)(1), [httpie](/man/httpie)(1), [postman](/man/postman)(1), [npm](/man/npm)(1)"]],"e":["curl","httpie","postman","npm"],"x":"newman run [collection.json]"},"nix-shell.3":{"n":"nix-shell.3","t":"Start an interactive shell based on a Nix expression","s":[["TAGLINE","Start an interactive shell based on a Nix expression"],["TLDR","**Development shell** from shell.nix\n\n```nix-shell [shell.nix]```\n\n**Pure shell** with no host environment leaking in\n\n```nix-shell --pure```\n\n**Ad-hoc shell** with specific packages\n\n```nix-shell -p [python3] [git]```\n\n**Run a command** in the shell environment and exit\n\n```nix-shell --run \"[make check]\"```\n\n**Select attribute** from a Nix expression\n\n```nix-shell -A [dev]```\n\n**Keep environment variable** in a pure shell\n\n```nix-shell --pure --keep [HOME]```"],["SYNOPSIS","**nix-shell** [_options_] [_path_]"],["PARAMETERS","_PATH_\n> Nix expression file (default: shell.nix or default.nix).\n\n**-p**, **--packages** _packages..._\n> Set up an environment with the specified packages from Nixpkgs.\n\n**--pure**\n> Clear the environment before starting the shell, retaining only HOME, USER, and DISPLAY.\n\n**--run** _cmd_\n> Run a command in a non-interactive shell, then exit.\n\n**--command** _cmd_\n> Run a command in an interactive shell, then exit.\n\n**-A**, **--attr** _attrPath_\n> Select an attribute from the Nix expression.\n\n**--keep** _name_\n> Keep an environment variable when using --pure.\n\n**--exclude** _regexp_\n> Do not build dependencies whose store path matches the regexp.\n\n**-i** _interpreter_\n> Specify the interpreter for nix-shell shebang scripts.\n\n**-I** _path_\n> Add a path to the Nix expression search path.\n\n**-v**\n> Verbose output."],["DESCRIPTION","**nix-shell** starts an interactive shell in which the environment is set up based on a Nix expression. It provides all build dependencies of the specified derivation without actually building it, making it ideal for development environments.\n\nWhen used with **--packages/-p**, it creates ad-hoc environments with specific packages available. When used with **--pure**, the host environment is cleared to closely match a real Nix build environment.\n\nnix-shell can also be used as a script interpreter via shebang lines (`#!/usr/bin/env nix-shell`) to create reproducible scripts with declared dependencies."],["CAVEATS","Without **--pure**, the host environment leaks into the shell, which may cause builds to succeed locally but fail in a clean Nix build. The **--packages** flag uses Nixpkgs attribute names, not package names."],["INSTALL","```apt: sudo apt install nix-bin```\n\n```dnf: sudo dnf install nix```\n\n```pacman: sudo pacman -S nix```\n\n```apk: sudo apk add nix```\n\n```zypper: sudo zypper install nix```\n\n```nix: nix profile install nixpkgs#nix```"],["SEE ALSO","[nix](/man/nix)(1), [nix-build](/man/nix-build)(1), [nix-shell](/man/nix-shell)(1), [nix-develop](/man/nix-develop)(1), [nix-store](/man/nix-store)(1)"]],"e":["nix","nix-build","nix-shell","nix-develop","nix-store"],"x":"nix-shell [shell.nix]"},"npm-dist-tag":{"n":"npm-dist-tag","t":"manages distribution tags for packages","s":[["TAGLINE","manages distribution tags for packages"],["TLDR","**List tags for package**\n\n```npm dist-tag ls [package-name]```\n\n**Add tag to version**\n\n```npm dist-tag add [package]@[version] [tag]```\n\n**Remove tag**\n\n```npm dist-tag rm [package] [tag]```"],["SYNOPSIS","**npm** **dist-tag** _command_ [_options_]"],["PARAMETERS","**ls** _package_\n> List tags for package.\n\n**add** _pkg@version_ _tag_\n> Add tag to version.\n\n**rm** _package_ _tag_\n> Remove tag."],["DESCRIPTION","**npm dist-tag** manages distribution tags for packages. Tags like latest, next, or beta point to specific versions. Used to control what version npm install gets by default."],["INSTALL","```pacman: sudo pacman -S npm```\n\n```apk: sudo apk add npm```\n\n```brew: brew install npm```"],["SEE ALSO","[npm](/man/npm)(1), [npm-publish](/man/npm-publish)(1)"]],"e":["npm","npm-publish"],"x":"npm dist-tag ls [package-name]"},"ocsp.1s":{"n":"ocsp.1s","t":"queries OCSP responders","s":[["TAGLINE","queries OCSP responders"],["TLDR","**Check certificate status**\n\n```openssl ocsp -issuer [issuer.pem] -cert [cert.pem] -url [ocsp_url]```\n\n**Query OCSP responder**\n\n```openssl ocsp -issuer [issuer.pem] -serial [serial_number] -url [ocsp_url]```\n\n**Verify response**\n\n```openssl ocsp -respin [response.der] -verify_other [ca.pem]```\n\n**Create OCSP request**\n\n```openssl ocsp -issuer [issuer.pem] -cert [cert.pem] -reqout [request.der]```"],["SYNOPSIS","**openssl** **ocsp** [_options_]"],["PARAMETERS","**-issuer** _FILE_\n> Issuer certificate.\n\n**-cert** _FILE_\n> Certificate to check.\n\n**-serial** _NUMBER_\n> Certificate serial number.\n\n**-url** _URL_\n> OCSP responder URL.\n\n**-respin** _FILE_\n> Read OCSP response.\n\n**-reqout** _FILE_\n> Write OCSP request.\n\n**--help**\n> Display help information."],["DESCRIPTION","**openssl ocsp** queries OCSP responders. Checks certificate revocation status.\n\nThe tool validates certificates online. Part of OpenSSL suite."],["CAVEATS","Requires network access. Part of OpenSSL. OCSP responder must be available."],["HISTORY","OCSP support was added to OpenSSL for **online certificate revocation** checking."],["SEE ALSO","[openssl](/man/openssl)(1), [openssl-verify](/man/openssl-verify)(1), [openssl-crl](/man/openssl-crl)(1)"]],"e":["openssl","openssl-verify","openssl-crl"],"x":"openssl ocsp -issuer [issuer.pem] -cert [cert.pem] -url [ocsp_url]"},"openssl-rsa":{"n":"openssl-rsa","t":"processes RSA keys","s":[["TAGLINE","processes RSA keys"],["TLDR","**View RSA key details**\n\n```openssl rsa -in [private.key] -text -noout```\n\n**Extract public key**\n\n```openssl rsa -in [private.key] -pubout -out [public.key]```\n\n**Remove passphrase**\n\n```openssl rsa -in [encrypted.key] -out [decrypted.key]```\n\n**Add passphrase**\n\n```openssl rsa -in [private.key] -aes256 -out [encrypted.key]```\n\n**Convert PEM to DER**\n\n```openssl rsa -in [private.key] -outform DER -out [private.der]```\n\n**Check key validity**\n\n```openssl rsa -in [private.key] -check```"],["SYNOPSIS","**openssl rsa** [_options_] [-in _file_] [-out _file_]"],["PARAMETERS","**-in** _file_\n> Input file.\n\n**-out** _file_\n> Output file.\n\n**-pubout**\n> Output public key.\n\n**-pubin**\n> Input is public key.\n\n**-text**\n> Print components.\n\n**-noout**\n> Don't output key.\n\n**-check**\n> Check consistency.\n\n**-aes256**, **-aes128**\n> Encrypt output.\n\n**-inform**, **-outform**\n> Input/output format."],["DESCRIPTION","**openssl rsa** processes RSA keys. It can convert formats, extract public keys, add/remove encryption, and verify key integrity.\n\nFor general key operations, consider using openssl pkey instead."],["KEY COMPONENTS","```\nn - Modulus\ne - Public exponent\nd - Private exponent\np, q - Prime factors\n```"],["CAVEATS","RSA-specific; use pkey for other algorithms. Protect private keys. Consider Ed25519 for new applications."],["HISTORY","RSA key handling has been core OpenSSL functionality since the library's creation, though genpkey/pkey are now preferred."],["INSTALL","```apt: sudo apt install openssl```\n\n```dnf: sudo dnf install openssl```\n\n```pacman: sudo pacman -S openssl```\n\n```apk: sudo apk add openssl```\n\n```zypper: sudo zypper install openssl```\n\n```brew: brew install openssl```\n\n```nix: nix profile install nixpkgs#openssl```"],["SEE ALSO","[openssl-pkey](/man/openssl-pkey)(1), [openssl-genpkey](/man/openssl-genpkey)(1), [openssl-genrsa](/man/openssl-genrsa)(1)"]],"e":["openssl-pkey","openssl-genpkey","openssl-genrsa"],"x":"openssl rsa -in [private.key] -text -noout"},"pamstack":{"n":"pamstack","t":"combines multiple single-plane images into a multi-plane PAM","s":[["TAGLINE","combines multiple single-plane images into a multi-plane PAM"],["TLDR","**Stack images as layers**\n\n```pamstack [red.pgm] [green.pgm] [blue.pgm] > [rgb.pam]```\n\n**Stack with alpha channel**\n\n```pamstack [image.ppm] [alpha.pgm] > [rgba.pam]```"],["SYNOPSIS","**pamstack** [_options_] _files_..."],["PARAMETERS","_files_\n> Input image files (same dimensions).\n\n**-tupletype** _type_\n> Output tuple type."],["DESCRIPTION","**pamstack** combines multiple single-plane images into a multi-plane PAM. Creates RGB from separate R, G, B files or adds alpha channel. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pamsplit](/man/pamsplit)(1)"]],"e":["pamsplit"],"x":"pamstack [red.pgm] [green.pgm] [blue.pgm] > [rgb.pam]"},"perldiag":{"n":"perldiag","t":"perl documentation file containing all diagnostic messages that Perl can produce","s":[["TAGLINE","perl documentation file containing all diagnostic messages that Perl can produce"],["TLDR","**View all Perl diagnostics**\n\n```perldoc perldiag```\n\n**Search for specific message**\n\n```perldoc perldiag | grep \"[error message]\"```\n\n**Use splain for explanations**\n\n```perl [script.pl] 2>&1 | splain```\n\n**Enable verbose warnings**\n\n```perl -Mdiagnostics [script.pl]```"],["SYNOPSIS","**perldoc perldiag**\n\n**splain** [_error_message_]"],["DESCRIPTION","**perldiag** is a Perl documentation file containing all diagnostic messages that Perl can produce. Each message includes explanation and suggested fixes.\n\nAccess via perldoc or use diagnostics module for runtime explanations."],["Read full diagnostics","perldoc perldiag"],["Use diagnostics module in script","perl -Mdiagnostics script.pl"],["Explain error messages","echo \"Use of uninitialized value\" | splain\n```"],["IN SCRIPT","```perl\nuse diagnostics;      # Verbose messages\nuse warnings;         # Enable warnings"],["Or for specific categories","use warnings qw(all);\nno warnings qw(uninitialized);\n```"],["MESSAGE TYPES","```\n(W) Warning - may indicate problem\n(D) Deprecation - feature being removed\n(S) Severe warning - almost certainly wrong\n(F) Fatal - compilation error\n(X) Alien - error from external source\n```"],["CAVEATS","Part of core Perl documentation. diagnostics module adds runtime overhead. Use only during development."],["HISTORY","perldiag is part of **Perl** core documentation by **Larry Wall** and the Perl community."],["SEE ALSO","[perl](/man/perl)(1), [perlre](/man/perlre)(1), [perldoc](/man/perldoc)(1)"]],"e":["perl","perlre","perldoc"],"x":"perldoc perldiag"},"pest":{"n":"pest","t":"elegant PHP testing framework","s":[["TAGLINE","elegant PHP testing framework"],["TLDR","**Run all tests**\n\n```pest```\n\n**Run specific test**\n\n```pest --filter [test_name]```\n\n**Run with coverage**\n\n```pest --coverage```\n\n**Run in parallel**\n\n```pest --parallel```\n\n**Stop on first failure**\n\n```pest --bail```\n\n**Watch for changes**\n\n```pest --watch```\n\n**Show test list**\n\n```pest --list-tests```\n\n**Run specific file**\n\n```pest [tests/Feature/ExampleTest.php]```"],["SYNOPSIS","**pest** [_--filter pattern_] [_--parallel_] [_--coverage_] [_options_] [_files_]"],["PARAMETERS","**--filter** _PATTERN_\n> Filter tests by name.\n\n**--parallel**\n> Run in parallel.\n\n**--coverage**\n> Generate coverage report.\n\n**--bail**\n> Stop on first failure.\n\n**--watch**\n> Watch mode.\n\n**--list-tests**\n> List available tests.\n\n**--min** _PERCENT_\n> Minimum coverage.\n\n**--group** _NAME_\n> Run test group.\n\n**--exclude-group** _NAME_\n> Exclude test group.\n\n**-v**, **--verbose**\n> Verbose output."],["DESCRIPTION","**pest** is an elegant PHP testing framework. It provides a clean, expressive syntax built on PHPUnit.\n\nTests use a describe/it syntax similar to JavaScript testing frameworks. Expectations chain fluently for readable assertions.\n\nParallel execution speeds up large test suites. Processes are distributed across available cores.\n\nCoverage reports show code coverage percentage. HTML and other formats are available.\n\nWatch mode reruns tests on file changes. Failed tests rerun first for faster feedback."],["CAVEATS","Requires PHP 8.1 or higher. Built on PHPUnit. Some PHPUnit features accessed differently."],["HISTORY","**Pest** was created by **Nuno Maduro** in **2020**. It brings a modern, expressive syntax to PHP testing while maintaining full PHPUnit compatibility."],["INSTALL","```nix: nix profile install nixpkgs#pest```"],["SEE ALSO","[phpunit](/man/phpunit)(1), [php](/man/php)(1), [composer](/man/composer)(1)"]],"e":["phpunit","php","composer"],"x":"pest"},"pixiecore":{"n":"pixiecore","t":"All-in-one PXE network boot server","s":[["TAGLINE","All-in-one PXE network boot server"],["TLDR","**Boot from kernel and initrd**\n\n```sudo pixiecore boot [vmlinuz] [initrd.img]```\n\n**Boot with kernel command-line args**\n\n```sudo pixiecore boot [vmlinuz] [initrd.img] --cmdline \"[console=ttyS0]\"```\n\n**Boot alongside an existing DHCP server**\n\n```sudo pixiecore boot [vmlinuz] [initrd.img] --dhcp-no-bind```\n\n**Serve boot config via external API endpoint**\n\n```sudo pixiecore api [http://localhost:8080]```\n\n**Listen on a specific address**\n\n```sudo pixiecore boot [vmlinuz] [initrd.img] --listen-addr [192.168.1.1]```\n\n**Enable debug output**\n\n```sudo pixiecore boot [vmlinuz] [initrd.img] --debug```"],["SYNOPSIS","**pixiecore** _command_ [_options_] [_args_]"],["PARAMETERS","**boot** _KERNEL_ _INITRD_...\n> Serve a static boot configuration to all clients.\n\n**api** _URL_\n> Delegate boot configuration to an external HTTP API (per-machine config via MAC address).\n\n**--cmdline** _ARGS_\n> Kernel command-line arguments passed to booting machines.\n\n**--listen-addr** _IP_\n> Address to listen on (default: all interfaces).\n\n**--port** _N_\n> HTTP port for serving boot files (default: 80).\n\n**--dhcp-no-bind**\n> Do not bind DHCP ports; operate as ProxyDHCP alongside an existing DHCP server.\n\n**--log-timestamps**\n> Add timestamps to log output.\n\n**--debug**\n> Enable verbose debug output."],["DESCRIPTION","**pixiecore** is an all-in-one network boot server that combines DHCP/ProxyDHCP, TFTP, and HTTP services into a single binary for PXE booting machines on a local network. Unlike traditional PXE setups that require configuring separate DHCP, TFTP, and boot menu services, pixiecore handles the entire boot process internally and can operate alongside an existing DHCP server without any changes to the network's DHCP configuration.\n\nThe tool offers three operating modes. In static boot mode, a fixed kernel and initrd are served to all clients with the same configuration. API mode delegates boot configuration to an external HTTP service, enabling per-machine customization based on MAC address or other identifiers, which is particularly useful for large-scale provisioning. Quick mode provides ready-made presets for common Linux distributions, requiring minimal configuration to get machines booting over the network."],["CAVEATS","Requires root or equivalent privileges to bind DHCP/TFTP ports. May conflict with existing DHCP servers unless **--dhcp-no-bind** is used. Network must allow broadcast traffic. Only supports BIOS PXE and iPXE; UEFI support is limited."],["HISTORY","**Pixiecore** was created at **Google** for simple, API-driven network booting. It simplifies PXE setups by eliminating need for separate DHCP/TFTP configuration."],["INSTALL","```nix: nix profile install nixpkgs#pixiecore```"],["SEE ALSO","[dnsmasq](/man/dnsmasq)(1), [pxelinux](/man/pxelinux)(1), [tftp](/man/tftp)(1)"]],"e":["dnsmasq","pxelinux","tftp"],"x":"sudo pixiecore boot [vmlinuz] [initrd.img]"},"platformio":{"n":"platformio","t":"Embedded development platform and build system","s":[["TAGLINE","Embedded development platform and build system"],["TLDR","**Initialize project**\n\n```pio init --board [uno]```\n\n**Build project**\n\n```pio run```\n\n**Upload to board**\n\n```pio run --target upload```\n\n**Monitor serial port**\n\n```pio device monitor```\n\n**List connected boards**\n\n```pio device list```\n\n**Install a library**\n\n```pio pkg install --library \"[library_name]\"```\n\n**Search for boards**\n\n```pio boards [pattern]```\n\n**Clean build files**\n\n```pio run --target clean```"],["SYNOPSIS","**pio** _command_ [_options_]"],["DESCRIPTION","**platformio** is an ecosystem for embedded development. It provides unified tools for building, uploading, and debugging embedded software across many platforms and frameworks.\n\nThe tool supports Arduino, ESP32, STM32, and hundreds of other boards with integrated library management and debugging."],["PARAMETERS","**init**\n> Initialize project.\n\n**run**\n> Build project.\n\n**run --target upload**\n> Upload firmware.\n\n**device monitor**\n> Serial monitor.\n\n**device list**\n> List devices.\n\n**pkg install**\n> Install packages (libraries, platforms, tools).\n\n**pkg install --library** _NAME_\n> Install a library dependency.\n\n**pkg search**\n> Search the PlatformIO Registry.\n\n**boards**\n> List supported boards.\n\n**check**\n> Run static code analysis.\n\n**debug**\n> Start debugger.\n\n**test**\n> Run unit tests.\n\n**home**\n> Launch PlatformIO Home web UI.\n\n**-e**, **--environment** _env_\n> Specific environment.\n\n**-v**, **--verbose**\n> Verbose output."],["CONFIGURATION","**platformio.ini**\n> Project configuration file defining board targets, frameworks, library dependencies, and build options.\n\n**~/.platformio/**\n> PlatformIO home directory containing installed platforms, packages, and global settings.\n\n**PLATFORMIO_CORE_DIR**\n> Environment variable to override the PlatformIO home directory location."],["CAVEATS","Initial setup downloads many dependencies. Some boards need drivers. The `pio lib` command family is deprecated since PlatformIO 6.0; use `pio pkg` instead."],["HISTORY","**PlatformIO** was created by **Ivan Kravets** in **2014** to unify embedded development tooling. It addressed the fragmentation of vendor-specific IDEs and build systems, becoming popular with IoT and maker communities."],["INSTALL","```brew: brew install platformio```\n\n```nix: nix profile install nixpkgs#platformio```"],["SEE ALSO","[pio](/man/pio)(1), [arduino-cli](/man/arduino-cli)(1), [make](/man/make)(1), [cmake](/man/cmake)(1), [gdb](/man/gdb)(1)"]],"e":["pio","arduino-cli","make","cmake","gdb"],"x":"pio init --board [uno]"},"pulumi-preview":{"n":"pulumi-preview","t":"Preview infrastructure changes before deployment","s":[["TAGLINE","Preview infrastructure changes before deployment"],["TLDR","**Preview changes**\n\n```pulumi preview```\n\n**Preview specific stack**\n\n```pulumi preview -s [stack]```\n\n**Preview with detailed diff**\n\n```pulumi preview --diff```\n\n**Preview in JSON format**\n\n```pulumi preview --json```\n\n**Preview specific targets**\n\n```pulumi preview --target [urn]```"],["SYNOPSIS","**pulumi** **preview** [_options_]"],["PARAMETERS","**-s**, **--stack** _name_\n> Target stack name.\n\n**--diff**\n> Show detailed property-level diff for each changed resource.\n\n**--json**\n> Emit machine-readable JSON output.\n\n**--target** _urn_\n> Preview operations only on the listed resource URNs (repeatable).\n\n**--target-dependents**\n> Also include resources that depend on the targeted ones.\n\n**--refresh**\n> Refresh state from the cloud provider before computing the preview.\n\n**--expect-no-changes**\n> Exit with a non-zero status if any changes are detected. Useful for CI drift detection.\n\n**-p**, **--parallel** _n_\n> Maximum number of resource operations to run concurrently.\n\n**--config** _key=value_\n> Override a config value for this preview.\n\n**--message** _text_\n> Optional message recorded with the update.\n\n**--show-replacement-steps**\n> Show the underlying create/delete steps that make up a replacement."],["DESCRIPTION","**pulumi preview** computes and displays the set of resource operations that **pulumi up** would perform, without actually making any changes. It compares the desired state described in the program with the current state recorded for the stack (optionally refreshed from the cloud), and reports the create/update/replace/delete operations that would result.\n\nUse **--diff** for a full property-level view of what would change, and **--expect-no-changes** in CI pipelines to detect drift between the program and the deployed infrastructure."],["CAVEATS","A preview only reflects what Pulumi can determine statically: provider-side validation errors or runtime failures may still surface during **pulumi up**. Without **--refresh**, the preview is computed against the last-known state, which may be stale."],["INSTALL","```pacman: sudo pacman -S pulumi```\n\n```apk: sudo apk add pulumi```\n\n```brew: brew install pulumi```\n\n```nix: nix profile install nixpkgs#pulumi```"],["SEE ALSO","[pulumi](/man/pulumi)(1), [pulumi-up](/man/pulumi-up)(1), [pulumi-destroy](/man/pulumi-destroy)(1)"]],"e":["pulumi","pulumi-up","pulumi-destroy"],"x":"pulumi preview"},"pyversions":{"n":"pyversions","t":"List available Python interpreter versions on Debian","s":[["TAGLINE","List available Python interpreter versions on Debian"],["TLDR","**List installed Python versions**\n\n```pyversions -i```\n\n**Show supported versions**\n\n```pyversions -s```\n\n**Show default version**\n\n```pyversions -d```\n\n**Show requested versions**\n\n```pyversions -r```"],["SYNOPSIS","**pyversions** [_options_]"],["PARAMETERS","**-i**, **--installed**\n> List installed Python versions.\n\n**-s**, **--supported**\n> List Python versions currently supported by the running Debian release.\n\n**-d**, **--default**\n> Print the default Python version (target of **/usr/bin/python**).\n\n**-r**, **--requested** [_pkg_|_setup.py_|_debian/control_]\n> Print the Python versions requested by a package source (defaults to the **debian/control** in the current directory).\n\n**-v**, **--version**\n> Combined with one of the above, also print the corresponding interpreter path."],["DESCRIPTION","**pyversions** is a Debian-specific helper that reports information about **Python 2** interpreter versions available on the system. It lists installed versions, the default version, the versions supported by the current Debian release, and the versions a source package requests via its **X-Python-Version** field in **debian/control**. It is used by Debian's Python packaging infrastructure (debhelper, dh_python2) to drive build-time decisions.\n\nThe Python 3 counterpart is **py3versions**, which is the tool you should use today; **pyversions** itself was retired together with Python 2 in **Debian 11 (bullseye)** and removed from later releases."],["CAVEATS","Debian/Ubuntu specific and Python-2 only. The package providing it (**python**) was dropped in Debian 11+ and Ubuntu 20.04+. On modern systems, use **py3versions** instead."],["HISTORY","**pyversions** shipped as part of Debian's **python-minimal** / **python** package and was the canonical way to query Python 2 versions in Debian packaging. After Python 2 reached end-of-life on **2020-01-01**, the tool was deprecated alongside the language and removed from current Debian/Ubuntu releases."],["SEE ALSO","[python](/man/python)(1), [py3versions](/man/py3versions)(1), [update-alternatives](/man/update-alternatives)(1)"]],"e":["python","py3versions","update-alternatives"],"x":"pyversions -i"},"rabbitmqctl-cluster":{"n":"rabbitmqctl-cluster","t":"Manage RabbitMQ cluster membership and status","s":[["TAGLINE","Manage RabbitMQ cluster membership and status"],["TLDR","**Show cluster status**\n\n```rabbitmqctl cluster_status```\n\n**Join cluster**\n\n```rabbitmqctl join_cluster [rabbit@node1]```\n\n**Leave cluster**\n\n```rabbitmqctl reset```\n\n**Change node type**\n\n```rabbitmqctl change_cluster_node_type [disc]```"],["SYNOPSIS","**rabbitmqctl** _cluster_command_ [_options_]"],["PARAMETERS","**cluster_status**\n> Show cluster status.\n\n**join_cluster** _node_\n> Join existing cluster.\n\n**forget_cluster_node** _node_\n> Remove offline node from cluster.\n\n**change_cluster_node_type** _type_\n> Change to disc or ram node.\n\n**rename_cluster_node** _old_ _new_\n> Rename cluster node.\n\n**force_boot**\n> Force node to start when last to shutdown."],["DESCRIPTION","**rabbitmqctl** cluster commands manage RabbitMQ cluster membership. Form clusters, add/remove nodes, and monitor cluster health. Part of RabbitMQ server management."],["INSTALL","```dnf: sudo dnf install rabbitmq-server```\n\n```apk: sudo apk add rabbitmq-server```\n\n```zypper: sudo zypper install rabbitmq-server```\n\n```nix: nix profile install nixpkgs#rabbitmq-server```"],["SEE ALSO","[rabbitmqctl](/man/rabbitmqctl)(1), [rabbitmq-queues](/man/rabbitmq-queues)(1)"]],"e":["rabbitmqctl","rabbitmq-queues"],"x":"rabbitmqctl cluster_status"},"recoverpy":{"n":"recoverpy","t":"Interactively find and recover deleted or overwritten files from your terminal","s":[["TAGLINE","Interactively find and recover deleted or overwritten files from your terminal"],["TLDR","**Launch the recovery TUI**\n\n```sudo recoverpy```\n\n**Install via pip**\n\n```pip install recoverpy```\n\n**Run via pipx without installing**\n\n```sudo pipx run recoverpy```"],["SYNOPSIS","**recoverpy** [_options_]"],["DESCRIPTION","**recoverpy** is a TUI (Text User Interface) tool for interactively recovering deleted or overwritten files. It scans raw partition data directly, searching for byte patterns across the entire block device. Unlike other recovery tools, it can recover both deleted files and overwritten data by scanning every block on the selected partition.\n\nOn launch, recoverpy presents a partition selector, then prompts for a search string. It scans each block on the device and displays matching results interactively, allowing you to navigate through adjacent blocks and save recovered content."],["CAVEATS","Requires **sudo** or root privileges for block device access. Does not reconstruct files automatically or infer file boundaries — results may be partial or fragmented. If blocks have already been overwritten, recovery is not possible. Works best on ext4 and other Linux-native filesystems. The search is performed on raw blocks, so scanning large partitions can take significant time."],["HISTORY","**recoverpy** was created by **PabloLec** and is written in **Python** using the **Textual** TUI framework."],["INSTALL","```brew: brew install recoverpy```"],["SEE ALSO","[testdisk](/man/testdisk)(1), [photorec](/man/photorec)(1), [ddrescue](/man/ddrescue)(1), [foremost](/man/foremost)(1), [extundelete](/man/extundelete)(1)"]],"e":["testdisk","photorec","ddrescue","foremost","extundelete"],"x":"sudo recoverpy"},"rubocop":{"n":"rubocop","t":"Ruby static code analyzer and formatter","s":[["TAGLINE","Ruby static code analyzer and formatter"],["TLDR","**Check current directory**\n\n```rubocop```\n\n**Check specific files**\n\n```rubocop [file.rb] [other.rb]```\n\n**Auto-correct safe issues**\n\n```rubocop -a```\n\n**Auto-correct all issues**\n\n```rubocop -A```\n\n**Check specific cops**\n\n```rubocop --only [Layout/LineLength,Style/StringLiterals]```\n\n**Generate config file**\n\n```rubocop --init```\n\n**Generate todo list**\n\n```rubocop --auto-gen-config```\n\n**Show offense details**\n\n```rubocop --extra-details```"],["SYNOPSIS","**rubocop** [_-a_] [_-A_] [_--only cops_] [_-c config_] [_options_] [_files_]"],["PARAMETERS","**-a**, **--auto-correct**\n> Auto-correct safe issues.\n\n**-A**, **--auto-correct-all**\n> Auto-correct all issues.\n\n**--only** _COPS_\n> Run specific cops only.\n\n**--except** _COPS_\n> Exclude specific cops.\n\n**-c** _FILE_\n> Configuration file.\n\n**--auto-gen-config**\n> Generate todo config.\n\n**--init**\n> Create .rubocop.yml.\n\n**-f** _FORMAT_\n> Output format.\n\n**--display-cop-names**\n> Show cop names.\n\n**--extra-details**\n> Show extra details.\n\n**-L**, **--list-target-files**\n> List files to check.\n\n**--parallel**\n> Run in parallel.\n\n**--safe-auto-correct**\n> Only safe corrections."],["DESCRIPTION","**rubocop** is a Ruby static code analyzer and formatter. It enforces style guidelines from the Ruby Style Guide.\n\nCops are individual rules checking specific style violations. Categories include Layout, Style, Lint, Metrics, and Naming.\n\nAuto-correct modifies files to fix violations. Safe mode (-a) applies only corrections that don't change behavior. Full mode (-A) includes all corrections.\n\nConfiguration in .rubocop.yml customizes rules. Cops can be enabled, disabled, or configured with different parameters.\n\nThe auto-gen-config command creates a todo file ignoring current violations. This enables gradual adoption in existing projects.\n\nIntegration with editors shows issues in real-time. CI integration enforces style on pull requests."],["CONFIGURATION","**.rubocop.yml**\n> Project-level configuration file defining enabled cops, custom parameters, file exclusions, and style preferences.\n\n**~/.rubocop.yml**\n> User-level default configuration applied when no project config is found.\n\n**.rubocop_todo.yml**\n> Auto-generated file listing current violations to be ignored, created by --auto-gen-config for gradual adoption."],["CAVEATS","Style is subjective - many teams customize heavily. Auto-correct can make mistakes. Large codebases take time to check."],["HISTORY","**RuboCop** was created by **Bozhidar Batsov** around **2012**. It became the standard Ruby linter, closely following the community Ruby Style Guide that Batsov also authored."],["INSTALL","```pacman: sudo pacman -S rubocop```\n\n```nix: nix profile install nixpkgs#rubocop```"],["SEE ALSO","[ruby](/man/ruby)(1), [bundle](/man/bundle)(1), [standardrb](/man/standardrb)(1), [reek](/man/reek)(1)"]],"e":["ruby","bundle","standardrb","reek"],"x":"rubocop"},"setup-alpine":{"n":"setup-alpine","t":"Interactive Alpine Linux system installer","s":[["TAGLINE","Interactive Alpine Linux system installer"],["TLDR","**Run the full interactive installer**\n\n```setup-alpine```\n\n**Quick mode** with sensible defaults (DHCP, no SSH, no disk install)\n\n```setup-alpine -q```\n\n**Replay** answers from a previously captured file\n\n```setup-alpine -f [/path/to/answers]```\n\n**Generate a template** answer file with default values\n\n```setup-alpine -c [/path/to/answers.new]```\n\n**Print** the embedded help text\n\n```setup-alpine -h```"],["SYNOPSIS","**setup-alpine** [**-ahq**] [**-c** _file_ | **-f** _file_]"],["PARAMETERS","**-a**\n> Run in **diskless** (\"alpine\") mode: configure a running system without installing to disk.\n\n**-c** _FILE_\n> **Create** an answer file at _FILE_ pre-filled with the current default answers, then exit.\n\n**-f** _FILE_\n> **Read** answers from _FILE_ instead of prompting interactively (allows unattended installs).\n\n**-q**\n> **Quick** mode: configure only keymap, hostname, DHCP networking, /etc/hosts and the default repositories.\n\n**-h**\n> Show usage and exit."],["DESCRIPTION","**setup-alpine** is the canonical first-boot installer shipped on every official Alpine Linux ISO. It runs from the live environment and guides the operator through the steps required to bring up a working Alpine system: keyboard layout, hostname, network interfaces, DNS servers, root password, timezone, HTTP proxy, NTP daemon, package mirrors, SSH server, and disk layout.\n\nInternally **setup-alpine** is a small POSIX shell script that orchestrates a number of dedicated **setup-***_step_ helpers (such as **setup-keymap**, **setup-hostname**, **setup-interfaces**, **setup-disk**, **setup-sshd**, **setup-ntp**, **setup-apkrepos**, **setup-lbu** and **setup-apkcache**). Each helper can also be invoked stand-alone after installation to reconfigure that aspect of the system.\n\nThree install targets are offered when the disk step runs: **sys** for a traditional persistent installation, **data** for a diskless system whose state is backed up via **lbu**, and **none** to skip disk setup entirely. The result is a small, repeatable, and scriptable Alpine deployment."],["ANSWER FILE","Use **setup-alpine -c answers.new** to dump a template, then edit the variables and replay it with **setup-alpine -f answers**. Common keys are **KEYMAPOPTS**, **HOSTNAMEOPTS**, **INTERFACESOPTS**, **DNSOPTS**, **TIMEZONEOPTS**, **PROXYOPTS**, **APKREPOSOPTS**, **SSHDOPTS**, **NTPOPTS**, **DISKOPTS**, **LBUOPTS** and **APKCACHEOPTS**. Setting a key to **none** skips that step."],["CAVEATS","Quick mode (**-q**) configures only DHCP — Wi-Fi must be set up manually with **setup-interfaces** or **wpa_supplicant**. Running **setup-alpine** on an already-installed system will reconfigure it in place, including overwriting **/etc/network/interfaces** and **/etc/hostname**. Disk mode **sys** wipes the selected disks. The script must be run as **root** and assumes the standard Alpine BusyBox userspace."],["HISTORY","**setup-alpine** has shipped with **Alpine Linux** since the project introduced its installer-on-rails approach in the late 2000s, replacing the earlier manual install process. It is part of the **alpine-conf** package, written in POSIX shell, and is also reused by **setup-bootable**, **setup-acf** and the LXC/Docker minirootfs build pipeline."],["INSTALL","```apk: sudo apk add alpine-conf```"],["SEE ALSO","[lbu](/man/lbu)(8), [apk](/man/apk)(8)"]],"e":["lbu","apk"],"x":"setup-alpine"},"showmount":{"n":"showmount","t":"Query NFS server export information","s":[["TAGLINE","Query NFS server export information"],["TLDR","**Show exports**\n\n```showmount -e [server]```\n\n**Show mounted directories**\n\n```showmount -d [server]```\n\n**Show all mounts**\n\n```showmount -a [server]```\n\n**Show local exports**\n\n```showmount -e```\n\n**No header output**\n\n```showmount --no-headers -e [server]```"],["SYNOPSIS","**showmount** [_-e_] [_-d_] [_-a_] [_options_] [_host_]"],["PARAMETERS","**-e**, **--exports**\n> Show exports.\n\n**-d**, **--directories**\n> Show mounted directories.\n\n**-a**, **--all**\n> Show all mount points.\n\n**--no-headers**\n> Suppress headers.\n\n**--version**\n> Show version."],["DESCRIPTION","**showmount** queries an NFS server's mount daemon to display information about exported filesystems and active mounts. The **-e** option lists all exported directories along with their access permissions and allowed client hosts, which is the most common usage for discovering available NFS shares.\n\nThe **-d** option shows only directories that are currently mounted by clients, while **-a** displays all active mount points as host:directory pairs, useful for auditing NFS usage and identifying connected clients. Without a host argument, the command queries the local machine's NFS server."],["CAVEATS","Requires rpcbind/portmapper. Firewalls may block queries. NFSv4 may not report all info."],["HISTORY","**showmount** is part of NFS utilities dating to original NFS implementations. It provides essential information for NFS administration."],["INSTALL","```apt: sudo apt install nfs-common```\n\n```dnf: sudo dnf install nfs-utils```\n\n```pacman: sudo pacman -S nfs-utils```\n\n```apk: sudo apk add nfs-utils```\n\n```nix: nix profile install nixpkgs#nfs-utils```"],["SEE ALSO","[mount](/man/mount)(8), [exports](/man/exports)(5), [nfsd](/man/nfsd)(8)"]],"e":["mount","exports","nfsd"],"x":"showmount -e [server]"},"smartd":{"n":"smartd","t":"SMART disk monitoring daemon","s":[["TAGLINE","SMART disk monitoring daemon"],["TLDR","**Start smartd** in the foreground\n\n```smartd -d```\n\n**Start with a specific configuration file**\n\n```smartd -c [/etc/smartd.conf]```\n\n**Check configuration file syntax**\n\n```smartd -q onecheck```\n\n**Run once and exit** (no daemon mode)\n\n```smartd -q onecheck -d```\n\n**Set polling interval** in seconds\n\n```smartd -i [1800]```"],["SYNOPSIS","**smartd** [_-d_] [_-c configfile_] [_-i interval_] [_-l facility_] [_-p pidfile_] [_-q opt_]"],["PARAMETERS","**-d**\n> Run in foreground (debug mode); do not fork into background\n\n**-c** _file_\n> Use specified configuration file instead of /etc/smartd.conf\n\n**-i** _N_\n> Set interval between disk checks in seconds (default: 1800)\n\n**-l** _facility_\n> Use specified syslog facility for logging\n\n**-p** _file_\n> Write PID to specified file\n\n**-q** _option_\n> Quiet mode options: errors, nodev, nodev0, never, onecheck, showtests\n\n**-r** _type_\n> Report ATA errors as type (ioctl, bpt)"],["DESCRIPTION","**smartd** is a daemon that monitors SMART (Self-Monitoring, Analysis and Reporting Technology) data from ATA/SATA and SCSI/SAS hard drives and SSDs. It detects degraded drives and predicts failures before data loss occurs.\n\nThe daemon polls devices at configurable intervals (default 30 minutes), logging changes to SMART attributes via syslog. It can send email alerts, run custom scripts, or execute tests when problems are detected.\n\nConfiguration is stored in **/etc/smartd.conf**, listing devices to monitor with their options. Without a config file, smartd scans and monitors all SMART-capable devices."],["CONFIGURATION","**/etc/smartd.conf**\n> Main configuration file listing devices to monitor and their check options, alert actions, and test schedules.\n\n**/etc/default/smartmontools**\n> Startup options for the smartd daemon on Debian-based systems."],["CAVEATS","smartd monitors disk health but is not a backup solution. Some USB enclosures don't pass SMART data through. Virtual drives and RAID controllers may not expose SMART to the host. Start smartd via systemd: **systemctl enable --now smartd**."],["HISTORY","smartd is part of **smartmontools**, originally developed by Bruce Allen starting in **2002**. The package provides open-source tools for monitoring SMART data across various drive types and operating systems."],["INSTALL","```apt: sudo apt install smartmontools```\n\n```dnf: sudo dnf install smartmontools```\n\n```pacman: sudo pacman -S smartmontools```\n\n```apk: sudo apk add smartmontools```\n\n```zypper: sudo zypper install smartmontools```\n\n```brew: brew install smartmontools```\n\n```nix: nix profile install nixpkgs#smartmontools```"],["SEE ALSO","[smartctl](/man/smartctl)(8), [hddtemp](/man/hddtemp)(8)"]],"e":["smartctl","hddtemp"],"x":"smartd -d"},"snowmachine":{"n":"snowmachine","t":"Make it snow in your terminal","s":[["TAGLINE","Make it snow in your terminal"],["TLDR","**Create a falling snow animation**\n\n```snowmachine snow```\n\n**Snow that stacks at the bottom in rainbow colors**\n\n```snowmachine snow --stack=pile --color=rainbow```\n\n**Render a Christmas tree with a classic star**\n\n```snowmachine tree --star=classic```\n\n**Slow down the falling snow** (lower is slower)\n\n```snowmachine snow --speed=[20]```\n\n**Use specific particle characters**\n\n```snowmachine snow --particle=\"[*]\"```"],["SYNOPSIS","**snowmachine** _command_ [_options_]"],["COMMANDS","**snow**\n> Render falling snow.\n\n**tree**\n> Render a Christmas tree."],["PARAMETERS","**--color** _NAME_\n> Particle colour: _white_, _rainbow_, etc.\n\n**--stack** _MODE_\n> Stack particles at the bottom: _pile_ keeps a growing snow drift; default has none.\n\n**--speed** _N_\n> Frame interval (lower = faster).\n\n**--particle** _CHAR_\n> Override the snowflake character.\n\n**--star** _STYLE_\n> Star atop the tree (e.g., _classic_)."],["DESCRIPTION","**snowmachine** is a Python script that renders animated falling snow or a Christmas tree in your terminal. It supports customizable particle colors, snow stacking effects, speed adjustments, and star decorations for the tree mode."],["HISTORY","**snowmachine** was created by **John Anderson** (sontek) and is written in **Python**."],["INSTALL","```nix: nix profile install nixpkgs#snowmachine```"],["SEE ALSO","[cmatrix](/man/cmatrix)(1), [asciiquarium](/man/asciiquarium)(1), [terminaltexteffects](/man/terminaltexteffects)(1)"]],"e":["cmatrix","asciiquarium","terminaltexteffects"],"x":"snowmachine snow"},"tgt":{"n":"tgt","t":"TUI client for Telegram","s":[["TAGLINE","TUI client for Telegram"],["TLDR","**Launch the Telegram TUI**\n\n```tgt```"],["SYNOPSIS","**tgt** [_options_]"],["DESCRIPTION","**tgt** is a terminal user interface for Telegram that lets you send, receive, edit, delete, and reply to messages entirely from the terminal. It features customizable themes and keybindings via TOML configuration files, full keyboard navigation, and photo viewing support."],["HISTORY","**tgt** was created by **Federico Bruzzone** and is written in **Rust**."],["INSTALL","```apt: sudo apt install tcm```\n\n```pacman: sudo pacman -S tgt```\n\n```zypper: sudo zypper install tgt```\n\n```nix: nix profile install nixpkgs#tgt```"],["SEE ALSO","[scli](/man/scli)(1)"]],"e":["scli"],"x":"tgt"},"tlp-stat":{"n":"tlp-stat","t":"TLP power management status reports","s":[["TAGLINE","TLP power management status reports"],["TLDR","Generate **full** status report\n\n```sudo tlp-stat```\n\nShow **battery** information\n\n```sudo tlp-stat -b```\n\nShow **processor** tunables\n\n```sudo tlp-stat -p```\n\nShow **disk** device tunables\n\n```sudo tlp-stat -d```\n\nShow **configuration**\n\n```sudo tlp-stat -c```\n\nShow **configuration differences** from defaults\n\n```sudo tlp-stat --cdiff```\n\nShow **temperatures** and fan speed\n\n```sudo tlp-stat -t```\n\nShow **system** information and TLP status\n\n```sudo tlp-stat -s```\n\nMonitor power supply **udev events**\n\n```sudo tlp-stat -P```"],["SYNOPSIS","**tlp-stat** [_OPTIONS_]"],["PARAMETERS","**-b, --battery**\n> Battery data\n\n**-c, --config**\n> Active configuration\n\n**--cdiff**\n> Differences between defaults and user configuration\n\n**-d, --disk**\n> Disk device tunables\n\n**-e, --pcie**\n> PCIe device tunables\n\n**-g, --graphics**\n> Graphics card tunables\n\n**-p, --processor**\n> Processor tunables\n\n**-r, --rfkill**\n> Radio device states and tunables\n\n**-s, --system**\n> System information and TLP status\n\n**-t, --temp**\n> Temperatures and fan speed\n\n**-u, --usb**\n> USB device tunables\n\n**-v, --verbose**\n> Show additional detail in supported categories\n\n**-q, --quiet**\n> Omit version header and reduce processor output\n\n**-P, --pev**\n> Monitor power supply udev events\n\n**--psup**\n> Power supply diagnostics\n\n**-T, --trace**\n> Trace output\n\n**--udev**\n> Check if udev rules are active\n\n**-w, --warn**\n> Warnings about SATA disks\n\n**--version**\n> Print TLP version"],["DESCRIPTION","**tlp-stat** generates status reports for TLP power management. It shows active settings, battery health, power consumption, and hardware status. The output helps diagnose power issues and verify TLP configuration."],["CAVEATS","Some information requires root privileges. Battery statistics may not be available on all hardware. Use verbose mode for detailed diagnostics. Some options like --cdiff, --version, and -q require TLP 1.7 or later."],["HISTORY","**tlp-stat** is the companion diagnostic tool for **TLP**, created by **Thomas Koch** for monitoring and troubleshooting Linux laptop power management."],["INSTALL","```dnf: sudo dnf install tlp```\n\n```pacman: sudo pacman -S tlp```\n\n```apk: sudo apk add tlp```\n\n```zypper: sudo zypper install tlp```\n\n```nix: nix profile install nixpkgs#tlp```"],["SEE ALSO","[tlp](/man/tlp)(8), [powertop](/man/powertop)(8), [rfkill](/man/rfkill)(8)"]],"e":["tlp","powertop","rfkill"],"x":"sudo tlp-stat"},"todoman":{"n":"todoman","t":"CalDAV-compatible todo list manager","s":[["TAGLINE","CalDAV-compatible todo list manager"],["TLDR","**List tasks**\n\n```todoman list```\n\n**Add task**\n\n```todoman new \"[task description]\"```\n\n**Complete task**\n\n```todoman done [id]```\n\n**Edit task**\n\n```todoman edit [id]```\n\n**Delete task**\n\n```todoman delete [id]```\n\n**Show task**\n\n```todoman show [id]```"],["SYNOPSIS","**todoman** _command_ [_options_] [_args_]"],["PARAMETERS","**list**\n> List tasks.\n\n**new**\n> Create task.\n\n**done**\n> Complete task.\n\n**edit**\n> Modify task.\n\n**delete**\n> Remove task.\n\n**--list** _NAME_\n> Specify list."],["DESCRIPTION","**todoman** is a command-line todo list manager that stores tasks in the standard iCalendar VTODO format. This makes it compatible with CalDAV servers and other calendar applications that support the iCalendar standard.\n\nWhen paired with **vdirsyncer**, todoman can sync tasks with CalDAV servers like Nextcloud, Radicale, or other compatible services. It supports multiple lists, recurring tasks, due dates, and priorities, all stored in standard .ics files on disk."],["CONFIGURATION","**~/.config/todoman/todoman.conf**\n> Main configuration file specifying calendar paths, date formats, default list, and display settings."],["CAVEATS","Requires configured calendars. vdirsyncer recommended. Python-based."],["HISTORY","**todoman** was created for managing todo items in iCalendar format, with CalDAV server support via vdirsyncer."],["INSTALL","```pacman: sudo pacman -S todoman```\n\n```brew: brew install todoman```\n\n```nix: nix profile install nixpkgs#todoman```"],["SEE ALSO","[vdirsyncer](/man/vdirsyncer)(1), [khal](/man/khal)(1), [task](/man/task)(1)"]],"e":["vdirsyncer","khal","task"],"x":"todoman list"},"tofu-output":{"n":"tofu-output","t":"Display OpenTofu state output values","s":[["TAGLINE","Display OpenTofu state output values"],["TLDR","**Show all outputs**\n\n```tofu output```\n\n**Show specific output**\n\n```tofu output [name]```\n\n**Output as JSON**\n\n```tofu output -json```\n\n**Get raw value**\n\n```tofu output -raw [name]```"],["SYNOPSIS","**tofu** **output** [_options_] [_name_]"],["PARAMETERS","**-json**\n> Output as JSON. Sensitive values are displayed in plain text.\n\n**-raw**\n> Output raw string value. Only works with values that can be automatically converted to strings.\n\n**-state** _path_\n> Read from a specific state file instead of the default.\n\n**-no-color**\n> Disable color output."],["DESCRIPTION","**tofu output** displays output values from OpenTofu state. OpenTofu is the open-source fork of Terraform. Shows values defined in output blocks. When called without a name argument, it displays all outputs. The default human-readable format may change between versions; use -json for stable machine-readable output in scripts. When using -json or -raw, sensitive values are displayed in plain text."],["INSTALL","```dnf: sudo dnf install opentofu```\n\n```pacman: sudo pacman -S opentofu```\n\n```apk: sudo apk add opentofu```\n\n```zypper: sudo zypper install opentofu```\n\n```brew: brew install opentofu```\n\n```nix: nix profile install nixpkgs#opentofu```"],["SEE ALSO","[tofu](/man/tofu)(1), [tofu-plan](/man/tofu-plan)(1), [terraform-output](/man/terraform-output)(1)"]],"e":["tofu","tofu-plan","terraform-output"],"x":"tofu output"},"trek":{"n":"trek","t":"Star Trek tactical simulation game","s":[["TAGLINE","Star Trek tactical simulation game"],["TLDR","**Start a game**\n\n```trek```"],["SYNOPSIS","**trek** [_options_]"],["PREVIEW","```\n . . . . . * . .\n . * . . . . . .\n . . . E . . . .\n . . . . . K . .\n . * . . . . . .\n```"],["DESCRIPTION","**trek** is a text-based Star Trek simulation game from the **bsdgames** collection. The player commands the USS Enterprise through a galaxy divided into quadrants, hunting Klingon warships while managing energy, torpedoes, and time limits.\n\nCommands include navigation, phaser fire, photon torpedoes, short-range and long-range scans, damage reports, and docking at starbases for repairs. The game ends when all Klingons are destroyed, time runs out, or the Enterprise is destroyed."],["CAVEATS","Part of the bsdgames package. Text-based interface with command-line input. Game state is not saved between sessions."],["HISTORY","**trek** is based on the classic **1971** Star Trek mainframe game, one of the earliest computer games. The BSD version was written by **Eric Allman** and has been part of BSD games distributions since the **1980s**."],["INSTALL","```aur: yay -S trek```"],["SEE ALSO","[rogue](/man/rogue)(6), [adventure](/man/adventure)(6), [battlestar](/man/battlestar)(6)"]],"e":["rogue","adventure","battlestar"],"x":"trek"},"udiskie-umount":{"n":"udiskie-umount","t":"Safely unmount removable devices","s":[["TAGLINE","Safely unmount removable devices"],["TLDR","**Unmount** a device\n\n```udiskie-umount [path/to/mount]```\n\nUnmount and **power down** USB\n\n```udiskie-umount --detach [path/to/mount]```\n\n**Eject** optical drive\n\n```udiskie-umount --eject [path/to/mount]```"],["SYNOPSIS","**udiskie-umount** [_OPTIONS_] _PATH_..."],["PARAMETERS","**--detach**\n> Unmount and power down the device\n\n**--eject**\n> Eject optical media\n\n**--help**\n> Display help information"],["DESCRIPTION","**udiskie-umount** manually unmounts devices using udiskie and udisks2. It safely unmounts removable media and can optionally power down USB devices or eject optical drives.\n\nThe path can be either the device path (like /dev/sdb1) or the mount point."],["CAVEATS","Device must not be in use by any process. Detaching USB devices powers them down completely. Ejecting is only applicable to optical drives."],["INSTALL","```dnf: sudo dnf install udiskie```\n\n```pacman: sudo pacman -S udiskie```\n\n```apk: sudo apk add udiskie```\n\n```zypper: sudo zypper install udiskie```\n\n```nix: nix profile install nixpkgs#udiskie```"],["SEE ALSO","[udiskie](/man/udiskie)(1), [udiskie-mount](/man/udiskie-mount)(1), [udisksctl](/man/udisksctl)(1)"]],"e":["udiskie","udiskie-mount","udisksctl"],"x":"udiskie-umount [path/to/mount]"},"upgradepkg":{"n":"upgradepkg","t":"Upgrade Slackware packages","s":[["TAGLINE","Upgrade Slackware packages"],["TLDR","**Upgrade** a package\n\n```sudo upgradepkg [path/to/package.tgz]```\n\n**Install** if not present\n\n```sudo upgradepkg --install-new [path/to/package.tgz]```\n\n**Reinstall** package\n\n```sudo upgradepkg --reinstall [path/to/package.tgz]```\n\n**Dry run** preview\n\n```upgradepkg --dry-run [path/to/package.tgz]```\n\n**Verbose** upgrade\n\n```sudo upgradepkg --verbose [path/to/package.tgz]```"],["SYNOPSIS","**upgradepkg** [_OPTIONS_] _PACKAGE_..."],["PARAMETERS","**--install-new**\n> Install package if not already present\n\n**--reinstall**\n> Reinstall even if same version exists\n\n**--dry-run**\n> Preview without making changes\n\n**--verbose**\n> Show detailed progress"],["DESCRIPTION","**upgradepkg** upgrades Slackware packages by replacing existing packages with new versions. It removes the old package and installs the new one, preserving configuration where possible.\n\nPart of the Slackware pkgtools package management system."],["CAVEATS","Slackware specific. Requires root privileges. Does not handle dependencies. Old package is removed before new one is installed."],["SEE ALSO","[installpkg](/man/installpkg)(8), [removepkg](/man/removepkg)(8), [makepkg](/man/makepkg)(8), [pkgtool](/man/pkgtool)(8)"]],"e":["installpkg","removepkg","makepkg","pkgtool"],"x":"sudo upgradepkg [path/to/package.tgz]"},"uv-tree":{"n":"uv-tree","t":"Display project dependency tree","s":[["TAGLINE","Display project dependency tree"],["TLDR","**Show dependency tree**\n\n```uv tree```\n\n**Show tree with depth limit**\n\n```uv tree --depth [n]```\n\n**Show inverted tree**\n\n```uv tree --invert```\n\n**Show tree for package**\n\n```uv tree --package [name]```"],["SYNOPSIS","**uv** **tree** [_options_]"],["PARAMETERS","**--depth** _n_\n> Limit tree depth.\n\n**--invert**\n> Show reverse dependencies.\n\n**--package** _name_\n> Show specific package.\n\n**--no-dev**\n> Exclude dev dependencies."],["DESCRIPTION","**uv tree** displays the dependency tree for a project. Shows packages and their dependencies in hierarchical format. Useful for understanding and debugging dependency relationships."],["INSTALL","```dnf: sudo dnf install uv```\n\n```pacman: sudo pacman -S uv```\n\n```apk: sudo apk add uv```\n\n```brew: brew install uv```\n\n```nix: nix profile install nixpkgs#uv```"],["SEE ALSO","[uv](/man/uv)(1)"]],"e":["uv"],"x":"uv tree"},"virsh-pool-define-as":{"n":"virsh-pool-define-as","t":"Define new libvirt storage pool","s":[["TAGLINE","Define new libvirt storage pool"],["TLDR","**Define directory pool**\n\n```virsh pool-define-as [name] dir --target [/path/to/pool]```\n\n**Define LVM pool**\n\n```virsh pool-define-as [name] logical --source-name [vgname] --target [/dev/vgname]```\n\n**Define NFS pool**\n\n```virsh pool-define-as [name] netfs --source-host [server] --source-path [/export] --target [/mnt]```"],["SYNOPSIS","**virsh** **pool-define-as** _name_ _type_ [_options_]"],["PARAMETERS","**dir**\n> Directory pool type.\n\n**logical**\n> LVM volume group.\n\n**netfs**\n> Network filesystem.\n\n**--target** _path_\n> Mount/target path.\n\n**--source-path** _path_\n> Source path.\n\n**--source-host** _host_\n> Source hostname.\n\n**--source-name** _name_\n> Source name (VG name)."],["DESCRIPTION","**virsh pool-define-as** defines a new storage pool from parameters. Creates pool configuration without starting it. Supports directory, LVM, NFS, iSCSI, and other pool types."],["SEE ALSO","[virsh](/man/virsh)(1), [virsh-pool-build](/man/virsh-pool-build)(1), [virsh-pool-start](/man/virsh-pool-start)(1)"]],"e":["virsh","virsh-pool-build","virsh-pool-start"],"x":"virsh pool-define-as [name] dir --target [/path/to/pool]"},"wait4x":{"n":"wait4x","t":"Wait for services to become available","s":[["TAGLINE","Wait for services to become available"],["TLDR","**Wait for a TCP port** to accept connections\n\n```wait4x tcp [localhost:8080]```\n\n**Wait for an HTTP endpoint** to return a specific status code\n\n```wait4x http [https://api.example.com/health] --expect-status-code [200]```\n\n**Wait for multiple services** in parallel\n\n```wait4x tcp [127.0.0.1:5432] [127.0.0.1:6379] [127.0.0.1:27017]```\n\n**Wait for a service and then run a command**\n\n```wait4x tcp [localhost:3306] -- [./start-app.sh]```\n\n**Wait for a DNS record** to resolve\n\n```wait4x dns A [example.com] --expected-ip [93.184.216.34]```\n\n**Wait for PostgreSQL** to be ready\n\n```wait4x postgresql 'postgres://[user]:[password]@[localhost]:5432/[mydb]?sslmode=disable'```\n\n**Wait for Redis** with key validation\n\n```wait4x redis redis://[localhost]:6379 --expect-key \"[status]=^ready$\"```\n\n**Wait for a service to become unavailable**\n\n```wait4x tcp [localhost:8080] --invert-check```"],["SYNOPSIS","**wait4x** _command_ [_options_] [_targets_] [**--** _command_]"],["SUBCOMMANDS","**tcp** _address..._\n> Wait for TCP port to accept connections.\n\n**http** _url..._\n> Wait for HTTP endpoint with response validation.\n\n**dns** _type_ _name_\n> Wait for DNS record resolution (A, AAAA, CNAME, MX, NS, TXT).\n\n**mysql** _dsn_\n> Wait for MySQL database readiness.\n\n**postgresql** _dsn_\n> Wait for PostgreSQL availability.\n\n**mongodb** _uri_\n> Wait for MongoDB connection.\n\n**redis** _uri_\n> Wait for Redis availability with optional key checks.\n\n**rabbitmq** _uri_\n> Wait for RabbitMQ message broker.\n\n**kafka** _uri..._\n> Wait for Kafka broker readiness.\n\n**influxdb** _url_\n> Wait for InfluxDB service.\n\n**temporal** _subcommand_\n> Wait for Temporal workflow engine (server or worker).\n\n**exec** _command_\n> Execute shell command and check exit code."],["PARAMETERS","**--timeout** _duration_\n> Maximum wait duration (e.g., 30s).\n\n**--interval** _duration_\n> Check frequency (default: 1s).\n\n**--invert-check**\n> Wait for service to become unavailable instead.\n\n**--backoff-policy** _policy_\n> Retry strategy (e.g., exponential).\n\n**--backoff-exponential-coefficient** _n_\n> Multiplier per retry (e.g., 2.0).\n\n**--backoff-exponential-max-interval** _duration_\n> Maximum interval between retries.\n\n**--expect-status-code** _code_\n> Expected HTTP response code (http subcommand).\n\n**--expect-body-regex** _pattern_\n> Pattern match in HTTP response body.\n\n**--expect-body-json** _path_\n> JSON path validation using GJSON syntax.\n\n**--expect-body-xpath** _expression_\n> XPath evaluation on HTML/XML response.\n\n**--expect-header** _header=value_\n> Expected HTTP response header.\n\n**--request-header** _header_\n> Custom HTTP request header (repeatable).\n\n**--expect-key** _key_\n> Expected Redis key, optionally with value regex (key=regex).\n\n**--expect-table** _table_\n> Expected database table (mysql/postgresql subcommands).\n\n**--expected-ip** _ip_\n> Expected IP address for DNS A/AAAA records.\n\n**--expected-domain** _domain_\n> Expected domain for DNS CNAME/MX records.\n\n**-n** _server_\n> DNS nameserver to query."],["DESCRIPTION","**wait4x** is a lightweight, zero-dependency tool for waiting until services reach a ready state. It supports TCP, HTTP, DNS, and direct integrations with databases and message brokers including MySQL, PostgreSQL, MongoDB, Redis, RabbitMQ, Kafka, InfluxDB, and Temporal.\n\nMultiple targets can be specified to check services in parallel. The **--invert-check** flag reverses the logic to wait for services to become unavailable. After a successful check, an arbitrary command can be executed by appending **--** followed by the command.\n\nwait4x supports configurable timeouts, retry intervals, and exponential backoff strategies, making it suitable for CI/CD pipelines, container orchestration, deployment scripts, and local development environments."],["CAVEATS","Database and broker connection strings are passed as command-line arguments, which may expose credentials in process listings. Use environment variables or secrets management in production. The tool exits with a non-zero status on timeout, so scripts should handle this accordingly."],["HISTORY","**wait4x** was created as a modern, single-binary alternative to shell-based wait scripts and tools like **wait-for-it** and **dockerize**. Written in **Go**, it expanded beyond simple TCP/HTTP checks to include native protocol support for popular databases and message brokers. The project is actively maintained and available via Homebrew, Alpine packages, AUR, NixOS, and Docker."],["INSTALL","```apk: sudo apk add wait4x```\n\n```zypper: sudo zypper install wait4x```\n\n```brew: brew install wait4x```\n\n```nix: nix profile install nixpkgs#wait4x```"],["SEE ALSO","[nc](/man/nc)(1), [curl](/man/curl)(1), [timeout](/man/timeout)(1)"]],"e":["nc","curl","timeout"],"x":"wait4x tcp [localhost:8080]"},"wego":{"n":"wego","t":"Terminal weather forecast with ASCII art","s":[["TAGLINE","Terminal weather forecast with ASCII art"],["TLDR","**Show 3-day forecast for current location**\n\n```wego```\n\n**Show forecast for specific city**\n\n```wego [London]```\n\n**Show 5-day forecast**\n\n```wego -d [5]```\n\n**Show forecast for city with days**\n\n```wego [Paris] [4]```\n\n**Use emoji frontend**\n\n```wego -f emoji```\n\n**Output as JSON**\n\n```wego -f json```"],["SYNOPSIS","**wego** [_options_] [_location_] [_days_]"],["PARAMETERS","**-d**, **--days** _num_\n> Number of forecast days (1-7, default 3).\n\n**-f**, **--frontend** _type_\n> Output frontend: ascii-art-table, emoji, or json.\n\n**-u**, **--units** _system_\n> Units: metric or imperial.\n\n**-b**, **--backend** _name_\n> Weather data source.\n\n**-l**, **--location** _place_\n> Set location for forecast.\n\n**-h**, **--help**\n> Display help."],["DESCRIPTION","**wego** is a terminal weather client that displays forecasts using ASCII art. Written in Go, it shows temperature, wind speed and direction, visibility, and precipitation probability.\n\nThe tool supports multiple weather data backends including OpenWeatherMap. You need to register for an API key and configure it in ~/.wegorc before use.\n\nThree output frontends are available: ascii-art-table (classic terminal look), emoji (modern icons), and json (for scripting). The display requires a UTF-8 terminal with 256 colors."],["CONFIGURATION","Config file: ~/.wegorc\n\nRequired: API key from weather backend. Optional: default location, units, and frontend preferences."],["CAVEATS","Requires API key from weather service. Some backends no longer offer free keys. Needs UTF-8 terminal with 256 colors and appropriate fonts for ASCII art display."],["HISTORY","**wego** was created as a fun terminal weather application. It inspired wttr.in, a web service that wraps wego and serves weather via curl, making terminal weather checks as simple as `curl wttr.in`."],["INSTALL","```apt: sudo apt install wego```\n\n```apk: sudo apk add wego```\n\n```brew: brew install wego```\n\n```nix: nix profile install nixpkgs#wego```"],["SEE ALSO","[curl](/man/curl)(1), [finger](/man/finger)(1)"]],"e":["curl","finger"],"x":"wego"},"wget":{"n":"wget","t":"Non-interactive network file downloader","s":[["TAGLINE","Non-interactive network file downloader"],["TLDR","**Download a file**\n\n```wget [https://example.com/file]```\n\n**Download with custom output name**\n\n```wget -O [filename] [url]```\n\n**Download to specific directory**\n\n```wget -P [directory] [url]```\n\n**Continue a partial download**\n\n```wget -c [url]```\n\n**Download in background**\n\n```wget -b [url]```\n\n**Download recursively** (mirror a website)\n\n```wget -r [url]```\n\n**Download with authentication**\n\n```wget --user=[username] --password=[password] [url]```\n\n**Limit download speed**\n\n```wget --limit-rate=[200k] [url]```\n\n**Download from a list of URLs**\n\n```wget -i [urls.txt]```"],["SYNOPSIS","**wget** [_options_] [_url_...]"],["PARAMETERS","**-O** _file_\n> Write to specified file\n\n**-P** _prefix_\n> Save to specified directory\n\n**-c**, **--continue**\n> Resume partial download\n\n**-b**, **--background**\n> Go to background after starting\n\n**-q**, **--quiet**\n> Quiet mode (no output)\n\n**-r**, **--recursive**\n> Recursive download\n\n**-l** _depth_, **--level**=_depth_\n> Maximum recursion depth (0 = infinite)\n\n**-k**, **--convert-links**\n> Convert links for local viewing\n\n**-p**, **--page-requisites**\n> Download all page requirements (images, CSS, etc.)\n\n**-m**, **--mirror**\n> Mirror mode (-r -N -l inf --no-remove-listing)\n\n**-i** _file_, **--input-file**=_file_\n> Read URLs from file\n\n**--limit-rate**=_rate_\n> Limit download speed\n\n**--user**=_user_, **--password**=_pass_\n> HTTP/FTP authentication\n\n**-U** _agent_, **--user-agent**=_agent_\n> Set User-Agent header\n\n**--no-check-certificate**\n> Don't validate SSL certificates\n\n**--header**=_string_\n> Send additional HTTP header"],["DESCRIPTION","**wget** is a non-interactive network downloader supporting HTTP, HTTPS, and FTP. It can download files, follow links, and mirror entire websites while handling interruptions and retrying failed transfers.\n\nFor simple downloads, wget automatically names the output file from the URL. Use **-O** to specify a different name or **-O -** to output to stdout.\n\nRecursive downloading with **-r** follows links and downloads connected pages. Combine with **-l** to limit depth, **-k** to convert links for offline browsing, and **-p** to get page resources.\n\nWget handles redirects, cookies, and authentication automatically. It's scriptable and works well for automated downloads and cron jobs."],["CAVEATS","Recursive downloading can consume significant bandwidth and storage. Always use **-l** to limit depth when mirroring, and respect robots.txt (wget does by default).\n\n**--no-check-certificate** bypasses SSL verification—use only when necessary and understand the security implications.\n\nSome sites block wget by User-Agent. Use **-U** to set a browser-like User-Agent string if needed.\n\nFor complex downloads with multiple retries and bandwidth control, consider creating a **.wgetrc** configuration file."],["INSTALL","```apt: sudo apt install wget```\n\n```pacman: sudo pacman -S wget```\n\n```apk: sudo apk add wget```\n\n```zypper: sudo zypper install wget```\n\n```brew: brew install wget```\n\n```nix: nix profile install nixpkgs#wget```"],["SEE ALSO","[curl](/man/curl)(1), [aria2c](/man/aria2c)(1), [httrack](/man/httrack)(1), [lftp](/man/lftp)(1)"]],"e":["curl","aria2c","httrack","lftp"],"x":"wget [https://example.com/file]"},"whois":{"n":"whois","t":"Query domain and IP registration information","s":[["TAGLINE","Query domain and IP registration information"],["TLDR","**Look up domain registration info**\n\n```whois [example.com]```\n\n**Look up IP address info**\n\n```whois [8.8.8.8]```\n\n**Query a specific WHOIS server**\n\n```whois -h [whois.verisign-grs.com] [example.com]```\n\n**Look up AS number**\n\n```whois [AS15169]```\n\n**Disable recursive lookups**\n\n```whois -r [example.com]```"],["SYNOPSIS","**whois** [_options_] _object_"],["PARAMETERS","**-h** _host_, **--host** _host_\n> Query the specified WHOIS server instead of guessing it from the object.\n\n**-p** _port_, **--port** _port_\n> Connect to the given TCP port (default: 43).\n\n**-H**\n> Hide the legal disclaimers that some registries show.\n\n**-r**\n> Disable recursive lookups for contact information (RIPE-style servers).\n\n**-I**\n> Query whois.iana.org first and then follow its referral.\n\n**-a**\n> Also search all the mirrored databases (RIPE).\n\n**-T** _type_\n> Restrict the search to objects of the given type (e.g. _inetnum_, _person_).\n\n**-B**\n> Disable object filtering; include e-mail addresses in results.\n\n**-R**\n> Disable following referrals; show the object from the local copy.\n\n**--verbose**\n> Verbose output; show which server is being queried."],["DESCRIPTION","**whois** queries databases for domain name registration information, IP address allocation, and autonomous system (AS) numbers. It provides details about domain ownership, registration dates, name servers, and contact information.\n\nFor domain names, whois contacts the appropriate registry (based on TLD) to retrieve registration data. For IP addresses, it queries regional internet registries (ARIN, RIPE, APNIC, etc.).\n\nThe data returned includes registrant information (often privacy-protected), registration and expiration dates, name servers, and registrar details.\n\nDifferent registries have different data formats and levels of detail. Some domains use privacy services that mask the actual registrant information."],["CAVEATS","Many domain registrations now use privacy protection services, showing proxy information instead of actual registrant details.\n\nRate limits may apply. Excessive queries can result in temporary blocks from WHOIS servers.\n\nData format varies significantly between registries. Parsing WHOIS output programmatically is challenging.\n\nRDAP (Registration Data Access Protocol) is gradually replacing WHOIS with a structured, standardized format."],["INSTALL","```apt: sudo apt install whois```\n\n```dnf: sudo dnf install whois```\n\n```pacman: sudo pacman -S whois```\n\n```apk: sudo apk add whois```\n\n```zypper: sudo zypper install whois```\n\n```brew: brew install whois```\n\n```nix: nix profile install nixpkgs#whois```"],["SEE ALSO","[dig](/man/dig)(1), [host](/man/host)(1), [nslookup](/man/nslookup)(1), [rdap](/man/rdap)(1)"]],"e":["dig","host","nslookup","rdap"],"x":"whois [example.com]"},"xsetroot":{"n":"xsetroot","t":"Set X root window appearance","s":[["TAGLINE","Set X root window appearance"],["TLDR","**Set solid background color**\n\n```xsetroot -solid [blue]```\n\n**Set gray background**\n\n```xsetroot -gray```\n\n**Set background from bitmap**\n\n```xsetroot -bitmap [pattern.xbm]```\n\n**Create plaid pattern**\n\n```xsetroot -mod [4] [4] -fg [red] -bg [white]```\n\n**Set cursor by name**\n\n```xsetroot -cursor_name [left_ptr]```\n\n**Reset to defaults**\n\n```xsetroot -def```\n\n**Set root window name**\n\n```xsetroot -name \"[status text]\"```"],["SYNOPSIS","**xsetroot** [_options_]"],["PARAMETERS","**-solid** _color_\n> Set background to solid color.\n\n**-gray**, **-grey**\n> Set background to gray mesh pattern.\n\n**-bitmap** _file_\n> Tile background with bitmap file.\n\n**-mod** _x_ _y_\n> Create plaid pattern (x, y: 1-16).\n\n**-fg**, **-foreground** _color_\n> Foreground color for patterns.\n\n**-bg**, **-background** _color_\n> Background color for patterns.\n\n**-rv**, **-reverse**\n> Swap foreground and background colors.\n\n**-cursor** _font_ _mask_\n> Set cursor from font glyphs.\n\n**-cursor_name** _name_\n> Set cursor by standard X cursor name.\n\n**-xcf** _file_ _size_\n> Set cursor from Xcursor file.\n\n**-def**, **-default**\n> Reset to default settings.\n\n**-name** _string_\n> Set root window name property.\n\n**-d**, **-display** _display_\n> X server to connect to.\n\n**-help**\n> Display help.\n\n**-version**\n> Display version."],["DESCRIPTION","**xsetroot** configures the appearance of the X root window (desktop background). It can set solid colors, patterns, bitmap tiles, and the root window cursor.\n\nThe tool is commonly used in window manager startup scripts to set initial background appearance. Only one background option (-solid, -gray, -bitmap, or -mod) can be used at a time.\n\nThe -name option sets the root window's WM_NAME property, which some window managers use for status display (e.g., dwm displays it in the status bar).\n\nFor more sophisticated backgrounds, consider using tools like feh or nitrogen."],["CAVEATS","Bitmap files must be in XBM format. Colors use X color names or hex values (#RRGGBB). Some compositing window managers may override root window appearance. The -name option is used by dwm for status bar text."],["HISTORY","**xsetroot** has been part of the X Window System since early releases. It provides basic root window customization following the Unix philosophy of simple, focused tools. Many users now prefer more feature-rich tools like feh for wallpapers."],["INSTALL","```apt: sudo apt install x11-xserver-utils```\n\n```dnf: sudo dnf install xsetroot```\n\n```apk: sudo apk add xsetroot```\n\n```zypper: sudo zypper install xsetroot```\n\n```nix: nix profile install nixpkgs#xsetroot```"],["SEE ALSO","[feh](/man/feh)(1), [nitrogen](/man/nitrogen)(1), [xwallpaper](/man/xwallpaper)(1), [hsetroot](/man/hsetroot)(1)"]],"e":["feh","nitrogen","xwallpaper","hsetroot"],"x":"xsetroot -solid [blue]"},"yadm":{"n":"yadm","t":"Git-based dotfiles manager","s":[["TAGLINE","Git-based dotfiles manager"],["TLDR","**Initialize a new dotfiles repository**\n\n```yadm init```\n\n**Clone an existing dotfiles repository**\n\n```yadm clone [https://github.com/user/dotfiles.git]```\n\n**Clone and run bootstrap automatically**\n\n```yadm clone --bootstrap [https://github.com/user/dotfiles.git]```\n\n**Add a file** to be tracked\n\n```yadm add [~/.bashrc]```\n\n**Commit changes**\n\n```yadm commit -m \"[message]\"```\n\n**Push to remote**\n\n```yadm push```\n\n**Pull updates** from remote\n\n```yadm pull```\n\n**List managed files**\n\n```yadm list -a```\n\n**Encrypt sensitive files**\n\n```yadm encrypt```\n\n**Decrypt files**\n\n```yadm decrypt```\n\n**List encrypted files** without decrypting\n\n```yadm decrypt -l```\n\n**Show status** of tracked dotfiles\n\n```yadm status```"],["SYNOPSIS","**yadm** _command_ [_options_]"],["COMMANDS","**init**\n> Initialize a new dotfiles repository\n\n**clone** _url_\n> Clone a remote dotfiles repository\n\n**add** _file_...\n> Add files to the repository\n\n**commit**\n> Record changes to the repository\n\n**push**\n> Upload changes to remote repository\n\n**pull**\n> Download changes from remote repository\n\n**list** [**-a**]\n> List managed files (-a for all)\n\n**encrypt**\n> Encrypt files matching patterns in ~/.config/yadm/encrypt\n\n**decrypt** [**-l**]\n> Decrypt files from the encrypted archive. Use -l to list only.\n\n**alt**\n> Create symbolic links and process templates for alternate files\n\n**enter**\n> Run a sub-shell with Git variables set\n\n**config**\n> Manage yadm configuration (works like git config)\n\n**bootstrap**\n> Run the bootstrap script if it exists\n\n**status**\n> Display repository status\n\n**diff**\n> View uncommitted changes to tracked files\n\n**remote** [**-v**]\n> Show details about configured remote repositories"],["DESCRIPTION","**yadm** (Yet Another Dotfiles Manager) manages dotfiles using Git while keeping them in their natural locations in **$HOME**. Any Git command works with yadm, making it easy to learn for Git users.\n\nThe repository is stored in **~/.local/share/yadm/repo.git** with **$HOME** as the working tree. Files stay in place; yadm tracks them without requiring symlinks or copying.\n\nKey features include **alternate files** (system-specific configurations based on hostname, OS, or user), **templates** using Jinja2 or envtpl for dynamic configuration, and **encryption** for sensitive data using GPG, OpenSSL, transcrypt, or git-crypt.\n\nThe **bootstrap** command runs **~/.config/yadm/bootstrap** after cloning, enabling automated setup of new machines."],["CAVEATS","yadm requires Git; encryption features require GPG or another supported tool. The working tree is $HOME by default, so be careful with destructive Git operations. Alternate files and templates may need manual **yadm alt** after changes."],["HISTORY","**yadm** was created by **Tim Byrne** as a simple dotfiles manager that leverages Git directly without complex abstractions. First released around **2015**, it gained popularity for its philosophy that managing dotfiles should be as simple as using Git, while adding features like encryption and system-specific alternates."],["INSTALL","```pacman: sudo pacman -S yadm```\n\n```apk: sudo apk add yadm```\n\n```zypper: sudo zypper install yadm```\n\n```brew: brew install yadm```\n\n```nix: nix profile install nixpkgs#yadm```"],["SEE ALSO","[git](/man/git)(1), [stow](/man/stow)(1), [chezmoi](/man/chezmoi)(1), [dotbot](/man/dotbot)(1), [gpg](/man/gpg)(1)"]],"e":["git","stow","chezmoi","dotbot","gpg"],"x":"yadm init"},"ykman-config":{"n":"ykman-config","t":"Configure YubiKey device settings","s":[["TAGLINE","Configure YubiKey device settings"],["TLDR","**Show device info**\n\n```ykman config info```\n\n**Set lock code**\n\n```ykman config set-lock-code```\n\n**Enable USB interface**\n\n```ykman config usb --enable [OTP]```\n\n**Disable NFC interface**\n\n```ykman config nfc --disable [FIDO2]```"],["SYNOPSIS","**ykman** **config** _command_ [_options_]"],["PARAMETERS","**info**\n> Show configuration.\n\n**set-lock-code**\n> Set or change lock code.\n\n**usb** [_options_]\n> Configure enabled applications over USB (OTP, FIDO2, U2F, OATH, PIV, OPENPGP, HSMAUTH).\n\n**nfc** [_options_]\n> Configure enabled applications over NFC.\n\n**--enable** _APP_\n> Enable the named application for the selected transport.\n\n**--disable** _APP_\n> Disable the named application for the selected transport.\n\n**--enable-all**\n> Enable all supported applications for the transport.\n\n**--disable-all**\n> Disable all applications for the transport.\n\n**--lock-code** _HEX_\n> Provide the current lock code when changing protected settings.\n\n**-f**, **--force**\n> Apply changes without confirmation."],["DESCRIPTION","**ykman config** manages YubiKey device configuration. Enable or disable applications and interfaces. Set lock codes for configuration protection."],["INSTALL","```dnf: sudo dnf install yubikey-manager```\n\n```pacman: sudo pacman -S yubikey-manager```\n\n```apk: sudo apk add yubikey-manager```\n\n```zypper: sudo zypper install yubikey-manager```\n\n```brew: brew install ykman```\n\n```nix: nix profile install nixpkgs#yubikey-manager```"],["SEE ALSO","[ykman](/man/ykman)(1), [ykman-fido](/man/ykman-fido)(1)"]],"e":["ykman","ykman-fido"],"x":"ykman config info"}});
