window.LCL_BUCKET(92,{"aws-vpc":{"n":"aws-vpc","t":"Manage Virtual Private Cloud networks, subnets, and gateways.","s":[["TAGLINE","Manage Virtual Private Cloud networks, subnets, and gateways."],["TLDR","**List all VPCs**\n\n```aws ec2 describe-vpcs```\n\n**Create a VPC**\n\n```aws ec2 create-vpc --cidr-block [10.0.0.0/16]```\n\n**List subnets**\n\n```aws ec2 describe-subnets --filters Name=vpc-id,Values=[vpc-xxxxxxxx]```\n\n**Create a subnet**\n\n```aws ec2 create-subnet --vpc-id [vpc-xxxxxxxx] --cidr-block [10.0.1.0/24] --availability-zone [us-east-1a]```\n\n**Create an internet gateway**\n\n```aws ec2 create-internet-gateway```\n\n**Attach internet gateway** to VPC\n\n```aws ec2 attach-internet-gateway --vpc-id [vpc-xxxxxxxx] --internet-gateway-id [igw-xxxxxxxx]```\n\n**List route tables**\n\n```aws ec2 describe-route-tables --filters Name=vpc-id,Values=[vpc-xxxxxxxx]```\n\n**Add a route** to internet gateway\n\n```aws ec2 create-route --route-table-id [rtb-xxxxxxxx] --destination-cidr-block 0.0.0.0/0 --gateway-id [igw-xxxxxxxx]```"],["SYNOPSIS","**aws ec2** _vpc-subcommand_ [_options_]"],["DESCRIPTION","**aws-vpc** refers to VPC (Virtual Private Cloud) commands within the AWS CLI ec2 namespace. VPC provides isolated virtual networks in AWS where you launch resources with complete control over IP addressing, subnets, routing, and security.\n\nKey components include **VPCs** (the network container), **subnets** (IP address ranges in availability zones), **route tables** (traffic routing rules), **internet gateways** (public internet access), **NAT gateways** (outbound-only internet for private subnets), and **security groups** (instance firewalls).\n\nVPC Peering connects VPCs privately. Transit Gateway connects multiple VPCs and on-premises networks at scale. VPC Endpoints provide private access to AWS services without internet traversal."],["PARAMETERS","**describe-vpcs**\n> List VPCs and their details.\n\n**create-vpc**\n> Create a new VPC.\n\n**delete-vpc**\n> Remove a VPC (must be empty).\n\n**describe-subnets**\n> List subnets.\n\n**create-subnet**\n> Create a subnet in a VPC.\n\n**delete-subnet**\n> Remove a subnet.\n\n**describe-internet-gateways**\n> List internet gateways.\n\n**create-internet-gateway**\n> Create an internet gateway.\n\n**attach-internet-gateway**\n> Attach gateway to VPC.\n\n**describe-route-tables**\n> List route tables.\n\n**create-route**\n> Add a route to a table.\n\n**describe-nat-gateways**\n> List NAT gateways.\n\n**create-nat-gateway**\n> Create a NAT gateway.\n\n**--vpc-id** _id_\n> VPC identifier.\n\n**--cidr-block** _cidr_\n> IP address range in CIDR notation.\n\n**--subnet-id** _id_\n> Subnet identifier.\n\n**--availability-zone** _az_\n> Availability zone for subnet.\n\n**--filters** _filters_\n> Filter results by attributes."],["CAVEATS","VPC CIDR blocks cannot overlap with peered VPCs. Each subnet exists in one availability zone. Public subnets need route to internet gateway and public IPs enabled. NAT gateways incur hourly and data charges. Default VPC has different behavior than custom VPCs. Security group rules are stateful; network ACLs are stateless."],["HISTORY","**Amazon VPC** launched in **August 2009** as one of AWS's foundational networking services, allowing customers to define isolated networks. Initially, EC2 ran in a shared network (EC2-Classic), but VPC became the default for new accounts in **2013**. **VPC Peering** added in **2014**. **NAT Gateway** as a managed service launched in **2015**. **Transit Gateway** introduced at **re:Invent 2018** for hub-and-spoke architectures. EC2-Classic was fully retired in **2022**."],["INSTALL","```dnf: sudo dnf install aws```\n\n```pacman: sudo pacman -S aws-cli```\n\n```apk: sudo apk add aws-cli```\n\n```zypper: sudo zypper install aws-cli```"],["SEE ALSO","[aws](/man/aws)(1), [aws-ec2](/man/aws-ec2)(1), [ip](/man/ip)(1)"]],"e":["aws","aws-ec2","ip"],"x":"aws ec2 describe-vpcs"},"az-webapp":{"n":"az-webapp","t":"Manage Azure App Service web applications","s":[["TAGLINE","Manage Azure App Service web applications"],["TLDR","**Create** web app\n\n```az webapp create --resource-group [MyRG] --plan [MyPlan] --name [MyApp]```\n\n**List** web apps\n\n```az webapp list```\n\n**Deploy** an artifact (zip, war, jar, static file)\n\n```az webapp deploy --name [MyApp] --resource-group [MyRG] --src-path [app.zip]```\n\n**Enable** local git deployment\n\n```az webapp deployment source config-local-git --name [MyApp] --resource-group [MyRG]```\n\n**Browse** web app\n\n```az webapp browse --name [MyApp] --resource-group [MyRG]```\n\n**List** available runtime stacks\n\n```az webapp list-runtimes --os-type [linux]```\n\n**Show** logs\n\n```az webapp log tail --name [MyApp] --resource-group [MyRG]```\n\n**Restart** web app\n\n```az webapp restart --name [MyApp] --resource-group [MyRG]```"],["SYNOPSIS","**az webapp** _command_ [_options_]"],["DESCRIPTION","**az webapp** manages Azure App Service web applications. It provides commands for creating, deploying, configuring, and monitoring web apps across multiple platforms including .NET, Node.js, Python, PHP, and Java.\n\nThe command group enables full web application lifecycle management without using the portal."],["PARAMETERS","**create**\n> Create web app\n\n**list**\n> List web apps\n\n**delete**\n> Delete web app\n\n**restart**\n> Restart web app\n\n**stop**\n> Stop web app\n\n**start**\n> Start web app\n\n**show**\n> Show web app details\n\n**browse**\n> Open web app in browser\n\n**deploy**\n> Deploy an artifact (zip, war, jar, static file) to the app\n\n**ssh**\n> Open an interactive SSH session to a Linux web app\n\n**list-runtimes**\n> List supported runtime stacks\n\n**up**\n> Create and deploy from the current local directory (deprecated; prefer create + deploy)\n\n**--resource-group**, **-g** _name_\n> Resource group name\n\n**--name**, **-n** _name_\n> Web app name (globally unique)\n\n**--plan**, **-p** _name_\n> App Service plan name or resource ID\n\n**--runtime**, **-r** _stack_\n> Runtime stack (e.g., \"NODE:20LTS\", \"PYTHON:3.12\", \"JAVA:21-java21\"). Run `az webapp list-runtimes` for current values.\n\n**--deployment-container-image-name**, **-i** _image_\n> Container image to deploy (e.g., myregistry.azurecr.io/image:tag)"],["COMMON OPERATIONS","```bash"],["Create Python web app","az webapp create -g MyRG -p MyPlan -n MyApp --runtime \"PYTHON:3.12\""],["Deploy from a zip artifact","az webapp deploy -g MyRG -n MyApp --src-path app.zip --type zip"],["Set environment variables","az webapp config appsettings set -g MyRG -n MyApp --settings KEY=VALUE"],["Enable logging","az webapp log config -g MyRG -n MyApp --application-logging filesystem --level information"],["Stream logs","az webapp log tail -g MyRG -n MyApp\n```"],["CAVEATS","Web app names must be globally unique (used in the azurewebsites.net URL). Basic (SCM/publish-profile) authentication is disabled by default on new apps; enable it with `--basic-auth Enabled` or `az webapp auth` if your deployment tool needs it. Deployment slots require Standard tier or higher. Some runtimes are not available in all regions. Costs vary by App Service plan tier. `az webapp up` is deprecated; prefer `az webapp create` followed by `az webapp deploy`."],["HISTORY","**az webapp** has been part of Azure CLI since version 2.0 in **2017**, replacing the older Azure CLI for managing App Service web apps."],["INSTALL","```dnf: sudo dnf install azure-cli```\n\n```pacman: sudo pacman -S azure-cli```\n\n```zypper: sudo zypper install azure-cli```\n\n```brew: brew install azure-cli```\n\n```nix: nix profile install nixpkgs#azure-cli```"],["SEE ALSO","[az](/man/az)(1), [azure-cli](/man/azure-cli)(1), [az-functionapp](/man/az-functionapp)(1), [az-appservice](/man/az-appservice)(1)"],["RESOURCES","```[Documentation](https://learn.microsoft.com/en-us/cli/azure/webapp)```\n\n```[Homepage](https://learn.microsoft.com/en-us/cli/azure/)```\n\n```[Source code](https://github.com/Azure/azure-cli)```"]],"e":["az","azure-cli","az-functionapp","az-appservice"],"x":"az webapp create --resource-group [MyRG] --plan [MyPlan] --name [MyApp]"},"bash":{"n":"bash","t":"Unix shell and command language interpreter","s":[["TAGLINE","Unix shell and command language interpreter"],["TLDR","**Run** script\n\n```bash [script.sh]```\n\n**Execute** command\n\n```bash -c [\"echo hello\"]```\n\n**Interactive** login shell\n\n```bash -l```\n\n**Read** from stdin\n\n```echo \"ls -la\" | bash```\n\n**Debug** script\n\n```bash -x [script.sh]```\n\nCheck **syntax** only\n\n```bash -n [script.sh]```"],["SYNOPSIS","**bash** [_options_] [_file_] [_arguments_]"],["DESCRIPTION","**bash** (Bourne Again SHell) is a Unix shell and command language interpreter. It's the default shell on most Linux distributions and macOS, providing command execution, scripting, job control, and extensive programming features.\n\nBash is backward-compatible with the original Bourne shell (sh) while adding numerous improvements and modern features."],["PARAMETERS","**-c** _string_\n> Execute command from string\n\n**-l**, **--login**\n> Login shell (sources profile files)\n\n**-i**\n> Interactive mode\n\n**-x**\n> Print commands before execution (debug mode)\n\n**-n**\n> Syntax check only (no execution)\n\n**-e**\n> Exit on first error\n\n**-u**\n> Error on undefined variables\n\n**-v**\n> Verbose mode (print input lines)\n\n**--noprofile**\n> Don't read profile files\n\n**--norc**\n> Don't read ~/.bashrc\n\n**--posix**\n> Behave according to POSIX standard\n\n**-r**, **--restricted**\n> Start a restricted shell\n\n**--version**\n> Display bash version"],["FEATURES","- Command history (readline)\n- Tab completion\n- Job control (fg, bg, jobs)\n- Arithmetic evaluation\n- Arrays and associative arrays\n- Functions\n- Pattern matching and globbing\n- Process substitution\n- Here documents\n- Conditional execution\n- Loops and flow control"],["STARTUP FILES","**Login shell**\n> /etc/profile, ~/.bash_profile, ~/.bash_login, ~/.profile\n\n**Interactive non-login**\n> ~/.bashrc\n\n**Non-interactive**\n> $BASH_ENV"],["SPECIAL VARIABLES","**$0** - Script name\n**$1, $2, ...** - Positional parameters\n**$@** - All parameters\n**$#** - Parameter count\n**$?** - Exit status of last command\n**$$** - Process ID\n**$!** - PID of last background job"],["CONFIGURATION","**~/.bashrc**\n> Per-user configuration for interactive non-login shells. Aliases, functions, prompt customization.\n\n**~/.bash_profile**\n> Per-user configuration for login shells. Typically sources ~/.bashrc.\n\n**~/.bash_logout**\n> Executed when a login shell exits.\n\n**/etc/bash.bashrc**\n> System-wide configuration for interactive shells.\n\n**/etc/profile**\n> System-wide configuration for login shells."],["CAVEATS","Bash-specific features not portable to POSIX sh. Scripts should use `#!/bin/bash` not `#!/bin/sh`. Unquoted variables can cause word splitting. Use `shellcheck` to validate scripts."],["HISTORY","**Bash** was created by Brian Fox for the GNU Project in **1989** as a free replacement for the Bourne shell, first released as version 0.99. Version 5.0 was released in **2019**."],["INSTALL","```apt: sudo apt install bash```\n\n```dnf: sudo dnf install bash```\n\n```pacman: sudo pacman -S bash```\n\n```apk: sudo apk add bash```\n\n```zypper: sudo zypper install bash```\n\n```brew: brew install bash```\n\n```nix: nix profile install nixpkgs#bash```"],["SEE ALSO","[sh](/man/sh)(1), [zsh](/man/zsh)(1), [shellcheck](/man/shellcheck)(1)"]],"e":["sh","zsh","shellcheck"],"x":"bash [script.sh]"},"cProfile":{"n":"cProfile","t":"Python deterministic profiler","s":[["TAGLINE","Python deterministic profiler"],["TLDR","**Profile a Python script**\n\n```python -m cProfile [script.py]```\n\n**Profile and sort by cumulative time**\n\n```python -m cProfile -s cumtime [script.py]```\n\n**Profile and save to file**\n\n```python -m cProfile -o [profile.prof] [script.py]```\n\n**Sort by total time in function**\n\n```python -m cProfile -s tottime [script.py]```\n\n**Profile specific function in code**\n\n```import cProfile; cProfile.run('[function_call()]')```\n\n**View saved profile with pstats**\n\n```python -c \"import pstats; p = pstats.Stats('[profile.prof]'); p.sort_stats('cumtime').print_stats(20)\"```\n\n**Visualize with snakeviz**\n\n```snakeviz [profile.prof]```"],["SYNOPSIS","**python -m cProfile** [**-o** _output_] [**-s** _sort_] _script.py_ [_args_]"],["PARAMETERS","**-o** _file_\n> Save profile statistics to file for later analysis.\n\n**-s** _sort_\n> Sort output by specified column.\n\n**-m** _module_\n> Profile a module run as a script (passed to python -m)."],["SORT OPTIONS","**calls** / **ncalls**: Number of calls\n**cumulative** / **cumtime**: Cumulative time (including subcalls)\n**tottime** / **time**: Total time in function (excluding subcalls)\n**filename**: File name\n**module**: Module name\n**name** / **nfl**: Function name (name/file/line)\n**pcalls**: Primitive call count\n**stdname**: Standard name\n**line**: Line number"],["DESCRIPTION","**cProfile** is Python's built-in deterministic profiler that measures how much time is spent in each function. It's implemented in C for low overhead, making it suitable for profiling production code.\n\nThe profiler tracks every function call and return, recording the number of calls and time spent. Output shows each function with call count, total time, time per call, cumulative time, and cumulative time per call.\n\nProfile data can be saved for later analysis with the **pstats** module or visualized with tools like **snakeviz**, **pyprof2calltree**, or **gprof2dot**. This enables detailed investigation of performance bottlenecks."],["PROGRAMMATIC USAGE","```python\nimport cProfile\nimport pstats\n\nprofiler = cProfile.Profile()\nprofiler.enable()"],["Code to profile","profiler.disable()\n\nstats = pstats.Stats(profiler)\nstats.sort_stats('cumtime')\nstats.print_stats(10)\n```"],["CAVEATS","Profiling adds overhead that can affect measurements, especially for fast functions. The profiler is deterministic, not statistical, so it captures every call but may impact performance. Use **line_profiler** for line-by-line profiling or **py-spy** for sampling-based profiling with lower overhead."],["HISTORY","cProfile was added to Python in version **2.5** (2006) as a faster alternative to the pure-Python **profile** module. It was based on **lsprof** contributed by **Armin Rigo**. The C implementation provides roughly 10x less overhead than the original profiler while maintaining the same interface."],["SEE ALSO","[py-spy](/man/py-spy)(1), [python](/man/python)(1)"]],"e":["py-spy","python"],"x":"python -m cProfile [script.py]"},"castxml":{"n":"castxml","t":"generate XML descriptions of C/C++ declarations","s":[["TAGLINE","generate XML descriptions of C/C++ declarations"],["TLDR","**Generate XML from C++ header**\n\n```castxml --castxml-output=1 [header.hpp] -o [output.xml]```\n\n**Generate gccxml-compatible output**\n\n```castxml --castxml-gccxml [header.hpp] -o [output.xml]```\n\n**Configure for GNU compiler**\n\n```castxml --castxml-cc-gnu [g++] [header.hpp] -o [output.xml]```\n\n**Start traversal at specific declaration**\n\n```castxml --castxml-start [ClassName] [header.hpp] -o [output.xml]```\n\n**Include path and defines**\n\n```castxml -I[/include/path] -D[MACRO] [header.hpp] -o [output.xml]```"],["SYNOPSIS","**castxml** [_options_] _input_file_"],["DESCRIPTION","**castxml** creates an XML representation of C and C++ declarations from source files. It uses the Clang compiler frontend to parse C-family source code and produces a structured XML output describing types, functions, classes, and other declarations.\n\nThe tool is commonly used as a foundation for generating language bindings, enabling projects like pygccxml to automatically create Python wrappers for C++ libraries. It also serves as a code analysis backend where machine-readable representations of header files are needed.\n\nCastXML was created as a replacement for the deprecated GCC-XML tool, providing a modern Clang-based alternative with compatible output formats for backward compatibility."],["PARAMETERS","**--castxml-output**=_version_\n> Generate castxml-format output (version must be '1')\n\n**--castxml-gccxml**\n> Generate gccxml-compatible output format\n\n**--castxml-cc-gnu** _compiler_\n> Configure preprocessor/target for GNU compiler\n\n**--castxml-cc-msvc** _compiler_\n> Configure preprocessor/target for MSVC compiler\n\n**--castxml-cc-gnu-c** _compiler_\n> Configure for GNU C compiler\n\n**--castxml-cc-msvc-c** _compiler_\n> Configure for MSVC C compiler\n\n**--castxml-start** _name_\n> Start AST traversal at named declaration\n\n**-o** _file_\n> Output file path\n\n**-I** _path_\n> Add include search path\n\n**-D** _macro_\n> Define preprocessor macro\n\n**-std**=_standard_\n> C++ standard (c++11, c++14, c++17, etc.)"],["USE CASES","**Language bindings**\n> Generate bindings for Python, Rust, etc.\n\n**Code analysis**\n> Parse and analyze C++ codebases\n\n**Documentation**\n> Extract API information"],["CAVEATS","Output format depends on selected option. GCCXML format provided for legacy compatibility. Use with pygccxml for Python-based analysis."],["INSTALL","```apt: sudo apt install castxml```\n\n```dnf: sudo dnf install castxml```\n\n```pacman: sudo pacman -S castxml```\n\n```brew: brew install castxml```\n\n```nix: nix profile install nixpkgs#castxml```"],["SEE ALSO","[clang](/man/clang)(1), [g++](/man/g++)(1)"],["RESOURCES","```[Source code](https://github.com/CastXML/CastXML)```\n\n```[Documentation](https://github.com/CastXML/CastXML/blob/master/doc/manual/castxml.1.rst)```"]],"e":["clang","g++"],"x":"castxml --castxml-output=1 [header.hpp] -o [output.xml]"},"clusterssh":{"n":"clusterssh","t":"simultaneous SSH to multiple hosts","s":[["TAGLINE","simultaneous SSH to multiple hosts"],["TLDR","**Open SSH connections to multiple hosts**\n\n```cssh [host1] [host2] [host3]```\n\n**Connect to hosts defined in a cluster tag**\n\n```cssh [cluster_name]```\n\n**Connect with specific username**\n\n```cssh -l [username] [host1] [host2]```\n\n**Connect to hosts from a file**\n\n```cssh -f [hosts.txt]```\n\n**Open connections with custom terminal**\n\n```cssh -T [/usr/bin/xterm] [host1] [host2]```\n\n**List configured clusters**\n\n```cssh -L```"],["SYNOPSIS","**cssh** [_options_] [_hosts_|_clusters_]"],["PARAMETERS","**-l** _username_\n> Username for SSH connections.\n\n**-f** _file_\n> Read hosts from file (one per line).\n\n**-T** _terminal_\n> Terminal application to use.\n\n**-L**\n> List configured clusters.\n\n**-c** _config_\n> Use alternate configuration file.\n\n**-o** _options_\n> Pass options to SSH.\n\n**-t** _title_\n> Set window title.\n\n**-a** _command_\n> Run a command in each session immediately after connecting.\n\n**-p** _port_\n> Override the default SSH port.\n\n**-Q**\n> Do not close cssh when the last terminal exits.\n\n**-q**\n> Quiet mode, suppress warnings.\n\n**-d**\n> Enable debug output (can be repeated up to level 4)."],["CONFIGURATION","Clusters defined in **~/.clusterssh/clusters**:\n```\nwebservers web1 web2 web3\ndbservers db1 db2\nall webservers dbservers\n```"],["DESCRIPTION","**ClusterSSH** (cssh) opens multiple SSH terminal windows simultaneously, with a control window that broadcasts keystrokes to all connections. This enables running the same commands across multiple servers in parallel.\n\nEach host gets its own xterm window, arranged on screen automatically. Typing in the control window sends input to all terminals simultaneously. Individual terminals can be selected for host-specific commands.\n\nClusters can be predefined in configuration files, grouping hosts by function (web servers, databases, etc.). Clusters can include other clusters, building hierarchies for complex environments."],["CAVEATS","Requires X11 display; cannot run in headless environments. Screen real estate limits practical simultaneous connections (typically 10-20 hosts). All hosts receive identical input; be careful with destructive commands. Network latency differences may cause commands to execute at slightly different times."],["HISTORY","ClusterSSH was written by **Duncan Ferguson** and first released in the early **2000s**. It addressed the common need to manage multiple similar servers before configuration management tools like Ansible became widespread. The tool remains popular for ad-hoc tasks and interactive troubleshooting where simultaneous access to multiple hosts is beneficial."],["INSTALL","```dnf: sudo dnf install clusterssh```\n\n```zypper: sudo zypper install clusterssh```"],["SEE ALSO","[tmux](/man/tmux)(1), [pssh](/man/pssh)(1), [ansible](/man/ansible)(1), [ssh](/man/ssh)(1)"]],"e":["tmux","pssh","ansible","ssh"],"x":"cssh [host1] [host2] [host3]"},"csvgrep":{"n":"csvgrep","t":"CSV row filter by column values","s":[["TAGLINE","CSV row filter by column values"],["TLDR","**Filter rows by column** matching a value\n\n```csvgrep -c [column] -m \"[value]\" [data.csv]```\n\n**Filter using regular expression**\n\n```csvgrep -c [column] -r \"[pattern]\" [data.csv]```\n\n**Filter by column number** instead of name\n\n```csvgrep -c [1] -m \"[value]\" [data.csv]```\n\n**Invert match** (exclude matching rows)\n\n```csvgrep -c [column] -m \"[value]\" -i [data.csv]```\n\n**Match against multiple columns**\n\n```csvgrep -c \"[col1,col2]\" -m \"[value]\" [data.csv]```\n\n**Case-insensitive matching**\n\n```csvgrep -c [column] -r \"(?i)[pattern]\" [data.csv]```"],["SYNOPSIS","**csvgrep** [_options_] _file_"],["PARAMETERS","**-c** _COLUMN_, **--columns** _COLUMN_\n> Column name(s) or index to search (comma-separated for multiple).\n\n**-m** _STRING_, **--match** _STRING_\n> Exact string to search for.\n\n**-r** _REGEX_, **--regex** _REGEX_\n> Regular expression pattern to match.\n\n**-f** _FILE_, **--file** _FILE_\n> File containing values to match (one per line).\n\n**-i**, **--invert-match**\n> Select non-matching rows.\n\n**-a**, **--any-match**\n> Match if any column matches (with multiple columns).\n\n**-d** _CHAR_, **--delimiter** _CHAR_\n> Field delimiter (default: comma).\n\n**-e** _ENCODING_, **--encoding** _ENCODING_\n> Input file encoding."],["DESCRIPTION","**csvgrep** is part of csvkit that filters CSV rows based on column values. Unlike standard grep, it understands CSV structure and can match against specific columns while preserving proper CSV output.\n\nThe tool supports both exact string matching and regular expressions. Multiple columns can be searched, and the match sense can be inverted to exclude rows. This makes it ideal for extracting subsets of data based on criteria.\n\ncsvgrep handles quoted fields correctly, ensuring that commas within fields don't break the search. Results include the header row, maintaining valid CSV structure."],["CAVEATS","Regular expressions use Python syntax. Column names are case-sensitive. Large files may be slow as the entire file is processed. Memory usage scales with file size for some operations."],["HISTORY","csvgrep is part of **csvkit**, created by Christopher Groskopf in **2011**. It provides grep-like functionality aware of CSV structure, solving the common problem of searching tabular data without breaking on embedded delimiters."],["SEE ALSO","[csvcut](/man/csvcut)(1), [csvsort](/man/csvsort)(1), [grep](/man/grep)(1), [csvkit](/man/csvkit)(1)"],["RESOURCES","```[Source code](https://github.com/wireservice/csvkit)```\n\n```[Documentation](https://csvkit.readthedocs.io)```"]],"e":["csvcut","csvsort","grep","csvkit"],"x":"csvgrep -c [column] -m \"[value]\" [data.csv]"},"d2":{"n":"d2","t":"modern declarative diagram scripting language","s":[["TAGLINE","modern declarative diagram scripting language"],["TLDR","**Compile a D2 diagram** to SVG\n\n```d2 [diagram.d2] [output.svg]```\n\n**Compile to PNG** format\n\n```d2 --format png [diagram.d2] [output.png]```\n\n**Watch for changes** and auto-recompile\n\n```d2 --watch [diagram.d2] [output.svg]```\n\n**Use a specific layout** engine\n\n```d2 --layout [elk] [diagram.d2] [output.svg]```\n\n**Apply a theme**\n\n```d2 --theme [200] [diagram.d2] [output.svg]```\n\n**Format/pretty-print** a D2 file\n\n```d2 fmt [diagram.d2]```"],["SYNOPSIS","**d2** [_options_] _input.d2_ [_output_]"],["PARAMETERS","_INPUT_\n> D2 source file to compile.\n\n_OUTPUT_\n> Output file path (format inferred from extension).\n\n**--format** _FORMAT_\n> Output format: svg, png, pdf.\n\n**--layout** _ENGINE_\n> Layout engine: dagre (default), elk, tala.\n\n**--theme** _ID_\n> Theme ID (0-200+).\n\n**--watch**, **-w**\n> Watch input file and recompile on changes.\n\n**--sketch**\n> Render in hand-drawn sketch style.\n\n**--pad** _N_\n> Padding around diagram in pixels.\n\n**fmt**\n> Format/reindent D2 source file.\n\n**--help**\n> Display help information."],["DESCRIPTION","**D2** is a modern diagram scripting language that turns text into diagrams. It uses a declarative syntax to describe nodes, connections, and layouts, producing professional-looking diagrams without manual positioning.\n\nD2 supports various diagram types including flowcharts, entity-relationship diagrams, sequence diagrams, and class diagrams. The language is designed to be readable and version-control friendly, with automatic layout handling.\n\nThe tool offers multiple layout engines with different strengths. Watch mode enables live preview during editing. Themes provide consistent styling, and the sketch mode creates a hand-drawn aesthetic."],["CAVEATS","Complex diagrams may require layout hints for optimal appearance. Some features require the TALA layout engine (proprietary). Large diagrams can be slow to render. PNG output requires additional dependencies."],["HISTORY","D2 was created by Terrastruct and released as open source in **2022**. It was designed as a modern alternative to older diagram tools like Graphviz, with better defaults, more features, and improved aesthetics out of the box."],["INSTALL","```dnf: sudo dnf install d2```\n\n```pacman: sudo pacman -S d2```\n\n```zypper: sudo zypper install d2```\n\n```brew: brew install d2```\n\n```nix: nix profile install nixpkgs#d2```"],["SEE ALSO","[dot](/man/dot)(1), [mermaid](/man/mermaid)(1), [plantuml](/man/plantuml)(1)"],["RESOURCES","```[Source code](https://github.com/terrastruct/d2)```\n\n```[Homepage](https://d2lang.com)```"]],"e":["dot","mermaid","plantuml"],"x":"d2 [diagram.d2] [output.svg]"},"devtools":{"n":"devtools","t":"Arch Linux package building scripts","s":[["TAGLINE","Arch Linux package building scripts"],["TLDR","**Build a package in a clean chroot**\n\n```extra-x86_64-build```\n\n**Build testing package**\n\n```testing-x86_64-build```\n\n**Build staging package**\n\n```staging-x86_64-build```\n\n**Create a clean chroot**\n\n```mkarchroot [/path/to/chroot/root] base-devel```\n\n**Update packages in chroot**\n\n```arch-nspawn [/path/to/chroot/root] pacman -Syu```\n\n**Run command in chroot**\n\n```arch-nspawn [/path/to/chroot/root] [command]```\n\n**Sign a package**\n\n```signpkg [package.pkg.tar.zst]```"],["SYNOPSIS","**extra-x86_64-build** [_options_]\n\n**mkarchroot** _chroot_ _packages_...\n\n**arch-nspawn** _chroot_ _command_"],["BUILD COMMANDS","**extra-x86_64-build**\n> Build for extra repository.\n\n**testing-x86_64-build**\n> Build for testing repository.\n\n**staging-x86_64-build**\n> Build for staging repository.\n\n**multilib-build**\n> Build 32-bit multilib packages."],["CHROOT COMMANDS","**mkarchroot** _path_ _pkgs_\n> Create a new chroot with specified packages.\n\n**arch-nspawn** _chroot_ _cmd_\n> Run command in existing chroot using nspawn.\n\n**makechrootpkg** [_options_]\n> Build package in chroot.\n\n**makerepropkg** [_options_]\n> Rebuild a package to check reproducibility."],["PACKAGE TOOLS","**signpkg** _pkg_\n> Sign a package with GPG.\n\n**checkpkg**\n> Compare package with repository version.\n\n**diffpkg** _pkg_\n> Compare package files using different modes.\n\n**find-libdeps** _pkg_\n> Find shared library dependencies.\n\n**find-libprovides** _pkg_\n> Find sonames provided by a package.\n\n**lddd**\n> Find broken library links on your system.\n\n**sogrep** _repo_ _lib_\n> Search for packages using a library."],["DESCRIPTION","**devtools** is a collection of tools used by Arch Linux developers and Trusted Users to build packages in clean chroot environments. It comes with a unified command-line frontend called **pkgctl** that aids to conveniently interact with all the different tools this collection provides.\n\nThe build scripts (extra-x86_64-build, etc.) create isolated chroots matching official repository states, build packages, and verify the results. This guarantees packages work on fresh Arch installations.\n\nAdditional tools help with package maintenance: finding dependencies, comparing versions, signing packages, and managing chroots."],["CONFIGURATION","**/etc/makepkg.conf**\n> System-wide makepkg configuration affecting build behavior.\n\n**~/.makepkg.conf**\n> User-specific makepkg overrides for package building."],["CAVEATS","Requires sudo and systemd-nspawn. Initial chroot creation downloads the base system. Disk space needed for chroot (several GB). Build times depend on package complexity. Chroots should be updated before building."],["HISTORY","devtools was developed by **Arch Linux developers** to standardize package building. The scripts evolved from manual processes into automated tools. Using clean chroots became mandatory for official Arch packages to ensure reproducibility. The tools have been refined over years of use by the Arch packaging community."],["INSTALL","```pacman: sudo pacman -S devtools```"],["SEE ALSO","[makepkg](/man/makepkg)(8), [pacman](/man/pacman)(8), [pkgctl](/man/pkgctl)(1), [systemd-nspawn](/man/systemd-nspawn)(1)"]],"e":["makepkg","pacman","pkgctl","systemd-nspawn"],"x":"extra-x86_64-build"},"dhclient":{"n":"dhclient","t":"ISC DHCP client for dynamic network configuration","s":[["TAGLINE","ISC DHCP client for dynamic network configuration"],["TLDR","**Obtain IP address** for interface\n\n```dhclient [eth0]```\n\n**Release current lease**\n\n```dhclient -r [eth0]```\n\n**Release and obtain** new lease\n\n```dhclient -r [eth0] && dhclient [eth0]```\n\n**Run in foreground** with verbose output\n\n```dhclient -d -v [eth0]```\n\n**Use specific configuration** file\n\n```dhclient -cf [/etc/dhcp/dhclient.conf] [eth0]```\n\n**Contact specific DHCP server**\n\n```dhclient -s [192.168.1.1] [eth0]```"],["SYNOPSIS","**dhclient** [_options_] [_interface_...]"],["PARAMETERS","_INTERFACE_\n> Network interface(s) to configure.\n\n**-r**\n> Release the current lease.\n\n**-d**\n> Run in foreground (debug mode).\n\n**-v**\n> Verbose output.\n\n**-cf** _FILE_\n> Use specified configuration file.\n\n**-lf** _FILE_\n> Use specified lease file.\n\n**-s** _ADDRESS_\n> Specify DHCP server address to contact.\n\n**-4**\n> Use DHCPv4 only.\n\n**-6**\n> Use DHCPv6 only.\n\n**--help**\n> Display help information."],["CONFIGURATION","**/etc/dhcp/dhclient.conf**\n> DHCP client configuration file. Controls request behavior, timeout values, and custom DHCP options.\n\n**/var/lib/dhcp/dhclient.leases**\n> Stores active and historical lease information for network interfaces."],["DESCRIPTION","**dhclient** is the Internet Systems Consortium DHCP client. It dynamically configures network interfaces by obtaining IP addresses, subnet masks, gateways, and DNS servers from DHCP servers.\n\nThe client implements the DHCP protocol, handling lease negotiation, renewal, and release. Configuration options in dhclient.conf allow customizing requests, including requesting specific options or addresses.\n\ndhclient maintains lease information in lease files, allowing it to request the same address on reconnection. It can manage multiple interfaces simultaneously and supports both DHCPv4 and DHCPv6."],["CAVEATS","Requires root privileges. May conflict with network managers like NetworkManager. Lease files accumulate over time. Some options require server support."],["HISTORY","dhclient is part of the **ISC DHCP** package, originally developed by **Ted Lemon** at the Internet Systems Consortium. ISC DHCP has been the reference implementation of DHCP since the late 1990s, though it entered maintenance mode as newer implementations emerged."],["INSTALL","```apt: sudo apt install isc-dhcp-client```\n\n```pacman: sudo pacman -S dhclient```"],["SEE ALSO","[dhcpcd](/man/dhcpcd)(8), [ip](/man/ip)(8), [ifconfig](/man/ifconfig)(8), [networkctl](/man/networkctl)(1)"]],"e":["dhcpcd","ip","ifconfig","networkctl"],"x":"dhclient [eth0]"},"ds4-agent":{"n":"ds4-agent","t":"Interactive coding agent for DwarfStar local LLM inference","s":[["TAGLINE","Interactive coding agent for DwarfStar local LLM inference"],["TLDR","**Start the agent** with a model and large context\n\n```ds4-agent -m [ds4flash.gguf] --ctx [100000]```\n\n**Run a single non-interactive turn**\n\n```ds4-agent -m [ds4flash.gguf] --non-interactive -p \"[fix the failing test]\"```\n\n**Lower GPU power** for quieter runs\n\n```ds4-agent -m [ds4flash.gguf] --power [50]```\n\n**Write a debug trace** of prompts and tool calls\n\n```ds4-agent -m [ds4flash.gguf] --trace [agent.trace]```\n\n**Change working directory** before loading assets\n\n```ds4-agent -m [ds4flash.gguf] --chdir [/path/to/project]```\n\n**Disable thinking** for direct answers\n\n```ds4-agent -m [ds4flash.gguf] --nothink```"],["SYNOPSIS","**ds4-agent** [_options_]"],["PARAMETERS","**-m**, **--model** _FILE_\n> Path to the GGUF model. Default: **ds4flash.gguf**.\n\n**-c**, **--ctx** _N_\n> Allocated context length in tokens.\n\n**-p**, **--prompt** _TEXT_\n> Submit an initial prompt after startup (or a single turn with **--non-interactive**).\n\n**--non-interactive**\n> Run without the TUI. With **-p**: one turn then exit; without **-p**: read repeated prompts from stdin.\n\n**-sys**, **--system** _TEXT_\n> Extra system prompt text. Empty string disables the extra text.\n\n**--trace** _FILE_\n> Write prompt, token, and DSML tool-call debug output.\n\n**--chdir** _DIR_\n> Change working directory before loading runtime assets (useful so relative project paths resolve correctly).\n\n**--power** _N_\n> GPU duty-cycle target from 1 to 100. Default: **100**.\n\n**--metal** | **--cuda** | **--rocm** | **--cpu**\n> Select the inference backend explicitly.\n\n**--ssd-streaming**\n> Stream model weights from SSD when full residency does not fit.\n\n**--think** / **--think-max** / **--nothink**\n> Control model thinking/reasoning mode."],["DESCRIPTION","**ds4-agent** is the integrated coding agent that ships with **DwarfStar** (**ds4**). It loads the same specialized GGUF models as the interactive **ds4** CLI and **ds4-server**, then runs a tool-using agent loop aimed at local development work (reading files, editing, running commands via the model's DSML tool protocol).\n\nIn the interactive TUI you can manage long-lived sessions with slash commands: **/save** and **/list** / **/switch** for session persistence under **~/.ds4/kvcache**, **/compact** to shrink context, **/history**, **/new**, and **/power**. The agent is designed to work with the project's end-to-end local stack rather than as a generic agent framework."],["CAVEATS","Marked **alpha** quality by upstream relative to the rest of DwarfStar. Requires a DwarfStar-compatible GGUF and a suitable GPU backend for practical speed. Tool use and long sessions can consume large amounts of context and disk for KV checkpoints. The sibling interactive inference binary is also named **ds4**, which collides with unrelated DualShock 4 utilities of the same name."],["HISTORY","**ds4-agent** was added to the **DwarfStar** project after the core inference CLI and HTTP server, as part of the goal of shipping model + engine + agent together for local coding workflows on high-memory machines and SSD-streamed setups."],["SEE ALSO","[ds4-server](/man/ds4-server)(1), [ds4-bench](/man/ds4-bench)(1), [ds4-eval](/man/ds4-eval)(1), [ollama](/man/ollama)(1), [llama-cli](/man/llama-cli)(1)"],["RESOURCES","```[Source code](https://github.com/antirez/ds4)```\n\n```[Documentation](https://github.com/antirez/ds4/blob/main/docs/ENGINE_REFERENCE.md)```"]],"e":["ds4-server","ds4-bench","ds4-eval","ollama","llama-cli"],"x":"ds4-agent -m [ds4flash.gguf] --ctx [100000]"},"dvc-gc":{"n":"dvc-gc","t":"Clean unused DVC cache files","s":[["TAGLINE","Clean unused DVC cache files"],["TLDR","**Clean cache keeping** workspace files\n\n```dvc gc -w```\n\n**Clean keeping all** branches\n\n```dvc gc -a```\n\n**Clean keeping all** tags and branches\n\n```dvc gc -a -T```\n\n**Dry run** (show what would be deleted)\n\n```dvc gc -w --dry```\n\n**Force clean** in cloud remote without confirmation\n\n```dvc gc -w -c -f```\n\n**Clean keeping data from the last** N commits\n\n```dvc gc -n [5]```"],["SYNOPSIS","**dvc gc** [_options_]"],["PARAMETERS","**-w**, **--workspace**\n> Keep files used in current workspace.\n\n**-a**, **--all-branches**\n> Keep files used in all Git branch tips.\n\n**-T**, **--all-tags**\n> Keep files used in all Git tags.\n\n**-A**, **--all-commits**\n> Keep files used in all Git commits.\n\n**--all-experiments**\n> Keep files used in all experiments.\n\n**-c**, **--cloud**\n> Also garbage collect in remote storage in addition to local cache.\n\n**-r** _NAME_, **--remote** _NAME_\n> Target a specific remote for garbage collection.\n\n**-n** _NUM_, **--num** _NUM_\n> Keep data from the last NUM commits (default: 1).\n\n**--rev** _COMMIT_\n> Keep data files from a specified Git commit.\n\n**--date** _YYYY-MM-DD_\n> Keep cached data from commits after the specified date.\n\n**--not-in-remote**\n> Keep data not present in remote storage.\n\n**-f**, **--force**\n> Skip confirmation prompts.\n\n**-j** _NUM_, **--jobs** _NUM_\n> Number of concurrent jobs for cloud operations.\n\n**--dry**\n> Preview what would be deleted without executing.\n\n**-p** _PATHS_, **--projects** _PATHS_\n> Include specified projects when sharing a cache directory."],["DESCRIPTION","**dvc gc** removes unused files from the DVC cache, freeing disk space. At least one scope option (**-w**, **-a**, **-T**, **-A**, **--all-experiments**, **-n**, **--rev**, or **--date**) must be specified to define which data to keep.\n\nThe cache accumulates files from all tracked versions. Garbage collection identifies and removes files no longer referenced by any specified commits, branches, or tags.\n\nThe cloud option (**-c**) extends cleaning to remote storage, removing files not needed by the specified scope."],["CAVEATS","Irreversible operation - removed cache files must be re-downloaded or re-computed. Consider keeping all branches for collaboration. Cloud gc may affect other users' access to data. A scope option is required; running without one produces an error."],["HISTORY","dvc gc implements garbage collection for **DVC** caches, similar to git gc but for versioned data files, enabling storage management in ML projects."],["INSTALL","```brew: brew install dvc```\n\n```nix: nix profile install nixpkgs#dvc```"],["SEE ALSO","[dvc](/man/dvc)(1), [dvc-init](/man/dvc-init)(1), [dvc-add](/man/dvc-add)(1), [git-gc](/man/git-gc)(1)"]],"e":["dvc","dvc-init","dvc-add","git-gc"],"x":"dvc gc -w"},"ed":{"n":"ed","t":"standard Unix line editor","s":[["TAGLINE","standard Unix line editor"],["TLDR","**Start ed with** file\n\n```ed [file.txt]```\n\n**Print line 5**\n\n```5p```\n\n**Print all lines**\n\n```,p```\n\n**Substitute text**\n\n```s/old/new/```\n\n**Substitute globally** on line\n\n```s/old/new/g```\n\n**Append after** current line\n\n```a```\n\n**Write and quit**\n\n```wq```\n\n**Edit a file non-interactively** by piping commands to ed\n\n```printf '%s\\n' ',s/old/new/g' 'w' 'q' | ed -s [file.txt]```"],["SYNOPSIS","**ed** [_options_] [_file_]"],["PARAMETERS","_FILE_\n> File to edit.\n\n**-p** _STRING_, **--prompt=**_STRING_\n> Use STRING as the command prompt (there is no prompt by default).\n\n**-s**, **--quiet**, **--script**\n> Suppress diagnostics, byte counts and the '!' prompt: the usual choice for scripts.\n\n**-E**, **--extended-regexp**\n> Use extended regular expressions instead of basic ones.\n\n**-G**, **--traditional**\n> Run in compatibility mode with the traditional Unix ed.\n\n**-l**, **--loose-exit-status**\n> Exit with 0 status even if a command fails.\n\n**-r**, **--restricted**\n> Restricted mode: disallow shell escapes and editing files outside the current directory.\n\n**-v**, **--verbose**\n> Print full error messages rather than a bare '?'.\n\n**--help**\n> Display help and exit.\n\n**--version**\n> Display version and exit."],["DESCRIPTION","**ed** is the standard Unix line editor. It operates in command mode by default, accepting single-letter commands to navigate, view, and modify text. It's the ancestor of sed and vi.\n\nCommands typically consist of an address or range followed by a command letter. Addresses can be line numbers, patterns, or special characters (. for current, $ for last line).\n\ned is useful for scripted editing, as commands can be piped to it. Its minimal interface makes it valuable for emergency system recovery when full-screen editors aren't available."],["CAVEATS","No visual feedback by default. Errors reported with \"?\" unless verbose. Learning curve for command syntax. Not suitable for casual editing."],["HISTORY","ed was written by **Ken Thompson** at Bell Labs in **1969**, making it one of the earliest Unix programs. Its design influenced sed, ex, vi, and ultimately modern text editors."],["INSTALL","```apt: sudo apt install ed```\n\n```dnf: sudo dnf install ed```\n\n```pacman: sudo pacman -S ed```\n\n```apk: sudo apk add ed```\n\n```zypper: sudo zypper install ed```\n\n```brew: brew install ed```\n\n```nix: nix profile install nixpkgs#ed```"],["SEE ALSO","[sed](/man/sed)(1), [vi](/man/vi)(1), [ex](/man/ex)(1)"],["RESOURCES","```[Homepage](https://www.gnu.org/software/ed/)```\n\n```[Documentation](https://www.gnu.org/software/ed/manual/ed_manual.html)```"]],"e":["sed","vi","ex"],"x":"ed [file.txt]"},"gh-at":{"n":"gh-at","t":"Download and verify artifact attestations (alias for gh attestation)","s":[["TAGLINE","Download and verify artifact attestations (alias for gh attestation)"],["TLDR","**Verify an artifact** against a repository's attestations\n\n```gh at verify [example.bin] --repo [owner/repo]```\n\n**Verify an artifact** against an organization's attestations\n\n```gh at verify [example.bin] --owner [org-name]```\n\n**Verify an OCI image**\n\n```gh at verify [oci://image-uri] --owner [org-name]```\n\n**Verify and print full JSON** result\n\n```gh at verify [example.bin] --owner [org-name] --format [json]```\n\n**Download attestations** for offline verification\n\n```gh at download [example.bin] --repo [owner/repo]```\n\n**Output the trusted root** for offline verification\n\n```gh at trusted-root --repo [owner/repo]```"],["SYNOPSIS","**gh at** _command_ [_flags_]"],["PARAMETERS","**verify** [_file-path_ | _oci://image-uri_]\n> Verify the integrity and provenance of an artifact using its associated attestations.\n\n**download** [_file-path_ | _oci://image-uri_]\n> Download an artifact's attestations for offline use.\n\n**trusted-root**\n> Output trusted_root.jsonl contents, for offline verification.\n\n**-o**, **--owner** _string_\n> GitHub organization to scope the attestation lookup by.\n\n**-R**, **--repo** _string_\n> Repository name in the format _owner/repo_."],["VERIFY FLAGS","**-d**, **--digest-alg** _string_\n> Digest algorithm used to compute the artifact's digest: sha256 or sha512 (default sha256).\n\n**--cert-identity** _string_\n> Enforce an exact match of the certificate's SubjectAlternativeName.\n\n**-i**, **--cert-identity-regex** _string_\n> Enforce a regex match of the certificate's SubjectAlternativeName.\n\n**--cert-oidc-issuer** _string_\n> Enforce the OIDC token issuer (default https://token.actions.githubusercontent.com).\n\n**--signer-repo** _string_\n> Enforce a match on the signer workflow's repository.\n\n**--signer-workflow** _string_\n> Enforce a match on the signer workflow's path.\n\n**-b**, **--bundle** _string_\n> Verify using a bundle stored on disk instead of fetching from GitHub.\n\n**--bundle-from-oci**\n> Fetch the attestation bundle from the OCI registry instead of GitHub.\n\n**--format** _string_\n> Output format; currently only **json** is supported.\n\n**-L**, **--limit** _int_\n> Maximum number of attestations to fetch (default 30)."],["DESCRIPTION","**gh at** is the built-in alias for **gh attestation**, part of the official **GitHub CLI**. It downloads and verifies build provenance attestations, cryptographically signed records that tie an artifact back to the GitHub repository, commit, and workflow that built it (via Sigstore). It's used to confirm that a downloaded binary, package, or container image genuinely came from the claimed source and build pipeline."],["CAVEATS","Requires either **--owner** or **--repo** to scope the lookup. Verification depends on network access to GitHub and Sigstore's public-good instance unless **--custom-trusted-root** or **--bundle** is used for fully offline verification."],["INSTALL","```apt: sudo apt install gh```\n\n```dnf: sudo dnf install gh```\n\n```pacman: sudo pacman -S github-cli```\n\n```apk: sudo apk add github-cli```\n\n```zypper: sudo zypper install gh```\n\n```brew: brew install gh```\n\n```nix: nix profile install nixpkgs#gh```"],["SEE ALSO","[gh](/man/gh)(1), [gh-release](/man/gh-release)(1)"],["RESOURCES","```[Manual](https://cli.github.com/manual/gh_attestation)```\n\n```[Source code](https://github.com/cli/cli)```"]],"e":["gh","gh-release"],"x":"gh at verify [example.bin] --repo [owner/repo]"},"go-get":{"n":"go-get","t":"download and manage Go package dependencies","s":[["TAGLINE","download and manage Go package dependencies"],["TLDR","**Download package**\n\n```go get [github.com/pkg/errors]```\n\n**Download specific version**\n\n```go get [github.com/pkg/errors@v0.9.1]```\n\n**Update package**\n\n```go get -u [package]```\n\n**Download all dependencies**\n\n```go get ./...```\n\n**Install binary**\n\n```go install [github.com/user/tool@latest]```"],["SYNOPSIS","**go get** [_options_] [_packages_]"],["PARAMETERS","_PACKAGES_\n> Packages to download.\n\n**-u**\n> Update to latest version.\n\n**-d**\n> Download only, don't install (default behavior since Go 1.16; the flag is now a deprecated no-op).\n\n**-t**\n> Include test dependencies.\n\n**@** _VERSION_\n> Specify version.\n\n**--help**\n> Display help information."],["DESCRIPTION","**go get** downloads and installs packages and dependencies. In module mode, it updates go.mod and go.sum files to track dependencies. For installing tool binaries, use **go install** instead.\n\nThe command resolves versions from module proxies, supporting semantic versioning. It can target specific versions, branches, or commits."],["CAVEATS","Since **Go 1.18**, **go get** no longer builds or installs packages; it only edits go.mod/go.sum and populates the module cache. Running it outside a module, or to install a command, fails with an error pointing to **go install**. Updates modify go.mod and go.sum, which should be committed."],["HISTORY","go get is part of the **Go** toolchain. Its build/install functionality was split off into **go install** in **Go 1.17-1.18** as part of the modules transition, leaving go get focused solely on dependency resolution."],["INSTALL","```apt: sudo apt install gccgo-go```\n\n```dnf: sudo dnf install gcc-go```\n\n```pacman: sudo pacman -S gcc-go```\n\n```apk: sudo apk add gcc-go```\n\n```zypper: sudo zypper install gcc-go```\n\n```brew: brew install go```\n\n```nix: nix profile install nixpkgs#go```"],["SEE ALSO","[go](/man/go)(1), [go-mod](/man/go-mod)(1), [go-install](/man/go-install)(1)"],["RESOURCES","```[Documentation](https://go.dev/ref/mod#go-get)```"]],"e":["go","go-mod","go-install"],"x":"go get [github.com/pkg/errors]"},"graph":{"n":"graph","t":"2D data plotting utility","s":[["TAGLINE","2D data plotting utility"],["TLDR","**Plot data from file**\n\n```graph [data.txt] > [plot.ps]```\n\n**Plot with specific range**\n\n```graph -x [0] [10] -y [0] [100] [data.txt] > [plot.ps]```\n\n**Create PNG output**\n\n```graph -T png [data.txt] > [plot.png]```\n\n**Plot with title**\n\n```graph --title-font-size [0.05] -L \"[My Graph]\" [data.txt] > [plot.ps]```\n\n**Multiple datasets**\n\n```graph [data1.txt] [data2.txt] > [plot.ps]```"],["SYNOPSIS","**graph** [_options_] [_files_]"],["PARAMETERS","**-T** _type_\n> Output format: X, png, pnm, gif, svg, ai, ps, cgm, fig, pcl, hpgl, regis, meta, tek. Defaults to meta (GNU graphics metafile, viewable with plot).\n\n**-x** _min_ _max_\n> X-axis range.\n\n**-y** _min_ _max_\n> Y-axis range.\n\n**-L** _label_\n> Top label (title).\n\n**-X** _label_\n> X-axis label.\n\n**-Y** _label_\n> Y-axis label.\n\n**-m** _type_\n> Line mode (0-5).\n\n**-S** _type_\n> Symbol type.\n\n**-C**\n> Use color.\n\n**-g** _type_\n> Grid type (0-3)."],["DESCRIPTION","**graph** is part of GNU plotutils, creating 2D plots from data files. It reads x,y coordinates from input and generates publication-quality graphs in various formats including PostScript, PNG, and SVG.\n\nThe tool supports multiple datasets, various line and symbol styles, and extensive customization for scientific and technical plotting."],["INPUT FORMAT","```"],["Comment line","1 2.5\n2 3.7\n3 4.2"],["Blank line separates datasets","1 1.5\n2 2.8\n```"],["CAVEATS","Part of plotutils package. Limited interactivity. For complex plots, consider gnuplot. The default output type is the GNU metafile format; use **-T** to select a concrete format such as png, svg, or ps."],["HISTORY","graph is part of **GNU plotutils**, maintained by the GNU project. The package provides a collection of plotting utilities following the Unix philosophy of simple, composable tools."],["INSTALL","```apt: sudo apt install plotutils```\n\n```dnf: sudo dnf install plotutils```\n\n```pacman: sudo pacman -S plotutils```\n\n```zypper: sudo zypper install plotutils```\n\n```brew: brew install plotutils```\n\n```nix: nix profile install nixpkgs#plotutils```"],["SEE ALSO","[gnuplot](/man/gnuplot)(1), [plot](/man/plot)(1)"]],"e":["gnuplot","plot"],"x":"graph [data.txt] > [plot.ps]"},"guacd":{"n":"guacd","t":"guacamole proxy daemon, the core component of Apache Guacamole","s":[["TAGLINE","guacamole proxy daemon, the core component of Apache Guacamole"],["TLDR","**Start guacd daemon**\n\n```guacd```\n\n**Run in foreground**\n\n```guacd -f```\n\n**Listen on specific address and port**\n\n```guacd -b [0.0.0.0] -l [4822]```\n\n**Run with debug logging**\n\n```guacd -f -L debug```\n\n**Specify PID file**\n\n```guacd -p [/var/run/guacd.pid]```"],["SYNOPSIS","**guacd** [_options_]"],["PARAMETERS","**-b** _address_\n> Bind to specified address.\n\n**-l** _port_\n> Listen on specified port (default 4822).\n\n**-f**\n> Run in foreground.\n\n**-L** _level_\n> Log level (error, warning, info, debug).\n\n**-p** _file_\n> PID file location.\n\n**-C** _file_\n> SSL certificate file.\n\n**-K** _file_\n> SSL private key file."],["DESCRIPTION","**guacd** is the Guacamole proxy daemon, the core component of Apache Guacamole. It handles connections between the Guacamole web application and remote desktop protocols (VNC, RDP, SSH, Telnet, Kubernetes). Written in C for performance, it translates these protocols into the Guacamole protocol for browser-based access."],["INSTALL","```dnf: sudo dnf install guacd```\n\n```apk: sudo apk add guacamole-server```\n\n```nix: nix profile install nixpkgs#guacamole-server```"],["SEE ALSO","[sshd](/man/sshd)(1)"]],"e":["sshd"],"x":"guacd"},"gvfs-mount":{"n":"gvfs-mount","t":"mounts virtual filesystems through GVFS","s":[["TAGLINE","mounts virtual filesystems through GVFS"],["TLDR","**Mount remote location**\n\n```gvfs-mount [smb://server/share]```\n\n**Mount SSH location**\n\n```gvfs-mount [sftp://user@host]```\n\n**List mounted locations**\n\n```gvfs-mount -l```\n\n**Unmount location**\n\n```gvfs-mount -u [smb://server/share]```\n\n**Eject media**\n\n```gvfs-mount -e [/dev/sr0]```"],["SYNOPSIS","**gvfs-mount** [_options_] [_location_...]\n\n**gvfs-mount** **--list** [_options_]\n\n**gvfs-mount** **--unmount-scheme**=_SCHEME_"],["PARAMETERS","_LOCATION_\n> File or directory path or URI to mount.\n\n**-m**, **--mountable**\n> Mount the specified location as a mountable, rather than the default mount operation.\n\n**-d**, **--device** _DEVICE_\n> Mount the volume with the given device file.\n\n**-u**, **--unmount**\n> Unmount the given locations.\n\n**-e**, **--eject**\n> Eject the given locations.\n\n**-s**, **--unmount-scheme** _SCHEME_\n> Unmount all mounts with the given scheme (e.g. smb, sftp).\n\n**-l**, **--list**\n> List all known mounts.\n\n**-o**, **--monitor**\n> Monitor mount and unmount events.\n\n**-i**, **--detail**\n> Show extra information for **--list** and **--monitor**.\n\n**-h**, **--help**\n> Print a short help text and exit."],["DESCRIPTION","**gvfs-mount** mounts virtual filesystems through GVFS (GNOME Virtual File System). It supports various protocols including SMB/CIFS, SFTP, FTP, WebDAV, and MTP.\n\nGVFS provides a userspace filesystem layer, allowing file managers and applications to access remote resources transparently."],["SUPPORTED SCHEMES","```\nsmb://    SMB/CIFS network shares\nsftp://   SSH file transfer\nftp://    FTP servers\ndav://    WebDAV servers\nmtp://    MTP devices (phones)\ngphoto2:// PTP cameras\n```"],["CAVEATS","Part of the gvfs-bin command-line tools, deprecated in gvfs 1.31 (2016) and removed entirely in gvfs 1.38 (2018) in favor of the unified **gio** command (`gio mount`). Modern distributions no longer ship gvfs-mount. Requires a running GVFS daemon; mount points appear under /run/user/$UID/gvfs/."],["HISTORY","**gvfs-mount** was part of GVFS's original gvfs-bin utilities, introduced for GNOME alongside GVFS in **2008** (GNOME 2.22). It has since been superseded by **gio mount**."],["SEE ALSO","[gio](/man/gio)(1), [mount](/man/mount)(8), [smbclient](/man/smbclient)(1), [fusermount](/man/fusermount)(1)"],["RESOURCES","```[Source code](https://gitlab.gnome.org/GNOME/gvfs)```\n\n```[Homepage](https://wiki.gnome.org/Projects/gvfs)```"]],"e":["gio","mount","smbclient","fusermount"],"x":"gvfs-mount [smb://server/share]"},"imgtool":{"n":"imgtool","t":"manages MCUboot firmware images","s":[["TAGLINE","manages MCUboot firmware images"],["TLDR","**Create MCUboot image**\n\n```imgtool create --key [key.pem] --align 4 --version 1.0.0 [app.bin] [signed.bin]```\n\n**Sign image**\n\n```imgtool sign --key [key.pem] [input.bin] [output.bin]```\n\n**Get image info**\n\n```imgtool getinfo [image.bin]```\n\n**Generate keys**\n\n```imgtool keygen --key [key.pem] --type rsa-2048```"],["SYNOPSIS","**imgtool** _command_ [_options_]"],["PARAMETERS","_COMMAND_\n> Operation to perform.\n\n**create**\n> Create signed image.\n\n**sign**\n> Sign existing image.\n\n**getinfo**\n> Show image information.\n\n**keygen**\n> Generate signing keys.\n\n**--key** _FILE_\n> Key file.\n\n**--help**\n> Display help information."],["DESCRIPTION","**imgtool** manages MCUboot firmware images. It creates, signs, and inspects bootloader-compatible images.\n\nThe tool is essential for secure boot workflows. It handles image headers, signatures, and version information."],["CAVEATS","Part of MCUboot project. Specific image format. Cryptographic keys required."],["HISTORY","imgtool is part of **MCUboot**, an open-source secure bootloader for microcontrollers."],["INSTALL","```apt: sudo apt install mame-tools```\n\n```dnf: sudo dnf install mame-tools```\n\n```pacman: sudo pacman -S mame-tools```\n\n```zypper: sudo zypper install mame-tools```\n\n```nix: nix profile install nixpkgs#mame-tools```"],["SEE ALSO","[openssl](/man/openssl)(1)"]],"e":["openssl"],"x":"imgtool create --key [key.pem] --align 4 --version 1.0.0 [app.bin] [signed.bin]"},"jira-browse":{"n":"jira-browse","t":"opens a Jira issue or project in your default web browser","s":[["TAGLINE","opens a Jira issue or project in your default web browser"],["TLDR","**Open issue in browser**\n\n```jira browse [ISSUE-123]```\n\n**Open current project board**\n\n```jira browse```\n\n**Open specific project**\n\n```jira browse --project [PROJECT]```"],["SYNOPSIS","**jira** **browse** [_issue-key_] [_options_]"],["PARAMETERS","**--project** _key_\n> Specify project key."],["DESCRIPTION","**jira browse** opens a Jira issue or project in your default web browser. Part of jira-cli, a command-line interface for Jira. Quickly access issues without navigating through the web interface manually."],["SEE ALSO","[jira](/man/jira)(1), [jira-issue](/man/jira-issue)(1)"]],"e":["jira","jira-issue"],"x":"jira browse [ISSUE-123]"},"kdeconnect-cli":{"n":"kdeconnect-cli","t":"controls KDE Connect from the command line","s":[["TAGLINE","controls KDE Connect from the command line"],["TLDR","**List devices**\n\n```kdeconnect-cli --list-available```\n\n**Pair with device**\n\n```kdeconnect-cli --pair -d [device-id]```\n\n**Send file**\n\n```kdeconnect-cli --share [file] -d [device-id]```\n\n**Ring device**\n\n```kdeconnect-cli --ring -d [device-id]```\n\n**Send SMS**\n\n```kdeconnect-cli --send-sms \"[message]\" --destination [number] -d [device-id]```\n\n**Get battery status**\n\n```kdeconnect-cli --battery -d [device-id]```"],["SYNOPSIS","**kdeconnect-cli** [_options_]"],["PARAMETERS","**--list-available**\n> List available devices.\n\n**--pair**\n> Pair with device.\n\n**--unpair**\n> Unpair device.\n\n**-d** _DEVICE_\n> Target device ID.\n\n**--share** _FILE_\n> Send file to device.\n\n**--ring**\n> Ring device.\n\n**--send-sms** _MSG_\n> Send SMS message.\n\n**--help**\n> Display help information."],["DESCRIPTION","**kdeconnect-cli** controls KDE Connect from the command line. It connects Linux desktops with mobile devices.\n\nThe tool sends files, notifications, and controls media. It enables phone integration from terminal."],["CAVEATS","Requires KDE Connect daemon. Devices must be on same network. Phone app needed."],["HISTORY","kdeconnect-cli is part of **KDE Connect**, enabling seamless integration between Linux desktops and Android devices."],["INSTALL","```apt: sudo apt install kdeconnect```\n\n```pacman: sudo pacman -S kdeconnect```\n\n```apk: sudo apk add kdeconnect```"],["SEE ALSO","[adb-connect](/man/adb-connect)(1), [nmcli-connection](/man/nmcli-connection)(1)"]],"e":["adb-connect","nmcli-connection"],"x":"kdeconnect-cli --list-available"},"lavadecode":{"n":"lavadecode","t":"Decode a LAVAFLOW stream into human readable form","s":[["TAGLINE","Decode a LAVAFLOW stream into human readable form"],["TLDR","**Decode a LAVAFLOW stream file**\n\n```lavadecode < [path/to/file.lava]```\n\n**Decode and print file offsets**\n\n```lavadecode -o < [path/to/file.lava]```\n\n**Decode and print hex file offsets**\n\n```lavadecode -h < [path/to/file.lava]```\n\n**Decode and save decompressed planes to .pbm files**\n\n```lavadecode -d [basename] < [path/to/file.lava]```\n\n**Decode with debug output**\n\n```lavadecode -D [level] < [path/to/file.lava]```"],["SYNOPSIS","**lavadecode** [_options_] <_lavaflow-file_"],["PARAMETERS","**-d** _basename_\n> Basename of .pbm file for saving decompressed planes.\n\n**-h**\n> Print hex file offsets.\n\n**-o**\n> Print file offsets.\n\n**-D** _level_\n> Set debug level (default 0)."],["DESCRIPTION","**lavadecode** decodes a LAVAFLOW stream into human readable form. LAVAFLOW is a printer language used by some Konica Minolta printers, such as the magicolor 2530 DL.\n\nIt is part of the **foo2zjs** suite of printer drivers and is used to inspect streams created by **foo2lava**."],["CAVEATS","Only handles LAVAFLOW printer streams. Input is read from stdin via redirection."],["INSTALL","```apt: sudo apt install printer-driver-foo2zjs```"],["SEE ALSO","[foo2lava](/man/foo2lava)(1), [foo2lava-wrapper](/man/foo2lava-wrapper)(1)"]],"e":["foo2lava","foo2lava-wrapper"],"x":"lavadecode < [path/to/file.lava]"},"ldapdomaindump":{"n":"ldapdomaindump","t":"extracts Active Directory information via LDAP, including users","s":[["TAGLINE","extracts Active Directory information via LDAP, including users"],["TLDR","Dump domain info with **credentials**\n\n```ldapdomaindump -u domain\\username -p password [hostname]```\n\nDump with **hostname resolution**\n\n```ldapdomaindump -r -u domain\\username -p password [hostname]```\n\nDump with **custom DNS server**\n\n```ldapdomaindump -r -n [dns_ip] -u domain\\username -p password [hostname]```\n\nDump to **specific directory** without JSON\n\n```ldapdomaindump --no-json -o [path/to/dir] -u domain\\username -p password [hostname]```"],["SYNOPSIS","**ldapdomaindump** [_options_] **-u** _domain_\\\\_user_ **-p** _password_ _hostname_"],["DESCRIPTION","**ldapdomaindump** extracts Active Directory information via LDAP, including users, computers, groups, operating systems, and group memberships. Output is generated in HTML, JSON, and greppable formats for analysis."],["PARAMETERS","**-u, --user DOMAIN\\USER**\n> Domain username for LDAP authentication\n\n**-p, --password PASSWORD**\n> Password or NTLM hash for authentication\n\n**-r, --resolve**\n> Resolve computer hostnames via DNS\n\n**-n, --dns-server IP**\n> DNS server for hostname resolution\n\n**-o, --outdir PATH**\n> Output directory for dump files\n\n**--no-json**\n> Skip JSON output generation\n\n**--no-html**\n> Skip HTML output generation\n\n**--no-grep**\n> Skip greppable output generation"],["CAVEATS","Requires valid domain credentials. Designed for authorized security assessments and penetration testing only."],["INSTALL","```nix: nix profile install nixpkgs#ldapdomaindump```"],["SEE ALSO","[ldapsearch](/man/ldapsearch)(1), [bloodhound](/man/bloodhound)(1)"]],"e":["ldapsearch","bloodhound"],"x":"ldapdomaindump -u domain\\username -p password [hostname]"},"loc":{"n":"loc","t":"counts lines of code quickly","s":[["TAGLINE","counts lines of code quickly"],["TLDR","**Count lines of code in the current directory**\n\n```loc```\n\n**Count lines of code in a specific directory**\n\n```loc [path/to/directory]```\n\n**Show per-file statistics**\n\n```loc --files```\n\n**Exclude files matching a regex pattern**\n\n```loc --exclude [test]```\n\n**Count only files matching a regex pattern**\n\n```loc --include [\\.rs$]```\n\n**Sort output by a specific column**\n\n```loc --sort [code]```\n\n**Include files ignored by .gitignore**\n\n```loc -u```\n\n**Include hidden files and directories as well**\n\n```loc -uu```"],["SYNOPSIS","**loc** [_options_] [_paths_]"],["PARAMETERS","_PATHS_\n> Directories or files to analyze. Defaults to the current directory.\n\n**--files**\n> Display statistics for each individual file parsed.\n\n**--sort** _COLUMN_\n> Sort results by the specified column (e.g., code, comment, lines, blank). Default is code in descending order.\n\n**--include** _REGEX_\n> Count only files matching the specified regex pattern.\n\n**--exclude** _REGEX_\n> Exclude files matching the specified regex pattern.\n\n**-u**\n> Unrestricted mode. Disregard .gitignore and .ignore files. Use twice (-uu) to also include hidden files and directories."],["DESCRIPTION","**loc** counts lines of code quickly. It identifies programming languages and separates code, comments, and blank lines. By default, it respects .gitignore and .ignore files and skips hidden files and directories.\n\nThe tool is written in Rust for speed and can process large codebases significantly faster than alternatives like cloc."],["CAVEATS","Results may differ from other line counters due to differing language detection heuristics and comment-parsing rules. The project is no longer actively maintained."],["INSTALL","```brew: brew install loc```"],["SEE ALSO","[cloc](/man/cloc)(1), [tokei](/man/tokei)(1), [sloccount](/man/sloccount)(1)"]],"e":["cloc","tokei","sloccount"],"x":"loc"},"macchanger":{"n":"macchanger","t":"manipulates the MAC address of network interfaces","s":[["TAGLINE","manipulates the MAC address of network interfaces"],["TLDR","**Show** current and permanent MAC addresses\n\n```macchanger -s [eth0]```\n\nSet **random** MAC address\n\n```sudo macchanger -r [eth0]```\n\nSet random MAC with **burned-in address** flag\n\n```sudo macchanger -r -b [eth0]```\n\nSet a **specific** MAC address\n\n```sudo macchanger -m [XX:XX:XX:XX:XX:XX] [eth0]```\n\nSet a random MAC from the **same vendor**\n\n```sudo macchanger -a [eth0]```\n\n**Reset** to permanent hardware MAC\n\n```sudo macchanger -p [eth0]```\n\nList known **vendor** MAC prefixes\n\n```macchanger -l```"],["SYNOPSIS","**macchanger** [_OPTIONS_] _interface_"],["DESCRIPTION","**macchanger** manipulates the MAC address of network interfaces. It can set random addresses, vendor-specific addresses, or restore the original hardware address. Useful for privacy, testing, and network troubleshooting."],["PARAMETERS","**-s, --show**\n> Show current and permanent MAC addresses\n\n**-r, --random**\n> Set a completely random MAC address\n\n**-b, --bia**\n> Pretend to be a burned-in address (vendor bit)\n\n**-m, --mac** _address_\n> Set a specific MAC address\n\n**-p, --permanent**\n> Reset to the permanent hardware MAC\n\n**-l, --list**\n> Print known vendor MAC prefixes\n\n**-e, --ending**\n> Only change the last bytes, preserving the vendor prefix\n\n**-a, --another**\n> Set random vendor MAC of same kind\n\n**-A**\n> Set random vendor MAC of any kind"],["CAVEATS","The interface must be down before changing MAC on most systems. Changes are not persistent across reboots. Some networks detect and block MAC address changes."],["HISTORY","**macchanger** was written by **Alvaro Lopez Ortega** and is commonly used for privacy and network testing on Linux systems."],["INSTALL","```apt: sudo apt install macchanger```\n\n```dnf: sudo dnf install macchanger```\n\n```pacman: sudo pacman -S macchanger```\n\n```apk: sudo apk add macchanger```\n\n```zypper: sudo zypper install macchanger```\n\n```brew: brew install macchanger```\n\n```nix: nix profile install nixpkgs#macchanger```"],["SEE ALSO","[ip-link](/man/ip-link)(8), [ifconfig](/man/ifconfig)(8)"]],"e":["ip-link","ifconfig"],"x":"macchanger -s [eth0]"},"mcpsnoop":{"n":"mcpsnoop","t":"Wireshark for MCP – transparent proxy and live TUI for AI client/server traffic","s":[["TAGLINE","Wireshark for MCP – transparent proxy and live TUI for AI client/server traffic"],["TLDR","**Wrap a server** to inspect live MCP (Model Context Protocol) JSON-RPC traffic\n\n```mcpsnoop -- node build/index.js```\n\n**Run the demo**\n\n```mcpsnoop demo```\n\n**Proxy an HTTP MCP server**\n\n```mcpsnoop http --target http://localhost:3000/mcp --listen :7000```\n\n**Start the TUI** (pairs automatically with wrapped servers)\n\n```mcpsnoop```"],["SYNOPSIS","**mcpsnoop** [--] [_server_ _args_...]\n\n**mcpsnoop** http [--target _url_] [--listen _addr_]\n\n**mcpsnoop** demo\n\n**mcpsnoop** export|open|remote|help ..."],["DESCRIPTION","mcpsnoop is a transparent proxy and interactive terminal UI for debugging traffic between AI clients (Claude Desktop, Cursor, Claude Code, etc.) and MCP servers. Unlike the official MCP Inspector, which connects as a separate client, mcpsnoop sits in the actual data path so it sees the real calls made by your client.\n\nIt consists of two cooperating roles in one binary:\n\n- The shim (`mcpsnoop -- <server command>`) that your client spawns. It forwards bytes verbatim while capturing every JSON-RPC frame.\n- The hub/TUI (`mcpsnoop` with no arguments) that receives frames and provides a live, filterable, replay-capable interface.\n\nThe two halves discover each other automatically via a well-known socket and on-disk logs; order of starting does not matter."],["FEATURES","- Live colour-coded JSON-RPC stream (requests, responses, notifications, server stderr)\n- Hung-call detection with live timers\n- Capability inspector\n- Frame inspector with search\n- Replay of any captured tool call against a fresh server copy\n- Powerful filter query language (`tool:`, `status:`, `dir:`, etc.)\n- Session export (json / html / text / otlp)\n- Single static binary, zero runtime dependencies"],["INSTALL","```bash\ngo install github.com/kerlenton/mcpsnoop/cmd/mcpsnoop@latest\n```\n\nOr via Homebrew:\n\n```bash\nbrew install kerlenton/mcpsnoop/mcpsnoop\n```\n\nPrebuilt binaries are available from the Releases page."],["PARAMETERS","**--**\n> End flag processing; everything after is the wrapped server command and its arguments.\n\nFor the HTTP reverse-proxy mode:\n\n**--target** _url_\n> Target HTTP MCP endpoint.\n\n**--listen** _addr_\n> Address to listen on (e.g. `:7000`).\n\nOptional project config may live in `.mcpsnoop.toml` (cwd only): `label`, `trace-file`, `redact-secrets`, `redact-key`, `no-trace`. CLI flags override the file."],["CAVEATS","Only wrap servers you trust. mcpsnoop executes the command you configure. Captured frames can include prompts, credentials, and tool results — use redaction flags when needed. The tool is pre-1.0; behaviour may change in minor releases."],["RESOURCES","```[Source code](https://github.com/kerlenton/mcpsnoop)```"]],"e":[],"x":"mcpsnoop -- node build/index.js"},"millwright":{"n":"millwright","t":"Self-hosted LLM router with policy and spend control","s":[["TAGLINE","Self-hosted LLM router with policy and spend control"],["TLDR","**Initialize** providers, model roles, and API keys\n\n```millwright init```\n\n**Start** the router gateway\n\n```millwright serve```\n\n**Show spend** summary and top teams\n\n```millwright spend```\n\n**Show** model and provider mix\n\n```millwright models```\n\n**Explain** a routing decision by trace id\n\n```millwright trace [trace-id]```\n\n**Live spend** and model activity in the terminal\n\n```millwright top```\n\n**Analyze** cost from a live gateway\n\n```millwright analyze --url [http://127.0.0.1:8080] --key [$MILLWRIGHT_OPERATOR_KEY] --since [30d] --report [analysis.html]```\n\n**Print** version and build metadata\n\n```millwright version```"],["SYNOPSIS","**millwright** _command_ [_options_]"],["DESCRIPTION","**millwright** is a self-hosted LLM router that sits between AI applications and model providers. It accepts OpenAI Chat Completions and Anthropic Messages, then routes each request to an OpenAI-compatible API, Anthropic, or Amazon Bedrock.\n\nYou assign models to **cheap**, **mid**, and **frontier** roles. Millwright picks the lowest estimated-cost healthy route the resolved role allows. With a session ID it keeps independent role-scoped affinity lanes so concurrent cheap/mid/frontier traffic can reuse warm prompt caches without serializing agent work.\n\nIt is a router, not an agent orchestrator: it does not spawn agents, schedule work, inspect prompts, or rewrite context. Routing decisions are inspectable via response headers, traces, and an optional usage ledger (SQLite by default; PostgreSQL for production).\n\nShips as one Rust binary with Docker Compose support. Provider credentials stay in environment variables; separate workload and operator keys control inference and management access."],["PARAMETERS","**init**\n> Interactive (or flag-driven) setup for providers, roles, pricing, and keys. Writes policy/models config and env references (not raw provider secrets). Common flags: **--non-interactive**, **--provider**, **--cheap**, **--mid**, **--frontier**, **--cache-ttl**, **--team**, **--force**.\n\n**serve**\n> Run the router HTTP gateway (default local URL **http://localhost:8080**).\n\n**spend** [**--url** _url_] [**--key** _key_] [**--json**]\n> Total spend, cache read rate, and per-team breakdown from the management API.\n\n**models** [**--url** _url_] [**--key** _key_] [**--json**]\n> Model and provider mix from the ledger.\n\n**trace** _trace-id_ [**--url** _url_] [**--key** _key_] [**--json**]\n> Explain one routing decision (including rejected alternatives). Trace ids appear in **x-millwright-trace-id**.\n\n**top** [**--url** _url_] [**--key** _key_]\n> Live terminal view of spend and model activity.\n\n**analyze** [_options_]\n> Analyze live ledger or local JSONL cost data. Produces HTML/Markdown reports and schema-versioned JSON. Key options: **--url**, **--key**, **--input**, **--input-format**, **--since**, **--until**, **--catalog**, **--candidates**, **--report**, **--json-out**.\n\n**healthcheck**\n> Probe the local gateway for container health checks.\n\n**version**\n> Print version and build metadata (also **--version** / **-v**).\n\nGateway client defaults: **--url** falls back to **MILLWRIGHT_URL** or **http://localhost:8080**; **--key** falls back to **MILLWRIGHT_OPERATOR_KEY**, then **MILLWRIGHT_API_KEY**."],["CAVEATS","Build from source requires a recent Rust toolchain (project pins **rustc** 1.97+). Provider API keys and operator keys must be configured before useful routing; `millwright init` stores env var *names* for secrets, not the secrets themselves. Cross-protocol translation supports a documented text-and-tools subset and rejects unsupported shapes rather than dropping data silently."],["HISTORY","**Millwright** is an Apache-2.0 open-source project by Northwood Systems. Version **0.1.0** focuses on deterministic role-based routing, cache-aware affinity, and local spend visibility for self-hosted LLM gateways."],["SEE ALSO","[curl](/man/curl)(1), [docker](/man/docker)(1), [docker-compose](/man/docker-compose)(1), [ollama](/man/ollama)(1), [caddy](/man/caddy)(1)"],["RESOURCES","```[Source code](https://github.com/Northwood-Systems/millwright)```\n\n```[Homepage](https://northwoodsystems.ai/research/projects/millwright/docs)```\n\n```[Documentation](https://github.com/Northwood-Systems/millwright/tree/main/docs)```"]],"e":["curl","docker","docker-compose","ollama","caddy"],"x":"millwright init"},"mux":{"n":"mux","t":"part of the Interception Tools suite for intercepting and multiplexing streams","s":[["TAGLINE","part of the Interception Tools suite for intercepting and multiplexing streams"],["TLDR","**Create** a new muxer with specified name\n\n```mux -c [muxer_name1] [muxer_name2]```\n\nSet muxer's **internal queue size**\n\n```mux -s [100]```\n\n**Read input** from a named muxer\n\n```mux -i [input_muxer_name]```\n\n**Write output** to a named muxer\n\n```mux -o [output_muxer_name]```"],["SYNOPSIS","**mux** [**-h** | [**-s** _size_] **-c** _name_ | [**-i** _name_] [**-o** _name_]]"],["PARAMETERS","**-h**\n> Show help message and exit\n\n**-c _name_**\n> Create a muxer with specified name (repeatable)\n\n**-s _size_**\n> Set muxer queue size; default is 100\n\n**-i _name_**\n> Read input from named muxer; can be repeated for switch mode\n\n**-o _name_**\n> Write output to named muxer; can be repeated"],["DESCRIPTION","**mux** is part of the Interception Tools suite for multiplexing streams of input events. A muxer must be created first with **-c**, then used as the input or output of a given pipeline.\n\nBesides combining multiple pipelines into one, mux can duplicate them (using multiple **-o** options) and act as a switch based on activity in other pipelines (using multiple **-i** options).\n\nThe tool is commonly used with udevmon and other Interception Tools components for keyboard remapping, event filtering, and input device manipulation at a low level."],["CAVEATS","Requires the Interception Tools framework to be installed and configured. Named muxers are created as FIFO files. Queue size affects latency and memory usage. Typically requires root privileges or uinput group membership."],["HISTORY","**mux** is part of **Interception Tools**, developed by Francisco Lopes (oblitum). The framework provides low-level input event interception on Linux, enabling tools like caps2esc and space2meta for keyboard customization."],["INSTALL","```apt: sudo apt install interception-tools```\n\n```pacman: sudo pacman -S interception-tools```\n\n```nix: nix profile install nixpkgs#interception-tools```"],["SEE ALSO","[intercept](/man/intercept)(1), [udevmon](/man/udevmon)(1), [uinput](/man/uinput)(1)"]],"e":["intercept","udevmon","uinput"],"x":"mux -c [muxer_name1] [muxer_name2]"},"nano-ffmpeg":{"n":"nano-ffmpeg","t":"Keyboard-driven terminal UI wrapping ffmpeg","s":[["TAGLINE","Keyboard-driven terminal UI wrapping ffmpeg"],["TLDR","**Launch the** interactive TUI in the current directory\n\n```nano-ffmpeg```\n\n**Open the file picker** at a specific directory\n\n```nano-ffmpeg -d [path/to/videos]```\n\n**Skip the picker** and jump straight to operations with a preloaded file\n\n```nano-ffmpeg -d [path/to/video.mp4]```\n\n**Force** a specific UI theme for the session\n\n```nano-ffmpeg -t [dark|light]```\n\n**Show** the version\n\n```nano-ffmpeg --version```"],["SYNOPSIS","**nano-ffmpeg** [_-t theme_] [_-d path_] [_--version_] [_--help_]"],["PARAMETERS","**-d** _path_, **--dir** _path_\n> If _path_ is a directory, open the file picker rooted there. If _path_ is a file, bypass the picker and load it directly into the operations menu.\n\n**-t** _theme_, **--theme** _theme_\n> Override the UI theme for the current session. Accepts **dark** or **light**.\n\n**--version**\n> Print the version and exit.\n\n**-h**, **--help**\n> Print usage information."],["DESCRIPTION","**nano-ffmpeg** is a terminal user interface that wraps **ffmpeg** behind a keyboard-driven dashboard. Instead of memorizing flags, the user browses files, picks an operation from a menu, tweaks parameters via presets, and watches a live progress bar while ffmpeg encodes in the background.\n\nThe TUI exposes twelve common operations: format conversion, audio extraction, resizing, trimming, compression, concatenation, subtitle handling, GIF creation, thumbnail extraction, watermarking, audio adjustments, and video filters such as stabilization, deinterlacing, and rotation. Every settings screen previews the exact **ffmpeg** invocation that will be executed, so users can learn the underlying flags as they work.\n\nHardware-accelerated encoders are detected automatically: **VideoToolbox** on macOS, **NVENC** on systems with NVIDIA GPUs, and **VAAPI** on Linux. Real-time stats during encoding include elapsed time, ETA, speed, FPS, bitrate, frames written, and output size."],["CAVEATS","Requires **ffmpeg** and **ffprobe** to be installed and discoverable in **PATH**; nano-ffmpeg only orchestrates them. A terminal of at least 80x24 cells is required. Hardware encoder availability depends on platform, drivers, and ffmpeg build options."],["HISTORY","**nano-ffmpeg** is an open-source project written in **Go** by **dgr8akki**, distributed via Homebrew, Scoop, **go install**, and prebuilt binaries on GitHub. It was created to lower the barrier to entry for ffmpeg by exposing a guided, discoverable workflow without sacrificing access to the underlying command."],["INSTALL","```aur: yay -S nano-ffmpeg```"],["SEE ALSO","[ffmpeg](/man/ffmpeg)(1), [ffprobe](/man/ffprobe)(1)"]],"e":["ffmpeg","ffprobe"],"x":"nano-ffmpeg"},"nbbattleship":{"n":"nbbattleship","t":"terminal battleship naval combat game","s":[["TAGLINE","terminal battleship naval combat game"],["TLDR","**Play battleship**\n\n```nbbattleship```"],["SYNOPSIS","**nbbattleship**"],["PREVIEW","```\n   A B C D E F\n 1 ~ ~ X ~ ~ ~\n 2 ~ ~ ~ ~ O ~\n 3 ~ O ~ ~ ~ ~\n 4 ~ ~ ~ ~ ~ ~\n```"],["DESCRIPTION","**nbbattleship** is a terminal-based battleship game from the **nbsdgames** collection. Players place ships on a grid and then take turns guessing the positions of the opponent's ships. Hit all segments of a ship to sink it; sink all ships to win.\n\nThe game uses an ncurses interface. Use arrow keys or hjkl to move, ENTER to shoot, R to rotate ships during placement, q to quit. Mouse input is also supported."],["CAVEATS","Requires a terminal with ncurses support. Part of the nbsdgames collection. No command-line options are available."],["HISTORY","**nbbattleship** is part of **nbsdgames**, a package of 18 text-based terminal games created by **abakh**. It implements the classic **Battleship** game, a strategy guessing game that originated as a pencil-and-paper game in the **1930s**."],["SEE ALSO","[nbmines](/man/nbmines)(1), [nbsos](/man/nbsos)(1), [nbcheckers](/man/nbcheckers)(1)"]],"e":["nbmines","nbsos","nbcheckers"],"x":"nbbattleship"},"opt":{"n":"opt","t":"LLVM optimizer","s":[["TAGLINE","LLVM optimizer"],["TLDR","**Optimize LLVM bitcode**\n\n```opt [input.bc] -o [output.bc]```\n\n**Run specific pass**\n\n```opt -mem2reg [input.bc] -o [output.bc]```\n\n**Enable all optimizations**\n\n```opt -O3 [input.bc] -o [output.bc]```\n\n**Print pass statistics**\n\n```opt -stats [input.bc] -o [output.bc]```\n\n**List available passes**\n\n```opt --print-passes```"],["SYNOPSIS","**opt** [_options_] [_input_]"],["PARAMETERS","_INPUT_\n> LLVM bitcode file.\n\n**-o** _OUTPUT_\n> Output file.\n\n**-O**_LEVEL_\n> Optimization level (0-3).\n\n**--print-passes**\n> List passes.\n\n**-stats**\n> Print statistics.\n\n**--help**\n> Display help information."],["DESCRIPTION","**opt** is the LLVM optimizer. Runs optimization passes on bitcode.\n\nThe tool transforms LLVM intermediate representation. Part of LLVM toolchain."],["CAVEATS","Part of LLVM. Works with bitcode only. Pass names version-specific."],["HISTORY","opt was developed as part of the **LLVM compiler infrastructure** project."],["INSTALL","```apt: sudo apt install llvm```\n\n```dnf: sudo dnf install llvm```\n\n```pacman: sudo pacman -S llvm```\n\n```apk: sudo apk add llvm```\n\n```zypper: sudo zypper install llvm```\n\n```brew: brew install llvm```\n\n```nix: nix profile install nixpkgs#llvm```"],["SEE ALSO","[llvm-as](/man/llvm-as)(1), [llc](/man/llc)(1), [clang](/man/clang)(1)"]],"e":["llvm-as","llc","clang"],"x":"opt [input.bc] -o [output.bc]"},"p0f":{"n":"p0f","t":"performs passive OS fingerprinting","s":[["TAGLINE","performs passive OS fingerprinting"],["TLDR","**Start fingerprinting**\n\n```p0f -i [eth0]```\n\n**Read from pcap**\n\n```p0f -r [capture.pcap]```\n\n**Write to log file**\n\n```p0f -i [eth0] -o [log.txt]```\n\n**Fingerprint specific port**\n\n```p0f -i [eth0] \"port 80\"```\n\n**Run in background**\n\n```p0f -i [eth0] -d```"],["SYNOPSIS","**p0f** [_options_] [_filter_]"],["PARAMETERS","**-i** _INTERFACE_\n> Listen on the named network interface.\n\n**-r** _FILE_\n> Read packets from a pcap capture file instead of a live interface.\n\n**-o** _FILE_\n> Append fingerprinting results to _FILE_ (text log).\n\n**-w** _FILE_\n> Write captured packets to a new pcap file (similar to **tcpdump -w**).\n\n**-s** _PATH_\n> Listen on _PATH_ as a Unix domain socket for API queries.\n\n**-d**\n> Run as a daemon in the background. Requires **-o** or **-s**.\n\n**-u** _USER_\n> Drop privileges to _USER_ after opening the capture interface.\n\n**-p**\n> Put the interface into promiscuous mode.\n\n**-f** _FILE_\n> Use _FILE_ as the fingerprint database (default: **/etc/p0f/p0f.fp**).\n\n**-S** _N_\n> Set the max number of concurrent API connections.\n\n**-c** _DIR_\n> Restrict output by chrooting to _DIR_.\n\n**-L**\n> List available capture interfaces and exit.\n\n**--help**\n> Display help information."],["DESCRIPTION","**p0f** is a passive OS, application, and link-type fingerprinter. It listens to TCP/IP traffic without sending any packets and infers the remote operating system, MTU/uplink, NAT presence, and approximate uptime from quirks of the SYN, SYN+ACK, and HTTP traffic it observes.\n\nBecause it never probes the target, **p0f** is invisible on the wire and well-suited to forensic analysis of pcaps, fingerprinting visitors of a public-facing service, and detecting policy violations such as un-NATted devices behind a firewall."],["CAVEATS","Needs raw socket / **CAP_NET_RAW** access (or root). Detection quality depends on having a current **p0f.fp** signature database. **p0f v3** is a rewrite that does not read v2 fingerprint files; ensure the bundled fingerprints match the binary version."],["HISTORY","p0f was created by **Michal Zalewski** for passive TCP/IP stack fingerprinting."],["INSTALL","```apt: sudo apt install p0f```\n\n```dnf: sudo dnf install p0f```\n\n```pacman: sudo pacman -S p0f```\n\n```zypper: sudo zypper install p0f```\n\n```brew: brew install p0f```\n\n```nix: nix profile install nixpkgs#p0f```"],["SEE ALSO","[nmap](/man/nmap)(1), [tcpdump](/man/tcpdump)(1), [wireshark](/man/wireshark)(1)"]],"e":["nmap","tcpdump","wireshark"],"x":"p0f -i [eth0]"},"pct-move_volume":{"n":"pct-move_volume","t":"Alternative form of pct move-volume that moves container volumes","s":[["TAGLINE","Alternative form of pct move-volume that moves container volumes"],["TLDR","**Move a container volume** to a different storage\n\n```pct move_volume [100] [rootfs] [local-lvm]```\n\n**Move a volume** to a different container\n\n```pct move_volume [100] [mp0] --target-vmid [200] --target-volume [mp1]```\n\n**Move and delete** the original volume after copy\n\n```pct move_volume [100] [mp0] [local-zfs] --delete 1```\n\n**Move with I/O** bandwidth limit\n\n```pct move_volume [100] [rootfs] [local-lvm] --bwlimit [10240]```"],["SYNOPSIS","**pct move_volume** _vmid_ _volume_ [_storage_] [_options_]"],["PARAMETERS","_vmid_\n> Container ID (100-999999999).\n\n_volume_\n> Volume to move (rootfs, mp0-mp255, or unused0-unused255).\n\n_storage_\n> Target storage identifier.\n\n**--bwlimit** _number_\n> Override I/O bandwidth limit in KiB/s (default: from datacenter or storage config).\n\n**--delete** _boolean_\n> Delete the original volume after successful copy. By default the original is kept as an unused volume entry (default: 0).\n\n**--digest** _string_\n> Prevent changes if the current configuration file has a different SHA1 digest.\n\n**--target-digest** _string_\n> Prevent changes if the target container configuration file has a different SHA1 digest.\n\n**--target-vmid** _vmid_\n> Target container ID (for moving a volume to a different container).\n\n**--target-volume** _volume_\n> Target volume key (defaults to the source volume key)."],["DESCRIPTION","**pct move_volume** is an alternative form of **pct move-volume** that moves container volumes to different storage backends in Proxmox VE. The underscore variant is provided for compatibility. It can move volumes between storages within the same container or to a different container entirely."],["SEE ALSO","[pct](/man/pct)(1), [pct-move-volume](/man/pct-move-volume)(1), [pvesm](/man/pvesm)(1)"]],"e":["pct","pct-move-volume","pvesm"],"x":"pct move_volume [100] [rootfs] [local-lvm]"},"pdf2svg":{"n":"pdf2svg","t":"converts PDF files to Scalable Vector Graphics format","s":[["TAGLINE","converts PDF files to Scalable Vector Graphics format"],["TLDR","**Convert PDF page to SVG**\n\n```pdf2svg [input.pdf] [output.svg]```\n\n**Convert specific page**\n\n```pdf2svg [input.pdf] [output.svg] [2]```\n\n**Convert all pages**\n\n```pdf2svg [input.pdf] [output_%d.svg] all```"],["SYNOPSIS","**pdf2svg** _input.pdf_ _output.svg_ [_page_|all]"],["PARAMETERS","**input.pdf**\n> Input PDF file.\n\n**output.svg**\n> Output SVG file.\n\n**page**\n> Page number (1-indexed).\n\n**all**\n> Convert all pages."],["DESCRIPTION","**pdf2svg** converts PDF files to Scalable Vector Graphics (SVG) format. It preserves vector graphics, text, and formatting, making the output suitable for web use and editing.\n\nThe tool uses **Poppler** for PDF parsing and **Cairo** for SVG rendering. To convert every page in a single run, use **all** as the page argument and include the **%d** placeholder in the output filename — pdf2svg substitutes the page number at write time (e.g. `pdf2svg book.pdf page_%d.svg all` produces `page_1.svg`, `page_2.svg`, …)."],["CAVEATS","One page per SVG file (no multi-page SVG output). Complex PDFs may produce very large SVGs. Fonts are typically converted to outline paths, which preserves rendering fidelity but loses selectable text."],["HISTORY","pdf2svg was created by **David Barton** using Poppler for PDF parsing and Cairo for SVG rendering."],["INSTALL","```apt: sudo apt install pdf2svg```\n\n```dnf: sudo dnf install pdf2svg```\n\n```pacman: sudo pacman -S pdf2svg```\n\n```zypper: sudo zypper install pdf2svg```\n\n```brew: brew install pdf2svg```\n\n```nix: nix profile install nixpkgs#pdf2svg```"],["SEE ALSO","[inkscape](/man/inkscape)(1), [pdftohtml](/man/pdftohtml)(1), [rsvg-convert](/man/rsvg-convert)(1)"]],"e":["inkscape","pdftohtml","rsvg-convert"],"x":"pdf2svg [input.pdf] [output.svg]"},"pecl":{"n":"pecl","t":"PHP Extension Community Library manager","s":[["TAGLINE","PHP Extension Community Library manager"],["TLDR","**Install extension**\n\n```pecl install [extension]```\n\n**Search extensions**\n\n```pecl search [query]```\n\n**List installed**\n\n```pecl list```\n\n**Uninstall extension**\n\n```pecl uninstall [extension]```\n\n**Upgrade extension**\n\n```pecl upgrade [extension]```\n\n**Show extension info**\n\n```pecl info [extension]```"],["SYNOPSIS","**pecl** [_command_] [_options_] [_package_]"],["PARAMETERS","**install** _PACKAGE_\n> Install extension.\n\n**uninstall** _PACKAGE_\n> Remove extension.\n\n**search** _QUERY_\n> Search packages.\n\n**list**\n> List installed.\n\n**upgrade** _PACKAGE_\n> Upgrade extension.\n\n**info** _PACKAGE_\n> Show package info."],["DESCRIPTION","**pecl** is PHP Extension Community Library manager. Installs PHP extensions.\n\nThe tool manages compiled extensions. Part of PEAR."],["CAVEATS","Requires PHP development tools. May need root. Compiler required."],["HISTORY","PECL was created for **distributing PHP extensions** separately from core."],["INSTALL","```apk: sudo apk add php85-pear```"],["SEE ALSO","[pear](/man/pear)(1), [php](/man/php)(1), [phpize](/man/phpize)(1)"]],"e":["pear","php","phpize"],"x":"pecl install [extension]"},"pgmtopbm":{"n":"pgmtopbm","t":"converts PGM grayscale images to PBM bitmap","s":[["TAGLINE","converts PGM grayscale images to PBM bitmap"],["TLDR","**Convert grayscale to bitmap using default Floyd-Steinberg dithering**\n\n```pgmtopbm [input.pgm] > [output.pbm]```\n\n**Convert with explicit Floyd-Steinberg dithering**\n\n```pgmtopbm -fs [input.pgm] > [output.pbm]```\n\n**Convert with simple threshold**\n\n```pgmtopbm -threshold -value [0.5] [input.pgm] > [output.pbm]```\n\n**Convert with clustered dithering**\n\n```pgmtopbm -cluster3 [input.pgm] > [output.pbm]```"],["SYNOPSIS","**pgmtopbm** [**-floyd** | **-fs** | **-threshold** | **-hilbert** | **-dither8** | **-d8** | **-cluster3** | **-c3** | **-cluster4** | **-c4** | **-cluster8** | **-c8**] [**-value** _val_] [**-clump** _size_] [**-randomseed** _integer_] [_pgmfile_]"],["PARAMETERS","**-floyd**, **-fs**\n> Floyd-Steinberg dithering (default).\n\n**-threshold**\n> Simple threshold conversion. Pixels above the threshold become white; below become black.\n\n**-value** _val_\n> Threshold value between 0.0 and 1.0. Default is 0.5. Used with **-threshold**.\n\n**-hilbert**\n> Hilbert curve dithering.\n\n**-dither8**, **-d8**\n> Ordered dithering using an 8x8 matrix.\n\n**-cluster3**, **-c3**\n> Clustered dithering using a 3x3 pattern.\n\n**-cluster4**, **-c4**\n> Clustered dithering using a 4x4 pattern.\n\n**-cluster8**, **-c8**\n> Clustered dithering using an 8x8 pattern.\n\n**-clump** _size_\n> Clump size for Hilbert curve dithering.\n\n**-randomseed** _integer_\n> Seed for randomization to produce reproducible results."],["DESCRIPTION","**pgmtopbm** converts PGM grayscale images to PBM bitmap format. It uses dithering or thresholding to create 1-bit output from grayscale input. Part of the Netpbm toolkit. This tool is largely obsolete since Netpbm 10.23 (July 2004); [pamditherbw](/man/pamditherbw)(1) is the recommended replacement with better gamma handling."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pbmtopgm](/man/pbmtopgm)(1), [pamditherbw](/man/pamditherbw)(1), [pgmtoppm](/man/pgmtoppm)(1)"]],"e":["pbmtopgm","pamditherbw","pgmtoppm"],"x":"pgmtopbm [input.pgm] > [output.pbm]"},"pipewire-aes67":{"n":"pipewire-aes67","t":"PipeWire daemon variant for AES67 audio-over-IP networking","s":[["TAGLINE","PipeWire daemon variant for AES67 audio-over-IP networking"],["TLDR","**Start the AES67 PipeWire daemon** with the default configuration\n\n```pipewire-aes67```\n\n**Use a custom configuration file**\n\n```pipewire-aes67 -c [path/to/pipewire-aes67.conf]```\n\n**Increase log verbosity**\n\n```pipewire-aes67 -v```\n\n**Show version information**\n\n```pipewire-aes67 --version```"],["SYNOPSIS","**pipewire-aes67** [_options_]"],["PARAMETERS","**-h**, **--help**\n> Show help text and exit.\n\n**-v**, **--verbose**\n> Increase logging verbosity (may be repeated, e.g. **-vvv**).\n\n**--version**\n> Show version information and exit.\n\n**-c** _FILE_, **--config**=_FILE_\n> Load the given configuration file (default **pipewire-aes67.conf**).\n\n**-P** _PROPS_, **--properties**=_PROPS_\n> Add JSON properties to the daemon context."],["DESCRIPTION","**pipewire-aes67** is a PipeWire daemon launched with an **AES67**-tailored configuration. AES67 is an open audio-over-IP interoperability standard (used by systems such as Dante and RAVENNA) that defines low-latency, uncompressed PCM audio transport over standard Ethernet using **RTP**, **PTP** for clock sync, and **SAP/SDP** for stream announcements.\n\nIn its default configuration the daemon configures a Precision Time Protocol (PTP) hardware clock node, multicast RTP transmitters and receivers, and SAP announcement modules, exposing AES67 streams as native PipeWire devices. Existing audio applications using ALSA, PulseAudio, or JACK clients can then send to or receive from AES67 endpoints transparently.\n\nThe binary is typically a symlink to **pipewire**(1); the program selects its default configuration based on **argv[0]**."],["CONFIGURATION","The default configuration is **/usr/share/pipewire/pipewire-aes67.conf**. Copy it to **/etc/pipewire/** for system-wide changes or to **~/.config/pipewire/** for per-user changes. Drop-in fragments may also be placed under a matching **pipewire-aes67.conf.d/** directory.\n\nKey items to configure include the PTP clock interface or device, multicast network interface, sample rate, channel count, and per-stream RTP/SAP parameters."],["CAVEATS","Requires a working **PTP** time source on the local network and a network interface that supports multicast and (ideally) hardware timestamping. Verified interoperable with Dante and RAVENNA from PipeWire **1.1.0** onwards. Firewall rules must allow the relevant multicast and PTP traffic."],["HISTORY","AES67 support was added to **PipeWire** as part of its expanding professional audio capabilities, with full Dante and RAVENNA interoperability landing in PipeWire **1.1.0** (2024). PipeWire itself was created by **Wim Taymans** at Red Hat and first released in **2017**."],["INSTALL","```apt: sudo apt install pipewire-bin```\n\n```dnf: sudo dnf install pipewire```\n\n```pacman: sudo pacman -S pipewire```\n\n```apk: sudo apk add pipewire```\n\n```zypper: sudo zypper install pipewire```\n\n```brew: brew install pipewire```\n\n```nix: nix profile install nixpkgs#pipewire```"],["SEE ALSO","[pipewire](/man/pipewire)(1), [pipewire-avb](/man/pipewire-avb)(1), [pipewire-pulse](/man/pipewire-pulse)(1), [pw-cli](/man/pw-cli)(1), [pw-dump](/man/pw-dump)(1)"]],"e":["pipewire","pipewire-avb","pipewire-pulse","pw-cli","pw-dump"],"x":"pipewire-aes67"},"pkgtool":{"n":"pkgtool","t":"Interactive Slackware package management tool","s":[["TAGLINE","Interactive Slackware package management tool"],["TLDR","Launch **interactive** package tool\n\n```sudo pkgtool```\n\n**Remove** packages interactively\n\n```sudo pkgtool --remove_menu```\n\n**View** installed packages\n\n```pkgtool --view_menu```\n\n**Install** packages from current directory\n\n```sudo pkgtool --install_menu```\n\n**Setup** packages (run installation scripts)\n\n```sudo pkgtool --setup_menu```"],["SYNOPSIS","**pkgtool** [_options_]"],["DESCRIPTION","**pkgtool** is an interactive menu-driven tool for managing Slackware packages. It provides a ncurses-based interface for installing, removing, and viewing packages."],["PARAMETERS","**--remove_menu**\n> Open the remove packages menu\n\n**--view_menu**\n> Open the view installed packages menu\n\n**--install_menu**\n> Open the install packages menu (from current directory)\n\n**--setup_menu**\n> Open the setup menu (runs doinst.sh scripts)"],["CAVEATS","For command-line package management, use installpkg, removepkg, and upgradepkg directly. pkgtool requires a terminal capable of displaying ncurses dialogs."],["HISTORY","**pkgtool** is part of the Slackware package management system, providing a user-friendly interface for managing packages on Slackware Linux."],["SEE ALSO","[installpkg](/man/installpkg)(8), [removepkg](/man/removepkg)(8), [upgradepkg](/man/upgradepkg)(8), [makepkg](/man/makepkg)(8)"]],"e":["installpkg","removepkg","upgradepkg","makepkg"],"x":"sudo pkgtool"},"pkgtop":{"n":"pkgtop","t":"Interactive package manager and resource monitor for GNU/Linux","s":[["TAGLINE","Interactive package manager and resource monitor for GNU/Linux"],["TLDR","**Launch the package dashboard**\n\n```pkgtop```\n\n**List packages alphabetically**\n\n```pkgtop -a```"],["SYNOPSIS","**pkgtop** [_options_]"],["PARAMETERS","**-a**\n> List packages alphabetically instead of by size.\n\n**-h**, **--help**\n> Display help information.\n\n**-v**, **--version**\n> Display version information."],["DESCRIPTION","**pkgtop** is an interactive terminal dashboard for managing packages and monitoring system resources on GNU/Linux. It lists installed packages by size, shows package information, and allows installing, upgrading, removing, and searching packages. The dashboard also displays disk usage and general system information."],["CAVEATS","Linux only. Supports Arch, Debian, Ubuntu, Fedora, CentOS, openSUSE, Void, Gentoo, NixOS, Guix, and their derivatives."],["HISTORY","**pkgtop** was created by **Orhun Parmaksiz** (orhun) and is written in **Go**."],["INSTALL","```nix: nix profile install nixpkgs#pkgtop```"],["SEE ALSO","[apt](/man/apt)(8), [pacman](/man/pacman)(8), [dnf](/man/dnf)(8), [htop](/man/htop)(1)"]],"e":["apt","pacman","dnf","htop"],"x":"pkgtop"},"pmb":{"n":"pmb","t":"Local-first persistent memory for AI coding agents over MCP","s":[["TAGLINE","Local-first persistent memory for AI coding agents over MCP"],["TLDR","**Detect your agent and wire the MCP entry**\n\n```pmb setup```\n\n**Show how many memories are stored and where**\n\n```pmb stats```\n\n**Search stored memory for a query**\n\n```pmb recall \"[query]\"```\n\n**Open the interactive terminal UI**\n\n```pmb tui```\n\n**Open the web dashboard (port 8765)**\n\n```pmb dashboard```\n\n**Index a codebase into memory**\n\n```pmb index project [.]```\n\n**Wire a specific agent (Claude Code, Cursor, Codex)**\n\n```pmb connect [claude]```"],["SYNOPSIS","**pmb** _command_ [_arguments_] [_options_]"],["DESCRIPTION","**pmb** provides local-first persistent memory for AI coding agents such as Claude Code, Cursor, and Codex, exposed over the **Model Context Protocol** (MCP). It stores decisions, lessons, and project facts in a single SQLite file on disk together with a local vector store, so it works fully offline with no API keys, accounts, or telemetry.\n\nOnce wired into an agent, pmb automatically injects relevant memory before the agent acts and captures new context through lifecycle hooks. Retrieval is hybrid, combining BM25 keyword search, vector similarity, and a graph of entities, and returns results in tens of milliseconds. The command-line interface drives setup, ingestion (codebases, PDFs, ChatGPT/Claude exports), maintenance, configuration, and inspection of what has been remembered.\n\npmb is installed from PyPI as **pmb-ai**, which provides the **pmb** command, or run directly via **npx pmb-ai**. Each workspace lives under **~/.pmb/<workspace_name>/** and is a self-contained directory that can be copied, version-controlled, or synced between machines."],["COMMANDS","**setup**\n\n> Detect the current agent and wire the MCP server entry.\n\n**connect** _agent_ [**--workspace** _name_]\n\n> Wire a specific agent (Claude Code, Cursor, Codex, and others), optionally pointing at a shared workspace.\n\n**stats**\n\n> Show memory counts and storage information.\n\n**recall** _\"query\"_\n\n> Search stored memory and print results with debug output.\n\n**tui**\n\n> Launch the interactive terminal user interface.\n\n**dashboard**\n\n> Start the web UI on port 8765.\n\n**index** _project|pdf_ _target_\n\n> Scan and embed a codebase, a PDF file, or a directory of PDFs (with **--recurse**).\n\n**import chatgpt** _path_\n\n> Import a ChatGPT or Claude conversation export.\n\n**hooks** _install|list|uninstall_ _agent_\n\n> Manage the lifecycle hooks that auto-capture and restore context.\n\n**config** _list|get|set_ [_key_] [_value_]\n\n> Inspect or change settings.\n\n**forget** _ulid_ [**--hard**]\n\n> Archive a stored fact, or permanently delete it with **--hard**.\n\n**doctor**\n\n> Run a health check on the installation."],["CONFIGURATION","**~/.pmb/<workspace_name>/**\n\n> Per-workspace directory holding the SQLite event database, the LanceDB vector store, and configuration files. Copy or sync this directory to move memory between machines.\n\n**pmb config set** _key_ _value_\n\n> Change one of the documented settings; **pmb config list** shows the common keys and **--pro** reveals the full set."],["CAVEAT","pmb stores everything locally and is offline by default, but memory written by the ambient auto-write layer is captured automatically, so review what is retained on shared or sensitive projects and use **pmb forget** or **pmb forget-auto** to prune it. Hooks integration depends on the host agent supporting MCP lifecycle hooks; **pmb hooks capabilities** shows what each agent supports."],["SEE ALSO","[claude](/man/claude)(1), [aider](/man/aider)(1), [cursor](/man/cursor)(1), [ollama](/man/ollama)(1)"],["RESOURCES","```[Source code](https://github.com/oleksiijko/pmb)```"]],"e":["claude","aider","cursor","ollama"],"x":"pmb setup"},"pnmscale":{"n":"pnmscale","t":"Scale PNM images (obsoleted by pamscale)","s":[["TAGLINE","Scale PNM images (obsoleted by pamscale)"],["TLDR","**Scale by factor**\n\n```pnmscale [0.5] [input.pnm] > [output.pnm]```\n\n**Scale to specific width**\n\n```pnmscale -width [640] [input.pnm] > [output.pnm]```\n\n**Scale to specific dimensions**\n\n```pnmscale -width [800] -height [600] [input.pnm] > [output.pnm]```\n\n**Scale by X and Y factors**\n\n```pnmscale -xscale [2] -yscale [1.5] [input.pnm] > [output.pnm]```"],["SYNOPSIS","**pnmscale** [_options_] [_pnmfile_]"],["PARAMETERS","**-width** _n_\n> Output width.\n\n**-height** _n_\n> Output height.\n\n**-xscale** _n_\n> Horizontal scale factor.\n\n**-yscale** _n_\n> Vertical scale factor.\n\n**-reduce** _n_\n> Reduce by integer factor.\n\n**-xysize** _x_ _y_\n> Fit within dimensions (synonym for -xyfit in pamscale).\n\n**-pixels** _n_\n> Scale to a given total number of pixels."],["DESCRIPTION","**pnmscale** scales PNM images up or down. It uses pixel mixing for smooth results when scaling down. This command was obsoleted by **pamscale** as of Netpbm 10.20 (January 2004) and removed in Netpbm 10.46 (March 2009). Use **pamscale** for new work, which is backward-compatible and supports PAM images.\n\nPart of the Netpbm toolkit."],["EXAMPLES","```bash"],["Half size","pnmscale 0.5 image.ppm > half.ppm"],["Specific width, maintain aspect","pnmscale -width 640 image.ppm > resized.ppm"],["Exact dimensions","pnmscale -width 800 -height 600 image.ppm > exact.ppm"],["Integer reduction (faster)","pnmscale -reduce 4 huge.ppm > quarter.ppm"],["Chain with conversion","jpegtopnm photo.jpg | pnmscale 0.25 | pnmtojpeg > thumb.jpg\n```"],["CAVEATS","Obsoleted by pamscale; use pamscale for new work. Pixel mixing can blur. Integer reduction (-reduce) is faster than fractional scaling."],["HISTORY","pnmscale is part of **Netpbm** by **Jef Poskanzer**, later superseded by pamscale with additional features."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pamscale](/man/pamscale)(1), [pnmenlarge](/man/pnmenlarge)(1), [pnmcut](/man/pnmcut)(1), [netpbm](/man/netpbm)(1)"]],"e":["pamscale","pnmenlarge","pnmcut","netpbm"],"x":"pnmscale [0.5] [input.pnm] > [output.pnm]"},"pwqgen":{"n":"pwqgen","t":"Create secure dictionary-based passphrases","s":[["TAGLINE","Create secure dictionary-based passphrases"],["TLDR","**Generate passphrase**\n\n```pwqgen```\n\n**Generate with specific word count**\n\n```pwqgen random=5```\n\n**Generate multiple**\n\n```pwqgen count=5```"],["SYNOPSIS","**pwqgen** [_options_]"],["PARAMETERS","**random=N**\n> Number of random words.\n\n**count=N**\n> Number of passphrases."],["DESCRIPTION","**pwqgen** generates random passphrases by combining dictionary words with separators, producing passwords that are both strong and easy to remember. The **random** parameter controls the number of words used, with more words providing higher entropy.\n\nThe tool is part of the **passwdqc** (password quality checking) suite and serves as a companion to **pwqcheck**. Generated passphrases follow the quality rules enforced by passwdqc's password policy, making them suitable for environments with strict password requirements."],["CAVEATS","Part of passwdqc. May not be installed by default."],["HISTORY","pwqgen is part of **passwdqc** password quality checking suite."],["INSTALL","```apt: sudo apt install passwdqc```\n\n```dnf: sudo dnf install passwdqc```\n\n```zypper: sudo zypper install passwdqc```\n\n```brew: brew install passwdqc```\n\n```nix: nix profile install nixpkgs#passwdqc```"],["SEE ALSO","[pwgen](/man/pwgen)(1), [apg](/man/apg)(1), [pwqcheck](/man/pwqcheck)(1)"]],"e":["pwgen","apg","pwqcheck"],"x":"pwqgen"},"realsr-ncnn-vulkan":{"n":"realsr-ncnn-vulkan","t":"AI-powered image upscaling with GPU acceleration","s":[["TAGLINE","AI-powered image upscaling with GPU acceleration"],["TLDR","**Upscale image**\n\n```realsr-ncnn-vulkan -i [input.jpg] -o [output.png]```\n\n**Specify scale factor**\n\n```realsr-ncnn-vulkan -i [input.jpg] -o [output.png] -s [4]```\n\n**Use specific model**\n\n```realsr-ncnn-vulkan -i [input.jpg] -o [output.png] -m [models-DF2K]```\n\n**Specify GPU**\n\n```realsr-ncnn-vulkan -i [input.jpg] -o [output.png] -g [0]```"],["SYNOPSIS","**realsr-ncnn-vulkan** [_options_] **-i** _input_ **-o** _output_"],["PARAMETERS","**-i** _path_\n> Input image file.\n\n**-o** _path_\n> Output image file.\n\n**-s** _scale_\n> Upscale ratio (4 default).\n\n**-m** _path_\n> Model directory.\n\n**-g** _gpu_\n> GPU device ID (-1 for CPU).\n\n**-t** _size_\n> Tile size.\n\n**-j** _threads_\n> Processing threads."],["DESCRIPTION","**realsr-ncnn-vulkan** is a Real-ESRGAN/Real-SR image upscaler using the ncnn neural network framework with Vulkan GPU acceleration. It produces high-quality upscaled images."],["EXAMPLES","```bash"],["Basic 4x upscale","realsr-ncnn-vulkan -i photo.jpg -o photo_4x.png"],["2x scale","realsr-ncnn-vulkan -i photo.jpg -o photo_2x.png -s 2"],["Use specific model","realsr-ncnn-vulkan -i anime.png -o anime_up.png -m models-DF2K_JPEG"],["CPU only","realsr-ncnn-vulkan -i input.jpg -o output.png -g -1"],["Batch process (use loop)","for f in *.jpg; do\n  realsr-ncnn-vulkan -i \"$f\" -o \"upscaled_$f\"\ndone\n```"],["MODELS","```\nmodels-DF2K       - General purpose\nmodels-DF2K_JPEG  - JPEG artifact reduction\n```"],["CAVEATS","Requires Vulkan-capable GPU. Memory-intensive for large images. Processing can be slow."],["HISTORY","realsr-ncnn-vulkan is based on **Real-ESRGAN** research, ported to ncnn by **nihui** for efficient cross-platform inference."],["INSTALL","```aur: yay -S realsr-ncnn-vulkan```"],["SEE ALSO","[waifu2x-ncnn-vulkan](/man/waifu2x-ncnn-vulkan)(1), [imagemagick](/man/imagemagick)(1)"]],"e":["waifu2x-ncnn-vulkan","imagemagick"],"x":"realsr-ncnn-vulkan -i [input.jpg] -o [output.png]"},"redland":{"n":"redland","t":"RDF data storage and processing libraries","s":[["TAGLINE","RDF data storage and processing libraries"],["TLDR","**Parse RDF file**\n\n```rapper -i rdfxml -o ntriples [file.rdf]```\n\n**Query RDF store**\n\n```roqet -i sparql \"[query]\" [data.rdf]```\n\n**Process RDF**\n\n```rdfproc [store] parse [file.rdf]```"],["SYNOPSIS","Redland RDF Libraries and utilities"],["UTILITIES","**rapper**\n> RDF parser utility.\n\n**roqet**\n> RDF query utility.\n\n**rdfproc**\n> RDF processing utility."],["DESCRIPTION","**Redland** is a set of C libraries and command-line utilities for working with RDF (Resource Description Framework) data. It provides comprehensive support for parsing, storing, querying via SPARQL, and serializing RDF across multiple formats including RDF/XML, Turtle, N-Triples, and N-Quads.\n\nThe toolkit includes **rapper** for format conversion and validation, **roqet** for executing SPARQL queries, and **rdfproc** for managing persistent RDF stores. These tools can be combined in pipelines for batch processing of semantic web data."],["EXAMPLES","```bash"],["Parse RDF/XML to N-Triples","rapper -i rdfxml -o ntriples data.rdf"],["Validate RDF","rapper -c data.rdf"],["SPARQL query","roqet -i sparql \"SELECT * WHERE { ?s ?p ?o } LIMIT 10\" data.rdf"],["Convert formats","rapper -i turtle -o rdfxml data.ttl > data.rdf"],["Create and query store","rdfproc mystore parse data.rdf\nrdfproc mystore query sparql - \"SELECT * WHERE { ?s ?p ?o }\"\n```"],["FORMATS","```\nrdfxml   - RDF/XML\nntriples - N-Triples\nturtle   - Turtle\nnquads   - N-Quads\ntrig     - TriG\n```"],["CAVEATS","Library-based (librdf). Multiple tools for different tasks. SPARQL 1.0 support."],["HISTORY","Redland was created by **Dave Beckett** starting in 2000 as a comprehensive RDF library suite."],["INSTALL","```dnf: sudo dnf install redland```\n\n```pacman: sudo pacman -S redland```\n\n```zypper: sudo zypper install redland```\n\n```brew: brew install redland```\n\n```nix: nix profile install nixpkgs#redland```"],["SEE ALSO","[rapper](/man/rapper)(1), [roqet](/man/roqet)(1), [rdfproc](/man/rdfproc)(1)"]],"e":["rapper","roqet","rdfproc"],"x":"rapper -i rdfxml -o ntriples [file.rdf]"},"rlog":{"n":"rlog","t":"Display RCS file revision history","s":[["TAGLINE","Display RCS file revision history"],["TLDR","**Show file history**\n\n```rlog [file.c]```\n\n**Show specific revision**\n\n```rlog -r[1.5] [file.c]```\n\n**Show revision range**\n\n```rlog -r[1.1:1.5] [file.c]```\n\n**Show by date**\n\n```rlog -d\"[>2024-01-01]\" [file.c]```\n\n**Show header only**\n\n```rlog -h [file.c]```\n\n**Show states**\n\n```rlog -s[Exp] [file.c]```\n\n**Show by author**\n\n```rlog -w[username] [file.c]```"],["SYNOPSIS","**rlog** [_-r revisions_] [_-d dates_] [_-h_] [_options_] _files_"],["PARAMETERS","**-r** _REV_\n> Specific revision(s).\n\n**-d** _DATE_\n> Date selection.\n\n**-h**\n> Header only.\n\n**-t**\n> Header and description.\n\n**-s** _STATE_\n> By state.\n\n**-w** _AUTHOR_\n> By author.\n\n**-b**\n> Default branch only.\n\n**-R**\n> Print RCS file name only.\n\n**-N**\n> Do not print symbolic names.\n\n**-L**\n> Ignore RCS files with no locks set.\n\n**-l**\n> Locked revisions only.\n\n**-V**\n> Print RCS version number.\n\n**-z** _zone_\n> Specify date output format and default time zone."],["DESCRIPTION","**rlog** prints information about RCS files. Without options, rlog prints the complete log: header information (RCS file name, working file, head revision, default branch, access list, locks, symbolic names, comment leader, total revisions) followed by entries for each revision (number, date, author, state, number of lines added/deleted, and log message).\n\nOptions **-d**, **-l**, **-s**, and **-w** select revisions by their intersection, while **-b** and **-r** select by their union. Only revisions matching all specified criteria are printed."],["CAVEATS","RCS is largely superseded by Git. File-level versioning only. Less common in modern development."],["HISTORY","**rlog** is part of **RCS** (Revision Control System), created by **Walter Tichy** at Purdue in the **1980s**. RCS was one of the first version control systems."],["INSTALL","```apt: sudo apt install rcs```\n\n```dnf: sudo dnf install rcs```\n\n```pacman: sudo pacman -S rcs```\n\n```apk: sudo apk add rcs```\n\n```zypper: sudo zypper install rcs```\n\n```brew: brew install rcs```\n\n```nix: nix profile install nixpkgs#rcs```"],["SEE ALSO","[rcs](/man/rcs)(1), [ci](/man/ci)(1), [co](/man/co)(1), [rcsdiff](/man/rcsdiff)(1), [git](/man/git)(1)"]],"e":["rcs","ci","co","rcsdiff","git"],"x":"rlog [file.c]"},"sacct":{"n":"sacct","t":"Display Slurm job accounting information","s":[["TAGLINE","Display Slurm job accounting information"],["TLDR","Display **recent jobs** with default fields\n\n```sacct```\n\nDisplay **brief** job information\n\n```sacct -b```\n\nDisplay **allocations** for a specific job\n\n```sacct -j job_id -X```\n\nDisplay **custom fields** for a job\n\n```sacct -j job_id -o Elapsed,JobName,ReqCPUS,ReqMem```\n\nDisplay jobs from **one week ago**\n\n```sacct -S $(date -d \"1 week ago\" +'%F')```\n\n**Expand field width** for long values\n\n```sacct -o JobID,JobName%100```"],["SYNOPSIS","**sacct** [_options_]"],["PARAMETERS","**-b**, **--brief**\n> Display brief job information\n\n**-j**, **--jobs** _id_\n> Show specific job\n\n**-X**, **--allocations**\n> Show only allocations\n\n**-o**, **--format** _fields_\n> Custom output format\n\n**-S**, **--starttime** _date_\n> Filter by start time"],["DESCRIPTION","**sacct** displays accounting data from the Slurm workload manager. It shows job history including resource usage, state, and exit codes for completed and running jobs.\n\nOutput format is highly customizable with many available fields for detailed job analysis."],["CAVEATS","Requires Slurm accounting to be configured. Historical data retention depends on Slurm configuration."],["HISTORY","Part of **Slurm** workload manager, the leading HPC cluster resource management system."],["INSTALL","```apt: sudo apt install slurm-client```"],["SEE ALSO","[squeue](/man/squeue)(1), [sinfo](/man/sinfo)(1), [scontrol](/man/scontrol)(1)"]],"e":["squeue","sinfo","scontrol"],"x":"sacct"},"scylladb":{"n":"scylladb","t":"High-performance Cassandra-compatible NoSQL database","s":[["TAGLINE","High-performance Cassandra-compatible NoSQL database"],["TLDR","**Start CQL shell**\n\n```cqlsh```\n\n**Connect to ScyllaDB node**\n\n```cqlsh [hostname] [9042]```\n\n**Connect with authentication**\n\n```cqlsh -u [username] -p [password]```\n\n**Execute CQL from file**\n\n```cqlsh -f [script.cql]```\n\n**Check node status**\n\n```nodetool status```\n\n**View ScyllaDB info**\n\n```scylla --version```\n\n**Check cluster status**\n\n```nodetool describecluster```\n\n**Flush memtables**\n\n```nodetool flush```"],["SYNOPSIS","**cqlsh** [_options_] [_host_] [_port_]\n\n**nodetool** [_options_] _command_ [_args_]"],["CQLSH PARAMETERS","**-u**, **--username** _user_\n> Authentication username.\n\n**-p**, **--password** _pass_\n> Authentication password.\n\n**-k**, **--keyspace** _keyspace_\n> Initial keyspace.\n\n**-f** _file_\n> Execute CQL file.\n\n**-e** _statement_\n> Execute single CQL statement.\n\n**--ssl**\n> Use SSL connection.\n\n**--request-timeout** _secs_\n> Query timeout."],["NODETOOL COMMANDS","**status**\n> Show cluster node status.\n\n**info**\n> Node information and metrics.\n\n**ring**\n> Token ring information.\n\n**repair** [_keyspace_]\n> Run repair operation.\n\n**compact** [_keyspace_]\n> Force compaction.\n\n**flush** [_keyspace_]\n> Flush memtables to SSTables.\n\n**snapshot** [_name_]\n> Take snapshot backup.\n\n**drain**\n> Drain node for shutdown.\n\n**version**\n> Show ScyllaDB version.\n\n**describecluster**\n> Cluster information.\n\n**toppartitions**\n> Show hot partitions."],["SCYLLADB-SPECIFIC","**nodetool toppartitions** _keyspace_ _table_ _duration_\n> Identify hot partitions causing load.\n\n**nodetool setlogginglevel** _logger_ _level_\n> Adjust logging dynamically.\n\n**scylla --developer-mode** _mode_\n> Development mode flag.\n\n**scylla --smp** _cores_\n> Number of CPU cores to use.\n\n**scylla --memory** _amount_\n> Memory allocation."],["DESCRIPTION","**ScyllaDB** uses Cassandra-compatible tools (**cqlsh** and **nodetool**) with the same commands and CQL syntax. ScyllaDB is a C++ rewrite of Cassandra designed for lower latency and higher throughput.\n\n**cqlsh** executes CQL statements. Keyspaces, tables, and query syntax match Cassandra. ScyllaDB adds lightweight transactions (LWT) improvements and materialized views.\n\n**nodetool** manages cluster operations. ScyllaDB extends it with **toppartitions** for identifying hot spots that cause load imbalance.\n\nScyllaDB's shard-per-core architecture eliminates locking between CPU cores. The **--smp** flag controls core allocation. Configuration differs from Cassandra, using scylla.yaml.\n\nScyllaDB Alternator provides DynamoDB-compatible API, accessible via AWS SDK tools instead of cqlsh."],["CONFIGURATION","**/etc/scylla/scylla.yaml**\n> Main ScyllaDB configuration file for cluster settings, networking, storage, and tuning parameters.\n\n**/etc/scylla/cassandra-rackdc.properties**\n> Datacenter and rack configuration for topology-aware replication.\n\n**~/.cassandra/cqlshrc**\n> CQL shell configuration file for connection defaults, formatting, and authentication."],["CAVEATS","While CQL-compatible, some Cassandra features differ or are unsupported. Configuration files differ from Cassandra. Repair is faster but still resource-intensive. Check compatibility matrix for drivers."],["HISTORY","ScyllaDB was founded in **2015** by **Avi Kivity** (KVM creator) and **Dor Laor**. The first release came in **2015** as a drop-in Cassandra replacement written in C++ using the Seastar framework. Version 2.0 (2017) added materialized views. ScyllaDB Open Source is Apache-licensed; Enterprise adds features like encryption and LDAP."],["SEE ALSO","[cqlsh](/man/cqlsh)(1)"]],"e":["cqlsh"],"x":"cqlsh"},"sesearch":{"n":"sesearch","t":"Search and analyze SELinux policy rules","s":[["TAGLINE","Search and analyze SELinux policy rules"],["TLDR","Search for all **allow rules** in the policy\n\n```sesearch --allow```\n\nSearch for rules related to a **specific target type**\n\n```sesearch --allow -t [type_name]```\n\nSearch for rules related to a **specific source type**\n\n```sesearch --allow -s [source_type]```\n\nSearch for rules with a specific **class and permission**\n\n```sesearch --allow -c [class_name] -p [permission]```\n\nSearch for **type transition** rules\n\n```sesearch --type_transition -s [source_type]```\n\nDisplay **verbose** information about matched rules\n\n```sesearch --allow -t [type_name] -v```"],["SYNOPSIS","**sesearch** [_OPTIONS_] [_EXPRESSION_] [_POLICY_]"],["PARAMETERS","**-A, --allow**\n> Search for allow and allowxperm rules\n\n**-T, --type_transition**\n> Search for type_transition rules\n\n**--auditallow**\n> Search for auditallow rules\n\n**--dontaudit**\n> Search for dontaudit rules\n\n**-s, --source** _NAME_\n> Find rules with matching source type/attribute\n\n**-t, --target** _NAME_\n> Find rules with matching target type/attribute\n\n**-c, --class** _NAME_\n> Find rules with matching object class\n\n**-p, --perm** _P1[,P2,...]_\n> Find rules with matching permissions (comma-separated)\n\n**-b, --bool** _NAME_\n> Find rules with matching conditional Boolean\n\n**-ds**\n> Match source type directly, not by attribute\n\n**-dt**\n> Match target type directly, not by attribute\n\n**-v, --verbose**\n> Display additional rule information\n\n**--version**\n> Print version and exit"],["DESCRIPTION","**sesearch** is a SELinux policy query tool from the **setools** package that allows users to search and analyze rules within a SELinux policy. It can query both binary policy files (typically named policy.XX) and modular policies, making it essential for SELinux policy debugging and development.\n\nThe tool supports searching various rule types including allow rules, type transitions, role-based access control rules, and MLS (Multi-Level Security) range transitions. Results can be filtered by source type, target type, object class, permissions, and Boolean conditions."],["CAVEATS","Policy files must be accessible; default location is **/sys/fs/selinux/policy** on running systems. Results may differ between policy versions. Searching large policies without filters can produce extensive output. Extended permission rules (xperm) require separate flags from standard permission searches."],["HISTORY","**sesearch** is part of the **SETools** (SELinux Policy Tools) project, originally developed at **Tresys Technology** starting in **2001** to provide analysis capabilities for SELinux policies. The setools package has evolved alongside SELinux, which was integrated into the Linux kernel mainline in **2003**. Current maintenance continues under the setools project on GitHub."],["INSTALL","```apt: sudo apt install setools```\n\n```dnf: sudo dnf install setools```\n\n```nix: nix profile install nixpkgs#setools```"],["SEE ALSO","[seinfo](/man/seinfo)(1), [semodule](/man/semodule)(8), [sestatus](/man/sestatus)(8), [getsebool](/man/getsebool)(8)"]],"e":["seinfo","semodule","sestatus","getsebool"],"x":"sesearch --allow"},"set":{"n":"set","t":"Set shell options and positional parameters","s":[["TAGLINE","Set shell options and positional parameters"],["TLDR","**Enable strict mode** (exit on error, undefined variables, pipeline failures)\n\n```set -euo pipefail```\n\n**Exit immediately** if any command returns non-zero status\n\n```set -e```\n\n**Treat unset variables** as errors\n\n```set -u```\n\n**Print commands** before execution (debugging)\n\n```set -x```\n\n**Disable an option** (use + instead of -)\n\n```set +x```\n\n**Disable filename expansion** (globbing)\n\n```set -f```\n\n**List all shell variables** and functions\n\n```set```\n\n**Set positional parameters**\n\n```set -- [arg1] [arg2] [arg3]```"],["SYNOPSIS","**set** [**-abefhkmnptuvxBCEHPT**] [**-o** _option-name_] [**--**] [_arg_...]"],["PARAMETERS","**-a** (**allexport**)\n> Export all created or modified variables and functions\n\n**-b** (**notify**)\n> Report terminated background job status immediately\n\n**-e** (**errexit**)\n> Exit immediately if a command returns non-zero status\n\n**-f** (**noglob**)\n> Disable filename expansion (globbing)\n\n**-h** (**hashall**)\n> Remember command locations as they are looked up\n\n**-k** (**keyword**)\n> Place all assignment arguments in the environment\n\n**-m** (**monitor**)\n> Enable job control\n\n**-n** (**noexec**)\n> Read commands but do not execute them (syntax check)\n\n**-p** (**privileged**)\n> Enable privileged mode\n\n**-t** (**onecmd**)\n> Exit after reading and executing one command\n\n**-u** (**nounset**)\n> Treat unset variables as an error during expansion\n\n**-v** (**verbose**)\n> Print shell input lines as they are read\n\n**-x** (**xtrace**)\n> Print commands and arguments before execution\n\n**-B** (**braceexpand**)\n> Enable brace expansion (default on)\n\n**-C** (**noclobber**)\n> Prevent overwriting files with output redirection\n\n**-E** (**errtrace**)\n> ERR trap is inherited by shell functions and subshells\n\n**-H** (**histexpand**)\n> Enable ! style history substitution\n\n**-P** (**physical**)\n> Do not resolve symbolic links for commands like cd\n\n**-T** (**functrace**)\n> DEBUG and RETURN traps inherited by functions\n\n**-o pipefail**\n> Pipeline returns status of last non-zero command\n\n**--**\n> End of options; remaining args become positional parameters"],["DESCRIPTION","**set** is a shell builtin that controls shell options and positional parameters. Options modify shell behavior for scripting, debugging, and interactive use. Use **-** to enable an option and **+** to disable it.\n\nWhen invoked without arguments, **set** displays all shell variables and functions. With **--** followed by arguments, it sets the positional parameters (**$1**, **$2**, etc.) to the provided values.\n\nThe combination **set -euo pipefail** is a common \"strict mode\" for shell scripts that causes immediate exit on errors, undefined variable references, or pipeline failures—catching bugs early and preventing silent failures."],["CAVEATS","The **-e** option has many exceptions: it does not trigger exit for commands in **if** tests, **while**/**until** conditions, commands negated with **!**, or the left side of **&&** and **||**. Understanding these exceptions is important for reliable error handling in scripts."],["HISTORY","The **set** command has been part of the Bourne shell since its creation by **Stephen Bourne** at Bell Labs in **1979**. It was included in POSIX and extended in bash with additional options like **pipefail**. The concept of shell options predates even the Bourne shell, with roots in early Unix shells."],["SEE ALSO","[bash](/man/bash)(1), [sh](/man/sh)(1), [shopt](/man/shopt)(1), [unset](/man/unset)(1), [export](/man/export)(1)"]],"e":["bash","sh","shopt","unset","export"],"x":"set -euo pipefail"},"shiori":{"n":"shiori","t":"Self-hosted bookmark manager with web interface","s":[["TAGLINE","Self-hosted bookmark manager with web interface"],["TLDR","**Start server**\n\n```shiori serve```\n\n**Add bookmark**\n\n```shiori add [https://example.com]```\n\n**Add with title**\n\n```shiori add -t \"[Title]\" [https://example.com]```\n\n**Search bookmarks**\n\n```shiori search [query]```\n\n**List bookmarks**\n\n```shiori print```\n\n**Delete bookmark**\n\n```shiori delete [id]```\n\n**Export bookmarks**\n\n```shiori export > [bookmarks.html]```\n\n**Import bookmarks**\n\n```shiori import [bookmarks.html]```"],["SYNOPSIS","**shiori** _command_ [_options_] [_args_]"],["PARAMETERS","**serve**\n> Start web server.\n\n**add** _URL_\n> Add bookmark.\n\n**search** _QUERY_\n> Search bookmarks.\n\n**print**\n> List bookmarks.\n\n**delete** _ID_\n> Remove bookmark.\n\n**export**\n> Export bookmarks.\n\n**import** _FILE_\n> Import bookmarks.\n\n**-t**, **--title** _TITLE_\n> Set title.\n\n**-e**, **--excerpt** _TEXT_\n> Set excerpt.\n\n**--tags** _TAGS_\n> Add tags."],["DESCRIPTION","**shiori** is a self-hosted bookmark manager that saves web pages for offline reading. When a URL is added, shiori downloads the page, extracts readable content using a readability algorithm, and stores both the original HTML and a clean text version in its database.\n\nBookmarks support full-text search across titles, URLs, and saved content, making it easy to find previously saved pages. Tags provide additional organization, and the built-in web interface offers a visual way to browse, edit, and manage the collection.\n\nData can be imported from and exported to standard bookmark HTML files for interoperability with browsers and other bookmark managers. The CLI provides the same functionality as the web interface for scripting and automation."],["CONFIGURATION","**SHIORI_DIR**\n> Environment variable setting the data directory for the database and archived pages (default: ~/.local/share/shiori)."],["CAVEATS","Requires database (SQLite default). Large archives use storage. Some pages don't parse well."],["HISTORY","**Shiori** was created by **RadhiFadlillah** as a simple bookmark manager. Named after Japanese for bookmark, it focuses on offline reading."],["INSTALL","```nix: nix profile install nixpkgs#shiori```"],["SEE ALSO","[buku](/man/buku)(1), [pocket](/man/pocket)(1)"]],"e":["buku","pocket"],"x":"shiori serve"},"smartctl":{"n":"smartctl","t":"Monitor and control SMART disk health data","s":[["TAGLINE","Monitor and control SMART disk health data"],["TLDR","**Show drive information**\n\n```smartctl -i /dev/[sda]```\n\n**Show all SMART data**\n\n```smartctl -a /dev/[sda]```\n\n**Check drive health**\n\n```smartctl -H /dev/[sda]```\n\n**Run short self-test**\n\n```smartctl -t short /dev/[sda]```\n\n**Run extended self-test**\n\n```smartctl -t long /dev/[sda]```\n\n**View self-test results**\n\n```smartctl -l selftest /dev/[sda]```\n\n**View error log**\n\n```smartctl -l error /dev/[sda]```\n\n**Enable SMART** on drive\n\n```smartctl -s on /dev/[sda]```\n\n**Scan for drives**\n\n```smartctl --scan```"],["SYNOPSIS","**smartctl** [_options_] _device_"],["DESCRIPTION","**smartctl** is a command-line tool for monitoring and controlling SMART (Self-Monitoring, Analysis and Reporting Technology) data on hard drives and SSDs. SMART data can predict drive failures before they occur.\n\nThe tool can display drive identity, health status, attributes, error logs, and self-test results. It can also initiate self-tests and control SMART features. Different manufacturers implement SMART differently, so attribute interpretation varies.\n\nsmartctl is part of smartmontools and is essential for system administrators monitoring disk health and planning replacements before failure."],["PARAMETERS","**-i**, **--info**\n> Show device identification.\n\n**-a**, **--all**\n> Show all SMART information.\n\n**-H**, **--health**\n> Show SMART health status.\n\n**-A**, **--attributes**\n> Show SMART attributes.\n\n**-l** _type_, **--log=** _type_\n> Show log (error, selftest, selective).\n\n**-t** _test_, **--test=** _test_\n> Run test (short, long, conveyance, selective, offline).\n\n**-s** _on|off_\n> Enable/disable SMART.\n\n**-d** _type_\n> Specify device type.\n\n**--scan**\n> Scan for devices.\n\n**-x**, **--xall**\n> Show all extended information (all SMART and non-SMART data).\n\n**-c**, **--capabilities**\n> Show SMART capabilities of the device.\n\n**-C**, **--captive**\n> Run self-tests in captive mode (blocks device until finished).\n\n**-X**, **--abort**\n> Abort an in-progress self-test.\n\n**-n** _POWERMODE_, **--nocheck=** _POWERMODE_\n> Skip check if device is in a low-power mode (standby, sleep, idle, never).\n\n**-q** _type_, **--quietmode=** _type_\n> Quiet output (errorsonly, silent, noserial)."],["KEY ATTRIBUTES","**Reallocated Sector Count**: Bad sectors moved to spare area\n**Current Pending Sector Count**: Sectors waiting to be remapped\n**Offline Uncorrectable**: Sectors that couldn't be corrected\n**Power-On Hours**: Total operating time\n**Temperature**: Drive temperature"],["CAVEATS","Requires root privileges for most operations. SMART must be supported and enabled on the drive. Attribute interpretation varies by manufacturer. Self-tests may affect performance while running."],["HISTORY","**smartmontools** was created by **Bruce Allen** in **2002** as a fork of the smartsuite package. It provides cross-platform SMART monitoring on Linux, BSD, macOS, and Windows. The project continues active development and is the standard SMART monitoring tool on Unix-like systems."],["INSTALL","```apt: sudo apt install smartmontools```\n\n```dnf: sudo dnf install smartmontools```\n\n```pacman: sudo pacman -S smartmontools```\n\n```apk: sudo apk add smartmontools```\n\n```zypper: sudo zypper install smartmontools```\n\n```brew: brew install smartmontools```\n\n```nix: nix profile install nixpkgs#smartmontools```"],["SEE ALSO","[smartd](/man/smartd)(8), [hdparm](/man/hdparm)(1), [nvme](/man/nvme)(1), [lsblk](/man/lsblk)(1)"]],"e":["smartd","hdparm","nvme","lsblk"],"x":"smartctl -i /dev/[sda]"},"spctoppm":{"n":"spctoppm","t":"Convert Atari Spectrum images to PPM","s":[["TAGLINE","Convert Atari Spectrum images to PPM"],["TLDR","**Convert Atari SPC to PPM**\n\n```spctoppm [input.spc] > [output.ppm]```"],["SYNOPSIS","**spctoppm** [_file_]"],["DESCRIPTION","**spctoppm** converts images in the Atari compressed Spectrum format (SPC) to PPM (Portable Pixmap) format. The Spectrum format was used on the Atari ST computer for storing color images. This tool is part of the Netpbm image processing toolkit, which provides converters between many image formats."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[sputoppm](/man/sputoppm)(1), [neotoppm](/man/neotoppm)(1)"]],"e":["sputoppm","neotoppm"],"x":"spctoppm [input.spc] > [output.ppm]"},"tailscale":{"n":"tailscale","t":"WireGuard-based mesh VPN","s":[["TAGLINE","WireGuard-based mesh VPN"],["TLDR","**Connect to network**\n\n```tailscale up```\n\n**Disconnect**\n\n```tailscale down```\n\n**Show status**\n\n```tailscale status```\n\n**Show IP addresses**\n\n```tailscale ip```\n\n**Ping node**\n\n```tailscale ping [hostname]```\n\n**SSH to node**\n\n```tailscale ssh [user@hostname]```\n\n**Share file**\n\n```tailscale file cp [file] [hostname:]```\n\n**Receive files**\n\n```tailscale file get [download_dir]```"],["SYNOPSIS","**tailscale** [_up_] [_down_] [_status_] [_ip_] [_ping_] [_ssh_] [_file_] [_options_]"],["PARAMETERS","**up**\n> Connect to Tailscale.\n\n**down**\n> Disconnect.\n\n**status**\n> Show network status.\n\n**ip** [_-4_] [_-6_]\n> Show IP addresses.\n\n**ping** _HOST_\n> Ping a Tailscale node.\n\n**ssh** _TARGET_\n> SSH to node.\n\n**file cp** _FILES_ _TARGET:_\n> Copy files.\n\n**file get** [_DIR_]\n> Receive files.\n\n**cert** _DOMAIN_\n> Get TLS certificate.\n\n**funnel**\n> Expose to internet.\n\n**serve**\n> Serve content.\n\n**logout**\n> Log out.\n\n**--accept-routes**\n> Accept subnet routes.\n\n**--advertise-routes** _ROUTES_\n> Advertise routes.\n\n**--exit-node** _NODE_\n> Use exit node."],["DESCRIPTION","**tailscale** creates encrypted mesh networks. Devices connect directly using WireGuard, without central servers forwarding traffic.\n\nAuthentication uses SSO providers. After login, devices join your tailnet (Tailscale network). Each device gets a stable IP.\n\nMagicDNS provides hostnames for devices. SSH integration enables authentication without managing keys.\n\nFile sharing transfers directly between devices. No cloud storage is involved - files go peer-to-peer.\n\nExit nodes route traffic through specific devices. This enables secure internet access through trusted locations.\n\nFunnel exposes services to the internet. Serve provides quick HTTP/HTTPS serving with automatic certificates."],["CAVEATS","Requires Tailscale account. Control plane is cloud-based. Some features need paid plan. Network depends on coordination server availability."],["HISTORY","**Tailscale** was founded by **Brad Fitzpatrick** and others in **2019**. It builds on WireGuard to provide easy-to-use mesh VPN, focusing on zero-configuration connectivity."],["INSTALL","```dnf: sudo dnf install tailscale```\n\n```pacman: sudo pacman -S tailscale```\n\n```apk: sudo apk add tailscale```\n\n```zypper: sudo zypper install tailscale```\n\n```brew: brew install tailscale```\n\n```nix: nix profile install nixpkgs#tailscale```"],["SEE ALSO","[wg](/man/wg)(8), [ssh](/man/ssh)(1), [openvpn](/man/openvpn)(8)"]],"e":["wg","ssh","openvpn"],"x":"tailscale up"},"termusic":{"n":"termusic","t":"Terminal music player with vim bindings","s":[["TAGLINE","Terminal music player with vim bindings"],["TLDR","Open to **specific** directory\n\n```termusic [path/to/directory]```\n\nDisable **album cover** display\n\n```termusic -c```\n\nDisplay **help**\n\n```termusic --help```\n\n**Scan** library and update database\n\n```termusic scan```"],["SYNOPSIS","**termusic** [_OPTIONS_] [_PATH_]"],["PARAMETERS","**-c**\n> Disable showing album cover\n\n**-m** _DEPTH_\n> Max depth of folder scanning (default: 4)\n\n**scan**\n> Scan library and update database\n\n**config**\n> Open config in editor\n\n**--help**\n> Display help information"],["DESCRIPTION","**termusic** is a terminal music player written in Rust featuring vim-like key bindings. It supports various audio formats and provides a TUI (terminal user interface) for browsing and playing music.\n\nConfiguration is stored in `~/.config/termusic/config.toml` where default directories and preferences can be set."],["KEY BINDINGS","**j/k** - Navigate up/down\n\n**Enter** - Play selected\n\n**Space** - Pause/resume\n\n**n/p** - Next/previous track\n\n**s** - Toggle shuffle\n\n**f** - Seek forward\n\n**F1** - Help\n\n**q** - Quit"],["CAVEATS","Album cover display requires a compatible terminal. Some audio formats may need additional codec support."],["HISTORY","**termusic** is a modern terminal music player developed by **tramhao**, inspired by vim's modal editing philosophy and designed for keyboard-driven music playback."],["INSTALL","```pacman: sudo pacman -S termusic```\n\n```brew: brew install termusic```\n\n```nix: nix profile install nixpkgs#termusic```"],["SEE ALSO","[cmus](/man/cmus)(1), [ncmpcpp](/man/ncmpcpp)(1), [mpv](/man/mpv)(1)"]],"e":["cmus","ncmpcpp","mpv"],"x":"termusic [path/to/directory]"},"terraform-destroy":{"n":"terraform-destroy","t":"Destroy Terraform-managed infrastructure","s":[["TAGLINE","Destroy Terraform-managed infrastructure"],["TLDR","**Destroy** all resources in the current configuration\n\n```terraform destroy```\n\n**Destroy** without interactive confirmation\n\n```terraform destroy -auto-approve```\n\n**Destroy** a **specific resource** only\n\n```terraform destroy -target=[resource_type.resource_name]```\n\n**Preview** what would be destroyed without executing\n\n```terraform plan -destroy```\n\n**Destroy** with specific **variable values**\n\n```terraform destroy -var '[name]=[value]'```\n\n**Destroy** with a **variable file**\n\n```terraform destroy -var-file=[prod.tfvars]```\n\n**Destroy** with limited **parallelism** to avoid API rate limits\n\n```terraform destroy -parallelism=[5]```"],["SYNOPSIS","**terraform destroy** [_options_]"],["PARAMETERS","**-auto-approve**\n> Skip the interactive confirmation prompt\n\n**-target=**_RESOURCE_\n> Destroy only the specified resource and its dependencies. Can be specified multiple times.\n\n**-var** '_NAME_=_VALUE_'\n> Set an input variable value. Can be specified multiple times.\n\n**-var-file=**_PATH_\n> Load variable values from a file\n\n**-lock=**_BOOL_\n> Control whether Terraform locks the state file. Default: **true**.\n\n**-lock-timeout=**_DURATION_\n> How long to wait to acquire the state lock. Example: **20s**.\n\n**-parallelism=**_N_\n> Limit concurrent resource operations. Default: **10**.\n\n**-refresh=**_BOOL_\n> Whether to refresh state before planning. Default: **true**.\n\n**-state=**_PATH_\n> Path to the state file. Default: **terraform.tfstate**.\n\n**-no-color**\n> Disable color output"],["DESCRIPTION","**terraform destroy** permanently deletes all infrastructure resources tracked in the current Terraform state file. It is a convenience alias for **terraform apply -destroy**.\n\nThe command reads the state file to identify managed resources, validates state against cloud provider APIs, builds a dependency graph to determine the correct reverse destruction order, presents a plan for confirmation, and then deletes resources via provider APIs. After completion, it updates the state file to reflect that the resources no longer exist.\n\nIt is designed primarily for ephemeral infrastructure in development and testing environments. For production, it should only be used in controlled decommissioning scenarios."],["CAVEATS","This command is **irreversible**. Once resources are destroyed, the cloud provider removes them permanently and Terraform cannot restore them. If a destroy operation fails partway through, you may end up in a partially destroyed state. The command operates exclusively based on the state file and has no awareness of resources not tracked in state. Resources with **deletion protection** enabled at the provider level cannot be destroyed until protection is manually disabled. Configurations using **lifecycle { prevent_destroy = true }** will cause the destroy to fail with an error. Using **-target** for selective destruction can leave infrastructure in an inconsistent state and should be reserved for exceptional situations."],["HISTORY","**Terraform** was created by **Mitchell Hashimoto** and **Armon Dadgar** at **HashiCorp**, with **v0.1.0** released in **July 2014**. The destroy subcommand has been part of Terraform's core workflow since the earliest releases. In **v0.15.2** (April 2021), **terraform apply -destroy** was added as the explicit form, making **terraform destroy** an official convenience alias. Terraform reached general availability with **v1.0.0** in **June 2021**."],["INSTALL","```pacman: sudo pacman -S terraform```\n\n```nix: nix profile install nixpkgs#terraform```"],["SEE ALSO","[terraform](/man/terraform)(1), [terraform-plan](/man/terraform-plan)(1), [terraform-apply](/man/terraform-apply)(1)"]],"e":["terraform","terraform-plan","terraform-apply"],"x":"terraform destroy"},"terraria":{"n":"terraria","t":"Terraria dedicated game server","s":[["TAGLINE","Terraria dedicated game server"],["TLDR","Start **interactive** server setup\n\n```[path/to]/TerrariaServer```\n\nStart server with **world**\n\n```[path/to]/TerrariaServer -world [path/to/world.wld]```"],["SYNOPSIS","**TerrariaServer** [_OPTIONS_]"],["PARAMETERS","**-world** _PATH_\n> World file to load\n\n**-port** _PORT_\n> Server port (default: 7777)\n\n**-maxplayers** _N_\n> Maximum players allowed\n\n**-password** _PASS_\n> Server password\n\n**-config** _FILE_\n> Configuration file path\n\n**-autocreate** _SIZE_\n> Auto-create world (1=small, 2=medium, 3=large)\n\n**-worldname** _NAME_\n> Name for auto-created world"],["DESCRIPTION","**TerrariaServer** creates and runs a headless Terraria game server. It can be started interactively to configure settings or with command-line arguments for automated deployment.\n\nThe server hosts multiplayer Terraria sessions, allowing players to connect and play together."],["CAVEATS","Requires a valid Terraria server installation. World files are platform-specific. Server consumes significant resources for large worlds with many players."],["HISTORY","**Terraria** is a 2D sandbox game developed by **Re-Logic**, first released in **2011**. The dedicated server allows hosting multiplayer games without running the full game client."],["SEE ALSO","[screen](/man/screen)(1), [tmux](/man/tmux)(1)"]],"e":["screen","tmux"],"x":"[path/to]/TerrariaServer"},"tlmgr-update":{"n":"tlmgr-update","t":"Update TeX Live packages","s":[["TAGLINE","Update TeX Live packages"],["TLDR","**Update all packages**\n\n```tlmgr update --all```\n\n**Update specific package**\n\n```tlmgr update [package]```\n\n**Check for updates**\n\n```tlmgr update --list```\n\n**Update self**\n\n```tlmgr update --self```"],["SYNOPSIS","**tlmgr update** [_options_] [_package_...]"],["PARAMETERS","**--all**\n> Update all installed packages.\n\n**--self**\n> Update tlmgr itself.\n\n**--list**\n> List available updates.\n\n**--dry-run**\n> Show what would be updated.\n\n**--backup**\n> Backup packages before updating.\n\n**--exclude** _pkg_\n> Exclude package from update."],["DESCRIPTION","**tlmgr update** updates TeX Live packages to latest versions. Can update the entire installation or specific packages. Part of TeX Live Manager."],["SEE ALSO","[tlmgr-install](/man/tlmgr-install)(1), [tlmgr](/man/tlmgr)(1)"]],"e":["tlmgr-install","tlmgr"],"x":"tlmgr update --all"},"tree":{"n":"tree","t":"Display directory contents in tree format","s":[["TAGLINE","Display directory contents in tree format"],["TLDR","**Display directory tree**\n\n```tree```\n\n**Show tree for specific directory**\n\n```tree [path/to/directory]```\n\n**Include hidden files**\n\n```tree -a```\n\n**Show only directories**\n\n```tree -d```\n\n**Limit depth** of tree\n\n```tree -L [2]```\n\n**Show file sizes**\n\n```tree -s```\n\n**Show human-readable sizes**\n\n```tree -h```\n\n**Output in JSON format**\n\n```tree -J```\n\n**Exclude patterns**\n\n```tree -I \"[pattern|pattern2]\"```\n\n**Show full path** for each file\n\n```tree -f```\n\n**Sort by modification time**, newest first\n\n```tree -t -r```\n\n**Show directory sizes and respect .gitignore**\n\n```tree --du --gitignore```"],["SYNOPSIS","**tree** [_options_] [_directory_...]"],["PARAMETERS","**-a**\n> Show all files including hidden\n\n**-d**\n> List directories only\n\n**-L** _level_\n> Limit depth to level\n\n**-f**\n> Print full path prefix\n\n**-s**\n> Print size of each file\n\n**-h**\n> Print sizes in human-readable format\n\n**-D**\n> Print last modification date\n\n**-p**\n> Print file permissions\n\n**-u**\n> Print username\n\n**-g**\n> Print group name\n\n**-I** _pattern_\n> Exclude files matching pattern\n\n**-P** _pattern_\n> List only files matching pattern\n\n**--dirsfirst**\n> List directories before files\n\n**-C**\n> Colorize output\n\n**-n**\n> Turn off colorization\n\n**-r**\n> Sort output in reverse order\n\n**-t**\n> Sort by last modification time\n\n**-o** _filename_\n> Send output to filename\n\n**--du**\n> Show cumulative directory sizes\n\n**--prune**\n> Prune empty directories from output\n\n**--gitignore**\n> Use .gitignore files for filtering\n\n**--noreport**\n> Omit file and directory count at end of listing\n\n**--filelimit** _N_\n> Do not descend directories with more than N entries\n\n**-H** _baseHREF_\n> Output HTML with base URL\n\n**-J**\n> Output JSON\n\n**-X**\n> Output XML"],["DESCRIPTION","**tree** displays the contents of directories in a tree-like format, showing the hierarchical relationship between files and subdirectories visually.\n\nThe default output uses ASCII or Unicode box-drawing characters to represent the tree structure. Colorization helps distinguish file types (directories, executables, symlinks, etc.).\n\nVarious output formats are supported for integration with other tools: JSON (**-J**) for programmatic parsing, XML (**-X**) for structured data, and HTML (**-H**) for web display.\n\nFiltering with **-I** (exclude) and **-P** (include) uses shell glob patterns, supporting wildcards like \\* and ?."],["CAVEATS","Large directories can produce overwhelming output. Use **-L** to limit depth or **-d** for directories only when exploring unfamiliar structures.\n\nTree does not follow symbolic links by default if they would cause recursion. Use **-l** to follow all symlinks.\n\nUsing **--prune** and **--du** causes tree to accumulate the entire tree in memory before output, which may be slow for very large directory trees."],["INSTALL","```apt: sudo apt install tree```\n\n```dnf: sudo dnf install tree```\n\n```pacman: sudo pacman -S tree```\n\n```apk: sudo apk add tree```\n\n```zypper: sudo zypper install tree```\n\n```brew: brew install tree```\n\n```nix: nix profile install nixpkgs#tree```"],["SEE ALSO","[ls](/man/ls)(1), [find](/man/find)(1), [du](/man/du)(1), [exa](/man/exa)(1), [eza](/man/eza)(1), [lsd](/man/lsd)(1)"]],"e":["ls","find","du","exa","eza","lsd"],"x":"tree"},"twurl-accounts":{"n":"twurl-accounts","t":"Manage twurl Twitter API accounts","s":[["TAGLINE","Manage twurl Twitter API accounts"],["TLDR","**List authorized accounts**\n\n```twurl accounts```\n\n**Set default account**\n\n```twurl accounts -d [username]```"],["SYNOPSIS","**twurl accounts** [_options_]"],["PARAMETERS","**-d** _username_\n> Set default account."],["DESCRIPTION","**twurl accounts** manages Twitter/X API accounts in twurl. Lists authorized accounts and switches between them. Part of twurl OAuth-enabled curl for Twitter API."],["INSTALL","```nix: nix profile install nixpkgs#twurl```"],["SEE ALSO","[twurl](/man/twurl)(1), [curl](/man/curl)(1)"]],"e":["twurl","curl"],"x":"twurl accounts"},"uniq":{"n":"uniq","t":"Filter or count adjacent duplicate lines","s":[["TAGLINE","Filter or count adjacent duplicate lines"],["TLDR","**Remove adjacent duplicate lines**\n\n```sort [file] | uniq```\n\n**Show only duplicate lines**\n\n```sort [file] | uniq -d```\n\n**Show only unique lines**\n\n```sort [file] | uniq -u```\n\n**Count occurrences**\n\n```sort [file] | uniq -c```\n\n**Ignore case when comparing**\n\n```sort [file] | uniq -i```\n\n**Skip first N fields** when comparing\n\n```sort [file] | uniq -f [N]```\n\n**Skip first N characters** when comparing\n\n```sort [file] | uniq -s [N]```"],["SYNOPSIS","**uniq** [_options_] [_input_ [_output_]]"],["PARAMETERS","**-c**, **--count**\n> Prefix lines with occurrence count\n\n**-d**, **--repeated**\n> Only print duplicate lines, one per group\n\n**-D**\n> Print all duplicate lines\n\n**-u**, **--unique**\n> Only print unique lines (occurring once)\n\n**-i**, **--ignore-case**\n> Ignore case when comparing\n\n**-f** _N_, **--skip-fields**=_N_\n> Skip first N fields when comparing\n\n**-s** _N_, **--skip-chars**=_N_\n> Skip first N characters when comparing\n\n**-w** _N_, **--check-chars**=_N_\n> Compare only first N characters\n\n**-z**, **--zero-terminated**\n> Line delimiter is NUL, not newline"],["DESCRIPTION","**uniq** filters adjacent matching lines from input, writing unique lines to output. It only compares consecutive lines, so input typically needs to be sorted first.\n\nCommonly used with **sort** in a pipeline: **sort file | uniq** removes all duplicates. Adding **-c** shows frequency counts, useful for analyzing log files or finding common patterns.\n\nThe **-d** option shows only lines that appear more than once, while **-u** shows only lines appearing exactly once. These are mutually exclusive perspectives on the data.\n\nField and character skipping options allow ignoring prefixes (like timestamps or line numbers) when comparing for uniqueness."],["CAVEATS","Uniq only removes **adjacent** duplicates. Without sorting first, non-adjacent duplicates remain. Always use **sort | uniq** or **sort -u** for true deduplication.\n\n**sort -u** is often more efficient than **sort | uniq** as it removes duplicates during sorting rather than in a separate pass.\n\nThe count option (**-c**) prefixes with spaces and count, which may need processing for further use."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[sort](/man/sort)(1), [comm](/man/comm)(1), [cut](/man/cut)(1), [wc](/man/wc)(1)"]],"e":["sort","comm","cut","wc"],"x":"sort [file] | uniq"},"unset":{"n":"unset","t":"Remove shell variables or functions","s":[["TAGLINE","Remove shell variables or functions"],["TLDR","**Unset variable**\n\n```unset [VARIABLE]```\n\n**Unset function**\n\n```unset -f [function_name]```\n\n**Unset multiple variables**\n\n```unset [VAR1] [VAR2]```\n\n**Unset variable explicitly (not function)**\n\n```unset -v [VARIABLE]```"],["SYNOPSIS","**unset** [_-f_] [_-v_] _name_ ..."],["PARAMETERS","**-f**\n> Unset function.\n\n**-v**\n> Unset variable (default).\n\n**-n**\n> Unset the nameref variable itself rather than the variable it references.\n\n_name_\n> Name(s) to unset."],["DESCRIPTION","**unset** is a shell builtin that removes variables and function definitions from the current shell environment. By default it removes variables, but with the **-f** flag it removes function definitions instead.\n\nUnsetting a variable removes it completely from the environment, unlike setting it to an empty string which leaves the variable defined but empty. This distinction matters for scripts that check whether a variable exists versus whether it has a value. Unsetting exported variables also removes them from the environment inherited by child processes.\n\nChanges made by unset only affect the current shell session. Variables and functions defined in shell startup files like ~/.bashrc will be restored when a new shell session starts. Read-only variables cannot be unset."],["CAVEATS","Shell builtin (POSIX). Cannot unset readonly variables (returns error). Changes only affect the current shell session. The -n option is a bash extension, not available in all POSIX shells."],["HISTORY","**unset** is a standard shell builtin command for removing variables and functions from the shell environment."],["SEE ALSO","[export](/man/export)(1), [set](/man/set)(1), [env](/man/env)(1)"]],"e":["export","set","env"],"x":"unset [VARIABLE]"},"vboxmanage-showvminfo":{"n":"vboxmanage-showvminfo","t":"Display VirtualBox VM configuration details","s":[["TAGLINE","Display VirtualBox VM configuration details"],["TLDR","**Show VM info**\n\n```vboxmanage showvminfo [vmname]```\n\n**Show as machine-readable**\n\n```vboxmanage showvminfo [vmname] --machinereadable```\n\n**Show specific details**\n\n```vboxmanage showvminfo [vmname] --details```"],["SYNOPSIS","**vboxmanage** **showvminfo** _vmname_ [_options_]"],["PARAMETERS","**--machinereadable**\n> Output parseable format.\n\n**--details**\n> Show detailed info.\n\n**--log** _n_\n> Show log file n."],["DESCRIPTION","**vboxmanage showvminfo** displays detailed VM configuration. Shows hardware settings, state, snapshots, and shared folders. Use --machinereadable for scripting."],["SEE ALSO","[vboxmanage](/man/vboxmanage)(1), [vboxmanage-list](/man/vboxmanage-list)(1)"]],"e":["vboxmanage","vboxmanage-list"],"x":"vboxmanage showvminfo [vmname]"},"virtualenv":{"n":"virtualenv","t":"Create isolated Python environments","s":[["TAGLINE","Create isolated Python environments"],["TLDR","**Create a virtual environment**\n\n```virtualenv [venv]```\n\n**Create with specific Python**\n\n```virtualenv -p [python3.11] [venv]```\n\n**Create without pip**\n\n```virtualenv --no-pip [venv]```\n\n**Create with system packages**\n\n```virtualenv --system-site-packages [venv]```\n\n**Activate the environment** (bash/zsh)\n\n```source [venv]/bin/activate```\n\n**Activate the environment** (fish)\n\n```source [venv]/bin/activate.fish```\n\n**Deactivate the environment**\n\n```deactivate```\n\n**Create with latest seed packages**\n\n```virtualenv --download [venv]```"],["SYNOPSIS","**virtualenv** [_options_] _dest_"],["DESCRIPTION","**virtualenv** creates isolated Python environments. Each environment has its own Python binary and independent set of installed packages, allowing different projects to have different dependencies without conflicts.\n\nWhen activated, the virtual environment modifies PATH to use the environment's Python and pip, isolating package installations from the system Python. This is essential for reproducible development environments.\n\nvirtualenv is the original virtual environment tool for Python, predating the built-in venv module. It offers additional features like choosing Python versions and faster environment creation."],["PARAMETERS","**-p** _python_, **--python=** _python_\n> Python interpreter to use.\n\n**--system-site-packages**\n> Give access to system site-packages.\n\n**--no-pip**\n> Don't install pip.\n\n**--no-setuptools**\n> Don't install setuptools.\n\n**--no-wheel**\n> Don't install wheel.\n\n**--download**\n> Download latest pip/setuptools/wheel.\n\n**--no-download**\n> Use bundled pip/setuptools/wheel.\n\n**--clear**\n> Clear existing environment.\n\n**--copies**\n> Use copies instead of symlinks.\n\n**--prompt** _text_\n> Custom prompt prefix for the activated environment.\n\n**--activators** _list_\n> Activators to generate (comma-separated: bash, fish, cshell, powershell, etc.).\n\n**-v**, **--verbose**\n> Increase verbosity.\n\n**-q**, **--quiet**\n> Decrease verbosity."],["CAVEATS","Environment must be activated in each new shell. Absolute paths are embedded in the environment, making it non-relocatable. Python version must exist on system. Consider using python -m venv for basic needs."],["HISTORY","**virtualenv** was created by **Ian Bicking** in **2007** to address Python's lack of built-in environment isolation. It became the standard tool for Python development before venv was added to the standard library in Python 3.3. virtualenv remains popular due to additional features and Python 2 support (historical)."],["INSTALL","```apk: sudo apk add py3-virtualenv```\n\n```brew: brew install virtualenv```\n\n```nix: nix profile install nixpkgs#virtualenv```"],["SEE ALSO","[python](/man/python)(1), [pip](/man/pip)(1), [pyenv](/man/pyenv)(1), [venv](/man/venv)(1)"]],"e":["python","pip","pyenv","venv"],"x":"virtualenv [venv]"},"waifu2x-ncnn-vulkan":{"n":"waifu2x-ncnn-vulkan","t":"AI-powered image upscaling with Vulkan","s":[["TAGLINE","AI-powered image upscaling with Vulkan"],["TLDR","**Upscale image**\n\n```waifu2x-ncnn-vulkan -i [input.png] -o [output.png]```\n\n**2x scale**\n\n```waifu2x-ncnn-vulkan -i [input.png] -o [output.png] -s [2]```\n\n**Denoise**\n\n```waifu2x-ncnn-vulkan -i [input.png] -o [output.png] -n [2]```\n\n**Use specific GPU**\n\n```waifu2x-ncnn-vulkan -i [input] -o [output] -g [0]```\n\n**Tile size**\n\n```waifu2x-ncnn-vulkan -i [input] -o [output] -t [400]```"],["SYNOPSIS","**waifu2x-ncnn-vulkan** [_-i input_] [_-o output_] [_-s scale_] [_-n noise_]"],["PARAMETERS","**-i** _FILE_\n> Input file.\n\n**-o** _FILE_\n> Output file.\n\n**-s** _SCALE_\n> Scale ratio (2).\n\n**-n** _LEVEL_\n> Noise level (-1,0,1,2,3).\n\n**-g** _GPU_\n> GPU device.\n\n**-t** _SIZE_\n> Tile size."],["DESCRIPTION","**waifu2x-ncnn-vulkan** is an AI-powered image upscaling and denoising tool that uses convolutional neural networks to enlarge images while preserving detail. It is particularly effective for anime-style artwork and illustrations, producing cleaner results than traditional interpolation methods.\n\nThe tool supports 2x upscaling and multiple noise reduction levels for removing JPEG compression artifacts. It uses the ncnn neural network inference framework with Vulkan GPU acceleration, enabling fast processing on a wide range of graphics hardware without requiring CUDA or specific vendor drivers.\n\nProcessing can be tuned with tile size options to manage GPU memory usage for large images, and specific GPUs can be selected on multi-GPU systems. Input and output support common image formats including PNG, JPEG, and WebP."],["CAVEATS","GPU recommended. Best for anime/illustrations. Large VRAM for big images."],["HISTORY","**waifu2x-ncnn-vulkan** is a Vulkan implementation of **waifu2x**, an AI image upscaler originally by nagadomi."],["INSTALL","```pacman: sudo pacman -S waifu2x-ncnn-vulkan```"],["SEE ALSO","[imagemagick](/man/imagemagick)(1), [realsr-ncnn-vulkan](/man/realsr-ncnn-vulkan)(1)"]],"e":["imagemagick","realsr-ncnn-vulkan"],"x":"waifu2x-ncnn-vulkan -i [input.png] -o [output.png]"},"wol":{"n":"wol","t":"Send Wake-on-LAN magic packets","s":[["TAGLINE","Send Wake-on-LAN magic packets"],["TLDR","Send **WoL** packet\n\n```wol [mac_address]```\n\nSend to different **subnet** by IP\n\n```wol -i [ip_address] [mac_address]```\n\nSend to different **subnet** by hostname\n\n```wol -h [hostname] [mac_address]```\n\nSpecify **port**\n\n```wol -p [port_number] [mac_address]```\n\nRead from **file**\n\n```wol -f [path/to/file]```\n\n**Verbose** output\n\n```wol -v [mac_address]```"],["SYNOPSIS","**wol** [_OPTIONS_] _MAC_ADDRESS_"],["PARAMETERS","**-i, --ipaddr** _IP_\n> Send to specified IP address\n\n**-h, --host** _HOSTNAME_\n> Send to specified hostname\n\n**-p, --port** _PORT_\n> Use specified port\n\n**-f, --file** _FILE_\n> Read addresses from file\n\n**-v, --verbose**\n> Verbose output"],["DESCRIPTION","**wol** sends Wake-on-LAN magic packets to wake up computers remotely. The target computer must have WoL enabled in BIOS and network interface configuration.\n\nMagic packets are broadcast on the local network by default. For remote networks, specify the target IP or hostname."],["CAVEATS","Target machine must support and have WoL enabled. May not work across routers without special configuration. MAC address must be known."],["INSTALL","```dnf: sudo dnf install wol```\n\n```pacman: sudo pacman -S wol```\n\n```zypper: sudo zypper install wol```\n\n```nix: nix profile install nixpkgs#wol```"],["SEE ALSO","[etherwake](/man/etherwake)(8), [wakeonlan](/man/wakeonlan)(1)"]],"e":["etherwake","wakeonlan"],"x":"wol [mac_address]"},"wuzz":{"n":"wuzz","t":"Interactive terminal HTTP client","s":[["TAGLINE","Interactive terminal HTTP client"],["TLDR","**Start interactive HTTP client**\n\n```wuzz```\n\n**Start with a URL pre-filled**\n\n```wuzz [https://api.example.com/users]```\n\n**Start with custom headers** (cURL-compatible syntax)\n\n```wuzz -H \"[Content-Type: application/json]\" [https://api.example.com]```\n\n**Start with POST data**\n\n```wuzz -X POST -d '{\"key\":\"value\"}' [https://api.example.com]```\n\n**Start with a config file**\n\n```wuzz -c [~/.wuzz/config.toml]```"],["SYNOPSIS","**wuzz** [**-H** _header_]... [**-d** _data_] [**-X** _method_] [**-t** _msecs_] [**-c** _config_] [_URL_]"],["PARAMETERS","**-H**, **--header** _HEADER_\n> Set a request header (can be specified multiple times).\n\n**-d**, **--data** _DATA_\n> Set the request body data.\n\n**-X**, **--request** _METHOD_\n> Set the HTTP method (GET, POST, PUT, etc.).\n\n**-t**, **--timeout** _MSECS_\n> Request timeout in milliseconds.\n\n**-F**, **--form** _FIELD_\n> Set multipart form data.\n\n**-c**, **--config** _FILE_\n> Use the specified configuration file.\n\n**--insecure**\n> Skip TLS certificate verification."],["DESCRIPTION","**wuzz** is an interactive HTTP inspection tool with a terminal user interface. Command line arguments are compatible with cURL syntax, so requests can be copied from browser network inspectors using \"copy as cURL\".\n\nThe interface has separate panels for URL, method, headers, request body, query parameters, and response. Navigation uses **Tab**/**Shift+Tab** between panels and **Ctrl+R** to send the request. **Ctrl+S** saves the response, **Ctrl+E** saves the request, **Ctrl+F** loads a saved request, and **Alt+H** toggles history. **F2-F9** jump to specific panels."],["CAVEATS","Terminal UI only. Configuration file uses TOML format (default: $XDG_CONFIG_HOME/wuzz/config.toml on Linux, ~/.wuzz/config.toml elsewhere)."],["HISTORY","**wuzz** was created by **asciimoo** as an interactive command-line HTTP inspection tool, written in Go. Available at https://github.com/asciimoo/wuzz."],["INSTALL","```apt: sudo apt install wuzz```\n\n```apk: sudo apk add wuzz```\n\n```nix: nix profile install nixpkgs#wuzz```"],["SEE ALSO","[curl](/man/curl)(1), [httpie](/man/httpie)(1), [postman](/man/postman)(1)"]],"e":["curl","httpie","postman"],"x":"wuzz"},"yacas":{"n":"yacas","t":"Free computer algebra system","s":[["TAGLINE","Free computer algebra system"],["TLDR","**Start calculator**\n\n```yacas```\n\n**Evaluate expression**\n\n```yacas -c \"[Solve(x^2-4==0,x)]\"```\n\n**Run script**\n\n```yacas [script.ys]```\n\n**Quiet mode**\n\n```yacas -p```"],["SYNOPSIS","**yacas** [_-c expr_] [_-p_] [_options_] [_file_]"],["PARAMETERS","**-c** _EXPR_\n> Evaluate expression.\n\n**-p**\n> Plain output.\n\n**-d** _DIR_\n> Scripts directory.\n\n**--help**\n> Show help."],["DESCRIPTION","**yacas** (Yet Another Computer Algebra System) is a free symbolic mathematics program that provides an interactive environment for algebraic computation. It supports symbolic manipulation of expressions, equation solving, calculus operations including derivatives and integrals, and linear algebra.\n\nThe system includes its own scripting language for defining custom functions and mathematical procedures. It can be used interactively at the command line or by running script files, and supports both exact symbolic computation and numerical evaluation."],["CAVEATS","Learning curve. Own syntax. Not as full as Mathematica."],["HISTORY","**Yacas** (Yet Another Computer Algebra System) was created as a free computer algebra system."],["INSTALL","```apt: sudo apt install yacas```\n\n```brew: brew install yacas```\n\n```nix: nix profile install nixpkgs#yacas```"],["SEE ALSO","[maxima](/man/maxima)(1), [octave](/man/octave)(1), [bc](/man/bc)(1)"]],"e":["maxima","octave","bc"],"x":"yacas"}});
