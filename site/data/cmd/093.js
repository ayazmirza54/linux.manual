window.LCL_BUCKET(93,{"adguardhome":{"n":"adguardhome","t":"Network-wide ad and tracker blocking DNS server","s":[["TAGLINE","Network-wide ad and tracker blocking DNS server"],["TLDR","**Start** AdGuard Home\n\n```sudo adguardhome -s start```\n\n**Install** as a system service\n\n```sudo adguardhome -s install```\n\n**Uninstall** the system service\n\n```sudo adguardhome -s uninstall```\n\nCheck service **status**\n\n```sudo adguardhome -s status```\n\nStart with **custom config**\n\n```adguardhome -c [/path/to/config.yaml]```"],["SYNOPSIS","**adguardhome** [_-c config_] [_-w workdir_] [_-h host_] [_-p port_] [_-s command_]"],["DESCRIPTION","**AdGuard Home** is a network-wide software for blocking ads, tracking, and malicious domains. It operates as a DNS server, filtering requests based on blocklists and custom rules before forwarding legitimate queries upstream.\n\nThe application provides a web interface for configuration and statistics, supports DNS-over-HTTPS, DNS-over-TLS, and DNS-over-QUIC, and can replace your router's DHCP server."],["PARAMETERS","**-s** _command_\n> Service control: start, stop, restart, status, install, uninstall\n\n**-c** _path_\n> Path to configuration file\n\n**-w** _path_\n> Path to working directory\n\n**-h** _host_\n> Web interface bind host\n\n**-p** _port_\n> Web interface bind port\n\n**--no-check-update**\n> Don't check for updates\n\n**--pidfile** _path_\n> Path to PID file\n\n**--verbose**\n> Enable verbose logging"],["CONFIGURATION","**AdGuardHome.yaml**\n> Main configuration file, typically located in the working directory (e.g. /opt/AdGuardHome/). Controls DNS settings, upstream servers, filtering rules, DHCP, clients, and web interface options."],["CAVEATS","Running as a DNS server requires port 53, which may conflict with systemd-resolved or other DNS services. First-time setup requires accessing the web interface to complete configuration."],["HISTORY","**AdGuard Home** was developed by AdGuard Software Limited, first released in **2018** as an open-source network-wide ad blocker. It was designed as a self-hosted alternative to Pi-hole with additional features like encrypted DNS protocols."],["INSTALL","```pacman: sudo pacman -S adguardhome```\n\n```apk: sudo apk add adguardhome```\n\n```nix: nix profile install nixpkgs#adguardhome```"],["SEE ALSO","[pihole](/man/pihole)(1), [dnsmasq](/man/dnsmasq)(1), [unbound](/man/unbound)(1)"]],"e":["pihole","dnsmasq","unbound"],"x":"sudo adguardhome -s start"},"alpine":{"n":"alpine","t":"Terminal-based email and news client.","s":[["TAGLINE","Terminal-based email and news client."],["TLDR","Open **alpine** email client\n\n```alpine```\n\nOpen alpine directly to **compose** an email\n\n```alpine [email@example.net]```\n\n**Quit** alpine\n\n```q y```"],["SYNOPSIS","**alpine** [_options_] [_address_]"],["DESCRIPTION","**alpine** is an email client and Usenet newsgroup program with a pico/nano-inspired interface. It supports most modern email services through IMAP and is designed for ease of use with a menu-driven interface.\n\nAlpine is the successor to Pine and was developed by the University of Washington."],["PARAMETERS","**address**\n> Open directly to message composition screen to send to specified address\n\n**-i**\n> Start up in the FOLDER INDEX screen instead of the main menu.\n\n**-f** _folder_\n> Open _folder_ instead of INBOX (in the first folder collection).\n\n**-c** _number_\n> Folder collection number for the **-f** argument.\n\n**-d** _debug-level_\n> Output diagnostic information at debug level 0 to 9.\n\n**-p** _config-file_\n> Use _config-file_ as the personal configuration file instead of ~/.pinerc.\n\n**-n** _number_\n> Start with the message numbered _number_ as the current message.\n\n**-z**\n> Enable use of suspend (Ctrl-Z) to put Alpine in the background."],["CONFIGURATION","Configuration is normally edited interactively with the **Setup** command (S) from the main menu, which writes changes back to the personal configuration file.\n\n**~/.pinerc**\n> Personal configuration file for Alpine. Stores mail server settings, folder collections, display preferences, and feature flags.\n\n**~/.addressbook**\n> Personal address book.\n\n**/etc/pine.conf**\n> System-wide default configuration.\n\n**/etc/pine.conf.fixed**\n> System-wide non-overridable configuration set by the administrator."],["CAVEATS","Configuration is stored in ~/.pinerc by default. Alpine may require additional setup for OAuth2 authentication with modern email providers like Gmail."],["HISTORY","Alpine was developed by the **University of Washington** as a successor to **Pine** (Program for Internet News & Email). First released in **2007**, it was designed to address licensing concerns with Pine."],["INSTALL","```apt: sudo apt install alpine```\n\n```dnf: sudo dnf install alpine```\n\n```aur: yay -S alpine```\n\n```apk: sudo apk add alpine```\n\n```zypper: sudo zypper install alpine```\n\n```brew: brew install alpine```\n\n```nix: nix profile install nixpkgs#alpine```"],["SEE ALSO","[mutt](/man/mutt)(1), [mail](/man/mail)(1), [pico](/man/pico)(1), [nano](/man/nano)(1), [pine](/man/pine)(1)"],["RESOURCES","```[Source code](https://repo.or.cz/alpine.git)```\n\n```[Homepage](https://alpineapp.email/)```"]],"e":["mutt","mail","pico","nano","pine"],"x":"alpine"},"atq":{"n":"atq","t":"List pending jobs in the at queue","s":[["TAGLINE","List pending jobs in the at queue"],["TLDR","**List** pending jobs\n\n```atq```\n\nList jobs in a **specific queue**\n\n```atq -q [a]```\n\nCustom **time format** for listed jobs\n\n```atq -o [%Y-%m-%d %H:%M]```"],["SYNOPSIS","**atq** [_-V_] [_-q queue_] [_-o fmt_]"],["DESCRIPTION","**atq** lists jobs pending in the at queue. It shows job numbers, execution times, and queue designations for scheduled one-time jobs.\n\nThis is equivalent to **at -l**."],["PARAMETERS","**-q** _queue_\n> Show only jobs in the specified queue. A queue designation consists of a single letter (a-z or A-Z). The default queue for **at** jobs is **a** and for **batch** jobs is **b**.\n\n**-o** _fmt_\n> Use the specified **strftime**-style format for the date and time shown in the listing.\n\n**-V**\n> Print the version number to standard error and exit."],["OUTPUT FORMAT","Each line shows the job number, the scheduled execution date and time, the queue letter, and the owning username."],["CAVEATS","Non-root users can only see their own jobs; the superuser sees everyone's jobs. Jobs do not run while the **atd** daemon is not running, and past jobs that could not run are still listed until removed."],["HISTORY","**atq** is part of the at command suite, scheduling deferred command execution in Unix since the **1970s**."],["INSTALL","```apt: sudo apt install at```\n\n```dnf: sudo dnf install at```\n\n```pacman: sudo pacman -S at```\n\n```apk: sudo apk add at```\n\n```zypper: sudo zypper install at```\n\n```nix: nix profile install nixpkgs#at```"],["SEE ALSO","[at](/man/at)(1), [atrm](/man/atrm)(1), [atd](/man/atd)(8), [cron](/man/cron)(8)"],["RESOURCES","```[Source code](https://salsa.debian.org/debian/at)```"]],"e":["at","atrm","atd","cron"],"x":"atq"},"blink":{"n":"blink","t":"blink(1) USB RGB LED controller command-line tool","s":[["TAGLINE","blink(1) USB RGB LED controller command-line tool"],["TLDR","Set the **blink(1) to red**\n\n```blink1-tool --red```\n\nSet to a specific **hex color**\n\n```blink1-tool --rgb [ff00ff]```\n\n**Blink** the LED a number of times\n\n```blink1-tool --blink [3]```\n\n**Fade** to a color over a duration in milliseconds\n\n```blink1-tool --rgb [00ff00] --fade [500]```\n\n**Turn off** the LED\n\n```blink1-tool --off```\n\nSet LED **pattern** to play\n\n```blink1-tool --play [1]```\n\nList **connected blink(1) devices**\n\n```blink1-tool --list```"],["SYNOPSIS","**blink1-tool** [_options_]"],["PARAMETERS","**--rgb** _RRGGBB_\n> Set color by hex RGB value.\n\n**--red**\n> Set LED to red.\n\n**--green**\n> Set LED to green.\n\n**--blue**\n> Set LED to blue.\n\n**--off**\n> Turn off the LED.\n\n**--blink** _N_\n> Blink the LED _N_ times.\n\n**--fade** _MILLIS_\n> Fade to color over _MILLIS_ milliseconds.\n\n**--play** _N_\n> Play stored pattern _N_.\n\n**--list**\n> List connected blink(1) devices.\n\n**--savergb** _RRGGBB_,_POS_\n> Save RGB color to position in pattern.\n\n**--savepattern**\n> Save the current pattern to the device.\n\n**-l** _N_, **--led** _N_\n> Select which LED to address (0=all, 1=top, 2=bottom) on blink(1) mk2+.\n\n**-q**, **--quiet**\n> Suppress output."],["DESCRIPTION","**blink1-tool** is the command-line interface for controlling **blink(1)** USB RGB LED indicator lights made by ThingM. It can set colors, create blink patterns, fade between colors, and manage stored light patterns on the device.\n\nThe blink(1) device is a small USB dongle containing a programmable RGB LED. It is commonly used for build status indicators, notification lights, ambient information displays, and visual alerts triggered by scripts or monitoring systems.\n\nThe tool communicates directly with the blink(1) hardware over USB HID, requiring no special drivers. Multiple devices can be addressed independently."],["CAVEATS","Requires a physical blink(1) USB device to be connected. On Linux, udev rules may need to be configured for non-root access. The mk1 hardware supports only a single LED, while mk2 and later support two independently addressable LEDs."],["HISTORY","The **blink(1)** was created by **Tod Kurt** of **ThingM** and successfully funded on **Kickstarter in 2012**. The blink1-tool command-line utility is part of the open-source blink1 software suite hosted on GitHub, supporting Linux, macOS, and Windows."],["INSTALL","```brew: brew install blink```\n\n```nix: nix profile install nixpkgs#blink```"],["SEE ALSO","[lsusb](/man/lsusb)(8)"]],"e":["lsusb"],"x":"blink1-tool --red"},"brittany":{"n":"brittany","t":"Format Haskell source code","s":[["TAGLINE","Format Haskell source code"],["TLDR","**Format file and output to stdout**\n\n```brittany [source.hs]```\n\n**Format from stdin**\n\n```cat [source.hs] | brittany```\n\n**Format files in place**\n\n```brittany --write-mode=inplace [*.hs]```\n\n**Check formatting without modifying**\n\n```brittany --check-mode [source.hs]```\n\n**Use specific config file**\n\n```brittany --config-file [brittany.yaml] [source.hs]```\n\n**Set column limit**\n\n```brittany --columns [100] [source.hs]```"],["SYNOPSIS","**brittany** [_options_] [_files_]"],["DESCRIPTION","**brittany** is a Haskell source code formatter that focuses on horizontal space utilization. It reformats code to make maximal use of available width while avoiding lines that are too long.\n\nThe formatter uses ghc-exactprint as the parser, supporting full GHC Haskell including extensions."],["PARAMETERS","**--write-mode** _mode_\n> Output mode: stdout, inplace, or display\n\n**--check-mode**\n> Exit with error if formatting would change\n\n**--columns** _n_\n> Maximum line width (default: 80)\n\n**--indent** _n_\n> Indentation width (default: 2)\n\n**--config-file** _file_\n> Use specific configuration file\n\n**--no-user-config**\n> Ignore user configuration file"],["CONFIGURATION","Default user config: **~/.config/brittany/config.yaml**\n\nProject config: First **brittany.yaml** found in current or parent directories.\n\nGHC extensions can be enabled in config:\n```yaml\nconf_forward:\n  options_ghc:\n    - -XLambdaCase\n    - -XMultiWayIf\n```"],["CAVEATS","Some less common Haskell syntax elements are not fully supported. In-source comments may occasionally be moved or lost. This project is effectively unmaintained; consider using Ormolu or Fourmolu instead."],["INSTALL","```aur: yay -S brittany```"],["SEE ALSO","[ghc](/man/ghc)(1)"]],"e":["ghc"],"x":"brittany [source.hs]"},"bun-pm-pack":{"n":"bun-pm-pack","t":"Create a tarball of the package","s":[["TAGLINE","Create a tarball of the package"],["TLDR","**Pack the current package** into a .tgz archive\n\n```bun pm pack```\n\n**Pack without writing** to disk (dry run)\n\n```bun pm pack --dry-run```\n\n**Pack to a specific directory**\n\n```bun pm pack --destination [path/to/dir]```\n\n**Pack with a custom filename**\n\n```bun pm pack --filename [package.tgz]```\n\n**Pack with minimal output** (only print tarball name)\n\n```bun pm pack --quiet```"],["SYNOPSIS","**bun** **pm** **pack** [**--dry-run**] [**--destination** _dir_] [**--filename** _name_] [**--ignore-scripts**] [**--gzip-level** _0-9_] [**--quiet**]"],["PARAMETERS","**--dry-run**\n> Perform all tasks except writing the tarball to disk.\n\n**--destination** _dir_\n> Specify an output directory for the tarball. Cannot be combined with **--filename**.\n\n**--filename** _name_\n> Set the exact filename for the tarball. Cannot be combined with **--destination**.\n\n**--ignore-scripts**\n> Skip running prepack, postpack, and prepare scripts.\n\n**--gzip-level** _0-9_\n> Set gzip compression level. Default is **9** (maximum compression).\n\n**--quiet**\n> Only output the tarball filename, suppressing verbose output."],["DESCRIPTION","**bun pm pack** creates a compressed .tgz archive containing all files that would be published to npm, following the same inclusion and exclusion rules as npm pack. This is useful for inspecting what will be published before running **bun publish**, testing local packages, or sharing packages without a registry.\n\nThe command respects the **files** field in package.json, **.npmignore**, and **.gitignore** when determining which files to include."],["SEE ALSO","[bun](/man/bun)(1), [bun-pm](/man/bun-pm)(1), [bun-publish](/man/bun-publish)(1)"]],"e":["bun","bun-pm","bun-publish"],"x":"bun pm pack"},"bun-pm":{"n":"bun-pm","t":"Bun package manager utilities","s":[["TAGLINE","Bun package manager utilities"],["TLDR","**Show the bin directory path**\n\n```bun pm bin```\n\n**Show the global bin directory path**\n\n```bun pm bin -g```\n\n**List installed packages**\n\n```bun pm ls```\n\n**List all installed packages including dependencies**\n\n```bun pm ls --all```\n\n**Print the package manager version**\n\n```bun pm version```\n\n**Migrate from npm or yarn to Bun**\n\n```bun pm migrate```\n\n**Generate or update the lockfile without installing**\n\n```bun pm hash```"],["SYNOPSIS","**bun** **pm** _subcommand_ [_options_]"],["SUBCOMMANDS","**bin**\n> Print the path to the bin folder. Use **-g** for the global bin directory.\n\n**ls**\n> List installed packages. Use **--all** to include transitive dependencies.\n\n**version**\n> Print the package manager version.\n\n**migrate**\n> Migrate from npm or yarn to Bun by converting lockfiles.\n\n**hash**\n> Print the hash of the lockfile.\n\n**hash-print**\n> Print the hash stored in the lockfile.\n\n**hash-string**\n> Print the string used to compute the lockfile hash.\n\n**cache**\n> Print the path to the global module cache, or clear it with **rm**."],["DESCRIPTION","**bun pm** provides package manager utilities for managing dependencies and package configuration. It offers commands for inspecting installations, migrating projects from npm/yarn, and working with the lockfile and cache."],["INSTALL","```pacman: sudo pacman -S bun```\n\n```brew: brew install bun```\n\n```nix: nix profile install nixpkgs#bun```"],["SEE ALSO","[bun](/man/bun)(1), [bun-install](/man/bun-install)(1), [bun-pm-ls](/man/bun-pm-ls)(1), [npm](/man/npm)(1)"]],"e":["bun","bun-install","bun-pm-ls","npm"],"x":"bun pm bin"},"bun-run":{"n":"bun-run","t":"Execute files and package scripts","s":[["TAGLINE","Execute files and package scripts"],["TLDR","**Run a JavaScript/TypeScript file**\n\n```bun run [script.ts]```\n\n**Run a package.json script**\n\n```bun run [start]```\n\n**Run file in watch mode**\n\n```bun --watch run [script.ts]```\n\n**List available scripts**\n\n```bun run```\n\n**Evaluate code string**\n\n```bun run --eval \"[console.log('hello')]\"```\n\n**Evaluate and print result**\n\n```bun run --print \"[1 + 2]\"```\n\n**Run with environment file**\n\n```bun run --env-file [.env] [script.ts]```"],["SYNOPSIS","**bun run** [_options_] _file|script_ [_arguments_]\n\n**bun** _file|script_ [_arguments_]"],["DESCRIPTION","**bun run** executes JavaScript, TypeScript, and JSX files using Bun's fast native transpiler. It can also run package.json scripts with lifecycle hook support.\n\nThe command can be shortened to just **bun** followed by the file or script name, though built-in commands take precedence."],["PARAMETERS","**--watch**\n> Re-run on file changes. Must be placed before `run` in the command.\n\n**--hot**\n> Enable hot reloading instead of full restart on file changes.\n\n**--eval, -e** _code_\n> Evaluate argument as script\n\n**--print, -p** _code_\n> Evaluate and print result\n\n**--env-file** _file_\n> Load environment variables from file\n\n**--cwd** _directory_\n> Set working directory\n\n**--silent**\n> Suppress script output\n\n**--smol**\n> Reduce memory usage at the cost of performance by running the garbage collector more frequently.\n\n**--if-present**\n> Exit without error if the entrypoint or script does not exist.\n\n**--filter** _pattern_\n> Run scripts in matching workspace packages by name pattern.\n\n**--bun**\n> Force the script to run through Bun's runtime, overriding shebang lines."],["BEHAVIOR","**Source files**\n> Paths starting with ./ or absolute paths run as source files\n\n**Package scripts**\n> Scripts defined in package.json are executed with lifecycle hooks (pre/post)\n\n**Resolution**\n> With bun run, package.json scripts take precedence over files with same name"],["LIFECYCLE HOOKS","Bun respects pre and post lifecycle hooks. Running **bun run build** will execute **prebuild**, **build**, and **postbuild** in order. If a pre-script fails, the main script is not executed."],["CAVEATS","TypeScript and JSX are transpiled on-the-fly without configuration. Startup time is significantly faster than npm run. Some Node.js-specific APIs may not be available."],["INSTALL","```pacman: sudo pacman -S bun```\n\n```brew: brew install bun```\n\n```nix: nix profile install nixpkgs#bun```"],["SEE ALSO","[bun](/man/bun)(1), [bun-test](/man/bun-test)(1), [bun-build](/man/bun-build)(1), [node](/man/node)(1), [npm-run](/man/npm-run)(1)"]],"e":["bun","bun-test","bun-build","node","npm-run"],"x":"bun run [script.ts]"},"cake":{"n":"cake","t":"Cross-platform build automation using C#","s":[["TAGLINE","Cross-platform build automation using C#"],["TLDR","**Run default build script**\n\n```dotnet cake```\n\n**Run specific build script**\n\n```dotnet cake [build.cake]```\n\n**Run specific target**\n\n```dotnet cake --target=[Clean]```\n\n**Run with verbosity**\n\n```dotnet cake --verbosity=[diagnostic]```\n\n**Show available tasks**\n\n```dotnet cake --showdescription```\n\n**Dry run without executing**\n\n```dotnet cake --dryrun```\n\n**Run with custom argument**\n\n```dotnet cake --configuration=[Release]```"],["SYNOPSIS","**dotnet cake** [_script_] [_options_]"],["DESCRIPTION","**Cake** (C# Make) is a cross-platform build automation system with a C# DSL. It compiles code, copies files, runs tests, compresses files, and builds NuGet packages. Scripts are written in C# and executed via Roslyn."],["PARAMETERS","**--target**=_name_\n> Target task to run\n\n**--verbosity**=_level_\n> Output verbosity (quiet, minimal, normal, verbose, diagnostic)\n\n**--dryrun**\n> Show tasks without executing\n\n**--showdescription**\n> Show task descriptions\n\n**--showtree**\n> Show task dependency tree\n\n**--configuration**=_config_\n> Build configuration (Debug, Release)\n\n**--exclusive**\n> Execute only target task without dependencies\n\n**--version**\n> Show version\n\n**--help**\n> Show help"],["INSTALLATION","```\ndotnet new tool-manifest\ndotnet tool install Cake.Tool\n```"],["CAVEATS","Requires .NET SDK installed. By convention, executes build.cake in current directory. Supports MSBuild, .NET CLI, NuGet, and many other tools through addins."],["INSTALL","```brew: brew install cake```"],["SEE ALSO","[dotnet](/man/dotnet)(1), [msbuild](/man/msbuild)(1), [make](/man/make)(1)"]],"e":["dotnet","msbuild","make"],"x":"dotnet cake"},"cksum":{"n":"cksum","t":"file checksum and byte count calculator","s":[["TAGLINE","file checksum and byte count calculator"],["TLDR","**Calculate checksum of file**\n\n```cksum [file.txt]```\n\n**Calculate checksum of multiple files**\n\n```cksum [file1] [file2]```\n\n**Calculate from stdin**\n\n```echo \"data\" | cksum```\n\n**Use SHA256 algorithm**\n\n```cksum --algorithm sha256 [file.txt]```\n\n**Verify checksums from file**\n\n```cksum -c [checksums.txt]```\n\n**Output in base64**\n\n```cksum --base64 [file.txt]```"],["SYNOPSIS","**cksum** [_option_]... [_file_]..."],["DESCRIPTION","**cksum** computes a cyclic redundancy check (CRC) checksum and byte count for each specified file, or for standard input if no file is given. It is part of GNU coreutils and is commonly used to verify file integrity after transfers or copies.\n\nBy default, cksum uses the POSIX-standard 32-bit CRC algorithm. Modern versions of cksum (GNU coreutils 9.0+) support additional algorithms including MD5, SHA1, SHA256, and SHA512 via the **--algorithm** option, making it a versatile replacement for separate checksum utilities.\n\nThe output format includes the checksum value, byte count, and filename. When using the **--check** option, cksum can read previously generated checksum files and verify that the listed files have not been modified."],["PARAMETERS","**-a**, **--algorithm** _name_\n> Select algorithm (crc, md5, sha1, sha256, sha512, etc.)\n\n**-c**, **--check**\n> Read checksums and verify them\n\n**-l**, **--length** _bits_\n> Digest length in bits\n\n**--base64**\n> Output base64-encoded digest\n\n**--raw**\n> Output raw binary digest\n\n**--tag**\n> BSD-style checksum (default)\n\n**--untagged**\n> Reversed style without digest type\n\n**-z**, **--zero**\n> End lines with NUL instead of newline\n\n**--ignore-missing**\n> Don't fail for missing files during check\n\n**--quiet**\n> Don't print OK for verified files\n\n**--status**\n> Silent mode, exit code shows result\n\n**-w**, **--warn**\n> Warn about malformed checksum lines"],["OUTPUT FORMAT","```\nchecksum byte_count filename\n```"],["CAVEATS","CRC32 is not cryptographically secure. Use SHA256 or stronger for security-critical verification. Reads stdin when no file given or file is -."],["HISTORY","**cksum** was standardized in **POSIX.2** (1992). It is part of GNU coreutils. The **--algorithm** option for SHA/MD5 was added in GNU coreutils **9.0** (2021)."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[md5sum](/man/md5sum)(1), [sha256sum](/man/sha256sum)(1), [sum](/man/sum)(1)"]],"e":["md5sum","sha256sum","sum"],"x":"cksum [file.txt]"},"cointop":{"n":"cointop","t":"Terminal-based cryptocurrency tracker","s":[["TAGLINE","Terminal-based cryptocurrency tracker"],["TLDR","**Open** the interactive TUI\n\n```cointop```\n\n**Start in portfolio** view\n\n```cointop --portfolio```\n\n**Show only the top N** cryptocurrencies\n\n```cointop --top [50]```\n\n**Clear** the cache\n\n```cointop clean```\n\n**Display current holdings** in human-readable format\n\n```cointop holdings --human```\n\n**Check price** of specific coins\n\n```cointop price --coins [bitcoin,ethereum]```\n\n**Reset configuration** to defaults\n\n```cointop reset --config```"],["SYNOPSIS","**cointop** [_command_] [_options_]"],["PARAMETERS","**clean**\n> Clear the cache.\n\n**holdings**\n> Display portfolio holdings.\n\n**price**\n> Check price of specific coins.\n\n**reset**\n> Reset configuration or cache.\n\n**version**\n> Display version information.\n\n**--portfolio**\n> Start in portfolio view.\n\n**--top** _N_\n> Display only the top N cryptocurrencies.\n\n**--no-color**\n> Disable color output.\n\n**--refresh-rate** _seconds_\n> Data refresh rate in seconds.\n\n**--human**\n> Display values in human-readable format (with holdings).\n\n**--coins** _coins_\n> Comma-separated list of coin names (with price)."],["DESCRIPTION","**cointop** is a fast, lightweight terminal-based cryptocurrency tracking and portfolio management tool. It displays real-time prices, market capitalizations, trading volumes, and price changes for thousands of cryptocurrencies in an interactive text user interface.\n\nThe interface provides keyboard-driven navigation with vim-style bindings (inspired by htop), allowing rapid browsing through coin lists, detailed charts, and portfolio views. Users can mark favorites, sort by various metrics, and track personal holdings with cost basis and profit/loss calculations.\n\nData is sourced from cryptocurrency APIs (CoinGecko by default) and cached locally for performance. The tool supports multiple currencies for price display and provides historical price charts rendered in ASCII. Configuration is stored in **~/.config/cointop/config.toml**."],["CAVEATS","Requires internet connection for price data. API rate limits may apply. Market data depends on the configured data source (CoinGecko or CoinMarketCap)."],["INSTALL","```nix: nix profile install nixpkgs#cointop```"],["SEE ALSO","[ticker](/man/ticker)(1)"]],"e":["ticker"],"x":"cointop"},"compare":{"n":"compare","t":"image difference detection and comparison","s":[["TAGLINE","image difference detection and comparison"],["TLDR","**Compare two images**\n\n```compare [image1.png] [image2.png] [diff.png]```\n\n**Highlight differences** in red\n\n```compare -highlight-color red [image1.png] [image2.png] [diff.png]```\n\n**Get difference metric**\n\n```compare -metric AE [image1.png] [image2.png] null:```\n\n**Compare with fuzz factor** (tolerance)\n\n```compare -fuzz [5]% [image1.png] [image2.png] [diff.png]```\n\n**Output only difference** regions\n\n```compare -compose src [image1.png] [image2.png] [diff.png]```\n\n**Use specific channel**\n\n```compare -channel red [image1.png] [image2.png] [diff.png]```"],["SYNOPSIS","**compare** [_options_] _image1_ _image2_ [_difference_]"],["DESCRIPTION","**compare** is an ImageMagick utility that compares two images and highlights the differences. It can output a visual difference image and/or calculate numeric difference metrics.\n\nThe tool is useful for visual regression testing, detecting changes in rendered output, and quality assurance workflows. Various metrics quantify the difference between images."],["PARAMETERS","**-metric** _type_\n> Output difference metric (AE, RMSE, MAE, PSNR, etc.).\n\n**-fuzz** _percent_\n> Allow color tolerance for comparison.\n\n**-highlight-color** _color_\n> Color to use for highlighting differences.\n\n**-lowlight-color** _color_\n> Color for matching areas.\n\n**-compose** _method_\n> Composition method for difference output.\n\n**-channel** _type_\n> Compare specific color channel.\n\n**-subimage-search**\n> Search for subimage in larger image.\n\n**-dissimilarity-threshold** _value_\n> Threshold for subimage matching.\n\n**-verbose**\n> Display detailed comparison information and computed metrics."],["METRICS","**AE**: Absolute Error (count of differing pixels)\n**RMSE**: Root Mean Square Error (0 = identical)\n**MAE**: Mean Absolute Error (0 = identical)\n**MSE**: Mean Squared Error\n**PSNR**: Peak Signal to Noise Ratio (higher = more similar)\n**SSIM**: Structural Similarity Index (1 = identical)\n**NCC**: Normalized Cross Correlation (default metric)\n**PHASH**: Perceptual Hash"],["CAVEATS","Images must be same dimensions unless using subimage search. Large images can be slow to compare. Lossy formats may show differences from compression."],["HISTORY","**compare** is part of **ImageMagick**, created by **John Cristy** in **1987**. ImageMagick has been continuously developed for image manipulation and conversion. The compare tool became essential for automated visual testing in software development."],["INSTALL","```pacman: sudo pacman -S imagemagick```\n\n```apk: sudo apk add imagemagick```\n\n```brew: brew install imagemagick```\n\n```nix: nix profile install nixpkgs#imagemagick```"],["SEE ALSO","[magick](/man/magick)(1), [convert](/man/convert)(1), [identify](/man/identify)(1), [diff](/man/diff)(1)"]],"e":["magick","convert","identify","diff"],"x":"compare [image1.png] [image2.png] [diff.png]"},"consul-kv":{"n":"consul-kv","t":"distributed key-value store management","s":[["TAGLINE","distributed key-value store management"],["TLDR","**Get a key's value**\n\n```consul kv get [key]```\n\n**Put a value**\n\n```consul kv put [key] [value]```\n\n**Delete a key**\n\n```consul kv delete [key]```\n\n**List keys** with a prefix\n\n```consul kv get -keys [prefix/]```\n\n**Get with recurse**\n\n```consul kv get -recurse [prefix/]```\n\n**Export all keys**\n\n```consul kv export [prefix/] > [backup.json]```\n\n**Import keys**\n\n```consul kv import @[backup.json]```"],["SYNOPSIS","**consul** **kv** _subcommand_ [_options_]"],["SUBCOMMANDS","**get**\n> Retrieve a value from the KV store.\n\n**put**\n> Set a value in the KV store.\n\n**delete**\n> Remove a key from the KV store.\n\n**export**\n> Export KV pairs in JSON format.\n\n**import**\n> Import KV pairs from JSON."],["PARAMETERS","**-keys**\n> List only keys, not values.\n\n**-recurse**\n> Retrieve all keys with the prefix.\n\n**-base64**\n> Encode/decode values as base64.\n\n**-detailed**\n> Provide additional metadata about the key.\n\n**-separator** _STRING_\n> String to use as separator for key listing (default: \"/\").\n\n**-cas**\n> Perform a Check-And-Set operation (requires -modify-index).\n\n**-modify-index** _N_\n> ModifyIndex used by **-cas** for atomic updates.\n\n**-flags** _N_\n> Unsigned integer flag value to associate with the KV pair.\n\n**-acquire**\n> Acquire a lock on the key (requires **-session**).\n\n**-release**\n> Release a lock on the key (requires **-session**).\n\n**-session** _ID_\n> Session identifier used with **-acquire**/**-release**.\n\n**-token** _TOKEN_\n> ACL token for API requests.\n\n**-datacenter** _DC_\n> Target datacenter for the request.\n\n**-http-addr** _ADDRESS_\n> Address of the Consul agent (default: \"http://127.0.0.1:8500\")."],["DESCRIPTION","**consul kv** manages Consul's key-value store, a distributed data store for configuration and service discovery. Values can be strings, JSON, or binary data.\n\nPart of HashiCorp Consul, the KV store is commonly used for dynamic configuration, feature flags, and service coordination. Keys are organized hierarchically using `/` as a separator, similar to a filesystem path structure. Values can be up to 512 KB in size."],["CAVEATS","The KV store is not designed for bulk data storage; there is a 512 KB limit per value. Recursive deletes (`consul kv delete -recurse`) are irreversible. Export/import uses JSON format and includes metadata. ACL tokens may be required depending on Consul configuration."],["INSTALL","```pacman: sudo pacman -S consul```\n\n```nix: nix profile install nixpkgs#consul```"],["SEE ALSO","[consul](/man/consul)(1), [etcdctl](/man/etcdctl)(1)"]],"e":["consul","etcdctl"],"x":"consul kv get [key]"},"crudini":{"n":"crudini","t":"Get and set values in INI-style config files","s":[["TAGLINE","Get and set values in INI-style config files"],["TLDR","**Get** a parameter\n\n```crudini --get [file.ini] [section] [param]```\n\n**Set** a parameter\n\n```crudini --set [file.ini] [section] [param] [value]```\n\n**Delete** a parameter\n\n```crudini --del [file.ini] [section] [param]```\n\n**Merge** INI from stdin into a file\n\n```crudini --merge [file.ini] < [snippet.ini]```\n\n**Set** a global (no section) parameter\n\n```crudini --set [file.ini] \"\" [param] [value]```"],["SYNOPSIS","**crudini** **--set**|**--get**|**--del**|**--merge** [*options*] *config_file* ..."],["DESCRIPTION","**crudini** manipulates INI configuration files from the shell: get/set/delete parameters, operate on sections, and merge fragments. Useful in provisioning scripts that must edit **.ini** files without brittle **sed**. Requires Python **iniparse**."],["PARAMETERS","**--get** *file* [*section*] [*param*]\n\n> Read a value or list sections/keys.\n\n**--set** *file* *section* [*param*] [*value*]\n\n> Create/update a value. Empty section **\"\"** targets global keys.\n\n**--del** *file* *section* [*param*]\n\n> Remove a parameter or section.\n\n**--merge** *file* [*section*]\n\n> Merge INI data from stdin.\n\n**--existing**\n\n> Fail if the item does not already exist.\n\nMultiple operations can be combined per invocation; see **crudini --help**."],["CAVEATS","INI dialects differ (duplicate keys, multiline, nested sections). Always test on copies of production configs. Not a general TOML/YAML editor."],["INSTALL","```apt: sudo apt install crudini```\n\n```dnf: sudo dnf install crudini```\n\n```zypper: sudo zypper install crudini```\n\n```nix: nix profile install nixpkgs#crudini```"],["SEE ALSO","[augtool](/man/augtool)(1), [sed](/man/sed)(1), [git-config](/man/git-config)(1)"],["RESOURCES","```[Source code](https://github.com/pixelb/crudini)```"]],"e":["augtool","sed","git-config"],"x":"crudini --get [file.ini] [section] [param]"},"cynative":{"n":"cynative","t":"Read-only deep research CLI for cloud, code, and Kubernetes infrastructure","s":[["TAGLINE","Read-only deep research CLI for cloud, code, and Kubernetes infrastructure"],["TLDR","Ask an **interactive** infrastructure research question\n\n```cynative \"what in my cloud is publicly exposed that shouldn't be?\"```\n\nRun a **single non-interactive** task and exit\n\n```cynative -p \"which IAM roles can escalate to admin?\"```\n\nPipe IaC into a **one-shot review**\n\n```cat main.tf | cynative -p \"review this Terraform for misconfigurations\"```\n\n**Validate** config and connectors without starting a session\n\n```cynative doctor```\n\nProbe the configured **LLM** as part of doctor\n\n```cynative doctor --live-llm```\n\nAuto-approve tool calls for **unattended** runs\n\n```cynative -p \"live cloud resources absent from IaC\" --auto-approve```"],["SYNOPSIS","**cynative** [_options_] [_\"task\"_]\n\n**cynative** **-p**|_--print_ _\"task\"_\n\n**cynative** **doctor** [_--live-llm_]"],["PARAMETERS","**-p**, **--print** _task_\n> Run one task non-interactively and exit (scripts and pipes)\n\n**--auto-approve**\n> Skip per-tool approval prompts (needed without a TTY)\n\n**-v**\n> Verbose mode (e.g. stream inner sandbox tool calls)\n\n**doctor**\n> Validate configuration and connector readiness\n\n**--live-llm**\n> With **doctor**, probe the configured model with a tool-less round-trip\n\n**completion** _shell_\n> Shell completion helpers (see `cynative completion <shell> --help`)\n\n**--version**\n> Print version, commit, build date, Go version, and platform"],["DESCRIPTION","**cynative** is a read-only infrastructure research agent. It runs frontier language models against your live environment — AWS, GCP, Azure, Kubernetes (including EKS/GKE/AKS), GitHub, and GitLab — and returns evidence-backed answers with verification.\n\nUnlike generic coding agents with MCP plugins, cynative is **read-only by construction**: every call is authorized against a security-audit style policy before credentials are attached, research code runs in a sandbox, secrets are redacted from model context, and every tool call is logged to a fail-closed JSONL audit log (`~/.cynative/audit.log` by default).\n\nIt uses credentials already present in your shell (no separate credential store). For bulk work it can write and run sandboxed JavaScript that fans out concurrent API calls. Interactive mode supports line editing and history; press Esc or Ctrl-C once to stop a running task gracefully.\n\nConfigure the LLM via environment variables such as `CYNATIVE_LLM_PROVIDER`, `CYNATIVE_LLM_MODEL`, and provider API keys, or via `~/.cynative/config.yaml`. Resource ceilings (`CYNATIVE_MAX_TOTAL_TOKENS`, `CYNATIVE_MAX_ITERATIONS`, etc.) bound unattended runs."],["CAVEATS","Always use least-privileged, read-only credentials. Hosted LLM providers receive prompts and tool results (after redaction). Token and iteration limits default high or unbounded — set ceilings for cron/CI. Operational footers (timing, tokens) go to **stderr** so stdout stays clean for piping."],["HISTORY","Open-source Go CLI (Apache-2.0) for sovereign, evidence-backed infrastructure research with built-in cloud/VCS connectors and an action gate that fails closed on writes."],["SEE ALSO","[aws](/man/aws)(1), [gcloud](/man/gcloud)(1), [az](/man/az)(1), [kubectl](/man/kubectl)(1)"],["RESOURCES","```[Source code](https://github.com/cynative/cynative)```\n\n```[Documentation](https://github.com/cynative/cynative/tree/main/docs)```"]],"e":["aws","gcloud","az","kubectl"],"x":"cynative \"what in my cloud is publicly exposed that shouldn't be?\""},"dalfox":{"n":"dalfox","t":"open-source XSS vulnerability scanner and parameter analyzer","s":[["TAGLINE","open-source XSS vulnerability scanner and parameter analyzer"],["TLDR","**Scan a URL** for XSS vulnerabilities\n\n```dalfox url [https://example.com?q=test]```\n\n**Scan with custom payloads** file\n\n```dalfox url [https://example.com?q=test] --custom-payload [payloads.txt]```\n\n**Scan multiple URLs** from file\n\n```dalfox file [urls.txt]```\n\n**Pipe URLs** from another tool\n\n```cat [urls.txt] | dalfox pipe```\n\n**Use blind XSS** callback\n\n```dalfox url [https://example.com?q=test] --blind [https://your.xss.ht]```\n\n**Output results** to JSON file\n\n```dalfox url [https://example.com?q=test] -o [results.json] --format json```"],["SYNOPSIS","**dalfox** _mode_ [_target_] [_options_]"],["PARAMETERS","_MODE_\n> Operation mode: url, file, pipe, sxss, server, payload.\n\n_TARGET_\n> URL or file containing URLs to scan.\n\n**--custom-payload** _FILE_\n> Use custom payload file.\n\n**--blind** _URL_\n> Blind XSS callback URL.\n\n**-o**, **--output** _FILE_\n> Output file path.\n\n**--format** _FORMAT_\n> Output format: plain, json, xml.\n\n**--cookie** _STRING_\n> Set cookies for requests.\n\n**-H**, **--header** _STRING_\n> Add custom header.\n\n**--proxy** _URL_\n> HTTP proxy to use.\n\n**-w**, **--worker** _N_\n> Number of concurrent workers.\n\n**--silence**\n> Suppress all output except results.\n\n**--mining-dict**\n> Enable dictionary-based parameter mining.\n\n**--mining-dom**\n> Enable DOM-based parameter mining.\n\n**--only-custom-payload**\n> Only test custom payloads, skip built-in payloads.\n\n**--remote-payloads** _SOURCE_\n> Use remote payloads (portswigger, payloadbox).\n\n**--skip-xss-scanning**\n> Skip XSS scanning, only perform parameter analysis.\n\n**--poc-type** _TYPE_\n> PoC output format (plain, curl, httpie, http-request).\n\n**--help**\n> Display help information."],["DESCRIPTION","**dalfox** is a powerful open-source XSS (Cross-Site Scripting) vulnerability scanner and parameter analyzer. It specializes in finding reflected XSS, stored XSS, and DOM-based XSS vulnerabilities through automated testing.\n\nThe tool performs parameter analysis to identify injection points, then tests with a comprehensive payload database. It supports blind XSS detection using external callback servers and can integrate with other security tools through piped input.\n\ndalfox includes features like DOM rendering analysis, WAF detection and evasion, and intelligent payload mutation. It's designed for penetration testers and security researchers conducting authorized security assessments."],["CAVEATS","Only use on systems you have explicit authorization to test. False positives may occur; verify findings manually. High request rates may trigger rate limiting or bans. Some XSS types require browser-based verification."],["HISTORY","dalfox was created by **hahwul** and released in **2020**. The name stands for \"Finder Of XSS\" with \"dal\" meaning moon in Korean. It was designed as a fast, flexible alternative to existing XSS scanners, with emphasis on automation and accuracy."],["INSTALL","```brew: brew install dalfox```\n\n```nix: nix profile install nixpkgs#dalfox```"],["SEE ALSO","[nikto](/man/nikto)(1), [sqlmap](/man/sqlmap)(1), [nuclei](/man/nuclei)(1), [httpx](/man/httpx)(1)"]],"e":["nikto","sqlmap","nuclei","httpx"],"x":"dalfox url [https://example.com?q=test]"},"dnspyre":{"n":"dnspyre","t":"High QPS DNS benchmark tool","s":[["TAGLINE","High QPS DNS benchmark tool"],["TLDR","**Benchmark a DNS server** with A queries for a domain\n\n```dnspyre --server [8.8.8.8] [example.com]```\n\n**Run a high concurrency benchmark** for 60 seconds\n\n```dnspyre --server [8.8.8.8] --duration 60s --concurrency [100] [example.com]```\n\n**Benchmark with a fixed number of queries**\n\n```dnspyre --server [8.8.8.8] --number [1000] [example.com]```\n\n**Test specific query types**\n\n```dnspyre --server [8.8.8.8] --type A --type AAAA [example.com]```\n\n**Load queries from a file**\n\n```dnspyre --server [8.8.8.8] @[path/to/queries.txt]```\n\n**Benchmark using DNS over HTTPS**\n\n```dnspyre --server https://dns.google/dns-query --doh-method post [example.com]```"],["SYNOPSIS","**dnspyre** [_options_] _queries_..."],["PARAMETERS","**-s**, **--server** _ADDR_\n> DNS server address to benchmark.\n\n**-n**, **--number** _N_\n> Number of query repetitions (mutually exclusive with --duration).\n\n**-d**, **--duration** _DURATION_\n> Test duration (default: 1m, mutually exclusive with --number).\n\n**-c**, **--concurrency** _N_\n> Number of concurrent connections (default: 1).\n\n**-t**, **--type** _TYPE_\n> DNS query type: A, AAAA, CNAME, MX, TXT, HTTPS, etc. Can be specified multiple times.\n\n**--tcp**\n> Use TCP instead of UDP.\n\n**--dot**\n> Use DNS over TLS (DoT).\n\n**--doh-method** _METHOD_\n> Use DNS over HTTPS with GET or POST method.\n\n**--doh-protocol** _PROTO_\n> HTTP protocol version for DoH (1.1, 2, 3).\n\n**--doq**\n> Use DNS over QUIC (DoQ).\n\n**--recurse**\n> Set the recursion desired flag (default: on).\n\n**--probability** _FLOAT_\n> Probability of sending each query (0.0-1.0).\n\n**--progress**\n> Show progress bar (default: on).\n\n**-o**, **--output** _FORMAT_\n> Output format: text, json, csv.\n\n**-v**, **--verbose**\n> Enable verbose output."],["DESCRIPTION","**dnspyre** is a high-performance DNS benchmarking tool designed to stress test DNS servers under heavy load. It can simulate thousands of concurrent queries per second and provides detailed statistics on response times, error rates, and throughput. It was originally forked from dnstrace but has been extensively rewritten and enhanced.\n\nQueries can be specified as domain names directly, loaded from local files using **@file-path**, or fetched from HTTP URLs. Multiple query sources can be combined in a single run. The tool supports UDP, TCP, DNS over TLS (DoT), DNS over HTTPS (DoH), and DNS over QUIC (DoQ)."],["OUTPUT METRICS","- Queries per second (QPS)\n- Latency percentiles (p50, p90, p99, p999)\n- Error rate and types\n- Connection statistics\n- Histogram of response times"],["CAVEATS","High QPS tests may overwhelm target servers. The --duration and --number options are mutually exclusive. Network conditions and DNS caching on intermediate resolvers may skew results."],["HISTORY","**dnspyre** was developed by **Tantalor93** as a modern fork of dnstrace, adding support for modern DNS protocols (DoT, DoH, DoQ) and improved concurrency control."],["INSTALL","```brew: brew install dnspyre```"],["SEE ALSO","[dig](/man/dig)(1), [nslookup](/man/nslookup)(1), [q](/man/q)(1)"]],"e":["dig","nslookup","q"],"x":"dnspyre --server [8.8.8.8] [example.com]"},"dss":{"n":"dss","t":"Canonical Data Science Stack CLI for Kubernetes notebooks","s":[["TAGLINE","Canonical Data Science Stack CLI for Kubernetes notebooks"],["TLDR","**Initialize DSS** on a Canonical Kubernetes cluster\n\n```dss initialize --kubeconfig \"$(sudo k8s config)\"```\n\n**Create** a Jupyter notebook with a specific image\n\n```dss create [my-notebook] --image=pytorch```\n\n**List** all notebooks in the DSS environment\n\n```dss list```\n\n**Show** the status of MLflow, GPUs, and core components\n\n```dss status```\n\n**Print logs** for a specific notebook\n\n```dss logs [my-notebook]```\n\n**Print logs** for the MLflow deployment\n\n```dss logs --mlflow```\n\n**Stop** a running notebook\n\n```dss stop [my-notebook]```\n\n**Start** a previously stopped notebook\n\n```dss start [my-notebook]```\n\n**Remove** a single notebook\n\n```dss remove [my-notebook]```\n\n**Purge** every DSS component and all notebooks (destructive)\n\n```dss purge```"],["SYNOPSIS","**dss** _command_ [_options_]"],["PARAMETERS","**initialize** [_--kubeconfig_ _file_]\n> Store cluster credentials, allocate persistent storage for notebooks, and deploy the MLflow model registry.\n\n**create** _name_ [_--image_ _image_]\n> Create a new Jupyter notebook of the given _name_ wired to the shared MLflow instance. _image_ may be a shorthand (**pytorch**, **tensorflow**) or a fully qualified image like **kubeflownotebookswg/jupyter-scipy:v1.8.0**.\n\n**list**\n> List every notebook tracked by DSS in the current cluster.\n\n**status**\n> Display deployment status, MLflow URL, and detected GPU availability.\n\n**logs** [_name_] [_--kubeconfig_ _file_] [_--all_] [_--mlflow_]\n> Print logs for the named notebook, for **--mlflow** (the MLflow pod), or for everything with **--all**.\n\n**start** _name_\n> Start a stopped notebook.\n\n**stop** _name_\n> Stop a running notebook.\n\n**remove** _name_\n> Delete a single notebook and its persistent volume claim.\n\n**purge**\n> Tear down every DSS component (notebooks, MLflow, supporting resources). **Irreversible** - all stored data is lost.\n\n**--kubeconfig** _file_\n> Path to a kubeconfig file. Used by **initialize** and **logs** when the user is not already authenticated to a cluster.\n\n**--help**\n> Show usage for **dss** or for a specific subcommand."],["DESCRIPTION","**dss** is the command-line front end to **Canonical's Data Science Stack**, an opinionated bundle of **Jupyter Notebook** images, **MLflow** model tracking, and Kubernetes plumbing distributed as a **snap**. It targets local Canonical Kubernetes (**k8s** snap) clusters but works against any Kubernetes that can host the underlying components.\n\nAfter **dss initialize**, the cluster runs a shared MLflow server and is ready to host notebook pods. **dss create** spawns a notebook backed by a configurable container image, persistent storage, and a service account that can talk to MLflow. The remaining lifecycle commands (**list**, **status**, **logs**, **start**, **stop**, **remove**, **purge**) operate on those notebooks without requiring direct **kubectl** use.\n\nDSS aims to be the easiest way to get a reproducible local data-science environment with GPU support and experiment tracking, while still leaving the underlying Kubernetes resources accessible to power users."],["CAVEATS","**dss purge** removes every notebook and the MLflow registry; backups must be made beforehand. The default Kubernetes target is the **k8s** snap; on other distributions pass **--kubeconfig** explicitly. Image shorthands like **pytorch** and **tensorflow** track moving upstream tags, which can change between **dss** releases.\n\nThe name **dss** is overloaded - **Dataiku Data Science Studio** ships separate **dsscli** and **dssadmin** binaries, and Open-E's **dss_cli** targets the unrelated DSS storage server. This page documents the Canonical snap."],["CONFIGURATION","DSS state (storage class, MLflow endpoint, kubeconfig reference) is persisted under the snap's data directory, typically **/var/snap/data-science-stack/current/**. Notebook resources, including the MLflow deployment, live in a dedicated namespace inside the connected Kubernetes cluster."],["HISTORY","**Data Science Stack** is published by **Canonical** as the **data-science-stack** snap. It launched as a streamlined alternative to the full **Charmed Kubeflow** distribution, focusing on a single-machine workflow that pairs Jupyter with MLflow on top of the **k8s** snap."],["SEE ALSO","[kubectl](/man/kubectl)(1), [snap](/man/snap)(1), [mlflow](/man/mlflow)(1), [jupyter](/man/jupyter)(1)"]],"e":["kubectl","snap","mlflow","jupyter"],"x":"dss initialize --kubeconfig \"$(sudo k8s config)\""},"fc-match":{"n":"fc-match","t":"find best matching font","s":[["TAGLINE","find best matching font"],["TLDR","**Find best matching font**\n\n```fc-match [pattern]```\n\n**Match monospace font**\n\n```fc-match monospace```\n\n**Match with specific family**\n\n```fc-match \"DejaVu Sans\"```\n\n**Show verbose output**\n\n```fc-match -v [pattern]```\n\n**Show specific elements**\n\n```fc-match --format \"%{family}\\n\" [pattern]```\n\n**Match all fonts**\n\n```fc-match -a [pattern]```"],["SYNOPSIS","**fc-match** [_options_] [_pattern_]"],["PARAMETERS","**-a**, **--all**\n> Show all matching fonts.\n\n**-s**, **--sort**\n> Sort output by priority.\n\n**-v**, **--verbose**\n> Verbose output.\n\n**--format** _format_\n> Format output string."],["DESCRIPTION","**fc-match** matches fonts against fontconfig patterns and shows which font will be used for a given request. The tool is essential for debugging font configuration, understanding why certain fonts are selected, and verifying font substitution rules.\n\nWhen applications request a font, fontconfig uses a sophisticated matching algorithm considering family name, style, weight, and other properties. fc-match reveals the result of this matching process, helping diagnose font-related issues."],["CAVEATS","Pattern syntax can be complex. Font matching rules depend on fontconfig configuration. Results may differ across systems with different font installations."],["INSTALL","```apt: sudo apt install fontconfig```\n\n```dnf: sudo dnf install fontconfig```\n\n```pacman: sudo pacman -S fontconfig```\n\n```apk: sudo apk add fontconfig```\n\n```zypper: sudo zypper install fontconfig```\n\n```brew: brew install fontconfig```\n\n```nix: nix profile install nixpkgs#fontconfig```"],["SEE ALSO","[fc-list](/man/fc-list)(1), [fc-cache](/man/fc-cache)(1), [fc-pattern](/man/fc-pattern)(1), [fc-query](/man/fc-query)(1)"],["RESOURCES","```[Source code](https://gitlab.freedesktop.org/fontconfig/fontconfig)```\n\n```[Homepage](https://www.freedesktop.org/wiki/Software/fontconfig/)```"]],"e":["fc-list","fc-cache","fc-pattern","fc-query"],"x":"fc-match [pattern]"},"finch":{"n":"finch","t":"console-based multi-protocol chat client","s":[["TAGLINE","console-based multi-protocol chat client"],["TLDR","**Launch** finch\n\n```finch```\n\n**Quit** finch\n\n```<Alt q>|<Ctrl c>```\n\nShow **actions** menu\n\n```<Alt a>```\n\nJump to **n-th** window\n\n```<Alt [Number]>```\n\n**Close** current window\n\n```<Alt c>```\n\nStart **moving** a window\n\n```<Alt m>```\n\nStart **resizing** a window\n\n```<Alt r>```"],["SYNOPSIS","**finch** [_options_]"],["DESCRIPTION","**finch** is a console-based modular messaging client using the libpurple library (same as Pidgin). It supports multiple protocols including XMPP, IRC, and various proprietary services.\n\nText-based alternative to the Pidgin GUI client."],["PARAMETERS","**-c, --config** _dir_\n> Use alternate configuration directory\n\n**-d, --debug**\n> Enable debug output\n\n**-v, --version**\n> Display version"],["CONFIGURATION","**~/.purple/prefs.xml**\n> Libpurple preferences shared with Pidgin.\n\n**~/.purple/accounts.xml**\n> Account credentials and connection settings.\n\n**~/.gntrc**\n> GNT (finch) UI keybindings and window behavior."],["CAVEATS","Requires ncurses library. Uses same libpurple plugins as Pidgin. Account configuration shared with Pidgin if using same config directory."],["INSTALL","```apt: sudo apt install finch```\n\n```dnf: sudo dnf install finch```\n\n```apk: sudo apk add finch```\n\n```zypper: sudo zypper install finch```"],["SEE ALSO","[irssi](/man/irssi)(1), [weechat](/man/weechat)(1)"]],"e":["irssi","weechat"],"x":"finch"},"flarectl":{"n":"flarectl","t":"Cloudflare management from the command line","s":[["TAGLINE","Cloudflare management from the command line"],["TLDR","**List DNS records**\n\n```flarectl dns list --zone [example.com]```\n\n**Create DNS record**\n\n```flarectl dns create --zone [example.com] --name [www] --type [A] --content [1.2.3.4]```\n\n**Delete DNS record**\n\n```flarectl dns delete --zone [example.com] --id [record_id]```\n\n**List zones**\n\n```flarectl zone list```\n\n**Purge cache**\n\n```flarectl zone purge --zone [example.com] --everything```\n\n**List firewall rules**\n\n```flarectl firewall rules list --zone [example.com]```"],["SYNOPSIS","**flarectl** _command_ _subcommand_ [_options_]"],["SUBCOMMANDS","**zone**\n> Manage zones.\n\n**dns**\n> Manage DNS records.\n\n**firewall**\n> Manage firewall rules.\n\n**user**\n> User information."],["PARAMETERS","**--zone** _zone_\n> Zone name.\n\n**--name** _name_\n> Record name.\n\n**--type** _type_\n> Record type.\n\n**--content** _content_\n> Record content."],["DESCRIPTION","**Flarectl** is the official command-line interface for managing Cloudflare services. It provides programmatic access to Cloudflare's API, enabling automation of DNS management, zone configuration, firewall rules, and CDN settings without using the web dashboard.\n\nThe tool supports common operations including creating and modifying DNS records, managing zone settings like caching and security features, configuring firewall rules and access controls, and retrieving analytics data. Authentication uses API tokens or legacy API keys stored in environment variables or configuration files.\n\nFlarectl is particularly useful for infrastructure-as-code workflows, CI/CD pipelines, and bulk operations across multiple domains. It outputs results in human-readable format by default, with JSON output available for scripting and automation."],["INSTALL","```apk: sudo apk add flarectl```\n\n```brew: brew install flarectl```\n\n```nix: nix profile install nixpkgs#flarectl```"],["SEE ALSO","[cloudflared](/man/cloudflared)(1)"],["RESOURCES","```[Source code](https://github.com/cloudflare/cloudflare-go)```"]],"e":["cloudflared"],"x":"flarectl dns list --zone [example.com]"},"flatpak-run":{"n":"flatpak-run","t":"execute sandboxed Flatpak applications","s":[["TAGLINE","execute sandboxed Flatpak applications"],["TLDR","**Run** an installed flatpak application\n\n```flatpak run [com.example.app]```\n\nRun an application from a **specific branch**\n\n```flatpak run --branch=[stable|beta|master] [com.example.app]```\n\nRun an **interactive shell** inside a flatpak\n\n```flatpak run --command=sh [com.example.app]```\n\nRun with a **specific runtime version**\n\n```flatpak run --runtime-version=[24.08] [com.example.app]```\n\nRun with a **different runtime**\n\n```flatpak run --runtime=[org.freedesktop.Sdk] [com.example.app]```\n\nRun with **additional filesystem access**\n\n```flatpak run --filesystem=[home] [com.example.app]```\n\nRun in **strict sandbox mode** (strip extra permissions)\n\n```flatpak run --sandbox [com.example.app]```"],["SYNOPSIS","**flatpak run** [_options_] _ref_ [_args..._]"],["PARAMETERS","**--user**\n> Use per-user installation\n\n**--system**\n> Use system-wide installation\n\n**--arch** _arch_\n> Target specific architecture\n\n**--branch** _branch_\n> Use specific branch (stable, beta, master)\n\n**--command** _command_\n> Run specified command instead of default\n\n**--runtime** _runtime_\n> Use a different runtime\n\n**--runtime-version** _version_\n> Use specific runtime version\n\n**--sandbox**\n> Run with minimal permissions (strip extras)\n\n**--share** _subsystem_\n> Share subsystem with host (network, ipc)\n\n**--unshare** _subsystem_\n> Unshare subsystem from host\n\n**--socket** _socket_\n> Expose well-known socket (x11, wayland, pulseaudio)\n\n**--nosocket** _socket_\n> Don't expose socket\n\n**--filesystem** _path_\n> Grant filesystem access (home, host, /path)\n\n**--nofilesystem** _path_\n> Revoke filesystem access\n\n**--env** _var=value_\n> Set environment variable\n\n**--device** _device_\n> Expose device to application (dri, input, usb, kvm, shm, all)\n\n**--devel**\n> Use the devel runtime specified in the application metadata\n\n**--die-with-parent**\n> Kill the application when the launching process dies"],["DESCRIPTION","**Flatpak run** executes applications or opens shells within Flatpak's sandboxed environment. The sandbox provides isolation with the runtime mounted at **/usr**, application at **/app**, and a writable **/var** directory for persistent data.\n\nWhen running an application, extra arguments pass through to the application. For runtimes, the command opens a shell for development and testing purposes.\n\nThe sandbox automatically adjusts environment variables including **PATH**, **LD_LIBRARY_PATH**, and graphics-related variables to ensure proper isolation and prevent interference with sandbox functioning."],["CAVEATS","Sandbox permissions are controlled by the application's manifest and can be overridden at runtime using **--share**, **--socket**, and **--filesystem** options. Using **--sandbox** provides maximum isolation but may break applications requiring specific permissions. Some applications may not function correctly with modified runtimes."],["HISTORY","Part of the **Flatpak** project developed by Alexander Larsson at Red Hat. Flatpak evolved from the xdg-app project around **2015**, providing sandboxed application execution on Linux with strong isolation guarantees."],["INSTALL","```apt: sudo apt install flatpak```\n\n```dnf: sudo dnf install flatpak```\n\n```pacman: sudo pacman -S flatpak```\n\n```apk: sudo apk add flatpak```\n\n```zypper: sudo zypper install flatpak```\n\n```nix: nix profile install nixpkgs#flatpak```"],["SEE ALSO","[flatpak](/man/flatpak)(1), [flatpak-install](/man/flatpak-install)(1)"]],"e":["flatpak","flatpak-install"],"x":"flatpak run [com.example.app]"},"fmtutil-user":{"n":"fmtutil-user","t":"manage TeX formats and Metafont bases for the current user","s":[["TAGLINE","manage TeX formats and Metafont bases for the current user"],["TLDR","**Rebuild all** user-local formats\n\n```fmtutil-user --all```\n\n**Create missing** user formats\n\n```fmtutil-user --missing```\n\n**Rebuild** a single format under the user tree\n\n```fmtutil-user --byfmt pdflatex```\n\n**List** user-visible format configuration\n\n```fmtutil-user --listcfg```"],["SYNOPSIS","**fmtutil-user** [_OPTION_]... [_COMMAND_]"],["DESCRIPTION","**fmtutil-user** is the per-user form of **fmtutil**. It writes format files under TEXMFVAR (for example **~/.texliveYYYY/texmf-var**) and can read additional config from TEXMFCONFIG / TEXMFHOME. It is equivalent to **fmtutil --user**.\n\nUser mode is only appropriate when formats must be private to one account (for example experimental engines under TEXMFHOME). For a normal single- or multi-user machine, **fmtutil-sys** is strongly preferred.\n\nExactly one command is required. Options match **fmtutil** / **fmtutil-sys**."],["PARAMETERS","**--all**\n> Recreate all format files in the user tree\n\n**--missing**\n> Create all missing format files\n\n**--byfmt** _FORMAT_\n> (Re)create format FORMAT\n\n**--byengine** _ENGINE_\n> (Re)create formats built with ENGINE\n\n**--refresh**\n> Recreate only existing format files\n\n**--listcfg**\n> List format configurations\n\n**--enablefmt** _FORMAT_[/_ENGINE_]\n> Enable FORMAT\n\n**--disablefmt** _FORMAT_[/_ENGINE_]\n> Disable FORMAT\n\n**--cnffile** _FILE_\n> Read alternate **fmtutil.cnf**\n\n**--dry-run**, **-n**\n> Do not actually build formats\n\n**--quiet**\n> Be silent\n\n**--version**, **--help**\n> Show version or help and exit"],["CAVEATS","Running **fmtutil-user** once causes user-tree formats to **override** system formats for that account forever after. Later **fmtutil-sys** or package updates will not be visible until you remove the generated files under TEXMFVAR/web2c (after backing them up) or re-run **fmtutil-user** whenever the system formats change. Prefer **fmtutil-sys** unless you understand this trade-off."],["HISTORY","Introduced as an explicit counterpart to **fmtutil-sys** in TeX Live 2017 so bare **fmtutil** would no longer silently default to ambiguous tree selection."],["INSTALL","```dnf: sudo dnf install texlive```\n\n```apk: sudo apk add texlive```\n\n```zypper: sudo zypper install texlive```\n\n```brew: brew install texlive```"],["SEE ALSO","[fmtutil](/man/fmtutil)(1), [fmtutil-sys](/man/fmtutil-sys)(1), [mktexfmt](/man/mktexfmt)(1), [kpsewhich](/man/kpsewhich)(1)"],["RESOURCES","```[Documentation](https://tug.org/texlive/scripts-sys-user.html)```\n\n```[Homepage](https://tug.org/texlive/)```"]],"e":["fmtutil","fmtutil-sys","mktexfmt","kpsewhich"],"x":"fmtutil-user --all"},"gcx":{"n":"gcx","t":"Grafana Cloud CLI optimized for agentic usage","s":[["TAGLINE","Grafana Cloud CLI optimized for agentic usage"],["TLDR","**Authenticate** to a Grafana Cloud or on-prem instance via browser OAuth\n\n```gcx auth login --server [https://myorg.grafana.net]```\n\n**Run a PromQL** metrics query over the last hour\n\n```gcx metrics query '[rate(http_requests_total[5m])]' --since [1h]```\n\n**Run a LogQL** logs query against Loki\n\n```gcx logs query '[{app=\"api\"} |= \"error\"]' --since [30m]```\n\n**List alert rules** currently firing\n\n```gcx alert rules list --state [firing]```\n\n**Export dashboards and folders** to local YAML files\n\n```gcx resources pull -p [./resources] -o [yaml]```\n\n**Validate and push** local resources back to Grafana\n\n```gcx resources push -p [./resources] --on-error [abort]```\n\n**List Service Level** Objectives\n\n```gcx slo definitions list```\n\n**Ask the Grafana Assistant** a question\n\n```gcx assistant prompt \"[why is latency high on checkout?]\"```\n\n**Generate shell completions**\n\n```gcx completion [bash]```"],["SYNOPSIS","**gcx** _command_ [_subcommand_] [_options_] [_args_]"],["PARAMETERS","**auth login** **--server** _URL_\n> Start browser-based OAuth login against a Grafana instance.\n\n**config set** _key value_\n> Set a configuration value (e.g. **contexts.my-grafana.grafana.token**).\n\n**config use-context** _name_\n> Switch the active configuration context.\n\n**config check**\n> Verify the active authentication and connectivity.\n\n**metrics query** _PROMQL_\n> Execute a PromQL query against Prometheus / Mimir.\n\n**logs query** _LOGQL_\n> Execute a LogQL query against Loki.\n\n**traces query** _QUERY_\n> Query trace data from Tempo.\n\n**profiles query** _QUERY_\n> Query profiling data from Pyroscope.\n\n**alert rules list** [**--state** _STATE_]\n> Display alert rules and their state, health, and pause status.\n\n**alert groups list**\n> Show alert groups.\n\n**resources pull** [**-p** _DIR_] [**-o** _FORMAT_]\n> Export dashboards, folders, and other resources to local files.\n\n**resources push** [**-p** _DIR_] [**--dry-run**] [**--on-error** _MODE_]\n> Deploy local resources back to Grafana.\n\n**resources delete**, **edit**, **validate**, **get**\n> Manage resources: remove, interactively modify, check, or list them.\n\n**slo definitions list**, **slo reports list**\n> Manage Service Level Objectives.\n\n**synth checks list**, **synth probes list**\n> Synthetic monitoring checks and probes.\n\n**irm oncall schedules list**, **irm incidents list**\n> Incident response and on-call management.\n\n**k6 load-tests list**, **k6 runs list**\n> Load testing with k6.\n\n**fleet pipelines list**, **fleet collectors list**\n> Fleet management.\n\n**kg status**, **kg search**, **kg entities show**\n> Grafana knowledge graph.\n\n**assistant investigations list**, **assistant prompt** _TEXT_\n> Drive the Grafana Assistant AI.\n\n**frontend apps list**\n> Frontend observability.\n\n**aio11y conversations list**, **aio11y agents list**\n> AI observability.\n\n**dev scaffold**, **dev import**, **dev serve**, **dev lint**\n> Observability-as-code workflow: scaffold, import, live-reload, and lint.\n\n**api** _PATH_\n> Raw passthrough to the Grafana HTTP API.\n\n**skills list**, **skills install**\n> Show and deploy agent skill bundles.\n\n**completion** _SHELL_\n> Generate shell completions for bash, zsh, or fish.\n\n**--since** _DURATION_\n> Time window for queries (e.g. **1h**, **24h**).\n\n**-o**, **--output** _FORMAT_\n> Output format: _json_ or _yaml_.\n\n**--dry-run**\n> Preview changes without applying them.\n\n**--on-error** _MODE_\n> Error-handling strategy: _abort_, _fail_, or _ignore_.\n\n**-p** _PATH_\n> Resource directory used by _resources_ commands.\n\n**-f** _FILE_\n> Input file for _resources_ commands."],["DESCRIPTION","**gcx** is the official command-line interface for Grafana Cloud, with first-class support for querying signals (metrics, logs, traces, profiles), managing alert rules and SLOs, and performing observability-as-code workflows against dashboards and folders. It is designed to be usable both by humans and by agentic coding assistants that need structured access to production observability data.\n\nEach Grafana product has its own top-level command (**metrics**, **logs**, **traces**, **profiles**, **alert**, **slo**, **synth**, **irm**, **k6**, **fleet**, **assistant**, and so on), and resources can be exported to local YAML or JSON with **gcx resources pull** and pushed back with **gcx resources push**, enabling GitOps-style management of Grafana objects."],["CONFIGURATION","Configuration is stored in contexts that point at a Grafana server and an authentication token:\n\n```\ngcx config set contexts.my-grafana.grafana.server [https://your-instance.grafana.net]\ngcx config set contexts.my-grafana.grafana.token [your-service-account-token]\ngcx config use-context [my-grafana]\n```\n\nEnvironment variables **GRAFANA_SERVER**, **GRAFANA_TOKEN**, and **GRAFANA_CLOUD_TOKEN** are honored as well, which is convenient for CI. Use **gcx config check** to verify the active credentials."],["CAVEATS","**gcx** supports Grafana Cloud, Grafana Enterprise, and Grafana OSS **12+**. Cloud-specific products (**slo**, **synth**, **irm**, parts of **fleet** and **kg**) require Grafana Cloud; feature availability varies by deployment type. Use service-account tokens rather than personal tokens for automation."],["HISTORY","**gcx** is published by **Grafana Labs** and is explicitly positioned as \"optimized for agentic usage\", closing the gap between AI coding tools that see only source code and production systems where the code actually runs."],["INSTALL","```brew: brew install gcx```\n\n```nix: nix profile install nixpkgs#gcx```"],["SEE ALSO","[grafana-cli](/man/grafana-cli)(1), [promtool](/man/promtool)(1), [k6](/man/k6)(1)"]],"e":["grafana-cli","promtool","k6"],"x":"gcx auth login --server [https://myorg.grafana.net]"},"gifdiff":{"n":"gifdiff","t":"Check whether two GIF images display identically","s":[["TAGLINE","Check whether two GIF images display identically"],["TLDR","**Compare two GIF files** (exit status 0 = same, 1 = different)\n\n```gifdiff [file1.gif] [file2.gif]```\n\n**Brief mode** — exit status only, no output\n\n```gifdiff -q [file1.gif] [file2.gif]```\n\n**Ignore frame-redraw differences** (frames that render identically but differ in redraw info)\n\n```gifdiff -w [file1.gif] [file2.gif]```\n\n**Use in a shell script**\n\n```gifdiff -q [file1.gif] [file2.gif] && echo \"identical\"```"],["SYNOPSIS","**gifdiff** [**-q** | **--brief**] [**-w** | **--ignore-redraw**] _gif1_ _gif2_"],["PARAMETERS","**-q**, **--brief**\n> Produce no output; only return an exit status (0 if images display identically, 1 otherwise).\n\n**-w**, **--ignore-redraw**\n> Ignore differences that only affect how frames are redrawn. Two animations that render identically but encode redraws differently are still treated as equal.\n\n**-h**, **--help**\n> Print usage information and exit.\n\n**-v**, **--version**\n> Print the gifdiff version and exit."],["DESCRIPTION","**gifdiff** compares two GIF image files and determines whether they display identically when rendered. Unlike a byte-wise comparison, it accounts for semantically irrelevant encoding differences (for example, different frame disposal methods that produce the same visible output).\n\ngifdiff is part of the **gifsicle** toolkit and is primarily useful for regression testing of GIF generators and for confirming that two differently encoded files produce the same animation."],["CAVEATS","gifdiff does **not** produce a visual diff image. For pixel-level visualization of differences use a tool like **compare(1)** from ImageMagick."],["INSTALL","```apt: sudo apt install gifsicle```\n\n```dnf: sudo dnf install gifsicle```\n\n```pacman: sudo pacman -S gifsicle```\n\n```apk: sudo apk add gifsicle```\n\n```zypper: sudo zypper install gifsicle```\n\n```brew: brew install gifsicle```\n\n```nix: nix profile install nixpkgs#gifsicle```"],["SEE ALSO","[gifsicle](/man/gifsicle)(1), [gifview](/man/gifview)(1), [compare](/man/compare)(1)"]],"e":["gifsicle","gifview","compare"],"x":"gifdiff [file1.gif] [file2.gif]"},"git-cp":{"n":"git-cp","t":"Copy files preserving git history","s":[["TAGLINE","Copy files preserving git history"],["TLDR","**Copy a file keeping its history**\n\n```git cp [source_file] [destination_file]```\n\n**Copy a file to a different directory keeping its history**\n\n```git cp [path/to/source] [path/to/destination]```"],["SYNOPSIS","**git-cp** _current_filename_ _destination_filename_"],["DESCRIPTION","**git cp** is a command from git-extras that copies a file while preserving its Git history. This allows merge conflict handling on the copied file.\n\nUnlike a simple filesystem copy followed by git add (which creates a new file with no history), git cp leverages Git's ability to detect file copies through content similarity. When viewing the history of the copied file with git log --follow --find-copies, Git can trace back to the original file's commits.\n\nThis is particularly useful when creating new files based on templates or when splitting functionality from one file into multiple files while maintaining attribution and history."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-mv](/man/git-mv)(1), [git-extras](/man/git-extras)(1)"]],"e":["git-mv","git-extras"],"x":"git cp [source_file] [destination_file]"},"git-fetch":{"n":"git-fetch","t":"Download objects and refs from remote repositories","s":[["TAGLINE","Download objects and refs from remote repositories"],["TLDR","**Fetch from origin**\n\n```git fetch```\n\n**Fetch from specific remote**\n\n```git fetch [remote]```\n\n**Fetch all remotes**\n\n```git fetch --all```\n\n**Fetch and prune**\n\n```git fetch -p```\n\n**Fetch specific branch**\n\n```git fetch [remote] [branch]```\n\n**Fetch with tags**\n\n```git fetch --tags```\n\n**Dry run**\n\n```git fetch --dry-run```"],["SYNOPSIS","**git** **fetch** [_options_] [_remote_] [_refspec_...]"],["PARAMETERS","**--all**\n> Fetch all remotes.\n\n**-p**, **--prune**\n> Remove deleted remote refs.\n\n**--tags**\n> Fetch all tags.\n\n**--depth** _depth_\n> Shallow fetch.\n\n**--dry-run**\n> Show what would be fetched.\n\n**-j**, **--jobs** _n_\n> Parallel fetches for submodules.\n\n**--unshallow**\n> Convert shallow to full.\n\n**-f**, **--force**\n> Allow non-fast-forward updates of remote-tracking branches.\n\n**--no-tags**\n> Do not download any tags (overrides remote.<name>.tagOpt).\n\n**-t**, **--tags**\n> Fetch all tags from the remote, in addition to whatever else would be fetched.\n\n**--update-shallow**\n> Accept refs that update **.git/shallow**, deepening the shallow clone.\n\n**--filter** _SPEC_\n> Partial-clone filter, e.g. `blob:none` (omit blobs) or `tree:0` (commits only).\n\n**--recurse-submodules** [_=on-demand_|_yes_|_no_]\n> Control submodule fetching behavior.\n\n**-q**, **--quiet** / **-v**, **--verbose**\n> Suppress / increase progress output."],["DESCRIPTION","**git fetch** retrieves commits, files, and references from a remote repository, updating your local repository's knowledge of remote branches without modifying your working directory or current branch. This makes it a safe operation for staying synchronized with remote changes.\n\nWhen you fetch, Git downloads all new commits and objects from the remote and updates remote-tracking branches (like origin/main). Your local branches remain unchanged, allowing you to review remote changes before integrating them. This is the crucial distinction from git pull, which fetches and then automatically merges.\n\nThe prune option (-p) removes references to remote branches that no longer exist on the server. Shallow fetches with --depth limit history download, useful for CI/CD environments. The --unshallow option converts a shallow clone to a complete repository."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-pull](/man/git-pull)(1), [git-push](/man/git-push)(1)"]],"e":["git-pull","git-push"],"x":"git fetch"},"grotty":{"n":"grotty","t":"Groff terminal output driver","s":[["TAGLINE","Groff terminal output driver"],["TLDR","**Display in terminal**\n\n```groff -Tutf8 [file.roff]```\n\n**ASCII output**\n\n```groff -Tascii [file.roff]```\n\n**Direct grotty usage**\n\n```grotty [file]```\n\n**Disable SGR sequences**\n\n```grotty -c [file]```"],["SYNOPSIS","**grotty** [_options_] [_file_]"],["PARAMETERS","_FILE_\n> Groff intermediate output.\n\n**-b**\n> Suppress overstriking for bold characters in legacy (**-c**) output format.\n\n**-c**\n> Use grotty's legacy output format (overstriking) instead of SGR escape sequences.\n\n**-d**\n> Ignore all drawing commands in the input.\n\n**-f**\n> Emit a form feed at the end of each page whose last line has no output.\n\n**-i**\n> Render slanted fonts using the SGR italic attribute instead of underlining.\n\n**-o**\n> Suppress overstriking, other than for bold and/or underlined characters.\n\n**-u**\n> Suppress underlining for italic characters in legacy output format.\n\n**-h**\n> Use literal horizontal tab characters in the output.\n\n**-t**\n> Assume the output device supports SGR 38/48 (24-bit color) escape sequences.\n\n**-F** _DIR_\n> Prepend _DIR_/devname to the search path for font and device description files.\n\n**-v**, **--version**\n> Show version information and exit.\n\n**--help**\n> Display help information."],["DESCRIPTION","**grotty** is a groff output driver for terminal display. It produces ASCII or UTF-8 output suitable for viewing in text terminals, supporting bold and underline via SGR escape sequences or overstriking.\n\nIt is the driver used for man page display and terminal document viewing."],["CAVEATS","Part of groff package. Output quality depends on terminal capabilities. Some formatting lost in text mode."],["HISTORY","grotty was developed as part of **GNU groff** to enable terminal viewing of formatted documents."],["INSTALL","```apt: sudo apt install groff-base```\n\n```dnf: sudo dnf install groff-base```\n\n```pacman: sudo pacman -S groff```\n\n```apk: sudo apk add groff```\n\n```zypper: sudo zypper install groff```\n\n```brew: brew install groff```\n\n```nix: nix profile install nixpkgs#groff```"],["SEE ALSO","[groff](/man/groff)(1), [nroff](/man/nroff)(1), [man](/man/man)(1)"],["RESOURCES","```[Source code](https://git.savannah.gnu.org/cgit/groff.git)```\n\n```[Homepage](https://www.gnu.org/software/groff/)```\n\n```[Documentation](https://www.gnu.org/software/groff/groff.html)```"]],"e":["groff","nroff","man"],"x":"groff -Tutf8 [file.roff]"},"grub-reboot":{"n":"grub-reboot","t":"set default boot entry for next reboot only","s":[["TAGLINE","set default boot entry for next reboot only"],["TLDR","Set the **default boot entry** for the next boot only\n\n```sudo grub-reboot [entry_number]```\n\nSet default for an **alternative boot directory** for next boot\n\n```sudo grub-reboot --boot-directory=/[path/to/boot_directory] [entry_number]```\n\nSet default for a **submenu entry** (use > to separate submenu and entry titles)\n\n```sudo grub-reboot \"[submenu_title]>[entry_title]\"```"],["SYNOPSIS","**grub-reboot** [_options_] _entry_"],["PARAMETERS","_entry_\n> Menu entry number (0-indexed), full entry name, or identifier.\n\n**--boot-directory**=_DIR_\n> Find GRUB images under DIR/grub instead of the default /boot/grub.\n\n**-h**, **--help**\n> Print help message and exit.\n\n**-V**, **--version**\n> Print version information and exit.\n\n**-v**, **--verbose**\n> Print verbose messages."],["DESCRIPTION","**grub-reboot** sets the default boot entry for the next boot only. After booting once, GRUB reverts to the permanent default set by **grub-set-default** or the configuration file.\n\nThe entry can be specified as a number (0 for first entry), the full entry name from grub.cfg, or a menu entry identifier. For entries within submenus, use the greater-than character (>) to separate the submenu title and the entry title, with no extra spaces (e.g., \"Advanced options>Ubuntu, with Linux 5.15\"). Use **grep menuentry /boot/grub/grub.cfg** to see available entries.\n\nThis is useful for one-time boots into recovery mode, alternative kernels, or other operating systems without permanently changing the boot order."],["CONFIGURATION","**/etc/default/grub**\n> Must contain GRUB_DEFAULT=saved for grub-reboot to function."],["CAVEATS","Requires **GRUB_DEFAULT=saved** in /etc/default/grub and a subsequent **grub-mkconfig** run to function. The setting is stored in grubenv, which must be writable. Only affects the immediate next boot. When GRUB cannot write to the environment block (e.g., on MDRAID or LVM), the chosen entry may remain the default even after reboot."],["HISTORY","grub-reboot is part of GRUB 2, providing the \"boot once\" functionality that was present in GRUB Legacy. It enables safe testing of alternative boot configurations with automatic fallback."],["INSTALL","```apt: sudo apt install grub2-common```\n\n```dnf: sudo dnf install grub2-common```\n\n```pacman: sudo pacman -S grub```\n\n```apk: sudo apk add grub```\n\n```zypper: sudo zypper install grub2-common```"],["SEE ALSO","[grub-set-default](/man/grub-set-default)(8), [grub-mkconfig](/man/grub-mkconfig)(8), [grub-editenv](/man/grub-editenv)(1)"]],"e":["grub-set-default","grub-mkconfig","grub-editenv"],"x":"sudo grub-reboot [entry_number]"},"hcloud":{"n":"hcloud","t":"command-line interface for Hetzner Cloud","s":[["TAGLINE","command-line interface for Hetzner Cloud"],["TLDR","**List servers**\n\n```hcloud server list```\n\n**Create server**\n\n```hcloud server create --name [myserver] --type [cx22] --image [ubuntu-24.04]```\n\n**SSH to server**\n\n```hcloud server ssh [myserver]```\n\n**Delete server**\n\n```hcloud server delete [myserver]```\n\n**List images**\n\n```hcloud image list```\n\n**Create snapshot**\n\n```hcloud server create-image [myserver] --type snapshot```"],["SYNOPSIS","**hcloud** [_global-options_] _command_ _subcommand_ [_options_]"],["PARAMETERS","_COMMAND_\n> Resource or subcommand to execute.\n\n**server**\n> Manage cloud servers.\n\n**image**\n> Manage server images and snapshots.\n\n**volume**\n> Manage block storage volumes.\n\n**network**\n> Manage private networks.\n\n**firewall**\n> Manage firewalls and rules.\n\n**load-balancer**\n> Manage load balancers.\n\n**ssh-key**\n> Manage SSH keys.\n\n**certificate**\n> Manage TLS certificates.\n\n**context**\n> Manage CLI contexts (project/API token profiles).\n\n**config**\n> Manage local CLI configuration.\n\n**--context** _name_\n> Use a specific context instead of the active one.\n\n**-o**, **--output** _format_\n> Output format: table, json, or yaml.\n\n**--debug**\n> Enable debug output.\n\n**--help**\n> Display help information."],["DESCRIPTION","**hcloud** is the command-line interface for Hetzner Cloud. It manages cloud servers, images, volumes, and networks.\n\nThe tool provides complete control over Hetzner Cloud infrastructure. It supports automation and scripting for cloud resource management."],["CAVEATS","Requires API token. Hetzner Cloud specific. Some operations incur costs."],["HISTORY","hcloud was developed by **Hetzner** as the official CLI for their cloud platform."],["INSTALL","```apt: sudo apt install hcloud-cli```\n\n```dnf: sudo dnf install hcloud```\n\n```pacman: sudo pacman -S hcloud```\n\n```apk: sudo apk add hcloud```\n\n```zypper: sudo zypper install hcloud-cli```\n\n```brew: brew install hcloud```\n\n```nix: nix profile install nixpkgs#hcloud```"],["SEE ALSO","[doctl](/man/doctl)(1), [aws](/man/aws)(1), [gcloud](/man/gcloud)(1)"],["RESOURCES","```[Source code](https://github.com/hetznercloud/cli)```\n```[Documentation](https://github.com/hetznercloud/cli/blob/main/docs/reference/manual/hcloud.md)```\n```[Homepage](https://www.hetzner.com/cloud)```"]],"e":["doctl","aws","gcloud"],"x":"hcloud server list"},"imessage-cli":{"n":"imessage-cli","t":"Provider-neutral CLI for sending and managing iMessage via third-party APIs","s":[["TAGLINE","Provider-neutral CLI for sending and managing iMessage via third-party APIs"],["TLDR","**Send a text message** through a configured provider\n\n```imessage-cli send --provider blooio --to +15551234567 --text 'Hello'```\n\n**List available providers** and their capabilities\n\n```imessage-cli provider list```\n\n**Save a named connection** (credentials go in the OS keychain)\n\n```imessage-cli connection add support --provider blooio```\n\n**Send using a saved connection**\n\n```imessage-cli send --connection support --to +15551234567 --text 'Hello'```\n\n**Dry-run a send** with machine-readable JSON output\n\n```imessage-cli send --provider blooio --to +15551234567 --text 'Hello' --dry-run --json```\n\n**Show agent input schema** for the send command\n\n```imessage-cli schema send --json```\n\n**Install globally** (requires Node.js 20.19+, 22.13+, or 24+)\n\n```npm install --global imessage-cli```"],["SYNOPSIS","**imessage-cli** \\<command\\> [_options_]"],["DESCRIPTION","**imessage-cli** is a command-line interface for iMessage-style messaging through provider-neutral APIs. It is part of the **imessage-sdk** project and is intended for local users and AI agents. It does not talk to Apple's Messages.app or iCloud directly; instead it routes through official bundled providers such as **blooio**, **photon**, and **sendblue**, each of which requires its own account and credentials.\n\nCommands cover sending messages (text, replies, attachments), reading conversations and messages, reactions, typing indicators, attachment download, and provider-specific extensions. Input can be flags or validated JSON (**--input** / stdin). With **--json**, successes go to stdout and structured errors to stderr, with stable exit codes for automation.\n\nCredentials can be stored as named connections in the OS credential store (macOS Keychain, Windows Credential Manager, Linux Secret Service), supplied via environment variables, or passed as one-time flags. Environment and flag values override saved connections for the current process and are never written back to disk. An experimental local signed-webhook server (**webhook serve --experimental**) can emit normalized events as JSON Lines for agent pipelines."],["PARAMETERS","**send**\n> Send a message. Common flags: **--provider**, **--connection**, **--to**, **--conversation**, **--text**, **--reply-to**, **--image**, **--video**, **--file**, **--input**, **--dry-run**, **--json**.\n\n**provider list** / **provider show** _name_\n> List bundled providers or show one provider's normalized capabilities.\n\n**connection add** / **list** / **show** / **doctor** / **remove**\n> Manage named saved connections and keychain-backed credentials.\n\n**schema** [_command_]\n> Print agent-facing input schemas.\n\n**webhook serve**\n> Experimental local signed-webhook receiver (requires **--experimental**).\n\n**completion** _shell_\n> Emit shell completion scripts (**bash**, **zsh**, **fish**, **powershell**).\n\n**--json**\n> Emit machine-readable success/error objects.\n\n**--help**\n> Show help for the CLI or a subcommand."],["CAVEATS","Requires a third-party provider account and API credentials; it is not a free bridge to personal iMessage via a local Mac. Group messaging may be disabled depending on the provider. Provider limits differ (for example Blooio requires public attachment URLs; Sendblue allows one attachment per message). Local attachments are limited to 100 MiB total per command. Passing secrets on the command line can expose them in process lists and shell history—prefer saved connections or environment variables. On headless Linux without a Secret Service daemon, keychain-backed connections may be session-scoped. The webhook server is experimental and not a production queue or HA endpoint."],["HISTORY","**imessage-cli** is published as part of the **imessage-sdk** monorepo by **jmisilo**. The SDK core, providers, Chat SDK adapter, and CLI are independently installable packages under a provider-neutral conversation API."],["SEE ALSO","[wacli](/man/wacli)(1), [matrix-commander](/man/matrix-commander)(1), [curl](/man/curl)(1)"],["RESOURCES","```[Source code](https://github.com/jmisilo/imessage-sdk)```\n\n```[Homepage](https://imessage-sdk.dev)```\n\n```[Documentation](https://github.com/jmisilo/imessage-sdk/blob/main/packages/cli/README.md)```"]],"e":["wacli","matrix-commander","curl"],"x":"imessage-cli send --provider blooio --to +15551234567 --text 'Hello'"},"incus":{"n":"incus","t":"system container and virtual machine manager","s":[["TAGLINE","system container and virtual machine manager"],["TLDR","**Initialize Incus**\n\n```incus admin init```\n\n**Launch a container**\n\n```incus launch images:ubuntu/22.04 [container-name]```\n\n**Launch a VM**\n\n```incus launch images:ubuntu/22.04 [vm-name] --vm```\n\n**List instances**\n\n```incus list```\n\n**Execute command in instance**\n\n```incus exec [name] -- [command]```\n\n**Open a shell in an instance**\n\n```incus shell [name]```\n\n**Copy a file into an instance**\n\n```incus file push [local/path] [name]/[remote/path]```\n\n**Show detailed instance info**\n\n```incus info [name]```\n\n**Stop an instance**\n\n```incus stop [name]```\n\n**Delete an instance**\n\n```incus delete [name]```"],["SYNOPSIS","**incus** _command_ [_options_]"],["SUBCOMMANDS","**admin init**\n> Initialize Incus.\n\n**launch** _image_ _name_\n> Create and start instance.\n\n**start** _name_\n> Start instance.\n\n**stop** _name_\n> Stop instance.\n\n**delete** _name_\n> Delete instance.\n\n**exec** _name_ **--** _cmd_\n> Run command in instance.\n\n**shell** _name_\n> Open an interactive shell in an instance.\n\n**info** _name_\n> Show detailed instance configuration and state.\n\n**file** _push|pull|edit_\n> Transfer or edit files inside an instance.\n\n**list**\n> List all instances.\n\n**image list**\n> List available images.\n\n**copy**\n> Copy instances.\n\n**move**\n> Move instances.\n\n**snapshot**\n> Manage snapshots."],["DESCRIPTION","**incus** is a system container and virtual machine manager. Community fork of LXD, maintained by the Linux Containers project. Manages containers via LXC and VMs via QEMU through a unified CLI and REST API. Supports local and remote access, profiles, storage pools, and networking."],["INSTALL","```apt: sudo apt install incus-client```\n\n```dnf: sudo dnf install incus-client```\n\n```pacman: sudo pacman -S incus```\n\n```apk: sudo apk add incus-client```\n\n```zypper: sudo zypper install incus```\n\n```brew: brew install incus```\n\n```nix: nix profile install nixpkgs#incus```"],["SEE ALSO","[lxc](/man/lxc)(1), [docker](/man/docker)(1)"]],"e":["lxc","docker"],"x":"incus admin init"},"jj-config-get":{"n":"jj-config-get","t":"retrieves configuration values from Jujutsu config files","s":[["TAGLINE","retrieves configuration values from Jujutsu config files"],["TLDR","**Get a configuration value**\n\n```jj config get [user.name]```\n\n**Get the user's email**\n\n```jj config get [user.email]```"],["SYNOPSIS","**jj** **config** **get** _name_"],["DESCRIPTION","**jj config get** retrieves a single configuration value from Jujutsu config files. The output is printed without extra formatting, making it suitable for use in scripts. Repo config takes precedence over user config when both define the same key.\n\nUnlike `jj config list`, this command outputs only the raw value without labels or quotes."],["INSTALL","```pacman: sudo pacman -S jujutsu```\n\n```apk: sudo apk add jujutsu```\n\n```zypper: sudo zypper install jujutsu```\n\n```brew: brew install jujutsu```\n\n```nix: nix profile install nixpkgs#jujutsu```"],["SEE ALSO","[jj](/man/jj)(1), [jj-config-set](/man/jj-config-set)(1), [jj-config-list](/man/jj-config-list)(1)"]],"e":["jj","jj-config-set","jj-config-list"],"x":"jj config get [user.name]"},"kdash":{"n":"kdash","t":"fast terminal dashboard for Kubernetes written in Rust","s":[["TAGLINE","fast terminal dashboard for Kubernetes written in Rust"],["TLDR","**Launch Kubernetes dashboard**\n\n```kdash```\n\n**Use specific kubeconfig**\n\n```kdash -c [~/.kube/config]```\n\n**Set refresh interval**\n\n```kdash -t [5]```"],["SYNOPSIS","**kdash** [_options_]"],["PARAMETERS","**-c**, **--config** _file_\n> Path to kubeconfig file.\n\n**-t**, **--tick-rate** _seconds_\n> Refresh interval in seconds.\n\n**-n**, **--namespace** _name_\n> Default namespace to view."],["DESCRIPTION","**kdash** is a fast terminal dashboard for Kubernetes written in Rust. Provides a read-only view of cluster resources, node metrics, and pod status. Requires metrics-server for resource utilization data. Navigate using keyboard shortcuts."],["CAVEATS","Requires metrics-server deployed on the cluster for resource metrics. May not work properly with Minikube or Kind when running via Docker."],["INSTALL","```brew: brew install kdash```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [k9s](/man/k9s)(1)"]],"e":["kubectl","k9s"],"x":"kdash"},"krew":{"n":"krew","t":"plugin manager for kubectl, the Kubernetes command-line tool","s":[["TAGLINE","plugin manager for kubectl, the Kubernetes command-line tool"],["TLDR","**Update the plugin index**\n\n```kubectl krew update```\n\n**Search for a plugin**\n\n```kubectl krew search [keyword]```\n\n**Install a plugin**\n\n```kubectl krew install [plugin-name]```\n\n**List installed plugins**\n\n```kubectl krew list```\n\n**Show plugin information**\n\n```kubectl krew info [plugin-name]```\n\n**Upgrade all plugins**\n\n```kubectl krew upgrade```\n\n**Upgrade a specific plugin**\n\n```kubectl krew upgrade [plugin-name]```\n\n**Uninstall a plugin**\n\n```kubectl krew uninstall [plugin-name]```\n\n**Add a custom plugin index**\n\n```kubectl krew index add [index-name] [git-url]```"],["SYNOPSIS","**kubectl krew** _command_ [_options_]"],["COMMANDS","**install** _plugin_\n> Install a kubectl plugin.\n\n**uninstall** _plugin_\n> Remove an installed plugin.\n\n**upgrade** [_plugin_]\n> Upgrade installed plugins. Upgrades all if no plugin specified.\n\n**update**\n> Update the local copy of the plugin index.\n\n**search** [_keyword_]\n> Search for available plugins.\n\n**list**\n> List installed kubectl plugins.\n\n**info** _plugin_\n> Show detailed information about a plugin.\n\n**index** _subcommand_\n> Manage custom plugin indexes.\n\n**version**\n> Show krew version and diagnostics.\n\n**completion** _shell_\n> Generate shell completion script.\n\n**help**\n> Help about any command."],["PARAMETERS","**-v**, **--v** _level_\n> Log level verbosity (0-4).\n\n**-h**, **--help**\n> Show help information."],["DESCRIPTION","**krew** is the plugin manager for kubectl, the Kubernetes command-line tool. It simplifies discovering, installing, and managing kubectl plugins across macOS, Linux, and Windows.\n\nKrew maintains a central index of available plugins. Use **kubectl krew update** to sync the local index with the remote repository. The **search** command finds plugins by name or keyword, and **info** shows detailed plugin descriptions and usage.\n\nPlugins are installed to **~/.krew/bin**, which must be in your PATH. After installation, plugins are invoked as **kubectl plugin-name** (without the \"krew\" prefix).\n\nKrew itself is a kubectl plugin, enabling self-updates via **kubectl krew upgrade krew**. Custom plugin indexes can be added with **kubectl krew index add** for private or third-party plugin repositories.\n\nPopular plugins include **ctx** (context switching), **ns** (namespace switching), **neat** (clean YAML output), and **tree** (resource hierarchy visualization)."],["CAVEATS","Requires kubectl v1.12 or later. The ~/.krew/bin directory must be added to PATH manually after installation. Plugin updates require explicit **upgrade** commands; they don't auto-update. Some plugins may have additional dependencies."],["HISTORY","Krew was created by the **Kubernetes SIG CLI** team to address the need for a standardized kubectl plugin distribution mechanism. It was inspired by package managers like Homebrew and apt. The project has grown to host hundreds of community-contributed plugins, becoming the de facto standard for kubectl plugin management."],["INSTALL","```pacman: sudo pacman -S krew```\n\n```brew: brew install krew```\n\n```nix: nix profile install nixpkgs#krew```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [k9s](/man/k9s)(1), [helm](/man/helm)(1)"]],"e":["kubectl","k9s","helm"],"x":"kubectl krew update"},"lein":{"n":"lein","t":"clojure build tool","s":[["TAGLINE","clojure build tool"],["TLDR","**Create new project**\n\n```lein new [template] [project-name]```\n\n**Run project**\n\n```lein run```\n\n**Start REPL**\n\n```lein repl```\n\n**Run tests**\n\n```lein test```\n\n**Build JAR**\n\n```lein jar```\n\n**Build uberjar**\n\n```lein uberjar```\n\n**Install dependencies**\n\n```lein deps```"],["SYNOPSIS","**lein** _task_ [_options_]"],["PARAMETERS","**new** _TEMPLATE_ _NAME_\n> Create project.\n\n**run**\n> Run main function.\n\n**repl**\n> Start REPL.\n\n**test**\n> Run tests.\n\n**jar**\n> Build JAR.\n\n**uberjar**\n> Build standalone JAR.\n\n**deps**\n> Download dependencies.\n\n**--help**\n> Display help information."],["DESCRIPTION","**Leiningen** is the Clojure build tool. It manages projects, dependencies, and builds.\n\nThe tool provides templates for project creation. It integrates with Maven repositories."],["CAVEATS","Requires Java. project.clj configuration. Consider deps.edn for newer projects."],["HISTORY","Leiningen was created by **Phil Hagelberg** as the standard build tool for Clojure projects."],["INSTALL","```pacman: sudo pacman -S leiningen```\n\n```apk: sudo apk add leiningen```\n\n```zypper: sudo zypper install leiningen```\n\n```brew: brew install leiningen```\n\n```nix: nix profile install nixpkgs#leiningen```"],["SEE ALSO","[clojure](/man/clojure)(1), [java](/man/java)(1), [boot](/man/boot)(1)"]],"e":["clojure","java","boot"],"x":"lein new [template] [project-name]"},"lilypond":{"n":"lilypond","t":"music engraving program that compiles text-based notation into sheet music","s":[["TAGLINE","music engraving program that compiles text-based notation into sheet music"],["TLDR","**Compile a score** to PDF (default)\n\n```lilypond [score.ly]```\n\n**Explicitly select PDF output**\n\n```lilypond --pdf [score.ly]```\n\n**Output PNG images** (one per page)\n\n```lilypond --png [score.ly]```\n\n**Output PostScript**\n\n```lilypond --ps [score.ly]```\n\n**Output SVG** (uses the SVG backend)\n\n```lilypond -dbackend=svg [score.ly]```\n\n**Write output to a directory** with custom file prefix\n\n```lilypond -o [output/name] [score.ly]```\n\n**Add an include path** for shared library files\n\n```lilypond -I [/path/to/includes] [score.ly]```\n\n**Disable point-and-click** for distributable PDFs\n\n```lilypond -dno-point-and-click [score.ly]```\n\n**Typeset a score found inside LaTeX** (via lilypond-book)\n\n```lilypond-book [document.lytex]```"],["SYNOPSIS","**lilypond** [_options_] _file_.ly..."],["PARAMETERS","_FILE_\n> LilyPond source file (`.ly`). Multiple files may be given; each is processed independently.\n\n**--pdf**\n> Generate PDF output (default when no backend is specified).\n\n**--png**\n> Generate PNG images (one per page).\n\n**--ps**\n> Generate PostScript. Implied by `--pdf`.\n\n**-f**, **--format=**_FORMAT_\n> Alternative syntax for output format (`pdf`, `ps`, `png`, `svg`).\n\n**-o**, **--output=**_FILE_\n> Output file prefix (or directory). `.pdf`/`.png`/`.ps` is appended.\n\n**-I**, **--include=**_DIR_\n> Add _DIR_ to the include search path. Can be repeated.\n\n**-d**_SETTING_[=_VALUE_]\n> Set a `ly:set-option` program option. Examples: `-dbackend=svg`, `-dno-point-and-click`, `-dpreview`, `-ddelete-intermediate-files`, `-dcrop`, `-dembed-source-code`.\n\n**-e**, **--evaluate=**_EXPR_\n> Evaluate _EXPR_ as Scheme before reading the `.ly` file.\n\n**-j**, **--jail=**_USER,GROUP,JAIL,DIR_\n> Run in a chroot jail (useful for server deployments).\n\n**-s**, **--silent**\n> Suppress progress messages.\n\n**-V**, **--verbose**\n> Verbose output (prints each file being read).\n\n**-w**, **--warranty**\n> Show warranty / GPL info.\n\n**-v**, **--version**\n> Show version.\n\n**-h**, **--help**\n> Show help."],["COMMON -d OPTIONS","**-dno-point-and-click**\n> Disable point-and-click links in the PDF. Recommended for anything you publish — point-and-click embeds absolute paths from your machine.\n\n**-dpreview**\n> Render only the first system, as a \"preview\" PNG/PDF.\n\n**-dcrop**\n> Produce a cropped PDF/PNG around the music only.\n\n**-dbackend=**_NAME_\n> Select a backend: `ps` (default), `svg`, `scm`, `null`.\n\n**-ddelete-intermediate-files**\n> Delete `.ps` / `-systems.*` files after PDF generation.\n\n**-dembed-source-code**\n> Embed the `.ly` source inside the output PDF."],["DESCRIPTION","**LilyPond** compiles a plain-text description of music into engraved sheet music of publication quality. It was inspired by **TeX**'s approach to typography: the user writes logical content (notes, rhythms, lyrics, dynamics) and LilyPond handles spacing, beaming, page breaks, and collision avoidance.\n\nThe same source file can target PDF, PostScript, SVG, or PNG. Backends are chosen via `-dbackend=`. Related tools:\n\n- **lilypond-book** — embed `.ly` snippets inside LaTeX/HTML/DocBook and render them inline.\n- **midi2ly** / **abc2ly** / **musicxml2ly** — convert other formats into LilyPond source.\n- **convert-ly** — upgrade older `.ly` syntax to match the current compiler."],["CAVEATS","Significant learning curve — the input language is rich and unlike WYSIWYG tools. Builds can be slow on large scores; use `-dpreview` during editing. Always pass `-dno-point-and-click` before sharing PDFs to avoid leaking filesystem paths."],["HISTORY","**LilyPond** was started in **1996** by **Han-Wen Nienhuys** and **Jan Nieuwenhuizen**, inspired by TeX's typographic quality and MusiXTeX's music input language. It is now a GNU project distributed under the GPL and written in C++ and Scheme (GNU Guile)."],["INSTALL","```apt: sudo apt install lilypond```\n\n```dnf: sudo dnf install lilypond```\n\n```pacman: sudo pacman -S lilypond```\n\n```apk: sudo apk add lilypond```\n\n```zypper: sudo zypper install lilypond```\n\n```brew: brew install lilypond```\n\n```nix: nix profile install nixpkgs#lilypond```"],["SEE ALSO","[abc2ly](/man/abc2ly)(1), [musescore](/man/musescore)(1)"]],"e":["abc2ly","musescore"],"x":"lilypond [score.ly]"},"mate-dictionary":{"n":"mate-dictionary","t":"graphical dictionary application for the MATE desktop","s":[["TAGLINE","graphical dictionary application for the MATE desktop"],["TLDR","**Open dictionary**\n\n```mate-dictionary```\n\n**Look up a word**\n\n```mate-dictionary --look-up [word]```\n\n**Look up a word using a specific database**\n\n```mate-dictionary --database [wn] --look-up [word]```\n\n**Look up a word and print to console without GUI**\n\n```mate-dictionary --no-window --look-up [word]```\n\n**Look up a word using a specific source**\n\n```mate-dictionary --source [source_name] --look-up [word]```"],["SYNOPSIS","**mate-dictionary** [_options_]"],["PARAMETERS","**--look-up** _word_\n> Look up the specified word using the pre-defined dictionary source.\n\n**--match** _word_\n> Find matching words using the pre-defined dictionary source.\n\n**--source**, **-s** _source_\n> Use the specified source for looking up words.\n\n**--database**, **-D** _database_\n> Use the specified database for looking up words.\n\n**--no-window**, **-n**\n> Print definitions to the console without launching the GUI."],["DESCRIPTION","**mate-dictionary** is a graphical dictionary application for the MATE desktop environment. It looks up words using DICT protocol servers and supports multiple dictionaries and search strategies. Results can be saved to file or printed."],["INSTALL","```apt: sudo apt install mate-utils```\n\n```dnf: sudo dnf install mate-utils```\n\n```pacman: sudo pacman -S mate-utils```\n\n```apk: sudo apk add mate-dictionary```\n\n```zypper: sudo zypper install mate-dictionary```\n\n```nix: nix profile install nixpkgs#mate-utils```"],["SEE ALSO","[dict](/man/dict)(1)"]],"e":["dict"],"x":"mate-dictionary"},"nexus":{"n":"nexus","t":"Terminal-based HTTP client for API testing","s":[["TAGLINE","Terminal-based HTTP client for API testing"],["TLDR","**Launch the HTTP client TUI**\n\n```nexus```"],["SYNOPSIS","**nexus** [_options_]"],["DESCRIPTION","**nexus** is a terminal-based HTTP client for API testing that replicates Postman's organized approach within a TUI. It supports all HTTP methods with request organization into collections.\n\nFeatures include composing requests with URL, headers, body, and parameters, formatted response display, request persistence between sessions, Postman collection import, and curl command export. Navigation and management use keyboard shortcuts throughout."],["CAVEATS","Does not support all Postman features like scripting or environment variable substitution."],["HISTORY","**nexus** was created by **Pranav CS** (pranav-cs-1) and is written in **Rust**."],["INSTALL","```dnf: sudo dnf install nexus```\n\n```brew: brew install nexus```\n\n```nix: nix profile install nixpkgs#nexus```"],["SEE ALSO","[curl](/man/curl)(1), [httpie](/man/httpie)(1)"]],"e":["curl","httpie"],"x":"nexus"},"nix-build.3":{"n":"nix-build.3","t":"Build a Nix derivation and create a result symlink","s":[["TAGLINE","Build a Nix derivation and create a result symlink"],["TLDR","**Build the default expression in the current directory**\n\n```nix-build```\n\n**Build a specific Nix expression**\n\n```nix-build [file.nix]```\n\n**Build a named attribute from a file**\n\n```nix-build [file.nix] -A [attribute]```\n\n**Pass a typed argument to the expression**\n\n```nix-build --arg [name] [value]```\n\n**Pass a string argument to the expression**\n\n```nix-build --argstr [name] \"[string]\"```\n\n**Continue building other derivations on failure**\n\n```nix-build --keep-going```\n\n**Show what would be built without building**\n\n```nix-build --dry-run```\n\n**Build without creating a result symlink**\n\n```nix-build --no-out-link```"],["SYNOPSIS","**nix-build** [_options_] [_paths_...]"],["PARAMETERS","_PATH_\n> Path to a Nix expression file or attribute. Defaults to **default.nix**.\n\n**-A** _attr_, **--attr** _attr_\n> Select an attribute from the top-level expression (dotted paths supported).\n\n**--arg** _NAME_ _VALUE_\n> Pass **VALUE** (a Nix expression) as the argument **NAME**.\n\n**--argstr** _NAME_ _VALUE_\n> Pass **VALUE** as a string argument **NAME**.\n\n**--keep-going**, **-k**\n> Keep building dependencies even after a failure.\n\n**--dry-run**\n> Show which derivations would be built or substituted without building them.\n\n**--no-out-link**\n> Do not create a **result** symlink to the output.\n\n**-o** _name_, **--out-link** _name_\n> Change the name of the result symlink (default: **result**).\n\n**-j** _n_, **--max-jobs** _n_\n> Number of derivations to build in parallel.\n\n**--option** _NAME_ _VALUE_\n> Set an arbitrary Nix configuration option."],["DESCRIPTION","**nix-build** is one of the classic Nix CLI tools. It evaluates a Nix expression into one or more derivations, realises them (building or substituting their outputs), and creates a symlink named **result** in the current directory pointing at the output path. If multiple outputs are produced, symlinks are named **result**, **result-2**, and so on.\n\nThis manpage variant documents the same binary as **nix-build(1)**, shipped under an alternate section in some distributions."],["CAVEATS","The **result** symlink is a garbage-collection root, preventing the build output from being garbage collected. Delete the symlink (or pass **--no-out-link**) when you no longer need the output. For flake-based workflows prefer the newer **nix build** command."],["INSTALL","```apt: sudo apt install nix-bin```\n\n```dnf: sudo dnf install nix```\n\n```pacman: sudo pacman -S nix```\n\n```apk: sudo apk add nix```\n\n```zypper: sudo zypper install nix```\n\n```nix: nix profile install nixpkgs#nix```"],["SEE ALSO","[nix-build](/man/nix-build)(1), [nix](/man/nix)(1), [nix-env](/man/nix-env)(1), [nix-store](/man/nix-store)(1)"]],"e":["nix-build","nix","nix-env","nix-store"],"x":"nix-build"},"oapi-codegen":{"n":"oapi-codegen","t":"generates Go code from OpenAPI 3","s":[["TAGLINE","generates Go code from OpenAPI 3"],["TLDR","**Generate Go server code**\n\n```oapi-codegen -generate server -package [api] [spec.yaml] > [server.go]```\n\n**Generate Go client code**\n\n```oapi-codegen -generate client -package [api] [spec.yaml] > [client.go]```\n\n**Generate types only**\n\n```oapi-codegen -generate types -package [api] [spec.yaml] > [types.go]```\n\n**Generate with config file**\n\n```oapi-codegen --config [config.yaml] [spec.yaml]```\n\n**Generate all components**\n\n```oapi-codegen -generate server,client,types -package [api] [spec.yaml]```"],["SYNOPSIS","**oapi-codegen** [_options_] _spec_"],["PARAMETERS","**-generate** _type_\n> server, client, types, spec, chi-server, gin-server, echo-server.\n\n**-package** _name_\n> Go package name.\n\n**-o** _file_\n> Output file.\n\n**--config** _file_\n> Config file.\n\n**-include-tags** _tags_\n> Include only these tags.\n\n**-exclude-tags** _tags_\n> Exclude these tags."],["DESCRIPTION","**oapi-codegen** generates Go code from OpenAPI 3.0 specifications. It can generate server boilerplate, clients, and type definitions.\n\nSupports multiple server frameworks including Chi, Gin, and Echo."],["CONFIG FILE","```yaml"],["config.yaml","package: api\ngenerate:\n  - types\n  - chi-server\n  - client\noutput: api.gen.go\n```"],["CAVEATS","OpenAPI 3.0+ only. Generated code may need modification. Large specs produce large files."],["HISTORY","oapi-codegen was created by **DeepMap, Inc.** to provide high-quality Go code generation from OpenAPI specifications."],["INSTALL","```nix: nix profile install nixpkgs#oapi-codegen```"],["SEE ALSO","[openapi-generator](/man/openapi-generator)(1)"]],"e":["openapi-generator"],"x":"oapi-codegen -generate server -package [api] [spec.yaml] > [server.go]"},"phetch":{"n":"phetch","t":"Quick terminal client for browsing the Gopher protocol","s":[["TAGLINE","Quick terminal client for browsing the Gopher protocol"],["TLDR","**Launch with the default Gopher page**\n\n```phetch```\n\n**Open a specific Gopher URL**\n\n```phetch [gopher://example.com]```\n\n**Use a custom media player for audio/video**\n\n```phetch -m [mpv]```\n\n**Open a URL with TLS encryption**\n\n```phetch -s [gopher://example.com]```\n\n**Open a URL through Tor**\n\n```phetch -o [gopher://example.com]```\n\n**Print raw Gopher response without interactive mode**\n\n```phetch -r [gopher://example.com]```\n\n**Connect to local Gopher server**\n\n```phetch -l```"],["SYNOPSIS","**phetch** [_options_] [_url_]"],["PARAMETERS","**-s**, **--tls**\n> Try to open Gopher URLs securely with TLS.\n\n**-o**, **--tor**\n> Use local Tor proxy to open all pages.\n\n**-S**, **-O**\n> Disable TLS or Tor.\n\n**-w**, **--wrap** _COLUMN_\n> Wrap long lines in text views at COLUMN.\n\n**-m**, **--media** _PROGRAM_\n> Set the media player for opening audio and video files.\n\n**-M**, **--no-media**\n> Just download media files, don't play them.\n\n**-a**, **--autoplay**\n> Autoplay media files without prompting.\n\n**-A**, **--no-autoplay**\n> Prompt before playing media files.\n\n**-r**, **--raw**\n> Print raw Gopher response only.\n\n**-p**, **--print**\n> Print rendered Gopher response only.\n\n**-l**, **--local**\n> Connect to 127.0.0.1:7070.\n\n**-e**, **--encoding** _ENCODING_\n> Render text documents in CP437 or UTF8.\n\n**-c**, **--config** _FILE_\n> Use FILE instead of ~/.config/phetch/phetch.conf.\n\n**-C**, **--no-config**\n> Don't use any config file.\n\n**-t**, **--theme** _FILE_\n> Use FILE for color theme or print current theme.\n\n**-h**, **--help**\n> Show help screen.\n\n**-v**, **--version**\n> Show version."],["DESCRIPTION","**phetch** is a small, fast Gopher protocol client for the terminal written in Rust. It supports Gopher searches, text and menu pages, file downloads, opt-in history tracking, TLS encryption, and Tor support. Navigation is entirely keyboard-driven with both jump-to-link and incremental search options."],["HISTORY","**phetch** was created by **xvxx** and is written in **Rust**."],["INSTALL","```nix: nix profile install nixpkgs#phetch```"],["SEE ALSO","[lynx](/man/lynx)(1), [curl](/man/curl)(1), [w3m](/man/w3m)(1)"]],"e":["lynx","curl","w3m"],"x":"phetch"},"picttoppm":{"n":"picttoppm","t":"converts Macintosh PICT images to PPM","s":[["TAGLINE","converts Macintosh PICT images to PPM"],["TLDR","**Convert a Macintosh PICT file to PPM**\n\n```picttoppm [input.pict] > [output.ppm]```\n\n**Convert with full resolution output**\n\n```picttoppm -fullres [input.pict] > [output.ppm]```\n\n**Convert with verbose diagnostic output**\n\n```picttoppm -verbose [input.pict] > [output.ppm]```\n\n**Convert a PICT file without the 512-byte header**\n\n```picttoppm -noheader [input.pict] > [output.ppm]```"],["SYNOPSIS","**picttoppm** [_options_] [_file_]"],["PARAMETERS","**-verbose** _n_\n> Print diagnostic information about the PICT file and conversion process. Verbosity level ranges from 0 to 2.\n\n**-fullres**\n> Force images to retain their full resolution instead of being scaled down. Disables all PICT operations except images.\n\n**-noheader**\n> Do not assume the first 512 bytes of the file are a header. Useful for PICT data not stored in the data fork of a PICT file.\n\n**-quickdraw**\n> Execute only pure QuickDraw operations. Disables interpretation of special PostScript printer operations.\n\n**-fontdir** _file_\n> Specify a file containing BDF fonts for use when drawing text."],["DESCRIPTION","**picttoppm** reads a Macintosh PICT file (version 1 or 2) and outputs a PPM image. PICT was the native graphics format on classic Mac OS, encoded in QuickDraw commands. Part of the Netpbm toolkit."],["INSTALL","```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[ppmtopict](/man/ppmtopict)(1), [macptopbm](/man/macptopbm)(1)"]],"e":["ppmtopict","macptopbm"],"x":"picttoppm [input.pict] > [output.ppm]"},"pkl":{"n":"pkl","t":"Programmable configuration language from Apple","s":[["TAGLINE","Programmable configuration language from Apple"],["TLDR","**Evaluate Pkl file**\n\n```pkl eval [config.pkl]```\n\n**Output as JSON**\n\n```pkl eval -f json [config.pkl]```\n\n**Output as YAML**\n\n```pkl eval -f yaml [config.pkl]```\n\n**Check syntax**\n\n```pkl check [config.pkl]```\n\n**Start REPL**\n\n```pkl repl```"],["SYNOPSIS","**pkl** [_command_] [_options_] [_file_]"],["PARAMETERS","**eval** _FILE_\n> Evaluate configuration.\n\n**check** _FILE_\n> Validate syntax.\n\n**repl**\n> Start interactive mode.\n\n**-f** _FORMAT_\n> Output format.\n\n**--help**\n> Display help."],["DESCRIPTION","**pkl** is a programmable configuration language developed by Apple. It evaluates .pkl files and generates output in multiple formats including JSON, YAML, XML, and property lists, combining the readability of static configuration with the power of a programming language.\n\nPkl provides type safety, validation, and code reuse through classes and modules, catching configuration errors at evaluation time rather than at deployment. The REPL mode allows interactive exploration and testing of configuration expressions."],["CAVEATS","Pkl files can import modules from the network, which may have security implications. The language requires a JVM-based runtime or native binaries for evaluation."],["HISTORY","Pkl was created by **Apple** and open-sourced in **February 2024** as a type-safe, programmable configuration language."],["INSTALL","```brew: brew install pkl```\n\n```nix: nix profile install nixpkgs#pkl```"],["SEE ALSO","[jsonnet](/man/jsonnet)(1), [dhall](/man/dhall)(1)"]],"e":["jsonnet","dhall"],"x":"pkl eval [config.pkl]"},"pnmtorle":{"n":"pnmtorle","t":"Convert PNM to Utah RLE format","s":[["TAGLINE","Convert PNM to Utah RLE format"],["TLDR","**Convert PNM to Utah RLE format**\n\n```pnmtorle [input.pnm] > [output.rle]```\n\n**Add alpha channel**\n\n```pnmtorle -alpha [mask.pgm] [input.pnm] > [output.rle]```"],["SYNOPSIS","**pnmtorle** [_options_] [_file_]"],["PARAMETERS","**-alpha** _file_\n> Alpha mask file.\n\n**-verbose**\n> Print progress information."],["DESCRIPTION","**pnmtorle** converts PNM images to Utah RLE (Run Length Encoded) format used by the Utah Raster Toolkit. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[rletopnm](/man/rletopnm)(1), [pnmtosgi](/man/pnmtosgi)(1)"]],"e":["rletopnm","pnmtosgi"],"x":"pnmtorle [input.pnm] > [output.rle]"},"ppmmorph":{"n":"ppmmorph","t":"Create morphing sequence between images","s":[["TAGLINE","Create morphing sequence between images"],["TLDR","**Morph between two images**\n\n```ppmmorph -n [10] [control.ctl] [image1.ppm] [image2.ppm] > [sequence.ppm]```\n\n**Generate frames**\n\n```ppmmorph -n [30] [morph.ctl] [start.ppm] [end.ppm]```"],["SYNOPSIS","**ppmmorph** [_options_] _controlfile_ _image1_ _image2_"],["PARAMETERS","**-n** _count_\n> Number of intermediate frames.\n\n**controlfile**\n> Defines corresponding points."],["DESCRIPTION","**ppmmorph** creates a morphing sequence between two images. Control points define corresponding features in both images, and the tool generates smooth transitions.\n\nPart of Netpbm toolkit."],["CONTROL FILE FORMAT","```"],["x1 y1 x2 y2 for image1 point to image2 point","100 50  150 60\n200 100 250 110\n```"],["EXAMPLES","```bash"],["Create 10-frame morph","ppmmorph -n 10 morph.ctl face1.ppm face2.ppm > sequence.ppm"],["Split into individual frames","ppmmorph -n 20 morph.ctl a.ppm b.ppm | pamsplit frame%d.ppm"],["Create animation","ppmmorph -n 30 ctrl.txt img1.ppm img2.ppm | \\\n  pamtogif > morph.gif\n```"],["CAVEATS","Requires careful control point placement. More points give smoother results. Images should be same size."],["HISTORY","ppmmorph is part of **Netpbm**, implementing the morphing technique popularized in the 1990s for film effects."],["SEE ALSO","[pnmpaste](/man/pnmpaste)(1), [ppmfade](/man/ppmfade)(1), [netpbm](/man/netpbm)(1)"]],"e":["pnmpaste","ppmfade","netpbm"],"x":"ppmmorph -n [10] [control.ctl] [image1.ppm] [image2.ppm] > [sequence.ppm]"},"prince":{"n":"prince","t":"Convert HTML and CSS to PDF documents","s":[["TAGLINE","Convert HTML and CSS to PDF documents"],["TLDR","**Convert HTML to PDF**\n\n```prince [input.html] -o [output.pdf]```\n\n**Apply CSS stylesheet**\n\n```prince -s [style.css] [input.html] -o [output.pdf]```\n\n**Convert from URL**\n\n```prince [https://example.com] -o [page.pdf]```\n\n**Multiple inputs**\n\n```prince [page1.html] [page2.html] -o [combined.pdf]```"],["SYNOPSIS","**prince** [_options_] _files_... [**-o** _output.pdf_]"],["PARAMETERS","**-o**, **--output** _file_\n> Output PDF file.\n\n**-s**, **--style** _file_\n> Additional CSS file.\n\n**--javascript**\n> Enable JavaScript.\n\n**--media** _type_\n> CSS media type.\n\n**--page-size** _size_\n> Page dimensions.\n\n**--page-margin** _margin_\n> Page margins.\n\n**--encrypt**\n> Encrypt PDF."],["DESCRIPTION","**Prince** is a commercial HTML/CSS to PDF converter with excellent standards compliance. It produces high-quality PDFs suitable for print production."],["EXAMPLES","```bash"],["Basic conversion","prince document.html -o document.pdf"],["With custom styles","prince -s print.css report.html -o report.pdf"],["From URL","prince https://example.com/page -o page.pdf"],["Multiple files combined","prince cover.html content.html appendix.html -o book.pdf"],["With JavaScript","prince --javascript dynamic.html -o output.pdf"],["Custom page size","prince --page-size=\"A4\" document.html -o a4.pdf\n```"],["CAVEATS","Commercial license required for production use. Free for non-commercial. Excellent CSS Paged Media support."],["HISTORY","Prince was developed by **YesLogic** starting in 2003 as a high-quality HTML to PDF converter for publishing."],["SEE ALSO","[wkhtmltopdf](/man/wkhtmltopdf)(1), [weasyprint](/man/weasyprint)(1), [pandoc](/man/pandoc)(1)"]],"e":["wkhtmltopdf","weasyprint","pandoc"],"x":"prince [input.html] -o [output.pdf]"},"printafm":{"n":"printafm","t":"Display Adobe Font Metrics information","s":[["TAGLINE","Display Adobe Font Metrics information"],["TLDR","**Display AFM font metrics**\n\n```printafm [font.afm]```\n\n**Show specific font info**\n\n```printafm [Times-Roman]```"],["SYNOPSIS","**printafm** [_font_]"],["PARAMETERS","_FONT_\n> AFM font file or name."],["DESCRIPTION","**printafm** displays detailed metrics from Adobe Font Metrics (AFM) files, including character widths, bounding boxes, kerning pairs, and other typographic properties. AFM files accompany PostScript Type 1 fonts and describe their dimensions for use in page layout and typesetting.\n\nThis Ghostscript utility is useful for inspecting font properties when debugging PostScript or PDF rendering, or when developing applications that need to calculate text dimensions for layout purposes."],["CAVEATS","Requires AFM files. Part of Ghostscript suite."],["HISTORY","printafm is part of **Ghostscript** for font metrics display."],["INSTALL","```apt: sudo apt install ghostscript```\n\n```dnf: sudo dnf install ghostscript```\n\n```pacman: sudo pacman -S ghostscript```\n\n```apk: sudo apk add ghostscript```\n\n```zypper: sudo zypper install ghostscript```\n\n```brew: brew install ghostscript```\n\n```nix: nix profile install nixpkgs#ghostscript```"],["SEE ALSO","[gs](/man/gs)(1), [fc-list](/man/fc-list)(1)"]],"e":["gs","fc-list"],"x":"printafm [font.afm]"},"pulumi-console":{"n":"pulumi-console","t":"Open Pulumi web console in browser","s":[["TAGLINE","Open Pulumi web console in browser"],["TLDR","**Open Pulumi Console in the default browser**\n\n```pulumi console```\n\n**Open a specific stack in the console**\n\n```pulumi console -s [stack_name]```\n\n**Open the console from a different project directory**\n\n```pulumi console -C [path/to/project]```"],["SYNOPSIS","**pulumi** **console** [_options_]"],["PARAMETERS","**-s**, **--stack** _string_\n> The name of the stack to view in the console.\n\n**-C**, **--cwd** _string_\n> Run as if pulumi was started in the given directory.\n\n**--color** _string_\n> Control colorized output (always, never, raw, auto). Default is auto.\n\n**--non-interactive**\n> Disable interactive mode for all commands."],["DESCRIPTION","**pulumi console** opens the Pulumi Cloud web console in the default browser. It provides a web-based management interface for viewing and managing stacks, resources, deployment history, and activity logs. If a stack is specified with **-s**, the console opens directly to that stack's page."],["INSTALL","```pacman: sudo pacman -S pulumi```\n\n```apk: sudo apk add pulumi```\n\n```brew: brew install pulumi```\n\n```nix: nix profile install nixpkgs#pulumi```"],["SEE ALSO","[pulumi](/man/pulumi)(1), [pulumi-stack](/man/pulumi-stack)(1), [pulumi-login](/man/pulumi-login)(1)"]],"e":["pulumi","pulumi-stack","pulumi-login"],"x":"pulumi console"},"pw-jack":{"n":"pw-jack","t":"Run JACK applications on PipeWire","s":[["TAGLINE","Run JACK applications on PipeWire"],["TLDR","Run a command with its arguments using **PipeWire**\n\n```pw-jack command arg1 arg2```\n\nRun a command in **verbose** mode\n\n```pw-jack -v command```\n\nConnect to a specific **remote** PipeWire instance\n\n```pw-jack -r remote_instance command```\n\nDisplay **help**\n\n```pw-jack -h```"],["SYNOPSIS","**pw-jack** [_options_] _command_ [_arguments_...]"],["PARAMETERS","**-v**\n> Enable verbose mode\n\n**-r** _name_\n> Connect to specific remote PipeWire instance\n\n**-h**\n> Display help message"],["DESCRIPTION","**pw-jack** runs JACK applications using PipeWire as the audio backend. It provides a compatibility layer that allows JACK-based audio applications to work seamlessly with PipeWire.\n\nThe wrapper intercepts JACK library calls and redirects them through PipeWire's JACK compatibility implementation."],["CAVEATS","Some advanced JACK features may behave differently under PipeWire. Applications that depend on specific JACK server behavior may require adjustment."],["HISTORY","Part of **PipeWire**, the modern multimedia framework for Linux. Enables JACK application compatibility without requiring a separate JACK server."],["INSTALL","```apt: sudo apt install pipewire-jack```\n\n```pacman: sudo pacman -S pipewire-jack```\n\n```apk: sudo apk add pipewire-jack```\n\n```zypper: sudo zypper install pipewire-jack```"],["SEE ALSO","[pipewire](/man/pipewire)(1), [jackd](/man/jackd)(1), [pw-cli](/man/pw-cli)(1)"]],"e":["pipewire","jackd","pw-cli"],"x":"pw-jack command arg1 arg2"},"recordmydesktop":{"n":"recordmydesktop","t":"Record desktop screen to video file","s":[["TAGLINE","Record desktop screen to video file"],["TLDR","**Record full screen**\n\n```recordmydesktop -o [output.ogv]```\n\n**Record specific window**\n\n```recordmydesktop --windowid $(xdotool selectwindow) -o [output.ogv]```\n\n**Record area**\n\n```recordmydesktop -x [100] -y [100] --width [800] --height [600] -o [output.ogv]```\n\n**Record without sound**\n\n```recordmydesktop --no-sound -o [output.ogv]```"],["SYNOPSIS","**recordmydesktop** [_options_]"],["PARAMETERS","**-o** _file_\n> Output file.\n\n**--windowid** _id_\n> Record specific window.\n\n**-x**, **-y**\n> Starting position.\n\n**--width**, **--height**\n> Recording dimensions.\n\n**--no-sound**\n> Disable audio.\n\n**--fps** _n_\n> Frames per second.\n\n**--delay** _n_\n> Delay before start.\n\n**--on-the-fly-encoding**\n> Encode while recording."],["DESCRIPTION","**recordmydesktop** is a lightweight screen recorder for Linux X11 sessions that captures desktop video and audio to Ogg Theora/Vorbis format. It can record the full screen, a specific window selected by ID, or a rectangular region defined by position and dimensions.\n\nAudio is captured from the default ALSA or OSS input device and can be disabled with **--no-sound**. Recording is stopped with Ctrl+C, after which the tool encodes the captured frames into the final output file. The **--on-the-fly-encoding** option encodes during recording to reduce post-processing time at the cost of higher CPU usage."],["EXAMPLES","```bash"],["Record full screen","recordmydesktop -o screencast.ogv"],["Record window (click to select)","recordmydesktop --windowid $(xdotool selectwindow)"],["Record region","recordmydesktop -x 0 -y 0 --width 1280 --height 720 -o region.ogv"],["High quality","recordmydesktop --v_quality 63 --s_quality 10 -o hq.ogv"],["No cursor","recordmydesktop --no-cursor -o nocursor.ogv\n```"],["CONTROLS","```\nCtrl+C       - Stop recording\nCtrl+Alt+P   - Pause (if enabled)\n```"],["CAVEATS","X11 only (no Wayland). Output is Ogg format. Convert with ffmpeg for other formats."],["HISTORY","recordmydesktop was created by **John Googalidis** as a simple, efficient screen recorder for Linux."],["INSTALL","```apt: sudo apt install recordmydesktop```\n\n```pacman: sudo pacman -S recordmydesktop```\n\n```apk: sudo apk add recordmydesktop```\n\n```zypper: sudo zypper install recordmydesktop```"],["SEE ALSO","[ffmpeg](/man/ffmpeg)(1), [peek](/man/peek)(1), [obs](/man/obs)(1)"]],"e":["ffmpeg","peek","obs"],"x":"recordmydesktop -o [output.ogv]"},"rkhunter":{"n":"rkhunter","t":"Scan system for rootkits and vulnerabilities","s":[["TAGLINE","Scan system for rootkits and vulnerabilities"],["TLDR","**Check** system for rootkits\n\n```sudo rkhunter --check```\n\n**Check** non-interactively (no keypress prompts)\n\n```sudo rkhunter --check --sk```\n\n**Update** database\n\n```sudo rkhunter --update```\n\n**Update** file properties database after legitimate changes\n\n```sudo rkhunter --propupd```\n\n**List** all tests\n\n```sudo rkhunter --list tests```\n\nCheck **version**\n\n```sudo rkhunter --versioncheck```\n\nRun from **cron** reporting warnings only\n\n```sudo rkhunter --cronjob --report-warnings-only```"],["SYNOPSIS","**rkhunter** [**--check**] [**--update**] [**--propupd**] [**--list**] [_options_]"],["PARAMETERS","**-c**, **--check**\n> Perform system check for rootkits, backdoors, and exploits.\n\n**--update**\n> Update data files and malware signatures.\n\n**--propupd**\n> Update file properties database with current values (run after legitimate system changes).\n\n**--list** [_tests|rootkits|lang|perl|propfiles_]\n> List supported capabilities.\n\n**--versioncheck**\n> Check for a newer rkhunter version.\n\n**-C**, **--config-check**\n> Validate configuration file(s).\n\n**--sk**, **--skip-keypress**\n> Don't wait for a keypress between test groups.\n\n**--rwo**, **--report-warnings-only**\n> Only display warnings.\n\n**--cronjob**\n> Optimize output for cron execution (no colors, no keypress).\n\n**--configfile** _file_\n> Use an alternate configuration file.\n\n**-l**, **--logfile** _file_\n> Write log output to a specific file.\n\n**--appendlog**\n> Append to an existing log file instead of overwriting.\n\n**--disable** _tests_\n> Disable specific tests (comma-separated list).\n\n**--enable** _tests_\n> Enable only the specified tests.\n\n**--pkgmgr** _type_\n> Use package manager verification (RPM, DPKG, BSD, SOLARIS, NONE).\n\n**-q**, **--quiet**\n> Suppress all output.\n\n**--nocolors**\n> Disable colored output.\n\n**-V**, **--version**\n> Display version information.\n\n**-h**, **--help**\n> Display help information."],["DESCRIPTION","**rkhunter** (Rootkit Hunter) scans Linux systems for rootkits, backdoors, and local exploits. It checks for hidden files, suspicious kernel modules, modified binaries, and other signs of compromise.\n\nThe tool maintains a database of known malware signatures and file checksums, comparing current system state against known-good values."],["CONFIGURATION","**/etc/rkhunter.conf**\n> Main configuration file controlling scan behavior, whitelisted files, update mirrors, and notification settings.\n\n**/var/lib/rkhunter/db/**\n> Database directory containing malware signatures, file property hashes, and known-good checksums used for comparison during scans.\n\n**/var/log/rkhunter.log**\n> Default log file where scan results and warnings are recorded."],["CAVEATS","Requires regular updates for effectiveness. May produce false positives on customized systems. Run --propupd after legitimate system changes. Log review recommended after checks."],["HISTORY","**rkhunter** was created by **Michael Boelen** to help system administrators detect rootkits and potential security issues. It's been actively maintained since 2003."],["INSTALL","```dnf: sudo dnf install rkhunter```\n\n```pacman: sudo pacman -S rkhunter```\n\n```brew: brew install rkhunter```"],["SEE ALSO","[chkrootkit](/man/chkrootkit)(1), [aide](/man/aide)(1), [tripwire](/man/tripwire)(8)"]],"e":["chkrootkit","aide","tripwire"],"x":"sudo rkhunter --check"},"sc_wartsdump":{"n":"sc_wartsdump","t":"Verbose dump of information in scamper warts files","s":[["TAGLINE","Verbose dump of information in scamper warts files"],["TLDR","**Display verbose warts file contents**\n\n```sc_wartsdump [input.warts]```\n\n**Dump multiple warts files**\n\n```sc_wartsdump [file1.warts] [file2.warts]```"],["SYNOPSIS","**sc_wartsdump** [_file_...]"],["DESCRIPTION","**sc_wartsdump** provides a verbose dump of the information contained in a sequence of warts files, which are the binary output format of scamper. The output is structured and suitable for initial analysis of results, but the format is not guaranteed to remain stable and is not suitable for automated parsing.\n\nFor machine-parsable output, use **sc_warts2json** or **sc_warts2text** instead. Part of the scamper Internet measurement toolkit developed by CAIDA."],["CAVEATS","The output format of sc_wartsdump may change between releases and should not be relied upon for automated processing. Use sc_warts2json for stable structured output."],["INSTALL","```apt: sudo apt install scamper```\n\n```zypper: sudo zypper install scamper```\n\n```brew: brew install scamper```"],["SEE ALSO","[sc_warts2text](/man/sc_warts2text)(1), [scamper](/man/scamper)(1)"]],"e":["sc_warts2text","scamper"],"x":"sc_wartsdump [input.warts]"},"simple-mtpfs":{"n":"simple-mtpfs","t":"FUSE filesystem driver for MTP devices","s":[["TAGLINE","FUSE filesystem driver for MTP devices"],["TLDR","**List** attached MTP devices\n\n```simple-mtpfs --list-devices```\n\n**Mount** the first available MTP device\n\n```simple-mtpfs [mountpoint]```\n\n**Mount** a specific device by number\n\n```simple-mtpfs --device [number] [mountpoint]```\n\n**Mount** using a libmtp device file\n\n```simple-mtpfs /dev/libmtp-[id] [mountpoint]```\n\n**Unmount** the MTP filesystem\n\n```fusermount -u [mountpoint]```"],["SYNOPSIS","**simple-mtpfs** [_options_] [_device_] _mountpoint_"],["PARAMETERS","**-l**, **--list-devices**\n> List available MTP devices with order numbers and names.\n\n**--device** _number_\n> Select which attached device to mount by its order number.\n\n**-h**, **--help**\n> Print help and exit.\n\n**-V**, **--version**\n> Print version and exit.\n\n**-o** _options_\n> Comma-separated mount options. SIMPLE-MTPFS-specific options include **enable-move** (allows move operations, which can be slow). Standard FUSE options such as **allow_other**, **allow_root**, **default_permissions**, **uid=**, **gid=**, and **umask=** are also supported.\n\n**-f**\n> Run in the foreground.\n\n**-d**\n> Enable debug output (implies **-f**)."],["DESCRIPTION","**simple-mtpfs** is a FUSE-based filesystem driver that exposes files on MTP (Media Transfer Protocol) devices over USB as a normal directory tree on Linux, macOS, and other systems with FUSE support. Once mounted, you can browse, copy, rename, and delete files on phones, cameras, and other MTP-capable hardware using standard shell tools.\n\nThe driver uses **libmtp** to communicate with devices and **FUSE** to present their storage as a userspace filesystem. Temporary files downloaded from the device are stored in a configurable directory (default **/tmp**). When multiple MTP devices are connected, use **--list-devices** to see their order numbers, then pass **--device** or a **/dev/libmtp-*** special file to choose which one to mount.\n\nUnmount with **fusermount -u** on the mountpoint. The project is no longer actively maintained upstream; many distributions still package it, but alternatives such as **jmtpfs** or **gvfs-mtp** may be preferable for new setups."],["CAVEATS","Move operations are disabled by default because they can be very slow on MTP devices; enable them with **-o enable-move** if needed. Performance is slower than a native block-device mount because every file access goes through the MTP protocol. The upstream project is unmaintained and deprecated in some package managers. Requires **libfuse** (version 2.7.3 or later) and **libmtp** at build time."],["HISTORY","**simple-mtpfs** was written by **Peter Hatina** and first released around **2013** as a lightweight alternative to heavier MTP browser tools. Version **0.2** shipped with a manual page in **January 2015**. The project reached **0.4.0** but has seen little upstream activity since; Homebrew and several Linux distributions still distribute it while marking it deprecated."],["INSTALL","```zypper: sudo zypper install simple-mtpfs```\n\n```brew: brew install simple-mtpfs```\n\n```nix: nix profile install nixpkgs#simple-mtpfs```"],["SEE ALSO","[fusermount](/man/fusermount)(1), [mount](/man/mount)(8), [sshfs](/man/sshfs)(1), [mtp-detect](/man/mtp-detect)(1)"],["RESOURCES","```[Source code](https://github.com/phatina/simple-mtpfs)```"]],"e":["fusermount","mount","sshfs","mtp-detect"],"x":"simple-mtpfs --list-devices"},"snmpnetstat":{"n":"snmpnetstat","t":"Display remote network status via SNMP","s":[["TAGLINE","Display remote network status via SNMP"],["TLDR","**Show network connections**\n\n```snmpnetstat -v [2c] -c [public] [host]```\n\n**Show routing table**\n\n```snmpnetstat -v [2c] -c [public] -Cr [host]```\n\n**Show interface statistics**\n\n```snmpnetstat -v [2c] -c [public] -Ci [host]```"],["SYNOPSIS","**snmpnetstat** [_options_] _agent_"],["PARAMETERS","**-v** _version_\n> SNMP version (1, 2c, 3).\n\n**-c** _community_\n> Community string.\n\n**-Ci**\n> Interface statistics.\n\n**-Cr**\n> Routing table.\n\n**-Cs**\n> Protocol statistics."],["DESCRIPTION","**snmpnetstat** displays network status from remote hosts via SNMP. Similar to netstat but retrieves data over network using SNMP protocol. Part of Net-SNMP suite."],["INSTALL","```apt: sudo apt install snmp```\n\n```apk: sudo apk add net-snmp-tools```"],["SEE ALSO","[snmpget](/man/snmpget)(1), [netstat](/man/netstat)(1)"]],"e":["snmpget","netstat"],"x":"snmpnetstat -v [2c] -c [public] [host]"},"systemd-cgtop":{"n":"systemd-cgtop","t":"Monitor cgroup resource usage","s":[["TAGLINE","Monitor cgroup resource usage"],["TLDR","**Start** an interactive view\n\n```systemd-cgtop```\n\n**Change** the sort order\n\n```systemd-cgtop --order [cpu|memory|path|tasks|io]```\n\n**Show** CPU usage by time instead of percentage\n\n```systemd-cgtop --cpu=percentage```\n\n**Change** the update interval\n\n```systemd-cgtop -d [interval]```\n\n**Only** count userspace processes (no kernel threads)\n\n```systemd-cgtop -P```"],["SYNOPSIS","**systemd-cgtop** [_options_]"],["PARAMETERS","**--order _field_**\n> Sort by cpu, memory, path, tasks, or io\n\n**-d, --delay _interval_**\n> Update interval (supports ms, us, min suffixes)\n\n**-P**\n> Count only userspace processes\n\n**-k**\n> Include kernel threads in counts\n\n**-n, --iterations _n_**\n> Run for n iterations and exit\n\n**-b, --batch**\n> Run in batch mode (non-interactive)\n\n**-r, --raw**\n> Output raw numbers without units\n\n**--depth _n_**\n> Maximum cgroup tree depth to show"],["DESCRIPTION","**systemd-cgtop** shows the top control groups by resource usage, similar to **top** but organized by cgroups. It displays CPU, memory, and I/O statistics for each control group.\n\nThe tool is useful for monitoring resource consumption by services and understanding which systemd units are using system resources. Interactive mode supports keyboard shortcuts for changing display options."],["CAVEATS","I/O accounting requires appropriate cgroup controllers to be enabled. Some resource statistics may not be available depending on kernel configuration. Part of the systemd suite."],["INSTALL","```apt: sudo apt install systemd```\n\n```dnf: sudo dnf install systemd```\n\n```pacman: sudo pacman -S systemd```\n\n```zypper: sudo zypper install systemd```\n\n```brew: brew install systemd```\n\n```nix: nix profile install nixpkgs#systemd```"],["SEE ALSO","[systemd-cgls](/man/systemd-cgls)(1), [top](/man/top)(1), [systemctl](/man/systemctl)(1), [cgroups](/man/cgroups)(7)"]],"e":["systemd-cgls","top","systemctl","cgroups"],"x":"systemd-cgtop"},"tail":{"n":"tail","t":"Output last part of files","s":[["TAGLINE","Output last part of files"],["TLDR","**Show last 10 lines** of a file\n\n```tail [file]```\n\n**Show last N lines**\n\n```tail -n [20] [file]```\n\n**Show all lines starting from line N**\n\n```tail -n +[10] [file]```\n\n**Follow a file** (watch for new content)\n\n```tail -f [file]```\n\n**Follow multiple files**\n\n```tail -f [file1] [file2]```\n\n**Follow and retry** if file is recreated\n\n```tail -F [file]```\n\n**Show last N bytes**\n\n```tail -c [100] [file]```"],["SYNOPSIS","**tail** [_options_] [_file_...]"],["PARAMETERS","**-n** _N_, **--lines**=_N_\n> Output last N lines (or +N for starting from line N)\n\n**-c** _N_, **--bytes**=_N_\n> Output last N bytes (or +N for starting from byte N)\n\n**-f**, **--follow**\n> Output appended data as file grows\n\n**-F**\n> Same as --follow=name --retry\n\n**--retry**\n> Keep trying to open file if inaccessible\n\n**-s** _N_, **--sleep-interval**=_N_\n> Sleep N seconds between iterations with -f\n\n**--pid**=_PID_\n> With -f, terminate after process PID dies\n\n**-q**, **--quiet**\n> Never output headers with file names\n\n**-v**, **--verbose**\n> Always output headers with file names"],["DESCRIPTION","**tail** outputs the last part of files. By default, it shows the last 10 lines. It's commonly used to view log files and monitor file changes in real-time.\n\nThe **-f** (follow) option is particularly useful for monitoring log files. Tail continues reading as new lines are appended, displaying them immediately.\n\nUsing **-n +N** outputs starting from line N rather than the last N lines. This is useful for skipping headers or combining with head for extracting specific ranges.\n\nMultiple files can be specified; tail shows headers indicating which file output comes from."],["CAVEATS","**-f** follows the file descriptor. If a file is deleted and recreated (common with log rotation), use **-F** which follows by name and retries.\n\nFor large files, tail is efficient—it seeks to near the end rather than reading the entire file.\n\nThe **+** syntax for -n and -c counts from the beginning (1-indexed for lines, 0-indexed for bytes). **tail -n +1** outputs the entire file."],["HISTORY","**tail** originated in **AT&T Unix** Version 1 (1971). It is part of POSIX and GNU coreutils on Linux."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[head](/man/head)(1), [less](/man/less)(1), [cat](/man/cat)(1), [multitail](/man/multitail)(1)"]],"e":["head","less","cat","multitail"],"x":"tail [file]"},"tailwindcss":{"n":"tailwindcss","t":"Utility-first CSS framework CLI","s":[["TAGLINE","Utility-first CSS framework CLI"],["TLDR","**Build CSS from input file**\n\n```tailwindcss -i [src/input.css] -o [dist/output.css]```\n\n**Build and watch for changes**\n\n```tailwindcss -i [src/input.css] -o [dist/output.css] --watch```\n\n**Build with minification**\n\n```tailwindcss -i [src/input.css] -o [dist/output.css] --minify```\n\n**Initialize a configuration file**\n\n```tailwindcss init```\n\n**Initialize with full configuration**\n\n```tailwindcss init --full```\n\n**Build using npx**\n\n```npx tailwindcss -i [src/input.css] -o [dist/output.css]```"],["SYNOPSIS","**tailwindcss** [_-i input_] [_-o output_] [_--watch_] [_--minify_] [_--config path_]"],["PARAMETERS","**-i** _file_, **--input** _file_\n> Input CSS file to process\n\n**-o** _file_, **--output** _file_\n> Output file for compiled CSS\n\n**-w**, **--watch**\n> Watch for changes and rebuild automatically\n\n**-m**, **--minify**\n> Minify the output CSS\n\n**-c** _file_, **--config** _file_\n> Path to tailwind.config.js file\n\n**--content** _paths_\n> Content files to scan for class usage\n\n**-h**, **--help**\n> Display help information"],["SUBCOMMANDS","**init**\n> Create a tailwind.config.js file\n\n**init --full**\n> Create configuration with all default values"],["DESCRIPTION","**Tailwind CSS** is a utility-first CSS framework that generates optimized CSS by scanning source files for class usage. The CLI compiles Tailwind's utility classes into standard CSS.\n\nThe build process scans template files (HTML, JSX, Vue, etc.) for Tailwind classes, generates only the CSS rules actually used, and outputs a production-ready stylesheet. The **--watch** flag enables live rebuilding during development.\n\nInput CSS files should contain Tailwind directives like @tailwind base, @tailwind components, and @tailwind utilities. Configuration is customized via tailwind.config.js."],["CAVEATS","The CLI requires Node.js. For production builds, always use **--minify** to reduce file size. Ensure content paths in configuration correctly point to all template files to avoid missing styles."],["HISTORY","Tailwind CSS was created by **Adam Wathan** and first released in **November 2017**. Version 4 (2024) introduced significant performance improvements with the @tailwindcss/cli package. The framework popularized the utility-first approach to CSS."],["INSTALL","```brew: brew install tailwindcss```\n\n```nix: nix profile install nixpkgs#tailwindcss```"],["SEE ALSO","[postcss](/man/postcss)(1), [sass](/man/sass)(1), [npm](/man/npm)(1)"]],"e":["postcss","sass","npm"],"x":"tailwindcss -i [src/input.css] -o [dist/output.css]"},"tetrinet":{"n":"tetrinet","t":"Multiplayer terminal Tetris game","s":[["TAGLINE","Multiplayer terminal Tetris game"],["TLDR","**Connect to a server**\n\n```tetrinet [nickname] [server.example.com]```\n\n**Connect with fancy graphics**\n\n```tetrinet -fancy [nickname] [server]```\n\n**Connect in tetrifast mode**\n\n```tetrinet -fast [nickname] [server]```\n\n**Use custom window size**\n\n```tetrinet -log [logfile] [nickname] [server]```\n\n**Enable shadow piece preview**\n\n```tetrinet -shadow [nickname] [server]```"],["SYNOPSIS","**tetrinet** [_options_] _nickname_ _server_"],["PARAMETERS","**-fancy**\n> Use fancy TTY graphics with box-drawing characters.\n\n**-fast**\n> Connect using tetrifast mode (faster gameplay).\n\n**-log** _file_\n> Log partyline messages to file.\n\n**-shadow**\n> Enable shadow piece showing where piece will land.\n\n**-slide**\n> Enable piece sliding after landing.\n\n**-noshadow**\n> Disable shadow piece.\n\n**-noslide**\n> Disable piece sliding.\n\n**-windows**\n> Emulate Windows client behavior."],["CONTROLS","**Left/Right arrows**: Move piece horizontally.\n\n**Up arrow**: Rotate piece clockwise.\n\n**Down arrow**: Soft drop.\n\n**Space**: Hard drop.\n\n**d**: Discard special block.\n\n**1-6**: Use special on player field.\n\n**t**: Talk in partyline."],["DESCRIPTION","**tetrinet** is a multiplayer Tetris game for up to six players that runs in a text terminal. It connects to TetriNET servers and supports both cooperative team play and competitive matches.\n\nThe game features special blocks that appear when clearing lines containing them. Specials include add lines, clear lines, nuke field, block bomb, gravity, and various other attacks that can be used against opponents or to help teammates.\n\nThe terminal client uses ncurses for display and requires a terminal with at least 50 lines. It's compatible with original Windows TetriNET servers and supports the faster tetrifast variant."],["CAVEATS","Requires a terminal with at least 50 lines. Server availability varies; public servers may have limited uptime. The -fancy option requires Unicode support in the terminal. Network latency can affect competitive play."],["HISTORY","**TetriNET** was created by St0rmCat in 1997 as a multiplayer Tetris variant for Windows. The Linux client was developed to provide compatible access from Unix systems. The game became popular in the late 1990s and early 2000s for its unique special block mechanics that added strategic depth to multiplayer Tetris."],["INSTALL","```pacman: sudo pacman -S tetrinet```"],["SEE ALSO","[tint](/man/tint)(1), [bastet](/man/bastet)(1), [vitetris](/man/vitetris)(1), [netris](/man/netris)(1)"]],"e":["tint","bastet","vitetris","netris"],"x":"tetrinet [nickname] [server.example.com]"},"transmission-show":{"n":"transmission-show","t":"Display torrent file metadata","s":[["TAGLINE","Display torrent file metadata"],["TLDR","**Show torrent information**\n\n```transmission-show [file.torrent]```\n\n**Display magnet URI**\n\n```transmission-show -m [file.torrent]```\n\n**Show file list**\n\n```transmission-show -f [file.torrent]```\n\n**Show info hash only**\n\n```transmission-show -i [file.torrent]```\n\n**Show total size**\n\n```transmission-show -s [file.torrent]```\n\n**Query tracker for peer count**\n\n```transmission-show --scrape [file.torrent]```"],["SYNOPSIS","**transmission-show** [_options_] _torrent-file_"],["PARAMETERS","**-m**, **--magnet**\n> Print the magnet URI for the torrent.\n\n**-i**, **--info**\n> Print only the info hash.\n\n**-s**, **--size**\n> Print total size of all files.\n\n**-f**, **--files**\n> Print detailed list of files with paths and sizes.\n\n**-c**, **--comment**\n> Print the comment field from metadata.\n\n**-t**, **--tracker**\n> Print list of tracker URLs.\n\n**--scrape**\n> Query trackers for peer and seeder counts.\n\n**-h**, **--help**\n> Display help and exit.\n\n**-V**, **--version**\n> Display version and exit."],["DESCRIPTION","**transmission-show** displays metadata from .torrent files without downloading or seeding. It parses torrent files and presents information including file lists, trackers, piece counts, creation date, and info hashes.\n\nThe tool is useful for inspecting torrents before adding them to a client, verifying torrent contents, extracting magnet links, or scripting torrent management tasks. The scrape option queries trackers for current swarm statistics.\n\nOutput is formatted for both human readability and script parsing depending on the options used."],["CAVEATS","Scrape queries require network access and functional trackers. Some trackers may not support or respond to scrape requests. Private trackers may require authentication for scrape data."],["HISTORY","**transmission-show** is part of the Transmission BitTorrent client suite, providing command-line inspection of torrent files. Transmission was created in 2005 and the CLI tools enable automation and headless server operation."],["INSTALL","```apt: sudo apt install transmission-cli```\n\n```dnf: sudo dnf install transmission-cli```\n\n```pacman: sudo pacman -S transmission-cli```\n\n```apk: sudo apk add transmission-extra```\n\n```brew: brew install transmission-cli```"],["SEE ALSO","[transmission-create](/man/transmission-create)(1), [transmission-daemon](/man/transmission-daemon)(1), [transmission-remote](/man/transmission-remote)(1)"]],"e":["transmission-create","transmission-daemon","transmission-remote"],"x":"transmission-show [file.torrent]"},"trufflehog":{"n":"trufflehog","t":"Secret and credential scanner for code","s":[["TAGLINE","Secret and credential scanner for code"],["TLDR","**Scan git repository**\n\n```trufflehog git [https://github.com/user/repo]```\n\n**Scan local git repository**\n\n```trufflehog git file://[/path/to/repo]```\n\n**Scan filesystem**\n\n```trufflehog filesystem [/path/to/scan]```\n\n**Scan GitHub organization**\n\n```trufflehog github --org [organization]```\n\n**Scan S3 bucket**\n\n```trufflehog s3 --bucket [bucket-name]```\n\n**Only verified secrets**\n\n```trufflehog git --only-verified [https://github.com/user/repo]```\n\n**Output as JSON**\n\n```trufflehog git --json [https://github.com/user/repo]```\n\n**Scan specific branch**\n\n```trufflehog git --branch [main] [https://github.com/user/repo]```"],["SYNOPSIS","**trufflehog** _source_ [_--only-verified_] [_--json_] [_options_] _target_"],["PARAMETERS","**git** _URL_\n> Scan git repository.\n\n**github**\n> Scan GitHub (org, user, or repo).\n\n**gitlab**\n> Scan GitLab.\n\n**filesystem** _PATH_\n> Scan local filesystem.\n\n**s3**\n> Scan S3 bucket.\n\n**gcs**\n> Scan Google Cloud Storage.\n\n**docker**\n> Scan Docker image.\n\n**--only-verified**\n> Only report verified credentials.\n\n**--json**\n> Output as JSON.\n\n**--no-update**\n> Don't check for updates.\n\n**--concurrency** _NUM_\n> Scanner concurrency.\n\n**--include-paths** _FILE_\n> Only scan matching paths.\n\n**--exclude-paths** _FILE_\n> Skip matching paths.\n\n**--include-detectors** _LIST_\n> Only use specified detectors.\n\n**--exclude-detectors** _LIST_\n> Skip specified detectors.\n\n**--branch** _NAME_\n> Scan specific branch.\n\n**--since-commit** _HASH_\n> Start from commit.\n\n**--max-depth** _NUM_\n> Maximum commit history depth.\n\n**--no-verification**\n> Skip credential verification against services."],["DESCRIPTION","**trufflehog** finds secrets and credentials in code repositories, filesystems, and cloud storage. It uses hundreds of detectors for API keys, passwords, and tokens.\n\nThe tool scans git history, catching secrets that were committed and later deleted. Each commit is checked against detector patterns. High entropy strings are flagged as potential secrets.\n\nVerification mode (--only-verified) actively tests found credentials against their services. This confirms whether secrets are still valid, prioritizing real exposures over false positives.\n\nDetectors cover major services: AWS, GCP, Azure, GitHub, Slack, Stripe, Twilio, and many more. Custom regex patterns can extend detection.\n\nMultiple source types enable comprehensive scanning: repositories, organizations, cloud storage, Docker images, and local filesystems.\n\nJSON output integrates with security tools and CI/CD pipelines. Exit codes indicate whether secrets were found, enabling automated gates."],["CAVEATS","Verification makes network requests to third-party services. Scanning history of large repos is slow. False positives occur with high-entropy strings. Encrypted or obfuscated secrets may be missed. Verification may trigger security alerts."],["HISTORY","**trufflehog** was created by **Dylan Ayrey** at **Truffle Security** around **2016**. Originally a Python tool, version 3 was rewritten in Go for performance. The project addresses the serious problem of secrets committed to version control, providing automated detection before breaches occur."],["INSTALL","```zypper: sudo zypper install trufflehog```\n\n```brew: brew install trufflehog```\n\n```nix: nix profile install nixpkgs#trufflehog```"],["SEE ALSO","[gitleaks](/man/gitleaks)(1), [git-secrets](/man/git-secrets)(1), [detect-secrets](/man/detect-secrets)(1)"]],"e":["gitleaks","git-secrets","detect-secrets"],"x":"trufflehog git [https://github.com/user/repo]"},"twurl-authorize":{"n":"twurl-authorize","t":"Authorize twurl with Twitter/X API credentials","s":[["TAGLINE","Authorize twurl with Twitter/X API credentials"],["TLDR","**Authorize** with consumer key and secret (interactive PIN flow)\n\n```twurl authorize --consumer-key [key] --consumer-secret [secret]```\n\n**Authorize** using short option names\n\n```twurl authorize -c [key] -s [secret]```\n\n**Generate a bearer token** (application-only authentication)\n\n```twurl authorize --bearer --consumer-key [key] --consumer-secret [secret]```\n\n**Authorize against a non-default host**\n\n```twurl authorize -c [key] -s [secret] -H [api.twitter.com]```\n\n**Authorize for a specific username**\n\n```twurl authorize -c [key] -s [secret] -u [username]```"],["SYNOPSIS","**twurl authorize** [**-c**|**--consumer-key** _key_] [**-s**|**--consumer-secret** _secret_] [_options_]"],["PARAMETERS","**-c**, **--consumer-key** _key_\n> OAuth consumer (API) key. Required. If the value is omitted, twurl prompts interactively.\n\n**-s**, **--consumer-secret** _secret_\n> OAuth consumer (API) secret. Required. If the value is omitted, twurl prompts interactively.\n\n**-u**, **--username** _username_\n> Username of the account being authorized.\n\n**--bearer**\n> Use application-only authentication and store a bearer token instead of a user access token.\n\n**-H**, **--host** _host_\n> API host used during authorization (default: **api.twitter.com**). Protocol prefix is stripped if present.\n\n**-a**, **--access-token** _token_\n> Supply an existing access token instead of running the full PIN handshake.\n\n**-S**, **--token-secret** _secret_\n> Supply an existing token secret to pair with **--access-token**.\n\n**-P**, **--proxy** _proxy_\n> HTTP proxy to use for the authorization requests.\n\n**-t**, **--trace**\n> Trace request and response traffic during the handshake."],["DESCRIPTION","**twurl authorize** is the first step when using **twurl**, Twitter's OAuth-enabled curl replacement for the X (Twitter) API. It registers a consumer key and secret with a user account (or generates an application-only bearer token), then saves the resulting credentials so later **twurl** requests are signed automatically.\n\nFor user-context OAuth, the tool prints an authorization URL. Open it in a browser, sign in to X, and enter the returned PIN back into the terminal. On success, credentials are written to **~/.twurlrc** and that profile becomes the default if none existed yet.\n\nWith **--bearer**, **twurl authorize** obtains and stores a bearer token for application-only calls. Subsequent requests can pass **--bearer** (and optionally **-c**) to use that token without a user context.\n\nMultiple accounts and consumer keys can be authorized over time. Use **twurl accounts** to list them and **twurl set default** to switch the active profile."],["CAVEATS","Requires a developer account and app credentials from the X Developer Portal. The authorize flow needs interactive PIN entry (or pre-supplied tokens). X API product tiers and endpoint access change over time; successful authorization does not guarantee every endpoint is available to your app. Ruby and the **twurl** gem must be installed."],["HISTORY","**twurl** was created by Twitter (now X) as a development helper for its HTTP API. The **authorize** subcommand has always been the entry point for storing OAuth credentials in **~/.twurlrc**."],["INSTALL","```nix: nix profile install nixpkgs#twurl```"],["SEE ALSO","[twurl](/man/twurl)(1), [twurl-accounts](/man/twurl-accounts)(1), [curl](/man/curl)(1), [httpie](/man/httpie)(1)"],["RESOURCES","```[Source code](https://github.com/twitter/twurl)```\n\n```[Documentation](https://github.com/twitter/twurl#getting-started)```"]],"e":["twurl","twurl-accounts","curl","httpie"],"x":"twurl authorize --consumer-key [key] --consumer-secret [secret]"},"v4l2-compliance":{"n":"v4l2-compliance","t":"Test Video4Linux2 driver API compliance","s":[["TAGLINE","Test Video4Linux2 driver API compliance"],["TLDR","**Test default video device**\n\n```v4l2-compliance```\n\n**Test specific device**\n\n```v4l2-compliance -d [/dev/video0]```\n\n**Run streaming tests**\n\n```v4l2-compliance -s```\n\n**Test all video formats**\n\n```v4l2-compliance -f```\n\n**Test color formats**\n\n```v4l2-compliance -c```\n\n**Verbose output**\n\n```v4l2-compliance -v```\n\n**Run all tests**\n\n```v4l2-compliance -s -f -c```"],["SYNOPSIS","**v4l2-compliance** [_options_]"],["PARAMETERS","**-d**, **--device** _device_\n> Video device to test (default: /dev/video0).\n\n**-s**, **--streaming**\n> Run basic streaming tests.\n\n**-f**, **--stream-all-formats**\n> Test all supported video formats.\n\n**-c**, **--stream-all-colors**\n> Test all color formats.\n\n**-v**, **--verbose**\n> Enable verbose output.\n\n**-w**, **--wrapper**\n> Use libv4l2 wrapper.\n\n**-T**, **--trace**\n> Trace ioctl calls.\n\n**-n**, **--no-warnings**\n> Suppress warnings.\n\n**--help**\n> Display help."],["DESCRIPTION","**v4l2-compliance** tests Video4Linux2 drivers and devices for API compliance. It verifies nearly all V4L2 ioctls and checks that drivers correctly implement the specification.\n\nThe tool tests video capture and output, VBI, radio, and software radio devices. It verifies required capabilities are present and optional features work correctly when advertised. Streaming tests validate buffer handling and format support.\n\nNew V4L2 drivers must pass compliance tests before kernel inclusion. The tool saves device state before testing and restores it afterward, including on Ctrl-C interruption."],["CAVEATS","Always use latest version from git for driver development. Some test failures may be tool bugs rather than driver issues. Streaming tests require valid input/output signal. Tests may take significant time with all options enabled."],["HISTORY","**v4l2-compliance** was developed as part of the v4l-utils package to ensure V4L2 driver quality. It evolved alongside the V4L2 API and now covers approximately 90% of the specification. It's become mandatory for new driver submissions to the Linux kernel."],["INSTALL","```apt: sudo apt install v4l-utils```\n\n```dnf: sudo dnf install v4l-utils```\n\n```pacman: sudo pacman -S v4l-utils```\n\n```apk: sudo apk add v4l-utils```\n\n```zypper: sudo zypper install v4l-utils```\n\n```nix: nix profile install nixpkgs#v4l-utils```"],["SEE ALSO","[v4l2-ctl](/man/v4l2-ctl)(1)"]],"e":["v4l2-ctl"],"x":"v4l2-compliance"},"watchman":{"n":"watchman","t":"File watching service for large codebases","s":[["TAGLINE","File watching service for large codebases"],["TLDR","**Watch a directory**\n\n```watchman watch [/path/to/dir]```\n\n**List watched directories**\n\n```watchman watch-list```\n\n**Stop watching a directory**\n\n```watchman watch-del [/path/to/dir]```\n\n**Get watch status**\n\n```watchman watch-project [/path/to/project]```\n\n**Find files matching pattern**\n\n```watchman find [/path/to/dir] -p [\"*.js\"]```\n\n**Trigger command on file changes**\n\n```watchman -- trigger [/path/to/dir] [triggername] [\"*.js\"] -- [command]```\n\n**Shutdown watchman**\n\n```watchman shutdown-server```\n\n**Check version**\n\n```watchman version```"],["SYNOPSIS","**watchman** _command_ [_options_] [_arguments_]"],["PARAMETERS","**watch** _PATH_\n> Start watching directory.\n\n**watch-del** _PATH_\n> Stop watching directory.\n\n**watch-list**\n> List all watches.\n\n**watch-project** _PATH_\n> Watch project root.\n\n**find** _ROOT_ [_ARGS_]\n> Find files matching criteria.\n\n**query** _ROOT_ _EXPR_\n> Query files with expression.\n\n**since** _ROOT_ _CLOCK_\n> Find files changed since clock.\n\n**trigger** _ROOT_ _NAME_ _PATTERN_ **--** _CMD_\n> Set up triggered command.\n\n**trigger-del** _ROOT_ _NAME_\n> Remove trigger.\n\n**trigger-list** _ROOT_\n> List triggers.\n\n**log-level** _LEVEL_\n> Set logging level.\n\n**get-config** _ROOT_\n> Get watch configuration.\n\n**shutdown-server**\n> Stop watchman daemon.\n\n**version**\n> Show version and capabilities."],["DESCRIPTION","**watchman** is a file watching service that monitors directories and triggers actions when files change. It was designed for large codebases requiring efficient file change detection.\n\nThe daemon maintains inotify/FSEvents watches and records file changes. Clients query for changes efficiently - even with millions of files, queries return in milliseconds.\n\nTriggers execute commands when matching files change. This enables automated testing, building, or syncing. Triggers can filter by file pattern and use templated commands.\n\nThe query system uses a JSON expression language for complex file matching. Queries can filter by name, type, size, modification time, and more. Results include file metadata.\n\nClock values track change history. Subscribing to changes since a clock enables incremental processing - only handling new changes rather than full scans.\n\nWatchman integrates with build tools (Buck, Bazel), IDEs, and testing frameworks. It provides the file watching layer that other tools build upon."],["CAVEATS","Daemon must be running for watches. System limits on watched files may need increasing. Not all filesystems support efficient watching. Configuration persists across restarts. Memory usage grows with file count."],["HISTORY","**watchman** was developed at **Facebook** (Meta) and released in **2012**. It was designed to handle Facebook's massive codebase where inotify alone wasn't sufficient. The project became a foundation for various developer tools needing efficient file change detection."],["INSTALL","```brew: brew install watchman```\n\n```nix: nix profile install nixpkgs#watchman```"],["SEE ALSO","[inotifywait](/man/inotifywait)(1), [entr](/man/entr)(1), [fswatch](/man/fswatch)(1)"]],"e":["inotifywait","entr","fswatch"],"x":"watchman watch [/path/to/dir]"},"xfs":{"n":"xfs","t":"X Window System font server","s":[["TAGLINE","X Window System font server"],["TLDR","**Start font server**\n\n```xfs```\n\n**Start with specific config**\n\n```xfs -config [/etc/X11/fs/config]```\n\n**Start on specific port**\n\n```xfs -port [7100]```\n\n**Run as daemon**\n\n```xfs -daemon```\n\n**Start with droppriv user**\n\n```xfs -user [nobody]```"],["SYNOPSIS","**xfs** [_options_]"],["PARAMETERS","**-config** _file_\n> Configuration file (default: /etc/X11/fs/config).\n\n**-port** _num_\n> TCP port to listen on (default: 7100).\n\n**-daemon**\n> Fork to background.\n\n**-droppriv**\n> Drop root privileges after startup.\n\n**-user** _user_\n> Run as specified user.\n\n**-ls** _num_\n> Listen socket queue length."],["DESCRIPTION","**xfs** is the X Window System font server. It provides fonts to X servers over the network, allowing centralized font management for multiple X displays.\n\nX servers can be configured to request fonts from xfs instead of (or in addition to) local font directories. This was useful for thin clients and diskless workstations sharing fonts from a server.\n\nConfiguration defines font paths, client limits, and network settings. The server listens on TCP port 7100 by default."],["CONFIGURATION","Config file: /etc/X11/fs/config\n\nDefines catalogue (font paths), client-limit, and clone-self settings."],["CAVEATS","Deprecated in favor of client-side font rendering (fontconfig/FreeType). Most modern systems don't use xfs. Network font access has security implications. Legacy application only."],["HISTORY","**xfs** was developed for the X Window System to centralize font management. With the transition to client-side rendering using fontconfig and FreeType, xfs became largely obsolete. It remains available for legacy X applications requiring server-side fonts."],["INSTALL","```nix: nix profile install nixpkgs#xfs```"],["SEE ALSO","[fc-cache](/man/fc-cache)(1), [xlsfonts](/man/xlsfonts)(1), [xset](/man/xset)(1)"]],"e":["fc-cache","xlsfonts","xset"],"x":"xfs"},"zapier-login":{"n":"zapier-login","t":"Authenticate with Zapier platform","s":[["TAGLINE","Authenticate with Zapier platform"],["TLDR","**Login to Zapier** interactively\n\n```zapier login```\n\n**Login with SSO** (for Google, Facebook, or Microsoft accounts)\n\n```zapier login --sso```"],["SYNOPSIS","**zapier** **login** [_options_]"],["PARAMETERS","**--sso**\n> Use SSO authentication. Required if you log into Zapier via single sign-on (Google, Facebook, or Microsoft) and do not have a Zapier password. Prompts you to enter a deploy key generated from your Zapier developer account."],["DESCRIPTION","**zapier login** authenticates with the Zapier platform and stores credentials in **~/.zapierrc**. Opens a browser for authentication. Required before using other Zapier CLI commands.\n\nAs an alternative to interactive login, you can set the **ZAPIER_DEPLOY_KEY** environment variable to run privileged commands without user input.\n\nNote: The CLI binary has been renamed from **zapier** to **zapier-platform** as of v18.0.0. The old **zapier** command still works but is deprecated."],["SEE ALSO","[zapier](/man/zapier)(1), [zapier-test](/man/zapier-test)(1), [zapier-push](/man/zapier-push)(1), [zapier-validate](/man/zapier-validate)(1)"]],"e":["zapier","zapier-test","zapier-push","zapier-validate"],"x":"zapier login"}});
