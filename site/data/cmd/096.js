window.LCL_BUCKET(96,{"abigen":{"n":"abigen","t":"Ethereum smart contract Go binding generator","s":[["TAGLINE","Ethereum smart contract Go binding generator"],["TLDR","**Generate** Go bindings from a Solidity ABI\n\n```abigen --abi [contract.abi] --pkg [packagename] --out [contract.go]```\n\nGenerate bindings from a **compiled contract**\n\n```abigen --bin [contract.bin] --abi [contract.abi] --pkg [packagename] --out [contract.go]```\n\nGenerate from **Solidity source** directly\n\n```abigen --sol [contract.sol] --pkg [packagename] --out [contract.go]```\n\nSpecify a custom **struct type** name\n\n```abigen --abi [contract.abi] --pkg [packagename] --type [MyContract] --out [contract.go]```"],["SYNOPSIS","**abigen** [_--abi file_] [_--bin file_] [_--sol file_] [_--pkg name_] [_--out file_] [_options_]"],["DESCRIPTION","**abigen** is an Ethereum tool that generates Go language bindings for interacting with smart contracts. It takes a contract's ABI (Application Binary Interface) and optionally its bytecode to produce type-safe Go code for deploying and calling contract methods.\n\nThe generated code handles ABI encoding/decoding, transaction creation, and event filtering, making it straightforward to interact with Ethereum contracts from Go applications. This eliminates the need for manual ABI manipulation and provides compile-time type checking."],["PARAMETERS","**--abi** _file_\n> Path to the contract ABI JSON file\n\n**--bin** _file_\n> Path to the contract bytecode (required for deployment code generation)\n\n**--sol** _file_\n> Path to Solidity source file (requires solc compiler)\n\n**--pkg** _name_\n> Package name for the generated Go code\n\n**--out** _file_\n> Output file path for generated code\n\n**--type** _name_\n> Struct name for the contract binding (defaults to contract name)\n\n**--lang** _language_\n> Target language for generated bindings (default: go).\n\n**--solc** _path_\n> Solidity compiler path when using --sol.\n\n**--combined-json** _file_\n> Path to the combined-json output from solc compiler.\n\n**--alias** _aliases_\n> Comma-separated list of identifier aliases (original=replacement)."],["CAVEATS","When using **--sol**, the Solidity compiler (solc) must be installed and accessible. ABI changes require regenerating bindings. Complex Solidity types may produce verbose Go code. Ensure ABI and bytecode match when deploying contracts."],["HISTORY","**abigen** was developed by the Ethereum Foundation as part of **go-ethereum** (geth), first appearing around **2015**. It was created to provide idiomatic Go bindings for smart contracts, supporting the growing ecosystem of Go-based Ethereum tools and applications."],["INSTALL","```apk: sudo apk add geth-tools```"],["SEE ALSO","[solc](/man/solc)(1), [geth](/man/geth)(1), [cast](/man/cast)(1)"]],"e":["solc","geth","cast"],"x":"abigen --abi [contract.abi] --pkg [packagename] --out [contract.go]"},"apt-get":{"n":"apt-get","t":"Install, upgrade, and manage packages.","s":[["TAGLINE","Install, upgrade, and manage packages."],["TLDR","**Update** the list of available packages\n\n```sudo apt-get update```\n\n**Install** a package or update it to the latest version\n\n```sudo apt-get install [package]```\n\n**Remove** a package\n\n```sudo apt-get remove [package]```\n\n**Remove** a package and its configuration files\n\n```sudo apt-get purge [package]```\n\n**Upgrade** all installed packages\n\n```sudo apt-get upgrade```\n\n**Clean** the local repository of interrupted downloads\n\n```sudo apt-get autoclean```\n\n**Remove** all packages that are no longer needed\n\n```sudo apt-get autoremove```\n\n**Upgrade** and handle changing dependencies\n\n```sudo apt-get dist-upgrade```"],["SYNOPSIS","**apt-get** [_options_] _command_ [_package_...]"],["DESCRIPTION","**apt-get** is the command-line package management utility for Debian and Ubuntu. It handles installation, upgrade, and removal of software packages. For interactive use, **apt** is recommended as a more user-friendly alternative."],["PARAMETERS","**update**\n> Resynchronize package index files from sources\n\n**upgrade**\n> Install newest versions of all currently installed packages\n\n**dist-upgrade**\n> Upgrade and handle changing dependencies\n\n**install package**\n> Install or upgrade specified packages\n\n**remove package**\n> Remove packages\n\n**purge package**\n> Remove packages and configuration files\n\n**autoremove**\n> Remove automatically installed packages no longer needed\n\n**autoclean**\n> Clear local repository of retrieved package files that can no longer be downloaded\n\n**clean**\n> Clear local repository of all retrieved package files\n\n**download package**\n> Download the binary package into the current directory\n\n**source package**\n> Fetch source packages\n\n**build-dep package**\n> Install the build dependencies of a source package\n\n**satisfy string**\n> Satisfy dependency strings (as used in Build-Depends); prefix an argument with 'Conflicts: ' to express a conflict\n\n**-y, --yes**\n> Automatic yes to prompts; run non-interactively\n\n**-s, --simulate**\n> No action; simulate the events that would occur without changing the system\n\n**-d, --download-only**\n> Download packages but don't unpack or install\n\n**--no-install-recommends**\n> Don't install recommended packages\n\n**--reinstall**\n> Reinstall packages already at the newest version\n\n**--only-upgrade**\n> Only upgrade already-installed packages; ignore requests to install new ones\n\n**-f, --fix-broken**\n> Attempt to fix broken dependencies"],["CONFIGURATION","**/etc/apt/sources.list**\n> Package source repositories.\n\n**/etc/apt/apt.conf**\n> Main APT configuration file.\n\n**/etc/apt/apt.conf.d/**\n> Directory for drop-in configuration fragments."],["CAVEATS","Run **apt-get update** before other commands to ensure the package list is current."],["HISTORY","Part of the **APT** (Advanced Package Tool) suite developed for Debian in **1998** by the APT Development Team."],["INSTALL","```apt: sudo apt install apt```\n\n```dnf: sudo dnf install apt```\n\n```pacman: sudo pacman -S apt```\n\n```apk: sudo apk add apt```\n\n```brew: brew install apt```\n\n```nix: nix profile install nixpkgs#apt```"],["SEE ALSO","[apt](/man/apt)(8), [apt-cache](/man/apt-cache)(8), [dpkg](/man/dpkg)(1), [aptitude](/man/aptitude)(8)"],["RESOURCES","```[Source code](https://salsa.debian.org/apt-team/apt)```\n\n```[Documentation](https://manpages.debian.org/apt-get)```"]],"e":["apt","apt-cache","dpkg","aptitude"],"x":"sudo apt-get update"},"atoum":{"n":"atoum","t":"Modern PHP unit testing framework","s":[["TAGLINE","Modern PHP unit testing framework"],["TLDR","**Run** tests\n\n```atoum -f [tests/units/MyTest.php]```\n\nRun **all tests** in directory\n\n```atoum -d [tests/units]```\n\nRun with **code coverage**\n\n```atoum -d [tests/] -c```\n\nGenerate **HTML** coverage report\n\n```atoum -d [tests/] --coverage-html [coverage/]```\n\nRun tests in **debug** mode\n\n```atoum -d [tests/] --debug```"],["SYNOPSIS","**atoum** [_-f file_] [_-d directory_] [_-c_] [_options_]"],["DESCRIPTION","**atoum** is a modern PHP unit testing framework emphasizing simplicity and clarity. It provides an intuitive API for writing tests with minimal boilerplate, featuring mocking, code coverage, and parallel execution.\n\nThe framework uses a natural, fluent assertion syntax and includes tools for test isolation and dependency injection."],["PARAMETERS","**-f** _file_\n> Run specific test file\n\n**-d** _directory_\n> Run all tests in directory\n\n**-c**, **--coverage**\n> Generate code coverage\n\n**--coverage-html** _dir_\n> HTML coverage report\n\n**--coverage-xml** _file_\n> XML coverage report\n\n**--debug**\n> Debug mode\n\n**-l**, **--loop**\n> Run tests in continuous mode\n\n**-p** _n_, **--max-children-number** _n_\n> Parallel test execution\n\n**--fail-if-void-methods**\n> Fail if test methods are empty"],["CONFIGURATION","**.atoum.php**\n> Project-level configuration file for test runners, reports, and coverage settings."],["CAVEATS","Requires PHP 5.6+. Less widely adopted than PHPUnit. Some IDE integrations may be limited. Documentation primarily in French."],["HISTORY","**atoum** was created by Frédéric Hardy and first released in **2011** as a modern alternative to PHPUnit, emphasizing developer experience and test clarity."],["SEE ALSO","[phpunit](/man/phpunit)(1), [phpspec](/man/phpspec)(1), [behat](/man/behat)(1)"],["RESOURCES","```[Homepage](https://atoum.org/)```\n\n```[Source code](https://github.com/atoum/atoum)```\n\n```[Documentation](https://docs.atoum.org/)```"]],"e":["phpunit","phpspec","behat"],"x":"atoum -f [tests/units/MyTest.php]"},"betterlockscreen":{"n":"betterlockscreen","t":"Customizable lock screen using i3lock","s":[["TAGLINE","Customizable lock screen using i3lock"],["TLDR","**Lock** screen\n\n```betterlockscreen -l```\n\nSet **background** image\n\n```betterlockscreen -u path/to/image.png```\n\nLock with **custom** text\n\n```betterlockscreen -l pixel --text \"Custom text\"```\n\nLock with **monitor** timeout\n\n```betterlockscreen --off 5 -l```"],["SYNOPSIS","**betterlockscreen** [_OPTIONS_]"],["DESCRIPTION","**betterlockscreen** is a lock screen wrapper for i3lock-color that provides visually appealing lock screens with customizable background effects. It supports dim, blur, dimblur, and pixel effects applied to a wallpaper image.\n\nBefore locking, the background image must be cached using the **-u** flag, which generates processed versions with each effect. This makes subsequent locks instant since the effects are pre-computed. The tool can also set the desktop wallpaper using the same cached images."],["PARAMETERS","**-l**, **--lock** [_effect_]\n> Lock the screen (effects: dim, blur, dimblur, pixel)\n\n**-u**, **--update** _image_\n> Cache the lock screen background image, pre-generating all effect variants\n\n**-t**, **--text** _text_\n> Display custom text on the lock screen\n\n**--off** _seconds_\n> Turn off monitor after specified seconds when locked\n\n**-w**, **--wall** [_effect_]\n> Set desktop wallpaper using the cached image\n\n**--display** _n_\n> Set the display number to draw the login box on (for multi-monitor setups)\n\n**--span**\n> Scale the image to span across multiple displays"],["CONFIGURATION","**~/.config/betterlockscreenrc**\n> Configuration file for default effects, colors, font settings, and lock screen appearance."],["CAVEATS","Requires i3lock-color and imagemagick. Background images are cached for faster locking. First-time setup requires running -u to generate cached images."],["HISTORY","**betterlockscreen** was originally created by **Pavan Jadhaw** and is now maintained at the betterlockscreen GitHub organization. It is a Bash wrapper script that adds blur, dim, and pixel effects on top of i3lock-color."],["INSTALL","```aur: yay -S betterlockscreen```\n\n```nix: nix profile install nixpkgs#betterlockscreen```"],["SEE ALSO","[i3lock](/man/i3lock)(1), [xscreensaver](/man/xscreensaver)(1)"]],"e":["i3lock","xscreensaver"],"x":"betterlockscreen -l"},"blahaj":{"n":"blahaj","t":"Colorize text and print sharks with pride flag colors","s":[["TAGLINE","Colorize text and print sharks with pride flag colors"],["TLDR","**Display a colorful shark**\n\n```blahaj --shark```\n\n**Display a pride flag**\n\n```blahaj --flag```\n\n**Colorize text input** with trans colors\n\n```echo \"[text]\" | blahaj```\n\n**Colorize with a specific flag**\n\n```echo \"[text]\" | blahaj --colors [rainbow]```\n\n**Color individual words**\n\n```echo \"[text]\" | blahaj --words```\n\n**Color the background** instead of text\n\n```echo \"[text]\" | blahaj --background```\n\n**List all available flags**\n\n```blahaj --flags```\n\n**Use a random color scheme**\n\n```echo \"[text]\" | blahaj --random```"],["SYNOPSIS","**blahaj** [_options_] [_text_]"],["DESCRIPTION","**blahaj** is a lolcat-like CLI tool that colorizes text input using pride flag colors and displays ASCII art sharks. Named after the popular IKEA shark plushie, it brings colorful output to your terminal.\n\nThe tool can colorize text by character, word, or line using various pride flag color schemes, and can also display the beloved BLAHAJ shark in ASCII art."],["PARAMETERS","**-s, --shark**\n> Display the BLAHAJ shark ASCII art\n\n**-f, --flag**\n> Display a pride flag\n\n**-c, --colors** _flag_\n> Color scheme to use (default: trans)\n\n**-r, --random**\n> Use a random color scheme\n\n**-i, --individual**\n> Color individual characters\n\n**-w, --words**\n> Color individual words\n\n**-b, --background**\n> Color the background instead of text\n\n**-m, --multiplier** _n_\n> Multiplier for flag size\n\n**--flags**\n> List all available color schemes/flags\n\n**-h, --help**\n> Show help message"],["AVAILABLE FLAGS","Common color schemes include: trans, rainbow, gay, lesbian, bi, pan, nonbinary, ace, aro, genderqueer, genderfluid, and many more."],["CAVEATS","Output requires a terminal that supports ANSI color codes. Some color schemes may not display correctly on terminals with limited color support. The tool is primarily for entertainment and terminal customization."],["HISTORY","BLAHAJ was created by **GeopJr** as a fun terminal tool inspired by lolcat and the viral popularity of the IKEA BLAHAJ shark plushie, particularly within LGBTQ+ communities. Multiple implementations exist in Crystal, Node.js, and Rust."],["INSTALL","```nix: nix profile install nixpkgs#blahaj```"],["SEE ALSO","[lolcat](/man/lolcat)(1), [figlet](/man/figlet)(1), [cowsay](/man/cowsay)(1)"],["RESOURCES","```[Source code](https://codeberg.org/GeopJr/BLAHAJ)```"]],"e":["lolcat","figlet","cowsay"],"x":"blahaj --shark"},"cargo-logout":{"n":"cargo-logout","t":"Remove a saved registry API token","s":[["TAGLINE","Remove a saved registry API token"],["TLDR","**Logout from crates.io**\n\n```cargo logout```\n\n**Logout from specific registry**\n\n```cargo logout --registry [my-registry]```"],["SYNOPSIS","**cargo logout** [_options_]"],["DESCRIPTION","**cargo logout** removes the API token for a registry from local storage. This revokes the token saved by cargo login."],["PARAMETERS","**--registry** _name_\n> Registry to logout from (default: crates-io)\n\n**-v**, **--verbose**\n> Verbose output\n\n**-q**, **--quiet**\n> Suppress output"],["BEHAVIOR","Removes the token entry from ~/.cargo/credentials.toml for the specified registry. Does not revoke the token on the server side."],["CAVEATS","To fully revoke access, also regenerate the token on the registry website (e.g., https://crates.io/me for crates.io). Only removes local credentials."],["INSTALL","```apt: sudo apt install cargo```\n\n```dnf: sudo dnf install cargo```\n\n```apk: sudo apk add cargo```\n\n```zypper: sudo zypper install cargo```\n\n```nix: nix profile install nixpkgs#cargo```"],["SEE ALSO","[cargo](/man/cargo)(1), [cargo-login](/man/cargo-login)(1), [cargo-publish](/man/cargo-publish)(1)"],["RESOURCES","```[Source code](https://github.com/rust-lang/cargo)```\n\n```[Documentation](https://doc.rust-lang.org/cargo/commands/cargo-logout.html)```"]],"e":["cargo","cargo-login","cargo-publish"],"x":"cargo logout"},"cargo-uninstall":{"n":"cargo-uninstall","t":"Remove a Rust binary installed by cargo install","s":[["TAGLINE","Remove a Rust binary installed by cargo install"],["TLDR","**Uninstall package**\n\n```cargo uninstall [package]```\n\n**Uninstall specific binary**\n\n```cargo uninstall [package] --bin [binary]```\n\n**Uninstall from custom root**\n\n```cargo uninstall [package] --root [~/.local]```\n\n**Uninstall multiple packages**\n\n```cargo uninstall [pkg1] [pkg2]```"],["SYNOPSIS","**cargo uninstall** [_options_] [_spec_...]"],["DESCRIPTION","**cargo uninstall** removes packages that were previously installed with `cargo install`. By default it removes all binaries associated with the package. The `--bin` flag can be used to remove only a specific binary while leaving others from the same package in place.\n\nThe installation root is determined by the `--root` flag, the `CARGO_INSTALL_ROOT` environment variable, the `install.root` configuration value, or `$CARGO_HOME` (defaulting to `~/.cargo`). Only the installed binaries are removed; cached build artifacts in the target directory are not cleaned up."],["PARAMETERS","**-p**, **--package** _spec_\n> Package to uninstall\n\n**--bin** _name_\n> Only uninstall specified binary\n\n**--example** _name_\n> Only uninstall specified example\n\n**--root** _dir_\n> Directory to uninstall from\n\n**-v**, **--verbose**\n> Verbose output (use twice for very verbose)\n\n**-q**, **--quiet**\n> Suppress output\n\n**--color** _when_\n> Control colored output: auto, always, or never"],["INSTALLATION ROOT","Determined by (in order):\n1. --root option\n2. CARGO_INSTALL_ROOT environment variable\n3. install.root config value\n4. CARGO_HOME environment variable\n5. ~/.cargo"],["CAVEATS","Only removes binaries, not cached build artifacts. Package spec can be name or name@version."],["INSTALL","```apt: sudo apt install cargo```\n\n```dnf: sudo dnf install cargo```\n\n```apk: sudo apk add cargo```\n\n```zypper: sudo zypper install cargo```\n\n```nix: nix profile install nixpkgs#cargo```"],["SEE ALSO","[cargo](/man/cargo)(1), [cargo-install](/man/cargo-install)(1)"]],"e":["cargo","cargo-install"],"x":"cargo uninstall [package]"},"chardet":{"n":"chardet","t":"detect character encoding of text files","s":[["TAGLINE","detect character encoding of text files"],["TLDR","**Detect encoding** of a file\n\n```chardet [file.txt]```\n\n**Detect encoding** of multiple files\n\n```chardet [file1.txt] [file2.txt] [file3.txt]```\n\n**Read from stdin**\n\n```cat [file.txt] | chardet```\n\n**Detect with verbose output**\n\n```chardetect [file.txt]```"],["SYNOPSIS","**chardet** [_file_...]\n\n**chardetect** [_file_...]"],["DESCRIPTION","**chardet** (or chardetect) is a command-line tool that detects the character encoding of text files. It uses the chardet library, which is a port of Mozilla's character encoding auto-detection algorithm.\n\nThe tool analyzes byte patterns in the file to determine the most likely encoding (UTF-8, ISO-8859-1, Windows-1252, etc.) and provides a confidence score. This is useful when dealing with files of unknown encoding or when preparing data for processing.\n\nchardet is part of the Python chardet package and is commonly used in text processing pipelines where encoding must be determined before conversion."],["PARAMETERS","**file**\n> One or more files to analyze.\n\n**--help**\n> Display help information.\n\n**--version**\n> Show version information."],["CAVEATS","Detection is heuristic and not 100% accurate, especially for short texts or ambiguous encodings. ASCII is a subset of many encodings, making detection difficult. The confidence score indicates reliability of the detection. Binary files may produce incorrect or low-confidence results."],["HISTORY","**chardet** is a Python implementation of the character encoding detection algorithm used in **Mozilla Firefox**. The original algorithm was developed by **Netscape/Mozilla** to handle the diversity of character encodings on the web. The Python port was created by **Mark Pilgrim** in **2006** and is now maintained by **Dan Blanchard** and the community."],["INSTALL","```brew: brew install chardet```"],["SEE ALSO","[file](/man/file)(1), [iconv](/man/iconv)(1), [enca](/man/enca)(1)"]],"e":["file","iconv","enca"],"x":"chardet [file.txt]"},"chrony":{"n":"chrony","t":"Versatile NTP time synchronization suite","s":[["TAGLINE","Versatile NTP time synchronization suite"],["TLDR","**Check synchronization status**\n\n```chronyc tracking```\n\n**Show NTP sources**\n\n```chronyc sources```\n\n**Show detailed source statistics**\n\n```chronyc sourcestats```\n\n**Force immediate synchronization**\n\n```sudo chronyc makestep```\n\n**Add a new NTP server**\n\n```chronyc add server [ntp.example.com]```\n\n**Show all peers**\n\n```chronyc clients```\n\n**Check if chrony is synchronized**\n\n```chronyc waitsync```"],["SYNOPSIS","**chronyc** [_options_] [_command_]\n\n**chronyd** [_options_]"],["PARAMETERS (chronyc)","**tracking**\n> Display system clock synchronization status.\n\n**sources** [**-v**]\n> Show NTP sources (verbose with -v).\n\n**sourcestats**\n> Display source statistics.\n\n**makestep**\n> Step clock immediately if offset is large.\n\n**add server** _address_\n> Add an NTP server.\n\n**delete** _address_\n> Remove an NTP source.\n\n**burst** _polls_/_good_\n> Speed up initial synchronization.\n\n**activity**\n> Show number of online/offline sources.\n\n**ntpdata** _address_\n> Show detailed NTP data for a source.\n\n**waitsync** [_max-tries_]\n> Wait until synchronized."],["PARAMETERS (chronyd)","**-d**\n> Run in foreground (debug mode).\n\n**-f** _file_\n> Specify configuration file.\n\n**-q**\n> Set clock once and exit.\n\n**-Q**\n> Like -q but don't set clock."],["DESCRIPTION","**chrony** is a versatile NTP (Network Time Protocol) implementation for synchronizing system clocks. It consists of **chronyd** (daemon) and **chronyc** (command-line interface).\n\nchrony excels in challenging environments: intermittent network connections, virtual machines with unstable clocks, and systems that sleep or hibernate. It synchronizes faster than ntpd and maintains better accuracy in variable conditions.\n\nConfiguration is in **/etc/chrony.conf** (or **/etc/chrony/chrony.conf**), specifying NTP servers, access controls, and clock adjustments. The daemon can operate as both NTP client and server."],["CONFIGURATION","**/etc/chrony.conf** (or **/etc/chrony/chrony.conf**)\n> Main configuration file specifying NTP servers, access controls, clock adjustments, and logging options.\n\n**/var/lib/chrony/drift**\n> Drift file tracking the system clock's frequency error for accurate correction when offline.\n\n**/var/log/chrony/** (or via syslog)\n> Log directory for tracking and measurements data."],["CAVEATS","The makestep command can cause time jumps that affect running applications. Firewall must allow UDP port 123 for NTP. chronyd must run as root or with CAP_SYS_TIME capability. Some applications may malfunction if time changes significantly."],["HISTORY","chrony was originally written by **Richard Curnow** starting in **1997** to provide better accuracy for systems with intermittent network connectivity. **Miroslav Lichvar** took over maintenance and development in **2007**. chrony has become the default NTP client in many Linux distributions including Fedora and RHEL, chosen over ntpd for its faster sync and better handling of modern computing environments."],["INSTALL","```dnf: sudo dnf install chrony```\n\n```pacman: sudo pacman -S chrony```\n\n```zypper: sudo zypper install chrony```\n\n```brew: brew install chrony```\n\n```nix: nix profile install nixpkgs#chrony```"],["SEE ALSO","[ntpd](/man/ntpd)(8), [timedatectl](/man/timedatectl)(1), [hwclock](/man/hwclock)(8)"],["RESOURCES","```[Homepage](https://chrony-project.org/)```\n\n```[Documentation](https://chrony-project.org/documentation.html)```"]],"e":["ntpd","timedatectl","hwclock"],"x":"chronyc tracking"},"concord":{"n":"concord","t":"Concord workflow CLI for linting and running flows locally","s":[["TAGLINE","Concord workflow CLI for linting and running flows locally"],["TLDR","**Show the installed CLI version**\n\n```concord --version```\n\n**Lint Concord YAML files in the current directory**\n\n```concord lint```\n\n**Lint files in a specific directory with verbose output**\n\n```concord lint -v [path/to/project]```\n\n**Run the default flow defined in concord.yml locally**\n\n```concord run```\n\n**Run with a custom local secret directory**\n\n```concord run --secret-dir=[$HOME/.my_secrets]```"],["SYNOPSIS","**concord** **lint** [_options_] [_target_dir_]\n\n**concord** **run** [_options_]\n\n**concord** **--version**"],["COMMANDS","**lint** [_dir_]\n> Parse and validate Concord YAML files in _dir_ (default: current directory). Checks DSL syntax and expression correctness without executing anything. Cannot verify task invocations, parameter types, or dynamically imported resources.\n\n**run**\n> Execute a Concord v2 flow locally without contacting a Concord server. Files from the current directory are copied to **$PWD/target**, dependencies are resolved, and the default flow is run."],["PARAMETERS","**-h**, **--help**\n> Show help text.\n\n**-v**, **--verbose**\n> Verbose output (lint command).\n\n**--secret-dir** _DIR_\n> Directory used by the local secret resolver (default **$HOME/.concord/secrets**).\n\n**--version**\n> Print the CLI version."],["DESCRIPTION","**concord** is the command-line companion to the **Concord** workflow orchestration server developed by Walmart Labs. The CLI focuses on the local development loop:\n\n- **Linting** parses **concord.yml** and any imported YAML, validating DSL syntax and expression syntax. It does not require a running Concord server.\n- **Running flows locally** executes a flow on the developer machine, resolving Maven dependencies into **$HOME/.concord/depsCache** and Git imports into **$HOME/.concord/repoCache**. Local file-based secrets are resolved from **$HOME/.concord/secrets** (or the directory passed to **--secret-dir**).\n\nLocal execution supports standard flow elements, tasks, external dependencies, Git imports, and string/key-pair/credential/file secrets. **Forms**, **profiles**, and **password-protected secrets** are currently unsupported. Local execution forces the runtime to **concord-v2**."],["CONFIGURATION","**concord.yml**\n> The flow definition consumed by both **lint** and **run**.\n\n**$HOME/.concord/secrets/_org_/_name_**\n> Default location for local secrets, organised by org and secret name. String, key-pair (**name** + **name.pub**), credential (JSON), and file secrets are recognised.\n\n**$HOME/.concord/depsCache/**\n> Cached Maven dependencies pulled in during **concord run**.\n\n**$HOME/.concord/repoCache/**\n> Cached Git repositories used by imports."],["CAVEATS","Requires **Java 17** or newer in **PATH**. Local **run** is not a full Concord server: forms, profiles, and password-protected secrets do not work. Linting is best-effort and may miss errors that only manifest at runtime, such as missing tasks, wrong parameter types, or unresolved dynamic imports."],["HISTORY","**Concord** was open-sourced by **Walmart Labs** in **2017** as a workflow orchestration and continuous-deployment platform built around YAML-defined flows and a server runtime. The standalone **concord** CLI was added later to give developers a fast local linting and execution loop without round-tripping through the Concord server."],["INSTALL","```brew: brew install concord```\n\n```nix: nix profile install nixpkgs#concord```"],["SEE ALSO","[java](/man/java)(1), [git](/man/git)(1), [mvn](/man/mvn)(1), [yq](/man/yq)(1)"]],"e":["java","git","mvn","yq"],"x":"concord --version"},"conda-run":{"n":"conda-run","t":"environment command executor","s":[["TAGLINE","environment command executor"],["TLDR","**Run a command** in an environment\n\n```conda run -n [env_name] [command]```\n\n**Run Python script** in environment\n\n```conda run -n [env_name] python [script.py]```\n\n**Run with live output**\n\n```conda run --live-stream -n [env_name] [command]```"],["SYNOPSIS","**conda** **run** [_options_] _command_"],["PARAMETERS","**-n**, **--name** _name_\n> Run in named environment.\n\n**-p**, **--prefix** _path_\n> Run in environment at path.\n\n**--live-stream**\n> Stream output in real-time.\n\n**--no-capture-output**\n> Don't capture stdout/stderr."],["CONFIGURATION","**~/.condarc**\n> User-level conda configuration for channels, package settings, and solver options.\n\n**/opt/conda/.condarc**\n> System-wide conda configuration."],["DESCRIPTION","**conda run** executes a command within a conda environment without activating it. This is useful for scripts and automation where you need to run commands in different environments."],["INSTALL","```dnf: sudo dnf install conda```\n\n```nix: nix profile install nixpkgs#conda```"],["SEE ALSO","[conda](/man/conda)(1), [conda-activate](/man/conda-activate)(1)"],["RESOURCES","```[Source code](https://github.com/conda/conda)```\n\n```[Homepage](https://docs.conda.io)```\n\n```[Documentation](https://docs.conda.io/projects/conda/en/stable/commands/index.html)```"]],"e":["conda","conda-activate"],"x":"conda run -n [env_name] [command]"},"dch":{"n":"dch","t":"Debian package changelog editor","s":[["TAGLINE","Debian package changelog editor"],["TLDR","**Add new changelog entry**\n\n```dch \"[changelog message]\"```\n\n**Increment version** for new release\n\n```dch -i```\n\n**Create new changelog** entry with version\n\n```dch -v [1.0.0-1]```\n\n**Edit changelog** in editor\n\n```dch -e```\n\n**Append to current** entry\n\n```dch -a \"[additional message]\"```\n\n**Create entry for** specific distribution\n\n```dch -D [unstable] \"[message]\"```\n\n**Set urgency level**\n\n```dch -u [high] \"[message]\"```"],["SYNOPSIS","**dch** [_options_] [_message_]"],["PARAMETERS","_MESSAGE_\n> Changelog entry text to add.\n\n**-i**, **--increment**\n> Increment version for new upstream release.\n\n**-v** _VERSION_\n> Create entry with specific version.\n\n**-a**, **--append**\n> Append to current changelog entry.\n\n**-e**, **--edit**\n> Open changelog in editor.\n\n**-D** _DIST_\n> Set target distribution.\n\n**-u** _URGENCY_\n> Set urgency: low, medium, high, emergency, critical.\n\n**-n**, **--nmu**\n> Non-maintainer upload increment.\n\n**-b**, **--force-bad-version**\n> Allow a version that is lower than the current.\n\n**-p**, **--preserve**\n> Preserve directory name for package name.\n\n**-r**, **--release**\n> Finalize entry for release (set distribution and timestamp).\n\n**--create**\n> Create a new changelog file.\n\n**--help**\n> Display help information."],["DESCRIPTION","**dch** (Debian changelog) is a tool for editing Debian package changelog files (debian/changelog). It automates version incrementing, entry formatting, and maintains the strict changelog format required by Debian packaging.\n\nThe tool handles the tedious formatting requirements of Debian changelogs, including proper date formats, maintainer information from DEBEMAIL/DEBFULLNAME environment variables, and version string manipulation.\n\ndch is essential for Debian package maintainers, automating the changelog workflow during package updates, security fixes, and new upstream releases."],["CAVEATS","Requires properly configured DEBEMAIL and DEBFULLNAME environment variables. Must be run in a directory containing debian/changelog. Version string format must follow Debian conventions."],["HISTORY","dch is part of **devscripts**, a collection of tools for Debian package maintainers. It has been a core Debian development tool since the mid-1990s, evolving alongside Debian's packaging standards."],["SEE ALSO","[debchange](/man/debchange)(1), [dpkg-buildpackage](/man/dpkg-buildpackage)(1), [dpkg](/man/dpkg)(1)"]],"e":["debchange","dpkg-buildpackage","dpkg"],"x":"dch \"[changelog message]\""},"debugfs":{"n":"debugfs","t":"interactive debugger for ext2/ext3/ext4 filesystems","s":[["TAGLINE","interactive debugger for ext2/ext3/ext4 filesystems"],["TLDR","Open filesystem in **read-only** mode\n\n```debugfs [/dev/sdXN]```\n\nOpen filesystem in **read-write** mode\n\n```debugfs -w [/dev/sdXN]```\n\nRead commands from **file**\n\n```debugfs -f [path/to/cmd_file] [/dev/sdXN]```\n\nExecute a **single command** and exit\n\n```debugfs -R \"stats\" [/dev/sdXN]```\n\n**List deleted** inodes (interactive)\n\n```lsdel```\n\n**Recover** a deleted inode to a file (interactive)\n\n```dump <[inode_number]> [/path/to/output]```"],["SYNOPSIS","**debugfs** [_options_] [_device_]"],["DESCRIPTION","**debugfs** is an interactive debugger for ext2/ext3/ext4 filesystems. It allows direct manipulation of filesystem structures, which is useful for data recovery, analysis, and troubleshooting.\n\nCan be used to recover deleted files, examine inodes, and manually fix filesystem issues."],["PARAMETERS","**-w**\n> Open in read-write mode (default is read-only)\n\n**-c**\n> Open in catastrophic mode (inode and group bitmaps not read initially, forces read-only)\n\n**-n**\n> Disable metadata checksum verification\n\n**-f** _cmd_file_\n> Read and execute commands from file, then exit\n\n**-R** _request_\n> Execute a single command and exit\n\n**-b** _blocksize_\n> Force a specific block size in bytes instead of auto-detecting\n\n**-s** _superblock_\n> Read superblock from the given block number (requires -b)\n\n**-i**\n> Treat device as an ext2 image file created by e2image\n\n**-D**\n> Open device using Direct I/O, bypassing the buffer cache\n\n**-z** _undo_file_\n> Write old block contents to an undo file before overwriting (for use with e2undo)"],["INTERACTIVE COMMANDS","**stats**\n> Show filesystem statistics\n\n**ls** _dir_\n> List directory contents\n\n**cat** _file_\n> Display file contents\n\n**lsdel**\n> List deleted inodes\n\n**undel** _<inode>_ _[pathname]_\n> Undelete inode and optionally link to pathname\n\n**dump** _inode_ _file_\n> Dump inode contents to a file\n\n**ncheck** _inode_\n> Translate inode numbers to pathnames\n\n**icheck** _block_\n> Translate block numbers to inodes\n\n**logdump**\n> Dump ext3/ext4 journal contents"],["CAVEATS","Requires root privileges. Use with extreme caution in write mode. Unmount filesystem before making changes. Improper use can cause data loss."],["INSTALL","```apt: sudo apt install e2fsprogs```\n\n```dnf: sudo dnf install e2fsprogs```\n\n```pacman: sudo pacman -S e2fsprogs```\n\n```apk: sudo apk add e2fsprogs-extra```\n\n```zypper: sudo zypper install e2fsprogs```\n\n```brew: brew install e2fsprogs```\n\n```nix: nix profile install nixpkgs#e2fsprogs```"],["SEE ALSO","[e2fsck](/man/e2fsck)(8), [dumpe2fs](/man/dumpe2fs)(8), [tune2fs](/man/tune2fs)(8)"]],"e":["e2fsck","dumpe2fs","tune2fs"],"x":"debugfs [/dev/sdXN]"},"deja":{"n":"deja","t":"Predictive ghost-text autosuggestions for zsh","s":[["TAGLINE","Predictive ghost-text autosuggestions for zsh"],["TLDR","**Import** existing zsh history into the local database\n\n```deja import```\n\n**Activate** shell integration in the current session\n\n```eval \"$(deja init zsh)\"```\n\n**Check** that the background daemon answers\n\n```deja ping```\n\n**Show** the current fuzzy-matching preset\n\n```deja fuzzy```\n\n**Tighten** fuzzy matching for near-adjacent character matches\n\n```deja fuzzy tight```\n\n**Query** the daemon for a suggestion (used by the zsh widget)\n\n```deja query```"],["SYNOPSIS","**deja** **import** [_options_]\n\n**deja** **init** _zsh_\n\n**deja** **ping**\n\n**deja** **fuzzy** [_preset_|**cycle**|**back**]\n\n**deja** **query** [_options_]"],["PARAMETERS","**--file** _path_\n> History file for **import** when **$HISTFILE** is unset or not exported (default **~/.zsh_history**).\n\n**DEJA_FUZZY**\n> Session override for fuzzy preset (**tight**, **smart**, **loose**) before the daemon starts.\n\n**DEJA_ACCEPT_KEY**, **DEJA_CYCLE_KEY**, **DEJA_TOGGLE_KEY**, **DEJA_DISMISS_KEY**\n> Remap key sequences (export before **eval \"$(deja init zsh)\"**)."],["DESCRIPTION","**deja** is a local-first replacement for **zsh-autosuggestions**. A single background Go daemon scores command history with fuzzy matching, directory affinity, frecency decay, and sequence prediction, then returns inline ghost-text suggestions over a Unix socket in under a millisecond.\n\nAfter **deja import**, add **eval \"$(deja init zsh)\"** to **~/.zshrc**. The init script spawns the daemon on first use and wires ZLE widgets: **→** accepts the full suggestion, **Tab** cycles ranked alternatives, **Ctrl+X** suppresses suggestions for the session, and **Shift+→** / **Shift+←** cycle fuzzy presets (**tight**, **smart**, **loose**).\n\nState lives in **~/.local/share/deja/deja.db** (SQLite WAL). Nothing is synced to a cloud service. If **zsh-autosuggestions** is already loaded, deja detects it and stands down to avoid conflicting ZLE wrappers."],["CONFIGURATION","Data directory: **~/.local/share/deja/** (**deja.db**, **sock**, generated **init.zsh**). Fuzzy preset persists across restarts via **deja fuzzy** _preset_. Oh My Zsh users can install the **deja** plugin from the upstream repo instead of hand-editing **~/.zshrc**, but should not enable both the plugin and a duplicate **eval \"$(deja init zsh)\"** line."],["CAVEATS","zsh only. Requires a reachable daemon (**deja ping** → **pong**). After a crash, remove a stale **~/.local/share/deja/sock** and open a new shell. Do not run alongside **zsh-autosuggestions**."],["INSTALL","```nix: nix profile install nixpkgs#deja```"],["SEE ALSO","[zsh](/man/zsh)(1), [history](/man/history)(1)"]],"e":["zsh","history"],"x":"deja import"},"dnf-group":{"n":"dnf-group","t":"manage package groups","s":[["TAGLINE","manage package groups"],["TLDR","**List** DNF groups\n\n```dnf group list```\n\nShow **group info**\n\n```dnf group info [group_name]```\n\n**Install** a group\n\n```dnf group install [group_name]```\n\n**Remove** a group\n\n```dnf group remove [group_name]```\n\n**Upgrade** a group\n\n```dnf group upgrade [group_name]```"],["SYNOPSIS","**dnf group** _command_ [_group_name_]"],["DESCRIPTION","**dnf group** manages virtual collections of packages. Groups bundle related packages together, such as \"Development Tools\" or \"Server\".\n\nInstalling a group installs all its mandatory and default packages."],["PARAMETERS","**list**\n> List available groups\n\n**info** _group_\n> Show group details including packages\n\n**install** _group_\n> Install group packages\n\n**remove** _group_\n> Remove group packages\n\n**upgrade** _group_\n> Upgrade group packages"],["CAVEATS","Groups can be referenced by name or ID. Optional packages in groups aren't installed by default. Removing a group may leave some packages installed."],["INSTALL","```pacman: sudo pacman -S dnf```"],["SEE ALSO","[dnf](/man/dnf)(8), [dnf-install](/man/dnf-install)(8)"],["RESOURCES","```[Source code](https://github.com/rpm-software-management/dnf)```\n\n```[Documentation](https://dnf.readthedocs.io/en/latest/command_ref.html)```"]],"e":["dnf","dnf-install"],"x":"dnf group list"},"dnf5":{"n":"dnf5","t":"next-generation DNF package manager","s":[["TAGLINE","next-generation DNF package manager"],["TLDR","This command is an alias of **dnf**.\n\nSee documentation for the original command.\n\n```tldr dnf```"],["DESCRIPTION","**dnf5** is the next-generation DNF package manager. On Fedora 37-40, `dnf` runs DNF v4 while `dnf5` runs DNF v5. From Fedora 41+, `dnf` defaults to v5."],["INSTALL","```dnf: sudo dnf install dnf5```\n\n```pacman: sudo pacman -S dnf5```\n\n```zypper: sudo zypper install dnf5```\n\n```nix: nix profile install nixpkgs#dnf5```"],["SEE ALSO","[dnf](/man/dnf)(8), [dnf4](/man/dnf4)(8)"],["RESOURCES","```[Source code](https://github.com/rpm-software-management/dnf5)```\n\n```[Documentation](https://dnf5.readthedocs.io/)```"]],"e":["dnf","dnf4"],"x":"tldr dnf"},"double-parenthesis":{"n":"double-parenthesis","t":"arithmetic evaluation and expansion","s":[["TAGLINE","arithmetic evaluation and expansion"],["TLDR","**Arithmetic evaluation**\n\n```(( x = 5 + 3 ))```\n\n**Increment variable**\n\n```(( count++ ))```\n\n**Conditional test**\n\n```(( x > 10 )) && echo \"big\"```\n\n**Capture arithmetic result**\n\n```result=$(( a * b ))```\n\n**Complex expression**\n\n```(( result = (a + b) * c / d ))```"],["SYNOPSIS","**((**_expression_**))**\n\n**$((**_expression_**))**"],["OPERATORS","**Arithmetic**: +, -, *, /, % (modulo), ** (exponent)\n\n**Assignment**: =, +=, -=, *=, /=, %=\n\n**Comparison**: ==, !=, <, >, <=, >=\n\n**Logical**: && (and), || (or), ! (not)\n\n**Bitwise**: &, |, ^, ~, <<, >>\n\n**Increment/Decrement**: ++, --\n\n**Ternary**: condition ? true : false"],["DESCRIPTION","**((  ))** is the shell's arithmetic evaluation construct. It evaluates mathematical expressions and returns success (0) if the result is non-zero, failure (1) if zero.\n\nInside **((...))**, variables don't need the **$** prefix and whitespace is ignored. Standard C-style operators work as expected.\n\n**$((...))** performs arithmetic expansion, substituting the result. **((...))** evaluates without substitution (for side effects like assignment).\n\n```bash"],["Evaluation (for conditionals/assignments)","(( x = 5 ))\n(( x > 3 )) && echo \"yes\""],["Expansion (to get the value)","echo \"Result: $(( 5 + 3 ))\"\n```"],["CAVEATS","Two different constructs are easily confused. **$((...))**, arithmetic *expansion*, **is POSIX** and works in every shell including dash. **((...))**, the arithmetic *command*, is **not** POSIX: it comes from ksh and works in bash, ksh, and zsh, but not in dash. In a `#!/bin/sh` script use `[ \"$(( a + b ))\" -gt 0 ]` rather than `(( a + b > 0 ))`.\n\nOnly **integers** are supported. `$(( 5 / 2 ))` is **2**, not 2.5, and there is no way to get a fractional result: use **bc** or **awk** for real arithmetic.\n\nThe exit status is inverted relative to the value, which trips up almost everyone once. A non-zero result means success (status 0) and a zero result means failure (status 1), because the construct is designed to read as a C-style truth test. The practical consequence is that `(( count-- ))` as the last line of a function returns failure when `count` reaches zero, and under `set -e` that will terminate the script. Write `(( count-- )) || true` when the value may be zero.\n\nBeware also that a leading zero makes a number **octal** inside arithmetic context: `$(( 010 ))` is 8. This bites scripts handling zero-padded dates, where `$(( 08 ))` is an error rather than eight. Force base ten with `10#`, as in `$(( 10#$month ))`.\n\n**\\*\\*** (exponentiation) is a bash/ksh/zsh extension and is not available everywhere, and the operator is not defined in POSIX arithmetic."],["HISTORY","Arithmetic in the original Bourne shell meant calling out to the external **expr** command, which was slow and needed escaping for almost every operator. The **Korn shell** introduced both `let` and the `((...))` command to make arithmetic a first-class part of the language, and POSIX subsequently standardised the `$((...))` expansion form, though not the `((...))` command. bash and zsh support all of them, which is why the same script can be written three different ways and why `expr` still turns up in old code."],["SEE ALSO","[bash](/man/bash)(1), [bc](/man/bc)(1), [expr](/man/expr)(1), [let](/man/let)(1), [double-bracket](/man/double-bracket)(1)"]],"e":["bash","bc","expr","let","double-bracket"],"x":"(( x = 5 + 3 ))"},"etckeeper":{"n":"etckeeper","t":"version control for /etc directory","s":[["TAGLINE","version control for /etc directory"],["TLDR","**Initialize** Git repo in /etc\n\n```sudo etckeeper init```\n\n**Commit** all changes in /etc\n\n```sudo etckeeper commit [message]```\n\nRun **arbitrary Git** commands\n\n```sudo etckeeper vcs [status]```\n\n**Check** for uncommitted changes\n\n```sudo etckeeper unclean```\n\n**Stop tracking** and destroy repo\n\n```sudo etckeeper uninit```"],["SYNOPSIS","**etckeeper** _command_ [_arguments_]"],["DESCRIPTION","**etckeeper** tracks system configuration files in /etc using version control (Git, Mercurial, Bazaar, or Darcs). It integrates seamlessly with package managers like apt, yum, pacman, and dnf to automatically commit changes before and after package operations.\n\nThis provides an audit trail of all system configuration modifications, making it possible to review what changed when, understand why a system behaves differently after updates, and revert problematic changes. The tool preserves file permissions and metadata that Git normally doesn't track.\n\netckeeper is valuable for system administrators managing multiple servers, troubleshooting configuration issues, and maintaining compliance documentation."],["PARAMETERS","**init**\n> Initialize repository in /etc\n\n**commit** _message_\n> Commit all changes\n\n**vcs** _command_\n> Run VCS command on repo\n\n**unclean**\n> Check for uncommitted changes\n\n**uninit**\n> Remove repository\n\n**pre-install**\n> Run before package install\n\n**post-install**\n> Run after package install"],["CONFIGURATION","**/etc/etckeeper/etckeeper.conf**\n> Main configuration file specifying VCS to use, package manager hooks to enable, and files to ignore."],["CAVEATS","Requires root privileges. Must run from /etc directory for init. Integrates with apt, yum, and pacman. Sensitive files may need gitignore entries."],["INSTALL","```dnf: sudo dnf install etckeeper```\n\n```pacman: sudo pacman -S etckeeper```\n\n```apk: sudo apk add etckeeper```\n\n```zypper: sudo zypper install etckeeper```"],["SEE ALSO","[git](/man/git)(1), [cron](/man/cron)(8)"],["RESOURCES","```[Source code](https://git.joeyh.name/index.cgi/etckeeper.git/)```\n\n```[Homepage](https://etckeeper.branchable.com/)```"]],"e":["git","cron"],"x":"sudo etckeeper init"},"f3write":{"n":"f3write","t":"flash drive capacity tester","s":[["TAGLINE","flash drive capacity tester"],["TLDR","**Fill a mounted flash drive** with test files\n\n```f3write [/media/flash_drive]```\n\n**Write test files starting** from a specific file number\n\n```f3write --start-at=[10] [/media/flash_drive]```\n\n**Write test files up to** a specific file number\n\n```f3write --end-at=[50] [/media/flash_drive]```\n\n**Write to a raw device** (unmounted)\n\n```sudo f3write -g [/dev/sdX]```\n\n**Force write even if** device appears busy\n\n```f3write --force [/media/flash_drive]```\n\n**Optimize for low memory** systems\n\n```f3write --low-memory [/media/flash_drive]```"],["SYNOPSIS","**f3write** [**--start-at**=_NUM_] [**--end-at**=_NUM_] _PATH_"],["PARAMETERS","**--start-at**=_NUM_\n> Initial number for file names (default: 1).\n\n**--end-at**=_NUM_\n> Final number for file names (default: infinity).\n\n**-a**, **--append**\n> Append new files to existing ones on the target device.\n\n**-b** _BYTES_, **--block-size**=_BYTES_\n> Set I/O block size in bytes (default: 1M).\n\n**-f**, **--force**\n> Force writing even if device appears busy or contains data.\n\n**-g**, **--full-device**\n> Write to the full raw device (device must be unmounted).\n\n**-L**, **--low-memory**\n> Optimize operation for systems with limited memory.\n\n**-s** _OFFSET_, **--start-offset**=_OFFSET_\n> Starting offset in bytes from device beginning.\n\n**-S** _OFFSET_, **--end-offset**=_OFFSET_\n> Ending offset in bytes where writing should stop."],["DESCRIPTION","**f3write** is part of the F3 (Fight Flash Fraud) suite and tests the actual capacity of flash storage devices by filling them with known data patterns. It writes 1GB files named N.h2w (where N is a number) to the target filesystem until the drive is full.\n\nThe typical workflow involves writing test data with f3write, then verifying with **f3read**. If the drive is counterfeit and reports false capacity, f3read will detect data corruption beyond the actual physical capacity.\n\nf3write can operate on a mounted filesystem (writing to a directory) or directly to a raw unmounted device using the **-g** option."],["CAVEATS","**All data on the tested disk will be overwritten.** Always backup important data before testing. When using raw device mode (-g), ensure you specify the correct device to avoid destroying data on other drives. The test can take considerable time depending on drive size and speed."],["HISTORY","f3write was created by Michel Machado as part of the F3 (Fight Flash Fraud) suite. The tool addresses the widespread problem of counterfeit flash storage that uses modified firmware to report inflated capacities. When users attempt to store more data than the actual capacity, the drive silently corrupts data by overwriting existing content or simply discarding writes."],["INSTALL","```apt: sudo apt install f3```\n\n```dnf: sudo dnf install f3```\n\n```apk: sudo apk add f3```\n\n```zypper: sudo zypper install f3```\n\n```brew: brew install f3```\n\n```nix: nix profile install nixpkgs#f3```"],["SEE ALSO","[f3read](/man/f3read)(1), [f3probe](/man/f3probe)(1), [f3fix](/man/f3fix)(1), [dd](/man/dd)(1)"],["RESOURCES","```[Source code](https://github.com/AltraMayor/f3)```"]],"e":["f3read","f3probe","f3fix","dd"],"x":"f3write [/media/flash_drive]"},"gdb":{"n":"gdb","t":"interactive debugger for compiled programs","s":[["TAGLINE","interactive debugger for compiled programs"],["TLDR","**Debug program**\n\n```gdb [program]```\n\n**Debug with core** file\n\n```gdb [program] [core]```\n\n**Attach to process**\n\n```gdb -p [pid]```\n\n**Run with arguments**\n\n```gdb --args [program] [arg1] [arg2]```\n\n**Execute commands**\n\n```gdb -x [commands.gdb] [program]```"],["SYNOPSIS","**gdb** [_options_] [_program_] [_core_|_pid_]"],["PARAMETERS","_PROGRAM_\n> Executable to debug.\n\n_CORE_\n> Core dump file.\n\n**-p** _PID_\n> Attach to running process.\n\n**--args**\n> Pass arguments to program.\n\n**-x** _FILE_\n> Execute GDB commands from file.\n\n**-q**, **--quiet**\n> Suppress startup messages.\n\n**--tui**\n> Enable text user interface.\n\n**--help**\n> Display help information."],["CONFIGURATION","**~/.gdbinit**\n> User-specific GDB initialization commands executed on startup.\n\n**/etc/gdb/gdbinit**\n> System-wide initialization file for all users.\n\n**.gdbinit**\n> Project-local initialization file for directory-specific commands."],["DESCRIPTION","**gdb** (GNU Debugger) is the standard debugger for C, C++, and other compiled languages on Unix-like systems. It provides comprehensive facilities for inspecting program state, setting breakpoints, stepping through code, examining variables, and analyzing memory contents during execution.\n\nThe debugger operates by controlling program execution, allowing developers to pause at specific points, examine the call stack, modify variable values, and trace execution flow. It can debug running processes by attaching to them, analyze core dumps from crashed programs, and even perform remote debugging across network connections via gdbserver.\n\ngdb supports scripting through its command language and Python integration, enabling automated debugging sessions and custom commands. The Text User Interface (TUI) mode provides a split-screen view of source code, assembly, registers, and command prompt. Debugging symbols (compiled with -g flag) are essential for meaningful source-level debugging, though gdb can still work with stripped binaries at the assembly level.\n\nCommon workflows include setting breakpoints at functions or line numbers, examining variables with print and display commands, stepping through code with next/step/continue, and analyzing crashes through backtrace. The tool is indispensable for diagnosing segmentation faults, memory corruption, race conditions, and complex logic errors in native applications."],["CAVEATS","Requires debug symbols (-g). Optimized code may behave unexpectedly. Learning curve for commands."],["HISTORY","gdb was written by **Richard Stallman** in 1986 as part of the GNU Project. It became the standard Unix debugger, supporting numerous architectures and languages."],["INSTALL","```apt: sudo apt install gdb```\n\n```dnf: sudo dnf install gdb```\n\n```pacman: sudo pacman -S gdb```\n\n```apk: sudo apk add gdb```\n\n```zypper: sudo zypper install gdb```\n\n```brew: brew install gdb```\n\n```nix: nix profile install nixpkgs#gdb```"],["SEE ALSO","[gcore](/man/gcore)(1), [gdbserver](/man/gdbserver)(1), [lldb](/man/lldb)(1)"],["RESOURCES","```[Homepage](https://www.gnu.org/software/gdb/)```\n\n```[Documentation](https://sourceware.org/gdb/current/onlinedocs/gdb/)```"]],"e":["gcore","gdbserver","lldb"],"x":"gdb [program]"},"git-format-patch":{"n":"git-format-patch","t":"Prepare patches for email submission","s":[["TAGLINE","Prepare patches for email submission"],["TLDR","**Create patches for last N commits**\n\n```git format-patch -[n]```\n\n**Create patch since commit**\n\n```git format-patch [commit]```\n\n**Create patch for range**\n\n```git format-patch [commit1]..[commit2]```\n\n**Output to directory**\n\n```git format-patch -o [patches/] [commit]```\n\n**Create single combined patch**\n\n```git format-patch --stdout [commit] > [combined.patch]```\n\n**Include cover letter**\n\n```git format-patch --cover-letter [commit]```\n\n**Create numbered, threaded patches with base info**\n\n```git format-patch --numbered --thread --base=auto [commit]```\n\n**Add email recipients to patches**\n\n```git format-patch --to=[maintainer@example.com] --cc=[list@example.com] -[3]```"],["SYNOPSIS","**git** **format-patch** [_options_] [_since_] | [_revision-range_]"],["PARAMETERS","**-n**, **--numbered**\n> Name output in [PATCH n/m] format.\n\n**-N**, **--no-numbered**\n> Name output in [PATCH] format without numbers.\n\n**-o**, **--output-directory** _dir_\n> Output directory for patch files.\n\n**--stdout**\n> Output all patches to stdout instead of files.\n\n**--cover-letter**\n> Generate a cover letter template for the patch series.\n\n**-s**, **--signoff**\n> Add Signed-off-by trailer.\n\n**--subject-prefix** _prefix_\n> Use given prefix instead of [PATCH].\n\n**-v**, **--reroll-count** _n_\n> Mark patches as v_n_ of the series (e.g., [PATCH v2]).\n\n**--thread**[=_style_]\n> Generate In-Reply-To and References headers. Style: shallow (default) or deep.\n\n**--in-reply-to** _message-id_\n> Make first mail appear as a reply to given message.\n\n**--to** _email_\n> Add To: header to patches. Can be used multiple times.\n\n**--cc** _email_\n> Add Cc: header to patches. Can be used multiple times.\n\n**--base**[=_commit_]\n> Record base tree info. Use \"auto\" for automatic selection.\n\n**--no-stat**\n> Generate plain patches without diffstats.\n\n**--no-binary**\n> Do not output binary file contents, only note changes.\n\n**--interdiff** _previous_\n> Insert interdiff into cover letter comparing to previous version.\n\n**--range-diff** _previous_\n> Insert range-diff into cover letter comparing to previous version.\n\n**--filename-max-length** _n_\n> Limit generated filenames to around _n_ bytes (default: 64)."],["DESCRIPTION","**git format-patch** generates patch files from commits in a format suitable for email-based code review and submission workflows. Each commit is formatted as a separate .patch file containing the diff, commit metadata, and message in mbox format.\n\nThis command is fundamental to email-based development workflows used by projects like the Linux kernel and Git itself. The generated patches can be sent using git send-email or standard email clients, and applied with git am while preserving full commit information including author and date.\n\nCover letters (--cover-letter) provide a way to introduce patch series with context. Version numbering with -v supports iterative review cycles. The signoff option adds Signed-off-by trailers for Developer Certificate of Origin compliance."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-am](/man/git-am)(1), [git-send-email](/man/git-send-email)(1), [git-apply](/man/git-apply)(1), [git-diff](/man/git-diff)(1)"]],"e":["git-am","git-send-email","git-apply","git-diff"],"x":"git format-patch -[n]"},"git-imerge":{"n":"git-imerge","t":"Incremental bisect-style merging","s":[["TAGLINE","Incremental bisect-style merging"],["TLDR","**Start an incremental merge** of a branch\n\n```git imerge start --name=[merge-name] [branch]```\n\n**Start an incremental rebase** onto current branch\n\n```git imerge rebase [branch]```\n\n**Continue resolving** after fixing a conflict\n\n```git imerge continue```\n\n**Show visual merge** progress diagram\n\n```git imerge diagram```\n\n**Finish the merge** when all conflicts are resolved\n\n```git imerge finish```\n\n**List all in-progress** incremental merges\n\n```git imerge list```\n\n**Abort and remove** the incremental merge\n\n```git imerge remove```"],["SYNOPSIS","**git imerge** _command_ [_options_]"],["PARAMETERS","**start**\n> Begin incremental merge.\n\n**merge**\n> Start an incremental merge (shorthand for start --goal merge).\n\n**rebase**\n> Start an incremental rebase (shorthand for start --goal rebase).\n\n**continue**\n> Continue resolving conflicts.\n\n**finish**\n> Complete the incremental merge.\n\n**remove**\n> Abort and remove incremental merge state.\n\n**diagram**\n> Show visual merge status grid.\n\n**list**\n> List all in-progress incremental merges.\n\n**autofill**\n> Attempt to fill in all merge blocks automatically.\n\n**simplify**\n> Simplify the merge history after finishing.\n\n**init**\n> Initialize an imerge from existing merge state.\n\n**record**\n> Record the result of a merge done manually.\n\n**drop**\n> Drop a commit from the merge.\n\n**revert**\n> Revert a commit in the merge.\n\n**--name** _NAME_\n> Name for the incremental merge.\n\n**--goal** _GOAL_\n> Merge goal: merge, rebase, rebase-with-history, border, border-with-history.\n\n**--first-parent**\n> Handle first-parent history only.\n\n**--force**\n> Force start even if one is already in progress.\n\n**--help**\n> Display help information."],["DESCRIPTION","**git imerge** performs incremental, bisect-style merges. Instead of one large merge with many conflicts, it merges commits one at a time, making conflict resolution more manageable and helping identify exactly which commits cause conflicts.\n\nThe tool tracks merge progress visually via the diagram command and allows conflicts to be resolved independently. This approach is particularly valuable for difficult merges between long-diverged branches."],["CAVEATS","Separate installation required. More complex than simple merge. Best for difficult merges."],["HISTORY","git imerge was created by **Michael Haggerty** to address the pain of large, conflict-heavy merges by breaking them into smaller pieces."],["INSTALL","```dnf: sudo dnf install git-imerge```\n\n```brew: brew install git-imerge```\n\n```nix: nix profile install nixpkgs#git-imerge```"],["SEE ALSO","[git-merge](/man/git-merge)(1), [git-rebase](/man/git-rebase)(1)"]],"e":["git-merge","git-rebase"],"x":"git imerge start --name=[merge-name] [branch]"},"git-pull":{"n":"git-pull","t":"Fetch and integrate remote changes","s":[["TAGLINE","Fetch and integrate remote changes"],["TLDR","**Pull from origin**\n\n```git pull```\n\n**Pull from specific remote**\n\n```git pull [remote]```\n\n**Pull specific branch**\n\n```git pull [remote] [branch]```\n\n**Pull with rebase**\n\n```git pull --rebase```\n\n**Pull all remotes**\n\n```git pull --all```\n\n**Pull without committing merge**\n\n```git pull --no-commit```\n\n**Pull with fast-forward only**\n\n```git pull --ff-only```"],["SYNOPSIS","**git** **pull** [_options_] [_remote_] [_branch_]"],["PARAMETERS","**--rebase**[=_true|merges|false_]\n> Rebase current branch on top of upstream after fetching instead of merging.\n\n**--no-rebase**\n> Merge upstream into current branch (overrides `pull.rebase` config).\n\n**--ff-only**\n> Only update if fast-forward is possible, fail otherwise.\n\n**--no-ff**\n> Always create a merge commit, even when fast-forward is possible.\n\n**--no-commit**\n> Perform the merge but stop before creating a commit.\n\n**--squash**\n> Squash all fetched commits into a single commit on the current branch.\n\n**--all**\n> Fetch from all remotes.\n\n**--autostash**\n> Automatically stash and reapply uncommitted changes.\n\n**--set-upstream**\n> Add upstream tracking reference for the pulled branch.\n\n**--depth** _n_\n> Limit fetch to specified number of commits from remote tip.\n\n**-t**, **--tags**\n> Fetch all tags from the remote.\n\n**-q**, **--quiet**\n> Suppress output during fetch and merge.\n\n**-v**, **--verbose**\n> Be verbose."],["DESCRIPTION","**git pull** fetches from a remote and integrates the changes with the current branch. It is equivalent to running `git fetch` followed by `git merge`, or `git rebase` when the `--rebase` option is used.\n\nPull strategies vary by workflow. Some teams prefer merge (preserving all history), others prefer rebase (linear history), and some use `--ff-only` to reject non-fast-forward updates and prevent unexpected merge commits. The `pull.rebase` configuration setting controls default behavior."],["CAVEATS","Pulling into a dirty working tree can cause conflicts. Use `--autostash` to automatically stash and reapply local changes. The default merge-or-rebase behavior is controlled by the `pull.rebase` configuration setting. Since Git 2.27, `git pull` warns if `pull.rebase` is not set."],["HISTORY","**git pull** has been part of Git since its initial release by Linus Torvalds in **2005**. The `--ff-only` option was added in Git 1.6.6. The `--autostash` option was added in Git 2.9."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-fetch](/man/git-fetch)(1), [git-merge](/man/git-merge)(1), [git-rebase](/man/git-rebase)(1), [git-stash](/man/git-stash)(1)"]],"e":["git-fetch","git-merge","git-rebase","git-stash"],"x":"git pull"},"gitlab-backup":{"n":"gitlab-backup","t":"Create and restore GitLab instance backups","s":[["TAGLINE","Create and restore GitLab instance backups"],["TLDR","**Create a full GitLab backup**\n\n```sudo gitlab-backup create```\n\n**Create a backup, skipping selected components**\n\n```sudo gitlab-backup create SKIP=db,uploads,registry```\n\n**Create an incremental backup**\n\n```sudo gitlab-backup create INCREMENTAL=yes PREVIOUS_BACKUP=[backup_id]```\n\n**Restore from a backup file**\n\n```sudo gitlab-backup restore BACKUP=[timestamp_version]```\n\n**Restore selected components only**\n\n```sudo gitlab-backup restore BACKUP=[timestamp] SKIP=registry,uploads```"],["SYNOPSIS","**gitlab-backup** _command_ [_VAR_=_value_ ...]"],["SUBCOMMANDS","**create**\n> Create a backup archive of the GitLab instance.\n\n**restore**\n> Restore a GitLab instance from a previously created backup."],["PARAMETERS","**SKIP** _components_\n> Comma-separated list of components to omit. Valid values: **db**, **repositories**, **uploads**, **builds**, **artifacts**, **lfs**, **registry**, **pages**, **terraform_state**, **packages**, **ci_secure_files**.\n\n**BACKUP** _id_\n> Identifier (timestamp_version) of the backup to restore. Required for **restore**.\n\n**STRATEGY** _value_\n> Use **copy** to copy data to a tmp dir before tarring (safer for active sites); default streams directly.\n\n**INCREMENTAL** _yes_\n> Create an incremental backup (requires existing PREVIOUS_BACKUP).\n\n**PREVIOUS_BACKUP** _id_\n> Backup ID to base the incremental backup on.\n\n**GITLAB_BACKUP_MAX_CONCURRENCY** _N_\n> Maximum number of concurrent processes for repository backups.\n\n**GZIP_RSYNCABLE** _yes_\n> Use gzip's --rsyncable mode so backups deduplicate efficiently with rsync."],["DESCRIPTION","**gitlab-backup** creates and restores backups of a GitLab Omnibus or source installation. A backup bundles the database, Git repositories, uploads, CI artifacts, container registry, Pages content, and other components into a single tar archive named **TIMESTAMP_VERSION_gitlab_backup.tar**.\n\nBackups are written to the directory configured by **gitlab_rails['backup_path']** (default **/var/opt/gitlab/backups**). The configuration files (gitlab.rb, secrets.json) are **not** included and must be backed up separately."],["CAVEATS","Run as root (typically via **sudo**). Restore requires the same GitLab major.minor version as the backup. Stop **puma** and **sidekiq** before a restore (**gitlab-ctl stop puma sidekiq**). Configuration files (**/etc/gitlab/gitlab.rb**, **/etc/gitlab/gitlab-secrets.json**) are **not** included in the backup tarball — back them up separately or restore will fail to decrypt secrets."],["HISTORY","**gitlab-backup** is a wrapper around the **gitlab:backup:create** and **gitlab:backup:restore** Rake tasks, introduced in **GitLab 12.2** (August 2019) as the recommended interface for Omnibus installations."],["INSTALL","```pacman: sudo pacman -S gitlab```\n\n```apk: sudo apk add py3-gitlab```\n\n```nix: nix profile install nixpkgs#gitlab```"],["SEE ALSO","[gitlab-ctl](/man/gitlab-ctl)(8), [gitlab](/man/gitlab)(1)"]],"e":["gitlab-ctl","gitlab"],"x":"sudo gitlab-backup create"},"gitlab":{"n":"gitlab","t":"Python CLI for the GitLab API","s":[["TAGLINE","Python CLI for the GitLab API"],["TLDR","**List projects**\n\n```gitlab project list```\n\n**Create a project**\n\n```gitlab project create --name \"[name]\"```\n\n**List merge requests for a project**\n\n```gitlab project-merge-request list --project-id [id]```\n\n**Create a merge request**\n\n```gitlab project-merge-request create --project-id [id] --source-branch [feature] --target-branch [main] --title \"[title]\"```\n\n**Use YAML output**\n\n```gitlab -o yaml project list```\n\n**Use a specific GitLab instance from the config file**\n\n```gitlab -g [instance-name] project list```"],["SYNOPSIS","**gitlab** [_options_] _object_ _action_ [_parameters_]"],["OBJECTS","**project**\n> Manage projects.\n\n**project-merge-request**\n> Manage merge requests.\n\n**project-issue**\n> Manage issues.\n\n**user**\n> Manage users.\n\n**group**\n> Manage groups.\n\n**group-member** / **project-member**\n> Manage group/project membership."],["PARAMETERS","**--project-id** _id_\n> Project ID.\n\n**--name** _name_\n> Resource name.\n\n**--title** _title_\n> Title.\n\n**--source-branch** _branch_\n> Source branch.\n\n**--target-branch** _branch_\n> Target branch.\n\n**-o**, **--output** _FORMAT_\n> Output format: `legacy` (default), `yaml`, or `json`.\n\n**-f**, **--fields** _FIELDS_\n> Comma-separated list of fields to display.\n\n**-c**, **--config-file** _FILE_\n> Use a specific configuration file instead of the default search path.\n\n**-g**, **--gitlab** _NAME_\n> GitLab instance to use, as defined in the configuration file.\n\n**-v**, **--verbose**\n> Show detailed information about the retrieved object(s)."],["DESCRIPTION","**gitlab** is the python-gitlab CLI for interacting with the GitLab API. It manages projects, merge requests, issues, users, groups, and other GitLab resources from the command line.\n\nThe tool follows an object-action pattern, where you specify the GitLab object type and the action to perform on it (e.g. `list`, `create`, `get`, `update`, `delete`)."],["CONFIGURATION","**~/.python-gitlab.cfg**\n> User configuration file (INI format) containing GitLab instance URLs, private tokens, and default settings under a `[global]` section and one section per instance.\n\n**/etc/python-gitlab.cfg**\n> System-wide configuration file, searched before the user config."],["INSTALL","```pacman: sudo pacman -S gitlab```\n\n```apk: sudo apk add py3-gitlab```\n\n```nix: nix profile install nixpkgs#gitlab```"],["SEE ALSO","[glab](/man/glab)(1), [git](/man/git)(1)"],["RESOURCES","```[Source code](https://github.com/python-gitlab/python-gitlab)```\n\n```[Documentation](https://python-gitlab.readthedocs.io/en/stable/cli-usage.html)```"]],"e":["glab","git"],"x":"gitlab project list"},"gnome-software":{"n":"gnome-software","t":"GNOME graphical software manager","s":[["TAGLINE","GNOME graphical software manager"],["TLDR","Launch the **GNOME Software GUI**\n\n```gnome-software```\n\nLaunch and navigate to a **specific page**\n\n```gnome-software --mode [updates|updated|installed|overview]```\n\nLaunch and view **details** of a specific package\n\n```gnome-software --details [package]```\n\nDisplay **version**\n\n```gnome-software --version```"],["SYNOPSIS","**gnome-software** [_options_]"],["PARAMETERS","**--mode** _MODE_\n> Start on specific page: updates, updated, installed, overview\n\n**--details** _PACKAGE_\n> Show details for specified package\n\n**--search** _QUERY_\n> Open with search results for query\n\n**--install** _PACKAGE_\n> Prompt to install specified package\n\n**--local-filename** _FILE_\n> Install from local package file\n\n**--details-pkg** _PKGNAME_\n> Show details by package name (not app ID)"],["DESCRIPTION","**gnome-software** is GNOME's graphical application for managing software. It provides a user-friendly interface for browsing, installing, removing, and updating applications.\n\nThe application aggregates software from multiple sources including distribution repositories (via PackageKit), Flatpak, and Snap, presenting them in a unified app store interface. It also handles system and firmware updates (via fwupd)."],["CAVEATS","Requires PackageKit, Flatpak, or Snap backends to be configured. Initial startup may be slow while loading metadata. Some repository operations require authentication via PolicyKit."],["HISTORY","gnome-software was introduced in GNOME 3.10 (**2013**) as a redesigned software center, replacing the older Ubuntu Software Center on GNOME-based distributions. It has evolved to support multiple package formats and provide a modern app store experience."],["INSTALL","```apt: sudo apt install gnome-software```\n\n```dnf: sudo dnf install gnome-software```\n\n```pacman: sudo pacman -S gnome-software```\n\n```apk: sudo apk add gnome-software```\n\n```zypper: sudo zypper install gnome-software```\n\n```nix: nix profile install nixpkgs#gnome-software```"],["SEE ALSO","[flatpak](/man/flatpak)(1), [snap](/man/snap)(1), [dnf](/man/dnf)(8), [apt](/man/apt)(8)"],["RESOURCES","```[Source code](https://gitlab.gnome.org/GNOME/gnome-software)```\n\n```[GNOME Help](https://help.gnome.org/users/gnome-software/stable/)```"]],"e":["flatpak","snap","dnf","apt"],"x":"gnome-software"},"grunt":{"n":"grunt","t":"JavaScript task runner for automation","s":[["TAGLINE","JavaScript task runner for automation"],["TLDR","**Run default task**\n\n```grunt```\n\n**Run specific task**\n\n```grunt [task-name]```\n\n**Run multiple tasks in sequence**\n\n```grunt [task-one] [task-two]```\n\n**List available tasks and options**\n\n```grunt --help```\n\n**Run with verbose output**\n\n```grunt --verbose```\n\n**Force execution despite warnings**\n\n```grunt --force```\n\n**Use an alternate Gruntfile**\n\n```grunt --gruntfile [path/to/Gruntfile.js] [task-name]```\n\n**Perform a dry run without writing files**\n\n```grunt --no-write```"],["SYNOPSIS","**grunt** [_options_] [_tasks_]"],["PARAMETERS","_TASKS_\n> Tasks to execute, in order. Runs the **default** task if none is given.\n\n**-f**, **--force**\n> Continue past warnings instead of aborting.\n\n**-v**, **--verbose**\n> Verbose mode, printing more information about what Grunt is doing.\n\n**-b**, **--base** _PATH_\n> Alternate base path; file paths are relative to the Gruntfile by default.\n\n**--no-color**\n> Disable colored output.\n\n**--gruntfile** _FILE_\n> Specify an alternate Gruntfile.\n\n**--no-write**\n> Disable writing files (dry run).\n\n**-d**, **--debug**\n> Enable debugging mode for tasks that support it.\n\n**--stack**\n> Print a stack trace when exiting with a warning or fatal error.\n\n**-V**, **--version**\n> Print the Grunt version; combine with --verbose for plugin versions too.\n\n**--help**\n> Display help information."],["DESCRIPTION","**Grunt** is a JavaScript task runner for automating repetitive tasks. It handles minification, compilation, testing, and linting through a plugin ecosystem.\n\nThe tool reads configuration from Gruntfile.js. The globally-installed **grunt-cli** package provides the **grunt** command itself; it just locates and runs the version of Grunt installed locally in the project, which is what actually loads the Gruntfile and plugins. Grunt was widely used before npm scripts and newer build tools like Gulp and webpack gained popularity."],["CONFIGURATION","**Gruntfile.js**\n> Project-level configuration file defining tasks, their options, and execution order.\n\n**package.json**\n> Lists Grunt plugins as devDependencies."],["CAVEATS","Requires Node.js. Configuration can be verbose. Newer alternatives exist (Gulp, npm scripts)."],["HISTORY","Grunt was created by **Ben Alman** in **2012** as one of the first JavaScript task runners."],["INSTALL","```brew: brew install grunt```"],["SEE ALSO","[gulp](/man/gulp)(1), [npm](/man/npm)(1), [webpack](/man/webpack)(1)"],["RESOURCES","```[Source code](https://github.com/gruntjs/grunt)```\n\n```[Homepage](https://gruntjs.com/)```\n\n```[Documentation](https://gruntjs.com/getting-started)```"]],"e":["gulp","npm","webpack"],"x":"grunt"},"jira-issues":{"n":"jira-issues","t":"lists Jira issues from the command line","s":[["TAGLINE","lists Jira issues from the command line"],["TLDR","**List all issues**\n\n```jira issues```\n\n**List issues with JQL query**\n\n```jira issues --jql \"[project = PROJ AND status = Open]\"```\n\n**List issues assigned to me**\n\n```jira issues --assignee me```\n\n**List issues in JSON format**\n\n```jira issues --plain --no-headers```"],["SYNOPSIS","**jira** **issues** [_options_]"],["PARAMETERS","**--jql** _query_\n> Filter issues using JQL query.\n\n**--assignee** _user_\n> Filter by assignee.\n\n**--status** _status_\n> Filter by status.\n\n**--project** _key_\n> Filter by project.\n\n**--plain**\n> Output without formatting.\n\n**--no-headers**\n> Omit column headers."],["DESCRIPTION","**jira issues** lists Jira issues from the command line. Part of jira-cli, it supports JQL filtering to find specific issues. An alias for `jira issue list` with convenient defaults for common queries."],["SEE ALSO","[jira](/man/jira)(1), [jira-issue](/man/jira-issue)(1)"]],"e":["jira","jira-issue"],"x":"jira issues"},"kaskade":{"n":"kaskade","t":"Text user interface for Apache Kafka","s":[["TAGLINE","Text user interface for Apache Kafka"],["TLDR","**Launch kaskade** with default connection\n\n```kaskade```\n\n**Connect to a specific broker**\n\n```kaskade -b [localhost:9092]```"],["SYNOPSIS","**kaskade** [**-b** _broker_] [_options_]"],["DESCRIPTION","**kaskade** is a TUI for **Apache Kafka** that allows you to interact with and consume topics directly from the terminal. It provides features for listing topics, partitions, consumer groups, and group members, as well as viewing topic information including lag, replicas, and record counts.\n\nThe tool supports creating, editing, and deleting topics, filtering topics by name, and consuming messages with deserialization support for JSON, string, integer, long, float, boolean, double, **Avro**, **Protobuf**, and **Schema Registry** formats. Messages can be filtered by key, value, header, or partition."],["CAVEATS","Requires a running Kafka cluster. Schema Registry integration requires a running Schema Registry instance. Complex Avro/Protobuf schemas may have deserialization limitations."],["HISTORY","**kaskade** was created by **Saul Jabin** (sauljabin) and is written in **Python** using the **Textual** framework. It was designed to bring a modern, interactive Kafka browsing experience to the terminal."],["INSTALL","```brew: brew install kaskade```"]],"e":[],"x":"kaskade"},"kl":{"n":"kl","t":"Interactive Kubernetes log viewer for the terminal","s":[["TAGLINE","Interactive Kubernetes log viewer for the terminal"],["TLDR","**View logs** from the current Kubernetes context\n\n```kl```\n\n**View logs from a specific namespace**\n\n```kl -n [namespace]```\n\n**View logs matching a pattern**\n\n```kl [pattern]```"],["SYNOPSIS","**kl** [_options_] [_pattern_]"],["DESCRIPTION","**kl** is an interactive Kubernetes log viewer for the terminal. It allows you to view logs across multiple containers, pods, namespaces, and clusters, selecting containers interactively or through automatic pattern matching against names and labels.\n\nFeatures include real-time log streaming, container filtering, log archiving, clipboard copying, and fullscreen views. You can focus on logs from specific containers, filter by labels, and toggle different Kubernetes contexts to narrow down the logs you want to view."],["CAVEATS","Requires a working kubectl configuration. Log availability depends on Kubernetes cluster settings and pod log retention policies."],["HISTORY","**kl** was created by **robinovitch61** and is written in **Go**. It was designed as a focused alternative to more general-purpose Kubernetes management tools, specifically optimized for log viewing workflows."],["INSTALL","```zypper: sudo zypper install kl```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [k9s](/man/k9s)(1), [stern](/man/stern)(1)"]],"e":["kubectl","k9s","stern"],"x":"kl"},"luacheck":{"n":"luacheck","t":"static analyzer and linter for Lua","s":[["TAGLINE","static analyzer and linter for Lua"],["TLDR","**Check a Lua file**\n\n```luacheck [file.lua]```\n\n**Check all Lua files in a directory recursively**\n\n```luacheck [directory]```\n\n**Check with a specific Lua standard**\n\n```luacheck --std [lua53] [file.lua]```\n\n**Ignore specific warning codes**\n\n```luacheck --ignore [211] [file.lua]```\n\n**Check showing warning codes in output**\n\n```luacheck --codes [file.lua]```\n\n**Output in a specific format**\n\n```luacheck --formatter [TAP|JUnit|plain|visual_studio] [file.lua]```\n\n**Check multiple files in parallel**\n\n```luacheck -j [4] [file1.lua] [file2.lua]```\n\n**Check from stdin with a custom filename**\n\n```cat [file.lua] | luacheck --filename [file.lua] -```"],["SYNOPSIS","**luacheck** [_options_] _files_..."],["PARAMETERS","**--std** _std_\n> Set standard globals (lua51, lua52, lua53, lua54, luajit, ngx_lua, love, busted, min, max, none).\n\n**-g**, **--no-global**\n> Exclude warnings about global variables.\n\n**-u**, **--no-unused**\n> Exclude warnings about unused variables and values.\n\n**-r**, **--no-redefined**\n> Exclude warnings about redefined variables.\n\n**-a**, **--no-unused-args**\n> Exclude warnings about unused arguments and loop variables.\n\n**-s**, **--no-unused-secondaries**\n> Exclude warnings about unused variables set alongside used ones.\n\n**-d**, **--allow-defined**\n> Allow implicit global definitions via assignment.\n\n**-t**, **--allow-defined-top**\n> Allow implicit global definitions at top level scope only.\n\n**-i**, **--ignore** _patt_ ...\n> Filter out warnings matching patterns.\n\n**-o**, **--only** _patt_ ...\n> Show only warnings matching patterns.\n\n**-e**, **--enable** _patt_ ...\n> Re-enable warnings matching patterns.\n\n**--globals** _names_ ...\n> Add custom allowed global variables.\n\n**--read-globals** _names_ ...\n> Add read-only global variables.\n\n**--max-line-length** _length_\n> Set maximum line length (default: 120). Use **--no-max-line-length** to disable.\n\n**--max-cyclomatic-complexity** _limit_\n> Set maximum cyclomatic complexity for functions.\n\n**--formatter** _fmt_\n> Output format (default, TAP, JUnit, visual_studio, plain).\n\n**--codes**\n> Show warning codes in output.\n\n**--ranges**\n> Show column ranges for warnings.\n\n**-q**, **--quiet**\n> Suppress output for files without warnings. Use **-qq** or **-qqq** for less output.\n\n**-j**, **--jobs** _num_\n> Check files in parallel (requires LuaLanes).\n\n**--cache** [_file_]\n> Enable caching of results (default: .luacheckcache).\n\n**--config** _file_\n> Path to custom configuration file (default: .luacheckrc).\n\n**--no-config**\n> Disable configuration file loading.\n\n**--no-color**\n> Disable colorized output.\n\n**--exclude-files** _glob_ ...\n> Skip files matching these glob patterns.\n\n**--include-files** _glob_ ...\n> Only check files matching these glob patterns."],["DESCRIPTION","**luacheck** is a static analyzer and linter for Lua. It detects various issues including undefined globals, unused variables, unreachable code, and stylistic problems.\n\nluacheck supports multiple Lua versions and can be configured per-project via a **.luacheckrc** file or inline comments."],["CONFIGURATION","```lua\n-- .luacheckrc\nstd = \"lua53\"\nglobals = {\"myGlobal\"}\nignore = {\"212\"}  -- unused argument\nmax_line_length = 120\n```"],["CAVEATS","Static analysis only; won't catch runtime errors. May have false positives with dynamic code. Parallel checking with **-j** requires the LuaLanes library."],["HISTORY","luacheck was created by **Peter Melnichenko** as a comprehensive Lua static analysis tool, filling the gap of linting tools for Lua."],["INSTALL","```pacman: sudo pacman -S luacheck```\n\n```apk: sudo apk add luacheck```\n\n```brew: brew install luacheck```"],["SEE ALSO","[lua](/man/lua)(1), [luac](/man/luac)(1), [selene](/man/selene)(1), [stylua](/man/stylua)(1)"]],"e":["lua","luac","selene","stylua"],"x":"luacheck [file.lua]"},"mkinitcpio":{"n":"mkinitcpio","t":"creates initial ramdisk environments for booting the Linux kernel","s":[["TAGLINE","creates initial ramdisk environments for booting the Linux kernel"],["TLDR","Perform a **dry run** (print what would be done)\n\n```mkinitcpio```\n\nGenerate ramdisk environments based on **all existing presets**\n\n```sudo mkinitcpio -P```\n\nGenerate a ramdisk environment based on the **linux preset**\n\n```sudo mkinitcpio -p linux```\n\nGenerate a ramdisk environment based on the **linux-lts preset**\n\n```sudo mkinitcpio -p linux-lts```\n\nGenerate an initramfs image using an **alternative configuration file**\n\n```sudo mkinitcpio -c [path/to/mkinitcpio.conf] -g [path/to/initramfs.img]```\n\nGenerate an initramfs image for a **different kernel version**\n\n```sudo mkinitcpio -k [kernel_version] -g [path/to/initramfs.img]```\n\n**List all available hooks**\n\n```mkinitcpio -L```\n\nDisplay **help for a specific hook**\n\n```mkinitcpio -H [hook_name]```"],["SYNOPSIS","**mkinitcpio** [**-p** _preset_] [**-P**] [**-c** _config_] [**-g** _filename_] [**-k** _version_] [**-A** _hooks_] [**-S** _hooks_] [**-L**] [**-H** _hook_] [**-v**]"],["PARAMETERS","**-g, --generate _filename_**\n> Output CPIO image to specified file\n\n**-U, --uki _filename_**\n> Generate unified kernel image for UEFI booting\n\n**-k, --kernel _version_**\n> Specify kernel version or path; default is current running kernel\n\n**-c, --config _file_**\n> Use custom configuration file; default is /etc/mkinitcpio.conf\n\n**-p, --preset _preset_**\n> Build from preset in /etc/mkinitcpio.d/\n\n**-P, --allpresets**\n> Process all available presets\n\n**-A, --addhooks _hooks_**\n> Add additional hooks (comma-separated)\n\n**-S, --skiphooks _hooks_**\n> Skip specified hooks during build\n\n**-L, --listhooks**\n> List all available hooks\n\n**-H, --hookhelp _hookname_**\n> Display documentation for specified hook\n\n**-d, --generatedir _dir_**\n> Set initramfs build location\n\n**-t, --builddir _tmpdir_**\n> Use alternate temporary directory for build\n\n**-z, --compress _method_**\n> Override compression program (gzip, xz, lz4, zstd)\n\n**-s, --save**\n> Retain build directory for debugging\n\n**-v, --verbose**\n> Display detailed build information"],["DESCRIPTION","**mkinitcpio** creates initial ramdisk environments for booting the Linux kernel. The initramfs loads early in the boot process, providing kernel modules and tools needed to mount the real root filesystem.\n\nThe tool uses hooks to modularly include features like encryption support, LVM, RAID, and filesystem drivers. Presets in /etc/mkinitcpio.d/ define standard image generation parameters for installed kernels."],["CAVEATS","Incorrect hook configuration can render a system unbootable. Always keep a working kernel and initramfs as fallback. The tool requires root privileges for most operations. After modifying /etc/mkinitcpio.conf, regenerate images with -P flag."],["HISTORY","**mkinitcpio** was created by the **Arch Linux** development team as a replacement for older initramfs generation tools. It uses a modular hook-based architecture and has been the standard initramfs generator for Arch Linux and derivatives since 2006."],["INSTALL","```pacman: sudo pacman -S mkinitcpio```"],["SEE ALSO","[dracut](/man/dracut)(8), [mkinitramfs](/man/mkinitramfs)(8)"]],"e":["dracut","mkinitramfs"],"x":"mkinitcpio"},"mknod":{"n":"mknod","t":"creates block devices, character devices, or FIFOs as special files","s":[["TAGLINE","creates block devices, character devices, or FIFOs as special files"],["TLDR","Create a **block device** special file\n\n```sudo mknod [path/to/device_file] b [major_device_number] [minor_device_number]```\n\nCreate a **character device** special file\n\n```sudo mknod [path/to/device_file] c [major_device_number] [minor_device_number]```\n\nCreate a **FIFO** (named pipe)\n\n```sudo mknod [path/to/device_file] p```\n\nCreate a device file with **specific permissions**\n\n```sudo mknod -m [0660] [path/to/device_file] c [major] [minor]```\n\nCreate a device file with **default SELinux context**\n\n```sudo mknod -Z [path/to/device_file] c [major] [minor]```"],["SYNOPSIS","**mknod** [_OPTION_]... _NAME_ _TYPE_ [_MAJOR_ _MINOR_]"],["PARAMETERS","**-m**, **--mode**=_MODE_\n> Set file permission bits to MODE (symbolic or numeric), not the default a=rw minus umask.\n\n**-Z**\n> Set the SELinux security context to the default type.\n\n**--context**[=_CTX_]\n> Like -Z, or if CTX is specified set the SELinux or SMACK security context to CTX.\n\n**--help**\n> Display help and exit.\n\n**--version**\n> Output version information and exit.\n\n**b**\n> Create a block (buffered) special file; MAJOR and MINOR are required.\n\n**c**, **u**\n> Create a character (unbuffered) special file; MAJOR and MINOR are required.\n\n**p**\n> Create a FIFO (named pipe); MAJOR and MINOR must be omitted."],["DESCRIPTION","**mknod** creates block devices, character devices, or FIFOs (named pipes) as special files. Device files provide an interface between user programs and device drivers in the kernel.\n\nBlock devices transfer data in fixed-size blocks and support buffering (e.g., hard drives). Character devices transfer data character by character without buffering (e.g., terminals, serial ports). FIFOs enable inter-process communication through a named file.\n\nMajor and minor numbers identify the device driver and specific device instance. These numbers can be specified in decimal, octal (prefix 0), or hexadecimal (prefix 0x)."],["CAVEATS","Modern Linux systems use udev and devtmpfs for dynamic device node creation, making manual mknod usage rare. Creating device nodes requires root privileges. Incorrect major/minor numbers can cause system instability. The /dev directory is typically mounted as devtmpfs and managed automatically."],["HISTORY","**mknod** is a standard Unix command dating back to early Unix systems. It is part of **GNU coreutils** on Linux. The command provides direct access to the mknod(2) system call for creating special files."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[mkfifo](/man/mkfifo)(1), [stat](/man/stat)(2), [mknod](/man/mknod)(2)"]],"e":["mkfifo","stat","mknod"],"x":"sudo mknod [path/to/device_file] b [major_device_number] [minor_device_number]"},"mkntfs":{"n":"mkntfs","t":"creates NTFS filesystems","s":[["TAGLINE","creates NTFS filesystems"],["TLDR","**Create NTFS filesystem**\n\n```mkntfs [/dev/sdb1]```\n\n**Quick format**\n\n```mkntfs -f [/dev/sdb1]```\n\n**Set volume label**\n\n```mkntfs -L \"[Label]\" [/dev/sdb1]```\n\n**Verbose output**\n\n```mkntfs -v [/dev/sdb1]```\n\n**Force creation**\n\n```mkntfs -F [/dev/sdb1]```\n\n**Set cluster size**\n\n```mkntfs -c [4096] [/dev/sdb1]```"],["SYNOPSIS","**mkntfs** [_options_] _device_"],["PARAMETERS","_DEVICE_\n> Block device to format.\n\n**-f**\n> Quick format.\n\n**-L** _LABEL_\n> Volume label.\n\n**-v**\n> Verbose output.\n\n**-F**\n> Force creation.\n\n**-c** _SIZE_\n> Cluster size.\n\n**--help**\n> Display help information."],["DESCRIPTION","**mkntfs** creates NTFS filesystems. NTFS is Windows' native filesystem.\n\nThe tool is part of ntfs-3g/ntfsprogs. Creates compatible NTFS volumes."],["CAVEATS","Destroys existing data. Part of ntfs-3g. Windows compatibility."],["HISTORY","mkntfs is part of **ntfs-3g** project, providing NTFS filesystem creation on Linux."],["INSTALL","```apt: sudo apt install ntfs-3g```\n\n```dnf: sudo dnf install ntfs-3g```\n\n```pacman: sudo pacman -S ntfs-3g```\n\n```apk: sudo apk add ntfs-3g-progs```\n\n```zypper: sudo zypper install ntfs-3g```\n\n```brew: brew install ntfs-3g```"],["SEE ALSO","[mkfs.ntfs](/man/mkfs.ntfs)(8), [ntfs-3g](/man/ntfs-3g)(8), [ntfsfix](/man/ntfsfix)(8)"]],"e":["mkfs.ntfs","ntfs-3g","ntfsfix"],"x":"mkntfs [/dev/sdb1]"},"mopac":{"n":"mopac","t":"Semiempirical quantum chemistry calculation program","s":[["TAGLINE","Semiempirical quantum chemistry calculation program"],["TLDR","**Run a calculation from an input file**\n\n```mopac [path/to/input_file.mop]```\n\n**Run a calculation with a .dat input file**\n\n```mopac [path/to/input_file.dat]```\n\n**Resume a calculation from an .arc archive file**\n\n```mopac [path/to/input_file.arc]```"],["SYNOPSIS","**mopac** _input_file_"],["DESCRIPTION","**MOPAC** (Molecular Orbital PACkage) is a semiempirical quantum chemistry program for computational chemistry calculations. It implements various methods based on the NDDO (Neglect of Diatomic Differential Overlap) approximation developed by Dewar and Thiel.\n\nThe program reads molecular geometry and calculation parameters from input files (.mop, .dat, or .arc) and performs electronic structure calculations, geometry optimizations, transition state searches, and property predictions. Output files (.out for main results, .arc for archival data) are written to the same directory as the input file, not the working directory.\n\nNo keywords are required; the default behavior is a geometry relaxation using the PM7 method. Common methods include PM7, PM6, AM1, and MNDO for calculating molecular properties, reaction energies, and optimized geometries."],["INPUT FILE FORMAT","Input files contain keywords on the first line, a title/comment on the second line, a blank third line, and atomic coordinates with element symbols and Cartesian or internal coordinates. Example:\n\n```\nPM7\nHF molecule optimization\n\nH 0.0 0.0 0.0\nF 0.95 0.0 0.0\n```\n\nCommon keywords include: **PM7**, **PM6**, **AM1**, **MNDO** (method selection), **CHARGE=n** (molecular charge), **EF** (transition state search), **1SCF** (single-point energy)."],["CAVEATS","Semiempirical methods are faster but less accurate than ab initio methods. Results depend heavily on the chosen method (PM7, AM1, etc.) and may not be reliable for all molecular systems. Large molecules require significant computation time."],["HISTORY","**MOPAC** was originally developed by **James J.P. Stewart** starting in the 1980s. The program has been continuously updated with new methods, with PM7 being one of the most recent parameterizations. **OpenMOPAC** provides the open-source version of the software."],["INSTALL","```apt: sudo apt install mopac```\n\n```dnf: sudo dnf install mopac```\n\n```nix: nix profile install nixpkgs#mopac```"],["SEE ALSO","[pymol](/man/pymol)(1)"]],"e":["pymol"],"x":"mopac [path/to/input_file.mop]"},"nexttrace":{"n":"nexttrace","t":"Visual traceroute with ASN, geolocation, and rich output","s":[["TAGLINE","Visual traceroute with ASN, geolocation, and rich output"],["TLDR","**Trace** a host\n\n```nexttrace [example.com]```\n\n**Fast mode** / map-related options\n\n```nexttrace --help```\n\n**JSON** output for scripting\n\n```nexttrace --json [host]```"],["SYNOPSIS","**nexttrace** [*options*] *host*"],["DESCRIPTION","**nexttrace** (NTrace) is an enhanced traceroute implementing multiple protocols with ASN lookups, geolocation, and polished terminal output. Developed as **nxtrace/NTrace-core**; the user-facing binary is typically **nexttrace**. Available via package managers and release builds for many architectures."],["PARAMETERS","*host*\n\n> Target hostname or IP.\n\nCommon flags (names vary by version): protocol selection (ICMP/TCP/UDP), max hops, parallel probes, language, and **--json**. Always prefer **nexttrace --help**."],["CAVEATS","Raw sockets/privileges may be required for some probe types. Geolocation databases can be wrong. Respect remote networks—do not abuse high-rate probing."],["INSTALL","```brew: brew install nexttrace```\n\n```nix: nix profile install nixpkgs#nexttrace```"],["SEE ALSO","[traceroute](/man/traceroute)(8), [mtr](/man/mtr)(8), [tracepath](/man/tracepath)(8)"],["RESOURCES","```[Source code](https://github.com/nxtrace/NTrace-core)```\n\n```[Homepage](https://nxtrace.org)```"]],"e":["traceroute","mtr","tracepath"],"x":"nexttrace [example.com]"},"ng-new":{"n":"ng-new","t":"creates a new Angular workspace and generates an initial application","s":[["TAGLINE","creates a new Angular workspace and generates an initial application"],["TLDR","**Create a new Angular workspace**\n\n```ng new [my-app]```\n\n**Create without prompts** using defaults\n\n```ng new [my-app] --defaults```\n\n**Create with routing enabled**\n\n```ng new [my-app] --routing```\n\n**Create with specific style format**\n\n```ng new [my-app] --style [scss|sass|less|css]```\n\n**Create without initial git repository**\n\n```ng new [my-app] --skip-git```\n\n**Create without installing dependencies**\n\n```ng new [my-app] --skip-install```\n\n**Create as standalone** application without NgModules\n\n```ng new [my-app] --standalone```\n\n**Create in dry-run mode** to preview changes\n\n```ng new [my-app] --dry-run```"],["SYNOPSIS","**ng** **new** [_name_] [_options_]"],["PARAMETERS","_NAME_\n> Name for the new workspace and initial project.\n\n**--routing**\n> Generate a routing module for the initial project.\n\n**--style** _STYLE_\n> File extension for style files (css, scss, sass, less).\n\n**--skip-git**\n> Do not initialize a git repository.\n\n**--skip-install**\n> Do not install dependency packages.\n\n**--skip-tests**\n> Do not generate spec files for the initial project.\n\n**--standalone**\n> Create an application based on the standalone API without NgModules (default since Angular 17).\n\n**--defaults**\n> Disable interactive prompts and use default values.\n\n**--dry-run**\n> Run through without making changes (preview mode).\n\n**--prefix** _PREFIX_\n> Prefix for generated selectors (default: app).\n\n**--package-manager** _PM_\n> Package manager to use (npm, yarn, pnpm, cnpm).\n\n**--directory** _DIR_\n> Directory to create the workspace in.\n\n**--inline-style**\n> Include styles inline in the component TS file.\n\n**--inline-template**\n> Include template inline in the component TS file.\n\n**--view-encapsulation** _MODE_\n> View encapsulation strategy (Emulated, None, ShadowDom).\n\n**--ssr**\n> Configure the application for Server-Side Rendering.\n\n**--create-application**\n> When false, creates an empty workspace with no initial application.\n\n**--help**\n> Display help information."],["DESCRIPTION","**ng new** creates a new Angular workspace and generates an initial application. It scaffolds the complete project structure including configuration files, source directories, and optionally installs npm dependencies.\n\nThe command sets up TypeScript configuration, Angular-specific settings in angular.json, and testing infrastructure. The generated workspace follows Angular best practices and conventions.\n\nWhen run without the **--defaults** flag, the CLI prompts for routing preferences and stylesheet format. Since Angular 17, the command generates standalone components by default."],["CAVEATS","Requires Node.js and Angular CLI installed globally (**npm install -g @angular/cli**). The workspace name must be a valid npm package name (lowercase, no spaces). Creating large workspaces with full dependency installation can take several minutes depending on network speed."],["HISTORY","The **ng new** command has been part of **Angular CLI** since its initial release in **2016**. It evolved alongside Angular, adding options for routing in Angular 4, strict mode in Angular 10, and SSR scaffolding in Angular 17. The command generates standalone components by default since Angular 17."],["SEE ALSO","[ng](/man/ng)(1), [ng-generate](/man/ng-generate)(1), [ng-serve](/man/ng-serve)(1), [ng-build](/man/ng-build)(1)"]],"e":["ng","ng-generate","ng-serve","ng-build"],"x":"ng new [my-app]"},"nm-connection-editor":{"n":"nm-connection-editor","t":"graphical editor for NetworkManager connections","s":[["TAGLINE","graphical editor for NetworkManager connections"],["TLDR","**Open connection editor**\n\n```nm-connection-editor```\n\n**Edit specific connection**\n\n```nm-connection-editor -e [connection_uuid]```\n\n**Create new connection**\n\n```nm-connection-editor -c```\n\n**Create specific type**\n\n```nm-connection-editor -t [wifi]```"],["SYNOPSIS","**nm-connection-editor** [_options_]"],["PARAMETERS","**-e**, **--edit** _uuid_\n> Edit connection by UUID.\n\n**-c**, **--create**\n> Create new connection.\n\n**-t**, **--type** _type_\n> Connection type for new.\n\n**-s**, **--show**\n> Show editor window."],["DESCRIPTION","**nm-connection-editor** is a graphical editor for NetworkManager connections. It provides a GUI for creating, editing, and managing network connections.\n\nThe editor handles wired, wireless, VPN, mobile broadband, and other connection types."],["CONNECTION TYPES","```\nethernet  - Wired connections\nwifi      - Wireless networks\nvpn       - VPN connections\nbond      - Bonded interfaces\nbridge    - Network bridges\nvlan      - VLAN configurations\n```"],["CONFIGURATION STORAGE","**/etc/NetworkManager/system-connections/**\n> Directory where connection profiles are stored as individual files."],["CAVEATS","Requires X11/Wayland display. Alternative: nmcli for CLI. Changes saved to system-connections."],["HISTORY","nm-connection-editor is part of the **NetworkManager** project developed by **Red Hat**, providing a GTK-based interface for network configuration."],["INSTALL","```apt: sudo apt install nm-connection-editor```\n\n```dnf: sudo dnf install nm-connection-editor```\n\n```pacman: sudo pacman -S nm-connection-editor```\n\n```apk: sudo apk add network-manager-applet```"],["SEE ALSO","[nmcli](/man/nmcli)(1), [nmtui](/man/nmtui)(1), [NetworkManager](/man/NetworkManager)(1)"]],"e":["nmcli","nmtui","NetworkManager"],"x":"nm-connection-editor"},"npm-it":{"n":"npm-it","t":"runs npm install followed by npm test","s":[["TAGLINE","runs npm install followed by npm test"],["TLDR","**Install dependencies and run tests**\n\n```npm it```\n\n**Install and test in a clean CI environment**\n\n```npm cit```\n\n**Show help for install-test**\n\n```npm it --help```"],["SYNOPSIS","**npm** **it** [_options_]\n\n**npm** **install-test** [_options_]"],["PARAMETERS","**--help**\n> Display help information.\n\nAny flag accepted by **npm install** may be passed and will be forwarded to the install step."],["DESCRIPTION","**npm it** is a shortcut that runs **npm install** followed by **npm test**. It is an alias for **npm install-test**, intended to quickly verify that a project installs cleanly and its test suite passes.\n\nA related command **npm cit** (alias of **npm clean-install-test**) performs a **npm ci** (clean install) followed by **npm test**, useful in CI pipelines where a reproducible install from the lockfile is required."],["CAVEATS","Both steps must succeed; if the install fails, tests are not run. If tests fail, the exit code is non-zero. Use **npm cit** in CI for clean, lockfile-based installs."],["HISTORY","npm it is a **shorthand alias** combining install and test operations."],["INSTALL","```pacman: sudo pacman -S npm```\n\n```apk: sudo apk add npm```\n\n```brew: brew install npm```"],["SEE ALSO","[npm](/man/npm)(1), [npm-install](/man/npm-install)(1), [npm-test](/man/npm-test)(1)"]],"e":["npm","npm-install","npm-test"],"x":"npm it"},"npm-name":{"n":"npm-name","t":"checks if package names are available on npm","s":[["TAGLINE","checks if package names are available on npm"],["TLDR","**Check if name available**\n\n```npm-name [package-name]```\n\n**Check multiple names**\n\n```npm-name [name1] [name2] [name3]```\n\n**Check scoped name**\n\n```npm-name [@scope/name]```"],["SYNOPSIS","**npm-name** [_names_]"],["PARAMETERS","_NAMES_\n> Package names to check.\n\n**--help**\n> Display help information."],["DESCRIPTION","**npm-name** checks if package names are available on the npm registry. It queries the npm registry API and reports whether each name is free to use or already taken. Supports scoped packages (@scope/name). Useful before publishing a new package to avoid name conflicts."],["CAVEATS","Third-party tool; install with **npm install -g npm-name-cli**. Requires network access to query the npm registry. Does not check for trademark conflicts or similar names."],["INSTALL","```pacman: sudo pacman -S npm```\n\n```apk: sudo apk add npm```\n\n```brew: brew install npm```"],["SEE ALSO","[npm](/man/npm)(1), [npm-publish](/man/npm-publish)(1)"]],"e":["npm","npm-publish"],"x":"npm-name [package-name]"},"opencode-stats":{"n":"opencode-stats","t":"Show OpenCode usage statistics","s":[["TAGLINE","Show OpenCode usage statistics"],["TLDR","**Show** token usage and cost statistics\n\n```opencode stats```"],["SYNOPSIS","**opencode stats**"],["DESCRIPTION","**opencode stats** displays token usage and cost statistics for OpenCode sessions. It shows cumulative data about tokens consumed, API calls made, and estimated costs across different AI providers. Useful for monitoring usage and budgeting API expenses."],["SEE ALSO","[opencode](/man/opencode)(1), [opencode-session](/man/opencode-session)(1), [opencode-models](/man/opencode-models)(1)"]],"e":["opencode","opencode-session","opencode-models"],"x":"opencode stats"},"openssl-crl":{"n":"openssl-crl","t":"processes Certificate Revocation Lists","s":[["TAGLINE","processes Certificate Revocation Lists"],["TLDR","**View CRL contents**\n\n```openssl crl -in [crl.pem] -text -noout```\n\n**Convert PEM to DER**\n\n```openssl crl -in [crl.pem] -outform DER -out [crl.der]```\n\n**Convert DER to PEM**\n\n```openssl crl -in [crl.der] -inform DER -out [crl.pem]```\n\n**Verify** CRL signature against a CA certificate\n\n```openssl crl -in [crl.pem] -CAfile [ca.crt] -verify```\n\n**Get CRL issuer name hash**\n\n```openssl crl -in [crl.pem] -hash -noout```\n\nDisplay CRL **fingerprint**\n\n```openssl crl -in [crl.pem] -fingerprint -noout```\n\nShow CRL **issuer** and update dates\n\n```openssl crl -in [crl.pem] -issuer -lastupdate -nextupdate -noout```"],["SYNOPSIS","**openssl crl** [_options_] [-in _file_] [-out _file_]"],["PARAMETERS","**-in** _file_\n> Input CRL.\n\n**-out** _file_\n> Output file.\n\n**-inform** _format_\n> Input format (PEM, DER).\n\n**-outform** _format_\n> Output format.\n\n**-text**\n> Print CRL in human-readable text form.\n\n**-noout**\n> Do not output the encoded version of the CRL.\n\n**-verify**\n> Verify the CRL signature. Implicitly enabled if -CApath, -CAfile, or -CAstore is specified.\n\n**-hash**\n> Output a hash of the issuer name, used to look up CRLs in a directory by issuer name.\n\n**-fingerprint**\n> Output the fingerprint of the CRL.\n\n**-issuer**\n> Output the issuer name.\n\n**-lastupdate**\n> Output the lastUpdate field.\n\n**-nextupdate**\n> Output the nextUpdate field.\n\n**-crlnumber**\n> Output the CRL number.\n\n**-nameopt** _option_\n> Specify how subject or issuer names are displayed.\n\n**-CAfile** _file_\n> CA certificate file for verification.\n\n**-CApath** _dir_\n> Directory of CA certificates for verification.\n\n**-gendelta** _file_\n> Output a comparison of the main CRL and the one specified.\n\n**-badsig**\n> Corrupt the signature before writing; useful for testing."],["DESCRIPTION","**openssl crl** processes Certificate Revocation Lists. CRLs list certificates that have been revoked before their expiration date.\n\nThe command can display, convert, and verify CRL files."],["CRL CONTENTS","```\nIssuer            - CA that issued CRL\nLast Update       - When CRL was issued\nNext Update       - When next CRL expected\nRevoked Certs     - List of serial numbers\n```"],["CAVEATS","CRLs can grow large. Consider OCSP for real-time checking. CRL must be current."],["HISTORY","CRLs are defined in X.509 standard as part of PKI infrastructure, implemented in OpenSSL since early versions."],["INSTALL","```apt: sudo apt install openssl```\n\n```dnf: sudo dnf install openssl```\n\n```pacman: sudo pacman -S openssl```\n\n```apk: sudo apk add openssl```\n\n```zypper: sudo zypper install openssl```\n\n```brew: brew install openssl```\n\n```nix: nix profile install nixpkgs#openssl```"],["SEE ALSO","[openssl-ca](/man/openssl-ca)(1), [openssl-verify](/man/openssl-verify)(1)"]],"e":["openssl-ca","openssl-verify"],"x":"openssl crl -in [crl.pem] -text -noout"},"pacman-query":{"n":"pacman-query","t":"queries the local package database on Arch Linux systems","s":[["TAGLINE","queries the local package database on Arch Linux systems"],["TLDR","**List** all installed packages\n\n```pacman -Q```\n\nList **explicitly** installed packages\n\n```pacman -Qe```\n\nFind package **owning** a file\n\n```pacman -Qo filename```\n\nShow package **information**\n\n```pacman -Qi package```\n\n**List** files owned by package\n\n```pacman -Ql package```\n\nList **orphan** packages\n\n```pacman -Qdtq```\n\nList **foreign** packages\n\n```pacman -Qm```\n\nList **upgradable** packages\n\n```pacman -Qu```"],["SYNOPSIS","**pacman -Q** [_OPTIONS_] [_PACKAGE_...]"],["DESCRIPTION","**pacman -Q** (query) queries the local package database on Arch Linux systems. It provides information about installed packages including version, dependencies, files, and installation status."],["PARAMETERS","**-Q, --query**\n> Query the local package database\n\n**-e, --explicit**\n> Filter to explicitly installed packages only\n\n**-d, --deps**\n> Filter to packages installed as dependencies\n\n**-t, --unrequired**\n> Filter to packages not required by any other package\n\n**-o, --owns** _file_\n> Search for the package that owns the specified file\n\n**-i, --info**\n> Display detailed package information\n\n**-l, --list**\n> List files owned by the package\n\n**-m, --foreign**\n> Filter to packages not found in sync databases (AUR packages)\n\n**-n, --native**\n> Filter to packages found in sync databases\n\n**-u, --upgrades**\n> Filter to out-of-date packages\n\n**-q, --quiet**\n> Show less information (package names only)"],["CAVEATS","The query operates on the local database only. Use **pacman -S** for sync database operations. Orphan packages (installed as dependencies but no longer required) can be found with **-Qdt**."],["HISTORY","**pacman** is the package manager for **Arch Linux** and its derivatives, designed for simplicity and speed."],["INSTALL","```apt: sudo apt install pacman-package-manager```\n\n```dnf: sudo dnf install pacman```\n\n```pacman: sudo pacman -S pacman```\n\n```apk: sudo apk add pacman```\n\n```nix: nix profile install nixpkgs#pacman```"],["SEE ALSO","[pacman](/man/pacman)(8), [pacman-sync](/man/pacman-sync)(8), [paru](/man/paru)(1), [yay](/man/yay)(1)"]],"e":["pacman","pacman-sync","paru","yay"],"x":"pacman -Q"},"pam_ecryptfs":{"n":"pam_ecryptfs","t":"handles ECryptfs encrypted home directories","s":[["TAGLINE","handles ECryptfs encrypted home directories"],["TLDR","**Auto-mount encrypted home**\n\n```auth optional pam_ecryptfs.so unwrap```\n\n**Session setup**\n\n```session optional pam_ecryptfs.so unwrap```\n\n**Password sync**\n\n```password optional pam_ecryptfs.so```"],["SYNOPSIS","**pam_ecryptfs.so** [_options_]"],["PARAMETERS","**unwrap**\n> Use the user's login passphrase to decrypt the wrapped mount passphrase stored in `~/.ecryptfs/wrapped-passphrase` and add it to the kernel keyring."],["DESCRIPTION","**pam_ecryptfs** is the PAM glue that makes per-user eCryptfs encrypted home directories transparent at login: when used in the `auth` and `session` stacks it inserts the user's mount passphrase into the kernel keyring (typically by unwrapping `~/.ecryptfs/wrapped-passphrase` with the login password) and then invokes the helper that mounts `~/.Private` (or the entire home) on top of `~`. On logout the session step unmounts and clears the keys.\n\nIt is the kernel-side counterpart of the **ecryptfs-utils** suite (`ecryptfs-setup-private`, `ecryptfs-mount-private`)."],["CAVEATS","Requires eCryptfs to be set up beforehand with **ecryptfs-setup-private**. The wrapped-passphrase file is only re-encrypted when the login password changes if the `password` PAM stack also calls `pam_ecryptfs.so` — otherwise password changes silently desynchronize and the user can no longer mount the home directory. eCryptfs is no longer the default in Ubuntu since 18.04 in favor of full-disk encryption (LUKS)."],["HISTORY","pam_ecryptfs enables **automatic encrypted home** directory mounting on login."],["SEE ALSO","[ecryptfs](/man/ecryptfs)(7), [ecryptfs-setup-private](/man/ecryptfs-setup-private)(1), [pam](/man/pam)(8)"]],"e":["ecryptfs","ecryptfs-setup-private","pam"],"x":"auth optional pam_ecryptfs.so unwrap"},"parcel":{"n":"parcel","t":"zero-configuration web application bundler","s":[["TAGLINE","zero-configuration web application bundler"],["TLDR","**Start development server**\n\n```parcel [index.html]```\n\n**Build for production**\n\n```parcel build [index.html]```\n\n**Specify output directory**\n\n```parcel build [index.html] --dist-dir [dist]```\n\n**Watch mode without server**\n\n```parcel watch [index.html]```\n\n**Specify port and open in browser**\n\n```parcel [index.html] --port [3000] --open```\n\n**Build only a specific target defined in package.json**\n\n```parcel build [index.html] --target [main]```\n\n**Build without minification or content hashing**\n\n```parcel build [index.html] --no-optimize --no-content-hash```"],["SYNOPSIS","**parcel** [_command_] [_options_] _entries_..."],["PARAMETERS","**build**\n> Production build and exit.\n\n**watch**\n> Watch for changes and rebuild without starting a dev server.\n\n**serve**\n> Start a development server with HMR (default command).\n\n**--dist-dir** _dir_\n> Output directory (default value for distDir in package.json targets).\n\n**-p**, **--port** _port_\n> Dev server port (also settable via PORT env var).\n\n**--host** _host_\n> Dev server host (default: all interfaces).\n\n**--open**\n> Automatically open entry in the default browser.\n\n**--lazy**\n> Defer building files until requested in the browser.\n\n**--https**\n> Enable HTTPS for the dev server.\n\n**--target** _target_\n> Build only a specific target defined in package.json. May be specified multiple times.\n\n**--public-url** _url_\n> Path prefix for absolute URLs.\n\n**--no-cache**\n> Disable reading from the filesystem cache.\n\n**--cache-dir** _dir_\n> Set cache directory (default: .parcel-cache).\n\n**--no-source-maps**\n> Disable source maps.\n\n**--no-optimize**\n> Disable optimizations such as minification.\n\n**--no-content-hash**\n> Disable content hashing of output file names.\n\n**--log-level** _level_\n> Set the log level (none, error, warn, info, verbose).\n\n**--config** _config_\n> Specify Parcel config to use (default: @parcel/config-default)."],["DESCRIPTION","**Parcel** is a zero-configuration web application bundler. It supports JavaScript, CSS, HTML, images, and other assets out of the box with automatic transforms.\n\nParcel uses worker processes for parallel builds and has a built-in dev server with Hot Module Replacement (HMR)."],["CAVEATS","Parcel 2 has a different CLI than v1. Cache is stored in .parcel-cache. Build targets are configured in package.json, not via `--target node` as in v1."],["HISTORY","Parcel was created by **Devon Govett** in **2017** as a zero-configuration alternative to webpack."],["SEE ALSO","[webpack](/man/webpack)(1), [vite](/man/vite)(1), [esbuild](/man/esbuild)(1), [rollup](/man/rollup)(1), [npm](/man/npm)(1)"]],"e":["webpack","vite","esbuild","rollup","npm"],"x":"parcel [index.html]"},"pipeform":{"n":"pipeform","t":"TUI that visualizes Terraform runtime progress","s":[["TAGLINE","TUI that visualizes Terraform runtime progress"],["TLDR","**Pipe a Terraform plan into the TUI**\n\n```terraform plan -json | pipeform```\n\n**Pipe a Terraform apply into the TUI**\n\n```terraform apply -auto-approve -json | pipeform```\n\n**Export timing data to CSV**\n\n```terraform apply -json | pipeform --time-csv=[output.csv]```"],["SYNOPSIS","_terraform_command_ **-json** | **pipeform** [_options_]"],["PARAMETERS","**--time-csv** _PATH_\n> Generate a CSV file with timing data for further analysis."],["DESCRIPTION","**pipeform** is a TUI that visualizes the progress of Terraform operations. It reads Terraform's machine-readable JSON output via a pipe and displays execution progress interactively. After execution, output variables are shown in a table with clipboard copy support."],["CAVEATS","All piped Terraform commands must include the **-json** flag. Only works with Terraform's machine-readable JSON output format."],["HISTORY","**pipeform** was created by **magodo** and is written in **Go**."],["INSTALL","```nix: nix profile install nixpkgs#pipeform```"],["SEE ALSO","[terraform](/man/terraform)(1)"]],"e":["terraform"],"x":"terraform plan -json | pipeform"},"podman":{"n":"podman","t":"Daemonless container engine for OCI containers","s":[["TAGLINE","Daemonless container engine for OCI containers"],["TLDR","**Run a container**\n\n```podman run [image]```\n\n**List running containers**\n\n```podman ps```\n\n**List all containers**\n\n```podman ps -a```\n\n**Pull an image**\n\n```podman pull [image]```\n\n**Build from Dockerfile**\n\n```podman build -t [tag] [path]```\n\n**Stop a container**\n\n```podman stop [container]```\n\n**Remove a container**\n\n```podman rm [container]```"],["SYNOPSIS","**podman** [_options_] _command_ [_args_]"],["PARAMETERS","**run**\n> Run a container.\n\n**ps**\n> List containers.\n\n**pull**\n> Pull an image.\n\n**build**\n> Build image from Containerfile.\n\n**stop**\n> Stop container.\n\n**rm**\n> Remove container.\n\n**images**\n> List images.\n\n**exec**\n> Execute in container.\n\n**-d**, **--detach**\n> Run in background."],["DESCRIPTION","**podman** is a daemonless, rootless container engine that manages OCI-compliant containers and images. Unlike Docker, it does not require a background daemon process, and containers can run entirely as an unprivileged user.\n\nThe CLI is designed to be drop-in compatible with Docker, supporting familiar commands like **run**, **build**, **pull**, **ps**, **stop**, and **rm**. Podman also supports pods (groups of containers sharing namespaces), which align with Kubernetes pod concepts. Developed by Red Hat, it uses Buildah for image building and crun/runc as the container runtime."],["CONFIGURATION","**/etc/containers/registries.conf**\n> System-wide container registry configuration including search registries and mirrors.\n\n**/etc/containers/storage.conf**\n> Storage driver and path configuration for container images and layers.\n\n**/etc/containers/containers.conf**\n> Default container runtime settings including resource limits, logging, and network configuration.\n\n**~/.config/containers/**\n> User-level overrides for rootless Podman configuration."],["CAVEATS","Rootless by default. Docker CLI compatible."],["HISTORY","Podman was created by **Red Hat** as a daemonless Docker alternative."],["INSTALL","```apt: sudo apt install podman```\n\n```dnf: sudo dnf install podman```\n\n```pacman: sudo pacman -S podman```\n\n```apk: sudo apk add podman```\n\n```zypper: sudo zypper install podman```\n\n```brew: brew install podman```\n\n```nix: nix profile install nixpkgs#podman```"],["SEE ALSO","[docker](/man/docker)(1), [buildah](/man/buildah)(1), [skopeo](/man/skopeo)(1)"]],"e":["docker","buildah","skopeo"],"x":"podman run [image]"},"ppdhtml":{"n":"ppdhtml","t":"Generate HTML from PPD printer files","s":[["TAGLINE","Generate HTML from PPD printer files"],["TLDR","**Convert PPD to HTML**\n\n```ppdhtml [file.ppd] > [output.html]```\n\n**Convert multiple files**\n\n```ppdhtml [file1.ppd] [file2.ppd]```"],["SYNOPSIS","**ppdhtml** [_options_] _file_"],["PARAMETERS","_FILE_\n> One or more PPD source (`.drv` / `.ppd`) files to render.\n\n**-D** _NAME=VALUE_\n> Define a variable for the PPD compiler (passed through to `ppdc`).\n\n**-I** _DIR_\n> Add a directory to the include search path."],["DESCRIPTION","**ppdhtml** reads a PPD or PPD compiler (`.drv`) source file and writes a human-readable HTML summary to standard output. The generated page documents the printer's options groups, UI constraints, paper sizes, resolutions, and other PPD attributes.\n\nIt is most often used while authoring printer drivers as part of the CUPS DDK toolchain — the same source files that **ppdc** compiles into installable PPDs."],["CAVEATS","Output goes only to stdout — there is no `-o` flag; redirect with `> output.html`. The CUPS DDK (which ships `ppdhtml`, `ppdc`, `ppdi`, `ppdmerge`, `ppdpo`) was deprecated upstream in CUPS 2.x and may be packaged separately (e.g., `cups-ppdc` on Debian/Ubuntu)."],["HISTORY","ppdhtml is part of **CUPS** for PPD documentation generation."],["INSTALL","```apt: sudo apt install cups-ppdc```\n\n```dnf: sudo dnf install cups-client```\n\n```apk: sudo apk add cups-client```\n\n```zypper: sudo zypper install cups-client```"],["SEE ALSO","[ppdc](/man/ppdc)(1), [ppdi](/man/ppdi)(1), [cups](/man/cups)(1)"]],"e":["ppdc","ppdi","cups"],"x":"ppdhtml [file.ppd] > [output.html]"},"ppmmix":{"n":"ppmmix","t":"Blend two PPM images together","s":[["TAGLINE","Blend two PPM images together"],["TLDR","**Mix two images**\n\n```ppmmix [factor] [image1.ppm] [image2.ppm] > [output.ppm]```\n\n**50% blend**\n\n```ppmmix 0.5 [a.ppm] [b.ppm] > [blend.ppm]```\n\n**25% from first image**\n\n```ppmmix 0.25 [a.ppm] [b.ppm] > [output.ppm]```"],["SYNOPSIS","**ppmmix** _fadefactor_ _ppmfile1_ _ppmfile2_"],["PARAMETERS","_FADEFACTOR_\n> Mixing factor (0.0-1.0). 0.0 produces entirely the first image, 1.0 produces entirely the second image.\n\n_PPMFILE1_\n> First input PPM image.\n\n_PPMFILE2_\n> Second input PPM image."],["DESCRIPTION","**ppmmix** blends two PPM images together using the specified fade factor. The factor controls the contribution of each image: 0.0 produces entirely the first image, 1.0 produces entirely the second, and values between create a proportional blend.\n\nThe fade factor is applied to brightness, not light intensity. This means a linearly increasing fade factor between a black and white image produces linearly increasing perceived brightness, but the actual light intensity increases faster at the bright end. For light-intensity-based mixing, use **pamcomp** instead.\n\nBoth input images must have the same dimensions and the same maxval. Part of the Netpbm toolkit."],["CAVEATS","Both images must have the same dimensions and maxval. There are no ppmmix-specific options; it only recognizes common libnetpbm options."],["HISTORY","ppmmix was created as part of **Netpbm** for image blending."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[ppmfade](/man/ppmfade)(1), [pamcomp](/man/pamcomp)(1), [ppm](/man/ppm)(5)"]],"e":["ppmfade","pamcomp","ppm"],"x":"ppmmix [factor] [image1.ppm] [image2.ppm] > [output.ppm]"},"ppmpat":{"n":"ppmpat","t":"Generate decorative pattern images","s":[["TAGLINE","Generate decorative pattern images"],["TLDR","**Create checkerboard pattern**\n\n```ppmpat -gingham2 [100] [100] > [pattern.ppm]```\n\n**Create tartan pattern**\n\n```ppmpat -tartan [200] [200] > [tartan.ppm]```\n\n**Create argyle pattern**\n\n```ppmpat -argyle1 [150] [150] > [argyle.ppm]```"],["SYNOPSIS","**ppmpat** _pattern_ _width_ _height_"],["PARAMETERS","**-gingham2**\n> Gingham checkerboard.\n\n**-gingham3**\n> 3-color gingham.\n\n**-tartan**\n> Tartan plaid.\n\n**-argyle1**\n> Argyle pattern.\n\n**-argyle2**\n> Argyle with lines.\n\n**-poles**\n> Barber pole stripes.\n\n**-squig**\n> Squiggly lines.\n\n**-camo**\n> Camouflage pattern."],["DESCRIPTION","**ppmpat** generates various pattern images. Creates test patterns, plaids, and decorative backgrounds. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pgmramp](/man/pgmramp)(1), [ppmforge](/man/ppmforge)(1)"]],"e":["pgmramp","ppmforge"],"x":"ppmpat -gingham2 [100] [100] > [pattern.ppm]"},"proctl":{"n":"proctl","t":"Manage project licenses and language configs","s":[["TAGLINE","Manage project licenses and language configs"],["TLDR","List available **licenses**\n\n```proctl -ll```\n\nList available **languages**\n\n```proctl -lL```\n\n**Pick** license interactively\n\n```proctl -pl```\n\n**Pick** language interactively\n\n```proctl -pL```\n\n**Remove** licenses from project\n\n```proctl -r```\n\nCreate new license **template**\n\n```proctl -t```\n\n**Delete** license template\n\n```proctl -R [@license_name]```"],["SYNOPSIS","**proctl** [**-i**|**-ll**|**-lL**|**-c**|**-pl**|**-pL**|**-P** _license_|**-r**|**-sl** _query_|**-sL** _query_|**-T**|**-t**|**-R** _license_|**-C**|**-k**]"],["PARAMETERS","**-i, -initialize**\n> Set up a git repository for a project\n\n**-ll, -list-licenses**\n> List all available license templates\n\n**-lL, -list-languages**\n> List supported programming languages\n\n**-c, -current-license**\n> Display the active project license\n\n**-pl, -pick-license**\n> Interactive license selection via fzf\n\n**-pL, -pick-languages**\n> Interactive language selection via fzf\n\n**-P, -preview-license**\n> View a specific license template\n\n**-r, -remove-license**\n> Remove licenses from current project\n\n**-sl, -search-license**\n> Find licenses matching a query pattern\n\n**-sL, -search-language**\n> Find languages matching a query pattern\n\n**-T, -template-help**\n> Show template variable documentation\n\n**-t, -new-template**\n> Create a new license or language template\n\n**-R, -delete-license**\n> Remove one or more license templates from the system\n\n**-C, -new-config**\n> Generate default configuration file\n\n**-k, -check-conflict**\n> Detect conflicting license files\n\n**-h, -help**\n> Display help information"],["DESCRIPTION","**proctl** manages project licenses and language configurations. It provides templated license management with interactive selection, making it easy to add or switch licenses in projects.\n\nThe tool supports custom license templates and integrates with fzf for fuzzy selection of licenses and languages. Configuration is stored in **~/.config/license.conf** (auto-generated on first launch or with **-new-config**)."],["CAVEATS","Optionally requires fzf for interactive pick menus (**-pl**, **-pL**). License templates stored in user configuration. Custom templates need manual creation."],["HISTORY","**proctl** was created by **HeCodes2Much** (CreativeCodeCat) to streamline project license management, particularly useful for developers who work on multiple projects with different licensing requirements."],["INSTALL","```aur: yay -S proctl```"],["SEE ALSO","[licensor](/man/licensor)(1), [fzf](/man/fzf)(1)"]],"e":["licensor","fzf"],"x":"proctl -ll"},"pulsar-admin":{"n":"pulsar-admin","t":"Apache Pulsar cluster administration tool","s":[["TAGLINE","Apache Pulsar cluster administration tool"],["TLDR","**List tenants**\n\n```pulsar-admin tenants list```\n\n**List namespaces in tenant**\n\n```pulsar-admin namespaces list [tenant]```\n\n**List topics in namespace**\n\n```pulsar-admin topics list [tenant/namespace]```\n\n**Create a topic**\n\n```pulsar-admin topics create persistent://[tenant]/[namespace]/[topic]```\n\n**Get topic stats**\n\n```pulsar-admin topics stats [topic]```\n\n**Check broker status**\n\n```pulsar-admin brokers list [cluster]```\n\n**Create a tenant**\n\n```pulsar-admin tenants create [tenant]```\n\n**Set namespace retention policy**\n\n```pulsar-admin namespaces set-retention [tenant/namespace] --size [10G] --time [3d]```"],["SYNOPSIS","**pulsar-admin** _command_ [_options_]"],["ADMIN COMMANDS","**tenants** list|create|delete|get|update\n> Manage tenants (multi-tenancy).\n\n**namespaces** list|create|delete|policies\n> Manage namespaces.\n\n**topics** list|create|delete|stats|subscriptions\n> Manage topics and subscriptions.\n\n**brokers** list|leader|namespaces\n> Broker information.\n\n**clusters** list|create|delete|update\n> Cluster management.\n\n**functions** create|delete|list|status\n> Manage Pulsar Functions.\n\n**sources**/**sinks** create|delete|list\n> Manage IO connectors."],["PARAMETERS","**--admin-url** _url_\n> Pulsar admin service URL.\n\n**--auth-plugin** _class_\n> Authentication plugin class.\n\n**--auth-params** _params_\n> Authentication parameters.\n\n**--tls-trust-certs-filepath** _path_\n> Path to TLS trust certificates file."],["DESCRIPTION","**pulsar-admin** manages Apache Pulsar clusters, handling tenants, namespaces, topics, and cluster configuration. It provides administrative control over the entire Pulsar infrastructure.\n\nPulsar's multi-tenancy model organizes resources: clusters contain tenants, tenants contain namespaces, namespaces contain topics. Each level has configurable policies.\n\nTopics use the format **persistent://tenant/namespace/topic** (or **non-persistent://**). Subscriptions define how consumers receive messages: exclusive, shared, failover, or key_shared.\n\n**pulsar-admin topics stats** shows message rates, storage size, subscriptions, and producer/consumer connections. This helps monitor topic health.\n\nPulsar Functions enable lightweight stream processing directly on the broker. Sources and sinks connect external systems for data ingestion and export."],["CAVEATS","Requires proper authentication configuration for secured clusters. Topic creation may be automatic depending on broker settings. Namespace policies affect all topics within. Functions require function worker to be enabled."],["HISTORY","Apache Pulsar was originally developed at **Yahoo!** and open-sourced in **2016**. It became an Apache top-level project in **2018**. Designed for multi-tenancy and geo-replication, Pulsar separates storage (BookKeeper) from serving (brokers). It offers features like topic compaction, tiered storage, and built-in schema registry. StreamNative provides commercial support."],["SEE ALSO","[nats](/man/nats)(1), [rabbitmqctl](/man/rabbitmqctl)(1)"]],"e":["nats","rabbitmqctl"],"x":"pulsar-admin tenants list"},"qm-help":{"n":"qm-help","t":"Display help for qm subcommands","s":[["TAGLINE","Display help for qm subcommands"],["TLDR","Display **help** for a specific command\n\n```qm help create```\n\nDisplay help with **detailed** information\n\n```qm help create --verbose true```"],["SYNOPSIS","**qm help** _command_ [_options_]"],["PARAMETERS","**--verbose** _true|false_\n> Show detailed help information"],["DESCRIPTION","**qm help** displays help information for qm commands. It provides usage instructions, options, and parameter descriptions for all qm subcommands.\n\nUse verbose mode for more detailed explanations and examples."],["INSTALL","```dnf: sudo dnf install qm```"],["SEE ALSO","[qm](/man/qm)(1)"]],"e":["qm"],"x":"qm help create"},"rndc":{"n":"rndc","t":"Control BIND DNS server remotely","s":[["TAGLINE","Control BIND DNS server remotely"],["TLDR","**Reload BIND configuration**\n\n```rndc reload```\n\n**Reload specific zone**\n\n```rndc reload [example.com]```\n\n**Show server status**\n\n```rndc status```\n\n**Flush cache**\n\n```rndc flush```\n\n**Stop server**\n\n```rndc stop```"],["SYNOPSIS","**rndc** [_options_] _command_ [_args_]"],["PARAMETERS","**-s** _server_\n> Server address.\n\n**-p** _port_\n> Port number.\n\n**-k** _keyfile_\n> Key file.\n\n**-c** _config_\n> Config file."],["COMMANDS","**reload** [_zone_]\n> Reload config/zone.\n\n**status**\n> Server status.\n\n**flush** [_view_]\n> Flush cache.\n\n**stop**\n> Stop server.\n\n**reconfig**\n> Reload config only.\n\n**zonestatus** _zone_\n> Zone status."],["DESCRIPTION","**rndc** (Remote Name Daemon Control) administers the BIND DNS server. It sends commands to named to reload zones, flush cache, and perform other management tasks."],["EXAMPLES","```bash"],["Check status","rndc status"],["Reload all","rndc reload"],["Reload specific zone","rndc reload example.com"],["Flush DNS cache","rndc flush"],["Flush specific name","rndc flushname example.com"],["Reload configuration only","rndc reconfig"],["Check zone status","rndc zonestatus example.com\n```"],["CONFIGURATION","**/etc/rndc.conf**\n> Main configuration file defining the authentication key, default server address, and port for connecting to named.\n\n**/etc/rndc.key**\n> Shared HMAC key file used to authenticate rndc commands with the BIND server. Generated with **rndc-confgen**.\n\n**/etc/named.conf**\n> BIND server configuration must include a matching **controls** block with the same key to accept rndc connections."],["CAVEATS","Requires shared key with named. Default port 953. Generate key with rndc-confgen."],["HISTORY","rndc is part of **BIND** (Berkeley Internet Name Domain) by **ISC** (Internet Systems Consortium)."],["INSTALL","```apt: sudo apt install bind9-utils```\n\n```dnf: sudo dnf install bind```\n\n```pacman: sudo pacman -S bind```\n\n```apk: sudo apk add bind```\n\n```zypper: sudo zypper install bind```\n\n```brew: brew install bind```\n\n```nix: nix profile install nixpkgs#bind```"],["SEE ALSO","[named](/man/named)(8), [named.conf](/man/named.conf)(5)"]],"e":["named","named.conf"],"x":"rndc reload"},"rnm":{"n":"rnm","t":"Bulk rename files with patterns","s":[["TAGLINE","Bulk rename files with patterns"],["TLDR","**Search and replace** in filenames\n\n```rnm -ss [old] -rs [new] [directory]```\n\nUse **literal** strings (no regex)\n\n```rnm -ssf [old] -rs [new] [files]```\n\nAdd **auto-increment** index\n\n```rnm -i 1 -inc 1 -rs [_] [files]```\n\nRename from **name list** file\n\n```rnm -ns/f [names.txt] [files]```\n\nRename **files only** (not directories)\n\n```rnm -fo -ss [pattern] -rs [replacement] [files]```\n\nSort by **modification time**\n\n```rnm -s/mt -ss [pattern] -rs [replacement] [files]```\n\n**Simulate** without changes\n\n```rnm -sim -ss [pattern] -rs [replacement] [files]```\n\n**Undo** last operation\n\n```rnm -u```"],["SYNOPSIS","**rnm** [**-ss** _pattern_] [**-rs** _replacement_] [**-sim**] [**-fo**] [_files_...]"],["PARAMETERS","**-ss _pattern_**\n> Search string/regex\n\n**-ssf _string_**\n> Fixed (literal) search string\n\n**-rs _replacement_**\n> Replacement string\n\n**-i _start_**\n> Starting index for numbering\n\n**-inc _step_**\n> Index increment value\n\n**-ns/f _file_**\n> Name list file\n\n**-fo**\n> Files only (ignore directories)\n\n**-sim**\n> Simulation mode (dry run)\n\n**-u**\n> Undo last operation\n\n**-s/mt**\n> Sort by modification time"],["DESCRIPTION","**rnm** is a powerful bulk rename utility supporting regex, indexing, and name list files. It can rename files based on patterns, add sequential numbers, or use predefined name lists.\n\nThe tool includes simulation mode for previewing changes and undo functionality for reverting mistakes. It handles complex renaming scenarios with fine-grained control."],["CAVEATS","Regex mode is default; use -ssf for literal strings. Undo only works for the last operation. Test with -sim first on important files."],["HISTORY","**rnm** was created by **Md Jahidul Hamid** (neurobin) as a comprehensive bulk rename solution with features missing from simpler rename tools."],["INSTALL","```nix: nix profile install nixpkgs#rnm```"],["SEE ALSO","[rename](/man/rename)(1), [mmv](/man/mmv)(1), [prename](/man/prename)(1)"]],"e":["rename","mmv","prename"],"x":"rnm -ss [old] -rs [new] [directory]"},"ronn":{"n":"ronn","t":"Convert markdown to man pages and HTML","s":[["TAGLINE","Convert markdown to man pages and HTML"],["TLDR","**Convert to man page**\n\n```ronn [file.ronn]```\n\n**Convert to HTML**\n\n```ronn --html [file.ronn]```\n\n**Convert to both**\n\n```ronn --roff --html [file.ronn]```\n\n**Preview in browser**\n\n```ronn --server [file.ronn]```\n\n**Specify output directory**\n\n```ronn --output-dir [man/] [file.ronn]```"],["SYNOPSIS","**ronn** [_options_] _files_..."],["PARAMETERS","**--roff**\n> Generate roff (man page).\n\n**--html**\n> Generate HTML.\n\n**--fragment**\n> HTML fragment only.\n\n**--server**, **-S**\n> Preview server.\n\n**--style** _style_\n> HTML style.\n\n**--output-dir** _dir_\n> Output directory.\n\n**--manual** _name_\n> Manual name.\n\n**--organization** _org_\n> Organization name for footer.\n\n**--date** _YYYY-MM-DD_\n> Published date for footer.\n\n**-w**\n> Show troff warnings on stderr.\n\n**-W**\n> Disable troff warnings."],["DESCRIPTION","**Ronn** converts Markdown-like text to Unix man pages and HTML. It uses an extended Markdown syntax for writing manual pages in a readable format."],["EXAMPLES","```bash"],["Generate man page","ronn mycommand.1.ronn"],["Generate HTML","ronn --html mycommand.1.ronn"],["Both formats","ronn --roff --html mycommand.1.ronn"],["Preview","ronn --server mycommand.1.ronn"],["Install man page","ronn mycommand.1.ronn\nsudo cp mycommand.1 /usr/local/share/man/man1/\n```"],["RONN FORMAT","```markdown\nmycommand(1) -- short description\n=============================================\n\n## SYNOPSIS\n\n`mycommand` [options] <file>\n\n## DESCRIPTION\n\nDescription of the command.\n\n## OPTIONS\n\n  * `-h`, `--help`:\n    Show help message.\n```"],["CAVEATS","Ruby gem (gem install ronn-ng). Filename convention: name.section.ronn."],["HISTORY","Ronn was created by **Ryan Tomayko** to simplify writing man pages. ronn-ng is the maintained fork."],["INSTALL","```apk: sudo apk add ronn```\n\n```brew: brew install ronn```\n\n```nix: nix profile install nixpkgs#ronn```"],["SEE ALSO","[man](/man/man)(1), [pandoc](/man/pandoc)(1), [groff](/man/groff)(1)"]],"e":["man","pandoc","groff"],"x":"ronn [file.ronn]"},"rpm-ostree":{"n":"rpm-ostree","t":"Hybrid image and package system for immutable OS","s":[["TAGLINE","Hybrid image and package system for immutable OS"],["TLDR","Show **deployment status**\n\n```rpm-ostree status```\n\n**Preview** available upgrades\n\n```rpm-ostree upgrade --preview```\n\n**Upgrade** and reboot\n\n```rpm-ostree upgrade -r```\n\n**Rollback** to previous deployment\n\n```rpm-ostree rollback -r```\n\n**Install** package and reboot\n\n```rpm-ostree install [package] -r```\n\n**Install** package and apply **live** without reboot\n\n```rpm-ostree install -A [package]```\n\n**Rebase** to a different image or OS variant\n\n```rpm-ostree rebase [ostree-remote:branch]```"],["SYNOPSIS","**rpm-ostree** [**status**|**upgrade**|**rollback**|**install**|**uninstall**] [**-r**] [_options_]"],["COMMANDS","**status**\n> Show current deployments\n\n**upgrade**\n> Download and prepare upgrade\n\n**rollback**\n> Switch to previous deployment\n\n**install**\n> Layer package on base image\n\n**uninstall**\n> Remove layered package\n\n**rebase**\n> Switch to different base image\n\n**override** _subcommand_\n> Override base packages: `replace`, `remove`, `reset`\n\n**deploy**\n> Create new deployment from a specific version/branch/commit\n\n**kargs**\n> Manage kernel boot arguments\n\n**initramfs**\n> Handle client-side initramfs regeneration\n\n**cleanup**\n> Remove pending deployments and cached data\n\n**db** _subcommand_\n> RPM database operations (diff, list, version)\n\n**cancel**\n> Stop a pending transaction\n\n**apply-live**\n> Apply changes to the booted deployment without reboot\n\n**usroverlay**\n> Create a transient writable overlay over /usr for temporary testing"],["PARAMETERS","**-r, --reboot**\n> Reboot after operation\n\n**--preview**\n> Show what would be upgraded\n\n**--check**\n> Check for updates only\n\n**--cache-only**\n> Use only cached data\n\n**--idempotent**\n> Skip operation if the request is already applied\n\n**--allow-inactive**\n> Permit packages that are already in the base layer\n\n**--apply-live**, **-A**\n> Apply changes to the booted deployment without reboot (install/uninstall only)\n\n**--force-replacefiles**\n> Allow overwriting files owned by other packages when installing\n\n**--download-only**\n> Fetch packages without deploying\n\n**-n**, **--dry-run**\n> Preview changes without executing\n\n**--json**\n> Output in JSON format (status command)"],["DESCRIPTION","**rpm-ostree** combines OSTree image-based deployment with RPM package layering. It manages immutable base images while allowing package customization, used by Fedora Silverblue, CoreOS, and similar systems.\n\nChanges create new deployments rather than modifying the running system. Multiple deployments can coexist, enabling easy rollback to previous states."],["CAVEATS","Changes require reboot to take effect. Base system is immutable. Package layering has overhead. Some packages may conflict with immutable model."],["HISTORY","**rpm-ostree** was developed by Red Hat to combine the reliability of OSTree image deployment with the flexibility of RPM package management for container-focused and immutable desktop distributions."],["INSTALL","```dnf: sudo dnf install rpm-ostree```\n\n```nix: nix profile install nixpkgs#rpm-ostree```"],["SEE ALSO","[ostree](/man/ostree)(1), [rpm](/man/rpm)(8), [flatpak](/man/flatpak)(1)"]],"e":["ostree","rpm","flatpak"],"x":"rpm-ostree status"},"scw":{"n":"scw","t":"Scaleway cloud infrastructure CLI","s":[["TAGLINE","Scaleway cloud infrastructure CLI"],["TLDR","**Login** / configure profile\n\n```scw init```\n\n**List** instances\n\n```scw instance server list```\n\n**Create** a small instance\n\n```scw instance server create type=[DEV1-S] image=[ubuntu_noble] zone=[fr-par-1]```\n\n**Kubernetes** cluster helper\n\n```scw k8s cluster list```"],["SYNOPSIS","**scw** *product* *resource* *command* [*args*]"],["DESCRIPTION","**scw** is the official Scaleway CLI (v2) for managing cloud resources—instances, object storage, Kubernetes (Kapsule), networking, and more—from the terminal. Hierarchical commands mirror the Scaleway API. Docs: https://cli.scaleway.com/"],["PARAMETERS","**init**\n\n> Interactive configuration of access keys and default zone/region.\n\n*product* *resource* *verb*\n\n> e.g. **instance server list**, **object list**.\n\n**-o** *json*|*yaml*|*human*\n\n> Output format (common global flag).\n\n**--help**\n\n> Context-sensitive help at any command level."],["CAVEATS","Requires Scaleway API keys; store them securely. Resource names and SKUs change—check current product docs. Incurs cloud costs when creating paid resources."],["INSTALL","```pacman: sudo pacman -S scaleway-cli```\n\n```apk: sudo apk add scaleway-cli```\n\n```brew: brew install scw```\n\n```nix: nix profile install nixpkgs#scaleway-cli```"],["SEE ALSO","[aws](/man/aws)(1), [doctl](/man/doctl)(1), [kubectl](/man/kubectl)(1)"],["RESOURCES","```[Source code](https://github.com/scaleway/scaleway-cli)```\n\n```[Documentation](https://cli.scaleway.com/)```"]],"e":["aws","doctl","kubectl"],"x":"scw init"},"setfont":{"n":"setfont","t":"Load console screen fonts for Linux TTYs","s":[["TAGLINE","Load console screen fonts for Linux TTYs"],["TLDR","**Change** the terminal font\n\n```setfont [font].gz```\n\nChange font by specifying a **full path**\n\n```setfont /usr/share/kbd/consolefonts/[font.psf.gz]```\n\n**Double** the font size\n\n```setfont -d```\n\n**Reset** to the default font\n\n```setfont```\n\n**Save** the current font before changing\n\n```setfont -o [saved_font] [new_font]```\n\nSet font for a **specific console**\n\n```setfont -C /dev/tty2 [font]```"],["SYNOPSIS","**setfont** [_OPTIONS_] [_font.new_...] [**-m** _cmap_] [**-u** _umap_] [**-C** _console_]"],["PARAMETERS","**-d**, **--double**\n> Double the font size for high-density displays.\n\n**-h** _N_, **--font-height** _N_\n> Override font height (useful for partial fonts).\n\n**-N**, **--default8x**=_N_\n> Load the built-in default font of width _N_ (8xN).\n\n**-o** _file_, **--output-font**=_file_\n> Save the previous font to _file_ before loading the new one.\n\n**-O** _file_, **--output-fullfont**=_file_\n> Save the previous font together with its Unicode map to _file_.\n\n**-om** _file_, **--output-consolemap**=_file_\n> Save the current console mapping table to _file_.\n\n**-ou** _file_, **--output-unicodemap**=_file_\n> Save the current Unicode mapping table to _file_.\n\n**-m** _file_, **--consolemap**=_file_\n> Load console map or Unicode console map from _file_.\n\n**-u** _file_, **--unicodemap**=_file_\n> Load the Unicode mapping table that describes the font.\n\n**-C** _device_, **--console**=_device_\n> Set the font on the specified console device.\n\n**-f**, **--force**\n> Force-load the Unicode map even if it is empty.\n\n**-R**, **--reset**\n> Restore the console font, size, and Unicode mapping to boot-up defaults.\n\n**-v**, **--verbose**\n> Enable verbose output.\n\n**-V**, **--version**\n> Print version and exit."],["DESCRIPTION","**setfont** loads console screen fonts into the EGA/VGA character generator for Linux virtual terminals (TTYs). It can load PSF (PC Screen Font) files and optionally associate Unicode mapping tables for proper character display.\n\nFonts are typically stored in **/usr/share/kbd/consolefonts/** and come in various sizes (8x8, 8x14, 8x16). The command operates on the current console by default but can target specific TTYs with **-C**. When called without arguments, it loads a default 8x8 font."],["CAVEATS","Only works on Linux virtual consoles (TTYs), not in terminal emulators like xterm or GNOME Terminal. Requires appropriate permissions (typically root) to modify console settings. Font changes do not persist across reboots; configure in **/etc/vconsole.conf** for systemd systems or appropriate init scripts."],["HISTORY","**setfont** is part of the **kbd** (keyboard and console utilities) package, which has been a standard Linux component since the early **1990s**. The PSF font format was developed specifically for Linux console fonts. The command evolved from earlier utilities like **loadfont** and integrates Unicode support added in Linux **2.0** era."],["INSTALL","```apt: sudo apt install kbd```\n\n```dnf: sudo dnf install kbd```\n\n```pacman: sudo pacman -S kbd```\n\n```apk: sudo apk add kbd```\n\n```zypper: sudo zypper install kbd```\n\n```nix: nix profile install nixpkgs#kbd```"],["SEE ALSO","[showconsolefont](/man/showconsolefont)(8), [loadkeys](/man/loadkeys)(1), [consolechars](/man/consolechars)(8)"]],"e":["showconsolefont","loadkeys","consolechars"],"x":"setfont [font].gz"},"sfdk-build-requires":{"n":"sfdk-build-requires","t":"Manage Sailfish OS build dependencies","s":[["TAGLINE","Manage Sailfish OS build dependencies"],["TLDR","**List build dependencies**\n\n```sfdk build-requires```\n\n**Install missing dependencies**\n\n```sfdk build-requires --install```"],["SYNOPSIS","**sfdk build-requires** [_options_]"],["PARAMETERS","**--install**\n> Install missing dependencies.\n\n**--target** _name_\n> Build target."],["DESCRIPTION","**sfdk build-requires** manages build dependencies for Sailfish OS projects. Lists and installs required packages. Part of Sailfish SDK."],["SEE ALSO","[sfdk](/man/sfdk)(1), [sfdk-build-init](/man/sfdk-build-init)(1)"]],"e":["sfdk","sfdk-build-init"],"x":"sfdk build-requires"},"snmpget":{"n":"snmpget","t":"Retrieve SNMP object values from agents","s":[["TAGLINE","Retrieve SNMP object values from agents"],["TLDR","**Get a single OID value** using SNMPv2c\n\n```snmpget -v 2c -c [community] [host] [OID]```\n\n**Get system description** from a device\n\n```snmpget -v 2c -c [public] [192.168.1.1] sysDescr.0```\n\n**Get multiple OID values** in one request\n\n```snmpget -v 2c -c [community] [host] [OID1] [OID2] [OID3]```\n\n**Query using SNMPv3** with authentication\n\n```snmpget -v 3 -u [username] -l authNoPriv -a MD5 -A [password] [host] [OID]```\n\n**Query using SNMPv3** with authentication and encryption\n\n```snmpget -v 3 -u [username] -l authPriv -a SHA -A [authpass] -x AES -X [privpass] [host] [OID]```\n\n**Set timeout and retries**\n\n```snmpget -v 2c -c [community] -t [5] -r [3] [host] [OID]```"],["SYNOPSIS","**snmpget** [_options_] _agent_ _OID_ [_OID_...]"],["PARAMETERS","**-v** _version_\n> SNMP version: **1**, **2c**, or **3**.\n\n**-c** _community_\n> Community string for SNMPv1/v2c authentication.\n\n**-u** _username_\n> SNMPv3 security name (username).\n\n**-l** _level_\n> SNMPv3 security level: **noAuthNoPriv**, **authNoPriv**, or **authPriv**.\n\n**-a** _protocol_\n> SNMPv3 authentication protocol: **MD5** or **SHA**.\n\n**-A** _passphrase_\n> SNMPv3 authentication passphrase.\n\n**-x** _protocol_\n> SNMPv3 privacy protocol: **DES** or **AES**.\n\n**-X** _passphrase_\n> SNMPv3 privacy passphrase.\n\n**-t** _timeout_\n> Timeout in seconds before retransmitting. Default: 1.\n\n**-r** _retries_\n> Number of retries before giving up. Default: 5.\n\n**-p** _port_\n> UDP port of the SNMP agent. Default: 161.\n\n**-m** _MIBLIST_\n> Colon-separated list of MIB modules to load. Use **ALL** for all MIBs.\n\n**-M** _PATHLIST_\n> Colon-separated list of directories to search for MIBs.\n\n**-O** _options_\n> Output format options (e.g., **n** for numeric OIDs, **q** for quick print)."],["DESCRIPTION","**snmpget** communicates with a network entity using SNMP GET requests to retrieve specific management information. It queries one or more Object Identifiers (OIDs) and returns their current values from the SNMP agent.\n\nThe tool supports SNMPv1, SNMPv2c, and SNMPv3. SNMPv1 and v2c use community strings for simple authentication, while SNMPv3 provides robust security with authentication (MD5/SHA) and encryption (DES/AES) options.\n\nOIDs can be specified numerically (e.g., **.1.3.6.1.2.1.1.1.0**) or using MIB names (e.g., **sysDescr.0**). Scalar values require a **.0** index suffix. Without it, snmpget will return an error."],["CONFIGURATION","**/etc/snmp/snmp.conf**\n> System-wide SNMP client configuration including default version, community strings, and MIB search paths.\n\n**~/.snmp/snmp.conf**\n> Per-user SNMP client configuration overriding system defaults.\n\n**MIBS**\n> Environment variable specifying which MIB modules to load (colon-separated list, or ALL).\n\n**MIBDIRS**\n> Environment variable specifying directories to search for MIB files."],["CAVEATS","Scalar OIDs require a **.0** index suffix; omitting it is a common error. SNMPv1/v2c community strings are transmitted in plaintext and should only be used on trusted networks. For secure environments, use SNMPv3 with authentication and encryption. MIB files must be installed for symbolic OID names to work."],["HISTORY","**snmpget** is part of the **Net-SNMP** project, an open-source suite of SNMP tools and libraries. Net-SNMP evolved from the CMU SNMP implementation in the 1990s and later the UCD-SNMP project. The project provides comprehensive SNMP v1, v2c, and v3 support and is widely used for network monitoring and management on Unix-like systems."],["INSTALL","```apt: sudo apt install snmp```\n\n```apk: sudo apk add net-snmp-tools```"],["SEE ALSO","[snmpwalk](/man/snmpwalk)(1), [snmpset](/man/snmpset)(1), [snmptranslate](/man/snmptranslate)(1)"]],"e":["snmpwalk","snmpset","snmptranslate"],"x":"snmpget -v 2c -c [community] [host] [OID]"},"sq":{"n":"sq","t":"Modern Sequoia OpenPGP command-line tool","s":[["TAGLINE","Modern Sequoia OpenPGP command-line tool"],["TLDR","**Generate key**\n\n```sq key generate --userid \"[Name <email>]\"```\n\n**Encrypt file**\n\n```sq encrypt --recipient-file [key.pgp] [file]```\n\n**Decrypt file**\n\n```sq decrypt [file.pgp]```\n\n**Sign file**\n\n```sq sign [file]```\n\n**Verify signature**\n\n```sq verify --signer-file [key.pgp] [file.sig]```\n\n**Inspect packet**\n\n```sq inspect [file.pgp]```"],["SYNOPSIS","**sq** _command_ [_options_] [_args_]"],["SUBCOMMANDS","**key**\n> Generate, modify, and inspect OpenPGP keys.\n\n**encrypt**\n> Encrypt data for one or more recipients.\n\n**decrypt**\n> Decrypt data using a secret key.\n\n**sign**\n> Create an OpenPGP signature (inline, detached, or cleartext).\n\n**verify**\n> Verify an OpenPGP signature.\n\n**inspect**\n> Examine the structure of OpenPGP packets without decrypting.\n\n**cert**\n> Manage TPKs (transferable public keys / certificates).\n\n**pki**\n> Authenticate and certify keys via a Web of Trust."],["COMMON OPTIONS","**--recipient-file** _FILE_\n> Encrypt for the certificate(s) in _FILE_.\n\n**--signer-file** _FILE_\n> Use the secret key in _FILE_ to sign or verify with.\n\n**--output** _FILE_, **-o** _FILE_\n> Write output to _FILE_ instead of stdout.\n\n**--binary**\n> Emit binary OpenPGP data instead of ASCII-armored output.\n\n**--force**\n> Overwrite existing output files."],["DESCRIPTION","**sq** is the command-line interface for Sequoia PGP, a modern OpenPGP implementation written in Rust. It provides key generation, encryption, decryption, signing, and signature verification following the OpenPGP standard.\n\nThe tool supports standard PGP workflows including key pair generation with user IDs, file encryption for recipients using public keys, and detached or inline signatures. The **inspect** subcommand allows examining OpenPGP packet structures for debugging and verification purposes.\n\nSequoia PGP was developed by former GnuPG contributors as a memory-safe, modular alternative to traditional PGP implementations. sq aims to provide a cleaner command-line interface while maintaining full OpenPGP compatibility."],["CAVEATS","Different from GPG. Sequoia-specific. Some features differ."],["HISTORY","**sq** is the CLI for **Sequoia PGP**, a modern OpenPGP implementation written in Rust by former GnuPG developers."],["INSTALL","```apt: sudo apt install sq```\n\n```dnf: sudo dnf install sequoia-sq```\n\n```pacman: sudo pacman -S sequoia-sq```\n\n```apk: sudo apk add sequoia-sq```\n\n```zypper: sudo zypper install sequoia-sq```\n\n```brew: brew install sq```\n\n```nix: nix profile install nixpkgs#sq```"],["SEE ALSO","[gpg](/man/gpg)(1), [age](/man/age)(1), [rage](/man/rage)(1)"]],"e":["gpg","age","rage"],"x":"sq key generate --userid \"[Name <email>]\""},"squeak":{"n":"squeak","t":"Open-source Smalltalk virtual machine","s":[["TAGLINE","Open-source Smalltalk virtual machine"],["TLDR","**Start Squeak** with default image\n\n```squeak```\n\n**Run a specific image file**\n\n```squeak [path/to/image.image]```\n\n**Run in headless mode** (no GUI)\n\n```squeak -headless [image.image]```\n\n**Run with a specific memory size**\n\n```squeak -memory [512m] [image.image]```\n\n**Run with a specific sources file**\n\n```squeak -pathenc utf8 [image.image]```\n\n**Run with VM options**\n\n```squeak -vm-sound-null [image.image]```"],["SYNOPSIS","**squeak** [_vm-options_] [_image_] [_script-arguments_]"],["PARAMETERS","**-headless**\n> Run without a graphical display. Useful for servers and CI environments.\n\n**-memory** _size_\n> Set initial memory size (e.g., 256m, 1g).\n\n**-vm-sound-null**\n> Disable sound output.\n\n**-vm-display-null**\n> Use null display driver.\n\n**-encoding** _enc_\n> Set character encoding for file operations.\n\n**-pathenc** _enc_\n> Set path encoding (utf8, latin1).\n\n**-plugins** _path_\n> Directory containing VM plugins.\n\n**-version**\n> Display VM version information.\n\n**-help**\n> Display help information."],["ENVIRONMENT","**SQUEAK_IMAGE**\n> Default image file to use if none specified on command line.\n\n**SQUEAK_PLUGINS**\n> Directory containing VM plugins."],["DESCRIPTION","**squeak** is the virtual machine launcher for Squeak, an open-source Smalltalk programming environment. It executes Squeak image files, which contain the complete state of a Smalltalk system including all objects, classes, and the development environment.\n\nThe VM provides a portable execution environment across platforms. When started without arguments, it looks for images in **~/squeak** and **/usr/share/squeak**, or uses the image specified in the **SQUEAK_IMAGE** environment variable.\n\nSqueak includes a complete IDE with code browser, debugger, and GUI toolkit. The headless mode enables running Squeak applications as servers or in CI pipelines without graphical output. CommandShell provides Unix shell integration within the Squeak environment."],["CAVEATS","On 64-bit Linux systems, running 32-bit Squeak images requires 32-bit compatibility libraries. X11 must be installed for graphical mode. Image files are not portable between significantly different VM versions. Memory settings may need adjustment for large projects."],["HISTORY","**Squeak** was created at Apple in **1996** by Alan Kay, Dan Ingalls, and others as an open-source implementation of Smalltalk-80. The name comes from the mouse mascot. It evolved from the original Xerox PARC Smalltalk and has been continuously developed by an international community. Squeak has spawned derivative projects including Pharo and serves as a platform for educational programming tools like Scratch (its original implementation)."],["INSTALL","```nix: nix profile install nixpkgs#squeak```"],["SEE ALSO","[pharo](/man/pharo)(1), [smalltalkci](/man/smalltalkci)(1)"]],"e":["pharo","smalltalkci"],"x":"squeak"},"svgtopam":{"n":"svgtopam","t":"Convert SVG graphics to PAM raster format","s":[["TAGLINE","Convert SVG graphics to PAM raster format"],["TLDR","**Convert SVG to PAM**\n\n```svgtopam [input.svg] > [output.pam]```\n\n**Convert with specific resolution**\n\n```svgtopam -resolution [300] [input.svg] > [output.pam]```"],["SYNOPSIS","**svgtopam** [_options_] [_file_]"],["PARAMETERS","**-resolution** _dpi_\n> Output resolution.\n\n**-dimension** _WxH_\n> Output dimensions."],["DESCRIPTION","**svgtopam** converts SVG vector graphics to PAM raster format. Rasterizes SVG at specified resolution. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[rsvg-convert](/man/rsvg-convert)(1), [inkscape](/man/inkscape)(1)"]],"e":["rsvg-convert","inkscape"],"x":"svgtopam [input.svg] > [output.pam]"},"systemctl-default":{"n":"systemctl-default","t":"Enter default system target","s":[["TAGLINE","Enter default system target"],["TLDR","Enter **default** mode\n\n```systemctl default```\n\nEnter default mode **asynchronously**\n\n```systemctl default --no-block```"],["SYNOPSIS","**systemctl default** [_OPTIONS_]"],["PARAMETERS","**--no-block**\n> Return immediately without waiting for the operation to complete"],["DESCRIPTION","**systemctl default** transitions the system to the default target, typically `graphical.target` or `multi-user.target` depending on system configuration. This is equivalent to `systemctl isolate default.target`.\n\nThis command is useful for returning a system to normal operation after being in rescue or emergency mode."],["CAVEATS","The default target is determined by the symlink at `/etc/systemd/system/default.target`. Switching targets may stop services that are not required by the new target."],["HISTORY","The **default** subcommand provides a convenient way to return to normal system operation without needing to know the specific default target name."],["SEE ALSO","[systemctl](/man/systemctl)(1), [systemctl-rescue](/man/systemctl-rescue)(1), [systemctl-emergency](/man/systemctl-emergency)(1), [systemctl-get-default](/man/systemctl-get-default)(1)"]],"e":["systemctl","systemctl-rescue","systemctl-emergency","systemctl-get-default"],"x":"systemctl default"},"systemd-dissect":{"n":"systemd-dissect","t":"Inspect and mount disk images","s":[["TAGLINE","Inspect and mount disk images"],["TLDR","**Show** general image information\n\n```systemd-dissect [path/to/image.raw]```\n\n**Mount** an OS image\n\n```systemd-dissect -m [path/to/image.raw] [/mnt/image]```\n\n**Unmount** an OS image\n\n```systemd-dissect -u [/mnt/image]```\n\n**List** files in an image\n\n```systemd-dissect -l [path/to/image.raw]```\n\n**Attach** an OS image to a loopback device\n\n```systemd-dissect --attach [path/to/image.raw]```\n\n**Detach** an OS image from a loopback device\n\n```systemd-dissect --detach [path/to/device]```"],["SYNOPSIS","**systemd-dissect** [_options_] [_image_]"],["PARAMETERS","**-m, --mount**\n> Mount the image to a directory\n\n**-u, --umount**\n> Unmount a mounted image\n\n**-l, --list**\n> List files in the image\n\n**--attach**\n> Attach image to loopback device\n\n**--detach**\n> Detach image from loopback device\n\n**-x, --copy-from**\n> Copy files from image\n\n**-a, --copy-to**\n> Copy files to image\n\n**--root-hash _hash_**\n> Specify dm-verity root hash"],["DESCRIPTION","**systemd-dissect** introspects and interacts with Discoverable Disk Images (DDIs) and other OS disk images. It can mount, list, and manipulate image contents without requiring manual loopback setup.\n\nThe tool supports various image formats including raw disk images with GPT partition tables, following the Discoverable Partitions Specification for automatic partition type recognition."],["CAVEATS","Requires appropriate kernel support for filesystem types in the image. dm-verity images require the root hash to be provided. Part of the systemd suite."],["INSTALL","```apt: sudo apt install systemd-container```\n\n```dnf: sudo dnf install systemd-container```\n\n```zypper: sudo zypper install systemd-container```"],["SEE ALSO","[systemd-nspawn](/man/systemd-nspawn)(1), [losetup](/man/losetup)(8), [mount](/man/mount)(8)"]],"e":["systemd-nspawn","losetup","mount"],"x":"systemd-dissect [path/to/image.raw]"},"textsnap":{"n":"textsnap","t":"CPU-only OCR for screenshots, images, PDFs, and webpages","s":[["TAGLINE","CPU-only OCR for screenshots, images, PDFs, and webpages"],["TLDR","**OCR a local image** to a text file in ./textsnaps/\n\n```textsnap [path/to/image.png]```\n\n**OCR an image at a URL**\n\n```textsnap [https://example.com/image.png]```\n\n**OCR a webpage** (renders the page, then extracts text)\n\n```textsnap [https://example.com/article]```\n\n**OCR an image already on the clipboard**, no arguments\n\n```textsnap```\n\n**Write output to a specific file**\n\n```textsnap [image.png] -o [out.txt]```\n\n**Strip markdown** and emit plain text only\n\n```textsnap [image.png] --plaintext```\n\n**Cap the decoder** at 1024 generated tokens\n\n```textsnap [image.png] --max-tokens 1024```\n\n**Use a custom local model directory**\n\n```textsnap [image.png] --model-dir [path/to/model]```\n\n**Show progress diagnostics** on stderr\n\n```textsnap -v [image.png]```"],["SYNOPSIS","**textsnap** [_options_] [_FILE_|_URL_]"],["PARAMETERS","**-o**, **--output** _PATH_\n> Write the OCR text to _PATH_. Default: **./textsnaps/_name_\\_ocr.txt**.\n\n**-v**, **--verbose**\n> Print progress diagnostics to stderr.\n\n**--plaintext**\n> Convert the default Markdown output into plain text (no tables, no headings).\n\n**--model-dir** _PATH_\n> Use ONNX model files from _PATH_ instead of the cached download.\n\n**--max-tokens** _N_\n> Cap the decoder at _N_ generated tokens (default **2048**).\n\n**--max-pixels** _N_\n> Limit the vision encoder pixel budget per image to _N_.\n\n**--no-verify**\n> Skip SHA-256 verification of downloaded model files.\n\n**--generate-checksums**\n> Re-download the model and rewrite the checksum manifest."],["DESCRIPTION","**textsnap** is a command-line OCR utility built around the **PaddleOCR-VL-1.5** vision-language model exported to ONNX. It reads an image from a file path, a URL, or the system clipboard and writes the recognized text to a file inside **./textsnaps/**, printing only the output path on stdout so it composes cleanly in shell pipelines.\n\nThe model runs entirely on the CPU, no GPU and no cloud calls. Default output is Markdown so structure such as tables, headings, and lists is preserved; **--plaintext** flattens it for callers that only want raw text. Webpage URLs are rendered before OCR, which makes the tool usable as a \"screenshot to text\" pipeline for content that is hard to copy directly."],["INPUT MODES","**No argument**\n> Read an image from the system clipboard. The OCR text is also copied back to the clipboard.\n\n**Local file**\n> Process a local image. Supported formats: PNG, JPG, JPEG, WebP, BMP, GIF, TIFF.\n\n**Image URL**\n> Fetch and OCR a remote image.\n\n**Webpage URL**\n> Render the page and OCR the resulting screenshot."],["CONFIGURATION","**~/.cache/textsnap/**\n> Cached ONNX model files (about **890 MB**) downloaded on first use. Contains the vision encoder (q4 quantized), the autoregressive decoder (q4 quantized), token embeddings (fp32), and a **model_checksums.sha256** manifest.\n\n**./textsnaps/**\n> Default output directory, created in the current working directory on first run.\n\n**TEXTSNAP_DECODE_THREADS**\n> Environment variable that overrides the decoder thread count. Defaults to the number of physical cores."],["CAVEATS","The first invocation downloads the model bundle (about 890 MB), so be ready for the initial wait and the disk usage. The tool is CPU-only, so OCR of large images or long PDFs takes longer than GPU pipelines. Webpage OCR requires a working headless browser stack provided by the package."],["HISTORY","**textsnap** was published by **kouhxp** in 2026 under the MIT license. The bundled model, **PaddleOCR-VL-1.5**, is released by **PaddlePaddle** under the Apache-2.0 license. The CLI is distributed through PyPI and depends on **onnxruntime** and **huggingface_hub**."],["SEE ALSO","[tesseract](/man/tesseract)(1), [pdftotext](/man/pdftotext)(1), [imagemagick](/man/imagemagick)(1), [curl](/man/curl)(1)"]],"e":["tesseract","pdftotext","imagemagick","curl"],"x":"textsnap [path/to/image.png]"},"trickle":{"n":"trickle","t":"Userspace bandwidth shaper for applications","s":[["TAGLINE","Userspace bandwidth shaper for applications"],["TLDR","**Limit download speed** of a command\n\n```trickle -d [100] [command]```\n\n**Limit upload speed** of a command\n\n```trickle -u [50] [command]```\n\n**Limit both download and upload**\n\n```trickle -d [100] -u [50] [command]```\n\n**Run in standalone mode**\n\n```trickle -s -d [100] -u [50] [command]```\n\n**Limit wget download speed**\n\n```trickle -d [200] wget [https://example.com/file.iso]```\n\n**Limit scp transfer speed**\n\n```trickle -d [50] -u [50] scp [file] [user@host:/path]```"],["SYNOPSIS","**trickle** [_options_] _command_ [_args_]"],["PARAMETERS","**-d** _rate_\n> Limit download bandwidth to rate KB/s.\n\n**-u** _rate_\n> Limit upload bandwidth to rate KB/s.\n\n**-s**\n> Run in standalone mode, independent of trickled.\n\n**-w** _length_\n> Peak detection window size in KB. Lower values are more aggressive but may over-shape (default: 512).\n\n**-t** _seconds_\n> Smoothing time interval in seconds (default: 5).\n\n**-l** _length_\n> Smoothing length in KB (default: 10).\n\n**-L** _latency_\n> Set latency in milliseconds for shaping calculations.\n\n**-n** _path_\n> trickled socket path (default: /tmp/.trickled.sock).\n\n**-v**\n> Increase verbosity level. Can be specified multiple times.\n\n**-V**\n> Display version information.\n\n**-h**\n> Display help."],["DESCRIPTION","**trickle** is a lightweight userspace bandwidth shaper that limits network bandwidth consumption of individual applications. It works without root privileges or kernel modifications by intercepting socket calls.\n\nThe tool uses preloading (LD_PRELOAD) to inject bandwidth limiting into dynamically linked applications. In standalone mode (-s), each application gets its own bandwidth limits. When trickled daemon is running, bandwidth can be shared among multiple applications.\n\nUse cases include preventing a single download from saturating your connection, testing application behavior under low bandwidth, and fair bandwidth allocation among processes."],["CAVEATS","Only works with dynamically linked executables using TCP (SOCK_STREAM) sockets. Does not work with statically linked or setuid programs. UDP traffic is not affected. Standalone mode uses fixed limits; daemon mode allows shared limits via trickled."],["HISTORY","**trickle** was developed by Marius Aamodt Eriksen as a simple solution for application-level bandwidth limiting. It provides an alternative to complex traffic control solutions like tc, working entirely in userspace without system-wide configuration."],["INSTALL","```apt: sudo apt install trickle```\n\n```nix: nix profile install nixpkgs#trickle```"],["SEE ALSO","[tc](/man/tc)(1), [wondershaper](/man/wondershaper)(1), [nethogs](/man/nethogs)(1)"]],"e":["tc","wondershaper","nethogs"],"x":"trickle -d [100] [command]"},"ttsx":{"n":"ttsx","t":"execute TypeScript with full-project type checking first","s":[["TAGLINE","execute TypeScript with full-project type checking first"],["TLDR","**Run** a TypeScript entry file\n\n```ttsx src/index.ts```\n\n**Run** with arguments passed to the script\n\n```ttsx src/cli.ts -- --flag value```\n\n**Type-check then execute** via **npx**\n\n```npx ttsx src/main.ts```"],["SYNOPSIS","**ttsx** _file_ [_script_args_...]"],["DESCRIPTION","**ttsx** is the execution companion to **ttsc**. Like **tsx** or **ts-node**, it runs a TypeScript file without a separate compile step, but it type-checks the **whole project** first. If type checking fails, the process exits before any user code runs.\n\nIt uses the same **typescript-go** toolchain and plugin pipeline as **ttsc**, so compiler plugins (transforms, **typia**, lint diagnostics, and so on) apply on every run. Install with **npm install -D ttsc typescript** (the **ttsx** binary ships with the **ttsc** package) and invoke via **npx ttsx** or a package script."],["PARAMETERS","_file_\n> TypeScript entry file to type-check and execute\n\n_script_args_\n> Arguments forwarded to the running program (often after **--** when using **npx**)"],["CAVEATS","Type-checking the full project adds startup latency compared with transpile-only runners. Project configuration must be valid (**tsconfig.json** discoverable). Behavior depends on the installed **ttsc** version and any enabled plugins. For pure build/check without execution, use **ttsc** instead."],["HISTORY","Part of the **ttsc** toolchain by Jeongho Nam (**samchon**), positioned as a type-safe alternative to **tsx** and **ts-node** on top of **typescript-go**."],["SEE ALSO","[ttsc](/man/ttsc)(1), [tsx](/man/tsx)(1), [ts-node](/man/ts-node)(1), [tsc](/man/tsc)(1), [node](/man/node)(1)"],["RESOURCES","```[Source code](https://github.com/samchon/ttsc)```\n\n```[Homepage](https://ttsc.dev)```\n\n```[Documentation](https://ttsc.dev/docs/ttsc/execute/)```"]],"e":["ttsc","tsx","ts-node","tsc","node"],"x":"ttsx src/index.ts"},"usleep":{"n":"usleep","t":"Pause execution for microseconds","s":[["TAGLINE","Pause execution for microseconds"],["TLDR","**Sleep microseconds**\n\n```usleep [1000000]```\n\n**Sleep 100ms**\n\n```usleep [100000]```\n\n**Sleep 1ms**\n\n```usleep [1000]```"],["SYNOPSIS","**usleep** _microseconds_"],["PARAMETERS","_microseconds_\n> Time to sleep."],["DESCRIPTION","**usleep** pauses execution for a specified number of microseconds (millionths of a second). It provides finer timing granularity than the standard **sleep** command, which traditionally only accepted whole seconds.\n\nThe command takes a single argument representing the number of microseconds to pause. For example, usleep 500000 pauses for half a second, and usleep 1000 pauses for one millisecond.\n\nOn modern Linux systems, usleep is largely obsolete because GNU **sleep** accepts fractional seconds (e.g., sleep 0.5). The usleep command was historically provided by the initscripts package and may not be available on all distributions."],["CAVEATS","Deprecated on many systems. Use sleep instead. Not in all distros."],["HISTORY","**usleep** was provided by initscripts package for microsecond sleep. Modern sleep accepts fractional seconds."],["INSTALL","```pacman: sudo pacman -S usleep```"],["SEE ALSO","[sleep](/man/sleep)(1), [nanosleep](/man/nanosleep)(2)"]],"e":["sleep","nanosleep"],"x":"usleep [1000000]"},"verilator":{"n":"verilator","t":"Verilog to C++ simulation compiler","s":[["TAGLINE","Verilog to C++ simulation compiler"],["TLDR","**Lint Verilog**\n\n```verilator --lint-only [design.v]```\n\n**Compile to C++**\n\n```verilator --cc [design.v]```\n\n**With testbench**\n\n```verilator --cc [design.v] --exe [testbench.cpp]```\n\n**Build simulation**\n\n```verilator --cc [design.v] --exe [tb.cpp] --build```\n\n**Generate waves**\n\n```verilator --cc [design.v] --trace```\n\n**Generate SystemC** output\n\n```verilator --sc [design.v]```\n\n**Build binary** directly\n\n```verilator --binary [design.v] --exe [tb.cpp]```\n\n**Compile with parallel jobs**\n\n```verilator --cc [design.v] --exe [tb.cpp] --build -j [4]```"],["SYNOPSIS","**verilator** [_--lint-only_] [_--cc_] [_--trace_] [_options_] _files_"],["PARAMETERS","**--lint-only**\n> Lint check only.\n\n**--cc**\n> Generate C++.\n\n**--exe**\n> Create executable.\n\n**--build**\n> Compile output.\n\n**--trace**\n> Enable waveforms.\n\n**--sc**\n> Generate SystemC output.\n\n**--binary**\n> Generate C++ and build a binary executable directly.\n\n**-Wall**\n> Enable all warnings.\n\n**--top-module** _module_\n> Specify the top-level module name.\n\n**-j** _N_\n> Parallelism for build jobs.\n\n**-CFLAGS** _flags_\n> Pass flags to the C++ compiler.\n\n**-LDFLAGS** _flags_\n> Pass flags to the C++ linker."],["DESCRIPTION","**verilator** converts synthesizable Verilog and SystemVerilog designs into optimized C++ or SystemC models. The generated code compiles to native executables that run significantly faster than interpreted simulators, making it one of the fastest open-source Verilog simulators available.\n\nThe tool also functions as a lint checker with **--lint-only**, catching common coding errors and style issues without generating simulation code. Waveform tracing can be enabled with **--trace** to produce VCD files for viewing in waveform viewers like GTKWave."],["CAVEATS","Verilator is cycle-based and only supports the synthesizable subset of Verilog/SystemVerilog. It does not support all simulation constructs (e.g., delays, some system tasks). Two-state simulation only (no X/Z propagation)."],["HISTORY","**Verilator** was created by **Wilson Snyder** as the fastest Verilog simulator through C++ compilation."],["INSTALL","```apt: sudo apt install verilator```\n\n```dnf: sudo dnf install verilator```\n\n```pacman: sudo pacman -S verilator```\n\n```apk: sudo apk add verilator```\n\n```zypper: sudo zypper install verilator```\n\n```brew: brew install verilator```\n\n```nix: nix profile install nixpkgs#verilator```"],["SEE ALSO","[iverilog](/man/iverilog)(1), [gtkwave](/man/gtkwave)(1), [yosys](/man/yosys)(1)"]],"e":["iverilog","gtkwave","yosys"],"x":"verilator --lint-only [design.v]"},"visudo":{"n":"visudo","t":"Safely edit sudoers configuration","s":[["TAGLINE","Safely edit sudoers configuration"],["TLDR","**Edit sudoers file**\n\n```sudo visudo```\n\n**Edit specific file**\n\n```sudo visudo -f [/etc/sudoers.d/custom]```\n\n**Check syntax only**\n\n```sudo visudo -c```\n\n**Check specific file**\n\n```sudo visudo -c -f [/etc/sudoers.d/custom]```\n\n**Strict mode**\n\n```sudo visudo -s```"],["SYNOPSIS","**visudo** [_-c_] [_-f file_] [_-s_] [_options_]"],["PARAMETERS","**-c**\n> Enable check-only mode. The existing sudoers file will be checked for syntax errors, owner and mode.\n\n**-f** _FILE_\n> Specify an alternate sudoers file to edit or check instead of the default /etc/sudoers.\n\n**-s**\n> Enable strict checking. If an alias is used before it is defined, visudo will consider this a parse error.\n\n**-q**\n> Enable quiet mode. Details about syntax errors are not printed. Only useful when combined with -c.\n\n**-O**\n> Enforce the default ownership (user and group) of the sudoers file.\n\n**-P**\n> Enforce the default permissions (mode) of the sudoers file.\n\n**-h**\n> Show a short help message and exit.\n\n**-V**\n> Show version number and exit."],["DESCRIPTION","**visudo** safely edits the sudoers file. It locks the file, validates syntax, and prevents saving invalid configurations.\n\nSyntax validation is critical. A syntax error in sudoers can lock out all sudo access. visudo catches errors before saving.\n\nThe editor is determined by SUDO_EDITOR, VISUAL, or EDITOR environment variables. It defaults to vi.\n\nThe tool handles file locking. Multiple simultaneous edits are prevented, avoiding corruption from concurrent changes.\n\nDrop-in files in /etc/sudoers.d/ can be edited with -f. These are included by the main sudoers file.\n\nCheck mode validates existing files without editing. It's useful for verifying configuration before deployment."],["CAVEATS","Requires root privileges. Editor must be trusted. Syntax errors are caught but logic errors aren't. Keep a root shell open when editing."],["HISTORY","**visudo** has been part of **sudo** since its early development by **Todd Miller**. It provides essential safety for editing the security-critical sudoers file."],["INSTALL","```apt: sudo apt install sudo```\n\n```dnf: sudo dnf install sudo```\n\n```pacman: sudo pacman -S sudo```\n\n```apk: sudo apk add sudo```\n\n```zypper: sudo zypper install sudo```\n\n```nix: nix profile install nixpkgs#sudo```"],["SEE ALSO","[sudo](/man/sudo)(8), [su](/man/su)(1), [vi](/man/vi)(1)"]],"e":["sudo","su","vi"],"x":"sudo visudo"},"vlock":{"n":"vlock","t":"Lock Linux virtual console sessions","s":[["TAGLINE","Lock Linux virtual console sessions"],["TLDR","**Lock** current console\n\n```vlock```\n\nLock **current** via flag\n\n```vlock -c```\n\nLock **all** consoles\n\n```vlock -a```\n\nDisplay **help**\n\n```vlock -h```\n\nDisplay **version**\n\n```vlock -v```"],["SYNOPSIS","**vlock** [_OPTIONS_]"],["PARAMETERS","**-c, --current**\n> Lock only the current virtual console\n\n**-a, --all**\n> Lock all virtual consoles\n\n**-h, --help**\n> Display help information\n\n**-v, --version**\n> Display version information"],["DESCRIPTION","**vlock** locks virtual console sessions, requiring a password to unlock. It can lock either the current console or all virtual consoles on the system.\n\nThis provides physical security for text-mode console sessions, similar to screen locking in graphical environments."],["CAVEATS","Locking all consoles prevents access to any console without the password. Root can still unlock. Works only on virtual consoles, not pseudo-terminals."],["INSTALL","```apt: sudo apt install vlock```\n\n```apk: sudo apk add kbd-vlock```\n\n```nix: nix profile install nixpkgs#vlock```"],["SEE ALSO","[physlock](/man/physlock)(1)"]],"e":["physlock"],"x":"vlock"},"wodim":{"n":"wodim","t":"Record data to CDs and DVDs","s":[["TAGLINE","Record data to CDs and DVDs"],["TLDR","**Display** optical drives available\n\n```wodim --devices```\n\n**Record** an audio-only disc\n\n```wodim dev=[/dev/optical_drive] -audio [track*.cdaudio]```\n\n**Burn** a file to a disc and eject when done\n\n```wodim -eject dev=[/dev/optical_drive] -data [file.iso]```\n\n**Burn** a file using track-at-once mode\n\n```wodim -tao dev=[/dev/optical_drive] -data [file.iso]```"],["SYNOPSIS","**wodim** [_options_] _track_..."],["PARAMETERS","**dev=_device_**\n> Specify the recording device\n\n**--devices**\n> Scan for available drives\n\n**-data**\n> Write data track\n\n**-audio**\n> Write audio track(s)\n\n**-eject**\n> Eject disc after recording\n\n**-tao**\n> Use track-at-once write mode\n\n**-dao**\n> Use disc-at-once write mode\n\n**speed=_n_**\n> Set recording speed\n\n**-v**\n> Verbose output\n\n**-dummy**\n> Simulate recording (dry run)\n\n**blank=_type_**\n> Blank a rewritable disc (fast, all)"],["DESCRIPTION","**wodim** (Write Optical DIsk Media) records data to CDs and DVDs. It is a fork of cdrecord and is aliased as **cdrecord** on many systems.\n\nThe tool supports burning ISO images, audio CDs, and multi-session discs. It can also blank rewritable media and provides various write modes for different disc types."],["CAVEATS","Burning at high speeds may produce errors on some media. Some operations like blanking can cause data loss. Requires appropriate permissions for device access. Originally forked due to licensing issues with cdrecord."],["INSTALL","```apt: sudo apt install wodim```\n\n```dnf: sudo dnf install wodim```\n\n```apk: sudo apk add cdrkit```\n\n```nix: nix profile install nixpkgs#cdrkit```"],["SEE ALSO","[genisoimage](/man/genisoimage)(1), [cdrdao](/man/cdrdao)(1), [growisofs](/man/growisofs)(1)"]],"e":["genisoimage","cdrdao","growisofs"],"x":"wodim --devices"},"wpaclean":{"n":"wpaclean","t":"Extract WPA handshakes from capture files","s":[["TAGLINE","Extract WPA handshakes from capture files"],["TLDR","**Clean capture file**\n\n```wpaclean [output.cap] [input.cap]```\n\n**Clean multiple files**\n\n```wpaclean [output.cap] [file1.cap] [file2.cap]```"],["SYNOPSIS","**wpaclean** _output_ _input_ ..."],["PARAMETERS","_output_\n> Cleaned output file.\n\n_input_\n> Input capture files."],["DESCRIPTION","**wpaclean** cleans capture files to get only the 4-way handshake and a beacon. It produces cleaned output files containing only the EAPOL authentication frames needed for offline password analysis, significantly reducing file size by removing all non-handshake traffic.\n\nMultiple input capture files can be processed and combined into a single cleaned output file. The tool is part of the aircrack-ng suite and is typically used to prepare capture files before running them through aircrack-ng or hashcat for WPA/WPA2 password recovery during authorized security assessments.\n\nNote that the output file is specified **before** the input file(s) in the argument order."],["CAVEATS","Part of the aircrack-ng suite. Only extracts WPA/WPA2 4-way handshakes and beacons; all other frames are discarded. The tool has no command-line options beyond the positional arguments. Input files must be in pcap format."],["HISTORY","**wpaclean** is part of the **aircrack-ng** suite, a set of tools for 802.11 wireless network security auditing."],["INSTALL","```apt: sudo apt install aircrack-ng```\n\n```dnf: sudo dnf install aircrack-ng```\n\n```pacman: sudo pacman -S aircrack-ng```\n\n```apk: sudo apk add aircrack-ng```\n\n```zypper: sudo zypper install aircrack-ng```\n\n```brew: brew install aircrack-ng```\n\n```nix: nix profile install nixpkgs#aircrack-ng```"],["SEE ALSO","[aircrack-ng](/man/aircrack-ng)(1), [airodump-ng](/man/airodump-ng)(1), [tcpdump](/man/tcpdump)(1), [wireshark](/man/wireshark)(1), [hashcat](/man/hashcat)(1)"]],"e":["aircrack-ng","airodump-ng","tcpdump","wireshark","hashcat"],"x":"wpaclean [output.cap] [input.cap]"},"zangband":{"n":"zangband","t":"Roguelike dungeon exploration game","s":[["TAGLINE","Roguelike dungeon exploration game"],["TLDR","**Start a new game**\n\n```zangband```\n\n**Start with a specific savefile**\n\n```zangband -u [savefile]```\n\n**Start in wizard mode (debugging)**\n\n```zangband -w```"],["SYNOPSIS","**zangband** [**-n** _num_] [**-u** _name_] [**-w**] [**-v**]"],["PARAMETERS","**-n** _num_\n> Start with specified server number\n\n**-u** _name_\n> Use specified savefile name\n\n**-w**\n> Start in wizard (debug) mode\n\n**-v**\n> Enable verbose mode\n\n**--**\n> Ignore remaining options"],["PREVIEW","```\n ##.##\n #...#  ####\n #.@.####..#\n #...#  #.>#\n #####  ####\n```"],["DESCRIPTION","**ZAngband** is a single-player roguelike dungeon exploration game, a variant of Angband which itself derives from Moria and ultimately Rogue. The goal is to descend through increasingly dangerous dungeon levels, gain experience and equipment, and ultimately defeat a powerful end boss.\n\nZAngband adds numerous features to Angband including wilderness areas between towns, additional races and classes, new monsters, and expanded magic systems. The game features permadeath - when your character dies, they're gone forever.\n\nThe interface is text-based using ASCII characters to represent the dungeon, monsters, and items. Movement and actions use keyboard commands.\n\nSavefiles are stored in **~/.angband/ZAngband/save/** by default."],["CAVEATS","ZAngband is a complex game with a steep learning curve. Pressing **?** in-game provides extensive help documentation.\n\nThe game uses permadeath. Save frequently, but savefile manipulation (save-scumming) defeats the intended challenge.\n\nDevelopment of ZAngband has been largely superseded by other Angband variants like ToME, Sil, and FAangband."],["INSTALL","```apk: sudo apk add zangband```"],["SEE ALSO","[angband](/man/angband)(6), [nethack](/man/nethack)(6), [crawl](/man/crawl)(6), [tome](/man/tome)(6)"]],"e":["angband","nethack","crawl","tome"],"x":"zangband"}});
