window.LCL_BUCKET(97,{"avdmanager":{"n":"avdmanager","t":"Create and manage Android Virtual Devices","s":[["TAGLINE","Create and manage Android Virtual Devices"],["TLDR","**List all available Android Virtual Device targets**\n\n```avdmanager list target```\n\n**List all available device definitions**\n\n```avdmanager list device```\n\n**List all created AVDs**\n\n```avdmanager list avd```\n\n**Create a new AVD**\n\n```avdmanager create avd -n [avd_name] -k \"[system_image]\" -d \"[device_id]\"```\n\n**Create AVD with specific settings**\n\n```avdmanager create avd -n [avd_name] -k \"system-images;android-[34];google_apis;x86_64\" -d \"pixel_6\"```\n\n**Delete an AVD**\n\n```avdmanager delete avd -n [avd_name]```\n\n**Move/rename an AVD**\n\n```avdmanager move avd -n [old_name] -r [new_name]```"],["SYNOPSIS","**avdmanager** _command_ [_options_]"],["PARAMETERS","**list target**\n> List available Android targets for AVD creation.\n\n**list device**\n> List available device definitions (phone, tablet, TV).\n\n**list avd**\n> List all created Android Virtual Devices.\n\n**create avd**\n> Create a new Android Virtual Device.\n\n**delete avd**\n> Delete an existing AVD.\n\n**move avd**\n> Move or rename an AVD.\n\n**-n**, **--name** _name_\n> Name for the AVD.\n\n**-k**, **--package** _path_\n> System image package path (e.g. \"system-images;android-34;google_apis;x86_64\").\n\n**-d**, **--device** _id_\n> Device definition to use.\n\n**-g**, **--tag** _tag_\n> System image tag to use (e.g. google_apis, default, google_apis_playstore).\n\n**-b**, **--abi** _abi_\n> ABI to use for the AVD (e.g. x86_64, arm64-v8a). Auto-selected when the package has only one ABI.\n\n**-c**, **--sdcard** _value_\n> Path to a shared SD card image, or size of a new SD card (e.g. 512M).\n\n**-p**, **--path** _directory_\n> Custom location for AVD files.\n\n**-r**, **--rename** _name_\n> New name when moving an AVD (used with **move avd**).\n\n**-f**, **--force**\n> Force creation, overwriting existing AVD."],["DESCRIPTION","**avdmanager** is the Android SDK command-line tool for creating and managing Android Virtual Devices (AVDs). AVDs are emulator configurations that define the hardware profile and system image for Android emulator instances.\n\nEach AVD specifies a device type (phone, tablet, TV, wear), Android API level, system image (with or without Google APIs/Play Store), and hardware characteristics like screen size, RAM, and storage. These configurations are used by the Android emulator to simulate various Android devices.\n\nThe tool works alongside **sdkmanager** for downloading system images and **emulator** for running the virtual devices. AVDs are stored in **~/.android/avd/** by default."],["CAVEATS","Creating an AVD requires the corresponding system image to be installed via sdkmanager first. Hardware acceleration (KVM on Linux, HAXM on macOS/Windows) is strongly recommended for acceptable emulator performance. AVDs for different architectures (arm64, x86_64) are not interchangeable."],["HISTORY","**avdmanager** was introduced in Android SDK Tools version 25.3 (2017) as a replacement for the **android** command's AVD management functionality. It is part of Google's effort to provide focused, standalone command-line tools for Android development. The tool is distributed as part of the Android SDK command-line tools package."],["SEE ALSO","[sdkmanager](/man/sdkmanager)(1), [emulator](/man/emulator)(1), [adb](/man/adb)(1)"],["RESOURCES","```[Documentation](https://developer.android.com/tools/avdmanager)```"]],"e":["sdkmanager","emulator","adb"],"x":"avdmanager list target"},"aws-s3":{"n":"aws-s3","t":"High-level S3 file management commands","s":[["TAGLINE","High-level S3 file management commands"],["TLDR","**List all buckets** in your account\n\n```aws s3 ls```\n\n**List objects** in a bucket\n\n```aws s3 ls s3://[bucket-name]/```\n\n**Copy a local file** to S3\n\n```aws s3 cp [file.txt] s3://[bucket-name]/```\n\n**Download a file** from S3\n\n```aws s3 cp s3://[bucket-name]/[file.txt] [./local-file.txt]```\n\n**Sync a local directory** to an S3 bucket\n\n```aws s3 sync [./local-dir] s3://[bucket-name]/[prefix/]```\n\n**Move a file** to S3 (uploads then deletes the source)\n\n```aws s3 mv [file.txt] s3://[bucket-name]/```\n\n**Generate a time-limited download URL**\n\n```aws s3 presign s3://[bucket-name]/[file.txt] --expires-in [3600]```\n\n**Create a new bucket**\n\n```aws s3 mb s3://[bucket-name]```\n\n**Remove an empty bucket**\n\n```aws s3 rb s3://[bucket-name]```\n\n**Delete an object** from S3\n\n```aws s3 rm s3://[bucket-name]/[file.txt]```"],["SYNOPSIS","**aws s3** _command_ [_arguments_] [_options_]"],["DESCRIPTION","**aws s3** provides high-level commands for managing Amazon S3 buckets and objects. It offers a simplified interface for common S3 operations like copying, syncing, and listing files, abstracting away the complexity of the underlying S3 API.\n\nThe command uses two types of path arguments: **LocalPath** (absolute or relative path to local files) and **S3Uri** (S3 resource path in the form **s3://bucket-name/key**). Most commands accept a source as the first argument and an optional destination as the second.\n\nFor low-level S3 API access with full control over request parameters, use **aws s3api** instead."],["SUBCOMMANDS","**cp**\n> Copy files between local filesystem and S3, or between S3 locations\n\n**ls**\n> List S3 buckets or objects within a bucket/prefix\n\n**mb**\n> Make (create) a new S3 bucket\n\n**mv**\n> Move files between local filesystem and S3, or between S3 locations\n\n**presign**\n> Generate a pre-signed URL for time-limited access to an S3 object\n\n**rb**\n> Remove (delete) an S3 bucket\n\n**rm**\n> Remove (delete) S3 objects\n\n**sync**\n> Synchronize directories and S3 prefixes (uploads new/changed files)\n\n**website**\n> Configure S3 bucket website hosting settings"],["PARAMETERS","**--recursive**\n> Apply command recursively to all files under a directory or S3 prefix\n\n**--exclude** _pattern_\n> Exclude files matching the pattern from the operation\n\n**--include** _pattern_\n> Include files matching the pattern (used with --exclude)\n\n**--dryrun**\n> Display operations that would be performed without executing them\n\n**--acl** _value_\n> Set the access control list (private, public-read, etc.)\n\n**--storage-class** _value_\n> Specify storage class (STANDARD, REDUCED_REDUNDANCY, GLACIER, etc.)\n\n**--sse** _value_\n> Enable server-side encryption (AES256 or aws:kms)\n\n**--delete**\n> With sync, delete destination files that no longer exist in the source"],["CAVEATS","The **sync** command only copies new or modified files by default; use **--delete** to remove files in the destination that don't exist in the source. Pattern matching with **--exclude** and **--include** is order-dependent; filters are applied in the order specified. The **rb** command fails on non-empty buckets unless **--force** is used."],["HISTORY","Amazon S3 (Simple Storage Service) launched in **March 2006** as one of AWS's first services. The **aws s3** high-level CLI commands were introduced with AWS CLI version 1 in **2013** to simplify common operations that previously required multiple API calls through **s3api**. The sync command and multipart upload support have been enhanced over subsequent releases."],["INSTALL","```dnf: sudo dnf install aws```\n\n```pacman: sudo pacman -S aws-cli```\n\n```apk: sudo apk add aws-cli```\n\n```zypper: sudo zypper install aws-cli```"],["SEE ALSO","[aws](/man/aws)(1), [aws-s3api](/man/aws-s3api)(1), [aws-s3-cp](/man/aws-s3-cp)(1), [aws-s3-sync](/man/aws-s3-sync)(1)"],["RESOURCES","```[Source code](https://github.com/aws/aws-cli)```\n\n```[Homepage](https://aws.amazon.com/cli/)```\n\n```[Documentation](https://docs.aws.amazon.com/cli/latest/reference/s3/index.html)```"]],"e":["aws","aws-s3api","aws-s3-cp","aws-s3-sync"],"x":"aws s3 ls"},"awstats":{"n":"awstats","t":"Generate advanced web server statistics","s":[["TAGLINE","Generate advanced web server statistics"],["TLDR","**Update statistics** for a virtual host\n\n```awstats -config [virtualhostname] -update```\n\n**Generate HTML report** for a virtual host\n\n```awstats -config [virtualhostname] -output```\n\n**Update statistics and generate report** in one command\n\n```awstats -config [virtualhostname] -update -output```\n\n**Generate report for a specific month**\n\n```awstats -config [virtualhostname] -output -month [01] -year [2024]```\n\n**Display help** and usage information\n\n```awstats -h```"],["SYNOPSIS","**awstats** **-config** _virtualhostname_ [_OPTIONS_]"],["PARAMETERS","**-config** _virtualhostname_\n> Specify the virtual host name to select the configuration file.\n\n**-update**\n> Update statistics from the log file (default action).\n\n**-output**\n> Generate the main HTML report. Statistics are only updated if -update is also specified.\n\n**-month** _MM_\n> Specify month for report output (01-12).\n\n**-year** _YYYY_\n> Specify year for report output.\n\n**-h**\n> Display usage information."],["DESCRIPTION","**AWStats** (Advanced Web Statistics) is a powerful log file analyzer that generates advanced web, streaming, FTP, or mail server statistics in graphical web pages. It processes log files to provide detailed information about visitors, pages, hits, search engines, keywords, robots, broken links, and more.\n\nAWStats works both as a CGI script and from the command line. It uses a partial information file to efficiently process large log files incrementally. The tool supports log formats from major server software including Apache (NCSA combined/XLF/ELF or common/CLF format), IIS (W3C log format), and various proxy, streaming, and mail servers.\n\nConfiguration files are searched in order: **awstats._virtualhostname_.conf**, **awstats.conf**, or a full path. These must be located in /etc/awstats, /usr/local/etc/awstats, or /etc."],["ENVIRONMENT","**AWSTATS_FORCE_CONFIG**\n> If defined, AWStats uses this value as the config regardless of command line."],["CONFIGURATION","**/etc/awstats/awstats.conf**\n> Default global configuration file.\n\n**/etc/awstats/awstats.MODEL.conf**\n> Per-virtual-host configuration file, where MODEL is the virtual host name specified with -config.\n\n**/usr/local/etc/awstats/awstats.conf**\n> Alternative global configuration location on some systems."],["CAVEATS","AWStats requires Perl with the Encode module. The configuration file must be properly set up with log file paths and site information before running. Statistics are cumulative, so processing the same log entries multiple times will produce incorrect results unless using AWStats' built-in tracking of processed entries."],["HISTORY","AWStats was created by Laurent Destailleur and first released in **2000**. It became one of the most popular open-source web analytics tools before the rise of JavaScript-based analytics like Google Analytics. The project continues to be maintained on SourceForge and GitHub, providing server-side log analysis that doesn't require JavaScript or client-side tracking."],["INSTALL","```pacman: sudo pacman -S awstats```\n\n```nix: nix profile install nixpkgs#awstats```"],["SEE ALSO","[goaccess](/man/goaccess)(1), [apache2](/man/apache2)(8)"]],"e":["goaccess","apache2"],"x":"awstats -config [virtualhostname] -update"},"az-repos":{"n":"az-repos","t":"Manage Azure DevOps Git repositories and pull requests","s":[["TAGLINE","Manage Azure DevOps Git repositories and pull requests"],["TLDR","**List all repositories** in a project\n\n```az repos list --project [MyProject]```\n\n**Show details** of a repository\n\n```az repos show --repository [MyRepo] --project [MyProject]```\n\n**Create a new repository**\n\n```az repos create --name [MyRepo] --project [MyProject]```\n\n**Get the clone URL** of a repository\n\n```az repos show --repository [MyRepo] --project [MyProject] --query remoteUrl```\n\n**Delete a repository**\n\n```az repos delete --id [repo_id] --project [MyProject]```\n\n**List pull requests**\n\n```az repos pr list --project [MyProject]```\n\n**Create a pull request**\n\n```az repos pr create --repository [MyRepo] --source-branch [feature] --target-branch [main] --title [My PR]```\n\n**Show pull request details**\n\n```az repos pr show --id [pr_id]```\n\n**Complete (merge) a pull request**\n\n```az repos pr update --id [pr_id] --status completed```"],["SYNOPSIS","**az** **repos** _subcommand_ [_options_]"],["SUBCOMMANDS","**list**\n> List repositories.\n\n**show**\n> Show repository details.\n\n**create**\n> Create a repository.\n\n**delete**\n> Delete a repository.\n\n**update**\n> Update a repository.\n\n**pr list**\n> List pull requests.\n\n**pr create**\n> Create a pull request.\n\n**pr show**\n> Get the details of a pull request.\n\n**pr update**\n> Update a pull request (set title, description, status; complete by setting --status completed).\n\n**pr checkout**\n> Check out the PR source branch locally.\n\n**pr set-vote**\n> Vote on a pull request (approve, reject, and so on).\n\n**pr reviewer**\n> Add, list, or remove pull request reviewers.\n\n**pr work-item**\n> Link or unlink work items to a pull request.\n\n**policy**\n> Manage branch policies (approver-count, build, comment-required, and more).\n\n**ref**\n> Manage Git references (create, delete, list, lock, unlock).\n\n**import create**\n> Create a Git import request."],["PARAMETERS","**--org**, **--organization**\n> Azure DevOps organization URL, for example `https://dev.azure.com/MyOrg/`. Required unless set as a default with **az devops configure -d organization=URL** or detected from local git config.\n\n**--project**, **-p**\n> Name or ID of the project. Required unless set as a default with **az devops configure -d project=NAME**.\n\n**--repository**, **-r**\n> Name or ID of the repository.\n\n**--detect**\n> Automatically detect the organization from local git context. Accepts **true** or **false**."],["DESCRIPTION","**az repos** manages Azure Repos, the Git repository hosting service in Azure DevOps. It provides commands for managing repositories, pull requests, references, branch policies, and repository imports.\n\nThis command group is part of the **azure-devops** extension and works with Azure DevOps Services."],["CAVEATS","Part of the **azure-devops** extension (Azure CLI 2.30.0 or higher). The extension installs automatically the first time an **az repos** command runs, or install it manually with **az extension add --name azure-devops**. Set persistent defaults with **az devops configure -d organization=URL project=NAME** to avoid repeating **--org** and **--project**."],["INSTALL","```dnf: sudo dnf install azure-cli```\n\n```pacman: sudo pacman -S azure-cli```\n\n```zypper: sudo zypper install azure-cli```\n\n```brew: brew install azure-cli```\n\n```nix: nix profile install nixpkgs#azure-cli```"],["SEE ALSO","[azure-cli](/man/azure-cli)(1), [az-devops](/man/az-devops)(1), [az-pipelines](/man/az-pipelines)(1), [git](/man/git)(1)"],["RESOURCES","```[Source code](https://github.com/Azure/azure-devops-cli-extension)```\n\n```[Documentation](https://learn.microsoft.com/en-us/cli/azure/repos)```\n\n```[Homepage](https://learn.microsoft.com/en-us/cli/azure/)```"]],"e":["azure-cli","az-devops","az-pipelines","git"],"x":"az repos list --project [MyProject]"},"bash4llm":{"n":"bash4llm","t":"Bash-first CLI wrapper for OpenAI-compatible LLM APIs","s":[["TAGLINE","Bash-first CLI wrapper for OpenAI-compatible LLM APIs"],["TLDR","**Send a prompt** to the default Groq provider\n\n```bash4llm \"[prompt]\"```\n\n**Run a prompt from a file**\n\n```bash4llm -f [prompt.txt]```\n\n**Use a specific model** for one request\n\n```bash4llm -m [llama-3.3-70b-versatile] \"[prompt]\"```\n\n**Start interactive chat** mode\n\n```bash4llm --chat```\n\n**Refresh the model list** from the provider API\n\n```bash4llm --refresh-models```\n\n**Enable contextual memory** across turns in a named session\n\n```bash4llm --session [chat1] \"[prompt]\"```"],["SYNOPSIS","**bash4llm** [_options_] [_prompt_]"],["PARAMETERS","**-f** _file_\n> Read prompt text from _file_.\n\n**-m**, **--model** _model_\n> Use _model_ for this run only.\n\n**--provider** _name_\n> Select an installed provider (for example **groq** or **gemini**).\n\n**--system** _text_\n> Set the system prompt.\n\n**--temperature**, **--ture** _n_\n> Set sampling temperature between **0.0** and **2.0**.\n\n**--max** _n_\n> Limit maximum output tokens.\n\n**--session** _id_\n> Enable session memory for contextual follow-up prompts.\n\n**--session-window** [_n_]\n> Limit how many prior session turns are included (default **10**).\n\n**--stream**, **--no-stream**\n> Enable or disable streaming output.\n\n**--chat**\n> Start an interactive REPL chat session.\n\n**--dry-run**\n> Validate configuration without calling the API.\n\n**--json**, **--pretty**, **--text**, **--raw**\n> Control response output format.\n\n**--save**, **--nosave**, **--out** _path_, **--threshold** _bytes_\n> Control automatic saving of long responses.\n\n**--refresh-models**, **--list-models**, **--list-providers**\n> Manage and inspect provider model lists.\n\n**--set-default** _model_\n> Persist the default model for the active provider.\n\n**--install-extras** [_dir_]\n> Install optional provider packs, templates, and tools.\n\n**--show-config**, **--diagnostics**\n> Print active configuration or run system checks.\n\n**-h**, **--help**\n> Show help.\n\n**--version**\n> Print version and exit."],["DESCRIPTION","**bash4llm** (Bash4LLM⁺) is a single self-contained Bash script that wraps OpenAI-compatible chat completion APIs, with Groq as the default provider. It is designed to be readable, auditable, and portable across Linux, macOS, WSL, Cygwin, Termux, and BSD systems.\n\nThe script fetches model lists dynamically from the provider API rather than hardcoding them, supports streaming and non-streaming responses, and can pipe input from files or standard input. Optional extras add more providers such as Gemini, Hugging Face, and Mistral. Session support stores turn history in NDJSON files when **--session** is used, giving short-term contextual memory without keeping state by default.\n\nSecurity is a core design goal: the script avoids **eval**, never executes model output, does not use shared **/tmp** directories, and isolates temporary files with restrictive permissions. Provider modules are treated as trusted code and should live in directories owned by the user."],["CONFIGURATION","**$BASH4LLM_CONFIG_DIR/config**\n> Local settings such as model, temperature, max tokens, output format, and save threshold.\n\n**$BASH4LLM_CONFIG_DIR/model.$PROVIDER**\n> Persistent default model for a provider.\n\n**$BASH4LLM_CONFIG_DIR/ui_state/**\n> Atomic JSON metadata for external tools and optional GUI integrations.\n\n**$BASH4LLM_HISTORY_DIR/sessions/<id>.ndjson**\n> Session conversation history when **--session** is enabled.\n\n**GROQ_API_KEY**\n> API key for the default Groq provider.\n\n**BASH4LLM_TMPDIR**\n> Private temporary directory used instead of system **/tmp**.\n\nModel selection precedence is: **-m/--model**, then **model.$PROVIDER**, then provider auto-selection, then the first whitelisted model, then the legacy global config file."],["CAVEATS","Requires **bash**, **curl**, **jq**, **gawk**, and common coreutils on **PATH**. Contextual memory exists only when **--session** is supplied on every related call. Provider extras are executable shell code and must be kept in trusted directories. On Termux, file locking falls back to atomic directory locks because **flock** is often unreliable."],["EXIT CODES","**0**\n> Success.\n\n**10**\n> Missing API key.\n\n**11**\n> Invalid or non-whitelisted model.\n\n**12**\n> Network or curl failure.\n\n**14**\n> No prompt provided.\n\n**15**\n> Filesystem or temporary-file error.\n\n**16**\n> Provider HTTP or API error."],["SEE ALSO","[groq](/man/groq)(1), [curl](/man/curl)(1), [jq](/man/jq)(1)"],["RESOURCES","```[Source code](https://github.com/kamaludu/bash4llm)```"]],"e":["groq","curl","jq"],"x":"bash4llm \"[prompt]\""},"busybox":{"n":"busybox","t":"Swiss army knife of embedded Linux utilities","s":[["TAGLINE","Swiss army knife of embedded Linux utilities"],["TLDR","**List** available applets\n\n```busybox --list```\n\n**Run** specific command\n\n```busybox ls [-la]```\n\n**Create** symlinks\n\n```busybox --install -s```\n\n**Show** help and version\n\n```busybox --help```"],["SYNOPSIS","**busybox** [_applet_] [_arguments_]"],["DESCRIPTION","**busybox** combines many common Unix utilities into a single small executable. It implements lightweight versions of over 300 commands, making it ideal for embedded systems, containers, and minimal installations.\n\nThe tool is ubiquitous in embedded Linux, Android, and container images like Alpine Linux."],["PARAMETERS","**--list**\n> List available applets\n\n**--install** [**-s**]\n> Create applet symlinks\n\n**--help** [_applet_]\n> Show help for applet"],["COMMON APPLETS","Core utilities:\n- **ls, cp, mv, rm** - File operations\n- **cat, grep, sed, awk** - Text processing\n- **sh** - Shell\n- **tar, gzip** - Archives\n- **wget** - Download\n- **ping, ifconfig** - Network"],["USAGE MODES","**Direct invocation**\n```bash\nbusybox ls -la\nbusybox grep pattern file\n```\n\n**Symlinks** (most common)\n```bash"],["After busybox --install -s","ls -la  # Actually runs busybox ls\ngrep pattern file  # Actually runs busybox grep\n```\n\n**Applet mode**\n```bash"],["When busybox is symlinked as command name","/bin/ls -> busybox\n```"],["FEATURES","- Single binary (~1-2MB)\n- 300+ Unix utilities\n- POSIX compatible\n- Configurable at compile time\n- Minimal dependencies\n- Embedded-friendly"],["CAVEATS","Simplified versions (fewer features than GNU tools). Some advanced options missing. Behavior may differ slightly from full versions. Configuration frozen at compile time. Not all applets available in all builds."],["HISTORY","**BusyBox** was created by Bruce Perens in **1996** for Debian installer, becoming the standard multi-call binary for embedded Linux systems."],["INSTALL","```apt: sudo apt install busybox```\n\n```dnf: sudo dnf install busybox```\n\n```pacman: sudo pacman -S busybox```\n\n```apk: sudo apk add busybox```\n\n```zypper: sudo zypper install busybox```\n\n```nix: nix profile install nixpkgs#busybox```"],["SEE ALSO","[alpine](/man/alpine)(1), [toybox](/man/toybox)(1), [coreutils](/man/coreutils)(7)"],["RESOURCES","```[Homepage](https://busybox.net)```"]],"e":["alpine","toybox","coreutils"],"x":"busybox --list"},"carl":{"n":"carl","t":"Command-line calendar with color and iCal support","s":[["TAGLINE","Command-line calendar with color and iCal support"],["TLDR","**Display** the current month\n\n```carl```\n\n**Display** a specific month and year\n\n```carl -m [3] -y [2024]```\n\n**Display** the entire year\n\n```carl -y [2024]```\n\n**Display** with week numbers\n\n```carl -w```\n\n**Display** previous, current, and next month\n\n```carl -3```\n\n**Display** with agenda listing events\n\n```carl -a```\n\n**Display** Julian day numbers\n\n```carl -j```"],["SYNOPSIS","**carl** [_options_]"],["PARAMETERS","**-m** _MONTH_\n> Display a specific month (1-12).\n\n**-y** _YEAR_\n> Display a specific year.\n\n**-w**\n> Show ISO week numbers.\n\n**-1, --one**\n> Display single month output (default).\n\n**-3, --three**\n> Display previous, current, and next month.\n\n**-n, --months** _NUMBER_\n> Display current and following months.\n\n**-s, --sunday**\n> Display Sunday as first day of week.\n\n**-j, --julian**\n> Display Julian dates (days numbered from January 1).\n\n**-a, --agenda**\n> Display event agenda below the calendar.\n\n**--theme** _THEME_\n> Set the color theme.\n\n**--themestyletype** _TYPE_\n> Use \"dark\" or \"light\" background theme styles."],["DESCRIPTION","**carl** is a command-line calendar tool written in Rust that displays colorful, formatted calendars in the terminal. It tries to mimic various cal(1) implementations but adds enhanced features like colors and iCal support. The output uses colors and formatting for improved readability compared to the traditional **cal** command.\n\nCarl can display single months, multiple months, full years, and highlight the current date. iCal files are configured via a config file (config.toml) using `[[ical]]` sections, and event dates are highlighted in the calendar view. The `--agenda` flag lists event summaries below the calendar."],["CAVEATS","iCal support covers basic events but may not handle all recurrence rules or complex calendar features. Color output requires a terminal with ANSI color support."],["INSTALL","```brew: brew install carl```\n\n```nix: nix profile install nixpkgs#carl```"],["SEE ALSO","[cal](/man/cal)(1), [calcurse](/man/calcurse)(1), [calcure](/man/calcure)(1)"]],"e":["cal","calcurse","calcure"],"x":"carl"},"clamav":{"n":"clamav","t":"open-source antivirus toolkit","s":[["TAGLINE","open-source antivirus toolkit"],["TLDR","**Scan a file** for viruses\n\n```clamscan [path/to/file]```\n\n**Scan a directory recursively**\n\n```clamscan -r [path/to/directory]```\n\n**Scan and remove** infected files\n\n```clamscan -r --remove [path/to/directory]```\n\n**Scan and move** infected files to quarantine\n\n```clamscan -r --move=[path/to/quarantine] [path/to/directory]```\n\n**Scan with verbose output** and display infected files only\n\n```clamscan -r -i -v [path/to/directory]```\n\n**Update virus definitions** (requires freshclam)\n\n```sudo freshclam```\n\n**Scan using the daemon** for faster multiple scans\n\n```clamdscan [path/to/file]```"],["SYNOPSIS","**clamscan** [_options_] [_file/directory_]"],["PARAMETERS","**-r**, **--recursive**\n> Scan directories and their subdirectories recursively.\n\n**-i**, **--infected**\n> Only print infected files in the output.\n\n**--remove**\n> Remove infected files. Use with caution.\n\n**--move**=_DIR_\n> Move infected files to the specified quarantine directory.\n\n**--copy**=_DIR_\n> Copy infected files to the specified directory.\n\n**-v**, **--verbose**\n> Be verbose during scanning.\n\n**-l** _FILE_, **--log**=_FILE_\n> Save scan report to the specified file.\n\n**--bell**\n> Sound bell on virus detection.\n\n**--exclude**=_REGEX_\n> Exclude files matching the regular expression.\n\n**--include**=_REGEX_\n> Only scan files matching the regular expression.\n\n**--max-filesize**=_SIZE_\n> Skip files larger than the specified size (default 100M).\n\n**-h**, **--help**\n> Display help information."],["DESCRIPTION","**ClamAV** is an open-source antivirus engine for detecting trojans, viruses, malware, and other malicious threats. The suite includes **clamscan** for on-demand scanning, **clamd** as a multi-threaded daemon, **clamdscan** for daemon-based scanning, and **freshclam** for automatic signature updates.\n\nClamAV is widely used in mail gateway scanning, particularly on Unix-based systems. It supports scanning of archives (ZIP, RAR, TAR, GZIP), documents (PDF, Office formats), executables, and many other file types. The signature database is maintained by Cisco Talos and updated multiple times daily.\n\nFor high-volume scanning, running the **clamd** daemon provides significantly better performance than repeated clamscan invocations, as it keeps the signature database loaded in memory."],["CONFIGURATION","**/etc/clamav/clamd.conf**\n> Main daemon configuration including socket path, scan limits, and logging.\n\n**/etc/clamav/freshclam.conf**\n> Virus database update settings including mirror, proxy, and update frequency."],["CAVEATS","ClamAV is primarily designed for server-side scanning of incoming files rather than real-time desktop protection. Scan times can be slow on large directories. The **--remove** option permanently deletes files without confirmation. Always keep virus definitions updated with **freshclam** for effective detection."],["HISTORY","ClamAV was created by Tomasz Kojm in **2001** as a GPL-licensed antivirus toolkit for Unix. It gained widespread adoption for mail server scanning. Sourcefire acquired the project in **2007**, and after Cisco acquired Sourcefire in **2013**, the project continues under Cisco Talos security intelligence."],["INSTALL","```dnf: sudo dnf install clamav```\n\n```pacman: sudo pacman -S clamav```\n\n```zypper: sudo zypper install clamav```\n\n```brew: brew install clamav```\n\n```nix: nix profile install nixpkgs#clamav```"],["SEE ALSO","[freshclam](/man/freshclam)(1), [clamd](/man/clamd)(8), [clamdscan](/man/clamdscan)(1)"]],"e":["freshclam","clamd","clamdscan"],"x":"clamscan [path/to/file]"},"crackle":{"n":"crackle","t":"crack Bluetooth Low Energy encryption","s":[["TAGLINE","crack Bluetooth Low Energy encryption"],["TLDR","**Crack BLE encryption** from pcap file\n\n```crackle -i [capture.pcap]```\n\n**Crack with specific LTK**\n\n```crackle -i [capture.pcap] -l [ltk_hex]```\n\n**Output decrypted traffic**\n\n```crackle -i [capture.pcap] -o [decrypted.pcap]```"],["SYNOPSIS","**crackle** [_options_] **-i** _input.pcap_"],["PARAMETERS","**-i** _file_\n> Input pcap file with BLE traffic.\n\n**-o** _file_\n> Output file for decrypted traffic.\n\n**-l** _ltk_\n> Long Term Key in hexadecimal."],["DESCRIPTION","**crackle** cracks Bluetooth Low Energy (BLE) encryption. It exploits weaknesses in the BLE pairing process to recover encryption keys and decrypt captured traffic.\n\nRequires captured BLE packets including the pairing exchange. Works with BLE Legacy Pairing."],["CAVEATS","Only effective against BLE Legacy Pairing (Bluetooth 4.0/4.1). LE Secure Connections (4.2+) are not vulnerable. Use only for authorized security testing."],["INSTALL","```nix: nix profile install nixpkgs#crackle```"],["SEE ALSO","[wireshark](/man/wireshark)(1)"]],"e":["wireshark"],"x":"crackle -i [capture.pcap]"},"cradle-deploy":{"n":"cradle-deploy","t":"deploy Cradle PHP applications to servers","s":[["TAGLINE","deploy Cradle PHP applications to servers"],["TLDR","**Deploy to production** server\n\n```cradle deploy production```\n\n**Deploy to staging** environment\n\n```cradle deploy staging```\n\n**Deploy with custom configuration**\n\n```cradle deploy --config [deploy.yml]```\n\n**Preview changes** without deploying\n\n```cradle deploy --dry-run production```\n\n**Deploy specific branch**\n\n```cradle deploy production --branch [release]```\n\n**Skip database migrations** during deploy\n\n```cradle deploy --no-migrate production```"],["SYNOPSIS","**cradle deploy** [_options_] _environment_"],["PARAMETERS","_ENVIRONMENT_\n> Target deployment environment (production, staging, development).\n\n**--config** _FILE_\n> Use custom deployment configuration file.\n\n**--dry-run**\n> Preview deployment without making changes.\n\n**--branch** _BRANCH_\n> Deploy a specific Git branch.\n\n**--no-migrate**\n> Skip database migrations during deployment.\n\n**--force**\n> Force deployment even with uncommitted changes.\n\n**--verbose**\n> Show detailed deployment output.\n\n**--help**\n> Display help information."],["DESCRIPTION","**cradle deploy** is a deployment command for the Cradle PHP framework. It automates the process of deploying applications to remote servers, handling tasks like code synchronization, dependency installation, database migrations, and cache clearing.\n\nThe command reads deployment configuration specifying server details, deployment paths, and environment-specific settings. It supports multiple environments, allowing different configurations for production, staging, and development servers.\n\nDeployment typically involves pushing code changes, running composer install, executing migrations, and clearing caches. The dry-run option lets you preview what changes will be made before committing to the deployment."],["CAVEATS","Requires SSH access to target servers. Deployment configuration must specify correct paths and credentials. Use **--dry-run** first to verify deployment steps. Database migrations should be tested before production deployment."],["HISTORY","cradle deploy was created as part of the Cradle framework's CLI tooling to simplify the deployment workflow. It follows patterns established by other PHP framework deployment tools, providing a consistent interface for pushing applications to servers."],["SEE ALSO","[cradle](/man/cradle)(1), [rsync](/man/rsync)(1), [ssh](/man/ssh)(1), [composer](/man/composer)(1)"]],"e":["cradle","rsync","ssh","composer"],"x":"cradle deploy production"},"faked-sysv":{"n":"faked-sysv","t":"System V IPC-based fakeroot daemon","s":[["TAGLINE","System V IPC-based fakeroot daemon"],["TLDR","**Start fakeroot** daemon\n\n```faked-sysv```\n\n**Run with specific** socket\n\n```faked-sysv --socket [/tmp/fakeroot.sock]```\n\n**Debug mode**\n\n```faked-sysv --debug```"],["SYNOPSIS","**faked-sysv** [_options_]"],["PARAMETERS","**--socket** _PATH_\n> Unix socket path for communication.\n\n**--debug**\n> Enable debug output.\n\n**--foreground**\n> Run in foreground (don't daemonize).\n\n**--help**\n> Display help information."],["DESCRIPTION","**faked-sysv** is the daemon component of fakeroot using System V IPC. It maintains a database of fake file ownerships and permissions, allowing unprivileged users to create archives with root-owned files.\n\nThe daemon intercepts file operations via LD_PRELOAD and returns fake ownership/permission information. This enables building packages with correct ownership without actual root privileges.\n\nfaked-sysv uses System V shared memory for communication, as opposed to faked-tcp which uses TCP sockets."],["CAVEATS","Only fakes metadata, not actual permissions. Requires corresponding fakeroot wrapper. SysV IPC has system limits. Not security isolation."],["HISTORY","faked-sysv is part of the **fakeroot** package created for Debian package building. It enables creating .deb packages with proper file ownership without requiring root privileges during the build process."],["INSTALL","```apt: sudo apt install fakeroot```\n\n```dnf: sudo dnf install fakeroot```\n\n```pacman: sudo pacman -S fakeroot```\n\n```zypper: sudo zypper install fakeroot```\n\n```brew: brew install fakeroot```\n\n```nix: nix profile install nixpkgs#fakeroot```"],["SEE ALSO","[fakeroot](/man/fakeroot)(1), [dpkg-buildpackage](/man/dpkg-buildpackage)(1)"]],"e":["fakeroot","dpkg-buildpackage"],"x":"faked-sysv"},"figlet":{"n":"figlet","t":"ASCII art text banner generator","s":[["TAGLINE","ASCII art text banner generator"],["TLDR","**Create ASCII art** text\n\n```figlet \"[Hello World]\"```\n\n**Use specific font**\n\n```figlet -f [slant] \"[text]\"```\n\n**Center output**\n\n```figlet -c \"[text]\"```\n\n**Set width**\n\n```figlet -w [80] \"[text]\"```\n\n**Preview all available fonts**\n\n```showfigfonts```"],["SYNOPSIS","**figlet** [_options_] [_message_]"],["PARAMETERS","_MESSAGE_\n> Text to render as ASCII art.\n\n**-f** _FONT_\n> Font file to use.\n\n**-w** _WIDTH_\n> Output width.\n\n**-c**\n> Center output.\n\n**-l**\n> Left justify.\n\n**-r**\n> Right justify.\n\n**-k**\n> Kerning mode.\n\n**-W**\n> Full width (no smushing).\n\n**--help**\n> Display help information."],["DESCRIPTION","**figlet** renders text as large ASCII art characters using various fonts. It's commonly used for terminal banners, welcome messages, and stylized text output.\n\nMultiple fonts provide different styles from simple block letters to elaborate designs. The tool adjusts character spacing (smushing) to create compact or spread-out output.\n\nfiglet reads from standard input if no message is provided, enabling piped input."],["CAVEATS","Font availability varies by installation. Very long text may wrap awkwardly. Some fonts work better at certain terminal widths."],["HISTORY","figlet (Frank, Ian & Glenn's Letters) was created in 1991 by Glenn Chappell and Ian Chai. It became a Unix classic for creating ASCII banners, spawning hundreds of community-created fonts."],["INSTALL","```dnf: sudo dnf install figlet```\n\n```pacman: sudo pacman -S figlet```\n\n```apk: sudo apk add figlet```\n\n```zypper: sudo zypper install figlet```\n\n```brew: brew install figlet```\n\n```nix: nix profile install nixpkgs#figlet```"],["SEE ALSO","[toilet](/man/toilet)(1), [banner](/man/banner)(1), [cowsay](/man/cowsay)(1), [showfigfonts](/man/showfigfonts)(6)"],["RESOURCES","```[Homepage](http://www.figlet.org/)```\n\n```[Source code](https://github.com/cmatsuoka/figlet)```"]],"e":["toilet","banner","cowsay","showfigfonts"],"x":"figlet \"[Hello World]\""},"flox":{"n":"flox","t":"reproducible development environments built on Nix","s":[["TAGLINE","reproducible development environments built on Nix"],["TLDR","**Initialize environment**\n\n```flox init```\n\n**Install package**\n\n```flox install [ripgrep]```\n\n**Activate environment**\n\n```flox activate```\n\n**Search for packages**\n\n```flox search [nodejs]```\n\n**List installed packages**\n\n```flox list```"],["SYNOPSIS","**flox** _command_ [_options_] [_arguments_]"],["PARAMETERS","_COMMAND_\n> Operation: init, install, activate, search, list, etc.\n\n**init**\n> Initialize new environment.\n\n**install** _PACKAGES_\n> Install packages.\n\n**activate**\n> Enter environment shell.\n\n**search** _QUERY_\n> Search for packages.\n\n**list**\n> Show installed packages.\n\n**uninstall** _PACKAGES_\n> Remove packages.\n\n**push**\n> Publish environment.\n\n**--help**\n> Display help information."],["DESCRIPTION","**flox** is a virtual environment manager built on Nix. It creates reproducible development environments with declarative package management, shareable across machines and teams.\n\nEnvironments are defined in a manifest file and can be pushed to FloxHub for sharing. Unlike traditional virtual environments, flox handles any language or tool from the Nix package collection.\n\nflox simplifies Nix's complexity while preserving its reproducibility guarantees."],["CAVEATS","Requires Nix installation. Large initial downloads for Nix store. Some packages may not be available."],["HISTORY","flox was created to make **Nix** accessible to mainstream developers. It provides familiar virtual environment workflows backed by Nix's reproducible package management."],["SEE ALSO","[nix](/man/nix)(1), [direnv](/man/direnv)(1)"]],"e":["nix","direnv"],"x":"flox init"},"gettgt.py":{"n":"gettgt.py","t":"Kerberos ticket granting ticket requester","s":[["TAGLINE","Kerberos ticket granting ticket requester"],["TLDR","**Get a TGT with** a password\n\n```getTGT.py [domain]/[username]:[password] -dc-ip [dc_ip]```\n\n**Get a TGT using** an NTLM hash\n\n```getTGT.py [domain]/[username] -hashes :[nthash] -dc-ip [dc_ip]```\n\n**Get a TGT using** an AES key\n\n```getTGT.py [domain]/[username] -aesKey [aes_key] -dc-ip [dc_ip]```\n\n**Prompt for the password** instead of passing it inline\n\n```getTGT.py [domain]/[username] -dc-ip [dc_ip]```"],["SYNOPSIS","**getTGT.py** _domain_/_user_[:_password_] [_options_]"],["PARAMETERS","_DOMAIN/USER_[:_PASSWORD_]\n> Target identity. The password is optional and is prompted for if omitted.\n\n**-dc-ip** _IP_\n> IP address of the domain controller. Defaults to the domain FQDN if omitted.\n\n**-hashes** _LMHASH:NTHASH_\n> NTLM hashes to authenticate with. The LM half may be left empty (`:NTHASH`).\n\n**-aesKey** _KEY_\n> AES key (128 or 256 bit) for Kerberos authentication.\n\n**-k**\n> Use Kerberos credentials from the ccache file (KRB5CCNAME), falling back to the supplied credentials.\n\n**-no-pass**\n> Do not prompt for a password, useful together with -k.\n\n**-debug**\n> Turn on DEBUG output.\n\n**-ts**\n> Add a timestamp to each log line."],["DESCRIPTION","**getTGT.py** is an Impacket example script that requests a Kerberos Ticket Granting Ticket (TGT) from an Active Directory domain controller, given a password, an NTLM hash, or an AES key. The official description reads: \"Given a password, hash or aesKey, it will request a TGT and save it as ccache.\"\n\nThe resulting ticket is written to a ccache file named after the principal (for example `username.ccache`). Set the **KRB5CCNAME** environment variable to that file and other Impacket tools, or native utilities, can reuse the ticket for pass-the-ticket authentication.\n\nThere is no dedicated output flag: the ccache filename is derived from the username automatically."],["CAVEATS","**Authorized testing only.** Requires valid credentials and clock sync with the domain controller, since large time skew breaks Kerberos. Tickets have a limited lifetime."],["HISTORY","getTGT.py is part of **Impacket**, the Python network protocol toolkit for Active Directory security testing, originally by Core Security and now maintained by Fortra."],["SEE ALSO","[getuserspns.py](/man/getuserspns.py)(1), [kerberos](/man/kerberos)(7), [kinit](/man/kinit)(1)"]],"e":["getuserspns.py","kerberos","kinit"],"x":"getTGT.py [domain]/[username]:[password] -dc-ip [dc_ip]"},"git-sizer":{"n":"git-sizer","t":"Compute and report repository size metrics","s":[["TAGLINE","Compute and report repository size metrics"],["TLDR","**Analyze repository size**\n\n```git-sizer```\n\n**Verbose output** showing all statistics\n\n```git-sizer -v```\n\n**JSON output** for machine processing\n\n```git-sizer --json```\n\n**Show only critical** statistics\n\n```git-sizer --critical```"],["SYNOPSIS","**git-sizer** [_options_]"],["PARAMETERS","**-v**, **--verbose**\n> Report all statistics, equivalent to --threshold=0\n\n**--json**\n> JSON output format\n\n**--json-version** _n_\n> JSON format version (1 or 2)\n\n**--threshold** _n_\n> Minimum concern level to report (0=all, 1=default, 30=critical only)\n\n**--critical**\n> Only report critical statistics (equivalent to --threshold=30)\n\n**--names** _mode_\n> Display names of large objects (none, hash, or full)\n\n**--no-progress**\n> Suppress progress output\n\n**--show-refs**\n> List the references being processed\n\n**--version**\n> Display version information"],["DESCRIPTION","**git-sizer** computes various size metrics for a Git repository, identifying potential performance problems such as oversized files, deep histories, or wide directory trees.\n\nThe tool reports metrics with concern levels shown as asterisks (*), helping maintainers understand whether their repository structure may cause issues with cloning, fetching, or general Git operations. It must be run from within a Git repository."],["INSTALL","```apt: sudo apt install git-sizer```\n\n```pacman: sudo pacman -S git-sizer```\n\n```apk: sudo apk add git-sizer```\n\n```brew: brew install git-sizer```\n\n```nix: nix profile install nixpkgs#git-sizer```"],["SEE ALSO","[git-filter-repo](/man/git-filter-repo)(1), [git-gc](/man/git-gc)(1)"]],"e":["git-filter-repo","git-gc"],"x":"git-sizer"},"gksu":{"n":"gksu","t":"Graphical frontend for su with GTK password dialog","s":[["TAGLINE","Graphical frontend for su with GTK password dialog"],["TLDR","Run a command as a **specific user**\n\n```gksu -u [userid] [command]```\n\nRun the command while **preserving** the current environment\n\n```gksu -u [userid] -k [command]```\n\nForce gksu to use **su** to run the command\n\n```gksu -u [userid] -w [command]```\n\nForce gksu to use **sudo** to run the command\n\n```gksu -u [userid] -S [command]```\n\nOutput **debug** info for the given command\n\n```gksu -u [userid] -d [command]```\n\nAsk gksu to **print the password** to stdout\n\n```gksu -p [command]```"],["SYNOPSIS","**gksu** [_options_] _command_"],["PARAMETERS","**-u**, **--user** _USER_\n> Run command as specified user (default: root)\n\n**-k**, **--preserve-env**\n> Preserve current environment variables, does not set `$HOME` or `$PATH`\n\n**-l**, **--login**\n> Make this a login shell (may cause problems with Xauthority)\n\n**-w**, **--su-mode**\n> Force use of su for authentication\n\n**-S**, **--sudo-mode**\n> Force use of sudo for authentication\n\n**-d**, **--debug**\n> Output debug information\n\n**-g**, **--disable-grab**\n> Disable locking of the keyboard, mouse, and focus while asking for the password\n\n**-P**, **--prompt**\n> Ask the user before grabbing the keyboard and mouse\n\n**-m**, **--message** _MSG_\n> Custom message in password dialog\n\n**-D**, **--description** _DESC_|_FILE_\n> Description for the command being run, used in the default message\n\n**-p**, **--print-pass**\n> Print the password to stdout, like ssh-askpass"],["DESCRIPTION","**gksu** is a graphical frontend for `su` that prompts for a password using a GTK dialog. It allows running graphical applications with elevated privileges from the command line or menu entries without needing a terminal.\n\nThe tool can operate in either su mode (authenticate as target user, typically root) or sudo mode (use current user's sudo privileges), with automatic detection based on system configuration.\n\n> **DEPRECATED**: gksu is no longer maintained and has security issues. Use **pkexec** from PolicyKit for graphical privilege escalation instead."],["CAVEATS","Deprecated and unmaintained. Running graphical applications as root can cause permission problems with user files (.Xauthority, config files). Modern desktop environments discourage running GUI apps as root entirely."],["HISTORY","gksu was created in the early 2000s for GNOME to provide a graphical password prompt for privileged operations. It was deprecated around **2012** in favor of PolicyKit (pkexec), which provides finer-grained authorization controls."],["INSTALL","```aur: yay -S gksu```"],["SEE ALSO","[pkexec](/man/pkexec)(1), [sudo](/man/sudo)(8), [su](/man/su)(1), [gksudo](/man/gksudo)(1)"],["RESOURCES","```[Debian manual page (archived)](https://manpages.debian.org/testing/gksu/gksu.1.en.html)```"]],"e":["pkexec","sudo","su","gksudo"],"x":"gksu -u [userid] [command]"},"glab-repo":{"n":"glab-repo","t":"Manage GitLab repositories from the command line","s":[["TAGLINE","Manage GitLab repositories from the command line"],["TLDR","**Clone a repository**\n\n```glab repo clone [owner/repo]```\n\n**Create a new repository**\n\n```glab repo create [name]```\n\n**Fork a repository**\n\n```glab repo fork [owner/repo]```\n\n**View repository details**\n\n```glab repo view [owner/repo]```\n\n**Archive repository**\n\n```glab repo archive [owner/repo]```\n\n**Search for repositories**\n\n```glab repo search [query]```\n\n**List repositories**\n\n```glab repo list```\n\n**Delete a repository**\n\n```glab repo delete [owner/repo]```"],["SYNOPSIS","**glab** **repo** _command_ [_options_]"],["SUBCOMMANDS","**clone**\n> Clone a repository.\n\n**create**\n> Create new repository.\n\n**fork**\n> Fork a repository.\n\n**view**\n> View repository details.\n\n**archive**\n> Download repository archive.\n\n**search**\n> Search for repositories.\n\n**list**\n> List repositories.\n\n**delete**\n> Delete a repository."],["PARAMETERS","**--public**\n> Create as public repository.\n\n**--private**\n> Create as private repository.\n\n**--description** _text_\n> Repository description.\n\n**--clone**\n> Clone after forking.\n\n**--group** _name_\n> Create repository in a specific group/namespace.\n\n**-y**, **--yes**\n> Skip confirmation prompts."],["DESCRIPTION","**glab repo** manages GitLab repositories from the command line. It provides subcommands for cloning, creating, forking, searching, and viewing repositories without leaving the terminal."],["INSTALL","```apt: sudo apt install glab```\n\n```dnf: sudo dnf install glab```\n\n```pacman: sudo pacman -S glab```\n\n```apk: sudo apk add glab```\n\n```zypper: sudo zypper install glab```\n\n```brew: brew install glab```\n\n```nix: nix profile install nixpkgs#glab```"],["SEE ALSO","[glab](/man/glab)(1), [glab-mr](/man/glab-mr)(1), [glab-issue](/man/glab-issue)(1), [git-clone](/man/git-clone)(1)"]],"e":["glab","glab-mr","glab-issue","git-clone"],"x":"glab repo clone [owner/repo]"},"gnmic-get":{"n":"gnmic-get","t":"retrieve configuration or state from gNMI devices","s":[["TAGLINE","retrieve configuration or state from gNMI devices"],["TLDR","**Get interface config**\n\n```gnmic get -a [router:57400] --path [/interfaces]```\n\n**Get with encoding**\n\n```gnmic get -a [router:57400] --path [/system] --encoding json```\n\n**Get multiple paths**\n\n```gnmic get -a [router:57400] --path [/interfaces] --path [/system]```\n\n**Get with type**\n\n```gnmic get -a [router:57400] --path [/interfaces] --type config```\n\n**Print only the values, not full paths**\n\n```gnmic get -a [router:57400] --path [/system/name] --values-only```\n\n**Apply a common prefix to all paths**\n\n```gnmic get -a [router:57400] --prefix [/interfaces] --path [interface[name=eth0]]```"],["SYNOPSIS","**gnmic get** [_options_]"],["PARAMETERS","**-a**, **--address** _ADDRESS_\n> Target address(es), as a global gnmic flag.\n\n**--path** _PATH_\n> YANG path to retrieve. Repeatable for multiple paths.\n\n**--prefix** _PATH_\n> Common prefix applied to all **--path** values.\n\n**--encoding** _ENC_\n> Data encoding (json, json_ietf, proto, bytes, ascii).\n\n**--type** _TYPE_\n> Data type to request: ALL, CONFIG, STATE, or OPERATIONAL. Defaults to ALL.\n\n**--model** _NAME_\n> YANG model name to include in the request.\n\n**--target** _NAME_\n> Target name to set in the gNMI prefix, for gateway/proxy setups.\n\n**--values-only**\n> Print only the returned values, omitting the full path structure.\n\n**--depth** _N_\n> gNMI extension depth value, limiting how deep results are returned.\n\n**--help**\n> Display help information."],["DESCRIPTION","**gnmic get** retrieves configuration or state data from gNMI-enabled devices. It fetches data at specified YANG paths and returns it in the requested encoding.\n\nThe command supports multiple paths, various encodings, and data type filtering. It is used for configuration retrieval and state inspection in network automation workflows."],["CAVEATS","Path must be valid YANG path. Device must support requested encoding. Large datasets may timeout."],["HISTORY","gnmic get is a subcommand of **gnmic**, the gNMI client created by Nokia."],["INSTALL","```brew: brew install gnmic```\n\n```nix: nix profile install nixpkgs#gnmic```"],["SEE ALSO","[gnmic](/man/gnmic)(1), [gnmic-set](/man/gnmic-set)(1), [gnmic-subscribe](/man/gnmic-subscribe)(1)"],["RESOURCES","```[Documentation](https://gnmic.openconfig.net/cmd/get/)```\n\n```[Source code](https://github.com/openconfig/gnmic)```"]],"e":["gnmic","gnmic-set","gnmic-subscribe"],"x":"gnmic get -a [router:57400] --path [/interfaces]"},"gsutil":{"n":"gsutil","t":"command-line tool for Google Cloud Storage","s":[["TAGLINE","command-line tool for Google Cloud Storage"],["TLDR","**List buckets**\n\n```gsutil ls```\n\n**List bucket contents**\n\n```gsutil ls gs://[bucket-name]```\n\n**Copy file to bucket**\n\n```gsutil cp [file.txt] gs://[bucket]/```\n\n**Copy bucket to local**\n\n```gsutil cp gs://[bucket]/[file] [./]```\n\n**Sync directory to bucket**\n\n```gsutil rsync -r [local_dir] gs://[bucket]/[path]```\n\n**Make object public**\n\n```gsutil acl ch -u AllUsers:R gs://[bucket]/[file]```\n\n**Remove object**\n\n```gsutil rm gs://[bucket]/[file]```"],["SYNOPSIS","**gsutil** [_options_] _command_ [_args_]"],["DESCRIPTION","**gsutil** is a command-line tool for interacting with Google Cloud Storage (GCS). It provides commands for creating and deleting buckets, uploading and downloading objects, synchronizing local directories with remote bucket paths, and managing access control lists (ACLs) and bucket policies. Objects are referenced using the `gs://bucket/path` URI scheme.\n\nFor large-scale operations, the `-m` flag enables parallel, multi-threaded execution of commands like `cp`, `mv`, `rm`, and `rsync`, significantly improving throughput when working with many files or large datasets. Transfers are resumable by default -- interrupted uploads and downloads can be retried without starting over. gsutil also supports server-side features such as object versioning, lifecycle management, and cross-bucket replication, and it authenticates through the Google Cloud SDK's credential system."],["PARAMETERS","**ls**\n> List buckets or objects.\n\n**cp**\n> Copy files/objects.\n\n**mv**\n> Move files/objects.\n\n**rm**\n> Remove objects.\n\n**rsync**\n> Synchronize directories.\n\n**mb**\n> Make bucket.\n\n**rb**\n> Remove bucket.\n\n**acl**\n> Manage access control.\n\n**-m**\n> Parallel operations.\n\n**-r**\n> Recursive operation.\n\n**-n**\n> Dry run (no changes)."],["CAVEATS","**gsutil is a legacy tool**: Google now recommends the newer **gcloud storage** commands (part of the Google Cloud CLI), which are faster and support features gsutil lacks, such as soft delete and managed folders. gsutil still ships with the Cloud CLI and works as before, but is only minimally maintained.\n\nRequires Google Cloud authentication. Large transfers may incur costs. ACL changes need careful consideration. Parallel operations use more resources."],["HISTORY","**gsutil** was developed by **Google** as part of the Google Cloud SDK. It evolved from early Cloud Storage access tools to become the standard CLI for Cloud Storage operations, supporting Python 2 initially and later Python 3."],["SEE ALSO","[gcloud](/man/gcloud)(1), [aws](/man/aws)(1), [azcopy](/man/azcopy)(1), [rclone](/man/rclone)(1)"],["RESOURCES","```[Source code](https://github.com/GoogleCloudPlatform/gsutil)```\n\n```[Documentation](https://cloud.google.com/storage/docs/gsutil)```"]],"e":["gcloud","aws","azcopy","rclone"],"x":"gsutil ls"},"gum":{"n":"gum","t":"tool for creating glamorous shell scripts","s":[["TAGLINE","tool for creating glamorous shell scripts"],["TLDR","**Prompt for input**\n\n```gum input --placeholder \"Enter name\"```\n\n**Choose from options**\n\n```gum choose \"Option 1\" \"Option 2\" \"Option 3\"```\n\n**Confirm action**\n\n```gum confirm \"Delete file?\" && rm file```\n\n**Style text**\n\n```gum style --foreground 212 \"Styled text\"```\n\n**Spin while running**\n\n```gum spin --title \"Loading\" -- sleep 5```\n\n**Write multiline text**\n\n```gum write --placeholder \"Description\"```\n\n**Filter a list with fuzzy search**\n\n```cat [flavors.txt] | gum filter```\n\n**Render a markdown string**\n\n```gum format -- \"# Heading\" \"- item one\" \"- item two\"```"],["SYNOPSIS","**gum** _command_ [_options_]"],["PARAMETERS","_COMMAND_\n> Subcommand to run.\n\n**input**\n> Prompt for a single line of input.\n\n**write**\n> Prompt for long-form, multi-line text.\n\n**choose**\n> Choose one or more options from a list.\n\n**confirm**\n> Ask a user to confirm an action (exit code 0 or 1).\n\n**filter**\n> Fuzzy-filter items from a list.\n\n**file**\n> Pick a file from a folder.\n\n**pager**\n> Scroll through a file or text.\n\n**table**\n> Render or select a row from tabular data.\n\n**style**\n> Apply coloring, borders, and spacing to text.\n\n**join**\n> Combine styled text blocks vertically or horizontally.\n\n**format**\n> Render markdown, template strings, or named emoji.\n\n**log**\n> Log leveled, structured messages to the terminal.\n\n**spin**\n> Show a spinner while running a command.\n\n**--help**\n> Display help information."],["DESCRIPTION","**gum** is a tool for creating glamorous shell scripts. It provides interactive prompts, styled output, and TUI components for building beautiful CLI applications without writing any Go code, built on Charm's Bubble Tea, Bubbles, and Lip Gloss libraries.\n\nEach subcommand is independently configurable via command-line flags or `GUM_<COMMAND>_<OPTION>` environment variables, so scripts can compose the components as needed."],["CAVEATS","Requires a terminal with color support (true color recommended for full styling). Part of Charm's suite of TUI tools."],["HISTORY","**gum** was created by **Charm** and first released in **2022** as part of their TUI toolkit for building delightful command-line tools. It is written in Go and released under the MIT license."],["INSTALL","```apt: sudo apt install gum```\n\n```dnf: sudo dnf install gum```\n\n```pacman: sudo pacman -S gum```\n\n```apk: sudo apk add gum```\n\n```zypper: sudo zypper install gum```\n\n```brew: brew install gum```\n\n```nix: nix profile install nixpkgs#gum```"],["SEE ALSO","[fzf](/man/fzf)(1), [dialog](/man/dialog)(1), [whiptail](/man/whiptail)(1)"],["RESOURCES","```[Homepage](https://charm.land/gum/)```\n\n```[Source code](https://github.com/charmbracelet/gum)```"]],"e":["fzf","dialog","whiptail"],"x":"gum input --placeholder \"Enter name\""},"httpd":{"n":"httpd","t":"Apache HTTP Server daemon","s":[["TAGLINE","Apache HTTP Server daemon"],["TLDR","**Start** the Apache HTTP server\n\n```sudo httpd```\n\n**Test** the configuration file syntax\n\n```httpd -t```\n\n**Start** in single-process debug mode (foreground)\n\n```httpd -X```\n\n**List all loaded** modules (static and shared)\n\n```httpd -M```\n\n**Show version** and build parameters\n\n```httpd -V```\n\n**Use a specific** configuration file\n\n```httpd -f [/path/to/httpd.conf]```\n\n**Send a signal** to a running instance (start, stop, restart, graceful)\n\n```httpd -k [graceful]```\n\n**Stop** Apache gracefully\n\n```httpd -k graceful-stop```"],["SYNOPSIS","**httpd** [_options_]"],["DESCRIPTION","**httpd** is the Apache HTTP Server daemon. It serves web content using HTTP/HTTPS protocols, handling static files, CGI scripts, and reverse proxying.\n\nApache is highly configurable through modules and configuration directives, supporting virtual hosts, access control, and numerous authentication methods."],["PARAMETERS","**-t**\n> Test configuration syntax.\n\n**-T**\n> Start without document check.\n\n**-X**\n> Run in single-process debug mode.\n\n**-f** _file_\n> Specify configuration file.\n\n**-M**\n> List loaded modules.\n\n**-V**\n> Show version and build info.\n\n**-k** _signal_\n> Send signal to running instance: start, stop, restart, graceful, graceful-stop.\n\n**-D** _param_\n> Define a runtime configuration parameter.\n\n**-d** _dir_\n> Set the ServerRoot directory.\n\n**-e** _level_\n> Set startup log level (debug, info, notice, warn, error, crit, alert, emerg).\n\n**-n** _name_\n> Set the service name (Windows only).\n\n**-l**\n> List compiled-in modules (static modules only)."],["CAVEATS","Configuration can be complex for beginners due to the large number of directives and modules. Module compatibility varies between major versions. Performance tuning (MPM selection, worker/thread limits) is needed for high-traffic sites. On many Linux distributions, **apachectl** or **systemctl** is the preferred way to manage the service rather than invoking **httpd** directly. On Debian/Ubuntu, the binary is named **apache2** instead of **httpd**."],["HISTORY","**Apache HTTP Server** was started in **1995** based on NCSA HTTPd, becoming the most popular web server for decades. The **Apache Software Foundation** grew around it. The name comes from \"a patchy server\" due to its patch-based origins."],["INSTALL","```apt: sudo apt install merecat```\n\n```dnf: sudo dnf install httpd```\n\n```apk: sudo apk add apache2```\n\n```zypper: sudo zypper install apache2```\n\n```brew: brew install apache2```\n\n```nix: nix profile install nixpkgs#merecat```"],["SEE ALSO","[nginx](/man/nginx)(1), [apache2](/man/apache2)(1), [apachectl](/man/apachectl)(1), [a2enmod](/man/a2enmod)(1), [htpasswd](/man/htpasswd)(1), [caddy](/man/caddy)(1)"]],"e":["nginx","apache2","apachectl","a2enmod","htpasswd","caddy"],"x":"sudo httpd"},"hub-issue":{"n":"hub-issue","t":"manages GitHub issues from the command line","s":[["TAGLINE","manages GitHub issues from the command line"],["TLDR","**List open issues**\n\n```hub issue```\n\n**List issues with a label**\n\n```hub issue -l [bug]```\n\n**Create a new issue** (opens an editor)\n\n```hub issue create```\n\n**Create with title and body**\n\n```hub issue create -m \"[title]\" -m \"[body]\"```\n\n**Show a specific issue**\n\n```hub issue show [123]```\n\n**List an assignee's issues**\n\n```hub issue -a [username]```"],["SYNOPSIS","**hub issue** [_-a_ _assignee_] [_-c_ _creator_] [_-@_ _user_] [_-s_ _state_] [_-f_ _format_] [_-M_ _milestone_] [_-l_ _labels_] [_-L_ _limit_]\n\n**hub issue show** [_-f_ _format_] _number_\n\n**hub issue create** [**-o**] [**-m** _message_ | **-F** _file_] [**--edit**] [**-a** _users_] [**-M** _milestone_] [**-l** _labels_]\n\n**hub issue transfer** _number_ _repository_"],["SUBCOMMANDS","**list**\n> List issues in the current repository (default when no subcommand is given).\n\n**show** _number_\n> Show an existing issue.\n\n**create**\n> Open a new issue in the current repository.\n\n**update** _number_\n> Update fields of an existing issue.\n\n**labels**\n> List the labels available in this repository.\n\n**transfer** _number_ _repository_\n> Transfer an issue to another repository."],["PARAMETERS","**-a**, **--assignee** _user_\n> In list mode, show only issues assigned to _user_. In create/update mode (**--assign**), a comma-separated list of users to assign.\n\n**-c**, **--creator** _user_\n> Show only issues created by _user_.\n\n**-@**, **--mentioned** _user_\n> Show only issues mentioning _user_.\n\n**-s**, **--state** _state_\n> Show issues with state **open**, **closed**, or **all** (default: open).\n\n**-f**, **--format** _format_\n> Customize output using placeholders such as %i (number), %t (title), %S (state), %l (labels), %b (body), %au (author).\n\n**-M**, **--milestone** _name_\n> Show only issues for the given milestone.\n\n**-l**, **--labels** _labels_\n> Show only issues with the given comma-separated labels.\n\n**-d**, **--since** _date_\n> Show only issues updated on or after _date_ (ISO 8601).\n\n**-L**, **--limit** _n_\n> Show only the first _n_ issues.\n\n**-m**, **--message** _msg_\n> Issue title/body text (repeatable; first line is the title).\n\n**-F**, **--file** _file_\n> Read the issue title and description from _file_.\n\n**--edit**\n> Open the title and description in a text editor before submitting.\n\n**-o**, **--browse**\n> Open the new issue in a web browser instead of printing its URL.\n\n**--include-pulls**\n> Include pull requests alongside issues."],["DESCRIPTION","**hub issue** manages GitHub issues from the command line. List, create, update, and view issues without leaving the terminal.\n\nWith no arguments, it lists open issues for the current repository, sorted by creation date. Subcommands cover viewing a single issue, filing new ones, editing existing ones, listing labels, and transferring an issue to another repository."],["CAVEATS","Part of **hub**, which is deprecated in favor of the official GitHub CLI (**gh**); consider using **gh issue** instead. Requires GitHub authentication."],["INSTALL","```apt: sudo apt install hub```\n\n```pacman: sudo pacman -S hub```\n\n```zypper: sudo zypper install hub```\n\n```brew: brew install hub```\n\n```nix: nix profile install nixpkgs#hub```"],["SEE ALSO","[hub](/man/hub)(1), [gh-issue](/man/gh-issue)(1)"],["RESOURCES","```[Source code](https://github.com/github/hub)```\n\n```[Homepage](https://hub.github.com/)```"]],"e":["hub","gh-issue"],"x":"hub issue"},"ifstat":{"n":"ifstat","t":"reports network interface bandwidth in a format similar to vmstat/iostat","s":[["TAGLINE","reports network interface bandwidth in a format similar to vmstat/iostat"],["TLDR","Display statistics for all interfaces, updating every **second**\n\n```ifstat```\n\nDisplay statistics every **5 seconds**\n\n```ifstat [5]```\n\nReport bandwidth in **kbits/sec** instead of kbytes/sec\n\n```ifstat -b```\n\nMonitor only **specific interfaces**\n\n```ifstat -i [eth0],[wlan0]```\n\nShow **total bandwidth** across all interfaces\n\n```ifstat -T```\n\nIgnore history and show **absolute counters**\n\n```ifstat -a```"],["SYNOPSIS","**ifstat** [_options_] [_delay_[/_delay2_] [_count_]]"],["PARAMETERS","**-a**\n> Ignore the history file and show absolute counters since boot\n\n**-b**\n> Report bandwidth in kbits/sec instead of kbytes/sec\n\n**-e**\n> Show errors\n\n**-i** _IFACE_\n> Monitor only the specified interfaces (comma-separated)\n\n**-l**\n> Enable monitoring of loopback interfaces\n\n**-n**\n> Turn off display of the header periodically\n\n**-r**\n> Reset history\n\n**-S**\n> Keep stats on the same line (no scrolling)\n\n**-T**\n> Show total bandwidth for all monitored interfaces\n\n**-t**\n> Add a timestamp at the beginning of each line\n\n**-w**\n> Use fixed-width columns\n\n**-z**\n> Hide interfaces with zero counters"],["DESCRIPTION","**ifstat** reports network interface bandwidth just like vmstat/iostat do for other system counters. It keeps history between invocations and by default shows the difference since the last call.\n\nThe positional argument _delay_ sets the update interval in seconds (default 1). An optional _count_ limits the number of updates. A second delay can be specified separated by a slash for alternating intervals.\n\nNote: Two different tools share the name ifstat. The standalone version (section 1) described here is the most common. The iproute2 version (section 8) has different options."],["CAVEATS","Statistics depend on kernel interface counters. Very high traffic rates may overflow 32-bit counters on older systems. The iproute2 ifstat(8) has different flags than the standalone ifstat(1)."],["INSTALL","```apt: sudo apt install ifstat```\n\n```dnf: sudo dnf install ifstat```\n\n```pacman: sudo pacman -S iproute2```\n\n```apk: sudo apk add iproute2```\n\n```zypper: sudo zypper install iproute2```\n\n```brew: brew install ifstat```\n\n```nix: nix profile install nixpkgs#iproute2```"],["SEE ALSO","[ip](/man/ip)(8), [netstat](/man/netstat)(8), [iftop](/man/iftop)(8), [vmstat](/man/vmstat)(8)"]],"e":["ip","netstat","iftop","vmstat"],"x":"ifstat"},"infokey":{"n":"infokey","t":"compile custom key bindings for the GNU Info reader","s":[["TAGLINE","compile custom key bindings for the GNU Info reader"],["TLDR","**Compile the default key bindings file**\n\n```infokey```\n\n**Compile a specific input file**\n\n```infokey [path/to/file]```\n\n**Compile and output to a specific file**\n\n```infokey -o [path/to/output] [path/to/input]```"],["SYNOPSIS","**infokey** [_options_] [_file_]"],["PARAMETERS","_FILE_\n> Input file (default $HOME/.infokey).\n\n**-o** _FILE_\n> Output compiled key file (default $HOME/.info).\n\n**--help**\n> Display help information.\n\n**--version**\n> Display version information."],["DESCRIPTION","**infokey** compiles a source file containing custom key bindings and variable settings for the GNU Info reader into a binary format. It reads $HOME/.infokey by default and writes the compiled output to $HOME/.info.\n\nThe input file contains sections for defining key sequences and their associated actions. The **#info** section defines key bindings for Info windows, the **#echo-area** section defines bindings for the echo area, and the **#var** section sets Info variables. Custom bindings override the default Info navigation keys."],["CAVEATS","The input file format is specific to Info and uses a custom syntax. The binary output is not human-readable. Requires the GNU Info reader to use the compiled bindings. In newer versions of Texinfo (6.0+), the infokey command was deprecated and the Info reader can read the .infokey text file directly without compilation."],["HISTORY","infokey is part of the **GNU Texinfo** package. It was introduced to allow customization of Info reader key bindings. In **Texinfo 6.0** (2015), the standalone infokey command was deprecated as the Info reader gained the ability to read the .infokey source file directly."],["SEE ALSO","[info](/man/info)(1)"]],"e":["info"],"x":"infokey"},"ip-address":{"n":"ip-address","t":"manages IP addresses on network interfaces","s":[["TAGLINE","manages IP addresses on network interfaces"],["TLDR","**List** all addresses\n\n```ip address show```\n\nShow only **active** interfaces\n\n```ip address show up```\n\nShow addresses for a **specific** interface\n\n```ip address show dev [eth0]```\n\nShow only **IPv4** addresses\n\n```ip -4 address show```\n\n**Add** an IP address to an interface\n\n```sudo ip address add [192.168.1.100/24] dev [eth0]```\n\n**Delete** an IP address from an interface\n\n```sudo ip address delete [192.168.1.100/24] dev [eth0]```\n\n**Flush** all addresses on an interface\n\n```sudo ip address flush dev [eth0]```"],["SYNOPSIS","**ip** [_OPTIONS_] **address** {_command_|**help**}"],["DESCRIPTION","**ip address** manages IP addresses on network interfaces. It can display, add, delete, and flush addresses for both IPv4 and IPv6 protocols."],["PARAMETERS","**show** [**dev** _device_]\n> Display IP addresses (default action).\n\n**add** _address/prefix_ **dev** _device_\n> Add an IP address to an interface.\n\n**change** _address/prefix_ **dev** _device_\n> Change properties of an existing address.\n\n**replace** _address/prefix_ **dev** _device_\n> Add or update an address on an interface.\n\n**delete** _address/prefix_ **dev** _device_\n> Remove an IP address from an interface.\n\n**flush** [**dev** _device_]\n> Remove all addresses matching criteria.\n\n**save**\n> Save address configuration to stdout.\n\n**restore**\n> Restore address configuration from stdin.\n\n**dev** _device_\n> Interface name to operate on.\n\n**scope** _global|link|host_\n> Filter or set address scope.\n\n**label** _label_\n> Set address label (must start with device name).\n\n**broadcast** _address_\n> Set broadcast address.\n\n**metric** _number_\n> Set priority of the prefix route for the address.\n\n**valid_lft** _lft_\n> Valid lifetime of the address (default: forever).\n\n**preferred_lft** _lft_\n> Preferred lifetime of the address (default: forever).\n\n**up**\n> Filter to show only active (UP) interfaces.\n\n**permanent**\n> Filter to show only permanent (static) addresses.\n\n**dynamic**\n> Filter to show only dynamically assigned addresses.\n\n**to** _prefix_\n> Filter addresses matching a prefix."],["CAVEATS","Addresses added are not persistent across reboots. Use network configuration files or NetworkManager for persistence. An interface can have multiple IP addresses."],["HISTORY","**ip address** is part of **iproute2**, replacing the deprecated ifconfig command."],["SEE ALSO","[ip](/man/ip)(8), [ip-link](/man/ip-link)(8), [ip-route](/man/ip-route)(8), [ip-neighbour](/man/ip-neighbour)(8)"]],"e":["ip","ip-link","ip-route","ip-neighbour"],"x":"ip address show"},"keep-header":{"n":"keep-header","t":"preserves the first line of a file while passing the remaining lines through","s":[["TAGLINE","preserves the first line of a file while passing the remaining lines through"],["TLDR","**Sort file keeping header**\n\n```keep-header [file] -- sort```\n\n**Filter with grep keeping header**\n\n```keep-header [file] -- grep [pattern]```\n\n**Pipe through multiple commands**\n\n```keep-header [file] -- sort | head -20```"],["SYNOPSIS","**keep-header** _file_ **--** _command_ [_args_...]"],["DESCRIPTION","**keep-header** preserves the first line (header) of a file while passing the remaining lines through another command. Useful for processing CSV or TSV files where you want to sort or filter data without losing column headers."],["SEE ALSO","[head](/man/head)(1), [tail](/man/tail)(1), [sort](/man/sort)(1)"]],"e":["head","tail","sort"],"x":"keep-header [file] -- sort"},"krdc":{"n":"krdc","t":"KDE remote desktop client","s":[["TAGLINE","KDE remote desktop client"],["TLDR","**Launch** the remote desktop client\n\n```krdc```\n\n**Connect to a VNC server**\n\n```krdc vnc://[hostname:display]```\n\n**Connect to an RDP server**\n\n```krdc rdp://[hostname]```\n\n**Connect to an RDP server** on a custom port\n\n```krdc rdp://[hostname:port]```\n\n**Start in fullscreen mode** connecting to a host\n\n```krdc --fullscreen vnc://[hostname]```"],["SYNOPSIS","**krdc** [_options_] [_URL_]"],["PARAMETERS","**--fullscreen**\n> Start in fullscreen mode. Works only when a single URL is specified.\n\n**-h**, **--help**\n> Display help text listing available options.\n\n**--version**\n> Display version information.\n\n**--author**\n> Show author information.\n\n**--license**\n> Show license information."],["DESCRIPTION","**KRDC** (KDE Remote Desktop Client) is a graphical application that allows viewing and controlling the desktop session on another machine running a compatible server. It operates at the frame-buffer level, making it compatible with multiple windowing systems including X11, Wayland, macOS, and Windows.\n\nKRDC supports two protocols: **VNC** (Virtual Network Computing) for cross-platform remote access, and **RDP** (Remote Desktop Protocol) for connecting to Windows machines and other RDP-enabled hosts. RDP support requires **xfreerdp** (FreeRDP) as a runtime dependency.\n\nThe client provides bookmark management for frequently-accessed connections, a history of recent sessions, per-host settings, and configurable connection speed profiles (LAN, DSL/Cable, Modem) that adjust quality and compression. Passwords can be stored securely via **KWallet** integration."],["CONFIGURATION","Connection settings are stored per-host and accessible through the configuration dialog, which provides three tabs: **General**, **VNC Defaults**, and **RDP Defaults**. Speed profiles control color depth and compression to balance quality against bandwidth. Per-host settings avoid repeated configuration prompts on reconnection."],["CAVEATS","KRDC is a graphical application and requires a running display server (X11 or Wayland). RDP connections require **xfreerdp** to be installed separately. VNC connections are unencrypted by default; tunnel through SSH for security on untrusted networks."],["HISTORY","KRDC was originally written by **Tim Jansen** as part of the KDE project. A major rewrite was completed by **Urs Wolfer** during **Google Summer of Code 2007**, modernizing the codebase and improving protocol support. It has been a standard component of the **KDE Applications** suite since KDE 3.x and continues active development as part of KDE Gear releases."],["INSTALL","```apt: sudo apt install krdc```\n\n```dnf: sudo dnf install krdc```\n\n```pacman: sudo pacman -S krdc```\n\n```apk: sudo apk add krdc```\n\n```zypper: sudo zypper install krdc```"],["SEE ALSO","[rdesktop](/man/rdesktop)(1), [xfreerdp](/man/xfreerdp)(1), [vncviewer](/man/vncviewer)(1), [ssh](/man/ssh)(1)"]],"e":["rdesktop","xfreerdp","vncviewer","ssh"],"x":"krdc"},"kubectl-api-versions":{"n":"kubectl-api-versions","t":"lists all available API versions in the Kubernetes cluster","s":[["TAGLINE","lists all available API versions in the Kubernetes cluster"],["TLDR","**List all API versions**\n\n```kubectl api-versions```"],["SYNOPSIS","**kubectl** **api-versions**"],["DESCRIPTION","**kubectl api-versions** lists all available API versions in the Kubernetes cluster. Shows group/version format for each available API. Useful for determining which API versions are supported when writing manifests."],["INSTALL","```apt: sudo apt install kubectl```\n\n```pacman: sudo pacman -S kubectl```\n\n```apk: sudo apk add kubectl```\n\n```brew: brew install kubectl```\n\n```nix: nix profile install nixpkgs#kubectl```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [kubectl-api-resources](/man/kubectl-api-resources)(1)"]],"e":["kubectl","kubectl-api-resources"],"x":"kubectl api-versions"},"libalpm":{"n":"libalpm","t":"arch Linux Package Management library","s":[["TAGLINE","arch Linux Package Management library"],["TLDR","**Library - typically used via pacman**\n\n```pacman -S [package]```\n\n**Query local database**\n\n```pacman -Q [package]```\n\n**Sync databases**\n\n```pacman -Sy```\n\n**Search packages**\n\n```pacman -Ss [pattern]```"],["SYNOPSIS","**libalpm** - Arch Linux Package Management library"],["DESCRIPTION","**libalpm** is the Arch Linux Package Management library. It provides the backend functionality for pacman and other package management tools in Arch Linux and derivatives.\n\nThe library handles package database management, dependency resolution, package installation/removal, and transaction handling."],["KEY FEATURES","```\n- Package database management\n- Dependency resolution\n- Delta/incremental updates\n- Signature verification\n- Transaction support\n- Hook system\n```"],["API USAGE","```c\n#include <alpm.h>\n\nalpm_handle_t *handle;\nhandle = alpm_initialize(\"/\", \"/var/lib/pacman/\", &err);\n```"],["CAVEATS","Library API, not directly executable. Used by pacman, yay, paru. API may change between major versions. Arch-specific."],["HISTORY","libalpm was developed as part of **pacman** by the Arch Linux team, with the library interface introduced to allow third-party frontends."],["INSTALL","```dnf: sudo dnf install libalpm```"],["SEE ALSO","[pacman](/man/pacman)(8), [makepkg](/man/makepkg)(8), [yay](/man/yay)(8), [paru](/man/paru)(1)"]],"e":["pacman","makepkg","yay","paru"],"x":"pacman -S [package]"},"lvrename":{"n":"lvrename","t":"renames a logical volume within its volume group","s":[["TAGLINE","renames a logical volume within its volume group"],["TLDR","Rename using **full paths**\n\n```sudo lvrename /dev/vg_name/old_lv /dev/vg_name/new_lv```\n\nRename using **VG and names**\n\n```sudo lvrename [vg_name] [old_lv] [new_lv]```\n\nRename with **auto-confirm**\n\n```sudo lvrename --yes /dev/vg_name/old_lv /dev/vg_name/new_lv```"],["SYNOPSIS","**lvrename** [_options_] _vg_/_old_lv_ _new_lv_"],["DESCRIPTION","**lvrename** renames a logical volume within its volume group. The volume can be active during the rename operation."],["PARAMETERS","**-y, --yes**\n> Answer yes to all prompts\n\n**-A, --autobackup y|n**\n> Automatically back up metadata after the change (default y).\n\n**--noudevsync**\n> Disable udev synchronisation\n\n**-t, --test**\n> Test mode (don't apply changes)\n\n**-v, --verbose**\n> Verbose output"],["CAVEATS","Update /etc/fstab and any scripts referencing the old LV name. Active filesystems may need remounting after rename."],["INSTALL","```apt: sudo apt install lvm2```\n\n```dnf: sudo dnf install lvm2```\n\n```pacman: sudo pacman -S lvm2```\n\n```apk: sudo apk add lvm2```\n\n```zypper: sudo zypper install lvm2```\n\n```nix: nix profile install nixpkgs#lvm2```"],["SEE ALSO","[lvcreate](/man/lvcreate)(8), [lvremove](/man/lvremove)(8), [lvm](/man/lvm)(8)"]],"e":["lvcreate","lvremove","lvm"],"x":"sudo lvrename /dev/vg_name/old_lv /dev/vg_name/new_lv"},"metro":{"n":"metro","t":"JavaScript bundler used by React Native","s":[["TAGLINE","JavaScript bundler used by React Native"],["TLDR","**Start Metro bundler**\n\n```npx react-native start```\n\n**Start with cache reset**\n\n```npx react-native start --reset-cache```\n\n**Start on specific port**\n\n```npx react-native start --port [8082]```\n\n**Bundle for production**\n\n```npx react-native bundle --platform [android] --dev false --entry-file [index.js] --bundle-output [bundle.js]```\n\n**Enable verbose logging**\n\n```npx react-native start --verbose```"],["SYNOPSIS","**metro** [_options_]"],["PARAMETERS","**--port** _port_\n> Server port.\n\n**--reset-cache**\n> Clear bundler cache.\n\n**--verbose**\n> Verbose output.\n\n**--config** _file_\n> Config file path.\n\n**--entry-file** _file_\n> Entry point file.\n\n**--bundle-output** _file_\n> Output bundle file.\n\n**--platform** _platform_\n> Target platform (ios, android).\n\n**--dev** _boolean_\n> Development mode flag for the bundle (default: true).\n\n**--host** _host_\n> Host to listen on (default: localhost).\n\n**--max-workers** _N_\n> Specify the maximum number of workers for parallelization."],["DESCRIPTION","**Metro** is the JavaScript bundler used by React Native. It compiles and bundles JavaScript code and assets, handles module resolution, and provides hot reloading during development.\n\nMetro is optimized for React Native's requirements with fast incremental builds."],["CONFIGURATION","```javascript\n// metro.config.js\nmodule.exports = {\n  resolver: {\n    sourceExts: ['js', 'jsx', 'ts', 'tsx'],\n  },\n  transformer: {\n    getTransformOptions: async () => ({\n      transform: { experimentalImportSupport: false },\n    }),\n  },\n};\n```"],["CAVEATS","React Native specific. Cache issues may require reset. Port conflicts with multiple instances."],["HISTORY","Metro was developed by **Facebook** (Meta) as the bundler for React Native, optimized for mobile development workflows."],["SEE ALSO","[react-native](/man/react-native)(1), [webpack](/man/webpack)(1), [babel](/man/babel)(1), [expo](/man/expo)(1)"]],"e":["react-native","webpack","babel","expo"],"x":"npx react-native start"},"mkdir":{"n":"mkdir","t":"creates directories with the specified names","s":[["TAGLINE","creates directories with the specified names"],["TLDR","**Create a directory**\n\n```mkdir [directory]```\n\n**Create multiple directories**\n\n```mkdir [dir1] [dir2] [dir3]```\n\n**Create parent directories** as needed (no error if existing)\n\n```mkdir -p [path/to/nested/directory]```\n\n**Create a directory with specific permissions**\n\n```mkdir -m [755] [directory]```\n\n**Create directories and show a message** for each\n\n```mkdir -v [dir1] [dir2]```"],["SYNOPSIS","**mkdir** [_-pv_] [_-m mode_] _directory ..._"],["PARAMETERS","**-p**, **--parents**\n> Create parent directories as needed; no error if existing\n\n**-m** _mode_, **--mode**=_mode_\n> Set file mode (permissions) for the new directory\n\n**-v**, **--verbose**\n> Print a message for each created directory\n\n**-Z**, **--context**\n> Set SELinux security context of each created directory\n\n**--help**\n> Display help information and exit\n\n**--version**\n> Output version information and exit"],["DESCRIPTION","**mkdir** creates directories with the specified names. By default, it creates the directory in the current working directory or at the specified path.\n\nThe **-p** option is particularly useful for creating nested directory structures in a single command, as it creates all necessary parent directories and does not report an error if the target directory already exists. This makes it ideal for scripts where the directory may or may not exist.\n\nPermissions for the new directory are affected by the current **umask** unless explicitly set with **-m**. The default permission is typically 777 minus the umask value."],["CAVEATS","Creating directories requires write permission in the parent directory. On filesystems with limited filename support (like FAT), certain characters in directory names may cause issues. The **-p** flag silently succeeds if the directory exists, which may mask errors in scripts expecting to create new directories."],["HISTORY","The **mkdir** command has been part of Unix since the **First Edition in 1971**, making it one of the original Unix commands. It was created by **Ken Thompson** at Bell Labs. The command has remained remarkably stable over the decades, with the **-p** option added in later versions to simplify directory tree creation. It is defined by the POSIX standard and is available on all Unix-like operating systems."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[rmdir](/man/rmdir)(1), [rm](/man/rm)(1), [ls](/man/ls)(1), [chmod](/man/chmod)(1), [install](/man/install)(1)"]],"e":["rmdir","rm","ls","chmod","install"],"x":"mkdir [directory]"},"mksquashfs":{"n":"mksquashfs","t":"creates and appends to squashfs filesystems","s":[["TAGLINE","creates and appends to squashfs filesystems"],["TLDR","Create a **squashfs filesystem** from files and directories\n\n```mksquashfs [path/to/source1] [path/to/source2] [filesystem.squashfs]```\n\nCreate squashfs using a specific **compression algorithm**\n\n```mksquashfs [path/to/source] [filesystem.squashfs] -comp [gzip|lzo|lz4|xz|zstd]```\n\n**Exclude** specific files or directories\n\n```mksquashfs [path/to/source] [filesystem.squashfs] -e [file_or_dir1] [file_or_dir2]```\n\nExclude files matching **wildcard pattern**\n\n```mksquashfs [path/to/source] [filesystem.squashfs] -wildcards -e \"[*.gz]\"```\n\nExclude files matching **regex pattern**\n\n```mksquashfs [path/to/source] [filesystem.squashfs] -regex -e \"[pattern]\"```\n\nSet custom **block size** (default 128 Kbytes, max 1 Mbyte)\n\n```mksquashfs [path/to/source] [filesystem.squashfs] -b [256K]```\n\nCreate a **new** filesystem without appending\n\n```mksquashfs [path/to/source] [filesystem.squashfs] -noappend```"],["SYNOPSIS","**mksquashfs** _source1_ [_source2_...] _filesystem_ [**-comp** _method_] [**-b** _blocksize_] [**-e** _excludes_] [_options_]"],["PARAMETERS","**-comp _method_**\n> Compression algorithm: gzip (default), lzo, lz4, xz, zstd, lzma\n\n**-b _size_**\n> Data block size; default 128 Kbytes, maximum 1 Mbyte. K/M suffixes supported\n\n**-e _items_**\n> Exclude specified files and directories\n\n**-wildcards**\n> Enable glob pattern matching for exclusions\n\n**-regex**\n> Use POSIX regular expressions for exclusions\n\n**-all-root**\n> Make all files owned by root\n\n**-force-uid _user_**\n> Override file owner user\n\n**-force-gid _group_**\n> Override file owner group\n\n**-processors _num_**\n> Number of compression threads\n\n**-mem _size_**\n> Set cache memory size\n\n**-noI**\n> Disable inode compression\n\n**-noD**\n> Disable data compression\n\n**-noF**\n> Disable fragment compression\n\n**-noX**\n> Disable extended attribute compression\n\n**-no-compression**\n> Disable all compression\n\n**-noappend**\n> Do not append to existing filesystem; create a new one\n\n**-info**\n> Print files written to filesystem\n\n**-no-recovery**\n> Do not generate a recovery file\n\n**-recover _file_**\n> Recover filesystem using a recovery file\n\n**-xattrs**\n> Store extended attributes (default)\n\n**-no-xattrs**\n> Do not store extended attributes\n\n**-tar**\n> Read uncompressed tar archive from stdin\n\n**-quiet**\n> Suppress verbose output\n\n**-progress**\n> Display progress bar"],["DESCRIPTION","**mksquashfs** creates and appends to squashfs filesystems. Squashfs is a highly compressed, read-only filesystem commonly used for live CDs, embedded systems, and application packaging (AppImage, snap).\n\nThe tool compresses files, inodes, and directories using configurable algorithms. It supports deduplication, extended attributes, and efficient storage of sparse files. Block compression allows random access to compressed data."],["CAVEATS","Squashfs filesystems are read-only and require the squashfs kernel module. Appending to existing archives may increase fragmentation. Very high compression levels significantly increase creation time. Maximum file size depends on block size configuration."],["HISTORY","**Squashfs** was created by **Phillip Lougher** and first released in **2002**. It was merged into the Linux kernel mainline in version **2.6.29 (2009)**. The filesystem is widely used in Linux distributions for live media, container images, and read-only root filesystems."],["INSTALL","```apt: sudo apt install squashfs-tools```\n\n```dnf: sudo dnf install squashfs-tools```\n\n```pacman: sudo pacman -S squashfs-tools```\n\n```apk: sudo apk add squashfs-tools```"],["SEE ALSO","[unsquashfs](/man/unsquashfs)(1), [sqfscat](/man/sqfscat)(1), [mount](/man/mount)(8)"]],"e":["unsquashfs","sqfscat","mount"],"x":"mksquashfs [path/to/source1] [path/to/source2] [filesystem.squashfs]"},"mountpoint":{"n":"mountpoint","t":"checks whether a given directory or file is a filesystem mountpoint","s":[["TAGLINE","checks whether a given directory or file is a filesystem mountpoint"],["TLDR","**Check** if a directory is a mountpoint\n\n```mountpoint [path/to/directory]```\n\nCheck **quietly** (no output, exit code only)\n\n```mountpoint -q [path/to/directory]```\n\nShow **major/minor numbers** of filesystem\n\n```mountpoint -d [path/to/directory]```\n\nShow major/minor numbers for a **block device**\n\n```mountpoint -x [/dev/sda1]```"],["SYNOPSIS","**mountpoint** [**-d**|**-q**] _directory_|_file_\n**mountpoint** **-x** _device_"],["PARAMETERS","**-d, --fs-devno**\n> Display major/minor device numbers for the mounted filesystem\n\n**-q, --quiet**\n> Suppress all output; use exit code only\n\n**--nofollow**\n> Do not follow symbolic links at final path component\n\n**-x, --devno**\n> Output major/minor numbers for a block device\n\n**-h, --help**\n> Display help and exit\n\n**-V, --version**\n> Display version and exit"],["DESCRIPTION","**mountpoint** checks whether a given directory or file is a filesystem mountpoint by examining /proc/self/mountinfo. It is commonly used in shell scripts to verify mount status before performing operations.\n\nThe command returns exit code 0 if the path is a mountpoint, and exit code 32 if it is not. Exit code 1 indicates usage errors or system failures."],["CAVEATS","The command checks /proc/self/mountinfo, so results depend on the mount namespace of the calling process. Bind mounts may produce different results than expected. Setting LIBMOUNT_DEBUG=all enables debugging output."],["HISTORY","**mountpoint** was originally part of **sysvinit** and is now maintained as part of **util-linux** by Karel Zak. It provides a reliable method for scripts to check mount status without parsing mount output."],["INSTALL","```apt: sudo apt install util-linux```\n\n```dnf: sudo dnf install util-linux```\n\n```pacman: sudo pacman -S util-linux```\n\n```apk: sudo apk add util-linux-misc```\n\n```zypper: sudo zypper install util-linux```\n\n```brew: brew install util-linux```\n\n```nix: nix profile install nixpkgs#util-linux```"],["SEE ALSO","[mount](/man/mount)(8), [umount](/man/umount)(8), [findmnt](/man/findmnt)(8)"]],"e":["mount","umount","findmnt"],"x":"mountpoint [path/to/directory]"},"ncmpcpp":{"n":"ncmpcpp","t":"ncurses-based MPD client","s":[["TAGLINE","ncurses-based MPD client"],["TLDR","**Start ncmpcpp**\n\n```ncmpcpp```\n\n**Connect to specific host**\n\n```ncmpcpp -h [localhost] -p [6600]```\n\n**Start with specific screen**\n\n```ncmpcpp -s [playlist]```\n\n**Show current song**\n\n```ncmpcpp --current-song```"],["SYNOPSIS","**ncmpcpp** [_options_]"],["DESCRIPTION","**ncmpcpp** is an ncurses-based MPD (Music Player Daemon) client. It provides a feature-rich interface for controlling MPD, browsing music, managing playlists, and visualizing audio.\n\nThe tool offers vim-like keybindings, a spectrum visualizer, and extensive customization options."],["PARAMETERS","**-h** _host_\n> MPD server host.\n\n**-p** _port_\n> MPD server port.\n\n**-c** _file_\n> Configuration file.\n\n**-b** _file_, **--bindings** _file_\n> Use a specified key bindings file.\n\n**-s** _screen_, **--screen** _screen_\n> Starting screen (playlist, browser, search_engine, media_library, playlist_editor, tag_editor, outputs, visualizer, clock).\n\n**-S** _screen_, **--slave-screen** _screen_\n> Starting slave screen.\n\n**--current-song**\n> Print current song info and exit.\n\n**-q**, **--quiet**\n> Suppress messages.\n\n**-v**, **--version**\n> Display version information."],["KEY BINDINGS","**1-8**: Switch screens\n**Enter**: Play/select\n**p**: Pause\n**s**: Stop\n**>/<**: Next/Previous\n**f/b**: Seek forward/back\n**+/-**: Volume\n**/**: Search\n**u**: Update database"],["SCREENS","Playlist, Browser, Search, Media library, Lyrics, Visualizer, Clock"],["CAVEATS","Requires running MPD instance. Configuration can be complex. Some features need additional setup. Visualizer needs FIFO output."],["HISTORY","**ncmpcpp** stands for \"NCurses Music Player Client (Plus Plus)\", created as an enhanced version of ncmpc. Written in C++ by **Andrzej Rybczak**, it adds features like lyrics fetching and visualization."],["INSTALL","```apt: sudo apt install ncmpcpp```\n\n```dnf: sudo dnf install ncmpcpp```\n\n```pacman: sudo pacman -S ncmpcpp```\n\n```apk: sudo apk add ncmpcpp```\n\n```zypper: sudo zypper install ncmpcpp```\n\n```brew: brew install ncmpcpp```\n\n```nix: nix profile install nixpkgs#ncmpcpp```"],["SEE ALSO","[mpd](/man/mpd)(1), [mpc](/man/mpc)(1), [ncmpc](/man/ncmpc)(1), [cmus](/man/cmus)(1)"]],"e":["mpd","mpc","ncmpc","cmus"],"x":"ncmpcpp"},"nixos-option":{"n":"nixos-option","t":"inspects NixOS configuration values, showing what options are set and their","s":[["TAGLINE","inspects NixOS configuration values, showing what options are set and their"],["TLDR","List all **subkeys** of a given option key\n\n```nixos-option [option_key]```\n\nList current **boot kernel modules**\n\n```nixos-option boot.kernelModules```\n\nList **authorized keys** for a specific user\n\n```nixos-option users.users.[username].openssh.authorizedKeys.keys```\n\nList all **remote builders**\n\n```nixos-option nix.buildMachines```\n\nList subkeys on **another NixOS configuration**\n\n```NIXOS_CONFIG=[path/to/configuration.nix] nixos-option [option_key]```\n\nShow **recursively all values** of a user\n\n```nixos-option -r users.users.[user]```"],["SYNOPSIS","**nixos-option** [_options_] _option_path_"],["PARAMETERS","**-r, --recursive**\n> Show all nested values recursively\n\n**_option_path_**\n> Dot-separated path to the configuration option\n\n**NIXOS_CONFIG**\n> Environment variable to specify an alternative configuration file"],["DESCRIPTION","**nixos-option** inspects NixOS configuration values, showing what options are set and their current values. It navigates the hierarchical NixOS module system using dot notation (e.g., services.nginx.enable).\n\nThe tool displays the type, default value, description, and current value of configuration options. For container options (like services.* or users.*), it lists available subkeys.\n\nThis is useful for exploring available NixOS options, debugging configuration issues, and understanding the current system state as defined by the Nix configuration."],["CAVEATS","Evaluates the Nix configuration, which can take time on complex systems. Only shows options from the NixOS module system; arbitrary Nix expressions are not inspectable. Some dynamic options may not be fully discoverable."],["INSTALL","```nix: nix profile install nixpkgs#nixos-option```"],["SEE ALSO","[nixos-rebuild](/man/nixos-rebuild)(8), [nix-env](/man/nix-env)(1), [configuration.nix](/man/configuration.nix)(5)"]],"e":["nixos-rebuild","nix-env","configuration.nix"],"x":"nixos-option [option_key]"},"opencode-uninstall":{"n":"opencode-uninstall","t":"Uninstall opencode","s":[["TAGLINE","Uninstall opencode"],["TLDR","**Uninstall opencode and remove all related files**\n\n```opencode uninstall```\n\n**Preview what would be removed without changing anything**\n\n```opencode uninstall --dry-run```\n\n**Uninstall but keep configuration files**\n\n```opencode uninstall --keep-config```\n\n**Uninstall but keep session data and snapshots**\n\n```opencode uninstall --keep-data```\n\n**Skip confirmation prompts**\n\n```opencode uninstall --force```"],["SYNOPSIS","**opencode uninstall** [_options_]"],["PARAMETERS","**-c**, **--keep-config**\n> Keep configuration files. Default: false.\n\n**-d**, **--keep-data**\n> Keep session data and snapshots. Default: false.\n\n**--dry-run**\n> Show what would be removed without removing anything. Default: false.\n\n**-f**, **--force**\n> Skip confirmation prompts. Default: false.\n\n**--print-logs**\n> Print logs to stderr.\n\n**--log-level** _LEVEL_\n> Log level: `DEBUG`, `INFO`, `WARN`, or `ERROR`.\n\n**-h**, **--help**\n> Show help.\n\n**-v**, **--version**\n> Show version number."],["DESCRIPTION","**opencode uninstall** removes the opencode binary and, by default, its supporting files from the current system: configuration, cached data, and session history. The `--keep-config` and `--keep-data` flags preserve specific categories of files, which is useful when reinstalling or switching channels.\n\nUse `--dry-run` first to audit exactly which paths will be deleted before running the real uninstall."],["CAVEATS","Without `--keep-config` or `--keep-data`, this operation is **irreversible**: local session data, configuration, and cached credentials are permanently deleted. If opencode was installed via a package manager (e.g. Homebrew), uninstall it through that manager instead."],["INSTALL","```pacman: sudo pacman -S opencode```\n\n```brew: brew install opencode```\n\n```nix: nix profile install nixpkgs#opencode```"],["SEE ALSO","[opencode](/man/opencode)(1), [opencode-upgrade](/man/opencode-upgrade)(1), [opencode-models](/man/opencode-models)(1)"]],"e":["opencode","opencode-upgrade","opencode-models"],"x":"opencode uninstall"},"particle":{"n":"particle","t":"controls Particle IoT devices","s":[["TAGLINE","controls Particle IoT devices"],["TLDR","**Login to Particle**\n\n```particle login```\n\n**List devices**\n\n```particle list```\n\n**Flash device**\n\n```particle flash [device_name] [firmware.bin]```\n\n**Compile locally**\n\n```particle compile [photon] [project_dir]```\n\n**Monitor serial output**\n\n```particle serial monitor```\n\n**Call device function**\n\n```particle call [device_name] [function] [arg]```\n\n**Get device variable**\n\n```particle get [device_name] [variable]```\n\n**Setup new device**\n\n```particle setup```"],["SYNOPSIS","**particle** [_options_] _command_ [_args_]"],["PARAMETERS","**login**\n> Authenticate with Particle Cloud.\n\n**list**\n> List claimed devices.\n\n**flash**\n> Flash firmware to device.\n\n**compile**\n> Compile project.\n\n**serial**\n> Serial port commands.\n\n**call**\n> Call device function.\n\n**get**\n> Get device variable.\n\n**setup**\n> Setup new device.\n\n**subscribe**\n> Subscribe to events.\n\n**publish**\n> Publish event.\n\n**webhook**\n> Manage webhooks."],["DESCRIPTION","**particle** controls Particle IoT devices. It manages device firmware, cloud functions, and variables.\n\nDevices include Photon, Argon, Boron, and Electron boards. Cloud connectivity enables remote management.\n\nCompilation can happen locally or in Particle Cloud. Flashing sends firmware over-the-air or via USB.\n\nFunctions and variables expose device capabilities. Events enable pub/sub messaging between devices.\n\nSerial monitoring shows device debug output. Useful during development and troubleshooting."],["CAVEATS","Requires Particle Cloud account. Some features need device to be online. OTA flash requires stable connection."],["HISTORY","**Particle** (formerly Spark) was founded in **2012** through a Kickstarter campaign. The CLI provides developer tools for their IoT platform and device ecosystem."],["SEE ALSO","[arduino-cli](/man/arduino-cli)(1), [platformio](/man/platformio)(1), [esptool](/man/esptool)(1)"]],"e":["arduino-cli","platformio","esptool"],"x":"particle login"},"pct-create":{"n":"pct-create","t":"creates a new LXC container in Proxmox VE from a template image","s":[["TAGLINE","creates a new LXC container in Proxmox VE from a template image"],["TLDR","**Create** container with disk size\n\n```pct create 100 local:vztmpl/distro.tar.zst --rootfs local-lvm:4```\n\nCreate with **memory** limit\n\n```pct create 100 local:vztmpl/distro.tar.zst --rootfs local-lvm:4 --memory 8192```\n\nCreate with **hostname** and password\n\n```pct create 100 local:vztmpl/distro.tar.zst --rootfs local-lvm:4 --hostname name --password pass```\n\nCreate with **network**\n\n```pct create 100 local:vztmpl/distro.tar.zst --rootfs local-lvm:4 --net0 name=eth0,bridge=vmbr0,ip=dhcp```\n\nCreate **unprivileged** container with CPU and swap limits\n\n```pct create 100 local:vztmpl/distro.tar.zst --rootfs local-lvm:4 --unprivileged 1 --cores 2 --swap 512```\n\nCreate and **start** immediately\n\n```pct create 100 local:vztmpl/distro.tar.zst --rootfs local-lvm:4 --start```"],["SYNOPSIS","**pct create** _vmid_ _template_ [_OPTIONS_]"],["DESCRIPTION","**pct create** creates a new LXC container in Proxmox VE from a template image. It configures storage, networking, resources, and other container properties during creation."],["PARAMETERS","**vmid**\n> The numeric ID for the new container\n\n**template**\n> Path to the container template (e.g., local:vztmpl/distro.tar.zst)\n\n**--rootfs** _storage:size_\n> Root filesystem storage and size in GB\n\n**--memory** _megabytes_\n> Memory limit in MB\n\n**--hostname** _name_\n> Container hostname\n\n**--password** _password_\n> Root password for the container\n\n**--net**_N_ _config_\n> Network interface configuration\n\n**--cores** _number_\n> Number of CPU cores assigned to the container\n\n**--swap** _megabytes_\n> Swap memory limit in MB (default: 512)\n\n**--unprivileged** _0|1_\n> Create as unprivileged container (1, recommended for security)\n\n**--ssh-public-keys** _filepath_\n> SSH public keys for root user authentication\n\n**--features** _list_\n> Enable features like nesting, keyctl, fuse, mknod\n\n**--start** _0|1_\n> Start container after creation (default: 0).\n\n**--onboot** _0|1_\n> Start container during system bootup (default: 0).\n\n**--storage** _storage_\n> Default storage for the container (default: local).\n\n**--nameserver** _ip_\n> DNS server IP address for the container.\n\n**--ostype** _type_\n> OS type (e.g., debian, ubuntu, centos, alpine, archlinux, fedora, gentoo, opensuse, unmanaged)."],["CAVEATS","Templates must be downloaded or uploaded to Proxmox storage first. The container ID must be unique across the cluster. Enable nesting for containers that run Docker or other containers inside."],["HISTORY","**pct create** is part of the **Proxmox VE** virtualization platform for managing LXC containers."],["SEE ALSO","[pct](/man/pct)(1), [pct-clone](/man/pct-clone)(1), [pct-enter](/man/pct-enter)(1), [pct-destroy](/man/pct-destroy)(1)"]],"e":["pct","pct-clone","pct-enter","pct-destroy"],"x":"pct create 100 local:vztmpl/distro.tar.zst --rootfs local-lvm:4"},"pct-start":{"n":"pct-start","t":"starts a Proxmox LXC container","s":[["TAGLINE","starts a Proxmox LXC container"],["TLDR","**Start** a container\n\n```pct start 100```\n\nStart with **verbose debug** logging\n\n```pct start 100 --debug```"],["SYNOPSIS","**pct start** _vmid_ [_OPTIONS_]"],["PARAMETERS","**vmid**\n> The numeric ID of the container (100–999999999)\n\n**--debug**\n> Enable very verbose debug log-level on start (default off)\n\n**--skiplock**\n> Ignore locks; only root is allowed to use this option"],["DESCRIPTION","**pct start** starts a Proxmox VE LXC container that is currently stopped. It applies pending configuration changes, sets up the container environment, and launches the guest init process.\n\nIf startup fails, **--debug** produces more detailed logs. You can also start in foreground/debug mode via related LXC tooling when diagnosing boot issues."],["CAVEATS","The container must not already be running. Locks from other **pct** operations (backup, migrate, snapshot, and so on) can block start unless **--skiplock** is used carefully by root. Resource limits (memory, CPU, storage) and missing volumes can cause start to fail."],["HISTORY","**pct start** is part of the **Proxmox VE** **pct** tool for managing LXC containers."],["SEE ALSO","[pct](/man/pct)(1), [pct-stop](/man/pct-stop)(1), [pct-shutdown](/man/pct-shutdown)(1), [pct-status](/man/pct-status)(1), [pct-reboot](/man/pct-reboot)(1)"],["RESOURCES","```[Documentation](https://pve.proxmox.com/pve-docs/pct.1.html)```\n\n```[Homepage](https://www.proxmox.com)```"]],"e":["pct","pct-stop","pct-shutdown","pct-status","pct-reboot"],"x":"pct start 100"},"pgmoil":{"n":"pgmoil","t":"applies an oil painting effect to PGM images by selecting the most common gray","s":[["TAGLINE","applies an oil painting effect to PGM images by selecting the most common gray"],["TLDR","**Apply oil painting effect**\n\n```pgmoil [input.pgm] > [output.pgm]```\n\n**Specify smear radius**\n\n```pgmoil -n [3] [input.pgm] > [output.pgm]```"],["SYNOPSIS","**pgmoil** [_options_] [_file_]"],["PARAMETERS","**-n** _radius_\n> Smear radius (default 3)."],["DESCRIPTION","**pgmoil** applies an oil painting effect to PGM images by selecting the most common gray value in a neighborhood. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pgmbentley](/man/pgmbentley)(1), [ppmspread](/man/ppmspread)(1)"]],"e":["pgmbentley","ppmspread"],"x":"pgmoil [input.pgm] > [output.pgm]"},"pnpm-audit":{"n":"pnpm-audit","t":"Check pnpm dependencies for vulnerabilities","s":[["TAGLINE","Check pnpm dependencies for vulnerabilities"],["TLDR","**Run security audit**\n\n```pnpm audit```\n\n**Fix vulnerabilities**\n\n```pnpm audit --fix```\n\n**JSON output**\n\n```pnpm audit --json```\n\n**Audit production only**\n\n```pnpm audit --prod```"],["SYNOPSIS","**pnpm audit** [_options_]"],["PARAMETERS","**--fix**\n> Add overrides to **package.json** that pin vulnerable transitive dependencies to safe versions.\n\n**--json**\n> Output the audit report as JSON.\n\n**-P**, **--prod**\n> Audit only production dependencies (skip _devDependencies_).\n\n**-D**, **--dev**\n> Audit only development dependencies.\n\n**--no-optional**\n> Skip optional dependencies during the audit.\n\n**--audit-level** _LEVEL_\n> Minimum severity to report: _low_, _moderate_, _high_, or _critical_ (default: _low_).\n\n**--ignore** _CVE_\n> Suppress reporting for a specific advisory by its identifier.\n\n**--ignore-unfixable**\n> Skip advisories that have no available patch.\n\n**--ignore-registry-errors**\n> Exit with code 0 when the registry returns a non-200 status, only failing if real vulnerabilities are found."],["DESCRIPTION","**pnpm audit** scans project dependencies for known security vulnerabilities using the npm advisory database. It reports affected packages, severity levels, and available patched versions.\n\nThe **--fix** option attempts to automatically update vulnerable packages to safe versions. Use **--prod** or **--dev** to limit scanning to production or development dependencies respectively. The **--audit-level** option sets the minimum severity threshold for reporting (low, moderate, high, critical)."],["CAVEATS","Requires network access. Uses npm advisory database."],["HISTORY","pnpm audit was added for **security vulnerability** scanning in dependencies."],["INSTALL","```dnf: sudo dnf install pnpm```\n\n```pacman: sudo pacman -S pnpm```\n\n```apk: sudo apk add pnpm```\n\n```zypper: sudo zypper install pnpm```\n\n```brew: brew install pnpm```\n\n```nix: nix profile install nixpkgs#pnpm```"],["SEE ALSO","[pnpm](/man/pnpm)(1), [npm-audit](/man/npm-audit)(1)"]],"e":["pnpm","npm-audit"],"x":"pnpm audit"},"pwck":{"n":"pwck","t":"Verify password file integrity and consistency","s":[["TAGLINE","Verify password file integrity and consistency"],["TLDR","**Check password file integrity**\n\n```pwck```\n\n**Check specific files**\n\n```pwck [/etc/passwd] [/etc/shadow]```\n\n**Read-only check**\n\n```pwck -r```\n\n**Quiet mode**\n\n```pwck -q```"],["SYNOPSIS","**pwck** [_options_] [_passwd_] [_shadow_]"],["PARAMETERS","_PASSWD_\n> Password file path.\n\n_SHADOW_\n> Shadow file path.\n\n**-r**\n> Read-only mode.\n\n**-q**\n> Quiet output.\n\n**-s**\n> Sort by UID."],["DESCRIPTION","**pwck** verifies the integrity of **/etc/passwd** and **/etc/shadow** files by checking for proper formatting, valid field values, and internal consistency. It detects problems like duplicate usernames, invalid UIDs/GIDs, missing home directories, and entries that don't match between the password and shadow files.\n\nBy default pwck prompts to delete invalid entries, but the **-r** flag runs in read-only mode for auditing without making changes. The command is part of **shadow-utils** and is typically run by administrators after manual edits to password files or as part of system health checks."],["CAVEATS","Requires root for shadow file. System administration tool."],["HISTORY","pwck is a **shadow-utils** utility for password file verification."],["INSTALL","```apt: sudo apt install passwd```\n\n```pacman: sudo pacman -S shadow```\n\n```apk: sudo apk add shadow```\n\n```zypper: sudo zypper install shadow```\n\n```nix: nix profile install nixpkgs#shadow```"],["SEE ALSO","[grpck](/man/grpck)(8), [passwd](/man/passwd)(1)"]],"e":["grpck","passwd"],"x":"pwck"},"py3versions":{"n":"py3versions","t":"List installed Python 3 interpreter versions","s":[["TAGLINE","List installed Python 3 interpreter versions"],["TLDR","**List installed Python 3 versions**\n\n```py3versions -i```\n\n**Show supported versions**\n\n```py3versions -s```\n\n**Show default version**\n\n```py3versions -d```\n\n**Show version numbers only** (combine with -i, -s, or -d)\n\n```py3versions -v -i```"],["SYNOPSIS","**py3versions** [_options_]"],["PARAMETERS","**-i**, **--installed**\n> Show installed supported python3 versions.\n\n**-s**, **--supported**\n> Show supported python3 versions for this distribution.\n\n**-d**, **--default**\n> Show the default python3 version.\n\n**-r**, **--requested**\n> Read the X-Python3-Version field from debian/control and show matching versions.\n\n**-v**, **--version**\n> Limit output to version numbers only (e.g., 3.10 instead of python3.10)."],["DESCRIPTION","**py3versions** reports information about Python 3 interpreter versions available on Debian and Ubuntu systems. It distinguishes between installed versions, supported versions for the distribution, and the default version used by the system.\n\nThe tool is part of Debian's **python3-minimal** package and is primarily used by packaging tools and maintainer scripts to determine which Python versions to compile against or clean up. The **-i** flag shows installed versions, **-s** shows supported versions, and **-d** shows the default. The supported list is ordered lowest to highest with the default version last."],["CAVEATS","Debian/Ubuntu specific. Part of python3-minimal."],["HISTORY","py3versions is a **Debian utility** for Python 3 version management."],["INSTALL","```apt: sudo apt install python3-minimal```"],["SEE ALSO","[python3](/man/python3)(1), [pyversions](/man/pyversions)(1), [update-alternatives](/man/update-alternatives)(1)"]],"e":["python3","pyversions","update-alternatives"],"x":"py3versions -i"},"pydoc2.7":{"n":"pydoc2.7","t":"Python 2.7 documentation viewer","s":[["TAGLINE","Python 2.7 documentation viewer"],["TLDR","**View Python 2.7 documentation**\n\n```pydoc2.7 [module]```\n\n**Search documentation**\n\n```pydoc2.7 -k [keyword]```\n\n**Start documentation server**\n\n```pydoc2.7 -p [8080]```"],["SYNOPSIS","**pydoc2.7** [_options_] [_module_]"],["PARAMETERS","_MODULE_\n> Module to document.\n\n**-k** _KEYWORD_\n> Search keyword.\n\n**-p** _PORT_\n> Start HTTP server.\n\n**-w**\n> Write HTML file."],["DESCRIPTION","**pydoc2.7** is the Python 2.7 version of the pydoc documentation tool. It displays module documentation, searches installed packages, and can serve documentation via a local HTTP server, specifically for the Python 2.7 interpreter and its installed libraries.\n\nThis version-specific binary exists on systems with multiple Python versions installed. Since Python 2 has reached end of life, users should migrate to **pydoc3** for active Python development."],["CAVEATS","Python 2 deprecated. Use pydoc3 instead."],["HISTORY","pydoc2.7 is **Python 2.7's** documentation tool."],["SEE ALSO","[pydoc](/man/pydoc)(1), [pydoc3](/man/pydoc3)(1), [python2.7](/man/python2.7)(1)"]],"e":["pydoc","pydoc3","python2.7"],"x":"pydoc2.7 [module]"},"redis-sentinel":{"n":"redis-sentinel","t":"High availability monitoring for Redis clusters","s":[["TAGLINE","High availability monitoring for Redis clusters"],["TLDR","**Start** Sentinel with a config file\n\n```redis-sentinel [/etc/redis/sentinel.conf]```\n\n**Start** via redis-server in sentinel mode\n\n```redis-server [sentinel.conf] --sentinel```\n\n**List** all monitored masters\n\n```redis-cli -p [26379] sentinel masters```\n\n**Get** current master address by name\n\n```redis-cli -p [26379] sentinel get-master-addr-by-name [mymaster]```\n\n**Trigger** manual failover\n\n```redis-cli -p [26379] sentinel failover [mymaster]```"],["SYNOPSIS","**redis-sentinel** _config_file_\n\n**redis-server** _config_file_ **--sentinel**"],["DESCRIPTION","**Redis Sentinel** provides high availability for Redis deployments through continuous monitoring, automatic failover, and notification. It watches master and replica instances, and when a master becomes unreachable and a quorum of Sentinel processes agree it is down, it automatically promotes a replica to master and reconfigures the remaining replicas to use the new master.\n\nSentinel also acts as a configuration provider, allowing clients to discover the current master address for a named service. Multiple Sentinel instances (at least three are recommended) form a distributed system that reaches consensus on failover decisions, preventing split-brain scenarios.\n\nA configuration file is mandatory; Sentinel rewrites it on failover to persist the new topology."],["PARAMETERS","**--sentinel**\n> When passed to `redis-server`, starts it in Sentinel mode using the provided config file."],["CAVEATS","At least three Sentinel instances are recommended for robust quorum. The default Sentinel port is 26379. Sentinel modifies its configuration file on failover. Sentinel does not shard data — it only monitors and redirects clients."],["HISTORY","Redis Sentinel was introduced in **Redis 2.4** (stable in **2.8**) by **Salvatore Sanfilippo** to provide high availability without manual intervention."],["INSTALL","```apt: sudo apt install redis-sentinel```\n\n```apk: sudo apk add valkey-compat```"],["SEE ALSO","[redis-server](/man/redis-server)(1), [redis-cli](/man/redis-cli)(1)"]],"e":["redis-server","redis-cli"],"x":"redis-sentinel [/etc/redis/sentinel.conf]"},"rtgen":{"n":"rtgen","t":"Generate rainbow tables for password cracking","s":[["TAGLINE","Generate rainbow tables for password cracking"],["TLDR","**Generate rainbow table**\n\n```rtgen [hash_algorithm] [charset] [min_len] [max_len] [table_index] [chain_len] [chain_num] [part_index]```\n\n**Generate MD5 table**\n\n```rtgen md5 loweralpha 1 7 0 2400 67108864 0```\n\n**Generate NTLM table**\n\n```rtgen ntlm numeric 1 8 0 2400 67108864 0```\n\n**Sort table**\n\n```rtsort [table.rt]```"],["SYNOPSIS","**rtgen** _algorithm_ _charset_ _min_ _max_ _index_ _chain_len_ _chain_num_ _part_"],["PARAMETERS","**algorithm**\n> Hash type (md5, sha1, ntlm, lm).\n\n**charset**\n> Character set (loweralpha, numeric, etc.).\n\n**min_len**\n> Minimum password length.\n\n**max_len**\n> Maximum password length.\n\n**table_index**\n> Table index for multiple tables.\n\n**chain_len**\n> Reduction chain length.\n\n**chain_num**\n> Number of chains.\n\n**part_index**\n> Part index."],["DESCRIPTION","**rtgen** generates rainbow tables for password cracking. Rainbow tables are precomputed tables for reversing cryptographic hash functions. Part of RainbowCrack."],["EXAMPLES","```bash"],["Generate MD5 lowercase table","rtgen md5 loweralpha 1 7 0 2400 67108864 0"],["NTLM numeric","rtgen ntlm numeric 1 8 0 2400 33554432 0"],["Sort generated table","rtsort md5_loweralpha#1-7_0_2400x67108864_0.rt"],["Use with rcrack","rcrack . -h 5d41402abc4b2a76b9719d911017c592\n```"],["CHARSETS","```\nnumeric           - 0-9\nloweralpha        - a-z\nalpha             - a-zA-Z\nloweralpha-numeric - a-z0-9\nmixalpha-numeric  - a-zA-Z0-9\n```"],["CAVEATS","Very storage intensive. Generation takes time. For authorized testing only. Part of RainbowCrack project."],["HISTORY","rtgen is part of **RainbowCrack** by **Zhu Shuanglei**, implementing Philippe Oechslin's faster time-memory trade-off."],["SEE ALSO","[rcrack](/man/rcrack)(1), [john](/man/john)(1), [hashcat](/man/hashcat)(1)"]],"e":["rcrack","john","hashcat"],"x":"rtgen [hash_algorithm] [charset] [min_len] [max_len] [table_index] [chain_len] [chain_num] [part_index]"},"sampler":{"n":"sampler","t":"Terminal dashboard from shell commands with YAML configuration","s":[["TAGLINE","Terminal dashboard from shell commands with YAML configuration"],["TLDR","**Launch a dashboard with a config file**\n\n```sampler -c [config.yml]```\n\n**Launch with environment variables**\n\n```sampler -c [config.yml] -e \"[VAR=value]\"```\n\n**Launch with multiple environment variables**\n\n```sampler -c [config.yml] -e \"[VAR1=value1]\" -e \"[VAR2=value2]\"```"],["SYNOPSIS","**sampler** **-c** _config.yml_ [_options_]"],["PARAMETERS","**-c** _FILE_\n> Path to the YAML configuration file.\n\n**-e** _VAR=VALUE_\n> Set environment variables for the dashboard."],["DESCRIPTION","**sampler** executes shell commands at configurable intervals and visualizes their output as runcharts, sparklines, barcharts, gauges, and text boxes in the terminal. It supports trigger-based alerts (visual, sound, terminal bell, or arbitrary shell commands) and works with databases, message queues, SSH, and JMX out of the box."],["CAVEATS","Windows support is experimental. Linux requires **libasound2-dev** for audio alerts."],["HISTORY","**sampler** was created by **Alexander Lukyanchikov** (sqshq) and is written in **Go**."],["INSTALL","```brew: brew install sampler```\n\n```nix: nix profile install nixpkgs#sampler```"],["SEE ALSO","[glances](/man/glances)(1), [htop](/man/htop)(1)"]],"e":["glances","htop"],"x":"sampler -c [config.yml]"},"st-info":{"n":"st-info","t":"","s":[["TLDR","**Show device info**\n\n```st-info --probe```\n\n**Show flash size**\n\n```st-info --flash```\n\n**Show SRAM size**\n\n```st-info --sram```\n\n**Show chip ID**\n\n```st-info --chipid```\n\n**Show description**\n\n```st-info --descr```\n\n**Show serial number**\n\n```st-info --serial```"],["SYNOPSIS","**st-info** [_--probe_] [_--flash_] [_--sram_] [_options_]"],["PARAMETERS","**--probe**\n> Probe connected device.\n\n**--flash**\n> Flash memory size.\n\n**--sram**\n> SRAM size.\n\n**--chipid**\n> Chip identifier.\n\n**--descr**\n> Device description.\n\n**--serial**\n> Programmer serial."],["DESCRIPTION","**st-info** queries information from STM32 microcontrollers connected via an ST-Link programmer/debugger. It reports device identification details including chip ID, device description, flash memory size, SRAM size, and the programmer's serial number.\n\nThe tool is useful for verifying hardware connections, identifying unknown STM32 variants, and confirming memory sizes before programming. The **--probe** option provides a comprehensive summary of all connected devices. It is part of the open-source stlink toolset."],["CAVEATS","ST-Link required. Device must be connected. STM32 only."],["HISTORY","**st-info** is part of **stlink**, providing device information for STM32 microcontrollers connected via ST-Link."],["INSTALL","```apt: sudo apt install stlink-tools```\n\n```dnf: sudo dnf install stlink```\n\n```pacman: sudo pacman -S stlink```\n\n```apk: sudo apk add stlink```\n\n```zypper: sudo zypper install stlink```\n\n```brew: brew install stlink```\n\n```nix: nix profile install nixpkgs#stlink```"],["SEE ALSO","[st-flash](/man/st-flash)(1), [st-util](/man/st-util)(1), [openocd](/man/openocd)(1)"]],"e":["st-flash","st-util","openocd"],"x":"st-info --probe"},"svg2png":{"n":"svg2png","t":"Rasterize SVG vector graphics to PNG","s":[["TAGLINE","Rasterize SVG vector graphics to PNG"],["TLDR","**Convert SVG to PNG**\n\n```svg2png [input.svg] [output.png]```\n\n**With specific width**\n\n```svg2png -w [800] [input.svg] [output.png]```\n\n**With specific height**\n\n```svg2png -h [600] [input.svg] [output.png]```\n\n**Scale factor**\n\n```svg2png --scale [2] [input.svg] [output.png]```"],["SYNOPSIS","**svg2png** [_-w width_] [_-h height_] [_options_] _input_ _output_"],["PARAMETERS","**-w** _WIDTH_\n> Output width.\n\n**-h** _HEIGHT_\n> Output height.\n\n**--scale** _FACTOR_\n> Scale multiplier.\n\n**--background** _COLOR_\n> Background color.\n\n**--help**\n> Show help."],["DESCRIPTION","**svg2png** converts SVG vector graphics files to PNG raster images. It rasterizes the scalable vector content at a specified resolution, producing pixel-based output suitable for web use, documentation, or any context requiring bitmap images.\n\nOutput dimensions can be controlled by specifying width, height, or a scale factor relative to the original SVG viewport size. The background is transparent by default, preserving alpha channel information from the SVG source. Various implementations exist based on different rendering libraries such as librsvg or Node.js-based engines."],["CAVEATS","Various implementations exist. Rendering may vary. librsvg or Node based."],["HISTORY","**svg2png** tools exist in various implementations for converting SVG vector graphics to PNG raster format."],["INSTALL","```apt: sudo apt install mapnik-utils```\n\n```dnf: sudo dnf install mapnik-utils```\n\n```brew: brew install svg2png```"],["SEE ALSO","[inkscape](/man/inkscape)(1), [convert](/man/convert)(1), [rsvg-convert](/man/rsvg-convert)(1)"]],"e":["inkscape","convert","rsvg-convert"],"x":"svg2png [input.svg] [output.png]"},"sxiv":{"n":"sxiv","t":"Simple lightweight X11 image viewer","s":[["TAGLINE","Simple lightweight X11 image viewer"],["TLDR","**Open** an image\n\n```sxiv path/to/image```\n\nOpen in **fullscreen** mode\n\n```sxiv -f path/to/image```\n\nRead filenames from **stdin**\n\n```echo path/to/file | sxiv -i```\n\nOpen as **slideshow**\n\n```sxiv -S 3 path/to/image1 path/to/image2```\n\nOpen in **thumbnail** mode\n\n```sxiv -t path/to/images/*```\n\n**Recursive** directory search\n\n```sxiv -r path/to/directory```"],["SYNOPSIS","**sxiv** [_options_] _FILE_..."],["DESCRIPTION","**sxiv** is a simple image viewer for X. It has two modes: image mode for displaying individual images and thumbnail mode for selecting from a grid view. It supports keyboard navigation, zooming, rotation, and slideshow functionality."],["PARAMETERS","**-f**\n> Start in fullscreen mode\n\n**-b**\n> Do not show info bar at bottom\n\n**-t**\n> Start in thumbnail mode\n\n**-i**\n> Read file list from stdin\n\n**-r**\n> Search directories recursively for images\n\n**-o**\n> Write marked files to stdout on exit\n\n**-a**\n> Play animations of multi-frame images\n\n**-A FRAMERATE**\n> Set animation framerate\n\n**-s MODE**\n> Set scale mode (d=down, f=fit, F=fill, w=width, h=height)\n\n**-z ZOOM**\n> Set zoom level as percentage\n\n**-g GEOMETRY**\n> Set window geometry (WxH+X+Y)\n\n**-G GAMMA**\n> Set gamma correction (-32 to 32)\n\n**-S SECONDS**\n> Start slideshow with specified interval\n\n**-n NUM**\n> Start at image number NUM\n\n**-c**\n> Clean thumbnail cache\n\n**-q**\n> Suppress warnings"],["CAVEATS","Requires an EWMH/NetWM compliant window manager for fullscreen functionality. Thumbnail cache is stored in $XDG_CACHE_HOME/sxiv/. Custom key handlers can be defined in $XDG_CONFIG_HOME/sxiv/exec/key-handler."],["HISTORY","**sxiv** (Simple X Image Viewer) was created as a lightweight alternative to heavier image viewers, focusing on speed and keyboard-driven operation."],["INSTALL","```apt: sudo apt install sxiv```\n\n```dnf: sudo dnf install sxiv```\n\n```pacman: sudo pacman -S sxiv```\n\n```zypper: sudo zypper install sxiv```\n\n```nix: nix profile install nixpkgs#sxiv```"],["SEE ALSO","[feh](/man/feh)(1), [imv](/man/imv)(1), [nsxiv](/man/nsxiv)(1)"]],"e":["feh","imv","nsxiv"],"x":"sxiv path/to/image"},"systemctl-exit":{"n":"systemctl-exit","t":"Exit user or container service manager","s":[["TAGLINE","Exit user or container service manager"],["TLDR","Exit **user** service manager\n\n```systemctl exit --user```\n\nExit with specific **exit code**\n\n```systemctl exit [code] --user```\n\nExit **container** service manager\n\n```systemctl exit```"],["SYNOPSIS","**systemctl exit** [_EXIT_CODE_]"],["PARAMETERS","**--user**\n> Exit the user service manager\n\n_EXIT_CODE_\n> Optional exit code to return (integer)"],["DESCRIPTION","**systemctl exit** asks the service manager to quit. When used with `--user`, it terminates the per-user systemd instance and all user services. When used without `--user` in a container, it cleanly shuts down the container.\n\nOn a regular system without `--user`, this is equivalent to `systemctl poweroff`."],["CAVEATS","When exiting the user service manager, all user services are terminated. The exit code is passed to the process that started the service manager. Not typically useful on bare-metal systems outside of containers."],["HISTORY","The **exit** subcommand supports clean shutdown of containerized systemd instances and user service managers, enabling proper service termination instead of forceful kills."],["SEE ALSO","[systemctl](/man/systemctl)(1), [systemctl-poweroff](/man/systemctl-poweroff)(1)"]],"e":["systemctl","systemctl-poweroff"],"x":"systemctl exit --user"},"terminal-notifier":{"n":"terminal-notifier","t":"Send macOS notifications from command line","s":[["TAGLINE","Send macOS notifications from command line"],["TLDR","**Send a basic notification**\n\n```terminal-notifier -message \"[Task completed]\"```\n\n**Send with title and subtitle**\n\n```terminal-notifier -title \"[Build]\" -subtitle \"[Success]\" -message \"[All tests passed]\"```\n\n**Send with a sound**\n\n```terminal-notifier -message \"[Done]\" -sound default```\n\n**Send and open URL on click**\n\n```terminal-notifier -message \"[New issue]\" -open \"[https://github.com]\"```\n\n**Send and activate an app on click**\n\n```terminal-notifier -message \"[Sync complete]\" -activate [com.apple.AddressBook]```\n\n**Send from piped input**\n\n```echo \"[Build finished]\" | terminal-notifier```\n\n**Group notifications** (replace previous)\n\n```terminal-notifier -group \"[build]\" -message \"[Completed]\"```"],["SYNOPSIS","**terminal-notifier** [_-title title_] [_-subtitle subtitle_] [_-message message_] [_-sound name_] [_-group id_] [_-activate bundleid_] [_-open url_]"],["PARAMETERS","**-message** _text_\n> The notification message body (required unless piping)\n\n**-title** _text_\n> The notification title (default: \"Terminal\")\n\n**-subtitle** _text_\n> The notification subtitle\n\n**-sound** _name_\n> Play a sound from /System/Library/Sounds; use \"default\" for system default\n\n**-group** _id_\n> Group identifier; only one notification per group shown at a time\n\n**-activate** _bundleid_\n> Activate specified app when notification is clicked\n\n**-open** _url_\n> Open URL when notification is clicked\n\n**-remove** _id_\n> Remove notification with specified group ID\n\n**-list** _id_\n> List notifications with specified group ID"],["DESCRIPTION","**terminal-notifier** sends macOS User Notifications from the command line. It enables scripts and terminal commands to display native system notifications with customizable content, sounds, and click actions.\n\nNotifications can include titles, subtitles, message bodies, and sounds. Click actions can open URLs, activate applications, or execute callbacks. Group identifiers allow replacing previous notifications instead of stacking them.\n\nThe tool is useful for alerting when long-running processes complete, such as builds, downloads, or scheduled tasks."],["CAVEATS","Sticky notifications (alerts) require manual configuration in System Settings > Notifications > terminal-notifier. Action buttons are not supported; use **alerter** for that functionality. Requires macOS 10.10 or higher."],["HISTORY","terminal-notifier was created by **Eloy Durán** (julienXX) as an open-source tool to bring macOS notifications to command-line workflows. It became popular for integrating native notifications into development toolchains."],["INSTALL","```brew: brew install terminal-notifier```\n\n```nix: nix profile install nixpkgs#terminal-notifier```"],["SEE ALSO","[notify-send](/man/notify-send)(1)"]],"e":["notify-send"],"x":"terminal-notifier -message \"[Task completed]\""},"unicornscan":{"n":"unicornscan","t":"Asynchronous stateless network port scanner","s":[["TAGLINE","Asynchronous stateless network port scanner"],["TLDR","**Basic TCP SYN scan**\n\n```unicornscan [192.168.1.1]```\n\n**Scan specific ports**\n\n```unicornscan [192.168.1.1]:[22,80,443]```\n\n**Scan port range**\n\n```unicornscan [192.168.1.1]:[1-1000]```\n\n**UDP scan**\n\n```unicornscan -mU [192.168.1.1]```\n\n**Scan at specific rate**\n\n```unicornscan -r [1000] [192.168.1.1]```\n\n**Immediate verbose output**\n\n```unicornscan -Iv [192.168.1.1]```\n\n**Scan network range**\n\n```unicornscan [192.168.1.0/24]:[80,443]```\n\n**Save results to pcap**\n\n```unicornscan -w [scan.pcap] [192.168.1.1]```"],["SYNOPSIS","**unicornscan** [_options_] _target_[:_ports_]"],["PARAMETERS","**-m** _mode_\n> Scan mode: T (TCP SYN), U (UDP), sf (connect), A (ARP).\n\n**-r** _pps_\n> Packets per second rate.\n\n**-i** _interface_\n> Network interface to use.\n\n**-I**\n> Immediate mode: display results as found.\n\n**-v**\n> Verbose output.\n\n**-H**\n> Resolve hostnames in output.\n\n**-l** _file_\n> Log output to file.\n\n**-w** _file_\n> Write packets to pcap file.\n\n**-L** _seconds_\n> Packet timeout (default: 7).\n\n**-p** _ports_\n> Global ports to scan.\n\n**-B** _port_\n> Source port for scanning.\n\n**-b** _layer_\n> Send broken CRC: T (transport), N (network), TN (both).\n\n**-e** _module_\n> Enable module (e.g., osdetect).\n\n**-E**\n> Process error responses (ICMP, RST).\n\n**-Q**\n> Quiet mode.\n\n**-h**\n> Display help."],["SCAN MODES","**-mT**: TCP SYN scan (default)\n\n**-mTsA**: TCP ACK scan\n\n**-mTsF**: TCP FIN scan\n\n**-mTs**: TCP NULL scan\n\n**-mTsFPU**: TCP Xmas scan\n\n**-msf**: TCP connect scan\n\n**-mU**: UDP scan"],["DESCRIPTION","**unicornscan** is an asynchronous network scanner designed for high-speed reconnaissance. It uses a custom TCP/IP stack that separates packet sending and receiving into different threads, enabling much faster scanning than traditional scanners.\n\nThe scanner supports various TCP flag combinations, UDP scanning, and OS/service fingerprinting through loadable modules. Its asynchronous design makes it particularly effective for scanning large networks.\n\nOutput can be customized and saved to pcap files for later analysis. The tool integrates with databases for storing scan results."],["CAVEATS","Requires root privileges for raw socket access. High scan rates may trigger IDS/IPS. Custom TCP stack may behave differently than system stack. Use only with authorization on target networks."],["HISTORY","**unicornscan** was developed by Jack Louis and Robert E. Lee. It was designed for scalability and speed, addressing limitations of synchronous scanners. The tool became popular for penetration testing and is included in Kali Linux."],["INSTALL","```aur: yay -S unicornscan```"],["SEE ALSO","[nmap](/man/nmap)(1), [masscan](/man/masscan)(1), [zmap](/man/zmap)(1), [hping3](/man/hping3)(1)"]],"e":["nmap","masscan","zmap","hping3"],"x":"unicornscan [192.168.1.1]"},"unshar":{"n":"unshar","t":"Extract files from shell archives","s":[["TAGLINE","Extract files from shell archives"],["TLDR","**Extract a shell archive**\n\n```unshar [archive.shar]```\n\n**Extract to specific directory**\n\n```unshar -d [/path/to/directory] [archive.shar]```\n\n**Overwrite existing files**\n\n```unshar -c [archive.shar]```\n\n**Extract from compressed archive**\n\n```gzip -dc [archive.shar.gz] | unshar```\n\n**Extract multiple archives from one file**\n\n```unshar -e [combined.shar]```\n\n**Extract from email message**\n\n```unshar [message.txt]```"],["SYNOPSIS","**unshar** [_options_] [_file_...]"],["PARAMETERS","**-d**, **--directory** _dir_\n> Change to directory before extracting.\n\n**-c**, **--overwrite**\n> Pass -c to shar script to overwrite existing files.\n\n**-e**, **--exit-0**\n> Separate archives at \"exit 0\" lines.\n\n**-E**, **--split-at** _string_\n> Split archives at specified string.\n\n**-f**, **--force**\n> Force overwrite without prompting.\n\n**-h**, **--help**\n> Display help.\n\n**-V**, **--version**\n> Display version."],["DESCRIPTION","**unshar** extracts files from shell archives (shar files). It scans input looking for shell archive headers, then executes each archive through the shell to extract its contents.\n\nShell archives are self-extracting scripts that contain files encoded as shell commands. They were historically used for distributing source code via email before modern archive formats became standard.\n\nWhen processing email messages, unshar automatically locates and extracts embedded shell archives, ignoring mail headers and signatures. Multiple archives can be concatenated and extracted with the -e option."],["CAVEATS","Executes shell code from the archive, posing security risks with untrusted files. Always verify archive source and contents before extraction. Some archives may require specific shell features. Part of the sharutils package."],["HISTORY","**unshar** and shell archives date back to early Unix when email was the primary method of sharing software. The shar format allowed binary and text files to be encoded in a form that could survive email transmission. While largely superseded by tar and zip, shar archives still appear in legacy code distributions and mailing list archives."],["INSTALL","```apt: sudo apt install sharutils```\n\n```dnf: sudo dnf install sharutils```\n\n```pacman: sudo pacman -S sharutils```\n\n```apk: sudo apk add sharutils```\n\n```zypper: sudo zypper install sharutils```\n\n```nix: nix profile install nixpkgs#sharutils```"],["SEE ALSO","[shar](/man/shar)(1), [tar](/man/tar)(1), [unzip](/man/unzip)(1), [uudecode](/man/uudecode)(1)"]],"e":["shar","tar","unzip","uudecode"],"x":"unshar [archive.shar]"},"virt-sysprep":{"n":"virt-sysprep","t":"Prepare VM images for cloning","s":[["TAGLINE","Prepare VM images for cloning"],["TLDR","**List** available operations\n\n```virt-sysprep --list-operations```\n\nRemove **sensitive** data\n\n```sudo virt-sysprep -a [path/to/image.qcow2]```\n\n**Dry run** by domain name\n\n```sudo virt-sysprep -d [vm_name] -n```\n\nRun specific **operations**\n\n```sudo virt-sysprep -d [vm_name] --operations [operation1,operation2]```\n\nSet **hostname** and machine-id\n\n```sudo virt-sysprep -d [vm_name] --enable customizations --hostname [host_name] --operation machine-id```"],["SYNOPSIS","**virt-sysprep** [_OPTIONS_]"],["PARAMETERS","**-a, --add** _IMAGE_\n> Add disk image\n\n**-d, --domain** _VM_\n> Specify VM by name\n\n**-n, --dry-run**\n> Show what would be done without making changes\n\n**--operations** _OPS_\n> Run only specified operations\n\n**--list-operations**\n> List all supported operations\n\n**--hostname** _NAME_\n> Set hostname\n\n**--enable** _FEATURE_\n> Enable specific feature"],["DESCRIPTION","**virt-sysprep** resets, unconfigures, or customizes a virtual machine image for cloning or deployment. It removes machine-specific data like SSH keys, logs, and persistent device names.\n\nThis prepares VM images for use as templates by removing identity information that should be unique per machine."],["CAVEATS","VM must be shut down. Root privileges typically required. Some operations may break the VM if used incorrectly. Always backup images before processing."],["INSTALL","```apt: sudo apt install guestfs-tools```\n\n```dnf: sudo dnf install guestfs-tools```\n\n```pacman: sudo pacman -S guestfs-tools```\n\n```zypper: sudo zypper install guestfs-tools```\n\n```nix: nix profile install nixpkgs#guestfs-tools```"],["SEE ALSO","[virt-clone](/man/virt-clone)(1), [virt-customize](/man/virt-customize)(1)"]],"e":["virt-clone","virt-customize"],"x":"virt-sysprep --list-operations"},"wacli-messages-list":{"n":"wacli-messages-list","t":"List messages from a WhatsApp chat","s":[["TAGLINE","List messages from a WhatsApp chat"],["TLDR","**List messages** from a specific chat in JSON\n\n```wacli messages list --chat 491234567890@s.whatsapp.net --json```\n\n**List messages received after a date**\n\n```wacli messages list --chat 491234567890@s.whatsapp.net --after 2026-01-01 --json```\n\n**Limit results** and list oldest first\n\n```wacli messages list --chat 491234567890@s.whatsapp.net --limit 20 --asc --json```\n\n**List only messages you sent**\n\n```wacli messages list --from-me --limit 20 --json```\n\n**List messages in a date range**\n\n```wacli messages list --chat 491234567890@s.whatsapp.net --after 2026-01-01 --before 2026-01-31 --json```"],["SYNOPSIS","**wacli** **messages** **list** [--chat _jid_] [--sender _jid_] [--from-me|--from-them] [--asc] [--limit _n_] [--after _date_] [--before _date_] [--forwarded] [--starred] [--json] [--store _dir_]"],["DESCRIPTION","**wacli messages list** retrieves messages from the local SQLite store, optionally filtered to a single chat (JID), sender, direction, flags, and/or a time window. Output is human-readable by default; use `--json` for machine consumption.\n\nWhatsApp status broadcasts are stored separately and are not returned by `messages list`.\n\nThe local store is populated by `wacli sync` (or `sync --follow`). Default store path is `~/.local/state/wacli` on Linux and `~/.wacli` elsewhere (existing `~/.wacli` on Linux continues to work)."],["PARAMETERS","**--chat** _jid_\n> Restrict to messages in this chat JID (e.g. `491234567890@s.whatsapp.net` or a group JID).\n\n**--sender** _jid_\n> Restrict to messages from this sender JID.\n\n**--from-me**, **--from-them**\n> Only messages sent by you, or only messages from others.\n\n**--asc**\n> Sort ascending (oldest first). Default order is newest first.\n\n**--after** _date_\n> Only messages on or after this time (RFC3339 or `YYYY-MM-DD`).\n\n**--before** _date_\n> Only messages before this time (RFC3339 or `YYYY-MM-DD`).\n\n**--limit** _n_\n> Maximum number of messages to return.\n\n**--forwarded**\n> Only forwarded messages.\n\n**--starred**\n> Only starred messages.\n\n**--json**\n> Emit JSON instead of a human-readable table.\n\n**--store** _dir_\n> Override the store directory (cannot be combined with `--account`)."],["SEE ALSO","[wacli](/man/wacli)(1), [wacli-messages-search](/man/wacli-messages-search)(1), [wacli-messages-show](/man/wacli-messages-show)(1), [wacli-messages-context](/man/wacli-messages-context)(1)"],["RESOURCES","```[Source code](https://github.com/openclaw/wacli)```\n\n```[Documentation](https://wacli.sh/messages.html)```"]],"e":["wacli","wacli-messages-search","wacli-messages-show","wacli-messages-context"],"x":"wacli messages list --chat 491234567890@s.whatsapp.net --json"},"xmake":{"n":"xmake","t":"Cross-platform Lua-based build system","s":[["TAGLINE","Cross-platform Lua-based build system"],["TLDR","**Create new project**\n\n```xmake create [project_name]```\n\n**Build project**\n\n```xmake```\n\n**Build with specific mode**\n\n```xmake -m [release]```\n\n**Run target**\n\n```xmake run [target]```\n\n**Install package**\n\n```xmake require [package]```\n\n**Clean build**\n\n```xmake clean```\n\n**Configure project**\n\n```xmake config -p [linux] -a [x86_64]```\n\n**Generate IDE project**\n\n```xmake project -k [cmake]```"],["SYNOPSIS","**xmake** [_build_] [_run_] [_clean_] [_require_] [_config_] [_options_] [_target_]"],["PARAMETERS","**build**\n> Build project (default).\n\n**run** [_TARGET_]\n> Run target.\n\n**clean**\n> Clean build files.\n\n**config**\n> Configure project.\n\n**require** _PKG_\n> Install package.\n\n**create** _NAME_\n> Create project.\n\n**project** **-k** _KIND_\n> Generate project files.\n\n**-m** _MODE_\n> Build mode (debug, release).\n\n**-p** _PLATFORM_\n> Target platform.\n\n**-a** _ARCH_\n> Target architecture.\n\n**-v**\n> Verbose output.\n\n**-j** _N_\n> Parallel jobs."],["DESCRIPTION","**xmake** is a cross-platform build system. It uses Lua for configuration, providing flexibility while remaining simple.\n\nBuild configuration in xmake.lua defines targets, dependencies, and compiler options. The syntax is more readable than CMake.\n\nPackage management is integrated. Dependencies are declared and automatically downloaded and built.\n\nCross-platform support covers Windows, Linux, macOS, and more. Cross-compilation and toolchain management are built-in.\n\nProject generation creates files for CMake, Make, Ninja, Visual Studio, and Xcode. This enables integration with other tools.\n\nRemote compilation distributes builds across machines. Cache sharing speeds up builds across teams."],["CAVEATS","Less widespread than CMake. Package repository smaller than vcpkg/conan. Lua config has learning curve for some."],["HISTORY","**xmake** was created by **ruki** around **2015** in China. It provides a modern alternative to CMake with integrated package management and simpler syntax."],["INSTALL","```apt: sudo apt install xmake```\n\n```dnf: sudo dnf install xmake```\n\n```pacman: sudo pacman -S xmake```\n\n```apk: sudo apk add xmake```\n\n```brew: brew install xmake```\n\n```nix: nix profile install nixpkgs#xmake```"],["SEE ALSO","[cmake](/man/cmake)(1), [make](/man/make)(1), [meson](/man/meson)(1), [ninja](/man/ninja)(1)"]],"e":["cmake","make","meson","ninja"],"x":"xmake create [project_name]"}});
