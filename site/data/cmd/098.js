window.LCL_BUCKET(98,{"ag":{"n":"ag","t":"Search code fast like ack but faster","s":[["TAGLINE","Search code fast like ack but faster"],["TLDR","**Search** for a pattern recursively\n\n```ag [pattern]```\n\nSearch in **specific file types**\n\n```ag --python [pattern]```\n\nSearch with **case insensitivity**\n\n```ag -i [pattern]```\n\nShow only **file names** containing matches\n\n```ag -l [pattern]```\n\nSearch for **literal string** (no regex)\n\n```ag -Q \"[literal string]\"```\n\nSearch with **context** lines\n\n```ag -C [3] [pattern]```"],["SYNOPSIS","**ag** [_options_] _pattern_ [_path_...]"],["DESCRIPTION","**ag** (The Silver Searcher) is a code-searching tool similar to ack but faster. It searches through source code recursively, automatically ignoring files listed in .gitignore and .hgignore, and skipping hidden files and binary files.\n\nThe tool is optimized for speed, using multiple threads and memory-mapped I/O. It supports Perl-compatible regular expressions and provides colored output with context lines."],["PARAMETERS","**-i**, **--ignore-case**\n> Case insensitive search\n\n**-s**, **--case-sensitive**\n> Case sensitive search\n\n**-v**, **--invert-match**\n> Invert the match\n\n**-w**, **--word-regexp**\n> Match whole words only\n\n**-l**, **--files-with-matches**\n> Print only file names with matches\n\n**-L**, **--files-without-matches**\n> Print file names without matches\n\n**-c**, **--count**\n> Print count of matches per file\n\n**-C** _n_, **--context=**_n_\n> Print n lines of context\n\n**-A** _n_, **--after=**_n_\n> Print n lines after match\n\n**-B** _n_, **--before=**_n_\n> Print n lines before match\n\n**-Q**, **--literal**\n> Treat pattern as literal string\n\n**--python**, **--js**, **--html**, etc.\n> Search only specific file types\n\n**-g** _pattern_\n> Search file names matching pattern\n\n**--hidden**\n> Search hidden files\n\n**-u**, **--unrestricted**\n> Search all files (ignore .gitignore)\n\n**-z**, **--search-zip**\n> Search inside compressed files"],["CONFIGURATION","**~/.agignore**\n> Global file patterns to ignore during searches.\n\n**.agignore**\n> Per-project file patterns to ignore, similar to .gitignore syntax."],["CAVEATS","Not as fast as **ripgrep** on very large codebases. Some regex features may differ from grep. File type detection is based on extensions, not content."],["HISTORY","**ag** was created by Geoff Greer and first released in **2011**. It was designed as a faster alternative to ack, achieving significant speed improvements through parallelization and smarter file filtering. It inspired later tools like ripgrep."],["INSTALL","```apt: sudo apt install silversearcher-ag```\n\n```dnf: sudo dnf install the_silver_searcher```\n\n```pacman: sudo pacman -S the_silver_searcher```\n\n```apk: sudo apk add the_silver_searcher```\n\n```brew: brew install the_silver_searcher```"],["SEE ALSO","[rg](/man/rg)(1), [ack](/man/ack)(1), [grep](/man/grep)(1), [git-grep](/man/git-grep)(1)"]],"e":["rg","ack","grep","git-grep"],"x":"ag [pattern]"},"airtun-ng":{"n":"airtun-ng","t":"Create virtual tunnel interfaces for encrypted WiFi traffic","s":[["TAGLINE","Create virtual tunnel interfaces for encrypted WiFi traffic"],["TLDR","**Create** a virtual tunnel interface\n\n```sudo airtun-ng -a [AP_BSSID] [wlan0mon]```\n\nCreate tunnel with **WEP key**\n\n```sudo airtun-ng -a [AP_BSSID] -w [hex_key] [wlan0mon]```\n\nCreate tunnel with **WPA passphrase**\n\n```sudo airtun-ng -a [AP_BSSID] -p [passphrase] -e [SSID] [wlan0mon]```\n\nBidirectional tunnel (**inject and receive**)\n\n```sudo airtun-ng -a [AP_BSSID] -w [hex_key] -b [wlan0mon]```"],["SYNOPSIS","**airtun-ng** [_-a bssid_] [_-w wep_key_] [_-p wpa_pass -e essid_] [_options_] _interface_"],["DESCRIPTION","**airtun-ng** creates virtual tunnel interfaces (at0, at1, etc.) that allow sending and receiving decrypted traffic to/from a wireless network. This enables using standard network tools with encrypted WiFi networks when you know the key.\n\nThe tool can operate in WEP or WPA mode, creating a virtual ethernet interface that handles encryption/decryption transparently."],["PARAMETERS","**-a** _bssid_\n> Access point MAC address\n\n**-w** _key_\n> WEP key in hexadecimal (encrypt outgoing packets)\n\n**-y** _file_\n> Read PRGA (keystream) from file instead of a WEP key\n\n**-p** _passphrase_\n> WPA passphrase (decrypt packets; use with **-a** and **-e**)\n\n**-e** _essid_\n> Network name (required for WPA)\n\n**-i** _iface_\n> Capture packets from this additional interface\n\n**-t** _tods_\n> Frame destination: 0 = to client, 1 = to AP, 2 = WDS/Bridge\n\n**-b**\n> Bidirectional mode (send and receive)\n\n**-r** _file_\n> Read frames to inject from a pcap file\n\n**-h** _mac_\n> Source MAC address"],["CAVEATS","For authorized testing only. Interface must be in monitor mode. WPA mode requires the 4-way handshake to be completed. Virtual interface must be configured (IP, etc.) after creation."],["HISTORY","**airtun-ng** was added to the aircrack-ng suite to enable post-exploitation scenarios where captured keys could be used to interact with the network using standard tools."],["INSTALL","```apt: sudo apt install aircrack-ng```\n\n```dnf: sudo dnf install aircrack-ng```\n\n```pacman: sudo pacman -S aircrack-ng```\n\n```apk: sudo apk add aircrack-ng```\n\n```zypper: sudo zypper install aircrack-ng```\n\n```brew: brew install aircrack-ng```\n\n```nix: nix profile install nixpkgs#aircrack-ng```"],["SEE ALSO","[aircrack-ng](/man/aircrack-ng)(1), [airbase-ng](/man/airbase-ng)(1), [airdecap-ng](/man/airdecap-ng)(1), [airodump-ng](/man/airodump-ng)(1), [aireplay-ng](/man/aireplay-ng)(1)"],["RESOURCES","```[Source code](https://github.com/aircrack-ng/aircrack-ng)```\n\n```[Homepage](https://www.aircrack-ng.org/)```\n\n```[Documentation](https://www.aircrack-ng.org/documentation.html)```"]],"e":["aircrack-ng","airbase-ng","airdecap-ng","airodump-ng","aireplay-ng"],"x":"sudo airtun-ng -a [AP_BSSID] [wlan0mon]"},"aws-events":{"n":"aws-events","t":"Manage Amazon EventBridge rules and event routing","s":[["TAGLINE","Manage Amazon EventBridge rules and event routing"],["TLDR","**List all rules** in the default event bus\n\n```aws events list-rules```\n\n**Describe a specific rule**\n\n```aws events describe-rule --name [rule_name]```\n\n**List targets** for a rule\n\n```aws events list-targets-by-rule --rule [rule_name]```\n\n**Create a scheduled rule** that runs every hour\n\n```aws events put-rule --name [rule_name] --schedule-expression \"rate(1 hour)\"```\n\n**Create an event pattern rule** to match specific events\n\n```aws events put-rule --name [rule_name] --event-pattern '{\"source\":[\"aws.ec2\"],\"detail-type\":[\"EC2 Instance State-change Notification\"]}'```\n\n**Add a Lambda target** to a rule\n\n```aws events put-targets --rule [rule_name] --targets Id=1,Arn=[arn:aws:lambda:region:account:function:name]```\n\n**Disable a rule**\n\n```aws events disable-rule --name [rule_name]```\n\n**Delete a rule** (remove targets first)\n\n```aws events delete-rule --name [rule_name]```"],["SYNOPSIS","**aws events** _subcommand_ [_options_]"],["DESCRIPTION","**aws events** is a subcommand of the AWS CLI that manages Amazon EventBridge (formerly CloudWatch Events), a serverless event bus service. It routes events between AWS services, SaaS applications, and your own applications.\n\nRules match incoming events based on patterns and route them to targets like Lambda functions, Step Functions, SNS topics, SQS queues, and more. Rules can also trigger on schedules using cron or rate expressions.\n\nEvent buses act as routers: the default bus receives AWS service events, while custom buses can receive events from your applications. Partner event sources connect to SaaS providers."],["PARAMETERS","**list-rules**\n> List rules on an event bus.\n\n**describe-rule**\n> Get details about a specific rule.\n\n**put-rule**\n> Create or update a rule.\n\n**delete-rule**\n> Remove a rule.\n\n**enable-rule**\n> Activate a disabled rule.\n\n**disable-rule**\n> Deactivate a rule without deleting it.\n\n**list-targets-by-rule**\n> List targets attached to a rule.\n\n**put-targets**\n> Add targets to a rule.\n\n**remove-targets**\n> Remove targets from a rule.\n\n**list-event-buses**\n> List event buses in the account.\n\n**put-events**\n> Send custom events to an event bus.\n\n**--name** _rule-name_\n> Name of the rule.\n\n**--schedule-expression** _expression_\n> Cron or rate expression for scheduled rules.\n\n**--event-pattern** _json_\n> JSON pattern to match events.\n\n**--targets** _targets_\n> Target specifications (Id, Arn, Input, etc.).\n\n**--event-bus-name** _name_\n> Event bus name (default if not specified)."],["CAVEATS","Must remove all targets before deleting a rule. Schedule expressions use UTC timezone. Event patterns match on exact values by default; use prefix or anything-but for flexibility. Maximum 5 targets per rule (can be increased via quota request). Rules can take up to one minute to become active."],["HISTORY","**CloudWatch Events** launched in **January 2016** as AWS's first event-driven integration service. It was rebranded as **Amazon EventBridge** at **AWS re:Invent 2019** with expanded capabilities including custom event buses and third-party SaaS integrations. Schema registry and discovery were added to help developers understand event structures. The service continues to be central to serverless and event-driven architectures on AWS."],["INSTALL","```dnf: sudo dnf install aws```\n\n```pacman: sudo pacman -S aws-cli```\n\n```apk: sudo apk add aws-cli```\n\n```zypper: sudo zypper install aws-cli```"],["SEE ALSO","[aws](/man/aws)(1), [aws-lambda](/man/aws-lambda)(1), [aws-sns](/man/aws-sns)(1), [aws-sqs](/man/aws-sqs)(1)"],["RESOURCES","```[Source code](https://github.com/aws/aws-cli)```\n\n```[Homepage](https://aws.amazon.com/cli/)```\n\n```[Documentation](https://docs.aws.amazon.com/cli/latest/reference/events/index.html)```"]],"e":["aws","aws-lambda","aws-sns","aws-sqs"],"x":"aws events list-rules"},"bitbang":{"n":"bitbang","t":"peer-to-peer remote shell, files, and proxy without accounts","s":[["TAGLINE","peer-to-peer remote shell, files, and proxy without accounts"],["TLDR","**Serve** shell, files, and proxy (prints URL, QR, pairing code)\n\n```bitbang serve```\n\n**Serve shell only**\n\n```bitbang serve shell```\n\n**Serve a files share** (add **-upload** to allow uploads)\n\n```bitbang serve files [~/share]```\n\n**Serve HTTP proxy** to a fixed local target\n\n```bitbang serve proxy [localhost:8080]```\n\n**Connect** an interactive shell from another machine\n\n```bitbang connect [url-or-code-or-name]```\n\n**Run a remote command** non-interactively\n\n```bitbang connect [url] -- [tail -f /var/log/syslog]```\n\n**Copy a file** scp-style\n\n```bitbang cp [url]:[/var/log/app.log] [./app.log]```\n\n**Pair once** and save a short device name\n\n```bitbang connect [482731] -name [nas1]```"],["SYNOPSIS","**bitbang** _command_ [_options_] [_args_...]"],["PARAMETERS","**serve** [_mode_] [_args_]\n> Start a listener. Modes: bare **serve** (shell + files + proxy), **shell**, **files** [_PATH_], **proxy** [_HOST:PORT_].\n\n**connect** _target_ [**--** _command_...]\n> Connect as client. _target_ may be a URL, 6-digit pair code, or saved device name. With **--** _command_, run that command and exit.\n\n**cp** _src_ _dst_\n> Copy files; exactly one side is remote as `url:/path`. **-** means stdin/stdout.\n\n**version**\n> Print version (also **--version**).\n\n**help**\n> Usage (also **-h**, **--help**).\n\n**-server** _HOST_\n> Signaling server hostname (default **bitba.ng**).\n\n**-pin** _PIN_\n> Require (serve) or send (connect/cp) a PIN.\n\n**-ephemeral**\n> Throwaway identity: fresh URL each serve run.\n\n**-nocode**\n> Disable spoken 6-digit pairing on the listener (URL still works).\n\n**-name** _NAME_\n> Save a newly paired/connected host under _NAME_ (first time only).\n\n**-relay**\n> Prefer TURN relay up front (ICE still tries direct paths).\n\n**-timeout** _DUR_\n> Dial timeout (default **30s**).\n\n**-v**\n> Verbose logging."],["DESCRIPTION","**bitbang** is a single static Go binary for remote access over WebRTC: open a shell, browse/transfer files, and reverse-proxy HTTP to the remote machine's network—without port forwarding, VPN setup, or an account.\n\nOn the machine you want to reach, run **bitbang serve**. It prints a URL (and QR/pairing code). Anyone who opens the URL in a browser gets the capabilities you enabled; another **bitbang** client can **connect** or **cp** with the same credential.\n\nThe public **bitba.ng** host only brokers signaling. Data is end-to-end encrypted (DTLS). After ICE succeeds, traffic is peer-to-peer when possible; otherwise a TURN relay carries ciphertext only. Device identity is an RSA key under **~/.bitbang/**; successful connects are remembered in **~/.bitbang/devices.json** for short names."],["CAVEATS","The serve URL is a **bearer credential**—anyone with it gets the served capabilities (including a full shell). Prefer short-lived/ephemeral listeners and **-pin** for standing hosts. Official install targets Linux (`amd64`/`arm64`/`armv7`) via GitHub releases; macOS/Windows builds may lag. Requires outbound connectivity to the signaling (and possibly TURN) server unless you self-host BitBang signaling."],["CONFIGURATION","Identity and known hosts live under **~/.bitbang/** (mode-restricted files). Keypair path is **~/.bitbang/**_program_**/identity.pem** (default program name **bitbang**). Saved devices: **~/.bitbang/devices.json**."],["HISTORY","**bitbang** is part of the open-source **BitBang** project by **richlegrand**, MIT-licensed. The CLI maps roughly to **sshd**/**ssh**/**scp** with WebRTC instead of TCP, emphasizing zero-config reach through NAT and browser clients without installing software on the connector."],["SEE ALSO","[ssh](/man/ssh)(1), [scp](/man/scp)(1), [ngrok](/man/ngrok)(1), [tailscale](/man/tailscale)(1)"],["RESOURCES","```[Source code](https://github.com/richlegrand/bitbang-cli)```\n\n```[Homepage](https://bitba.ng)```\n\n```[Documentation](https://github.com/richlegrand/bitbang-cli#readme)```"]],"e":["ssh","scp","ngrok","tailscale"],"x":"bitbang serve"},"brew-install":{"n":"brew-install","t":"Install packages via Homebrew","s":[["TAGLINE","Install packages via Homebrew"],["TLDR","**Install a formula**\n\n```brew install [formula]```\n\n**Install a cask**\n\n```brew install --cask [cask]```\n\n**Install without upgrading existing version**\n\n```HOMEBREW_NO_INSTALL_UPGRADE=1 brew install [formula]```\n\n**Install with debug session on failure**\n\n```brew install --debug [formula]```\n\n**Force install without checking existing versions**\n\n```brew install --force [formula]```\n\n**Install and display install times**\n\n```brew install --display-times [formula]```\n\n**Install the latest development (HEAD) version**\n\n```brew install --HEAD [formula]```\n\n**Preview what would be installed without installing**\n\n```brew install --dry-run [formula]```"],["SYNOPSIS","**brew install** [_options_] _formula|cask_ ..."],["DESCRIPTION","**brew install** installs a formula or cask. If the formula is already installed but outdated, it will be upgraded unless HOMEBREW_NO_INSTALL_UPGRADE is set.\n\nAfter installation, cleanup runs automatically for installed formulae or every 30 days for all formulae unless HOMEBREW_NO_INSTALL_CLEANUP is set."],["PARAMETERS","**--cask**\n> Treat all arguments as casks\n\n**--formula**\n> Treat all arguments as formulae\n\n**--force**\n> Install without checking for previously installed keg-only or non-migrated versions\n\n**--debug**\n> Open interactive debugging session if brewing fails\n\n**--display-times**\n> Print install times for each package\n\n**--verbose**\n> Print detailed build information\n\n**-s, --build-from-source**\n> Compile formula from source instead of using a bottle\n\n**--force-bottle**\n> Install from a bottle even if it would not normally be used\n\n**--HEAD**\n> Install the HEAD version, fetching the latest commit from the upstream repository\n\n**-n, --dry-run**\n> Show what would be installed without actually installing anything\n\n**--keep-tmp**\n> Retain temporary build files after installation\n\n**--include-test**\n> Install test dependencies required to run brew test"],["ENVIRONMENT","**HOMEBREW_NO_INSTALL_UPGRADE**\n> Prevent automatic upgrade of outdated formulae\n\n**HOMEBREW_NO_INSTALL_CLEANUP**\n> Prevent automatic cleanup after installation\n\n**HOMEBREW_NO_INSTALLED_DEPENDENTS_CHECK**\n> Skip checking outdated dependents"],["CAVEATS","Formula-specific options can be appended to the command. Bottles (precompiled binaries) are used when available. Building from source requires Xcode Command Line Tools on macOS."],["SEE ALSO","[brew](/man/brew)(1), [brew-uninstall](/man/brew-uninstall)(1), [brew-upgrade](/man/brew-upgrade)(1), [brew-info](/man/brew-info)(1)"],["RESOURCES","```[Source code](https://github.com/Homebrew/brew)```\n\n```[Homepage](https://brew.sh)```\n\n```[Documentation](https://docs.brew.sh)```"]],"e":["brew","brew-uninstall","brew-upgrade","brew-info"],"x":"brew install [formula]"},"btrfs":{"n":"btrfs","t":"Manage btrfs copy-on-write filesystems","s":[["TAGLINE","Manage btrfs copy-on-write filesystems"],["TLDR","**Create** subvolume\n\n```sudo btrfs subvolume create path/to/subvolume```\n\n**List** subvolumes\n\n```sudo btrfs subvolume list path/to/mount```\n\nShow **space usage**\n\n```sudo btrfs filesystem df path/to/mount```\n\nEnable **quota**\n\n```sudo btrfs quota enable path/to/subvolume```\n\nShow **quota**\n\n```sudo btrfs qgroup show path/to/subvolume```"],["SYNOPSIS","**btrfs** _command_ [_OPTIONS_] [_path_]"],["DESCRIPTION","**btrfs** is the primary management tool for Btrfs (B-tree filesystem), a modern copy-on-write Linux filesystem with built-in support for snapshots, subvolumes, transparent compression, online resizing, multi-device pools, checksumming, and RAID0/1/10/5/6. The command dispatches to subcommand groups (**subvolume**, **filesystem**, **device**, **balance**, **scrub**, **send**, **receive**, **quota**, **qgroup**, **inspect-internal**, ...) that map directly to filesystem operations.\n\nMost subcommands operate on a mounted Btrfs path. The kernel does the heavy lifting; the **btrfs** binary issues IOCTLs to invoke balance, scrub, snapshot, defrag, and other operations. The **send** / **receive** pair enables efficient incremental replication of snapshots to remote hosts or backup volumes."],["PARAMETERS","**subvolume** (su)\n> Manage subvolumes and snapshots\n\n**filesystem** (fi)\n> Manage filesystem properties\n\n**device** (dev)\n> Manage devices in the filesystem\n\n**balance** (bal)\n> Balance data across devices\n\n**scrub** (sc)\n> Verify data integrity\n\n**quota** (qu)\n> Enable/disable quota support\n\n**qgroup** (qg)\n> Manage quota groups"],["CAVEATS","Btrfs is under active development. Some features like RAID5/6 may have stability concerns. Regular scrubs are recommended for data integrity verification. Snapshots share space with their source until modified."],["HISTORY","**btrfs** is the management tool for the **B-tree file system**, a modern Linux filesystem featuring advanced storage capabilities."],["INSTALL","```apt: sudo apt install btrfs-progs```\n\n```dnf: sudo dnf install btrfs-progs```\n\n```pacman: sudo pacman -S btrfs-progs```\n\n```apk: sudo apk add btrfs-progs```\n\n```brew: brew install btrfs-progs```\n\n```nix: nix profile install nixpkgs#btrfs-progs```"],["SEE ALSO","[btrfs-subvolume](/man/btrfs-subvolume)(8), [btrfs-filesystem](/man/btrfs-filesystem)(8), [mkfs.btrfs](/man/mkfs.btrfs)(8)"]],"e":["btrfs-subvolume","btrfs-filesystem","mkfs.btrfs"],"x":"sudo btrfs subvolume create path/to/subvolume"},"cacaclock":{"n":"cacaclock","t":"Display time as ASCII art in the terminal","s":[["TAGLINE","Display time as ASCII art in the terminal"],["TLDR","Display the **time** as ASCII art\n\n```cacaclock```\n\nChange the **font**\n\n```cacaclock -f [font]```\n\nChange the time **format**\n\n```cacaclock -d \"[strftime_arguments]\"```"],["SYNOPSIS","**cacaclock** [_options_]"],["DESCRIPTION","**cacaclock** displays the current time as ASCII art in the terminal using the libcaca library. It provides a visually distinctive clock display using text characters.\n\nThe display format can be customized using strftime format specifiers."],["PARAMETERS","**-f, --font** _font_\n> Use specified font for display\n\n**-d, --dateformat** _format_\n> Time format using strftime specifiers (e.g., %H:%M:%S)"],["INSTALL","```apt: sudo apt install caca-utils```\n\n```dnf: sudo dnf install caca-utils```\n\n```apk: sudo apk add libcaca-apps```\n\n```zypper: sudo zypper install caca-utils```"],["SEE ALSO","[cacademo](/man/cacademo)(1), [cacafire](/man/cacafire)(1), [cacaview](/man/cacaview)(1)"],["RESOURCES","```[Source code](https://github.com/cacalabs/libcaca)```"]],"e":["cacademo","cacafire","cacaview"],"x":"cacaclock"},"cqlsh":{"n":"cqlsh","t":"interactive Cassandra Query Language shell","s":[["TAGLINE","interactive Cassandra Query Language shell"],["TLDR","**Start CQL shell**\n\n```cqlsh```\n\n**Connect to remote host**\n\n```cqlsh [hostname] [9042]```\n\n**Connect with authentication**\n\n```cqlsh -u [username] -p [password]```\n\n**Execute CQL from file**\n\n```cqlsh -f [script.cql]```\n\n**Execute single statement**\n\n```cqlsh -e \"SELECT * FROM [keyspace].[table]\"```\n\n**Check node status**\n\n```nodetool status```\n\n**View cluster info**\n\n```nodetool info```\n\n**Repair a node**\n\n```nodetool repair```"],["SYNOPSIS","**cqlsh** [_options_] [_host_] [_port_]\n\n**nodetool** [_options_] _command_ [_args_]"],["CQLSH PARAMETERS","**-u**, **--username** _user_\n> Authentication username.\n\n**-p**, **--password** _pass_\n> Authentication password.\n\n**-k**, **--keyspace** _keyspace_\n> Initial keyspace.\n\n**-f** _file_\n> Execute CQL file.\n\n**-e** _statement_\n> Execute single CQL statement.\n\n**--ssl**\n> Use SSL connection.\n\n**--connect-timeout** _secs_\n> Connection timeout.\n\n**--request-timeout** _secs_\n> Query timeout."],["NODETOOL COMMANDS","**status**\n> Show cluster node status.\n\n**info**\n> Node information.\n\n**ring**\n> Token ring information.\n\n**repair** [_keyspace_]\n> Run anti-entropy repair.\n\n**cleanup** [_keyspace_]\n> Remove data not belonging to node.\n\n**compact** [_keyspace_]\n> Force compaction.\n\n**flush** [_keyspace_]\n> Flush memtables to SSTables.\n\n**snapshot** [_name_]\n> Take snapshot backup.\n\n**decommission**\n> Decommission the node.\n\n**drain**\n> Drain node for shutdown.\n\n**describecluster**\n> Cluster information."],["CONFIGURATION","**~/.cassandra/cqlshrc**\n> User-specific cqlsh settings including connection defaults, display formatting, and authentication credentials.\n\n**/etc/cassandra/cassandra.yaml**\n> Main Cassandra configuration file controlling cluster behavior, data directories, network settings, and replication."],["DESCRIPTION","**cqlsh** is the Cassandra Query Language shell for executing CQL statements interactively or from scripts. **nodetool** manages Cassandra node operations.\n\nCQL resembles SQL with some differences. Keyspaces are like databases; tables have partition keys and clustering columns. Queries must include partition key for efficient lookups.\n\n**nodetool status** shows cluster health with Up/Down/Normal/Leaving/Joining states and token ownership. **nodetool repair** ensures data consistency across replicas.\n\nSnapshots create hard-linked backups of SSTables for point-in-time recovery. **cleanup** removes data after topology changes. **compact** forces SSTable compaction.\n\nNode lifecycle operations: **drain** prepares for shutdown, **decommission** removes node from cluster streaming data to others."],["CAVEATS","CQL is not SQL; avoid joins and complex queries. Wide partitions impact performance. Repair is resource-intensive; schedule appropriately. Decommission before removing nodes. Authentication default is off."],["HISTORY","Apache Cassandra was initially developed at **Facebook** for inbox search and open-sourced in **2008**. It became an Apache project in **2009** and top-level in **2010**. Cassandra combines Dynamo's distribution model with Bigtable's data model. DataStax was founded in **2010** to provide commercial support. Version 4.0 (2021) brought major improvements after extensive testing."],["SEE ALSO","[scylladb](/man/scylladb)(1), [cockroach](/man/cockroach)(1), [mongo](/man/mongo)(1)"],["RESOURCES","```[Source code](https://github.com/apache/cassandra)```\n\n```[Homepage](https://cassandra.apache.org)```\n\n```[Documentation](https://cassandra.apache.org/doc/latest/cassandra/managing/tools/cqlsh.html)```"]],"e":["scylladb","cockroach","mongo"],"x":"cqlsh"},"crond":{"n":"crond","t":"cron daemon for job scheduling","s":[["TAGLINE","cron daemon for job scheduling"],["TLDR","Start daemon in **background**\n\n```crond```\n\nStart daemon in **foreground**\n\n```crond -n```\n\nSend output to **syslog**\n\n```crond -s```\n\nAccept **custom crontabs** (override limitations)\n\n```crond -p```\n\nInherit **PATH** from environment\n\n```crond -P```\n\nEnable **clustering** support\n\n```crond -c```"],["SYNOPSIS","**crond** [_OPTIONS_]"],["DESCRIPTION","**crond** is a daemon that executes scheduled commands from crontab files. It starts automatically at boot and checks crontab files for jobs that need to run each minute. It monitors /var/spool/cron/ (user crontabs), /etc/crontab (system crontab), and /etc/cron.d/ (system cronjobs). The flags below describe the **cronie** implementation, the crond shipped on most modern distributions; BusyBox and dcron provide their own crond with overlapping but not identical options."],["PARAMETERS","**-n**\n> Run in foreground (useful for init systems)\n\n**-s**\n> Send job output to syslog instead of email\n\n**-m COMMAND**\n> Specify custom mail command or disable with \"off\"\n\n**-p**\n> Override default limitations and accept custom crontabs\n\n**-P**\n> Inherit PATH from environment instead of setting default\n\n**-c**\n> Enable clustering support for shared crontabs\n\n**-h**\n> Display help information\n\n**-V**\n> Display version number"],["CONFIGURATION","**/etc/crontab**\n> System-wide crontab file monitored by crond.\n\n**/etc/cron.d/**\n> Directory containing additional system crontab files.\n\n**/var/spool/cron/**\n> Directory containing user-specific crontab files (one per user). Debian's cron uses /var/spool/cron/crontabs/ instead."],["CAVEATS","Job output is mailed to the crontab owner unless redirected to syslog with -s or a custom mail command. Crontab files must be regular files, not executable, and writable only by the owner. Local time changes (like DST) under three hours receive special handling."],["HISTORY","**crond** is the daemon component of cron, a standard Unix job scheduler that has been in use since Version 7 Unix. Modern implementations include Vixie cron and cronie."],["INSTALL","```apt: sudo apt install cronie```\n\n```dnf: sudo dnf install cronie```\n\n```pacman: sudo pacman -S cronie```\n\n```apk: sudo apk add cronie```\n\n```zypper: sudo zypper install cronie```\n\n```nix: nix profile install nixpkgs#cronie```"],["SEE ALSO","[crontab](/man/crontab)(1), [crontab](/man/crontab)(5), [anacron](/man/anacron)(8)"],["RESOURCES","```[Source code](https://github.com/cronie-crond/cronie)```"]],"e":["crontab","crontab","anacron"],"x":"crond"},"dialog":{"n":"dialog","t":"text-mode dialog boxes for shell scripts","s":[["TAGLINE","text-mode dialog boxes for shell scripts"],["TLDR","Display a **message**\n\n```dialog --msgbox \"[Message]\" [height] [width]```\n\nPrompt for **text input**\n\n```dialog --inputbox \"[Enter text:]\" 8 40 2>[output.txt]```\n\nPrompt for **yes/no** question\n\n```dialog --yesno \"[Continue?]\" 7 40```\n\nDisplay a **menu** selection\n\n```dialog --menu \"[Choose one:]\" 15 40 4 1 \"[Option A]\" 2 \"[Option B]\" 3 \"[Option C]\" 2>[output.txt]```\n\nDisplay a **checklist** with multiple selections\n\n```dialog --checklist \"[Select items:]\" 15 40 4 1 \"[Item A]\" on 2 \"[Item B]\" off 3 \"[Item C]\" on 2>[output.txt]```\n\nShow a **progress bar**\n\n```dialog --gauge \"[Installing...]\" 7 40 0```\n\nDisplay a **password** input box\n\n```dialog --passwordbox \"[Enter password:]\" 8 40 2>[output.txt]```"],["SYNOPSIS","**dialog** [_options_] _box-type_ [_box-options_]"],["DESCRIPTION","**dialog** displays dialog boxes from shell scripts, providing a user-friendly text-based interface for interactive scripts. It supports various widget types including message boxes, input boxes, menus, checklists, radio lists, progress bars, and file selection dialogs.\n\nThe program uses the ncurses library to create pseudo-graphical interfaces in text terminals. This enables shell scripts to present professional-looking user interfaces without requiring a graphical environment.\n\nOutput is written to stderr (or a specified file), while exit status indicates which button was pressed (0 for OK/Yes, 1 for Cancel/No, 255 for ESC). This design allows scripts to capture both user selections and continue using stdout for other purposes. dialog is widely used in system administration scripts, installation programs, and configuration utilities."],["PARAMETERS","**--msgbox** _text_ _height_ _width_\n> Display message with OK button\n\n**--yesno** _text_ _height_ _width_\n> Yes/No question dialog\n\n**--inputbox** _text_ _height_ _width_ [_init_]\n> Text input dialog\n\n**--menu** _text_ _height_ _width_ _menu-height_ _tag_ _item_...\n> Menu selection\n\n**--checklist** _text_ _height_ _width_ _list-height_ _tag_ _item_ _status_...\n> Checkbox selection\n\n**--radiolist** _text_ _height_ _width_ _list-height_ _tag_ _item_ _status_...\n> Radio button selection (single choice)\n\n**--gauge** _text_ _height_ _width_ _percent_\n> Progress bar display\n\n**--passwordbox** _text_ _height_ _width_\n> Password input (hidden characters)\n\n**--fselect** _filepath_ _height_ _width_\n> File selection dialog\n\n**--calendar** _text_ _height_ _width_ _day_ _month_ _year_\n> Calendar date selection\n\n**--title** _title_\n> Set dialog title\n\n**--backtitle** _title_\n> Set background title at top of screen\n\n**--clear**\n> Clear screen on exit\n\n**--colors**\n> Enable embedded color codes in dialog text\n\n**--output-fd** _fd_\n> Output to specified file descriptor instead of stderr"],["CAVEATS","Output goes to stderr by default; redirect with 2>file. Exit status: 0=OK/Yes, 1=Cancel/No, 255=ESC. For graphical dialogs, see zenity or kdialog."],["HISTORY","**dialog** was originally written by **Savio Lam** in **1994** and has been maintained by **Thomas E. Dickey** since **1999**. It is based on the ncurses library and has become the standard tool for creating text-mode user interfaces in shell scripts, widely used in Linux distribution installers and configuration tools."],["INSTALL","```apt: sudo apt install dialog```\n\n```dnf: sudo dnf install dialog```\n\n```pacman: sudo pacman -S dialog```\n\n```apk: sudo apk add dialog```\n\n```zypper: sudo zypper install dialog```\n\n```brew: brew install dialog```\n\n```nix: nix profile install nixpkgs#dialog```"],["SEE ALSO","[whiptail](/man/whiptail)(1), [zenity](/man/zenity)(1), [kdialog](/man/kdialog)(1), [gum](/man/gum)(1)"]],"e":["whiptail","zenity","kdialog","gum"],"x":"dialog --msgbox \"[Message]\" [height] [width]"},"distrobox-enter":{"n":"distrobox-enter","t":"enter and run commands in Distrobox containers","s":[["TAGLINE","enter and run commands in Distrobox containers"],["TLDR","**Enter** a container\n\n```distrobox-enter [container_name]```\n\nEnter and **run a command**\n\n```distrobox-enter [container_name] -- [sh -l]```\n\nEnter **without TTY** (for scripts)\n\n```distrobox-enter -T [container_name] -- [uptime --pretty]```"],["SYNOPSIS","**distrobox-enter** [_options_] [_name_] [_-- command_]"],["DESCRIPTION","**distrobox-enter** enters a Distrobox container. By default it runs your $SHELL, but you can specify different shells or entire commands.\n\nFor scripts, applications, or services, use --no-tty to disable TTY and interactivity."],["PARAMETERS","**-n, --name** _name_\n> Container name (defaults to my-distrobox)\n\n**-T, --no-tty**\n> Do not instantiate a tty (for scripts)\n\n**-nw, --no-workdir**\n> Start from the container's home directory instead of the current directory\n\n**-a, --additional-flags** _flags_\n> Additional flags passed to the container manager command\n\n**-r, --root**\n> Launch podman/docker with root privileges\n\n**-d, --dry-run**\n> Only print the container manager command generated\n\n**-v, --verbose**\n> Show increased verbosity\n\n**-- command**\n> Terminate option parsing; remaining arguments are executed inside the container"],["CAVEATS","Container must exist (create with distrobox-create). First entry initializes the container which takes longer. Commands after -- are passed to the container."],["INSTALL","```dnf: sudo dnf install distrobox```\n\n```pacman: sudo pacman -S distrobox```\n\n```apk: sudo apk add distrobox```\n\n```zypper: sudo zypper install distrobox```\n\n```brew: brew install distrobox```\n\n```nix: nix profile install nixpkgs#distrobox```"],["SEE ALSO","[distrobox](/man/distrobox)(1), [distrobox-create](/man/distrobox-create)(1), [distrobox-export](/man/distrobox-export)(1)"]],"e":["distrobox","distrobox-create","distrobox-export"],"x":"distrobox-enter [container_name]"},"doppler":{"n":"doppler","t":"secrets management and injection platform","s":[["TAGLINE","secrets management and injection platform"],["TLDR","**Login to Doppler**\n\n```doppler login```\n\n**Setup project** configuration\n\n```doppler setup```\n\n**List secrets** in current config\n\n```doppler secrets```\n\n**Run command with** secrets injected\n\n```doppler run -- [command]```\n\n**Dump secrets** to stdout as a dotenv file\n\n```doppler secrets download --no-file --format [env]```\n\n**Run against a specific** project and config\n\n```doppler run -p [backend] -c [prod] -- [command]```\n\n**Authenticate in CI** with a service token\n\n```echo \"[dp.st.prd.xxxx]\" | doppler configure set token --scope /```"],["SYNOPSIS","**doppler** _command_ [_subcommand_] [_flags_]"],["COMMANDS","**login** / **logout**\n> Authenticate this machine against Doppler, or disconnect it.\n\n**setup**\n> Interactively bind the current directory to a project and config, so later commands need no **-p**/**-c**.\n\n**run** **--** _command_\n> Run _command_ with the config's secrets injected into its environment.\n\n**secrets**\n> List, get, set, delete, download, upload, and substitute secrets.\n\n**projects** / **configs** / **environments**\n> Manage the project, config, and environment hierarchy.\n\n**configure**\n> View and edit the CLI's own configuration, including the auth token and the directory-to-config mapping.\n\n**import**\n> Import secrets from another source into a config.\n\n**activity**\n> Show the audit log of recent changes.\n\n**me**\n> Show which identity the current token belongs to.\n\n**open**\n> Open the current project's dashboard in a browser.\n\n**update**\n> Update the CLI itself to the latest version."],["PARAMETERS","**-p**, **--project** _NAME_ / **-c**, **--config** _NAME_\n> Project and config to act on, overriding what `doppler setup` chose for this directory.\n\n**--command** _STRING_\n> Pass the command to **run** as a single shell string instead of after `--`.\n\n**--preserve-env** [_LIST_]\n> Let existing environment values win over Doppler's for the named secrets.\n\n**--mount** _PATH_\n> Write secrets to an ephemeral file instead of injecting them into the environment.\n\n**--fallback** _PATH_, **--fallback-only**\n> Use an encrypted local fallback file so processes still start when Doppler is unreachable.\n\n**--json**\n> Print output as JSON."],["DESCRIPTION","**Doppler** is a hosted secrets manager, and its CLI exists mainly to keep secrets out of files. The central command is **doppler run**, which fetches the current config's secrets and injects them as environment variables into a child process. Nothing is written to disk, so there is no `.env` to leak, commit, or leave stale.\n\nThe model is a three-level hierarchy: a *project* per service, an *environment* per stage (dev, staging, production), and a *config* holding the actual key/value pairs. `doppler setup` records which project and config a directory belongs to, so that a developer can `cd` into a repository and have `doppler run` pick the right secrets automatically.\n\nFor CI and production, a *service token* scoped to a single config replaces the interactive login, which is why the same command works unchanged on a laptop and in a pipeline. An encrypted **fallback file** is written after each successful fetch so that a deploy still comes up if Doppler itself is unreachable."],["CAVEATS","Doppler is a commercial SaaS product: it needs an account, and secrets are fetched over the network at process start, so an outage or a missing token stops your application from booting unless a fallback file is in place. Secrets injected into the environment are visible to the child process and its descendants, and on Linux to anyone who can read `/proc/<pid>/environ` for that user, so **--mount** is the safer option for high-value credentials. Exporting with `secrets download --no-file` puts plaintext on your terminal and into shell history, which defeats much of the point of using the tool."],["HISTORY","Doppler was founded in **2018**, part of a wave of tools reacting to the ubiquity of `.env` files, which are easy to use, easy to commit by accident, and impossible to rotate centrally. The CLI is open source and written in Go, even though the backend service is proprietary, so the injection mechanism can be audited independently of the platform."],["INSTALL","```brew: brew install doppler```\n\n```nix: nix profile install nixpkgs#doppler```"],["SEE ALSO","[doppler-secrets](/man/doppler-secrets)(1), [doppler-projects](/man/doppler-projects)(1), [vault](/man/vault)(1), [aws-vault](/man/aws-vault)(1), [sops](/man/sops)(1), [env](/man/env)(1)"],["RESOURCES","```[Source code](https://github.com/DopplerHQ/cli)```\n\n```[Homepage](https://www.doppler.com)```\n\n```[Documentation](https://docs.doppler.com/docs/cli)```"]],"e":["doppler-secrets","doppler-projects","vault","aws-vault","sops","env"],"x":"doppler login"},"dropbearkey":{"n":"dropbearkey","t":"generate SSH keys for Dropbear","s":[["TAGLINE","generate SSH keys for Dropbear"],["TLDR","Generate **ed25519** key\n\n```dropbearkey -t ed25519 -f [path/to/key_file]```\n\nGenerate **ECDSA** key\n\n```dropbearkey -t ecdsa -f [path/to/key_file]```\n\nGenerate **RSA** key with 4096 bits\n\n```dropbearkey -t rsa -s 4096 -f [path/to/key_file]```\n\nPrint **fingerprint and public key**\n\n```dropbearkey -y -f [path/to/key_file]```"],["SYNOPSIS","**dropbearkey** [_options_]"],["DESCRIPTION","**dropbearkey** generates SSH host and user keys in Dropbear's native format. Dropbear is a lightweight SSH implementation commonly used on embedded systems, routers, and resource-constrained devices where OpenSSH would be too large.\n\nThe tool supports modern key types including Ed25519, ECDSA, and RSA with configurable key sizes. Generated keys are stored in Dropbear's own format, which differs from OpenSSH's format. If you need to use keys with OpenSSH or vice versa, use dropbearconvert to convert between formats.\n\nHost keys are typically stored in /etc/dropbear/, while user keys can be placed in standard SSH locations."],["PARAMETERS","**-t** _type_\n> Key type: **rsa**, **ecdsa**, **ed25519**, or **dss**.\n\n**-f** _file_\n> File to write the private key to.\n\n**-s** _bits_\n> Key size in bits, which should be a multiple of 8. Applies to RSA and ECDSA; Ed25519 has a fixed size and ignores it.\n\n**-y**\n> Print the public key and fingerprint of an existing private key instead of generating one."],["CAVEATS","The generated file is in **Dropbear's own private key format**, not OpenSSH's, so it cannot be handed to `ssh -i` directly: convert it first with `dropbearconvert`. The *public* key printed by **-y** is in the usual OpenSSH one-line form, so it can be pasted straight into an `authorized_keys` file.\n\nUnlike `ssh-keygen`, dropbearkey does **not** encrypt the private key with a passphrase, and there is no option to do so. Anyone who can read the file has the key. For user keys that matter, generate with `ssh-keygen` and convert, rather than the other way round.\n\nPrefer **ed25519**: it is small, fast, and well supported. **dss** is obsolete, rejected by modern SSH implementations, and should never be chosen for a new key.\n\nGenerating an RSA key on a slow embedded device can take a surprisingly long time, and on a freshly booted system the entropy pool may not be seeded, so host key generation at first boot is a classic source of both delay and weak keys."],["INSTALL","```apt: sudo apt install dropbear-bin```\n\n```dnf: sudo dnf install dropbear```\n\n```pacman: sudo pacman -S dropbear```\n\n```apk: sudo apk add dropbear```\n\n```brew: brew install dropbear```\n\n```nix: nix profile install nixpkgs#dropbear```"],["SEE ALSO","[dropbearconvert](/man/dropbearconvert)(1), [dropbear](/man/dropbear)(8), [dbclient](/man/dbclient)(1), [ssh-keygen](/man/ssh-keygen)(1)"],["RESOURCES","```[Source code](https://github.com/mkj/dropbear)```\n\n```[Homepage](https://matt.ucc.asn.au/dropbear/dropbear.html)```"]],"e":["dropbearconvert","dropbear","dbclient","ssh-keygen"],"x":"dropbearkey -t ed25519 -f [path/to/key_file]"},"ds4-server":{"n":"ds4-server","t":"Local OpenAI/Anthropic-compatible HTTP server for DwarfStar LLM inference","s":[["TAGLINE","Local OpenAI/Anthropic-compatible HTTP server for DwarfStar LLM inference"],["TLDR","**Start the API** with a GGUF model (default bind 127.0.0.1:8000)\n\n```ds4-server -m [ds4flash.gguf]```\n\n**Serve with a large context** and Metal backend\n\n```ds4-server -m [ds4flash.gguf] --metal --ctx [100000]```\n\n**Listen on a custom host and port**\n\n```ds4-server -m [ds4flash.gguf] --host [127.0.0.1] --port [8000]```\n\n**Enable CORS** for browser clients\n\n```ds4-server -m [ds4flash.gguf] --cors```\n\n**Stream model weights from SSD** when the model does not fit in RAM\n\n```ds4-server -m [ds4flash.gguf] --ssd-streaming --ctx [32768]```\n\n**Persist KV cache** to disk under a size budget\n\n```ds4-server -m [ds4flash.gguf] --ctx [100000] --kv-disk-dir [~/.ds4/server-kv] --kv-disk-space-mb [8192]```\n\n**List models** via the local API\n\n```curl http://127.0.0.1:8000/v1/models```\n\n**Lower GPU duty cycle** for quieter thermals\n\n```ds4-server -m [ds4flash.gguf] --power [60]```"],["SYNOPSIS","**ds4-server** [_options_]"],["PARAMETERS","**-m**, **--model** _FILE_\n> Path to the GGUF model. Default: **ds4flash.gguf**.\n\n**--metal** | **--cuda** | **--rocm** | **--cpu**\n> Select the inference backend explicitly. Metal is primary on macOS; CUDA/ROCm on Linux where built.\n\n**-c**, **--ctx** _N_\n> Allocated context length in tokens.\n\n**-n**, **--tokens** _N_\n> Default maximum output tokens when the client does not set a limit.\n\n**--host** _HOST_\n> Bind address. Default: **127.0.0.1**.\n\n**--port** _N_\n> Bind port. Default: **8000**.\n\n**--cors**\n> Add **Access-Control-Allow-*** headers for browser JavaScript clients.\n\n**--power** _N_\n> GPU duty-cycle target from 1 to 100. Default: **100**.\n\n**--ssd-streaming**\n> Opt into SSD-backed model streaming instead of full RAM residency.\n\n**--ssd-streaming-cache-experts** _N_|_NGB_\n> Size of the routed-expert cache (expert count or GiB, e.g. **32GB**).\n\n**--think** / **--think-max** / **--nothink**\n> Default thinking mode for chat-style requests (server also maps OpenAI/Anthropic effort fields).\n\n**--kv-disk-dir** _DIR_\n> Enable on-disk KV checkpoints in _DIR_.\n\n**--kv-disk-space-mb** _N_\n> Disk budget in megabytes when KV disk is enabled. Default: **4096**.\n\n**--trace** _FILE_\n> Write prompts, cache decisions, outputs, and tool calls to a trace file.\n\n**-t**, **--threads** _N_\n> CPU helper threads for host-side work."],["DESCRIPTION","**ds4-server** is the HTTP API front end of **DwarfStar** (project **ds4** by **Salvatore Sanfilippo** / **antirez** and contributors). It loads a project-specific **GGUF** and serves OpenAI- and Anthropic-compatible endpoints so local tools, IDEs, and coding agents can talk to a specialized DeepSeek V4 (and related) inference engine without calling a cloud API.\n\nEach client connection is handled by a blocking request thread; inference itself is serialized onto a single worker that owns the live session and KV state. That design keeps session reuse, optional disk KV checkpoints, and graph execution in one place. Endpoints include **/v1/chat/completions**, **/v1/responses**, **/v1/completions**, and **/v1/messages**. Model name aliases such as **deepseek-v4-flash** and **deepseek-v4-pro** both refer to the GGUF currently loaded.\n\nUnlike generic GGUF runners, DwarfStar targets a narrow set of carefully prepared weights (DeepSeek V4 Flash/PRO and experimental forks for other MoE models). Use the project's download scripts and published GGUFs; arbitrary community GGUF files are not expected to work. Backends include **Metal** (primary on Apple Silicon), **CUDA** (including DGX Spark), and **ROCm** (e.g. Strix Halo). **--ssd-streaming** keeps models runnable when they exceed RAM by paging routed experts from disk."],["CAVEATS","Software is explicitly **beta**. Only DwarfStar-prepared GGUFs are supported. Default bind is localhost; expose to a network only if you understand there is no built-in auth. On-disk KV and large contexts need substantial free disk and RAM. CPU-only builds are mainly for diagnostics; production use expects Metal, CUDA, or ROCm. The interactive CLI binary is also named **ds4**, which collides with unrelated DualShock 4 tools of the same name on Linux."],["HISTORY","**DwarfStar** (**ds4**) was created by **Salvatore Sanfilippo** (**antirez**) as a small, self-contained local inference stack optimized for large open-weight MoE models that barely fit (or do not fit) in consumer RAM. **ds4-server** provides the HTTP API half of that stack so coding agents and OpenAI-compatible clients can use the same engine as the interactive **ds4** CLI."],["SEE ALSO","[ds4-agent](/man/ds4-agent)(1), [ds4-bench](/man/ds4-bench)(1), [ds4-eval](/man/ds4-eval)(1), [ollama](/man/ollama)(1), [llama-cli](/man/llama-cli)(1), [llama.cpp](/man/llama.cpp)(1)"],["RESOURCES","```[Source code](https://github.com/antirez/ds4)```\n\n```[Documentation](https://github.com/antirez/ds4/blob/main/docs/ENGINE_REFERENCE.md)```"]],"e":["ds4-agent","ds4-bench","ds4-eval","ollama","llama-cli","llama.cpp"],"x":"ds4-server -m [ds4flash.gguf]"},"earth-game":{"n":"earth-game","t":"offline CLI for turning life goals into quests","s":[["TAGLINE","offline CLI for turning life goals into quests"],["TLDR","**Initialize** local storage\n\n```earth init```\n\nEdit **character** values and purpose\n\n```earth character edit```\n\n**Add** and **start** a quest\n\n```earth quest add\nearth quest list\nearth quest start [1]```\n\nShow **today's** focus (current quest, next action, open loops)\n\n```earth today```\n\n**Close** open loops and **review**\n\n```earth loop add --text \"[task]\"\nearth review\nearth export [path.json]```\n\nStart the **local web UI** (loopback only)\n\n```earth web```"],["SYNOPSIS","**earth** _command_ [_options_]\n\n**earth-game** _command_ [_options_]"],["DESCRIPTION","**earth-game** (invoked as **earth** or **earth-game** after install) is a private, offline personal quest tracker. It uses only the Python standard library and local SQLite—no accounts, outbound network, scoring, or AI advice.\n\nYou maintain a character profile (values, strengths, purpose), quests with a single current focus and next action, open loops to close, and periodic reviews. Data defaults to **${XDG_DATA_HOME:-$HOME/.local/share}/earth-game/earth.db**, overridable with **EARTH_GAME_DB**. **earth web** serves the same database on **127.0.0.1** only."],["PARAMETERS","**init**\n> Create local storage without overwriting an existing database\n\n**character** **show**|**edit**\n> View or edit personal profile fields\n\n**quest** **add**|**list**|**start**|**done**|**drop**\n> Manage quests; only one quest is current\n\n**loop** **add**|**list**|**close**\n> Capture and resolve open loops\n\n**today**\n> Summary of current quest, next action, and review status\n\n**review**\n> Record short reflections (**--update-quest** can set the next action)\n\n**export** [_path_]\n> Export all data as JSON (never overwrites existing files)\n\n**web**\n> Local browser UI (**--port**, **--no-open**)"],["CONFIGURATION","Set **EARTH_GAME_DB** to point at another SQLite file. On Arch Linux the package may be installed from the AUR as **earth-game**."],["INSTALL","```aur: yay -S earth-game```"],["SEE ALSO","[taskwarrior](/man/task)(1)"],["RESOURCES","```[Source code](https://github.com/skorotkiewicz/earth-game)```"]],"e":["taskwarrior"],"x":"earth init"},"etherwake":{"n":"etherwake","t":"Wake-on-LAN magic packet sender","s":[["TAGLINE","Wake-on-LAN magic packet sender"],["TLDR","**Wake computer by MAC address**\n\n```sudo etherwake [00:11:22:33:44:55]```\n\n**Wake using specific interface**\n\n```sudo etherwake -i [eth0] [00:11:22:33:44:55]```\n\n**Wake with broadcast**\n\n```sudo etherwake -b [00:11:22:33:44:55]```\n\n**Wake with password**\n\n```sudo etherwake -p [password] [00:11:22:33:44:55]```\n\n**Debug mode**\n\n```sudo etherwake -D [00:11:22:33:44:55]```"],["SYNOPSIS","**etherwake** [_options_] _mac-address_"],["PARAMETERS","**-i** _interface_\n> Network interface to use.\n\n**-b**\n> Use broadcast address.\n\n**-D**\n> Increase debug level; show packet contents (repeat for more).\n\n**-p** _password_\n> SecureOn password (6 bytes).\n\n**-V**\n> Show version.\n\n_mac-address_\n> Target MAC address (XX:XX:XX:XX:XX:XX)."],["DESCRIPTION","**etherwake** sends Wake-on-LAN (WoL) magic packets to wake computers from sleep, hibernation, or powered-off state. The target computer must have WoL enabled in BIOS/UEFI and on the network interface.\n\nThe magic packet contains a sync stream followed by the target MAC address repeated 16 times. When the network card detects this pattern, it signals the computer to power on."],["REQUIREMENTS","- Target must have WoL enabled in BIOS/UEFI\n- Network interface must support WoL\n- Target must be on same network segment (or use directed broadcast)\n- Target's NIC must have power in standby"],["CAVEATS","Requires root or CAP_NET_RAW capability. Works on local network only; WoL over internet needs port forwarding or VPN. Some NICs need specific driver configuration. Wireless usually doesn't support WoL."],["HISTORY","Wake-on-LAN was developed in **1996** by AMD and HP as part of the Advanced Manageability Alliance. etherwake is one of several Linux implementations of WoL sending tools."],["INSTALL","```apt: sudo apt install etherwake```"],["SEE ALSO","[wol](/man/wol)(1), [wakeonlan](/man/wakeonlan)(1), [ethtool](/man/ethtool)(8)"],["RESOURCES","```[Documentation](https://manpages.debian.org/etherwake)```"]],"e":["wol","wakeonlan","ethtool"],"x":"sudo etherwake [00:11:22:33:44:55]"},"fatrace":{"n":"fatrace","t":"trace file access events in real-time","s":[["TAGLINE","trace file access events in real-time"],["TLDR","Print file access events on **all** filesystems\n\n```sudo fatrace```\n\nLimit to a specific **command** name\n\n```sudo fatrace -C [program_name]```\n\nPrint events on **current mount** only\n\n```sudo fatrace -c```\n\nAdd **timestamps** to output\n\n```sudo fatrace -t```\n\nShow only **read and write** events\n\n```sudo fatrace -f RW```\n\nStop after **10 seconds** and write to file\n\n```sudo fatrace -s [10] -o [output.log]```"],["SYNOPSIS","**fatrace** [_options_]"],["DESCRIPTION","**fatrace** reports file access events from the kernel's fanotify interface. It shows which processes are accessing which files in real-time, helping identify processes causing unnecessary disk activity or wake-ups.\n\nUseful for debugging, security monitoring, and understanding application file access patterns."],["PARAMETERS","**-c**, **--current-mount**\n> Only record events on the partition/mount of the current directory.\n\n**-C**, **--command** _COMMAND_\n> Show only events for this command name.\n\n**-f**, **--filter** _TYPES_\n> Show only the given event types: C (close), R (read), O (open), W (write), D (delete), + (create), < (move).\n\n**-o**, **--output** _FILE_\n> Write events to the given file instead of standard output.\n\n**-p**, **--ignore-pid** _PID_\n> Ignore events for this process ID. Can be specified multiple times.\n\n**-s**, **--seconds** _SECONDS_\n> Stop after the given number of seconds.\n\n**-t**, **--timestamp**\n> Add timestamps to events. Specify twice for epoch format.\n\n**-h**, **--help**\n> Print help and exit."],["CAVEATS","Requires root privileges. Uses fanotify which requires Linux 2.6.37+. May impact system performance on busy systems."],["INSTALL","```apt: sudo apt install fatrace```\n\n```dnf: sudo dnf install fatrace```\n\n```zypper: sudo zypper install fatrace```\n\n```nix: nix profile install nixpkgs#fatrace```"],["SEE ALSO","[inotifywait](/man/inotifywait)(1), [lsof](/man/lsof)(8), [strace](/man/strace)(1)"]],"e":["inotifywait","lsof","strace"],"x":"sudo fatrace"},"flock":{"n":"flock","t":"manage advisory file locks from shell scripts","s":[["TAGLINE","manage advisory file locks from shell scripts"],["TLDR","Run command with **file lock**\n\n```flock /tmp/lock.lock command```\n\nRun with **non-blocking** lock (fail if locked)\n\n```flock -n /tmp/lock.lock command```\n\nRun with **timeout** of 10 seconds\n\n```flock -w 10 /tmp/lock.lock command```\n\nRun with **custom exit code** on lock failure\n\n```flock -n -E 42 /tmp/lock.lock command```\n\nExecute **shell command** with lock\n\n```flock /tmp/lock.lock -c \"command1 && command2\"```\n\nUse **shared** (read) lock\n\n```flock -s /tmp/lock.lock command```"],["SYNOPSIS","**flock** [_options_] _file_|_directory_ _command_ [_arguments_]\n\n**flock** [_options_] _file_|_directory_ **-c** _command_\n\n**flock** [_options_] _number_"],["DESCRIPTION","**flock** manages file locks from shell scripts. It executes a command while holding a lock on a specified file or directory, creating the lock file if needed. By default, the command waits indefinitely for the lock to become available. This is useful for ensuring only one instance of a command runs at a time."],["PARAMETERS","**-c, --command**\n> Execute a single command string via the shell\n\n**-e, -x, --exclusive**\n> Obtain an exclusive (write) lock (default)\n\n**-s, --shared**\n> Obtain a shared (read) lock\n\n**-n, --nb, --nonblock**\n> Fail immediately if the lock is not available\n\n**-w, --wait, --timeout SECONDS**\n> Fail if the lock is not acquired within specified seconds\n\n**-o, --close**\n> Close the file descriptor before executing the command\n\n**-u, --unlock**\n> Manually drop the lock\n\n**-E, --conflict-exit-code NUM**\n> Exit code when lock fails with -n or timeout (default: 1)\n\n**-F, --no-fork**\n> Do not fork before executing the command\n\n**--verbose**\n> Report how long the lock took to acquire"],["CAVEATS","**flock** does not detect deadlock situations. File locking has limited support on certain network filesystems like NFS and CIFS. When the -n or -w option is used and the lock cannot be acquired, flock exits with the code specified by -E (default 1)."],["HISTORY","**flock** is part of the **util-linux** package, providing shell-level access to the flock(2) system call."],["INSTALL","```apt: sudo apt install util-linux```\n\n```dnf: sudo dnf install util-linux```\n\n```pacman: sudo pacman -S util-linux```\n\n```apk: sudo apk add flock```\n\n```zypper: sudo zypper install util-linux```\n\n```brew: brew install util-linux```\n\n```nix: nix profile install nixpkgs#util-linux```"],["SEE ALSO","[flock](/man/flock)(2)"]],"e":["flock"],"x":"flock /tmp/lock.lock command"},"fossil-ci":{"n":"fossil-ci","t":"shorthand for fossil commit","s":[["TAGLINE","shorthand for fossil commit"],["TLDR","**Commit changes**\n\n```fossil ci -m \"[message]\"```\n\n**Commit with comment editor**\n\n```fossil ci```\n\n**Commit specific files**\n\n```fossil ci [file1] [file2] -m \"[message]\"```\n\n**Commit with branch**\n\n```fossil ci --branch [branch_name] -m \"[message]\"```"],["SYNOPSIS","**fossil** **ci** [_options_] [_files_...]"],["PARAMETERS","**-m**, **--comment** _text_\n> Commit message.\n\n**--branch** _name_\n> Create new branch.\n\n**--private**\n> Make commit private.\n\n**--allow-empty**\n> Allow empty commit."],["DESCRIPTION","**fossil ci** is a shorthand alias for **fossil commit**. It records changes in the working directory to the repository with a commit message.\n\nThis command is provided for convenience and compatibility with users familiar with other version control systems that use similar abbreviations. All options and behaviors are identical to **fossil commit**."],["INSTALL","```apt: sudo apt install fossil```\n\n```dnf: sudo dnf install fossil```\n\n```pacman: sudo pacman -S fossil```\n\n```apk: sudo apk add fossil```\n\n```zypper: sudo zypper install fossil```\n\n```brew: brew install fossil```\n\n```nix: nix profile install nixpkgs#fossil```"],["SEE ALSO","[fossil-commit](/man/fossil-commit)(1), [fossil-add](/man/fossil-add)(1)"],["RESOURCES","```[Homepage](https://fossil-scm.org/)```\n\n```[Documentation](https://fossil-scm.org/home/help?cmd=commit)```"]],"e":["fossil-commit","fossil-add"],"x":"fossil ci -m \"[message]\""},"fossil-delete":{"n":"fossil-delete","t":"remove files from version control","s":[["TAGLINE","remove files from version control"],["TLDR","**Delete a file**\n\n```fossil delete [file]```\n\n**Delete with dry run**\n\n```fossil delete --dry-run [file]```\n\n**Delete with force**\n\n```fossil delete -f [file]```"],["SYNOPSIS","**fossil** **delete** [_options_] _file_..."],["PARAMETERS","**-n**, **--dry-run**\n> Show what would happen without modifying anything.\n\n**--hard**\n> Also delete the files from the working check-out, not only from version control.\n\n**--soft**\n> Keep files in the working check-out (this is the default). Overrides **--hard** if both are given.\n\n**--case-sensitive** _BOOL_\n> Override the repository case-sensitivity setting for the file name match.\n\n**--reset**\n> Undo a prior, uncommitted deletion — the files return to normal tracking status. Cannot be combined with flags other than **--verbose** and **--dry-run**.\n\n**-v**, **--verbose**\n> Used with **--reset** to print each restored file."],["DESCRIPTION","**fossil delete** is an alias for **fossil rm**. It schedules files for removal from version control on the next commit.\n\nThis command is provided for convenience and compatibility. All options and behaviors are identical to **fossil rm**. By default, files remain on disk and are only removed from version control."],["INSTALL","```apt: sudo apt install fossil```\n\n```dnf: sudo dnf install fossil```\n\n```pacman: sudo pacman -S fossil```\n\n```apk: sudo apk add fossil```\n\n```zypper: sudo zypper install fossil```\n\n```brew: brew install fossil```\n\n```nix: nix profile install nixpkgs#fossil```"],["SEE ALSO","[fossil-rm](/man/fossil-rm)(1), [fossil-add](/man/fossil-add)(1), [fossil-forget](/man/fossil-forget)(1), [fossil-commit](/man/fossil-commit)(1)"]],"e":["fossil-rm","fossil-add","fossil-forget","fossil-commit"],"x":"fossil delete [file]"},"fprintd-list":{"n":"fprintd-list","t":"display enrolled fingerprints","s":[["TAGLINE","display enrolled fingerprints"],["TLDR","List enrolled fingerprints for a **specific user**\n\n```fprintd-list [username]```\n\nList enrolled fingerprints for **multiple users**\n\n```fprintd-list [username1] [username2] ...```\n\nDisplay **help**\n\n```fprintd-list```"],["SYNOPSIS","**fprintd-list** _username_ [_username_...]"],["DESCRIPTION","**fprintd-list** displays the fingerprints enrolled in the fprintd database for specified users. The output shows which fingers have been registered for each user.\n\nThe command queries the fprintd daemon via D-Bus to retrieve enrollment information. This is useful for verifying which fingers are registered before attempting authentication or deciding which additional fingers to enroll."],["CAVEATS","Requires the fprintd daemon to be running. May require appropriate permissions to list fingerprints for other users."],["INSTALL","```apt: sudo apt install fprintd```\n\n```dnf: sudo dnf install fprintd```\n\n```pacman: sudo pacman -S fprintd```\n\n```apk: sudo apk add fprintd```\n\n```zypper: sudo zypper install fprintd```\n\n```nix: nix profile install nixpkgs#fprintd```"],["SEE ALSO","[fprintd](/man/fprintd)(1), [fprintd-enroll](/man/fprintd-enroll)(1), [fprintd-delete](/man/fprintd-delete)(1), [fprintd-verify](/man/fprintd-verify)(1)"],["RESOURCES","```[Source code](https://gitlab.freedesktop.org/libfprint/fprintd)```"]],"e":["fprintd","fprintd-enroll","fprintd-delete","fprintd-verify"],"x":"fprintd-list [username]"},"fractal":{"n":"fractal","t":"Hierarchical agent loops in git worktrees","s":[["TAGLINE","Hierarchical agent loops in git worktrees"],["TLDR","**Install** the CLI from PyPI\n\n```pip install plasma-fractal```\n\n**Open** the live TUI dashboard for an initialized project\n\n```fractal open```\n\n**Install** bundled agent skills into your agent config\n\n```fractal install```\n\n**Show** top-level help\n\n```fractal --help```\n\n**Open** the dashboard with a light color scheme\n\n```fractal open --light```"],["SYNOPSIS","**fractal** [_command_] [_options_]"],["DESCRIPTION","**fractal** (Python package **plasma-fractal**) runs hierarchical autonomous agent loops that organize themselves into a tree. Each node iterates toward a goal inside its own **git worktree** and can spawn child nodes for separable subtasks, so the tree grows to fit the problem rather than a fixed plan.\n\nHard caps (iterations, depth, children, cost, time) bound every loop; an operator can steer or stop work at any time. Run metadata including cost is stored in a local **SQLite** database and can be watched live in a terminal UI (**fractal open**). Agents run in **tmux** sessions.\n\nSupported agent backends include Claude Code (**claude**), Codex (**codex**), Grok Build (**grok**), OpenCode (**opencode**), and Oh My Pi (**omp**), selected per node with **--agent**. Claude and Codex can route through OpenRouter via **--provider=openrouter** when **OPENROUTER_API_KEY** is set.\n\nNodes are commonly spawned via the **/fractal** agent skill (plugin marketplace or **fractal install**), which walks through node parameters and launches work in tmux. The CLI is also usable directly; run **fractal \\<command\\> --help** for per-command flags."],["PARAMETERS","**open** [**--light**]\n> Open the TUI dashboard from a project root that has been initialized as a fractal. **--light** suits light terminal themes.\n\n**install** [**--link**] [**--project**]\n> Copy or symlink fractal and wiki skills into agent skill directories (**~/.claude/skills**, **~/.agents/skills**). **--link** symlinks instead of copying; **--project** limits install to the current project.\n\nOther subcommands manage node lifecycle, runs, and configuration (see **fractal --help** and **fractal \\<command\\> --help**). Common node parameters when spawning include **name**, **path**, **base** branch, **agent**, **provider**, **model**, **max-iters**, **max-depth**, **max-children**, **timeout**, **max-cost**, and related budget/time limits."],["CAVEATS","Requires a git repository and, for live agent sessions, **tmux** plus the chosen agent CLI on **PATH**. Cost caps and provider routing depend on correct API credentials. **pipx**/**uv tool** installs may need **plasma-wiki** co-installed so the **wiki** helper is available. Node names are restricted (letters, digits, and **_** only in the skill workflow)."],["HISTORY","**fractal** is an Apache-2.0 project from Plasma AI for recursive multi-agent worktrees with local observability."],["INSTALL","```dnf: sudo dnf install fractal```\n\n```pacman: sudo pacman -S fractal```\n\n```apk: sudo apk add fractal```\n\n```zypper: sudo zypper install fractal```\n\n```nix: nix profile install nixpkgs#fractal```"],["SEE ALSO","[tmux](/man/tmux)(1), [git](/man/git)(1), [git-worktree](/man/git-worktree)(1)"],["RESOURCES","```[Source code](https://github.com/plasma-ai/fractal)```\n\n```[Documentation](https://docs.plasma.ai/fractal)```"]],"e":["tmux","git","git-worktree"],"x":"pip install plasma-fractal"},"gau":{"n":"gau","t":"URL fetcher from multiple archive sources","s":[["TAGLINE","URL fetcher from multiple archive sources"],["TLDR","**Fetch URLs for** domain\n\n```gau [example.com]```\n\n**Output to file**\n\n```gau [example.com] -o [urls.txt]```\n\n**Fetch with providers**\n\n```gau --providers [wayback,otx] [example.com]```\n\n**Include subdomains**\n\n```gau --subs [example.com]```\n\n**Filter by date**\n\n```gau --from [202201] --to [202212] [example.com]```"],["SYNOPSIS","**gau** [_options_] _domains_..."],["PARAMETERS","_DOMAINS_\n> Target domains to fetch URLs for.\n\n**-o** _FILE_, **--o** _FILE_\n> Output file.\n\n**--providers** _LIST_\n> URL sources: wayback, otx, commoncrawl.\n\n**--subs**\n> Include subdomains.\n\n**--from** _DATE_\n> Start date (YYYYMM).\n\n**--to** _DATE_\n> End date (YYYYMM).\n\n**--help**\n> Display help information."],["DESCRIPTION","**gau** (Get All URLs) fetches known URLs for domains from multiple sources including Wayback Machine, Common Crawl, and AlienVault OTX. It's used for reconnaissance and security research.\n\nThe tool aggregates historical URLs that may reveal hidden endpoints, parameters, or old vulnerabilities. Results include archived pages, API endpoints, and file paths.\n\ngau enables discovering attack surface by finding URLs that were once publicly accessible."],["CAVEATS","Results include historical dead URLs. May produce large output. Subject to source rate limits."],["HISTORY","gau was created for security research and bug bounty hunting, providing easy access to archived URL databases for reconnaissance purposes."],["INSTALL","```brew: brew install gau```\n\n```nix: nix profile install nixpkgs#gau```"],["SEE ALSO","[waybackurls](/man/waybackurls)(1), [hakrawler](/man/hakrawler)(1)"]],"e":["waybackurls","hakrawler"],"x":"gau [example.com]"},"gdal_translate":{"n":"gdal_translate","t":"converts and transforms raster data","s":[["TAGLINE","converts and transforms raster data"],["TLDR","**Convert raster format**\n\n```gdal_translate [input.tif] [output.png]```\n\n**Convert with specific format**\n\n```gdal_translate -of GTiff [input.png] [output.tif]```\n\n**Subset by bounds**\n\n```gdal_translate -projwin [ulx] [uly] [lrx] [lry] [input.tif] [output.tif]```\n\n**Rescale to 8-bit**\n\n```gdal_translate -ot Byte -scale [input.tif] [output.tif]```\n\n**Set compression**\n\n```gdal_translate -co COMPRESS=LZW [input.tif] [output.tif]```\n\n**Extract bands**\n\n```gdal_translate -b 1 -b 2 -b 3 [input.tif] [rgb.tif]```"],["SYNOPSIS","**gdal_translate** [_options_] _src_ _dst_"],["PARAMETERS","**-of** _format_\n> Output format (GTiff, PNG, JPEG, etc.).\n\n**-ot** _type_\n> Output data type (Byte, Int16, Float32, etc.).\n\n**-b** _band_\n> Select band(s).\n\n**-projwin** _ulx_ _uly_ _lrx_ _lry_\n> Subset by coordinates.\n\n**-srcwin** _xoff_ _yoff_ _xsize_ _ysize_\n> Subset by pixels.\n\n**-scale** [_src_min_ _src_max_ [_dst_min_ _dst_max_]]\n> Scale values.\n\n**-co** _NAME=VALUE_\n> Creation option.\n\n**-outsize** _xsize_ _ysize_\n> Output size (pixels or %).\n\n**-a_srs** _srs_\n> Assign spatial reference.\n\n**-a_nodata** _value_\n> Assign nodata value."],["DESCRIPTION","**gdal_translate** converts raster data between formats, extracts subsets, and applies transformations. It's part of GDAL (Geospatial Data Abstraction Library), the standard for raster geospatial operations.\n\nCommon uses include format conversion, creating image pyramids, applying compression, and extracting regions from large datasets."],["CAVEATS","Large files may require significant memory. Some formats don't support all features. Lossy compression loses data. Check gdalinfo for source properties first."],["HISTORY","GDAL was started by **Frank Warmerdam** in **1998** and is now an OSGeo project. gdal_translate is one of the core GDAL utilities, supporting over 150 raster formats."],["INSTALL","```apt: sudo apt install gdal-bin```\n\n```apk: sudo apk add gdal-tools```"],["SEE ALSO","[gdalwarp](/man/gdalwarp)(1), [gdalinfo](/man/gdalinfo)(1), [ogr2ogr](/man/ogr2ogr)(1)"]],"e":["gdalwarp","gdalinfo","ogr2ogr"],"x":"gdal_translate [input.tif] [output.png]"},"genie":{"n":"genie","t":"systemd namespace bottle for WSL","s":[["TAGLINE","systemd namespace bottle for WSL"],["TLDR","**Initialize** the bottle (run once at start)\n\n```genie -i```\n\nRun a **login shell** inside the bottle\n\n```genie -s```\n\nRun a **specified command** inside the bottle\n\n```genie -c [command]```"],["SYNOPSIS","**genie** [_options_]"],["PARAMETERS","**-i**, **--initialize**\n> Initialize the bottle namespace for systemd\n\n**-s**, **--shell**\n> Start a login shell inside the bottle\n\n**-c**, **--command** _COMMAND_\n> Run a command inside the bottle and exit\n\n**-u**, **--user** _USER_\n> Run as specified user (default: current user)\n\n**-v**, **--verbose**\n> Enable verbose output"],["CONFIGURATION","**/etc/genie.ini**\n> Main configuration file controlling systemd path, user mappings, and bottle behavior."],["DESCRIPTION","**genie** creates a \"bottle\" - a PID namespace with systemd running as PID 1 - inside Windows Subsystem for Linux (WSL). This enables running systemd-dependent services that would otherwise not work in WSL's default environment.\n\nWSL normally uses a Microsoft init system, which prevents systemd services from running. genie works around this by creating a namespace where systemd can run normally, then providing access to that environment.\n\nInitialize the bottle once per WSL session with **-i**, then use **-s** to enter or **-c** to run commands within the systemd environment."],["CAVEATS","Requires WSL 2. Some systemd services may still not work perfectly due to WSL limitations. The bottle must be initialized before use and persists until WSL is shut down. Network configuration may behave differently inside the bottle."],["HISTORY","genie was created by Arkane Systems to address the lack of systemd support in WSL. As systemd became the standard init system for most Linux distributions, many services and tools expected it to be present, creating compatibility issues in WSL's unique environment."]],"e":[],"x":"genie -i"},"git-agecrypt":{"n":"git-agecrypt","t":"Transparent file-level encryption for Git, powered by age","s":[["TAGLINE","Transparent file-level encryption for Git, powered by age"],["TLDR","**Initialise** the current Git repository for transparent encryption\n\n```git agecrypt init```\n\n**Register** an age identity (private key) for decryption\n\n```git-agecrypt config add -i [path/to/age.key]```\n\n**Register** an age recipient for one or more paths\n\n```git-agecrypt config add -r [age1...] -p [secrets/prod.env]```\n\n**List** the current recipients and identities\n\n```git-agecrypt config list```\n\n**Remove** a previously registered recipient\n\n```git-agecrypt config remove -r [age1...] -p [secrets/prod.env]```\n\n**Show** which tracked files are encrypted\n\n```git-agecrypt status```\n\n**Tear down** the integration in this repository\n\n```git-agecrypt deinit```"],["SYNOPSIS","**git-agecrypt** _command_ [_options_]\n**git agecrypt** _command_ [_options_]"],["PARAMETERS","**init**\n> Install the **clean**, **smudge**, and **textconv** filters into _.git/config_ so encryption / decryption happen automatically on commit and checkout.\n\n**deinit**\n> Remove the filter integration from the current repository. Tracked encrypted blobs are unaffected.\n\n**config add -r** _recipient_ **-p** _path..._\n> Register an age _recipient_ (public key) that should be able to decrypt the listed _paths_. Repeat to grant access to multiple keys. SSH **ed25519** public keys and age **age1...** keys are both accepted.\n\n**config add -i** _identity_\n> Register an age _identity_ (private key file) used by the local checkout to decrypt files. Stored in **.git/config**, never committed.\n\n**config remove -r** _recipient_ [**-p** _path..._]\n> Revoke a recipient, optionally limited to specific paths.\n\n**config remove -i** _identity_\n> Forget a local identity.\n\n**config list**\n> Print the configured recipients (per-path) and registered local identities.\n\n**status**\n> Report which tracked paths are configured for encryption and whether the working copy matches the encrypted blob.\n\n**clean**\n> Internal filter entry point: read plaintext from stdin and write ciphertext to stdout. Invoked by Git via the **filter.agecrypt.clean** hook.\n\n**smudge**\n> Internal filter entry point: read ciphertext from stdin and write plaintext to stdout. Invoked by Git on checkout.\n\n**textconv** _file_\n> Internal helper used by **git diff** to render encrypted blobs as plaintext for diffing without leaking ciphertext into the working copy.\n\n**-v**, **--verbose**\n> Increase logging verbosity.\n\n**--help**\n> Print help for the chosen subcommand.\n\n**--version**\n> Print version and exit."],["DESCRIPTION","**git-agecrypt** is a Git integration that keeps secrets in a repository encrypted at rest while exposing plaintext to the working tree. It is an alternative to **git-crypt**, swapping **GPG** for **age**, with smaller key material and support for **SSH** keys and age plugin stubs (for example **age-plugin-yubikey**).\n\nAfter **git-agecrypt init**, the repository's _.git/config_ contains **clean**, **smudge**, and **textconv** filter entries pointing at the binary. Every file matched by a path pattern in _.gitattributes_ (typically **filter=agecrypt diff=agecrypt**) is encrypted on its way into the object store and decrypted on its way out. Recipients and per-path scoping live in a committed _git-agecrypt.toml_, while local private identities live in _.git/config_ so they never leave the developer's machine.\n\nBecause age encryption is non-deterministic, git-agecrypt records a **BLAKE3** hash of each plaintext under _.git/git-agecrypt/_; if the plaintext has not changed, the previously stored ciphertext is reused so commits do not produce noisy churn."],["CONFIGURATION","**git-agecrypt.toml** (committed) declares recipients and the paths they can decrypt:\n\n```\n[[recipient]]\nkey = \"age1abc...\"\npaths = [\"secrets/*.env\", \"deploy/*.yaml\"]\n\n[[recipient]]\nkey = \"ssh-ed25519 AAAA... user@host\"\npaths = [\"secrets/*.env\"]\n```\n\n**.gitattributes** marks which files are subject to encryption:\n\n```\nsecrets/*.env  filter=agecrypt diff=agecrypt\ndeploy/*.yaml  filter=agecrypt diff=agecrypt\n```\n\n**.git/config** (per-checkout, never committed) stores identity locations and the filter wiring written by **init**:\n\n```\n[filter \"agecrypt\"]\n        clean = git-agecrypt clean %f\n        smudge = git-agecrypt smudge %f\n        required = true\n[diff \"agecrypt\"]\n        textconv = git-agecrypt textconv\n[agecrypt]\n        identity = /home/user/.config/age/keys.txt\n```"],["CAVEATS","Both **git-crypt** and **git-agecrypt** rely on Git filters, which means **git log -p**, **git blame**, and similar tools can leak plaintext through **textconv** unless tooling is run on a checkout that lacks the identity. **age** is an authenticated stream cipher rather than a deterministic format, so encrypted blobs change on every encryption unless git-agecrypt's hash cache is preserved - clearing _.git/git-agecrypt/_ will produce diffs even when the plaintext is unchanged. Revoking a leaked recipient requires rewriting history; like every Git-level encryption tool, **git-agecrypt** cannot un-publish ciphertext that already left the repository."],["HISTORY","**git-agecrypt** was created by **vlaci** and is maintained as an open-source project on GitHub. It builds on **age**, the modern file encryption tool by **Filippo Valsorda**, and is positioned as a lighter, more flexible successor to **git-crypt** for teams that already use age or SSH keys for secrets management."],["INSTALL","```nix: nix profile install nixpkgs#git-agecrypt```"],["SEE ALSO","[git-crypt](/man/git-crypt)(1), [age](/man/age)(1), [sops](/man/sops)(1), [git](/man/git)(1)"]],"e":["git-crypt","age","sops","git"],"x":"git agecrypt init"},"git-update-ref":{"n":"git-update-ref","t":"Safely update Git reference values","s":[["TAGLINE","Safely update Git reference values"],["TLDR","**Update ref to new value**\n\n```git update-ref [refs/heads/branch] [commit]```\n\n**Delete ref**\n\n```git update-ref -d [refs/heads/branch]```\n\n**Create symbolic ref**\n\n```git update-ref --symbolic-ref [ref] [target]```\n\n**Update with old value check**\n\n```git update-ref [ref] [new] [old]```"],["SYNOPSIS","**git** **update-ref** [_options_] _ref_ _newvalue_ [_oldvalue_]"],["PARAMETERS","**-d**\n> Delete ref.\n\n**--create-reflog**\n> Create reflog.\n\n**--no-deref**\n> Don't dereference symbolic.\n\n**-m** _reason_\n> Reflog message.\n\n**--stdin**\n> Read update instructions (update, create, delete, verify, symref-update, etc.) from standard input, applying them together as one atomic transaction.\n\n**-z**\n> Use NUL-terminated arguments with **--stdin** instead of quoted, whitespace-terminated strings.\n\n**--batch-updates**\n> With **--stdin**, allow individual updates in the transaction to fail without aborting the others."],["DESCRIPTION","**git update-ref** safely updates ref names stored in the repository. It provides atomic ref updates with optional old value verification, ensuring that a ref is only changed if it currently points to an expected value.\n\nThis is a low-level plumbing command used internally by higher-level commands like `git branch` and `git tag`. With **--stdin**, multiple ref updates can be batched into a single atomic transaction."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-symbolic-ref](/man/git-symbolic-ref)(1), [git-branch](/man/git-branch)(1), [git-tag](/man/git-tag)(1)"],["RESOURCES","```[Documentation](https://git-scm.com/docs/git-update-ref)```\n\n```[Source code](https://github.com/git/git)```"]],"e":["git-symbolic-ref","git-branch","git-tag"],"x":"git update-ref [refs/heads/branch] [commit]"},"gnome-keyring":{"n":"gnome-keyring","t":"secure password and key storage daemon","s":[["TAGLINE","secure password and key storage daemon"],["TLDR","**Start keyring daemon**\n\n```gnome-keyring-daemon --start```\n\n**Start with specific components**\n\n```gnome-keyring-daemon --start --components=secrets,ssh```\n\n**Replace running daemon**\n\n```gnome-keyring-daemon --replace```\n\n**Store a secret via CLI**\n\n```secret-tool store --label=\"My Secret\" service myapp```\n\n**Lookup secret**\n\n```secret-tool lookup service myapp```"],["SYNOPSIS","**gnome-keyring-daemon** [_options_]"],["PARAMETERS","**--start**\n> Start daemon if not running.\n\n**--replace**\n> Replace running daemon.\n\n**--components** _list_\n> Components: secrets, pkcs11, ssh.\n\n**--daemonize**\n> Run as daemon.\n\n**-f**, **--foreground**\n> Run in foreground.\n\n**--unlock**\n> Read a password from stdin and unlock the login keyring, or create it if it does not exist.\n\n**-l**, **--login**\n> Read login password from stdin (used by PAM).\n\n**-C**, **--control-directory** _directory_\n> Directory for creating communication sockets.\n\n**-V**, **--version**\n> Print version and exit."],["DESCRIPTION","**GNOME Keyring** is a daemon that stores secrets, passwords, and keys. It provides secure storage for applications and handles SSH key management.\n\nThe keyring is typically started at login and unlocked with the user's password. Applications access it through libsecret or the Secret Service D-Bus API."],["COMPONENTS","- **secrets**: Password/secret storage\n- **ssh**: SSH agent (replaces ssh-agent)\n- **pkcs11**: PKCS#11 smart card support"],["CONFIGURATION","**~/.local/share/keyrings/**\n> Directory containing keyring data files."],["CAVEATS","Usually started by desktop session. SSH component may conflict with ssh-agent. Keyring locked when session locks. Non-GNOME desktops may need manual setup."],["HISTORY","GNOME Keyring was created for the GNOME desktop to provide secure credential storage. It implements the freedesktop.org Secret Service specification for cross-desktop compatibility."],["INSTALL","```apt: sudo apt install gnome-keyring```\n\n```dnf: sudo dnf install gnome-keyring```\n\n```pacman: sudo pacman -S gnome-keyring```\n\n```apk: sudo apk add gnome-keyring```\n\n```zypper: sudo zypper install gnome-keyring```\n\n```nix: nix profile install nixpkgs#gnome-keyring```"],["SEE ALSO","[secret-tool](/man/secret-tool)(1), [ssh-agent](/man/ssh-agent)(1), [gpg](/man/gpg)(1)"]],"e":["secret-tool","ssh-agent","gpg"],"x":"gnome-keyring-daemon --start"},"gnugo":{"n":"gnugo","t":"play the game of Go against the computer","s":[["TAGLINE","play the game of Go against the computer"],["TLDR","**Start a game with ASCII display**\n\n```gnugo --mode gtp```\n\n**Play with a specific board size**\n\n```gnugo --boardsize [9]```\n\n**Set AI difficulty level**\n\n```gnugo --level [10]```\n\n**Play interactively in ASCII mode**\n\n```gnugo --mode ascii```\n\n**Replay an SGF game file**\n\n```gnugo -l [game.sgf] --mode ascii```\n\n**Score a finished game**\n\n```gnugo -l [game.sgf] --score finish```"],["SYNOPSIS","**gnugo** [_options_]"],["PARAMETERS","**--mode** _MODE_\n> Interface mode: ascii, gtp, or gmp.\n\n**--boardsize** _N_\n> Board size (default: 19). Common sizes: 9, 13, 19.\n\n**--level** _N_\n> AI thinking level (0-10, default: 10).\n\n**--handicap** _N_\n> Number of handicap stones.\n\n**--color** _COLOR_\n> Choose color: black or white.\n\n**--komi** _N_\n> Set komi (compensation points, default: 5.5).\n\n**-l**, **--infile** _FILE_\n> Load an SGF game file for analysis or replay.\n\n**-L**, **--until** _MOVE_\n> Stop loading the SGF file just before the specified move is played.\n\n**-o**, **--outfile** _FILE_\n> Save the game to an SGF file.\n\n**--score** _MODE_\n> Estimate or finalize the score of a loaded game (_estimate_, _finish_, _aftermath_).\n\n**--printboard**\n> Print the board after each move.\n\n**--quiet**\n> Suppress informational messages."],["PREVIEW","```\n    A B C D E F G\n 7  . . . . . . .\n 6  . . X . . . .\n 5  . X O X . . .\n 4  . . X O . . .\n 3  . . . . . . .\n```"],["DESCRIPTION","**gnugo** is a program that plays the board game Go against the user. It supports multiple board sizes, adjustable AI strength, and can communicate through the Go Text Protocol (GTP) for integration with graphical frontends.\n\nIn ASCII mode, moves are entered as coordinates (e.g., \"C4\"). The AI analyzes board positions using pattern matching and reading ahead. Use **pass** to pass your turn and **quit** to exit."],["CAVEATS","Requires no special terminal support in ASCII mode. The AI is moderately strong but not competitive with modern Go engines like KataGo. GTP mode is intended for use with graphical frontends."],["HISTORY","**GNU Go** has been developed by the **Free Software Foundation** since **1989**. It was one of the strongest open-source Go programs before the advent of neural network-based engines."],["INSTALL","```apt: sudo apt install gnugo```\n\n```dnf: sudo dnf install gnugo```\n\n```pacman: sudo pacman -S gnugo```\n\n```zypper: sudo zypper install gnugo```\n\n```nix: nix profile install nixpkgs#gnugo```"],["SEE ALSO","[gomoku](/man/gomoku)(6), [gnuchess](/man/gnuchess)(6)"]],"e":["gomoku","gnuchess"],"x":"gnugo --mode gtp"},"go-tool":{"n":"go-tool","t":"Run Go toolchain programs","s":[["TAGLINE","Run Go toolchain programs"],["TLDR","**List available tools**\n\n```go tool```\n\n**Run pprof**\n\n```go tool pprof [profile.pb.gz]```\n\n**Run trace**\n\n```go tool trace [trace.out]```\n\n**Run compile**\n\n```go tool compile [file.go]```\n\n**Run objdump**\n\n```go tool objdump [binary]```"],["SYNOPSIS","**go tool** [_tool_] [_arguments_]"],["PARAMETERS","_TOOL_\n> Tool name to run.\n\n_ARGUMENTS_\n> Tool-specific arguments.\n\n**pprof**\n> Profile viewer.\n\n**trace**\n> Execution tracer.\n\n**compile**\n> Go compiler.\n\n**link**\n> Go linker.\n\n**objdump**\n> Object file disassembler.\n\n**--help**\n> Display help information."],["DESCRIPTION","**go tool** runs Go toolchain programs that underlie the standard go commands. It provides access to low-level tools like the compiler, linker, profiler, and tracer.\n\nThe command is used for advanced debugging, profiling, and understanding compiler behavior. Each tool has its own options and usage patterns."],["CAVEATS","Low-level tools. May change between versions. For advanced users."],["HISTORY","go tool provides access to **Go** toolchain internals, supporting debugging and optimization workflows."],["INSTALL","```apt: sudo apt install gccgo-go```\n\n```dnf: sudo dnf install gcc-go```\n\n```pacman: sudo pacman -S gcc-go```\n\n```apk: sudo apk add gcc-go```\n\n```zypper: sudo zypper install gcc-go```\n\n```brew: brew install go```\n\n```nix: nix profile install nixpkgs#go```"],["SEE ALSO","[go](/man/go)(1), [go-build](/man/go-build)(1)"],["RESOURCES","```[Documentation](https://pkg.go.dev/cmd/go#hdr-Run_specified_go_tool)```"]],"e":["go","go-build"],"x":"go tool"},"grafana-cli":{"n":"grafana-cli","t":"Grafana plugin and administration manager","s":[["TAGLINE","Grafana plugin and administration manager"],["TLDR","**Install plugin**\n\n```grafana-cli plugins install [plugin-id]```\n\n**List installed plugins**\n\n```grafana-cli plugins ls```\n\n**Update all installed plugins**\n\n```grafana-cli plugins update-all```\n\n**Update plugin**\n\n```grafana-cli plugins update [plugin-id]```\n\n**Remove plugin**\n\n```grafana-cli plugins remove [plugin-id]```\n\n**List available plugins** in the remote repository\n\n```grafana-cli plugins list-remote```\n\n**Reset the admin password**\n\n```grafana-cli admin reset-admin-password [newpassword]```"],["SYNOPSIS","**grafana-cli** [_global-options_] _command_ _subcommand_ [_args_]"],["COMMANDS","**plugins install** _ID_ [_VERSION_]\n> Install a plugin, optionally pinning to a specific version.\n\n**plugins ls**\n> List installed plugins.\n\n**plugins update** _ID_\n> Update a single plugin to the latest compatible version.\n\n**plugins update-all**\n> Update every installed plugin.\n\n**plugins remove** _ID_\n> Remove an installed plugin.\n\n**plugins list-remote**\n> List all plugins available in the configured repository.\n\n**plugins list-versions** _ID_\n> List available versions for a given plugin.\n\n**admin reset-admin-password** _PASSWORD_\n> Reset the built-in admin account password.\n\n**admin data-migration encrypt-datasource-passwords**\n> Migrate plaintext data-source passwords to the secureJsonData field."],["PARAMETERS","**--config** _FILE_\n> Use an alternate **grafana.ini** configuration file.\n\n**--homepath** _DIR_\n> Grafana home directory used to locate config and plugin paths.\n\n**--pluginsDir** _DIR_\n> Override the plugin install directory.\n\n**--pluginUrl** _URL_\n> Install a plugin from a custom URL (zip) instead of the plugin repository.\n\n**--repo** _URL_\n> Use a different plugin repository.\n\n**--insecure**\n> Skip TLS verification when downloading plugins.\n\n**-d**, **--debug**\n> Enable debug output.\n\n**-v**, **--version**\n> Show the CLI version.\n\n**--help**\n> Display help information."],["DESCRIPTION","**grafana-cli** manages Grafana plugins and administration tasks. It installs, updates, and removes visualization plugins and data sources from the Grafana plugin repository.\n\nThe tool handles plugin lifecycle management independently of the Grafana server. It also provides admin commands for password reset and database migration."],["CAVEATS","Requires appropriate permissions. May need Grafana restart. Plugin compatibility varies."],["HISTORY","grafana-cli was developed alongside **Grafana** to provide command-line management of the visualization platform."],["INSTALL","```dnf: sudo dnf install grafana```\n\n```pacman: sudo pacman -S grafana```\n\n```apk: sudo apk add grafana```\n\n```zypper: sudo zypper install grafana```\n\n```brew: brew install grafana```\n\n```nix: nix profile install nixpkgs#grafana```"]],"e":[],"x":"grafana-cli plugins install [plugin-id]"},"grub-mount":{"n":"grub-mount","t":"mount filesystems using GRUB's drivers via FUSE","s":[["TAGLINE","mount filesystems using GRUB's drivers via FUSE"],["TLDR","**Mount** a block device or file system image to a mount point\n\n```grub-mount [/dev/sdXY] [/mnt]```\n\nMount a **specific partition** from a disk image\n\n```grub-mount -r [2] [disk.img] [/mnt]```\n\nMount an **encrypted** device with passphrase prompt\n\n```grub-mount -C [/dev/sdXY] [/mnt]```\n\nLoad a **ZFS encryption key** from a file\n\n```grub-mount -K [path/to/zfs.key] [/dev/sdX] [/mnt]```\n\nEnable **verbose** output\n\n```grub-mount -v [image] [/mnt]```"],["SYNOPSIS","**grub-mount** [_options_] _device_ _mountpoint_"],["PARAMETERS","**-r**, **--root** _PARTITION_\n> Specify partition number to mount from a disk image\n\n**-C**, **--crypto**\n> Enable cryptographic device support, prompts for passphrase\n\n**-K**, **--zfs-key** _FILE_\n> Load ZFS encryption key from specified file\n\n**-d**, **--debug** _STRING_\n> Show debugging output for matching category\n\n**-v**, **--verbose**\n> Enable verbose output\n\n**--version**\n> Display version information"],["DESCRIPTION","**grub-mount** mounts file systems or disk images read-only using GRUB's built-in file system drivers via FUSE. This is useful for accessing file systems that GRUB supports but the host operating system might not, or for inspecting boot images.\n\nThe tool supports various file systems including ext2/3/4, XFS, Btrfs, ZFS, FAT, NTFS, and ISO9660. It can also handle encrypted volumes with the appropriate options."],["CAVEATS","All mounts are read-only. Requires FUSE support on the host system. Some advanced file system features may not be fully supported. Encrypted volumes require appropriate keys or passphrases."],["HISTORY","grub-mount is part of GRUB 2, providing a FUSE-based mounting interface using GRUB's file system drivers. This allows administrators to access and verify boot environments without requiring kernel support for all file system types."],["INSTALL","```apt: sudo apt install grub-common```\n\n```apk: sudo apk add grub-mount```"],["SEE ALSO","[grub-probe](/man/grub-probe)(8), [grub-install](/man/grub-install)(8), [grub-mkconfig](/man/grub-mkconfig)(8), [mount](/man/mount)(8), [fusermount](/man/fusermount)(1)"]],"e":["grub-probe","grub-install","grub-mkconfig","mount","fusermount"],"x":"grub-mount [/dev/sdXY] [/mnt]"},"haxelib":{"n":"haxelib","t":"Package manager for the Haxe programming language","s":[["TAGLINE","Package manager for the Haxe programming language"],["TLDR","**Install a library**\n\n```haxelib install [library]```\n\n**Install a specific version**\n\n```haxelib install [library] [version]```\n\n**Install from a zip file**\n\n```haxelib install [file.zip]```\n\n**Update a single library**\n\n```haxelib update [library]```\n\n**Update all installed libraries**\n\n```haxelib update```\n\n**List installed libraries**\n\n```haxelib list```\n\n**Search for libraries**\n\n```haxelib search [query]```\n\n**Remove a library**\n\n```haxelib remove [library]```\n\n**Use a git repository as a library**\n\n```haxelib git [library] [https://github.com/user/repo.git]```\n\n**Point a library at a local development directory**\n\n```haxelib dev [library] [path/to/source]```"],["SYNOPSIS","**haxelib** _command_ [_arguments_]"],["SUBCOMMANDS","**install** _name_ [_version_]\n> Install a library from the haxelib repository or a local zip.\n\n**update** [_name_]\n> Update a single library, or all installed libraries when no name is given.\n\n**upgrade**\n> Update every installed library to the latest version.\n\n**remove** _name_ [_version_]\n> Remove an installed library.\n\n**list**\n> List installed libraries and their versions.\n\n**search** _query_\n> Search the haxelib repository for libraries matching _query_.\n\n**info** _name_\n> Show information about a library.\n\n**run** _name_ [_args..._]\n> Execute a library's run script.\n\n**dev** _name_ _path_\n> Point a library at a local development directory. Pass no path to clear.\n\n**git** _name_ _url_ [_branch_]\n> Use a git repository as the source for a library.\n\n**hg** _name_ _url_ [_branch_]\n> Use a mercurial repository as the source for a library.\n\n**path** _name..._\n> Print source paths, dependencies, and compiler defines for the given libraries.\n\n**libpath** _name..._\n> Print the root path of each library, one per line.\n\n**version**\n> Print the haxelib version.\n\n**config**\n> Print the haxelib repository path.\n\n**setup** [_path_]\n> Configure the haxelib repository path.\n\n**newrepo**\n> Create a local repository in the current directory.\n\n**deleterepo**\n> Remove the local repository in the current directory.\n\n**selfupdate**\n> Update haxelib itself."],["DESCRIPTION","**haxelib** is the package manager for the **Haxe** programming language. It installs, updates, and removes libraries from the central registry at `lib.haxe.org`, and also supports installing libraries from git or mercurial repositories or local zip archives.\n\nThe `dev` command points a library at a local working copy, which is the standard workflow for developing a library and testing it inside another project. The `newrepo` command creates a per-project library directory so each project can pin its own dependency versions."],["CAVEATS","The default repository is shared system-wide; use `haxelib newrepo` for project-local installs. Library names are case-sensitive on case-sensitive filesystems."],["INSTALL","```apt: sudo apt install haxe```\n\n```dnf: sudo dnf install haxe```\n\n```pacman: sudo pacman -S haxe```\n\n```apk: sudo apk add haxe```\n\n```zypper: sudo zypper install haxe```\n\n```brew: brew install haxe```\n\n```nix: nix profile install nixpkgs#haxe```"],["SEE ALSO","[npm](/man/npm)(1)"]],"e":["npm"],"x":"haxelib install [library]"},"hg-commit":{"n":"hg-commit","t":"saves changes to the repository as a new changeset","s":[["TAGLINE","saves changes to the repository as a new changeset"],["TLDR","**Commit all changes**\n\n```hg commit -m \"[message]\"```\n\n**Commit specific files**\n\n```hg commit [file1] [file2] -m \"[message]\"```\n\n**Commit with editor for message**\n\n```hg commit```\n\n**Amend the last commit**\n\n```hg commit --amend```\n\n**Close a branch**\n\n```hg commit --close-branch -m \"[message]\"```"],["SYNOPSIS","**hg** **commit** [_options_] [_file_...]"],["PARAMETERS","**-m**, **--message** _text_\n> Commit message.\n\n**-A**, **--addremove**\n> Add/remove files before committing.\n\n**--amend**\n> Amend the parent changeset.\n\n**-u**, **--user** _user_\n> Record user as committer.\n\n**-d**, **--date** _date_\n> Record date as commit date.\n\n**--close-branch**\n> Mark branch as closed.\n\n**-i**, **--interactive**\n> Interactively select which changes to include.\n\n**-I**, **--include** _PATTERN_\n> Include only files matching the given pattern.\n\n**-X**, **--exclude** _PATTERN_\n> Exclude files matching the given pattern.\n\n**-l**, **--logfile** _FILE_\n> Read commit message from file.\n\n**-s**, **--secret**\n> Use the secret phase for committing."],["DESCRIPTION","**hg commit** saves changes to the repository as a new changeset. With no files specified, all modified files are committed. Use **-m** for inline message or omit it to open an editor. The **--amend** flag modifies the parent commit instead of creating a new one."],["CAVEATS","Only tracked files are committed; new files need **hg add** or **-A** first. **--amend** rewrites history and should be avoided on already-shared changesets."],["HISTORY","Commit is a core **Mercurial** command, present since the project's first release in 2005."],["SEE ALSO","[hg](/man/hg)(1), [hg-add](/man/hg-add)(1), [hg-status](/man/hg-status)(1), [hg-init](/man/hg-init)(1)"],["RESOURCES","```[Source code](https://foss.heptapod.net/mercurial/mercurial-devel)```\n\n```[Documentation](https://wiki.mercurial-scm.org/)```"]],"e":["hg","hg-add","hg-status","hg-init"],"x":"hg commit -m \"[message]\""},"hugo":{"n":"hugo","t":"fast static site generator written in Go","s":[["TAGLINE","fast static site generator written in Go"],["TLDR","**Create new site**\n\n```hugo new site [site-name]```\n\n**Build site**\n\n```hugo```\n\n**Create new content**\n\n```hugo new content [posts/my-post.md]```\n\n**Start development server**\n\n```hugo server```\n\n**Build with drafts included**\n\n```hugo --buildDrafts```\n\n**Build for production with minification**\n\n```hugo --minify```\n\n**Build to a specific directory**\n\n```hugo -d [/path/to/output]```"],["SYNOPSIS","**hugo** [_command_] [_options_]"],["PARAMETERS","**new content**\n> Create new content file.\n\n**new site**\n> Create a new site scaffold.\n\n**server**\n> Start built-in development server.\n\n**-b**, **--baseURL** _URL_\n> Hostname and path to the root.\n\n**-D**, **--buildDrafts**\n> Include content marked as draft.\n\n**-E**, **--buildExpired**\n> Include expired content.\n\n**-F**, **--buildFuture**\n> Include content with future publish dates.\n\n**-d**, **--destination** _DIR_\n> Filesystem path to write files to.\n\n**--minify**\n> Minify supported output formats (HTML, XML, etc.).\n\n**--gc**\n> Run cleanup tasks after the build (remove unused cache files).\n\n**--cleanDestinationDir**\n> Remove files from destination not found in static directories.\n\n**-t**, **--theme** _THEME_\n> Theme to use.\n\n**--logLevel** _LEVEL_\n> Log level (debug, info, warn, error)."],["DESCRIPTION","**Hugo** is a fast static site generator written in Go. It builds websites from Markdown content using templates and themes.\n\nHugo is extremely fast, building thousands of pages in seconds. It supports themes, taxonomies, and shortcodes for complex sites."],["CAVEATS","Go-template syntax learning curve. Theme compatibility varies. No dynamic content."],["HISTORY","Hugo was created by **Steve Francia** in **2013** as a fast alternative to Jekyll, written in Go. Note: `hugo new` for creating content is deprecated; use `hugo new content` instead."],["INSTALL","```apt: sudo apt install hugo```\n\n```dnf: sudo dnf install hugo```\n\n```pacman: sudo pacman -S hugo```\n\n```apk: sudo apk add hugo```\n\n```zypper: sudo zypper install hugo```\n\n```brew: brew install hugo```\n\n```nix: nix profile install nixpkgs#hugo```"],["SEE ALSO","[hugo-server](/man/hugo-server)(1), [jekyll](/man/jekyll)(1), [hexo](/man/hexo)(1)"]],"e":["hugo-server","jekyll","hexo"],"x":"hugo new site [site-name]"},"hwatu":{"n":"hwatu","t":"Fast headless verification browser for AI coding agents","s":[["TAGLINE","Fast headless verification browser for AI coding agents"],["TLDR","**Install** then run setup to detect agent workflows\n\n```hwatu setup```\n\nOpen a site **headlessly** and run a one-call verify pass\n\n```hwatu check localhost:5173 --eval 'document.title' --shot=/tmp/after.png```\n\nOpen a URL like a terminal browser (headless by default)\n\n```hwatu localhost:3000```\n\n**Pixel-diff** two open windows and print match percent\n\n```hwatu diff --id 2 --other 1 --heatmap /tmp/heat.png```\n\n**Render** HTML from stdin and screenshot\n\n```echo '<h1>hi</h1>' | hwatu render --stdin --shot=/tmp/gen.png```\n\nHand a live session to a **human** in the tiling WM\n\n```hwatu focus [window_id]```\n\nStart the **MCP** server for agent clients\n\n```hwatu mcp```"],["SYNOPSIS","**hwatu** [_options_] [_url_]\n\n**hwatu** **check**|_diff_|_motion_|_snapshot_|_focus_|_render_|_setup_|_doctor_|_mcp_ ..."],["PARAMETERS","**check** _url_\n> One-call verify: load, optional eval/screenshot, return JSON metrics\n\n**diff** **--id** _n_ **--other** _m_\n> Pixel-diff two windows; optional **--heatmap** path\n\n**motion** **--id** _n_\n> Report animations as numbers (duration, easing, velocity)\n\n**snapshot**\n> Page state as structured JSON (tokens, not pixels)\n\n**render** **--stdin**\n> Render markup from stdin without a local HTTP server\n\n**focus** _id_\n> Materialize a headless window into a visible session for human hand-off\n\n**setup** [**--client** _name_] [**--scope** project|user] [**--dry-run**] [**--undo**]\n> Detect coding agents and configure MCP/socket connections\n\n**doctor**\n> Environment and readiness checks\n\n**mcp**\n> Run as an MCP server (`command: hwatu`, `args: [\"mcp\"]`)\n\n**demo**\n> Run a built-in demo verification pass\n\n**--headless**\n> Headless window mode (default for agent use; per-window property)"],["DESCRIPTION","**hwatu** is a Linux verification browser built for AI agent loops: a warm WebKitGTK daemon that keeps the engine hot so page checks stay cheap (~tens of milliseconds), returns measurement primitives (pixel match scores, animation metrics, console/network errors) instead of only raw screenshots, and can hand the **same live session** to a human mid-task without losing cookies or state.\n\nIt ships as a static binary plus the distro's webkitgtk, exposes a plain CLI, an MCP server, and a newline-delimited JSON Unix socket protocol. Typical agent flow: open headless windows, `check` or `diff` until the match percent climbs, then `focus` only when a CAPTCHA or judgment call needs a person.\n\nLinux-only; renders WebKit, not Chromium — keep a Chromium matrix in CI for engine-specific bugs."],["CAVEATS","Requires WebKitGTK 6.x on the host. Headless is a per-window property switchable live, not a launch-only mode. AGPL-3.0 licensed. Not a full replacement for Playwright multi-browser CI matrices."],["HISTORY","Open-source Rust project aimed at making agent browser verification one tool call instead of multi-step Playwright/devtools sessions, with interruptible human hand-off as a first-class feature."],["INSTALL","```aur: yay -S hwatu```"],["SEE ALSO","[playwright](/man/playwright)(1), [chromium](/man/chromium)(1)"],["RESOURCES","```[Source code](https://github.com/hongnoul/hwatu)```\n\n```[Documentation](https://github.com/hongnoul/hwatu/tree/main/docs)```"]],"e":["playwright","chromium"],"x":"hwatu setup"},"id3tag":{"n":"id3tag","t":"command-line tool for writing ID3 tags to MP3 files","s":[["TAGLINE","command-line tool for writing ID3 tags to MP3 files"],["TLDR","**Set song title**\n\n```id3tag -s \"[title]\" [file.mp3]```\n\n**Set artist**\n\n```id3tag -a \"[artist]\" [file.mp3]```\n\n**Set album**\n\n```id3tag -A \"[album]\" [file.mp3]```\n\n**Set year and track number**\n\n```id3tag -y [2024] -t [1] [file.mp3]```\n\n**Set genre by number**\n\n```id3tag -g [17] [file.mp3]```\n\n**Set multiple tags at once**\n\n```id3tag -a \"[artist]\" -s \"[title]\" -A \"[album]\" -y [2024] -t [1] [file.mp3]```\n\n**Write only ID3v2 tags**\n\n```id3tag -2 -a \"[artist]\" -s \"[title]\" [file.mp3]```"],["SYNOPSIS","**id3tag** [_options_] _file_..."],["PARAMETERS","**-1**, **--v1tag**\n> Render only the id3v1 tag.\n\n**-2**, **--v2tag**\n> Render only the id3v2 tag.\n\n**-a**, **--artist** _ARTIST_\n> Set artist name.\n\n**-s**, **--song** _SONG_\n> Set song title.\n\n**-A**, **--album** _ALBUM_\n> Set album name.\n\n**-y**, **--year** _NUM_\n> Set release year.\n\n**-t**, **--track** _NUM_\n> Set track number.\n\n**-T**, **--total** _NUM_\n> Set total number of tracks on the album.\n\n**-g**, **--genre** _NUM_\n> Set genre by number.\n\n**-c**, **--comment** _COMMENT_\n> Set comment.\n\n**-C**, **--desc** _DESCRIPTION_\n> Set comment description.\n\n**-v**, **--version**\n> Display version information.\n\n**-h**, **--help**\n> Display help and exit."],["DESCRIPTION","**id3tag** is a command-line tool for writing ID3 tags to MP3 files. Part of the id3lib/libid3-tools package. Renders both ID3v1 and ID3v2 tags by default; use **-1** or **-2** to render only one type. Can set title, artist, album, year, track number, genre, and comments. Useful for batch tagging with shell scripts. Use **id3info** to view existing tags."],["INSTALL","```apt: sudo apt install libid3-tools```\n\n```dnf: sudo dnf install id3lib```\n\n```pacman: sudo pacman -S id3lib```\n\n```apk: sudo apk add id3lib```\n\n```brew: brew install id3lib```\n\n```nix: nix profile install nixpkgs#id3lib```"],["SEE ALSO","[id3v2](/man/id3v2)(1), [mp3info](/man/mp3info)(1)"]],"e":["id3v2","mp3info"],"x":"id3tag -s \"[title]\" [file.mp3]"},"informant":{"n":"informant","t":"Arch Linux news reader and pacman hook","s":[["TAGLINE","Arch Linux news reader and pacman hook"],["TLDR","**Check** for unread news items\n\n```informant check```\n\n**Read** all unread news interactively\n\n```sudo informant read```\n\n**Mark all news as read** without printing\n\n```sudo informant read --all```\n\n**List** latest news titles\n\n```informant list```\n\n**List only unread** news items\n\n```informant list --unread```\n\n**List news** in reverse order (newest first)\n\n```informant list --reverse```"],["SYNOPSIS","**informant** [_options_] _command_"],["PARAMETERS","**check**\n> Check for unread news items (exit code equals number of unread items). Prints and marks as read if only one unread.\n\n**read**\n> Loop through unread items, printing each and prompting to continue. Specify an item by index or title match.\n\n**read --all**\n> Mark all items as read without printing them.\n\n**list**\n> List recent news titles (read and unread).\n\n**--unread**\n> Only show unread items (with `list`).\n\n**--reverse**\n> Show items newest first (with `list`).\n\n**-n**, **--count** _NUM_\n> Number of items to display.\n\n**-h**, **--help**\n> Display help message."],["DESCRIPTION","**informant** is a tool for reading Arch Linux news from the archlinux.org website. It helps users stay informed about important package updates, manual intervention requirements, and system changes.\n\nThe tool can be integrated with pacman hooks to prevent updates when unread news items exist, ensuring users review important announcements before proceeding with system updates."],["CAVEATS","Specific to Arch Linux. Requires network access to fetch news. Some operations require root privileges to mark news as read in system-wide cache."],["HISTORY","informant was created to address a common issue in Arch Linux: users updating their systems without reading important news announcements that may require manual intervention. It can block pacman updates until news is acknowledged."],["INSTALL","```aur: yay -S informant```"],["SEE ALSO","[pacman](/man/pacman)(8), [yay](/man/yay)(8), [paru](/man/paru)(8)"]],"e":["pacman","yay","paru"],"x":"informant check"},"intel_error_decode":{"n":"intel_error_decode","t":"decodes Intel GPU error states from kernel logs","s":[["TAGLINE","decodes Intel GPU error states from kernel logs"],["TLDR","**Decode GPU error from dmesg**\n\n```dmesg | intel_error_decode```\n\n**Decode error file**\n\n```intel_error_decode < [error_state.txt]```\n\n**Decode with shader info**\n\n```intel_error_decode -s [shader.bin]```"],["SYNOPSIS","**intel_error_decode** [_options_]"],["PARAMETERS","**-s** _FILE_\n> Shader binary for context.\n\n**-c**\n> Color output.\n\n**--help**\n> Display help information."],["DESCRIPTION","**intel_error_decode** decodes Intel GPU error states from kernel logs. It translates hardware register dumps into human-readable information.\n\nThe tool parses GPU hang information to identify failing batches and instructions. Essential for debugging graphics driver issues."],["CAVEATS","Intel graphics only. Requires error state from dmesg. Part of intel-gpu-tools."],["HISTORY","intel_error_decode is part of **intel-gpu-tools**, created for debugging GPU hangs on Intel graphics hardware."],["INSTALL","```apt: sudo apt install intel-gpu-tools```\n\n```pacman: sudo pacman -S intel-gpu-tools```\n\n```zypper: sudo zypper install intel-gpu-tools```\n\n```nix: nix profile install nixpkgs#intel-gpu-tools```"],["SEE ALSO","[intel_bios_reader](/man/intel_bios_reader)(1), [dmesg](/man/dmesg)(1)"]],"e":["intel_bios_reader","dmesg"],"x":"dmesg | intel_error_decode"},"ip-neighbour":{"n":"ip-neighbour","t":"manages the ARP and NDP neighbour tables","s":[["TAGLINE","manages the ARP and NDP neighbour tables"],["TLDR","Display the **neighbour/ARP table**\n\n```ip neighbour```\n\n**Flush** entries for a specific device\n\n```sudo ip neighbour flush dev [eth0]```\n\n**Lookup** a neighbour entry\n\n```ip neighbour get [192.168.1.1] dev [eth0]```\n\n**Add** a static ARP entry\n\n```sudo ip neighbour add [192.168.1.100] lladdr [00:11:22:33:44:55] dev [eth0] nud reachable```\n\n**Delete** an ARP entry\n\n```sudo ip neighbour delete [192.168.1.100] dev [eth0]```\n\n**Replace** an ARP entry\n\n```sudo ip neighbour replace [192.168.1.100] lladdr [00:11:22:33:44:55] dev [eth0]```"],["SYNOPSIS","**ip neighbour** [_command_] [_options_]"],["PARAMETERS","**show** [**dev** _DEVICE_]\n> Display neighbour table entries\n\n**add** _IP_ **lladdr** _MAC_ **dev** _DEVICE_\n> Add a neighbour entry\n\n**delete** _IP_ **dev** _DEVICE_\n> Remove a neighbour entry\n\n**change** _IP_ **lladdr** _MAC_ **dev** _DEVICE_\n> Modify existing entry\n\n**replace** _IP_ **lladdr** _MAC_ **dev** _DEVICE_\n> Add or change entry\n\n**flush** **dev** _DEVICE_\n> Clear entries for a device\n\n**get** _IP_ **dev** _DEVICE_\n> Lookup single entry\n\n**nud** _STATE_\n> Neighbour state: permanent, noarp, reachable, stale, none, incomplete, delay, probe, failed.\n\n**proxy** _ADDR_\n> Manage proxy ARP/NDP entries.\n\n**to** _PREFIX_\n> Filter entries by address prefix (used with show/flush)."],["DESCRIPTION","**ip neighbour** manages the ARP (IPv4) and NDP (IPv6) neighbour tables. These tables map IP addresses to link-layer (MAC) addresses for hosts on directly connected networks.\n\nThe neighbour cache is normally populated automatically through ARP/NDP protocols, but static entries can be added for hosts that don't respond to ARP or for security purposes."],["CAVEATS","Modifying entries requires root privileges. Static entries may become stale if the actual MAC changes. IPv6 uses NDP instead of ARP but is managed through the same interface."],["HISTORY","ip neighbour is part of iproute2 and replaces the older arp command from net-tools. It provides unified IPv4 and IPv6 neighbour management."],["SEE ALSO","[ip](/man/ip)(8), [arp](/man/arp)(8), [arping](/man/arping)(8)"]],"e":["ip","arp","arping"],"x":"ip neighbour"},"ipfs":{"n":"ipfs","t":"InterPlanetary File System distributed peer-to-peer CLI","s":[["TAGLINE","InterPlanetary File System distributed peer-to-peer CLI"],["TLDR","**Initialize IPFS repository**\n\n```ipfs init```\n\n**Start IPFS daemon**\n\n```ipfs daemon```\n\n**Add file to IPFS**\n\n```ipfs add [file]```\n\n**Add directory recursively**\n\n```ipfs add -r [directory]```\n\n**Get file by CID**\n\n```ipfs get [CID]```\n\n**View file contents**\n\n```ipfs cat [CID]```\n\n**Pin a file**\n\n```ipfs pin add [CID]```\n\n**List pinned files**\n\n```ipfs pin ls```\n\n**Show node info**\n\n```ipfs id```"],["SYNOPSIS","**ipfs** _command_ [_options_]"],["SUBCOMMANDS","**init**\n> Initialize local repository.\n\n**daemon**\n> Start IPFS daemon.\n\n**add** _file_\n> Add file to IPFS.\n\n**get** _cid_\n> Download file by CID.\n\n**cat** _cid_\n> Display file contents.\n\n**pin add** _cid_\n> Pin content locally.\n\n**pin ls**\n> List pinned content.\n\n**id**\n> Show node identity.\n\n**swarm peers**\n> List connected peers.\n\n**files**\n> Manage MFS (Mutable File System)."],["PARAMETERS","**-r**, **--recursive**\n> Add directories recursively.\n\n**-q**, **--quiet**\n> Minimal output.\n\n**-w**, **--wrap-with-directory**\n> Wrap files in directory."],["DESCRIPTION","**ipfs** is the command-line interface for the **Kubo** implementation of the InterPlanetary File System, a peer-to-peer content-addressed storage and distribution network. Each file or block is identified by a CID (Content Identifier) derived from the cryptographic hash of its contents, so the same data is reachable from any node that holds it without requiring a fixed host or URL.\n\nA local **ipfs daemon** acts as a gateway between the user and the global IPFS swarm: it dials peers, fetches blocks, pins content for long-term retention, and exposes both an HTTP gateway and a JSON-RPC API. The **add** subcommand stores files in the local datastore and prints the resulting CID; **cat** and **get** retrieve content; **pin add** keeps content from being garbage-collected; and **files** offers a POSIX-like mutable filesystem (MFS) layered on top of immutable IPLD objects."],["CAVEATS","A daemon must be running for most subcommands; **ipfs init** must be called once per user to create the configuration in **~/.ipfs/**. Content is not private by default - anyone who knows a CID can fetch the underlying data through any IPFS node. The default port allocation (4001 TCP/UDP for swarm, 5001 for API, 8080 for gateway) needs to be firewall-friendly for good connectivity."],["HISTORY","**IPFS** was proposed by **Juan Benet** at **Protocol Labs** in a **2014** whitepaper. The first reference implementation in Go (later renamed **Kubo**) reached **0.4.0** in **2016** and **1.0.0** in **2023**. A Rust implementation (**iroh**) and a JavaScript implementation (**js-ipfs**, later **Helia**) followed."],["INSTALL","```pacman: sudo pacman -S kubo```\n\n```apk: sudo apk add kubo```\n\n```zypper: sudo zypper install kubo```\n\n```brew: brew install kubo```\n\n```nix: nix profile install nixpkgs#kubo```"],["SEE ALSO","[curl](/man/curl)(1), [wget](/man/wget)(1), [rsync](/man/rsync)(1)"]],"e":["curl","wget","rsync"],"x":"ipfs init"},"jj-parallelize":{"n":"jj-parallelize","t":"restructures a series of sequential commits to be parallel siblings","s":[["TAGLINE","restructures a series of sequential commits to be parallel siblings"],["TLDR","**Make revisions parallel**\n\n```jj parallelize [rev1]::[rev2]```\n\n**Parallelize range of commits**\n\n```jj parallelize [start]::[end]```"],["SYNOPSIS","**jj** **parallelize** _revisions_"],["DESCRIPTION","**jj parallelize** restructures a series of sequential commits to be parallel siblings. Takes commits that were children of each other and makes them all children of a common parent. Useful for reorganizing history when changes are independent."],["SEE ALSO","[jj](/man/jj)(1), [jj-rebase](/man/jj-rebase)(1)"]],"e":["jj","jj-rebase"],"x":"jj parallelize [rev1]::[rev2]"},"jpegtopnm":{"n":"jpegtopnm","t":"converts JFIF/JPEG images to Netpbm PNM format","s":[["TAGLINE","converts JFIF/JPEG images to Netpbm PNM format"],["TLDR","**Convert JPEG to PNM**\n\n```jpegtopnm [image.jpg] > [image.pnm]```\n\n**Convert with EXIF extraction**\n\n```jpegtopnm -exif=[exif.dat] [image.jpg] > [image.pnm]```\n\n**Repair corrupted JPEG**\n\n```jpegtopnm -repair [corrupted.jpg] > [output.pnm]```\n\n**Display comments from JPEG**\n\n```jpegtopnm -comments [image.jpg] > /dev/null```"],["SYNOPSIS","**jpegtopnm** [_options_] [_filename_]"],["PARAMETERS","**-repair**\n> Attempt to salvage data from corrupted JPEG.\n\n**-exif** _file_\n> Extract EXIF data to file.\n\n**-comments**\n> Print comments from JPEG file.\n\n**-dct** _method_\n> DCT method: int, fast, or float.\n\n**-multiple**\n> Read multiple images from stream."],["DESCRIPTION","**jpegtopnm** converts JFIF/JPEG images to Netpbm PNM format. Outputs PPM for color images or PGM for grayscale. Part of the Netpbm toolkit, it uses the Independent JPEG Group's library for decoding."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pnmtojpeg](/man/pnmtojpeg)(1), [djpeg](/man/djpeg)(1), [pamscale](/man/pamscale)(1)"]],"e":["pnmtojpeg","djpeg","pamscale"],"x":"jpegtopnm [image.jpg] > [image.pnm]"},"kubeaudit":{"n":"kubeaudit","t":"audits Kubernetes clusters and manifests for security vulnerabilities","s":[["TAGLINE","audits Kubernetes clusters and manifests for security vulnerabilities"],["TLDR","**Audit running cluster**\n\n```kubeaudit all```\n\n**Audit manifest files**\n\n```kubeaudit all -f [deployment.yaml]```\n\n**Run specific auditor**\n\n```kubeaudit [privileged] -f [manifest.yaml]```\n\n**Audit in cluster**\n\n```kubeaudit all -c [cluster]```\n\n**Output as JSON**\n\n```kubeaudit all -f [manifest.yaml] -p json```\n\n**Audit specific namespace**\n\n```kubeaudit all -n [namespace]```\n\n**Autofix a manifest file**\n\n```kubeaudit autofix -f [manifest.yaml]```"],["SYNOPSIS","**kubeaudit** [_auditor_] [_options_]"],["PARAMETERS","**all**\n> Run all auditors.\n\n**-f** _file_\n> Audit manifest file.\n\n**-d** _dir_\n> Audit directory of manifests.\n\n**-c**, **--context** _name_\n> Kubernetes context.\n\n**-n**, **--namespace** _name_\n> Target namespace.\n\n**-p**, **--format** _format_\n> Output format: logrus, json, sarif.\n\n**--kubeconfig** _path_\n> Path to kubeconfig file.\n\n**--minSeverity** _level_\n> Minimum severity to report (error, warning, info)."],["AUDITORS","```\nprivileged       Privileged containers\ncapabilities     Added capabilities\nrootfs           Read-only root filesystem\nrunAsNonRoot     Running as root user\nlimits           Resource limits\nhostns           Host namespace usage\nseccomp          Seccomp profile\napparmor         AppArmor profile\nimage            Image tag requirements\nautomountServiceAccountToken  Service account token\n```"],["DESCRIPTION","**kubeaudit** audits Kubernetes clusters and manifests for security vulnerabilities. It checks configurations against security best practices.\n\nThe tool can audit running clusters, local manifests, or both, providing actionable findings for improving cluster security."],["CAVEATS","Some findings may be intentional configurations. Cluster audit requires permissions. False positives possible for specialized workloads."],["HISTORY","kubeaudit was created by **Shopify** to identify security risks in Kubernetes deployments before they reach production."],["INSTALL","```zypper: sudo zypper install kubeaudit```\n\n```brew: brew install kubeaudit```\n\n```nix: nix profile install nixpkgs#kubeaudit```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [kube-linter](/man/kube-linter)(1), [trivy](/man/trivy)(1)"]],"e":["kubectl","kube-linter","trivy"],"x":"kubeaudit all"},"kwriteconfig5":{"n":"kwriteconfig5","t":"writes values to KDE Plasma 5 configuration files","s":[["TAGLINE","writes values to KDE Plasma 5 configuration files"],["TLDR","Set a **global configuration** key\n\n```kwriteconfig5 --group [group_name] --key [key] [value]```\n\nSet a key in a **specific file**\n\n```kwriteconfig5 --file [path/to/file] --group [group_name] --key [key] [value]```\n\n**Delete** a key\n\n```kwriteconfig5 --group [group_name] --key [key] --delete```\n\nEnable **systemd boot** for Plasma session\n\n```kwriteconfig5 --file startkderc --group General --key systemdBoot true```\n\nHide title bar when **window is maximized**\n\n```kwriteconfig5 --file ~/.config/kwinrc --group Windows --key BorderlessMaximizedWindows true```"],["SYNOPSIS","**kwriteconfig5** [_options_]"],["PARAMETERS","**--file** _FILE_\n> Configuration file to write to\n\n**--group** _GROUP_\n> Configuration group (section)\n\n**--key** _KEY_\n> Key name to set\n\n**--delete**\n> Delete the key instead of setting it\n\n**--type** _TYPE_\n> Value type (bool, string, int, etc.)"],["DESCRIPTION","**kwriteconfig5** writes values to KDE Plasma 5 configuration files. It enables scripted modification of KDE settings without manually editing configuration files.\n\nConfiguration files are stored in ~/.config/ with names like kdeglobals, kwinrc, plasmarc. Changes may require restarting the affected application or Plasma session to take effect."],["CAVEATS","KDE 5 specific; KDE 6 uses kwriteconfig6. Some changes require restarting Plasma or logging out. Groups and keys are case-sensitive."],["HISTORY","kwriteconfig5 is part of KDE Frameworks 5, complementing kreadconfig5 for reading settings. It enables automation of KDE configuration in scripts and dotfile management."],["INSTALL","```apt: sudo apt install libkf5config-bin```\n\n```pacman: sudo pacman -S kconfig5```\n\n```apk: sudo apk add kconfig5```"],["SEE ALSO","[kreadconfig5](/man/kreadconfig5)(1), [dconf](/man/dconf)(1)"]],"e":["kreadconfig5","dconf"],"x":"kwriteconfig5 --group [group_name] --key [key] [value]"},"lnav":{"n":"lnav","t":"advanced log file viewer that leverages semantic information from log files","s":[["TAGLINE","advanced log file viewer that leverages semantic information from log files"],["TLDR","View **log files**\n\n```lnav path/to/log```\n\nView logs in a **directory**\n\n```lnav /var/log/```\n\nView **remote** log files via SSH\n\n```lnav ssh user@host:/var/log/syslog```\n\n**Validate** log format configuration\n\n```lnav -C path/to/log```\n\nInclude **rotated/archived** logs\n\n```lnav -R /var/log/syslog```\n\nRun **headless** with command\n\n```lnav -n -c \":filter-in error\" path/to/log```"],["SYNOPSIS","**lnav** [**-hHVCnqirRt**] [**-c** _CMD_] [**-f** _PATH_] [**-I** _PATH_] [**-d** _FILE_] [**-w** _FILE_] [_logfile1_ _logfile2_ ...]"],["DESCRIPTION","**lnav** is an advanced log file viewer that leverages semantic information from log files. It can interleave messages from multiple files, create time-based histograms, and provides efficient navigation through logs using hotkeys."],["PARAMETERS","**-h**\n> Display help information\n\n**-H**\n> Show internal help text\n\n**-V**\n> Print version details\n\n**-n**\n> Run in headless mode without UI\n\n**-c CMD**\n> Execute command after loading files\n\n**-f PATH**\n> Run commands from a configuration file\n\n**-I PATH**\n> Add directory to configuration search path\n\n**-q**\n> Quiet mode; suppress messages\n\n**-i**\n> Install format files to user directory. Pass 'extra' to install the default set of third-party formats.\n\n**-u**\n> Update installed format files from repositories\n\n**-a**\n> Load all of the most recent log file types\n\n**-C**\n> Validate configuration and log format files\n\n**-d FILE**\n> Output debug messages to file\n\n**-r**\n> Recursively load files from directories\n\n**-R**\n> Include rotated and archived log files\n\n**-t**\n> Add timestamps to stdin data\n\n**-w FILE**\n> Write stdin content to file"],["CONFIGURATION","**~/.lnav/formats/**\n> Directory for user-defined log format files (JSON format definitions).\n\n**~/.lnav/configs/**\n> Directory for user configuration files."],["CAVEATS","Press \"?\" for online help while running, and \"q\" to exit. lnav automatically detects and parses many common log formats (syslog, Apache, strace, etc.). Custom formats can be defined in JSON configuration files. Use \":filter-in\" and \":filter-out\" commands to filter log lines interactively."],["HISTORY","**lnav** was created to provide an advanced log file navigator with automatic format detection, syntax highlighting, and efficient navigation for system administrators and developers."],["INSTALL","```apt: sudo apt install lnav```\n\n```dnf: sudo dnf install lnav```\n\n```pacman: sudo pacman -S lnav```\n\n```apk: sudo apk add lnav```\n\n```zypper: sudo zypper install lnav```\n\n```brew: brew install lnav```\n\n```nix: nix profile install nixpkgs#lnav```"],["SEE ALSO","[less](/man/less)(1), [tail](/man/tail)(1), [journalctl](/man/journalctl)(1), [multitail](/man/multitail)(1)"]],"e":["less","tail","journalctl","multitail"],"x":"lnav path/to/log"},"mcptube":{"n":"mcptube","t":"YouTube video knowledge engine with transcripts and vision","s":[["TAGLINE","YouTube video knowledge engine with transcripts and vision"],["TLDR","**Add** a YouTube video to the library\n\n```mcptube add \"[youtube_url]\"```\n\n**Add** a video in text-only mode (no frame extraction)\n\n```mcptube add \"[youtube_url]\" --text-only```\n\n**List** all videos in the library\n\n```mcptube list```\n\n**Search** across video transcripts\n\n```mcptube search \"[query]\"```\n\n**Ask a question** about video content\n\n```mcptube ask \"[question]\"```\n\n**Generate a report** on a specific topic\n\n```mcptube report [video_query] --focus \"[topic]\"```\n\n**Start** the MCP server\n\n```mcptube serve --stdio```"],["SYNOPSIS","**mcptube** \\<command\\> [_options_]"],["PARAMETERS","**add** _url_ [_--text-only_]\n> Add a YouTube video to the library; use --text-only to skip frame extraction\n\n**remove** _query_\n> Remove a video from the library\n\n**list**\n> List all videos in the library\n\n**info** _query_\n> Show detailed information about a video\n\n**search** _query_\n> Search across video transcripts\n\n**ask** _question_\n> Ask a natural language question about video content\n\n**frame** _query_ _timestamp_\n> Extract a frame at a specific timestamp\n\n**frame-query** _query_ _description_\n> Find frames matching a visual description\n\n**classify** _query_\n> Classify video content\n\n**report** _query_ [_--focus topic_] [_--format html_] [_-o file_]\n> Generate a report about a video\n\n**report-query** _topic_ [_--tag tag_]\n> Generate a report across videos by topic\n\n**discover** _topic_\n> Discover new videos related to a topic\n\n**wiki list** [_--type type_] [_--tag tag_]\n> List wiki pages\n\n**wiki show** _slug_\n> Display a wiki page\n\n**wiki search** _query_\n> Search wiki content\n\n**wiki toc**\n> Show wiki table of contents\n\n**wiki export** [_--format html_] [_--page slug_]\n> Export wiki pages\n\n**serve** [_--stdio_] [_--host host_] [_--port port_] [_--reload_]\n> Start the MCP server"],["DESCRIPTION","**mcptube** is a YouTube video knowledge engine that extracts metadata, transcripts, and frames from YouTube videos, indexes them for semantic search, and exposes everything as both a CLI tool and an MCP (Model Context Protocol) server.\n\nThe tool builds a persistent wiki knowledge base that grows richer with each video ingested, rather than treating videos as isolated searchable chunks. It uses **scene-change detection** instead of fixed-interval sampling to capture high-information-density frames. Search combines FTS5 keyword matching with LLM-powered reasoning for hybrid retrieval.\n\nmcptube operates in two modes: **CLI mode** using your own API keys (Anthropic, OpenAI, or Google) for deterministic results, and **MCP passthrough mode** where the connected AI assistant analyzes data using its own model, avoiding double-billing."],["CAVEATS","Requires **Python 3.12 or 3.13** (ChromaDB is not compatible with Python 3.14) and **ffmpeg** for frame extraction. Full feature set requires API keys from at least one LLM provider (Anthropic, OpenAI, or Google). The MCP server is currently local-only. Text-only mode is available for cost reduction when vision features are not needed."],["HISTORY","mcptube was created by **0xchamin** and written in **Python**. It evolved from a simple transcript search tool into a full video knowledge engine with wiki capabilities, vision-based frame analysis, and MCP server integration for use with AI coding assistants."],["SEE ALSO","[yt-dlp](/man/yt-dlp)(1), [ffmpeg](/man/ffmpeg)(1), [youtube-dl](/man/youtube-dl)(1)"]],"e":["yt-dlp","ffmpeg","youtube-dl"],"x":"mcptube add \"[youtube_url]\""},"meteor":{"n":"meteor","t":"full-stack JavaScript framework","s":[["TAGLINE","full-stack JavaScript framework"],["TLDR","**Create new project**\n\n```meteor create [project-name]```\n\n**Run development server**\n\n```meteor```\n\n**Add package**\n\n```meteor add [package-name]```\n\n**Remove package**\n\n```meteor remove [package-name]```\n\n**Build for production**\n\n```meteor build [output-dir]```\n\n**Deploy to Galaxy**\n\n```meteor deploy [app.meteorapp.com]```"],["SYNOPSIS","**meteor** [_command_] [_options_]"],["PARAMETERS","_COMMAND_\n> Meteor command.\n\n**create** _NAME_\n> Create new project.\n\n**add** _PKG_\n> Add package.\n\n**remove** _PKG_\n> Remove package.\n\n**build** _DIR_\n> Build for production.\n\n**deploy** _HOST_\n> Deploy application.\n\n**--help**\n> Display help information."],["DESCRIPTION","**meteor** is the command-line driver for the **Meteor.js** full-stack JavaScript framework. Running **meteor** in a project directory bundles the client, server, and database scripts together, starts a local MongoDB instance, watches the source tree for changes, and serves the application with hot-code reload to connected clients.\n\nBeyond the dev server, the CLI bootstraps new projects (**meteor create**), installs Atmosphere or npm packages (**meteor add**, **meteor npm install**), produces architecture-specific bundles for deployment (**meteor build**), and pushes apps to Meteor's hosted **Galaxy** platform (**meteor deploy**). Underneath sits a reactive data layer (DDP) that synchronizes documents between server-side MongoDB collections and client-side Minimongo caches over WebSockets."],["CAVEATS","Node.js-based; modern versions require Node 14 or later. MongoDB is the only first-class database, though external SQL stores can be reached through community packages. The reactivity model (Tracker, publications, subscriptions) has a learning curve and can become a performance bottleneck at scale."],["HISTORY","**Meteor** was launched by **Meteor Development Group** in **2012** and released **1.0** in **2014**. Stewardship moved to **Tiny** in **2022**, where development continues alongside Meteor's Galaxy hosting service."],["INSTALL","```nix: nix profile install nixpkgs#meteor```"],["SEE ALSO","[node](/man/node)(1), [npm](/man/npm)(1), [mongod](/man/mongod)(1)"]],"e":["node","npm","mongod"],"x":"meteor create [project-name]"},"moro":{"n":"moro","t":"command-line work hours tracker","s":[["TAGLINE","command-line work hours tracker"],["TLDR","**Clock in (start work timer)**\n\n```moro hi```\n\n**Clock in at a specific time**\n\n```moro hi [09:30]```\n\n**Clock out (stop work timer)**\n\n```moro bye```\n\n**Set break time in minutes**\n\n```moro break [30]```\n\n**Show today's work report**\n\n```moro report```\n\n**Show report for all days**\n\n```moro report --all```\n\n**Set default break time**\n\n```moro config --break [45]```\n\n**Clear all recorded data**\n\n```moro clear```"],["SYNOPSIS","**moro** [_command_] [_options_]"],["PARAMETERS","**hi** [_HH:MM_]\n> Clock in. Optionally specify a past start time.\n\n**bye** [_HH:MM_]\n> Clock out. Optionally specify a past end time.\n\n**break** _MINUTES_\n> Set break time in minutes.\n\n**report** [**--all**]\n> Display work report. Use --all to show all days.\n\n**config** **--break** _MINUTES_\n> Set default break time.\n\n**clear**\n> Clear all recorded data.\n\n**--help**\n> Display help information."],["DESCRIPTION","**moro** is a command-line work hours tracker. It records clock in/out times and calculates total work hours minus breaks.\n\nThe formula is: (clock out time) - (clock in time) - (breaks) = work hours. Default break is 30 minutes."],["CAVEATS","Node.js based. Simple flat file storage. Local machine tracking only. The project is deprecated; the released npm version is considered the only stable version."],["HISTORY","moro was created as a **simple time tracker** for developers who work from the terminal."],["INSTALL","```aur: yay -S moro```"],["SEE ALSO","[timew](/man/timew)(1), [watson](/man/watson)(1)"]],"e":["timew","watson"],"x":"moro hi"},"nap":{"n":"nap","t":"Code snippet manager for the terminal","s":[["TAGLINE","Code snippet manager for the terminal"],["TLDR","**Launch the snippet TUI**\n\n```nap```\n\n**Create a new snippet**\n\n```nap \"[snippet_name]\"```\n\n**List all snippets**\n\n```nap list```\n\n**Fuzzy find and copy a snippet**\n\n```nap [query]```"],["SYNOPSIS","**nap** [_options_] [_query_]"],["DESCRIPTION","**nap** is a code snippet manager with both a CLI and TUI interface. The TUI lets you browse, manage, and organize code snippets directly in your terminal, while the CLI provides quick access with fuzzy searching.\n\nSnippets can be created from files or stdin, organized into folders, and copied to the clipboard. The tool is configured through **$XDG_CONFIG_HOME/nap/config.yaml**."],["CONFIGURATION","**$XDG_CONFIG_HOME/nap/config.yaml**\n> Configuration file for snippet storage location and behavior."],["CAVEATS","Snippets are stored locally. No built-in sync across devices."],["HISTORY","**nap** was created by **maaslalani** and is written in **Go** using the Charm framework."],["INSTALL","```brew: brew install nap```\n\n```nix: nix profile install nixpkgs#nap```"],["SEE ALSO","[pet](/man/pet)(1)"]],"e":["pet"],"x":"nap"},"nebula":{"n":"nebula","t":"scalable overlay networking tool for connecting hosts across networks","s":[["TAGLINE","scalable overlay networking tool for connecting hosts across networks"],["TLDR","**Generate CA certificate**\n\n```nebula-cert ca -name \"[My Organization]\"```\n\n**Sign a host certificate**\n\n```nebula-cert sign -name \"[server1]\" -ip \"[10.0.0.1/24]\"```\n\n**Sign with groups**\n\n```nebula-cert sign -name \"[web1]\" -ip \"[10.0.0.2/24]\" -groups \"[servers,web]\"```\n\n**Start Nebula**\n\n```nebula -config [/etc/nebula/config.yaml]```\n\n**Print certificate details**\n\n```nebula-cert print -path [host.crt]```\n\n**Verify certificate**\n\n```nebula-cert verify -ca [ca.crt] -crt [host.crt]```\n\n**Generate example config**\n\n```nebula -print-default```"],["SYNOPSIS","**nebula** [_options_]\n\n**nebula-cert** _command_ [_options_]"],["NEBULA OPTIONS","**-config** _path_\n> Configuration file path.\n\n**-test**\n> Test configuration and exit.\n\n**-print-default**\n> Print default configuration.\n\n**-version**\n> Show version information."],["NEBULA-CERT COMMANDS","**ca**\n> Generate CA certificate and key.\n\n**sign**\n> Sign a host certificate.\n\n**print**\n> Print certificate details.\n\n**verify**\n> Verify certificate against CA."],["CA OPTIONS","**-name** _name_\n> CA name.\n\n**-duration** _duration_\n> Validity duration. Default: 1 year.\n\n**-out-crt** _path_\n> CA certificate output.\n\n**-out-key** _path_\n> CA key output."],["SIGN OPTIONS","**-name** _name_\n> Host name.\n\n**-ip** _cidr_\n> Nebula IP address with subnet.\n\n**-groups** _list_\n> Comma-separated group names.\n\n**-ca-crt** _path_\n> CA certificate path.\n\n**-ca-key** _path_\n> CA key path.\n\n**-out-crt** _path_\n> Host certificate output.\n\n**-out-key** _path_\n> Host key output.\n\n**-duration** _duration_\n> Certificate validity."],["DESCRIPTION","**nebula** is a scalable overlay networking tool for connecting hosts across networks. It uses certificate-based authentication and peer-to-peer connectivity.\n\nEach Nebula network has a CA that signs host certificates. Certificates define the host's Nebula IP address and group memberships for access control.\n\n**Lighthouses** are known nodes that help other nodes discover each other. At least one lighthouse with a public IP is needed. Other nodes connect peer-to-peer once discovered.\n\nConfiguration (YAML) defines the CA, host cert/key, lighthouses, firewall rules, and network settings. Firewall rules use groups from certificates for access control.\n\nNebula punches through NAT using UDP hole punching. Nodes behind NAT can communicate directly without routing through central servers."],["CAVEATS","Requires UDP port (default 4242) accessible on lighthouses. Clock sync important for certificate validation. Certificate revocation requires redistribution. Some NAT types may prevent hole punching."],["HISTORY","Nebula was created at **Slack** and open-sourced in **November 2019**. It was developed to connect Slack's global infrastructure securely. The project was designed for simplicity and scale, using modern cryptography and mesh networking principles. Nebula is used by organizations needing software-defined networks across cloud providers and on-premise infrastructure."],["INSTALL","```apt: sudo apt install nebula```\n\n```dnf: sudo dnf install nebula```\n\n```pacman: sudo pacman -S nebula```\n\n```apk: sudo apk add nebula```\n\n```zypper: sudo zypper install nebula```\n\n```brew: brew install nebula```\n\n```nix: nix profile install nixpkgs#nebula```"],["SEE ALSO","[headscale](/man/headscale)(1), [tailscale](/man/tailscale)(1), [zerotier-one](/man/zerotier-one)(1)"]],"e":["headscale","tailscale","zerotier-one"],"x":"nebula-cert ca -name \"[My Organization]\""},"ng-version":{"n":"ng-version","t":"displays Angular CLI and package versions","s":[["TAGLINE","displays Angular CLI and package versions"],["TLDR","**Show Angular versions**\n\n```ng version```\n\n**Show version as JSON**\n\n```ng version --json```"],["SYNOPSIS","**ng** **version** [_options_]"],["PARAMETERS","**--json**\n> Output as JSON.\n\n**--help**\n> Display help information."],["DESCRIPTION","**ng version** displays Angular CLI and package versions, including `@angular/core`, `@angular/cli`, RxJS, TypeScript, Node.js, and the package manager in use. When run outside an Angular workspace it prints only the globally installed CLI information. `v` is accepted as a short alias (`ng v`).\n\nThe output is useful when reporting bugs, auditing dependency versions, or diagnosing build issues."],["CAVEATS","Part of Angular CLI. Useful for troubleshooting. Shows local project versions."],["HISTORY","ng version is part of **Angular CLI** for displaying version information and dependency details."],["SEE ALSO","[ng](/man/ng)(1), [node](/man/node)(1), [npm](/man/npm)(1)"]],"e":["ng","node","npm"],"x":"ng version"},"nix3-why-depends":{"n":"nix3-why-depends","t":"traces dependency relationships","s":[["TAGLINE","traces dependency relationships"],["TLDR","**Show dependency path**\n\n```nix3 why-depends nixpkgs#[package] nixpkgs#[dep]```\n\n**Show all paths**\n\n```nix3 why-depends --all [pkg] [dep]```"],["SYNOPSIS","**nix3** **why-depends** [_options_] _package_ _dependency_"],["PARAMETERS","_PACKAGE_\n> Package to analyze.\n\n_DEPENDENCY_\n> Dependency to trace.\n\n**--all**\n> Show all paths.\n\n**--help**\n> Display help information."],["DESCRIPTION","**nix3 why-depends** traces dependency relationships. Shows why packages depend on others.\n\nThe tool debugs package closures. Helps reduce unnecessary dependencies."],["CAVEATS","Alias for nix why-depends. Closure analysis. Evaluation required."],["HISTORY","nix3 why-depends is part of **Nix 3.x** for debugging dependency graphs."],["SEE ALSO","[nix](/man/nix)(1), [nix-why-depends](/man/nix-why-depends)(1), [nix-store](/man/nix-store)(1)"]],"e":["nix","nix-why-depends","nix-store"],"x":"nix3 why-depends nixpkgs#[package] nixpkgs#[dep]"},"ntpctl":{"n":"ntpctl","t":"queries the OpenNTPD daemon for synchronization status","s":[["TAGLINE","queries the OpenNTPD daemon for synchronization status"],["TLDR","**Show synchronization status**\n\n```ntpctl -s status```\n\n**Show all peers**\n\n```ntpctl -s peers```\n\n**Show all sensors**\n\n```ntpctl -s sensors```\n\n**Show all information**\n\n```ntpctl -s all```"],["SYNOPSIS","**ntpctl** [_-s command_]"],["PARAMETERS","**-s status**\n> Show synchronization status.\n\n**-s peers**\n> Show peer status.\n\n**-s sensors**\n> Show sensor status.\n\n**-s all**\n> Show all information."],["DESCRIPTION","**ntpctl** queries the OpenNTPD daemon for synchronization status. It displays information about time sources and synchronization state.\n\nThe status view shows whether the clock is synchronized, the stratum level, and the offset from the reference time. A synchronized clock means the system time is being corrected.\n\nPeer display lists configured NTP servers with their status, stratum, and offset. This helps identify which servers are reachable and providing good time.\n\nSensors show hardware time sources like GPS receivers or radio clocks that may be configured. These provide local reference time independent of network.\n\nThe tool communicates with ntpd through a control socket. The daemon must be running with control socket enabled for ntpctl to work.\n\nOpenNTPD emphasizes security and simplicity over feature completeness. ntpctl reflects this by providing essential status information without complex queries."],["CAVEATS","OpenNTPD specific - not compatible with ISC ntpd. Control socket must be enabled in ntpd configuration. Limited compared to ntpq."],["HISTORY","**ntpctl** is part of **OpenNTPD**, developed by the **OpenBSD** project starting around **2004**. OpenNTPD was created as a simpler, more secure alternative to the reference NTP implementation."],["INSTALL","```apt: sudo apt install openntpd```\n\n```pacman: sudo pacman -S openntpd```\n\n```apk: sudo apk add openntpd```\n\n```nix: nix profile install nixpkgs#openntpd```"],["SEE ALSO","[ntpd](/man/ntpd)(8), [ntpq](/man/ntpq)(1), [chronyc](/man/chronyc)(1), [timedatectl](/man/timedatectl)(1)"]],"e":["ntpd","ntpq","chronyc","timedatectl"],"x":"ntpctl -s status"},"ocaml":{"n":"ocaml","t":"interactive toplevel for OCaml, a statically-typed functional programming","s":[["TAGLINE","interactive toplevel for OCaml, a statically-typed functional programming"],["TLDR","**Start interactive toplevel**\n\n```ocaml```\n\n**Run OCaml script**\n\n```ocaml [script.ml]```\n\n**Run with library**\n\n```ocaml -I +[str] str.cma [script.ml]```\n\n**Run expression**\n\n```ocaml -e \"print_endline \\\"Hello\\\"\"```\n\n**Start with custom init**\n\n```ocaml -init [.ocamlinit]```\n\n**Include library path**\n\n```ocaml -I [/path/to/lib]```\n\n**Run with profiling**\n\n```ocaml -p [script.ml]```"],["SYNOPSIS","**ocaml** [_-I dir_] [_-init file_] [_-e expr_] [_options_] [_script.ml_] [_arguments_]"],["PARAMETERS","**-I** _DIR_\n> Add directory to library search path.\n\n**-init** _FILE_\n> Load initialization file.\n\n**-noinit**\n> Don't load .ocamlinit.\n\n**-e** _EXPR_\n> Evaluate expression and exit.\n\n**-stdin**\n> Read script from stdin.\n\n**-noprompt**\n> Suppress prompt in interactive mode.\n\n**-w** _FLAGS_\n> Warning settings.\n\n**-warn-error** _FLAGS_\n> Treat warnings as errors.\n\n**-version**\n> Print version.\n\n**-vnum**\n> Print version number only.\n\n**-help**\n> Show help."],["TOPLEVEL COMMANDS","**#use \"file.ml\";;**\n> Load source file.\n\n**#load \"module.cma\";;**\n> Load bytecode library.\n\n**#directory \"path\";;**\n> Add to search path.\n\n**#show identifier;;**\n> Show type/module info.\n\n**#trace function;;**\n> Trace function calls.\n\n**#quit;;**\n> Exit toplevel.\n\n**#help;;**\n> Show directives help."],["DESCRIPTION","**ocaml** is the interactive toplevel (REPL) for OCaml, a statically-typed functional programming language with type inference.\n\nThe toplevel reads expressions, compiles them, executes, and prints results with types. Expressions end with `;;`. The type inference system catches errors at compile time, even in interactive mode.\n\nLoading files with #use executes their definitions in the current session. Libraries (.cma bytecode) load with #load. The -I flag adds directories to search for modules.\n\nThe .ocamlinit file in home or current directory runs on startup. It can load commonly used libraries, define utilities, and configure the environment.\n\nIntegration with utop provides an enhanced toplevel with syntax highlighting, completion, and history. Many users prefer utop for interactive development.\n\nFor larger projects, the Dune build system handles compilation, but ocaml remains useful for exploration, prototyping, and learning."],["CAVEATS","Expressions must end with ;;. Some libraries need explicit loading. Error messages can be cryptic for beginners. Mutable state possible but discouraged. Build systems preferred for real projects."],["HISTORY","**OCaml** evolved from Caml Light, developed at **INRIA** in France since the **1980s**. OCaml (Objective Caml) added objects and native compilation in **1996**. The language influenced Rust, F#, and others. The ocaml toplevel has been the standard REPL throughout its history."],["INSTALL","```apt: sudo apt install ocaml-interp```\n\n```dnf: sudo dnf install ocaml```\n\n```pacman: sudo pacman -S ocaml```\n\n```apk: sudo apk add ocaml5```\n\n```zypper: sudo zypper install ocaml```\n\n```brew: brew install ocaml```\n\n```nix: nix profile install nixpkgs#ocaml```"],["SEE ALSO","[ocamlopt](/man/ocamlopt)(1), [ocamlc](/man/ocamlc)(1), [dune](/man/dune)(1), [opam](/man/opam)(1), [utop](/man/utop)(1)"]],"e":["ocamlopt","ocamlc","dune","opam","utop"],"x":"ocaml"},"openai":{"n":"openai","t":"chat completion","s":[["TAGLINE","chat completion"],["TLDR","**Chat completion**\n\n```openai api chat.completions.create -m gpt-4 -g user \"[prompt]\"```\n\n**Create embedding**\n\n```openai api embeddings.create -m text-embedding-ada-002 -i \"[text]\"```\n\n**List models**\n\n```openai api models.list```\n\n**Generate image**\n\n```openai api images.generate -p \"[description]\"```"],["SYNOPSIS","**openai** [_command_] [_options_]"],["PARAMETERS","**api** _ENDPOINT_\n> API command.\n\n**-m** _MODEL_\n> Model name.\n\n**-g** _ROLE_ _MESSAGE_\n> Message with role.\n\n**-i** _INPUT_\n> Input text.\n\n**-p** _PROMPT_\n> Prompt text.\n\n**--help**\n> Display help information."],["DESCRIPTION","**openai** is the OpenAI CLI. Interacts with GPT models and other APIs.\n\nThe tool provides command-line API access. Requires API key."],["CAVEATS","Requires OPENAI_API_KEY. API usage billed. Rate limits apply."],["HISTORY","openai CLI was created by **OpenAI** for command-line API access."],["SEE ALSO","[curl](/man/curl)(1), [ollama](/man/ollama)(1)"]],"e":["curl","ollama"],"x":"openai api chat.completions.create -m gpt-4 -g user \"[prompt]\""},"opencode-web":{"n":"opencode-web","t":"Start OpenCode with web interface","s":[["TAGLINE","Start OpenCode with web interface"],["TLDR","**Start OpenCode** with web UI on a random local port\n\n```opencode web```\n\n**Bind to all interfaces** on a fixed port (for remote access)\n\n```opencode web --hostname 0.0.0.0 --port [4096]```\n\n**Advertise the service over mDNS/Bonjour**\n\n```opencode web --mdns```\n\n**Enable CORS** for cross-origin browser clients\n\n```opencode web --cors```\n\n**Stream server logs** to the terminal\n\n```opencode web --print-logs --log-level debug```"],["SYNOPSIS","**opencode web** [_options_]"],["PARAMETERS","**--hostname** _HOST_\n> Interface to bind to (default **127.0.0.1**).\n\n**--port** _PORT_\n> TCP port (default **0** — pick an ephemeral port).\n\n**--mdns**\n> Announce the server on the local network via mDNS.\n\n**--mdns-domain** _DOMAIN_\n> Override the mDNS service domain (default **.local**).\n\n**--cors**\n> Allow cross-origin requests from browsers.\n\n**--print-logs**\n> Print server-side logs to the terminal.\n\n**--log-level** _LEVEL_\n> Log verbosity: **trace**, **debug**, **info**, **warn**, **error**.\n\n**-h**, **--help**, **-v**, **--version**\n> Display help or version information."],["DESCRIPTION","**opencode web** starts a headless OpenCode server that exposes a web-based interface instead of the terminal TUI. This allows interacting with OpenCode through a browser, which can be useful for remote access, sharing sessions, or when a graphical interface is preferred over the terminal. When **--mdns** is enabled the service is discoverable on the local network without needing to know its IP."],["INSTALL","```pacman: sudo pacman -S opencode```\n\n```brew: brew install opencode```\n\n```nix: nix profile install nixpkgs#opencode```"],["SEE ALSO","[opencode](/man/opencode)(1), [opencode-session](/man/opencode-session)(1)"]],"e":["opencode","opencode-session"],"x":"opencode web"},"pam_mail":{"n":"pam_mail","t":"notifies users of new mail","s":[["TAGLINE","notifies users of new mail"],["TLDR","**Check for new mail**\n\n```session optional pam_mail.so```\n\n**Show standard mail path**\n\n```session optional pam_mail.so standard```\n\n**Custom mail directory**\n\n```session optional pam_mail.so dir=~/Maildir```\n\n**Quiet on empty**\n\n```session optional pam_mail.so quiet```\n\n**Suppress new-mail check (login MOTD only)**\n\n```session optional pam_mail.so nopen```"],["SYNOPSIS","**pam_mail.so** [_options_]"],["PARAMETERS","**dir=**_PATH_\n> Mail directory (overrides the MAIL environment variable).\n\n**standard**\n> Use the standard mail spool location.\n\n**close**\n> Also print a notice at session close if new mail has arrived.\n\n**empty**\n> Report even when the mailbox is empty.\n\n**hash=**_N_\n> Look in N hashed subdirectories under `dir` (for sites with hashed spools).\n\n**nopen**\n> Do not inspect any mailboxes on login (suppresses the status message).\n\n**noenv**\n> Do not export the `MAIL` environment variable.\n\n**quiet**\n> Only report if there is new mail (no \"no mail\" message)."],["DESCRIPTION","**pam_mail** notifies users of new mail. Checks mail spool at login.\n\nThe module displays mail notifications. Session login feature."],["CAVEATS","Session module. Mail spool dependent. Optional notification."],["HISTORY","pam_mail provides **mail notification** at login for Unix mail spools."],["SEE ALSO","[pam](/man/pam)(8), [mail](/man/mail)(1)"]],"e":["pam","mail"],"x":"session optional pam_mail.so"},"papermc":{"n":"papermc","t":"high-performance Minecraft server","s":[["TAGLINE","high-performance Minecraft server"],["TLDR","**Start Minecraft server**\n\n```java -jar paper.jar```\n\n**Start with memory limits**\n\n```java -Xms2G -Xmx4G -jar paper.jar```\n\n**Accept EULA**\n\n```java -jar paper.jar --accepteula```\n\n**Specify world folder**\n\n```java -jar paper.jar --world-dir [worlds]```"],["SYNOPSIS","**java** **-jar** paper.jar [_options_]"],["PARAMETERS","**--accepteula**\n> Accept EULA automatically.\n\n**--world-dir** _DIR_\n> World storage directory.\n\n**--plugins** _DIR_\n> Plugins directory.\n\n**--config** _FILE_\n> Configuration file.\n\n**nogui**\n> Start without GUI."],["DESCRIPTION","**PaperMC** is a high-performance Minecraft server. Fork of Spigot with optimizations.\n\nThe server provides improved performance. Plugin compatible with Bukkit/Spigot."],["CAVEATS","Requires Java. Memory allocation important. Plugin compatibility varies."],["HISTORY","PaperMC was forked from **Spigot** for improved Minecraft server performance."],["INSTALL","```nix: nix profile install nixpkgs#papermc```"],["SEE ALSO","[java](/man/java)(1), [screen](/man/screen)(1)"]],"e":["java","screen"],"x":"java -jar paper.jar"},"phpstorm":{"n":"phpstorm","t":"launches PhpStorm IDE","s":[["TAGLINE","launches PhpStorm IDE"],["TLDR","**Open project**\n\n```phpstorm [path/to/project]```\n\n**Open file at line**\n\n```phpstorm --line [42] [file.php]```\n\n**Compare files**\n\n```phpstorm diff [file1.php] [file2.php]```\n\n**Open in new window**\n\n```phpstorm --new-window [path/to/project]```\n\n**Wait for close**\n\n```phpstorm --wait [file.php]```\n\n**Inspect code**\n\n```phpstorm inspect [project] [output] -v2```"],["SYNOPSIS","**phpstorm** [_--line n_] [_--new-window_] [_options_] [_path_]"],["PARAMETERS","**--line** _N_\n> Open at line number.\n\n**--column** _N_\n> Open at column.\n\n**--new-window**\n> Force new window.\n\n**--wait**\n> Wait until closed.\n\n**diff**\n> Compare two files.\n\n**inspect**\n> Run code inspection.\n\n**--help**\n> Show help."],["DESCRIPTION","**phpstorm** launches PhpStorm IDE. It opens projects and files.\n\nJetBrains PHP IDE. Professional development environment.\n\nCommand-line launcher enables integration. Open from terminal.\n\nDiff mode compares files. Built-in comparison tool."],["CAVEATS","Requires PhpStorm installation. Commercial software. Launcher script location varies."],["HISTORY","**PhpStorm** is developed by **JetBrains**. It provides professional PHP development tools with intelligent coding assistance."],["INSTALL","```aur: yay -S phpstorm```"],["SEE ALSO","[idea](/man/idea)(1), [webstorm](/man/webstorm)(1), [code](/man/code)(1)"]],"e":["idea","webstorm","code"],"x":"phpstorm [path/to/project]"},"phpunit":{"n":"phpunit","t":"PHP testing framework","s":[["TAGLINE","PHP testing framework"],["TLDR","**Run all tests**\n\n```phpunit```\n\n**Run specific test file**\n\n```phpunit [tests/ExampleTest.php]```\n\n**Run tests in directory**\n\n```phpunit [tests/]```\n\n**Run specific test**\n\n```phpunit --filter [testMethodName]```\n\n**Generate coverage report**\n\n```phpunit --coverage-html [coverage/]```\n\n**Use configuration**\n\n```phpunit -c [phpunit.xml]```"],["SYNOPSIS","**phpunit** [_options_] [_files_...]"],["PARAMETERS","_FILES_\n> Test files or directories.\n\n**--filter** _PATTERN_\n> Filter tests.\n\n**--coverage-html** _DIR_\n> HTML coverage report.\n\n**-c** _FILE_\n> Configuration file.\n\n**--testdox**\n> TestDox output.\n\n**--help**\n> Display help."],["DESCRIPTION","**phpunit** is the de facto unit and integration testing framework for PHP, based on the xUnit family. It discovers and runs test classes that extend `PHPUnit\\Framework\\TestCase`, providing rich assertions, data providers, test doubles, and code coverage reporting.\n\nTests are typically organized under a `tests/` directory and configured via a `phpunit.xml` file at the project root, which defines test suites, bootstrap files, and coverage filters. Results can be output in TestDox, JUnit XML, and other formats for CI integration."],["CONFIGURATION","**phpunit.xml**, **phpunit.xml.dist**\n> Project configuration: test suites, bootstrap, coverage filters, and PHP settings."],["CAVEATS","Configuration via phpunit.xml. Code coverage requires Xdebug or PCOV to be installed. Major versions (9, 10, 11) have removed deprecated APIs, so test code may need updates when upgrading."],["HISTORY","PHPUnit was created by **Sebastian Bergmann** as the standard **PHP testing framework**."],["INSTALL","```apk: sudo apk add phpunit```\n\n```brew: brew install phpunit```\n\n```nix: nix profile install nixpkgs#phpunit```"],["SEE ALSO","[phpspec](/man/phpspec)(1), [pest](/man/pest)(1), [php](/man/php)(1)"]],"e":["phpspec","pest","php"],"x":"phpunit"},"pip-inspect":{"n":"pip-inspect","t":"Output installed package metadata as JSON","s":[["TAGLINE","Output installed package metadata as JSON"],["TLDR","**Inspect installed packages**\n\n```pip inspect```\n\n**Output as JSON**\n\n```pip inspect --local```"],["SYNOPSIS","**pip** **inspect** [_options_]"],["PARAMETERS","**--local**\n> Only inspect virtualenv packages.\n\n**--user**\n> Only inspect user packages.\n\n**--path** _path_\n> Inspect packages in path."],["DESCRIPTION","**pip inspect** outputs JSON describing installed packages and their metadata. Provides detailed information about installed distributions including versions, requires, and provides metadata. Useful for programmatic environment analysis."],["INSTALL","```apk: sudo apk add py3-pip```"],["SEE ALSO","[pip](/man/pip)(1), [pip-list](/man/pip-list)(1), [pip-show](/man/pip-show)(1)"]],"e":["pip","pip-list","pip-show"],"x":"pip inspect"},"pkgctl-repo":{"n":"pkgctl-repo","t":"Manage Arch Linux Git packaging repositories","s":[["TAGLINE","Manage Arch Linux Git packaging repositories"],["TLDR","**Clone** a package repository (requires SSH key in Arch Linux GitLab)\n\n```pkgctl repo clone [pkgname]```\n\nClone a package repository over **HTTPS**\n\n```pkgctl repo clone --protocol https [pkgname]```\n\n**Create** a new GitLab package repository and clone it\n\n```pkgctl repo create [pkgbase]```\n\n**Switch** a package repository to a specified version\n\n```pkgctl repo switch [version] [pkgbase]```\n\n**Open** a package repository's website\n\n```pkgctl repo web [pkgbase]```"],["SYNOPSIS","**pkgctl repo** _command_ [_options_] [_package_]"],["PARAMETERS","**clone**\n> Clone a package repository\n\n**create**\n> Create a new GitLab repository\n\n**switch**\n> Switch to a specific version/branch\n\n**web**\n> Open repository in web browser\n\n**--protocol _proto_**\n> Clone protocol: ssh (default) or https\n\n**--maintainer _name_**\n> Filter by maintainer\n\n**--universe**\n> Clone from universe (community) rather than packages"],["DESCRIPTION","**pkgctl repo** manages Git packaging repositories for Arch Linux packages hosted on GitLab. It provides commands to clone, create, and manage package source repositories.\n\nThe tool integrates with Arch Linux's GitLab infrastructure at gitlab.archlinux.org, where official package sources are maintained. SSH access requires configuring keys in your GitLab account.\n\nPackage maintainers use this to obtain sources for updating packages, while read-only HTTPS access allows anyone to view package build files."],["CAVEATS","SSH clone requires GitLab account with SSH key configured. Repository creation requires valid GitLab API authentication and appropriate permissions. Part of the pkgctl toolset for Arch Linux development."],["SEE ALSO","[pkgctl](/man/pkgctl)(1), [pkgctl-build](/man/pkgctl-build)(1), [git](/man/git)(1), [asp](/man/asp)(1)"]],"e":["pkgctl","pkgctl-build","git","asp"],"x":"pkgctl repo clone [pkgname]"},"ppmtopjxl":{"n":"ppmtopjxl","t":"Convert PPM to HP PaintJet XL format","s":[["TAGLINE","Convert PPM to HP PaintJet XL format"],["TLDR","**Convert PPM to HP PaintJet XL format**\n\n```ppmtopjxl [input.ppm] > [output.pjxl]```\n\n**Specify render mode**\n\n```ppmtopjxl -render [best] [input.ppm] > [output.pjxl]```"],["SYNOPSIS","**ppmtopjxl** [_options_] [_file_]"],["PARAMETERS","**-render** _mode_\n> Render mode (draft, normal, best).\n\n**-dpi** _n_\n> Print resolution."],["DESCRIPTION","**ppmtopjxl** converts PPM images to HP PaintJet XL/XL300 PCL format for direct printing. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[ppmtopj](/man/ppmtopj)(1), [ppmtolj](/man/ppmtolj)(1)"]],"e":["ppmtopj","ppmtolj"],"x":"ppmtopjxl [input.ppm] > [output.pjxl]"},"prlimit":{"n":"prlimit","t":"Get or set process resource limits","s":[["TAGLINE","Get or set process resource limits"],["TLDR","Display **all limits** for current process\n\n```prlimit```\n\nDisplay limits for **specific process**\n\n```prlimit -p [pid]```\n\nRun command with **custom file limit**\n\n```prlimit -n [1024] [command]```"],["SYNOPSIS","**prlimit** [**-p** _pid_] [_limit_=_value_] [_command_]"],["PARAMETERS","**-p, --pid _pid_**\n> Process ID to query or modify\n\n**-n, --nofile _limit_**\n> Maximum number of open files\n\n**-u, --nproc _limit_**\n> Maximum number of processes\n\n**-s, --stack _limit_**\n> Maximum stack size\n\n**-v, --as _limit_**\n> Maximum virtual memory size\n\n**-m, --rss _limit_**\n> Maximum resident set size"],["DESCRIPTION","**prlimit** gets or sets process resource limits. It can query limits for a running process or run a new command with specified limits. Limits can be set as soft:hard or just a single value for both.\n\nThe tool provides finer control than ulimit, allowing modification of another process's limits and setting both soft and hard limits simultaneously."],["CAVEATS","Modifying another process requires appropriate privileges. Hard limits can only be raised by root. Some limits may not be adjustable on all systems."],["HISTORY","**prlimit** is part of **util-linux**, providing a modern interface to the prlimit system call. It offers more flexibility than the shell built-in ulimit command."],["INSTALL","```apt: sudo apt install util-linux```\n\n```dnf: sudo dnf install util-linux```\n\n```pacman: sudo pacman -S util-linux```\n\n```apk: sudo apk add util-linux-misc```\n\n```zypper: sudo zypper install util-linux```\n\n```brew: brew install util-linux```\n\n```nix: nix profile install nixpkgs#util-linux```"],["SEE ALSO","[ulimit](/man/ulimit)(1), [getrlimit](/man/getrlimit)(2)"]],"e":["ulimit","getrlimit"],"x":"prlimit"},"qm-mtunnel":{"n":"qm-mtunnel","t":"Internal migration tunnel for Proxmox VMs","s":[["TAGLINE","Internal migration tunnel for Proxmox VMs"],["TLDR","Command used by **qmigrate** during data migration\n\n```qm mtunnel```"],["SYNOPSIS","**qm mtunnel**"],["DESCRIPTION","**qm mtunnel** is used internally by qmigrate for data migration between hosts. It creates a tunnel for transferring VM data during live migration.\n\nThis command should not be invoked manually; it is called automatically by the migration process."],["CAVEATS","Internal use only. Manual invocation may interfere with ongoing migrations or cause unexpected behavior."],["HISTORY","Part of **Proxmox VE** QEMU/KVM management tools. Provides the data transfer mechanism for VM migration."],["SEE ALSO","[qm](/man/qm)(1), [qm-migrate](/man/qm-migrate)(1)"]],"e":["qm","qm-migrate"],"x":"qm mtunnel"},"qq":{"n":"qq","t":"Multi-format transcoder and query tool powered by jq syntax","s":[["TAGLINE","Multi-format transcoder and query tool powered by jq syntax"],["TLDR","**Convert YAML to JSON**\n\n```qq '.' [config.yaml] -o json```\n\n**Query a TOML file**\n\n```qq '.database.host' [config.toml]```\n\n**Convert CSV to Parquet**\n\n```qq '.' [data.csv] -o parquet```\n\n**Interactive query mode**\n\n```qq . [file.json] --interactive```"],["SYNOPSIS","**qq** [_filter_] [_file_] [_options_]"],["PARAMETERS","**-i**, **--input** _FORMAT_\n> Specify input format explicitly (e.g., json, yaml, toml, csv, xml).\n\n**-o**, **--output** _FORMAT_\n> Specify output format.\n\n**--interactive**\n> Interactive query builder with autocomplete and real-time preview.\n\n**--stream**\n> Streaming mode for memory-efficient processing of large files (identical to jq).\n\n**-s**, **--slurp**\n> Slurp mode: read multiple inputs into an array.\n\n**-e**\n> Exit-status mode: use the last value for the exit code.\n\n**--monochrome-output**\n> Disable colored output.\n\n**--help**\n> Display help information."],["DESCRIPTION","**qq** is a multi-format transcoder and query tool powered by jq syntax via the gojq engine. It supports reading and writing JSON, YAML, TOML, XML, HCL/Terraform, CSV, TSV, INI, Parquet, MessagePack, CBOR, Avro, and more. Format is auto-detected from file extensions."],["HISTORY","**qq** was created by **JFryy** and is written in **Go**."],["INSTALL","```nix: nix profile install nixpkgs#qq```"],["SEE ALSO","[jq](/man/jq)(1), [yq](/man/yq)(1), [oq](/man/oq)(1), [gron](/man/gron)(1)"]],"e":["jq","yq","oq","gron"],"x":"qq '.' [config.yaml] -o json"},"rustup-default":{"n":"rustup-default","t":"Set the default Rust toolchain","s":[["TAGLINE","Set the default Rust toolchain"],["TLDR","**Set default toolchain**\n\n```rustup default stable```\n\n**Set nightly as default**\n\n```rustup default nightly```\n\n**Set specific version**\n\n```rustup default [1.70.0]```\n\n**Show current default**\n\n```rustup default```"],["SYNOPSIS","**rustup** **default** [_toolchain_]"],["PARAMETERS","_toolchain_\n> Toolchain name: **stable**, **beta**, **nightly**, or a specific version like **1.70.0**. Can include a target triple (e.g., `nightly-x86_64-unknown-linux-gnu`)."],["DESCRIPTION","**rustup default** sets or shows the default Rust toolchain. The default toolchain is used when no override is active (via `rustup override` or `rust-toolchain.toml`). Common choices are stable, beta, or nightly. If the specified toolchain is not installed, rustup will download and install it automatically."],["CAVEATS","Project-level overrides (from `rust-toolchain.toml` or `rustup override`) take precedence over the default. Changing the default does not affect projects that have overrides set."],["INSTALL","```apt: sudo apt install rustup```\n\n```dnf: sudo dnf install rustup```\n\n```pacman: sudo pacman -S rustup```\n\n```zypper: sudo zypper install rustup```\n\n```brew: brew install rustup```\n\n```nix: nix profile install nixpkgs#rustup```"],["SEE ALSO","[rustup](/man/rustup)(1), [rustup-toolchain](/man/rustup-toolchain)(1), [rustup-override](/man/rustup-override)(1)"]],"e":["rustup","rustup-toolchain","rustup-override"],"x":"rustup default stable"},"rustup-override":{"n":"rustup-override","t":"Set directory-specific Rust toolchain overrides","s":[["TAGLINE","Set directory-specific Rust toolchain overrides"],["TLDR","**Set directory override**\n\n```rustup override set nightly```\n\n**Remove directory override**\n\n```rustup override unset```\n\n**List overrides**\n\n```rustup override list```"],["SYNOPSIS","**rustup** **override** _command_ [_options_]"],["PARAMETERS","**set** _toolchain_\n> Set override for current directory.\n\n**unset**\n> Remove current directory override.\n\n**list**\n> Show all overrides.\n\n**--path** _path_\n> Target specific directory."],["DESCRIPTION","**rustup override** manages directory-specific toolchain overrides. Overrides take precedence over default toolchain. Useful for projects requiring specific Rust versions."],["INSTALL","```apt: sudo apt install rustup```\n\n```dnf: sudo dnf install rustup```\n\n```pacman: sudo pacman -S rustup```\n\n```zypper: sudo zypper install rustup```\n\n```brew: brew install rustup```\n\n```nix: nix profile install nixpkgs#rustup```"],["SEE ALSO","[rustup](/man/rustup)(1), [rustup-default](/man/rustup-default)(1)"]],"e":["rustup","rustup-default"],"x":"rustup override set nightly"},"semanage-boolean":{"n":"semanage-boolean","t":"Manage persistent SELinux boolean settings","s":[["TAGLINE","Manage persistent SELinux boolean settings"],["TLDR","**List** all boolean settings\n\n```sudo semanage boolean -l```\n\n**List** all user-defined boolean settings without headings\n\n```sudo semanage boolean -l -C -n```\n\n**Set** or unset a boolean persistently\n\n```sudo semanage boolean -m --on|--off [haproxy_connect_any]```"],["SYNOPSIS","**semanage boolean** [**-l**|**-m**] [_options_] [_boolean_]"],["PARAMETERS","**-l, --list**\n> List all booleans and their current values\n\n**-m, --modify**\n> Modify a boolean's persistent value\n\n**-C, --locallist**\n> Show only locally modified booleans\n\n**-n, --noheading**\n> Omit column headings from output\n\n**-1, --on**\n> Set boolean to on/true\n\n**-0, --off**\n> Set boolean to off/false"],["DESCRIPTION","**semanage boolean** manages persistent SELinux boolean settings. Booleans allow administrators to customize how SELinux policy rules affect confined processes without modifying the policy itself.\n\nUnlike **setsebool**, which can make temporary changes, semanage boolean changes persist across system reboots. The **-C** flag shows which booleans have been customized from their default values."],["CAVEATS","Requires root privileges. Boolean changes take effect immediately but are also saved persistently. Use **getsebool -a** for a quick non-persistent view of all booleans."],["SEE ALSO","[semanage](/man/semanage)(8), [getsebool](/man/getsebool)(8), [setsebool](/man/setsebool)(8), [sestatus](/man/sestatus)(8), [selinuxenabled](/man/selinuxenabled)(8)"]],"e":["semanage","getsebool","setsebool","sestatus","selinuxenabled"],"x":"sudo semanage boolean -l"},"showconsolefont":{"n":"showconsolefont","t":"Display Linux console font glyphs","s":[["TAGLINE","Display Linux console font glyphs"],["TLDR","**Display the current console font** in a table\n\n```showconsolefont```\n\n**Show only font information** without the glyph table\n\n```showconsolefont --info```\n\n**Display font from a specific console device**\n\n```showconsolefont --console=[/dev/tty1]```\n\n**Display with verbose output**\n\n```showconsolefont --verbose```"],["SYNOPSIS","**showconsolefont** [**-C** _console_] [**-v**] [**-V**] [**-h**] [**-i**]"],["PARAMETERS","**-i**, **--info**\n> Don't print the font table, just show ROWSxCOLSxCOUNT and exit.\n\n**-C**, **--console=**_DEV_\n> Specify the console device to be used. The argument is a pathname (e.g., /dev/tty1).\n\n**-v**, **--verbose**\n> Be more verbose. Provides additional information about the font, such as whether it is a Unicode font.\n\n**-V**, **--version**\n> Print version number.\n\n**-h**, **--help**\n> Print usage message."],["DESCRIPTION","**showconsolefont** outputs the current EGA/VGA console screen font to standard output. It displays a table of glyphs (characters) in the currently loaded font, allowing you to see all available characters.\n\nThe command is useful for troubleshooting font rendering issues on the Linux console, verifying that a specific font is loaded after using **setfont**, or determining font characteristics for scripting purposes. With the **--info** option, it outputs just the font dimensions and character count without displaying the full glyph table."],["CAVEATS","This command only works on the Linux console (virtual terminals), not in terminal emulators running under X11 or Wayland. Requires access to the console device, which may need appropriate permissions. On systems using framebuffer consoles, the output represents the currently loaded PSF (PC Screen Font) file."],["HISTORY","**showconsolefont** is part of the **kbd** project, a collection of tools for managing Linux keyboard and console fonts. The kbd project provides utilities that have been essential for Linux console configuration since the early days of Linux. These tools evolved from earlier console utilities and continue to be maintained for use on virtual terminals."],["INSTALL","```apt: sudo apt install kbd```\n\n```dnf: sudo dnf install kbd```\n\n```pacman: sudo pacman -S kbd```\n\n```apk: sudo apk add kbd```\n\n```zypper: sudo zypper install kbd```\n\n```nix: nix profile install nixpkgs#kbd```"],["SEE ALSO","[setfont](/man/setfont)(8), [consolechars](/man/consolechars)(8), [loadkeys](/man/loadkeys)(1), [dumpkeys](/man/dumpkeys)(1)"]],"e":["setfont","consolechars","loadkeys","dumpkeys"],"x":"showconsolefont"},"spotify_player":{"n":"spotify_player","t":"Feature-rich Spotify client for the terminal","s":[["TAGLINE","Feature-rich Spotify client for the terminal"],["TLDR","**Start** the player TUI\n\n```spotify_player```\n\n**CLI** playback helpers (see help)\n\n```spotify_player [command] --help```"],["SYNOPSIS","**spotify_player** [*command*] [*options*]"],["DESCRIPTION","**spotify_player** is a Rust Spotify TUI with streaming, Spotify Connect control, lyrics, audio visualization, and media keys. The binary name uses an underscore. Install via **cargo install spotify_player** or distribution packages.\n\nRequires Spotify authentication (OAuth / session). Many features need Spotify Premium for playback."],["PARAMETERS","Default invocation opens the TUI. Subcommands exist for non-interactive control—run **spotify_player --help**. Config lives under the XDG config directory (see upstream docs for **app.toml** / theme files)."],["CAVEATS","Spotify API and ToS constraints apply; features break when Spotify changes APIs. Streaming quality and device limits depend on account type."],["INSTALL","```brew: brew install spotify_player```"],["SEE ALSO","[spt](/man/spt)(1), [ncspot](/man/ncspot)(1), [playerctl](/man/playerctl)(1)"],["RESOURCES","```[Source code](https://github.com/aome510/spotify-player)```"]],"e":["spt","ncspot","playerctl"],"x":"spotify_player"},"swaybar":{"n":"swaybar","t":"Default status bar for Sway compositor","s":[["TAGLINE","Default status bar for Sway compositor"],["TLDR","**Start swaybar** (usually started by sway)\n\n```swaybar```\n\n**Start with specific bar ID**\n\n```swaybar -b [bar-0]```\n\n**Start with custom config**\n\n```swaybar -c [~/.config/sway/config]```\n\n**Enable debug logging**\n\n```swaybar -d```"],["SYNOPSIS","**swaybar** [_options_]"],["PARAMETERS","**-b**, **--bar_id** _id_\n> Bar ID for which to get the configuration.\n\n**-c**, **--config** _file_\n> Path to sway configuration file.\n\n**-d**, **--debug**\n> Enable debug logging.\n\n**-h**, **--help**\n> Display help message.\n\n**-v**, **--version**\n> Display version information."],["DESCRIPTION","**swaybar** is the default status bar for the Sway Wayland compositor. It displays workspace buttons, window titles, and a customizable status area.\n\nThe status bar is typically started automatically by Sway based on the bar configuration in the sway config file. The status area content is generated by executing a status_command, which outputs text or JSON to stdout.\n\nSwaybar supports the i3bar JSON protocol for structured status output with colors and click events. It also provides a system tray for applications using the StatusNotifierItem protocol.\n\nConfiguration including colors, position, fonts, and behavior is done through the sway configuration file within a bar block."],["CONFIGURATION","Configure in sway config file:\n\n```\nbar {\n    status_command i3status\n    position top\n    colors {\n        background #323232\n        statusline #ffffff\n    }\n}\n```"],["CAVEATS","Must be run under Sway compositor. Limited system tray support compared to standalone bars. For advanced customization, consider Waybar as an alternative. Status commands must output data compatible with swaybar or i3bar protocol."],["HISTORY","**swaybar** was developed as part of the Sway project, providing a native Wayland status bar. Sway, created by Drew DeVault, aims for i3 compatibility on Wayland. The bar follows i3bar conventions while operating in a pure Wayland environment without X11 dependencies."],["INSTALL","```apt: sudo apt install sway```\n\n```dnf: sudo dnf install sway```\n\n```pacman: sudo pacman -S sway```\n\n```apk: sudo apk add swaybar```\n\n```zypper: sudo zypper install sway```\n\n```nix: nix profile install nixpkgs#sway```"],["SEE ALSO","[sway](/man/sway)(1), [i3status](/man/i3status)(1), [waybar](/man/waybar)(1)"]],"e":["sway","i3status","waybar"],"x":"swaybar"},"systemd-tmpfiles":{"n":"systemd-tmpfiles","t":"Manage temporary files and directories","s":[["TAGLINE","Manage temporary files and directories"],["TLDR","**Create** files and directories as specified in configuration\n\n```systemd-tmpfiles --create```\n\n**Clean up** files and directories with age parameters\n\n```systemd-tmpfiles --clean```\n\n**Remove** files and directories as specified in configuration\n\n```systemd-tmpfiles --remove```\n\n**Apply** operations for user-specific configurations\n\n```systemd-tmpfiles --create --user```\n\n**Execute** lines marked for early boot\n\n```systemd-tmpfiles --create --boot```\n\n**Apply** only to paths under a specific prefix\n\n```systemd-tmpfiles --create --prefix=[/tmp]```\n\n**Create, clean, and remove** in a single invocation\n\n```systemd-tmpfiles --create --clean --remove```"],["SYNOPSIS","**systemd-tmpfiles** [_options_] [_configfile_...]"],["PARAMETERS","**--create**\n> Create files and directories as configured\n\n**--clean**\n> Clean up files older than configured age\n\n**--remove**\n> Remove files and directories\n\n**--user**\n> Apply user configuration\n\n**--boot**\n> Execute boot-time entries\n\n**--purge**\n> Delete all files and directories created by the specified configuration files\n\n**--prefix** _path_\n> Only apply rules with paths starting with specified prefix (repeatable)\n\n**--exclude-prefix** _path_\n> Ignore rules with paths starting with specified prefix (repeatable)\n\n**-E**\n> Shortcut for excluding /dev, /proc, /run, and /sys hierarchies\n\n**--root** _path_\n> Prefix all paths with the given alternate root directory\n\n**--replace** _path_\n> Use command-line arguments instead of the specified configuration file\n\n**--cat-config**\n> Print the contents of configuration files to stdout\n\n**--no-pager**\n> Do not pipe output into a pager\n\n**-h**, **--help**\n> Print help text and exit"],["DESCRIPTION","**systemd-tmpfiles** creates, deletes, and cleans up volatile and temporary files and directories. Configuration files in **/etc/tmpfiles.d/**, **/usr/lib/tmpfiles.d/**, and related directories define what actions to take.\n\nThe tool is automatically invoked during system boot by systemd services. Manual execution is typically only needed for testing configurations or one-time cleanup operations."],["CAVEATS","Running manually is usually not needed as systemd handles this automatically. It is possible to combine --create, --clean, and --remove in one invocation. Configuration syntax errors may prevent proper cleanup. Age-based cleanup requires properly configured time values. Part of the systemd suite."],["INSTALL","```apt: sudo apt install systemd```\n\n```dnf: sudo dnf install systemd```\n\n```pacman: sudo pacman -S systemd```\n\n```zypper: sudo zypper install systemd```\n\n```brew: brew install systemd```\n\n```nix: nix profile install nixpkgs#systemd```"],["SEE ALSO","[systemctl](/man/systemctl)(1)"]],"e":["systemctl"],"x":"systemd-tmpfiles --create"},"tginfo":{"n":"tginfo","t":"Fetch info about a Telegram username or URL","s":[["TAGLINE","Fetch info about a Telegram username or URL"],["TLDR","**Get info about a Telegram username**\n\n```tginfo [username]```\n\n**Get info from a Telegram URL**\n\n```tginfo https://t.me/[username]```\n\n**Display only specific attributes**\n\n```tginfo [username] --attrs=title,type,subscribers```\n\n**Output as JSON**\n\n```tginfo [username] --json```"],["SYNOPSIS","**tginfo** _slug/url_ [_options_]"],["PARAMETERS","**--attrs**=_ATTR1,ATTR2_\n> Display only specific attributes.\n\n**--json**\n> Display JSON instead of a human readable view.\n\n**--help**\n> Print help message.\n\n**--version**\n> Display version."],["DESCRIPTION","**tginfo** is a zero-dependency Node.js module that fetches information about a Telegram username or URL. It accepts web URLs (https://t.me/username), deep links (tg://resolve?domain=username), plain usernames, or invite codes.\n\nThe output includes attributes such as type, title, webUrl, tgUrl, and optional fields like username, info, verified, image, subscribers, members, and online count depending on the entity type."],["CAVEATS","Requires Node.js. Install globally via `npm install -g tginfo` or run with `npx tginfo`."],["SEE ALSO","[npx](/man/npx)(1), [npm](/man/npm)(1)"]],"e":["npx","npm"],"x":"tginfo [username]"},"tlmgr-generate":{"n":"tlmgr-generate","t":"Regenerate TeX Live configuration files","s":[["TAGLINE","Regenerate TeX Live configuration files"],["TLDR","**Regenerate configuration files**\n\n```tlmgr generate updmap```\n\n**Regenerate language files**\n\n```tlmgr generate language```\n\n**Regenerate font maps**\n\n```tlmgr generate fmtutil```"],["SYNOPSIS","**tlmgr generate** _config_"],["PARAMETERS","**updmap**\n> Regenerate font map configuration.\n\n**language**\n> Regenerate language.dat files.\n\n**fmtutil**\n> Regenerate format configuration."],["DESCRIPTION","**tlmgr generate** regenerates TeX Live configuration files. Rebuilds font maps, format files, and language hyphenation patterns. Part of TeX Live Manager maintenance."],["SEE ALSO","[tlmgr](/man/tlmgr)(1)"]],"e":["tlmgr"],"x":"tlmgr generate updmap"},"tlmgr-gui":{"n":"tlmgr-gui","t":"Graphical TeX Live Manager interface","s":[["TAGLINE","Graphical TeX Live Manager interface"],["TLDR","**Start graphical interface**\n\n```tlmgr gui```\n\n**Start with specific repository**\n\n```tlmgr --repository [url] gui```"],["SYNOPSIS","**tlmgr gui** [_options_]"],["PARAMETERS","**--load** _profile_\n> Load GUI profile."],["DESCRIPTION","**tlmgr gui** launches the graphical TeX Live Manager interface. Provides visual package browsing, installation, and configuration. Requires Perl/Tk."],["CAVEATS","Requires **perl-tk** package to be installed."],["SEE ALSO","[tlmgr](/man/tlmgr)(1), [tlmgr-shell](/man/tlmgr-shell)(1)"]],"e":["tlmgr","tlmgr-shell"],"x":"tlmgr gui"},"toilet":{"n":"toilet","t":"Display colorful ASCII art text banners","s":[["TAGLINE","Display colorful ASCII art text banners"],["TLDR","Generate **ASCII art** for text\n\n```toilet [input_text]```\n\nUse custom **font**\n\n```toilet [input_text] -f [font_filename]```\n\nApply **filter**\n\n```toilet [input_text] -F [filter_name]```\n\n**List** available filters\n\n```toilet -F list```"],["SYNOPSIS","**toilet** [_OPTIONS_] [_TEXT_...]"],["PARAMETERS","**-f, --font** _NAME_\n> Use specified font file\n\n**-F, --filter** _NAME_\n> Apply filter to output\n\n**-w, --width** _N_\n> Set output width\n\n**-t, --term**\n> Use terminal width\n\n**-d, --directory** _DIR_\n> Specify font directory"],["DESCRIPTION","**toilet** (The Other Implementation's letters) displays ASCII-art text banners using various fonts and filters. It is similar to FIGlet but with additional features like color filters and Unicode support.\n\nThe program reads text from command-line arguments or standard input and renders it using the specified font. Various filters can apply effects like color gradients, borders, and transformations."],["CAVEATS","Font availability depends on installation. Some filters require terminal color support. Wide output may wrap incorrectly on narrow terminals. Unicode fonts require UTF-8 terminal support."],["HISTORY","**toilet** was created by **Sam Hocevar** as part of the libcaca project, providing an alternative to FIGlet with additional features like color support and more rendering options."],["INSTALL","```apt: sudo apt install toilet```\n\n```dnf: sudo dnf install toilet```\n\n```pacman: sudo pacman -S toilet```\n\n```zypper: sudo zypper install toilet```\n\n```brew: brew install toilet```\n\n```nix: nix profile install nixpkgs#toilet```"],["SEE ALSO","[figlet](/man/figlet)(1), [banner](/man/banner)(1)"]],"e":["figlet","banner"],"x":"toilet [input_text]"},"urpmf":{"n":"urpmf","t":"Search Mageia package files and metadata","s":[["TAGLINE","Search Mageia package files and metadata"],["TLDR","Search packages containing **file**\n\n```urpmf [filename]```\n\nSearch **summary** with AND\n\n```urpmf --summary [keyword1] -a [keyword2]```\n\nSearch **description** with OR\n\n```urpmf --description [keyword1] -o [keyword2]```\n\nSearch with **exclusion**\n\n```urpmf --description ! [keyword] -F'|'```"],["SYNOPSIS","**urpmf** [_OPTIONS_] _PATTERN_"],["PARAMETERS","**--summary**\n> Search in package summaries\n\n**--description**\n> Search in package descriptions\n\n**-a**\n> AND operator between patterns\n\n**-o**\n> OR operator between patterns\n\n**!**\n> Negation (exclude matches)\n\n**-F** _SEP_\n> Field separator (default: colon)"],["DESCRIPTION","**urpmf** finds files in packages and queries information about them in Mageia Linux. It can search by filename, summary, or description using boolean operators.\n\nPart of the urpmi package management suite for Mageia."],["CAVEATS","Mageia specific. Searches through all configured media. Large queries may take time to process."],["SEE ALSO","[urpmi](/man/urpmi)(8), [urpmq](/man/urpmq)(8)"]],"e":["urpmi","urpmq"],"x":"urpmf [filename]"},"vagrant-reload":{"n":"vagrant-reload","t":"Restart Vagrant machine with updated config","s":[["TAGLINE","Restart Vagrant machine with updated config"],["TLDR","**Reload VM**\n\n```vagrant reload```\n\n**Reload with provisioning**\n\n```vagrant reload --provision```\n\n**Reload specific VM**\n\n```vagrant reload [name]```"],["SYNOPSIS","**vagrant** **reload** [_options_] [_name_]"],["PARAMETERS","**--provision**\n> Run provisioners.\n\n**--no-provision**\n> Skip provisioners.\n\n**-f**, **--force**\n> Force reload."],["DESCRIPTION","**vagrant reload** halts and restarts a Vagrant machine. Applies Vagrantfile changes that require a restart. Equivalent to vagrant halt followed by vagrant up."],["INSTALL","```apt: sudo apt install vagrant```\n\n```dnf: sudo dnf install vagrant```\n\n```nix: nix profile install nixpkgs#vagrant```"],["SEE ALSO","[vagrant](/man/vagrant)(1), [vagrant-halt](/man/vagrant-halt)(1)"]],"e":["vagrant","vagrant-halt"],"x":"vagrant reload"},"wafw00f":{"n":"wafw00f","t":"Web Application Firewall detection tool","s":[["TAGLINE","Web Application Firewall detection tool"],["TLDR","**Detect WAF** protecting a site\n\n```wafw00f [https://example.com]```\n\n**Scan multiple URLs** in one run\n\n```wafw00f [https://site1.com] [https://site2.com]```\n\n**Read targets** from an input file\n\n```wafw00f -i [urls.txt]```\n\n**List all known WAFs** that can be detected\n\n```wafw00f -l```\n\n**Find all** matching WAFs (don't stop at first match)\n\n```wafw00f -a [https://example.com]```\n\n**Verbose output** for debugging (repeatable)\n\n```wafw00f -vv [https://example.com]```\n\n**Route through a proxy** (e.g. Burp Suite)\n\n```wafw00f -p [http://127.0.0.1:8080] [https://example.com]```\n\n**Save output** as CSV, JSON, or text (format inferred from extension)\n\n```wafw00f -o [result.json] [https://example.com]```"],["SYNOPSIS","**wafw00f** [_options_] _url_ [_url_ ...]"],["PARAMETERS","**-h**, **--help**\n> Show help message and exit.\n\n**-V**, **--version**\n> Print the current version and exit.\n\n**-v**, **--verbose**\n> Enable verbosity. Can be repeated (e.g. **-vv**) for more detail.\n\n**-a**, **--findall**\n> Find all WAFs matching the signatures; do not stop after the first match.\n\n**-r**, **--noredirect**\n> Do not follow 3xx redirections.\n\n**-t** _TEST_, **--test**=_TEST_\n> Test for one specific WAF (name from **-l** output).\n\n**-l**, **--list**\n> List all WAFs that wafw00f is able to detect.\n\n**-p** _PROXY_, **--proxy**=_PROXY_\n> Use an HTTP/SOCKS5 proxy, e.g. _http://host:8080_, _socks5://host:1080_, _http://user:pass@host:8080_.\n\n**-H** _FILE_, **--headers**=_FILE_\n> Pass custom headers from a text file, overwriting the default header set.\n\n**-i** _FILE_, **--input-file**=_FILE_\n> Read targets from a text, CSV, or JSON file.\n\n**-o** _FILE_, **--output**=_FILE_\n> Write results to a file. Format is chosen from the extension (csv, json, text).\n\n**-f** _FORMAT_, **--format**=_FORMAT_\n> Force output format regardless of filename extension."],["DESCRIPTION","**wafw00f** is a security tool that identifies Web Application Firewalls (WAFs) protecting websites. It works by sending specially crafted HTTP requests and analyzing the responses for signatures and behaviors characteristic of specific WAF products.\n\nThe tool can detect a wide range of commercial and open-source WAFs including Cloudflare, AWS WAF, Akamai, F5 BIG-IP, ModSecurity, Imperva, Sucuri, and many others. In its default mode it stops after identifying the first WAF, while the all-detection mode tests against every known WAF fingerprint for thorough analysis.\n\nMultiple URLs can be tested in batch from an input file, and proxy support allows routing traffic through interception tools like Burp Suite. The tool is designed for authorized penetration testing and security assessments to help identify protection measures before deeper testing."],["DETECTED WAFS","Cloudflare, AWS WAF, Akamai, F5 BIG-IP, ModSecurity, Imperva, Sucuri, Fortinet, and many more."],["CAVEATS","For authorized testing only. Some WAFs may block detection. False positives possible."],["HISTORY","**wafw00f** was originally written by **Sandro Gauci** and released by **EnableSecurity** around **2009**. It has since become a standard recon tool, bundled with Kali Linux and actively maintained on GitHub. The name is a pun on \"WAF\" combined with a dog's bark."],["INSTALL","```dnf: sudo dnf install wafw00f```\n\n```nix: nix profile install nixpkgs#wafw00f```"],["SEE ALSO","[nmap](/man/nmap)(1), [nikto](/man/nikto)(1), [wfuzz](/man/wfuzz)(1), [burpsuite](/man/burpsuite)(1)"]],"e":["nmap","nikto","wfuzz","burpsuite"],"x":"wafw00f [https://example.com]"},"wat2wasm":{"n":"wat2wasm","t":"Assemble WebAssembly text to binary","s":[["TAGLINE","Assemble WebAssembly text to binary"],["TLDR","**Compile WAT to WASM**\n\n```wat2wasm [input.wat]```\n\n**Output to file**\n\n```wat2wasm [input.wat] -o [output.wasm]```\n\n**Validate only**\n\n```wat2wasm --validate [input.wat]```\n\n**Verbose output**\n\n```wat2wasm -v [input.wat]```\n\n**Debug names**\n\n```wat2wasm --debug-names [input.wat]```"],["SYNOPSIS","**wat2wasm** [_-o output_] [_-v_] [_options_] _input_"],["PARAMETERS","**-o** _FILE_\n> Output file.\n\n**-v**\n> Verbose.\n\n**--validate**\n> Validate only.\n\n**--debug-names**\n> Include debug names.\n\n**--help**\n> Show help."],["DESCRIPTION","**wat2wasm** is an assembler from the WABT (WebAssembly Binary Toolkit) that compiles WebAssembly Text Format (WAT) files into binary WebAssembly (.wasm) modules. It is the counterpart to wasm2wat, which performs the reverse conversion.\n\nThe tool validates the input WAT source against the WebAssembly specification during compilation, reporting syntax and type errors before producing output. A validate-only mode can check files without generating binary output, which is useful in build pipelines.\n\nThe --debug-names option embeds function and variable names from the WAT source into the binary as custom sections, making the output easier to debug in browsers and other tools that display named symbols."],["CAVEATS","WAT syntax required. Part of WABT. Strict validation."],["HISTORY","**wat2wasm** is part of **WABT** (WebAssembly Binary Toolkit), compiling WAT text format to binary WASM."],["INSTALL","```apt: sudo apt install wabt```\n\n```dnf: sudo dnf install wabt```\n\n```pacman: sudo pacman -S wabt```\n\n```brew: brew install wabt```\n\n```nix: nix profile install nixpkgs#wabt```"],["SEE ALSO","[wasm2wat](/man/wasm2wat)(1), [wasm-opt](/man/wasm-opt)(1), [wasm-validate](/man/wasm-validate)(1)"]],"e":["wasm2wat","wasm-opt","wasm-validate"],"x":"wat2wasm [input.wat]"},"winicontopam":{"n":"winicontopam","t":"Convert Windows icons to PAM format","s":[["TAGLINE","Convert Windows icons to PAM format"],["TLDR","**Convert Windows icon to PAM** (selects highest-quality image by default)\n\n```winicontopam [input.ico] > [output.pam]```\n\n**Extract a specific image index**\n\n```winicontopam -image=[0] [input.ico] > [output.pam]```\n\n**Extract all images as a multi-image PAM**\n\n```winicontopam -allimages [input.ico] > [output.pam]```\n\n**Include AND mask as a fifth channel**\n\n```winicontopam -andmasks [input.ico] > [output.pam]```"],["SYNOPSIS","**winicontopam** [_options_] [_file_]"],["PARAMETERS","**-image=**_N_\n> Extract image at index _N_ (zero-based). Cannot be combined with **-allimages**.\n\n**-allimages**\n> Extract every image in the ICO/CUR file as a multi-image PAM stream.\n\n**-andmasks**\n> Emit five-channel PAM with the AND mask as the fifth plane (in addition to RGBA).\n\n**-verbose**\n> Print informational messages about the input file.\n\n**-quiet**\n> Suppress informational output (standard libnetpbm option)."],["DESCRIPTION","**winicontopam** converts Microsoft Windows ICO or CUR files into Netpbm PAM images with an alpha channel. Windows icon files may contain several images at different resolutions and color depths; by default, winicontopam picks the highest-quality one (largest size, then most bits per pixel).\n\nPart of the **Netpbm** toolkit. Read from stdin when no file is given."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pamtowinicon](/man/pamtowinicon)(1), [winicontoppm](/man/winicontoppm)(1)"]],"e":["pamtowinicon","winicontoppm"],"x":"winicontopam [input.ico] > [output.pam]"},"xmllint":{"n":"xmllint","t":"Parse and validate XML documents","s":[["TAGLINE","Parse and validate XML documents"],["TLDR","**Check if an XML file is well-formed**\n\n```xmllint [file.xml]```\n\n**Format and pretty-print** an XML file\n\n```xmllint --format [file.xml]```\n\n**Validate against a DTD**\n\n```xmllint --valid [file.xml]```\n\n**Validate against an XSD schema**\n\n```xmllint --schema [schema.xsd] [file.xml]```\n\n**Extract data using XPath**\n\n```xmllint --xpath \"[//element/@attribute]\" [file.xml]```\n\n**Format in place** (output to same file)\n\n```xmllint --format [file.xml] -o [file.xml]```\n\n**Remove blank nodes** from output\n\n```xmllint --noblanks [file.xml]```"],["SYNOPSIS","**xmllint** [_options_] _xmlfile..._"],["PARAMETERS","**--format**\n> Reformat and reindent the output\n\n**--valid**\n> Validate against the DTD declared in the document\n\n**--schema** _file_\n> Validate against the specified XSD schema\n\n**--relaxng** _file_\n> Validate against a RelaxNG schema\n\n**--dtdvalid** _file_\n> Validate against the specified DTD\n\n**--xpath** _expression_\n> Evaluate XPath expression and print results\n\n**--output** _file_, **-o** _file_\n> Write output to specified file\n\n**--noout**\n> Suppress normal output (useful with validation)\n\n**--noblanks**\n> Remove ignorable blank nodes from output\n\n**--encode** _encoding_\n> Output in specified encoding\n\n**--html**\n> Parse input as HTML instead of XML\n\n**--recover**\n> Try to recover from parsing errors\n\n**--nonet**\n> Don't fetch external entities from network\n\n**--c14n**\n> Canonicalize output (for digital signatures)\n\n**--shell**\n> Enter interactive shell mode\n\n**--version**\n> Display version information"],["DESCRIPTION","**xmllint** is a command-line XML parser and processor from the **libxml2** library. It can parse XML files to check for well-formedness, validate documents against DTDs or schemas (XSD, RelaxNG), format XML for readability, and query documents using XPath expressions.\n\nThe tool is indispensable for working with XML data in shell scripts and automated workflows. It can detect syntax errors, validate against various schema types, extract specific data using XPath, and transform or clean up XML output.\n\nCommon uses include validating configuration files, extracting data from XML APIs, formatting machine-generated XML for human readability, and preprocessing XML before further transformation with XSLT."],["CAVEATS","XPath support is limited to XPath 1.0; for XPath 2.0+ use tools like **saxon**. Large files may consume significant memory as the entire document is loaded. External entity expansion is enabled by default and can pose security risks with untrusted input; use **--nonet** and **--noent** flags for untrusted data. Some HTML parsing may require the **--html --recover** combination for malformed documents."],["HISTORY","**xmllint** was developed as part of the **libxml2** project, created by **Daniel Veillard** starting in **1998**. The library and its tools became the standard XML processing toolkit on Unix-like systems, used by projects like GNOME, PHP, and countless others. It is installed by default on most Linux distributions and macOS. The tool has been continuously maintained and updated to support evolving XML standards."],["INSTALL","```apt: sudo apt install libxml2-utils```\n\n```apk: sudo apk add libxml2-utils```"],["SEE ALSO","[xsltproc](/man/xsltproc)(1), [xmlstarlet](/man/xmlstarlet)(1), [jq](/man/jq)(1), [yq](/man/yq)(1), [xq](/man/xq)(1)"]],"e":["xsltproc","xmlstarlet","jq","yq","xq"],"x":"xmllint [file.xml]"},"yadm-encrypt":{"n":"yadm-encrypt","t":"Encrypt sensitive dotfiles with yadm","s":[["TAGLINE","Encrypt sensitive dotfiles with yadm"],["TLDR","**Encrypt sensitive files**\n\n```yadm encrypt```"],["SYNOPSIS","**yadm** **encrypt** [_options_]"],["DESCRIPTION","**yadm encrypt** encrypts files listed in ~/.config/yadm/encrypt. Creates encrypted archive for sensitive configs (SSH keys, tokens). Uses GPG symmetric encryption. Safe to commit encrypted archive."],["SEE ALSO","[yadm](/man/yadm)(1), [yadm-decrypt](/man/yadm-decrypt)(1)"]],"e":["yadm","yadm-decrypt"],"x":"yadm encrypt"},"zrok":{"n":"zrok","t":"Secure tunneling and sharing platform","s":[["TAGLINE","Secure tunneling and sharing platform"],["TLDR","**Share local HTTP service publicly**\n\n```zrok share public localhost:[port]```\n\n**Share with reserved subdomain**\n\n```zrok reserve public localhost:[port] --unique-name [subdomain]```\n\n**Share privately (requires zrok access)**\n\n```zrok share private localhost:[port]```\n\n**Access private share**\n\n```zrok access private [share_token]```\n\n**Copy files to share**\n\n```zrok copy [file] [share_token]:[remote_path]```"],["SYNOPSIS","**zrok** _command_ [_options_]"],["SUBCOMMANDS","**enable**\n> Enable zrok environment\n\n**share** _mode_ _target_\n> Share local resource\n\n**reserve** _mode_ _target_\n> Create reserved share\n\n**release** _share_\n> Release reserved share\n\n**access** _mode_ _share_\n> Access shared resource\n\n**copy**\n> Copy files to/from shares\n\n**status**\n> Show environment status\n\n**disable**\n> Disable zrok environment"],["PARAMETERS (share)","**public**\n> Publicly accessible share\n\n**private**\n> Share accessible only with token\n\n**--backend-mode** _mode_\n> Backend type (proxy, web, files)\n\n**--unique-name** _name_\n> Reserve specific subdomain\n\n**--headless**\n> Run without terminal UI"],["DESCRIPTION","**zrok** is a next-generation sharing platform built on OpenZiti, providing secure tunneling for HTTP services, files, and arbitrary TCP/UDP traffic.\n\nPublic shares create internet-accessible URLs pointing to local services. Private shares require an access token and go through encrypted Ziti network overlay, providing zero-trust security.\n\nzrok can share:\n- HTTP/HTTPS services (web apps, APIs)\n- Static files (file server mode)\n- Raw TCP/UDP endpoints\n\nUnlike simple tunnels, zrok's private shares never expose traffic to the public internet - all traffic stays within the zero-trust overlay network."],["CAVEATS","Public shares expose your local service to the internet. Ensure appropriate authentication/security.\n\nFree tier has usage limits. Heavy usage may require a paid account or self-hosted infrastructure.\n\nReserved names persist but shares don't - you must re-run share commands after restart.\n\nzrok account required. Run **zrok invite** to get started."],["INSTALL","```brew: brew install zrok```\n\n```nix: nix profile install nixpkgs#zrok```"],["SEE ALSO","[ngrok](/man/ngrok)(1), [cloudflared](/man/cloudflared)(1), [tailscale](/man/tailscale)(8)"]],"e":["ngrok","cloudflared","tailscale"],"x":"zrok share public localhost:[port]"}});
