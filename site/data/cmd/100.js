window.LCL_BUCKET(100,{"NetworkManager.conf":{"n":"NetworkManager.conf","t":"main configuration file for NetworkManager","s":[["TAGLINE","main configuration file for NetworkManager"],["TLDR","**Basic configuration**\n\n```[main]\\nplugins=ifupdown,keyfile```\n\n**Set DNS mode**\n\n```[main]\\ndns=dnsmasq```\n\n**Disable MAC randomization**\n\n```[device]\\nwifi.scan-rand-mac-address=no```\n\n**Set hostname mode**\n\n```[main]\\nhostname-mode=dhcp```"],["SYNOPSIS","**/etc/NetworkManager/NetworkManager.conf**"],["PARAMETERS","**[main]**\n> Main configuration section.\n\n**plugins**\n> System settings plugin names (keyfile is always appended).\n\n**dns**\n> DNS processing mode (default, dnsmasq, systemd-resolved, none).\n\n**dhcp**\n> DHCP client to use (internal, dhcpcd).\n\n**hostname-mode**\n> How the transient hostname is managed (default, dhcp, none).\n\n**no-auto-default**\n> Devices for which no automatic default connection is created.\n\n**[keyfile]**\n> Keyfile plugin-specific settings.\n\n**[ifupdown]**\n> Settings for the ifupdown plugin (Debian/Ubuntu).\n\n**managed**\n> Whether devices listed in /etc/network/interfaces are managed by NetworkManager (true/false).\n\n**[connection]**\n> Default values for connection properties.\n\n**[device]**\n> Per-device persistent configuration.\n\n**[logging]**\n> Controls logging output.\n\n**level**\n> Log verbosity level (OFF, ERR, WARN, INFO, DEBUG, TRACE).\n\n**domains**\n> Filter log messages by topic (PLATFORM, WIFI, DHCP4, DNS, VPN, etc.).\n\n**[connectivity]**\n> Optional internet connectivity checking.\n\n**uri**\n> URL to check for connectivity.\n\n**interval**\n> Check frequency in seconds (default: 300).\n\n**[global-dns]**\n> System-wide DNS configuration that overrides per-connection settings."],["DESCRIPTION","**NetworkManager.conf** is the main configuration file for NetworkManager. It controls DNS handling, plugins, device management, and various global settings. The file uses an ini-style key file format with sections of key-value pairs.\n\nAdditional configuration can be placed in /etc/NetworkManager/conf.d/ as drop-in files."],["EXAMPLE CONFIG","```ini\n[main]\nplugins=ifupdown,keyfile\ndns=systemd-resolved\n\n[ifupdown]\nmanaged=false\n\n[device]\nwifi.scan-rand-mac-address=yes\n\n[connection]\nwifi.powersave=2\n\n[logging]\nlevel=WARN\n```"],["DNS MODES","```\ndefault          - Update resolv.conf directly\ndnsmasq          - Run local dnsmasq instance\nsystemd-resolved - Use systemd-resolved\nnone             - No DNS management\n```"],["CAVEATS","Syntax errors may prevent NetworkManager from starting. Use conf.d/ for drop-in files. Restart NetworkManager after changes with `systemctl restart NetworkManager`."],["SEE ALSO","[NetworkManager](/man/NetworkManager)(8), [nmcli](/man/nmcli)(1), [nmtui](/man/nmtui)(1), [nm-settings](/man/nm-settings)(5)"]],"e":["NetworkManager","nmcli","nmtui","nm-settings"],"x":"[main]\\nplugins=ifupdown,keyfile"},"age-inspect":{"n":"age-inspect","t":"inspect age-encrypted files without decrypting them","s":[["TAGLINE","inspect age-encrypted files without decrypting them"],["TLDR","**Show metadata** about an age file\n\n```age-inspect [secrets.age]```\n\n**Read from stdin**\n\n```cat [secrets.age] | age-inspect```\n\n**Read from stdin** with explicit dash\n\n```age-inspect - < [secrets.age]```\n\n**Emit machine-readable JSON** for scripting\n\n```age-inspect --json [secrets.age]```\n\n**Print the version** and exit\n\n```age-inspect --version```"],["SYNOPSIS","**age-inspect** [**--json**] [_INPUT_]"],["DESCRIPTION","**age-inspect** reports metadata about a file produced by **age**(1) without performing any decryption. It parses the age header to identify the file format version, list the recipient stanza types used to wrap the file key (for example `X25519`, `scrypt`, `ssh-ed25519`, or the post-quantum `mlkem768x25519`), and report whether ASCII armor was used.\n\nWhen the input is a regular file (not a pipe) the tool also prints a size breakdown: header, armor overhead, encryption overhead and payload, so the size of the protected data can be estimated without supplying an identity. Because no key material is required, **age-inspect** is safe to run on files received from untrusted parties to determine which recipients can decrypt them, or to audit a directory of archived secrets.\n\nIf _INPUT_ is omitted or set to `-`, the file is read from standard input."],["PARAMETERS","**--json**\n> Emit the inspection result as a JSON object suitable for programmatic processing (recipient types, post-quantum flag, armored flag, size fields).\n\n**--version**\n> Print the **age-inspect** version and exit."],["CAVEATS","**age-inspect** never decrypts the payload, so it cannot tell you whether a given identity will succeed in decryption, only which recipient stanza types are present. Size figures are exact for seekable files; when reading from a pipe, the payload size is reported as unknown. The tool was added to the official **age** distribution in version **1.3.0**; older releases ship only `age` and `age-keygen`."],["HISTORY","**age-inspect** ships with the reference **age** implementation by **Filippo Valsorda**. It was introduced in **age 1.3.0** in **2025**, alongside support for the post-quantum `mlkem768x25519` recipient type, to make it possible to reason about archived age files (recipient mix, armor, size) without unsealing them."],["INSTALL","```apt: sudo apt install age```\n\n```dnf: sudo dnf install age```\n\n```pacman: sudo pacman -S age```\n\n```apk: sudo apk add age```\n\n```zypper: sudo zypper install age```\n\n```brew: brew install age```\n\n```nix: nix profile install nixpkgs#age```"],["SEE ALSO","[age](/man/age)(1), [age-keygen](/man/age-keygen)(1), [gpg](/man/gpg)(1), [openssl](/man/openssl)(1)"]],"e":["age","age-keygen","gpg","openssl"],"x":"age-inspect [secrets.age]"},"aha":{"n":"aha","t":"Convert ANSI escape sequences to HTML","s":[["TAGLINE","Convert ANSI escape sequences to HTML"],["TLDR","**Convert colored terminal output to HTML**\n\n```ls --color=always | aha > ls.html```\n\n**Convert a man page** (preserving formatting) to HTML\n\n```MAN_KEEP_FORMATTING=1 man command | ul | aha > man.html```\n\n**Convert from a file** with black background and title\n\n```aha -f input.txt --black --title \"My Log\" > output.html```\n\n**Handle cursor-position sequences** (e.g. htop)\n\n```echo q | htop | aha --black --line-fix > htop.html```\n\n**Add custom CSS** and enable word wrap\n\n```command | aha --css style.css --word-wrap > output.html```\n\n**Use pink background**\n\n```command | aha --pink > output.html```"],["SYNOPSIS","**aha** [_options_] [_-f file_]"],["DESCRIPTION","**aha** (Ansi HTML Adapter) reads text containing ANSI SGR escape sequences (colors, bold, underline, etc.) from a file or stdin and writes a complete, standards-compliant HTML document to stdout.\n\nIt is commonly used to publish colored terminal sessions, diffs, logs, or interactive tool output (htop, git, ls --color, etc.) on the web while preserving the original appearance.\n\naha supports several output customizations such as forcing a black or pink background, setting the HTML document title, injecting an external CSS stylesheet, enabling word-wrap, and emitting stylesheet-based styles instead of inline styles."],["PARAMETERS","**-f**, **--file** _file_\n> Read input from _file_ instead of stdin.\n\n**-b**, **--black**\n> Use a black background with white text.\n\n**-p**, **--pink**\n> Use a pink background.\n\n**-t**, **--title** _title_\n> Set the HTML `<title>` (default: stdin or the input filename).\n\n**-c**, **--css** _file_\n> Link an external CSS stylesheet (`<link rel=\"stylesheet\" href=\"...\">`).\n\n**-s**, **--stylesheet**\n> Use a stylesheet instead of inline styles.\n\n**-w**, **--word-wrap**\n> Enable word wrapping to avoid horizontal scrollbars.\n\n**-l**, **--line-fix**\n> Fix inputs that use control sequences to change the cursor position (needed for tools like htop). Hot fix; may not work with every program.\n\n**-n**, **--no-header**\n> Do not wrap output in `<html>`, `<head>`, and `<body>` tags.\n\n**-r**, **--ignore-cr**\n> Ignore carriage returns (`\\r`), which may otherwise produce double newlines in HTML.\n\n**-i**, **--iso** _X_\n> Use ISO-8859-_X_ (1–16) instead of UTF-8 for the output.\n\n**-y**, **--style** _STYLE_\n> Append _STYLE_ to the `<body>` element style after other style parameters.\n\n**-L**, **--lang** _LANG_\n> Set the document language to ISO-639-1 code _LANG_.\n\n**-x**, **--no-xml**\n> Use HTML doctype instead of XML (may help older browsers).\n\n**-v**, **--version**\n> Print version number and exit.\n\n**-h**, **--help**\n> Show help."],["CAVEATS","For tools that change output based on whether stdout is a tty (e.g. `ls`, `grep`), you must force color with flags such as `--color=always`.\n\nSome programs (htop, man) emit additional control sequences; piping through `ul` or using `--line-fix` may be required for best results. Blinking text needs `--stylesheet` and a custom `.blink` CSS class on modern browsers."],["INSTALL","```apt: sudo apt install aha```\n\n```dnf: sudo dnf install aha```\n\n```pacman: sudo pacman -S aha```\n\n```apk: sudo apk add aha```\n\n```zypper: sudo zypper install aha```\n\n```brew: brew install aha```\n\n```nix: nix profile install nixpkgs#aha```"],["SEE ALSO","[ul](/man/ul)(1)"],["RESOURCES","```[Source code](https://github.com/theZiz/aha)```"]],"e":["ul"],"x":"ls --color=always | aha > ls.html"},"ansible-galaxy":{"n":"ansible-galaxy","t":"Manage Ansible roles and collections from Galaxy","s":[["TAGLINE","Manage Ansible roles and collections from Galaxy"],["TLDR","**Install** a role from Galaxy\n\n```ansible-galaxy install [username.role_name]```\n\nInstall **collection** from Galaxy\n\n```ansible-galaxy collection install [community.docker]```\n\nInstall from **requirements file**\n\n```ansible-galaxy install -r [requirements.yml]```\n\n**List** installed roles\n\n```ansible-galaxy list```\n\n**Initialize** a new role\n\n```ansible-galaxy init [role_name]```\n\n**Search** for roles\n\n```ansible-galaxy search [nginx]```"],["SYNOPSIS","**ansible-galaxy** [_role_|_collection_] _command_ [_options_]"],["DESCRIPTION","**ansible-galaxy** manages Ansible roles and collections from Ansible Galaxy and other sources. Galaxy is a public repository of community-contributed content, providing reusable automation components.\n\nThe tool can install, create, and manage roles (traditional content) and collections (bundled content including modules, plugins, and roles). Subcommands are grouped by content type as **ansible-galaxy role** _command_ and **ansible-galaxy collection** _command_; if the type is omitted, role is assumed (for example, \"ansible-galaxy install\" is equivalent to \"ansible-galaxy role install\")."],["PARAMETERS","**install** _name_\n> Install a role (or use \"collection install\" for a collection)\n\n**init** _name_\n> Create a new role or collection scaffold\n\n**list**\n> List installed roles or collections\n\n**search** _query_\n> Search Galaxy for roles\n\n**info** _name_\n> Show detailed information about a role or collection\n\n**remove** _name_\n> Remove an installed role or collection\n\n**build**\n> Build a collection artifact (collection only)\n\n**publish** _tarball_\n> Publish a collection to a Galaxy server (collection only)\n\n**-r** _file_, **--role-file** _file_, **--requirements-file** _file_\n> Install items listed in a YAML requirements file\n\n**-p** _path_, **--roles-path** _path_\n> Installation path for roles (--collections-path for collections)\n\n**--force**\n> Force overwrite of an existing role or collection\n\n**-s** _server_, **--server** _server_\n> Galaxy API server URL\n\n**--offline**\n> Work offline (init/install/verify, using installed content only)"],["CONFIGURATION","**/etc/ansible/ansible.cfg**\n> System-wide Ansible configuration, including Galaxy server URL and roles path.\n\n**~/.ansible.cfg**\n> Per-user Ansible configuration overriding system defaults.\n\n**ansible.cfg**\n> Project-level configuration in the current directory, highest priority."],["CAVEATS","Galaxy content is community-maintained; review before use in production. Collection names are namespaced (namespace.collection). Roles and collections have different installation paths."],["HISTORY","**ansible-galaxy** was introduced with Ansible Galaxy in **2013** to share and distribute roles. Collections support was added in Ansible 2.9 (**2019**) as a new content distribution format."],["INSTALL","```dnf: sudo dnf install ansible-core```\n\n```pacman: sudo pacman -S ansible-core```\n\n```apk: sudo apk add ansible-core```\n\n```zypper: sudo zypper install ansible-core```"],["SEE ALSO","[ansible](/man/ansible)(1), [ansible-playbook](/man/ansible-playbook)(1), [ansible-doc](/man/ansible-doc)(1)"],["RESOURCES","```[Source code](https://github.com/ansible/ansible)```\n\n```[Documentation](https://docs.ansible.com/ansible/latest/cli/ansible-galaxy.html)```"]],"e":["ansible","ansible-playbook","ansible-doc"],"x":"ansible-galaxy install [username.role_name]"},"apptainer-push":{"n":"apptainer-push","t":"Upload container images to remote registries.","s":[["TAGLINE","Upload container images to remote registries."],["TLDR","Push a container to the **Container Library**\n\n```apptainer push [path/to/image.sif] library://[user/collection/container]:[tag]```\n\nPush a container to an **OCI registry**\n\n```apptainer push [path/to/image.sif] oras://[registry/namespace/image]:[tag]```\n\nPush an **unsigned container** (skip the signature requirement)\n\n```apptainer push -U [path/to/image.sif] library://[user/collection/container]:[tag]```\n\nPush to an OCI registry using a **specific auth file**\n\n```apptainer push --authfile [path/to/auth.json] [path/to/image.sif] oras://[registry/namespace/image]:[tag]```\n\nPush a container with a **description** (library only)\n\n```apptainer push -D \"[description]\" [path/to/image.sif] library://[user/collection/container]:[tag]```"],["SYNOPSIS","**apptainer push** [_options_] _image_ _URI_"],["DESCRIPTION","**apptainer push** uploads container images to remote registries including the Sylabs Container Library and OCI-compatible registries. This enables sharing containers with collaborators and deploying containers to production environments.\n\nAuthentication is required for most registries. Use **apptainer remote login** to configure credentials before pushing."],["PARAMETERS","**-U, --allow-unsigned**\n> Allow pushing unsigned containers (by default a valid signature is required for library:// pushes).\n\n**-D, --description** _text_\n> Attach a description to the container (library:// only).\n\n**--library** _url_\n> Specify the Container Library URL to push to.\n\n**--authfile** _path_\n> Docker-style authentication file for OCI registry credentials (oras://).\n\n**--no-https**\n> Use HTTP instead of HTTPS for docker://, oras://, and library:// URIs."],["CAVEATS","Pushing to the Container Library requires a Sylabs account and API token. Large images may take considerable time to upload depending on network bandwidth. Unsigned containers may be rejected by some registries."],["INSTALL","```dnf: sudo dnf install apptainer```\n\n```pacman: sudo pacman -S apptainer```\n\n```apk: sudo apk add apptainer```\n\n```zypper: sudo zypper install apptainer```\n\n```brew: brew install apptainer```\n\n```nix: nix profile install nixpkgs#apptainer```"],["SEE ALSO","[apptainer](/man/apptainer)(1), [apptainer-pull](/man/apptainer-pull)(1), [apptainer-sign](/man/apptainer-sign)(1), [apptainer-delete](/man/apptainer-delete)(1)"],["RESOURCES","```[Source code](https://github.com/apptainer/apptainer)```\n\n```[Homepage](https://apptainer.org)```\n\n```[Documentation](https://apptainer.org/docs/)```"]],"e":["apptainer","apptainer-pull","apptainer-sign","apptainer-delete"],"x":"apptainer push [path/to/image.sif] library://[user/collection/container]:[tag]"},"apturl":{"n":"apturl","t":"Install packages from apt: protocol URLs","s":[["TAGLINE","Install packages from apt: protocol URLs"],["TLDR","**Install** package via apt: URL\n\n```apturl apt:firefox```\n\nInstall **multiple** packages\n\n```apturl \"apt:vim,git,curl\"```\n\nInstall from a specific **repository section**\n\n```apturl \"apt:[package]?section=universe\"```"],["SYNOPSIS","**apturl** [_options_] _apt:package_[,_package_...]"],["DESCRIPTION","**apturl** is a simple graphical application that takes an apt-protocol URL as a command line option and handles package installation. When a user clicks an apt: link in a web browser, apturl launches and prompts for confirmation before installing the specified packages.\n\nIt has been pre-installed on Ubuntu since version 7.10, providing a user-friendly way to install software without using the command line."],["PARAMETERS","**-p**, **--http-proxy**\n> Specify an HTTP proxy for package downloads\n\n**apt:_package_**\n> Package to install\n\n**apt:_package1_,_package2_**\n> Multiple packages to install\n\n**?channel=**\n> Repository or PPA source (e.g., ?channel=lucid-partner)\n\n**?section=**\n> Repository section to enable (e.g., ?section=universe)\n\n**?refresh=yes**\n> Force a repository refresh before looking for the package"],["CAVEATS","Requires a graphical environment (X11/Wayland). Should only be used with trusted links. Verification dialogs should be read carefully. Exit codes: 0 = success, 1 = cancelled, 2 = error, 3 = invalid arguments."],["HISTORY","**apturl** was developed for Ubuntu to simplify software installation, allowing websites to offer direct installation links for Linux packages."],["SEE ALSO","[apt](/man/apt)(8), [apt-get](/man/apt-get)(8), [dpkg](/man/dpkg)(1), [snap](/man/snap)(8)"]],"e":["apt","apt-get","dpkg","snap"],"x":"apturl apt:firefox"},"avahi-publish-address":{"n":"avahi-publish-address","t":"Register mDNS hostname-to-address mappings","s":[["TAGLINE","Register mDNS hostname-to-address mappings"],["TLDR","**Register** a hostname to IP address mapping\n\n```avahi-publish-address [hostname] [ip_address]```\n\n**Register** an IPv4 address with a hostname\n\n```avahi-publish-address myserver.local 192.168.1.100```\n\n**Register** an IPv6 address with a hostname\n\n```avahi-publish-address myserver.local fe80::1```\n\n**Register** in verbose mode\n\n```avahi-publish-address -v [hostname] [ip_address]```\n\n**Register** in a specific domain\n\n```avahi-publish-address --domain=[domain] [hostname] [ip_address]```\n\n**Register** without reverse DNS entry\n\n```avahi-publish-address --no-reverse [hostname] [ip_address]```"],["SYNOPSIS","**avahi-publish-address** [_options_] _hostname_ _address_"],["PARAMETERS","**-v, --verbose**\n> Enable verbose output with detailed information\n\n**-d, --domain=**_DOMAIN_\n> Specify mDNS domain name to register in (defaults to .local)\n\n**-R, --no-reverse**\n> Do not publish reverse DNS (PTR) entry for the address\n\n**-f, --no-fail**\n> Continue and reconnect if daemon becomes unavailable\n\n**-h, --help**\n> Display help information\n\n**-V, --version**\n> Show version information"],["DESCRIPTION","**avahi-publish-address** is a command-line utility for registering hostname-to-address mappings via the Avahi daemon using mDNS (Multicast DNS). This command is equivalent to **avahi-publish -a** and provides a convenient shortcut specifically for address registration.\n\nThe tool enables local network name resolution by advertising a hostname and its associated IPv4 or IPv6 address. The hostname typically resolves as **hostname.local** on the local network, allowing other devices to discover and connect to the host by name rather than by IP address.\n\nWhen invoked, **avahi-publish-address** connects to the Avahi daemon and maintains the hostname-to-address mapping until the process is terminated. The mapping is automatically withdrawn when the process exits.\n\nBy default, the tool publishes both forward (name-to-address) and reverse (address-to-name) DNS entries. The reverse entry can be suppressed using the **--no-reverse** option. The protocol (IPv4 or IPv6) is automatically determined from the address format.\n\nThe published hostname becomes discoverable by other mDNS-capable devices on the local network segment, including Linux systems with Avahi, macOS systems with Bonjour, and Windows systems with appropriate mDNS support."],["CAVEATS","Requires **avahi-daemon** to be running. The hostname registration persists only while the **avahi-publish-address** process is active; terminating the process removes the mapping. For persistent hostname registration, configure static mappings in **/etc/avahi/hosts**."],["INSTALL","```apt: sudo apt install avahi-utils```\n\n```dnf: sudo dnf install avahi-tools```\n\n```apk: sudo apk add avahi-tools```\n\n```zypper: sudo zypper install avahi-utils```"],["SEE ALSO","[avahi-publish](/man/avahi-publish)(1), [avahi-publish-service](/man/avahi-publish-service)(1), [avahi-resolve](/man/avahi-resolve)(1), [avahi-browse](/man/avahi-browse)(1), [avahi-daemon](/man/avahi-daemon)(8)"]],"e":["avahi-publish","avahi-publish-service","avahi-resolve","avahi-browse","avahi-daemon"],"x":"avahi-publish-address [hostname] [ip_address]"},"awk":{"n":"awk","t":"Pattern scanning and text processing language","s":[["TAGLINE","Pattern scanning and text processing language"],["TLDR","Print the **fifth column** (a.k.a. field) in a space-separated file\n\n```awk '{print $5}' [path/to/file] ```\n\nPrint the second column of the **lines containing \"foo\"** in a space-separated file\n\n```awk '/[foo]/ {print $2}' [path/to/file] ```\n\nPrint the **last column** of each line in a file, using a **comma** (instead of space) as a field separator\n\n```awk -F ',' '{print $NF}' [path/to/file]```\n\n**Sum the values** in the first column of a file and print the total\n\n```awk '{s+=$1} END {print s}' [path/to/file]```\n\nPrint different values **based on conditions**\n\n```awk '{if ($1 == \"foo\") print \"Exact match foo\"; else if ($1 ~ \"bar\") print \"Partial match bar\"; else print \"Baz\"}' [path/to/file]```\n\nPrint all the lines which the 10th column value is **between a min and a max**\n\n```awk '($10 >= [min_value] && $10 <= [max_value])'```"],["SYNOPSIS","**awk** [_-F fs_] [_-v var=val_] [_-f progfile_] [_--posix_] [_--_] '_program_' [_files_]"],["PARAMETERS","**-F  _fs_**  \n> Field separator (_fs_); default whitespace or TAB  \n  \n**-f  _file_**  \n> Read awk program from _file_ instead of command line  \n  \n**-v  _var_=_val_**  \n> Assign _val_ to _var_ before program runs (repeatable)  \n  \n**--**  \n> End options; treat following as filenames  \n  \n**-V**  \n> Print version and exit (gawk)  \n  \n**--help**  \n> Print help and exit (gawk)  \n  \n**--posix**  \n> Enforce POSIX compatibility (gawk)  \n  \n**-mf  _n_**  \n> Limit function args to _n_ (debugging; gawk)  \n  \n**-mr  _n_**  \n> Limit record size to _n_ bytes (debugging; gawk)  \n  \n**-W  _traditional_**  \n> Use original awk behavior (gawk)"],["DESCRIPTION","**awk** is a pattern-scanning and text-processing language designed for extracting and transforming structured data. It reads input line by line, splits each line into fields, and applies user-defined rules consisting of patterns and actions.\n\nAn awk program is a sequence of **pattern { action }** rules. For each input line, awk tests the patterns and executes the associated actions for any that match. If no pattern is given, the action applies to every line. If no action is given, matching lines are printed.\n\nFields are accessed as **$1**, **$2**, etc., with **$0** representing the entire line. The default field separator is whitespace, changeable with **-F**. Built-in variables include **NR** (current line number), **NF** (number of fields in current line), **FS** (field separator), and **OFS** (output field separator).\n\nSpecial patterns **BEGIN** and **END** execute actions before and after all input is processed, useful for initialization and summary output. Awk supports variables, arrays, arithmetic, string functions, printf formatting, and control flow statements, making it a complete programming language for text processing."],["FIELD ACCESS","**\\$0**: Full line\n**\\$1**: First field\n**NF**: Fields count\n**NR**: Record (line) number\n**FILENAME**: Current input file\n**FS** / **OFS**: Input / output field separator\n**RS** / **ORS**: Input / output record separator\nUse **\\$(n)** for nth field"],["CAVEATS","Different awk implementations (gawk, mawk, nawk) have varying feature sets. Regular expressions and string functions may behave differently across implementations. Associative arrays are unordered. Floating-point arithmetic may produce rounding errors. Very large files are processed efficiently but complex programs with many arrays can consume significant memory."],["HISTORY","**awk** was created by **Alfred Aho**, **Peter Weinberger**, and **Brian Kernighan** at Bell Labs in **1977**, with the name derived from their initials. It was redesigned and expanded in **1985** as \"new awk\" (nawk). **GNU awk** (gawk) added many extensions including networking and internationalization. Awk is a standard POSIX utility available on virtually all Unix-like systems."],["INSTALL","```dnf: sudo dnf install gawk```\n\n```pacman: sudo pacman -S gawk```\n\n```apk: sudo apk add gawk```\n\n```zypper: sudo zypper install gawk```\n\n```brew: brew install gawk```\n\n```nix: nix profile install nixpkgs#gawk```"],["SEE ALSO","[sed](/man/sed)(1), [grep](/man/grep)(1), [cut](/man/cut)(1), [perl](/man/perl)(1), [gawk](/man/gawk)(1)"],["RESOURCES","```[Source code](https://cgit.git.savannah.gnu.org/cgit/gawk.git)```\n\n```[Homepage](https://www.gnu.org/software/gawk/)```\n\n```[Documentation](https://www.gnu.org/software/gawk/manual/)```"]],"e":["sed","grep","cut","perl","gawk"],"x":"awk '{print $5}' [path/to/file]"},"aws-quicksight":{"n":"aws-quicksight","t":"Manage business intelligence dashboards and analytics","s":[["TAGLINE","Manage business intelligence dashboards and analytics"],["TLDR","**List all dashboards** in an account\n\n```aws quicksight list-dashboards --aws-account-id [account-id]```\n\n**Describe a dashboard**\n\n```aws quicksight describe-dashboard --aws-account-id [account-id] --dashboard-id [dashboard-id]```\n\n**Create a data source** from RDS\n\n```aws quicksight create-data-source --aws-account-id [account-id] --data-source-id [source-id] --name [my-source] --type RDS --data-source-parameters file://[rds-params.json]```\n\n**Create a data set** from a data source\n\n```aws quicksight create-data-set --aws-account-id [account-id] --data-set-id [dataset-id] --name [my-dataset] --physical-table-map file://[table-map.json] --import-mode SPICE```\n\n**Generate an embed URL** for a dashboard\n\n```aws quicksight generate-embed-url-for-registered-user --aws-account-id [account-id] --user-arn [arn:aws:quicksight:region:account:user/default/username] --experience-configuration Dashboard={InitialDashboardId=[dashboard-id]}```\n\n**Register a new user**\n\n```aws quicksight register-user --aws-account-id [account-id] --namespace default --email [user@example.com] --identity-type IAM --user-role READER --iam-arn [arn:aws:iam::account:user/username]```"],["SYNOPSIS","**aws quicksight** _command_ [_options_]"],["DESCRIPTION","**aws quicksight** is the AWS CLI interface for Amazon QuickSight, a serverless business intelligence service for creating visualizations, dashboards, and performing ad-hoc analysis. QuickSight uses SPICE (Super-fast Parallel In-memory Calculation Engine) for fast query performance.\n\nQuickSight supports connections to various data sources including S3, Athena, Redshift, RDS, and third-party databases. Dashboards can be embedded in applications and shared with users across the organization."],["COMMANDS","**list-dashboards**\n> List all dashboards in the account\n\n**describe-dashboard**\n> Get dashboard details\n\n**create-data-source**\n> Create a connection to a data source\n\n**create-data-set**\n> Define a dataset from a source\n\n**create-analysis**\n> Create an analysis for exploration\n\n**create-dashboard**\n> Publish an analysis as a dashboard\n\n**register-user**\n> Add a QuickSight user\n\n**generate-embed-url-for-registered-user**\n> Get embed URL for dashboard embedding\n\n**create-ingestion**\n> Refresh a SPICE dataset\n\n**list-data-sources**\n> List configured data sources\n\n**list-data-sets**\n> List all datasets in the account\n\n**delete-dashboard**\n> Delete a dashboard\n\n**update-dashboard**\n> Update an existing dashboard\n\n**describe-data-source**\n> Get data source details"],["CAVEATS","SPICE datasets have storage limits based on subscription tier. Dashboard refresh depends on data source refresh schedules. Embedded dashboards require proper CORS and domain configuration. User management is separate from IAM; QuickSight has its own user registry."],["HISTORY","Amazon QuickSight launched in **November 2016** as a cloud-native BI service. Q (natural language querying) was added in **2020**. The service has expanded to include paginated reports, APIs for programmatic dashboard creation, and ML-powered insights."],["INSTALL","```dnf: sudo dnf install aws```\n\n```pacman: sudo pacman -S aws-cli```\n\n```apk: sudo apk add aws-cli```\n\n```zypper: sudo zypper install aws-cli```"],["SEE ALSO","[aws](/man/aws)(1), [aws-athena](/man/aws-athena)(1), [aws-redshift](/man/aws-redshift)(1)"]],"e":["aws","aws-athena","aws-redshift"],"x":"aws quicksight list-dashboards --aws-account-id [account-id]"},"basalt":{"n":"basalt","t":"TUI for managing Obsidian vaults and notes from the terminal","s":[["TAGLINE","TUI for managing Obsidian vaults and notes from the terminal"],["TLDR","**Launch basalt** to select and open an Obsidian vault\n\n```basalt```\n\n**Install via Cargo**\n\n```cargo install basalt-tui```"],["SYNOPSIS","**basalt**"],["DESCRIPTION","**basalt** is a cross-platform terminal user interface (TUI) for managing Obsidian vaults and notes directly from the command line. It provides a minimalist interface with rendered markdown preview, vim-inspired navigation, and an experimental built-in editor.\n\nThe interface consists of three main panes: the **Explorer** pane on the left for browsing notes and folders, the **Note Editor** pane in the center for reading notes with rendered markdown, and the **Outline** pane on the right for navigating headings within a note. Only one pane has focus at a time, indicated by a thicker border.\n\nbasalt renders CommonMark and GitHub Flavored Markdown including headings, lists, task lists, code blocks, block quotes, Obsidian-style callouts, wiki-links, and inline code. Navigation uses vim-style keybindings: **j**/**k** or arrow keys to move, **Tab**/**Shift+Tab** to switch panes, **Enter** to open a note, **t** to toggle the explorer, **Ctrl+O** to toggle the outline, **Ctrl+U**/**Ctrl+D** for half-page scrolling, **s** to toggle sort order, and **r** to rename notes or folders. When renaming a note, all wiki-links referencing it are automatically updated throughout the vault.\n\nGlobal shortcuts include **q** to quit, **?** to open the help modal, and **Ctrl+G** to open the vault selector for switching between Obsidian vaults. Custom commands can be configured to launch external editors or applications using the **exec:** and **spawn:** prefixes with dynamic variables **%vault**, **%note**, and **%note_path**.\n\nBuilt with Rust using ratatui for terminal rendering and pulldown-cmark for markdown parsing. The project is organized into three crates: **basalt-core** for domain logic, **basalt-widgets** for reusable TUI widgets, and **basalt-tui** for the main application."],["CONFIGURATION","Configuration uses TOML files. The first discovered file takes precedence:\n\n**macOS/Linux:**\n> **$HOME/.basalt.toml** or **$XDG_CONFIG_HOME/basalt/config.toml**\n\n**Windows:**\n> **%USERPROFILE%\\\\.basalt.toml** or **%APPDATA%\\\\basalt\\\\config.toml**\n\n**Top-level settings:**\n> **experimental_editor** = true/false -- Enable the experimental built-in editor (default: false)\n> **vim_mode** = true/false -- Activate vim-like keybinding preset (default: false)\n\n**Sections:**\n> **[global]** -- Universal keybindings applied across the application\n> **[explorer]** -- Explorer pane keybindings and behavior\n> **[outline]** -- Outline pane keybindings\n> **[note_editor]** -- Note editor pane keybindings\n> **[input_modal]** -- Input modal keybindings\n> **[help_modal]** -- Help modal keybindings\n> **[vault_selector_modal]** -- Vault selector keybindings\n\nEach key mapping follows the format: **{ key = \"binding\", command = \"action\" }**. Custom commands use **exec:** for synchronous execution or **spawn:** for asynchronous launching."],["CAVEATS","The application is in active development. Images, tables, horizontal rules, footnotes, and HTML content are not rendered. External links are not clickable. Syntax highlighting in code blocks is not supported. Inline text styling (bold, italic, strikethrough) is parsed but not visually rendered in the terminal.\n\nFile operations are limited: creating, deleting, moving, copying, and searching notes are not supported. Only renaming and sorting are available.\n\nThe experimental editor has significant limitations including no undo/redo, no clipboard support, no text selection, no multi-line deletion, and non-customizable keybindings in edit mode. It operates on individual blocks rather than full documents.\n\nShell features like pipes, redirects, and command substitution are not supported in custom commands. Obsidian plugins, graph view, and the backlinks panel are not available."],["HISTORY","**basalt** was created by **Erik Juhani Nylund** with the repository established in **September 2023**. The first public release (v0.3.7) was published in **May 2025**. The project is written in Rust and licensed under the MIT license. As of early 2026, it has over 30 releases and around 1000 GitHub stars."],["INSTALL","```aur: yay -S basalt```\n\n```nix: nix profile install nixpkgs#basalt```"],["SEE ALSO","[obsidian](/man/obsidian)(1), [glow](/man/glow)(1), [vim](/man/vim)(1), [nvim](/man/nvim)(1)"]],"e":["obsidian","glow","vim","nvim"],"x":"basalt"},"bowtie2":{"n":"bowtie2","t":"Align sequencing reads to reference genomes.","s":[["TAGLINE","Align sequencing reads to reference genomes."],["TLDR","**Align reads** to a reference genome\n\n```bowtie2 -x [genome_index] -1 [reads_1.fq] -2 [reads_2.fq] -S [output.sam]```\n\n**Align single-end reads**\n\n```bowtie2 -x [genome_index] -U [reads.fq] -S [output.sam]```\n\n**Use multiple threads** for faster alignment\n\n```bowtie2 -p [8] -x [genome_index] -1 [reads_1.fq] -2 [reads_2.fq] -S [output.sam]```\n\n**Build an index** from a reference genome\n\n```bowtie2-build [reference.fa] [index_base]```\n\n**Align with local** (soft-clipping) mode\n\n```bowtie2 --local -x [genome_index] -U [reads.fq] -S [output.sam]```\n\n**Very sensitive alignment**\n\n```bowtie2 --very-sensitive -x [genome_index] -1 [r1.fq] -2 [r2.fq] -S [out.sam]```\n\n**Output unaligned reads** to a file\n\n```bowtie2 -x [genome_index] -U [reads.fq] -S [output.sam] --un [unaligned.fq]```"],["SYNOPSIS","**bowtie2** [_options_] **-x** _index_ {**-1** _m1_ **-2** _m2_ | **-U** _reads_} **-S** _sam_\n\n**bowtie2-build** [_options_] _reference_ _index_base_"],["DESCRIPTION","**bowtie2** is a fast and memory-efficient tool for aligning sequencing reads to long reference sequences. It is particularly good at aligning reads of about 50 to 1000 base pairs to relatively large genomes like the human genome.\n\nBowtie2 uses an FM Index (based on the Burrows-Wheeler transform) for the reference genome, enabling fast alignment while maintaining low memory usage. It supports gapped, local, and paired-end alignment modes.\n\nThe alignment output is SAM format, which can be processed by samtools and other downstream tools for variant calling, expression analysis, and other genomics workflows."],["PARAMETERS","**-x** _index_\n> Index filename prefix (built with bowtie2-build).\n\n**-1** _reads_\n> Comma-separated files with #1 mates.\n\n**-2** _reads_\n> Comma-separated files with #2 mates.\n\n**-U** _reads_\n> Comma-separated files with unpaired reads.\n\n**-S** _sam_\n> Output SAM file.\n\n**-p** _threads_\n> Number of parallel threads.\n\n**--local**\n> Local alignment mode (soft-clipping).\n\n**--end-to-end**\n> End-to-end alignment (default).\n\n**--very-fast**\n> Preset for very fast alignment.\n\n**--sensitive**\n> Preset for sensitive alignment (default).\n\n**--very-sensitive**\n> Preset for very sensitive alignment.\n\n**--un** _file_\n> Write unaligned reads to file.\n\n**--al** _file_\n> Write aligned reads to file.\n\n**-q**\n> Input files are FASTQ (default).\n\n**-f**\n> Input files are FASTA."],["CAVEATS","Index must be built before alignment using bowtie2-build. Memory usage scales with genome size. Very sensitive mode is significantly slower. Paired-end alignment requires coordinated mate files. Output needs sorting for many downstream applications."],["HISTORY","**Bowtie2** was developed by **Ben Langmead** and **Steven Salzberg** at Johns Hopkins University, published in **2012** in Nature Methods. It succeeded the original Bowtie aligner with improved handling of longer reads and gapped alignments. Bowtie2 has become one of the most widely used aligners in genomics research, particularly for DNA-seq and ChIP-seq analysis."],["INSTALL","```apt: sudo apt install bowtie2```\n\n```dnf: sudo dnf install bowtie2```\n\n```zypper: sudo zypper install bowtie2```\n\n```brew: brew install bowtie2```\n\n```nix: nix profile install nixpkgs#bowtie2```"],["SEE ALSO","[bwa](/man/bwa)(1), [samtools](/man/samtools)(1)"]],"e":["bwa","samtools"],"x":"bowtie2 -x [genome_index] -1 [reads_1.fq] -2 [reads_2.fq] -S [output.sam]"},"cheat":{"n":"cheat","t":"interactive command-line cheatsheets","s":[["TAGLINE","interactive command-line cheatsheets"],["TLDR","**View** cheatsheet\n\n```cheat [tar]```\n\n**List** available cheatsheets\n\n```cheat -l```\n\n**Search** cheatsheets\n\n```cheat -s [keyword]```\n\n**Edit** cheatsheet\n\n```cheat -e [command]```\n\n**Create** new cheatsheet\n\n```cheat -e [newcommand]```"],["SYNOPSIS","**cheat** [_options_] [_command_]"],["DESCRIPTION","**cheat** allows you to create and view interactive cheatsheets on the command line. It provides quick access to common command examples and usage patterns, making it easier to remember complex command syntax.\n\nThe tool is designed to be a more practical alternative to man pages for everyday use."],["PARAMETERS","**-l**, **--list**\n> List available cheatsheets\n\n**-s**, **--search** _keyword_\n> Search cheatsheets\n\n**-e**, **--edit** _sheet_\n> Edit cheatsheet\n\n**-p**, **--path**\n> Show cheatsheet path\n\n**-d**, **--directories**\n> List cheatsheet directories\n\n**-v**, **--version**\n> Show version"],["CONFIGURATION","**~/.config/cheat/conf.yml**\n> Main configuration file for editor, cheatsheet directories, and syntax highlighting.\n\n**~/.config/cheat/cheatsheets/personal/**\n> Directory for user-created custom cheatsheets."],["CAVEATS","Requires initial setup. Community cheatsheets need downloading. Not as comprehensive as man pages. Custom cheatsheets need manual creation. Not installed by default."],["HISTORY","**cheat** was created by Chris Lane around **2013** to provide quick-access command examples, inspired by projects like tldr."],["INSTALL","```dnf: sudo dnf install cheat```\n\n```zypper: sudo zypper install cheat```\n\n```brew: brew install cheat```\n\n```nix: nix profile install nixpkgs#cheat```"],["SEE ALSO","[tldr](/man/tldr)(1), [man](/man/man)(1), [info](/man/info)(1)"],["RESOURCES","```[Source code](https://github.com/cheat/cheat)```"]],"e":["tldr","man","info"],"x":"cheat [tar]"},"dockdiver":{"n":"dockdiver","t":"explore and extract Docker registry contents","s":[["TAGLINE","explore and extract Docker registry contents"],["TLDR","**List all repositories** in a registry\n\n```dockdiver -url [registry_host] -list```\n\n**Dump a specific repository**\n\n```dockdiver -url [registry_host] -dump [repository]```\n\n**Dump every repository** into a directory\n\n```dockdiver -url [registry_host] -dump-all -dir [output_dir]```\n\n**Authenticate with basic** credentials\n\n```dockdiver -url [registry_host] -username [user] -password [pass] -list```\n\n**Use a bearer token** and skip TLS verification\n\n```dockdiver -url [registry_host] -bearer [token] -insecure -dump-all```"],["SYNOPSIS","**dockdiver** [_options_]"],["PARAMETERS","**-url** _STRING_\n> Base URL or hostname of the Docker registry (required).\n\n**-port** _INT_\n> Registry port used when not specified in the URL. Default 5000.\n\n**-list**\n> List all repositories in the registry.\n\n**-dump** _REPOSITORY_\n> Download a single repository (manifest, config blob, and layer tarballs).\n\n**-dump-all**\n> Download all repositories found in the registry.\n\n**-dir** _DIRECTORY_\n> Output directory for dumped files. Default docker_dump.\n\n**-username** _STRING_\n> Username for HTTP Basic authentication.\n\n**-password** _STRING_\n> Password for HTTP Basic authentication.\n\n**-bearer** _STRING_\n> Bearer token for the Authorization header.\n\n**-headers** _JSON_\n> Custom request headers as JSON, e.g. '{\"X-Custom\": \"Value\"}'.\n\n**-insecure**\n> Skip TLS certificate verification.\n\n**-rate** _INT_\n> Requests per second. Default 3.\n\n**-timeout** _DURATION_\n> HTTP request timeout, e.g. 10s or 500ms. Default 30s.\n\n**-proxy** _URL_\n> Proxy URL (http, https, or socks5).\n\n**-proxy-username** _STRING_\n> Username for SOCKS5 proxy authentication.\n\n**-proxy-password** _STRING_\n> Password for SOCKS5 proxy authentication."],["DESCRIPTION","**dockdiver** is a Go utility that enumerates and extracts the contents of a Docker registry exposed over the Registry HTTP API V2. It can list repositories, dump a single repository, or dump every repository, retrieving manifests, configuration blobs, and layer tarballs while verifying each blob with its SHA256 digest.\n\nIt is aimed at penetration testing, bug bounty, and CTF scenarios where a registry (often a self-hosted one on port 5000) is reachable and may expose images that contain source code, configuration, or credentials. Authentication via Basic credentials or a bearer token, custom headers, proxy support, and a configurable request rate make it suitable for working against access-controlled or rate-limited registries.\n\nThis tool targets the registry itself rather than the public Docker Hub web interface, and it downloads raw image data for offline inspection rather than scanning layers for secrets on its own."],["CAVEATS","Only registries reachable over the HTTP API V2 are supported. Use against registries you are authorized to access. Dumping all repositories can transfer large amounts of data; the default rate of 3 requests per second limits load and helps avoid tripping rate limits."],["HISTORY","dockdiver is an open-source Go tool published on GitHub by MachiavelliII. It ships with a registry lab environment in its lab directory for safe testing."],["SEE ALSO","[dive](/man/dive)(1), [trivy](/man/trivy)(1), [grype](/man/grype)(1), [docker-pull](/man/docker-pull)(1)"]],"e":["dive","trivy","grype","docker-pull"],"x":"dockdiver -url [registry_host] -list"},"ds4-bench":{"n":"ds4-bench","t":"Prefill and generation speed benchmark for DwarfStar","s":[["TAGLINE","Prefill and generation speed benchmark for DwarfStar"],["TLDR","**Sweep prefill/generation speed** across context frontiers\n\n```ds4-bench --prompt-file [long.txt] --ctx-max [32768]```\n\n**Write results as CSV**\n\n```ds4-bench --prompt-file [long.txt] --ctx-max [32768] --csv [speed.csv]```\n\n**Prefill only** (no generation tokens)\n\n```ds4-bench --prompt-file [long.txt] --gen-tokens [0]```\n\n**Measure with reduced GPU power**\n\n```ds4-bench --prompt-file [long.txt] --power [70]```\n\n**Benchmark a chat-rendered prompt**\n\n```ds4-bench --chat-prompt-file [prompt.txt] --ctx-max [16384]```\n\n**SSD-streamed model** on a memory-constrained machine\n\n```ds4-bench -m [ds4flash.gguf] --ssd-streaming --prompt-file [long.txt]```"],["SYNOPSIS","**ds4-bench** [_options_]"],["PARAMETERS","**-m**, **--model** _FILE_\n> Path to the GGUF model. Default: **ds4flash.gguf**.\n\n**--prompt-file** _FILE_\n> Raw benchmark text; the token sequence is sliced at each measured frontier.\n\n**--chat-prompt-file** _FILE_\n> Render _FILE_ as one no-thinking chat user message instead of raw text.\n\n**-sys**, **--system** _TEXT_\n> System prompt used only with **--chat-prompt-file**.\n\n**--ctx-start** _N_\n> First measured context frontier. Default: **2048**.\n\n**--ctx-max** _N_\n> Last measured context frontier. Default: **32768**.\n\n**--ctx-alloc** _N_\n> Allocated context size. Default: **ctx-max + gen-tokens + 1**.\n\n**--step-incr** _N_\n> Linear step between frontiers when **--step-mul** is 1. Default: **2048**.\n\n**--step-mul** _F_\n> Multiplicative step between frontiers. Default: **1** (linear).\n\n**--gen-tokens** _N_\n> Greedy decode tokens per frontier. **0** for pure prefill. Default: **128**.\n\n**--csv** _FILE_\n> Write CSV to _FILE_ instead of stdout.\n\n**--power** _N_\n> GPU duty-cycle target from 1 to 100.\n\n**--metal** | **--cuda** | **--rocm** | **--cpu**\n> Select the inference backend explicitly.\n\n**--ssd-streaming**\n> Opt into SSD-backed model streaming for the benchmark run."],["DESCRIPTION","**ds4-bench** measures prefill and generation throughput for the **DwarfStar** (**ds4**) inference engine across a sweep of context lengths. You supply a long prompt file; the tool tokenizes it once, then at each frontier runs prefill (and optional greedy decode) so you can compare tokens/s as context grows.\n\nIt is the project's standard speed-bench entry point for Metal, CUDA, and ROCm paths, and for comparing full-resident versus **--ssd-streaming** configurations. Results can be printed to the terminal or written as CSV for charts and regression tracking in **speed-bench/** workflows."],["CAVEATS","Needs a DwarfStar-compatible GGUF and enough memory (or SSD bandwidth) for the chosen **--ctx-alloc**. Timings depend heavily on backend, power limit, thermal state, and cold vs warm expert cache; treat single runs as indicative, not absolute rankings across hardware. Prefill-only mode (**--gen-tokens 0**) does not exercise the decode path."],["HISTORY","**ds4-bench** is part of the **DwarfStar** toolkit by **Salvatore Sanfilippo** (**antirez**) and contributors, used to gate performance work on large MoE models (DeepSeek V4 Flash/PRO and experimental forks) where long-context prefill cost dominates."],["SEE ALSO","[ds4-server](/man/ds4-server)(1), [ds4-eval](/man/ds4-eval)(1), [ds4-agent](/man/ds4-agent)(1), [llama-cli](/man/llama-cli)(1)"],["RESOURCES","```[Source code](https://github.com/antirez/ds4)```\n\n```[Documentation](https://github.com/antirez/ds4/tree/main/speed-bench)```"]],"e":["ds4-server","ds4-eval","ds4-agent","llama-cli"],"x":"ds4-bench --prompt-file [long.txt] --ctx-max [32768]"},"elasticsearch-certutil":{"n":"elasticsearch-certutil","t":"Elasticsearch TLS certificate generator","s":[["TAGLINE","Elasticsearch TLS certificate generator"],["TLDR","**Generate CA certificate**\n\n```elasticsearch-certutil ca```\n\n**Generate node certificate**\n\n```elasticsearch-certutil cert --ca [elastic-stack-ca.p12]```\n\n**Generate CSR**\n\n```elasticsearch-certutil csr```\n\n**Generate HTTP certificates**\n\n```elasticsearch-certutil http```\n\n**Output in PEM format**\n\n```elasticsearch-certutil ca --pem```"],["SYNOPSIS","**elasticsearch-certutil** _mode_ [_options_]"],["SUBCOMMANDS","**ca**\n> Generate certificate authority.\n\n**cert**\n> Generate X.509 certificates.\n\n**csr**\n> Generate certificate signing requests.\n\n**http**\n> Generate HTTP layer certificates."],["PARAMETERS","**--ca** _file_\n> Path to existing CA in PKCS#12 format (cert mode).\n\n**--ca-cert** _file_\n> Path to CA certificate in PEM format.\n\n**--ca-key** _file_\n> Path to CA private key in PEM format.\n\n**--ca-pass** _password_\n> Password for the CA private key.\n\n**--out** _file_\n> Output file path.\n\n**--pem**\n> Output in PEM format instead of PKCS#12.\n\n**--dns** _names_\n> Comma-separated list of DNS names for the certificate.\n\n**--ip** _addresses_\n> Comma-separated list of IP addresses for the certificate.\n\n**--name** _name_\n> Filename (without extension) for generated certificate.\n\n**--pass** _password_\n> Password for the generated private key.\n\n**--days** _n_\n> Certificate validity period in days (default: 1095).\n\n**--keysize** _bits_\n> RSA key size in bits (default: 2048).\n\n**--multiple**\n> Generate certificates for multiple instances.\n\n**--in** _file_\n> YAML file describing instances to generate certificates for.\n\n**--self-signed**\n> Create a self-signed certificate (cert mode).\n\n**-s**, **--silent**\n> Minimal output.\n\n**-v**, **--verbose**\n> Verbose output."],["DESCRIPTION","**elasticsearch-certutil** generates certificates and certificate authorities for securing Elasticsearch clusters. It simplifies the certificate management process by automating the creation of self-signed certificates, certificate authorities, and certificate signing requests.\n\nThe tool is essential for enabling TLS/SSL encryption on both the transport layer (node-to-node communication) and the HTTP layer (client API access). It generates certificates in PKCS#12 or PEM format and can include multiple DNS names and IP addresses for cluster deployments.\n\nelasticsearch-certutil streamlines security configuration required for production Elasticsearch deployments and enables features like Elastic Stack security."],["SEE ALSO","[elasticsearch](/man/elasticsearch)(1), [elasticsearch-keystore](/man/elasticsearch-keystore)(1), [elasticsearch-users](/man/elasticsearch-users)(1)"]],"e":["elasticsearch","elasticsearch-keystore","elasticsearch-users"],"x":"elasticsearch-certutil ca"},"fc-list":{"n":"fc-list","t":"list available fonts","s":[["TAGLINE","list available fonts"],["TLDR","**List all fonts**\n\n```fc-list```\n\n**List fonts matching pattern**\n\n```fc-list : family style | sort```\n\n**Find specific font**\n\n```fc-list | grep -i \"[arial]\"```\n\n**List monospace fonts**\n\n```fc-list :spacing=mono family```\n\n**Show font file paths**\n\n```fc-list : file family```\n\n**List fonts with specific style**\n\n```fc-list :style=Bold```\n\n**List fonts by language**\n\n```fc-list :lang=[zh]```"],["SYNOPSIS","**fc-list** [_options_] [_pattern_] [_elements_]"],["PARAMETERS","**-v**, **--verbose**\n> Verbose output with all properties.\n\n**-f** _format_\n> Custom output format.\n\n**-q**, **--quiet**\n> Suppress normal output.\n\n**--version**\n> Show version.\n\n_pattern_\n> Font matching pattern.\n\n_elements_\n> Properties to display: family, style, file, etc."],["DESCRIPTION","**fc-list** lists fonts available to applications using fontconfig. It displays font family names, styles, file locations, and other properties.\n\nThe command is essential for discovering installed fonts, finding font file paths, and verifying font installation. It queries the fontconfig database which caches font information."],["CAVEATS","Output can be long; pipe to grep or sort. Font names may vary by language. Newly installed fonts need `fc-cache -fv`. Some applications cache fonts independently."],["HISTORY","fc-list is part of **fontconfig**, created by **Keith Packard** around **2000** to provide system-wide font configuration for X11 and other systems. It replaced the older X font system with a more flexible, file-based approach."],["INSTALL","```apt: sudo apt install fontconfig```\n\n```dnf: sudo dnf install fontconfig```\n\n```pacman: sudo pacman -S fontconfig```\n\n```apk: sudo apk add fontconfig```\n\n```zypper: sudo zypper install fontconfig```\n\n```brew: brew install fontconfig```\n\n```nix: nix profile install nixpkgs#fontconfig```"],["SEE ALSO","[fc-cache](/man/fc-cache)(1), [fc-match](/man/fc-match)(1), [fc-query](/man/fc-query)(1)"]],"e":["fc-cache","fc-match","fc-query"],"x":"fc-list"},"fierce":{"n":"fierce","t":"DNS reconnaissance and subdomain discovery tool","s":[["TAGLINE","DNS reconnaissance and subdomain discovery tool"],["TLDR","**DNS reconnaissance on** domain\n\n```fierce --domain [example.com]```\n\n**Use custom DNS** server\n\n```fierce --domain [example.com] --dns-servers [8.8.8.8]```\n\n**Search with wordlist**\n\n```fierce --domain [example.com] --subdomain-file [wordlist.txt]```\n\n**Expand search range**\n\n```fierce --domain [example.com] --wide```\n\n**Output to file**\n\n```fierce --domain [example.com] > [output.txt]```"],["SYNOPSIS","**fierce** [_options_]"],["PARAMETERS","**--domain** _DOMAIN_\n> Target domain to scan.\n\n**--dns-servers** _SERVERS_\n> Custom DNS servers.\n\n**--subdomain-file** _FILE_\n> Wordlist for subdomain bruteforce.\n\n**--wide**\n> Scan entire class C of found hosts.\n\n**--traverse** _N_\n> Scan adjacent IPs.\n\n**--delay** _SEC_\n> Delay between queries.\n\n**--help**\n> Display help information."],["DESCRIPTION","**fierce** is a DNS reconnaissance tool that locates non-contiguous IP space and hostnames for a target. It performs zone transfers, subdomain brute forcing, and adjacent IP discovery.\n\nThe tool attempts DNS zone transfers first, then falls back to dictionary-based subdomain enumeration. When hosts are found, it scans nearby IP addresses to discover additional systems.\n\nfierce is used in penetration testing for initial target enumeration and attack surface mapping."],["CAVEATS","Only use against authorized targets. Zone transfers usually fail on properly configured servers. DNS queries may be logged."],["HISTORY","fierce was created by **RSnake** (Robert Hansen) as a DNS reconnaissance tool for penetration testers. The Python 3 rewrite modernized the original Perl script for current security workflows."],["INSTALL","```brew: brew install fierce```\n\n```nix: nix profile install nixpkgs#fierce```"],["SEE ALSO","[dig](/man/dig)(1), [nmap](/man/nmap)(1), [dnsenum](/man/dnsenum)(1)"],["RESOURCES","```[Source code](https://github.com/mschwager/fierce)```"]],"e":["dig","nmap","dnsenum"],"x":"fierce --domain [example.com]"},"fzy":{"n":"fzy","t":"Simple, fast fuzzy text selector for the terminal","s":[["TAGLINE","Simple, fast fuzzy text selector for the terminal"],["TLDR","**Fuzzy-pick a line** from stdin\n\n```[command] | fzy```\n\n**Pick a file** to edit\n\n```find . -type f | fzy | xargs -r $EDITOR```\n\n**Limit number of visible matches**\n\n```[command] | fzy -l [20]```\n\n**Show help**\n\n```fzy --help```"],["SYNOPSIS","**fzy** [**-l** *lines*] [**-p** *prompt*] [**-q** *query*] [**-e** *query*] [**-t**] [**-s**] [**-0**] [**-j** *workers*]"],["DESCRIPTION","**fzy** is a minimal fuzzy finder: it reads newline-separated candidates from stdin, interactively filters them as you type, and prints the selected line to stdout. It emphasizes ranking quality and speed with a small codebase, making it a lightweight alternative to heavier selectors."],["PARAMETERS","**-l**, **--lines** *n*\n\n> Maximum number of matches shown (default often 10).\n\n**-p**, **--prompt** *text*\n\n> Input prompt string.\n\n**-q**, **--query** *text*\n\n> Initial query.\n\n**-e**, **--show-matches** *query*\n\n> Non-interactive: print sorted matches for *query* and exit.\n\n**-t**, **--tty-only**\n\n> Only use /dev/tty (behavior depends on version).\n\n**-0**, **--read-null**\n\n> Read NUL-separated input.\n\n**-j** *workers*\n\n> Parallel scoring workers."],["CAVEATS","Needs a TTY for interactive mode. Ranking differs from **fzf**; scripts may need adjustment when switching tools."],["INSTALL","```apt: sudo apt install fzy```\n\n```pacman: sudo pacman -S fzy```\n\n```apk: sudo apk add fzy```\n\n```zypper: sudo zypper install fzy```\n\n```brew: brew install fzy```\n\n```nix: nix profile install nixpkgs#fzy```"],["SEE ALSO","[fzf](/man/fzf)(1), [pick](/man/pick)(1), [sk](/man/sk)(1)"],["RESOURCES","```[Source code](https://github.com/jhawthorn/fzy)```"]],"e":["fzf","pick","sk"],"x":"[command] | fzy"},"gamescope":{"n":"gamescope","t":"Wayland gaming micro-compositor","s":[["TAGLINE","Wayland gaming micro-compositor"],["TLDR","Run a program with **gamescope**\n\n```gamescope -- [program]```\n\nRun a game with gamescope through **Steam**\n\n```gamescope -- %command%```\n\n**Upscale** a 720p game to 1440p with integer scaling\n\n```gamescope -h 720 -H 1440 -S integer -- [command]```\n\n**Limit** a vsynced game to 30 FPS\n\n```gamescope -r 30 -- [command]```\n\nLaunch **Steam in Big Picture Mode** with gamescope integration\n\n```gamescope -e -- /usr/bin/steam -tenfoot```\n\nSpecify which **display** to prefer\n\n```gamescope -O [HDMI-A-1|DP-3|...] -- [program]```\n\nToggle **fullscreen**\n\n```Super+f```\n\nDisplay **help**\n\n```gamescope --help```"],["SYNOPSIS","**gamescope** [_options_] **--** _command_ [_arguments_]"],["PARAMETERS","**-h**, **--nested-height** _HEIGHT_\n> Set internal (nested) resolution height\n\n**-H**, **--output-height** _HEIGHT_\n> Set output resolution height\n\n**-w**, **--nested-width** _WIDTH_\n> Set internal (nested) resolution width\n\n**-W**, **--output-width** _WIDTH_\n> Set output resolution width\n\n**-r**, **--nested-refresh** _RATE_\n> Set internal refresh rate (FPS limit for vsync games)\n\n**-S**, **--scaler** _SCALER_\n> Scaling filter: auto, integer, fit, fill, stretch\n\n**-e**, **--steam**\n> Enable Steam integration mode\n\n**-O**, **--prefer-output** _OUTPUT_\n> Preferred display output\n\n**-f**, **--fullscreen**\n> Start in fullscreen mode\n\n**-b**, **--borderless**\n> Start in borderless windowed mode\n\n**-F**, **--filter** _FILTER_\n> Upscaler filter: linear, nearest, fsr, nis, pixel.\n\n**--hdr-enabled**\n> Enable HDR output (requires compatible display and hardware).\n\n**--mangoapp**\n> Launch MangoHud overlay (mangoapp) alongside gamescope.\n\n**--force-grab-cursor**\n> Always use relative mouse mode instead of flipping between relative and absolute.\n\n**--adaptive-sync**\n> Enable adaptive sync / VRR on the output if supported."],["DESCRIPTION","**gamescope** is a Wayland micro-compositor developed by Valve for gaming. It acts as an intermediary layer between games and the display server, providing features like resolution scaling, frame limiting, HDR support, and improved game compatibility.\n\nKey features include FSR (FidelityFX Super Resolution) upscaling, integer scaling for retro games, independent refresh rate control, and seamless fullscreen management. It's the basis for the Steam Deck's gaming mode compositor.\n\nWhen used with Steam (**-e** flag), it integrates directly with Steam's overlay and game management features."],["KEYBOARD SHORTCUTS","**Super+F** - Toggle fullscreen\n**Super+S** - Take screenshot\n**Super+G** - Toggle FPS limiter"],["CAVEATS","Requires Vulkan support. Some games may have compatibility issues. HDR support requires appropriate hardware and display. Performance overhead is minimal but present."],["HISTORY","gamescope was developed by Valve Software and first released in **2020**. It was created primarily for the Steam Deck handheld gaming PC, providing the foundation for SteamOS 3.0's gaming compositor. The project evolved from earlier work on the steamcompmgr compositor."],["INSTALL","```dnf: sudo dnf install gamescope```\n\n```pacman: sudo pacman -S gamescope```\n\n```apk: sudo apk add gamescope```\n\n```zypper: sudo zypper install gamescope```\n\n```nix: nix profile install nixpkgs#gamescope```"],["SEE ALSO","[steam](/man/steam)(1), [cage](/man/cage)(1), [sway](/man/sway)(1), [weston](/man/weston)(1), [Xwayland](/man/Xwayland)(1)"]],"e":["steam","cage","sway","weston","Xwayland"],"x":"gamescope -- [program]"},"gdaldem":{"n":"gdaldem","t":"analyzes and visualizes terrain elevation","s":[["TAGLINE","analyzes and visualizes terrain elevation"],["TLDR","**Generate a hillshade** from a DEM\n\n```gdaldem hillshade [dem.tif] [hillshade.tif]```\n\n**Create a slope map** in degrees\n\n```gdaldem slope [dem.tif] [slope.tif]```\n\n**Generate an aspect map**\n\n```gdaldem aspect [dem.tif] [aspect.tif]```\n\n**Create color relief** using a color table file\n\n```gdaldem color-relief [dem.tif] [colors.txt] [relief.tif]```\n\n**Generate hillshade with custom** light direction and altitude\n\n```gdaldem hillshade -az [315] -alt [45] [dem.tif] [hillshade.tif]```\n\n**Compute Terrain Ruggedness Index**\n\n```gdaldem TRI [dem.tif] [tri.tif]```"],["SYNOPSIS","**gdaldem** _mode_ [_options_] _input_ _output_"],["PARAMETERS","_MODE_\n> Operation: hillshade, slope, aspect, color-relief, TRI, TPI, roughness.\n\n_INPUT_\n> Input DEM raster.\n\n_OUTPUT_\n> Output raster file.\n\n**-az** _DEGREES_\n> Light azimuth for hillshade.\n\n**-alt** _DEGREES_\n> Light altitude for hillshade.\n\n**-s** _SCALE_\n> Ratio of vertical to horizontal units (e.g., 111120 for feet with degree coordinates).\n\n**-z** _FACTOR_\n> Vertical exaggeration factor.\n\n**-of** _FORMAT_\n> Output raster format (default: GTiff).\n\n**-compute_edges**\n> Compute values at raster edges and nodata boundaries.\n\n**--help**\n> Display help information."],["DESCRIPTION","**gdaldem** analyzes and visualizes digital elevation models. It generates terrain derivatives including hillshade, slope, aspect, and various roughness indices.\n\nThe tool produces visualization-ready output from raw elevation data. Hillshading creates realistic terrain lighting. Slope and aspect support terrain analysis.\n\ngdaldem is essential for cartographic terrain representation and geomorphological analysis."],["CAVEATS","Requires properly scaled DEM. Edge effects at boundaries. Large files need more memory."],["HISTORY","gdaldem is part of **GDAL** (Geospatial Data Abstraction Library), providing DEM analysis tools for terrain visualization and analysis."],["INSTALL","```apt: sudo apt install gdal-bin```\n\n```apk: sudo apk add gdal-tools```"],["SEE ALSO","[gdal_contour](/man/gdal_contour)(1), [gdalinfo](/man/gdalinfo)(1), [ogr2ogr](/man/ogr2ogr)(1)"]],"e":["gdal_contour","gdalinfo","ogr2ogr"],"x":"gdaldem hillshade [dem.tif] [hillshade.tif]"},"git-checkout":{"n":"git-checkout","t":"Switch branches or restore working tree files","s":[["TAGLINE","Switch branches or restore working tree files"],["TLDR","**Switch to branch**\n\n```git checkout [branch-name]```\n\n**Create and switch to branch**\n\n```git checkout -b [new-branch]```\n\n**Restore a file**\n\n```git checkout -- [file.txt]```\n\n**Checkout specific commit**\n\n```git checkout [commit-hash]```\n\n**Checkout from remote**\n\n```git checkout -t origin/[branch]```"],["SYNOPSIS","**git checkout** [_options_] _branch_|_commit_|_file_"],["PARAMETERS","_BRANCH_\n> Branch to switch to.\n\n**-b** _BRANCH_\n> Create and switch to new branch.\n\n**-t**, **--track**\n> Set up tracking for remote branch.\n\n**--** _FILE_\n> Restore file from index.\n\n**-f**, **--force**\n> Force switch, discard changes.\n\n**-d**, **--detach**\n> Detach HEAD at the named commit instead of updating a branch.\n\n**-p**, **--patch**\n> Interactively select hunks to restore from the index or tree.\n\n**-m**, **--merge**\n> Perform a 3-way merge between the current branch, your local changes, and the new branch.\n\n**--orphan** _BRANCH_\n> Create orphan branch.\n\n**--help**\n> Display help information."],["DESCRIPTION","**git checkout** switches branches or restores files. It updates the working tree to match the specified branch, commit, or file version from history.\n\nThe command serves multiple purposes: branch switching, branch creation with **-b**, file restoration with **--**, and detached HEAD operations. Since **Git 2.23**, the recommended approach is **git switch** for branches and **git restore** for files, which provide clearer separation of concerns.\n\nDespite the newer alternatives, git checkout remains widely used for its versatility and is deeply embedded in existing workflows, documentation, and scripts."],["CAVEATS","Uncommitted changes may be lost with -f. Detached HEAD requires care. Consider git switch/restore for clarity."],["HISTORY","git checkout is an original **Git** command that combines several operations. **Git 2.23** introduced git switch and git restore as clearer alternatives."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-switch](/man/git-switch)(1), [git-restore](/man/git-restore)(1), [git-branch](/man/git-branch)(1)"],["RESOURCES","```[Source code](https://github.com/git/git)```\n\n```[Homepage](https://git-scm.com/)```\n\n```[Documentation](https://git-scm.com/docs/git-checkout)```"]],"e":["git-switch","git-restore","git-branch"],"x":"git checkout [branch-name]"},"glances":{"n":"glances","t":"cross-platform system monitoring tool","s":[["TAGLINE","cross-platform system monitoring tool"],["TLDR","**Start glances**\n\n```glances```\n\n**Web server mode**\n\n```glances -w```\n\n**Client mode**\n\n```glances -c [server-ip]```\n\n**Export to CSV**\n\n```glances --export csv --export-csv-file [output.csv]```\n\n**Per-CPU stats** with 5-second refresh\n\n```glances -1 -t 5```\n\n**Server mode** on a custom port\n\n```glances -s -p [61210]```\n\n**Quiet mode**\n\n```glances -q```"],["SYNOPSIS","**glances** [_options_]"],["PARAMETERS","**-w**, **--webserver**\n> Start web server.\n\n**-c** _SERVER_, **--client** _SERVER_\n> Connect to server.\n\n**-s**, **--server**\n> Start server mode.\n\n**-t** _SECONDS_\n> Refresh time.\n\n**--export** _FORMAT_\n> Export to format.\n\n**-p** _PORT_, **--port** _PORT_\n> TCP port for client/server/web mode (default: 61209).\n\n**-1**, **--percpu**\n> Display per-CPU stats instead of aggregated.\n\n**-b**, **--byte**\n> Display network I/O in bytes per second instead of bits.\n\n**-q**, **--quiet**\n> No display, background.\n\n**--disable-plugin** _PLUGIN_\n> Disable a specific plugin (e.g., docker, gpu).\n\n**--stdout** _STATS_\n> Display stats to stdout (comma-separated list).\n\n**--help**\n> Display help information."],["DESCRIPTION","**glances** is a cross-platform system monitoring tool. It displays real-time CPU, memory, disk, network, and process information in a comprehensive terminal interface.\n\nThe tool provides more information than top or htop in a single view. It supports client-server mode, a web interface, and exports to various formats for monitoring and analysis."],["CONFIGURATION","**~/.config/glances/glances.conf**\n> Main configuration file for thresholds, display options, and export settings."],["CAVEATS","Requires Python and psutil. Some features (web mode, Docker, GPU monitoring) need optional dependencies. Web mode requires a browser and the bottle/fastapi module. High refresh rates may increase CPU usage."],["HISTORY","glances was created by **Nicolas Hennion** as a comprehensive system monitoring solution that goes beyond traditional tools like top."],["INSTALL","```dnf: sudo dnf install glances```\n\n```pacman: sudo pacman -S glances```\n\n```apk: sudo apk add glances```\n\n```brew: brew install glances```\n\n```nix: nix profile install nixpkgs#glances```"],["SEE ALSO","[htop](/man/htop)(1), [top](/man/top)(1), [iotop](/man/iotop)(1), [nmon](/man/nmon)(1), [vmstat](/man/vmstat)(8)"]],"e":["htop","top","iotop","nmon","vmstat"],"x":"glances"},"go-mod":{"n":"go-mod","t":"Go module dependency management","s":[["TAGLINE","Go module dependency management"],["TLDR","**Initialize new module**\n\n```go mod init [module-name]```\n\n**Download dependencies**\n\n```go mod download```\n\n**Tidy dependencies**\n\n```go mod tidy```\n\n**Vendor dependencies**\n\n```go mod vendor```\n\n**Verify checksums**\n\n```go mod verify```"],["SYNOPSIS","**go mod** _command_ [_arguments_]"],["PARAMETERS","**init** _NAME_\n> Initialize new module.\n\n**download**\n> Download modules to cache.\n\n**tidy**\n> Add missing, remove unused.\n\n**vendor**\n> Create vendor directory.\n\n**verify**\n> Verify dependencies.\n\n**graph**\n> Print dependency graph.\n\n**why** _PACKAGE_\n> Explain why needed.\n\n**--help**\n> Display help information."],["DESCRIPTION","**go mod** manages Go modules, the official dependency management system for Go. Modules are collections of packages with versioned dependencies defined in go.mod files.\n\nThe command initializes modules, manages dependencies, and maintains reproducible builds. It integrates with module proxies and checksum databases to ensure package integrity and availability."],["CONFIGURATION","**go.mod**\n> Module definition file containing module path, Go version, and dependency requirements.\n\n**go.sum**\n> Checksums of module dependencies for verification.\n\n**GOSUMDB**\n> Name of the checksum database used to verify downloaded modules; set to **off** to disable verification.\n\n**GONOSUMDB**\n> Glob patterns of module paths to exclude from checksum-database verification (defaults from **GOPRIVATE** if unset).\n\n**GOFLAGS**\n> Default flags applied to go commands, including go mod subcommands."],["CAVEATS","Requires Go 1.11+. go.sum should be committed. Vendor mode for offline builds."],["HISTORY","Go modules were introduced in **Go 1.11** as the official dependency management solution, replacing GOPATH-based workflows."],["INSTALL","```apt: sudo apt install gccgo-go```\n\n```dnf: sudo dnf install gcc-go```\n\n```pacman: sudo pacman -S gcc-go```\n\n```apk: sudo apk add gcc-go```\n\n```zypper: sudo zypper install gcc-go```\n\n```brew: brew install go```\n\n```nix: nix profile install nixpkgs#go```"],["SEE ALSO","[go](/man/go)(1), [go-get](/man/go-get)(1)"],["RESOURCES","```[Documentation](https://go.dev/ref/mod)```"]],"e":["go","go-get"],"x":"go mod init [module-name]"},"grub-mkfont":{"n":"grub-mkfont","t":"convert fonts to GRUB PF2 format","s":[["TAGLINE","convert fonts to GRUB PF2 format"],["TLDR","**Convert font to GRUB format**\n\n```grub-mkfont -o [output.pf2] [input.ttf]```\n\n**Specify font size**\n\n```grub-mkfont -s [16] -o [font16.pf2] [input.ttf]```\n\n**Include specific characters**\n\n```grub-mkfont --range=[0x0-0xff] -o [font.pf2] [input.ttf]```\n\n**Verbose conversion**\n\n```grub-mkfont -v -o [output.pf2] [input.ttf]```"],["SYNOPSIS","**grub-mkfont** [_options_] _font-file_"],["PARAMETERS","_FONT-FILE_\n> Input font (TTF, OTF, PF2).\n\n**-o**, **--output** _FILE_\n> Output PF2 file.\n\n**-s**, **--size** _N_\n> Font size in pixels.\n\n**-i**, **--index** _N_\n> Face index for TTC.\n\n**-n**, **--name** _NAME_\n> Set font family name.\n\n**-b**, **--bold**\n> Convert to a bold font.\n\n**-r**, **--range** _FROM-TO_\n> Unicode range(s) to include (comma-separated).\n\n**-v**, **--verbose**\n> Verbose output.\n\n**--help**\n> Display help information."],["DESCRIPTION","**grub-mkfont** converts fonts to GRUB's PF2 bitmap format, enabling custom fonts in the GRUB boot menu. The tool accepts TrueType, OpenType, and other font formats.\n\nIt can generate fonts at specific sizes and with specific character ranges, allowing fine control over which glyphs are included in the output."],["CAVEATS","Part of GRUB package. Larger fonts increase boot time. Limited to bitmap conversion."],["HISTORY","grub-mkfont was developed as part of **GNU GRUB 2** to support graphical boot menus with custom fonts."],["INSTALL","```apt: sudo apt install grub-common```\n\n```apk: sudo apk add grub-mkfont```"],["SEE ALSO","[grub](/man/grub)(8), [grub-mkconfig](/man/grub-mkconfig)(8), [fc-list](/man/fc-list)(1)"],["RESOURCES","```[Homepage](https://www.gnu.org/software/grub/)```\n\n```[Source code](https://git.savannah.gnu.org/cgit/grub.git)```\n\n```[Documentation](https://www.gnu.org/software/grub/manual/grub/grub.html)```"]],"e":["grub","grub-mkconfig","fc-list"],"x":"grub-mkfont -o [output.pf2] [input.ttf]"},"hike":{"n":"hike","t":"Markdown browser for the terminal","s":[["TAGLINE","Markdown browser for the terminal"],["TLDR","**Start hike** to browse markdown files\n\n```hike```\n\n**Open a specific markdown file**\n\n```hike [path/to/file.md]```"],["SYNOPSIS","**hike** [_options_] [_file_]"],["DESCRIPTION","**hike** is a terminal-based Markdown browser that provides the ability to browse and view local Markdown files, as well as Markdown files downloaded from the web. It includes shortcuts for quickly accessing Markdown files on popular git forges like **GitHub**, **GitLab**, **Codeberg**, and **Bitbucket**.\n\nHike supports editing Markdown documents from the local filesystem with a built-in editor, and also supports using your own choice of external editor. Commands can be discovered via the command palette (**Ctrl+P**)."],["CAVEATS","Requires a terminal with modern rendering capabilities. Web-hosted Markdown files require an internet connection for fetching."],["HISTORY","**hike** was created by **Dave Pearson** (davep) and is written in **Python** using the **Textual** TUI framework. It was designed to provide a native terminal experience for browsing documentation and README files."],["SEE ALSO","[glow](/man/glow)(1), [mdcat](/man/mdcat)(1), [bat](/man/bat)(1)"]],"e":["glow","mdcat","bat"],"x":"hike"},"hostnamectl":{"n":"hostnamectl","t":"queries and changes the system hostname and related machine metadata","s":[["TAGLINE","queries and changes the system hostname and related machine metadata"],["TLDR","Show current **hostname** information\n\n```hostnamectl```\n\nSet the **hostname**\n\n```sudo hostnamectl set-hostname \"hostname\"```\n\nSet **static and pretty** hostnames\n\n```sudo hostnamectl set-hostname --static \"hostname.example.com\" && sudo hostnamectl set-hostname --pretty \"My Computer\"```\n\nReset **pretty hostname** to default\n\n```sudo hostnamectl set-hostname --pretty \"\"```\n\nSet **chassis type**\n\n```sudo hostnamectl set-chassis desktop```\n\nSet **deployment environment**\n\n```sudo hostnamectl set-deployment production```"],["SYNOPSIS","**hostnamectl** [_OPTIONS_] [_COMMAND_]"],["DESCRIPTION","**hostnamectl** queries and changes the system hostname and related machine metadata. It manages three hostname types: static (persistent), pretty (human-readable), and transient (temporary)."],["COMMANDS","**status**\n> Show current hostname and machine information\n\n**hostname [NAME]**\n> Get or set the system hostname\n\n**set-hostname NAME**\n> Set the system hostname\n\n**set-chassis TYPE**\n> Set the chassis type (desktop, laptop, server, vm, container, etc.)\n\n**set-deployment ENV**\n> Set deployment environment (development, production, etc.)\n\n**set-location LOC**\n> Set physical location of the machine\n\n**set-icon-name NAME**\n> Set icon name for graphical UIs"],["PARAMETERS","**--static**\n> Operate on static hostname only\n\n**--transient**\n> Operate on transient hostname only\n\n**--pretty**\n> Operate on pretty hostname only\n\n**--no-ask-password**\n> Do not prompt for password\n\n**-H, --host**\n> Execute operation on remote host\n\n**-M, --machine**\n> Execute operation on container\n\n**-j, --json**\n> Output in JSON format"],["HOSTNAME TYPES","**Static**: Stored in /etc/hostname, survives reboots\n**Transient**: Temporary, set by DHCP or mDNS\n**Pretty**: Free-form UTF-8 name for display purposes"],["CAVEATS","The static hostname should be a single valid DNS label (no spaces, only alphanumeric and hyphens). The pretty hostname can contain any characters and is for display only."],["HISTORY","**hostnamectl** is part of **systemd**, providing unified hostname management through systemd-hostnamed."],["INSTALL","```apt: sudo apt install systemd```\n\n```dnf: sudo dnf install systemd```\n\n```pacman: sudo pacman -S systemd```\n\n```zypper: sudo zypper install systemd```\n\n```brew: brew install systemd```\n\n```nix: nix profile install nixpkgs#systemd```"],["SEE ALSO","[hostname](/man/hostname)(1), [systemctl](/man/systemctl)(1)"]],"e":["hostname","systemctl"],"x":"hostnamectl"},"hp-levels":{"n":"hp-levels","t":"Displays bar graphs of current ink or toner supply levels for HP printers","s":[["TAGLINE","Displays bar graphs of current ink or toner supply levels for HP printers"],["TLDR","**Show ink/toner levels** for the default/only installed printer\n\n```hp-levels```\n\n**Show levels for a specific CUPS printer**\n\n```hp-levels -p [printer-name]```\n\n**Show levels by device URI**\n\n```hp-levels -d [device-uri]```\n\n**Run interactively**, choosing the printer from a list\n\n```hp-levels -i```\n\n**Draw a wider bar graph** (default width is 51 characters)\n\n```hp-levels -s [80]```"],["SYNOPSIS","**hp-levels** [_DEVICE-URI_|_PRINTER-NAME_] [_MODE_] [_OPTIONS_]"],["PARAMETERS","**-d** _device-uri_, **--device**=_device-uri_\n> Specify the target printer by CUPS device URI.\n\n**-p** _printer_, **--printer**=_printer_\n> Specify the target printer by CUPS queue name.\n\n**-i**, **--interactive**\n> Run in interactive mode, prompting to choose a device.\n\n**-s** _size_, **--size**=_size_\n> Bar graph width in characters (default 51).\n\n**-c**, **--color**\n> Use colored bar graphs (colorized by default).\n\n**-a** _char_, **--char**=_char_\n> Character used to draw the bar graph (default **/**).\n\n**-l** _level_, **--logging**=_level_\n> Set logging verbosity: none, info (default), error, warn, or debug.\n\n**-g**\n> Run with debug-level logging (equivalent to **-l debug**).\n\n**-h**, **--help**\n> Display help and exit."],["DESCRIPTION","**hp-levels** queries an HPLIP-supported HP printer for its current ink or toner supply levels and renders them as text bar graphs in the terminal, one bar per installed cartridge or tank.\n\nPoint it at a printer with **-p** or **-d**; with no target given it uses the sole installed HPLIP device or prompts to choose one when several are configured. This is the CLI equivalent of the supply-levels panel in the HPLIP toolbox GUI."],["CAVEATS","Part of the HPLIP package. Level reporting accuracy and granularity vary by printer model; some cheaper models only report ok/low/out rather than a percentage, and a few report nothing at all."],["HISTORY","**hp-levels** is one of the supply-monitoring utilities in **HPLIP** (HP Linux Imaging and Printing), HP's open-source printer/scanner driver suite for Linux."],["INSTALL","```apt: sudo apt install hplip```\n\n```dnf: sudo dnf install hplip```\n\n```pacman: sudo pacman -S hplip```\n\n```zypper: sudo zypper install hplip```\n\n```nix: nix profile install nixpkgs#hplip```"],["SEE ALSO","[hp-info](/man/hp-info)(1), [hp-check](/man/hp-check)(1), [lpstat](/man/lpstat)(1)"],["RESOURCES","```[Source code](https://sourceforge.net/projects/hplip/)```\n\n```[Homepage](https://developers.hp.com/hp-linux-imaging-and-printing)```\n\n```[Documentation](https://developers.hp.com/hp-linux-imaging-and-printing/tech_docs/man_pages/index)```"]],"e":["hp-info","hp-check","lpstat"],"x":"hp-levels"},"jj-config":{"n":"jj-config","t":"manages Jujutsu configuration","s":[["TAGLINE","manages Jujutsu configuration"],["TLDR","**List all config values**\n\n```jj config list```\n\n**Get specific config value**\n\n```jj config get [user.name]```\n\n**Set config value**\n\n```jj config set --user [key] \"[value]\"```\n\n**Edit config in editor**\n\n```jj config edit --user```"],["SYNOPSIS","**jj** **config** _subcommand_ [_options_]"],["PARAMETERS","**list**\n> List configuration values.\n\n**get** _name_\n> Get a configuration value.\n\n**set** _name_ _value_\n> Set a configuration value.\n\n**unset** _name_\n> Remove a configuration value.\n\n**edit**\n> Edit configuration in editor.\n\n**path**\n> Show configuration file path."],["DESCRIPTION","**jj config** manages Jujutsu configuration. Supports user-level config (global) and repo-level config (per-repository). Configuration uses TOML format and controls settings like user identity, aliases, and UI preferences."],["INSTALL","```pacman: sudo pacman -S jujutsu```\n\n```apk: sudo apk add jujutsu```\n\n```zypper: sudo zypper install jujutsu```\n\n```brew: brew install jujutsu```\n\n```nix: nix profile install nixpkgs#jujutsu```"],["SEE ALSO","[jj](/man/jj)(1), [jj-config-list](/man/jj-config-list)(1), [jj-config-set](/man/jj-config-set)(1)"]],"e":["jj","jj-config-list","jj-config-set"],"x":"jj config list"},"kopia":{"n":"kopia","t":"fast, secure backup tool with encryption, deduplication, and compression","s":[["TAGLINE","fast, secure backup tool with encryption, deduplication, and compression"],["TLDR","**Create a local repository**\n\n```kopia repository create filesystem --path [/path/to/repo]```\n\n**Connect to an existing repository**\n\n```kopia repository connect filesystem --path [/path/to/repo]```\n\n**Create a backup snapshot**\n\n```kopia snapshot create [/path/to/backup]```\n\n**List all snapshots**\n\n```kopia snapshot list```\n\n**Restore a snapshot to a directory**\n\n```kopia restore [snapshot_id] [/path/to/restore]```\n\n**Mount snapshots as a FUSE filesystem**\n\n```kopia mount all [/mnt/kopia]```\n\n**Show repository status**\n\n```kopia repository status```\n\n**Set retention policy**\n\n```kopia policy set --keep-latest [10] --keep-daily [7] [/path/to/backup]```"],["SYNOPSIS","**kopia** _command_ [_options_]"],["PARAMETERS","**repository create** _type_\n> Create new repository (filesystem, s3, b2, gcs, azure, sftp, etc.).\n\n**repository connect** _type_\n> Connect to existing repository.\n\n**repository status**\n> Show repository connection status.\n\n**snapshot create** _path_\n> Create a backup snapshot.\n\n**snapshot list** [_path_]\n> List snapshots, optionally filtered by source.\n\n**restore** _id_ _target_\n> Restore snapshot to directory.\n\n**mount** _snapshots_ _mountpoint_\n> Mount snapshots as FUSE filesystem.\n\n**policy set** [_options_] [_path_]\n> Set backup and retention policies.\n\n**policy show** [_path_]\n> Show effective policy.\n\n**maintenance run**\n> Run repository maintenance.\n\n**server start**\n> Start Kopia server (UI and API).\n\n**--password** _pass_\n> Repository password.\n\n**--config-file** _path_\n> Use alternate config file."],["DESCRIPTION","**Kopia** is a fast, secure backup tool with encryption, deduplication, and compression. It supports multiple storage backends including local filesystem, cloud storage (S3, B2, GCS, Azure), and SFTP servers.\n\nBackups are encrypted client-side with AES-256-GCM before leaving your machine. Content-defined chunking enables efficient deduplication across all snapshots. Compression further reduces storage requirements.\n\nThe snapshot model provides point-in-time backups. Each snapshot captures directory state with metadata. Incremental backups only store changed data, but each snapshot is logically complete - you can restore any snapshot independently.\n\nPolicies control backup behavior per directory: compression, scheduling, retention (how many snapshots to keep), and file exclusions. Policies cascade from global to specific paths.\n\nThe optional server provides a web UI for browsing snapshots, monitoring backup status, and managing policies. It can also serve as a centralized backup server for multiple machines.\n\nMount functionality allows browsing snapshots as a filesystem, enabling file-level recovery without full restore."],["CAVEATS","Repository password cannot be recovered if lost - keep secure backups of credentials. Large initial backups take time. FUSE mount requires FUSE support. Some storage backends may have API costs. Deduplication effectiveness depends on data patterns."],["HISTORY","**Kopia** was created by Jarek Kowalski starting around **2019** as a modern backup solution. The design emphasized security (mandatory encryption), performance (Go implementation, parallelism), and flexibility (multiple backends). It gained popularity as an alternative to Restic and Borg, offering a web UI and cross-platform support."],["INSTALL","```zypper: sudo zypper install kopia```\n\n```brew: brew install kopia```\n\n```nix: nix profile install nixpkgs#kopia```"],["SEE ALSO","[restic](/man/restic)(1), [borg](/man/borg)(1), [duplicacy](/man/duplicacy)(1), [rclone](/man/rclone)(1)"]],"e":["restic","borg","duplicacy","rclone"],"x":"kopia repository create filesystem --path [/path/to/repo]"},"mdadm":{"n":"mdadm","t":"manages Linux software RAID arrays","s":[["TAGLINE","manages Linux software RAID arrays"],["TLDR","**Create** RAID array\n\n```sudo mdadm --create /dev/md/MyRAID --level raid1 --raid-devices 2 /dev/sda1 /dev/sdb1```\n\n**Stop** array\n\n```sudo mdadm --stop /dev/md0```\n\nMark disk **failed**\n\n```sudo mdadm --fail /dev/md0 /dev/sda1```\n\n**Remove** disk\n\n```sudo mdadm --remove /dev/md0 /dev/sda1```\n\n**Add** disk\n\n```sudo mdadm --assemble /dev/md0 /dev/sdc1```\n\nShow **details**\n\n```sudo mdadm --detail /dev/md0```\n\n**Clear** RAID metadata\n\n```sudo mdadm --zero-superblock /dev/sda1```"],["SYNOPSIS","**mdadm** [_mode_] [_OPTIONS_] _device_"],["DESCRIPTION","**mdadm** manages Linux software RAID arrays (md devices). It can create, assemble, monitor, grow, and manage arrays of various RAID levels including RAID 0, 1, 5, 6, and 10."],["PARAMETERS","**--create** _device_\n> Create a new array\n\n**--assemble** _device_\n> Assemble a previously created array\n\n**--stop** _device_\n> Stop an active array\n\n**--detail** _device_\n> Display detailed information about an array\n\n**--examine** _device_\n> Examine RAID superblock on a component device\n\n**--fail** _device_ _component_\n> Mark a component as failed\n\n**--remove** _device_ _component_\n> Remove a component from an array\n\n**--add** _device_ _component_\n> Add a component to an array\n\n**--level** _level_\n> RAID level (0, 1, 5, 6, 10, linear)\n\n**--raid-devices** _n_\n> Number of active devices in array\n\n**--zero-superblock** _device_\n> Erase RAID superblock from a device"],["CAVEATS","Creating an array destroys existing data on component devices. RAID is not a backup solution. RAID 5/6 require minimum 3/4 devices respectively. Always monitor arrays for failed drives."],["HISTORY","**mdadm** is the modern Linux software RAID management tool, replacing older raidtools."],["INSTALL","```apt: sudo apt install mdadm```\n\n```dnf: sudo dnf install mdadm```\n\n```pacman: sudo pacman -S mdadm```\n\n```apk: sudo apk add mdadm```\n\n```zypper: sudo zypper install mdadm```\n\n```nix: nix profile install nixpkgs#mdadm```"],["SEE ALSO","[lsblk](/man/lsblk)(8), [blkid](/man/blkid)(8)"]],"e":["lsblk","blkid"],"x":"sudo mdadm --create /dev/md/MyRAID --level raid1 --raid-devices 2 /dev/sda1 /dev/sdb1"},"muffin":{"n":"muffin","t":"Cinnamon desktop window manager and compositor","s":[["TAGLINE","Cinnamon desktop window manager and compositor"],["TLDR","**Start Muffin window manager**\n\n```muffin```\n\n**Replace running window manager**\n\n```muffin --replace```\n\n**Connect to a specific X display**\n\n```muffin --display=[display]```\n\n**Display version**\n\n```muffin --version```"],["SYNOPSIS","**muffin** [_options_]"],["PARAMETERS","**--replace**\n> Replace an already-running window manager with Muffin.\n\n**--display**=_DISPLAY_\n> Connect to the specified X display.\n\n**--sm-client-id**=_ID_\n> Specify a session management client ID.\n\n**--sm-disable**\n> Disable session management.\n\n**--sm-save-file**=_FILENAME_\n> Load a previously saved session from file.\n\n**--version**\n> Show version information.\n\n**-?**, **--help**\n> Display help information."],["DESCRIPTION","**muffin** is a Clutter-based compositing window manager for the Cinnamon desktop environment. It is a fork of GNOME's Mutter, aimed at nontechnical users and designed to integrate closely with the Cinnamon desktop.\n\nMuffin handles window placement, compositing, and basic window management. It uses GTK and intentionally omits some features found in traditional Unix window managers."],["CAVEATS","Cinnamon desktop specific. Requires X11 compositing support. GPU acceleration recommended. Does not support Wayland."],["HISTORY","muffin was forked from **GNOME's Mutter** for use with the Linux Mint Cinnamon desktop environment. It is maintained as part of the Cinnamon project."],["INSTALL","```apt: sudo apt install muffin```\n\n```dnf: sudo dnf install muffin```\n\n```pacman: sudo pacman -S muffin```\n\n```zypper: sudo zypper install muffin```\n\n```nix: nix profile install nixpkgs#muffin```"],["SEE ALSO","[mutter](/man/mutter)(1), [cinnamon](/man/cinnamon)(1), [kwin](/man/kwin)(1), [openbox](/man/openbox)(1)"]],"e":["mutter","cinnamon","kwin","openbox"],"x":"muffin"},"nmcli-device":{"n":"nmcli-device","t":"manages network interfaces using NetworkManager","s":[["TAGLINE","manages network interfaces using NetworkManager"],["TLDR","Show **status** of all network interfaces\n\n```nmcli device```\n\nList available **Wi-Fi networks**\n\n```nmcli device wifi```\n\n**Connect** to a Wi-Fi network (prompt for password)\n\n```nmcli device wifi connect [ssid] --ask```\n\nConnect to a **hidden** Wi-Fi network\n\n```nmcli device wifi connect [ssid] password [password] hidden yes```\n\nShow **password** and QR code for current Wi-Fi\n\n```nmcli device wifi show-password```\n\nShow **detailed information** about a device\n\n```nmcli device show [wlan0]```\n\n**Disconnect** a device\n\n```nmcli device disconnect [wlan0]```\n\n**Rescan** for available Wi-Fi networks\n\n```nmcli device wifi rescan```"],["SYNOPSIS","**nmcli device** {_status_|_show_|_set_|_connect_|_reapply_|_modify_|_disconnect_|_delete_|_monitor_|_wifi_|_lldp_} [_ARGUMENTS_]"],["DESCRIPTION","**nmcli device** manages network interfaces using NetworkManager. It can show device status, connect to networks, manage Wi-Fi connections, and control interface state."],["COMMANDS","**status**\n> Print status of all network devices\n\n**show [ifname]**\n> Display detailed information about a device\n\n**set ifname [property value]**\n> Set device properties (autoconnect, managed)\n\n**connect ifname**\n> Connect a device\n\n**reapply ifname**\n> Reapply connection settings to a device\n\n**disconnect ifname**\n> Disconnect a device\n\n**delete ifname**\n> Delete a software device\n\n**monitor [ifname]**\n> Monitor device activity\n\n**wifi list**\n> List available Wi-Fi access points\n\n**wifi connect ssid [password passwd] [hidden yes|no]**\n> Connect to a Wi-Fi network, optionally specifying password and hidden status\n\n**wifi rescan**\n> Request a Wi-Fi scan\n\n**wifi hotspot [ifname wlan0] [ssid name] [password passwd]**\n> Create a Wi-Fi hotspot\n\n**wifi show-password**\n> Show password for current Wi-Fi connection\n\n**lldp [list]**\n> Show LLDP neighbors"],["PARAMETERS","**-a, --ask**\n> Prompt for password when connecting\n\n**--rescan auto|no|yes**\n> Control Wi-Fi scanning before listing\n\n**-w, --wait seconds**\n> Timeout for finishing the operation\n\n**ifname**\n> Network interface name (eth0, wlan0, etc.)"],["CAVEATS","Wi-Fi operations require the NetworkManager daemon to be running and the device to be managed. Use **--ask** with wifi connect to interactively enter the password."],["HISTORY","Part of the **nmcli** command-line interface for **NetworkManager**. Provides device-level network management."],["INSTALL","```apt: sudo apt install network-manager```\n\n```apk: sudo apk add networkmanager-cli```"],["SEE ALSO","[nmcli](/man/nmcli)(1), [nmcli-connection](/man/nmcli-connection)(1), [nmcli-general](/man/nmcli-general)(1), [iw](/man/iw)(8), [iwctl](/man/iwctl)(1)"]],"e":["nmcli","nmcli-connection","nmcli-general","iw","iwctl"],"x":"nmcli device"},"openapi-tui":{"n":"openapi-tui","t":"TUI for browsing and running OpenAPI-defined APIs","s":[["TAGLINE","TUI for browsing and running OpenAPI-defined APIs"],["TLDR","**Open a local spec file**\n\n```openapi-tui -i [path/to/spec.yaml]```\n\n**Open a spec from URL**\n\n```openapi-tui -i [https://example.com/openapi.json]```"],["SYNOPSIS","**openapi-tui** [_options_]"],["PARAMETERS","**-i**, **--input** _PATH_OR_URL_\n> OpenAPI spec file path or URL (JSON or YAML)."],["DESCRIPTION","**openapi-tui** is a terminal UI for listing, browsing, and running APIs defined with OpenAPI v3.0 and v3.1 specifications. It supports loading specs from local files or URLs in both JSON and YAML formats, adding query strings and headers, and making API calls directly from the terminal."],["CAVEATS","Only supports OpenAPI v3.0 and v3.1 specifications. Swagger v2 specs must be converted first."],["HISTORY","**openapi-tui** was created by **zaghaghi** and is written in **Rust** using the ratatui library."],["INSTALL","```pacman: sudo pacman -S openapi-tui```\n\n```brew: brew install openapi-tui```\n\n```nix: nix profile install nixpkgs#openapi-tui```"],["SEE ALSO","[curl](/man/curl)(1), [httpie](/man/httpie)(1)"]],"e":["curl","httpie"],"x":"openapi-tui -i [path/to/spec.yaml]"},"optimus-manager":{"n":"optimus-manager","t":"manages GPU switching on laptops with Nvidia Optimus technology","s":[["TAGLINE","manages GPU switching on laptops with Nvidia Optimus technology"],["TLDR","**Switch** between different GPU modes\n\n```optimus-manager --switch [nvidia|integrated|hybrid]```\n\n**Clean up** configuration and state\n\n```optimus-manager --cleanup```"],["SYNOPSIS","**optimus-manager** [_options_]"],["PARAMETERS","**--switch _mode_**\n> Switch to specified GPU mode: nvidia, integrated, or hybrid\n\n**--cleanup**\n> Clean up leftover configuration from previous sessions\n\n**--status**\n> Display current GPU mode and configuration\n\n**--print-mode**\n> Print only the current mode name\n\n**--print-startup**\n> Print the startup mode from configuration\n\n**--no-confirm**\n> Don't ask for confirmation before switching"],["DESCRIPTION","**optimus-manager** manages GPU switching on laptops with Nvidia Optimus technology (dual Intel/Nvidia graphics). It handles the complex task of switching between the integrated Intel GPU, discrete Nvidia GPU, or hybrid mode.\n\nAvailable modes are:\n- **integrated**: Use only the Intel GPU (best battery life)\n- **nvidia**: Use only the Nvidia GPU (best performance)\n- **hybrid**: Both GPUs active, Nvidia for specific applications (via PRIME)\n\nSwitching modes typically requires logging out or rebooting. The tool manages Xorg configuration, kernel module loading, and display manager integration."],["CAVEATS","Switching GPUs requires logging out of the desktop session. Hybrid mode requires proper PRIME render offload configuration. Works with various display managers but may need configuration. Specific to Linux laptops with Nvidia Optimus hardware."],["SEE ALSO","[nvidia-smi](/man/nvidia-smi)(1), [prime-run](/man/prime-run)(1), [envycontrol](/man/envycontrol)(1)"]],"e":["nvidia-smi","prime-run","envycontrol"],"x":"optimus-manager --switch [nvidia|integrated|hybrid]"},"php-artisan":{"n":"php-artisan","t":"laravel framework's command line interface","s":[["TAGLINE","laravel framework's command line interface"],["TLDR","**List commands**\n\n```php artisan list```\n\n**Start development server**\n\n```php artisan serve```\n\n**Run migrations**\n\n```php artisan migrate```\n\n**Generate controller**\n\n```php artisan make:controller [UserController]```\n\n**Generate model with migration**\n\n```php artisan make:model [User] -m```\n\n**Clear cache**\n\n```php artisan cache:clear```\n\n**Generate migration**\n\n```php artisan make:migration [create_users_table]```\n\n**Rollback last migration**\n\n```php artisan migrate:rollback```\n\n**List all routes**\n\n```php artisan route:list```\n\n**Start interactive shell (tinker)**\n\n```php artisan tinker```\n\n**Cache configuration**\n\n```php artisan config:cache```"],["SYNOPSIS","**php artisan** _command_ [_options_] [_arguments_]"],["PARAMETERS","**serve**\n> Start development server.\n\n**migrate**\n> Run database migrations.\n\n**make:controller** _name_\n> Generate controller.\n\n**make:model** _name_\n> Generate model.\n\n**make:migration** _name_\n> Generate migration.\n\n**cache:clear**\n> Clear application cache.\n\n**config:cache**\n> Cache configuration.\n\n**route:list**\n> List all routes.\n\n**tinker**\n> Start interactive shell.\n\n**queue:work**\n> Start processing jobs on the queue.\n\n**schedule:run**\n> Run the scheduled commands.\n\n**migrate:rollback**\n> Rollback the last database migration.\n\n**migrate:fresh**\n> Drop all tables and re-run all migrations.\n\n**make:middleware** _name_\n> Generate middleware class.\n\n**make:seeder** _name_\n> Generate database seeder class.\n\n**-v**, **-vv**, **-vvv**\n> Increase verbosity of output.\n\n**--env** _ENV_\n> The environment the command should run under."],["DESCRIPTION","**php artisan** is the Laravel framework's command line interface. Provides commands for common development tasks like code generation, database migrations, caching, and server management."],["INSTALL","```dnf: sudo dnf install php```\n\n```pacman: sudo pacman -S php```\n\n```apk: sudo apk add php85```\n\n```brew: brew install php```\n\n```nix: nix profile install nixpkgs#php85```"],["SEE ALSO","[php](/man/php)(1), [composer](/man/composer)(1), [laravel](/man/laravel)(1)"]],"e":["php","composer","laravel"],"x":"php artisan list"},"pip-freeze":{"n":"pip-freeze","t":"Output installed packages in requirements format","s":[["TAGLINE","Output installed packages in requirements format"],["TLDR","**List installed packages**\n\n```pip freeze```\n\n**Output to requirements file**\n\n```pip freeze > requirements.txt```\n\n**Include all packages including pip and setuptools**\n\n```pip freeze --all```\n\n**Exclude editable packages**\n\n```pip freeze --exclude-editable```\n\n**Exclude specific packages**\n\n```pip freeze --exclude [package]```\n\n**Only list packages in the current virtualenv**\n\n```pip freeze --local```"],["SYNOPSIS","**pip** **freeze** [_options_]"],["PARAMETERS","**-r**, **--requirement** _file_\n> Use requirements file order.\n\n**-l**, **--local**\n> Only show virtualenv packages.\n\n**--user**\n> Only show user packages.\n\n**--exclude** _package_\n> Exclude package from output.\n\n**--all**\n> Include all packages, even pip and setuptools that are normally excluded.\n\n**--exclude-editable**\n> Exclude editable packages.\n\n**--path** _directory_\n> Restrict to the specified installation path for listing packages.\n\n**-q**, **--quiet**\n> Decrease output verbosity."],["DESCRIPTION","**pip freeze** outputs installed packages in requirements format. Each package is shown with its exact version (package==version). This is the standard method for capturing Python environment dependencies for reproducibility. By default, pip, setuptools, wheel, and distribute are excluded from the output; use **--all** to include them. Packages are listed in case-insensitive sorted order."],["INSTALL","```apk: sudo apk add py3-pip```"],["SEE ALSO","[pip](/man/pip)(1), [pip-list](/man/pip-list)(1), [pip-install](/man/pip-install)(1), [pip-show](/man/pip-show)(1)"]],"e":["pip","pip-list","pip-install","pip-show"],"x":"pip freeze"},"pkgrm":{"n":"pkgrm","t":"Remove installed packages on CRUX Linux","s":[["TAGLINE","Remove installed packages on CRUX Linux"],["TLDR","Uninstall a package that was installed with **pkgadd**\n\n```pkgrm [package]```"],["SYNOPSIS","**pkgrm** [**-r** _root_] _package_"],["PARAMETERS","**-r _root_**\n> Use alternate root directory\n\n**package**\n> Name of package to remove"],["DESCRIPTION","**pkgrm** removes installed packages from a CRUX Linux system. It deletes all files belonging to the package and removes the package entry from the system database.\n\nThe tool does not check for dependencies, so removing packages that other software depends on may break the system. Users must manage dependencies manually."],["CAVEATS","No dependency checking. Files modified after installation are not removed. Directories are only removed if empty. Use pkginfo to check package contents before removal."],["HISTORY","**pkgrm** is part of **pkgutils**, the native package management system for CRUX Linux. It follows CRUX's philosophy of simple, transparent tools that trust the user to manage their system."],["SEE ALSO","[pkgadd](/man/pkgadd)(8), [pkginfo](/man/pkginfo)(1), [pkgmk](/man/pkgmk)(8)"]],"e":["pkgadd","pkginfo","pkgmk"],"x":"pkgrm [package]"},"podman-login":{"n":"podman-login","t":"Authenticate with container registries","s":[["TAGLINE","Authenticate with container registries"],["TLDR","**Login to Docker Hub**\n\n```podman login docker.io```\n\n**Login to specific registry**\n\n```podman login [registry.example.com]```\n\n**Login with credentials**\n\n```podman login -u [username] -p [password] [registry]```\n\n**Login with stdin password**\n\n```echo [password] | podman login -u [user] --password-stdin [registry]```\n\n**Check which user is logged in** to a registry\n\n```podman login --get-login [registry]```\n\n**Login using a TLS client certificate**\n\n```podman login --cert-dir [/path/to/certs] [registry]```"],["SYNOPSIS","**podman** **login** [_options_] _registry_"],["PARAMETERS","**-u**, **--username** _name_\n> Registry username.\n\n**-p**, **--password** _pass_\n> Registry password.\n\n**--password-stdin**\n> Read password from stdin.\n\n**--tls-verify**\n> Require HTTPS and verify TLS certificates when contacting registries (default: **true**).\n\n**--authfile** _file_\n> Path to the authentication file (default: `${XDG_RUNTIME_DIR}/containers/auth.json`).\n\n**--cert-dir** _path_\n> Use TLS certificates at _path_ (*.crt, *.cert, *.key) to connect to the registry.\n\n**--compat-auth-file** _path_\n> Write credentials to _path_ in Docker-compatible format.\n\n**--get-login**\n> Return the logged-in user for the registry. Returns an error if no login is found.\n\n**--secret** _name_\n> Read the password from a podman secret.\n\n**-v**, **--verbose**\n> Print detailed information about the credential store."],["DESCRIPTION","**podman login** authenticates with container registries. Credentials are stored in an auth file (default: `${XDG_RUNTIME_DIR}/containers/auth.json`) for subsequent pulls and pushes. If no registry is specified, the first registry under `[registries.search]` from `registries.conf` is used.\n\nSupports Docker Hub, Quay.io, GitHub Container Registry, and private registries."],["INSTALL","```apt: sudo apt install podman```\n\n```dnf: sudo dnf install podman```\n\n```pacman: sudo pacman -S podman```\n\n```apk: sudo apk add podman```\n\n```zypper: sudo zypper install podman```\n\n```brew: brew install podman```\n\n```nix: nix profile install nixpkgs#podman```"],["SEE ALSO","[podman](/man/podman)(1), [podman-pull](/man/podman-pull)(1), [docker-login](/man/docker-login)(1)"]],"e":["podman","podman-pull","docker-login"],"x":"podman login docker.io"},"poetry-export":{"n":"poetry-export","t":"Export Poetry dependencies to other formats","s":[["TAGLINE","Export Poetry dependencies to other formats"],["TLDR","**Export to requirements.txt**\n\n```poetry export -f requirements.txt -o requirements.txt```\n\n**Export with dev dependencies**\n\n```poetry export --with dev -f requirements.txt```\n\n**Export without hashes**\n\n```poetry export --without-hashes -o requirements.txt```\n\n**Export only specific dependency groups**\n\n```poetry export --only [main,docs] -f requirements.txt```\n\n**Export to standard output**\n\n```poetry export -f requirements.txt```\n\n**Export including extras**\n\n```poetry export -f requirements.txt -E [extra_name]```"],["SYNOPSIS","**poetry** **export** [_options_]"],["PARAMETERS","**-f**, **--format** _format_\n> Output format (requirements.txt, constraints.txt, pylock.toml).\n\n**-o**, **--output** _file_\n> Output file path. If omitted, prints to standard output.\n\n**--with** _groups_\n> Include optional dependency groups.\n\n**--without** _groups_\n> Exclude dependency groups.\n\n**--only** _groups_\n> Include only the specified dependency groups.\n\n**--without-hashes**\n> Exclude hashes from output.\n\n**--without-urls**\n> Exclude source URLs from output.\n\n**-E**, **--extras** _extras_\n> Include extras."],["DESCRIPTION","**poetry export** exports the lock file to other formats. It is provided by the **poetry-plugin-export** plugin. Primarily used to generate requirements.txt files for environments that don't use Poetry directly, such as Docker builds or production deployments."],["CAVEATS","Requires the **poetry-plugin-export** plugin to be installed. The `--dev` flag is deprecated in favor of `--with dev`."],["INSTALL","```dnf: sudo dnf install poetry```\n\n```apk: sudo apk add poetry```\n\n```brew: brew install poetry```\n\n```nix: nix profile install nixpkgs#poetry```"],["SEE ALSO","[poetry](/man/poetry)(1), [poetry-lock](/man/poetry-lock)(1), [poetry-install](/man/poetry-install)(1), [pip](/man/pip)(1)"]],"e":["poetry","poetry-lock","poetry-install","pip"],"x":"poetry export -f requirements.txt -o requirements.txt"},"portablemc":{"n":"portablemc","t":"Cross-platform command-line Minecraft launcher","s":[["TAGLINE","Cross-platform command-line Minecraft launcher"],["TLDR","**Start** the latest release\n\n```portablemc start```\n\n**Start** a specific version\n\n```portablemc start [1.16.5]```\n\n**Search** available versions\n\n```portablemc search```\n\n**Search** release channel, limit results\n\n```portablemc search --channel release -l[10]```\n\n**Start offline** with a username\n\n```portablemc start -u [MyUsername]```\n\n**Log in** with a Microsoft account\n\n```portablemc auth login```\n\n**List** authenticated accounts\n\n```portablemc auth list```\n\n**Start with** an authenticated account\n\n```portablemc start -u [username] -a```"],["SYNOPSIS","**portablemc** *command* [*options*] [*args*]"],["DESCRIPTION","**portablemc** is a cross-platform command-line utility for installing and launching Minecraft quickly and reliably. It supports official Mojang versions and installs popular mod loaders seamlessly, including Forge, NeoForge, Fabric, Quilt, LegacyFabric, and Babric.\n\nIt can run offline or with a Microsoft account, browse supported versions, download game files in parallel, locate a compatible system Java runtime (falling back to Mojang-provided runtimes when needed), and apply known launch fixes. Output verbosity and machine-readable modes are available for scripting.\n\nInstall via release binaries, **cargo install portablemc-cli**, AUR packages (**portablemc** / **portablemc-bin**), or nixpkgs."],["PARAMETERS","**start** [*version*]\n\n> Install (if needed) and launch a game version. **\\-u** sets the username; **\\-a** uses an authenticated account. See **portablemc start --help** for JVM, directory, and loader options.\n\n**search**\n\n> List or search supported versions. **--channel** filters (for example **release**); **-l** limits result count.\n\n**auth login**\n\n> Authenticate a Microsoft account for online play.\n\n**auth list**\n\n> List stored authenticated accounts.\n\nUse **portablemc --help** and subcommand **--help** for the full flag set of your installed version."],["CAVEATS","Online play requires a valid Microsoft account flow. Some unsupported architectures may need extra flags. Mod loader installs depend on upstream loader availability. Network access is required for first-time downloads."],["INSTALL","```nix: nix profile install nixpkgs#portablemc```"],["SEE ALSO","[java](/man/java)(1), [minecraft](/man/minecraft)(1)"],["RESOURCES","```[Source code](https://github.com/theorzr/portablemc)```\n\n```[Documentation](https://docs.rs/portablemc/latest/portablemc)```"]],"e":["java","minecraft"],"x":"portablemc start"},"print.runmailcap":{"n":"print.runmailcap","t":"Print files using MIME type handlers","s":[["TAGLINE","Print files using MIME type handlers"],["TLDR","**Print** any file using default run-mailcap tool\n\n```print filename```\n\nPrint using **run-mailcap** directly\n\n```run-mailcap --action=print filename```"],["SYNOPSIS","**print** _file_"],["DESCRIPTION","**print** is an alias for **run-mailcap** that defaults to the `print` action. It processes files based on their MIME type and sends them to the appropriate print handler defined in the mailcap configuration (typically `/etc/mailcap` and `~/.mailcap`).\n\nThe related aliases **see**, **edit**, and **compose** default to the view, edit, and compose actions respectively. All of them accept the same options as run-mailcap, including `--action`, `--debug`, `--nopager`, and `--norun`."],["CAVEATS","Requires proper mailcap configuration for the file's MIME type. Print behavior depends on system print configuration."],["HISTORY","Part of the **mime-support** package, providing MIME type handling utilities for Unix-like systems."],["SEE ALSO","[run-mailcap](/man/run-mailcap)(1), [lpr](/man/lpr)(1)"]],"e":["run-mailcap","lpr"],"x":"print filename"},"pueue-parallel":{"n":"pueue-parallel","t":"Set pueue task concurrency level","s":[["TAGLINE","Set pueue task concurrency level"],["TLDR","**Set parallel tasks**\n\n```pueue parallel [4]```\n\n**Set for specific group**\n\n```pueue parallel --group [group_name] [2]```\n\n**Unlimited parallel**\n\n```pueue parallel 0```\n\n**Single task at a time**\n\n```pueue parallel 1```"],["SYNOPSIS","**pueue parallel** [**-g** _group_] [_amount_]"],["PARAMETERS","_amount_\n> Number of tasks allowed to run in parallel in the target group. A value of **0** means unlimited. Omit _amount_ to query the current value.\n\n**-g**, **--group** _NAME_\n> Apply the setting to the named group instead of the **default** group."],["DESCRIPTION","**pueue parallel** sets how many tasks can run simultaneously in a pueue group. Each group has its own independent parallelism limit. If no _amount_ is given, pueue prints the group's current value.\n\nChanges only affect scheduling of new tasks — already-running tasks are never stopped when the limit is lowered."],["CAVEATS","Setting 0 disables the concurrency limit entirely. Use **pueue group** to list groups and **pueue status** to view current limits."],["HISTORY","pueue parallel is part of **pueue** by **Arne Beer**, providing task queue management with parallelism control."],["INSTALL","```pacman: sudo pacman -S pueue```\n\n```apk: sudo apk add pueue```\n\n```zypper: sudo zypper install pueue```\n\n```brew: brew install pueue```\n\n```nix: nix profile install nixpkgs#pueue```"],["SEE ALSO","[pueue](/man/pueue)(1), [pueue-add](/man/pueue-add)(1), [pueue-group](/man/pueue-group)(1), [pueue-status](/man/pueue-status)(1)"]],"e":["pueue","pueue-add","pueue-group","pueue-status"],"x":"pueue parallel [4]"},"pueue-start":{"n":"pueue-start","t":"Resume paused pueue tasks or groups","s":[["TAGLINE","Resume paused pueue tasks or groups"],["TLDR","**Start paused task**\n\n```pueue start [task_id]```\n\n**Start all tasks**\n\n```pueue start --all```\n\n**Start group**\n\n```pueue start --group [group_name]```\n\n**Start multiple specific tasks**\n\n```pueue start [task_id_1] [task_id_2] [task_id_3]```"],["SYNOPSIS","**pueue start** [_options_] [_task_ids_]"],["PARAMETERS","**--all**, **-a**\n> Start all tasks.\n\n**--group**, **-g** _name_\n> Start specific group."],["DESCRIPTION","**pueue start** resumes paused tasks or groups in the pueue task queue. Tasks that were paused with pueue pause can be resumed to continue execution."],["CAVEATS","Tasks must be in a paused or queued state. Use **pueue status** to check task states before starting. The pueue daemon must be running."],["HISTORY","**pueue start** is part of **pueue** by **Arne Beer**, a command-line task manager for sequential and parallel execution."],["INSTALL","```pacman: sudo pacman -S pueue```\n\n```apk: sudo apk add pueue```\n\n```zypper: sudo zypper install pueue```\n\n```brew: brew install pueue```\n\n```nix: nix profile install nixpkgs#pueue```"],["SEE ALSO","[pueue](/man/pueue)(1), [pueue-pause](/man/pueue-pause)(1), [pueue-add](/man/pueue-add)(1), [pueue-status](/man/pueue-status)(1)"]],"e":["pueue","pueue-pause","pueue-add","pueue-status"],"x":"pueue start [task_id]"},"pw-cat":{"n":"pw-cat","t":"Play and record audio through PipeWire","s":[["TAGLINE","Play and record audio through PipeWire"],["TLDR","**Play** a WAV file\n\n```pw-cat -p [path/to/file.wav]```\n\nPlay with specific **resampler quality**\n\n```pw-cat -q [8] -p [path/to/file.wav]```\n\n**Record** to file with volume adjustment\n\n```pw-cat -r --volume [1.25] [path/to/file.wav]```\n\nRecord with **custom sample rate**\n\n```pw-cat -r --rate [48000] [path/to/file.wav]```\n\nDisplay **help**\n\n```pw-cat -h```"],["SYNOPSIS","**pw-cat** [**-p**|**-r**] [**-q** _quality_] [**--volume** _vol_] [**--rate** _rate_] _file_"],["PARAMETERS","**-p, --playback**\n> Play audio file\n\n**-r, --record**\n> Record to audio file\n\n**-q, --quality _level_**\n> Resampler quality (0-15, default 4)\n\n**--volume _level_**\n> Volume level (1.0 = 100%)\n\n**--rate _hz_**\n> Sample rate in Hz\n\n**--channels _num_**\n> Number of channels\n\n**--target _target_**\n> Target node to connect to"],["DESCRIPTION","**pw-cat** plays and records audio files through PipeWire. It provides a simple command-line interface for audio playback and recording similar to pacat for PulseAudio.\n\nThe tool supports various audio formats and can connect to specific PipeWire nodes. It's useful for testing audio setup and simple recording tasks."],["CAVEATS","Requires running PipeWire instance. Limited format support compared to full audio players. Use pw-play and pw-record as simpler aliases."],["HISTORY","**pw-cat** is part of the **PipeWire** multimedia framework. It provides similar functionality to PulseAudio's pacat tool for the PipeWire ecosystem."],["INSTALL","```apt: sudo apt install pipewire-bin```\n\n```apk: sudo apk add pipewire-tools```\n\n```zypper: sudo zypper install pipewire-tools```"],["SEE ALSO","[pw-play](/man/pw-play)(1), [pw-record](/man/pw-record)(1), [pw-cli](/man/pw-cli)(1), [wpctl](/man/wpctl)(1)"]],"e":["pw-play","pw-record","pw-cli","wpctl"],"x":"pw-cat -p [path/to/file.wav]"},"quotaon":{"n":"quotaon","t":"Enable disk quota enforcement on filesystems","s":[["TAGLINE","Enable disk quota enforcement on filesystems"],["TLDR","**Enable quotas on filesystem**\n\n```sudo quotaon [/home]```\n\n**Enable all quotas**\n\n```sudo quotaon -a```\n\n**Enable user quotas only**\n\n```sudo quotaon -u [/home]```\n\n**Enable group quotas only**\n\n```sudo quotaon -g [/home]```\n\n**Verbose output**\n\n```sudo quotaon -v [/home]```"],["SYNOPSIS","**quotaon** [_options_] [_filesystem_...]"],["PARAMETERS","**-a**\n> Enable all filesystems in /etc/fstab.\n\n**-u**\n> Enable user quotas.\n\n**-g**\n> Enable group quotas.\n\n**-v**\n> Verbose mode.\n\n**-p**\n> Print status only.\n\n**-f**\n> Force enable."],["DESCRIPTION","**quotaon** enables disk quota enforcement on filesystems. Quotas must first be configured in /etc/fstab and quota files created with quotacheck."],["EXAMPLES","```bash"],["Enable all configured quotas","sudo quotaon -a"],["Enable on specific filesystem","sudo quotaon /home"],["Enable user quotas only","sudo quotaon -u /home"],["Check what would be enabled","sudo quotaon -p /home"],["Enable with verbose output","sudo quotaon -vug /home\n```"],["SETUP WORKFLOW","```bash"],["2. Remount filesystem","mount -o remount /home"],["3. Create quota files","quotacheck -cug /home"],["4. Enable quotas","quotaon /home"],["5. Set user limits","edquota -u username\n```"],["CAVEATS","Requires quota support in kernel and filesystem. Must run quotacheck first. Requires root privileges."],["HISTORY","Disk quotas originated in **BSD Unix** and were adopted by Linux for managing disk space allocation."],["INSTALL","```apt: sudo apt install quota```\n\n```dnf: sudo dnf install quota```\n\n```pacman: sudo pacman -S quota-tools```\n\n```apk: sudo apk add quota-tools```\n\n```zypper: sudo zypper install quota```\n\n```nix: nix profile install nixpkgs#quota```"],["SEE ALSO","[quotacheck](/man/quotacheck)(8), [edquota](/man/edquota)(8), [repquota](/man/repquota)(8)"]],"e":["quotacheck","edquota","repquota"],"x":"sudo quotaon [/home]"},"rails-new":{"n":"rails-new","t":"Create a new Ruby on Rails application","s":[["TAGLINE","Create a new Ruby on Rails application"],["TLDR","**Create a new Rails application**\n\n```rails new [app_name]```\n\n**Create an application with PostgreSQL** as the database\n\n```rails new [app_name] --database=postgresql```\n\n**Create an application with MySQL**\n\n```rails new [app_name] --database=mysql```\n\n**Create an API-only application** (no views or assets)\n\n```rails new [app_name] --api```\n\n**Create an application without Action Mailer**\n\n```rails new [app_name] --skip-action-mailer```\n\n**Create an application with a specific Rails version**\n\n```rails _[version]_ new [app_name]```\n\n**Create an application in the current directory**\n\n```rails new .```"],["SYNOPSIS","**rails new** _app_name_ [**--database**=_adapter_] [**--api**] [**--skip-***] [_options_]"],["PARAMETERS","**--database**, **-d** _adapter_\n> Preconfigure for selected database (sqlite3, mysql, postgresql, oracle, sqlserver, etc.)\n\n**--api**\n> Create an API-only application (smaller middleware stack, no views)\n\n**--skip-git**\n> Skip git init and .gitignore generation\n\n**--skip-docker**\n> Skip Dockerfile and related files\n\n**--skip-action-mailer**\n> Skip Action Mailer files\n\n**--skip-action-mailbox**\n> Skip Action Mailbox gem\n\n**--skip-action-text**\n> Skip Action Text gem\n\n**--skip-active-record**\n> Skip Active Record files (for non-database apps)\n\n**--skip-active-storage**\n> Skip Active Storage files\n\n**--skip-action-cable**\n> Skip Action Cable files\n\n**--skip-asset-pipeline**\n> Skip asset pipeline\n\n**--skip-javascript**\n> Skip JavaScript files\n\n**--skip-hotwire**\n> Skip Hotwire integration\n\n**--skip-jbuilder**\n> Skip jbuilder gem\n\n**--skip-test**\n> Skip test files\n\n**--skip-bundle**\n> Don't run bundle install\n\n**--css** _processor_\n> Choose CSS processor (tailwind, bootstrap, bulma, postcss, sass)\n\n**--javascript** _bundler_\n> Choose JavaScript bundler (importmap, bun, webpack, esbuild, rollup)\n\n**--template**, **-m** _path_\n> Apply an application template from a path or URL\n\n**--force**, **-f**\n> Overwrite files that already exist\n\n**--pretend**, **-p**\n> Run but do not make any changes\n\n**--quiet**, **-q**\n> Suppress status output"],["DESCRIPTION","**rails new** creates a new Ruby on Rails application with the standard directory structure, configuration files, and dependencies. It generates everything needed to start developing a web application immediately.\n\nThe command creates directories for models, views, controllers, tests, and configurations. It initializes a Gemfile with common dependencies, sets up database configuration, and optionally initializes a git repository.\n\nRails applications follow convention over configuration. The generated structure establishes conventions that other Rails commands and the framework expect, enabling features like automatic routing and model discovery."],["CAVEATS","The **rails new** command is typically run outside of any existing Rails application directory. Running it inside an existing Rails app may cause conflicts.\n\nThe default database is SQLite, which is suitable for development but not recommended for production. Specify a production-ready database with **--database** when creating applications intended for deployment.\n\nBundle install runs automatically unless **--skip-bundle** is specified. This can take time on first run as gems are downloaded and compiled."],["INSTALL","```nix: nix profile install nixpkgs#rails-new```"],["SEE ALSO","[rails](/man/rails)(1), [bundle](/man/bundle)(1), [gem](/man/gem)(1), [ruby](/man/ruby)(1)"]],"e":["rails","bundle","gem","ruby"],"x":"rails new [app_name]"},"recfmt":{"n":"recfmt","t":"Format GNU recutils records with templates","s":[["TAGLINE","Format GNU recutils records with templates"],["TLDR","**Format records with inline template**\n\n```recsel [file.rec] | recfmt \"{{Name}}: {{Email}}\"```\n\n**Format records using a template file**\n\n```recsel [file.rec] | recfmt -f [template.fmt]```\n\n**Select and format specific record type**\n\n```recsel -t [Type] [file.rec] | recfmt \"{{Title}} by {{Author}}\"```"],["SYNOPSIS","**recfmt** [_options_] [_template_]"],["PARAMETERS","**-f**, **--file** _FILENAME_\n> Load the template from a file instead of a command-line argument.\n\n**--help**\n> Print help and exit.\n\n**--version**\n> Show version and exit."],["DESCRIPTION","**recfmt** applies a template to records read from standard input. Fields are referenced using double-brace slots like **{{FieldName}}**. For each input record, one copy of the template is generated with field values substituted. Typically used by piping output from **recsel** into **recfmt**.\n\nPart of GNU recutils."],["EXAMPLES","```bash"],["Simple format from recsel output","recsel contacts.rec | recfmt \"Name: {{Name}}, Email: {{Email}}\""],["From template file","recsel data.rec | recfmt -f report.fmt"],["Specific record type with selection","recsel -t Book library.rec | recfmt \"{{Title}} by {{Author}}\""],["Combine with expression filtering","recsel -e \"Status = 'active'\" items.rec | recfmt \"- {{Name}}\"\n```"],["TEMPLATE SYNTAX","```\n{{FieldName}}          - Substituted with field value\n```"],["TEMPLATE FILE","```"],["report.fmt","=== {{Name}} ===\nEmail: {{Email}}\nPhone: {{Phone}}\n---\n```"],["CAVEATS","Template syntax differs from other templating systems. Part of GNU recutils."],["HISTORY","recfmt is part of **GNU recutils** by **Jose E. Marchesi** for flexible record formatting."],["INSTALL","```dnf: sudo dnf install recutils```\n\n```apk: sudo apk add recutils```\n\n```brew: brew install recutils```\n\n```nix: nix profile install nixpkgs#recutils```"],["SEE ALSO","[recsel](/man/recsel)(1), [rec2csv](/man/rec2csv)(1), [recins](/man/recins)(1), [recdel](/man/recdel)(1), [recset](/man/recset)(1)"]],"e":["recsel","rec2csv","recins","recdel","recset"],"x":"recsel [file.rec] | recfmt \"{{Name}}: {{Email}}\""},"reportbug":{"n":"reportbug","t":"Report bugs in Debian packages","s":[["TAGLINE","Report bugs in Debian packages"],["TLDR","Generate a **bug report** about a package\n\n```reportbug package```\n\nReport a bug about **general problems** (not package-specific)\n\n```reportbug other```\n\nWrite bug report to a **file** instead of emailing\n\n```reportbug -o filename package```\n\n**Query existing bugs** without filing a report\n\n```reportbug --query-only [package]```\n\nReport a bug with specific **severity**\n\n```reportbug --severity [serious] [package]```\n\n**Attach a file** to the bug report\n\n```reportbug --attach [screenshot.png] [package]```"],["SYNOPSIS","**reportbug** [_options_] _package_"],["PARAMETERS","**-o**, **--output** _file_\n> Write bug report to file instead of sending by email.\n\n**--severity** _level_\n> Set bug severity: critical, grave, serious, important, normal, minor, wishlist.\n\n**--query-only**\n> Query existing bugs without filing a report.\n\n**--attach** _file_\n> Attach a file to the bug report.\n\n**--include** _file_\n> Include file contents in the message body.\n\n**--tag** _tag_\n> Add a tag to the report.\n\n**--email** _address_\n> Set sender email address.\n\n**--smtphost** _host_\n> Use specified SMTP server.\n\n**--mode** _mode_\n> Operating mode: novice, standard, advanced, expert.\n\n**--template**\n> Output a template report to stdout."],["DESCRIPTION","**reportbug** is the official bug reporting tool for Debian-based distributions. It generates bug reports with system information and sends them to the Debian Bug Tracking System via email.\n\nThe tool guides users through providing necessary information about the bug and the affected package."],["CAVEATS","Requires email configuration for sending reports. Use \"other\" as package name for infrastructure or general issues."],["HISTORY","Part of **Debian** quality assurance infrastructure. Provides standardized bug reporting for the Debian ecosystem."],["INSTALL","```aur: yay -S reportbug```"],["SEE ALSO","[apt](/man/apt)(8), [dpkg](/man/dpkg)(1)"]],"e":["apt","dpkg"],"x":"reportbug package"},"rlogin":{"n":"rlogin","t":"Remote login to another host","s":[["TAGLINE","Remote login to another host"],["TLDR","**Log in** to a remote host\n\n```rlogin [remote_host]```\n\n**Log in with a specific username**\n\n```rlogin -l [username] [remote_host]```\n\n**Use a custom escape character** instead of the default tilde\n\n```rlogin -e [^] [remote_host]```\n\n**Force IPv4 connection**\n\n```rlogin -4 [remote_host]```\n\n**Allow 8-bit data** (for non-ASCII locales)\n\n```rlogin -8 [remote_host]```"],["SYNOPSIS","**rlogin** [**-468DEd**] [**-e** _char_] [**-i** _localname_] [**-l** _username_] _host_"],["PARAMETERS","**-4**\n> Use IPv4 addresses only.\n\n**-6**\n> Use IPv6 addresses only.\n\n**-8**\n> Allow an eight-bit input data path at all times; otherwise parity bits are stripped.\n\n**-D**\n> Set the TCP_NODELAY socket option to improve interactive response.\n\n**-d**\n> Turn on socket debugging (SO_DEBUG) on the TCP sockets used for communication.\n\n**-E**\n> Stop any character from being recognized as an escape character.\n\n**-e** _char_\n> Set the escape character (default: **~**). A literal character, or \\\\nnn for octal.\n\n**-i** _localname_\n> Specify a different local name to be used for authentication.\n\n**-l** _username_\n> Specify a different username for the remote login (default: current user)."],["DESCRIPTION","**rlogin** starts a terminal session on a remote host using Berkeley's rhosts authorization mechanism. It reads **~/.rhosts** and **/etc/hosts.equiv** to decide whether to permit passwordless login from trusted accounts.\n\nThe escape character (default **~**), when typed as the first character on a line, enables special actions: **~.** disconnects, **~^Z** suspends the session, and **~~** sends a literal tilde. The remote terminal type and window size are propagated from the local environment."],["CAVEATS","**Deprecated and insecure**: rlogin transmits credentials, commands, and session data in plaintext over the network, making it trivial to sniff passwords and hijack sessions. The rhosts trust mechanism is also vulnerable to spoofing. Most modern distributions ship it disabled or omit it entirely. **Use [ssh](/man/ssh)(1) instead** for any remote login purpose."],["HISTORY","Originated in **4.2BSD** (1983) as part of the Berkeley \"r-commands\" suite (**rlogin**, **rsh**, **rcp**). Widely deployed throughout the 1980s and 1990s on Unix networks before being superseded by **ssh** in the late 1990s due to its lack of encryption. On Linux it is typically provided by **GNU inetutils**."],["SEE ALSO","[ssh](/man/ssh)(1), [rsh](/man/rsh)(1), [rcp](/man/rcp)(1), [rexec](/man/rexec)(1), [telnet](/man/telnet)(1)"]],"e":["ssh","rsh","rcp","rexec","telnet"],"x":"rlogin [remote_host]"},"rlwrap":{"n":"rlwrap","t":"Add readline editing to any command","s":[["TAGLINE","Add readline editing to any command"],["TLDR","**Add readline to command**\n\n```rlwrap [command]```\n\n**With history file**\n\n```rlwrap -H [~/.command_history] [command]```\n\n**With completion file**\n\n```rlwrap -f [completions.txt] [command]```\n\n**Always readline** (even if terminal)\n\n```rlwrap -a [command]```\n\n**Remember multi-line input**\n\n```rlwrap -m [command]```\n\n**Quote special characters**\n\n```rlwrap -q '\"' [command]```\n\n**Case-insensitive completion**\n\n```rlwrap -i [command]```"],["SYNOPSIS","**rlwrap** [_-a_] [_-H file_] [_-f file_] [_-i_] [_options_] _command_ [_args_]"],["PARAMETERS","**-a**, **--always-readline**\n> Always use readline.\n\n**-H** _FILE_\n> History file.\n\n**-f** _FILE_\n> Completions file.\n\n**-i**, **--case-insensitive**\n> Case-insensitive completion.\n\n**-m** [_CHAR_]\n> Multi-line mode.\n\n**-q** _CHAR_\n> Quote character.\n\n**-c**, **--complete-filenames**\n> Complete filenames.\n\n**-r**, **--remember**\n> Remember input for completion.\n\n**-p** _PROMPT_\n> Prompt color.\n\n**-b** _CHARS_\n> Word break characters.\n\n**-s** _N_\n> History size.\n\n**--no-warnings**\n> Suppress warnings."],["DESCRIPTION","**rlwrap** adds readline capabilities to any command. It provides line editing, history, and completion for programs that lack them.\n\nHistory persists across sessions when using -H. Previous inputs are searchable with Ctrl-R. Arrow keys navigate history.\n\nCompletion files contain words for Tab completion. The -r option learns words from output, building dynamic completions.\n\nMulti-line mode handles commands spanning multiple lines. This is useful for SQL clients and REPLs that accept multi-line input.\n\nThe tool intercepts input, processes it through readline, then passes it to the wrapped command. Output passes through unchanged.\n\nPrompts are detected and colorized. This improves visibility in interactive sessions."],["CAVEATS","May interfere with programs that handle terminals specially. Prompt detection is heuristic. Some control sequences may not work properly."],["HISTORY","**rlwrap** was written by **Hans Lub** to add readline support to arbitrary programs. It fills the gap for programs without built-in line editing."],["INSTALL","```apt: sudo apt install rlwrap```\n\n```dnf: sudo dnf install rlwrap```\n\n```pacman: sudo pacman -S rlwrap```\n\n```apk: sudo apk add rlwrap```\n\n```zypper: sudo zypper install rlwrap```\n\n```brew: brew install rlwrap```\n\n```nix: nix profile install nixpkgs#rlwrap```"],["SEE ALSO","[readline](/man/readline)(3), [bash](/man/bash)(1), [ledit](/man/ledit)(1)"]],"e":["readline","bash","ledit"],"x":"rlwrap [command]"},"sail":{"n":"sail","t":"age-of-sail naval warfare simulation game","s":[["TAGLINE","age-of-sail naval warfare simulation game"],["TLDR","**Start a game**\n\n```sail```\n\n**List available scenarios**\n\n```sail -l```\n\n**Play a specific scenario**\n\n```sail -s [1]```"],["SYNOPSIS","**sail** [_options_]"],["PARAMETERS","**-l**\n> List available scenarios.\n\n**-s** _NUM_\n> Select scenario number.\n\n**-x**\n> Play previously saved game."],["PREVIEW","```\n ~~~~~~~~  Wind: NE\n ~~A~~~~~\n ~~~~B~~~~\n ~~~~~~~~~\n ~~~~C~~~~\n A: Constitution\n```"],["DESCRIPTION","**sail** is a naval warfare simulation from the **bsdgames** collection, set in the age of wooden ships and iron men. Players command ships in historical naval battles, managing sail configuration, crew, and cannon fire.\n\nCommands control ship heading, sail trim, and gunnery. The game simulates wind effects, hull damage, crew casualties, and boarding actions."],["CAVEATS","Part of the bsdgames package. Complex controls with a learning curve. Supports multiple players on the same system."],["HISTORY","**sail** was written by **David Riggle** and **Ed Wang** and is based on the **Avalon Hill** board game \"Wooden Ships and Iron Men.\" It has been part of BSD games since the **1980s**."],["INSTALL","```dnf: sudo dnf install bsd-games```\n\n```pacman: sudo pacman -S bsd-games```\n\n```apk: sudo apk add bsd-games```\n\n```zypper: sudo zypper install bsd-games```\n\n```brew: brew install sail```\n\n```nix: nix profile install nixpkgs#sail```"],["SEE ALSO","[trek](/man/trek)(6), [atc](/man/atc)(6), [battlestar](/man/battlestar)(6)"]],"e":["trek","atc","battlestar"],"x":"sail"},"scout":{"n":"scout","t":"Search packages by binaries, modules, and other attributes on openSUSE","s":[["TAGLINE","Search packages by binaries, modules, and other attributes on openSUSE"],["TLDR","**Search for a binary in available packages**\n\n```scout bin [command_name]```\n\n**Search for a Java class**\n\n```scout java [class_name]```\n\n**Search for an autoconf macro**\n\n```scout autoconf [macro_name]```\n\n**Search for a Python module**\n\n```scout python [module_name]```\n\n**Search using a specific repository**\n\n```scout -r [repo_name] bin [command_name]```"],["SYNOPSIS","**scout** [_module_] [_options_] [_search_term_]"],["PARAMETERS","**bin**\n> Search for binaries/executables in packages\n\n**java**\n> Search for Java classes in packages\n\n**python**\n> Search for Python modules in packages\n\n**autoconf**\n> Search for autoconf macros in packages\n\n**-r** _repo_, **--repo**=_repo_\n> Specify repository to search (default: zypp)\n\n**-p**, **--package**\n> Display package names only\n\n**-l**, **--listrepos**\n> List available repositories"],["DESCRIPTION","**Scout** is an openSUSE command-line utility for searching packages by various attributes such as binaries, Java classes, Python modules, and autoconf macros. It powers the \"command-not-found\" feature in bash and zsh.\n\nWhen you type an unknown command, the shell automatically invokes scout to find which package provides it and suggests installation via zypper. Scout searches package indexes without requiring packages to be installed.\n\nThe tool integrates with the zypp package management system and can query multiple repositories."],["CAVEATS","Scout relies on pre-built indexes that may not include all packages or the latest additions. The command-not-found handler is automatically enabled in bash and zsh on openSUSE systems."],["HISTORY","Scout was developed by the **openSUSE** project to provide helpful suggestions when users type commands that are not installed. The project has been succeeded by **cnf** (command-not-found) in newer openSUSE releases."],["INSTALL","```apt: sudo apt install openafs-client```\n\n```nix: nix profile install nixpkgs#scout```"],["SEE ALSO","[zypper](/man/zypper)(8), [pkgfile](/man/pkgfile)(1), [apt-file](/man/apt-file)(1)"]],"e":["zypper","pkgfile","apt-file"],"x":"scout bin [command_name]"},"sdiag":{"n":"sdiag","t":"Display Slurm controller diagnostic information","s":[["TAGLINE","Display Slurm controller diagnostic information"],["TLDR","**Show scheduling diagnostic information** (default mode)\n\n```sdiag```\n\n**Show diagnostics sorted by RPC total run time**\n\n```sdiag -t```\n\n**Show diagnostics sorted by RPC average run time**\n\n```sdiag -T```\n\n**Reset performance counters** (requires operator/admin privileges)\n\n```sdiag -r```\n\n**Output diagnostics as JSON**\n\n```sdiag --json```"],["SYNOPSIS","**sdiag** [_options_]"],["PARAMETERS","**-a**, **--all**\n> Get and report information. This is the default mode of operation.\n\n**-h**, **--help**\n> Print description of options and exit.\n\n**-i**, **--sort-by-id**\n> Sort RPC data by message type ID and user ID.\n\n**-r**, **--reset**\n> Reset scheduler and RPC counters to 0. Only supported for Slurm operators and administrators.\n\n**-t**, **--sort-by-time**\n> Sort RPC data by total run time.\n\n**-T**, **--sort-by-time2**\n> Sort RPC data by average run time.\n\n**--json**\n> Output information as JSON.\n\n**--yaml**\n> Output information as YAML.\n\n**-V**, **--version**\n> Print version number and exit.\n\n**--usage**\n> Print list of options and exit."],["DESCRIPTION","**sdiag** displays diagnostic information about slurmctld, the Slurm controller daemon. It shows performance metrics, scheduling statistics, RPC counters, and resource usage data.\n\nThis is useful for monitoring cluster health, troubleshooting scheduling performance, and identifying bottlenecks in the Slurm controller."],["CAVEATS","Requires appropriate permissions to access Slurm controller data. The reset option requires operator or administrator privileges and affects all users' view of counters."],["HISTORY","Part of **Slurm** workload manager, providing diagnostic tools for cluster administrators."],["INSTALL","```apt: sudo apt install slurm-client```"],["SEE ALSO","[scontrol](/man/scontrol)(1), [sinfo](/man/sinfo)(1), [squeue](/man/squeue)(1), [sacct](/man/sacct)(1)"]],"e":["scontrol","sinfo","squeue","sacct"],"x":"sdiag"},"sealbot":{"n":"sealbot","t":"Seal and verify documents with the Let's Seal (SEAL) standard","s":[["TAGLINE","Seal and verify documents with the Let's Seal (SEAL) standard"],["TLDR","**Seal** a file (PDF or other)\n\n```sealbot seal [contract.pdf]```\n\n**Verify** a sealed file offline\n\n```sealbot verify [contract.sealed.pdf]```\n\nInstall via **npm**\n\n```npm i -g sealbot```\n\nRun without installing via **npx**\n\n```npx sealbot seal [file]```"],["SYNOPSIS","**sealbot** {_seal_|_verify_|_issue_|_anchor_|_watch_} [_options_] [_file_]"],["PARAMETERS","**seal** _file_\n> Seal a PDF or other file through the Let's Seal API or self-hosted service\n\n**verify** _file_\n> Verify a seal offline (integrity, issuer, time anchor)\n\n**issue**\n> Issue-related sealing operations (see `sealbot --help`)\n\n**anchor**\n> Anchor related proofs (e.g. time anchoring workflows)\n\n**watch**\n> Watch/automation mode for sealing workflows"],["DESCRIPTION","**sealbot** is the command-line client for **Let's Seal**, an open standard (SEAL — Sealed Evidence Anchored to a Ledger) for proving that a file is unaltered, sealed by a known certificate, and existed by a certain date. Seals use format-native mechanisms where possible (PAdES for PDF, C2PA for media, detached CMS for arbitrary files) and can be verified without trusting a single vendor forever.\n\nInstall with `npm i -g sealbot` or run `npx sealbot`. A self-contained Rust build lives in the upstream `cli-rs/` directory. Hosted sealing uses organization API keys; verification is free and works offline once roots and proofs are available."],["CAVEATS","Sealing through the hosted service requires network access and credentials. Verification needs pinned trust roots for full authenticity verdicts. Format support varies (PDF, images, XML, email, detached signatures)."],["HISTORY","Part of the **Let's Seal** open-source project (Apache-2.0), modeled after free public certificate infrastructure for document authenticity."],["SEE ALSO","[openssl](/man/openssl)(1), [ots](/man/ots)(1)"],["RESOURCES","```[Source code](https://github.com/letsseal/letsseal)```\n\n```[Homepage](https://letsseal.org)```\n\n```[Documentation](https://letsseal.org)```"]],"e":["openssl","ots"],"x":"sealbot seal [contract.pdf]"},"systemd-cryptsetup":{"n":"systemd-cryptsetup","t":"Manage encrypted volume mappings","s":[["TAGLINE","Manage encrypted volume mappings"],["TLDR","**Attach** (open) encrypted volume\n\n```systemd-cryptsetup attach [mapping_name] [/dev/sdXY]```\n\nAttach with **options**\n\n```systemd-cryptsetup attach [mapping_name] [/dev/sdXY] none [crypttab_options]```\n\nAttach with **keyfile**\n\n```systemd-cryptsetup attach [mapping_name] [/dev/sdXY] [/path/to/keyfile] [options]```\n\n**Detach** (close) mapping\n\n```systemd-cryptsetup detach [mapping_name]```"],["SYNOPSIS","**systemd-cryptsetup** attach _VOLUME_ _DEVICE_ [_KEY_] [_OPTIONS_]\n\n**systemd-cryptsetup** detach _VOLUME_"],["DESCRIPTION","**systemd-cryptsetup** creates or removes decrypted mappings of encrypted LUKS volumes. It is the systemd equivalent of `cryptsetup open` and `cryptsetup close`.\n\nArguments follow the same format as `/etc/crypttab` entries. This tool is primarily used internally by systemd to unlock encrypted devices during boot based on crypttab configuration."],["CAVEATS","Typically called by systemd automatically, not manually. The mapping appears at `/dev/mapper/<name>`. Supports all crypttab options like luks, discard, etc."],["HISTORY","**systemd-cryptsetup** integrates encrypted volume management with systemd's boot process, enabling automatic unlocking based on `/etc/crypttab` configuration."],["INSTALL","```apt: sudo apt install systemd-cryptsetup```"],["SEE ALSO","[cryptsetup](/man/cryptsetup)(8), [crypttab](/man/crypttab)(5), [systemd-cryptenroll](/man/systemd-cryptenroll)(1)"]],"e":["cryptsetup","crypttab","systemd-cryptenroll"],"x":"systemd-cryptsetup attach [mapping_name] [/dev/sdXY]"},"unix2mac":{"n":"unix2mac","t":"Convert Unix line endings to classic Mac format","s":[["TAGLINE","Convert Unix line endings to classic Mac format"],["TLDR","**Convert** a file to classic Mac line endings (CR) in place\n\n```unix2mac [path/to/file]```\n\n**Write** converted output to a new file\n\n```unix2mac -n [path/to/file] [path/to/new_file]```\n\n**Display** file information without converting\n\n```unix2mac -i [path/to/file]```\n\n**Convert and keep** the original file timestamp\n\n```unix2mac -k [path/to/file]```\n\n**Force conversion** of binary files\n\n```unix2mac -f [path/to/file]```"],["SYNOPSIS","**unix2mac** [_options_] [_file_...]"],["PARAMETERS","**-n, --newfile** _INFILE_ _OUTFILE_\n> Write to new file instead of modifying in place.\n\n**-o, --oldfile** _FILE_\n> In-place conversion (default mode).\n\n**-i, --info** [_FLAGS_]\n> Display file information without converting.\n\n**-k, --keepdate**\n> Keep output file date same as input.\n\n**-f, --force**\n> Force conversion of binary files.\n\n**-s, --safe**\n> Skip binary files (default).\n\n**-b, --keep-bom**\n> Keep Byte Order Mark.\n\n**-m, --add-bom**\n> Add UTF-8 Byte Order Mark.\n\n**-r, --remove-bom**\n> Remove Byte Order Mark.\n\n**-q, --quiet**\n> Quiet mode, suppress warnings.\n\n**-v, --verbose**\n> Display detailed conversion information.\n\n**-l, --newline**\n> Add additional newline.\n\n**-F, --follow-symlink**\n> Convert symbolic link targets."],["DESCRIPTION","**unix2mac** converts text files from Unix line ending format (LF) to classic Mac format (CR). This format was used by Mac OS 9 and earlier. Modern macOS uses Unix-style LF line endings.\n\nThe tool is primarily useful for compatibility with legacy Mac software or when working with files that specifically require CR line endings."],["CAVEATS","Classic Mac line endings (CR only) are rarely needed today as modern macOS uses LF. Binary files should not be processed. Part of the dos2unix package."],["INSTALL","```apt: sudo apt install dos2unix```\n\n```dnf: sudo dnf install dos2unix```\n\n```pacman: sudo pacman -S dos2unix```\n\n```apk: sudo apk add dos2unix```\n\n```zypper: sudo zypper install dos2unix```\n\n```brew: brew install dos2unix```\n\n```nix: nix profile install nixpkgs#dos2unix```"],["SEE ALSO","[dos2unix](/man/dos2unix)(1), [unix2dos](/man/unix2dos)(1), [mac2unix](/man/mac2unix)(1), [iconv](/man/iconv)(1), [file](/man/file)(1)"]],"e":["dos2unix","unix2dos","mac2unix","iconv","file"],"x":"unix2mac [path/to/file]"},"unlzma":{"n":"unlzma","t":"Decompress LZMA compressed files","s":[["TAGLINE","Decompress LZMA compressed files"],["TLDR","**Decompress file**\n\n```unlzma [file.lzma]```\n\n**Keep original**\n\n```unlzma -k [file.lzma]```\n\n**To stdout**\n\n```unlzma -c [file.lzma]```\n\n**Force overwrite**\n\n```unlzma -f [file.lzma]```\n\n**Verbose**\n\n```unlzma -v [file.lzma]```"],["SYNOPSIS","**unlzma** [_-k_] [_-c_] [_-f_] [_options_] _files_"],["PARAMETERS","**-k**\n> Keep original.\n\n**-c**\n> Write to stdout.\n\n**-f**\n> Force overwrite.\n\n**-v**\n> Verbose mode.\n\n**-q**\n> Quiet mode. Specify twice to suppress errors.\n\n**-T**, **--threads** _N_\n> Use up to N threads for decompression (xz 5.2+)."],["DESCRIPTION","**unlzma** decompresses files that were compressed with the LZMA (Lempel-Ziv-Markov chain Algorithm) compression format. It is functionally equivalent to running **xz --format=lzma --decompress** and is provided as a convenience command within the xz-utils package.\n\nBy default, unlzma replaces the compressed .lzma file with the decompressed output, removing the original. The **-k** flag preserves the original compressed file, and **-c** writes the decompressed data to standard output for piping to other commands.\n\nThe LZMA format is a legacy compression format that has been largely superseded by the XZ format, which uses the improved LZMA2 algorithm with better multi-threading support. unlzma remains useful for decompressing older archives and for compatibility with systems that still use the .lzma format."],["CAVEATS","The .lzma format is a legacy format without integrity checks. Use xz for new files. Part of xz-utils. The command line syntax is a superset of LZMA Utils 4.32.x for backward compatibility."],["HISTORY","**unlzma** is part of **xz-utils**, providing decompression for the legacy LZMA format."],["INSTALL","```dnf: sudo dnf install xz```\n\n```pacman: sudo pacman -S xz```\n\n```apk: sudo apk add xz```\n\n```zypper: sudo zypper install xz```\n\n```brew: brew install xz```\n\n```nix: nix profile install nixpkgs#xz```"],["SEE ALSO","[xz](/man/xz)(1), [lzma](/man/lzma)(1), [unxz](/man/unxz)(1)"]],"e":["xz","lzma","unxz"],"x":"unlzma [file.lzma]"},"uudecode":{"n":"uudecode","t":"Decode uuencoded binary files","s":[["TAGLINE","Decode uuencoded binary files"],["TLDR","**Decode a uuencoded file**\n\n```uudecode [encoded.txt]```\n\n**Decode to a specific output file**\n\n```uudecode -o [output] [encoded.txt]```\n\n**Decode from stdin**\n\n```cat [encoded.txt] | uudecode```\n\n**Decode multiple files**\n\n```uudecode [file1.txt] [file2.txt]```"],["SYNOPSIS","**uudecode** [_-o outfile_] [_file ..._]"],["PARAMETERS","**-o** _outfile_\n> Write output to specified file instead of the name in the encoded data.\n\n_file_\n> Input file(s) to decode (uses stdin if omitted)."],["DESCRIPTION","**uudecode** transforms uuencoded files back to their original binary form. It automatically detects whether the input uses traditional UU encoding or base64 encoding and handles both formats.\n\nBy default, the output filename is taken from the encoded file's header line. The **-o** option overrides this. File permissions are restored from the encoded header, except that setuid and execute bits are not retained for security.\n\nWhen multiple files are specified, each is decoded separately."],["CAVEATS","Setuid and execute bits from the original file are stripped for security. The output filename in the encoded header can be a security risk if decoding untrusted files; use **-o** to specify a safe output path. Ensure the encoded file was created with the same encoding method (UU or base64)."],["HISTORY","uudecode was developed at Bell Labs and first appeared in **BSD 4.0** in the early 1980s as the counterpart to uuencode. Together they enabled binary file transfer over text-only UUCP networks. While largely superseded by MIME for email attachments, the commands remain available on Unix-like systems for backward compatibility and simple encoding tasks."],["INSTALL","```apt: sudo apt install sharutils```\n\n```dnf: sudo dnf install sharutils```\n\n```pacman: sudo pacman -S sharutils```\n\n```apk: sudo apk add sharutils```\n\n```zypper: sudo zypper install sharutils```\n\n```nix: nix profile install nixpkgs#sharutils```"],["SEE ALSO","[uuencode](/man/uuencode)(1), [base64](/man/base64)(1), [mail](/man/mail)(1)"]],"e":["uuencode","base64","mail"],"x":"uudecode [encoded.txt]"},"uv-python":{"n":"uv-python","t":"Manage Python version installations","s":[["TAGLINE","Manage Python version installations"],["TLDR","**List available Python versions**\n\n```uv python list```\n\n**Install Python version**\n\n```uv python install [version]```\n\n**Uninstall Python version**\n\n```uv python uninstall [version]```\n\n**Find Python executable**\n\n```uv python find [version]```\n\n**Pin Python version**\n\n```uv python pin [version]```"],["SYNOPSIS","**uv** **python** _command_ [_options_]"],["PARAMETERS","**list**\n> Show available versions.\n\n**install** _version_\n> Install Python version.\n\n**uninstall** _version_\n> Remove Python version.\n\n**find** _version_\n> Find Python executable.\n\n**pin** _version_\n> Set project Python version.\n\n**dir**\n> Show Python install directory."],["DESCRIPTION","**uv python** manages Python installations. Download, install, and switch between Python versions. Provides hermetic Python management without system Python."],["INSTALL","```dnf: sudo dnf install uv```\n\n```pacman: sudo pacman -S uv```\n\n```apk: sudo apk add uv```\n\n```brew: brew install uv```\n\n```nix: nix profile install nixpkgs#uv```"],["SEE ALSO","[uv](/man/uv)(1), [pyenv](/man/pyenv)(1)"]],"e":["uv","pyenv"],"x":"uv python list"},"uv-self":{"n":"uv-self","t":"Manage the uv installation itself","s":[["TAGLINE","Manage the uv installation itself"],["TLDR","**Update uv to the latest version**\n\n```uv self update```\n\n**Update uv to a specific version**\n\n```uv self update [0.6.0]```\n\n**Uninstall uv**\n\n```uv self uninstall```"],["SYNOPSIS","**uv** **self** _command_ [_options_]"],["PARAMETERS","**update**\n> Update uv to the latest version, or to a specified version.\n\n**uninstall**\n> Uninstall uv.\n\n**--target-version** _version_\n> Update to a specific version (used with update subcommand)."],["DESCRIPTION","**uv self** manages the uv installation itself. Use to update uv to the latest version, update to a specific version, or uninstall uv entirely. The update command downloads the new binary and replaces the current installation in place."],["SEE ALSO","[uv](/man/uv)(1), [uv-version](/man/uv-version)(1), [uv-pip](/man/uv-pip)(1), [uv-tool](/man/uv-tool)(1), [pip](/man/pip)(1)"]],"e":["uv","uv-version","uv-pip","uv-tool","pip"],"x":"uv self update"},"visidata":{"n":"visidata","t":"Terminal spreadsheet multitool for exploring data","s":[["TAGLINE","Terminal spreadsheet multitool for exploring data"],["TLDR","**Open a CSV file**\n\n```vd [data.csv]```\n\n**Explore piped JSON data**\n\n```curl [https://api.example.com/data] | vd```\n\n**Browse a SQLite database**\n\n```vd [database.db]```"],["SYNOPSIS","**vd** [_options_] _input_"],["DESCRIPTION","**VisiData** combines the clarity of a spreadsheet, the efficiency of the terminal, and the power of Python into a lightweight tool that handles millions of rows. It supports dozens of formats including CSV, JSON, Excel, SQLite, HDF5, and more. Features include sorting, filtering, aggregation, frequency analysis, and interactive plotting."],["HISTORY","**VisiData** was created by **Saul Pwanson** and is written in **Python**."],["INSTALL","```dnf: sudo dnf install visidata```\n\n```pacman: sudo pacman -S visidata```\n\n```brew: brew install visidata```\n\n```nix: nix profile install nixpkgs#visidata```"],["SEE ALSO","[csvkit](/man/csvkit)(1), [xsv](/man/xsv)(1), [miller](/man/miller)(1)"]],"e":["csvkit","xsv","miller"],"x":"vd [data.csv]"},"wmo":{"n":"wmo","t":"World Model Optimizer CLI for routing, distilling, and serving models","s":[["TAGLINE","World Model Optimizer CLI for routing, distilling, and serving models"],["TLDR","Install and **configure providers**\n\n```pip install world-model-optimizer```\n\n```wmo providers set```\n\n**Build** a model from agent traces\n\n```wmo build --file [traces.jsonl] --name [my-model]```\n\n**Serve** a tuned router/model\n\n```wmo serve --name [my-model]```\n\n**Log in** to the hosted platform\n\n```wmo login```\n\n**Run** a hosted agent or world model\n\n```wmo run [agent-id]```"],["SYNOPSIS","**wmo** _command_ [_options_]"],["PARAMETERS","**providers set**\n> Register model providers and candidates in `.wmo/pool.toml`\n\n**build** **--file** _traces_ **--name** _name_\n> Build a world model / routing setup from OTel or agent traces\n\n**optimize**\n> Subcommands for route sweep/fit/report, distill, harness, and pin\n\n**serve** **--name** _name_\n> Serve an endpoint that routes between frontier and smaller models\n\n**login**\n> Authenticate against the Experiential Labs hosted platform\n\n**run** _id_\n> Run a hosted world model or agent harness\n\n**config telemetry** {_enable_|_disable_|_status_}\n> Control anonymous usage telemetry\n\n**eval**\n> Evaluate harnesses/tasks (optional E2B backend)"],["DESCRIPTION","**wmo** (World Model Optimizer) turns collected agent traces into smaller open models and cost-aware routers. It can distill via external APIs (e.g. Tinker), fit routing policies on held-out traces, and serve a local endpoint that keeps frontier quality while cutting inference cost.\n\nThe project also ships world-model simulation APIs for closed-loop agent testing and optional E2B sandbox backends for harness optimization. Local state lives under `.wmo/`."],["CAVEATS","Requires Python packaging (`pip install world-model-optimizer`) and provider API keys for most optimize/serve workflows. Hosted features need `wmo login`. Telemetry is on by default; disable with `wmo config telemetry disable` or `DO_NOT_TRACK=1` / `WMO_TELEMETRY=0`."],["HISTORY","Open-source CLI from **Experiential Labs** for continual improvement of agent models from production traces."],["SEE ALSO","[pip](/man/pip)(1), [python](/man/python)(1)"],["RESOURCES","```[Source code](https://github.com/experientiallabs/world-model-optimizer)```\n\n```[Homepage](https://platform.experientiallabs.ai)```"]],"e":["pip","python"],"x":"pip install world-model-optimizer"},"xml-elements":{"n":"xml-elements","t":"List XML element names and paths","s":[["TAGLINE","List XML element names and paths"],["TLDR","**List element names**\n\n```xml elements [file.xml]```\n\n**List with paths**\n\n```xml elements -a [file.xml]```\n\n**List unique elements**\n\n```xml elements -u [file.xml]```"],["SYNOPSIS","**xml elements** [_options_] _file_"],["PARAMETERS","**-a**\n> Show XPath for each element.\n\n**-u**\n> Show unique elements only.\n\n**-d** _depth_\n> Maximum depth."],["DESCRIPTION","**xml elements** lists XML element names in a document. Part of xmlstarlet toolkit. Useful for exploring XML structure and discovering available element paths."],["INSTALL","```dnf: sudo dnf install xmlstarlet```\n\n```pacman: sudo pacman -S xmlstarlet```\n\n```apk: sudo apk add xmlstarlet```\n\n```zypper: sudo zypper install xmlstarlet```\n\n```brew: brew install xmlstarlet```\n\n```nix: nix profile install nixpkgs#xmlstarlet```"],["SEE ALSO","[xml-select](/man/xml-select)(1), [xml-list](/man/xml-list)(1)"]],"e":["xml-select","xml-list"],"x":"xml elements [file.xml]"},"yank":{"n":"yank","t":"Interactively select and copy terminal output","s":[["TAGLINE","Interactively select and copy terminal output"],["TLDR","**Select and copy**\n\n```echo \"[text]\" | yank```\n\n**Custom delimiter**\n\n```echo \"[text]\" | yank -d \"[\\n]\"```\n\n**Copy to X clipboard**\n\n```echo \"[text]\" | yank -x```\n\n**With selection command**\n\n```echo \"[text]\" | yank -- xclip```"],["SYNOPSIS","**yank** [_-d delim_] [_-x_] [_options_] [_-- command_]"],["PARAMETERS","**-d** _DELIM_\n> Field delimiter.\n\n**-x**\n> X clipboard.\n\n**-l**\n> Line delimiter.\n\n**--** _CMD_\n> Yank command."],["DESCRIPTION","**yank** reads input from a pipe or file and presents it in a terminal selection interface. The user navigates lines or fields with arrow keys, selects the desired text, and yank copies it to the clipboard or passes it to a specified command.\n\nBy default, input is split into lines. The **-d** option sets a custom delimiter for splitting input into selectable fields, useful for extracting specific columns from structured output. The **-l** option forces line-by-line splitting regardless of content.\n\nClipboard integration works through an external command such as **xclip** or **xsel**, specified after **--**. On systems with X11, the **-x** flag provides a shortcut for X clipboard access. The tool is designed to fit naturally into Unix pipelines, accepting input from any command that writes to stdout."],["CAVEATS","Terminal required. Clipboard setup varies. Simple tool."],["HISTORY","**yank** was created for interactively selecting and copying terminal output to the clipboard."],["INSTALL","```apt: sudo apt install emboss```\n\n```dnf: sudo dnf install yank```\n\n```pacman: sudo pacman -S yank```\n\n```apk: sudo apk add yank```\n\n```zypper: sudo zypper install yank```\n\n```brew: brew install yank```\n\n```nix: nix profile install nixpkgs#emboss```"],["SEE ALSO","[xclip](/man/xclip)(1), [xsel](/man/xsel)(1), [fzf](/man/fzf)(1)"]],"e":["xclip","xsel","fzf"],"x":"echo \"[text]\" | yank"},"yarn-why":{"n":"yarn-why","t":"Explain why a package is installed","s":[["TAGLINE","Explain why a package is installed"],["TLDR","**Explain** why a package is installed\n\n```yarn why [package]```\n\n**Trace** a specific version of a package (Yarn Berry)\n\n```yarn why [package@version]```\n\n**Show full recursive reasoning** (Yarn Berry)\n\n```yarn why [package] -R```\n\n**JSON output** for scripting (Yarn Berry)\n\n```yarn why [package] --json```"],["SYNOPSIS","**yarn why** _package_ [_options_]"],["DESCRIPTION","**yarn why** explains why a given package is installed in the current project. It walks the dependency tree and prints the chain of packages that ultimately require the target, plus a hash/checksum and a disk-size estimate.\n\nIt is useful for diagnosing why an unwanted transitive dependency exists, for spotting version conflicts (the same package pinned through different paths), and for auditing **node_modules** before publishing."],["PARAMETERS","_package_\n> Package name (or **name@version** in Yarn Berry) to investigate.\n\n**--json**\n> Yarn Berry only: emit machine-readable JSON.\n\n**-R**, **--recursive**\n> Yarn Berry only: print the full transitive dependency tree, not just the first reason."],["CAVEATS","Output format and accepted flags differ between **Yarn Classic (1.x)** and **Yarn Berry (2+)**. The recursive and JSON options are Berry extensions not present in 1.x. The command needs an installed project (it reads **node_modules** / **.pnp.cjs**), not just **package.json**."],["INSTALL","```pacman: sudo pacman -S yarn```\n\n```apk: sudo apk add yarn```\n\n```zypper: sudo zypper install yarn```\n\n```brew: brew install yarn```\n\n```nix: nix profile install nixpkgs#yarn```"],["SEE ALSO","[yarn](/man/yarn)(1), [npm-explain](/man/npm-explain)(1)"]],"e":["yarn","npm-explain"],"x":"yarn why [package]"},"ykman-openpgp":{"n":"ykman-openpgp","t":"Manage YubiKey OpenPGP keys","s":[["TAGLINE","Manage YubiKey OpenPGP keys"],["TLDR","**Show OpenPGP info**\n\n```ykman openpgp info```\n\n**Set PIN**\n\n```ykman openpgp access change-pin```\n\n**Set Admin PIN**\n\n```ykman openpgp access change-admin-pin```\n\n**Reset OpenPGP**\n\n```ykman openpgp reset```"],["SYNOPSIS","**ykman** **openpgp** _command_ [_options_]"],["PARAMETERS","**info**\n> Show OpenPGP information.\n\n**access** _cmd_\n> Manage PINs and access.\n\n**certificates** _cmd_\n> Manage certificates.\n\n**keys** _cmd_\n> Manage keys.\n\n**reset**\n> Reset OpenPGP application."],["DESCRIPTION","**ykman openpgp** manages OpenPGP on YubiKey. Configure PGP keys for encryption, signing, and authentication. Hardware-backed GPG key storage."],["CAVEATS","Reset permanently removes all OpenPGP keys from the device."],["INSTALL","```dnf: sudo dnf install yubikey-manager```\n\n```pacman: sudo pacman -S yubikey-manager```\n\n```apk: sudo apk add yubikey-manager```\n\n```zypper: sudo zypper install yubikey-manager```\n\n```brew: brew install ykman```\n\n```nix: nix profile install nixpkgs#yubikey-manager```"],["SEE ALSO","[ykman](/man/ykman)(1), [gpg](/man/gpg)(1)"]],"e":["ykman","gpg"],"x":"ykman openpgp info"},"zeit":{"n":"zeit","t":"Simple command-line tool for tracking time","s":[["TAGLINE","Simple command-line tool for tracking time"],["TLDR","**Start tracking time**\n\n```zeit track --project [myproject] --task \"[coding]\"```\n\n**List time entries for a project**\n\n```zeit list --project [myproject]```\n\n**Export entries as JSON**\n\n```zeit export --format json```"],["SYNOPSIS","**zeit** _command_ [_options_]"],["DESCRIPTION","**zeit** is a cross-platform terminal time tracker that supports hierarchical categorization by project, task, and entry. It stores data locally, supports natural language arguments, and can export recorded time blocks as JSON for external tools."],["HISTORY","**zeit** was created by **Marius** (mrusme) and is written in **Go**."],["INSTALL","```aur: yay -S zeit```"],["SEE ALSO","[watson](/man/watson)(1)"]],"e":["watson"],"x":"zeit track --project [myproject] --task \"[coding]\""}});
