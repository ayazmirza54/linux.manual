window.LCL_BUCKET(102,{"Xvfb":{"n":"Xvfb","t":"Virtual framebuffer X server","s":[["TAGLINE","Virtual framebuffer X server"],["TLDR","**Start virtual display**\n\n```Xvfb :99```\n\n**Start with specific screen size**\n\n```Xvfb :99 -screen 0 [1920x1080x24]```\n\n**Start with multiple screens**\n\n```Xvfb :1 -screen 0 [1280x1024x24] -screen 1 [800x600x16]```\n\n**Use shared memory**\n\n```Xvfb :99 -shmem -screen 0 [1024x768x24]```\n\n**Store framebuffer in directory**\n\n```Xvfb :99 -fbdir [/tmp/xvfb]```\n\n**Run application with xvfb-run**\n\n```xvfb-run -a [application]```\n\n**xvfb-run with custom screen**\n\n```xvfb-run -s \"-screen 0 1280x1024x24\" [application]```"],["SYNOPSIS","**Xvfb** [:_display_] [_options_]"],["PARAMETERS","**:display**\n> Display number (default: 0).\n\n**-screen** _num_ _WxHxD_\n> Configure screen: number, width x height x depth.\n\n**-pixdepths** _list_\n> Additional pixmap depths to support.\n\n**-fbdir** _dir_\n> Directory for memory-mapped framebuffer files.\n\n**-shmem**\n> Use shared memory for framebuffer.\n\n**-linebias** _n_\n> Adjust line pixelization.\n\n**-blackpixel** _value_\n> Set black pixel value.\n\n**-whitepixel** _value_\n> Set white pixel value."],["XVFB-RUN OPTIONS","**-a**, **--auto-servernum**\n> Find available display number automatically.\n\n**-s** _args_\n> Arguments to pass to Xvfb.\n\n**-e** _file_\n> File to store Xvfb error output.\n\n**-f** _file_\n> Authority file to use.\n\n**-n** _num_\n> Server number to use."],["DESCRIPTION","**Xvfb** (X Virtual FrameBuffer) is an X server that performs all graphical operations in memory without any physical display. It implements the X11 protocol, allowing X applications to run without visible output.\n\nCommon use cases include running GUI applications on headless servers, automated testing of graphical applications, rendering graphics for web services, and CI/CD pipelines that require X applications.\n\nThe **xvfb-run** wrapper script simplifies usage by automatically selecting a display number and handling authentication.\n\nDefault screen configuration is 1280x1024x24 (width x height x depth in bits)."],["CAVEATS","No GPU acceleration. Some applications may behave differently without real display. Memory usage scales with screen size and depth. Applications expecting specific display features may fail."],["HISTORY","**Xvfb** has been part of the X.Org server distribution since the X11R5 release. It was developed to enable X applications to run on systems without display hardware, supporting server-side rendering and automated testing long before headless browser technologies."],["INSTALL","```apt: sudo apt install xvfb```\n\n```apk: sudo apk add xvfb```\n\n```nix: nix profile install nixpkgs#xvfb```"],["SEE ALSO","[xvfb-run](/man/xvfb-run)(1), [xdpyinfo](/man/xdpyinfo)(1)"]],"e":["xvfb-run","xdpyinfo"],"x":"Xvfb :99"},"abrt-action-generate-backtrace":{"n":"abrt-action-generate-backtrace","t":"generate human-readable backtrace from a coredump","s":[["TAGLINE","generate human-readable backtrace from a coredump"],["TLDR","**Generate backtrace** from a problem directory\n\n```abrt-action-generate-backtrace -d [/var/spool/abrt/ccpp-2024-01-15]```\n\n**Generate backtrace** with verbose output\n\n```abrt-action-generate-backtrace -v -d [/var/spool/abrt/ccpp-2024-01-15]```\n\n**Generate backtrace** with a timeout for gdb\n\n```abrt-action-generate-backtrace -d [problem_dir] -t [300]```"],["SYNOPSIS","**abrt-action-generate-backtrace** [**-v**] [**-d** _DIR_] [**-t** _NUM_]"],["PARAMETERS","**-d** _DIR_\n> Path to the problem directory containing the coredump file. Defaults to the current working directory\n\n**-v**\n> Be more verbose; can be specified multiple times for increased verbosity\n\n**-t** _NUM_\n> Kill gdb if it runs for more than NUM seconds (timeout)"],["DESCRIPTION","**abrt-action-generate-backtrace** is part of the ABRT (Automatic Bug Reporting Tool) suite. It analyzes a coredump file and generates a human-readable backtrace showing the state of an application at the moment it crashed.\n\nThe tool runs **gdb** on a file named \"coredump\" in the specified problem directory. GDB generates the backtrace and other diagnostic information, which is then saved as a new element named \"backtrace\" in the problem directory. This backtrace can be used for debugging, bug reporting, or crash analysis.\n\nFor best results, the corresponding debuginfo packages should be installed for the crashed application and its libraries. Without debugging symbols, the backtrace will contain only memory addresses rather than function names and line numbers.\n\nThe tool integrates with libreport events and is typically invoked automatically by ABRT when analyzing C/C++ application crashes."],["CAVEATS","Requires **gdb** to be installed. Quality of the backtrace depends on availability of debuginfo packages. Processing large coredumps can be memory-intensive and time-consuming; use the **-t** option to set a timeout if needed. Only works with coredumps from applications, not kernel crashes."],["HISTORY","ABRT (Automatic Bug Reporting Tool) was developed by **Red Hat** starting around **2009** for Fedora and RHEL systems. It provides automated crash detection and reporting infrastructure. The backtrace generation component was created to simplify the process of collecting diagnostic information from application crashes for submission to bug tracking systems like Bugzilla."],["SEE ALSO","[gdb](/man/gdb)(1), [abrt-cli](/man/abrt-cli)(1), [coredumpctl](/man/coredumpctl)(1), [abrt](/man/abrt)(1)"],["RESOURCES","```[Source code](https://github.com/abrt/abrt)```\n\n```[Documentation](https://abrt.readthedocs.io/)```"]],"e":["gdb","abrt-cli","coredumpctl","abrt"],"x":"abrt-action-generate-backtrace -d [/var/spool/abrt/ccpp-2024-01-15]"},"akmods":{"n":"akmods","t":"Rebuild out-of-tree kernel modules from akmod packages","s":[["TAGLINE","Rebuild out-of-tree kernel modules from akmod packages"],["TLDR","**Check and rebuild** missing or outdated modules for the running kernel\n\n```sudo akmods```\n\n**Force rebuild** of all modules even if they failed or look up to date\n\n```sudo akmods --rebuild --force```\n\nBuild and install modules only for a **specific kernel** (same format as `uname -r`)\n\n```sudo akmods --kernels [kernel_version]```\n\nRebuild only one **akmod** package\n\n```sudo akmods --akmod [akmod_name]```\n\nShow status of the background **akmods service**\n\n```systemctl status akmods```"],["SYNOPSIS","**akmods** [_options_]"],["DESCRIPTION","**akmods** checks installed **akmod** packages and rebuilds out-of-tree kernel modules when they are missing, outdated, or broken for a given kernel. It is the Fedora/RHEL-family counterpart to frameworks like DKMS: an **akmod** package ships module sources and build metadata; **akmods** compiles them on the local system (often into a matching **kmod-*** RPM) whenever a new kernel appears.\n\nTypical use is after installing or updating drivers from RPM Fusion (for example NVIDIA via **akmod-nvidia**). A systemd unit may run **akmods** at boot so modules for the current kernel are ready before they are needed. Manual runs are common when a build failed, **kernel-devel** was missing during an earlier attempt, or you need modules for a kernel other than the one currently running.\n\nBuilds need a compiler toolchain and matching kernel headers/devel packages for each target kernel. Secure Boot setups may also need module signing keys under **/etc/pki/akmods**."],["OPTIONS","**--force**\n\n> Try all akmods, including ones that failed on earlier runs.\n\n**--kernels** _kernel_\n\n> Build and install only for the given kernel version string (same format as **uname -r**).\n\n**--rebuild**\n\n> Rebuild all modules even when they already appear up to date.\n\n**--akmod** _akmod_\n\n> Build and install only the named akmod package.\n\n**--quiet**\n\n> Reduce output.\n\n**--verbose**\n\n> Increase output."],["CAVEATS","Failed builds often leave no usable **kmod** until headers, compiler tools, and enough disk space are available, then **akmods --force** or **--rebuild** is re-run. On ostree-based systems (Silverblue/Kinoite) module signing and rebuild timing can differ from classic package installs. The man page is sparse; log output under the **akmods** service and journal is usually the best diagnostic trail."],["INSTALL","```dnf: sudo dnf install akmods```"],["SEE ALSO","[dkms](/man/dkms)(1), [modprobe](/man/modprobe)(8), [uname](/man/uname)(1)"],["RESOURCES","```[Documentation](https://rpmfusion.org/Packaging/KernelModules/Akmods)```"]],"e":["dkms","modprobe","uname"],"x":"sudo akmods"},"asciijump":{"n":"asciijump","t":"ASCII art ski jumping game","s":[["TAGLINE","ASCII art ski jumping game"],["TLDR","**Start the game**\n\n```asciijump```\n\n**Start with a custom hill**\n\n```asciijump -n [hillname]```"],["SYNOPSIS","**asciijump** [_options_]"],["PARAMETERS","**-n** _hillname_\n> Use the specified hill from the hills directory.\n\n**-h**\n> Display help."],["PREVIEW","```\n  \\O/\n   |    /\n  / \\  /\n______/________\n   90m  K-point\n```"],["DESCRIPTION","**asciijump** is an ncurses-based ASCII-art ski jumping game. The player controls a ski jumper, timing the takeoff and landing to achieve maximum distance. The game features multiple hills, training and competition modes (selected from the in-game menu), and multiplayer support for up to 10 players.\n\nControls: press space at the edge of the jump to take off and again to land safely. Timing affects both distance and style points."],["CAVEATS","Requires a terminal with ncurses support. Custom hills can be created and placed in the hills directory."],["HISTORY","**asciijump** was created by **Fabian Breitkreutz** as a terminal recreation of ski jumping competitions."],["INSTALL","```nix: nix profile install nixpkgs#asciijump```"],["SEE ALSO","[moon-buggy](/man/moon-buggy)(6), [ninvaders](/man/ninvaders)(6)"]],"e":["moon-buggy","ninvaders"],"x":"asciijump"},"baksmali":{"n":"baksmali","t":"Android DEX file disassembler","s":[["TAGLINE","Android DEX file disassembler"],["TLDR","**Disassemble** a DEX file\n\n```baksmali disassemble [classes.dex]```\n\nDisassemble to a **specific directory**\n\n```baksmali disassemble -o [output/] [classes.dex]```\n\n**Specify** API level for disassembly\n\n```baksmali disassemble -a [28] [classes.dex]```\n\n**List** available subcommands\n\n```baksmali --help```\n\n**List DEX classes** without disassembling\n\n```baksmali list classes [classes.dex]```\n\n**Disassemble** with code offset comments\n\n```baksmali disassemble --code-offsets [classes.dex]```"],["SYNOPSIS","**baksmali** _command_ [_options_] _dex-file_"],["DESCRIPTION","**baksmali** is a disassembler for Android DEX (Dalvik Executable) files. It converts DEX bytecode into smali assembly language, enabling analysis, modification, and reverse engineering of Android applications.\n\nThe tool is the counterpart to smali, together forming a complete assembler/disassembler suite for Android. Since version 2.0, baksmali uses a subcommand-based interface."],["PARAMETERS","**disassemble** (or **d**)\n> Disassemble a DEX file into smali files.\n\n**list** _type_\n> List items (classes, methods, fields, etc.) from a DEX file.\n\n**deodex** (or **x**)\n> Deodex an odex/oat file.\n\n**-o**, **--output** _dir_\n> Output directory (default: out/).\n\n**-a**, **--api-level** _level_\n> API level for disassembly.\n\n**-j**, **--jobs** _num_\n> Number of threads for parallel processing.\n\n**-b**, **--bootclasspath** _classpath_\n> Colon-separated bootclasspath jars for analysis.\n\n**-d**, **--bootclasspath-dir** _dir_\n> Directory containing framework files.\n\n**--debug-info**, **--di**\n> Include debug metadata in output (default: true).\n\n**--use-locals**\n> Use .locals directive instead of .registers.\n\n**--code-offsets**, **--off**\n> Add a comment before each instruction with its code offset.\n\n**--accessor-comments**, **--ac**\n> Generate helper comments for synthetic accessors (default: true).\n\n**--sequential-labels**, **--sl**\n> Use sequential numbering for labels.\n\n**--classes** _LIST_\n> Comma-separated list of classes to disassemble."],["FEATURES","- DEX to smali conversion\n- Multi-DEX support\n- API level awareness\n- Parallel processing\n- Debug info preservation\n- Local variable names"],["WORKFLOW","```bash"],["Disassemble DEX","baksmali disassemble classes.dex"],["Reassemble with smali","smali assemble out/ -o new-classes.dex\n```"],["CAVEATS","Requires Java runtime. Obfuscated code produces hard-to-read output. API level affects instruction interpretation. For research and educational purposes only. Respect software licenses and terms of service."],["HISTORY","**baksmali** (Icelandic for \"disassembler\") was created by Ben Gruver (JesusFreke) around **2010** as part of the smali project for Android reverse engineering."],["SEE ALSO","[apktool](/man/apktool)(1), [dex2jar](/man/dex2jar)(1), [jadx](/man/jadx)(1)"]],"e":["apktool","dex2jar","jadx"],"x":"baksmali disassemble [classes.dex]"},"barcode":{"n":"barcode","t":"Generate barcodes in PostScript format.","s":[["TAGLINE","Generate barcodes in PostScript format."],["TLDR","**Generate a barcode as PostScript**\n\n```barcode -b \"[data]\" -o [output.ps]```\n\n**Generate a barcode as EPS**\n\n```barcode -b \"[data]\" -E -o [output.eps]```\n\n**Generate Code 39 barcode**\n\n```barcode -e code39 -b \"[DATA123]\" -o [output.ps]```\n\n**Generate EAN-13 barcode**\n\n```barcode -e ean -b \"[5901234123457]\" -o [output.ps]```\n\n**Generate Code 128 barcode**\n\n```barcode -e code128 -b \"[data]\" -o [output.ps]```\n\n**Generate barcode without text label**\n\n```barcode -b \"[data]\" -n -o [output.ps]```\n\n**Set custom dimensions**\n\n```barcode -b \"[data]\" -g [100x50] -o [output.ps]```"],["SYNOPSIS","**barcode** [**-e** _encoding_] [**-b** _string_] [**-o** _file_] [_options_]"],["PARAMETERS","**-b** _string_\n> Data to encode in the barcode.\n\n**-e** _encoding_\n> Barcode encoding type (ean, upc, code39, code128, etc.). Defaults to the **BARCODE_ENCODING** environment variable or auto-detection.\n\n**-i** _file_\n> Read strings to encode from a file (default: stdin).\n\n**-o** _file_\n> Output file (default: stdout).\n\n**-g** _geometry_\n> Barcode geometry of the form _WxH+x+y_ (size and position).\n\n**-n**\n> Numeric output: print only the bars, suppressing the human-readable label.\n\n**-c**\n> Omit the checksum character for encodings that allow it.\n\n**-t** _WxH_\n> Table mode: generate multiple barcodes per page.\n\n**-m** _margin_\n> Margin size around barcode.\n\n**-p** _pagesize_\n> Page size (a4, letter, etc.).\n\n**-u** _unit_\n> Unit for dimensions (mm, in, pt).\n\n**-E**\n> Output as EPS (Encapsulated PostScript).\n\n**-P**\n> Output as PCL (for HP printers)."],["SUPPORTED ENCODINGS","**ean** / **ean13**: European Article Number (13 digits)\n**upc** / **upc-a**: Universal Product Code\n**isbn**: International Standard Book Number\n**code39**: Code 39 (alphanumeric)\n**code128**: Code 128 (high-density)\n**code93**: Code 93\n**i25**: Interleaved 2 of 5\n**cbr** / **codabar**: Codabar\n**msi**: MSI Plessey\n**pls**: Plessey"],["DESCRIPTION","**barcode** is a GNU utility that generates barcodes in various formats from textual data. It produces PostScript or EPS output that can be printed directly, converted to other formats, or embedded in documents.\n\nThe tool supports all major 1D barcode symbologies used in retail, logistics, and inventory management. Each encoding has specific rules for valid characters and optional check digits. The tool automatically calculates check digits for formats that require them.\n\nOutput can be customized with different sizes, margins, and label options. Table mode allows generating sheets of labels for batch printing."],["CAVEATS","Output is PostScript/EPS only; conversion to PNG or other raster formats requires tools like Ghostscript or ImageMagick. Not all encodings accept all characters; Code 39 is uppercase only, EAN/UPC require specific digit counts. Check digit validation is performed automatically but data must be valid for the chosen encoding."],["HISTORY","GNU barcode was written by **Alessandro Rubini** and first released in the **late 1990s** as part of the GNU project. It was designed for use with Unix printing systems and document preparation workflows. While newer tools and libraries have emerged for generating barcodes in various formats, GNU barcode remains useful for its simplicity and PostScript output quality."],["INSTALL","```apt: sudo apt install barcode```\n\n```dnf: sudo dnf install barcode```\n\n```pacman: sudo pacman -S barcode```\n\n```apk: sudo apk add barcode```\n\n```zypper: sudo zypper install barcode```\n\n```nix: nix profile install nixpkgs#barcode```"],["SEE ALSO","[qrencode](/man/qrencode)(1), [zint](/man/zint)(1), [gs](/man/gs)(1)"],["RESOURCES","```[Homepage](https://www.gnu.org/software/barcode/)```\n\n```[Source code](https://git.savannah.gnu.org/cgit/barcode.git)```"]],"e":["qrencode","zint","gs"],"x":"barcode -b \"[data]\" -o [output.ps]"},"bashmarks":{"n":"bashmarks","t":"Directory bookmarking for the shell","s":[["TAGLINE","Directory bookmarking for the shell"],["TLDR","**Save** current directory bookmark\n\n```s [bookmark-name]```\n\n**Go** to bookmark\n\n```g [bookmark-name]```\n\n**List** bookmarks\n\n```l```\n\n**Print** bookmark path\n\n```p [bookmark-name]```\n\n**Delete** bookmark\n\n```d [bookmark-name]```"],["SYNOPSIS","bashmarks directory bookmarking functions"],["DESCRIPTION","**bashmarks** is a bash script that provides directory bookmarking functionality. It allows you to save frequently-used directory paths with short names and jump to them quickly.\n\nThe tool adds convenient shell functions for managing and navigating to bookmarked directories."],["FUNCTIONS","**s** _name_\n> Save current directory as bookmark\n\n**g** _name_\n> Go to bookmarked directory\n\n**l**\n> List all bookmarks\n\n**d** _name_\n> Delete bookmark\n\n**p** _name_\n> Print bookmark path"],["FEATURES","- Simple bookmark management\n- Tab completion for bookmarks\n- Persistent storage\n- Integration with bash/zsh"],["WORKFLOW","```bash"],["Save bookmark","cd /var/log/apache2\ns apache"],["Jump to bookmark","g apache"],["List all","l"],["Remove","d apache\n```"],["CAVEATS","Requires sourcing **~/.local/bin/bashmarks.sh** in your shell config. Bookmark names must be simple (no spaces or special chars). Bookmarks are stored in **~/.sdirs**. Different from z or autojump which rank directories by frequency. Must be installed separately."],["HISTORY","**bashmarks** was created by Huy Nguyen around **2010** to provide simple directory bookmarking for bash users."],["SEE ALSO","[z](/man/z)(1), [autojump](/man/autojump)(1), [fasd](/man/fasd)(1)"],["RESOURCES","```[Source code](https://github.com/huyng/bashmarks)```"]],"e":["z","autojump","fasd"],"x":"s [bookmark-name]"},"bashtop":{"n":"bashtop","t":"Terminal-based resource monitor written in bash (deprecated)","s":[["TAGLINE","Terminal-based resource monitor written in bash (deprecated)"],["TLDR","> **bashtop** is deprecated and no longer maintained. Use **btop** (its C++ successor) instead.\n\n**Start** resource monitor\n\n```bashtop```"],["SYNOPSIS","**bashtop**"],["DESCRIPTION","**bashtop** is a resource monitor written in bash showing CPU, memory, disk, network, and process information in a terminal UI. It provides an attractive, mouse-enabled interface for system monitoring.\n\nThe tool offers game-like visuals and interactivity while remaining a pure bash script. Command-line argument parsing was never implemented, so it is launched simply as **bashtop** and configured from its in-app menu.\n\n**bashtop** is no longer actively developed. It was first rewritten in Python as **bpytop**, and then rewritten again in C++ as **btop**, which is the recommended replacement on all platforms."],["FEATURES","- CPU usage with core breakdown\n- Memory and swap usage\n- Disk I/O statistics\n- Network bandwidth\n- Process list with filtering\n- Mouse support\n- Customizable themes\n- Configuration menu"],["INTERFACE","Display sections:\n- **CPU** - Usage graphs per core\n- **Memory** - RAM and swap usage\n- **Disks** - I/O and space usage\n- **Network** - Bandwidth by interface\n- **Processes** - Sortable process list"],["KEYBINDINGS","- **q** - Quit\n- **m** - Configuration menu\n- **f** - Filter processes\n- **p/n** - Sort processes\n- **Arrow keys** - Navigate\n- **Mouse** - Click to interact"],["CONFIGURATION","**~/.config/bashtop/bashtop.cfg**\n> User configuration file. Controls theme, update interval, process sorting, and display options."],["CAVEATS","Written in pure bash, with the performance limitations that implies. Requires bash 4.4 or newer. Relatively high CPU overhead for the monitoring itself. **No longer maintained**: superseded by **bpytop** (Python) and then **btop** (C++)."],["HISTORY","**bashtop** was created by aristocratos (Jakob Palme) in **2020** as a system monitor written entirely in bash, later rewritten as **bpytop** and then **btop**."],["INSTALL","```pacman: sudo pacman -S bashtop```"],["SEE ALSO","[btop](/man/btop)(1), [bpytop](/man/bpytop)(1), [htop](/man/htop)(1), [top](/man/top)(1), [glances](/man/glances)(1)"],["RESOURCES","```[Source code](https://github.com/aristocratos/bashtop)```"]],"e":["btop","bpytop","htop","top","glances"],"x":"bashtop"},"chef":{"n":"chef","t":"infrastructure automation and configuration management platform","s":[["TAGLINE","infrastructure automation and configuration management platform"],["TLDR","**Generate a new cookbook**\n\n```chef generate cookbook [cookbook_name]```\n\n**Generate a new recipe**\n\n```chef generate recipe [cookbook_name] [recipe_name]```\n\n**Verify cookbook syntax**\n\n```chef exec cookstyle [cookbook_path]```\n\n**Run cookbook locally**\n\n```chef-client --local-mode --runlist \"[recipe[cookbook::recipe]]\"```\n\n**Show installed version**\n\n```chef --version```\n\n**Install gem into Chef**\n\n```chef gem install [gem_name]```\n\n**Run InSpec tests**\n\n```chef exec inspec exec [test_path]```"],["SYNOPSIS","**chef** _command_ [_options_]"],["DESCRIPTION","**chef** is the command-line interface for Chef Workstation, a configuration management and infrastructure automation platform. It provides tools for developing, testing, and managing cookbooks that define system configurations.\n\nCookbooks contain recipes written in Ruby DSL that describe desired system states. Chef applies these recipes to nodes, ensuring consistent configuration across infrastructure. The workflow includes local development, testing with Test Kitchen, and deployment to Chef Server."],["PARAMETERS","**generate** _type_ _name_\n> Generate cookbook, recipe, template, or other component.\n\n**exec** _command_\n> Run command in Chef context with bundled Ruby.\n\n**gem** _subcommand_\n> Manage Ruby gems in Chef's environment.\n\n**install** _policyfile_\n> Install cookbooks from Policyfile.\n\n**update** _policyfile_\n> Update cookbooks in Policyfile.\n\n**export** _policyfile_ _path_\n> Export policy archive for deployment.\n\n**push** _policy_group_ _policyfile_\n> Push policy to Chef Server.\n\n**--version**\n> Show version information.\n\n**--help**\n> Display help information."],["CONFIGURATION","**~/.chef/config.rb**\n> Main Chef Workstation configuration file. Sets default Chef Server URL, client key path, cookbook paths, and node name.\n\n**~/.chef/credentials**\n> Stores Chef Server credentials and profiles for managing multiple organizations.\n\n**/etc/chef/client.rb**\n> Chef client configuration on managed nodes. Sets server URL, node name, validation key, and run list intervals."],["CAVEATS","Requires Ruby knowledge for recipe development. Chef Server setup needed for centralized management. Local mode testing requires sufficient resources. Cookbooks may have complex dependencies."],["HISTORY","**Chef** was created by **Adam Jacob** at **Opscode** (later Chef Software) in **2009**. It pioneered the \"infrastructure as code\" concept alongside Puppet. Chef was written in Ruby and Erlang, and gained popularity for its flexibility. In **2020**, Chef was acquired by **Progress Software**. The tooling has evolved from knife-centric workflows to modern Chef Workstation."],["SEE ALSO","[knife](/man/knife)(1), [ansible](/man/ansible)(1), [puppet](/man/puppet)(1)"]],"e":["knife","ansible","puppet"],"x":"chef generate cookbook [cookbook_name]"},"chiko":{"n":"chiko","t":"TUI gRPC client for the terminal","s":[["TAGLINE","TUI gRPC client for the terminal"],["TLDR","**Launch** the interactive gRPC TUI\n\n```chiko```\n\n**Connect** with TLS but skip certificate verification\n\n```chiko -insecure```\n\n**Connect** with a custom CA certificate\n\n```chiko -cacert [/path/to/ca.pem]```\n\n**Connect** with client certificate authentication\n\n```chiko -cert [/path/to/client.pem] -key [/path/to/client-key.pem]```\n\n**Set** a connection timeout of 30 seconds\n\n```chiko -connect-timeout 30```"],["SYNOPSIS","**chiko** [_flags_]"],["PARAMETERS","**-plaintext**\n> Use plain-text HTTP/2 when connecting (no TLS). Default: true.\n\n**-insecure**\n> Skip server certificate and domain verification.\n\n**-cacert** _file_\n> File containing trusted root certificates for verifying the server.\n\n**-cert** _file_\n> File containing client certificate (public key).\n\n**-key** _file_\n> File containing client private key.\n\n**-d** _data_\n> Data for request contents.\n\n**-allow-unknown-fields**\n> Allow unknown fields in JSON request.\n\n**-connect-timeout** _seconds_\n> Maximum time in seconds to wait for connection (default: 10).\n\n**-keepalive-time** _seconds_\n> Maximum idle time before sending a keepalive probe.\n\n**-max-time** _seconds_\n> Maximum total operation time in seconds.\n\n**-max-msg-sz** _bytes_\n> Maximum encoded response message size (default: 4194304)."],["DESCRIPTION","**chiko** is a terminal user interface for interacting with gRPC services, written in Go. It combines the power of **grpcurl** with the elegance of a visual TUI. Instead of memorizing complex grpcurl command syntax, chiko provides an interactive interface for discovering services, crafting requests, and testing gRPC APIs.\n\nKey features include smart server reflection for automatic service discovery, built-in Bearer token support for authentication, rich metadata and custom header support, instant payload generation from service definitions, smart bookmarks for saving and replaying requests, and import/export of grpcurl commands."],["CAVEATS","The **-plaintext** flag defaults to true, meaning connections are unencrypted by default -- appropriate for local development but not production servers. Server reflection must be enabled on the gRPC server for automatic service discovery. Proto file import is not yet supported. Pre-stable software (v0.x)."],["HISTORY","**chiko** was created by **Felix Angga** and is written in Go under the MIT license. It was built to provide a developer-friendly visual alternative to the raw **grpcurl** command-line tool. Available via Homebrew and Go install."],["INSTALL","```brew: brew install chiko```"],["SEE ALSO","[grpcurl](/man/grpcurl)(1), [curl](/man/curl)(1), [protoc](/man/protoc)(1)"]],"e":["grpcurl","curl","protoc"],"x":"chiko"},"clang++":{"n":"clang++","t":"LLVM C++ compiler frontend","s":[["TAGLINE","LLVM C++ compiler frontend"],["TLDR","**Compile a C++ source file**\n\n```clang++ [source.cpp] -o [output]```\n\n**Compile with C++17 standard**\n\n```clang++ -std=c++17 [source.cpp] -o [output]```\n\n**Compile with optimizations**\n\n```clang++ -O2 [source.cpp] -o [output]```\n\n**Compile with debug symbols**\n\n```clang++ -g [source.cpp] -o [output]```\n\n**Compile with all warnings and treat them as errors**\n\n```clang++ -Wall -Wextra -Werror [source.cpp] -o [output]```\n\n**Define a preprocessor macro and compile**\n\n```clang++ -D[MACRO=value] [source.cpp] -o [output]```\n\n**Link with a library**\n\n```clang++ [source.cpp] -l[library] -o [output]```\n\n**Generate object file only**\n\n```clang++ -c [source.cpp] -o [source.o]```"],["SYNOPSIS","**clang++** [_options_] _files..._"],["PARAMETERS","**-o** _file_\n> Output file name.\n\n**-c**\n> Compile only, don't link.\n\n**-std=**_standard_\n> Set C++ standard (c++11, c++14, c++17, c++20, c++23, c++26).\n\n**-O**_level_\n> Optimization level (0, 1, 2, 3, s, z).\n\n**-g**\n> Generate debug information.\n\n**-Wall**\n> Enable most warnings.\n\n**-Wextra**\n> Enable extra warnings.\n\n**-I** _path_\n> Add include directory.\n\n**-L** _path_\n> Add library search path.\n\n**-l** _library_\n> Link with library.\n\n**-D** _macro=value_\n> Define preprocessor macro.\n\n**-Werror**\n> Treat warnings as errors.\n\n**-Wpedantic**\n> Issue warnings demanded by strict ISO C++ compliance.\n\n**-stdlib=**_library_\n> Specify C++ standard library (libc++ or libstdc++)."],["DESCRIPTION","**clang++** is the Clang C++ compiler frontend, part of the LLVM project. It compiles C++ source code to executable binaries or object files. Clang provides fast compilation, expressive diagnostics, and GCC compatibility.\n\nIt supports modern C++ standards and provides advanced static analysis and tooling integration."],["CAVEATS","Some GCC-specific extensions may not be supported. Use **-stdlib=libc++** for LLVM's libc++ or **-stdlib=libstdc++** for GNU's standard library."],["INSTALL","```apt: sudo apt install clang```\n\n```dnf: sudo dnf install clang```\n\n```pacman: sudo pacman -S clang```\n\n```apk: sudo apk add clang22```\n\n```zypper: sudo zypper install clang```\n\n```nix: nix profile install nixpkgs#clang```"],["SEE ALSO","[clang](/man/clang)(1), [g++](/man/g++)(1), [clang-format](/man/clang-format)(1)"]],"e":["clang","g++","clang-format"],"x":"clang++ [source.cpp] -o [output]"},"codium":{"n":"codium","t":"privacy-focused VS Code without Microsoft telemetry","s":[["TAGLINE","privacy-focused VS Code without Microsoft telemetry"],["TLDR","**Open current directory** in VSCodium\n\n```codium .```\n\n**Open specific files**\n\n```codium [file1.js] [file2.js]```\n\n**Open file at specific line** and column\n\n```codium -g [file.js:25:10]```\n\n**Compare two files** in diff view\n\n```codium -d [file1.js] [file2.js]```\n\n**Install an extension**\n\n```codium --install-extension [ms-python.python]```\n\n**List installed extensions**\n\n```codium --list-extensions```\n\n**Open in new window**\n\n```codium -n [path/to/project]```"],["SYNOPSIS","**codium** [_options_] [_paths_...]"],["PARAMETERS","**-n**, **--new-window**\n> Open a new VSCodium window.\n\n**-r**, **--reuse-window**\n> Reuse the last active window.\n\n**-g**, **--goto** _file:line[:column]_\n> Open file at specific line and optional column position.\n\n**-d**, **--diff** _file1_ _file2_\n> Open diff editor comparing two files.\n\n**-m**, **--merge** _path1_ _path2_ _base_ _result_\n> Perform three-way merge.\n\n**-w**, **--wait**\n> Wait for files to be closed before returning.\n\n**--locale** _locale_\n> Set display language.\n\n**--user-data-dir** _dir_\n> Specify directory for user data; enables isolated instances.\n\n**--extensions-dir** _dir_\n> Set root path for extensions.\n\n**--disable-extensions**\n> Disable all installed extensions.\n\n**--disable-gpu**\n> Disable GPU hardware acceleration.\n\n**--inspect-extensions** _port_\n> Debug extensions at specified port.\n\n**-s**, **--status**\n> Print process usage and diagnostics.\n\n**--verbose**\n> Print verbose output.\n\n**-v**, **--version**\n> Display version information.\n\n**-h**, **--help**\n> Show help information."],["EXTENSION COMMANDS","**--install-extension** _id_\n> Install or update extension by ID from Open VSX Registry.\n\n**--uninstall-extension** _id_\n> Remove an installed extension.\n\n**--list-extensions**\n> Show all installed extensions.\n\n**--show-versions**\n> Display extension version numbers with --list-extensions.\n\n**--update-extensions**\n> Update all installed extensions."],["DESCRIPTION","**codium** is the command-line interface for VSCodium, a community-driven, freely-licensed binary distribution of Visual Studio Code without Microsoft telemetry and tracking. It provides the same functionality as VS Code with enhanced privacy.\n\nThe CLI is functionally identical to VS Code's **code** command. All files, folders, and multi-root workspace features work the same way. Extensions are sourced from the **Open VSX Registry** instead of Microsoft's marketplace by default.\n\nSettings are stored in **~/.config/VSCodium/User/settings.json** on Linux and extensions in **~/.vscode-oss**. This separation from VS Code allows running both editors simultaneously without conflicts.\n\nVSCodium is available as native packages for Windows, macOS, and Linux, as well as Flatpak and Snap packages. The standalone CLI is also distributed separately for headless and remote scenarios."],["CAVEATS","Some proprietary VS Code extensions (like Remote Development, Live Share, and some GitHub integrations) are not available from the Open VSX Registry. Extensions must be installed from Open VSX or manually from VSIX files. Certain Microsoft-specific features may not function identically."],["HISTORY","VSCodium was created in **2018** as a response to concerns about Microsoft's telemetry in Visual Studio Code. The project builds VS Code from the open-source **vscode** repository but removes Microsoft branding, telemetry, and licensing restrictions. It uses the **MIT license** and has grown to become the primary choice for developers seeking a fully open-source VS Code experience."],["SEE ALSO","[code](/man/code)(1), [neovim](/man/neovim)(1), [vim](/man/vim)(1)"]],"e":["code","neovim","vim"],"x":"codium ."},"cxl":{"n":"cxl","t":"Compute Express Link device management utility","s":[["TAGLINE","Compute Express Link device management utility"],["TLDR","**List CXL memory devices**\n\n```cxl list```\n\n**Show detailed device information**\n\n```cxl list -vvv```\n\n**List memory regions**\n\n```cxl list -R```\n\n**Show device health**\n\n```cxl list -H```\n\n**Enable a memory device**\n\n```cxl enable-memdev [mem0]```\n\n**Create a region**\n\n```cxl create-region -d [decoder0.0] -m [mem0]```"],["SYNOPSIS","**cxl** _command_ [_options_] [_device_]"],["PARAMETERS","**list**\n> List CXL devices, regions, or decoders.\n\n**enable-memdev** _device_\n> Enable a memory device.\n\n**disable-memdev** _device_\n> Disable a memory device.\n\n**create-region**\n> Create a memory region.\n\n**destroy-region** _region_\n> Destroy a memory region.\n\n**enable-region** _region_\n> Enable a memory region.\n\n**disable-region** _region_\n> Disable a memory region.\n\n**-m**, **--memdev** _device_\n> Specify memory device.\n\n**-d**, **--decoder** _decoder_\n> Specify decoder.\n\n**-R**, **--regions**\n> List regions.\n\n**-H**, **--health**\n> Include health information.\n\n**-v**, **--verbose**\n> Increase verbosity (repeat for more)."],["DESCRIPTION","**cxl** is the management utility for Compute Express Link (CXL) devices, a new interconnect standard for attaching memory and accelerators to CPUs. It manages CXL memory expanders, configures memory regions, and monitors device health.\n\nCXL enables disaggregated memory architectures where memory can be pooled and dynamically allocated to different hosts. The cxl tool configures how this memory appears to the operating system through regions and decoders.\n\nCommon operations include listing available CXL devices, checking health status, creating memory regions that can be used by the system, and managing the device lifecycle."],["CAVEATS","Requires kernel 5.15+ with CXL support enabled. Hardware must support CXL (Intel 4th gen Xeon, AMD EPYC, etc.). Configuration changes may require system restart. CXL is evolving technology; some features depend on specific kernel versions."],["HISTORY","CXL was developed by the **CXL Consortium**, founded in **2019** by Intel, with specifications contributed by major hardware vendors. The Linux **cxl** tool was developed as part of the **ndctl** project by **Dan Williams** at Intel. CXL 1.1 support appeared in Linux 5.12 (2021), with ongoing development adding features for newer CXL specifications (2.0, 3.0)."],["INSTALL","```apt: sudo apt install cxl```\n\n```dnf: sudo dnf install ndctl```\n\n```pacman: sudo pacman -S ndctl```\n\n```apk: sudo apk add ndctl```\n\n```zypper: sudo zypper install ndctl```"],["SEE ALSO","[ndctl](/man/ndctl)(1), [daxctl](/man/daxctl)(1), [lspci](/man/lspci)(8)"],["RESOURCES","```[Source code](https://github.com/pmem/ndctl)```\n\n```[Documentation](https://docs.pmem.io/ndctl-user-guide/)```"]],"e":["ndctl","daxctl","lspci"],"x":"cxl list"},"ddcutil":{"n":"ddcutil","t":"DDC/CI monitor control via I2C","s":[["TAGLINE","DDC/CI monitor control via I2C"],["TLDR","**List** all compatible displays\n\n```ddcutil detect```\n\n**Set brightness** to 50%\n\n```ddcutil -d [1] setvcp 10 [50]```\n\n**Increase contrast** by 5%\n\n```ddcutil -d [1] setvcp 12 + [5]```\n\n**Read** all display settings\n\n```ddcutil -d [1] getvcp ALL```"],["SYNOPSIS","**ddcutil** [_options_] _command_ [_arguments_]"],["DESCRIPTION","**ddcutil** queries and modifies display settings using the DDC/CI (Display Data Channel Command Interface) protocol over I2C bus communication. It can control brightness, contrast, input source, color temperature, and many other monitor parameters that would normally require physical button presses or OSD navigation.\n\nThe tool communicates with monitors through the I2C bus interface, which requires the i2c-dev kernel module. Each monitor exposes a set of VCP (Virtual Control Panel) features that can be read or written. Different monitors support different features, and ddcutil can query capabilities to discover what a specific monitor supports.\n\nCommon use cases include scripting brightness adjustments based on time of day, switching monitor inputs automatically, and saving/restoring monitor configurations. This is particularly valuable for multi-monitor setups where manual adjustment would be tedious, or for accessibility scenarios requiring programmatic control of display properties. The tool also enables remote monitor management over SSH for headless systems."],["PARAMETERS","**detect**\n> List connected DDC-capable displays\n\n**getvcp** _feature_\n> Read a VCP feature value\n\n**setvcp** _feature_ _value_\n> Set a VCP feature value\n\n**-d, --display** _num_\n> Select display number\n\n**vcpinfo** _feature_\n> Show VCP feature information\n\n**capabilities**\n> Query display capabilities"],["VCP FEATURES","**0x10**\n> Brightness\n\n**0x12**\n> Contrast\n\n**0x60**\n> Input source\n\n**ALL**\n> All supported features"],["CAVEATS","Requires the i2c-dev module: `modprobe i2c-dev`. May need to add user to i2c group. Not all monitors support DDC/CI. Some features may be read-only."],["INSTALL","```apt: sudo apt install ddcutil```\n\n```dnf: sudo dnf install ddcutil```\n\n```pacman: sudo pacman -S ddcutil```\n\n```apk: sudo apk add ddcutil```\n\n```zypper: sudo zypper install ddcutil```\n\n```brew: brew install ddcutil```\n\n```nix: nix profile install nixpkgs#ddcutil```"],["SEE ALSO","[xrandr](/man/xrandr)(1), [modprobe](/man/modprobe)(8)"],["RESOURCES","```[Source code](https://github.com/rockowitz/ddcutil)```\n\n```[Documentation](https://www.ddcutil.com)```"]],"e":["xrandr","modprobe"],"x":"ddcutil detect"},"docker-buildx-prune":{"n":"docker-buildx-prune","t":"Remove Docker Buildx build cache","s":[["TAGLINE","Remove Docker Buildx build cache"],["TLDR","**Prune all build cache** with confirmation prompt\n\n```docker buildx prune```\n\n**Force prune** without confirmation\n\n```docker buildx prune -f```\n\n**Prune all cache** including internal and frontend images\n\n```docker buildx prune --all```\n\n**Prune cache** older than 24 hours\n\n```docker buildx prune --filter until=[24h]```\n\n**Prune cache** and keep at most 2 GB\n\n```docker buildx prune --max-used-space [2gb]```\n\n**Prune cache** ensuring at least 10 GB free disk space\n\n```docker buildx prune --min-free-space [10gb]```"],["SYNOPSIS","**docker buildx prune** [_options_]"],["PARAMETERS","**-a**, **--all**\n> Remove all cache including internal and frontend images.\n\n**-f**, **--force**\n> Skip the confirmation prompt.\n\n**--filter** _key=value_\n> Filter cache records to prune (e.g., until=24h, type, inuse, shared).\n\n**--max-used-space** _size_\n> Maximum total disk space for the cache (e.g., 2gb, 512mb).\n\n**--min-free-space** _size_\n> Target amount of free disk space after pruning.\n\n**--reserved-space** _size_\n> Minimum disk space permanently reserved for cache.\n\n**--timeout** _duration_\n> Override default timeout for loading builder status (default: 20s).\n\n**--verbose**\n> Show detailed output."],["DESCRIPTION","**docker buildx prune** clears the build cache of the currently selected builder instance. By default it removes only reclaimable cache entries, prompting for confirmation. With **--all**, it also removes internal and frontend images.\n\nThe space management flags (**--max-used-space**, **--min-free-space**, **--reserved-space**) allow fine-grained control over disk usage. The **--filter** flag supports selectors like **until**, **id**, **type**, **inuse**, **mutable**, **shared**, and **private**, combined with AND logic."],["CAVEATS","Without **--all**, internal images and frontend cache are preserved. Space flags accept human-readable values (e.g., 128mb, 2gb). When multiple space flags are specified, all constraints are honored simultaneously."],["INSTALL","```apt: sudo apt install docker-cli```\n\n```dnf: sudo dnf install docker-cli```\n\n```pacman: sudo pacman -S docker```\n\n```apk: sudo apk add docker-cli```\n\n```zypper: sudo zypper install docker```\n\n```brew: brew install docker```\n\n```nix: nix profile install nixpkgs#docker```"],["SEE ALSO","[docker-buildx-du](/man/docker-buildx-du)(1), [docker-buildx-rm](/man/docker-buildx-rm)(1), [docker-build](/man/docker-build)(1), [docker](/man/docker)(1)"]],"e":["docker-buildx-du","docker-buildx-rm","docker-build","docker"],"x":"docker buildx prune"},"dockerd":{"n":"dockerd","t":"Docker daemon background service","s":[["TAGLINE","Docker daemon background service"],["TLDR","Run Docker **daemon**\n\n```dockerd```\n\nListen on **specific sockets**\n\n```dockerd -H unix://[path/to/tmp.sock] -H tcp://[ip_address]```\n\nSpecify **PID file**\n\n```dockerd -p [path/to/pid_file]```\n\nRun in **debug mode**\n\n```dockerd -D```\n\nSet **log level**\n\n```dockerd -l [debug|info|warn|error|fatal]```"],["SYNOPSIS","**dockerd** [_options_]"],["DESCRIPTION","**dockerd** is the Docker daemon process that manages containers, images, networks, and volumes. It's a persistent background service that listens for Docker API requests.\n\nUsually managed by systemd rather than run directly."],["PARAMETERS","**-H, --host** _socket_\n> Socket to listen on (unix://, tcp://, fd://)\n\n**-p, --pidfile** _file_\n> PID file path\n\n**-D, --debug**\n> Enable debug mode\n\n**-l, --log-level** _level_\n> Logging level\n\n**--storage-driver** _driver_\n> Storage driver (overlay2, devicemapper, etc.)\n\n**--data-root** _path_\n> Root directory for Docker data (default /var/lib/docker).\n\n**--tls**, **--tlsverify**\n> Use TLS (and verify remote certificates).\n\n**--tlscert** _file_, **--tlskey** _file_, **--tlscacert** _file_\n> TLS certificate, key, and trusted CA for secure daemon connections.\n\n**--userland-proxy**=_true|false_\n> Enable userland-proxy for loopback traffic to published ports.\n\n**--live-restore**\n> Keep containers alive during daemon downtime."],["CONFIGURATION","**/etc/docker/daemon.json**\n> Main configuration file for the Docker daemon. Controls storage drivers, logging, networking, registry mirrors, and runtime settings."],["CAVEATS","Requires root privileges or proper group membership. Configuration typically via /etc/docker/daemon.json. Exposing TCP socket without TLS is insecure."],["INSTALL","```apt: sudo apt install docker.io```\n\n```apk: sudo apk add docker-engine```\n\n```brew: brew install docker-engine```"],["SEE ALSO","[docker](/man/docker)(1), [systemctl](/man/systemctl)(1), [containerd](/man/containerd)(8)"]],"e":["docker","systemctl","containerd"],"x":"dockerd"},"doctl-databases-user":{"n":"doctl-databases-user","t":"manage database cluster users","s":[["TAGLINE","manage database cluster users"],["TLDR","**List database users**\n\n```doctl databases user list [cluster_id]```\n\n**Create a user**\n\n```doctl databases user create [cluster_id] [username]```\n\n**Get user info**\n\n```doctl databases user get [cluster_id] [username]```\n\n**Reset user auth** password or MySQL auth plugin\n\n```doctl databases user reset [cluster_id] [username] [new_auth_mode]```\n\n**Create a user** with a specific MySQL auth plugin\n\n```doctl databases user create [cluster_id] [username] --mysql-auth-plugin [caching_sha2_password]```\n\n**List users** with custom output format\n\n```doctl databases user list [cluster_id] --format Name,Role```\n\n**Delete a user**\n\n```doctl databases user delete [cluster_id] [username]```"],["SYNOPSIS","**doctl** **databases** **user** _command_ [_options_]"],["SUBCOMMANDS","**list**\n> Retrieve a list of database users.\n\n**create**\n> Create a database user. New users are given a role of normal and an automatically-generated password.\n\n**get**\n> Retrieve details about a database user.\n\n**delete**\n> Delete a database user.\n\n**reset**\n> Reset the auth password or the MySQL authorization plugin for a given user and return new credentials."],["PARAMETERS","**--mysql-auth-plugin** _PLUGIN_\n> Set MySQL authorization plugin (caching_sha2_password or mysql_native_password). Used with create.\n\n**--acl** _RULES_\n> Comma-separated Kafka ACL rules in topic:permission format. Used with create.\n\n**--opensearch-acl** _RULES_\n> Comma-separated OpenSearch ACL rules in index:permission format. Used with create.\n\n**--format** _COLUMNS_\n> Columns for output (e.g., Name,Role).\n\n**--no-header**\n> Omit the header row from output.\n\n**--output** _FORMAT_\n> Output format: text or json (default: text)."],["DESCRIPTION","**doctl databases user** manages users for DigitalOcean managed database clusters. Users represent authentication credentials that applications and administrators use to connect to databases.\n\nEach user has associated credentials (username and password) and potentially different privileges depending on the database engine. User management includes creating users for different applications or services, rotating credentials via password reset, and removing users when access is no longer needed.\n\nProper user management follows security best practices: creating dedicated users for each application, regularly rotating credentials, and removing unused accounts to minimize security exposure."],["INSTALL","```dnf: sudo dnf install doctl```\n\n```pacman: sudo pacman -S doctl```\n\n```apk: sudo apk add doctl```\n\n```brew: brew install doctl```\n\n```nix: nix profile install nixpkgs#doctl```"],["SEE ALSO","[doctl-databases](/man/doctl-databases)(1), [doctl-databases-pool](/man/doctl-databases-pool)(1), [doctl-databases-db](/man/doctl-databases-db)(1), [doctl-databases-firewalls](/man/doctl-databases-firewalls)(1)"]],"e":["doctl-databases","doctl-databases-pool","doctl-databases-db","doctl-databases-firewalls"],"x":"doctl databases user list [cluster_id]"},"dtlv":{"n":"dtlv","t":"Datalevin database CLI, REPL, and server","s":[["TAGLINE","Datalevin database CLI, REPL, and server"],["TLDR","**Start** an interactive Datalevin REPL\n\n```dtlv```\n\n**Show** help for all commands\n\n```dtlv help```\n\n**Run** as a network server\n\n```dtlv serv```\n\n**Copy** (backup) a database directory, compacting pages in use\n\n```dtlv -d [source_db_dir] -c copy [dest_db_dir]```\n\n**Dump** a Datalog database to a file\n\n```dtlv -d [db_dir] -g -f [dump_file] dump```\n\n**Execute** Clojure code against a database in a script\n\n```dtlv exec '(def conn (get-conn \"/tmp/db\")) (q (quote [:find ?e :where [?e :name _]]) @conn) (close conn)'```\n\n**Run** the local MCP server over stdio\n\n```dtlv mcp```"],["SYNOPSIS","**dtlv** [_options_] [_command_] [_arguments_]"],["DESCRIPTION","**dtlv** is the native command-line tool for **Datalevin**, a durable Datalog database built on LMDB. One binary covers an interactive Clojure REPL, shell-oriented database maintenance, query/transaction execution, an optional networked server, and a stdio MCP server for AI clients.\n\nWithout arguments (or with **repl**), **dtlv** starts a SCI-based interactive console where Datalevin public functions are available without requiring namespaces—the same Clojure APIs used in libraries. **exec** runs code from arguments or stdin for scripting. Maintenance commands include **copy** (hot-capable backup, optional compact), **dump**/**load** (text or nippy export/import), **drop**, and **stat**. **serv** listens for client connections (default **127.0.0.1:8898**, data root **/var/lib/datalevin** on POSIX). **mcp** exposes Datalevin over the Model Context Protocol.\n\nInstall via Homebrew (**huahaiy/brew/datalevin**), release binaries, Docker (**huahaiy/datalevin**), or the JVM uberjar. The same executable can also load as a Babashka pod."],["PARAMETERS","**repl**\n> Enter the interactive shell (default when no command is given).\n\n**exec** [_code_]\n> Execute Datalevin/Clojure code from the argument or standard input.\n\n**copy**\n> Copy a database directory. Requires **-d** source path; destination is the argument. **-c**/**--compact** copies only pages in use.\n\n**dump**\n> Dump database content to stdout or **-f** file. **-g** Datalog-only; **-n** nippy binary; **-a** all sub-databases; **-l** list sub-database names.\n\n**load**\n> Load data from stdin or **-f** into the database at **-d**.\n\n**drop**\n> Clear or (**-D**) delete sub-databases.\n\n**stat**\n> Display database statistics.\n\n**serv**\n> Run as a server. **--host**, **-p**/**--port**, **-r**/**--root**, **-v** verbose.\n\n**mcp**\n> Run an MCP server over stdio. **-w**/**--allow-writes** enables write tools (off by default).\n\n**help** [_command_]\n> Show usage.\n\n**-d**, **--dir** _PATH_\n> Database directory path.\n\n**-f**, **--file** _PATH_\n> Input/output file for dump/load and related operations.\n\n**-V**, **--version**\n> Show Datalevin version and exit."],["CAVEATS","Native **dtlv** is built with GraalVM SerialGC and may be less suitable than the JVM uberjar for long-running, highly concurrent servers. Binding **serv** to a non-loopback address requires **DATALEVIN_DEFAULT_PASSWORD**. Default server account credentials must be changed for any exposed deployment. **mcp** write tools stay disabled until **--allow-writes**."],["HISTORY","Datalevin is an open-source Datalog store (Eclipse Public License) by Huahai Yang and contributors, developed for simple durable Datalog with competitive query performance. The **dtlv** native image and server mode landed early in the project roadmap and remain the primary ops interface."],["SEE ALSO","[bb](/man/bb)(1), [sqlite3](/man/sqlite3)(1), [java](/man/java)(1)"],["RESOURCES","```[Source code](https://github.com/datalevin/datalevin)```\n\n```[Homepage](https://datalevin.org)```\n\n```[Documentation](https://github.com/datalevin/datalevin/blob/master/doc/dtlv.md)```"]],"e":["bb","sqlite3","java"],"x":"dtlv"},"eg":{"n":"eg","t":"Command-line tool for useful command examples","s":[["TAGLINE","Command-line tool for useful command examples"],["TLDR","**Show** examples for a command\n\n```eg [command]```\n\n**Show** examples for a subcommand\n\n```eg git log```\n\n**List** all available example topics\n\n```eg --list```\n\n**Use a custom examples directory**\n\n```eg --examples-dir [path/to/examples] [command]```\n\n**Use a custom pager** for output\n\n```eg --pager-cmd [less] [command]```"],["SYNOPSIS","**eg** [_options_] _command_"],["PARAMETERS","**--list**\n> List all available commands with examples.\n\n**--color**\n> Colorize output.\n\n**--squeeze**\n> Remove blank lines from output.\n\n**--pager-cmd** _PAGER_\n> Use specified pager program for output.\n\n**--examples-dir** _DIR_\n> Use custom examples directory.\n\n**--custom-dir** _DIR_\n> Use custom user-provided examples directory.\n\n**-v**, **--version**\n> Display version and exit.\n\n**-h**, **--help**\n> Display help and exit."],["DESCRIPTION","**eg** is a command-line tool that provides practical examples for common commands. Unlike traditional man pages that focus on comprehensive documentation, eg shows the most common and useful usage patterns with clear explanations.\n\nWritten in Python, eg works out of the box with no configuration required. It depends only on standard Python libraries and supports both Python 2 and 3. Users can add custom examples by placing files in a custom directory alongside the built-in examples.\n\nThe tool is particularly helpful for developers who know what they want to do but need a quick reminder of the exact syntax. It covers popular tools like git, docker, and many Unix utilities."],["CAVEATS","Example database may not cover all command variations. Examples prioritize common use cases over edge cases. Updates to built-in examples require updating the tool itself. Custom examples can supplement but not override built-in ones by default."],["HISTORY","**eg** was created by **Sam Sudar** to provide a more practical alternative to man pages, focusing on real-world usage examples that developers actually need in their daily work. The source is available on GitHub at **github.com/srsudar/eg**."],["INSTALL","```dnf: sudo dnf install eg```\n\n```brew: brew install eg```"],["SEE ALSO","[man](/man/man)(1), [tldr](/man/tldr)(1), [cheat](/man/cheat)(1)"]],"e":["man","tldr","cheat"],"x":"eg [command]"},"filecheck":{"n":"filecheck","t":"LLVM pattern-matching testing tool","s":[["TAGLINE","LLVM pattern-matching testing tool"],["TLDR","**Run FileCheck on** file\n\n```FileCheck [check-file] < [input]```\n\n**Check with patterns** from stdin\n\n```FileCheck --input-file=[input] [check-file]```\n\n**Enable verbose output**\n\n```FileCheck -v [check-file] < [input]```\n\n**Require full line matches**\n\n```FileCheck --match-full-lines [check-file] < [input]```"],["SYNOPSIS","**FileCheck** [_options_] _check-file_"],["PARAMETERS","_CHECK-FILE_\n> File containing CHECK directives.\n\n**--input-file** _FILE_\n> Input file to verify.\n\n**-v**, **--verbose**\n> Print good matches.\n\n**--match-full-lines**\n> Require full line matches.\n\n**--strict-whitespace**\n> Strict whitespace matching.\n\n**--check-prefix** _PREFIX_\n> Use custom check prefix (default: CHECK).\n\n**--check-prefixes** _PREFIXES_\n> Comma-separated list of check prefixes.\n\n**-vv**\n> Print information useful for diagnostic analysis.\n\n**--dump-input** _MODE_\n> Dump annotated input on failure (help, always, fail, never).\n\n**--allow-empty**\n> Allow the check file to be empty.\n\n**--help**\n> Display help information."],["DESCRIPTION","**FileCheck** is an LLVM testing tool that verifies text output matches specified patterns. It reads a file containing CHECK directives and verifies they match against input, commonly compiler output.\n\nThe tool supports various check types: CHECK for sequential matching, CHECK-NEXT for consecutive lines, CHECK-NOT for absence, CHECK-SAME for same-line matches, CHECK-DAG for unordered matches, CHECK-LABEL for scoping, and CHECK-EMPTY for blank lines.\n\nFileCheck is essential for LLVM and compiler testing, validating generated code matches expected patterns."],["CAVEATS","Pattern syntax uses a subset of regular expressions with LLVM-specific extensions like variable captures (`[[VAR:regex]]`). Whitespace is canonicalized by default (multiple spaces match one). Primarily designed for LLVM/compiler testing but usable for any text verification."],["HISTORY","FileCheck was developed as part of **LLVM's testing infrastructure**. It provides a flexible pattern-matching system for validating compiler output in regression tests."],["INSTALL","```apk: sudo apk add filecheck```\n\n```nix: nix profile install nixpkgs#filecheck```"],["SEE ALSO","[grep](/man/grep)(1), [diff](/man/diff)(1)"]],"e":["grep","diff"],"x":"FileCheck [check-file] < [input]"},"flatpak-install":{"n":"flatpak-install","t":"install applications from Flatpak repositories","s":[["TAGLINE","install applications from Flatpak repositories"],["TLDR","**Install an application** from configured remotes\n\n```flatpak install [application_id]```\n\n**Install from a specific remote**\n\n```flatpak install [flathub] [org.gnome.gedit]```\n\n**Install for current user** only\n\n```flatpak install --user [application_id]```\n\n**Install from a .flatpakref** file\n\n```flatpak install --from [https://example.com/app.flatpakref]```\n\n**Install from a local bundle** file\n\n```flatpak install --bundle [path/to/app.flatpak]```\n\n**Install or update** if already installed\n\n```flatpak install --or-update [application_id]```\n\n**Install without confirmation** prompts\n\n```flatpak install -y [application_id]```"],["SYNOPSIS","**flatpak install** [_OPTIONS_] [_REMOTE_] _REF_...\n\n**flatpak install** [_OPTIONS_] [**--from**|**--bundle**] _LOCATION_"],["PARAMETERS","**-u**, **--user**\n> Install in per-user installation directory.\n\n**--system**\n> Install in default system-wide installation.\n\n**--installation**=_NAME_\n> Install in a specific system-wide installation.\n\n**--reinstall**\n> Uninstall first if already installed.\n\n**--or-update**\n> Update silently if already installed.\n\n**--bundle**\n> Treat location as a single-bundle .flatpak file.\n\n**--from**\n> Treat location as a .flatpakref application description.\n\n**-y**, **--assumeyes**\n> Automatically answer yes to all prompts.\n\n**--no-deploy**\n> Download but do not deploy.\n\n**--no-pull**\n> Deploy from local cache without downloading.\n\n**-v**, **--verbose**\n> Print debug information.\n\n**-h**, **--help**\n> Show help and exit."],["DESCRIPTION","**flatpak install** installs applications or runtimes from Flatpak repositories. The REF argument specifies what to install using the format **(app|runtime)/ID/ARCH/BRANCH**. Typically only the application ID is needed; other parts are matched automatically or prompt for selection when ambiguous.\n\nInstallation sources include configured remotes (like Flathub), .flatpakref files (application description URLs), .flatpak bundle files, or OCI images. By default, applications install system-wide; use **--user** for per-user installation.\n\nDependencies (runtimes, extensions) are automatically resolved and installed. The command prompts for confirmation showing what will be downloaded and installed."],["CAVEATS","System-wide installation requires root privileges or polkit authentication. Per-user installations are stored in ~/.local/share/flatpak/. Some applications require specific runtimes that will be installed automatically. Repository remotes must be configured before installing from them."],["HISTORY","Flatpak was developed by Red Hat's Alexander Larsson, with the first stable release in **2015** (originally called xdg-app). It provides sandboxed application distribution independent of Linux distributions. Flathub, the primary Flatpak repository, launched in **2017** and has become the largest source of Flatpak applications."],["INSTALL","```apt: sudo apt install flatpak```\n\n```dnf: sudo dnf install flatpak```\n\n```pacman: sudo pacman -S flatpak```\n\n```apk: sudo apk add flatpak```\n\n```zypper: sudo zypper install flatpak```\n\n```nix: nix profile install nixpkgs#flatpak```"],["SEE ALSO","[flatpak](/man/flatpak)(1), [flatpak-update](/man/flatpak-update)(1), [flatpak-run](/man/flatpak-run)(1)"]],"e":["flatpak","flatpak-update","flatpak-run"],"x":"flatpak install [application_id]"},"freshclam":{"n":"freshclam","t":"ClamAV virus database updater","s":[["TAGLINE","ClamAV virus database updater"],["TLDR","**Update virus definitions**\n\n```freshclam```\n\n**Check for updates** only\n\n```freshclam --check```\n\n**Run in daemon** mode\n\n```freshclam -d```\n\n**Verbose output**\n\n```freshclam -v```\n\n**Use specific config**\n\n```freshclam --config-file=[/path/freshclam.conf]```"],["SYNOPSIS","**freshclam** [_options_]"],["PARAMETERS","**-d**, **--daemon**\n> Run as background daemon.\n\n**-c** _N_, **--checks** _N_\n> Daily checks when running as daemon.\n\n**-v**, **--verbose**\n> Verbose output.\n\n**--check**\n> Check for updates without downloading.\n\n**--config-file** _FILE_\n> Configuration file path.\n\n**-l** _FILE_, **--log** _FILE_\n> Log file path.\n\n**-F**, **--foreground**\n> When combined with **-d**, stay in the foreground instead of detaching.\n\n**-u** _USER_, **--user** _USER_\n> When started as root, drop privileges to the given user.\n\n**--datadir** _PATH_\n> Override the database directory (default `/var/lib/clamav`).\n\n**--stdout**\n> Write all log output to stdout.\n\n**--show-progress**\n> Show a progress bar during downloads.\n\n**--quiet**\n> Only print error messages.\n\n**--no-warnings**\n> Suppress warning messages.\n\n**--update-db** _NAME_\n> Only update the named database (`main`, `daily`, `bytecode`).\n\n**--on-update-execute** _CMD_\n> Run _CMD_ after a successful update.\n\n**--on-error-execute** _CMD_\n> Run _CMD_ when an update fails.\n\n**-V**, **--version**\n> Print version information and exit.\n\n**--help**\n> Display help information."],["CONFIGURATION","**/etc/clamav/freshclam.conf**\n> Main configuration file controlling database mirrors, update frequency, proxy settings, and notification options."],["DESCRIPTION","**freshclam** downloads and updates ClamAV virus signature databases. It connects to ClamAV mirrors to fetch the latest malware definitions for the clamscan and clamd scanners.\n\nThe tool can run as a daemon for automatic updates or be triggered manually. Multiple mirrors provide redundancy. Downloaded databases are verified before installation.\n\nfreshclam is essential for keeping ClamAV effective against new threats."],["CAVEATS","Requires network access to mirrors. Download frequency limits apply. Large database downloads on first run."],["HISTORY","freshclam is part of **ClamAV**, the open source antivirus engine. ClamAV was created in 2001 and acquired by Sourcefire (later Cisco), providing free virus scanning for Unix systems."],["INSTALL","```apt: sudo apt install clamav-freshclam```\n\n```dnf: sudo dnf install clamav-freshclam```\n\n```apk: sudo apk add freshclam```"],["SEE ALSO","[clamscan](/man/clamscan)(1), [clamd](/man/clamd)(8), [clamdscan](/man/clamdscan)(1)"]],"e":["clamscan","clamd","clamdscan"],"x":"freshclam"},"git-mv":{"n":"git-mv","t":"Move or rename tracked files","s":[["TAGLINE","Move or rename tracked files"],["TLDR","**Rename a file**\n\n```git mv [old-name.txt] [new-name.txt]```\n\n**Move file to directory**\n\n```git mv [file.txt] [directory/]```\n\n**Force overwrite**\n\n```git mv -f [source] [destination]```\n\n**Move multiple files to a directory**\n\n```git mv [file1.txt] [file2.txt] [directory/]```\n\n**Dry run**\n\n```git mv -n [source] [destination]```"],["SYNOPSIS","**git mv** [_options_] _source_ _destination_\n**git mv** [_options_] _source_... _destination-directory_"],["PARAMETERS","_SOURCE_\n> File or directory to move.\n\n_DESTINATION_\n> Target path or directory.\n\n**-f**, **--force**\n> Force move/rename even if the destination exists.\n\n**-k**\n> Skip move or rename actions that would lead to an error condition.\n\n**-n**, **--dry-run**\n> Show what would happen.\n\n**-v**, **--verbose**\n> Report moved files.\n\n**--help**\n> Display help information."],["DESCRIPTION","**git mv** moves or renames files and directories while updating the Git index. It is equivalent to moving the file with `mv`, deleting the old path with `git rm`, and adding the new path with `git add`.\n\nUsing this command ensures the rename is properly staged for the next commit. While Git can detect renames automatically through content analysis, using `git mv` makes the intent explicit and updates the index in one step."],["CAVEATS","Actually a convenience wrapper. Git detects renames anyway. History follows content, not command."],["HISTORY","git mv is a core **Git** command providing explicit rename/move tracking, though git can detect renames automatically through content analysis."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-rm](/man/git-rm)(1), [git-add](/man/git-add)(1), [mv](/man/mv)(1)"]],"e":["git-rm","git-add","mv"],"x":"git mv [old-name.txt] [new-name.txt]"},"grolbp":{"n":"grolbp","t":"Groff Canon LBP printer driver","s":[["TAGLINE","Groff Canon LBP printer driver"],["TLDR","**Generate Canon LBP output**\n\n```groff -Tlbp [file.roff] | lpr -P[printer]```\n\n**Direct grolbp usage**\n\n```grolbp [file] > output.lbp```\n\n**Specify font**\n\n```grolbp -F [fontdir] [file]```"],["SYNOPSIS","**grolbp** [_options_] [_file_]"],["PARAMETERS","_FILE_\n> Groff intermediate output file.\n\n**-c** _N_, **--copies** _N_\n> Print N copies of each page.\n\n**-F** _DIR_, **--fontdir** _DIR_\n> Prepend directory to the font search path.\n\n**-l**, **--landscape**\n> Format the document in landscape orientation.\n\n**-o** _ORIENTATION_, **--orientation** _ORIENTATION_\n> Set orientation to portrait or landscape.\n\n**-p** _SIZE_, **--papersize** _SIZE_\n> Set paper size (A4, letter, legal, executive).\n\n**-w** _WIDTH_, **--linewidth** _WIDTH_\n> Set default line thickness in thousandths of an em (default: 40).\n\n**-v**, **--version**\n> Display version information.\n\n**-h**, **--help**\n> Display help information."],["DESCRIPTION","**grolbp** is a groff output driver that produces output in CAPSL and VDM format suitable for Canon LBP-4 and LBP-8 series laser printers. It supports various paper sizes, orientations, and copy counts.\n\nThe driver processes groff intermediate output and produces printer-specific commands for direct printing to Canon LBP hardware."],["CAVEATS","Part of groff package. Specific to Canon LBP printers. May require printer-specific configuration."],["HISTORY","grolbp was developed as part of **GNU groff** to support Canon laser beam printers."],["INSTALL","```apt: sudo apt install groff```\n\n```dnf: sudo dnf install groff```\n\n```pacman: sudo pacman -S groff```\n\n```apk: sudo apk add groff```\n\n```zypper: sudo zypper install groff```\n\n```brew: brew install groff```\n\n```nix: nix profile install nixpkgs#groff```"],["SEE ALSO","[groff](/man/groff)(1), [troff](/man/troff)(1), [grops](/man/grops)(1), [grolj4](/man/grolj4)(1), [lpr](/man/lpr)(1)"]],"e":["groff","troff","grops","grolj4","lpr"],"x":"groff -Tlbp [file.roff] | lpr -P[printer]"},"groupmems":{"n":"groupmems","t":"manage group membership","s":[["TAGLINE","manage group membership"],["TLDR","**Add user to group**\n\n```sudo groupmems -g [group] -a [user]```\n\n**Remove user from group**\n\n```sudo groupmems -g [group] -d [user]```\n\n**List group members**\n\n```sudo groupmems -g [group] -l```\n\n**Purge all members**\n\n```sudo groupmems -g [group] -p```"],["SYNOPSIS","**groupmems** [_options_]"],["PARAMETERS","**-g**, **--group** _GROUP_\n> Target group name. Required for superuser; regular users operate on their primary group.\n\n**-a**, **--add** _USER_\n> Add user to group.\n\n**-d**, **--delete** _USER_\n> Delete user from group.\n\n**-l**, **--list**\n> List group members.\n\n**-p**, **--purge**\n> Purge all members from the group.\n\n**-R**, **--root** _DIR_\n> Apply changes in the specified chroot directory.\n\n**-h**, **--help**\n> Display help information."],["DESCRIPTION","**groupmems** manages group membership. It allows administrators to add or remove users from groups without directly editing /etc/group.\n\nThe tool provides a safe interface for group membership changes. Regular users can manage their own primary group without superuser privileges; only the superuser can modify other groups using the **-g** option."],["CAVEATS","Requires root or primary group ownership. Part of the shadow-utils package. Changes take effect on next login. If /etc/gshadow exists, entries are updated automatically."],["HISTORY","groupmems was developed as part of the **shadow** password suite for secure group management."],["INSTALL","```pacman: sudo pacman -S shadow```\n\n```apk: sudo apk add shadow```\n\n```zypper: sudo zypper install shadow```\n\n```nix: nix profile install nixpkgs#shadow```"],["SEE ALSO","[groups](/man/groups)(1), [gpasswd](/man/gpasswd)(1), [usermod](/man/usermod)(8), [groupadd](/man/groupadd)(8), [groupmod](/man/groupmod)(8)"]],"e":["groups","gpasswd","usermod","groupadd","groupmod"],"x":"sudo groupmems -g [group] -a [user]"},"grub2-mkpasswd-pbkdf2":{"n":"grub2-mkpasswd-pbkdf2","t":"generate PBKDF2 password hash for GRUB protection","s":[["TAGLINE","generate PBKDF2 password hash for GRUB protection"],["TLDR","Create a **password hash** for GRUB 2\n\n```sudo grub2-mkpasswd-pbkdf2```\n\nCreate hash with custom **iteration count** and **salt length**\n\n```sudo grub2-mkpasswd-pbkdf2 -c [10000] -s [64]```"],["SYNOPSIS","**grub2-mkpasswd-pbkdf2** [_options_]"],["PARAMETERS","**-c**, **--iteration-count** _COUNT_\n> Number of PBKDF2 iterations (higher is more secure but slower). Defaults to 10000.\n\n**-s**, **--salt** _LENGTH_\n> Length of the salt in bytes. Defaults to 64.\n\n**-l**, **--buflen** _LENGTH_\n> Length of the generated hash in bytes. Defaults to 64."],["DESCRIPTION","**grub2-mkpasswd-pbkdf2** generates hashed passwords for protecting GRUB 2 bootloader configuration. The output hash can be used in grub.cfg to password-protect boot entries or the GRUB command line.\n\nPBKDF2 (Password-Based Key Derivation Function 2) provides strong protection against brute-force attacks by using multiple iterations and a random salt. The resulting hash is safe to store in configuration files."],["CAVEATS","Requires root privileges to run. The generated hash must be manually added to GRUB configuration. Higher iteration counts increase security but also boot-time password verification delay. On RHEL/Fedora the tool is named **grub2-mkpasswd-pbkdf2** to match the grub2 package naming; it is the same tool upstream calls **grub-mkpasswd-pbkdf2**."],["HISTORY","grub2-mkpasswd-pbkdf2 is part of GRUB 2. Password protection for GRUB was introduced to prevent unauthorized users from modifying boot parameters or accessing single-user mode. PBKDF2 replaced simpler hashing methods for improved security."],["SEE ALSO","[grub-mkconfig](/man/grub-mkconfig)(8), [grub-install](/man/grub-install)(8), [grub-editenv](/man/grub-editenv)(1)"],["RESOURCES","```[Homepage](https://www.gnu.org/software/grub/)```\n\n```[Source code](https://git.savannah.gnu.org/cgit/grub.git)```\n\n```[Documentation](https://www.gnu.org/software/grub/manual/grub/grub.html)```"]],"e":["grub-mkconfig","grub-install","grub-editenv"],"x":"sudo grub2-mkpasswd-pbkdf2"},"gvfsd":{"n":"gvfsd","t":"GVFS daemon that handles virtual filesystem operations","s":[["TAGLINE","GVFS daemon that handles virtual filesystem operations"],["TLDR","**Start GVFS daemon**\n\n```gvfsd```\n\n**Replace existing daemon**\n\n```gvfsd --replace```\n\n**Debug mode**\n\n```gvfsd --debug```\n\n**No fuse support**\n\n```gvfsd --no-fuse```"],["SYNOPSIS","**gvfsd** [_options_]"],["PARAMETERS","**--replace**\n> Replace running daemon.\n\n**--no-fuse**\n> Disable FUSE support.\n\n**--debug**\n> Enable debug output.\n\n**-h**, **--help**\n> Display help information.\n\n**--version**\n> Show version number and exit."],["DESCRIPTION","**gvfsd** is the main daemon for the GVFS virtual filesystem. It provides the `org.gtk.vfs.Daemon` name on the session bus and is autostarted by GIO clients if not already running.\n\nThe primary task of gvfsd is to act as a mount tracker/manager. It spawns new backends when requested, keeps track of their lifecycle, maintains a list of active mounts, and creates direct connections to them. GVFS backends run as children of the gvfsd process."],["CAVEATS","Usually auto-started by D-Bus. Requires proper D-Bus session. GNOME specific."],["HISTORY","gvfsd was developed as part of **GVFS** for the **GNOME** desktop environment to provide virtual filesystem support."],["SEE ALSO","[gvfs-open](/man/gvfs-open)(1), [gvfsd-fuse](/man/gvfsd-fuse)(1), [gio](/man/gio)(1), [dbus-daemon](/man/dbus-daemon)(1)"]],"e":["gvfs-open","gvfsd-fuse","gio","dbus-daemon"],"x":"gvfsd"},"hledger-balance":{"n":"hledger-balance","t":"shows account balances from the journal","s":[["TAGLINE","shows account balances from the journal"],["TLDR","**Show all balances**\n\n```hledger balance```\n\n**Show assets only**\n\n```hledger balance assets```\n\n**Show with hierarchy**\n\n```hledger balance --tree```\n\n**Show flat list**\n\n```hledger balance --flat```\n\n**Monthly breakdown**\n\n```hledger balance -M```\n\n**Show percentages**\n\n```hledger balance --percent```"],["SYNOPSIS","**hledger balance** [_options_] [_patterns_]"],["PARAMETERS","_PATTERNS_\n> Account name patterns.\n\n**--tree**\n> Hierarchical display.\n\n**--flat**\n> Flat account list.\n\n**-M**, **--monthly**\n> Monthly breakdown.\n\n**-Q**, **--quarterly**\n> Quarterly breakdown.\n\n**-Y**, **--yearly**\n> Yearly breakdown.\n\n**--percent**\n> Show percentages.\n\n**--depth** _N_\n> Limit account depth.\n\n**--help**\n> Display help information."],["DESCRIPTION","**hledger balance** shows account balances from the journal. It summarizes debits and credits across all matching accounts.\n\nThe command supports filtering by account pattern, time period, and display format. It's the primary tool for checking financial status."],["CAVEATS","Zero balances hidden by default. Requires valid journal. Part of hledger suite."],["HISTORY","Balance is a core command in **hledger** for displaying account summaries in plain text accounting."],["INSTALL","```apt: sudo apt install hledger```\n\n```dnf: sudo dnf install hledger```\n\n```pacman: sudo pacman -S hledger```\n\n```apk: sudo apk add hledger```\n\n```zypper: sudo zypper install hledger```\n\n```brew: brew install hledger```\n\n```nix: nix profile install nixpkgs#hledger```"],["SEE ALSO","[hledger](/man/hledger)(1), [hledger-incomestatement](/man/hledger-incomestatement)(1)"]],"e":["hledger","hledger-incomestatement"],"x":"hledger balance"},"hoard":{"n":"hoard","t":"CLI command organizer for saving and recalling snippets","s":[["TAGLINE","CLI command organizer for saving and recalling snippets"],["TLDR","**Save a new command** to hoard\n\n```hoard new```\n\n**List and search saved commands**\n\n```hoard list```\n\n**Edit a saved command**\n\n```hoard edit```\n\n**Remove a saved command**\n\n```hoard remove```\n\n**Save a parameterized command** with placeholders\n\n```hoard new --name \"[name]\" --command \"curl -X POST #url# -d #data#\"```"],["SYNOPSIS","**hoard** _command_ [_options_]"],["DESCRIPTION","**hoard** is a CLI command organizer that saves commands you frequently use but are too complicated or long to remember. For every hoarded command, it saves the command parameterized with a customizable token (default **#**), so when you select a command, hoard asks for all missing parameters before sending the complete command to your shell.\n\nWhen a **trove.yml** file is present in the local directory, hoard loads only that file, enabling project-specific command collections. It can be installed as a shell plugin for autocomplete integration."],["CONFIGURATION","Commands are stored in a **trove.yml** file. Global configuration supports customizing the parameter token, default namespace, and shell integration settings."],["CAVEATS","Shell plugin installation is required for full autocomplete and inline execution features. Parameterized commands use a simple token replacement that doesn't support complex templating."],["HISTORY","**hoard** was created by **Hyde46** and is written in **Rust**. It was designed to solve the problem of remembering complex CLI commands by providing a quick-access organized collection with parameter substitution."],["INSTALL","```nix: nix profile install nixpkgs#hoard```"],["SEE ALSO","[pet](/man/pet)(1), [navi](/man/navi)(1), [tldr](/man/tldr)(1)"]],"e":["pet","navi","tldr"],"x":"hoard new"},"iPXE":{"n":"iPXE","t":"open-source network boot firmware","s":[["TAGLINE","open-source network boot firmware"],["TLDR","**Boot from iPXE command line**\n\n```dhcp && chain http://server/boot.ipxe```\n\n**Boot specific kernel**\n\n```kernel http://server/vmlinuz initrd=initrd.img```\n\n**Load initrd**\n\n```initrd http://server/initrd.img```\n\n**Boot loaded kernel**\n\n```boot```\n\n**Show network configuration**\n\n```ifstat```"],["SYNOPSIS","iPXE command-line or script commands"],["COMMANDS","**dhcp**\n> Obtain IP via DHCP.\n\n**ifopen**\n> Open network interface.\n\n**ifstat**\n> Show interface statistics.\n\n**kernel** _url_\n> Load kernel image.\n\n**initrd** _url_\n> Load initial ramdisk.\n\n**boot**\n> Boot loaded kernel.\n\n**chain** _url_\n> Load and execute script.\n\n**imgfree**\n> Free loaded images.\n\n**shell**\n> Enter iPXE shell.\n\n**exit**\n> Exit iPXE."],["DESCRIPTION","**iPXE** is an open-source network boot firmware. It replaces or extends PXE (Preboot Execution Environment), supporting HTTP, iSCSI, FCoE, and many other protocols for network booting.\n\niPXE can be embedded in BIOS/UEFI, burned to ROM, or chainloaded from existing PXE. It enables flexible network-based system installation and diskless booting."],["BOOT SCRIPT EXAMPLE","```\n#!ipxe\ndhcp\nkernel http://server/vmlinuz ip=dhcp\ninitrd http://server/initrd.img\nboot\n```"],["CAVEATS","Requires network boot support. HTTPS needs certificates. UEFI and BIOS need different builds. Some NICs may lack driver support."],["HISTORY","iPXE evolved from Etherboot and gPXE projects. It provides advanced network booting beyond standard PXE, supporting modern protocols and scripting capabilities."],["SEE ALSO","[pxelinux](/man/pxelinux)(1), [dnsmasq](/man/dnsmasq)(8), [tftp](/man/tftp)(1)"]],"e":["pxelinux","dnsmasq","tftp"],"x":"dhcp && chain http://server/boot.ipxe"},"ibmcloud-iam":{"n":"ibmcloud-iam","t":"manages Identity and Access Management","s":[["TAGLINE","manages Identity and Access Management"],["TLDR","**List API keys**\n\n```ibmcloud iam api-keys```\n\n**Create API key**\n\n```ibmcloud iam api-key-create [name]```\n\n**List service IDs**\n\n```ibmcloud iam service-ids```\n\n**Show access groups**\n\n```ibmcloud iam access-groups```\n\n**Get user policies**\n\n```ibmcloud iam user-policies [user]```"],["SYNOPSIS","**ibmcloud iam** _command_ [_options_]"],["PARAMETERS","_COMMAND_\n> IAM command.\n\n**api-keys**\n> List API keys.\n\n**api-key-create**\n> Create API key.\n\n**service-ids**\n> List service IDs.\n\n**access-groups**\n> List access groups.\n\n**user-policies**\n> Show user policies.\n\n**--help**\n> Display help information."],["DESCRIPTION","**ibmcloud iam** manages Identity and Access Management. It handles API keys, service IDs, access groups, and policies.\n\nThe tool controls authentication and authorization for IBM Cloud resources. It enables secure service-to-service communication."],["CAVEATS","Account owner or admin needed. Policy changes immediate. Audit trails available."],["HISTORY","ibmcloud iam is the IAM management component of **IBM Cloud** CLI."],["SEE ALSO","[ibmcloud](/man/ibmcloud)(1), [ibmcloud-login](/man/ibmcloud-login)(1)"]],"e":["ibmcloud","ibmcloud-login"],"x":"ibmcloud iam api-keys"},"ifmetric":{"n":"ifmetric","t":"manipulates IPv4 route metrics for network interfaces","s":[["TAGLINE","manipulates IPv4 route metrics for network interfaces"],["TLDR","Set the **priority** of a network interface (higher number = lower priority)\n\n```sudo ifmetric [interface] [value]```\n\n**Reset** the priority of an interface to default\n\n```sudo ifmetric [interface] 0```\n\nPrefer **wired** over wireless by giving wireless a higher metric\n\n```sudo ifmetric wlan0 100```"],["SYNOPSIS","**ifmetric** _interface_ _metric_"],["PARAMETERS","_interface_\n> Name of the network interface (e.g., eth0, wlan0)\n\n_metric_\n> Route metric value (higher values mean lower priority, 0 resets to default)"],["DESCRIPTION","**ifmetric** manipulates IPv4 route metrics for network interfaces. Route metrics determine which interface is preferred when multiple routes to the same destination exist - lower metric values have higher priority.\n\nThis is useful for systems with multiple network connections (e.g., wired and wireless) where you want to control which connection is preferred for outgoing traffic."],["CAVEATS","Requires root privileges. Only affects IPv4 routes. Changes are not persistent across reboots unless scripted. Does not work with IPv6 routes."],["HISTORY","ifmetric was created by Lennart Poettering (who later created PulseAudio and systemd) to provide a simple way to manipulate routing metrics without complex ip route commands."],["INSTALL","```apt: sudo apt install ifmetric```\n\n```nix: nix profile install nixpkgs#ifmetric```"],["SEE ALSO","[ip](/man/ip)(8), [route](/man/route)(8), [ifconfig](/man/ifconfig)(8), [ifup](/man/ifup)(8)"]],"e":["ip","route","ifconfig","ifup"],"x":"sudo ifmetric [interface] [value]"},"inotify-wait":{"n":"inotify-wait","t":"waits for filesystem events using Linux's inotify interface","s":[["TAGLINE","waits for filesystem events using Linux's inotify interface"],["TLDR","**Watch file for changes**\n\n```inotifywait [file]```\n\n**Monitor directory recursively**\n\n```inotifywait -r [directory]```\n\n**Watch for specific events**\n\n```inotifywait -e [modify,create,delete] [path]```\n\n**Monitor continuously**\n\n```inotifywait -m [path]```\n\n**Output in CSV format**\n\n```inotifywait -m --csv [path]```\n\n**Watch with timeout**\n\n```inotifywait -t [30] [path]```"],["SYNOPSIS","**inotifywait** [_options_] _path_..."],["PARAMETERS","**-m**, **--monitor**\n> Monitor continuously (don't exit after first event).\n\n**-r**, **--recursive**\n> Watch directories recursively.\n\n**-e** _EVENT_\n> Events to watch (access, modify, create, delete, move).\n\n**-t** _SECONDS_\n> Timeout in seconds.\n\n**--csv**\n> Output in CSV format.\n\n**--format** _FMT_\n> Custom output format.\n\n**-q**, **--quiet**\n> Suppress non-essential output.\n\n**--help**\n> Display help information."],["DESCRIPTION","**inotifywait** waits for filesystem events using Linux's inotify interface. It exits when specified events occur on watched paths.\n\nThe tool enables event-driven scripts that react to file changes. It supports monitoring files, directories, and recursive watching."],["CAVEATS","Linux-only (inotify). Watch limits may need adjustment. Not for NFS/remote filesystems."],["HISTORY","inotifywait is part of **inotify-tools**, created to provide command-line access to Linux's inotify file monitoring API."],["SEE ALSO","[fswatch](/man/fswatch)(1), [entr](/man/entr)(1), [watch](/man/watch)(1)"]],"e":["fswatch","entr","watch"],"x":"inotifywait [file]"},"jello":{"n":"jello","t":"Filter JSON and JSON Lines data using Python expressions","s":[["TAGLINE","Filter JSON and JSON Lines data using Python expressions"],["TLDR","**Filter JSON with Python**\n\n```echo '[1,2,3]' | jello '[x*2 for x in _]'```\n\n**Extract field**\n\n```cat [data.json] | jello '_.name'```\n\n**Filter array**\n\n```cat [data.json] | jello '[x for x in _ if x[\"age\"] > 30]'```\n\n**Get nested value**\n\n```cat [data.json] | jello '_.users[0].email'```\n\n**Print JSON schema** in grep-able format\n\n```cat [data.json] | jello -s```\n\n**Output as lines** (for bash array assignment)\n\n```cat [data.json] | jello -l '_.items'```\n\n**Load input from a file** instead of stdin\n\n```jello -f [data.json] '_.name'```"],["SYNOPSIS","**jello** [_options_] [_expression_]"],["PARAMETERS","_EXPRESSION_\n> Python expression (input is _).\n\n**-p**, **--pretty**\n> Pretty print output.\n\n**-l**, **--lines**\n> Output JSON lines.\n\n**-r**, **--raw**\n> Raw string output.\n\n**-c**, **--compact**\n> Compact JSON output (no pretty printing).\n\n**-C**\n> Force color output even when using pipes.\n\n**-m**, **--mono**\n> Monochrome output (no colors).\n\n**-n**, **--nulls**\n> Print selected null values.\n\n**-s**, **--schema**\n> Print the JSON schema in grep-able format.\n\n**-t**, **--types**\n> Print type annotations in schema view.\n\n**-e**\n> Empty data: initialize _ as None instead of reading input.\n\n**-i**\n> Initialize the data as a list for JSON Lines processing.\n\n**-f** _FILE_\n> Load input data from a JSON file.\n\n**-q** _FILE_\n> Load query from a file.\n\n**-h**, **--help**\n> Display help information."],["DESCRIPTION","**jello** filters JSON and JSON Lines data using Python expressions. The input data is deserialized and made available as the underscore (**_**) variable. Any valid Python expression can be used to transform, filter, or extract data.\n\nThe tool combines jq-like filtering with Python's full expression power, including list comprehensions, dictionary methods, string operations, and standard library functions. It handles both JSON objects and JSON Lines (newline-delimited JSON) input."],["CAVEATS","Requires Python 3.6 or later. Slower than jq for simple tasks due to Python startup overhead. The underscore (**_**) variable name is a convention and cannot be changed. Importing external modules is not supported by default."],["HISTORY","jello was created by **Kelly Brazil** as a Python-based alternative to jq for users more familiar with Python syntax."],["INSTALL","```dnf: sudo dnf install jello```\n\n```brew: brew install jello```\n\n```nix: nix profile install nixpkgs#jello```"],["SEE ALSO","[jq](/man/jq)(1), [jc](/man/jc)(1), [python](/man/python)(1), [gron](/man/gron)(1), [fx](/man/fx)(1), [yq](/man/yq)(1)"]],"e":["jq","jc","python","gron","fx","yq"],"x":"echo '[1,2,3]' | jello '[x*2 for x in _]'"},"limine-enroll-config":{"n":"limine-enroll-config","t":"embeds or resets the BLAKE2B hash of a Limine configuration file into","s":[["TAGLINE","embeds or resets the BLAKE2B hash of a Limine configuration file into"],["TLDR","**Embed** a config file's BLAKE2B hash into the Limine EFI executable\n\n```limine-enroll-config [path/to/BOOTX64.EFI] [blake2b_hash]```\n\n**Remove** the enrolled hash from the executable, disabling config integrity check\n\n```limine-enroll-config --reset [path/to/BOOTX64.EFI]```\n\nDisplay **help**\n\n```limine-enroll-config --help```"],["SYNOPSIS","**limine-enroll-config** [_--reset_] _EFI_FILE_ [_HASH_]"],["PARAMETERS","**--reset**\n> Remove the enrolled configuration hash from the EFI executable\n\n**--help**\n> Display help information\n\n**_EFI_FILE_**\n> Path to the Limine UEFI executable (e.g., BOOTX64.EFI)\n\n**_HASH_**\n> The BLAKE2B hash of the limine.conf configuration file"],["DESCRIPTION","**limine-enroll-config** embeds or resets the BLAKE2B hash of a Limine configuration file (limine.conf) into the Limine EFI executable. This ensures the bootloader configuration has not been tampered with when Secure Boot is enabled.\n\nWhen Secure Boot validates the signed Limine executable, the embedded hash is used to verify the configuration file's integrity before applying boot settings. If the hash doesn't match, Limine will refuse to load the configuration."],["CAVEATS","The configuration hash must be regenerated and re-enrolled whenever limine.conf is modified. This tool is only relevant for UEFI Secure Boot environments; BIOS systems do not support this verification mechanism."],["SEE ALSO","[limine](/man/limine)(1), [limine-entry-tool](/man/limine-entry-tool)(1)"]],"e":["limine","limine-entry-tool"],"x":"limine-enroll-config [path/to/BOOTX64.EFI] [blake2b_hash]"},"lprm":{"n":"lprm","t":"removes print jobs from the queue","s":[["TAGLINE","removes print jobs from the queue"],["TLDR","**Cancel all jobs**\n\n```lprm -```\n\n**Cancel specific job**\n\n```lprm [job_id]```\n\n**Cancel jobs on printer**\n\n```lprm -P [printer] -```\n\n**Cancel user's jobs**\n\n```lprm [username]```\n\n**Cancel job by ID on printer**\n\n```lprm -P [printer] [job_id]```"],["SYNOPSIS","**lprm** [_options_] [_job_]"],["PARAMETERS","_JOB_\n> Job ID or \"-\" for all.\n\n**-P** _PRINTER_\n> Specify printer.\n\n**-E**\n> Force encryption.\n\n**-U** _USER_\n> Specify username.\n\n**--help**\n> Display help information."],["DESCRIPTION","**lprm** removes print jobs from the queue. It cancels pending or processing jobs.\n\nThe tool is the BSD-style job removal command. Users can only remove their own jobs unless root."],["CAVEATS","Requires CUPS. Users limited to own jobs. May not stop printing in progress."],["HISTORY","lprm originated in **BSD Unix** for removing print jobs, now implemented by CUPS."],["INSTALL","```apt: sudo apt install lpr```\n\n```dnf: sudo dnf install cups-client```\n\n```apk: sudo apk add cups-client```\n\n```zypper: sudo zypper install cups-client```"],["SEE ALSO","[lp](/man/lp)(1), [lpr](/man/lpr)(1), [lpq](/man/lpq)(1), [cancel](/man/cancel)(1)"]],"e":["lp","lpr","lpq","cancel"],"x":"lprm -"},"luajit":{"n":"luajit","t":"Just-In-Time Compiler for the Lua Language","s":[["TAGLINE","Just-In-Time Compiler for the Lua Language"],["TLDR","**Start interactive interpreter**\n\n```luajit```\n\n**Run a Lua script**\n\n```luajit [script.lua]```\n\n**Execute a string of Lua code**\n\n```luajit -e \"[print('Hello')]\"```\n\n**Run a script with arguments**\n\n```luajit [script.lua] [arg1] [arg2]```\n\n**Save bytecode** to a file\n\n```luajit -b [script.lua] [script.out]```\n\n**Load a library** before running a script\n\n```luajit -l [library] [script.lua]```\n\n**Enter interactive mode** after running a script\n\n```luajit -i [script.lua]```"],["SYNOPSIS","**luajit** [_options_] [_script_ [_args_]]"],["PARAMETERS","**-e** _chunk_\n> Run the given chunk of Lua code.\n\n**-l** _library_\n> Load the named library, equivalent to require(\"library\").\n\n**-b** _..._\n> Save or list bytecode. Run without arguments to get help on options.\n\n**-j** _command_\n> Perform a LuaJIT control command (e.g., -jv for verbose trace info, -jdump for detailed trace dumps).\n\n**-O**[_opt_]\n> Control LuaJIT optimizations. -O0 disables all optimizations, -O (default) is equivalent to -O3.\n\n**-i**\n> Run in interactive mode after executing the script or code.\n\n**-v**\n> Show LuaJIT version.\n\n**-E**\n> Ignore environment variables.\n\n**--**\n> Stop processing options."],["DESCRIPTION","**luajit** is a Just-In-Time compiler for the Lua programming language. It provides significantly faster execution than the standard Lua interpreter through trace-based JIT compilation, an efficient interpreter, and a built-in FFI (Foreign Function Interface).\n\nLuaJIT is fully compatible with Lua 5.1 and includes several extensions. Script arguments are passed via the global `arg` table. Interactive mode is activated automatically when no script or `-e` option is provided."],["CAVEATS","LuaJIT is compatible with Lua 5.1 only, not Lua 5.2 or later. The FFI library requires knowledge of C types and native code. JIT compilation is not available on all architectures. The `-b` bytecode format is not compatible with standard Lua bytecode."],["HISTORY","LuaJIT was created by **Mike Pall** starting in 2005. LuaJIT 2.0, featuring the current trace compiler architecture, was released in 2012."],["INSTALL","```apt: sudo apt install luajit```\n\n```dnf: sudo dnf install luajit```\n\n```pacman: sudo pacman -S luajit```\n\n```apk: sudo apk add luajit```\n\n```zypper: sudo zypper install luajit```\n\n```brew: brew install luajit```\n\n```nix: nix profile install nixpkgs#luajit```"],["SEE ALSO","[lua](/man/lua)(1), [luac](/man/luac)(1)"]],"e":["lua","luac"],"x":"luajit"},"md5sum":{"n":"md5sum","t":"computes and verifies MD5 message digests","s":[["TAGLINE","computes and verifies MD5 message digests"],["TLDR","**Calculate MD5 hash** of a file\n\n```md5sum [file]```\n\n**Calculate hash** for multiple files\n\n```md5sum [file1] [file2] [file3]```\n\n**Calculate hash** from stdin\n\n```echo \"text\" | md5sum```\n\n**Save hashes** to file\n\n```md5sum [*.iso] > checksums.md5```\n\n**Verify files** against checksums\n\n```md5sum -c checksums.md5```\n\n**Verify silently** (only show failures)\n\n```md5sum -c --quiet checksums.md5```\n\n**Verify with status** code only\n\n```md5sum -c --status checksums.md5```\n\n**Output in BSD format**\n\n```md5sum --tag [file]```"],["SYNOPSIS","**md5sum** [_options_] [_file_...]"],["DESCRIPTION","**md5sum** computes and verifies MD5 message digests (checksums). MD5 produces a 128-bit hash value, typically displayed as a 32-character hexadecimal number.\n\nThe tool is commonly used to verify file integrity after transfers, detect changes in files, and generate unique identifiers for content. Files can be checked against a previously generated checksum file.\n\nNote that MD5 is cryptographically broken and should not be used for security purposes. Use SHA-256 or better for security-critical applications."],["PARAMETERS","**-c**, **--check**\n> Read checksums and verify files.\n\n**--quiet**\n> Don't print OK for verified files.\n\n**--status**\n> Don't output, use exit status only.\n\n**-w**, **--warn**\n> Warn about malformed checksum lines.\n\n**--strict**\n> Exit non-zero for malformed lines.\n\n**--tag**\n> Create BSD-style checksum output.\n\n**-b**, **--binary**\n> Read files in binary mode.\n\n**-t**, **--text**\n> Read files in text mode (default).\n\n**--ignore-missing**\n> Ignore missing files when checking."],["CAVEATS","MD5 is cryptographically broken and vulnerable to collision attacks. Not suitable for security purposes. Different results for binary vs text mode on some systems. Empty file produces constant hash d41d8cd98f00b204e9800998ecf8427e."],["HISTORY","**md5sum** is part of **GNU coreutils**, implementing the MD5 algorithm designed by **Ronald Rivest** in **1991**. MD5 was widely used for file verification until cryptographic weaknesses were demonstrated in **2004-2005**. While still commonly used for non-security checksums, SHA-2 family hashes are recommended for security applications."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[sha256sum](/man/sha256sum)(1), [sha1sum](/man/sha1sum)(1), [cksum](/man/cksum)(1), [b2sum](/man/b2sum)(1)"]],"e":["sha256sum","sha1sum","cksum","b2sum"],"x":"md5sum [file]"},"modutil":{"n":"modutil","t":"manages PKCS#11 security modules in NSS databases","s":[["TAGLINE","manages PKCS#11 security modules in NSS databases"],["TLDR","**List modules**\n\n```modutil -list -dbdir [sql:/etc/pki/nssdb]```\n\n**Add module**\n\n```modutil -add [module_name] -libfile [/path/to/lib.so] -dbdir [sql:/etc/pki/nssdb]```\n\n**Delete module**\n\n```modutil -delete [module_name] -dbdir [sql:/etc/pki/nssdb]```\n\n**Enable module**\n\n```modutil -enable [module_name] -dbdir [sql:/etc/pki/nssdb]```"],["SYNOPSIS","**modutil** [_options_]"],["PARAMETERS","**-list**\n> List installed modules.\n\n**-add** _name_\n> Add PKCS#11 module.\n\n**-delete** _name_\n> Remove module.\n\n**-enable** _name_\n> Enable module.\n\n**-disable** _name_\n> Disable module.\n\n**-libfile** _path_\n> Module library path.\n\n**-dbdir** _dir_\n> NSS database directory."],["DESCRIPTION","**modutil** manages PKCS#11 security modules in NSS databases. Add, remove, and configure hardware tokens and software modules. Part of Mozilla NSS tools."],["INSTALL","```apt: sudo apt install libnss3-tools```\n\n```dnf: sudo dnf install nss-tools```\n\n```apk: sudo apk add nss-tools```"],["SEE ALSO","[certutil](/man/certutil)(1)"]],"e":["certutil"],"x":"modutil -list -dbdir [sql:/etc/pki/nssdb]"},"mysqld":{"n":"mysqld","t":"mySQL server daemon","s":[["TAGLINE","mySQL server daemon"],["TLDR","**Start MySQL server**\n\n```mysqld```\n\n**Start with specific config**\n\n```mysqld --defaults-file=[/etc/mysql/my.cnf]```\n\n**Start with data directory**\n\n```mysqld --datadir=[/var/lib/mysql]```\n\n**Run in safe mode**\n\n```mysqld_safe```\n\n**Initialize data directory**\n\n```mysqld --initialize```\n\n**Run with specific user**\n\n```mysqld --user=[mysql]```\n\n**Verbose logging**\n\n```mysqld --verbose```"],["SYNOPSIS","**mysqld** [_options_]"],["PARAMETERS","**--defaults-file** _FILE_\n> Configuration file.\n\n**--datadir** _DIR_\n> Data directory path.\n\n**--user** _USER_\n> Run as user.\n\n**--initialize**\n> Initialize data directory.\n\n**--port** _PORT_\n> Listen port.\n\n**--verbose**\n> Verbose mode.\n\n**--help**\n> Display help information."],["DESCRIPTION","**mysqld** is the MySQL server daemon. It handles database storage and query processing.\n\nThe tool is the core database engine. Manages connections, queries, and data."],["CAVEATS","Requires proper configuration. Resource intensive. Security setup critical."],["HISTORY","mysqld is the **MySQL** database server, originally developed by MySQL AB and now maintained by Oracle."],["INSTALL","```apt: sudo apt install mariadb-server-core```\n\n```dnf: sudo dnf install mariadb```\n\n```pacman: sudo pacman -S mariadb```\n\n```apk: sudo apk add mariadb```\n\n```zypper: sudo zypper install mariadb```\n\n```brew: brew install mariadb```\n\n```nix: nix profile install nixpkgs#mariadb```"],["SEE ALSO","[mysql](/man/mysql)(1), [mysqladmin](/man/mysqladmin)(1)"]],"e":["mysql","mysqladmin"],"x":"mysqld"},"pacmd":{"n":"pacmd","t":"command-line tool for reconfiguring PulseAudio at runtime","s":[["TAGLINE","command-line tool for reconfiguring PulseAudio at runtime"],["TLDR","**List sinks (outputs)**\n\n```pacmd list-sinks```\n\n**List sources (inputs)**\n\n```pacmd list-sources```\n\n**Set default sink**\n\n```pacmd set-default-sink [sink_name]```\n\n**Set sink volume**\n\n```pacmd set-sink-volume [sink_name] [65536]```\n\n**Move stream to sink**\n\n```pacmd move-sink-input [index] [sink_name]```\n\n**Interactive mode**\n\n```pacmd```"],["SYNOPSIS","**pacmd** [_command_]"],["PARAMETERS","**list-sinks**\n> List output devices.\n\n**list-sources**\n> List input devices.\n\n**list-sink-inputs**\n> List playback streams.\n\n**list-source-outputs**\n> List recording streams.\n\n**list-modules**\n> List loaded PulseAudio modules.\n\n**set-default-sink** _name_\n> Set default output.\n\n**set-default-source** _name_\n> Set default input.\n\n**set-sink-volume** _name_ _vol_\n> Set sink volume (0-65536 linear).\n\n**set-source-volume** _name_ _vol_\n> Set source volume.\n\n**set-sink-mute** _name_ _0|1_\n> Mute/unmute a sink.\n\n**set-source-mute** _name_ _0|1_\n> Mute/unmute a source.\n\n**move-sink-input** _idx_ _sink_\n> Move a playback stream to another sink.\n\n**move-source-output** _idx_ _source_\n> Move a recording stream to another source.\n\n**load-module** _name_ [_args_]\n> Load a PulseAudio module.\n\n**unload-module** _id_\n> Unload a module by index or name.\n\n**help**\n> Show all supported commands.\n\n**exit**\n> Terminate the PulseAudio daemon (in interactive mode, use Ctrl+D to quit)."],["DESCRIPTION","**pacmd** is a command-line tool for reconfiguring PulseAudio at runtime. It provides full access to PulseAudio's internal configuration and state.\n\nRunning without arguments enters interactive mode with tab completion."],["VOLUME SCALE","```\n0     = 0%   (muted)\n32768 = 50%\n65536 = 100% (normal)\n98304 = 150% (amplified)\n```"],["CAVEATS","PulseAudio specific. Use pactl for simpler operations. Changes may not persist across restarts. Consider PipeWire migration."],["HISTORY","pacmd is part of **PulseAudio**, developed by **Lennart Poettering** starting in **2004** as a next-generation sound server."],["INSTALL","```apt: sudo apt install pulseaudio-utils```\n\n```dnf: sudo dnf install pulseaudio-utils```\n\n```apk: sudo apk add pulseaudio-utils```\n\n```zypper: sudo zypper install pulseaudio-utils```"],["SEE ALSO","[pactl](/man/pactl)(1), [pulseaudio](/man/pulseaudio)(1), [pavucontrol](/man/pavucontrol)(1)"]],"e":["pactl","pulseaudio","pavucontrol"],"x":"pacmd list-sinks"},"pacrepairfile":{"n":"pacrepairfile","t":"resets properties on files managed by alpm","s":[["TAGLINE","resets properties on files managed by alpm"],["TLDR","Search for package and **reset file properties**\n\n```pacrepairfile path/to/file --package```\n\nReset a file **quietly**\n\n```pacrepairfile package_name --quiet --package```\n\nReset specific **file properties** (mode, uid, gid, or mtime)\n\n```pacrepairfile package_name --mode --package```\n\nDisplay **help**\n\n```pacrepairfile --help```"],["SYNOPSIS","**pacrepairfile** [_options_] _target_"],["PARAMETERS","**--package**\n> Search for the package owning the file\n\n**--mode**\n> Reset file permissions mode\n\n**--uid**\n> Reset file owner UID\n\n**--gid**\n> Reset file group GID\n\n**--mtime**\n> Reset file modification time\n\n**--quiet**\n> Suppress non-essential output\n\n**--help**\n> Display help message\n\n**--version**\n> Display version information"],["DESCRIPTION","**pacrepairfile** resets properties on files managed by alpm (Arch Linux Package Manager). It can restore file permissions, ownership, and modification times to their original values as recorded in the package database.\n\nThis is useful when file properties have been accidentally changed or corrupted."],["CAVEATS","Only works on files tracked by installed packages. Cannot repair files that were never part of a package."],["HISTORY","Part of **pacutils**, a collection of utilities for Arch Linux package management built on libalpm."],["SEE ALSO","[pacman](/man/pacman)(8), [pacrepairdb](/man/pacrepairdb)(1)"]],"e":["pacman","pacrepairdb"],"x":"pacrepairfile path/to/file --package"},"pam_sepermit":{"n":"pam_sepermit","t":"PAM module to allow/deny login based on SELinux user state","s":[["TAGLINE","PAM module to allow/deny login based on SELinux user state"],["TLDR","**Enable in PAM auth stack**\n\n```auth required pam_sepermit.so```\n\n**Enable with exclusive login enforcement**\n\n```auth required pam_sepermit.so exclusive```\n\n**Use custom configuration file**\n\n```auth required pam_sepermit.so conf=/etc/security/custom_sepermit.conf```\n\n**Add user permit rule to config**\n\n```echo \"%wheel\" >> /etc/security/sepermit.conf```"],["SYNOPSIS","**pam_sepermit.so** [**conf=**_file_] [**exclusive**]"],["PARAMETERS","**exclusive**\n> Only allow one login session at a time for the SELinux user. If another session is active, login is denied.\n\n**conf=**_FILE_\n> Use an alternate configuration file instead of the default /etc/security/sepermit.conf."],["CONFIGURATION","**/etc/security/sepermit.conf**\n> Configuration file listing SELinux users, UNIX users, or UNIX groups (prefixed with %) that are allowed or denied login. Lines beginning with # are comments. Each line specifies a user or group and optional modifiers like exclusive or ignore."],["DESCRIPTION","**pam_sepermit** is a PAM module that allows or denies login depending on the SELinux user state. It checks whether SELinux is enforcing and whether the user's SELinux context matches entries in the configuration file. When SELinux is in permissive mode or disabled, the module allows access by default.\n\nThe module is typically placed in the auth stack and is used to restrict which users can log in on SELinux-enabled systems based on their mapped SELinux identity."],["CAVEATS","Requires SELinux to be enabled and configured on the system. When SELinux is disabled, the module permits all access. The module must be placed correctly in the PAM stack order. Configuration errors can lock out users."],["HISTORY","**pam_sepermit** is part of the Linux-PAM project and was introduced to provide fine-grained login control on SELinux-enabled systems, complementing the broader pam_selinux module."],["SEE ALSO","[pam](/man/pam)(8), [pam_selinux](/man/pam_selinux)(8), [getenforce](/man/getenforce)(8), [sestatus](/man/sestatus)(8)"]],"e":["pam","pam_selinux","getenforce","sestatus"],"x":"auth required pam_sepermit.so"},"pamtosrf":{"n":"pamtosrf","t":"converts PAM images to SRF format used for custom vehicle icons on Garmin GPS","s":[["TAGLINE","converts PAM images to SRF format used for custom vehicle icons on Garmin GPS"],["TLDR","**Convert PAM to SRF format**\n\n```pamtosrf [input.pam] > [output.srf]```"],["SYNOPSIS","**pamtosrf** [_file_]"],["DESCRIPTION","**pamtosrf** converts PAM images to SRF (Garmin vehicle image) format used for custom vehicle icons on Garmin GPS devices. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[srftopam](/man/srftopam)(1), [pnmtopng](/man/pnmtopng)(1)"]],"e":["srftopam","pnmtopng"],"x":"pamtosrf [input.pam] > [output.srf]"},"pct-config":{"n":"pct-config","t":"displays the configuration of a Proxmox LXC container","s":[["TAGLINE","displays the configuration of a Proxmox LXC container"],["TLDR","**Show** container configuration\n\n```pct config 100```\n\nShow **current** running config\n\n```pct config 100 --current```\n\nShow **snapshot** configuration\n\n```pct config 100 --snapshot snapshot_name```"],["SYNOPSIS","**pct config** [_OPTIONS_] _vmid_"],["DESCRIPTION","**pct config** displays the configuration of a Proxmox LXC container. By default, it shows the saved configuration including pending changes that will be applied on next start."],["PARAMETERS","**vmid**\n> The unique numeric ID of the container (integer, 100 to 999999999)\n\n**--current**\n> Get current values instead of pending values (boolean, default 0)\n\n**--snapshot** _name_\n> Fetch configuration values from a given snapshot"],["CAVEATS","The default output shows saved configuration with pending changes. Use **--current** to see only the active runtime configuration."],["HISTORY","**pct config** is part of the **Proxmox VE** virtualization platform for managing LXC containers."],["SEE ALSO","[pct](/man/pct)(1), [pct-set](/man/pct-set)(1), [pct-list](/man/pct-list)(1), [pct-status](/man/pct-status)(1)"]],"e":["pct","pct-set","pct-list","pct-status"],"x":"pct config 100"},"peco":{"n":"peco","t":"Simplistic interactive filtering tool","s":[["TAGLINE","Simplistic interactive filtering tool"],["TLDR","**Interactive filter**\n\n```ls | peco```\n\n**Filter with query**\n\n```history | peco --query \"[pattern]\"```\n\n**Select file and open with a command**\n\n```find . -type f | peco | xargs [command]```\n\n**With custom prompt**\n\n```peco --prompt=\"Select> \"```"],["SYNOPSIS","**peco** [_options_]"],["PARAMETERS","**--query** _STRING_\n> Initial query string.\n\n**--prompt** _STRING_\n> Custom prompt string.\n\n**--layout** _TYPE_\n> Display layout: top-down or bottom-up. Default is top-down.\n\n**--initial-index** _N_\n> Start selection at line N.\n\n**--rcfile** _PATH_\n> Path to the settings file.\n\n**-b**, **--buffer-size** _N_\n> Number of lines to keep in search buffer. Useful when piping from an infinite stream.\n\n**--null**\n> Expect NUL (\\0) as separator for target/output.\n\n**--select-1**\n> Immediately select and exit if input contains only one item.\n\n**--on-cancel** _ACTION_\n> Action on user cancel: success or error. Default is success.\n\n**--initial-filter** _FILTER_\n> Specify the default filter (e.g., Regexp, SmartCase)."],["DESCRIPTION","**peco** is an interactive filtering tool. Select from stdin with fuzzy matching.\n\nThe tool provides incremental search. Similar to fzf and percol."],["CAVEATS","Requires a terminal for interactive use. Written in Go. Configuration can be customized via a JSON config file (~/.config/peco/config.json)."],["HISTORY","peco was created as an **interactive grep** tool inspired by percol."],["INSTALL","```apt: sudo apt install peco```\n\n```pacman: sudo pacman -S peco```\n\n```brew: brew install peco```\n\n```nix: nix profile install nixpkgs#peco```"],["SEE ALSO","[fzf](/man/fzf)(1), [percol](/man/percol)(1), [grep](/man/grep)(1)"]],"e":["fzf","percol","grep"],"x":"ls | peco"},"pio-lib":{"n":"pio-lib","t":"manages PlatformIO libraries (deprecated, use pio pkg)","s":[["TAGLINE","manages PlatformIO libraries (deprecated, use pio pkg)"],["TLDR","**Search for libraries**\n\n```pio lib search [mqtt]```\n\n**Install library**\n\n```pio lib install [PubSubClient]```\n\n**Install specific version**\n\n```pio lib install \"[email protected]\"```\n\n**List installed libraries**\n\n```pio lib list```\n\n**Uninstall library**\n\n```pio lib uninstall [PubSubClient]```\n\n**Update all installed libraries**\n\n```pio lib update```"],["SYNOPSIS","**pio lib** _command_ [_options_]"],["PARAMETERS","**search** _query_\n> Search library registry.\n\n**install** _library_\n> Install library.\n\n**uninstall** _library_\n> Remove library.\n\n**list**\n> List installed libraries.\n\n**update**\n> Update installed libraries.\n\n**show** _library_\n> Show library details.\n\n**builtin**\n> Show built-in libraries.\n\n**register**\n> Register a library in the PlatformIO registry.\n\n**stats**\n> Show library statistics.\n\n**-d**, **--storage-dir** _dir_\n> Manage custom library storage directory.\n\n**-g**, **--global**\n> Work with global PlatformIO library storage.\n\n**-e**, **--environment** _name_\n> Target a specific project build environment."],["DESCRIPTION","**pio lib** manages PlatformIO libraries. Search, install, and update libraries from the PlatformIO registry. Handles dependencies automatically for embedded projects. This command is deprecated since PlatformIO 6.0; use **pio pkg** instead."],["INSTALL","```aur: yay -S pio```"],["SEE ALSO","[pio-pkg](/man/pio-pkg)(1), [pio-init](/man/pio-init)(1), [pio-run](/man/pio-run)(1), [pio](/man/pio)(1)"]],"e":["pio-pkg","pio-init","pio-run","pio"],"x":"pio lib search [mqtt]"},"potrace":{"n":"potrace","t":"Trace bitmaps into vector graphics","s":[["TAGLINE","Trace bitmaps into vector graphics"],["TLDR","**Convert bitmap to SVG**\n\n```potrace -s [input.bmp] -o [output.svg]```\n\n**Convert to PDF**\n\n```potrace -b pdf [input.bmp] -o [output.pdf]```\n\n**Convert to EPS**\n\n```potrace -e [input.bmp] -o [output.eps]```\n\n**Invert colors**\n\n```potrace -s --invert [input.bmp] -o [output.svg]```\n\n**From PBM/PGM/PPM**\n\n```potrace -s [input.pbm] -o [output.svg]```"],["SYNOPSIS","**potrace** [_options_] [_files_]"],["PARAMETERS","**-b**, **--backend** _format_\n> Output format (svg, pdf, eps, ps).\n\n**-s**, **--svg**\n> SVG output.\n\n**-e**, **--eps**\n> EPS output.\n\n**-p**, **--postscript**\n> PostScript output.\n\n**-o** _file_\n> Output file.\n\n**--invert**\n> Invert input.\n\n**-t**, **--turdsize** _n_\n> Suppress speckles.\n\n**-a**, **--alphamax** _n_\n> Corner threshold."],["DESCRIPTION","**potrace** transforms bitmap images into smooth, scalable vector graphics by tracing the boundaries between black and white regions. It uses an optimal polygon-fitting algorithm to produce clean curves, making it ideal for converting logos, line art, scanned drawings, and other high-contrast images into SVG, PDF, EPS, or PostScript format.\n\nThe tool accepts PBM, PGM, PPM, and BMP input formats. Tuning parameters control corner detection (alphamax) and speckle suppression (turdsize) to balance accuracy against smoothness. For photographs or color images, preprocessing with threshold or edge-detection tools is needed first."],["EXAMPLES","```bash"],["Bitmap to SVG","potrace -s logo.bmp -o logo.svg"],["PDF output with smoothing","potrace -b pdf --alphamax 1 image.bmp -o output.pdf"],["Remove small spots","potrace -s --turdsize 5 noisy.bmp -o clean.svg"],["From PNG (via conversion)","convert image.png image.bmp && potrace -s image.bmp\n```"],["INPUT FORMATS","Accepts PBM, PGM, PPM, and BMP formats. Use ImageMagick or similar to convert other formats first."],["CAVEATS","Works best on high-contrast images. Photos don't trace well. Input should be monochrome for best results."],["HISTORY","potrace was written by **Peter Selinger** as an improved alternative to autotrace, using optimal polygon algorithms."],["INSTALL","```apt: sudo apt install potrace```\n\n```dnf: sudo dnf install potrace```\n\n```pacman: sudo pacman -S potrace```\n\n```apk: sudo apk add potrace```\n\n```zypper: sudo zypper install potrace```\n\n```brew: brew install potrace```\n\n```nix: nix profile install nixpkgs#potrace```"],["SEE ALSO","[inkscape](/man/inkscape)(1), [convert](/man/convert)(1)"]],"e":["inkscape","convert"],"x":"potrace -s [input.bmp] -o [output.svg]"},"print":{"n":"print","t":"Shell built-in text output command","s":[["TAGLINE","Shell built-in text output command"],["TLDR","**Print text (zsh/ksh)**\n\n```print \"Hello, World!\"```\n\n**Print without newline**\n\n```print -n \"text\"```\n\n**Print to file descriptor**\n\n```print -u [2] \"Error message\"```\n\n**Print with escape sequences**\n\n```print \"Line1\\nLine2\"```"],["SYNOPSIS","**print** [_options_] [_args_]"],["PARAMETERS","_ARGS_\n> Text to print.\n\n**-n**\n> No trailing newline.\n\n**-r**\n> Ignore echo escape conventions (raw output).\n\n**-R**\n> BSD-style echo; escape sequences only with **-e**.\n\n**-u** _N_\n> Print to file descriptor N (e.g., -u2 for stderr).\n\n**-l**\n> Print arguments on separate lines.\n\n**-N**\n> Separate and terminate arguments with null bytes.\n\n**-f** _format_\n> Format arguments using printf-style format string.\n\n**-P** (zsh)\n> Perform prompt expansion on the arguments.\n\n**-s** (zsh)\n> Save arguments in the history list instead of printing.\n\n**-v** _name_ (zsh)\n> Store the output in the parameter _name_ instead of printing.\n\n**-c** (zsh)\n> Print arguments in columns."],["DESCRIPTION","**print** is a shell built-in command available in ksh and zsh that outputs text to stdout. It provides more control than **echo**, supporting escape sequence processing, output to specific file descriptors, and suppression of the trailing newline.\n\nThe **-u** option directs output to a numbered file descriptor (useful for writing to stderr or other open descriptors), while **-l** prints each argument on a separate line. Unlike echo, print's behavior is consistent and not affected by argument values that look like options."],["CAVEATS","Shell built-in. Not available in bash (use printf)."],["HISTORY","print is a **ksh/zsh built-in** for text output."],["SEE ALSO","[echo](/man/echo)(1), [printf](/man/printf)(1)"]],"e":["echo","printf"],"x":"print \"Hello, World!\""},"proxify":{"n":"proxify","t":"HTTP/HTTPS proxy for traffic capture","s":[["TAGLINE","HTTP/HTTPS proxy for traffic capture"],["TLDR","**Start proxy server**\n\n```proxify```\n\n**Specify port**\n\n```proxify -p [8888]```\n\n**Output to directory**\n\n```proxify -o [logs/]```\n\n**Upstream proxy**\n\n```proxify -upstream [http://127.0.0.1:8080]```\n\n**Dump requests**\n\n```proxify -dump-req```\n\n**Dump responses**\n\n```proxify -dump-resp```\n\n**Filter by content type**\n\n```proxify -match-resp-body \"[text/html]\"```"],["SYNOPSIS","**proxify** [_-p port_] [_-o dir_] [_-upstream proxy_] [_options_]"],["PARAMETERS","**-p**, **--port** _PORT_\n> Listen port.\n\n**-o**, **--output** _DIR_\n> Output directory.\n\n**-upstream** _PROXY_\n> Upstream proxy.\n\n**-dump-req**\n> Dump requests.\n\n**-dump-resp**\n> Dump responses.\n\n**-match-resp-body** _PATTERN_\n> Filter response body.\n\n**-match-req-body** _PATTERN_\n> Filter request body.\n\n**-silent**\n> Silent mode.\n\n**-v**, **--verbose**\n> Verbose output."],["DESCRIPTION","**proxify** is a lightweight HTTP/HTTPS proxy designed for capturing and logging web traffic during security testing. It intercepts requests and responses, dumping headers and bodies to the console or organized output directories for later analysis.\n\nPattern matching options filter captured traffic by request or response body content, allowing testers to focus on specific content types or keywords of interest. Upstream proxy support enables chaining through tools like Burp Suite or mitmproxy for more advanced inspection.\n\nThe tool is developed by ProjectDiscovery and integrates with their security testing toolkit. Traffic is saved organized by domain, making it straightforward to review captured data from specific targets."],["CAVEATS","HTTPS requires certificate installation. Large traffic volumes consume disk. Some apps detect proxies."],["HISTORY","**Proxify** was created by **ProjectDiscovery** for security testing. It provides simple traffic interception for web application analysis."],["INSTALL","```brew: brew install proxify```\n\n```nix: nix profile install nixpkgs#proxify```"],["SEE ALSO","[mitmproxy](/man/mitmproxy)(1), [burpsuite](/man/burpsuite)(1), [charles](/man/charles)(1)"]],"e":["mitmproxy","burpsuite","charles"],"x":"proxify"},"rbac-tool":{"n":"rbac-tool","t":"Analyze and visualize Kubernetes RBAC policies","s":[["TAGLINE","Analyze and visualize Kubernetes RBAC policies"],["TLDR","**Show who can perform an action on a resource**\n\n```rbac-tool who-can [get] [pods]```\n\n**Visualize RBAC as HTML**\n\n```rbac-tool viz --outformat html > [rbac.html]```\n\n**Look up roles assigned to a specific subject**\n\n```rbac-tool lookup -e [user@example.com]```\n\n**List policy rules for a subject**\n\n```rbac-tool policy-rules -e [system:serviceaccount:default:.*]```\n\n**Analyze RBAC for risky configurations**\n\n```rbac-tool analysis```\n\n**Generate a ClusterRole from audit events**\n\n```rbac-tool auditgen -f [audit.log]```\n\n**Show current user's permissions**\n\n```rbac-tool whoami```"],["SYNOPSIS","**rbac-tool** _command_ [_options_]"],["COMMANDS","**analysis**\n> Examine RBAC permissions to identify overly permissive principals and risky configurations.\n\n**viz**\n> Generate a visual representation of RBAC relationships (DOT or HTML).\n\n**who-can** _verb_ _kind_|_kind/name_|_url_\n> Find all subjects with permission to perform a given action on a resource.\n\n**lookup**\n> List Roles and ClusterRoles assigned to specified users, service accounts, or groups.\n\n**policy-rules**\n> List RBAC policy rules associated with the given subjects.\n\n**generate** | **gen**\n> Generate a Role or ClusterRole with explicit permissions.\n\n**auditgen**\n> Generate RBAC policies from Kubernetes audit events.\n\n**show**\n> Generate a ClusterRole containing the cluster's available permissions.\n\n**whoami**\n> Display the currently authenticated subject as seen by the API server.\n\n**version**\n> Print rbac-tool version."],["PARAMETERS","**--outformat** _FORMAT_\n> Output format. For **viz**: `dot` or `html`. For data commands: `json`, `yaml`, `table`.\n\n**-e** _REGEX_\n> Subject filter as a regular expression (used with **lookup**, **policy-rules**).\n\n**--config** _FILE_\n> Custom analysis ruleset file (used with **analysis**).\n\n**--cluster-context** _NAME_\n> kubeconfig context to use.\n\n**--exclude-namespaces** _LIST_\n> Comma-separated namespaces to exclude from visualization.\n\n**--generated-type** _TYPE_\n> Role or ClusterRole (used with **generate**).\n\n**--allowed-verbs** _LIST_\n> Verbs to include in generated role.\n\n**--allowed-groups** _LIST_\n> API groups to include in generated role.\n\n**--deny-resources** _LIST_\n> Resources to exclude from generated role.\n\n**--for-groups** _LIST_\n> API groups to include with the **show** command.\n\n**-f** _PATH_|_URL_\n> Source file, directory, or HTTP URL of audit events (used with **auditgen**)."],["CONFIGURATION","**~/.kube/config**\n> Default kubeconfig used to connect to the target cluster."],["DESCRIPTION","**rbac-tool** analyzes and visualizes Kubernetes RBAC (Role-Based Access Control) configurations. It helps administrators understand who can do what, identify over-privileged subjects, generate least-privilege policies, and visualize the role/subject graph.\n\nThe `viz` command produces a Graphviz DOT graph or an interactive HTML page. `policy-rules` and `lookup` accept regular expressions, making it easy to query large clusters."],["CAVEATS","Requires a working kubeconfig. Some commands need cluster-admin privileges to enumerate all roles and bindings. Generated policies should be reviewed before applying."],["HISTORY","**rbac-tool** is developed by **Alcide** (now part of Rapid7) and released under Apache 2.0. It was built to help Kubernetes administrators understand and manage complex RBAC configurations."],["SEE ALSO","[kubectl](/man/kubectl)(1), [kubeaudit](/man/kubeaudit)(1)"]],"e":["kubectl","kubeaudit"],"x":"rbac-tool who-can [get] [pods]"},"realpath":{"n":"realpath","t":"Resolve absolute canonical file paths","s":[["TAGLINE","Resolve absolute canonical file paths"],["TLDR","**Get absolute path** of a file\n\n```realpath [file.txt]```\n\n**Resolve symlinks** and get canonical path\n\n```realpath [symlink]```\n\n**Get path without resolving symlinks**\n\n```realpath -s [symlink]```\n\n**Get path relative** to another directory\n\n```realpath --relative-to=[/base/path] [/full/path/file]```\n\n**Get relative base path**\n\n```realpath --relative-base=[/base] [/base/subdir/file]```\n\n**Get absolute paths** for multiple files\n\n```realpath [file1] [file2] [file3]```\n\n**Require all path components to exist**\n\n```realpath -e [path/to/file]```\n\n**Quiet mode** (suppress errors)\n\n```realpath -q [file]```"],["SYNOPSIS","**realpath** [_options_] _file_..."],["DESCRIPTION","**realpath** prints the resolved absolute pathname of files. It expands all symbolic links, resolves references to /./, /../, and removes extra slashes to produce a canonical path.\n\nThe tool is useful in shell scripts when you need the absolute path of a file regardless of how it was specified (relative path, symlink, etc.). It can also compute relative paths between two locations.\n\nrealpath is part of GNU coreutils and provides similar functionality to the readlink command with -f flag."],["PARAMETERS","**-e**, **--canonicalize-existing**\n> All path components must exist.\n\n**-m**, **--canonicalize-missing**\n> No path component needs to exist.\n\n**-L**, **--logical**\n> Resolve .. before symlinks.\n\n**-P**, **--physical**\n> Resolve symlinks first (default).\n\n**-s**, **--strip**, **--no-symlinks**\n> Don't resolve symlinks.\n\n**--relative-to=** _dir_\n> Print path relative to directory.\n\n**--relative-base=** _dir_\n> Print relative path if below base.\n\n**-q**, **--quiet**\n> Suppress error messages.\n\n**-z**, **--zero**\n> End each output line with NUL."],["CAVEATS","Behavior may differ between GNU coreutils and BSD versions. Without -e, path may not exist. Relative path options require careful base directory specification."],["HISTORY","**realpath** as a standalone command became part of **GNU coreutils** in version **8.15** (released **2012**). Prior to this, the realpath() function existed in C libraries, and the readlink command provided similar functionality. The standalone command simplified common shell scripting tasks."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[readlink](/man/readlink)(1), [dirname](/man/dirname)(1), [basename](/man/basename)(1), [pwd](/man/pwd)(1)"]],"e":["readlink","dirname","basename","pwd"],"x":"realpath [file.txt]"},"sails":{"n":"sails","t":"MVC web framework for Node.js","s":[["TAGLINE","MVC web framework for Node.js"],["TLDR","**Create new app**\n\n```sails new [project-name]```\n\n**Start development server**\n\n```sails lift```\n\n**Generate API**\n\n```sails generate api [user]```\n\n**Generate model**\n\n```sails generate model [user]```\n\n**Generate controller**\n\n```sails generate controller [user]```\n\n**Open console**\n\n```sails console```\n\n**Show version**\n\n```sails --version```"],["SYNOPSIS","**sails** _command_ [_options_] [_args_]"],["PARAMETERS","**new** _NAME_\n> Create new app.\n\n**lift**\n> Start server.\n\n**generate** _TYPE_ _NAME_\n> Generate component.\n\n**console**\n> Interactive REPL.\n\n**debug**\n> Start app in debug mode.\n\n**--version**\n> Show installed Sails version.\n\n**--port** _PORT_\n> Set server port.\n\n**--prod**\n> Start in production mode."],["DESCRIPTION","**Sails.js** is an MVC web framework for Node.js, inspired by Ruby on Rails. It provides conventions and scaffolding for rapidly building REST APIs and web applications with a familiar structure of models, views, and controllers.\n\nA standout feature is Blueprints, which auto-generate RESTful API endpoints for models with full CRUD operations. Combined with the Waterline ORM, which supports MySQL, PostgreSQL, MongoDB, and other databases through a unified interface, applications can be prototyped with minimal code.\n\nBuilt-in Socket.io integration enables real-time features, allowing Blueprint routes to work over both HTTP and WebSockets. Policies provide middleware-like access control for protecting routes and enforcing authentication."],["CAVEATS","Framework has learning curve. Configuration can be complex. Performance tuning may be needed."],["HISTORY","**Sails.js** was created by **Mike McNeil** and released in **2012**. Inspired by Rails, it brings convention-over-configuration to Node.js development."],["SEE ALSO","[node](/man/node)(1), [npm](/man/npm)(1), [express](/man/express)(1)"]],"e":["node","npm","express"],"x":"sails new [project-name]"},"systemctl-unset-environment":{"n":"systemctl-unset-environment","t":"Remove service manager environment variables","s":[["TAGLINE","Remove service manager environment variables"],["TLDR","**Unset** environment variable\n\n```systemctl unset-environment [VAR]```\n\nUnset **multiple** variables\n\n```systemctl unset-environment [VAR1 VAR2 ...]```\n\nUnset in **user** manager\n\n```systemctl unset-environment [VAR] --user```"],["SYNOPSIS","**systemctl unset-environment** _VARIABLE_..."],["PARAMETERS","**--user**\n> Unset in user service manager environment\n\n**--system**\n> Unset in system manager environment (default)"],["DESCRIPTION","**systemctl unset-environment** removes environment variables from the systemd service manager's environment. This undoes the effect of `systemctl set-environment`.\n\nServices started after the unset will not receive the removed variables."],["CAVEATS","Only affects services started after the change. Already running services keep their environment. Changes are temporary and lost on reboot."],["HISTORY","The **unset-environment** subcommand complements `set-environment`, providing complete runtime control over the service manager's environment."],["SEE ALSO","[systemctl-set-environment](/man/systemctl-set-environment)(1), [systemctl-show-environment](/man/systemctl-show-environment)(1), [systemctl](/man/systemctl)(1)"]],"e":["systemctl-set-environment","systemctl-show-environment","systemctl"],"x":"systemctl unset-environment [VAR]"},"systemd-delta":{"n":"systemd-delta","t":"Find overridden configuration files","s":[["TAGLINE","Find overridden configuration files"],["TLDR","Show **all** overridden configuration files\n\n```systemd-delta```\n\nShow only **specific types**\n\n```systemd-delta -t [masked|equivalent|redirected|overridden|extended|unchanged]```\n\nShow overrides in specific **directory**\n\n```systemd-delta [/etc|/run|/usr/lib]```\n\nShow overrides for specific **category**\n\n```systemd-delta [tmpfiles.d|sysctl.d|systemd/system]```"],["SYNOPSIS","**systemd-delta** [_OPTIONS_] [_PREFIX_|_PREFIX/SUFFIX_|_SUFFIX_...]"],["PARAMETERS","**-t, --type=** _TYPE_\n> Filter by difference type (masked, equivalent, redirected, overridden, extended, unchanged)\n\n**--diff=** _BOOL_\n> Toggle diff display for modified files (default: true)\n\n**--no-pager**\n> Suppress pagination of output\n\n**-h, --help**\n> Display help"],["DESCRIPTION","**systemd-delta** identifies and compares configuration files that override other configuration files. It helps understand configuration precedence across system directories.\n\nThe priority hierarchy places `/etc/` highest, `/run/` second, and `/usr/lib/` lowest. Files can be overridden by placing identically-named files in higher-priority directories. Drop-in directories with `.d` suffix can extend configuration files."],["DIFFERENCE TYPES","**masked** - Files in higher-priority locations completely hide lower ones (symlink to /dev/null)\n\n**equivalent** - Identical content in multiple locations\n\n**redirected** - Symlink pointing elsewhere\n\n**overridden** - File in higher-priority location replaces lower one\n\n**extended** - Drop-in files augment the original\n\n**unchanged** - No overrides present"],["CAVEATS","Only shows differences for systemd-managed configuration types (unit files, tmpfiles.d, sysctl.d, etc.). Does not analyze arbitrary configuration files. The diff output can be verbose for heavily customized systems."],["HISTORY","**systemd-delta** is part of the **systemd** project. It addresses the common administrative challenge of understanding which configuration files are in effect when multiple sources provide overlapping configuration."],["INSTALL","```apt: sudo apt install systemd```\n\n```dnf: sudo dnf install systemd```\n\n```pacman: sudo pacman -S systemd```\n\n```zypper: sudo zypper install systemd```\n\n```brew: brew install systemd```\n\n```nix: nix profile install nixpkgs#systemd```"],["SEE ALSO","[systemctl](/man/systemctl)(1), [systemd-analyze](/man/systemd-analyze)(1)"]],"e":["systemctl","systemd-analyze"],"x":"systemd-delta"},"tor":{"n":"tor","t":"Anonymizing onion routing network daemon","s":[["TAGLINE","Anonymizing onion routing network daemon"],["TLDR","Start **Tor** client\n\n```tor```\n\nUse specific **configuration file**\n\n```tor -f /etc/tor/torrc```\n\n**Verify** configuration syntax\n\n```tor --verify-config```\n\nGenerate **hashed password** for control port\n\n```tor --hash-password mypassword```\n\nDisplay **version** information\n\n```tor --version```"],["SYNOPSIS","**tor** [_OPTION_ _value_]..."],["DESCRIPTION","**tor** is a connection-oriented anonymizing communication service. Users establish source-routed paths through a distributed network of relays where each node only knows its neighboring nodes, not the full path. This enables anonymous TCP stream routing for web browsing, FTP, SSH, and other services."],["PARAMETERS","**-h, --help**\n> Display help message and exit\n\n**-f, --torrc-file FILE**\n> Specify configuration file location\n\n**--verify-config**\n> Verify configuration file syntax and exit\n\n**--hash-password PASSWORD**\n> Generate hashed control port password\n\n**--version**\n> Display version information\n\n**--keygen**\n> Generate ed25519 master identity key for relays\n\n**--list-fingerprint**\n> Generate keys and output fingerprint\n\n**--quiet**\n> Only log errors during startup"],["CONFIGURATION","**/etc/tor/torrc**\n> Main configuration file specifying SOCKS port, relay settings, hidden services, bridge configuration, and logging options.\n\n**-f** _file_\n> Use an alternate configuration file at runtime."],["CAVEATS","By default, Tor runs as a client-only service. The SOCKS protocol is unencrypted and unauthenticated, so exposing it to untrusted networks could leak information. Configuration files use \"OptionName OptionValue\" format while command-line uses \"--OptionName OptionValue\" format."],["HISTORY","**tor** is \"The Onion Router,\" a second-generation anonymity network originally developed by the U.S. Naval Research Laboratory and now maintained by The Tor Project."],["INSTALL","```apt: sudo apt install tor```\n\n```dnf: sudo dnf install tor```\n\n```pacman: sudo pacman -S tor```\n\n```apk: sudo apk add tor```\n\n```zypper: sudo zypper install tor```\n\n```brew: brew install tor```\n\n```nix: nix profile install nixpkgs#tor```"],["SEE ALSO","[torsocks](/man/torsocks)(1), [torify](/man/torify)(1), [privoxy](/man/privoxy)(8)"]],"e":["torsocks","torify","privoxy"],"x":"tor"},"torchrun":{"n":"torchrun","t":"PyTorch distributed training launcher","s":[["TAGLINE","PyTorch distributed training launcher"],["TLDR","**Run distributed training** on a single node with 4 GPUs\n\n```torchrun --standalone --nproc_per_node=4 [train.py]```\n\n**Run multi-node training** with 2 nodes and 4 GPUs each\n\n```torchrun --nnodes=2 --nproc_per_node=4 --rdzv_endpoint=[master_ip:29500] [train.py]```\n\n**Run with specific rendezvous backend**\n\n```torchrun --nnodes=2 --nproc_per_node=4 --rdzv_backend=c10d --rdzv_endpoint=[master_ip:29500] [train.py]```\n\n**Run with fault tolerance** allowing restarts\n\n```torchrun --nnodes=2 --nproc_per_node=4 --max_restarts=3 --rdzv_endpoint=[master_ip:29500] [train.py]```\n\n**Run single GPU training** (equivalent to regular python)\n\n```torchrun --standalone --nproc_per_node=1 [train.py]```"],["SYNOPSIS","**torchrun** [**--nnodes** _N_] [**--nproc_per_node** _N_] [**--rdzv_backend** _backend_] [**--rdzv_endpoint** _host:port_] [**--standalone**] _script.py_ [_script_args_]"],["PARAMETERS","**--nnodes** _min_:max_ or _N_\n> Number of nodes participating in training. Can be a range for elastic training.\n\n**--nproc_per_node** _N_\n> Number of processes to spawn per node. Typically equals the number of GPUs.\n\n**--standalone**\n> Single-node mode without external rendezvous. Sets up local rendezvous automatically.\n\n**--rdzv_backend** _backend_\n> Rendezvous backend: c10d (default), etcd, etcd-v2, or static.\n\n**--rdzv_endpoint** _host:port_\n> Rendezvous endpoint address. For c10d, the master node's IP and port.\n\n**--rdzv_id** _id_\n> User-defined ID for the rendezvous group. All nodes must use the same ID.\n\n**--max_restarts** _N_\n> Maximum number of worker group restarts on failure. Default is 0.\n\n**--node_rank** _N_\n> Rank of this node (for static rendezvous).\n\n**--master_addr** _addr_\n> Master node address (legacy, use --rdzv_endpoint instead).\n\n**--master_port** _port_\n> Master node port (legacy, use --rdzv_endpoint instead).\n\n**--local-addr** _addr_\n> Local address to bind to. Defaults to localhost.\n\n**--redirects** _N_\n> Redirect stdout and stderr for each worker to log files. Format: 0:1,1:2 redirects stdout of worker 0 to file 1, etc.\n\n**--tee** _N_\n> Tee stdout/stderr to both console and log files. Same format as --redirects.\n\n**--log-dir** _dir_\n> Directory for log files when using --redirects or --tee."],["DESCRIPTION","**torchrun** is PyTorch's distributed training launcher that replaces the deprecated torch.distributed.launch. It spawns multiple processes across GPUs and nodes, setting up the distributed environment for training neural networks at scale.\n\nThe launcher supports various distributed strategies including Data Distributed Parallel (DDP), Fully Sharded Data Parallel (FSDP), tensor parallelism, and hybrid approaches. It automatically sets environment variables like RANK, WORLD_SIZE, LOCAL_RANK, MASTER_ADDR, and MASTER_PORT for distributed communication.\n\nFor single-node multi-GPU training, use **--standalone** mode. For multi-node training, all nodes must specify the same rendezvous endpoint where they coordinate. The launcher supports elastic training with dynamic node counts and fault tolerance with automatic restarts when workers fail."],["CAVEATS","Training scripts must be written to handle distributed setup using torch.distributed. The number of processes per node should not exceed available GPUs for GPU training. All nodes in a distributed job must use compatible PyTorch versions and NCCL configurations. Network firewalls must allow communication on the rendezvous port."],["HISTORY","**torchrun** was introduced in **PyTorch 1.10** (October 2021) as part of the TorchElastic project, replacing the older torch.distributed.launch module. It was designed to provide elastic and fault-tolerant distributed training capabilities. From **PyTorch 2.0** (March 2023), the command-line argument style changed from underscores to dashes (--local-rank instead of --local_rank)."],["INSTALL","```apt: sudo apt install python3-torch```\n\n```dnf: sudo dnf install python3-torch```"],["SEE ALSO","[python](/man/python)(1), [nvidia-smi](/man/nvidia-smi)(1)"]],"e":["python","nvidia-smi"],"x":"torchrun --standalone --nproc_per_node=4 [train.py]"},"twine":{"n":"twine","t":"Upload Python packages to PyPI","s":[["TAGLINE","Upload Python packages to PyPI"],["TLDR","**Upload package to PyPI**\n\n```twine upload dist/*```\n\n**Upload to TestPyPI**\n\n```twine upload --repository testpypi dist/*```\n\n**Upload with credentials**\n\n```twine upload -u [username] -p [password] dist/*```\n\n**Check package before upload**\n\n```twine check dist/*```\n\n**Upload specific files**\n\n```twine upload [dist/package-1.0.tar.gz]```\n\n**Upload with verbose output**\n\n```twine upload --verbose dist/*```"],["SYNOPSIS","**twine** [_upload_] [_check_] [_-r repository_] [_-u user_] [_options_] _files_"],["PARAMETERS","**upload** _FILES_\n> Upload packages.\n\n**check** _FILES_\n> Check package metadata.\n\n**-r** _REPO_, **--repository** _REPO_\n> Target repository (pypi, testpypi, or URL).\n\n**--repository-url** _URL_\n> Repository URL.\n\n**-u** _USER_, **--username** _USER_\n> Username.\n\n**-p** _PASS_, **--password** _PASS_\n> Password.\n\n**--non-interactive**\n> Don't prompt for credentials.\n\n**-s**, **--sign**\n> Sign files with GPG.\n\n**-c** _FILE_, **--config-file** _FILE_\n> Config file.\n\n**--skip-existing**\n> Skip already-uploaded files.\n\n**--verbose**\n> Verbose output."],["DESCRIPTION","**twine** uploads Python packages to PyPI and other package indexes. It's the recommended tool for publishing Python packages.\n\nPackage files (.tar.gz and .whl) in the dist directory are uploaded. These are created by build tools like build or setuptools.\n\nCheck mode validates package metadata before upload. It catches common issues that would cause upload failure.\n\nTestPyPI allows testing the upload process without affecting the real PyPI. Packages can be installed from TestPyPI to verify.\n\nAuthentication uses username/password, API tokens, or keyring integration. API tokens are recommended for security.\n\nHTTPS is always used for uploads. Previous tools allowed insecure uploads, which twine explicitly prevents."],["CONFIGURATION","**~/.pypirc**\n> Repository credentials and URLs for PyPI, TestPyPI, and custom indexes.\n\n**TWINE_USERNAME**\n> Environment variable for authentication username.\n\n**TWINE_PASSWORD**\n> Environment variable for authentication password or API token."],["CAVEATS","Package names are permanent on PyPI. Version numbers can't be reused. Credentials need secure handling. Package must pass validation."],["HISTORY","**Twine** was created by the Python Packaging Authority around **2013** to replace the insecure upload mechanism in setuptools. It ensures all uploads use HTTPS."],["INSTALL","```dnf: sudo dnf install twine```\n\n```pacman: sudo pacman -S twine```\n\n```apk: sudo apk add twine```\n\n```brew: brew install twine```\n\n```nix: nix profile install nixpkgs#twine```"],["SEE ALSO","[pip](/man/pip)(1), [build](/man/build)(1), [flit](/man/flit)(1)"]],"e":["pip","build","flit"],"x":"twine upload dist/*"},"urlview":{"n":"urlview","t":"Interactive URL selector from text files","s":[["TAGLINE","Interactive URL selector from text files"],["TLDR","**Extract URLs from a file** and display interactive menu\n\n```urlview [path/to/file]```\n\n**Extract URLs from multiple files**\n\n```urlview [file1] [file2] [file3]```\n\n**Extract URLs from stdin** (e.g., piped from mutt)\n\n```cat [path/to/email.txt] | urlview```\n\n**Read from stdin** explicitly using dash\n\n```command | urlview -```\n\n**Jump to a specific line number** in the URL list\n\n```urlview -[5] [path/to/file]```"],["SYNOPSIS","**urlview** _filename_ [_filename_...]"],["PARAMETERS","_filename_\n> One or more text files to scan for URLs. If no files specified, reads from stdin. Use `-` to explicitly read from stdin.\n\n**-**_number_\n> Jump to the specified line number in the URL list."],["DESCRIPTION","**urlview** is a screen-oriented utility that extracts URLs from text files and displays an interactive menu for launching them in a browser. It scans input using a regular expression to identify URLs and presents a navigable list.\n\nThe tool is commonly used with terminal email clients like **mutt**, allowing users to quickly access links embedded in messages. When a URL is selected from the menu, urlview launches the configured browser or URL handler.\n\nConfiguration is read from ~/.urlview or /etc/urlview/system.urlview. The configuration file supports the following directives: REGEXP (custom URL matching pattern), COMMAND (URL launch command with %s placeholder), WRAP (yes/no for long URL wrapping), QUITONLAUNCH (exit after launching a URL).\n\nThe default handler is url_handler.sh, but the BROWSER environment variable can override this with a browser command or colon-delimited list of browsers to try."],["CAVEATS","The default URL regular expression may not match all valid URL formats. Complex URLs with special characters may require configuration adjustments. urlview works best with plain text; for emails with MIME encoding, consider **urlscan** which handles quoted-printable and base64 encodings."],["HISTORY","**urlview** was originally written by **Michael Elkins**, the creator of the mutt email client, in the late **1990s**. It was designed to complement mutt by providing easy URL handling in a terminal environment. The tool has been maintained by various contributors and remains a standard utility for terminal-based email workflows."],["INSTALL","```apt: sudo apt install urlview```\n\n```dnf: sudo dnf install urlview```\n\n```apk: sudo apk add urlview```\n\n```zypper: sudo zypper install urlview```\n\n```brew: brew install urlview```"],["SEE ALSO","[urlscan](/man/urlscan)(1), [mutt](/man/mutt)(1), [lynx](/man/lynx)(1), [xdg-open](/man/xdg-open)(1)"]],"e":["urlscan","mutt","lynx","xdg-open"],"x":"urlview [path/to/file]"},"vsce":{"n":"vsce","t":"VS Code extension manager CLI","s":[["TAGLINE","VS Code extension manager CLI"],["TLDR","**Package** an extension into a .vsix file\n\n```vsce package```\n\n**Publish** an extension to the Marketplace\n\n```vsce publish```\n\n**Publish with an automatic** version bump\n\n```vsce publish [patch|minor|major]```\n\n**Log in** as a publisher\n\n```vsce login [publisher_name]```\n\n**Verify a Personal Access Token**\n\n```vsce verify-pat [publisher_name]```\n\n**Show metadata** for an extension\n\n```vsce show [publisher.extension]```\n\n**List files** that would be included in the package\n\n```vsce ls```\n\n**Unpublish** an extension\n\n```vsce unpublish [publisher.extension]```"],["SYNOPSIS","**vsce** _command_ [_options_]"],["PARAMETERS","**package**\n> Package the extension into a .vsix file.\n\n**publish** [_VERSION_]\n> Publish the extension to the Marketplace. _VERSION_ may be `major`, `minor`, `patch`, or an explicit version.\n\n**login** _PUBLISHER_\n> Store credentials for a publisher.\n\n**logout** _PUBLISHER_\n> Remove a publisher's stored credentials.\n\n**verify-pat** [_PUBLISHER_]\n> Verify that the Personal Access Token has publish rights.\n\n**show** _EXTENSION_\n> Show metadata (downloads, ratings, versions) for an extension.\n\n**search** _TEXT_\n> Search the Marketplace for extensions.\n\n**ls**\n> List files that would be packaged or published.\n\n**ls-publishers**\n> List known publishers.\n\n**delete-publisher** _NAME_\n> Delete a publisher.\n\n**unpublish** [_EXTENSION_]\n> Remove an extension from the Marketplace.\n\n**-p**, **--pat** _TOKEN_\n> Personal Access Token used to authenticate.\n\n**-t**, **--target** _TARGET_\n> Target platform (e.g., `linux-x64`, `win32-x64`).\n\n**-o**, **--out** _PATH_\n> Output file or directory for the packaged .vsix.\n\n**--pre-release**\n> Mark the package or publication as a pre-release.\n\n**--no-yarn** / **--yarn**\n> Force use of npm or Yarn for dependency installation.\n\n**--skip-duplicate**\n> Skip publishing when the same version already exists."],["DESCRIPTION","**vsce** (Visual Studio Code Extension Manager) is Microsoft's official CLI for packaging, publishing, and managing VS Code extensions on the Visual Studio Marketplace. It handles the full lifecycle of extension development from local packaging to public distribution.\n\nThe `package` command bundles an extension project into a `.vsix` file that can be installed locally or shared directly. The `publish` command uploads the packaged extension to the Marketplace, with optional automatic version bumping using semver increments (major, minor, patch).\n\nA publisher identity is required for Marketplace operations. Authentication uses Azure DevOps Personal Access Tokens. Additional commands handle publisher credentials, inspection of package contents, and querying Marketplace metadata."],["CAVEATS","Requires a Marketplace account and an Azure DevOps PAT with the Marketplace (Manage) scope. Extensions must follow the Marketplace publishing guidelines. The package is typically installed via `npm install -g @vscode/vsce`."],["HISTORY","**vsce** (Visual Studio Code Extension Manager) is developed by **Microsoft** and distributed on npm as `@vscode/vsce`. It has been the primary tool for packaging and publishing VS Code extensions since the Marketplace launched."],["INSTALL","```brew: brew install vsce```\n\n```nix: nix profile install nixpkgs#vsce```"],["SEE ALSO","[code](/man/code)(1), [npm](/man/npm)(1)"]],"e":["code","npm"],"x":"vsce package"},"warp":{"n":"warp","t":"Secure and simple terminal sharing","s":[["TAGLINE","Secure and simple terminal sharing"],["TLDR","**Share your terminal**\n\n```warp open [session-name]```\n\n**Connect to a shared terminal**\n\n```warp connect [session-name]```\n\n**Grant write access to a connected user**\n\n```warp authorize [username]```"],["SYNOPSIS","**warp** _command_ [_options_]"],["DESCRIPTION","**warp** enables secure terminal sharing with a simple command. One user opens a session and others can connect to view the terminal in real-time. Connections are TLS-encrypted and read-only by default, with explicit authorization required for write access. No SSH or shared server needed."],["HISTORY","**warp** was created by **spolu** and is written in **Go**."],["INSTALL","```apt: sudo apt install libghc-wai-app-static-dev```\n\n```pacman: sudo pacman -S warp```\n\n```apk: sudo apk add warp```\n\n```zypper: sudo zypper install warp```\n\n```nix: nix profile install nixpkgs#warp```"],["SEE ALSO","[tmux](/man/tmux)(1), [screen](/man/screen)(1)"]],"e":["tmux","screen"],"x":"warp open [session-name]"},"xkill":{"n":"xkill","t":"Force-close X11 windows by clicking","s":[["TAGLINE","Force-close X11 windows by clicking"],["TLDR","**Kill window by clicking**\n\n```xkill```\n\n**Kill specific window ID**\n\n```xkill -id [window_id]```\n\n**Kill all with matching name**\n\n```xkill -frame```\n\n**Show button to use**\n\n```xkill -button any```"],["SYNOPSIS","**xkill** [_-id window_id_] [_-button button_] [_-frame_] [_options_]"],["PARAMETERS","**-id** _WINDOWID_\n> Kill specific window by ID.\n\n**-button** _BUTTON_\n> Button to use (any, 1, 2, 3).\n\n**-frame**\n> Include window frame.\n\n**-all**\n> Kill all matching clients.\n\n**-display** _DISPLAY_\n> X server display."],["DESCRIPTION","**xkill** forcibly closes X11 windows. After running, the cursor changes and the next clicked window is killed.\n\nThe tool sends a kill request to the X server. This is more forceful than asking the window to close normally.\n\nWindow manager frames can be targeted. Using -frame kills the window even if clicking the decoration rather than content.\n\nAny mouse button can trigger the kill. By default, only button 1 (left click) works. The -button option changes this.\n\nPressing any key cancels xkill without killing a window. This provides an escape if started accidentally.\n\nThe tool is useful when applications become unresponsive and normal close methods fail."],["CAVEATS","X11 only - doesn't work on Wayland. May lose unsaved data. Doesn't gracefully close applications. Some windows may resist."],["HISTORY","**xkill** has been part of the **X.Org** utilities since the early days of X11. It provides a simple way to deal with misbehaving X applications."],["INSTALL","```apt: sudo apt install x11-utils```\n\n```dnf: sudo dnf install xkill```\n\n```apk: sudo apk add xkill```\n\n```zypper: sudo zypper install xkill```\n\n```nix: nix profile install nixpkgs#xkill```"],["SEE ALSO","[kill](/man/kill)(1), [killall](/man/killall)(1), [xwininfo](/man/xwininfo)(1), [xdotool](/man/xdotool)(1)"]],"e":["kill","killall","xwininfo","xdotool"],"x":"xkill"},"xmrig":{"n":"xmrig","t":"Cryptocurrency mining software","s":[["TAGLINE","Cryptocurrency mining software"],["TLDR","**Mine to a pool** with wallet address\n\n```xmrig -o [pool.example.com:3333] -u [wallet_address]```\n\n**Mine using config file**\n\n```xmrig --config [config.json]```\n\n**Run benchmark**\n\n```xmrig --bench [1M]```\n\n**Mine with specific CPU threads**\n\n```xmrig -o [pool:port] -u [wallet] -t [4]```\n\n**Run in background**\n\n```xmrig -o [pool:port] -u [wallet] -B```\n\n**Enable CUDA** (NVIDIA GPU)\n\n```xmrig -o [pool:port] -u [wallet] --cuda```\n\n**Enable OpenCL** (AMD GPU)\n\n```xmrig -o [pool:port] -u [wallet] --opencl```\n\n**Dry run** to test configuration\n\n```xmrig --config [config.json] --dry-run```"],["SYNOPSIS","**xmrig** [_options_]\n\n**xmrig** -o _pool_ -u _wallet_ [_options_]"],["PARAMETERS","**-o** _URL_, **--url** _URL_\n> Mining pool URL (host:port)\n\n**-u** _WALLET_, **--user** _WALLET_\n> Wallet address or username\n\n**-p** _PASS_, **--pass** _PASS_\n> Password for pool\n\n**-t** _NUM_, **--threads** _NUM_\n> Number of CPU threads\n\n**-c** _FILE_, **--config** _FILE_\n> JSON configuration file\n\n**-B**, **--background**\n> Run in background (daemon mode)\n\n**--cuda**\n> Enable CUDA backend (NVIDIA)\n\n**--opencl**\n> Enable OpenCL backend (AMD)\n\n**--no-cpu**\n> Disable CPU mining\n\n**--cuda-devices** _LIST_\n> Comma-separated CUDA device indices\n\n**--opencl-devices** _LIST_\n> Comma-separated OpenCL device indices\n\n**--donate-level** _N_\n> Donation percentage (default: 1%)\n\n**--randomx-1gb-pages**\n> Use 1GB hugepages (Linux, requires setup)\n\n**--bench** _N_\n> Run benchmark (1M to 10M hashes)\n\n**--stress**\n> Continuous stress test mode\n\n**--dry-run**\n> Test configuration without mining\n\n**-V**, **--version**\n> Show version\n\n**-h**, **--help**\n> Show help"],["DESCRIPTION","**xmrig** is a high-performance cryptocurrency miner supporting RandomX (Monero), KawPow (Ravencoin), CryptoNight, and GhostRider algorithms. It supports CPU and GPU mining on Windows, Linux, macOS, and FreeBSD.\n\nConfiguration is preferably done via JSON config file, which supports features not available via command line like multiple pools and mining profiles. The miner automatically selects optimal settings based on hardware detection.\n\nFor RandomX mining on Linux, enabling hugepages significantly improves performance. The miner includes a 1% default donation to developers, which can be adjusted via configuration."],["CAVEATS","Cryptocurrency mining consumes significant electricity and generates heat. Profitability depends on hardware, electricity costs, and cryptocurrency prices. Some cloud providers and ISPs prohibit mining. Running at full capacity may reduce hardware lifespan. Ensure adequate cooling for sustained operation."],["HISTORY","XMRig was created as an open-source Monero miner, originally focusing on CPU mining with the CryptoNight algorithm. When Monero switched to the RandomX algorithm in **2019**, XMRig was updated to support it. The project expanded to include GPU support via CUDA and OpenCL backends. XMRig became one of the most widely used miners due to its performance, cross-platform support, and active development."],["INSTALL","```apt: sudo apt install xmrig```\n\n```pacman: sudo pacman -S xmrig```\n\n```apk: sudo apk add xmrig```\n\n```zypper: sudo zypper install xmrig```\n\n```brew: brew install xmrig```\n\n```nix: nix profile install nixpkgs#xmrig```"],["SEE ALSO","[cgminer](/man/cgminer)(1), [bfgminer](/man/bfgminer)(1)"]],"e":["cgminer","bfgminer"],"x":"xmrig -o [pool.example.com:3333] -u [wallet_address]"},"xpdig":{"n":"xpdig","t":"TUI to explore Crossplane traces like k9s","s":[["TAGLINE","TUI to explore Crossplane traces like k9s"],["TLDR","**Launch the Crossplane trace explorer**\n\n```xpdig```\n\n**Specify a kubeconfig file**\n\n```xpdig --kubeconfig [~/.kube/config]```"],["SYNOPSIS","**xpdig** [_options_]"],["DESCRIPTION","**xpdig** provides a terminal UI similar to k9s for interactively exploring Crossplane resource traces. It supports expanded details with issue highlighting, the ability to get, describe, edit, and delete objects, and automatic watching for resource changes."],["HISTORY","**xpdig** was created by **Bruno Luiz Silva** (brunoluiz) and is written in **Go**."],["SEE ALSO","[k9s](/man/k9s)(1), [kubectl](/man/kubectl)(1)"]],"e":["k9s","kubectl"],"x":"xpdig"},"zm":{"n":"zm","t":"Manage ZoneMinder surveillance daemons","s":[["TAGLINE","Manage ZoneMinder surveillance daemons"],["TLDR","**Start the ZoneMinder system**\n\n```sudo zmpkg.pl start```\n\n**Stop the ZoneMinder system**\n\n```sudo zmpkg.pl stop```\n\n**Check system status**\n\n```zmpkg.pl status```\n\n**Start a specific daemon**\n\n```sudo zmdc.pl start [zmc] -m [monitor_id]```\n\n**Check daemon status**\n\n```zmdc.pl status```\n\n**Query monitor status**\n\n```zmu -m [monitor_id] -q```"],["SYNOPSIS","**zm** [_subcommand_]\n\n**zmpkg.pl** [**start**|**stop**|**restart**|**status**|**logrot**|**version**]\n\n**zmdc.pl** [**start**|**stop**|**status**|**check**|**startup**|**shutdown**] [_daemon_]"],["DESCRIPTION","**ZoneMinder** is a video camera security and surveillance system. The **zm** family of commands manages the ZoneMinder daemons and services.\n\nKey components:\n- **zmpkg.pl** - Package control (start/stop entire system)\n- **zmdc.pl** - Daemon control (manage individual daemons)\n- **zmu** - Monitor utility (query monitor status)\n- **zmwatch.pl** - Watchdog daemon\n- **zmaudit.pl** - Database audit tool\n\nZoneMinder consists of multiple daemons: capture daemons for each camera, analysis daemons for motion detection, and support services for web interface and event processing.\n\nThe primary interface is web-based, with these commands providing backend management and troubleshooting capabilities."],["CAVEATS","ZoneMinder requires a database (MySQL/MariaDB) and web server to function. Command-line tools manage daemons but configuration is done via the web interface.\n\nCamera configuration, zone setup, and event management are handled through the web UI, not command line.\n\nDaemon management typically requires root privileges.\n\nResource usage scales with the number of cameras and analysis complexity."],["SEE ALSO","[motion](/man/motion)(1), [ffmpeg](/man/ffmpeg)(1), [systemctl](/man/systemctl)(1)"]],"e":["motion","ffmpeg","systemctl"],"x":"sudo zmpkg.pl start"}});
